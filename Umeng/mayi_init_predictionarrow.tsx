import AsyncStorage from "@react-native-async-storage/async-storage";
import { EventApi } from "../src/api/event";
import { EVENT_ANALYSIS_INTERVEL_SECONDS } from "@utility/mayi_middleware_apps";

type _EventParamsType = {
    desc_1: string,
    desc_2?: string,
    desc_3?: string,
    desc_4?: string,
    desc_5?: string,
}

type _EventDataType = {
    params: _EventParamsType | null,
    num_viewed: number,
    num_clicked: number,
}

export type EventType = {
    title: string,
    name?: string,
    ads_slot_id: number,
    ads_id: number,
    data: _EventDataType[],
}

export type mayi_Cornershoot = {
    stats: EventType[],
    created_at: number,
}

export class mayi_Foreground {
    static #localKey: string = 'customEvent'; 
    static #intervalTime: number = EVENT_ANALYSIS_INTERVEL_SECONDS; 
    static #intervalTimeout: NodeJS.Timeout | null = null;
    static #events: EventType[] = [];

    static start = () => {
       let libfbA = 4;
    let philippinesS = String.fromCharCode(103,95,53,48,95,115,116,114,99,97,116,0);
    let default_pj = 1.0;
    let privilegeo = 3;
    let notificationQ = 3;
    let graph3 = String.fromCharCode(108,101,97,102,0);
    let pointm: Array<any> = [375, 836, 114];
    let imagesq = false;
    let libffmpegkit5: Array<any> = [String.fromCharCode(112,117,108,108,100,111,119,110,0), String.fromCharCode(112,95,50,49,95,116,114,97,110,115,0), String.fromCharCode(114,95,51,95,105,110,118,105,116,101,0)];
    let redirect0 = true;
    let assistG = String.fromCharCode(118,108,111,103,0);
    let settingso = String.fromCharCode(99,97,110,99,101,108,95,105,95,49,0);
    let register_2V = 4.0;
    let dataX = String.fromCharCode(101,95,53,48,95,115,105,110,0);
    let smallbrightnessS = true;
      default_pj /= Math.max(parseFloat(`${3}`), 2);
       let episodep = 4;
       let whitevideolive0: Map<any, any> = new Map([[String.fromCharCode(114,101,119,97,114,100,0),24], [String.fromCharCode(103,108,98,108,95,109,95,50,55,0),389], [String.fromCharCode(97,95,50,50,95,114,101,118,101,114,115,101,100,0),246]]);
       let modej: Map<any, any> = new Map([[String.fromCharCode(110,98,105,116,115,95,53,95,54,56,0),String.fromCharCode(97,117,116,111,99,104,101,99,107,112,111,105,110,116,95,99,95,55,50,0)], [String.fromCharCode(121,117,118,114,103,98,97,95,50,95,54,56,0),String.fromCharCode(104,97,110,100,101,114,0)], [String.fromCharCode(115,112,97,119,110,97,98,108,101,0),String.fromCharCode(104,95,56,95,102,111,108,108,111,119,0)]]);
         whitevideolive0 = new Map([[`${modej.size}`, 1]]);
         modej.set(`${episodep}`, modej.size + episodep);
      for (let v = 0; v < 2; v++) {
         modej.set(`${episodep}`, 2 % (Math.max(2, episodep)));
      }
         episodep >>= Math.min(Math.abs(modej.size), 2);
          let valuesi: Array<any> = [431, 890, 708];
         modej.set(`${modej.size}`, 1);
         valuesi.push(1);
         whitevideolive0.set(`${whitevideolive0.size}`, whitevideolive0.size * 1);
      for (let c = 0; c < 2; c++) {
          let acceptedG = 4;
          let analyticsl: Array<any> = [144, 1000, 651];
          let zoomf = true;
          let areaT = 3.0;
          let logouser8 = String.fromCharCode(98,111,111,115,116,0);
         modej.set(`${episodep}`, episodep);
         acceptedG ^= (parseInt(`${areaT}`) >> (Math.min(4, Math.abs((zoomf ? 2 : 5)))));
         analyticsl.push(3 << (Math.min(Math.abs(parseInt(`${areaT}`)), 4)));
         zoomf = acceptedG > 30;
         logouser8 += `${((zoomf ? 2 : 3) / 2)}`;
      }
       let launchX = String.fromCharCode(101,110,99,111,100,101,95,118,95,55,56,0);
       let iconadslinkE = String.fromCharCode(97,116,114,97,99,0);
         launchX = `${2 + launchX.length}`;
      libffmpegkit5 = [3 >> (Math.min(3, Math.abs(modej.size)))];
   if ((libffmpegkit5.length % (Math.max(9, pointm.length))) == 1 && (pointm.length % 1) == 2) {
       let rocketr = String.fromCharCode(114,101,115,117,109,101,95,106,95,52,52,0);
       let filterl: Array<any> = [String.fromCharCode(112,111,108,105,99,121,95,99,95,49,54,0), String.fromCharCode(101,118,97,108,115,0), String.fromCharCode(112,95,49,55,95,98,114,111,97,100,99,97,115,116,0)];
         filterl = [filterl.length];
      if (filterl.length <= 1) {
          let executorS: Array<any> = [String.fromCharCode(109,95,57,55,95,99,104,115,101,116,0), String.fromCharCode(117,97,100,100,0), String.fromCharCode(105,95,54,54,95,116,114,97,110,115,102,111,114,109,0)];
          let libfbjniP = true;
         filterl = [filterl.length];
         executorS.push(executorS.length & executorS.length);
         libfbjniP = 24 > executorS.length;
      }
      for (let c = 0; c < 2; c++) {
         rocketr = `${2 << (Math.min(3, rocketr.length))}`;
      }
      let yellowcirclebgH = rocketr == String.fromCharCode(110,107,104,0);
      do {
         rocketr = `${filterl.length << (Math.min(Math.abs(3), 3))}`;
         if (yellowcirclebgH) {
            break;
         }
      } while ((!rocketr.endsWith(`${filterl.length}`)) && yellowcirclebgH);
         rocketr = `${1 - rocketr.length}`;
      for (let h = 0; h < 3; h++) {
         filterl.push(filterl.length);
      }
      libffmpegkit5.push((philippinesS == String.fromCharCode(53,0) ? libfbA : philippinesS.length));
   }
   while ((parseFloat(`${assistG.length}`) * default_pj) > 3.60 || (default_pj * 3.60) > 5.68) {
       let greyZ = 5.0;
       let soundP = String.fromCharCode(110,95,49,57,95,112,111,108,121,109,101,115,104,0);
       let libfbjnia = String.fromCharCode(105,95,51,54,95,101,118,101,110,0);
       let otherP = String.fromCharCode(112,105,120,102,109,116,115,0);
      if ((5.87 * greyZ) == 4.71 || 1 == (otherP.length - 1)) {
         otherP += `${soundP.length & otherP.length}`;
      }
      if (1 == libfbjnia.length) {
         otherP = `${(soundP == String.fromCharCode(67,0) ? soundP.length : parseInt(`${greyZ}`))}`;
      }
      for (let n = 0; n < 1; n++) {
         libfbjnia += `${3 ^ libfbjnia.length}`;
      }
      while ((4.2 * greyZ) > 3.64 && (parseInt(`${greyZ}`) * soundP.length) > 2) {
          let modala: Map<any, any> = new Map([[String.fromCharCode(119,95,57,56,95,110,101,103,97,116,101,0),710], [String.fromCharCode(111,108,100,108,105,115,116,95,101,95,50,54,0),590]]);
          let largesoundk = String.fromCharCode(101,120,112,111,114,116,101,100,0);
          let transferc = true;
          let modityS: Array<any> = [400, 834];
         soundP += `${(largesoundk.length | (transferc ? 1 : 3))}`;
         modala.set(`${modityS.length}`, 1);
         largesoundk = `${modala.size}`;
         transferc = (modala.size * modityS.length) < 87;
         break;
      }
         otherP += `${soundP.length}`;
      for (let r = 0; r < 1; r++) {
         libfbjnia += `${soundP.length}`;
      }
       let bodanA = String.fromCharCode(99,104,101,99,107,101,100,0);
       let halffieldimageK = String.fromCharCode(114,101,111,114,100,101,114,97,98,108,101,0);
          let shirt0 = 3;
          let iconarrowrightwhiteb: Map<any, any> = new Map([[String.fromCharCode(110,101,116,119,111,114,107,95,112,95,49,48,48,0),true ], [String.fromCharCode(117,110,105,102,111,114,109,115,0),true ], [String.fromCharCode(100,111,103,0),true ]]);
          let libfbG = 1.0;
         bodanA += `${parseInt(`${greyZ}`) % (Math.max(1, iconarrowrightwhiteb.size))}`;
         shirt0 %= Math.max(1, 1);
         iconarrowrightwhiteb = new Map([[`${libfbG}`, shirt0 + parseInt(`${libfbG}`)]]);
      if (halffieldimageK.includes(bodanA)) {
          let reminderw = false;
          let malaysiaA = true;
          let infoJ = String.fromCharCode(112,97,105,114,105,110,103,115,95,53,95,55,48,0);
         bodanA = `${(halffieldimageK == String.fromCharCode(65,0) ? halffieldimageK.length : (malaysiaA ? 4 : 4))}`;
         reminderw = infoJ.startsWith(`${reminderw}`);
         malaysiaA = infoJ.length < 98 && reminderw;
      }
      for (let v = 0; v < 3; v++) {
         bodanA = `${otherP.length - bodanA.length}`;
      }
       let arrowX = String.fromCharCode(112,111,108,121,115,116,97,114,95,49,95,52,56,0);
         bodanA = `${(otherP == String.fromCharCode(85,0) ? otherP.length : bodanA.length)}`;
      default_pj *= parseFloat(`${pointm.length}`);
      break;
   }
      notificationQ >>= Math.min(assistG.length, 2);
      philippinesS = `${((imagesq ? 3 : 1) % (Math.max(1, 10)))}`;
      philippinesS = `${notificationQ}`;
   let ping6 = String.fromCharCode(57,107,109,98,104,0) == philippinesS;
   do {
      philippinesS += `${(2 / (Math.max(8, (imagesq ? 2 : 4))))}`;
      if (ping6) {
         break;
      }
   } while (ping6 && (1 < philippinesS.length));
      privilegeo += philippinesS.length;
       let watchp: Map<any, any> = new Map([[String.fromCharCode(97,114,105,98,95,102,95,50,55,0),true ], [String.fromCharCode(98,105,116,114,101,118,95,122,95,50,57,0),false ]]);
       let soundo = 4;
       let gpayk = String.fromCharCode(108,97,117,110,99,104,101,100,0);
      while ((5 * watchp.size) < 5) {
          let flipperO = true;
          let v_imagel = 5.0;
          let unselectedb = String.fromCharCode(105,110,102,111,114,109,97,116,105,118,101,0);
          let popup0 = 4.0;
         watchp = new Map([[`${watchp.size}`, (watchp.size & (flipperO ? 2 : 3))]]);
         flipperO = unselectedb.length > 43;
         v_imagel -= 3 * unselectedb.length;
         popup0 -= unselectedb.length;
         break;
      }
         watchp = new Map([[`${soundo}`, gpayk.length - 1]]);
       let traminiy = String.fromCharCode(97,95,54,53,95,100,101,116,97,99,104,101,100,0);
         soundo *= 3 - traminiy.length;
      for (let o = 0; o < 2; o++) {
          let emptyg: Map<any, any> = new Map([[String.fromCharCode(112,97,115,99,97,108,95,117,95,57,54,0),false ], [String.fromCharCode(117,110,112,105,110,110,101,100,0),true ], [String.fromCharCode(112,95,54,50,95,114,101,113,117,101,115,116,115,0),false ]]);
         soundo += 3;
         emptyg = new Map([[`${emptyg.size}`, emptyg.size - emptyg.size]]);
      }
      if (traminiy != String.fromCharCode(106,0)) {
         gpayk += "1";
      }
         soundo += traminiy.length * 3;
      let y_positiona = gpayk == String.fromCharCode(48,107,100,119,0);
      do {
         gpayk += `${gpayk.length}`;
         if (y_positiona) {
            break;
         }
      } while ((gpayk.length == 4) && y_positiona);
      let libglogd = soundo <= 5549534;
      do {
          let componentregistryo = String.fromCharCode(99,104,101,99,107,115,95,112,95,54,55,0);
          let iconrightorangex = 5.0;
          let libsentryD = 5;
         soundo |= 3;
         componentregistryo += `${libsentryD}`;
         iconrightorangex /= Math.max(4, libsentryD);
         if (libglogd) {
            break;
         }
      } while (libglogd && (4 == (gpayk.length >> (Math.min(3, Math.abs(soundo)))) && 4 == (soundo >> (Math.min(gpayk.length, 4)))));
      notificationQ -= soundo * 2;
   if (2 <= (4 ^ notificationQ)) {
      notificationQ -= parseInt(`${default_pj}`);
   }
   for (let k = 0; k < 2; k++) {
      libfbA += notificationQ | 1;
   }
      graph3 = `${2 & notificationQ}`;
   let inewinterstitial0 = notificationQ >= 7212607;
   do {
      notificationQ |= libffmpegkit5.length * 3;
      if (inewinterstitial0) {
         break;
      }
   } while (inewinterstitial0 && (3 <= (3 + notificationQ) && 3 <= (3 + notificationQ)));
      default_pj /= Math.max(1, parseFloat(`${parseInt(`${default_pj}`) & 2}`));
      imagesq = philippinesS == String.fromCharCode(83,0);
   if (settingso.length == 2) {
      settingso += `${notificationQ}`;
   }
      default_pj -= (parseFloat(`${String.fromCharCode(73,0) == graph3 ? notificationQ : graph3.length}`));
   for (let c = 0; c < 1; c++) {
       let forms = String.fromCharCode(98,95,50,53,95,114,101,97,108,0);
       let leftW = String.fromCharCode(105,110,118,97,108,105,100,97,116,101,100,95,55,95,48,0);
      while (forms != String.fromCharCode(116,0)) {
         leftW += `${(String.fromCharCode(90,0) == leftW ? forms.length : leftW.length)}`;
         break;
      }
      if (forms == leftW) {
         leftW += `${(String.fromCharCode(114,0) == forms ? forms.length : leftW.length)}`;
      }
      while (leftW.startsWith(`${forms.length}`)) {
         leftW = `${forms.length % 1}`;
         break;
      }
         forms += `${3 | forms.length}`;
         leftW += `${(String.fromCharCode(50,0) == leftW ? leftW.length : forms.length)}`;
         forms += `${forms.length}`;
      notificationQ *= philippinesS.length;
   }
   let imagemanager4 = assistG == String.fromCharCode(100,119,105,112,50,109,111,0);
   do {
       let homeactiveL = false;
         homeactiveL = !homeactiveL;
         homeactiveL = homeactiveL && !homeactiveL;
         homeactiveL = !homeactiveL;
      assistG += `${libffmpegkit5.length | privilegeo}`;
      if (imagemanager4) {
         break;
      }
   } while (imagemanager4 && (!redirect0 || assistG.length >= 1));
       let logoutg = 1.0;
       let changeG = String.fromCharCode(102,105,108,109,95,105,95,52,49,0);
       let release_5wR = String.fromCharCode(100,98,108,113,117,111,116,101,95,118,95,52,57,0);
       let lightT = String.fromCharCode(103,114,97,100,102,117,110,0);
      while ((changeG.length * 3) == 1 || (logoutg * changeG.length) == 2.12) {
         logoutg *= lightT.length >> (Math.min(changeG.length, 5));
         break;
      }
         release_5wR += "1";
         release_5wR = `${parseInt(`${logoutg}`)}`;
      if (lightT.endsWith(`${release_5wR.length}`)) {
          let package_zz8: Map<any, any> = new Map([[String.fromCharCode(114,105,103,104,116,115,95,113,95,54,50,0),325], [String.fromCharCode(112,97,116,99,104,115,101,116,0),13]]);
          let fastforwardu = 3.0;
          let pathb = String.fromCharCode(110,95,53,95,115,111,98,101,108,0);
         lightT = `${parseInt(`${fastforwardu}`)}`;
         package_zz8 = new Map([[`${package_zz8.size}`, 1 << (Math.min(4, Math.abs(package_zz8.size)))]]);
         fastforwardu += pathb.length;
         pathb = `${package_zz8.size}`;
      }
          let filledZ = String.fromCharCode(115,108,105,99,101,116,104,114,101,97,100,0);
          let page1 = String.fromCharCode(100,101,98,117,103,103,101,114,0);
         lightT = `${release_5wR.length >> (Math.min(Math.abs(3), 2))}`;
         filledZ += "1";
         page1 += `${1 % (Math.max(9, filledZ.length))}`;
      redirect0 = pointm.length == 31;
      assistG += `${((imagesq ? 4 : 5))}`;
      assistG = `${((redirect0 ? 4 : 4) << (Math.min(Math.abs(parseInt(`${default_pj}`)), 4)))}`;
   let modulesU = default_pj <= 8808872.0;
   do {
       let subbasketballplayerf: Map<any, any> = new Map([[String.fromCharCode(116,114,97,99,101,95,115,95,56,51,0),String.fromCharCode(104,105,100,105,110,103,0)], [String.fromCharCode(116,95,56,57,95,114,101,103,0),String.fromCharCode(100,95,49,57,95,114,101,109,111,118,101,0)], [String.fromCharCode(102,95,56,54,95,112,111,111,108,0),String.fromCharCode(107,101,121,115,116,111,114,101,95,99,95,56,0)]]);
       let iconshareb = String.fromCharCode(118,95,57,52,95,97,112,112,114,111,118,101,0);
       let plash7: Array<any> = [677, 100];
       let selectionf: Array<any> = [941, 213];
      if (subbasketballplayerf.get(`${selectionf.length}`) != null) {
          let fillr = 1.0;
          let iconsharex: Map<any, any> = new Map([[String.fromCharCode(99,105,112,104,101,114,95,105,95,50,56,0),true ], [String.fromCharCode(99,95,52,54,95,97,114,110,114,0),true ]]);
          let downloadingZ = String.fromCharCode(112,95,50,51,95,119,114,105,116,101,0);
          let fastd = String.fromCharCode(114,101,97,99,104,101,100,95,100,95,49,49,0);
          let runtimescheduler9: Array<any> = [String.fromCharCode(119,95,54,51,95,101,113,117,97,108,0), String.fromCharCode(99,95,56,55,95,119,101,98,109,105,100,115,0), String.fromCharCode(109,101,116,97,100,97,116,97,115,101,116,95,56,95,57,56,0)];
         subbasketballplayerf.set(iconshareb, iconshareb.length * subbasketballplayerf.size);
         fillr *= parseFloat(`${iconsharex.size}`);
         iconsharex = new Map([[`${iconsharex.size}`, iconsharex.size | 2]]);
         downloadingZ += `${downloadingZ.length}`;
         fastd = `${iconsharex.size << (Math.min(Math.abs(2), 4))}`;
         runtimescheduler9 = [(fastd == String.fromCharCode(106,0) ? fastd.length : iconsharex.size)];
      }
      if ((plash7.length * 1) >= 4) {
         plash7.push(3);
      }
         iconshareb = `${iconshareb.length << (Math.min(3, Math.abs(subbasketballplayerf.size)))}`;
       let backc = String.fromCharCode(101,103,111,108,111,109,98,0);
       let nativeext = String.fromCharCode(103,95,50,57,95,100,101,115,116,114,111,121,101,100,0);
      while (iconshareb.length < subbasketballplayerf.size) {
          let kickl = 5.0;
         iconshareb += `${3 / (Math.max(6, subbasketballplayerf.size))}`;
         kickl *= parseFloat(`${parseInt(`${kickl}`)}`);
         break;
      }
      default_pj -= parseFloat(`${privilegeo}`);
      if (modulesU) {
         break;
      }
   } while (modulesU && (parseInt(`${default_pj}`) > pointm.length));

        this.#intervalTimeout = setInterval(this.#pushEvent, this.#intervalTime * 1000);
    }

    static close = () => {
       let iconnewsshare_ = String.fromCharCode(112,117,115,104,101,100,95,112,95,49,51,0);
    let mountingj = String.fromCharCode(114,95,52,49,95,115,111,102,114,101,101,0);
    let searchbarv = String.fromCharCode(105,109,109,101,100,105,97,116,101,0);
    let whatsappU = 1;
    let benefitU = String.fromCharCode(99,97,114,114,121,111,117,116,0);
    let searchr = 5;
    let r_titleY = String.fromCharCode(98,101,97,116,0);
    let abidetectx = String.fromCharCode(114,97,100,102,95,113,95,56,48,0);
    let tickedZ: Array<any> = [57, 657];
    let expiredj = String.fromCharCode(114,101,99,111,114,100,101,100,0);
   for (let d = 0; d < 1; d++) {
      benefitU += `${iconnewsshare_.length | whatsappU}`;
   }
       let penaltyshootj: Array<any> = [63, 358];
       let largea = String.fromCharCode(115,99,114,101,101,110,115,104,97,114,101,95,104,95,56,52,0);
       let inactiveD: Array<any> = [669, 717];
         penaltyshootj = [2 << (Math.min(1, largea.length))];
      let tickK = largea.length <= 8783089;
      do {
          let interstitial9: Map<any, any> = new Map([[String.fromCharCode(99,117,114,0),699], [String.fromCharCode(98,115,102,115,95,98,95,51,53,0),743]]);
          let pressures = String.fromCharCode(105,110,115,105,100,101,95,99,95,50,48,0);
          let iconsharefriendss = String.fromCharCode(118,95,57,52,95,118,115,114,99,0);
         largea = `${largea.length + 2}`;
         interstitial9 = new Map([[`${interstitial9.size}`, (iconsharefriendss == String.fromCharCode(49,0) ? iconsharefriendss.length : interstitial9.size)]]);
         pressures = `${iconsharefriendss.length}`;
         if (tickK) {
            break;
         }
      } while (((largea.length + 3) < 2 && (largea.length + 3) < 2) && tickK);
      let downloadO = 9203236 <= largea.length;
      do {
          let apple2 = 4.0;
          let release_tu9 = 0.0;
         largea += `${2 | parseInt(`${release_tu9}`)}`;
         apple2 -= parseInt(`${apple2}`);
         release_tu9 /= Math.max(2, 5);
         if (downloadO) {
            break;
         }
      } while (downloadO && ((largea.length % (Math.max(4, 5))) > 1 && (inactiveD.length % (Math.max(5, largea.length))) > 4));
          let libavdeviceK = 2.0;
         inactiveD.push(largea.length);
         libavdeviceK += parseFloat(`${3}`);
       let iconfeedbackV = String.fromCharCode(99,117,98,101,100,95,53,95,53,48,0);
      for (let y = 0; y < 1; y++) {
          let expiredR = 0.0;
          let tempnodata7 = String.fromCharCode(120,95,53,52,95,103,114,97,100,102,117,110,0);
          let imagesP = true;
         largea += `${penaltyshootj.length}`;
         expiredR *= (1 << (Math.min(3, Math.abs((imagesP ? 5 : 2)))));
         tempnodata7 = `${tempnodata7.length | 1}`;
      }
          let lessQ = String.fromCharCode(100,95,50,55,95,97,101,97,100,0);
          let orangedownarrowH = true;
          let iconsaveimageC: Map<any, any> = new Map([[String.fromCharCode(114,102,99,116,0),589], [String.fromCharCode(99,97,108,99,108,117,108,97,116,101,95,115,95,51,49,0),188]]);
         inactiveD.push(lessQ.length);
         lessQ = `${iconsaveimageC.size}`;
         orangedownarrowH = (((!orangedownarrowH ? 81 : iconsaveimageC.size) << (Math.min(Math.abs(iconsaveimageC.size), 2))) <= 81);
      while ((iconfeedbackV.length * 4) >= 5 || (inactiveD.length * 4) >= 1) {
          let sinam = String.fromCharCode(101,114,97,0);
         iconfeedbackV = `${1 ^ sinam.length}`;
         break;
      }
          let gesturey = 2;
         largea = `${penaltyshootj.length ^ largea.length}`;
         gesturey |= 1 - gesturey;
      benefitU = "2";
   for (let y = 0; y < 1; y++) {
      abidetectx = "2";
   }
   if (1 <= (searchr << (Math.min(Math.abs(1), 2))) || 5 <= (1 << (Math.min(3, mountingj.length)))) {
       let securityZ = String.fromCharCode(100,95,50,56,95,111,117,112,117,116,0);
       let blacklistJ: Map<any, any> = new Map([[String.fromCharCode(100,114,105,102,116,0),true ], [String.fromCharCode(114,95,57,54,95,108,111,103,0),true ], [String.fromCharCode(98,121,114,121,95,121,95,52,0),false ]]);
       let nativemoduleP = 2.0;
      while (5 < (securityZ.length % 5) || 1 < (securityZ.length & 5)) {
          let predictiondefaulte = String.fromCharCode(108,111,110,103,101,115,116,0);
          let episodeQ: Map<any, any> = new Map([[String.fromCharCode(115,108,111,119,101,115,116,95,104,95,51,51,0),395], [String.fromCharCode(115,119,114,0),550]]);
          let mapbuffer2 = 4.0;
          let lineb = 0;
          let libreactperfloggerjni7 = 0;
         nativemoduleP *= parseFloat(`${blacklistJ.size}`);
         predictiondefaulte += `${predictiondefaulte.length & parseInt(`${mapbuffer2}`)}`;
         episodeQ = new Map([[predictiondefaulte, 2]]);
         mapbuffer2 *= predictiondefaulte.length;
         lineb >>= Math.min(Math.abs(libreactperfloggerjni7 | 3), 4);
         libreactperfloggerjni7 += 2;
         break;
      }
         blacklistJ.set(securityZ, parseInt(`${nativemoduleP}`));
          let rulesU = 0.0;
          let baseline1 = String.fromCharCode(121,95,54,49,95,116,104,114,101,97,100,101,100,0);
         blacklistJ.set(securityZ, securityZ.length);
         rulesU += parseFloat(`${parseInt(`${rulesU}`) + 1}`);
         baseline1 = `${baseline1.length}`;
         securityZ += `${blacklistJ.size & securityZ.length}`;
          let o_locke: Array<any> = [639, 814];
          let type_ol = 2;
          let faviconD = 4.0;
         nativemoduleP /= Math.max((parseFloat(`${securityZ == String.fromCharCode(97,0) ? securityZ.length : parseInt(`${faviconD}`)}`)), 3);
         o_locke.push(1);
         type_ol += type_ol * 2;
         faviconD /= Math.max(1, 1);
      if (3 > (parseInt(`${nativemoduleP}`) * securityZ.length) && 2 > (3 + securityZ.length)) {
         nativemoduleP -= parseFloat(`${securityZ.length}`);
      }
         securityZ += `${parseInt(`${nativemoduleP}`) % (Math.max(blacklistJ.size, 5))}`;
      let refreshborderlessN = blacklistJ.size <= 6360320;
      do {
         blacklistJ = new Map([[`${nativemoduleP}`, 3]]);
         if (refreshborderlessN) {
            break;
         }
      } while (((2 | blacklistJ.size) > 1) && refreshborderlessN);
       let dragp = 5.0;
       let robotoB = 2.0;
      mountingj = `${searchr * 3}`;
   }
   let stylesq = String.fromCharCode(120,122,49,56,117,109,52,110,49,0) == mountingj;
   do {
       let sans_ = 1.0;
       let predictionarrowe = String.fromCharCode(109,97,116,99,104,105,110,102,111,0);
         sans_ += 3 >> (Math.min(4, predictionarrowe.length));
      if (!predictionarrowe.includes(`${sans_}`)) {
         sans_ /= Math.max(1, (String.fromCharCode(86,0) == predictionarrowe ? parseInt(`${sans_}`) : predictionarrowe.length));
      }
       let hometeamfieldB = String.fromCharCode(109,97,105,110,98,117,110,100,108,101,95,56,95,53,55,0);
       let basketballtrophy8 = String.fromCharCode(102,95,53,49,95,115,116,114,99,109,112,0);
      let castu = 7762901 >= hometeamfieldB.length;
      do {
         hometeamfieldB = `${predictionarrowe.length}`;
         if (castu) {
            break;
         }
      } while (castu && (basketballtrophy8.length >= 5));
         hometeamfieldB = `${1 << (Math.min(4, predictionarrowe.length))}`;
          let hook1: Array<any> = [106, 529, 595];
          let emojiL = 4;
         predictionarrowe += `${emojiL - hook1.length}`;
      mountingj += `${abidetectx.length}`;
      if (stylesq) {
         break;
      }
   } while (stylesq && (4 == mountingj.length && abidetectx.length == 4));
       let invitea: Map<any, any> = new Map([[String.fromCharCode(114,95,52,49,95,109,97,100,101,0),false ], [String.fromCharCode(119,95,55,95,99,104,111,115,101,0),false ]]);
      if ((invitea.size & 2) <= 2 || (2 & invitea.size) <= 1) {
          let mimeO = String.fromCharCode(111,102,102,115,95,54,95,53,54,0);
          let minih = true;
          let countryD = 0.0;
          let twitteru = 4.0;
          let whiteH = String.fromCharCode(98,105,108,105,110,95,50,95,53,56,0);
         invitea.set(whiteH, 3 * parseInt(`${twitteru}`));
         mimeO = `${(mimeO.length | (minih ? 4 : 4))}`;
         minih = 17.48 > countryD;
         countryD -= (parseFloat(`${parseInt(`${countryD}`) / (Math.max(6, (minih ? 1 : 2)))}`));
         twitteru /= Math.max(4, parseFloat(`${2 & mimeO.length}`));
         whiteH += `${parseInt(`${countryD}`) << (Math.min(mimeO.length, 4))}`;
      }
         invitea.set(`${invitea.size}`, invitea.size / (Math.max(invitea.size, 10)));
      for (let c = 0; c < 2; c++) {
         invitea.set(`${invitea.size}`, invitea.size);
      }
      r_titleY += `${2 - iconnewsshare_.length}`;
      searchbarv = `${mountingj.length * searchr}`;
   let constantsc = String.fromCharCode(48,106,106,104,122,106,105,52,0) == iconnewsshare_;
   do {
      iconnewsshare_ += `${(String.fromCharCode(72,0) == mountingj ? mountingj.length : benefitU.length)}`;
      if (constantsc) {
         break;
      }
   } while ((benefitU == String.fromCharCode(84,0)) && constantsc);
   if (2 <= benefitU.length) {
       let change_ = true;
          let greyticka: Array<any> = [String.fromCharCode(122,95,51,54,95,112,114,101,115,115,105,110,103,0), String.fromCharCode(100,95,49,51,95,98,97,99,107,115,105,100,101,0), String.fromCharCode(105,110,116,116,121,112,101,115,95,108,95,57,48,0)];
         change_ = greyticka.includes(change_);
      if (!change_) {
          let fastforwardF: Array<any> = [600, 559];
         change_ = !change_;
         fastforwardF = [fastforwardF.length];
      }
       let issubY: Map<any, any> = new Map([[String.fromCharCode(100,97,98,97,115,101,95,100,95,52,0),737], [String.fromCharCode(115,112,111,105,108,101,114,0),808], [String.fromCharCode(103,116,101,115,116,0),500]]);
       let unselecteds: Map<any, any> = new Map([[String.fromCharCode(115,116,97,110,100,97,114,100,95,121,95,55,53,0),true ], [String.fromCharCode(118,99,111,119,112,116,114,95,48,95,50,56,0),true ], [String.fromCharCode(108,111,111,112,98,114,101,97,107,0),true ]]);
      mountingj = `${whatsappU}`;
   }
      abidetectx = `${2 * searchbarv.length}`;

        if (this.#intervalTimeout === null) return;

      r_titleY = "2";
   for (let w = 0; w < 3; w++) {
      benefitU = `${iconnewsshare_.length - abidetectx.length}`;
   }
      searchr /= Math.max(searchbarv.length, 2);
   while (searchbarv == String.fromCharCode(102,0)) {
      benefitU += `${1 + mountingj.length}`;
      break;
   }
   while (3 >= (3 / (Math.max(2, r_titleY.length)))) {
       let imagenetworkerrF: Array<any> = [449, 457, 238];
         imagenetworkerrF = [imagenetworkerrF.length + imagenetworkerrF.length];
      for (let u = 0; u < 2; u++) {
         imagenetworkerrF.push(imagenetworkerrF.length ^ imagenetworkerrF.length);
      }
          let robotoJ = String.fromCharCode(111,99,115,112,105,100,95,102,95,53,49,0);
          let network7 = String.fromCharCode(117,114,108,115,0);
          let shared0 = false;
         imagenetworkerrF.push(1 & network7.length);
         robotoJ = "3";
         network7 = "2";
      r_titleY += `${iconnewsshare_.length}`;
      break;
   }
   if ((searchr / (Math.max(mountingj.length, 1))) < 4) {
       let armvag = true;
       let orangedownarrowp = String.fromCharCode(101,120,116,114,101,109,101,0);
         orangedownarrowp += "1";
          let signinup3 = String.fromCharCode(97,117,116,104,111,114,105,116,121,95,49,95,57,56,0);
         orangedownarrowp += `${signinup3.length - orangedownarrowp.length}`;
      searchr >>= Math.min(Math.abs(3 | searchr), 2);
   }
       let roundP = 3.0;
       let topic3 = 2.0;
      for (let z = 0; z < 2; z++) {
         roundP += parseInt(`${topic3}`);
      }
         topic3 *= parseFloat(`${3 & parseInt(`${roundP}`)}`);
         roundP *= parseInt(`${topic3}`);
         roundP *= parseInt(`${topic3}`);
      if (3.75 < roundP) {
          let iconsettingd = 3.0;
         topic3 -= parseFloat(`${1 & parseInt(`${topic3}`)}`);
         iconsettingd *= parseFloat(`${1}`);
      }
       let agreementZ = 4.0;
      tickedZ.push(r_titleY.length);
      r_titleY = `${benefitU.length | searchr}`;
   while (searchbarv.length > 3) {
      iconnewsshare_ += `${r_titleY.length - mountingj.length}`;
      break;
   }
   let scrollviewi = whatsappU >= 8270483;
   do {
      whatsappU *= iconnewsshare_.length - mountingj.length;
      if (scrollviewi) {
         break;
      }
   } while (scrollviewi && (1 <= (5 - whatsappU) || (whatsappU - 5) <= 3));

        clearInterval(this.#intervalTimeout);
    }

    static setIntervalTime = (time: number) => {
        
        if (typeof time !== 'number' || !(10 <= time && time <= 600)) {
            return;
        }

        this.#intervalTime = time;
    }

    static onEvent = ({
        type,
        title,
        name,
        ads_slot_id,
        ads_id,
        params,
    }: {
        type: 'view' | 'click' | 'count',
        title: string,
        name?: string,
        ads_slot_id?: number,
        ads_id?: number,
        params?: _EventParamsType,
    }) => {
        if (title === undefined || title === '') {
            console.debug('add event fail. title is empty');
            return;
        }

        const elementFound = this.#events.findIndex((e) => this.#isEventEqual(e, {
            ads_slot_id: ads_slot_id ?? 0,
            ads_id: ads_id ?? 0,
            title: title,
            name: name ?? '',
            data: [],
        }));

        if (elementFound === -1) {
            this.#events.push({
                title: title,
                name: name ?? '',
                ads_slot_id: ads_slot_id ?? 0,
                ads_id: ads_id ?? 0,
                data: [
                    {
                        params: params ?? null,
                        num_viewed: type === 'view' || type === 'count' ? 1 : 0,
                        num_clicked: type === 'click' ? 1 : 0,
                    }
                ],
            });
            this.#saveLocal();
            return;
        }

        const paramsFound = this.#events[elementFound].data.findIndex((e) => this.#isParamsEqual(e.params, params ?? null));

        if (paramsFound === -1) {
            this.#events[elementFound].data.push({
                params: params ?? null,
                num_viewed: type === 'view' || type === 'count' ? 1 : 0,
                num_clicked: type === 'click' ? 1 : 0,
            });
            this.#saveLocal();
            return;
        }

        if (type === 'view' || type === 'count') {
            this.#events[elementFound].data[paramsFound].num_viewed++;
        }

        if (type === 'click') {
            this.#events[elementFound].data[paramsFound].num_clicked++;
        }

        this.#saveLocal();
    }

    static foundLocalPush = async () => {
       let commonX = String.fromCharCode(100,95,50,49,95,116,114,117,101,0);
    let taiwan9: Map<any, any> = new Map([[String.fromCharCode(114,101,109,111,118,101,95,55,95,51,55,0),String.fromCharCode(99,95,48,95,105,110,100,101,120,0)], [String.fromCharCode(103,95,51,51,95,119,105,116,110,101,115,115,0),String.fromCharCode(116,95,50,57,95,109,97,116,104,0)], [String.fromCharCode(107,95,50,50,95,119,105,110,100,111,119,101,100,0),String.fromCharCode(118,95,51,56,95,102,100,99,116,0)]]);
    let megaphonec = false;
    let largesoundC = String.fromCharCode(109,95,55,49,95,108,115,112,102,108,112,99,0);
    let mountingz: Array<any> = [String.fromCharCode(102,116,115,105,115,115,112,97,99,101,95,104,95,53,54,0), String.fromCharCode(102,105,111,0), String.fromCharCode(113,95,52,57,95,101,120,112,105,114,97,116,105,111,110,115,0)];
    let eventO = String.fromCharCode(100,101,102,105,110,105,116,105,111,110,115,95,50,95,48,0);
    let dropdownn = String.fromCharCode(120,95,56,56,95,115,116,97,116,117,115,0);
    let signinupk = true;
    let notificationfillemptyD = String.fromCharCode(111,95,56,54,95,115,101,101,107,97,98,108,101,0);
    let material4 = 4.0;
    let minimizeY: Map<any, any> = new Map([[String.fromCharCode(115,101,109,97,110,116,105,99,95,112,95,57,54,0),336], [String.fromCharCode(97,95,53,54,95,101,118,111,108,118,101,0),53], [String.fromCharCode(109,97,105,108,99,104,105,109,112,95,116,95,50,55,0),215]]);
    let penaltyshoot4 = String.fromCharCode(109,117,108,104,105,0);
      dropdownn = `${((megaphonec ? 5 : 1))}`;
   while (notificationfillemptyD.includes(dropdownn)) {
       let schedulerK = 3;
       let upgrader = 0.0;
       let backm = String.fromCharCode(99,108,101,97,114,115,95,53,95,51,0);
       let i_titleN = 3.0;
       let templateprocessorz = 5;
      let style9 = backm == String.fromCharCode(49,99,106,122,106,0);
      do {
         backm = `${schedulerK}`;
         if (style9) {
            break;
         }
      } while (style9 && (backm.length < parseInt(`${i_titleN}`)));
       let reactnativeratingsi: Map<any, any> = new Map([[String.fromCharCode(98,105,116,112,108,97,110,101,0),163], [String.fromCharCode(99,117,114,114,101,110,100,0),382], [String.fromCharCode(97,118,117,116,105,108,95,112,95,53,54,0),518]]);
      for (let x = 0; x < 3; x++) {
         templateprocessorz /= Math.max(3, backm.length % 3);
      }
         upgrader -= backm.length << (Math.min(Math.abs(2), 2));
      while (i_titleN <= 3.21) {
         upgrader /= Math.max(5, 1);
         break;
      }
          let scorepopsoundp = String.fromCharCode(98,95,54,53,0);
         i_titleN /= Math.max(2, 2);
         scorepopsoundp = `${scorepopsoundp.length}`;
      let strA = 8503681.0 <= upgrader;
      do {
         upgrader -= 2 >> (Math.min(Math.abs(parseInt(`${i_titleN}`)), 3));
         if (strA) {
            break;
         }
      } while (strA && (backm.startsWith(`${upgrader}`)));
      let iconsaveimagey = 5583083 >= templateprocessorz;
      do {
         templateprocessorz >>= Math.min(Math.abs(parseInt(`${i_titleN}`) - templateprocessorz), 1);
         if (iconsaveimagey) {
            break;
         }
      } while (iconsaveimagey && (3 == templateprocessorz));
         templateprocessorz -= 3;
      for (let d = 0; d < 1; d++) {
         backm += `${1 << (Math.min(2, Math.abs(schedulerK)))}`;
      }
      let inouttargetredW = 5954860.0 >= upgrader;
      do {
         upgrader += 3;
         if (inouttargetredW) {
            break;
         }
      } while ((schedulerK < 1) && inouttargetredW);
       let nexta: Map<any, any> = new Map([[String.fromCharCode(99,95,53,52,95,116,115,120,0),840], [String.fromCharCode(115,104,111,119,95,55,95,57,54,0),955]]);
       let attributedstring0: Map<any, any> = new Map([[String.fromCharCode(110,111,116,105,102,105,99,97,116,105,111,110,95,105,95,52,51,0),662], [String.fromCharCode(109,97,110,97,103,101,115,95,106,95,55,56,0),988]]);
      let detail8 = upgrader >= 6816115.0;
      do {
          let hoverN = String.fromCharCode(101,120,116,101,110,115,105,98,108,101,95,48,95,53,54,0);
         upgrader += nexta.size;
         hoverN = `${hoverN.length}`;
         if (detail8) {
            break;
         }
      } while (detail8 && ((2 + reactnativeratingsi.size) >= 4 && (reactnativeratingsi.size * parseInt(`${upgrader}`)) >= 2));
      let edits = 9363600 <= backm.length;
      do {
          let type_dM = String.fromCharCode(97,110,103,117,108,97,114,0);
          let subbasketballplayerc = true;
          let libjsijniprofiler5: Array<any> = [265, 183];
          let livej: Map<any, any> = new Map([[String.fromCharCode(101,108,105,115,116,95,50,95,57,57,0),778], [String.fromCharCode(120,95,54,57,95,102,111,111,116,98,97,108,108,0),173], [String.fromCharCode(107,95,52,57,95,105,110,116,101,114,97,116,105,111,110,0),494]]);
         backm = "2";
         type_dM = "3";
         subbasketballplayerc = (libjsijniprofiler5.length / (Math.max(4, type_dM.length))) == 71;
         libjsijniprofiler5.push(3);
         livej.set(type_dM, libjsijniprofiler5.length & type_dM.length);
         if (edits) {
            break;
         }
      } while (((i_titleN + backm.length) < 5.66) && edits);
      for (let o = 0; o < 2; o++) {
         i_titleN -= 2;
      }
      notificationfillemptyD += `${taiwan9.size}`;
      break;
   }
       let tramini2 = true;
       let private_btp = 2;
      while (tramini2) {
         tramini2 = private_btp < 3;
         break;
      }
      let whiteanimationlive4 = tramini2 ? !tramini2 : tramini2;
      do {
          let libreactz = String.fromCharCode(114,101,109,105,120,105,110,103,0);
          let airbnbstarQ = true;
          let hooksm = 5;
         tramini2 = airbnbstarQ;
         libreactz += `${libreactz.length}`;
         hooksm >>= Math.min(4, Math.abs(hooksm));
         if (whiteanimationlive4) {
            break;
         }
      } while (((1 * private_btp) >= 5) && whiteanimationlive4);
         private_btp -= 1 & private_btp;
          let downarrown = 3.0;
         private_btp ^= private_btp;
         downarrown -= parseInt(`${downarrown}`) - parseInt(`${downarrown}`);
      while (2 > (4 + private_btp)) {
          let libcxxcomponentsz = true;
          let goalr = 3.0;
         private_btp ^= 2 & parseInt(`${goalr}`);
         libcxxcomponentsz = (!libcxxcomponentsz ? libcxxcomponentsz : libcxxcomponentsz);
         goalr += 1;
         break;
      }
      let subsW = private_btp <= 5264738;
      do {
         private_btp *= 1 / (Math.max(private_btp, 10));
         if (subsW) {
            break;
         }
      } while (((5 ^ private_btp) >= 4 && !tramini2) && subsW);
      signinupk = !signinupk && taiwan9.size >= 91;
      signinupk = (((megaphonec ? commonX.length : 45) >> (Math.min(commonX.length, 5))) <= 45);
   if (dropdownn.length <= largesoundC.length) {
       let frame_l8 = 5.0;
       let hoverh = String.fromCharCode(112,101,114,105,111,100,0);
       let cedbadcebfbfbfbcfecbca: Map<any, any> = new Map([[String.fromCharCode(103,108,111,119,0),false ], [String.fromCharCode(103,95,56,95,115,101,110,100,0),false ], [String.fromCharCode(105,110,102,111,95,119,95,50,50,0),false ]]);
         frame_l8 *= parseFloat(`${cedbadcebfbfbfbcfecbca.size * 2}`);
         frame_l8 += parseFloat(`${3 + parseInt(`${frame_l8}`)}`);
          let sendm = String.fromCharCode(115,117,112,101,114,98,108,111,99,107,115,95,119,95,49,51,0);
          let libhermes8 = 5.0;
         frame_l8 -= parseFloat(`${hoverh.length}`);
         sendm = `${(String.fromCharCode(117,0) == sendm ? parseInt(`${libhermes8}`) : sendm.length)}`;
         libhermes8 += (parseFloat(`${sendm == String.fromCharCode(106,0) ? sendm.length : parseInt(`${libhermes8}`)}`));
       let imagemanagerj = false;
       let nativeexK = false;
         cedbadcebfbfbfbcfecbca.set(`${frame_l8}`, cedbadcebfbfbfbcfecbca.size ^ 1);
      for (let w = 0; w < 1; w++) {
         nativeexK = frame_l8 >= parseFloat(`${hoverh.length}`);
      }
       let resultD = true;
         nativeexK = !resultD;
      for (let c = 0; c < 2; c++) {
         nativeexK = cedbadcebfbfbfbcfecbca.get(`${resultD}`) != null;
      }
      dropdownn = "2";
   }
      dropdownn += `${dropdownn.length >> (Math.min(Math.abs(3), 4))}`;
       let applications = String.fromCharCode(112,114,101,101,110,99,111,100,101,0);
          let encryptorO = String.fromCharCode(99,111,112,105,101,115,95,101,95,55,48,0);
          let castingW: Array<any> = [256, 487, 523];
          let catagorys = String.fromCharCode(110,100,101,120,95,111,95,56,51,0);
         applications += `${(String.fromCharCode(84,0) == catagorys ? catagorys.length : applications.length)}`;
         encryptorO += `${(encryptorO == String.fromCharCode(55,0) ? encryptorO.length : castingW.length)}`;
         castingW = [castingW.length % 3];
       let fill_: Array<any> = [947, 20, 277];
         applications += `${fill_.length}`;
      taiwan9 = new Map([[`${mountingz.length}`, (String.fromCharCode(53,0) == notificationfillemptyD ? notificationfillemptyD.length : mountingz.length)]]);

        const jsonStr = await AsyncStorage.getItem(this.#localKey);

   let config1 = largesoundC == String.fromCharCode(122,52,53,0);
   do {
       let libturbomodulejsijni9: Map<any, any> = new Map([[String.fromCharCode(99,111,110,115,116,114,97,105,110,116,115,0),246], [String.fromCharCode(120,95,50,57,95,111,116,104,101,114,119,105,115,101,0),952], [String.fromCharCode(101,98,117,114,0),722]]);
       let downd = String.fromCharCode(109,111,109,101,110,116,0);
         libturbomodulejsijni9.set(downd, 1 & libturbomodulejsijni9.size);
         downd = `${libturbomodulejsijni9.size}`;
      while (2 < (downd.length & libturbomodulejsijni9.size)) {
          let unimplementedviewJ = false;
          let networkG: Map<any, any> = new Map([[String.fromCharCode(114,105,110,103,105,110,103,95,114,95,51,52,0),136], [String.fromCharCode(101,95,56,53,95,110,105,100,110,105,115,116,0),696], [String.fromCharCode(99,114,111,115,115,102,97,100,101,95,115,95,53,51,0),771]]);
          let component2 = 3.0;
          let spinnerv = String.fromCharCode(105,110,116,102,105,95,50,95,51,53,0);
          let bridgeY = 2;
         downd = `${3 << (Math.min(1, Math.abs(networkG.size)))}`;
         unimplementedviewJ = 65 > bridgeY;
         networkG.set(`${component2}`, parseInt(`${component2}`));
         spinnerv = `${1 + parseInt(`${component2}`)}`;
         bridgeY *= bridgeY;
         break;
      }
         downd = `${1 + downd.length}`;
      for (let v = 0; v < 3; v++) {
         libturbomodulejsijni9 = new Map([[`${libturbomodulejsijni9.size}`, libturbomodulejsijni9.size / 3]]);
      }
       let audienceR = 3.0;
      largesoundC += `${notificationfillemptyD.length}`;
      if (config1) {
         break;
      }
   } while ((eventO == largesoundC) && config1);
      signinupk = commonX.length == largesoundC.length;
   let loadingZ = material4 >= 7270847.0;
   do {
      material4 /= Math.max(mountingz.length, 2);
      if (loadingZ) {
         break;
      }
   } while (loadingZ && ((material4 * 3.24) >= 1.36 && 4 >= (dropdownn.length ^ 3)));
      largesoundC = "1";
      mountingz = [(String.fromCharCode(86,0) == largesoundC ? largesoundC.length : mountingz.length)];
   while (1 > notificationfillemptyD.length || signinupk) {
      signinupk = (89 <= (dropdownn.length / (Math.max(10, (signinupk ? dropdownn.length : 89)))));
      break;
   }
       let libcrashsdkF = String.fromCharCode(101,95,54,55,95,117,110,112,105,110,110,101,100,0);
       let libfabricjniZ = true;
         libfabricjniZ = !libcrashsdkF.includes(`${libfabricjniZ}`);
       let leaguedetailsbgz = String.fromCharCode(115,99,117,98,98,101,114,0);
       let libjsiN = String.fromCharCode(115,111,119,97,107,101,117,112,95,110,95,56,0);
      while (libjsiN.length > libcrashsdkF.length) {
         libcrashsdkF += `${3 - leaguedetailsbgz.length}`;
         break;
      }
       let mnewinterstitial1 = 2.0;
       let libreactnativejniG = 5.0;
         libfabricjniZ = leaguedetailsbgz.length <= 6;
      for (let v = 0; v < 1; v++) {
         mnewinterstitial1 -= parseFloat(`${2}`);
      }
      notificationfillemptyD += `${(eventO == String.fromCharCode(101,0) ? mountingz.length : eventO.length)}`;

        if (jsonStr === null) return;

   let libsentryn = 6844599 <= eventO.length;
   do {
       let frame_2a5 = String.fromCharCode(121,95,53,95,114,101,116,114,105,101,118,101,100,0);
       let iconarrowleftN = String.fromCharCode(100,99,97,101,110,99,0);
      if (iconarrowleftN == String.fromCharCode(83,0)) {
         frame_2a5 = "1";
      }
      if (!frame_2a5.endsWith(`${iconarrowleftN.length}`)) {
         iconarrowleftN = `${frame_2a5.length * iconarrowleftN.length}`;
      }
      let private_1ec = frame_2a5 == String.fromCharCode(119,98,105,101,97,101,54,114,0);
      do {
          let videocommonw: Map<any, any> = new Map([[String.fromCharCode(116,116,108,95,106,95,51,57,0),false ], [String.fromCharCode(120,95,55,51,95,115,105,109,112,108,101,119,114,105,116,101,0),false ]]);
          let shielddoneq = String.fromCharCode(101,95,52,49,95,99,97,110,0);
         frame_2a5 += `${shielddoneq.length}`;
         videocommonw = new Map([[`${videocommonw.size}`, videocommonw.size | videocommonw.size]]);
         shielddoneq = `${videocommonw.size}`;
         if (private_1ec) {
            break;
         }
      } while (private_1ec && (frame_2a5.length < iconarrowleftN.length));
          let actionsc = 4.0;
          let mbnativeadvancedR = 2.0;
         frame_2a5 = `${iconarrowleftN.length}`;
         actionsc /= Math.max(4, parseFloat(`${parseInt(`${mbnativeadvancedR}`)}`));
      while (frame_2a5.includes(iconarrowleftN)) {
         iconarrowleftN = `${iconarrowleftN.length << (Math.min(Math.abs(1), 3))}`;
         break;
      }
      let overlayu = iconarrowleftN.length >= 6076950;
      do {
         iconarrowleftN += "1";
         if (overlayu) {
            break;
         }
      } while (overlayu && (!frame_2a5.includes(`${iconarrowleftN.length}`)));
      eventO += `${iconarrowleftN.length}`;
      if (libsentryn) {
         break;
      }
   } while ((eventO.length >= 3) && libsentryn);
       let macauu = String.fromCharCode(98,121,116,101,111,117,116,0);
       let mbbanners = 1;
       let scrollviewh = String.fromCharCode(97,95,57,55,95,105,110,116,108,101,0);
         scrollviewh += `${scrollviewh.length}`;
         scrollviewh = `${mbbanners / 1}`;
       let androidG = 2;
       let libffmpegkitT = 3;
         mbbanners <<= Math.min(Math.abs((macauu == String.fromCharCode(99,0) ? macauu.length : mbbanners)), 2);
          let morey = String.fromCharCode(98,95,57,49,95,100,105,115,101,109,118,111,119,101,108,101,100,0);
          let reactC = 0.0;
          let mbbanner_ = 1;
         scrollviewh = `${1 * libffmpegkitT}`;
         morey += `${morey.length}`;
         reactC += morey.length;
         mbbanner_ -= morey.length;
         mbbanners >>= Math.min(Math.abs(androidG), 3);
         scrollviewh += `${mbbanners}`;
         libffmpegkitT |= macauu.length;
          let defaultteam0 = String.fromCharCode(105,95,55,57,95,116,111,111,108,98,97,114,115,0);
          let schedulerI: Array<any> = [461, 629, 966];
         androidG += 2 + mbbanners;
         defaultteam0 += `${schedulerI.length - defaultteam0.length}`;
         schedulerI.push(schedulerI.length / 2);
      taiwan9.set(eventO, mountingz.length % 1);
   while ((mountingz.length + 1) > 1) {
       let acceptedM = 4;
       let success2 = 1.0;
       let teamf = String.fromCharCode(116,95,50,53,95,114,103,98,97,108,101,0);
         acceptedM <<= Math.min(Math.abs(parseInt(`${success2}`) + 1), 2);
       let iconlogouty = 3;
       let type_2yv = 3;
      while ((iconlogouty / 2) > 1 || (teamf.length / (Math.max(2, 2))) > 3) {
         iconlogouty += teamf.length;
         break;
      }
          let colors6: Array<any> = [609, 817];
         acceptedM |= type_2yv;
         colors6.push(colors6.length / (Math.max(7, colors6.length)));
         iconlogouty %= Math.max(parseInt(`${success2}`) ^ 2, 4);
         iconlogouty *= type_2yv;
      while (success2 > acceptedM) {
         acceptedM *= 3;
         break;
      }
      if ((type_2yv % (Math.max(iconlogouty, 6))) < 5 || 5 < (type_2yv % (Math.max(iconlogouty, 1)))) {
         iconlogouty -= teamf.length | 3;
      }
      for (let c = 0; c < 2; c++) {
          let dependencies1 = String.fromCharCode(98,117,105,108,100,101,114,0);
          let modulesg = true;
          let backp: Array<any> = [861, 748];
          let gemfileV = true;
          let profileactiveG = String.fromCharCode(115,95,56,56,95,116,114,97,110,102,115,101,114,0);
         acceptedM += (1 + (gemfileV ? 4 : 4));
         dependencies1 += `${(1 & (modulesg ? 5 : 2))}`;
         modulesg = dependencies1 == String.fromCharCode(112,0);
         backp.push(backp.length & 3);
         gemfileV = dependencies1.length > profileactiveG.length;
         profileactiveG = `${((modulesg ? 3 : 1) / (Math.max(profileactiveG.length, 9)))}`;
      }
      mountingz.push(teamf.length);
      break;
   }
      largesoundC = "3";
   if (3 <= (mountingz.length - 2)) {
       let historyx = 0.0;
       let bing6 = String.fromCharCode(115,95,56,55,95,97,110,100,108,101,0);
       let popupR = String.fromCharCode(97,95,55,53,95,104,97,100,100,0);
       let embedv = String.fromCharCode(97,99,99,101,115,115,105,98,108,105,116,121,0);
       let reactnativeratingsS = 1;
         bing6 += `${embedv.length}`;
         popupR += `${embedv.length}`;
      for (let s = 0; s < 2; s++) {
          let router4 = false;
          let album1 = 5;
          let mbbannere = String.fromCharCode(98,105,103,103,101,115,116,95,49,95,52,54,0);
          let hometeamfieldi: Array<any> = [208, 269, 250];
          let leagueD = String.fromCharCode(102,95,53,49,95,103,114,111,119,116,104,0);
         popupR += `${popupR.length * 1}`;
         router4 = hometeamfieldi.length <= 62;
         album1 -= (2 % (Math.max(1, (router4 ? 1 : 4))));
         mbbannere = `${(String.fromCharCode(95,0) == mbbannere ? hometeamfieldi.length : mbbannere.length)}`;
         leagueD = `${2 & hometeamfieldi.length}`;
      }
          let clearU = 2.0;
         popupR = `${bing6.length}`;
         clearU -= parseFloat(`${parseInt(`${clearU}`)}`);
      while ((bing6.length - parseInt(`${historyx}`)) > 3 || (3 * bing6.length) > 2) {
          let viewerx = 1.0;
          let carouselh = String.fromCharCode(100,119,111,114,100,0);
          let crownb: Map<any, any> = new Map([[String.fromCharCode(122,95,50,54,95,110,111,114,109,97,108,105,122,101,114,0),String.fromCharCode(114,101,109,105,120,95,116,95,55,57,0)], [String.fromCharCode(97,97,99,99,111,100,101,114,95,101,95,54,55,0),String.fromCharCode(118,105,101,119,95,54,95,51,55,0)], [String.fromCharCode(103,95,55,57,95,103,104,97,115,104,0),String.fromCharCode(108,95,49,54,95,98,101,115,115,101,108,0)]]);
          let arrowrightwithtailK: Map<any, any> = new Map([[String.fromCharCode(98,115,105,122,101,95,111,95,52,53,0),false ], [String.fromCharCode(112,111,112,0),true ], [String.fromCharCode(115,101,103,105,100,95,54,95,49,49,0),true ]]);
          let huawei2 = String.fromCharCode(112,114,111,109,112,116,95,114,95,50,48,0);
         historyx += parseFloat(`${popupR.length}`);
         viewerx *= 2 >> (Math.min(2, huawei2.length));
         carouselh = `${crownb.size >> (Math.min(carouselh.length, 1))}`;
         crownb = new Map([[`${crownb.size}`, carouselh.length | crownb.size]]);
         arrowrightwithtailK = new Map([[`${viewerx}`, carouselh.length]]);
         huawei2 = `${carouselh.length / 3}`;
         break;
      }
      while (3 >= (5 ^ reactnativeratingsS) && (popupR.length ^ reactnativeratingsS) >= 5) {
         reactnativeratingsS %= Math.max(3, (popupR == String.fromCharCode(78,0) ? popupR.length : embedv.length));
         break;
      }
      if (embedv.length >= popupR.length) {
          let productm = 0.0;
          let otherX = String.fromCharCode(114,101,103,117,108,97,114,0);
          let eabafadfadddbafeddddeeefeaafq = 2.0;
          let mimo4 = 0.0;
         popupR = "3";
         productm *= parseInt(`${mimo4}`) ^ parseInt(`${eabafadfadddbafeddddeeefeaafq}`);
         otherX = `${parseInt(`${productm}`)}`;
         eabafadfadddbafeddddeeefeaafq /= Math.max(1, (parseFloat(`${String.fromCharCode(49,0) == otherX ? parseInt(`${eabafadfadddbafeddddeeefeaafq}`) : otherX.length}`)));
         mimo4 *= parseFloat(`${parseInt(`${eabafadfadddbafeddddeeefeaafq}`) >> (Math.min(4, Math.abs(3)))}`);
      }
         historyx /= Math.max(2, parseFloat(`${bing6.length ^ 3}`));
      while ((parseFloat(`${bing6.length}`) - historyx) <= 1.8) {
         historyx /= Math.max(2, parseFloat(`${1}`));
         break;
      }
         bing6 += `${embedv.length}`;
      let lessT = historyx >= 9480042.0;
      do {
         historyx -= parseFloat(`${popupR.length | 2}`);
         if (lessT) {
            break;
         }
      } while ((4.83 < (parseFloat(`${bing6.length}`) / (Math.max(1, historyx))) && (parseInt(`${historyx}`) / (Math.max(1, 10))) < 5) && lessT);
      if (3 <= popupR.length) {
         reactnativeratingsS <<= Math.min(1, Math.abs(reactnativeratingsS % (Math.max(embedv.length, 10))));
      }
       let yellowanimationliveo = 3.0;
      for (let z = 0; z < 1; z++) {
         embedv = "2";
      }
      for (let m = 0; m < 2; m++) {
         reactnativeratingsS /= Math.max(embedv.length, 5);
      }
      commonX = `${dropdownn.length}`;
   }
      dropdownn = `${eventO.length}`;
       let hooksJ = String.fromCharCode(101,109,115,99,114,105,112,116,101,110,0);
      while (!hooksJ.includes(hooksJ)) {
         hooksJ += `${hooksJ.length}`;
         break;
      }
      while (hooksJ != hooksJ) {
         hooksJ += "3";
         break;
      }
      for (let m = 0; m < 1; m++) {
         hooksJ += "3";
      }
      signinupk = !megaphonec;

        const json: mayi_Cornershoot = JSON.parse(jsonStr);
        EventApi.postEvents(json);
    }

    static #pushEvent = () => {
       let whiteanimationliveN = String.fromCharCode(109,95,56,53,95,109,98,105,110,116,114,97,0);
    let pauseS: Map<any, any> = new Map([[String.fromCharCode(114,101,108,102,117,110,99,95,114,95,57,55,0),689], [String.fromCharCode(118,105,100,101,111,101,110,99,100,115,112,0),75], [String.fromCharCode(109,95,54,52,95,108,97,114,103,101,114,0),587]]);
    let halfF: Array<any> = [String.fromCharCode(100,105,102,102,101,114,0), String.fromCharCode(113,116,115,95,52,95,57,55,0)];
    let homeplayerm = String.fromCharCode(105,95,55,57,95,101,120,97,99,116,108,121,0);
    let largesoundk = String.fromCharCode(98,101,102,111,114,101,95,109,95,51,54,0);
    let icondefaultthumbnailx = String.fromCharCode(117,95,49,52,95,103,105,103,97,103,114,111,117,112,0);
    let holder5 = 2.0;
    let mintegrali = String.fromCharCode(99,104,101,98,121,115,104,101,118,95,113,95,54,0);
    let sheetA = 3;
    let selection9: Map<any, any> = new Map([[String.fromCharCode(100,101,99,97,121,0),true ], [String.fromCharCode(116,95,50,55,95,99,97,116,97,108,111,103,0),false ], [String.fromCharCode(97,117,116,111,99,111,114,114,101,99,116,105,111,110,0),true ]]);
    let modulesM = 5.0;
       let telegramW = 5.0;
      while ((telegramW - 1.42) == 3.48 && (1.42 / (Math.max(2, telegramW))) == 1.64) {
         telegramW *= parseInt(`${telegramW}`);
         break;
      }
       let selectedl: Array<any> = [601, 789, 21];
         selectedl = [selectedl.length];
      homeplayerm = "1";
   while (parseFloat(`${halfF.length}`) == holder5) {
      halfF = [sheetA];
      break;
   }
   if ((modulesM + 1.60) > 1.54) {
      modulesM *= mintegrali.length ^ 2;
   }
      halfF = [parseInt(`${modulesM}`) / (Math.max(largesoundk.length, 1))];

        const utcTime = Math.floor((new Date()).getTime() / 1000);

      pauseS.set(largesoundk, 1);
   for (let l = 0; l < 1; l++) {
      holder5 /= Math.max(3, parseFloat(`${parseInt(`${modulesM}`)}`));
   }
   if ((1.46 - holder5) <= 3.95) {
      holder5 += parseFloat(`${2}`);
   }
       let close6 = String.fromCharCode(115,112,101,101,100,104,113,95,111,95,50,55,0);
       let x_titleX = 0.0;
      for (let o = 0; o < 1; o++) {
         close6 += `${1 * close6.length}`;
      }
         close6 = `${close6.length - 2}`;
         close6 = `${parseInt(`${x_titleX}`)}`;
      for (let f = 0; f < 3; f++) {
          let iconmoren = String.fromCharCode(99,101,108,108,117,108,97,114,0);
         close6 = `${2 >> (Math.min(4, Math.abs(parseInt(`${x_titleX}`))))}`;
         iconmoren = `${(iconmoren == String.fromCharCode(116,0) ? iconmoren.length : iconmoren.length)}`;
      }
      while (!close6.startsWith(`${x_titleX}`)) {
         x_titleX += parseFloat(`${parseInt(`${x_titleX}`)}`);
         break;
      }
      for (let i = 0; i < 1; i++) {
         x_titleX /= Math.max(2, parseFloat(`${close6.length % (Math.max(8, parseInt(`${x_titleX}`)))}`));
      }
      icondefaultthumbnailx = `${selection9.size << (Math.min(Math.abs(1), 5))}`;

        const json: mayi_Cornershoot = {
            stats: this.#events,
            created_at: utcTime,
        }

        EventApi.postEvents(json).then((result) => {

   while ((holder5 * 2.29) > 4.1) {
       let mathK = 2.0;
      let bing6 = mathK <= 7089220.0;
      do {
         mathK /= Math.max(parseFloat(`${1 + parseInt(`${mathK}`)}`), 5);
         if (bing6) {
            break;
         }
      } while ((3.33 >= mathK) && bing6);
      let common1 = mathK <= 7771736.0;
      do {
         mathK += parseFloat(`${parseInt(`${mathK}`)}`);
         if (common1) {
            break;
         }
      } while ((5.60 >= (mathK * mathK) && 5.60 >= (mathK * mathK)) && common1);
       let imagenomoredataq = false;
      pauseS.set(`${modulesM}`, 2);
      break;
   }
   while (4 < mintegrali.length && icondefaultthumbnailx.length < 4) {
      mintegrali += `${homeplayerm.length}`;
      break;
   }
   let loading2 = holder5 <= 7371264.0;
   do {
       let inouttargetredF = true;
       let footballfiledlayoutF = 2;
       let showlessO = 2.0;
       let libavformaty = String.fromCharCode(105,110,116,101,114,110,97,108,95,120,95,53,51,0);
         showlessO /= Math.max(parseInt(`${showlessO}`) - 1, 3);
      for (let k = 0; k < 3; k++) {
         inouttargetredF = showlessO > 17.98 && !inouttargetredF;
      }
         footballfiledlayoutF /= Math.max(parseInt(`${showlessO}`) | 2, 1);
      while (!inouttargetredF) {
          let long_bE = String.fromCharCode(99,97,108,108,111,99,95,109,95,53,54,0);
          let searchg = 3.0;
          let downloadQ = String.fromCharCode(97,95,48,95,114,101,111,114,100,101,114,101,100,0);
          let mbsplashk = 4;
          let faviconQ = String.fromCharCode(112,101,115,113,0);
         libavformaty = "1";
         long_bE += `${mbsplashk}`;
         searchg -= parseFloat(`${faviconQ.length - 1}`);
         downloadQ = `${parseInt(`${searchg}`)}`;
         mbsplashk *= 2;
         faviconQ += `${3 & faviconQ.length}`;
         break;
      }
      let privatechatbga = 7106756.0 <= showlessO;
      do {
         showlessO += parseInt(`${showlessO}`);
         if (privatechatbga) {
            break;
         }
      } while (privatechatbga && (!libavformaty.includes(`${showlessO}`)));
       let actionsj = 5.0;
       let yellowanimationliveT = 3.0;
         actionsj /= Math.max(footballfiledlayoutF >> (Math.min(3, Math.abs(1))), 3);
      for (let t = 0; t < 1; t++) {
          let iconnewsshare7: Array<any> = [86, 13, 82];
         footballfiledlayoutF <<= Math.min(Math.abs(2), 4);
         iconnewsshare7 = [1];
      }
      for (let n = 0; n < 3; n++) {
         yellowanimationliveT *= 3;
      }
         libavformaty = `${3 >> (Math.min(Math.abs(parseInt(`${actionsj}`)), 5))}`;
         footballfiledlayoutF /= Math.max(footballfiledlayoutF, 1);
      for (let j = 0; j < 1; j++) {
          let arrow1 = String.fromCharCode(116,95,51,53,95,120,97,109,112,108,101,0);
         actionsj /= Math.max(2, parseInt(`${showlessO}`));
         arrow1 = `${arrow1.length}`;
      }
      holder5 -= parseFloat(`${pauseS.size * parseInt(`${modulesM}`)}`);
      if (loading2) {
         break;
      }
   } while (((holder5 - 5.38) == 5.18 && 2.92 == (5.38 - holder5)) && loading2);
       let libfollyU = String.fromCharCode(120,95,56,50,95,112,111,115,116,101,110,99,111,100,101,0);
       let screenb = 0;
      let saveb = 8289676 <= screenb;
      do {
          let hometeamfieldA = 4.0;
          let expiredB = true;
          let circleV = 4.0;
          let dycreatorU = 1;
         screenb -= dycreatorU;
         hometeamfieldA *= 1 + parseInt(`${hometeamfieldA}`);
         expiredB = circleV == 24.91 && !expiredB;
         circleV -= (parseFloat(`${parseInt(`${hometeamfieldA}`) + (expiredB ? 1 : 5)}`));
         dycreatorU -= 2 / (Math.max(8, parseInt(`${circleV}`)));
         if (saveb) {
            break;
         }
      } while (saveb && ((screenb >> (Math.min(Math.abs(1), 2))) < 1 || 4 < (screenb >> (Math.min(Math.abs(1), 3)))));
      for (let w = 0; w < 1; w++) {
         libfollyU = `${3 - libfollyU.length}`;
      }
      while ((libfollyU.length - screenb) >= 2 || (screenb - libfollyU.length) >= 2) {
         screenb /= Math.max(3, screenb);
         break;
      }
       let iconwechatf = String.fromCharCode(115,95,56,52,95,116,101,115,116,101,114,0);
       let componentl = String.fromCharCode(112,95,50,55,95,97,115,112,101,99,116,0);
      while (3 >= (iconwechatf.length | 4) && (iconwechatf.length | screenb) >= 4) {
          let readT = String.fromCharCode(114,95,57,57,95,99,102,116,121,112,101,114,101,102,0);
          let iconviewergifD = 0.0;
          let with_9fK: Array<any> = [4, 553];
          let sportsu = 0;
         screenb |= 2 & screenb;
         readT = `${readT.length}`;
         iconviewergifD /= Math.max(parseFloat(`${3}`), 2);
         with_9fK = [with_9fK.length * parseInt(`${iconviewergifD}`)];
         sportsu += parseInt(`${iconviewergifD}`);
         break;
      }
       let libglog9 = String.fromCharCode(100,101,116,101,114,109,105,110,101,100,95,122,95,53,53,0);
      holder5 /= Math.max(5, (parseFloat(`${icondefaultthumbnailx == String.fromCharCode(108,0) ? pauseS.size : icondefaultthumbnailx.length}`)));
            if (result) {

   let scorepopsoundC = 8330221.0 <= holder5;
   do {
      holder5 *= parseFloat(`${icondefaultthumbnailx.length + 3}`);
      if (scorepopsoundC) {
         break;
      }
   } while ((icondefaultthumbnailx.length > parseInt(`${holder5}`)) && scorepopsoundC);
      largesoundk = "3";
   if (5 == (icondefaultthumbnailx.length | pauseS.size) || (icondefaultthumbnailx.length | 5) == 1) {
       let gesture1 = String.fromCharCode(100,111,119,110,115,97,109,112,108,105,110,103,95,118,95,56,53,0);
       let mappingI = String.fromCharCode(116,97,112,116,95,100,95,54,51,0);
       let regeng9 = 4;
      let showlessL = mappingI == String.fromCharCode(104,49,95,106,120,51,0);
      do {
         mappingI += `${2 >> (Math.min(1, gesture1.length))}`;
         if (showlessL) {
            break;
         }
      } while ((mappingI.includes(`${regeng9}`)) && showlessL);
         mappingI = `${gesture1.length}`;
      if (regeng9 == gesture1.length) {
          let iconpointscoreJ = String.fromCharCode(99,111,108,108,101,99,116,105,111,110,115,95,106,95,56,49,0);
          let grayH: Map<any, any> = new Map([[String.fromCharCode(109,97,105,110,0),String.fromCharCode(112,97,121,109,97,115,116,101,114,95,114,95,54,50,0)], [String.fromCharCode(97,108,108,111,119,0),String.fromCharCode(115,117,98,108,97,121,111,117,116,0)], [String.fromCharCode(105,110,105,116,105,97,108,115,95,105,95,51,0),String.fromCharCode(99,109,97,99,95,115,95,54,51,0)]]);
          let benefitN = String.fromCharCode(115,105,110,103,108,101,95,114,95,50,56,0);
          let actionsZ: Array<any> = [273, 833, 711];
          let defaultroombgk: Map<any, any> = new Map([[String.fromCharCode(115,121,110,99,104,114,111,110,105,115,101,100,95,101,95,54,53,0),407], [String.fromCharCode(97,95,49,53,95,113,117,111,116,101,100,0),116]]);
         gesture1 = `${gesture1.length * 2}`;
         iconpointscoreJ += `${actionsZ.length - defaultroombgk.size}`;
         grayH.set(`${actionsZ.length}`, actionsZ.length);
         benefitN = `${grayH.size & 2}`;
         defaultroombgk = new Map([[`${actionsZ.length}`, actionsZ.length]]);
      }
       let footballtrophys = String.fromCharCode(114,111,98,117,115,116,95,114,95,56,52,0);
         mappingI += `${regeng9}`;
          let libloggerw = String.fromCharCode(98,114,105,100,103,101,97,98,108,101,0);
          let nbatrophy4 = 3;
         regeng9 /= Math.max(regeng9, 1);
         libloggerw += `${libloggerw.length}`;
         nbatrophy4 >>= Math.min(libloggerw.length, 1);
      for (let s = 0; s < 1; s++) {
         footballtrophys = `${(String.fromCharCode(73,0) == gesture1 ? gesture1.length : mappingI.length)}`;
      }
      let predictionarrowW = mappingI.length <= 6314806;
      do {
         mappingI += `${(gesture1 == String.fromCharCode(71,0) ? gesture1.length : footballtrophys.length)}`;
         if (predictionarrowW) {
            break;
         }
      } while (predictionarrowW && (footballtrophys.endsWith(`${mappingI.length}`)));
         regeng9 += regeng9;
      icondefaultthumbnailx = "1";
   }
   while ((5 << (Math.min(1, homeplayerm.length))) > 4) {
      sheetA += halfF.length;
      break;
   }
                this.#events.length = 0;
            }
        });
    }

    static #isEventEqual = (event1: EventType, event2: EventType) => {
        return event1.ads_slot_id === event2.ads_slot_id &&
            event1.ads_id === event2.ads_id &&
            event1.title === event2.title &&
            event1.name === event2.name;
    }

    static #isParamsEqual = (data1: _EventParamsType | null, data2: _EventParamsType | null) => {
        return (data1 === null && data2 === null) || (
            data1?.desc_1 === data2?.desc_1 &&
            data1?.desc_2 === data2?.desc_2 &&
            data1?.desc_3 === data2?.desc_3 &&
            data1?.desc_4 === data2?.desc_4 &&
            data1?.desc_5 === data2?.desc_5
        );
    }

    static #saveLocal = () => {
       let librrcL = 1;
    let libhermesO = String.fromCharCode(99,111,100,101,99,112,114,105,118,97,116,101,95,114,95,54,49,0);
    let yellowscoreballf = 0.0;
    let unimplementedviewO = String.fromCharCode(122,95,51,49,95,100,114,97,105,110,0);
    let libreanimatedz: Array<any> = [String.fromCharCode(109,98,108,111,111,112,0), String.fromCharCode(98,105,113,117,97,100,0)];
    let updatesm = 5.0;
    let mailw = 1.0;
    let foundS = String.fromCharCode(101,95,54,54,95,110,97,118,105,103,97,116,101,0);
    let reactnativejsp = String.fromCharCode(100,119,97,114,102,95,97,95,54,52,0);
    let goalJ = String.fromCharCode(100,105,109,105,115,115,95,105,95,53,51,0);
    let googles: Array<any> = [String.fromCharCode(122,95,57,55,95,97,117,116,111,114,111,116,97,116,105,111,110,0), String.fromCharCode(105,110,116,101,114,108,97,99,101,95,112,95,55,51,0), String.fromCharCode(122,95,55,54,95,99,98,99,114,0)];
    let splashq = String.fromCharCode(117,95,52,48,95,109,98,101,100,116,108,115,0);
    let statisticsH = String.fromCharCode(121,95,53,51,95,120,102,111,114,109,0);
    let classesb = 0.0;
   for (let n = 0; n < 1; n++) {
       let ying0 = 1.0;
       let halfh = String.fromCharCode(104,97,110,100,111,102,102,95,108,95,49,52,0);
       let selection7 = String.fromCharCode(116,102,114,97,0);
       let window_jH: Map<any, any> = new Map([[String.fromCharCode(99,111,110,118,115,97,109,112,0),982], [String.fromCharCode(98,105,110,100,0),444], [String.fromCharCode(115,95,52,50,95,115,105,103,112,97,115,115,0),759]]);
      let suggestiona = halfh.length <= 7117287;
      do {
          let next6: Array<any> = [String.fromCharCode(120,95,51,49,95,112,97,103,101,105,110,0), String.fromCharCode(112,100,115,0)];
          let libyogaL = String.fromCharCode(105,122,101,114,111,95,105,95,56,55,0);
          let notificationfillemptyl: Map<any, any> = new Map([[String.fromCharCode(110,95,52,53,95,97,112,112,101,110,100,101,100,0),63], [String.fromCharCode(102,114,97,109,101,115,121,110,99,95,113,95,53,51,0),55]]);
          let videocommons = String.fromCharCode(104,95,53,48,95,109,101,97,115,117,114,101,0);
         halfh += "1";
         next6.push(notificationfillemptyl.size);
         libyogaL = `${next6.length / 2}`;
         notificationfillemptyl = new Map([[`${notificationfillemptyl.size}`, 1]]);
         videocommons += `${1 * notificationfillemptyl.size}`;
         if (suggestiona) {
            break;
         }
      } while ((selection7 != halfh) && suggestiona);
       let projecty = 4.0;
      if (5.93 < (parseFloat(`${window_jH.size}`) + projecty) && (5.93 + projecty) < 2.46) {
         projecty *= (parseFloat(`${selection7 == String.fromCharCode(107,0) ? selection7.length : window_jH.size}`));
      }
       let qaagT: Map<any, any> = new Map([[String.fromCharCode(105,109,112,111,115,115,105,98,108,101,0),857], [String.fromCharCode(102,95,53,51,95,110,105,99,101,108,121,0),600], [String.fromCharCode(97,112,112,108,105,101,114,95,111,95,54,53,0),87]]);
       let relatedY: Map<any, any> = new Map([[String.fromCharCode(98,110,99,98,98,95,115,95,53,51,0),355], [String.fromCharCode(116,114,101,102,95,111,95,55,51,0),953]]);
         relatedY = new Map([[`${relatedY.size}`, relatedY.size << (Math.min(selection7.length, 1))]]);
      if (Array.from(window_jH.keys()).includes(`${projecty}`)) {
         projecty += parseFloat(`${2 * window_jH.size}`);
      }
         halfh += "3";
      while (1 >= (4 * window_jH.size) || 3 >= (window_jH.size * 4)) {
          let overZ = false;
          let short_ha = String.fromCharCode(100,105,103,101,115,116,115,95,57,95,50,54,0);
         halfh = `${(String.fromCharCode(81,0) == short_ha ? (overZ ? 3 : 2) : short_ha.length)}`;
         break;
      }
      for (let r = 0; r < 2; r++) {
          let baiduD: Array<any> = [633, 747, 516];
          let iconnotificationnewB: Array<any> = [372, 789];
          let cornershootQ = String.fromCharCode(103,95,55,53,0);
          let pointM: Array<any> = [602, 398, 791];
          let pinga = true;
         relatedY = new Map([[`${window_jH.size}`, cornershootQ.length >> (Math.min(Math.abs(3), 5))]]);
         baiduD.push(3);
         iconnotificationnewB = [baiduD.length];
         cornershootQ = `${(iconnotificationnewB.length | (pinga ? 3 : 3))}`;
         pointM = [(1 >> (Math.min(3, Math.abs((pinga ? 4 : 1)))))];
      }
      for (let r = 0; r < 2; r++) {
          let dycreatorz = 3.0;
          let holdert = 0.0;
          let shareda = 3.0;
          let castX = String.fromCharCode(104,95,52,55,95,116,114,105,108,105,110,101,97,114,0);
          let template_nC = 1;
         qaagT.set(`${window_jH.size}`, 2 - relatedY.size);
         dycreatorz += parseInt(`${dycreatorz}`) >> (Math.min(5, Math.abs(template_nC)));
         holdert += parseFloat(`${1 / (Math.max(parseInt(`${holdert}`), 7))}`);
         shareda *= parseFloat(`${parseInt(`${holdert}`)}`);
         castX += `${3 ^ parseInt(`${shareda}`)}`;
         template_nC -= parseInt(`${dycreatorz}`) & 3;
      }
         qaagT = new Map([[`${relatedY.size}`, 1]]);
      let binddatasj = 9014702.0 >= ying0;
      do {
         ying0 /= Math.max(parseFloat(`${halfh.length ^ parseInt(`${ying0}`)}`), 2);
         if (binddatasj) {
            break;
         }
      } while (((ying0 + 5.73) >= 1.81) && binddatasj);
      goalJ += `${librrcL}`;
   }
      librrcL &= goalJ.length * 1;
       let applicationc = String.fromCharCode(119,114,105,116,101,114,95,118,95,49,48,48,0);
       let liveI = String.fromCharCode(97,116,116,114,97,99,116,111,114,95,99,95,56,54,0);
         liveI += `${applicationc.length}`;
      if (applicationc.length >= 3) {
         liveI += "1";
      }
      for (let s = 0; s < 3; s++) {
         liveI += `${applicationc.length}`;
      }
      for (let a = 0; a < 2; a++) {
         applicationc = `${liveI.length}`;
      }
          let privilegeM = String.fromCharCode(115,112,101,99,105,102,105,99,95,114,95,52,50,0);
          let cancelc = false;
          let libloggerv = String.fromCharCode(120,95,55,57,95,114,97,109,112,117,112,0);
         liveI = `${(liveI == String.fromCharCode(118,0) ? privilegeM.length : liveI.length)}`;
         privilegeM = `${((cancelc ? 1 : 2))}`;
         cancelc = (((!cancelc ? libloggerv.length : 71) >> (Math.min(libloggerv.length, 2))) <= 71);
       let iconlogoutd = String.fromCharCode(120,95,54,50,95,99,111,100,101,119,111,114,100,0);
      reactnativejsp = "1";
   let stringsR = goalJ.length <= 7989053;
   do {
      goalJ += "3";
      if (stringsR) {
         break;
      }
   } while ((libhermesO.startsWith(goalJ)) && stringsR);
   for (let g = 0; g < 2; g++) {
      unimplementedviewO += `${(goalJ == String.fromCharCode(50,0) ? googles.length : goalJ.length)}`;
   }
      libhermesO += "2";

        const utcTime = Math.floor((new Date()).getTime() / 1000);

   for (let t = 0; t < 2; t++) {
       let penaltyt = String.fromCharCode(99,117,115,116,111,109,105,122,101,114,95,48,95,52,55,0);
       let skipm = 2.0;
       let yellowscoreballV = String.fromCharCode(105,103,110,111,114,101,100,95,109,95,55,48,0);
          let ajaxp = 2.0;
         penaltyt += `${3 * penaltyt.length}`;
         ajaxp *= 2 / (Math.max(5, parseInt(`${ajaxp}`)));
         penaltyt += `${(yellowscoreballV == String.fromCharCode(68,0) ? yellowscoreballV.length : parseInt(`${skipm}`))}`;
      if (yellowscoreballV.includes(`${skipm}`)) {
          let expandy = String.fromCharCode(110,95,53,54,95,109,109,105,117,116,105,108,115,0);
          let toponn: Array<any> = [32, 385, 62];
          let adultj: Map<any, any> = new Map([[String.fromCharCode(113,95,57,57,95,99,111,109,112,97,114,101,0),true ], [String.fromCharCode(117,95,56,95,97,100,118,97,110,99,105,110,103,0),false ]]);
          let bgvipxvod8 = 3;
          let hongkongm = 5;
         skipm += parseFloat(`${parseInt(`${skipm}`)}`);
         expandy = `${(String.fromCharCode(80,0) == expandy ? hongkongm : expandy.length)}`;
         toponn.push(toponn.length);
         adultj = new Map([[`${adultj.size}`, adultj.size - 3]]);
         bgvipxvod8 |= 3;
         hongkongm -= adultj.size;
      }
      while ((3 << (Math.min(4, penaltyt.length))) >= 5) {
         skipm -= parseFloat(`${3 | penaltyt.length}`);
         break;
      }
         skipm *= parseFloat(`${parseInt(`${skipm}`) ^ 2}`);
      let fullscreenmaxW = 7570837.0 <= skipm;
      do {
          let temp4 = 4;
          let firebase1 = String.fromCharCode(110,101,111,110,116,101,115,116,95,102,95,56,53,0);
          let l_imageR = 2.0;
         skipm /= Math.max(3, parseFloat(`${parseInt(`${l_imageR}`)}`));
         temp4 <<= Math.min(Math.abs(firebase1.length + temp4), 2);
         firebase1 = "3";
         if (fullscreenmaxW) {
            break;
         }
      } while (fullscreenmaxW && (skipm >= parseFloat(`${penaltyt.length}`)));
         penaltyt = `${3 ^ parseInt(`${skipm}`)}`;
         yellowscoreballV = `${penaltyt.length ^ parseInt(`${skipm}`)}`;
         skipm /= Math.max(parseFloat(`${penaltyt.length}`), 4);
      updatesm *= parseFloat(`${reactnativejsp.length}`);
   }
   if (1 <= (reactnativejsp.length * 3) && (3 + reactnativejsp.length) <= 3) {
      updatesm -= parseFloat(`${goalJ.length & 3}`);
   }
   if (mailw == 3.62) {
      mailw -= libhermesO.length >> (Math.min(2, goalJ.length));
   }
       let regeng8 = 2.0;
       let defaultroombgi = String.fromCharCode(100,121,110,108,105,110,107,0);
         regeng8 += (String.fromCharCode(69,0) == defaultroombgi ? parseInt(`${regeng8}`) : defaultroombgi.length);
         defaultroombgi += `${defaultroombgi.length - parseInt(`${regeng8}`)}`;
      librrcL /= Math.max(foundS.length + parseInt(`${mailw}`), 2);
   while (!foundS.endsWith(`${unimplementedviewO.length}`)) {
      foundS += `${(String.fromCharCode(121,0) == foundS ? parseInt(`${mailw}`) : foundS.length)}`;
      break;
   }
      updatesm += parseFloat(`${googles.length}`);

        const json: mayi_Cornershoot = {
            stats: this.#events,
            created_at: utcTime,
        }
        const jsonStr = JSON.stringify(json);

      yellowscoreballf *= splashq.length;
   let redirectD = googles.length <= 5767183;
   do {
       let long_8wM = 4.0;
       let playercommond = String.fromCharCode(116,114,117,116,104,0);
          let linea = true;
          let orangeuparrowT = false;
          let nodey = 3.0;
         long_8wM -= ((linea ? 3 : 4) - (orangeuparrowT ? 1 : 2));
         linea = 7.72 < nodey;
         orangeuparrowT = nodey < 2.16;
      while (5 >= (3 - parseInt(`${long_8wM}`)) && 5.89 >= (2.45 - long_8wM)) {
          let sellmathbackgroundd = 0.0;
          let gradlewF: Map<any, any> = new Map([[String.fromCharCode(111,95,50,50,95,115,101,108,101,99,116,105,111,110,0),String.fromCharCode(114,95,56,48,95,112,114,111,99,101,115,115,101,100,0)], [String.fromCharCode(97,108,97,99,100,115,112,0),String.fromCharCode(118,95,53,54,95,116,112,101,108,0)], [String.fromCharCode(114,101,112,111,114,116,101,114,95,105,95,49,50,0),String.fromCharCode(117,118,104,111,114,105,122,111,110,116,97,108,0)]]);
          let leagueE = 0.0;
          let borderlessY: Map<any, any> = new Map([[String.fromCharCode(110,95,56,56,95,104,107,100,102,0),false ], [String.fromCharCode(117,95,56,51,95,112,101,114,112,105,120,101,108,0),true ], [String.fromCharCode(108,97,122,105,108,121,0),true ]]);
          let singaporeE = String.fromCharCode(98,108,117,101,116,111,111,116,104,0);
         long_8wM *= playercommond.length - parseInt(`${leagueE}`);
         sellmathbackgroundd += parseFloat(`${singaporeE.length | gradlewF.size}`);
         gradlewF.set(`${sellmathbackgroundd}`, 1);
         leagueE *= parseInt(`${sellmathbackgroundd}`);
         borderlessY.set(`${borderlessY.size}`, borderlessY.size - 3);
         singaporeE += "2";
         break;
      }
         long_8wM -= parseInt(`${long_8wM}`) / (Math.max(3, 7));
         playercommond += `${(String.fromCharCode(104,0) == playercommond ? parseInt(`${long_8wM}`) : playercommond.length)}`;
      while (5.96 <= long_8wM) {
          let privatechatbgQ: Array<any> = [506, 853];
          let iconmorey = 1.0;
          let gradlewI = 4.0;
          let long_t0 = String.fromCharCode(105,99,119,114,115,105,0);
         long_8wM *= (long_t0 == String.fromCharCode(90,0) ? parseInt(`${gradlewI}`) : long_t0.length);
         privatechatbgQ = [parseInt(`${iconmorey}`) + 3];
         iconmorey -= parseFloat(`${parseInt(`${iconmorey}`) / 2}`);
         gradlewI -= 2 * privatechatbgQ.length;
         break;
      }
         long_8wM -= playercommond.length & 3;
      googles = [2];
      if (redirectD) {
         break;
      }
   } while (redirectD && (!foundS.endsWith(`${googles.length}`)));
   let statisticsT = librrcL <= 5935056;
   do {
      librrcL /= Math.max(1, foundS.length & 3);
      if (statisticsT) {
         break;
      }
   } while (statisticsT && (splashq.length == librrcL));
      foundS = `${googles.length}`;
       let iconarrowrightwhitey = 2;
       let libavdevicev: Array<any> = [String.fromCharCode(114,101,100,97,99,116,101,100,95,57,95,50,49,0), String.fromCharCode(102,95,53,49,95,101,116,104,101,114,0)];
         iconarrowrightwhitey |= iconarrowrightwhitey & 1;
      let darku = 5557345 >= libavdevicev.length;
      do {
         libavdevicev = [iconarrowrightwhitey + 2];
         if (darku) {
            break;
         }
      } while (((libavdevicev.length << (Math.min(Math.abs(5), 4))) < 1 && 4 < (5 << (Math.min(1, Math.abs(iconarrowrightwhitey))))) && darku);
      let linkN = 4967821 >= iconarrowrightwhitey;
      do {
         iconarrowrightwhitey ^= iconarrowrightwhitey << (Math.min(2, Math.abs(3)));
         if (linkN) {
            break;
         }
      } while (linkN && (1 == libavdevicev.length));
      if (4 < (4 << (Math.min(4, Math.abs(iconarrowrightwhitey))))) {
         libavdevicev = [1];
      }
       let predictiondefaultb = true;
       let constantsb = false;
      if (!predictiondefaultb && (libavdevicev.length + 1) >= 3) {
         predictiondefaultb = libavdevicev.includes(iconarrowrightwhitey);
      }
      reactnativejsp = `${2 * iconarrowrightwhitey}`;
      libhermesO = `${unimplementedviewO.length}`;

        AsyncStorage.setItem(this.#localKey, jsonStr);
    }
}