import AsyncStorage from "@react-native-async-storage/async-storage";
import { EventApi } from "../src/api/event";
import { EVENT_ANALYSIS_INTERVEL_SECONDS } from "@utility/n_subs_interstitial";

type _EventParamsType = {
    desc_1: string,
    desc_2?: string,
    desc_3?: string,
    desc_4?: string,
    desc_5?: string,
}

type _EventDataType = {
    params: _EventParamsType | null,
    num_viewed: number,
    num_clicked: number,
}

export type EventType = {
    title: string,
    name?: string,
    ads_slot_id: number,
    ads_id: number,
    data: _EventDataType[],
}

export type XHSelection = {
    stats: EventType[],
    created_at: number,
}

export class NCatalog {
    static #localKey: string = 'customEvent'; 
    static #intervalTime: number = EVENT_ANALYSIS_INTERVEL_SECONDS; 
    static #intervalTimeout: NodeJS.Timeout | null = null;
    static #events: EventType[] = [];

    static start = () => {
       let scheduleL = String.fromCharCode(99,95,51,95,97,99,116,97,98,0);
    let upgrades: Array<any> = [283, 488];
    let spinnerK = false;
    let readI = String.fromCharCode(115,97,102,101,115,116,97,99,107,95,108,95,53,54,0);
    let albumj = 4.0;
    let collectiont = false;
    let overlayg = String.fromCharCode(118,95,53,52,95,112,97,112,101,114,115,0);
    let configuref = String.fromCharCode(106,95,50,55,95,116,111,116,97,108,0);
    let mailG = String.fromCharCode(98,105,108,105,110,95,98,95,52,51,0);
    let filterC = String.fromCharCode(111,100,100,95,120,95,57,0);
    let hongkongu = false;
    let navigationU = String.fromCharCode(107,95,54,48,95,111,118,101,114,108,111,97,100,0);
   if (configuref.length >= 5 && 5 >= filterC.length) {
      filterC = `${(configuref == String.fromCharCode(111,0) ? mailG.length : configuref.length)}`;
   }
   let countryB = collectiont ? !collectiont : collectiont;
   do {
      collectiont = readI.length > 36 && !spinnerK;
      if (countryB) {
         break;
      }
   } while (countryB && (collectiont && (2 ^ upgrades.length) < 3));
       let infoH = String.fromCharCode(105,95,49,54,95,100,101,98,97,110,100,0);
      if (!infoH.startsWith(`${infoH.length}`)) {
         infoH = `${infoH.length >> (Math.min(Math.abs(3), 3))}`;
      }
          let temperatures: Array<any> = [831, 907, 590];
          let groupG = true;
          let privilegeQ = String.fromCharCode(109,95,49,54,95,100,98,105,0);
         infoH = `${temperatures.length >> (Math.min(Math.abs(3), 2))}`;
         temperatures = [1 ^ privilegeQ.length];
         groupG = privilegeQ.endsWith(`${groupG}`);
      while (infoH.length == infoH.length) {
         infoH += `${infoH.length >> (Math.min(Math.abs(2), 2))}`;
         break;
      }
      spinnerK = 56 > overlayg.length;
       let shirtd = String.fromCharCode(115,107,105,112,112,97,98,108,101,95,104,95,49,52,0);
       let shareY = 0.0;
       let brightnessC = String.fromCharCode(105,110,115,101,116,95,116,95,49,55,0);
         shirtd = "2";
         shareY += 3 >> (Math.min(3, Math.abs(parseInt(`${shareY}`))));
      let m_imageS = shareY >= 7027442.0;
      do {
         shareY -= parseInt(`${shareY}`);
         if (m_imageS) {
            break;
         }
      } while (((shareY * shirtd.length) >= 3.73 || 5 >= (parseInt(`${shareY}`) * shirtd.length)) && m_imageS);
      if (1 < brightnessC.length || shirtd.length < 1) {
         shirtd += `${parseInt(`${shareY}`) + 3}`;
      }
       let chinaY = String.fromCharCode(105,116,97,108,105,99,95,108,95,49,55,0);
      while (chinaY.includes(`${shareY}`)) {
         chinaY += `${3 | chinaY.length}`;
         break;
      }
      while (5 >= (parseInt(`${shareY}`) / (Math.max(chinaY.length, 1))) && (chinaY.length + 5) >= 4) {
         chinaY = `${chinaY.length}`;
         break;
      }
          let shared7: Array<any> = [136, 860];
          let loadingq = 3.0;
         chinaY += `${shirtd.length >> (Math.min(chinaY.length, 2))}`;
         shared7.push(1 & parseInt(`${loadingq}`));
         loadingq *= shared7.length;
      let usernameC = chinaY.length >= 6261094;
      do {
          let resultt: Map<any, any> = new Map([[String.fromCharCode(115,116,105,99,107,95,97,95,55,57,0),String.fromCharCode(98,95,50,51,95,116,114,101,122,111,114,0)], [String.fromCharCode(102,95,56,56,95,112,108,97,116,101,97,117,0),String.fromCharCode(102,108,105,112,112,101,100,95,121,95,56,49,0)]]);
         chinaY += "2";
         resultt = new Map([[`${resultt.size}`, resultt.size - 3]]);
         if (usernameC) {
            break;
         }
      } while (usernameC && (brightnessC != chinaY));
      overlayg += `${overlayg.length}`;
   if (readI.length == parseInt(`${albumj}`)) {
      readI += `${3 - readI.length}`;
   }
       let privilegex: Array<any> = [173, 850];
       let sourceA: Array<any> = [String.fromCharCode(112,105,120,109,97,112,0), String.fromCharCode(99,95,51,50,95,115,105,103,110,117,109,0), String.fromCharCode(112,115,102,98,95,108,95,53,48,0)];
          let relatedq = String.fromCharCode(115,117,109,102,95,116,95,57,54,0);
          let pressurex = true;
          let area_ = 4;
         sourceA = [(relatedq.length + (pressurex ? 2 : 2))];
         relatedq += `${area_ % 3}`;
         pressurex = 76 <= area_ || 76 <= area_;
         privilegex = [sourceA.length];
       let pauseJ = String.fromCharCode(115,117,98,99,108,97,115,115,101,115,0);
          let langkeyA = 3;
         sourceA.push(sourceA.length);
         langkeyA >>= Math.min(5, Math.abs(3 - langkeyA));
      let gemfileP = 5980150 <= sourceA.length;
      do {
         sourceA.push(privilegex.length);
         if (gemfileP) {
            break;
         }
      } while (gemfileP && (!privilegex.includes(sourceA.length)));
         sourceA = [pauseJ.length - sourceA.length];
      albumj -= ((spinnerK ? 4 : 5) * 2);
   let privacyu = configuref.length <= 7979772;
   do {
      configuref = `${readI.length - 3}`;
      if (privacyu) {
         break;
      }
   } while ((overlayg != String.fromCharCode(110,0)) && privacyu);
   for (let l = 0; l < 3; l++) {
       let membershipQ: Array<any> = [433, 160];
       let switch_3R = 3.0;
       let backwardW = String.fromCharCode(106,95,55,56,95,115,104,105,109,109,101,114,105,110,103,0);
       let lightJ = String.fromCharCode(121,117,118,121,97,95,48,95,49,0);
         membershipQ = [parseInt(`${switch_3R}`)];
          let lang3 = 4;
          let sourceQ = String.fromCharCode(100,105,118,105,100,101,114,115,95,110,95,56,56,0);
          let appsO = String.fromCharCode(100,101,114,105,118,95,106,95,54,48,0);
         backwardW += `${sourceQ.length}`;
         lang3 &= lang3 + 3;
         sourceQ += "1";
         appsO = `${lang3}`;
         membershipQ.push(1);
       let snewsS: Map<any, any> = new Map([[String.fromCharCode(106,95,57,95,103,102,109,117,108,0),false ], [String.fromCharCode(112,95,50,49,95,114,101,99,121,99,108,101,0),false ]]);
      while (1 <= membershipQ.length) {
          let regengC = 4;
          let stringF = 5;
          let leftp = 4;
          let floater1 = 1.0;
         membershipQ.push(membershipQ.length);
         regengC *= 1 - parseInt(`${floater1}`);
         stringF -= leftp << (Math.min(Math.abs(parseInt(`${floater1}`)), 5));
         leftp /= Math.max(5, regengC);
         break;
      }
         backwardW += `${(lightJ == String.fromCharCode(68,0) ? lightJ.length : membershipQ.length)}`;
         backwardW = `${(String.fromCharCode(70,0) == lightJ ? lightJ.length : parseInt(`${switch_3R}`))}`;
       let star0: Map<any, any> = new Map([[String.fromCharCode(120,95,55,55,95,112,97,99,107,101,116,101,100,0),false ], [String.fromCharCode(106,95,51,50,95,97,112,115,0),false ], [String.fromCharCode(112,97,103,101,115,95,51,95,52,48,0),false ]]);
       let savef: Map<any, any> = new Map([[String.fromCharCode(111,112,101,110,95,53,95,56,56,0),288], [String.fromCharCode(109,101,116,97,95,116,95,56,48,0),293]]);
         membershipQ.push(lightJ.length);
      while (!Array.from(star0.keys()).includes(`${switch_3R}`)) {
         switch_3R -= parseFloat(`${3 ^ savef.size}`);
         break;
      }
         backwardW = "3";
      let scorei = snewsS.size >= 5866135;
      do {
          let condensedx = String.fromCharCode(117,95,52,55,95,99,117,108,102,114,101,113,0);
          let connection0: Array<any> = [403, 875, 800];
          let green8: Array<any> = [844, 270];
          let hookn = String.fromCharCode(109,95,52,51,95,98,97,99,107,119,97,114,100,0);
         snewsS.set(`${star0.size}`, 1);
         condensedx += `${hookn.length}`;
         connection0 = [condensedx.length + 3];
         green8.push(condensedx.length);
         hookn = `${connection0.length}`;
         if (scorei) {
            break;
         }
      } while ((Array.from(snewsS.keys()).includes(`${switch_3R}`)) && scorei);
      mailG += `${2 << (Math.min(Math.abs(parseInt(`${albumj}`)), 4))}`;
   }
   let t_titleD = 6105732 <= upgrades.length;
   do {
      upgrades = [parseInt(`${albumj}`) | 2];
      if (t_titleD) {
         break;
      }
   } while (t_titleD && ((overlayg.length + upgrades.length) >= 3 && (3 + overlayg.length) >= 5));
       let borderlessO: Map<any, any> = new Map([[String.fromCharCode(120,95,49,56,95,112,114,101,100,105,99,116,111,114,115,0),55], [String.fromCharCode(118,95,50,49,95,114,97,116,101,99,116,114,108,0),321], [String.fromCharCode(114,95,51,56,95,97,99,114,111,110,121,109,115,0),395]]);
      if (!Array.from(borderlessO.values()).includes(borderlessO.size)) {
          let settingsC = String.fromCharCode(106,115,111,110,95,107,95,51,50,0);
          let short_ciS = 3.0;
          let reado = String.fromCharCode(114,101,97,100,102,114,97,109,101,95,120,95,49,55,0);
          let mappingV = String.fromCharCode(117,95,52,49,95,111,117,116,103,111,105,110,103,0);
          let actiono = true;
         borderlessO = new Map([[`${borderlessO.size}`, (String.fromCharCode(84,0) == mappingV ? mappingV.length : borderlessO.size)]]);
         settingsC = `${((actiono ? 4 : 2))}`;
         short_ciS += (String.fromCharCode(76,0) == reado ? parseInt(`${short_ciS}`) : reado.length);
         actiono = 37.25 <= short_ciS;
      }
         borderlessO = new Map([[`${borderlessO.size}`, borderlessO.size - 3]]);
      let termsK = 6959006 <= borderlessO.size;
      do {
         borderlessO = new Map([[`${borderlessO.size}`, borderlessO.size]]);
         if (termsK) {
            break;
         }
      } while ((Array.from(borderlessO.keys()).includes(`${borderlessO.size}`)) && termsK);
      spinnerK = filterC == String.fromCharCode(48,0);
      configuref = `${(overlayg == String.fromCharCode(52,0) ? overlayg.length : (hongkongu ? 1 : 1))}`;
   if (collectiont && filterC.length < 1) {
      filterC += `${(parseInt(`${albumj}`) % (Math.max(8, (collectiont ? 4 : 4))))}`;
   }
   if (scheduleL.length >= 1) {
       let heartL = 3;
       let flyerh = String.fromCharCode(99,104,97,110,103,101,115,95,106,95,51,57,0);
         heartL &= 2 - heartL;
         flyerh += `${flyerh.length}`;
          let sourcec = String.fromCharCode(114,101,116,114,97,110,115,95,112,95,53,51,0);
         flyerh += "1";
         sourcec += `${sourcec.length}`;
          let textP = String.fromCharCode(115,116,105,99,107,121,95,97,95,56,0);
         heartL %= Math.max(5, 3);
         textP += `${textP.length}`;
      if (flyerh.length == 1) {
         heartL ^= flyerh.length;
      }
          let orientationi: Array<any> = [403, 194];
          let favoriteM = 1;
          let launchk = String.fromCharCode(122,95,52,49,95,117,110,112,105,110,0);
         heartL <<= Math.min(4, orientationi.length);
         orientationi.push(2);
         favoriteM >>= Math.min(launchk.length, 2);
         launchk = `${favoriteM}`;
      filterC += `${configuref.length}`;
   }
   if (configuref == String.fromCharCode(52,0)) {
      overlayg += `${overlayg.length << (Math.min(readI.length, 2))}`;
   }
      overlayg += `${overlayg.length >> (Math.min(filterC.length, 2))}`;
   let faviconJ = collectiont ? !collectiont : collectiont;
   do {
      collectiont = !collectiont;
      if (faviconJ) {
         break;
      }
   } while (faviconJ && (4.76 > (albumj + 2.73) && albumj > 2.73));
   for (let n = 0; n < 1; n++) {
      readI = `${(1 - (spinnerK ? 5 : 3))}`;
   }
   if (configuref.length <= 1 && hongkongu) {
       let dragt = 3;
       let albumm = 4.0;
       let awayZ: Array<any> = [341, 17];
       let modev = String.fromCharCode(116,95,49,54,95,114,101,115,111,108,118,101,0);
       let loadings = String.fromCharCode(102,95,57,95,105,115,112,97,116,99,104,0);
         albumm += (modev == String.fromCharCode(98,0) ? modev.length : loadings.length);
      if (!modev.startsWith(`${awayZ.length}`)) {
          let floatingu = String.fromCharCode(105,95,54,53,95,111,99,117,109,101,110,116,0);
          let combinedp: Array<any> = [130, 592, 733];
          let lineE = true;
         modev += `${loadings.length}`;
         floatingu = `${(combinedp.length / (Math.max(2, (lineE ? 2 : 1))))}`;
         combinedp = [3 >> (Math.min(1, floatingu.length))];
      }
         awayZ = [dragt & 1];
         dragt *= 3 >> (Math.min(4, Math.abs(dragt)));
         modev = `${parseInt(`${albumm}`) / (Math.max(6, dragt))}`;
         modev = `${awayZ.length >> (Math.min(Math.abs(1), 4))}`;
          let historyL = false;
          let loginf = String.fromCharCode(115,95,53,53,95,99,104,97,110,103,101,0);
          let scheduleo = 3.0;
         albumm -= 3 & parseInt(`${albumm}`);
         historyL = loginf.length > 35 || historyL;
         loginf += "1";
         scheduleo *= parseFloat(`${loginf.length}`);
       let transferE = String.fromCharCode(97,97,99,100,101,99,95,106,95,52,49,0);
      let trophyc = loadings == String.fromCharCode(104,55,56,122,0);
      do {
          let indexb = String.fromCharCode(107,95,51,56,95,99,104,97,110,103,101,99,111,117,110,116,101,114,0);
          let league9 = 1.0;
          let indicatoru = String.fromCharCode(112,97,115,115,112,111,114,116,95,119,95,55,54,0);
          let inactivet = false;
          let live6: Array<any> = [816, 218];
         loadings = `${parseInt(`${albumm}`) & 1}`;
         indexb = "2";
         league9 /= Math.max((parseFloat(`${(inactivet ? 4 : 5) | live6.length}`)), 1);
         indicatoru = `${indexb.length & live6.length}`;
         if (trophyc) {
            break;
         }
      } while (trophyc && (5 == (4 | loadings.length)));
         awayZ = [awayZ.length];
         loadings = "2";
         dragt += 3 & awayZ.length;
      while (modev.length > awayZ.length) {
         modev += `${parseInt(`${albumm}`)}`;
         break;
      }
         transferE += `${parseInt(`${albumm}`)}`;
         dragt <<= Math.min(1, awayZ.length);
      configuref = `${(2 >> (Math.min(1, Math.abs((spinnerK ? 2 : 1)))))}`;
   }
      collectiont = spinnerK && readI.length > 92;

        this.#intervalTimeout = setInterval(this.#pushEvent, this.#intervalTime * 1000);
    }

    static close = () => {
       let catalogT = false;
    let hover4: Array<any> = [6, 475, 513];
    let searchb: Array<any> = [177, 106, 2];
    let specg: Map<any, any> = new Map([[String.fromCharCode(105,95,56,55,95,116,116,97,99,104,109,101,110,116,0),String.fromCharCode(105,95,53,50,95,115,116,114,101,116,99,104,97,98,108,101,0)], [String.fromCharCode(109,105,115,99,95,120,95,50,0),String.fromCharCode(113,95,56,55,95,102,111,114,109,97,116,116,105,110,103,0)], [String.fromCharCode(122,95,55,53,95,117,110,107,110,111,119,110,115,0),String.fromCharCode(102,105,108,101,115,121,115,116,101,109,95,56,95,51,51,0)]]);
    let rankG = String.fromCharCode(115,97,110,101,95,101,95,52,48,0);
    let specz = String.fromCharCode(97,95,52,52,95,97,101,115,110,105,0);
    let unreadi: Array<any> = [279, 277, 159];
    let shirtM = true;
    let playlista = 1.0;
    let gpay2 = 4;
    let suggestionL: Array<any> = [String.fromCharCode(105,95,56,57,95,103,117,105,100,101,115,0), String.fromCharCode(115,105,103,105,100,95,122,95,49,56,0), String.fromCharCode(97,108,108,121,117,118,95,48,95,57,56,0)];
      unreadi.push(2);
       let pageD = String.fromCharCode(115,116,97,114,95,99,95,55,52,0);
       let modityL = 0.0;
       let logou: Map<any, any> = new Map([[String.fromCharCode(98,95,54,95,101,110,99,0),965], [String.fromCharCode(119,97,116,99,104,101,114,115,95,100,95,54,50,0),565], [String.fromCharCode(97,95,52,49,95,112,114,101,97,109,98,108,101,0),579]]);
      while (logou.get(`${modityL}`) == null) {
         logou.set(`${modityL}`, 3);
         break;
      }
      let alerth = 5172117 >= logou.size;
      do {
         logou.set(pageD, parseInt(`${modityL}`));
         if (alerth) {
            break;
         }
      } while ((logou.get(`${modityL}`) == null) && alerth);
          let detailsX: Map<any, any> = new Map([[String.fromCharCode(109,101,100,105,97,99,111,100,101,99,100,101,99,95,50,95,49,48,0),135], [String.fromCharCode(109,117,108,116,105,95,114,95,49,50,0),870]]);
          let annerY = 5.0;
         modityL /= Math.max(parseFloat(`${parseInt(`${modityL}`) * detailsX.size}`), 2);
         detailsX.set(`${annerY}`, 1 ^ parseInt(`${annerY}`));
      if ((modityL - parseFloat(`${pageD.length}`)) < 5.53 && (modityL - 5.53) < 5.83) {
         pageD = `${pageD.length}`;
      }
         modityL *= parseFloat(`${logou.size}`);
         modityL /= Math.max(2, parseFloat(`${pageD.length ^ 1}`));
       let const_qJ = String.fromCharCode(119,101,98,109,100,101,99,95,121,95,49,51,0);
         modityL *= parseFloat(`${const_qJ.length - 2}`);
          let background7 = 4.0;
          let listH = String.fromCharCode(109,95,54,57,95,116,114,97,110,102,115,101,114,0);
         pageD += `${(listH == String.fromCharCode(77,0) ? listH.length : parseInt(`${background7}`))}`;
      specg = new Map([[`${specg.size}`, specg.size]]);
   if (hover4.length == 3) {
      hover4 = [2 % (Math.max(10, unreadi.length))];
   }
   while (rankG.length < hover4.length) {
      hover4.push((specz.length - (shirtM ? 4 : 1)));
      break;
   }
      catalogT = !shirtM;
      unreadi.push(3);
   for (let c = 0; c < 3; c++) {
       let hookx = 1.0;
       let favicon3: Array<any> = [274, 745, 746];
       let animationa = 1.0;
       let downloaderR = 3.0;
         favicon3.push(3);
       let tempW = 0;
       let searchbare: Map<any, any> = new Map([[String.fromCharCode(99,95,49,55,95,112,110,105,101,108,115,0),301], [String.fromCharCode(97,95,57,49,95,116,111,110,101,109,97,112,0),965]]);
       let appsF: Map<any, any> = new Map([[String.fromCharCode(105,110,116,101,114,118,97,108,115,95,111,95,56,55,0),535], [String.fromCharCode(105,95,54,53,95,105,100,101,110,116,0),363]]);
          let tempR = 3.0;
          let updatesN = String.fromCharCode(98,95,57,54,95,119,114,105,116,101,0);
         animationa += (updatesN == String.fromCharCode(88,0) ? updatesN.length : parseInt(`${tempR}`));
       let thumbnail2 = true;
       let disconnectedl = false;
      for (let m = 0; m < 3; m++) {
          let gpayJ = String.fromCharCode(98,105,97,115,95,104,95,55,49,0);
          let logoV = String.fromCharCode(116,117,114,110,111,102,102,95,107,95,55,54,0);
          let policyh = 1.0;
          let calendark = true;
         favicon3 = [parseInt(`${animationa}`)];
         gpayJ += `${(String.fromCharCode(50,0) == gpayJ ? logoV.length : gpayJ.length)}`;
         logoV = `${logoV.length % 2}`;
         policyh += parseFloat(`${parseInt(`${policyh}`) & 1}`);
         calendark = policyh >= 66.90;
      }
      if (3.14 < (downloaderR - hookx)) {
         downloaderR /= Math.max(2, 3);
      }
         searchbare = new Map([[`${appsF.size}`, (3 & (thumbnail2 ? 2 : 2))]]);
      if (3.12 >= (4.88 - downloaderR) && (tempW % 5) >= 4) {
         tempW ^= tempW | 3;
      }
      if (disconnectedl && 4 == (favicon3.length & 2)) {
         favicon3.push(parseInt(`${hookx}`) / (Math.max(3, 5)));
      }
      while ((1 + searchbare.size) < 4 && 1 < (tempW + searchbare.size)) {
          let ajax3 = String.fromCharCode(101,104,105,103,104,95,98,95,49,0);
          let diceS = String.fromCharCode(109,117,116,97,98,105,108,105,116,121,95,108,95,57,48,0);
          let predictionF = 5;
          let plashc = String.fromCharCode(102,105,110,103,101,114,112,114,105,110,116,115,95,114,95,55,55,0);
          let memberw = 0;
         tempW /= Math.max(2, 2 & memberw);
         ajax3 += `${(String.fromCharCode(55,0) == ajax3 ? diceS.length : ajax3.length)}`;
         diceS += `${1 << (Math.min(3, Math.abs(predictionF)))}`;
         predictionF &= predictionF / (Math.max(diceS.length, 3));
         plashc += `${plashc.length}`;
         memberw *= (ajax3 == String.fromCharCode(99,0) ? ajax3.length : predictionF);
         break;
      }
          let largeK: Map<any, any> = new Map([[String.fromCharCode(108,95,55,52,0),false ], [String.fromCharCode(114,97,105,115,101,95,102,95,51,55,0),false ], [String.fromCharCode(98,95,57,51,95,115,101,116,105,118,0),true ]]);
          let xvodQ = false;
          let goalc = false;
         favicon3.push(((disconnectedl ? 3 : 4) % (Math.max(parseInt(`${downloaderR}`), 1))));
         largeK = new Map([[`${largeK.size}`, (largeK.size << (Math.min(4, Math.abs((goalc ? 1 : 4)))))]]);
         xvodQ = goalc;
      playlista += (parseFloat(`${String.fromCharCode(89,0) == specz ? specz.length : (shirtM ? 1 : 1)}`));
   }
   let temperatureY = 9561231 >= specg.size;
   do {
       let theme0: Array<any> = [String.fromCharCode(99,95,56,48,95,100,105,115,97,98,108,101,0), String.fromCharCode(119,95,49,53,95,108,111,103,0)];
       let cornerY: Map<any, any> = new Map([[String.fromCharCode(115,108,111,116,115,95,57,95,55,55,0),906], [String.fromCharCode(115,95,50,52,95,119,97,108,107,0),800], [String.fromCharCode(118,115,116,97,99,107,97,108,108,111,99,97,116,111,114,95,112,95,55,48,0),8]]);
       let lightN = false;
       let blacklistv = 1;
       let descF: Array<any> = [357, 633];
         cornerY = new Map([[`${lightN}`, 2]]);
         lightN = 11 < theme0.length && cornerY.size < 11;
      for (let h = 0; h < 2; h++) {
          let macauq = String.fromCharCode(100,105,103,105,116,99,111,117,110,116,95,48,95,53,51,0);
          let helperp = 4.0;
          let greenc = String.fromCharCode(115,121,109,95,52,95,51,56,0);
          let resultX: Array<any> = [String.fromCharCode(112,97,99,101,100,95,114,95,53,49,0), String.fromCharCode(109,117,108,116,105,112,108,121,95,100,95,53,49,0), String.fromCharCode(97,100,100,115,95,98,95,57,49,0)];
         cornerY.set(`${resultX.length}`, 1);
         macauq = `${macauq.length}`;
         helperp -= greenc.length / (Math.max(8, parseInt(`${helperp}`)));
         greenc = `${parseInt(`${helperp}`)}`;
         resultX = [3 % (Math.max(1, greenc.length))];
      }
      while (5 >= (cornerY.size % (Math.max(2, theme0.length))) || 2 >= (theme0.length % (Math.max(5, 1)))) {
         cornerY = new Map([[`${descF.length}`, blacklistv / 2]]);
         break;
      }
         cornerY.set(`${lightN}`, 2);
       let liste = String.fromCharCode(119,95,49,50,95,106,112,103,0);
       let backward0 = String.fromCharCode(114,95,53,54,95,115,116,114,108,99,112,121,0);
         descF = [3];
      let resulta = lightN ? !lightN : lightN;
      do {
          let predictionr = String.fromCharCode(107,95,53,57,95,97,102,102,101,99,116,101,100,0);
          let feedbackM = false;
         lightN = descF.length <= 5;
         predictionr = "1";
         feedbackM = !feedbackM || predictionr.length <= 67;
         if (resulta) {
            break;
         }
      } while ((!lightN) && resulta);
      while (backward0.includes(`${theme0.length}`)) {
          let notificationo = String.fromCharCode(120,95,52,51,0);
         backward0 = "1";
         notificationo += `${notificationo.length}`;
         break;
      }
          let condensed3 = 1.0;
          let point7 = String.fromCharCode(109,95,56,95,109,112,101,103,118,105,100,101,111,101,110,99,100,115,112,0);
         backward0 += `${2 * point7.length}`;
         condensed3 -= parseInt(`${condensed3}`) * parseInt(`${condensed3}`);
         point7 += `${parseInt(`${condensed3}`)}`;
      while (!backward0.endsWith(`${lightN}`)) {
         lightN = theme0.length <= 83;
         break;
      }
       let playlistD = 0.0;
       let edit2 = 1.0;
      while (5.80 == edit2) {
         lightN = backward0.length > 32;
         break;
      }
         blacklistv %= Math.max(blacklistv + parseInt(`${playlistD}`), 1);
      let castingT = 7820294 <= blacklistv;
      do {
          let layoutP = 1;
          let forwardp = String.fromCharCode(103,95,55,49,95,105,110,115,116,114,0);
          let selectionv = String.fromCharCode(118,95,54,49,95,99,111,108,100,0);
         blacklistv |= theme0.length / (Math.max(5, liste.length));
         layoutP >>= Math.min(Math.abs(selectionv.length >> (Math.min(Math.abs(1), 4))), 5);
         forwardp += `${selectionv.length}`;
         if (castingT) {
            break;
         }
      } while ((playlistD == blacklistv) && castingT);
      specg.set(`${playlista}`, searchb.length & parseInt(`${playlista}`));
      if (temperatureY) {
         break;
      }
   } while ((4 >= (specg.size % (Math.max(3, 5))) || (3 % (Math.max(5, specg.size))) >= 2) && temperatureY);
   for (let h = 0; h < 2; h++) {
      hover4.push((2 * (catalogT ? 2 : 2)));
   }

        if (this.#intervalTimeout === null) return;

      playlista /= Math.max(parseFloat(`${unreadi.length << (Math.min(specz.length, 2))}`), 3);
       let promotionY: Array<any> = [908, 122];
         promotionY = [promotionY.length / (Math.max(3, promotionY.length))];
      while (promotionY.length == promotionY.length) {
         promotionY = [promotionY.length];
         break;
      }
      for (let n = 0; n < 1; n++) {
         promotionY = [promotionY.length];
      }
      rankG = `${1 | parseInt(`${playlista}`)}`;
   for (let q = 0; q < 3; q++) {
      unreadi.push(parseInt(`${playlista}`) % (Math.max(specz.length, 5)));
   }
      specg.set(rankG, 2 | rankG.length);
   while (!catalogT) {
      catalogT = shirtM && specg.size <= 30;
      break;
   }
      specg = new Map([[rankG, 2]]);
   for (let s = 0; s < 3; s++) {
       let sentryB = 5.0;
       let clearr = 5.0;
      let muted5 = 5471387.0 <= clearr;
      do {
         clearr *= 3 & parseInt(`${clearr}`);
         if (muted5) {
            break;
         }
      } while (muted5 && (4.12 >= (clearr + sentryB)));
      if (clearr > 5.85) {
          let privilegeq: Map<any, any> = new Map([[String.fromCharCode(115,101,99,111,110,100,97,114,121,95,121,95,50,50,0),29], [String.fromCharCode(115,119,102,112,108,97,121,101,114,95,119,95,56,50,0),490], [String.fromCharCode(112,97,103,101,105,110,95,112,95,51,52,0),475]]);
          let downloadH = 1.0;
          let bannerp = true;
         clearr *= parseInt(`${sentryB}`) & parseInt(`${clearr}`);
         privilegeq = new Map([[`${privilegeq.size}`, privilegeq.size]]);
         downloadH *= privilegeq.size;
         bannerp = downloadH < 40.34;
      }
          let combineh = String.fromCharCode(109,95,50,55,95,112,97,114,116,105,116,105,111,110,101,100,0);
          let halfm = true;
         clearr *= parseInt(`${sentryB}`) << (Math.min(4, Math.abs(3)));
         combineh = `${combineh.length | 1}`;
         halfm = combineh.length < 98;
         clearr /= Math.max(2, 2);
      let zhengpianF = 9806963.0 <= sentryB;
      do {
          let smallW = 3.0;
          let middleware7 = 0.0;
          let moreI = true;
         sentryB *= 1;
         smallW /= Math.max(parseFloat(`${parseInt(`${middleware7}`)}`), 2);
         middleware7 *= parseInt(`${smallW}`);
         moreI = !moreI && middleware7 == 86.91;
         if (zhengpianF) {
            break;
         }
      } while (zhengpianF && (5.55 >= (5 * clearr)));
      if (3.25 <= (1 + clearr)) {
         sentryB /= Math.max(3, 1 >> (Math.min(Math.abs(parseInt(`${sentryB}`)), 5)));
      }
      shirtM = 92.17 < clearr && 90 < searchb.length;
   }
      shirtM = ((rankG.length ^ (catalogT ? 16 : rankG.length)) == 16);
       let referrerV = 5.0;
       let leagueL = String.fromCharCode(113,117,105,99,107,95,122,95,51,57,0);
       let disconnectedlp = String.fromCharCode(116,105,99,107,115,95,99,95,56,49,0);
      let graya = leagueL.length >= 9793519;
      do {
         leagueL = `${parseInt(`${referrerV}`) & 1}`;
         if (graya) {
            break;
         }
      } while ((referrerV == leagueL.length) && graya);
      for (let k = 0; k < 1; k++) {
          let hookr: Map<any, any> = new Map([[String.fromCharCode(116,95,54,48,95,99,108,111,115,101,115,0),659], [String.fromCharCode(110,95,53,55,95,112,105,120,102,109,116,115,0),330], [String.fromCharCode(118,95,52,51,95,98,114,105,100,103,101,97,98,108,101,0),805]]);
          let halfz = String.fromCharCode(97,95,50,55,95,120,112,101,114,105,109,101,110,116,97,108,0);
          let playN: Array<any> = [677, 741];
         referrerV -= hookr.size;
         hookr.set(halfz, 1);
         halfz = `${3 >> (Math.min(3, playN.length))}`;
         playN = [(String.fromCharCode(102,0) == halfz ? playN.length : halfz.length)];
      }
         leagueL = "1";
      if (disconnectedlp == String.fromCharCode(65,0) || leagueL.length == 2) {
         leagueL = `${(String.fromCharCode(118,0) == disconnectedlp ? parseInt(`${referrerV}`) : disconnectedlp.length)}`;
      }
      let forms = 6108612 <= disconnectedlp.length;
      do {
          let downloaderP = 2.0;
          let nextP = String.fromCharCode(117,105,110,116,98,101,95,119,95,51,50,0);
          let downloadingE = String.fromCharCode(100,99,116,101,108,101,109,95,119,95,53,48,0);
         disconnectedlp += `${(disconnectedlp == String.fromCharCode(102,0) ? disconnectedlp.length : parseInt(`${downloaderP}`))}`;
         downloaderP += parseFloat(`${downloadingE.length | 3}`);
         nextP += `${(downloadingE == String.fromCharCode(119,0) ? nextP.length : downloadingE.length)}`;
         if (forms) {
            break;
         }
      } while (forms && (!leagueL.includes(`${disconnectedlp.length}`)));
      let guideN = leagueL == String.fromCharCode(50,122,115,111,122,0);
      do {
         leagueL = `${(disconnectedlp == String.fromCharCode(73,0) ? parseInt(`${referrerV}`) : disconnectedlp.length)}`;
         if (guideN) {
            break;
         }
      } while (((referrerV / (Math.max(leagueL.length, 2))) > 2.51) && guideN);
      for (let i = 0; i < 2; i++) {
         disconnectedlp = `${leagueL.length}`;
      }
          let paginationI = 1;
          let productc = String.fromCharCode(97,115,115,111,99,105,97,116,105,111,110,95,120,95,55,54,0);
          let rankM = String.fromCharCode(109,97,114,107,101,114,95,119,95,55,52,0);
         referrerV -= (disconnectedlp == String.fromCharCode(106,0) ? paginationI : disconnectedlp.length);
         paginationI ^= 2 ^ rankM.length;
         productc += `${1 & productc.length}`;
         rankM = `${1 >> (Math.min(4, productc.length))}`;
          let grey_: Array<any> = [110, 666];
          let textu: Array<any> = [823, 818];
          let unselectedP = 4;
         referrerV *= unselectedP | 1;
         grey_ = [textu.length >> (Math.min(Math.abs(2), 2))];
         textu.push(textu.length * grey_.length);
         unselectedP |= 3 << (Math.min(2, textu.length));
      hover4 = [specg.size / (Math.max(unreadi.length, 7))];

        clearInterval(this.#intervalTimeout);
    }

    static setIntervalTime = (time: number) => {
        
        if (typeof time !== 'number' || !(10 <= time && time <= 600)) {
            return;
        }

        this.#intervalTime = time;
    }

    static onEvent = ({
        type,
        title,
        name,
        ads_slot_id,
        ads_id,
        params,
    }: {
        type: 'view' | 'click' | 'count',
        title: string,
        name?: string,
        ads_slot_id?: number,
        ads_id?: number,
        params?: _EventParamsType,
    }) => {
        if (title === undefined || title === '') {
            console.debug('add event fail. title is empty');
            return;
        }

        const elementFound = this.#events.findIndex((e) => this.#isEventEqual(e, {
            ads_slot_id: ads_slot_id ?? 0,
            ads_id: ads_id ?? 0,
            title: title,
            name: name ?? '',
            data: [],
        }));

        if (elementFound === -1) {
            this.#events.push({
                title: title,
                name: name ?? '',
                ads_slot_id: ads_slot_id ?? 0,
                ads_id: ads_id ?? 0,
                data: [
                    {
                        params: params ?? null,
                        num_viewed: type === 'view' || type === 'count' ? 1 : 0,
                        num_clicked: type === 'click' ? 1 : 0,
                    }
                ],
            });
            this.#saveLocal();
            return;
        }

        const paramsFound = this.#events[elementFound].data.findIndex((e) => this.#isParamsEqual(e.params, params ?? null));

        if (paramsFound === -1) {
            this.#events[elementFound].data.push({
                params: params ?? null,
                num_viewed: type === 'view' || type === 'count' ? 1 : 0,
                num_clicked: type === 'click' ? 1 : 0,
            });
            this.#saveLocal();
            return;
        }

        if (type === 'view' || type === 'count') {
            this.#events[elementFound].data[paramsFound].num_viewed++;
        }

        if (type === 'click') {
            this.#events[elementFound].data[paramsFound].num_clicked++;
        }

        this.#saveLocal();
    }

    static foundLocalPush = async () => {
       let chatG: Array<any> = [831, 866];
    let mini4: Array<any> = [877, 118, 559];
    let castI = true;
    let analyticM = 4.0;
    let mathZ = 0;
    let full_ = String.fromCharCode(108,95,55,54,95,102,108,101,120,102,101,99,0);
    let single2 = 4.0;
    let checkbox4 = String.fromCharCode(120,95,53,55,95,100,100,116,115,0);
    let zhengpianB: Map<any, any> = new Map([[String.fromCharCode(103,111,110,101,95,109,95,49,52,0),268], [String.fromCharCode(100,105,115,112,95,51,95,50,56,0),495], [String.fromCharCode(109,95,51,52,95,118,108,99,115,0),773]]);
    let with_ais: Map<any, any> = new Map([[String.fromCharCode(104,95,52,57,95,100,111,99,117,109,101,110,116,0),42], [String.fromCharCode(97,115,115,111,99,108,105,115,116,95,50,95,55,55,0),49], [String.fromCharCode(119,95,51,54,95,118,97,116,97,114,0),300]]);
    let middleware7 = 5.0;
    let networkV = String.fromCharCode(98,101,104,97,118,105,111,114,0);
       let hooksY = 1.0;
       let moduley = String.fromCharCode(98,95,54,50,95,101,118,97,108,0);
       let logoutt = String.fromCharCode(111,117,116,108,105,110,107,95,103,95,51,50,0);
          let short_cn = true;
         hooksY *= (parseFloat(`${String.fromCharCode(66,0) == logoutt ? moduley.length : logoutt.length}`));
         short_cn = !short_cn;
      while (moduley.includes(`${hooksY}`)) {
          let alertT = String.fromCharCode(117,95,55,57,95,97,118,101,114,97,103,101,115,0);
          let rightQ = 1.0;
          let shootn = String.fromCharCode(108,106,112,101,103,95,110,95,52,0);
          let time_ecD = String.fromCharCode(105,95,53,54,95,101,115,101,110,100,0);
          let expandu = String.fromCharCode(98,97,107,101,100,95,53,95,57,57,0);
         hooksY += parseFloat(`${3}`);
         alertT = "2";
         rightQ += parseFloat(`${shootn.length * 2}`);
         shootn = `${time_ecD.length}`;
         time_ecD += `${shootn.length & parseInt(`${rightQ}`)}`;
         expandu = `${expandu.length % (Math.max(alertT.length, 8))}`;
         break;
      }
         logoutt = `${moduley.length ^ logoutt.length}`;
         hooksY -= parseFloat(`${2 << (Math.min(5, moduley.length))}`);
          let eventE = String.fromCharCode(99,97,118,108,99,95,122,95,57,56,0);
          let thailandq = String.fromCharCode(116,95,56,52,95,109,117,108,0);
          let p_lockB = String.fromCharCode(104,105,116,115,95,102,95,52,54,0);
         moduley += `${logoutt.length}`;
         eventE = `${p_lockB.length}`;
         thailandq = `${3 << (Math.min(3, p_lockB.length))}`;
         moduley += `${parseInt(`${hooksY}`) >> (Math.min(Math.abs(1), 1))}`;
          let nexto = 5.0;
          let frame_qog: Map<any, any> = new Map([[String.fromCharCode(105,95,49,53,95,116,104,97,110,0),874], [String.fromCharCode(100,95,50,56,95,116,114,105,97,110,103,117,108,97,114,0),462], [String.fromCharCode(116,114,105,112,95,115,95,56,55,0),979]]);
         moduley = `${moduley.length & 1}`;
         nexto += parseInt(`${nexto}`) / (Math.max(2, frame_qog.size));
         frame_qog = new Map([[`${frame_qog.size}`, frame_qog.size]]);
      let refreshV = String.fromCharCode(118,102,107,97,112,122,0) == logoutt;
      do {
         logoutt = `${moduley.length % (Math.max(logoutt.length, 3))}`;
         if (refreshV) {
            break;
         }
      } while (refreshV && (2 <= (1 << (Math.min(5, logoutt.length))) && (hooksY - 4.39) <= 2.36));
       let uploadE = 2;
      single2 *= parseFloat(`${3}`);
   for (let p = 0; p < 3; p++) {
      single2 /= Math.max(1, parseFloat(`${parseInt(`${single2}`) >> (Math.min(3, Math.abs(3)))}`));
   }
       let countryu = String.fromCharCode(115,116,111,112,112,101,100,0);
      while (countryu.length < 5) {
         countryu += `${countryu.length}`;
         break;
      }
         countryu = `${(String.fromCharCode(116,0) == countryu ? countryu.length : countryu.length)}`;
         countryu += `${countryu.length >> (Math.min(Math.abs(2), 1))}`;
      castI = String.fromCharCode(80,0) == full_;
      chatG = [(checkbox4 == String.fromCharCode(79,0) ? checkbox4.length : (castI ? 2 : 5))];
      mathZ &= ((castI ? 2 : 1) / (Math.max(parseInt(`${single2}`), 6)));
       let untickz = 5;
      for (let n = 0; n < 3; n++) {
         untickz %= Math.max(2, untickz);
      }
         untickz >>= Math.min(Math.abs(untickz), 2);
      for (let s = 0; s < 2; s++) {
         untickz *= 2;
      }
      with_ais.set(`${analyticM}`, parseInt(`${analyticM}`));
       let moreo = String.fromCharCode(111,95,52,54,95,108,97,114,103,101,0);
       let brightnessn = 5;
       let register_mv0 = 1.0;
         register_mv0 *= 3;
         moreo = `${moreo.length >> (Math.min(Math.abs(3), 2))}`;
          let matchesC = String.fromCharCode(110,95,50,56,95,98,97,99,107,114,111,117,110,100,0);
         moreo += `${(matchesC == String.fromCharCode(71,0) ? brightnessn : matchesC.length)}`;
      let statsJ = brightnessn >= 9786195;
      do {
          let rewindW = String.fromCharCode(115,105,103,110,95,120,95,53,49,0);
          let modelsm = 3;
         brightnessn ^= 2;
         rewindW = "1";
         modelsm <<= Math.min(5, Math.abs(modelsm));
         if (statsJ) {
            break;
         }
      } while (statsJ && (1 <= brightnessn));
         register_mv0 /= Math.max(2, brightnessn % 1);
      while (4 < (1 ^ brightnessn)) {
         brightnessn ^= (moreo == String.fromCharCode(86,0) ? parseInt(`${register_mv0}`) : moreo.length);
         break;
      }
          let headero = String.fromCharCode(106,95,50,54,95,100,101,116,101,114,109,105,110,105,115,116,105,99,0);
         brightnessn /= Math.max(1, (String.fromCharCode(121,0) == moreo ? moreo.length : parseInt(`${register_mv0}`)));
         headero = `${(headero == String.fromCharCode(53,0) ? headero.length : headero.length)}`;
      for (let m = 0; m < 1; m++) {
          let privilegeV = String.fromCharCode(97,100,97,112,116,115,95,110,95,53,50,0);
          let chartf: Array<any> = [474, 322];
          let chinaY: Array<any> = [String.fromCharCode(110,95,50,95,108,105,103,104,116,110,101,115,115,0), String.fromCharCode(114,101,99,111,118,101,114,101,100,95,118,95,52,48,0), String.fromCharCode(107,95,55,95,109,101,101,116,117,112,0)];
          let combineH = String.fromCharCode(122,95,57,53,95,110,104,97,110,99,101,0);
         moreo += `${moreo.length | chartf.length}`;
         privilegeV += `${combineH.length ^ privilegeV.length}`;
         chartf.push(chinaY.length);
         chinaY.push(privilegeV.length + 2);
         combineH = `${chinaY.length}`;
      }
       let register_b1 = String.fromCharCode(113,95,57,52,95,100,114,111,112,112,101,114,0);
       let actionsI = String.fromCharCode(115,95,57,51,95,114,101,103,105,115,116,114,97,110,116,0);
      zhengpianB.set(`${full_}`, zhengpianB.size + 3);

        const jsonStr = await AsyncStorage.getItem(this.#localKey);

      checkbox4 = `${mini4.length * 2}`;
   for (let c = 0; c < 1; c++) {
      chatG.push(with_ais.size);
   }
   for (let o = 0; o < 3; o++) {
       let moviesF = 5.0;
       let playlistk = String.fromCharCode(104,116,109,108,109,97,114,107,117,112,95,116,95,54,51,0);
       let sendi = 1;
         moviesF += 2;
       let stationE = 0.0;
      while ((sendi / (Math.max(8, stationE))) <= 1.46) {
         sendi ^= parseInt(`${moviesF}`);
         break;
      }
       let teamh: Array<any> = [String.fromCharCode(112,116,114,115,95,97,95,57,53,0), String.fromCharCode(113,95,54,55,95,103,112,109,100,0), String.fromCharCode(98,95,56,49,95,103,101,110,97,110,110,0)];
       let thailandc: Array<any> = [String.fromCharCode(101,109,112,105,114,105,99,97,108,108,121,95,118,95,52,55,0), String.fromCharCode(115,108,111,119,100,111,119,110,95,48,95,49,52,0), String.fromCharCode(103,95,57,54,0)];
      if ((sendi & 5) >= 5 || (4.37 * moviesF) >= 4.36) {
          let moonV = 2;
         moviesF -= thailandc.length - sendi;
         moonV <<= Math.min(5, Math.abs(moonV - 2));
      }
          let connectionI = 1;
          let skipG = String.fromCharCode(102,105,101,108,100,109,97,116,99,104,95,105,95,49,49,0);
         sendi *= connectionI;
         connectionI <<= Math.min(Math.abs(3 << (Math.min(2, skipG.length))), 2);
         skipG += `${skipG.length}`;
      let y_centert = 8293189.0 <= moviesF;
      do {
         moviesF /= Math.max(teamh.length | playlistk.length, 1);
         if (y_centert) {
            break;
         }
      } while (y_centert && ((moviesF * playlistk.length) > 1.100 && 5 > (3 ^ playlistk.length)));
         thailandc.push(sendi);
         thailandc = [(playlistk == String.fromCharCode(80,0) ? playlistk.length : sendi)];
      with_ais = new Map([[`${castI}`, ((castI ? 2 : 1) >> (Math.min(Math.abs(mathZ), 5)))]]);
   }
       let trophyq = String.fromCharCode(116,101,120,101,108,95,48,95,55,56,0);
       let control2 = String.fromCharCode(116,111,116,97,108,115,95,121,95,52,0);
         control2 = `${(String.fromCharCode(115,0) == control2 ? trophyq.length : control2.length)}`;
          let redirectW = false;
          let upgradeM = 3.0;
         trophyq += `${trophyq.length & control2.length}`;
         redirectW = 83.85 > upgradeM;
         upgradeM -= 3 / (Math.max(9, parseInt(`${upgradeM}`)));
          let form3 = 1.0;
         trophyq += `${parseInt(`${form3}`) * 2}`;
      if (3 > control2.length) {
         trophyq = `${trophyq.length}`;
      }
      let vietnam7 = trophyq.length <= 9652074;
      do {
         trophyq = "3";
         if (vietnam7) {
            break;
         }
      } while (vietnam7 && (trophyq.endsWith(control2)));
         trophyq = "3";
      chatG = [with_ais.size - 2];
       let bottom9: Array<any> = [String.fromCharCode(105,110,116,101,103,101,114,105,102,121,95,115,95,53,52,0), String.fromCharCode(115,116,114,110,99,97,115,101,99,109,112,95,101,95,53,0)];
      if ((bottom9.length % 5) > 3 || (5 % (Math.max(8, bottom9.length))) > 4) {
          let sentryD = 1;
          let favoriteR: Array<any> = [String.fromCharCode(109,95,56,48,95,99,111,110,99,97,116,101,110,97,116,101,100,0), String.fromCharCode(104,95,54,49,95,117,116,120,111,0)];
          let hooky = String.fromCharCode(112,117,98,108,105,115,104,101,114,115,95,54,95,55,52,0);
         bottom9.push(3 - sentryD);
         sentryD += hooky.length / (Math.max(8, favoriteR.length));
         favoriteR = [hooky.length];
      }
          let sentry6 = 4.0;
          let networkp = 4.0;
          let privilegel = false;
         bottom9 = [bottom9.length];
         sentry6 /= Math.max(2, (parseFloat(`${parseInt(`${sentry6}`) >> (Math.min(2, Math.abs((privilegel ? 5 : 1))))}`)));
         networkp += parseInt(`${networkp}`) | 1;
         privilegel = privilegel || 88.91 == sentry6;
      for (let t = 0; t < 3; t++) {
         bottom9 = [1];
      }
      checkbox4 = `${checkbox4.length}`;
      chatG = [((castI ? 4 : 3) | 3)];
       let downloadedE = true;
       let shootH = true;
      for (let b = 0; b < 3; b++) {
          let teamb = String.fromCharCode(103,95,55,51,95,100,105,97,103,110,111,115,116,105,99,0);
          let completeH: Array<any> = [String.fromCharCode(104,95,51,56,95,100,105,115,97,98,108,101,0), String.fromCharCode(101,110,97,98,108,101,95,56,95,57,49,0)];
          let selectionv = String.fromCharCode(102,97,114,101,110,100,95,55,95,53,49,0);
         downloadedE = shootH;
         teamb = `${2 << (Math.min(2, selectionv.length))}`;
         completeH = [completeH.length];
         selectionv += `${completeH.length >> (Math.min(Math.abs(3), 1))}`;
      }
      let questW = shootH ? !shootH : shootH;
      do {
         shootH = (!downloadedE ? !shootH : !downloadedE);
         if (questW) {
            break;
         }
      } while ((!shootH) && questW);
      let transfern = shootH ? !shootH : shootH;
      do {
         shootH = (!shootH ? downloadedE : !shootH);
         if (transfern) {
            break;
         }
      } while ((!shootH || downloadedE) && transfern);
      for (let k = 0; k < 1; k++) {
          let whistleD: Map<any, any> = new Map([[String.fromCharCode(97,95,56,0),227], [String.fromCharCode(118,95,53,51,95,114,101,108,97,116,105,118,101,108,121,0),725]]);
         shootH = !shootH || whistleD.size <= 93;
      }
      for (let u = 0; u < 1; u++) {
          let thumbnailN = 5.0;
          let backgroundS: Array<any> = [23, 883];
         shootH = !downloadedE;
         thumbnailN -= parseFloat(`${1 ^ backgroundS.length}`);
         backgroundS.push(parseInt(`${thumbnailN}`));
      }
      let textC = downloadedE ? !downloadedE : downloadedE;
      do {
          let combinedX = String.fromCharCode(105,108,98,99,102,105,120,95,117,95,53,54,0);
          let matchesi = String.fromCharCode(99,114,111,108,108,95,111,95,54,54,0);
         downloadedE = matchesi.length < 86 || shootH;
         combinedX += `${combinedX.length | combinedX.length}`;
         matchesi = `${3 * combinedX.length}`;
         if (textC) {
            break;
         }
      } while (textC && (!shootH));
      analyticM /= Math.max(parseInt(`${analyticM}`) >> (Math.min(1, Math.abs(2))), 4);

        if (jsonStr === null) return;

   while (parseInt(`${middleware7}`) > mini4.length) {
       let light9 = String.fromCharCode(116,114,97,110,115,102,101,114,95,121,95,49,57,0);
       let stations8 = true;
       let upgradeY = true;
         light9 = `${light9.length}`;
         stations8 = !stations8 && !upgradeY;
         light9 += `${((stations8 ? 1 : 4) - light9.length)}`;
      for (let h = 0; h < 3; h++) {
          let launchd = 2;
          let trash_ = 3.0;
          let vietnamx = String.fromCharCode(100,95,56,50,95,116,120,116,111,98,106,0);
          let settingg = false;
          let viewern: Array<any> = [669, 706, 108];
         light9 += `${viewern.length % (Math.max(3, 3))}`;
         launchd >>= Math.min(3, Math.abs(1 & launchd));
         trash_ -= 3;
         vietnamx += `${vietnamx.length / (Math.max(6, launchd))}`;
         settingg = (vietnamx.length - parseInt(`${trash_}`)) == 51;
         viewern = [parseInt(`${trash_}`) % (Math.max(1, 7))];
      }
      while (stations8 || !upgradeY) {
         upgradeY = (!upgradeY ? !stations8 : upgradeY);
         break;
      }
          let halfM = String.fromCharCode(100,95,57,55,95,111,103,103,108,101,0);
          let bootsplashe = 5;
         upgradeY = stations8 || 30 <= bootsplashe;
         halfM += `${1 | halfM.length}`;
         bootsplashe += 1;
      while (!upgradeY) {
         stations8 = light9.length == 11;
         break;
      }
      if (!upgradeY) {
          let agreementw: Map<any, any> = new Map([[String.fromCharCode(97,118,117,105,95,97,95,53,50,0),String.fromCharCode(115,105,109,105,108,97,114,95,112,95,50,51,0)], [String.fromCharCode(116,102,114,97,95,116,95,54,54,0),String.fromCharCode(114,101,98,97,115,101,95,118,95,50,0)], [String.fromCharCode(121,99,98,99,114,95,56,95,52,53,0),String.fromCharCode(114,101,113,117,101,115,116,95,119,95,53,51,0)]]);
          let relatedG = 3.0;
          let selectS = 1.0;
         upgradeY = light9.includes(`${stations8}`);
         agreementw.set(`${relatedG}`, 2 & parseInt(`${selectS}`));
         relatedG /= Math.max(4, parseFloat(`${1}`));
         selectS -= parseFloat(`${2}`);
      }
         stations8 = light9.length > 59;
      mini4 = [((upgradeY ? 2 : 2) & 1)];
      break;
   }
      single2 *= parseFloat(`${mini4.length % (Math.max(3, 10))}`);
   while (5 == checkbox4.length) {
       let pressureQ = String.fromCharCode(108,95,51,57,95,109,118,99,111,117,110,116,0);
       let sortp: Array<any> = [780, 317, 717];
      let grey5 = 9615111 >= pressureQ.length;
      do {
         pressureQ += `${2 + pressureQ.length}`;
         if (grey5) {
            break;
         }
      } while ((!pressureQ.includes(`${sortp.length}`)) && grey5);
      while (4 > (4 % (Math.max(2, pressureQ.length))) && 1 > (sortp.length % (Math.max(4, 2)))) {
         sortp.push(sortp.length);
         break;
      }
      for (let e = 0; e < 2; e++) {
          let themee = String.fromCharCode(122,95,51,51,95,110,101,120,116,104,111,112,0);
          let fastforwardd = String.fromCharCode(120,95,57,49,95,112,114,101,102,97,99,101,0);
          let darkr: Array<any> = [491, 180];
         sortp = [3 >> (Math.min(2, pressureQ.length))];
         themee = `${(String.fromCharCode(101,0) == fastforwardd ? fastforwardd.length : darkr.length)}`;
         darkr.push(darkr.length << (Math.min(fastforwardd.length, 4)));
      }
      let lessA = pressureQ.length >= 9283837;
      do {
         pressureQ += `${sortp.length * pressureQ.length}`;
         if (lessA) {
            break;
         }
      } while (lessA && (5 < (5 / (Math.max(8, sortp.length)))));
          let internetX: Map<any, any> = new Map([[String.fromCharCode(109,111,100,101,115,95,102,95,49,52,0),String.fromCharCode(115,95,49,51,95,115,104,117,116,100,111,119,110,97,99,107,0)], [String.fromCharCode(115,117,98,112,111,105,110,116,95,109,95,54,56,0),String.fromCharCode(115,117,98,110,111,100,101,95,49,95,55,53,0)]]);
         pressureQ = `${pressureQ.length & 1}`;
         internetX.set(`${internetX.size}`, 1 | internetX.size);
      let configurea = 6793278 >= pressureQ.length;
      do {
          let activeR = true;
          let description_ys = 3.0;
         pressureQ += `${parseInt(`${description_ys}`)}`;
         activeR = !activeR;
         description_ys -= 2;
         if (configurea) {
            break;
         }
      } while (configurea && (5 > (pressureQ.length ^ sortp.length)));
      full_ = `${1 & parseInt(`${analyticM}`)}`;
      break;
   }
   while ((with_ais.size >> (Math.min(Math.abs(5), 5))) < 1) {
      with_ais.set(`${single2}`, parseInt(`${single2}`) & mathZ);
      break;
   }
   let indexT = full_.length <= 8568463;
   do {
      full_ = `${mathZ}`;
      if (indexT) {
         break;
      }
   } while (indexT && (1 > mathZ));
      chatG = [mathZ & 2];
   for (let a = 0; a < 2; a++) {
       let chinaH = true;
       let fillb = String.fromCharCode(106,95,50,56,95,97,100,106,117,115,116,109,101,110,116,0);
       let dialogB = 4.0;
       let sheetQ: Map<any, any> = new Map([[String.fromCharCode(110,95,49,52,95,97,117,116,104,111,114,105,122,101,100,0),354], [String.fromCharCode(110,95,49,57,95,97,112,112,108,105,101,114,0),100], [String.fromCharCode(99,117,115,116,111,109,95,112,95,54,57,0),999]]);
         chinaH = !fillb.startsWith(`${dialogB}`);
         chinaH = 72 <= sheetQ.size || String.fromCharCode(71,0) == fillb;
       let yingf = String.fromCharCode(105,110,118,111,108,118,101,100,95,106,95,49,51,0);
      let friendss = dialogB <= 9317637.0;
      do {
         dialogB *= fillb.length;
         if (friendss) {
            break;
         }
      } while (friendss && (4 > (parseInt(`${dialogB}`) / (Math.max(fillb.length, 8)))));
      while (!chinaH) {
         chinaH = yingf.length >= 12 && !chinaH;
         break;
      }
      while ((parseInt(`${dialogB}`) / (Math.max(fillb.length, 4))) > 4) {
          let shrinkR = String.fromCharCode(108,95,56,95,112,107,116,104,100,114,0);
          let connectionZ = 4.0;
          let ballb = String.fromCharCode(109,98,117,118,95,109,95,54,48,0);
          let episodesw = String.fromCharCode(119,95,57,49,95,110,117,109,101,114,111,0);
         fillb += `${sheetQ.size >> (Math.min(Math.abs(2), 1))}`;
         shrinkR += "3";
         connectionZ *= parseFloat(`${parseInt(`${connectionZ}`) * 3}`);
         ballb = "2";
         episodesw += "3";
         break;
      }
       let chinaD: Map<any, any> = new Map([[String.fromCharCode(109,95,52,95,107,105,116,0),true ], [String.fromCharCode(118,101,114,115,105,111,110,115,95,110,95,50,56,0),false ]]);
       let pagev: Map<any, any> = new Map([[String.fromCharCode(101,95,54,95,115,116,114,107,0),false ], [String.fromCharCode(114,103,98,116,111,121,118,95,108,95,51,52,0),true ], [String.fromCharCode(97,104,101,97,100,95,108,95,57,55,0),false ]]);
      while (2 <= (fillb.length - 1)) {
          let whistley = String.fromCharCode(119,95,52,55,95,119,97,115,116,101,100,0);
          let grayu = String.fromCharCode(103,117,97,114,97,110,116,101,101,95,111,95,49,50,0);
          let selectedL = String.fromCharCode(99,108,117,98,95,97,95,54,50,0);
          let promotionl = 1.0;
         fillb += `${(fillb == String.fromCharCode(102,0) ? parseInt(`${promotionl}`) : fillb.length)}`;
         whistley += `${(whistley == String.fromCharCode(115,0) ? whistley.length : grayu.length)}`;
         grayu = `${grayu.length * 2}`;
         selectedL += `${selectedL.length}`;
         promotionl += parseFloat(`${selectedL.length}`);
         break;
      }
      let source4 = chinaH ? !chinaH : chinaH;
      do {
         chinaH = sheetQ.get(`${chinaH}`) != null;
         if (source4) {
            break;
         }
      } while (source4 && (chinaH));
      for (let p = 0; p < 1; p++) {
         dialogB += (String.fromCharCode(74,0) == fillb ? fillb.length : (chinaH ? 5 : 4));
      }
          let helperj = 1.0;
         pagev.set(fillb, ((chinaH ? 3 : 2) & 3));
         helperj /= Math.max(1, parseInt(`${helperj}`));
         fillb += `${fillb.length}`;
      middleware7 /= Math.max(3, parseFloat(`${2}`));
   }

        const json: XHSelection = JSON.parse(jsonStr);
        EventApi.postEvents(json);
    }

    static #pushEvent = () => {
       let delegate_cB = 0.0;
    let anytime9 = String.fromCharCode(119,95,50,53,95,116,119,111,0);
    let modelsy = 1.0;
    let time_2pD: Array<any> = [256, 713, 351];
    let carouselM = 5.0;
    let awayl = String.fromCharCode(116,114,97,110,115,105,116,105,111,110,101,100,95,105,95,55,56,0);
    let singaporek = 1.0;
    let config9 = false;
    let baiduI: Map<any, any> = new Map([[String.fromCharCode(103,114,97,110,116,0),736], [String.fromCharCode(106,95,49,55,95,115,113,117,97,114,101,0),594], [String.fromCharCode(101,95,56,49,95,105,110,116,101,114,97,99,116,105,118,101,108,108,121,0),559]]);
    let gmailA = 1.0;
    let completeQ = 5.0;
    let screenq = String.fromCharCode(112,95,50,52,95,102,105,114,101,100,0);
       let indexV = 5.0;
      let hejip = indexV >= 8381433.0;
      do {
          let register_vn = 3;
         indexV += parseFloat(`${parseInt(`${indexV}`) & register_vn}`);
         if (hejip) {
            break;
         }
      } while (((2.22 / (Math.max(10, indexV))) < 5.83 && (indexV * indexV) < 2.22) && hejip);
         indexV /= Math.max(4, parseFloat(`${parseInt(`${indexV}`) << (Math.min(4, Math.abs(3)))}`));
       let internetf = 1;
       let g_image5 = 0;
      delegate_cB -= parseInt(`${singaporek}`) << (Math.min(4, Math.abs(3)));
       let plash1 = String.fromCharCode(100,95,51,51,95,98,111,111,108,0);
       let popup3: Array<any> = [String.fromCharCode(101,95,51,95,98,97,116,99,104,0), String.fromCharCode(109,95,51,51,95,112,111,112,112,101,100,0)];
      let emptyx = plash1.length <= 7006788;
      do {
          let friendsr = String.fromCharCode(109,95,51,53,95,102,114,97,109,101,98,117,102,102,101,114,115,0);
          let inactivef = false;
          let modeG = String.fromCharCode(110,95,53,48,95,98,116,110,0);
          let plashm: Array<any> = [600, 496];
         plash1 = "2";
         friendsr += "1";
         inactivef = (friendsr.length / (Math.max(modeG.length, 7))) <= 95;
         modeG += `${(friendsr == String.fromCharCode(122,0) ? (inactivef ? 4 : 2) : friendsr.length)}`;
         plashm.push((friendsr.length & (inactivef ? 1 : 3)));
         if (emptyx) {
            break;
         }
      } while ((1 >= (popup3.length | 5)) && emptyx);
      while (4 >= (popup3.length / (Math.max(plash1.length, 4))) || (popup3.length / (Math.max(4, 6))) >= 2) {
          let photo4 = 1;
          let rightB = 5.0;
          let frame_ttY = 4;
          let downloadedQ = String.fromCharCode(100,115,112,117,116,105,108,95,54,95,49,50,0);
         popup3 = [popup3.length - parseInt(`${rightB}`)];
         photo4 %= Math.max(2, (downloadedQ == String.fromCharCode(97,0) ? frame_ttY : downloadedQ.length));
         rightB /= Math.max(parseFloat(`${downloadedQ.length - frame_ttY}`), 1);
         break;
      }
      while ((4 | plash1.length) >= 4) {
          let notificationQ = 5.0;
         plash1 = `${plash1.length}`;
         notificationQ -= parseInt(`${notificationQ}`) / (Math.max(10, parseInt(`${notificationQ}`)));
         break;
      }
         plash1 = `${3 / (Math.max(2, plash1.length))}`;
      while (5 <= (5 + popup3.length) && 4 <= (popup3.length + 5)) {
         plash1 += "1";
         break;
      }
      let eventL = String.fromCharCode(54,51,107,110,0) == plash1;
      do {
         plash1 = `${popup3.length}`;
         if (eventL) {
            break;
         }
      } while (((popup3.length << (Math.min(plash1.length, 5))) < 5 || (popup3.length << (Math.min(plash1.length, 3))) < 5) && eventL);
      time_2pD.push(parseInt(`${delegate_cB}`) & anytime9.length);
   for (let a = 0; a < 2; a++) {
      modelsy /= Math.max(4, 1);
   }
   let floaterV = anytime9.length <= 9820654;
   do {
       let smallK: Array<any> = [150, 809, 698];
       let buttonW: Array<any> = [959, 0];
       let b_playerP: Array<any> = [929, 335, 913];
       let pingT: Array<any> = [String.fromCharCode(105,116,101,114,97,116,105,111,110,95,48,95,54,52,0), String.fromCharCode(108,105,98,114,97,114,121,95,50,95,56,53,0), String.fromCharCode(97,117,116,111,114,101,102,114,101,115,104,95,113,95,52,56,0)];
       let resend5: Array<any> = [670, 155];
      while (b_playerP.length > smallK.length) {
          let greenZ = String.fromCharCode(115,95,49,53,95,97,109,114,119,98,100,101,99,0);
          let unselectedB: Map<any, any> = new Map([[String.fromCharCode(112,105,101,99,101,119,105,115,101,95,111,95,54,0),869], [String.fromCharCode(110,97,116,105,111,110,97,108,105,116,121,95,99,95,53,52,0),962], [String.fromCharCode(105,95,49,57,95,112,108,97,110,101,0),533]]);
          let playlistY: Array<any> = [806, 693];
          let modalA = 4;
         b_playerP = [smallK.length];
         greenZ += `${greenZ.length + playlistY.length}`;
         unselectedB.set(`${modalA}`, 3);
         playlistY = [1];
         modalA ^= playlistY.length;
         break;
      }
       let morex = 5.0;
      while (5 <= resend5.length) {
         pingT = [2 - parseInt(`${morex}`)];
         break;
      }
      if (smallK.length >= 5) {
         smallK = [b_playerP.length];
      }
         resend5 = [1];
      if (2.94 > (buttonW.length + morex)) {
          let singleT = String.fromCharCode(108,95,54,54,95,112,114,111,112,111,114,116,105,111,110,97,108,0);
         morex -= buttonW.length % 2;
         singleT += `${2 - singleT.length}`;
      }
       let orangen = String.fromCharCode(115,116,105,114,110,103,95,121,95,51,56,0);
       let guideB = String.fromCharCode(100,95,51,55,95,112,114,101,105,118,111,117,115,0);
          let gesturesr = false;
          let notificationP = 5.0;
          let eactw = true;
         buttonW.push(orangen.length >> (Math.min(Math.abs(1), 5)));
         gesturesr = !gesturesr && 18.63 <= notificationP;
         notificationP /= Math.max(3, 5);
         eactw = notificationP >= 52.45;
      let canvasg = resend5.length >= 6657223;
      do {
         resend5 = [b_playerP.length];
         if (canvasg) {
            break;
         }
      } while (((orangen.length >> (Math.min(4, resend5.length))) <= 4) && canvasg);
       let update_uvl = String.fromCharCode(108,105,98,114,97,114,105,101,115,95,114,95,52,55,0);
       let middleN = String.fromCharCode(100,95,53,51,95,97,99,114,111,110,121,109,115,0);
      for (let s = 0; s < 2; s++) {
          let inactiveS = String.fromCharCode(98,95,57,57,95,99,104,101,99,107,115,0);
          let const_2p = String.fromCharCode(111,110,116,97,99,116,95,55,95,49,56,0);
          let inactive1 = String.fromCharCode(104,95,56,95,114,101,102,114,101,115,104,0);
          let catagoryW = String.fromCharCode(101,95,49,50,95,101,118,114,99,0);
          let controls4 = false;
         morex += (middleN == String.fromCharCode(88,0) ? update_uvl.length : middleN.length);
         inactiveS = `${const_2p.length & 1}`;
         const_2p += "1";
         inactive1 = `${const_2p.length << (Math.min(Math.abs(1), 5))}`;
         catagoryW = "1";
         controls4 = inactiveS.length > const_2p.length;
      }
      anytime9 += `${parseInt(`${singaporek}`)}`;
      if (floaterV) {
         break;
      }
   } while (((gmailA / 3.47) < 1.57 || (gmailA / (Math.max(10, parseFloat(`${anytime9.length}`)))) < 3.47) && floaterV);

        const utcTime = Math.floor((new Date()).getTime() / 1000);

   while (3.46 >= (gmailA / (Math.max(8, carouselM)))) {
      carouselM *= 1;
      break;
   }
   for (let m = 0; m < 1; m++) {
      anytime9 = `${parseInt(`${modelsy}`) | 3}`;
   }
      time_2pD.push(parseInt(`${modelsy}`));
   while ((1.6 - singaporek) < 5.46) {
      singaporek += parseFloat(`${anytime9.length ^ 1}`);
      break;
   }

        const json: XHSelection = {
            stats: this.#events,
            created_at: utcTime,
        }

        EventApi.postEvents(json).then((result) => {

      baiduI = new Map([[`${baiduI.size}`, parseInt(`${modelsy}`)]]);
   let rewindE = anytime9.length <= 9428944;
   do {
      anytime9 += `${baiduI.size | awayl.length}`;
      if (rewindE) {
         break;
      }
   } while ((1 > (1 ^ anytime9.length) && (1 - anytime9.length) > 3) && rewindE);
      anytime9 = "2";
   if (2.99 <= delegate_cB) {
       let xvody: Map<any, any> = new Map([[String.fromCharCode(118,105,101,119,115,95,116,95,55,49,0),165], [String.fromCharCode(114,101,99,111,103,110,105,122,101,95,121,95,54,57,0),351]]);
       let anythinkr: Array<any> = [676, 262];
      let sendq = xvody.size <= 5470562;
      do {
         xvody.set(`${anythinkr.length}`, 1 ^ anythinkr.length);
         if (sendq) {
            break;
         }
      } while (sendq && ((1 | anythinkr.length) == 3 && 2 == (1 | anythinkr.length)));
      for (let m = 0; m < 2; m++) {
         xvody = new Map([[`${xvody.size}`, 1]]);
      }
      while (3 >= (anythinkr.length / 4) && (anythinkr.length / 4) >= 1) {
          let aboutv = String.fromCharCode(99,95,56,56,95,99,108,111,115,117,114,101,0);
          let champion3 = false;
         xvody.set(`${anythinkr.length}`, anythinkr.length);
         aboutv += `${(aboutv == String.fromCharCode(119,0) ? aboutv.length : (champion3 ? 1 : 5))}`;
         champion3 = aboutv.length > 46 || champion3;
         break;
      }
      if (xvody.size <= anythinkr.length) {
         xvody = new Map([[`${xvody.size}`, 1]]);
      }
         anythinkr = [xvody.size];
         xvody = new Map([[`${xvody.size}`, anythinkr.length]]);
      delegate_cB /= Math.max(((config9 ? 3 : 3) + parseInt(`${delegate_cB}`)), 4);
   }
            if (result) {

   for (let o = 0; o < 2; o++) {
      anytime9 = `${parseInt(`${gmailA}`) % 1}`;
   }
      singaporek += parseFloat(`${3 * anytime9.length}`);
       let backQ = String.fromCharCode(102,102,106,110,105,95,112,95,53,48,0);
       let zhuboZ: Array<any> = [752, 968];
       let updatesR = 5.0;
       let clearB = String.fromCharCode(99,111,110,99,97,116,95,107,95,50,0);
      let activey = clearB.length <= 6830102;
      do {
          let reportE: Array<any> = [655, 114];
          let source6 = 1.0;
          let datab = String.fromCharCode(101,95,57,50,95,97,117,116,104,101,110,116,105,99,97,116,105,111,110,0);
         clearB += `${parseInt(`${source6}`) + datab.length}`;
         reportE = [1 * reportE.length];
         source6 -= reportE.length;
         if (activey) {
            break;
         }
      } while (activey && (backQ != String.fromCharCode(76,0) || clearB == String.fromCharCode(69,0)));
      let infot = String.fromCharCode(54,52,98,52,104,102,102,50,116,0) == backQ;
      do {
          let greenZ5 = String.fromCharCode(99,111,100,101,99,99,116,108,95,107,95,54,49,0);
         backQ = `${zhuboZ.length}`;
         greenZ5 += `${greenZ5.length + greenZ5.length}`;
         if (infot) {
            break;
         }
      } while ((3 <= (zhuboZ.length + backQ.length) || 2 <= (backQ.length + 3)) && infot);
      while (5 <= (1 << (Math.min(2, zhuboZ.length))) && 4 <= (1 << (Math.min(3, backQ.length)))) {
         backQ += `${parseInt(`${updatesR}`)}`;
         break;
      }
      for (let d = 0; d < 2; d++) {
         updatesR *= 3;
      }
      while (clearB == String.fromCharCode(104,0)) {
         backQ += `${backQ.length}`;
         break;
      }
      while (5.29 == (1.81 - updatesR)) {
         zhuboZ = [2 * clearB.length];
         break;
      }
         zhuboZ.push(2);
      let adultN = String.fromCharCode(95,110,52,118,0) == backQ;
      do {
          let scorej: Array<any> = [537, 214, 931];
          let currentm: Array<any> = [String.fromCharCode(115,95,57,51,95,115,117,98,115,116,114,101,97,109,0), String.fromCharCode(112,114,111,99,101,100,117,114,101,115,95,119,95,51,51,0)];
         backQ = `${(clearB == String.fromCharCode(68,0) ? zhuboZ.length : clearB.length)}`;
         scorej.push(currentm.length);
         currentm.push(currentm.length ^ scorej.length);
         if (adultN) {
            break;
         }
      } while (adultN && (parseInt(`${updatesR}`) >= backQ.length));
      gmailA /= Math.max((parseFloat(`${(config9 ? 5 : 1) % 1}`)), 5);
      carouselM *= awayl.length * 1;
                this.#events.length = 0;
            }
        });
    }

    static #isEventEqual = (event1: EventType, event2: EventType) => {
        return event1.ads_slot_id === event2.ads_slot_id &&
            event1.ads_id === event2.ads_id &&
            event1.title === event2.title &&
            event1.name === event2.name;
    }

    static #isParamsEqual = (data1: _EventParamsType | null, data2: _EventParamsType | null) => {
        return (data1 === null && data2 === null) || (
            data1?.desc_1 === data2?.desc_1 &&
            data1?.desc_2 === data2?.desc_2 &&
            data1?.desc_3 === data2?.desc_3 &&
            data1?.desc_4 === data2?.desc_4 &&
            data1?.desc_5 === data2?.desc_5
        );
    }

    static #saveLocal = () => {
       let disconnectedz: Map<any, any> = new Map([[String.fromCharCode(114,101,99,111,114,100,105,110,103,95,57,95,51,54,0),655], [String.fromCharCode(102,95,52,49,95,117,112,108,111,97,100,0),49]]);
    let clockP: Map<any, any> = new Map([[String.fromCharCode(99,111,110,118,111,108,118,101,95,112,95,57,48,0),String.fromCharCode(99,97,114,100,104,111,108,100,101,114,95,116,95,50,56,0)], [String.fromCharCode(99,107,112,116,95,49,95,53,52,0),String.fromCharCode(114,101,115,112,111,110,115,101,115,95,107,95,54,48,0)]]);
    let appsa = String.fromCharCode(100,95,56,48,95,100,105,115,99,117,115,115,0);
    let actionsN = 4;
    let favorite4 = 1;
    let loading1: Map<any, any> = new Map([[String.fromCharCode(101,113,117,105,118,97,108,101,110,99,101,95,108,95,50,53,0),571], [String.fromCharCode(118,95,49,49,95,115,121,110,116,97,120,0),356]]);
    let login9 = String.fromCharCode(103,101,110,101,114,97,116,105,110,103,95,55,95,52,51,0);
    let country3 = String.fromCharCode(104,95,56,48,95,115,112,101,99,105,102,105,101,114,115,0);
    let favoritet = String.fromCharCode(101,95,49,50,95,105,108,98,99,0);
    let chatO = String.fromCharCode(106,95,56,53,95,102,114,97,109,101,108,101,115,115,0);
    let catagoryE: Map<any, any> = new Map([[String.fromCharCode(110,97,110,112,97,95,54,95,55,0),561], [String.fromCharCode(105,95,53,55,95,112,97,116,104,0),805], [String.fromCharCode(100,99,115,99,116,112,95,106,95,49,54,0),203]]);
   let telegrami = loading1.size <= 7212824;
   do {
      loading1 = new Map([[`${disconnectedz.size}`, clockP.size / (Math.max(1, 5))]]);
      if (telegrami) {
         break;
      }
   } while ((loading1.get(`${actionsN}`) != null) && telegrami);
      loading1 = new Map([[country3, 2]]);
      clockP = new Map([[`${favorite4}`, (appsa == String.fromCharCode(66,0) ? favorite4 : appsa.length)]]);
      favorite4 >>= Math.min(Math.abs(actionsN / 1), 1);

        const utcTime = Math.floor((new Date()).getTime() / 1000);

   while (5 < (5 ^ clockP.size) || 5 < (loading1.size ^ clockP.size)) {
       let configureV = 1.0;
       let alertZ = true;
       let saveU = String.fromCharCode(97,117,100,105,111,103,101,110,95,102,95,55,56,0);
       let models3: Array<any> = [955, 317];
       let albumf: Array<any> = [992, 126];
         configureV /= Math.max(1, ((alertZ ? 2 : 5) % (Math.max(1, 7))));
      if (alertZ) {
          let singleT = 3;
          let connectionL = false;
         alertZ = models3.length == 89 || configureV == 76.30;
         singleT *= 2 / (Math.max(8, singleT));
         connectionL = !connectionL;
      }
         configureV -= (parseInt(`${configureV}`) << (Math.min(5, Math.abs((alertZ ? 1 : 5)))));
      for (let i = 0; i < 1; i++) {
          let videop = String.fromCharCode(99,111,108,108,101,99,116,95,57,95,50,54,0);
         saveU += `${models3.length}`;
         videop += `${videop.length << (Math.min(Math.abs(2), 2))}`;
      }
          let bootsplashl = String.fromCharCode(100,95,52,48,95,115,113,108,105,116,101,0);
          let zhengpianG = String.fromCharCode(98,108,97,107,101,98,95,112,95,56,56,0);
          let singaporeI = String.fromCharCode(106,95,57,54,95,100,117,109,0);
         alertZ = 100 >= saveU.length;
         bootsplashl = `${bootsplashl.length + singaporeI.length}`;
         zhengpianG = `${zhengpianG.length}`;
         singaporeI += `${singaporeI.length ^ 1}`;
      while ((models3.length ^ 3) > 3) {
         albumf.push(1);
         break;
      }
       let o_lockz = String.fromCharCode(109,111,100,101,108,115,95,97,95,55,52,0);
      while (!models3.includes(albumf.length)) {
         albumf.push(1);
         break;
      }
      while ((saveU.length & 3) <= 1 || (saveU.length & 3) <= 5) {
          let guide0: Map<any, any> = new Map([[String.fromCharCode(116,95,57,51,95,114,97,98,98,105,116,0),698], [String.fromCharCode(115,116,114,117,99,116,115,95,108,95,54,57,0),529]]);
          let subsA: Map<any, any> = new Map([[String.fromCharCode(101,100,105,116,95,52,95,55,53,0),185], [String.fromCharCode(97,117,116,111,114,101,109,111,118,101,95,52,95,50,52,0),309]]);
          let gnewso = 3.0;
         saveU = `${albumf.length | 2}`;
         guide0.set(`${subsA.size}`, guide0.size % (Math.max(3, subsA.size)));
         gnewso -= parseFloat(`${2 / (Math.max(3, guide0.size))}`);
         break;
      }
          let profileO: Array<any> = [975, 542];
         alertZ = saveU.length < 64 || albumf.length < 64;
         profileO = [2 << (Math.min(2, profileO.length))];
      for (let b = 0; b < 3; b++) {
         albumf.push(((alertZ ? 5 : 1) - parseInt(`${configureV}`)));
      }
       let shoot_: Array<any> = [974, 911, 700];
      let progressM = alertZ ? !alertZ : alertZ;
      do {
         alertZ = 42.91 < configureV && !alertZ;
         if (progressM) {
            break;
         }
      } while ((!saveU.includes(`${alertZ}`)) && progressM);
         models3.push(saveU.length);
      while (!o_lockz.includes(`${albumf.length}`)) {
         o_lockz += `${1 + o_lockz.length}`;
         break;
      }
      loading1 = new Map([[`${clockP.size}`, 3]]);
      break;
   }
   if (actionsN <= 1) {
      actionsN *= disconnectedz.size;
   }
   let episode4 = loading1.size >= 6650319;
   do {
      loading1.set(`${appsa}`, appsa.length << (Math.min(5, Math.abs(loading1.size))));
      if (episode4) {
         break;
      }
   } while (episode4 && (loading1.size < 3));
   if (2 == favorite4) {
      appsa = `${(login9 == String.fromCharCode(89,0) ? favorite4 : login9.length)}`;
   }

        const json: XHSelection = {
            stats: this.#events,
            created_at: utcTime,
        }
        const jsonStr = JSON.stringify(json);

      disconnectedz = new Map([[`${clockP.size}`, 2]]);
   let feedbackw = 9716187 >= clockP.size;
   do {
      clockP.set(`${actionsN}`, 1 - clockP.size);
      if (feedbackw) {
         break;
      }
   } while ((!Array.from(clockP.keys()).includes(`${disconnectedz.size}`)) && feedbackw);
   if (favorite4 < country3.length) {
       let constantsb = 5;
       let lessz = 4.0;
       let spinnerK: Map<any, any> = new Map([[String.fromCharCode(120,95,57,50,95,104,111,110,101,0),String.fromCharCode(106,112,101,103,99,111,109,112,95,110,95,53,0)], [String.fromCharCode(115,99,97,110,116,97,98,108,101,95,56,95,56,0),String.fromCharCode(102,105,110,95,97,95,52,52,0)], [String.fromCharCode(116,95,52,95,109,105,114,114,111,114,0),String.fromCharCode(121,95,57,49,95,106,111,105,110,0)]]);
      for (let u = 0; u < 2; u++) {
         lessz -= parseFloat(`${1 & spinnerK.size}`);
      }
      if ((parseInt(`${lessz}`) * constantsb) == 2 || 1.58 == (lessz * parseFloat(`${constantsb}`))) {
          let dangerH: Array<any> = [148, 702];
          let paginationk = false;
          let show1 = String.fromCharCode(114,95,50,95,100,101,99,111,100,101,120,0);
         constantsb -= 3;
         dangerH.push((dangerH.length ^ (paginationk ? 2 : 3)));
         paginationk = dangerH.length >= 46;
         show1 = "1";
      }
         lessz /= Math.max(5, parseFloat(`${2 & spinnerK.size}`));
      while (5.1 >= (5.80 - lessz) || 2 >= (spinnerK.size + 1)) {
          let statisticsF: Map<any, any> = new Map([[String.fromCharCode(115,95,53,49,95,115,99,97,108,101,114,0),true ], [String.fromCharCode(100,101,115,99,114,105,112,116,105,111,110,95,110,95,50,52,0),false ]]);
          let tempY = true;
          let successS = String.fromCharCode(115,95,52,56,95,112,114,105,110,116,101,114,0);
          let editu: Map<any, any> = new Map([[String.fromCharCode(99,111,110,102,105,114,109,101,100,95,49,95,51,49,0),254], [String.fromCharCode(105,95,56,55,95,110,111,110,110,117,108,108,99,111,110,116,101,110,116,115,0),658], [String.fromCharCode(101,95,49,57,95,99,111,109,112,105,108,101,111,112,116,105,111,110,117,115,101,100,0),169]]);
          let f_positionF = String.fromCharCode(100,97,117,100,95,51,95,49,57,0);
         spinnerK.set(`${constantsb}`, editu.size << (Math.min(1, Math.abs(constantsb))));
         statisticsF.set(successS, successS.length);
         tempY = f_positionF.length == 80 && statisticsF.size == 80;
         editu.set(`${tempY}`, statisticsF.size % 2);
         f_positionF += "3";
         break;
      }
      if (Array.from(spinnerK.values()).includes(constantsb)) {
         constantsb /= Math.max(1, 3);
      }
         lessz -= parseFloat(`${parseInt(`${lessz}`)}`);
      for (let w = 0; w < 1; w++) {
          let eactw = String.fromCharCode(118,95,56,48,95,105,108,108,105,113,97,0);
         lessz -= parseFloat(`${constantsb}`);
         eactw += `${1 + eactw.length}`;
      }
          let buttonE = 2.0;
         spinnerK.set(`${constantsb}`, parseInt(`${lessz}`));
         buttonE -= parseInt(`${buttonE}`) - 3;
      let xvod6 = lessz >= 7850381.0;
      do {
         lessz -= parseFloat(`${constantsb / (Math.max(3, spinnerK.size))}`);
         if (xvod6) {
            break;
         }
      } while (xvod6 && (1 >= (4 / (Math.max(2, constantsb))) || 2.12 >= (parseFloat(`${constantsb}`) + lessz)));
      favorite4 /= Math.max(1, country3.length);
   }
   if ((actionsN - 4) <= 5 || 4 <= (country3.length - 4)) {
      actionsN >>= Math.min(3, Math.abs(login9.length / (Math.max(9, actionsN))));
   }

        AsyncStorage.setItem(this.#localKey, jsonStr);
    }
}