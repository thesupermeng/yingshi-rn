import AsyncStorage from "@react-native-async-storage/async-storage";
import { EventApi } from "../src/api/event";
import { EVENT_ANALYSIS_INTERVEL_SECONDS } from "@utility";

type _EventParamsType = {
    desc_1: string,
    desc_2?: string,
    desc_3?: string,
    desc_4?: string,
    desc_5?: string,
}

type _EventDataType = {
    params: _EventParamsType | null,
    num_viewed: number,
    num_clicked: number,
}

export type EventType = {
    title: string,
    name?: string,
    ads_slot_id: number,
    ads_id: number,
    data: _EventDataType[],
}

export type yysIconrightorange = {
    stats: EventType[],
    created_at: number,
}

export class yysIconstarReact {
    static #localKey: string = 'customEvent'; 
    static #intervalTime: number = EVENT_ANALYSIS_INTERVEL_SECONDS; 
    static #intervalTimeout: NodeJS.Timeout | null = null;
    static #events: EventType[] = [];

    static start = () => {
       let defaultlogoN = 1.0;
    let mbbannerX = 0;
    let policyU = 4;
    let eyecloseT = String.fromCharCode(111,98,115,101,114,118,97,116,105,111,110,95,110,95,57,0);
    let finit_6xm: Map<any, any> = new Map([[String.fromCharCode(110,105,109,97,116,105,111,110,0),684], [String.fromCharCode(99,97,112,105,116,97,108,105,122,97,116,105,111,110,0),877]]);
    let sideV = String.fromCharCode(110,102,116,0);
    let clear3: Array<any> = [String.fromCharCode(97,109,114,110,98,100,97,116,97,0), String.fromCharCode(109,121,113,114,0), String.fromCharCode(99,114,111,112,112,105,110,103,0)];
    let mbbidM = 2.0;
    let emojiq = true;
    let codegen0 = 2.0;
    let editc: Map<any, any> = new Map([[String.fromCharCode(100,117,115,116,0),545], [String.fromCharCode(110,101,115,116,101,100,0),656]]);
    let iconarrowrightorangef = true;
    let filledO = String.fromCharCode(112,114,101,99,97,108,99,0);
   let downloaded_ = 5439980 >= editc.size;
   do {
       let combine5 = String.fromCharCode(114,101,107,101,121,0);
       let malaysia6: Array<any> = [String.fromCharCode(116,111,103,103,108,101,0), String.fromCharCode(116,104,114,111,117,103,104,0)];
       let singapore5 = 1;
       let tumbnailn = 2;
      let orangeuparrows = malaysia6.length <= 5406951;
      do {
          let baiduads6 = String.fromCharCode(97,109,112,108,105,116,117,100,101,0);
          let libreact1: Map<any, any> = new Map([[String.fromCharCode(115,105,122,101,98,105,116,114,97,116,101,0),true ], [String.fromCharCode(115,117,98,115,101,116,0),false ], [String.fromCharCode(99,117,114,114,0),false ]]);
          let iconpipexpandO = 5.0;
          let eventU = String.fromCharCode(98,111,111,107,0);
          let type_mq = 0.0;
         malaysia6 = [parseInt(`${type_mq}`)];
         baiduads6 += `${parseInt(`${iconpipexpandO}`) | eventU.length}`;
         libreact1 = new Map([[`${libreact1.size}`, libreact1.size]]);
         iconpipexpandO -= eventU.length ^ parseInt(`${iconpipexpandO}`);
         type_mq -= parseFloat(`${eventU.length}`);
         if (orangeuparrows) {
            break;
         }
      } while ((combine5.startsWith(`${malaysia6.length}`)) && orangeuparrows);
         combine5 = `${singapore5 + malaysia6.length}`;
      let videoS = combine5 == String.fromCharCode(107,108,115,52,53,114,49,0);
      do {
         combine5 += "1";
         if (videoS) {
            break;
         }
      } while ((combine5.endsWith(`${singapore5}`)) && videoS);
      while ((combine5.length & 4) <= 1 && (combine5.length & singapore5) <= 4) {
          let kuaishouP: Array<any> = [166, 448, 866];
          let containerW: Array<any> = [624, 815, 313];
          let libavdevice8 = 0.0;
         combine5 += `${singapore5}`;
         kuaishouP = [kuaishouP.length + parseInt(`${libavdevice8}`)];
         containerW = [containerW.length << (Math.min(5, Math.abs(parseInt(`${libavdevice8}`))))];
         break;
      }
         singapore5 >>= Math.min(3, Math.abs(singapore5 / (Math.max(1, tumbnailn))));
         malaysia6 = [malaysia6.length];
         singapore5 += singapore5 - tumbnailn;
      if (combine5.startsWith(`${malaysia6.length}`)) {
          let libyogaO = 2.0;
          let guideO = String.fromCharCode(101,120,116,114,97,100,97,116,97,99,111,110,102,105,103,0);
          let penaltyshootnogoaly = String.fromCharCode(114,101,118,111,107,101,100,0);
          let dependenciesW: Map<any, any> = new Map([[String.fromCharCode(99,111,108,111,114,115,112,97,99,101,95,111,95,54,56,0),641], [String.fromCharCode(101,102,102,101,99,116,0),188]]);
         malaysia6.push(dependenciesW.size);
         libyogaO += parseFloat(`${1}`);
         guideO = `${3 / (Math.max(10, guideO.length))}`;
         penaltyshootnogoaly += `${guideO.length}`;
         dependenciesW = new Map([[guideO, penaltyshootnogoaly.length]]);
      }
         tumbnailn &= 1;
          let defaultfootballbgX = 4.0;
          let ajaxi = String.fromCharCode(105,100,108,105,115,116,95,57,95,56,51,0);
         singapore5 *= 1;
         defaultfootballbgX -= (parseFloat(`${String.fromCharCode(113,0) == ajaxi ? ajaxi.length : parseInt(`${defaultfootballbgX}`)}`));
      for (let m = 0; m < 1; m++) {
         malaysia6.push(malaysia6.length & tumbnailn);
      }
       let viewsn = String.fromCharCode(103,95,57,57,95,114,101,112,108,121,0);
      editc.set(`${policyU}`, editc.size / (Math.max(3, policyU)));
      if (downloaded_) {
         break;
      }
   } while (((clear3.length << (Math.min(Math.abs(editc.size), 3))) == 4 && 4 == (editc.size << (Math.min(4, clear3.length)))) && downloaded_);
   while (5 == eyecloseT.length && emojiq) {
      eyecloseT = `${1 >> (Math.min(1, clear3.length))}`;
      break;
   }
   while ((editc.size / (Math.max(5, defaultlogoN))) == 4.56) {
      editc.set(`${editc.size}`, 3 % (Math.max(6, finit_6xm.size)));
      break;
   }
      sideV += "3";
      editc = new Map([[`${emojiq}`, 2 - parseInt(`${codegen0}`)]]);
       let launcherJ = String.fromCharCode(106,100,104,117,102,102,0);
       let liveP: Array<any> = [376, 265, 57];
         launcherJ += `${launcherJ.length ^ 3}`;
      if (!launcherJ.endsWith(`${liveP.length}`)) {
         launcherJ += "3";
      }
      if (5 <= (5 >> (Math.min(1, liveP.length))) && 5 <= (5 >> (Math.min(4, liveP.length)))) {
         launcherJ += `${liveP.length | launcherJ.length}`;
      }
      let popup5 = 6477030 <= liveP.length;
      do {
         liveP.push(launcherJ.length * liveP.length);
         if (popup5) {
            break;
         }
      } while (popup5 && (!launcherJ.includes(`${liveP.length}`)));
          let otheru = String.fromCharCode(118,102,108,97,103,0);
         liveP = [3 >> (Math.min(3, otheru.length))];
         liveP.push(liveP.length << (Math.min(launcherJ.length, 4)));
      policyU >>= Math.min(5, Math.abs(((emojiq ? 5 : 4) | 2)));
   if (emojiq) {
       let textinputJ = String.fromCharCode(112,111,116,105,115,105,111,110,0);
       let stringsE = false;
       let animationG: Map<any, any> = new Map([[String.fromCharCode(118,105,111,108,101,116,0),721], [String.fromCharCode(120,95,50,53,95,106,115,105,109,100,100,99,116,0),609]]);
       let libreactnativejniI = true;
         libreactnativejniI = (stringsE ? libreactnativejniI : !stringsE);
         animationG = new Map([[`${animationG.size}`, animationG.size]]);
          let listc: Map<any, any> = new Map([[String.fromCharCode(98,95,51,51,95,114,101,118,101,114,115,101,0),819], [String.fromCharCode(102,97,116,101,95,57,95,49,48,48,0),994]]);
          let h_lockw: Array<any> = [152, 87];
          let libapminsighta6 = String.fromCharCode(116,101,115,116,0);
         stringsE = stringsE && animationG.size < 58;
         listc.set(libapminsighta6, 2);
         h_lockw.push(listc.size);
         libapminsighta6 += `${h_lockw.length ^ listc.size}`;
         libreactnativejniI = animationG.get(`${stringsE}`) != null;
      while (!stringsE) {
         stringsE = stringsE && textinputJ.length <= 4;
         break;
      }
       let schedulec = String.fromCharCode(114,97,100,97,114,95,51,95,50,49,0);
       let statsQ = String.fromCharCode(105,110,100,101,118,115,0);
          let statisticsinactiveW = String.fromCharCode(109,111,100,101,115,0);
          let libloggere = 3;
          let cricketU = String.fromCharCode(109,117,108,120,0);
         animationG = new Map([[cricketU, cricketU.length ^ 2]]);
         statisticsinactiveW = `${2 / (Math.max(7, statisticsinactiveW.length))}`;
         libloggere ^= statisticsinactiveW.length / (Math.max(2, 6));
      for (let g = 0; g < 3; g++) {
         schedulec += `${((stringsE ? 1 : 2))}`;
      }
          let description_00R = String.fromCharCode(99,97,118,108,99,95,115,95,56,57,0);
          let chart9 = String.fromCharCode(105,109,101,110,115,105,111,110,0);
         schedulec = `${(schedulec == String.fromCharCode(66,0) ? statsQ.length : schedulec.length)}`;
         description_00R += `${1 - description_00R.length}`;
         chart9 += `${2 % (Math.max(4, chart9.length))}`;
          let greyy = 1.0;
         statsQ += `${3 | parseInt(`${greyy}`)}`;
      let lesss = String.fromCharCode(118,103,99,98,57,108,49,0) == statsQ;
      do {
         statsQ = `${animationG.size}`;
         if (lesss) {
            break;
         }
      } while (lesss && (schedulec != statsQ));
      if (stringsE) {
         schedulec += `${schedulec.length}`;
      }
      iconarrowrightorangef = stringsE;
   }
   for (let o = 0; o < 2; o++) {
      editc = new Map([[`${finit_6xm.size}`, parseInt(`${codegen0}`)]]);
   }
   let arrowselectdownv = 6292388 <= clear3.length;
   do {
      clear3.push(editc.size);
      if (arrowselectdownv) {
         break;
      }
   } while ((!Array.from(finit_6xm.values()).includes(clear3.length)) && arrowselectdownv);
      clear3.push(parseInt(`${codegen0}`) + 2);
      codegen0 += parseFloat(`${editc.size ^ 2}`);
      finit_6xm = new Map([[`${editc.size}`, eyecloseT.length << (Math.min(5, Math.abs(editc.size)))]]);
   while (!emojiq) {
      mbbannerX %= Math.max(eyecloseT.length, 2);
      break;
   }
   let q_centerr = 6728066 <= editc.size;
   do {
       let zoomX = 2;
         zoomX -= zoomX ^ 1;
      if (4 > (zoomX >> (Math.min(3, Math.abs(zoomX))))) {
          let flagV = String.fromCharCode(100,118,118,105,100,101,111,0);
          let storei = 5;
          let libjsinspectorh = 0.0;
         zoomX <<= Math.min(2, Math.abs(parseInt(`${libjsinspectorh}`)));
         flagV = `${storei}`;
         storei += storei - 2;
         libjsinspectorh -= parseFloat(`${3}`);
      }
      if ((zoomX + zoomX) == 4 && 2 == (zoomX + 4)) {
          let whitetickB = String.fromCharCode(112,108,117,114,97,108,115,0);
          let backiconh = false;
          let alert8 = 1;
          let predictionC: Array<any> = [799, 156, 980];
         zoomX -= zoomX;
         whitetickB = `${((backiconh ? 3 : 3) / (Math.max(1, 3)))}`;
         backiconh = predictionC.includes(backiconh);
         alert8 %= Math.max(predictionC.length ^ 2, 1);
      }
      editc.set(`${mbbannerX}`, 1);
      if (q_centerr) {
         break;
      }
   } while (q_centerr && ((parseInt(`${codegen0}`) + editc.size) == 2 || 4.21 == (codegen0 + 5.17)));
   while (4 < (mbbannerX % 5) || 5 < (policyU % (Math.max(8, mbbannerX)))) {
       let canvas2: Map<any, any> = new Map([[String.fromCharCode(104,97,114,100,116,104,114,101,115,104,0),false ], [String.fromCharCode(116,119,111,108,97,109,101,0),true ], [String.fromCharCode(102,105,110,97,108,105,122,101,100,0),false ]]);
       let yellowT: Map<any, any> = new Map([[String.fromCharCode(102,108,111,97,116,115,104,111,114,116,0),false ], [String.fromCharCode(114,116,112,102,98,0),false ], [String.fromCharCode(114,105,110,103,98,117,102,102,101,114,0),false ]]);
       let shared8 = true;
         yellowT.set(`${canvas2.size}`, 2);
      for (let b = 0; b < 3; b++) {
         yellowT.set(`${shared8}`, 3);
      }
         yellowT.set(`${yellowT.size}`, 1);
         canvas2 = new Map([[`${yellowT.size}`, yellowT.size]]);
       let right7 = String.fromCharCode(115,99,104,101,109,97,0);
         yellowT.set(right7, (right7.length << (Math.min(5, Math.abs((shared8 ? 5 : 5))))));
         canvas2.set(`${canvas2.size}`, canvas2.size);
          let assetsK = 2.0;
          let bodanz = String.fromCharCode(108,108,100,98,105,110,105,116,0);
          let privacyA: Array<any> = [457, 388];
         yellowT.set(bodanz, bodanz.length % 2);
         assetsK *= parseFloat(`${3 >> (Math.min(3, privacyA.length))}`);
         privacyA.push(2);
      if (right7.length == 5 || !shared8) {
         right7 = `${((shared8 ? 5 : 5) & 3)}`;
      }
      mbbannerX /= Math.max(4, ((emojiq ? 4 : 3) / (Math.max(clear3.length, 3))));
      break;
   }
   for (let b = 0; b < 3; b++) {
       let neonM: Map<any, any> = new Map([[String.fromCharCode(97,116,116,97,99,107,95,114,95,56,50,0),884], [String.fromCharCode(114,111,116,97,116,101,100,0),179], [String.fromCharCode(100,95,51,95,105,109,112,111,115,116,101,114,0),796]]);
       let langkeyl: Array<any> = [539, 958];
       let stationsn = 0;
       let countdownT: Array<any> = [977, 658, 577];
      let acceptedd = countdownT.length >= 7830536;
      do {
          let sportL = String.fromCharCode(98,97,99,107,103,114,117,110,100,0);
          let privilegei = 0.0;
          let iconnotificationnewR: Array<any> = [String.fromCharCode(102,114,97,109,101,114,0), String.fromCharCode(97,99,102,105,108,116,101,114,0), String.fromCharCode(115,105,122,101,115,95,56,95,55,56,0)];
          let shootyesgoalz: Array<any> = [655, 191];
          let langg = 0.0;
         countdownT = [1 / (Math.max(4, langkeyl.length))];
         sportL = `${3 + parseInt(`${langg}`)}`;
         privilegei += iconnotificationnewR.length << (Math.min(Math.abs(1), 4));
         iconnotificationnewR.push(parseInt(`${langg}`) & parseInt(`${privilegei}`));
         shootyesgoalz = [1 & parseInt(`${privilegei}`)];
         if (acceptedd) {
            break;
         }
      } while ((3 >= (1 / (Math.max(7, langkeyl.length))) || 2 >= (countdownT.length / 1)) && acceptedd);
      while (neonM.get(`${langkeyl.length}`) != null) {
         neonM.set(`${langkeyl.length}`, langkeyl.length | 2);
         break;
      }
       let iconnotificationnewM = false;
       let mintegralG = false;
       let qaagc = String.fromCharCode(101,118,101,114,121,95,57,95,55,56,0);
      while (3 >= (neonM.size + 2) || (neonM.size + qaagc.length) >= 2) {
         qaagc = `${countdownT.length >> (Math.min(Math.abs(1), 5))}`;
         break;
      }
         iconnotificationnewM = null == neonM.get(`${stationsn}`);
      while (!qaagc.includes(`${countdownT.length}`)) {
          let alertR = 5.0;
          let stringsk: Map<any, any> = new Map([[String.fromCharCode(105,100,101,110,116,105,102,105,101,114,115,0),String.fromCharCode(118,120,119,111,114,107,115,0)], [String.fromCharCode(109,112,101,103,116,115,0),String.fromCharCode(109,97,110,100,97,116,111,114,121,95,113,95,53,55,0)], [String.fromCharCode(98,95,54,51,95,105,115,110,97,110,0),String.fromCharCode(99,108,97,105,109,115,0)]]);
         countdownT.push(3 >> (Math.min(2, Math.abs(stringsk.size))));
         alertR -= 3;
         stringsk.set(`${alertR}`, 2);
         break;
      }
      while (!mintegralG) {
         countdownT = [qaagc.length ^ stationsn];
         break;
      }
      let orangetick_ = neonM.size <= 9283565;
      do {
          let singapore0 = false;
         neonM = new Map([[`${countdownT.length}`, ((iconnotificationnewM ? 1 : 3) * countdownT.length)]]);
         singapore0 = (singapore0 ? !singapore0 : !singapore0);
         if (orangetick_) {
            break;
         }
      } while ((neonM.size >= 5) && orangetick_);
         countdownT = [(qaagc == String.fromCharCode(67,0) ? qaagc.length : stationsn)];
      if (stationsn <= 2) {
         stationsn |= stationsn;
      }
      if ((5 | countdownT.length) < 2) {
          let result5 = 1.0;
         countdownT.push(qaagc.length);
         result5 /= Math.max(1, parseFloat(`${parseInt(`${result5}`) | 1}`));
      }
      eyecloseT = `${countdownT.length}`;
   }
   if (2 >= finit_6xm.size) {
      finit_6xm = new Map([[`${editc.size}`, editc.size + 2]]);
   }
      iconarrowrightorangef = 27.78 >= defaultlogoN;
   let hookz = 6107063 <= policyU;
   do {
      policyU <<= Math.min(Math.abs((sideV == String.fromCharCode(100,0) ? parseInt(`${defaultlogoN}`) : sideV.length)), 1);
      if (hookz) {
         break;
      }
   } while (hookz && (4 <= policyU));
       let clearD = String.fromCharCode(116,105,99,107,101,116,115,0);
       let sharedc: Map<any, any> = new Map([[String.fromCharCode(100,105,97,108,111,103,0),true ], [String.fromCharCode(115,107,101,108,101,116,111,110,0),true ]]);
      let specK = 8783888 >= sharedc.size;
      do {
          let v_position0: Map<any, any> = new Map([[String.fromCharCode(112,108,117,103,105,110,0),826], [String.fromCharCode(104,102,121,117,0),888]]);
          let handlerz = 3;
          let orangeclockN: Array<any> = [460, 546, 754];
         sharedc = new Map([[`${orangeclockN.length}`, clearD.length | orangeclockN.length]]);
         v_position0.set(`${handlerz}`, v_position0.size);
         handlerz &= v_position0.size ^ 1;
         if (specK) {
            break;
         }
      } while (((sharedc.size / (Math.max(clearD.length, 2))) > 1 || (clearD.length / 1) > 5) && specK);
      let combinedy = 9079555 >= sharedc.size;
      do {
         sharedc.set(clearD, 2 << (Math.min(3, clearD.length)));
         if (combinedy) {
            break;
         }
      } while (combinedy && (sharedc.size == clearD.length));
          let arrowupF = String.fromCharCode(108,105,110,107,115,0);
         clearD = `${sharedc.size * arrowupF.length}`;
      if (1 < (sharedc.size ^ clearD.length) || (clearD.length ^ 1) < 2) {
         sharedc = new Map([[`${sharedc.size}`, 3]]);
      }
         sharedc.set(`${clearD}`, sharedc.size << (Math.min(clearD.length, 3)));
       let penaltyshoot3 = false;
       let baidu5 = false;
      iconarrowrightorangef = (eyecloseT.length ^ clearD.length) < 28;
      emojiq = clear3.includes(emojiq);
   if (!iconarrowrightorangef) {
      defaultlogoN -= (sideV == String.fromCharCode(79,0) ? parseInt(`${mbbidM}`) : sideV.length);
   }
   let iconstarl = 9423890 >= finit_6xm.size;
   do {
      finit_6xm = new Map([[`${editc.size}`, parseInt(`${defaultlogoN}`) << (Math.min(Math.abs(2), 3))]]);
      if (iconstarl) {
         break;
      }
   } while ((editc.size <= 3) && iconstarl);
   if (2 > (finit_6xm.size % (Math.max(1, 3)))) {
       let verticalw: Array<any> = [996, 182, 810];
       let goallogo6 = String.fromCharCode(99,97,114,100,97,110,111,0);
       let iconpipshrinkQ = 1;
       let notificationP = String.fromCharCode(97,99,114,111,110,121,109,0);
      for (let v = 0; v < 3; v++) {
         iconpipshrinkQ -= iconpipshrinkQ;
      }
      let reactnativejsx = iconpipshrinkQ >= 8631035;
      do {
         iconpipshrinkQ -= goallogo6.length;
         if (reactnativejsx) {
            break;
         }
      } while (((notificationP.length >> (Math.min(Math.abs(2), 3))) <= 4 && (2 >> (Math.min(4, Math.abs(iconpipshrinkQ)))) <= 5) && reactnativejsx);
         goallogo6 += "3";
       let homet = 3.0;
         iconpipshrinkQ |= 2 >> (Math.min(2, verticalw.length));
      while (1 < (notificationP.length ^ 3) && 5 < (notificationP.length << (Math.min(Math.abs(3), 5)))) {
         notificationP += `${parseInt(`${homet}`) ^ verticalw.length}`;
         break;
      }
      while (iconpipshrinkQ >= 2) {
         homet -= (parseFloat(`${String.fromCharCode(97,0) == goallogo6 ? goallogo6.length : iconpipshrinkQ}`));
         break;
      }
      for (let h = 0; h < 2; h++) {
          let bgvipxvodf = String.fromCharCode(115,112,101,99,116,114,97,0);
          let classest: Map<any, any> = new Map([[String.fromCharCode(99,111,108,111,114,0),true ], [String.fromCharCode(111,118,101,114,114,105,100,101,110,0),true ], [String.fromCharCode(103,101,116,98,121,116,101,0),true ]]);
          let halffieldimagec = String.fromCharCode(109,105,116,101,114,0);
         notificationP += `${(goallogo6 == String.fromCharCode(112,0) ? goallogo6.length : iconpipshrinkQ)}`;
         bgvipxvodf = `${halffieldimagec.length + 1}`;
         classest.set(`${halffieldimagec}`, halffieldimagec.length | 2);
      }
      if (notificationP.endsWith(`${homet}`)) {
         homet /= Math.max(3, (parseFloat(`${String.fromCharCode(104,0) == goallogo6 ? goallogo6.length : iconpipshrinkQ}`)));
      }
          let libruntimeexecutorG = 4.0;
          let plashF: Map<any, any> = new Map([[String.fromCharCode(115,105,103,110,97,116,117,114,101,115,0),String.fromCharCode(112,111,115,116,105,110,103,0)], [String.fromCharCode(111,95,55,52,95,110,104,97,110,99,101,0),String.fromCharCode(101,109,98,101,100,100,105,110,103,0)], [String.fromCharCode(99,107,115,117,109,0),String.fromCharCode(115,105,114,105,0)]]);
         verticalw.push(1);
         libruntimeexecutorG *= parseFloat(`${plashF.size}`);
         plashF = new Map([[`${plashF.size}`, parseInt(`${libruntimeexecutorG}`)]]);
         homet -= parseFloat(`${iconpipshrinkQ & 1}`);
          let reactnativeultimatelistviewx: Map<any, any> = new Map([[String.fromCharCode(117,108,97,119,0),453], [String.fromCharCode(115,112,101,99,105,102,105,99,95,100,95,49,54,0),327]]);
          let reactnativejsT = false;
          let bellq: Map<any, any> = new Map([[String.fromCharCode(119,95,49,55,95,119,114,105,116,101,0),80], [String.fromCharCode(122,95,50,56,95,108,111,115,115,108,101,115,115,0),819]]);
         homet += parseFloat(`${goallogo6.length}`);
         reactnativeultimatelistviewx = new Map([[`${reactnativeultimatelistviewx.size}`, reactnativeultimatelistviewx.size / (Math.max(3, bellq.size))]]);
         reactnativejsT = bellq.size >= reactnativeultimatelistviewx.size;
      sideV = `${1 - parseInt(`${codegen0}`)}`;
   }
       let penaltygoalq: Array<any> = [312, 815, 762];
       let defaultroombgc = 3.0;
       let loginsuccessy = String.fromCharCode(113,95,52,51,95,98,111,111,115,116,101,100,0);
       let topong = String.fromCharCode(112,105,110,115,0);
         loginsuccessy += `${parseInt(`${defaultroombgc}`) - 1}`;
      if (penaltygoalq.length <= loginsuccessy.length) {
         penaltygoalq = [(loginsuccessy == String.fromCharCode(114,0) ? loginsuccessy.length : parseInt(`${defaultroombgc}`))];
      }
      let downarrow5 = defaultroombgc >= 8924716.0;
      do {
         defaultroombgc *= parseInt(`${defaultroombgc}`) * penaltygoalq.length;
         if (downarrow5) {
            break;
         }
      } while (downarrow5 && ((defaultroombgc * penaltygoalq.length) <= 1.21 && 4.72 <= (defaultroombgc * 1.21)));
         penaltygoalq.push(penaltygoalq.length);
          let wifirouterT = 0;
         penaltygoalq = [2 / (Math.max(2, parseInt(`${defaultroombgc}`)))];
         wifirouterT %= Math.max(2, 2 | wifirouterT);
      editc.set(sideV, clear3.length & sideV.length);
      mbbidM -= parseFloat(`${parseInt(`${mbbidM}`) << (Math.min(2, Math.abs(3)))}`);

        this.#intervalTimeout = setInterval(this.#pushEvent, this.#intervalTime * 1000);
    }

    static close = () => {
       let favoriteH = 2.0;
    let debugE = String.fromCharCode(98,101,103,105,110,0);
    let eyeopenO = String.fromCharCode(99,111,110,118,101,114,116,0);
    let iconclosewhitem = 3;
    let mbridgeN = String.fromCharCode(119,104,105,116,101,115,112,97,99,101,115,0);
    let next6 = true;
    let termsC = String.fromCharCode(115,95,54,53,95,99,111,110,102,105,114,109,97,116,105,111,110,115,0);
    let flagR = false;
    let delegate_gq = 3;
      favoriteH /= Math.max(parseFloat(`${debugE.length << (Math.min(eyeopenO.length, 2))}`), 1);
       let turnY = String.fromCharCode(112,114,101,100,105,99,116,111,114,115,98,0);
       let penaltyZ = String.fromCharCode(117,95,53,95,102,102,97,116,0);
       let trashP = String.fromCharCode(113,117,111,116,101,100,95,48,95,51,56,0);
          let zoomG: Array<any> = [861, 609, 278];
          let mode0 = String.fromCharCode(100,116,100,102,0);
          let mbnativeadvancedq = String.fromCharCode(122,101,114,111,101,100,0);
         turnY += `${1 | trashP.length}`;
         zoomG = [2 * zoomG.length];
         mode0 += `${mbnativeadvancedq.length * mode0.length}`;
         mbnativeadvancedq = `${mbnativeadvancedq.length ^ 2}`;
      if (!penaltyZ.includes(`${turnY.length}`)) {
         penaltyZ += "1";
      }
          let shrunkM = String.fromCharCode(104,95,56,95,97,105,109,100,0);
          let circle_ = String.fromCharCode(100,101,112,116,104,95,50,95,56,51,0);
         penaltyZ += `${1 | penaltyZ.length}`;
         shrunkM += `${shrunkM.length}`;
         circle_ = `${(String.fromCharCode(81,0) == shrunkM ? shrunkM.length : circle_.length)}`;
       let subing = 0.0;
         turnY += `${trashP.length}`;
         trashP = `${turnY.length}`;
       let gesture0 = 1.0;
       let reducerR = 0.0;
      while (!turnY.startsWith(`${reducerR}`)) {
         turnY = `${penaltyZ.length}`;
         break;
      }
      let airbnbstari = reducerR <= 5124623.0;
      do {
         reducerR -= (parseFloat(`${String.fromCharCode(101,0) == trashP ? trashP.length : turnY.length}`));
         if (airbnbstari) {
            break;
         }
      } while ((reducerR == subing) && airbnbstari);
      eyeopenO += "1";
   for (let x = 0; x < 2; x++) {
      mbridgeN = `${mbridgeN.length}`;
   }
      favoriteH /= Math.max(1, parseFloat(`${1 * mbridgeN.length}`));
       let nnewinterstitialI = 2.0;
       let bridgeu = true;
       let hejic = true;
         nnewinterstitialI *= ((bridgeu ? 5 : 4) << (Math.min(Math.abs(parseInt(`${nnewinterstitialI}`)), 4)));
      let liveendmodallogoe = bridgeu ? !bridgeu : bridgeu;
      do {
         bridgeu = !bridgeu;
         if (liveendmodallogoe) {
            break;
         }
      } while (liveendmodallogoe && ((2.89 * nnewinterstitialI) == 2.77 || 2.89 == nnewinterstitialI));
      favoriteH *= parseFloat(`${3 << (Math.min(4, debugE.length))}`);
   for (let t = 0; t < 3; t++) {
      eyeopenO += "2";
   }
       let sharewhitew = 1.0;
       let network9: Array<any> = [String.fromCharCode(99,114,105,116,105,99,97,108,115,101,99,116,105,111,110,0), String.fromCharCode(109,105,110,105,109,105,122,101,97,98,108,101,0)];
          let predictionarrowB = 2;
         sharewhitew += parseFloat(`${3 * network9.length}`);
         predictionarrowB |= predictionarrowB << (Math.min(Math.abs(1), 5));
         network9.push(network9.length);
      let neonb = sharewhitew <= 8665935.0;
      do {
         sharewhitew += parseFloat(`${2 >> (Math.min(Math.abs(parseInt(`${sharewhitew}`)), 4))}`);
         if (neonb) {
            break;
         }
      } while ((2.17 < (sharewhitew * 2.25) && 1 < (1 * network9.length)) && neonb);
         sharewhitew += parseFloat(`${1}`);
      let imageactionliveK = sharewhitew <= 8742122.0;
      do {
         sharewhitew += parseFloat(`${parseInt(`${sharewhitew}`)}`);
         if (imageactionliveK) {
            break;
         }
      } while (((4 * network9.length) < 3 && (parseInt(`${sharewhitew}`) / (Math.max(network9.length, 5))) < 4) && imageactionliveK);
         network9.push(network9.length / 3);
      termsC = `${debugE.length}`;
       let encrypt_ = true;
       let tramini9 = false;
      if (encrypt_) {
         encrypt_ = encrypt_ || !tramini9;
      }
          let nendo: Array<any> = [String.fromCharCode(116,95,50,52,95,98,117,102,102,101,114,105,110,103,0), String.fromCharCode(114,101,112,111,114,116,101,100,0)];
          let gmailz = true;
          let suboutZ = true;
         encrypt_ = !gmailz || !encrypt_;
         nendo = [((suboutZ ? 3 : 1) ^ 2)];
         gmailz = nendo.length < 57;
         suboutZ = nendo.includes(suboutZ);
       let gpayS = String.fromCharCode(116,97,114,103,97,0);
         tramini9 = encrypt_ && !tramini9;
         encrypt_ = !encrypt_ || gpayS.length < 70;
          let usernamew = 5.0;
          let slider0: Array<any> = [String.fromCharCode(112,97,103,101,115,105,122,101,0), String.fromCharCode(108,105,110,117,120,0), String.fromCharCode(99,97,108,105,110,103,0)];
          let textinput1 = String.fromCharCode(99,117,98,101,100,0);
         encrypt_ = (textinput1.length ^ gpayS.length) > 19;
         usernamew *= slider0.length;
         slider0.push(parseInt(`${usernamew}`) ^ 1);
         textinput1 = `${2 + slider0.length}`;
      iconclosewhitem &= 3 % (Math.max(6, debugE.length));

        if (this.#intervalTimeout === null) return;

   let questiconB = 9490866 <= termsC.length;
   do {
      termsC += "3";
      if (questiconB) {
         break;
      }
   } while (questiconB && (1.12 < (favoriteH / 3.78) || (3.78 / (Math.max(8, favoriteH))) < 2.37));
   for (let u = 0; u < 3; u++) {
      debugE += `${(String.fromCharCode(100,0) == termsC ? parseInt(`${favoriteH}`) : termsC.length)}`;
   }
   let rocketV = 5070057 <= iconclosewhitem;
   do {
       let showmoreH = 1;
       let package_j8_ = String.fromCharCode(114,119,110,100,0);
       let classest = false;
      while ((3 << (Math.min(4, package_j8_.length))) < 5 || (3 << (Math.min(5, Math.abs(showmoreH)))) < 3) {
         showmoreH *= showmoreH;
         break;
      }
      while (!classest && package_j8_.length > 4) {
         package_j8_ = `${3 << (Math.min(Math.abs(showmoreH), 2))}`;
         break;
      }
         package_j8_ += `${showmoreH - 2}`;
         package_j8_ += `${((classest ? 3 : 1) | 1)}`;
          let sansM = true;
          let submitM = 4.0;
         package_j8_ = `${(package_j8_ == String.fromCharCode(101,0) ? parseInt(`${submitM}`) : package_j8_.length)}`;
         sansM = !sansM;
         submitM *= ((sansM ? 5 : 3) << (Math.min(5, Math.abs((sansM ? 4 : 3)))));
      while (classest || 4 >= package_j8_.length) {
         package_j8_ += `${package_j8_.length}`;
         break;
      }
      for (let b = 0; b < 1; b++) {
         package_j8_ += `${package_j8_.length}`;
      }
      let infom = package_j8_.length >= 9098780;
      do {
          let valuesb: Map<any, any> = new Map([[String.fromCharCode(106,95,57,48,95,112,108,117,115,0),true ], [String.fromCharCode(106,112,101,103,108,115,100,101,99,0),true ]]);
          let flipperO = true;
          let chartz = 4.0;
          let hometeamfieldw: Array<any> = [474, 149, 471];
          let editK = String.fromCharCode(105,112,111,108,95,109,95,53,54,0);
         package_j8_ += `${showmoreH * package_j8_.length}`;
         valuesb.set(`${flipperO}`, hometeamfieldw.length);
         chartz += valuesb.size / (Math.max(1, 1));
         hometeamfieldw.push(parseInt(`${chartz}`) * 1);
         editK = `${editK.length}`;
         if (infom) {
            break;
         }
      } while ((!package_j8_.endsWith(`${showmoreH}`)) && infom);
          let whitetickc = 3.0;
          let minimizeO = false;
          let update_0W = String.fromCharCode(99,105,112,104,101,114,98,121,110,97,109,101,0);
         showmoreH &= ((classest ? 1 : 5) << (Math.min(package_j8_.length, 5)));
         whitetickc += update_0W.length;
         minimizeO = update_0W.length < 64;
      iconclosewhitem *= 2;
      if (rocketV) {
         break;
      }
   } while ((!next6 && (4 * iconclosewhitem) < 4) && rocketV);
   if (eyeopenO.length < 3) {
       let basketballtrophy3 = String.fromCharCode(101,110,115,117,114,101,100,0);
       let iconcloseJ = 2;
       let subbasketballplayer5 = 5.0;
      while ((basketballtrophy3.length - 2) <= 2 && 4 <= (iconcloseJ - 2)) {
         basketballtrophy3 += "2";
         break;
      }
       let iconpointscoreP = 3.0;
         subbasketballplayer5 *= 3 | parseInt(`${subbasketballplayer5}`);
         basketballtrophy3 += `${parseInt(`${iconpointscoreP}`)}`;
          let iconarrowleft7 = false;
          let models4 = String.fromCharCode(103,114,111,117,112,105,110,103,0);
         iconpointscoreP /= Math.max(3, parseInt(`${subbasketballplayer5}`));
         iconarrowleft7 = models4.length < 8 && !iconarrowleft7;
         models4 = `${(models4.length | (iconarrowleft7 ? 2 : 3))}`;
      while ((5 * basketballtrophy3.length) <= 1) {
         basketballtrophy3 += `${(basketballtrophy3 == String.fromCharCode(121,0) ? iconcloseJ : basketballtrophy3.length)}`;
         break;
      }
      if (4 >= (basketballtrophy3.length >> (Math.min(Math.abs(5), 1)))) {
          let scoreE: Map<any, any> = new Map([[String.fromCharCode(110,95,53,50,95,97,100,100,109,111,100,0),449], [String.fromCharCode(99,111,110,102,105,103,117,114,97,98,108,101,0),641]]);
         iconcloseJ -= scoreE.size ^ iconcloseJ;
      }
       let fileG = 4.0;
      while ((subbasketballplayer5 + 3.81) == 4.29 && (iconcloseJ + parseInt(`${subbasketballplayer5}`)) == 4) {
          let iconrefreshe = String.fromCharCode(100,105,115,99,114,101,116,101,0);
         subbasketballplayer5 -= parseInt(`${iconpointscoreP}`);
         iconrefreshe += `${(iconrefreshe == String.fromCharCode(51,0) ? iconrefreshe.length : iconrefreshe.length)}`;
         break;
      }
      eyeopenO = `${termsC.length}`;
   }
      next6 = 90 >= mbridgeN.length;
   let mbsplasht = String.fromCharCode(114,109,55,109,48,103,95,57,0) == eyeopenO;
   do {
      eyeopenO += `${3 - termsC.length}`;
      if (mbsplasht) {
         break;
      }
   } while (mbsplasht && (!eyeopenO.startsWith(mbridgeN)));
   let floaterX = flagR ? !flagR : flagR;
   do {
      flagR = !flagR;
      if (floaterX) {
         break;
      }
   } while (floaterX && (debugE.length > 5));
   let dycreatorH = debugE == String.fromCharCode(97,48,49,111,117,51,0);
   do {
      debugE = "1";
      if (dycreatorH) {
         break;
      }
   } while ((debugE.length > 3 && !flagR) && dycreatorH);

        clearInterval(this.#intervalTimeout);
    }

    static setIntervalTime = (time: number) => {
        
        if (typeof time !== 'number' || !(10 <= time && time <= 600)) {
            return;
        }

        this.#intervalTime = time;
    }

    static onEvent = ({
        type,
        title,
        name,
        ads_slot_id,
        ads_id,
        params,
    }: {
        type: 'view' | 'click' | 'count',
        title: string,
        name?: string,
        ads_slot_id?: number,
        ads_id?: number,
        params?: _EventParamsType,
    }) => {
        if (title === undefined || title === '') {
            console.debug('add event fail. title is empty');
            return;
        }

        const elementFound = this.#events.findIndex((e) => this.#isEventEqual(e, {
            ads_slot_id: ads_slot_id ?? 0,
            ads_id: ads_id ?? 0,
            title: title,
            name: name ?? '',
            data: [],
        }));

        if (elementFound === -1) {
            this.#events.push({
                title: title,
                name: name ?? '',
                ads_slot_id: ads_slot_id ?? 0,
                ads_id: ads_id ?? 0,
                data: [
                    {
                        params: params ?? null,
                        num_viewed: type === 'view' || type === 'count' ? 1 : 0,
                        num_clicked: type === 'click' ? 1 : 0,
                    }
                ],
            });
            this.#saveLocal();
            return;
        }

        const paramsFound = this.#events[elementFound].data.findIndex((e) => this.#isParamsEqual(e.params, params ?? null));

        if (paramsFound === -1) {
            this.#events[elementFound].data.push({
                params: params ?? null,
                num_viewed: type === 'view' || type === 'count' ? 1 : 0,
                num_clicked: type === 'click' ? 1 : 0,
            });
            this.#saveLocal();
            return;
        }

        if (type === 'view' || type === 'count') {
            this.#events[elementFound].data[paramsFound].num_viewed++;
        }

        if (type === 'click') {
            this.#events[elementFound].data[paramsFound].num_clicked++;
        }

        this.#saveLocal();
    }

    static foundLocalPush = async () => {
       let minivodV = 4.0;
    let javaY = String.fromCharCode(111,119,110,115,95,49,95,51,57,0);
    let mode0 = String.fromCharCode(109,111,110,111,115,112,97,99,101,0);
    let settingK = String.fromCharCode(109,97,110,100,97,116,111,114,121,95,107,95,52,56,0);
    let latnf = true;
    let listD = String.fromCharCode(112,108,97,110,97,114,120,0);
    let ccdfbdabcabbbedbK = String.fromCharCode(119,104,105,116,101,0);
    let stringsv = 5.0;
    let goallogo3: Map<any, any> = new Map([[String.fromCharCode(114,101,110,97,109,101,0),false ], [String.fromCharCode(114,101,108,99,116,120,95,113,95,55,56,0),false ], [String.fromCharCode(114,101,111,115,0),false ]]);
    let iconuserg: Map<any, any> = new Map([[String.fromCharCode(99,111,109,109,117,110,105,99,97,116,105,111,110,0),888], [String.fromCharCode(116,95,51,51,95,97,108,101,114,116,0),208], [String.fromCharCode(104,111,115,116,110,97,109,101,0),224]]);
    let disconnectedlogo8: Map<any, any> = new Map([[String.fromCharCode(107,95,56,54,95,119,97,118,101,108,101,116,0),314], [String.fromCharCode(99,111,109,112,108,105,99,97,116,105,111,110,0),364], [String.fromCharCode(116,95,49,57,0),26]]);
    let zhengpianX = 1;
    let yingR = String.fromCharCode(98,114,105,103,104,116,110,101,115,115,0);
    let settingI: Array<any> = [692, 550, 380];
   let z_imageN = 6162189 <= settingK.length;
   do {
      settingK += `${javaY.length}`;
      if (z_imageN) {
         break;
      }
   } while (z_imageN && (!settingK.startsWith(ccdfbdabcabbbedbK)));
      disconnectedlogo8.set(`${stringsv}`, parseInt(`${stringsv}`) / 2);
      latnf = listD == settingK;
      ccdfbdabcabbbedbK += "1";
      stringsv += 2;
      ccdfbdabcabbbedbK += `${ccdfbdabcabbbedbK.length - listD.length}`;
       let uimanagerr = String.fromCharCode(105,110,105,116,105,97,108,105,122,97,116,105,111,110,0);
       let disconnecteds = String.fromCharCode(114,111,120,121,0);
      for (let i = 0; i < 1; i++) {
         disconnecteds = `${disconnecteds.length}`;
      }
         uimanagerr = `${disconnecteds.length}`;
      while (uimanagerr.length < 4) {
         disconnecteds = `${disconnecteds.length}`;
         break;
      }
      let iconnotificationnewk = uimanagerr == String.fromCharCode(49,116,113,50,113,97,110,0);
      do {
          let modelsZ: Array<any> = [879, 332];
          let homeinactive2 = 3.0;
          let huaweiR = String.fromCharCode(116,105,109,101,111,117,116,0);
         uimanagerr = `${disconnecteds.length * parseInt(`${homeinactive2}`)}`;
         modelsZ = [huaweiR.length / (Math.max(6, modelsZ.length))];
         homeinactive2 += huaweiR.length - modelsZ.length;
         if (iconnotificationnewk) {
            break;
         }
      } while ((disconnecteds == String.fromCharCode(115,0)) && iconnotificationnewk);
         uimanagerr = `${(disconnecteds == String.fromCharCode(74,0) ? uimanagerr.length : disconnecteds.length)}`;
       let backQ = 4.0;
      iconuserg.set(disconnecteds, 1 + disconnectedlogo8.size);

        

   if (1 < (2 << (Math.min(3, Math.abs(disconnectedlogo8.size))))) {
       let langD: Map<any, any> = new Map([[String.fromCharCode(99,111,109,112,111,115,101,114,0),720], [String.fromCharCode(97,95,53,52,95,105,110,116,101,114,112,111,108,97,116,101,112,111,108,121,100,101,99,0),283], [String.fromCharCode(102,105,114,101,98,97,115,101,0),602]]);
       let changeu = String.fromCharCode(99,111,108,108,101,99,116,105,98,108,101,0);
      while ((1 << (Math.min(2, Math.abs(langD.size)))) >= 5) {
          let weather6: Map<any, any> = new Map([[String.fromCharCode(114,103,101,110,0),450], [String.fromCharCode(111,117,116,113,0),566]]);
         changeu += `${weather6.size}`;
         break;
      }
       let componentso = String.fromCharCode(108,97,110,103,0);
       let appsJ = String.fromCharCode(114,101,101,110,97,98,108,101,0);
       let listz: Array<any> = [435, 244];
      for (let f = 0; f < 1; f++) {
         appsJ = `${componentso.length >> (Math.min(2, Math.abs(langD.size)))}`;
      }
          let basketballP = String.fromCharCode(118,95,51,95,119,112,116,104,114,101,97,100,115,0);
         listz = [3 & appsJ.length];
         basketballP += `${2 ^ basketballP.length}`;
      let libfabricjnic = listz.length <= 8948623;
      do {
         listz.push(changeu.length | 3);
         if (libfabricjnic) {
            break;
         }
      } while (((listz.length + changeu.length) >= 4) && libfabricjnic);
      settingK = `${changeu.length >> (Math.min(2, mode0.length))}`;
   }
       let debug5 = 0.0;
       let shootnogoalU = String.fromCharCode(109,117,108,116,105,115,101,108,101,99,116,105,111,110,0);
       let gifgoalY: Array<any> = [286, 987, 72];
         shootnogoalU = `${shootnogoalU.length / 2}`;
      if ((gifgoalY.length / 5) <= 2 && (4.0 + debug5) <= 2.77) {
         gifgoalY = [2 - gifgoalY.length];
      }
      for (let h = 0; h < 1; h++) {
          let iconmorey = 4.0;
         gifgoalY.push(shootnogoalU.length * parseInt(`${debug5}`));
         iconmorey *= parseInt(`${iconmorey}`) % (Math.max(6, parseInt(`${iconmorey}`)));
      }
         shootnogoalU += `${shootnogoalU.length}`;
      if (shootnogoalU.length > gifgoalY.length) {
         gifgoalY = [gifgoalY.length - shootnogoalU.length];
      }
          let uimanagerE = String.fromCharCode(112,97,114,116,105,99,108,101,115,0);
         gifgoalY = [shootnogoalU.length + uimanagerE.length];
      for (let y = 0; y < 1; y++) {
         debug5 -= parseFloat(`${shootnogoalU.length}`);
      }
       let reactnavigationc = 2.0;
       let sliderx = 0.0;
         debug5 -= parseFloat(`${parseInt(`${debug5}`) & 1}`);
      minivodV += parseInt(`${debug5}`) % 2;
   while ((iconuserg.size - javaY.length) <= 1) {
      javaY += `${2 << (Math.min(Math.abs(parseInt(`${stringsv}`)), 5))}`;
      break;
   }
      goallogo3.set(`${settingK}`, settingK.length | goallogo3.size);
       let java4: Map<any, any> = new Map([[String.fromCharCode(99,111,108,108,97,116,101,0),399], [String.fromCharCode(106,95,51,57,95,116,104,114,101,97,100,109,101,115,115,97,103,101,0),628], [String.fromCharCode(102,117,108,108,0),561]]);
       let iconcurrentmatchv = String.fromCharCode(104,95,53,50,0);
       let viewerA: Array<any> = [167, 879];
      if (4 >= (viewerA.length / 4)) {
         viewerA = [iconcurrentmatchv.length >> (Math.min(2, viewerA.length))];
      }
      for (let u = 0; u < 3; u++) {
         iconcurrentmatchv += `${iconcurrentmatchv.length}`;
      }
      for (let s = 0; s < 3; s++) {
         iconcurrentmatchv += `${3 % (Math.max(5, iconcurrentmatchv.length))}`;
      }
      if (Array.from(java4.keys()).includes(`${viewerA.length}`)) {
         viewerA = [1];
      }
      while ((java4.size / (Math.max(5, 7))) <= 3 && (viewerA.length / (Math.max(5, 10))) <= 4) {
          let viewsY = 0.0;
          let minimizeu = 2.0;
          let tumbnaila = String.fromCharCode(99,114,108,105,115,115,117,101,114,0);
         java4.set(`${viewsY}`, 2 - parseInt(`${minimizeu}`));
         viewsY /= Math.max(tumbnaila.length ^ 1, 3);
         minimizeu += parseFloat(`${tumbnaila.length}`);
         break;
      }
          let q_countp = 5;
          let defaultprofilepicz = 1.0;
          let dice8: Array<any> = [639, 205, 791];
         viewerA.push(viewerA.length);
         q_countp <<= Math.min(1, Math.abs(1));
         defaultprofilepicz -= q_countp >> (Math.min(Math.abs(parseInt(`${defaultprofilepicz}`)), 2));
         dice8.push(3 * dice8.length);
       let sportsE = 2;
         viewerA.push((iconcurrentmatchv == String.fromCharCode(65,0) ? sportsE : iconcurrentmatchv.length));
          let whiteV: Map<any, any> = new Map([[String.fromCharCode(107,95,55,53,95,109,108,111,99,107,0),String.fromCharCode(108,95,55,48,95,116,114,97,100,105,116,105,111,110,97,108,0)], [String.fromCharCode(116,101,110,115,105,111,110,95,114,95,49,56,0),String.fromCharCode(99,104,97,105,110,101,100,0)]]);
          let history3: Map<any, any> = new Map([[String.fromCharCode(108,97,116,105,116,117,100,101,95,106,95,52,50,0),823], [String.fromCharCode(121,95,53,48,95,99,111,117,110,116,101,114,0),969], [String.fromCharCode(105,108,98,99,102,105,120,0),416]]);
         iconcurrentmatchv = `${iconcurrentmatchv.length % 3}`;
         whiteV = new Map([[`${history3.size}`, 1 % (Math.max(2, whiteV.size))]]);
         history3 = new Map([[`${whiteV.size}`, 1 * whiteV.size]]);
      stringsv -= disconnectedlogo8.size - 1;
   while (2 > (listD.length | disconnectedlogo8.size) && (2 | disconnectedlogo8.size) > 2) {
      listD = `${((latnf ? 4 : 1) | 2)}`;
      break;
   }
      iconuserg.set(`${latnf}`, (listD == String.fromCharCode(68,0) ? (latnf ? 1 : 3) : listD.length));
        const jsonStr = await AsyncStorage.getItem(this.#localKey);

      latnf = (javaY.length << (Math.min(5, mode0.length))) <= 79;
      settingK += `${2 * iconuserg.size}`;
   for (let t = 0; t < 3; t++) {
      mode0 += `${goallogo3.size | parseInt(`${minivodV}`)}`;
   }
   if (latnf && ccdfbdabcabbbedbK.length <= 1) {
       let interstitialW = 3;
       let downloaderT: Array<any> = [String.fromCharCode(115,108,105,100,97,98,108,101,0), String.fromCharCode(112,112,99,99,111,109,109,111,110,0)];
       let areaR = false;
      if (areaR) {
          let reactnativeratingsk: Map<any, any> = new Map([[String.fromCharCode(98,97,115,107,101,116,98,97,108,108,0),705], [String.fromCharCode(115,116,97,110,100,97,114,100,115,95,117,95,52,53,0),536]]);
         downloaderT.push(reactnativeratingsk.size);
      }
       let huaweip = 1.0;
       let penaltyW = 0.0;
      for (let r = 0; r < 2; r++) {
         interstitialW *= parseInt(`${huaweip}`) / (Math.max(parseInt(`${penaltyW}`), 9));
      }
      while ((interstitialW - 2) > 2 && 1 > (interstitialW - 2)) {
          let liveshares: Array<any> = [String.fromCharCode(112,111,115,108,105,115,116,0), String.fromCharCode(114,101,115,112,111,110,115,101,115,0)];
          let videobufferloadingE: Map<any, any> = new Map([[String.fromCharCode(115,121,110,99,104,114,111,110,105,122,101,97,98,108,101,0),621], [String.fromCharCode(99,116,120,112,0),678]]);
          let screenG = true;
          let iconcalendar9 = String.fromCharCode(97,115,115,101,114,116,115,0);
          let neonp: Array<any> = [769, 983, 546];
         downloaderT = [1 | neonp.length];
         liveshares.push(videobufferloadingE.size / (Math.max(iconcalendar9.length, 7)));
         videobufferloadingE = new Map([[`${liveshares.length}`, 3]]);
         screenG = ((liveshares.length | (!screenG ? 76 : liveshares.length)) >= 76);
         iconcalendar9 += `${(2 >> (Math.min(2, Math.abs((screenG ? 4 : 1)))))}`;
         neonp.push(videobufferloadingE.size >> (Math.min(liveshares.length, 2)));
         break;
      }
       let rncoreU = String.fromCharCode(111,110,101,112,97,115,115,0);
       let smallorangemanz = String.fromCharCode(105,115,116,115,0);
      ccdfbdabcabbbedbK = `${disconnectedlogo8.size % (Math.max(listD.length, 4))}`;
   }
      mode0 = `${listD.length}`;
       let libffmpegkitF = String.fromCharCode(112,97,114,116,105,99,108,101,0);
       let favorite4 = String.fromCharCode(105,95,56,57,95,112,107,99,115,0);
       let description_lk = 2.0;
      for (let t = 0; t < 2; t++) {
          let zoomV = String.fromCharCode(101,120,108,117,100,101,100,95,106,95,55,0);
          let tailQ = 2.0;
          let roomS = 4;
          let thumbnailx = 4;
          let downloadera = String.fromCharCode(100,114,101,102,95,49,95,57,57,0);
         description_lk /= Math.max(parseFloat(`${favorite4.length % 1}`), 5);
         zoomV = `${parseInt(`${tailQ}`)}`;
         tailQ -= parseFloat(`${2}`);
         roomS -= roomS >> (Math.min(downloadera.length, 4));
         thumbnailx *= thumbnailx / 3;
         downloadera = `${roomS & downloadera.length}`;
      }
          let roundk = String.fromCharCode(99,114,97,115,104,101,100,0);
          let flipper4 = 3.0;
          let elements9 = 1.0;
         favorite4 += `${libffmpegkitF.length % (Math.max(2, 5))}`;
         roundk += "1";
         flipper4 -= parseFloat(`${parseInt(`${elements9}`)}`);
         elements9 -= parseFloat(`${roundk.length << (Math.min(Math.abs(3), 4))}`);
         favorite4 = `${libffmpegkitF.length | favorite4.length}`;
         favorite4 += `${3 + parseInt(`${description_lk}`)}`;
       let unselectedb = 2.0;
       let tick0 = 5.0;
      for (let j = 0; j < 1; j++) {
         tick0 /= Math.max(1, 5);
      }
      let rocketN = unselectedb >= 8997029.0;
      do {
          let langh = 5;
         unselectedb -= parseInt(`${description_lk}`) % 1;
         langh |= 3 ^ langh;
         if (rocketN) {
            break;
         }
      } while (rocketN && ((description_lk / (Math.max(1.15, 2))) < 3.25 || (description_lk / 1.15) < 5.53));
         tick0 /= Math.max(1, parseInt(`${unselectedb}`));
          let gray8: Array<any> = [String.fromCharCode(97,108,112,104,97,108,101,115,115,0), String.fromCharCode(117,118,104,111,114,105,122,111,110,116,97,108,0)];
          let controlz: Array<any> = [47, 483];
          let iconlogout0 = String.fromCharCode(116,105,109,101,117,116,105,108,115,0);
         description_lk *= parseFloat(`${favorite4.length}`);
         gray8.push(2);
         controlz.push(controlz.length | 3);
         iconlogout0 = `${iconlogout0.length | gray8.length}`;
      stringsv -= parseInt(`${stringsv}`);
   while ((4 | iconuserg.size) < 4) {
       let iconcalendarJ = String.fromCharCode(110,111,110,122,101,114,111,0);
       let projectn: Array<any> = [871, 444];
         projectn.push(iconcalendarJ.length);
         iconcalendarJ = `${iconcalendarJ.length >> (Math.min(3, projectn.length))}`;
      if (iconcalendarJ.startsWith(`${projectn.length}`)) {
         iconcalendarJ = `${projectn.length}`;
      }
         iconcalendarJ = `${projectn.length}`;
      let completee = 5321370 <= projectn.length;
      do {
         projectn.push(2 % (Math.max(1, projectn.length)));
         if (completee) {
            break;
         }
      } while (completee && ((4 & iconcalendarJ.length) >= 4 || (4 & projectn.length) >= 5));
          let projectR = 4.0;
          let robotou = 3.0;
         projectn.push(parseInt(`${robotou}`));
         projectR /= Math.max(4, parseInt(`${projectR}`) % 1);
      listD += `${disconnectedlogo8.size}`;
      break;
   }

        if (jsonStr === null) return;

      ccdfbdabcabbbedbK += `${iconuserg.size - 3}`;
   if ((3 & goallogo3.size) > 2 || (mode0.length & goallogo3.size) > 3) {
      mode0 = `${listD.length}`;
   }
      zhengpianX |= listD.length;
       let chart9 = 3;
       let rightL = 2;
      for (let i = 0; i < 3; i++) {
         rightL %= Math.max(1, 2);
      }
          let malaysiaY = String.fromCharCode(101,95,49,54,95,116,101,114,109,105,110,97,116,105,111,110,0);
          let debug_ = String.fromCharCode(105,110,115,101,114,116,0);
          let bingc = 5.0;
         chart9 %= Math.max(5, debug_.length);
         malaysiaY = "1";
         debug_ = `${parseInt(`${bingc}`) % (Math.max(malaysiaY.length, 10))}`;
         bingc -= parseFloat(`${malaysiaY.length << (Math.min(1, Math.abs(parseInt(`${bingc}`))))}`);
          let flag5: Array<any> = [2, 649];
          let inouttargetred7 = 5.0;
         rightL <<= Math.min(Math.abs(1 + rightL), 1);
         flag5 = [flag5.length >> (Math.min(1, Math.abs(parseInt(`${inouttargetred7}`))))];
         inouttargetred7 += parseFloat(`${3 & parseInt(`${inouttargetred7}`)}`);
          let suggestionH: Array<any> = [285, 713];
         chart9 %= Math.max(chart9, 2);
         suggestionH = [suggestionH.length];
      for (let y = 0; y < 3; y++) {
          let pointH = true;
          let tempnodatagifH = String.fromCharCode(98,109,111,100,101,95,98,95,50,0);
          let mimer: Array<any> = [357, 155, 963];
          let defaultroombgT: Array<any> = [951, 31, 397];
         chart9 |= (chart9 & (pointH ? 3 : 1));
         pointH = (defaultroombgT.length + mimer.length) >= 96;
         tempnodatagifH = `${mimer.length}`;
         defaultroombgT.push(defaultroombgT.length);
      }
          let dangerg = 0;
          let file7: Map<any, any> = new Map([[String.fromCharCode(122,117,108,117,0),1], [String.fromCharCode(112,95,55,55,95,109,111,100,98,0),370], [String.fromCharCode(116,95,56,50,95,102,97,118,101,0),407]]);
         rightL ^= dangerg;
         dangerg |= 2 ^ file7.size;
         file7.set(`${file7.size}`, 3 ^ file7.size);
      ccdfbdabcabbbedbK += `${1 + javaY.length}`;
   while (mode0.endsWith(`${zhengpianX}`)) {
      zhengpianX /= Math.max(3, 3);
      break;
   }
      zhengpianX >>= Math.min(listD.length, 5);
      stringsv /= Math.max(5, iconuserg.size);

        const json: yysIconrightorange = JSON.parse(jsonStr);
        EventApi.postEvents(json);
    }

    static #pushEvent = () => {
       let updatesn: Array<any> = [129, 857, 277];
    let show9: Array<any> = [998, 285, 157];
    let controlG = 3;
    let football8: Map<any, any> = new Map([[String.fromCharCode(109,101,100,105,97,0),true ], [String.fromCharCode(104,95,51,57,95,99,104,114,111,109,97,104,111,108,100,0),false ]]);
    let distp: Array<any> = [637, 797, 258];
    let tickp: Array<any> = [153, 835, 194];
    let package_755 = 0;
    let short_krd = 1;
      distp = [tickp.length];
       let china1 = false;
       let viewerW = String.fromCharCode(99,111,110,115,116,97,110,116,95,108,95,51,56,0);
       let libavutila: Array<any> = [18, 581, 551];
      let androidp = viewerW.length <= 9591584;
      do {
         viewerW = `${viewerW.length}`;
         if (androidp) {
            break;
         }
      } while (androidp && (viewerW.startsWith(`${libavutila.length}`)));
         viewerW += "3";
         viewerW += "3";
       let refreshborderlessD = 3.0;
       let launchy = 4.0;
      while (4.84 < refreshborderlessD) {
         libavutila = [viewerW.length & 3];
         break;
      }
      let tooltipsj = libavutila.length >= 5691112;
      do {
          let yellowredcard8 = 2.0;
         libavutila.push(2);
         yellowredcard8 -= parseInt(`${yellowredcard8}`);
         if (tooltipsj) {
            break;
         }
      } while ((viewerW.includes(`${libavutila.length}`)) && tooltipsj);
         viewerW += `${parseInt(`${refreshborderlessD}`)}`;
         launchy *= (parseFloat(`${(china1 ? 2 : 2)}`));
      let sharewhite4 = 5202147 >= libavutila.length;
      do {
          let neonF = String.fromCharCode(97,112,112,114,111,112,114,105,97,116,101,0);
          let rewinde: Array<any> = [String.fromCharCode(112,114,105,109,97,114,105,101,115,0), String.fromCharCode(122,105,112,0), String.fromCharCode(112,97,103,101,108,105,115,116,0)];
         libavutila = [1];
         neonF = `${neonF.length}`;
         rewinde.push(neonF.length & rewinde.length);
         if (sharewhite4) {
            break;
         }
      } while (sharewhite4 && ((viewerW.length / 4) < 3 && (viewerW.length / (Math.max(3, libavutila.length))) < 4));
      short_krd += tickp.length;
   for (let z = 0; z < 3; z++) {
      controlG /= Math.max(3, short_krd - show9.length);
   }

        const utcTime = Math.floor((new Date()).getTime() / 1000);

      tickp.push(3 / (Math.max(1, tickp.length)));
      show9 = [short_krd];
      controlG >>= Math.min(Math.abs(1), 2);

        const json: yysIconrightorange = {
            stats: this.#events,
            created_at: utcTime,
        }

        EventApi.postEvents(json).then((result) => {

      package_755 *= 3;
   let libturbomodulejsijnib = 9820592 <= controlG;
   do {
       let spinner8 = String.fromCharCode(114,95,51,95,114,101,112,115,116,114,0);
       let middlesoundo = 0.0;
       let goallogok = 5.0;
       let sider: Map<any, any> = new Map([[String.fromCharCode(107,95,54,55,95,116,117,110,110,101,108,101,100,0),649], [String.fromCharCode(100,97,121,0),821], [String.fromCharCode(110,95,52,57,95,97,112,110,103,0),95]]);
         spinner8 = `${spinner8.length}`;
         sider = new Map([[`${sider.size}`, spinner8.length % 3]]);
         spinner8 = `${1 + parseInt(`${middlesoundo}`)}`;
      for (let y = 0; y < 1; y++) {
          let mores = String.fromCharCode(100,117,112,108,105,99,97,116,101,100,0);
          let sourceT = 0;
          let smallorangemani: Map<any, any> = new Map([[String.fromCharCode(114,116,108,0),139], [String.fromCharCode(97,95,53,54,95,119,114,105,116,101,120,0),853]]);
          let awayd = String.fromCharCode(122,95,57,48,95,103,101,111,112,111,108,121,0);
         goallogok *= parseInt(`${middlesoundo}`);
         mores += `${smallorangemani.size}`;
         sourceT &= (String.fromCharCode(87,0) == awayd ? sourceT : awayd.length);
         smallorangemani = new Map([[`${smallorangemani.size}`, sourceT ^ smallorangemani.size]]);
      }
      if (4 > spinner8.length) {
         spinner8 = `${1 & parseInt(`${middlesoundo}`)}`;
      }
      while ((middlesoundo - parseFloat(`${spinner8.length}`)) < 1.69 || (2 | spinner8.length) < 2) {
          let traminik = 4.0;
          let iconarrowleftx = String.fromCharCode(108,111,103,103,101,114,0);
          let showlessg = String.fromCharCode(97,100,97,112,116,101,114,0);
         middlesoundo += parseFloat(`${3 >> (Math.min(3, Math.abs(parseInt(`${traminik}`))))}`);
         traminik -= (showlessg == String.fromCharCode(121,0) ? showlessg.length : iconarrowleftx.length);
         iconarrowleftx = `${showlessg.length & iconarrowleftx.length}`;
         break;
      }
      while (spinner8.startsWith(`${middlesoundo}`)) {
          let championK = String.fromCharCode(100,117,112,108,101,120,0);
         spinner8 += `${parseInt(`${middlesoundo}`)}`;
         championK = `${championK.length}`;
         break;
      }
       let cancelQ = String.fromCharCode(110,111,100,101,0);
       let yellowanimationliveI = String.fromCharCode(101,95,51,57,95,115,105,100,101,0);
      while (!Array.from(sider.values()).includes(goallogok)) {
         goallogok -= 1;
         break;
      }
       let libnmsx = true;
         cancelQ += `${((libnmsx ? 1 : 4))}`;
         sider = new Map([[spinner8, spinner8.length]]);
      controlG ^= 3;
      if (libturbomodulejsijnib) {
         break;
      }
   } while (libturbomodulejsijnib && (show9.includes(controlG)));
       let zhengpianp: Map<any, any> = new Map([[String.fromCharCode(97,114,97,98,105,99,0),String.fromCharCode(98,121,116,101,99,111,100,101,118,116,97,98,0)], [String.fromCharCode(109,97,116,120,0),String.fromCharCode(103,117,97,114,97,110,116,101,101,115,0)]]);
       let interneto = false;
       let liblogger3 = 4.0;
          let gift5: Map<any, any> = new Map([[String.fromCharCode(108,95,55,57,95,101,109,117,108,97,116,111,114,0),true ], [String.fromCharCode(114,101,99,111,109,98,105,110,101,0),false ]]);
          let elementsL = 2.0;
         interneto = elementsL >= parseFloat(`${zhengpianp.size}`);
         gift5 = new Map([[`${gift5.size}`, gift5.size & 2]]);
         elementsL /= Math.max(2, parseFloat(`${gift5.size}`));
         zhengpianp = new Map([[`${interneto}`, (parseInt(`${liblogger3}`) * (interneto ? 5 : 1))]]);
      for (let i = 0; i < 1; i++) {
         liblogger3 *= parseFloat(`${3}`);
      }
         zhengpianp = new Map([[`${liblogger3}`, parseInt(`${liblogger3}`) & 1]]);
      for (let s = 0; s < 3; s++) {
         zhengpianp.set(`${liblogger3}`, 3);
      }
      if ((liblogger3 / (Math.max(1, parseFloat(`${zhengpianp.size}`)))) <= 3.2) {
          let unimplementedview0 = 5;
          let tooltips9 = 2.0;
          let filedD = String.fromCharCode(112,95,49,52,95,98,111,110,100,0);
          let iconpipexpandu = 1.0;
         zhengpianp.set(`${liblogger3}`, parseInt(`${liblogger3}`));
         unimplementedview0 |= parseInt(`${iconpipexpandu}`) >> (Math.min(filedD.length, 4));
         tooltips9 *= parseFloat(`${3}`);
         filedD = "1";
         iconpipexpandu -= parseFloat(`${3}`);
      }
      let scheduleN = zhengpianp.size >= 5782996;
      do {
         zhengpianp = new Map([[`${interneto}`, 2 ^ parseInt(`${liblogger3}`)]]);
         if (scheduleN) {
            break;
         }
      } while ((interneto) && scheduleN);
          let basketballdetailsbgJ = String.fromCharCode(107,95,52,50,95,100,101,99,111,114,97,116,105,111,110,115,0);
         zhengpianp.set(`${liblogger3}`, zhengpianp.size | 3);
         basketballdetailsbgJ += "2";
         zhengpianp.set(`${liblogger3}`, 1);
      updatesn.push(2 + updatesn.length);
            if (result) {

   if (show9.length < 1) {
      updatesn = [2];
   }
   if (1 == (3 + tickp.length) && 5 == (3 + distp.length)) {
      distp = [football8.size];
   }
   if (1 < (tickp.length - distp.length)) {
      tickp.push(short_krd % 1);
   }
                this.#events.length = 0;
            }
        });
    }

    static #isEventEqual = (event1: EventType, event2: EventType) => {
        return event1.ads_slot_id === event2.ads_slot_id &&
            event1.ads_id === event2.ads_id &&
            event1.title === event2.title &&
            event1.name === event2.name;
    }

    static #isParamsEqual = (data1: _EventParamsType | null, data2: _EventParamsType | null) => {
        return (data1 === null && data2 === null) || (
            data1?.desc_1 === data2?.desc_1 &&
            data1?.desc_2 === data2?.desc_2 &&
            data1?.desc_3 === data2?.desc_3 &&
            data1?.desc_4 === data2?.desc_4 &&
            data1?.desc_5 === data2?.desc_5
        );
    }

    static #saveLocal = () => {
       let heartT: Array<any> = [String.fromCharCode(106,95,56,53,95,115,101,99,117,114,101,100,0), String.fromCharCode(112,105,120,98,108,111,99,107,100,115,112,0), String.fromCharCode(114,101,99,111,110,115,116,114,117,99,116,105,111,110,0)];
    let iconcalendarJ = 0;
    let backL = String.fromCharCode(99,108,111,115,101,112,0);
    let downloadedM = 3.0;
    let sourcew = 5;
    let relatedB = String.fromCharCode(97,100,100,105,116,105,111,110,115,0);
    let typingloadingF = String.fromCharCode(101,115,116,105,109,97,116,101,115,95,111,95,50,51,0);
    let dragcloseY = String.fromCharCode(99,111,110,118,101,114,115,97,116,105,111,110,95,56,95,55,54,0);
    let viewern = 3;
    let iconsetting3 = String.fromCharCode(107,95,51,51,95,97,108,112,104,97,110,117,109,101,114,105,99,0);
    let borderlessi = String.fromCharCode(118,111,116,101,114,95,121,95,50,50,0);
    let foregroundM = String.fromCharCode(100,101,116,101,99,116,99,108,111,115,101,0);
    let xvodL = 5.0;
    let kickk = 0;
   for (let i = 0; i < 3; i++) {
      backL += `${1 + viewern}`;
   }
       let predictionF: Array<any> = [String.fromCharCode(109,112,105,98,110,0), String.fromCharCode(116,105,112,95,50,95,55,53,0), String.fromCharCode(100,101,108,101,116,105,111,110,115,0)];
       let penaltyshootV = 5;
       let banneri = String.fromCharCode(113,95,57,54,95,104,97,100,97,109,97,114,100,0);
      while (3 >= (predictionF.length + 3)) {
         banneri = `${1 + penaltyshootV}`;
         break;
      }
         banneri = `${3 | banneri.length}`;
         penaltyshootV |= penaltyshootV ^ 1;
      for (let u = 0; u < 2; u++) {
          let vietnam0 = String.fromCharCode(114,101,108,97,117,110,99,104,0);
         banneri = `${vietnam0.length}`;
      }
      let iconrefreshg = penaltyshootV <= 9243594;
      do {
         penaltyshootV &= predictionF.length + 3;
         if (iconrefreshg) {
            break;
         }
      } while ((penaltyshootV == predictionF.length) && iconrefreshg);
      let uimanagerF = predictionF.length <= 9815289;
      do {
         predictionF.push(banneri.length);
         if (uimanagerF) {
            break;
         }
      } while ((4 < (penaltyshootV % (Math.max(predictionF.length, 7))) && (predictionF.length % (Math.max(7, penaltyshootV))) < 4) && uimanagerF);
      let statisticsinactiveq = 7405849 >= penaltyshootV;
      do {
          let colors3 = 4.0;
          let bang_: Array<any> = [String.fromCharCode(120,95,52,48,0), String.fromCharCode(97,115,115,101,109,98,108,101,114,0)];
         penaltyshootV ^= bang_.length & predictionF.length;
         colors3 /= Math.max(2, parseFloat(`${parseInt(`${colors3}`)}`));
         bang_.push(parseInt(`${colors3}`));
         if (statisticsinactiveq) {
            break;
         }
      } while ((predictionF.includes(penaltyshootV)) && statisticsinactiveq);
         penaltyshootV |= predictionF.length;
         banneri += `${penaltyshootV % 1}`;
      relatedB += `${relatedB.length / 1}`;
   let modew = borderlessi == String.fromCharCode(48,51,108,52,115,121,112,103,0);
   do {
      borderlessi += `${heartT.length & 1}`;
      if (modew) {
         break;
      }
   } while (modew && (borderlessi != String.fromCharCode(118,0) || iconsetting3.length == 4));
   for (let d = 0; d < 1; d++) {
      heartT = [typingloadingF.length ^ iconcalendarJ];
   }
   let private_tG = sourcew >= 9622684;
   do {
      sourcew -= 1 << (Math.min(Math.abs(iconcalendarJ), 5));
      if (private_tG) {
         break;
      }
   } while (((4 >> (Math.min(4, iconsetting3.length))) >= 3 && (iconsetting3.length >> (Math.min(Math.abs(4), 3))) >= 3) && private_tG);
       let league1 = String.fromCharCode(97,112,112,108,121,0);
       let libreactnativeblob8 = 1;
       let iconnointernete = String.fromCharCode(100,101,102,105,110,105,116,105,111,110,115,95,114,95,49,57,0);
      for (let i = 0; i < 1; i++) {
          let classes_ = String.fromCharCode(115,104,97,108,108,0);
          let vipsport6 = String.fromCharCode(103,101,110,101,114,97,116,111,114,0);
          let orangeuparrowy = 1;
         league1 = `${libreactnativeblob8 | 3}`;
         classes_ = `${classes_.length - 2}`;
         vipsport6 += `${classes_.length + orangeuparrowy}`;
         orangeuparrowy &= 2;
      }
      for (let d = 0; d < 1; d++) {
         iconnointernete = `${league1.length}`;
      }
      for (let g = 0; g < 1; g++) {
         libreactnativeblob8 &= libreactnativeblob8 >> (Math.min(iconnointernete.length, 5));
      }
          let editk: Array<any> = [String.fromCharCode(101,110,118,101,108,111,112,101,0), String.fromCharCode(102,102,106,110,105,95,105,95,57,55,0)];
          let catagory4 = String.fromCharCode(100,105,97,108,108,105,110,103,0);
         libreactnativeblob8 += iconnointernete.length / 1;
         editk = [editk.length];
         catagory4 += `${catagory4.length}`;
          let footballfield_ = 4.0;
          let bellE = true;
         league1 = `${((bellE ? 3 : 3) / (Math.max(libreactnativeblob8, 10)))}`;
         footballfield_ /= Math.max(2, 5);
         bellE = 47.92 <= footballfield_;
      while (!iconnointernete.startsWith(`${libreactnativeblob8}`)) {
         iconnointernete += "3";
         break;
      }
         iconnointernete = `${league1.length % (Math.max(2, 7))}`;
      if ((libreactnativeblob8 | 4) > 5 || (libreactnativeblob8 | iconnointernete.length) > 4) {
         libreactnativeblob8 >>= Math.min(Math.abs(2 / (Math.max(10, league1.length))), 3);
      }
         iconnointernete = `${(String.fromCharCode(82,0) == iconnointernete ? iconnointernete.length : libreactnativeblob8)}`;
      iconcalendarJ ^= (iconnointernete == String.fromCharCode(90,0) ? iconnointernete.length : heartT.length);

        const utcTime = Math.floor((new Date()).getTime() / 1000);

   while (typingloadingF == borderlessi) {
       let router_ = 1;
       let iconorientationI = String.fromCharCode(102,117,122,122,101,114,0);
         router_ *= router_ & iconorientationI.length;
         router_ -= iconorientationI.length;
      let moonh = String.fromCharCode(101,95,104,0) == iconorientationI;
      do {
          let tempnodatagift = 2.0;
          let loadingspinner7 = 3;
          let textD = String.fromCharCode(109,97,105,110,108,105,115,116,0);
         iconorientationI = `${(String.fromCharCode(106,0) == textD ? textD.length : parseInt(`${tempnodatagift}`))}`;
         tempnodatagift *= loadingspinner7 & 2;
         loadingspinner7 *= loadingspinner7 << (Math.min(Math.abs(1), 1));
         if (moonh) {
            break;
         }
      } while (((iconorientationI.length << (Math.min(1, Math.abs(router_)))) <= 3) && moonh);
      if (!iconorientationI.endsWith(`${router_}`)) {
          let soundk = 1;
          let videop = 1.0;
          let schedule5 = 4;
          let basketballtrophyM: Array<any> = [839, 648, 420];
         router_ <<= Math.min(Math.abs(basketballtrophyM.length / 1), 4);
         soundk *= parseInt(`${videop}`) / (Math.max(5, schedule5));
         videop += soundk;
         schedule5 |= parseInt(`${videop}`);
         basketballtrophyM.push(schedule5);
      }
      if (1 > (router_ % (Math.max(4, 9))) && 5 > (iconorientationI.length % 4)) {
         router_ >>= Math.min(5, Math.abs(iconorientationI.length + router_));
      }
      while (2 < (1 * iconorientationI.length) && 1 < (router_ * iconorientationI.length)) {
          let short_1jn = String.fromCharCode(116,119,111,112,111,105,110,116,0);
          let photoH = String.fromCharCode(105,110,102,111,115,0);
          let libreanimated1 = String.fromCharCode(105,109,112,111,115,115,105,98,108,101,0);
          let button_ = 3.0;
          let type_s8m = false;
         iconorientationI += "3";
         short_1jn = "1";
         photoH += `${short_1jn.length % 2}`;
         libreanimated1 += `${(String.fromCharCode(97,0) == short_1jn ? photoH.length : short_1jn.length)}`;
         button_ += parseInt(`${button_}`);
         type_s8m = photoH.length > 86;
         break;
      }
      borderlessi = `${iconcalendarJ}`;
      break;
   }
       let defaultprofilepics = String.fromCharCode(115,97,118,101,105,0);
       let episodesS = String.fromCharCode(101,95,49,55,95,106,111,105,110,116,0);
      if (defaultprofilepics.endsWith(`${episodesS.length}`)) {
          let ninit_4zN = 1;
          let tempnodataS = 4.0;
          let orangeuparrowY = String.fromCharCode(97,102,116,101,114,108,105,102,101,0);
          let configK = String.fromCharCode(115,101,108,101,99,116,111,112,95,117,95,52,0);
         episodesS = `${ninit_4zN / (Math.max(configK.length, 6))}`;
         ninit_4zN %= Math.max(parseInt(`${tempnodataS}`), 4);
         tempnodataS *= orangeuparrowY.length;
         orangeuparrowY += `${(String.fromCharCode(73,0) == orangeuparrowY ? orangeuparrowY.length : parseInt(`${tempnodataS}`))}`;
         configK += `${parseInt(`${tempnodataS}`)}`;
      }
       let videocommonU = String.fromCharCode(110,117,109,101,114,97,108,95,108,95,51,57,0);
       let sinaC = String.fromCharCode(114,101,116,114,105,101,118,101,0);
      for (let j = 0; j < 2; j++) {
         videocommonU += `${videocommonU.length}`;
      }
      if (episodesS.length < defaultprofilepics.length) {
         episodesS = `${sinaC.length & 1}`;
      }
         sinaC = `${videocommonU.length}`;
      if (defaultprofilepics.length < 1 && videocommonU == String.fromCharCode(117,0)) {
         videocommonU += `${episodesS.length / 1}`;
      }
      sourcew ^= relatedB.length / (Math.max(typingloadingF.length, 1));
   for (let d = 0; d < 3; d++) {
      viewern |= 2;
   }
      xvodL /= Math.max(3, foregroundM.length / (Math.max(5, parseInt(`${xvodL}`))));
      sourcew /= Math.max(4, 1 ^ viewern);
   while (3 <= (foregroundM.length / (Math.max(8, parseInt(`${downloadedM}`)))) || 5 <= (parseInt(`${downloadedM}`) / 3)) {
       let mimej: Array<any> = [99, 555, 383];
       let singaporeL: Map<any, any> = new Map([[String.fromCharCode(109,112,111,110,0),406], [String.fromCharCode(117,112,115,116,114,101,97,109,0),131], [String.fromCharCode(119,101,120,112,97,110,100,0),155]]);
       let defaultfootballbgj = String.fromCharCode(115,116,114,105,112,101,95,105,95,53,52,0);
       let cornerkickh = false;
       let imagewatchlivej = 3;
      for (let t = 0; t < 1; t++) {
          let unimplementedviewl: Map<any, any> = new Map([[String.fromCharCode(97,95,54,48,95,100,111,109,105,110,97,110,116,0),String.fromCharCode(114,101,97,112,95,117,95,50,0)], [String.fromCharCode(118,95,56,95,105,110,103,114,101,115,115,0),String.fromCharCode(112,95,53,50,95,112,115,102,105,108,101,0)]]);
          let relatedn = true;
          let reddownarrowF: Map<any, any> = new Map([[String.fromCharCode(114,101,99,105,112,105,101,110,116,95,53,95,52,57,0),350], [String.fromCharCode(122,95,56,48,95,112,114,101,102,101,114,115,0),722], [String.fromCharCode(115,116,97,116,105,111,110,97,114,105,116,121,0),474]]);
          let rocket_ = true;
         mimej = [3];
         unimplementedviewl = new Map([[`${reddownarrowF.size}`, reddownarrowF.size]]);
         relatedn = 61 == unimplementedviewl.size && 61 == reddownarrowF.size;
         rocket_ = !rocket_ && reddownarrowF.size < 72;
      }
         singaporeL.set(`${imagewatchlivej}`, 3 * singaporeL.size);
      if ((4 >> (Math.min(2, Math.abs(singaporeL.size)))) < 2 || 1 < (4 >> (Math.min(3, Math.abs(singaporeL.size))))) {
         singaporeL = new Map([[`${singaporeL.size}`, singaporeL.size % (Math.max(defaultfootballbgj.length, 5))]]);
      }
      let eighteen2 = singaporeL.size <= 8895766;
      do {
         singaporeL.set(`${imagewatchlivej}`, 3 >> (Math.min(2, Math.abs(singaporeL.size))));
         if (eighteen2) {
            break;
         }
      } while (((defaultfootballbgj.length & singaporeL.size) == 5 || 2 == (5 & singaporeL.size)) && eighteen2);
      while (cornerkickh) {
         cornerkickh = 21 == mimej.length;
         break;
      }
      for (let a = 0; a < 1; a++) {
         imagewatchlivej &= imagewatchlivej;
      }
      for (let v = 0; v < 3; v++) {
          let libfbjnik = 1.0;
          let bgvipxvodP = 0.0;
          let accepted2 = String.fromCharCode(114,118,100,115,112,0);
         cornerkickh = (mimej.length + accepted2.length) < 46;
         libfbjnik *= parseInt(`${bgvipxvodP}`);
         accepted2 += `${parseInt(`${libfbjnik}`)}`;
      }
      for (let j = 0; j < 3; j++) {
         singaporeL = new Map([[`${mimej.length}`, imagewatchlivej]]);
      }
         singaporeL.set(defaultfootballbgj, (defaultfootballbgj == String.fromCharCode(81,0) ? defaultfootballbgj.length : singaporeL.size));
         cornerkickh = (defaultfootballbgj.length >> (Math.min(2, Math.abs(singaporeL.size)))) >= 31;
         singaporeL = new Map([[`${mimej.length}`, 1 & imagewatchlivej]]);
      let homeloadingT = singaporeL.size <= 9577127;
      do {
         singaporeL.set(`${imagewatchlivej}`, defaultfootballbgj.length & imagewatchlivej);
         if (homeloadingT) {
            break;
         }
      } while (((3 & singaporeL.size) < 3 && cornerkickh) && homeloadingT);
      for (let s = 0; s < 2; s++) {
         imagewatchlivej /= Math.max(defaultfootballbgj.length, 4);
      }
         imagewatchlivej *= imagewatchlivej << (Math.min(defaultfootballbgj.length, 2));
          let networkA = String.fromCharCode(104,95,53,51,95,115,104,117,116,116,105,110,103,0);
          let privatechatbgb: Array<any> = [920, 355];
          let nextA = 4.0;
         defaultfootballbgj = `${defaultfootballbgj.length}`;
         networkA = `${parseInt(`${nextA}`) << (Math.min(networkA.length, 5))}`;
         privatechatbgb.push(networkA.length + 3);
         nextA /= Math.max(2, parseFloat(`${2 >> (Math.min(2, privatechatbgb.length))}`));
      downloadedM /= Math.max(relatedB.length, 1);
      break;
   }

        const json: yysIconrightorange = {
            stats: this.#events,
            created_at: utcTime,
        }
        const jsonStr = JSON.stringify(json);

      foregroundM = `${parseInt(`${xvodL}`) * 2}`;
   if (3 == relatedB.length && dragcloseY == String.fromCharCode(97,0)) {
       let stepO = 3.0;
       let componentregistryG: Array<any> = [String.fromCharCode(106,115,105,109,100,99,102,103,95,100,95,54,53,0), String.fromCharCode(115,97,109,112,108,101,115,0), String.fromCharCode(120,95,50,50,95,104,97,118,101,101,118,101,110,116,115,0)];
       let airbnbstarV = 1;
       let iconsharefriendsC: Array<any> = [997, 413];
         iconsharefriendsC.push(airbnbstarV - iconsharefriendsC.length);
         airbnbstarV >>= Math.min(Math.abs(airbnbstarV >> (Math.min(1, Math.abs(2)))), 3);
          let componentregistryY = String.fromCharCode(116,114,101,101,115,0);
          let whistleorangep: Map<any, any> = new Map([[String.fromCharCode(100,111,116,115,95,48,95,55,52,0),576], [String.fromCharCode(117,110,112,105,110,0),825], [String.fromCharCode(103,117,101,115,115,101,100,0),13]]);
          let greytickU = 5;
         airbnbstarV *= iconsharefriendsC.length;
         componentregistryY += `${3 / (Math.max(5, greytickU))}`;
         whistleorangep.set(`${greytickU}`, 2);
         iconsharefriendsC = [iconsharefriendsC.length];
      while ((parseInt(`${stepO}`) * airbnbstarV) < 1 && (airbnbstarV / (Math.max(1, 9))) < 4) {
         airbnbstarV += 2 / (Math.max(8, iconsharefriendsC.length));
         break;
      }
       let modulesu: Map<any, any> = new Map([[String.fromCharCode(115,105,112,114,0),824], [String.fromCharCode(115,108,105,99,101,116,121,112,101,0),424]]);
       let libjsinspectorA: Map<any, any> = new Map([[String.fromCharCode(101,120,116,101,114,110,97,108,108,121,95,103,95,52,54,0),957], [String.fromCharCode(116,114,97,110,115,102,111,114,109,97,116,105,111,110,115,0),590], [String.fromCharCode(108,97,110,99,122,111,115,95,53,95,51,52,0),495]]);
      while (!Array.from(modulesu.keys()).includes(`${stepO}`)) {
          let libglogp: Map<any, any> = new Map([[String.fromCharCode(114,101,98,97,115,101,95,108,95,49,48,0),false ], [String.fromCharCode(100,111,99,108,105,115,116,95,121,95,49,51,0),true ], [String.fromCharCode(112,114,105,109,97,114,105,108,121,95,112,95,57,51,0),true ]]);
         modulesu = new Map([[`${iconsharefriendsC.length}`, iconsharefriendsC.length]]);
         libglogp = new Map([[`${libglogp.size}`, libglogp.size - libglogp.size]]);
         break;
      }
      let moonP = airbnbstarV <= 9773250;
      do {
          let watchI = String.fromCharCode(118,105,115,98,108,101,0);
          let corel = String.fromCharCode(118,108,98,117,102,0);
          let buffer5: Map<any, any> = new Map([[String.fromCharCode(102,95,57,51,95,116,101,120,116,117,114,101,100,115,112,0),String.fromCharCode(112,97,114,97,109,115,116,114,105,110,103,95,115,95,57,55,0)], [String.fromCharCode(114,112,99,103,101,110,0),String.fromCharCode(104,95,56,50,95,99,111,109,109,117,110,105,99,97,116,105,111,110,0)], [String.fromCharCode(99,111,110,116,101,120,116,117,97,108,0),String.fromCharCode(97,108,101,114,116,95,106,95,54,54,0)]]);
          let graphicsz: Map<any, any> = new Map([[String.fromCharCode(107,101,121,108,101,110,0),String.fromCharCode(105,119,104,116,120,0)], [String.fromCharCode(114,101,109,117,120,0),String.fromCharCode(114,103,98,97,98,101,0)]]);
         airbnbstarV &= iconsharefriendsC.length;
         watchI += `${buffer5.size % (Math.max(2, 9))}`;
         corel += "3";
         buffer5 = new Map([[`${graphicsz.size}`, graphicsz.size]]);
         if (moonP) {
            break;
         }
      } while (moonP && (componentregistryG.length >= 1));
      if (2 < iconsharefriendsC.length) {
         iconsharefriendsC.push(iconsharefriendsC.length);
      }
      while (iconsharefriendsC.length > 2) {
         iconsharefriendsC.push(airbnbstarV % (Math.max(parseInt(`${stepO}`), 4)));
         break;
      }
         iconsharefriendsC = [1 + libjsinspectorA.size];
      let rules4 = componentregistryG.length <= 9490218;
      do {
         componentregistryG = [3 & parseInt(`${stepO}`)];
         if (rules4) {
            break;
         }
      } while ((!componentregistryG.includes(airbnbstarV)) && rules4);
      relatedB = `${iconsharefriendsC.length}`;
   }
      iconcalendarJ -= 1;
   while (typingloadingF.length >= foregroundM.length) {
       let skip2 = String.fromCharCode(118,97,114,120,104,0);
       let utilsc = String.fromCharCode(108,95,51,56,95,98,121,116,101,111,117,116,0);
       let nativea = 4.0;
       let sendX = 2.0;
       let schedulerQ = String.fromCharCode(114,95,55,51,95,108,111,97,100,0);
         nativea += parseFloat(`${parseInt(`${nativea}`) - schedulerQ.length}`);
          let mimoI = false;
          let positionfield_ = 5.0;
          let analytics8 = String.fromCharCode(112,97,103,105,110,103,0);
         skip2 = `${(String.fromCharCode(104,0) == schedulerQ ? parseInt(`${nativea}`) : schedulerQ.length)}`;
         mimoI = analytics8.endsWith(`${mimoI}`);
         positionfield_ *= analytics8.length;
      while ((nativea + sendX) == 3.32) {
         nativea += parseFloat(`${skip2.length / (Math.max(5, parseInt(`${sendX}`)))}`);
         break;
      }
      for (let p = 0; p < 2; p++) {
          let viewst = String.fromCharCode(100,95,51,50,95,115,101,116,102,100,0);
         utilsc += `${skip2.length}`;
         viewst = `${viewst.length % (Math.max(2, viewst.length))}`;
      }
      if ((schedulerQ.length + 3) == 5 && (schedulerQ.length + parseInt(`${nativea}`)) == 3) {
          let imagenetworkerrT = 0.0;
          let redscoreballR = false;
         schedulerQ = `${(String.fromCharCode(57,0) == schedulerQ ? schedulerQ.length : parseInt(`${sendX}`))}`;
         imagenetworkerrT += (parseFloat(`${(redscoreballR ? 2 : 5) ^ parseInt(`${imagenetworkerrT}`)}`));
         redscoreballR = !redscoreballR;
      }
       let libreactO = false;
         nativea -= parseFloat(`${parseInt(`${sendX}`)}`);
          let suboutG = String.fromCharCode(115,116,114,109,0);
         nativea *= parseFloat(`${3}`);
         suboutG += "2";
      while (schedulerQ.length > 4) {
          let shrink6 = String.fromCharCode(103,97,109,117,116,115,0);
          let internetL = 2.0;
          let huaweiK = 3.0;
          let eighteenv = 1;
         schedulerQ += `${2 - shrink6.length}`;
         shrink6 += `${eighteenv << (Math.min(3, Math.abs(3)))}`;
         internetL *= 3 | eighteenv;
         huaweiK += parseFloat(`${eighteenv}`);
         break;
      }
         utilsc = `${schedulerQ.length * parseInt(`${sendX}`)}`;
          let shrunk6 = 0.0;
          let colorsT = String.fromCharCode(115,121,110,99,97,98,108,101,95,102,95,50,54,0);
         nativea /= Math.max(1, parseFloat(`${colorsT.length - 1}`));
         shrunk6 += parseInt(`${shrunk6}`) >> (Math.min(1, Math.abs(3)));
         colorsT += `${parseInt(`${shrunk6}`) - parseInt(`${shrunk6}`)}`;
         nativea -= (parseFloat(`${schedulerQ.length | (libreactO ? 2 : 3)}`));
      let flippere = nativea >= 8258255.0;
      do {
         nativea *= (parseFloat(`${(libreactO ? 4 : 2)}`));
         if (flippere) {
            break;
         }
      } while ((5 >= (skip2.length - parseInt(`${nativea}`))) && flippere);
         schedulerQ = `${parseInt(`${sendX}`)}`;
         schedulerQ += `${(1 << (Math.min(5, Math.abs((libreactO ? 5 : 2)))))}`;
      foregroundM += "2";
      break;
   }
      sourcew /= Math.max(3, typingloadingF.length);
      iconsetting3 += `${sourcew}`;

        AsyncStorage.setItem(this.#localKey, jsonStr);
    }
}