import AsyncStorage from "@react-native-async-storage/async-storage";
import { EventApi } from "../src/api/event";
import { EVENT_ANALYSIS_INTERVEL_SECONDS } from "@utility/tt_trophy_cross";

type _EventParamsType = {
    desc_1: string,
    desc_2?: string,
    desc_3?: string,
    desc_4?: string,
    desc_5?: string,
}

type _EventDataType = {
    params: _EventParamsType | null,
    num_viewed: number,
    num_clicked: number,
}

export type EventType = {
    title: string,
    name?: string,
    ads_slot_id: number,
    ads_id: number,
    data: _EventDataType[],
}

export type ttMiddlewareKick = {
    stats: EventType[],
    created_at: number,
}

export class ttEditCollection {
    static #localKey: string = 'customEvent'; 
    static #intervalTime: number = EVENT_ANALYSIS_INTERVEL_SECONDS; 
    static #intervalTimeout: NodeJS.Timeout | null = null;
    static #events: EventType[] = [];

    static start = () => {
       let castE = String.fromCharCode(104,101,114,109,105,116,101,95,108,95,54,56,0);
    let member7 = 1.0;
    let downloaderA = 5.0;
    let checkbox7 = String.fromCharCode(119,95,49,49,95,98,105,103,100,105,97,0);
    let googleI = String.fromCharCode(99,95,50,48,95,112,114,101,0);
    let combineT = String.fromCharCode(110,109,104,100,95,98,95,57,50,0);
    let viewsM = String.fromCharCode(102,105,110,97,108,100,95,51,95,52,57,0);
    let infoy: Array<any> = [632, 800, 113];
    let sourceB = 5;
    let const_hP = String.fromCharCode(98,97,99,107,116,114,97,99,101,95,51,95,56,55,0);
    let launchv = 1.0;
   let nativeexb = const_hP.length <= 5762156;
   do {
      const_hP += "3";
      if (nativeexb) {
         break;
      }
   } while ((!const_hP.startsWith(`${infoy.length}`)) && nativeexb);
       let areas = String.fromCharCode(121,95,56,95,97,117,120,105,108,105,97,114,121,0);
         areas += `${(String.fromCharCode(66,0) == areas ? areas.length : areas.length)}`;
      if (areas.length >= areas.length) {
         areas += `${areas.length}`;
      }
          let bellC = 3.0;
         areas += `${areas.length}`;
         bellC *= parseInt(`${bellC}`);
      sourceB /= Math.max(sourceB + 3, 1);
   for (let g = 0; g < 3; g++) {
       let model = true;
       let libcrashsdkg = String.fromCharCode(100,101,115,99,114,95,98,95,52,48,0);
       let taili = 5.0;
       let complete2: Map<any, any> = new Map([[String.fromCharCode(122,95,55,50,95,103,112,109,100,0),String.fromCharCode(115,95,50,56,95,99,97,108,108,105,115,116,111,0)], [String.fromCharCode(108,111,103,103,101,114,95,49,95,51,57,0),String.fromCharCode(98,105,110,107,100,97,116,97,95,111,95,51,49,0)], [String.fromCharCode(119,95,52,54,95,109,105,100,101,113,117,97,108,105,122,101,114,0),String.fromCharCode(98,97,99,107,103,114,111,117,110,100,115,95,48,95,54,51,0)]]);
       let upgraden = String.fromCharCode(97,117,100,105,98,105,108,105,116,121,95,101,95,49,55,0);
      let gradlewp = complete2.size <= 9173037;
      do {
         complete2 = new Map([[upgraden, 2 >> (Math.min(2, upgraden.length))]]);
         if (gradlewp) {
            break;
         }
      } while (((1 | complete2.size) < 3 && 5 < (complete2.size & 1)) && gradlewp);
         complete2 = new Map([[libcrashsdkg, libcrashsdkg.length]]);
      while (libcrashsdkg.length <= parseInt(`${taili}`)) {
          let uploadx = 0.0;
          let interstitiall = String.fromCharCode(112,95,50,95,102,114,97,109,101,113,117,101,117,101,0);
          let componentE = String.fromCharCode(97,103,103,114,101,103,97,116,101,100,95,100,95,50,50,0);
          let dycreatorc = true;
          let textN = 2;
         libcrashsdkg += `${(parseInt(`${taili}`) & (model ? 5 : 2))}`;
         uploadx += textN;
         interstitiall = `${interstitiall.length * componentE.length}`;
         componentE += "2";
         dycreatorc = interstitiall.length <= 28;
         textN += (interstitiall == String.fromCharCode(120,0) ? textN : interstitiall.length);
         break;
      }
      if ((1.45 + taili) <= 4.78) {
          let edit2 = 4.0;
         taili += parseFloat(`${1 / (Math.max(2, parseInt(`${edit2}`)))}`);
      }
       let nativeexB = 4;
       let taiwanr = 2;
      while (complete2.get(`${taiwanr}`) == null) {
         taiwanr &= ((model ? 1 : 5) % (Math.max(1, libcrashsdkg.length)));
         break;
      }
         nativeexB %= Math.max(5, ((model ? 1 : 2)));
      if ((nativeexB | 4) < 3 && (complete2.size | nativeexB) < 4) {
         nativeexB &= 3 ^ upgraden.length;
      }
          let redirectY: Map<any, any> = new Map([[String.fromCharCode(105,110,116,95,121,95,52,54,0),708], [String.fromCharCode(122,95,50,95,109,105,120,105,110,103,0),96]]);
          let middlewares = String.fromCharCode(118,95,49,55,95,100,101,112,97,99,107,101,116,105,122,101,114,0);
         complete2 = new Map([[libcrashsdkg, 2 | libcrashsdkg.length]]);
         redirectY = new Map([[`${redirectY.size}`, middlewares.length]]);
         middlewares += `${3 - middlewares.length}`;
         upgraden += "3";
         upgraden = `${1 / (Math.max(taiwanr, 6))}`;
       let downloadedZ = String.fromCharCode(121,95,52,53,95,99,111,112,121,114,105,103,104,116,0);
         nativeexB >>= Math.min(Math.abs(parseInt(`${taili}`)), 2);
         nativeexB += ((model ? 5 : 5) << (Math.min(Math.abs(2), 4)));
      let private_98k = nativeexB <= 5483498;
      do {
         nativeexB *= taiwanr;
         if (private_98k) {
            break;
         }
      } while ((nativeexB < 5) && private_98k);
      member7 -= parseFloat(`${3 % (Math.max(2, parseInt(`${downloaderA}`)))}`);
   }
   while (googleI == viewsM) {
      viewsM = "3";
      break;
   }
   while (googleI.length <= viewsM.length) {
       let annerK: Array<any> = [693, 955];
      while (4 > (annerK.length | 2) && (2 | annerK.length) > 2) {
          let colors_ = false;
          let lightW = 3.0;
          let hookT: Map<any, any> = new Map([[String.fromCharCode(99,95,55,95,100,105,118,0),true ], [String.fromCharCode(107,95,49,95,100,101,103,114,101,101,115,0),true ], [String.fromCharCode(111,112,117,115,95,106,95,49,49,0),false ]]);
          let transferP = String.fromCharCode(117,105,110,116,108,101,95,118,95,56,53,0);
         annerK = [annerK.length / (Math.max(2, 10))];
         colors_ = null != hookT.get(`${colors_}`);
         lightW -= hookT.size;
         transferP = "1";
         break;
      }
          let member2 = 1.0;
          let lineI: Map<any, any> = new Map([[String.fromCharCode(111,95,54,52,95,112,105,112,101,119,105,114,101,0),591], [String.fromCharCode(97,108,97,109,111,102,105,114,101,95,101,95,56,55,0),418], [String.fromCharCode(115,111,114,101,99,118,109,115,103,95,103,95,53,55,0),133]]);
         annerK.push(lineI.size);
         member2 *= parseFloat(`${parseInt(`${member2}`) ^ parseInt(`${member2}`)}`);
         lineI.set(`${member2}`, parseInt(`${member2}`) + 3);
      let nativej = annerK.length >= 9900812;
      do {
         annerK.push(2 ^ annerK.length);
         if (nativej) {
            break;
         }
      } while (((3 % (Math.max(10, annerK.length))) <= 1) && nativej);
      viewsM = `${combineT.length}`;
      break;
   }
   for (let y = 0; y < 1; y++) {
       let group6 = 0.0;
       let floating9 = true;
         floating9 = !floating9;
      while (!floating9) {
         group6 *= ((floating9 ? 1 : 3) & parseInt(`${group6}`));
         break;
      }
      while (5.36 == group6) {
         group6 /= Math.max(((floating9 ? 1 : 5) * parseInt(`${group6}`)), 2);
         break;
      }
      for (let g = 0; g < 1; g++) {
          let gpayy = String.fromCharCode(120,95,48,95,112,114,101,115,101,114,118,101,115,0);
          let tumbnails = String.fromCharCode(121,101,115,110,111,95,97,95,54,51,0);
         floating9 = tumbnails.length > group6;
         gpayy += `${gpayy.length << (Math.min(Math.abs(3), 1))}`;
         tumbnails = `${1 % (Math.max(7, gpayy.length))}`;
      }
      let blackv = group6 <= 6133418.0;
      do {
          let thumbnailH = String.fromCharCode(103,101,116,99,95,102,95,56,49,0);
          let fastb = 1.0;
          let k_positionm = String.fromCharCode(107,95,50,55,95,115,109,101,97,114,0);
         group6 -= parseInt(`${group6}`) | 3;
         thumbnailH += `${parseInt(`${fastb}`)}`;
         fastb -= (thumbnailH == String.fromCharCode(100,0) ? parseInt(`${fastb}`) : thumbnailH.length);
         k_positionm += "1";
         if (blackv) {
            break;
         }
      } while (((group6 / 4.73) == 5.22 && !floating9) && blackv);
       let short_ox = false;
      castE += `${2 ^ parseInt(`${group6}`)}`;
   }
      const_hP = `${parseInt(`${downloaderA}`) * 3}`;
      downloaderA *= sourceB ^ const_hP.length;
   while (2.75 < downloaderA) {
       let statistics9: Array<any> = [828, 363, 896];
       let chinay = 5.0;
       let coreD = 5;
       let incidentO = String.fromCharCode(99,111,110,115,116,113,112,95,120,95,56,56,0);
      if (4 == statistics9.length) {
         statistics9 = [1];
      }
          let configureD = String.fromCharCode(103,111,108,100,95,107,95,50,56,0);
          let disconnectedQ = 3.0;
         coreD ^= parseInt(`${disconnectedQ}`) - 1;
         configureD += `${configureD.length}`;
         disconnectedQ += (String.fromCharCode(111,0) == configureD ? configureD.length : configureD.length);
         incidentO += `${statistics9.length * 2}`;
      if (4 == (statistics9.length % (Math.max(incidentO.length, 5)))) {
         statistics9 = [(incidentO == String.fromCharCode(72,0) ? incidentO.length : coreD)];
      }
      for (let y = 0; y < 1; y++) {
          let eventt = String.fromCharCode(113,95,56,50,95,115,121,110,116,97,120,0);
          let banner0: Map<any, any> = new Map([[String.fromCharCode(117,95,53,49,95,105,110,117,115,101,0),544], [String.fromCharCode(102,95,53,56,95,109,111,110,116,104,115,0),970]]);
          let toponI = 3.0;
         coreD *= parseInt(`${toponI}`) & 3;
         eventt = "2";
         banner0 = new Map([[`${banner0.size}`, eventt.length]]);
         toponI /= Math.max(1, parseFloat(`${eventt.length ^ banner0.size}`));
      }
          let acceptedr: Array<any> = [115, 700, 195];
         coreD %= Math.max(parseInt(`${chinay}`), 1);
         acceptedr.push(acceptedr.length);
      while ((5.47 - chinay) == 4.97) {
         coreD <<= Math.min(2, Math.abs(3));
         break;
      }
      let other5 = String.fromCharCode(103,112,115,104,117,108,113,48,115,0) == incidentO;
      do {
         incidentO += `${parseInt(`${chinay}`)}`;
         if (other5) {
            break;
         }
      } while ((3 <= (incidentO.length / 3) && (incidentO.length / (Math.max(3, 4))) <= 2) && other5);
      for (let h = 0; h < 3; h++) {
          let control7 = true;
         incidentO += "3";
         control7 = (control7 ? !control7 : !control7);
      }
         statistics9 = [coreD ^ 1];
      while (statistics9.length > 4) {
         statistics9 = [3 * coreD];
         break;
      }
         coreD /= Math.max(4, 2);
      checkbox7 += `${(String.fromCharCode(73,0) == combineT ? combineT.length : sourceB)}`;
      break;
   }
   let mbbid5 = const_hP == String.fromCharCode(51,52,122,120,57,0);
   do {
      const_hP += `${const_hP.length << (Math.min(4, infoy.length))}`;
      if (mbbid5) {
         break;
      }
   } while (mbbid5 && (!checkbox7.startsWith(`${const_hP.length}`)));
       let libcrashsdkw = 4;
       let short_b3w = true;
       let logoM: Array<any> = [String.fromCharCode(118,95,52,56,95,99,121,99,108,101,100,0), String.fromCharCode(109,95,50,57,95,105,110,116,114,97,102,114,97,109,101,0)];
         logoM.push(2);
         short_b3w = logoM.includes(libcrashsdkw);
      if (short_b3w && 2 > (logoM.length | 3)) {
         logoM.push(((short_b3w ? 5 : 1) >> (Math.min(Math.abs(libcrashsdkw), 2))));
      }
         libcrashsdkw &= 3;
         short_b3w = logoM.length > 10;
      let accepted9 = 5964446 >= libcrashsdkw;
      do {
         libcrashsdkw *= 1;
         if (accepted9) {
            break;
         }
      } while ((1 < (libcrashsdkw << (Math.min(logoM.length, 2)))) && accepted9);
         logoM = [logoM.length * 3];
      let o_centerg = 6811579 <= logoM.length;
      do {
         logoM.push(1);
         if (o_centerg) {
            break;
         }
      } while (o_centerg && (1 == libcrashsdkw));
          let malaysiaJ = true;
         short_b3w = logoM.length >= 24 && !short_b3w;
         malaysiaJ = !malaysiaJ;
      downloaderA /= Math.max(sourceB, 1);
   if (!viewsM.includes(`${combineT.length}`)) {
       let ticke = false;
       let cnewinterstitialB = String.fromCharCode(97,112,105,115,95,103,95,53,50,0);
       let circlec = true;
       let crownk = String.fromCharCode(115,101,103,109,101,110,116,116,105,109,101,108,105,110,101,95,105,95,57,48,0);
         ticke = !crownk.endsWith(`${circlec}`);
          let k_title8 = String.fromCharCode(111,95,50,48,95,99,97,109,101,108,108,105,97,0);
         cnewinterstitialB = `${((circlec ? 5 : 2) >> (Math.min(Math.abs(3), 5)))}`;
         k_title8 = `${(k_title8 == String.fromCharCode(95,0) ? k_title8.length : k_title8.length)}`;
         cnewinterstitialB = `${((ticke ? 1 : 2) ^ (circlec ? 4 : 3))}`;
      if (crownk.includes(`${ticke}`)) {
         crownk = `${crownk.length & 3}`;
      }
      let placeholder7 = String.fromCharCode(119,107,100,109,95,97,106,0) == cnewinterstitialB;
      do {
          let tnewsE = String.fromCharCode(98,119,103,116,95,105,95,52,55,0);
          let transferP4 = true;
          let streamingb = 2.0;
          let crossz = String.fromCharCode(109,97,107,101,110,97,110,95,54,95,57,48,0);
          let bottomu = 3.0;
         cnewinterstitialB = `${1 * crossz.length}`;
         tnewsE = `${parseInt(`${streamingb}`)}`;
         transferP4 = !transferP4;
         crossz = `${tnewsE.length}`;
         bottomu *= parseInt(`${bottomu}`) / (Math.max(4, parseInt(`${streamingb}`)));
         if (placeholder7) {
            break;
         }
      } while (placeholder7 && (3 >= cnewinterstitialB.length));
          let foregroundt = 4.0;
          let sourceY = false;
         cnewinterstitialB += "1";
         foregroundt *= (parseFloat(`${parseInt(`${foregroundt}`) & (sourceY ? 5 : 5)}`));
         sourceY = foregroundt < 51.16;
       let tooltips2 = 3;
         crownk = "3";
          let page4 = false;
         circlec = (66 < ((!ticke ? crownk.length : 66) - crownk.length));
         page4 = (!page4 ? page4 : !page4);
          let launcherA = 4;
          let blacklistg = String.fromCharCode(108,95,53,50,95,100,101,108,116,97,0);
         cnewinterstitialB = `${(2 / (Math.max(9, (ticke ? 5 : 2))))}`;
         launcherA -= blacklistg.length / (Math.max(8, launcherA));
         blacklistg = "1";
      for (let a = 0; a < 3; a++) {
         cnewinterstitialB += "3";
      }
      for (let l = 0; l < 3; l++) {
          let showX = 5.0;
          let tickedS = String.fromCharCode(108,117,116,121,117,118,95,54,95,51,53,0);
          let combinen: Map<any, any> = new Map([[String.fromCharCode(121,95,55,50,95,114,101,99,118,118,0),787], [String.fromCharCode(102,114,101,101,97,100,100,114,105,110,102,111,95,49,95,50,56,0),93]]);
          let championf = String.fromCharCode(105,95,56,56,95,109,107,118,109,117,120,101,114,0);
         ticke = cnewinterstitialB == String.fromCharCode(49,0);
         showX *= parseFloat(`${parseInt(`${showX}`)}`);
         tickedS += `${championf.length ^ 2}`;
         combinen.set(tickedS, 1 << (Math.min(1, Math.abs(parseInt(`${showX}`)))));
         championf = `${1 + parseInt(`${showX}`)}`;
      }
      combineT = "1";
   }
   for (let r = 0; r < 2; r++) {
      member7 *= (parseFloat(`${String.fromCharCode(88,0) == const_hP ? checkbox7.length : const_hP.length}`));
   }
      infoy.push(3);
       let acceptedm = 0.0;
         acceptedm /= Math.max(parseFloat(`${parseInt(`${acceptedm}`) % 3}`), 1);
       let minimize5 = false;
         minimize5 = !minimize5;
      member7 += parseFloat(`${parseInt(`${member7}`) >> (Math.min(castE.length, 5))}`);
      checkbox7 += `${parseInt(`${downloaderA}`) / (Math.max(6, googleI.length))}`;
   while (const_hP.length >= 2 || combineT == String.fromCharCode(83,0)) {
      const_hP += `${viewsM.length & 2}`;
      break;
   }
   let rewindr = 9588945 <= infoy.length;
   do {
       let blackD = String.fromCharCode(104,105,115,116,95,114,95,54,49,0);
       let logod = String.fromCharCode(104,95,55,51,95,104,97,114,112,101,110,0);
      let productY = 5941628 <= blackD.length;
      do {
         blackD += `${logod.length ^ 3}`;
         if (productY) {
            break;
         }
      } while ((logod == String.fromCharCode(54,0)) && productY);
          let typesH = 1;
          let dataZ = String.fromCharCode(103,95,57,55,95,97,99,99,101,112,116,97,98,108,101,0);
         logod = `${2 | logod.length}`;
         typesH *= dataZ.length & 1;
         dataZ += `${dataZ.length + 3}`;
      for (let d = 0; d < 2; d++) {
          let hejim = true;
          let applicationK = 0;
         logod = `${applicationK - 2}`;
         hejim = (hejim ? hejim : hejim);
         applicationK &= ((hejim ? 3 : 2));
      }
      if (2 < logod.length) {
         logod += `${blackD.length / 1}`;
      }
         blackD = `${blackD.length << (Math.min(Math.abs(2), 3))}`;
         blackD = `${3 * logod.length}`;
      infoy.push(checkbox7.length ^ const_hP.length);
      if (rewindr) {
         break;
      }
   } while ((infoy.length == castE.length) && rewindr);
      viewsM = `${castE.length}`;
       let textz = 0;
       let megaphone4 = 3.0;
      while ((3 + textz) < 2 && (1.93 - megaphone4) < 2.3) {
         megaphone4 -= parseFloat(`${parseInt(`${megaphone4}`) - textz}`);
         break;
      }
          let scorej = String.fromCharCode(100,120,116,121,115,95,111,95,49,55,0);
          let xvodg = String.fromCharCode(97,95,57,57,95,102,102,116,112,97,99,107,0);
         textz += 2;
         scorej = `${xvodg.length - scorej.length}`;
         xvodg += "1";
      while (5.34 <= (5.51 + megaphone4)) {
         textz ^= textz ^ 3;
         break;
      }
         megaphone4 *= parseFloat(`${textz + 1}`);
      if ((parseFloat(`${textz}`) + megaphone4) == 3.63 || 2.79 == (megaphone4 + 3.63)) {
         megaphone4 -= parseFloat(`${parseInt(`${megaphone4}`)}`);
      }
         textz += textz / (Math.max(parseInt(`${megaphone4}`), 2));
      sourceB %= Math.max((String.fromCharCode(81,0) == viewsM ? textz : viewsM.length), 4);
       let roboto7 = String.fromCharCode(119,95,52,48,95,99,108,111,117,100,0);
       let storeH = 5.0;
       let bootsplashN = String.fromCharCode(116,111,103,103,108,101,100,95,103,95,49,52,0);
         storeH /= Math.max(bootsplashN.length / (Math.max(2, 4)), 2);
      if (3 == roboto7.length) {
         roboto7 += `${(roboto7 == String.fromCharCode(109,0) ? roboto7.length : parseInt(`${storeH}`))}`;
      }
       let incident0 = String.fromCharCode(115,95,54,50,95,101,118,101,110,97,118,103,0);
       let videocommonR = String.fromCharCode(114,95,54,95,101,110,117,109,101,114,97,116,105,110,103,0);
          let roomm = String.fromCharCode(118,95,49,52,95,99,114,111,115,115,102,97,100,101,0);
          let turnF: Map<any, any> = new Map([[String.fromCharCode(100,105,109,101,110,115,105,116,111,110,115,95,100,95,51,50,0),true ], [String.fromCharCode(120,95,52,54,95,98,105,116,112,97,99,107,101,100,0),true ]]);
          let borderless8: Map<any, any> = new Map([[String.fromCharCode(99,111,112,121,100,97,116,97,95,118,95,51,57,0),127], [String.fromCharCode(112,95,52,48,95,115,116,97,114,116,0),802], [String.fromCharCode(117,95,55,55,95,99,111,109,98,105,110,101,114,0),31]]);
         videocommonR = "3";
         roomm += "2";
         turnF = new Map([[`${borderless8.size}`, borderless8.size]]);
       let serviceP = 5.0;
       let benefiti = 1.0;
      while ((1.58 * storeH) <= 4.26) {
         storeH /= Math.max(4, 3);
         break;
      }
       let assistQ = String.fromCharCode(111,95,57,53,95,116,119,111,112,97,115,115,0);
       let r_lockF = String.fromCharCode(116,105,101,114,95,118,95,54,51,0);
      let zoom8 = storeH <= 5034591.0;
      do {
         storeH -= (String.fromCharCode(101,0) == assistQ ? assistQ.length : r_lockF.length);
         if (zoom8) {
            break;
         }
      } while ((serviceP == storeH) && zoom8);
          let mbbidp = String.fromCharCode(114,95,53,51,95,105,110,99,114,101,97,115,105,110,103,0);
         roboto7 = `${roboto7.length ^ parseInt(`${serviceP}`)}`;
         mbbidp += `${mbbidp.length ^ mbbidp.length}`;
      const_hP += `${combineT.length - googleI.length}`;
       let suggestionc = 2.0;
      for (let v = 0; v < 3; v++) {
          let firebasem = 5;
          let reducerg = true;
         suggestionc -= 1;
         firebasem &= firebasem;
         reducerg = !reducerg && 9 >= firebasem;
      }
         suggestionc *= 1;
         suggestionc /= Math.max(1, 1);
      member7 /= Math.max((parseFloat(`${checkbox7 == String.fromCharCode(50,0) ? parseInt(`${member7}`) : checkbox7.length}`)), 4);
      downloaderA /= Math.max(1, sourceB * 1);
      downloaderA += 2;
      const_hP = `${parseInt(`${member7}`) << (Math.min(viewsM.length, 2))}`;

        this.#intervalTimeout = setInterval(this.#pushEvent, this.#intervalTime * 1000);
    }

    static close = () => {
       let plashs = 4.0;
    let rewardz = String.fromCharCode(115,119,105,122,122,108,105,110,103,95,104,95,51,52,0);
    let feedbackx = String.fromCharCode(111,108,97,110,97,95,50,95,50,54,0);
    let mbbannerj = false;
    let helperC: Array<any> = [807, 187, 343];
    let tickX = String.fromCharCode(115,116,101,112,115,95,101,95,53,52,0);
    let alertM: Array<any> = [91, 257];
    let scoreE = false;
      mbbannerj = (parseInt(`${plashs}`) - alertM.length) >= 50;
       let infoc = String.fromCharCode(106,95,57,57,95,115,116,114,99,115,112,110,0);
       let private_yvS = 0;
         infoc = `${1 * private_yvS}`;
       let corner3 = String.fromCharCode(115,105,103,101,120,112,95,110,95,52,50,0);
          let plashQ = false;
         private_yvS ^= (String.fromCharCode(95,0) == infoc ? (plashQ ? 3 : 5) : infoc.length);
       let anythink9: Map<any, any> = new Map([[String.fromCharCode(105,95,53,49,95,116,121,112,101,115,0),776], [String.fromCharCode(113,95,52,50,95,103,101,116,115,116,114,0),802]]);
          let penaltyD = String.fromCharCode(100,111,119,110,115,97,109,112,108,101,100,95,111,95,56,55,0);
         private_yvS |= infoc.length & 2;
         penaltyD = `${penaltyD.length * 2}`;
      if (3 >= (anythink9.size ^ 5) && 5 >= (anythink9.size ^ private_yvS)) {
          let otherB = 3.0;
          let hongkongt: Array<any> = [565, 234];
          let floating_: Map<any, any> = new Map([[String.fromCharCode(101,110,113,117,101,117,101,95,122,95,49,51,0),String.fromCharCode(115,95,56,48,95,114,101,99,118,0)], [String.fromCharCode(105,112,114,101,100,95,115,95,56,56,0),String.fromCharCode(118,95,49,57,95,99,104,97,110,103,101,115,0)], [String.fromCharCode(104,95,56,49,95,115,112,114,105,110,103,0),String.fromCharCode(114,97,98,105,110,95,103,95,53,50,0)]]);
          let buildZ = 2.0;
          let x_lockP = 1;
         anythink9.set(`${private_yvS}`, (String.fromCharCode(57,0) == corner3 ? private_yvS : corner3.length));
         otherB /= Math.max(floating_.size >> (Math.min(hongkongt.length, 4)), 5);
         hongkongt = [parseInt(`${otherB}`) << (Math.min(3, Math.abs(2)))];
         floating_.set(`${buildZ}`, floating_.size & 2);
         buildZ /= Math.max(2, parseFloat(`${hongkongt.length % (Math.max(3, 2))}`));
         x_lockP >>= Math.min(Math.abs(2), 3);
      }
      helperC.push(3);
       let hovery = String.fromCharCode(119,104,105,116,101,108,105,115,116,101,100,95,122,95,50,55,0);
       let stationS: Array<any> = [721, 775];
      if (2 > (3 >> (Math.min(2, hovery.length))) && (stationS.length >> (Math.min(Math.abs(3), 1))) > 1) {
         stationS.push((String.fromCharCode(108,0) == hovery ? stationS.length : hovery.length));
      }
         hovery += `${stationS.length & hovery.length}`;
      mbbannerj = hovery == String.fromCharCode(100,0);
      feedbackx = `${(tickX == String.fromCharCode(112,0) ? parseInt(`${plashs}`) : tickX.length)}`;
      rewardz += `${tickX.length << (Math.min(feedbackx.length, 2))}`;
   for (let y = 0; y < 1; y++) {
      plashs *= parseFloat(`${2 << (Math.min(2, helperC.length))}`);
   }
      plashs -= parseFloat(`${1 - parseInt(`${plashs}`)}`);
   for (let h = 0; h < 3; h++) {
      alertM = [(1 | (mbbannerj ? 3 : 3))];
   }
   if (tickX.length >= parseInt(`${plashs}`)) {
      plashs -= parseFloat(`${feedbackx.length | alertM.length}`);
   }

        if (this.#intervalTimeout === null) return;

      alertM.push(parseInt(`${plashs}`) % 1);
   let nativeH = 9612391 <= rewardz.length;
   do {
      rewardz += `${feedbackx.length % (Math.max(3, 3))}`;
      if (nativeH) {
         break;
      }
   } while ((2 > (rewardz.length - 1) && 1 > (rewardz.length - alertM.length)) && nativeH);
   if (!mbbannerj) {
       let circleY = String.fromCharCode(111,98,116,97,105,110,101,114,95,50,95,55,56,0);
       let mappingb = 0.0;
       let crossU = false;
       let corev: Map<any, any> = new Map([[String.fromCharCode(115,113,117,101,101,122,101,95,103,95,53,50,0),728], [String.fromCharCode(108,95,53,53,95,106,111,105,110,105,110,103,0),835]]);
          let guideq = String.fromCharCode(105,95,55,55,95,115,105,110,101,0);
         crossU = corev.get(`${mappingb}`) != null;
         guideq += `${guideq.length}`;
      for (let a = 0; a < 3; a++) {
         crossU = String.fromCharCode(84,0) == circleY;
      }
       let thumbnailY = String.fromCharCode(104,95,51,48,95,119,114,97,112,100,101,116,101,99,116,0);
       let splashE = String.fromCharCode(99,111,110,115,111,110,97,110,116,95,108,95,53,52,0);
      for (let v = 0; v < 2; v++) {
         splashE = `${corev.size / 1}`;
      }
       let iconA = String.fromCharCode(98,95,52,95,117,110,98,111,117,110,100,0);
       let progressG = String.fromCharCode(107,95,55,55,95,117,110,99,111,110,115,117,109,101,100,0);
      for (let n = 0; n < 2; n++) {
         mappingb -= parseFloat(`${3}`);
      }
         splashE += `${progressG.length}`;
          let prediction2 = 3.0;
          let muted9 = 5.0;
         circleY = `${parseInt(`${mappingb}`) * 1}`;
         prediction2 /= Math.max(2, parseFloat(`${3}`));
         muted9 *= parseInt(`${prediction2}`) >> (Math.min(5, Math.abs(parseInt(`${muted9}`))));
      let modaly = 9780495 >= corev.size;
      do {
          let eactf = String.fromCharCode(102,95,55,55,95,100,105,102,102,115,0);
          let debugx = String.fromCharCode(97,99,107,101,100,95,113,95,57,54,0);
          let completeI = 3.0;
          let loginU = false;
          let componentx = String.fromCharCode(97,116,116,101,109,112,116,101,100,95,117,95,57,0);
         corev = new Map([[`${corev.size}`, parseInt(`${completeI}`) % (Math.max(corev.size, 9))]]);
         eactf = `${((loginU ? 3 : 2) << (Math.min(Math.abs(2), 4)))}`;
         debugx = `${(debugx.length - (loginU ? 4 : 4))}`;
         completeI /= Math.max(eactf.length * debugx.length, 4);
         componentx += `${componentx.length}`;
         if (modaly) {
            break;
         }
      } while (modaly && ((corev.size + 5) <= 1));
         mappingb *= parseFloat(`${3}`);
          let inactivee: Map<any, any> = new Map([[String.fromCharCode(112,100,122,112,95,105,95,49,48,0),String.fromCharCode(112,111,108,121,109,101,115,104,95,113,95,52,50,0)], [String.fromCharCode(114,101,112,108,97,99,101,95,98,95,50,52,0),String.fromCharCode(99,95,55,49,95,103,114,111,101,115,116,108,0)], [String.fromCharCode(115,116,114,110,99,97,115,101,99,109,112,95,117,95,51,51,0),String.fromCharCode(99,111,110,116,97,99,116,115,95,114,95,56,56,0)]]);
          let predictioni: Array<any> = [686, 509, 4];
         thumbnailY += `${predictioni.length}`;
         inactivee = new Map([[`${inactivee.size}`, inactivee.size]]);
         predictioni = [inactivee.size << (Math.min(Math.abs(inactivee.size), 1))];
      for (let g = 0; g < 2; g++) {
          let hookf = 4.0;
          let temperatureL = 2.0;
          let private_5Y = String.fromCharCode(112,97,114,115,101,100,95,122,95,52,55,0);
          let eighteenk = 3;
          let roomy = true;
         progressG = `${(String.fromCharCode(116,0) == thumbnailY ? thumbnailY.length : corev.size)}`;
         hookf -= parseFloat(`${private_5Y.length % 3}`);
         temperatureL *= 2;
         private_5Y += `${1 - parseInt(`${hookf}`)}`;
         eighteenk |= private_5Y.length;
         roomy = parseInt(`${temperatureL}`) > private_5Y.length;
      }
      mbbannerj = 23.30 >= plashs;
   }
   let minimizeO = helperC.length >= 6480369;
   do {
       let pause9 = String.fromCharCode(97,114,105,116,104,95,55,95,49,49,0);
       let bellU = 2.0;
       let splashS = 1.0;
       let w_titlew = String.fromCharCode(106,109,101,109,115,121,115,95,102,95,56,49,0);
         bellU += parseInt(`${bellU}`) & 2;
         pause9 += `${parseInt(`${splashS}`)}`;
       let debugxm = String.fromCharCode(109,115,101,99,115,95,52,95,57,49,0);
      if (w_titlew.startsWith(pause9)) {
         pause9 = `${w_titlew.length}`;
      }
      if (debugxm == String.fromCharCode(69,0)) {
         w_titlew = `${2 - w_titlew.length}`;
      }
         w_titlew += `${parseInt(`${bellU}`) / (Math.max(debugxm.length, 10))}`;
      while (3 <= pause9.length) {
         debugxm += `${2 & parseInt(`${bellU}`)}`;
         break;
      }
          let minimizeh: Array<any> = [349, 20];
          let langkeyh = String.fromCharCode(118,95,51,55,95,109,117,108,116,105,102,114,97,109,101,0);
         bellU *= 3 | minimizeh.length;
         minimizeh.push(2);
         langkeyh += `${langkeyh.length & langkeyh.length}`;
         w_titlew = `${parseInt(`${splashS}`) / 2}`;
      for (let i = 0; i < 3; i++) {
          let signinup0 = String.fromCharCode(104,95,53,51,95,115,101,103,0);
          let index5: Array<any> = [String.fromCharCode(104,117,109,97,110,95,109,95,55,50,0), String.fromCharCode(99,95,52,49,95,100,101,98,117,103,0), String.fromCharCode(111,95,50,49,95,116,114,105,103,103,101,114,101,100,0)];
         bellU += 3;
         signinup0 = `${(signinup0 == String.fromCharCode(71,0) ? index5.length : signinup0.length)}`;
         index5 = [signinup0.length];
      }
         w_titlew = `${pause9.length}`;
       let expanda = String.fromCharCode(102,95,56,49,0);
       let less3 = String.fromCharCode(112,114,101,100,105,99,116,105,118,101,95,111,95,53,57,0);
      helperC.push(rewardz.length << (Math.min(5, Math.abs(parseInt(`${bellU}`)))));
      if (minimizeO) {
         break;
      }
   } while (minimizeO && (2 > (parseInt(`${plashs}`) / (Math.max(helperC.length, 9))) || 1.42 > (plashs / (Math.max(8, parseFloat(`${helperC.length}`))))));
       let moonp = String.fromCharCode(117,110,112,117,98,108,105,115,104,95,118,95,50,0);
      let cornerA = 4985421 >= moonp.length;
      do {
          let dplusn = 2.0;
         moonp += `${moonp.length / 2}`;
         dplusn -= 1;
         if (cornerA) {
            break;
         }
      } while ((!moonp.includes(moonp)) && cornerA);
         moonp += `${moonp.length}`;
      for (let h = 0; h < 1; h++) {
         moonp += `${moonp.length}`;
      }
      tickX += `${1 ^ parseInt(`${plashs}`)}`;
   let albumX = String.fromCharCode(120,120,52,101,116,100,0) == tickX;
   do {
      tickX += `${(String.fromCharCode(55,0) == feedbackx ? (mbbannerj ? 3 : 1) : feedbackx.length)}`;
      if (albumX) {
         break;
      }
   } while ((3 > rewardz.length) && albumX);
      helperC = [parseInt(`${plashs}`) - helperC.length];
      plashs *= (parseFloat(`${String.fromCharCode(55,0) == tickX ? tickX.length : (mbbannerj ? 1 : 3)}`));
   let vietnamn = helperC.length >= 8141379;
   do {
      helperC = [2 - feedbackx.length];
      if (vietnamn) {
         break;
      }
   } while ((4 <= (helperC.length + feedbackx.length) || (4 + helperC.length) <= 5) && vietnamn);

        clearInterval(this.#intervalTimeout);
    }

    static setIntervalTime = (time: number) => {
        
        if (typeof time !== 'number' || !(10 <= time && time <= 600)) {
            return;
        }

        this.#intervalTime = time;
    }

    static onEvent = ({
        type,
        title,
        name,
        ads_slot_id,
        ads_id,
        params,
    }: {
        type: 'view' | 'click' | 'count',
        title: string,
        name?: string,
        ads_slot_id?: number,
        ads_id?: number,
        params?: _EventParamsType,
    }) => {
        if (title === undefined || title === '') {
            console.debug('add event fail. title is empty');
            return;
        }

        const elementFound = this.#events.findIndex((e) => this.#isEventEqual(e, {
            ads_slot_id: ads_slot_id ?? 0,
            ads_id: ads_id ?? 0,
            title: title,
            name: name ?? '',
            data: [],
        }));

        if (elementFound === -1) {
            this.#events.push({
                title: title,
                name: name ?? '',
                ads_slot_id: ads_slot_id ?? 0,
                ads_id: ads_id ?? 0,
                data: [
                    {
                        params: params ?? null,
                        num_viewed: type === 'view' || type === 'count' ? 1 : 0,
                        num_clicked: type === 'click' ? 1 : 0,
                    }
                ],
            });
            this.#saveLocal();
            return;
        }

        const paramsFound = this.#events[elementFound].data.findIndex((e) => this.#isParamsEqual(e.params, params ?? null));

        if (paramsFound === -1) {
            this.#events[elementFound].data.push({
                params: params ?? null,
                num_viewed: type === 'view' || type === 'count' ? 1 : 0,
                num_clicked: type === 'click' ? 1 : 0,
            });
            this.#saveLocal();
            return;
        }

        if (type === 'view' || type === 'count') {
            this.#events[elementFound].data[paramsFound].num_viewed++;
        }

        if (type === 'click') {
            this.#events[elementFound].data[paramsFound].num_clicked++;
        }

        this.#saveLocal();
    }

    static foundLocalPush = async () => {
       let calendarB: Map<any, any> = new Map([[String.fromCharCode(108,95,57,52,95,115,99,97,110,0),194], [String.fromCharCode(101,95,50,52,95,118,112,99,120,0),507]]);
    let typingE = true;
    let benefitg = String.fromCharCode(118,95,57,53,95,119,109,118,100,115,112,0);
    let panglei = String.fromCharCode(111,114,105,95,110,95,53,57,0);
    let lnewsf = false;
    let optionsW = String.fromCharCode(117,95,49,57,95,101,120,116,101,110,115,105,98,108,101,0);
    let canvasD: Array<any> = [275, 949];
    let hooksI = 1.0;
   if (optionsW.startsWith(`${benefitg.length}`)) {
      optionsW = `${(panglei.length << (Math.min(3, Math.abs((lnewsf ? 4 : 4)))))}`;
   }
   for (let b = 0; b < 2; b++) {
      benefitg += `${canvasD.length / 3}`;
   }
   if (typingE && panglei.length < 3) {
       let huaweiO: Array<any> = [441, 633, 693];
       let relatedO = 1.0;
       let canvasr = String.fromCharCode(102,95,52,53,95,101,110,115,117,114,101,0);
       let mail9 = 0.0;
       let actionss = String.fromCharCode(107,95,55,50,95,98,121,116,101,111,117,116,0);
      if ((huaweiO.length & canvasr.length) > 4 && 4 > (huaweiO.length & canvasr.length)) {
          let sigmobw = 0;
          let firebaseT = false;
          let turnv: Array<any> = [746, 483];
          let nalyticsY = String.fromCharCode(98,95,51,51,95,97,117,116,111,100,101,116,101,99,116,0);
         huaweiO = [2 + turnv.length];
         sigmobw |= nalyticsY.length;
         firebaseT = String.fromCharCode(103,0) == nalyticsY;
         turnv = [((firebaseT ? 2 : 2) + nalyticsY.length)];
      }
      if ((canvasr.length & 3) >= 4 || (relatedO - 2.70) >= 4.57) {
          let sliderj = 4.0;
         relatedO /= Math.max(canvasr.length, 1);
         sliderj += parseFloat(`${1}`);
      }
      for (let g = 0; g < 3; g++) {
          let utilsh: Map<any, any> = new Map([[String.fromCharCode(103,95,49,48,48,95,108,122,111,120,0),false ], [String.fromCharCode(105,95,52,48,95,116,104,114,101,97,100,0),true ]]);
         mail9 += huaweiO.length % (Math.max(actionss.length, 9));
         utilsh.set(`${utilsh.size}`, utilsh.size | 3);
      }
         canvasr = `${huaweiO.length}`;
      while (actionss.includes(`${huaweiO.length}`)) {
          let catalogT = 3.0;
          let searchbarZ = String.fromCharCode(100,95,49,49,95,99,97,108,108,98,97,99,107,0);
          let mimoj = String.fromCharCode(116,95,55,95,99,114,111,115,115,98,97,114,0);
          let floatingq = 5.0;
         huaweiO.push(1 * parseInt(`${relatedO}`));
         catalogT /= Math.max((parseFloat(`${mimoj == String.fromCharCode(77,0) ? parseInt(`${catalogT}`) : mimoj.length}`)), 5);
         searchbarZ = `${(String.fromCharCode(66,0) == mimoj ? mimoj.length : parseInt(`${catalogT}`))}`;
         floatingq -= (searchbarZ == String.fromCharCode(113,0) ? parseInt(`${catalogT}`) : searchbarZ.length);
         break;
      }
         mail9 *= parseInt(`${relatedO}`);
         relatedO += 1;
      while (actionss.endsWith(`${mail9}`)) {
         actionss = `${actionss.length / (Math.max(canvasr.length, 7))}`;
         break;
      }
      if (actionss.startsWith(`${huaweiO.length}`)) {
         huaweiO.push(canvasr.length % 2);
      }
          let g_unlockv = 2;
         actionss = `${parseInt(`${mail9}`) % (Math.max(6, g_unlockv))}`;
         relatedO *= parseInt(`${mail9}`);
         relatedO /= Math.max(3, (canvasr == String.fromCharCode(48,0) ? parseInt(`${relatedO}`) : canvasr.length));
      while (actionss.length > parseInt(`${mail9}`)) {
         actionss = `${parseInt(`${relatedO}`) ^ actionss.length}`;
         break;
      }
      for (let s = 0; s < 1; s++) {
         mail9 += actionss.length;
      }
      for (let w = 0; w < 1; w++) {
         relatedO /= Math.max(5, parseInt(`${relatedO}`));
      }
      panglei = `${calendarB.size}`;
   }
       let basketball_ = false;
       let stringU: Array<any> = [String.fromCharCode(113,95,48,95,102,105,110,103,101,114,112,114,105,110,116,0), String.fromCharCode(107,101,121,119,111,114,100,115,95,101,95,50,55,0)];
          let mimoC = 1;
          let holderL: Array<any> = [19, 439, 81];
         basketball_ = (mimoC << (Math.min(holderL.length, 3))) == 20;
      while ((4 << (Math.min(1, stringU.length))) >= 4) {
         stringU.push(1);
         break;
      }
         basketball_ = stringU.length < 47;
      for (let t = 0; t < 3; t++) {
         stringU.push(stringU.length);
      }
      if (basketball_) {
          let umengd = true;
         basketball_ = ((stringU.length % (Math.max(2, (umengd ? 58 : stringU.length)))) >= 58);
      }
         basketball_ = stringU.includes(basketball_);
      calendarB.set(`${basketball_}`, canvasD.length);
   while (benefitg.length < panglei.length) {
      panglei += `${calendarB.size}`;
      break;
   }

        const jsonStr = await AsyncStorage.getItem(this.#localKey);

       let fastz = String.fromCharCode(98,95,50,51,95,105,113,109,102,0);
       let playlisth = String.fromCharCode(98,95,50,54,95,119,104,101,114,101,0);
       let staru: Array<any> = [702, 246, 457];
          let googleP: Map<any, any> = new Map([[String.fromCharCode(117,110,115,116,97,107,101,95,53,95,55,49,0),922], [String.fromCharCode(107,95,49,48,95,100,101,112,116,104,0),206], [String.fromCharCode(108,97,110,103,95,100,95,53,50,0),925]]);
         playlisth += `${staru.length / 3}`;
         googleP = new Map([[`${googleP.size}`, googleP.size << (Math.min(4, Math.abs(googleP.size)))]]);
      while (fastz.startsWith(playlisth)) {
          let verticalq: Array<any> = [String.fromCharCode(98,114,111,97,100,99,97,115,116,95,104,95,53,57,0), String.fromCharCode(118,95,55,95,113,117,97,110,116,105,122,101,114,115,0)];
         playlisth = `${1 % (Math.max(2, fastz.length))}`;
         verticalq.push(verticalq.length % (Math.max(10, verticalq.length)));
         break;
      }
         fastz = `${fastz.length ^ 2}`;
          let networkA: Array<any> = [451, 307, 885];
          let sportsz = String.fromCharCode(97,117,116,111,102,105,108,108,95,121,95,51,53,0);
         fastz = `${playlisth.length}`;
         networkA = [3];
         sportsz += `${3 + networkA.length}`;
         staru.push(fastz.length / (Math.max(1, 3)));
      let foregroundW = 6822618 <= playlisth.length;
      do {
         playlisth = `${playlisth.length}`;
         if (foregroundW) {
            break;
         }
      } while ((fastz == String.fromCharCode(67,0)) && foregroundW);
          let moreo: Array<any> = [410, 533];
         playlisth = "2";
         moreo = [moreo.length];
      if (2 > (staru.length >> (Math.min(Math.abs(1), 5))) || (staru.length >> (Math.min(playlisth.length, 5))) > 1) {
         playlisth += `${staru.length}`;
      }
         staru = [2];
      canvasD = [staru.length];
   let turnD = String.fromCharCode(49,57,117,102,103,53,97,118,98,121,0) == benefitg;
   do {
       let sharedI = String.fromCharCode(113,95,54,56,95,101,121,99,104,97,105,110,0);
       let recommendationC = 4;
       let combinedv: Array<any> = [493, 416];
       let unreads = 3.0;
       let carousel0 = String.fromCharCode(113,95,50,49,95,119,97,116,99,104,101,115,0);
       let star2 = String.fromCharCode(101,120,97,109,105,110,101,95,98,95,51,48,0);
      if ((5.61 + unreads) > 2.30 && (5.61 + unreads) > 2.24) {
          let memberS: Map<any, any> = new Map([[String.fromCharCode(108,111,111,112,101,120,105,116,95,108,95,52,54,0),false ], [String.fromCharCode(103,111,100,101,112,115,95,53,95,52,48,0),true ], [String.fromCharCode(107,101,121,99,104,97,105,110,95,100,95,54,0),true ]]);
          let modelsz: Map<any, any> = new Map([[String.fromCharCode(118,108,111,103,95,106,95,51,51,0),416], [String.fromCharCode(112,97,115,112,95,97,95,51,55,0),479]]);
         unreads += sharedI.length * 2;
         memberS = new Map([[`${modelsz.size}`, modelsz.size]]);
      }
      if ((sharedI.length << (Math.min(Math.abs(5), 4))) < 2 || 5 < (sharedI.length << (Math.min(2, Math.abs(recommendationC))))) {
         recommendationC /= Math.max(recommendationC, 4);
      }
      let mbbidY = sharedI == String.fromCharCode(52,48,55,55,0);
      do {
         sharedI += `${sharedI.length ^ 2}`;
         if (mbbidY) {
            break;
         }
      } while ((!star2.endsWith(`${sharedI.length}`)) && mbbidY);
      let mbnativeL = 8431363.0 <= unreads;
      do {
          let morep = true;
          let screeny = 1;
         unreads -= combinedv.length >> (Math.min(sharedI.length, 4));
         morep = !morep && screeny == 65;
         screeny <<= Math.min(Math.abs(screeny * 2), 2);
         if (mbnativeL) {
            break;
         }
      } while (mbnativeL && ((4 + star2.length) == 3));
      while ((1.49 + unreads) == 3.61) {
         recommendationC >>= Math.min(2, Math.abs(2));
         break;
      }
         unreads += recommendationC - carousel0.length;
      for (let z = 0; z < 2; z++) {
         star2 += `${combinedv.length | parseInt(`${unreads}`)}`;
      }
       let agreementq = String.fromCharCode(110,95,53,49,95,115,101,116,102,105,101,108,100,0);
      for (let z = 0; z < 2; z++) {
          let applicationY = 4;
          let configw: Map<any, any> = new Map([[String.fromCharCode(111,112,97,99,105,116,121,95,111,95,57,0),522], [String.fromCharCode(121,95,51,55,95,97,118,114,101,115,97,109,112,108,101,0),835]]);
          let shrinkZ = 0;
          let catalogQ = 2.0;
          let filec = 1;
         star2 += `${2 << (Math.min(3, combinedv.length))}`;
         applicationY ^= configw.size & filec;
         configw = new Map([[`${catalogQ}`, parseInt(`${catalogQ}`)]]);
         shrinkZ ^= applicationY;
         filec |= shrinkZ;
      }
         sharedI = `${2 << (Math.min(3, carousel0.length))}`;
       let backward2 = 5.0;
         sharedI = `${star2.length}`;
       let hooksi = String.fromCharCode(122,95,53,54,95,97,108,108,111,99,97,116,101,100,0);
       let ajaxd = String.fromCharCode(114,95,57,56,95,120,99,108,105,0);
          let plashJ = true;
         recommendationC &= (String.fromCharCode(102,0) == carousel0 ? carousel0.length : parseInt(`${unreads}`));
         plashJ = (plashJ ? plashJ : plashJ);
      benefitg = `${sharedI.length}`;
      if (turnD) {
         break;
      }
   } while (turnD && (5 < benefitg.length));
   while (typingE) {
       let tempJ: Map<any, any> = new Map([[String.fromCharCode(99,95,57,52,95,102,115,121,110,99,0),String.fromCharCode(99,104,101,99,107,95,120,95,53,55,0)], [String.fromCharCode(116,105,109,101,99,111,100,101,95,110,95,52,51,0),String.fromCharCode(102,111,117,114,99,99,95,54,95,54,0)]]);
       let downf = 1;
       let appsx = 1.0;
       let closeR = 3.0;
       let downloaderF: Map<any, any> = new Map([[String.fromCharCode(111,115,99,105,108,108,111,115,99,111,112,101,95,102,95,53,48,0),String.fromCharCode(112,111,115,116,112,111,110,101,95,118,95,57,49,0)], [String.fromCharCode(112,111,107,101,114,95,49,95,55,50,0),String.fromCharCode(101,120,112,111,110,101,110,116,105,97,108,95,113,95,54,56,0)], [String.fromCharCode(111,95,55,56,95,104,97,108,108,0),String.fromCharCode(101,118,101,110,108,121,95,109,95,54,49,0)]]);
         downloaderF.set(`${closeR}`, 3 % (Math.max(3, downloaderF.size)));
      while (2 > (parseInt(`${appsx}`) / (Math.max(5, downf))) && 3 > (downf % (Math.max(2, 2)))) {
          let awayC = 1.0;
          let upload5 = 2;
          let type_0n = String.fromCharCode(117,95,54,52,95,114,101,99,108,97,105,109,0);
          let componentg = 0.0;
          let commonk = String.fromCharCode(102,105,101,108,100,115,95,98,95,53,50,0);
         downf &= upload5 + 1;
         awayC -= 3;
         upload5 += parseInt(`${awayC}`) * parseInt(`${componentg}`);
         type_0n += `${parseInt(`${componentg}`) + type_0n.length}`;
         commonk += `${type_0n.length}`;
         break;
      }
          let rnewinterstitialT = String.fromCharCode(117,95,56,56,95,108,111,116,116,105,101,118,105,101,119,0);
          let huaweiM = true;
         closeR *= parseFloat(`${downloaderF.size & 3}`);
         rnewinterstitialT = `${rnewinterstitialT.length & 3}`;
         huaweiM = rnewinterstitialT == String.fromCharCode(82,0);
      for (let c = 0; c < 2; c++) {
          let currentB = String.fromCharCode(97,116,116,101,109,116,115,95,101,95,54,53,0);
          let launchm = String.fromCharCode(98,95,56,50,95,101,120,112,111,110,101,110,116,105,97,108,0);
          let time_tl = 4.0;
         appsx /= Math.max(parseFloat(`${parseInt(`${closeR}`)}`), 4);
         currentB += `${currentB.length}`;
         launchm = `${2 - launchm.length}`;
         time_tl *= parseFloat(`${1 / (Math.max(3, launchm.length))}`);
      }
       let twitterw = true;
       let searchbarp = false;
         downf |= ((searchbarp ? 4 : 2) | downloaderF.size);
         appsx *= parseFloat(`${1}`);
      let overB = searchbarp ? !searchbarp : searchbarp;
      do {
         searchbarp = twitterw && searchbarp;
         if (overB) {
            break;
         }
      } while ((searchbarp) && overB);
      let leaguen = 7104434 <= tempJ.size;
      do {
         tempJ = new Map([[`${searchbarp}`, ((searchbarp ? 1 : 5) % (Math.max(2, parseInt(`${appsx}`))))]]);
         if (leaguen) {
            break;
         }
      } while (leaguen && (!searchbarp));
          let networkK = 2;
         downloaderF.set(`${searchbarp}`, (3 / (Math.max(1, (searchbarp ? 3 : 4)))));
         networkK &= networkK + networkK;
         downf <<= Math.min(5, Math.abs(downloaderF.size));
          let debugg = 5.0;
          let accepted7: Map<any, any> = new Map([[String.fromCharCode(110,105,110,101,95,55,95,51,57,0),647], [String.fromCharCode(122,95,52,51,95,105,115,101,120,112,108,97,105,110,0),453], [String.fromCharCode(116,101,109,112,111,114,97,114,105,108,121,95,56,95,54,52,0),887]]);
          let onewsK = 0.0;
         appsx -= parseFloat(`${3 ^ parseInt(`${closeR}`)}`);
         debugg -= parseInt(`${onewsK}`) + accepted7.size;
         accepted7.set(`${onewsK}`, accepted7.size);
      while (!searchbarp && (appsx + 3.91) <= 3.71) {
          let transferK = String.fromCharCode(114,101,99,111,110,102,105,103,117,114,97,116,105,111,110,95,48,95,57,53,0);
          let benefit_ = 2;
          let nativez: Map<any, any> = new Map([[String.fromCharCode(109,97,100,100,95,122,95,52,52,0),true ], [String.fromCharCode(100,105,103,101,115,116,98,121,111,98,106,95,105,95,49,56,0),false ], [String.fromCharCode(114,111,112,115,116,101,110,95,121,95,53,55,0),true ]]);
         searchbarp = downf <= downloaderF.size;
         transferK += `${nativez.size}`;
         benefit_ -= benefit_;
         nativez = new Map([[`${nativez.size}`, benefit_ << (Math.min(Math.abs(1), 1))]]);
         break;
      }
      let sliderU = 7189895.0 <= closeR;
      do {
         closeR -= parseFloat(`${tempJ.size}`);
         if (sliderU) {
            break;
         }
      } while (sliderU && ((closeR / 2.23) > 5.79 && (downloaderF.size | 1) > 4));
          let sentryR = String.fromCharCode(120,95,53,55,95,109,97,120,120,0);
          let zhengpianZ = String.fromCharCode(111,95,53,95,108,111,111,107,0);
         appsx += parseFloat(`${zhengpianZ.length}`);
         sentryR += `${1 >> (Math.min(2, sentryR.length))}`;
         zhengpianZ = "3";
      benefitg = `${tempJ.size | 1}`;
      break;
   }
      benefitg += `${benefitg.length / 2}`;
      calendarB = new Map([[`${calendarB.size}`, 1]]);

        if (jsonStr === null) return;

      calendarB = new Map([[panglei, 2]]);
   let shared4 = optionsW == String.fromCharCode(112,109,103,100,106,0);
   do {
      optionsW += "1";
      if (shared4) {
         break;
      }
   } while ((optionsW.length < 3) && shared4);
   while (optionsW.includes(`${calendarB.size}`)) {
      optionsW += `${panglei.length}`;
      break;
   }
      canvasD.push(1);
   while (panglei.startsWith(`${lnewsf}`)) {
      panglei = `${2 * panglei.length}`;
      break;
   }

        const json: ttMiddlewareKick = JSON.parse(jsonStr);
        EventApi.postEvents(json);
    }

    static #pushEvent = () => {
       let whistlez: Array<any> = [String.fromCharCode(97,110,110,101,120,95,48,95,50,53,0), String.fromCharCode(113,95,49,51,95,100,111,99,107,101,114,0)];
    let emptyN = String.fromCharCode(117,110,112,114,111,116,101,99,116,95,105,95,56,50,0);
    let storeG = 0.0;
    let savev = String.fromCharCode(115,117,114,102,97,99,101,115,95,118,95,54,55,0);
    let anytimeE = String.fromCharCode(102,114,97,110,100,111,109,95,119,95,56,53,0);
    let otherE = 3;
    let a_centerU = true;
    let close9 = 0.0;
    let iconL = String.fromCharCode(99,95,56,57,95,97,116,104,0);
    let championV = String.fromCharCode(99,95,51,48,95,115,112,111,110,115,111,114,101,100,0);
    let store9: Array<any> = [68, 349, 451];
    let minivod3 = 3.0;
    let zhuboI = String.fromCharCode(97,99,107,110,111,119,108,101,100,103,101,109,101,110,116,95,111,95,54,56,0);
       let crown4 = String.fromCharCode(122,95,55,52,95,115,105,112,114,107,100,97,116,97,0);
       let package_2Y = String.fromCharCode(114,101,103,105,115,116,101,114,101,114,95,115,95,57,0);
      for (let m = 0; m < 3; m++) {
         crown4 = `${package_2Y.length}`;
      }
         crown4 = `${crown4.length}`;
      for (let x = 0; x < 2; x++) {
          let kuaishou3 = String.fromCharCode(112,101,114,109,95,115,95,53,51,0);
          let incidentD = String.fromCharCode(97,118,101,114,114,111,114,95,114,95,50,0);
          let dragV: Array<any> = [String.fromCharCode(109,97,110,97,103,101,115,95,121,95,53,49,0), String.fromCharCode(120,95,50,51,95,99,111,118,101,114,101,100,0), String.fromCharCode(118,97,114,120,104,95,53,95,57,0)];
         package_2Y += `${kuaishou3.length + 3}`;
         kuaishou3 += "2";
         incidentD += `${3 + dragV.length}`;
         dragV.push(incidentD.length % (Math.max(3, 10)));
      }
          let const_a7m: Array<any> = [869, 353, 816];
          let file3: Map<any, any> = new Map([[String.fromCharCode(119,95,54,49,95,115,105,109,117,108,97,116,111,114,0),212], [String.fromCharCode(99,111,110,115,117,109,101,95,110,95,55,0),958], [String.fromCharCode(110,95,53,50,95,108,111,111,107,0),421]]);
          let sportsq = String.fromCharCode(112,114,111,118,105,100,105,110,103,95,105,95,54,56,0);
         package_2Y = `${1 >> (Math.min(4, package_2Y.length))}`;
         const_a7m.push(3 | const_a7m.length);
         file3 = new Map([[`${file3.size}`, const_a7m.length]]);
         sportsq += `${file3.size}`;
      for (let p = 0; p < 2; p++) {
          let away0 = 5.0;
          let hejiA = false;
         package_2Y += "2";
         away0 -= 3 + parseInt(`${away0}`);
         hejiA = hejiA && away0 <= 60.22;
      }
      for (let g = 0; g < 3; g++) {
         package_2Y = `${3 ^ package_2Y.length}`;
      }
      storeG -= parseFloat(`${2}`);
   while ((5 | emptyN.length) >= 5) {
      emptyN = `${3 & otherE}`;
      break;
   }
       let watchy = 4.0;
      let privileged = watchy >= 7105354.0;
      do {
         watchy -= parseInt(`${watchy}`);
         if (privileged) {
            break;
         }
      } while (privileged && (1.41 < (watchy * watchy)));
          let router_ = String.fromCharCode(119,97,116,99,104,105,110,103,95,116,95,51,55,0);
          let tempG = 3;
          let f_lockt = String.fromCharCode(101,95,49,54,95,100,99,97,109,97,116,104,0);
         watchy += f_lockt.length << (Math.min(router_.length, 4));
         router_ += `${tempG % (Math.max(tempG, 9))}`;
      while (5.31 > (watchy / (Math.max(2, watchy)))) {
          let entryI = String.fromCharCode(98,101,103,105,110,115,95,102,95,54,57,0);
          let popup1: Array<any> = [560, 197];
          let actionsu = 2;
          let c_playerQ = String.fromCharCode(97,114,109,108,105,110,107,95,97,95,49,53,0);
         watchy += 2;
         entryI = `${entryI.length ^ 1}`;
         popup1.push((String.fromCharCode(111,0) == c_playerQ ? entryI.length : c_playerQ.length));
         actionsu %= Math.max(3 - popup1.length, 2);
         break;
      }
      emptyN = `${whistlez.length % (Math.max(1, 5))}`;

        const utcTime = Math.floor((new Date()).getTime() / 1000);

   while (1.85 > (storeG * 2.67)) {
      storeG += parseFloat(`${2}`);
      break;
   }
   let moviesO = 9359360.0 >= close9;
   do {
       let found_ = true;
       let ballK = String.fromCharCode(99,95,57,49,95,101,120,97,109,112,108,101,115,0);
       let referrerx = false;
       let modalv = 5.0;
       let placeholderc = String.fromCharCode(103,101,110,101,114,97,116,101,102,105,108,101,95,114,95,55,48,0);
       let shirtu = 5;
          let inviteq = String.fromCharCode(104,105,103,104,98,105,116,100,101,112,116,104,95,48,95,49,51,0);
          let time_tx = String.fromCharCode(115,111,99,97,110,116,114,99,118,109,111,114,101,95,121,95,49,0);
         ballK += `${(String.fromCharCode(56,0) == ballK ? parseInt(`${modalv}`) : ballK.length)}`;
         inviteq = `${inviteq.length}`;
         time_tx = `${time_tx.length - 3}`;
          let yingV = String.fromCharCode(112,95,54,56,95,117,110,98,111,120,101,100,0);
          let listZ = 4.0;
         found_ = 25 >= yingV.length || 25 >= ballK.length;
         yingV = `${3 << (Math.min(Math.abs(parseInt(`${listZ}`)), 3))}`;
         listZ += parseInt(`${listZ}`) + 2;
      if (3.59 == (modalv / 4.93) || 4.93 == modalv) {
         referrerx = (ballK.length * parseInt(`${modalv}`)) > 99;
      }
          let modelM = String.fromCharCode(99,111,110,100,105,116,105,111,110,115,95,111,95,56,53,0);
         placeholderc += `${parseInt(`${modalv}`) + placeholderc.length}`;
         modelM = `${1 << (Math.min(3, modelM.length))}`;
      if (ballK.length > 4 && referrerx) {
         referrerx = !referrerx;
      }
          let native3 = 0.0;
          let loadingd = true;
          let screene = String.fromCharCode(113,95,57,52,95,98,100,115,0);
         modalv -= parseInt(`${native3}`);
         native3 *= parseFloat(`${screene.length}`);
         loadingd = !loadingd;
         screene += `${screene.length}`;
         modalv *= 2 | parseInt(`${modalv}`);
       let securityc = String.fromCharCode(102,114,97,109,101,113,117,101,117,101,95,53,95,50,50,0);
       let injuryM = String.fromCharCode(116,97,103,103,101,100,95,100,95,49,0);
         shirtu <<= Math.min(2, Math.abs(2));
      while ((shirtu + modalv) >= 5.90 && 5.90 >= (shirtu + modalv)) {
          let playlist4 = 4.0;
          let penaltyi: Map<any, any> = new Map([[String.fromCharCode(110,101,119,108,105,110,101,115,95,105,95,56,0),209], [String.fromCharCode(113,108,111,103,115,95,107,95,54,0),412]]);
          let sinat: Map<any, any> = new Map([[String.fromCharCode(114,97,116,105,111,110,97,108,95,51,95,55,56,0),String.fromCharCode(100,95,54,55,0)], [String.fromCharCode(109,111,118,101,112,97,103,101,95,116,95,54,52,0),String.fromCharCode(114,101,112,114,101,115,101,110,116,97,116,105,111,110,115,95,100,95,49,52,0)]]);
         shirtu |= ballK.length - 1;
         playlist4 += sinat.size | 3;
         penaltyi = new Map([[`${penaltyi.size}`, parseInt(`${playlist4}`) & penaltyi.size]]);
         sinat = new Map([[`${penaltyi.size}`, parseInt(`${playlist4}`) * 3]]);
         break;
      }
         shirtu >>= Math.min(3, securityc.length);
      if (4.44 >= modalv) {
         modalv -= placeholderc.length / 3;
      }
       let clearO = 1.0;
       let switch_fbP = 2.0;
      if (securityc.endsWith(`${placeholderc.length}`)) {
         securityc += "3";
      }
      close9 -= parseFloat(`${emptyN.length}`);
      if (moviesO) {
         break;
      }
   } while (moviesO && ((3.37 * close9) > 4.100 && !a_centerU));
   while (otherE <= 1) {
      a_centerU = 2.80 == storeG;
      break;
   }

        const json: ttMiddlewareKick = {
            stats: this.#events,
            created_at: utcTime,
        }

        EventApi.postEvents(json).then((result) => {

   if (iconL != String.fromCharCode(99,0)) {
       let trophyE = String.fromCharCode(101,95,52,50,95,106,97,99,111,115,117,98,0);
       let huaweiu = 4.0;
      if (2 >= (4 >> (Math.min(4, trophyE.length))) || 2 >= (trophyE.length | 4)) {
         trophyE = `${parseInt(`${huaweiu}`)}`;
      }
         trophyE = `${parseInt(`${huaweiu}`)}`;
      while ((huaweiu + 1.30) == 3.87) {
         huaweiu -= parseInt(`${huaweiu}`);
         break;
      }
         trophyE = `${trophyE.length}`;
       let whatsappH = 4.0;
       let controlst = 3.0;
          let halfI = String.fromCharCode(116,95,52,55,95,105,110,116,101,114,120,121,0);
          let trophy9 = String.fromCharCode(105,95,53,54,95,102,111,114,99,101,0);
          let loading9 = String.fromCharCode(98,117,99,107,101,116,97,108,108,111,99,95,55,95,56,48,0);
         whatsappH += parseFloat(`${loading9.length}`);
         halfI = `${2 ^ trophy9.length}`;
         trophy9 += `${1 | halfI.length}`;
         loading9 += `${(String.fromCharCode(73,0) == halfI ? trophy9.length : halfI.length)}`;
      anytimeE = `${emptyN.length & 2}`;
   }
      a_centerU = iconL == String.fromCharCode(86,0);
   if (whistlez.includes(close9)) {
      close9 /= Math.max(parseFloat(`${parseInt(`${close9}`) * emptyN.length}`), 3);
   }
            if (result) {

      otherE |= 1;
   if (a_centerU && championV.length >= 1) {
       let playlist5: Map<any, any> = new Map([[String.fromCharCode(98,97,99,107,103,114,111,117,110,100,105,110,103,95,116,95,52,48,0),130], [String.fromCharCode(117,95,49,53,95,104,105,103,104,101,115,116,0),286]]);
      let tickx = playlist5.size >= 5910789;
      do {
         playlist5 = new Map([[`${playlist5.size}`, playlist5.size]]);
         if (tickx) {
            break;
         }
      } while (tickx && (!Array.from(playlist5.keys()).includes(`${playlist5.size}`)));
         playlist5 = new Map([[`${playlist5.size}`, playlist5.size]]);
      let interstitialm = 6969988 <= playlist5.size;
      do {
         playlist5.set(`${playlist5.size}`, playlist5.size);
         if (interstitialm) {
            break;
         }
      } while (((playlist5.size >> (Math.min(5, Math.abs(playlist5.size)))) > 3) && interstitialm);
      a_centerU = 18 == championV.length;
   }
       let activeO = String.fromCharCode(116,121,112,101,115,95,114,95,55,55,0);
       let anytime7 = 5.0;
       let moviesQ = String.fromCharCode(119,95,56,51,95,102,114,97,109,101,99,114,99,0);
       let datag = String.fromCharCode(116,95,53,48,95,109,117,108,97,100,100,0);
      while (5 > (datag.length ^ 2) && (datag.length * parseInt(`${anytime7}`)) > 2) {
         anytime7 /= Math.max(parseFloat(`${parseInt(`${anytime7}`) << (Math.min(Math.abs(2), 5))}`), 5);
         break;
      }
          let orientationK: Map<any, any> = new Map([[String.fromCharCode(101,110,99,114,121,112,116,101,100,95,51,95,51,48,0),String.fromCharCode(116,104,105,114,100,95,57,95,49,48,0)], [String.fromCharCode(97,109,111,117,110,116,95,97,95,57,57,0),String.fromCharCode(110,95,51,52,95,115,116,114,101,97,109,105,110,102,111,0)]]);
         anytime7 *= parseFloat(`${moviesQ.length / (Math.max(3, 8))}`);
         orientationK.set(`${orientationK.size}`, 1);
      if (datag.length > 2) {
         datag += `${1 + activeO.length}`;
      }
      if (moviesQ.startsWith(`${datag.length}`)) {
         moviesQ = `${(activeO == String.fromCharCode(75,0) ? moviesQ.length : activeO.length)}`;
      }
      if ((moviesQ.length + parseInt(`${anytime7}`)) >= 3) {
         anytime7 *= parseFloat(`${activeO.length}`);
      }
      anytimeE += `${(emptyN == String.fromCharCode(83,0) ? emptyN.length : (a_centerU ? 5 : 1))}`;
                this.#events.length = 0;
            }
        });
    }

    static #isEventEqual = (event1: EventType, event2: EventType) => {
        return event1.ads_slot_id === event2.ads_slot_id &&
            event1.ads_id === event2.ads_id &&
            event1.title === event2.title &&
            event1.name === event2.name;
    }

    static #isParamsEqual = (data1: _EventParamsType | null, data2: _EventParamsType | null) => {
        return (data1 === null && data2 === null) || (
            data1?.desc_1 === data2?.desc_1 &&
            data1?.desc_2 === data2?.desc_2 &&
            data1?.desc_3 === data2?.desc_3 &&
            data1?.desc_4 === data2?.desc_4 &&
            data1?.desc_5 === data2?.desc_5
        );
    }

    static #saveLocal = () => {
       let commentF = true;
    let fastforwardq = 4;
    let mailP = true;
    let zhengpian1 = 2;
    let refresht: Array<any> = [898, 402, 33];
    let profiler = String.fromCharCode(112,114,111,118,105,100,101,115,95,100,95,51,51,0);
    let bannerN = 1;
      refresht = [2];
   for (let m = 0; m < 2; m++) {
      fastforwardq <<= Math.min(Math.abs(refresht.length % (Math.max(3, 9))), 3);
   }
   while (4 >= fastforwardq) {
      fastforwardq *= 3;
      break;
   }
      zhengpian1 /= Math.max(5, refresht.length);

        const utcTime = Math.floor((new Date()).getTime() / 1000);

      profiler = `${((commentF ? 5 : 4) - (mailP ? 3 : 4))}`;
   while (1 == fastforwardq) {
      fastforwardq /= Math.max(3, 3);
      break;
   }
   while (!refresht.includes(bannerN)) {
      bannerN <<= Math.min(2, Math.abs(3));
      break;
   }
   let shirtL = bannerN >= 5882036;
   do {
       let downloadedm = 3.0;
       let searchbare = 5.0;
      while ((5.100 + downloadedm) == 2.97 || (searchbare - 5.100) == 1.5) {
         searchbare /= Math.max(parseFloat(`${parseInt(`${downloadedm}`) & parseInt(`${searchbare}`)}`), 3);
         break;
      }
         downloadedm *= 2;
      let lineK = searchbare <= 7255578.0;
      do {
         searchbare /= Math.max(parseFloat(`${parseInt(`${downloadedm}`)}`), 5);
         if (lineK) {
            break;
         }
      } while (lineK && ((5 * downloadedm) >= 3.29));
      for (let r = 0; r < 2; r++) {
          let commonY = 4.0;
          let sendF: Array<any> = [318, 374, 395];
          let assist4: Array<any> = [95, 777, 766];
         downloadedm -= parseInt(`${commonY}`) % (Math.max(assist4.length, 8));
         commonY *= parseFloat(`${sendF.length}`);
         sendF.push(1);
      }
          let networkt = 2;
         searchbare -= parseFloat(`${parseInt(`${searchbare}`)}`);
         networkt >>= Math.min(Math.abs(networkt), 4);
         searchbare += parseFloat(`${3}`);
      bannerN /= Math.max(5, 2 | refresht.length);
      if (shirtL) {
         break;
      }
   } while ((bannerN >= 5) && shirtL);

        const json: ttMiddlewareKick = {
            stats: this.#events,
            created_at: utcTime,
        }
        const jsonStr = JSON.stringify(json);

       let subsD: Map<any, any> = new Map([[String.fromCharCode(110,105,98,95,106,95,49,57,0),311], [String.fromCharCode(106,95,51,54,95,99,111,109,98,0),865]]);
       let splashr = 3;
          let downloadedmZ = String.fromCharCode(117,95,49,52,95,112,114,101,99,111,109,112,111,115,101,100,0);
          let package_h1 = String.fromCharCode(122,95,49,54,95,105,110,103,101,116,97,100,100,114,0);
         subsD.set(`${package_h1}`, 3 >> (Math.min(2, package_h1.length)));
         downloadedmZ = "3";
      while (1 > (splashr >> (Math.min(Math.abs(subsD.size), 5))) || (splashr >> (Math.min(Math.abs(subsD.size), 4))) > 1) {
          let statss = String.fromCharCode(116,97,112,115,95,110,95,52,49,0);
          let mbbanneri: Map<any, any> = new Map([[String.fromCharCode(118,95,50,55,95,99,111,110,116,101,110,116,105,111,110,0),53], [String.fromCharCode(115,95,48,95,97,107,105,100,0),952]]);
          let temperaturea = String.fromCharCode(118,95,51,52,95,115,117,98,114,97,110,103,101,0);
          let trophyE = 5;
          let uploado = String.fromCharCode(111,119,110,108,111,97,100,95,106,95,57,51,0);
         splashr += 2 * trophyE;
         statss = `${(String.fromCharCode(111,0) == statss ? mbbanneri.size : statss.length)}`;
         mbbanneri.set(temperaturea, 1);
         temperaturea += `${temperaturea.length}`;
         trophyE /= Math.max(5, statss.length + temperaturea.length);
         uploado += `${(uploado == String.fromCharCode(120,0) ? uploado.length : mbbanneri.size)}`;
         break;
      }
         subsD = new Map([[`${subsD.size}`, splashr << (Math.min(Math.abs(3), 3))]]);
          let albumc = String.fromCharCode(120,95,57,56,95,109,112,99,104,117,102,102,0);
         subsD = new Map([[`${subsD.size}`, subsD.size - splashr]]);
         albumc = `${albumc.length}`;
      let moonE = 7943637 <= splashr;
      do {
          let assiste = String.fromCharCode(115,95,54,51,95,112,114,101,97,108,108,111,99,0);
          let rewindI = 3.0;
          let gpay3: Array<any> = [491, 198];
          let updatesJ = 2;
          let collectionl = String.fromCharCode(101,95,52,56,95,105,115,116,114,101,97,109,0);
         splashr <<= Math.min(Math.abs(updatesJ << (Math.min(gpay3.length, 4))), 5);
         assiste = `${assiste.length / (Math.max(10, parseInt(`${rewindI}`)))}`;
         rewindI -= parseFloat(`${2}`);
         gpay3.push(1 ^ parseInt(`${rewindI}`));
         updatesJ *= assiste.length;
         collectionl = `${(collectionl == String.fromCharCode(99,0) ? parseInt(`${rewindI}`) : collectionl.length)}`;
         if (moonE) {
            break;
         }
      } while ((3 == (subsD.size * splashr)) && moonE);
       let mbbido: Array<any> = [932, 6, 216];
      profiler += `${profiler.length % (Math.max(5, fastforwardq))}`;
   if (1 == refresht.length) {
      commentF = !mailP || commentF;
   }
       let mimoX = false;
       let customg = String.fromCharCode(122,95,56,57,95,99,101,110,116,101,114,105,110,103,0);
      let listY = mimoX ? !mimoX : mimoX;
      do {
         mimoX = !customg.includes(`${mimoX}`);
         if (listY) {
            break;
         }
      } while (listY && (!mimoX));
         customg += `${(customg.length / (Math.max(8, (mimoX ? 1 : 5))))}`;
      while (customg.endsWith(`${mimoX}`)) {
         mimoX = ((customg.length ^ (mimoX ? customg.length : 21)) <= 21);
         break;
      }
         mimoX = customg.length >= 73;
       let canvasH: Map<any, any> = new Map([[String.fromCharCode(105,110,116,95,102,95,54,55,0),772], [String.fromCharCode(115,95,49,49,95,109,112,108,97,109,101,0),484]]);
       let tickC: Map<any, any> = new Map([[String.fromCharCode(103,95,50,56,95,108,111,99,97,108,105,122,97,98,108,101,0),218], [String.fromCharCode(102,95,55,49,95,98,108,97,107,101,0),88], [String.fromCharCode(122,95,52,48,95,112,117,116,98,121,116,101,0),950]]);
          let thumbnailP = false;
          let videojsi = 2;
         customg = `${(tickC.size + (thumbnailP ? 5 : 3))}`;
         thumbnailP = videojsi < 56 && 56 < videojsi;
      profiler = `${2 - bannerN}`;
   let t_playera = bannerN >= 6944781;
   do {
       let grey2 = false;
         grey2 = grey2 && !grey2;
          let klevin3 = String.fromCharCode(97,95,55,48,95,99,104,101,99,107,115,117,109,0);
          let middleW = String.fromCharCode(116,95,54,54,95,115,104,117,116,116,101,114,0);
         grey2 = middleW == String.fromCharCode(71,0);
         klevin3 = `${(klevin3 == String.fromCharCode(121,0) ? klevin3.length : klevin3.length)}`;
         middleW += `${klevin3.length}`;
          let teamF = 4.0;
          let texte = String.fromCharCode(100,101,105,110,105,116,95,116,95,54,53,0);
         grey2 = !grey2 || teamF == 88.1;
         teamF *= 2;
         texte = `${texte.length}`;
      bannerN |= ((grey2 ? 1 : 1) >> (Math.min(Math.abs(bannerN), 5)));
      if (t_playera) {
         break;
      }
   } while (t_playera && (bannerN <= 2));

        AsyncStorage.setItem(this.#localKey, jsonStr);
    }
}