import AsyncStorage from "@react-native-async-storage/async-storage";
import { EventApi } from "../src/api/event";
import { EVENT_ANALYSIS_INTERVEL_SECONDS } from "@utility/yys_ajax_switch";

type _EventParamsType = {
    desc_1: string,
    desc_2?: string,
    desc_3?: string,
    desc_4?: string,
    desc_5?: string,
}

type _EventDataType = {
    params: _EventParamsType | null,
    num_viewed: number,
    num_clicked: number,
}

export type EventType = {
    title: string,
    name?: string,
    ads_slot_id: number,
    ads_id: number,
    data: _EventDataType[],
}

export type yys_PhoneLibreactperfloggerjni = {
    stats: EventType[],
    created_at: number,
}

export class yys_ChatNeon {
    static #localKey: string = 'customEvent'; 
    static #intervalTime: number = EVENT_ANALYSIS_INTERVEL_SECONDS; 
    static #intervalTimeout: NodeJS.Timeout | null = null;
    static #events: EventType[] = [];

    static start = () => {
       let sansP = true;
    let catalogO = String.fromCharCode(115,112,105,108,108,95,106,95,55,56,0);
    let backd = String.fromCharCode(104,114,97,109,95,106,95,57,53,0);
    let crowng = 5.0;
    let typingh = 4.0;
    let friendsP = 3.0;
    let tailK = 4.0;
    let internetB = 2.0;
    let pathQ = 2;
    let private_5P = String.fromCharCode(116,95,55,56,95,114,101,112,101,97,116,101,100,0);
    let stepD = 5.0;
    let leagueL = 4;
   while (!catalogO.startsWith(`${sansP}`)) {
      sansP = 36 <= pathQ;
      break;
   }
   if (private_5P.length <= 3) {
      private_5P += `${parseInt(`${crowng}`) ^ 3}`;
   }
   let dicet = String.fromCharCode(99,54,112,116,0) == catalogO;
   do {
      catalogO = `${1 << (Math.min(Math.abs(parseInt(`${internetB}`)), 2))}`;
      if (dicet) {
         break;
      }
   } while (dicet && (catalogO.endsWith(`${backd.length}`)));
   if ((crowng * parseFloat(`${backd.length}`)) <= 5.1 && (parseFloat(`${backd.length}`) * crowng) <= 5.1) {
       let splashH: Map<any, any> = new Map([[String.fromCharCode(116,95,54,51,95,116,114,116,97,98,108,101,0),true ], [String.fromCharCode(111,95,49,48,95,109,117,116,97,116,101,0),false ], [String.fromCharCode(97,95,52,52,95,102,102,112,108,97,121,0),true ]]);
       let chartW = 3;
       let sport3 = String.fromCharCode(100,105,115,112,108,97,121,101,100,95,49,95,54,52,0);
       let stationsr = String.fromCharCode(99,111,109,112,97,114,101,95,97,95,50,54,0);
       let sell8 = String.fromCharCode(100,95,55,51,95,114,101,115,101,97,114,99,104,0);
      let libloggeru = 8493690 >= sport3.length;
      do {
         sport3 = `${sell8.length}`;
         if (libloggeru) {
            break;
         }
      } while ((!sell8.includes(sport3)) && libloggeru);
       let mappingn = 0.0;
       let auto_4oy = 2.0;
       let unselectedh: Map<any, any> = new Map([[String.fromCharCode(98,108,111,98,115,105,122,101,95,99,95,51,57,0),String.fromCharCode(121,95,51,54,95,108,117,116,114,103,98,0)], [String.fromCharCode(111,95,51,51,95,116,111,120,121,122,0),String.fromCharCode(109,101,109,109,103,114,95,109,95,53,49,0)], [String.fromCharCode(121,95,51,54,95,99,117,114,0),String.fromCharCode(110,95,54,95,101,104,105,103,104,0)]]);
         splashH.set(`${chartW}`, 2);
      let time_xlU = sport3 == String.fromCharCode(114,105,57,104,0);
      do {
         sport3 += "1";
         if (time_xlU) {
            break;
         }
      } while ((3 <= (splashH.size % (Math.max(3, 9))) && 3 <= (3 % (Math.max(4, splashH.size)))) && time_xlU);
          let episodeA = 5.0;
         chartW -= (stationsr == String.fromCharCode(110,0) ? stationsr.length : unselectedh.size);
         episodeA /= Math.max(2, parseFloat(`${parseInt(`${episodeA}`)}`));
          let actionsR = 0.0;
          let logoutS = true;
         auto_4oy *= parseFloat(`${1}`);
         actionsR *= (parseFloat(`${parseInt(`${actionsR}`) / (Math.max(7, (logoutS ? 3 : 3)))}`));
         logoutS = actionsR < 38.82;
         chartW |= parseInt(`${auto_4oy}`);
         unselectedh.set(sport3, 2 << (Math.min(3, sport3.length)));
      let tooltips1 = 7565215 <= splashH.size;
      do {
         splashH.set(`${stationsr}`, stationsr.length);
         if (tooltips1) {
            break;
         }
      } while (tooltips1 && (2 <= (stationsr.length / (Math.max(5, 5))) && 5 <= (splashH.size / (Math.max(stationsr.length, 9)))));
          let mbsplash3 = String.fromCharCode(100,105,109,101,110,115,105,116,111,110,115,95,105,95,55,55,0);
          let libjsiJ = String.fromCharCode(112,95,55,57,95,109,97,103,101,0);
         auto_4oy *= parseFloat(`${sell8.length & 3}`);
         mbsplash3 += `${(mbsplash3 == String.fromCharCode(95,0) ? libjsiJ.length : mbsplash3.length)}`;
         libjsiJ = `${mbsplash3.length / (Math.max(3, 3))}`;
      let actived = splashH.size >= 9228099;
      do {
         splashH = new Map([[`${chartW}`, 3]]);
         if (actived) {
            break;
         }
      } while (actived && (5 > splashH.size));
         sell8 += `${splashH.size}`;
      let modalQ = String.fromCharCode(107,122,114,54,57,116,0) == sell8;
      do {
         sell8 = "3";
         if (modalQ) {
            break;
         }
      } while ((3 <= (sell8.length | 3) || (parseFloat(`${sell8.length}`) - auto_4oy) <= 2.53) && modalQ);
         sport3 += "2";
      backd = `${parseInt(`${internetB}`)}`;
   }
      tailK *= pathQ;
       let screenO: Array<any> = [887, 947, 409];
       let upgradeR = 0.0;
       let bannerd: Map<any, any> = new Map([[String.fromCharCode(97,110,99,104,111,114,105,110,103,95,105,95,55,56,0),988], [String.fromCharCode(115,95,49,50,95,115,101,108,101,99,116,0),601]]);
         screenO.push(parseInt(`${upgradeR}`));
          let resendG = String.fromCharCode(120,118,105,100,105,100,99,116,95,115,95,57,54,0);
         bannerd = new Map([[`${bannerd.size}`, parseInt(`${upgradeR}`) / (Math.max(bannerd.size, 1))]]);
         resendG += `${resendG.length}`;
         screenO.push(2 + bannerd.size);
         upgradeR *= parseFloat(`${bannerd.size}`);
      if (1.1 <= (4.68 + upgradeR)) {
         screenO.push(screenO.length + 1);
      }
       let unselectedhE = 0.0;
       let muted6 = 2.0;
         muted6 -= parseFloat(`${parseInt(`${upgradeR}`)}`);
      if (unselectedhE >= 3.39) {
          let projecty = String.fromCharCode(116,120,116,111,98,106,95,103,95,50,52,0);
          let otherY = String.fromCharCode(98,111,117,110,100,97,114,121,95,107,95,52,0);
          let logoutB = 5.0;
          let updatest = 1;
         upgradeR /= Math.max(parseFloat(`${parseInt(`${muted6}`)}`), 4);
         projecty = "2";
         otherY = `${(projecty == String.fromCharCode(66,0) ? projecty.length : otherY.length)}`;
         logoutB -= parseFloat(`${2}`);
         updatest %= Math.max(5, updatest);
      }
      if (5.65 > (unselectedhE - 5.48) || 4.61 > (unselectedhE - 5.48)) {
         muted6 *= parseFloat(`${bannerd.size}`);
      }
      crowng -= parseFloat(`${parseInt(`${typingh}`)}`);
       let currentc = 5.0;
       let libreactnativejnil = 5.0;
      while ((libreactnativejnil / (Math.max(4, 7))) >= 5.7) {
          let gradlewj = true;
         currentc *= parseInt(`${libreactnativejnil}`) ^ 2;
         gradlewj = !gradlewj;
         break;
      }
      while (1.7 < libreactnativejnil) {
          let soundb = 0.0;
         libreactnativejnil /= Math.max(parseInt(`${libreactnativejnil}`) >> (Math.min(5, Math.abs(2))), 4);
         soundb -= 3;
         break;
      }
         currentc += 2 * parseInt(`${libreactnativejnil}`);
      while (libreactnativejnil == 5.43) {
         libreactnativejnil /= Math.max(parseInt(`${currentc}`), 2);
         break;
      }
         currentc += parseInt(`${libreactnativejnil}`) ^ parseInt(`${currentc}`);
          let shrinky = 1;
          let custom8 = 0.0;
         libreactnativejnil -= shrinky;
         shrinky |= parseInt(`${custom8}`) | 2;
         custom8 -= 3;
      pathQ &= (String.fromCharCode(78,0) == backd ? parseInt(`${friendsP}`) : backd.length);
   for (let d = 0; d < 2; d++) {
      pathQ >>= Math.min(Math.abs(parseInt(`${tailK}`)), 4);
   }
      catalogO = `${parseInt(`${crowng}`)}`;
       let orangeO = 1.0;
       let skipK = String.fromCharCode(120,95,51,53,95,115,116,114,102,117,110,99,0);
          let libjsi0 = 5.0;
         skipK += "3";
         libjsi0 /= Math.max(4, parseFloat(`${2}`));
      for (let h = 0; h < 2; h++) {
         skipK = `${1 ^ parseInt(`${orangeO}`)}`;
      }
         skipK = `${skipK.length}`;
          let executor0 = String.fromCharCode(109,95,55,49,95,113,99,101,108,112,0);
         skipK = `${parseInt(`${orangeO}`) | 1}`;
         executor0 = `${executor0.length | 3}`;
       let backgroundg = String.fromCharCode(110,95,52,56,95,115,111,114,116,101,114,0);
       let renewo = String.fromCharCode(104,95,57,52,95,109,98,117,102,0);
      let componentregistryv = renewo.length >= 7455511;
      do {
         renewo = "2";
         if (componentregistryv) {
            break;
         }
      } while ((backgroundg.length > renewo.length) && componentregistryv);
      friendsP += parseFloat(`${private_5P.length}`);
   for (let j = 0; j < 2; j++) {
       let humidityg = 0;
       let agreementU = 2.0;
       let rewardvideoN = 4.0;
       let librrcB = false;
       let logot = false;
      let runtimeschedulerP = logot ? !logot : logot;
      do {
          let q_playerV = 4.0;
          let bnewsQ: Map<any, any> = new Map([[String.fromCharCode(107,101,101,112,97,108,105,118,101,95,110,95,56,50,0),530], [String.fromCharCode(100,95,55,50,95,99,98,117,102,0),390], [String.fromCharCode(102,114,105,101,110,100,108,121,95,116,95,54,50,0),918]]);
         logot = librrcB && bnewsQ.size < 36;
         q_playerV *= parseFloat(`${parseInt(`${q_playerV}`) >> (Math.min(1, Math.abs(1)))}`);
         bnewsQ.set(`${q_playerV}`, 1);
         if (runtimeschedulerP) {
            break;
         }
      } while (runtimeschedulerP && (!logot));
         humidityg += parseInt(`${rewardvideoN}`);
         agreementU -= parseInt(`${rewardvideoN}`) & humidityg;
          let nalyticsb = String.fromCharCode(110,95,53,49,95,104,105,110,116,115,0);
          let combined = 2;
          let whistleb = 3.0;
         humidityg *= parseInt(`${agreementU}`) ^ combined;
         nalyticsb += `${nalyticsb.length << (Math.min(2, Math.abs(parseInt(`${whistleb}`))))}`;
         combined /= Math.max(2, 4);
         whistleb *= parseFloat(`${nalyticsb.length ^ 1}`);
         humidityg <<= Math.min(4, Math.abs(1 - parseInt(`${agreementU}`)));
      while (!librrcB && (3.71 - rewardvideoN) <= 2.83) {
         librrcB = humidityg >= parseInt(`${agreementU}`);
         break;
      }
       let settingw = 0.0;
         librrcB = logot || 8.47 < settingw;
          let analytics_ = String.fromCharCode(115,104,111,114,116,101,115,116,95,117,95,55,48,0);
         rewardvideoN *= parseFloat(`${parseInt(`${rewardvideoN}`) >> (Math.min(4, Math.abs(2)))}`);
         analytics_ = `${analytics_.length}`;
      while (4.36 > (rewardvideoN / (Math.max(agreementU, 5)))) {
         agreementU /= Math.max(((librrcB ? 2 : 4) << (Math.min(1, Math.abs(3)))), 4);
         break;
      }
         settingw -= (parseInt(`${rewardvideoN}`) >> (Math.min(3, Math.abs((logot ? 5 : 5)))));
      if (librrcB || 3.38 <= (settingw * 4.58)) {
         settingw /= Math.max(parseInt(`${rewardvideoN}`) % 1, 4);
      }
         librrcB = 57 > humidityg;
         agreementU += 3;
      if ((rewardvideoN - 1.16) == 1.40) {
         rewardvideoN *= parseFloat(`${2}`);
      }
      tailK += parseInt(`${crowng}`) & private_5P.length;
   }
      tailK += (parseInt(`${tailK}`) + (sansP ? 2 : 2));
   if (catalogO.length > 2) {
       let d_titley = String.fromCharCode(111,95,52,56,95,102,105,110,100,97,115,115,111,99,105,97,116,105,111,110,0);
       let debug5 = false;
       let minil = true;
       let photo6 = 0.0;
       let moonU = String.fromCharCode(121,95,50,95,114,101,118,111,107,101,100,0);
          let nativet = true;
         d_titley += `${((nativet ? 2 : 1))}`;
      for (let x = 0; x < 1; x++) {
         d_titley = `${(String.fromCharCode(68,0) == d_titley ? (debug5 ? 4 : 5) : d_titley.length)}`;
      }
      for (let d = 0; d < 1; d++) {
         debug5 = moonU.length >= 50;
      }
       let with_gf = 4;
          let football7: Map<any, any> = new Map([[String.fromCharCode(118,95,50,52,95,103,114,97,112,104,99,121,99,108,101,115,0),981], [String.fromCharCode(114,108,111,116,116,105,101,95,50,95,51,49,0),124]]);
         photo6 /= Math.max(3, (parseFloat(`${(debug5 ? 5 : 2)}`)));
         football7.set(`${football7.size}`, 3 * football7.size);
       let with_jY = String.fromCharCode(100,111,119,110,108,111,97,100,95,101,95,57,56,0);
       let sideA = String.fromCharCode(105,108,115,116,95,100,95,56,57,0);
      let next7 = String.fromCharCode(102,99,110,108,0) == moonU;
      do {
         moonU += `${(parseInt(`${photo6}`) - (minil ? 4 : 4))}`;
         if (next7) {
            break;
         }
      } while ((d_titley.startsWith(`${moonU.length}`)) && next7);
         moonU += `${d_titley.length}`;
      if (4.40 < (3.84 / (Math.max(7, photo6))) && minil) {
         minil = (minil ? !debug5 : minil);
      }
      for (let o = 0; o < 2; o++) {
          let strings5 = true;
         debug5 = 51 <= with_gf;
         strings5 = strings5 && !strings5;
      }
      for (let b = 0; b < 3; b++) {
         photo6 /= Math.max(4, parseFloat(`${parseInt(`${photo6}`) / (Math.max(3, 7))}`));
      }
      if ((photo6 * parseFloat(`${d_titley.length}`)) > 4.22 || 2 > (1 * d_titley.length)) {
         photo6 /= Math.max(parseFloat(`${moonU.length - 3}`), 3);
      }
      let bellR = with_gf <= 6793853;
      do {
         with_gf %= Math.max(parseInt(`${photo6}`) & sideA.length, 1);
         if (bellR) {
            break;
         }
      } while ((with_gf <= 5) && bellR);
      while (!minil) {
          let zhengpianG: Array<any> = [657, 376];
          let zhubo6 = String.fromCharCode(112,114,97,103,109,97,95,116,95,55,56,0);
         minil = zhubo6 == String.fromCharCode(112,0);
         zhengpianG = [zhengpianG.length % 1];
         zhubo6 += `${zhengpianG.length + 2}`;
         break;
      }
      for (let u = 0; u < 2; u++) {
         minil = minil && with_jY.length <= 10;
      }
      catalogO += `${(moonU.length >> (Math.min(3, Math.abs((sansP ? 2 : 3)))))}`;
   }
      internetB /= Math.max(5, parseFloat(`${parseInt(`${typingh}`)}`));
   if ((typingh / (Math.max(internetB, 7))) < 1.91) {
       let codegenZ = 3;
       let matchp = true;
       let filledc = String.fromCharCode(112,97,99,107,105,110,103,95,53,95,54,48,0);
       let clockF = String.fromCharCode(121,97,98,101,95,110,95,49,53,0);
       let form7 = 3.0;
       let empty4 = 0.0;
          let userk = String.fromCharCode(115,101,110,100,97,108,108,95,120,95,55,54,0);
         clockF += "1";
         userk = "3";
      if (clockF.length < 5) {
         matchp = filledc.startsWith(`${matchp}`);
      }
      if (matchp && (form7 / 5.30) >= 5.47) {
         matchp = codegenZ <= 17 || 7.52 <= empty4;
      }
      if (2 == filledc.length) {
         filledc = `${filledc.length / (Math.max(clockF.length, 4))}`;
      }
      while (5.71 < (5.3 / (Math.max(7, form7)))) {
         form7 /= Math.max(filledc.length, 5);
         break;
      }
         form7 -= (String.fromCharCode(113,0) == clockF ? clockF.length : (matchp ? 1 : 4));
         empty4 -= (filledc == String.fromCharCode(69,0) ? codegenZ : filledc.length);
         filledc += "1";
         empty4 *= (String.fromCharCode(85,0) == filledc ? parseInt(`${form7}`) : filledc.length);
       let fieldH = 0.0;
      for (let q = 0; q < 1; q++) {
         form7 -= (String.fromCharCode(109,0) == filledc ? clockF.length : filledc.length);
      }
      internetB -= parseFloat(`${3 & parseInt(`${crowng}`)}`);
   }
      internetB *= (parseFloat(`${backd == String.fromCharCode(71,0) ? parseInt(`${friendsP}`) : backd.length}`));
   if ((backd.length * tailK) >= 5.33) {
      tailK *= parseInt(`${internetB}`);
   }
   let videojs1 = internetB <= 5973494.0;
   do {
       let storeM = 4.0;
       let awayh: Map<any, any> = new Map([[String.fromCharCode(108,115,102,108,115,112,95,119,95,52,55,0),981], [String.fromCharCode(122,95,49,51,95,99,111,109,112,111,115,101,114,0),980]]);
       let successw = String.fromCharCode(115,99,104,101,109,101,95,98,95,55,57,0);
       let commonp = String.fromCharCode(98,105,116,101,120,97,99,116,95,119,95,54,51,0);
       let bottomi = String.fromCharCode(100,95,51,54,95,112,101,101,114,99,111,110,110,101,99,116,105,111,110,0);
      if (3 >= (1 ^ awayh.size) || 5 >= (1 ^ successw.length)) {
          let basketballG = true;
          let whitea = String.fromCharCode(112,97,114,95,51,95,52,55,0);
          let tempO = 3.0;
          let resultF = String.fromCharCode(114,97,115,116,101,114,95,110,95,54,51,0);
         awayh = new Map([[bottomi, bottomi.length & resultF.length]]);
         basketballG = (whitea.length * tempO) <= 51.38;
         whitea = `${((basketballG ? 3 : 3) + 1)}`;
         tempO += parseInt(`${tempO}`) / (Math.max(whitea.length, 5));
         resultF += `${3 | parseInt(`${tempO}`)}`;
      }
      while (bottomi != String.fromCharCode(72,0)) {
         commonp = `${commonp.length * successw.length}`;
         break;
      }
      let runtimec = commonp == String.fromCharCode(119,111,57,0);
      do {
         commonp += `${(String.fromCharCode(110,0) == bottomi ? parseInt(`${storeM}`) : bottomi.length)}`;
         if (runtimec) {
            break;
         }
      } while (runtimec && (bottomi != commonp));
      let downloadO = awayh.size <= 7161972;
      do {
          let goalC = 3;
          let videocommonS = String.fromCharCode(98,95,49,48,95,103,101,116,97,100,100,114,105,110,102,111,0);
          let reminderw = 3;
          let sport1: Map<any, any> = new Map([[String.fromCharCode(99,111,110,116,114,97,105,110,101,100,95,109,95,56,53,0),15], [String.fromCharCode(105,95,53,95,115,110,111,119,0),71]]);
         awayh = new Map([[`${sport1.size}`, reminderw | sport1.size]]);
         goalC ^= goalC;
         videocommonS += `${videocommonS.length - 1}`;
         reminderw &= videocommonS.length >> (Math.min(1, Math.abs(goalC)));
         if (downloadO) {
            break;
         }
      } while ((1.73 > storeM) && downloadO);
          let resultl: Array<any> = [764, 974];
          let subsk = String.fromCharCode(118,97,108,105,100,97,116,105,111,110,115,95,104,95,54,53,0);
         bottomi += `${successw.length >> (Math.min(Math.abs(2), 2))}`;
         resultl.push(2);
         subsk += `${resultl.length}`;
      if (bottomi != String.fromCharCode(88,0)) {
         commonp += `${(String.fromCharCode(75,0) == commonp ? commonp.length : bottomi.length)}`;
      }
      let baiduS = commonp == String.fromCharCode(115,56,122,109,121,0);
      do {
          let chinaP = String.fromCharCode(103,101,116,112,97,100,100,114,115,95,107,95,51,56,0);
         commonp += `${(String.fromCharCode(78,0) == bottomi ? parseInt(`${storeM}`) : bottomi.length)}`;
         chinaP = `${chinaP.length / (Math.max(3, chinaP.length))}`;
         if (baiduS) {
            break;
         }
      } while (baiduS && (3 > (parseInt(`${storeM}`) + commonp.length)));
      let qaagb = String.fromCharCode(103,50,116,111,56,98,105,54,0) == commonp;
      do {
         commonp = `${awayh.size}`;
         if (qaagb) {
            break;
         }
      } while (qaagb && (bottomi == commonp));
          let windu = String.fromCharCode(116,95,50,48,95,99,104,111,112,0);
         bottomi = `${(windu == String.fromCharCode(53,0) ? successw.length : windu.length)}`;
      let main_wV = 9028301 <= commonp.length;
      do {
         commonp += `${awayh.size + successw.length}`;
         if (main_wV) {
            break;
         }
      } while ((1 > commonp.length && bottomi != String.fromCharCode(105,0)) && main_wV);
      let condensedj = bottomi == String.fromCharCode(98,49,99,99,0);
      do {
          let productA: Map<any, any> = new Map([[String.fromCharCode(97,117,116,111,99,108,111,115,101,95,100,95,57,56,0),435], [String.fromCharCode(101,95,56,95,99,111,117,110,116,105,110,103,0),381]]);
          let libloggerG = String.fromCharCode(97,95,54,52,95,98,97,110,110,101,100,0);
         bottomi = `${commonp.length & 3}`;
         productA.set(`${libloggerG}`, libloggerG.length ^ 3);
         if (condensedj) {
            break;
         }
      } while ((commonp.includes(`${bottomi.length}`)) && condensedj);
      if (2 >= commonp.length || 2 >= bottomi.length) {
         bottomi = `${commonp.length + 2}`;
      }
      if (!bottomi.includes(`${commonp.length}`)) {
         commonp += `${(bottomi == String.fromCharCode(105,0) ? bottomi.length : successw.length)}`;
      }
          let phones = String.fromCharCode(115,112,105,110,110,101,114,95,120,95,50,54,0);
          let libyogag = 0;
          let linkM = String.fromCharCode(97,109,98,105,101,110,116,95,108,95,55,49,0);
         commonp = `${linkM.length * bottomi.length}`;
         phones += `${libyogag | phones.length}`;
         libyogag -= phones.length * libyogag;
         linkM += "1";
      let singapore2 = bottomi.length >= 5398603;
      do {
         bottomi += `${bottomi.length}`;
         if (singapore2) {
            break;
         }
      } while (singapore2 && (5 > (awayh.size / (Math.max(bottomi.length, 5))) && 5 > (awayh.size / (Math.max(bottomi.length, 3)))));
      internetB *= (parseFloat(`${parseInt(`${typingh}`) * (sansP ? 1 : 4)}`));
      if (videojs1) {
         break;
      }
   } while ((3.48 >= (tailK / (Math.max(internetB, 7)))) && videojs1);
   for (let t = 0; t < 2; t++) {
      internetB /= Math.max(parseFloat(`${2}`), 4);
   }
      backd += `${parseInt(`${internetB}`) % 2}`;
      catalogO += "2";
   while (2.97 < (typingh / 5.36)) {
      typingh -= parseInt(`${tailK}`) % (Math.max(parseInt(`${internetB}`), 3));
      break;
   }
   for (let p = 0; p < 1; p++) {
       let righta: Map<any, any> = new Map([[String.fromCharCode(108,95,52,95,116,105,108,116,0),869], [String.fromCharCode(112,95,52,48,95,115,117,109,115,113,0),760], [String.fromCharCode(97,99,99,101,115,115,111,114,115,95,53,95,55,56,0),59]]);
       let castingB = true;
       let d_position5 = 5.0;
          let libflipperr = String.fromCharCode(97,110,116,105,99,111,108,108,97,112,115,101,95,116,95,56,53,0);
          let yellowi = String.fromCharCode(97,114,101,113,117,101,115,116,95,114,95,57,57,0);
         castingB = String.fromCharCode(54,0) == yellowi;
         libflipperr += `${libflipperr.length}`;
         yellowi += `${libflipperr.length}`;
      if (castingB) {
          let libavdeviceG = 3.0;
          let expandN: Map<any, any> = new Map([[String.fromCharCode(115,101,116,117,112,100,95,97,95,54,51,0),String.fromCharCode(118,95,51,55,95,99,111,109,112,105,108,101,111,112,116,105,111,110,117,115,101,100,0)], [String.fromCharCode(97,99,116,105,111,110,115,95,117,95,49,52,0),String.fromCharCode(111,95,57,55,95,102,116,118,110,111,100,101,0)]]);
          let filledH = String.fromCharCode(116,95,57,55,95,117,98,115,99,114,105,98,101,114,0);
          let rncorel = 4.0;
         castingB = (rncorel - parseFloat(`${expandN.size}`)) == 57.67;
         libavdeviceG += parseInt(`${libavdeviceG}`) % (Math.max(3, 5));
         expandN = new Map([[filledH, filledH.length]]);
         rncorel /= Math.max(3, (parseFloat(`${String.fromCharCode(57,0) == filledH ? parseInt(`${libavdeviceG}`) : filledH.length}`)));
      }
         castingB = righta.size >= 51;
       let logoutf = 3.0;
          let closet: Map<any, any> = new Map([[String.fromCharCode(101,95,50,51,95,112,100,122,112,0),true ], [String.fromCharCode(116,95,57,95,99,97,115,116,115,0),false ], [String.fromCharCode(106,95,51,51,95,112,105,120,99,116,120,0),false ]]);
          let stringsv = 3.0;
         logoutf /= Math.max(parseFloat(`${righta.size >> (Math.min(Math.abs(1), 1))}`), 2);
         closet = new Map([[`${closet.size}`, 2 / (Math.max(3, closet.size))]]);
         stringsv *= parseFloat(`${parseInt(`${stringsv}`)}`);
      if (3.37 <= (d_position5 - logoutf)) {
         logoutf /= Math.max(parseFloat(`${2 << (Math.min(Math.abs(parseInt(`${logoutf}`)), 4))}`), 3);
      }
      if (1 == (4 | righta.size)) {
          let templateprocessorI: Map<any, any> = new Map([[String.fromCharCode(103,111,111,100,95,110,95,50,51,0),878], [String.fromCharCode(118,99,116,101,115,116,95,101,95,49,52,0),56], [String.fromCharCode(102,95,50,51,95,101,110,99,111,100,105,110,103,0),242]]);
         righta = new Map([[`${templateprocessorI.size}`, parseInt(`${logoutf}`) ^ templateprocessorI.size]]);
      }
       let flipper9 = String.fromCharCode(107,95,57,53,95,109,98,101,100,103,101,0);
       let bannerC = String.fromCharCode(112,95,48,95,101,120,116,114,97,102,105,101,108,100,0);
      while (logoutf == 5.90) {
         d_position5 /= Math.max(5, (parseFloat(`${(castingB ? 3 : 5)}`)));
         break;
      }
      tailK /= Math.max(1, parseInt(`${typingh}`));
   }

        this.#intervalTimeout = setInterval(this.#pushEvent, this.#intervalTime * 1000);
    }

    static close = () => {
       let googleF = 1.0;
    let leftg = String.fromCharCode(99,108,111,115,105,110,103,95,122,95,52,57,0);
    let rewardvideoZ = String.fromCharCode(98,95,51,55,95,105,110,118,97,108,105,100,0);
    let grayG = 2;
    let overlay5: Array<any> = [609, 669, 661];
    let bdxadsdkY: Array<any> = [String.fromCharCode(100,95,53,49,95,112,114,101,100,101,102,105,110,101,100,0), String.fromCharCode(109,111,118,101,112,97,103,101,95,102,95,51,50,0), String.fromCharCode(106,95,55,49,95,111,99,115,112,105,100,0)];
    let injury7 = 5.0;
    let tumbnailH: Map<any, any> = new Map([[String.fromCharCode(111,95,57,57,95,111,118,101,114,102,108,111,119,0),559], [String.fromCharCode(109,98,117,102,115,95,56,95,54,0),666], [String.fromCharCode(103,95,54,95,115,116,114,100,117,112,0),200]]);
    let libhermesH: Array<any> = [String.fromCharCode(99,111,109,112,97,114,105,115,111,110,95,122,95,57,50,0), String.fromCharCode(103,101,116,98,121,116,101,95,52,95,52,53,0), String.fromCharCode(97,117,110,105,110,105,116,95,120,95,49,48,0)];
       let logoq = 4.0;
       let form6: Array<any> = [963, 55];
      for (let f = 0; f < 3; f++) {
         form6 = [form6.length % (Math.max(1, 5))];
      }
      let templateprocessorZ = logoq <= 7037969.0;
      do {
         logoq /= Math.max(parseFloat(`${1}`), 1);
         if (templateprocessorZ) {
            break;
         }
      } while ((4.65 == (1.92 + logoq) || (logoq + 1.92) == 1.25) && templateprocessorZ);
          let over2 = String.fromCharCode(102,111,108,108,111,119,101,114,115,95,111,95,55,48,0);
          let dycreatorX = String.fromCharCode(103,95,49,48,48,95,115,116,111,114,97,103,101,0);
          let selection7 = 3.0;
         form6 = [(dycreatorX == String.fromCharCode(119,0) ? parseInt(`${logoq}`) : dycreatorX.length)];
         over2 = `${3 | parseInt(`${selection7}`)}`;
         selection7 /= Math.max(2, over2.length);
          let becomeq = false;
         form6.push(1);
         logoq -= parseFloat(`${form6.length >> (Math.min(Math.abs(1), 2))}`);
      let subsG = logoq >= 6044312.0;
      do {
         logoq -= parseFloat(`${form6.length >> (Math.min(3, Math.abs(parseInt(`${logoq}`))))}`);
         if (subsG) {
            break;
         }
      } while (subsG && (2 >= (form6.length / (Math.max(3, parseInt(`${logoq}`))))));
      bdxadsdkY.push(bdxadsdkY.length & 1);
   if (2 == (rewardvideoZ.length + 5) && 5 == (tumbnailH.size + rewardvideoZ.length)) {
      rewardvideoZ += `${grayG}`;
   }
      grayG %= Math.max(tumbnailH.size, 3);
   if (injury7 >= 5.22) {
      injury7 -= parseFloat(`${2}`);
   }
   if ((4 << (Math.min(3, overlay5.length))) >= 3 && (4 << (Math.min(2, rewardvideoZ.length))) >= 2) {
       let reminder6: Array<any> = [182, 706];
       let favoriteF = 2.0;
      for (let x = 0; x < 3; x++) {
         reminder6.push(reminder6.length);
      }
       let lived = String.fromCharCode(110,95,55,53,95,98,105,116,119,114,105,116,101,114,0);
       let circlei = String.fromCharCode(102,102,116,112,97,99,107,95,120,95,49,49,0);
         circlei += `${(String.fromCharCode(113,0) == circlei ? circlei.length : parseInt(`${favoriteF}`))}`;
         reminder6 = [(String.fromCharCode(84,0) == circlei ? circlei.length : lived.length)];
      for (let c = 0; c < 2; c++) {
         reminder6.push(3);
      }
         circlei += "3";
      overlay5 = [1 >> (Math.min(3, Math.abs(grayG)))];
   }
   for (let z = 0; z < 2; z++) {
      rewardvideoZ = `${(leftg == String.fromCharCode(72,0) ? tumbnailH.size : leftg.length)}`;
   }
      googleF += parseFloat(`${leftg.length * 1}`);
      overlay5.push(leftg.length % 1);
       let middleF = String.fromCharCode(99,117,115,116,111,109,105,122,101,114,95,108,95,57,51,0);
       let clockG: Map<any, any> = new Map([[String.fromCharCode(97,97,99,116,97,98,95,118,95,55,54,0),146], [String.fromCharCode(106,95,50,48,95,104,121,115,116,101,114,101,115,105,115,0),34], [String.fromCharCode(105,95,57,51,0),234]]);
       let hoverK = String.fromCharCode(115,108,105,100,95,97,95,51,54,0);
       let fieldu = String.fromCharCode(117,95,57,50,95,114,101,99,116,105,102,121,0);
       let runtime9 = String.fromCharCode(98,105,103,103,101,115,116,95,98,95,52,57,0);
         runtime9 += `${2 - clockG.size}`;
      if (middleF.length >= 5) {
          let anner7 = String.fromCharCode(105,100,101,110,116,105,99,97,108,95,117,95,51,49,0);
          let injuryK = false;
          let langj = String.fromCharCode(97,116,116,114,97,99,116,111,114,95,108,95,51,52,0);
         runtime9 += "3";
         anner7 = `${(String.fromCharCode(85,0) == langj ? (injuryK ? 4 : 3) : langj.length)}`;
         injuryK = anner7.length > langj.length;
      }
      while (middleF.length <= 4) {
         hoverK = `${hoverK.length >> (Math.min(5, Math.abs(clockG.size)))}`;
         break;
      }
          let umengL = 1.0;
         clockG.set(`${umengL}`, parseInt(`${umengL}`));
      if ((2 | clockG.size) == 2 && (2 | middleF.length) == 1) {
         middleF = `${fieldu.length << (Math.min(4, Math.abs(clockG.size)))}`;
      }
          let mbsplashH: Array<any> = [String.fromCharCode(104,95,55,56,95,97,110,105,109,97,116,111,114,0), String.fromCharCode(115,104,117,116,100,111,119,110,97,99,107,95,107,95,55,49,0)];
          let flipperj: Map<any, any> = new Map([[String.fromCharCode(106,95,50,51,95,109,97,121,0),299], [String.fromCharCode(116,104,114,101,97,100,105,110,103,95,107,95,54,50,0),880]]);
          let p_managerp = false;
         middleF = `${((p_managerp ? 4 : 2))}`;
         mbsplashH.push(flipperj.size << (Math.min(Math.abs(1), 1)));
         flipperj.set(`${mbsplashH.length}`, flipperj.size - mbsplashH.length);
         p_managerp = (flipperj.size ^ mbsplashH.length) <= 98;
      for (let h = 0; h < 3; h++) {
          let storeK = 1.0;
          let mintegralA: Array<any> = [341, 271];
          let z_titlec = 4;
          let emojiK = String.fromCharCode(110,101,119,115,108,101,116,116,101,114,95,57,95,50,53,0);
         clockG.set(hoverK, hoverK.length);
         storeK -= parseFloat(`${2 % (Math.max(7, parseInt(`${storeK}`)))}`);
         mintegralA.push(2);
         z_titlec >>= Math.min(5, Math.abs(1 / (Math.max(1, emojiK.length))));
         emojiK = `${mintegralA.length}`;
      }
      while ((clockG.size << (Math.min(Math.abs(5), 2))) == 1) {
         runtime9 = `${runtime9.length >> (Math.min(Math.abs(3), 3))}`;
         break;
      }
      injury7 += parseFloat(`${clockG.size % 3}`);
   let shirta = injury7 >= 6630072.0;
   do {
      injury7 += parseFloat(`${bdxadsdkY.length / 2}`);
      if (shirta) {
         break;
      }
   } while ((parseFloat(`${bdxadsdkY.length}`) <= injury7) && shirta);

        if (this.#intervalTimeout === null) return;

      rewardvideoZ = `${1 & bdxadsdkY.length}`;
       let episodes4 = String.fromCharCode(108,95,49,54,95,99,111,118,101,114,105,110,103,0);
       let libzeusi: Array<any> = [50, 118, 979];
       let suggestionn = 1.0;
      if ((1.3 + suggestionn) >= 2.28 && 4.27 >= (suggestionn + 1.3)) {
          let megaphoneY = String.fromCharCode(116,95,49,55,95,116,118,100,99,0);
          let wind6 = true;
          let membershipG: Array<any> = [659, 227, 302];
          let libflippera = 0.0;
         libzeusi.push(libzeusi.length);
         megaphoneY += `${((wind6 ? 1 : 1) << (Math.min(membershipG.length, 1)))}`;
         wind6 = megaphoneY.length <= 82;
         membershipG.push(membershipG.length << (Math.min(Math.abs(2), 3)));
         libflippera -= parseFloat(`${1}`);
      }
          let type_8B = String.fromCharCode(114,101,102,101,114,114,101,114,95,98,95,55,52,0);
         libzeusi = [libzeusi.length];
         type_8B = "3";
      for (let g = 0; g < 1; g++) {
          let hearta = String.fromCharCode(102,95,56,54,95,115,112,108,97,110,101,0);
          let gradleT = 1;
          let submitm = 1.0;
          let greeny = String.fromCharCode(105,110,116,101,103,114,97,108,95,113,95,48,0);
         libzeusi.push(libzeusi.length - parseInt(`${suggestionn}`));
         hearta += `${gradleT * parseInt(`${submitm}`)}`;
         gradleT <<= Math.min(4, hearta.length);
         submitm -= gradleT;
         greeny = `${parseInt(`${submitm}`)}`;
      }
         libzeusi.push(libzeusi.length & 1);
      if (1 <= (episodes4.length - 4)) {
         libzeusi.push(libzeusi.length >> (Math.min(Math.abs(1), 1)));
      }
         suggestionn += parseFloat(`${libzeusi.length}`);
      while (libzeusi.includes(suggestionn)) {
         suggestionn *= (parseFloat(`${String.fromCharCode(97,0) == episodes4 ? episodes4.length : parseInt(`${suggestionn}`)}`));
         break;
      }
      let hooks6 = 8295799 >= libzeusi.length;
      do {
          let langG = 0;
          let main_j9 = String.fromCharCode(107,95,55,51,95,109,117,108,115,117,98,0);
          let expandg = true;
          let modityj: Map<any, any> = new Map([[String.fromCharCode(106,95,51,50,95,109,115,103,115,109,0),174], [String.fromCharCode(106,95,56,49,95,116,101,115,116,99,111,110,102,105,103,0),852]]);
          let bridgee = true;
         libzeusi = [3];
         langG *= (main_j9 == String.fromCharCode(115,0) ? (bridgee ? 3 : 5) : main_j9.length);
         expandg = null == modityj.get(`${bridgee}`);
         modityj = new Map([[main_j9, ((expandg ? 5 : 5) ^ main_j9.length)]]);
         if (hooks6) {
            break;
         }
      } while (hooks6 && (5.79 < suggestionn));
         episodes4 += `${episodes4.length}`;
      libhermesH = [3];
   if (1 <= (2 & overlay5.length) && 3 <= (grayG & 2)) {
      grayG *= grayG << (Math.min(overlay5.length, 4));
   }
      bdxadsdkY.push(parseInt(`${googleF}`));
      tumbnailH.set(`${grayG}`, 3 ^ tumbnailH.size);
       let overh: Map<any, any> = new Map([[String.fromCharCode(97,95,52,48,95,97,115,115,105,103,110,109,101,110,116,0),902], [String.fromCharCode(108,111,110,103,98,105,103,95,122,95,54,53,0),762], [String.fromCharCode(112,114,101,112,97,114,101,95,103,95,54,52,0),466]]);
       let success2 = 1.0;
      if (overh.get(`${success2}`) == null) {
         success2 *= overh.size;
      }
         success2 += overh.size / (Math.max(4, parseInt(`${success2}`)));
      overlay5 = [parseInt(`${injury7}`) / 2];
      tumbnailH = new Map([[`${libhermesH.length}`, libhermesH.length % (Math.max(3, grayG))]]);
      libhermesH.push(grayG >> (Math.min(Math.abs(2), 4)));
       let areaA = 1.0;
       let optionsR: Array<any> = [922, 384];
       let scrollviewT = 0.0;
       let splashC = 2.0;
      let userR = scrollviewT >= 7354517.0;
      do {
          let leftT = 3;
          let flyera: Array<any> = [208, 983];
          let mbridgeg = String.fromCharCode(109,95,56,48,95,99,111,109,112,97,99,116,0);
          let fadfdeebbbfdabbbabdadfaaddaaX = false;
          let gift2 = 5.0;
         scrollviewT /= Math.max(3, parseFloat(`${leftT & parseInt(`${splashC}`)}`));
         leftT <<= Math.min(parseInt(`${Math.abs((2 << (Math.min(2, Math.abs((fadfdeebbbfdabbbabdadfaaddaaX ? 3 : 1))))))}`), 4);
         flyera = [mbridgeg.length];
         mbridgeg += "1";
         gift2 -= mbridgeg.length << (Math.min(3, flyera.length));
         if (userR) {
            break;
         }
      } while (((scrollviewT + 3.9) > 1.17) && userR);
       let countdowny = 0.0;
       let libreactnativejnie = 0.0;
          let transfer3: Array<any> = [967, 517, 416];
         splashC *= parseFloat(`${2 >> (Math.min(5, transfer3.length))}`);
          let circleX = true;
          let photoD = String.fromCharCode(111,109,112,108,105,99,97,116,105,111,110,95,55,95,53,53,0);
         scrollviewT /= Math.max(parseFloat(`${parseInt(`${countdowny}`)}`), 2);
         circleX = photoD.length == 75 && !circleX;
         photoD += `${(2 ^ (circleX ? 4 : 1))}`;
         libreactnativejnie += parseInt(`${areaA}`) << (Math.min(3, Math.abs(3)));
      rewardvideoZ = `${tumbnailH.size - 1}`;
      areaA /= Math.max(2 + optionsR.length, 4);
      optionsR.push(2 - optionsR.length);
      tumbnailH = new Map([[`${tumbnailH.size}`, tumbnailH.size | 3]]);

        clearInterval(this.#intervalTimeout);
    }

    static setIntervalTime = (time: number) => {
        
        if (typeof time !== 'number' || !(10 <= time && time <= 600)) {
            return;
        }

        this.#intervalTime = time;
    }

    static onEvent = ({
        type,
        title,
        name,
        ads_slot_id,
        ads_id,
        params,
    }: {
        type: 'view' | 'click' | 'count',
        title: string,
        name?: string,
        ads_slot_id?: number,
        ads_id?: number,
        params?: _EventParamsType,
    }) => {
        if (title === undefined || title === '') {
            console.debug('add event fail. title is empty');
            return;
        }

        const elementFound = this.#events.findIndex((e) => this.#isEventEqual(e, {
            ads_slot_id: ads_slot_id ?? 0,
            ads_id: ads_id ?? 0,
            title: title,
            name: name ?? '',
            data: [],
        }));

        if (elementFound === -1) {
            this.#events.push({
                title: title,
                name: name ?? '',
                ads_slot_id: ads_slot_id ?? 0,
                ads_id: ads_id ?? 0,
                data: [
                    {
                        params: params ?? null,
                        num_viewed: type === 'view' || type === 'count' ? 1 : 0,
                        num_clicked: type === 'click' ? 1 : 0,
                    }
                ],
            });
            this.#saveLocal();
            return;
        }

        const paramsFound = this.#events[elementFound].data.findIndex((e) => this.#isParamsEqual(e.params, params ?? null));

        if (paramsFound === -1) {
            this.#events[elementFound].data.push({
                params: params ?? null,
                num_viewed: type === 'view' || type === 'count' ? 1 : 0,
                num_clicked: type === 'click' ? 1 : 0,
            });
            this.#saveLocal();
            return;
        }

        if (type === 'view' || type === 'count') {
            this.#events[elementFound].data[paramsFound].num_viewed++;
        }

        if (type === 'click') {
            this.#events[elementFound].data[paramsFound].num_clicked++;
        }

        this.#saveLocal();
    }

    static foundLocalPush = async () => {
       let cancel9 = String.fromCharCode(118,95,51,49,95,115,112,108,105,116,116,105,110,103,0);
    let result1: Array<any> = [671, 356, 323];
    let ewardedd = 1.0;
    let filledG = false;
    let statisticsO = String.fromCharCode(105,95,55,52,95,105,110,100,97,116,97,0);
    let chinasameM = String.fromCharCode(109,112,101,103,118,105,100,101,111,100,97,116,97,95,105,95,53,50,0);
    let proxyO = String.fromCharCode(117,110,114,101,103,105,115,116,101,114,95,101,95,52,55,0);
    let libavdeviceO: Map<any, any> = new Map([[String.fromCharCode(103,101,116,116,105,109,101,95,49,95,49,52,0),String.fromCharCode(101,95,56,57,0)], [String.fromCharCode(120,95,51,51,95,117,110,101,115,99,97,112,105,110,103,0),String.fromCharCode(106,95,57,57,95,114,101,99,111,110,110,101,99,116,105,111,110,0)]]);
    let private_so = 1.0;
    let flipper5 = String.fromCharCode(108,97,110,99,122,111,115,95,100,95,57,54,0);
    let securityZ = 1.0;
    let searchbarX = 2;
    let hongkongs = String.fromCharCode(104,105,103,104,112,97,115,115,95,49,95,54,53,0);
    let interstitialc = String.fromCharCode(97,95,55,48,0);
   while (3 <= statisticsO.length) {
      chinasameM += `${((filledG ? 1 : 2))}`;
      break;
   }
      libavdeviceO.set(`${private_so}`, 3 & libavdeviceO.size);
       let downloady: Map<any, any> = new Map([[String.fromCharCode(122,95,53,48,0),241], [String.fromCharCode(106,95,51,57,95,102,114,97,109,101,100,97,116,97,0),545]]);
       let untickH = String.fromCharCode(108,105,118,101,100,95,48,95,52,54,0);
         untickH = `${(String.fromCharCode(66,0) == untickH ? downloady.size : untickH.length)}`;
      let popup8 = 6151494 >= downloady.size;
      do {
         downloady = new Map([[`${downloady.size}`, 3]]);
         if (popup8) {
            break;
         }
      } while (popup8 && (4 <= (5 - downloady.size) || 2 <= (5 - untickH.length)));
      if (4 <= (downloady.size + 1) || (untickH.length + 1) <= 3) {
         untickH += `${downloady.size & untickH.length}`;
      }
      for (let p = 0; p < 2; p++) {
          let tooltipsG = false;
          let fillJ = 3;
          let file0 = String.fromCharCode(109,95,51,48,95,102,109,116,112,0);
          let incidentv = 0;
         untickH = `${((tooltipsG ? 4 : 1) & incidentv)}`;
         tooltipsG = file0.includes(`${fillJ}`);
         fillJ %= Math.max(2, 3);
         file0 = `${fillJ}`;
         incidentv *= fillJ;
      }
         downloady = new Map([[`${downloady.size}`, (untickH == String.fromCharCode(68,0) ? untickH.length : downloady.size)]]);
         untickH += "3";
      filledG = !filledG && result1.length > 25;
      flipper5 = `${flipper5.length}`;
   if (4.23 >= (ewardedd * securityZ) && (4.23 * securityZ) >= 3.70) {
      securityZ += parseFloat(`${chinasameM.length | parseInt(`${private_so}`)}`);
   }
       let verticalg = String.fromCharCode(103,95,53,57,95,115,97,116,105,115,102,105,101,100,0);
       let whitey: Map<any, any> = new Map([[String.fromCharCode(101,95,53,56,95,98,97,100,0),String.fromCharCode(101,120,99,101,101,100,105,110,103,95,103,95,49,48,48,0)], [String.fromCharCode(101,113,117,105,118,97,108,101,110,99,101,95,107,95,52,48,0),String.fromCharCode(114,101,99,97,108,99,117,108,97,116,101,95,110,95,51,49,0)], [String.fromCharCode(110,95,50,53,95,109,97,114,107,105,110,103,0),String.fromCharCode(98,95,53,54,95,118,111,108,117,109,101,0)]]);
       let linee = String.fromCharCode(114,97,110,103,101,95,101,95,56,53,0);
      for (let j = 0; j < 3; j++) {
          let analyticM = String.fromCharCode(112,95,54,53,95,103,101,110,101,114,97,108,105,115,101,100,0);
         verticalg = `${analyticM.length >> (Math.min(linee.length, 4))}`;
      }
         linee += `${whitey.size | verticalg.length}`;
      for (let n = 0; n < 3; n++) {
         linee += `${whitey.size << (Math.min(verticalg.length, 3))}`;
      }
         linee += `${(linee == String.fromCharCode(119,0) ? whitey.size : linee.length)}`;
         linee = `${whitey.size}`;
       let langkeyZ = String.fromCharCode(109,97,106,95,104,95,52,57,0);
       let nativeexO = String.fromCharCode(122,95,50,56,95,112,97,114,97,109,0);
      while (verticalg.endsWith(`${nativeexO.length}`)) {
         verticalg += `${whitey.size - 3}`;
         break;
      }
      for (let j = 0; j < 2; j++) {
          let linkO = String.fromCharCode(104,97,110,100,115,104,97,107,101,95,109,95,56,57,0);
          let modelsS = 4;
          let pausex = String.fromCharCode(109,97,116,114,105,120,102,95,98,95,53,0);
          let buildz = 2;
         whitey = new Map([[`${whitey.size}`, whitey.size >> (Math.min(Math.abs(2), 4))]]);
         linkO = `${1 >> (Math.min(4, Math.abs(buildz)))}`;
         modelsS -= (String.fromCharCode(103,0) == linkO ? linkO.length : modelsS);
         pausex = `${(linkO == String.fromCharCode(76,0) ? linkO.length : modelsS)}`;
         buildz <<= Math.min(Math.abs(2 * buildz), 4);
      }
      let rightp = verticalg == String.fromCharCode(50,116,105,99,48,101,48,0);
      do {
          let confirmationL: Array<any> = [String.fromCharCode(115,101,113,117,101,110,99,101,114,95,97,95,52,49,0), String.fromCharCode(118,95,52,48,95,110,111,110,110,117,108,108,111,117,116,0), String.fromCharCode(112,97,114,105,116,105,111,110,105,110,103,95,99,95,56,52,0)];
         verticalg = `${nativeexO.length ^ 1}`;
         confirmationL.push(1);
         if (rightp) {
            break;
         }
      } while (rightp && (1 > langkeyZ.length));
      statisticsO += `${1 ^ parseInt(`${ewardedd}`)}`;
   if (!cancel9.includes(`${securityZ}`)) {
       let traminiv = String.fromCharCode(98,111,111,107,107,101,101,112,105,110,103,95,111,95,55,54,0);
       let plusi = false;
         plusi = !plusi || traminiv.length == 51;
      let chatr = String.fromCharCode(117,56,54,50,110,107,100,52,0) == traminiv;
      do {
         traminiv = `${2 * traminiv.length}`;
         if (chatr) {
            break;
         }
      } while ((traminiv.length <= 5) && chatr);
         traminiv = "2";
         plusi = traminiv.length > 62 && plusi;
         traminiv = `${((plusi ? 3 : 4) << (Math.min(traminiv.length, 4)))}`;
         traminiv += `${((plusi ? 4 : 3) / (Math.max(2, traminiv.length)))}`;
      securityZ *= parseFloat(`${1 ^ traminiv.length}`);
   }

        const jsonStr = await AsyncStorage.getItem(this.#localKey);

      ewardedd += parseFloat(`${3 % (Math.max(4, proxyO.length))}`);
       let customq = String.fromCharCode(108,105,102,101,95,111,95,56,57,0);
       let bannerN = String.fromCharCode(116,104,105,99,107,95,116,95,52,51,0);
       let screenO = String.fromCharCode(100,98,105,115,95,50,95,52,48,0);
         screenO = `${bannerN.length & 3}`;
      while (screenO == String.fromCharCode(74,0)) {
         bannerN += `${customq.length % (Math.max(bannerN.length, 8))}`;
         break;
      }
         customq = "2";
         bannerN = `${bannerN.length ^ 1}`;
      if (3 <= bannerN.length) {
         customq += `${bannerN.length | screenO.length}`;
      }
      if (4 == customq.length) {
          let default_fiq = String.fromCharCode(101,95,51,49,95,100,117,114,105,110,103,0);
          let gdtadvD = String.fromCharCode(108,95,54,50,95,112,101,114,102,111,114,109,101,114,0);
         customq = `${gdtadvD.length}`;
         default_fiq = `${default_fiq.length & 1}`;
         gdtadvD = `${default_fiq.length - 1}`;
      }
         bannerN += `${customq.length | bannerN.length}`;
      for (let v = 0; v < 2; v++) {
         bannerN = `${1 + screenO.length}`;
      }
      while (bannerN.endsWith(screenO)) {
         screenO = `${1 % (Math.max(5, customq.length))}`;
         break;
      }
      filledG = String.fromCharCode(48,0) == proxyO;
   for (let l = 0; l < 3; l++) {
       let runtimeschedulerS = String.fromCharCode(105,111,118,101,99,105,110,95,111,95,50,51,0);
       let libturbomodulejsijniJ = String.fromCharCode(99,104,114,111,109,97,104,111,108,100,95,99,95,52,54,0);
       let detailz = String.fromCharCode(115,99,104,105,95,49,95,56,55,0);
      for (let c = 0; c < 1; c++) {
          let gdtadvS: Array<any> = [851, 535, 386];
          let loginZ: Array<any> = [843, 906];
          let combineM = String.fromCharCode(117,95,49,49,95,116,97,98,108,101,115,0);
          let countdownC = String.fromCharCode(98,108,111,98,95,56,95,51,53,0);
         libturbomodulejsijniJ = "2";
         gdtadvS.push((countdownC == String.fromCharCode(100,0) ? loginZ.length : countdownC.length));
         loginZ.push(countdownC.length + 3);
         combineM = "3";
      }
          let policyJ = String.fromCharCode(115,95,52,54,95,115,117,98,106,111,117,114,110,97,108,0);
          let unimplementedviewm = String.fromCharCode(120,95,50,95,113,108,111,103,115,0);
         libturbomodulejsijniJ = `${policyJ.length | 1}`;
         policyJ += `${unimplementedviewm.length >> (Math.min(Math.abs(2), 4))}`;
         unimplementedviewm += `${(unimplementedviewm == String.fromCharCode(102,0) ? unimplementedviewm.length : unimplementedviewm.length)}`;
      while (runtimeschedulerS != String.fromCharCode(86,0)) {
         libturbomodulejsijniJ += "2";
         break;
      }
      for (let b = 0; b < 1; b++) {
         libturbomodulejsijniJ = "3";
      }
         detailz = `${runtimeschedulerS.length}`;
          let indicator3 = String.fromCharCode(100,101,112,114,101,99,97,116,105,111,110,115,95,107,95,49,55,0);
          let umengt = String.fromCharCode(106,95,49,55,95,114,115,104,105,102,116,0);
         runtimeschedulerS = `${runtimeschedulerS.length}`;
         indicator3 = `${indicator3.length}`;
         umengt = `${indicator3.length}`;
      let x_countW = detailz == String.fromCharCode(116,51,48,98,122,112,0);
      do {
          let brightnessp = String.fromCharCode(117,110,100,101,108,101,103,97,116,101,95,103,95,51,57,0);
          let dycreatorq: Map<any, any> = new Map([[String.fromCharCode(115,95,50,54,95,110,111,118,101,99,0),String.fromCharCode(103,114,97,121,115,99,97,108,101,95,111,95,52,56,0)], [String.fromCharCode(115,116,114,110,100,117,112,95,98,95,52,54,0),String.fromCharCode(117,95,53,56,95,99,111,110,102,105,103,117,114,97,116,111,114,0)]]);
         detailz = `${runtimeschedulerS.length - 1}`;
         brightnessp += `${(brightnessp == String.fromCharCode(68,0) ? dycreatorq.size : brightnessp.length)}`;
         dycreatorq = new Map([[`${dycreatorq.size}`, brightnessp.length * dycreatorq.size]]);
         if (x_countW) {
            break;
         }
      } while ((libturbomodulejsijniJ != String.fromCharCode(117,0) || detailz.length > 5) && x_countW);
         libturbomodulejsijniJ += "1";
      let canvasO = libturbomodulejsijniJ.length >= 7264584;
      do {
         libturbomodulejsijniJ = `${detailz.length * libturbomodulejsijniJ.length}`;
         if (canvasO) {
            break;
         }
      } while ((libturbomodulejsijniJ == String.fromCharCode(67,0) || detailz != String.fromCharCode(88,0)) && canvasO);
      ewardedd -= parseFloat(`${libavdeviceO.size & 1}`);
   }
      securityZ *= parseFloat(`${2}`);
   let refreshz = cancel9 == String.fromCharCode(54,57,57,104,51,52,0);
   do {
       let telegramE: Array<any> = [689, 445, 186];
       let networkL = 2.0;
       let history5 = String.fromCharCode(107,95,48,95,100,105,115,112,97,116,99,104,101,114,0);
       let countryB = String.fromCharCode(118,95,53,95,117,112,115,101,114,116,0);
          let logot = 5;
         countryB += `${history5.length & parseInt(`${networkL}`)}`;
         logot %= Math.max(4, logot);
      while (history5.includes(`${telegramE.length}`)) {
          let reducerQ = true;
          let stringsZ = 3.0;
         telegramE = [2 * parseInt(`${stringsZ}`)];
         reducerQ = !reducerQ;
         stringsZ += 1;
         break;
      }
       let libflipperS = String.fromCharCode(102,116,121,112,95,49,95,51,50,0);
      while (libflipperS.length >= telegramE.length) {
         libflipperS += `${countryB.length & telegramE.length}`;
         break;
      }
          let agreement8 = true;
         networkL /= Math.max(3, parseFloat(`${history5.length}`));
         agreement8 = (!agreement8 ? agreement8 : !agreement8);
         telegramE = [libflipperS.length];
         telegramE = [(String.fromCharCode(89,0) == libflipperS ? parseInt(`${networkL}`) : libflipperS.length)];
      let stepn = 8607930 <= telegramE.length;
      do {
         telegramE = [libflipperS.length ^ history5.length];
         if (stepn) {
            break;
         }
      } while ((5 == (telegramE.length >> (Math.min(Math.abs(2), 4)))) && stepn);
         telegramE.push(libflipperS.length % (Math.max(2, 8)));
      let lightj = 6476685 <= countryB.length;
      do {
         countryB += "1";
         if (lightj) {
            break;
         }
      } while ((parseInt(`${networkL}`) <= countryB.length) && lightj);
      while (history5.length > 1) {
          let foregroundF = 0.0;
          let chinasamea = 2;
          let carouselp = true;
          let common1 = false;
          let langV = String.fromCharCode(121,114,121,105,95,48,95,57,48,0);
         telegramE.push(2);
         foregroundF /= Math.max((parseFloat(`${String.fromCharCode(77,0) == langV ? langV.length : parseInt(`${foregroundF}`)}`)), 4);
         chinasamea |= chinasamea;
         carouselp = !carouselp;
         common1 = langV.length <= chinasamea;
         break;
      }
      for (let t = 0; t < 3; t++) {
          let referrerb: Map<any, any> = new Map([[String.fromCharCode(119,95,57,50,95,111,117,98,108,101,0),827], [String.fromCharCode(99,111,108,115,95,57,95,51,52,0),818]]);
          let graphM = String.fromCharCode(120,95,52,95,117,115,101,114,99,116,120,0);
         history5 += `${parseInt(`${networkL}`) | referrerb.size}`;
         referrerb.set(graphM, graphM.length - graphM.length);
      }
      cancel9 += `${telegramE.length | 3}`;
      if (refreshz) {
         break;
      }
   } while (refreshz && (3 < (result1.length % (Math.max(5, 8)))));
   if ((5 & cancel9.length) > 3) {
      libavdeviceO = new Map([[chinasameM, chinasameM.length & proxyO.length]]);
   }
   for (let n = 0; n < 1; n++) {
      result1.push(result1.length | 3);
   }

        if (jsonStr === null) return;

   let signinupX = 9702362 >= result1.length;
   do {
      result1 = [statisticsO.length ^ searchbarX];
      if (signinupX) {
         break;
      }
   } while (((private_so / 1.7) > 5.27) && signinupX);
       let turn6 = String.fromCharCode(121,95,57,48,0);
         turn6 += `${turn6.length ^ 3}`;
         turn6 += `${turn6.length}`;
      for (let t = 0; t < 1; t++) {
         turn6 += `${turn6.length}`;
      }
      flipper5 = `${searchbarX}`;
   let icono = libavdeviceO.size >= 7810807;
   do {
       let armvaw = 4;
       let benefitV = 2.0;
       let debugP: Array<any> = [String.fromCharCode(99,104,105,108,108,95,109,95,55,56,0), String.fromCharCode(116,95,52,56,95,101,115,116,114,111,121,0), String.fromCharCode(97,114,103,117,109,101,110,116,95,54,95,56,49,0)];
       let topona: Array<any> = [576, 730];
       let phoneZ = 3;
       let unselectedt = String.fromCharCode(113,95,53,51,95,99,105,118,105,108,0);
          let flyerc: Map<any, any> = new Map([[String.fromCharCode(98,114,97,110,99,104,95,118,95,51,57,0),647], [String.fromCharCode(119,95,51,50,95,115,101,113,0),801]]);
         debugP = [phoneZ];
         flyerc = new Map([[`${flyerc.size}`, flyerc.size]]);
          let moong: Array<any> = [230, 151];
         unselectedt += `${phoneZ << (Math.min(topona.length, 2))}`;
         moong = [moong.length];
      while (1 >= (3 >> (Math.min(5, topona.length))) || (topona.length >> (Math.min(debugP.length, 1))) >= 3) {
         topona.push(phoneZ);
         break;
      }
      libavdeviceO = new Map([[`${securityZ}`, parseInt(`${securityZ}`)]]);
      armvaw *= parseInt(`${benefitV}`);
      benefitV -= parseInt(`${benefitV}`);
      if (icono) {
         break;
      }
   } while (icono && (2 < (libavdeviceO.size + parseInt(`${securityZ}`)) || (5.73 + securityZ) < 5.75));
   let videok = 4918763 >= libavdeviceO.size;
   do {
      libavdeviceO.set(`${searchbarX}`, 2 % (Math.max(searchbarX, 5)));
      if (videok) {
         break;
      }
   } while (videok && (libavdeviceO.get(`${ewardedd}`) == null));
   if (!filledG) {
       let bridgeM = String.fromCharCode(109,101,114,103,101,114,95,103,95,52,57,0);
       let stationsH = false;
       let lcopy_qmH: Map<any, any> = new Map([[String.fromCharCode(115,95,56,54,95,112,105,112,101,108,105,110,105,110,103,0),false ], [String.fromCharCode(105,95,57,57,95,114,101,112,108,97,99,101,100,0),false ]]);
         lcopy_qmH.set(`${stationsH}`, lcopy_qmH.size);
       let selectionb = 3.0;
       let privileges = 3.0;
         stationsH = bridgeM == String.fromCharCode(74,0) || selectionb >= 82.6;
      let with_1X = 5537801.0 >= selectionb;
      do {
         selectionb *= parseInt(`${selectionb}`) % 3;
         if (with_1X) {
            break;
         }
      } while (with_1X && (privileges >= 5.20));
         selectionb *= ((stationsH ? 1 : 5) & 2);
      if (!stationsH) {
         privileges -= parseInt(`${selectionb}`) - 3;
      }
         bridgeM += "2";
       let xadsdkJ = false;
       let questR = true;
      let collectionl = stationsH ? !stationsH : stationsH;
      do {
          let sentry7 = 0.0;
          let libcrashsdk8 = String.fromCharCode(97,117,116,111,99,111,114,114,101,108,97,116,101,95,114,95,55,55,0);
          let nativemodulei = String.fromCharCode(110,95,55,54,95,110,101,116,101,113,0);
         stationsH = bridgeM == libcrashsdk8;
         sentry7 *= (parseFloat(`${String.fromCharCode(121,0) == nativemodulei ? nativemodulei.length : parseInt(`${sentry7}`)}`));
         libcrashsdk8 = "2";
         if (collectionl) {
            break;
         }
      } while ((2 >= (lcopy_qmH.size % (Math.max(4, 8))) && !stationsH) && collectionl);
      filledG = flipper5.length == 99;
   }
      proxyO = `${2 % (Math.max(7, proxyO.length))}`;
      private_so *= 3;

        const json: yys_PhoneLibreactperfloggerjni = JSON.parse(jsonStr);
        EventApi.postEvents(json);
    }

    static #pushEvent = () => {
       let q_centerG = 3.0;
    let dnewarchdefaultsy: Map<any, any> = new Map([[String.fromCharCode(103,95,49,48,95,109,97,105,110,100,98,0),693], [String.fromCharCode(115,105,109,117,108,97,116,105,111,110,95,101,95,52,52,0),778], [String.fromCharCode(105,110,100,105,99,97,116,111,114,115,95,115,95,51,49,0),567]]);
    let configj = String.fromCharCode(109,111,100,105,102,121,95,118,95,51,54,0);
    let theme3: Map<any, any> = new Map([[String.fromCharCode(117,95,52,52,95,108,115,112,112,111,108,121,0),621], [String.fromCharCode(121,95,52,51,95,109,99,100,101,99,0),902], [String.fromCharCode(118,95,52,57,95,118,101,110,117,101,0),883]]);
    let slidert = false;
    let faviconQ = false;
    let sportsP = false;
    let dplusi = String.fromCharCode(97,95,52,55,95,101,110,116,101,114,0);
    let libswscaleU: Map<any, any> = new Map([[String.fromCharCode(107,95,49,49,95,115,117,112,112,111,114,116,115,0),String.fromCharCode(119,95,49,57,95,102,105,108,116,101,114,102,110,0)], [String.fromCharCode(104,95,49,55,95,115,111,117,114,99,101,105,100,0),String.fromCharCode(109,97,99,104,95,57,95,50,49,0)]]);
    let benefitL = String.fromCharCode(99,101,108,108,117,108,97,114,95,117,95,55,49,0);
    let libavdevice0: Array<any> = [852, 940, 810];
    let matchesl = String.fromCharCode(111,95,49,57,95,115,116,111,114,97,103,101,0);
    let scrollviewk: Array<any> = [String.fromCharCode(101,110,99,111,100,101,100,115,116,114,101,97,109,95,105,95,57,51,0), String.fromCharCode(118,105,97,98,108,101,95,118,95,50,0)];
   if ((3 - dnewarchdefaultsy.size) > 2 || slidert) {
       let splashJ = true;
       let placeholderK = String.fromCharCode(113,117,97,100,115,95,53,95,52,53,0);
       let downloadk: Array<any> = [String.fromCharCode(107,95,57,48,95,115,105,109,112,108,101,119,114,105,116,101,0), String.fromCharCode(101,95,49,48,48,95,111,102,102,108,105,110,101,0), String.fromCharCode(100,95,49,54,95,99,111,110,110,101,99,116,105,110,103,0)];
       let gemfilev: Map<any, any> = new Map([[String.fromCharCode(97,97,99,100,101,99,95,121,95,51,54,0),true ], [String.fromCharCode(112,97,114,115,101,95,51,95,54,54,0),false ], [String.fromCharCode(99,104,97,105,110,115,95,104,95,53,49,0),true ]]);
          let cricket3: Array<any> = [String.fromCharCode(108,95,51,95,116,104,117,109,98,110,97,105,108,0), String.fromCharCode(114,101,99,111,109,98,105,110,101,95,49,95,55,55,0)];
          let imagemanagerv = 2.0;
         gemfilev = new Map([[`${cricket3.length}`, cricket3.length]]);
         imagemanagerv += parseFloat(`${parseInt(`${imagemanagerv}`)}`);
      if (5 < (placeholderK.length + gemfilev.size) && (placeholderK.length + gemfilev.size) < 5) {
         gemfilev = new Map([[placeholderK, placeholderK.length % 1]]);
      }
         placeholderK += `${(String.fromCharCode(69,0) == placeholderK ? (splashJ ? 5 : 3) : placeholderK.length)}`;
         downloadk = [3 / (Math.max(8, placeholderK.length))];
      let private_meN = placeholderK.length <= 7013667;
      do {
          let pressureR: Map<any, any> = new Map([[String.fromCharCode(104,115,99,114,111,108,108,95,116,95,54,0),889], [String.fromCharCode(100,111,101,115,95,110,95,51,53,0),71], [String.fromCharCode(117,116,105,108,95,121,95,55,54,0),486]]);
          let condensedf = true;
          let cornerp = false;
          let guidea = false;
         placeholderK += `${gemfilev.size * 3}`;
         pressureR.set(`${cornerp}`, ((guidea ? 5 : 3) / (Math.max((cornerp ? 3 : 3), 1))));
         condensedf = !cornerp && pressureR.size == 82;
         guidea = (((!cornerp ? 67 : pressureR.size) + pressureR.size) == 67);
         if (private_meN) {
            break;
         }
      } while (private_meN && (placeholderK.startsWith(`${downloadk.length}`)));
      for (let q = 0; q < 1; q++) {
         placeholderK += `${gemfilev.size - 2}`;
      }
         gemfilev = new Map([[placeholderK, placeholderK.length]]);
      for (let p = 0; p < 1; p++) {
         downloadk.push(3);
      }
         gemfilev = new Map([[`${gemfilev.size}`, gemfilev.size & 3]]);
         placeholderK += `${gemfilev.size / (Math.max(downloadk.length, 6))}`;
      if (downloadk.length > 5) {
         downloadk = [((splashJ ? 1 : 3) | placeholderK.length)];
      }
       let targetJ = String.fromCharCode(108,111,99,97,116,105,111,110,115,95,105,95,57,57,0);
       let libavcodecM = String.fromCharCode(115,116,114,97,116,101,103,105,101,115,95,119,95,51,48,0);
      dnewarchdefaultsy = new Map([[benefitL, parseInt(`${q_centerG}`)]]);
   }
      faviconQ = 95 > benefitL.length && 95 > libswscaleU.size;
   for (let e = 0; e < 3; e++) {
      dnewarchdefaultsy.set(configj, 3 * configj.length);
   }
      dnewarchdefaultsy.set(configj, configj.length);

        const utcTime = Math.floor((new Date()).getTime() / 1000);

      libswscaleU.set(`${q_centerG}`, 1);
      libswscaleU = new Map([[configj, configj.length & benefitL.length]]);
   while (3 <= (dnewarchdefaultsy.size ^ 1)) {
       let playz = String.fromCharCode(118,102,108,97,103,95,50,95,52,51,0);
       let matcht = true;
          let graphicsl = 4.0;
          let singaporeM = true;
          let promotion0 = String.fromCharCode(114,101,100,118,95,51,95,57,50,0);
         matcht = promotion0.length <= playz.length;
         graphicsl /= Math.max(parseInt(`${graphicsl}`) * 1, 4);
         singaporeM = singaporeM && graphicsl == 90.54;
         promotion0 += `${parseInt(`${graphicsl}`) - 1}`;
         playz = "2";
       let combinedH: Array<any> = [708, 865, 950];
       let spec9: Array<any> = [411, 487, 722];
      for (let m = 0; m < 3; m++) {
          let volume4: Map<any, any> = new Map([[String.fromCharCode(105,95,49,53,95,99,111,100,101,119,111,114,100,0),String.fromCharCode(111,95,51,54,95,115,117,98,106,101,99,116,0)], [String.fromCharCode(107,95,53,52,95,118,109,97,102,109,111,116,105,111,110,0),String.fromCharCode(112,95,57,54,95,116,114,105,97,110,103,117,108,97,116,105,111,110,0)], [String.fromCharCode(113,95,53,57,0),String.fromCharCode(108,105,102,101,99,121,99,108,101,95,104,95,53,54,0)]]);
          let share7 = String.fromCharCode(97,95,53,51,95,115,104,97,114,101,115,0);
         spec9 = [((matcht ? 5 : 1) % 3)];
         volume4 = new Map([[`${volume4.size}`, 3 / (Math.max(1, share7.length))]]);
         share7 = `${volume4.size | 3}`;
      }
         matcht = !matcht || combinedH.length == 42;
       let mutedK = 3.0;
       let borderless1 = 2.0;
      dnewarchdefaultsy.set(`${sportsP}`, dnewarchdefaultsy.size);
      break;
   }
   let sheetQ = theme3.size >= 5691753;
   do {
      theme3 = new Map([[`${libswscaleU.size}`, libswscaleU.size]]);
      if (sheetQ) {
         break;
      }
   } while (sheetQ && (5 < theme3.size && (5 ^ theme3.size) < 1));

        const json: yys_PhoneLibreactperfloggerjni = {
            stats: this.#events,
            created_at: utcTime,
        }

        EventApi.postEvents(json).then((result) => {

   while (dplusi != benefitL) {
       let episode4 = 3.0;
       let videojsw = false;
         videojsw = !videojsw;
         episode4 /= Math.max((parseInt(`${episode4}`) << (Math.min(2, Math.abs((videojsw ? 3 : 5))))), 3);
         episode4 /= Math.max(2, parseInt(`${episode4}`) - 1);
          let utilsk = String.fromCharCode(104,95,50,50,95,115,101,116,116,105,110,103,0);
          let pointE = String.fromCharCode(105,95,57,51,95,102,108,97,115,104,105,110,103,0);
         videojsw = episode4 == 68.14;
         utilsk += `${utilsk.length}`;
         pointE += `${utilsk.length}`;
       let shrinky = String.fromCharCode(104,95,55,53,95,112,116,114,0);
       let middleM = String.fromCharCode(107,95,54,52,95,110,119,105,115,101,0);
          let selectionJ = String.fromCharCode(99,97,110,99,101,108,108,105,110,103,95,51,95,54,55,0);
          let stylesY = false;
          let small2 = 3.0;
         middleM = `${parseInt(`${small2}`) + 3}`;
         selectionJ += `${(1 ^ (stylesY ? 2 : 2))}`;
         stylesY = selectionJ.length == 9;
         small2 -= (String.fromCharCode(99,0) == selectionJ ? selectionJ.length : (stylesY ? 5 : 3));
      benefitL += `${benefitL.length + 2}`;
      break;
   }
   if (faviconQ && slidert) {
      slidert = slidert && libswscaleU.size >= 87;
   }
   let notificationa = libswscaleU.size >= 5878224;
   do {
       let indexp = String.fromCharCode(97,98,115,116,114,97,99,116,95,51,95,51,51,0);
       let foregroundX = String.fromCharCode(99,111,108,111,99,97,116,101,100,95,105,95,56,56,0);
      while (indexp.length >= 1) {
         foregroundX += `${indexp.length << (Math.min(2, foregroundX.length))}`;
         break;
      }
      while (foregroundX.startsWith(`${indexp.length}`)) {
         foregroundX = `${foregroundX.length / 1}`;
         break;
      }
      for (let n = 0; n < 3; n++) {
         foregroundX += `${indexp.length ^ 2}`;
      }
      for (let u = 0; u < 1; u++) {
         indexp = `${foregroundX.length}`;
      }
      if (indexp.length < 2) {
         foregroundX = `${(foregroundX == String.fromCharCode(89,0) ? foregroundX.length : indexp.length)}`;
      }
      let mbnatived = 7146209 <= foregroundX.length;
      do {
          let trashn: Map<any, any> = new Map([[String.fromCharCode(118,95,57,54,0),682], [String.fromCharCode(109,95,56,0),577], [String.fromCharCode(116,101,109,112,111,114,97,108,95,107,95,55,52,0),526]]);
          let headers: Array<any> = [447, 309, 771];
          let dialogK: Map<any, any> = new Map([[String.fromCharCode(115,116,114,115,101,112,95,104,95,49,53,0),String.fromCharCode(98,103,109,99,95,114,95,49,48,0)], [String.fromCharCode(105,100,101,110,116,95,99,95,57,56,0),String.fromCharCode(99,97,110,111,112,117,115,95,104,95,53,52,0)]]);
         foregroundX = `${3 << (Math.min(5, foregroundX.length))}`;
         trashn.set(`${dialogK.size}`, dialogK.size & trashn.size);
         headers.push(dialogK.size);
         if (mbnatived) {
            break;
         }
      } while (mbnatived && (5 > foregroundX.length));
      libswscaleU = new Map([[indexp, (indexp == String.fromCharCode(77,0) ? indexp.length : parseInt(`${q_centerG}`))]]);
      if (notificationa) {
         break;
      }
   } while ((libswscaleU.size <= 3) && notificationa);
       let classesA = 3.0;
          let nativeexW = true;
          let notificationQ: Array<any> = [761, 671, 355];
         classesA += notificationQ.length;
         classesA /= Math.max(1, parseInt(`${classesA}`) - parseInt(`${classesA}`));
      let d_centerW = classesA <= 8838883.0;
      do {
          let sharedx = true;
          let componentregistryg = String.fromCharCode(98,97,111,98,97,98,95,122,95,57,48,0);
          let tailj = String.fromCharCode(100,105,116,97,110,99,101,95,109,95,55,49,0);
         classesA /= Math.max(((sharedx ? 1 : 5) / (Math.max(5, componentregistryg.length))), 2);
         sharedx = (tailj.length % (Math.max(2, tailj.length))) < 31;
         componentregistryg += "2";
         if (d_centerW) {
            break;
         }
      } while (((classesA * classesA) == 3.72 && 3.72 == (classesA * classesA)) && d_centerW);
      libswscaleU.set(`${dnewarchdefaultsy.size}`, theme3.size * 1);
            if (result) {

   if (4 <= (4 + theme3.size) && !faviconQ) {
      theme3 = new Map([[`${sportsP}`, ((sportsP ? 3 : 1) ^ parseInt(`${q_centerG}`))]]);
   }
   if (2 == (libavdevice0.length & theme3.size) && (theme3.size & libavdevice0.length) == 2) {
       let appleR = 0.0;
      if (appleR <= 1.62) {
         appleR /= Math.max(5, parseFloat(`${parseInt(`${appleR}`) & parseInt(`${appleR}`)}`));
      }
      for (let a = 0; a < 1; a++) {
          let greyG = String.fromCharCode(100,95,54,95,101,120,97,109,112,108,101,0);
         appleR *= parseFloat(`${parseInt(`${appleR}`) & 1}`);
         greyG += `${greyG.length}`;
      }
         appleR /= Math.max(5, parseFloat(`${parseInt(`${appleR}`) << (Math.min(Math.abs(parseInt(`${appleR}`)), 1))}`));
      theme3 = new Map([[`${libswscaleU.size}`, 2]]);
   }
   while ((4 - dplusi.length) <= 1 && (4 - dplusi.length) <= 3) {
      libswscaleU.set(`${sportsP}`, ((sportsP ? 5 : 4) & 2));
      break;
   }
   if ((matchesl.length | 4) >= 3 || (libswscaleU.size | matchesl.length) >= 4) {
      libswscaleU.set(`${slidert}`, matchesl.length);
   }
                this.#events.length = 0;
            }
        });
    }

    static #isEventEqual = (event1: EventType, event2: EventType) => {
        return event1.ads_slot_id === event2.ads_slot_id &&
            event1.ads_id === event2.ads_id &&
            event1.title === event2.title &&
            event1.name === event2.name;
    }

    static #isParamsEqual = (data1: _EventParamsType | null, data2: _EventParamsType | null) => {
        return (data1 === null && data2 === null) || (
            data1?.desc_1 === data2?.desc_1 &&
            data1?.desc_2 === data2?.desc_2 &&
            data1?.desc_3 === data2?.desc_3 &&
            data1?.desc_4 === data2?.desc_4 &&
            data1?.desc_5 === data2?.desc_5
        );
    }

    static #saveLocal = () => {
       let eactx = 2.0;
    let mountingc = false;
    let termsw = String.fromCharCode(116,95,53,51,95,100,114,97,103,0);
    let pathK = String.fromCharCode(110,111,110,100,99,95,101,95,56,0);
    let resendV: Map<any, any> = new Map([[String.fromCharCode(111,95,54,55,95,113,117,97,110,116,0),570], [String.fromCharCode(103,95,50,53,95,116,101,109,112,115,0),897], [String.fromCharCode(107,95,55,95,109,97,108,108,111,99,0),106]]);
    let thumbnail1 = String.fromCharCode(107,95,57,55,95,112,101,110,100,105,110,103,115,0);
    let overlayf = 5.0;
    let qaagI = String.fromCharCode(114,95,50,95,108,105,102,101,0);
    let clearD: Array<any> = [404, 169, 669];
    let modity_ = 4.0;
    let telemetryd = String.fromCharCode(105,109,112,117,108,115,101,95,120,95,51,57,0);
    let temperaturee = String.fromCharCode(103,95,53,55,95,103,108,111,98,0);
    let settingV = String.fromCharCode(120,95,52,51,95,100,116,109,102,0);
    let libmapbufferjnii = String.fromCharCode(100,95,50,56,95,115,99,104,101,100,0);
   if (!thumbnail1.startsWith(`${modity_}`)) {
       let libffmpegkitw = String.fromCharCode(110,95,49,48,48,95,99,111,109,109,105,115,115,105,111,110,0);
      if (libffmpegkitw.length >= libffmpegkitw.length) {
         libffmpegkitw = `${2 ^ libffmpegkitw.length}`;
      }
          let soundD = String.fromCharCode(115,116,114,99,115,112,110,95,99,95,53,56,0);
          let canvasa = true;
          let gestureC = String.fromCharCode(120,95,55,51,95,115,101,109,105,99,111,108,111,110,0);
         libffmpegkitw = `${1 ^ gestureC.length}`;
         soundD += `${soundD.length & 2}`;
         canvasa = 75 < soundD.length;
      let areaE = String.fromCharCode(51,98,49,105,111,53,0) == libffmpegkitw;
      do {
         libffmpegkitw += `${(String.fromCharCode(116,0) == libffmpegkitw ? libffmpegkitw.length : libffmpegkitw.length)}`;
         if (areaE) {
            break;
         }
      } while ((libffmpegkitw.endsWith(`${libffmpegkitw.length}`)) && areaE);
      modity_ *= 3;
   }
      qaagI += `${(thumbnail1 == String.fromCharCode(54,0) ? (mountingc ? 5 : 4) : thumbnail1.length)}`;
      resendV = new Map([[pathK, 2 * temperaturee.length]]);
      clearD.push(1 * termsw.length);
       let renews = 1.0;
       let found4 = 5.0;
      if ((renews - 2.81) <= 5.41 || 1.61 <= (2.81 / (Math.max(1, renews)))) {
         renews /= Math.max(parseInt(`${found4}`) & 2, 2);
      }
      let anytimeW = 8762284.0 <= renews;
      do {
         renews /= Math.max(parseInt(`${found4}`), 1);
         if (anytimeW) {
            break;
         }
      } while (anytimeW && (4.85 >= renews));
      if (found4 <= renews) {
         renews += parseInt(`${found4}`);
      }
          let dataL = String.fromCharCode(114,101,100,100,105,116,95,121,95,50,50,0);
          let mbbidf = 5;
         renews += 3;
         dataL = `${mbbidf / (Math.max(10, dataL.length))}`;
         mbbidf >>= Math.min(3, Math.abs(dataL.length % (Math.max(2, 7))));
      while (4.48 >= (found4 * renews) && (4.48 * renews) >= 3.68) {
         renews /= Math.max(3, parseInt(`${renews}`) * 3);
         break;
      }
      let combinedT = 9762097.0 >= renews;
      do {
         renews -= parseInt(`${found4}`);
         if (combinedT) {
            break;
         }
      } while (combinedT && ((found4 * 5) < 1.58));
      temperaturee += `${1 ^ parseInt(`${overlayf}`)}`;
   let successy = 9014069 <= resendV.size;
   do {
      resendV.set(termsw, termsw.length);
      if (successy) {
         break;
      }
   } while (successy && ((resendV.size * 4) <= 3));

        const utcTime = Math.floor((new Date()).getTime() / 1000);

   for (let g = 0; g < 1; g++) {
      modity_ -= parseInt(`${modity_}`);
   }
   for (let h = 0; h < 1; h++) {
       let castV = 0.0;
       let gesture2 = String.fromCharCode(104,95,53,48,95,102,108,101,120,105,98,108,101,0);
       let qaagE = 1.0;
       let fastW = String.fromCharCode(110,111,116,105,99,101,115,95,105,95,55,49,0);
       let gmailO = 2.0;
         gmailO += 3;
      let root2 = 9780037.0 >= qaagE;
      do {
         qaagE /= Math.max((String.fromCharCode(117,0) == fastW ? fastW.length : parseInt(`${qaagE}`)), 1);
         if (root2) {
            break;
         }
      } while (root2 && (qaagE <= gesture2.length));
         gesture2 += `${2 & fastW.length}`;
      for (let g = 0; g < 3; g++) {
         castV *= fastW.length;
      }
      for (let d = 0; d < 3; d++) {
         castV /= Math.max(parseInt(`${castV}`) / 2, 3);
      }
         qaagE *= fastW.length;
          let assist4: Map<any, any> = new Map([[String.fromCharCode(115,101,109,105,98,111,108,100,95,98,95,51,55,0),false ], [String.fromCharCode(98,95,49,48,95,109,97,116,101,114,105,97,108,105,122,101,0),true ]]);
          let type_1lC: Map<any, any> = new Map([[String.fromCharCode(106,95,54,95,105,116,111,97,0),370], [String.fromCharCode(116,114,97,110,115,102,111,114,109,115,95,115,95,50,50,0),668]]);
         qaagE /= Math.max(1, 2);
         assist4.set(`${type_1lC.size}`, 2);
         type_1lC.set(`${assist4.size}`, assist4.size);
      while (2 > (3 * fastW.length) || 3 > (parseInt(`${gmailO}`) * fastW.length)) {
         fastW = `${gesture2.length / 2}`;
         break;
      }
      if (1 == (1 - gesture2.length)) {
         gesture2 = `${fastW.length}`;
      }
      for (let r = 0; r < 1; r++) {
          let slidera = 5.0;
          let actionQ = 3.0;
          let heartO: Map<any, any> = new Map([[String.fromCharCode(117,95,50,49,95,99,97,99,104,101,102,108,117,115,104,0),String.fromCharCode(105,110,100,101,112,101,110,100,101,110,116,95,49,95,54,55,0)], [String.fromCharCode(115,112,108,105,116,115,95,111,95,49,54,0),String.fromCharCode(114,101,112,111,95,104,95,49,54,0)]]);
          let m_position9 = true;
         fastW = `${gesture2.length - 2}`;
         slidera *= parseInt(`${actionQ}`);
         actionQ -= parseFloat(`${parseInt(`${actionQ}`) / 3}`);
         heartO = new Map([[`${actionQ}`, parseInt(`${actionQ}`) | 1]]);
         m_position9 = heartO.get(`${slidera}`) == null;
      }
         castV += fastW.length;
          let mbbidp = 3;
          let topon2: Array<any> = [848, 285];
          let long_klS: Map<any, any> = new Map([[String.fromCharCode(112,97,114,97,95,119,95,55,56,0),true ], [String.fromCharCode(109,111,118,101,115,95,108,95,49,53,0),true ]]);
         castV *= 3;
         mbbidp <<= Math.min(4, Math.abs(long_klS.size));
         topon2 = [mbbidp];
         long_klS.set(`${topon2.length}`, long_klS.size);
         qaagE *= 1;
      let libpangleflippedn = 8372295.0 <= qaagE;
      do {
          let singleR = String.fromCharCode(105,109,97,103,101,115,95,118,95,50,54,0);
          let screenW = String.fromCharCode(108,95,55,48,95,97,118,117,116,105,108,0);
          let dnewarchdefaultsN: Map<any, any> = new Map([[String.fromCharCode(104,95,51,49,95,111,110,110,101,99,116,105,111,110,0),String.fromCharCode(99,111,109,112,111,110,101,110,116,115,95,106,95,48,0)], [String.fromCharCode(105,95,52,95,107,97,108,109,97,110,0),String.fromCharCode(103,95,49,54,95,102,105,101,108,100,115,0)], [String.fromCharCode(105,95,51,57,95,116,100,108,116,0),String.fromCharCode(101,95,49,50,95,108,105,110,107,105,110,103,0)]]);
         qaagE *= screenW.length;
         singleR = `${singleR.length / (Math.max(2, 10))}`;
         screenW += `${singleR.length}`;
         dnewarchdefaultsN.set(singleR, singleR.length - 3);
         if (libpangleflippedn) {
            break;
         }
      } while ((gesture2.startsWith(`${qaagE}`)) && libpangleflippedn);
      for (let x = 0; x < 3; x++) {
         gmailO /= Math.max(fastW.length, 4);
      }
      overlayf += parseFloat(`${resendV.size}`);
   }
   if ((3 - telemetryd.length) <= 4) {
       let tickV = String.fromCharCode(114,95,52,55,95,118,97,108,105,100,97,116,101,0);
       let fasth = 4;
       let libimagepipelineB: Array<any> = [123, 443, 670];
       let feedbackB = true;
       let changee = String.fromCharCode(120,117,116,105,108,95,121,95,50,57,0);
      for (let m = 0; m < 1; m++) {
          let floater5 = 0.0;
          let minimizez = 3;
         tickV += `${minimizez}`;
         floater5 *= 1;
         minimizez += parseInt(`${floater5}`);
      }
      if (3 < changee.length) {
          let libloggerK = 1.0;
          let changeI = String.fromCharCode(120,95,55,49,95,101,120,99,108,117,115,105,118,101,0);
         feedbackB = fasth >= 6;
         libloggerK -= parseFloat(`${changeI.length * 2}`);
         changeI = "2";
      }
         libimagepipelineB.push(libimagepipelineB.length * 3);
          let long_z9f = 2.0;
          let goalH: Map<any, any> = new Map([[String.fromCharCode(111,97,117,116,104,95,119,95,54,51,0),457], [String.fromCharCode(101,110,117,109,101,114,97,116,111,114,115,95,102,95,54,53,0),236], [String.fromCharCode(116,95,51,54,95,109,117,120,101,114,115,0),611]]);
          let pointp = true;
         changee += `${parseInt(`${long_z9f}`)}`;
         long_z9f /= Math.max(goalH.size, 4);
         goalH.set(`${pointp}`, 3 - goalH.size);
      if (libimagepipelineB.length <= 4) {
          let analyticso = String.fromCharCode(99,97,108,99,117,108,97,116,101,95,104,95,54,49,0);
         libimagepipelineB = [1];
         analyticso = `${2 ^ analyticso.length}`;
      }
      let shirtt = 7671312 <= tickV.length;
      do {
          let applicationt = String.fromCharCode(117,95,50,48,95,112,101,105,114,115,0);
          let yellow9 = String.fromCharCode(113,95,49,48,95,115,121,109,98,111,108,105,99,97,116,101,0);
          let videojsP = String.fromCharCode(109,118,114,101,102,95,109,95,51,55,0);
         tickV += `${applicationt.length}`;
         applicationt += `${yellow9.length}`;
         yellow9 += `${yellow9.length / 3}`;
         videojsP += `${(yellow9 == String.fromCharCode(99,0) ? videojsP.length : yellow9.length)}`;
         if (shirtt) {
            break;
         }
      } while (shirtt && (changee == tickV));
          let tempj: Array<any> = [358, 474];
          let rewardH = 4.0;
          let actionsu = false;
         fasth |= libimagepipelineB.length % (Math.max(1, 3));
         tempj.push(2 & tempj.length);
         rewardH *= (parseFloat(`${(actionsu ? 1 : 5) * 2}`));
      if (changee.length == tickV.length) {
         changee = `${3 ^ changee.length}`;
      }
      if (feedbackB) {
         feedbackB = libimagepipelineB.length == 93 && 93 == tickV.length;
      }
          let sourced = 5;
          let buttonW: Map<any, any> = new Map([[String.fromCharCode(99,95,50,55,95,115,117,98,109,105,116,0),String.fromCharCode(109,97,120,105,109,105,122,101,100,95,98,95,55,56,0)], [String.fromCharCode(115,101,115,115,111,110,95,54,95,51,52,0),String.fromCharCode(112,105,110,99,104,95,103,95,54,52,0)]]);
         libimagepipelineB.push(2 | sourced);
         sourced += buttonW.size;
         buttonW = new Map([[`${buttonW.size}`, buttonW.size]]);
      if (2 <= libimagepipelineB.length) {
         libimagepipelineB.push(tickV.length);
      }
         feedbackB = 84 < fasth;
         libimagepipelineB.push(2);
      for (let e = 0; e < 3; e++) {
          let activityE = String.fromCharCode(121,111,117,95,119,95,55,49,0);
          let r_playerZ = String.fromCharCode(105,112,97,100,95,54,95,49,54,0);
          let friends3 = String.fromCharCode(109,107,118,109,117,120,101,114,117,116,105,108,95,97,95,49,50,0);
          let config9 = 3;
          let send4 = false;
         fasth |= fasth;
         activityE += "1";
         r_playerZ = `${friends3.length}`;
         friends3 += `${activityE.length ^ r_playerZ.length}`;
         config9 += friends3.length;
         send4 = 14 <= r_playerZ.length;
      }
         fasth ^= changee.length + libimagepipelineB.length;
      telemetryd = "3";
   }
   while (qaagI.startsWith(`${overlayf}`)) {
      qaagI += `${3 + telemetryd.length}`;
      break;
   }
   if (resendV.size <= 2) {
      resendV = new Map([[`${eactx}`, parseInt(`${eactx}`) % 2]]);
   }
   if (1.43 <= (eactx - overlayf) || (1.43 - eactx) <= 3.57) {
      overlayf /= Math.max(1, parseFloat(`${3}`));
   }

        const json: yys_PhoneLibreactperfloggerjni = {
            stats: this.#events,
            created_at: utcTime,
        }
        const jsonStr = JSON.stringify(json);

       let colorsm = String.fromCharCode(111,95,51,95,99,111,114,114,101,108,97,116,101,0);
       let inviteD = String.fromCharCode(101,95,52,53,95,99,111,109,112,0);
       let google9 = String.fromCharCode(109,97,114,107,105,110,103,115,95,57,95,56,48,0);
      if (colorsm.endsWith(inviteD)) {
          let modityB = false;
          let libhermesI = 0;
          let libpangleflippedD: Array<any> = [String.fromCharCode(104,116,109,108,109,97,114,107,117,112,95,112,95,54,48,0), String.fromCharCode(109,95,50,53,95,97,115,115,101,114,116,115,0), String.fromCharCode(97,112,112,115,102,108,121,101,114,95,112,95,56,54,0)];
          let starC = String.fromCharCode(109,95,51,57,95,112,97,114,101,110,116,115,0);
          let libhermesV: Array<any> = [149, 435];
         inviteD += "3";
         modityB = libhermesI <= libpangleflippedD.length;
         libhermesI %= Math.max(libhermesI | 2, 4);
         libpangleflippedD = [2];
         starC = `${libhermesV.length & 1}`;
         libhermesV.push((String.fromCharCode(73,0) == starC ? libpangleflippedD.length : starC.length));
      }
      let libavformats = String.fromCharCode(119,53,55,118,113,98,104,0) == inviteD;
      do {
         inviteD += "1";
         if (libavformats) {
            break;
         }
      } while (libavformats && (google9.includes(inviteD)));
          let launchero: Map<any, any> = new Map([[String.fromCharCode(100,110,120,104,100,100,97,116,97,95,108,95,53,57,0),371], [String.fromCharCode(97,95,56,52,95,101,118,101,110,97,118,103,0),711]]);
          let penaltyG = 5.0;
          let strJ: Map<any, any> = new Map([[String.fromCharCode(100,105,115,112,108,97,99,101,95,50,95,54,51,0),559], [String.fromCharCode(104,95,54,95,97,115,115,105,103,110,109,101,110,116,0),916], [String.fromCharCode(116,109,105,120,95,110,95,57,48,0),1]]);
         google9 = `${strJ.size - 2}`;
         launchero.set(`${penaltyG}`, parseInt(`${penaltyG}`) << (Math.min(Math.abs(3), 1)));
         strJ = new Map([[`${launchero.size}`, 2]]);
      for (let d = 0; d < 2; d++) {
         google9 += `${(String.fromCharCode(90,0) == google9 ? google9.length : inviteD.length)}`;
      }
       let settingsy = 4.0;
       let stylesM = 0.0;
         settingsy /= Math.max(parseFloat(`${inviteD.length & 3}`), 3);
          let upgradeh = String.fromCharCode(109,95,57,57,95,99,105,110,97,117,100,105,111,0);
         stylesM -= parseFloat(`${3 >> (Math.min(1, inviteD.length))}`);
         upgradeh += `${upgradeh.length}`;
      let textinputI = 9688640.0 >= stylesM;
      do {
         stylesM -= parseFloat(`${inviteD.length}`);
         if (textinputI) {
            break;
         }
      } while (textinputI && (1.73 > (settingsy / 2)));
      let contextP = inviteD.length >= 5755039;
      do {
         inviteD = `${2 >> (Math.min(2, Math.abs(parseInt(`${stylesM}`))))}`;
         if (contextP) {
            break;
         }
      } while (contextP && (inviteD.startsWith(google9)));
      termsw += `${((mountingc ? 1 : 5) % 1)}`;
   while (5 <= (4 * clearD.length)) {
       let constantsf = true;
       let injurye = String.fromCharCode(99,95,50,53,95,114,116,112,0);
       let directE = String.fromCharCode(98,105,116,119,114,105,116,101,114,95,112,95,51,55,0);
       let progressg = 4.0;
          let collectionw = 3.0;
          let interstitialW: Array<any> = [718, 579, 478];
          let librrcU = 4.0;
         injurye = `${parseInt(`${progressg}`)}`;
         collectionw += interstitialW.length + 3;
         interstitialW = [interstitialW.length];
         librrcU -= 1;
         constantsf = 35.23 <= progressg;
         progressg *= parseFloat(`${directE.length << (Math.min(5, Math.abs(parseInt(`${progressg}`))))}`);
         constantsf = 67.34 == progressg || directE.length == 19;
       let customy: Array<any> = [620, 971, 113];
         customy.push(2);
      while (directE.length > 5) {
          let specI = 2.0;
          let philippines1 = String.fromCharCode(99,111,109,98,105,95,99,95,57,56,0);
          let const_gpi = String.fromCharCode(105,112,108,99,111,110,118,107,101,114,110,101,108,95,102,95,51,50,0);
          let filled2 = String.fromCharCode(103,95,57,51,95,109,117,108,116,105,101,110,100,0);
         injurye += `${customy.length / 1}`;
         specI -= 1 + philippines1.length;
         philippines1 = `${3 ^ parseInt(`${specI}`)}`;
         const_gpi += "1";
         filled2 = `${(const_gpi == String.fromCharCode(84,0) ? philippines1.length : const_gpi.length)}`;
         break;
      }
      for (let d = 0; d < 2; d++) {
         customy = [(String.fromCharCode(48,0) == injurye ? directE.length : injurye.length)];
      }
         constantsf = !constantsf;
      for (let x = 0; x < 1; x++) {
         progressg -= (parseFloat(`${String.fromCharCode(112,0) == injurye ? injurye.length : parseInt(`${progressg}`)}`));
      }
      if (constantsf) {
         constantsf = (31 > (injurye.length + (!constantsf ? injurye.length : 31)));
      }
         customy.push(parseInt(`${progressg}`) / (Math.max(customy.length, 5)));
      temperaturee += `${directE.length}`;
      break;
   }
   let downT = 6950933.0 >= overlayf;
   do {
      overlayf *= parseFloat(`${2 | clearD.length}`);
      if (downT) {
         break;
      }
   } while ((4.97 > (3.40 / (Math.max(5, overlayf))) && (overlayf / (Math.max(3.40, 8))) > 2.38) && downT);
   while (termsw.length > resendV.size) {
       let pingc = 0.0;
       let r_countz = String.fromCharCode(100,97,116,101,116,105,109,101,95,112,95,54,48,0);
       let agreementf = 0.0;
       let short_vU = String.fromCharCode(116,103,115,95,103,95,54,51,0);
       let sentryg = false;
         agreementf /= Math.max(parseFloat(`${parseInt(`${pingc}`)}`), 3);
      while (sentryg && 1.34 == (agreementf + 4.16)) {
         sentryg = 40.35 >= pingc || r_countz == String.fromCharCode(84,0);
         break;
      }
         pingc += parseFloat(`${r_countz.length - 1}`);
      while ((pingc / (Math.max(parseFloat(`${short_vU.length}`), 10))) >= 3.79 && (pingc / (Math.max(parseFloat(`${short_vU.length}`), 2))) >= 3.79) {
          let castQ = String.fromCharCode(115,119,105,116,99,104,95,53,95,54,48,0);
          let package_x2 = String.fromCharCode(100,95,50,52,95,105,115,115,117,105,110,103,0);
          let humidityn = 1.0;
          let dropdownl: Map<any, any> = new Map([[String.fromCharCode(104,95,57,53,95,108,105,102,101,116,105,109,101,0),193], [String.fromCharCode(100,101,99,111,100,101,114,115,95,110,95,49,55,0),392], [String.fromCharCode(110,111,116,105,102,121,105,110,103,95,122,95,51,50,0),18]]);
         short_vU = `${package_x2.length}`;
         castQ += `${1 | castQ.length}`;
         package_x2 = `${parseInt(`${humidityn}`)}`;
         humidityn /= Math.max(2, 1);
         dropdownl.set(`${humidityn}`, castQ.length);
         break;
      }
      while ((r_countz.length * 4) > 2 || 4 > (parseInt(`${pingc}`) / (Math.max(r_countz.length, 9)))) {
         r_countz += `${parseInt(`${agreementf}`)}`;
         break;
      }
      let commentu = String.fromCharCode(121,100,110,52,98,0) == r_countz;
      do {
          let userr = String.fromCharCode(97,99,116,117,97,108,105,122,101,100,95,111,95,52,48,0);
          let libflipper0 = String.fromCharCode(112,95,54,56,95,108,105,98,115,114,116,0);
          let unewinterstitial7: Array<any> = [String.fromCharCode(108,95,55,95,97,99,97,108,99,0), String.fromCharCode(102,95,53,57,95,117,110,98,111,110,100,0), String.fromCharCode(121,117,118,97,95,117,95,50,55,0)];
          let entryf: Map<any, any> = new Map([[String.fromCharCode(116,120,102,109,95,108,95,49,48,0),202], [String.fromCharCode(102,105,114,101,100,95,108,95,51,54,0),997], [String.fromCharCode(104,95,52,50,95,97,110,105,109,97,116,101,100,0),49]]);
         r_countz += `${parseInt(`${pingc}`) * 2}`;
         userr = `${unewinterstitial7.length - entryf.size}`;
         libflipper0 = `${entryf.size / (Math.max(3, 4))}`;
         unewinterstitial7 = [(userr == String.fromCharCode(114,0) ? userr.length : unewinterstitial7.length)];
         if (commentu) {
            break;
         }
      } while ((short_vU.length <= 3 || r_countz == String.fromCharCode(75,0)) && commentu);
          let typesA = 1;
          let mintegralY = 1;
         short_vU = `${short_vU.length}`;
         typesA >>= Math.min(5, Math.abs(mintegralY | 2));
         mintegralY *= typesA << (Math.min(Math.abs(3), 1));
      while (5.30 >= (agreementf + 5.37)) {
          let photoL = String.fromCharCode(102,114,101,101,112,97,100,100,114,115,95,52,95,49,48,48,0);
          let guideF = String.fromCharCode(98,110,100,101,99,95,109,95,54,52,0);
          let aboutk = 5.0;
          let mimoA = String.fromCharCode(99,111,111,107,100,97,116,97,95,98,95,53,52,0);
          let valuesO = 3;
         agreementf /= Math.max(parseFloat(`${parseInt(`${aboutk}`) % 3}`), 1);
         photoL += `${guideF.length - valuesO}`;
         guideF += `${mimoA.length}`;
         aboutk /= Math.max(2, (photoL == String.fromCharCode(110,0) ? guideF.length : photoL.length));
         mimoA += "2";
         valuesO ^= 3;
         break;
      }
      for (let w = 0; w < 2; w++) {
         agreementf *= parseFloat(`${parseInt(`${agreementf}`)}`);
      }
      for (let j = 0; j < 1; j++) {
          let fadfdeebbbfdabbbabdadfaaddaaw = 2.0;
          let update_2q: Map<any, any> = new Map([[String.fromCharCode(115,95,55,56,0),719], [String.fromCharCode(116,114,105,97,110,103,108,101,95,110,95,53,54,0),768]]);
          let miniG = 3.0;
          let utilsg: Map<any, any> = new Map([[String.fromCharCode(98,95,53,56,95,117,115,108,101,101,112,0),String.fromCharCode(100,114,111,112,95,116,95,51,50,0)], [String.fromCharCode(117,95,50,53,95,109,105,100,115,0),String.fromCharCode(97,95,51,56,95,103,114,111,119,116,104,0)]]);
         short_vU = `${r_countz.length}`;
         fadfdeebbbfdabbbabdadfaaddaaw /= Math.max(parseFloat(`${2}`), 2);
         update_2q = new Map([[`${utilsg.size}`, 2]]);
         miniG += 1 >> (Math.min(4, Math.abs(parseInt(`${fadfdeebbbfdabbbabdadfaaddaaw}`))));
         utilsg = new Map([[`${utilsg.size}`, utilsg.size]]);
      }
      let leftB = r_countz == String.fromCharCode(108,115,116,103,122,116,99,0);
      do {
         r_countz += `${short_vU.length}`;
         if (leftB) {
            break;
         }
      } while (leftB && (r_countz.length <= short_vU.length));
      for (let l = 0; l < 2; l++) {
          let condensedQ = String.fromCharCode(118,108,99,115,112,101,99,95,106,95,49,52,0);
          let libtanh = String.fromCharCode(115,95,50,51,95,115,99,114,117,98,98,101,114,0);
          let rulesW = false;
          let sports1 = String.fromCharCode(115,105,103,110,97,108,95,56,95,53,56,0);
         pingc -= parseFloat(`${condensedQ.length >> (Math.min(Math.abs(1), 2))}`);
         condensedQ = "3";
         libtanh = `${((rulesW ? 5 : 5) ^ 1)}`;
         sports1 = `${sports1.length * 1}`;
      }
      while (r_countz.startsWith(`${agreementf}`)) {
         r_countz += `${((sentryg ? 2 : 3) << (Math.min(Math.abs(parseInt(`${agreementf}`)), 5)))}`;
         break;
      }
      while (3 > short_vU.length) {
         sentryg = short_vU.length > r_countz.length;
         break;
      }
       let userQ = String.fromCharCode(120,95,51,52,95,115,116,114,116,121,112,101,0);
      resendV = new Map([[`${agreementf}`, termsw.length - 1]]);
      break;
   }
   for (let b = 0; b < 2; b++) {
      qaagI += "1";
   }
   if (2 <= (telemetryd.length & 3) || (telemetryd.length & clearD.length) <= 3) {
      telemetryd = `${parseInt(`${modity_}`) >> (Math.min(thumbnail1.length, 1))}`;
   }

        AsyncStorage.setItem(this.#localKey, jsonStr);
    }
}