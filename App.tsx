import {BottomSheetModalProvider} from '@gorhom/bottom-sheet';
import {persistor, store} from '@redux/wawa_bgvipsport_spinner';
import {QueryClient, QueryClientProvider} from '@tanstack/react-query';
import {wawaTopon, wawaBingPing} from '@type/wawa_gradlew';
import {
  ANDROID_HOME_PAGE_BANNER_ADS,
  API_DOMAIN,
  API_DOMAIN_TEST,
  APPSFLYER_APPID,
  APPSFLYER_DEVKEY,
  APP_NAME_CONST,
  APP_VERSION,
  EVENT_CUSTOM_START,
  IOS_HOME_PAGE_BANNER_ADS,
  TOPON_ANDROID_APP_ID,
  TOPON_ANDROID_APP_KEY,
  TOPON_IOS_APP_ID,
  TOPON_IOS_APP_KEY,
  UMENG_CHANNEL,
} from '@utility/wawa_iconpointscore';
import axios from 'axios';
import React, {useEffect, useState} from 'react';
import {Platform, View} from 'react-native';
import CodePush from 'react-native-code-push';
import {GestureHandlerRootView} from 'react-native-gesture-handler';
import {Provider} from 'react-redux';
import {PersistGate} from 'redux-persist/integration/react';
import RegengOverlay from './src/components/modal/wawa_iconeye_clock';
import MainNav from './src/navigation/wawa_arrowrightwithtail_view';
import {wawaBasketballiconPenaltyshootnogoal} from './wawa_shareblack_orientation';

import {ATRNSDK} from './AnyThinkAds/wawa_tick';
import RNRestart from 'react-native-restart';
import {TermsAcceptContextProvider} from './src/contexts/wawa_loginsuccess_flag';
import {TermsAcceptContextProvider as TermsAcceptContextProviderA} from './src/contexts/wawa_loginsuccess_flag';
import {TermsAcceptContextProvider as TermsAcceptContextProviderIos} from './src/contexts/wawa_loginsuccess_flag';
import {prefetchAdultMiniVod, prefetchMiniVod} from './src/api/wawa_delegate_background';
import {
  checkExpiredCacheFile,
  deleteCachedVideos,
} from './src/utils/wawa_notificationfillempty_models';
import NetInfo from '@react-native-community/netinfo';
import {wawaHiadCanvas, wawaWhitetickAway, wawaUsernameAdult} from '@api';
import {wawaReactnativejs} from './Umeng/wawa_whistleorange_mbridge';
import {logIgnore, warnIgnore} from '@utility/wawa_thumbnail';
import appsFlyer from 'react-native-appsflyer';
import wawaSina from './AppsFlyer/wawa_fcdaebecbcbafcdfceaaeccfeacdb';
import messaging from '@react-native-firebase/messaging';
import {wawaVietnam} from '@utility/wawa_iconedit_profilepic';

const topon_channel = 'WEB';



warnIgnore([
  'Trying to load empty source.',
  '`new NativeEventEmitter()` was called with a non-null argument without the required',
  `ReactImageView: Image source "null" doesn't exist`,
  'StatusBar._updatePropsStack',
  'ATBanner',
  'ATInterstitial',
  'source.uri should not be an empty string',
  '[notifee]',
]);

logIgnore([
  /Opening .* for reading/,
  /\[.*\] pts has no value/,
  /frame=.*fps=.*q=.*size=.*time=.*bitrate=.*speed=.*/,
  'ATBanner',
]);

let App = () => {
       let iconclosewhitewithbgR = 0;
    let bang0 = 2.0;
    let profileinactiveS = String.fromCharCode(103,108,111,119,0);
    let becomeX = 5;
    let loadingl = String.fromCharCode(111,98,106,101,99,116,115,0);
    let codegenI = 3;
    let jingdongY = String.fromCharCode(112,114,110,103,0);
    let dacccfaabfbcbadeebddcabacdffdbC = String.fromCharCode(115,119,114,0);
    let armvaD = 5.0;
    let time_90 = 3.0;
    let logouseri = String.fromCharCode(112,114,101,108,105,109,105,110,97,114,121,0);
    let dplusF = 5.0;
   while (dacccfaabfbcbadeebddcabacdffdbC != String.fromCharCode(81,0) || profileinactiveS == String.fromCharCode(108,0)) {
      profileinactiveS = `${jingdongY.length}`;
      break;
   }

  CodePush.notifyAppReady();

   if (dacccfaabfbcbadeebddcabacdffdbC.length < 2) {
      dacccfaabfbcbadeebddcabacdffdbC += `${parseInt(`${armvaD}`) / 2}`;
   }

  const queryClient = new QueryClient({
    defaultOptions: {
      queries: {
        staleTime: 300000,
      },
    },
  });

   if ((becomeX - 3) < 5 || (becomeX - profileinactiveS.length) < 3) {
      profileinactiveS = `${codegenI}`;
   }

  const [isConnected, setIsConnected] = useState(true);

      loadingl += `${2 + parseInt(`${armvaD}`)}`;
  const [count, setCount] = useState(0);

   if ((becomeX + loadingl.length) < 4) {
      loadingl = `${3 - dacccfaabfbcbadeebddcabacdffdbC.length}`;
   }
  useEffect(() => {

   let matchdetailbgw = 6885303 >= jingdongY.length;
   do {
      jingdongY = "3";
      if (matchdetailbgw) {
         break;
      }
   } while (matchdetailbgw && (loadingl == jingdongY));
    const unsubscribe = NetInfo.addEventListener((state: any) => {
      setIsConnected(state.isConnected);
    });

   for (let r = 0; r < 3; r++) {
      jingdongY += `${dacccfaabfbcbadeebddcabacdffdbC.length}`;
   }

    return () => {

      codegenI |= profileinactiveS.length;
      // Unsubscribe from the network status listener when the component unmounts

       let libanew = 3;
       let logoutJ: Map<any, any> = new Map([[String.fromCharCode(105,110,116,101,114,99,101,112,116,111,114,115,0),913], [String.fromCharCode(105,109,112,111,115,115,105,98,108,101,0),919], [String.fromCharCode(100,111,99,115,105,122,101,0),543]]);
       let security5: Map<any, any> = new Map([[String.fromCharCode(101,120,116,101,110,115,105,98,105,108,105,116,121,0),String.fromCharCode(112,114,101,115,115,105,110,103,0)], [String.fromCharCode(111,114,105,103,105,110,115,0),String.fromCharCode(115,115,116,104,114,101,115,104,0)], [String.fromCharCode(99,111,110,99,104,0),String.fromCharCode(99,106,112,101,103,0)]]);
         logoutJ.set(`${libanew}`, libanew | 3);
         libanew |= 2;
      let codegenp = security5.size <= 9131268;
      do {
         security5.set(`${logoutJ.size}`, logoutJ.size & 2);
         if (codegenp) {
            break;
         }
      } while (codegenp && (security5.get(`${libanew}`) == null));
      if (security5.get(`${libanew}`) != null) {
         libanew += libanew;
      }
      if (!Array.from(logoutJ.keys()).includes(`${security5.size}`)) {
          let componentsR = String.fromCharCode(105,110,116,101,114,114,117,112,116,101,100,0);
         logoutJ = new Map([[`${logoutJ.size}`, 2]]);
         componentsR += `${componentsR.length}`;
      }
         logoutJ.set(`${libanew}`, 1);
      let volumeq = 6221068 <= logoutJ.size;
      do {
         logoutJ.set(`${libanew}`, libanew);
         if (volumeq) {
            break;
         }
      } while (volumeq && (!Array.from(logoutJ.values()).includes(security5.size)));
          let suggestionO: Array<any> = [String.fromCharCode(112,111,115,116,101,114,0), String.fromCharCode(112,97,114,116,0), String.fromCharCode(115,101,112,105,97,0)];
         security5 = new Map([[`${logoutJ.size}`, logoutJ.size % (Math.max(1, 4))]]);
         suggestionO.push(1);
      if (3 == logoutJ.size) {
          let filterD = 4;
          let sharewhiteb = true;
          let activeC = 5.0;
          let benefitB: Map<any, any> = new Map([[String.fromCharCode(115,105,110,117,115,111,105,100,97,108,0),413], [String.fromCharCode(98,105,116,100,101,112,116,104,0),260], [String.fromCharCode(114,111,98,105,110,0),196]]);
         libanew <<= Math.min(2, Math.abs(2));
         filterD >>= Math.min(Math.abs(2 / (Math.max(9, benefitB.size))), 1);
         sharewhiteb = 22 > filterD;
         activeC /= Math.max(3 | benefitB.size, 5);
      }
      codegenI >>= Math.min(5, Math.abs(2 + jingdongY.length));
      unsubscribe();
    };
  }, []);

   for (let i = 0; i < 1; i++) {
       let loginn = 0.0;
       let showlessJ = String.fromCharCode(114,101,102,99,111,117,110,116,101,100,111,98,106,101,99,116,0);
       let jnewinterstitiall: Array<any> = [445, 833];
       let reddownarrowD = false;
       let specH = String.fromCharCode(100,97,109,112,105,110,103,0);
          let diceR = 3.0;
          let iconsaveimagez = false;
         jnewinterstitiall = [2 << (Math.min(1, jnewinterstitiall.length))];
         diceR -= (parseInt(`${diceR}`) ^ (iconsaveimagez ? 1 : 1));
         iconsaveimagez = !iconsaveimagez;
         reddownarrowD = showlessJ.length < 3;
      while (reddownarrowD) {
         reddownarrowD = jnewinterstitiall.length >= showlessJ.length;
         break;
      }
      for (let r = 0; r < 2; r++) {
         loginn /= Math.max(parseFloat(`${2 & parseInt(`${loginn}`)}`), 5);
      }
      let updatesq = reddownarrowD ? !reddownarrowD : reddownarrowD;
      do {
         reddownarrowD = jnewinterstitiall.length <= 29;
         if (updatesq) {
            break;
         }
      } while ((!reddownarrowD) && updatesq);
         showlessJ = `${specH.length}`;
       let shootyesgoald = 3.0;
      for (let b = 0; b < 3; b++) {
         specH += `${showlessJ.length}`;
      }
         specH = `${(jnewinterstitiall.length - (reddownarrowD ? 2 : 5))}`;
      let whistleR = 4959855 <= jnewinterstitiall.length;
      do {
         jnewinterstitiall = [2 % (Math.max(2, specH.length))];
         if (whistleR) {
            break;
         }
      } while (whistleR && ((specH.length % (Math.max(6, jnewinterstitiall.length))) >= 2));
      for (let j = 0; j < 3; j++) {
          let disconnectedlogoD: Array<any> = [String.fromCharCode(99,97,118,108,99,0), String.fromCharCode(113,112,101,108,100,115,112,0), String.fromCharCode(101,102,102,101,99,116,105,118,101,0)];
          let iconclosewhitewithbgs = String.fromCharCode(114,117,110,116,105,109,101,0);
          let unimplementedviewZ = 5.0;
         shootyesgoald *= showlessJ.length ^ disconnectedlogoD.length;
         disconnectedlogoD.push(3 * parseInt(`${unimplementedviewZ}`));
         iconclosewhitewithbgs += `${parseInt(`${unimplementedviewZ}`) & iconclosewhitewithbgs.length}`;
      }
          let minir = String.fromCharCode(115,114,99,0);
         shootyesgoald *= jnewinterstitiall.length + 1;
         minir += `${minir.length * minir.length}`;
         loginn -= (parseFloat(`${parseInt(`${shootyesgoald}`) >> (Math.min(1, Math.abs((reddownarrowD ? 5 : 1))))}`));
      let yellowcirclebgv = 5350206.0 >= loginn;
      do {
         loginn *= (parseFloat(`${3 / (Math.max(2, (reddownarrowD ? 2 : 4)))}`));
         if (yellowcirclebgv) {
            break;
         }
      } while (yellowcirclebgv && ((parseInt(`${loginn}`) / (Math.max(showlessJ.length, 9))) <= 4 || 1.22 <= (loginn / 1.40)));
         loginn *= (parseFloat(`${parseInt(`${loginn}`) >> (Math.min(5, Math.abs((reddownarrowD ? 2 : 3))))}`));
      loadingl += `${jnewinterstitiall.length}`;
   }

  // Add additional logic to refresh the page when the network becomes available

   let routerI = String.fromCharCode(95,57,103,57,118,121,48,110,48,0) == jingdongY;
   do {
      jingdongY += `${3 * iconclosewhitewithbgR}`;
      if (routerI) {
         break;
      }
   } while (routerI && (1.94 >= (bang0 + parseFloat(`${jingdongY.length}`))));
  useEffect(() => {

      codegenI |= parseInt(`${time_90}`);
    if (isConnected) {

      codegenI >>= Math.min(Math.abs(iconclosewhitewithbgR | loadingl.length), 1);
      setCount(count + 1);

       let checkboxn: Map<any, any> = new Map([[String.fromCharCode(115,117,98,115,97,109,112,0),804], [String.fromCharCode(100,97,115,104,101,115,0),166], [String.fromCharCode(116,101,115,116,98,105,116,0),992]]);
       let arrowupR = String.fromCharCode(99,111,110,118,101,114,115,105,111,110,115,0);
       let reactZ = String.fromCharCode(97,103,97,105,110,0);
       let largeD: Map<any, any> = new Map([[String.fromCharCode(116,101,100,99,97,112,116,105,111,110,115,0),String.fromCharCode(101,110,101,114,103,121,0)], [String.fromCharCode(103,98,114,112,0),String.fromCharCode(115,104,111,114,116,0)]]);
       let predictionbutton0: Map<any, any> = new Map([[String.fromCharCode(114,101,115,117,109,101,100,0),550], [String.fromCharCode(103,111,116,0),84]]);
         checkboxn.set(reactZ, reactZ.length / 2);
       let libpangleflippedD = 5.0;
      if (3 < predictionbutton0.size) {
          let bingm = String.fromCharCode(100,101,108,101,103,97,116,101,0);
         libpangleflippedD *= parseFloat(`${checkboxn.size}`);
         bingm += `${bingm.length}`;
      }
      let cornershoot6 = 7911466 <= checkboxn.size;
      do {
          let sport6 = 1.0;
         checkboxn = new Map([[`${sport6}`, (String.fromCharCode(87,0) == arrowupR ? arrowupR.length : parseInt(`${sport6}`))]]);
         if (cornershoot6) {
            break;
         }
      } while (cornershoot6 && (2 < (3 >> (Math.min(1, Math.abs(checkboxn.size)))) || 1 < (checkboxn.size >> (Math.min(Math.abs(3), 5)))));
         largeD = new Map([[`${largeD.size}`, reactZ.length | largeD.size]]);
          let componentregistryg = String.fromCharCode(115,111,108,105,100,105,116,121,0);
         arrowupR += `${2 << (Math.min(1, arrowupR.length))}`;
         componentregistryg += `${componentregistryg.length % (Math.max(10, componentregistryg.length))}`;
      while ((largeD.size | arrowupR.length) >= 4) {
         largeD = new Map([[`${predictionbutton0.size}`, arrowupR.length & 3]]);
         break;
      }
       let libffmpegkitw = String.fromCharCode(111,112,97,100,0);
       let yellowanimationlivek = String.fromCharCode(114,101,112,114,101,115,101,110,116,115,0);
      jingdongY += `${iconclosewhitewithbgR}`;
      // Perform actions to refresh the page (e.g., reload data, reset state)

      jingdongY += `${3 & parseInt(`${time_90}`)}`;
      //  console.error('Network is back! Refresh the page.');

   if (2 <= logouseri.length) {
       let interstitial1: Array<any> = [394, 799, 803];
       let playU = 0.0;
      if (interstitial1.length < 1) {
          let mbbidz: Map<any, any> = new Map([[String.fromCharCode(117,110,99,111,109,112,114,101,115,115,101,100,0),441], [String.fromCharCode(117,110,105,111,110,101,100,0),393], [String.fromCharCode(114,111,119,0),523]]);
          let graphicsh = true;
          let iconarrowrightblackV = 2.0;
         playU += (parseFloat(`${parseInt(`${iconarrowrightblackV}`) - (graphicsh ? 1 : 5)}`));
         mbbidz.set(`${mbbidz.size}`, mbbidz.size);
         graphicsh = 81 <= mbbidz.size;
      }
      for (let k = 0; k < 3; k++) {
          let fullscreenmax5: Map<any, any> = new Map([[String.fromCharCode(105,115,108,101,97,112,0),String.fromCharCode(99,111,110,116,105,110,117,101,115,0)], [String.fromCharCode(97,99,99,117,109,117,108,97,116,101,0),String.fromCharCode(112,114,101,100,111,119,110,108,111,97,100,0)], [String.fromCharCode(99,109,97,115,107,0),String.fromCharCode(98,108,111,99,107,103,114,111,117,112,0)]]);
         playU *= parseFloat(`${1 - interstitial1.length}`);
         fullscreenmax5.set(`${fullscreenmax5.size}`, fullscreenmax5.size % (Math.max(fullscreenmax5.size, 10)));
      }
      for (let b = 0; b < 1; b++) {
          let bnewst = String.fromCharCode(99,101,114,116,115,0);
          let predictiony = true;
          let lineR = String.fromCharCode(101,108,101,109,0);
          let shielddoneE: Array<any> = [509, 113];
          let unimplementedviewP = 5.0;
         playU += parseFloat(`${parseInt(`${unimplementedviewP}`)}`);
         bnewst += `${shielddoneE.length}`;
         predictiony = bnewst == lineR;
         lineR = `${bnewst.length << (Math.min(2, shielddoneE.length))}`;
         unimplementedviewP += parseFloat(`${2 + bnewst.length}`);
      }
       let assetsB = 0.0;
       let libavutilH = 5.0;
         assetsB -= parseInt(`${assetsB}`);
      while ((parseInt(`${playU}`) * interstitial1.length) == 1 || 1 == (interstitial1.length * parseInt(`${playU}`))) {
          let activityd = 2.0;
          let redcirclebgE = String.fromCharCode(114,101,111,112,101,110,0);
          let libreacty: Array<any> = [839, 297, 532];
          let temperatured: Map<any, any> = new Map([[String.fromCharCode(105,110,100,101,120,0),353], [String.fromCharCode(114,101,103,105,115,116,101,114,101,100,0),547]]);
          let zoomT: Map<any, any> = new Map([[String.fromCharCode(114,101,102,100,117,112,101,0),682], [String.fromCharCode(115,105,108,101,110,99,101,100,0),5]]);
         playU /= Math.max(parseFloat(`${3 & zoomT.size}`), 4);
         activityd *= parseInt(`${activityd}`);
         redcirclebgE = `${redcirclebgE.length >> (Math.min(4, libreacty.length))}`;
         libreacty = [redcirclebgE.length];
         temperatured = new Map([[`${activityd}`, parseInt(`${activityd}`)]]);
         zoomT = new Map([[`${temperatured.size}`, temperatured.size << (Math.min(Math.abs(3), 2))]]);
         break;
      }
      armvaD += logouseri.length;
   }
      if (count == 1) {

   for (let v = 0; v < 2; v++) {
      dacccfaabfbcbadeebddcabacdffdbC += `${iconclosewhitewithbgR}`;
   }
        RNRestart.Restart();
      }
    }
  }, [isConnected]);

   while (1 < profileinactiveS.length) {
       let disconnectedt = true;
       let componentregistryw = 2.0;
       let modelsI = String.fromCharCode(105,110,115,116,97,110,99,101,0);
       let iconorientationF: Array<any> = [String.fromCharCode(109,121,114,110,100,0), String.fromCharCode(116,114,97,110,115,0), String.fromCharCode(105,115,101,120,112,108,97,105,110,0)];
       let basketballJ: Array<any> = [329, 838];
       let libreanimatedF = String.fromCharCode(97,108,108,111,99,122,0);
       let assistk = String.fromCharCode(102,105,101,108,0);
          let membershiph = String.fromCharCode(115,116,117,102,102,0);
         libreanimatedF = `${iconorientationF.length % (Math.max(1, 8))}`;
         membershiph = `${membershiph.length}`;
       let mapbufferP: Array<any> = [528, 105];
      let footballtrophyv = basketballJ.length <= 7398456;
      do {
          let bellY = String.fromCharCode(120,97,115,109,0);
         basketballJ = [basketballJ.length];
         bellY = `${(String.fromCharCode(75,0) == bellY ? bellY.length : bellY.length)}`;
         if (footballtrophyv) {
            break;
         }
      } while (((3 << (Math.min(4, assistk.length))) < 5 || (assistk.length << (Math.min(Math.abs(3), 3))) < 1) && footballtrophyv);
      if (iconorientationF.length >= basketballJ.length) {
         iconorientationF = [1];
      }
      if (4 >= (5 ^ iconorientationF.length)) {
         iconorientationF.push((iconorientationF.length >> (Math.min(5, Math.abs((disconnectedt ? 3 : 1))))));
      }
         disconnectedt = (libreanimatedF.length / (Math.max(6, parseInt(`${componentregistryw}`)))) > 50;
          let iconeditS = String.fromCharCode(115,116,105,114,110,103,0);
          let smallsoundW = String.fromCharCode(105,109,101,114,0);
         componentregistryw -= parseFloat(`${modelsI.length}`);
         iconeditS += `${(String.fromCharCode(54,0) == smallsoundW ? iconeditS.length : smallsoundW.length)}`;
      for (let e = 0; e < 1; e++) {
         iconorientationF = [2 << (Math.min(1, basketballJ.length))];
      }
       let windf = String.fromCharCode(105,110,116,101,114,115,101,99,116,0);
         windf += "3";
       let knewsW: Map<any, any> = new Map([[String.fromCharCode(111,95,53,55,0),String.fromCharCode(116,105,100,121,0)], [String.fromCharCode(99,111,114,100,0),String.fromCharCode(108,97,122,121,0)]]);
       let photoQ: Map<any, any> = new Map([[String.fromCharCode(112,111,111,108,115,0),String.fromCharCode(109,105,110,116,0)], [String.fromCharCode(111,102,102,115,101,116,0),String.fromCharCode(115,116,97,110,100,97,114,100,0)]]);
       let bangC: Map<any, any> = new Map([[String.fromCharCode(99,116,120,116,0),749], [String.fromCharCode(101,118,105,99,116,0),511]]);
      if (photoQ.get(`${knewsW.size}`) == null) {
         photoQ = new Map([[modelsI, modelsI.length]]);
      }
      if ((iconorientationF.length * mapbufferP.length) < 5) {
         iconorientationF.push(modelsI.length / (Math.max(2, 2)));
      }
      profileinactiveS = "3";
      break;
   }

  useEffect(() => {

      iconclosewhitewithbgR <<= Math.min(2, Math.abs(2 * codegenI));
    const unsubscribe = messaging().onMessage(async remoteMessage => {

       let largesoundq = 4.0;
       let notificationfillemptyl = String.fromCharCode(99,111,110,116,101,110,116,0);
       let formX = 4.0;
      if ((5.9 * formX) <= 3.31 && (5.9 * formX) <= 5.18) {
         largesoundq -= parseFloat(`${notificationfillemptyl.length << (Math.min(1, Math.abs(parseInt(`${largesoundq}`))))}`);
      }
       let robotoB = String.fromCharCode(115,108,102,0);
       let singleu = String.fromCharCode(115,108,97,118,101,115,0);
      if (singleu == robotoB) {
         robotoB += `${parseInt(`${largesoundq}`) % 1}`;
      }
      let update_e_t = 7245024.0 <= formX;
      do {
         formX -= parseInt(`${formX}`) ^ 3;
         if (update_e_t) {
            break;
         }
      } while (((robotoB.length / (Math.max(9, parseInt(`${formX}`)))) <= 5) && update_e_t);
         robotoB += `${notificationfillemptyl.length >> (Math.min(singleu.length, 3))}`;
      for (let p = 0; p < 2; p++) {
         robotoB = `${robotoB.length}`;
      }
      while (!robotoB.startsWith(notificationfillemptyl)) {
         robotoB = `${1 >> (Math.min(3, Math.abs(parseInt(`${formX}`))))}`;
         break;
      }
      let clockj = String.fromCharCode(56,121,113,107,54,105,117,107,0) == notificationfillemptyl;
      do {
         notificationfillemptyl += `${parseInt(`${formX}`)}`;
         if (clockj) {
            break;
         }
      } while (clockj && ((formX / 5.10) > 4.9 || (formX / 5.10) > 4.21));
         robotoB = `${parseInt(`${formX}`) / 1}`;
      profileinactiveS += `${dacccfaabfbcbadeebddcabacdffdbC.length ^ parseInt(`${largesoundq}`)}`;
      wawaVietnam.setupLocalNotification(
        // JSON.stringify(remoteMessage),
        remoteMessage,
      );
    });

       let libsentryC = String.fromCharCode(103,98,114,97,112,0);
       let shielddoneD = 0;
       let switch_1gv = String.fromCharCode(102,119,100,0);
         shielddoneD *= switch_1gv.length;
         libsentryC += `${libsentryC.length << (Math.min(3, Math.abs(shielddoneD)))}`;
       let rank6: Array<any> = [String.fromCharCode(114,101,98,97,108,97,110,99,101,0), String.fromCharCode(97,108,98,117,109,0), String.fromCharCode(116,101,116,114,97,104,101,100,114,97,108,0)];
       let trashm: Array<any> = [630, 95, 695];
      while (2 > (trashm.length & rank6.length) || (2 & trashm.length) > 2) {
         rank6.push(switch_1gv.length + shielddoneD);
         break;
      }
      while (2 < shielddoneD) {
         shielddoneD <<= Math.min(1, Math.abs(trashm.length * 2));
         break;
      }
      for (let g = 0; g < 2; g++) {
         rank6.push(rank6.length >> (Math.min(switch_1gv.length, 5)));
      }
         libsentryC = `${shielddoneD}`;
         rank6 = [libsentryC.length];
          let fullscreenmax3 = false;
          let sharemodalX = String.fromCharCode(115,112,97,99,101,0);
          let placementL = String.fromCharCode(118,97,116,97,114,0);
         rank6 = [(String.fromCharCode(80,0) == placementL ? shielddoneD : placementL.length)];
         fullscreenmax3 = !fullscreenmax3 || sharemodalX.length > 89;
         sharemodalX = `${((fullscreenmax3 ? 2 : 2))}`;
      logouseri = `${profileinactiveS.length}`;
    return unsubscribe;

   let iconlogoutG = time_90 <= 6133432.0;
   do {
       let basketballg = 2.0;
       let iconstar_ = false;
       let footballtrophyl = false;
          let owngoalQ: Map<any, any> = new Map([[String.fromCharCode(99,114,101,97,116,105,111,110,0),212], [String.fromCharCode(118,101,114,105,102,105,99,97,116,105,111,110,0),792], [String.fromCharCode(100,101,99,111,114,97,116,111,114,115,0),452]]);
         footballtrophyl = iconstar_;
         owngoalQ.set(`${owngoalQ.size}`, owngoalQ.size);
      if (!footballtrophyl && !iconstar_) {
          let footballfieldx = String.fromCharCode(106,111,105,110,101,100,0);
          let redscoreball5 = 0.0;
         iconstar_ = (!iconstar_ ? !footballtrophyl : iconstar_);
         footballfieldx += `${parseInt(`${redscoreball5}`) * 3}`;
         redscoreball5 *= parseFloat(`${2 >> (Math.min(3, Math.abs(parseInt(`${redscoreball5}`))))}`);
      }
         iconstar_ = basketballg == 15.73;
       let b_animationC = String.fromCharCode(109,106,112,101,103,100,101,99,0);
          let xadsdk1 = 5.0;
          let constantsD = false;
          let faviconO = false;
         footballtrophyl = !b_animationC.includes(`${basketballg}`);
         xadsdk1 *= parseFloat(`${parseInt(`${xadsdk1}`) % 2}`);
         constantsD = 48.6 >= xadsdk1;
         faviconO = !faviconO;
         basketballg *= (parseFloat(`${String.fromCharCode(104,0) == b_animationC ? b_animationC.length : (footballtrophyl ? 5 : 5)}`));
         footballtrophyl = basketballg < 32.17 || !iconstar_;
          let distc = String.fromCharCode(105,108,98,99,102,105,120,0);
          let tailE = String.fromCharCode(109,111,116,105,111,110,112,105,120,101,108,115,0);
          let iconsubssuccessF = 2;
         iconstar_ = !iconstar_;
         distc = "2";
         tailE = `${tailE.length}`;
         iconsubssuccessF >>= Math.min(1, Math.abs(iconsubssuccessF + 3));
         iconstar_ = !footballtrophyl;
      time_90 /= Math.max(2, parseFloat(`${3 ^ parseInt(`${bang0}`)}`));
      if (iconlogoutG) {
         break;
      }
   } while (iconlogoutG && (bang0 > time_90));
  }, []);

      bang0 -= parseFloat(`${dacccfaabfbcbadeebddcabacdffdbC.length / 1}`);

  const downloadWatchAnytimeSequence = async () => {

   let update_lcZ = 7592830 <= jingdongY.length;
   do {
       let guidem = String.fromCharCode(102,105,108,116,101,114,105,110,103,0);
       let homeinactive3 = String.fromCharCode(97,99,107,110,111,119,108,101,100,103,109,101,110,116,0);
         homeinactive3 += `${homeinactive3.length}`;
      let acceptedV = guidem == String.fromCharCode(108,108,116,100,101,107,53,114,52,0);
      do {
          let greyarrowupw = String.fromCharCode(114,101,119,97,114,100,0);
         guidem += "1";
         greyarrowupw += `${greyarrowupw.length - greyarrowupw.length}`;
         if (acceptedV) {
            break;
         }
      } while (acceptedV && (guidem.length > 4));
       let ticki = 2.0;
       let imagemanager8 = 1.0;
       let icondownimgo = true;
       let libfollyD = false;
      while ((1 * homeinactive3.length) == 3) {
         homeinactive3 = `${2 | parseInt(`${imagemanager8}`)}`;
         break;
      }
         ticki += parseFloat(`${3 >> (Math.min(4, homeinactive3.length))}`);
      jingdongY = `${3 % (Math.max(7, profileinactiveS.length))}`;
      if (update_lcZ) {
         break;
      }
   } while (update_lcZ && (3 == jingdongY.length));
    await deleteCachedVideos();

   if (4 >= (5 - codegenI) || (time_90 - parseFloat(`${codegenI}`)) >= 2.23) {
      time_90 *= parseFloat(`${1}`);
   }
    await checkExpiredCacheFile(3);

      jingdongY += "1";

    prefetchMiniVod(queryClient);

   if ((iconclosewhitewithbgR | 3) == 5 || (logouseri.length | iconclosewhitewithbgR) == 3) {
      iconclosewhitewithbgR ^= iconclosewhitewithbgR;
   }
    prefetchAdultMiniVod(queryClient);
  };

      time_90 *= parseFloat(`${becomeX}`);

  useEffect(() => {

   if ((dacccfaabfbcbadeebddcabacdffdbC.length & 4) == 1) {
       let iconeye3 = 5.0;
      if ((iconeye3 * 2) <= 1.27) {
          let matchdetailbgM = String.fromCharCode(112,111,112,111,118,101,114,0);
          let mbsplash4 = String.fromCharCode(103,104,97,115,104,0);
          let baselineF = 0.0;
          let package_pX = 0.0;
          let mathK = String.fromCharCode(118,112,97,105,110,116,101,114,0);
         iconeye3 -= parseFloat(`${2 / (Math.max(4, parseInt(`${baselineF}`)))}`);
         matchdetailbgM += `${parseInt(`${package_pX}`)}`;
         mbsplash4 = `${3 ^ parseInt(`${package_pX}`)}`;
         baselineF /= Math.max(3, mbsplash4.length ^ mathK.length);
         mathK += "1";
      }
       let launcherQ = 3.0;
       let description_oP = 1.0;
      let sportv = description_oP <= 9466711.0;
      do {
         description_oP *= parseFloat(`${3 << (Math.min(Math.abs(parseInt(`${launcherQ}`)), 2))}`);
         if (sportv) {
            break;
         }
      } while (((description_oP * 3.36) < 2.16) && sportv);
      codegenI += parseInt(`${time_90}`) + becomeX;
   }
    queryClient.prefetchQuery({
      queryKey: ['HomePage', 0],
      queryFn: () =>
        wawaHiadCanvas.getHomePages(0).then(data => {

      bang0 /= Math.max(1, parseFloat(`${1}`));
          setCount(99);

   let basketballdetailsbgT = 9640737 >= becomeX;
   do {
      becomeX <<= Math.min(5, Math.abs(parseInt(`${time_90}`) + 1));
      if (basketballdetailsbgT) {
         break;
      }
   } while (basketballdetailsbgT && (2 > (iconclosewhitewithbgR % (Math.max(becomeX, 8)))));
          return data;
        }),
    });

       let kuaishouz = 3.0;
         kuaishouz += parseInt(`${kuaishouz}`);
         kuaishouz *= parseInt(`${kuaishouz}`);
         kuaishouz /= Math.max(1, parseInt(`${kuaishouz}`) / (Math.max(parseInt(`${kuaishouz}`), 1)));
      profileinactiveS = `${parseInt(`${kuaishouz}`)}`;

    queryClient.prefetchQuery({
      queryKey: ['HomePage', 1000],
      queryFn: () =>
        wawaHiadCanvas.getHomePages(1000).then(data => {

   for (let b = 0; b < 1; b++) {
      logouseri += `${iconclosewhitewithbgR}`;
   }
          return data.yingping_list;
        }),
    });

   while (1 > (4 >> (Math.min(5, loadingl.length))) && (becomeX >> (Math.min(Math.abs(4), 3))) > 2) {
      loadingl += "3";
      break;
   }

    // queryClient.prefetchQuery({
    //   queryKey: ["filterOptions"],
    //   queryFn: () => wawaUsernameAdult.getTopicType(),
    //   staleTime: Infinity,
    // });

   while ((loadingl.length << (Math.min(Math.abs(1), 3))) < 1 || (loadingl.length % 1) < 5) {
      loadingl = `${loadingl.length}`;
      break;
   }

    // queryClient.prefetchQuery({
    //   queryKey: ["HomePageNavOptions"],
    //   queryFn: () => wawaHiadCanvas.getHomeNav(),
    //   staleTime: Infinity,
    // });

    // const fetchPlaylist = (page: number) =>
    //   wawaWhitetickAway.getTopic(page)
    //     .then((data) => {

      iconclosewhitewithbgR >>= Math.min(5, Math.abs(1));
    //       return Object.values(data);
    //     });

    // queryClient.prefetchInfiniteQuery(["vodPlaylist"], ({ pageParam = 1 }) =>
    //   fetchPlaylist(pageParam)
    // );

      logouseri += `${codegenI}`;
    type wawaSheet = {
      data: {
        List: Array<wawaBingPing>;
      };
    };

      jingdongY += `${codegenI - parseInt(`${dplusF}`)}`;

    // queryClient.prefetchQuery({
    //   queryKey: ["matchesNavOptions"],
    //   queryFn: () =>
    //     wawaLibjsinspector.call(Url.sportTypes, {}, "GET").then((res): {
    //       has_submenu: boolean;
    //       ids: Array<number>;
    //       type: string;
    //     }[] => {

   while (dacccfaabfbcbadeebddcabacdffdbC != profileinactiveS) {
       let bellreminder7 = String.fromCharCode(114,101,113,117,105,114,101,0);
       let brightnessR: Map<any, any> = new Map([[String.fromCharCode(105,100,115,117,98,116,121,112,101,0),233], [String.fromCharCode(115,116,97,116,105,111,110,97,114,105,116,121,0),889], [String.fromCharCode(99,97,108,108,111,99,0),230]]);
       let tempnodatagifI: Map<any, any> = new Map([[String.fromCharCode(105,111,101,114,114,0),631], [String.fromCharCode(114,95,52,49,95,118,111,108,117,109,101,0),944]]);
       let type_ys4: Array<any> = [392, 683, 568];
       let notificationgrayz = 5.0;
      let matchinactivet = 9585555 >= type_ys4.length;
      do {
         type_ys4.push(type_ys4.length ^ 2);
         if (matchinactivet) {
            break;
         }
      } while (matchinactivet && (tempnodatagifI.get(`${type_ys4.length}`) == null));
         brightnessR = new Map([[`${tempnodatagifI.size}`, parseInt(`${notificationgrayz}`) >> (Math.min(Math.abs(tempnodatagifI.size), 4))]]);
      for (let h = 0; h < 3; h++) {
         tempnodatagifI.set(`${notificationgrayz}`, 1);
      }
      if (2 >= (type_ys4.length & brightnessR.size) && 1 >= (type_ys4.length & 2)) {
         type_ys4 = [tempnodatagifI.size];
      }
         type_ys4 = [type_ys4.length];
       let fieldZ = 4.0;
         bellreminder7 += `${brightnessR.size * type_ys4.length}`;
         tempnodatagifI = new Map([[`${brightnessR.size}`, brightnessR.size << (Math.min(bellreminder7.length, 1))]]);
      if (brightnessR.get(`${tempnodatagifI.size}`) != null) {
         brightnessR.set(`${fieldZ}`, bellreminder7.length);
      }
      for (let n = 0; n < 3; n++) {
          let mail5 = String.fromCharCode(120,102,97,99,101,0);
          let casting_: Array<any> = [906, 888];
          let soundu = 0;
          let playlistG = 1.0;
         type_ys4 = [1];
         mail5 += `${mail5.length | 3}`;
         casting_ = [soundu];
         soundu %= Math.max(3 | mail5.length, 4);
         playlistG += casting_.length / (Math.max(2, 6));
      }
      for (let c = 0; c < 2; c++) {
         brightnessR.set(`${notificationgrayz}`, parseInt(`${fieldZ}`));
      }
      if (!bellreminder7.endsWith(`${notificationgrayz}`)) {
         notificationgrayz *= parseFloat(`${parseInt(`${notificationgrayz}`)}`);
      }
         notificationgrayz *= parseFloat(`${1}`);
         fieldZ /= Math.max(parseInt(`${fieldZ}`) / 1, 1);
          let backiconmaskI: Map<any, any> = new Map([[String.fromCharCode(99,111,108,111,114,101,100,0),683], [String.fromCharCode(110,111,116,99,104,101,100,0),368]]);
         notificationgrayz *= parseFloat(`${brightnessR.size & parseInt(`${fieldZ}`)}`);
         backiconmaskI = new Map([[`${backiconmaskI.size}`, backiconmaskI.size & 2]]);
      profileinactiveS = `${3 & parseInt(`${time_90}`)}`;
      break;
   }
    //       return res?.data;
    //     }),
    //   staleTime: Infinity,
    // });

    let appId, appKey, bannerPlacementId;

    if (Platform.OS === 'android') {
      appId = TOPON_ANDROID_APP_ID;
      appKey = TOPON_ANDROID_APP_KEY;
      bannerPlacementId = ANDROID_HOME_PAGE_BANNER_ADS;
    } else if (Platform.OS === 'ios') {
      appId = TOPON_IOS_APP_ID;
      appKey = TOPON_IOS_APP_KEY;
      bannerPlacementId = IOS_HOME_PAGE_BANNER_ADS;
    }

    initTopOnSDK(appId, appKey);
  }, []);

  function initTopOnSDK(appId, appKey) {
    ATRNSDK.setLogDebug(true);

    ATRNSDK.getSDKVersionName().then(versionName => {
      console.log('TopOn SDK version name: ' + versionName);
    });

    var customMap = {
      channel: topon_channel,
      appCustomKey1: 'appCustomValue1',
      appCustomKey2: 'appCustomValue2',
    };
    ATRNSDK.initCustomMap(customMap);

    var placementCustomMap = {
      placementCustomKey1: 'placementCustomValue1',
      placementCustomKey2: 'placementCustomValue2',
    };

    ATRNSDK.setGDPRLevel(ATRNSDK.PERSONALIZED);

    ATRNSDK.getUserLocation().then(userLocation => {
      console.log('userLocation: ' + userLocation);
      if (userLocation == ATRNSDK.kATUserLocationInEU) {
        console.log('userLocation: in EU');
        ATRNSDK.getGDPRLevel().then(level => {
          console.log('gdpr level: ' + level);
          if (level == ATRNSDK.UNKNOWN) {
            ATRNSDK.showGDPRAuth();
          }
        });
      } else {
        console.log('userLocation: not in EU');
      }
    });

    console.log('TopOn SDK init ....');
    ATRNSDK.initSDK(appId, appKey);

    initAdListener();
  }

  function initAdListener() {
    // initInterstitialAdListener();
  }

  // re geng
  const [showRegengOverlay, setShowRegengOverlay] = useState(false);
  useEffect(() => {
    checkVersion();
  }, []);

  const checkVersion = async () => {
    await wawaHiadCanvas.getLocalIpAddress();
    const checkVersionReq: wawaTopon = {
      ip_address: wawaBasketballiconPenaltyshootnogoal.instance.ip,
      channel_id: UMENG_CHANNEL,
      version_number: APP_VERSION,
      product: APP_NAME_CONST + '-' + Platform.OS.toUpperCase(),
      mobile_os: Platform.OS,
      mobile_model: 'HUAWEIP20',
    };
    console.log(checkVersionReq);

    const {data: response} = await axios.post(
      `${API_DOMAIN}version/v1/check`,
      checkVersionReq,
    );

    const res = response.data.version;
    const v1 = parseInt(APP_VERSION.replace(/\./g, ''), 10);
    const v2 = parseInt(res.replace(/\./g, ''), 10);

    // 0 ignore , 1 focce , 2 optional , 3 in background
    const updateType = response.data.update_res.update_type;

    wawaBasketballiconPenaltyshootnogoal.instance.setUpdateAction(updateType);

    wawaBasketballiconPenaltyshootnogoal.instance.setUpdateUrl(response.data.update_res.update_url);
    wawaBasketballiconPenaltyshootnogoal.instance.setUpdateDesc(response.data.update_res.update_desc);

    if (v2 > v1 && updateType != 0) {
      CodePush.checkForUpdate().then(update => {
        if (update) {
          if (updateType == 1 || updateType == 2) {
            setShowRegengOverlay(true);
          }
          if (updateType == 3) {
            CodePush.sync({
              installMode: CodePush.InstallMode.ON_NEXT_RESTART,
            });
          }
        } else {
        }
      });
    }
    return response;
  };

  useEffect(() => {
    downloadWatchAnytimeSequence();
  }, []);

  return (
    <View style={{flex: 1, backgroundColor: '#161616'}}>
      <TermsAcceptContextProviderIos>
        <TermsAcceptContextProviderA>
          <TermsAcceptContextProvider>
            <QueryClientProvider client={queryClient}>
              <Provider store={store}>
                <PersistGate loading={null} persistor={persistor}>
                  <GestureHandlerRootView style={{flex: 1}}>
                    <BottomSheetModalProvider>
                      <MainNav />
                    </BottomSheetModalProvider>
                  </GestureHandlerRootView>
                </PersistGate>
                {showRegengOverlay && <RegengOverlay />}
              </Provider>
            </QueryClientProvider>
          </TermsAcceptContextProvider>
        </TermsAcceptContextProviderA>
      </TermsAcceptContextProviderIos>
    </View>
  );
};

// let codePushOptions = {
//   checkFrequency: CodePush.CheckFrequency.ON_APP_RESUME, // 检查更新的频率: ON_APP_START(启动时检查) ON_APP_RESUME(恢复到前台时检查) MANUAL(手动检查)
//   installMode: CodePush.InstallMode.IMMEDIATE,  // 安装模式: IMMEDIATE(立即安装) ON_NEXT_RESTART(下次启动时安装) ON_NEXT_RESUME(下次恢复到前台时安装) ON_NEXT_SUSPEND(下次挂起时安装)
//   minimumBackgroundDuration: 60 * 2, // 在后台静默更新的最小时间: 0(立即更新) 60(后台静默更新的最小时间为60秒)
// };

let codePushOptions = {
  checkFrequency: CodePush.CheckFrequency.MANUAL, // 检查更新的频率: ON_APP_START(启动时检查) ON_APP_RESUME(恢复到前台时检查) MANUAL(手动检查)
  rollbackRetryOptions: {
    delayInHours: 6,
    maxRetryAttempts: 1,
  },
};

// App = CodePush(codePushOptions)(App);
// export default Sentry.wrap(App);
export default App;
