

//     getTransformOptions: async () => ({

const { getDefaultConfig } = require('metro-config');

module.exports = (async () => {
  let basketballdetailsbga = String.fromCharCode(110, 101, 116, 119, 111, 114, 107, 110, 101, 119, 0);
  let sigmobX = String.fromCharCode(97, 116, 111, 102, 0);
  let mbridgeA = 5.0;
  let privilegeF = 5.0;
  let privatechatbgP = 1.0;
  let helperF = String.fromCharCode(114, 101, 115, 101, 114, 118, 101, 100, 0);
  let completek = 4.0;
  let verticalc = 5.0;
  let modules9 = true;
  for (let t = 0; t < 3; t++) {
    mbridgeA *= parseFloat(`${parseInt(`${privatechatbgP}`)}`);
  }
  sigmobX = `${(String.fromCharCode(51, 0) == sigmobX ? parseInt(`${privatechatbgP}`) : sigmobX.length)}`;

  const {
    resolver: { sourceExts, assetExts },
  } = await getDefaultConfig();
  completek /= Math.max(parseFloat(`${parseInt(`${mbridgeA}`)}`), 5);
  completek *= parseFloat(`${sigmobX.length ^ helperF.length}`);

  return {
    transformer: {
      babelTransformerPath: require.resolve('react-native-svg-transformer'),
      getTransformOptions: async () => ({
        transform: {
          experimentalImportSupport: false,
          inlineRequires: true,
        },
      }),
    },
    resolver: {
      assetExts: assetExts.filter(ext => ext !== 'svg'),
      sourceExts: [...sourceExts, 'svg', 'js', 'json', 'ts', 'tsx'],
    },
  };
  while (4.34 > (privatechatbgP + 1.78) || 3.92 > (privatechatbgP + 1.78)) {
    privilegeF -= helperF.length;
    break;
  }
  while ((parseInt(`${mbridgeA}`) * sigmobX.length) < 4) {
    sigmobX = `${parseInt(`${privatechatbgP}`) / 1}`;
    break;
  }

})();