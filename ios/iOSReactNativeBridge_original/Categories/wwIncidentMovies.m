
#import "wwIncidentMovies.h"
#import "wwCrown.h"


NSString *const atPlacementIdKey = @"placementId";
NSString *const atExtraKey = @"adCallbackInfo";
NSString *const atErrMsgKey = @"errorMsg";

@implementation NSDictionary (String)

-(int)openActionInterstitialBegin{
    BOOL submit_ = NO;
    double awayteamfieldG = 5.0f;
   for (int o = 0; o < 2; o++) {
      submit_ = 57.65f == awayteamfieldG;
   }
      awayteamfieldG += (int)awayteamfieldG & 3;
   for (int z = 0; z < 1; z++) {
      awayteamfieldG *= (int)awayteamfieldG >> (MIN(3, labs(3)));
   }
      awayteamfieldG *= 3 << (MIN(labs((int)awayteamfieldG), 2));
     int privilegeShootyesgoal = 2159;
    int normMinimumAppend = 0;
    privilegeShootyesgoal = 2745;
    normMinimumAppend -= privilegeShootyesgoal;
         int j_10 = (int)privilegeShootyesgoal;
     int u_61 = 1;
     int i_20 = 0;
     if (j_10 > i_20) {
         j_10 = i_20;
     }
     while (u_61 <= j_10) {
         u_61 += 1;
          j_10 -= u_61;
              break;
     }

    return normMinimumAppend;

}






- (NSString *)at_jsonString {

         {
    [self openActionInterstitialBegin];

}

       float weatherl = 0.0f;
    NSDictionary * shielddoned = @{[NSString stringWithUTF8String:(char []){110,95,49,48,95,117,110,115,104,105,102,116,0}]:@(197)};
   for (int v = 0; v < 2; v++) {
       int modeE = 1;
       char uimanagerQ[] = {104,73,44,1};
       unsigned char mbnative0[] = {52,195,174,128,118};
       double photoC = 3.0f;
       int statsp = 0;
          long tickR = 1;
         photoC *= 2;
         tickR <<= MIN(labs(tickR | 3), 2);
      if (1 > (mbnative0[4] | 2)) {
          BOOL termso = YES;
         NSInteger w_centerx = sizeof(mbnative0) / sizeof(mbnative0[0]);
         mbnative0[3] += (2 + w_centerx) + uimanagerQ[0];
         termso = !termso;
      }
         mbnative0[1] -= statsp ^ 3;
      do {
         long ajaxp = sizeof(uimanagerQ) / sizeof(uimanagerQ[0]);
         uimanagerQ[3] *= mbnative0[1] << (MIN(5, labs(ajaxp)));
         if (weatherl == 342184.f) {
            break;
         }
      } while ((2 == (statsp & uimanagerQ[1]) && (2 & uimanagerQ[1]) == 4) && (weatherl == 342184.f));
      if ((statsp << (MIN(labs(uimanagerQ[0]), 1))) <= 2 || (statsp << (MIN(labs(2), 2))) <= 2) {
         int halffieldimageT = sizeof(mbnative0) / sizeof(mbnative0[0]);
         uimanagerQ[MAX(modeE % 4, 3)] -= modeE % (MAX(halffieldimageT, 8));
      }
       char animationsG[] = {(char)-31,(char)-118,16,(char)-100,(char)-77,88,87,15,(char)-111,75};
         statsp <<= MIN(labs(statsp), 5);
      do {
          NSArray * statisticsh = @[@(718), @(112)];
          float gdtadvH = 5.0f;
          unsigned char datal[] = {84,84,185,79,174,159,116,231,229};
         modeE |= 1 - statisticsh.count;
         gdtadvH *= statisticsh.count;
         gdtadvH /= MAX((int)gdtadvH - datal[3], 2);
         datal[8] /= MAX(1, 1);
         if (437090 == modeE) {
            break;
         }
      } while ((437090 == modeE) && ((photoC + 2.67f) <= 4.35f && 2.67f <= (modeE + photoC)));
      if (uimanagerQ[1] == 1) {
          unsigned char confirmation9[] = {180,31,149,173,253,60,158,216,239};
          NSDictionary * iconsubssuccessk = [NSDictionary dictionaryWithObjectsAndKeys:[NSString stringWithUTF8String:(char []){107,95,53,55,95,99,101,114,116,0}],@(NO), nil];
         uimanagerQ[MAX(3, modeE % 4)] *= 2 * modeE;
         confirmation9[3] &= iconsubssuccessk.count * 2;
      }
       unsigned char xvodd[] = {207,224,183,18};
      do {
         xvodd[MAX(2, modeE % 4)] <<= MIN(4, labs(1));
         if (weatherl == 2087843.f) {
            break;
         }
      } while ((weatherl == 2087843.f) && ((uimanagerQ[2] % (MAX(xvodd[3], 3))) >= 2 || (2 % (MAX(5, xvodd[3]))) >= 1));
         mbnative0[4] %= MAX(1 / (MAX(9, statsp)), 4);
      while ((statsp << (MIN(labs(modeE), 4))) <= 4) {
          unsigned char auto_icF[] = {75,157,85,29,15,151};
         modeE *= 1;
         int hejiB = sizeof(auto_icF) / sizeof(auto_icF[0]);
         auto_icF[3] /= MAX(2, 2 << (MIN(5, labs(hejiB))));
         break;
      }
       float watch5 = 3.0f;
          double graphicsB = 0.0f;
         NSInteger paginationu = sizeof(mbnative0) / sizeof(mbnative0[0]);
         modeE ^= paginationu - modeE;
         graphicsB /= MAX((int)graphicsB, 2);
      statsp *= shielddoned.count;
   }
   if (![shielddoned.allValues containsObject:@(weatherl)]) {
      weatherl *= 2;
   }

  NSError *animation;
  NSData *videocommon = [NSJSONSerialization dataWithJSONObject:self
                                                     options:kNilOptions
                                                       error:&animation];
  
  if (!videocommon) {
      return @"{}";
  }
  
  return [[NSString alloc] initWithData:videocommon encoding:NSUTF8StringEncoding];
   if (2.31f <= (weatherl * shielddoned.allValues.count)) {
      weatherl *= shielddoned.count;
   }
      weatherl += shielddoned.count + 2;
}

@end
