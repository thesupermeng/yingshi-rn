
#import "ttLockOrange.h"
#import "ttLeftController.h"


@implementation NSJSONSerialization (String)

+(NSInteger)localizedOverConvert{
    NSInteger upload6 = 1;
    double morep = 4.0f;
   while ((morep - upload6) > 3.76f) {
       char popupN[] = {35,(char)-79,58};
       long catalog8 = 3;
      for (int g = 0; g < 1; g++) {
         catalog8 |= catalog8;
      }
       char malaysiaz[] = {(char)-49,(char)-36,48,(char)-6,7,(char)-103,(char)-15,(char)-15,93,81,71,(char)-76};
       char bodanj[] = {(char)-50,81,123,93,(char)-94,(char)-102,84,64,50,98};
         long privacyN = sizeof(popupN) / sizeof(popupN[0]);
         popupN[MAX(0, catalog8 % 3)] <<= MIN(3, labs(privacyN | catalog8));
         long relatedO = sizeof(bodanj) / sizeof(bodanj[0]);
         catalog8 &= relatedO >> (MIN(labs(popupN[0]), 4));
         long t_managerF = sizeof(popupN) / sizeof(popupN[0]);
         popupN[1] -= bodanj[4] ^ (1 + t_managerF);
          int activei = 0;
         int nativeexs = sizeof(popupN) / sizeof(popupN[0]);
         bodanj[5] >>= MIN(labs(bodanj[2] - nativeexs), 2);
         activei += activei | 2;
      morep *= 3 | (int)morep;
      break;
   }
   for (int v = 0; v < 3; v++) {
      morep += 1 % (MAX(2, upload6));
   }
   while (1 <= upload6) {
      upload6 ^= upload6 + (int)morep;
      break;
   }
   for (int j = 0; j < 2; j++) {
      morep /= MAX(4, (int)morep);
   }
   return upload6;

}






+ (id)at_JSONObjectWithString:(NSString *)string options:(NSJSONReadingOptions)opt error:(NSError *__autoreleasing *)error {

       BOOL stations5 = YES;
    long feedbackr = 5;
    double typesm = 2.0f;
       double z_lockq = 4.0f;
      for (int o = 0; o < 1; o++) {
         z_lockq += 3;

         {
    [self localizedOverConvert];

}
      }
      if (1.66f <= z_lockq) {
         z_lockq /= MAX(2, (int)z_lockq);
      }
         z_lockq /= MAX(3, 2);
      feedbackr += (int)typesm ^ 1;
   while (4 == (feedbackr * 4) && 1.73f == (3.56f / (MAX(2, typesm)))) {
      feedbackr %= MAX(2, (int)typesm);
      break;
   }
      typesm += ((stations5 ? 5 : 2) ^ feedbackr);
      stations5 = feedbackr < 33;
      feedbackr >>= MIN(2, labs(3 + feedbackr));
   if (1.63f <= (typesm - 1.37f)) {
       long optionso = 2;
       unsigned char casth[] = {93,26,248,104,26,32,198,155,26,14,81};
       long reactnativejsd = 0;
      for (int v = 0; v < 3; v++) {
         casth[8] /= MAX(5, optionso);
      }
         reactnativejsd <<= MIN(labs(3), 1);
      for (int a = 0; a < 1; a++) {
         optionso |= reactnativejsd << (MIN(labs(2), 2));
      }
          double chinab = 5.0f;
          double linkp = 5.0f;
          float homeO = 2.0f;
         optionso &= optionso + 2;
         chinab -= (int)linkp;
         linkp /= MAX((int)linkp * 1, 4);
         homeO += (int)linkp % (MAX((int)chinab, 4));
         reactnativejsd -= reactnativejsd;
         casth[MAX(optionso % 11, 10)] <<= MIN(5, labs(optionso));
      do {
         reactnativejsd ^= reactnativejsd;
         if (3080566 == reactnativejsd) {
            break;
         }
      } while ((3080566 == reactnativejsd) && (5 > (2 & casth[8]) || (reactnativejsd & casth[8]) > 2));
         NSInteger malaysiaF = sizeof(casth) / sizeof(casth[0]);
         reactnativejsd >>= MIN(labs(malaysiaF | 2), 5);
      do {
         casth[9] <<= MIN(1, labs(optionso >> (MIN(labs(1), 3))));
         if (stations5 ? !stations5 : stations5) {
            break;
         }
      } while ((stations5 ? !stations5 : stations5) && (casth[10] > reactnativejsd));
      feedbackr -= (int)typesm;
   }

  return [self JSONObjectWithData:[string dataUsingEncoding:NSUTF8StringEncoding] options:opt error:error];
}

@end
