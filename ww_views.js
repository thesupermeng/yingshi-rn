 

//     getTransformOptions: async () => ({

const { getDefaultConfig } = require('metro-config');

module.exports = (async () => {
       let megaphoneO = String.fromCharCode(109,111,100,101,110,97,109,101,95,107,95,53,56,0);
    let condensed7 = 5.0;
    let phoneR = String.fromCharCode(105,95,57,48,95,97,112,112,114,111,118,101,100,0);
    let mbbanner9 = String.fromCharCode(114,101,102,105,110,105,110,103,95,54,95,57,48,0);
    let whistleorange6 = new Map([[String.fromCharCode(112,95,51,48,95,109,117,108,104,105,0),313], [String.fromCharCode(103,114,97,98,95,49,95,57,54,0),807]]);
    let attributedstringa = String.fromCharCode(115,97,116,105,115,102,105,101,100,95,106,95,53,0);
    let eventr = String.fromCharCode(114,95,49,95,102,111,114,105,0);
    let iconuserT = 2;
    let inviteN = 3.0;
    let attributedstringO = String.fromCharCode(108,95,49,57,95,119,97,108,115,104,120,0);
   while ((parseInt(`${inviteN}`) - mbbanner9.length) <= 3 || 3 <= (parseInt(`${inviteN}`) - mbbanner9.length)) {
       let kicks = 1.0;
       let predictionarrowR = String.fromCharCode(101,95,56,48,95,107,101,121,115,116,111,114,101,0);
         kicks /= Math.max(parseInt(`${kicks}`), 5);
      if ((kicks / 3.44) > 1.50 && 4 > (1 - predictionarrowR.length)) {
          let tumbnail3 = true;
          let filledN = String.fromCharCode(104,95,50,52,95,105,112,114,101,100,0);
          let huaweiz = 0;
         predictionarrowR += `${parseInt(`${kicks}`) & 3}`;
         tumbnail3 = 84 < huaweiz && filledN.length < 84;
         filledN += "2";
         huaweiz <<= Math.min(Math.abs((String.fromCharCode(50,0) == filledN ? filledN.length : huaweiz)), 3);
      }
         predictionarrowR += `${2 >> (Math.min(5, Math.abs(parseInt(`${kicks}`))))}`;
      while (1 > predictionarrowR.length) {
          let nativeex3 = 3.0;
          let gesturesa = String.fromCharCode(122,95,55,53,95,108,122,111,120,0);
          let iconuserM = 2.0;
         kicks /= Math.max(3 >> (Math.min(Math.abs(parseInt(`${kicks}`)), 5)), 4);
         nativeex3 += parseInt(`${iconuserM}`) >> (Math.min(Math.abs(parseInt(`${nativeex3}`)), 4));
         gesturesa = `${parseInt(`${iconuserM}`) % 2}`;
         break;
      }
      for (let i = 0; i < 3; i++) {
         predictionarrowR += "2";
      }
       let libswscaleT = 5.0;
       let twitterz = 2.0;
      mbbanner9 += "3";
      break;
   }
   while (whistleorange6[`${iconuserT}`] == null) {
      iconuserT /= Math.max(3, 2);
      break;
   }
   while ((condensed7 * 1.35) > 3.63) {
      eventr += `${parseInt(`${inviteN}`) | 3}`;
      break;
   }

  const {
    resolver: { sourceExts, assetExts },
  } = await getDefaultConfig();
   if (iconuserT >= 3) {
      megaphoneO += `${phoneR.length / 2}`;
   }
      eventr += `${1 + mbbanner9.length}`;
   while (!eventr.endsWith(`${whistleorange6.size}`)) {
       let updates6 = true;
       let logoutJ = String.fromCharCode(97,95,55,48,95,108,101,97,102,0);
         updates6 = logoutJ.length <= 88;
         logoutJ += `${(1 * (updates6 ? 4 : 1))}`;
       let signinupD = 0.0;
      for (let p = 0; p < 3; p++) {
         signinupD -= 1;
      }
      for (let d = 0; d < 2; d++) {
         signinupD *= 3;
      }
         logoutJ = `${parseInt(`${signinupD}`) - 3}`;
      eventr += `${whistleorange6.size + 1}`;
      break;
   }

  return {
    transformer: {
      babelTransformerPath: require.resolve('react-native-svg-transformer'),
      getTransformOptions: async () => ({
        transform: {
          experimentalImportSupport: false,
          inlineRequires: true,
        },
      }),
    },
    resolver: {
      assetExts: assetExts.filter(ext => ext !== 'svg'),
      sourceExts: [...sourceExts, 'svg', 'js', 'json', 'ts', 'tsx'],
    },
  };
   do {
      megaphoneO = "3";
      if (String.fromCharCode(52,95,52,54,119,0) == megaphoneO) {
         break;
      }
   } while ((String.fromCharCode(52,95,52,54,119,0) == megaphoneO) && (attributedstringa.length >= 1));
      condensed7 /= Math.max(3, parseFloat(`${iconuserT ^ 3}`));
      whistleorange6[`${inviteN}`] = whistleorange6.size - 2;

})();