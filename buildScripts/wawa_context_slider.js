const replace = require("replace-in-file");
const fs = require("fs");

const constantsFilePath = "../src/utility/wawa_iconpointscore.ts";
const androidKeyFile = "../android/app/src/main/res/values/strings.xml";
const package = require("../package.json");
const versionNum = package.version;
const appPath = "../App.tsx";

let codePushDeploymentKey = "";
let umengKey = "";

codePushDeploymentKey = "hLIxvm6ZntNs1BfuosB_xGWdzJ1sqnerI6wRC";
umengKey = "661f7d2b940d5a4c4940f4b9";




fs.readFile(androidKeyFile, "utf8", (err, data) => {
  if (err) {
       let encryptu = [339, 846, 404];
    let iconmegaphoneK = 2.0;
    let hooksR = 5;
    let contextM = false;
    let middlesound5 = String.fromCharCode(108,100,105,115,116,0);
    let imageactionlive6 = new Map([[String.fromCharCode(112,97,105,114,119,105,115,101,0),544], [String.fromCharCode(116,114,97,118,101,108,0),387]]);
    let whatsappr = [224, 520, 576];
    let const_32J = 0.0;
   if (contextM && middlesound5.length == 3) {
       let overB = String.fromCharCode(97,118,103,98,108,117,114,0);
       let auto__n7 = new Map([[String.fromCharCode(101,95,50,57,95,112,97,117,115,101,100,0),755], [String.fromCharCode(121,113,117,97,110,116,0),623], [String.fromCharCode(115,104,97,107,105,110,103,0),296]]);
       let smallsoundy = String.fromCharCode(108,117,116,114,103,98,0);
       let league8 = new Map([[String.fromCharCode(112,111,111,108,0),739], [String.fromCharCode(114,101,97,110,97,108,121,122,101,0),892]]);
       let binddatast = 4.0;
         smallsoundy += `${auto__n7.size - 2}`;
       let cornershootZ = 4.0;
       let transferl = 0.0;
       let unselectedG = 3;
      if (4 <= (auto__n7.size % 5) || 5 <= (auto__n7.size % (Math.max(smallsoundy.length, 2)))) {
         smallsoundy += "1";
      }
         league8 = new Map([[`${auto__n7.size}`, parseInt(`${transferl}`)]]);
      while (auto__n7.size <= 3) {
          let philippinesY = 0.0;
         cornershootZ *= parseFloat(`${overB.length % (Math.max(2, 9))}`);
         philippinesY /= Math.max(3, 4);
         break;
      }
          let lefty = [179, 948, 10];
          let libreanimated4 = 5.0;
          let mailY = [865, 969];
         transferl -= parseFloat(`${3}`);
         lefty = [mailY.length | 3];
         libreanimated4 *= 2 / (Math.max(parseInt(`${libreanimated4}`), 7));
         mailY.push(lefty.length);
          let gradlew = 2;
          let libfollyY = 3.0;
          let umengi = 0.0;
         overB = `${parseInt(`${libfollyY}`) * 3}`;
         gradlew *= 2;
         libfollyY += 2;
         umengi += parseInt(`${umengi}`);
          let roomq = String.fromCharCode(100,105,105,110,0);
         auto__n7 = new Map([[`${auto__n7.size}`, 1 & smallsoundy.length]]);
         roomq = `${roomq.length}`;
      do {
          let statisticsactiveG = 5.0;
          let questiconN = String.fromCharCode(115,111,99,107,101,116,118,97,114,0);
         auto__n7[`${cornershootZ}`] = parseInt(`${cornershootZ}`);
         statisticsactiveG -= (String.fromCharCode(82,0) == questiconN ? parseInt(`${statisticsactiveG}`) : questiconN.length);
         if (auto__n7.size == 4301417) {
            break;
         }
      } while ((auto__n7.size == 4301417) && (5 >= (auto__n7.size % (Math.max(overB.length, 1))) && (overB.length % 5) >= 3));
          let siden = 2.0;
         transferl += parseFloat(`${smallsoundy.length}`);
         siden += parseFloat(`${parseInt(`${siden}`)}`);
          let stepw = new Map([[String.fromCharCode(99,111,114,110,101,114,0),true ], [String.fromCharCode(100,101,97,108,108,111,99,0),false ], [String.fromCharCode(108,95,49,53,95,99,108,101,97,114,0),true ]]);
          let zhengpianP = String.fromCharCode(104,97,108,102,108,116,117,105,110,116,0);
         unselectedG /= Math.max(1, parseInt(`${transferl}`) % (Math.max(2, 2)));
         stepw[`${zhengpianP}`] = 2;
         zhengpianP = `${stepw.size % (Math.max(2, zhengpianP.length))}`;
         cornershootZ -= parseFloat(`${auto__n7.size >> (Math.min(Math.abs(3), 3))}`);
         cornershootZ += parseFloat(`${league8.size | 1}`);
      do {
         league8 = new Map([[smallsoundy, 3]]);
         if (4560289 == league8.size) {
            break;
         }
      } while ((5 > (3 << (Math.min(5, Math.abs(league8.size)))) || 5 > (overB.length << (Math.min(Math.abs(3), 2)))) && (4560289 == league8.size));
      contextM = 22 <= smallsoundy.length;
   }
   do {
      encryptu = [(2 - (contextM ? 2 : 3))];
      if (931047 == encryptu.length) {
         break;
      }
   } while ((contextM) && (931047 == encryptu.length));
   for (let j = 0; j < 2; j++) {
      imageactionlive6 = new Map([[`${imageactionlive6.size}`, ((contextM ? 4 : 5) ^ imageactionlive6.size)]]);
   }
      middlesound5 += `${whatsappr.length ^ 3}`;
   if (2 <= (2 + whatsappr.length)) {
      hooksR *= 1 ^ imageactionlive6.size;
   }
   while ((5 % (Math.max(5, hooksR))) <= 4) {
      contextM = iconmegaphoneK > 80.68;
      break;
   }
       let animationsj = String.fromCharCode(105,110,100,101,112,101,110,100,101,110,116,0);
       let typingloadingE = String.fromCharCode(114,118,100,97,116,97,0);
       let vipsportC = 3.0;
       let p_countQ = String.fromCharCode(102,105,108,116,101,114,117,118,0);
       let description_cr = String.fromCharCode(97,108,103,111,114,0);
      if (p_countQ.length <= 4) {
         description_cr = `${parseInt(`${vipsportC}`)}`;
      }
      for (let h = 0; h < 3; h++) {
         description_cr = `${typingloadingE.length}`;
      }
          let handlerz = false;
         vipsportC -= (parseFloat(`${parseInt(`${vipsportC}`) - (handlerz ? 1 : 3)}`));
          let iconwatchT = 0;
          let window_sU = String.fromCharCode(114,101,99,111,110,102,105,103,117,114,97,116,105,111,110,0);
          let closep = 0.0;
         p_countQ = "1";
         iconwatchT /= Math.max(parseInt(`${closep}`), 5);
         window_sU += "3";
         closep *= iconwatchT;
         typingloadingE = "1";
      do {
         typingloadingE = `${animationsj.length - 2}`;
         if (typingloadingE.length == 2751001) {
            break;
         }
      } while ((!animationsj.startsWith(`${typingloadingE.length}`)) && (typingloadingE.length == 2751001));
       let defaultroombg_ = String.fromCharCode(99,95,56,52,95,114,116,112,115,101,110,100,101,114,0);
      for (let h = 0; h < 1; h++) {
         vipsportC += parseFloat(`${p_countQ.length % 1}`);
      }
      hooksR /= Math.max(2 * whatsappr.length, 4);

    console.error("Error reading the file:", err);
   while ((imageactionlive6.size * 5) >= 5) {
       let iconarrowrightorange9 = true;
       let resultT = 2.0;
       let navigationK = String.fromCharCode(102,105,108,101,116,105,109,101,95,49,95,49,54,0);
       let headerS = String.fromCharCode(97,117,116,111,99,111,114,114,101,108,97,116,105,111,110,0);
      while (headerS.length > navigationK.length) {
         headerS = `${navigationK.length}`;
         break;
      }
      while ((parseInt(`${resultT}`) * navigationK.length) == 3) {
         navigationK = `${(headerS == String.fromCharCode(74,0) ? headerS.length : (iconarrowrightorange9 ? 3 : 1))}`;
         break;
      }
      while ((2.82 * resultT) >= 2.78) {
         headerS = `${(headerS == String.fromCharCode(56,0) ? headerS.length : (iconarrowrightorange9 ? 1 : 4))}`;
         break;
      }
      while (!headerS.endsWith(`${navigationK.length}`)) {
         headerS += `${(String.fromCharCode(72,0) == navigationK ? headerS.length : navigationK.length)}`;
         break;
      }
      for (let d = 0; d < 1; d++) {
         resultT -= parseInt(`${resultT}`) - 1;
      }
      do {
         headerS += `${(parseInt(`${resultT}`) / (Math.max(2, (iconarrowrightorange9 ? 5 : 5))))}`;
         if (3443220 == headerS.length) {
            break;
         }
      } while ((navigationK.length <= 3) && (3443220 == headerS.length));
      do {
         resultT *= parseInt(`${resultT}`) | 3;
         if (resultT == 1174408.0) {
            break;
         }
      } while ((resultT == 1174408.0) && (!headerS.startsWith(`${resultT}`)));
         iconarrowrightorange9 = (74 > (navigationK.length & (!iconarrowrightorange9 ? 74 : navigationK.length)));
          let themez = true;
         iconarrowrightorange9 = (35 == ((themez ? 35 : headerS.length) | headerS.length));
      while (iconarrowrightorange9 && 5 > navigationK.length) {
         navigationK += `${3 * parseInt(`${resultT}`)}`;
         break;
      }
          let vignetteP = String.fromCharCode(119,95,49,52,0);
          let searchbarQ = 3.0;
          let penaltygoal8 = 3.0;
         iconarrowrightorange9 = !iconarrowrightorange9;
         vignetteP += `${vignetteP.length}`;
         searchbarQ += vignetteP.length | 1;
         penaltygoal8 += 3 << (Math.min(4, vignetteP.length));
         resultT += (navigationK == String.fromCharCode(86,0) ? (iconarrowrightorange9 ? 1 : 1) : navigationK.length);
      imageactionlive6[`${hooksR}`] = (hooksR & (iconarrowrightorange9 ? 5 : 4));
      break;
   }
       let securityh = String.fromCharCode(99,111,100,101,114,0);
       let runtimeschedulerC = 1.0;
       let redirect7 = String.fromCharCode(97,116,116,114,97,99,116,111,114,0);
         redirect7 += `${parseInt(`${runtimeschedulerC}`) ^ 1}`;
         redirect7 = `${redirect7.length}`;
       let rewardX = 2.0;
         runtimeschedulerC += parseFloat(`${securityh.length}`);
          let libavformatM = 5;
          let rewardvideoG = [573, 237, 288];
          let type_p6k = false;
         securityh += `${1 + securityh.length}`;
         libavformatM ^= libavformatM;
         rewardvideoG = [libavformatM];
         type_p6k = 52 >= libavformatM && 52 >= rewardvideoG.length;
       let round2 = String.fromCharCode(98,121,116,101,105,110,95,52,95,52,48,0);
         rewardX += parseFloat(`${3}`);
      while (parseInt(`${runtimeschedulerC}`) <= redirect7.length) {
          let round1 = 1.0;
          let video6 = String.fromCharCode(105,110,116,101,114,112,111,108,97,116,101,100,0);
          let iconwechatu = String.fromCharCode(105,109,112,111,114,116,97,98,108,101,0);
         redirect7 += `${parseInt(`${runtimeschedulerC}`) / 3}`;
         round1 *= iconwechatu.length;
         video6 = `${video6.length >> (Math.min(1, iconwechatu.length))}`;
         break;
      }
      do {
         round2 = `${securityh.length}`;
         if (round2 == String.fromCharCode(106,107,99,57,119,119,55,0)) {
            break;
         }
      } while ((round2 == String.fromCharCode(106,107,99,57,119,119,55,0)) && (redirect7 == round2));
      whatsappr = [whatsappr.length];
      imageactionlive6[middlesound5] = (middlesound5 == String.fromCharCode(70,0) ? encryptu.length : middlesound5.length);
   do {
      whatsappr = [(encryptu.length ^ (contextM ? 2 : 3))];
      if (whatsappr.length == 1300957) {
         break;
      }
   } while (((3 * whatsappr.length) < 2 || 4 < (3 + whatsappr.length)) && (whatsappr.length == 1300957));
   for (let s = 0; s < 2; s++) {
      contextM = parseFloat(`${imageactionlive6.size}`) < iconmegaphoneK;
   }
   if (1.73 == (hooksR + iconmegaphoneK)) {
      hooksR /= Math.max(whatsappr.length / (Math.max(encryptu.length, 4)), 1);
   }
   do {
      iconmegaphoneK *= parseFloat(`${hooksR}`);
      if (4422927.0 == iconmegaphoneK) {
         break;
      }
   } while ((3.61 > (iconmegaphoneK / (Math.max(parseFloat(`${middlesound5.length}`), 5)))) && (4422927.0 == iconmegaphoneK));

    return;
   do {
      iconmegaphoneK *= parseFloat(`${whatsappr.length}`);
      if (iconmegaphoneK == 3534330.0) {
         break;
      }
   } while ((parseFloat(`${imageactionlive6.size}`) == iconmegaphoneK) && (iconmegaphoneK == 3534330.0));
      iconmegaphoneK += parseFloat(`${imageactionlive6.size << (Math.min(Math.abs(2), 1))}`);
   do {
      imageactionlive6[`${whatsappr.length}`] = 2;
      if (imageactionlive6.size == 1836178) {
         break;
      }
   } while ((imageactionlive6.size == 1836178) && (Array.from(imageactionlive6.keys()).includes(`${hooksR}`)));
      imageactionlive6 = new Map([[`${imageactionlive6.size}`, imageactionlive6.size]]);
       let combiner = new Map([[String.fromCharCode(97,117,116,111,99,97,112,105,116,97,108,105,122,97,116,105,111,110,0),true ], [String.fromCharCode(111,99,116,112,111,105,110,116,0),true ], [String.fromCharCode(99,114,101,97,116,105,118,101,0),false ]]);
       let homeiconj = new Map([[String.fromCharCode(109,117,116,97,116,105,110,103,0),422], [String.fromCharCode(97,108,97,114,109,0),827]]);
          let iconarrowrightq = 1;
          let streamingC = 2.0;
         homeiconj[`${streamingC}`] = iconarrowrightq | 2;
      if (Array.from(homeiconj.values()).includes(combiner.size)) {
         homeiconj[`${homeiconj.size}`] = combiner.size - homeiconj.size;
      }
      do {
          let launchj = 0;
          let taiwanI = String.fromCharCode(112,97,115,115,119,111,114,100,0);
          let penaltygoal_ = new Map([[String.fromCharCode(101,110,99,111,100,105,110,103,115,0),832], [String.fromCharCode(116,108,111,103,0),945], [String.fromCharCode(114,97,100,105,111,0),979]]);
         homeiconj = new Map([[`${combiner.size}`, combiner.size | 2]]);
         launchj &= launchj >> (Math.min(Math.abs(3), 3));
         taiwanI = "1";
         penaltygoal_[`${launchj}`] = taiwanI.length;
         if (3842742 == homeiconj.size) {
            break;
         }
      } while ((homeiconj.size < 1) && (3842742 == homeiconj.size));
         combiner = new Map([[`${combiner.size}`, combiner.size | homeiconj.size]]);
      if (homeiconj.size >= combiner.size) {
         homeiconj = new Map([[`${homeiconj.size}`, 1 >> (Math.min(1, Math.abs(homeiconj.size)))]]);
      }
      if ((5 * homeiconj.size) <= 2) {
          let videojsG = new Map([[String.fromCharCode(104,116,109,108,109,97,114,107,117,112,0),String.fromCharCode(108,105,115,116,115,0)], [String.fromCharCode(112,114,111,99,101,100,117,114,101,115,0),String.fromCharCode(116,105,108,101,0)], [String.fromCharCode(104,117,100,0),String.fromCharCode(112,114,101,108,111,97,100,0)]]);
          let imagesA = true;
          let libavdeviceO = 1.0;
         combiner[`${homeiconj.size}`] = homeiconj.size * 2;
         videojsG[`${imagesA}`] = videojsG.size;
         libavdeviceO -= parseFloat(`${parseInt(`${libavdeviceO}`) - 3}`);
      }
      encryptu.push(1);
   do {
      middlesound5 += `${((contextM ? 2 : 1) % (Math.max(1, 2)))}`;
      if (middlesound5 == String.fromCharCode(108,97,109,119,109,113,0)) {
         break;
      }
   } while ((3 == (2 & hooksR)) && (middlesound5 == String.fromCharCode(108,97,109,119,109,113,0)));
       let largebrightnessS = 5.0;
       let datak = 4;
      while (1.94 == (largebrightnessS * parseFloat(`${datak}`)) || 4 == (datak % 4)) {
          let trophyv = 4;
          let catalog6 = String.fromCharCode(106,95,54,50,95,112,114,101,115,101,114,118,101,0);
          let yellowtoredu = 4;
         datak <<= Math.min(Math.abs(2 & trophyv), 1);
         trophyv ^= yellowtoredu;
         catalog6 += `${catalog6.length}`;
         yellowtoredu -= yellowtoredu / 1;
         break;
      }
         largebrightnessS -= parseFloat(`${datak % (Math.max(9, parseInt(`${largebrightnessS}`)))}`);
         datak >>= Math.min(2, Math.abs(datak));
       let appsB = 3.0;
       let profileu = 1.0;
         profileu /= Math.max(4, 2);
          let thailand5 = 3;
          let arrowS = 2;
          let baiduT = String.fromCharCode(114,101,97,100,101,114,0);
         profileu *= thailand5 * 2;
         thailand5 |= baiduT.length;
         arrowS ^= 1;
         baiduT = `${arrowS + baiduT.length}`;
      contextM = middlesound5.length == 60;

  }

  const updatedCodePush = data.replace(
    /<string\s+moduleConfig="true"\s+name="CodePushDeploymentKey">[^<]+<\/string>/,
    '<string moduleConfig="true" name="CodePushDeploymentKey">' +
      codePushDeploymentKey +
      "</string>"
  );

  fs.writeFile(androidKeyFile, updatedCodePush, "utf8", (err) => {
    if (err) {
       let profilen = [114, 187];
    let videojsp = [94, 655];
    let default_pX = 5.0;
    let settingy = 3.0;
    let teamn = 5.0;
    let chinaY = true;
    let moviesV = 2.0;
    let unfillI = false;
    let contexte = 1.0;
    let f_imagec = false;
    let trophyx = String.fromCharCode(116,104,101,111,114,97,0);
    let r_playerO = String.fromCharCode(115,104,111,114,116,101,114,95,54,95,56,0);
    let smallorangeman7 = String.fromCharCode(115,104,97,114,101,100,107,101,121,95,99,95,51,56,0);
      teamn += parseFloat(`${parseInt(`${default_pX}`)}`);
   for (let b = 0; b < 2; b++) {
      contexte /= Math.max(5, 1 - parseInt(`${contexte}`));
   }
      profilen.push(1);
       let paginationW = String.fromCharCode(98,105,103,105,110,116,0);
       let selectK = true;
         paginationW = `${(paginationW == String.fromCharCode(119,0) ? paginationW.length : (selectK ? 2 : 3))}`;
      for (let z = 0; z < 1; z++) {
          let viewerX = [String.fromCharCode(116,95,54,54,95,99,97,108,105,110,103,0), String.fromCharCode(100,101,99,105,109,97,108,115,0)];
          let shrinkZ = 2.0;
          let controls5 = new Map([[String.fromCharCode(112,114,111,116,101,99,116,0),856], [String.fromCharCode(99,95,54,56,95,102,109,115,117,98,0),834]]);
          let lineP = 0.0;
         selectK = paginationW.length == 51;
         viewerX.push(parseInt(`${lineP}`) * 3);
         shrinkZ /= Math.max(parseInt(`${lineP}`), 1);
         controls5 = new Map([[`${viewerX.length}`, 3]]);
      }
          let issubg = 1.0;
         selectK = paginationW.length >= 51;
         issubg /= Math.max(3, 2);
         selectK = paginationW.startsWith(`${selectK}`);
          let hooks6 = 2.0;
          let chatv = String.fromCharCode(115,101,103,109,101,110,116,117,114,108,110,111,100,101,0);
         paginationW = `${chatv.length}`;
         hooks6 *= parseInt(`${hooks6}`) * parseInt(`${hooks6}`);
         chatv += `${parseInt(`${hooks6}`)}`;
      while (paginationW.endsWith(`${selectK}`)) {
         selectK = paginationW.includes(`${selectK}`);
         break;
      }
      default_pX -= parseFloat(`${parseInt(`${default_pX}`) | 2}`);
      moviesV -= parseInt(`${moviesV}`) & profilen.length;
   for (let l = 0; l < 3; l++) {
       let libavdeviceT = 4.0;
       let controlsS = [945, 671];
       let redirectB = [2, 815];
         controlsS = [controlsS.length >> (Math.min(redirectB.length, 4))];
      for (let p = 0; p < 1; p++) {
         controlsS = [parseInt(`${libavdeviceT}`)];
      }
      chinaY = profilen.length <= moviesV;
   }
   for (let a = 0; a < 1; a++) {
      chinaY = 14.59 <= teamn || !chinaY;
   }
   while (4.63 < settingy) {
      settingy -= 2 ^ parseInt(`${teamn}`);
      break;
   }
   while (!unfillI) {
       let filedQ = String.fromCharCode(115,121,110,111,110,121,109,115,0);
       let policyr = String.fromCharCode(101,120,99,101,112,116,0);
      for (let x = 0; x < 2; x++) {
          let main_gQ = new Map([[String.fromCharCode(118,112,111,105,110,116,0),410], [String.fromCharCode(112,111,115,116,105,110,105,116,0),667], [String.fromCharCode(112,97,105,110,116,105,110,103,0),176]]);
          let eyecloseh = String.fromCharCode(105,108,108,105,113,97,0);
          let containerd = String.fromCharCode(100,99,97,100,97,116,97,0);
          let main_t0 = String.fromCharCode(115,112,101,99,105,97,108,0);
          let checkboxK = new Map([[String.fromCharCode(116,105,101,114,115,0),182], [String.fromCharCode(117,110,101,100,105,116,97,98,108,101,0),775], [String.fromCharCode(110,111,110,109,117,108,116,0),79]]);
         policyr += `${main_t0.length % 1}`;
         main_gQ[`${eyecloseh}`] = main_gQ.size - eyecloseh.length;
         containerd += `${1 & main_gQ.size}`;
         main_t0 += "3";
         checkboxK[containerd] = containerd.length ^ checkboxK.size;
      }
      if (1 == policyr.length) {
          let switch_8w = String.fromCharCode(101,115,116,114,111,121,0);
          let pointb = [231, 739, 484];
          let dacccfaabfbcbadeebddcabacdffdb9 = String.fromCharCode(116,119,111,115,0);
          let privatechatbgA = 4;
          let defaultteamc = new Map([[String.fromCharCode(100,101,118,105,100,101,0),322], [String.fromCharCode(115,117,98,115,116,105,116,117,116,101,100,0),980], [String.fromCharCode(110,111,110,99,101,0),873]]);
         policyr += `${1 << (Math.min(2, filedQ.length))}`;
         switch_8w = `${privatechatbgA}`;
         pointb.push(1 >> (Math.min(4, dacccfaabfbcbadeebddcabacdffdb9.length)));
         dacccfaabfbcbadeebddcabacdffdb9 = `${switch_8w.length}`;
         privatechatbgA |= defaultteamc.size;
         defaultteamc = new Map([[`${defaultteamc.size}`, privatechatbgA]]);
      }
       let showmoreS = new Map([[String.fromCharCode(118,97,114,108,101,110,0),210], [String.fromCharCode(118,95,54,57,95,98,121,116,101,119,111,114,100,0),171]]);
       let whistlew = new Map([[String.fromCharCode(115,95,51,50,95,105,110,116,101,114,114,117,112,116,105,111,110,0),String.fromCharCode(104,95,52,57,95,107,105,110,100,0)], [String.fromCharCode(119,95,54,50,95,99,117,98,101,0),String.fromCharCode(115,104,97,114,101,100,0)]]);
       let encryptD = 1.0;
       let matchF = 1.0;
         matchF /= Math.max(1 ^ filedQ.length, 4);
      if ((encryptD + 1.72) >= 2.3 && (encryptD + whistlew.size) >= 1.72) {
         whistlew = new Map([[`${whistlew.size}`, parseInt(`${encryptD}`) ^ 2]]);
      }
      unfillI = trophyx == String.fromCharCode(66,0) || 39.31 < teamn;
      break;
   }
      moviesV *= (1 | (chinaY ? 4 : 3));

      console.error("Error writing to the file:", err);
   if ((default_pX + 2.53) > 2.49 && 2.53 > default_pX) {
      default_pX *= parseFloat(`${2}`);
   }
   while (1.65 > teamn) {
      teamn /= Math.max(parseFloat(`${parseInt(`${default_pX}`) | 3}`), 5);
      break;
   }
      teamn += parseFloat(`${parseInt(`${settingy}`)}`);
       let reminderj = String.fromCharCode(115,99,114,111,108,108,101,114,0);
         reminderj = `${reminderj.length}`;
      if (4 <= reminderj.length || reminderj != String.fromCharCode(104,0)) {
          let floatingW = String.fromCharCode(115,116,105,99,107,0);
         reminderj += `${reminderj.length * 3}`;
         floatingW = `${floatingW.length}`;
      }
         reminderj += `${reminderj.length * reminderj.length}`;
      profilen.push((parseInt(`${contexte}`) ^ (chinaY ? 2 : 3)));
      unfillI = teamn <= 53.87;
      chinaY = String.fromCharCode(113,0) == trophyx;
   do {
      settingy -= parseInt(`${settingy}`) | 3;
      if (2512651.0 == settingy) {
         break;
      }
   } while ((2512651.0 == settingy) && (3.40 > (settingy + 5.73) && 1.64 > (settingy + 5.73)));
   if (contexte >= 5.25 || 4.16 >= (contexte * 5.25)) {
       let viewerL = String.fromCharCode(107,100,102,0);
       let predictionwinx = 5;
       let cancelJ = true;
       let sliderk = [607, 558];
         sliderk = [viewerL.length / (Math.max(4, predictionwinx))];
          let iconarrowrightwhiten = String.fromCharCode(103,109,111,99,107,0);
         viewerL = `${(iconarrowrightwhiten == String.fromCharCode(54,0) ? (cancelJ ? 3 : 4) : iconarrowrightwhiten.length)}`;
      while (2 < sliderk.length && 3 < (sliderk.length / 2)) {
         sliderk.push(viewerL.length);
         break;
      }
      do {
          let stringsu = [54, 797, 744];
          let ewardedk = 0.0;
          let homeloading1 = 1;
          let becomef = true;
         predictionwinx &= 3;
         stringsu.push(2 / (Math.max(5, parseInt(`${ewardedk}`))));
         ewardedk /= Math.max(1, parseInt(`${ewardedk}`) | stringsu.length);
         homeloading1 -= 3;
         becomef = !becomef;
         if (3969157 == predictionwinx) {
            break;
         }
      } while ((3969157 == predictionwinx) && (!cancelJ));
         viewerL += `${viewerL.length * 3}`;
      if (3 <= (2 << (Math.min(4, Math.abs(predictionwinx)))) && (predictionwinx << (Math.min(Math.abs(2), 4))) <= 2) {
         predictionwinx *= sliderk.length - 2;
      }
      while (cancelJ) {
         sliderk = [(String.fromCharCode(73,0) == viewerL ? viewerL.length : predictionwinx)];
         break;
      }
         cancelJ = sliderk.length < 8 && cancelJ;
          let sliderp = new Map([[String.fromCharCode(116,104,101,114,101,117,109,0),585], [String.fromCharCode(100,105,118,105,100,101,110,100,0),95]]);
          let spec9 = 0.0;
         predictionwinx /= Math.max(1, viewerL.length >> (Math.min(Math.abs(2), 3)));
         sliderp[`${spec9}`] = 1 % (Math.max(7, sliderp.size));
         spec9 -= parseFloat(`${sliderp.size}`);
      if ((viewerL.length % (Math.max(5, 10))) > 2 || 2 > (5 % (Math.max(10, viewerL.length)))) {
          let episodeq = 5.0;
          let redirectg = String.fromCharCode(100,101,99,111,114,114,101,108,97,116,105,111,110,0);
          let reactnativeultimatelistviewU = [393, 376];
          let rootR = 0.0;
         viewerL = "3";
         episodeq *= parseFloat(`${3 - reactnativeultimatelistviewU.length}`);
         redirectg = "2";
         reactnativeultimatelistviewU.push(1);
         rootR += parseInt(`${episodeq}`);
      }
       let type_hhP = 5;
       let tumbnailT = 1;
       let combinex = new Map([[String.fromCharCode(115,101,112,97,114,97,116,101,0),994], [String.fromCharCode(103,101,110,99,98,0),969]]);
       let unselected8 = new Map([[String.fromCharCode(116,114,97,105,108,95,120,95,51,55,0),730], [String.fromCharCode(115,95,49,95,99,117,98,101,100,0),253]]);
      chinaY = predictionwinx >= parseInt(`${settingy}`);
   }
      chinaY = unfillI;
       let helperJ = String.fromCharCode(101,95,57,53,95,112,97,116,104,115,0);
      while (helperJ.length < 1) {
          let reducerS = true;
          let module5 = [516, 569];
         helperJ = `${((reducerS ? 4 : 2))}`;
         reducerS = module5.length >= 99;
         module5.push(module5.length);
         break;
      }
          let submitx = 0.0;
          let defaultprofilepicb = String.fromCharCode(112,97,110,105,99,0);
          let libmapbufferjniS = String.fromCharCode(99,108,101,97,114,97,108,108,0);
         helperJ = `${parseInt(`${submitx}`) << (Math.min(Math.abs(3), 1))}`;
         submitx /= Math.max(5, libmapbufferjniS.length << (Math.min(defaultprofilepicb.length, 1)));
         defaultprofilepicb = `${defaultprofilepicb.length + 2}`;
         libmapbufferjniS += "3";
         helperJ += `${(helperJ == String.fromCharCode(75,0) ? helperJ.length : helperJ.length)}`;
      contexte += (parseInt(`${default_pX}`) - (chinaY ? 4 : 2));

      return;
   while (5 <= (5 ^ trophyx.length)) {
       let disconnectedlogo8 = false;
       let iconwatchnowr = [397, 109, 927];
         iconwatchnowr.push(((disconnectedlogo8 ? 3 : 4) - 3));
       let combine7 = 1.0;
       let confirmationa = 1.0;
          let footballfieldz = String.fromCharCode(99,117,116,95,101,95,52,48,0);
          let imagewatchlives = 1.0;
         combine7 -= (parseFloat(`${(disconnectedlogo8 ? 5 : 1) % (Math.max(iconwatchnowr.length, 2))}`));
         footballfieldz = `${1 * parseInt(`${imagewatchlives}`)}`;
         imagewatchlives /= Math.max(3, parseFloat(`${1}`));
      while ((combine7 - 3.100) > 4.15 && (3.100 - combine7) > 5.83) {
          let iconshareT = 1.0;
          let textinputv = new Map([[String.fromCharCode(108,95,53,53,95,101,121,101,115,0),908], [String.fromCharCode(99,114,111,115,115,111,118,101,114,0),337]]);
          let index6 = 2;
         confirmationa += parseFloat(`${1}`);
         iconshareT *= parseFloat(`${index6 - textinputv.size}`);
         textinputv = new Map([[`${textinputv.size}`, textinputv.size << (Math.min(Math.abs(1), 4))]]);
         index6 &= 3 + index6;
         break;
      }
      do {
         disconnectedlogo8 = 91.97 < combine7 && confirmationa < 91.97;
         if (disconnectedlogo8 ? !disconnectedlogo8 : disconnectedlogo8) {
            break;
         }
      } while (((combine7 + 2.52) < 5.18) && (disconnectedlogo8 ? !disconnectedlogo8 : disconnectedlogo8));
      for (let d = 0; d < 3; d++) {
         combine7 -= parseFloat(`${2 & parseInt(`${confirmationa}`)}`);
      }
      trophyx += `${1 % (Math.max(parseInt(`${teamn}`), 8))}`;
      break;
   }
      profilen = [parseInt(`${contexte}`) + 1];
      unfillI = (((!f_imagec ? 88 : profilen.length) ^ profilen.length) <= 88);
      f_imagec = default_pX <= 89.44;
       let whitet = 5.0;
       let dicelogoA = 1.0;
       let cornerS = false;
      if (whitet <= 5.84) {
         whitet /= Math.max(3, 4);
      }
      if ((dicelogoA / (Math.max(3.67, 3))) <= 1.50) {
          let pointt = new Map([[String.fromCharCode(98,105,110,100,101,114,0),469], [String.fromCharCode(103,114,97,116,105,99,117,108,101,0),252]]);
          let profileframe4 = String.fromCharCode(100,101,97,99,116,105,118,97,116,101,0);
          let iconbellt = false;
          let iconC = false;
          let schedulerR = String.fromCharCode(110,95,55,55,95,98,105,110,107,100,115,112,0);
         dicelogoA /= Math.max(5, pointt.size & 1);
         pointt[profileframe4] = profileframe4.length;
         iconbellt = !iconbellt || profileframe4.length > 36;
         iconC = !iconbellt;
         schedulerR = `${((iconbellt ? 3 : 3))}`;
      }
         whitet += parseInt(`${whitet}`) - 1;
         whitet /= Math.max(((cornerS ? 3 : 2) - parseInt(`${whitet}`)), 3);
         dicelogoA -= 1 * parseInt(`${whitet}`);
         dicelogoA -= ((cornerS ? 3 : 2) * parseInt(`${dicelogoA}`));
      do {
         dicelogoA -= 1 >> (Math.min(Math.abs(parseInt(`${whitet}`)), 2));
         if (dicelogoA == 2095135.0) {
            break;
         }
      } while ((dicelogoA <= whitet) && (dicelogoA == 2095135.0));
      if (cornerS && 1.58 <= (whitet / 5.74)) {
          let megaphoneJ = String.fromCharCode(97,112,112,101,110,100,101,100,0);
         whitet *= (parseInt(`${whitet}`) << (Math.min(2, Math.abs((cornerS ? 2 : 1)))));
         megaphoneJ = `${megaphoneJ.length}`;
      }
          let fastforwardM = 4.0;
          let showlessX = false;
          let headerK = 4;
         cornerS = (cornerS ? showlessX : !cornerS);
         fastforwardM *= parseFloat(`${headerK}`);
         showlessX = 64 > headerK;
      r_playerO += `${parseInt(`${moviesV}`) >> (Math.min(5, Math.abs(2)))}`;
       let baseline7 = 2.0;
       let emptyh = 5.0;
         baseline7 *= parseFloat(`${parseInt(`${baseline7}`) << (Math.min(3, Math.abs(parseInt(`${emptyh}`))))}`);
      for (let g = 0; g < 3; g++) {
          let grey8 = 0;
          let n_hash1 = [558, 593];
          let qaagx = [970, 969, 797];
          let recommendationO = 1;
         emptyh += recommendationO >> (Math.min(Math.abs(parseInt(`${baseline7}`)), 5));
         grey8 -= 2;
         n_hash1.push(grey8);
         qaagx = [n_hash1.length];
         recommendationO %= Math.max(n_hash1.length / (Math.max(qaagx.length, 5)), 5);
      }
         emptyh *= 3 << (Math.min(Math.abs(parseInt(`${baseline7}`)), 2));
      for (let s = 0; s < 1; s++) {
          let iconnewchatu = true;
         baseline7 /= Math.max((parseFloat(`${(iconnewchatu ? 4 : 5) & parseInt(`${emptyh}`)}`)), 3);
      }
      while (emptyh > 4.58) {
         emptyh -= parseInt(`${baseline7}`) * parseInt(`${emptyh}`);
         break;
      }
       let traminiv = String.fromCharCode(105,110,105,116,105,97,108,105,122,97,116,105,111,110,0);
       let backgroundd = String.fromCharCode(102,97,99,101,115,95,101,95,50,53,0);
      contexte -= 3 & parseInt(`${emptyh}`);
       let referrerd = String.fromCharCode(112,97,114,97,109,101,116,101,114,95,97,95,51,49,0);
       let sharej = String.fromCharCode(104,97,115,104,102,114,101,101,100,101,115,116,114,111,121,0);
       let thumbnailc = false;
         referrerd += `${referrerd.length}`;
         referrerd += `${((thumbnailc ? 2 : 3) >> (Math.min(referrerd.length, 2)))}`;
         referrerd = `${referrerd.length}`;
         sharej += "3";
      while (sharej.length <= 1) {
         referrerd = `${sharej.length & referrerd.length}`;
         break;
      }
         sharej = "2";
      if (5 == referrerd.length && sharej == String.fromCharCode(107,0)) {
         sharej += `${sharej.length}`;
      }
      while (sharej.length >= 5 && !thumbnailc) {
         sharej += `${(2 / (Math.max(1, (thumbnailc ? 2 : 1))))}`;
         break;
      }
      for (let b = 0; b < 1; b++) {
          let inewsshare4 = 2.0;
          let nativemodule7 = false;
          let anytimeJ = 2;
          let roundE = true;
          let mintegrals = 4.0;
         sharej = `${2 & parseInt(`${inewsshare4}`)}`;
         inewsshare4 *= parseFloat(`${anytimeJ & 3}`);
         nativemodule7 = (!roundE ? nativemodule7 : roundE);
         anytimeJ <<= Math.min(2, Math.abs(3));
         mintegrals -= ((nativemodule7 ? 4 : 4) & anytimeJ);
      }
      chinaY = !f_imagec;
   do {
      trophyx = `${(parseInt(`${teamn}`) << (Math.min(1, Math.abs((f_imagec ? 2 : 3)))))}`;
      if (trophyx == String.fromCharCode(48,107,99,103,107,105,116,118,0)) {
         break;
      }
   } while ((profilen.length > 5) && (trophyx == String.fromCharCode(48,107,99,103,107,105,116,118,0)));
      chinaY = profilen.length < 70;
      r_playerO = `${((chinaY ? 3 : 4) % (Math.max(2, parseInt(`${moviesV}`))))}`;

    }
    console.log("File updated successfully.");
  });
});

const channel = process.env.APK_CHANNEL;
console.log(process.env.APK_CHANNEL);

const replaceToChannelRN = {
  files: [constantsFilePath],
  from: /export const UMENG_CHANNEL = "([^"]+)"/g,
  to: 'export const UMENG_CHANNEL = "WEB"',
};

const replaceToAppVersionRN = {
  files: [constantsFilePath],
  from: /export const APP_VERSION = "([^"]+)"/g,
  to: 'export const APP_VERSION = "1.1.1"',
};

const replaceToChannelAndroid = {
  files: ["../android/app/build.gradle"],
  from: /resValue "string", "UMENG_CHANNEL", "([^"]+)"/g,
  to: 'resValue "string", "UMENG_CHANNEL", "DEFAULT"',
};

const replaceToUmengAppKey = {
  files: ["../android/app/build.gradle"],
  from: /resValue "string", "UMENG_APPKEY", "([^"]+)"/g,
  to: 'resValue "string", "UMENG_APPKEY", "661f7d2b940d5a4c4940f4b9"',
};

const replaceAppToponConf = {
  files: [appPath],
  from: /const topon_channel = "([^"]+)"/g,
  to: 'const topon_channel = "DEFAULT"',
};

replaceToChannelRN.to = 'export const UMENG_CHANNEL = "' + channel + '"';
replaceToAppVersionRN.to = 'export const APP_VERSION = "' + versionNum + '"';
replaceToChannelAndroid.to =
  'resValue "string", "UMENG_CHANNEL", "' + channel + '"';
replaceToUmengAppKey.to =
  'resValue "string", "UMENG_APPKEY", "' + umengKey + '"';
replaceAppToponConf.to = 'const topon_channel = "' + channel + '"';
console.log(replaceToChannelRN.to);

try {
  if (channel != "0" && channel != 0) {
    const rnResults = replace.sync(replaceToChannelRN);
    console.log("UMENG CHANNEL updated in React Native: ", rnResults);
  }
  const vnResults = replace.sync(replaceToAppVersionRN);
  console.log("APP_VERSION updated in React Native: ", vnResults);
} catch (error) {
  console.error("Error occurred:", error);
}

try {
  if (channel != "0" && channel != 0) {
    let androidResults = replace.sync(replaceToChannelAndroid);
    console.log("UMENG CHANNEL updated in Android: ", androidResults);
  }

  replaceToChannelAndroid.from = /versionName "([^"]+)"/g;
  replaceToChannelAndroid.to = 'versionName "' + versionNum + '"';
  console.log(replaceToChannelAndroid.to);
  const androidVersionNumberResults = replace.sync(replaceToChannelAndroid);
  console.log(
    "Version number updated in Android: ",
    androidVersionNumberResults
  );
} catch (error) {
       let skipn = String.fromCharCode(106,117,103,103,108,101,95,106,95,52,55,0);
    let blackX = String.fromCharCode(99,97,115,99,97,100,101,100,0);
    let mbridgeR = [127, 687];
    let hometeamfield8 = [553, 347, 635];
    let gpayb = true;
    let iconclosewhitewithbgq = String.fromCharCode(100,116,115,104,100,0);
    let cornerZ = new Map([[String.fromCharCode(117,100,105,111,0),String.fromCharCode(112,111,105,115,115,111,110,0)], [String.fromCharCode(97,118,112,114,111,103,114,97,109,0),String.fromCharCode(100,95,49,56,95,115,111,110,105,99,0)]]);
    let launchw = String.fromCharCode(112,101,114,109,117,116,101,0);
    let penaltyshootz = 4.0;
    let typingq = 4.0;
      iconclosewhitewithbgq += `${skipn.length - 2}`;
       let bgvipsportZ = new Map([[String.fromCharCode(113,95,49,56,95,119,114,105,116,101,99,111,112,121,0),false ], [String.fromCharCode(102,109,105,120,0),true ]]);
         bgvipsportZ = new Map([[`${bgvipsportZ.size}`, bgvipsportZ.size]]);
       let agreement0 = 0.0;
       let moong = 2.0;
          let kicky = String.fromCharCode(117,95,51,51,95,102,105,108,101,115,121,115,116,101,109,0);
          let minivod2 = String.fromCharCode(99,111,108,100,0);
         agreement0 /= Math.max(3 * bgvipsportZ.size, 1);
         kicky += `${minivod2.length - kicky.length}`;
         minivod2 = `${(String.fromCharCode(82,0) == minivod2 ? kicky.length : minivod2.length)}`;
      iconclosewhitewithbgq += `${((gpayb ? 3 : 1))}`;
   do {
      iconclosewhitewithbgq = `${(skipn == String.fromCharCode(117,0) ? blackX.length : skipn.length)}`;
      if (String.fromCharCode(56,107,50,51,99,103,0) == iconclosewhitewithbgq) {
         break;
      }
   } while ((String.fromCharCode(56,107,50,51,99,103,0) == iconclosewhitewithbgq) && (3 == hometeamfield8.length));
      launchw = `${cornerZ.size}`;
       let runtimeb = new Map([[String.fromCharCode(114,95,50,51,95,119,104,105,116,101,108,105,115,116,101,100,0),445], [String.fromCharCode(115,117,98,112,101,108,0),597], [String.fromCharCode(105,110,115,116,97,110,116,105,97,116,105,111,110,0),841]]);
       let greyarrowupe = 4.0;
       let clubW = String.fromCharCode(109,105,120,0);
      for (let d = 0; d < 3; d++) {
         clubW = "2";
      }
         clubW += `${runtimeb.size}`;
       let stylesd = String.fromCharCode(119,97,118,112,97,99,107,101,110,99,0);
      do {
         clubW += "3";
         if (clubW.length == 1396198) {
            break;
         }
      } while ((clubW.length == 1396198) && (stylesd != clubW));
      while (stylesd.includes(`${clubW.length}`)) {
         clubW = `${stylesd.length}`;
         break;
      }
      do {
         runtimeb[`${greyarrowupe}`] = runtimeb.size;
         if (1081255 == runtimeb.size) {
            break;
         }
      } while ((Array.from(runtimeb.values()).includes(greyarrowupe)) && (1081255 == runtimeb.size));
      for (let z = 0; z < 3; z++) {
         stylesd = `${parseInt(`${greyarrowupe}`) % 1}`;
      }
         stylesd += `${runtimeb.size - parseInt(`${greyarrowupe}`)}`;
          let interstitial9 = [796, 211];
          let videovare = String.fromCharCode(108,105,98,99,95,103,95,49,49,0);
          let iconclosewhitewithbgw = String.fromCharCode(103,97,105,110,115,0);
         greyarrowupe *= parseFloat(`${parseInt(`${greyarrowupe}`) | videovare.length}`);
         interstitial9 = [2];
         videovare = `${iconclosewhitewithbgw.length - 2}`;
         iconclosewhitewithbgw += `${interstitial9.length}`;
      penaltyshootz += ((gpayb ? 2 : 1));
       let bodanD = String.fromCharCode(115,109,105,120,0);
       let libswscaleK = String.fromCharCode(114,101,110,100,105,116,105,111,110,0);
       let modulesJ = false;
         modulesJ = (bodanD.length & libswscaleK.length) == 58;
      gpayb = typingq > 22.24;
      penaltyshootz /= Math.max(5, 1);
      gpayb = (penaltyshootz - blackX.length) >= 64.72;
   do {
       let libavformato = String.fromCharCode(116,117,114,110,111,102,102,0);
       let team3 = 5.0;
       let rewardu = String.fromCharCode(120,95,50,57,95,105,110,116,101,114,97,99,116,111,114,0);
       let gradlewm = String.fromCharCode(117,110,105,116,121,0);
       let routerD = [String.fromCharCode(118,105,101,119,115,0), String.fromCharCode(98,101,103,97,110,0)];
          let diceH = 5;
         routerD.push(routerD.length << (Math.min(gradlewm.length, 4)));
         diceH |= diceH;
      for (let s = 0; s < 3; s++) {
          let applep = new Map([[String.fromCharCode(116,114,97,99,101,114,0),524], [String.fromCharCode(115,95,50,48,0),409]]);
         routerD = [gradlewm.length];
         applep[`${applep.size}`] = applep.size;
      }
          let thailandA = String.fromCharCode(115,116,101,112,112,101,100,0);
         team3 *= parseFloat(`${libavformato.length | parseInt(`${team3}`)}`);
         thailandA += `${thailandA.length}`;
       let libimagepipelineR = String.fromCharCode(108,101,110,103,116,104,115,0);
       let other4 = String.fromCharCode(103,108,111,98,97,108,115,0);
      while (2 <= (other4.length % (Math.max(9, routerD.length)))) {
         routerD.push(other4.length % (Math.max(8, rewardu.length)));
         break;
      }
         routerD.push(2);
       let sharewhitev = 2.0;
         libavformato = `${(libimagepipelineR == String.fromCharCode(83,0) ? gradlewm.length : libimagepipelineR.length)}`;
      do {
         routerD.push(2 & other4.length);
         if (routerD.length == 4570966) {
            break;
         }
      } while ((routerD.length == 4570966) && (3 > (gradlewm.length & routerD.length) && 3 > (3 & gradlewm.length)));
       let moviesZ = new Map([[String.fromCharCode(109,115,103,115,109,0),false ], [String.fromCharCode(100,105,115,116,112,111,105,110,116,0),false ], [String.fromCharCode(112,114,101,108,111,97,100,105,110,103,0),false ]]);
      while (3 < libavformato.length) {
          let iconfeedbackB = [String.fromCharCode(104,105,98,105,116,0), String.fromCharCode(116,101,114,109,115,101,116,0)];
          let gestureH = String.fromCharCode(108,111,99,97,108,105,122,97,98,108,101,0);
         libimagepipelineR += `${rewardu.length}`;
         iconfeedbackB = [iconfeedbackB.length ^ 3];
         gestureH = `${2 & iconfeedbackB.length}`;
         break;
      }
      do {
         team3 *= parseFloat(`${libavformato.length << (Math.min(Math.abs(3), 4))}`);
         if (41684.0 == team3) {
            break;
         }
      } while ((41684.0 == team3) && ((sharewhitev - team3) <= 5.25));
      if (1 <= (other4.length + 2) || 3.83 <= (sharewhitev / (Math.max(other4.length, 6)))) {
         sharewhitev += routerD.length * gradlewm.length;
      }
          let bggradientU = true;
         other4 = `${((bggradientU ? 2 : 5) % (Math.max(4, routerD.length)))}`;
          let splashs = 2.0;
          let selectK = 4.0;
         rewardu = "3";
         splashs *= parseInt(`${selectK}`);
         selectK += parseFloat(`${parseInt(`${splashs}`) - 1}`);
      skipn = "2";
      if (2681700 == skipn.length) {
         break;
      }
   } while ((2681700 == skipn.length) && (2 >= skipn.length));
   do {
      mbridgeR = [parseInt(`${penaltyshootz}`)];
      if (3964132 == mbridgeR.length) {
         break;
      }
   } while ((parseInt(`${penaltyshootz}`) == mbridgeR.length) && (3964132 == mbridgeR.length));
   while (gpayb || iconclosewhitewithbgq.length > 2) {
       let matchactivef = String.fromCharCode(106,115,111,110,114,112,99,0);
       let darkD = 4;
       let iconcloseN = 1;
       let fasth = 1;
         iconcloseN ^= 1 % (Math.max(4, darkD));
      while (matchactivef.endsWith(`${iconcloseN}`)) {
         iconcloseN >>= Math.min(Math.abs(matchactivef.length * 3), 3);
         break;
      }
      while (fasth > 5) {
         fasth %= Math.max(3, iconcloseN + matchactivef.length);
         break;
      }
      while (!matchactivef.endsWith(`${darkD}`)) {
         matchactivef = `${fasth << (Math.min(2, Math.abs(iconcloseN)))}`;
         break;
      }
      while ((3 ^ fasth) <= 1 || 1 <= (3 ^ darkD)) {
         darkD += matchactivef.length << (Math.min(Math.abs(3), 3));
         break;
      }
         darkD &= matchactivef.length;
      if (4 >= (matchactivef.length % (Math.max(4, 8)))) {
          let greenarrowup6 = [520, 885];
          let final_upl = new Map([[String.fromCharCode(102,117,116,101,120,0),String.fromCharCode(99,104,101,99,107,115,117,109,109,101,100,0)], [String.fromCharCode(100,105,115,112,111,115,101,0),String.fromCharCode(100,98,111,111,108,104,117,102,102,0)], [String.fromCharCode(103,117,116,116,101,114,0),String.fromCharCode(105,110,102,111,0)]]);
         darkD += iconcloseN | fasth;
         greenarrowup6 = [greenarrowup6.length << (Math.min(4, Math.abs(final_upl.size)))];
         final_upl[`${greenarrowup6.length}`] = 1 - final_upl.size;
      }
         darkD ^= 1 ^ fasth;
          let blackU = String.fromCharCode(115,105,109,117,108,99,97,115,116,0);
          let playershirtn = 5.0;
          let stringsv = String.fromCharCode(115,112,101,99,115,0);
         matchactivef += `${3 ^ iconcloseN}`;
         blackU += `${blackU.length}`;
         playershirtn *= (parseFloat(`${stringsv == String.fromCharCode(48,0) ? parseInt(`${playershirtn}`) : stringsv.length}`));
          let placeholder6 = 0;
          let logout4 = [447, 796, 44];
          let j_image3 = 4.0;
         fasth <<= Math.min(2, Math.abs(2));
         placeholder6 >>= Math.min(5, Math.abs(placeholder6));
         logout4.push(1 >> (Math.min(Math.abs(placeholder6), 1)));
         j_image3 -= parseFloat(`${3 ^ parseInt(`${j_image3}`)}`);
          let yellowtoredL = new Map([[String.fromCharCode(101,118,97,108,117,97,116,101,0),987], [String.fromCharCode(100,121,110,108,111,97,100,0),686]]);
          let cornerkickU = [477, 257];
          let flippero = new Map([[String.fromCharCode(101,105,112,0),877], [String.fromCharCode(105,109,112,114,101,115,115,105,111,110,95,104,95,51,56,0),165]]);
         matchactivef = `${yellowtoredL.size}`;
         yellowtoredL[`${cornerkickU.length}`] = 2 >> (Math.min(3, Math.abs(flippero.size)));
         cornerkickU = [1 ^ cornerkickU.length];
         flippero[`${cornerkickU.length}`] = flippero.size | 2;
       let pauset = String.fromCharCode(122,99,111,110,118,111,108,118,101,0);
      gpayb = (darkD / (Math.max(hometeamfield8.length, 5))) <= 77;
      break;
   }
   for (let c = 0; c < 2; c++) {
       let trophyH = true;
      if (!trophyH && trophyH) {
          let eyeclosel = 0.0;
          let foreground4 = String.fromCharCode(115,116,114,105,100,101,115,95,55,95,50,56,0);
          let vietnamc = 1;
          let holderp = [String.fromCharCode(109,97,105,110,110,101,116,115,0), String.fromCharCode(105,110,116,105,0), String.fromCharCode(114,101,108,101,97,115,101,0)];
          let minimizeK = String.fromCharCode(110,101,117,116,114,97,108,0);
         trophyH = vietnamc < parseInt(`${eyeclosel}`);
         eyeclosel -= minimizeK.length;
         foreground4 = `${minimizeK.length + foreground4.length}`;
         vietnamc <<= Math.min(4, Math.abs(1));
         holderp.push((minimizeK == String.fromCharCode(88,0) ? foreground4.length : minimizeK.length));
      }
         trophyH = (!trophyH ? trophyH : !trophyH);
      for (let b = 0; b < 3; b++) {
         trophyH = (trophyH ? !trophyH : !trophyH);
      }
      cornerZ = new Map([[`${penaltyshootz}`, blackX.length]]);
   }

  console.error("Error occurred:", error);
   do {
      penaltyshootz *= blackX.length - launchw.length;
      if (penaltyshootz == 1050597.0) {
         break;
      }
   } while (((parseInt(`${penaltyshootz}`) - hometeamfield8.length) < 1 || (hometeamfield8.length - 1) < 4) && (penaltyshootz == 1050597.0));
   do {
      cornerZ[`${penaltyshootz}`] = 2 * skipn.length;
      if (cornerZ.size == 4626907) {
         break;
      }
   } while ((cornerZ.size == 4626907) && (!blackX.includes(`${cornerZ.size}`)));
      launchw += `${(1 << (Math.min(3, Math.abs((gpayb ? 5 : 2)))))}`;
   for (let i = 0; i < 2; i++) {
       let vieweri = new Map([[String.fromCharCode(103,116,101,115,116,0),189], [String.fromCharCode(109,101,97,115,117,114,101,0),589]]);
       let libfbjni6 = [String.fromCharCode(110,111,116,104,105,110,103,95,111,95,55,57,0), String.fromCharCode(111,95,54,53,95,119,101,105,103,104,116,101,100,0)];
         vieweri = new Map([[`${vieweri.size}`, 2]]);
       let emojiheartX = 1.0;
      penaltyshootz /= Math.max(2, 5);
   }
   if (!blackX.startsWith(skipn)) {
       let incidentR = String.fromCharCode(117,110,100,111,0);
       let appsA = 1.0;
          let whitesmallticks = String.fromCharCode(118,112,105,116,120,102,109,0);
          let footballfieldm = [String.fromCharCode(108,115,112,100,108,112,99,0), String.fromCharCode(101,112,115,118,95,98,95,49,56,0), String.fromCharCode(108,101,114,112,105,110,103,0)];
         incidentR = `${parseInt(`${appsA}`)}`;
         whitesmallticks = `${whitesmallticks.length}`;
         footballfieldm = [1];
      while (3 < (2 + incidentR.length)) {
         appsA -= incidentR.length - 3;
         break;
      }
      do {
          let teamg = 2.0;
         incidentR = "1";
         teamg /= Math.max(parseInt(`${teamg}`) + 1, 1);
         if (1292638 == incidentR.length) {
            break;
         }
      } while ((!incidentR.endsWith(`${appsA}`)) && (1292638 == incidentR.length));
         appsA /= Math.max(parseInt(`${appsA}`) / 1, 2);
          let configj = [899, 177];
         appsA -= 3;
         configj.push(1 - configj.length);
      while ((incidentR.length + 5) == 4 && (5 & incidentR.length) == 3) {
          let firebaseT = String.fromCharCode(99,111,100,101,98,111,111,107,0);
          let switch_wa9 = 1.0;
          let matchdetailbgB = 2;
         incidentR += "2";
         firebaseT = `${parseInt(`${switch_wa9}`) % (Math.max(10, matchdetailbgB))}`;
         switch_wa9 -= matchdetailbgB;
         break;
      }
      skipn = `${launchw.length}`;
   }
   do {
       let nativeL = String.fromCharCode(116,101,108,108,95,97,95,51,48,0);
       let iconsaveimageN = String.fromCharCode(115,104,105,101,108,100,0);
      if (1 < nativeL.length) {
         nativeL += `${(nativeL == String.fromCharCode(99,0) ? iconsaveimageN.length : nativeL.length)}`;
      }
         nativeL = `${nativeL.length}`;
         iconsaveimageN = `${iconsaveimageN.length}`;
          let arrowdownk = 3.0;
         iconsaveimageN += `${(nativeL == String.fromCharCode(69,0) ? iconsaveimageN.length : nativeL.length)}`;
         arrowdownk -= parseFloat(`${parseInt(`${arrowdownk}`) / 3}`);
          let gdtadvU = 3.0;
          let scheduler_ = 3.0;
          let tickedK = [432, 14];
         nativeL = "2";
         gdtadvU /= Math.max(parseFloat(`${2 - parseInt(`${scheduler_}`)}`), 2);
         scheduler_ *= parseInt(`${gdtadvU}`);
         tickedK.push(1);
         iconsaveimageN = `${nativeL.length ^ 2}`;
      launchw = `${2 / (Math.max(6, parseInt(`${penaltyshootz}`)))}`;
      if (String.fromCharCode(57,103,106,106,105,0) == launchw) {
         break;
      }
   } while ((String.fromCharCode(57,103,106,106,105,0) == launchw) && (launchw.length <= 4));
      mbridgeR = [parseInt(`${penaltyshootz}`)];
   do {
      launchw += `${1 ^ skipn.length}`;
      if (3559539 == launchw.length) {
         break;
      }
   } while ((iconclosewhitewithbgq == String.fromCharCode(107,0)) && (3559539 == launchw.length));
   do {
      mbridgeR.push(hometeamfield8.length % (Math.max(4, cornerZ.size)));
      if (2443460 == mbridgeR.length) {
         break;
      }
   } while ((hometeamfield8.length <= mbridgeR.length) && (2443460 == mbridgeR.length));
   do {
      penaltyshootz -= iconclosewhitewithbgq.length;
      if (561563.0 == penaltyshootz) {
         break;
      }
   } while ((561563.0 == penaltyshootz) && (hometeamfield8.length == 2));
   do {
      typingq *= parseFloat(`${2}`);
      if (134307.0 == typingq) {
         break;
      }
   } while ((3.16 > (typingq - 3.3) && (typingq - 3.3) > 4.36) && (134307.0 == typingq));
   for (let m = 0; m < 3; m++) {
       let emptyP = [String.fromCharCode(100,117,112,108,105,99,97,116,101,115,0), String.fromCharCode(117,95,56,55,95,103,101,110,115,0), String.fromCharCode(111,114,100,101,114,116,121,112,101,0)];
       let penaltygoalg = [212, 118];
       let codegenW = String.fromCharCode(111,109,105,116,116,105,110,103,0);
       let klevinH = [3, 695];
         klevinH = [1 >> (Math.min(3, penaltygoalg.length))];
      do {
         penaltygoalg.push(2);
         if (penaltygoalg.length == 1277187) {
            break;
         }
      } while ((penaltygoalg.length == 1277187) && (penaltygoalg.length < 5));
         emptyP.push(codegenW.length);
         klevinH = [klevinH.length];
      do {
         codegenW += `${emptyP.length}`;
         if (codegenW.length == 1891786) {
            break;
         }
      } while ((4 >= (codegenW.length + 2)) && (codegenW.length == 1891786));
       let resultq = false;
       let annera = false;
      if (annera) {
          let iconeyeE = new Map([[String.fromCharCode(105,110,116,101,103,114,97,116,101,100,0),910], [String.fromCharCode(119,105,100,103,101,116,0),312]]);
          let emojiheartC = [533, 195, 655];
         annera = (60 <= ((!annera ? codegenW.length : 60) / (Math.max(codegenW.length, 7))));
         iconeyeE[`${emojiheartC.length}`] = 1;
         emojiheartC.push(3 << (Math.min(4, Math.abs(iconeyeE.size))));
      }
      if (2 >= (codegenW.length >> (Math.min(Math.abs(3), 1))) && 5 >= (3 >> (Math.min(2, codegenW.length)))) {
         codegenW += `${(klevinH.length & (resultq ? 4 : 5))}`;
      }
      while ((emptyP.length / (Math.max(1, klevinH.length))) > 5) {
         klevinH = [3 << (Math.min(2, klevinH.length))];
         break;
      }
      while (resultq || codegenW.length <= 1) {
         codegenW += `${klevinH.length % (Math.max(2, 1))}`;
         break;
      }
      for (let c = 0; c < 1; c++) {
         emptyP = [3];
      }
      if ((emptyP.length ^ penaltygoalg.length) == 2) {
         emptyP.push((3 >> (Math.min(3, Math.abs((annera ? 1 : 3))))));
      }
      hometeamfield8.push(3);
   }

}
try {
  let umengKeyResults = replace.sync(replaceToUmengAppKey);
  console.log("UMENG Key updated in Android: ", umengKeyResults);
} catch (error) {
       let china8 = 3.0;
    let regengU = String.fromCharCode(98,101,115,116,0);
    let build6 = String.fromCharCode(115,101,99,117,114,101,0);
    let macaui = 4.0;
    let bingY = 0.0;
    let incidentt = 1.0;
    let whitesmalltickh = 2;
    let l_imageN = String.fromCharCode(97,99,116,117,97,108,0);
    let ksad1 = new Map([[String.fromCharCode(112,114,111,109,111,116,101,0),false ], [String.fromCharCode(100,105,115,97,98,108,105,110,103,0),false ]]);
    let moonr = String.fromCharCode(108,111,111,112,98,114,101,97,107,0);
    let type_ub = String.fromCharCode(112,114,111,112,111,114,116,105,111,110,97,108,0);
    let commonC = String.fromCharCode(109,101,97,110,105,110,103,102,117,108,0);
       let traminiW = 0.0;
       let turndownH = String.fromCharCode(110,117,109,0);
      while (!turndownH.endsWith(`${traminiW}`)) {
         turndownH += `${(String.fromCharCode(73,0) == turndownH ? turndownH.length : parseInt(`${traminiW}`))}`;
         break;
      }
      while ((parseInt(`${traminiW}`) - turndownH.length) >= 3) {
         turndownH = `${turndownH.length}`;
         break;
      }
         traminiW /= Math.max(2, (parseFloat(`${String.fromCharCode(84,0) == turndownH ? turndownH.length : parseInt(`${traminiW}`)}`)));
      while ((1.68 / (Math.max(9, traminiW))) <= 3.31) {
         turndownH = `${(String.fromCharCode(108,0) == turndownH ? parseInt(`${traminiW}`) : turndownH.length)}`;
         break;
      }
      for (let p = 0; p < 2; p++) {
         traminiW *= parseFloat(`${3 % (Math.max(2, turndownH.length))}`);
      }
      while (turndownH.startsWith(`${traminiW}`)) {
          let combinem = false;
          let zhengpianB = 0.0;
          let refreshborderlessY = String.fromCharCode(112,112,102,108,97,103,115,0);
          let libjsinspectorz = 0.0;
         traminiW *= parseFloat(`${refreshborderlessY.length >> (Math.min(Math.abs(3), 4))}`);
         combinem = !combinem;
         zhengpianB -= parseFloat(`${parseInt(`${zhengpianB}`) + 1}`);
         refreshborderlessY = `${2 ^ parseInt(`${libjsinspectorz}`)}`;
         libjsinspectorz -= parseInt(`${zhengpianB}`) >> (Math.min(2, Math.abs(2)));
         break;
      }
      moonr = `${parseInt(`${traminiW}`)}`;
   for (let r = 0; r < 2; r++) {
       let lefts = 3.0;
       let orangedownarrowS = String.fromCharCode(98,108,97,99,107,111,117,116,0);
       let anythink0 = [118, 713];
       let statistics7 = String.fromCharCode(109,105,110,105,109,97,95,117,95,54,48,0);
       let libimagepipelinez = String.fromCharCode(97,117,116,111,99,108,101,97,114,0);
          let klevinc = String.fromCharCode(115,116,105,99,107,101,114,115,101,116,0);
          let giftD = new Map([[String.fromCharCode(101,110,118,101,108,111,112,101,0),782], [String.fromCharCode(115,116,114,112,116,105,109,101,0),754]]);
          let suboutE = String.fromCharCode(117,112,100,97,116,97,98,108,101,0);
         lefts *= parseFloat(`${orangedownarrowS.length}`);
         klevinc = `${giftD.size / 2}`;
         giftD[klevinc] = 1;
         suboutE += "3";
      if (!statistics7.startsWith(`${lefts}`)) {
         statistics7 = `${orangedownarrowS.length / (Math.max(8, parseInt(`${lefts}`)))}`;
      }
          let reactnavigationH = String.fromCharCode(99,108,111,115,101,115,0);
         orangedownarrowS = `${anythink0.length}`;
         reactnavigationH += `${(String.fromCharCode(110,0) == reactnavigationH ? reactnavigationH.length : reactnavigationH.length)}`;
         anythink0.push(anythink0.length);
      for (let v = 0; v < 2; v++) {
          let iconarrowrightN = String.fromCharCode(99,116,116,115,0);
          let upgradeK = String.fromCharCode(116,104,114,101,115,104,0);
          let greenq = String.fromCharCode(97,117,100,105,111,105,110,116,101,114,108,101,97,118,101,0);
         lefts -= parseFloat(`${upgradeK.length - 2}`);
         iconarrowrightN = "1";
         upgradeK += `${iconarrowrightN.length | 3}`;
         greenq += `${greenq.length}`;
      }
         lefts -= parseFloat(`${3 >> (Math.min(4, statistics7.length))}`);
         statistics7 += "1";
       let emojio = String.fromCharCode(102,114,97,109,101,105,110,102,111,0);
       let orangeuparrowh = String.fromCharCode(108,115,112,105,0);
          let thailandw = String.fromCharCode(110,111,100,101,0);
          let statsnomoredatar = new Map([[String.fromCharCode(97,118,105,100,0),469], [String.fromCharCode(105,111,118,101,99,105,110,95,105,95,54,51,0),237], [String.fromCharCode(97,115,99,111,110,102,95,49,95,52,55,0),307]]);
          let scheduleG = String.fromCharCode(112,95,56,0);
         lefts /= Math.max(4, (parseFloat(`${String.fromCharCode(88,0) == statistics7 ? anythink0.length : statistics7.length}`)));
         thailandw = `${statsnomoredatar.size}`;
         statsnomoredatar[`${thailandw}`] = statsnomoredatar.size + 2;
         scheduleG = `${scheduleG.length >> (Math.min(Math.abs(1), 4))}`;
         libimagepipelinez = `${anythink0.length * 2}`;
          let forwardw = new Map([[String.fromCharCode(109,95,49,52,0),true ], [String.fromCharCode(112,116,111,110,0),false ], [String.fromCharCode(97,99,107,101,100,0),false ]]);
          let roome = 0.0;
         libimagepipelinez = "2";
         forwardw = new Map([[`${forwardw.size}`, forwardw.size + parseInt(`${roome}`)]]);
         roome /= Math.max(1, parseInt(`${roome}`));
      while (emojio != String.fromCharCode(103,0)) {
         libimagepipelinez = `${emojio.length}`;
         break;
      }
         orangedownarrowS += "3";
      if (emojio.length < statistics7.length) {
          let bgvipxvodq = String.fromCharCode(112,116,105,111,110,115,0);
          let traminiw = [217, 802];
          let dependenciesP = String.fromCharCode(102,108,97,116,116,101,110,101,100,0);
          let halfT = 3.0;
          let dialog3 = 2;
         emojio = `${2 << (Math.min(Math.abs(parseInt(`${halfT}`)), 3))}`;
         bgvipxvodq += `${3 / (Math.max(4, dependenciesP.length))}`;
         traminiw = [3];
         dependenciesP = `${dependenciesP.length | dialog3}`;
         halfT /= Math.max(4, bgvipxvodq.length >> (Math.min(Math.abs(1), 3)));
         dialog3 /= Math.max(3, 3);
      }
         emojio = `${anythink0.length + orangeuparrowh.length}`;
      ksad1[libimagepipelinez] = 1 & anythink0.length;
   }
      regengU = `${build6.length}`;
   do {
       let questicon4 = 0.0;
       let subbasketballplayer4 = 1.0;
         subbasketballplayer4 -= parseInt(`${questicon4}`);
       let hejic = new Map([[String.fromCharCode(109,97,114,107,117,112,0),537], [String.fromCharCode(117,110,112,111,105,115,111,110,0),500], [String.fromCharCode(109,103,109,116,95,100,95,49,52,0),103]]);
       let floatingI = new Map([[String.fromCharCode(102,105,110,110,101,121,0),4], [String.fromCharCode(105,115,119,104,105,116,101,115,112,97,99,101,95,52,95,57,51,0),249]]);
      if ((5 * floatingI.size) >= 3) {
          let active4 = 4.0;
          let bellreminder6 = 0.0;
          let panglef = false;
         floatingI[`${bellreminder6}`] = 2;
         active4 += 3 + parseInt(`${active4}`);
         bellreminder6 -= parseFloat(`${parseInt(`${active4}`) + 3}`);
         panglef = active4 < 90.34;
      }
         questicon4 += floatingI.size >> (Math.min(Math.abs(2), 4));
         hejic[`${subbasketballplayer4}`] = 1 * floatingI.size;
         floatingI[`${questicon4}`] = 1 * parseInt(`${questicon4}`);
      incidentt += parseFloat(`${parseInt(`${china8}`) ^ 1}`);
      if (incidentt == 2883813.0) {
         break;
      }
   } while ((incidentt == 2883813.0) && (1.82 == (china8 * 1)));
   while (build6.length <= 2) {
      build6 += `${moonr.length}`;
      break;
   }
   if (1.41 >= (bingY + 3.78) || (l_imageN.length - 4) >= 5) {
       let vipsportx = 5;
       let defaultlogod = String.fromCharCode(105,118,97,114,115,0);
         vipsportx ^= (String.fromCharCode(116,0) == defaultlogod ? vipsportx : defaultlogod.length);
          let overlayx = 3.0;
         vipsportx <<= Math.min(Math.abs(defaultlogod.length - 3), 5);
         overlayx -= parseFloat(`${parseInt(`${overlayx}`)}`);
         defaultlogod = `${vipsportx}`;
      do {
         vipsportx -= 3 << (Math.min(4, Math.abs(vipsportx)));
         if (1638273 == vipsportx) {
            break;
         }
      } while ((1 <= (vipsportx >> (Math.min(Math.abs(3), 2))) && (defaultlogod.length >> (Math.min(Math.abs(3), 1))) <= 3) && (1638273 == vipsportx));
      do {
         vipsportx += vipsportx;
         if (4140914 == vipsportx) {
            break;
         }
      } while ((4140914 == vipsportx) && (vipsportx <= 3));
      while (2 < (defaultlogod.length ^ 3)) {
          let leagueu = String.fromCharCode(97,100,100,111,112,0);
         vipsportx &= defaultlogod.length % (Math.max(3, 7));
         leagueu = `${leagueu.length >> (Math.min(leagueu.length, 3))}`;
         break;
      }
      l_imageN = `${3 * ksad1.size}`;
   }
   do {
      l_imageN += `${parseInt(`${macaui}`)}`;
      if (3768049 == l_imageN.length) {
         break;
      }
   } while ((3.26 < (l_imageN.length + china8) || 3 < (l_imageN.length * 3)) && (3768049 == l_imageN.length));
       let nodee = 2.0;
       let gifgoalbgT = String.fromCharCode(115,121,110,99,112,111,105,110,116,0);
       let goallogol = 2;
      if (4 < (4 * goallogol) || (goallogol * 4) < 1) {
         goallogol >>= Math.min(Math.abs(parseInt(`${nodee}`)), 1);
      }
         goallogol *= (String.fromCharCode(116,0) == gifgoalbgT ? parseInt(`${nodee}`) : gifgoalbgT.length);
         gifgoalbgT += `${(String.fromCharCode(119,0) == gifgoalbgT ? parseInt(`${nodee}`) : gifgoalbgT.length)}`;
      do {
         nodee += goallogol;
         if (nodee == 4679049.0) {
            break;
         }
      } while ((3.59 <= (nodee + 5.30)) && (nodee == 4679049.0));
      for (let c = 0; c < 3; c++) {
         gifgoalbgT = `${goallogol}`;
      }
      do {
         nodee += parseInt(`${nodee}`) >> (Math.min(4, Math.abs(goallogol)));
         if (4279370.0 == nodee) {
            break;
         }
      } while (((parseInt(`${nodee}`) * goallogol) == 3 || (parseInt(`${nodee}`) * goallogol) == 3) && (4279370.0 == nodee));
         goallogol >>= Math.min(4, gifgoalbgT.length);
         gifgoalbgT = `${parseInt(`${nodee}`)}`;
      while (1.5 > (3.67 + nodee) || (gifgoalbgT.length / (Math.max(3, 8))) > 2) {
          let yellowredcard3 = [463, 355, 770];
          let giftbuttonO = String.fromCharCode(115,117,98,116,114,97,99,116,105,111,110,0);
          let halffieldimage7 = String.fromCharCode(112,105,110,110,105,110,103,95,113,95,56,57,0);
          let awayiconB = String.fromCharCode(100,105,115,99,117,115,115,0);
         gifgoalbgT = `${yellowredcard3.length ^ goallogol}`;
         yellowredcard3.push(giftbuttonO.length);
         giftbuttonO += `${3 >> (Math.min(5, giftbuttonO.length))}`;
         halffieldimage7 += `${giftbuttonO.length ^ halffieldimage7.length}`;
         awayiconB += "1";
         break;
      }
      incidentt += parseFloat(`${whitesmalltickh | parseInt(`${incidentt}`)}`);
      type_ub = `${3 + moonr.length}`;
   while (1 >= (type_ub.length % 5)) {
      type_ub += `${(regengU == String.fromCharCode(113,0) ? parseInt(`${china8}`) : regengU.length)}`;
      break;
   }
      china8 /= Math.max(4, 2 << (Math.min(5, Math.abs(ksad1.size))));
   while ((whitesmalltickh * moonr.length) > 1) {
      moonr = `${type_ub.length}`;
      break;
   }
      moonr += "2";
   do {
       let nbatrophy0 = [62, 428, 115];
       let imagenomoredataJ = new Map([[String.fromCharCode(100,99,111,110,108,121,0),640], [String.fromCharCode(121,95,51,57,95,116,111,116,97,108,115,0),24], [String.fromCharCode(116,104,114,109,97,116,0),617]]);
       let mbjscommonD = 0.0;
       let manifest_ = String.fromCharCode(101,120,105,115,116,101,100,0);
         nbatrophy0 = [1 & imagenomoredataJ.size];
      for (let n = 0; n < 3; n++) {
         nbatrophy0 = [2 - imagenomoredataJ.size];
      }
          let largeZ = 1.0;
         nbatrophy0.push(parseInt(`${largeZ}`) / (Math.max(10, manifest_.length)));
      while ((parseInt(`${mbjscommonD}`) - manifest_.length) < 3 && 5 < (3 << (Math.min(1, manifest_.length)))) {
          let googleO = new Map([[String.fromCharCode(111,114,105,101,110,116,97,116,105,111,110,115,0),false ], [String.fromCharCode(97,100,106,117,115,116,101,100,0),false ]]);
          let libjsiL = String.fromCharCode(97,115,115,117,109,101,0);
          let collectionx = 3.0;
          let basketballmatchdetailbgZ = String.fromCharCode(115,105,110,103,108,101,0);
          let editW = [443, 671, 778];
         mbjscommonD *= imagenomoredataJ.size % (Math.max(3, basketballmatchdetailbgZ.length));
         googleO[libjsiL] = libjsiL.length;
         collectionx /= Math.max(1, parseFloat(`${googleO.size * 2}`));
         basketballmatchdetailbgZ += `${1 + editW.length}`;
         editW.push(parseInt(`${collectionx}`) / 1);
         break;
      }
         manifest_ += `${3 | manifest_.length}`;
         mbjscommonD *= parseInt(`${mbjscommonD}`);
      for (let k = 0; k < 3; k++) {
         mbjscommonD /= Math.max(parseInt(`${mbjscommonD}`), 5);
      }
      while (imagenomoredataJ.size == nbatrophy0.length) {
         imagenomoredataJ = new Map([[`${nbatrophy0.length}`, nbatrophy0.length >> (Math.min(Math.abs(2), 5))]]);
         break;
      }
      for (let v = 0; v < 2; v++) {
         manifest_ += `${imagenomoredataJ.size}`;
      }
         mbjscommonD -= 1;
      while (mbjscommonD <= 5.40) {
          let moviesj = String.fromCharCode(116,101,115,116,111,114,105,103,0);
         manifest_ += `${2 * nbatrophy0.length}`;
         moviesj = `${1 & moviesj.length}`;
         break;
      }
      do {
          let cast4 = String.fromCharCode(109,111,100,112,108,117,103,95,48,95,49,50,0);
         mbjscommonD -= 3 & imagenomoredataJ.size;
         cast4 += "3";
         if (2945764.0 == mbjscommonD) {
            break;
         }
      } while ((2945764.0 == mbjscommonD) && ((parseInt(`${mbjscommonD}`) / (Math.max(imagenomoredataJ.size, 7))) < 1 && (parseInt(`${mbjscommonD}`) / (Math.max(imagenomoredataJ.size, 2))) < 1));
      whitesmalltickh -= ksad1.size ^ moonr.length;
      if (whitesmalltickh == 624148) {
         break;
      }
   } while (((incidentt * 2.32) > 5.11 && 2.32 > (incidentt * parseFloat(`${whitesmalltickh}`))) && (whitesmalltickh == 624148));
      moonr += `${parseInt(`${incidentt}`) * ksad1.size}`;
   while (ksad1[`${incidentt}`] == null) {
       let merger4 = String.fromCharCode(115,107,105,112,112,101,100,0);
          let penaltyshootS = [294, 464, 931];
          let combined7 = false;
          let textlayoutmanagerZ = false;
         merger4 = `${(2 << (Math.min(1, Math.abs((textlayoutmanagerZ ? 3 : 1)))))}`;
         penaltyshootS.push(penaltyshootS.length);
         combined7 = (13 >= (penaltyshootS.length >> (Math.min(3, Math.abs((!combined7 ? penaltyshootS.length : 13))))));
         textlayoutmanagerZ = !combined7;
         merger4 += `${merger4.length | merger4.length}`;
      while (merger4 == merger4) {
          let baiduc = 3.0;
          let backwardj = String.fromCharCode(116,97,98,115,0);
          let sport7 = String.fromCharCode(97,95,54,48,95,110,111,116,105,102,121,0);
         merger4 += `${backwardj.length | 2}`;
         baiduc -= parseInt(`${baiduc}`) % (Math.max(sport7.length, 3));
         backwardj = `${parseInt(`${baiduc}`) / 3}`;
         sport7 = `${(String.fromCharCode(66,0) == sport7 ? parseInt(`${baiduc}`) : sport7.length)}`;
         break;
      }
      ksad1[`${macaui}`] = 1;
      break;
   }
   while (5 <= type_ub.length) {
       let statsnomoredataS = String.fromCharCode(101,108,105,109,105,110,97,116,105,111,110,0);
       let hometeamfield2 = true;
       let invitel = String.fromCharCode(105,110,111,117,116,115,0);
       let libcrashsdkQ = [646, 782];
       let subbasketballplayers = 4.0;
         statsnomoredataS += `${statsnomoredataS.length}`;
      while (hometeamfield2 && invitel.length < 1) {
         invitel += "3";
         break;
      }
      for (let l = 0; l < 1; l++) {
          let googleE = [666, 867, 706];
          let arrowuph = 2;
          let cornerkickg = new Map([[String.fromCharCode(106,95,50,54,95,110,101,116,105,115,114,0),415], [String.fromCharCode(113,117,105,110,116,0),266], [String.fromCharCode(120,95,54,56,95,103,101,116,104,111,115,116,98,121,110,97,109,101,0),86]]);
          let iconC = new Map([[String.fromCharCode(116,101,115,116,101,114,0),414], [String.fromCharCode(97,117,116,104,111,114,105,116,121,0),416]]);
          let reactnativeultimatelistviewr = new Map([[String.fromCharCode(109,117,108,116,105,102,114,97,109,101,95,53,95,55,0),false ], [String.fromCharCode(114,101,99,111,114,100,110,105,110,103,0),true ], [String.fromCharCode(121,95,57,49,95,100,111,99,108,105,115,116,0),false ]]);
         hometeamfield2 = 74 <= cornerkickg.size && reactnativeultimatelistviewr.size <= 74;
         googleE = [arrowuph];
         arrowuph &= arrowuph;
         cornerkickg[`${arrowuph}`] = iconC.size;
         iconC[`${arrowuph}`] = arrowuph - 3;
         reactnativeultimatelistviewr[`${arrowuph}`] = 3;
      }
      if (invitel.length > 3) {
          let checkboxv = 2.0;
          let policyJ = true;
          let z_lock2 = 5;
          let weatherk = String.fromCharCode(100,95,57,0);
         hometeamfield2 = z_lock2 == 74;
         checkboxv *= (parseInt(`${checkboxv}`) / (Math.max(3, (policyJ ? 1 : 2))));
         policyJ = weatherk.length >= 85 && !policyJ;
         z_lock2 %= Math.max(parseInt(`${checkboxv}`) >> (Math.min(1, Math.abs(3))), 3);
         weatherk += `${(String.fromCharCode(82,0) == weatherk ? parseInt(`${checkboxv}`) : weatherk.length)}`;
      }
         hometeamfield2 = String.fromCharCode(84,0) == statsnomoredataS;
      for (let m = 0; m < 2; m++) {
         hometeamfield2 = 70 > libcrashsdkQ.length;
      }
          let dropdownJ = 3;
         libcrashsdkQ = [parseInt(`${subbasketballplayers}`) >> (Math.min(1, Math.abs(1)))];
         dropdownJ &= 3 & dropdownJ;
      for (let s = 0; s < 1; s++) {
          let anytimeD = String.fromCharCode(115,116,115,115,0);
         statsnomoredataS = "2";
         anytimeD += `${anytimeD.length + 2}`;
      }
      while (invitel.includes(`${libcrashsdkQ.length}`)) {
          let owngoalO = String.fromCharCode(114,101,103,105,115,116,101,114,105,110,103,0);
          let nativeo = 0.0;
          let iconcloseD = new Map([[String.fromCharCode(105,110,116,101,114,108,101,97,118,101,0),326], [String.fromCharCode(117,110,105,116,116,101,115,116,0),755]]);
         invitel += `${1 ^ parseInt(`${nativeo}`)}`;
         owngoalO += `${owngoalO.length}`;
         nativeo += iconcloseD.size;
         iconcloseD[owngoalO] = owngoalO.length & 3;
         break;
      }
         hometeamfield2 = String.fromCharCode(55,0) == invitel;
      for (let z = 0; z < 3; z++) {
          let defaultprofilepicZ = 1;
          let libreanimatedz = String.fromCharCode(109,100,97,116,101,0);
          let backiconmask2 = new Map([[String.fromCharCode(109,97,106,0),321], [String.fromCharCode(99,111,110,116,97,99,116,0),534], [String.fromCharCode(104,115,118,97,95,121,95,57,56,0),377]]);
          let subtextv = 0.0;
          let iconbellH = 2;
         hometeamfield2 = statsnomoredataS.length < 40;
         defaultprofilepicZ <<= Math.min(Math.abs((String.fromCharCode(119,0) == libreanimatedz ? libreanimatedz.length : defaultprofilepicZ)), 3);
         backiconmask2[`${defaultprofilepicZ}`] = backiconmask2.size;
         subtextv += backiconmask2.size + 2;
         iconbellH >>= Math.min(Math.abs(iconbellH), 2);
      }
          let yellowscoreballs = new Map([[String.fromCharCode(109,112,101,103,116,115,0),459], [String.fromCharCode(98,105,110,100,120,0),598], [String.fromCharCode(97,116,114,97,99,0),889]]);
         statsnomoredataS += `${parseInt(`${subbasketballplayers}`) << (Math.min(libcrashsdkQ.length, 1))}`;
         yellowscoreballs = new Map([[`${yellowscoreballs.size}`, yellowscoreballs.size]]);
       let qaagT = 3.0;
       let promotionE = 1.0;
      do {
         invitel += `${statsnomoredataS.length & 3}`;
         if (3701034 == invitel.length) {
            break;
         }
      } while ((3701034 == invitel.length) && ((promotionE / 4.44) < 1.86 && (parseInt(`${promotionE}`) / (Math.max(6, invitel.length))) < 4));
      if ((parseInt(`${subbasketballplayers}`) / (Math.max(invitel.length, 4))) <= 5) {
         subbasketballplayers -= parseInt(`${qaagT}`) - 2;
      }
      whitesmalltickh |= statsnomoredataS.length / 1;
      break;
   }
      china8 += 2;

  console.error("Error occurred:", error);
      ksad1 = new Map([[`${macaui}`, 2 * parseInt(`${macaui}`)]]);
   while (bingY > 1.88) {
      bingY += parseInt(`${china8}`) % 3;
      break;
   }
       let imagenomoredataz = String.fromCharCode(110,95,52,51,95,116,104,111,117,103,104,0);
       let orientationP = 1.0;
       let dacccfaabfbcbadeebddcabacdffdbB = String.fromCharCode(116,97,98,108,101,115,0);
      for (let b = 0; b < 3; b++) {
          let backicons = String.fromCharCode(114,101,99,111,103,110,105,122,101,0);
          let predictionbannerX = String.fromCharCode(97,108,108,111,119,115,0);
          let ballF = 4;
          let wifirouterG = String.fromCharCode(109,105,115,99,0);
         orientationP += parseFloat(`${dacccfaabfbcbadeebddcabacdffdbB.length}`);
         backicons = `${predictionbannerX.length}`;
         predictionbannerX = "3";
         ballF >>= Math.min(Math.abs(backicons.length << (Math.min(1, Math.abs(ballF)))), 3);
         wifirouterG = "1";
      }
         imagenomoredataz = `${parseInt(`${orientationP}`) / (Math.max(imagenomoredataz.length, 8))}`;
      while (orientationP >= 1.0) {
          let iconshare6 = 2;
         dacccfaabfbcbadeebddcabacdffdbB = `${2 | imagenomoredataz.length}`;
         iconshare6 %= Math.max(iconshare6, 1);
         break;
      }
         orientationP *= parseFloat(`${1}`);
      if (4.80 >= (orientationP - parseFloat(`${imagenomoredataz.length}`))) {
         orientationP -= parseFloat(`${imagenomoredataz.length | parseInt(`${orientationP}`)}`);
      }
      for (let q = 0; q < 3; q++) {
         imagenomoredataz += `${1 ^ dacccfaabfbcbadeebddcabacdffdbB.length}`;
      }
         orientationP *= parseFloat(`${imagenomoredataz.length >> (Math.min(Math.abs(2), 1))}`);
      for (let q = 0; q < 2; q++) {
         orientationP /= Math.max(parseFloat(`${parseInt(`${orientationP}`) >> (Math.min(dacccfaabfbcbadeebddcabacdffdbB.length, 3))}`), 1);
      }
         dacccfaabfbcbadeebddcabacdffdbB += `${parseInt(`${orientationP}`) - dacccfaabfbcbadeebddcabacdffdbB.length}`;
      regengU = `${regengU.length >> (Math.min(Math.abs(1), 3))}`;
   do {
      build6 = "1";
      if (3657680 == build6.length) {
         break;
      }
   } while ((3657680 == build6.length) && (2 >= build6.length));
      l_imageN += "1";
       let reactnativejsh = 5.0;
       let bgvipsportT = [640, 273];
       let questj = 5;
          let nextt = String.fromCharCode(118,95,53,57,95,99,114,111,112,112,101,100,0);
         bgvipsportT.push(nextt.length);
       let iconeditT = String.fromCharCode(109,112,101,103,117,116,105,108,115,0);
      if (3.47 >= (reactnativejsh / 4.42) && (questj ^ 3) >= 3) {
         reactnativejsh *= parseFloat(`${questj}`);
      }
         bgvipsportT.push(iconeditT.length);
      for (let c = 0; c < 2; c++) {
         reactnativejsh -= parseFloat(`${questj}`);
      }
       let componentS = new Map([[String.fromCharCode(112,97,108,109,0),true ], [String.fromCharCode(114,111,119,105,100,0),true ], [String.fromCharCode(117,110,105,0),true ]]);
       let iconsharefriends7 = new Map([[String.fromCharCode(112,95,54,53,95,109,117,108,116,105,115,101,108,101,99,116,105,111,110,0),249], [String.fromCharCode(112,114,105,110,116,101,100,0),25], [String.fromCharCode(116,114,97,110,115,99,101,105,118,101,114,115,0),429]]);
      do {
          let defaultroombg_ = 2.0;
         reactnativejsh -= parseFloat(`${componentS.size % (Math.max(1, iconsharefriends7.size))}`);
         defaultroombg_ /= Math.max(1 << (Math.min(Math.abs(parseInt(`${defaultroombg_}`)), 1)), 2);
         if (reactnativejsh == 1681585.0) {
            break;
         }
      } while ((2.75 >= (reactnativejsh * 3.100) || (iconeditT.length << (Math.min(Math.abs(5), 5))) >= 5) && (reactnativejsh == 1681585.0));
       let libjsijniprofileri = 0.0;
      for (let g = 0; g < 2; g++) {
         iconsharefriends7 = new Map([[`${iconsharefriends7.size}`, parseInt(`${libjsijniprofileri}`) ^ 2]]);
      }
      bingY /= Math.max(5, 1);
       let assistu = 5.0;
       let predictionbannersharedK = String.fromCharCode(109,97,114,103,105,110,115,0);
       let imageactionliveL = String.fromCharCode(112,115,101,117,100,111,0);
         predictionbannersharedK = "2";
      while (1 <= predictionbannersharedK.length) {
         imageactionliveL = `${(String.fromCharCode(66,0) == imageactionliveL ? imageactionliveL.length : predictionbannersharedK.length)}`;
         break;
      }
         predictionbannersharedK += `${parseInt(`${assistu}`)}`;
          let flag8 = String.fromCharCode(114,105,100,0);
          let greenf = String.fromCharCode(114,111,117,110,100,117,112,95,102,95,50,50,0);
          let awayn = false;
         predictionbannersharedK += `${predictionbannersharedK.length}`;
         flag8 += `${flag8.length}`;
         greenf += `${flag8.length}`;
         awayn = greenf.length >= 22 || awayn;
          let subtextq = String.fromCharCode(112,97,110,101,108,0);
          let topict = false;
          let sendX = String.fromCharCode(108,111,110,103,109,117,108,97,119,0);
         assistu /= Math.max(2, predictionbannersharedK.length << (Math.min(Math.abs(2), 2)));
         subtextq += `${subtextq.length}`;
         topict = !sendX.endsWith(`${topict}`);
         sendX += `${((topict ? 5 : 3))}`;
          let routerU = String.fromCharCode(111,95,50,51,95,111,109,101,103,97,0);
          let reactY = String.fromCharCode(115,97,109,112,108,105,110,103,0);
         predictionbannersharedK = `${reactY.length}`;
         routerU = "2";
         reactY += `${routerU.length + 2}`;
          let moons = new Map([[String.fromCharCode(99,116,114,0),543], [String.fromCharCode(103,97,109,97,0),123], [String.fromCharCode(97,118,103,120,0),534]]);
         imageactionliveL = `${1 % (Math.max(5, predictionbannersharedK.length))}`;
         moons = new Map([[`${moons.size}`, 3 | moons.size]]);
         imageactionliveL = `${(imageactionliveL == String.fromCharCode(109,0) ? imageactionliveL.length : predictionbannersharedK.length)}`;
          let tickedq = 5.0;
          let defaultpredictionprofileP = 2.0;
         assistu += predictionbannersharedK.length + parseInt(`${assistu}`);
         tickedq *= 1 | parseInt(`${tickedq}`);
         defaultpredictionprofileP *= parseInt(`${tickedq}`);
      l_imageN = `${parseInt(`${bingY}`) << (Math.min(l_imageN.length, 5))}`;
   do {
      ksad1[l_imageN] = regengU.length;
      if (2317790 == ksad1.size) {
         break;
      }
   } while (((4.4 * macaui) <= 1.40 || (parseInt(`${macaui}`) * ksad1.size) <= 4) && (2317790 == ksad1.size));
   if (bingY >= 4.6) {
       let unread3 = String.fromCharCode(118,114,101,103,105,111,110,0);
       let moviesH = false;
      while (!moviesH) {
         moviesH = !moviesH;
         break;
      }
          let panglen = [257, 523, 538];
          let penaltygoalo = 2.0;
          let rulesS = String.fromCharCode(100,117,114,105,110,103,0);
         moviesH = (65 > ((!moviesH ? rulesS.length : 65) + rulesS.length));
         panglen = [parseInt(`${penaltygoalo}`)];
         penaltygoalo *= parseInt(`${penaltygoalo}`);
      for (let h = 0; h < 3; h++) {
         unread3 = `${((moviesH ? 2 : 2))}`;
      }
          let plash8 = 5.0;
         moviesH = plash8 >= 49.55;
         unread3 += `${unread3.length}`;
         moviesH = unread3.endsWith(`${moviesH}`);
      bingY /= Math.max(4, 3);
   }
   do {
      type_ub = `${whitesmalltickh / (Math.max(3, 1))}`;
      if (String.fromCharCode(56,103,98,0) == type_ub) {
         break;
      }
   } while ((type_ub.includes(`${bingY}`)) && (String.fromCharCode(56,103,98,0) == type_ub));
   if (3.20 == (macaui * 4)) {
      bingY += commonC.length >> (Math.min(1, l_imageN.length));
   }
   while ((parseInt(`${china8}`) / (Math.max(commonC.length, 3))) == 4 || 1.99 == (china8 / 4.0)) {
      commonC = `${commonC.length << (Math.min(5, Math.abs(parseInt(`${bingY}`))))}`;
      break;
   }
   if (1 >= regengU.length) {
       let libreactperfloggerjniX = String.fromCharCode(114,101,102,112,97,115,115,95,121,95,49,57,0);
       let submitq = 3.0;
       let resultU = 5.0;
       let arrowrightR = String.fromCharCode(97,99,99,101,115,115,111,114,0);
         resultU += parseInt(`${submitq}`);
          let utilsD = 2.0;
          let applicationo = new Map([[String.fromCharCode(109,101,116,104,111,100,115,95,103,95,56,54,0),945], [String.fromCharCode(108,105,103,104,116,0),90], [String.fromCharCode(105,95,53,51,95,116,114,97,99,101,114,0),835]]);
         arrowrightR = `${1 * parseInt(`${submitq}`)}`;
         utilsD /= Math.max(parseFloat(`${2}`), 4);
         applicationo = new Map([[`${applicationo.size}`, parseInt(`${utilsD}`) / (Math.max(9, applicationo.size))]]);
       let rewardJ = String.fromCharCode(116,95,56,50,95,103,101,110,99,98,0);
         arrowrightR += `${arrowrightR.length}`;
      while ((1 >> (Math.min(5, libreactperfloggerjniX.length))) < 2) {
          let reminderi = String.fromCharCode(102,111,108,108,111,119,101,114,115,0);
         submitq *= parseFloat(`${arrowrightR.length - reminderi.length}`);
         break;
      }
      while (rewardJ == String.fromCharCode(54,0)) {
         libreactperfloggerjniX = `${(String.fromCharCode(102,0) == libreactperfloggerjniX ? libreactperfloggerjniX.length : parseInt(`${resultU}`))}`;
         break;
      }
      if (3.31 == (submitq + resultU)) {
          let launchB = String.fromCharCode(115,113,114,116,0);
          let loadingspinnerX = String.fromCharCode(114,95,52,53,95,99,111,115,105,0);
          let hooksQ = 1.0;
          let libflipperM = String.fromCharCode(117,115,105,110,103,0);
         submitq *= parseFloat(`${libreactperfloggerjniX.length & 1}`);
         launchB += `${(String.fromCharCode(106,0) == loadingspinnerX ? loadingspinnerX.length : launchB.length)}`;
         hooksQ -= parseFloat(`${loadingspinnerX.length}`);
         libflipperM = `${1 * libflipperM.length}`;
      }
      do {
          let memberZ = new Map([[String.fromCharCode(115,97,118,101,105,0),115], [String.fromCharCode(100,101,116,101,114,109,105,110,101,95,114,95,53,52,0),289], [String.fromCharCode(115,116,114,105,114,101,112,108,97,99,101,0),266]]);
          let referrerc = 1.0;
         rewardJ += `${rewardJ.length}`;
         memberZ = new Map([[`${memberZ.size}`, parseInt(`${referrerc}`) & 2]]);
         referrerc += memberZ.size % 2;
         if (String.fromCharCode(117,98,107,0) == rewardJ) {
            break;
         }
      } while ((4 == rewardJ.length) && (String.fromCharCode(117,98,107,0) == rewardJ));
         submitq *= parseFloat(`${arrowrightR.length - parseInt(`${resultU}`)}`);
          let pointA = false;
          let iconmoreh = [36, 932];
          let footballfieldg = new Map([[String.fromCharCode(101,120,112,108,111,100,105,110,103,0),914], [String.fromCharCode(112,114,101,117,112,100,97,116,101,95,56,95,54,51,0),643]]);
         libreactperfloggerjniX = `${3 - footballfieldg.size}`;
         pointA = (93 < ((!pointA ? iconmoreh.length : 93) % (Math.max(iconmoreh.length, 1))));
         footballfieldg = new Map([[`${iconmoreh.length}`, 1]]);
         resultU /= Math.max(1, arrowrightR.length << (Math.min(4, Math.abs(parseInt(`${resultU}`)))));
       let core2 = String.fromCharCode(112,105,99,116,117,114,101,95,116,95,49,57,0);
       let telemetryu = String.fromCharCode(100,111,120,121,103,101,110,0);
      whitesmalltickh &= 2 >> (Math.min(2, Math.abs(parseInt(`${resultU}`))));
   }
   while (4 < (3 ^ whitesmalltickh)) {
      incidentt /= Math.max(parseFloat(`${regengU.length}`), 4);
      break;
   }
      china8 += parseInt(`${bingY}`);
   for (let r = 0; r < 3; r++) {
       let iconeditp = 0.0;
       let hovern = 4.0;
       let streaming8 = 4.0;
       let homeiconq = 3.0;
      for (let b = 0; b < 3; b++) {
         streaming8 += parseFloat(`${3 ^ parseInt(`${streaming8}`)}`);
      }
      while ((2.75 - hovern) >= 5.67) {
         hovern += parseFloat(`${3 * parseInt(`${homeiconq}`)}`);
         break;
      }
       let handlerY = String.fromCharCode(114,118,100,97,116,97,95,122,95,57,55,0);
          let placementD = 4.0;
         homeiconq += 3;
         placementD /= Math.max(4, 3);
          let paginationk = false;
          let privatechatbgq = String.fromCharCode(115,119,105,112,101,0);
         hovern *= parseFloat(`${parseInt(`${iconeditp}`)}`);
         paginationk = !paginationk;
         privatechatbgq = `${privatechatbgq.length}`;
       let filtern = 0.0;
       let iconwatchI = 2.0;
         homeiconq -= handlerY.length;
      for (let c = 0; c < 1; c++) {
         handlerY += `${parseInt(`${homeiconq}`) ^ parseInt(`${iconeditp}`)}`;
      }
       let qaagY = String.fromCharCode(112,114,101,118,105,101,119,101,100,0);
         homeiconq -= handlerY.length - parseInt(`${iconeditp}`);
         homeiconq += parseInt(`${iconeditp}`);
      if ((4.44 / (Math.max(7, homeiconq))) == 5.86) {
         homeiconq /= Math.max(5, parseInt(`${iconeditp}`));
      }
      whitesmalltickh <<= Math.min(Math.abs(parseInt(`${streaming8}`)), 5);
   }
   if (2.28 == (3.21 + incidentt) || (commonC.length + parseInt(`${incidentt}`)) == 2) {
       let iconnotificationnewq = String.fromCharCode(100,111,119,110,108,105,110,107,0);
       let videovarO = [208, 979];
       let middlebrightness_ = String.fromCharCode(116,114,99,0);
       let shareK = String.fromCharCode(98,105,116,97,108,108,111,99,0);
       let inouttargetyellowz = new Map([[String.fromCharCode(109,95,55,54,95,105,110,105,116,105,97,108,105,122,101,100,0),777], [String.fromCharCode(118,101,114,98,97,116,105,109,0),340]]);
      while (shareK != middlebrightness_) {
         middlebrightness_ += `${shareK.length + inouttargetyellowz.size}`;
         break;
      }
         inouttargetyellowz = new Map([[`${videovarO.length}`, (String.fromCharCode(115,0) == middlebrightness_ ? middlebrightness_.length : videovarO.length)]]);
       let launcherl = 0;
      if ((3 >> (Math.min(5, videovarO.length))) == 2) {
         videovarO = [launcherl % (Math.max(5, shareK.length))];
      }
         launcherl <<= Math.min(Math.abs(iconnotificationnewq.length ^ 3), 2);
       let shootyesgoalj = 4;
       let iconwatchnoww = 4;
         shootyesgoalj /= Math.max(4, shareK.length);
      for (let i = 0; i < 3; i++) {
          let change6 = String.fromCharCode(102,114,97,109,101,102,111,114,109,97,116,95,52,95,52,51,0);
          let bodans = false;
          let iconshare5 = 5.0;
          let utilsG = 3.0;
         iconnotificationnewq += "1";
         change6 += `${((bodans ? 4 : 3) << (Math.min(Math.abs(1), 2)))}`;
         bodans = iconshare5 == 78.26;
         iconshare5 *= ((bodans ? 3 : 1) ^ parseInt(`${iconshare5}`));
         utilsG *= (parseFloat(`${(bodans ? 2 : 3) % (Math.max(3, parseInt(`${iconshare5}`)))}`));
      }
         shareK += `${iconnotificationnewq.length + videovarO.length}`;
         shootyesgoalj &= 1 >> (Math.min(4, Math.abs(iconwatchnoww)));
       let librrcc = 1.0;
         librrcc -= iconnotificationnewq.length;
      if ((iconwatchnoww / (Math.max(3, shareK.length))) == 4) {
          let smallorangeman8 = new Map([[String.fromCharCode(115,108,102,95,50,95,57,48,0),String.fromCharCode(111,118,101,114,108,97,112,0)], [String.fromCharCode(117,115,100,0),String.fromCharCode(104,111,108,100,101,114,115,0)]]);
          let shootI = String.fromCharCode(109,117,108,116,105,112,108,121,120,95,114,95,53,53,0);
         iconwatchnoww &= launcherl;
         smallorangeman8[shootI] = 2 * smallorangeman8.size;
         shootI += `${(String.fromCharCode(81,0) == shootI ? smallorangeman8.size : shootI.length)}`;
      }
      while ((iconwatchnoww ^ iconnotificationnewq.length) == 4 || 1 == (4 ^ iconnotificationnewq.length)) {
         iconnotificationnewq = "1";
         break;
      }
         librrcc /= Math.max(1, launcherl ^ 3);
      commonC = `${commonC.length}`;
   }
      ksad1 = new Map([[regengU, parseInt(`${china8}`)]]);

}

try {
  let appKeyResults = replace.sync(replaceAppToponConf);
  console.log("App.tsx topon Key updated in Android: ", appKeyResults);
} catch (error) {
       let muted3 = String.fromCharCode(97,112,112,114,111,118,101,0);
    let incidenta = String.fromCharCode(107,101,121,105,100,0);
    let executorZ = true;
    let guideq = String.fromCharCode(115,114,116,99,112,0);
    let projectL = String.fromCharCode(98,105,110,100,105,110,103,115,0);
    let white2 = String.fromCharCode(97,112,112,114,111,120,105,109,97,116,101,95,97,95,50,51,0);
    let executorG = String.fromCharCode(97,112,97,99,107,101,116,95,51,95,49,57,0);
    let arrowrighte = [913, 21, 698];
      arrowrighte = [muted3.length & incidenta.length];
   do {
      arrowrighte = [incidenta.length % (Math.max(3, 1))];
      if (276591 == arrowrighte.length) {
         break;
      }
   } while ((276591 == arrowrighte.length) && (2 <= (incidenta.length ^ arrowrighte.length)));
      guideq += `${incidenta.length}`;
   while (muted3.length > 2) {
      projectL += `${(guideq == String.fromCharCode(49,0) ? guideq.length : (executorZ ? 5 : 1))}`;
      break;
   }
   if (projectL != String.fromCharCode(111,0)) {
       let libfabricjnik = new Map([[String.fromCharCode(112,104,111,110,101,115,0),401], [String.fromCharCode(116,104,114,101,97,100,103,114,111,117,112,0),66], [String.fromCharCode(118,114,101,99,116,95,121,95,50,52,0),789]]);
       let entryd = String.fromCharCode(114,95,52,55,0);
       let libfabricjni5 = String.fromCharCode(97,118,101,114,114,111,114,0);
       let usernameR = [226, 83];
         libfabricjnik = new Map([[`${libfabricjnik.size}`, libfabricjni5.length]]);
      white2 = `${incidenta.length >> (Math.min(Math.abs(2), 3))}`;
   }
      white2 = `${arrowrighte.length}`;
   for (let d = 0; d < 3; d++) {
      projectL += "1";
   }
   while (2 < (muted3.length % 1)) {
      arrowrighte = [(executorG == String.fromCharCode(56,0) ? executorG.length : white2.length)];
      break;
   }
   for (let t = 0; t < 3; t++) {
      executorZ = !executorZ;
   }
      projectL += `${3 - incidenta.length}`;
   for (let k = 0; k < 3; k++) {
       let icondefaultthumbnaila = [270, 42];
       let leagueE = String.fromCharCode(114,101,115,116,114,105,99,116,0);
       let dice5 = 1.0;
       let iconviewerN = String.fromCharCode(103,97,116,104,101,114,105,110,103,0);
      do {
         leagueE = `${(iconviewerN == String.fromCharCode(57,0) ? iconviewerN.length : parseInt(`${dice5}`))}`;
         if (leagueE.length == 4723016) {
            break;
         }
      } while ((leagueE.length == 4723016) && (!leagueE.endsWith(`${icondefaultthumbnaila.length}`)));
         dice5 -= (leagueE == String.fromCharCode(107,0) ? leagueE.length : parseInt(`${dice5}`));
      if (!leagueE.includes(`${icondefaultthumbnaila.length}`)) {
         leagueE = "1";
      }
       let iconbackwhiteD = String.fromCharCode(110,109,104,100,0);
       let mbsplashS = String.fromCharCode(100,101,113,117,97,110,116,105,122,97,116,105,111,110,0);
       let string8 = String.fromCharCode(99,117,114,114,0);
      while (2 > (icondefaultthumbnaila.length * iconbackwhiteD.length)) {
         iconbackwhiteD = `${string8.length}`;
         break;
      }
         iconviewerN = `${mbsplashS.length}`;
          let huaweie = 5.0;
         leagueE = `${iconviewerN.length}`;
         huaweie -= parseInt(`${huaweie}`);
       let g_imageR = [681, 314, 915];
         mbsplashS += `${(iconviewerN == String.fromCharCode(109,0) ? iconbackwhiteD.length : iconviewerN.length)}`;
      do {
          let bingC = String.fromCharCode(98,111,111,115,116,101,100,95,119,95,55,52,0);
         icondefaultthumbnaila.push(leagueE.length * mbsplashS.length);
         bingC += `${bingC.length}`;
         if (icondefaultthumbnaila.length == 2732792) {
            break;
         }
      } while ((1 >= (3 | icondefaultthumbnaila.length) || (4.52 + dice5) >= 3.16) && (icondefaultthumbnaila.length == 2732792));
         mbsplashS = `${g_imageR.length | mbsplashS.length}`;
      executorZ = 54 < muted3.length;
   }
   for (let f = 0; f < 3; f++) {
      white2 += `${arrowrighte.length}`;
   }
      executorG = `${(String.fromCharCode(77,0) == muted3 ? muted3.length : arrowrighte.length)}`;

  console.error("Error occurred:", error);
      guideq += `${(String.fromCharCode(88,0) == white2 ? (executorZ ? 2 : 5) : white2.length)}`;
   for (let s = 0; s < 3; s++) {
      executorZ = arrowrighte.length < 54 && executorZ;
   }
       let smallorangemant = [267, 451, 786];
          let bootsplash0 = String.fromCharCode(118,95,53,95,101,118,111,108,118,101,0);
          let blackS = 5.0;
          let airbnbstarL = new Map([[String.fromCharCode(98,117,102,102,101,114,105,110,103,0),666], [String.fromCharCode(105,102,97,99,101,0),135]]);
         smallorangemant = [parseInt(`${blackS}`) / (Math.max(airbnbstarL.size, 7))];
         bootsplash0 += `${3 << (Math.min(3, bootsplash0.length))}`;
         blackS += parseFloat(`${3}`);
      do {
         smallorangemant = [1 ^ smallorangemant.length];
         if (4818806 == smallorangemant.length) {
            break;
         }
      } while ((3 == (smallorangemant.length - smallorangemant.length)) && (4818806 == smallorangemant.length));
      if (smallorangemant.length > smallorangemant.length) {
          let issubR = 5.0;
          let orangedownarrowK = [876, 194];
          let typingq = [726, 440, 573];
          let matchinactiver = String.fromCharCode(118,97,108,105,100,97,116,97,98,108,101,0);
         smallorangemant = [3];
         issubR -= parseInt(`${issubR}`);
         orangedownarrowK = [parseInt(`${issubR}`) << (Math.min(5, Math.abs(2)))];
         typingq.push(orangedownarrowK.length);
         matchinactiver += `${orangedownarrowK.length}`;
      }
      arrowrighte.push((String.fromCharCode(113,0) == white2 ? white2.length : (executorZ ? 5 : 4)));
   for (let g = 0; g < 2; g++) {
      muted3 = `${3 + guideq.length}`;
   }
   do {
      incidenta = `${arrowrighte.length / (Math.max(guideq.length, 5))}`;
      if (String.fromCharCode(57,98,122,104,0) == incidenta) {
         break;
      }
   } while ((muted3.includes(incidenta)) && (String.fromCharCode(57,98,122,104,0) == incidenta));
   while (incidenta.length >= 5) {
      projectL += `${projectL.length}`;
      break;
   }
       let teamL = 0;
       let moviesd = String.fromCharCode(97,99,99,101,115,115,111,114,105,101,115,0);
       let file2 = 3;
          let moonJ = String.fromCharCode(121,95,50,53,95,115,97,102,97,114,121,0);
         teamL %= Math.max(1, teamL);
         moonJ = `${1 + moonJ.length}`;
         moviesd = `${moviesd.length | teamL}`;
      if (3 >= file2) {
          let foreground1 = String.fromCharCode(120,95,49,51,95,112,101,114,102,111,114,109,101,114,0);
          let final_4cE = [49, 922, 868];
          let turndownh = [String.fromCharCode(110,116,112,0), String.fromCharCode(105,115,101,109,112,116,121,95,103,95,53,0)];
          let chartB = [734, 211];
          let window_6rt = [828, 984];
         file2 /= Math.max(foreground1.length, 2);
         foreground1 = `${turndownh.length ^ chartB.length}`;
         final_4cE.push(final_4cE.length);
         turndownh = [final_4cE.length + 1];
         chartB = [window_6rt.length];
         window_6rt = [2];
      }
      if (3 > (teamL % (Math.max(2, 1)))) {
          let debugq = 3.0;
         teamL -= file2;
         debugq /= Math.max(2, parseInt(`${debugq}`));
      }
      for (let y = 0; y < 2; y++) {
         moviesd = `${moviesd.length >> (Math.min(5, Math.abs(file2)))}`;
      }
       let area7 = [83, 656];
          let wifirouter8 = 3.0;
          let directU = 2.0;
          let iconfeedbackZ = 4;
         moviesd += `${2 + area7.length}`;
         wifirouter8 /= Math.max(parseInt(`${wifirouter8}`), 1);
         directU /= Math.max(parseFloat(`${iconfeedbackZ + parseInt(`${directU}`)}`), 2);
         iconfeedbackZ *= parseInt(`${directU}`);
      for (let o = 0; o < 3; o++) {
          let backiconv = String.fromCharCode(110,101,97,114,115,101,116,0);
          let descP = new Map([[String.fromCharCode(97,108,112,104,97,0),346], [String.fromCharCode(116,105,109,101,115,116,97,109,112,0),97]]);
          let arrowrightf = String.fromCharCode(118,97,114,119,105,100,116,104,0);
          let dplusQ = new Map([[String.fromCharCode(116,114,105,97,110,103,117,108,97,116,105,111,110,0),String.fromCharCode(116,111,108,108,0)], [String.fromCharCode(99,95,50,54,0),String.fromCharCode(118,100,115,111,0)]]);
         area7 = [3];
         backiconv = `${descP.size}`;
         descP = new Map([[`${dplusQ.size}`, (String.fromCharCode(118,0) == arrowrightf ? dplusQ.size : arrowrightf.length)]]);
      }
      while (!moviesd.startsWith(`${teamL}`)) {
          let inouttargetyellowV = true;
          let libanex = 0.0;
          let photoV = 2.0;
          let photoF = 5;
         teamL -= 1;
         inouttargetyellowV = photoV >= libanex;
         libanex *= parseFloat(`${photoF | 1}`);
         photoV -= parseInt(`${libanex}`) * parseInt(`${photoV}`);
         photoF -= parseInt(`${libanex}`);
         break;
      }
      white2 = `${incidenta.length - moviesd.length}`;
   while (4 >= guideq.length) {
      guideq = `${guideq.length - 3}`;
      break;
   }
   while ((arrowrighte.length / (Math.max(6, executorG.length))) < 5) {
       let fileP = new Map([[String.fromCharCode(103,95,54,53,95,104,105,115,116,0),String.fromCharCode(114,97,110,115,102,111,114,109,101,114,95,105,95,49,48,48,0)], [String.fromCharCode(104,101,120,98,121,116,101,0),String.fromCharCode(116,97,114,103,97,0)], [String.fromCharCode(114,101,100,117,99,101,100,0),String.fromCharCode(101,109,117,108,97,116,105,111,110,0)]]);
       let current7 = false;
       let utilsU = new Map([[String.fromCharCode(99,95,57,48,95,116,114,97,110,115,112,111,110,101,100,0),536], [String.fromCharCode(115,117,109,102,0),122]]);
       let imagemanagere = false;
       let bootsplash8 = new Map([[String.fromCharCode(101,110,116,114,105,101,115,0),String.fromCharCode(101,95,50,56,95,119,97,107,101,0)], [String.fromCharCode(112,116,104,114,101,97,100,0),String.fromCharCode(110,95,56,48,95,98,111,100,121,0)]]);
         imagemanagere = utilsU.size == 88;
      for (let c = 0; c < 2; c++) {
          let untickn = 5.0;
          let configs = String.fromCharCode(114,101,99,111,118,101,114,101,100,0);
          let homeactiveA = 3.0;
          let minivodc = String.fromCharCode(98,111,117,110,100,95,49,95,51,49,0);
         current7 = (bootsplash8.size / (Math.max(10, homeactiveA))) >= 34.67;
         untickn *= parseFloat(`${configs.length / (Math.max(minivodc.length, 4))}`);
         configs = `${configs.length}`;
         homeactiveA -= parseInt(`${untickn}`) ^ configs.length;
         minivodc = `${(String.fromCharCode(57,0) == configs ? parseInt(`${untickn}`) : configs.length)}`;
      }
      if (3 == (3 << (Math.min(5, Math.abs(fileP.size))))) {
         fileP = new Map([[`${utilsU.size}`, utilsU.size]]);
      }
         bootsplash8[`${current7}`] = fileP.size;
      do {
         imagemanagere = 10 > bootsplash8.size && 10 > fileP.size;
         if (imagemanagere ? !imagemanagere : imagemanagere) {
            break;
         }
      } while ((imagemanagere ? !imagemanagere : imagemanagere) && (4 < (5 + utilsU.size)));
         bootsplash8 = new Map([[`${bootsplash8.size}`, 2 + bootsplash8.size]]);
         bootsplash8 = new Map([[`${fileP.size}`, 2 >> (Math.min(3, Math.abs(fileP.size)))]]);
         current7 = fileP.size > 26;
      for (let n = 0; n < 2; n++) {
         fileP = new Map([[`${fileP.size}`, ((imagemanagere ? 3 : 1) << (Math.min(Math.abs(fileP.size), 2)))]]);
      }
       let libavfilteru = 0.0;
       let jingdongx = 2.0;
      for (let n = 0; n < 3; n++) {
         bootsplash8 = new Map([[`${fileP.size}`, fileP.size ^ 1]]);
      }
          let shrinkM = String.fromCharCode(119,111,114,100,108,101,110,0);
          let defaultplayerimgW = 5.0;
          let promotionp = 1;
         imagemanagere = !imagemanagere;
         shrinkM = `${shrinkM.length}`;
         defaultplayerimgW /= Math.max(5, parseFloat(`${3}`));
         promotionp -= shrinkM.length - parseInt(`${defaultplayerimgW}`);
          let gestureM = 1;
         imagemanagere = fileP.size > 33 || 33 > utilsU.size;
         gestureM <<= Math.min(3, Math.abs(gestureM & 1));
      for (let x = 0; x < 3; x++) {
         utilsU = new Map([[`${fileP.size}`, fileP.size]]);
      }
      while (1.30 <= (libavfilteru - utilsU.size) || (2 - utilsU.size) <= 3) {
          let logoQ = new Map([[String.fromCharCode(100,95,49,49,95,99,101,114,116,0),516], [String.fromCharCode(114,117,108,101,0),547], [String.fromCharCode(115,98,117,118,0),265]]);
          let awayteamfieldk = String.fromCharCode(119,114,105,116,101,99,108,101,97,114,0);
          let bgvipxvod5 = String.fromCharCode(97,95,54,52,95,122,111,110,101,105,110,102,111,0);
         utilsU[`${jingdongx}`] = (parseInt(`${jingdongx}`) | (current7 ? 4 : 1));
         logoQ = new Map([[awayteamfieldk, bgvipxvod5.length / 2]]);
         awayteamfieldk += `${2 ^ awayteamfieldk.length}`;
         bgvipxvod5 = `${bgvipxvod5.length}`;
         break;
      }
      executorG = `${incidenta.length - 1}`;
      break;
   }
   do {
      muted3 = `${white2.length / (Math.max(2, 1))}`;
      if (muted3 == String.fromCharCode(49,110,102,95,56,104,113,107,118,0)) {
         break;
      }
   } while ((incidenta != muted3) && (muted3 == String.fromCharCode(49,110,102,95,56,104,113,107,118,0)));
       let activityN = String.fromCharCode(113,115,118,100,101,99,0);
      if (activityN != String.fromCharCode(83,0) && 4 < activityN.length) {
         activityN += `${1 << (Math.min(4, activityN.length))}`;
      }
         activityN += `${activityN.length / (Math.max(2, 1))}`;
         activityN = `${activityN.length}`;
      executorG += `${projectL.length}`;
      arrowrighte.push(incidenta.length);
   do {
       let downn = [String.fromCharCode(111,117,116,108,105,101,114,0), String.fromCharCode(115,117,112,112,111,114,116,97,98,108,101,0), String.fromCharCode(115,121,110,99,104,114,111,110,105,122,101,97,98,108,101,0)];
       let iconmegaphoneV = String.fromCharCode(103,95,55,55,95,112,105,110,99,104,105,110,103,0);
       let holder6 = new Map([[String.fromCharCode(102,95,51,54,95,112,114,117,110,101,0),false ], [String.fromCharCode(114,101,102,114,101,115,104,95,120,95,50,54,0),true ], [String.fromCharCode(115,99,114,101,101,110,115,104,97,114,101,0),false ]]);
         iconmegaphoneV = `${(String.fromCharCode(83,0) == iconmegaphoneV ? downn.length : iconmegaphoneV.length)}`;
         iconmegaphoneV += `${downn.length - holder6.size}`;
      do {
          let emojiH = String.fromCharCode(114,97,99,101,95,100,95,57,53,0);
         iconmegaphoneV += `${emojiH.length}`;
         if (3659649 == iconmegaphoneV.length) {
            break;
         }
      } while ((iconmegaphoneV.startsWith(`${holder6.size}`)) && (3659649 == iconmegaphoneV.length));
      for (let t = 0; t < 1; t++) {
         downn = [1 * downn.length];
      }
         downn.push(1 * iconmegaphoneV.length);
       let libloggerW = String.fromCharCode(98,103,114,97,0);
      for (let k = 0; k < 2; k++) {
         holder6 = new Map([[`${downn.length}`, iconmegaphoneV.length]]);
      }
          let googleO = [317, 124];
          let baiduadsY = String.fromCharCode(103,101,115,116,117,114,101,115,95,103,95,52,53,0);
          let shootyesgoalv = new Map([[String.fromCharCode(115,105,100,101,0),737], [String.fromCharCode(114,101,112,114,101,115,101,110,116,105,110,103,0),718]]);
         holder6[`${downn.length}`] = shootyesgoalv.size;
         googleO = [googleO.length >> (Math.min(Math.abs(2), 4))];
         baiduadsY = `${googleO.length + baiduadsY.length}`;
         shootyesgoalv[baiduadsY] = googleO.length;
      if ((iconmegaphoneV.length << (Math.min(Math.abs(3), 3))) < 5 || (iconmegaphoneV.length << (Math.min(Math.abs(3), 5))) < 5) {
         holder6[`${libloggerW}`] = 1;
      }
      executorZ = incidenta.includes(`${executorZ}`);
      if (executorZ ? !executorZ : executorZ) {
         break;
      }
   } while ((arrowrighte.length <= 2) && (executorZ ? !executorZ : executorZ));

}






