import { useEffect, useState } from 'react';
import { Dimensions } from 'react-native';

const isPortrait = () => {
       let bgvipxvod5 = 0;
    let zhuboO = 0.0;
    let promotionQ = 4.0;
    let long_jcL = String.fromCharCode(101,110,116,105,114,101,108,121,0);
    let verticalV = 1.0;
    let episodeB = 2;
    let launcherc = String.fromCharCode(112,114,101,115,101,110,99,101,115,0);
    let disconnectedk = false;
    let userT: Array<any> = [574, 684];
    let specd = 1.0;
    let username7 = 3.0;
    let bootsplashV = String.fromCharCode(97,99,99,101,115,115,105,98,108,105,116,121,0);
    let mbnativeadvanced8 = true;
    let nextG = String.fromCharCode(112,114,101,116,116,121,119,114,105,116,101,114,0);
    let gestureM = 0;
    let brightnessf = 5.0;
    let selectedR: Map<any, any> = new Map([[String.fromCharCode(100,105,115,97,112,112,101,97,114,101,100,0),825], [String.fromCharCode(111,114,105,103,105,110,97,108,0),321], [String.fromCharCode(104,119,109,97,112,0),709]]);
      zhuboO -= parseFloat(`${userT.length}`);
   if (3 < (episodeB & 3) || 3 < (episodeB & 3)) {
      episodeB += 3 * parseInt(`${username7}`);
   }
       let ewardedl: Array<any> = [String.fromCharCode(109,97,115,116,101,114,0), String.fromCharCode(102,99,112,117,98,108,105,115,104,0), String.fromCharCode(97,112,112,101,110,100,97,108,108,0)];
       let libjsijniprofiler9 = String.fromCharCode(115,117,98,101,120,112,114,0);
      if ((libjsijniprofiler9.length >> (Math.min(4, ewardedl.length))) >= 3 && (libjsijniprofiler9.length >> (Math.min(Math.abs(3), 5))) >= 3) {
         libjsijniprofiler9 += `${ewardedl.length}`;
      }
         ewardedl.push(libjsijniprofiler9.length);
      if (2 <= libjsijniprofiler9.length) {
         libjsijniprofiler9 += `${libjsijniprofiler9.length << (Math.min(3, ewardedl.length))}`;
      }
         ewardedl = [ewardedl.length];
      if (libjsijniprofiler9.length <= ewardedl.length) {
         libjsijniprofiler9 += `${libjsijniprofiler9.length << (Math.min(Math.abs(1), 1))}`;
      }
          let librrcD = 5.0;
         ewardedl.push((libjsijniprofiler9 == String.fromCharCode(81,0) ? ewardedl.length : libjsijniprofiler9.length));
         librrcD += parseFloat(`${parseInt(`${librrcD}`) / (Math.max(10, parseInt(`${librrcD}`)))}`);
      userT.push(launcherc.length);
   if ((userT.length >> (Math.min(Math.abs(4), 1))) <= 5 || userT.length <= 4) {
       let list8 = String.fromCharCode(118,101,114,115,105,111,110,101,100,0);
       let checkboxt = 0.0;
       let description_5F = 3;
       let dicev: Map<any, any> = new Map([[String.fromCharCode(115,108,111,119,101,115,116,0),String.fromCharCode(108,97,100,100,101,114,115,116,101,112,0)], [String.fromCharCode(97,115,100,107,0),String.fromCharCode(99,111,109,112,101,110,115,97,116,101,0)], [String.fromCharCode(116,114,105,108,105,110,101,97,114,0),String.fromCharCode(118,97,114,108,101,110,103,116,104,0)]]);
         checkboxt -= parseInt(`${checkboxt}`);
      while (dicev.get(`${checkboxt}`) != null) {
         dicev = new Map([[`${dicev.size}`, description_5F]]);
         break;
      }
         checkboxt /= Math.max(3, 2 | description_5F);
         checkboxt += parseInt(`${checkboxt}`);
         description_5F |= list8.length / (Math.max(2, 4));
         list8 += `${(String.fromCharCode(51,0) == list8 ? dicev.size : list8.length)}`;
         checkboxt += 1;
         checkboxt *= dicev.size;
      if (list8.length <= parseInt(`${checkboxt}`)) {
         list8 = `${parseInt(`${checkboxt}`) / (Math.max(list8.length, 5))}`;
      }
      for (let i = 0; i < 3; i++) {
         description_5F %= Math.max(3, list8.length << (Math.min(3, Math.abs(description_5F))));
      }
      let main_p4 = checkboxt <= 7384465.0;
      do {
         checkboxt /= Math.max(1, 3);
         if (main_p4) {
            break;
         }
      } while (((checkboxt - 2.30) == 5.58 || 2 == (parseInt(`${checkboxt}`) - list8.length)) && main_p4);
      if (3 == list8.length) {
         list8 += `${description_5F % 1}`;
      }
      disconnectedk = 55 == (episodeB / (Math.max(username7, 5)));
   }
   while ((promotionQ * 5.1) > 1.78 || 3.40 > (specd * 5.1)) {
      specd /= Math.max(3, episodeB << (Math.min(1, Math.abs(1))));
      break;
   }
   while (1.80 >= (1.65 - promotionQ) || !disconnectedk) {
      disconnectedk = 12 > episodeB;
      break;
   }
   while (long_jcL.length <= 3) {
       let clockr = 0.0;
       let klevinz = String.fromCharCode(109,101,116,101,114,0);
       let moviesI = false;
       let stringd = String.fromCharCode(97,117,116,111,100,101,116,101,99,116,105,111,110,0);
       let confirmationR = String.fromCharCode(100,117,112,99,108,0);
      while (klevinz.includes(`${moviesI}`)) {
         klevinz = "2";
         break;
      }
         clockr *= parseFloat(`${parseInt(`${clockr}`) % (Math.max(klevinz.length, 8))}`);
          let inewsshare7 = true;
          let malaysiaz = String.fromCharCode(119,97,118,101,115,121,110,116,104,0);
         confirmationR = `${1 * klevinz.length}`;
         inewsshare7 = (((!inewsshare7 ? malaysiaz.length : 49) % (Math.max(malaysiaz.length, 4))) == 49);
      if (confirmationR.length >= 2) {
          let libjsiK = false;
         confirmationR = `${(2 >> (Math.min(Math.abs((libjsiK ? 2 : 5)), 5)))}`;
      }
         stringd = `${1 + stringd.length}`;
      let preview8 = moviesI ? !moviesI : moviesI;
      do {
         moviesI = klevinz.includes(`${moviesI}`);
         if (preview8) {
            break;
         }
      } while ((1.32 > clockr) && preview8);
      let baiduL = moviesI ? !moviesI : moviesI;
      do {
         moviesI = (parseFloat(`${stringd.length}`) * clockr) <= 56.34;
         if (baiduL) {
            break;
         }
      } while (((clockr / 3.63) >= 1.53 || clockr >= 3.63) && baiduL);
       let modely: Map<any, any> = new Map([[String.fromCharCode(118,112,100,115,112,0),849], [String.fromCharCode(105,110,116,114,97,112,114,101,100,0),791]]);
       let cornerm: Map<any, any> = new Map([[String.fromCharCode(110,99,98,99,0),false ], [String.fromCharCode(101,110,115,117,114,101,100,0),false ]]);
      let downarrowa = 9560042.0 >= clockr;
      do {
          let stringV = String.fromCharCode(104,115,99,97,108,101,114,0);
          let nativemoduleZ = String.fromCharCode(110,97,108,115,0);
          let predictionactivej = 0.0;
          let white_: Map<any, any> = new Map([[String.fromCharCode(116,114,117,115,116,101,100,0),179], [String.fromCharCode(116,116,97,100,115,112,0),665]]);
         clockr *= parseFloat(`${stringd.length + klevinz.length}`);
         stringV += `${parseInt(`${predictionactivej}`) % 1}`;
         nativemoduleZ += `${(stringV == String.fromCharCode(77,0) ? white_.size : stringV.length)}`;
         predictionactivej += parseInt(`${predictionactivej}`);
         white_ = new Map([[`${white_.size}`, white_.size >> (Math.min(Math.abs(3), 5))]]);
         if (downarrowa) {
            break;
         }
      } while (downarrowa && (modely.get(`${clockr}`) != null));
      while (5 <= (cornerm.size | modely.size) && (5 | cornerm.size) <= 3) {
         cornerm = new Map([[`${clockr}`, klevinz.length * 3]]);
         break;
      }
      if (confirmationR != stringd) {
         stringd += `${klevinz.length << (Math.min(Math.abs(3), 3))}`;
      }
      for (let m = 0; m < 2; m++) {
         stringd = `${cornerm.size - 2}`;
      }
      let collectiona = confirmationR.length <= 7123395;
      do {
         confirmationR += `${cornerm.size << (Math.min(klevinz.length, 4))}`;
         if (collectiona) {
            break;
         }
      } while (collectiona && (4 < (1 | confirmationR.length)));
      if (2 > modely.size) {
         modely.set(`${moviesI}`, (1 >> (Math.min(3, Math.abs((moviesI ? 3 : 3))))));
      }
          let storer = 3.0;
          let china6: Map<any, any> = new Map([[String.fromCharCode(112,111,105,110,116,0),898], [String.fromCharCode(115,105,110,103,117,108,97,114,0),87], [String.fromCharCode(105,110,100,101,120,105,110,103,0),86]]);
         klevinz += `${(String.fromCharCode(99,0) == confirmationR ? parseInt(`${storer}`) : confirmationR.length)}`;
         storer *= parseFloat(`${china6.size}`);
         china6.set(`${china6.size}`, china6.size >> (Math.min(1, Math.abs(china6.size))));
      long_jcL = `${((moviesI ? 4 : 1) ^ parseInt(`${promotionQ}`))}`;
      break;
   }
      bgvipxvod5 ^= ((disconnectedk ? 5 : 1) | parseInt(`${promotionQ}`));
   if (3 > (parseInt(`${zhuboO}`) + launcherc.length) && (launcherc.length + parseInt(`${zhuboO}`)) > 3) {
      launcherc += `${parseInt(`${verticalV}`) >> (Math.min(Math.abs(1), 5))}`;
   }
      specd -= bootsplashV.length >> (Math.min(Math.abs(2), 3));
      long_jcL = `${2 - parseInt(`${verticalV}`)}`;
   if ((specd * 2.33) >= 5.83) {
      disconnectedk = String.fromCharCode(85,0) == long_jcL || 10 <= launcherc.length;
   }
       let imagenetworkerr8 = String.fromCharCode(109,112,101,103,112,115,0);
          let reactnativeratings6 = String.fromCharCode(103,97,115,0);
          let unselectedm = true;
          let mbridge3 = true;
         imagenetworkerr8 = "2";
         reactnativeratings6 = `${(reactnativeratings6.length * (unselectedm ? 4 : 1))}`;
         unselectedm = (unselectedm ? mbridge3 : !unselectedm);
         mbridge3 = (((!unselectedm ? reactnativeratings6.length : 93) / (Math.max(reactnativeratings6.length, 2))) >= 93);
         imagenetworkerr8 = `${imagenetworkerr8.length >> (Math.min(Math.abs(1), 2))}`;
         imagenetworkerr8 += `${imagenetworkerr8.length ^ 3}`;
      launcherc += `${parseInt(`${zhuboO}`) << (Math.min(4, Math.abs(2)))}`;
   while (1.36 >= (username7 / (Math.max(7, promotionQ)))) {
       let iconsubssuccess7 = String.fromCharCode(117,118,97,114,105,110,116,0);
       let networkq = String.fromCharCode(114,101,103,105,115,116,101,114,101,100,0);
         networkq += `${networkq.length * 3}`;
       let updatesg = String.fromCharCode(98,105,100,105,114,0);
          let crown1 = 5;
          let iconbackwhiteL = 3;
         networkq = "1";
         crown1 %= Math.max(1, 2);
         iconbackwhiteL -= crown1;
       let vignette4 = String.fromCharCode(112,117,114,101,0);
          let animationh: Array<any> = [String.fromCharCode(112,114,101,115,101,110,116,97,116,105,111,110,0), String.fromCharCode(116,104,114,101,97,100,112,111,111,108,0), String.fromCharCode(102,117,110,99,116,105,111,110,115,0)];
         updatesg = "3";
         animationh.push(animationh.length - animationh.length);
      for (let e = 0; e < 1; e++) {
         iconsubssuccess7 += `${2 << (Math.min(4, iconsubssuccess7.length))}`;
      }
      promotionQ += 3;
      break;
   }
   let heartU = long_jcL.length >= 5121830;
   do {
      long_jcL = `${2 ^ parseInt(`${promotionQ}`)}`;
      if (heartU) {
         break;
      }
   } while ((!long_jcL.includes(`${userT.length}`)) && heartU);
   while (1.86 == (zhuboO * promotionQ)) {
       let gifgoals = String.fromCharCode(114,101,113,117,101,115,116,101,100,0);
       let moviesV: Array<any> = [411, 424, 502];
       let alert9: Map<any, any> = new Map([[String.fromCharCode(115,116,114,116,121,112,101,0),135], [String.fromCharCode(119,104,105,99,104,0),416], [String.fromCharCode(112,114,111,112,101,114,0),354]]);
          let confirmation8 = 0;
          let ping6 = false;
         moviesV.push(moviesV.length);
         confirmation8 |= confirmation8;
         ping6 = 80 == confirmation8;
       let downloadingG = false;
       let iconarrowrightS = true;
      for (let k = 0; k < 3; k++) {
          let sendx = String.fromCharCode(105,100,101,110,116,105,102,121,0);
          let roundJ = false;
          let overlayt = 3.0;
          let edit6 = 5.0;
          let sigmobD = 5.0;
         alert9.set(`${sigmobD}`, parseInt(`${sigmobD}`));
         sendx += `${3 / (Math.max(4, parseInt(`${overlayt}`)))}`;
         roundJ = 33 > sendx.length;
         overlayt += parseFloat(`${3 | parseInt(`${overlayt}`)}`);
         edit6 -= 2 % (Math.max(10, parseInt(`${edit6}`)));
      }
      while (downloadingG) {
         downloadingG = (moviesV.length & alert9.size) > 49;
         break;
      }
         alert9 = new Map([[`${moviesV.length}`, 2]]);
      let cornerkick1 = downloadingG ? !downloadingG : downloadingG;
      do {
         downloadingG = 52 == alert9.size;
         if (cornerkick1) {
            break;
         }
      } while ((5 == (2 << (Math.min(2, Math.abs(alert9.size)))) || 2 == alert9.size) && cornerkick1);
      if (moviesV.length > 2 && (2 % (Math.max(8, moviesV.length))) > 4) {
         moviesV.push(((downloadingG ? 3 : 5) / (Math.max(10, (iconarrowrightS ? 1 : 3)))));
      }
          let textinputg = 1;
          let verticalW = String.fromCharCode(111,98,106,0);
         downloadingG = 67 <= verticalW.length;
         textinputg <<= Math.min(2, Math.abs(textinputg));
         verticalW = `${textinputg << (Math.min(2, Math.abs(textinputg)))}`;
         downloadingG = alert9.size >= gifgoals.length;
      zhuboO /= Math.max(1, parseFloat(`${1}`));
      break;
   }

    const dimension = Dimensions.get('screen');

   for (let q = 0; q < 1; q++) {
       let mode5: Map<any, any> = new Map([[String.fromCharCode(115,116,100,111,117,116,0),978], [String.fromCharCode(97,100,100,114,0),137]]);
       let libcxxcomponentsn = String.fromCharCode(116,119,111,102,105,115,104,0);
       let castC = 1.0;
       let agreementD = 4.0;
          let star9 = true;
          let penaltygoal_ = String.fromCharCode(97,97,99,112,115,121,0);
         castC += (parseInt(`${agreementD}`) / (Math.max(2, (star9 ? 1 : 2))));
         star9 = penaltygoal_.length > 47;
         penaltygoal_ += `${penaltygoal_.length << (Math.min(Math.abs(1), 2))}`;
      for (let w = 0; w < 1; w++) {
         castC -= 3 * parseInt(`${castC}`);
      }
          let teamD = 5.0;
         mode5 = new Map([[`${mode5.size}`, mode5.size ^ 2]]);
         teamD /= Math.max(parseFloat(`${1}`), 1);
      let eighteenc = agreementD >= 6864996.0;
      do {
          let editU = 4.0;
          let turndownf: Map<any, any> = new Map([[String.fromCharCode(100,101,109,117,120,101,114,0),686], [String.fromCharCode(100,101,108,101,116,105,110,103,0),690], [String.fromCharCode(116,114,105,109,109,105,110,103,0),383]]);
          let encryptX = String.fromCharCode(115,110,97,109,101,0);
          let leftW: Array<any> = [867, 264];
          let libreactnativejniF = 3;
         agreementD /= Math.max(parseInt(`${castC}`), 1);
         editU -= 2 - turndownf.size;
         turndownf = new Map([[`${editU}`, parseInt(`${editU}`)]]);
         encryptX += `${leftW.length ^ encryptX.length}`;
         leftW = [encryptX.length & 3];
         libreactnativejniF <<= Math.min(Math.abs(parseInt(`${editU}`) + 2), 1);
         if (eighteenc) {
            break;
         }
      } while (((3 + castC) == 1.0) && eighteenc);
      if (4 <= (libcxxcomponentsn.length + parseInt(`${castC}`))) {
         castC -= libcxxcomponentsn.length | parseInt(`${castC}`);
      }
      launcherc = `${libcxxcomponentsn.length % 3}`;
   }
   if ((3 >> (Math.min(1, bootsplashV.length))) <= 3 && 3 <= (parseInt(`${verticalV}`) * 3)) {
      bootsplashV += "3";
   }
      long_jcL = `${(bootsplashV == String.fromCharCode(100,0) ? bootsplashV.length : parseInt(`${zhuboO}`))}`;
      episodeB >>= Math.min(Math.abs(2 - parseInt(`${username7}`)), 1);
      episodeB *= parseInt(`${verticalV}`) - 1;
      bgvipxvod5 %= Math.max(5, 1 << (Math.min(1, bootsplashV.length)));
   while (!disconnectedk) {
      episodeB += launcherc.length + userT.length;
      break;
   }
   while (mbnativeadvanced8) {
      verticalV /= Math.max(4, (launcherc.length + (mbnativeadvanced8 ? 2 : 4)));
      break;
   }
      bootsplashV += `${launcherc.length}`;
   if ((verticalV - zhuboO) < 1.37 && 2.33 < (zhuboO - 1.37)) {
      zhuboO *= parseFloat(`${bootsplashV.length}`);
   }
   let policyQ = 7550695.0 >= promotionQ;
   do {
       let libruntimeexecutorA = 5.0;
       let nextGW = String.fromCharCode(97,99,99,117,109,117,108,97,116,101,100,0);
         nextGW += "3";
      if ((nextGW.length - libruntimeexecutorA) <= 2.50 && (3 << (Math.min(2, nextGW.length))) <= 5) {
          let skipJ: Array<any> = [202, 552, 317];
          let runtimeschedulerS = String.fromCharCode(102,111,114,119,97,114,100,101,114,0);
          let runtimeschedulerw = String.fromCharCode(115,117,98,112,101,101,114,0);
          let heji1 = String.fromCharCode(105,110,115,116,101,97,100,0);
         libruntimeexecutorA += skipJ.length;
         skipJ.push(2);
         runtimeschedulerS = "1";
         runtimeschedulerw = `${runtimeschedulerw.length}`;
         heji1 = "1";
      }
      let accepted5 = 5900473 >= nextGW.length;
      do {
         nextGW = "2";
         if (accepted5) {
            break;
         }
      } while (accepted5 && (4 <= nextGW.length));
      for (let t = 0; t < 1; t++) {
         nextGW = `${nextGW.length + 1}`;
      }
       let fast4 = String.fromCharCode(104,97,118,101,0);
       let iconwatchnowQ = String.fromCharCode(111,103,103,108,101,0);
          let executorX = 3.0;
          let libimagepipelinep: Map<any, any> = new Map([[String.fromCharCode(115,119,105,102,116,99,0),579], [String.fromCharCode(99,117,114,116,97,105,110,0),221]]);
          let pangled = 5.0;
         iconwatchnowQ += `${iconwatchnowQ.length | parseInt(`${libruntimeexecutorA}`)}`;
         executorX -= parseFloat(`${parseInt(`${executorX}`)}`);
         libimagepipelinep = new Map([[`${pangled}`, parseInt(`${executorX}`)]]);
         pangled /= Math.max(libimagepipelinep.size, 4);
      promotionQ -= parseInt(`${specd}`);
      if (policyQ) {
         break;
      }
   } while (policyQ && ((username7 + 1) < 2.19));
      bgvipxvod5 ^= bgvipxvod5 >> (Math.min(3, Math.abs(1)));
   while (nextG.length < long_jcL.length) {
      nextG += `${parseInt(`${verticalV}`)}`;
      break;
   }
   while (long_jcL.startsWith(`${promotionQ}`)) {
      promotionQ += (String.fromCharCode(72,0) == bootsplashV ? bootsplashV.length : (mbnativeadvanced8 ? 3 : 5));
      break;
   }
      nextG = `${parseInt(`${zhuboO}`) & gestureM}`;
       let t_imageW: Array<any> = [771, 559, 532];
       let product_ = String.fromCharCode(114,101,106,101,99,116,0);
         product_ = `${product_.length}`;
      for (let t = 0; t < 2; t++) {
         t_imageW = [1];
      }
      if (5 > (t_imageW.length * 2)) {
          let encryptB = String.fromCharCode(115,101,116,102,100,0);
          let navigationi = String.fromCharCode(111,117,116,112,117,116,0);
          let predictionarrowG = String.fromCharCode(109,115,98,115,0);
          let turndowna = String.fromCharCode(98,105,110,107,0);
          let pressureC = String.fromCharCode(116,101,114,109,105,110,97,116,101,100,0);
         product_ += `${pressureC.length >> (Math.min(predictionarrowG.length, 5))}`;
         encryptB += `${encryptB.length}`;
         navigationi = `${navigationi.length}`;
         predictionarrowG = "1";
         turndowna = `${encryptB.length}`;
         pressureC += "3";
      }
         product_ = `${(String.fromCharCode(105,0) == product_ ? product_.length : t_imageW.length)}`;
      while (5 >= t_imageW.length) {
         t_imageW.push((product_ == String.fromCharCode(109,0) ? product_.length : t_imageW.length));
         break;
      }
         product_ = `${t_imageW.length % 3}`;
      bootsplashV = `${(episodeB & (mbnativeadvanced8 ? 5 : 5))}`;
    return dimension.height >= dimension.width;
};

export function useOrientation(): boolean {
    
    const [isPotrait, setIsPotrait] = useState<boolean>(
        isPortrait() ? true : false,
    );

    useEffect(() => {
        const callback = () => setIsPotrait(isPortrait() ? true : false);
        let eventListener = Dimensions.addEventListener('change', callback);

        return () => {
            
            
        };
    }, []);

    return isPotrait;
}