import { useEffect, useState } from 'react';
import { Dimensions } from 'react-native';

const isPortrait = () => {
       let node1 = String.fromCharCode(115,116,97,116,105,111,110,97,114,121,95,115,95,54,53,0);
    let abidetectX = String.fromCharCode(115,116,111,114,101,102,114,97,109,101,95,110,95,54,56,0);
    let lang6 = 1;
    let sansO = String.fromCharCode(108,97,118,117,95,51,95,55,55,0);
    let tooltipsI = 4.0;
    let twitteru: Map<any, any> = new Map([[String.fromCharCode(109,95,56,54,95,116,97,112,101,115,0),619], [String.fromCharCode(102,111,114,119,97,114,100,115,95,121,95,55,52,0),270]]);
    let libimagepipelineI = 0;
    let imagenetworkerrw = String.fromCharCode(103,95,56,95,99,101,110,99,0);
    let whiteanimationliveV: Map<any, any> = new Map([[String.fromCharCode(99,95,51,48,95,115,118,101,99,116,111,114,0),String.fromCharCode(98,108,101,110,100,109,111,100,101,95,100,95,52,50,0)], [String.fromCharCode(117,110,100,101,114,115,99,111,114,101,95,103,95,51,56,0),String.fromCharCode(105,95,48,95,109,97,114,115,104,97,108,0)], [String.fromCharCode(102,98,100,101,118,95,110,95,52,50,0),String.fromCharCode(121,95,56,95,116,105,109,101,122,111,110,101,0)]]);
    let appleC = String.fromCharCode(101,95,52,51,95,115,104,97,100,101,114,0);
    let reactnativeratings0 = String.fromCharCode(101,95,56,48,95,105,99,119,114,115,105,0);
    let helper_ = 5;
    let networky = 4;
    let downe = 5;
   while (reactnativeratings0.startsWith(`${libimagepipelineI}`)) {
      reactnativeratings0 = `${imagenetworkerrw.length}`;
      break;
   }
   if (abidetectX.length <= 4) {
      abidetectX += `${libimagepipelineI - 1}`;
   }
       let j_imageD = String.fromCharCode(116,95,52,57,95,101,120,116,101,116,110,100,101,100,0);
       let libloggerN = String.fromCharCode(114,119,103,116,95,56,95,54,49,0);
       let backiconmaskK: Array<any> = [845, 902];
      let register_lg = 9453311 <= backiconmaskK.length;
      do {
         backiconmaskK = [1];
         if (register_lg) {
            break;
         }
      } while (register_lg && ((backiconmaskK.length << (Math.min(j_imageD.length, 2))) == 1));
      if (4 < libloggerN.length) {
          let valuesy = 1.0;
          let readK = String.fromCharCode(97,100,109,105,110,115,95,102,95,51,53,0);
          let smallsoundj = 4;
         j_imageD += `${libloggerN.length}`;
         valuesy *= smallsoundj * readK.length;
         readK += `${parseInt(`${valuesy}`)}`;
         smallsoundj -= smallsoundj + 2;
      }
      while (1 <= (2 * backiconmaskK.length) || 4 <= (j_imageD.length * 2)) {
          let iconbellactivey = String.fromCharCode(114,101,97,100,105,110,103,95,99,95,53,48,0);
          let baseD: Map<any, any> = new Map([[String.fromCharCode(109,95,56,52,95,109,101,109,106,114,110,108,0),String.fromCharCode(100,111,110,110,97,95,115,95,57,50,0)], [String.fromCharCode(107,95,57,56,95,102,111,114,107,0),String.fromCharCode(113,117,97,110,116,120,95,117,95,57,48,0)]]);
         backiconmaskK = [iconbellactivey.length];
         iconbellactivey = `${baseD.size - baseD.size}`;
         break;
      }
      while (2 == (2 >> (Math.min(4, libloggerN.length)))) {
         backiconmaskK = [1];
         break;
      }
      for (let o = 0; o < 2; o++) {
         backiconmaskK.push((String.fromCharCode(111,0) == libloggerN ? libloggerN.length : backiconmaskK.length));
      }
         backiconmaskK.push(backiconmaskK.length << (Math.min(libloggerN.length, 1)));
      if (libloggerN != String.fromCharCode(117,0) && j_imageD == String.fromCharCode(106,0)) {
         j_imageD += "1";
      }
         j_imageD = `${1 + backiconmaskK.length}`;
         backiconmaskK = [(libloggerN == String.fromCharCode(78,0) ? backiconmaskK.length : libloggerN.length)];
      tooltipsI /= Math.max(4, parseFloat(`${reactnativeratings0.length + backiconmaskK.length}`));
      libimagepipelineI -= imagenetworkerrw.length;
      tooltipsI *= parseFloat(`${abidetectX.length}`);
   let iconcurrentmatchP = 8074603 <= appleC.length;
   do {
      appleC = "2";
      if (iconcurrentmatchP) {
         break;
      }
   } while (iconcurrentmatchP && (sansO != String.fromCharCode(81,0)));
       let goallogo8: Array<any> = [934, 819];
       let runtimev = String.fromCharCode(118,95,53,53,95,114,112,99,0);
       let smallsoundb: Map<any, any> = new Map([[String.fromCharCode(98,95,52,50,95,109,105,100,105,0),479], [String.fromCharCode(101,95,50,50,95,104,111,114,105,122,111,110,116,97,108,0),385], [String.fromCharCode(97,112,115,95,102,95,56,54,0),907]]);
       let reddownarrowL: Map<any, any> = new Map([[String.fromCharCode(114,101,116,117,114,110,101,100,95,55,95,54,57,0),true ], [String.fromCharCode(113,95,49,57,95,98,117,110,100,108,101,0),true ], [String.fromCharCode(109,101,115,115,97,103,101,115,95,122,95,53,57,0),false ]]);
      for (let a = 0; a < 3; a++) {
         runtimev = "3";
      }
      let iconpipshrinka = runtimev.length >= 7054243;
      do {
         runtimev = `${runtimev.length / (Math.max(5, goallogo8.length))}`;
         if (iconpipshrinka) {
            break;
         }
      } while ((runtimev.startsWith(`${reddownarrowL.size}`)) && iconpipshrinka);
         smallsoundb.set(`${runtimev}`, runtimev.length);
      while ((goallogo8.length >> (Math.min(Math.abs(smallsoundb.size), 5))) == 2 || 2 == (smallsoundb.size >> (Math.min(2, goallogo8.length)))) {
         smallsoundb.set(runtimev, runtimev.length / (Math.max(5, smallsoundb.size)));
         break;
      }
      let orangeclocks = goallogo8.length >= 6759466;
      do {
         goallogo8 = [goallogo8.length];
         if (orangeclocks) {
            break;
         }
      } while (orangeclocks && (3 <= (smallsoundb.size % (Math.max(1, goallogo8.length))) && (3 % (Math.max(8, goallogo8.length))) <= 3));
      abidetectX += `${runtimev.length | sansO.length}`;
   while ((tooltipsI * 1.3) > 3.52 && (1 | twitteru.size) > 5) {
      twitteru = new Map([[appleC, (appleC == String.fromCharCode(52,0) ? parseInt(`${tooltipsI}`) : appleC.length)]]);
      break;
   }
      lang6 *= whiteanimationliveV.size & twitteru.size;
      abidetectX += `${sansO.length}`;
      appleC = `${reactnativeratings0.length}`;
   for (let i = 0; i < 2; i++) {
      sansO = `${(String.fromCharCode(53,0) == sansO ? imagenetworkerrw.length : sansO.length)}`;
   }
   for (let g = 0; g < 2; g++) {
       let activityU = String.fromCharCode(102,95,49,49,95,115,115,108,114,111,111,116,115,0);
       let contextN = String.fromCharCode(111,95,49,52,95,105,110,100,101,111,0);
       let matchactiveo = String.fromCharCode(102,95,54,54,0);
       let toponx = String.fromCharCode(105,110,118,105,116,97,116,105,111,110,95,102,95,52,52,0);
       let homeplayerA = true;
       let bannerN = false;
       let pathe = true;
       let yellowvideoliveH = true;
      let mbridge_ = 6973280 <= contextN.length;
      do {
          let iconarrowleftF = 2.0;
         contextN += `${1 & contextN.length}`;
         iconarrowleftF -= 3 << (Math.min(Math.abs(parseInt(`${iconarrowleftF}`)), 1));
         if (mbridge_) {
            break;
         }
      } while (mbridge_ && (contextN.includes(`${homeplayerA}`)));
         activityU = `${toponx.length}`;
          let videobufferloadingK = String.fromCharCode(102,95,57,56,95,110,117,108,108,105,102,0);
         contextN += `${toponx.length << (Math.min(Math.abs(2), 2))}`;
         videobufferloadingK += `${(videobufferloadingK == String.fromCharCode(87,0) ? videobufferloadingK.length : videobufferloadingK.length)}`;
         bannerN = (homeplayerA ? yellowvideoliveH : homeplayerA);
      for (let k = 0; k < 2; k++) {
         pathe = contextN.length <= 15;
      }
          let basketballawayteamI = 4;
         yellowvideoliveH = !pathe;
         basketballawayteamI *= basketballawayteamI;
      let regengS = toponx.length >= 5382315;
      do {
         toponx += `${(3 & (homeplayerA ? 1 : 1))}`;
         if (regengS) {
            break;
         }
      } while (regengS && (toponx.length == 3));
      let iconclosewhitewithbgi = homeplayerA ? !homeplayerA : homeplayerA;
      do {
          let ballk = true;
          let armvaK: Array<any> = [49, 955, 689];
         homeplayerA = activityU.length > 38;
         ballk = armvaK.length <= 99 || ballk;
         armvaK = [((ballk ? 4 : 3) / 3)];
         if (iconclosewhitewithbgi) {
            break;
         }
      } while ((pathe) && iconclosewhitewithbgi);
      if (homeplayerA && bannerN) {
          let pausee = 1.0;
          let cornerd = false;
          let gifgoalz: Array<any> = [168, 790];
          let close_: Map<any, any> = new Map([[String.fromCharCode(101,95,54,95,115,109,105,120,0),199], [String.fromCharCode(107,95,49,55,95,98,111,120,101,115,0),841], [String.fromCharCode(111,95,57,51,95,98,97,99,107,105,110,103,0),686]]);
          let libjsijniprofilerN: Map<any, any> = new Map([[String.fromCharCode(115,95,52,56,95,116,111,111,108,98,97,114,0),811], [String.fromCharCode(99,117,101,115,95,109,95,55,56,0),196], [String.fromCharCode(101,95,57,56,95,106,112,101,103,100,119,116,0),826]]);
         homeplayerA = close_.size <= toponx.length;
         pausee /= Math.max(3, (gifgoalz.length - (cornerd ? 2 : 1)));
         cornerd = 67.95 > pausee || cornerd;
         gifgoalz = [gifgoalz.length * 2];
         close_ = new Map([[`${cornerd}`, ((cornerd ? 3 : 5) & parseInt(`${pausee}`))]]);
         libjsijniprofilerN.set(`${gifgoalz.length}`, libjsijniprofilerN.size);
      }
         matchactiveo += `${activityU.length}`;
      imagenetworkerrw = `${lang6}`;
   }
   if (5 > (reactnativeratings0.length << (Math.min(Math.abs(4), 1))) && (reactnativeratings0.length - parseInt(`${tooltipsI}`)) > 4) {
       let statsc = String.fromCharCode(100,105,115,112,97,116,99,104,95,50,95,49,0);
       let assetsq: Array<any> = [String.fromCharCode(116,111,111,108,116,105,112,95,109,95,56,49,0), String.fromCharCode(119,95,51,54,95,99,105,114,99,108,101,100,0)];
       let bodanM = true;
       let forwardb = 3.0;
         statsc = "2";
          let imagemanager6 = String.fromCharCode(109,95,53,53,95,118,97,114,105,97,98,105,108,105,116,121,0);
          let defaultfootballbgd = false;
          let basketballawayteamY = String.fromCharCode(114,101,104,97,115,104,95,107,95,48,0);
         bodanM = defaultfootballbgd && forwardb <= 44.72;
         imagemanager6 = `${basketballawayteamY.length}`;
         defaultfootballbgd = basketballawayteamY == imagemanager6;
      while (4.10 == (forwardb * 2.77)) {
         assetsq.push((String.fromCharCode(82,0) == statsc ? assetsq.length : statsc.length));
         break;
      }
      while (!bodanM) {
         bodanM = !statsc.endsWith(`${forwardb}`);
         break;
      }
       let nalyticsD = String.fromCharCode(101,95,51,55,95,111,112,117,115,0);
      if (forwardb <= 5.19) {
         forwardb *= (parseFloat(`${assetsq.length * (bodanM ? 1 : 3)}`));
      }
         forwardb -= parseFloat(`${statsc.length ^ 1}`);
      for (let c = 0; c < 2; c++) {
          let giflivestreamingd = 4;
         forwardb += parseFloat(`${1 << (Math.min(4, assetsq.length))}`);
         giflivestreamingd -= 3;
      }
      let matchinactive_ = statsc.length >= 5005375;
      do {
         statsc = `${parseInt(`${forwardb}`) | statsc.length}`;
         if (matchinactive_) {
            break;
         }
      } while ((nalyticsD != statsc) && matchinactive_);
      if (statsc.includes(`${forwardb}`)) {
          let stationP = 2.0;
          let orangetickb = 3.0;
          let chartT = 4.0;
          let sheetP = false;
         statsc = `${parseInt(`${stationP}`)}`;
         orangetickb -= 3 * parseInt(`${orangetickb}`);
         chartT /= Math.max(2, parseFloat(`${parseInt(`${chartT}`) | 2}`));
         sheetP = !sheetP;
      }
      if (5 < (assetsq.length | statsc.length)) {
         assetsq = [1 * assetsq.length];
      }
         assetsq = [((bodanM ? 5 : 3) / (Math.max(1, parseInt(`${forwardb}`))))];
      tooltipsI += (parseFloat(`${3 - (bodanM ? 2 : 1)}`));
   }
       let nextd = false;
       let iconplayC = String.fromCharCode(120,95,53,51,95,114,101,99,101,105,118,101,0);
      for (let u = 0; u < 1; u++) {
         iconplayC += `${iconplayC.length}`;
      }
      let yellowscoreballK = nextd ? !nextd : nextd;
      do {
          let topic0: Map<any, any> = new Map([[String.fromCharCode(97,95,54,50,95,100,117,114,103,101,114,107,105,110,103,0),String.fromCharCode(101,95,56,48,95,116,105,110,116,101,100,0)], [String.fromCharCode(115,121,109,98,111,108,105,99,97,116,101,100,95,105,95,54,51,0),String.fromCharCode(103,95,54,51,95,105,110,116,101,114,111,112,0)]]);
          let orangedownarrowG = 0;
          let baselineD = 5.0;
          let greytickt = 4.0;
          let promotion7 = 3;
         nextd = baselineD == 52.62 || 52.62 == greytickt;
         topic0 = new Map([[`${orangedownarrowG}`, 1]]);
         orangedownarrowG += 1 + promotion7;
         baselineD *= orangedownarrowG;
         greytickt += 1 << (Math.min(3, Math.abs(orangedownarrowG)));
         promotion7 /= Math.max(orangedownarrowG ^ topic0.size, 3);
         if (yellowscoreballK) {
            break;
         }
      } while ((iconplayC.length >= 3) && yellowscoreballK);
          let issubr: Map<any, any> = new Map([[String.fromCharCode(115,117,98,108,97,121,101,114,115,95,55,95,49,48,0),776], [String.fromCharCode(98,105,116,101,95,121,95,53,54,0),918], [String.fromCharCode(109,95,55,52,95,100,111,110,110,97,0),961]]);
          let searchk = String.fromCharCode(99,95,53,53,95,112,97,121,108,111,97,100,0);
         iconplayC += `${issubr.size}`;
         issubr.set(searchk, searchk.length % (Math.max(3, searchk.length)));
         iconplayC += `${((nextd ? 1 : 3) % 1)}`;
          let pangles = 1.0;
         iconplayC += `${iconplayC.length}`;
         pangles += parseFloat(`${parseInt(`${pangles}`) - 3}`);
         iconplayC = `${iconplayC.length}`;
      imagenetworkerrw = `${sansO.length}`;
   while (!Array.from(twitteru.keys()).includes(`${tooltipsI}`)) {
      twitteru.set(abidetectX, 3);
      break;
   }
       let uimanagerF = 4;
      if (4 <= uimanagerF) {
         uimanagerF *= 3 / (Math.max(8, uimanagerF));
      }
      let libreactnativeblobh = 5014533 >= uimanagerF;
      do {
         uimanagerF &= uimanagerF / (Math.max(3, 3));
         if (libreactnativeblobh) {
            break;
         }
      } while ((1 >= (4 / (Math.max(2, uimanagerF)))) && libreactnativeblobh);
         uimanagerF ^= uimanagerF;
      imagenetworkerrw = `${node1.length & abidetectX.length}`;

    const dimension = Dimensions.get('screen');

      lang6 ^= 1;
       let iconadslinko = true;
       let constantsc = false;
       let iconsharefriends0 = true;
         iconadslinko = !constantsc;
         constantsc = !iconsharefriends0;
      for (let v = 0; v < 2; v++) {
         iconsharefriends0 = constantsc;
      }
         iconsharefriends0 = !constantsc && !iconsharefriends0;
         constantsc = !iconsharefriends0;
          let libbufferi = 2.0;
         constantsc = !iconsharefriends0 || iconadslinko;
         libbufferi -= parseFloat(`${parseInt(`${libbufferi}`) - parseInt(`${libbufferi}`)}`);
          let o_manager7 = false;
          let suggestionE = String.fromCharCode(107,105,115,115,95,120,95,51,52,0);
         constantsc = !iconadslinko;
         o_manager7 = !o_manager7;
         suggestionE += `${((o_manager7 ? 4 : 4))}`;
      if (iconsharefriends0) {
         constantsc = (iconsharefriends0 ? iconadslinko : iconsharefriends0);
      }
       let graphE = 3.0;
      whiteanimationliveV = new Map([[appleC, sansO.length << (Math.min(Math.abs(1), 5))]]);
   for (let n = 0; n < 3; n++) {
      whiteanimationliveV.set(reactnativeratings0, 1);
   }
   let corex = node1 == String.fromCharCode(118,100,98,50,49,57,53,0);
   do {
      node1 += `${sansO.length}`;
      if (corex) {
         break;
      }
   } while (corex && (node1.length <= 5));
       let ewardedf: Array<any> = [149, 974];
       let emoji4 = 5;
       let predictionbutton2 = 2.0;
      for (let d = 0; d < 2; d++) {
          let dependencyw = String.fromCharCode(114,100,109,117,108,116,95,54,95,55,55,0);
         emoji4 *= ewardedf.length / 1;
         dependencyw += `${dependencyw.length - dependencyw.length}`;
      }
      for (let w = 0; w < 3; w++) {
         emoji4 += 3 & emoji4;
      }
      let videos = ewardedf.length <= 6986692;
      do {
         ewardedf = [2];
         if (videos) {
            break;
         }
      } while (videos && (2 == emoji4));
          let megaphone6 = false;
          let goalS = String.fromCharCode(108,95,50,57,95,109,97,116,99,104,101,0);
          let backb = String.fromCharCode(99,111,110,116,101,120,116,95,48,95,49,57,0);
         predictionbutton2 -= (parseFloat(`${parseInt(`${predictionbutton2}`) - (megaphone6 ? 2 : 5)}`));
         megaphone6 = 90 > backb.length && 90 > goalS.length;
         goalS += `${3 & backb.length}`;
       let encryptorF = true;
       let bannerd = true;
         ewardedf.push(ewardedf.length - 1);
         predictionbutton2 -= parseFloat(`${parseInt(`${predictionbutton2}`) / 1}`);
       let flipperD = String.fromCharCode(105,95,57,54,95,97,115,115,112,111,114,116,0);
      for (let n = 0; n < 2; n++) {
          let giftD = false;
          let selection4 = String.fromCharCode(105,95,56,49,95,102,114,97,109,101,0);
         ewardedf = [(emoji4 - (encryptorF ? 2 : 3))];
         giftD = ((selection4.length / (Math.max(8, (giftD ? selection4.length : 25)))) == 25);
      }
      twitteru = new Map([[`${libimagepipelineI}`, libimagepipelineI]]);
   while (twitteru.size <= 4) {
      reactnativeratings0 += `${2 + reactnativeratings0.length}`;
      break;
   }
   if (3.44 > (3.93 / (Math.max(3, tooltipsI)))) {
      imagenetworkerrw = "3";
   }
      whiteanimationliveV.set(abidetectX, 1);
      imagenetworkerrw += `${2 * abidetectX.length}`;
      lang6 ^= 3 | imagenetworkerrw.length;
      node1 = `${1 * libimagepipelineI}`;
       let backwhiteF: Map<any, any> = new Map([[String.fromCharCode(112,97,108,108,101,116,101,95,116,95,57,51,0),true ], [String.fromCharCode(112,107,112,107,101,121,95,56,95,50,55,0),false ], [String.fromCharCode(97,100,111,112,116,101,100,95,104,95,56,48,0),false ]]);
      for (let w = 0; w < 3; w++) {
          let mapbufferM = 4.0;
          let u_center_ = String.fromCharCode(99,111,109,112,108,101,109,101,110,116,105,110,103,95,108,95,52,48,0);
         backwhiteF.set(`${u_center_}`, 3);
         mapbufferM *= parseFloat(`${1}`);
         u_center_ += `${parseInt(`${mapbufferM}`)}`;
      }
         backwhiteF = new Map([[`${backwhiteF.size}`, backwhiteF.size + backwhiteF.size]]);
      while (backwhiteF.get(`${backwhiteF.size}`) != null) {
          let privatechatbgz = 3;
         backwhiteF = new Map([[`${backwhiteF.size}`, backwhiteF.size]]);
         privatechatbgz |= 2;
         break;
      }
      networky -= sansO.length >> (Math.min(Math.abs(1), 5));
      imagenetworkerrw = `${imagenetworkerrw.length}`;
   while (twitteru.size == 1) {
      helper_ ^= (sansO == String.fromCharCode(114,0) ? twitteru.size : sansO.length);
      break;
   }
      reactnativeratings0 += `${1 ^ appleC.length}`;
      reactnativeratings0 += `${libimagepipelineI >> (Math.min(Math.abs(2), 5))}`;
      reactnativeratings0 += `${sansO.length + helper_}`;
    return dimension.height >= dimension.width;
};

export function useOrientation(): boolean {
    
    const [isPotrait, setIsPotrait] = useState<boolean>(
        isPortrait() ? true : false,
    );

    useEffect(() => {
        const callback = () => setIsPotrait(isPortrait() ? true : false);
        let eventListener = Dimensions.addEventListener('change', callback);

        return () => {
            
            
        };
    }, []);

    return isPotrait;
}