import { useEffect, useState } from 'react';
import { Dimensions } from 'react-native';

const isPortrait = () => {
       let window_8L: Array<any> = [30, 166];
    let reactnativejsH = true;
    let searchbarp = 4;
    let zhuboC = true;
    let k_manager7 = String.fromCharCode(111,95,50,52,95,109,98,101,100,103,101,0);
    let libfbo = String.fromCharCode(99,95,57,54,95,97,98,105,0);
    let rulesT = 3.0;
    let crownk = String.fromCharCode(115,117,98,115,116,105,116,117,116,101,100,95,107,95,51,57,0);
    let sellL = String.fromCharCode(115,95,54,49,95,112,101,114,99,101,112,116,0);
    let temperaturex = 2.0;
       let textinputP: Array<any> = [201, 582];
      if ((5 / (Math.max(2, textinputP.length))) <= 1 || 1 <= (5 / (Math.max(4, textinputP.length)))) {
         textinputP = [textinputP.length];
      }
      if (textinputP.length > 4) {
         textinputP = [textinputP.length + textinputP.length];
      }
      for (let c = 0; c < 2; c++) {
          let security4 = 2.0;
          let long_od6 = 0.0;
          let profileM = String.fromCharCode(107,95,55,55,95,115,108,111,119,109,111,100,101,0);
          let tumbnaily = false;
         textinputP.push(2);
         security4 *= parseInt(`${long_od6}`);
         profileM = `${((tumbnaily ? 4 : 4) ^ profileM.length)}`;
         tumbnaily = 41.14 < long_od6 || tumbnaily;
      }
      searchbarp >>= Math.min(1, Math.abs(3));
   while (zhuboC) {
      zhuboC = 11.39 < rulesT;
      break;
   }
      rulesT += parseFloat(`${window_8L.length}`);
      searchbarp >>= Math.min(Math.abs(searchbarp), 5);
   let twitterT = 9637285 >= libfbo.length;
   do {
      libfbo = `${(k_manager7 == String.fromCharCode(53,0) ? (zhuboC ? 5 : 1) : k_manager7.length)}`;
      if (twitterT) {
         break;
      }
   } while (twitterT && (libfbo.length <= 5));
   for (let k = 0; k < 1; k++) {
       let gpayu = 5.0;
       let dnewsU = String.fromCharCode(113,95,50,52,0);
       let bdxadsdkq = 2.0;
       let formp = String.fromCharCode(116,119,114,112,95,51,95,52,0);
       let turnI = String.fromCharCode(98,103,114,97,95,97,95,51,53,0);
         gpayu -= parseFloat(`${parseInt(`${bdxadsdkq}`)}`);
         dnewsU += `${(String.fromCharCode(72,0) == turnI ? parseInt(`${bdxadsdkq}`) : turnI.length)}`;
          let rewardvideo1 = String.fromCharCode(114,95,55,48,95,111,103,103,118,111,114,98,105,115,0);
         bdxadsdkq += parseFloat(`${parseInt(`${gpayu}`) % (Math.max(turnI.length, 9))}`);
         rewardvideo1 += "2";
         dnewsU = `${(dnewsU == String.fromCharCode(65,0) ? formp.length : dnewsU.length)}`;
          let commonp = String.fromCharCode(99,111,100,101,98,111,111,107,115,95,114,95,51,53,0);
         gpayu /= Math.max(parseFloat(`${parseInt(`${bdxadsdkq}`)}`), 1);
         commonp += `${(String.fromCharCode(105,0) == commonp ? commonp.length : commonp.length)}`;
      if (dnewsU == formp) {
         formp = `${parseInt(`${bdxadsdkq}`)}`;
      }
       let selection3 = String.fromCharCode(101,110,117,109,118,97,108,117,101,95,105,95,51,48,0);
       let filledl = String.fromCharCode(109,107,118,109,117,120,101,114,117,116,105,108,95,113,95,53,56,0);
       let rewardvideoy = 3.0;
         selection3 += `${(String.fromCharCode(53,0) == selection3 ? selection3.length : parseInt(`${bdxadsdkq}`))}`;
       let modelsr: Map<any, any> = new Map([[String.fromCharCode(114,101,99,101,105,118,105,110,103,95,55,95,51,50,0),String.fromCharCode(116,95,49,55,95,109,117,108,116,105,112,108,101,114,115,0)], [String.fromCharCode(117,95,55,48,95,97,99,102,105,108,116,101,114,0),String.fromCharCode(103,97,112,108,101,115,115,95,108,95,49,48,48,0)]]);
         dnewsU = "3";
      while (filledl.startsWith(`${rewardvideoy}`)) {
         rewardvideoy -= 1 | parseInt(`${bdxadsdkq}`);
         break;
      }
         bdxadsdkq -= parseFloat(`${1}`);
       let castE = 2.0;
       let gifth = 0.0;
      while ((parseInt(`${gifth}`) / (Math.max(9, formp.length))) > 1) {
         gifth *= parseFloat(`${parseInt(`${rewardvideoy}`)}`);
         break;
      }
      libfbo = `${k_manager7.length & 2}`;
   }
      rulesT /= Math.max((parseFloat(`${(reactnativejsH ? 1 : 4)}`)), 4);
   if (3 == (window_8L.length * 1) && reactnativejsH) {
       let lightc = false;
       let minivodG = String.fromCharCode(105,110,108,105,110,101,95,122,95,52,55,0);
       let componentk: Map<any, any> = new Map([[String.fromCharCode(106,95,52,48,0),false ], [String.fromCharCode(97,108,119,97,121,115,95,117,95,49,48,48,0),true ], [String.fromCharCode(100,105,115,99,111,110,116,105,103,117,111,117,115,95,55,95,51,57,0),true ]]);
       let singaporeZ = false;
       let encrypts = 5.0;
         componentk = new Map([[`${singaporeZ}`, (parseInt(`${encrypts}`) % (Math.max(4, (singaporeZ ? 2 : 4))))]]);
      let qaagu = 9651578.0 <= encrypts;
      do {
         encrypts /= Math.max(parseFloat(`${componentk.size}`), 5);
         if (qaagu) {
            break;
         }
      } while (qaagu && (2.41 <= encrypts));
      while (minivodG.length == 2 && singaporeZ) {
         singaporeZ = minivodG.includes(`${singaporeZ}`);
         break;
      }
         lightc = !lightc;
       let zhengpianU: Map<any, any> = new Map([[String.fromCharCode(100,101,99,114,101,109,101,110,116,95,54,95,52,50,0),543], [String.fromCharCode(115,112,108,105,116,95,110,95,54,57,0),314], [String.fromCharCode(112,117,108,115,101,97,117,100,105,111,115,121,109,98,111,108,116,97,98,108,101,95,54,95,57,52,0),389]]);
       let material7: Map<any, any> = new Map([[String.fromCharCode(112,114,111,109,111,116,101,95,52,95,50,49,0),String.fromCharCode(115,97,118,101,105,95,119,95,51,0)], [String.fromCharCode(99,104,97,110,103,101,100,104,95,106,95,49,55,0),String.fromCharCode(103,95,54,51,95,102,108,97,115,104,105,110,103,0)]]);
      for (let p = 0; p < 1; p++) {
         material7 = new Map([[`${componentk.size}`, 2]]);
      }
         minivodG = `${((singaporeZ ? 1 : 4) + componentk.size)}`;
      for (let a = 0; a < 1; a++) {
         singaporeZ = zhengpianU.get(`${encrypts}`) == null;
      }
         singaporeZ = 96.44 >= encrypts && 95 >= material7.size;
      for (let h = 0; h < 2; h++) {
         zhengpianU = new Map([[`${material7.size}`, 3]]);
      }
         lightc = singaporeZ;
      for (let n = 0; n < 1; n++) {
          let floating3 = 1.0;
          let plus5 = false;
          let typingl = String.fromCharCode(119,101,98,115,95,102,95,55,50,0);
          let rewardX: Array<any> = [493, 426, 387];
          let reactnativejsq = false;
         singaporeZ = !reactnativejsq;
         floating3 /= Math.max(parseFloat(`${parseInt(`${floating3}`)}`), 5);
         plus5 = (parseInt(`${floating3}`) - typingl.length) >= 15;
         typingl += "2";
         rewardX = [(typingl == String.fromCharCode(86,0) ? rewardX.length : typingl.length)];
      }
      for (let z = 0; z < 3; z++) {
         zhengpianU = new Map([[`${componentk.size}`, ((singaporeZ ? 5 : 5) << (Math.min(Math.abs(2), 1)))]]);
      }
      if (material7.size > 4) {
         material7.set(`${zhengpianU.size}`, zhengpianU.size / (Math.max(2, 1)));
      }
         componentk = new Map([[`${encrypts}`, minivodG.length + 3]]);
      reactnativejsH = k_manager7.length < 79;
   }
   if ((4 | sellL.length) > 3) {
      searchbarp ^= window_8L.length;
   }
       let singlex = String.fromCharCode(109,97,105,110,110,101,116,95,100,95,49,54,0);
       let membershipN = 2;
       let indexP = 1.0;
      while ((membershipN << (Math.min(singlex.length, 4))) >= 5 && (5 << (Math.min(4, singlex.length))) >= 1) {
          let switch_ur = false;
          let hejiJ: Array<any> = [900, 294];
          let disconnected0 = 2.0;
         membershipN *= 3 >> (Math.min(Math.abs(parseInt(`${disconnected0}`)), 2));
         switch_ur = hejiJ.length > 79;
         hejiJ.push(2);
         disconnected0 += parseFloat(`${hejiJ.length}`);
         break;
      }
      while ((2.35 + indexP) > 3.84 && (2.35 + indexP) > 2.91) {
          let gradlew8 = 4.0;
          let referrerQ = 2.0;
         singlex = `${1 % (Math.max(8, parseInt(`${indexP}`)))}`;
         gradlew8 -= parseFloat(`${3 & parseInt(`${referrerQ}`)}`);
         referrerQ /= Math.max(parseFloat(`${parseInt(`${gradlew8}`) << (Math.min(4, Math.abs(3)))}`), 2);
         break;
      }
         membershipN |= parseInt(`${indexP}`);
      while (5 <= (parseInt(`${indexP}`) / (Math.max(singlex.length, 6)))) {
          let schedule4: Map<any, any> = new Map([[String.fromCharCode(104,95,50,54,95,115,111,102,116,116,104,114,101,115,104,0),838], [String.fromCharCode(110,111,116,103,101,116,95,122,95,55,51,0),504]]);
          let libjsijniprofilerp = 4.0;
          let viewerT: Map<any, any> = new Map([[String.fromCharCode(113,117,97,110,116,115,95,106,95,53,51,0),true ], [String.fromCharCode(105,95,51,51,95,115,118,101,99,116,111,114,0),false ], [String.fromCharCode(121,95,54,48,95,109,117,109,98,97,105,0),true ]]);
         singlex = `${singlex.length}`;
         schedule4.set(`${libjsijniprofilerp}`, schedule4.size);
         libjsijniprofilerp /= Math.max(5, viewerT.size / (Math.max(3, 6)));
         viewerT = new Map([[`${viewerT.size}`, schedule4.size]]);
         break;
      }
      let typingf = membershipN <= 5005046;
      do {
         membershipN /= Math.max(membershipN * 3, 2);
         if (typingf) {
            break;
         }
      } while (typingf && ((5 >> (Math.min(1, Math.abs(membershipN)))) < 3));
      for (let r = 0; r < 3; r++) {
          let libcrashsdk_ = 0;
          let brightnessX: Array<any> = [963, 212];
          let controlsT = String.fromCharCode(102,115,121,110,99,95,49,95,52,51,0);
         singlex = `${singlex.length & 2}`;
         libcrashsdk_ += brightnessX.length << (Math.min(1, Math.abs(libcrashsdk_)));
         brightnessX.push(controlsT.length);
         controlsT += `${controlsT.length % (Math.max(9, brightnessX.length))}`;
      }
          let views4 = 5.0;
         membershipN >>= Math.min(Math.abs(parseInt(`${views4}`) & 1), 5);
      let grayk = singlex.length >= 5209721;
      do {
         singlex += "2";
         if (grayk) {
            break;
         }
      } while (grayk && (singlex.startsWith(`${membershipN}`)));
      while (4 == (singlex.length & membershipN)) {
         membershipN |= 3 >> (Math.min(3, Math.abs(parseInt(`${indexP}`))));
         break;
      }
      sellL += `${1 + libfbo.length}`;
   while (libfbo.startsWith(`${window_8L.length}`)) {
      libfbo += `${(String.fromCharCode(56,0) == sellL ? window_8L.length : sellL.length)}`;
      break;
   }
      crownk += `${((zhuboC ? 2 : 3))}`;
   for (let r = 0; r < 1; r++) {
      zhuboC = rulesT <= 10.21 || String.fromCharCode(75,0) == sellL;
   }

    const dimension = Dimensions.get('screen');

   for (let h = 0; h < 1; h++) {
       let acceptedq: Array<any> = [String.fromCharCode(114,95,57,95,102,105,108,116,101,114,103,114,97,112,104,0), String.fromCharCode(117,95,51,52,95,98,114,111,107,101,110,0), String.fromCharCode(97,95,51,55,95,114,101,112,108,97,99,101,109,101,110,116,0)];
       let baiduu = 5;
       let areaO = String.fromCharCode(109,95,50,49,95,116,105,99,107,101,114,115,0);
       let actionG = String.fromCharCode(121,95,49,48,95,112,105,120,101,108,115,120,0);
       let libfabricjnig: Map<any, any> = new Map([[String.fromCharCode(105,109,112,108,105,99,105,116,101,108,121,95,55,95,55,52,0),61], [String.fromCharCode(99,105,118,105,108,95,102,95,49,50,0),265]]);
         baiduu |= areaO.length;
      while (5 >= (areaO.length % 5) && 3 >= (5 % (Math.max(9, libfabricjnig.size)))) {
         areaO += "2";
         break;
      }
      let anytimeJ = 7014118 <= actionG.length;
      do {
          let libcxxcomponentsp = true;
          let nativemodule5: Map<any, any> = new Map([[String.fromCharCode(98,114,101,103,95,51,95,55,52,0),String.fromCharCode(112,95,53,52,95,115,121,109,111,100,100,0)], [String.fromCharCode(100,95,56,52,95,118,111,114,98,105,115,100,115,112,0),String.fromCharCode(108,111,99,107,115,99,114,101,101,110,95,51,95,57,48,0)], [String.fromCharCode(112,111,121,116,109,95,49,95,54,53,0),String.fromCharCode(98,95,55,54,95,104,97,110,100,115,104,97,107,101,0)]]);
          let watchC: Map<any, any> = new Map([[String.fromCharCode(113,95,56,95,113,117,97,110,116,105,116,121,0),false ], [String.fromCharCode(102,111,108,108,111,119,101,114,95,116,95,57,54,0),true ]]);
          let videojs9 = 2.0;
         actionG = `${parseInt(`${videojs9}`)}`;
         libcxxcomponentsp = (((libcxxcomponentsp ? nativemodule5.size : 46) % (Math.max(6, nativemodule5.size))) >= 6);
         watchC.set(`${libcxxcomponentsp}`, 1 + nativemodule5.size);
         videojs9 -= parseFloat(`${nativemodule5.size}`);
         if (anytimeJ) {
            break;
         }
      } while (((3 * actionG.length) > 2 || 5 > (3 * libfabricjnig.size)) && anytimeJ);
         acceptedq = [(String.fromCharCode(82,0) == areaO ? areaO.length : libfabricjnig.size)];
         libfabricjnig.set(`${baiduu}`, 2);
          let libturbomodulejsijni7: Map<any, any> = new Map([[String.fromCharCode(112,97,99,107,101,116,115,95,104,95,57,53,0),722], [String.fromCharCode(116,104,114,109,97,116,95,105,95,51,54,0),155], [String.fromCharCode(103,97,112,115,95,48,95,55,55,0),986]]);
          let libjsijniprofilerY = 1.0;
          let layoutx = false;
         actionG += `${acceptedq.length}`;
         libturbomodulejsijni7 = new Map([[`${libjsijniprofilerY}`, 2 ^ parseInt(`${libjsijniprofilerY}`)]]);
         layoutx = parseFloat(`${libturbomodulejsijni7.size}`) <= libjsijniprofilerY;
      if (actionG.length > areaO.length) {
          let condensedO = String.fromCharCode(115,116,114,101,97,109,99,111,112,121,95,113,95,49,48,48,0);
          let volumeJ = false;
          let analyticsp: Map<any, any> = new Map([[String.fromCharCode(115,110,100,105,111,95,104,95,52,57,0),952], [String.fromCharCode(112,95,49,53,95,117,116,116,101,114,97,110,99,101,0),295]]);
         actionG = `${(actionG == String.fromCharCode(77,0) ? areaO.length : actionG.length)}`;
         condensedO += `${analyticsp.size}`;
         volumeJ = (97 == ((volumeJ ? 97 : analyticsp.size) >> (Math.min(Math.abs(analyticsp.size), 4))));
      }
       let libffmpegkitX: Array<any> = [835, 434, 217];
         baiduu |= 2;
      let thailand3 = 9318160 >= acceptedq.length;
      do {
         acceptedq.push(libfabricjnig.size);
         if (thailand3) {
            break;
         }
      } while (thailand3 && (2 < (acceptedq.length - 1)));
      while (1 < (libfabricjnig.size >> (Math.min(2, acceptedq.length))) || 1 < (1 >> (Math.min(2, acceptedq.length)))) {
         libfabricjnig.set(`${baiduu}`, libffmpegkitX.length << (Math.min(4, Math.abs(baiduu))));
         break;
      }
         areaO += `${3 >> (Math.min(3, actionG.length))}`;
      if ((acceptedq.length * actionG.length) == 5 || (actionG.length * acceptedq.length) == 5) {
          let verticali = 4.0;
          let layout5 = String.fromCharCode(107,95,55,56,95,100,97,114,107,0);
         actionG += `${layout5.length - 2}`;
         verticali *= parseInt(`${verticali}`);
         layout5 += `${parseInt(`${verticali}`) * parseInt(`${verticali}`)}`;
      }
      while (5 == (areaO.length % 5)) {
         libfabricjnig = new Map([[`${libfabricjnig.size}`, (areaO == String.fromCharCode(53,0) ? areaO.length : libfabricjnig.size)]]);
         break;
      }
          let cnewarchdefaultsb = false;
          let time_u5: Map<any, any> = new Map([[String.fromCharCode(116,95,51,95,102,105,108,101,115,121,115,116,101,109,0),932], [String.fromCharCode(100,95,53,52,95,100,101,97,108,108,111,99,0),718]]);
          let castingk = String.fromCharCode(100,105,118,112,111,119,109,95,122,95,49,50,0);
         areaO += `${(libffmpegkitX.length - (cnewarchdefaultsb ? 5 : 2))}`;
         cnewarchdefaultsb = castingk.length == time_u5.size;
         time_u5.set(`${castingk}`, time_u5.size + 2);
      zhuboC = k_manager7 == areaO;
   }
   for (let e = 0; e < 3; e++) {
       let stepx: Array<any> = [String.fromCharCode(104,95,51,50,95,116,109,112,0), String.fromCharCode(122,95,53,51,95,104,101,97,100,112,104,111,110,101,115,0)];
         stepx.push(2);
         stepx = [stepx.length ^ 2];
      let recommendationF = 9124399 <= stepx.length;
      do {
         stepx.push(stepx.length);
         if (recommendationF) {
            break;
         }
      } while (recommendationF && ((stepx.length ^ stepx.length) <= 4 || (stepx.length ^ stepx.length) <= 4));
      k_manager7 = "3";
   }
   for (let q = 0; q < 1; q++) {
      rulesT /= Math.max(3, parseFloat(`${parseInt(`${rulesT}`) - 1}`));
   }
   for (let b = 0; b < 2; b++) {
      k_manager7 += `${((zhuboC ? 3 : 2))}`;
   }
   if (!reactnativejsH) {
      zhuboC = parseInt(`${rulesT}`) <= libfbo.length;
   }
   while (crownk != String.fromCharCode(73,0) || 3 < sellL.length) {
       let privilegeu: Array<any> = [796, 923];
       let statisticsA = 0.0;
       let untickp = 4;
       let rules4 = String.fromCharCode(118,95,54,54,95,99,111,110,115,111,110,97,110,116,0);
          let bridgez = String.fromCharCode(98,95,57,95,113,99,101,108,112,100,97,116,97,0);
          let trashb = String.fromCharCode(113,95,52,54,95,116,111,112,109,111,115,116,0);
         statisticsA -= parseFloat(`${untickp / (Math.max(rules4.length, 10))}`);
         bridgez += `${bridgez.length}`;
         trashb += `${2 << (Math.min(5, trashb.length))}`;
         rules4 += `${privilegeu.length}`;
      let indexv = rules4.length >= 8178648;
      do {
          let libfbjnin = String.fromCharCode(117,95,52,50,95,100,105,103,101,115,116,115,0);
         rules4 += `${parseInt(`${statisticsA}`)}`;
         libfbjnin += `${libfbjnin.length / 3}`;
         if (indexv) {
            break;
         }
      } while (indexv && ((rules4.length - untickp) > 1 && (untickp - rules4.length) > 1));
         rules4 = `${parseInt(`${statisticsA}`) % (Math.max(3, privilegeu.length))}`;
          let mappingM = 3;
         rules4 += "3";
         mappingM %= Math.max(mappingM, 3);
      if (2.3 <= statisticsA) {
         statisticsA /= Math.max(parseFloat(`${privilegeu.length * 2}`), 4);
      }
      if ((privilegeu.length | 2) < 5 && (privilegeu.length | 2) < 5) {
         privilegeu = [untickp];
      }
       let areaF = 3;
      if ((untickp % 1) > 3 || (1 % (Math.max(3, untickp))) > 2) {
         rules4 += `${untickp}`;
      }
         rules4 += `${2 >> (Math.min(Math.abs(parseInt(`${statisticsA}`)), 4))}`;
      for (let r = 0; r < 3; r++) {
         areaF %= Math.max(4, areaF);
      }
          let recommendationZ = 1;
         privilegeu.push(3);
         recommendationZ -= recommendationZ / (Math.max(8, recommendationZ));
      crownk = `${3 | parseInt(`${rulesT}`)}`;
      break;
   }
       let libsgcoreQ = false;
       let shirt_ = String.fromCharCode(110,95,53,55,95,97,108,105,103,110,0);
       let orientation8: Map<any, any> = new Map([[String.fromCharCode(105,95,50,52,95,99,116,114,108,0),false ], [String.fromCharCode(115,117,98,116,114,97,99,116,111,114,95,55,95,54,57,0),false ], [String.fromCharCode(98,95,54,52,95,112,97,114,115,101,114,0),false ]]);
       let styleu: Map<any, any> = new Map([[String.fromCharCode(109,95,51,54,95,112,115,121,109,111,100,101,108,0),840], [String.fromCharCode(106,95,50,50,95,110,111,99,97,115,101,0),133]]);
      while (4 == (orientation8.size ^ 1)) {
         orientation8.set(`${shirt_}`, orientation8.size >> (Math.min(shirt_.length, 5)));
         break;
      }
      while (2 >= (2 | styleu.size) || (styleu.size | orientation8.size) >= 2) {
          let malaysia2 = String.fromCharCode(120,95,49,55,95,115,101,108,101,99,116,0);
          let userT = String.fromCharCode(115,116,121,108,101,100,95,110,95,50,56,0);
          let matchT = true;
         orientation8 = new Map([[`${styleu.size}`, shirt_.length]]);
         malaysia2 += `${malaysia2.length % 1}`;
         userT += `${((matchT ? 3 : 3) - malaysia2.length)}`;
         matchT = userT.length == 13 || 13 == malaysia2.length;
         break;
      }
          let typingt = String.fromCharCode(118,95,55,51,95,103,114,111,117,112,110,97,109,101,115,0);
          let actionsv = String.fromCharCode(99,111,109,112,108,101,116,101,95,56,95,52,48,0);
         shirt_ = `${orientation8.size & 3}`;
         typingt = `${actionsv.length | 3}`;
         actionsv = `${typingt.length}`;
         orientation8.set(shirt_, orientation8.size);
      for (let s = 0; s < 1; s++) {
          let kuaishoub = 1.0;
         shirt_ = `${1 % (Math.max(parseInt(`${kuaishoub}`), 8))}`;
      }
      k_manager7 = "1";
      libsgcoreQ = (!libsgcoreQ ? !libsgcoreQ : !libsgcoreQ);
   while ((searchbarp & 4) <= 1) {
      searchbarp >>= Math.min(5, Math.abs(1));
      break;
   }
      rulesT += parseFloat(`${3 ^ sellL.length}`);
      sellL = `${(2 & (zhuboC ? 4 : 2))}`;
      crownk = `${3 + window_8L.length}`;
      reactnativejsH = crownk.length < 9;
      temperaturex -= (crownk == String.fromCharCode(98,0) ? (zhuboC ? 4 : 3) : crownk.length);
    return dimension.height >= dimension.width;
};

export function useOrientation(): boolean {
    
    const [isPotrait, setIsPotrait] = useState<boolean>(
        isPortrait() ? true : false,
    );

    useEffect(() => {
        const callback = () => setIsPotrait(isPortrait() ? true : false);
        let eventListener = Dimensions.addEventListener('change', callback);

        return () => {
            
            
        };
    }, []);

    return isPotrait;
}