import { useEffect, useState } from 'react';
import { Dimensions } from 'react-native';

const isPortrait = () => {
       let eventO = true;
    let footballfieldQ: Array<any> = [389, 249];
    let feedback9 = 0;
    let episodeP = String.fromCharCode(99,104,97,110,103,101,115,95,52,95,50,49,0);
    let scorepopsounds = false;
    let statisticsinactiveV = String.fromCharCode(101,115,116,105,109,97,116,111,114,95,54,95,57,53,0);
    let playC = true;
    let successB: Array<any> = [435, 166];
    let applicationy = String.fromCharCode(98,95,50,52,95,98,97,116,99,104,101,100,0);
      successB = [((eventO ? 5 : 1) + statisticsinactiveV.length)];
       let connectionS = String.fromCharCode(115,101,109,97,110,116,105,99,97,108,108,121,95,122,95,49,50,0);
       let predictionwinD = 0;
         connectionS += `${predictionwinD + 3}`;
       let paginationX = String.fromCharCode(113,95,53,95,117,108,116,105,0);
      let inouttargetredo = 5574123 <= predictionwinD;
      do {
          let iconcalendard = String.fromCharCode(106,95,56,57,95,99,116,120,105,100,120,105,110,99,0);
          let mimol: Array<any> = [606, 284];
          let china3: Map<any, any> = new Map([[String.fromCharCode(118,100,114,97,119,104,101,108,112,101,114,95,53,95,57,49,0),312], [String.fromCharCode(115,95,50,48,95,102,117,122,122,101,114,0),19]]);
          let schedulei = String.fromCharCode(121,95,53,51,95,115,98,108,111,103,0);
         predictionwinD |= (paginationX == String.fromCharCode(102,0) ? paginationX.length : connectionS.length);
         iconcalendard += "3";
         mimol = [(schedulei == String.fromCharCode(118,0) ? schedulei.length : china3.size)];
         china3 = new Map([[`${mimol.length}`, iconcalendard.length % (Math.max(1, 5))]]);
         if (inouttargetredo) {
            break;
         }
      } while (inouttargetredo && ((connectionS.length | 4) == 4));
         paginationX += `${connectionS.length / 3}`;
       let iconmoreo: Array<any> = [822, 552, 180];
      if (3 >= (iconmoreo.length * 1) && 2 >= (iconmoreo.length * 1)) {
          let libreactnativejni9 = String.fromCharCode(107,95,53,48,95,99,111,110,116,97,105,110,0);
         connectionS += `${iconmoreo.length}`;
         libreactnativejni9 = `${libreactnativejni9.length}`;
      }
      playC = connectionS.length >= statisticsinactiveV.length;
      feedback9 &= (String.fromCharCode(103,0) == episodeP ? (eventO ? 4 : 1) : episodeP.length);
       let privacy7 = true;
       let bgvipsportr = String.fromCharCode(118,109,110,99,95,107,95,54,55,0);
       let iconshare0: Array<any> = [String.fromCharCode(97,110,105,109,97,116,105,111,110,95,100,95,56,51,0), String.fromCharCode(97,115,116,114,111,110,111,109,105,99,97,108,95,115,95,53,48,0)];
       let closeJ = 4;
       let homeinactiveN = 1;
         closeJ &= 3;
         privacy7 = bgvipsportr.length > 100 && 100 > iconshare0.length;
         homeinactiveN -= (bgvipsportr == String.fromCharCode(70,0) ? bgvipsportr.length : (privacy7 ? 4 : 1));
          let predictionbuttong = 4.0;
         closeJ /= Math.max(homeinactiveN, 3);
         predictionbuttong /= Math.max(5, 1 % (Math.max(parseInt(`${predictionbuttong}`), 9)));
      let castr = bgvipsportr.length <= 5785419;
      do {
          let ecopy_lz_: Array<any> = [158, 316, 172];
         bgvipsportr += "3";
         ecopy_lz_ = [ecopy_lz_.length * 1];
         if (castr) {
            break;
         }
      } while (castr && (1 < bgvipsportr.length));
      while (4 >= (iconshare0.length * 1) && 2 >= (iconshare0.length * 1)) {
         iconshare0 = [iconshare0.length / (Math.max(2, closeJ))];
         break;
      }
       let analytic1 = String.fromCharCode(97,95,55,52,0);
         privacy7 = 5 > closeJ;
      statisticsinactiveV = `${iconshare0.length / 2}`;
      episodeP += `${(3 * (playC ? 1 : 3))}`;
   let static_wZ = 7095000 >= successB.length;
   do {
      successB.push(successB.length);
      if (static_wZ) {
         break;
      }
   } while (static_wZ && (4 > (feedback9 >> (Math.min(successB.length, 4))) || (feedback9 >> (Math.min(Math.abs(4), 4))) > 4));
   let matchest = feedback9 >= 8625313;
   do {
       let iconpointscoreE: Array<any> = [String.fromCharCode(115,112,114,111,112,95,106,95,55,53,0), String.fromCharCode(111,100,101,114,110,95,118,95,51,50,0), String.fromCharCode(109,98,114,116,104,114,101,97,100,95,103,95,51,0)];
       let whistleorangeA = String.fromCharCode(101,95,56,95,97,100,105,100,0);
       let homeactivep: Array<any> = [472, 340, 148];
         iconpointscoreE = [(String.fromCharCode(113,0) == whistleorangeA ? homeactivep.length : whistleorangeA.length)];
      for (let h = 0; h < 2; h++) {
         iconpointscoreE.push(2 / (Math.max(5, iconpointscoreE.length)));
      }
      while (3 <= (1 << (Math.min(3, whistleorangeA.length))) || 5 <= (iconpointscoreE.length << (Math.min(Math.abs(1), 4)))) {
          let libreanimatedr = 3;
          let policyh = String.fromCharCode(111,111,108,116,105,112,95,112,95,56,53,0);
          let matchdetailbgX = true;
          let countdowni: Map<any, any> = new Map([[String.fromCharCode(100,95,57,55,95,97,118,112,105,99,116,117,114,101,0),true ], [String.fromCharCode(121,95,49,53,95,104,101,105,103,104,116,115,0),false ]]);
         whistleorangeA = `${homeactivep.length | 2}`;
         libreanimatedr %= Math.max(1, policyh.length & 1);
         policyh = `${libreanimatedr & policyh.length}`;
         matchdetailbgX = policyh.includes(`${libreanimatedr}`);
         countdowni = new Map([[`${countdowni.size}`, ((matchdetailbgX ? 1 : 1) % (Math.max(countdowni.size, 10)))]]);
         break;
      }
      for (let c = 0; c < 3; c++) {
          let predictionactiveK = String.fromCharCode(101,110,111,117,103,104,95,110,95,56,53,0);
          let libtobp = String.fromCharCode(102,116,118,98,108,97,110,107,95,114,95,49,49,0);
          let emptyN: Map<any, any> = new Map([[String.fromCharCode(99,104,97,110,103,101,115,95,113,95,49,48,48,0),896], [String.fromCharCode(119,104,116,120,95,50,95,54,52,0),170]]);
         whistleorangeA = `${iconpointscoreE.length}`;
         predictionactiveK = `${(String.fromCharCode(88,0) == libtobp ? emptyN.size : libtobp.length)}`;
         emptyN.set(libtobp, emptyN.size ^ 1);
      }
      while (iconpointscoreE.length <= 1) {
          let predictiony = String.fromCharCode(98,97,99,107,105,110,103,95,121,95,56,50,0);
          let trophyC = String.fromCharCode(117,95,57,55,95,117,110,113,117,97,110,116,0);
         homeactivep.push(homeactivep.length & 2);
         predictiony += "1";
         trophyC += "2";
         break;
      }
      let h_image4 = 8608265 <= iconpointscoreE.length;
      do {
         iconpointscoreE = [1];
         if (h_image4) {
            break;
         }
      } while (((homeactivep.length % 4) == 3) && h_image4);
          let with_vo = 3;
          let typing3: Array<any> = [String.fromCharCode(121,95,52,48,95,105,111,101,114,114,0), String.fromCharCode(115,105,110,101,115,95,100,95,53,0)];
          let eact1 = 4.0;
         iconpointscoreE.push(iconpointscoreE.length / (Math.max(6, whistleorangeA.length)));
         with_vo >>= Math.min(Math.abs(1 << (Math.min(Math.abs(parseInt(`${eact1}`)), 1))), 5);
         typing3.push(1);
         eact1 *= parseFloat(`${typing3.length}`);
         whistleorangeA = "3";
          let actionsX = 4.0;
          let shootyesgoalm = 4.0;
         whistleorangeA += `${parseInt(`${shootyesgoalm}`)}`;
         actionsX += parseFloat(`${parseInt(`${actionsX}`)}`);
      feedback9 >>= Math.min(Math.abs((statisticsinactiveV.length | (scorepopsounds ? 5 : 5))), 5);
      if (matchest) {
         break;
      }
   } while ((2 == footballfieldQ.length) && matchest);
   for (let b = 0; b < 2; b++) {
      playC = episodeP.length >= 44;
   }
       let bootsplashJ = 5.0;
         bootsplashJ /= Math.max(1, parseFloat(`${parseInt(`${bootsplashJ}`) % 2}`));
          let frame_j_ = 4.0;
          let analyticsT = String.fromCharCode(117,95,49,95,97,114,114,97,110,103,101,109,101,110,116,0);
         bootsplashJ -= parseFloat(`${analyticsT.length}`);
         frame_j_ += parseFloat(`${parseInt(`${frame_j_}`)}`);
         analyticsT += `${parseInt(`${frame_j_}`) % (Math.max(8, parseInt(`${frame_j_}`)))}`;
      let feedbackO = bootsplashJ >= 8418522.0;
      do {
          let pressureO = String.fromCharCode(114,95,53,48,95,112,97,103,101,115,101,101,107,0);
          let gesturesO = 3;
          let iconwatchnowp = false;
         bootsplashJ *= (parseFloat(`${pressureO == String.fromCharCode(97,0) ? pressureO.length : gesturesO}`));
         gesturesO |= ((iconwatchnowp ? 2 : 1));
         if (feedbackO) {
            break;
         }
      } while (feedbackO && (1.21 >= (bootsplashJ - bootsplashJ) || 1.21 >= (bootsplashJ - bootsplashJ)));
      playC = 73.33 >= bootsplashJ && 11 >= successB.length;
      playC = episodeP.length > 94;
   while (3 <= successB.length || (successB.length / 3) <= 1) {
       let signinupt = 1.0;
       let eighteenN = 1.0;
      let libtobm = signinupt <= 9294290.0;
      do {
         signinupt += parseFloat(`${parseInt(`${eighteenN}`) - parseInt(`${signinupt}`)}`);
         if (libtobm) {
            break;
         }
      } while (((eighteenN / (Math.max(2, 5))) == 5.59) && libtobm);
         eighteenN -= parseFloat(`${parseInt(`${signinupt}`)}`);
      scorepopsounds = scorepopsounds && statisticsinactiveV.length >= 46;
      break;
   }
      statisticsinactiveV += "2";
   while ((5 >> (Math.min(4, footballfieldQ.length))) < 1 || 5 < (footballfieldQ.length >> (Math.min(Math.abs(5), 4)))) {
       let ajaxG = 2;
         ajaxG += 2 | ajaxG;
         ajaxG *= ajaxG >> (Math.min(Math.abs(1), 3));
         ajaxG <<= Math.min(4, Math.abs(ajaxG));
      feedback9 -= 2 + successB.length;
      break;
   }

    const dimension = Dimensions.get('screen');

   for (let n = 0; n < 2; n++) {
       let headerZ = String.fromCharCode(97,95,54,57,95,99,108,111,115,105,110,103,0);
       let container1 = String.fromCharCode(109,97,116,104,101,115,95,117,95,49,57,0);
          let volumeG = String.fromCharCode(104,97,115,120,95,120,95,57,55,0);
          let trophyu = String.fromCharCode(108,95,50,48,95,117,112,108,105,110,107,0);
          let tickedH = String.fromCharCode(100,111,103,95,52,95,54,56,0);
         headerZ = `${2 * trophyu.length}`;
         volumeG = `${tickedH.length / (Math.max(1, 10))}`;
         trophyu = `${tickedH.length}`;
         headerZ = `${(String.fromCharCode(122,0) == headerZ ? container1.length : headerZ.length)}`;
          let countdownd = String.fromCharCode(99,95,56,0);
          let policyi = String.fromCharCode(105,95,57,52,95,100,108,115,121,109,0);
         container1 += `${3 ^ container1.length}`;
         countdownd += `${(String.fromCharCode(70,0) == policyi ? countdownd.length : policyi.length)}`;
       let backwardh = String.fromCharCode(109,95,57,49,95,100,101,99,114,101,109,101,110,116,0);
       let imagenomoredataK = String.fromCharCode(102,95,50,51,95,116,121,112,101,99,111,100,101,0);
         imagenomoredataK += `${container1.length << (Math.min(Math.abs(1), 5))}`;
      for (let o = 0; o < 3; o++) {
         backwardh += `${(container1 == String.fromCharCode(113,0) ? container1.length : backwardh.length)}`;
      }
      eventO = feedback9 >= 38;
   }
       let gpayj = String.fromCharCode(103,95,51,54,95,119,97,108,108,112,97,112,101,114,0);
         gpayj += `${gpayj.length & 2}`;
          let customz = String.fromCharCode(119,105,110,97,114,109,95,49,95,57,51,0);
          let splashp = 3.0;
          let rewindg = String.fromCharCode(110,111,110,115,101,99,117,114,101,95,56,95,51,54,0);
         gpayj = `${2 << (Math.min(1, Math.abs(parseInt(`${splashp}`))))}`;
         customz = `${customz.length | rewindg.length}`;
         splashp *= customz.length;
         rewindg = `${3 >> (Math.min(2, customz.length))}`;
      let time_9l5 = 9694144 <= gpayj.length;
      do {
         gpayj = `${gpayj.length}`;
         if (time_9l5) {
            break;
         }
      } while (time_9l5 && (!gpayj.includes(`${gpayj.length}`)));
      footballfieldQ = [(String.fromCharCode(99,0) == statisticsinactiveV ? feedback9 : statisticsinactiveV.length)];
   for (let r = 0; r < 3; r++) {
      statisticsinactiveV = `${3 | successB.length}`;
   }
   while (eventO) {
      statisticsinactiveV += `${((eventO ? 5 : 3) + footballfieldQ.length)}`;
      break;
   }
   while (!playC) {
      playC = statisticsinactiveV.length == 20 && playC;
      break;
   }
   if (!scorepopsounds && (1 * feedback9) >= 2) {
       let holderZ = true;
       let storep = 5.0;
       let change9: Map<any, any> = new Map([[String.fromCharCode(97,95,54,49,95,98,105,116,120,0),562], [String.fromCharCode(97,118,105,97,108,97,98,108,101,95,55,95,57,0),820]]);
         change9 = new Map([[`${storep}`, (parseInt(`${storep}`) | (holderZ ? 5 : 4))]]);
       let thumbnail6 = String.fromCharCode(105,110,105,116,105,97,108,115,95,115,95,54,54,0);
       let showV = String.fromCharCode(98,97,114,114,101,116,116,95,112,95,57,49,0);
      for (let k = 0; k < 1; k++) {
         storep += parseFloat(`${1 + parseInt(`${storep}`)}`);
      }
          let read_ = 3;
          let crossX = true;
          let malaysiaO = String.fromCharCode(100,101,112,95,50,95,51,52,0);
         storep += parseFloat(`${3 | showV.length}`);
         read_ >>= Math.min(4, parseInt(`${Math.abs(((crossX ? 5 : 1) >> (Math.min(Math.abs(read_), 5))))}`));
         crossX = read_ == 54 || !crossX;
         malaysiaO = "1";
         thumbnail6 = `${thumbnail6.length + 1}`;
         thumbnail6 = "1";
      for (let q = 0; q < 2; q++) {
          let indicatorA: Map<any, any> = new Map([[String.fromCharCode(98,95,49,49,95,115,97,108,116,0),String.fromCharCode(108,95,51,48,95,101,120,116,114,97,0)], [String.fromCharCode(112,95,55,49,95,105,110,116,101,114,97,99,116,0),String.fromCharCode(105,95,54,56,95,104,97,115,0)], [String.fromCharCode(114,95,53,53,95,103,101,110,115,0),String.fromCharCode(106,95,55,48,95,102,111,114,103,111,116,0)]]);
          let disconnectedL = String.fromCharCode(113,95,52,56,95,116,111,117,99,104,101,115,0);
          let iconR: Map<any, any> = new Map([[String.fromCharCode(108,95,56,95,110,101,116,119,111,114,107,105,110,103,0),true ], [String.fromCharCode(119,95,57,48,95,121,117,118,112,0),false ], [String.fromCharCode(97,110,110,111,116,97,116,105,111,110,115,95,54,95,54,55,0),true ]]);
          let dependencies3: Map<any, any> = new Map([[String.fromCharCode(115,98,97,100,95,54,95,55,50,0),false ], [String.fromCharCode(110,95,57,57,0),false ]]);
         holderZ = 33 > showV.length;
         indicatorA.set(`${dependencies3.size}`, dependencies3.size - indicatorA.size);
         disconnectedL += `${iconR.size}`;
         iconR = new Map([[`${iconR.size}`, iconR.size >> (Math.min(disconnectedL.length, 1))]]);
      }
      while (!holderZ) {
          let statsnomoredatab = 5.0;
          let vietnamZ = String.fromCharCode(109,97,110,116,105,115,115,97,115,95,122,95,49,54,0);
          let goalR = 0;
          let latnf = 4.0;
         storep -= parseFloat(`${3}`);
         statsnomoredatab *= parseFloat(`${parseInt(`${statsnomoredatab}`)}`);
         vietnamZ += `${1 + goalR}`;
         goalR *= vietnamZ.length << (Math.min(2, Math.abs(goalR)));
         latnf /= Math.max(1, parseFloat(`${parseInt(`${latnf}`) % (Math.max(2, 3))}`));
         break;
      }
       let checkboxo = 0.0;
      scorepopsounds = footballfieldQ.includes(storep);
   }
       let chartg = 2;
      let turnd = chartg >= 9589024;
      do {
          let bottomb = String.fromCharCode(110,95,56,51,95,116,117,110,110,101,108,0);
          let imagenetworkerre = String.fromCharCode(109,101,116,97,115,111,117,110,100,95,117,95,56,49,0);
          let list8 = String.fromCharCode(119,119,119,95,109,95,52,57,0);
         chartg *= bottomb.length / (Math.max(3, 2));
         bottomb = `${list8.length}`;
         imagenetworkerre = `${imagenetworkerre.length}`;
         list8 = `${imagenetworkerre.length}`;
         if (turnd) {
            break;
         }
      } while (((chartg - chartg) == 5 && (chartg - chartg) == 5) && turnd);
      if (chartg < 3) {
         chartg |= chartg;
      }
          let libimagepipelines = 0;
          let fullscreenminX = String.fromCharCode(107,95,52,51,95,116,114,105,97,108,115,0);
         chartg *= chartg % (Math.max(1, libimagepipelines));
         libimagepipelines <<= Math.min(2, Math.abs(fullscreenminX.length / (Math.max(9, fullscreenminX.length))));
      statisticsinactiveV += "1";
      eventO = footballfieldQ.length < 57 && !eventO;
       let whitetickV = 2;
       let crownF = String.fromCharCode(100,95,50,51,95,97,99,111,110,102,105,103,0);
       let profilepicw = String.fromCharCode(117,110,97,114,99,104,105,118,101,95,111,95,52,51,0);
         whitetickV -= whitetickV;
      while (whitetickV > profilepicw.length) {
         whitetickV -= profilepicw.length | crownF.length;
         break;
      }
      for (let l = 0; l < 3; l++) {
          let fullscreenminu = true;
          let usernamei = 1.0;
          let checkboxC = String.fromCharCode(118,95,52,51,95,102,116,118,99,108,0);
          let uploadA: Array<any> = [String.fromCharCode(105,110,111,100,101,95,101,95,51,55,0), String.fromCharCode(100,110,115,108,97,98,101,108,95,118,95,52,54,0)];
          let stringsZ: Array<any> = [String.fromCharCode(116,95,52,54,95,99,111,110,100,101,110,115,101,100,0), String.fromCharCode(119,95,56,57,95,97,98,115,100,105,102,102,0)];
         profilepicw += `${3 | crownF.length}`;
         fullscreenminu = checkboxC.length >= parseInt(`${usernamei}`);
         usernamei /= Math.max(2 >> (Math.min(Math.abs(parseInt(`${usernamei}`)), 1)), 3);
         checkboxC += `${stringsZ.length / 3}`;
         uploadA = [uploadA.length * checkboxC.length];
         stringsZ.push(((fullscreenminu ? 4 : 1) >> (Math.min(uploadA.length, 3))));
      }
      if (crownF.length == 4) {
          let ruless = false;
          let eacty = 0;
         profilepicw += `${(whitetickV / (Math.max(10, (ruless ? 4 : 5))))}`;
         ruless = 74 >= (eacty >> (Math.min(Math.abs(eacty), 5)));
      }
      for (let q = 0; q < 2; q++) {
         whitetickV &= 2 ^ crownF.length;
      }
         whitetickV ^= 3 - crownF.length;
      while (2 < (whitetickV & crownF.length) && 5 < (2 & whitetickV)) {
         whitetickV /= Math.max(profilepicw.length, 3);
         break;
      }
      while (1 <= (whitetickV % (Math.max(3, 9)))) {
         whitetickV /= Math.max(crownF.length * whitetickV, 1);
         break;
      }
      for (let i = 0; i < 1; i++) {
         profilepicw = `${crownF.length}`;
      }
      statisticsinactiveV = "3";
      statisticsinactiveV = `${successB.length - 3}`;
   let videocommony = scorepopsounds ? !scorepopsounds : scorepopsounds;
   do {
       let updatesS = String.fromCharCode(97,98,115,100,105,102,102,95,50,95,56,55,0);
       let profileU = String.fromCharCode(116,104,101,97,116,101,114,95,104,95,55,51,0);
       let time_kU = false;
       let filled9 = 5.0;
       let tooltipsX = 1.0;
      while (5.18 < filled9 && 3.20 < (5.18 - filled9)) {
          let update_9b = false;
          let package_kz = 0.0;
          let trophyt: Array<any> = [796, 144];
          let iconcloseL = String.fromCharCode(105,95,54,49,95,102,105,114,115,116,112,97,115,115,0);
         filled9 *= 1 + iconcloseL.length;
         update_9b = 9.85 >= package_kz;
         package_kz /= Math.max(parseInt(`${package_kz}`) & trophyt.length, 1);
         trophyt = [trophyt.length];
         iconcloseL += `${(parseInt(`${package_kz}`) + (update_9b ? 2 : 4))}`;
         break;
      }
         updatesS += `${updatesS.length % 2}`;
      for (let a = 0; a < 3; a++) {
          let gifgoalH = false;
          let libapminsightbY = 4.0;
          let viewsm = String.fromCharCode(101,95,51,0);
          let update_oe4: Array<any> = [String.fromCharCode(106,95,50,57,95,112,108,116,101,0), String.fromCharCode(114,101,112,114,101,115,101,110,116,97,116,105,111,110,95,117,95,49,53,0)];
         tooltipsX /= Math.max(2, ((gifgoalH ? 3 : 1) >> (Math.min(viewsm.length, 1))));
         gifgoalH = parseInt(`${libapminsightbY}`) >= update_oe4.length;
         libapminsightbY -= parseInt(`${libapminsightbY}`);
         viewsm += `${update_oe4.length << (Math.min(3, Math.abs(parseInt(`${libapminsightbY}`))))}`;
      }
         updatesS += `${parseInt(`${filled9}`) | 1}`;
      for (let n = 0; n < 3; n++) {
         updatesS = `${((time_kU ? 1 : 2) % 3)}`;
      }
         updatesS = `${profileU.length}`;
         filled9 /= Math.max(profileU.length % (Math.max(updatesS.length, 2)), 4);
         updatesS += `${(2 & (time_kU ? 5 : 3))}`;
      scorepopsounds = (playC ? scorepopsounds : playC);
      if (videocommony) {
         break;
      }
   } while ((!scorepopsounds) && videocommony);
   for (let z = 0; z < 1; z++) {
      scorepopsounds = !eventO;
   }
   let scorepopsoundH = 7105600 <= successB.length;
   do {
      successB = [((eventO ? 1 : 2))];
      if (scorepopsoundH) {
         break;
      }
   } while ((1 < (footballfieldQ.length - successB.length)) && scorepopsoundH);
    return dimension.height >= dimension.width;
};

export function useOrientation(): boolean {
    
    const [isPotrait, setIsPotrait] = useState<boolean>(
        isPortrait() ? true : false,
    );

    useEffect(() => {
        const callback = () => setIsPotrait(isPortrait() ? true : false);
        let eventListener = Dimensions.addEventListener('change', callback);

        return () => {
            
            
        };
    }, []);

    return isPotrait;
}