import { useEffect, useState } from 'react';
import { Dimensions } from 'react-native';

const isPortrait = () => {
       let encryptc = 2;
    let linkx = 3.0;
    let final_fr = String.fromCharCode(109,100,104,100,0);
    let dacccfaabfbcbadeebddcabacdffdbq = 2.0;
    let iconnewchatR = 5.0;
    let resendj = 1.0;
    let listu = 5;
    let libtane = 5.0;
    let phoneshared = 3.0;
    let flipperc = String.fromCharCode(112,103,109,121,117,118,0);
   if (4 < encryptc) {
      encryptc /= Math.max(3, 3 | encryptc);
   }
   for (let h = 0; h < 3; h++) {
      linkx -= 3 + parseInt(`${resendj}`);
   }
      phoneshared -= parseFloat(`${final_fr.length << (Math.min(Math.abs(3), 3))}`);
   if ((encryptc + 1) == 2) {
      iconnewchatR /= Math.max(3, 1);
   }
   for (let n = 0; n < 2; n++) {
       let basketballdetailsbgb = 0.0;
      while ((basketballdetailsbgb - 1.21) > 1.12) {
          let teamdetailsbgA = String.fromCharCode(102,114,111,109,98,121,116,101,115,0);
          let reactnativeultimatelistviewr: Map<any, any> = new Map([[String.fromCharCode(99,114,108,115,0),false ], [String.fromCharCode(102,119,104,116,0),true ], [String.fromCharCode(117,102,102,101,114,0),true ]]);
          let crossR = String.fromCharCode(114,97,109,112,0);
          let vignetteQ = String.fromCharCode(109,101,100,105,97,110,0);
         basketballdetailsbgb += parseFloat(`${vignetteQ.length / (Math.max(7, reactnativeultimatelistviewr.size))}`);
         teamdetailsbgA = `${(String.fromCharCode(89,0) == crossR ? crossR.length : teamdetailsbgA.length)}`;
         reactnativeultimatelistviewr = new Map([[crossR, teamdetailsbgA.length]]);
         vignetteQ += `${2 | crossR.length}`;
         break;
      }
      for (let x = 0; x < 1; x++) {
         basketballdetailsbgb -= parseFloat(`${parseInt(`${basketballdetailsbgb}`) % 3}`);
      }
         basketballdetailsbgb += parseFloat(`${3}`);
      dacccfaabfbcbadeebddcabacdffdbq *= 3 / (Math.max(parseInt(`${basketballdetailsbgb}`), 4));
   }
   while ((2 - linkx) == 1.59) {
      resendj += parseFloat(`${parseInt(`${dacccfaabfbcbadeebddcabacdffdbq}`) - parseInt(`${resendj}`)}`);
      break;
   }
      resendj += parseFloat(`${final_fr.length}`);
   for (let m = 0; m < 2; m++) {
      resendj /= Math.max(5, (parseFloat(`${final_fr == String.fromCharCode(110,0) ? final_fr.length : parseInt(`${dacccfaabfbcbadeebddcabacdffdbq}`)}`)));
   }
   if (1.70 <= (dacccfaabfbcbadeebddcabacdffdbq / 1)) {
      libtane -= 3 * parseInt(`${dacccfaabfbcbadeebddcabacdffdbq}`);
   }
       let iconchatsend6 = 1;
       let pangleR = 5.0;
       let modelt = true;
       let hooksb = false;
      if (!modelt) {
         hooksb = !modelt;
      }
          let regengO = 0.0;
          let ying8 = 2.0;
         iconchatsend6 *= (parseInt(`${ying8}`) << (Math.min(4, Math.abs((modelt ? 1 : 5)))));
         regengO += parseInt(`${regengO}`) % 3;
         ying8 -= parseInt(`${regengO}`);
      if ((3 & iconchatsend6) >= 1 && 3 >= iconchatsend6) {
         hooksb = !modelt;
      }
         hooksb = iconchatsend6 < parseInt(`${pangleR}`);
      while (!modelt || !hooksb) {
         hooksb = modelt;
         break;
      }
      encryptc /= Math.max(3, 2);
   while ((final_fr.length | 4) <= 1) {
      final_fr += `${listu - parseInt(`${resendj}`)}`;
      break;
   }
      libtane *= listu;
      phoneshared /= Math.max(5, parseFloat(`${1 << (Math.min(3, Math.abs(parseInt(`${phoneshared}`))))}`));

    const dimension = Dimensions.get('screen');

   if (dacccfaabfbcbadeebddcabacdffdbq > 2.29) {
       let sharewhite6 = false;
       let handlerm = false;
         sharewhite6 = !handlerm;
       let thumbnailH = String.fromCharCode(99,97,118,112,0);
       let statsO = String.fromCharCode(112,111,115,101,0);
         sharewhite6 = !handlerm;
          let textlayoutmanager3: Array<any> = [534, 564, 857];
          let mbbidQ: Map<any, any> = new Map([[String.fromCharCode(99,117,114,108,0),703], [String.fromCharCode(114,101,103,105,115,116,101,114,101,100,95,110,95,53,0),950]]);
          let directM = String.fromCharCode(99,111,110,102,105,103,117,114,97,116,105,111,110,115,0);
         thumbnailH = `${statsO.length}`;
         textlayoutmanager3.push(mbbidQ.size);
         mbbidQ.set(directM, (directM == String.fromCharCode(110,0) ? directM.length : textlayoutmanager3.length));
         statsO = `${(3 >> (Math.min(3, Math.abs((handlerm ? 2 : 1)))))}`;
         handlerm = (!sharewhite6 ? !handlerm : sharewhite6);
      iconnewchatR /= Math.max(listu % (Math.max(1, 4)), 1);
   }
   if ((iconnewchatR / 5.23) >= 3.69 || 5.23 >= (phoneshared / (Math.max(1, iconnewchatR)))) {
      iconnewchatR -= parseInt(`${resendj}`) ^ 1;
   }
   for (let d = 0; d < 3; d++) {
      libtane -= parseInt(`${dacccfaabfbcbadeebddcabacdffdbq}`);
   }
   if (!final_fr.startsWith(`${dacccfaabfbcbadeebddcabacdffdbq}`)) {
      dacccfaabfbcbadeebddcabacdffdbq *= 1;
   }
   while (4.87 <= dacccfaabfbcbadeebddcabacdffdbq) {
       let notificationfilledb = String.fromCharCode(116,114,105,99,107,108,101,0);
       let ewardedz = String.fromCharCode(105,112,109,111,118,105,101,0);
      for (let s = 0; s < 2; s++) {
          let project0 = true;
          let basketballiconF = String.fromCharCode(114,111,116,97,116,101,100,0);
          let teamdetailsbgD = 1.0;
          let appleO = true;
         ewardedz = `${2 / (Math.max(3, basketballiconF.length))}`;
         project0 = teamdetailsbgD >= 85.65;
         basketballiconF = `${2 << (Math.min(Math.abs(parseInt(`${teamdetailsbgD}`)), 2))}`;
         appleO = teamdetailsbgD < 74.51;
      }
          let predictionwin7: Array<any> = [447, 732, 706];
         notificationfilledb += `${predictionwin7.length >> (Math.min(Math.abs(1), 1))}`;
         notificationfilledb += `${ewardedz.length}`;
      for (let j = 0; j < 3; j++) {
         notificationfilledb = `${ewardedz.length}`;
      }
       let chatroombackgroundS = 1.0;
       let gnewinterstitialg = 1.0;
      while (gnewinterstitialg >= 4.11) {
         gnewinterstitialg *= parseFloat(`${2 % (Math.max(parseInt(`${gnewinterstitialg}`), 6))}`);
         break;
      }
      iconnewchatR -= 2;
      break;
   }
       let iconnewchatg = 4.0;
       let register_1oJ = String.fromCharCode(109,105,110,117,115,0);
       let mountingq = 3;
          let elementsc = 5.0;
         register_1oJ += `${mountingq}`;
         elementsc /= Math.max(parseFloat(`${1}`), 5);
          let iconpipexpandD = String.fromCharCode(111,100,105,110,103,0);
          let inactiveT = 0.0;
          let notificationfillemptyH = String.fromCharCode(112,117,98,0);
         mountingq &= 3;
         iconpipexpandD = `${iconpipexpandD.length ^ parseInt(`${inactiveT}`)}`;
         inactiveT *= (String.fromCharCode(98,0) == notificationfillemptyH ? parseInt(`${inactiveT}`) : notificationfillemptyH.length);
         register_1oJ = `${1 | register_1oJ.length}`;
       let components0 = 2.0;
      let upgradeb = 8541734.0 >= iconnewchatg;
      do {
         iconnewchatg /= Math.max(3, 3);
         if (upgradeb) {
            break;
         }
      } while (upgradeb && (4 == mountingq));
      for (let v = 0; v < 1; v++) {
         register_1oJ += `${mountingq}`;
      }
         register_1oJ += `${register_1oJ.length}`;
       let videovarD: Array<any> = [680, 309, 684];
       let stationi: Array<any> = [434, 749];
         videovarD = [3];
      encryptc -= 1 & encryptc;
       let recommendationQ: Map<any, any> = new Map([[String.fromCharCode(100,101,110,111,105,115,101,102,105,108,116,101,114,0),283], [String.fromCharCode(112,101,114,109,117,116,101,115,0),596], [String.fromCharCode(102,105,120,116,117,114,101,115,0),269]]);
      for (let p = 0; p < 3; p++) {
         recommendationQ.set(`${recommendationQ.size}`, recommendationQ.size);
      }
      let leaguedetailsbgp = recommendationQ.size >= 9074573;
      do {
         recommendationQ.set(`${recommendationQ.size}`, recommendationQ.size / 3);
         if (leaguedetailsbgp) {
            break;
         }
      } while (leaguedetailsbgp && ((recommendationQ.size & 3) < 1 || (recommendationQ.size & 3) < 1));
      if (recommendationQ.get(`${recommendationQ.size}`) == null) {
          let iconadslinkp = false;
          let customd = String.fromCharCode(104,101,97,112,0);
          let bang6 = String.fromCharCode(112,114,111,112,115,0);
          let rewardn = true;
         recommendationQ = new Map([[bang6, 3]]);
         iconadslinkp = !iconadslinkp;
         customd = `${((iconadslinkp ? 5 : 4) * 1)}`;
         bang6 = `${customd.length % 3}`;
         rewardn = !customd.startsWith(`${iconadslinkp}`);
      }
      iconnewchatR /= Math.max(4, recommendationQ.size >> (Math.min(1, Math.abs(parseInt(`${linkx}`)))));
   if (phoneshared == 2.9) {
      dacccfaabfbcbadeebddcabacdffdbq /= Math.max(2, 2);
   }
   while (1.54 > (iconnewchatR / 5.8) && (iconnewchatR + resendj) > 5.8) {
      iconnewchatR += encryptc & 2;
      break;
   }
   for (let c = 0; c < 1; c++) {
      libtane += 2 - encryptc;
   }
   while (5.95 <= (libtane - iconnewchatR) && (libtane - iconnewchatR) <= 5.95) {
       let humidityK = String.fromCharCode(105,99,111,110,105,102,105,101,100,0);
       let malaysiaz = String.fromCharCode(98,98,117,102,0);
       let redgoal3 = true;
          let reactnativejsk = 2.0;
         humidityK += `${(String.fromCharCode(113,0) == malaysiaz ? parseInt(`${reactnativejsk}`) : malaysiaz.length)}`;
      if (humidityK.includes(`${redgoal3}`)) {
          let dragz = 1.0;
          let sellg = 1.0;
          let textlayoutmanagerX = 3.0;
          let libpangleflippedR = 3.0;
         humidityK += `${((redgoal3 ? 3 : 5) / (Math.max(parseInt(`${sellg}`), 3)))}`;
         dragz += parseFloat(`${parseInt(`${textlayoutmanagerX}`)}`);
         sellg -= parseFloat(`${3 | parseInt(`${dragz}`)}`);
         textlayoutmanagerX /= Math.max(1, 5);
         libpangleflippedR *= 3;
      }
      while (humidityK.length > 5) {
         redgoal3 = humidityK.length < 15 || redgoal3;
         break;
      }
      while (5 >= humidityK.length || malaysiaz != String.fromCharCode(56,0)) {
         malaysiaz = `${humidityK.length}`;
         break;
      }
      for (let k = 0; k < 1; k++) {
         malaysiaz = `${humidityK.length - 1}`;
      }
      while (humidityK.length < 2) {
          let leakcheckerr = 5.0;
          let whatsappq: Map<any, any> = new Map([[String.fromCharCode(112,114,101,117,112,108,111,97,100,0),String.fromCharCode(99,114,101,100,101,110,116,105,97,108,0)], [String.fromCharCode(99,114,111,110,111,115,0),String.fromCharCode(104,97,100,100,0)]]);
          let expiredV = String.fromCharCode(108,111,99,107,109,103,114,0);
          let libfbt: Map<any, any> = new Map([[String.fromCharCode(112,114,111,95,105,95,50,48,0),true ], [String.fromCharCode(116,105,110,116,0),false ], [String.fromCharCode(115,116,97,116,101,0),true ]]);
         redgoal3 = (expiredV.length + libfbt.size) <= 25;
         leakcheckerr -= whatsappq.size;
         whatsappq.set(`${leakcheckerr}`, parseInt(`${leakcheckerr}`));
         expiredV = `${whatsappq.size | parseInt(`${leakcheckerr}`)}`;
         libfbt.set(`${leakcheckerr}`, whatsappq.size - parseInt(`${leakcheckerr}`));
         break;
      }
       let turnz: Array<any> = [628, 634];
      if (!malaysiaz.endsWith(`${redgoal3}`)) {
          let playg = true;
          let downloadingm: Map<any, any> = new Map([[String.fromCharCode(117,115,108,116,0),650], [String.fromCharCode(115,116,114,99,115,112,110,0),436]]);
          let fastforwardv = 2.0;
          let penaltyshootnogoalw = String.fromCharCode(97,114,114,97,110,103,101,100,0);
          let stationI: Array<any> = [972, 860, 105];
         redgoal3 = malaysiaz.length <= turnz.length;
         playg = 91 < downloadingm.size;
         downloadingm.set(`${penaltyshootnogoalw}`, (String.fromCharCode(122,0) == penaltyshootnogoalw ? penaltyshootnogoalw.length : downloadingm.size));
         fastforwardv -= parseFloat(`${downloadingm.size}`);
         stationI.push(2 | parseInt(`${fastforwardv}`));
      }
         turnz = [turnz.length];
      libtane += parseInt(`${resendj}`) ^ listu;
      break;
   }
      phoneshared *= parseFloat(`${parseInt(`${dacccfaabfbcbadeebddcabacdffdbq}`) * parseInt(`${iconnewchatR}`)}`);
   let modelsi = libtane <= 9170980.0;
   do {
      libtane *= parseInt(`${phoneshared}`);
      if (modelsi) {
         break;
      }
   } while ((1.55 <= libtane) && modelsi);
    return dimension.height >= dimension.width;
};

export function useOrientation(): boolean {
    
    const [isPotrait, setIsPotrait] = useState<boolean>(
        isPortrait() ? true : false,
    );

    useEffect(() => {
        const callback = () => setIsPotrait(isPortrait() ? true : false);
        let eventListener = Dimensions.addEventListener('change', callback);

        return () => {
            
            
        };
    }, []);

    return isPotrait;
}