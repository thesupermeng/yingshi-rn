import { useEffect, useState } from 'react';
import { Dimensions } from 'react-native';

const isPortrait = () => {
       let bufferd = String.fromCharCode(98,95,56,95,116,105,108,101,0);
    let moreO = 4.0;
    let zoomw = 4.0;
    let iconK = 1.0;
    let sharea = false;
    let sheetA = String.fromCharCode(108,95,57,49,95,116,97,110,0);
    let sportb: Array<any> = [619, 519, 331];
    let catagory5: Map<any, any> = new Map([[String.fromCharCode(114,116,112,101,110,99,95,102,95,50,56,0),17], [String.fromCharCode(99,97,112,97,98,105,108,105,116,121,95,114,95,56,52,0),740], [String.fromCharCode(122,95,48,95,109,112,101,103,100,97,116,97,0),667]]);
    let interstitiali: Array<any> = [248, 585, 255];
   if (1 < sheetA.length) {
      sheetA += `${sportb.length << (Math.min(Math.abs(3), 1))}`;
   }
      zoomw += ((sharea ? 4 : 4) * 2);
      zoomw *= sportb.length;
       let screenD = String.fromCharCode(98,111,114,100,101,114,115,95,103,95,52,48,0);
       let feedbackz = String.fromCharCode(100,95,49,54,95,116,105,109,101,118,97,108,0);
       let gift6 = 4.0;
         gift6 *= parseFloat(`${feedbackz.length}`);
          let roomm = 3;
          let readp = 3.0;
          let launcher1: Array<any> = [496, 646];
         gift6 *= parseFloat(`${launcher1.length}`);
         roomm >>= Math.min(Math.abs(1 / (Math.max(8, roomm))), 3);
         readp /= Math.max(5, roomm | parseInt(`${readp}`));
         launcher1.push(roomm * 3);
      while (1.72 > (3.67 - gift6)) {
         feedbackz += `${parseInt(`${gift6}`)}`;
         break;
      }
         feedbackz = "2";
      if (2 > (screenD.length / 2) || 4 > (2 % (Math.max(8, screenD.length)))) {
          let listP = String.fromCharCode(100,111,99,107,101,114,95,116,95,56,51,0);
         screenD = `${(String.fromCharCode(116,0) == screenD ? listP.length : screenD.length)}`;
      }
      for (let w = 0; w < 1; w++) {
         gift6 -= parseFloat(`${feedbackz.length}`);
      }
         screenD += `${feedbackz.length ^ 3}`;
      let firebase0 = 9031326 <= screenD.length;
      do {
         screenD += `${feedbackz.length * parseInt(`${gift6}`)}`;
         if (firebase0) {
            break;
         }
      } while ((5 == (1 * screenD.length) || (parseFloat(`${screenD.length}`) / (Math.max(2, gift6))) == 1.38) && firebase0);
      if (screenD.length == feedbackz.length) {
         screenD += `${parseInt(`${gift6}`) % (Math.max(feedbackz.length, 6))}`;
      }
      catagory5 = new Map([[bufferd, bufferd.length / (Math.max(3, 3))]]);
   while (sharea || 5.19 == (iconK * 5.95)) {
      sharea = (bufferd.length - parseInt(`${zoomw}`)) == 92;
      break;
   }
   if (1.14 == zoomw) {
       let tickN = String.fromCharCode(98,108,111,98,95,122,95,50,57,0);
       let pageS = String.fromCharCode(115,117,98,109,105,116,116,101,100,95,103,95,54,56,0);
       let malaysia6 = String.fromCharCode(100,97,112,95,119,95,56,54,0);
       let ajax_ = String.fromCharCode(107,95,57,55,95,113,119,111,114,100,0);
       let natived = String.fromCharCode(111,95,57,52,95,117,110,102,97,105,114,0);
      for (let a = 0; a < 3; a++) {
         ajax_ = `${(String.fromCharCode(117,0) == tickN ? tickN.length : malaysia6.length)}`;
      }
       let expandX = 4;
      for (let a = 0; a < 2; a++) {
         natived = `${pageS.length << (Math.min(Math.abs(1), 1))}`;
      }
          let whatsappl = String.fromCharCode(101,95,56,50,95,115,99,114,101,101,110,115,104,111,116,0);
          let minivodO = String.fromCharCode(115,95,51,49,95,112,114,101,102,101,114,101,110,99,101,0);
         pageS += `${(String.fromCharCode(80,0) == ajax_ ? natived.length : ajax_.length)}`;
         whatsappl = `${whatsappl.length}`;
         minivodO += `${whatsappl.length}`;
      while (3 == (pageS.length | 4) || 3 == (expandX | 4)) {
          let hejii = 0;
          let homeU = true;
          let description_1Q = String.fromCharCode(117,95,53,57,95,118,105,100,101,111,112,114,111,99,101,115,115,111,114,0);
         expandX &= ajax_.length;
         hejii += 1;
         homeU = hejii >= 24 || !homeU;
         description_1Q += `${(description_1Q.length >> (Math.min(1, Math.abs((homeU ? 5 : 2)))))}`;
         break;
      }
         pageS = `${2 << (Math.min(5, natived.length))}`;
      if (tickN.length > malaysia6.length) {
          let pangleE = 3.0;
          let ballo: Array<any> = [68, 19];
          let grayZ = 1.0;
         tickN = `${ajax_.length ^ 2}`;
         pangleE *= parseFloat(`${parseInt(`${pangleE}`) / (Math.max(8, parseInt(`${grayZ}`)))}`);
         ballo = [1 >> (Math.min(Math.abs(parseInt(`${grayZ}`)), 4))];
      }
      let backgroundA = 9154693 >= malaysia6.length;
      do {
         malaysia6 += `${ajax_.length}`;
         if (backgroundA) {
            break;
         }
      } while ((malaysia6.endsWith(`${ajax_.length}`)) && backgroundA);
         ajax_ += `${ajax_.length / 3}`;
         pageS += `${ajax_.length}`;
         expandX ^= (pageS == String.fromCharCode(73,0) ? expandX : pageS.length);
      for (let d = 0; d < 3; d++) {
          let screen7: Map<any, any> = new Map([[String.fromCharCode(100,95,49,49,95,102,114,101,97,100,0),779], [String.fromCharCode(98,95,49,52,95,104,119,99,111,110,102,105,103,0),113]]);
          let componentA = 2.0;
          let materialZ = true;
          let changeb = 4.0;
         pageS += `${screen7.size}`;
         screen7 = new Map([[`${materialZ}`, parseInt(`${componentA}`) % 2]]);
         componentA += parseInt(`${componentA}`) ^ 2;
         materialZ = changeb > 67.92;
         changeb -= 1 << (Math.min(Math.abs(parseInt(`${changeb}`)), 4));
      }
      for (let h = 0; h < 2; h++) {
         tickN += `${tickN.length | malaysia6.length}`;
      }
         pageS = `${(String.fromCharCode(108,0) == pageS ? tickN.length : pageS.length)}`;
          let forwardX: Array<any> = [324, 310];
         natived += "3";
         forwardX = [forwardX.length];
      sheetA += `${(pageS == String.fromCharCode(112,0) ? pageS.length : sportb.length)}`;
   }
      catagory5 = new Map([[sheetA, parseInt(`${zoomw}`) % (Math.max(sheetA.length, 9))]]);
      moreO *= parseInt(`${iconK}`) & sportb.length;
   if (sheetA.length > sportb.length) {
       let pressure3 = 3.0;
       let combinedV = 5;
       let areaq = String.fromCharCode(109,101,109,109,103,114,95,103,95,49,48,48,0);
          let g_titleQ = false;
          let optionsM: Map<any, any> = new Map([[String.fromCharCode(119,95,56,49,95,97,118,101,115,0),314], [String.fromCharCode(109,95,52,51,95,112,105,112,101,108,105,110,105,110,103,0),253], [String.fromCharCode(117,115,101,114,110,97,109,101,95,119,95,49,56,0),91]]);
         pressure3 /= Math.max(3, 5);
         g_titleQ = optionsM.size > optionsM.size;
          let footballP = String.fromCharCode(105,95,57,95,109,117,108,116,120,109,117,108,116,0);
         areaq = `${combinedV / (Math.max(1, parseInt(`${pressure3}`)))}`;
         footballP = `${footballP.length}`;
      let downloadingV = 9616440.0 >= pressure3;
      do {
         pressure3 += 2 % (Math.max(6, combinedV));
         if (downloadingV) {
            break;
         }
      } while (((2.46 + pressure3) > 3.73) && downloadingV);
      let utilsY = combinedV >= 6203525;
      do {
          let klevinf: Map<any, any> = new Map([[String.fromCharCode(111,117,116,103,111,105,110,103,95,107,95,56,0),false ], [String.fromCharCode(119,95,56,55,95,103,108,111,98,97,108,108,121,0),true ]]);
          let description_zF: Map<any, any> = new Map([[String.fromCharCode(117,112,112,101,114,99,97,115,101,95,56,95,56,56,0),String.fromCharCode(115,95,51,95,102,101,116,99,104,0)], [String.fromCharCode(112,114,111,112,101,114,121,95,110,95,55,48,0),String.fromCharCode(121,95,55,50,95,115,101,97,100,0)], [String.fromCharCode(98,95,54,50,0),String.fromCharCode(100,105,102,102,101,114,101,110,116,95,113,95,51,52,0)]]);
          let mbnativeI: Array<any> = [802, 348];
          let zoomN: Array<any> = [String.fromCharCode(112,95,50,49,95,115,116,114,97,116,101,103,105,101,115,0), String.fromCharCode(99,111,111,114,100,95,112,95,56,55,0)];
          let clear6: Array<any> = [90, 852];
         combinedV %= Math.max(4, mbnativeI.length - clear6.length);
         klevinf.set(`${zoomN.length}`, zoomN.length / (Math.max(2, 9)));
         description_zF = new Map([[`${description_zF.size}`, klevinf.size]]);
         mbnativeI = [2];
         clear6.push(description_zF.size);
         if (utilsY) {
            break;
         }
      } while ((3 >= areaq.length) && utilsY);
          let champion4 = 3.0;
          let y_managerl = String.fromCharCode(115,119,97,112,95,110,95,57,0);
         combinedV ^= y_managerl.length;
         champion4 *= parseInt(`${champion4}`);
         y_managerl = "1";
          let tumbnaild: Array<any> = [216, 854];
          let spinnerO = 2;
         areaq = `${spinnerO % (Math.max(tumbnaild.length, 2))}`;
      for (let m = 0; m < 2; m++) {
         pressure3 *= combinedV / (Math.max(8, parseInt(`${pressure3}`)));
      }
         pressure3 /= Math.max(4, parseInt(`${pressure3}`) * combinedV);
         pressure3 += 2;
      sheetA += `${parseInt(`${moreO}`) + 1}`;
   }
   if (2 < sheetA.length || !sharea) {
       let inactivev = 2.0;
      let viewerk = 5791968.0 <= inactivev;
      do {
          let helperL = 2.0;
          let scorev: Map<any, any> = new Map([[String.fromCharCode(115,105,110,113,105,95,54,95,49,54,0),String.fromCharCode(114,111,116,97,116,105,111,110,97,110,103,108,101,95,54,95,52,49,0)], [String.fromCharCode(112,115,104,95,53,95,55,52,0),String.fromCharCode(115,95,52,48,95,101,121,101,0)]]);
          let linkX = String.fromCharCode(97,117,116,111,102,111,99,117,115,105,110,103,95,49,95,50,50,0);
          let launcherl = 5;
         inactivev -= 2 - scorev.size;
         helperL -= parseFloat(`${3}`);
         scorev.set(linkX, linkX.length);
         launcherl += linkX.length;
         if (viewerk) {
            break;
         }
      } while ((inactivev < inactivev) && viewerk);
      let benefitz = 5734344.0 >= inactivev;
      do {
         inactivev += parseInt(`${inactivev}`);
         if (benefitz) {
            break;
         }
      } while ((inactivev < inactivev) && benefitz);
          let selectU = String.fromCharCode(111,95,54,52,95,112,105,120,101,108,115,120,0);
         inactivev /= Math.max(1 / (Math.max(4, parseInt(`${inactivev}`))), 1);
         selectU = `${3 | selectU.length}`;
      sharea = moreO < bufferd.length;
   }
      sharea = sheetA == String.fromCharCode(73,0);
      sportb = [(parseInt(`${iconK}`) ^ (sharea ? 1 : 1))];
   for (let e = 0; e < 2; e++) {
      interstitiali.push(sportb.length);
   }

    const dimension = Dimensions.get('screen');

      zoomw += 1 ^ interstitiali.length;
      moreO -= bufferd.length;
   for (let w = 0; w < 1; w++) {
      catagory5.set(`${sharea}`, sheetA.length);
   }
   for (let d = 0; d < 2; d++) {
       let handlerz = 4.0;
       let action9 = String.fromCharCode(102,95,57,53,95,98,101,97,116,0);
       let goal8 = 1;
         action9 += `${action9.length - 1}`;
         goal8 &= 3 & goal8;
      if ((handlerz / 5.55) > 5.83) {
          let reportC: Map<any, any> = new Map([[String.fromCharCode(108,95,56,48,95,99,104,111,112,0),false ], [String.fromCharCode(112,97,99,107,115,95,104,95,51,54,0),true ]]);
          let filledt = String.fromCharCode(106,102,105,101,108,100,115,95,120,95,54,50,0);
          let storeq = String.fromCharCode(101,97,115,101,95,117,95,54,56,0);
          let mutedn: Array<any> = [606, 695, 272];
          let umengj = String.fromCharCode(99,97,118,112,95,52,95,52,57,0);
         handlerz *= parseFloat(`${action9.length ^ parseInt(`${handlerz}`)}`);
         reportC.set(`${mutedn.length}`, mutedn.length / 1);
         filledt = `${reportC.size}`;
         storeq += `${umengj.length - storeq.length}`;
         umengj += `${storeq.length - 3}`;
      }
      if (3 == (action9.length % (Math.max(6, goal8))) && 2 == (action9.length % (Math.max(3, 9)))) {
          let reactN = 2;
          let downloadingi = 0.0;
         action9 += "1 * goal8";
         reactN *= reactN;
         downloadingi /= Math.max(4, parseFloat(`${parseInt(`${downloadingi}`) % 3}`));
      }
       let with_i5_ = String.fromCharCode(102,111,117,110,100,95,55,95,52,48,0);
       let sansH = String.fromCharCode(116,104,117,109,98,110,97,105,108,115,95,120,95,54,51,0);
      if (action9.startsWith(`${handlerz}`)) {
         action9 = "3";
      }
      while (parseFloat(`${sansH.length}`) >= handlerz) {
         sansH = "2";
         break;
      }
      while ((1.78 - handlerz) < 3.37) {
          let turn4: Array<any> = [String.fromCharCode(114,95,54,57,95,111,112,101,114,97,110,100,0), String.fromCharCode(97,118,114,101,115,97,109,112,108,101,114,101,115,95,111,95,52,55,0), String.fromCharCode(110,111,111,112,95,111,95,54,56,0)];
          let mathj = true;
          let taiwanH = String.fromCharCode(105,95,49,57,95,113,99,97,110,0);
         goal8 >>= Math.min(sansH.length, 4);
         turn4.push(taiwanH.length);
         mathj = !taiwanH.startsWith(`${mathj}`);
         break;
      }
      if (action9.startsWith(`${goal8}`)) {
         goal8 >>= Math.min(2, sansH.length);
      }
      zoomw += parseInt(`${iconK}`) - 1;
   }
       let stepS = 0;
       let stringsc = 3.0;
         stepS |= 3;
         stringsc += parseInt(`${stringsc}`) - stepS;
      let analyticsY = stringsc >= 6764187.0;
      do {
          let specp = String.fromCharCode(121,118,99,111,110,102,105,103,95,97,95,50,55,0);
          let type_tm1 = String.fromCharCode(114,95,52,53,95,118,97,114,116,105,109,101,0);
          let analyticG = String.fromCharCode(97,95,49,57,95,122,111,110,101,0);
          let awayS: Map<any, any> = new Map([[String.fromCharCode(108,105,118,101,100,95,54,95,53,49,0),true ], [String.fromCharCode(121,95,57,51,95,102,105,101,108,100,0),false ]]);
         stringsc *= (String.fromCharCode(118,0) == analyticG ? specp.length : analyticG.length);
         specp = `${awayS.size}`;
         type_tm1 = "2";
         awayS = new Map([[`${awayS.size}`, awayS.size]]);
         if (analyticsY) {
            break;
         }
      } while (analyticsY && (4 >= (3 % (Math.max(8, stepS)))));
         stepS >>= Math.min(Math.abs(2), 5);
      let service5 = stepS >= 5761569;
      do {
         stepS >>= Math.min(4, Math.abs(parseInt(`${stringsc}`) << (Math.min(5, Math.abs(3)))));
         if (service5) {
            break;
         }
      } while (((stepS ^ 1) <= 5 || 1 <= (parseInt(`${stringsc}`) + stepS)) && service5);
      while ((stepS % 2) < 4) {
         stepS %= Math.max(1, stepS);
         break;
      }
      catagory5.set(`${interstitiali.length}`, catagory5.size % (Math.max(3, 1)));
      sheetA = `${((sharea ? 1 : 4) << (Math.min(sportb.length, 5)))}`;
      zoomw += 1;
      zoomw -= bufferd.length;
   for (let c = 0; c < 1; c++) {
       let confirmationF = 3.0;
       let down9 = 4;
       let chinasameB: Array<any> = [857, 678, 841];
       let matchesq = String.fromCharCode(115,105,112,114,95,114,95,57,50,0);
       let rewardvideoi = false;
      if (3 > (chinasameB.length & 4) && 5 > (4 & matchesq.length)) {
         chinasameB = [2];
      }
      let belld = 8453092 <= matchesq.length;
      do {
         matchesq += `${parseInt(`${confirmationF}`)}`;
         if (belld) {
            break;
         }
      } while ((down9 <= 2) && belld);
       let forwardl = String.fromCharCode(115,109,111,111,116,104,101,110,95,99,95,56,49,0);
       let episode4 = String.fromCharCode(110,101,119,101,115,116,95,106,95,52,51,0);
       let videojs1 = String.fromCharCode(99,104,97,110,110,101,108,95,53,95,56,49,0);
       let shoot6 = String.fromCharCode(102,97,116,101,95,122,95,50,57,0);
      if (forwardl.length <= 5) {
         episode4 = "3";
      }
      let incidentT = confirmationF >= 9248112.0;
      do {
         confirmationF -= 2;
         if (incidentT) {
            break;
         }
      } while ((!forwardl.startsWith(`${confirmationF}`)) && incidentT);
      for (let t = 0; t < 2; t++) {
         down9 |= (3 & (rewardvideoi ? 1 : 4));
      }
         videojs1 = `${forwardl.length}`;
      let hejiA = String.fromCharCode(105,48,109,106,0) == matchesq;
      do {
          let volume6 = String.fromCharCode(114,101,110,100,101,114,105,110,103,95,101,95,53,0);
          let tailM = 5.0;
          let mbnativeadvanced2 = 3.0;
          let componentS = String.fromCharCode(112,95,56,52,95,100,109,117,108,0);
          let zoomm = 0;
         matchesq = `${componentS.length}`;
         volume6 = `${volume6.length}`;
         tailM -= parseFloat(`${parseInt(`${mbnativeadvanced2}`)}`);
         mbnativeadvanced2 /= Math.max(zoomm & parseInt(`${mbnativeadvanced2}`), 4);
         componentS = `${1 - parseInt(`${tailM}`)}`;
         zoomm ^= parseInt(`${tailM}`);
         if (hejiA) {
            break;
         }
      } while (hejiA && (4 < matchesq.length));
       let overlayL = String.fromCharCode(108,97,110,99,122,111,115,95,98,95,49,56,0);
      while (episode4.length >= 2 && !rewardvideoi) {
         rewardvideoi = 69.57 == confirmationF;
         break;
      }
       let favoritef: Array<any> = [64, 763];
       let overs: Array<any> = [String.fromCharCode(112,95,52,49,95,104,101,120,98,115,0), String.fromCharCode(112,114,111,120,121,95,117,95,55,52,0), String.fromCharCode(100,95,50,56,95,98,108,111,99,107,100,99,0)];
         overs.push(videojs1.length);
      while (favoritef.includes(overs.length)) {
         overs.push(shoot6.length);
         break;
      }
         videojs1 = `${overs.length}`;
      sharea = sportb.length == 71;
   }
       let away6 = 3.0;
       let mappingJ = String.fromCharCode(98,97,99,107,116,114,97,99,101,95,121,95,57,55,0);
      while (2 == (parseInt(`${away6}`) + 4) && (parseInt(`${away6}`) + mappingJ.length) == 4) {
         mappingJ += `${(mappingJ == String.fromCharCode(55,0) ? parseInt(`${away6}`) : mappingJ.length)}`;
         break;
      }
         away6 *= parseFloat(`${parseInt(`${away6}`)}`);
         mappingJ = `${parseInt(`${away6}`)}`;
      while (1 < (parseInt(`${away6}`) + mappingJ.length) && 3.12 < (away6 + 3.29)) {
         mappingJ = "1";
         break;
      }
      let n_titleD = mappingJ == String.fromCharCode(122,56,55,122,112,112,49,0);
      do {
         mappingJ += `${mappingJ.length}`;
         if (n_titleD) {
            break;
         }
      } while ((mappingJ.length >= 5) && n_titleD);
         away6 += parseFloat(`${mappingJ.length}`);
      catagory5 = new Map([[bufferd, (bufferd == String.fromCharCode(79,0) ? parseInt(`${moreO}`) : bufferd.length)]]);
      sharea = (parseInt(`${iconK}`) * sheetA.length) >= 68;
   while (zoomw >= 2.79) {
      moreO -= sportb.length << (Math.min(2, Math.abs(parseInt(`${moreO}`))));
      break;
   }
   let mbnativeL = 6155391 >= catagory5.size;
   do {
       let carouselL = String.fromCharCode(103,95,54,52,95,104,101,118,99,0);
       let debugJ: Map<any, any> = new Map([[String.fromCharCode(116,116,108,95,52,95,54,0),525], [String.fromCharCode(115,99,116,112,99,111,110,110,95,51,95,53,0),770], [String.fromCharCode(112,114,111,103,114,101,115,105,118,101,95,107,95,56,50,0),438]]);
       let football6: Array<any> = [String.fromCharCode(98,95,53,55,95,108,111,99,107,115,99,114,101,101,110,0), String.fromCharCode(99,95,50,55,95,100,101,102,97,117,108,116,99,111,101,102,99,111,117,110,116,115,0), String.fromCharCode(118,95,55,50,95,115,99,97,108,101,102,97,99,116,111,114,115,0)];
       let areak = true;
      while (football6.length > 5) {
         debugJ.set(carouselL, (String.fromCharCode(95,0) == carouselL ? debugJ.size : carouselL.length));
         break;
      }
         areak = (carouselL.length + football6.length) < 48;
         areak = (((!areak ? 33 : carouselL.length) | carouselL.length) >= 33);
      if (football6.length > carouselL.length) {
         football6.push(1);
      }
      while (4 == (debugJ.size + 1) && (1 + debugJ.size) == 2) {
          let mbbannerQ = 0.0;
          let turnI = String.fromCharCode(114,95,55,54,95,109,117,116,97,116,105,110,103,0);
          let expand0 = 1;
         carouselL += `${(parseInt(`${mbbannerQ}`) >> (Math.min(4, Math.abs((areak ? 2 : 2)))))}`;
         mbbannerQ *= turnI.length;
         turnI += "3 & expand0";
         expand0 += expand0 << (Math.min(turnI.length, 3));
         break;
      }
       let dialog0 = 2.0;
      for (let c = 0; c < 1; c++) {
         areak = (debugJ.size - parseInt(`${dialog0}`)) > 80;
      }
       let grey8: Array<any> = [811, 35];
      catagory5 = new Map([[`${debugJ.size}`, debugJ.size << (Math.min(Math.abs(2), 3))]]);
      if (mbnativeL) {
         break;
      }
   } while (((zoomw / 5.96) > 3.69 || 2.70 > (zoomw / 5.96)) && mbnativeL);
    return dimension.height >= dimension.width;
};

export function useOrientation(): boolean {
    
    const [isPotrait, setIsPotrait] = useState<boolean>(
        isPortrait() ? true : false,
    );

    useEffect(() => {
        const callback = () => setIsPotrait(isPortrait() ? true : false);
        let eventListener = Dimensions.addEventListener('change', callback);

        return () => {
            
            
        };
    }, []);

    return isPotrait;
}