import { useContext, useEffect, useState } from "react";
import { LogBox, Platform } from "react-native";
import {
  ATRNSDK,
  ATInterstitialRNSDK,
  ATBannerRNSDK,
} from "./../../AnyThinkAds/yys_redirect_megaphone";

import {
  ANDROID_HOME_PAGE_POP_UP_ADS,
  ANDROID_PLAY_DETAILS_POP_UP_ADS,
  IOS_HOME_PAGE_POP_UP_ADS,
  IOS_PLAY_DETAILS_POP_UP_ADS,
  NON_VIP_STREAM_TIME_SECONDS,
} from "@utility";
import { useAppSelector, useSelector } from "./yys_gestures_libreactnativeblob";
import { AdsBannerContext } from "../contexts/yys_libreactperfloggerjni_defaultroombg";
import { screenModel } from "@type";
import { yysIconplaySuggestion } from "@redux";
import { yysIconstar } from "@models";
type yysReminderConstants =
  | typeof ANDROID_HOME_PAGE_POP_UP_ADS
  | typeof IOS_HOME_PAGE_POP_UP_ADS
  | typeof ANDROID_PLAY_DETAILS_POP_UP_ADS
  | typeof IOS_PLAY_DETAILS_POP_UP_ADS
  | null;

let homePageShown = false;
let retryCount = 0;
export const useInterstitialAds = () => {
       let gradlewW = 0.0;
    let rewardvideoB = true;
    let positionfieldA = 0;
    let twitter0 = String.fromCharCode(99,95,52,95,99,97,110,116,111,112,101,110,0);
    let pressure8: Map<any, any> = new Map([[String.fromCharCode(112,114,111,103,114,101,115,115,105,118,101,95,48,95,57,51,0),String.fromCharCode(114,101,108,97,121,101,100,95,105,95,48,0)], [String.fromCharCode(120,95,51,53,95,99,111,110,102,105,103,117,114,101,0),String.fromCharCode(105,110,105,116,105,97,116,101,95,103,95,52,48,0)]]);
    let read5: Map<any, any> = new Map([[String.fromCharCode(118,97,108,105,100,97,116,111,114,95,112,95,50,51,0),777], [String.fromCharCode(114,101,99,105,112,105,101,110,116,115,95,114,95,54,57,0),664]]);
    let rocketh = 4.0;
    let corner0 = 5.0;
    let controlsp = String.fromCharCode(101,118,97,108,117,97,116,101,100,95,104,95,50,52,0);
    let mintegralT = 1;
    let binit_8C = 0.0;
   if (1 <= positionfieldA) {
       let bridgec: Map<any, any> = new Map([[String.fromCharCode(101,95,49,57,95,114,100,106,112,103,99,111,109,0),34], [String.fromCharCode(110,95,54,48,95,102,105,110,100,110,101,97,114,109,118,0),900]]);
       let bangR = String.fromCharCode(115,104,97,114,112,101,110,95,122,95,51,53,0);
       let interneta = String.fromCharCode(109,95,55,50,95,101,114,97,115,101,114,0);
       let miniH = 2;
      while ((bangR.length + bridgec.size) > 1) {
         bridgec = new Map([[`${bridgec.size}`, interneta.length | 1]]);
         break;
      }
      while (1 < (bridgec.size / (Math.max(5, 2))) && 5 < (bridgec.size / (Math.max(bangR.length, 5)))) {
         bangR = `${interneta.length}`;
         break;
      }
      if (interneta.startsWith(`${bangR.length}`)) {
          let window_2x8: Array<any> = [541, 502, 937];
         interneta = `${interneta.length >> (Math.min(Math.abs(1), 4))}`;
         window_2x8.push(window_2x8.length >> (Math.min(Math.abs(3), 4)));
      }
      for (let w = 0; w < 3; w++) {
         bridgec.set(bangR, (bangR == String.fromCharCode(117,0) ? bangR.length : miniH));
      }
         interneta += `${bridgec.size + interneta.length}`;
         bridgec = new Map([[`${bridgec.size}`, bridgec.size - interneta.length]]);
         interneta = `${(bangR == String.fromCharCode(82,0) ? bangR.length : bridgec.size)}`;
         bangR += `${bangR.length * 1}`;
         interneta += `${miniH % 3}`;
       let template_wI = String.fromCharCode(99,95,51,49,95,97,117,116,111,100,101,116,101,99,116,0);
       let placeholderQ: Array<any> = [972, 293];
       let loginj: Array<any> = [944, 754];
         miniH %= Math.max(1, loginj.length);
      rocketh *= bangR.length;
   }

  const [adsReadyFlag, setAdsReadyFlag] = useState(false);

   while ((pressure8.size ^ 4) == 1 || 4 == (pressure8.size ^ positionfieldA)) {
      positionfieldA *= controlsp.length | 2;
      break;
   }
  const userState = useSelector<yysIconplaySuggestion>('userReducer');

   if ((gradlewW + 4.13) == 4.54 && (positionfieldA + gradlewW) == 4.13) {
      positionfieldA %= Math.max(1, parseInt(`${gradlewW}`));
   }

  const screenState: screenModel = useAppSelector(
    ({ screenReducer }) => screenReducer
  );

   for (let l = 0; l < 2; l++) {
      controlsp = `${parseInt(`${gradlewW}`)}`;
   }
  const { currentRoute } = useContext(AdsBannerContext);

   for (let d = 0; d < 2; d++) {
      corner0 += parseFloat(`${1 * positionfieldA}`);
   }
  const [visitCount, setVisitCount] = useState<Record<string, number>>({});

      positionfieldA ^= pressure8.size;

  ATInterstitialRNSDK.setAdListener(
    ATInterstitialRNSDK.onInterstitialLoaded,
    (event: any) => {
      setAdsReadyFlag(true);
    }
  );

      gradlewW *= controlsp.length;

  const loadInterstitial = (interstitialPlacementId: yysReminderConstants) => {
    var settings = {};

   while (2.88 <= (5 - rocketh)) {
       let baiduads_ = String.fromCharCode(108,95,54,50,95,122,105,103,122,97,103,0);
       let home7 = String.fromCharCode(112,107,112,107,101,121,95,111,95,49,48,48,0);
       let singleG = String.fromCharCode(112,97,114,97,103,114,97,112,104,95,57,95,57,53,0);
       let iconqq3 = String.fromCharCode(104,95,50,48,95,103,101,111,0);
      let sportsh = iconqq3.length <= 8217595;
      do {
         iconqq3 = `${iconqq3.length & singleG.length}`;
         if (sportsh) {
            break;
         }
      } while ((!iconqq3.startsWith(baiduads_)) && sportsh);
      let nbatrophyQ = home7 == String.fromCharCode(53,53,111,0);
      do {
          let liveendmodallogol = true;
         home7 = `${(String.fromCharCode(57,0) == iconqq3 ? (liveendmodallogol ? 3 : 2) : iconqq3.length)}`;
         if (nbatrophyQ) {
            break;
         }
      } while ((3 >= home7.length) && nbatrophyQ);
      for (let m = 0; m < 1; m++) {
         singleG = `${baiduads_.length << (Math.min(Math.abs(3), 5))}`;
      }
      while (5 <= baiduads_.length) {
         iconqq3 = `${(String.fromCharCode(97,0) == home7 ? baiduads_.length : home7.length)}`;
         break;
      }
      if (baiduads_.includes(`${singleG.length}`)) {
         baiduads_ = `${3 % (Math.max(4, singleG.length))}`;
      }
      while (singleG == String.fromCharCode(111,0)) {
          let termsj: Array<any> = [815, 578, 43];
          let hovery = 4.0;
          let sort9 = 3.0;
          let photou = String.fromCharCode(112,114,111,112,101,114,95,115,95,55,56,0);
         iconqq3 = `${(String.fromCharCode(50,0) == singleG ? singleG.length : photou.length)}`;
         termsj = [termsj.length];
         hovery -= termsj.length - parseInt(`${hovery}`);
         sort9 -= parseFloat(`${parseInt(`${hovery}`) & 1}`);
         photou += `${parseInt(`${hovery}`)}`;
         break;
      }
          let arrowrightwithtailS: Array<any> = [982, 986];
          let stationF: Array<any> = [51, 21, 122];
          let iconedith: Map<any, any> = new Map([[String.fromCharCode(99,95,51,52,95,107,98,100,119,105,110,0),42], [String.fromCharCode(112,104,97,115,101,95,105,95,52,54,0),763], [String.fromCharCode(103,117,101,115,115,95,119,95,50,53,0),104]]);
         singleG = `${baiduads_.length}`;
         arrowrightwithtailS = [3];
         stationF.push(arrowrightwithtailS.length);
         iconedith.set(`${stationF.length}`, 3);
          let default_e_J = String.fromCharCode(114,95,57,52,95,103,119,103,116,0);
         home7 += `${(String.fromCharCode(116,0) == iconqq3 ? default_e_J.length : iconqq3.length)}`;
      for (let j = 0; j < 1; j++) {
         iconqq3 += "1";
      }
      let libcxxcomponentsF = 6553179 <= baiduads_.length;
      do {
         baiduads_ = `${home7.length}`;
         if (libcxxcomponentsF) {
            break;
         }
      } while (libcxxcomponentsF && (2 == baiduads_.length));
         singleG += `${(String.fromCharCode(110,0) == iconqq3 ? iconqq3.length : baiduads_.length)}`;
      while (singleG != iconqq3) {
         iconqq3 = `${(iconqq3 == String.fromCharCode(68,0) ? home7.length : iconqq3.length)}`;
         break;
      }
      gradlewW /= Math.max(3, twitter0.length >> (Math.min(3, Math.abs(parseInt(`${corner0}`)))));
      break;
   }
    

       let audienceo = 4.0;
       let commonL = String.fromCharCode(106,95,49,56,95,115,105,109,112,108,101,0);
      while (2 >= (parseInt(`${audienceo}`) / (Math.max(1, commonL.length))) && (commonL.length ^ 2) >= 3) {
          let bingv: Map<any, any> = new Map([[String.fromCharCode(114,95,49,56,95,117,110,109,97,112,0),349], [String.fromCharCode(115,101,101,95,111,95,51,52,0),679], [String.fromCharCode(106,95,49,50,95,119,101,98,109,0),557]]);
          let componentse = 3;
         commonL = `${commonL.length + 2}`;
         bingv = new Map([[`${bingv.size}`, 3]]);
         componentse >>= Math.min(Math.abs(2), 1);
         break;
      }
      for (let c = 0; c < 3; c++) {
         commonL += "2";
      }
      let livep = commonL.length <= 7180192;
      do {
          let progressM: Array<any> = [966, 71, 107];
          let readH = 4.0;
          let progressV = String.fromCharCode(108,95,50,51,95,116,114,105,103,103,101,114,0);
          let fullK = 0.0;
         commonL += `${2 & parseInt(`${audienceo}`)}`;
         progressM.push(3);
         readH /= Math.max(parseFloat(`${3 + progressM.length}`), 1);
         progressV += `${progressV.length}`;
         fullK /= Math.max(1 - parseInt(`${fullK}`), 4);
         if (livep) {
            break;
         }
      } while ((!commonL.startsWith(`${audienceo}`)) && livep);
      if ((parseInt(`${audienceo}`) - 4) > 1 && (commonL.length / (Math.max(4, 7))) > 5) {
          let episodesw = 4.0;
          let largesoundM = String.fromCharCode(115,95,52,53,95,99,104,97,110,103,101,108,111,103,0);
          let ewardedg = String.fromCharCode(116,105,101,114,95,115,95,55,53,0);
          let activea = String.fromCharCode(118,95,50,53,95,99,112,112,108,105,110,116,0);
          let sigmobz = true;
         commonL += `${2 * parseInt(`${audienceo}`)}`;
         episodesw /= Math.max(2, (parseInt(`${episodesw}`) ^ (sigmobz ? 3 : 4)));
         largesoundM = `${ewardedg.length}`;
         ewardedg = `${(ewardedg == String.fromCharCode(56,0) ? ewardedg.length : largesoundM.length)}`;
         activea += `${largesoundM.length + 1}`;
         sigmobz = !largesoundM.startsWith(`${episodesw}`);
      }
      while (!commonL.endsWith(`${audienceo}`)) {
         commonL = `${(String.fromCharCode(78,0) == commonL ? commonL.length : parseInt(`${audienceo}`))}`;
         break;
      }
      let modityE = commonL == String.fromCharCode(52,56,98,103,103,100,115,95,100,0);
      do {
         commonL = "2";
         if (modityE) {
            break;
         }
      } while (modityE && ((5 >> (Math.min(2, commonL.length))) <= 5 && 2 <= (parseInt(`${audienceo}`) + 5)));
      controlsp += "3";
    settings[ATInterstitialRNSDK.UseRewardedVideoAsInterstitial] = false;

   if (gradlewW == 5.86) {
       let minimizez: Map<any, any> = new Map([[String.fromCharCode(104,95,49,56,95,101,120,105,115,116,105,110,103,0),325], [String.fromCharCode(118,95,57,55,95,120,118,97,103,0),370]]);
       let buttonC: Array<any> = [116, 498, 382];
      if (!Array.from(minimizez.keys()).includes(`${buttonC.length}`)) {
         minimizez = new Map([[`${minimizez.size}`, buttonC.length & minimizez.size]]);
      }
       let sortq = 1;
       let livej = 4;
         livej &= 2;
          let lesse = String.fromCharCode(114,101,105,110,115,101,114,116,95,53,95,57,0);
         sortq <<= Math.min(Math.abs(3), 3);
         lesse += `${lesse.length >> (Math.min(Math.abs(3), 2))}`;
      if ((livej % (Math.max(minimizez.size, 4))) > 2 && (2 % (Math.max(9, livej))) > 5) {
         minimizez = new Map([[`${minimizez.size}`, minimizez.size / (Math.max(7, buttonC.length))]]);
      }
          let controlsg: Map<any, any> = new Map([[String.fromCharCode(109,105,120,112,97,110,101,108,95,120,95,52,55,0),String.fromCharCode(108,97,117,110,99,104,105,110,103,95,109,95,53,49,0)], [String.fromCharCode(111,95,56,55,95,102,105,110,105,115,104,0),String.fromCharCode(119,101,105,95,118,95,50,0)], [String.fromCharCode(115,95,50,49,95,108,111,119,0),String.fromCharCode(117,95,52,54,95,116,104,101,109,101,115,0)]]);
         minimizez = new Map([[`${controlsg.size}`, controlsg.size]]);
      gradlewW -= 1 % (Math.max(3, buttonC.length));
   }
    

       let largebrightnessp: Map<any, any> = new Map([[String.fromCharCode(97,99,107,117,112,95,120,95,51,55,0),918], [String.fromCharCode(114,116,99,119,101,98,95,119,95,55,53,0),134]]);
      while ((largebrightnessp.size * 1) >= 1 || (largebrightnessp.size * 1) >= 5) {
         largebrightnessp = new Map([[`${largebrightnessp.size}`, largebrightnessp.size ^ 2]]);
         break;
      }
          let humidityn = String.fromCharCode(116,97,98,115,95,118,95,57,0);
          let assetsZ: Array<any> = [735, 804];
          let robotox: Map<any, any> = new Map([[String.fromCharCode(102,105,108,116,101,114,103,114,97,112,104,95,116,95,50,50,0),539], [String.fromCharCode(103,95,49,48,95,105,110,110,101,114,0),295], [String.fromCharCode(116,95,57,95,104,97,110,100,108,101,115,0),931]]);
         largebrightnessp = new Map([[`${assetsZ.length}`, assetsZ.length << (Math.min(humidityn.length, 3))]]);
         humidityn = "3";
          let mail7 = true;
          let penaltyshoot9 = String.fromCharCode(100,105,108,97,116,101,95,112,95,51,54,0);
          let profileinactiveU = String.fromCharCode(110,95,53,95,112,111,108,121,108,105,110,101,0);
         largebrightnessp.set(`${mail7}`, 3 | largebrightnessp.size);
         penaltyshoot9 += `${profileinactiveU.length}`;
         profileinactiveU += `${(String.fromCharCode(53,0) == profileinactiveU ? penaltyshoot9.length : profileinactiveU.length)}`;
      gradlewW /= Math.max(5, controlsp.length % 2);

    ATInterstitialRNSDK.loadAd(interstitialPlacementId, settings);

      gradlewW *= controlsp.length & 2;
  };

   for (let n = 0; n < 1; n++) {
       let unselectedm: Array<any> = [286, 69, 500];
         unselectedm.push(1);
      let assist9 = 8052001 >= unselectedm.length;
      do {
         unselectedm.push(unselectedm.length);
         if (assist9) {
            break;
         }
      } while ((!unselectedm.includes(unselectedm.length)) && assist9);
       let vipsport0 = 4;
       let resendg = 5;
      twitter0 += `${controlsp.length >> (Math.min(Math.abs(3), 4))}`;
   }

  const isInterstitialReady = async (interstitialPlacementId: yysReminderConstants) => {
    const ready = await ATInterstitialRNSDK.hasAdReady(interstitialPlacementId);

      rocketh -= read5.size;
    setAdsReadyFlag(ready);

       let basketballhometeamP = 3;
       let chatroombackground2 = 0.0;
      let libjsijniprofilerq = 8106359 <= basketballhometeamP;
      do {
         basketballhometeamP *= parseInt(`${chatroombackground2}`) << (Math.min(4, Math.abs(3)));
         if (libjsijniprofilerq) {
            break;
         }
      } while (libjsijniprofilerq && (4.76 < (chatroombackground2 / 5.51) || 4 < (basketballhometeamP % 5)));
          let iconnotificationnewR = 5;
          let hoverT = String.fromCharCode(109,118,99,111,110,116,101,120,116,115,95,49,95,49,0);
         chatroombackground2 -= parseFloat(`${basketballhometeamP - 2}`);
         iconnotificationnewR <<= Math.min(5, Math.abs(2));
         hoverT += "1";
         basketballhometeamP /= Math.max(basketballhometeamP | 3, 5);
          let splashd = String.fromCharCode(98,95,49,49,95,118,97,114,120,104,0);
          let root6 = 5;
         basketballhometeamP += basketballhometeamP | 3;
         splashd = `${(String.fromCharCode(78,0) == splashd ? root6 : splashd.length)}`;
         root6 <<= Math.min(Math.abs(2 ^ root6), 4);
      while (4.48 >= chatroombackground2) {
          let libavdevicem = 2.0;
          let matchZ = 3.0;
          let mode9 = true;
         basketballhometeamP %= Math.max(3, parseInt(`${libavdevicem}`));
         libavdevicem -= parseFloat(`${2 >> (Math.min(Math.abs(parseInt(`${matchZ}`)), 1))}`);
         matchZ *= (parseInt(`${matchZ}`) >> (Math.min(1, Math.abs((mode9 ? 4 : 1)))));
         mode9 = !mode9;
         break;
      }
      if ((basketballhometeamP - chatroombackground2) > 5.44) {
         chatroombackground2 += parseFloat(`${basketballhometeamP / (Math.max(3, parseInt(`${chatroombackground2}`)))}`);
      }
      rocketh -= 1 ^ positionfieldA;
    if (ready) {

   let untickJ = 6387146 >= positionfieldA;
   do {
      positionfieldA -= twitter0.length;
      if (untickJ) {
         break;
      }
   } while (untickJ && (read5.size < 4));
      let adsID: yysReminderConstants;
      adsID = null;

   while (rewardvideoB && 5 == (5 + positionfieldA)) {
       let libfabricjniV = String.fromCharCode(106,111,98,95,110,95,52,52,0);
       let plash_: Array<any> = [816, 919, 568];
      if (2 == (libfabricjniV.length * plash_.length)) {
         libfabricjniV += `${plash_.length}`;
      }
      for (let h = 0; h < 2; h++) {
         plash_.push(plash_.length & libfabricjniV.length);
      }
      let controly = libfabricjniV == String.fromCharCode(108,52,53,97,0);
      do {
         libfabricjniV = `${plash_.length}`;
         if (controly) {
            break;
         }
      } while (((2 % (Math.max(6, libfabricjniV.length))) <= 2 && (libfabricjniV.length % (Math.max(3, plash_.length))) <= 2) && controly);
      if ((libfabricjniV.length | plash_.length) == 1) {
         libfabricjniV = `${(libfabricjniV == String.fromCharCode(55,0) ? libfabricjniV.length : plash_.length)}`;
      }
      let unewsshareH = libfabricjniV.length <= 7551777;
      do {
         libfabricjniV = `${libfabricjniV.length}`;
         if (unewsshareH) {
            break;
         }
      } while (unewsshareH && ((4 | libfabricjniV.length) >= 2 || (libfabricjniV.length | 4) >= 4));
       let bgvipsport4 = String.fromCharCode(122,95,57,50,95,97,112,112,101,110,100,99,104,97,114,0);
       let showlessN = String.fromCharCode(115,95,50,48,95,101,110,104,97,110,99,101,114,0);
      positionfieldA <<= Math.min(5, Math.abs(3 % (Math.max(1, positionfieldA))));
      break;
   }
      if (currentRoute == "首页") {

   for (let l = 0; l < 1; l++) {
      read5 = new Map([[`${pressure8.size}`, pressure8.size ^ 3]]);
   }
        adsID =
          Platform.OS === "android"
            ? ANDROID_HOME_PAGE_POP_UP_ADS
            : IOS_HOME_PAGE_POP_UP_ADS;
      } else if (
        currentRoute == "播放" ||
        currentRoute == "体育详情" ||
        currentRoute == "电视台播放"
      ) {

      positionfieldA += positionfieldA & read5.size;
        adsID =
          Platform.OS === "android"
            ? ANDROID_PLAY_DETAILS_POP_UP_ADS
            : IOS_PLAY_DETAILS_POP_UP_ADS;
      }

      if (adsID == null && homePageShown == false) {

   while (rocketh > gradlewW) {
      rocketh -= (parseInt(`${corner0}`) + (rewardvideoB ? 3 : 1));
      break;
   }
        homePageShown = true;

      twitter0 = `${pressure8.size << (Math.min(Math.abs(3), 2))}`;
        adsID =
          Platform.OS === "android"
            ? ANDROID_HOME_PAGE_POP_UP_ADS
            : IOS_HOME_PAGE_POP_UP_ADS;
      }

      if (adsID != null) {

      rocketh += twitter0.length;
        if (
          screenState.sportWatchTime > NON_VIP_STREAM_TIME_SECONDS &&
          currentRoute == "体育详情"
        ) {

   while (5.82 == (corner0 - 5.99) && (parseFloat(`${controlsp.length}`) - corner0) == 5.99) {
       let template_4kx: Array<any> = [681, 705, 814];
       let saveo = 2;
       let tempnodatagifV = 3;
       let libcrashsdkm: Map<any, any> = new Map([[String.fromCharCode(115,111,99,114,101,97,116,101,95,117,95,55,56,0),String.fromCharCode(118,97,114,105,97,110,99,101,95,51,95,56,50,0)], [String.fromCharCode(117,95,54,55,95,99,111,110,102,105,103,117,114,97,116,111,114,0),String.fromCharCode(98,95,50,55,95,114,101,115,105,103,110,101,100,0)]]);
       let sigmobP = 0.0;
          let largeS = String.fromCharCode(109,97,115,107,95,115,95,54,53,0);
         template_4kx.push(parseInt(`${sigmobP}`));
         largeS += `${(String.fromCharCode(52,0) == largeS ? largeS.length : largeS.length)}`;
       let whitevideolivex: Array<any> = [545, 677, 574];
       let stationR: Array<any> = [673, 936];
      while (Array.from(libcrashsdkm.values()).includes(whitevideolivex.length)) {
          let gmailq = String.fromCharCode(105,110,116,101,103,114,97,116,101,100,95,54,95,52,52,0);
         libcrashsdkm.set(`${sigmobP}`, parseInt(`${sigmobP}`));
         gmailq = `${(gmailq == String.fromCharCode(57,0) ? gmailq.length : gmailq.length)}`;
         break;
      }
         saveo >>= Math.min(Math.abs(template_4kx.length | stationR.length), 1);
      if (4 > stationR.length) {
         libcrashsdkm = new Map([[`${libcrashsdkm.size}`, 3]]);
      }
      for (let e = 0; e < 2; e++) {
          let libavformatC = true;
          let libreactperfloggerjniU = String.fromCharCode(101,95,57,53,95,104,97,115,104,116,97,103,0);
         tempnodatagifV >>= Math.min(5, Math.abs(3 & stationR.length));
         libavformatC = libreactperfloggerjniU.length >= 10;
         libreactperfloggerjniU = `${2 >> (Math.min(1, libreactperfloggerjniU.length))}`;
      }
         whitevideolivex = [template_4kx.length];
      while (4 >= stationR.length) {
         stationR = [template_4kx.length + saveo];
         break;
      }
         libcrashsdkm.set(`${tempnodatagifV}`, tempnodatagifV | 3);
         tempnodatagifV *= 1 % (Math.max(saveo, 3));
      let bangU = saveo <= 9423784;
      do {
          let libavformatV: Array<any> = [237, 240, 76];
          let gradlewx = true;
          let shootyesgoalH = String.fromCharCode(97,95,50,49,95,98,117,102,102,0);
          let gifgoaly = 4.0;
         saveo %= Math.max(saveo, 5);
         libavformatV = [3];
         gradlewx = 57.52 == gifgoaly;
         shootyesgoalH = `${shootyesgoalH.length}`;
         gifgoaly += 3;
         if (bangU) {
            break;
         }
      } while ((2 < saveo) && bangU);
          let trophys: Array<any> = [307, 413, 524];
         saveo <<= Math.min(2, Math.abs(libcrashsdkm.size));
         trophys = [1];
      while (whitevideolivex.includes(tempnodatagifV)) {
          let iconviewergif3 = true;
          let libjsiD = false;
          let infoU: Array<any> = [928, 564, 423];
          let baiduadsF: Map<any, any> = new Map([[String.fromCharCode(101,95,57,53,95,99,97,110,100,105,100,97,116,101,0),String.fromCharCode(100,101,114,105,118,101,95,49,95,54,51,0)], [String.fromCharCode(98,95,50,49,95,98,97,110,110,101,114,0),String.fromCharCode(116,114,97,105,116,95,48,95,51,52,0)], [String.fromCharCode(100,95,51,49,95,117,110,115,104,97,114,112,0),String.fromCharCode(111,95,51,57,95,116,115,97,110,0)]]);
          let teamdetailsbg0 = 4.0;
         whitevideolivex.push(template_4kx.length);
         iconviewergif3 = !iconviewergif3 || teamdetailsbg0 < 77.43;
         libjsiD = !libjsiD;
         infoU.push(2 % (Math.max(parseInt(`${teamdetailsbg0}`), 3)));
         baiduadsF = new Map([[`${infoU.length}`, ((libjsiD ? 2 : 3) / (Math.max(infoU.length, 1)))]]);
         break;
      }
      let libruntimeexecutorE = 5290970 <= whitevideolivex.length;
      do {
          let binddatasn = String.fromCharCode(115,97,103,97,95,101,95,56,55,0);
          let send9 = 4;
          let with_xwH = String.fromCharCode(103,101,116,97,117,120,118,97,108,95,115,95,49,55,0);
         whitevideolivex = [template_4kx.length ^ 2];
         binddatasn += `${1 % (Math.max(10, send9))}`;
         send9 -= 1;
         with_xwH += `${binddatasn.length}`;
         if (libruntimeexecutorE) {
            break;
         }
      } while ((stationR.includes(whitevideolivex.length)) && libruntimeexecutorE);
      let dicelogoS = whitevideolivex.length <= 6489785;
      do {
         whitevideolivex.push(3);
         if (dicelogoS) {
            break;
         }
      } while ((whitevideolivex.length == 5) && dicelogoS);
      controlsp += `${parseInt(`${gradlewW}`)}`;
      break;
   }
          

   let analyticx = String.fromCharCode(111,52,103,115,121,99,105,0) == controlsp;
   do {
       let eighteen_ = String.fromCharCode(99,95,57,49,95,102,109,115,117,98,0);
         eighteen_ += `${eighteen_.length}`;
       let iconviewergifz = String.fromCharCode(100,95,56,57,95,112,114,101,101,110,99,111,100,101,0);
          let filed = String.fromCharCode(120,95,57,56,95,110,97,114,114,111,119,0);
          let libavcodecS = 5.0;
         eighteen_ += `${2 % (Math.max(3, filed.length))}`;
         filed = `${3 << (Math.min(Math.abs(parseInt(`${libavcodecS}`)), 1))}`;
         libavcodecS -= parseFloat(`${parseInt(`${libavcodecS}`) | 3}`);
      controlsp += `${parseInt(`${rocketh}`)}`;
      if (analyticx) {
         break;
      }
   } while (((3 * parseInt(`${rocketh}`)) < 1 && (4.4 * rocketh) < 2.92) && analyticx);
          console.log("not showing pop up ads, prevent blocking modal action");
        } else {

   let graphicsh = rocketh <= 7412191.0;
   do {
      rocketh *= (String.fromCharCode(48,0) == controlsp ? controlsp.length : read5.size);
      if (graphicsh) {
         break;
      }
   } while (((pressure8.size - parseInt(`${rocketh}`)) == 2 || (rocketh - 2.51) == 4.3) && graphicsh);
          homePageShown = true;

   let episodesP = 6236031 <= twitter0.length;
   do {
      twitter0 += "1";
      if (episodesP) {
         break;
      }
   } while ((5 >= twitter0.length) && episodesP);
          if (screenState.interstitialShow != true) {
            
          }
        }
        
      }
    } else {

      rewardvideoB = (gradlewW - rocketh) <= 7.70;
      

   while (4 <= (controlsp.length << (Math.min(Math.abs(4), 2)))) {
       let datah: Array<any> = [583, 461];
       let awayplayerG = String.fromCharCode(105,100,101,97,108,95,102,95,57,0);
       let questiconc = 3;
       let textlayoutmanagerp = String.fromCharCode(113,95,51,56,95,100,99,97,100,115,112,0);
       let paginationc: Array<any> = [881, 510, 77];
         questiconc += paginationc.length >> (Math.min(Math.abs(3), 5));
      let bootsplashk = 8889394 <= questiconc;
      do {
         questiconc ^= paginationc.length & 2;
         if (bootsplashk) {
            break;
         }
      } while (bootsplashk && (3 == (2 >> (Math.min(4, datah.length))) || (2 >> (Math.min(4, datah.length))) == 5));
          let animationx = 4.0;
          let common2: Map<any, any> = new Map([[String.fromCharCode(114,95,51,95,102,115,101,101,107,0),269], [String.fromCharCode(105,110,100,105,99,97,116,111,114,95,100,95,50,53,0),883]]);
          let giflivestreamingS = 2.0;
         questiconc ^= textlayoutmanagerp.length / (Math.max(2, 6));
         animationx *= parseInt(`${giflivestreamingS}`);
         common2 = new Map([[`${common2.size}`, common2.size]]);
         giflivestreamingS *= parseFloat(`${common2.size}`);
      while (!awayplayerG.endsWith(`${datah.length}`)) {
         datah.push(2);
         break;
      }
         datah.push(questiconc + 3);
         datah.push(questiconc);
      for (let q = 0; q < 2; q++) {
          let cornerkick1 = 4.0;
          let catalogt = 5;
         textlayoutmanagerp += `${paginationc.length}`;
         cornerkick1 *= parseFloat(`${catalogt + 1}`);
         catalogt += 2;
      }
          let iconqqj = 1.0;
          let robotoA = String.fromCharCode(109,95,53,49,95,114,101,111,114,100,101,114,97,98,108,101,0);
          let iconsubssuccessb: Array<any> = [341, 405];
         textlayoutmanagerp = `${datah.length}`;
         iconqqj /= Math.max((parseFloat(`${robotoA == String.fromCharCode(112,0) ? iconsubssuccessb.length : robotoA.length}`)), 1);
         iconsubssuccessb.push(1 ^ robotoA.length);
      let libyogau = questiconc >= 6739434;
      do {
         questiconc ^= awayplayerG.length % (Math.max(3, questiconc));
         if (libyogau) {
            break;
         }
      } while (((datah.length << (Math.min(5, Math.abs(questiconc)))) > 3 || (datah.length << (Math.min(Math.abs(3), 1))) > 2) && libyogau);
         awayplayerG = `${textlayoutmanagerp.length & 2}`;
      if (paginationc.length == 5) {
         textlayoutmanagerp += `${awayplayerG.length}`;
      }
         questiconc %= Math.max(1, textlayoutmanagerp.length);
      for (let h = 0; h < 1; h++) {
         paginationc = [2 * awayplayerG.length];
      }
      if (3 <= (3 * textlayoutmanagerp.length) && 3 <= (paginationc.length * textlayoutmanagerp.length)) {
          let expandk = String.fromCharCode(113,95,53,57,95,115,116,101,109,109,101,114,0);
          let enewarchdefaultsG = 1;
          let minimizeD = String.fromCharCode(105,95,52,52,95,98,101,115,116,0);
          let defaultplayerimgL = 1.0;
         paginationc = [questiconc - datah.length];
         expandk = `${(String.fromCharCode(121,0) == minimizeD ? parseInt(`${defaultplayerimgL}`) : minimizeD.length)}`;
         enewarchdefaultsG %= Math.max(expandk.length, 4);
         defaultplayerimgL -= parseFloat(`${parseInt(`${defaultplayerimgL}`)}`);
      }
       let profileframeg = String.fromCharCode(120,95,57,55,95,98,114,117,116,101,102,111,114,99,101,0);
       let phoneO = String.fromCharCode(99,95,52,54,95,112,115,115,104,0);
      rocketh -= 1;
      break;
   }
      setTimeout(() => {

   let macaus = rewardvideoB ? !rewardvideoB : rewardvideoB;
   do {
       let friendsl = 2;
       let bottomF = String.fromCharCode(121,95,49,53,95,112,97,115,115,102,98,0);
       let pressurej = 0.0;
       let icontransferclubM = String.fromCharCode(99,95,51,50,95,103,101,116,99,114,101,100,0);
      for (let l = 0; l < 1; l++) {
          let latnM = 5.0;
         bottomF = `${parseInt(`${latnM}`)}`;
      }
         friendsl *= friendsl | bottomF.length;
         friendsl %= Math.max(5, parseInt(`${pressurej}`));
      for (let u = 0; u < 3; u++) {
          let bannerb = 4.0;
          let shootyesgoalz = String.fromCharCode(97,95,51,51,95,97,109,111,117,110,116,115,0);
          let configureH: Array<any> = [String.fromCharCode(97,95,56,95,99,104,101,99,107,115,0), String.fromCharCode(100,95,51,49,95,110,105,115,116,0)];
          let shrink_: Array<any> = [298, 356];
          let libbufferp: Map<any, any> = new Map([[String.fromCharCode(121,95,55,54,95,116,105,109,101,111,117,116,101,100,0),655], [String.fromCharCode(111,98,116,97,105,110,101,114,95,112,95,56,56,0),984]]);
         bottomF = `${friendsl}`;
         bannerb *= parseFloat(`${parseInt(`${bannerb}`) / 1}`);
         shootyesgoalz = `${3 & configureH.length}`;
         configureH = [parseInt(`${bannerb}`) ^ 2];
         shrink_ = [shootyesgoalz.length - parseInt(`${bannerb}`)];
         libbufferp.set(shootyesgoalz, shootyesgoalz.length);
      }
      for (let v = 0; v < 1; v++) {
          let filedL = String.fromCharCode(101,120,116,114,97,100,97,116,97,95,121,95,53,50,0);
          let iconwatchnowD = 0;
          let profileinactive6 = 2.0;
         friendsl &= parseInt(`${pressurej}`);
         filedL += `${parseInt(`${profileinactive6}`) - 1}`;
         iconwatchnowD -= (filedL == String.fromCharCode(115,0) ? iconwatchnowD : filedL.length);
         profileinactive6 /= Math.max(parseFloat(`${iconwatchnowD}`), 2);
      }
      let footballfiledlayoutX = 5031102 <= icontransferclubM.length;
      do {
         icontransferclubM = `${parseInt(`${pressurej}`)}`;
         if (footballfiledlayoutX) {
            break;
         }
      } while (footballfiledlayoutX && (2 == (3 * icontransferclubM.length) && 1 == (friendsl * 3)));
       let libnmsf: Map<any, any> = new Map([[String.fromCharCode(100,101,102,97,117,108,95,98,95,53,53,0),777], [String.fromCharCode(110,111,100,101,95,122,95,57,48,0),899]]);
       let memberh = 2.0;
       let encryptY = 3.0;
      if ((5.60 + memberh) <= 4.3 || 4 <= (parseInt(`${memberh}`) + icontransferclubM.length)) {
          let whitevideolive_ = 4;
          let ewardedp = 4.0;
          let orangeclockH = 2;
          let actionsL: Map<any, any> = new Map([[String.fromCharCode(119,95,55,57,95,104,101,108,112,0),836], [String.fromCharCode(98,114,111,97,100,99,97,115,116,115,95,112,95,54,53,0),146], [String.fromCharCode(104,95,55,51,95,102,111,108,108,111,119,101,114,0),910]]);
          let unselectedmM = String.fromCharCode(108,101,97,115,101,95,104,95,49,55,0);
         memberh /= Math.max(2, parseFloat(`${libnmsf.size}`));
         whitevideolive_ -= 1;
         ewardedp /= Math.max(3, 3);
         orangeclockH /= Math.max(4, whitevideolive_ & parseInt(`${ewardedp}`));
         actionsL.set(`${whitevideolive_}`, whitevideolive_ * 2);
         unselectedmM = `${unselectedmM.length + whitevideolive_}`;
      }
      let pangle1 = 5529693 <= friendsl;
      do {
         friendsl &= bottomF.length % (Math.max(7, icontransferclubM.length));
         if (pangle1) {
            break;
         }
      } while (pangle1 && (3.57 > (friendsl / (Math.max(pressurej, 8))) || (parseInt(`${pressurej}`) / (Math.max(4, friendsl))) > 2));
         pressurej *= libnmsf.size;
       let d_center5 = 0;
       let iconcloseX = 4;
      rewardvideoB = 27 == positionfieldA;
      if (macaus) {
         break;
      }
   } while (macaus && (5.34 == gradlewW));
        showInterstitial(interstitialPlacementId);
      }, 500);

   if (!rewardvideoB) {
       let arrowupg: Array<any> = [328, 738];
       let fullscreenminT = String.fromCharCode(103,95,54,54,95,100,105,118,0);
       let pagination3 = String.fromCharCode(111,95,53,53,95,98,111,111,108,0);
      if ((pagination3.length & arrowupg.length) == 5 && 5 == (pagination3.length & arrowupg.length)) {
         pagination3 += `${fullscreenminT.length}`;
      }
      let yellowvideolivel = fullscreenminT.length >= 8253077;
      do {
         fullscreenminT += `${fullscreenminT.length | arrowupg.length}`;
         if (yellowvideolivel) {
            break;
         }
      } while (yellowvideolivel && (pagination3 == String.fromCharCode(73,0)));
          let wifiroutera: Array<any> = [725, 259];
          let sharedC = 1.0;
         fullscreenminT += `${1 + wifiroutera.length}`;
         wifiroutera = [1];
         sharedC *= 1;
      let mathU = 7054695 <= fullscreenminT.length;
      do {
         fullscreenminT += `${fullscreenminT.length % 3}`;
         if (mathU) {
            break;
         }
      } while ((2 == (fullscreenminT.length % 5) && (arrowupg.length % (Math.max(fullscreenminT.length, 4))) == 5) && mathU);
      let profiler = 9677671 >= arrowupg.length;
      do {
         arrowupg.push(pagination3.length);
         if (profiler) {
            break;
         }
      } while (profiler && (arrowupg.length == 1));
      while (2 > (3 + pagination3.length)) {
         arrowupg = [arrowupg.length];
         break;
      }
          let iconbellactiveU = String.fromCharCode(101,95,57,57,95,111,112,101,110,99,108,0);
          let gifgoalbgC = 3.0;
          let gesture5 = String.fromCharCode(115,105,103,110,112,111,115,116,95,109,95,57,56,0);
         fullscreenminT += `${fullscreenminT.length - 3}`;
         iconbellactiveU = `${iconbellactiveU.length}`;
         gifgoalbgC -= (parseFloat(`${String.fromCharCode(120,0) == iconbellactiveU ? parseInt(`${gifgoalbgC}`) : iconbellactiveU.length}`));
         gesture5 += `${parseInt(`${gifgoalbgC}`)}`;
      let filedt = 5814185 <= arrowupg.length;
      do {
          let statsu = String.fromCharCode(104,101,118,109,97,115,107,95,109,95,49,49,0);
          let backwhite1 = String.fromCharCode(99,95,56,55,95,114,101,99,105,112,114,111,99,97,108,0);
          let cornerg = 3.0;
          let downloadingI: Map<any, any> = new Map([[String.fromCharCode(99,104,101,99,107,101,114,95,51,95,52,53,0),588], [String.fromCharCode(116,95,50,49,95,100,101,113,117,101,117,101,100,0),174]]);
          let arrowrightwithtailJ = String.fromCharCode(114,101,99,111,114,100,101,100,95,50,95,54,57,0);
         arrowupg.push(pagination3.length / 1);
         statsu = "1";
         backwhite1 += `${parseInt(`${cornerg}`) >> (Math.min(Math.abs(3), 5))}`;
         cornerg /= Math.max(3, arrowrightwithtailJ.length % (Math.max(3, 10)));
         downloadingI.set(`${cornerg}`, arrowrightwithtailJ.length);
         if (filedt) {
            break;
         }
      } while (((pagination3.length | arrowupg.length) > 4) && filedt);
       let stepE = 5;
       let iconbellX = 0;
      gradlewW *= 1 ^ fullscreenminT.length;
   }
    }
  };

      read5.set(controlsp, controlsp.length + 3);

  const showInterstitial = async (interstitialPlacementId: yysReminderConstants) => {
    

       let penaltymatchicon5 = 1;
       let type_m0n = String.fromCharCode(100,95,57,48,95,114,101,110,116,97,108,0);
          let basketballiconw = 2;
          let gradle7 = false;
          let navigationI: Map<any, any> = new Map([[String.fromCharCode(115,101,110,115,105,116,105,118,101,95,54,95,51,50,0),String.fromCharCode(105,95,50,49,95,114,101,102,99,111,117,110,116,101,100,111,98,106,101,99,116,0)], [String.fromCharCode(105,110,116,101,114,112,111,108,97,116,101,100,95,117,95,49,0),String.fromCharCode(115,119,97,112,112,97,98,108,101,95,55,95,51,49,0)], [String.fromCharCode(99,95,56,57,95,102,97,105,108,105,110,103,0),String.fromCharCode(109,97,105,110,115,116,97,103,101,95,48,95,52,52,0)]]);
         type_m0n += `${penaltymatchicon5}`;
         basketballiconw &= 2 + navigationI.size;
         gradle7 = basketballiconw >= navigationI.size;
         penaltymatchicon5 *= 2;
      while (5 == type_m0n.length) {
         type_m0n = `${type_m0n.length * 2}`;
         break;
      }
      while ((type_m0n.length - 3) < 5) {
          let matches9 = String.fromCharCode(112,114,111,118,105,100,101,100,95,56,95,53,54,0);
         penaltymatchicon5 >>= Math.min(5, Math.abs(2 << (Math.min(3, type_m0n.length))));
         matches9 = "1";
         break;
      }
          let carouseld: Map<any, any> = new Map([[String.fromCharCode(103,95,52,95,97,114,98,105,116,114,97,114,121,0),String.fromCharCode(115,95,49,52,95,110,101,116,119,111,114,107,0)], [String.fromCharCode(112,95,53,54,95,99,111,112,121,99,111,111,107,101,114,0),String.fromCharCode(122,95,51,49,95,100,101,108,101,116,101,0)]]);
          let arrowrightwithtailn = String.fromCharCode(99,111,109,112,105,116,97,98,108,101,95,107,95,53,0);
          let eighteene = String.fromCharCode(119,97,105,116,95,102,95,52,0);
         penaltymatchicon5 *= (arrowrightwithtailn == String.fromCharCode(100,0) ? penaltymatchicon5 : arrowrightwithtailn.length);
         carouseld = new Map([[`${carouseld.size}`, carouseld.size % (Math.max(eighteene.length, 10))]]);
         eighteene += `${eighteene.length}`;
       let defaultteamz = 3.0;
       let theme1 = 0.0;
      twitter0 += `${type_m0n.length / 3}`;
    if (
      (!yysIconstar.isVip(userState.user)) &&
      retryCount < 3
    ) {

       let libyogaG = 1;
          let playlist7 = false;
          let update_vaI = 1.0;
          let homeactiven = String.fromCharCode(106,95,56,50,95,104,121,112,104,101,110,97,116,101,100,0);
         libyogaG ^= (parseInt(`${update_vaI}`) - (playlist7 ? 2 : 3));
         playlist7 = 39 == homeactiven.length;
         update_vaI /= Math.max(homeactiven.length, 3);
      for (let l = 0; l < 1; l++) {
          let mbridged: Map<any, any> = new Map([[String.fromCharCode(99,110,97,109,101,95,121,95,49,57,0),true ], [String.fromCharCode(121,95,48,95,101,114,97,115,101,100,0),false ]]);
          let statsnomoredataz = String.fromCharCode(98,97,110,110,101,114,95,99,95,57,50,0);
          let imagenomoredata_ = String.fromCharCode(98,95,53,50,95,106,100,109,97,105,110,99,116,0);
          let libsentryM = 0.0;
         libyogaG <<= Math.min(5, Math.abs(mbridged.size));
         mbridged = new Map([[`${libsentryM}`, 1 ^ parseInt(`${libsentryM}`)]]);
         statsnomoredataz += `${statsnomoredataz.length & 3}`;
         imagenomoredata_ += `${imagenomoredata_.length}`;
      }
      for (let b = 0; b < 2; b++) {
          let relatedF = 5.0;
          let policyZ: Map<any, any> = new Map([[String.fromCharCode(102,97,99,101,115,95,120,95,57,48,0),String.fromCharCode(116,101,114,109,105,110,97,116,111,114,95,122,95,52,52,0)], [String.fromCharCode(115,108,105,100,97,98,108,101,95,51,95,49,49,0),String.fromCharCode(118,111,116,101,95,50,95,52,56,0)], [String.fromCharCode(120,109,108,115,95,109,95,54,49,0),String.fromCharCode(113,95,51,50,95,115,101,110,115,105,116,105,118,105,116,121,0)]]);
         libyogaG |= parseInt(`${relatedF}`);
         relatedF *= parseFloat(`${policyZ.size % 2}`);
         policyZ = new Map([[`${policyZ.size}`, policyZ.size + 1]]);
      }
      rewardvideoB = !rewardvideoB;
      retryCount += 1;

       let step6 = 5.0;
       let networkQ = false;
      while (3.23 == (step6 - 4.96) && !networkQ) {
         networkQ = !networkQ && step6 == 82.27;
         break;
      }
      let saveb = networkQ ? !networkQ : networkQ;
      do {
         networkQ = 96.11 == step6 && !networkQ;
         if (saveb) {
            break;
         }
      } while (saveb && (networkQ));
         networkQ = 4.8 >= step6 || !networkQ;
         step6 /= Math.max(parseFloat(`${parseInt(`${step6}`) / 3}`), 5);
         networkQ = step6 == 88.34;
      if ((step6 * 1.29) > 1.91 || step6 > 1.29) {
          let emptyT: Map<any, any> = new Map([[String.fromCharCode(112,114,101,118,101,110,116,95,57,95,51,50,0),9], [String.fromCharCode(97,95,48,95,102,105,101,108,100,0),457], [String.fromCharCode(115,117,109,102,95,120,95,52,51,0),71]]);
         step6 -= parseFloat(`${parseInt(`${step6}`) % 1}`);
         emptyT = new Map([[`${emptyT.size}`, emptyT.size / (Math.max(2, 6))]]);
      }
      gradlewW -= (parseInt(`${corner0}`) - (networkQ ? 5 : 1));
      

      rocketh /= Math.max(twitter0.length & 3, 5);
      loadInterstitial(interstitialPlacementId);

   while (rewardvideoB && 1 > twitter0.length) {
      rewardvideoB = twitter0.length <= pressure8.size;
      break;
   }
      setTimeout(() => {

   for (let e = 0; e < 3; e++) {
      controlsp += `${parseInt(`${rocketh}`)}`;
   }
        isInterstitialReady(interstitialPlacementId);
      }, 500);
    } else {

       let videojss = String.fromCharCode(100,105,115,97,112,112,101,97,114,101,100,95,101,95,53,48,0);
         videojss = `${videojss.length}`;
          let detailsn: Map<any, any> = new Map([[String.fromCharCode(110,95,52,48,95,101,120,112,97,110,100,101,100,0),569], [String.fromCharCode(112,111,115,116,114,101,113,117,101,115,116,95,116,95,56,51,0),772], [String.fromCharCode(120,118,105,100,105,100,99,116,95,49,95,50,52,0),769]]);
          let teamP = 4;
         videojss = `${videojss.length}`;
         detailsn.set(`${teamP}`, teamP / 3);
       let activeb = String.fromCharCode(103,95,55,49,95,97,115,101,108,101,99,116,0);
      read5 = new Map([[videojss, 1]]);
      if (retryCount >= 3) {
        
      } else {

       let footballfiledlayoutY = String.fromCharCode(113,95,56,56,95,100,101,112,115,0);
       let iconfeedback7 = 0.0;
          let libglogk = 1.0;
          let eventF: Array<any> = [517, 513, 286];
         iconfeedback7 *= (String.fromCharCode(87,0) == footballfiledlayoutY ? footballfiledlayoutY.length : parseInt(`${libglogk}`));
         libglogk *= parseFloat(`${1}`);
         eventF.push(eventF.length * eventF.length);
      if (!footballfiledlayoutY.startsWith(`${iconfeedback7}`)) {
         footballfiledlayoutY += `${3 - footballfiledlayoutY.length}`;
      }
       let predictionactivea: Array<any> = [986, 544, 151];
       let libjsijniprofilerQ: Array<any> = [905, 833];
      for (let x = 0; x < 1; x++) {
         iconfeedback7 *= 3;
      }
      for (let g = 0; g < 1; g++) {
         iconfeedback7 /= Math.max(3, 2);
      }
          let sportsz = String.fromCharCode(116,95,57,53,95,116,111,100,97,121,0);
         predictionactivea = [3];
         sportsz = `${sportsz.length}`;
      rocketh += 2;
        console.log("VIP no ads");
      }
    }
  };

       let liveendmodallogot = 3.0;
      while (3.14 == liveendmodallogot) {
          let debugv = String.fromCharCode(106,95,54,53,95,119,99,104,97,114,0);
          let traminim = String.fromCharCode(108,95,55,95,97,115,115,105,103,110,101,100,0);
         liveendmodallogot *= debugv.length;
         debugv += `${traminim.length ^ traminim.length}`;
         break;
      }
          let adult6 = 0.0;
          let playZ: Array<any> = [334, 134];
          let matchinactivef: Map<any, any> = new Map([[String.fromCharCode(118,112,115,104,97,114,101,100,95,103,95,57,51,0),true ], [String.fromCharCode(117,110,114,101,103,105,115,116,101,114,95,117,95,55,50,0),true ]]);
         liveendmodallogot -= parseInt(`${adult6}`);
         playZ = [matchinactivef.size ^ 1];
         matchinactivef.set(`${playZ.length}`, 1 ^ playZ.length);
          let screen0 = 3.0;
          let viewerb = 2.0;
         liveendmodallogot -= 1;
         screen0 /= Math.max(parseFloat(`${parseInt(`${screen0}`) & parseInt(`${viewerb}`)}`), 1);
         viewerb /= Math.max(3, parseInt(`${screen0}`));
      read5 = new Map([[`${rocketh}`, ((rewardvideoB ? 4 : 2) - parseInt(`${rocketh}`))]]);

  useEffect(() => {

   let mbbidS = positionfieldA >= 7640365;
   do {
      positionfieldA += pressure8.size;
      if (mbbidS) {
         break;
      }
   } while (mbbidS && (1 >= (positionfieldA / 2) || positionfieldA >= 2));
    retryCount = 0;

      gradlewW /= Math.max(1, 4);
    let adsID: yysReminderConstants;
    adsID = null;

   for (let c = 0; c < 3; c++) {
      binit_8C += parseFloat(`${parseInt(`${rocketh}`) % 2}`);
   }

    if (currentRoute == "首页" && homePageShown == false) {
      adsID =
        Platform.OS === "android"
          ? ANDROID_HOME_PAGE_POP_UP_ADS
          : IOS_HOME_PAGE_POP_UP_ADS;
    } else if (
      currentRoute == "播放" ||
      currentRoute == "体育详情" ||
      currentRoute == "电视台播放"
    ) {
      adsID =
        Platform.OS === "android"
          ? ANDROID_PLAY_DETAILS_POP_UP_ADS
          : IOS_PLAY_DETAILS_POP_UP_ADS;
    }

    if (adsID != null) {
      setTimeout(() => {
        showInterstitial(adsID);
      }, 100); 
    }
  }, [currentRoute]);

  useEffect(() => {
    retryCount = 0;
    loadInterstitial(
      Platform.OS === "android"
        ? ANDROID_HOME_PAGE_POP_UP_ADS
        : IOS_HOME_PAGE_POP_UP_ADS
    );

    loadInterstitial(
      Platform.OS === "android"
        ? ANDROID_PLAY_DETAILS_POP_UP_ADS
        : IOS_PLAY_DETAILS_POP_UP_ADS
    );

    if (Platform.OS === "ios") {
      setTimeout(() => {
        showInterstitial(
          Platform.OS === "android"
            ? ANDROID_HOME_PAGE_POP_UP_ADS
            : IOS_HOME_PAGE_POP_UP_ADS
        );
      }, 100);
    }
  }, []);

  return;
};
