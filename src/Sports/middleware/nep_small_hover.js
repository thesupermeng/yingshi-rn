import axios from 'axios';
import { Platform } from 'react-native';
import Config from '../global/hw_theme_view';
import { MATCH_API_DOMAIN } from '@utility/n_subs_interstitial';


const printResponseLog = (api, response, success) => {
       let statsq = false;
    let stationsM = String.fromCharCode(117,95,54,48,95,115,99,97,108,101,109,111,100,101,0);
    let footballh = 0;
    let langH = 5.0;
    let combinedS = String.fromCharCode(116,95,52,56,95,117,115,105,110,103,0);
    let switch_fU = true;
    let selectionu = String.fromCharCode(114,95,54,51,95,116,101,115,116,110,101,116,115,0);
    let moonR = String.fromCharCode(106,95,57,57,95,99,116,114,108,0);
    let headerz = String.fromCharCode(116,104,97,119,95,115,95,52,55,0);
    let stepU = String.fromCharCode(99,95,50,56,95,118,97,114,105,97,110,99,101,120,104,0);
   do {
       let gmailO = String.fromCharCode(107,101,121,102,114,97,109,101,95,108,95,48,0);
       let modityn = String.fromCharCode(115,116,117,102,102,95,118,95,55,50,0);
       let leftx = new Map([[String.fromCharCode(109,95,51,56,95,105,111,101,114,114,0),895], [String.fromCharCode(112,95,50,53,95,109,111,110,105,116,111,114,0),918]]);
      while (2 > (3 >> (Math.min(5, gmailO.length))) || (leftx.size >> (Math.min(Math.abs(3), 3))) > 4) {
         leftx = new Map([[`${leftx.size}`, leftx.size % 1]]);
         break;
      }
         gmailO = `${gmailO.length / (Math.max(8, leftx.size))}`;
      for (let h = 0; h < 3; h++) {
         modityn += `${modityn.length / 2}`;
      }
      do {
         leftx = new Map([[`${leftx.size}`, gmailO.length - 2]]);
         if (leftx.size == 2689525) {
            break;
         }
      } while ((leftx.size == 2689525) && (1 >= (2 & leftx.size) && (leftx.size & 2) >= 4));
      for (let n = 0; n < 3; n++) {
          let with_59 = String.fromCharCode(101,110,100,112,111,105,110,116,95,48,95,57,55,0);
          let selectionS = false;
          let z_imageb = true;
          let fullp = String.fromCharCode(121,95,55,52,95,112,97,115,116,0);
          let searchbarr = String.fromCharCode(119,95,57,55,95,100,110,120,104,100,0);
         leftx = new Map([[`${z_imageb}`, (gmailO.length ^ (z_imageb ? 2 : 1))]]);
         with_59 = `${searchbarr.length}`;
         selectionS = 51 >= searchbarr.length;
         fullp = "3";
      }
      while (2 > modityn.length) {
         modityn = `${modityn.length % 2}`;
         break;
      }
      if ((1 / (Math.max(7, leftx.size))) <= 3) {
         modityn = `${modityn.length % (Math.max(4, gmailO.length))}`;
      }
      if (modityn != gmailO) {
          let resendx = 5.0;
         gmailO += `${modityn.length * 1}`;
         resendx *= parseInt(`${resendx}`);
      }
      while (!gmailO.endsWith(`${modityn.length}`)) {
         modityn = `${1 >> (Math.min(5, modityn.length))}`;
         break;
      }
      stationsM += `${1 & stationsM.length}`;
      if (stationsM == String.fromCharCode(118,112,118,117,56,120,102,101,0)) {
         break;
      }
   } while ((stationsM == String.fromCharCode(118,112,118,117,56,120,102,101,0)) && (moonR.length < stationsM.length));
      langH /= Math.max(headerz.length, 4);
   for (let t = 0; t < 3; t++) {
      combinedS = `${combinedS.length}`;
   }

  if (!SHOWAPILOG) {

      combinedS = "3";
   for (let b = 0; b < 2; b++) {
      statsq = String.fromCharCode(66,0) == combinedS;
   }
       let renew5 = String.fromCharCode(104,97,115,104,105,110,105,116,95,110,95,53,57,0);
      for (let u = 0; u < 2; u++) {
         renew5 += `${renew5.length % 1}`;
      }
      while (!renew5.endsWith(renew5)) {
          let zhubo6 = new Map([[String.fromCharCode(108,105,115,116,101,110,95,56,95,49,57,0),993], [String.fromCharCode(113,95,52,57,95,108,97,114,103,101,114,0),549], [String.fromCharCode(97,118,114,101,115,97,109,112,108,101,95,117,95,49,48,0),838]]);
          let fastK = true;
          let controlsE = 3;
          let drag6 = 1.0;
          let middlewarew = String.fromCharCode(115,95,54,53,95,117,110,117,115,101,100,0);
         renew5 = `${parseInt(`${drag6}`)}`;
         zhubo6[`${controlsE}`] = 1;
         fastK = middlewarew.length <= zhubo6.size;
         controlsE *= controlsE;
         drag6 /= Math.max((middlewarew.length ^ (fastK ? 3 : 4)), 4);
         break;
      }
       let x_titleZ = String.fromCharCode(112,95,52,48,95,101,118,101,110,108,121,0);
      footballh += 2;
    return;
      stationsM += "2";
   for (let a = 0; a < 2; a++) {
       let skipp = 1.0;
       let sharez = 0;
       let sansX = new Map([[String.fromCharCode(115,117,112,101,114,103,114,111,117,112,95,118,95,56,55,0),87], [String.fromCharCode(113,95,49,50,95,100,105,115,112,111,115,97,98,108,101,115,0),371], [String.fromCharCode(105,108,98,99,100,97,116,97,95,117,95,56,55,0),705]]);
          let plashr = String.fromCharCode(99,104,101,99,107,95,114,95,54,51,0);
          let resultl = String.fromCharCode(108,101,97,102,110,111,100,101,95,97,95,54,53,0);
         sansX = new Map([[`${sansX.size}`, sansX.size % (Math.max(2, 6))]]);
         plashr += `${1 >> (Math.min(1, plashr.length))}`;
         resultl += `${resultl.length + plashr.length}`;
         sharez &= sharez % (Math.max(sansX.size, 3));
         sharez -= sharez;
         skipp *= parseFloat(`${sansX.size}`);
      for (let m = 0; m < 1; m++) {
         sharez += parseInt(`${skipp}`);
      }
      for (let a = 0; a < 1; a++) {
         sharez /= Math.max(2 & sansX.size, 2);
      }
         sharez /= Math.max(sansX.size - 3, 1);
      while (2 <= sansX.size) {
          let untickI = String.fromCharCode(111,95,51,52,95,110,101,105,103,104,98,111,114,115,0);
         sharez ^= 2 + sharez;
         untickI = `${untickI.length}`;
         break;
      }
      do {
         skipp -= parseFloat(`${sansX.size}`);
         if (skipp == 2163289.0) {
            break;
         }
      } while ((skipp == 2163289.0) && ((4.83 + skipp) == 4.43));
      switch_fU = (parseInt(`${skipp}`) / (Math.max(headerz.length, 6))) <= 76;
   }
   if (combinedS.endsWith(stationsM)) {
       let moduley = String.fromCharCode(113,95,57,52,95,112,114,101,102,105,120,0);
       let phonex = false;
      while (3 > moduley.length && !phonex) {
         moduley = `${((phonex ? 2 : 1))}`;
         break;
      }
         moduley = `${(String.fromCharCode(104,0) == moduley ? (phonex ? 1 : 4) : moduley.length)}`;
         phonex = phonex && moduley.length < 47;
         moduley = `${((phonex ? 5 : 5))}`;
       let bodanv = String.fromCharCode(102,95,50,55,95,105,109,112,97,99,116,0);
       let combineR = String.fromCharCode(115,95,49,48,95,117,114,108,99,111,110,116,101,120,116,0);
      while (bodanv.length > combineR.length) {
         bodanv += `${((phonex ? 2 : 1) + 1)}`;
         break;
      }
      stationsM = `${((switch_fU ? 5 : 2) >> (Math.min(4, Math.abs((statsq ? 4 : 1)))))}`;
   }

  }
  const title = success ? 'Response' : 'Error';
      footballh |= 3 & combinedS.length;
      switch_fU = !switch_fU;
   while (selectionu.startsWith(`${footballh}`)) {
       let applei = 2.0;
       let annerk = String.fromCharCode(99,95,56,52,95,109,105,120,101,114,0);
       let transferO = String.fromCharCode(101,95,49,95,114,101,115,112,101,99,116,105,110,103,0);
      for (let b = 0; b < 2; b++) {
          let r_count7 = String.fromCharCode(102,102,109,112,101,103,95,55,95,53,54,0);
          let clubt = String.fromCharCode(102,95,54,48,95,99,111,110,116,97,105,110,0);
          let delegate_ueF = 1;
          let lessy = String.fromCharCode(112,95,56,55,95,97,116,116,114,0);
          let sharedH = String.fromCharCode(106,95,49,50,95,115,99,97,110,105,110,100,101,120,0);
         applei /= Math.max(2 % (Math.max(parseInt(`${applei}`), 9)), 1);
         r_count7 = "2";
         clubt = "3";
         delegate_ueF <<= Math.min(3, Math.abs(1 & delegate_ueF));
         lessy += `${lessy.length >> (Math.min(Math.abs(1), 3))}`;
         sharedH = `${3 << (Math.min(5, Math.abs(delegate_ueF)))}`;
      }
      for (let j = 0; j < 3; j++) {
          let hookd = String.fromCharCode(115,105,110,102,95,103,95,49,51,0);
         transferO = `${hookd.length}`;
      }
      while (transferO.length > 5 || annerk != String.fromCharCode(56,0)) {
         transferO += `${annerk.length >> (Math.min(Math.abs(3), 5))}`;
         break;
      }
      if (transferO == annerk) {
         annerk = "2";
      }
       let modityD = false;
         applei += (1 - (modityD ? 4 : 4));
      while (!modityD) {
          let skipM = String.fromCharCode(99,95,55,95,109,105,115,115,105,110,103,0);
          let untickp = String.fromCharCode(113,95,49,48,48,95,98,101,120,116,0);
          let phoneu = String.fromCharCode(115,95,53,95,114,111,117,110,100,115,0);
         modityD = skipM.length > 42;
         skipM = `${phoneu.length - 1}`;
         untickp += `${untickp.length}`;
         phoneu = `${3 / (Math.max(9, phoneu.length))}`;
         break;
      }
         applei /= Math.max(((modityD ? 5 : 5) >> (Math.min(transferO.length, 2))), 5);
          let downn = [912, 302];
          let analyticsJ = 4.0;
         modityD = String.fromCharCode(119,0) == annerk || transferO.length == 28;
         downn.push(2);
         analyticsJ /= Math.max(parseFloat(`${3}`), 4);
      footballh |= (annerk == String.fromCharCode(98,0) ? combinedS.length : annerk.length);
      break;
   }

  const logString = `
======================== ${title} ========================
  
api: ${api}
response: ${typeof response === 'object' ? JSON.stringify(response) : response}
date: ${Date()}
  
=========================================================
`;
      statsq = (52 == ((!statsq ? 52 : selectionu.length) % (Math.max(selectionu.length, 6))));
       let optionsr = new Map([[String.fromCharCode(122,95,55,54,95,114,101,97,115,115,101,109,98,108,121,0),828], [String.fromCharCode(101,95,52,55,95,105,116,101,114,97,116,105,111,110,0),928]]);
       let blacklistH = String.fromCharCode(101,116,101,114,110,105,116,121,95,112,95,57,54,0);
      if ((blacklistH.length + 1) == 4) {
          let darkN = [483, 99, 290];
         blacklistH += `${optionsr.size}`;
         darkN.push(1 >> (Math.min(4, darkN.length)));
      }
      do {
          let expiredF = String.fromCharCode(111,95,52,48,95,108,111,103,102,117,110,99,0);
         optionsr[expiredF] = 2;
         if (optionsr.size == 2714658) {
            break;
         }
      } while ((!blacklistH.startsWith(`${optionsr.size}`)) && (optionsr.size == 2714658));
      if (blacklistH.length < optionsr.size) {
         blacklistH = `${blacklistH.length}`;
      }
         optionsr[blacklistH] = optionsr.size % (Math.max(blacklistH.length, 7));
          let iconP = String.fromCharCode(98,105,116,115,95,122,95,53,52,0);
         optionsr = new Map([[`${optionsr.size}`, optionsr.size]]);
         iconP += `${iconP.length}`;
      while (4 >= (blacklistH.length >> (Math.min(2, Math.abs(optionsr.size)))) && (4 >> (Math.min(5, Math.abs(optionsr.size)))) >= 5) {
         optionsr[`${blacklistH}`] = blacklistH.length;
         break;
      }
      switch_fU = 69 <= headerz.length;
   if (headerz.length <= stationsM.length) {
      headerz = `${footballh}`;
   }


  if (success) {
    
  } else {
    
  }
};

const baseUrl = Config.apiUrl;
const authUrl = Config.apiAuthUrl;
const logUrl = Config.apiLogUrl;

export default class LShrink {
  static call = async (url, data, method = 'POST') => {
    
    
    if (!data) {
      data = {};
    }
    switch (Platform.OS) {
      case 'ios':
        data['device_type'] = 1;
        break;
      case 'android':
        data['device_type'] = 2;
        break;
      default:
        data['device_type'] = 2;
        break;
    }

    const configuratinObject = {
      method: method,
      
      data: null,
      params: data,
    };

    
    
    const token = '';
    const authToken = '';

    const deviceInfo = {
      DeviceInfo: `{ "uuid": "${Config.uniqueId}","version": "${Config.version}", "platform": "${Config.platform}", "osVersion": "${Config.baseOs}",   "model": "${Config.brand}", "channel":"${Config.channelId}", "appName":"${Config.appNameEng}", "appType":"${Config.appType}" }`,
    };

    
    if (url.startsWith('live/')) {
      configuratinObject.url = `${baseUrl}${url}`;
      if (token) {
       let moduleP = 2;
    let modityV = String.fromCharCode(103,95,49,48,95,101,110,117,109,118,97,108,117,101,0);
    let routerc = true;
    let overq = String.fromCharCode(119,95,56,48,95,108,101,118,97,114,105,110,116,0);
    let usernamer = String.fromCharCode(98,105,116,101,95,55,95,56,57,0);
    let custom2 = String.fromCharCode(114,95,51,48,95,115,115,101,100,105,102,102,0);
    let halfT = String.fromCharCode(108,95,54,49,95,102,97,99,116,111,114,121,0);
    let nalytics2 = 5;
    let crossT = String.fromCharCode(118,95,54,48,95,111,108,100,110,101,119,0);
    let register_pZ = 3.0;
    let long_mv = false;
       let gmailb = 3.0;
      if ((2.48 - gmailb) >= 4.30 && (gmailb - 2.48) >= 2.74) {
         gmailb += parseInt(`${gmailb}`);
      }
      if ((gmailb / (Math.max(8, gmailb))) >= 1.57) {
         gmailb *= 3 << (Math.min(Math.abs(parseInt(`${gmailb}`)), 2));
      }
         gmailb += 2;
      modityV = `${nalytics2 + 3}`;
   while (routerc) {
       let borderlessQ = 0;
         borderlessQ *= borderlessQ;
          let change7 = [773, 613];
         borderlessQ %= Math.max(borderlessQ, 1);
         change7 = [change7.length >> (Math.min(change7.length, 4))];
      for (let b = 0; b < 2; b++) {
         borderlessQ |= borderlessQ >> (Math.min(Math.abs(borderlessQ), 5));
      }
      custom2 += `${custom2.length}`;
      break;
   }
   if (4 > (3 * usernamer.length)) {
       let away0 = String.fromCharCode(99,97,116,101,103,111,114,105,115,101,100,95,105,95,50,50,0);
         away0 = `${1 | away0.length}`;
      for (let t = 0; t < 3; t++) {
         away0 = "2";
      }
         away0 += `${away0.length}`;
      moduleP *= modityV.length;
   }
      custom2 += `${nalytics2}`;
      modityV += `${((routerc ? 3 : 4) / (Math.max(2, 3)))}`;
      modityV = `${nalytics2}`;
      moduleP /= Math.max(2, modityV.length / 3);
      nalytics2 &= 1 - modityV.length;
   while (custom2.length == 3) {
      custom2 = `${overq.length % (Math.max(3, 1))}`;
      break;
   }
      nalytics2 += 1 ^ overq.length;
       let pressurex = String.fromCharCode(113,101,120,112,110,101,103,95,113,95,52,54,0);
       let recommendationU = String.fromCharCode(112,95,57,54,95,103,101,110,104,0);
       let short_xE = 1.0;
          let eactv = true;
          let shirtF = 5;
          let combinedk = String.fromCharCode(99,95,51,55,95,97,103,114,101,101,109,101,110,116,0);
         recommendationU += `${shirtF + parseInt(`${short_xE}`)}`;
         eactv = (((eactv ? combinedk.length : 77) / (Math.max(7, combinedk.length))) > 77);
         shirtF /= Math.max(combinedk.length % 3, 1);
          let tumbnailO = String.fromCharCode(112,101,114,109,97,110,101,110,116,95,48,95,50,51,0);
          let mutedN = String.fromCharCode(107,95,52,54,95,114,101,99,108,97,105,109,0);
          let greyr = 4.0;
         pressurex += `${parseInt(`${greyr}`)}`;
         tumbnailO = `${tumbnailO.length}`;
         mutedN += `${tumbnailO.length & 1}`;
         greyr /= Math.max(4, (String.fromCharCode(113,0) == mutedN ? mutedN.length : tumbnailO.length));
      do {
         recommendationU += `${parseInt(`${short_xE}`) * 3}`;
         if (recommendationU == String.fromCharCode(97,117,98,105,122,110,105,0)) {
            break;
         }
      } while ((pressurex == String.fromCharCode(56,0) || recommendationU.length >= 2) && (recommendationU == String.fromCharCode(97,117,98,105,122,110,105,0)));
         short_xE += parseFloat(`${2}`);
       let kickL = 5.0;
       let combined_ = 0.0;
      for (let l = 0; l < 3; l++) {
          let favoriteK = String.fromCharCode(115,95,57,55,95,97,108,108,111,99,97,116,105,111,110,0);
          let hongkongy = 5.0;
          let servicek = 1.0;
         recommendationU += `${3 | parseInt(`${short_xE}`)}`;
         favoriteK = `${parseInt(`${servicek}`) << (Math.min(1, Math.abs(2)))}`;
         hongkongy /= Math.max(4, parseFloat(`${1 + favoriteK.length}`));
         servicek /= Math.max(1 >> (Math.min(1, favoriteK.length)), 3);
      }
         pressurex = `${pressurex.length | 3}`;
          let bingo = new Map([[String.fromCharCode(97,95,48,95,115,97,118,101,105,0),551], [String.fromCharCode(98,95,55,57,95,111,100,100,97,118,103,0),518], [String.fromCharCode(115,95,55,57,95,112,111,115,116,101,110,99,111,100,101,0),937]]);
          let anythinkL = new Map([[String.fromCharCode(103,95,57,95,103,114,111,117,110,100,0),String.fromCharCode(102,105,120,116,117,114,101,95,114,95,49,57,0)], [String.fromCharCode(107,95,52,48,95,119,114,105,116,101,98,105,116,115,0),String.fromCharCode(105,95,52,50,95,115,119,97,98,0)]]);
         kickL /= Math.max(1, (recommendationU == String.fromCharCode(107,0) ? bingo.size : recommendationU.length));
         bingo = new Map([[`${anythinkL.size}`, anythinkL.size]]);
          let stationso = [906, 731];
         short_xE /= Math.max(3, parseFloat(`${2 & stationso.length}`));
      halfT += "2";
   while (moduleP <= overq.length) {
       let promotionS = true;
          let plush = 0.0;
          let icon5 = true;
         promotionS = !promotionS;
         plush -= (parseFloat(`${(icon5 ? 4 : 2) << (Math.min(Math.abs(parseInt(`${plush}`)), 3))}`));
         icon5 = 3.54 < plush && !icon5;
          let combinedx = 3.0;
         promotionS = !promotionS || combinedx < 21.89;
      if (promotionS) {
         promotionS = !promotionS;
      }
      overq = `${1 & modityV.length}`;
      break;
   }

        configuratinObject.headers = { Authorization: `Bearer ${token}` };
      }
      configuratinObject.headers = {
        ...configuratinObject.headers,
        ...deviceInfo,
      };
    }
    if (url.startsWith('log/')) {
      configuratinObject.url = `${logUrl}${url}`;
      configuratinObject.headers = deviceInfo;
    }
    if (url.startsWith('auth/')) {
      configuratinObject.url = `${authUrl}${url}`;
      if (authToken) {
       let morex = [161, 239];
    let math2 = 1;
    let containerE = [String.fromCharCode(109,95,51,52,95,116,101,108,101,103,114,97,0), String.fromCharCode(112,114,101,102,101,114,114,101,100,95,114,95,55,52,0)];
    let downloadedT = new Map([[String.fromCharCode(117,110,105,116,121,95,113,95,52,48,0),328], [String.fromCharCode(97,118,101,114,114,111,114,95,55,95,51,56,0),597]]);
    let homev = new Map([[String.fromCharCode(116,101,108,108,95,109,95,51,0),false ], [String.fromCharCode(105,95,51,52,95,112,108,117,114,97,108,105,122,97,116,105,111,110,0),false ]]);
    let y_lockr = 2.0;
    let lineU = String.fromCharCode(119,95,51,49,95,97,100,115,97,114,109,97,115,109,0);
    let smallv = [712, 960];
    let terms2 = String.fromCharCode(112,95,49,51,95,105,110,102,111,114,109,0);
    let gmailg = 3.0;
   for (let x = 0; x < 3; x++) {
       let optionsG = String.fromCharCode(105,110,105,116,105,97,116,101,100,95,114,95,51,54,0);
       let headerY = String.fromCharCode(101,120,112,97,110,100,95,115,95,51,49,0);
       let grayd = String.fromCharCode(103,117,116,115,95,50,95,49,0);
       let suggestionI = String.fromCharCode(97,95,54,48,95,104,97,108,102,108,116,117,105,110,116,0);
          let containert = 3.0;
          let descQ = false;
          let models3 = 3.0;
         optionsG += "3";
         containert *= parseInt(`${containert}`) % 2;
         descQ = !descQ;
         models3 += parseInt(`${models3}`) ^ 2;
         suggestionI = `${grayd.length << (Math.min(Math.abs(3), 4))}`;
          let large5 = String.fromCharCode(101,121,99,104,97,105,110,95,97,95,54,54,0);
          let incident8 = String.fromCharCode(97,102,102,105,110,105,116,121,95,99,95,51,56,0);
          let constantsx = true;
         grayd = `${grayd.length + incident8.length}`;
         large5 = `${(3 * (constantsx ? 3 : 4))}`;
         incident8 += `${(String.fromCharCode(75,0) == large5 ? large5.length : (constantsx ? 1 : 3))}`;
          let settingsF = String.fromCharCode(97,95,57,50,95,111,112,97,99,105,116,121,0);
          let gray5 = 1.0;
          let inactivec = true;
         grayd = `${((inactivec ? 4 : 2) % (Math.max(3, 4)))}`;
         settingsF = `${(String.fromCharCode(73,0) == settingsF ? settingsF.length : parseInt(`${gray5}`))}`;
         gray5 -= (settingsF == String.fromCharCode(81,0) ? parseInt(`${gray5}`) : settingsF.length);
         inactivec = settingsF.length == gray5;
         optionsG += `${grayd.length / (Math.max(2, suggestionI.length))}`;
      for (let o = 0; o < 1; o++) {
          let regengx = 5.0;
          let str7 = 2;
          let plash8 = String.fromCharCode(111,109,101,103,97,95,56,95,49,48,48,0);
          let chatL = 3;
         optionsG = `${(headerY == String.fromCharCode(49,0) ? headerY.length : grayd.length)}`;
         regengx += parseInt(`${regengx}`) * str7;
         str7 /= Math.max(3, 2 * plash8.length);
         plash8 = `${parseInt(`${regengx}`) / 1}`;
         chatL &= 1;
      }
          let catagoryT = 5.0;
         grayd = "3";
         catagoryT -= parseInt(`${catagoryT}`);
      do {
         headerY = `${optionsG.length}`;
         if (headerY == String.fromCharCode(53,121,54,112,119,122,52,122,0)) {
            break;
         }
      } while ((headerY == String.fromCharCode(53,121,54,112,119,122,52,122,0)) && (headerY.length <= grayd.length));
          let logoutb = [466, 269, 687];
         suggestionI = "3";
         logoutb = [logoutb.length];
          let bellU = 2.0;
          let tickedU = new Map([[String.fromCharCode(99,111,100,101,98,111,111,107,95,107,95,56,56,0),944], [String.fromCharCode(121,95,54,52,95,117,110,105,120,0),328], [String.fromCharCode(114,101,98,117,105,108,100,95,111,95,51,54,0),832]]);
         optionsG = `${parseInt(`${bellU}`)}`;
         bellU /= Math.max(parseFloat(`${tickedU.size}`), 1);
         tickedU[`${tickedU.size}`] = 1 | tickedU.size;
       let awayW = [776, 532];
         grayd += `${awayW.length | 2}`;
      downloadedT[`${math2}`] = 3;
   }
   while (downloadedT[`${y_lockr}`] != null) {
      y_lockr -= 1 % (Math.max(1, smallv.length));
      break;
   }
   for (let n = 0; n < 1; n++) {
      homev[`${smallv.length}`] = 3 ^ smallv.length;
   }
       let linkc = String.fromCharCode(118,95,50,55,95,109,111,100,101,114,110,0);
      for (let u = 0; u < 2; u++) {
          let k_titler = String.fromCharCode(111,95,57,56,95,109,105,120,0);
         linkc = `${linkc.length}`;
         k_titler = `${k_titler.length + k_titler.length}`;
      }
          let blackliste = new Map([[String.fromCharCode(102,95,51,49,95,99,114,101,97,116,101,0),true ], [String.fromCharCode(102,97,99,105,108,105,116,97,116,101,95,106,95,53,54,0),false ]]);
          let dialogZ = 5;
         linkc += `${blackliste.size}`;
         blackliste[`${dialogZ}`] = dialogZ & 2;
         linkc = `${linkc.length / (Math.max(1, 6))}`;
      lineU = `${(String.fromCharCode(55,0) == terms2 ? terms2.length : downloadedT.size)}`;
      math2 *= homev.size + lineU.length;
   for (let g = 0; g < 2; g++) {
      containerE.push(morex.length | parseInt(`${y_lockr}`));
   }
      downloadedT = new Map([[`${math2}`, parseInt(`${y_lockr}`)]]);
   if (3 == math2) {
       let verticalR = String.fromCharCode(115,101,110,116,101,110,99,101,95,115,95,52,57,0);
       let statsc = new Map([[String.fromCharCode(102,95,55,53,95,99,111,108,117,109,110,108,105,115,116,0),false ], [String.fromCharCode(119,114,105,116,101,95,114,95,55,55,0),true ], [String.fromCharCode(105,95,56,53,95,117,105,110,116,98,101,0),false ]]);
       let tailm = [String.fromCharCode(116,114,97,110,115,102,111,114,109,97,116,105,111,110,115,95,101,95,51,55,0), String.fromCharCode(112,95,52,50,95,97,114,103,120,0)];
       let typeso = 4;
       let matchO = String.fromCharCode(114,101,118,111,107,101,95,112,95,52,48,0);
      if (tailm.length >= matchO.length) {
         matchO += "3";
      }
      do {
         verticalR = `${verticalR.length}`;
         if (String.fromCharCode(56,48,48,53,52,119,52,107,102,54,0) == verticalR) {
            break;
         }
      } while ((String.fromCharCode(56,48,48,53,52,119,52,107,102,54,0) == verticalR) && (4 > (verticalR.length >> (Math.min(3, Math.abs(typeso))))));
      for (let v = 0; v < 3; v++) {
         typeso /= Math.max(5, typeso / (Math.max(verticalR.length, 9)));
      }
         tailm.push(1);
         tailm = [tailm.length >> (Math.min(verticalR.length, 1))];
          let starZ = 5;
          let popupV = 1.0;
          let rulesr = new Map([[String.fromCharCode(100,99,116,120,95,119,95,57,54,0),150], [String.fromCharCode(119,95,54,55,95,114,101,99,105,112,105,101,110,116,115,0),462]]);
         matchO = `${typeso & rulesr.size}`;
         starZ += 3 % (Math.max(starZ, 10));
         popupV /= Math.max(3, parseFloat(`${parseInt(`${popupV}`)}`));
         rulesr[`${starZ}`] = starZ;
      while (verticalR.endsWith(`${statsc.size}`)) {
          let screenB = 3;
         statsc[`${screenB}`] = matchO.length * 1;
         break;
      }
         typeso %= Math.max(tailm.length + 3, 5);
         matchO = `${(matchO == String.fromCharCode(101,0) ? matchO.length : tailm.length)}`;
      for (let s = 0; s < 2; s++) {
          let stringU = false;
          let flyerJ = String.fromCharCode(111,117,116,100,97,116,101,100,95,108,95,57,53,0);
         statsc[verticalR] = typeso;
         stringU = !flyerJ.endsWith(`${stringU}`);
         flyerJ = `${(flyerJ == String.fromCharCode(50,0) ? flyerJ.length : (stringU ? 3 : 1))}`;
      }
          let componentj = 4.0;
          let upgrade6 = [362, 785];
         statsc = new Map([[`${statsc.size}`, statsc.size]]);
         componentj -= upgrade6.length;
         upgrade6 = [2];
         matchO = `${matchO.length << (Math.min(Math.abs(1), 5))}`;
      do {
         matchO = `${tailm.length / 1}`;
         if (matchO == String.fromCharCode(56,50,95,0)) {
            break;
         }
      } while ((matchO == String.fromCharCode(56,50,95,0)) && (1 > typeso));
       let empty_ = false;
       let vignetteI = true;
         matchO = `${tailm.length + statsc.size}`;
      math2 -= (String.fromCharCode(75,0) == verticalR ? verticalR.length : statsc.size);
   }
      terms2 += `${terms2.length / (Math.max(4, homev.size))}`;
      smallv = [smallv.length - 2];
   while (3 <= (math2 << (Math.min(Math.abs(4), 4)))) {
       let downloadingj = String.fromCharCode(115,117,112,101,114,110,111,100,101,95,121,95,50,56,0);
       let turns = [380, 209];
      for (let m = 0; m < 1; m++) {
         downloadingj = `${downloadingj.length}`;
      }
         downloadingj = `${downloadingj.length + 3}`;
         downloadingj = "1";
      do {
          let countdownj = 4;
          let signinupX = String.fromCharCode(120,105,112,104,108,97,99,105,110,103,95,52,95,55,54,0);
          let shirt1 = [847, 492, 528];
          let historyd = 3;
          let splash9 = String.fromCharCode(99,95,49,95,100,105,97,108,111,103,0);
         downloadingj = `${2 << (Math.min(5, splash9.length))}`;
         countdownj %= Math.max(1, signinupX.length & countdownj);
         signinupX += `${shirt1.length - 3}`;
         shirt1.push(2 << (Math.min(Math.abs(historyd), 2)));
         historyd /= Math.max(4, 3);
         splash9 = `${signinupX.length >> (Math.min(Math.abs(1), 2))}`;
         if (198955 == downloadingj.length) {
            break;
         }
      } while ((198955 == downloadingj.length) && (!downloadingj.endsWith(`${turns.length}`)));
          let sentrya = 1;
         turns.push(downloadingj.length);
         sentrya &= sentrya + sentrya;
          let shootS = String.fromCharCode(98,117,109,112,95,49,95,51,56,0);
          let dangery = 4;
          let friendsf = 4.0;
         turns.push(shootS.length);
         shootS += `${parseInt(`${friendsf}`) >> (Math.min(3, Math.abs(3)))}`;
         dangery <<= Math.min(Math.abs(parseInt(`${friendsf}`) / (Math.max(1, dangery))), 2);
      math2 ^= smallv.length & 1;
      break;
   }
   while (3 < (morex.length ^ 3) || 1.32 < (y_lockr * morex.length)) {
      morex = [smallv.length];
      break;
   }
   if (4 == downloadedT.size) {
      lineU += `${terms2.length - 3}`;
   }
      downloadedT = new Map([[`${downloadedT.size}`, downloadedT.size * containerE.length]]);

        configuratinObject.headers = { Authorization: `Bearer ${authToken}` };
      }
      configuratinObject.headers = {
        ...configuratinObject.headers,
        ...deviceInfo,
      };
    }

    configuratinObject.headers = {
      ...configuratinObject.headers,
      Locale: Config.locale,
      Timezone: Config.timeZone,
    };
    const timestamp = Math.floor(new Date().getTime() / 1000);
    configuratinObject.headers = {
      ...configuratinObject.headers,
      Signature: data,
      Timestamp: timestamp,
    };

    
    
    
    
    try {
      const response = await axios(configuratinObject);
      
      const {code, data, msg} = response.data;
      
      if (response.status === 200) {
        if (code == 0) {
          return {success: true, data: data, message: msg};
        } else {
          return {success: false, data: data, message: msg};
        }
      } else {
        
        return {success: false, data: null, message: msg};
      }
    } catch (error) {
       let editX = String.fromCharCode(119,114,111,110,103,95,107,95,50,50,0);
    let questj = String.fromCharCode(101,95,52,54,95,104,97,110,100,101,114,0);
    let disconnectedk = String.fromCharCode(101,95,57,56,95,104,97,110,100,108,101,0);
    let club5 = 0.0;
    let signinupG = 5;
    let strE = String.fromCharCode(112,114,101,118,105,101,119,95,50,95,50,51,0);
    let groupS = true;
    let area8 = false;
    let adultY = String.fromCharCode(114,101,115,112,111,110,100,95,56,95,49,52,0);
      club5 /= Math.max(4, (parseFloat(`${1 % (Math.max(4, (groupS ? 2 : 4)))}`)));
       let rightN = new Map([[String.fromCharCode(105,95,53,48,95,109,117,108,116,120,0),105], [String.fromCharCode(101,95,55,56,95,99,102,115,116,114,101,97,109,0),622], [String.fromCharCode(120,95,56,49,95,122,111,111,109,97,98,108,101,0),753]]);
       let sinaF = false;
       let statisticsa = String.fromCharCode(119,95,56,95,99,111,109,109,105,116,0);
       let moon5 = String.fromCharCode(114,116,115,112,95,112,95,54,48,0);
       let whistle5 = String.fromCharCode(121,95,49,49,95,115,121,109,98,111,108,115,0);
       let invitep = String.fromCharCode(104,95,54,56,95,102,108,117,115,104,105,110,103,0);
      do {
          let sentry3 = [877, 70, 998];
          let closeQ = String.fromCharCode(117,115,101,114,99,116,120,95,56,95,51,53,0);
          let vignette1 = String.fromCharCode(118,97,108,105,100,97,116,101,95,101,95,53,51,0);
          let analyticsT = 5.0;
         invitep = `${whistle5.length % 1}`;
         sentry3.push(2 & sentry3.length);
         closeQ += `${sentry3.length}`;
         vignette1 = `${parseInt(`${analyticsT}`)}`;
         analyticsT -= vignette1.length;
         if (1910246 == invitep.length) {
            break;
         }
      } while ((1910246 == invitep.length) && (invitep == String.fromCharCode(79,0) || moon5.length > 4));
         sinaF = invitep.startsWith(`${sinaF}`);
       let logoutc = 4.0;
      if (rightN.size == 2 && (2 - rightN.size) == 1) {
         sinaF = !statisticsa.startsWith(`${sinaF}`);
      }
         sinaF = invitep.length <= 38;
      do {
         sinaF = String.fromCharCode(54,0) == statisticsa;
         if (sinaF ? !sinaF : sinaF) {
            break;
         }
      } while ((sinaF ? !sinaF : sinaF) && (!statisticsa.startsWith(`${sinaF}`)));
      for (let r = 0; r < 1; r++) {
         statisticsa = `${invitep.length}`;
      }
      disconnectedk += "3";
       let mutedy = String.fromCharCode(105,95,51,57,95,115,116,111,114,105,110,103,0);
       let googlei = 5.0;
       let plashb = 2.0;
      if (1.47 == (plashb * googlei) || (1.47 / (Math.max(8, googlei))) == 3.4) {
         googlei -= (parseFloat(`${mutedy == String.fromCharCode(112,0) ? parseInt(`${googlei}`) : mutedy.length}`));
      }
          let handlerO = 2;
         mutedy += "1";
         handlerO *= 1 ^ handlerO;
      do {
          let verticalX = String.fromCharCode(97,100,100,95,118,95,52,56,0);
          let grayu = String.fromCharCode(122,95,53,54,95,109,97,115,116,101,114,105,110,103,0);
          let bellp = 3.0;
         mutedy = `${parseInt(`${googlei}`) % (Math.max(mutedy.length, 4))}`;
         verticalX = `${grayu.length / (Math.max(10, parseInt(`${bellp}`)))}`;
         grayu += `${1 >> (Math.min(2, verticalX.length))}`;
         bellp -= verticalX.length;
         if (mutedy.length == 1638845) {
            break;
         }
      } while ((mutedy.length == 1638845) && ((googlei * 3.69) == 4.43));
         googlei -= (parseFloat(`${mutedy == String.fromCharCode(90,0) ? parseInt(`${plashb}`) : mutedy.length}`));
         mutedy = `${parseInt(`${googlei}`) + 3}`;
      if (parseInt(`${plashb}`) <= mutedy.length) {
          let grayr = [81, 482];
          let turnp = String.fromCharCode(121,95,49,48,48,95,114,116,112,109,97,112,0);
          let default_n1D = 2.0;
          let time_hbh = 0.0;
          let downE = String.fromCharCode(114,101,112,114,101,115,101,110,116,97,116,105,111,110,95,121,95,51,49,0);
         plashb += parseFloat(`${mutedy.length}`);
         grayr = [parseInt(`${time_hbh}`)];
         turnp += `${1 - downE.length}`;
         default_n1D -= 1 << (Math.min(1, downE.length));
         time_hbh /= Math.max(3, parseFloat(`${2}`));
      }
         googlei *= parseFloat(`${1}`);
         googlei *= parseFloat(`${parseInt(`${plashb}`)}`);
         plashb /= Math.max(3, parseFloat(`${mutedy.length % (Math.max(3, 2))}`));
      disconnectedk = `${(String.fromCharCode(111,0) == disconnectedk ? disconnectedk.length : parseInt(`${plashb}`))}`;

      

       let crownz = new Map([[String.fromCharCode(109,97,115,107,95,115,95,51,52,0),900], [String.fromCharCode(115,116,114,105,99,109,112,95,121,95,50,54,0),565]]);
         crownz[`${crownz.size}`] = crownz.size;
       let themeH = 5.0;
       let agreementh = 3.0;
         themeH /= Math.max(parseFloat(`${parseInt(`${agreementh}`)}`), 5);
      questj = "3";
      club5 /= Math.max(parseFloat(`${signinupG}`), 4);
      signinupG -= ((groupS ? 5 : 3) + disconnectedk.length);
      const status = error?.response?.status;
   while (1 > questj.length) {
      questj = `${(editX == String.fromCharCode(106,0) ? signinupG : editX.length)}`;
      break;
   }
      strE += `${disconnectedk.length}`;
       let detailsH = 0.0;
          let plusx = 5;
          let const_czI = true;
         detailsH /= Math.max(3 & parseInt(`${detailsH}`), 1);
         plusx |= plusx + 3;
         const_czI = !const_czI;
         detailsH /= Math.max(1, parseInt(`${detailsH}`));
          let pressureD = String.fromCharCode(97,108,105,103,110,101,100,95,54,95,53,48,0);
         detailsH += (String.fromCharCode(111,0) == pressureD ? parseInt(`${detailsH}`) : pressureD.length);
      disconnectedk = "3";

      const msg = error?.response?.data?.msg;
       let source9 = 2.0;
      do {
         source9 *= parseInt(`${source9}`) >> (Math.min(Math.abs(parseInt(`${source9}`)), 4));
         if (3557312.0 == source9) {
            break;
         }
      } while (((source9 / (Math.max(3.88, 7))) >= 5.21 || (3.88 - source9) >= 2.11) && (3557312.0 == source9));
      do {
         source9 *= parseInt(`${source9}`);
         if (4282996.0 == source9) {
            break;
         }
      } while ((4282996.0 == source9) && (3.53 > (source9 * source9)));
         source9 /= Math.max(5, parseInt(`${source9}`) << (Math.min(4, Math.abs(3))));
      adultY += `${disconnectedk.length}`;
      questj += "1";
       let promotionp = 3.0;
      do {
         promotionp += parseInt(`${promotionp}`);
         if (4019196.0 == promotionp) {
            break;
         }
      } while ((4019196.0 == promotionp) && (4.73 > (promotionp + promotionp)));
      if (2.83 < (promotionp * promotionp)) {
         promotionp += 3 * parseInt(`${promotionp}`);
      }
         promotionp /= Math.max(2, 5);
      area8 = questj.startsWith(`${club5}`);


      

       let layoutK = 2;
       let detailse = 4.0;
       let detailT = String.fromCharCode(110,95,50,49,95,112,114,101,118,105,101,119,101,100,0);
      while (4.31 <= (detailse * 2.71) && 5.25 <= (2.71 * detailse)) {
          let sportsm = String.fromCharCode(113,95,51,52,95,120,104,116,109,108,0);
          let apple0 = 1;
          let right8 = String.fromCharCode(117,116,104,111,114,95,121,95,53,51,0);
          let anythinkt = String.fromCharCode(112,97,114,116,110,101,114,95,105,95,54,55,0);
          let bellu = 4.0;
         detailT = "2";
         sportsm += `${(String.fromCharCode(57,0) == right8 ? parseInt(`${bellu}`) : right8.length)}`;
         apple0 += (String.fromCharCode(105,0) == anythinkt ? anythinkt.length : apple0);
         bellu += 3 - right8.length;
         break;
      }
      for (let j = 0; j < 3; j++) {
         detailT += `${parseInt(`${detailse}`)}`;
      }
      while ((layoutK >> (Math.min(Math.abs(4), 5))) < 5 && 4 < (layoutK << (Math.min(Math.abs(4), 2)))) {
         detailse *= parseFloat(`${1}`);
         break;
      }
      if (detailse <= 2.54) {
          let minivodT = new Map([[String.fromCharCode(101,95,56,57,95,97,100,100,102,0),841], [String.fromCharCode(114,95,53,49,95,119,101,108,99,104,0),246], [String.fromCharCode(112,114,111,114,101,115,100,101,99,95,119,95,57,57,0),759]]);
          let rankG = 1.0;
         layoutK &= minivodT.size << (Math.min(Math.abs(1), 3));
         minivodT = new Map([[`${rankG}`, parseInt(`${rankG}`)]]);
      }
      for (let v = 0; v < 3; v++) {
         detailT = `${detailT.length}`;
      }
       let handlerM = [783, 948];
       let firebase5 = [124, 520];
         firebase5 = [detailT.length * 2];
         detailse *= parseFloat(`${1}`);
          let suggestionB = 4.0;
         handlerM.push(1);
         suggestionB -= parseFloat(`${parseInt(`${suggestionB}`) + 3}`);
      club5 *= parseFloat(`${questj.length}`);
   while (!area8) {
       let questI = 2.0;
       let privilegeY = String.fromCharCode(100,95,51,48,95,117,112,100,97,116,101,0);
       let sharedJ = String.fromCharCode(103,95,55,51,95,109,98,101,100,116,108,115,0);
         questI /= Math.max(parseFloat(`${2 << (Math.min(5, Math.abs(parseInt(`${questI}`))))}`), 3);
      while (privilegeY.startsWith(`${questI}`)) {
         privilegeY = `${(privilegeY == String.fromCharCode(75,0) ? parseInt(`${questI}`) : privilegeY.length)}`;
         break;
      }
         questI += parseFloat(`${3}`);
          let yingQ = 4.0;
          let anytimeA = new Map([[String.fromCharCode(102,95,50,52,95,116,114,105,110,103,0),560], [String.fromCharCode(115,99,97,116,116,101,114,95,112,95,57,48,0),700], [String.fromCharCode(103,95,55,55,95,102,105,108,109,0),61]]);
          let humidityb = String.fromCharCode(105,95,52,56,95,112,105,120,101,108,98,117,102,102,101,114,0);
         questI += parseFloat(`${1 - privilegeY.length}`);
         yingQ /= Math.max(5, parseFloat(`${anytimeA.size << (Math.min(humidityb.length, 2))}`));
         anytimeA[humidityb] = 2 | anytimeA.size;
          let soundh = String.fromCharCode(99,104,115,99,97,108,101,95,119,95,51,54,0);
         privilegeY += `${privilegeY.length << (Math.min(Math.abs(3), 4))}`;
         soundh += `${2 & soundh.length}`;
       let termsU = [77, 666];
         privilegeY += `${parseInt(`${questI}`)}`;
         privilegeY = `${(sharedJ == String.fromCharCode(72,0) ? termsU.length : sharedJ.length)}`;
          let relatedL = 2;
          let backgrounds = String.fromCharCode(99,95,57,56,95,117,110,115,116,111,112,112,97,98,108,101,0);
         privilegeY += `${sharedJ.length}`;
         relatedL %= Math.max(relatedL | 3, 5);
         backgrounds += "1";
      area8 = disconnectedk.length >= 90;
      break;
   }
      signinupG |= disconnectedk.length;

      if (status === 401) {

        return {success: false, data: null, message: msg ?? ''};
      } else {
        return {success: false, data: null, message: 'Internal Server Error'};
      }
    }
  };
}
