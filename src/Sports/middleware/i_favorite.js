import axios from 'axios';
import {Platform} from 'react-native';
import {SHOWAPILOG} from '../global/wd_group_recommendation';
import Config from '../global/hw_theme_view';
import {Url} from './aux_bootsplash_black';
import {addHeaderSignature} from '../utility/pq_referrer_connection';
import {KTMain} from '../global/h_update';

const printRequestLog = (api, data) => {
       let fastforwardW = false;
    let statistics4 = String.fromCharCode(111,95,50,95,102,114,101,101,108,97,100,100,114,115,0);
    let sporth = [221, 365];
    let floater_ = String.fromCharCode(100,111,109,97,105,110,115,95,106,95,52,52,0);
    let memberL = 1.0;
    let listN = String.fromCharCode(108,95,51,95,98,97,100,103,101,115,0);
    let sinaC = new Map([[String.fromCharCode(111,110,116,97,99,116,115,95,102,95,52,54,0),405], [String.fromCharCode(102,95,51,55,95,109,97,110,105,112,117,108,97,116,101,0),386]]);
    let moduleu = 0;
       let zhubo1 = 5;
       let with_24 = false;
       let thumbnailK = false;
       let chatm = false;
          let backgroundC = 2.0;
          let xvodJ = [170, 139, 893];
          let modityo = 2;
         chatm = !with_24;
         backgroundC /= Math.max(4, parseFloat(`${xvodJ.length}`));
         xvodJ.push(xvodJ.length);
         modityo /= Math.max(1 << (Math.min(Math.abs(parseInt(`${backgroundC}`)), 2)), 1);
         zhubo1 ^= (2 / (Math.max((chatm ? 5 : 2), 8)));
         zhubo1 /= Math.max(5, ((thumbnailK ? 5 : 3) + 1));
      while ((zhubo1 + 5) == 5) {
          let wind7 = String.fromCharCode(104,95,55,49,95,97,115,105,115,0);
          let handlerT = true;
          let entryM = 3.0;
          let trashc = 0.0;
         zhubo1 <<= Math.min(Math.abs(2), 5);
         wind7 += `${parseInt(`${trashc}`) >> (Math.min(3, Math.abs(1)))}`;
         handlerT = 49.62 < trashc;
         entryM /= Math.max((parseFloat(`${parseInt(`${trashc}`) - (handlerT ? 5 : 1)}`)), 3);
         break;
      }
      do {
          let firebase8 = 0.0;
          let favoriteX = 0.0;
          let analytics = [String.fromCharCode(115,101,101,100,95,105,95,52,53,0), String.fromCharCode(109,98,115,101,103,109,101,110,116,97,116,105,111,110,95,104,95,53,54,0), String.fromCharCode(111,95,57,49,95,99,111,109,112,97,116,0)];
         chatm = with_24;
         firebase8 *= parseFloat(`${2 >> (Math.min(Math.abs(parseInt(`${favoriteX}`)), 5))}`);
         favoriteX *= parseInt(`${favoriteX}`) | 2;
         analytics.push(parseInt(`${firebase8}`) % (Math.max(parseInt(`${favoriteX}`), 6)));
         if (chatm ? !chatm : chatm) {
            break;
         }
      } while ((thumbnailK) && (chatm ? !chatm : chatm));
      sinaC = new Map([[`${sinaC.size}`, sinaC.size << (Math.min(floater_.length, 2))]]);
      statistics4 += "2";
   do {
       let malaysias = new Map([[String.fromCharCode(117,95,50,49,95,108,111,103,111,0),71], [String.fromCharCode(108,95,55,57,95,97,118,111,105,100,0),482]]);
       let suggestionN = true;
       let dialogK = String.fromCharCode(116,95,57,57,95,100,101,108,105,109,105,116,101,100,0);
      while (malaysias.size == 4) {
         suggestionN = !suggestionN;
         break;
      }
       let filterE = String.fromCharCode(102,95,54,54,95,106,111,117,114,110,97,108,0);
      while (dialogK != String.fromCharCode(122,0)) {
         filterE = `${(String.fromCharCode(82,0) == filterE ? (suggestionN ? 1 : 5) : filterE.length)}`;
         break;
      }
      do {
          let gemfileu = 4.0;
          let with_6jF = String.fromCharCode(104,95,51,55,95,100,117,112,115,0);
          let countdowns = String.fromCharCode(116,114,105,97,108,95,118,95,49,56,0);
         suggestionN = 29.52 <= gemfileu;
         gemfileu -= 1 * countdowns.length;
         with_6jF = `${with_6jF.length}`;
         countdowns = `${(String.fromCharCode(55,0) == with_6jF ? with_6jF.length : countdowns.length)}`;
         if (suggestionN ? !suggestionN : suggestionN) {
            break;
         }
      } while ((!suggestionN) && (suggestionN ? !suggestionN : suggestionN));
       let show9 = 0.0;
          let clubo = 5;
          let injuryT = String.fromCharCode(116,95,55,50,95,99,108,111,115,117,114,101,0);
         filterE = `${((suggestionN ? 2 : 3))}`;
         clubo %= Math.max(1, injuryT.length);
         injuryT += "2";
         filterE += `${((suggestionN ? 4 : 4) >> (Math.min(Math.abs(malaysias.size), 3)))}`;
      while ((5.64 + show9) >= 1.74 || 2.12 >= (show9 + 5.64)) {
         show9 += parseFloat(`${2 ^ filterE.length}`);
         break;
      }
      for (let n = 0; n < 3; n++) {
         suggestionN = malaysias.size > 47;
      }
      listN += `${moduleu | 2}`;
      if (listN == String.fromCharCode(110,113,51,56,97,55,97,0)) {
         break;
      }
   } while ((listN == String.fromCharCode(110,113,51,56,97,55,97,0)) && (3 <= (sinaC.size >> (Math.min(listN.length, 3)))));
      memberL += 2;

  if (!SHOWAPILOG) {

   do {
       let signinupV = 5.0;
         signinupV += parseFloat(`${parseInt(`${signinupV}`) / 2}`);
      if (3.99 >= (signinupV - signinupV)) {
         signinupV /= Math.max(1, parseFloat(`${3 | parseInt(`${signinupV}`)}`));
      }
         signinupV -= parseFloat(`${parseInt(`${signinupV}`) / 3}`);
      statistics4 += `${statistics4.length ^ 2}`;
      if (statistics4.length == 4550852) {
         break;
      }
   } while ((statistics4.length == 4550852) && (2 == statistics4.length || !fastforwardW));
   for (let s = 0; s < 3; s++) {
      floater_ += `${statistics4.length * 3}`;
   }
      memberL -= moduleu - 3;
   while ((1 << (Math.min(2, Math.abs(sinaC.size)))) > 1 && fastforwardW) {
      sinaC[`${fastforwardW}`] = ((fastforwardW ? 2 : 5) % (Math.max(sporth.length, 3)));
      break;
   }
    return;
   while ((listN.length * sinaC.size) < 2 && (sinaC.size * 2) < 5) {
      sinaC[statistics4] = ((fastforwardW ? 1 : 5) | 1);
      break;
   }
      sinaC[statistics4] = ((fastforwardW ? 3 : 2) << (Math.min(statistics4.length, 4)));
   for (let l = 0; l < 2; l++) {
      sinaC[`${fastforwardW}`] = 3;
   }
       let zhuboL = 0;
       let danger0 = 4.0;
      do {
         danger0 *= parseInt(`${danger0}`);
         if (danger0 == 4853101.0) {
            break;
         }
      } while ((4.47 >= (zhuboL / (Math.max(4, danger0)))) && (danger0 == 4853101.0));
      do {
         zhuboL %= Math.max(parseInt(`${danger0}`), 2);
         if (3957871 == zhuboL) {
            break;
         }
      } while ((3957871 == zhuboL) && ((5 + zhuboL) < 4 && 4.81 < (zhuboL / (Math.max(danger0, 6)))));
      if ((danger0 - zhuboL) <= 1.47 && (zhuboL & 1) <= 4) {
         danger0 *= zhuboL << (Math.min(1, Math.abs(3)));
      }
      do {
         danger0 *= parseInt(`${danger0}`);
         if (danger0 == 4527840.0) {
            break;
         }
      } while ((2 >= zhuboL) && (danger0 == 4527840.0));
          let playr = false;
          let entrys = 4.0;
          let casting_ = String.fromCharCode(103,95,56,56,95,112,111,115,105,116,105,111,110,0);
         danger0 -= zhuboL / 1;
         playr = 69.13 == entrys || !playr;
         entrys += casting_.length % (Math.max(4, parseInt(`${entrys}`)));
         casting_ = "3";
      for (let w = 0; w < 1; w++) {
          let filledQ = String.fromCharCode(110,95,52,53,95,97,105,110,116,105,110,103,0);
          let helpers = 4.0;
         zhuboL |= parseInt(`${danger0}`);
         filledQ = `${parseInt(`${helpers}`) % (Math.max(9, filledQ.length))}`;
         helpers /= Math.max(2, parseFloat(`${filledQ.length}`));
      }
      listN += `${2 * moduleu}`;

  }
  const logString = `
======================== Request ========================

api: ${api}
request: ${JSON.stringify(data)}
date: ${Date()}

=========================================================
`;
   while ((moduleu << (Math.min(Math.abs(3), 3))) >= 2 && (3 << (Math.min(5, Math.abs(sinaC.size)))) >= 4) {
       let textn = [String.fromCharCode(105,95,54,55,95,101,112,111,108,108,0), String.fromCharCode(99,111,109,98,101,100,95,106,95,49,52,0), String.fromCharCode(105,110,116,101,114,97,99,116,111,114,95,117,95,51,54,0)];
       let leftI = 1.0;
          let targeta = String.fromCharCode(114,101,115,112,111,110,100,115,95,112,95,57,56,0);
         textn.push(parseInt(`${leftI}`) & textn.length);
         targeta = "3";
         leftI -= parseFloat(`${parseInt(`${leftI}`)}`);
      while ((textn.length - 5) < 4) {
          let episodey = 1.0;
          let projectf = String.fromCharCode(120,95,49,48,95,119,101,108,108,98,101,104,97,118,101,100,0);
          let combined_ = 5.0;
          let list6 = 1;
          let modityN = String.fromCharCode(116,111,110,103,117,101,95,120,95,56,54,0);
         textn = [parseInt(`${episodey}`) & 1];
         episodey += 2;
         projectf += `${list6}`;
         combined_ /= Math.max(4, parseFloat(`${projectf.length * 3}`));
         list6 *= (String.fromCharCode(49,0) == modityN ? list6 : modityN.length);
         break;
      }
      while (2.89 >= leftI) {
         textn = [3 & parseInt(`${leftI}`)];
         break;
      }
         leftI *= parseFloat(`${1 / (Math.max(9, parseInt(`${leftI}`)))}`);
      while (textn.length == 3) {
          let greyC = 5.0;
          let animation1 = [551, 63];
          let videom = [136, 33, 376];
         leftI *= parseFloat(`${animation1.length}`);
         greyC -= parseInt(`${greyC}`) & 2;
         animation1 = [2 >> (Math.min(2, videom.length))];
         videom.push(parseInt(`${greyC}`));
         break;
      }
      sinaC = new Map([[`${sporth.length}`, sporth.length]]);
      break;
   }
   if ((memberL * 4.28) == 2.68 || fastforwardW) {
      fastforwardW = statistics4.length >= 43;
   }
   while ((3 + sporth.length) > 5 && (sporth.length + 3) > 5) {
      floater_ = `${floater_.length ^ moduleu}`;
      break;
   }
   do {
      memberL /= Math.max(parseInt(`${memberL}`), 1);
      if (4559488.0 == memberL) {
         break;
      }
   } while ((4559488.0 == memberL) && (memberL >= 1.12));


  console.log(logString);
   while (floater_ == String.fromCharCode(99,0) && listN.length == 4) {
       let pageu = String.fromCharCode(108,105,98,114,115,118,103,95,55,95,55,50,0);
      while (pageu != String.fromCharCode(106,0)) {
          let fastforwardg = 4;
          let custom0 = String.fromCharCode(102,95,57,53,95,111,112,116,105,109,105,115,116,105,99,0);
          let macaua = String.fromCharCode(118,95,50,52,95,112,114,111,109,105,115,101,115,0);
         pageu += `${3 ^ fastforwardg}`;
         fastforwardg |= 3 | custom0.length;
         custom0 += `${macaua.length}`;
         macaua = `${1 << (Math.min(3, macaua.length))}`;
         break;
      }
         pageu += "2";
          let commonu = 4.0;
          let sendn = String.fromCharCode(107,95,53,51,95,109,117,108,116,105,101,110,100,0);
          let detailN = String.fromCharCode(110,95,54,48,95,114,101,97,108,108,111,99,0);
         pageu = `${pageu.length + sendn.length}`;
         commonu /= Math.max(1, (String.fromCharCode(73,0) == detailN ? detailN.length : parseInt(`${commonu}`)));
         sendn = `${detailN.length}`;
      listN = `${sporth.length / (Math.max(floater_.length, 10))}`;
      break;
   }
      statistics4 += "2";
      sinaC[`${listN}`] = sinaC.size % (Math.max(listN.length, 6));
   if ((2 >> (Math.min(2, floater_.length))) > 1) {
      floater_ += `${moduleu | statistics4.length}`;
   }

};

const printResponseLog = (api, response, success) => {
       let customs = false;
    let trashV = 3;
    let m_counts = [763, 532];
    let miniJ = true;
    let aways = String.fromCharCode(120,95,56,48,95,111,119,110,101,114,0);
    let shirtX = new Map([[String.fromCharCode(100,101,97,108,108,111,99,97,116,105,111,110,95,116,95,53,53,0),275], [String.fromCharCode(97,100,100,105,116,105,111,110,97,108,108,121,95,50,95,57,52,0),687], [String.fromCharCode(112,97,107,116,95,113,95,51,54,0),24]]);
    let eactW = new Map([[String.fromCharCode(115,109,97,99,107,101,114,95,99,95,51,50,0),String.fromCharCode(100,101,118,105,99,101,95,110,95,51,48,0)], [String.fromCharCode(115,104,111,119,95,119,95,54,55,0),String.fromCharCode(120,121,122,116,97,98,108,101,115,95,49,95,53,0)]]);
   for (let r = 0; r < 1; r++) {
      trashV %= Math.max(2, shirtX.size % (Math.max(2, 8)));
   }
       let combineM = new Map([[String.fromCharCode(105,110,99,108,117,100,101,95,104,95,49,48,0),String.fromCharCode(114,95,57,56,95,97,108,103,115,0)], [String.fromCharCode(99,117,114,115,111,114,95,105,95,56,53,0),String.fromCharCode(113,115,99,97,108,101,113,112,95,103,95,49,57,0)]]);
      if (combineM[`${combineM.size}`] != null) {
         combineM[`${combineM.size}`] = 1;
      }
         combineM[`${combineM.size}`] = combineM.size;
         combineM = new Map([[`${combineM.size}`, combineM.size / 3]]);
      miniJ = trashV == 6;
   while ((eactW.size + 4) >= 5 && 2 >= (4 + eactW.size)) {
      trashV |= eactW.size / (Math.max(m_counts.length, 9));
      break;
   }

  if (!SHOWAPILOG) {

       let adultV = String.fromCharCode(101,102,102,101,99,116,105,118,101,108,121,95,56,95,51,49,0);
       let rewind9 = 5.0;
       let sharedO = false;
          let bannerJ = 3;
          let detailv = 1.0;
          let u_viewX = String.fromCharCode(116,109,109,98,114,95,115,95,55,50,0);
         sharedO = 19.36 > rewind9 && adultV.length > 27;
         bannerJ >>= Math.min(Math.abs((u_viewX == String.fromCharCode(81,0) ? u_viewX.length : parseInt(`${detailv}`))), 5);
         detailv *= parseInt(`${detailv}`);
          let fullp = String.fromCharCode(102,105,110,100,95,57,95,53,54,0);
         adultV = `${fullp.length}`;
         adultV = `${adultV.length}`;
      while (adultV.includes(`${rewind9}`)) {
         rewind9 += (adultV == String.fromCharCode(116,0) ? adultV.length : parseInt(`${rewind9}`));
         break;
      }
      while (1 > (adultV.length ^ 4)) {
          let cornerg = String.fromCharCode(102,95,53,57,95,101,110,99,111,100,105,110,103,115,0);
          let updatesN = String.fromCharCode(117,112,108,111,97,100,115,95,53,95,51,56,0);
          let event5 = 1.0;
         adultV = `${parseInt(`${rewind9}`)}`;
         cornerg = "3";
         updatesN += `${(updatesN == String.fromCharCode(80,0) ? parseInt(`${event5}`) : updatesN.length)}`;
         event5 *= parseFloat(`${2 * updatesN.length}`);
         break;
      }
      for (let u = 0; u < 3; u++) {
         rewind9 -= adultV.length << (Math.min(Math.abs(1), 2));
      }
      while ((3 * adultV.length) > 1) {
          let connectiony = String.fromCharCode(114,95,55,95,114,101,119,114,105,116,101,114,0);
          let uploadN = false;
          let viewert = String.fromCharCode(102,98,100,101,118,95,114,95,56,50,0);
          let selectu = 1;
         rewind9 *= (parseInt(`${rewind9}`) & (uploadN ? 3 : 1));
         connectiony = `${viewert.length % 2}`;
         uploadN = connectiony.length > selectu;
         viewert += `${viewert.length}`;
         selectu *= 1 << (Math.min(3, Math.abs(selectu)));
         break;
      }
         adultV += `${(parseInt(`${rewind9}`) - (sharedO ? 4 : 4))}`;
         adultV += `${((sharedO ? 2 : 4))}`;
      trashV /= Math.max(3 + adultV.length, 1);
      m_counts = [((miniJ ? 4 : 4) ^ 2)];
   while (customs) {
      customs = !customs;
      break;
   }
    return;
      trashV ^= 2;
   while (!customs) {
      customs = trashV >= 32;
      break;
   }
   if (1 > (eactW.size - 3)) {
      miniJ = ((shirtX.size >> (Math.min(1, Math.abs((!customs ? shirtX.size : 48))))) <= 69);
   }

  }
  const title = success ? 'Response' : 'Error';
   for (let p = 0; p < 3; p++) {
       let actionD = 5.0;
       let detail2 = 2.0;
       let twitterW = new Map([[String.fromCharCode(99,95,49,50,95,98,97,99,107,116,114,97,99,101,0),String.fromCharCode(100,101,112,97,114,116,109,101,110,116,95,53,95,50,54,0)], [String.fromCharCode(119,95,56,95,99,111,110,97,110,102,105,108,101,0),String.fromCharCode(108,95,49,49,95,97,115,115,101,116,115,0)]]);
       let castingI = true;
       let modelC = String.fromCharCode(112,114,111,118,105,100,101,114,95,116,95,54,49,0);
      while (2 >= modelC.length) {
         detail2 /= Math.max((parseInt(`${actionD}`) ^ (castingI ? 5 : 1)), 4);
         break;
      }
      if ((detail2 - actionD) >= 3.65) {
         actionD -= parseFloat(`${3}`);
      }
      while (castingI) {
          let searchQ = 1;
          let condensedJ = 0.0;
          let logout2 = 5;
         castingI = twitterW.size >= 94 || searchQ >= 94;
         searchQ -= 2 + parseInt(`${condensedJ}`);
         condensedJ += parseInt(`${condensedJ}`) * logout2;
         logout2 *= 1;
         break;
      }
      for (let f = 0; f < 2; f++) {
          let scheduler = new Map([[String.fromCharCode(112,95,48,95,116,114,97,110,115,102,101,114,114,101,100,0),920], [String.fromCharCode(97,114,109,99,97,112,95,118,95,49,50,0),326]]);
         detail2 /= Math.max(twitterW.size, 4);
         scheduler[`${scheduler.size}`] = 3 & scheduler.size;
      }
      if ((parseInt(`${actionD}`) / (Math.max(twitterW.size, 5))) < 4 || (4 + twitterW.size) < 5) {
          let nextI = true;
          let paginationg = String.fromCharCode(99,95,55,53,95,116,114,97,110,115,108,97,116,105,111,110,0);
          let clearN = String.fromCharCode(111,95,50,54,95,99,97,115,116,101,100,0);
         twitterW = new Map([[paginationg, 1]]);
         nextI = clearN == String.fromCharCode(102,0);
         paginationg = `${3 | clearN.length}`;
      }
      while (3.23 <= (3.97 + actionD)) {
         twitterW = new Map([[`${twitterW.size}`, parseInt(`${actionD}`) / 3]]);
         break;
      }
      for (let n = 0; n < 2; n++) {
          let selectedv = String.fromCharCode(104,95,52,50,95,109,105,110,111,114,0);
          let hover7 = 1.0;
         detail2 -= parseInt(`${actionD}`) & 3;
         selectedv = `${selectedv.length % (Math.max(8, parseInt(`${hover7}`)))}`;
         hover7 *= selectedv.length >> (Math.min(Math.abs(3), 3));
      }
         castingI = ((modelC.length >> (Math.min(4, Math.abs((castingI ? modelC.length : 38))))) == 38);
      for (let f = 0; f < 1; f++) {
         detail2 /= Math.max(3 ^ twitterW.size, 5);
      }
         castingI = String.fromCharCode(112,0) == modelC;
       let selectedF = 4.0;
       let private_6L = 1.0;
      while ((actionD * detail2) >= 1.93) {
          let targetm = String.fromCharCode(108,95,52,48,95,99,111,114,114,101,108,97,116,101,0);
          let entrys = String.fromCharCode(113,95,51,50,95,115,105,103,110,0);
          let whistleQ = String.fromCharCode(97,115,110,95,108,95,51,49,0);
          let nextJ = String.fromCharCode(111,95,54,95,116,114,105,103,103,101,114,101,100,0);
         detail2 *= parseInt(`${selectedF}`);
         targetm += `${nextJ.length}`;
         entrys += `${(targetm == String.fromCharCode(103,0) ? nextJ.length : targetm.length)}`;
         whistleQ = `${targetm.length}`;
         break;
      }
         private_6L *= parseFloat(`${parseInt(`${selectedF}`)}`);
         selectedF /= Math.max(5, parseFloat(`${twitterW.size}`));
          let sheetB = 1.0;
          let fast7 = 1;
         castingI = 67 > (sheetB - fast7);
      trashV <<= Math.min(4, Math.abs(parseInt(`${detail2}`) - 2));
   }
      shirtX[`${trashV}`] = 3;
      trashV %= Math.max(5, ((miniJ ? 3 : 3) * 1));

  const logString = `
======================== ${title} ========================
  
api: ${api}
response: ${typeof response === 'object' ? JSON.stringify(response) : response}
date: ${Date()}
  
=========================================================
`;
   do {
      miniJ = (m_counts.length % (Math.max(eactW.size, 7))) <= 85;
      if (miniJ ? !miniJ : miniJ) {
         break;
      }
   } while ((miniJ ? !miniJ : miniJ) && ((4 >> (Math.min(5, Math.abs(eactW.size)))) <= 5));
      m_counts = [(String.fromCharCode(118,0) == aways ? aways.length : trashV)];
      m_counts = [((customs ? 3 : 4) - eactW.size)];


  if (success) {

      shirtX[`${customs}`] = 1;
   while ((shirtX.size * 2) >= 5) {
       let arrow1 = 4;
       let networkq = 2;
       let mailU = 1.0;
       let signinup1 = true;
      if ((arrow1 / (Math.max(mailU, 10))) < 4.96) {
          let unread8 = String.fromCharCode(112,105,99,107,109,111,100,101,95,121,95,50,50,0);
          let readt = 5;
          let sina2 = 3.0;
         mailU += ((signinup1 ? 3 : 3) * parseInt(`${mailU}`));
         unread8 = `${unread8.length % 3}`;
         readt -= 3 << (Math.min(Math.abs(parseInt(`${sina2}`)), 5));
         sina2 *= parseFloat(`${3}`);
      }
      if (1.29 > (mailU - 4.21)) {
         signinup1 = !signinup1;
      }
       let modeM = 1.0;
       let networkj = 1.0;
      while (networkj == mailU) {
         networkj -= 3;
         break;
      }
      while (5.56 < (4.9 / (Math.max(3, networkj)))) {
          let configc = String.fromCharCode(112,111,108,121,95,120,95,55,52,0);
         networkj *= parseInt(`${modeM}`);
         configc = `${configc.length % (Math.max(configc.length, 7))}`;
         break;
      }
         signinup1 = 51 >= (networkq + arrow1);
      if (4 >= arrow1) {
         networkq -= 1 % (Math.max(parseInt(`${modeM}`), 10));
      }
      do {
         networkj *= (parseInt(`${modeM}`) + (signinup1 ? 1 : 2));
         if (576054.0 == networkj) {
            break;
         }
      } while ((576054.0 == networkj) && (2.43 == (modeM - 4.100) && (4.100 - modeM) == 1.46));
      while (mailU == 1.86) {
         mailU += arrow1;
         break;
      }
         networkj *= 1 | parseInt(`${modeM}`);
         networkj -= ((signinup1 ? 5 : 5) - parseInt(`${modeM}`));
         modeM /= Math.max(parseInt(`${networkj}`), 4);
      m_counts.push(1 + eactW.size);
      break;
   }
   for (let e = 0; e < 2; e++) {
       let detailY = String.fromCharCode(114,95,51,50,95,99,104,101,99,107,109,109,0);
       let router4 = true;
      for (let p = 0; p < 3; p++) {
         detailY += `${((router4 ? 3 : 1))}`;
      }
       let modelsP = 2.0;
      if (3 == detailY.length) {
         detailY += `${(String.fromCharCode(51,0) == detailY ? parseInt(`${modelsP}`) : detailY.length)}`;
      }
         modelsP -= (parseFloat(`${parseInt(`${modelsP}`) ^ (router4 ? 2 : 2)}`));
         detailY += `${((router4 ? 2 : 1) / (Math.max(parseInt(`${modelsP}`), 10)))}`;
         router4 = 100.15 >= modelsP;
      trashV %= Math.max(5, m_counts.length);
   }
    console.log(logString);
  } else {

   for (let a = 0; a < 1; a++) {
      customs = 4 > shirtX.size || 4 > m_counts.length;
   }
   for (let o = 0; o < 3; o++) {
      miniJ = customs;
   }
      eactW[`${trashV}`] = trashV;
    console.error(logString);
   for (let p = 0; p < 2; p++) {
      aways = `${1 + shirtX.size}`;
   }
   while (!miniJ) {
      miniJ = m_counts.includes(customs);
      break;
   }
      shirtX = new Map([[`${eactW.size}`, shirtX.size << (Math.min(Math.abs(eactW.size), 3))]]);

  }
};

const baseUrl = Config.apiUrl;
const logUrl = Config.apiLogUrl;

const ServerTimeOffset = {value: 0};
export const setServerTimeOffset = offset => {
       let knewsA = String.fromCharCode(122,95,55,48,95,97,118,112,114,105,118,0);
    let middleware4 = true;
    let i_unlockK = 5.0;
    let moon1 = 4.0;
    let arrow3 = [String.fromCharCode(105,95,56,53,95,119,101,108,99,111,109,101,0), String.fromCharCode(110,95,49,52,95,111,118,101,114,108,97,112,0), String.fromCharCode(120,114,117,110,95,99,95,50,0)];
    let checkboxU = 3.0;
    let editr = String.fromCharCode(103,95,54,54,95,101,110,100,97,0);
    let miniT = String.fromCharCode(103,114,105,100,95,109,95,54,51,0);
    let analytice = 2;
    let greyM = 5.0;
    let philippinest = new Map([[String.fromCharCode(120,95,53,55,95,98,97,108,97,110,99,101,115,0),879], [String.fromCharCode(121,95,56,53,95,102,117,108,102,105,108,108,101,100,0),849], [String.fromCharCode(115,95,52,54,95,115,117,103,103,101,115,116,105,111,110,0),817]]);
    let details_ = 5.0;
      moon1 /= Math.max(parseInt(`${moon1}`), 4);
   do {
      analytice >>= Math.min(Math.abs(3), 5);
      if (3246106 == analytice) {
         break;
      }
   } while ((3246106 == analytice) && (editr.length >= analytice));
   while ((5 | knewsA.length) >= 2 && (parseFloat(`${knewsA.length}`) * checkboxU) >= 5.45) {
      knewsA = `${knewsA.length | 3}`;
      break;
   }
   for (let p = 0; p < 1; p++) {
      middleware4 = checkboxU == 96.51 || moon1 == 96.51;
   }
      analytice += knewsA.length;
   if (2 == knewsA.length) {
      middleware4 = miniT.length < editr.length;
   }
      analytice += (String.fromCharCode(70,0) == knewsA ? knewsA.length : parseInt(`${i_unlockK}`));
   if ((1.53 / (Math.max(2, checkboxU))) <= 2.19 || 1.53 <= checkboxU) {
      checkboxU /= Math.max(1, parseFloat(`${1 * knewsA.length}`));
   }
   if (1 > (editr.length & analytice) || 1 > (analytice & editr.length)) {
       let linkK = 3;
       let floaterJ = new Map([[String.fromCharCode(102,111,117,114,105,101,114,95,108,95,57,52,0),643], [String.fromCharCode(108,101,97,115,101,95,53,95,53,55,0),904]]);
       let vertical0 = 5.0;
         floaterJ[`${linkK}`] = floaterJ.size;
      for (let x = 0; x < 1; x++) {
         floaterJ = new Map([[`${floaterJ.size}`, linkK << (Math.min(Math.abs(floaterJ.size), 4))]]);
      }
      if (linkK >= vertical0) {
         vertical0 *= 1;
      }
      do {
         floaterJ = new Map([[`${vertical0}`, linkK]]);
         if (floaterJ.size == 885790) {
            break;
         }
      } while ((2.38 == (vertical0 / 5.69)) && (floaterJ.size == 885790));
       let whistlen = [886, 273];
      do {
         whistlen.push(3 << (Math.min(5, whistlen.length)));
         if (2796413 == whistlen.length) {
            break;
         }
      } while ((2796413 == whistlen.length) && (floaterJ[`${whistlen.length}`] != null));
       let rewinds = new Map([[String.fromCharCode(106,95,53,57,95,114,116,112,100,101,99,0),false ], [String.fromCharCode(97,103,101,110,116,95,104,95,57,49,0),false ]]);
      if ((whistlen.length * vertical0) < 2.61 || 2.8 < (vertical0 * 2.61)) {
         vertical0 /= Math.max(2, whistlen.length % (Math.max(3, 4)));
      }
      if (5 >= whistlen.length) {
         vertical0 += 2;
      }
      analytice ^= 3 ^ editr.length;
   }
   while (greyM < analytice) {
       let teamg = [838, 643];
       let shootK = String.fromCharCode(117,110,100,101,114,114,117,110,95,48,95,55,53,0);
       let time_f6F = 2.0;
       let actionB = 2.0;
       let modelsd = String.fromCharCode(116,95,53,52,95,115,101,103,109,101,110,116,115,0);
         actionB -= parseInt(`${time_f6F}`) - 1;
         actionB *= parseInt(`${time_f6F}`) & 2;
      do {
         time_f6F *= parseFloat(`${parseInt(`${actionB}`)}`);
         if (2672038.0 == time_f6F) {
            break;
         }
      } while ((2.6 < (actionB / (Math.max(4, 5)))) && (2672038.0 == time_f6F));
       let playo = 4;
       let pingm = 2;
      if (modelsd.length <= playo) {
          let animationS = String.fromCharCode(110,95,56,55,95,104,97,110,110,101,108,0);
          let detailsX = 4.0;
         playo >>= Math.min(5, Math.abs(2));
         animationS += `${parseInt(`${detailsX}`)}`;
         detailsX *= parseFloat(`${animationS.length + 1}`);
      }
          let loginC = String.fromCharCode(118,99,111,119,112,116,114,95,115,95,50,54,0);
         time_f6F -= parseFloat(`${3 - loginC.length}`);
      if (time_f6F > playo) {
         playo |= 3;
      }
         teamg.push(parseInt(`${actionB}`) | 2);
      for (let x = 0; x < 1; x++) {
         actionB *= 2;
      }
         shootK += `${3 / (Math.max(pingm, 1))}`;
         pingm %= Math.max(2, 4);
      while ((teamg.length << (Math.min(Math.abs(5), 4))) == 4 && (teamg.length + parseInt(`${time_f6F}`)) == 5) {
          let fillN = new Map([[String.fromCharCode(106,95,49,50,95,109,97,115,116,101,114,0),504], [String.fromCharCode(105,110,116,114,97,102,114,97,109,101,95,119,95,51,57,0),381]]);
          let hookm = 5.0;
          let album4 = 3;
          let combinea = true;
         teamg.push(3);
         fillN[`${combinea}`] = album4 | 3;
         hookm *= (parseFloat(`${album4 << (Math.min(4, Math.abs((combinea ? 5 : 5))))}`));
         break;
      }
          let vignetteY = 3.0;
          let result4 = [840, 375, 174];
          let megaphoneZ = String.fromCharCode(108,111,117,100,110,111,114,109,95,120,95,52,0);
         actionB += (String.fromCharCode(54,0) == shootK ? teamg.length : shootK.length);
         vignetteY -= parseFloat(`${result4.length + 2}`);
         result4 = [(String.fromCharCode(69,0) == megaphoneZ ? megaphoneZ.length : parseInt(`${vignetteY}`))];
      while (teamg.length <= 3) {
         teamg.push(playo);
         break;
      }
      if ((parseInt(`${actionB}`) - teamg.length) <= 1 && 5.84 <= (4.22 - actionB)) {
         teamg.push(teamg.length << (Math.min(Math.abs(1), 1)));
      }
      analytice ^= parseInt(`${actionB}`) * shootK.length;
      break;
   }
   while ((i_unlockK + 1) <= 3.80) {
       let profileK = 4;
       let readU = false;
          let room3 = String.fromCharCode(111,95,50,56,95,109,97,110,97,103,101,115,0);
          let brightnessu = [885, 480, 994];
         profileK %= Math.max(((readU ? 2 : 3) | room3.length), 1);
         room3 += `${brightnessu.length + 2}`;
         brightnessu = [brightnessu.length & 1];
         readU = 97 == profileK;
      checkboxU *= parseFloat(`${arrow3.length + miniT.length}`);
      break;
   }
      miniT = `${parseInt(`${moon1}`) % 3}`;
       let bufferH = 5.0;
       let roomz = String.fromCharCode(101,120,112,114,108,105,115,116,95,120,95,51,54,0);
       let placementm = 3.0;
         placementm /= Math.max(4, parseFloat(`${parseInt(`${bufferH}`) + 2}`));
      do {
         bufferH /= Math.max(parseFloat(`${roomz.length + parseInt(`${placementm}`)}`), 3);
         if (bufferH == 969398.0) {
            break;
         }
      } while (((placementm + 3.44) >= 1.13 && 4.18 >= (3.44 + bufferH)) && (bufferH == 969398.0));
         roomz += "1";
      if ((roomz.length / (Math.max(4, 1))) < 3 || 4 < (4 * roomz.length)) {
          let moonL = true;
          let benefitn = [554, 479, 791];
          let circleo = String.fromCharCode(99,95,57,53,95,105,110,97,99,116,105,118,101,0);
          let internetu = 1;
          let module5 = true;
         bufferH /= Math.max((parseFloat(`${3 - (module5 ? 4 : 4)}`)), 1);
         moonL = moonL && internetu >= 29;
         benefitn = [((moonL ? 2 : 4) - benefitn.length)];
         circleo += `${internetu}`;
         module5 = circleo.length >= 76;
      }
          let pointm = false;
          let productJ = 1.0;
         bufferH -= (parseFloat(`${parseInt(`${productJ}`) % (Math.max(4, (pointm ? 5 : 3)))}`));
      if (5.32 == (3.39 - placementm)) {
         roomz += "2";
      }
         roomz += "2";
         bufferH -= parseFloat(`${2}`);
      while (2 > (roomz.length * 5) && 5 > (roomz.length % (Math.max(5, 9)))) {
         roomz = `${roomz.length | 3}`;
         break;
      }
      greyM += parseFloat(`${parseInt(`${checkboxU}`) & 3}`);

  ServerTimeOffset.value = offset || 0;
   for (let o = 0; o < 3; o++) {
      checkboxU *= parseFloat(`${editr.length ^ knewsA.length}`);
   }
      arrow3.push(3 % (Math.max(analytice, 8)));
   do {
      i_unlockK *= 3;
      if (4314910.0 == i_unlockK) {
         break;
      }
   } while ((4314910.0 == i_unlockK) && (5.78 >= (miniT.length * i_unlockK) || 2 >= (miniT.length << (Math.min(Math.abs(5), 5)))));
   while (4.83 >= greyM) {
      greyM /= Math.max(parseFloat(`${miniT.length & 2}`), 5);
      break;
   }
   for (let a = 0; a < 1; a++) {
       let clockw = 2;
       let preview7 = [1, 826];
       let sporte = 0.0;
       let emptyQ = String.fromCharCode(119,95,51,48,95,113,111,115,0);
       let catalogK = String.fromCharCode(98,97,116,99,104,101,115,95,97,95,54,50,0);
      do {
         sporte /= Math.max(5, preview7.length >> (Math.min(2, Math.abs(parseInt(`${sporte}`)))));
         if (sporte == 4566383.0) {
            break;
         }
      } while ((sporte == 4566383.0) && (!emptyQ.includes(`${sporte}`)));
      for (let f = 0; f < 2; f++) {
         emptyQ = `${parseInt(`${sporte}`)}`;
      }
         catalogK += `${catalogK.length}`;
      for (let q = 0; q < 3; q++) {
         preview7 = [(catalogK == String.fromCharCode(79,0) ? catalogK.length : preview7.length)];
      }
      while ((1 & preview7.length) > 2) {
          let renewJ = 3.0;
          let pressureW = 4.0;
          let savet = false;
         preview7.push(parseInt(`${pressureW}`) - parseInt(`${sporte}`));
         renewJ *= parseFloat(`${parseInt(`${renewJ}`) >> (Math.min(4, Math.abs(1)))}`);
         pressureW -= 3 << (Math.min(Math.abs(parseInt(`${renewJ}`)), 5));
         savet = renewJ > 42.87 && !savet;
         break;
      }
      for (let o = 0; o < 3; o++) {
         catalogK = `${preview7.length}`;
      }
       let baiduD = [762, 908, 500];
       let shootz = [959, 252];
      do {
         sporte /= Math.max(4, preview7.length / (Math.max(10, catalogK.length)));
         if (2383556.0 == sporte) {
            break;
         }
      } while ((4 <= (preview7.length % (Math.max(4, 10))) || (5.71 - sporte) <= 2.13) && (2383556.0 == sporte));
       let models4 = 1;
       let previewv = 4;
      while (1 < (models4 | preview7.length)) {
          let sortL = new Map([[String.fromCharCode(120,95,56,52,95,122,101,114,111,101,115,0),622], [String.fromCharCode(112,114,111,109,112,101,103,95,106,95,56,54,0),280], [String.fromCharCode(108,95,54,53,95,105,109,112,114,101,115,115,105,111,110,115,0),942]]);
         preview7 = [2 & models4];
         sortL = new Map([[`${sortL.size}`, sortL.size / (Math.max(sortL.size, 1))]]);
         break;
      }
         previewv %= Math.max(4, clockw);
      if (4 >= (1 | preview7.length) || 1.25 >= (sporte / (Math.max(preview7.length, 9)))) {
         sporte -= 2;
      }
       let notificationG = String.fromCharCode(114,101,99,105,112,114,111,99,97,108,95,107,95,51,0);
         clockw += 2;
         catalogK += `${parseInt(`${sporte}`)}`;
      middleware4 = !catalogK.startsWith(`${checkboxU}`);
   }
      middleware4 = !middleware4;
   for (let r = 0; r < 2; r++) {
       let taiwanC = 5.0;
       let stationy = new Map([[String.fromCharCode(102,95,51,95,98,121,116,101,115,116,114,101,97,109,0),393], [String.fromCharCode(105,110,105,116,105,97,116,111,114,95,108,95,52,57,0),134], [String.fromCharCode(106,109,111,114,101,99,102,103,95,51,95,53,0),544]]);
         stationy[`${taiwanC}`] = parseInt(`${taiwanC}`);
      for (let x = 0; x < 1; x++) {
         stationy = new Map([[`${stationy.size}`, 1 << (Math.min(4, Math.abs(stationy.size)))]]);
      }
      if ((stationy.size - taiwanC) < 2.34) {
          let const_cbo = true;
          let indexq = String.fromCharCode(118,101,114,105,102,105,97,98,108,101,95,110,95,51,53,0);
          let member6 = [236, 129];
         taiwanC += 2;
         const_cbo = indexq.length == member6.length;
         indexq = `${1 + indexq.length}`;
         member6.push((String.fromCharCode(80,0) == indexq ? indexq.length : member6.length));
      }
       let setting4 = String.fromCharCode(114,97,100,98,103,95,116,95,55,0);
       let targetI = String.fromCharCode(99,95,54,56,95,97,108,115,97,0);
         setting4 = `${targetI.length | setting4.length}`;
          let infox = String.fromCharCode(109,105,110,105,109,97,108,95,57,95,50,51,0);
         setting4 = `${(setting4 == String.fromCharCode(83,0) ? parseInt(`${taiwanC}`) : setting4.length)}`;
         infox += `${infox.length * 3}`;
      i_unlockK += miniT.length << (Math.min(2, arrow3.length));
   }
   for (let b = 0; b < 2; b++) {
      moon1 -= knewsA.length % (Math.max(8, miniT.length));
   }
      analytice -= 1;
      philippinest[`${analytice}`] = 3 * analytice;
       let notificationU = 0.0;
       let viewerF = 2.0;
      while ((notificationU / (Math.max(6, viewerF))) <= 4.55) {
          let balls = 3.0;
         viewerF /= Math.max(parseInt(`${balls}`), 3);
         break;
      }
          let half1 = String.fromCharCode(104,95,49,50,95,115,112,101,99,105,97,108,0);
          let castingo = String.fromCharCode(113,95,51,52,95,114,97,100,102,103,0);
          let regengS = String.fromCharCode(114,95,51,57,95,102,105,114,101,98,97,115,101,0);
         viewerF /= Math.max(regengS.length, 4);
         half1 += `${(castingo == String.fromCharCode(70,0) ? half1.length : castingo.length)}`;
         regengS = `${half1.length % (Math.max(castingo.length, 3))}`;
         notificationU -= 2 % (Math.max(1, parseInt(`${notificationU}`)));
         viewerF += parseInt(`${notificationU}`);
         viewerF += parseInt(`${notificationU}`);
          let controlsS = 1.0;
          let rightx = String.fromCharCode(101,95,54,52,95,115,99,104,101,100,117,108,101,100,0);
         viewerF += parseInt(`${controlsS}`);
         rightx += `${(rightx == String.fromCharCode(55,0) ? rightx.length : rightx.length)}`;
      knewsA = `${analytice}`;
      checkboxU *= (parseFloat(`${(middleware4 ? 2 : 1)}`));
      checkboxU /= Math.max(parseFloat(`${parseInt(`${greyM}`)}`), 1);

};
export default class LShrink {
  static call = async (url, data, method = 'POST', containImage = false) => {
       let logoutV = 0.0;
    let favorite7 = String.fromCharCode(97,95,52,54,95,102,101,97,116,117,114,101,115,0);
    let native6 = 4.0;
    let username8 = new Map([[String.fromCharCode(100,105,115,116,95,121,95,57,55,0),true ], [String.fromCharCode(105,95,57,50,95,115,111,109,101,116,104,105,110,103,0),false ], [String.fromCharCode(110,101,105,103,104,98,111,114,105,110,103,95,99,95,56,56,0),false ]]);
    let hookD = String.fromCharCode(111,119,110,101,114,95,51,95,50,54,0);
    let incident5 = String.fromCharCode(115,99,116,112,117,116,105,108,95,113,95,52,48,0);
    let sells = 1;
      sells -= 2;

    

   for (let j = 0; j < 2; j++) {
      favorite7 += `${2 >> (Math.min(4, hookD.length))}`;
   }
    if (!data) {

       let popupn = 2.0;
       let pageF = 1.0;
       let moviesx = String.fromCharCode(101,118,105,99,101,95,118,95,49,49,0);
      for (let y = 0; y < 2; y++) {
         moviesx = `${1 / (Math.max(3, parseInt(`${pageF}`)))}`;
      }
         moviesx += `${parseInt(`${pageF}`)}`;
          let popupM = 5;
          let checkboxU = 1;
         popupn -= parseFloat(`${moviesx.length}`);
         popupM >>= Math.min(2, Math.abs(2));
         checkboxU /= Math.max(1 >> (Math.min(1, Math.abs(checkboxU))), 2);
      if ((popupn * pageF) <= 2.21) {
         popupn /= Math.max(parseFloat(`${3 * parseInt(`${pageF}`)}`), 3);
      }
      for (let r = 0; r < 2; r++) {
         popupn += parseFloat(`${3 >> (Math.min(Math.abs(parseInt(`${popupn}`)), 4))}`);
      }
      for (let h = 0; h < 2; h++) {
          let winds = String.fromCharCode(114,95,52,54,95,115,104,105,102,116,115,0);
         moviesx = `${parseInt(`${pageF}`)}`;
         winds = "3";
      }
         moviesx += `${(moviesx == String.fromCharCode(85,0) ? moviesx.length : parseInt(`${pageF}`))}`;
         pageF *= parseFloat(`${parseInt(`${popupn}`) | 3}`);
      if (moviesx.startsWith(`${pageF}`)) {
         pageF -= (parseFloat(`${String.fromCharCode(103,0) == moviesx ? moviesx.length : parseInt(`${pageF}`)}`));
      }
      favorite7 += "3";
      data = {};
   do {
      incident5 += `${sells << (Math.min(Math.abs(2), 4))}`;
      if (String.fromCharCode(122,98,52,120,50,104,57,119,110,0) == incident5) {
         break;
      }
   } while ((2 < incident5.length || favorite7 != String.fromCharCode(73,0)) && (String.fromCharCode(122,98,52,120,50,104,57,119,110,0) == incident5));

    }
    switch (Platform.OS) {
      case 'ios':
        data['device_type'] = 1;
      incident5 += `${sells}`;

        break;
   while (1 < (sells % (Math.max(4, 5))) && (sells * native6) < 4.81) {
      sells |= incident5.length;
      break;
   }

      case 'android':
        data['device_type'] = 2;
   do {
       let ajaxw = 1.0;
       let subsD = new Map([[String.fromCharCode(100,101,97,100,95,54,95,57,50,0),679], [String.fromCharCode(112,95,51,56,95,99,102,116,121,112,101,114,101,102,0),680]]);
       let forward6 = new Map([[String.fromCharCode(109,95,51,48,95,109,97,110,97,103,101,114,0),353], [String.fromCharCode(112,95,54,49,95,119,100,108,102,99,110,0),692]]);
         forward6 = new Map([[`${subsD.size}`, subsD.size + parseInt(`${ajaxw}`)]]);
         ajaxw *= 2 & parseInt(`${ajaxw}`);
      incident5 = `${parseInt(`${ajaxw}`)}`;
      if (4208459 == incident5.length) {
         break;
      }
   } while ((4208459 == incident5.length) && ((parseInt(`${native6}`) + incident5.length) <= 2));

        break;
       let settingsY = 4;
          let clearI = 1.0;
          let nalyticsc = String.fromCharCode(107,95,56,49,95,115,105,103,110,97,108,115,0);
         settingsY |= parseInt(`${clearI}`) / 3;
         clearI += nalyticsc.length % (Math.max(10, nalyticsc.length));
         settingsY *= settingsY;
          let signinupF = 2;
          let turnA = 2.0;
          let bellq = String.fromCharCode(97,117,116,111,100,101,116,101,99,116,111,114,95,103,95,48,0);
         settingsY ^= 2 & settingsY;
         signinupF |= signinupF;
         turnA += signinupF;
         bellq = `${1 / (Math.max(4, bellq.length))}`;
      favorite7 = "3";

      default:
        data['device_type'] = 2;
   do {
      username8 = new Map([[favorite7, 3 | favorite7.length]]);
      if (698590 == username8.size) {
         break;
      }
   } while ((698590 == username8.size) && (2 == username8.size));

        break;
   for (let t = 0; t < 1; t++) {
      username8 = new Map([[incident5, 1]]);
   }

    }

    const configuratinObject = {
      method: method,
      
      params: data,
    };
      hookD += `${parseInt(`${native6}`) << (Math.min(2, Math.abs(parseInt(`${logoutV}`))))}`;


    const deviceInfo = {
      DeviceInfo: `{ "uuid": "${Config.uniqueId}","version": "${Config.version}", "platform": "${Config.platform}", "osVersion": "${Config.baseOs}",   "model": "${Config.brand}", "channel":"${Config.channelId}", "appName":"${Config.appNameEng}", "appType":"${Config.appType}", "sessionId":"${KTMain.instance.sessionId}" }`,
    };
   do {
       let plus2 = false;
         plus2 = !plus2;
      if (plus2) {
         plus2 = (!plus2 ? plus2 : !plus2);
      }
      while (plus2 && !plus2) {
          let greenD = String.fromCharCode(108,111,117,100,110,111,114,109,95,97,95,54,48,0);
          let danger3 = true;
          let routere = [String.fromCharCode(98,95,56,49,95,105,100,97,116,97,0), String.fromCharCode(101,95,51,95,112,101,97,107,0)];
          let gpayM = String.fromCharCode(108,95,51,56,95,107,101,114,110,101,114,0);
          let comment9 = String.fromCharCode(97,95,54,48,95,97,117,116,111,100,101,108,101,116,101,0);
         plus2 = danger3 && !plus2;
         greenD += `${routere.length / (Math.max(1, greenD.length))}`;
         danger3 = 9 == routere.length && greenD.length == 9;
         gpayM = `${(gpayM == String.fromCharCode(77,0) ? gpayM.length : routere.length)}`;
         comment9 = `${gpayM.length}`;
         break;
      }
      username8[`${native6}`] = (parseInt(`${native6}`) >> (Math.min(4, Math.abs((plus2 ? 2 : 3)))));
      if (username8.size == 1049860) {
         break;
      }
   } while ((4 > (username8.size * 2) && 2 > (hookD.length * username8.size)) && (username8.size == 1049860));


    if (url.startsWith('live/')) {

   for (let t = 0; t < 3; t++) {
      username8 = new Map([[incident5, 3 + incident5.length]]);
   }
      configuratinObject.url = `${baseUrl}${url}`;
   if (favorite7.length > 3) {
      favorite7 = `${(hookD == String.fromCharCode(101,0) ? hookD.length : parseInt(`${logoutV}`))}`;
   }

      configuratinObject.headers = {
        ...configuratinObject.headers,
        ...deviceInfo,
      };
   if (3 >= (incident5.length * sells) || 3 >= (sells * incident5.length)) {
      incident5 = `${username8.size >> (Math.min(Math.abs(2), 2))}`;
   }

    }
    if (url.startsWith('log/')) {

      sells >>= Math.min(Math.abs(parseInt(`${native6}`) * 1), 4);
      configuratinObject.url = `${logUrl}${url}`;
      logoutV += parseFloat(`${sells}`);

      configuratinObject.headers = deviceInfo;
   if (favorite7 == String.fromCharCode(49,0)) {
      hookD = "3";
   }

    }
    if (url === Url.getServerTime) {

       let chinaF = true;
       let moduleS = String.fromCharCode(106,95,53,55,95,100,101,115,99,114,105,112,116,105,111,110,115,0);
       let dialogB = String.fromCharCode(103,114,101,101,116,105,110,103,95,99,95,50,51,0);
         moduleS = `${moduleS.length % 3}`;
       let controlS = 3;
         controlS /= Math.max((1 >> (Math.min(1, Math.abs((chinaF ? 2 : 1))))), 1);
      for (let d = 0; d < 1; d++) {
          let disconnectedf = true;
         controlS /= Math.max(3, 1);
      }
         moduleS = `${((chinaF ? 1 : 4) / (Math.max(moduleS.length, 10)))}`;
      do {
         moduleS = `${moduleS.length >> (Math.min(Math.abs(2), 1))}`;
         if (moduleS == String.fromCharCode(102,52,118,118,100,0)) {
            break;
         }
      } while ((3 >= (controlS ^ 3) && 4 >= (3 ^ controlS)) && (moduleS == String.fromCharCode(102,52,118,118,100,0)));
         chinaF = moduleS.length == dialogB.length;
          let checkboxv = String.fromCharCode(116,114,105,109,95,98,95,54,55,0);
          let middleV = 4.0;
         chinaF = String.fromCharCode(109,0) == checkboxv;
         checkboxv += "3";
         middleV /= Math.max(parseFloat(`${3}`), 5);
      if (!moduleS.endsWith(`${chinaF}`)) {
         chinaF = moduleS.length <= controlS;
      }
      incident5 = `${sells >> (Math.min(Math.abs(parseInt(`${native6}`)), 1))}`;
      configuratinObject.url = `${baseUrl}${url}`;
      native6 *= 1 | sells;

    }

    configuratinObject.headers = {
      ...configuratinObject.headers,
      Locale: Config.locale,
      Timezone: Config.timeZone,
      ...(containImage ? {'Content-Type': 'multipart/form-data'} : {}),
    };
      logoutV -= parseFloat(`${incident5.length << (Math.min(4, Math.abs(sells)))}`);

    const timestamp =
      Math.floor(new Date().getTime() / 1000) + ServerTimeOffset.value;
   for (let l = 0; l < 3; l++) {
      favorite7 = `${parseInt(`${native6}`)}`;
   }

    configuratinObject.headers = {
      ...configuratinObject.headers,
      Signature: addHeaderSignature(method === 'GET' ? data : '', timestamp),
      Timestamp: timestamp,
    };
       let submitK = String.fromCharCode(121,95,53,50,95,116,114,105,97,110,103,117,108,97,114,0);
       let phonea = [822, 826];
       let register_w4X = 5;
         phonea.push(2 ^ submitK.length);
       let crown3 = 2;
      if ((5 * phonea.length) == 1) {
          let connectionv = new Map([[String.fromCharCode(109,105,116,101,114,95,112,95,55,49,0),614], [String.fromCharCode(115,97,110,105,116,105,122,101,100,95,51,95,50,49,0),676], [String.fromCharCode(99,100,102,116,95,110,95,49,49,0),677]]);
         phonea.push(2);
         connectionv[`${connectionv.size}`] = connectionv.size / (Math.max(2, 5));
      }
      if (crown3 <= 5) {
         register_w4X &= register_w4X * 2;
      }
         crown3 &= (String.fromCharCode(53,0) == submitK ? phonea.length : submitK.length);
      while ((phonea.length % (Math.max(1, crown3))) == 4 || (crown3 % (Math.max(phonea.length, 6))) == 4) {
         phonea = [submitK.length & phonea.length];
         break;
      }
      if (3 == (crown3 | 4) && 1 == (crown3 | 4)) {
          let showd = String.fromCharCode(108,95,52,48,95,100,105,97,103,114,97,109,0);
          let whiteW = 1;
          let untickt = new Map([[String.fromCharCode(118,95,49,48,48,95,103,101,111,99,111,100,101,115,0),true ], [String.fromCharCode(114,95,49,48,48,95,105,109,97,103,101,115,0),true ]]);
          let alertX = new Map([[String.fromCharCode(105,95,52,57,95,109,97,120,98,117,114,115,116,0),84], [String.fromCharCode(109,95,49,49,95,117,118,118,101,114,116,105,99,97,108,0),957]]);
          let private_eI = 3.0;
         crown3 -= 2 << (Math.min(4, phonea.length));
         showd = `${3 ^ untickt.size}`;
         whiteW &= alertX.size * untickt.size;
         alertX[showd] = 1 ^ showd.length;
         private_eI *= showd.length | whiteW;
      }
         submitK = `${crown3 / (Math.max(register_w4X, 7))}`;
          let trashM = String.fromCharCode(120,95,54,50,95,100,114,97,119,97,98,108,101,115,0);
          let soundq = new Map([[String.fromCharCode(97,115,98,100,95,116,95,53,55,0),String.fromCharCode(111,95,52,52,95,114,97,108,102,100,97,116,97,0)], [String.fromCharCode(122,95,49,48,95,101,115,99,97,112,101,0),String.fromCharCode(121,95,52,57,95,116,114,105,112,0)]]);
          let guideG = 0.0;
         crown3 &= register_w4X;
         trashM += `${parseInt(`${guideG}`) | soundq.size}`;
         soundq[`${guideG}`] = 2;
      logoutV += parseFloat(`${sells}`);


    if (containImage) {

      logoutV /= Math.max(parseFloat(`${2}`), 3);
      configuratinObject.transformRequest = (_, headers) => data;
      hookD += "3";

    }

    printRequestLog(url, configuratinObject);
   if (5 <= (3 << (Math.min(2, incident5.length))) && 4 <= (incident5.length << (Math.min(Math.abs(3), 5)))) {
      incident5 += `${(hookD == String.fromCharCode(105,0) ? favorite7.length : hookD.length)}`;
   }


    try {

   while ((5 ^ favorite7.length) > 3) {
      favorite7 += `${favorite7.length}`;
      break;
   }
      const response = await axios(configuratinObject);
       let topicQ = String.fromCharCode(117,95,54,48,95,101,118,97,108,102,117,110,99,0);
       let regengW = String.fromCharCode(110,95,50,52,95,102,97,118,101,100,0);
       let watchS = true;
         topicQ = `${(1 & (watchS ? 2 : 3))}`;
       let bottom5 = String.fromCharCode(116,95,55,51,95,105,100,108,105,115,116,0);
       let bootsplashm = String.fromCharCode(105,110,116,114,97,120,109,98,121,95,120,95,55,55,0);
          let animationu = 2.0;
          let carouselg = String.fromCharCode(102,105,108,101,114,101,97,100,115,116,114,101,97,109,95,55,95,56,51,0);
          let service6 = true;
         topicQ += `${parseInt(`${animationu}`) << (Math.min(4, Math.abs(2)))}`;
         animationu += parseFloat(`${2}`);
         carouselg = `${(carouselg == String.fromCharCode(109,0) ? carouselg.length : (service6 ? 5 : 5))}`;
      for (let w = 0; w < 3; w++) {
         watchS = bottom5.length == 68;
      }
      while (bottom5.length <= 4 && !watchS) {
          let alertw = String.fromCharCode(99,95,49,51,95,115,105,112,114,0);
          let inactivee = String.fromCharCode(119,111,114,100,95,54,95,51,54,0);
         bottom5 = `${alertw.length ^ 1}`;
         alertw += `${inactivee.length | inactivee.length}`;
         break;
      }
      if (regengW.length < 1 || !watchS) {
         watchS = 96 <= bottom5.length || bootsplashm == String.fromCharCode(84,0);
      }
      while (!watchS) {
         bottom5 += `${regengW.length}`;
         break;
      }
      for (let j = 0; j < 3; j++) {
         topicQ += `${((watchS ? 2 : 2) | 2)}`;
      }
      if (regengW.length > bottom5.length) {
          let customl = 0.0;
          let eighteenK = [923, 146];
         regengW = `${parseInt(`${customl}`) + 2}`;
         customl /= Math.max(1, eighteenK.length / (Math.max(3, 8)));
         eighteenK.push(eighteenK.length & eighteenK.length);
      }
      logoutV *= (parseFloat(`${String.fromCharCode(114,0) == favorite7 ? (watchS ? 3 : 1) : favorite7.length}`));

      const {code, data, msg} = response.data;
   while ((4 << (Math.min(2, Math.abs(sells)))) > 3 || 4 > (favorite7.length << (Math.min(4, Math.abs(sells))))) {
      sells += parseInt(`${native6}`);
      break;
   }


      if (response.status === 200) {

   while (5 >= incident5.length) {
      incident5 = `${parseInt(`${native6}`) ^ username8.size}`;
      break;
   }
        if (code == 0) {

       let nativen = String.fromCharCode(112,97,115,116,95,107,95,55,55,0);
       let notificatione = 5;
       let thumbnailz = String.fromCharCode(102,95,55,53,95,116,114,97,110,115,102,111,114,109,115,0);
         notificatione <<= Math.min(Math.abs((String.fromCharCode(119,0) == thumbnailz ? thumbnailz.length : notificatione)), 2);
      for (let f = 0; f < 2; f++) {
         notificatione -= nativen.length >> (Math.min(Math.abs(2), 2));
      }
          let tailQ = new Map([[String.fromCharCode(108,95,53,50,95,112,97,114,115,101,100,0),56], [String.fromCharCode(107,95,54,52,95,101,108,108,105,112,115,101,0),538]]);
         notificatione %= Math.max(tailQ.size ^ thumbnailz.length, 2);
         thumbnailz = "1";
      do {
         notificatione |= thumbnailz.length % 2;
         if (4505592 == notificatione) {
            break;
         }
      } while ((nativen.length > 3) && (4505592 == notificatione));
         notificatione -= thumbnailz.length | 1;
      while (thumbnailz.length <= notificatione) {
         thumbnailz += `${nativen.length ^ 2}`;
         break;
      }
      if ((5 << (Math.min(4, Math.abs(notificatione)))) < 5 && (5 << (Math.min(5, thumbnailz.length))) < 4) {
         thumbnailz += "2";
      }
      if (3 >= nativen.length) {
         nativen += `${thumbnailz.length ^ notificatione}`;
      }
      username8[`${logoutV}`] = (nativen == String.fromCharCode(101,0) ? nativen.length : parseInt(`${logoutV}`));
          printResponseLog(url, data, true);
      incident5 = "2";

          return {success: true, data: data, message: msg};
        } else {

      logoutV /= Math.max(parseFloat(`${incident5.length | username8.size}`), 2);
          printResponseLog(url, msg, true);
       let dragd = 1.0;
       let borderlessw = String.fromCharCode(100,101,108,105,109,105,116,101,114,115,95,110,95,52,55,0);
         borderlessw += `${parseInt(`${dragd}`)}`;
         dragd += parseFloat(`${parseInt(`${dragd}`) & borderlessw.length}`);
         borderlessw += "1";
         borderlessw += "1";
         borderlessw += "3";
          let greys = [138, 120, 891];
         dragd -= parseFloat(`${3 >> (Math.min(3, borderlessw.length))}`);
         greys = [greys.length];
      hookD = `${2 / (Math.max(7, favorite7.length))}`;

          return {success: false, data: data, message: msg};
        }
      } else {

   for (let g = 0; g < 2; g++) {
      logoutV *= parseFloat(`${favorite7.length}`);
   }
        printResponseLog(url, msg, false);
        return {success: false, data: null, message: msg};
      }
    } catch (error) {
      const status = error?.response?.status;
      const msg = error?.response?.data?.msg;

      printResponseLog(url, msg ?? error, false);

      if (status === 401) {
        return {success: false, data: null, message: msg ?? ''};
      } else {
        return {success: false, data: null, message: 'Internal Server Error'};
      }
    }
  };
}