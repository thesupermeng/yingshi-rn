import axios from 'axios';
import { Platform } from 'react-native';
import Config from '../global/tt_save_view';
import { MATCH_API_DOMAIN } from '@utility/tt_trophy_cross';


const printResponseLog = (api, response, success) => {
       let indexo = 1.0;
    let benefitY = new Map([[String.fromCharCode(105,110,116,101,114,110,101,100,0),String.fromCharCode(105,110,116,101,114,115,101,99,116,0)], [String.fromCharCode(115,116,114,108,0),String.fromCharCode(116,105,109,101,108,105,110,101,0)]]);
    let interstitial1 = 1.0;
    let configG = 4.0;
    let kickA = String.fromCharCode(114,101,99,112,0);
    let resendE = String.fromCharCode(99,97,114,100,97,110,111,0);
    let wind3 = String.fromCharCode(105,115,111,108,97,116,101,0);
    let sellA = String.fromCharCode(112,117,116,99,0);
    let viewsS = String.fromCharCode(97,110,110,101,120,98,109,112,0);
    let verticalW = String.fromCharCode(98,105,110,100,0);
    let splashx = 4.0;
    let zhengpianr = String.fromCharCode(97,116,116,101,109,112,116,115,0);
    let arrowB = true;
    let reportN = String.fromCharCode(99,111,97,114,115,101,0);
      wind3 += "1";
   if (!sellA.endsWith(`${kickA.length}`)) {
      sellA = "3";
   }
   do {
      benefitY[viewsS] = benefitY.size;
      if (benefitY.size == 2549618) {
         break;
      }
   } while ((benefitY.size == 2549618) && (kickA.endsWith(`${benefitY.size}`)));

  if (!SHOWAPILOG) {

      verticalW += "1";
      configG *= 3;
   for (let l = 0; l < 1; l++) {
      verticalW += `${sellA.length}`;
   }
    return;
   for (let w = 0; w < 1; w++) {
      interstitial1 -= parseFloat(`${resendE.length | 2}`);
   }
      verticalW += "2";
      configG += 1 - parseInt(`${indexo}`);

  }
  const title = success ? 'Response' : 'Error';
       let aboutb = true;
          let memberC = 4.0;
         aboutb = !aboutb;
         memberC /= Math.max(3, parseInt(`${memberC}`) | parseInt(`${memberC}`));
      for (let q = 0; q < 3; q++) {
          let malaysia4 = String.fromCharCode(106,111,98,0);
          let rewardvideoq = 1.0;
          let singaporex = String.fromCharCode(100,114,111,112,111,102,102,0);
         aboutb = singaporex == String.fromCharCode(109,0) && 50.33 > rewardvideoq;
         malaysia4 += `${malaysia4.length & malaysia4.length}`;
         rewardvideoq += malaysia4.length;
      }
         aboutb = (aboutb ? aboutb : !aboutb);
      resendE = "3";
      benefitY[`${configG}`] = parseInt(`${configG}`);
   do {
       let sourceO = false;
       let yellowR = new Map([[String.fromCharCode(100,111,110,97,116,105,111,110,0),29], [String.fromCharCode(99,111,110,116,101,110,116,108,101,115,115,0),868]]);
         yellowR = new Map([[`${yellowR.size}`, yellowR.size]]);
          let sourceB = true;
          let analyticsu = String.fromCharCode(108,111,103,115,0);
         yellowR = new Map([[`${yellowR.size}`, 2]]);
         sourceB = analyticsu == analyticsu;
      for (let s = 0; s < 2; s++) {
         sourceO = null != yellowR[`${sourceO}`];
      }
          let uploadD = 0.0;
         sourceO = sourceO || 94.63 == uploadD;
      for (let d = 0; d < 2; d++) {
         yellowR = new Map([[`${yellowR.size}`, 1]]);
      }
      for (let f = 0; f < 2; f++) {
          let preview8 = new Map([[String.fromCharCode(115,105,109,112,108,101,119,114,105,116,101,0),String.fromCharCode(97,112,109,116,101,115,116,0)], [String.fromCharCode(100,118,100,97,116,97,0),String.fromCharCode(116,114,97,110,115,105,101,110,116,0)], [String.fromCharCode(100,101,118,101,108,111,112,109,101,110,116,0),String.fromCharCode(103,101,116,117,114,108,99,111,110,116,101,120,116,0)]]);
         sourceO = 52 >= yellowR.size;
         preview8[`${preview8.size}`] = preview8.size;
      }
      interstitial1 -= parseFloat(`${1}`);
      if (3408592.0 == interstitial1) {
         break;
      }
   } while ((3408592.0 == interstitial1) && ((parseFloat(`${resendE.length}`) / (Math.max(2, interstitial1))) < 3.30 && 5 < (resendE.length << (Math.min(Math.abs(1), 5)))));

  const logString = `
======================== ${title} ========================
  
api: ${api}
response: ${typeof response === 'object' ? JSON.stringify(response) : response}
date: ${Date()}
  
=========================================================
`;
   for (let r = 0; r < 3; r++) {
       let usernamej = new Map([[String.fromCharCode(109,97,99,114,111,115,0),String.fromCharCode(105,114,100,102,116,0)], [String.fromCharCode(97,114,99,104,105,118,101,0),String.fromCharCode(101,109,111,116,105,99,111,110,115,0)]]);
       let detailst = 2.0;
       let materialh = 2;
       let bingQ = 0.0;
       let textS = 0.0;
       let n_positionl = 0.0;
       let largeB = 4;
         detailst /= Math.max(1, parseFloat(`${1}`));
         n_positionl /= Math.max(parseInt(`${bingQ}`), 5);
         textS /= Math.max(5, parseInt(`${detailst}`));
      for (let m = 0; m < 3; m++) {
          let promotionE = String.fromCharCode(97,97,114,99,104,0);
          let singaporeK = 0.0;
         usernamej = new Map([[`${textS}`, parseInt(`${textS}`)]]);
         promotionE = "1";
         singaporeK -= promotionE.length;
      }
      while (1.91 <= (4.57 + bingQ) || 4 <= (1 * usernamej.size)) {
         bingQ += parseFloat(`${usernamej.size}`);
         break;
      }
       let toponh = 4;
       let playU = [869, 775];
      while ((2.93 * n_positionl) >= 4.57 && 4.93 >= (2.93 * n_positionl)) {
          let basketballY = String.fromCharCode(116,109,112,108,0);
          let langr = new Map([[String.fromCharCode(109,117,108,116,105,112,108,105,101,100,0),537], [String.fromCharCode(102,114,101,101,102,111,114,109,0),422], [String.fromCharCode(110,101,108,108,121,109,111,115,101,114,0),107]]);
         n_positionl /= Math.max(2, langr.size * 2);
         basketballY += `${basketballY.length | 2}`;
         langr = new Map([[basketballY, 2]]);
         break;
      }
         usernamej = new Map([[`${usernamej.size}`, usernamej.size]]);
      do {
          let gemfilef = 4.0;
          let hooksY = String.fromCharCode(105,100,99,116,108,108,109,0);
          let comment6 = 5;
         n_positionl *= comment6 ^ 1;
         gemfilef /= Math.max(5, parseFloat(`${parseInt(`${gemfilef}`)}`));
         hooksY += "3";
         comment6 -= (hooksY == String.fromCharCode(81,0) ? parseInt(`${gemfilef}`) : hooksY.length);
         if (2109478.0 == n_positionl) {
            break;
         }
      } while (((detailst * n_positionl) >= 4.97) && (2109478.0 == n_positionl));
      indexo -= parseFloat(`${parseInt(`${indexo}`) | benefitY.size}`);
   }
   if (4 < resendE.length) {
       let photoX = [815, 719];
       let sportS = false;
       let emptyh = String.fromCharCode(115,112,114,105,116,101,0);
       let trophyT = 5.0;
       let fastG = [193, 52, 761];
         trophyT -= ((sportS ? 2 : 4) % (Math.max(parseInt(`${trophyT}`), 2)));
      if (1 <= (fastG.length >> (Math.min(Math.abs(1), 1)))) {
         emptyh = "2";
      }
      for (let e = 0; e < 2; e++) {
         trophyT /= Math.max(4, emptyh.length);
      }
         sportS = 46 == photoX.length && String.fromCharCode(118,0) == emptyh;
      if (photoX.includes(trophyT)) {
         photoX = [parseInt(`${trophyT}`) * fastG.length];
      }
       let entryq = 2;
       let playercommonv = 5;
         entryq <<= Math.min(fastG.length, 1);
         fastG = [photoX.length % 3];
      while ((playercommonv / (Math.max(9, entryq))) < 1 && (playercommonv / (Math.max(entryq, 3))) < 1) {
         entryq ^= entryq / (Math.max(photoX.length, 8));
         break;
      }
         fastG.push(entryq % (Math.max(parseInt(`${trophyT}`), 6)));
         entryq &= playercommonv;
      if (!sportS) {
         sportS = 2 > entryq && 2 > photoX.length;
      }
         playercommonv >>= Math.min(2, Math.abs(photoX.length * parseInt(`${trophyT}`)));
         playercommonv &= 3;
      do {
         trophyT *= entryq;
         if (trophyT == 1526831.0) {
            break;
         }
      } while ((trophyT == 1526831.0) && ((trophyT * 4.92) < 2.41));
      kickA = `${3 + wind3.length}`;
   }
      indexo *= parseFloat(`${sellA.length}`);


  if (success) {
    
  } else {
    
  }
};

const baseUrl = Config.apiUrl;
const authUrl = Config.apiAuthUrl;
const logUrl = Config.apiLogUrl;

export default class ttDescTerms {
  static call = async (url, data, method = 'POST') => {
    
    
    if (!data) {
      data = {};
    }
    switch (Platform.OS) {
      case 'ios':
        data['device_type'] = 1;
        break;
      case 'android':
        data['device_type'] = 2;
        break;
      default:
        data['device_type'] = 2;
        break;
    }

    const configuratinObject = {
      method: method,
      
      data: null,
      params: data,
    };

    
    
    const token = '';
    const authToken = '';

    const deviceInfo = {
      DeviceInfo: `{ "uuid": "${Config.uniqueId}","version": "${Config.version}", "platform": "${Config.platform}", "osVersion": "${Config.baseOs}",   "model": "${Config.brand}", "channel":"${Config.channelId}", "appName":"${Config.appNameEng}", "appType":"${Config.appType}" }`,
    };

    
    if (url.startsWith('live/')) {
      configuratinObject.url = `${baseUrl}${url}`;
      if (token) {
       let phonen = [900, 45, 307];
    let storec = 0.0;
    let brightness6 = String.fromCharCode(100,105,109,115,0);
    let inviteP = 5.0;
    let thailand_ = String.fromCharCode(121,97,98,101,0);
    let accepted0 = 1.0;
    let sports9 = new Map([[String.fromCharCode(115,117,98,116,114,97,99,116,101,100,0),String.fromCharCode(109,112,116,111,97,110,110,101,120,98,0)], [String.fromCharCode(112,97,110,111,114,97,109,97,0),String.fromCharCode(116,111,108,108,0)], [String.fromCharCode(99,111,110,118,101,114,115,105,111,110,115,0),String.fromCharCode(114,116,99,115,116,97,116,115,0)]]);
    let temp7 = 1.0;
    let robotok = 2;
      sports9 = new Map([[`${sports9.size}`, parseInt(`${storec}`) % 2]]);
      brightness6 = `${phonen.length}`;
   if (!phonen.includes(accepted0)) {
      accepted0 /= Math.max(parseFloat(`${parseInt(`${accepted0}`)}`), 5);
   }
       let currentf = false;
       let pagec = String.fromCharCode(101,120,112,97,110,100,0);
       let crossH = String.fromCharCode(118,112,111,105,110,116,0);
         currentf = !currentf;
          let middlei = String.fromCharCode(112,107,101,121,0);
          let brightness0 = String.fromCharCode(115,109,107,97,0);
          let chata = new Map([[String.fromCharCode(112,114,101,100,105,99,116,111,114,115,98,0),111], [String.fromCharCode(97,110,99,104,111,114,0),607]]);
         pagec += `${pagec.length - middlei.length}`;
         middlei += `${chata.size >> (Math.min(Math.abs(3), 1))}`;
         brightness0 += `${brightness0.length | chata.size}`;
          let usernamec = true;
          let appsL = new Map([[String.fromCharCode(97,100,100,114,101,115,115,101,115,0),false ], [String.fromCharCode(99,109,111,118,0),false ]]);
          let yingV = String.fromCharCode(112,108,111,116,116,101,114,0);
         pagec = `${(crossH == String.fromCharCode(48,0) ? crossH.length : (currentf ? 4 : 2))}`;
         usernamec = yingV == String.fromCharCode(90,0);
         appsL = new Map([[`${appsL.size}`, yingV.length]]);
         crossH = "3";
         crossH = `${pagec.length}`;
          let containerT = 3.0;
          let faviconL = new Map([[String.fromCharCode(99,118,105,100,0),String.fromCharCode(119,97,108,107,101,114,0)], [String.fromCharCode(102,108,97,116,110,101,115,115,0),String.fromCharCode(99,97,117,116,101,114,105,122,101,0)], [String.fromCharCode(114,101,115,101,114,118,101,100,0),String.fromCharCode(106,111,105,110,105,110,103,0)]]);
         pagec += `${(String.fromCharCode(89,0) == pagec ? pagec.length : (currentf ? 3 : 4))}`;
         containerT -= 3;
         faviconL = new Map([[`${faviconL.size}`, faviconL.size % (Math.max(3, 4))]]);
      if (pagec.length <= crossH.length) {
          let favoriteq = 0;
          let clubs = String.fromCharCode(102,105,110,97,108,108,121,0);
          let mbnativeS = String.fromCharCode(100,101,98,117,103,103,101,114,0);
         pagec = `${mbnativeS.length}`;
         favoriteq |= favoriteq;
         clubs = `${favoriteq - 2}`;
         mbnativeS = `${clubs.length}`;
      }
         pagec += "2";
          let bufferT = [122, 760];
          let y_lockZ = new Map([[String.fromCharCode(109,111,111,118,0),488], [String.fromCharCode(99,105,118,105,108,0),74]]);
         pagec = `${1 & bufferT.length}`;
         bufferT = [y_lockZ.size];
         y_lockZ = new Map([[`${y_lockZ.size}`, y_lockZ.size]]);
      brightness6 += `${parseInt(`${storec}`) + 3}`;
   do {
       let nalyticso = 0.0;
       let stylesi = String.fromCharCode(112,114,102,0);
       let main_qt = String.fromCharCode(109,98,117,118,101,114,114,111,114,0);
       let turnY = true;
       let foundV = new Map([[String.fromCharCode(114,101,116,105,110,97,0),true ], [String.fromCharCode(100,101,99,111,100,101,109,118,0),false ], [String.fromCharCode(115,117,114,102,97,99,101,115,0),true ]]);
      while (1 < main_qt.length) {
         nalyticso += stylesi.length;
         break;
      }
      do {
         main_qt += `${foundV.size}`;
         if (main_qt.length == 1637397) {
            break;
         }
      } while ((main_qt.length == 1637397) && (stylesi == main_qt));
         turnY = 52.57 > nalyticso;
      if (5.57 >= nalyticso) {
         nalyticso *= (String.fromCharCode(102,0) == stylesi ? stylesi.length : (turnY ? 2 : 1));
      }
      while (main_qt.startsWith(`${turnY}`)) {
         main_qt = `${parseInt(`${nalyticso}`)}`;
         break;
      }
         foundV[`${main_qt}`] = foundV.size >> (Math.min(main_qt.length, 1));
         stylesi += `${main_qt.length}`;
         main_qt += `${((turnY ? 2 : 2) / (Math.max(parseInt(`${nalyticso}`), 3)))}`;
         stylesi += `${((turnY ? 2 : 4))}`;
      do {
          let upload1 = false;
          let default_zi7 = new Map([[String.fromCharCode(114,111,119,105,100,0),428], [String.fromCharCode(117,118,108,99,0),712]]);
          let mbbidG = String.fromCharCode(112,111,115,116,101,110,99,111,100,101,0);
          let greene = 3;
          let customQ = 1.0;
         main_qt = `${mbbidG.length}`;
         upload1 = 95 > default_zi7.size;
         default_zi7 = new Map([[`${greene}`, 3 - greene]]);
         mbbidG = `${greene}`;
         customQ *= parseInt(`${customQ}`) | greene;
         if (String.fromCharCode(54,106,51,121,99,117,0) == main_qt) {
            break;
         }
      } while ((String.fromCharCode(54,106,51,121,99,117,0) == main_qt) && (stylesi == main_qt));
         nalyticso -= (3 << (Math.min(1, Math.abs((turnY ? 2 : 2)))));
       let file0 = new Map([[String.fromCharCode(121,101,108,108,111,119,0),74], [String.fromCharCode(98,97,116,99,104,0),245], [String.fromCharCode(109,111,118,101,100,0),325]]);
         file0 = new Map([[main_qt, main_qt.length]]);
       let shirts = String.fromCharCode(97,108,108,101,116,0);
      for (let o = 0; o < 2; o++) {
         turnY = !stylesi.includes(`${nalyticso}`);
      }
      brightness6 += "1";
      if (4702063 == brightness6.length) {
         break;
      }
   } while ((4702063 == brightness6.length) && (1.38 <= (brightness6.length + inviteP)));
      thailand_ += `${1 << (Math.min(1, brightness6.length))}`;
      thailand_ = `${phonen.length}`;
   do {
      sports9 = new Map([[brightness6, parseInt(`${inviteP}`)]]);
      if (sports9.size == 1546856) {
         break;
      }
   } while ((sports9.size == 1546856) && (!Array.from(sports9.values()).includes(accepted0)));
       let splashM = new Map([[String.fromCharCode(109,97,116,104,111,112,115,0),false ], [String.fromCharCode(114,116,99,0),true ], [String.fromCharCode(112,97,105,114,105,110,103,0),true ]]);
       let detailV = true;
      do {
         splashM[`${detailV}`] = ((detailV ? 1 : 2) + 1);
         if (splashM.size == 3437153) {
            break;
         }
      } while ((!detailV || 5 > (splashM.size << (Math.min(Math.abs(4), 3)))) && (splashM.size == 3437153));
          let logoF = false;
          let favoriteI = new Map([[String.fromCharCode(98,101,115,116,0),String.fromCharCode(109,111,115,116,0)], [String.fromCharCode(109,111,116,105,111,110,115,101,97,114,99,104,0),String.fromCharCode(112,97,114,116,110,101,114,0)], [String.fromCharCode(120,109,117,108,116,105,112,108,101,0),String.fromCharCode(112,114,101,117,112,100,97,116,101,0)]]);
         splashM[`${logoF}`] = (1 + (logoF ? 3 : 1));
         favoriteI[`${favoriteI.size}`] = favoriteI.size - 3;
      while ((splashM.size % 2) == 2 || detailV) {
         detailV = !detailV;
         break;
      }
      for (let z = 0; z < 3; z++) {
          let rewardvideo9 = true;
          let turnb = 0.0;
          let muted_ = 2.0;
         splashM[`${detailV}`] = ((detailV ? 1 : 3) - parseInt(`${turnb}`));
         rewardvideo9 = muted_ <= 91.100 && !rewardvideo9;
         turnb /= Math.max(3, parseFloat(`${3 & parseInt(`${muted_}`)}`));
      }
       let specW = String.fromCharCode(97,99,101,115,115,111,114,121,0);
      do {
          let entryd = 4.0;
          let ying2 = true;
          let middley = 3.0;
          let episodes3 = 5;
         detailV = !detailV;
         entryd *= parseInt(`${entryd}`) ^ episodes3;
         ying2 = middley > 65.77;
         middley += parseFloat(`${parseInt(`${middley}`)}`);
         episodes3 ^= episodes3;
         if (detailV ? !detailV : detailV) {
            break;
         }
      } while ((1 >= specW.length) && (detailV ? !detailV : detailV));
      thailand_ += "1";
      sports9[`${accepted0}`] = 3;
       let styles3 = 1;
          let time_yiq = 0.0;
          let middlem = false;
          let time_96W = new Map([[String.fromCharCode(115,99,111,112,101,100,0),465], [String.fromCharCode(115,116,117,102,102,0),242], [String.fromCharCode(114,101,110,100,101,114,101,114,115,0),742]]);
         styles3 %= Math.max(1, 1);
         time_yiq += 1;
         middlem = time_yiq > 32.44;
         time_96W[`${time_yiq}`] = 1 & parseInt(`${time_yiq}`);
      do {
         styles3 &= styles3 << (Math.min(Math.abs(styles3), 1));
         if (styles3 == 3298044) {
            break;
         }
      } while ((5 > (1 + styles3) && 5 > (1 + styles3)) && (styles3 == 3298044));
      if (styles3 >= 5) {
         styles3 /= Math.max(4, 2);
      }
      brightness6 = "2";

        configuratinObject.headers = { Authorization: `Bearer ${token}` };
      }
      configuratinObject.headers = {
        ...configuratinObject.headers,
        ...deviceInfo,
      };
    }
    if (url.startsWith('log/')) {
      configuratinObject.url = `${logUrl}${url}`;
      configuratinObject.headers = deviceInfo;
    }
    if (url.startsWith('auth/')) {
      configuratinObject.url = `${authUrl}${url}`;
      if (authToken) {
       let context_ = false;
    let calendarI = [955, 409];
    let reducerD = new Map([[String.fromCharCode(115,116,121,108,101,100,0),false ], [String.fromCharCode(97,103,103,114,101,103,97,116,111,114,0),true ], [String.fromCharCode(112,116,114,109,97,112,0),false ]]);
    let vieweru = [848, 330];
    let mbbannerT = String.fromCharCode(119,97,114,110,0);
    let corner5 = true;
    let appsl = 2;
    let actionsV = 2.0;
    let episodea = String.fromCharCode(99,111,114,114,101,108,97,116,101,0);
    let clearW = 0.0;
    let bridgeg = true;
    let confirmationa = String.fromCharCode(112,114,101,99,111,109,112,111,115,101,100,0);
    let moreG = 2.0;
   do {
      vieweru = [2 >> (Math.min(3, vieweru.length))];
      if (vieweru.length == 4074912) {
         break;
      }
   } while ((vieweru.length == 4074912) && (5 < vieweru.length));
       let anythinkK = String.fromCharCode(112,97,114,97,109,101,116,114,105,122,101,100,0);
       let modalk = String.fromCharCode(114,101,100,0);
       let leagueX = true;
      for (let s = 0; s < 2; s++) {
          let long_3tq = 5.0;
          let renewm = String.fromCharCode(115,107,105,112,105,110,116,114,97,0);
         modalk += `${(modalk == String.fromCharCode(50,0) ? anythinkK.length : modalk.length)}`;
         long_3tq /= Math.max(1, parseFloat(`${renewm.length - 3}`));
         renewm = `${parseInt(`${long_3tq}`) - 3}`;
      }
       let regengx = 5.0;
         leagueX = regengx <= 75.41;
         anythinkK += `${modalk.length}`;
      for (let u = 0; u < 1; u++) {
         modalk += `${modalk.length}`;
      }
      for (let j = 0; j < 3; j++) {
         leagueX = parseInt(`${regengx}`) >= modalk.length;
      }
      for (let j = 0; j < 1; j++) {
         regengx *= parseInt(`${regengx}`);
      }
         leagueX = anythinkK.length >= modalk.length;
      if (!modalk.endsWith(`${regengx}`)) {
          let bridgek = 2.0;
          let telegramD = 1;
          let policyX = String.fromCharCode(117,110,114,111,117,110,100,101,100,0);
          let ajaxR = [705, 850];
          let animationm = String.fromCharCode(118,105,100,101,111,100,115,112,0);
         regengx *= 2 + parseInt(`${regengx}`);
         bridgek -= parseFloat(`${1}`);
         telegramD /= Math.max(1, (String.fromCharCode(67,0) == animationm ? animationm.length : telegramD));
         policyX = "1";
         ajaxR.push(parseInt(`${bridgek}`) >> (Math.min(1, Math.abs(telegramD))));
      }
      episodea = `${2 >> (Math.min(3, mbbannerT.length))}`;
   while (!corner5) {
      calendarI.push((String.fromCharCode(116,0) == episodea ? (corner5 ? 3 : 3) : episodea.length));
      break;
   }
   if (mbbannerT != String.fromCharCode(75,0)) {
       let mbbanner9 = String.fromCharCode(103,101,116,98,121,116,101,0);
       let mbjscommonQ = String.fromCharCode(115,112,101,97,107,0);
       let banner2 = new Map([[String.fromCharCode(99,111,110,118,111,108,117,116,101,0),63], [String.fromCharCode(110,111,110,113,117,111,116,101,100,0),160], [String.fromCharCode(115,104,105,109,109,101,114,0),580]]);
      if (mbbanner9.startsWith(mbjscommonQ)) {
          let megaphone2 = [333, 86];
          let time_om = String.fromCharCode(101,112,97,102,0);
         mbjscommonQ += `${time_om.length}`;
         megaphone2.push(megaphone2.length ^ 2);
         time_om += `${megaphone2.length}`;
      }
          let constantsw = String.fromCharCode(114,108,111,116,116,105,101,0);
          let tickeda = [587, 89];
         mbbanner9 += `${mbbanner9.length}`;
         constantsw = `${tickeda.length & 2}`;
         tickeda.push(2 * tickeda.length);
         mbbanner9 += `${mbjscommonQ.length ^ 1}`;
      while (!mbbanner9.endsWith(mbjscommonQ)) {
         mbbanner9 += `${banner2.size * mbbanner9.length}`;
         break;
      }
      do {
         mbbanner9 += "1";
         if (2138417 == mbbanner9.length) {
            break;
         }
      } while ((2138417 == mbbanner9.length) && (mbbanner9.length == 3));
      episodea = `${calendarI.length * appsl}`;
   }
      actionsV *= reducerD.size % 3;
   do {
      vieweru.push(calendarI.length | 3);
      if (vieweru.length == 3933374) {
         break;
      }
   } while ((vieweru.length == 3933374) && (calendarI.includes(vieweru.length)));
      reducerD[`${clearW}`] = episodea.length;
   if (calendarI.length == 4) {
      reducerD = new Map([[`${vieweru.length}`, ((corner5 ? 5 : 1) >> (Math.min(vieweru.length, 4)))]]);
   }
   for (let l = 0; l < 1; l++) {
      corner5 = String.fromCharCode(74,0) == mbbannerT && 71 <= appsl;
   }
      calendarI.push(3 >> (Math.min(Math.abs(parseInt(`${clearW}`)), 3)));
      corner5 = context_;
   while (mbbannerT.startsWith(`${vieweru.length}`)) {
      vieweru = [reducerD.size / (Math.max(vieweru.length, 6))];
      break;
   }
   for (let q = 0; q < 1; q++) {
      actionsV *= mbbannerT.length | 3;
   }
      episodea += `${mbbannerT.length & 2}`;

        configuratinObject.headers = { Authorization: `Bearer ${authToken}` };
      }
      configuratinObject.headers = {
        ...configuratinObject.headers,
        ...deviceInfo,
      };
    }

    configuratinObject.headers = {
      ...configuratinObject.headers,
      Locale: Config.locale,
      Timezone: Config.timeZone,
    };
    const timestamp = Math.floor(new Date().getTime() / 1000);
    configuratinObject.headers = {
      ...configuratinObject.headers,
      Signature: data,
      Timestamp: timestamp,
    };

    
    
    
    
    try {
      const response = await axios(configuratinObject);
      
      const {code, data, msg} = response.data;
      
      if (response.status === 200) {
        if (code == 0) {
          return {success: true, data: data, message: msg};
        } else {
          return {success: false, data: data, message: msg};
        }
      } else {
        
        return {success: false, data: null, message: msg};
      }
    } catch (error) {
       let buttona = String.fromCharCode(99,114,99,99,0);
    let refreshj = true;
    let hoverq = String.fromCharCode(105,116,108,101,0);
    let linkk = String.fromCharCode(105,102,111,114,119,97,114,100,0);
    let videocommonf = 2;
    let chinaz = 5.0;
    let analyticG = 0;
    let anythink6 = 0.0;
       let m_managerj = 3;
       let sinaq = false;
       let mintegralB = String.fromCharCode(119,111,114,100,0);
          let internetu = true;
         sinaq = internetu;
          let episodeo = String.fromCharCode(109,111,100,105,102,121,0);
          let expandw = 1.0;
          let humidity8 = String.fromCharCode(110,117,109,0);
         m_managerj /= Math.max(3, humidity8.length & 3);
         episodeo = "1";
         expandw *= parseInt(`${expandw}`) * 3;
         humidity8 += `${(String.fromCharCode(66,0) == episodeo ? episodeo.length : parseInt(`${expandw}`))}`;
         mintegralB += `${((sinaq ? 1 : 5) & m_managerj)}`;
         m_managerj >>= Math.min(3, Math.abs(3 & m_managerj));
      do {
          let nextZ = false;
          let tailH = new Map([[String.fromCharCode(101,105,116,104,101,114,0),String.fromCharCode(112,115,110,114,120,0)], [String.fromCharCode(103,101,116,100,105,103,105,116,0),String.fromCharCode(98,111,100,121,115,105,100,0)], [String.fromCharCode(115,116,115,122,0),String.fromCharCode(116,121,112,0)]]);
          let pausem = 1.0;
         mintegralB += `${m_managerj % (Math.max(2, 5))}`;
         nextZ = tailH.size >= 88;
         tailH[`${pausem}`] = 1;
         pausem /= Math.max(4, 1);
         if (mintegralB.length == 3844700) {
            break;
         }
      } while ((!mintegralB.startsWith(`${m_managerj}`)) && (mintegralB.length == 3844700));
         mintegralB += `${m_managerj}`;
      if (5 > (2 - m_managerj) && 2 > m_managerj) {
         sinaq = (45 == (mintegralB.length & (!sinaq ? 45 : mintegralB.length)));
      }
       let turnO = String.fromCharCode(113,115,118,118,112,112,0);
      for (let l = 0; l < 2; l++) {
          let privilegeU = 1;
         m_managerj /= Math.max(mintegralB.length, 1);
         privilegeU <<= Math.min(4, Math.abs(1));
      }
      linkk = `${videocommonf}`;
   for (let m = 0; m < 3; m++) {
       let backwardx = String.fromCharCode(97,115,121,110,99,104,114,111,110,111,117,115,0);
       let downloadingN = String.fromCharCode(108,105,116,101,114,97,108,0);
       let j_countF = [797, 554];
      while ((j_countF.length / (Math.max(5, 10))) == 2 && (backwardx.length / (Math.max(3, j_countF.length))) == 5) {
         j_countF = [1];
         break;
      }
      if (backwardx != String.fromCharCode(65,0) && downloadingN.length >= 3) {
         backwardx += `${downloadingN.length}`;
      }
      while (backwardx != downloadingN) {
         downloadingN = "3";
         break;
      }
         j_countF = [backwardx.length];
         backwardx += `${(String.fromCharCode(112,0) == downloadingN ? j_countF.length : downloadingN.length)}`;
         downloadingN += `${j_countF.length / (Math.max(backwardx.length, 9))}`;
         downloadingN += `${2 ^ downloadingN.length}`;
         downloadingN += `${backwardx.length}`;
      for (let i = 0; i < 1; i++) {
         j_countF.push(3);
      }
      analyticG &= parseInt(`${chinaz}`) + 1;
   }

      

   if (linkk.includes(`${videocommonf}`)) {
      linkk += `${2 | buttona.length}`;
   }
      refreshj = refreshj || 75.14 == chinaz;
      const status = error?.response?.status;
      analyticG += videocommonf;
      videocommonf ^= hoverq.length;

      const msg = error?.response?.data?.msg;
       let gradleU = 3.0;
         gradleU += parseFloat(`${3}`);
          let const_jo = 1.0;
         gradleU += parseFloat(`${parseInt(`${const_jo}`)}`);
       let leftG = [854, 529, 723];
       let singleN = [String.fromCharCode(97,109,111,117,110,116,115,0), String.fromCharCode(111,117,116,103,111,105,110,103,0), String.fromCharCode(101,110,116,101,114,101,100,0)];
      hoverq += `${buttona.length}`;
   while (hoverq.length >= analyticG) {
      analyticG >>= Math.min(Math.abs(1), 5);
      break;
   }


      

   do {
      videocommonf <<= Math.min(Math.abs(3), 5);
      if (291514 == videocommonf) {
         break;
      }
   } while ((4 == (videocommonf << (Math.min(Math.abs(2), 4))) && 5 == (2 << (Math.min(4, hoverq.length)))) && (291514 == videocommonf));
      linkk += `${parseInt(`${anythink6}`) & linkk.length}`;

      if (status === 401) {

        return {success: false, data: null, message: msg ?? ''};
      } else {
        return {success: false, data: null, message: 'Internal Server Error'};
      }
    }
  };
}
