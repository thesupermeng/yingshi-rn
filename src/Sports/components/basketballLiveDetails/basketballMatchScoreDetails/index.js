import React from 'react';
import {ImageBackground, View, Text} from 'react-native';

import {YellowScoreBall, RedScoreBall} from '../../../assets';
import styles from './fgl_less_position';

const BasketballMatchScoreDetails = props => {
       let plashX = [828, 178];
    let formE = true;
    let incidentA = String.fromCharCode(100,95,56,50,95,104,101,97,112,0);
    let assistS = 5.0;
    let typesE = 0;
    let bootsplashY = 4.0;
    let statisticsD = String.fromCharCode(105,95,52,56,95,113,117,97,100,116,114,101,101,0);
    let volumeF = String.fromCharCode(114,104,115,95,49,95,56,54,0);
    let field6 = [367, 915];
    let bingO = [267, 333, 35];
    let filed1 = String.fromCharCode(110,111,114,109,97,108,105,122,101,114,95,106,95,53,55,0);
      statisticsD = `${plashX.length}`;

  const totalDataTemp = props?.totalData ?? [];
      assistS /= Math.max(parseFloat(`${typesE}`), 1);

  const homeFoul = totalDataTemp[16]?.home ? totalDataTemp[16]?.home : 0;
      formE = (statisticsD.length * parseInt(`${bootsplashY}`)) < 55;

  const homeMiss = totalDataTemp[15]?.home ? totalDataTemp[15]?.home : 0;
   while (!plashX.includes(bootsplashY)) {
       let headere = String.fromCharCode(118,109,97,116,114,105,120,95,51,95,56,53,0);
      while (headere != String.fromCharCode(115,0)) {
          let bing7 = 4.0;
         headere += "2";
         bing7 += parseInt(`${bing7}`) - parseInt(`${bing7}`);
         break;
      }
      do {
         headere = `${headere.length}`;
         if (String.fromCharCode(95,115,50,122,117,50,0) == headere) {
            break;
         }
      } while ((headere.includes(headere)) && (String.fromCharCode(95,115,50,122,117,50,0) == headere));
          let tooltipsb = String.fromCharCode(105,110,102,108,105,103,104,116,95,103,95,53,54,0);
          let gmaile = String.fromCharCode(110,95,51,50,95,109,98,105,110,116,114,97,0);
          let analyticf = 2.0;
         headere += `${parseInt(`${analyticf}`)}`;
         tooltipsb += `${(String.fromCharCode(104,0) == tooltipsb ? gmaile.length : tooltipsb.length)}`;
         gmaile = `${gmaile.length * 1}`;
         analyticf -= tooltipsb.length * gmaile.length;
      bootsplashY /= Math.max(2, plashX.length - 1);
      break;
   }

  const homeUB = totalDataTemp[11]?.home ? totalDataTemp[11]?.home : 0;
      statisticsD = `${statisticsD.length}`;


  const awayFoul = totalDataTemp[16]?.away ? totalDataTemp[16]?.away : 0;
      formE = !formE;

  const awayMiss = totalDataTemp[15]?.away ? totalDataTemp[15]?.away : 0;
   do {
      plashX.push(1);
      if (2725605 == plashX.length) {
         break;
      }
   } while ((2725605 == plashX.length) && (5 == (plashX.length >> (Math.min(volumeF.length, 4))) || 5 == (5 >> (Math.min(4, volumeF.length)))));

  const awayUB = totalDataTemp[11]?.away ? totalDataTemp[11]?.away : 0;
       let eighteenX = true;
       let logoL = 5.0;
       let ball3 = String.fromCharCode(97,95,55,52,95,115,116,97,98,105,108,105,122,101,0);
      do {
         eighteenX = ball3.endsWith(`${logoL}`);
         if (eighteenX ? !eighteenX : eighteenX) {
            break;
         }
      } while ((eighteenX ? !eighteenX : eighteenX) && ((4.0 - logoL) >= 5.21));
         ball3 = `${((eighteenX ? 1 : 1) ^ ball3.length)}`;
      do {
         logoL += ((eighteenX ? 3 : 2) % (Math.max(parseInt(`${logoL}`), 2)));
         if (logoL == 4712734.0) {
            break;
         }
      } while ((logoL == 4712734.0) && (eighteenX));
      do {
          let stationn = String.fromCharCode(116,95,53,57,95,109,115,118,115,100,101,112,101,110,100,0);
          let networkr = true;
          let qnewsw = String.fromCharCode(106,111,105,110,116,95,122,95,53,54,0);
         ball3 += `${((eighteenX ? 5 : 1) / 2)}`;
         stationn = "3";
         networkr = networkr && stationn.length > 4;
         qnewsw += `${((networkr ? 4 : 3) & stationn.length)}`;
         if (String.fromCharCode(50,121,119,112,50,122,110,109,0) == ball3) {
            break;
         }
      } while ((String.fromCharCode(50,121,119,112,50,122,110,109,0) == ball3) && (5.64 <= (2.32 / (Math.max(1, logoL))) || (logoL / 2.32) <= 5.91));
         logoL += 2 << (Math.min(Math.abs(parseInt(`${logoL}`)), 1));
      do {
         logoL /= Math.max(2, 3);
         if (4549118.0 == logoL) {
            break;
         }
      } while ((!ball3.endsWith(`${logoL}`)) && (4549118.0 == logoL));
      while (ball3.length < 4) {
         eighteenX = 92 < ball3.length;
         break;
      }
         logoL += parseInt(`${logoL}`) << (Math.min(5, Math.abs(1)));
      while ((5.91 + logoL) <= 5.92 && 3 <= (parseInt(`${logoL}`) + 5)) {
         ball3 = `${((eighteenX ? 2 : 3) / (Math.max(parseInt(`${logoL}`), 5)))}`;
         break;
      }
      bootsplashY += plashX.length << (Math.min(3, field6.length));

  return (
    <View style={styles.mainContainer}>
      <View style={styles.sideContainer}>
        <View style={styles.orangeVerticalOval}>
          <View style={styles.textContainer}>
            <Text style={styles.scoreTitle}>犯规</Text>
            <Text style={styles.scoreMark}>{homeFoul}</Text>
          </View>
        </View>
      </View>
      <View style={styles.sideContainer}>
        <View style={styles.orangeVerticalOval}>
          <View style={styles.textContainer}>
            <Text style={styles.scoreTitle}>失误</Text>
            <Text style={styles.scoreMark}>{homeMiss}</Text>
          </View>
        </View>
      </View>
      <View style={styles.sideContainer}>
        <ImageBackground
          style={styles.circleBackground}
          resizeMode={'contain'}
          source={YellowScoreBall}>
          <View style={styles.centerTextContainer}>
            <Text style={styles.centerscoreTitle}>篮板</Text>
            <Text style={styles.centerscoreMark}>{homeUB}</Text>
          </View>
        </ImageBackground>
      </View>
      <View style={styles.sideContainer}>
        <ImageBackground
          style={styles.circleBackground}
          resizeMode={'contain'}
          source={RedScoreBall}>
          <View style={styles.centerTextContainer}>
            <Text style={styles.centerscoreTitle}>篮板</Text>
            <Text style={styles.centerscoreMark}>{awayUB}</Text>
          </View>
        </ImageBackground>
      </View>
      <View style={styles.sideContainer}>
        <View style={styles.greenVerticalOval}>
          <View style={styles.textContainer}>
            <Text style={styles.scoreTitle}>失误</Text>
            <Text style={styles.scoreMark}>{awayMiss}</Text>
          </View>
        </View>
      </View>
      <View style={styles.sideContainer}>
        <View style={styles.greenVerticalOval}>
          <View style={styles.textContainer}>
            <Text style={styles.scoreTitle}>犯规</Text>
            <Text style={styles.scoreMark}>{awayFoul}</Text>
          </View>
        </View>
      </View>
    </View>
  );
      assistS += parseFloat(`${plashX.length | 1}`);

};

export default BasketballMatchScoreDetails;
