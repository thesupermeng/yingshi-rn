import { View, Text, Image, ImageBackground } from 'react-native';
import { AwayIcon, HomeIcon } from '../../../assets';
import styles from './ww_utils_orangeclock';

const BasketballMatchTeamDetails = data => {
       let subsY = [String.fromCharCode(118,109,97,102,109,111,116,105,111,110,95,52,95,56,55,0), String.fromCharCode(119,101,98,115,111,99,107,101,116,95,53,95,52,50,0), String.fromCharCode(99,95,51,53,0)];
    let graphicsD = String.fromCharCode(99,104,115,99,97,108,101,95,101,95,53,0);
    let currentT = [625, 425, 714];
    let whitetickl = 2;
    let videocommonS = 3.0;
    let iconstare = false;
    let codegen7 = String.fromCharCode(116,101,120,105,100,101,112,95,57,95,52,54,0);
    let libcrashsdkk = 2.0;
    let foundZ = String.fromCharCode(115,117,98,106,101,99,116,115,95,116,95,51,55,0);
    let completek = true;
    let hejiX = false;
    let dangeru = 0.0;
   if (4 <= (graphicsD.length | currentT.length)) {
      currentT = [(2 | (iconstare ? 1 : 1))];
   }

  const totalDataTemp = data?.totalData ?? [];
   while (whitetickl <= 2) {
      whitetickl |= 1 * foundZ.length;
      break;
   }

  const thisTimeDataTemp = data?.thisTimeData ?? [];
      completek = !codegen7.startsWith(`${completek}`);

  const homeData = data?.homeData;
      hejiX = (36 <= (codegen7.length * (hejiX ? codegen7.length : 36)));

  const awayData = data?.awayData;
   do {
       let foregroundJ = String.fromCharCode(103,101,116,115,111,99,107,97,100,100,114,95,117,95,55,53,0);
       let connectioni = String.fromCharCode(99,114,97,115,104,101,100,95,103,95,54,57,0);
       let libfb_ = 1;
      for (let w = 0; w < 1; w++) {
         libfb_ %= Math.max(1 / (Math.max(3, connectioni.length)), 1);
      }
      if (4 > connectioni.length) {
         connectioni += `${connectioni.length - 3}`;
      }
          let zhengpianB = true;
         libfb_ <<= Math.min(Math.abs(libfb_ & 3), 2);
         zhengpianB = !zhengpianB;
      while (libfb_ == connectioni.length) {
         connectioni += `${(connectioni == String.fromCharCode(103,0) ? connectioni.length : libfb_)}`;
         break;
      }
      while (5 >= connectioni.length) {
         foregroundJ = `${foregroundJ.length}`;
         break;
      }
         connectioni += `${(String.fromCharCode(117,0) == foregroundJ ? foregroundJ.length : connectioni.length)}`;
      while (3 <= (connectioni.length & libfb_) || 3 <= (connectioni.length & libfb_)) {
         libfb_ %= Math.max(connectioni.length, 3);
         break;
      }
      while (libfb_ <= connectioni.length) {
         libfb_ >>= Math.min(4, Math.abs((String.fromCharCode(117,0) == foregroundJ ? foregroundJ.length : libfb_)));
         break;
      }
      if (connectioni.length > foregroundJ.length) {
          let unimplementedviewa = [229, 601, 698];
          let xvodc = new Map([[String.fromCharCode(109,111,117,115,95,52,95,56,53,0),549], [String.fromCharCode(115,101,114,105,97,108,105,122,101,95,52,95,50,52,0),923]]);
          let sourceE = 0.0;
         foregroundJ += `${parseInt(`${sourceE}`)}`;
         unimplementedviewa = [unimplementedviewa.length | 3];
         xvodc = new Map([[`${xvodc.size}`, xvodc.size]]);
         sourceE -= xvodc.size;
      }
      hejiX = (foregroundJ.length + currentT.length) == 59;
      if (hejiX ? !hejiX : hejiX) {
         break;
      }
   } while ((hejiX ? !hejiX : hejiX) && (!hejiX));

  const homeFoul = thisTimeDataTemp[4]?.home;
      graphicsD = `${3 >> (Math.min(Math.abs(parseInt(`${libcrashsdkk}`)), 4))}`;

  const awayFoul = thisTimeDataTemp[4]?.away;
   do {
      videocommonS /= Math.max(5, 1 ^ whitetickl);
      if (3633910.0 == videocommonS) {
         break;
      }
   } while ((3633910.0 == videocommonS) && (5.8 <= (videocommonS / 3.34)));

  const homeLeftPause = thisTimeDataTemp[3]?.home;
       let largeA = String.fromCharCode(109,101,109,111,114,121,115,116,114,101,97,109,95,57,95,52,49,0);
       let launcherk = String.fromCharCode(118,95,55,51,95,97,114,116,0);
      do {
          let moonL = 4.0;
          let teamdetailsbgf = 1.0;
         largeA += "2";
         moonL += parseInt(`${teamdetailsbgf}`);
         if (181205 == largeA.length) {
            break;
         }
      } while ((181205 == largeA.length) && (largeA.length == launcherk.length));
      while (launcherk != String.fromCharCode(78,0)) {
          let iconplayy = String.fromCharCode(111,118,101,114,108,97,121,105,110,103,95,116,95,57,54,0);
          let binddatasu = 1.0;
          let u_playerQ = 4.0;
         largeA += `${largeA.length}`;
         iconplayy += `${(iconplayy == String.fromCharCode(78,0) ? parseInt(`${binddatasu}`) : iconplayy.length)}`;
         binddatasu *= 3 - parseInt(`${u_playerQ}`);
         u_playerQ -= parseFloat(`${parseInt(`${u_playerQ}`) & parseInt(`${binddatasu}`)}`);
         break;
      }
         launcherk += `${3 ^ launcherk.length}`;
      if (launcherk.length <= 5) {
         launcherk += `${largeA.length * 3}`;
      }
         launcherk = `${largeA.length}`;
      while (launcherk.length <= 2 && 2 <= largeA.length) {
         largeA += `${(largeA == String.fromCharCode(81,0) ? launcherk.length : largeA.length)}`;
         break;
      }
      hejiX = subsY.length >= 26;

  const awayLeftPause = thisTimeDataTemp[3]?.away;
   if (graphicsD.length <= currentT.length) {
      currentT.push(parseInt(`${libcrashsdkk}`));
   }

  let structuredData = [
    {
      title: '3分球',
      home: totalDataTemp[4]?.home ? totalDataTemp[4]?.home : 0,
      away: totalDataTemp[4]?.away ? totalDataTemp[4]?.away : 0,
    },

    {
      title: '2分球',
      home: totalDataTemp[20]?.home ? totalDataTemp[20]?.home : 0,
      away: totalDataTemp[20]?.away ? totalDataTemp[20]?.away : 0,
    },
    {
      title: '罚球',
      home: totalDataTemp[7]?.home ? totalDataTemp[7]?.home : 0,
      away: totalDataTemp[7]?.away ? totalDataTemp[7]?.away : 0,
    },
    {
      title: '罚球命中率',
      home: totalDataTemp[9]?.home ? totalDataTemp[9]?.home : 0,
      away: totalDataTemp[9]?.away ? totalDataTemp[9]?.away : 0,
    },
    
    
    
    
    
    
    //     ).toFixed(1) > 0
    
    
    
    
    
    
    
    
    
    //     ).toFixed(1) > 0
    
    
    
    
    
    
  ];
   do {
      libcrashsdkk /= Math.max(parseFloat(`${parseInt(`${libcrashsdkk}`)}`), 5);
      if (libcrashsdkk == 1730650.0) {
         break;
      }
   } while ((5 <= (parseInt(`${libcrashsdkk}`) * codegen7.length) || (codegen7.length * parseInt(`${libcrashsdkk}`)) <= 5) && (libcrashsdkk == 1730650.0));


  let calWidth = (data1, data2) => {

   if (hejiX) {
       let stepH = new Map([[String.fromCharCode(113,95,53,54,95,114,97,119,0),238], [String.fromCharCode(98,95,57,52,95,111,110,116,111,0),102]]);
       let predictionactiveP = new Map([[String.fromCharCode(102,105,110,103,101,114,95,99,95,49,57,0),false ], [String.fromCharCode(103,101,110,101,114,97,116,101,95,49,95,54,51,0),true ]]);
         stepH = new Map([[`${predictionactiveP.size}`, stepH.size >> (Math.min(Math.abs(1), 4))]]);
      for (let y = 0; y < 2; y++) {
          let sigmobU = 1.0;
          let modal9 = String.fromCharCode(119,95,57,56,95,112,111,115,115,105,98,108,101,0);
          let clockf = 3;
          let iconfeedback8 = String.fromCharCode(115,95,57,56,95,120,105,112,104,108,97,99,105,110,103,0);
         predictionactiveP = new Map([[iconfeedback8, (String.fromCharCode(105,0) == iconfeedback8 ? clockf : iconfeedback8.length)]]);
         sigmobU += parseInt(`${sigmobU}`);
         modal9 = `${(String.fromCharCode(103,0) == modal9 ? parseInt(`${sigmobU}`) : modal9.length)}`;
         clockf >>= Math.min(Math.abs(1 & parseInt(`${sigmobU}`)), 2);
      }
      do {
         predictionactiveP = new Map([[`${predictionactiveP.size}`, 2]]);
         if (1733151 == predictionactiveP.size) {
            break;
         }
      } while (((predictionactiveP.size & 2) <= 3) && (1733151 == predictionactiveP.size));
      while ((stepH.size - 2) == 5 && (2 - predictionactiveP.size) == 4) {
          let catagoryT = false;
          let dialogI = 2.0;
         stepH = new Map([[`${predictionactiveP.size}`, 2]]);
         catagoryT = !catagoryT || dialogI <= 21.62;
         dialogI /= Math.max(parseInt(`${dialogI}`) - 2, 3);
         break;
      }
          let tumbnailT = new Map([[String.fromCharCode(112,114,101,100,114,97,119,110,95,50,95,56,48,0),775], [String.fromCharCode(102,114,97,109,101,115,101,116,116,101,114,95,50,95,56,49,0),313], [String.fromCharCode(99,95,53,54,95,114,101,115,105,100,117,97,108,0),724]]);
         predictionactiveP = new Map([[`${stepH.size}`, 1]]);
         tumbnailT[`${tumbnailT.size}`] = tumbnailT.size / (Math.max(tumbnailT.size, 5));
      do {
         predictionactiveP = new Map([[`${stepH.size}`, 1 ^ stepH.size]]);
         if (predictionactiveP.size == 355365) {
            break;
         }
      } while ((!Array.from(predictionactiveP.values()).includes(stepH.size)) && (predictionactiveP.size == 355365));
      hejiX = whitetickl == 19;
   }
    return (Number(data1) / (Number(data1) + Number(data2))) * 100;
   do {
      dangeru -= parseFloat(`${whitetickl % 3}`);
      if (dangeru == 3834934.0) {
         break;
      }
   } while ((1.80 <= dangeru) && (dangeru == 3834934.0));

  };
      iconstare = codegen7.length <= 25;


  return (
    <View style={styles.mainContainer}>
      <View style={styles.teamsView}>
        <View style={styles.teamView}>
          {homeData?.icon != undefined && homeData?.icon.length > 0 ? (
            <Image style={styles.teamImage} source={{ uri: homeData?.icon }} />
          ) : (
            <ImageBackground source={HomeIcon} style={styles.teamImage}>
              <Text style={styles.teamImageText(data.textColor)}>
                {homeData?.name.charAt(0)}
              </Text>
            </ImageBackground>
          )}
          <Text style={styles.teamText(data.textColor)} numberOfLines={1} ellipsizeMode="tail">
            {homeData?.name}
          </Text>
        </View>
        <View style={styles.teamView}>
          <Text style={styles.teamText(data.textColor)} numberOfLines={1} ellipsizeMode="tail">
            {awayData?.name}
          </Text>
          {awayData?.icon != undefined && awayData?.icon.length > 0 ? (
            <Image style={styles.teamImage} source={{ uri: awayData?.icon }} />
          ) : (
            <ImageBackground source={AwayIcon} style={styles.teamImage}>
              <Text style={styles.teamImageText(data.textColor)}>
                {awayData?.name.charAt(0)}
              </Text>
            </ImageBackground>
          )}
        </View>
      </View>
      <View style={styles.averageContainer}>
        <View style={styles.verticalMarkContainer}>
          <View style={styles.numberMatchContainer}>
            <Text style={styles.numberMatchTitle(data.textColor)}>本节犯规数</Text>
            <Text style={styles.numberMatchValue(data.textColor)}>
              {homeFoul ? homeFoul : '0'}
            </Text>
          </View>
          <View style={styles.numberMatchContainer}>
            <Text style={styles.numberMatchTitle(data.textColor)}>剩余暂停</Text>
            <Text style={styles.numberMatchValue(data.textColor)}>
              {homeLeftPause ? homeLeftPause : '0'}
            </Text>
          </View>
        </View>
        <View
          style={{
            
            display: 'flex',
            width: '50%',
            
            alignItems: 'center',
          }}>
          {structuredData.map((item, idx) => {

   for (let b = 0; b < 2; b++) {
      iconstare = completek && hejiX;
   }
            let tempPercent = 0;
      currentT = [parseInt(`${libcrashsdkk}`) << (Math.min(2, Math.abs(3)))];

            tempPercent = calWidth(item.home, item.away) + '%';
   if ((1.60 + videocommonS) == 4.74) {
      videocommonS *= parseInt(`${videocommonS}`) & currentT.length;
   }

            if (item.home == 0 && item.away == 0) {

       let gpayd = 5.0;
       let zhubol = 0.0;
       let bgvipxvodI = String.fromCharCode(105,95,56,55,95,101,120,99,105,116,97,116,105,111,110,0);
      while ((parseInt(`${gpayd}`) / (Math.max(8, bgvipxvodI.length))) <= 2 && (gpayd / 5.79) <= 2.16) {
         gpayd -= parseFloat(`${parseInt(`${zhubol}`) << (Math.min(Math.abs(3), 5))}`);
         break;
      }
      for (let n = 0; n < 3; n++) {
         zhubol /= Math.max(parseInt(`${gpayd}`) % (Math.max(2, 9)), 2);
      }
         zhubol /= Math.max(1, 1 - parseInt(`${gpayd}`));
       let albumX = [753, 862];
         zhubol += bgvipxvodI.length >> (Math.min(Math.abs(1), 3));
       let leftu = 0.0;
       let holderW = 0.0;
       let shielddonef = String.fromCharCode(100,95,55,48,95,105,111,115,0);
       let gmail3 = String.fromCharCode(121,95,57,52,95,102,108,97,115,104,0);
       let rules9 = String.fromCharCode(116,102,104,100,95,116,95,53,0);
         holderW -= (shielddonef == String.fromCharCode(114,0) ? parseInt(`${zhubol}`) : shielddonef.length);
      completek = 93 >= currentT.length;
              tempPercent = '50%';
   if (5.2 == (libcrashsdkk - dangeru)) {
       let libavformatY = String.fromCharCode(104,95,54,55,95,115,100,101,115,0);
       let libtany = String.fromCharCode(121,95,49,95,105,110,105,116,97,108,105,122,101,0);
         libtany += `${libavformatY.length}`;
      if (libtany.length == 5) {
         libtany += `${(String.fromCharCode(75,0) == libtany ? libavformatY.length : libtany.length)}`;
      }
          let changeN = [String.fromCharCode(108,95,55,55,95,105,116,101,114,97,116,105,111,110,0), String.fromCharCode(112,114,111,98,108,101,109,95,50,95,49,52,0)];
          let settingsT = false;
         libavformatY = `${(String.fromCharCode(65,0) == libavformatY ? changeN.length : libavformatY.length)}`;
         changeN = [2];
         libtany += `${2 << (Math.min(1, libavformatY.length))}`;
         libtany += "2";
         libtany += `${(String.fromCharCode(100,0) == libtany ? libavformatY.length : libtany.length)}`;
      libcrashsdkk += parseFloat(`${codegen7.length}`);
   }

            }
            return (
              <View
                style={{
                  flex: 1,
                  display: 'flex',
                  justifyContent: 'center',
                  alignItems: 'center',
                  
                }}
                key={`basketballMatchDetail-${idx}`}>
                <View style={styles.statTitleView}>
                  <Text style={styles.statTitleText(data.textColor)}>{item.title}</Text>
                </View>
                <View style={styles.statView}>
                  <View style={[styles.statTextView, { alignItems: 'flex-end' }]}>
                    <Text style={styles.statText(data.textColor)}>{item.home}</Text>
                  </View>
                  <View style={styles.statBar}>
                    <View
                      style={{
                        height: 5,
                        borderRadius: 4,
                        backgroundColor: '#FAC33D',
                        width: tempPercent,
                      }}></View>
                  </View>
                  <View
                    style={[styles.statTextView, { alignItems: 'flex-start' }]}>
                    <Text style={styles.statText(data.textColor)}>{item.away}</Text>
                  </View>
                </View>
              </View>
            );
          })}
        </View>
        <View style={styles.verticalMarkContainer}>
          <View style={styles.numberMatchContainer}>
            <Text style={styles.numberMatchTitle(data.textColor)}>本节犯规数</Text>
            <Text style={styles.numberMatchValue(data.textColor)}>
              {awayFoul ? awayFoul : '0'}
            </Text>
          </View>
          <View style={styles.numberMatchContainer}>
            <Text style={styles.numberMatchTitle(data.textColor)}>剩余暂停</Text>
            <Text style={styles.numberMatchValue(data.textColor)}>
              {awayLeftPause ? awayLeftPause : '0'}
            </Text>
          </View>
        </View>
      </View>
    </View>
  );
      subsY = [((iconstare ? 3 : 3) - 2)];

};

export default BasketballMatchTeamDetails;
