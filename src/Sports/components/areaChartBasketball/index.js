import {Image, Text, View, useWindowDimensions} from 'react-native';
import {LineChart} from 'react-native-chart-kit';
import {Colors} from '../../global/tt_umeng';
import DefaultTeamIcon from '../defaultTeamIcon';
import styles from './tt_with_success';
import {calculateScore} from '../../utility/utils';

const AreaChartBasketball = props => {
       let detailS = 4;
    let nextn = new Map([[String.fromCharCode(105,110,100,105,118,105,100,117,97,108,0),82], [String.fromCharCode(102,105,108,116,101,114,0),76], [String.fromCharCode(100,117,112,108,105,99,97,116,101,0),390]]);
    let loadingA = 2.0;
    let apps4 = 4.0;
    let d_imager = 4.0;
    let colorsd = String.fromCharCode(111,110,101,108,105,110,101,0);
    let floateru = String.fromCharCode(98,105,111,0);
    let actionsy = String.fromCharCode(101,110,100,0);
    let tailU = 2;
    let unselectedK = true;
    let hookr = 2.0;
    let historyW = true;
    let contextt = String.fromCharCode(99,111,111,107,105,101,115,0);
    let delegate_1i2 = 5;
    let humidityt = String.fromCharCode(97,97,99,100,101,99,116,97,98,0);
    let forwardL = true;
      unselectedK = (tailU + nextn.size) <= 22;

  const {liveRoomUpdate} = props;
      hookr -= tailU;

  if (!liveRoomUpdate || !liveRoomUpdate?.basketball_match_trend) return;
      colorsd = `${parseInt(`${loadingA}`)}`;

  const {basketball_match_trend, home, away} = liveRoomUpdate;
   for (let t = 0; t < 3; t++) {
      tailU ^= 1 - parseInt(`${apps4}`);
   }

  const count = basketball_match_trend?.count;
       let kuaishouo = 2;
       let mbridge7 = String.fromCharCode(102,105,108,108,105,110,0);
      do {
          let connectionl = String.fromCharCode(97,115,102,99,114,121,112,116,0);
          let langA = String.fromCharCode(114,101,112,108,97,99,101,100,0);
          let play7 = 1.0;
          let humidityw = String.fromCharCode(119,104,105,116,101,115,112,97,99,101,115,0);
          let soundT = 0;
         kuaishouo <<= Math.min(Math.abs(1 >> (Math.min(5, Math.abs(kuaishouo)))), 3);
         connectionl += `${2 + connectionl.length}`;
         langA = `${humidityw.length | 3}`;
         play7 -= parseFloat(`${humidityw.length}`);
         soundT >>= Math.min(Math.abs(2), 2);
         if (3744996 == kuaishouo) {
            break;
         }
      } while ((4 > (mbridge7.length ^ 1) || 2 > (mbridge7.length ^ 1)) && (3744996 == kuaishouo));
         kuaishouo -= kuaishouo >> (Math.min(Math.abs(3), 3));
       let common6 = String.fromCharCode(107,117,107,105,0);
         mbridge7 = `${mbridge7.length}`;
          let less2 = new Map([[String.fromCharCode(100,97,116,101,116,105,109,101,0),743], [String.fromCharCode(101,108,101,109,0),720]]);
          let servicer = String.fromCharCode(99,111,110,115,101,99,117,116,105,118,101,0);
          let frame_lh = String.fromCharCode(112,114,101,112,97,114,105,110,103,0);
         common6 = `${servicer.length + 1}`;
         less2[`${frame_lh}`] = (frame_lh == String.fromCharCode(51,0) ? frame_lh.length : less2.size);
         servicer = "2";
      do {
         kuaishouo += 2;
         if (kuaishouo == 2959559) {
            break;
         }
      } while ((kuaishouo == 2959559) && (5 == (kuaishouo + mbridge7.length)));
      tailU <<= Math.min(Math.abs(colorsd.length ^ 3), 2);
 

      floateru = `${floateru.length / 1}`;
  const per = basketball_match_trend?.per;
      detailS -= 2 | nextn.size;
 

      nextn = new Map([[`${historyW}`, 2 | parseInt(`${apps4}`)]]);
  const max = basketball_match_trend?.max;
       let crownU = [733, 816, 799];
       let fullS = 4.0;
         crownU = [crownU.length & parseInt(`${fullS}`)];
         fullS -= parseFloat(`${parseInt(`${fullS}`) * crownU.length}`);
       let math4 = true;
       let verticald = false;
          let less9 = String.fromCharCode(115,119,97,98,0);
          let chartE = String.fromCharCode(98,105,103,117,105,110,116,0);
          let policyh = String.fromCharCode(99,111,108,108,97,103,101,0);
         math4 = !verticald;
         less9 += `${(less9 == String.fromCharCode(116,0) ? less9.length : chartE.length)}`;
         chartE += `${chartE.length}`;
         policyh = `${less9.length}`;
         crownU.push(parseInt(`${fullS}`) ^ 1);
      do {
         fullS *= (parseFloat(`${(math4 ? 2 : 3) + (verticald ? 4 : 1)}`));
         if (3123963.0 == fullS) {
            break;
         }
      } while ((3123963.0 == fullS) && ((fullS / (Math.max(2.24, 5))) < 5.27));
      unselectedK = hookr == 4.15 || d_imager == 4.15;
 

   for (let p = 0; p < 1; p++) {
       let agreementH = String.fromCharCode(97,109,114,119,98,100,97,116,97,0);
       let teamX = String.fromCharCode(105,101,101,101,0);
          let half4 = String.fromCharCode(100,105,115,99,111,114,100,0);
          let flipperI = [535, 261];
         teamX += `${(teamX == String.fromCharCode(72,0) ? flipperI.length : teamX.length)}`;
         half4 += `${half4.length ^ half4.length}`;
         flipperI = [half4.length];
          let long_iK = String.fromCharCode(97,99,114,111,115,115,102,97,100,101,0);
          let field2 = false;
         teamX = `${(String.fromCharCode(74,0) == agreementH ? agreementH.length : teamX.length)}`;
         long_iK += `${long_iK.length}`;
         field2 = long_iK.length > 92;
      do {
          let with_t6a = String.fromCharCode(115,99,104,101,109,101,115,0);
          let default_8_ = false;
          let s_positionI = 5.0;
         agreementH += `${(agreementH == String.fromCharCode(53,0) ? agreementH.length : with_t6a.length)}`;
         with_t6a += `${3 * parseInt(`${s_positionI}`)}`;
         default_8_ = 72.89 <= s_positionI;
         if (agreementH.length == 1545350) {
            break;
         }
      } while ((!agreementH.endsWith(`${teamX.length}`)) && (agreementH.length == 1545350));
          let checkboxX = 0.0;
          let descE = 4;
         teamX = `${parseInt(`${checkboxX}`) << (Math.min(agreementH.length, 1))}`;
         checkboxX *= descE;
       let turnw = 2.0;
         teamX += `${parseInt(`${turnw}`) % 3}`;
      d_imager -= parseFloat(`${1}`);
   }
  const matchStatus = liveRoomUpdate?.status;
   if (!contextt.endsWith(`${d_imager}`)) {
      d_imager += parseFloat(`${1 << (Math.min(3, floateru.length))}`);
   }

  const homeScore = calculateScore(
    liveRoomUpdate?.home_score,
    liveRoomUpdate?.sports_type,
  );
       let albumc = new Map([[String.fromCharCode(112,115,101,117,100,111,0),433], [String.fromCharCode(112,114,111,109,112,116,0),196]]);
       let homei = String.fromCharCode(105,110,118,101,114,116,0);
         homei = `${homei.length / 3}`;
         albumc = new Map([[`${albumc.size}`, albumc.size]]);
      nextn = new Map([[`${albumc.size}`, colorsd.length * 2]]);

  const awayScore = calculateScore(
    liveRoomUpdate?.away_score,
    liveRoomUpdate?.sports_type,
  );
      historyW = !unselectedK;

  const scoreDif = Math.abs(homeScore - awayScore);
   while ((2 >> (Math.min(3, contextt.length))) <= 4 || (contextt.length >> (Math.min(Math.abs(2), 4))) <= 3) {
       let kuaishou6 = false;
       let away1 = String.fromCharCode(109,105,110,105,109,105,122,101,100,0);
       let ajaxk = String.fromCharCode(105,100,101,110,116,105,116,121,0);
       let strC = 4.0;
      do {
         kuaishou6 = (69 >= ((!kuaishou6 ? 69 : away1.length) / (Math.max(away1.length, 2))));
         if (kuaishou6 ? !kuaishou6 : kuaishou6) {
            break;
         }
      } while ((kuaishou6) && (kuaishou6 ? !kuaishou6 : kuaishou6));
         kuaishou6 = strC <= parseFloat(`${away1.length}`);
      for (let a = 0; a < 1; a++) {
         away1 = `${(3 | (kuaishou6 ? 4 : 4))}`;
      }
          let logoU = String.fromCharCode(120,102,111,114,109,101,100,0);
          let matchU = 0.0;
         ajaxk += `${3 - parseInt(`${strC}`)}`;
         logoU = `${logoU.length * 3}`;
         matchU -= (parseFloat(`${logoU == String.fromCharCode(118,0) ? parseInt(`${matchU}`) : logoU.length}`));
      if (5 >= away1.length) {
         away1 += `${(away1 == String.fromCharCode(55,0) ? away1.length : (kuaishou6 ? 2 : 3))}`;
      }
         kuaishou6 = !kuaishou6;
         away1 += `${ajaxk.length / 2}`;
      while (kuaishou6) {
          let interstitialX = 1;
          let pointm = String.fromCharCode(105,100,99,116,99,111,108,0);
          let targetP = String.fromCharCode(105,110,116,101,114,115,101,99,116,105,111,110,0);
         ajaxk = `${interstitialX}`;
         interstitialX &= 2;
         pointm += `${pointm.length}`;
         targetP = `${pointm.length % (Math.max(2, 10))}`;
         break;
      }
      while (ajaxk.length == 3) {
         away1 += `${parseInt(`${strC}`)}`;
         break;
      }
          let calendars = new Map([[String.fromCharCode(114,97,110,115,102,111,114,109,0),494], [String.fromCharCode(112,116,114,115,0),57], [String.fromCharCode(100,114,105,102,116,0),394]]);
          let roundr = String.fromCharCode(109,117,108,116,105,102,114,97,109,101,0);
         away1 = `${away1.length + ajaxk.length}`;
         calendars[roundr] = calendars.size | 2;
         roundr += `${(String.fromCharCode(70,0) == roundr ? calendars.size : roundr.length)}`;
         strC -= parseFloat(`${2 & ajaxk.length}`);
       let actionsH = 1;
      contextt = `${(contextt.length & (kuaishou6 ? 5 : 5))}`;
      break;
   }

  const totalScore = homeScore + awayScore;
   while (2.93 <= (apps4 + d_imager)) {
      apps4 /= Math.max(4, parseFloat(`${1}`));
      break;
   }

  const chartData = basketball_match_trend?.data ?? [];
      unselectedK = parseFloat(`${floateru.length}`) > d_imager;

  const {width} = useWindowDimensions();
  const widthLength = width * 0.82;
       let math3 = String.fromCharCode(114,97,100,105,97,108,0);
         math3 = "2";
         math3 = "1";
         math3 += `${(String.fromCharCode(66,0) == math3 ? math3.length : math3.length)}`;
      hookr -= math3.length + 3;
 

      d_imager /= Math.max(parseFloat(`${colorsd.length}`), 2);

  let newArray = [];
      d_imager -= parseFloat(`${actionsy.length}`);
 

      colorsd += "3";
  const positive = num => num > 0;
   do {
       let routerj = new Map([[String.fromCharCode(116,119,111,102,105,115,104,0),391], [String.fromCharCode(99,105,114,99,108,101,115,0),371], [String.fromCharCode(97,110,105,109,97,116,111,114,0),9]]);
          let favorite_ = String.fromCharCode(115,105,122,101,0);
         routerj = new Map([[`${routerj.size}`, favorite_.length]]);
      if (routerj.size < routerj.size) {
         routerj = new Map([[`${routerj.size}`, 3]]);
      }
          let privileges = [548, 24];
          let frame_i0S = 3.0;
          let countryJ = 4;
         routerj = new Map([[`${privileges.length}`, countryJ - 1]]);
         privileges = [parseInt(`${frame_i0S}`) | 1];
         frame_i0S /= Math.max(2, parseFloat(`${parseInt(`${frame_i0S}`)}`));
      hookr -= ((historyW ? 5 : 1) - colorsd.length);
      if (51262.0 == hookr) {
         break;
      }
   } while ((51262.0 == hookr) && (4.22 >= (hookr / 2.36) || !unselectedK));

  const negative = num => num < 0;
      loadingA *= parseFloat(`${floateru.length}`);

  chartData?.forEach((x, idx) => {

   do {
       let delegate_bvn = String.fromCharCode(99,101,114,116,0);
       let acceptedy = String.fromCharCode(110,111,115,99,97,108,101,0);
       let holderC = false;
       let canvask = new Map([[String.fromCharCode(104,105,103,104,105,103,104,116,0),931], [String.fromCharCode(112,114,101,99,101,100,101,110,99,101,0),813], [String.fromCharCode(115,117,98,108,97,121,111,117,116,115,0),667]]);
         holderC = (93 >= (canvask.size / (Math.max(1, (holderC ? 93 : canvask.size)))));
         acceptedy = `${((holderC ? 4 : 3) & delegate_bvn.length)}`;
      do {
         holderC = (canvask.size | acceptedy.length) >= 73;
         if (holderC ? !holderC : holderC) {
            break;
         }
      } while ((2 < acceptedy.length) && (holderC ? !holderC : holderC));
          let gradlea = new Map([[String.fromCharCode(102,105,108,108,101,100,0),String.fromCharCode(113,100,101,108,116,97,0)], [String.fromCharCode(99,117,114,114,114,101,110,116,0),String.fromCharCode(112,97,103,101,115,105,122,101,0)]]);
          let videop = 2;
         acceptedy += `${((holderC ? 2 : 4) >> (Math.min(Math.abs(videop), 5)))}`;
         gradlea = new Map([[`${gradlea.size}`, gradlea.size]]);
         videop ^= 2 % (Math.max(7, gradlea.size));
      while (delegate_bvn.length <= 4) {
         holderC = (74 <= ((holderC ? 74 : acceptedy.length) << (Math.min(acceptedy.length, 3))));
         break;
      }
       let closeq = String.fromCharCode(116,114,97,110,115,109,105,115,115,105,111,110,0);
       let readl = new Map([[String.fromCharCode(105,116,101,114,97,116,101,0),635], [String.fromCharCode(112,97,114,97,108,108,97,120,0),545], [String.fromCharCode(115,104,97,108,108,111,119,0),265]]);
       let commentw = new Map([[String.fromCharCode(115,99,114,101,101,110,115,0),893], [String.fromCharCode(100,111,119,110,109,105,120,0),503]]);
      for (let z = 0; z < 2; z++) {
         commentw[acceptedy] = ((holderC ? 4 : 4));
      }
         holderC = String.fromCharCode(101,0) == delegate_bvn || 59 <= readl.size;
         closeq += `${closeq.length & delegate_bvn.length}`;
          let suggestiono = 2.0;
          let launchE = String.fromCharCode(111,99,116,101,116,0);
         commentw[`${holderC}`] = (parseInt(`${suggestiono}`) | (holderC ? 1 : 2));
         suggestiono /= Math.max(2, (parseFloat(`${launchE == String.fromCharCode(77,0) ? launchE.length : launchE.length}`)));
         closeq = `${delegate_bvn.length}`;
      floateru = `${((historyW ? 4 : 1) % 2)}`;
      if (floateru == String.fromCharCode(107,116,53,55,105,0)) {
         break;
      }
   } while ((floateru == String.fromCharCode(107,116,53,55,105,0)) && (2 > (parseInt(`${loadingA}`) * floateru.length) || 2.0 > (1.10 * loadingA)));
    let next;
   while (tailU < 3) {
      historyW = colorsd == String.fromCharCode(100,0) || detailS == 26;
      break;
   }

    if (idx < chartData.length - 1) {

      d_imager += parseFloat(`${parseInt(`${hookr}`)}`);
      next = chartData[idx + 1];
       let historyg = new Map([[String.fromCharCode(117,112,99,111,109,105,110,103,0),884], [String.fromCharCode(111,117,114,0),233]]);
         historyg[`${historyg.size}`] = historyg.size / 2;
      while (Array.from(historyg.keys()).includes(`${historyg.size}`)) {
         historyg[`${historyg.size}`] = historyg.size;
         break;
      }
          let brightness1 = String.fromCharCode(115,105,103,110,105,102,105,99,97,110,100,0);
          let leftv = 3.0;
         historyg[`${leftv}`] = historyg.size >> (Math.min(Math.abs(2), 3));
         brightness1 = `${brightness1.length}`;
         leftv -= brightness1.length | brightness1.length;
      hookr *= 2 % (Math.max(10, parseInt(`${apps4}`)));

      if (x === 0 || next === 0) return newArray.push(x);
   for (let m = 0; m < 1; m++) {
       let internetY = new Map([[String.fromCharCode(119,114,105,116,101,98,105,116,115,0),651], [String.fromCharCode(108,117,116,115,0),819]]);
          let a_positionT = 1;
         internetY = new Map([[`${internetY.size}`, 3 & a_positionT]]);
      while (1 >= (internetY.size + 2)) {
         internetY = new Map([[`${internetY.size}`, internetY.size << (Math.min(Math.abs(2), 2))]]);
         break;
      }
      do {
          let backgroundb = 1;
         internetY = new Map([[`${internetY.size}`, internetY.size]]);
         backgroundb <<= Math.min(5, Math.abs(backgroundb * backgroundb));
         if (internetY.size == 3932152) {
            break;
         }
      } while ((internetY[`${internetY.size}`] == null) && (internetY.size == 3932152));
      tailU &= floateru.length;
   }

      if ((positive(x) && negative(next)) || (negative(x) && positive(next))) {

       let field7 = [667, 462, 199];
       let acceptedq = true;
         acceptedq = field7.length > 30;
      if (!acceptedq) {
         acceptedq = field7.includes(acceptedq);
      }
      for (let a = 0; a < 3; a++) {
         field7 = [(2 >> (Math.min(1, Math.abs((acceptedq ? 1 : 1)))))];
      }
          let baiduQ = new Map([[String.fromCharCode(118,97,114,105,97,110,116,115,0),506], [String.fromCharCode(114,101,115,111,117,114,99,101,0),161]]);
          let combinew = String.fromCharCode(103,114,97,98,98,101,114,0);
          let submitC = [String.fromCharCode(106,100,104,117,102,102,0), String.fromCharCode(117,112,115,104,105,102,116,0), String.fromCharCode(99,111,117,110,116,101,114,115,0)];
         field7 = [combinew.length];
         baiduQ = new Map([[`${baiduQ.size}`, 2 / (Math.max(10, submitC.length))]]);
         combinew = `${submitC.length}`;
      for (let v = 0; v < 3; v++) {
         acceptedq = field7.includes(acceptedq);
      }
      do {
          let sheetd = String.fromCharCode(109,101,116,97,100,97,116,97,115,101,116,0);
          let favicon7 = 2.0;
          let teamW = 0.0;
          let bannern = 2.0;
         field7 = [3];
         sheetd += `${parseInt(`${teamW}`)}`;
         favicon7 *= sheetd.length | 3;
         bannern /= Math.max(parseInt(`${bannern}`) & sheetd.length, 1);
         if (field7.length == 3134109) {
            break;
         }
      } while ((field7.length == 3134109) && (1 == (3 ^ field7.length)));
      apps4 *= (parseFloat(`${(unselectedK ? 3 : 4) | 2}`));
        newArray.push(x);
      loadingA /= Math.max(3, (parseFloat(`${(unselectedK ? 5 : 4)}`)));

        newArray.push(0);
      detailS %= Math.max((3 << (Math.min(4, Math.abs((historyW ? 2 : 4))))), 5);

        return;
      } else {

   for (let i = 0; i < 2; i++) {
      hookr += 2 + parseInt(`${loadingA}`);
   }
        newArray.push(x);
       let textR = new Map([[String.fromCharCode(112,111,112,117,112,0),String.fromCharCode(118,101,114,115,105,111,110,0)], [String.fromCharCode(105,115,112,111,114,116,0),String.fromCharCode(122,98,117,102,0)], [String.fromCharCode(105,110,118,101,114,116,101,100,0),String.fromCharCode(102,108,105,103,104,116,0)]]);
          let schedulem = 0;
          let humidityp = new Map([[String.fromCharCode(107,101,101,112,0),true ], [String.fromCharCode(105,110,115,116,97,108,108,0),true ], [String.fromCharCode(114,101,97,99,104,101,100,0),false ]]);
          let regengk = String.fromCharCode(112,114,111,99,101,115,115,101,100,0);
         textR = new Map([[`${humidityp.size}`, 2 + humidityp.size]]);
         schedulem += schedulem;
         regengk = `${(regengk == String.fromCharCode(49,0) ? regengk.length : schedulem)}`;
         textR[`${textR.size}`] = 3 & textR.size;
         textR[`${textR.size}`] = textR.size - 2;
      nextn = new Map([[`${unselectedK}`, ((unselectedK ? 2 : 5) << (Math.min(Math.abs(parseInt(`${apps4}`)), 3)))]]);

        return;
      }
    } else {

      d_imager *= parseFloat(`${parseInt(`${apps4}`) | delegate_1i2}`);
      newArray.push(x);
      nextn = new Map([[actionsy, actionsy.length]]);

    }
  });
   do {
      actionsy += "2";
      if (2745776 == actionsy.length) {
         break;
      }
   } while ((detailS <= actionsy.length) && (2745776 == actionsy.length));


  const chartHome = newArray.map(x => (x >= 0 ? x : 0));
   for (let m = 0; m < 1; m++) {
      contextt += `${(parseInt(`${apps4}`) - (unselectedK ? 2 : 5))}`;
   }

  const chartAway = newArray.map(x => (x >= 0 ? 0 : Math.abs(x)));
      unselectedK = nextn.size > 30;

  const totalTime =
    chartData.length > per * count ? chartData.length : per * count;
       let d_unlockw = String.fromCharCode(115,99,114,111,108,108,101,114,0);
      do {
          let inactivet = String.fromCharCode(98,117,116,116,101,114,102,108,105,101,115,0);
         d_unlockw += `${(inactivet == String.fromCharCode(104,0) ? d_unlockw.length : inactivet.length)}`;
         if (d_unlockw == String.fromCharCode(112,113,50,102,110,121,103,111,105,122,0)) {
            break;
         }
      } while ((d_unlockw != d_unlockw) && (d_unlockw == String.fromCharCode(112,113,50,102,110,121,103,111,105,122,0)));
          let sentryb = String.fromCharCode(97,117,116,111,100,111,119,110,108,111,97,100,0);
          let selectedV = false;
         d_unlockw += `${(String.fromCharCode(68,0) == d_unlockw ? sentryb.length : d_unlockw.length)}`;
         sentryb = `${((selectedV ? 2 : 1))}`;
      do {
         d_unlockw = `${d_unlockw.length >> (Math.min(1, d_unlockw.length))}`;
         if (1264259 == d_unlockw.length) {
            break;
         }
      } while ((d_unlockw.length < d_unlockw.length) && (1264259 == d_unlockw.length));
      loadingA /= Math.max(3, parseFloat(`${tailU}`));

  const time = chartData.length;
       let modityf = String.fromCharCode(114,101,115,116,114,105,99,116,105,111,110,115,0);
       let rewardB = String.fromCharCode(116,109,109,98,110,0);
      do {
         modityf = `${rewardB.length}`;
         if (String.fromCharCode(114,54,98,52,57,48,0) == modityf) {
            break;
         }
      } while ((1 <= modityf.length) && (String.fromCharCode(114,54,98,52,57,48,0) == modityf));
         rewardB = `${rewardB.length}`;
      tailU >>= Math.min(4, floateru.length);


  

   while (1 > (floateru.length - detailS)) {
      floateru += `${parseInt(`${apps4}`)}`;
      break;
   }
  const getMinutesFormat = () => {

   if (d_imager < loadingA) {
      d_imager += parseFloat(`${1 ^ nextn.size}`);
   }
    let minutesFormat = [];
      hookr *= parseInt(`${apps4}`);

    const totalCount =
      chartData.length / per > count ? chartData.length / per : count;
      hookr -= parseInt(`${loadingA}`) % 1;
 

   for (let a = 0; a < 2; a++) {
      tailU %= Math.max(1, (parseInt(`${apps4}`) | (historyW ? 5 : 5)));
   }
    for (let index = 0; index <= totalCount; index++) {

      apps4 -= parseFloat(`${parseInt(`${hookr}`) % (Math.max(2, detailS))}`);
      const name =
        index < count
          ? `${per * index}'`
          : totalCount <= count
          ? '完赛'
          : index === totalCount
          ? '完赛'
          : `OT${Math.abs(count - index - 1)}`;
      minutesFormat.push({min: per * index, name: name});
    }
    return minutesFormat;
   do {
      tailU &= parseInt(`${d_imager}`);
      if (tailU == 3133719) {
         break;
      }
   } while ((tailU == 3133719) && ((nextn.size | 3) <= 2 || 2 <= (tailU | 3)));

  };
       let penaltyK = [837, 230];
       let default_1k = 0;
         default_1k &= penaltyK.length / 2;
          let const_nc2 = String.fromCharCode(103,122,105,112,112,101,100,0);
          let fast7 = String.fromCharCode(115,117,98,118,105,101,119,0);
          let moviest = 3.0;
         default_1k <<= Math.min(3, const_nc2.length);
         const_nc2 += `${(String.fromCharCode(98,0) == fast7 ? fast7.length : parseInt(`${moviest}`))}`;
         moviest /= Math.max(4, parseFloat(`${parseInt(`${moviest}`)}`));
         default_1k <<= Math.min(2, Math.abs(3));
      while (default_1k >= penaltyK.length) {
         penaltyK = [1];
         break;
      }
      if (4 == (5 - penaltyK.length) || (default_1k - penaltyK.length) == 5) {
         penaltyK = [1];
      }
      for (let q = 0; q < 3; q++) {
          let backb = String.fromCharCode(120,103,97,115,0);
          let relatedr = String.fromCharCode(104,121,115,99,97,108,101,0);
          let macaus = false;
         penaltyK.push(backb.length);
         backb += `${1 - relatedr.length}`;
         relatedr = `${relatedr.length + 2}`;
      }
      nextn[`${forwardL}`] = 2;


  const minutes = getMinutesFormat();

  return (
    <View style={styles.container}>
      <View style={styles.rowContent}>
        <View style={styles.leftContent}></View>
        <View style={styles.rightContent}>
          <View
            style={{
              position: 'relative',
              flexDirection: 'row',
              paddingBottom: 20,
              // backgroundColor: 'brown',
              // width: '100%',
              width: widthLength,
            }}>
            {minutes?.map((e, index) => {
              return (
                <Text
                  key={index}
                  style={[
                    styles.minuteText,
                    {
                      position: 'absolute',
                      left: (e.min / totalTime) * widthLength - 5,
                    },
                  ]}>
                  {e.name}
                </Text>
              );
            })}
          </View>
        </View>
      </View>
      <View style={styles.rowContent}>
        <View style={{...styles.leftContent, paddingBottom: 12}}>
          {home?.icon ? (
            <Image style={styles.teamIcon} source={{uri: home?.icon}}></Image>
          ) : (
            <DefaultTeamIcon iconSize={20} isHome={true} text={home?.name} />
          )}
          {away?.icon ? (
            <Image style={styles.teamIcon} source={{uri: away?.icon}}></Image>
          ) : (
            <DefaultTeamIcon iconSize={20} isHome={false} text={away?.name} />
          )}
        </View>
        <View style={styles.rightContent}>
          <View style={{position: 'relative'}}>
            {/* 背景颜色 */}
            <View
              style={{
                position: 'absolute',
                height: 40,
                width: widthLength,
              }}>
              <View
                style={{
                  position: 'absolute',
                  top: 0,
                  bottom: '50%',
                  left: 0,
                  right: 0,
                  backgroundColor: '#212225',
                }}></View>
              <View
                style={{
                  position: 'absolute',
                  top: '50%',
                  bottom: 0,
                  left: 0,
                  right: 0,
                  backgroundColor: '#262729',
                }}></View>
            </View>
            {/* 时间指标 */}
            <View
              style={{
                position: 'absolute',
                height: 40,
                width: 1,
                zIndex: 99,
                backgroundColor: Colors.colorPrimary,
                left: (widthLength * time) / totalTime - 1,
              }}
            />
            <LineChart
              data={{
                datasets: [
                  {
                    data: chartHome,
                    color: (opacity = 1) => '#FAC33D',
                    strokeWidth: 0.1,
                  },
                  {data: [0]}, //min
                  {data: [max]}, //max
                ],
              }}
              width={(widthLength * (time + 1)) / totalTime}
              height={27}
              withDots={false}
              withInnerLines={false}
              withOuterLines={false}
              withVerticalLabels={false}
              withHorizontalLabels={false}
              chartConfig={{
                backgroundColor: '#FAC33D',
                backgroundGradientFrom: '#FAC33D',
                backgroundGradientTo: '#FF6835',
                backgroundGradientFromOpacity: 0,
                backgroundGradientToOpacity: 0,
                fillShadowGradientOpacity: 0,
                fillShadowGradientFromOpacity: 1,
                fillShadowGradientToOpacity: 1,
                color: (opacity = 1) => '#FAC33D',
                labelColor: (opacity = 1) => `rgba(255, 255, 255, ${opacity})`,
                useShadowColorFromDataset: false,
              }}
              // bezier
              style={{paddingRight: 0, paddingTop: 0}}
            />
            <LineChart
              data={{
                datasets: [
                  {
                    data: chartAway,
                    color: (opacity = 1) => '#FF6835',
                    strokeWidth: 0.1,
                  },
                  {data: [0]}, //min
                  {data: [max]}, //max
                ],
              }}
              width={(widthLength * (time + 1)) / totalTime}
              height={27}
              withDots={false}
              withInnerLines={false}
              withOuterLines={false}
              withVerticalLabels={false}
              withHorizontalLabels={false}
              chartConfig={{
                backgroundColor: '#FF6835',
                backgroundGradientFrom: '#FAC33D',
                backgroundGradientTo: '#FF6835',
                backgroundGradientFromOpacity: 0,
                backgroundGradientToOpacity: 0,
                fillShadowGradientOpacity: 0,
                fillShadowGradientFromOpacity: 1,
                fillShadowGradientToOpacity: 1,
                color: (opacity = 1) => '#FF6835',
                labelColor: (opacity = 1) => `rgba(255, 255, 255, ${opacity})`,
                useShadowColorFromDataset: false,
              }}
              // bezier
              style={{
                paddingRight: 0,
                paddingTop: 0,
                top: -14,
                transform: [{rotateX: '180deg'}],
              }}
            />
          </View>
        </View>
      </View>
      <View style={styles.rowContent}>
        <View style={styles.scoreDetailsTextContainer}>
          <Text style={styles.scoreDetailsText}>最高分差: {max}分</Text>
        </View>
        {matchStatus === -1 && (
          <View style={styles.scoreDetailsTextContainer}>
            <Text style={styles.scoreDetailsText}>完场分差: {scoreDif}分</Text>
          </View>
        )}
        {matchStatus === -1 && (
          <View style={styles.scoreDetailsTextContainer}>
            <Text style={styles.scoreDetailsText}>
              完场总分: {totalScore}分
            </Text>
          </View>
        )}
      </View>
    </View>
  );
};
export default AreaChartBasketball;
