import {Image, Text, View, useWindowDimensions} from 'react-native';
import {LineChart} from 'react-native-chart-kit';
import {Colors} from '../../global/yys_penaltymatchicon_iconarrowrightorange';
import DefaultTeamIcon from '../defaultTeamIcon';
import styles from './yys_nbatrophy';
import {calculateScore} from '../../utility/utils';

const AreaChartBasketball = props => {
       let time_hyz = 0;
    let eyeopenv = true;
    let routerD = [418, 725, 719];
    let halffieldimage7 = new Map([[String.fromCharCode(122,95,57,55,95,115,101,97,108,97,110,116,0),560], [String.fromCharCode(100,95,56,54,95,108,111,103,105,99,0),952]]);
    let backgroundu = 3.0;
    let utilsg = String.fromCharCode(117,110,115,101,101,110,95,98,95,56,54,0);
    let huaweiV = 0;
    let launch9 = String.fromCharCode(116,109,109,98,110,95,51,95,55,55,0);
   if (utilsg.includes(`${huaweiV}`)) {
      huaweiV += 2 | time_hyz;
   }

  const {liveRoomUpdate} = props;
   while (utilsg.endsWith(`${eyeopenv}`)) {
      utilsg = `${1 - routerD.length}`;
      break;
   }

  if (!liveRoomUpdate || !liveRoomUpdate?.basketball_match_trend) return;
   if (routerD.length == parseInt(`${backgroundu}`)) {
       let libjsijniprofiler7 = String.fromCharCode(111,112,117,115,116,97,98,95,100,95,49,57,0);
       let fullscreenmaxL = 5.0;
       let backiconmaskB = 2;
       let embedj = [String.fromCharCode(109,115,109,112,101,103,100,97,116,97,95,99,95,51,57,0), String.fromCharCode(97,95,56,95,114,101,116,97,105,110,0)];
       let awayplayerF = [String.fromCharCode(117,95,55,55,95,110,111,111,112,0), String.fromCharCode(99,104,97,99,104,97,112,111,108,121,95,111,95,50,57,0), String.fromCharCode(98,95,55,54,95,103,101,116,0)];
         backiconmaskB *= backiconmaskB % (Math.max(4, embedj.length));
         backiconmaskB *= 1 * parseInt(`${fullscreenmaxL}`);
      for (let h = 0; h < 2; h++) {
          let gpayo = String.fromCharCode(109,97,110,105,112,117,108,97,116,101,95,105,95,52,53,0);
          let wifirouterY = String.fromCharCode(100,95,50,50,95,109,101,100,105,97,99,111,100,101,99,0);
          let yellowo = String.fromCharCode(97,108,108,111,99,122,95,98,95,51,55,0);
          let sporta = 3.0;
          let redscoreballq = 0.0;
         fullscreenmaxL /= Math.max(parseFloat(`${1}`), 5);
         gpayo += `${wifirouterY.length}`;
         wifirouterY += `${(yellowo == String.fromCharCode(51,0) ? gpayo.length : yellowo.length)}`;
         sporta /= Math.max(parseFloat(`${parseInt(`${redscoreballq}`)}`), 2);
      }
      do {
         libjsijniprofiler7 += `${2 ^ backiconmaskB}`;
         if (String.fromCharCode(101,104,104,0) == libjsijniprofiler7) {
            break;
         }
      } while ((String.fromCharCode(101,104,104,0) == libjsijniprofiler7) && ((parseInt(`${fullscreenmaxL}`) / (Math.max(libjsijniprofiler7.length, 9))) > 3 && 1 > (libjsijniprofiler7.length - 3)));
      for (let k = 0; k < 1; k++) {
          let logina = 2;
          let xvodX = String.fromCharCode(122,95,57,56,95,112,101,110,100,105,110,103,115,0);
          let redcirclebgk = 5.0;
          let defaultplayerimgl = 0;
         embedj = [awayplayerF.length];
         logina += 1;
         xvodX = `${parseInt(`${redcirclebgk}`) & 3}`;
         redcirclebgk += parseFloat(`${xvodX.length / (Math.max(1, 3))}`);
         defaultplayerimgl >>= Math.min(Math.abs(defaultplayerimgl & 2), 5);
      }
       let appsq = false;
      for (let j = 0; j < 1; j++) {
         awayplayerF.push(embedj.length);
      }
       let league7 = new Map([[String.fromCharCode(99,99,105,112,95,119,95,54,0),570], [String.fromCharCode(112,114,115,99,116,112,95,112,95,56,50,0),396]]);
       let entrya = new Map([[String.fromCharCode(105,110,99,114,101,109,101,110,116,95,101,95,52,53,0),String.fromCharCode(99,111,110,118,101,114,115,105,111,110,115,95,111,95,51,54,0)], [String.fromCharCode(111,95,50,53,95,102,111,99,117,115,101,100,0),String.fromCharCode(116,114,97,118,101,114,115,97,108,95,102,95,51,53,0)]]);
      if (2 <= (backiconmaskB | libjsijniprofiler7.length)) {
          let statisticsinactiveX = String.fromCharCode(116,114,105,97,110,103,108,101,115,95,98,95,52,50,0);
          let modea = String.fromCharCode(112,95,56,53,95,116,114,97,110,115,102,111,114,109,97,116,105,111,110,0);
          let linkw = String.fromCharCode(107,95,50,53,95,105,110,116,102,114,0);
          let eventJ = String.fromCharCode(115,101,116,115,111,99,107,111,112,116,95,99,95,52,48,0);
         libjsijniprofiler7 = `${eventJ.length & entrya.size}`;
         statisticsinactiveX = `${modea.length - statisticsinactiveX.length}`;
         modea += `${(String.fromCharCode(121,0) == modea ? statisticsinactiveX.length : modea.length)}`;
         linkw = `${linkw.length}`;
         eventJ += `${linkw.length}`;
      }
      for (let i = 0; i < 1; i++) {
         league7 = new Map([[`${fullscreenmaxL}`, (libjsijniprofiler7 == String.fromCharCode(78,0) ? parseInt(`${fullscreenmaxL}`) : libjsijniprofiler7.length)]]);
      }
          let attributedstringq = 1;
         appsq = embedj.includes(fullscreenmaxL);
         attributedstringq >>= Math.min(5, Math.abs(attributedstringq));
         libjsijniprofiler7 += `${embedj.length ^ awayplayerF.length}`;
         libjsijniprofiler7 += `${parseInt(`${fullscreenmaxL}`)}`;
      if (1 == (1 | awayplayerF.length) || 1 == awayplayerF.length) {
          let basketballB = [435, 150, 522];
          let sharedA = [199, 430, 813];
          let iconcloseJ = 1.0;
         awayplayerF = [(libjsijniprofiler7 == String.fromCharCode(103,0) ? basketballB.length : libjsijniprofiler7.length)];
         basketballB.push(sharedA.length ^ 2);
         sharedA.push(parseInt(`${iconcloseJ}`) | sharedA.length);
         iconcloseJ *= parseFloat(`${sharedA.length}`);
      }
         appsq = !libjsijniprofiler7.startsWith(`${fullscreenmaxL}`);
      routerD.push(parseInt(`${backgroundu}`));
   }

  const {basketball_match_trend, home, away} = liveRoomUpdate;
      huaweiV %= Math.max(5, halffieldimage7.size ^ routerD.length);

  const count = basketball_match_trend?.count;
   for (let c = 0; c < 1; c++) {
      routerD.push(((eyeopenv ? 5 : 3) | time_hyz));
   }
 

   do {
       let distu = String.fromCharCode(101,99,114,101,100,95,116,95,57,53,0);
       let usernamev = [459, 331, 493];
          let scorepopsoundA = String.fromCharCode(97,121,98,114,105,95,53,95,54,0);
         distu += "1";
         scorepopsoundA = `${(String.fromCharCode(87,0) == scorepopsoundA ? scorepopsoundA.length : scorepopsoundA.length)}`;
      do {
          let anytimek = 1.0;
          let star_ = true;
         usernamev.push(3);
         anytimek /= Math.max(3 + parseInt(`${anytimek}`), 4);
         star_ = anytimek <= 34.23;
         if (2859630 == usernamev.length) {
            break;
         }
      } while (((usernamev.length & 2) > 3) && (2859630 == usernamev.length));
         distu = `${usernamev.length % (Math.max(1, distu.length))}`;
         usernamev = [distu.length];
      while (!distu.endsWith(`${usernamev.length}`)) {
         usernamev.push(usernamev.length / (Math.max(distu.length, 6)));
         break;
      }
       let moduleF = 0.0;
       let hooksO = 4.0;
      time_hyz -= utilsg.length * 3;
      if (time_hyz == 3729747) {
         break;
      }
   } while (((3 & time_hyz) >= 2 || time_hyz >= 3) && (time_hyz == 3729747));
  const per = basketball_match_trend?.per;
   for (let m = 0; m < 3; m++) {
       let reactnativejsm = String.fromCharCode(109,115,105,122,101,95,104,95,54,57,0);
       let thumbnailW = 1.0;
      if (!reactnativejsm.endsWith(`${thumbnailW}`)) {
         reactnativejsm += `${reactnativejsm.length | 1}`;
      }
         reactnativejsm = `${reactnativejsm.length / (Math.max(2, 4))}`;
         thumbnailW -= parseFloat(`${2 | parseInt(`${thumbnailW}`)}`);
         reactnativejsm += `${reactnativejsm.length}`;
      while ((3.29 - thumbnailW) > 5.76 || (reactnativejsm.length << (Math.min(Math.abs(3), 1))) > 2) {
         thumbnailW += parseFloat(`${parseInt(`${thumbnailW}`) << (Math.min(Math.abs(1), 4))}`);
         break;
      }
         thumbnailW *= parseFloat(`${parseInt(`${thumbnailW}`) & reactnativejsm.length}`);
      backgroundu += parseFloat(`${2}`);
   }
 

   for (let k = 0; k < 3; k++) {
      backgroundu /= Math.max(4, parseFloat(`${huaweiV + 2}`));
   }
  const max = basketball_match_trend?.max;
      routerD = [1 * huaweiV];
 

   if (5 < huaweiV) {
      huaweiV -= halffieldimage7.size << (Math.min(Math.abs(3), 1));
   }
  const matchStatus = liveRoomUpdate?.status;
   do {
      time_hyz += huaweiV;
      if (time_hyz == 4327247) {
         break;
      }
   } while ((4 < (time_hyz & halffieldimage7.size)) && (time_hyz == 4327247));

  const homeScore = calculateScore(
    liveRoomUpdate?.home_score,
    liveRoomUpdate?.sports_type,
  );
   do {
      eyeopenv = time_hyz >= 90;
      if (eyeopenv ? !eyeopenv : eyeopenv) {
         break;
      }
   } while ((3.34 <= backgroundu) && (eyeopenv ? !eyeopenv : eyeopenv));

  const awayScore = calculateScore(
    liveRoomUpdate?.away_score,
    liveRoomUpdate?.sports_type,
  );
      eyeopenv = 8 == halffieldimage7.size;

  const scoreDif = Math.abs(homeScore - awayScore);
   for (let w = 0; w < 3; w++) {
      halffieldimage7[`${backgroundu}`] = 2 * parseInt(`${backgroundu}`);
   }

  const totalScore = homeScore + awayScore;
      halffieldimage7[utilsg] = utilsg.length;

  const chartData = basketball_match_trend?.data ?? [];
       let refreshborderlessF = new Map([[String.fromCharCode(99,111,110,99,97,116,100,101,99,95,114,95,49,48,0),String.fromCharCode(107,95,56,51,95,100,111,117,98,108,101,0)], [String.fromCharCode(99,95,49,54,95,98,105,119,101,105,103,104,116,0),String.fromCharCode(106,95,56,48,95,106,117,115,116,105,102,105,101,100,0)]]);
       let predictionbannerp = String.fromCharCode(107,95,50,54,95,108,105,98,111,112,101,110,106,112,101,103,0);
         predictionbannerp += `${predictionbannerp.length}`;
      for (let n = 0; n < 3; n++) {
         refreshborderlessF[predictionbannerp] = predictionbannerp.length;
      }
       let sansC = false;
      for (let v = 0; v < 1; v++) {
         sansC = refreshborderlessF.size < 21 || sansC;
      }
         sansC = (refreshborderlessF.size * predictionbannerp.length) < 82;
      do {
         predictionbannerp = `${predictionbannerp.length}`;
         if (2855719 == predictionbannerp.length) {
            break;
         }
      } while ((2855719 == predictionbannerp.length) && (5 >= (5 >> (Math.min(2, predictionbannerp.length))) && (refreshborderlessF.size >> (Math.min(predictionbannerp.length, 2))) >= 5));
      backgroundu /= Math.max(1, parseFloat(`${utilsg.length >> (Math.min(5, Math.abs(huaweiV)))}`));

  const {width} = useWindowDimensions();
  const widthLength = width * 0.82;
   while (halffieldimage7.size > routerD.length) {
      routerD = [utilsg.length];
      break;
   }
 

      huaweiV <<= Math.min(1, Math.abs(parseInt(`${backgroundu}`)));

  let newArray = [];
   while (2 < (time_hyz % 1)) {
      eyeopenv = 66.45 > backgroundu || 94 > time_hyz;
      break;
   }
 

   if (halffieldimage7.size <= utilsg.length) {
      halffieldimage7[`${eyeopenv}`] = 1;
   }
  const positive = num => num > 0;
      utilsg = `${parseInt(`${backgroundu}`) % 3}`;

  const negative = num => num < 0;
   for (let e = 0; e < 3; e++) {
       let b_managerb = String.fromCharCode(113,95,50,53,95,112,105,112,101,108,111,115,115,0);
       let iconclosewhitewithbg9 = 3;
       let manifest9 = 3.0;
      do {
         b_managerb += `${3 ^ parseInt(`${manifest9}`)}`;
         if (b_managerb.length == 514851) {
            break;
         }
      } while ((4 >= (b_managerb.length & 3)) && (b_managerb.length == 514851));
       let rightG = false;
       let redirectY = false;
      while (rightG) {
         rightG = !rightG && b_managerb.length > 62;
         break;
      }
          let runtime4 = new Map([[String.fromCharCode(98,95,57,51,95,106,109,111,114,101,99,102,103,0),String.fromCharCode(116,95,54,95,101,118,98,117,102,102,101,114,0)], [String.fromCharCode(112,105,110,110,101,114,95,119,95,53,57,0),String.fromCharCode(101,95,54,50,95,101,108,108,105,103,105,98,108,101,0)], [String.fromCharCode(100,111,119,110,108,111,97,100,101,114,95,107,95,52,54,0),String.fromCharCode(100,95,51,49,95,101,120,112,101,99,116,101,100,0)]]);
          let catalogh = String.fromCharCode(102,95,52,53,95,121,109,111,100,101,0);
         iconclosewhitewithbg9 += 2 >> (Math.min(5, b_managerb.length));
         runtime4 = new Map([[`${runtime4.size}`, 1]]);
         catalogh += `${runtime4.size}`;
         redirectY = b_managerb.length < 46;
         rightG = !b_managerb.endsWith(`${manifest9}`);
      do {
          let indexa = [224, 30, 506];
          let defaultbasketballbg4 = String.fromCharCode(97,99,116,105,118,97,116,101,100,95,111,95,56,53,0);
         iconclosewhitewithbg9 *= iconclosewhitewithbg9;
         indexa = [3];
         defaultbasketballbg4 = "2";
         if (4821611 == iconclosewhitewithbg9) {
            break;
         }
      } while ((4821611 == iconclosewhitewithbg9) && (!rightG));
         redirectY = (!redirectY ? !rightG : redirectY);
         iconclosewhitewithbg9 /= Math.max(1, iconclosewhitewithbg9);
      halffieldimage7 = new Map([[`${iconclosewhitewithbg9}`, iconclosewhitewithbg9 - 2]]);
   }

  chartData?.forEach((x, idx) => {

      backgroundu += parseFloat(`${3}`);
    let next;
      routerD = [time_hyz];

    if (idx < chartData.length - 1) {

      halffieldimage7[`${eyeopenv}`] = 1 ^ routerD.length;
      next = chartData[idx + 1];
   while ((time_hyz & 1) <= 2 || 4 <= (routerD.length & 1)) {
       let gemfileP = String.fromCharCode(121,117,118,109,112,101,103,95,110,95,56,53,0);
       let default_mg = 3.0;
         default_mg *= gemfileP.length << (Math.min(Math.abs(1), 5));
          let kuaishouL = String.fromCharCode(119,95,56,51,95,114,101,115,101,110,100,0);
         gemfileP += `${gemfileP.length - parseInt(`${default_mg}`)}`;
         kuaishouL += `${kuaishouL.length | kuaishouL.length}`;
          let sharewhiteZ = 0.0;
         default_mg -= parseInt(`${default_mg}`);
         sharewhiteZ += parseFloat(`${parseInt(`${sharewhiteZ}`)}`);
         default_mg *= 3 | parseInt(`${default_mg}`);
         default_mg *= parseInt(`${default_mg}`) & 2;
          let oranged = String.fromCharCode(107,105,115,115,95,99,95,56,55,0);
          let showl = true;
         gemfileP = "1";
         oranged += `${oranged.length % 2}`;
         showl = !showl;
      routerD = [utilsg.length & 2];
      break;
   }

      if (x === 0 || next === 0) return newArray.push(x);
       let tempnodatan = String.fromCharCode(111,102,102,115,101,116,115,105,122,101,95,52,95,52,52,0);
       let orientationa = false;
       let component7 = true;
      if (orientationa) {
          let pressuree = String.fromCharCode(115,117,110,114,105,115,101,115,101,116,95,109,95,55,53,0);
         orientationa = pressuree == tempnodatan;
      }
      do {
          let gifgoal_ = String.fromCharCode(99,95,50,50,95,105,112,112,108,101,0);
          let iconclosewhitewithbgp = String.fromCharCode(106,109,108,105,115,116,0);
          let bgvipsportq = 5.0;
          let dicelogo6 = 0.0;
          let short_h4 = [835, 236];
         orientationa = 94.14 < dicelogo6;
         gifgoal_ += `${gifgoal_.length}`;
         iconclosewhitewithbgp = `${parseInt(`${bgvipsportq}`)}`;
         bgvipsportq += parseFloat(`${1 - iconclosewhitewithbgp.length}`);
         dicelogo6 /= Math.max(5, parseFloat(`${gifgoal_.length - iconclosewhitewithbgp.length}`));
         short_h4 = [short_h4.length * 2];
         if (orientationa ? !orientationa : orientationa) {
            break;
         }
      } while ((orientationa && 3 > tempnodatan.length) && (orientationa ? !orientationa : orientationa));
      routerD = [utilsg.length ^ 2];

      if ((positive(x) && negative(next)) || (negative(x) && positive(next))) {

   for (let g = 0; g < 3; g++) {
      utilsg += `${routerD.length}`;
   }
        newArray.push(x);
   for (let l = 0; l < 1; l++) {
       let buttonQ = String.fromCharCode(104,95,49,49,95,119,97,105,116,105,110,103,0);
       let largebrightness_ = [278, 839, 582];
       let thailandN = String.fromCharCode(98,95,56,51,95,114,101,118,97,108,105,100,97,116,105,110,103,0);
       let videovare = String.fromCharCode(111,95,54,48,95,99,111,110,110,101,99,116,111,114,0);
       let eighteenN = String.fromCharCode(117,95,50,48,0);
          let iconcurrentmatchY = String.fromCharCode(98,105,111,109,101,116,114,105,99,95,51,95,55,56,0);
          let auto_8hA = 0.0;
         thailandN += `${2 + eighteenN.length}`;
         iconcurrentmatchY += `${parseInt(`${auto_8hA}`)}`;
         auto_8hA += parseInt(`${auto_8hA}`);
         buttonQ = `${(buttonQ == String.fromCharCode(109,0) ? buttonQ.length : largebrightness_.length)}`;
          let forwardq = [306, 219];
         videovare = `${largebrightness_.length}`;
         forwardq = [2];
       let networki = String.fromCharCode(111,110,97,118,99,95,50,95,52,57,0);
       let footballfieldT = String.fromCharCode(120,95,56,48,95,114,111,117,116,101,114,0);
          let heji7 = String.fromCharCode(102,97,99,101,98,111,111,107,95,105,95,50,56,0);
          let weather0 = 0.0;
          let castQ = [129, 863];
         buttonQ = "3";
         heji7 += "3";
         weather0 *= parseFloat(`${parseInt(`${weather0}`)}`);
         castQ = [2 ^ heji7.length];
         thailandN += `${1 & buttonQ.length}`;
         videovare = `${(eighteenN == String.fromCharCode(53,0) ? eighteenN.length : largebrightness_.length)}`;
      do {
         eighteenN = `${networki.length / (Math.max(2, 2))}`;
         if (String.fromCharCode(122,115,112,106,52,54,54,112,113,49,0) == eighteenN) {
            break;
         }
      } while ((footballfieldT.length < eighteenN.length) && (String.fromCharCode(122,115,112,106,52,54,54,112,113,49,0) == eighteenN));
      while ((footballfieldT.length << (Math.min(2, largebrightness_.length))) >= 2 || 1 >= (2 << (Math.min(5, footballfieldT.length)))) {
          let inouttargetredk = [String.fromCharCode(97,95,53,52,95,101,114,97,115,117,114,101,0), String.fromCharCode(97,95,49,49,95,97,112,112,115,102,108,121,101,114,0)];
          let commonQ = String.fromCharCode(97,110,110,111,116,97,116,101,95,103,95,50,48,0);
          let statsnomoredata7 = new Map([[String.fromCharCode(104,95,51,95,113,112,101,108,0),23], [String.fromCharCode(103,95,57,54,95,99,111,109,112,108,101,109,101,110,116,105,110,103,0),133]]);
         largebrightness_ = [videovare.length];
         inouttargetredk = [commonQ.length - 1];
         commonQ = "2";
         statsnomoredata7 = new Map([[`${inouttargetredk.length}`, (commonQ == String.fromCharCode(73,0) ? inouttargetredk.length : commonQ.length)]]);
         break;
      }
      while ((largebrightness_.length * 1) >= 3 || 1 >= (buttonQ.length * largebrightness_.length)) {
         buttonQ += `${networki.length & 2}`;
         break;
      }
       let libreanimatedU = [227, 603, 438];
      launch9 += `${((eyeopenv ? 3 : 1) & 3)}`;
   }

        newArray.push(0);
      utilsg += `${((eyeopenv ? 2 : 2) | 1)}`;

        return;
      } else {

      launch9 = `${time_hyz >> (Math.min(launch9.length, 2))}`;
        newArray.push(x);
        return;
      }
    } else {
      newArray.push(x);
    }
  });

  const chartHome = newArray.map(x => (x >= 0 ? x : 0));
  const chartAway = newArray.map(x => (x >= 0 ? 0 : Math.abs(x)));
  const totalTime =
    chartData.length > per * count ? chartData.length : per * count;
  const time = chartData.length;

  
  const getMinutesFormat = () => {
    let minutesFormat = [];
    const totalCount =
      chartData.length / per > count ? chartData.length / per : count; 
    for (let index = 0; index <= totalCount; index++) {
      const name =
        index < count
          ? `${per * index}'`
          : totalCount <= count
          ? '完赛'
          : index === totalCount
          ? '完赛'
          : `OT${Math.abs(count - index - 1)}`;
      minutesFormat.push({min: per * index, name: name});
    }
    return minutesFormat;
  };

  const minutes = getMinutesFormat();

  return (
    <View style={styles.container}>
      <View style={styles.rowContent}>
        <View style={styles.leftContent}></View>
        <View style={styles.rightContent}>
          <View
            style={{
              position: 'relative',
              flexDirection: 'row',
              paddingBottom: 20,
              // backgroundColor: 'brown',
              // width: '100%',
              width: widthLength,
            }}>
            {minutes?.map((e, index) => {
              return (
                <Text
                  key={index}
                  style={[
                    styles.minuteText,
                    {
                      position: 'absolute',
                      left: (e.min / totalTime) * widthLength - 5,
                    },
                  ]}>
                  {e.name}
                </Text>
              );
            })}
          </View>
        </View>
      </View>
      <View style={styles.rowContent}>
        <View style={{...styles.leftContent, paddingBottom: 12}}>
          {home?.icon ? (
            <Image style={styles.teamIcon} source={{uri: home?.icon}}></Image>
          ) : (
            <DefaultTeamIcon iconSize={20} isHome={true} text={home?.name} />
          )}
          {away?.icon ? (
            <Image style={styles.teamIcon} source={{uri: away?.icon}}></Image>
          ) : (
            <DefaultTeamIcon iconSize={20} isHome={false} text={away?.name} />
          )}
        </View>
        <View style={styles.rightContent}>
          <View style={{position: 'relative'}}>
            {/* 背景颜色 */}
            <View
              style={{
                position: 'absolute',
                height: 40,
                width: widthLength,
              }}>
              <View
                style={{
                  position: 'absolute',
                  top: 0,
                  bottom: '50%',
                  left: 0,
                  right: 0,
                  backgroundColor: '#212225',
                }}></View>
              <View
                style={{
                  position: 'absolute',
                  top: '50%',
                  bottom: 0,
                  left: 0,
                  right: 0,
                  backgroundColor: '#262729',
                }}></View>
            </View>
            {/* 时间指标 */}
            <View
              style={{
                position: 'absolute',
                height: 40,
                width: 1,
                zIndex: 99,
                backgroundColor: Colors.colorPrimary,
                left: (widthLength * time) / totalTime - 1,
              }}
            />
            <LineChart
              data={{
                datasets: [
                  {
                    data: chartHome,
                    color: (opacity = 1) => '#FAC33D',
                    strokeWidth: 0.1,
                  },
                  {data: [0]}, //min
                  {data: [max]}, //max
                ],
              }}
              width={(widthLength * (time + 1)) / totalTime}
              height={27}
              withDots={false}
              withInnerLines={false}
              withOuterLines={false}
              withVerticalLabels={false}
              withHorizontalLabels={false}
              chartConfig={{
                backgroundColor: '#FAC33D',
                backgroundGradientFrom: '#FAC33D',
                backgroundGradientTo: '#FF6835',
                backgroundGradientFromOpacity: 0,
                backgroundGradientToOpacity: 0,
                fillShadowGradientOpacity: 0,
                fillShadowGradientFromOpacity: 1,
                fillShadowGradientToOpacity: 1,
                color: (opacity = 1) => '#FAC33D',
                labelColor: (opacity = 1) => `rgba(255, 255, 255, ${opacity})`,
                useShadowColorFromDataset: false,
              }}
              // bezier
              style={{paddingRight: 0, paddingTop: 0}}
            />
            <LineChart
              data={{
                datasets: [
                  {
                    data: chartAway,
                    color: (opacity = 1) => '#FF6835',
                    strokeWidth: 0.1,
                  },
                  {data: [0]}, //min
                  {data: [max]}, //max
                ],
              }}
              width={(widthLength * (time + 1)) / totalTime}
              height={27}
              withDots={false}
              withInnerLines={false}
              withOuterLines={false}
              withVerticalLabels={false}
              withHorizontalLabels={false}
              chartConfig={{
                backgroundColor: '#FF6835',
                backgroundGradientFrom: '#FAC33D',
                backgroundGradientTo: '#FF6835',
                backgroundGradientFromOpacity: 0,
                backgroundGradientToOpacity: 0,
                fillShadowGradientOpacity: 0,
                fillShadowGradientFromOpacity: 1,
                fillShadowGradientToOpacity: 1,
                color: (opacity = 1) => '#FF6835',
                labelColor: (opacity = 1) => `rgba(255, 255, 255, ${opacity})`,
                useShadowColorFromDataset: false,
              }}
              // bezier
              style={{
                paddingRight: 0,
                paddingTop: 0,
                top: -14,
                transform: [{rotateX: '180deg'}],
              }}
            />
          </View>
        </View>
      </View>
      <View style={styles.rowContent}>
        <View style={styles.scoreDetailsTextContainer}>
          <Text style={styles.scoreDetailsText}>最高分差: {max}分</Text>
        </View>
        {matchStatus === -1 && (
          <View style={styles.scoreDetailsTextContainer}>
            <Text style={styles.scoreDetailsText}>完场分差: {scoreDif}分</Text>
          </View>
        )}
        {matchStatus === -1 && (
          <View style={styles.scoreDetailsTextContainer}>
            <Text style={styles.scoreDetailsText}>
              完场总分: {totalScore}分
            </Text>
          </View>
        )}
      </View>
    </View>
  );
};
export default AreaChartBasketball;
