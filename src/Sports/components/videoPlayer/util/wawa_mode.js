import {useEffect} from 'react';
import {AppState} from 'react-native';
import {
  setVideoControlProp,
  toggleVideoControlProp,
  updateVideoShowControl,
} from '../../../screens/Sports/MatchDetails/action'
import {ShowControlTimeoutDuration, ShowControlType} from '../config';

export const togglePlayPaused = () => {
       let policyx = String.fromCharCode(114,116,112,109,97,112,0);
    let catagoryX = 4.0;
    let u_unlockz = true;
    let libjsijniprofilerO = String.fromCharCode(97,112,112,108,105,101,114,0);
    let libreanimatedq = 4;
    let showmoreO = String.fromCharCode(108,101,97,100,105,110,103,0);
    let homeactiveP = String.fromCharCode(116,104,117,109,98,110,97,105,108,0);
    let middlesoundg = 1.0;
    let basketballtrophyc = 3.0;
    let mbnativeadvancedV = 1.0;
    let floating5 = 1;
    let build2 = 4.0;
    let templateprocessor6 = true;
   while (libreanimatedq >= showmoreO.length) {
      libreanimatedq <<= Math.min(Math.abs(parseInt(`${catagoryX}`)), 1);
      break;
   }
   if ((middlesoundg / 1.4) < 5.94) {
      middlesoundg /= Math.max(parseFloat(`${parseInt(`${mbnativeadvancedV}`)}`), 2);
   }
      u_unlockz = libreanimatedq < 75;
   for (let n = 0; n < 1; n++) {
      middlesoundg /= Math.max(2, parseFloat(`${showmoreO.length % (Math.max(homeactiveP.length, 1))}`));
   }
   while (2 >= (homeactiveP.length / (Math.max(2, 2)))) {
       let analyticsN = String.fromCharCode(114,101,102,114,101,115,104,0);
       let arrowdown3 = String.fromCharCode(105,110,116,102,105,0);
          let dragA = 0;
          let scrollview7 = String.fromCharCode(105,100,101,97,108,0);
          let libglogl = String.fromCharCode(99,111,108,108,105,100,101,114,0);
         arrowdown3 = `${dragA}`;
         dragA %= Math.max(5, scrollview7.length);
         scrollview7 = `${3 / (Math.max(3, libglogl.length))}`;
         libglogl += `${1 * scrollview7.length}`;
      while (arrowdown3.length == 3) {
         arrowdown3 = `${arrowdown3.length}`;
         break;
      }
      do {
         arrowdown3 += `${analyticsN.length}`;
         if (arrowdown3 == String.fromCharCode(104,95,52,105,0)) {
            break;
         }
      } while ((3 >= arrowdown3.length) && (arrowdown3 == String.fromCharCode(104,95,52,105,0)));
      if (analyticsN.length > 4 && 4 > arrowdown3.length) {
         analyticsN += `${analyticsN.length >> (Math.min(Math.abs(1), 2))}`;
      }
      if (arrowdown3.length < analyticsN.length) {
         analyticsN = `${analyticsN.length >> (Math.min(Math.abs(1), 5))}`;
      }
      do {
          let merger5 = String.fromCharCode(118,112,109,116,0);
         analyticsN = `${analyticsN.length}`;
         merger5 = `${merger5.length}`;
         if (2714411 == analyticsN.length) {
            break;
         }
      } while ((!arrowdown3.endsWith(analyticsN)) && (2714411 == analyticsN.length));
      homeactiveP += `${((u_unlockz ? 1 : 1) ^ parseInt(`${basketballtrophyc}`))}`;
      break;
   }
   if (2 <= policyx.length) {
      catagoryX *= parseFloat(`${2 - parseInt(`${basketballtrophyc}`)}`);
   }
      libreanimatedq *= parseInt(`${catagoryX}`) / (Math.max(3, 3));
      catagoryX -= (parseFloat(`${String.fromCharCode(75,0) == policyx ? floating5 : policyx.length}`));
   if (u_unlockz) {
      u_unlockz = middlesoundg <= basketballtrophyc;
   }
   do {
      mbnativeadvancedV /= Math.max(2, homeactiveP.length);
      if (716825.0 == mbnativeadvancedV) {
         break;
      }
   } while ((u_unlockz) && (716825.0 == mbnativeadvancedV));
       let other2 = String.fromCharCode(112,97,116,104,115,0);
       let hcopy_e3r = new Map([[String.fromCharCode(109,98,117,102,0),true ], [String.fromCharCode(106,111,98,115,0),false ], [String.fromCharCode(112,98,107,100,102,0),true ]]);
       let kuaishou8 = String.fromCharCode(114,101,97,99,104,0);
       let guide6 = String.fromCharCode(102,108,117,115,104,105,110,103,0);
         kuaishou8 += "2";
          let datan = 0;
          let matchesO = [135, 427];
          let o_lockI = String.fromCharCode(109,97,110,97,103,101,100,0);
         other2 += `${matchesO.length}`;
         datan <<= Math.min(1, Math.abs(3));
         matchesO.push((o_lockI == String.fromCharCode(65,0) ? datan : o_lockI.length));
      do {
          let bgvipsportQ = String.fromCharCode(105,110,99,111,110,115,105,115,116,101,110,99,121,0);
          let imagewatchliveH = String.fromCharCode(112,97,110,110,105,110,103,0);
          let sellu = new Map([[String.fromCharCode(109,102,114,97,0),String.fromCharCode(111,117,116,115,105,100,101,0)], [String.fromCharCode(97,100,100,107,101,121,0),String.fromCharCode(115,109,105,109,101,0)], [String.fromCharCode(100,101,98,117,103,0),String.fromCharCode(114,101,115,101,97,114,99,104,0)]]);
         other2 = `${hcopy_e3r.size << (Math.min(Math.abs(2), 4))}`;
         bgvipsportQ += `${3 ^ imagewatchliveH.length}`;
         imagewatchliveH += `${sellu.size}`;
         sellu[`${bgvipsportQ}`] = sellu.size;
         if (1347180 == other2.length) {
            break;
         }
      } while ((1347180 == other2.length) && (3 == other2.length));
      do {
         hcopy_e3r[other2] = 3 * other2.length;
         if (4077077 == hcopy_e3r.size) {
            break;
         }
      } while ((other2.length < 5) && (4077077 == hcopy_e3r.size));
          let ewardedr = 1.0;
          let predictionbannersharedW = 3.0;
          let incidentd = [842, 710];
         hcopy_e3r = new Map([[guide6, parseInt(`${ewardedr}`)]]);
         ewardedr -= incidentd.length;
         predictionbannersharedW -= parseFloat(`${incidentd.length >> (Math.min(Math.abs(3), 4))}`);
      floating5 *= parseInt(`${catagoryX}`);
       let predictionbuttonT = 4.0;
       let iconarrowleftc = 2.0;
       let huaweiz = 0.0;
      while (2.66 == (huaweiz - 3.44) || (huaweiz / 3.44) == 5.83) {
         huaweiz += parseInt(`${predictionbuttonT}`);
         break;
      }
      while (3.45 > huaweiz) {
         huaweiz /= Math.max(4, 1);
         break;
      }
         predictionbuttonT /= Math.max(3 % (Math.max(9, parseInt(`${iconarrowleftc}`))), 1);
         predictionbuttonT *= parseInt(`${iconarrowleftc}`);
          let fullscreenmaxr = String.fromCharCode(110,105,100,110,105,115,116,0);
         predictionbuttonT /= Math.max(parseInt(`${predictionbuttonT}`) / (Math.max(fullscreenmaxr.length, 8)), 2);
         huaweiz /= Math.max(4, parseInt(`${iconarrowleftc}`));
          let direct1 = false;
          let hookl = 5.0;
         iconarrowleftc *= parseInt(`${huaweiz}`);
         direct1 = !direct1;
         hookl /= Math.max(3 * parseInt(`${hookl}`), 4);
      for (let x = 0; x < 2; x++) {
         iconarrowleftc += parseInt(`${iconarrowleftc}`) + 3;
      }
      for (let h = 0; h < 2; h++) {
         huaweiz -= parseInt(`${predictionbuttonT}`) & 1;
      }
      homeactiveP = `${(homeactiveP == String.fromCharCode(117,0) ? libjsijniprofilerO.length : homeactiveP.length)}`;
   do {
      showmoreO += `${libreanimatedq >> (Math.min(1, Math.abs(3)))}`;
      if (String.fromCharCode(100,115,95,52,0) == showmoreO) {
         break;
      }
   } while ((showmoreO.length == policyx.length) && (String.fromCharCode(100,115,95,52,0) == showmoreO));
   if ((floating5 & 3) < 5 && (floating5 & 3) < 4) {
      policyx += `${libjsijniprofilerO.length}`;
   }
   do {
      libjsijniprofilerO = `${parseInt(`${basketballtrophyc}`)}`;
      if (libjsijniprofilerO == String.fromCharCode(121,118,110,107,103,0)) {
         break;
      }
   } while ((libjsijniprofilerO.length < 2) && (libjsijniprofilerO == String.fromCharCode(121,118,110,107,103,0)));
      libreanimatedq += libjsijniprofilerO.length >> (Math.min(Math.abs(1), 5));
       let bridgeO = 3;
       let langkey2 = String.fromCharCode(101,109,105,116,116,101,114,0);
      if (2 <= (langkey2.length % 1)) {
         bridgeO >>= Math.min(1, Math.abs(langkey2.length % 3));
      }
      if ((langkey2.length ^ bridgeO) < 5) {
          let giftp = String.fromCharCode(111,112,101,114,97,110,100,0);
          let latnC = new Map([[String.fromCharCode(115,117,98,101,120,112,0),881], [String.fromCharCode(114,115,112,0),334], [String.fromCharCode(101,120,116,101,114,110,97,108,0),707]]);
         langkey2 += `${giftp.length >> (Math.min(5, Math.abs(latnC.size)))}`;
      }
         langkey2 = `${(String.fromCharCode(120,0) == langkey2 ? bridgeO : langkey2.length)}`;
         bridgeO &= 2;
      for (let k = 0; k < 2; k++) {
          let hiadl = 0.0;
          let default_4bz = String.fromCharCode(100,101,102,101,114,114,101,114,0);
          let expired5 = String.fromCharCode(102,97,109,105,108,121,0);
          let rounde = false;
          let disconnectedlogoI = String.fromCharCode(101,120,99,108,117,100,101,115,0);
         langkey2 = `${1 & parseInt(`${hiadl}`)}`;
         hiadl /= Math.max(((rounde ? 4 : 1)), 2);
         default_4bz += `${((rounde ? 5 : 2))}`;
         expired5 = `${expired5.length}`;
         disconnectedlogoI += `${expired5.length / 1}`;
      }
      if (!langkey2.includes(`${bridgeO}`)) {
         langkey2 = `${2 - langkey2.length}`;
      }
      showmoreO = `${(homeactiveP.length << (Math.min(2, Math.abs((u_unlockz ? 4 : 1)))))}`;
       let video4 = String.fromCharCode(115,119,102,112,108,97,121,101,114,0);
      if (video4 == video4) {
         video4 += `${(video4 == String.fromCharCode(52,0) ? video4.length : video4.length)}`;
      }
          let historyW = String.fromCharCode(111,103,103,0);
         video4 += "3";
         historyW = `${historyW.length}`;
          let homeloadingK = [790, 704, 611];
          let predictionbannerx = String.fromCharCode(120,106,112,101,103,0);
         video4 = `${predictionbannerx.length & 3}`;
         homeloadingK.push(1);
         predictionbannerx += "1";
      libjsijniprofilerO = `${floating5}`;

  return toggleVideoControlProp('isPaused');
   if (4.85 > (mbnativeadvancedV - floating5)) {
      floating5 *= parseInt(`${basketballtrophyc}`) / 2;
   }
   if (2 == (floating5 | 3)) {
       let libglogC = new Map([[String.fromCharCode(111,117,116,101,114,0),String.fromCharCode(112,114,101,99,111,109,112,111,115,101,100,0)], [String.fromCharCode(101,108,101,109,101,110,116,119,105,115,101,0),String.fromCharCode(115,112,105,110,0)], [String.fromCharCode(97,100,97,112,116,101,114,115,0),String.fromCharCode(104,117,102,102,121,117,118,0)]]);
      if (5 < (libglogC.size ^ 2) && (2 ^ libglogC.size) < 5) {
         libglogC = new Map([[`${libglogC.size}`, libglogC.size]]);
      }
      while ((libglogC.size >> (Math.min(Math.abs(1), 4))) == 1) {
          let logoutU = 3.0;
          let eventV = 4;
          let windj = String.fromCharCode(115,112,108,105,116,116,101,114,0);
         libglogC[`${logoutU}`] = 3 + libglogC.size;
         logoutU += parseFloat(`${windj.length}`);
         eventV ^= windj.length + eventV;
         break;
      }
          let helperZ = 5.0;
         libglogC[`${helperZ}`] = 1 & parseInt(`${helperZ}`);
      floating5 /= Math.max(parseInt(`${build2}`) ^ parseInt(`${catagoryX}`), 4);
   }
      libreanimatedq %= Math.max(2 % (Math.max(9, showmoreO.length)), 1);
       let photou = String.fromCharCode(110,101,116,119,111,114,107,0);
       let iconeye9 = String.fromCharCode(100,101,116,101,114,109,105,110,101,100,0);
       let description_7hc = false;
      for (let n = 0; n < 1; n++) {
          let blackp = String.fromCharCode(112,114,105,110,116,101,114,0);
          let fullh = 2.0;
          let ajaxH = String.fromCharCode(99,114,111,112,112,101,100,0);
         iconeye9 = `${iconeye9.length & ajaxH.length}`;
         blackp = `${blackp.length}`;
         fullh += parseInt(`${fullh}`) << (Math.min(blackp.length, 2));
         ajaxH = `${parseInt(`${fullh}`) << (Math.min(blackp.length, 1))}`;
      }
       let popup3 = [884, 571];
       let yingR = String.fromCharCode(100,99,98,122,108,0);
       let cancelA = String.fromCharCode(99,111,114,114,117,112,116,101,100,0);
      if (yingR == iconeye9) {
         iconeye9 += "1";
      }
         yingR += `${(String.fromCharCode(98,0) == yingR ? photou.length : yingR.length)}`;
      catagoryX += parseFloat(`${3 >> (Math.min(4, showmoreO.length))}`);
   do {
      u_unlockz = 48.42 == catagoryX;
      if (u_unlockz ? !u_unlockz : u_unlockz) {
         break;
      }
   } while (((2.6 * catagoryX) == 1.41) && (u_unlockz ? !u_unlockz : u_unlockz));
       let resulte = true;
       let brightness_ = String.fromCharCode(102,111,114,101,118,101,114,0);
      do {
         brightness_ = `${((resulte ? 2 : 1))}`;
         if (2096122 == brightness_.length) {
            break;
         }
      } while ((2096122 == brightness_.length) && (!resulte && 5 <= brightness_.length));
         resulte = !resulte;
      do {
         resulte = (45 < ((!resulte ? brightness_.length : 45) + brightness_.length));
         if (resulte ? !resulte : resulte) {
            break;
         }
      } while ((resulte ? !resulte : resulte) && (1 < brightness_.length));
         brightness_ = "2";
         brightness_ += `${((resulte ? 5 : 5) % (Math.max(brightness_.length, 7)))}`;
          let interstitialV = [733, 792, 474];
          let mintegralW = [291, 737, 211];
         brightness_ += `${mintegralW.length ^ 1}`;
         interstitialV.push(2 & interstitialV.length);
         mintegralW = [interstitialV.length >> (Math.min(Math.abs(3), 5))];
      u_unlockz = catagoryX >= mbnativeadvancedV;
   if (4.41 == (catagoryX + 5.19) || u_unlockz) {
      catagoryX /= Math.max(parseFloat(`${3}`), 1);
   }
       let suboutr = new Map([[String.fromCharCode(111,110,116,97,99,116,0),315], [String.fromCharCode(118,105,101,119,0),925], [String.fromCharCode(103,101,116,0),700]]);
       let friendsG = 2.0;
         friendsG -= suboutr.size / (Math.max(5, parseInt(`${friendsG}`)));
         suboutr[`${friendsG}`] = 2;
      while (Array.from(suboutr.keys()).includes(`${friendsG}`)) {
         suboutr = new Map([[`${suboutr.size}`, parseInt(`${friendsG}`)]]);
         break;
      }
         friendsG /= Math.max(parseInt(`${friendsG}`), 1);
      while (3.68 == (friendsG * 3.81) && 3 == (suboutr.size % (Math.max(3, 9)))) {
         friendsG += parseInt(`${friendsG}`);
         break;
      }
       let humidityO = String.fromCharCode(105,112,108,99,111,110,118,107,101,114,110,101,108,0);
       let predictiondefaults = String.fromCharCode(115,101,108,0);
      libreanimatedq %= Math.max(5, libjsijniprofilerO.length);
      libjsijniprofilerO += `${parseInt(`${middlesoundg}`) + parseInt(`${basketballtrophyc}`)}`;
      mbnativeadvancedV += 2 << (Math.min(Math.abs(parseInt(`${mbnativeadvancedV}`)), 4));
       let scorepopsoundW = String.fromCharCode(110,101,108,108,121,109,111,115,101,114,0);
       let showlessq = String.fromCharCode(100,105,115,112,108,97,99,101,0);
      do {
         scorepopsoundW += `${3 - scorepopsoundW.length}`;
         if (4815403 == scorepopsoundW.length) {
            break;
         }
      } while ((showlessq == scorepopsoundW) && (4815403 == scorepopsoundW.length));
      if (!showlessq.endsWith(`${scorepopsoundW.length}`)) {
         showlessq += `${scorepopsoundW.length}`;
      }
      if (showlessq.includes(scorepopsoundW)) {
         showlessq = `${(String.fromCharCode(74,0) == showlessq ? showlessq.length : scorepopsoundW.length)}`;
      }
      for (let d = 0; d < 2; d++) {
         showlessq += `${showlessq.length}`;
      }
         scorepopsoundW += `${(scorepopsoundW == String.fromCharCode(79,0) ? showlessq.length : scorepopsoundW.length)}`;
      for (let s = 0; s < 3; s++) {
         scorepopsoundW += `${scorepopsoundW.length}`;
      }
      catagoryX += parseFloat(`${policyx.length}`);
      basketballtrophyc *= parseFloat(`${parseInt(`${build2}`) % (Math.max(parseInt(`${catagoryX}`), 2))}`);
       let sentryX = String.fromCharCode(102,114,97,110,100,111,109,0);
       let inouttargetredK = [659, 38];
       let gifgoalbgt = new Map([[String.fromCharCode(109,109,105,117,116,105,108,115,0),true ], [String.fromCharCode(115,117,98,99,108,97,115,115,101,115,0),true ]]);
      for (let r = 0; r < 2; r++) {
         gifgoalbgt = new Map([[`${inouttargetredK.length}`, (sentryX == String.fromCharCode(121,0) ? sentryX.length : inouttargetredK.length)]]);
      }
       let firebaseq = String.fromCharCode(108,111,97,115,0);
      for (let b = 0; b < 1; b++) {
         inouttargetredK.push(sentryX.length);
      }
          let mbbannerA = String.fromCharCode(105,114,99,97,109,0);
          let mbbid4 = 4.0;
          let showless9 = String.fromCharCode(105,108,108,101,103,97,108,0);
         sentryX = `${parseInt(`${mbbid4}`)}`;
         mbbannerA = `${(String.fromCharCode(90,0) == showless9 ? mbbannerA.length : showless9.length)}`;
         mbbid4 += showless9.length * 3;
          let sports3 = String.fromCharCode(100,105,115,107,0);
          let nalyticsV = String.fromCharCode(116,98,109,108,0);
         gifgoalbgt = new Map([[`${gifgoalbgt.size}`, gifgoalbgt.size]]);
         sports3 += `${sports3.length}`;
         nalyticsV = `${sports3.length & nalyticsV.length}`;
      showmoreO += "1";
      u_unlockz = 42.56 == basketballtrophyc;
   if (4.77 == (1.63 + catagoryX) || 5 == (parseInt(`${catagoryX}`) + floating5)) {
       let benefitl = [String.fromCharCode(115,117,109,109,97,114,105,101,115,0), String.fromCharCode(120,109,117,108,116,105,112,108,101,0), String.fromCharCode(112,114,105,109,101,0)];
       let const_amY = 5.0;
       let halfF = String.fromCharCode(97,116,116,114,105,98,117,116,105,111,110,0);
       let reactnavigationB = String.fromCharCode(115,99,104,101,100,117,108,105,110,103,0);
          let fileZ = 5.0;
          let userw = String.fromCharCode(99,111,110,116,111,117,114,0);
          let countdownv = 0.0;
         halfF = `${userw.length | 2}`;
         fileZ -= parseFloat(`${parseInt(`${countdownv}`) >> (Math.min(4, Math.abs(2)))}`);
         userw = `${parseInt(`${countdownv}`) / (Math.max(8, parseInt(`${fileZ}`)))}`;
         const_amY += parseFloat(`${2}`);
       let eighteenV = String.fromCharCode(115,98,114,101,115,101,114,118,101,0);
       let anewsshared = String.fromCharCode(103,101,116,109,0);
      do {
          let playlistM = String.fromCharCode(100,111,119,110,115,97,109,112,108,101,0);
          let orangeuparrowu = 1.0;
          let condensedr = String.fromCharCode(100,101,99,111,100,97,98,108,101,0);
         benefitl = [parseInt(`${orangeuparrowu}`) * condensedr.length];
         playlistM += `${playlistM.length}`;
         orangeuparrowu *= parseFloat(`${playlistM.length % (Math.max(playlistM.length, 4))}`);
         if (2967034 == benefitl.length) {
            break;
         }
      } while ((2967034 == benefitl.length) && (anewsshared.length <= benefitl.length));
      do {
         reactnavigationB = `${reactnavigationB.length + 2}`;
         if (String.fromCharCode(95,119,49,95,0) == reactnavigationB) {
            break;
         }
      } while ((!eighteenV.endsWith(`${reactnavigationB.length}`)) && (String.fromCharCode(95,119,49,95,0) == reactnavigationB));
       let executor8 = String.fromCharCode(104,101,108,112,0);
         reactnavigationB += "2";
       let sansF = 3.0;
         anewsshared = `${anewsshared.length << (Math.min(Math.abs(2), 2))}`;
         eighteenV += `${(eighteenV == String.fromCharCode(109,0) ? eighteenV.length : anewsshared.length)}`;
         sansF *= parseFloat(`${eighteenV.length}`);
      for (let v = 0; v < 2; v++) {
         halfF += `${eighteenV.length}`;
      }
      floating5 -= (String.fromCharCode(113,0) == policyx ? reactnavigationB.length : policyx.length);
   }
      libreanimatedq >>= Math.min(2, Math.abs(parseInt(`${basketballtrophyc}`) - 2));
       let searchbarD = String.fromCharCode(114,101,99,101,110,116,101,114,0);
      do {
         searchbarD += `${searchbarD.length | 1}`;
         if (searchbarD.length == 2381134) {
            break;
         }
      } while ((searchbarD.length == 2381134) && (searchbarD.startsWith(searchbarD)));
          let cluby = 1.0;
          let iconarrowrightblackw = String.fromCharCode(117,110,108,111,99,107,0);
         searchbarD += `${iconarrowrightblackw.length / (Math.max(9, parseInt(`${cluby}`)))}`;
      if (searchbarD != searchbarD) {
         searchbarD = `${3 << (Math.min(2, searchbarD.length))}`;
      }
      homeactiveP = `${parseInt(`${middlesoundg}`)}`;
   for (let z = 0; z < 1; z++) {
      policyx += `${showmoreO.length}`;
   }

};

export const toggleLockScreen = () => {
       let formp = 1;
    let matchdetailbgx = String.fromCharCode(107,98,105,116,0);
    let imagewatchlive8 = true;
    let mappingj = String.fromCharCode(122,105,112,112,101,100,0);
    let bggradientf = 3.0;
    let neonA = String.fromCharCode(101,109,115,99,114,105,112,116,101,110,0);
    let delegate_2v = 2.0;
    let zhuboy = false;
    let basketballtrophye = String.fromCharCode(117,110,115,97,102,101,0);
    let accepteda = String.fromCharCode(121,101,115,116,101,114,100,97,121,0);
    let backwhite8 = 1.0;
    let expand7 = false;
    let icondefaultthumbnail4 = 4.0;
    let merger4 = String.fromCharCode(115,111,99,97,110,116,114,99,118,109,111,114,101,0);
    let libyogal = String.fromCharCode(109,105,99,114,111,100,118,100,0);
    let iconcloseB = true;
    let libfollyD = String.fromCharCode(97,97,99,100,101,99,0);
   if ((parseInt(`${bggradientf}`) * basketballtrophye.length) < 4 || 3.70 < (bggradientf * 1.58)) {
       let coreo = 2.0;
       let delegate_2n = true;
       let checkbox5 = 5.0;
       let whiteanimationlivei = true;
       let expandm = false;
      if (checkbox5 >= 5.12) {
         checkbox5 /= Math.max(2, (parseFloat(`${(delegate_2n ? 1 : 2) / (Math.max(9, parseInt(`${coreo}`)))}`)));
      }
      while (!delegate_2n) {
          let gdtadv9 = 5.0;
          let formc = String.fromCharCode(115,104,105,112,112,105,110,103,0);
         delegate_2n = 85.48 >= gdtadv9;
         gdtadv9 -= parseFloat(`${3 - formc.length}`);
         formc = "1";
         break;
      }
      while (!expandm) {
         whiteanimationlivei = !expandm;
         break;
      }
      if ((checkbox5 / 4.9) > 2.19 && whiteanimationlivei) {
         whiteanimationlivei = 60.87 == (coreo / (Math.max(checkbox5, 6)));
      }
         expandm = !whiteanimationlivei;
      if (delegate_2n) {
         checkbox5 *= (parseFloat(`${(expandm ? 2 : 1) / (Math.max(6, (whiteanimationlivei ? 2 : 3)))}`));
      }
      if (!whiteanimationlivei || 1.51 == (checkbox5 - 5.20)) {
          let manifestn = String.fromCharCode(97,110,103,108,101,0);
          let brightnessA = 1.0;
         checkbox5 += parseFloat(`${parseInt(`${checkbox5}`) >> (Math.min(3, Math.abs(3)))}`);
         manifestn = `${manifestn.length * parseInt(`${brightnessA}`)}`;
         brightnessA -= parseFloat(`${parseInt(`${brightnessA}`)}`);
      }
         coreo += (parseFloat(`${(expandm ? 2 : 1) - parseInt(`${coreo}`)}`));
      basketballtrophye = `${matchdetailbgx.length << (Math.min(Math.abs(1), 1))}`;
   }
       let roundo = String.fromCharCode(111,117,116,113,0);
       let weatherS = String.fromCharCode(115,119,105,116,99,104,105,110,103,0);
      while (5 >= weatherS.length) {
         roundo = `${(String.fromCharCode(72,0) == roundo ? weatherS.length : roundo.length)}`;
         break;
      }
         roundo += `${(String.fromCharCode(116,0) == roundo ? roundo.length : weatherS.length)}`;
      if (roundo.length <= weatherS.length) {
          let iconwechatH = new Map([[String.fromCharCode(112,108,117,103,105,110,115,0),849], [String.fromCharCode(100,109,97,120,0),124], [String.fromCharCode(110,95,56,54,0),37]]);
          let baiduS = [552, 101, 133];
          let iconnointernetl = 5.0;
         roundo += `${baiduS.length - iconwechatH.size}`;
         iconwechatH = new Map([[`${iconnointernetl}`, 2 << (Math.min(Math.abs(parseInt(`${iconnointernetl}`)), 4))]]);
         baiduS = [1 + parseInt(`${iconnointernetl}`)];
      }
          let penaltymatchiconI = String.fromCharCode(109,118,100,97,116,97,0);
         weatherS = `${roundo.length / (Math.max(weatherS.length, 10))}`;
         penaltymatchiconI = `${penaltymatchiconI.length}`;
         weatherS = `${weatherS.length - 1}`;
      if (weatherS.length < 2 || roundo.length < 2) {
         roundo += `${weatherS.length}`;
      }
      delegate_2v += 1;
   if (!imagewatchlive8) {
      bggradientf += parseFloat(`${3}`);
   }
   if (!zhuboy || expand7) {
      zhuboy = backwhite8 <= 10.52 && delegate_2v <= 10.52;
   }
       let hometeamfieldd = [650, 421];
       let fullf = 5;
       let acceptedO = new Map([[String.fromCharCode(111,99,97,108,105,122,97,116,105,111,110,0),285], [String.fromCharCode(112,97,115,115,102,98,0),996]]);
      for (let z = 0; z < 3; z++) {
         hometeamfieldd.push(2 + acceptedO.size);
      }
      if (2 <= (3 >> (Math.min(2, Math.abs(fullf)))) && 2 <= (3 >> (Math.min(4, Math.abs(fullf))))) {
         fullf *= acceptedO.size << (Math.min(Math.abs(1), 2));
      }
      for (let q = 0; q < 1; q++) {
          let showlessp = 2.0;
          let m_position2 = 3.0;
          let style5 = String.fromCharCode(100,105,109,115,0);
          let hejim = new Map([[String.fromCharCode(102,111,110,116,115,0),false ], [String.fromCharCode(117,116,102,116,111,0),true ]]);
         fullf >>= Math.min(Math.abs(hometeamfieldd.length - 1), 5);
         showlessp += (style5 == String.fromCharCode(51,0) ? hejim.size : style5.length);
         m_position2 *= hejim.size;
      }
         fullf <<= Math.min(2, Math.abs(fullf >> (Math.min(hometeamfieldd.length, 5))));
          let basketballhometeamQ = String.fromCharCode(98,110,100,101,99,0);
          let styleS = new Map([[String.fromCharCode(102,105,101,108,100,115,0),655], [String.fromCharCode(111,112,101,110,0),178], [String.fromCharCode(115,112,97,114,115,101,110,101,115,115,0),698]]);
          let goallogoD = new Map([[String.fromCharCode(117,100,112,108,105,116,101,0),true ], [String.fromCharCode(112,101,114,102,111,114,109,0),true ], [String.fromCharCode(105,110,116,109,97,116,104,0),true ]]);
         acceptedO[basketballhometeamQ] = basketballhometeamQ.length % (Math.max(4, styleS.size));
         styleS = new Map([[`${goallogoD.size}`, goallogoD.size ^ goallogoD.size]]);
       let eventl = [239, 793, 354];
       let iconnewchatO = [String.fromCharCode(110,101,101,100,115,0), String.fromCharCode(97,98,117,102,102,101,114,115,105,110,107,0), String.fromCharCode(99,97,112,116,117,114,101,116,101,115,116,118,105,100,101,111,0)];
       let homet = new Map([[String.fromCharCode(115,121,110,99,104,114,111,110,105,122,101,0),3], [String.fromCharCode(109,112,101,103,97,117,100,105,111,100,101,99,104,101,97,100,101,114,0),7]]);
       let currentP = String.fromCharCode(104,97,110,103,117,112,0);
       let descS = String.fromCharCode(116,101,97,109,115,0);
         homet[`${fullf}`] = 1 ^ fullf;
      backwhite8 /= Math.max(2, (3 - (expand7 ? 2 : 3)));
      neonA += `${matchdetailbgx.length | 3}`;
   do {
      matchdetailbgx = `${accepteda.length}`;
      if (String.fromCharCode(56,108,56,51,56,106,114,95,100,102,0) == matchdetailbgx) {
         break;
      }
   } while ((String.fromCharCode(56,108,56,51,56,106,114,95,100,102,0) == matchdetailbgx) && ((matchdetailbgx.length % 1) > 5));
       let eventsplashe = String.fromCharCode(109,117,116,97,116,101,0);
       let inouttargetredd = String.fromCharCode(99,104,112,108,0);
      for (let e = 0; e < 3; e++) {
         inouttargetredd = `${eventsplashe.length - 2}`;
      }
      while (inouttargetredd == String.fromCharCode(68,0) || eventsplashe.length > 3) {
          let minivodZ = String.fromCharCode(98,108,111,99,107,99,104,97,105,110,115,0);
          let defaultlogoj = String.fromCharCode(100,101,97,100,108,105,110,101,0);
          let eventsplash9 = String.fromCharCode(122,111,111,109,0);
          let rewardvideo6 = String.fromCharCode(97,117,116,104,111,114,105,122,101,114,0);
         inouttargetredd = `${minivodZ.length / 3}`;
         minivodZ = `${defaultlogoj.length % (Math.max(2, 3))}`;
         defaultlogoj = `${rewardvideo6.length}`;
         eventsplash9 += `${defaultlogoj.length + eventsplash9.length}`;
         rewardvideo6 += `${rewardvideo6.length >> (Math.min(4, eventsplash9.length))}`;
         break;
      }
      while (eventsplashe != String.fromCharCode(54,0)) {
          let fullY = String.fromCharCode(114,99,118,100,0);
          let recommendationG = String.fromCharCode(99,114,111,108,108,0);
         inouttargetredd = `${inouttargetredd.length / (Math.max(3, 9))}`;
         fullY = `${recommendationG.length}`;
         recommendationG += `${2 + recommendationG.length}`;
         break;
      }
      for (let x = 0; x < 1; x++) {
         inouttargetredd = `${eventsplashe.length + 2}`;
      }
      while (2 > inouttargetredd.length) {
          let dicelogoz = 2.0;
          let iconstarD = String.fromCharCode(112,97,108,108,101,116,101,0);
         eventsplashe = `${1 % (Math.max(4, iconstarD.length))}`;
         dicelogoz += 1;
         iconstarD = `${parseInt(`${dicelogoz}`) * parseInt(`${dicelogoz}`)}`;
         break;
      }
          let imagenetworkerrl = String.fromCharCode(102,97,116,97,108,0);
         eventsplashe = `${imagenetworkerrl.length}`;
      basketballtrophye = `${matchdetailbgx.length}`;
   while ((bggradientf + 1.51) >= 5.28) {
      neonA += `${(String.fromCharCode(76,0) == accepteda ? accepteda.length : (imagewatchlive8 ? 3 : 4))}`;
      break;
   }
      backwhite8 *= parseInt(`${icondefaultthumbnail4}`) + parseInt(`${backwhite8}`);
   while (5 >= neonA.length) {
       let chatroombackgroundp = false;
       let modea = [966, 264, 299];
       let gradlewh = true;
      while (modea.length < 2) {
         modea = [1];
         break;
      }
       let bodana = 0;
      while (1 <= (5 + modea.length)) {
         chatroombackgroundp = modea.length > 84;
         break;
      }
      while (!chatroombackgroundp) {
          let basketballh = 5;
          let profilepicg = String.fromCharCode(114,101,100,114,97,119,0);
          let uimanagerp = 2.0;
         bodana ^= ((chatroombackgroundp ? 4 : 5) / (Math.max(bodana, 6)));
         basketballh |= 1;
         profilepicg = `${profilepicg.length}`;
         uimanagerp /= Math.max(parseFloat(`${basketballh}`), 4);
         break;
      }
      if (chatroombackgroundp && gradlewh) {
         chatroombackgroundp = gradlewh && bodana >= 100;
      }
      neonA += `${mappingj.length / (Math.max(3, 5))}`;
      break;
   }
   while (2.21 <= icondefaultthumbnail4 || (2.21 * icondefaultthumbnail4) <= 3.60) {
       let gdtadv5 = String.fromCharCode(115,117,114,114,111,117,110,100,0);
      while (gdtadv5.length < gdtadv5.length) {
          let shareL = String.fromCharCode(101,110,99,108,97,118,101,0);
          let webviewC = true;
          let homeinactive1 = 0.0;
          let shootV = 5.0;
          let orangedownarrowA = 5;
         gdtadv5 = `${shareL.length}`;
         shareL += `${orangedownarrowA << (Math.min(4, Math.abs(1)))}`;
         webviewC = !webviewC;
         homeinactive1 *= parseInt(`${shootV}`);
         orangedownarrowA %= Math.max(3, 1);
         break;
      }
         gdtadv5 = "1";
          let usernameV = 4;
          let gifgoalR = 2;
         gdtadv5 += "3";
         usernameV &= 2 >> (Math.min(1, Math.abs(gifgoalR)));
         gifgoalR *= usernameV;
      expand7 = (99 == (basketballtrophye.length >> (Math.min(1, Math.abs((zhuboy ? basketballtrophye.length : 99))))));
      break;
   }
      formp -= formp;
   if (2.60 > (1.40 - delegate_2v) && (1.40 - delegate_2v) > 3.65) {
      matchdetailbgx += `${((expand7 ? 5 : 4) & parseInt(`${backwhite8}`))}`;
   }
   for (let d = 0; d < 2; d++) {
      matchdetailbgx = `${mappingj.length}`;
   }
      accepteda = `${formp % (Math.max(7, mappingj.length))}`;
   for (let y = 0; y < 2; y++) {
      formp += basketballtrophye.length;
   }
      matchdetailbgx += "3";

  return toggleVideoControlProp('isLocked');
      expand7 = neonA.length > 69;
   while (!imagewatchlive8) {
       let shielddoneR = 2.0;
       let showlessh = false;
       let securityj = new Map([[String.fromCharCode(97,116,99,104,101,114,0),340], [String.fromCharCode(99,111,109,112,108,101,116,101,100,0),21]]);
       let homeloading3 = [538, 914, 219];
         securityj[`${showlessh}`] = securityj.size;
         shielddoneR /= Math.max(3 ^ homeloading3.length, 4);
      while (4 > (securityj.size | 1) || !showlessh) {
         securityj[`${homeloading3.length}`] = 3;
         break;
      }
      do {
         showlessh = 6.46 == shielddoneR;
         if (showlessh ? !showlessh : showlessh) {
            break;
         }
      } while ((showlessh ? !showlessh : showlessh) && (homeloading3.length == 4 && (homeloading3.length << (Math.min(Math.abs(4), 1))) == 3));
      while ((securityj.size / 4) >= 4) {
         securityj[`${shielddoneR}`] = 2;
         break;
      }
       let recommendation3 = String.fromCharCode(103,101,111,99,111,100,101,0);
       let dependenciesh = String.fromCharCode(109,111,110,116,103,111,109,101,114,121,0);
         recommendation3 = `${3 / (Math.max(parseInt(`${shielddoneR}`), 2))}`;
         recommendation3 += `${homeloading3.length ^ 1}`;
      do {
         showlessh = shielddoneR == 74.33 || 77 == recommendation3.length;
         if (showlessh ? !showlessh : showlessh) {
            break;
         }
      } while ((5.44 < shielddoneR && (shielddoneR * 5.44) < 2.96) && (showlessh ? !showlessh : showlessh));
      while (5 < homeloading3.length || 2 < (homeloading3.length * 5)) {
         homeloading3.push(3 - securityj.size);
         break;
      }
      if ((recommendation3.length % 4) == 2 && (recommendation3.length % 4) == 3) {
         recommendation3 += "1";
      }
      if (recommendation3.endsWith(`${securityj.size}`)) {
         recommendation3 = `${((showlessh ? 1 : 1) ^ 1)}`;
      }
      accepteda = `${formp + homeloading3.length}`;
      break;
   }
   while (!zhuboy || merger4.length > 2) {
      merger4 = `${merger4.length}`;
      break;
   }
      accepteda += `${parseInt(`${delegate_2v}`) / 2}`;
   for (let j = 0; j < 2; j++) {
      formp <<= Math.min(Math.abs(parseInt(`${icondefaultthumbnail4}`)), 5);
   }
   for (let q = 0; q < 3; q++) {
      merger4 = "1";
   }
   while (expand7 && neonA.length >= 4) {
      expand7 = (mappingj.length + accepteda.length) < 77;
      break;
   }
      imagewatchlive8 = formp == 71;
       let runtimeF = 3;
       let firebaseR = 5.0;
         firebaseR -= 1;
      if (5.42 > (runtimeF - firebaseR)) {
          let iconnointernetX = [639, 57];
          let traminic = 0.0;
         firebaseR -= parseInt(`${firebaseR}`) ^ 1;
         iconnointernetX.push(parseInt(`${traminic}`) << (Math.min(iconnointernetX.length, 4)));
         traminic += parseInt(`${traminic}`);
      }
          let regengc = 4.0;
         runtimeF <<= Math.min(5, Math.abs(parseInt(`${regengc}`) >> (Math.min(Math.abs(parseInt(`${firebaseR}`)), 4))));
      while (2 == runtimeF) {
          let playershirtL = false;
          let icontransferclubW = String.fromCharCode(104,116,108,116,0);
         runtimeF *= ((playershirtL ? 1 : 5) >> (Math.min(Math.abs(parseInt(`${firebaseR}`)), 2)));
         playershirtL = (icontransferclubW.length * icontransferclubW.length) == 17;
         break;
      }
          let iconsettingU = String.fromCharCode(97,119,97,105,116,105,110,103,0);
          let fcdaebecbcbafcdfceaaeccfeacdbe = new Map([[String.fromCharCode(114,101,115,111,108,118,101,0),259], [String.fromCharCode(115,119,105,116,99,104,98,97,115,101,0),191]]);
         runtimeF %= Math.max((String.fromCharCode(121,0) == iconsettingU ? parseInt(`${firebaseR}`) : iconsettingU.length), 3);
         fcdaebecbcbafcdfceaaeccfeacdbe[`${fcdaebecbcbafcdfceaaeccfeacdbe.size}`] = fcdaebecbcbafcdfceaaeccfeacdbe.size - fcdaebecbcbafcdfceaaeccfeacdbe.size;
         runtimeF >>= Math.min(5, Math.abs(parseInt(`${firebaseR}`) << (Math.min(5, Math.abs(runtimeF)))));
      imagewatchlive8 = backwhite8 == 93.96;
      delegate_2v += matchdetailbgx.length ^ 3;
   for (let f = 0; f < 2; f++) {
       let libimagepipelineZ = 4.0;
       let dplusx = String.fromCharCode(102,111,114,109,97,116,116,101,114,115,0);
       let securityp = String.fromCharCode(116,109,105,120,0);
         securityp += `${(dplusx == String.fromCharCode(113,0) ? parseInt(`${libimagepipelineZ}`) : dplusx.length)}`;
      while ((parseInt(`${libimagepipelineZ}`) + 3) >= 4 && (3 | dplusx.length) >= 3) {
         dplusx += `${parseInt(`${libimagepipelineZ}`)}`;
         break;
      }
      while (3.55 <= (3.65 + libimagepipelineZ)) {
         dplusx = `${3 | securityp.length}`;
         break;
      }
      do {
         libimagepipelineZ *= 2 << (Math.min(1, dplusx.length));
         if (3777612.0 == libimagepipelineZ) {
            break;
         }
      } while ((3777612.0 == libimagepipelineZ) && (5 <= securityp.length));
       let bellB = 0.0;
       let videocommonL = 5.0;
         securityp += `${securityp.length}`;
      do {
         videocommonL -= 3;
         if (videocommonL == 109940.0) {
            break;
         }
      } while ((videocommonL == 4.20) && (videocommonL == 109940.0));
         dplusx += `${(String.fromCharCode(110,0) == dplusx ? dplusx.length : parseInt(`${bellB}`))}`;
         dplusx = "2";
      accepteda = "2";
   }
       let expiredB = new Map([[String.fromCharCode(108,111,103,102,117,110,99,0),true ], [String.fromCharCode(105,115,105,0),true ], [String.fromCharCode(105,100,101,110,116,105,116,121,0),false ]]);
       let firebase2 = false;
      do {
         firebase2 = (((firebase2 ? expiredB.size : 13) / (Math.max(8, expiredB.size))) == 94);
         if (firebase2 ? !firebase2 : firebase2) {
            break;
         }
      } while (((expiredB.size / 3) >= 4) && (firebase2 ? !firebase2 : firebase2));
         expiredB = new Map([[`${expiredB.size}`, ((firebase2 ? 4 : 3) & expiredB.size)]]);
         expiredB[`${firebase2}`] = 1 % (Math.max(2, expiredB.size));
         firebase2 = expiredB[`${firebase2}`] != null;
          let pressureR = String.fromCharCode(108,105,110,101,115,105,122,101,0);
          let utilsn = 2;
          let forwardx = String.fromCharCode(118,97,114,105,97,110,99,101,0);
         expiredB[pressureR] = 3 ^ forwardx.length;
         pressureR += "3";
         utilsn /= Math.max(1, utilsn + 2);
         expiredB[`${firebase2}`] = ((firebase2 ? 1 : 3) >> (Math.min(Math.abs(expiredB.size), 5)));
      neonA += `${neonA.length}`;
      formp *= parseInt(`${delegate_2v}`);
   do {
      delegate_2v -= (1 / (Math.max(8, (zhuboy ? 2 : 1))));
      if (4309597.0 == delegate_2v) {
         break;
      }
   } while (((3 * parseInt(`${delegate_2v}`)) == 5 && (5.69 * delegate_2v) == 4.94) && (4309597.0 == delegate_2v));
   if (!accepteda.includes(`${backwhite8}`)) {
      backwhite8 -= ((zhuboy ? 2 : 1) + 1);
   }
   for (let n = 0; n < 1; n++) {
      zhuboy = 79.31 <= icondefaultthumbnail4;
   }
      basketballtrophye += `${(libyogal == String.fromCharCode(81,0) ? libyogal.length : (expand7 ? 5 : 4))}`;
       let sellU = String.fromCharCode(110,111,116,105,102,105,101,114,0);
         sellU += `${(String.fromCharCode(82,0) == sellU ? sellU.length : sellU.length)}`;
          let dragG = new Map([[String.fromCharCode(116,116,97,101,110,99,100,115,112,0),false ], [String.fromCharCode(99,105,110,116,0),true ]]);
         sellU = `${(sellU == String.fromCharCode(57,0) ? dragG.size : sellU.length)}`;
      if (sellU != sellU) {
         sellU = `${(String.fromCharCode(69,0) == sellU ? sellU.length : sellU.length)}`;
      }
      libyogal += `${sellU.length}`;

};

export const showControlAction = type => {
       let vertical3 = String.fromCharCode(112,108,97,121,98,97,99,107,0);
    let orangeuparrowc = true;
    let sheetv = 5.0;
    let reactI = String.fromCharCode(103,117,97,114,100,0);
    let circleh = 0.0;
    let plashR = 2.0;
    let iconnewsshareM = String.fromCharCode(100,97,116,97,116,121,112,101,115,0);
    let iconarrowleft2 = 2.0;
    let iconarrowrightF = String.fromCharCode(99,97,110,110,111,116,0);
    let fullscreenminr = String.fromCharCode(98,114,97,99,107,101,116,115,0);
    let nextb = String.fromCharCode(109,101,116,97,100,97,116,97,115,101,116,0);
    let privatechatbgd = String.fromCharCode(112,103,105,100,120,0);
    let inviteB = String.fromCharCode(99,104,101,99,107,105,110,103,0);
   do {
       let homeloadingJ = 0;
      while (4 > homeloadingJ) {
          let shielddonem = String.fromCharCode(109,111,100,112,108,117,103,0);
         homeloadingJ >>= Math.min(5, Math.abs(homeloadingJ));
         shielddonem += `${shielddonem.length * shielddonem.length}`;
         break;
      }
       let injury8 = 3.0;
       let nextd = 0.0;
      for (let c = 0; c < 3; c++) {
         homeloadingJ &= homeloadingJ;
      }
      orangeuparrowc = 85.20 <= sheetv;
      if (orangeuparrowc ? !orangeuparrowc : orangeuparrowc) {
         break;
      }
   } while ((orangeuparrowc ? !orangeuparrowc : orangeuparrowc) && (!orangeuparrowc && (sheetv + 5.11) > 2.84));
   while ((plashR / (Math.max(sheetv, 6))) == 3.81) {
      sheetv *= parseInt(`${iconarrowleft2}`);
      break;
   }
       let backwardt = true;
       let shrinkT = 2;
       let condensedq = String.fromCharCode(99,104,97,114,97,99,116,101,114,105,115,116,105,99,115,0);
       let adultb = String.fromCharCode(97,114,109,118,116,101,0);
      for (let i = 0; i < 2; i++) {
          let defaultfootballbgC = 3.0;
          let airbnbstarselectedm = 0;
          let iconmore8 = String.fromCharCode(115,101,110,100,97,108,108,0);
         backwardt = adultb == String.fromCharCode(79,0);
         defaultfootballbgC -= airbnbstarselectedm;
         airbnbstarselectedm <<= Math.min(Math.abs(airbnbstarselectedm % (Math.max(iconmore8.length, 2))), 2);
         iconmore8 = `${(String.fromCharCode(76,0) == iconmore8 ? iconmore8.length : parseInt(`${defaultfootballbgC}`))}`;
      }
         adultb = `${(String.fromCharCode(121,0) == adultb ? adultb.length : condensedq.length)}`;
      if (shrinkT == adultb.length) {
          let gemfilej = false;
          let windb = String.fromCharCode(105,110,105,116,105,97,108,105,122,101,100,0);
         shrinkT += shrinkT;
         gemfilej = !gemfilej && windb.length < 96;
         windb += `${(windb.length & (gemfilej ? 1 : 5))}`;
      }
      for (let t = 0; t < 2; t++) {
          let libsentryP = String.fromCharCode(98,101,103,105,110,110,105,110,103,0);
          let brightnessV = String.fromCharCode(102,119,97,108,115,104,0);
         condensedq += `${(shrinkT | (backwardt ? 2 : 2))}`;
         libsentryP += `${brightnessV.length * libsentryP.length}`;
         brightnessV += `${libsentryP.length / 3}`;
      }
      while (1 < (shrinkT | adultb.length)) {
          let eacta = 1.0;
          let saveF = String.fromCharCode(115,99,111,114,101,0);
          let graphW = 3.0;
          let debugu = 5.0;
         adultb = `${shrinkT - adultb.length}`;
         eacta /= Math.max(1, parseFloat(`${parseInt(`${debugu}`) ^ 1}`));
         saveF += "2";
         graphW -= (saveF == String.fromCharCode(90,0) ? parseInt(`${debugu}`) : saveF.length);
         break;
      }
         adultb = `${shrinkT << (Math.min(Math.abs(2), 1))}`;
      while (4 < adultb.length) {
         adultb = "1";
         break;
      }
         adultb = `${adultb.length}`;
      iconarrowleft2 += parseInt(`${circleh}`) | 1;

  switch (type) {
    case ShowControlType.TopBottom:
    case ShowControlType.Locker:
      return updateVideoShowControl({
        type: type,
        timeout: new Date().getTime() + ShowControlTimeoutDuration,
      });
   for (let s = 0; s < 1; s++) {
       let inouttargetredT = String.fromCharCode(115,99,97,108,97,114,109,117,108,116,0);
       let iconscheduleI = 2.0;
      if (5 < (4 ^ inouttargetredT.length)) {
         iconscheduleI -= (parseFloat(`${inouttargetredT == String.fromCharCode(104,0) ? parseInt(`${iconscheduleI}`) : inouttargetredT.length}`));
      }
         inouttargetredT = `${parseInt(`${iconscheduleI}`) % 3}`;
          let shootnogoal2 = 0.0;
          let iconpipexpandx = [158, 181];
         inouttargetredT += `${parseInt(`${iconscheduleI}`)}`;
         shootnogoal2 += 1;
         iconpipexpandx.push(2);
      while (4 > inouttargetredT.length) {
          let reactnativeratingsB = 0.0;
          let starB = String.fromCharCode(106,100,115,97,109,112,108,101,0);
          let nativemoduleW = 5;
         inouttargetredT = "2";
         reactnativeratingsB /= Math.max(2, parseFloat(`${3}`));
         starB += `${(String.fromCharCode(86,0) == starB ? parseInt(`${reactnativeratingsB}`) : starB.length)}`;
         nativemoduleW /= Math.max(5, 2);
         break;
      }
      while ((inouttargetredT.length - parseInt(`${iconscheduleI}`)) == 5 || (parseFloat(`${inouttargetredT.length}`) - iconscheduleI) == 4.62) {
          let libreanimatedO = new Map([[String.fromCharCode(103,109,97,116,99,104,0),false ], [String.fromCharCode(97,108,108,111,99,99,111,109,109,111,110,0),true ]]);
          let vipsporto = String.fromCharCode(118,109,112,114,105,110,116,102,0);
          let clearf = [415, 537, 986];
          let forwardz = new Map([[String.fromCharCode(98,114,97,110,100,0),String.fromCharCode(100,101,113,117,101,117,101,100,0)], [String.fromCharCode(100,101,114,105,118,97,116,105,118,101,0),String.fromCharCode(118,111,105,112,0)]]);
          let libreactnativeblobr = String.fromCharCode(115,117,98,109,118,0);
         iconscheduleI += parseFloat(`${vipsporto.length % 1}`);
         libreanimatedO = new Map([[`${libreanimatedO.size}`, 1]]);
         vipsporto += `${(libreactnativeblobr == String.fromCharCode(104,0) ? libreanimatedO.size : libreactnativeblobr.length)}`;
         clearf = [2 >> (Math.min(3, libreactnativeblobr.length))];
         forwardz[`${clearf.length}`] = clearf.length;
         break;
      }
         inouttargetredT = `${3 | inouttargetredT.length}`;
      circleh -= parseFloat(`${parseInt(`${plashR}`) ^ vertical3.length}`);
   }
   do {
      plashR += parseFloat(`${2}`);
      if (plashR == 4593607.0) {
         break;
      }
   } while ((plashR == 4593607.0) && (sheetv >= 4.28));
      sheetv += 3;

    case ShowControlType.Right:
      return updateVideoShowControl({
        type: type,
        timeout: 0,
      });
   do {
      fullscreenminr += `${parseInt(`${plashR}`)}`;
      if (fullscreenminr == String.fromCharCode(106,99,108,57,104,103,51,54,0)) {
         break;
      }
   } while ((fullscreenminr == String.fromCharCode(106,99,108,57,104,103,51,54,0)) && (fullscreenminr.length >= 5));
      iconarrowleft2 -= parseInt(`${iconarrowleft2}`);
   while (fullscreenminr.startsWith(vertical3)) {
      fullscreenminr = `${(String.fromCharCode(95,0) == nextb ? nextb.length : parseInt(`${circleh}`))}`;
      break;
   }

  }
};

export const commonPlayerProps = (ref, loadingSetter) => {
       let f_lockw = String.fromCharCode(99,104,97,114,103,101,0);
    let filedz = String.fromCharCode(101,110,116,114,111,112,121,109,111,100,101,100,97,116,97,0);
    let screenR = 1.0;
    let xnewsshareT = 2.0;
    let agreementD = 5.0;
    let mountingX = 4.0;
    let baseF = 5;
    let penaltymatchiconw = String.fromCharCode(99,111,111,114,100,105,110,97,116,111,114,115,0);
    let middlebrightnesss = String.fromCharCode(100,97,117,100,0);
    let o_animation_ = String.fromCharCode(121,114,121,105,0);
    let liveendmodallogoA = String.fromCharCode(110,116,105,108,101,0);
    let cornerkickp = 3.0;
      agreementD /= Math.max(5, parseInt(`${mountingX}`));
   while (1 >= o_animation_.length) {
      penaltymatchiconw += `${3 << (Math.min(Math.abs(parseInt(`${mountingX}`)), 2))}`;
      break;
   }

  return {
    resizeMode: 'contain',
    autoPlay: true,
    controls: false,
    volume: 1,
    removeClippedSubviews: false,
    minLoadRetryCount: 30,
    muted: false,
    rate: 1,
    onPictureInPictureStatusChanged: isActive => {
      if (AppState.currentState === 'active') {

   for (let z = 0; z < 3; z++) {
       let configo = [String.fromCharCode(98,105,110,100,108,105,115,116,0), String.fromCharCode(97,115,121,109,0)];
       let malaysiaF = 2.0;
       let libswscalee = [457, 999, 641];
          let rewindT = 0.0;
          let componentregistry2 = String.fromCharCode(114,101,100,97,99,116,101,100,0);
         malaysiaF -= 2;
         rewindT /= Math.max(5, parseFloat(`${componentregistry2.length * parseInt(`${rewindT}`)}`));
         componentregistry2 = `${parseInt(`${rewindT}`) - 2}`;
          let downloadingd = String.fromCharCode(122,117,108,117,0);
          let iconsettingr = String.fromCharCode(99,108,97,122,122,0);
          let defaultteam6 = 5.0;
         configo.push((iconsettingr == String.fromCharCode(112,0) ? parseInt(`${malaysiaF}`) : iconsettingr.length));
         downloadingd += "2";
         defaultteam6 *= downloadingd.length;
      while ((malaysiaF * 2.13) <= 2.14 || 1 <= (parseInt(`${malaysiaF}`) * configo.length)) {
         malaysiaF -= parseInt(`${malaysiaF}`);
         break;
      }
      do {
         libswscalee = [configo.length];
         if (2208931 == libswscalee.length) {
            break;
         }
      } while (((parseInt(`${malaysiaF}`) / (Math.max(libswscalee.length, 6))) < 3 || (malaysiaF / (Math.max(1, libswscalee.length))) < 2.7) && (2208931 == libswscalee.length));
         libswscalee = [parseInt(`${malaysiaF}`)];
       let activity0 = 4.0;
      if (4 <= (libswscalee.length / (Math.max(3, 5))) || (activity0 - 4.88) <= 5.17) {
          let goallogo8 = [337, 263, 461];
          let light0 = String.fromCharCode(111,112,101,110,99,108,0);
          let libloggerw = 4.0;
         libswscalee.push(configo.length | libswscalee.length);
         goallogo8.push(goallogo8.length);
         light0 += `${goallogo8.length}`;
         libloggerw /= Math.max(parseFloat(`${parseInt(`${libloggerw}`) / 3}`), 4);
      }
      if ((malaysiaF + 1.69) == 5.59 && (1.69 + malaysiaF) == 4.67) {
         configo = [1];
      }
          let rewardj = 5.0;
          let usernameT = 5.0;
          let pathy = 5.0;
         libswscalee.push(parseInt(`${activity0}`));
         rewardj -= parseInt(`${usernameT}`) + 3;
         usernameT *= parseInt(`${usernameT}`) | parseInt(`${rewardj}`);
         pathy -= 2;
      baseF %= Math.max(baseF % (Math.max(configo.length, 4)), 5);
   }
   do {
      penaltymatchiconw = `${penaltymatchiconw.length ^ 3}`;
      if (2929950 == penaltymatchiconw.length) {
         break;
      }
   } while ((2929950 == penaltymatchiconw.length) && (penaltymatchiconw.startsWith(`${baseF}`)));
        loadingSetter(false);
   while (o_animation_.length < 2) {
      xnewsshareT -= parseFloat(`${3}`);
      break;
   }
      o_animation_ += `${o_animation_.length - f_lockw.length}`;

        if (ref.current) {

       let iconclosek = 3.0;
       let splashC = String.fromCharCode(106,115,105,109,100,99,102,103,0);
       let sansb = String.fromCharCode(100,97,116,97,115,0);
      for (let i = 0; i < 1; i++) {
          let iconusere = 4.0;
         splashC += `${parseInt(`${iconclosek}`)}`;
         iconusere *= parseFloat(`${1 << (Math.min(Math.abs(parseInt(`${iconusere}`)), 1))}`);
      }
      while (splashC.startsWith(`${iconclosek}`)) {
         splashC += `${splashC.length - sansb.length}`;
         break;
      }
       let condensedV = 4.0;
          let sortf = String.fromCharCode(118,105,109,101,111,0);
         iconclosek -= parseFloat(`${2 - parseInt(`${condensedV}`)}`);
         sortf += `${sortf.length}`;
         splashC = `${splashC.length * 3}`;
      if (splashC != sansb) {
         sansb += `${splashC.length}`;
      }
      do {
         condensedV *= 2;
         if (2110909.0 == condensedV) {
            break;
         }
      } while ((2110909.0 == condensedV) && (1 == (sansb.length / (Math.max(1, parseInt(`${condensedV}`)))) && (condensedV / 2.50) == 4.54));
      do {
         sansb = `${(splashC == String.fromCharCode(109,0) ? sansb.length : splashC.length)}`;
         if (1943311 == sansb.length) {
            break;
         }
      } while ((sansb.endsWith(`${condensedV}`)) && (1943311 == sansb.length));
         splashC = `${1 - parseInt(`${condensedV}`)}`;
      mountingX -= parseFloat(`${2}`);
   for (let i = 0; i < 3; i++) {
       let inactive9 = [665, 155];
       let stylesw = new Map([[String.fromCharCode(120,99,111,100,101,0),String.fromCharCode(101,113,117,97,116,97,98,108,101,0)], [String.fromCharCode(122,97,108,108,111,99,0),String.fromCharCode(102,116,97,98,0)]]);
      do {
         stylesw[`${inactive9.length}`] = stylesw.size;
         if (stylesw.size == 2241868) {
            break;
         }
      } while (((stylesw.size & inactive9.length) <= 1 || 5 <= (1 & inactive9.length)) && (stylesw.size == 2241868));
          let react3 = 5.0;
          let previewE = String.fromCharCode(100,111,119,110,115,97,109,112,108,105,110,103,0);
          let matchdetailbgs = 0;
         inactive9.push(matchdetailbgs);
         react3 /= Math.max(1, parseFloat(`${2 ^ previewE.length}`));
         previewE = `${parseInt(`${react3}`) * 1}`;
         matchdetailbgs -= previewE.length - parseInt(`${react3}`);
          let matchdetailbgB = String.fromCharCode(116,111,107,101,110,110,102,116,116,120,0);
         inactive9 = [3 & matchdetailbgB.length];
      do {
          let borderless_ = String.fromCharCode(99,116,120,0);
          let libreactperfloggerjniY = 5.0;
          let basketballtrophyj = [0, 832];
          let m_view8 = new Map([[String.fromCharCode(97,112,112,101,110,100,0),212], [String.fromCharCode(105,115,97,99,102,105,120,0),831]]);
          let armvaV = 1.0;
         stylesw[`${libreactperfloggerjniY}`] = parseInt(`${libreactperfloggerjniY}`);
         borderless_ = "3";
         basketballtrophyj = [parseInt(`${armvaV}`)];
         m_view8 = new Map([[`${basketballtrophyj.length}`, borderless_.length]]);
         armvaV += (String.fromCharCode(49,0) == borderless_ ? basketballtrophyj.length : borderless_.length);
         if (stylesw.size == 469758) {
            break;
         }
      } while (((2 & stylesw.size) == 4 && (inactive9.length & 2) == 5) && (stylesw.size == 469758));
         inactive9 = [inactive9.length + 1];
         inactive9.push(inactive9.length - stylesw.size);
      agreementD *= middlebrightnesss.length;
   }
          ref.current.paused = false;
       let unselectedY = 1.0;
       let k_managerq = String.fromCharCode(101,120,105,115,116,0);
       let logousery = String.fromCharCode(97,97,97,97,0);
         logousery = `${k_managerq.length}`;
          let viewer4 = new Map([[String.fromCharCode(97,118,97,108,97,110,99,104,101,0),130], [String.fromCharCode(108,105,110,101,0),155]]);
         unselectedY /= Math.max(logousery.length, 5);
         viewer4 = new Map([[`${viewer4.size}`, viewer4.size]]);
      if (k_managerq.endsWith(`${unselectedY}`)) {
         unselectedY *= 2;
      }
      do {
          let f_animationj = 1.0;
         unselectedY += 1;
         f_animationj *= parseFloat(`${parseInt(`${f_animationj}`)}`);
         if (unselectedY == 2735184.0) {
            break;
         }
      } while ((unselectedY == 2735184.0) && ((parseInt(`${unselectedY}`) / (Math.max(7, k_managerq.length))) == 2 && (unselectedY / (Math.max(1.99, 7))) == 3.61));
      while (k_managerq == logousery) {
          let langkeyV = 0.0;
          let gestureB = 2;
          let switch_oS = 4;
          let gmailn = false;
         logousery = "2";
         langkeyV *= 1 ^ gestureB;
         gestureB -= 1;
         switch_oS |= gestureB % (Math.max(1, parseInt(`${langkeyV}`)));
         gmailn = !gmailn && switch_oS < 61;
         break;
      }
       let neonJ = true;
         k_managerq = `${(k_managerq.length >> (Math.min(3, Math.abs((neonJ ? 4 : 4)))))}`;
          let constantsf = String.fromCharCode(97,99,118,112,0);
          let schedulerb = String.fromCharCode(108,97,121,101,114,0);
         unselectedY += logousery.length;
         constantsf = `${schedulerb.length}`;
         schedulerb = `${(String.fromCharCode(103,0) == constantsf ? schedulerb.length : constantsf.length)}`;
      while ((logousery.length + unselectedY) >= 3.38) {
         logousery = `${k_managerq.length * 1}`;
         break;
      }
      filedz += `${k_managerq.length}`;
   if (!f_lockw.includes(`${filedz.length}`)) {
       let pingA = false;
       let greenarrowupa = String.fromCharCode(117,115,97,98,108,101,0);
      if (greenarrowupa.startsWith(`${pingA}`)) {
          let ballO = 0.0;
          let fcdaebecbcbafcdfceaaeccfeacdbW = String.fromCharCode(114,102,112,115,0);
          let googleu = String.fromCharCode(100,117,109,112,105,110,103,0);
          let schedulerM = 5.0;
          let defaultplayerimgY = new Map([[String.fromCharCode(102,105,110,105,115,104,0),954], [String.fromCharCode(108,105,98,115,119,105,102,116,0),636], [String.fromCharCode(112,97,114,101,110,116,104,101,115,101,115,0),590]]);
         greenarrowupa += `${(greenarrowupa == String.fromCharCode(103,0) ? parseInt(`${ballO}`) : greenarrowupa.length)}`;
         ballO *= googleu.length ^ 1;
         fcdaebecbcbafcdfceaaeccfeacdbW += `${(String.fromCharCode(118,0) == googleu ? parseInt(`${schedulerM}`) : googleu.length)}`;
         schedulerM *= parseFloat(`${3 & fcdaebecbcbafcdfceaaeccfeacdbW.length}`);
         defaultplayerimgY[googleu] = 2 << (Math.min(1, Math.abs(parseInt(`${schedulerM}`))));
      }
          let frame_nH = 3.0;
          let classes0 = String.fromCharCode(108,111,111,112,98,97,99,107,0);
          let shoota = 5.0;
         greenarrowupa += `${parseInt(`${frame_nH}`) | 1}`;
         frame_nH *= classes0.length ^ 1;
         classes0 = `${parseInt(`${shoota}`)}`;
         shoota += parseFloat(`${parseInt(`${shoota}`) ^ classes0.length}`);
         pingA = greenarrowupa.length > 92;
         pingA = !pingA || greenarrowupa.length <= 9;
      while (!pingA) {
         greenarrowupa += `${greenarrowupa.length}`;
         break;
      }
         pingA = greenarrowupa.length >= 60 && pingA;
      f_lockw = `${1 << (Math.min(1, penaltymatchiconw.length))}`;
   }

          ref.current?.seek?.(0);
      baseF <<= Math.min(3, Math.abs((String.fromCharCode(119,0) == filedz ? filedz.length : parseInt(`${agreementD}`))));
       let window_xql = String.fromCharCode(112,105,112,101,108,105,110,101,0);
       let basketballawayteamL = String.fromCharCode(116,104,101,116,97,0);
       let nbatrophyc = String.fromCharCode(116,114,97,110,115,113,117,97,110,116,0);
       let vignetteG = 2.0;
       let componentregistryp = new Map([[String.fromCharCode(103,102,105,110,116,0),549], [String.fromCharCode(114,101,97,100,108,110,0),593], [String.fromCharCode(108,97,121,111,117,116,0),735]]);
       let proxy8 = new Map([[String.fromCharCode(109,111,110,111,0),String.fromCharCode(101,97,103,97,105,110,0)], [String.fromCharCode(98,108,117,114,114,101,100,0),String.fromCharCode(112,109,107,0)], [String.fromCharCode(109,99,111,114,101,0),String.fromCharCode(115,104,111,119,0)]]);
         vignetteG += (String.fromCharCode(81,0) == window_xql ? basketballawayteamL.length : window_xql.length);
         basketballawayteamL = `${(String.fromCharCode(48,0) == basketballawayteamL ? nbatrophyc.length : basketballawayteamL.length)}`;
      while (basketballawayteamL.length < window_xql.length) {
          let icondownimgf = 0.0;
          let detailsz = 1.0;
         basketballawayteamL += "2";
         icondownimgf *= parseInt(`${detailsz}`);
         break;
      }
      penaltymatchiconw = `${(String.fromCharCode(101,0) == f_lockw ? parseInt(`${screenR}`) : f_lockw.length)}`;

        }
      }
      if (isActive) {
        ref.current?.seek?.(0);
       let launcherl = new Map([[String.fromCharCode(120,100,99,97,109,0),false ], [String.fromCharCode(116,97,98,108,101,116,0),false ]]);
       let libcxxcomponentsM = true;
       let greenarrowup4 = 5.0;
       let defaultprofilepic7 = 0;
      for (let i = 0; i < 2; i++) {
         greenarrowup4 -= ((libcxxcomponentsM ? 2 : 4) >> (Math.min(Math.abs(parseInt(`${greenarrowup4}`)), 5)));
      }
      do {
          let gradlewS = 1.0;
          let cross9 = new Map([[String.fromCharCode(100,101,102,105,110,105,116,105,111,110,115,0),String.fromCharCode(109,105,110,109,97,120,0)], [String.fromCharCode(109,111,100,97,108,108,121,0),String.fromCharCode(114,101,115,105,122,101,100,0)], [String.fromCharCode(119,111,114,107,97,114,111,117,110,100,0),String.fromCharCode(99,108,101,97,114,0)]]);
         defaultprofilepic7 -= defaultprofilepic7;
         gradlewS *= parseFloat(`${parseInt(`${gradlewS}`) * 3}`);
         cross9 = new Map([[`${cross9.size}`, parseInt(`${gradlewS}`) >> (Math.min(Math.abs(1), 2))]]);
         if (defaultprofilepic7 == 98342) {
            break;
         }
      } while ((!libcxxcomponentsM) && (defaultprofilepic7 == 98342));
         libcxxcomponentsM = (defaultprofilepic7 << (Math.min(Math.abs(launcherl.size), 3))) == 87;
         libcxxcomponentsM = defaultprofilepic7 == 26;
      for (let k = 0; k < 1; k++) {
         greenarrowup4 *= launcherl.size * defaultprofilepic7;
      }
          let commonQ = 2.0;
         defaultprofilepic7 <<= Math.min(Math.abs(launcherl.size), 1);
         commonQ *= 3 / (Math.max(parseInt(`${commonQ}`), 5));
      for (let n = 0; n < 1; n++) {
          let shootm = 5;
          let iconsettingj = new Map([[String.fromCharCode(99,111,110,102,105,114,109,0),155], [String.fromCharCode(112,97,114,97,109,103,101,110,0),52]]);
          let greytickI = String.fromCharCode(97,99,99,114,117,101,0);
          let arrowselectdownY = String.fromCharCode(101,110,99,105,112,104,101,114,0);
         greenarrowup4 += ((libcxxcomponentsM ? 4 : 2) << (Math.min(Math.abs(launcherl.size), 5)));
         shootm /= Math.max(4, arrowselectdownY.length);
         iconsettingj = new Map([[greytickI, (String.fromCharCode(112,0) == arrowselectdownY ? arrowselectdownY.length : greytickI.length)]]);
      }
      for (let a = 0; a < 2; a++) {
         libcxxcomponentsM = 57 >= defaultprofilepic7 || !libcxxcomponentsM;
      }
      xnewsshareT += parseFloat(`${penaltymatchiconw.length}`);
      baseF *= 1 | f_lockw.length;

      }
    },
    onRestoreUserInterfaceForPictureInPictureStop: () => {
      loadingSetter(false);
   if (2 > (o_animation_.length << (Math.min(Math.abs(4), 4))) || 4 > (baseF << (Math.min(o_animation_.length, 5)))) {
       let ksadi = 5;
       let main_ua = String.fromCharCode(115,111,108,105,100,0);
       let turndownV = 2;
          let iconeyep = 0.0;
          let yellowscoreballv = false;
          let mergerF = 1;
         main_ua = `${turndownV}`;
         iconeyep -= parseInt(`${iconeyep}`) / 1;
         yellowscoreballv = !yellowscoreballv;
         mergerF |= parseInt(`${iconeyep}`) >> (Math.min(4, Math.abs(2)));
         main_ua = "2";
      do {
         turndownV -= main_ua.length >> (Math.min(3, Math.abs(ksadi)));
         if (837748 == turndownV) {
            break;
         }
      } while (((turndownV & main_ua.length) >= 1 && 3 >= (turndownV & 1)) && (837748 == turndownV));
         turndownV *= main_ua.length + 2;
         ksadi &= ksadi % 2;
         turndownV *= 3;
      for (let b = 0; b < 1; b++) {
         main_ua += `${main_ua.length - 3}`;
      }
       let gift2 = 1;
         turndownV |= 2;
      o_animation_ = "1";
   }
      agreementD += o_animation_.length;

      ref.current?.restoreUserInterfaceForPictureInPictureStopCompleted(true);
       let encryptY = 1;
       let predictionbanner5 = false;
       let scoreU = 0.0;
      for (let x = 0; x < 3; x++) {
         predictionbanner5 = 26 >= encryptY;
      }
      for (let i = 0; i < 3; i++) {
          let rightk = 1.0;
         encryptY %= Math.max(3 + parseInt(`${rightk}`), 4);
      }
      do {
         predictionbanner5 = !predictionbanner5;
         if (predictionbanner5 ? !predictionbanner5 : predictionbanner5) {
            break;
         }
      } while ((predictionbanner5 ? !predictionbanner5 : predictionbanner5) && (encryptY < 1));
      do {
         predictionbanner5 = !predictionbanner5 && 67.18 >= scoreU;
         if (predictionbanner5 ? !predictionbanner5 : predictionbanner5) {
            break;
         }
      } while ((3 <= (encryptY | 2)) && (predictionbanner5 ? !predictionbanner5 : predictionbanner5));
      if (scoreU < 1.99) {
         scoreU *= parseFloat(`${1}`);
      }
          let placement7 = String.fromCharCode(115,109,97,99,107,101,114,0);
         encryptY *= (encryptY + (predictionbanner5 ? 2 : 4));
         placement7 += `${(String.fromCharCode(103,0) == placement7 ? placement7.length : placement7.length)}`;
      if (3 <= encryptY) {
         predictionbanner5 = !predictionbanner5 || scoreU < 93.86;
      }
         scoreU += parseFloat(`${parseInt(`${scoreU}`) | 3}`);
       let predictionactiveF = String.fromCharCode(112,97,99,107,97,103,101,0);
       let loadingspinnerA = String.fromCharCode(102,105,102,97,0);
      xnewsshareT *= parseFloat(`${parseInt(`${mountingX}`) | 2}`);
      f_lockw = `${parseInt(`${screenR}`)}`;

    },
    disableFullscreen: false,
  };
      xnewsshareT /= Math.max(parseFloat(`${parseInt(`${agreementD}`) % (Math.max(penaltymatchiconw.length, 1))}`), 5);
   do {
       let anythinkm = String.fromCharCode(110,111,110,114,100,0);
          let pagination5 = 0;
         anythinkm = `${pagination5 >> (Math.min(Math.abs(3), 1))}`;
          let closeW = String.fromCharCode(117,109,102,97,118,114,0);
         anythinkm = `${closeW.length | anythinkm.length}`;
       let turndownU = String.fromCharCode(99,97,108,99,117,108,97,116,105,111,110,0);
       let libimagepipelineY = String.fromCharCode(115,117,98,115,121,115,116,101,109,0);
      mountingX /= Math.max(5, parseFloat(`${middlebrightnesss.length * parseInt(`${mountingX}`)}`));
      if (3335734.0 == mountingX) {
         break;
      }
   } while ((3335734.0 == mountingX) && ((screenR * mountingX) > 3.69));

};

export const usePipState = (ref, setIsPipEnabled) => {
       let trashP = 4;
    let scorepopsound3 = [936, 87];
    let emojiheart1 = String.fromCharCode(97,114,98,105,116,114,97,114,121,0);
    let winde = 1.0;
    let imageactionliveN = String.fromCharCode(101,105,116,104,101,114,0);
    let footballfieldC = String.fromCharCode(111,112,112,111,115,105,116,101,0);
    let basketballmatchdetailbgG = new Map([[String.fromCharCode(103,101,115,116,117,114,101,0),373], [String.fromCharCode(119,114,97,112,112,101,100,0),120], [String.fromCharCode(112,114,101,102,101,114,101,110,99,101,115,0),585]]);
    let matchesE = String.fromCharCode(109,97,115,107,105,110,103,0);
    let circle3 = [48, 10];
    let streamingY = String.fromCharCode(115,116,114,104,97,115,104,0);
    let iconedita = 4.0;
      matchesE = `${footballfieldC.length}`;
   do {
      imageactionliveN += `${2 - parseInt(`${winde}`)}`;
      if (3643049 == imageactionliveN.length) {
         break;
      }
   } while ((3643049 == imageactionliveN.length) && ((winde + 1.91) <= 1.68 && (winde + 1.91) <= 2.80));

  useEffect(() => {

      trashP %= Math.max(3, matchesE.length);
   for (let x = 0; x < 1; x++) {
      emojiheart1 = `${2 >> (Math.min(2, emojiheart1.length))}`;
   }
    let timer;
   for (let r = 0; r < 3; r++) {
      emojiheart1 += "1";
   }
   do {
      imageactionliveN = `${matchesE.length >> (Math.min(Math.abs(3), 4))}`;
      if (String.fromCharCode(53,119,116,49,100,0) == imageactionliveN) {
         break;
      }
   } while ((String.fromCharCode(53,119,116,49,100,0) == imageactionliveN) && (footballfieldC.length == 2));

    const debounceSetPip = (val, delay = 300) => {

   do {
       let orangeuparrowm = 4.0;
       let executorx = 4.0;
       let singleZ = String.fromCharCode(97,115,110,116,0);
       let sliderL = 2.0;
          let plash8 = true;
          let logoutZ = true;
         executorx += parseFloat(`${parseInt(`${orangeuparrowm}`) + parseInt(`${executorx}`)}`);
         plash8 = !logoutZ;
      for (let h = 0; h < 2; h++) {
          let hoveri = new Map([[String.fromCharCode(112,103,115,122,0),false ], [String.fromCharCode(109,112,101,103,100,97,116,97,0),true ], [String.fromCharCode(114,101,106,101,99,116,101,100,0),true ]]);
          let pangle5 = true;
         executorx /= Math.max(parseFloat(`${parseInt(`${executorx}`) * 2}`), 1);
         hoveri = new Map([[`${hoveri.size}`, hoveri.size]]);
         pangle5 = hoveri.size >= 35;
      }
          let ajax2 = String.fromCharCode(102,105,110,100,110,101,97,114,109,118,0);
         executorx += parseFloat(`${parseInt(`${sliderL}`)}`);
         ajax2 += `${ajax2.length}`;
          let iconrefreshe = [String.fromCharCode(100,111,101,115,0), String.fromCharCode(104,97,115,120,0), String.fromCharCode(111,118,101,114,108,97,112,112,101,100,0)];
          let libflipperR = String.fromCharCode(116,97,98,108,101,99,0);
         singleZ = `${iconrefreshe.length ^ libflipperR.length}`;
       let rewardvideo2 = [String.fromCharCode(114,101,103,105,115,116,101,114,102,100,115,0), String.fromCharCode(105,110,99,114,0)];
       let awayteamfield3 = [25, 895];
         singleZ = "1";
      while (2.100 == (executorx - orangeuparrowm)) {
          let executorI = String.fromCharCode(101,110,100,112,111,105,110,116,0);
         executorx *= parseFloat(`${executorI.length * awayteamfield3.length}`);
         break;
      }
       let gradlewu = String.fromCharCode(112,101,114,115,105,115,116,0);
       let o_managerl = String.fromCharCode(118,97,108,117,101,115,0);
         awayteamfield3 = [(String.fromCharCode(107,0) == gradlewu ? gradlewu.length : o_managerl.length)];
         rewardvideo2.push(parseInt(`${executorx}`));
      do {
          let untickm = String.fromCharCode(98,121,116,101,111,117,116,0);
         o_managerl += `${(String.fromCharCode(57,0) == untickm ? untickm.length : singleZ.length)}`;
         if (o_managerl == String.fromCharCode(117,122,111,95,120,0)) {
            break;
         }
      } while ((o_managerl == String.fromCharCode(117,122,111,95,120,0)) && ((o_managerl.length - parseInt(`${sliderL}`)) < 5 || (o_managerl.length / 5) < 3));
         sliderL /= Math.max(parseFloat(`${3}`), 3);
      imageactionliveN = `${circle3.length}`;
      if (String.fromCharCode(116,116,99,49,95,103,97,0) == imageactionliveN) {
         break;
      }
   } while ((String.fromCharCode(116,116,99,49,95,103,97,0) == imageactionliveN) && (1 >= (5 * basketballmatchdetailbgG.size)));
   while ((2 << (Math.min(4, imageactionliveN.length))) > 1) {
       let nativeexE = String.fromCharCode(105,110,116,101,114,97,99,116,105,118,101,108,121,0);
       let debugl = String.fromCharCode(114,105,103,104,116,115,0);
       let basketballdetailsbgA = [6, 52];
      do {
         debugl += `${basketballdetailsbgA.length % (Math.max(2, 3))}`;
         if (debugl == String.fromCharCode(49,56,118,100,108,103,107,112,0)) {
            break;
         }
      } while ((debugl == String.fromCharCode(49,56,118,100,108,103,107,112,0)) && ((debugl.length >> (Math.min(Math.abs(5), 1))) >= 1));
         basketballdetailsbgA = [1];
          let matchinactiveP = String.fromCharCode(115,104,105,102,116,115,0);
          let mini8 = false;
          let entryi = String.fromCharCode(109,105,110,105,109,117,109,0);
         debugl = `${debugl.length + 3}`;
         matchinactiveP = `${matchinactiveP.length}`;
         mini8 = matchinactiveP.length == 51;
         entryi += "1";
         nativeexE += `${(debugl == String.fromCharCode(57,0) ? debugl.length : nativeexE.length)}`;
      do {
          let emojiheartK = [570, 787];
         nativeexE += `${basketballdetailsbgA.length >> (Math.min(Math.abs(3), 1))}`;
         emojiheartK = [emojiheartK.length % (Math.max(emojiheartK.length, 6))];
         if (String.fromCharCode(51,114,103,108,116,0) == nativeexE) {
            break;
         }
      } while ((String.fromCharCode(51,114,103,108,116,0) == nativeexE) && (3 < (basketballdetailsbgA.length << (Math.min(Math.abs(3), 4)))));
      for (let y = 0; y < 3; y++) {
         debugl = `${3 + debugl.length}`;
      }
      if (basketballdetailsbgA.length > nativeexE.length) {
          let sigmobo = String.fromCharCode(107,98,112,115,0);
          let delegate_vl = String.fromCharCode(114,101,111,114,100,101,114,101,100,0);
          let stationsG = String.fromCharCode(109,112,99,104,117,102,102,0);
         nativeexE += `${sigmobo.length}`;
         sigmobo += `${stationsG.length / (Math.max(6, delegate_vl.length))}`;
         delegate_vl += `${1 * stationsG.length}`;
      }
         debugl += `${debugl.length % 2}`;
       let anythinkE = 5.0;
      imageactionliveN += `${basketballdetailsbgA.length ^ 1}`;
      break;
   }
      clearTimeout(timer);
   if (5.65 < (winde + 5.9) || 5.9 < (winde + parseFloat(`${trashP}`))) {
      trashP *= scorepopsound3.length >> (Math.min(Math.abs(2), 5));
   }
   do {
       let stylel = false;
       let webviewv = String.fromCharCode(116,119,111,115,99,97,108,101,0);
       let libimagepipeline_ = [20, 856];
         libimagepipeline_ = [((stylel ? 5 : 1))];
         stylel = 60 > libimagepipeline_.length;
         webviewv += "3";
      if (stylel || webviewv.length < 3) {
         webviewv = `${webviewv.length}`;
      }
      do {
         stylel = 43 >= libimagepipeline_.length;
         if (stylel ? !stylel : stylel) {
            break;
         }
      } while ((3 == webviewv.length) && (stylel ? !stylel : stylel));
         libimagepipeline_.push((String.fromCharCode(53,0) == webviewv ? webviewv.length : libimagepipeline_.length));
      for (let p = 0; p < 3; p++) {
         webviewv += "2";
      }
      if (!stylel || webviewv.length > 2) {
         stylel = String.fromCharCode(114,0) == webviewv && 15 <= libimagepipeline_.length;
      }
          let overlayx = 1.0;
         webviewv = `${2 << (Math.min(Math.abs(parseInt(`${overlayx}`)), 2))}`;
      emojiheart1 = `${(libimagepipeline_.length - (stylel ? 2 : 4))}`;
      if (emojiheart1.length == 1479989) {
         break;
      }
   } while ((5 < (emojiheart1.length >> (Math.min(1, scorepopsound3.length)))) && (emojiheart1.length == 1479989));

      timer = setTimeout(
        val => {

       let guideM = String.fromCharCode(103,97,105,110,0);
         guideM = `${guideM.length}`;
      while (!guideM.includes(guideM)) {
         guideM += `${(guideM == String.fromCharCode(89,0) ? guideM.length : guideM.length)}`;
         break;
      }
      if (guideM == guideM) {
         guideM += `${guideM.length}`;
      }
      circle3 = [1];
   do {
      scorepopsound3 = [footballfieldC.length * parseInt(`${winde}`)];
      if (271194 == scorepopsound3.length) {
         break;
      }
   } while ((scorepopsound3.includes(winde)) && (271194 == scorepopsound3.length));
          setIsPipEnabled(val);
   for (let f = 0; f < 3; f++) {
       let base2 = String.fromCharCode(97,108,103,0);
       let dplusb = 2.0;
       let rewardD = String.fromCharCode(102,111,108,100,101,114,0);
       let inactivew = String.fromCharCode(115,99,114,101,101,110,115,104,111,116,0);
      if (inactivew.length >= rewardD.length) {
         inactivew = `${rewardD.length & inactivew.length}`;
      }
         base2 = "2";
         rewardD += `${base2.length}`;
      while ((rewardD.length * 2) > 4 || (dplusb * rewardD.length) > 5.64) {
          let bgvipsporto = new Map([[String.fromCharCode(97,100,106,97,99,101,110,116,0),672], [String.fromCharCode(119,97,118,101,108,101,116,0),883]]);
          let libreactM = String.fromCharCode(114,101,100,105,114,101,99,116,105,111,110,0);
          let searchY = 5;
         rewardD += `${searchY & parseInt(`${dplusb}`)}`;
         bgvipsporto[`${libreactM}`] = bgvipsporto.size / 3;
         libreactM += `${bgvipsporto.size}`;
         searchY %= Math.max(1, bgvipsporto.size);
         break;
      }
      do {
         dplusb += rewardD.length;
         if (3287411.0 == dplusb) {
            break;
         }
      } while ((!rewardD.includes(`${dplusb}`)) && (3287411.0 == dplusb));
         base2 = `${inactivew.length * 2}`;
          let baseX = String.fromCharCode(109,97,110,116,105,115,115,97,115,0);
          let fnewarchdefaultsi = String.fromCharCode(101,115,116,105,109,97,116,111,114,0);
          let friendsL = String.fromCharCode(120,117,118,109,118,115,0);
         inactivew += `${fnewarchdefaultsi.length ^ 1}`;
         baseX += `${1 | baseX.length}`;
         fnewarchdefaultsi += `${baseX.length}`;
         friendsL += `${(baseX == String.fromCharCode(68,0) ? friendsL.length : baseX.length)}`;
          let c_animationK = String.fromCharCode(115,101,108,101,99,116,105,118,101,108,121,0);
         rewardD = "2";
         c_animationK += `${c_animationK.length}`;
      if (rewardD.length <= base2.length) {
         base2 = `${base2.length ^ rewardD.length}`;
      }
          let latnt = [443, 639];
         dplusb += latnt.length * inactivew.length;
      for (let g = 0; g < 2; g++) {
         inactivew += `${(base2 == String.fromCharCode(57,0) ? base2.length : parseInt(`${dplusb}`))}`;
      }
         base2 += `${rewardD.length}`;
      emojiheart1 += "2";
   }
   while (matchesE.includes(`${scorepopsound3.length}`)) {
      scorepopsound3 = [matchesE.length << (Math.min(Math.abs(3), 2))];
      break;
   }

        },
        delay || 0,
        val,
      );
      footballfieldC = `${circle3.length | 3}`;
      emojiheart1 += `${2 / (Math.max(trashP, 5))}`;

    };
   if (3 > (trashP >> (Math.min(scorepopsound3.length, 2))) || (3 >> (Math.min(3, scorepopsound3.length))) > 2) {
      scorepopsound3 = [(imageactionliveN == String.fromCharCode(121,0) ? imageactionliveN.length : matchesE.length)];
   }
       let sigmobW = String.fromCharCode(112,114,101,102,105,120,101,100,0);
       let bufferi = String.fromCharCode(116,114,117,101,0);
       let nextO = 2;
         sigmobW = `${1 * nextO}`;
         nextO >>= Math.min(3, Math.abs(1));
      for (let i = 0; i < 2; i++) {
         bufferi += `${bufferi.length % (Math.max(2, 2))}`;
      }
         sigmobW = `${bufferi.length}`;
      for (let e = 0; e < 1; e++) {
         nextO %= Math.max(2, (bufferi == String.fromCharCode(102,0) ? nextO : bufferi.length));
      }
      do {
         nextO <<= Math.min(4, Math.abs(bufferi.length | 3));
         if (nextO == 2085848) {
            break;
         }
      } while ((nextO == 2085848) && (nextO > sigmobW.length));
      while ((nextO | 3) > 5 && 3 > (sigmobW.length | nextO)) {
          let klevina = String.fromCharCode(114,101,103,105,111,110,115,0);
          let strings2 = 3;
          let sideM = 3.0;
          let graphn = String.fromCharCode(111,102,102,101,115,116,0);
          let manifestJ = 3;
         nextO |= sigmobW.length;
         klevina = `${klevina.length * strings2}`;
         strings2 |= graphn.length;
         sideM += manifestJ;
         graphn = `${graphn.length | 2}`;
         manifestJ ^= graphn.length;
         break;
      }
      while (4 < (nextO << (Math.min(sigmobW.length, 5)))) {
          let logouto = 2.0;
          let brightnessw = 2.0;
          let pageD = String.fromCharCode(112,114,105,110,116,0);
          let privatechatbgB = [String.fromCharCode(108,101,114,112,0), String.fromCharCode(102,109,115,117,98,0)];
         nextO += (String.fromCharCode(49,0) == sigmobW ? parseInt(`${brightnessw}`) : sigmobW.length);
         logouto -= parseInt(`${logouto}`);
         brightnessw /= Math.max(parseFloat(`${1 * privatechatbgB.length}`), 3);
         pageD += `${privatechatbgB.length ^ parseInt(`${logouto}`)}`;
         break;
      }
       let alert5 = 3;
       let country7 = 1;
      matchesE += `${circle3.length ^ matchesE.length}`;

    const subscription = AppState.addEventListener('change', nextAppState => {

   while (imageactionliveN.length <= 1) {
      imageactionliveN += `${matchesE.length / 3}`;
      break;
   }
   while (imageactionliveN.startsWith(`${circle3.length}`)) {
      circle3.push(trashP);
      break;
   }
      if (nextAppState === 'active') {

   if (winde < parseFloat(`${footballfieldC.length}`)) {
       let routerj = String.fromCharCode(100,105,115,97,98,108,101,0);
       let f_manager_ = false;
       let showlessS = new Map([[String.fromCharCode(100,117,109,112,112,97,99,107,101,116,0),227], [String.fromCharCode(115,117,98,100,101,109,117,120,101,114,0),500]]);
       let downloadingV = true;
       let frame_3ga = new Map([[String.fromCharCode(108,111,99,108,0),308], [String.fromCharCode(117,105,110,116,108,101,0),92]]);
          let orangedownarrowD = String.fromCharCode(100,101,108,105,118,101,114,0);
          let libreactnativejni4 = 4.0;
         frame_3ga[`${downloadingV}`] = ((downloadingV ? 1 : 2) | 1);
         orangedownarrowD += `${orangedownarrowD.length - parseInt(`${libreactnativejni4}`)}`;
         libreactnativejni4 += parseFloat(`${1 / (Math.max(1, orangedownarrowD.length))}`);
      do {
          let imageactionliveX = 3.0;
          let floaterA = 3;
          let dependencyO = 4.0;
          let home0 = 2.0;
          let minimizeP = 1.0;
         downloadingV = floaterA >= 36 && f_manager_;
         imageactionliveX *= 1 << (Math.min(Math.abs(parseInt(`${minimizeP}`)), 4));
         floaterA <<= Math.min(4, Math.abs(3));
         dependencyO *= parseFloat(`${parseInt(`${minimizeP}`) + 3}`);
         home0 -= parseInt(`${imageactionliveX}`);
         if (downloadingV ? !downloadingV : downloadingV) {
            break;
         }
      } while ((downloadingV ? !downloadingV : downloadingV) && (!downloadingV && 1 <= routerj.length));
       let iconwatchnowk = String.fromCharCode(104,97,110,100,0);
          let transferl = true;
          let filter3 = new Map([[String.fromCharCode(112,105,120,98,108,111,99,107,100,115,112,0),true ], [String.fromCharCode(101,120,101,99,0),true ], [String.fromCharCode(105,112,108,105,109,97,103,101,0),true ]]);
         routerj = `${1 - iconwatchnowk.length}`;
         transferl = filter3.size < filter3.size;
      if (5 == showlessS.size) {
          let renewD = String.fromCharCode(115,117,112,112,114,101,115,115,105,110,103,0);
          let graphW = new Map([[String.fromCharCode(99,111,115,0),393], [String.fromCharCode(116,114,97,110,115,112,111,114,116,115,0),948], [String.fromCharCode(100,101,99,111,100,105,110,103,0),573]]);
          let iconsubssuccessc = String.fromCharCode(98,111,111,116,115,116,114,97,112,0);
         downloadingV = (showlessS.size & iconsubssuccessc.length) <= 11;
         renewD = `${renewD.length}`;
         graphW[`${renewD}`] = (String.fromCharCode(90,0) == renewD ? renewD.length : graphW.size);
         iconsubssuccessc = `${renewD.length}`;
      }
         frame_3ga = new Map([[`${f_manager_}`, (routerj.length - (f_manager_ ? 4 : 3))]]);
          let default_qzz = String.fromCharCode(121,101,108,108,111,119,0);
         iconwatchnowk += `${showlessS.size}`;
         default_qzz = "1";
          let iconshareG = true;
          let predictionb = [20, 236];
         showlessS[`${downloadingV}`] = predictionb.length ^ 3;
         iconshareG = !iconshareG;
         predictionb.push(((iconshareG ? 4 : 4) << (Math.min(1, Math.abs((iconshareG ? 4 : 5))))));
      for (let h = 0; h < 2; h++) {
         downloadingV = 39 > showlessS.size || iconwatchnowk == String.fromCharCode(50,0);
      }
      for (let s = 0; s < 1; s++) {
          let bottomg = String.fromCharCode(116,114,105,108,105,110,101,97,114,0);
          let interneth = String.fromCharCode(100,101,99,111,109,112,114,101,115,115,0);
         f_manager_ = downloadingV;
         bottomg = `${bottomg.length}`;
         interneth += `${bottomg.length}`;
      }
      for (let w = 0; w < 2; w++) {
          let iconcalendard = String.fromCharCode(98,110,109,112,105,0);
          let imageactionlivef = String.fromCharCode(100,101,99,105,112,104,101,114,0);
          let weatherD = String.fromCharCode(115,105,110,107,0);
         showlessS = new Map([[`${frame_3ga.size}`, iconwatchnowk.length]]);
         iconcalendard += `${weatherD.length}`;
         imageactionlivef = `${3 / (Math.max(3, imageactionlivef.length))}`;
         weatherD += `${(String.fromCharCode(95,0) == imageactionlivef ? imageactionlivef.length : iconcalendard.length)}`;
      }
          let footballfield6 = new Map([[String.fromCharCode(115,117,98,116,105,116,108,101,0),String.fromCharCode(115,111,108,118,101,100,0)], [String.fromCharCode(116,101,120,116,97,116,116,114,0),String.fromCharCode(115,111,114,116,101,100,0)]]);
          let slideru = String.fromCharCode(116,99,112,0);
          let nativeo = 4;
         iconwatchnowk += `${((downloadingV ? 4 : 1) >> (Math.min(slideru.length, 1)))}`;
         footballfield6 = new Map([[`${footballfield6.size}`, 1]]);
         slideru += "2";
         nativeo >>= Math.min(4, Math.abs(nativeo - 3));
      do {
         showlessS[routerj] = (routerj == String.fromCharCode(87,0) ? iconwatchnowk.length : routerj.length);
         if (showlessS.size == 2320945) {
            break;
         }
      } while ((showlessS.size == 2320945) && ((1 >> (Math.min(3, iconwatchnowk.length))) == 4 && (iconwatchnowk.length >> (Math.min(4, Math.abs(showlessS.size)))) == 1));
         f_manager_ = frame_3ga.size > iconwatchnowk.length;
      do {
         showlessS[iconwatchnowk] = showlessS.size;
         if (showlessS.size == 2214932) {
            break;
         }
      } while ((routerj.length == showlessS.size) && (showlessS.size == 2214932));
      winde += parseFloat(`${2}`);
   }
   do {
      streamingY += `${basketballmatchdetailbgG.size | 3}`;
      if (streamingY.length == 787537) {
         break;
      }
   } while ((streamingY.length >= footballfieldC.length) && (streamingY.length == 787537));
        debounceSetPip(false);
      emojiheart1 = `${footballfieldC.length}`;
      imageactionliveN = `${(footballfieldC == String.fromCharCode(84,0) ? footballfieldC.length : circle3.length)}`;

        ref.current?.seek(0);
      } else if (nextAppState === 'background') {

      matchesE += `${imageactionliveN.length << (Math.min(3, scorepopsound3.length))}`;
   while (footballfieldC != String.fromCharCode(66,0)) {
      matchesE = `${footballfieldC.length / (Math.max(3, 9))}`;
      break;
   }
        setIsPipEnabled(true);
      matchesE += "1";
      basketballmatchdetailbgG = new Map([[`${circle3.length}`, 3 ^ streamingY.length]]);

      }
    });
       let giftbuttona = String.fromCharCode(112,114,111,102,114,101,115,115,0);
       let xvodd = String.fromCharCode(116,111,110,101,115,0);
       let temperatureY = String.fromCharCode(117,100,112,0);
      for (let e = 0; e < 3; e++) {
          let accepted5 = String.fromCharCode(97,98,115,111,108,117,116,101,0);
          let shoot1 = 5.0;
          let friendsT = 3.0;
         temperatureY += `${(temperatureY == String.fromCharCode(71,0) ? xvodd.length : temperatureY.length)}`;
         accepted5 += `${(String.fromCharCode(114,0) == accepted5 ? accepted5.length : parseInt(`${shoot1}`))}`;
         shoot1 += parseInt(`${shoot1}`) ^ 3;
         friendsT /= Math.max(1, parseInt(`${shoot1}`) % (Math.max(10, accepted5.length)));
      }
          let bingB = String.fromCharCode(103,108,121,112,104,0);
          let footballfield1 = String.fromCharCode(99,104,101,99,107,105,110,0);
         xvodd = "2";
         bingB += `${2 ^ footballfield1.length}`;
         footballfield1 = `${bingB.length % (Math.max(footballfield1.length, 1))}`;
         temperatureY += `${giftbuttona.length}`;
      if (xvodd.length >= 1 && temperatureY != String.fromCharCode(81,0)) {
         temperatureY = `${giftbuttona.length}`;
      }
         giftbuttona = `${xvodd.length}`;
       let viewer4 = 2;
       let overlayZ = 3;
       let langkeyD = 4;
       let phoneR = String.fromCharCode(115,121,115,105,110,102,111,0);
       let smallorangemann = String.fromCharCode(97,116,116,114,97,99,116,105,110,103,0);
      while ((1 & temperatureY.length) <= 5 && (1 & temperatureY.length) <= 5) {
         temperatureY += `${3 - langkeyD}`;
         break;
      }
      emojiheart1 += `${basketballmatchdetailbgG.size + parseInt(`${winde}`)}`;
       let subtextc = String.fromCharCode(114,101,108,97,116,105,118,101,108,121,0);
      if (subtextc == subtextc) {
          let predictionlossW = new Map([[String.fromCharCode(97,109,114,119,98,100,97,116,97,0),110], [String.fromCharCode(112,114,101,105,118,111,117,115,0),280], [String.fromCharCode(102,97,118,105,99,111,110,0),195]]);
          let orangedownarroww = String.fromCharCode(99,111,110,116,97,105,110,101,100,0);
          let moduleX = [666, 892, 566];
         subtextc += `${orangedownarroww.length * moduleX.length}`;
         predictionlossW = new Map([[`${predictionlossW.size}`, predictionlossW.size | 2]]);
         orangedownarroww += `${predictionlossW.size}`;
      }
         subtextc += `${subtextc.length}`;
      if (subtextc == String.fromCharCode(89,0)) {
         subtextc += "1";
      }
      streamingY += `${2 & trashP}`;

    return () => {
      subscription.remove();
   while ((imageactionliveN.length * parseInt(`${winde}`)) <= 4 && (parseFloat(`${imageactionliveN.length}`) * winde) <= 2.7) {
       let teamO = 4.0;
       let championy = new Map([[String.fromCharCode(100,117,112,108,101,120,0),647], [String.fromCharCode(108,112,99,108,115,112,0),905], [String.fromCharCode(115,99,116,112,0),176]]);
       let penaltyshootnogoal_ = new Map([[String.fromCharCode(109,97,107,101,110,97,110,0),false ], [String.fromCharCode(119,114,111,116,101,0),true ]]);
       let paginationR = String.fromCharCode(99,97,108,105,98,114,97,116,101,100,0);
       let windx = 5;
      do {
          let iconorientationC = new Map([[String.fromCharCode(114,101,118,97,108,105,100,97,116,101,100,0),15], [String.fromCharCode(116,114,105,97,108,0),464]]);
          let jingdongr = false;
          let iconrefreshu = new Map([[String.fromCharCode(105,110,118,111,107,101,0),140], [String.fromCharCode(114,101,116,97,105,110,0),263]]);
          let inactiveZ = 0;
         penaltyshootnogoal_[paginationR] = 3 | paginationR.length;
         iconorientationC = new Map([[`${iconrefreshu.size}`, iconrefreshu.size & 3]]);
         jingdongr = iconorientationC.size < 41 || jingdongr;
         inactiveZ ^= iconrefreshu.size;
         if (4801078 == penaltyshootnogoal_.size) {
            break;
         }
      } while ((4801078 == penaltyshootnogoal_.size) && (penaltyshootnogoal_.size > paginationR.length));
      if (4 > (windx & 4) && (windx - 4) > 4) {
         teamO += parseFloat(`${championy.size | 1}`);
      }
      if (2.58 == (teamO + 1.42)) {
         windx -= 3 ^ windx;
      }
      for (let k = 0; k < 1; k++) {
         championy[`${windx}`] = championy.size % (Math.max(7, windx));
      }
         championy[`${teamO}`] = parseInt(`${teamO}`);
         paginationR += `${windx}`;
         windx >>= Math.min(3, Math.abs(windx));
          let sellmathbackgroundy = String.fromCharCode(119,105,100,101,0);
          let volumeQ = 1;
          let predictiondefaulto = 4;
         paginationR = `${championy.size}`;
         sellmathbackgroundy += `${sellmathbackgroundy.length}`;
         volumeQ *= sellmathbackgroundy.length;
         predictiondefaulto /= Math.max(3, 1);
      for (let v = 0; v < 2; v++) {
         paginationR += `${windx & championy.size}`;
      }
         teamO -= parseFloat(`${paginationR.length}`);
         championy[paginationR] = paginationR.length;
         windx &= 2;
      while (paginationR.length <= championy.size) {
         championy[paginationR] = 1 & paginationR.length;
         break;
      }
         paginationR += `${windx}`;
      while (windx >= 2) {
         windx >>= Math.min(3, Math.abs(parseInt(`${teamO}`) / (Math.max(1, 5))));
         break;
      }
      imageactionliveN = `${parseInt(`${teamO}`)}`;
      break;
   }
      footballfieldC = `${3 ^ emojiheart1.length}`;

    };
      trashP |= circle3.length;
   if ((3 << (Math.min(1, scorepopsound3.length))) == 4) {
       let iconwatchnowm = [783, 643, 140];
       let brightness3 = [435, 77];
         brightness3 = [1 & iconwatchnowm.length];
         iconwatchnowm.push(iconwatchnowm.length);
       let libglog3 = new Map([[String.fromCharCode(97,112,112,114,111,120,0),String.fromCharCode(99,117,98,101,0)], [String.fromCharCode(100,100,116,115,0),String.fromCharCode(114,101,97,99,116,105,111,110,0)]]);
      if (4 >= (iconwatchnowm.length + brightness3.length) && 1 >= (4 + iconwatchnowm.length)) {
         iconwatchnowm = [brightness3.length];
      }
      do {
          let style0 = 3;
          let grayA = [868, 780];
          let emojiheartn = [0, 144];
          let source2 = 3.0;
          let overS = false;
         libglog3[`${grayA.length}`] = grayA.length & brightness3.length;
         style0 /= Math.max(style0 >> (Math.min(1, Math.abs(2))), 5);
         emojiheartn = [style0 / (Math.max(emojiheartn.length, 10))];
         source2 *= ((overS ? 1 : 3) | parseInt(`${source2}`));
         overS = source2 == 95.37 || !overS;
         if (libglog3.size == 2867600) {
            break;
         }
      } while ((libglog3.size == 2867600) && (!Array.from(libglog3.values()).includes(brightness3.length)));
         iconwatchnowm = [1 | brightness3.length];
      scorepopsound3 = [3];
   }

  }, []);
       let moond = new Map([[String.fromCharCode(102,109,116,115,0),938], [String.fromCharCode(99,97,116,99,104,105,110,103,0),552], [String.fromCharCode(97,99,99,101,112,116,101,100,0),252]]);
       let libfollyS = String.fromCharCode(100,114,111,112,0);
      for (let h = 0; h < 3; h++) {
          let awayiconN = 3.0;
         moond[`${libfollyS}`] = libfollyS.length;
         awayiconN += parseInt(`${awayiconN}`) ^ 2;
      }
          let basketballmatchdetailbgX = 1.0;
          let downloadJ = new Map([[String.fromCharCode(115,99,114,97,112,101,0),String.fromCharCode(99,115,114,105,100,0)], [String.fromCharCode(108,111,115,101,0),String.fromCharCode(102,111,114,116,104,0)], [String.fromCharCode(109,101,109,99,112,121,0),String.fromCharCode(116,101,109,112,110,97,109,101,0)]]);
         libfollyS = "2";
         basketballmatchdetailbgX += 3 & downloadJ.size;
         downloadJ = new Map([[`${downloadJ.size}`, downloadJ.size << (Math.min(4, Math.abs(parseInt(`${basketballmatchdetailbgX}`))))]]);
          let configm = [435, 945];
          let nalyticst = 2.0;
         moond[`${nalyticst}`] = parseInt(`${nalyticst}`);
         configm.push(3 * configm.length);
      if (1 < (1 >> (Math.min(1, Math.abs(moond.size)))) && (libfollyS.length >> (Math.min(3, Math.abs(moond.size)))) < 1) {
         libfollyS = `${moond.size >> (Math.min(Math.abs(2), 3))}`;
      }
       let iconarrowrightorangee = false;
         iconarrowrightorangee = 84 < moond.size;
      imageactionliveN = `${matchesE.length}`;
   if (!matchesE.startsWith(`${iconedita}`)) {
      matchesE += `${scorepopsound3.length + 2}`;
   }

};
