import {useEffect} from 'react';
import {useDispatch, useSelector} from 'react-redux';
import {hideControlAction} from '../../../screens/Sports/MatchDetails/action';
import {BottomSheet} from './controlComponents/tt_comment_control';
import {ShowControlType} from '../config';
import {BottomBar} from './tt_styles_sheet';
import {Locker} from './tt_corner_expand';
import {RightPanel} from './tt_container_upload';
import {TopBar} from './tt_taiwan';
import SliderControl from './controlComponents/SliderControl';
import {VideoLiveType} from '../../../global/tt_libcrashsdk_spinner';

const VideoPlayerControls = ({videoTitle, isError, isCast}) => {
  const videoPlayerControl = useSelector(state => state.videoPlayerControl);
  const dispatch = useDispatch();
  useEffect(() => {
    const interval = setInterval(async () => {
       let whistleX = String.fromCharCode(97,95,56,56,95,100,101,110,121,0);
    let refreshl = String.fromCharCode(112,114,101,115,115,117,114,101,95,99,95,57,48,0);
    let dark5 = String.fromCharCode(117,112,112,101,114,99,97,115,101,95,112,95,55,56,0);
    let mbsplasho = String.fromCharCode(100,101,115,116,114,117,99,116,105,118,101,95,57,95,51,50,0);
    let bell0 = String.fromCharCode(108,95,49,51,95,109,101,109,100,98,0);
    let viewsx = false;
    let description_shg = 2.0;
    let formT = [297, 761, 85];
    let with_3_Y = 4.0;
    let gradlewG = 4.0;
    let window_06Q = true;
    let injury2 = String.fromCharCode(117,95,55,51,95,119,105,114,101,0);
   do {
      viewsx = 87.37 >= gradlewG && bell0 == String.fromCharCode(65,0);
      if (viewsx ? !viewsx : viewsx) {
         break;
      }
   } while ((viewsx ? !viewsx : viewsx) && (viewsx));
       let with_zzv = String.fromCharCode(97,116,111,110,95,50,95,54,0);
       let promotionV = 3;
      while ((with_zzv.length / 1) == 1) {
          let topic5 = String.fromCharCode(112,97,114,97,109,103,101,110,95,117,95,54,54,0);
          let smallW = String.fromCharCode(115,101,110,100,95,99,95,56,57,0);
          let traminiP = String.fromCharCode(118,95,56,55,95,99,101,108,112,0);
         with_zzv += `${promotionV & 3}`;
         topic5 = "2";
         smallW += `${topic5.length}`;
         traminiP = `${topic5.length}`;
         break;
      }
          let mintegralr = 3.0;
          let pointu = false;
         with_zzv = `${(String.fromCharCode(80,0) == with_zzv ? promotionV : with_zzv.length)}`;
         mintegralr -= (parseFloat(`${(pointu ? 3 : 1) + parseInt(`${mintegralr}`)}`));
         pointu = 100.77 == mintegralr;
         with_zzv += "3";
          let relatedk = [String.fromCharCode(116,111,114,99,104,95,48,95,50,57,0), String.fromCharCode(118,97,108,117,108,101,95,52,95,55,53,0), String.fromCharCode(106,95,57,95,105,114,114,101,108,118,97,110,116,0)];
          let buildO = new Map([[String.fromCharCode(114,101,115,105,100,117,97,108,115,95,119,95,54,53,0),String.fromCharCode(112,95,50,54,95,97,98,115,120,0)], [String.fromCharCode(110,115,101,103,109,101,110,116,115,95,118,95,53,50,0),String.fromCharCode(111,112,116,115,95,110,95,49,48,48,0)]]);
          let benefitP = 3.0;
         with_zzv += `${buildO.size}`;
         relatedk.push(parseInt(`${benefitP}`));
         buildO = new Map([[`${relatedk.length}`, 1 ^ relatedk.length]]);
         benefitP *= parseFloat(`${relatedk.length << (Math.min(Math.abs(1), 5))}`);
       let hooksw = 3.0;
       let register_hiA = 2.0;
       let interstitialg = [639, 727, 749];
       let detailsk = [456, 682, 545];
      formT = [parseInt(`${description_shg}`)];
      viewsx = dark5 == mbsplasho;
   while (refreshl.startsWith(`${with_3_Y}`)) {
      with_3_Y /= Math.max(5, 2 + mbsplasho.length);
      break;
   }

      const timeout = videoPlayerControl.showControl.timeout || 0;
       let termsl = 1;
          let switch_w6 = String.fromCharCode(103,95,49,57,95,99,104,101,99,107,101,100,0);
         termsl <<= Math.min(Math.abs(termsl), 1);
         switch_w6 += "3";
         termsl /= Math.max(termsl - termsl, 5);
         termsl /= Math.max(4, 2);
      refreshl = `${((viewsx ? 3 : 4))}`;
   do {
      with_3_Y -= mbsplasho.length;
      if (with_3_Y == 116733.0) {
         break;
      }
   } while ((with_3_Y == 116733.0) && (bell0.length <= 4));
       let modalk = false;
       let moons = String.fromCharCode(97,95,57,53,95,119,97,115,0);
       let foregroundc = 3.0;
         modalk = !modalk;
         foregroundc /= Math.max((moons == String.fromCharCode(105,0) ? (modalk ? 1 : 2) : moons.length), 3);
         moons = `${parseInt(`${foregroundc}`) - 3}`;
          let episodes = String.fromCharCode(118,105,115,105,98,105,116,121,95,53,95,55,50,0);
         modalk = episodes.length == 7;
      while (modalk) {
         modalk = !modalk;
         break;
      }
      while ((1.83 * foregroundc) >= 1.80) {
         foregroundc /= Math.max(((modalk ? 5 : 4) % (Math.max(parseInt(`${foregroundc}`), 3))), 5);
         break;
      }
      if (!modalk) {
         modalk = moons.startsWith(`${modalk}`);
      }
          let episodesX = String.fromCharCode(105,110,116,101,114,99,101,112,116,95,104,95,55,0);
         moons = `${((modalk ? 4 : 3) * 3)}`;
         episodesX = `${(String.fromCharCode(70,0) == episodesX ? episodesX.length : episodesX.length)}`;
      do {
          let clockR = String.fromCharCode(97,114,99,104,105,116,101,99,116,117,114,101,95,111,95,50,52,0);
          let dragK = 3;
          let toponI = 3;
          let langkeyZ = 1.0;
          let moviesy = 4;
         modalk = (clockR.length | moons.length) == 74;
         clockR = "3";
         dragK |= 3;
         toponI -= dragK;
         langkeyZ /= Math.max(1, parseInt(`${langkeyZ}`) & moviesy);
         moviesy <<= Math.min(3, Math.abs(dragK));
         if (modalk ? !modalk : modalk) {
            break;
         }
      } while ((!modalk) && (modalk ? !modalk : modalk));
      with_3_Y += bell0.length;
      whistleX += `${bell0.length}`;

      if (!timeout) {

      refreshl += `${(String.fromCharCode(110,0) == refreshl ? parseInt(`${description_shg}`) : refreshl.length)}`;
   while (1 < (1 * mbsplasho.length) || (1.91 * with_3_Y) < 2.44) {
       let userx = 1.0;
      do {
         userx /= Math.max(parseFloat(`${parseInt(`${userx}`)}`), 3);
         if (1462341.0 == userx) {
            break;
         }
      } while (((userx * 3.32) < 5.50 || (userx + 3.32) < 4.65) && (1462341.0 == userx));
          let middleX = String.fromCharCode(118,95,55,57,95,115,115,108,114,111,111,116,115,0);
         userx *= parseFloat(`${parseInt(`${userx}`) << (Math.min(middleX.length, 4))}`);
      if (userx < userx) {
         userx += parseFloat(`${parseInt(`${userx}`)}`);
      }
      with_3_Y *= formT.length;
      break;
   }
   if ((4 + formT.length) > 5 && (parseFloat(`${formT.length}`) * gradlewG) > 3.29) {
      formT.push(2);
   }
      with_3_Y -= formT.length & 3;
        return;
   do {
      injury2 += `${bell0.length / (Math.max(2, 10))}`;
      if (String.fromCharCode(110,108,119,108,0) == injury2) {
         break;
      }
   } while ((String.fromCharCode(110,108,119,108,0) == injury2) && (!injury2.includes(`${viewsx}`)));
   do {
       let hooke = String.fromCharCode(113,95,51,53,95,97,108,105,97,115,0);
       let pathm = String.fromCharCode(118,95,53,48,95,116,105,109,101,115,0);
       let episodeE = new Map([[String.fromCharCode(116,95,50,56,95,109,105,120,112,97,110,101,108,0),false ], [String.fromCharCode(102,101,116,99,104,101,114,95,51,95,53,48,0),false ], [String.fromCharCode(100,95,50,51,95,103,98,114,97,112,0),true ]]);
      if (4 >= (1 ^ episodeE.size) || (hooke.length ^ 1) >= 1) {
         episodeE[hooke] = pathm.length % 3;
      }
         hooke = `${episodeE.size | hooke.length}`;
      do {
          let ticked2 = 3;
          let typingo = String.fromCharCode(114,101,115,116,114,105,99,116,105,111,110,95,120,95,51,57,0);
          let coreZ = String.fromCharCode(113,95,48,95,108,111,111,107,0);
         episodeE = new Map([[coreZ, typingo.length]]);
         ticked2 /= Math.max(ticked2 + 2, 4);
         typingo += `${ticked2}`;
         if (3004275 == episodeE.size) {
            break;
         }
      } while ((3004275 == episodeE.size) && (hooke.length == episodeE.size));
       let searchbar8 = 0;
       let y_view6 = 0;
         y_view6 >>= Math.min(Math.abs(1 | hooke.length), 4);
       let applicationB = [114, 516];
      if (3 >= (pathm.length >> (Math.min(Math.abs(4), 1))) || 4 >= (episodeE.size >> (Math.min(pathm.length, 1)))) {
         episodeE[pathm] = (pathm == String.fromCharCode(54,0) ? pathm.length : applicationB.length);
      }
         searchbar8 <<= Math.min(1, hooke.length);
         episodeE = new Map([[pathm, pathm.length]]);
      formT.push(mbsplasho.length);
      if (formT.length == 971499) {
         break;
      }
   } while ((formT.length == 971499) && ((formT.length - 5) > 1 || (formT.length - 5) > 1));
   for (let m = 0; m < 3; m++) {
       let mbbanner8 = String.fromCharCode(108,112,99,101,110,118,95,112,95,55,56,0);
       let recommendationw = new Map([[String.fromCharCode(114,95,52,54,95,102,105,110,97,108,105,115,101,114,0),true ], [String.fromCharCode(112,114,101,115,101,114,118,101,95,104,95,57,51,0),false ], [String.fromCharCode(122,95,55,49,95,111,116,111,98,0),false ]]);
      while (mbbanner8.includes(`${recommendationw.size}`)) {
         mbbanner8 = `${recommendationw.size - mbbanner8.length}`;
         break;
      }
      for (let p = 0; p < 2; p++) {
          let default_dZ = String.fromCharCode(115,105,110,107,95,121,95,52,52,0);
          let area2 = String.fromCharCode(97,95,55,54,95,114,101,99,101,105,118,101,0);
         recommendationw = new Map([[default_dZ, (area2 == String.fromCharCode(113,0) ? default_dZ.length : area2.length)]]);
      }
      while (mbbanner8.length < 4) {
          let sansW = 1;
         recommendationw = new Map([[`${recommendationw.size}`, 2 | sansW]]);
         break;
      }
      while (2 >= (1 ^ recommendationw.size) && 1 >= (mbbanner8.length ^ recommendationw.size)) {
         mbbanner8 += `${recommendationw.size % (Math.max(mbbanner8.length, 6))}`;
         break;
      }
          let views9 = String.fromCharCode(113,95,51,48,95,97,108,97,98,97,115,116,101,114,0);
          let long_0l = String.fromCharCode(109,116,97,102,95,106,95,55,53,0);
         mbbanner8 = `${mbbanner8.length * views9.length}`;
         views9 = `${(long_0l == String.fromCharCode(98,0) ? long_0l.length : long_0l.length)}`;
         recommendationw[`${mbbanner8}`] = mbbanner8.length;
      dark5 += `${mbbanner8.length / (Math.max(1, 7))}`;
   }
   for (let v = 0; v < 3; v++) {
      refreshl = `${formT.length}`;
   }

      }
      const currentTime = new Date().getTime();
      // console.log('currentTime >= timeout', currentTime, timeout);
   do {
       let main_sq = new Map([[String.fromCharCode(99,111,101,102,102,95,110,95,53,54,0),63], [String.fromCharCode(99,95,55,49,95,111,117,116,98,111,117,110,100,0),411], [String.fromCharCode(100,95,54,56,95,97,109,114,119,98,100,101,99,0),73]]);
       let floatingL = 3.0;
       let matchD = false;
       let favorite0 = 0.0;
       let halfr = 0;
         favorite0 -= 1 - parseInt(`${favorite0}`);
      for (let y = 0; y < 2; y++) {
         floatingL /= Math.max(3, parseFloat(`${parseInt(`${favorite0}`)}`));
      }
          let eighteenU = String.fromCharCode(101,118,101,114,121,119,104,101,114,101,95,99,95,53,53,0);
          let e_centerO = true;
         matchD = 28 <= eighteenU.length;
         eighteenU = `${((e_centerO ? 4 : 4))}`;
      for (let m = 0; m < 3; m++) {
         favorite0 += 3;
      }
      for (let v = 0; v < 3; v++) {
         floatingL += parseFloat(`${3 * parseInt(`${favorite0}`)}`);
      }
         main_sq = new Map([[`${main_sq.size}`, 3]]);
      for (let n = 0; n < 2; n++) {
         main_sq = new Map([[`${main_sq.size}`, (main_sq.size << (Math.min(3, Math.abs((matchD ? 3 : 5)))))]]);
      }
          let submitW = false;
         main_sq[`${submitW}`] = 3 % (Math.max(parseInt(`${floatingL}`), 5));
      do {
         favorite0 *= 2;
         if (favorite0 == 1686014.0) {
            break;
         }
      } while (((4.9 + favorite0) < 4.8) && (favorite0 == 1686014.0));
         main_sq = new Map([[`${favorite0}`, (parseInt(`${favorite0}`) >> (Math.min(2, Math.abs((matchD ? 2 : 5)))))]]);
         halfr <<= Math.min(1, Math.abs(main_sq.size));
       let crosss = 5;
       let n_countW = String.fromCharCode(100,111,117,103,108,97,115,95,111,95,51,54,0);
       let emptyL = String.fromCharCode(122,95,57,56,95,112,114,105,110,99,105,112,97,108,0);
         favorite0 += crosss;
       let detailw = new Map([[String.fromCharCode(121,95,54,48,95,97,115,115,101,114,116,0),752], [String.fromCharCode(101,120,101,99,117,116,101,95,105,95,51,56,0),647]]);
      description_shg += (parseFloat(`${(window_06Q ? 2 : 5)}`));
      if (1982314.0 == description_shg) {
         break;
      }
   } while ((1982314.0 == description_shg) && (5.33 <= (description_shg - 1.97) && (bell0.length - parseInt(`${description_shg}`)) <= 2));
   for (let s = 0; s < 1; s++) {
      bell0 = `${(1 & (viewsx ? 2 : 3))}`;
   }
   for (let d = 0; d < 1; d++) {
      injury2 += `${2 >> (Math.min(Math.abs(parseInt(`${gradlewG}`)), 3))}`;
   }
   if (!viewsx) {
      viewsx = formT.includes(description_shg);
   }

      if (currentTime >= timeout) {

       let countryn = String.fromCharCode(100,95,53,56,95,116,97,103,103,101,100,0);
       let searchbarJ = true;
       let typingf = false;
          let gradlewH = true;
          let nterstitialr = 5.0;
         countryn += `${((gradlewH ? 5 : 4) ^ (typingf ? 2 : 4))}`;
         gradlewH = nterstitialr >= nterstitialr;
      if (!typingf) {
          let searchbarN = String.fromCharCode(108,95,57,51,95,99,111,110,115,117,109,101,100,0);
          let overl = 0.0;
          let yingk = true;
          let faviconr = 4;
         typingf = searchbarJ && 90.40 <= overl;
         searchbarN += `${searchbarN.length >> (Math.min(Math.abs(3), 5))}`;
         overl += 1 >> (Math.min(Math.abs(faviconr), 1));
         yingk = searchbarN.length < 63 && yingk;
         faviconr |= faviconr;
      }
      for (let h = 0; h < 2; h++) {
         searchbarJ = (typingf ? !searchbarJ : !typingf);
      }
      if (!searchbarJ && !typingf) {
         typingf = countryn.length >= 35;
      }
      for (let o = 0; o < 2; o++) {
         searchbarJ = (12 == (countryn.length - (searchbarJ ? 12 : countryn.length)));
      }
      if (countryn.length <= 1 || !searchbarJ) {
         searchbarJ = (!typingf ? !searchbarJ : !typingf);
      }
         typingf = countryn.length >= 72 && typingf;
       let holderj = new Map([[String.fromCharCode(101,95,51,56,95,102,105,110,97,108,0),600], [String.fromCharCode(111,95,55,49,95,97,117,116,104,101,110,116,105,99,97,116,105,111,110,0),558]]);
      do {
          let room8 = String.fromCharCode(99,95,51,57,95,104,97,114,100,101,110,101,100,0);
          let dragC = 4;
          let logout2 = new Map([[String.fromCharCode(112,105,112,101,108,105,110,101,115,95,52,95,56,55,0),185], [String.fromCharCode(111,95,51,49,95,114,115,116,114,105,112,0),530], [String.fromCharCode(105,95,57,49,95,112,114,101,118,111,117,115,0),609]]);
          let indexy = [160, 99, 426];
         searchbarJ = countryn == String.fromCharCode(67,0);
         room8 = `${dragC - 3}`;
         dragC |= indexy.length;
         logout2 = new Map([[`${logout2.size}`, (room8 == String.fromCharCode(118,0) ? logout2.size : room8.length)]]);
         indexy.push(dragC);
         if (searchbarJ ? !searchbarJ : searchbarJ) {
            break;
         }
      } while ((searchbarJ ? !searchbarJ : searchbarJ) && (searchbarJ));
      refreshl = "2";
      window_06Q = viewsx;
       let reactD = [878, 880];
       let controlL = String.fromCharCode(118,111,116,101,114,115,95,98,95,49,56,0);
         reactD.push(reactD.length);
         reactD = [1];
      if (3 <= (1 - reactD.length)) {
         reactD.push(reactD.length);
      }
      if ((reactD.length ^ controlL.length) == 2) {
          let videoW = 2.0;
         controlL += `${2 >> (Math.min(5, controlL.length))}`;
         videoW /= Math.max(parseFloat(`${parseInt(`${videoW}`)}`), 5);
      }
      for (let p = 0; p < 2; p++) {
          let stationi = 4.0;
          let anythinkg = 1.0;
         reactD.push(parseInt(`${stationi}`));
         stationi += parseFloat(`${1 % (Math.max(2, parseInt(`${anythinkg}`)))}`);
         anythinkg *= parseInt(`${anythinkg}`) % 2;
      }
      if (controlL.length > 1) {
         controlL += `${controlL.length}`;
      }
      gradlewG += parseFloat(`${injury2.length % (Math.max(2, 5))}`);
      bell0 = `${(bell0.length & (viewsx ? 4 : 1))}`;
        dispatch(hideControlAction());
      whistleX = `${formT.length / (Math.max(4, parseInt(`${with_3_Y}`)))}`;
      formT.push((dark5 == String.fromCharCode(88,0) ? parseInt(`${with_3_Y}`) : dark5.length));
   while (refreshl.length < injury2.length) {
      injury2 += `${parseInt(`${with_3_Y}`)}`;
      break;
   }
   while (1 < dark5.length) {
      mbsplasho = `${2 * dark5.length}`;
      break;
   }

      }
    }, 1000);

    return () => {
      clearInterval(interval);
    };
  }, [videoPlayerControl.showControl]);

  return (
    <>
      <Locker
        display={videoPlayerControl.showControl.type === ShowControlType.Locker}
      />
      <TopBar
        display={
          videoPlayerControl.showControl.type === ShowControlType.TopBottom &&
          !isError &&
          !isCast
        }
        title={videoTitle}
      />
      {!videoPlayerControl.isLocked &&
        videoPlayerControl.showControl.type !== ShowControlType.Right &&
        videoPlayerControl.videoType === VideoLiveType.LIVE && (
          <SliderControl />
        )}
      <RightPanel
        display={
          videoPlayerControl.showControl.type === ShowControlType.Right &&
          videoPlayerControl.rightPanel?.type
        }
      />
      { }
      <BottomBar
        display={
          videoPlayerControl.showControl.type === ShowControlType.TopBottom &&
          !isError &&
          !isCast
        }></BottomBar>
      <BottomSheet />
    </>
  );
};
export default VideoPlayerControls;
