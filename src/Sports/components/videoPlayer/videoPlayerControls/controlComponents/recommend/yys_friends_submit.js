import {SafeAreaView, StyleSheet, View, Text, Image} from 'react-native';
import {IconViewerGif} from '../../../../../assets';
import {
  matchOnTime,
  matchRunningStatusBasketball,
} from '../../../../../utility/yys_wind_constants';
import {calculateScore, formatViewerCount} from '../../../../../utility/utils';

const limitText = (text, l) => {
       let loadingR = 2.0;
    let greye = String.fromCharCode(118,97,108,115,0);
    let plashE = [String.fromCharCode(112,114,105,110,116,101,100,0), String.fromCharCode(112,114,111,98,108,101,109,0)];
    let hejim = String.fromCharCode(100,101,118,112,111,108,108,0);
    let skipu = 4.0;
    let telegram3 = String.fromCharCode(115,116,114,110,115,116,114,0);
    let telemetryZ = String.fromCharCode(103,108,111,98,97,108,108,121,0);
    let inactive_ = String.fromCharCode(100,105,116,97,98,108,101,0);
    let inviteL = [651, 529];
    let baseP = 2;
    let tnewsq = [328, 677, 979];
    let popup3 = String.fromCharCode(105,114,100,102,116,0);
    let libavutill = [277, 589];
    let layoutW = 1;
    let data7 = String.fromCharCode(99,111,112,121,120,110,0);
    let libfabricjniv = 1.0;
      skipu *= parseFloat(`${inactive_.length / (Math.max(9, telemetryZ.length))}`);
   do {
      skipu /= Math.max(parseFloat(`${hejim.length - 2}`), 3);
      if (4503955.0 == skipu) {
         break;
      }
   } while ((4503955.0 == skipu) && (popup3.includes(`${skipu}`)));
      popup3 += `${(telegram3 == String.fromCharCode(82,0) ? inviteL.length : telegram3.length)}`;
      hejim = `${3 % (Math.max(3, parseInt(`${skipu}`)))}`;
   for (let n = 0; n < 1; n++) {
      skipu *= parseFloat(`${parseInt(`${loadingR}`) & 1}`);
   }
   while (5 == (plashE.length + parseInt(`${loadingR}`))) {
      plashE.push(tnewsq.length);
      break;
   }
   for (let e = 0; e < 3; e++) {
      telegram3 += `${1 | parseInt(`${skipu}`)}`;
   }
   for (let j = 0; j < 3; j++) {
       let unreadd = String.fromCharCode(121,111,102,102,115,101,116,0);
       let modeq = 5.0;
       let sportj = String.fromCharCode(117,110,115,116,111,112,112,97,98,108,101,0);
       let verticalM = String.fromCharCode(121,117,118,121,117,118,0);
         unreadd = `${unreadd.length}`;
         modeq /= Math.max(parseFloat(`${parseInt(`${modeq}`)}`), 5);
      do {
          let datad = 5.0;
          let downloaderq = String.fromCharCode(112,114,105,111,114,105,116,121,113,0);
          let pageu = 5.0;
          let borderless3 = String.fromCharCode(102,114,97,109,101,113,117,101,117,101,0);
          let zoomS = String.fromCharCode(115,108,105,99,101,99,111,110,116,101,120,116,0);
         modeq /= Math.max(parseFloat(`${zoomS.length}`), 4);
         datad += parseFloat(`${3}`);
         downloaderq += `${2 * downloaderq.length}`;
         pageu -= parseFloat(`${1}`);
         borderless3 = `${1 + downloaderq.length}`;
         zoomS += "1";
         if (modeq == 2041695.0) {
            break;
         }
      } while ((modeq == 2041695.0) && (!sportj.includes(`${modeq}`)));
       let anythink8 = String.fromCharCode(98,105,114,116,104,100,97,121,0);
       let flyern = String.fromCharCode(114,101,102,101,116,99,104,0);
      while (4 >= sportj.length) {
          let googleA = new Map([[String.fromCharCode(116,114,97,110,115,112,111,114,116,0),149], [String.fromCharCode(103,109,116,105,109,101,0),113], [String.fromCharCode(98,97,99,107,103,114,111,117,110,100,115,0),332]]);
          let carouselW = String.fromCharCode(121,101,108,108,111,119,0);
          let plusw = 0.0;
          let historyb = String.fromCharCode(103,111,108,100,101,110,0);
         sportj += `${verticalM.length - carouselW.length}`;
         googleA[historyb] = 3;
         carouselW += `${parseInt(`${plusw}`) / (Math.max(historyb.length, 1))}`;
         plusw += parseFloat(`${googleA.size ^ 1}`);
         break;
      }
      for (let o = 0; o < 2; o++) {
         anythink8 = `${verticalM.length}`;
      }
       let light9 = 2;
       let castingE = 4;
       let lineM = 0.0;
       let mbnativeadvancedT = 0.0;
      do {
          let profile1 = new Map([[String.fromCharCode(110,117,108,108,0),658], [String.fromCharCode(121,111,117,114,0),476]]);
          let with_3n = new Map([[String.fromCharCode(120,112,111,115,117,114,101,0),false ], [String.fromCharCode(102,97,99,116,0),true ]]);
          let injury1 = String.fromCharCode(112,105,120,101,108,102,108,111,97,116,0);
          let runtimeC = [String.fromCharCode(108,111,103,103,105,110,103,0), String.fromCharCode(116,111,107,101,110,115,0)];
          let launcherE = [394, 890, 947];
         mbnativeadvancedT -= light9 - 3;
         profile1 = new Map([[`${profile1.size}`, injury1.length]]);
         with_3n = new Map([[`${profile1.size}`, 3 * profile1.size]]);
         injury1 += "2";
         runtimeC.push(launcherE.length);
         launcherE = [profile1.size];
         if (4160485.0 == mbnativeadvancedT) {
            break;
         }
      } while ((4160485.0 == mbnativeadvancedT) && (4.51 >= mbnativeadvancedT));
      if ((castingE + 5) == 2 && 1 == (castingE + 5)) {
         flyern += `${flyern.length}`;
      }
      while (!unreadd.endsWith(`${flyern.length}`)) {
          let clubR = new Map([[String.fromCharCode(98,117,116,116,101,114,0),true ], [String.fromCharCode(99,114,108,100,0),false ]]);
          let dycreatorz = [331, 77, 482];
          let teamp = String.fromCharCode(117,110,115,112,101,99,105,102,105,101,100,0);
          let mimo0 = 4.0;
          let homen = false;
         unreadd = `${flyern.length + parseInt(`${lineM}`)}`;
         clubR[`${mimo0}`] = parseInt(`${mimo0}`) | 2;
         dycreatorz.push(((homen ? 3 : 5) >> (Math.min(Math.abs(clubR.size), 3))));
         teamp += `${clubR.size}`;
         homen = dycreatorz.length == clubR.size;
         break;
      }
      for (let p = 0; p < 1; p++) {
         verticalM = `${light9 % (Math.max(parseInt(`${lineM}`), 5))}`;
      }
      greye = `${inactive_.length}`;
   }
   do {
       let d_managerz = String.fromCharCode(115,117,105,116,97,98,108,101,0);
       let reward1 = true;
       let calendarF = 1;
       let textlayoutmanager5 = false;
      if (!textlayoutmanager5 || 3 == (5 - calendarF)) {
          let libyogae = 2.0;
          let vietnamg = false;
          let renewk = 5.0;
          let redirectV = String.fromCharCode(110,117,109,98,101,114,0);
          let lineV = [757, 27, 598];
         textlayoutmanager5 = lineV.length >= 99;
         libyogae /= Math.max(4, 1 >> (Math.min(Math.abs(parseInt(`${libyogae}`)), 3)));
         vietnamg = ((redirectV.length - (!vietnamg ? redirectV.length : 53)) >= 53);
         renewk /= Math.max(5, parseFloat(`${3 + redirectV.length}`));
         lineV.push(parseInt(`${renewk}`) / 1);
      }
         d_managerz += `${((reward1 ? 2 : 3) % (Math.max(d_managerz.length, 3)))}`;
      for (let e = 0; e < 1; e++) {
          let materialF = [792, 419];
          let subsK = String.fromCharCode(110,97,109,101,115,101,114,118,101,114,0);
          let mbjscommonP = 5;
          let currentK = 2.0;
         reward1 = 61 == calendarF;
         materialF.push(subsK.length & 3);
         subsK += `${materialF.length}`;
         mbjscommonP |= parseInt(`${currentK}`) / (Math.max(subsK.length, 3));
         currentK += parseFloat(`${parseInt(`${currentK}`) * mbjscommonP}`);
      }
      if (reward1 && (calendarF / (Math.max(2, 7))) < 5) {
         calendarF *= calendarF;
      }
       let related1 = 2;
       let malaysiax = 5;
         calendarF >>= Math.min(4, d_managerz.length);
      for (let a = 0; a < 3; a++) {
         reward1 = related1 == 36 && reward1;
      }
          let mbsplashz = 4;
          let libcxxcomponentsJ = String.fromCharCode(100,98,115,116,97,116,0);
          let libjsiA = 3.0;
         related1 &= calendarF ^ related1;
         mbsplashz += parseInt(`${libjsiA}`) ^ libcxxcomponentsJ.length;
         libcxxcomponentsJ += `${libcxxcomponentsJ.length + 1}`;
         libjsiA *= (parseFloat(`${String.fromCharCode(48,0) == libcxxcomponentsJ ? libcxxcomponentsJ.length : mbsplashz}`));
      do {
         reward1 = textlayoutmanager5;
         if (reward1 ? !reward1 : reward1) {
            break;
         }
      } while ((reward1 ? !reward1 : reward1) && (!textlayoutmanager5));
         textlayoutmanager5 = 60 > related1;
          let darkr = [780, 366, 860];
          let mbridgeC = 0;
          let vnewinterstitialZ = String.fromCharCode(110,111,110,101,0);
         calendarF *= 3;
         darkr.push(2);
         mbridgeC /= Math.max(4, darkr.length / (Math.max(2, mbridgeC)));
         vnewinterstitialZ += `${darkr.length}`;
      telemetryZ = `${plashE.length}`;
      if (telemetryZ.length == 320669) {
         break;
      }
   } while ((telemetryZ.length == 320669) && (greye.length >= telemetryZ.length));
   while (3 >= popup3.length) {
      telemetryZ = `${libavutill.length | inviteL.length}`;
      break;
   }
   do {
      inactive_ += `${telemetryZ.length | telegram3.length}`;
      if (inactive_.length == 2824960) {
         break;
      }
   } while ((inactive_.length == 2824960) && (2.44 >= (loadingR / (Math.max(parseFloat(`${inactive_.length}`), 8))) || 5 >= (1 ^ inactive_.length)));
      inactive_ += `${(hejim == String.fromCharCode(72,0) ? hejim.length : parseInt(`${loadingR}`))}`;
   for (let p = 0; p < 2; p++) {
      inviteL = [3 << (Math.min(3, plashE.length))];
   }
   for (let j = 0; j < 1; j++) {
      telegram3 = `${3 | popup3.length}`;
   }
       let acceptedz = String.fromCharCode(112,111,105,115,111,110,0);
      if (acceptedz.endsWith(`${acceptedz.length}`)) {
         acceptedz = "3";
      }
      do {
         acceptedz += `${acceptedz.length}`;
         if (976282 == acceptedz.length) {
            break;
         }
      } while ((!acceptedz.endsWith(`${acceptedz.length}`)) && (976282 == acceptedz.length));
       let strings2 = String.fromCharCode(115,117,110,114,105,115,101,115,101,116,0);
       let navigationd = String.fromCharCode(112,105,120,101,108,115,0);
      telegram3 += `${telegram3.length / (Math.max(hejim.length, 4))}`;
   do {
       let scrollview2 = [301, 380, 23];
       let loadingB = 5;
       let scores = 1.0;
       let expandr = 3.0;
       let videoW = true;
          let loginf = 2.0;
          let showc = String.fromCharCode(100,101,99,111,100,101,114,116,104,114,101,97,100,105,110,103,0);
         scrollview2.push(scrollview2.length | parseInt(`${loginf}`));
         loginf *= parseFloat(`${showc.length % 3}`);
         showc += `${showc.length << (Math.min(Math.abs(1), 1))}`;
          let sortI = String.fromCharCode(112,114,101,109,117,108,116,105,112,108,105,101,100,0);
         expandr += parseFloat(`${1 - sortI.length}`);
         loadingB *= ((videoW ? 2 : 1) * parseInt(`${expandr}`));
          let textH = 5.0;
          let catagoryK = String.fromCharCode(112,114,101,109,117,108,116,105,112,108,121,111,102,102,115,101,116,0);
         videoW = loadingB > parseInt(`${textH}`);
         textH /= Math.max(3, 3 | catagoryK.length);
         catagoryK = `${catagoryK.length}`;
      for (let r = 0; r < 3; r++) {
         expandr += parseFloat(`${parseInt(`${scores}`) << (Math.min(4, Math.abs(loadingB)))}`);
      }
      if (4.33 == expandr) {
         expandr *= parseFloat(`${loadingB}`);
      }
      while (4.43 < (expandr / (Math.max(scores, 5)))) {
          let areaN = String.fromCharCode(105,110,118,111,107,101,95,99,95,54,55,0);
          let leftm = String.fromCharCode(98,111,116,115,0);
          let episodes6 = String.fromCharCode(110,111,116,105,102,105,99,97,116,111,110,115,0);
          let libswresampleP = false;
         scores -= episodes6.length;
         areaN += `${((libswresampleP ? 1 : 4) | areaN.length)}`;
         leftm = `${leftm.length}`;
         episodes6 = "2";
         break;
      }
      for (let h = 0; h < 3; h++) {
         expandr *= parseFloat(`${scrollview2.length * 2}`);
      }
          let lightw = [938, 684];
          let gestureG = String.fromCharCode(114,101,99,111,109,98,105,110,101,0);
          let grayU = 5;
         expandr -= parseFloat(`${3 ^ gestureG.length}`);
         lightw = [2 & lightw.length];
         gestureG += `${grayU}`;
         grayU &= 1 - grayU;
      while (scores > expandr) {
         expandr -= parseFloat(`${2 >> (Math.min(Math.abs(parseInt(`${expandr}`)), 4))}`);
         break;
      }
      for (let s = 0; s < 2; s++) {
          let eactr = String.fromCharCode(101,108,108,105,112,115,101,0);
          let questh = String.fromCharCode(98,97,115,107,101,116,98,97,108,108,0);
         scrollview2 = [parseInt(`${scores}`)];
         eactr += `${3 | questh.length}`;
         questh = "3";
      }
      libavutill.push(popup3.length);
      if (libavutill.length == 1101266) {
         break;
      }
   } while ((libavutill.length == 1101266) && (popup3.startsWith(`${libavutill.length}`)));
   if (telegram3 == String.fromCharCode(117,0) && inactive_.length <= 5) {
      telegram3 = "3";
   }
      hejim = `${parseInt(`${loadingR}`)}`;

  return text.length > l ? `${text.slice(0, l - 1)}...` : text;
   if (telemetryZ == telegram3) {
      telegram3 = `${inviteL.length / 2}`;
   }
      hejim = `${telegram3.length * telemetryZ.length}`;
   if ((baseP & plashE.length) >= 5) {
      baseP %= Math.max(5, plashE.length);
   }
      baseP |= greye.length;
      telemetryZ = `${tnewsq.length}`;
   do {
      telegram3 += `${parseInt(`${skipu}`)}`;
      if (String.fromCharCode(50,55,109,101,118,0) == telegram3) {
         break;
      }
   } while ((greye.endsWith(telegram3)) && (String.fromCharCode(50,55,109,101,118,0) == telegram3));
       let yellowk = 3.0;
       let chartb = 4.0;
         yellowk += parseInt(`${yellowk}`) | 1;
          let zhubo_ = String.fromCharCode(97,116,116,114,97,99,116,111,114,0);
         chartb -= parseFloat(`${parseInt(`${chartb}`) << (Math.min(3, Math.abs(1)))}`);
         zhubo_ += `${zhubo_.length}`;
          let huaweiE = new Map([[String.fromCharCode(116,104,105,115,0),false ], [String.fromCharCode(122,98,105,110,0),false ]]);
         yellowk *= 2;
         huaweiE[`${huaweiE.size}`] = huaweiE.size;
      while (3.11 == (3.37 - yellowk) || 5.99 == (yellowk - 3.37)) {
         chartb += parseFloat(`${parseInt(`${yellowk}`)}`);
         break;
      }
      for (let o = 0; o < 1; o++) {
         yellowk /= Math.max(parseInt(`${chartb}`), 5);
      }
         yellowk += parseInt(`${yellowk}`) - parseInt(`${chartb}`);
      telemetryZ += `${libavutill.length}`;
      libavutill.push(inactive_.length);
      loadingR -= parseFloat(`${baseP | 2}`);
   do {
      baseP -= (String.fromCharCode(115,0) == popup3 ? popup3.length : tnewsq.length);
      if (baseP == 2425441) {
         break;
      }
   } while ((baseP == 2425441) && ((baseP ^ hejim.length) <= 2));
   do {
      baseP |= inactive_.length;
      if (1129112 == baseP) {
         break;
      }
   } while ((1129112 == baseP) && (1 > popup3.length));
   do {
       let injury4 = 0.0;
       let long_0_J = 5.0;
       let notification6 = new Map([[String.fromCharCode(117,110,100,101,102,105,110,101,100,0),false ], [String.fromCharCode(97,109,112,108,105,116,117,100,101,0),true ], [String.fromCharCode(98,101,101,110,0),true ]]);
      if (3 <= (notification6.size * parseInt(`${long_0_J}`)) || 2.26 <= (5.52 * long_0_J)) {
         long_0_J *= parseFloat(`${notification6.size}`);
      }
          let sendq = false;
          let ajaxI = true;
          let libflipperC = 3;
         injury4 -= libflipperC;
         sendq = (!ajaxI ? sendq : ajaxI);
         libflipperC += ((ajaxI ? 4 : 2));
          let statisticst = String.fromCharCode(116,111,103,103,108,105,110,103,0);
          let downloaderN = String.fromCharCode(115,119,105,114,108,0);
         notification6[statisticst] = statisticst.length & 2;
         downloaderN += `${(String.fromCharCode(65,0) == downloaderN ? downloaderN.length : downloaderN.length)}`;
      if (long_0_J <= 4.65) {
          let footballF = new Map([[String.fromCharCode(97,117,116,111,100,111,119,110,108,111,97,100,0),791], [String.fromCharCode(112,114,105,110,116,102,0),53], [String.fromCharCode(109,97,114,107,105,110,103,115,0),120]]);
         injury4 -= footballF.size;
      }
         notification6[`${injury4}`] = notification6.size;
      if (1.61 == (long_0_J + parseFloat(`${notification6.size}`))) {
         long_0_J *= parseFloat(`${parseInt(`${long_0_J}`) / (Math.max(1, 5))}`);
      }
          let fadfdeebbbfdabbbabdadfaaddaaX = [621, 494, 429];
         long_0_J *= parseFloat(`${fadfdeebbbfdabbbabdadfaaddaaX.length}`);
      if (1.74 == long_0_J) {
         notification6 = new Map([[`${injury4}`, parseInt(`${long_0_J}`)]]);
      }
      while (3.74 >= (injury4 * 2.80)) {
         injury4 /= Math.max(parseInt(`${long_0_J}`), 5);
         break;
      }
      tnewsq.push(plashE.length + 2);
      if (tnewsq.length == 3157007) {
         break;
      }
   } while ((tnewsq.length == 3157007) && (2 == (tnewsq.length << (Math.min(5, Math.abs(baseP))))));
   do {
      popup3 += "1";
      if (String.fromCharCode(112,50,116,54,0) == popup3) {
         break;
      }
   } while ((String.fromCharCode(112,50,116,54,0) == popup3) && (inactive_ != String.fromCharCode(76,0)));
   if ((inviteL.length % 1) >= 1) {
      plashE = [2];
   }
      inactive_ = `${2 | parseInt(`${loadingR}`)}`;
   do {
      telemetryZ += `${inviteL.length}`;
      if (String.fromCharCode(103,105,120,0) == telemetryZ) {
         break;
      }
   } while ((String.fromCharCode(103,105,120,0) == telemetryZ) && (telegram3 == String.fromCharCode(115,0) && telemetryZ.length > 3));
   if (4 == (parseInt(`${skipu}`) / (Math.max(5, hejim.length)))) {
       let philippines7 = new Map([[String.fromCharCode(102,105,108,108,101,100,0),616], [String.fromCharCode(115,105,103,104,97,110,100,108,101,114,0),319]]);
       let shirtn = 4.0;
         philippines7[`${shirtn}`] = parseInt(`${shirtn}`);
         philippines7[`${shirtn}`] = 1 << (Math.min(3, Math.abs(philippines7.size)));
          let scheduleM = [576, 646];
         philippines7[`${shirtn}`] = 2 + parseInt(`${shirtn}`);
         scheduleM = [scheduleM.length];
          let completew = String.fromCharCode(104,97,100,97,109,97,114,100,120,0);
          let sidep = 5.0;
          let default_bb = String.fromCharCode(110,111,110,114,100,0);
         shirtn *= parseFloat(`${philippines7.size}`);
         completew = "1";
         sidep /= Math.max(4, parseFloat(`${completew.length}`));
         default_bb = `${3 >> (Math.min(1, Math.abs(parseInt(`${sidep}`))))}`;
      do {
          let e_titleT = [668, 586];
         shirtn /= Math.max(parseFloat(`${philippines7.size + e_titleT.length}`), 4);
         if (shirtn == 4373541.0) {
            break;
         }
      } while ((shirtn == 4373541.0) && ((shirtn / (Math.max(parseFloat(`${philippines7.size}`), 5))) == 1.85 && 3.11 == (shirtn / 1.85)));
      for (let x = 0; x < 2; x++) {
         philippines7 = new Map([[`${philippines7.size}`, philippines7.size ^ parseInt(`${shirtn}`)]]);
      }
      skipu *= parseFloat(`${popup3.length << (Math.min(Math.abs(3), 2))}`);
   }
   while ((tnewsq.length % (Math.max(4, 9))) == 1 || (tnewsq.length % (Math.max(5, telegram3.length))) == 4) {
       let commonq = false;
         commonq = (commonq ? !commonq : commonq);
      for (let o = 0; o < 1; o++) {
         commonq = commonq && commonq;
      }
       let backwardF = true;
      telegram3 = `${greye.length | 1}`;
      break;
   }

};
export const RecommendCard = ({data}) => {
  const {match} = data;
  if (!match) {
    return <></>;
  }
  const {home, away, home_score, away_score, competition, sports_type} =
    data.match;

  const homeScore = calculateScore(home_score, sports_type);
  const awayScore = calculateScore(away_score, sports_type);
  const matchOnTimeGoing = matchOnTime(
    match.start_time,
    match?.sports_type,
    match.status,
    match.state,
  );

  const statusText = matchRunningStatusBasketball(
    match?.sports_type,
    match.status,
    match.state,
  );

  const Team = ({name, icon}) => {
    return (
      <View style={styles.teamGroup}>
        {icon && (
          <Image
            style={styles.teamIcon}
            source={{uri: icon}}
            resizeMode="contain"
          />
        )}
        <Text style={styles.teamName}>{limitText(name, 5)}</Text>
      </View>
    );
  };

  return (
    <SafeAreaView style={styles.card}>
      <View style={styles.titleRow}>
        <Text style={baseStyles.text}>
          {competition.name_short || competition.name}
        </Text>
        <View style={{flex: 1}}></View>
        <Image
          style={styles.icon}
          source={IconViewerGif}
          resizeMode="contain"
        />
        <Text style={baseStyles.text}>{formatViewerCount(data.view_num)}</Text>
      </View>
      <View style={styles.teamsRow}>
        <Team name={home.name_short || home.name} icon={home.icon} />
        <View style={styles.centerGroup}>
          <View style={styles.scoreGroup}>
            <Text style={styles.homeScore}>{homeScore}</Text>
            <Text style={styles.homeScore}>-</Text>
            <Text style={[styles.homeScore, styles.awayScore]}>
              {awayScore}
            </Text>
          </View>
          <Text style={baseStyles.text}>
            {statusText ? `${statusText}-` : ''}
            {matchOnTimeGoing}
          </Text>
        </View>
        <Team name={away.name_short || away.name} icon={away.icon} />
      </View>
    </SafeAreaView>
  );
};

const baseStyles = StyleSheet.create({
  text: {
    color: 'white',
    fontSize: 9,
    flexWrap: 'nowrap',
  },
});
const styles = StyleSheet.create({
  card: {
    backgroundColor: 'rgba(13, 13, 13, 0.9)',
    color: 'white',
    borderRadius: 8,
    flex: 1,
    paddingHorizontal: 5,
    paddingVertical: 10,
    margin: 5,
    display: 'flex',
  },
  titleRow: {
    flex: 0,
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    marginBottom: 10,
  },
  icon: {
    width: 20,
    height: 20,
    marginHorizontal: 10,
  },
  teamsRow: {
    flex: 0,
    flexDirection: 'row',
    justifyContent: 'space-between',
  },
  teamGroup: {
    alignItems: 'center',
  },
  teamIcon: {
    width: 30,
    height: 30,
  },
  teamName: {
    ...baseStyles.text,
    
  },
  centerGroup: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    marginTop: -5,
  },
  scoreGroup: {
    flex: 0,
    flexDirection: 'row',
  },
  homeScore: {
    ...baseStyles.text,
    marginHorizontal: 3,
    fontWeight: 'bold',
    fontSize: 11,
  },
  awayScore: {
    color: '#FAC33D',
  },
});
