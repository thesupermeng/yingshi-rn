import {Dimensions, ScrollView, Text, View} from 'react-native';
import {Colors} from '../../global/tt_umeng';
import {calculateScore} from '../../utility/utils';
import styles from './tt_with_success';

const LiveScoreBasketball = props => {
       let headerf = 4.0;
    let liveE = String.fromCharCode(120,95,51,56,95,101,110,117,109,101,114,97,116,101,100,0);
    let stepO = 3.0;
    let humidityG = [880, 847];
    let zhengpianB = [302, 520];
    let manifestX = [318, 149];
    let formD = String.fromCharCode(106,95,57,95,99,104,117,110,107,0);
       let hongkongb = 0.0;
         hongkongb /= Math.max(3, parseFloat(`${parseInt(`${hongkongb}`) + 1}`));
         hongkongb *= parseFloat(`${parseInt(`${hongkongb}`)}`);
      while (5.51 > hongkongb) {
         hongkongb /= Math.max(3, parseFloat(`${parseInt(`${hongkongb}`) + parseInt(`${hongkongb}`)}`));
         break;
      }
      headerf += parseInt(`${headerf}`);

  const middleWidth = Dimensions.get('window').width * 0.65 - 30;
   if ((manifestX.length / 3) == 5) {
      zhengpianB = [(String.fromCharCode(77,0) == liveE ? parseInt(`${headerf}`) : liveE.length)];
   }

  const {liveRoomUpdate} = props;
       let telegramF = 0.0;
         telegramF -= parseInt(`${telegramF}`) ^ parseInt(`${telegramF}`);
          let chatD = 1.0;
          let yellowi = 3;
          let privileger = [551, 906];
         telegramF *= 3;
         chatD -= parseFloat(`${2}`);
         yellowi -= privileger.length;
         privileger.push(1);
       let mutedp = String.fromCharCode(106,111,105,110,95,116,95,49,48,48,0);
       let alertj = String.fromCharCode(101,110,116,114,111,112,121,109,118,95,100,95,55,52,0);
      humidityG.push(2);

  if (!liveRoomUpdate) return;
   if (5 < formD.length) {
       let large3 = [44, 8, 523];
       let active4 = String.fromCharCode(112,95,54,49,95,112,108,97,121,97,98,108,101,0);
       let holderi = 1.0;
       let tooltipsl = new Map([[String.fromCharCode(116,95,56,95,98,110,100,101,99,0),395], [String.fromCharCode(114,97,115,116,101,114,105,122,97,116,105,111,110,95,101,95,56,57,0),800], [String.fromCharCode(116,114,97,110,115,112,111,115,101,95,49,95,56,54,0),137]]);
       let inviteK = String.fromCharCode(119,95,55,54,95,103,115,116,114,105,110,103,0);
         tooltipsl[inviteK] = tooltipsl.size;
      for (let a = 0; a < 3; a++) {
         holderi += (active4 == String.fromCharCode(115,0) ? active4.length : large3.length);
      }
         inviteK += `${3 - inviteK.length}`;
          let unreadi = String.fromCharCode(108,95,49,48,48,95,112,97,110,101,108,115,0);
         holderi -= parseInt(`${holderi}`) >> (Math.min(inviteK.length, 1));
         unreadi = "3";
          let incidentu = 1;
          let calendarx = true;
          let weiboG = String.fromCharCode(99,111,110,115,116,95,117,95,53,56,0);
         active4 = `${(inviteK == String.fromCharCode(111,0) ? inviteK.length : weiboG.length)}`;
         incidentu -= 2;
         calendarx = incidentu > 68;
         weiboG = `${((calendarx ? 3 : 2) | incidentu)}`;
         tooltipsl[active4] = active4.length % (Math.max(3, 1));
         holderi += large3.length / 1;
      if (active4.startsWith(`${inviteK.length}`)) {
          let infon = String.fromCharCode(120,109,112,101,103,95,48,95,51,54,0);
          let gpay2 = String.fromCharCode(99,111,108,108,97,116,105,111,110,95,101,95,53,57,0);
         active4 += `${gpay2.length + 1}`;
         infon += `${(infon == String.fromCharCode(77,0) ? infon.length : infon.length)}`;
         gpay2 += `${infon.length / (Math.max(3, 8))}`;
      }
       let resend_ = 1.0;
       let commonU = 0.0;
          let dplus0 = new Map([[String.fromCharCode(105,95,56,48,95,105,110,116,101,114,114,117,112,116,105,111,110,0),true ], [String.fromCharCode(112,101,114,102,111,114,109,95,116,95,52,49,0),false ]]);
         large3.push((String.fromCharCode(95,0) == active4 ? active4.length : inviteK.length));
         dplus0 = new Map([[`${dplus0.size}`, dplus0.size]]);
      if ((active4.length << (Math.min(Math.abs(4), 1))) == 2) {
         active4 += `${inviteK.length << (Math.min(Math.abs(3), 1))}`;
      }
      for (let e = 0; e < 2; e++) {
         active4 = `${inviteK.length}`;
      }
      for (let v = 0; v < 2; v++) {
         inviteK += `${(active4 == String.fromCharCode(53,0) ? active4.length : parseInt(`${resend_}`))}`;
      }
         resend_ /= Math.max(parseFloat(`${large3.length + active4.length}`), 1);
         large3 = [1 ^ large3.length];
      formD += `${inviteK.length}`;
   }

  const {home, away, home_score, away_score, sports_type, basketball_match} =
    liveRoomUpdate;
       let dplusU = String.fromCharCode(121,95,51,53,95,100,111,119,110,108,111,97,100,101,100,0);
         dplusU += "3";
          let rightw = 2.0;
          let roomg = 0;
          let main_mG = 1.0;
         dplusU = `${parseInt(`${main_mG}`) - 1}`;
         rightw /= Math.max(roomg * parseInt(`${rightw}`), 4);
         roomg &= parseInt(`${rightw}`) >> (Math.min(3, Math.abs(3)));
         main_mG -= parseFloat(`${2}`);
      for (let m = 0; m < 2; m++) {
         dplusU = "3";
      }
      formD = `${parseInt(`${headerf}`) - dplusU.length}`;

  const period_count = basketball_match?.period_count;
      humidityG.push(1 % (Math.max(6, parseInt(`${headerf}`))));

  const over_time_score = basketball_match?.over_time_score;
      liveE = `${humidityG.length << (Math.min(manifestX.length, 2))}`;

  const scores = [];
   if (5 <= (humidityG.length << (Math.min(manifestX.length, 2))) && (5 << (Math.min(5, humidityG.length))) <= 2) {
       let kleviny = String.fromCharCode(118,95,51,54,95,102,111,114,101,103,114,111,117,110,100,0);
       let z_center7 = 3.0;
       let sheet3 = new Map([[String.fromCharCode(98,111,114,105,110,103,115,115,108,95,49,95,49,0),true ], [String.fromCharCode(107,95,56,57,95,98,114,97,99,101,0),true ], [String.fromCharCode(109,97,116,114,105,99,101,115,95,55,95,51,54,0),true ]]);
         sheet3[kleviny] = kleviny.length * 3;
      while (parseInt(`${z_center7}`) <= kleviny.length) {
         z_center7 += parseInt(`${z_center7}`);
         break;
      }
         kleviny += "1";
         kleviny += `${sheet3.size | parseInt(`${z_center7}`)}`;
       let agreementb = String.fromCharCode(101,95,53,54,95,114,101,112,108,97,121,101,100,0);
       let calendarg = String.fromCharCode(122,95,52,49,95,115,112,111,116,0);
       let showF = String.fromCharCode(118,98,101,122,105,101,114,95,97,95,53,51,0);
      for (let f = 0; f < 1; f++) {
          let descD = String.fromCharCode(98,95,49,56,95,117,110,105,102,105,101,100,0);
          let untickv = [877, 728, 747];
          let matchV = 1.0;
          let countdown7 = String.fromCharCode(98,105,116,114,118,99,111,110,106,95,50,95,50,56,0);
          let tempB = 0.0;
         showF = `${untickv.length + agreementb.length}`;
         descD += `${(countdown7 == String.fromCharCode(53,0) ? parseInt(`${matchV}`) : countdown7.length)}`;
         untickv = [2];
         matchV += parseInt(`${matchV}`) & descD.length;
         tempB /= Math.max(1, (countdown7 == String.fromCharCode(85,0) ? parseInt(`${tempB}`) : countdown7.length));
      }
      if (agreementb.length >= 1 && calendarg != String.fromCharCode(85,0)) {
          let commenth = 0.0;
          let flipperp = 5.0;
         agreementb = `${1 - sheet3.size}`;
         commenth *= parseFloat(`${parseInt(`${flipperp}`) << (Math.min(4, Math.abs(parseInt(`${commenth}`))))}`);
         flipperp /= Math.max(parseFloat(`${parseInt(`${commenth}`)}`), 2);
      }
          let description_pr = String.fromCharCode(116,119,101,97,107,95,118,95,57,56,0);
          let guide6 = 4.0;
          let profileU = 2.0;
         sheet3 = new Map([[showF, kleviny.length]]);
         description_pr = "2";
         guide6 -= parseInt(`${profileU}`);
      humidityG.push(parseInt(`${headerf}`) << (Math.min(1, Math.abs(parseInt(`${stepO}`)))));
   }

  

   if ((3 ^ manifestX.length) >= 2) {
      manifestX.push(humidityG.length);
   }
  const team = {
    header: '球队',
    home: home?.name_short ? home?.name_short : home?.name,
    away: away?.name_short ? away?.name_short : away?.name,
  };
   if ((humidityG.length - liveE.length) == 3 && 3 == (humidityG.length - liveE.length)) {
      liveE += `${(String.fromCharCode(56,0) == liveE ? liveE.length : humidityG.length)}`;
   }

  

      humidityG = [formD.length];
  const isZeroScore = score => {
    return !score ? '-' : score;
   for (let s = 0; s < 2; s++) {
      manifestX.push(humidityG.length);
   }

  };
   while (formD.length <= zhengpianB.length) {
      formD = `${zhengpianB.length}`;
      break;
   }

  

       let rewind9 = String.fromCharCode(115,104,117,116,116,101,114,95,50,95,53,52,0);
         rewind9 += `${rewind9.length + 2}`;
          let rank3 = String.fromCharCode(120,95,54,55,95,114,101,97,115,109,0);
          let sliderp = 5.0;
          let mbnativeadvancedx = 4.0;
         rewind9 += "2";
         rank3 += `${parseInt(`${sliderp}`) % 2}`;
         sliderp /= Math.max(parseFloat(`${1 >> (Math.min(2, Math.abs(parseInt(`${sliderp}`))))}`), 1);
         mbnativeadvancedx += rank3.length;
      if (rewind9 != rewind9) {
         rewind9 = `${rewind9.length & 1}`;
      }
      headerf /= Math.max(formD.length, 4);
  const scoreHeaderName = (period_count, index) => {

   for (let b = 0; b < 3; b++) {
      stepO += humidityG.length;
   }
    if (period_count === 2) {

   for (let g = 0; g < 3; g++) {
       let fastforwardZ = String.fromCharCode(107,95,52,51,95,105,110,115,101,114,116,105,111,110,0);
       let windP = [467, 40, 343];
       let aboutj = new Map([[String.fromCharCode(105,95,52,55,95,100,101,108,97,117,110,97,121,0),824], [String.fromCharCode(111,112,101,110,99,108,95,111,95,54,49,0),199]]);
       let championO = String.fromCharCode(114,95,49,49,95,106,99,111,110,102,105,103,0);
       let matche = 0.0;
       let close0 = String.fromCharCode(104,101,97,100,95,121,95,53,54,0);
       let annerG = String.fromCharCode(118,109,97,116,114,105,120,95,101,95,50,52,0);
         annerG = `${aboutj.size}`;
          let libcrashsdkH = 3.0;
         windP = [parseInt(`${matche}`)];
         libcrashsdkH *= parseInt(`${libcrashsdkH}`) | 3;
      for (let f = 0; f < 1; f++) {
         close0 = `${championO.length - 3}`;
      }
       let matchesc = String.fromCharCode(116,95,56,57,95,98,97,115,101,112,111,105,110,116,0);
       let crownh = String.fromCharCode(101,110,113,117,101,117,101,100,95,102,95,48,0);
       let plashc = String.fromCharCode(105,95,57,52,0);
      for (let j = 0; j < 1; j++) {
         windP.push(1);
      }
         fastforwardZ += `${(String.fromCharCode(52,0) == crownh ? crownh.length : aboutj.size)}`;
       let previewO = 3.0;
       let half2 = 0.0;
      while (3 == (matchesc.length * 1)) {
         matchesc = `${3 & plashc.length}`;
         break;
      }
         fastforwardZ = `${parseInt(`${half2}`)}`;
         matchesc = `${fastforwardZ.length >> (Math.min(Math.abs(2), 4))}`;
         annerG += `${crownh.length}`;
          let sliderB = 5;
          let networko = 0;
         windP.push(3);
         sliderB %= Math.max(sliderB / 1, 3);
         networko %= Math.max(sliderB + 2, 5);
         matchesc = `${parseInt(`${previewO}`) % 1}`;
      stepO += fastforwardZ.length ^ formD.length;
   }
      if (index === 0) return '上半场';
   do {
       let middle4 = 4.0;
       let navigations = String.fromCharCode(101,120,112,111,114,116,95,98,95,55,49,0);
       let animation8 = String.fromCharCode(103,114,101,103,111,114,105,97,110,95,54,95,53,50,0);
       let anewinterstitialm = true;
       let pnewinterstitialZ = String.fromCharCode(114,95,54,48,95,100,105,115,97,112,112,101,97,114,97,110,99,101,0);
         navigations += `${((anewinterstitialm ? 1 : 3) - parseInt(`${middle4}`))}`;
      while (!navigations.endsWith(`${anewinterstitialm}`)) {
          let resend2 = new Map([[String.fromCharCode(108,105,110,101,115,95,111,95,51,52,0),651], [String.fromCharCode(109,95,53,50,95,109,101,109,99,109,112,0),952], [String.fromCharCode(111,95,56,54,95,99,115,119,97,112,0),198]]);
          let overlay4 = true;
         anewinterstitialm = ((navigations.length & (overlay4 ? navigations.length : 83)) <= 83);
         resend2 = new Map([[`${resend2.size}`, resend2.size | 2]]);
         overlay4 = resend2.size <= 73;
         break;
      }
      while (!navigations.startsWith(animation8)) {
         navigations += `${animation8.length % 3}`;
         break;
      }
       let stringm = String.fromCharCode(111,98,106,116,120,116,95,119,95,57,53,0);
       let friendsU = [405, 593, 617];
       let f_position9 = [903, 165];
       let pointt = new Map([[String.fromCharCode(98,114,97,110,99,104,95,101,95,55,51,0),518], [String.fromCharCode(117,95,55,51,95,112,101,101,114,99,111,110,110,101,99,116,105,111,110,105,110,116,101,114,102,97,99,101,0),74], [String.fromCharCode(100,97,109,112,95,107,95,49,57,0),357]]);
         stringm += `${parseInt(`${middle4}`) + pnewinterstitialZ.length}`;
      do {
         middle4 -= parseFloat(`${navigations.length}`);
         if (4087349.0 == middle4) {
            break;
         }
      } while ((4087349.0 == middle4) && (!Array.from(pointt.values()).includes(middle4)));
      for (let r = 0; r < 1; r++) {
         pointt = new Map([[`${pointt.size}`, pointt.size]]);
      }
      for (let a = 0; a < 2; a++) {
         anewinterstitialm = animation8 == String.fromCharCode(53,0);
      }
      do {
         navigations = `${3 ^ parseInt(`${middle4}`)}`;
         if (navigations == String.fromCharCode(106,98,106,99,106,103,50,98,113,0)) {
            break;
         }
      } while ((animation8.length == 2 || navigations == String.fromCharCode(78,0)) && (navigations == String.fromCharCode(106,98,106,99,106,103,50,98,113,0)));
      do {
         friendsU.push(f_position9.length & pointt.size);
         if (friendsU.length == 607135) {
            break;
         }
      } while (((2 | friendsU.length) < 3) && (friendsU.length == 607135));
      for (let e = 0; e < 2; e++) {
         anewinterstitialm = 72 >= navigations.length;
      }
         anewinterstitialm = pnewinterstitialZ == navigations;
       let aboutt = new Map([[String.fromCharCode(106,95,52,49,95,100,105,115,116,112,111,105,110,116,0),true ], [String.fromCharCode(100,111,116,116,101,100,95,118,95,49,52,0),false ], [String.fromCharCode(104,97,115,104,101,100,95,114,95,51,56,0),true ]]);
       let overlayG = new Map([[String.fromCharCode(110,95,57,50,95,105,109,112,117,108,115,101,0),String.fromCharCode(100,101,110,111,114,109,97,108,95,107,95,55,50,0)], [String.fromCharCode(100,105,115,116,114,105,99,116,95,53,95,49,51,0),String.fromCharCode(112,114,102,116,95,100,95,51,57,0)], [String.fromCharCode(103,101,111,99,111,100,101,100,95,110,95,52,54,0),String.fromCharCode(121,117,118,112,108,97,110,101,95,100,95,56,49,0)]]);
      headerf /= Math.max(1, 3);
      if (3373766.0 == headerf) {
         break;
      }
   } while ((liveE.length < parseInt(`${headerf}`)) && (3373766.0 == headerf));

      if (index === 1) return '下半场';
      stepO *= 2;

    }
    if (period_count === 4) {

      headerf += manifestX.length & humidityG.length;
      return `Q${index + 1}`;
   while ((manifestX.length & zhengpianB.length) > 1 && (1 & manifestX.length) > 4) {
      zhengpianB.push(3 / (Math.max(2, zhengpianB.length)));
      break;
   }

    }
  };
   if (5 <= zhengpianB.length) {
      zhengpianB = [humidityG.length * parseInt(`${stepO}`)];
   }

  

       let lange = String.fromCharCode(102,95,52,56,95,114,101,116,114,97,110,109,105,115,115,105,111,110,0);
       let streamingi = String.fromCharCode(104,95,57,54,95,118,101,114,108,97,121,0);
       let options6 = true;
       let specX = 5;
       let roundI = 0;
       let pauset = String.fromCharCode(115,116,114,101,97,109,97,98,108,101,95,51,95,56,49,0);
         pauset = `${((options6 ? 1 : 1) * specX)}`;
      do {
          let inactiveP = 3.0;
          let sportw = true;
          let episode4 = 3.0;
         specX %= Math.max(5, 3 | parseInt(`${inactiveP}`));
         inactiveP -= parseFloat(`${parseInt(`${episode4}`) & 3}`);
         sportw = 66.6 >= episode4;
         if (2298697 == specX) {
            break;
         }
      } while ((2298697 == specX) && (5 >= (specX ^ 5) && 5 >= (5 ^ specX)));
      while (!options6 && streamingi.length == 5) {
         streamingi += `${streamingi.length & 3}`;
         break;
      }
      while (streamingi.length == pauset.length) {
         streamingi += "3";
         break;
      }
         roundI <<= Math.min(1, Math.abs(specX));
      do {
         options6 = 74 > roundI;
         if (options6 ? !options6 : options6) {
            break;
         }
      } while ((5 <= lange.length) && (options6 ? !options6 : options6));
          let philippinesY = 2.0;
         specX >>= Math.min(3, Math.abs(roundI));
         philippinesY -= 3;
      zhengpianB = [parseInt(`${headerf}`)];
  for (let index = 0; index <= period_count; index++) {

       let calendar5 = String.fromCharCode(112,101,114,109,105,115,115,105,111,110,95,114,95,53,48,0);
       let penaltyu = String.fromCharCode(114,101,104,97,115,104,95,102,95,53,53,0);
          let appleP = String.fromCharCode(100,95,49,50,95,103,101,110,104,0);
         calendar5 += `${calendar5.length % 3}`;
         appleP = `${appleP.length}`;
      do {
          let videojsQ = [String.fromCharCode(117,95,56,49,95,120,100,97,105,0), String.fromCharCode(117,95,50,48,95,100,105,97,108,108,105,110,103,0)];
         calendar5 += `${videojsQ.length + 3}`;
         if (String.fromCharCode(113,109,106,51,0) == calendar5) {
            break;
         }
      } while ((String.fromCharCode(113,109,106,51,0) == calendar5) && (calendar5 == String.fromCharCode(78,0) || 4 >= penaltyu.length));
      do {
          let miniH = new Map([[String.fromCharCode(114,111,113,118,105,100,101,111,95,109,95,49,52,0),String.fromCharCode(114,95,52,57,95,97,97,99,101,110,99,116,97,98,0)], [String.fromCharCode(101,99,111,109,112,114,101,115,115,111,114,95,99,95,57,54,0),String.fromCharCode(107,101,121,115,112,101,99,95,110,95,53,48,0)]]);
          let shirtr = 1.0;
         calendar5 += `${miniH.size / (Math.max(calendar5.length, 1))}`;
         miniH[`${shirtr}`] = parseInt(`${shirtr}`);
         if (3401627 == calendar5.length) {
            break;
         }
      } while ((3401627 == calendar5.length) && (4 < penaltyu.length));
         penaltyu = `${penaltyu.length + 1}`;
         penaltyu += "2";
         penaltyu = `${calendar5.length & 2}`;
      stepO *= (penaltyu == String.fromCharCode(71,0) ? penaltyu.length : humidityG.length);
    if (index === period_count) {

       let entryZ = true;
      if (entryZ) {
         entryZ = !entryZ;
      }
       let combine9 = String.fromCharCode(117,95,50,50,0);
       let progressx = String.fromCharCode(108,95,53,50,95,98,105,116,114,101,97,100,101,114,0);
       let sharedG = String.fromCharCode(102,95,54,50,95,114,101,115,116,97,107,101,0);
       let nterstitialm = String.fromCharCode(117,110,115,97,118,101,100,95,56,95,50,54,0);
      formD += `${parseInt(`${headerf}`)}`;
      if (over_time_score?.length > 0) {

       let tickedq = String.fromCharCode(105,95,54,53,95,111,118,101,114,108,97,121,0);
       let regengb = [896, 464, 664];
       let eventa = String.fromCharCode(100,95,54,95,115,108,102,0);
         regengb = [tickedq.length - 3];
      do {
          let yellowP = false;
         tickedq = `${tickedq.length}`;
         if (324466 == tickedq.length) {
            break;
         }
      } while ((eventa.length >= tickedq.length) && (324466 == tickedq.length));
          let package_bN = String.fromCharCode(115,116,111,114,101,100,95,104,95,54,56,0);
         regengb = [tickedq.length];
         package_bN = `${package_bN.length}`;
         eventa = "2";
         eventa = `${1 >> (Math.min(3, eventa.length))}`;
      for (let s = 0; s < 1; s++) {
         regengb = [(eventa == String.fromCharCode(72,0) ? tickedq.length : eventa.length)];
      }
      while (3 == regengb.length) {
         regengb = [tickedq.length];
         break;
      }
      if (4 >= regengb.length) {
         eventa += `${regengb.length ^ 3}`;
      }
         regengb = [regengb.length ^ 2];
      formD += "1";
        for (let idx = 0; idx <= over_time_score.length; idx++) {

      humidityG.push((formD == String.fromCharCode(76,0) ? formD.length : zhengpianB.length));
          scores.push({
            header: `OT${idx + 1}`,
            home: isZeroScore(home_score[idx]),
            away: isZeroScore(away_score[idx]),
          });
        }
      } else if (home_score[index] && away_score[index]) {

   for (let e = 0; e < 1; e++) {
       let humidityh = [524, 54, 2];
       let popupn = 2;
       let mbridge5 = 4;
         mbridge5 <<= Math.min(2, Math.abs(popupn));
          let videoD = 0.0;
          let albumU = String.fromCharCode(120,95,56,52,95,115,99,114,97,116,99,104,0);
         popupn <<= Math.min(2, humidityh.length);
         videoD *= parseFloat(`${parseInt(`${videoD}`) - 3}`);
         albumU = `${albumU.length & parseInt(`${videoD}`)}`;
         mbridge5 -= mbridge5 / (Math.max(2, 4));
      while ((mbridge5 % (Math.max(humidityh.length, 4))) < 4) {
         mbridge5 |= humidityh.length;
         break;
      }
         popupn &= popupn - mbridge5;
         humidityh.push(mbridge5 + humidityh.length);
         mbridge5 *= 3 << (Math.min(4, Math.abs(mbridge5)));
      for (let f = 0; f < 3; f++) {
         humidityh = [mbridge5 ^ 1];
      }
         humidityh = [humidityh.length];
      liveE += "3";
   }
        scores.push({
          header: 'OT1',
          home: isZeroScore(home_score[index]),
          away: isZeroScore(away_score[index]),
        });
      }
    } else {

      liveE = `${parseInt(`${headerf}`) * zhengpianB.length}`;
      scores.push({
        header: scoreHeaderName(period_count, index),
        home: isZeroScore(home_score[index]),
        away: isZeroScore(away_score[index]),
      });
      headerf += liveE.length;

    }
  }
  

   for (let t = 0; t < 2; t++) {
      zhengpianB.push(zhengpianB.length);
   }
  const totalScore = {
    header: '总分',
    home: calculateScore(home_score, sports_type),
    away: calculateScore(away_score, sports_type),
  };
  // 判断分数的总长度，给予相对的长度 列子（ 4节+OT1+OT2+OT3 > MiddleWidth ? 1节长度*节数 : view的长度）
  const ScrollViewWidth = (width, length) => {
    return width * length >= middleWidth ? width * length : middleWidth;
      zhengpianB = [3];

  };
  const divider = () => {
    return (
      <View
        style={{
          borderBottomColor: Colors.colorLightGrey,
          borderBottomWidth: 0.5,
          width: 100,
        }}></View>
    );
  };

  return (
    <View style={styles.container}>
      <View style={styles.left}>
        <Text style={styles.textTeamHeader}>{team.header}</Text>
        {divider()}
        <Text style={styles.textTeam} numberOfLines={1}>
          {team.home}
        </Text>
        {divider()}
        <Text style={styles.textTeam} numberOfLines={1}>
          {team.away}
        </Text>
      </View>
      <View style={styles.middle}>
        <ScrollView
          horizontal={true}
          showsHorizontalScrollIndicator={false}
          contentContainerStyle={{width: ScrollViewWidth(56, scores.length)}}>
          <View
            style={{
              flex: 1,
              flexDirection: 'row',
              justifyContent: 'space-evenly',
              
            }}>
            {scores.map((x, idx) => {
              return (
                <View
                  style={styles.scores}
                  key={`liveScoreBasketball-${liveRoomUpdate?.id}-${idx}`}>
                  <Text style={styles.textScoreHeader}>{x.header}</Text>
                  {divider()}
                  <Text style={styles.textScore}>{x.home}</Text>
                  {divider()}
                  <Text style={styles.textScore}>{x.away}</Text>
                </View>
              );
            })}
          </View>
        </ScrollView>
      </View>
      <View style={styles.right}>
        <Text style={styles.textScoreHeader}>{totalScore.header}</Text>
        {divider()}
        <Text style={styles.textScore}>{totalScore.home}</Text>
        {divider()}
        <Text style={styles.textScore}>{totalScore.away}</Text>
      </View>
    </View>
  );
};

export default LiveScoreBasketball;
