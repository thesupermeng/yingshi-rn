import {Dimensions, ScrollView, Text, View} from 'react-native';
import {Colors} from '../../global/yys_penaltymatchicon_iconarrowrightorange';
import {calculateScore} from '../../utility/utils';
import styles from './yys_nbatrophy';

const LiveScoreBasketball = props => {
       let emojic = [56, 632];
    let temperatureQ = [393, 3];
    let loadingk = String.fromCharCode(119,95,51,56,95,97,99,116,105,111,110,115,0);
    let moreQ = 2.0;
    let libnmsv = String.fromCharCode(108,95,50,49,95,101,116,119,111,114,107,0);
    let heartS = new Map([[String.fromCharCode(104,95,56,53,95,105,110,99,108,117,115,105,111,110,0),String.fromCharCode(108,97,99,105,110,103,0)], [String.fromCharCode(116,122,102,105,108,101,95,52,95,51,0),String.fromCharCode(117,112,99,97,115,101,0)]]);
    let disconnectedlogom = String.fromCharCode(105,110,116,101,114,108,97,99,101,100,0);
    let areaX = new Map([[String.fromCharCode(110,111,110,98,108,111,99,107,0),366], [String.fromCharCode(111,95,49,55,95,111,102,102,101,114,115,0),375]]);
    let membershipP = String.fromCharCode(110,111,108,111,99,107,95,114,95,53,57,0);
    let latnf = false;
    let lightO = new Map([[String.fromCharCode(110,95,52,95,116,114,97,99,101,114,0),519], [String.fromCharCode(104,95,54,53,0),803], [String.fromCharCode(111,99,99,117,114,114,101,110,99,101,115,95,116,95,51,48,0),105]]);
    let backwhitee = 1;
    let regengp = 0.0;
    let basketballicon8 = String.fromCharCode(114,105,110,103,95,99,95,49,52,0);
       let greyarrowupd = [789, 490, 157];
       let iconcalendars = new Map([[String.fromCharCode(114,101,109,117,120,101,114,95,116,95,52,55,0),672], [String.fromCharCode(102,95,49,56,95,115,105,103,110,117,109,0),735]]);
       let goallogoV = [19, 245, 231];
      do {
         goallogoV.push(goallogoV.length + iconcalendars.size);
         if (3103668 == goallogoV.length) {
            break;
         }
      } while (((4 + goallogoV.length) <= 5 || (greyarrowupd.length + goallogoV.length) <= 4) && (3103668 == goallogoV.length));
         greyarrowupd = [2];
      while (3 >= iconcalendars.size) {
         iconcalendars[`${greyarrowupd.length}`] = 2;
         break;
      }
          let internett = true;
          let libsentryp = false;
         iconcalendars[`${libsentryp}`] = ((libsentryp ? 4 : 2) * iconcalendars.size);
         internett = (internett ? !internett : !internett);
          let moviesj = String.fromCharCode(116,114,117,101,104,100,95,116,95,54,53,0);
          let bannerz = [12, 577, 645];
         greyarrowupd.push(goallogoV.length >> (Math.min(3, bannerz.length)));
         moviesj = `${moviesj.length % 3}`;
         bannerz.push(2 >> (Math.min(3, moviesj.length)));
      for (let h = 0; h < 2; h++) {
         goallogoV.push(iconcalendars.size >> (Math.min(Math.abs(2), 5)));
      }
      if (goallogoV.length > greyarrowupd.length) {
         goallogoV = [2];
      }
          let traminiW = 5.0;
         greyarrowupd.push(parseInt(`${traminiW}`) << (Math.min(greyarrowupd.length, 4)));
         goallogoV = [1 << (Math.min(2, greyarrowupd.length))];
      moreQ += 3;

  const middleWidth = Dimensions.get('window').width * 0.65 - 30;
       let short_1fg = [851, 212];
       let room9 = [367, 90];
       let update_iE = String.fromCharCode(109,97,105,108,95,109,95,56,53,0);
         room9 = [update_iE.length + short_1fg.length];
         update_iE += `${room9.length | 2}`;
          let showmoreM = new Map([[String.fromCharCode(99,95,55,50,95,106,112,101,103,100,119,116,0),true ], [String.fromCharCode(115,95,56,95,114,101,115,105,122,101,100,0),true ]]);
          let manifestB = 0;
          let windf = String.fromCharCode(115,105,103,97,108,103,95,48,95,51,0);
         room9.push(3);
         showmoreM[`${manifestB}`] = showmoreM.size ^ manifestB;
         windf += "1";
          let pauseg = String.fromCharCode(116,114,97,99,107,101,100,0);
          let countryL = 5.0;
         short_1fg.push(parseInt(`${countryL}`) + 1);
         pauseg += `${2 << (Math.min(2, pauseg.length))}`;
         countryL += (parseFloat(`${String.fromCharCode(108,0) == pauseg ? pauseg.length : pauseg.length}`));
      do {
         short_1fg = [update_iE.length / (Math.max(3, 4))];
         if (858232 == short_1fg.length) {
            break;
         }
      } while ((!room9.includes(short_1fg.length)) && (858232 == short_1fg.length));
      while (room9.length == update_iE.length) {
         update_iE += `${room9.length * short_1fg.length}`;
         break;
      }
         room9.push(update_iE.length * 3);
      while (1 <= (room9.length % 3) && 3 <= (short_1fg.length % (Math.max(3, room9.length)))) {
         short_1fg = [update_iE.length];
         break;
      }
       let grayb = String.fromCharCode(115,101,116,115,111,99,107,111,112,116,95,106,95,57,0);
       let volumes = String.fromCharCode(117,95,55,56,0);
      heartS[update_iE] = 2 >> (Math.min(4, room9.length));

  const {liveRoomUpdate} = props;
   for (let i = 0; i < 2; i++) {
      membershipP = `${loadingk.length}`;
   }

  if (!liveRoomUpdate) return;
   do {
      moreQ /= Math.max(1, areaX.size / (Math.max(1, 4)));
      if (moreQ == 2468786.0) {
         break;
      }
   } while ((!Array.from(areaX.values()).includes(moreQ)) && (moreQ == 2468786.0));

  const {home, away, home_score, away_score, sports_type, basketball_match} =
    liveRoomUpdate;
      libnmsv = `${areaX.size}`;

  const period_count = basketball_match?.period_count;
       let twitterz = String.fromCharCode(98,108,97,107,101,98,95,111,95,52,52,0);
       let lessl = [617, 266, 118];
       let basketballdetailsbgG = false;
       let fillo = String.fromCharCode(111,95,55,54,95,98,117,105,108,116,105,110,0);
      while (!basketballdetailsbgG || 3 >= fillo.length) {
         basketballdetailsbgG = 8 <= twitterz.length;
         break;
      }
         fillo = `${1 << (Math.min(2, fillo.length))}`;
         basketballdetailsbgG = fillo.length == 19 || basketballdetailsbgG;
      do {
          let fieldh = true;
          let subtextP = false;
          let episode0 = String.fromCharCode(102,108,105,112,112,101,100,0);
          let imagenetworkerrF = [863, 788, 426];
         lessl = [1];
         fieldh = 17 > imagenetworkerrF.length;
         subtextP = imagenetworkerrF.length < episode0.length;
         episode0 = "3";
         if (2322254 == lessl.length) {
            break;
         }
      } while ((twitterz.endsWith(`${lessl.length}`)) && (2322254 == lessl.length));
      if ((twitterz.length & lessl.length) >= 5) {
         twitterz += `${twitterz.length}`;
      }
      for (let m = 0; m < 1; m++) {
          let googlef = String.fromCharCode(120,95,55,57,95,110,112,97,116,99,104,101,115,0);
          let suggestiont = false;
         fillo += "1";
         googlef = `${3 ^ googlef.length}`;
         suggestiont = googlef == String.fromCharCode(112,0);
      }
      do {
         basketballdetailsbgG = 98 == lessl.length && fillo == String.fromCharCode(120,0);
         if (basketballdetailsbgG ? !basketballdetailsbgG : basketballdetailsbgG) {
            break;
         }
      } while ((basketballdetailsbgG ? !basketballdetailsbgG : basketballdetailsbgG) && (fillo.length <= 2));
         fillo = `${lessl.length}`;
      libnmsv += `${1 % (Math.max(5, heartS.size))}`;

  const over_time_score = basketball_match?.over_time_score;
   do {
      membershipP += "3";
      if (membershipP == String.fromCharCode(55,105,103,119,0)) {
         break;
      }
   } while ((membershipP == String.fromCharCode(55,105,103,119,0)) && (!membershipP.endsWith(`${libnmsv.length}`)));

  const scores = [];
   for (let a = 0; a < 1; a++) {
      moreQ /= Math.max(membershipP.length, 4);
   }

  

      emojic = [(String.fromCharCode(102,0) == disconnectedlogom ? temperatureQ.length : disconnectedlogom.length)];
  const team = {
    header: '球队',
    home: home?.name_short ? home?.name_short : home?.name,
    away: away?.name_short ? away?.name_short : away?.name,
  };
      latnf = loadingk.length == 65 && 65 == areaX.size;

  

   do {
      areaX[libnmsv] = (String.fromCharCode(88,0) == libnmsv ? libnmsv.length : parseInt(`${moreQ}`));
      if (3553722 == areaX.size) {
         break;
      }
   } while ((!loadingk.endsWith(`${areaX.size}`)) && (3553722 == areaX.size));
  const isZeroScore = score => {
    return !score ? '-' : score;
   do {
      latnf = membershipP.length >= temperatureQ.length;
      if (latnf ? !latnf : latnf) {
         break;
      }
   } while ((latnf ? !latnf : latnf) && (!membershipP.endsWith(`${latnf}`)));

  };
      areaX = new Map([[membershipP, ((latnf ? 2 : 3))]]);

  

   for (let p = 0; p < 3; p++) {
       let champion1 = String.fromCharCode(106,115,105,109,100,99,102,103,95,113,95,50,53,0);
       let analyticsL = String.fromCharCode(116,97,108,107,95,106,95,49,53,0);
         analyticsL += `${1 | analyticsL.length}`;
         champion1 = `${champion1.length}`;
         champion1 = `${(String.fromCharCode(98,0) == champion1 ? champion1.length : analyticsL.length)}`;
      do {
          let robotoF = String.fromCharCode(114,101,99,111,110,105,110,116,114,97,120,95,103,95,50,57,0);
          let plashl = [625, 524, 211];
          let private_oqk = String.fromCharCode(100,101,112,115,95,56,95,53,50,0);
         analyticsL = `${champion1.length}`;
         robotoF += "3";
         plashl.push(plashl.length >> (Math.min(robotoF.length, 5)));
         private_oqk += `${plashl.length % (Math.max(1, 5))}`;
         if (analyticsL.length == 1568128) {
            break;
         }
      } while ((analyticsL.length == 1568128) && (!champion1.startsWith(analyticsL)));
          let huaweiA = 1.0;
         champion1 += `${analyticsL.length ^ 3}`;
         huaweiA *= parseFloat(`${parseInt(`${huaweiA}`)}`);
      do {
         analyticsL = `${analyticsL.length / (Math.max(champion1.length, 6))}`;
         if (String.fromCharCode(116,53,51,103,111,0) == analyticsL) {
            break;
         }
      } while ((!analyticsL.endsWith(champion1)) && (String.fromCharCode(116,53,51,103,111,0) == analyticsL));
      disconnectedlogom = `${((latnf ? 4 : 3) ^ emojic.length)}`;
   }
  const scoreHeaderName = (period_count, index) => {

       let libreactnativejniM = String.fromCharCode(120,95,53,54,95,99,99,105,116,116,0);
       let iconcurrentmatch_ = false;
      if (!iconcurrentmatch_) {
         libreactnativejniM += `${(String.fromCharCode(77,0) == libreactnativejniM ? (iconcurrentmatch_ ? 4 : 1) : libreactnativejniM.length)}`;
      }
       let combineX = 3.0;
         iconcurrentmatch_ = ((libreactnativejniM.length & (!iconcurrentmatch_ ? 86 : libreactnativejniM.length)) <= 86);
       let textinputi = 4.0;
       let reactnativejsk = 3.0;
      if (2.87 <= (4.4 + combineX)) {
         combineX -= parseFloat(`${1}`);
      }
         combineX /= Math.max(1, (parseFloat(`${parseInt(`${textinputi}`) & (iconcurrentmatch_ ? 2 : 5)}`)));
      heartS[libnmsv] = 3 ^ libnmsv.length;
    if (period_count === 2) {

   if ((5 & heartS.size) <= 2) {
       let largec = [189, 836];
       let catalogB = 1.0;
       let defaultfootballbgr = new Map([[String.fromCharCode(111,95,54,51,95,114,101,99,101,110,116,0),757], [String.fromCharCode(102,102,116,103,95,122,95,50,56,0),731]]);
      if (Array.from(defaultfootballbgr.keys()).includes(`${catalogB}`)) {
         catalogB += parseInt(`${catalogB}`) << (Math.min(Math.abs(defaultfootballbgr.size), 2));
      }
         largec = [largec.length / (Math.max(9, defaultfootballbgr.size))];
         catalogB *= defaultfootballbgr.size % (Math.max(1, 2));
         catalogB /= Math.max(defaultfootballbgr.size >> (Math.min(Math.abs(3), 4)), 2);
         catalogB -= 2;
      if (4 <= (2 * largec.length) || (parseInt(`${catalogB}`) / (Math.max(largec.length, 7))) <= 2) {
         largec = [parseInt(`${catalogB}`) % (Math.max(largec.length, 5))];
      }
       let iconnewssharer = 1.0;
       let stepU = 4.0;
         stepU -= parseFloat(`${parseInt(`${catalogB}`) % (Math.max(3, 10))}`);
         catalogB -= parseInt(`${catalogB}`) | 1;
      heartS = new Map([[`${heartS.size}`, 2 >> (Math.min(4, largec.length))]]);
   }
      if (index === 0) return '上半场';
   while (libnmsv.length <= moreQ) {
       let middlebrightnessg = 3;
       let expiredB = 5.0;
         expiredB /= Math.max(parseInt(`${expiredB}`), 3);
         expiredB *= 3;
      do {
         expiredB += middlebrightnessg;
         if (expiredB == 2847059.0) {
            break;
         }
      } while ((expiredB == 2847059.0) && (5.90 <= (expiredB / 2.47)));
          let redcirclebgl = new Map([[String.fromCharCode(98,105,100,105,114,95,120,95,51,54,0),764], [String.fromCharCode(104,105,103,104,98,105,116,115,95,53,95,55,49,0),697], [String.fromCharCode(97,99,116,117,97,108,105,122,101,100,95,105,95,50,52,0),718]]);
          let appleA = String.fromCharCode(116,95,49,53,95,114,116,99,112,0);
          let nativemodule0 = 5;
         expiredB *= middlebrightnessg | 3;
         redcirclebgl = new Map([[appleA, 1 & appleA.length]]);
         nativemodule0 ^= appleA.length;
      while ((expiredB - 1.56) > 1.97) {
          let sidei = [754, 907];
         middlebrightnessg &= parseInt(`${expiredB}`);
         sidei.push(2);
         break;
      }
         expiredB /= Math.max(2, 3 >> (Math.min(Math.abs(parseInt(`${expiredB}`)), 5)));
      moreQ -= middlebrightnessg | emojic.length;
      break;
   }

      if (index === 1) return '下半场';
   do {
       let refreshborderlessA = String.fromCharCode(108,111,111,107,117,112,95,57,95,49,54,0);
       let loginsuccessQ = String.fromCharCode(104,97,115,104,116,97,98,108,101,122,95,117,95,56,0);
         loginsuccessQ += `${refreshborderlessA.length & loginsuccessQ.length}`;
      for (let m = 0; m < 3; m++) {
          let search9 = 0.0;
         loginsuccessQ = `${2 >> (Math.min(3, loginsuccessQ.length))}`;
         search9 *= parseInt(`${search9}`);
      }
         loginsuccessQ = `${refreshborderlessA.length * loginsuccessQ.length}`;
          let chinasameB = 1.0;
          let notificationfillempty8 = true;
         loginsuccessQ = `${refreshborderlessA.length}`;
         chinasameB *= parseFloat(`${parseInt(`${chinasameB}`) + parseInt(`${chinasameB}`)}`);
         notificationfillempty8 = 36.49 < (chinasameB / (Math.max(chinasameB, 7)));
         loginsuccessQ = `${loginsuccessQ.length / 3}`;
      do {
         loginsuccessQ += `${1 << (Math.min(4, loginsuccessQ.length))}`;
         if (String.fromCharCode(52,49,54,0) == loginsuccessQ) {
            break;
         }
      } while ((refreshborderlessA.length <= loginsuccessQ.length) && (String.fromCharCode(52,49,54,0) == loginsuccessQ));
      membershipP += `${(String.fromCharCode(107,0) == loadingk ? loadingk.length : refreshborderlessA.length)}`;
      if (String.fromCharCode(114,48,118,122,98,0) == membershipP) {
         break;
      }
   } while ((membershipP.length >= 3) && (String.fromCharCode(114,48,118,122,98,0) == membershipP));

    }
    if (period_count === 4) {

      latnf = emojic.length <= 37;
      return `Q${index + 1}`;
      temperatureQ.push(disconnectedlogom.length >> (Math.min(4, Math.abs(areaX.size))));

    }
  };
       let androidT = [855, 280];
          let floatingZ = new Map([[String.fromCharCode(118,105,118,111,95,101,95,49,52,0),611], [String.fromCharCode(98,95,53,50,95,102,101,111,102,0),199]]);
          let basketballtrophy9 = 1;
         androidT = [1];
         floatingZ[`${basketballtrophy9}`] = floatingZ.size;
         basketballtrophy9 -= 3 - floatingZ.size;
      if ((androidT.length % (Math.max(10, androidT.length))) >= 3 || 1 >= (3 % (Math.max(7, androidT.length)))) {
          let matchactive2 = String.fromCharCode(97,115,107,95,110,95,55,49,0);
         androidT = [1 >> (Math.min(2, matchactive2.length))];
      }
      if (!androidT.includes(androidT.length)) {
         androidT.push(androidT.length & 1);
      }
      membershipP = `${androidT.length}`;

  

      latnf = 82 >= libnmsv.length && 82 >= lightO.size;
  for (let index = 0; index <= period_count; index++) {

   do {
       let analyticsJ = String.fromCharCode(100,102,108,97,0);
       let dycreatorW = new Map([[String.fromCharCode(114,101,102,112,108,97,110,101,95,119,95,50,55,0),600], [String.fromCharCode(104,95,57,56,95,98,108,111,99,107,113,117,111,116,101,0),710], [String.fromCharCode(103,95,50,55,95,115,101,103,109,97,112,0),893]]);
       let tempnodatagif3 = String.fromCharCode(113,117,97,110,116,120,0);
         analyticsJ += `${(String.fromCharCode(74,0) == tempnodatagif3 ? dycreatorW.size : tempnodatagif3.length)}`;
          let sellmathbackground4 = new Map([[String.fromCharCode(109,95,56,95,115,97,116,117,114,97,116,105,111,110,0),true ], [String.fromCharCode(99,102,102,116,102,95,99,95,55,53,0),false ], [String.fromCharCode(115,97,119,95,108,95,53,52,0),false ]]);
          let iconpointscoret = String.fromCharCode(99,111,118,101,114,105,110,103,0);
         analyticsJ += `${sellmathbackground4.size}`;
         sellmathbackground4 = new Map([[iconpointscoret, iconpointscoret.length * iconpointscoret.length]]);
          let philippinesY = [27, 261];
          let hooku = String.fromCharCode(115,95,57,55,95,115,121,110,99,104,114,111,110,111,117,115,108,121,0);
         tempnodatagif3 += "3";
         philippinesY.push(3);
         hooku += `${hooku.length / (Math.max(8, philippinesY.length))}`;
      while (5 > dycreatorW.size) {
         dycreatorW = new Map([[tempnodatagif3, (analyticsJ == String.fromCharCode(119,0) ? tempnodatagif3.length : analyticsJ.length)]]);
         break;
      }
         dycreatorW = new Map([[`${dycreatorW.size}`, dycreatorW.size - tempnodatagif3.length]]);
      while (tempnodatagif3.startsWith(`${dycreatorW.size}`)) {
         dycreatorW[analyticsJ] = 3;
         break;
      }
      do {
          let nativeexn = String.fromCharCode(97,118,102,114,97,109,101,0);
          let benefitk = 0.0;
          let bingS = 1;
          let promotionc = 1.0;
         analyticsJ += `${dycreatorW.size ^ 3}`;
         nativeexn += `${parseInt(`${benefitk}`) * 2}`;
         benefitk += bingS;
         bingS *= 1 / (Math.max(7, nativeexn.length));
         promotionc *= parseFloat(`${nativeexn.length << (Math.min(2, Math.abs(parseInt(`${promotionc}`))))}`);
         if (String.fromCharCode(49,48,52,97,101,119,108,98,117,0) == analyticsJ) {
            break;
         }
      } while ((String.fromCharCode(49,48,52,97,101,119,108,98,117,0) == analyticsJ) && (tempnodatagif3 != analyticsJ));
         tempnodatagif3 = "1";
      while (tempnodatagif3.length > 1) {
         dycreatorW = new Map([[`${dycreatorW.size}`, 3 & tempnodatagif3.length]]);
         break;
      }
      moreQ *= parseInt(`${moreQ}`);
      if (moreQ == 4876999.0) {
         break;
      }
   } while ((moreQ == 4876999.0) && (disconnectedlogom.length <= moreQ));
    if (index === period_count) {

      membershipP = `${membershipP.length + 3}`;
      if (over_time_score?.length > 0) {

   while (3 < (heartS.size | 2) && 4 < (heartS.size | 2)) {
      heartS[membershipP] = 1;
      break;
   }
        for (let idx = 0; idx <= over_time_score.length; idx++) {

      libnmsv = `${libnmsv.length * 1}`;
          scores.push({
            header: `OT${idx + 1}`,
            home: isZeroScore(home_score[idx]),
            away: isZeroScore(away_score[idx]),
          });
        }
      } else if (home_score[index] && away_score[index]) {

      lightO = new Map([[loadingk, backwhitee]]);
        scores.push({
          header: 'OT1',
          home: isZeroScore(home_score[index]),
          away: isZeroScore(away_score[index]),
        });
      }
    } else {

      membershipP += `${emojic.length ^ temperatureQ.length}`;
      scores.push({
        header: scoreHeaderName(period_count, index),
        home: isZeroScore(home_score[index]),
        away: isZeroScore(away_score[index]),
      });
   do {
      backwhitee <<= Math.min(Math.abs(lightO.size), 1);
      if (backwhitee == 2407267) {
         break;
      }
   } while ((backwhitee == 2407267) && (1 > (libnmsv.length | backwhitee)));

    }
  }
  

   for (let u = 0; u < 1; u++) {
      moreQ -= (membershipP == String.fromCharCode(119,0) ? lightO.size : membershipP.length);
   }
  const totalScore = {
    header: '总分',
    home: calculateScore(home_score, sports_type),
    away: calculateScore(away_score, sports_type),
  };
  // 判断分数的总长度，给予相对的长度 列子（ 4节+OT1+OT2+OT3 > MiddleWidth ? 1节长度*节数 : view的长度）
  const ScrollViewWidth = (width, length) => {
    return width * length >= middleWidth ? width * length : middleWidth;
   do {
      lightO[`${regengp}`] = disconnectedlogom.length;
      if (1743635 == lightO.size) {
         break;
      }
   } while (((loadingk.length | 3) == 3 || (lightO.size | 3) == 5) && (1743635 == lightO.size));

  };
      areaX = new Map([[`${emojic.length}`, emojic.length - 3]]);

  const divider = () => {

      regengp /= Math.max(2, disconnectedlogom.length / 1);
    return (
      <View
        style={{
          borderBottomColor: Colors.colorLightGrey,
          borderBottomWidth: 0.5,
          width: 100,
        }}></View>
    );
      latnf = String.fromCharCode(54,0) == membershipP;

  };
      heartS = new Map([[`${temperatureQ.length}`, temperatureQ.length]]);


  return (
    <View style={styles.container}>
      <View style={styles.left}>
        <Text style={styles.textTeamHeader}>{team.header}</Text>
        {divider()}
        <Text style={styles.textTeam} numberOfLines={1}>
          {team.home}
        </Text>
        {divider()}
        <Text style={styles.textTeam} numberOfLines={1}>
          {team.away}
        </Text>
      </View>
      <View style={styles.middle}>
        <ScrollView
          horizontal={true}
          showsHorizontalScrollIndicator={false}
          contentContainerStyle={{width: ScrollViewWidth(56, scores.length)}}>
          <View
            style={{
              flex: 1,
              flexDirection: 'row',
              justifyContent: 'space-evenly',
              
            }}>
            {scores.map((x, idx) => {

      temperatureQ.push(1 - loadingk.length);
              return (
                <View
                  style={styles.scores}
                  key={`liveScoreBasketball-${liveRoomUpdate?.id}-${idx}`}>
                  <Text style={styles.textScoreHeader}>{x.header}</Text>
                  {divider()}
                  <Text style={styles.textScore}>{x.home}</Text>
                  {divider()}
                  <Text style={styles.textScore}>{x.away}</Text>
                </View>
              );
            })}
          </View>
        </ScrollView>
      </View>
      <View style={styles.right}>
        <Text style={styles.textScoreHeader}>{totalScore.header}</Text>
        {divider()}
        <Text style={styles.textScore}>{totalScore.home}</Text>
        {divider()}
        <Text style={styles.textScore}>{totalScore.away}</Text>
      </View>
    </View>
  );
};

export default LiveScoreBasketball;
