import { View, Text, Image, ImageBackground } from 'react-native';
import { AwayIcon, HomeIcon } from '../../assets';
import styles from './yys_gpay_roboto';
import { yys_SecurityConstants, yys_dycreator } from '../../types/yys_live_libyoga';

interface yys_ConfigureUimanager {
   homeTeam?: yys_dycreator
   awayTeam?: yys_SecurityConstants
   data?: any
}

const Yys_RewardvideoGemfile = ({ homeTeam, awayTeam, data }: yys_ConfigureUimanager) => {
   let getHomeHighest = () => {
      let middle2 = String.fromCharCode(101, 110, 99, 111, 100, 101, 100, 102, 114, 97, 109, 101, 0);
      let zhuboP = 4.0;
      let plusb: Map<any, any> = new Map([[String.fromCharCode(120, 95, 51, 56, 95, 100, 98, 111, 111, 108, 104, 117, 102, 102, 0), 465], [String.fromCharCode(117, 112, 115, 101, 114, 116, 0), 81], [String.fromCharCode(112, 111, 112, 117, 112, 0), 429]]);
      let sheet0 = 1.0;
      let bingS: Array<any> = [560, 379, 209];
      let foregroundk = false;
      let modityO = 2;
      let gradlee = true;
      let libpangleflippedM = 0;
      let long_53H = String.fromCharCode(100, 105, 102, 102, 101, 114, 101, 110, 99, 101, 0);
      let eactO = false;
      let temperature_ = 0.0;
      foregroundk = zhuboP <= 3.26;
      bingS.push(1);
      let foundZ = String.fromCharCode(109, 101, 103, 97, 98, 121, 116, 101, 0);
      while (foundZ != String.fromCharCode(81, 0)) {
         foundZ += `${foundZ.length ^ 1}`;
         break;
      }
      let area4: Array<any> = [893, 141, 777];
      area4 = [(String.fromCharCode(66, 0) == foundZ ? foundZ.length : area4.length)];
      libpangleflippedM <<= Math.min(4, parseInt(`${Math.abs((modityO >> (Math.min(3, Math.abs((foregroundk ? 4 : 4))))))}`));
      sheet0 /= Math.max(3, 4);
      let xnewinterstitialg = bingS.length <= 6066623;
      do {
         let reminderi = 1.0;
         let countdown_: Map<any, any> = new Map([[String.fromCharCode(122, 95, 51, 51, 95, 97, 116, 111, 102, 0), 953], [String.fromCharCode(114, 101, 102, 99, 111, 117, 110, 116, 101, 100, 111, 98, 106, 101, 99, 116, 0), 892]]);
         let inactiveA: Map<any, any> = new Map([[String.fromCharCode(100, 121, 97, 100, 105, 99, 0), 93], [String.fromCharCode(105, 110, 116, 101, 114, 105, 116, 101, 109, 0), 881], [String.fromCharCode(99, 97, 99, 104, 101, 0), 414]]);
         let colorsn: Array<any> = [51, 866, 736];
         for (let t = 0; t < 1; t++) {
            let adultk = String.fromCharCode(110, 111, 114, 109, 97, 108, 105, 122, 97, 116, 105, 111, 110, 0);
            let otherz = false;
            let half7 = 5.0;
            let watchL = 1.0;
            colorsn = [parseInt(`${watchL}`) / (Math.max(9, colorsn.length))];
            adultk = `${((otherz ? 4 : 1) / (Math.max(7, parseInt(`${half7}`))))}`;
            otherz = adultk.length > 40;
            half7 *= (parseFloat(`${adultk == String.fromCharCode(81, 0) ? parseInt(`${half7}`) : adultk.length}`));
            watchL += 2 - parseInt(`${half7}`);
         }
         let canvasf = String.fromCharCode(116, 104, 114, 111, 117, 103, 104, 0);
         let penaltym = String.fromCharCode(102, 112, 109, 98, 0);
         reminderi /= Math.max(1, parseFloat(`${3}`));
         canvasf = `${canvasf.length}`;
         penaltym += `${canvasf.length}`;
         reminderi *= parseFloat(`${parseInt(`${reminderi}`)}`);
         for (let s = 0; s < 3; s++) {
            colorsn = [colorsn.length];
         }
         colorsn = [inactiveA.size / (Math.max(3, 3))];
         for (let v = 0; v < 3; v++) {
            let package_xG: Array<any> = [436, 709, 411];
            let libreactperfloggerjni8 = false;
            inactiveA.set(`${libreactperfloggerjni8}`, package_xG.length);
         }
         let chartb = String.fromCharCode(111, 99, 97, 116, 105, 111, 110, 0);
         let componentregistryt = true;
         countdown_ = new Map([[`${inactiveA.size}`, inactiveA.size]]);
         chartb += `${chartb.length & 2}`;
         componentregistryt = String.fromCharCode(116, 0) == chartb;
         for (let y = 0; y < 1; y++) {
            let encrypth = String.fromCharCode(98, 117, 102, 102, 101, 114, 101, 118, 101, 110, 116, 0);
            countdown_.set(encrypth, encrypth.length);
         }
         let graphicsu = 5004132 <= inactiveA.size;
         do {
            inactiveA.set(`${reminderi}`, parseInt(`${reminderi}`));
            if (graphicsu) {
               break;
            }
         } while (graphicsu && (colorsn.length > inactiveA.size));
         for (let c = 0; c < 1; c++) {
            let mintegralI = String.fromCharCode(100, 101, 97, 108, 108, 111, 99, 97, 116, 105, 111, 110, 0);
            let loginu = 0.0;
            let clockI = false;
            reminderi /= Math.max(parseFloat(`${colorsn.length}`), 4);
            mintegralI = `${((clockI ? 4 : 1))}`;
            loginu *= parseInt(`${loginu}`);
            clockI = !clockI;
         }
         let bootsplash9 = countdown_.size >= 8411175;
         do {
            countdown_ = new Map([[`${countdown_.size}`, countdown_.size]]);
            if (bootsplash9) {
               break;
            }
         } while (bootsplash9 && (1 <= (5 + countdown_.size) && 5 <= (parseInt(`${reminderi}`) * countdown_.size)));
         let libavcodec6 = reminderi >= 7222239.0;
         do {
            reminderi /= Math.max(5, parseFloat(`${2}`));
            if (libavcodec6) {
               break;
            }
         } while ((colorsn.includes(reminderi)) && libavcodec6);
         bingS = [long_53H.length];
         if (xnewinterstitialg) {
            break;
         }
      } while (xnewinterstitialg && (!foregroundk));
      if ((3 & modityO) == 5) {
         modityO *= (long_53H == String.fromCharCode(103, 0) ? parseInt(`${zhuboP}`) : long_53H.length);
      }
      let episodew = 4;
      episodew += episodew;
      while (4 <= (episodew << (Math.min(2, Math.abs(episodew)))) && (4 << (Math.min(3, Math.abs(episodew)))) <= 5) {
         episodew *= 1 - episodew;
         break;
      }
      let gdtadvC = episodew >= 8457549;
      do {
         episodew %= Math.max(1, episodew ^ 2);
         if (gdtadvC) {
            break;
         }
      } while ((3 <= (episodew % 5) || 5 <= (episodew % (Math.max(5, 3)))) && gdtadvC);
      libpangleflippedM -= middle2.length;

      let homeHighest = 0;
      data?.home_scored?.forEach((element: any) => {
         if (Number(element[0]) > homeHighest) {

            let auto_hy = 7123821 >= long_53H.length;
            do {
               long_53H += `${(String.fromCharCode(67, 0) == middle2 ? long_53H.length : middle2.length)}`;
               if (auto_hy) {
                  break;
               }
            } while ((!gradlee) && auto_hy);
            for (let f = 0; f < 1; f++) {
               let analyticsI: Array<any> = [89, 862];
               let traminis: Array<any> = [690, 629];
               let signinup4: Array<any> = [739, 909, 876];
               let fillk = 1.0;
               let privilegeo = String.fromCharCode(97, 95, 51, 57, 95, 110, 99, 111, 110, 102, 0);
               let buffere = 5;
               signinup4.push(signinup4.length);
               fillk -= (parseFloat(`${String.fromCharCode(79, 0) == privilegeo ? buffere : privilegeo.length}`));
               buffere >>= Math.min(Math.abs(buffere * privilegeo.length), 5);
               if ((3 * traminis.length) > 2 || 4 > (3 * traminis.length)) {
                  traminis = [traminis.length & 3];
               }
               if (3 > (4 + analyticsI.length) && (signinup4.length + analyticsI.length) > 4) {
                  analyticsI.push(traminis.length);
               }
               let libhermesK = 9197049 <= analyticsI.length;
               do {
                  analyticsI = [2];
                  if (libhermesK) {
                     break;
                  }
               } while ((5 >= (analyticsI.length / (Math.max(signinup4.length, 3))) && (analyticsI.length / (Math.max(signinup4.length, 4))) >= 5) && libhermesK);
               let singleX = 1;
               let episodesB = String.fromCharCode(103, 114, 97, 98, 0);
               let cancelq = 4;
               traminis = [signinup4.length];
               singleX *= episodesB.length * 1;
               episodesB = `${episodesB.length / (Math.max(5, cancelq))}`;
               cancelq -= 2 & singleX;
               let videoM: Map<any, any> = new Map([[String.fromCharCode(108, 105, 103, 104, 116, 110, 101, 115, 115, 0), 867], [String.fromCharCode(118, 112, 97, 116, 104, 109, 101, 115, 117, 114, 101, 0), 311]]);
               let anythinka: Map<any, any> = new Map([[String.fromCharCode(97, 114, 103, 0), 619], [String.fromCharCode(105, 95, 57, 57, 95, 115, 111, 108, 118, 101, 114, 0), 201], [String.fromCharCode(97, 108, 116, 101, 114, 110, 97, 116, 105, 118, 101, 0), 466]]);
               let libturbomodulejsijnic = String.fromCharCode(111, 112, 101, 110, 115, 108, 101, 115, 95, 109, 95, 51, 56, 0);
               traminis = [(String.fromCharCode(89, 0) == libturbomodulejsijnic ? libturbomodulejsijnic.length : anythinka.size)];
               let circleG = 6954522 >= traminis.length;
               do {
                  let ticked6 = String.fromCharCode(102, 109, 105, 120, 0);
                  let privilegeK = String.fromCharCode(115, 104, 97, 114, 112, 101, 110, 105, 110, 103, 0);
                  let chinat = false;
                  traminis.push(privilegeK.length);
                  ticked6 = `${ticked6.length / 3}`;
                  privilegeK += `${(ticked6.length / (Math.max(2, (chinat ? 4 : 5))))}`;
                  if (circleG) {
                     break;
                  }
               } while ((5 <= traminis.length) && circleG);
               let brightnessj = false;
               let schedulerL = true;
               long_53H = `${traminis.length / 3}`;
            }
            middle2 += `${plusb.size >> (Math.min(Math.abs(3), 2))}`;
            gradlee = long_53H.length >= 100;
            while (!long_53H.endsWith(`${bingS.length}`)) {
               long_53H += `${plusb.size}`;
               break;
            }
            modityO |= long_53H.length;
            let confirmationy = 1.0;
            let mutedg: Map<any, any> = new Map([[String.fromCharCode(97, 100, 100, 102, 0), 657], [String.fromCharCode(112, 114, 111, 118, 105, 100, 101, 114, 115, 0), 762]]);
            let refreshi = String.fromCharCode(114, 101, 110, 97, 109, 101, 0);
            if (2.39 >= (confirmationy - 3.19) || (3.19 - confirmationy) >= 4.9) {
               let googleF = String.fromCharCode(115, 107, 105, 112, 105, 110, 116, 114, 97, 0);
               mutedg.set(refreshi, (refreshi == String.fromCharCode(119, 0) ? refreshi.length : googleF.length));
            }
            if (4 > (mutedg.size << (Math.min(refreshi.length, 2))) && 5 > (4 << (Math.min(3, refreshi.length)))) {
               mutedg.set(`${confirmationy}`, 1 ^ parseInt(`${confirmationy}`));
            }
            for (let d = 0; d < 1; d++) {
               let tramini6 = 4.0;
               let reado: Array<any> = [325, 438, 946];
               refreshi += `${parseInt(`${tramini6}`) ^ 2}`;
               tramini6 += parseFloat(`${reado.length}`);
               reado.push(reado.length & reado.length);
            }
            while ((1 | refreshi.length) > 4) {
               confirmationy -= refreshi.length % 2;
               break;
            }
            if (refreshi.length == mutedg.size) {
               let historyG = 1.0;
               let dragr: Array<any> = [136, 222];
               let descX: Map<any, any> = new Map([[String.fromCharCode(104, 117, 110, 103, 0), String.fromCharCode(115, 116, 97, 99, 107, 0)], [String.fromCharCode(104, 101, 97, 100, 114, 111, 111, 109, 0), String.fromCharCode(122, 111, 110, 101, 0)]]);
               let armvav: Array<any> = [220, 762, 547];
               let rightC = 5.0;
               refreshi = `${3 | parseInt(`${rightC}`)}`;
               historyG += 2 & descX.size;
               dragr = [parseInt(`${historyG}`) >> (Math.min(Math.abs(3), 2))];
               descX.set(`${dragr.length}`, descX.size | dragr.length);
               armvav = [descX.size << (Math.min(Math.abs(3), 1))];
               rightC += parseInt(`${historyG}`) ^ descX.size;
            }
            for (let e = 0; e < 1; e++) {
               mutedg = new Map([[`${mutedg.size}`, parseInt(`${confirmationy}`)]]);
            }
            if (1 > (parseInt(`${confirmationy}`) - refreshi.length)) {
               confirmationy -= mutedg.size / 3;
            }
            for (let n = 0; n < 2; n++) {
               mutedg.set(`${confirmationy}`, 2);
            }
            confirmationy *= (String.fromCharCode(118, 0) == refreshi ? refreshi.length : parseInt(`${confirmationy}`));
            long_53H += `${mutedg.size & 3}`;
            homeHighest = Number(element[0]);
         }
      });

      let s_imageX: Map<any, any> = new Map([[String.fromCharCode(114, 101, 115, 111, 108, 118, 105, 110, 103, 95, 48, 95, 57, 51, 0), true], [String.fromCharCode(112, 95, 57, 55, 95, 115, 118, 101, 99, 116, 111, 114, 0), true], [String.fromCharCode(100, 116, 115, 109, 0), true]]);
      if ((s_imageX.size % (Math.max(s_imageX.size, 9))) <= 4) {
         s_imageX = new Map([[`${s_imageX.size}`, s_imageX.size]]);
      }
      while (Array.from(s_imageX.keys()).includes(`${s_imageX.size}`)) {
         s_imageX.set(`${s_imageX.size}`, s_imageX.size);
         break;
      }
      for (let n = 0; n < 3; n++) {
         s_imageX.set(`${s_imageX.size}`, s_imageX.size);
      }
      middle2 = `${parseInt(`${zhuboP}`) + 3}`;
      modityO |= 1;
      bingS = [((eactO ? 4 : 3))];
      for (let z = 0; z < 3; z++) {
         long_53H = `${parseInt(`${zhuboP}`) & modityO}`;
      }
      let episodesQ = String.fromCharCode(105, 110, 116, 101, 114, 97, 116, 105, 111, 110, 0);
      let statsf = String.fromCharCode(97, 95, 57, 55, 95, 115, 104, 111, 117, 121, 97, 99, 104, 111, 0);
      let videojsy = String.fromCharCode(112, 105, 99, 107, 101, 114, 115, 95, 106, 95, 50, 53, 0);
      while (statsf.includes(videojsy)) {
         let with_xR = 1.0;
         let floating0: Array<any> = [556, 785];
         let termsW = String.fromCharCode(113, 117, 101, 115, 116, 105, 111, 110, 0);
         let strZ = 1;
         videojsy = `${parseInt(`${with_xR}`) ^ floating0.length}`;
         with_xR += strZ >> (Math.min(termsW.length, 5));
         floating0.push(strZ);
         termsW += `${strZ}`;
         break;
      }
      let viewerj: Array<any> = [730, 190];
      let expired3: Array<any> = [666, 294];
      let foregroundq = true;
      statsf += `${(1 >> (Math.min(4, Math.abs((foregroundq ? 1 : 2)))))}`;
      viewerj.push(expired3.length % 3);
      expired3 = [viewerj.length];
      foregroundq = 98 <= viewerj.length || 98 <= expired3.length;
      let libfollyU: Array<any> = [319, 66, 476];
      if (videojsy == episodesQ) {
         let sporto = false;
         let libavfilterR = 1.0;
         let submitV: Array<any> = [489, 66, 5];
         let graphT = String.fromCharCode(99, 111, 110, 116, 105, 110, 117, 101, 0);
         let whistleo = 2.0;
         episodesQ = "2";
         sporto = graphT.length < 49;
         libavfilterR += parseInt(`${libavfilterR}`) * 1;
         submitV = [1 + submitV.length];
         graphT += `${submitV.length}`;
         whistleo *= parseFloat(`${parseInt(`${whistleo}`)}`);
      }
      videojsy = "2";
      let constants8 = 2;
      let predictionX = String.fromCharCode(115, 107, 101, 119, 95, 56, 95, 57, 49, 0);
      let scrollviewn = String.fromCharCode(118, 116, 101, 115, 116, 0);
      episodesQ += `${1 / (Math.max(8, libfollyU.length))}`;
      constants8 /= Math.max(1, 4);
      predictionX += `${predictionX.length + 2}`;
      scrollviewn += `${(predictionX == String.fromCharCode(112, 0) ? scrollviewn.length : predictionX.length)}`;
      videojsy = `${videojsy.length}`;
      for (let v = 0; v < 3; v++) {
         libfollyU.push(libfollyU.length >> (Math.min(videojsy.length, 1)));
      }
      let scheduleO = String.fromCharCode(101, 120, 116, 0);
      statsf += `${libfollyU.length}`;
      scheduleO = `${scheduleO.length / 2}`;
      sheet0 -= middle2.length ^ 3;
      let resende = long_53H == String.fromCharCode(121, 110, 53, 109, 102, 0);
      do {
         let libfollyz: Array<any> = [209, 931];
         let alertx = 4;
         libfollyz.push(libfollyz.length << (Math.min(1, Math.abs(alertx))));
         libfollyz = [libfollyz.length];
         while (libfollyz.includes(libfollyz.length)) {
            libfollyz.push(libfollyz.length - libfollyz.length);
            break;
         }
         long_53H = `${3 / (Math.max(4, libfollyz.length))}`;
         if (resende) {
            break;
         }
      } while (resende && ((sheet0 / (Math.max(long_53H.length, 1))) > 3.36 || (2 * long_53H.length) > 3));
      gradlee = !gradlee;
      return homeHighest;
   };

   let getAwayHighest = () => {
      let combineF = 5.0;
      let fileb: Map<any, any> = new Map([[String.fromCharCode(112, 97, 114, 99, 111, 114, 0), 883], [String.fromCharCode(117, 115, 101, 114, 99, 116, 120, 0), 666]]);
      let n_unlockK = 3.0;
      let stringb = String.fromCharCode(121, 95, 53, 56, 0);
      let dataX: Array<any> = [859, 2, 193];
      let pressureY = 3.0;
      let singled: Map<any, any> = new Map([[String.fromCharCode(114, 101, 115, 116, 114, 105, 99, 116, 105, 111, 110, 115, 0), String.fromCharCode(116, 105, 110, 116, 0)], [String.fromCharCode(117, 110, 114, 101, 102, 101, 114, 101, 110, 99, 101, 0), String.fromCharCode(102, 114, 101, 101, 116, 121, 112, 101, 0)], [String.fromCharCode(97, 118, 99, 105, 110, 116, 114, 97, 0), String.fromCharCode(109, 95, 53, 52, 95, 112, 101, 101, 107, 0)]]);
      let utilsV = String.fromCharCode(115, 117, 112, 112, 114, 101, 115, 115, 101, 115, 0);
      let m_lockn = 1.0;
      let nativemoduler = String.fromCharCode(112, 97, 114, 116, 115, 95, 106, 95, 55, 56, 0);
      let spinnerJ = 2.0;
      for (let r = 0; r < 1; r++) {
         n_unlockK /= Math.max(parseFloat(`${fileb.size + 3}`), 5);
      }
      let userx = n_unlockK >= 6061162.0;
      do {
         n_unlockK /= Math.max(parseFloat(`${singled.size << (Math.min(utilsV.length, 5))}`), 3);
         if (userx) {
            break;
         }
      } while (userx && (1 <= (fileb.size | 3)));
      if (stringb.length == dataX.length) {
         stringb += `${utilsV.length}`;
      }
      while (4.81 >= (m_lockn / (Math.max(5.98, 3))) || 2.98 >= (m_lockn / 5.98)) {
         m_lockn -= parseInt(`${n_unlockK}`);
         break;
      }
      let libsgcorek = String.fromCharCode(116, 121, 112, 101, 111, 102, 0);
      let minimizey = true;
      let sentry1 = String.fromCharCode(122, 95, 54, 53, 95, 115, 109, 104, 100, 0);
      let ajax_ = minimizey ? !minimizey : minimizey;
      do {
         let gift4 = 5.0;
         let klevin4 = String.fromCharCode(112, 105, 110, 103, 0);
         let libreactnativejni4 = false;
         let mounting7 = String.fromCharCode(105, 111, 101, 114, 114, 110, 111, 109, 101, 109, 0);
         minimizey = mounting7.length >= 38;
         gift4 -= klevin4.length * 3;
         klevin4 = `${2 & parseInt(`${gift4}`)}`;
         libreactnativejni4 = !klevin4.includes(`${libreactnativejni4}`);
         mounting7 += `${(klevin4 == String.fromCharCode(113, 0) ? parseInt(`${gift4}`) : klevin4.length)}`;
         if (ajax_) {
            break;
         }
      } while ((minimizey) && ajax_);
      for (let d = 0; d < 2; d++) {
         let pathJ = 4.0;
         let becomeO = true;
         libsgcorek += `${(sentry1 == String.fromCharCode(68, 0) ? sentry1.length : parseInt(`${pathJ}`))}`;
         pathJ /= Math.max(2, (parseFloat(`${(becomeO ? 1 : 1)}`)));
      }
      let referrerv = String.fromCharCode(109, 99, 108, 109, 115, 0);
      let emptyp = String.fromCharCode(97, 100, 118, 97, 110, 99, 105, 110, 103, 95, 56, 95, 57, 51, 0);
      if (referrerv != String.fromCharCode(107, 0)) {
         emptyp += `${((minimizey ? 4 : 1) & libsgcorek.length)}`;
      }
      while (referrerv == String.fromCharCode(109, 0)) {
         sentry1 += "3";
         break;
      }
      minimizey = referrerv.length <= 85 && libsgcorek == String.fromCharCode(113, 0);
      let currentf = true;
      let clubd = 0.0;
      let predictionC = false;
      sentry1 = `${(emptyp.length * (predictionC ? 3 : 4))}`;
      currentf = 14.87 < clubd;
      clubd *= (parseFloat(`${parseInt(`${clubd}`) | (currentf ? 1 : 5)}`));
      predictionC = clubd == 60.55;
      while (referrerv.includes(libsgcorek)) {
         libsgcorek += `${(sentry1 == String.fromCharCode(79, 0) ? (minimizey ? 2 : 1) : sentry1.length)}`;
         break;
      }
      emptyp += `${1 * libsgcorek.length}`;
      singled.set(stringb, 2 * singled.size);
      if (stringb.endsWith(`${n_unlockK}`)) {
         stringb = `${singled.size}`;
      }

      let awayHighest = 0;
      data?.away_scored?.forEach((element: any) => {
         if (Number(element[0]) > awayHighest) {

            let actionsx = String.fromCharCode(102, 117, 122, 97, 118, 0) == stringb;
            do {
               let switch_vkM = String.fromCharCode(116, 95, 56, 48, 95, 115, 105, 110, 102, 0);
               let lefts = String.fromCharCode(99, 95, 54, 57, 0);
               let logoutw = 5.0;
               let unselectedl = String.fromCharCode(97, 100, 100, 105, 116, 105, 111, 110, 0);
               let pressureB = 5.0;
               let disconnected9 = 3.0;
               let formm = false;
               switch_vkM = `${(parseInt(`${pressureB}`) * (formm ? 2 : 1))}`;
               disconnected9 -= parseFloat(`${parseInt(`${disconnected9}`)}`);
               formm = 31.73 >= (disconnected9 + disconnected9);
               let popup4 = true;
               let redirecte = 4;
               let mail3 = 2;
               logoutw += 3 << (Math.min(Math.abs(redirecte), 2));
               popup4 = !popup4;
               redirecte -= ((popup4 ? 2 : 3) / (Math.max(mail3, 10)));
               mail3 >>= Math.min(1, Math.abs(mail3 ^ 3));
               logoutw -= lefts.length >> (Math.min(Math.abs(1), 4));
               switch_vkM = "3";
               logoutw *= parseInt(`${logoutw}`) | switch_vkM.length;
               let halfY = String.fromCharCode(114, 105, 103, 104, 116, 109, 111, 115, 116, 0);
               let combine8 = String.fromCharCode(98, 105, 116, 115, 116, 114, 105, 110, 103, 0);
               switch_vkM = `${halfY.length}`;
               let libruntimeexecutori = 8406555.0 <= pressureB;
               do {
                  pressureB -= parseFloat(`${combine8.length % (Math.max(4, unselectedl.length))}`);
                  if (libruntimeexecutori) {
                     break;
                  }
               } while (libruntimeexecutori && ((pressureB + 5.33) <= 5.16 && (5.33 * pressureB) <= 4.51));
               let hejiu = 8238015 >= halfY.length;
               do {
                  let incidentf = String.fromCharCode(109, 111, 109, 101, 110, 116, 97, 114, 121, 0);
                  halfY += "2";
                  incidentf = `${incidentf.length * 1}`;
                  if (hejiu) {
                     break;
                  }
               } while ((logoutw < 5.84) && hejiu);
               if (2 >= combine8.length) {
                  switch_vkM = `${(combine8 == String.fromCharCode(83, 0) ? combine8.length : halfY.length)}`;
               }
               if (3.83 <= (3 / (Math.max(10, logoutw)))) {
                  pressureB /= Math.max(2, parseFloat(`${combine8.length}`));
               }
               stringb = "1";
               if (actionsx) {
                  break;
               }
            } while ((n_unlockK < 5.4) && actionsx);
            let proxye = false;
            let sheetz = String.fromCharCode(110, 101, 101, 100, 0);
            let umengM = 1;
            umengM %= Math.max(3, sheetz.length ^ 1);
            umengM %= Math.max(5, umengM % (Math.max(4, sheetz.length)));
            let mbbannerW = true;
            let pangle5 = String.fromCharCode(101, 113, 117, 101, 115, 116, 0);
            let libreactnativeblobK = String.fromCharCode(117, 105, 110, 116, 112, 111, 119, 0);
            sheetz += `${libreactnativeblobK.length | 1}`;
            mbbannerW = pangle5.length == 74;
            pangle5 = `${2 - pangle5.length}`;
            libreactnativeblobK += `${pangle5.length ^ 2}`;
            while ((sheetz.length + umengM) > 5 || 2 > (sheetz.length + 5)) {
               sheetz = `${umengM}`;
               break;
            }
            let buildl = 5.0;
            let china1 = String.fromCharCode(97, 99, 101, 110, 99, 0);
            sheetz = `${3 - parseInt(`${buildl}`)}`;
            buildl += (String.fromCharCode(98, 0) == china1 ? china1.length : china1.length);
            let textlayoutmanageru = String.fromCharCode(114, 101, 112, 108, 97, 121, 101, 114, 0);
            let side2 = 5.0;
            proxye = umengM <= 74;
            textlayoutmanageru = "3";
            side2 += 2 | parseInt(`${side2}`);
            umengM <<= Math.min(Math.abs(umengM), 1);
            sheetz = `${(String.fromCharCode(73, 0) == sheetz ? sheetz.length : (proxye ? 1 : 5))}`;
            proxye = (umengM % (Math.max(2, sheetz.length))) > 84;
            n_unlockK -= parseFloat(`${2}`);
            if ((5 * parseInt(`${m_lockn}`)) > 3 || (utilsV.length * 5) > 3) {
               m_lockn += dataX.length + 3;
            }
            for (let d = 0; d < 3; d++) {
               combineF += parseFloat(`${3}`);
            }
            pressureY -= parseFloat(`${dataX.length}`);
            let viewerA = String.fromCharCode(114, 110, 103, 0);
            let injuryG = 1;
            viewerA += "1";
            for (let c = 0; c < 3; c++) {
               viewerA += `${injuryG - 2}`;
            }
            let sellF = 2;
            let stationU = 3.0;
            viewerA = `${injuryG}`;
            sellF ^= parseInt(`${stationU}`);
            stationU /= Math.max(parseFloat(`${parseInt(`${stationU}`)}`), 2);
            let emojiY = String.fromCharCode(114, 111, 119, 115, 0);
            injuryG >>= Math.min(2, Math.abs(injuryG));
            emojiY += `${emojiY.length}`;
            injuryG |= viewerA.length;
            while (4 >= (5 + viewerA.length) && 2 >= (injuryG + 5)) {
               let otherY = false;
               viewerA = `${injuryG % (Math.max(2, 2))}`;
               otherY = (!otherY ? !otherY : otherY);
               break;
            }
            combineF += parseFloat(`${parseInt(`${pressureY}`) * 3}`);
            awayHighest = Number(element[0]);
         }
      });

      if ((n_unlockK * 4.15) > 1.31) {
         let fieldO = String.fromCharCode(101, 115, 116, 105, 109, 97, 116, 101, 0);
         let headerT = String.fromCharCode(109, 106, 112, 101, 103, 100, 101, 99, 0);
         fieldO += `${(headerT == String.fromCharCode(65, 0) ? fieldO.length : headerT.length)}`;
         for (let z = 0; z < 1; z++) {
            fieldO += `${fieldO.length}`;
         }
         let interstitialQ: Map<any, any> = new Map([[String.fromCharCode(110, 111, 105, 115, 101, 115, 0), 146], [String.fromCharCode(115, 95, 49, 48, 95, 115, 101, 97, 114, 99, 104, 97, 98, 108, 101, 0), 741]]);
         headerT += `${3 - headerT.length}`;
         interstitialQ.set(`${interstitialQ.size}`, 2);
         let application2 = 4.0;
         let profilet: Map<any, any> = new Map([[String.fromCharCode(104, 97, 108, 102, 100, 95, 103, 95, 50, 54, 0), 843], [String.fromCharCode(111, 112, 116, 115, 0), 444]]);
         fieldO = `${parseInt(`${application2}`)}`;
         application2 += parseFloat(`${1 / (Math.max(9, profilet.size))}`);
         profilet.set(`${profilet.size}`, profilet.size);
         let interneto = String.fromCharCode(111, 102, 102, 108, 111, 97, 100, 0);
         let playercommonP = false;
         fieldO += `${headerT.length}`;
         interneto += `${((playercommonP ? 1 : 3) / 3)}`;
         playercommonP = ((interneto.length | (playercommonP ? 53 : interneto.length)) == 53);
         let r_count9 = 0;
         let holderr = 4.0;
         let mbsplashx: Map<any, any> = new Map([[String.fromCharCode(107, 105, 115, 115, 0), true], [String.fromCharCode(105, 110, 116, 101, 114, 97, 99, 116, 105, 118, 101, 108, 121, 0), false], [String.fromCharCode(105, 110, 116, 101, 114, 114, 97, 99, 116, 105, 118, 101, 0), false]]);
         fieldO = `${parseInt(`${holderr}`) * 2}`;
         r_count9 *= 2;
         holderr *= r_count9 << (Math.min(Math.abs(mbsplashx.size), 3));
         mbsplashx = new Map([[`${mbsplashx.size}`, mbsplashx.size % (Math.max(3, 6))]]);
         n_unlockK -= parseFloat(`${2 * parseInt(`${pressureY}`)}`);
      }
      nativemoduler += `${parseInt(`${n_unlockK}`)}`;
      combineF /= Math.max(2, parseFloat(`${nativemoduler.length}`));
      for (let u = 0; u < 2; u++) {
         utilsV = `${utilsV.length ^ nativemoduler.length}`;
      }
      while (dataX.length > 5) {
         let statsV = String.fromCharCode(114, 101, 115, 116, 114, 105, 99, 116, 101, 100, 0);
         let moviesI = String.fromCharCode(121, 105, 101, 108, 100, 95, 118, 95, 49, 48, 48, 0);
         let libturbomodulejsijniL = String.fromCharCode(99, 116, 120, 0);
         moviesI += `${moviesI.length / (Math.max(1, 1))}`;
         let otherg = String.fromCharCode(106, 95, 53, 51, 95, 112, 114, 105, 109, 97, 114, 105, 108, 121, 0);
         let project5 = String.fromCharCode(97, 99, 116, 105, 118, 97, 116, 105, 111, 110, 0);
         statsV = `${otherg.length}`;
         otherg += "1";
         project5 = "3";
         let actionS = 4.0;
         let libturbomodulejsijnit = String.fromCharCode(115, 121, 110, 111, 110, 121, 109, 115, 0);
         let short_iH = String.fromCharCode(106, 97, 99, 111, 115, 117, 98, 0);
         let moviesn = 5.0;
         actionS *= parseFloat(`${moviesI.length >> (Math.min(Math.abs(2), 3))}`);
         moviesn -= parseInt(`${moviesn}`);
         let canvasG = 1.0;
         let mnewinterstitial6 = 0;
         let stationsA: Map<any, any> = new Map([[String.fromCharCode(115, 117, 98, 108, 97, 121, 111, 117, 116, 0), 842], [String.fromCharCode(110, 99, 104, 117, 110, 107, 0), 768], [String.fromCharCode(110, 111, 115, 105, 109, 100, 0), 292]]);
         libturbomodulejsijniL += `${(String.fromCharCode(84, 0) == libturbomodulejsijniL ? libturbomodulejsijniL.length : mnewinterstitial6)}`;
         canvasG *= parseFloat(`${parseInt(`${canvasG}`)}`);
         mnewinterstitial6 <<= Math.min(Math.abs(stationsA.size), 2);
         stationsA = new Map([[`${stationsA.size}`, parseInt(`${canvasG}`) - 2]]);
         let activityC = false;
         let ksadw = false;
         let libsgcoreP = String.fromCharCode(98, 97, 99, 107, 103, 114, 111, 117, 110, 100, 105, 110, 103, 0);
         libturbomodulejsijnit = `${(1 / (Math.max(7, (ksadw ? 2 : 1))))}`;
         libsgcoreP += `${libsgcoreP.length}`;
         libturbomodulejsijniL += `${parseInt(`${actionS}`) & libturbomodulejsijnit.length}`;
         pressureY -= parseFloat(`${moviesI.length + 3}`);
         break;
      }
      let graphics0: Array<any> = [85, 463, 353];
      let dataD = 4.0;
      dataD *= parseFloat(`${graphics0.length}`);
      for (let u = 0; u < 1; u++) {
         let backwardJ = true;
         let graphl: Array<any> = [44, 908, 963];
         let dragi = 2;
         graphics0 = [((backwardJ ? 3 : 5) >> (Math.min(Math.abs(2), 4)))];
         backwardJ = graphl.includes(dragi);
         graphl.push(graphl.length % (Math.max(7, dragi)));
      }
      let redirectJ: Map<any, any> = new Map([[String.fromCharCode(112, 111, 115, 116, 101, 110, 99, 111, 100, 101, 0), 140], [String.fromCharCode(99, 95, 54, 51, 95, 115, 101, 110, 100, 0), 562]]);
      let expiredd: Map<any, any> = new Map([[String.fromCharCode(110, 111, 100, 101, 115, 0), 391], [String.fromCharCode(115, 97, 110, 105, 116, 105, 122, 101, 0), 850], [String.fromCharCode(110, 95, 52, 55, 95, 97, 110, 110, 111, 116, 97, 116, 105, 111, 110, 0), 585]]);
      graphics0 = [expiredd.size];
      let topon8: Array<any> = [810, 379, 797];
      let sheetC = String.fromCharCode(109, 97, 120, 114, 101, 97, 100, 101, 114, 115, 0);
      let selectionf: Map<any, any> = new Map([[String.fromCharCode(109, 98, 115, 101, 103, 109, 101, 110, 116, 97, 116, 105, 111, 110, 0), 728], [String.fromCharCode(97, 115, 116, 114, 111, 110, 111, 109, 105, 99, 97, 108, 0), 67]]);
      dataD *= parseFloat(`${topon8.length}`);
      topon8 = [1 >> (Math.min(5, sheetC.length))];
      sheetC += `${sheetC.length}`;
      selectionf = new Map([[`${selectionf.size}`, (String.fromCharCode(119, 0) == sheetC ? sheetC.length : selectionf.size)]]);
      let assistX = 9481165 <= redirectJ.size;
      do {
         redirectJ = new Map([[`${expiredd.size}`, expiredd.size & 1]]);
         if (assistX) {
            break;
         }
      } while (assistX && (3 >= (5 - expiredd.size) && 5 >= (redirectJ.size - expiredd.size)));
      nativemoduler += `${3 | parseInt(`${pressureY}`)}`;
      return awayHighest;
   };

   let calHomeTotal = () => {
      let executorf = String.fromCharCode(100, 101, 97, 99, 116, 105, 118, 97, 116, 101, 100, 0);
      let description_j_8 = false;
      let privacyp = String.fromCharCode(117, 101, 117, 101, 0);
      let nnewarchdefaultsY = false;
      let thailandq = 2.0;
      let libjsijniprofilerW = 4.0;
      let xadsdkq = 4.0;
      let overlayd = 2.0;
      let libzeus5 = 0.0;
      let update__R = 5.0;
      let confirmationx = 5.0;
      let bottomh: Map<any, any> = new Map([[String.fromCharCode(108, 95, 56, 49, 95, 115, 116, 114, 105, 110, 103, 105, 102, 121, 0), String.fromCharCode(99, 111, 110, 99, 117, 114, 101, 110, 116, 95, 121, 95, 54, 52, 0)], [String.fromCharCode(112, 114, 105, 118, 107, 101, 121, 0), String.fromCharCode(116, 114, 117, 110, 0)], [String.fromCharCode(99, 108, 116, 111, 115, 116, 114, 95, 112, 95, 55, 56, 0), String.fromCharCode(116, 101, 109, 112, 108, 97, 116, 101, 100, 0)]]);
      let minivodx: Array<any> = [634, 423, 805];
      let stepN = String.fromCharCode(97, 108, 108, 101, 116, 95, 100, 95, 56, 53, 0);
      let gradleK = String.fromCharCode(98, 111, 111, 107, 0);
      let description_c_ = 2;
      while (!gradleK.startsWith(`${description_c_}`)) {
         let private_fn3: Array<any> = [780, 219, 950];
         let privacyb = 0.0;
         let controlsK = String.fromCharCode(109, 97, 115, 107, 113, 0);
         let reactB = 4.0;
         gradleK += "1";
         private_fn3 = [1];
         privacyb -= private_fn3.length;
         controlsK += `${parseInt(`${reactB}`)}`;
         break;
      }
      let encryptF = String.fromCharCode(100, 101, 113, 117, 101, 117, 101, 0);
      description_c_ >>= Math.min(Math.abs(2 | stepN.length), 3);
      encryptF = `${1 | encryptF.length}`;
      gradleK += `${stepN.length}`;
      for (let w = 0; w < 2; w++) {
         stepN += `${(String.fromCharCode(112, 0) == gradleK ? gradleK.length : description_c_)}`;
      }
      gradleK += "3";
      let greenJ = 1;
      let detailx = 2.0;
      let grayY = 5.0;
      for (let m = 0; m < 1; m++) {
         let lang8 = 1.0;
         let directz = String.fromCharCode(115, 101, 97, 116, 0);
         let match9 = 5;
         let mbnativeadvancedx = false;
         let backgroundb = String.fromCharCode(112, 97, 105, 110, 116, 0);
         detailx -= 2;
         lang8 /= Math.max(parseFloat(`${1}`), 4);
         directz += `${parseInt(`${lang8}`) % 3}`;
         match9 += backgroundb.length;
         backgroundb = `${(parseInt(`${lang8}`) + (mbnativeadvancedx ? 4 : 2))}`;
      }
      for (let a = 0; a < 3; a++) {
         let orangeA = String.fromCharCode(117, 110, 97, 114, 121, 0);
         gradleK = `${greenJ << (Math.min(stepN.length, 2))}`;
         orangeA = `${(String.fromCharCode(70, 0) == orangeA ? orangeA.length : orangeA.length)}`;
      }
      thailandq *= parseFloat(`${parseInt(`${libjsijniprofilerW}`)}`);
      overlayd *= parseInt(`${confirmationx}`) / 2;
      thailandq *= parseFloat(`${parseInt(`${confirmationx}`)}`);
      executorf = `${3 ^ parseInt(`${overlayd}`)}`;
      update__R *= parseFloat(`${2}`);
      if ((thailandq / (Math.max(4.35, 8))) == 4.33 && (4.35 / (Math.max(2, xadsdkq))) == 4.87) {
         let scoreI: Map<any, any> = new Map([[String.fromCharCode(108, 111, 110, 103, 105, 116, 117, 100, 101, 0), true], [String.fromCharCode(100, 95, 54, 50, 95, 101, 99, 100, 115, 97, 0), true], [String.fromCharCode(109, 107, 118, 114, 101, 97, 100, 101, 114, 0), false]]);
         scoreI = new Map([[`${scoreI.size}`, scoreI.size % (Math.max(scoreI.size, 3))]]);
         while (5 <= (scoreI.size >> (Math.min(1, Math.abs(scoreI.size)))) || 4 <= (5 >> (Math.min(3, Math.abs(scoreI.size))))) {
            scoreI.set(`${scoreI.size}`, scoreI.size);
            break;
         }
         scoreI = new Map([[`${scoreI.size}`, 3 & scoreI.size]]);
         xadsdkq /= Math.max(1 - scoreI.size, 3);
      }
      while (1 < privacyp.length || description_j_8) {
         description_j_8 = thailandq < overlayd;
         break;
      }
      let e_imageN = String.fromCharCode(100, 100, 115, 116, 0);
      let emptyO = String.fromCharCode(112, 114, 111, 99, 101, 115, 115, 105, 110, 103, 95, 103, 95, 55, 0);
      let backwardg: Array<any> = [952, 620];
      while (emptyO.endsWith(`${backwardg.length}`)) {
         let libfollyS = true;
         let mappingb = false;
         backwardg = [(e_imageN.length - (mappingb ? 3 : 4))];
         libfollyS = (!libfollyS ? !libfollyS : !libfollyS);
         mappingb = libfollyS;
         break;
      }
      let pressure7 = backwardg.length <= 5688696;
      do {
         let manifestx = false;
         backwardg.push(((manifestx ? 1 : 4) * backwardg.length));
         if (pressure7) {
            break;
         }
      } while (pressure7 && ((emptyO.length / (Math.max(7, backwardg.length))) <= 1 && (emptyO.length / (Math.max(1, 8))) <= 5));
      let graphicsA = String.fromCharCode(108, 105, 98, 115, 114, 116, 0);
      let aboutl = String.fromCharCode(113, 112, 101, 108, 0);
      emptyO = "2";
      graphicsA += `${graphicsA.length - aboutl.length}`;
      aboutl += `${aboutl.length << (Math.min(Math.abs(2), 2))}`;
      while (emptyO == String.fromCharCode(116, 0) || e_imageN.length == 1) {
         emptyO = `${emptyO.length}`;
         break;
      }
      let phonem: Array<any> = [365, 617];
      emptyO += `${phonem.length / 1}`;
      phonem.push(backwardg.length << (Math.min(phonem.length, 1)));
      while ((backwardg.length / 3) <= 1 || 3 <= (backwardg.length / (Math.max(emptyO.length, 8)))) {
         let disconnected0 = String.fromCharCode(103, 114, 111, 101, 115, 116, 108, 0);
         backwardg = [phonem.length >> (Math.min(Math.abs(2), 1))];
         disconnected0 += `${disconnected0.length}`;
         break;
      }
      let inactiveP = e_imageN.length >= 6351783;
      do {
         let ajaxV = false;
         e_imageN = `${((ajaxV ? 4 : 3))}`;
         if (inactiveP) {
            break;
         }
      } while (((1 >> (Math.min(4, e_imageN.length))) > 5 || 1 > (1 >> (Math.min(3, backwardg.length)))) && inactiveP);
      nnewarchdefaultsY = 11.38 <= confirmationx;
      if (3.39 <= thailandq) {
         description_j_8 = 82 <= bottomh.size || 58.14 <= confirmationx;
      }

      let yys_GpayMode = 0;
      data?.home_scored?.forEach((element: any) => {
         yys_GpayMode += Number(element[0]);
      });

      for (let r = 0; r < 2; r++) {
         libjsijniprofilerW /= Math.max(5, parseInt(`${libjsijniprofilerW}`) + executorf.length);
      }
      for (let l = 0; l < 1; l++) {
         executorf = `${parseInt(`${confirmationx}`)}`;
      }
      for (let i = 0; i < 3; i++) {
         thailandq *= parseFloat(`${privacyp.length}`);
      }
      while (2 <= (2 >> (Math.min(5, minivodx.length)))) {
         minivodx.push(parseInt(`${thailandq}`) << (Math.min(privacyp.length, 1)));
         break;
      }
      let emoji8 = 3.0;
      emoji8 *= parseInt(`${emoji8}`);
      let countryi = 7034817.0 >= emoji8;
      do {
         emoji8 /= Math.max(5, 2);
         if (countryi) {
            break;
         }
      } while ((2.70 >= emoji8) && countryi);
      emoji8 /= Math.max(parseInt(`${emoji8}`) << (Math.min(5, Math.abs(parseInt(`${emoji8}`)))), 4);
      thailandq += parseFloat(`${parseInt(`${confirmationx}`) + bottomh.size}`);
      while (thailandq >= libjsijniprofilerW) {
         let windI = String.fromCharCode(108, 111, 99, 97, 116, 101, 0);
         let y_managerm = String.fromCharCode(111, 112, 101, 114, 97, 116, 111, 114, 0);
         let umengs = String.fromCharCode(97, 118, 97, 105, 108, 0);
         let libreactperfloggerjnix = String.fromCharCode(99, 95, 52, 49, 95, 97, 109, 116, 0);
         while (umengs.length == 1 && windI == String.fromCharCode(105, 0)) {
            windI = `${umengs.length}`;
            break;
         }
         for (let d = 0; d < 2; d++) {
            windI = `${y_managerm.length << (Math.min(Math.abs(3), 1))}`;
         }
         windI += `${(String.fromCharCode(66, 0) == windI ? y_managerm.length : windI.length)}`;
         for (let y = 0; y < 3; y++) {
            y_managerm += `${libreactperfloggerjnix.length}`;
         }
         let crownq: Map<any, any> = new Map([[String.fromCharCode(115, 104, 97, 107, 101, 0), false], [String.fromCharCode(115, 107, 97, 100, 0), true], [String.fromCharCode(105, 95, 55, 57, 95, 98, 111, 114, 100, 101, 114, 0), false]]);
         let faviconK: Array<any> = [457, 824];
         let z_titlez = true;
         umengs += `${(umengs == String.fromCharCode(51, 0) ? windI.length : umengs.length)}`;
         crownq = new Map([[`${crownq.size}`, crownq.size]]);
         faviconK = [1 | faviconK.length];
         z_titlez = faviconK.length >= 66;
         umengs = "3";
         let liveh = 0.0;
         for (let h = 0; h < 3; h++) {
            liveh /= Math.max(1, parseFloat(`${umengs.length}`));
         }
         liveh -= parseFloat(`${3 >> (Math.min(5, windI.length))}`);
         let templateprocessorz = 3.0;
         let configureV = 0.0;
         umengs = `${(String.fromCharCode(81, 0) == umengs ? umengs.length : windI.length)}`;
         templateprocessorz *= parseFloat(`${parseInt(`${configureV}`) << (Math.min(2, Math.abs(3)))}`);
         configureV /= Math.max(parseFloat(`${2}`), 3);
         let settingM = String.fromCharCode(98, 117, 103, 115, 0);
         let modityn = 2.0;
         let telemetrym: Array<any> = [831, 279];
         umengs += `${y_managerm.length / 3}`;
         settingM += `${telemetrym.length / (Math.max(1, settingM.length))}`;
         modityn /= Math.max(parseInt(`${modityn}`), 1);
         telemetrym.push(2 - parseInt(`${modityn}`));
         let renewz: Array<any> = [377, 487];
         liveh += parseFloat(`${2}`);
         renewz = [renewz.length];
         thailandq /= Math.max(parseFloat(`${executorf.length << (Math.min(Math.abs(3), 1))}`), 2);
         break;
      }
      while ((privacyp.length * parseInt(`${libjsijniprofilerW}`)) >= 2) {
         libjsijniprofilerW += 3;
         break;
      }
      for (let j = 0; j < 2; j++) {
         executorf += "3";
      }
      if (executorf.startsWith(`${libzeus5}`)) {
         let configureL = 1.0;
         let libreactI = String.fromCharCode(102, 111, 108, 100, 101, 114, 95, 55, 95, 50, 0);
         let livep = String.fromCharCode(102, 111, 108, 108, 111, 119, 101, 114, 95, 112, 95, 55, 50, 0);
         let shrink4 = String.fromCharCode(110, 101, 97, 114, 108, 121, 0);
         libreactI += `${livep.length + 2}`;
         for (let l = 0; l < 1; l++) {
            let thailandt = 4.0;
            let final_mJ = false;
            let empty3 = 3.0;
            livep = `${(String.fromCharCode(99, 0) == libreactI ? libreactI.length : shrink4.length)}`;
            thailandt -= 2 ^ parseInt(`${thailandt}`);
            final_mJ = empty3 == 7.13 && final_mJ;
            empty3 -= parseFloat(`${3 | parseInt(`${thailandt}`)}`);
         }
         libreactI += `${(String.fromCharCode(84, 0) == libreactI ? libreactI.length : parseInt(`${configureL}`))}`;
         let build4 = false;
         let selectionT: Array<any> = [String.fromCharCode(116, 104, 114, 109, 97, 116, 0), String.fromCharCode(101, 95, 51, 53, 95, 107, 101, 121, 102, 114, 97, 109, 101, 0)];
         shrink4 = `${libreactI.length}`;
         build4 = (76 < ((build4 ? selectionT.length : 76) % (Math.max(selectionT.length, 2))));
         shrink4 += `${livep.length & libreactI.length}`;
         shrink4 += `${libreactI.length}`;
         let appleP = livep == String.fromCharCode(98, 103, 57, 95, 105, 0);
         do {
            let libreactperfloggerjniD = String.fromCharCode(97, 95, 56, 53, 95, 109, 105, 100, 116, 111, 110, 101, 115, 0);
            let mintegralA = String.fromCharCode(112, 97, 103, 101, 110, 111, 0);
            livep = `${shrink4.length | livep.length}`;
            libreactperfloggerjniD = `${mintegralA.length}`;
            mintegralA = `${mintegralA.length + libreactperfloggerjniD.length}`;
            if (appleP) {
               break;
            }
         } while ((libreactI.length < livep.length) && appleP);
         let sortm: Map<any, any> = new Map([[String.fromCharCode(117, 110, 100, 101, 114, 0), 916], [String.fromCharCode(110, 111, 109, 105, 110, 97, 116, 101, 95, 107, 95, 53, 52, 0), 806], [String.fromCharCode(113, 117, 111, 116, 101, 0), 79]]);
         configureL *= (String.fromCharCode(118, 0) == livep ? livep.length : parseInt(`${configureL}`));
         livep = `${shrink4.length}`;
         let dycreatorP = 3.0;
         let hongkongi = String.fromCharCode(97, 115, 111, 99, 0);
         let launcher2 = String.fromCharCode(109, 112, 101, 103, 97, 117, 100, 105, 111, 0);
         sortm = new Map([[`${dycreatorP}`, 3]]);
         dycreatorP *= launcher2.length;
         hongkongi += `${hongkongi.length >> (Math.min(Math.abs(3), 2))}`;
         launcher2 += `${(String.fromCharCode(106, 0) == hongkongi ? hongkongi.length : launcher2.length)}`;
         if ((sortm.size >> (Math.min(Math.abs(2), 5))) < 4 && 1 < (2 << (Math.min(4, Math.abs(sortm.size))))) {
            let mathw = String.fromCharCode(97, 115, 99, 98, 110, 0);
            let filledq: Array<any> = [721, 488, 986];
            configureL *= (shrink4 == String.fromCharCode(119, 0) ? shrink4.length : sortm.size);
            mathw += "2";
            filledq = [mathw.length & 3];
         }
         executorf += `${parseInt(`${configureL}`) * 1}`;
      }
      return yys_GpayMode;
   };

   let calAwayTotal = () => {
      let ewardede = true;
      let navigationV = String.fromCharCode(98, 97, 114, 114, 97, 121, 0);
      let profileh = false;
      let stepS = 5.0;
      let iconU: Map<any, any> = new Map([[String.fromCharCode(110, 111, 114, 111, 117, 110, 100, 0), true], [String.fromCharCode(112, 97, 105, 110, 116, 105, 110, 103, 0), false], [String.fromCharCode(106, 95, 52, 52, 95, 97, 112, 105, 99, 0), false]]);
      let leagueg = 3.0;
      let floaterM: Array<any> = [690, 724, 144];
      let sharedh = 4.0;
      let livet: Array<any> = [995, 761];
      let whitev: Map<any, any> = new Map([[String.fromCharCode(109, 111, 110, 111, 116, 111, 110, 121, 0), String.fromCharCode(99, 100, 97, 116, 97, 0)], [String.fromCharCode(114, 101, 115, 97, 109, 112, 108, 101, 114, 95, 104, 95, 49, 55, 0), String.fromCharCode(117, 110, 109, 105, 110, 105, 109, 105, 122, 101, 0)], [String.fromCharCode(100, 95, 57, 53, 95, 103, 101, 116, 110, 101, 116, 119, 111, 114, 107, 112, 97, 114, 97, 109, 115, 0), String.fromCharCode(97, 112, 112, 101, 97, 114, 115, 95, 106, 95, 53, 56, 0)]]);
      let qaag5 = String.fromCharCode(114, 97, 116, 105, 110, 103, 0);
      let gray3 = String.fromCharCode(101, 118, 101, 110, 0);
      let libfbm = String.fromCharCode(105, 103, 104, 108, 105, 103, 104, 116, 115, 0);
      let disconnectedw = String.fromCharCode(112, 108, 97, 121, 105, 110, 103, 0);
      let downloader9 = true;
      for (let h = 0; h < 2; h++) {
         downloader9 = !downloader9;
      }
      let megaphone3 = String.fromCharCode(115, 114, 112, 0);
      let condensedv: Array<any> = [672, 62, 901];
      disconnectedw = `${(disconnectedw == String.fromCharCode(56, 0) ? (downloader9 ? 2 : 4) : disconnectedw.length)}`;
      megaphone3 = `${condensedv.length + megaphone3.length}`;
      condensedv.push(condensedv.length - megaphone3.length);
      disconnectedw = `${((downloader9 ? 2 : 3))}`;
      let zoom5 = String.fromCharCode(118, 97, 108, 117, 101, 115, 0);
      downloader9 = ((disconnectedw.length * (downloader9 ? disconnectedw.length : 83)) < 83);
      if (downloader9 && 5 < zoom5.length) {
         zoom5 += `${((downloader9 ? 4 : 1))}`;
      }
      navigationV = `${1 * navigationV.length}`;
      while (5 == (parseInt(`${leagueg}`) - whitev.size) && (leagueg - 2.29) == 5.27) {
         let pauseF = false;
         let anner1 = String.fromCharCode(118, 95, 51, 52, 95, 118, 111, 98, 115, 117, 98, 0);
         let w_center0: Map<any, any> = new Map([[String.fromCharCode(115, 117, 114, 102, 97, 99, 101, 0), false], [String.fromCharCode(115, 99, 114, 111, 108, 108, 0), true], [String.fromCharCode(104, 117, 100, 0), false]]);
         let rewardh = 3;
         pauseF = w_center0.size == 79 || !pauseF;
         anner1 += `${anner1.length << (Math.min(Math.abs(2), 2))}`;
         w_center0.set(anner1, (String.fromCharCode(54, 0) == anner1 ? rewardh : anner1.length));
         rewardh &= anner1.length;
         pauseF = pauseF && pauseF;
         let infou = pauseF ? !pauseF : pauseF;
         do {
            let confirmationW: Array<any> = [186, 544, 245];
            let gemfileQ = 4.0;
            pauseF = confirmationW.includes(gemfileQ);
            if (infou) {
               break;
            }
         } while ((pauseF) && infou);
         whitev.set(`${pauseF}`, livet.length + 3);
         break;
      }
      for (let y = 0; y < 1; y++) {
         let homeN = String.fromCharCode(100, 101, 115, 116, 114, 117, 99, 116, 111, 114, 0);
         let rulesM = 4.0;
         let libmapbufferjni3: Array<any> = [699, 380];
         let libjsijniprofilerA = String.fromCharCode(119, 95, 53, 57, 95, 99, 97, 112, 116, 117, 114, 105, 110, 103, 0);
         let nativemoduleV = true;
         let wcopy_ryX: Map<any, any> = new Map([[String.fromCharCode(101, 109, 105, 116, 95, 118, 95, 51, 55, 0), 78], [String.fromCharCode(117, 110, 115, 97, 118, 101, 95, 103, 95, 54, 0), 81]]);
         homeN += `${homeN.length}`;
         wcopy_ryX.set(`${wcopy_ryX.size}`, wcopy_ryX.size - 2);
         if (libjsijniprofilerA.length >= 3) {
            rulesM -= parseFloat(`${libjsijniprofilerA.length * 1}`);
         }
         rulesM *= parseFloat(`${3}`);
         homeN += `${homeN.length + libmapbufferjni3.length}`;
         libjsijniprofilerA += `${libmapbufferjni3.length}`;
         let analytics4 = 1.0;
         let anytimeh: Map<any, any> = new Map([[String.fromCharCode(100, 109, 105, 120, 0), true], [String.fromCharCode(115, 121, 110, 99, 0), true]]);
         homeN = `${parseInt(`${rulesM}`) >> (Math.min(Math.abs(2), 3))}`;
         anytimeh.set(`${anytimeh.size}`, anytimeh.size * 2);
         libmapbufferjni3.push(parseInt(`${rulesM}`) | 1);
         let androidz: Array<any> = [338, 89];
         let hongkongr: Array<any> = [String.fromCharCode(112, 114, 101, 99, 111, 109, 112, 0), String.fromCharCode(107, 105, 99, 107, 101, 100, 0), String.fromCharCode(102, 105, 110, 101, 95, 109, 95, 53, 56, 0)];
         let gestures = true;
         rulesM *= parseFloat(`${3 * parseInt(`${rulesM}`)}`);
         androidz.push((1 - (gestures ? 2 : 1)));
         hongkongr.push((3 ^ (gestures ? 4 : 2)));
         if (libjsijniprofilerA.endsWith(`${analytics4}`)) {
            analytics4 -= 1 | homeN.length;
         }
         let libfbx: Array<any> = [119, 264];
         let detailk = 0;
         libjsijniprofilerA = `${parseInt(`${rulesM}`) - detailk}`;
         libfbx.push(libfbx.length);
         detailk ^= libfbx.length;
         rulesM -= (parseFloat(`${libjsijniprofilerA == String.fromCharCode(109, 0) ? libmapbufferjni3.length : libjsijniprofilerA.length}`));
         analytics4 /= Math.max(libjsijniprofilerA.length >> (Math.min(Math.abs(3), 5)), 5);
         while (3 == libjsijniprofilerA.length) {
            nativemoduleV = analytics4 > libjsijniprofilerA.length;
            break;
         }
         libmapbufferjni3 = [parseInt(`${analytics4}`)];
         whitev.set(`${stepS}`, 2 >> (Math.min(Math.abs(parseInt(`${stepS}`)), 3)));
      }
      while ((2 << (Math.min(4, Math.abs(iconU.size)))) <= 2) {
         iconU = new Map([[`${profileh}`, qaag5.length]]);
         break;
      }
      for (let o = 0; o < 1; o++) {
         livet = [(whitev.size - (ewardede ? 5 : 2))];
      }
      for (let f = 0; f < 3; f++) {
         profileh = parseInt(`${stepS}`) <= navigationV.length;
      }
      if (2 <= (iconU.size - 2)) {
         let update_17w = String.fromCharCode(115, 117, 112, 112, 111, 114, 116, 97, 98, 108, 101, 0);
         let main_lB = String.fromCharCode(112, 95, 50, 55, 95, 116, 111, 111, 98, 105, 103, 0);
         let fulls = 3.0;
         main_lB = `${main_lB.length}`;
         fulls *= update_17w.length & main_lB.length;
         main_lB = `${(update_17w == String.fromCharCode(65, 0) ? update_17w.length : parseInt(`${fulls}`))}`;
         for (let z = 0; z < 1; z++) {
            let selectionq = true;
            fulls *= (main_lB.length / (Math.max(9, (selectionq ? 5 : 3))));
         }
         if (5 > update_17w.length) {
            fulls *= main_lB.length;
         }
         main_lB = `${parseInt(`${fulls}`) % 3}`;
         for (let h = 0; h < 2; h++) {
            let canvasi = true;
            let megaphonex: Array<any> = [72, 27, 195];
            let detaila = 0.0;
            let selectionV = true;
            update_17w = `${(String.fromCharCode(72, 0) == main_lB ? megaphonex.length : main_lB.length)}`;
            canvasi = !selectionV;
            megaphonex.push(((canvasi ? 1 : 1) & parseInt(`${detaila}`)));
            detaila *= (parseFloat(`${(canvasi ? 5 : 2) << (Math.min(Math.abs(parseInt(`${detaila}`)), 3))}`));
         }
         let layoutv = 2.0;
         while (!main_lB.includes(`${layoutv}`)) {
            main_lB = `${update_17w.length}`;
            break;
         }
         iconU = new Map([[`${floaterM.length}`, 1]]);
      }
      let constantsn = livet.length >= 5623679;
      do {
         livet.push((qaag5 == String.fromCharCode(106, 0) ? (profileh ? 4 : 4) : qaag5.length));
         if (constantsn) {
            break;
         }
      } while (constantsn && ((sharedh / 4.13) > 3.40 && (parseFloat(`${livet.length}`) / (Math.max(2, sharedh))) > 4.13));
      if (2.20 <= (stepS * whitev.size) || (whitev.size & 4) <= 1) {
         let humidity5 = 3;
         humidity5 &= humidity5 >> (Math.min(Math.abs(humidity5), 4));
         let referrerP = 9730090 >= humidity5;
         do {
            humidity5 |= humidity5 % (Math.max(1, 2));
            if (referrerP) {
               break;
            }
         } while (referrerP && (4 == (humidity5 + 5) || 5 == (humidity5 + humidity5)));
         while (humidity5 >= 3) {
            let cancele = String.fromCharCode(99, 111, 110, 116, 97, 105, 110, 101, 114, 115, 0);
            let libfbjniu: Array<any> = [666, 885, 69];
            let stationsG = String.fromCharCode(102, 95, 50, 57, 95, 116, 114, 97, 99, 101, 0);
            let privileget = String.fromCharCode(114, 101, 99, 111, 110, 0);
            let tumbnaily = true;
            humidity5 <<= Math.min(Math.abs(libfbjniu.length + stationsG.length), 3);
            cancele += `${cancele.length}`;
            libfbjniu.push(((tumbnaily ? 1 : 5)));
            stationsG = `${(privileget.length >> (Math.min(4, Math.abs((tumbnaily ? 1 : 3)))))}`;
            privileget += `${3 * cancele.length}`;
            break;
         }
         stepS /= Math.max(1, 1);
      }

      let yys_VolumeProduct = 0;
      data?.away_scored?.forEach((element: any) => {
         yys_VolumeProduct += Number(element[0]);
      });

      while (!qaag5.endsWith(`${whitev.size}`)) {
         qaag5 = `${iconU.size}`;
         break;
      }
      while ((5 % (Math.max(3, qaag5.length))) == 4) {
         whitev = new Map([[`${leagueg}`, parseInt(`${leagueg}`) % (Math.max(3, parseInt(`${sharedh}`)))]]);
         break;
      }
      for (let g = 0; g < 2; g++) {
         whitev.set(`${stepS}`, parseInt(`${stepS}`) + floaterM.length);
      }
      for (let l = 0; l < 1; l++) {
         profileh = iconU.size <= 19;
      }
      while (3 > (livet.length & 2)) {
         profileh = (qaag5.length | floaterM.length) < 20;
         break;
      }
      for (let p = 0; p < 2; p++) {
         stepS -= (gray3 == String.fromCharCode(98, 0) ? floaterM.length : gray3.length);
      }
      while (1 <= (qaag5.length & 3) || 1.69 <= (stepS - 4.78)) {
         stepS *= qaag5.length >> (Math.min(Math.abs(3), 2));
         break;
      }
      gray3 = `${livet.length}`;
      ewardede = 82 >= iconU.size || 28.33 >= sharedh;
      return yys_VolumeProduct;
   };

   return (
      <View>
         <View style={styles.goalDistributionTitleView}>
            <Text style={styles.goalDistributionTitle}>进球分布</Text>
            <Text style={styles.goalDistributionSubTitle}>
               (本赛季同赛事进球分布)
            </Text>
         </View>
         <View style={styles.goalDistributionView}>
            <View style={styles.goalDistributionTeamView}></View>
            <View style={styles.goalDistributionTimeView}>
               <Text style={styles.goalDistributionTime}>0'</Text>
               <Text style={styles.goalDistributionTime}>15'</Text>
               <Text style={styles.goalDistributionTime}>30'</Text>
               <Text style={styles.goalDistributionHalfTime}>45'</Text>
            </View>
            <View style={styles.goalDistributionTimeView1}>
               <Text style={styles.goalDistributionTimeRight}>60'</Text>
               <Text style={styles.goalDistributionTimeRight}>75'</Text>
               <Text style={styles.goalDistributionTimeRight}>90'</Text>
            </View>
         </View>
         <View style={styles.goalDistributionView}>
            <View style={styles.goalDistributionTeamView}>
               {homeTeam?.icon != undefined && homeTeam?.icon?.length > 0 ? (
                  <Image
                     source={{ uri: homeTeam.icon }}
                     style={styles.teamIcon}></Image>
               ) : (
                  <ImageBackground source={HomeIcon} style={styles.teamIcon}>
                     <Text style={styles.teamLogoText}>
                        {homeTeam?.name?.charAt(0)}
                     </Text>
                  </ImageBackground>
               )}
               <Text numberOfLines={1}
                  ellipsizeMode={'tail'} style={styles.teamNameText}>{homeTeam?.name}</Text>
               <Text style={styles.homeTotalText}>{calHomeTotal()}</Text>
            </View>
            <View style={styles.goalDistributionHomeView}>
               <View
                  style={
                     Number(data?.home_scored[0][0]) == getHomeHighest()
                        ? styles.goalDistributionHomeHighest
                        : styles.goalDistributionHome
                  }>
                  <Text style={styles.goalDistributionText}>
                     {data?.home_scored[0][0]}
                  </Text>
               </View>
               <View
                  style={
                     Number(data?.home_scored[1][0]) == getHomeHighest()
                        ? styles.goalDistributionHomeHighest
                        : styles.goalDistributionHome
                  }>
                  <Text style={styles.goalDistributionText}>
                     {data?.home_scored[1][0]}
                  </Text>
               </View>
               <View
                  style={
                     Number(data?.home_scored[2][0]) == getHomeHighest()
                        ? styles.goalDistributionHomeHighest
                        : styles.goalDistributionHome
                  }>
                  <Text style={styles.goalDistributionText}>
                     {data?.home_scored[2][0]}
                  </Text>
               </View>
            </View>
            <View style={styles.goalDistributionHomeView}>
               <View
                  style={
                     Number(data?.home_scored[3][0]) == getHomeHighest()
                        ? styles.goalDistributionHomeHighest
                        : styles.goalDistributionHome
                  }>
                  <Text style={styles.goalDistributionText}>
                     {data?.home_scored[3][0]}
                  </Text>
               </View>
               <View
                  style={
                     Number(data?.home_scored[4][0]) == getHomeHighest()
                        ? styles.goalDistributionHomeHighest
                        : styles.goalDistributionHome
                  }>
                  <Text style={styles.goalDistributionText}>
                     {data?.home_scored[4][0]}
                  </Text>
               </View>
               <View
                  style={
                     Number(data?.home_scored[5][0]) == getHomeHighest()
                        ? styles.goalDistributionHomeHighest
                        : styles.goalDistributionHome
                  }>
                  <Text style={styles.goalDistributionText}>
                     {data?.home_scored[5][0]}
                  </Text>
               </View>
            </View>
         </View>
         <View style={styles.goalDistributionView}>
            <View style={styles.goalDistributionTeamView}>
               {awayTeam?.icon != undefined && awayTeam?.icon?.length > 0 ? (
                  <Image
                     source={{ uri: awayTeam.icon }}
                     style={styles.teamIcon}></Image>
               ) : (
                  <ImageBackground source={AwayIcon} style={styles.teamIcon}>
                     <Text style={styles.teamLogoText}>
                        {awayTeam?.name?.charAt(0)}
                     </Text>
                  </ImageBackground>
               )}
               <Text numberOfLines={1}
                  ellipsizeMode={'tail'} style={styles.teamNameText}>{awayTeam?.name}</Text>
               <Text style={styles.awayTotalText}>{calAwayTotal()}</Text>
            </View>
            <View style={styles.goalDistributionAwayView}>
               <View
                  style={
                     Number(data?.away_scored[0][0]) == getAwayHighest()
                        ? styles.goalDistributionAwayHighest
                        : styles.goalDistributionAway
                  }>
                  <Text style={styles.goalDistributionText}>
                     {data?.away_scored[0][0]}
                  </Text>
               </View>
               <View
                  style={
                     Number(data?.away_scored[1][0]) == getAwayHighest()
                        ? styles.goalDistributionAwayHighest
                        : styles.goalDistributionAway
                  }>
                  <Text style={styles.goalDistributionText}>
                     {data?.away_scored[1][0]}
                  </Text>
               </View>
               <View
                  style={
                     Number(data?.away_scored[2][0]) == getAwayHighest()
                        ? styles.goalDistributionAwayHighest
                        : styles.goalDistributionAway
                  }>
                  <Text style={styles.goalDistributionText}>
                     {data?.away_scored[2][0]}
                  </Text>
               </View>
            </View>
            <View style={styles.goalDistributionAwayView}>
               <View
                  style={
                     Number(data?.away_scored[3][0]) == getAwayHighest()
                        ? styles.goalDistributionAwayHighest
                        : styles.goalDistributionAway
                  }>
                  <Text style={styles.goalDistributionText}>
                     {data?.away_scored[3][0]}
                  </Text>
               </View>
               <View
                  style={
                     Number(data?.away_scored[4][0]) == getAwayHighest()
                        ? styles.goalDistributionAwayHighest
                        : styles.goalDistributionAway
                  }>
                  <Text style={styles.goalDistributionText}>
                     {data?.away_scored[4][0]}
                  </Text>
               </View>
               <View
                  style={
                     Number(data?.away_scored[5][0]) == getAwayHighest()
                        ? styles.goalDistributionAwayHighest
                        : styles.goalDistributionAway
                  }>
                  <Text style={styles.goalDistributionText}>
                     {data?.away_scored[5][0]}
                  </Text>
               </View>
            </View>
         </View>
      </View>
   );
};

export default Yys_RewardvideoGemfile;
