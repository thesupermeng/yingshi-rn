import {View, Text} from 'react-native';
import styles from './fgl_less_position';
import vars from '../../../utility/ift_macau_change';

const MatchStatisticComponent = ({index, data, sportType}) => {
  const {away, home, type} = data;

  const homePercent = () => {
       let notification4 = String.fromCharCode(115,117,110,114,105,115,101,0);
    let watchN = new Map([[String.fromCharCode(110,101,105,103,104,98,111,117,114,0),String.fromCharCode(105,110,105,116,105,97,108,105,122,97,116,105,111,110,115,0)], [String.fromCharCode(109,101,109,115,121,115,0),String.fromCharCode(101,118,111,108,118,101,0)]]);
    let brightness9 = String.fromCharCode(104,101,108,112,101,114,0);
    let episodesT = String.fromCharCode(112,97,114,97,109,103,101,110,95,102,95,56,0);
    let verticalP = String.fromCharCode(108,108,118,105,100,100,115,112,101,110,99,0);
    let livep = new Map([[String.fromCharCode(101,98,117,108,97,115,0),538], [String.fromCharCode(116,109,105,120,0),442], [String.fromCharCode(115,116,97,114,116,101,100,0),512]]);
    let sourceZ = 5.0;
    let awayJ = true;
    let lighti = String.fromCharCode(98,115,111,110,0);
    let selectedS = [423, 148];
   while (brightness9.length <= 2) {
       let foundN = String.fromCharCode(116,104,114,101,101,0);
         foundN = "1";
      if (foundN != foundN) {
          let tickedG = String.fromCharCode(97,118,99,99,0);
          let singlet = String.fromCharCode(99,111,110,118,101,114,115,97,116,105,111,110,0);
          let annerR = 2.0;
          let shareP = 4.0;
          let awayR = 4;
         foundN += `${parseInt(`${annerR}`)}`;
         tickedG += `${(singlet == String.fromCharCode(74,0) ? awayR : singlet.length)}`;
         annerR += parseFloat(`${awayR}`);
         shareP -= parseFloat(`${parseInt(`${shareP}`)}`);
      }
      if (foundN != String.fromCharCode(68,0)) {
          let historyo = String.fromCharCode(97,95,53,57,95,117,114,108,0);
         foundN = `${3 << (Math.min(1, historyo.length))}`;
      }
      awayJ = 63 < notification4.length;
      break;
   }
   if (brightness9.includes(`${verticalP.length}`)) {
      verticalP += `${verticalP.length}`;
   }
       let mailV = [668, 812];
       let setting7 = false;
       let projectW = 5.0;
         projectW /= Math.max(1, parseFloat(`${2 * parseInt(`${projectW}`)}`));
         projectW /= Math.max(3, parseFloat(`${2 * mailV.length}`));
       let injuryk = String.fromCharCode(120,95,57,55,95,106,115,105,109,100,99,112,117,0);
         projectW /= Math.max(parseFloat(`${parseInt(`${projectW}`) - 1}`), 1);
          let homeM = String.fromCharCode(115,117,98,102,114,97,109,101,0);
          let sansZ = String.fromCharCode(99,111,110,116,101,120,116,99,111,110,102,105,103,0);
         injuryk = `${((setting7 ? 1 : 3) % (Math.max(1, 6)))}`;
         homeM = `${homeM.length * 2}`;
         sansZ = `${homeM.length}`;
      notification4 = `${verticalP.length * episodesT.length}`;
      awayJ = (verticalP.length << (Math.min(4, Math.abs(watchN.size)))) <= 70;
   if (1 >= brightness9.length) {
      awayJ = String.fromCharCode(119,0) == verticalP;
   }
   for (let y = 0; y < 1; y++) {
      awayJ = episodesT.length < 36;
   }
   if (4.23 > (sourceZ * parseFloat(`${livep.size}`)) && 2.5 > (4.23 * sourceZ)) {
      sourceZ -= parseFloat(`${verticalP.length}`);
   }
      verticalP = `${brightness9.length ^ 3}`;
   do {
      sourceZ /= Math.max((parseFloat(`${notification4 == String.fromCharCode(114,0) ? notification4.length : (awayJ ? 1 : 4)}`)), 1);
      if (247734.0 == sourceZ) {
         break;
      }
   } while ((247734.0 == sourceZ) && (livep[`${sourceZ}`] != null));
       let smallq = 1.0;
       let umengr = [948, 742, 283];
       let episodesb = true;
      while (episodesb) {
          let result2 = String.fromCharCode(105,95,57,54,95,105,115,108,101,97,112,0);
          let selectedL = 0.0;
         episodesb = umengr.includes(selectedL);
         result2 = `${result2.length}`;
         selectedL += result2.length << (Math.min(1, result2.length));
         break;
      }
         episodesb = smallq >= 30.5;
         umengr.push(((episodesb ? 2 : 5) << (Math.min(Math.abs(3), 4))));
         umengr = [(umengr.length << (Math.min(5, Math.abs((episodesb ? 3 : 4)))))];
         umengr.push(((episodesb ? 5 : 3) + parseInt(`${smallq}`)));
      do {
         umengr = [(parseInt(`${smallq}`) >> (Math.min(2, Math.abs((episodesb ? 4 : 5)))))];
         if (umengr.length == 4656283) {
            break;
         }
      } while ((umengr.length == 4656283) && (4.23 == (4.91 - smallq) || (parseInt(`${smallq}`) - umengr.length) == 3));
         episodesb = smallq >= 76.49;
         episodesb = 47 > umengr.length;
         umengr.push(2);
      verticalP += `${(watchN.size & (episodesb ? 1 : 2))}`;
   for (let t = 0; t < 3; t++) {
       let fcopy_baP = String.fromCharCode(100,101,116,101,114,109,105,110,105,115,116,105,99,0);
       let benefitM = String.fromCharCode(97,115,112,101,99,116,0);
       let viewsN = String.fromCharCode(115,117,98,112,101,108,0);
       let read_ = String.fromCharCode(108,111,103,102,110,0);
       let crossc = 1;
      do {
         fcopy_baP = `${(String.fromCharCode(113,0) == benefitM ? viewsN.length : benefitM.length)}`;
         if (String.fromCharCode(53,48,101,111,0) == fcopy_baP) {
            break;
         }
      } while ((5 == fcopy_baP.length) && (String.fromCharCode(53,48,101,111,0) == fcopy_baP));
      do {
         crossc += 1 * benefitM.length;
         if (crossc == 438167) {
            break;
         }
      } while ((crossc == 438167) && (!benefitM.startsWith(`${crossc}`)));
      do {
          let modityn = String.fromCharCode(117,110,114,101,99,111,103,0);
          let teamH = String.fromCharCode(97,114,105,116,104,95,105,95,53,56,0);
         benefitM += "3";
         modityn = `${teamH.length}`;
         teamH = `${modityn.length ^ 1}`;
         if (benefitM.length == 3671981) {
            break;
         }
      } while ((benefitM.length == 3671981) && (benefitM != String.fromCharCode(69,0) && viewsN == String.fromCharCode(101,0)));
         read_ = `${(String.fromCharCode(99,0) == fcopy_baP ? fcopy_baP.length : read_.length)}`;
      for (let y = 0; y < 1; y++) {
         benefitM = `${3 % (Math.max(2, benefitM.length))}`;
      }
         benefitM = `${3 & read_.length}`;
       let popupy = 3.0;
         viewsN = `${viewsN.length << (Math.min(Math.abs(1), 3))}`;
          let lightS = String.fromCharCode(106,100,99,111,101,102,99,116,0);
         benefitM = `${fcopy_baP.length ^ lightS.length}`;
      if (read_.length <= 4) {
         read_ += `${fcopy_baP.length << (Math.min(Math.abs(3), 3))}`;
      }
         crossc -= read_.length;
         popupy *= parseFloat(`${read_.length & 2}`);
          let stationx = true;
          let sourceF = 0;
          let step8 = [645, 346, 191];
         popupy /= Math.max(parseFloat(`${parseInt(`${popupy}`)}`), 4);
         stationx = step8.length > 93;
         sourceF >>= Math.min(5, Math.abs(sourceF));
         step8.push(sourceF);
         viewsN += `${benefitM.length / (Math.max(10, viewsN.length))}`;
         crossc &= parseInt(`${popupy}`) ^ 1;
      verticalP = `${watchN.size + 3}`;
   }
      awayJ = episodesT.length > verticalP.length;

    return ((Number(home) / (Number(home) + Number(away))) * 100) ? (Number(home) / (Number(home) + Number(away))) * 100 + '%' : '0%';
   if (awayJ) {
      sourceZ *= parseFloat(`${episodesT.length}`);
   }
   while ((2 * watchN.size) >= 4) {
       let regengs = new Map([[String.fromCharCode(100,95,52,0),46], [String.fromCharCode(109,112,101,103,97,117,100,105,111,100,97,116,97,0),694], [String.fromCharCode(102,111,114,119,97,114,100,101,114,0),558]]);
       let indicator0 = 1;
       let moreq = String.fromCharCode(109,98,99,110,116,0);
       let viewse = String.fromCharCode(114,105,100,0);
         moreq = `${viewse.length >> (Math.min(1, Math.abs(regengs.size)))}`;
          let expired2 = 3.0;
          let storeZ = String.fromCharCode(114,101,99,111,103,110,105,122,101,100,0);
          let stringB = new Map([[String.fromCharCode(97,115,115,111,99,105,97,116,105,111,110,0),639], [String.fromCharCode(100,105,102,102,101,114,101,110,99,101,0),244]]);
         regengs = new Map([[`${indicator0}`, (String.fromCharCode(122,0) == storeZ ? storeZ.length : indicator0)]]);
         expired2 *= parseInt(`${expired2}`) + 3;
         stringB[`${expired2}`] = stringB.size;
       let floatern = new Map([[String.fromCharCode(119,95,53,49,95,112,117,98,108,105,115,104,0),String.fromCharCode(101,118,105,99,116,0)], [String.fromCharCode(109,98,117,102,99,104,97,105,110,0),String.fromCharCode(98,97,110,95,48,95,57,0)], [String.fromCharCode(121,95,50,48,95,99,109,97,107,101,0),String.fromCharCode(115,97,110,105,116,105,122,101,100,0)]]);
       let gpayx = 4.0;
       let sinaY = 2.0;
       let indexz = String.fromCharCode(112,97,101,116,104,0);
       let scoreN = String.fromCharCode(115,118,101,99,116,111,114,0);
         regengs = new Map([[scoreN, indexz.length << (Math.min(Math.abs(2), 2))]]);
          let borderlessP = 2.0;
          let ajaxB = 1.0;
         gpayx -= parseInt(`${borderlessP}`) + 3;
         borderlessP -= parseInt(`${ajaxB}`);
      do {
         floatern = new Map([[`${regengs.size}`, 3]]);
         if (3992900 == floatern.size) {
            break;
         }
      } while ((3992900 == floatern.size) && ((floatern.size * 3) >= 1 && (viewse.length * floatern.size) >= 3));
          let modelsk = false;
          let thailand7 = 2.0;
         regengs = new Map([[`${modelsk}`, 2 / (Math.max(parseInt(`${sinaY}`), 5))]]);
         modelsk = thailand7 < thailand7;
         floatern[moreq] = moreq.length;
         scoreN = "2";
         floatern[scoreN] = scoreN.length / 3;
      notification4 += `${((awayJ ? 3 : 2) & indicator0)}`;
      break;
   }
   while (notification4 != String.fromCharCode(104,0) || verticalP.length <= 1) {
       let listy = 3.0;
       let typingk = String.fromCharCode(105,110,116,101,114,102,97,99,101,115,95,121,95,57,55,0);
       let analyticsx = true;
       let livec = 5.0;
          let moreT = 5;
          let arrowd = 3.0;
          let groupc = String.fromCharCode(121,95,49,53,95,97,114,114,97,121,0);
         listy -= parseFloat(`${groupc.length}`);
         moreT %= Math.max(2, 1);
         arrowd *= 3;
         groupc = `${parseInt(`${arrowd}`) - 3}`;
         listy -= (parseFloat(`${parseInt(`${listy}`) - (analyticsx ? 2 : 1)}`));
       let chatz = String.fromCharCode(99,111,110,118,111,108,118,101,0);
         livec += (parseFloat(`${String.fromCharCode(98,0) == typingk ? (analyticsx ? 5 : 2) : typingk.length}`));
      if (5.85 < (listy - 1.18) && (parseInt(`${listy}`) - 3) < 1) {
          let targett = String.fromCharCode(109,97,121,0);
         chatz = `${chatz.length}`;
         targett = "3";
      }
          let favoriteR = 3;
         analyticsx = chatz == String.fromCharCode(97,0) && 59 <= typingk.length;
         favoriteR ^= 2;
      if (4.32 >= (5.61 * livec)) {
         livec -= (parseFloat(`${chatz.length & (analyticsx ? 5 : 2)}`));
      }
         typingk += `${(parseInt(`${livec}`) ^ (analyticsx ? 4 : 5))}`;
       let membershipT = 3;
      if ((listy * membershipT) >= 3.34) {
         membershipT >>= Math.min(Math.abs(parseInt(`${listy}`) % 1), 4);
      }
      for (let a = 0; a < 2; a++) {
         typingk += `${typingk.length / (Math.max(1, 6))}`;
      }
         membershipT %= Math.max(typingk.length >> (Math.min(Math.abs(2), 1)), 3);
      notification4 += `${((awayJ ? 5 : 5) % (Math.max(brightness9.length, 8)))}`;
      break;
   }
      watchN[`${sourceZ}`] = parseInt(`${sourceZ}`) & notification4.length;
      brightness9 += `${episodesT.length >> (Math.min(1, brightness9.length))}`;
       let unticki = String.fromCharCode(100,101,116,101,99,116,0);
       let countdownW = 0;
       let knewss = 2;
         knewss |= countdownW;
         countdownW /= Math.max(2, 5);
      while (unticki.endsWith(`${countdownW}`)) {
         countdownW *= 3;
         break;
      }
      do {
         unticki += "3";
         if (String.fromCharCode(55,56,105,97,52,120,0) == unticki) {
            break;
         }
      } while ((String.fromCharCode(55,56,105,97,52,120,0) == unticki) && (unticki.length == countdownW));
      do {
         unticki = `${countdownW & unticki.length}`;
         if (unticki == String.fromCharCode(113,108,95,55,108,97,51,106,99,0)) {
            break;
         }
      } while ((unticki == String.fromCharCode(113,108,95,55,108,97,51,106,99,0)) && (unticki.includes(`${knewss}`)));
         unticki = `${countdownW}`;
         countdownW /= Math.max(unticki.length, 3);
      do {
         countdownW >>= Math.min(1, Math.abs((unticki == String.fromCharCode(111,0) ? knewss : unticki.length)));
         if (2379169 == countdownW) {
            break;
         }
      } while ((2379169 == countdownW) && (3 > countdownW));
       let corner1 = String.fromCharCode(105,95,52,95,100,105,114,101,99,116,120,0);
      sourceZ *= parseFloat(`${watchN.size % (Math.max(livep.size, 7))}`);
      episodesT = "2";
   for (let z = 0; z < 2; z++) {
      notification4 = `${lighti.length}`;
   }
   while (notification4.length < livep.size) {
       let entryG = [230, 84, 166];
       let vignettes = new Map([[String.fromCharCode(105,100,108,101,0),863], [String.fromCharCode(112,97,103,101,114,0),146], [String.fromCharCode(99,114,108,100,112,0),509]]);
      while (2 >= (4 + entryG.length) || 2 >= (4 + entryG.length)) {
         entryG = [vignettes.size + 2];
         break;
      }
      do {
         entryG.push(1 + entryG.length);
         if (843167 == entryG.length) {
            break;
         }
      } while ((3 == (vignettes.size + entryG.length)) && (843167 == entryG.length));
         vignettes = new Map([[`${vignettes.size}`, 3]]);
       let sina1 = String.fromCharCode(118,105,111,108,97,116,105,111,110,0);
          let team2 = String.fromCharCode(118,97,97,99,97,108,99,117,108,97,116,105,111,110,0);
          let logoY = 0;
         vignettes[`${logoY}`] = sina1.length;
         team2 += `${team2.length ^ 1}`;
         logoY %= Math.max(2 << (Math.min(4, team2.length)), 3);
         entryG = [sina1.length];
      notification4 += `${(verticalP.length % (Math.max(4, (awayJ ? 2 : 1))))}`;
      break;
   }
   if ((sourceZ * parseFloat(`${livep.size}`)) <= 4.4) {
       let sheete = [744, 517];
      for (let k = 0; k < 1; k++) {
          let navigation7 = 2;
          let shirtW = 4.0;
          let whatsapp9 = String.fromCharCode(115,117,98,112,111,105,110,116,0);
          let tumbnails = 4.0;
          let weiboI = 0.0;
         sheete.push(parseInt(`${shirtW}`));
         navigation7 += 1;
         shirtW /= Math.max(5, parseFloat(`${parseInt(`${tumbnails}`) + 1}`));
         whatsapp9 = "2";
         tumbnails += parseInt(`${weiboI}`);
         weiboI -= parseFloat(`${parseInt(`${tumbnails}`) ^ navigation7}`);
      }
       let settingo = String.fromCharCode(100,105,97,108,108,101,100,0);
       let disconnectedO = String.fromCharCode(97,98,115,116,114,97,99,116,0);
       let footballf = String.fromCharCode(115,104,111,114,116,101,110,0);
      livep = new Map([[notification4, notification4.length + verticalP.length]]);
   }
   for (let y = 0; y < 3; y++) {
      episodesT += "1";
   }
       let fastx = String.fromCharCode(108,105,110,101,115,0);
       let filln = String.fromCharCode(114,101,109,105,110,100,101,114,0);
       let albumH = String.fromCharCode(115,105,103,110,105,110,103,0);
       let rules8 = String.fromCharCode(108,101,97,118,101,0);
         albumH += `${3 | fastx.length}`;
          let filedu = new Map([[String.fromCharCode(100,105,118,105,100,101,0),String.fromCharCode(120,117,118,109,118,115,95,49,95,51,49,0)], [String.fromCharCode(116,111,103,103,108,101,100,0),String.fromCharCode(99,111,110,116,105,110,117,97,108,0)], [String.fromCharCode(115,109,111,111,116,104,101,100,0),String.fromCharCode(114,101,116,97,105,110,0)]]);
          let bannerD = false;
          let lightd = 4.0;
         fastx += `${filln.length}`;
         filedu[`${lightd}`] = 2;
         bannerD = lightd >= 25.45;
      for (let k = 0; k < 3; k++) {
         filln = `${albumH.length}`;
      }
       let long_q2K = 1.0;
      while (4.57 >= (long_q2K + 5.40)) {
         long_q2K -= fastx.length + 2;
         break;
      }
      livep = new Map([[fastx, verticalP.length & 2]]);

  };

  const awayPercent = () => {
       let right4 = true;
    let friendsG = 3.0;
    let minid = [772, 8, 795];
    let telegramp = String.fromCharCode(109,95,49,56,95,109,111,100,117,108,97,116,101,0);
    let hooksf = false;
    let flyer5 = String.fromCharCode(115,99,97,110,115,116,97,116,117,115,0);
    let unselectedM = 0.0;
    let views2 = [891, 729, 631];
    let serviceI = 1.0;
    let tickE = [6, 266];
    let stationw = true;
    let windT = 0;
    let transfer4 = true;
    let layoutg = true;
      views2.push(telegramp.length << (Math.min(2, views2.length)));
      views2 = [((stationw ? 3 : 4) ^ 2)];
   for (let t = 0; t < 2; t++) {
      right4 = 55.10 > serviceI && hooksf;
   }
      hooksf = (telegramp.length * friendsG) <= 71.0;
      friendsG += (1 & (stationw ? 5 : 2));
      unselectedM /= Math.max((parseFloat(`${(stationw ? 4 : 5)}`)), 3);
   do {
      telegramp = `${minid.length}`;
      if (telegramp == String.fromCharCode(120,52,57,120,56,122,0)) {
         break;
      }
   } while ((telegramp == String.fromCharCode(120,52,57,120,56,122,0)) && (1.54 <= serviceI));
      stationw = ((views2.length * (stationw ? views2.length : 7)) > 7);
      telegramp += `${telegramp.length}`;
      transfer4 = (20 == ((hooksf ? views2.length : 20) / (Math.max(9, views2.length))));
      transfer4 = 91.33 <= friendsG;
       let gpayP = 1.0;
       let ballP = false;
         ballP = 41.61 > gpayP;
      do {
         ballP = 89.3 < gpayP && ballP;
         if (ballP ? !ballP : ballP) {
            break;
         }
      } while (((2.21 * gpayP) == 5.80) && (ballP ? !ballP : ballP));
          let navigationz = String.fromCharCode(98,105,114,116,104,0);
          let starV = true;
         ballP = ballP || gpayP <= 22.63;
         navigationz += `${((starV ? 2 : 5))}`;
         starV = !starV;
      while (4.99 == gpayP) {
         ballP = !ballP;
         break;
      }
      while (!ballP) {
         gpayP /= Math.max(parseFloat(`${3 * parseInt(`${gpayP}`)}`), 2);
         break;
      }
      do {
          let unselectedd = 0;
         ballP = 23.99 >= gpayP;
         unselectedd >>= Math.min(3, Math.abs(unselectedd - 3));
         if (ballP ? !ballP : ballP) {
            break;
         }
      } while ((ballP ? !ballP : ballP) && (2.52 <= (4.87 - gpayP) && ballP));
      telegramp = `${(1 % (Math.max(3, (right4 ? 3 : 4))))}`;
      telegramp += "2";
      windT %= Math.max(3, ((stationw ? 4 : 5) / (Math.max(parseInt(`${friendsG}`), 7))));
   if (windT == unselectedM) {
      windT >>= Math.min(1, Math.abs(((hooksf ? 5 : 1))));
   }
   while ((5 + views2.length) < 2 && 5 < views2.length) {
      views2 = [1];
      break;
   }
      transfer4 = telegramp.length > 64 || stationw;
      windT <<= Math.min(Math.abs(1), 1);
       let nalyticsO = new Map([[String.fromCharCode(98,101,108,111,119,0),String.fromCharCode(104,97,118,101,95,111,95,51,54,0)], [String.fromCharCode(98,112,114,105,110,116,0),String.fromCharCode(109,101,109,98,101,114,0)], [String.fromCharCode(120,95,55,55,95,112,114,101,118,118,101,99,0),String.fromCharCode(99,109,115,103,115,0)]]);
       let readr = [974, 337, 239];
       let container1 = String.fromCharCode(112,97,114,101,110,116,97,103,101,0);
       let chatn = [863, 808, 73];
         nalyticsO = new Map([[`${chatn.length}`, 3]]);
          let descH = 4.0;
         chatn = [container1.length];
         descH /= Math.max(5, parseFloat(`${2 & parseInt(`${descH}`)}`));
          let with_5v = new Map([[String.fromCharCode(105,110,100,105,99,97,116,111,114,0),579], [String.fromCharCode(110,111,116,99,104,0),645], [String.fromCharCode(97,117,116,111,105,110,99,114,101,109,101,110,116,95,122,95,53,57,0),839]]);
          let h_locko = String.fromCharCode(114,101,111,114,100,101,114,105,110,103,0);
          let clear6 = false;
         container1 = `${h_locko.length * with_5v.size}`;
         with_5v[`${clear6}`] = ((clear6 ? 2 : 5));
         h_locko += `${((clear6 ? 4 : 5) + (clear6 ? 4 : 3))}`;
      while (!container1.startsWith(`${readr.length}`)) {
         readr = [nalyticsO.size + chatn.length];
         break;
      }
         container1 += `${2 + container1.length}`;
         chatn = [chatn.length];
          let private_jP = String.fromCharCode(114,101,102,99,111,117,110,116,101,100,0);
          let main_a8 = new Map([[String.fromCharCode(116,95,55,57,95,102,114,97,109,101,115,0),true ], [String.fromCharCode(114,101,99,111,110,115,116,114,117,99,116,0),false ]]);
          let singleK = new Map([[String.fromCharCode(99,117,114,116,97,105,110,0),936], [String.fromCharCode(111,118,101,114,108,111,97,100,0),305]]);
         nalyticsO[`${chatn.length}`] = 2 + main_a8.size;
         private_jP = `${singleK.size}`;
         main_a8 = new Map([[`${singleK.size}`, (String.fromCharCode(121,0) == private_jP ? private_jP.length : singleK.size)]]);
      do {
          let googleX = String.fromCharCode(103,95,55,48,95,114,101,111,114,100,101,114,0);
          let shrinkg = [778, 139, 192];
          let expandg = 5.0;
         readr.push(googleX.length + 2);
         googleX = `${3 ^ shrinkg.length}`;
         shrinkg.push(1);
         expandg *= parseFloat(`${shrinkg.length}`);
         if (3097459 == readr.length) {
            break;
         }
      } while ((3097459 == readr.length) && (2 == (readr.length ^ 5)));
      telegramp += "2";

    return ((Number(away) / (Number(home) + Number(away))) * 100) ? (Number(away) / (Number(home) + Number(away))) * 100 + '%' : '0%';
   do {
      transfer4 = views2.length == 38;
      if (transfer4 ? !transfer4 : transfer4) {
         break;
      }
   } while ((!transfer4) && (transfer4 ? !transfer4 : transfer4));
      friendsG += (String.fromCharCode(50,0) == telegramp ? (hooksf ? 4 : 2) : telegramp.length);
   while (!telegramp.endsWith(`${serviceI}`)) {
      telegramp += `${flyer5.length}`;
      break;
   }
   while ((parseInt(`${serviceI}`) / (Math.max(flyer5.length, 2))) <= 5 || 3 <= (flyer5.length & 5)) {
      serviceI /= Math.max(4, (parseFloat(`${3 | (stationw ? 1 : 1)}`)));
      break;
   }
   do {
      hooksf = right4 && !stationw;
      if (hooksf ? !hooksf : hooksf) {
         break;
      }
   } while ((1.94 > unselectedM) && (hooksf ? !hooksf : hooksf));
      tickE = [2 | minid.length];
      windT %= Math.max(1, 1);
   for (let i = 0; i < 2; i++) {
       let umengz = 4;
       let l_playerO = 0.0;
       let light5 = [65, 24, 881];
       let bottomJ = [String.fromCharCode(119,105,100,103,101,116,0), String.fromCharCode(110,100,111,116,115,0)];
       let modityc = 2;
          let volume4 = new Map([[String.fromCharCode(99,97,112,115,0),645], [String.fromCharCode(112,114,101,115,101,110,116,0),820], [String.fromCharCode(118,109,97,112,115,105,110,0),750]]);
         modityc %= Math.max(3 % (Math.max(parseInt(`${l_playerO}`), 6)), 3);
         volume4 = new Map([[`${volume4.size}`, volume4.size & volume4.size]]);
      for (let b = 0; b < 2; b++) {
          let gpaye = true;
          let malaysiar = [934, 955];
          let googled = false;
          let pauseo = 5.0;
          let condensedi = [147, 352, 821];
         bottomJ.push(malaysiar.length);
         gpaye = (35 >= ((gpaye ? condensedi.length : 35) + condensedi.length));
         malaysiar.push(((gpaye ? 5 : 5) * parseInt(`${pauseo}`)));
         googled = pauseo >= 9.37;
      }
      if ((modityc >> (Math.min(Math.abs(1), 5))) > 5) {
         umengz >>= Math.min(light5.length, 1);
      }
         umengz &= parseInt(`${l_playerO}`);
      for (let t = 0; t < 2; t++) {
          let hongkong5 = [108, 519, 523];
          let malaysian = String.fromCharCode(115,97,109,112,0);
          let combinee = 5.0;
          let detailu = String.fromCharCode(105,110,102,111,0);
         umengz += malaysian.length | bottomJ.length;
         hongkong5 = [hongkong5.length | parseInt(`${combinee}`)];
         malaysian = `${parseInt(`${combinee}`) - hongkong5.length}`;
         detailu = `${3 >> (Math.min(1, detailu.length))}`;
      }
          let spinner2 = true;
          let constantsf = [50, 687];
          let goalI = String.fromCharCode(119,109,97,112,114,111,100,97,116,97,0);
         light5 = [1];
         spinner2 = constantsf.length == 44;
         constantsf.push((String.fromCharCode(72,0) == goalI ? goalI.length : constantsf.length));
          let nnewss = true;
          let vignettel = 2;
         modityc /= Math.max(1 >> (Math.min(Math.abs(parseInt(`${l_playerO}`)), 2)), 3);
         nnewss = !nnewss;
         vignettel <<= Math.min(4, Math.abs(vignettel << (Math.min(3, Math.abs(3)))));
       let submitk = 0.0;
      while (4 < (umengz - 1)) {
         umengz *= light5.length;
         break;
      }
          let plush = [686, 568, 479];
         submitk *= plush.length >> (Math.min(Math.abs(3), 2));
       let refreshU = 4.0;
       let favorite0 = 2.0;
         bottomJ.push(1);
      for (let i = 0; i < 2; i++) {
         l_playerO /= Math.max(2, parseFloat(`${parseInt(`${favorite0}`)}`));
      }
      do {
          let icons = String.fromCharCode(121,99,98,99,114,0);
          let logon = String.fromCharCode(114,99,118,100,0);
          let animationV = 4.0;
         l_playerO *= parseFloat(`${2}`);
         icons += `${logon.length - 3}`;
         logon = `${logon.length}`;
         animationV -= parseFloat(`${3 ^ logon.length}`);
         if (4060130.0 == l_playerO) {
            break;
         }
      } while ((4060130.0 == l_playerO) && (submitk < l_playerO));
      do {
          let interstitialO = 0;
          let policyo = 4;
          let dropdowni = String.fromCharCode(115,117,98,116,114,101,101,0);
         submitk /= Math.max(3 >> (Math.min(Math.abs(parseInt(`${submitk}`)), 5)), 5);
         interstitialO -= dropdowni.length;
         policyo += dropdowni.length;
         if (submitk == 2625755.0) {
            break;
         }
      } while ((5.57 <= (submitk / (Math.max(2, modityc)))) && (submitk == 2625755.0));
      minid.push(2);
   }
   if (flyer5.length >= 1 || layoutg) {
      layoutg = stationw;
   }
   while (1.37 > friendsG) {
      telegramp += `${views2.length & 3}`;
      break;
   }
   for (let i = 0; i < 3; i++) {
      layoutg = tickE.length == 31 || hooksf;
   }
      flyer5 += "3";
   if (5.96 <= (1.84 * friendsG) || 1.84 <= friendsG) {
       let placeholderZ = new Map([[String.fromCharCode(99,111,101,102,102,115,112,0),128], [String.fromCharCode(112,114,101,115,117,109,101,95,121,95,56,56,0),482], [String.fromCharCode(109,97,116,105,99,0),744]]);
       let becomes = 0.0;
       let referrerr = String.fromCharCode(106,95,57,49,95,98,115,116,114,97,99,116,0);
      do {
         becomes += parseInt(`${becomes}`);
         if (becomes == 2254133.0) {
            break;
         }
      } while ((!Array.from(placeholderZ.values()).includes(becomes)) && (becomes == 2254133.0));
      while (4.59 >= (becomes * referrerr.length) || (parseInt(`${becomes}`) * referrerr.length) >= 5) {
         becomes += 1;
         break;
      }
       let productj = String.fromCharCode(100,97,109,112,0);
      for (let k = 0; k < 1; k++) {
          let weiboh = 0;
          let humidityz = 1;
          let entryi = new Map([[String.fromCharCode(116,114,97,118,101,114,115,101,0),String.fromCharCode(110,97,118,105,103,97,116,105,111,110,0)], [String.fromCharCode(99,111,112,121,102,100,0),String.fromCharCode(121,98,121,114,0)], [String.fromCharCode(105,112,112,108,101,0),String.fromCharCode(116,119,105,99,101,0)]]);
          let constantsX = 1.0;
         placeholderZ[`${weiboh}`] = productj.length - 3;
         weiboh |= parseInt(`${constantsX}`) + 1;
         humidityz *= humidityz;
         entryi = new Map([[`${entryi.size}`, entryi.size]]);
         constantsX /= Math.max(parseFloat(`${1}`), 3);
      }
      while (2 >= (placeholderZ.size + referrerr.length) && (referrerr.length + 2) >= 1) {
         referrerr = `${placeholderZ.size / (Math.max(9, productj.length))}`;
         break;
      }
      for (let g = 0; g < 2; g++) {
         placeholderZ = new Map([[referrerr, productj.length]]);
      }
      while (1 <= (placeholderZ.size ^ productj.length) && (placeholderZ.size ^ 1) <= 3) {
         placeholderZ[`${becomes}`] = 2 + referrerr.length;
         break;
      }
         placeholderZ[productj] = productj.length / 3;
         becomes -= 3;
      friendsG -= views2.length / 1;
   }
      stationw = friendsG < 58.71;
   for (let a = 0; a < 2; a++) {
      serviceI *= parseFloat(`${telegramp.length}`);
   }
      minid.push(1);
   do {
      unselectedM -= parseFloat(`${1}`);
      if (4014542.0 == unselectedM) {
         break;
      }
   } while ((4014542.0 == unselectedM) && ((flyer5.length / 5) == 4));
       let guide2 = 4;
      while (5 >= (guide2 / 2)) {
         guide2 |= guide2;
         break;
      }
         guide2 &= guide2 & guide2;
      if ((5 ^ guide2) >= 2 && (guide2 ^ guide2) >= 5) {
         guide2 &= guide2 << (Math.min(2, Math.abs(guide2)));
      }
      minid = [((stationw ? 4 : 5) | parseInt(`${serviceI}`))];
      layoutg = !layoutg;

  };

  return (
    <View key={`matchAverage-${index}`} style={{height: 43}}>
            <View style={styles.statTitleView}>
              <Text style={styles.statTitleText}>{sportType == 1 ? vars.footballMatchStats[type] : type}</Text>
            </View>
            <View style={styles.statView}>
              <View style={styles.statTextView}>
                <Text style={styles.statText}>{data.home}</Text>
              </View>
              <View style={home == 0 && away == 0 ? [styles.statBar, {backgroundColor: '#383838'}] : styles.statBar}>
                <View
                  style={{
                    height: 5,
                    borderRadius: 4,
                    backgroundColor: '#FAC33D',
                    width: homePercent(),
                  }}></View>
              </View>
              <View style={styles.statTextViewRight}>
                <Text style={styles.statText}>{data.away}</Text>
              </View>
            </View>
          </View>
    // <View style={styles.matchStatisticView}>
    //   <View style={styles.homeStatisticView}>
    //     <View style={{width: '47%'}}>
    //       <Text style={true ? styles.highStatisticText : styles.statisticText}>
    
    //       </Text>
    //     </View>
    //     <View style={styles.progressBar}>
    
    
    
    
    
    
    //         }}></View>
    //     </View>
    //   </View>
    //   <View style={styles.statisticTitleView}>
    //     <Text style={styles.statisticTitleText}>
    
    //     </Text>
    //   </View>
    //   <View style={styles.awayStatisticView}>
    //     <View style={styles.progressBar}>
    
    
    
    
    
    //         }}></View>
    //     </View>
    //     <View style={{width: '47%', alignItems: 'flex-end'}}>
    //       <Text style={true ? styles.highStatisticText : styles.statisticText}>
    
    //       </Text>
    //     </View>
    //   </View>
    // </View>
  );
};

export default MatchStatisticComponent;
