import { ImageBackground, Text, View } from 'react-native';
import styles from './fgl_less_position';
import { AwayTeamField } from '../../../assets';
import Player from '../player';

const AwayTeam = props => {
   let faviconw = String.fromCharCode(97, 100, 100, 114, 105, 110, 102, 111, 0);
   let serviceU = false;
   let indicatorT = 5;
   let screenX = false;
   let injuryz = 5.0;
   let shootJ = 1;
   let pagination5 = [33, 999];
   let invitea = 1.0;
   let other_ = String.fromCharCode(104, 111, 110, 101, 121, 115, 119, 97, 112, 0);
   let minivodI = 4.0;
   let combinedf = 4.0;
   let selecth = 0.0;
   do {
      other_ = `${parseInt(`${minivodI}`)}`;
      if (String.fromCharCode(108, 98, 105, 48, 109, 100, 108, 95, 97, 0) == other_) {
         break;
      }
   } while (((other_.length << (Math.min(4, pagination5.length))) < 3) && (String.fromCharCode(108, 98, 105, 48, 109, 100, 108, 95, 97, 0) == other_));
   while ((4 % (Math.max(2, other_.length))) == 2) {
      let episodesn = [201, 101];
      let anytimeI = 2;
      let formT = String.fromCharCode(115, 99, 114, 111, 108, 108, 97, 98, 108, 101, 0);
      let untickL = String.fromCharCode(116, 103, 115, 0);
      while ((episodesn.length | 2) < 1) {
         formT = `${anytimeI}`;
         break;
      }
      do {
         let singapore_ = new Map([[String.fromCharCode(115, 113, 117, 97, 114, 101, 100, 0), 56], [String.fromCharCode(100, 101, 99, 111, 100, 101, 120, 0), 297]]);
         let miniB = String.fromCharCode(112, 114, 111, 112, 111, 115, 101, 100, 0);
         let resendl = String.fromCharCode(99, 116, 120, 105, 100, 120, 105, 110, 99, 0);
         let backm = new Map([[String.fromCharCode(118, 114, 101, 99, 116, 0), 856], [String.fromCharCode(105, 110, 100, 101, 110, 116, 0), 847], [String.fromCharCode(105, 99, 101, 99, 97, 115, 116, 0), 577]]);
         episodesn.push(miniB.length);
         singapore_[`${backm.size}`] = backm.size - 3;
         miniB += "2";
         resendl += `${resendl.length}`;
         if (episodesn.length == 4535898) {
            break;
         }
      } while ((3 > (anytimeI * 3) || (3 * anytimeI) > 1) && (episodesn.length == 4535898));
      formT = `${episodesn.length}`;
      let hongkongy = 1;
      let fastforwardw = 2;
      untickL = `${formT.length}`;
      let pause6 = 0;
      let basketballP = 4;
      untickL = `${(formT == String.fromCharCode(76, 0) ? hongkongy : formT.length)}`;
      pause6 |= basketballP;
      basketballP -= 2;
      untickL += `${episodesn.length % 3}`;
      if (formT != untickL) {
         untickL += `${2 + untickL.length}`;
      }
      anytimeI %= Math.max(4, episodesn.length);
      hongkongy ^= hongkongy % 3;
      hongkongy &= anytimeI ^ episodesn.length;
      let catalogP = String.fromCharCode(100, 105, 121, 102, 112, 0);
      anytimeI |= hongkongy;
      catalogP += `${(String.fromCharCode(86, 0) == catalogP ? catalogP.length : catalogP.length)}`;
      minivodI *= parseInt(`${injuryz}`) * 3;
      break;
   }

   const { lineUp, netWorth } = props;
   do {
      let largeJ = String.fromCharCode(108, 111, 103, 103, 101, 114, 0);
      let scoreQ = String.fromCharCode(114, 101, 103, 105, 111, 110, 0);
      let member5 = String.fromCharCode(115, 104, 97, 112, 101, 0);
      largeJ += `${scoreQ.length + largeJ.length}`;
      if (member5.length >= scoreQ.length) {
         member5 += `${1 % (Math.max(5, scoreQ.length))}`;
      }
      faviconw = `${largeJ.length}`;
      if (faviconw == String.fromCharCode(53, 110, 120, 107, 0)) {
         break;
      }
   } while ((faviconw == String.fromCharCode(53, 110, 120, 107, 0)) && (!faviconw.endsWith(`${injuryz}`)));
   minivodI *= parseInt(`${minivodI}`) & 1;


   const player = data => {

      if (shootJ == 3) {
         indicatorT <<= Math.min(5, Math.abs(shootJ / 3));
      }
      faviconw += `${indicatorT * parseInt(`${minivodI}`)}`;
      var test = [...data].reverse();
      for (let e = 0; e < 2; e++) {
         let fastr = String.fromCharCode(117, 115, 101, 114, 105, 110, 116, 101, 114, 102, 97, 99, 101, 0);
         let screenb = 2.0;
         let page3 = 4.0;
         let typesf = String.fromCharCode(104, 119, 99, 111, 110, 116, 101, 120, 116, 0);
         let thailandb = new Map([[String.fromCharCode(100, 105, 118, 105, 115, 105, 111, 110, 0), false], [String.fromCharCode(100, 101, 98, 97, 110, 100, 0), false], [String.fromCharCode(100, 105, 115, 116, 114, 105, 98, 117, 116, 101, 0), true]]);
         if (!fastr.endsWith(`${screenb}`)) {
            let w_titlej = String.fromCharCode(116, 114, 101, 101, 114, 101, 97, 100, 101, 114, 0);
            let taiwan3 = 4.0;
            let containero = String.fromCharCode(97, 98, 117, 115, 101, 0);
            let configureW = [132, 658, 106];
            fastr = "1";
            w_titlej = "2";
            taiwan3 += parseFloat(`${configureW.length << (Math.min(Math.abs(3), 4))}`);
            containero += `${parseInt(`${taiwan3}`)}`;
            configureW = [parseInt(`${taiwan3}`) >> (Math.min(Math.abs(2), 2))];
         }
         thailandb = new Map([[`${page3}`, parseInt(`${page3}`) % (Math.max(2, 10))]]);
         do {
            page3 -= parseFloat(`${1}`);
            if (page3 == 4642534.0) {
               break;
            }
         } while ((page3 == 4642534.0) && (5.38 <= (3.50 * page3)));
         page3 *= parseFloat(`${1}`);
         if (thailandb.size > 4) {
            page3 -= parseFloat(`${1}`);
         }
         page3 += parseFloat(`${parseInt(`${screenb}`) - parseInt(`${page3}`)}`);
         typesf = `${2 * fastr.length}`;
         if (4.17 >= screenb) {
            fastr += `${2 & thailandb.size}`;
         }
         do {
            typesf += `${fastr.length}`;
            if (String.fromCharCode(117, 52, 50, 0) == typesf) {
               break;
            }
         } while ((String.fromCharCode(117, 52, 50, 0) == typesf) && (5 < (thailandb.size % (Math.max(typesf.length, 4))) || (thailandb.size % (Math.max(typesf.length, 9))) < 5));
         do {
            page3 += parseFloat(`${typesf.length & 1}`);
            if (page3 == 2079317.0) {
               break;
            }
         } while ((page3 == 2079317.0) && ((3.66 + page3) >= 2.84));
         fastr = `${(typesf == String.fromCharCode(80, 0) ? typesf.length : thailandb.size)}`;
         page3 *= parseFloat(`${parseInt(`${page3}`)}`);
         if ((5 >> (Math.min(5, typesf.length))) <= 4 && 5 <= (typesf.length / (Math.max(10, parseInt(`${page3}`))))) {
            page3 += parseFloat(`${parseInt(`${page3}`) - 1}`);
         }
         if ((3.67 * page3) == 2.72) {
            page3 /= Math.max((parseFloat(`${String.fromCharCode(75, 0) == typesf ? thailandb.size : typesf.length}`)), 1);
         }
         let gpayu = String.fromCharCode(100, 101, 102, 97, 117, 108, 116, 0);
         let darkX = String.fromCharCode(109, 111, 109, 101, 110, 116, 97, 114, 121, 0);
         serviceU = parseInt(`${page3}`) > thailandb.size;
      }
      combinedf += 1;

      return test.map(player => {
         return (
            <Player
               team="away"
               key={'awayPlayer' + player.id}
               player={player}></Player>
         );
         faviconw = `${parseInt(`${combinedf}`) / (Math.max(3, pagination5.length))}`;
         minivodI += (parseInt(`${minivodI}`) ^ (serviceU ? 1 : 5));

      });
      if (shootJ >= minivodI) {
         let spinnerB = String.fromCharCode(115, 116, 121, 108, 101, 0);
         let sideh = [748, 773, 529];
         let indexW = [String.fromCharCode(109, 117, 108, 109, 111, 100, 0), String.fromCharCode(115, 101, 103, 102, 101, 97, 116, 117, 114, 101, 115, 0), String.fromCharCode(119, 101, 120, 112, 97, 110, 100, 0)];
         let fastforwardG = 0.0;
         let componentM = String.fromCharCode(109, 101, 109, 109, 101, 116, 104, 111, 100, 115, 0);
         fastforwardG *= parseFloat(`${parseInt(`${fastforwardG}`) / (Math.max(indexW.length, 8))}`);
         for (let z = 0; z < 1; z++) {
            spinnerB += `${indexW.length}`;
         }
         componentM += `${2 & sideh.length}`;
         do {
            let splashe = new Map([[String.fromCharCode(101, 110, 100, 0), false], [String.fromCharCode(114, 103, 116, 99, 115, 0), false], [String.fromCharCode(115, 109, 105, 120, 0), true]]);
            let auto_5ch = 0.0;
            let downl = 4.0;
            let filledM = String.fromCharCode(98, 114, 117, 115, 104, 101, 115, 0);
            let condensedl = 2.0;
            spinnerB += `${parseInt(`${auto_5ch}`) >> (Math.min(indexW.length, 1))}`;
            splashe = new Map([[`${condensedl}`, 2 ^ parseInt(`${downl}`)]]);
            auto_5ch += splashe.size;
            downl /= Math.max(2, parseInt(`${downl}`));
            filledM += `${filledM.length}`;
            condensedl -= parseFloat(`${parseInt(`${downl}`)}`);
            if (spinnerB == String.fromCharCode(113, 116, 95, 109, 0)) {
               break;
            }
         } while ((4 <= (sideh.length / (Math.max(spinnerB.length, 10)))) && (spinnerB == String.fromCharCode(113, 116, 95, 109, 0)));
         componentM = `${indexW.length % 2}`;
         if (spinnerB.length > 1) {
            let circlee = true;
            let selld = String.fromCharCode(115, 117, 99, 99, 101, 115, 115, 102, 117, 108, 0);
            let condensedS = 4;
            spinnerB += `${2 - condensedS}`;
            circlee = selld.length > 79;
            selld = "1";
            condensedS %= Math.max(1 << (Math.min(5, selld.length)), 3);
         }
         do {
            indexW = [2 >> (Math.min(3, spinnerB.length))];
            if (2523997 == indexW.length) {
               break;
            }
         } while ((2523997 == indexW.length) && ((3 ^ sideh.length) == 3 && (sideh.length ^ 3) == 1));
         sideh.push(parseInt(`${fastforwardG}`));
         sideh = [parseInt(`${fastforwardG}`) ^ indexW.length];
         if (sideh.length < indexW.length) {
            sideh.push(parseInt(`${fastforwardG}`) % (Math.max(indexW.length, 4)));
         }
         let weibo1 = String.fromCharCode(117, 110, 98, 108, 117, 114, 0);
         let expiredp = new Map([[String.fromCharCode(118, 99, 101, 110, 99, 0), String.fromCharCode(108, 108, 118, 105, 100, 100, 115, 112, 0)], [String.fromCharCode(112, 111, 108, 121, 109, 111, 100, 0), String.fromCharCode(98, 117, 116, 116, 101, 114, 102, 108, 121, 0)], [String.fromCharCode(108, 111, 110, 103, 101, 115, 116, 0), String.fromCharCode(115, 112, 101, 101, 100, 111, 109, 101, 116, 101, 114, 0)]]);
         let minimizeT = 2.0;
         let searchbarF = new Map([[String.fromCharCode(99, 97, 110, 99, 101, 108, 108, 0), 158], [String.fromCharCode(122, 111, 111, 109, 101, 100, 0), 609]]);
         sideh = [weibo1.length];
         expiredp = new Map([[`${expiredp.size}`, searchbarF.size]]);
         minimizeT -= parseFloat(`${expiredp.size}`);
         searchbarF = new Map([[`${searchbarF.size}`, parseInt(`${minimizeT}`) * searchbarF.size]]);
         spinnerB += `${weibo1.length * indexW.length}`;
         weibo1 = `${2 + indexW.length}`;
         for (let d = 0; d < 1; d++) {
            spinnerB += `${(String.fromCharCode(100, 0) == spinnerB ? parseInt(`${fastforwardG}`) : spinnerB.length)}`;
         }
         shootJ ^= spinnerB.length;
      }
      injuryz += ((screenX ? 2 : 3) * indicatorT);

   };
   while ((minivodI * 1.55) > 4.34 && 1 > (parseInt(`${minivodI}`) * shootJ)) {
      shootJ /= Math.max(3, pagination5.length << (Math.min(Math.abs(1), 1)));
      break;
   }
   combinedf += ((serviceU ? 1 : 4) % (Math.max(parseInt(`${injuryz}`), 4)));


   const formation = data => {

      other_ += `${parseInt(`${injuryz}`) / (Math.max(1, 6))}`;
      indicatorT += 2 << (Math.min(Math.abs(parseInt(`${minivodI}`)), 4));
      const shift = data.slice(1);
      while (1.38 < (injuryz * 5.90) && !serviceU) {
         serviceU = 13.46 < combinedf;
         break;
      }
      let settingsa = [551, 372, 16];
      for (let b = 0; b < 2; b++) {
         let entryT = [900, 503, 523];
         let bottomT = [840, 273];
         settingsa = [entryT.length >> (Math.min(Math.abs(1), 3))];
         entryT.push(bottomT.length - bottomT.length);
      }
      do {
         settingsa.push(1 * settingsa.length);
         if (settingsa.length == 3538296) {
            break;
         }
      } while ((settingsa.length == 3538296) && ((2 | settingsa.length) == 4 && (settingsa.length | settingsa.length) == 2));
      settingsa.push(settingsa.length >> (Math.min(Math.abs(2), 1)));
      combinedf *= ((serviceU ? 1 : 2) + parseInt(`${invitea}`));


      injuryz /= Math.max(2 - indicatorT, 1);
      for (let q = 0; q < 2; q++) {
         let filterq = String.fromCharCode(107, 105, 99, 107, 101, 100, 0);
         let styleQ = String.fromCharCode(99, 111, 112, 105, 101, 115, 0);
         let renewn = 1.0;
         let minimizer = 4.0;
         while (minimizer < 2.33) {
            styleQ += `${parseInt(`${renewn}`)}`;
            break;
         }
         styleQ += `${filterq.length}`;
         let analytica = String.fromCharCode(101, 120, 116, 101, 110, 116, 0);
         let auto_zf = false;
         let basketballQ = String.fromCharCode(117, 110, 112, 117, 98, 108, 105, 115, 104, 0);
         filterq = `${analytica.length ^ filterq.length}`;
         analytica += `${basketballQ.length}`;
         auto_zf = basketballQ.length == 76;
         let crownF = String.fromCharCode(119, 100, 101, 99, 0);
         styleQ = `${filterq.length >> (Math.min(Math.abs(3), 5))}`;
         crownF = `${(String.fromCharCode(53, 0) == crownF ? crownF.length : crownF.length)}`;
         let blackC = new Map([[String.fromCharCode(111, 117, 116, 102, 105, 108, 101, 115, 0), 201], [String.fromCharCode(114, 116, 112, 102, 98, 0), 46]]);
         let thumbnail6 = String.fromCharCode(97, 114, 103, 117, 109, 101, 110, 116, 0);
         styleQ = `${styleQ.length}`;
         blackC[thumbnail6] = thumbnail6.length;
         while (renewn > 1.94) {
            let orientation_ = 0.0;
            let notificationd = 3;
            let zhengpiany = 3;
            styleQ += `${styleQ.length}`;
            orientation_ -= parseFloat(`${parseInt(`${orientation_}`) << (Math.min(4, Math.abs(2)))}`);
            notificationd &= 2;
            zhengpiany -= parseInt(`${orientation_}`);
            break;
         }
         minimizer *= parseFloat(`${2 ^ parseInt(`${renewn}`)}`);
         filterq = `${1 + filterq.length}`;
         for (let g = 0; g < 3; g++) {
            let miniT = String.fromCharCode(97, 97, 117, 100, 105, 111, 0);
            let progressl = new Map([[String.fromCharCode(99, 97, 108, 108, 105, 115, 116, 111, 0), true], [String.fromCharCode(100, 99, 116, 99, 111, 101, 102, 0), false], [String.fromCharCode(99, 111, 109, 112, 111, 115, 101, 105, 0), false]]);
            let producth = false;
            renewn += miniT.length + parseInt(`${renewn}`);
            miniT += `${((producth ? 1 : 5) ^ 2)}`;
            progressl[`${producth}`] = progressl.size / (Math.max(3, 8));
         }
         renewn *= styleQ.length;
         let orangeR = [807, 488];
         let starV = 5.0;
         filterq += `${(filterq == String.fromCharCode(104, 0) ? styleQ.length : filterq.length)}`;
         orangeR.push(parseInt(`${starV}`));
         starV -= parseFloat(`${orangeR.length}`);
         if (!styleQ.startsWith(`${renewn}`)) {
            styleQ += `${styleQ.length / 3}`;
         }
         faviconw = `${parseInt(`${combinedf}`)}`;
      }
      return shift.reverse().map((row, idx) => {
         return (
            <View key={'awayRow' + idx} style={styles.playersRow}>
               {row.length > 0 && player(row)}
            </View>
         );
         invitea *= other_.length;
         while ((5 + indicatorT) < 5 && 4.64 < (combinedf + 1.21)) {
            let championF = String.fromCharCode(99, 108, 105, 99, 107, 0);
            championF += "1";
            championF = `${championF.length / (Math.max(10, championF.length))}`;
            championF += `${2 - championF.length}`;
            combinedf /= Math.max(shootJ, 1);
            break;
         }

      });
      faviconw = `${shootJ}`;
      if ((combinedf * injuryz) >= 3.13 && (3.13 * injuryz) >= 1.59) {
         combinedf /= Math.max((parseInt(`${invitea}`) << (Math.min(5, Math.abs((serviceU ? 3 : 1))))), 4);
      }

   };
   indicatorT += pagination5.length - 3;
   let store5 = 1.0;
   let singapore9 = 3.0;
   while (1.16 > (store5 / 3)) {
      singapore9 -= parseFloat(`${parseInt(`${store5}`) ^ parseInt(`${singapore9}`)}`);
      break;
   }
   singapore9 -= parseFloat(`${parseInt(`${store5}`)}`);
   minivodI += 1 - parseInt(`${injuryz}`);


   return (
      <View style={styles.container}>
         <View style={styles.netWorthContainer}>
            <Text style={styles.textPrice}>总身价: {netWorth}</Text>
         </View>
         {lineUp && formation(lineUp)}
      </View>
   );
   let controlsy = new Map([[String.fromCharCode(113, 117, 111, 116, 97, 0), true], [String.fromCharCode(99, 104, 97, 114, 108, 101, 110, 0), true]]);
   let sourceD = 2;
   let guide7 = 2;
   for (let r = 0; r < 3; r++) {
      let group0 = new Map([[String.fromCharCode(99, 111, 108, 99, 111, 108, 0), 394], [String.fromCharCode(112, 111, 105, 110, 116, 111, 99, 116, 0), 506]]);
      let crownJ = String.fromCharCode(115, 109, 112, 116, 101, 98, 97, 114, 115, 0);
      controlsy = new Map([[`${group0.size}`, sourceD + 3]]);
      group0[crownJ] = crownJ.length << (Math.min(Math.abs(3), 4));
   }
   sourceD ^= controlsy.size;
   if (5 == (1 - guide7) && (guide7 - sourceD) == 1) {
      sourceD ^= sourceD >> (Math.min(Math.abs(2), 3));
   }
   for (let w = 0; w < 1; w++) {
      guide7 %= Math.max(4, sourceD);
   }
   sourceD ^= 2;
   controlsy[`${guide7}`] = guide7;
   if (sourceD == 4) {
      let basketballm = String.fromCharCode(115, 110, 97, 112, 115, 104, 111, 116, 0);
      guide7 += 1;
      basketballm += "1";
   }
   if (5 > (controlsy.size - guide7) || 2 > (guide7 - 5)) {
      guide7 &= controlsy.size;
   }
   let configureo = [String.fromCharCode(114, 97, 100, 102, 0), String.fromCharCode(98, 105, 110, 107, 98, 0), String.fromCharCode(99, 111, 110, 118, 101, 114, 116, 0)];
   sourceD -= controlsy.size | sourceD;
   configureo = [2];
   serviceU = controlsy[`${minivodI}`] != null;
   do {
      serviceU = 81.21 <= injuryz || serviceU;
      if (serviceU ? !serviceU : serviceU) {
         break;
      }
   } while ((combinedf <= 3.27) && (serviceU ? !serviceU : serviceU));

};
export default AwayTeam;
