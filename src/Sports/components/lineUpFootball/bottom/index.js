import {Image, Text, View} from 'react-native';
import {logoUser} from '../../../assets';
import styles from './ww_utils_orangeclock';

const Bottom = props => {
       let settingsM = String.fromCharCode(116,98,117,102,95,54,95,51,55,0);
    let actionsA = false;
    let bingG = String.fromCharCode(105,110,100,101,110,116,95,119,95,54,49,0);
    let gradlewV = String.fromCharCode(112,95,49,53,95,115,117,98,102,105,101,108,100,115,0);
    let streamingJ = String.fromCharCode(121,95,55,51,95,104,119,97,99,99,101,108,115,0);
    let release_vwm = 0.0;
    let turndownQ = false;
    let combineW = String.fromCharCode(102,95,52,51,95,109,112,97,100,115,112,0);
    let fullj = false;
      actionsA = 82.38 == release_vwm && !actionsA;
      release_vwm += ((actionsA ? 5 : 2));

  const {teamIcon, formation, coach} = props;
   do {
      streamingJ += `${1 * streamingJ.length}`;
      if (streamingJ.length == 3909198) {
         break;
      }
   } while ((streamingJ.length == 3909198) && (1 < (streamingJ.length / (Math.max(3, 7))) || (parseInt(`${release_vwm}`) - streamingJ.length) < 3));
   do {
      gradlewV = `${(String.fromCharCode(75,0) == gradlewV ? gradlewV.length : (turndownQ ? 1 : 5))}`;
      if (3531901 == gradlewV.length) {
         break;
      }
   } while ((3531901 == gradlewV.length) && (actionsA));


  return (
    <View style={styles.bottom}>
      <View style={styles.teamContainer}>
        <View style={styles.imageContianer}>
          <Image
            style={styles.imageLogo}
            source={{uri: teamIcon}}
            resizeMode="contain"
          />
        </View>
        <View style={styles.formationContainer}>
          <Text style={styles.text}>阵型: {formation}</Text>
          <Text style={styles.text}>教练: {coach}</Text>
        </View>
      </View>
    </View>
  );
      bingG = `${bingG.length}`;
   if (gradlewV.length < 2 && !turndownQ) {
       let becomeS = 4;
       let macau9 = String.fromCharCode(97,112,101,114,95,49,95,53,49,0);
       let profileactive8 = String.fromCharCode(107,95,50,54,95,101,108,105,103,105,98,108,101,0);
       let activityC = 4.0;
      for (let c = 0; c < 1; c++) {
         activityC -= parseInt(`${activityC}`);
      }
         macau9 = `${parseInt(`${activityC}`) & profileactive8.length}`;
       let circleZ = String.fromCharCode(109,95,50,50,95,111,112,116,105,111,110,115,0);
         macau9 += `${parseInt(`${activityC}`) + becomeS}`;
          let gradlewM = 0.0;
          let lightQ = [19, 836, 862];
         circleZ += `${becomeS % (Math.max(macau9.length, 7))}`;
         gradlewM += 2;
         lightQ.push(lightQ.length);
          let binddatas1 = String.fromCharCode(102,108,105,112,112,101,100,95,105,95,53,56,0);
          let detailS = String.fromCharCode(98,117,99,107,101,116,0);
         macau9 = "1";
         binddatas1 += `${(binddatas1 == String.fromCharCode(52,0) ? detailS.length : binddatas1.length)}`;
         detailS += `${(String.fromCharCode(79,0) == binddatas1 ? binddatas1.length : detailS.length)}`;
      if (3 >= profileactive8.length) {
          let executorS = 5.0;
          let downloaderI = 1.0;
         activityC *= circleZ.length ^ becomeS;
         executorS -= parseInt(`${downloaderI}`) / (Math.max(parseInt(`${executorS}`), 5));
         downloaderI += parseFloat(`${parseInt(`${executorS}`) * parseInt(`${downloaderI}`)}`);
      }
          let codeh = [270, 628, 31];
          let borderlessg = 4.0;
          let xvodT = [612, 924];
         profileactive8 = `${circleZ.length >> (Math.min(2, Math.abs(parseInt(`${activityC}`))))}`;
         codeh.push(3);
         borderlessg += parseInt(`${borderlessg}`);
         xvodT = [codeh.length / 1];
         circleZ += `${1 ^ circleZ.length}`;
         circleZ += "2";
      do {
          let l_image1 = 4;
          let sharedm = 4;
         activityC += circleZ.length << (Math.min(Math.abs(1), 5));
         l_image1 -= l_image1 % (Math.max(3, 8));
         sharedm *= l_image1;
         if (4257948.0 == activityC) {
            break;
         }
      } while ((4257948.0 == activityC) && ((activityC * becomeS) < 4.96 || (becomeS * activityC) < 4.96));
         profileactive8 += "1";
      turndownQ = String.fromCharCode(102,0) == streamingJ;
   }

};

export default Bottom;
