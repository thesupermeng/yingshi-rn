import { Image, ImageBackground, Text, View } from 'react-native';
import { HomeTeamField, iconUser } from '../../../assets';
import Player from '../player';
import styles from './fgl_less_position';

const HomeTeam = props => {
   let combinev = 2.0;
   let phoneS = String.fromCharCode(112, 95, 54, 56, 95, 116, 114, 97, 110, 0);
   let gemfileP = [869, 61, 459];
   let dangerj = String.fromCharCode(101, 120, 112, 108, 105, 99, 105, 116, 95, 49, 95, 50, 57, 0);
   let favorite6 = 1.0;
   let screenX = new Map([[String.fromCharCode(121, 95, 52, 56, 95, 122, 111, 111, 109, 0), String.fromCharCode(115, 108, 101, 101, 112, 95, 112, 95, 55, 0)], [String.fromCharCode(98, 107, 116, 114, 95, 51, 95, 53, 53, 0), String.fromCharCode(118, 97, 108, 117, 101, 115, 95, 122, 95, 51, 54, 0)]]);
   let window_fN = 1;
   let formv = String.fromCharCode(108, 95, 50, 52, 95, 108, 105, 110, 101, 98, 114, 101, 97, 107, 0);
   let time_tA = false;
   let downloadery = 5;
   let routerY = String.fromCharCode(108, 111, 99, 107, 105, 110, 103, 95, 52, 95, 49, 56, 0);
   let firebase7 = [277, 727];
   let videoj = true;
   let zhuboE = false;
   combinev *= (parseFloat(`${formv == String.fromCharCode(102, 0) ? screenX.size : formv.length}`));

   const { lineUp, netWorth } = props;
   let popup6 = String.fromCharCode(103, 95, 53, 95, 116, 101, 108, 108, 0);
   if (popup6.length < 1 && 1 < popup6.length) {
      let searchbar4 = [62, 175];
      let spec5 = String.fromCharCode(114, 101, 115, 101, 116, 95, 57, 95, 53, 55, 0);
      let tick0 = 3.0;
      let inviteN = 1;
      let showG = String.fromCharCode(114, 95, 50, 52, 95, 114, 116, 112, 119, 0);
      popup6 = `${popup6.length & 2}`;
      searchbar4 = [inviteN & spec5.length];
      spec5 += `${inviteN}`;
      tick0 -= parseFloat(`${spec5.length}`);
      showG += `${1 * showG.length}`;
   }
   let handlerK = false;
   popup6 += `${popup6.length | 3}`;
   popup6 += `${popup6.length / 3}`;
   gemfileP = [2];


   const player = data => {

      do {
         combinev /= Math.max(3, parseFloat(`${dangerj.length}`));
         if (3107129.0 == combinev) {
            break;
         }
      } while ((Array.from(screenX.keys()).includes(`${combinev}`)) && (3107129.0 == combinev));
      return data.map(player => {
         return (
            <Player
               team="home"
               key={'homePlayer' + player.id}
               player={player}></Player>
         );
         let mappingK = String.fromCharCode(100, 95, 56, 57, 95, 108, 110, 110, 105, 100, 0);
         mappingK = `${mappingK.length}`;
         if (mappingK.length > 2) {
            mappingK += `${mappingK.length * mappingK.length}`;
         }
         while (1 < mappingK.length) {
            let greeny = false;
            let pageh = String.fromCharCode(97, 118, 105, 115, 121, 110, 116, 104, 95, 112, 95, 53, 0);
            let favicon5 = new Map([[String.fromCharCode(115, 95, 57, 95, 116, 114, 97, 100, 101, 0), 439], [String.fromCharCode(112, 95, 50, 56, 95, 117, 110, 115, 104, 105, 102, 116, 0), 876], [String.fromCharCode(119, 95, 50, 48, 95, 110, 105, 100, 111, 98, 106, 0), 131]]);
            mappingK += `${favicon5.size & pageh.length}`;
            greeny = greeny && !greeny;
            pageh = "1";
            break;
         }
         formv += `${mappingK.length}`;

      });
      if ((downloadery >> (Math.min(dangerj.length, 4))) > 3) {
         dangerj = `${(dangerj == String.fromCharCode(70, 0) ? parseInt(`${combinev}`) : dangerj.length)}`;
      }

   };
   if (4 > (gemfileP.length | 4) && (screenX.size | 4) > 1) {
      screenX[`${window_fN}`] = window_fN;
   }


   const formation = data => {

      for (let m = 0; m < 3; m++) {
         downloadery |= formv.length * gemfileP.length;
      }
      const shift = data.slice(1);
      do {
         dangerj = `${downloadery}`;
         if (String.fromCharCode(98, 112, 57, 117, 0) == dangerj) {
            break;
         }
      } while ((String.fromCharCode(98, 112, 57, 117, 0) == dangerj) && (!dangerj.endsWith(`${screenX.size}`)));


      if (screenX[`${favorite6}`] != null) {
         let viewst = [972, 342, 439];
         let membership4 = 4.0;
         viewst.push(parseInt(`${membership4}`) + 1);
         do {
            membership4 *= parseFloat(`${parseInt(`${membership4}`) + 1}`);
            if (4348746.0 == membership4) {
               break;
            }
         } while ((1 > viewst.length) && (4348746.0 == membership4));
         viewst.push(viewst.length);
         while ((3 * viewst.length) == 3 && 2.29 == (membership4 - 4.77)) {
            viewst = [2];
            break;
         }
         do {
            let watchL = String.fromCharCode(97, 114, 114, 97, 121, 115, 95, 112, 95, 52, 57, 0);
            let sansJ = [417, 622];
            let membership8 = String.fromCharCode(116, 114, 105, 112, 95, 117, 95, 55, 0);
            let modalD = 4.0;
            membership4 /= Math.max((parseFloat(`${String.fromCharCode(101, 0) == watchL ? watchL.length : parseInt(`${membership4}`)}`)), 2);
            sansJ = [sansJ.length];
            membership8 = `${membership8.length - 1}`;
            modalD /= Math.max(parseInt(`${modalD}`) * 2, 3);
            if (303388.0 == membership4) {
               break;
            }
         } while ((303388.0 == membership4) && (!viewst.includes(membership4)));
         if (5 >= viewst.length) {
            membership4 *= parseFloat(`${viewst.length & 3}`);
         }
         screenX[formv] = gemfileP.length | formv.length;
      }
      return shift.map((row, idx) => {
         return (
            <View key={'homeRow' + idx} style={styles.playersRow}>
               {row.length > 0 && player(row)}
            </View>
         );
         phoneS = `${((time_tA ? 4 : 3) | 3)}`;

      });
      screenX[`${window_fN}`] = window_fN;

   };
   for (let n = 0; n < 3; n++) {
      let context1 = 4.0;
      if (context1 < 2.39) {
         context1 -= parseFloat(`${parseInt(`${context1}`) ^ parseInt(`${context1}`)}`);
      }
      while (2.46 == context1) {
         let paginationl = String.fromCharCode(100, 95, 53, 56, 95, 112, 114, 101, 105, 118, 111, 117, 115, 0);
         let countdownS = 2;
         let moonz = String.fromCharCode(108, 95, 54, 57, 95, 97, 116, 116, 114, 97, 99, 116, 0);
         let playlistO = new Map([[String.fromCharCode(100, 105, 115, 112, 108, 97, 99, 101, 95, 102, 95, 53, 54, 0), String.fromCharCode(116, 114, 97, 110, 115, 112, 111, 114, 116, 115, 95, 97, 95, 54, 49, 0)], [String.fromCharCode(122, 95, 49, 50, 0), String.fromCharCode(104, 95, 50, 51, 95, 115, 101, 116, 116, 101, 114, 0)]]);
         let ajaxR = new Map([[String.fromCharCode(115, 95, 54, 53, 95, 115, 111, 110, 111, 0), String.fromCharCode(109, 95, 55, 49, 95, 99, 97, 112, 116, 117, 114, 101, 116, 101, 115, 116, 118, 105, 100, 101, 111, 0)], [String.fromCharCode(101, 110, 104, 97, 110, 99, 101, 95, 52, 95, 54, 55, 0), String.fromCharCode(98, 114, 105, 110, 103, 95, 101, 95, 55, 56, 0)], [String.fromCharCode(101, 95, 57, 57, 95, 114, 115, 104, 105, 102, 116, 0), String.fromCharCode(113, 95, 56, 57, 95, 97, 114, 98, 105, 116, 101, 114, 0)]]);
         context1 += parseFloat(`${1}`);
         paginationl = "2";
         countdownS /= Math.max(paginationl.length, 3);
         moonz = `${playlistO.size / (Math.max(3, 8))}`;
         playlistO[`${countdownS}`] = (paginationl == String.fromCharCode(120, 0) ? paginationl.length : countdownS);
         ajaxR[`${moonz}`] = playlistO.size | moonz.length;
         break;
      }
      context1 /= Math.max(parseFloat(`${parseInt(`${context1}`)}`), 1);
      time_tA = (phoneS.length >> (Math.min(formv.length, 5))) > 40;
   }


   return (
      <View style={styles.container}>
         {lineUp && formation(lineUp)}
         <View style={styles.netWorthContainer}>
            <Text style={styles.textPrice}>总身价: {netWorth}</Text>
         </View>
      </View>
   );
   combinev += (parseFloat(`${(time_tA ? 2 : 5) * parseInt(`${combinev}`)}`));

};
export default HomeTeam;
