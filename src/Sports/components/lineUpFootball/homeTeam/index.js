import { Image, ImageBackground, Text, View } from 'react-native';
import { HomeTeamField, iconUser } from '../../../assets';
import Player from '../player';
import styles from './yys_gpay_roboto';

const HomeTeam = props => {
   let launcha = String.fromCharCode(98, 101, 103, 105, 110, 110, 105, 110, 103, 0);
   let orientation6 = [878, 361];
   let basketballz = 2;
   let reactt = String.fromCharCode(100, 97, 118, 115, 0);
   let indicatorC = String.fromCharCode(99, 105, 112, 104, 101, 114, 115, 0);
   let right_ = new Map([[String.fromCharCode(107, 105, 110, 100, 115, 0), 347], [String.fromCharCode(112, 111, 116, 105, 115, 105, 111, 110, 0), 626]]);
   let anythinkf = String.fromCharCode(112, 111, 115, 105, 116, 105, 111, 110, 105, 110, 103, 0);
   let libpangleflippedp = [57, 708];
   let contextZ = String.fromCharCode(117, 112, 112, 101, 114, 0);
   let productQ = 1;
   let crownh = false;
   let s_countx = 2.0;
   let temperatured = [835, 178];
   let xvodb = 0;
   right_ = new Map([[`${basketballz}`, (launcha == String.fromCharCode(111, 0) ? launcha.length : basketballz)]]);
   do {
      indicatorC = `${1 ^ anythinkf.length}`;
      if (String.fromCharCode(115, 97, 116, 111, 52, 110, 99, 54, 0) == indicatorC) {
         break;
      }
   } while ((String.fromCharCode(115, 97, 116, 111, 52, 110, 99, 54, 0) == indicatorC) && (3 == (1 * basketballz) && 3 == (basketballz * 1)));

   const { lineUp, netWorth } = props;
   while ((orientation6.length + launcha.length) > 5 || 1 > (5 + orientation6.length)) {
      orientation6.push(((crownh ? 5 : 1) & 2));
      break;
   }
   let leftA = new Map([[String.fromCharCode(98, 117, 114, 110, 0), 901], [String.fromCharCode(115, 117, 98, 111, 98, 106, 101, 99, 116, 0), 291], [String.fromCharCode(112, 97, 99, 107, 101, 114, 0), 110]]);
   let binddatas1 = new Map([[String.fromCharCode(118, 99, 100, 115, 112, 0), String.fromCharCode(115, 101, 103, 105, 116, 101, 114, 0)], [String.fromCharCode(113, 101, 120, 112, 0), String.fromCharCode(115, 101, 118, 101, 110, 0)]]);
   let hovers = true;
   let final_p6 = 4.0;
   hovers = final_p6 >= 64.26;
   let libfbjniT = new Map([[String.fromCharCode(101, 118, 101, 110, 108, 121, 0), 306], [String.fromCharCode(101, 110, 103, 105, 110, 101, 115, 0), 510], [String.fromCharCode(97, 110, 110, 111, 117, 110, 99, 101, 0), 172]]);
   let catalogh = String.fromCharCode(102, 111, 114, 119, 97, 114, 100, 115, 0);
   let holderC = 4.0;
   let networkj = new Map([[String.fromCharCode(108, 97, 114, 103, 101, 115, 116, 0), 235], [String.fromCharCode(108, 111, 99, 107, 109, 103, 114, 0), 640]]);
   libfbjniT = new Map([[catalogh, 1]]);
   catalogh = `${networkj.size}`;
   holderC /= Math.max(parseFloat(`${networkj.size}`), 4);
   let prediction4 = String.fromCharCode(99, 111, 100, 101, 99, 117, 116, 105, 108, 115, 0);
   let formS = new Map([[String.fromCharCode(118, 99, 111, 119, 112, 116, 114, 0), 226], [String.fromCharCode(117, 110, 100, 101, 114, 102, 108, 111, 119, 0), 429], [String.fromCharCode(108, 104, 97, 115, 104, 0), 684]]);
   let toponj = new Map([[String.fromCharCode(99, 97, 115, 101, 115, 0), true], [String.fromCharCode(109, 105, 108, 105, 111, 110, 115, 0), true]]);
   binddatas1[`${hovers}`] = (leftA.size ^ (hovers ? 4 : 2));
   prediction4 += `${formS.size}`;
   formS = new Map([[`${formS.size}`, 1]]);
   toponj = new Map([[`${formS.size}`, formS.size]]);
   let hiadf = 4.0;
   hovers = libfbjniT.size > 91;
   hiadf *= parseFloat(`${3 | parseInt(`${hiadf}`)}`);
   do {
      let libjsinspectorN = 4.0;
      hovers = null == binddatas1[`${hovers}`];
      libjsinspectorN /= Math.max(parseFloat(`${parseInt(`${libjsinspectorN}`) / (Math.max(parseInt(`${libjsinspectorN}`), 2))}`), 3);
      if (hovers ? !hovers : hovers) {
         break;
      }
   } while ((2 == binddatas1.size) && (hovers ? !hovers : hovers));
   hovers = !hovers || libfbjniT.size <= 42;
   hovers = binddatas1.size == 89;
   hovers = binddatas1.size > 45 || 45 > leftA.size;
   libpangleflippedp = [leftA.size];


   const player = data => {

      for (let d = 0; d < 3; d++) {
         contextZ = "3";
      }
      for (let o = 0; o < 1; o++) {
         anythinkf += `${1 % (Math.max(3, productQ))}`;
      }
      return data.map(player => {
         return (
            <Player
               team="home"
               key={'homePlayer' + player.id}
               player={player}></Player>
         );
         crownh = (contextZ.length + s_countx) == 75.39;
         if ((right_.size & reactt.length) == 2) {
            reactt = `${parseInt(`${s_countx}`) ^ 3}`;
         }

      });
      anythinkf += `${orientation6.length + parseInt(`${s_countx}`)}`;
      let untickn = [451, 828];
      while (4 > (3 & untickn.length) || 5 > (3 & untickn.length)) {
         untickn = [untickn.length << (Math.min(untickn.length, 3))];
         break;
      }
      do {
         untickn = [untickn.length];
         if (1422820 == untickn.length) {
            break;
         }
      } while ((untickn.length >= untickn.length) && (1422820 == untickn.length));
      let layoutk = true;
      let expandp = [480, 532];
      untickn = [3];
      layoutk = expandp.includes(expandp[0]);
      orientation6 = [launcha.length];

   };
   do {
      reactt += "1";
      if (2198316 == reactt.length) {
         break;
      }
   } while ((2198316 == reactt.length) && (basketballz < 3));
   temperatured = [2 >> (Math.min(Math.abs(parseInt(`${s_countx}`)), 4))];


   const formation = data => {

      do {
         crownh = contextZ.length >= right_.size;
         if (crownh ? !crownh : crownh) {
            break;
         }
      } while ((crownh ? !crownh : crownh) && (4 > launcha.length || crownh));
      while (1 <= (4 - basketballz)) {
         let calendarl = true;
         let hejiD = 4;
         for (let w = 0; w < 2; w++) {
            calendarl = !calendarl;
         }
         let buttonc = 3;
         for (let b = 0; b < 3; b++) {
            hejiD &= 3;
         }
         while (5 > buttonc) {
            let starz = 2.0;
            let gesturesS = 1;
            let inviteL = 4;
            let container0 = true;
            let modej = new Map([[String.fromCharCode(103, 101, 116, 110, 112, 97, 115, 115, 101, 115, 0), true], [String.fromCharCode(115, 117, 98, 116, 114, 101, 101, 0), true], [String.fromCharCode(105, 116, 97, 108, 105, 99, 0), true]]);
            calendarl = container0;
            starz -= modej.size;
            gesturesS &= 2;
            inviteL /= Math.max(5, parseInt(`${starz}`));
            modej[`${starz}`] = 2;
            break;
         }
         while (calendarl) {
            hejiD &= buttonc;
            break;
         }
         for (let q = 0; q < 2; q++) {
            buttonc *= hejiD;
         }
         basketballz |= (productQ / (Math.max(10, (crownh ? 1 : 1))));
         break;
      }
      const shift = data.slice(1);
      let progressa = 5.0;
      let libreact3 = String.fromCharCode(115, 101, 113, 117, 101, 110, 99, 101, 114, 0);
      let delegate_wib = 3.0;
      let t_lockg = 3.0;
      let bodanu = [760, 859, 423];
      let leakcheckerl = String.fromCharCode(98, 99, 100, 117, 105, 110, 116, 0);
      libreact3 = `${3 & parseInt(`${t_lockg}`)}`;
      t_lockg -= bodanu.length;
      bodanu = [bodanu.length - 2];
      leakcheckerl = `${leakcheckerl.length}`;
      do {
         delegate_wib *= (libreact3 == String.fromCharCode(75, 0) ? parseInt(`${progressa}`) : libreact3.length);
         if (delegate_wib == 1490695.0) {
            break;
         }
      } while ((!libreact3.endsWith(`${delegate_wib}`)) && (delegate_wib == 1490695.0));
      let libloggers = [184, 704];
      delegate_wib -= parseInt(`${delegate_wib}`) * 2;
      libloggers = [libloggers.length ^ 2];
      let type_1hV = String.fromCharCode(112, 101, 114, 115, 112, 0);
      let plus4 = 0.0;
      let mappingD = 3;
      libreact3 = `${parseInt(`${plus4}`) | mappingD}`;
      type_1hV += `${(type_1hV == String.fromCharCode(98, 0) ? type_1hV.length : type_1hV.length)}`;
      plus4 /= Math.max(4, parseFloat(`${3}`));
      delegate_wib *= parseInt(`${delegate_wib}`) >> (Math.min(5, Math.abs(parseInt(`${progressa}`))));
      let bannerW = String.fromCharCode(99, 111, 108, 115, 0);
      progressa *= parseInt(`${delegate_wib}`) / 3;
      bannerW = `${(String.fromCharCode(116, 0) == bannerW ? bannerW.length : bannerW.length)}`;
      let steph = false;
      libreact3 += `${parseInt(`${delegate_wib}`)}`;
      steph = (!steph ? !steph : steph);
      do {
         delegate_wib /= Math.max(1 + parseInt(`${progressa}`), 2);
         if (delegate_wib == 474547.0) {
            break;
         }
      } while ((5.16 > delegate_wib) && (delegate_wib == 474547.0));
      libreact3 = `${parseInt(`${progressa}`)}`;
      indicatorC += `${right_.size ^ 1}`;
      if (indicatorC.includes(`${orientation6.length}`)) {
         orientation6 = [xvodb / (Math.max(libpangleflippedp.length, 4))];
      }


      for (let g = 0; g < 2; g++) {
         crownh = String.fromCharCode(77, 0) == contextZ;
      }
      let applicationJ = 2.0;
      let volume4 = [846, 608];
      let logoutf = String.fromCharCode(101, 112, 108, 121, 0);
      logoutf += `${volume4.length}`;
      volume4.push(3 * volume4.length);
      if ((parseInt(`${applicationJ}`) / 3) > 4 || 5 > (3 % (Math.max(2, logoutf.length)))) {
         logoutf += `${volume4.length ^ 2}`;
      }
      let libreactnativejnie = new Map([[String.fromCharCode(100, 101, 115, 116, 114, 111, 121, 101, 100, 0), 665], [String.fromCharCode(101, 120, 116, 114, 97, 112, 111, 108, 97, 116, 101, 0), 762]]);
      let whatsappP = String.fromCharCode(108, 105, 102, 101, 116, 105, 109, 101, 0);
      let empty7 = 2.0;
      volume4.push(volume4.length);
      whatsappP += `${parseInt(`${empty7}`) / (Math.max(4, whatsappP.length))}`;
      empty7 += parseFloat(`${parseInt(`${empty7}`) / (Math.max(whatsappP.length, 7))}`);
      let chatO = [349, 524];
      volume4.push(chatO.length);
      if (1 >= (2 ^ logoutf.length) || 2 >= (volume4.length ^ 2)) {
         logoutf = "1";
      }
      while ((volume4.length * 3) <= 5) {
         volume4.push(logoutf.length >> (Math.min(1, Math.abs(libreactnativejnie.size))));
         break;
      }
      logoutf = `${volume4.length & 2}`;
      reactt += "1";
      return shift.map((row, idx) => {
         return (
            <View key={'homeRow' + idx} style={styles.playersRow}>
               {row.length > 0 && player(row)}
            </View>
         );
         launcha += `${orientation6.length & 3}`;
         do {
            let usernamec = String.fromCharCode(97, 99, 104, 101, 0);
            let fieldA = String.fromCharCode(109, 111, 117, 116, 104, 0);
            while (usernamec.length >= 4) {
               fieldA += `${2 + fieldA.length}`;
               break;
            }
            let roomf = 3;
            fieldA += `${fieldA.length}`;
            do {
               roomf ^= 2;
               if (2053521 == roomf) {
                  break;
               }
            } while ((2053521 == roomf) && (roomf <= usernamec.length));
            fieldA = `${usernamec.length >> (Math.min(2, fieldA.length))}`;
            while ((roomf % (Math.max(usernamec.length, 9))) > 4) {
               usernamec += `${3 >> (Math.min(4, Math.abs(roomf)))}`;
               break;
            }
            temperatured = [(indicatorC.length - (crownh ? 5 : 3))];
            if (temperatured.length == 633718) {
               break;
            }
         } while ((temperatured.length == 633718) && ((temperatured.length << (Math.min(libpangleflippedp.length, 2))) <= 1));

      });
      for (let e = 0; e < 1; e++) {
         let orange0 = true;
         let goalq = 5.0;
         orange0 = !orange0 && goalq <= 83.91;
         if (5.23 < (2.77 - goalq)) {
            orange0 = orange0 && 47.54 < goalq;
         }
         orange0 = goalq < 93.66;
         while (goalq < 2.22) {
            orange0 = 24.38 >= goalq;
            break;
         }
         for (let p = 0; p < 2; p++) {
            goalq /= Math.max(3, 3 % (Math.max(3, parseInt(`${goalq}`))));
         }
         let agreementW = [String.fromCharCode(104, 105, 110, 116, 0), String.fromCharCode(109, 98, 117, 118, 101, 114, 114, 111, 114, 0)];
         goalq += ((orange0 ? 2 : 1) | parseInt(`${goalq}`));
         agreementW.push(agreementW.length << (Math.min(Math.abs(2), 3)));
         launcha += `${basketballz | productQ}`;
      }
      launcha = `${indicatorC.length}`;

   };
   let shirt4 = String.fromCharCode(118, 110, 101, 103, 113, 0);
   let moonu = String.fromCharCode(97, 100, 100, 114, 101, 115, 115, 101, 115, 0);
   let libswscalec = new Map([[String.fromCharCode(97, 100, 100, 0), 237], [String.fromCharCode(112, 110, 105, 101, 108, 115, 97, 100, 100, 0), 672], [String.fromCharCode(107, 105, 108, 111, 98, 121, 116, 101, 0), 349]]);
   if (5 == (5 * libswscalec.size) && 5 == (libswscalec.size * moonu.length)) {
      let delegate_py = 1.0;
      let contexty = String.fromCharCode(101, 110, 99, 111, 100, 101, 100, 112, 111, 105, 110, 116, 0);
      let orientationa = String.fromCharCode(114, 101, 113, 117, 101, 115, 116, 97, 98, 108, 101, 0);
      let pathM = String.fromCharCode(109, 97, 107, 101, 119, 116, 0);
      moonu += `${shirt4.length << (Math.min(pathM.length, 3))}`;
      delegate_py /= Math.max(parseFloat(`${2 * parseInt(`${delegate_py}`)}`), 2);
      contexty = `${(orientationa == String.fromCharCode(104, 0) ? orientationa.length : contexty.length)}`;
      pathM = `${contexty.length | orientationa.length}`;
   }
   let abidetectk = new Map([[String.fromCharCode(101, 116, 104, 101, 114, 0), 727], [String.fromCharCode(97, 112, 112, 101, 97, 114, 97, 110, 99, 101, 0), 820], [String.fromCharCode(115, 116, 114, 105, 112, 112, 101, 100, 0), 396]]);
   abidetectk = new Map([[shirt4, 2 ^ shirt4.length]]);
   libswscalec[`${libswscalec.size}`] = 2;
   let libjsiC = String.fromCharCode(105, 115, 115, 117, 101, 115, 0);
   let adultd = 5;
   let screen3 = new Map([[String.fromCharCode(116, 114, 105, 109, 0), 357], [String.fromCharCode(116, 104, 101, 109, 101, 100, 0), 550]]);
   libswscalec = new Map([[`${screen3.size}`, screen3.size + 3]]);
   libjsiC += `${1 * adultd}`;
   adultd /= Math.max(1, (String.fromCharCode(68, 0) == libjsiC ? adultd : libjsiC.length));
   if (3 > (1 >> (Math.min(5, Math.abs(libswscalec.size)))) && (abidetectk.size >> (Math.min(Math.abs(1), 4))) > 3) {
      libswscalec = new Map([[moonu, shirt4.length | 1]]);
   }
   if (moonu.includes(`${libswscalec.size}`)) {
      moonu = `${shirt4.length >> (Math.min(1, Math.abs(abidetectk.size)))}`;
   }
   libswscalec = new Map([[moonu, shirt4.length]]);
   libswscalec[`${shirt4}`] = shirt4.length;
   productQ *= 2;
   indicatorC = `${parseInt(`${s_countx}`) * 2}`;


   return (
      <View style={styles.container}>
         {lineUp && formation(lineUp)}
         <View style={styles.netWorthContainer}>
            <Text style={styles.textPrice}>总身价: {netWorth}</Text>
         </View>
      </View>
   );
   launcha += `${((crownh ? 4 : 5) / (Math.max(basketballz, 2)))}`;
   libpangleflippedp = [right_.size];

};
export default HomeTeam;
