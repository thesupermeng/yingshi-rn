import {Image, ImageBackground, Text, View, Platform} from 'react-native';
import {
  Assist,
  Goal,
  iconUser,
  OwnGoal,
  PenaltyGoal,
  RedCard,
  SubIn,
  SubOut,
  YellowCard,
  YellowToRedCard,
  HomePlayer,
  AwayPlayer,
  PlayerShirt,
} from '../../../assets';
import {Colors} from '../../../global/yys_penaltymatchicon_iconarrowrightorange';
import {round} from '../../../utility/utils';
import styles from './yys_nbatrophy';

const Player = props => {
       let basketballplayerplaceholder4 = [String.fromCharCode(100,105,115,116,114,105,98,117,116,105,111,110,95,120,95,56,0), String.fromCharCode(100,95,49,53,95,114,101,99,111,118,101,114,121,0), String.fromCharCode(104,95,50,49,95,100,114,111,112,111,102,102,0)];
    let filey = 5;
    let imagenomoredatac = false;
    let debugk = true;
    let middlesound2 = 1.0;
    let robotog = new Map([[String.fromCharCode(111,95,51,56,95,115,111,102,114,101,101,0),603], [String.fromCharCode(112,102,102,102,116,95,112,95,57,57,0),388], [String.fromCharCode(101,95,57,49,95,97,108,112,104,97,110,117,109,101,114,105,99,115,0),798]]);
    let user0 = false;
    let championX = String.fromCharCode(103,95,50,55,95,114,101,99,111,109,112,111,115,101,0);
    let predictionbannerA = 2.0;
    let predictionbuttonF = 1.0;
    let analyticsY = true;
   do {
       let yellow3 = String.fromCharCode(114,111,117,110,100,101,100,95,110,95,56,49,0);
         yellow3 = `${2 * yellow3.length}`;
         yellow3 += `${yellow3.length}`;
         yellow3 = "2";
      filey <<= Math.min(championX.length, 1);
      if (filey == 226709) {
         break;
      }
   } while ((4 > (2 - robotog.size)) && (filey == 226709));

  const {player, team} = props;
      championX = "1";

  const rateColor = rate => {

       let buttonH = String.fromCharCode(100,95,50,55,95,104,109,97,99,0);
         buttonH += `${buttonH.length}`;
      if (3 >= buttonH.length) {
         buttonH += `${buttonH.length}`;
      }
         buttonH += `${buttonH.length - buttonH.length}`;
      championX += `${((imagenomoredatac ? 5 : 4) << (Math.min(Math.abs(2), 5)))}`;
    return rate >= 8.1
      ? {backgroundColor: '#FD9F28'}
      : rate >= 7.1
      ? {backgroundColor: '#FFBA63'}
      : rate >= 6.1
      ? {backgroundColor: '#027B3C'}
      : {backgroundColor: '#8C8C8C'};
  };
      debugk = robotog.size >= 17 || !user0;

  const {incidents} = player;
   do {
       let sharemodalU = 1.0;
      do {
          let mappingf = 3;
          let bing4 = new Map([[String.fromCharCode(105,95,57,57,95,101,120,112,97,110,100,0),false ], [String.fromCharCode(109,95,50,50,95,103,114,105,100,0),true ]]);
          let emptyG = false;
         sharemodalU -= parseFloat(`${parseInt(`${sharemodalU}`) / (Math.max(2, mappingf))}`);
         mappingf *= (bing4.size & (emptyG ? 5 : 2));
         bing4[`${emptyG}`] = ((emptyG ? 1 : 4) % (Math.max(1, bing4.size)));
         if (3571957.0 == sharemodalU) {
            break;
         }
      } while ((3571957.0 == sharemodalU) && (sharemodalU <= 5.6));
      do {
          let smallbrightnessZ = 5.0;
          let giftbuttonD = 0.0;
          let mutedM = String.fromCharCode(116,95,53,52,95,119,101,108,115,101,110,99,100,101,109,111,0);
          let thumbnailI = String.fromCharCode(103,95,57,52,95,101,110,117,109,101,114,97,116,101,100,0);
         sharemodalU /= Math.max(parseFloat(`${parseInt(`${sharemodalU}`) / 3}`), 2);
         smallbrightnessZ -= thumbnailI.length;
         giftbuttonD += mutedM.length / 3;
         mutedM = `${thumbnailI.length >> (Math.min(Math.abs(2), 3))}`;
         if (830481.0 == sharemodalU) {
            break;
         }
      } while ((830481.0 == sharemodalU) && (4.9 >= (sharemodalU + sharemodalU) || (sharemodalU + 4.9) >= 1.61));
      for (let o = 0; o < 1; o++) {
          let rootn = 4.0;
         sharemodalU += parseFloat(`${3}`);
         rootn += parseInt(`${rootn}`);
      }
      robotog = new Map([[`${basketballplayerplaceholder4.length}`, (basketballplayerplaceholder4.length - (imagenomoredatac ? 5 : 2))]]);
      if (robotog.size == 194734) {
         break;
      }
   } while ((robotog.size == 194734) && (imagenomoredatac));

  const imgSource =
    Platform.OS === 'android'
      ? team === 'home'
        ? PlayerShirt
        : AwayPlayer
      : team === 'home'
      ? HomePlayer
      : AwayPlayer;

  

      filey |= 2 % (Math.max(2, filey));
  const subComponent = (incidents, playerId) => {

   do {
       let penaltyshoot9 = 1.0;
       let mime7 = true;
      while (4.12 == penaltyshoot9) {
         mime7 = !mime7;
         break;
      }
         mime7 = !mime7;
          let activeK = [744, 23, 508];
          let iconarrowrightblackh = String.fromCharCode(110,95,54,57,95,108,117,116,121,117,118,0);
          let popupt = 3.0;
         mime7 = activeK.length < 53;
         activeK = [iconarrowrightblackh.length - 1];
         iconarrowrightblackh += `${2 << (Math.min(3, iconarrowrightblackh.length))}`;
         popupt /= Math.max(1, parseFloat(`${iconarrowrightblackh.length}`));
      while (4.2 >= (penaltyshoot9 + 1.93)) {
          let smallsoundJ = String.fromCharCode(115,95,54,95,114,101,115,97,109,112,108,101,0);
          let penaltyshootu = 5.0;
          let typingloadingx = 4;
          let shirta = 0;
         penaltyshoot9 *= typingloadingx % (Math.max(1, 6));
         smallsoundJ += `${3 & smallsoundJ.length}`;
         penaltyshootu /= Math.max(smallsoundJ.length - parseInt(`${penaltyshootu}`), 1);
         typingloadingx >>= Math.min(Math.abs(2), 4);
         shirta /= Math.max(2, 2);
         break;
      }
      do {
         penaltyshoot9 /= Math.max(3, (parseInt(`${penaltyshoot9}`) - (mime7 ? 2 : 3)));
         if (penaltyshoot9 == 753569.0) {
            break;
         }
      } while ((mime7) && (penaltyshoot9 == 753569.0));
          let yellowtoredL = new Map([[String.fromCharCode(98,95,55,95,100,117,112,108,105,99,97,116,101,115,0),499], [String.fromCharCode(99,111,110,99,101,97,108,95,117,95,49,50,0),881], [String.fromCharCode(108,95,56,50,95,97,115,115,101,115,115,109,101,110,116,0),965]]);
          let themeH = [String.fromCharCode(119,95,51,50,95,97,100,115,103,97,115,0), String.fromCharCode(109,95,57,54,95,105,110,116,101,114,112,114,101,116,0)];
         mime7 = mime7 || 72.14 >= penaltyshoot9;
         yellowtoredL = new Map([[`${yellowtoredL.size}`, yellowtoredL.size % (Math.max(themeH.length, 1))]]);
         themeH = [yellowtoredL.size];
      filey <<= Math.min(parseInt(`${Math.abs(((debugk ? 3 : 2) % (Math.max(parseInt(`${middlesound2}`), 9))))}`), 2);
      if (filey == 1202447) {
         break;
      }
   } while ((3 > (1 * filey) && (1 / (Math.max(2, filey))) > 5) && (filey == 1202447));
    return incidents.map(incident => {
      if (incident.type === 9) {

      championX = `${(parseInt(`${middlesound2}`) + (imagenomoredatac ? 3 : 4))}`;
        return (
          <View key={'subsitution' + playerId} style={styles.subContainer}>
            <Image
              style={styles.imageIncident}
              source={incident.in_player.id === playerId ? SubIn : SubOut}
              resizeMode="stretch"></Image>
          </View>
        );
      debugk = user0;

      }
    });
   if (!debugk) {
      imagenomoredatac = robotog.size <= 1;
   }

  };
   if (2 == basketballplayerplaceholder4.length) {
      user0 = robotog.size > 85;
   }


  

   if (!user0 || (5 + robotog.size) <= 3) {
      user0 = basketballplayerplaceholder4.length > 73;
   }
  const cardComponent = incidents => {

   do {
       let googlea = String.fromCharCode(119,114,105,116,101,114,95,97,95,50,0);
      if (googlea.includes(googlea)) {
         googlea += "1";
      }
         googlea += `${(String.fromCharCode(95,0) == googlea ? googlea.length : googlea.length)}`;
          let a_hashq = false;
         googlea = `${(String.fromCharCode(104,0) == googlea ? googlea.length : (a_hashq ? 1 : 5))}`;
      robotog = new Map([[`${middlesound2}`, 3 >> (Math.min(Math.abs(parseInt(`${middlesound2}`)), 5))]]);
      if (758759 == robotog.size) {
         break;
      }
   } while ((championX.endsWith(`${robotog.size}`)) && (758759 == robotog.size));
    let cardType;
       let shootZ = new Map([[String.fromCharCode(115,119,102,104,97,115,104,95,105,95,54,51,0),String.fromCharCode(100,95,57,51,95,105,110,116,101,114,108,97,99,101,0)], [String.fromCharCode(99,104,97,110,103,101,115,101,116,95,52,95,50,52,0),String.fromCharCode(106,95,53,51,95,109,111,118,101,0)]]);
       let baidu6 = String.fromCharCode(119,95,49,95,109,101,116,97,108,0);
         baidu6 = `${baidu6.length << (Math.min(Math.abs(2), 3))}`;
      if ((baidu6.length * shootZ.size) == 5 || 5 == (shootZ.size * baidu6.length)) {
         shootZ[`${baidu6}`] = (String.fromCharCode(55,0) == baidu6 ? shootZ.size : baidu6.length);
      }
         shootZ[`${baidu6}`] = baidu6.length;
       let fieldu = [849, 523];
         baidu6 = `${baidu6.length}`;
      while ((baidu6.length / 1) <= 5 || (fieldu.length / 1) <= 4) {
          let leaguedetailsbgW = 0.0;
          let currentx = true;
          let empty0 = 1;
         baidu6 += "2 * empty0";
         leaguedetailsbgW /= Math.max(parseInt(`${leaguedetailsbgW}`) ^ 1, 1);
         currentx = !currentx;
         empty0 *= 3 * parseInt(`${leaguedetailsbgW}`);
         break;
      }
      championX = `${parseInt(`${middlesound2}`) >> (Math.min(Math.abs(3), 1))}`;


    incidents.map(incident => {

   while ((parseInt(`${predictionbannerA}`) + championX.length) >= 1 || (parseInt(`${predictionbannerA}`) + championX.length) >= 1) {
      championX = `${filey}`;
      break;
   }
      if (incident.type === 3 || incident.type === 4 || incident.type === 15) {

       let dropdownR = String.fromCharCode(118,105,100,101,111,104,100,114,95,103,95,53,49,0);
       let singleo = false;
       let embed3 = String.fromCharCode(97,100,116,115,116,111,97,115,99,95,53,95,57,54,0);
         singleo = dropdownR.length <= 16 || !singleo;
       let predictionlossz = String.fromCharCode(112,95,56,52,95,120,118,97,103,0);
       let lineN = String.fromCharCode(116,101,109,112,108,97,116,101,95,107,95,49,51,0);
         embed3 += `${dropdownR.length * 2}`;
       let vipsportK = String.fromCharCode(103,95,52,48,95,100,105,121,102,112,0);
       let libyoga7 = true;
       let bangT = false;
       let airbnbstarselectedQ = new Map([[String.fromCharCode(102,95,55,50,95,100,105,114,97,99,116,97,98,0),553], [String.fromCharCode(107,95,52,57,95,115,116,105,99,107,101,114,0),620], [String.fromCharCode(119,95,52,56,95,115,117,98,111,98,106,101,99,116,0),794]]);
       let baselineA = new Map([[String.fromCharCode(120,95,52,54,95,118,101,114,105,102,105,97,98,108,101,0),true ], [String.fromCharCode(103,95,49,57,95,108,97,121,101,114,0),true ]]);
         bangT = !predictionlossz.endsWith(`${singleo}`);
      do {
          let orangeclockB = 1.0;
          let settingsZ = String.fromCharCode(118,115,102,114,97,109,101,95,55,95,53,49,0);
         embed3 += `${baselineA.size - settingsZ.length}`;
         orangeclockB += parseInt(`${orangeclockB}`) | 2;
         settingsZ += `${parseInt(`${orangeclockB}`)}`;
         if (embed3.length == 3792813) {
            break;
         }
      } while ((embed3.length == 3792813) && ((airbnbstarselectedQ.size >> (Math.min(Math.abs(5), 4))) <= 4));
         singleo = null == airbnbstarselectedQ[`${bangT}`];
      predictionbannerA *= 1;
        cardType =
          incident.type === 3
            ? YellowCard
            : incident.type === 4
            ? RedCard
            : YellowToRedCard;
      }
    });
   while ((championX.length * parseInt(`${predictionbuttonF}`)) < 5 || 1 < (championX.length * 5)) {
      championX += `${2 >> (Math.min(Math.abs(parseInt(`${predictionbannerA}`)), 1))}`;
      break;
   }

    if (cardType) {

      filey -= parseInt(`${predictionbuttonF}`) << (Math.min(Math.abs(parseInt(`${predictionbannerA}`)), 4));
      return (
        <View style={styles.cardContainer}>
          <Image
            style={styles.imageIncident}
            source={cardType}
            resizeMode="stretch"></Image>
        </View>
      );
   do {
      middlesound2 *= parseFloat(`${1 - parseInt(`${predictionbuttonF}`)}`);
      if (middlesound2 == 2198585.0) {
         break;
      }
   } while ((middlesound2 == 2198585.0) && (1.98 > (1.65 * middlesound2) && 2 > (robotog.size / (Math.max(4, 7)))));

    }
  };
       let read7 = 0;
       let iconpointscore6 = String.fromCharCode(112,95,56,50,95,98,97,110,100,0);
       let iconeditY = String.fromCharCode(102,95,51,53,95,105,110,118,105,116,97,116,105,111,110,115,0);
         iconeditY += `${(String.fromCharCode(73,0) == iconeditY ? iconeditY.length : iconpointscore6.length)}`;
         iconpointscore6 += `${iconeditY.length + 1}`;
          let iconusery = [362, 715];
         read7 <<= Math.min(3, iconpointscore6.length);
         iconusery.push(2);
         iconeditY = `${read7 << (Math.min(iconpointscore6.length, 3))}`;
      while ((iconeditY.length * read7) > 2 || 3 > (read7 * 2)) {
         read7 %= Math.max(iconeditY.length, 2);
         break;
      }
      do {
         iconeditY = `${read7 ^ iconeditY.length}`;
         if (iconeditY.length == 2384663) {
            break;
         }
      } while ((read7 == 2) && (iconeditY.length == 2384663));
      while (iconeditY.endsWith(iconpointscore6)) {
         iconeditY = `${iconeditY.length & read7}`;
         break;
      }
          let anythink6 = 5.0;
         iconeditY = `${iconeditY.length ^ read7}`;
         anythink6 -= parseFloat(`${parseInt(`${anythink6}`) << (Math.min(3, Math.abs(3)))}`);
         iconpointscore6 += `${iconeditY.length}`;
      basketballplayerplaceholder4.push(2 + parseInt(`${predictionbuttonF}`));


  

   while (imagenomoredatac && 1 > (basketballplayerplaceholder4.length % 5)) {
      imagenomoredatac = !imagenomoredatac || middlesound2 <= 90.7;
      break;
   }
  const goalComponent = (incidents, type, playerId) => {

   do {
      user0 = predictionbuttonF > 15.40;
      if (user0 ? !user0 : user0) {
         break;
      }
   } while ((user0 ? !user0 : user0) && (!user0));
    let totalOwnGoal = incidents.filter(incident => {

   while (championX.endsWith(`${predictionbannerA}`)) {
      predictionbannerA -= filey;
      break;
   }
      if (incident.type === 17) return true;
       let feedbackS = [973, 812];
       let notificationS = 3.0;
         notificationS -= parseFloat(`${parseInt(`${notificationS}`) >> (Math.min(feedbackS.length, 3))}`);
         notificationS *= parseFloat(`${feedbackS.length ^ 2}`);
          let dropdownq = 0.0;
          let iconviewerd = 0.0;
         feedbackS = [2];
         dropdownq -= parseInt(`${iconviewerd}`) & parseInt(`${dropdownq}`);
         iconviewerd -= parseFloat(`${parseInt(`${iconviewerd}`) - 2}`);
          let commentq = [745, 169];
          let nendI = new Map([[String.fromCharCode(106,95,52,55,95,99,114,108,115,0),206], [String.fromCharCode(115,116,114,108,105,107,101,95,116,95,51,50,0),751]]);
         feedbackS.push(1);
         commentq = [commentq.length ^ nendI.size];
         nendI = new Map([[`${nendI.size}`, nendI.size]]);
      while (5 <= (5 + feedbackS.length) || (feedbackS.length ^ 5) <= 5) {
         feedbackS.push(2);
         break;
      }
         feedbackS = [parseInt(`${notificationS}`) ^ feedbackS.length];
      imagenomoredatac = (!imagenomoredatac ? !debugk : imagenomoredatac);

    }).length;
   if (5 >= (filey << (Math.min(Math.abs(4), 4)))) {
       let executorZ = String.fromCharCode(111,95,57,56,95,115,117,98,112,97,99,107,101,116,0);
       let iconwatchnowy = true;
       let iconadslinkl = String.fromCharCode(100,105,102,95,100,95,57,53,0);
      do {
          let build8 = String.fromCharCode(114,95,54,49,95,104,105,103,104,108,105,103,104,116,101,100,0);
          let orangedownarrowH = false;
          let libfabricjniY = 2.0;
         iconwatchnowy = (executorZ.length + iconadslinkl.length) < 97;
         build8 = `${((orangedownarrowH ? 3 : 2) % (Math.max(parseInt(`${libfabricjniY}`), 4)))}`;
         orangedownarrowH = build8.startsWith(`${orangedownarrowH}`);
         libfabricjniY /= Math.max(parseInt(`${libfabricjniY}`) ^ 1, 3);
         if (iconwatchnowy ? !iconwatchnowy : iconwatchnowy) {
            break;
         }
      } while ((!iconwatchnowy || 5 <= iconadslinkl.length) && (iconwatchnowy ? !iconwatchnowy : iconwatchnowy));
         iconwatchnowy = executorZ.includes(`${iconwatchnowy}`);
          let awayY = true;
          let phoneQ = [872, 214];
          let basketballdetailsbgC = 2;
         iconwatchnowy = iconadslinkl.length == 48 || awayY;
         awayY = 62 > phoneQ.length || basketballdetailsbgC > 62;
         phoneQ.push(basketballdetailsbgC << (Math.min(phoneQ.length, 5)));
          let redirectT = 0.0;
          let vignetteP = new Map([[String.fromCharCode(97,110,115,105,95,106,95,49,51,0),235], [String.fromCharCode(97,95,50,54,95,119,104,105,116,101,115,112,97,99,101,0),587], [String.fromCharCode(113,105,110,100,101,120,95,100,95,51,53,0),196]]);
          let linev = 1.0;
         iconwatchnowy = !iconadslinkl.startsWith(`${redirectT}`);
         redirectT *= 3;
         vignetteP = new Map([[`${vignetteP.size}`, vignetteP.size]]);
         linev /= Math.max(3, parseInt(`${linev}`) + vignetteP.size);
         iconwatchnowy = executorZ.length <= 87;
      do {
          let overP = new Map([[String.fromCharCode(107,101,121,112,97,116,104,95,117,95,49,48,48,0),String.fromCharCode(105,110,118,105,116,101,114,95,103,95,56,50,0)], [String.fromCharCode(120,95,53,51,95,101,120,116,101,110,115,105,111,110,115,0),String.fromCharCode(115,99,97,110,116,97,98,108,101,115,95,116,95,56,55,0)]]);
         iconwatchnowy = (iconadslinkl.length * executorZ.length) < 97;
         overP[`${overP.size}`] = overP.size;
         if (iconwatchnowy ? !iconwatchnowy : iconwatchnowy) {
            break;
         }
      } while ((2 < executorZ.length) && (iconwatchnowy ? !iconwatchnowy : iconwatchnowy));
       let closea = false;
      for (let w = 0; w < 1; w++) {
         closea = !iconwatchnowy;
      }
      if (iconwatchnowy && !closea) {
          let wind7 = 0.0;
          let shootyesgoalM = 1;
          let libfolly8 = String.fromCharCode(97,114,103,117,109,101,110,116,115,95,52,95,49,55,0);
         iconwatchnowy = 77 <= libfolly8.length && executorZ.length <= 77;
         wind7 *= 3 - parseInt(`${wind7}`);
         shootyesgoalM /= Math.max(parseInt(`${wind7}`) % (Math.max(4, shootyesgoalM)), 2);
         libfolly8 += `${3 % (Math.max(shootyesgoalM, 10))}`;
      }
      basketballplayerplaceholder4 = [iconadslinkl.length & championX.length];
   }

    let totalPenatlyGoal = incidents.filter(incident => {

      basketballplayerplaceholder4.push(basketballplayerplaceholder4.length * robotog.size);
      if (incident.type === 8) return true;
       let large6 = false;
       let send9 = true;
       let mintegralo = String.fromCharCode(97,95,53,49,95,99,97,108,108,105,110,103,0);
         large6 = send9;
         large6 = (send9 ? !large6 : send9);
      for (let t = 0; t < 3; t++) {
         large6 = !send9;
      }
          let kuaishouy = String.fromCharCode(115,95,50,57,95,99,116,108,111,117,116,112,117,116,0);
          let sharedI = [81, 754];
          let reacto = true;
         send9 = large6 || kuaishouy.length <= 3;
         kuaishouy = `${sharedI.length}`;
         sharedI.push(1);
       let tickedC = [684, 709, 119];
         send9 = !send9;
          let internetD = [177, 440];
          let read0 = 2;
         large6 = !send9;
         internetD.push(read0 % (Math.max(internetD.length, 8)));
         read0 <<= Math.min(2, Math.abs(read0));
         tickedC.push(1);
         large6 = mintegralo.length < 50;
      predictionbuttonF /= Math.max(1 | filey, 2);

    }).length;
       let notificationfillemptyt = false;
       let arrowupl = String.fromCharCode(117,110,112,111,105,115,111,110,95,109,95,52,50,0);
      while (arrowupl.startsWith(`${notificationfillemptyt}`)) {
         arrowupl = `${3 >> (Math.min(3, arrowupl.length))}`;
         break;
      }
      for (let f = 0; f < 3; f++) {
          let gifgoalx = String.fromCharCode(97,110,103,108,101,115,95,50,95,54,49,0);
          let iconbellactiveU = 1.0;
          let sharedi = 4.0;
          let libavdevicew = new Map([[String.fromCharCode(113,95,54,48,95,115,116,111,114,121,98,111,97,114,100,0),false ], [String.fromCharCode(104,105,103,104,101,114,95,54,95,55,48,0),true ]]);
         arrowupl += `${parseInt(`${sharedi}`)}`;
         gifgoalx += `${2 >> (Math.min(4, Math.abs(parseInt(`${iconbellactiveU}`))))}`;
         iconbellactiveU -= parseFloat(`${libavdevicew.size << (Math.min(Math.abs(1), 2))}`);
         sharedi -= parseFloat(`${parseInt(`${iconbellactiveU}`) * 3}`);
         libavdevicew[`${gifgoalx}`] = gifgoalx.length;
      }
         arrowupl = `${((notificationfillemptyt ? 4 : 4))}`;
      do {
          let libruntimeexecutorB = 2.0;
          let traminiV = false;
         arrowupl = "3";
         libruntimeexecutorB -= 1 * parseInt(`${libruntimeexecutorB}`);
         traminiV = libruntimeexecutorB < 47.46;
         if (arrowupl == String.fromCharCode(110,99,106,51,56,49,50,57,121,0)) {
            break;
         }
      } while ((notificationfillemptyt) && (arrowupl == String.fromCharCode(110,99,106,51,56,49,50,57,121,0)));
      do {
         notificationfillemptyt = (((!notificationfillemptyt ? 78 : arrowupl.length) & arrowupl.length) == 78);
         if (notificationfillemptyt ? !notificationfillemptyt : notificationfillemptyt) {
            break;
         }
      } while ((notificationfillemptyt ? !notificationfillemptyt : notificationfillemptyt) && (!notificationfillemptyt));
         arrowupl += `${arrowupl.length}`;
      basketballplayerplaceholder4 = [(1 << (Math.min(1, Math.abs((analyticsY ? 2 : 4)))))];


    let totalGoal;
      basketballplayerplaceholder4.push(((imagenomoredatac ? 4 : 1) % (Math.max(3, (debugk ? 1 : 5)))));

    try {

   while (robotog.size < 3 && 5 < (3 - robotog.size)) {
       let listZ = 0.0;
       let castinge = false;
      if (!castinge) {
          let flippery = 3.0;
          let miniy = 0;
         castinge = miniy < 67;
         flippery *= parseFloat(`${3}`);
         miniy += 2;
      }
          let emojiD = String.fromCharCode(97,95,57,53,95,115,111,109,101,116,104,105,110,103,0);
          let file4 = 1.0;
          let expandH = String.fromCharCode(110,95,49,55,95,101,110,99,114,121,112,116,105,110,103,0);
         listZ /= Math.max(2, parseInt(`${file4}`) / 3);
         emojiD = `${expandH.length >> (Math.min(3, emojiD.length))}`;
         file4 -= parseFloat(`${expandH.length << (Math.min(5, emojiD.length))}`);
      do {
         castinge = !castinge;
         if (castinge ? !castinge : castinge) {
            break;
         }
      } while ((castinge ? !castinge : castinge) && ((listZ / (Math.max(5.86, 1))) >= 2.77));
      if (5.51 == listZ) {
         listZ -= parseInt(`${listZ}`) / 3;
      }
      if (castinge) {
          let configuree = [748, 526, 715];
          let stationsL = new Map([[String.fromCharCode(114,101,109,117,120,95,48,95,52,0),873], [String.fromCharCode(107,95,49,55,95,116,105,108,101,100,0),600]]);
          let iconwechatT = 3;
          let stringsL = 4.0;
          let teamdetailsbgt = 4.0;
         castinge = 36 == iconwechatT || castinge;
         configuree = [configuree.length];
         stationsL = new Map([[`${stationsL.size}`, configuree.length]]);
         iconwechatT /= Math.max(5, 3);
         stringsL -= configuree.length;
         teamdetailsbgt *= parseFloat(`${1 << (Math.min(Math.abs(parseInt(`${stringsL}`)), 2))}`);
      }
       let greytickg = [553, 754];
       let modalY = [118, 625, 25];
      robotog = new Map([[`${basketballplayerplaceholder4.length}`, basketballplayerplaceholder4.length & 2]]);
      break;
   }
      totalGoal = incidents.filter(incident => {

   while (user0) {
      robotog = new Map([[`${middlesound2}`, 1 / (Math.max(parseInt(`${middlesound2}`), 4))]]);
      break;
   }
        if (incident.type === 1 && incident.player.id === playerId) return true;
   if (1 < (filey - 2)) {
       let libreact2 = 3.0;
       let yellowanimationliveZ = String.fromCharCode(113,95,53,49,95,118,101,114,98,97,116,105,109,0);
       let k_managerk = String.fromCharCode(117,95,55,57,95,97,117,116,111,99,111,109,109,105,116,0);
       let wifirouterO = [467, 17, 922];
      while (!wifirouterO.includes(libreact2)) {
         libreact2 -= parseFloat(`${k_managerk.length ^ 2}`);
         break;
      }
         libreact2 -= parseFloat(`${3 * wifirouterO.length}`);
         k_managerk = `${(String.fromCharCode(80,0) == yellowanimationliveZ ? k_managerk.length : yellowanimationliveZ.length)}`;
      while (wifirouterO.length < 1) {
          let rocketc = String.fromCharCode(119,95,54,53,95,103,104,97,115,104,0);
         wifirouterO = [3];
         rocketc = `${(rocketc == String.fromCharCode(86,0) ? rocketc.length : rocketc.length)}`;
         break;
      }
      for (let z = 0; z < 3; z++) {
         yellowanimationliveZ += `${parseInt(`${libreact2}`) | 1}`;
      }
         k_managerk += "1";
      for (let x = 0; x < 1; x++) {
         k_managerk = `${(k_managerk == String.fromCharCode(56,0) ? k_managerk.length : parseInt(`${libreact2}`))}`;
      }
      for (let q = 0; q < 1; q++) {
          let singleu = 0.0;
          let filln = 5.0;
          let gifgoalbgt = new Map([[String.fromCharCode(121,95,56,49,95,112,117,98,108,105,115,104,101,100,0),883], [String.fromCharCode(112,95,49,95,99,116,105,109,101,0),999]]);
         libreact2 -= parseFloat(`${1}`);
         singleu *= parseFloat(`${parseInt(`${filln}`)}`);
         filln /= Math.max(parseInt(`${singleu}`), 4);
         gifgoalbgt = new Map([[`${singleu}`, parseInt(`${filln}`)]]);
      }
      do {
         yellowanimationliveZ += `${parseInt(`${libreact2}`)}`;
         if (String.fromCharCode(56,106,112,51,118,0) == yellowanimationliveZ) {
            break;
         }
      } while (((libreact2 / (Math.max(3.11, 4))) < 4.99 && 2.52 < (3.11 / (Math.max(6, libreact2)))) && (String.fromCharCode(56,106,112,51,118,0) == yellowanimationliveZ));
          let ewardedk = 5;
          let tumbnaili = String.fromCharCode(101,95,52,55,95,103,114,97,116,105,99,117,108,101,0);
         k_managerk = `${ewardedk ^ 2}`;
         ewardedk += tumbnaili.length;
         tumbnaili += `${1 * tumbnaili.length}`;
       let animations1 = String.fromCharCode(112,95,51,50,95,101,110,117,109,101,114,97,116,111,114,0);
       let yellowscoreball2 = String.fromCharCode(113,95,49,50,95,97,118,112,114,111,103,114,97,109,0);
         animations1 += `${yellowanimationliveZ.length}`;
      filey += robotog.size / 2;
   }

      }).length;
   for (let n = 0; n < 1; n++) {
      basketballplayerplaceholder4 = [((debugk ? 3 : 4) & robotog.size)];
   }

    } catch (error) {

   while (1.70 == predictionbuttonF) {
      predictionbuttonF += robotog.size;
      break;
   }
      totalGoal = 0;
      middlesound2 -= parseFloat(`${robotog.size % (Math.max(2, 9))}`);

    }

    incidents.filter(incident => {
      if (incident.type === 1 && incident.player.id === playerId) return true;
    }).length;

    
    if (totalOwnGoal > 0 && type === 17) {
      return (
        <View style={styles.ownGoalContainer}>
          <Image
            style={styles.imageIncident}
            source={OwnGoal}
            resizeMode="stretch"></Image>
          {totalOwnGoal > 1 && (
            <View style={styles.assistCountContainer}>
              <Text style={styles.textNumberOfCount}>{totalOwnGoal}</Text>
            </View>
          )}
        </View>
      );
    }
    
    if (totalPenatlyGoal > 0 && type === 8) {
      return (
        <View style={styles.ballContainer}>
          <Image
            style={styles.imageIncident}
            source={PenaltyGoal}
            resizeMode="stretch"></Image>
          {totalPenatlyGoal > 1 && (
            <View style={styles.ballCountContainer}>
              <Text style={styles.textNumberOfCount}>{totalPenatlyGoal}</Text>
            </View>
          )}
        </View>
      );
    }
    
    if (totalGoal > 0 && type === 1) {
      return (
        <View style={styles.ballContainer}>
          <Image
            style={styles.imageIncident}
            source={Goal}
            resizeMode="stretch"></Image>
          {totalGoal > 1 && (
            <View style={styles.ballCountContainer}>
              <Text style={styles.textNumberOfCount}>{totalGoal}</Text>
            </View>
          )}
        </View>
      );
    }
  };

  
  const assistComponent = (incidents, playerId) => {
    let totalAssist = incidents.filter(incident => {
      if (
        incident?.type === 1 &&
        (incident?.assist1?.id === playerId ||
          incident?.assist2?.id === playerId)
      )
        return true;
    }).length;
    if (totalAssist > 0) {
      return (
        <View style={styles.assistContainer}>
          <Image
            style={styles.imageIncident}
            source={Assist}
            resizeMode="stretch"></Image>
          {totalAssist > 1 && (
            <View style={styles.assistCountContainer}>
              <Text style={styles.textNumberOfCount}>{totalAssist}</Text>
            </View>
          )}
        </View>
      );
    }
  };

  const topLeft = () => {
    return (
      <View style={styles.topLeftContainer}>
        {incidents && assistComponent(incidents, player?.id)}
      </View>
    );
  };

  const topRight = () => {
    return (
      <View style={styles.topRightContainer}>
        {incidents && subComponent(incidents, player?.id)}
      </View>
    );
  };

  const bottomLeft = () => {
    return (
      <View style={styles.bottomLeftContainer}>
        <View style={styles.bottomLeftFlex}>
          {incidents && goalComponent(incidents, 17)}
          {incidents && cardComponent(incidents)}
        </View>
      </View>
    );
  };

  const bottomRight = () => {
    return (
      <View style={styles.bottomRightContainer}>
        <View style={styles.bottomRightFlex}>
          {incidents && goalComponent(incidents, 1, player.id)}
          {incidents && goalComponent(incidents, 8)}
        </View>
      </View>
    );
  };

  return (
    <View key={team + player.id} style={styles.player}>
      <View style={styles.imageContainer}>
        {player?.logo ? (
          <Image
            style={styles.imagePlayer}
            source={{
              uri: player?.logo
                ? player?.logo
                : team === 'home'
                ? HomePlayer
                : AwayPlayer,
            }}></Image>
        ) : (
          <ImageBackground
            style={[
              styles.imagePlayer,
              {justifyContent: 'center', alignContent: 'center'},
            ]}
            
            source={imgSource}
            resizeMode="center">
            <Text
              style={{
                textAlign: 'center',
                fontFamily: 'PingFang SC',
                fontStyle: 'normal',
                fontWeight: '600',
                fontSize: 6,
                color: '#F4F4F4',
              }}>
              {player?.shirt_number}
            </Text>
          </ImageBackground>
        )}
        {topLeft()}
        {topRight()}
        {bottomLeft()}
        {bottomRight()}
      </View>
      <Text style={styles.textPlayerNumber}>{player?.shirt_number}</Text>
      <Text style={styles.textPlayerName} numberOfLines={1}>
        {player?.name}
      </Text>
      <View style={[styles.playerRate, rateColor(player?.rating)]}>
        <Text style={[styles.textPlayerRate]}>{round(player?.rating, 1)}</Text>
      </View>
    </View>
  );
};

export default Player;
