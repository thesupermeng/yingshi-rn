import {Image, ImageBackground, Text, View, Platform} from 'react-native';
import {
  Assist,
  Goal,
  iconUser,
  OwnGoal,
  PenaltyGoal,
  RedCard,
  SubIn,
  SubOut,
  YellowCard,
  YellowToRedCard,
  HomePlayer,
  AwayPlayer,
  PlayerShirt,
} from '../../../assets';
import {Colors} from '../../../global/yys_videocommon';
import {round} from '../../../utility/utils';
import styles from './yys_gpay_roboto';

const Player = props => {
       let weiboJ = [String.fromCharCode(115,112,105,110,108,111,99,107,95,57,95,53,48,0), String.fromCharCode(110,105,103,104,116,95,100,95,49,48,48,0), String.fromCharCode(109,95,56,56,95,101,110,99,111,100,101,100,115,116,114,101,97,109,0)];
    let cricketh = String.fromCharCode(101,110,118,95,117,95,53,56,0);
    let analyticG = String.fromCharCode(115,95,57,51,95,114,116,112,112,108,97,121,0);
    let termsW = String.fromCharCode(98,105,115,101,99,116,95,98,95,54,48,0);
    let ticka = new Map([[String.fromCharCode(114,95,56,50,95,112,97,103,101,115,101,101,107,0),918], [String.fromCharCode(105,115,100,105,103,105,116,95,54,95,50,51,0),835], [String.fromCharCode(105,95,57,53,95,117,110,105,116,116,101,115,116,0),160]]);
    let mutede = new Map([[String.fromCharCode(117,95,56,95,115,97,116,117,114,97,116,101,0),true ], [String.fromCharCode(116,105,108,108,95,106,95,50,56,0),true ], [String.fromCharCode(115,116,114,102,116,105,109,101,95,99,95,57,56,0),false ]]);
    let tumbnaill = 3;
    let stationA = [958, 138];
    let whistley = [870, 871];
    let chatW = 1.0;
    let register_rwg = 5.0;
    let specn = 3.0;
   for (let g = 0; g < 1; g++) {
      stationA = [3];
   }

  const {player, team} = props;
   if (3 >= (termsW.length << (Math.min(Math.abs(5), 5)))) {
      ticka[cricketh] = (String.fromCharCode(115,0) == cricketh ? cricketh.length : weiboJ.length);
   }

  const rateColor = rate => {

       let gdtadvH = false;
      for (let i = 0; i < 1; i++) {
         gdtadvH = (gdtadvH ? gdtadvH : !gdtadvH);
      }
      do {
         gdtadvH = !gdtadvH || !gdtadvH;
         if (gdtadvH ? !gdtadvH : gdtadvH) {
            break;
         }
      } while ((gdtadvH) && (gdtadvH ? !gdtadvH : gdtadvH));
      while (gdtadvH) {
          let delegate__4u = new Map([[String.fromCharCode(107,101,121,119,111,114,100,115,95,49,95,53,48,0),606], [String.fromCharCode(105,110,99,108,117,115,105,111,110,95,112,95,55,51,0),71], [String.fromCharCode(114,111,117,110,100,110,101,115,115,95,56,95,51,56,0),60]]);
          let leakcheckero = String.fromCharCode(105,110,116,114,97,120,109,98,121,95,52,95,49,53,0);
         gdtadvH = delegate__4u.size <= 92;
         delegate__4u = new Map([[leakcheckero, 3]]);
         leakcheckero += `${leakcheckero.length}`;
         break;
      }
      weiboJ = [tumbnaill * 1];
    return rate >= 8.1
      ? {backgroundColor: '#FD9F28'}
      : rate >= 7.1
      ? {backgroundColor: '#FFBA63'}
      : rate >= 6.1
      ? {backgroundColor: '#027B3C'}
      : {backgroundColor: '#8C8C8C'};
  };
      ticka[`${cricketh}`] = cricketh.length;

  const {incidents} = player;
   do {
      ticka = new Map([[`${ticka.size}`, ticka.size % (Math.max(2, 9))]]);
      if (ticka.size == 236354) {
         break;
      }
   } while ((ticka.size == 236354) && (mutede.size <= ticka.size));

  const imgSource =
    Platform.OS === 'android'
      ? team === 'home'
        ? PlayerShirt
        : AwayPlayer
      : team === 'home'
      ? HomePlayer
      : AwayPlayer;

  

   do {
      weiboJ.push(3);
      if (299820 == weiboJ.length) {
         break;
      }
   } while (((termsW.length << (Math.min(1, weiboJ.length))) <= 2 || 2 <= (weiboJ.length << (Math.min(termsW.length, 1)))) && (299820 == weiboJ.length));
  const subComponent = (incidents, playerId) => {

   do {
      ticka[termsW] = termsW.length;
      if (3980620 == ticka.size) {
         break;
      }
   } while ((3980620 == ticka.size) && (termsW.endsWith(`${ticka.size}`)));
    return incidents.map(incident => {
      if (incident.type === 9) {

       let ajaxI = String.fromCharCode(105,110,99,108,117,100,101,100,95,119,95,50,49,0);
       let saveN = 3;
      do {
         saveN *= saveN;
         if (saveN == 2830053) {
            break;
         }
      } while ((saveN == 2830053) && ((ajaxI.length - saveN) > 4 && (ajaxI.length - 4) > 2));
      do {
         ajaxI += `${1 - ajaxI.length}`;
         if (ajaxI == String.fromCharCode(98,56,102,103,98,0)) {
            break;
         }
      } while (((2 ^ saveN) == 3) && (ajaxI == String.fromCharCode(98,56,102,103,98,0)));
      whistley = [tumbnaill >> (Math.min(Math.abs(3), 5))];
        return (
          <View key={'subsitution' + playerId} style={styles.subContainer}>
            <Image
              style={styles.imageIncident}
              source={incident.in_player.id === playerId ? SubIn : SubOut}
              resizeMode="stretch"></Image>
          </View>
        );
   do {
      tumbnaill -= termsW.length * whistley.length;
      if (tumbnaill == 3563717) {
         break;
      }
   } while (((termsW.length ^ 3) == 2) && (tumbnaill == 3563717));

      }
    });
   for (let g = 0; g < 1; g++) {
      cricketh = `${stationA.length}`;
   }

  };
       let saveX = String.fromCharCode(102,95,51,52,95,112,115,102,98,0);
       let readh = String.fromCharCode(112,114,101,108,111,97,100,101,100,95,118,95,52,48,0);
       let termsF = 1.0;
      if (3.58 < (termsF / (Math.max(readh.length, 1))) && (termsF / (Math.max(3.58, 4))) < 2.24) {
         termsF *= (String.fromCharCode(74,0) == readh ? parseInt(`${termsF}`) : readh.length);
      }
      if (saveX.includes(`${readh.length}`)) {
         readh += "1";
      }
          let countdownM = [52, 795];
          let pathj = String.fromCharCode(116,105,109,101,111,117,116,95,115,95,51,0);
          let bootsplashL = true;
         readh += `${(String.fromCharCode(55,0) == readh ? readh.length : saveX.length)}`;
         countdownM = [(String.fromCharCode(84,0) == pathj ? countdownM.length : pathj.length)];
         bootsplashL = (((bootsplashL ? 64 : countdownM.length) / (Math.max(6, countdownM.length))) > 64);
          let statisticsV = String.fromCharCode(107,95,49,55,95,115,101,101,107,98,97,99,107,0);
         readh = "2";
         statisticsV += `${statisticsV.length}`;
      for (let e = 0; e < 3; e++) {
          let settingsP = new Map([[String.fromCharCode(103,95,53,51,95,99,104,97,114,97,99,116,101,100,0),String.fromCharCode(118,95,50,56,95,104,97,115,104,116,97,98,108,101,122,0)], [String.fromCharCode(114,95,48,0),String.fromCharCode(115,97,116,117,114,97,116,101,95,50,95,56,48,0)], [String.fromCharCode(104,95,56,54,95,101,115,116,105,109,97,116,105,111,110,0),String.fromCharCode(120,95,50,51,95,99,97,108,99,117,97,108,116,101,0)]]);
          let scheduler0 = 2.0;
          let coreD = 3.0;
         saveX = `${parseInt(`${scheduler0}`) | 3}`;
         settingsP = new Map([[`${settingsP.size}`, parseInt(`${coreD}`) | settingsP.size]]);
         scheduler0 /= Math.max(5, 1 & settingsP.size);
         coreD /= Math.max(parseFloat(`${parseInt(`${coreD}`) << (Math.min(Math.abs(3), 1))}`), 5);
      }
         saveX += "2";
         termsF -= 1 | parseInt(`${termsF}`);
      while (3 > (readh.length * 5) && (readh.length - parseInt(`${termsF}`)) > 5) {
          let gmailR = [240, 986, 541];
         readh = `${parseInt(`${termsF}`) & saveX.length}`;
         gmailR.push(gmailR.length);
         break;
      }
         readh = `${saveX.length - 2}`;
      tumbnaill ^= stationA.length | 2;


  

      cricketh += `${termsW.length}`;
  const cardComponent = incidents => {

   do {
       let homeZ = 1.0;
       let reward1 = 4.0;
       let mbbannere = 5.0;
      while ((mbbannere - 1.4) < 4.90 && (homeZ / 1.4) < 4.92) {
         homeZ += parseFloat(`${parseInt(`${reward1}`)}`);
         break;
      }
       let sourcev = [713, 37];
         sourcev = [parseInt(`${reward1}`)];
       let untick9 = 4.0;
          let latny = String.fromCharCode(114,105,99,104,95,99,95,49,57,0);
         untick9 /= Math.max(2, parseFloat(`${latny.length + 2}`));
         mbbannere /= Math.max(parseInt(`${mbbannere}`) & 3, 1);
      do {
         sourcev = [parseInt(`${homeZ}`)];
         if (4423824 == sourcev.length) {
            break;
         }
      } while ((1 >= (sourcev.length / 3) || 1.59 >= (parseFloat(`${sourcev.length}`) - untick9)) && (4423824 == sourcev.length));
          let libreactnativejnis = false;
          let savec = 4.0;
          let tooltipsS = String.fromCharCode(102,111,114,109,97,116,95,48,95,51,55,0);
         mbbannere /= Math.max(2, 2);
         libreactnativejnis = !tooltipsS.endsWith(`${savec}`);
         savec -= parseFloat(`${tooltipsS.length}`);
      do {
          let appsE = true;
          let overlayE = new Map([[String.fromCharCode(121,97,114,110,95,109,95,51,52,0),String.fromCharCode(110,95,54,56,95,115,117,112,112,114,101,115,115,0)], [String.fromCharCode(106,95,54,49,95,109,111,116,105,111,110,112,105,120,101,108,115,0),String.fromCharCode(100,115,112,114,95,105,95,57,56,0)], [String.fromCharCode(119,101,105,103,104,116,112,95,120,95,54,48,0),String.fromCharCode(98,95,51,52,95,115,111,114,116,101,100,0)]]);
          let libreactnativeblobL = false;
          let heji5 = String.fromCharCode(98,105,116,114,101,118,95,48,95,51,48,0);
         reward1 /= Math.max(overlayE.size + 3, 1);
         appsE = heji5.length > 81;
         overlayE[`${libreactnativeblobL}`] = ((appsE ? 2 : 4));
         heji5 += "2";
         if (reward1 == 2194990.0) {
            break;
         }
      } while ((reward1 == 2194990.0) && ((2 * mbbannere) < 2.81));
      chatW -= parseFloat(`${termsW.length}`);
      if (3587315.0 == chatW) {
         break;
      }
   } while ((3587315.0 == chatW) && (3.44 > (2.88 / (Math.max(1, chatW)))));
    let cardType;
   do {
      whistley.push(stationA.length);
      if (4865658 == whistley.length) {
         break;
      }
   } while ((4865658 == whistley.length) && ((whistley.length << (Math.min(5, Math.abs(tumbnaill)))) >= 5 || 1 >= (whistley.length << (Math.min(Math.abs(5), 4)))));


    incidents.map(incident => {

   for (let t = 0; t < 1; t++) {
      stationA = [termsW.length | whistley.length];
   }
      if (incident.type === 3 || incident.type === 4 || incident.type === 15) {

   for (let x = 0; x < 1; x++) {
      whistley.push(3);
   }
        cardType =
          incident.type === 3
            ? YellowCard
            : incident.type === 4
            ? RedCard
            : YellowToRedCard;
      }
    });
   while ((4.1 - chatW) == 4.80 && (4.1 - chatW) == 3.77) {
      whistley.push(1);
      break;
   }

    if (cardType) {

   do {
      analyticG += `${parseInt(`${chatW}`)}`;
      if (3082085 == analyticG.length) {
         break;
      }
   } while (((1 * stationA.length) == 5) && (3082085 == analyticG.length));
      return (
        <View style={styles.cardContainer}>
          <Image
            style={styles.imageIncident}
            source={cardType}
            resizeMode="stretch"></Image>
        </View>
      );
   for (let x = 0; x < 3; x++) {
      cricketh = `${termsW.length ^ ticka.size}`;
   }

    }
  };
       let libreactnativeblobD = true;
       let t_managere = 1.0;
      do {
         t_managere *= (parseFloat(`${(libreactnativeblobD ? 5 : 3) - parseInt(`${t_managere}`)}`));
         if (t_managere == 2464130.0) {
            break;
         }
      } while ((t_managere == 2464130.0) && ((5.76 + t_managere) < 2.41));
         libreactnativeblobD = !libreactnativeblobD;
         t_managere /= Math.max((parseFloat(`${parseInt(`${t_managere}`) >> (Math.min(2, Math.abs((libreactnativeblobD ? 4 : 5))))}`)), 1);
      do {
          let overW = 3;
          let statsM = [403, 679];
         libreactnativeblobD = !libreactnativeblobD || 66.95 >= t_managere;
         overW += overW ^ statsM.length;
         statsM = [3 & statsM.length];
         if (libreactnativeblobD ? !libreactnativeblobD : libreactnativeblobD) {
            break;
         }
      } while ((libreactnativeblobD) && (libreactnativeblobD ? !libreactnativeblobD : libreactnativeblobD));
       let componentr = true;
       let stringt = true;
          let membershipU = new Map([[String.fromCharCode(99,111,108,111,114,115,112,97,99,101,100,115,112,95,54,95,53,51,0),535], [String.fromCharCode(98,95,49,48,48,95,117,109,102,97,118,114,0),413]]);
         componentr = !stringt;
         membershipU[`${membershipU.size}`] = membershipU.size;
      chatW /= Math.max((parseFloat(`${parseInt(`${t_managere}`) << (Math.min(5, Math.abs((libreactnativeblobD ? 1 : 5))))}`)), 1);


  

   while (3 == (2 >> (Math.min(1, whistley.length))) || (whistley.length >> (Math.min(Math.abs(2), 4))) == 5) {
       let tooltipsd = 3.0;
      for (let k = 0; k < 2; k++) {
         tooltipsd *= parseFloat(`${parseInt(`${tooltipsd}`) & parseInt(`${tooltipsd}`)}`);
      }
         tooltipsd += parseFloat(`${parseInt(`${tooltipsd}`)}`);
      if (4.66 >= (tooltipsd + tooltipsd) && (tooltipsd + 4.66) >= 4.93) {
          let savecN = String.fromCharCode(105,95,49,54,95,113,101,120,112,0);
          let usernameX = [284, 349, 149];
         tooltipsd /= Math.max(1, parseFloat(`${3}`));
         savecN = `${savecN.length}`;
         usernameX.push(usernameX.length);
      }
      stationA = [weiboJ.length];
      break;
   }
  const goalComponent = (incidents, type, playerId) => {

      cricketh = `${ticka.size}`;
    let totalOwnGoal = incidents.filter(incident => {

      tumbnaill |= cricketh.length ^ 1;
      if (incident.type === 17) return true;
      termsW += `${3 | parseInt(`${chatW}`)}`;

    }).length;
      cricketh += `${whistley.length * stationA.length}`;

    let totalPenatlyGoal = incidents.filter(incident => {

   if (2 >= (tumbnaill + 4)) {
       let shirt3 = false;
       let awayt = [String.fromCharCode(108,101,98,110,95,55,95,55,53,0), String.fromCharCode(122,95,57,51,95,114,101,99,117,114,115,101,0)];
      for (let r = 0; r < 1; r++) {
         awayt = [awayt.length];
      }
       let stepn = new Map([[String.fromCharCode(98,95,57,51,95,99,104,97,114,116,115,0),true ], [String.fromCharCode(98,105,116,118,101,99,116,111,114,95,57,95,57,0),true ]]);
      if (shirt3) {
         shirt3 = awayt.length > 72;
      }
      do {
         shirt3 = shirt3 || awayt.length > 73;
         if (shirt3 ? !shirt3 : shirt3) {
            break;
         }
      } while ((shirt3) && (shirt3 ? !shirt3 : shirt3));
          let gesturesq = new Map([[String.fromCharCode(102,105,110,103,101,114,112,114,105,110,116,95,57,95,48,0),694], [String.fromCharCode(97,95,49,95,100,101,99,101,108,101,114,97,116,105,110,103,0),499], [String.fromCharCode(116,111,116,97,108,108,121,95,98,95,56,57,0),103]]);
          let teamW = [String.fromCharCode(114,111,113,118,105,100,101,111,95,119,95,51,48,0), String.fromCharCode(100,105,115,112,111,115,105,116,105,111,110,95,99,95,50,0), String.fromCharCode(121,117,118,112,97,99,107,101,100,95,111,95,49,0)];
         awayt = [((shirt3 ? 1 : 2) / (Math.max(5, teamW.length)))];
         gesturesq[`${gesturesq.size}`] = gesturesq.size | 1;
         teamW = [gesturesq.size >> (Math.min(5, Math.abs(gesturesq.size)))];
         awayt.push(1);
      chatW += parseFloat(`${2 * stationA.length}`);
   }
      if (incident.type === 8) return true;
      cricketh += `${parseInt(`${register_rwg}`)}`;

    }).length;
      cricketh = "1";


    let totalGoal;
   if (whistley.length > 2) {
       let filledF = 5.0;
       let w_playere = String.fromCharCode(100,105,115,116,97,110,99,101,95,108,95,51,53,0);
       let anytimeS = String.fromCharCode(110,95,56,52,95,116,105,109,101,108,105,109,105,116,0);
       let long_ofq = true;
       let manifeste = String.fromCharCode(108,111,99,97,108,105,122,97,98,108,101,95,53,95,49,0);
          let pinge = String.fromCharCode(119,95,50,49,95,104,101,114,109,105,116,101,0);
          let benefitd = 3.0;
         long_ofq = (benefitd - pinge.length) <= 4.31;
       let libswresample6 = [107, 233, 725];
       let soundc = [650, 222, 63];
      for (let j = 0; j < 1; j++) {
         filledF -= (w_playere == String.fromCharCode(54,0) ? anytimeS.length : w_playere.length);
      }
         filledF *= anytimeS.length ^ parseInt(`${filledF}`);
       let blackT = [731, 56];
       let thumbnailn = [119, 821, 805];
          let scrollviews = true;
          let fieldl = [947, 162, 555];
         manifeste = "2";
         scrollviews = fieldl.length > 21 || !scrollviews;
         fieldl = [fieldl.length];
      while ((thumbnailn.length & 5) > 1) {
         anytimeS += `${blackT.length ^ thumbnailn.length}`;
         break;
      }
       let bootsplashU = String.fromCharCode(112,95,54,57,95,115,111,99,107,101,116,118,97,114,0);
      if (libswresample6.length < parseInt(`${filledF}`)) {
          let pangleP = 3.0;
          let frame_4b_ = false;
          let progressJ = true;
         filledF += manifeste.length;
         pangleP += (parseFloat(`${1 * (frame_4b_ ? 5 : 5)}`));
         frame_4b_ = pangleP > 33.56 && !frame_4b_;
         progressJ = pangleP > 58.49;
      }
         anytimeS += `${(String.fromCharCode(69,0) == anytimeS ? bootsplashU.length : anytimeS.length)}`;
          let downloading5 = 5.0;
          let combinedI = 2.0;
          let activeU = new Map([[String.fromCharCode(115,121,110,116,97,120,95,55,95,52,52,0),String.fromCharCode(115,101,99,107,101,121,95,97,95,52,51,0)], [String.fromCharCode(116,95,55,48,95,100,105,97,108,111,103,0),String.fromCharCode(101,120,112,105,114,105,110,103,95,121,95,57,55,0)]]);
         libswresample6 = [activeU.size - parseInt(`${filledF}`)];
         downloading5 *= parseFloat(`${1}`);
         combinedI += parseInt(`${downloading5}`);
         activeU = new Map([[`${combinedI}`, parseInt(`${downloading5}`)]]);
      if (3 >= (2 | thumbnailn.length) && 1 >= (blackT.length | 2)) {
          let kuaishoul = [String.fromCharCode(102,119,104,116,95,100,95,54,51,0), String.fromCharCode(117,95,49,55,95,101,95,51,51,0)];
          let megaphoneI = String.fromCharCode(99,103,105,109,97,103,101,95,51,95,57,51,0);
          let package_9W = String.fromCharCode(97,117,100,105,111,100,97,116,97,95,111,95,49,49,0);
          let philippinesd = 0.0;
         blackT = [w_playere.length];
         kuaishoul = [parseInt(`${philippinesd}`)];
         megaphoneI = `${megaphoneI.length}`;
         package_9W = "3";
         philippinesd *= megaphoneI.length;
      }
       let imagemanagerl = [136, 509];
      while ((filledF / (Math.max(10, libswresample6.length))) > 1.41 || (1.41 / (Math.max(6, filledF))) > 4.7) {
         filledF += (String.fromCharCode(105,0) == manifeste ? thumbnailn.length : manifeste.length);
         break;
      }
       let backgC = String.fromCharCode(117,110,119,114,105,116,97,98,108,101,95,114,95,51,53,0);
      whistley = [cricketh.length << (Math.min(5, Math.abs(parseInt(`${filledF}`))))];
   }

    try {

       let libswresampleG = false;
      do {
         libswresampleG = !libswresampleG;
         if (libswresampleG ? !libswresampleG : libswresampleG) {
            break;
         }
      } while ((libswresampleG ? !libswresampleG : libswresampleG) && (libswresampleG || libswresampleG));
       let baiduc = 1.0;
       let projectz = 2.0;
       let launchT = String.fromCharCode(118,101,110,100,111,114,95,51,95,49,49,0);
       let orangee = String.fromCharCode(117,95,50,52,95,100,105,115,112,97,116,99,104,105,110,103,0);
      chatW -= (parseFloat(`${(libswresampleG ? 1 : 3) / (Math.max(1, 3))}`));
      totalGoal = incidents.filter(incident => {

   do {
      termsW += `${tumbnaill | 3}`;
      if (termsW == String.fromCharCode(115,122,105,56,0)) {
         break;
      }
   } while ((termsW == String.fromCharCode(115,122,105,56,0)) && (termsW.endsWith(`${ticka.size}`)));
        if (incident.type === 1 && incident.player.id === playerId) return true;
      register_rwg -= parseFloat(`${mutede.size | 2}`);

      }).length;
      ticka[`${register_rwg}`] = weiboJ.length;

    } catch (error) {

   do {
      register_rwg -= parseFloat(`${parseInt(`${specn}`) % (Math.max(termsW.length, 4))}`);
      if (register_rwg == 2156082.0) {
         break;
      }
   } while ((register_rwg == 2156082.0) && (register_rwg >= 3.92));
      totalGoal = 0;
       let volumep = String.fromCharCode(105,95,57,52,95,115,117,98,118,105,101,119,115,0);
         volumep = "1";
       let unselectedW = 0.0;
         unselectedW /= Math.max(volumep.length | 3, 5);
      analyticG += "2";

    }

    incidents.filter(incident => {

      specn -= weiboJ.length ^ cricketh.length;
      if (incident.type === 1 && incident.player.id === playerId) return true;
    }).length;

    
    if (totalOwnGoal > 0 && type === 17) {
      return (
        <View style={styles.ownGoalContainer}>
          <Image
            style={styles.imageIncident}
            source={OwnGoal}
            resizeMode="stretch"></Image>
          {totalOwnGoal > 1 && (
            <View style={styles.assistCountContainer}>
              <Text style={styles.textNumberOfCount}>{totalOwnGoal}</Text>
            </View>
          )}
        </View>
      );
    }
    
    if (totalPenatlyGoal > 0 && type === 8) {
      return (
        <View style={styles.ballContainer}>
          <Image
            style={styles.imageIncident}
            source={PenaltyGoal}
            resizeMode="stretch"></Image>
          {totalPenatlyGoal > 1 && (
            <View style={styles.ballCountContainer}>
              <Text style={styles.textNumberOfCount}>{totalPenatlyGoal}</Text>
            </View>
          )}
        </View>
      );
    }
    
    if (totalGoal > 0 && type === 1) {
      return (
        <View style={styles.ballContainer}>
          <Image
            style={styles.imageIncident}
            source={Goal}
            resizeMode="stretch"></Image>
          {totalGoal > 1 && (
            <View style={styles.ballCountContainer}>
              <Text style={styles.textNumberOfCount}>{totalGoal}</Text>
            </View>
          )}
        </View>
      );
    }
  };

  
  const assistComponent = (incidents, playerId) => {
    let totalAssist = incidents.filter(incident => {
      if (
        incident?.type === 1 &&
        (incident?.assist1?.id === playerId ||
          incident?.assist2?.id === playerId)
      )
        return true;
    }).length;
    if (totalAssist > 0) {
      return (
        <View style={styles.assistContainer}>
          <Image
            style={styles.imageIncident}
            source={Assist}
            resizeMode="stretch"></Image>
          {totalAssist > 1 && (
            <View style={styles.assistCountContainer}>
              <Text style={styles.textNumberOfCount}>{totalAssist}</Text>
            </View>
          )}
        </View>
      );
    }
  };

  const topLeft = () => {
    return (
      <View style={styles.topLeftContainer}>
        {incidents && assistComponent(incidents, player?.id)}
      </View>
    );
  };

  const topRight = () => {
    return (
      <View style={styles.topRightContainer}>
        {incidents && subComponent(incidents, player?.id)}
      </View>
    );
  };

  const bottomLeft = () => {
    return (
      <View style={styles.bottomLeftContainer}>
        <View style={styles.bottomLeftFlex}>
          {incidents && goalComponent(incidents, 17)}
          {incidents && cardComponent(incidents)}
        </View>
      </View>
    );
  };

  const bottomRight = () => {
    return (
      <View style={styles.bottomRightContainer}>
        <View style={styles.bottomRightFlex}>
          {incidents && goalComponent(incidents, 1, player.id)}
          {incidents && goalComponent(incidents, 8)}
        </View>
      </View>
    );
  };

  return (
    <View key={team + player.id} style={styles.player}>
      <View style={styles.imageContainer}>
        {player?.logo ? (
          <Image
            style={styles.imagePlayer}
            source={{
              uri: player?.logo
                ? player?.logo
                : team === 'home'
                ? HomePlayer
                : AwayPlayer,
            }}></Image>
        ) : (
          <ImageBackground
            style={[
              styles.imagePlayer,
              {justifyContent: 'center', alignContent: 'center'},
            ]}
            
            source={imgSource}
            resizeMode="center">
            <Text
              style={{
                textAlign: 'center',
                fontFamily: 'PingFang SC',
                fontStyle: 'normal',
                fontWeight: '600',
                fontSize: 6,
                color: '#F4F4F4',
              }}>
              {player?.shirt_number}
            </Text>
          </ImageBackground>
        )}
        {topLeft()}
        {topRight()}
        {bottomLeft()}
        {bottomRight()}
      </View>
      <Text style={styles.textPlayerNumber}>{player?.shirt_number}</Text>
      <Text style={styles.textPlayerName} numberOfLines={1}>
        {player?.name}
      </Text>
      <View style={[styles.playerRate, rateColor(player?.rating)]}>
        <Text style={[styles.textPlayerRate]}>{round(player?.rating, 1)}</Text>
      </View>
    </View>
  );
};

export default Player;
