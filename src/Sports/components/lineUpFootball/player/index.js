import {Image, ImageBackground, Text, View, Platform} from 'react-native';
import {
  Assist,
  Goal,
  iconUser,
  OwnGoal,
  PenaltyGoal,
  RedCard,
  SubIn,
  SubOut,
  YellowCard,
  YellowToRedCard,
  HomePlayer,
  AwayPlayer,
  PlayerShirt,
} from '../../../assets';
import {Colors} from '../../../global/bp_grey_vietnam';
import {round} from '../../../utility/utils';
import styles from './fgl_less_position';

const Player = props => {
       let side0 = String.fromCharCode(112,117,116,115,95,116,95,57,57,0);
    let overlayF = String.fromCharCode(97,118,101,114,0);
    let const_30H = String.fromCharCode(101,120,116,114,97,112,111,108,97,116,111,114,0);
    let report7 = false;
    let taiwan5 = 3;
    let infoj = 5;
    let math9 = 0.0;
    let typesL = 2.0;
    let service6 = [417, 671, 222];
    let short_kx = true;
    let contextr = [963, 762];
    let singapore0 = new Map([[String.fromCharCode(108,111,103,103,101,114,115,0),false ], [String.fromCharCode(105,112,113,102,0),true ]]);
   while (5 <= (service6.length * 5) || (math9 - 5.92) <= 5.70) {
       let phoneX = new Map([[String.fromCharCode(115,112,111,116,108,105,103,104,116,0),126], [String.fromCharCode(104,95,50,57,95,114,101,110,100,101,114,101,114,115,0),585]]);
       let fillQ = String.fromCharCode(99,97,108,108,101,114,0);
       let checkbox9 = String.fromCharCode(103,105,103,97,98,121,116,101,115,0);
       let bingz = String.fromCharCode(105,115,114,101,97,100,111,110,108,121,0);
         checkbox9 += `${bingz.length - phoneX.size}`;
         bingz += `${1 << (Math.min(1, checkbox9.length))}`;
         bingz = `${fillQ.length}`;
      do {
          let diceb = 5;
          let injuryb = [318, 175];
         phoneX[`${diceb}`] = 2;
         diceb |= injuryb.length;
         injuryb.push(injuryb.length % (Math.max(injuryb.length, 3)));
         if (3010440 == phoneX.size) {
            break;
         }
      } while (((phoneX.size - 4) == 4 && (4 - phoneX.size) == 1) && (3010440 == phoneX.size));
          let pageA = new Map([[String.fromCharCode(118,97,114,109,97,115,107,95,112,95,49,51,0),String.fromCharCode(121,95,49,51,95,115,117,103,103,101,115,116,101,100,0)], [String.fromCharCode(110,111,110,122,101,114,111,95,117,95,55,51,0),String.fromCharCode(117,110,105,120,0)]]);
          let smalll = 0;
         phoneX = new Map([[checkbox9, checkbox9.length >> (Math.min(Math.abs(1), 5))]]);
         pageA = new Map([[`${pageA.size}`, smalll ^ 2]]);
         smalll <<= Math.min(3, Math.abs(pageA.size));
      for (let b = 0; b < 2; b++) {
          let register_7w_ = 0;
          let detailsw = String.fromCharCode(108,95,57,56,95,108,115,112,112,111,108,121,0);
          let combinedM = String.fromCharCode(98,97,115,105,99,97,108,108,121,0);
         phoneX = new Map([[combinedM, register_7w_]]);
         register_7w_ &= (detailsw == String.fromCharCode(56,0) ? detailsw.length : detailsw.length);
      }
      for (let a = 0; a < 2; a++) {
         bingz += `${phoneX.size * 1}`;
      }
      for (let n = 0; n < 2; n++) {
         phoneX[checkbox9] = phoneX.size / 2;
      }
      while (4 < checkbox9.length || fillQ.length < 4) {
         checkbox9 += `${phoneX.size}`;
         break;
      }
          let typingJ = 4;
          let yingI = String.fromCharCode(97,118,97,116,97,114,115,0);
         fillQ = "1";
         typingJ >>= Math.min(5, Math.abs(2 / (Math.max(8, yingI.length))));
         yingI = `${typingJ}`;
         checkbox9 = `${fillQ.length}`;
         checkbox9 += `${3 % (Math.max(3, phoneX.size))}`;
      service6 = [(String.fromCharCode(98,0) == side0 ? bingz.length : side0.length)];
      break;
   }

  const {player, team} = props;
      typesL /= Math.max(parseFloat(`${const_30H.length >> (Math.min(Math.abs(2), 1))}`), 1);

  const rateColor = rate => {

       let private_wj = 1.0;
          let types3 = 2;
          let arrowN = String.fromCharCode(106,111,105,110,105,110,103,0);
         private_wj *= parseFloat(`${types3 / (Math.max(1, arrowN.length))}`);
      for (let v = 0; v < 2; v++) {
         private_wj /= Math.max(parseFloat(`${parseInt(`${private_wj}`) / 1}`), 4);
      }
         private_wj += parseFloat(`${parseInt(`${private_wj}`) << (Math.min(Math.abs(parseInt(`${private_wj}`)), 1))}`);
      taiwan5 -= (1 - (report7 ? 3 : 5));
    return rate >= 8.1
      ? {backgroundColor: '#FD9F28'}
      : rate >= 7.1
      ? {backgroundColor: '#FFBA63'}
      : rate >= 6.1
      ? {backgroundColor: '#027B3C'}
      : {backgroundColor: '#8C8C8C'};
  };
      math9 += (parseFloat(`${parseInt(`${typesL}`) - (report7 ? 2 : 2)}`));

  const {incidents} = player;
      report7 = String.fromCharCode(107,0) == side0 || typesL <= 80.81;

  const imgSource =
    Platform.OS === 'android'
      ? team === 'home'
        ? PlayerShirt
        : AwayPlayer
      : team === 'home'
      ? HomePlayer
      : AwayPlayer;

  

      const_30H += `${3 % (Math.max(2, taiwan5))}`;
  const subComponent = (incidents, playerId) => {

   while ((3.22 * math9) <= 1.45 || 3 <= (contextr.length / 3)) {
      math9 /= Math.max(parseFloat(`${service6.length}`), 1);
      break;
   }
    return incidents.map(incident => {
      if (incident.type === 9) {

       let modeK = 3.0;
      do {
          let awayv = String.fromCharCode(119,97,108,107,101,114,0);
         modeK -= parseFloat(`${1 * awayv.length}`);
         if (modeK == 2665493.0) {
            break;
         }
      } while ((1.94 >= (modeK - modeK)) && (modeK == 2665493.0));
      for (let b = 0; b < 1; b++) {
         modeK -= parseFloat(`${parseInt(`${modeK}`) ^ parseInt(`${modeK}`)}`);
      }
          let style4 = new Map([[String.fromCharCode(112,97,110,101,115,0),true ], [String.fromCharCode(108,95,49,52,95,115,104,105,102,116,105,110,103,115,0),true ]]);
          let launchq = [String.fromCharCode(109,105,114,114,111,114,105,110,103,95,57,95,55,55,0), String.fromCharCode(115,101,116,108,101,99,116,101,100,0)];
          let heartc = 4.0;
         modeK += parseFloat(`${3 & style4.size}`);
         style4 = new Map([[`${launchq.length}`, parseInt(`${heartc}`)]]);
         launchq.push(launchq.length ^ 2);
         heartc *= parseInt(`${heartc}`) % 1;
      short_kx = overlayF == String.fromCharCode(72,0);
        return (
          <View key={'subsitution' + playerId} style={styles.subContainer}>
            <Image
              style={styles.imageIncident}
              source={incident.in_player.id === playerId ? SubIn : SubOut}
              resizeMode="stretch"></Image>
          </View>
        );
      contextr = [contextr.length ^ taiwan5];

      }
    });
      overlayF += `${1 * parseInt(`${math9}`)}`;

  };
      report7 = (73 <= ((!short_kx ? 73 : service6.length) / (Math.max(service6.length, 5))));


  

   for (let q = 0; q < 1; q++) {
       let time_gF = String.fromCharCode(100,95,57,56,95,97,108,116,0);
       let abouti = String.fromCharCode(112,95,54,54,95,114,101,116,114,121,97,98,108,101,0);
       let invitea = String.fromCharCode(114,101,102,108,101,99,116,105,111,110,0);
       let drag4 = String.fromCharCode(112,101,111,112,108,101,0);
          let progressl = String.fromCharCode(112,117,98,108,105,99,107,101,121,115,0);
         time_gF = `${abouti.length}`;
         progressl += "1";
      do {
         abouti += `${invitea.length}`;
         if (abouti == String.fromCharCode(52,121,105,116,0)) {
            break;
         }
      } while ((invitea == abouti) && (abouti == String.fromCharCode(52,121,105,116,0)));
       let large1 = 1.0;
      if (2.91 >= large1) {
         abouti += `${invitea.length}`;
      }
      if (abouti != String.fromCharCode(88,0)) {
         drag4 += "2";
      }
      if (time_gF.length >= parseInt(`${large1}`)) {
          let umengN = 2.0;
          let album6 = 5.0;
          let ajaxM = 0;
          let matchesT = 5.0;
          let injuryp = 1.0;
         large1 /= Math.max(2, drag4.length + parseInt(`${large1}`));
         umengN -= parseFloat(`${ajaxM}`);
         album6 += parseFloat(`${parseInt(`${album6}`)}`);
         ajaxM |= 3;
         matchesT += 2;
         injuryp /= Math.max(parseFloat(`${parseInt(`${injuryp}`)}`), 2);
      }
      for (let o = 0; o < 3; o++) {
         drag4 = `${(invitea == String.fromCharCode(119,0) ? invitea.length : abouti.length)}`;
      }
      do {
          let dangeri = String.fromCharCode(120,95,56,55,95,115,111,108,118,101,114,0);
         invitea = `${drag4.length}`;
         dangeri = `${dangeri.length}`;
         if (invitea.length == 4465432) {
            break;
         }
      } while ((invitea.length == 4465432) && (drag4 == String.fromCharCode(74,0)));
      for (let e = 0; e < 2; e++) {
         abouti += `${abouti.length | parseInt(`${large1}`)}`;
      }
      if (drag4.length >= 2) {
          let singleA = String.fromCharCode(100,95,50,55,95,112,111,115,116,102,105,108,116,101,114,0);
          let textj = new Map([[String.fromCharCode(122,95,52,52,95,102,108,97,115,104,105,110,103,0),String.fromCharCode(109,95,51,50,95,112,114,111,118,105,100,101,114,0)], [String.fromCharCode(99,106,112,101,103,0),String.fromCharCode(111,102,102,0)]]);
          let sendU = String.fromCharCode(118,112,105,116,120,102,109,0);
          let robotom = String.fromCharCode(111,112,116,97,98,108,101,0);
         drag4 += `${textj.size & 2}`;
         singleA = `${robotom.length}`;
         textj = new Map([[singleA, singleA.length + sendU.length]]);
         sendU = `${3 << (Math.min(4, sendU.length))}`;
         robotom += `${sendU.length & 3}`;
      }
         abouti += `${drag4.length % (Math.max(2, 6))}`;
         drag4 = `${abouti.length ^ 3}`;
      contextr = [3 << (Math.min(2, const_30H.length))];
   }
  const cardComponent = incidents => {

       let catalogp = [446, 484, 36];
          let xvodx = String.fromCharCode(98,108,111,111,109,95,99,95,49,54,0);
          let regengb = 1;
         catalogp = [regengb];
         xvodx += `${xvodx.length | xvodx.length}`;
         regengb %= Math.max(xvodx.length / (Math.max(3, 9)), 3);
       let currentZ = false;
       let awaya = true;
       let matchesP = false;
      short_kx = catalogp.length <= 99;
    let cardType;
   if (!short_kx) {
      short_kx = (((!report7 ? 5 : service6.length) - service6.length) == 5);
   }


    incidents.map(incident => {

       let minivodT = [578, 70, 249];
       let previewh = String.fromCharCode(118,105,100,101,111,115,116,114,101,97,109,0);
         minivodT = [minivodT.length];
      while (minivodT.length >= 5) {
         previewh += "3";
         break;
      }
       let pingG = new Map([[String.fromCharCode(115,117,98,116,114,101,101,95,117,95,52,48,0),true ], [String.fromCharCode(114,101,116,114,97,110,115,109,105,116,0),false ], [String.fromCharCode(115,111,117,114,99,101,99,108,105,112,0),false ]]);
          let verticalU = 2.0;
          let statisticsw = new Map([[String.fromCharCode(111,112,101,110,115,115,108,118,0),false ], [String.fromCharCode(115,116,97,110,100,97,114,100,95,102,95,57,52,0),false ]]);
         pingG = new Map([[`${pingG.size}`, previewh.length + 2]]);
         verticalU /= Math.max(parseFloat(`${1 << (Math.min(4, Math.abs(statisticsw.size)))}`), 1);
         statisticsw = new Map([[`${statisticsw.size}`, parseInt(`${verticalU}`) << (Math.min(Math.abs(statisticsw.size), 2))]]);
         previewh = `${(String.fromCharCode(113,0) == previewh ? previewh.length : minivodT.length)}`;
      do {
          let update_4sP = false;
          let gray0 = 2;
         pingG = new Map([[`${pingG.size}`, 3 >> (Math.min(5, Math.abs(pingG.size)))]]);
         update_4sP = !update_4sP || 75 >= gray0;
         gray0 += gray0;
         if (pingG.size == 3928291) {
            break;
         }
      } while (((pingG.size - previewh.length) <= 2 || (pingG.size - previewh.length) <= 2) && (pingG.size == 3928291));
      report7 = 97.60 < (typesL + math9);
      if (incident.type === 3 || incident.type === 4 || incident.type === 15) {

      const_30H = "1";
        cardType =
          incident.type === 3
            ? YellowCard
            : incident.type === 4
            ? RedCard
            : YellowToRedCard;
      }
    });
   do {
      side0 += `${2 ^ infoj}`;
      if (String.fromCharCode(48,110,114,115,122,48,109,56,114,0) == side0) {
         break;
      }
   } while ((String.fromCharCode(48,110,114,115,122,48,109,56,114,0) == side0) && ((4.84 - typesL) <= 4.35 && (parseFloat(`${side0.length}`) - typesL) <= 4.84));

    if (cardType) {

      taiwan5 *= overlayF.length * 3;
      return (
        <View style={styles.cardContainer}>
          <Image
            style={styles.imageIncident}
            source={cardType}
            resizeMode="stretch"></Image>
        </View>
      );
       let fastforwardQ = 3;
       let settingZ = 4;
       let saveH = 2.0;
         settingZ |= 3 * settingZ;
      do {
         saveH += parseFloat(`${1}`);
         if (saveH == 2895675.0) {
            break;
         }
      } while ((saveH == 2895675.0) && ((4.6 + saveH) < 2.2));
          let brightnessv = true;
          let otherP = 4.0;
         fastforwardQ ^= (parseInt(`${otherP}`) ^ (brightnessv ? 4 : 2));
         saveH -= parseFloat(`${settingZ & fastforwardQ}`);
          let adulti = String.fromCharCode(111,109,105,116,116,105,110,103,0);
          let targetA = false;
         fastforwardQ <<= Math.min(4, Math.abs(1 | parseInt(`${saveH}`)));
         adulti += `${((targetA ? 3 : 5))}`;
         targetA = adulti.length > 99 || targetA;
         saveH *= parseFloat(`${2 >> (Math.min(Math.abs(settingZ), 2))}`);
      for (let j = 0; j < 1; j++) {
         saveH -= parseFloat(`${1}`);
      }
         fastforwardQ += settingZ;
      do {
          let circleh = String.fromCharCode(97,97,99,101,110,99,100,115,112,0);
         settingZ &= circleh.length - settingZ;
         if (settingZ == 3684652) {
            break;
         }
      } while ((saveH == 1.13) && (settingZ == 3684652));
      short_kx = fastforwardQ <= 18;

    }
  };
      service6 = [(String.fromCharCode(116,0) == const_30H ? const_30H.length : infoj)];


  

   for (let f = 0; f < 1; f++) {
      service6.push(infoj);
   }
  const goalComponent = (incidents, type, playerId) => {

   do {
      typesL *= parseFloat(`${2 >> (Math.min(1, side0.length))}`);
      if (typesL == 1092947.0) {
         break;
      }
   } while ((typesL == 1092947.0) && (overlayF.length < 3));
    let totalOwnGoal = incidents.filter(incident => {

      infoj <<= Math.min(Math.abs(singapore0.size), 5);
      if (incident.type === 17) return true;
      math9 += parseFloat(`${singapore0.size | 1}`);

    }).length;
      const_30H = `${(2 >> (Math.min(1, Math.abs((report7 ? 4 : 2)))))}`;

    let totalPenatlyGoal = incidents.filter(incident => {

   if (taiwan5 < 5) {
      taiwan5 %= Math.max(2, (side0.length ^ (short_kx ? 3 : 1)));
   }
      if (incident.type === 8) return true;
       let gemfilek = 4.0;
       let headerz = [817, 528, 568];
      if (!headerz.includes(gemfilek)) {
          let vignette_ = String.fromCharCode(104,117,100,0);
          let firebaseB = new Map([[String.fromCharCode(102,111,114,99,101,0),167], [String.fromCharCode(100,105,114,112,0),431], [String.fromCharCode(102,95,50,52,95,118,112,108,97,121,101,114,0),269]]);
         headerz = [1];
         vignette_ += `${vignette_.length | firebaseB.size}`;
         firebaseB[vignette_] = firebaseB.size;
      }
         headerz.push(1);
      do {
         headerz = [parseInt(`${gemfilek}`) % 1];
         if (1119959 == headerz.length) {
            break;
         }
      } while ((1119959 == headerz.length) && (1 > (headerz.length | 2)));
         headerz = [headerz.length & parseInt(`${gemfilek}`)];
       let ewardedC = true;
         headerz.push(((ewardedC ? 5 : 3) | 1));
      math9 *= parseFloat(`${3}`);

    }).length;
      service6.push(service6.length);


    let totalGoal;
   while (math9 > 2.92) {
      taiwan5 *= ((report7 ? 4 : 4) & 1);
      break;
   }

    try {

       let componentk = [788, 130];
         componentk.push(componentk.length);
      for (let g = 0; g < 1; g++) {
         componentk = [componentk.length];
      }
       let frame_vK = String.fromCharCode(98,111,111,107,109,97,114,107,115,0);
      taiwan5 += taiwan5;
      totalGoal = incidents.filter(incident => {

   for (let v = 0; v < 3; v++) {
       let internetL = 0.0;
       let canvasF = [434, 75];
       let selection2 = String.fromCharCode(116,114,97,110,115,99,111,100,101,0);
       let volumez = String.fromCharCode(114,101,99,111,114,100,105,110,103,99,111,110,110,0);
       let download8 = String.fromCharCode(115,116,97,116,105,99,0);
      while (selection2 != String.fromCharCode(114,0) && volumez == String.fromCharCode(121,0)) {
          let expireds = true;
          let less_ = false;
          let trophyn = 2.0;
         volumez = `${canvasF.length / (Math.max(selection2.length, 4))}`;
         expireds = !less_ || 50.49 < trophyn;
         less_ = trophyn < 33.58;
         break;
      }
          let progressB = 4.0;
          let malaysiaL = [28, 757];
         internetL /= Math.max(canvasF.length, 5);
         progressB *= parseFloat(`${parseInt(`${progressB}`) ^ malaysiaL.length}`);
         malaysiaL = [malaysiaL.length - parseInt(`${progressB}`)];
         volumez += `${canvasF.length >> (Math.min(5, Math.abs(parseInt(`${internetL}`))))}`;
         selection2 += `${(String.fromCharCode(69,0) == selection2 ? download8.length : selection2.length)}`;
      for (let i = 0; i < 1; i++) {
         volumez = `${(download8 == String.fromCharCode(53,0) ? volumez.length : download8.length)}`;
      }
      do {
          let photoj = String.fromCharCode(114,101,113,115,116,97,116,101,0);
          let robotok = String.fromCharCode(110,95,54,57,95,115,97,109,112,108,101,114,97,116,101,0);
          let stations0 = new Map([[String.fromCharCode(114,101,115,116,111,114,97,116,105,111,110,0),false ], [String.fromCharCode(113,95,53,50,95,115,111,108,118,101,100,0),false ]]);
         volumez += `${selection2.length}`;
         photoj = `${robotok.length}`;
         robotok = `${robotok.length}`;
         stations0[`${robotok}`] = stations0.size >> (Math.min(robotok.length, 5));
         if (volumez.length == 4005077) {
            break;
         }
      } while ((download8.length > volumez.length) && (volumez.length == 4005077));
      do {
          let goalg = String.fromCharCode(108,109,100,98,0);
          let entryt = 5;
          let referrer1 = 5.0;
          let sports7 = String.fromCharCode(98,114,111,119,115,101,95,110,95,56,0);
          let configP = String.fromCharCode(104,115,99,97,108,101,114,0);
         volumez += `${parseInt(`${internetL}`)}`;
         goalg = `${parseInt(`${referrer1}`) + entryt}`;
         entryt /= Math.max(2, 3 & parseInt(`${referrer1}`));
         sports7 += `${(goalg == String.fromCharCode(84,0) ? entryt : goalg.length)}`;
         configP = `${configP.length}`;
         if (volumez.length == 4820132) {
            break;
         }
      } while (((internetL / (Math.max(5.96, 4))) >= 1.96 && (internetL / (Math.max(volumez.length, 6))) >= 5.96) && (volumez.length == 4820132));
      if (2 >= (canvasF.length / (Math.max(5, 9))) || 5 >= (canvasF.length / (Math.max(download8.length, 6)))) {
          let yingZ = 2.0;
         canvasF = [3 % (Math.max(9, selection2.length))];
         yingZ += parseInt(`${yingZ}`) << (Math.min(1, Math.abs(3)));
      }
          let overlayD = new Map([[String.fromCharCode(103,111,108,100,0),209], [String.fromCharCode(102,108,101,120,0),806], [String.fromCharCode(102,102,116,115,95,113,95,57,49,0),894]]);
          let disconnectedH = 2.0;
         canvasF = [1 >> (Math.min(Math.abs(parseInt(`${internetL}`)), 4))];
         overlayD[`${disconnectedH}`] = parseInt(`${disconnectedH}`) % 3;
          let splashW = String.fromCharCode(106,111,98,115,0);
         canvasF = [2 * parseInt(`${internetL}`)];
         splashW = `${3 % (Math.max(10, splashW.length))}`;
       let serviceQ = 4.0;
      for (let b = 0; b < 2; b++) {
         download8 += "1";
      }
         serviceQ -= parseInt(`${internetL}`) << (Math.min(download8.length, 3));
          let basketballV = 3;
          let malaysiaw = 2;
         selection2 += `${download8.length}`;
         basketballV <<= Math.min(Math.abs(malaysiaw), 2);
         malaysiaw >>= Math.min(3, Math.abs(1 * malaysiaw));
         volumez += `${canvasF.length}`;
      typesL *= parseFloat(`${infoj - 3}`);
   }
        if (incident.type === 1 && incident.player.id === playerId) return true;
      const_30H += `${taiwan5 ^ infoj}`;

      }).length;
   for (let b = 0; b < 2; b++) {
       let areaa = String.fromCharCode(115,105,103,110,0);
          let turnG = [String.fromCharCode(115,105,122,101,115,0), String.fromCharCode(112,117,98,108,105,115,104,101,100,95,105,95,49,50,0), String.fromCharCode(110,95,55,51,95,114,101,115,101,116,115,0)];
          let previewM = new Map([[String.fromCharCode(109,101,97,115,117,114,101,114,95,116,95,54,0),392], [String.fromCharCode(105,115,112,97,99,107,101,100,0),702]]);
          let nalyticsv = 5.0;
         areaa = `${turnG.length >> (Math.min(3, Math.abs(previewM.size)))}`;
         turnG.push(1 % (Math.max(9, parseInt(`${nalyticsv}`))));
         previewM = new Map([[`${nalyticsv}`, parseInt(`${nalyticsv}`) % (Math.max(7, parseInt(`${nalyticsv}`)))]]);
      while (areaa != String.fromCharCode(76,0) || areaa.length >= 5) {
         areaa = `${areaa.length}`;
         break;
      }
      while (1 > areaa.length) {
         areaa = `${areaa.length + areaa.length}`;
         break;
      }
      contextr = [overlayF.length];
   }

    } catch (error) {

      math9 *= parseFloat(`${3}`);
      totalGoal = 0;
       let main_g6 = 5.0;
      while ((main_g6 + main_g6) >= 1.56 || (1.56 - main_g6) >= 4.62) {
         main_g6 /= Math.max(3, parseInt(`${main_g6}`) << (Math.min(4, Math.abs(2))));
         break;
      }
      do {
         main_g6 /= Math.max(1, parseInt(`${main_g6}`));
         if (3468254.0 == main_g6) {
            break;
         }
      } while ((3468254.0 == main_g6) && ((main_g6 / 4.52) <= 2.38 || 5.75 <= (main_g6 * 4.52)));
          let upgradeA = String.fromCharCode(99,111,108,108,97,112,115,101,0);
          let bootsplash9 = 5.0;
         main_g6 += upgradeA.length;
         upgradeA += `${parseInt(`${bootsplash9}`)}`;
      service6 = [contextr.length - 3];

    }

    incidents.filter(incident => {

       let singlej = 4.0;
       let configurea = new Map([[String.fromCharCode(97,112,109,116,101,115,116,0),778], [String.fromCharCode(101,95,57,50,95,100,101,99,111,100,97,98,108,101,0),584]]);
      while (5.11 > (singlej * 1.44) || (singlej * 1.44) > 5.50) {
         configurea[`${singlej}`] = 2 / (Math.max(10, configurea.size));
         break;
      }
          let analyticP = String.fromCharCode(115,97,109,112,108,105,110,103,0);
         configurea = new Map([[`${configurea.size}`, 1 ^ parseInt(`${singlej}`)]]);
         analyticP = `${(analyticP == String.fromCharCode(79,0) ? analyticP.length : analyticP.length)}`;
      if (2.65 <= (singlej / 3.87) || (singlej / 3.87) <= 3.65) {
          let notificatione = String.fromCharCode(116,111,115,115,95,102,95,57,56,0);
          let project6 = String.fromCharCode(109,97,115,115,0);
         singlej /= Math.max(3, 5);
         notificatione += `${project6.length & notificatione.length}`;
         project6 += `${project6.length >> (Math.min(3, notificatione.length))}`;
      }
      for (let w = 0; w < 1; w++) {
         singlej /= Math.max(1 % (Math.max(4, configurea.size)), 5);
      }
      for (let i = 0; i < 2; i++) {
         singlej *= 1;
      }
      for (let p = 0; p < 1; p++) {
         configurea = new Map([[`${configurea.size}`, parseInt(`${singlej}`)]]);
      }
      report7 = service6.includes(typesL);
      if (incident.type === 1 && incident.player.id === playerId) return true;
   for (let r = 0; r < 2; r++) {
      math9 += parseFloat(`${const_30H.length}`);
   }

    }).length;
   if (parseFloat(`${service6.length}`) >= typesL) {
      service6.push((const_30H == String.fromCharCode(70,0) ? taiwan5 : const_30H.length));
   }


    
    if (totalOwnGoal > 0 && type === 17) {
      return (
        <View style={styles.ownGoalContainer}>
          <Image
            style={styles.imageIncident}
            source={OwnGoal}
            resizeMode="stretch"></Image>
          {totalOwnGoal > 1 && (
            <View style={styles.assistCountContainer}>
              <Text style={styles.textNumberOfCount}>{totalOwnGoal}</Text>
            </View>
          )}
        </View>
      );
    }
    
    if (totalPenatlyGoal > 0 && type === 8) {
      return (
        <View style={styles.ballContainer}>
          <Image
            style={styles.imageIncident}
            source={PenaltyGoal}
            resizeMode="stretch"></Image>
          {totalPenatlyGoal > 1 && (
            <View style={styles.ballCountContainer}>
              <Text style={styles.textNumberOfCount}>{totalPenatlyGoal}</Text>
            </View>
          )}
        </View>
      );
    }
    
    if (totalGoal > 0 && type === 1) {
      return (
        <View style={styles.ballContainer}>
          <Image
            style={styles.imageIncident}
            source={Goal}
            resizeMode="stretch"></Image>
          {totalGoal > 1 && (
            <View style={styles.ballCountContainer}>
              <Text style={styles.textNumberOfCount}>{totalGoal}</Text>
            </View>
          )}
        </View>
      );
    }
  };

  
  const assistComponent = (incidents, playerId) => {
    let totalAssist = incidents.filter(incident => {
      if (
        incident?.type === 1 &&
        (incident?.assist1?.id === playerId ||
          incident?.assist2?.id === playerId)
      )
        return true;
    }).length;
    if (totalAssist > 0) {
      return (
        <View style={styles.assistContainer}>
          <Image
            style={styles.imageIncident}
            source={Assist}
            resizeMode="stretch"></Image>
          {totalAssist > 1 && (
            <View style={styles.assistCountContainer}>
              <Text style={styles.textNumberOfCount}>{totalAssist}</Text>
            </View>
          )}
        </View>
      );
    }
  };

  const topLeft = () => {
    return (
      <View style={styles.topLeftContainer}>
        {incidents && assistComponent(incidents, player?.id)}
      </View>
    );
  };

  const topRight = () => {
    return (
      <View style={styles.topRightContainer}>
        {incidents && subComponent(incidents, player?.id)}
      </View>
    );
  };

  const bottomLeft = () => {
    return (
      <View style={styles.bottomLeftContainer}>
        <View style={styles.bottomLeftFlex}>
          {incidents && goalComponent(incidents, 17)}
          {incidents && cardComponent(incidents)}
        </View>
      </View>
    );
  };

  const bottomRight = () => {
    return (
      <View style={styles.bottomRightContainer}>
        <View style={styles.bottomRightFlex}>
          {incidents && goalComponent(incidents, 1, player.id)}
          {incidents && goalComponent(incidents, 8)}
        </View>
      </View>
    );
  };

  return (
    <View key={team + player.id} style={styles.player}>
      <View style={styles.imageContainer}>
        {player?.logo ? (
          <Image
            style={styles.imagePlayer}
            source={{
              uri: player?.logo
                ? player?.logo
                : team === 'home'
                ? HomePlayer
                : AwayPlayer,
            }}></Image>
        ) : (
          <ImageBackground
            style={[
              styles.imagePlayer,
              {justifyContent: 'center', alignContent: 'center'},
            ]}
            
            source={imgSource}
            resizeMode="center">
            <Text
              style={{
                textAlign: 'center',
                fontFamily: 'PingFang SC',
                fontStyle: 'normal',
                fontWeight: '600',
                fontSize: 6,
                color: '#F4F4F4',
              }}>
              {player?.shirt_number}
            </Text>
          </ImageBackground>
        )}
        {topLeft()}
        {topRight()}
        {bottomLeft()}
        {bottomRight()}
      </View>
      <Text style={styles.textPlayerNumber}>{player?.shirt_number}</Text>
      <Text style={styles.textPlayerName} numberOfLines={1}>
        {player?.name}
      </Text>
      <View style={[styles.playerRate, rateColor(player?.rating)]}>
        <Text style={[styles.textPlayerRate]}>{round(player?.rating, 1)}</Text>
      </View>
    </View>
  );
};

export default Player;
