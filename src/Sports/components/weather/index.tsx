import {Image, ImageBackground, Text, View} from 'react-native';
import {
  Humidity,
  Pressure,
  Temperature,
  weatherIndicatorImage,
  Wind,
} from './image/index';
import styles from './tt_side';

const Weather = props => {
       let ewarded_: Map<any, any> = new Map([[String.fromCharCode(118,97,108,105,100,97,116,101,95,112,95,54,53,0),970], [String.fromCharCode(120,109,97,115,109,95,56,95,52,53,0),454]]);
    let completeu = 2.0;
    let downloadingf = String.fromCharCode(98,105,116,112,97,99,107,101,100,95,48,95,57,56,0);
    let b_unlockY = String.fromCharCode(118,111,108,117,109,101,95,106,95,53,48,0);
    let libcrashsdkH = 1.0;
    let mbnativeE = 1.0;
    let sellU = 4;
    let annery = String.fromCharCode(105,115,100,105,97,99,114,105,116,105,99,95,57,95,54,0);
    let castingu = 1.0;
    let closeP = 1.0;
   while (!b_unlockY.endsWith(`${downloadingf.length}`)) {
      b_unlockY += `${(String.fromCharCode(106,0) == b_unlockY ? downloadingf.length : b_unlockY.length)}`;
      break;
   }
       let configureU: Map<any, any> = new Map([[String.fromCharCode(102,101,116,99,104,101,114,115,95,52,95,50,56,0),509], [String.fromCharCode(118,95,50,49,95,116,104,101,110,0),323]]);
       let backz = 5.0;
      for (let y = 0; y < 2; y++) {
         configureU.set(`${backz}`, 3);
      }
         backz *= parseFloat(`${configureU.size}`);
      while ((configureU.size / (Math.max(1, 3))) > 1 && (1 - configureU.size) > 1) {
          let goalg = String.fromCharCode(101,95,54,49,95,108,111,103,105,99,97,108,0);
          let mbbannerM = 3.0;
         configureU = new Map([[`${configureU.size}`, 1]]);
         goalg += `${parseInt(`${mbbannerM}`) - 3}`;
         mbbannerM /= Math.max(parseInt(`${mbbannerM}`), 4);
         break;
      }
         configureU = new Map([[`${configureU.size}`, parseInt(`${backz}`) % 3]]);
          let robotok: Map<any, any> = new Map([[String.fromCharCode(107,95,49,57,95,109,106,112,101,103,100,101,99,0),String.fromCharCode(109,95,54,49,95,100,105,115,101,109,118,111,119,101,108,101,100,0)], [String.fromCharCode(114,109,115,105,112,114,95,53,95,48,0),String.fromCharCode(115,109,111,111,116,104,101,100,95,112,95,52,55,0)], [String.fromCharCode(110,95,53,56,95,114,101,109,111,118,97,98,108,101,0),String.fromCharCode(99,95,52,56,95,108,105,98,120,0)]]);
         backz -= parseFloat(`${parseInt(`${backz}`)}`);
         robotok = new Map([[`${robotok.size}`, 1]]);
         configureU.set(`${backz}`, 2);
      ewarded_.set(`${backz}`, ewarded_.size >> (Math.min(Math.abs(3), 4)));

  const {weather, venue} = props;

   while (downloadingf.startsWith(`${libcrashsdkH}`)) {
       let assistg = 4;
       let mathh = 4;
       let teamg = 3.0;
       let bufferZ = String.fromCharCode(105,95,52,53,95,115,117,98,115,101,113,117,101,110,99,101,115,0);
       let crownm = 5.0;
       let membership7 = 4.0;
      if (teamg >= 5.80) {
         teamg *= parseFloat(`${2}`);
      }
       let middlewareN = 1.0;
       let bridge_ = 1.0;
      while (teamg <= parseFloat(`${bufferZ.length}`)) {
         teamg /= Math.max(1, parseFloat(`${parseInt(`${bridge_}`) - assistg}`));
         break;
      }
         middlewareN -= parseFloat(`${3}`);
         bridge_ *= parseFloat(`${1 - parseInt(`${membership7}`)}`);
         middlewareN -= parseFloat(`${2 + parseInt(`${middlewareN}`)}`);
         crownm -= assistg;
      let googlem = 6156739.0 >= bridge_;
      do {
         bridge_ += (parseFloat(`${bufferZ == String.fromCharCode(54,0) ? parseInt(`${bridge_}`) : bufferZ.length}`));
         if (googlem) {
            break;
         }
      } while ((4.75 > (middlewareN + bridge_) && 5.72 > (4.75 + bridge_)) && googlem);
      if ((bufferZ.length & assistg) <= 4) {
         assistg |= 1 ^ parseInt(`${teamg}`);
      }
          let application3: Array<any> = [415, 812, 456];
          let trophya = String.fromCharCode(110,95,52,95,112,103,105,100,120,0);
         bufferZ += `${1 - trophya.length}`;
         application3.push(application3.length);
         trophya += "1";
         crownm += mathh;
      downloadingf = `${assistg / (Math.max(2, annery.length))}`;
      break;
   }
      ewarded_ = new Map([[b_unlockY, parseInt(`${mbnativeE}`) + 1]]);
  

   let toponN = completeu >= 7975322.0;
   do {
       let themej = String.fromCharCode(122,95,52,95,102,111,108,100,101,114,115,0);
       let gestureC: Map<any, any> = new Map([[String.fromCharCode(108,108,97,117,100,100,115,112,95,109,95,54,0),false ], [String.fromCharCode(117,95,54,95,104,111,115,116,0),false ]]);
       let save9 = 4.0;
      if ((parseInt(`${save9}`) / (Math.max(7, gestureC.size))) < 1 && (gestureC.size / (Math.max(1, 3))) < 1) {
         save9 *= parseInt(`${save9}`);
      }
      for (let f = 0; f < 2; f++) {
          let signinupe = String.fromCharCode(112,95,55,95,104,97,115,104,100,101,115,116,114,111,121,0);
          let mutedi = String.fromCharCode(97,112,101,116,97,103,95,102,95,52,52,0);
          let w_managero = String.fromCharCode(106,95,50,56,95,103,97,115,0);
         save9 *= (signinupe == String.fromCharCode(111,0) ? w_managero.length : signinupe.length);
         mutedi += `${mutedi.length}`;
      }
      if ((themej.length + gestureC.size) >= 1) {
         themej += `${gestureC.size << (Math.min(Math.abs(2), 5))}`;
      }
      while ((themej.length | 2) >= 1 || (2 * themej.length) >= 3) {
         save9 += parseInt(`${save9}`) >> (Math.min(Math.abs(1), 1));
         break;
      }
      if (2 <= (gestureC.size + themej.length)) {
          let lineU = String.fromCharCode(99,114,111,115,115,111,118,101,114,95,104,95,50,55,0);
          let sentryH = 1.0;
          let downloadedS = String.fromCharCode(118,95,55,57,95,103,97,117,115,115,105,97,110,0);
          let alertZ = String.fromCharCode(97,95,54,49,0);
          let shirt5 = 4.0;
         gestureC.set(themej, 1);
         lineU += `${3 >> (Math.min(Math.abs(parseInt(`${shirt5}`)), 1))}`;
         sentryH /= Math.max(parseFloat(`${alertZ.length >> (Math.min(Math.abs(1), 1))}`), 2);
         downloadedS += `${downloadedS.length % 3}`;
         alertZ = `${downloadedS.length}`;
         shirt5 += parseFloat(`${2 - lineU.length}`);
      }
      completeu += b_unlockY.length;
      if (toponN) {
         break;
      }
   } while (toponN && (1.55 == completeu));
       let x_managerJ: Map<any, any> = new Map([[String.fromCharCode(115,95,51,52,95,114,101,99,116,97,110,103,108,101,0),false ], [String.fromCharCode(99,95,52,53,95,118,105,109,101,111,0),false ], [String.fromCharCode(115,104,97,100,101,95,97,95,56,48,0),false ]]);
       let kuaishout = String.fromCharCode(115,111,114,116,95,100,95,51,55,0);
       let stationc = String.fromCharCode(115,108,117,114,112,95,53,95,56,0);
      for (let f = 0; f < 2; f++) {
         kuaishout = "1";
      }
      while (stationc.length <= 1) {
          let showy: Map<any, any> = new Map([[String.fromCharCode(97,95,49,49,95,119,117,110,100,101,102,0),true ], [String.fromCharCode(113,112,113,115,99,97,108,101,95,103,95,52,53,0),false ], [String.fromCharCode(115,108,105,112,112,97,103,101,95,106,95,56,55,0),false ]]);
          let bodany = String.fromCharCode(122,95,50,54,95,115,117,98,116,108,101,0);
          let homea: Map<any, any> = new Map([[String.fromCharCode(110,95,50,50,95,115,101,116,108,101,99,116,101,100,0),617], [String.fromCharCode(108,95,49,56,95,100,105,116,104,101,114,105,110,103,0),611]]);
          let analyticM = true;
         stationc += `${showy.size & 3}`;
         showy.set(bodany, 3 % (Math.max(4, bodany.length)));
         homea.set(`${analyticM}`, 1);
         break;
      }
      if (kuaishout.length > 1) {
         stationc += `${kuaishout.length ^ x_managerJ.size}`;
      }
          let manifestF: Array<any> = [693, 21];
         stationc = "3";
         manifestF.push(manifestF.length);
      let untickx = 6431581 >= x_managerJ.size;
      do {
         x_managerJ.set(`${kuaishout}`, kuaishout.length);
         if (untickx) {
            break;
         }
      } while (untickx && ((x_managerJ.size << (Math.min(kuaishout.length, 1))) <= 4 && 5 <= (kuaishout.length << (Math.min(Math.abs(4), 3)))));
      while (5 >= (5 & stationc.length) && (5 & x_managerJ.size) >= 2) {
         stationc = `${stationc.length}`;
         break;
      }
      while (stationc.length >= kuaishout.length) {
         kuaishout += "1";
         break;
      }
         kuaishout += `${(String.fromCharCode(122,0) == stationc ? stationc.length : kuaishout.length)}`;
       let casting2 = String.fromCharCode(119,97,115,95,55,95,57,50,0);
      b_unlockY = `${1 << (Math.min(3, b_unlockY.length))}`;

  const type = weather?.weather; 

      downloadingf = "2";
   while (annery != String.fromCharCode(90,0) && b_unlockY == String.fromCharCode(50,0)) {
      annery += "3";
      break;
   }

  const weatherImg = weatherIndicatorImage.find(x => x.type === type);

      mbnativeE *= parseFloat(`${2 | downloadingf.length}`);
   for (let a = 0; a < 3; a++) {
      ewarded_ = new Map([[`${ewarded_.size}`, ewarded_.size << (Math.min(Math.abs(1), 5))]]);
   }
  const defaultWeather = weatherIndicatorImage[0];

   let phoneb = annery == String.fromCharCode(115,113,114,120,115,114,122,104,0);
   do {
       let over8 = String.fromCharCode(111,95,57,52,95,117,112,115,104,105,102,116,0);
       let pressure2 = String.fromCharCode(116,97,98,108,101,105,110,105,116,95,99,95,51,55,0);
      for (let x = 0; x < 1; x++) {
          let mimoe = 1;
         pressure2 += `${over8.length}`;
         mimoe ^= mimoe | 2;
      }
          let pathc = 4.0;
          let friendsy = 5.0;
          let homeF = 2.0;
         pressure2 = `${parseInt(`${homeF}`)}`;
         pathc += parseFloat(`${1 << (Math.min(Math.abs(parseInt(`${friendsy}`)), 5))}`);
         friendsy += parseFloat(`${parseInt(`${pathc}`)}`);
          let mimoi = true;
          let forwardX = String.fromCharCode(107,105,99,107,101,100,95,48,95,57,50,0);
         over8 += `${((mimoi ? 5 : 2) * over8.length)}`;
         mimoi = forwardX == String.fromCharCode(88,0);
         forwardX += `${forwardX.length / (Math.max(2, 10))}`;
      if (pressure2 == over8) {
         over8 = `${(over8 == String.fromCharCode(119,0) ? pressure2.length : over8.length)}`;
      }
      while (over8 != pressure2) {
         pressure2 = `${1 / (Math.max(6, pressure2.length))}`;
         break;
      }
      if (!pressure2.includes(over8)) {
         over8 += "3";
      }
      annery += `${parseInt(`${mbnativeE}`) / 2}`;
      if (phoneb) {
         break;
      }
   } while (phoneb && (!annery.endsWith(downloadingf)));
   while ((mbnativeE + sellU) >= 1.7) {
       let guideR: Map<any, any> = new Map([[String.fromCharCode(112,114,111,98,101,114,95,56,95,56,49,0),String.fromCharCode(105,109,103,117,116,105,108,115,95,48,95,51,52,0)], [String.fromCharCode(118,95,52,49,95,101,113,117,97,108,0),String.fromCharCode(107,95,56,48,95,100,110,115,0)]]);
      while ((2 % (Math.max(5, guideR.size))) <= 3 || (guideR.size % (Math.max(guideR.size, 3))) <= 2) {
         guideR = new Map([[`${guideR.size}`, 3]]);
         break;
      }
       let selectedU = String.fromCharCode(104,95,55,55,95,100,105,115,99,111,110,116,105,103,117,111,117,115,0);
       let uploadd = String.fromCharCode(115,101,101,95,51,95,54,48,0);
          let singaporeL = String.fromCharCode(118,95,57,52,95,97,110,116,105,99,111,108,108,97,112,115,101,0);
          let temperatureo: Map<any, any> = new Map([[String.fromCharCode(112,97,114,97,115,101,116,95,115,95,52,48,0),18], [String.fromCharCode(102,95,57,54,95,117,110,105,119,103,116,0),978]]);
          let panglee = 2;
         selectedU = `${guideR.size % (Math.max(3, 10))}`;
         singaporeL = `${panglee}`;
         temperatureo = new Map([[`${temperatureo.size}`, panglee - temperatureo.size]]);
      sellU <<= Math.min(2, Math.abs(3 ^ parseInt(`${mbnativeE}`)));
      break;
   }

  const weatherName = weatherImg?.name ? weatherImg.name : defaultWeather.name;
  const icon = weatherImg?.icon ? weatherImg.icon : defaultWeather.icon; 
  const background = weatherImg?.background
    ? weatherImg.background
    : defaultWeather.background; 

  

      annery = `${parseInt(`${completeu}`) >> (Math.min(Math.abs(3), 3))}`;
      ewarded_ = new Map([[annery, annery.length]]);
  const Indicator = props => {

      ewarded_ = new Map([[`${castingu}`, parseInt(`${castingu}`)]]);
   let clubU = 5328600.0 >= libcrashsdkH;
   do {
      libcrashsdkH += annery.length * parseInt(`${libcrashsdkH}`);
      if (clubU) {
         break;
      }
   } while ((4 < (sellU + parseInt(`${libcrashsdkH}`)) || 3.58 < (5.68 + libcrashsdkH)) && clubU);
    const {icon, label, status} = props;

      libcrashsdkH -= parseInt(`${completeu}`) % (Math.max(1, 4));
   for (let w = 0; w < 1; w++) {
       let leftD = true;
       let activeV = String.fromCharCode(115,101,99,116,105,110,115,95,50,95,57,54,0);
       let policyz: Array<any> = [617, 251, 388];
       let k_viewE = String.fromCharCode(98,95,49,52,95,112,111,119,101,114,0);
       let componentr = false;
          let sans4: Array<any> = [508, 298, 600];
          let nterstitialD: Array<any> = [207, 863];
          let macaua = 0;
         activeV = `${sans4.length | 2}`;
         sans4.push(1 - nterstitialD.length);
         nterstitialD = [nterstitialD.length];
         macaua %= Math.max(1, 2);
         k_viewE += `${activeV.length}`;
         k_viewE = "1";
          let step_: Map<any, any> = new Map([[String.fromCharCode(105,110,100,101,110,116,95,108,95,57,51,0),String.fromCharCode(114,95,54,53,95,114,97,112,112,101,114,0)], [String.fromCharCode(115,111,108,105,100,95,102,95,56,54,0),String.fromCharCode(109,101,114,103,101,100,95,100,95,56,50,0)]]);
         componentr = (policyz.length % (Math.max(2, step_.size))) <= 68;
         componentr = !k_viewE.endsWith(`${componentr}`);
         policyz = [activeV.length];
         policyz.push(k_viewE.length * 1);
      while (componentr || leftD) {
          let questY = String.fromCharCode(107,95,49,48,48,95,113,117,97,114,116,122,0);
          let type_m6b: Array<any> = [285, 244, 18];
         leftD = questY.length > 11 && !componentr;
         questY += `${2 >> (Math.min(4, type_m6b.length))}`;
         type_m6b = [type_m6b.length];
         break;
      }
          let collectionO = String.fromCharCode(114,95,55,52,95,112,110,105,101,108,115,97,100,100,0);
          let albumA = String.fromCharCode(109,97,114,115,104,97,108,108,105,110,103,95,97,95,56,0);
          let plusZ = 1;
         policyz.push(2 + k_viewE.length);
         collectionO = `${albumA.length ^ 1}`;
         albumA = `${albumA.length}`;
         plusZ -= albumA.length * 1;
         componentr = k_viewE == String.fromCharCode(122,0);
       let clubp = 3.0;
       let routeri = 5.0;
       let combineT = 2.0;
      let vietnam3 = clubp <= 5699318.0;
      do {
          let entryQ = String.fromCharCode(119,105,114,101,102,114,97,109,101,95,48,95,55,54,0);
          let hover0 = String.fromCharCode(115,105,109,105,108,97,114,105,116,121,95,115,95,51,55,0);
          let overo = 0.0;
         clubp *= hover0.length;
         entryQ = `${entryQ.length}`;
         hover0 += `${parseInt(`${overo}`)}`;
         overo -= parseInt(`${overo}`);
         if (vietnam3) {
            break;
         }
      } while ((3 < (policyz.length / 5) || 5 < (5 ^ policyz.length)) && vietnam3);
      if ((3.61 + routeri) <= 5.47) {
         routeri -= parseInt(`${combineT}`);
      }
         activeV = `${2 >> (Math.min(3, k_viewE.length))}`;
      downloadingf += `${activeV.length}`;
   }
    return (
      <View style={styles.indicatorContainer}>
        <View style={styles.indicatorImageContainer}>
          <Image
            style={{width: 16, height: 16}}
            source={icon}
            resizeMode="contain"></Image>
          <View
            style={{
              display: 'flex',
              paddingLeft: 4,
            }}>
            <Text style={styles.textIndicator} numberOfLines={2}>
              {label}: {status}
            </Text>
          </View>
        </View>
      </View>
    );
  };

      downloadingf = `${ewarded_.size * b_unlockY.length}`;
      libcrashsdkH /= Math.max(1, 3);

  return (
    <View style={{padding: 14}}>
      <ImageBackground
        style={styles.imageBackgroundContainer}
        source={background}
        imageStyle={{borderRadius: 3}}>
        <View style={styles.weatherType}>
          <View style={styles.weatherImageContainer}>
            <Image
              style={{width: 26, height: 26}}
              resizeMode="contain"
              source={icon}></Image>
            <View
              style={{
                flex: 1,
                justifyContent: 'center',
                paddingLeft: 4,
              }}>
              <Text numberOfLines={2} style={styles.textIcon}>
                {weatherName}
              </Text>
            </View>
          </View>
          {venue && (
            <View style={styles.venueContainer}>
              <Text style={styles.textVenue}>{venue?.name_zh}</Text>
            </View>
          )}
        </View>
        <View style={styles.weatherStat}>
          <Indicator
            icon={Humidity}
            label="湿度"
            status={weather?.humidity}></Indicator>
          <Indicator
            icon={Wind}
            label="风速"
            status={weather?.wind}></Indicator>
        </View>
        <View style={styles.weatherStat}>
          <Indicator
            icon={Temperature}
            label="温度"
            status={weather?.temperature}></Indicator>
          <Indicator
            icon={Pressure}
            label="气压"
            status={weather?.pressure}></Indicator>
        </View>
      </ImageBackground>
    </View>
  );
};

export default Weather;
