import {Image, ImageBackground, Text, View} from 'react-native';
import {
  Humidity,
  Pressure,
  Temperature,
  weatherIndicatorImage,
  Wind,
} from './image/index';
import styles from './wawa_redgoal_fill';

const Weather = props => {
       let cornershootF = String.fromCharCode(119,97,108,107,105,110,103,0);
    let upgradee: Array<any> = [83, 1, 894];
    let package_1i = String.fromCharCode(98,103,112,104,99,104,101,99,107,0);
    let iconstarg = 0.0;
    let sharewhite1 = 0.0;
    let yellow9 = false;
    let projectK = 5.0;
    let twitterK: Map<any, any> = new Map([[String.fromCharCode(115,116,105,99,107,101,114,115,0),945], [String.fromCharCode(99,111,100,101,99,115,117,98,98,108,111,99,107,0),510], [String.fromCharCode(115,99,97,108,105,110,103,0),582]]);
    let signinupz = 3.0;
    let circleS = 0.0;
      upgradee.push(twitterK.size);
       let tempP = 1.0;
       let teamW = String.fromCharCode(117,110,98,111,117,110,100,0);
      let hook6 = 8617049.0 <= tempP;
      do {
         tempP /= Math.max(5, teamW.length);
         if (hook6) {
            break;
         }
      } while (hook6 && (2 >= (5 - parseInt(`${tempP}`)) || (tempP - teamW.length) >= 5.35));
       let liveX = String.fromCharCode(109,105,99,0);
         teamW += `${liveX.length ^ 3}`;
      if (teamW.length > liveX.length) {
         liveX += "2";
      }
          let eventsplashU = String.fromCharCode(99,111,111,114,100,115,95,55,95,55,57,0);
         liveX = "1";
         eventsplashU = `${eventsplashU.length * eventsplashU.length}`;
      let countdownZ = String.fromCharCode(95,102,98,114,0) == teamW;
      do {
         teamW += `${parseInt(`${tempP}`) * liveX.length}`;
         if (countdownZ) {
            break;
         }
      } while ((2.1 == (tempP * teamW.length)) && countdownZ);
      twitterK = new Map([[teamW, 3]]);

  const {weather, venue} = props;

   for (let i = 0; i < 3; i++) {
       let expiredW = 0.0;
      while ((expiredW - 5.43) >= 4.8) {
         expiredW /= Math.max(parseFloat(`${2}`), 2);
         break;
      }
      let tail_ = 8292593.0 >= expiredW;
      do {
         expiredW /= Math.max(4, parseFloat(`${parseInt(`${expiredW}`) / 3}`));
         if (tail_) {
            break;
         }
      } while (tail_ && (4.42 > (expiredW - expiredW)));
         expiredW += parseFloat(`${parseInt(`${expiredW}`)}`);
      projectK *= parseFloat(`${parseInt(`${sharewhite1}`)}`);
   }
      cornershootF += `${parseInt(`${signinupz}`) >> (Math.min(3, Math.abs(2)))}`;
  

   while (yellow9) {
       let switch_fk = String.fromCharCode(102,99,102,115,0);
       let arrowselectdowna = String.fromCharCode(104,108,115,112,108,97,121,108,105,115,116,0);
         switch_fk = `${switch_fk.length + arrowselectdowna.length}`;
         switch_fk += `${arrowselectdowna.length}`;
      while (arrowselectdowna.length == switch_fk.length) {
         switch_fk += `${arrowselectdowna.length % 3}`;
         break;
      }
          let splashE: Array<any> = [271, 775];
         switch_fk = `${switch_fk.length}`;
         splashE.push(2 | splashE.length);
      for (let w = 0; w < 1; w++) {
         arrowselectdowna += `${arrowselectdowna.length ^ 3}`;
      }
      let profilepich = 7409016 >= arrowselectdowna.length;
      do {
         arrowselectdowna += "2";
         if (profilepich) {
            break;
         }
      } while ((switch_fk != arrowselectdowna) && profilepich);
      signinupz *= parseFloat(`${parseInt(`${signinupz}`)}`);
      break;
   }
      cornershootF = `${upgradee.length | package_1i.length}`;

  const type = weather?.weather; 

      upgradee = [2];
      upgradee.push(twitterK.size ^ package_1i.length);

  const weatherImg = weatherIndicatorImage.find(x => x.type === type);

       let defaultplayerimgv: Map<any, any> = new Map([[String.fromCharCode(115,117,99,99,101,101,100,0),String.fromCharCode(109,97,120,114,101,97,100,101,114,115,0)], [String.fromCharCode(97,115,115,101,115,115,109,101,110,116,95,54,95,52,56,0),String.fromCharCode(117,109,98,101,114,0)]]);
       let analyticn: Map<any, any> = new Map([[String.fromCharCode(122,95,52,95,97,102,116,101,114,101,102,102,101,99,116,115,107,101,121,112,97,116,104,0),String.fromCharCode(98,115,111,110,0)], [String.fromCharCode(116,114,105,112,108,101,116,0),String.fromCharCode(99,108,111,115,117,114,101,0)], [String.fromCharCode(101,111,109,101,116,114,121,0),String.fromCharCode(112,105,110,107,0)]]);
       let homeicont: Array<any> = [747, 470, 528];
      if ((analyticn.size >> (Math.min(Math.abs(2), 4))) <= 3 || 4 <= (2 >> (Math.min(1, Math.abs(analyticn.size))))) {
         analyticn = new Map([[`${analyticn.size}`, 2]]);
      }
      while (!Array.from(analyticn.keys()).includes(`${homeicont.length}`)) {
          let const_4aI = 0.0;
          let modelsI = 1.0;
          let gmailO = String.fromCharCode(116,101,115,116,98,114,105,100,103,101,0);
          let iconarrowrightblackF = 2.0;
         analyticn.set(`${const_4aI}`, defaultplayerimgv.size);
         const_4aI *= parseFloat(`${1 - gmailO.length}`);
         modelsI /= Math.max((String.fromCharCode(50,0) == gmailO ? gmailO.length : parseInt(`${modelsI}`)), 5);
         iconarrowrightblackF -= parseInt(`${modelsI}`) * 2;
         break;
      }
         analyticn = new Map([[`${analyticn.size}`, analyticn.size % (Math.max(homeicont.length, 1))]]);
         defaultplayerimgv.set(`${homeicont.length}`, 2);
      for (let l = 0; l < 1; l++) {
          let moonT: Array<any> = [String.fromCharCode(116,114,97,110,115,109,105,116,116,101,100,0), String.fromCharCode(103,95,55,55,95,112,108,97,121,112,97,117,115,101,0), String.fromCharCode(116,104,114,101,97,100,115,97,102,101,95,97,95,51,57,0)];
          let placeholderr = 5;
          let bells = String.fromCharCode(97,114,114,97,121,95,48,95,57,56,0);
          let regengx: Map<any, any> = new Map([[String.fromCharCode(120,95,53,50,95,102,114,97,109,101,115,101,116,116,101,114,0),true ], [String.fromCharCode(100,111,118,101,0),true ], [String.fromCharCode(100,101,98,108,111,99,107,0),true ]]);
          let libmapbufferjniE: Map<any, any> = new Map([[String.fromCharCode(103,114,101,97,116,101,115,116,0),true ], [String.fromCharCode(98,95,50,53,95,113,117,101,115,116,105,111,110,0),false ]]);
         analyticn = new Map([[`${analyticn.size}`, 3]]);
         moonT.push(placeholderr);
         placeholderr += 1;
         bells = `${bells.length}`;
         regengx = new Map([[`${regengx.size}`, placeholderr | regengx.size]]);
         libmapbufferjniE.set(`${regengx.size}`, regengx.size | 1);
      }
      package_1i += `${(parseInt(`${signinupz}`) % (Math.max(7, (yellow9 ? 5 : 2))))}`;
   let iconadslinky = signinupz >= 6647123.0;
   do {
      signinupz *= (parseFloat(`${(yellow9 ? 2 : 3) % (Math.max(parseInt(`${iconstarg}`), 3))}`));
      if (iconadslinky) {
         break;
      }
   } while (iconadslinky && (3 > (parseInt(`${signinupz}`) - cornershootF.length) || 2 > (parseInt(`${signinupz}`) - 3)));
  const defaultWeather = weatherIndicatorImage[0];

       let playershirtT = String.fromCharCode(115,97,110,115,95,54,95,57,49,0);
       let libcxxcomponentsb = 2.0;
       let privatechatbgb: Array<any> = [122, 885, 51];
      let iconcloseK = libcxxcomponentsb <= 5519845.0;
      do {
         libcxxcomponentsb -= privatechatbgb.length;
         if (iconcloseK) {
            break;
         }
      } while (iconcloseK && (2.72 == libcxxcomponentsb));
      while (1 == privatechatbgb.length) {
         privatechatbgb.push(parseInt(`${libcxxcomponentsb}`));
         break;
      }
          let routero = 2.0;
          let settings9 = true;
         libcxxcomponentsb += 1 * parseInt(`${routero}`);
         routero -= (1 % (Math.max((settings9 ? 3 : 4), 2)));
      for (let d = 0; d < 2; d++) {
          let bellreminderg = 1;
          let awayteamfield8: Array<any> = [String.fromCharCode(110,117,108,108,0), String.fromCharCode(116,114,97,110,115,105,101,110,116,0)];
          let predictionbannerp = 4;
         privatechatbgb.push(parseInt(`${libcxxcomponentsb}`));
         bellreminderg |= bellreminderg - awayteamfield8.length;
         awayteamfield8 = [predictionbannerp ^ 3];
         predictionbannerp >>= Math.min(Math.abs(bellreminderg & predictionbannerp), 4);
      }
      if ((libcxxcomponentsb / (Math.max(1.71, 3))) < 1.75) {
         libcxxcomponentsb /= Math.max(1, privatechatbgb.length * 2);
      }
         libcxxcomponentsb -= privatechatbgb.length;
      while (1 == (4 + playershirtT.length)) {
         libcxxcomponentsb /= Math.max(2, (String.fromCharCode(115,0) == playershirtT ? privatechatbgb.length : playershirtT.length));
         break;
      }
          let backiconj = true;
          let becomef: Map<any, any> = new Map([[String.fromCharCode(117,112,109,105,120,0),String.fromCharCode(114,97,110,107,105,110,103,0)], [String.fromCharCode(115,112,108,105,116,116,105,110,103,95,101,95,53,49,0),String.fromCharCode(98,105,97,115,0)], [String.fromCharCode(107,105,116,0),String.fromCharCode(97,118,97,105,108,97,98,105,108,105,116,121,95,100,95,52,53,0)]]);
         libcxxcomponentsb /= Math.max(4, ((backiconj ? 4 : 4)));
         backiconj = becomef.size < 85 || becomef.size < 85;
      if ((1 << (Math.min(1, privatechatbgb.length))) >= 5 && 3.18 >= (libcxxcomponentsb + 3.18)) {
          let sellV: Map<any, any> = new Map([[String.fromCharCode(115,116,97,116,105,115,116,105,99,115,0),855], [String.fromCharCode(109,117,108,116,105,112,108,101,114,115,0),794], [String.fromCharCode(114,101,99,105,100,0),350]]);
          let sigmobO = 1;
          let utilsw = false;
         privatechatbgb.push(sigmobO / 2);
         sellV = new Map([[`${sellV.size}`, 2]]);
         sigmobO |= (3 >> (Math.min(1, Math.abs((utilsw ? 2 : 3)))));
      }
      projectK -= parseFloat(`${1 & cornershootF.length}`);
   while (1.28 >= (sharewhite1 + iconstarg)) {
      iconstarg *= parseFloat(`${twitterK.size >> (Math.min(4, Math.abs(parseInt(`${signinupz}`))))}`);
      break;
   }

  const weatherName = weatherImg?.name ? weatherImg.name : defaultWeather.name;
  const icon = weatherImg?.icon ? weatherImg.icon : defaultWeather.icon; 
  const background = weatherImg?.background
    ? weatherImg.background
    : defaultWeather.background; 

  

      cornershootF = `${2 - cornershootF.length}`;
   let securityM = 6999271 >= upgradee.length;
   do {
       let containerP = 1.0;
       let mountingJ = 4.0;
       let yellowredcardN = 3.0;
      while ((2 + containerP) == 3.19) {
         mountingJ /= Math.max(1, 1 - parseInt(`${containerP}`));
         break;
      }
      if (4.44 >= (containerP * mountingJ)) {
         mountingJ *= parseInt(`${yellowredcardN}`);
      }
         yellowredcardN *= parseFloat(`${parseInt(`${yellowredcardN}`) - parseInt(`${mountingJ}`)}`);
       let application1: Array<any> = [481, 826];
      let bridgeC = 9240298 <= application1.length;
      do {
         application1 = [parseInt(`${yellowredcardN}`)];
         if (bridgeC) {
            break;
         }
      } while (((yellowredcardN + parseFloat(`${application1.length}`)) < 5.89 && (application1.length / 3) < 2) && bridgeC);
      while ((2 + application1.length) >= 5) {
          let policyo = true;
          let logouserb = 0;
         application1.push(parseInt(`${yellowredcardN}`));
         policyo = !policyo;
         logouserb %= Math.max(logouserb, 4);
         break;
      }
         yellowredcardN += parseFloat(`${parseInt(`${mountingJ}`)}`);
          let mintegralY = 5.0;
          let awayplayer1 = false;
         mountingJ -= parseInt(`${mintegralY}`) % (Math.max(6, application1.length));
         mintegralY += ((awayplayer1 ? 3 : 4));
      for (let h = 0; h < 1; h++) {
         application1 = [parseInt(`${containerP}`)];
      }
      upgradee = [3 - parseInt(`${containerP}`)];
      if (securityM) {
         break;
      }
   } while ((upgradee.length < 2) && securityM);
  const Indicator = props => {

       let hoverk = 4.0;
       let positionfieldY: Map<any, any> = new Map([[String.fromCharCode(99,104,97,114,97,99,116,101,114,0),102], [String.fromCharCode(104,105,103,104,98,105,116,115,95,99,95,51,56,0),820]]);
       let analyticv = false;
         analyticv = null != positionfieldY.get(`${hoverk}`);
          let whistleorangeq: Array<any> = [268, 958];
          let gradleL = String.fromCharCode(110,97,118,105,103,97,116,111,114,0);
         positionfieldY = new Map([[`${positionfieldY.size}`, 2]]);
         whistleorangeq = [whistleorangeq.length];
         gradleL = `${1 >> (Math.min(5, gradleL.length))}`;
         positionfieldY = new Map([[`${positionfieldY.size}`, positionfieldY.size >> (Math.min(1, Math.abs(parseInt(`${hoverk}`))))]]);
         hoverk *= ((analyticv ? 5 : 3) % (Math.max(parseInt(`${hoverk}`), 4)));
      for (let b = 0; b < 1; b++) {
         analyticv = positionfieldY.size > 68 && analyticv;
      }
         analyticv = 63.15 >= hoverk && positionfieldY.size >= 52;
         hoverk /= Math.max(1, positionfieldY.size / 3);
          let foundp = String.fromCharCode(112,99,109,98,0);
         hoverk += ((analyticv ? 1 : 3) | parseInt(`${hoverk}`));
         foundp += `${foundp.length}`;
      if (1 <= (positionfieldY.size & 3) || analyticv) {
          let subinX = 3.0;
          let utilsr = 0;
          let dragm = String.fromCharCode(97,116,116,105,98,117,116,101,115,0);
         analyticv = positionfieldY.size >= utilsr;
         subinX += 1 - dragm.length;
         utilsr >>= Math.min(3, dragm.length);
      }
      iconstarg += (parseFloat(`${(yellow9 ? 5 : 1) * parseInt(`${iconstarg}`)}`));
       let notificationq = String.fromCharCode(102,112,101,108,0);
          let emojiheartI = String.fromCharCode(99,111,110,115,116,114,97,105,110,101,100,0);
          let whatsappp = 4;
         notificationq = `${emojiheartI.length / (Math.max(2, 9))}`;
         emojiheartI += `${whatsappp}`;
         notificationq = "3";
       let iconarrowrightwhiteP = true;
       let dacccfaabfbcbadeebddcabacdffdbF = true;
      sharewhite1 /= Math.max(1 | notificationq.length, 1);
    const {icon, label, status} = props;

       let sentryo = false;
       let benefitt: Array<any> = [825, 386, 499];
       let annerq: Map<any, any> = new Map([[String.fromCharCode(115,101,110,100,101,114,115,0),895], [String.fromCharCode(98,117,116,116,101,114,0),684], [String.fromCharCode(99,111,109,109,117,110,105,99,97,116,111,114,0),253]]);
         benefitt = [benefitt.length - 1];
      for (let g = 0; g < 2; g++) {
          let unfillG = String.fromCharCode(114,95,54,49,95,97,108,109,111,115,116,0);
          let yellowanimationlivef = String.fromCharCode(99,101,108,112,0);
          let iconnotificationnewL = String.fromCharCode(99,95,50,53,95,115,116,114,105,100,101,115,0);
         benefitt = [((sentryo ? 2 : 5) & yellowanimationlivef.length)];
         unfillG += `${unfillG.length - iconnotificationnewL.length}`;
         yellowanimationlivef = "2";
         iconnotificationnewL += `${unfillG.length % 3}`;
      }
         annerq.set(`${sentryo}`, 3);
      while ((5 - benefitt.length) > 1) {
         benefitt = [benefitt.length];
         break;
      }
      if ((benefitt.length >> (Math.min(Math.abs(3), 3))) >= 4 && 2 >= (benefitt.length >> (Math.min(Math.abs(3), 1)))) {
         benefitt = [3];
      }
      let main_cA = benefitt.length <= 5522550;
      do {
          let iconshare1: Map<any, any> = new Map([[String.fromCharCode(115,95,51,95,119,97,108,108,112,97,112,101,114,0),String.fromCharCode(107,95,56,56,95,105,103,110,97,108,0)], [String.fromCharCode(114,111,120,121,0),String.fromCharCode(100,101,108,97,121,97,98,108,101,0)], [String.fromCharCode(97,116,116,105,98,117,116,101,0),String.fromCharCode(97,110,103,117,108,97,114,0)]]);
          let confige = 0.0;
         benefitt.push(parseInt(`${confige}`) | 3);
         iconshare1 = new Map([[`${iconshare1.size}`, iconshare1.size]]);
         confige *= parseFloat(`${iconshare1.size + 2}`);
         if (main_cA) {
            break;
         }
      } while (main_cA && ((annerq.size << (Math.min(Math.abs(5), 5))) >= 4 || 2 >= (5 << (Math.min(1, benefitt.length)))));
          let liven = 2.0;
          let pingQ: Array<any> = [323, 157, 597];
         benefitt.push(((sentryo ? 1 : 1) >> (Math.min(Math.abs(2), 4))));
         liven *= parseFloat(`${2}`);
         pingQ = [3 - parseInt(`${liven}`)];
          let iconfeedbackE: Array<any> = [982, 246, 395];
          let constantsz: Array<any> = [688, 380];
         benefitt = [iconfeedbackE.length - 3];
         iconfeedbackE = [constantsz.length ^ 1];
         constantsz = [constantsz.length];
      let codeZ = 5504917 <= annerq.size;
      do {
         annerq.set(`${benefitt.length}`, benefitt.length);
         if (codeZ) {
            break;
         }
      } while ((!sentryo) && codeZ);
      cornershootF += `${1 & upgradee.length}`;
   for (let f = 0; f < 2; f++) {
      sharewhite1 /= Math.max(cornershootF.length & 3, 4);
   }
    return (
      <View style={styles.indicatorContainer}>
        <View style={styles.indicatorImageContainer}>
          <Image
            style={{width: 16, height: 16}}
            source={icon}
            resizeMode="contain"></Image>
          <View
            style={{
              display: 'flex',
              paddingLeft: 4,
            }}>
            <Text style={styles.textIndicator} numberOfLines={2}>
              {label}: {status}
            </Text>
          </View>
        </View>
      </View>
    );
  };

   let libavutil0 = sharewhite1 <= 6464370.0;
   do {
      sharewhite1 += upgradee.length;
      if (libavutil0) {
         break;
      }
   } while (libavutil0 && (4.89 == (sharewhite1 * 1.89)));
   for (let x = 0; x < 3; x++) {
      sharewhite1 *= 1 - parseInt(`${sharewhite1}`);
   }

  return (
    <View style={{padding: 14}}>
      <ImageBackground
        style={styles.imageBackgroundContainer}
        source={background}
        imageStyle={{borderRadius: 3}}>
        <View style={styles.weatherType}>
          <View style={styles.weatherImageContainer}>
            <Image
              style={{width: 26, height: 26}}
              resizeMode="contain"
              source={icon}></Image>
            <View
              style={{
                flex: 1,
                justifyContent: 'center',
                paddingLeft: 4,
              }}>
              <Text numberOfLines={2} style={styles.textIcon}>
                {weatherName}
              </Text>
            </View>
          </View>
          {venue && (
            <View style={styles.venueContainer}>
              <Text style={styles.textVenue}>{venue?.name_zh}</Text>
            </View>
          )}
        </View>
        <View style={styles.weatherStat}>
          <Indicator
            icon={Humidity}
            label="湿度"
            status={weather?.humidity}></Indicator>
          <Indicator
            icon={Wind}
            label="风速"
            status={weather?.wind}></Indicator>
        </View>
        <View style={styles.weatherStat}>
          <Indicator
            icon={Temperature}
            label="温度"
            status={weather?.temperature}></Indicator>
          <Indicator
            icon={Pressure}
            label="气压"
            status={weather?.pressure}></Indicator>
        </View>
      </ImageBackground>
    </View>
  );
};

export default Weather;
