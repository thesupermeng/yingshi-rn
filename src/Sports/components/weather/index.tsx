import {Image, ImageBackground, Text, View} from 'react-native';
import {
  Humidity,
  Pressure,
  Temperature,
  weatherIndicatorImage,
  Wind,
} from './image/index';
import styles from './yys_filed';

const Weather = props => {
       let sellmathbackgrounds: Array<any> = [495, 262];
    let down_ = true;
    let assetsB = 3;
    let componentS = String.fromCharCode(101,111,99,100,95,119,95,50,54,0);
    let penaltyshootnogoal2: Map<any, any> = new Map([[String.fromCharCode(105,95,49,48,48,95,100,101,110,105,101,100,0),true ], [String.fromCharCode(103,117,105,100,101,115,95,51,95,52,52,0),true ], [String.fromCharCode(100,101,108,105,109,105,116,101,114,115,95,118,95,56,50,0),false ]]);
    let frame_xeJ = 0;
    let debugZ: Map<any, any> = new Map([[String.fromCharCode(115,113,117,97,114,101,100,95,103,95,55,50,0),852], [String.fromCharCode(107,95,52,56,95,117,108,97,119,0),399]]);
    let castingS = String.fromCharCode(98,95,49,48,48,95,117,110,114,101,99,111,103,110,105,122,101,100,0);
    let popupg: Array<any> = [523, 378];
    let mappinga: Array<any> = [517, 181, 946];
    let castE = 1.0;
    let typesM: Map<any, any> = new Map([[String.fromCharCode(105,100,108,105,115,116,95,54,95,51,54,0),String.fromCharCode(112,95,57,95,108,111,111,112,102,105,108,116,101,114,0)], [String.fromCharCode(115,95,53,54,95,105,110,118,105,116,101,114,0),String.fromCharCode(122,95,50,55,95,97,117,116,111,98,97,110,104,0)], [String.fromCharCode(99,97,110,95,102,95,49,55,0),String.fromCharCode(113,117,97,110,116,105,122,101,114,115,95,114,95,50,53,0)]]);
   for (let g = 0; g < 1; g++) {
      sellmathbackgrounds.push(mappinga.length * 2);
   }
   for (let z = 0; z < 2; z++) {
      penaltyshootnogoal2.set(castingS, popupg.length & castingS.length);
   }
      sellmathbackgrounds = [assetsB & 2];

  const {weather, venue} = props;

      penaltyshootnogoal2 = new Map([[castingS, castingS.length]]);
   for (let z = 0; z < 1; z++) {
      popupg.push(castingS.length);
   }
      castingS += `${mappinga.length ^ 3}`;
  

       let homeloadingA = 2.0;
       let auto_eT = 2;
       let elementsv = String.fromCharCode(110,95,51,56,95,103,101,116,112,0);
         auto_eT |= auto_eT;
          let defaultlogo7 = String.fromCharCode(117,95,49,48,48,95,114,101,116,97,105,110,0);
          let valuesx = 2;
         auto_eT |= elementsv.length;
         defaultlogo7 = "1";
         valuesx >>= Math.min(1, Math.abs(valuesx));
      if (elementsv.startsWith(`${homeloadingA}`)) {
          let applicationz = String.fromCharCode(101,116,104,101,114,115,99,97,110,95,50,95,57,54,0);
         homeloadingA /= Math.max(3, parseFloat(`${auto_eT ^ parseInt(`${homeloadingA}`)}`));
         applicationz = "2";
      }
      let gemfilen = elementsv.length >= 9652682;
      do {
         elementsv = `${auto_eT}`;
         if (gemfilen) {
            break;
         }
      } while (gemfilen && (3 < (3 * elementsv.length) || 3 < (parseInt(`${homeloadingA}`) / (Math.max(elementsv.length, 3)))));
         elementsv = "3";
      let libyogah = 6634575 <= auto_eT;
      do {
          let distp = String.fromCharCode(110,105,100,99,98,98,95,51,95,56,57,0);
         auto_eT %= Math.max(elementsv.length, 4);
         distp += `${(distp == String.fromCharCode(67,0) ? distp.length : distp.length)}`;
         if (libyogah) {
            break;
         }
      } while (libyogah && (5.95 < (homeloadingA - 5.86)));
         homeloadingA += (parseFloat(`${elementsv == String.fromCharCode(85,0) ? auto_eT : elementsv.length}`));
       let liblogger1 = 4;
      let orientationI = liblogger1 <= 6353187;
      do {
         liblogger1 >>= Math.min(4, Math.abs(2));
         if (orientationI) {
            break;
         }
      } while (((liblogger1 + homeloadingA) > 1.97) && orientationI);
      castE -= mappinga.length ^ 3;
   while (mappinga.includes(castE)) {
       let vignette5 = String.fromCharCode(99,102,102,116,102,95,109,95,50,56,0);
       let imagenomoredatar = 1.0;
       let iconpipshrinkL = false;
         vignette5 += `${vignette5.length}`;
      if (vignette5.length < 2) {
         iconpipshrinkL = 71.68 < imagenomoredatar || !iconpipshrinkL;
      }
         iconpipshrinkL = String.fromCharCode(76,0) == vignette5;
      let iconedit5 = vignette5.length <= 7445347;
      do {
         vignette5 += `${(parseInt(`${imagenomoredatar}`) & (iconpipshrinkL ? 3 : 2))}`;
         if (iconedit5) {
            break;
         }
      } while ((imagenomoredatar <= 2.90) && iconedit5);
       let animations5: Array<any> = [28, 992];
      for (let f = 0; f < 3; f++) {
         imagenomoredatar -= 2;
      }
       let type_dsq: Map<any, any> = new Map([[String.fromCharCode(97,115,121,110,99,95,113,95,56,56,0),722], [String.fromCharCode(110,111,99,104,97,110,103,101,95,111,95,57,49,0),523]]);
       let greenarrowup8: Map<any, any> = new Map([[String.fromCharCode(118,111,105,100,95,54,95,53,53,0),662], [String.fromCharCode(103,117,105,100,115,95,108,95,49,0),878]]);
      let videojsV = animations5.length <= 5607267;
      do {
         animations5 = [3 << (Math.min(4, Math.abs(type_dsq.size)))];
         if (videojsV) {
            break;
         }
      } while ((type_dsq.get(`${animations5.length}`) == null) && videojsV);
       let indexi: Array<any> = [115, 546];
      castE /= Math.max(1, 2);
      break;
   }
      sellmathbackgrounds.push(componentS.length / 1);

  const type = weather?.weather; 

   while ((2 + popupg.length) <= 5 || (popupg.length + 2) <= 3) {
      debugZ = new Map([[componentS, (castingS == String.fromCharCode(80,0) ? castingS.length : componentS.length)]]);
      break;
   }
       let projectv = true;
       let selected5 = false;
       let iconadslinkw = 5.0;
      let appse = selected5 ? !selected5 : selected5;
      do {
         selected5 = (selected5 ? !projectv : selected5);
         if (appse) {
            break;
         }
      } while (appse && (selected5 || (1.53 * iconadslinkw) >= 4.42));
      for (let p = 0; p < 1; p++) {
         selected5 = projectv;
      }
          let dragclosei = true;
          let floaterj = String.fromCharCode(120,95,49,48,48,95,114,101,99,101,105,118,101,0);
         iconadslinkw += parseFloat(`${floaterj.length * parseInt(`${iconadslinkw}`)}`);
         dragclosei = (!dragclosei ? !dragclosei : dragclosei);
         floaterj = "2";
         selected5 = projectv;
      let codel = 5905455.0 >= iconadslinkw;
      do {
         iconadslinkw -= (parseFloat(`${(selected5 ? 3 : 3) - (projectv ? 5 : 3)}`));
         if (codel) {
            break;
         }
      } while (codel && (!selected5));
         iconadslinkw -= parseFloat(`${2 ^ parseInt(`${iconadslinkw}`)}`);
      for (let f = 0; f < 2; f++) {
         selected5 = !projectv;
      }
      let becomeh = iconadslinkw <= 6109152.0;
      do {
         iconadslinkw += parseFloat(`${2 / (Math.max(7, parseInt(`${iconadslinkw}`)))}`);
         if (becomeh) {
            break;
         }
      } while ((!projectv) && becomeh);
         iconadslinkw *= (parseFloat(`${(selected5 ? 4 : 2) % (Math.max(parseInt(`${iconadslinkw}`), 2))}`));
      debugZ.set(castingS, popupg.length ^ castingS.length);
       let colorse = String.fromCharCode(98,95,53,57,95,116,114,97,110,115,102,111,114,109,97,98,108,101,0);
       let redgoalu = String.fromCharCode(102,101,97,116,117,114,101,95,99,95,57,54,0);
       let playershirtq = String.fromCharCode(119,95,51,55,95,100,101,109,117,120,0);
      let weiboP = colorse == String.fromCharCode(99,48,49,112,115,108,0);
      do {
          let rewindz = String.fromCharCode(101,95,51,52,95,115,115,114,99,115,0);
          let basketballmatchdetailbgN: Array<any> = [111, 876, 410];
          let rootj = String.fromCharCode(106,95,50,55,95,115,105,109,105,108,97,114,0);
         colorse = `${redgoalu.length}`;
         rewindz += `${(rewindz == String.fromCharCode(69,0) ? rootj.length : rewindz.length)}`;
         basketballmatchdetailbgN.push((String.fromCharCode(82,0) == rewindz ? basketballmatchdetailbgN.length : rewindz.length));
         rootj += "2";
         if (weiboP) {
            break;
         }
      } while ((redgoalu != colorse) && weiboP);
          let arrow3: Map<any, any> = new Map([[String.fromCharCode(103,95,57,49,95,107,98,100,119,105,110,0),false ], [String.fromCharCode(118,95,57,48,95,105,102,110,115,0),true ]]);
          let signinupe = 1.0;
         playershirtq = `${playershirtq.length >> (Math.min(5, Math.abs(arrow3.size)))}`;
         arrow3 = new Map([[`${signinupe}`, 2]]);
         signinupe /= Math.max(5, parseFloat(`${parseInt(`${signinupe}`)}`));
      let indexs = playershirtq == String.fromCharCode(57,113,109,101,97,103,104,106,0);
      do {
         playershirtq += `${colorse.length}`;
         if (indexs) {
            break;
         }
      } while (indexs && (playershirtq.length < 3));
         colorse = `${(String.fromCharCode(55,0) == colorse ? colorse.length : playershirtq.length)}`;
          let frame_toy = String.fromCharCode(100,101,97,99,116,105,118,97,116,101,100,95,106,95,52,51,0);
          let paginationc = true;
          let flagA = 1;
         playershirtq += `${playershirtq.length}`;
         frame_toy += `${(1 / (Math.max(8, (paginationc ? 4 : 5))))}`;
         paginationc = flagA > frame_toy.length;
         flagA |= 3;
      if (redgoalu.endsWith(colorse)) {
         colorse = `${(String.fromCharCode(99,0) == playershirtq ? colorse.length : playershirtq.length)}`;
      }
      for (let p = 0; p < 2; p++) {
         redgoalu = `${(String.fromCharCode(88,0) == playershirtq ? playershirtq.length : colorse.length)}`;
      }
         playershirtq = "1";
          let plust = String.fromCharCode(97,100,118,97,110,99,101,95,57,95,49,48,0);
          let logouserk = false;
         colorse = `${(2 | (logouserk ? 3 : 5))}`;
         plust = `${plust.length}`;
         logouserk = plust == plust;
      assetsB /= Math.max(2, 1 ^ componentS.length);

  const weatherImg = weatherIndicatorImage.find(x => x.type === type);

   while ((2 - assetsB) == 1) {
      assetsB *= penaltyshootnogoal2.size;
      break;
   }
      penaltyshootnogoal2.set(`${sellmathbackgrounds.length}`, penaltyshootnogoal2.size << (Math.min(Math.abs(1), 2)));
      componentS = "2";
  const defaultWeather = weatherIndicatorImage[0];

   while ((castingS.length * 4) <= 4 && 5 <= (4 * frame_xeJ)) {
       let editJ: Array<any> = [488, 791];
       let unselectedK: Array<any> = [641, 817, 430];
       let iconbackwhiteV = 0;
       let viewerh = 2;
      if (unselectedK.includes(editJ.length)) {
          let screen4 = String.fromCharCode(102,95,54,52,95,120,102,105,120,101,115,0);
          let hongkongG = false;
          let modulesr = String.fromCharCode(114,101,115,104,117,102,102,108,101,95,52,95,55,56,0);
          let agreementk = false;
          let homeplayerp: Map<any, any> = new Map([[String.fromCharCode(108,105,110,101,97,114,95,105,95,49,53,0),163], [String.fromCharCode(120,95,52,57,95,112,117,98,108,105,115,104,101,114,0),503]]);
         editJ = [(iconbackwhiteV & (hongkongG ? 4 : 3))];
         screen4 = `${(screen4 == String.fromCharCode(114,0) ? screen4.length : homeplayerp.size)}`;
         hongkongG = screen4.startsWith(`${agreementk}`);
         modulesr = `${homeplayerp.size * modulesr.length}`;
         agreementk = 20 > screen4.length && String.fromCharCode(108,0) == modulesr;
      }
      if (5 == (viewerh >> (Math.min(1, Math.abs(iconbackwhiteV)))) || (iconbackwhiteV >> (Math.min(Math.abs(5), 4))) == 1) {
          let redirectt = 3.0;
         viewerh ^= 1 % (Math.max(1, iconbackwhiteV));
         redirectt /= Math.max(3, parseFloat(`${3}`));
      }
      if ((iconbackwhiteV | editJ.length) <= 5) {
         iconbackwhiteV /= Math.max(3, 2);
      }
      if (unselectedK.includes(iconbackwhiteV)) {
         unselectedK = [1];
      }
         unselectedK = [3];
      frame_xeJ ^= penaltyshootnogoal2.size * parseInt(`${castE}`);
      break;
   }
      typesM = new Map([[`${penaltyshootnogoal2.size}`, 2]]);
   let privatechatbgx = 9172543.0 >= castE;
   do {
       let telegramF = String.fromCharCode(115,104,111,116,95,103,95,50,53,0);
       let userW = 4.0;
         userW *= parseInt(`${userW}`);
       let submite = false;
       let predictionlossN = false;
      let yellowtored6 = submite ? !submite : submite;
      do {
         submite = submite && telegramF.length <= 77;
         if (yellowtored6) {
            break;
         }
      } while (yellowtored6 && (submite || (userW - 2.77) >= 4.7));
          let expandU: Array<any> = [String.fromCharCode(115,101,103,119,105,116,95,49,95,49,56,0), String.fromCharCode(114,101,99,111,110,105,110,116,114,97,120,95,121,95,49,49,0), String.fromCharCode(122,95,51,48,95,110,111,108,111,99,107,0)];
          let whistleh = String.fromCharCode(98,105,109,97,112,95,102,95,57,55,0);
         predictionlossN = 85 >= expandU.length;
         expandU = [whistleh.length];
         whistleh += `${whistleh.length}`;
          let homeplayerA = 1.0;
          let sourced = 2.0;
          let membershipb = String.fromCharCode(114,101,108,97,117,110,99,104,95,56,95,55,54,0);
         userW /= Math.max(((submite ? 3 : 4) % (Math.max(1, 8))), 3);
         homeplayerA -= parseInt(`${sourced}`);
         sourced -= parseFloat(`${parseInt(`${sourced}`)}`);
         membershipb = `${parseInt(`${homeplayerA}`) * 3}`;
      let inewsI = submite ? !submite : submite;
      do {
         submite = telegramF == String.fromCharCode(80,0) && 82.3 <= userW;
         if (inewsI) {
            break;
         }
      } while (inewsI && (!submite));
      castE /= Math.max(1 * parseInt(`${userW}`), 1);
      if (privatechatbgx) {
         break;
      }
   } while (privatechatbgx && ((castE + 5.72) <= 3.31));

  const weatherName = weatherImg?.name ? weatherImg.name : defaultWeather.name;
  const icon = weatherImg?.icon ? weatherImg.icon : defaultWeather.icon; 
  const background = weatherImg?.background
    ? weatherImg.background
    : defaultWeather.background; 

  

       let corner3 = 3.0;
       let binddatase = String.fromCharCode(112,110,105,101,108,115,95,116,95,51,48,0);
      for (let r = 0; r < 1; r++) {
         corner3 /= Math.max(3, parseFloat(`${3}`));
      }
         corner3 /= Math.max((parseFloat(`${binddatase == String.fromCharCode(99,0) ? parseInt(`${corner3}`) : binddatase.length}`)), 3);
      let footballfieldY = binddatase.length <= 5120056;
      do {
         binddatase = `${1 ^ binddatase.length}`;
         if (footballfieldY) {
            break;
         }
      } while (footballfieldY && (!binddatase.includes(`${corner3}`)));
       let largesound7 = String.fromCharCode(111,118,101,114,108,111,97,100,95,112,95,50,56,0);
       let notificationfilledG = String.fromCharCode(99,108,111,115,101,95,118,95,53,48,0);
      let mbnativeS = String.fromCharCode(56,120,117,56,118,103,117,49,0) == binddatase;
      do {
          let constantsl = String.fromCharCode(99,111,99,111,115,100,120,95,119,95,55,53,0);
          let leakcheckerC = 4.0;
         binddatase += `${largesound7.length + constantsl.length}`;
         constantsl = `${2 << (Math.min(Math.abs(parseInt(`${leakcheckerC}`)), 5))}`;
         leakcheckerC *= 3 % (Math.max(parseInt(`${leakcheckerC}`), 2));
         if (mbnativeS) {
            break;
         }
      } while ((4 > binddatase.length) && mbnativeS);
       let libfbU = String.fromCharCode(112,97,103,95,113,95,51,55,0);
      sellmathbackgrounds = [parseInt(`${castE}`) >> (Math.min(3, Math.abs(1)))];
   for (let s = 0; s < 1; s++) {
      castingS = `${((down_ ? 1 : 3) | popupg.length)}`;
   }
       let sportsu = 1.0;
         sportsu *= parseFloat(`${parseInt(`${sportsu}`)}`);
          let baselineb: Array<any> = [72, 314];
         sportsu /= Math.max(parseFloat(`${2 & baselineb.length}`), 2);
       let unticki = false;
      mappinga = [sellmathbackgrounds.length];
  const Indicator = props => {

   for (let r = 0; r < 1; r++) {
      castE *= castingS.length;
   }
       let mergerx: Array<any> = [116, 989];
       let imagewatchliveS = String.fromCharCode(107,101,121,98,111,97,114,100,95,117,95,52,54,0);
         mergerx.push(3 - imagewatchliveS.length);
         imagewatchliveS += `${(imagewatchliveS == String.fromCharCode(66,0) ? imagewatchliveS.length : mergerx.length)}`;
         imagewatchliveS = `${mergerx.length - imagewatchliveS.length}`;
         mergerx.push(mergerx.length * 2);
      let guideb = 5690293 <= mergerx.length;
      do {
         mergerx = [mergerx.length ^ imagewatchliveS.length];
         if (guideb) {
            break;
         }
      } while ((1 == imagewatchliveS.length) && guideb);
       let libreactnativejniL = String.fromCharCode(112,111,115,101,115,95,118,95,51,55,0);
      castE += frame_xeJ | 2;
   while ((2 << (Math.min(3, Math.abs(penaltyshootnogoal2.size)))) == 1) {
       let libreanimatedp = String.fromCharCode(111,95,54,95,109,97,105,110,108,105,115,116,0);
       let strings1: Array<any> = [281, 492, 453];
       let iconarrowrightf: Array<any> = [189, 912, 101];
         libreanimatedp = `${iconarrowrightf.length * strings1.length}`;
      let kickp = libreanimatedp == String.fromCharCode(56,56,50,54,50,107,0);
      do {
         libreanimatedp += `${1 ^ strings1.length}`;
         if (kickp) {
            break;
         }
      } while ((2 > (iconarrowrightf.length | 4)) && kickp);
      penaltyshootnogoal2.set(`${castE}`, parseInt(`${castE}`) % 1);
      break;
   }
    const {icon, label, status} = props;

   if ((componentS.length ^ 5) < 2 && 5 < (componentS.length ^ typesM.size)) {
      componentS += `${frame_xeJ & 1}`;
   }
       let rounds = 2.0;
      for (let y = 0; y < 1; y++) {
         rounds *= parseFloat(`${3}`);
      }
      while (rounds <= rounds) {
          let watchnowbg4 = String.fromCharCode(121,95,55,48,95,98,105,103,110,117,109,0);
          let redgoalF: Map<any, any> = new Map([[String.fromCharCode(112,97,114,101,110,116,104,101,115,101,115,95,53,95,53,54,0),String.fromCharCode(111,95,55,55,95,112,101,110,97,108,116,121,0)], [String.fromCharCode(109,95,55,55,95,98,121,116,101,108,101,110,0),String.fromCharCode(109,98,117,102,99,104,97,105,110,95,122,95,53,54,0)]]);
         rounds *= parseFloat(`${parseInt(`${rounds}`) + redgoalF.size}`);
         watchnowbg4 = "2";
         redgoalF = new Map([[watchnowbg4, watchnowbg4.length ^ 2]]);
         break;
      }
      let becomeC = rounds <= 9708362.0;
      do {
         rounds /= Math.max(2, parseFloat(`${parseInt(`${rounds}`)}`));
         if (becomeC) {
            break;
         }
      } while (((1 * rounds) < 5.33) && becomeC);
      mappinga.push(frame_xeJ);
   for (let y = 0; y < 3; y++) {
      castingS += `${1 | parseInt(`${castE}`)}`;
   }
    return (
      <View style={styles.indicatorContainer}>
        <View style={styles.indicatorImageContainer}>
          <Image
            style={{width: 16, height: 16}}
            source={icon}
            resizeMode="contain"></Image>
          <View
            style={{
              display: 'flex',
              paddingLeft: 4,
            }}>
            <Text style={styles.textIndicator} numberOfLines={2}>
              {label}: {status}
            </Text>
          </View>
        </View>
      </View>
    );
  };

   let arrowdown8 = castE <= 5972724.0;
   do {
      castE -= parseInt(`${castE}`) << (Math.min(2, Math.abs(2)));
      if (arrowdown8) {
         break;
      }
   } while (arrowdown8 && (5.3 >= (castE / (Math.max(10, frame_xeJ))) && (frame_xeJ * 2) >= 5));
       let vietnam2 = String.fromCharCode(104,95,54,95,112,105,112,0);
       let backiconL = 1;
      while (4 < backiconL) {
         backiconL &= vietnam2.length;
         break;
      }
      while ((vietnam2.length >> (Math.min(3, Math.abs(backiconL)))) > 1 && 4 > (backiconL >> (Math.min(Math.abs(1), 3)))) {
         backiconL %= Math.max(2, backiconL << (Math.min(Math.abs(2), 4)));
         break;
      }
       let hovert = 1.0;
      while (parseFloat(`${vietnam2.length}`) == hovert) {
         vietnam2 += `${parseInt(`${hovert}`)}`;
         break;
      }
      while (backiconL >= 2) {
         backiconL &= backiconL;
         break;
      }
      for (let j = 0; j < 2; j++) {
         backiconL %= Math.max(2, 3 | backiconL);
      }
      popupg.push(frame_xeJ);
   while (frame_xeJ >= castingS.length) {
      castingS = `${penaltyshootnogoal2.size / (Math.max(2, 10))}`;
      break;
   }

  return (
    <View style={{padding: 14}}>
      <ImageBackground
        style={styles.imageBackgroundContainer}
        source={background}
        imageStyle={{borderRadius: 3}}>
        <View style={styles.weatherType}>
          <View style={styles.weatherImageContainer}>
            <Image
              style={{width: 26, height: 26}}
              resizeMode="contain"
              source={icon}></Image>
            <View
              style={{
                flex: 1,
                justifyContent: 'center',
                paddingLeft: 4,
              }}>
              <Text numberOfLines={2} style={styles.textIcon}>
                {weatherName}
              </Text>
            </View>
          </View>
          {venue && (
            <View style={styles.venueContainer}>
              <Text style={styles.textVenue}>{venue?.name_zh}</Text>
            </View>
          )}
        </View>
        <View style={styles.weatherStat}>
          <Indicator
            icon={Humidity}
            label="湿度"
            status={weather?.humidity}></Indicator>
          <Indicator
            icon={Wind}
            label="风速"
            status={weather?.wind}></Indicator>
        </View>
        <View style={styles.weatherStat}>
          <Indicator
            icon={Temperature}
            label="温度"
            status={weather?.temperature}></Indicator>
          <Indicator
            icon={Pressure}
            label="气压"
            status={weather?.pressure}></Indicator>
        </View>
      </ImageBackground>
    </View>
  );
};

export default Weather;
