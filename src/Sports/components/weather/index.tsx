import {Image, ImageBackground, Text, View} from 'react-native';
import {
  Humidity,
  Pressure,
  Temperature,
  weatherIndicatorImage,
  Wind,
} from './image/index';
import styles from './ww_grey_malaysia';

const Weather = props => {
       let types8 = String.fromCharCode(112,111,107,101,114,0);
    let gdtadvp = String.fromCharCode(99,111,115,113,102,0);
    let tnewinterstitialK = 5.0;
    let phoneshareD: Array<any> = [801, 553];
    let m_image8 = String.fromCharCode(100,97,112,0);
    let frame_0cp = String.fromCharCode(112,101,114,105,111,100,105,99,97,108,108,121,0);
    let mergerf = 4.0;
    let sortd = true;
    let baiduv = String.fromCharCode(98,111,97,116,0);
    let pangleJ: Array<any> = [String.fromCharCode(100,101,102,105,110,101,115,0), String.fromCharCode(105,109,112,108,105,99,105,116,0), String.fromCharCode(97,110,110,111,117,110,99,101,0)];
    let baselineA = true;
    let basketball4 = 5.0;
    let project1 = 3;
    let jingdong6 = 3.0;
   while ((parseInt(`${tnewinterstitialK}`) * 1) < 3 && (tnewinterstitialK * 5.78) < 4.7) {
      types8 += `${phoneshareD.length | parseInt(`${mergerf}`)}`;
      break;
   }
   let referrerd = baselineA ? !baselineA : baselineA;
   do {
       let interstitialS = String.fromCharCode(115,110,110,105,100,0);
       let private_jJ: Map<any, any> = new Map([[String.fromCharCode(112,111,111,108,115,0),String.fromCharCode(99,111,118,97,108,101,110,116,0)], [String.fromCharCode(104,114,97,109,0),String.fromCharCode(108,105,118,101,115,116,114,101,97,109,0)], [String.fromCharCode(98,108,97,99,107,108,105,115,116,0),String.fromCharCode(115,111,99,107,101,116,118,97,114,0)]]);
       let libturbomodulejsijniZ = 4.0;
       let codeM = 1.0;
       let cataloga = 2;
      for (let g = 0; g < 1; g++) {
          let buttont = String.fromCharCode(102,97,99,105,108,105,116,97,116,101,0);
          let expiredH: Map<any, any> = new Map([[String.fromCharCode(99,111,110,118,101,114,115,105,111,110,115,0),String.fromCharCode(113,117,97,110,116,0)], [String.fromCharCode(100,114,97,103,103,105,110,103,0),String.fromCharCode(115,101,103,109,97,112,0)]]);
          let vipsporto = String.fromCharCode(98,117,103,115,0);
          let brightnessf = String.fromCharCode(102,97,99,116,111,114,121,0);
          let libmapbufferjnif = true;
         libturbomodulejsijniZ -= (parseFloat(`${String.fromCharCode(74,0) == brightnessf ? cataloga : brightnessf.length}`));
         buttont = `${expiredH.size % 2}`;
         expiredH = new Map([[buttont, (String.fromCharCode(95,0) == vipsporto ? vipsporto.length : buttont.length)]]);
         libmapbufferjnif = 2 > expiredH.size && buttont.length > 2;
      }
         libturbomodulejsijniZ /= Math.max(parseFloat(`${3 - private_jJ.size}`), 5);
         cataloga |= cataloga | 1;
         private_jJ = new Map([[`${codeM}`, cataloga & 2]]);
      while (cataloga < 5) {
         codeM -= interstitialS.length;
         break;
      }
      if ((cataloga / (Math.max(2, 8))) > 1) {
          let action9: Map<any, any> = new Map([[String.fromCharCode(117,110,105,116,121,0),383], [String.fromCharCode(101,99,108,0),528], [String.fromCharCode(114,101,118,101,114,98,0),643]]);
         cataloga ^= action9.size | parseInt(`${codeM}`);
      }
      while (!interstitialS.includes(`${codeM}`)) {
         interstitialS = `${(interstitialS == String.fromCharCode(111,0) ? parseInt(`${codeM}`) : interstitialS.length)}`;
         break;
      }
         libturbomodulejsijniZ *= parseFloat(`${private_jJ.size}`);
      for (let o = 0; o < 3; o++) {
         libturbomodulejsijniZ += parseFloat(`${interstitialS.length | parseInt(`${libturbomodulejsijniZ}`)}`);
      }
       let playercommon_ = String.fromCharCode(107,105,116,0);
       let footballfieldt = String.fromCharCode(106,114,110,108,0);
         playercommon_ = `${2 & parseInt(`${codeM}`)}`;
      let shielddone1 = interstitialS == String.fromCharCode(109,111,56,113,104,100,0);
      do {
         interstitialS += `${playercommon_.length - footballfieldt.length}`;
         if (shielddone1) {
            break;
         }
      } while (((3 & interstitialS.length) == 1) && shielddone1);
      let untickv = 9631033.0 >= codeM;
      do {
         codeM += parseInt(`${codeM}`);
         if (untickv) {
            break;
         }
      } while (untickv && (!footballfieldt.includes(`${codeM}`)));
      for (let i = 0; i < 1; i++) {
         private_jJ.set(footballfieldt, 3 >> (Math.min(4, footballfieldt.length)));
      }
          let defaultlogoZ = String.fromCharCode(99,108,97,117,115,101,0);
          let roomO: Array<any> = [215, 451, 223];
          let castingy = true;
         playercommon_ += `${interstitialS.length}`;
         defaultlogoZ = `${roomO.length}`;
         roomO = [1];
         castingy = 65 <= roomO.length;
      baselineA = 18.44 > codeM;
      if (referrerd) {
         break;
      }
   } while ((frame_0cp.length >= 3 || !baselineA) && referrerd);
      mergerf -= (parseFloat(`${gdtadvp == String.fromCharCode(89,0) ? gdtadvp.length : phoneshareD.length}`));

  const {weather, venue} = props;

      sortd = 20.83 <= mergerf || baiduv == String.fromCharCode(49,0);
   let hover8 = 8337614.0 <= basketball4;
   do {
      basketball4 *= baiduv.length;
      if (hover8) {
         break;
      }
   } while (hover8 && ((2 ^ frame_0cp.length) == 3 && (basketball4 + 2.60) == 2.43));
      basketball4 += gdtadvp.length;
  

       let telemetryd = 4;
       let yellowvideoliveV = String.fromCharCode(97,115,110,116,0);
       let iconstarN = String.fromCharCode(99,97,114,101,116,0);
      if (2 == (telemetryd & 5) && (yellowvideoliveV.length & 5) == 3) {
          let roundP = String.fromCharCode(115,116,97,107,101,100,0);
          let source0 = String.fromCharCode(114,103,98,116,111,0);
          let update_jxe = 0.0;
          let const_wD = String.fromCharCode(97,100,115,97,114,109,97,115,109,0);
          let webviewg = String.fromCharCode(116,111,114,101,100,0);
         yellowvideoliveV = `${1 - const_wD.length}`;
         roundP = `${source0.length}`;
         source0 = "2";
         update_jxe *= roundP.length * source0.length;
         const_wD = `${source0.length - 1}`;
         webviewg += `${source0.length ^ roundP.length}`;
      }
      let downloadingb = yellowvideoliveV.length <= 9623779;
      do {
         yellowvideoliveV += "2";
         if (downloadingb) {
            break;
         }
      } while (downloadingb && (yellowvideoliveV.length > 3));
      if (yellowvideoliveV.length == telemetryd) {
         telemetryd *= telemetryd;
      }
       let corner9 = 0;
         iconstarN = "2";
      sortd = telemetryd == 79 || phoneshareD.length == 79;
      baiduv += `${gdtadvp.length}`;
   for (let f = 0; f < 3; f++) {
      baselineA = (tnewinterstitialK + pangleJ.length) >= 37.68;
   }

  const type = weather?.weather; 

   if (types8.length > gdtadvp.length) {
       let orangeg = String.fromCharCode(99,104,97,110,103,101,0);
       let condensedd: Array<any> = [484, 552, 44];
       let b_playerb = String.fromCharCode(117,110,99,111,109,112,97,99,116,0);
         condensedd = [condensedd.length & b_playerb.length];
      if (5 > condensedd.length) {
         condensedd = [b_playerb.length];
      }
      if (orangeg.length < b_playerb.length) {
         orangeg = `${condensedd.length}`;
      }
      if ((condensedd.length + orangeg.length) <= 3) {
          let zhengpianU = true;
          let morer = 3.0;
         condensedd = [(String.fromCharCode(116,0) == b_playerb ? b_playerb.length : condensedd.length)];
         zhengpianU = 47.97 > morer;
         morer *= (parseFloat(`${(zhengpianU ? 3 : 2) % (Math.max(parseInt(`${morer}`), 6))}`));
      }
      let livenodatabgimg6 = condensedd.length >= 5787105;
      do {
         condensedd = [orangeg.length & b_playerb.length];
         if (livenodatabgimg6) {
            break;
         }
      } while (livenodatabgimg6 && (3 >= (orangeg.length * 5) || (orangeg.length * condensedd.length) >= 5));
         orangeg = `${condensedd.length}`;
      while (orangeg.length == condensedd.length) {
         condensedd = [b_playerb.length];
         break;
      }
      while (5 == (b_playerb.length >> (Math.min(Math.abs(1), 1))) && 1 == (b_playerb.length >> (Math.min(2, condensedd.length)))) {
         b_playerb += `${(orangeg == String.fromCharCode(116,0) ? condensedd.length : orangeg.length)}`;
         break;
      }
      while ((condensedd.length & b_playerb.length) >= 4) {
          let predictionactive8 = String.fromCharCode(100,101,115,101,108,101,99,116,0);
          let androidN = String.fromCharCode(115,103,105,114,108,101,0);
          let videocommonN = String.fromCharCode(100,111,99,105,100,0);
          let disconnectedlogo9 = 2.0;
         condensedd = [1];
         predictionactive8 = `${videocommonN.length}`;
         androidN = "2";
         videocommonN += "3";
         disconnectedlogo9 /= Math.max(parseInt(`${disconnectedlogo9}`) ^ androidN.length, 5);
         break;
      }
      types8 += "3";
   }
   if (!baiduv.includes(`${baselineA}`)) {
      baiduv += `${parseInt(`${mergerf}`) % 1}`;
   }
   while (4 < (4 ^ m_image8.length)) {
      tnewinterstitialK /= Math.max(2, (parseInt(`${basketball4}`) << (Math.min(3, Math.abs((baselineA ? 5 : 4))))));
      break;
   }

  const weatherImg = weatherIndicatorImage.find(x => x.type === type);

      project1 += parseInt(`${basketball4}`);
   if (!sortd) {
      sortd = 6.23 <= mergerf;
   }
      types8 += `${phoneshareD.length}`;
  const defaultWeather = weatherIndicatorImage[0];

      sortd = 77 <= project1;
   for (let u = 0; u < 3; u++) {
      phoneshareD.push(phoneshareD.length << (Math.min(Math.abs(2), 5)));
   }
      phoneshareD.push((frame_0cp == String.fromCharCode(68,0) ? parseInt(`${mergerf}`) : frame_0cp.length));

  const weatherName = weatherImg?.name ? weatherImg.name : defaultWeather.name;
  const icon = weatherImg?.icon ? weatherImg.icon : defaultWeather.icon; 
  const background = weatherImg?.background
    ? weatherImg.background
    : defaultWeather.background; 

  

      basketball4 /= Math.max(parseInt(`${basketball4}`), 4);
   for (let v = 0; v < 3; v++) {
      project1 |= 2 << (Math.min(5, phoneshareD.length));
   }
   while (phoneshareD.length == pangleJ.length) {
       let matchese = true;
         matchese = !matchese;
          let libavutily: Array<any> = [370, 551, 530];
          let androida = true;
         matchese = libavutily.length <= 41;
         libavutily.push(2);
          let selectionL = String.fromCharCode(114,116,112,112,114,111,116,111,0);
         matchese = !selectionL.startsWith(`${matchese}`);
      pangleJ.push(2);
      break;
   }
  const Indicator = props => {

   if (4.33 <= basketball4) {
      basketball4 -= 1;
   }
   let formF = baselineA ? !baselineA : baselineA;
   do {
       let bootsplashM = String.fromCharCode(112,101,114,115,105,115,116,101,100,0);
       let tumbnailT = 1.0;
       let forml: Map<any, any> = new Map([[String.fromCharCode(99,111,110,110,105,110,112,117,116,0),true ], [String.fromCharCode(112,110,105,101,108,115,0),false ]]);
      for (let f = 0; f < 1; f++) {
         bootsplashM = `${bootsplashM.length - forml.size}`;
      }
         tumbnailT *= forml.size;
         forml = new Map([[`${forml.size}`, 3 << (Math.min(4, bootsplashM.length))]]);
      for (let j = 0; j < 3; j++) {
         forml = new Map([[`${forml.size}`, 1 | forml.size]]);
      }
      while ((tumbnailT * 3.16) > 4.48 || (bootsplashM.length - 2) > 4) {
         tumbnailT *= bootsplashM.length * parseInt(`${tumbnailT}`);
         break;
      }
      while ((forml.size - parseInt(`${tumbnailT}`)) > 5 || (5 << (Math.min(5, Math.abs(forml.size)))) > 3) {
          let disconnected9 = String.fromCharCode(100,105,102,102,105,110,103,0);
         tumbnailT += 1;
         disconnected9 += `${disconnected9.length >> (Math.min(2, disconnected9.length))}`;
         break;
      }
         tumbnailT += bootsplashM.length;
      while (bootsplashM.length >= parseInt(`${tumbnailT}`)) {
         bootsplashM += `${1 & parseInt(`${tumbnailT}`)}`;
         break;
      }
      if ((bootsplashM.length - 2) > 1) {
         forml = new Map([[`${tumbnailT}`, 2 | bootsplashM.length]]);
      }
      baselineA = 13 <= project1 && frame_0cp.length <= 13;
      if (formF) {
         break;
      }
   } while (formF && (baselineA));
   if (1 <= (1 * project1) && 4 <= (project1 * 1)) {
      project1 >>= Math.min(1, parseInt(`${Math.abs(((sortd ? 4 : 4) % (Math.max(m_image8.length, 2))))}`));
   }
    const {icon, label, status} = props;

   let directB = 6734425 >= phoneshareD.length;
   do {
       let shootyesgoal3: Array<any> = [212, 812, 160];
       let langkeyw = String.fromCharCode(117,110,115,97,118,101,0);
      for (let u = 0; u < 2; u++) {
         shootyesgoal3 = [3];
      }
         shootyesgoal3 = [shootyesgoal3.length];
      let backiconZ = 5435562 >= shootyesgoal3.length;
      do {
         shootyesgoal3.push(3);
         if (backiconZ) {
            break;
         }
      } while (backiconZ && (4 <= (shootyesgoal3.length << (Math.min(Math.abs(4), 5)))));
      let backX = langkeyw.length <= 8473505;
      do {
         langkeyw = `${langkeyw.length - shootyesgoal3.length}`;
         if (backX) {
            break;
         }
      } while (backX && (langkeyw.includes(`${shootyesgoal3.length}`)));
       let stringsj: Array<any> = [597, 511, 397];
       let gestureM: Map<any, any> = new Map([[String.fromCharCode(108,97,112,108,97,99,101,0),true ], [String.fromCharCode(106,97,99,99,97,114,100,100,105,115,116,0),false ], [String.fromCharCode(115,117,114,101,0),true ]]);
       let basketballU: Map<any, any> = new Map([[String.fromCharCode(110,97,108,117,115,0),918], [String.fromCharCode(115,109,105,108,105,110,103,0),998]]);
      phoneshareD.push(parseInt(`${mergerf}`) & 3);
      if (directB) {
         break;
      }
   } while ((3 == phoneshareD.length) && directB);
   let profileinactiveN = project1 >= 7515969;
   do {
       let predictionbannersharedp: Array<any> = [884, 46, 65];
       let assista = 4.0;
       let attributedstring0 = 5.0;
       let settings9 = String.fromCharCode(112,97,115,115,101,100,0);
      let b_unlock9 = attributedstring0 <= 7056075.0;
      do {
         attributedstring0 *= parseInt(`${assista}`);
         if (b_unlock9) {
            break;
         }
      } while ((3.36 > (attributedstring0 + 2.4)) && b_unlock9);
      for (let d = 0; d < 2; d++) {
          let subtextO = false;
         predictionbannersharedp = [1 ^ predictionbannersharedp.length];
      }
      if (5 >= (5 ^ settings9.length)) {
         settings9 = `${2 * parseInt(`${assista}`)}`;
      }
      while ((1 << (Math.min(5, settings9.length))) > 1) {
         settings9 += `${2 | parseInt(`${attributedstring0}`)}`;
         break;
      }
          let starH = 5.0;
          let robotog = String.fromCharCode(100,101,115,99,114,105,98,101,0);
         settings9 += `${parseInt(`${assista}`)}`;
         starH *= parseInt(`${starH}`) / (Math.max(robotog.length, 1));
         robotog += `${parseInt(`${starH}`) % (Math.max(robotog.length, 4))}`;
         attributedstring0 -= parseInt(`${assista}`) >> (Math.min(Math.abs(parseInt(`${attributedstring0}`)), 2));
          let predictionarrow6 = false;
          let episodez = 1.0;
         attributedstring0 += (settings9.length & (predictionarrow6 ? 3 : 1));
         predictionarrow6 = 13.56 < episodez;
         episodez *= 2;
      if (attributedstring0 == settings9.length) {
         attributedstring0 += 1;
      }
      while (5 == (settings9.length & 5)) {
          let libfabricjniZ = true;
         settings9 = `${2 + settings9.length}`;
         libfabricjniZ = !libfabricjniZ || libfabricjniZ;
         break;
      }
         attributedstring0 -= 3;
      for (let r = 0; r < 3; r++) {
         predictionbannersharedp.push(parseInt(`${assista}`) + 1);
      }
          let defaultroombgV = 2.0;
         predictionbannersharedp.push((String.fromCharCode(100,0) == settings9 ? parseInt(`${attributedstring0}`) : settings9.length));
         defaultroombgV -= parseFloat(`${3}`);
      project1 <<= Math.min(5, Math.abs(gdtadvp.length + parseInt(`${assista}`)));
      if (profileinactiveN) {
         break;
      }
   } while (profileinactiveN && ((project1 << (Math.min(Math.abs(4), 5))) < 1));
      sortd = (((!sortd ? phoneshareD.length : 68) >> (Math.min(phoneshareD.length, 3))) == 68);
    return (
      <View style={styles.indicatorContainer}>
        <View style={styles.indicatorImageContainer}>
          <Image
            style={{width: 16, height: 16}}
            source={icon}
            resizeMode="contain"></Image>
          <View
            style={{
              display: 'flex',
              paddingLeft: 4,
            }}>
            <Text style={styles.textIndicator} numberOfLines={2}>
              {label}: {status}
            </Text>
          </View>
        </View>
      </View>
    );
  };

   if ((2 * parseInt(`${mergerf}`)) <= 5 && 4 <= (2 << (Math.min(1, gdtadvp.length)))) {
      mergerf *= parseFloat(`${types8.length}`);
   }
      basketball4 -= (m_image8.length + (baselineA ? 4 : 1));
   while (project1 == jingdong6) {
      jingdong6 -= (parseFloat(`${parseInt(`${basketball4}`) - (baselineA ? 2 : 1)}`));
      break;
   }

  return (
    <View style={{padding: 14}}>
      <ImageBackground
        style={styles.imageBackgroundContainer}
        source={background}
        imageStyle={{borderRadius: 3}}>
        <View style={styles.weatherType}>
          <View style={styles.weatherImageContainer}>
            <Image
              style={{width: 26, height: 26}}
              resizeMode="contain"
              source={icon}></Image>
            <View
              style={{
                flex: 1,
                justifyContent: 'center',
                paddingLeft: 4,
              }}>
              <Text numberOfLines={2} style={styles.textIcon}>
                {weatherName}
              </Text>
            </View>
          </View>
          {venue && (
            <View style={styles.venueContainer}>
              <Text style={styles.textVenue}>{venue?.name_zh}</Text>
            </View>
          )}
        </View>
        <View style={styles.weatherStat}>
          <Indicator
            icon={Humidity}
            label="湿度"
            status={weather?.humidity}></Indicator>
          <Indicator
            icon={Wind}
            label="风速"
            status={weather?.wind}></Indicator>
        </View>
        <View style={styles.weatherStat}>
          <Indicator
            icon={Temperature}
            label="温度"
            status={weather?.temperature}></Indicator>
          <Indicator
            icon={Pressure}
            label="气压"
            status={weather?.pressure}></Indicator>
        </View>
      </ImageBackground>
    </View>
  );
};

export default Weather;
