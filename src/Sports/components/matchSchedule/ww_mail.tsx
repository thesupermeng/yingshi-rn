import React, { memo, useState } from "react";
import { View, Text, ImageBackground } from "react-native";
import FastImage from "../../../components/common/ww_result";
import { useTheme } from "@react-navigation/native";
import styles from "./ww_utils_orangeclock";
import { IconViewerGif } from "../../assets";
import { TouchableOpacity } from "react-native";
import { HomeIcon, AwayIcon, AnimationLive, VideoLive } from "../../assets";
import {
  calculateScore,
  getMatchStatus,
  liveRoomName,
} from "../../utility/utils";
import store, { wwEighteenShirt } from "@redux/ww_small";
import { useNavigation } from "@react-navigation/native";
import { wwInfo } from "../../types/ww_dice";
import BecomeVipOverlay from "../../../components/modal/ww_animation";
import { useAppSelector } from "@hooks/ww_catagory_neon";
import { showBecomeVip } from "@redux/actions/ww_hash";
import { useDispatch } from "react-redux";
import { screenModel } from "@type/ww_jingdong_libavfilter";
import ww_runtime from "../../../../Umeng/ww_runtime";

interface wwIndexDice {
  matchSche: wwInfo;
  onPress?: () => any;
  followMatchIds?: Array<number>;
  isMatchPage?: boolean;
  setShowBecomeVIPOverlay: any;
  bgDark?: boolean;
  borderFlag?: string;
}

const MatchSchedule = ({
  matchSche,
  onPress = () => {
       let footballu: Array<any> = [String.fromCharCode(101,110,116,114,111,112,121,109,118,95,112,95,51,56,0), String.fromCharCode(103,101,116,99,114,101,100,95,110,95,57,54,0)];
    let bridgeK = 0.0;
    let videojsO: Map<any, any> = new Map([[String.fromCharCode(115,117,115,112,101,110,100,101,100,95,110,95,55,52,0),278], [String.fromCharCode(101,95,55,52,95,122,105,112,102,0),554]]);
    let leakcheckero = 2.0;
    let tempnodatagifV: Array<any> = [347, 569, 862];
    let defaultlogoK = 1.0;
    let trophyb: Array<any> = [220, 622, 312];
    let searchbarJ: Map<any, any> = new Map([[String.fromCharCode(99,97,110,99,101,108,97,98,108,101,95,117,95,50,51,0),664], [String.fromCharCode(121,95,57,95,100,101,115,99,114,0),463], [String.fromCharCode(98,97,99,107,103,114,111,117,110,100,95,104,95,50,56,0),434]]);
    let cornerkicku = String.fromCharCode(117,116,117,114,101,95,57,95,50,52,0);
    let settingsk = 2.0;
   while (3 > (videojsO.size | 3) && 5 > (videojsO.size - 3)) {
      videojsO.set(`${footballu.length}`, tempnodatagifV.length << (Math.min(1, footballu.length)));
      break;
   }
       let pressureQ = 5;
      if (pressureQ >= pressureQ) {
          let settingsK: Array<any> = [866, 40];
         pressureQ |= 1;
         settingsK.push(2 | settingsK.length);
      }
      for (let v = 0; v < 2; v++) {
         pressureQ -= pressureQ - 1;
      }
      let rncorej = 5672888 >= pressureQ;
      do {
         pressureQ %= Math.max(5, pressureQ);
         if (rncorej) {
            break;
         }
      } while (rncorej && ((pressureQ * 5) <= 2));
      searchbarJ.set(`${pressureQ}`, parseInt(`${bridgeK}`) % (Math.max(6, pressureQ)));
   for (let q = 0; q < 1; q++) {
       let coreX = String.fromCharCode(114,101,109,97,105,110,95,57,95,51,57,0);
         coreX = `${coreX.length ^ coreX.length}`;
      while (!coreX.startsWith(coreX)) {
          let a_player_ = 1;
          let b_unlockB = 1.0;
         coreX += `${(coreX == String.fromCharCode(100,0) ? coreX.length : a_player_)}`;
         a_player_ %= Math.max(3, parseInt(`${b_unlockB}`) * 2);
         b_unlockB -= parseFloat(`${parseInt(`${b_unlockB}`) ^ 1}`);
         break;
      }
      let catagory5 = String.fromCharCode(100,115,99,104,116,99,116,112,0) == coreX;
      do {
         coreX += `${coreX.length}`;
         if (catagory5) {
            break;
         }
      } while (catagory5 && (coreX.endsWith(coreX)));
      searchbarJ = new Map([[`${searchbarJ.size}`, 2]]);
   }
   if (footballu.includes(tempnodatagifV.length)) {
      tempnodatagifV = [videojsO.size];
   }
       let productq = 0;
      while (3 >= (1 & productq) || 3 >= (productq & 1)) {
          let iconrefreshM = 0;
          let components7 = false;
          let stationY = 0.0;
         productq >>= Math.min(1, Math.abs(1 & parseInt(`${stationY}`)));
         iconrefreshM |= ((components7 ? 1 : 2) + iconrefreshM);
         components7 = components7 && 78 <= iconrefreshM;
         stationY += iconrefreshM;
         break;
      }
         productq ^= productq;
      while (5 == productq) {
          let libfbjniB = String.fromCharCode(111,102,102,101,114,101,100,95,97,95,55,0);
          let reviewG = String.fromCharCode(105,95,57,95,117,116,116,101,114,97,110,99,101,0);
          let iconwechato = String.fromCharCode(100,105,109,109,101,100,95,57,95,56,57,0);
          let fastA = String.fromCharCode(102,95,50,55,95,99,108,101,97,114,118,105,100,101,111,100,97,116,97,0);
         productq *= reviewG.length << (Math.min(Math.abs(1), 5));
         libfbjniB = `${iconwechato.length}`;
         reviewG = "2";
         iconwechato = `${(libfbjniB == String.fromCharCode(100,0) ? libfbjniB.length : iconwechato.length)}`;
         fastA += `${(String.fromCharCode(101,0) == fastA ? iconwechato.length : fastA.length)}`;
         break;
      }
      footballu.push(2 ^ searchbarJ.size);
      defaultlogoK /= Math.max(parseFloat(`${searchbarJ.size}`), 4);
       let viewsn = String.fromCharCode(105,95,52,49,95,109,97,115,107,101,100,99,108,97,109,112,0);
       let dacccfaabfbcbadeebddcabacdffdb8 = 0.0;
       let watchnowbgA = String.fromCharCode(117,110,115,104,97,114,112,95,104,95,57,0);
      let zoomC = String.fromCharCode(122,112,50,51,116,103,101,114,115,113,0) == watchnowbgA;
      do {
         watchnowbgA = `${2 + parseInt(`${dacccfaabfbcbadeebddcabacdffdb8}`)}`;
         if (zoomC) {
            break;
         }
      } while ((watchnowbgA.length > 3 || viewsn == String.fromCharCode(100,0)) && zoomC);
         watchnowbgA += "1";
      let rootS = watchnowbgA.length >= 8772940;
      do {
         watchnowbgA += "2";
         if (rootS) {
            break;
         }
      } while (((watchnowbgA.length * 2) == 3) && rootS);
       let mbridge2 = 5;
       let awayB = 5;
          let directz = String.fromCharCode(102,105,108,101,115,95,55,95,56,0);
          let inactiveI: Array<any> = [925, 469];
         awayB &= parseInt(`${dacccfaabfbcbadeebddcabacdffdb8}`);
         directz += `${(String.fromCharCode(87,0) == directz ? inactiveI.length : directz.length)}`;
         inactiveI.push(inactiveI.length - directz.length);
      bridgeK /= Math.max(5, searchbarJ.size >> (Math.min(Math.abs(2), 3)));
      bridgeK /= Math.max(footballu.length | tempnodatagifV.length, 2);
      defaultlogoK += parseFloat(`${1 >> (Math.min(Math.abs(parseInt(`${bridgeK}`)), 2))}`);
   if (defaultlogoK <= bridgeK) {
      bridgeK += 2;
   }
      bridgeK /= Math.max(2, parseInt(`${leakcheckero}`));
      trophyb.push(footballu.length);
       let suggestionW: Map<any, any> = new Map([[String.fromCharCode(100,117,109,95,54,95,51,53,0),648], [String.fromCharCode(104,95,54,53,95,108,97,114,103,101,114,0),406]]);
       let ajaxm: Map<any, any> = new Map([[String.fromCharCode(104,101,97,100,95,112,95,53,55,0),String.fromCharCode(97,103,114,101,101,109,101,110,116,95,105,95,54,48,0)], [String.fromCharCode(101,108,97,115,116,105,99,95,108,95,52,0),String.fromCharCode(121,95,50,51,95,112,114,97,112,97,114,101,0)]]);
       let faviconi = String.fromCharCode(122,95,52,52,95,99,104,101,99,107,115,116,114,105,100,101,0);
          let screen5 = String.fromCharCode(109,107,118,119,114,105,116,101,114,95,106,95,55,57,0);
          let disconnectedm = 2;
         ajaxm.set(faviconi, faviconi.length);
         screen5 = `${3 & disconnectedm}`;
         disconnectedm /= Math.max(4, disconnectedm);
      let pressureA = 6982058 >= ajaxm.size;
      do {
         ajaxm = new Map([[`${ajaxm.size}`, 2]]);
         if (pressureA) {
            break;
         }
      } while (pressureA && ((faviconi.length % 3) < 4));
      if ((2 % (Math.max(8, faviconi.length))) == 3) {
          let reducerZ = String.fromCharCode(98,95,55,48,95,97,99,111,109,112,114,101,115,115,111,114,0);
          let with_cef = 0.0;
         faviconi = `${parseInt(`${with_cef}`) % (Math.max(7, ajaxm.size))}`;
         reducerZ += "2";
         with_cef += reducerZ.length ^ 1;
      }
      while (ajaxm.size <= suggestionW.size) {
          let coreh = 0.0;
          let subbasketballplayerk = 1.0;
          let hcopy_t_ = true;
         ajaxm = new Map([[`${subbasketballplayerk}`, 3]]);
         coreh *= parseFloat(`${3 / (Math.max(parseInt(`${coreh}`), 4))}`);
         subbasketballplayerk += ((hcopy_t_ ? 3 : 3) & parseInt(`${coreh}`));
         hcopy_t_ = !hcopy_t_ && 16.36 < coreh;
         break;
      }
      let carouselY = String.fromCharCode(101,121,49,103,111,112,121,108,0) == faviconi;
      do {
         faviconi += `${faviconi.length}`;
         if (carouselY) {
            break;
         }
      } while (carouselY && ((faviconi.length / (Math.max(4, 5))) == 2));
         faviconi += `${3 & faviconi.length}`;
      if (suggestionW.size > faviconi.length) {
         faviconi = "2";
      }
          let orangeq = 4.0;
          let scheduler1 = 2.0;
         ajaxm = new Map([[faviconi, parseInt(`${orangeq}`) << (Math.min(faviconi.length, 2))]]);
         orangeq -= parseFloat(`${parseInt(`${scheduler1}`) >> (Math.min(5, Math.abs(2)))}`);
         scheduler1 -= parseInt(`${scheduler1}`);
         faviconi = `${(faviconi == String.fromCharCode(101,0) ? ajaxm.size : faviconi.length)}`;
      tempnodatagifV = [videojsO.size * 2];
   if (searchbarJ.get(`${defaultlogoK}`) == null) {
      searchbarJ = new Map([[`${trophyb.length}`, trophyb.length]]);
   }
      leakcheckero *= videojsO.size - 1;
   for (let z = 0; z < 1; z++) {
      footballu = [parseInt(`${defaultlogoK}`) >> (Math.min(1, Math.abs(3)))];
   }
   let whitep = tempnodatagifV.length >= 9088060;
   do {
       let gradlec = String.fromCharCode(100,95,52,57,95,109,108,111,99,107,0);
       let mbridge_ = 4;
       let whatsappv = 4.0;
       let malaysiaX = String.fromCharCode(108,95,52,48,95,113,117,97,100,115,0);
       let gifgoalbgb: Array<any> = [88, 112, 202];
          let readf = String.fromCharCode(117,95,51,48,95,110,101,103,97,116,105,118,101,0);
          let module3 = 2.0;
         whatsappv -= parseFloat(`${3}`);
         readf = `${readf.length & parseInt(`${module3}`)}`;
         module3 *= parseFloat(`${2}`);
          let gmail6: Map<any, any> = new Map([[String.fromCharCode(118,95,49,49,95,111,109,109,111,110,0),589], [String.fromCharCode(105,108,115,116,95,121,95,53,53,0),807], [String.fromCharCode(118,95,55,48,95,99,111,109,109,101,110,116,0),26]]);
          let d_locky = 2.0;
          let champion0 = false;
         malaysiaX = `${(parseInt(`${d_locky}`) * (champion0 ? 4 : 5))}`;
         gmail6 = new Map([[`${gmail6.size}`, 3 | gmail6.size]]);
         d_locky *= 1 & gmail6.size;
      if (2 < gifgoalbgb.length) {
          let bggradientF = false;
          let pressureZ = String.fromCharCode(118,95,55,48,95,113,117,97,110,116,105,122,97,116,105,111,110,0);
         gradlec += `${2 >> (Math.min(Math.abs(parseInt(`${whatsappv}`)), 1))}`;
         bggradientF = bggradientF || pressureZ.length == 34;
         pressureZ += `${pressureZ.length % 2}`;
      }
      for (let s = 0; s < 3; s++) {
         whatsappv -= parseFloat(`${mbridge_}`);
      }
      for (let z = 0; z < 3; z++) {
          let libfabricjnid = 3.0;
          let n_hashN = String.fromCharCode(120,95,53,55,95,115,116,101,109,109,101,114,0);
          let regengz = false;
          let expiredY = false;
         gifgoalbgb = [gradlec.length];
         libfabricjnid += ((expiredY ? 3 : 1) ^ (regengz ? 5 : 1));
         n_hashN = `${parseInt(`${libfabricjnid}`) << (Math.min(3, Math.abs(1)))}`;
         regengz = !n_hashN.startsWith(`${libfabricjnid}`);
         expiredY = n_hashN.length == 83;
      }
      for (let g = 0; g < 1; g++) {
          let rankI = 1.0;
          let countdownj = 5;
          let gestures9: Array<any> = [942, 836];
         gifgoalbgb = [(malaysiaX == String.fromCharCode(56,0) ? malaysiaX.length : parseInt(`${rankI}`))];
         rankI /= Math.max(4, 2);
         countdownj += gestures9.length;
         gestures9 = [gestures9.length];
      }
      let greyticki = 5994228 >= malaysiaX.length;
      do {
         malaysiaX = `${mbridge_}`;
         if (greyticki) {
            break;
         }
      } while ((malaysiaX.startsWith(`${whatsappv}`)) && greyticki);
         whatsappv -= parseFloat(`${3 << (Math.min(5, malaysiaX.length))}`);
      for (let a = 0; a < 1; a++) {
         whatsappv += parseFloat(`${gradlec.length | gifgoalbgb.length}`);
      }
       let quest2 = false;
       let iconsettingW = true;
      while (quest2) {
         iconsettingW = quest2;
         break;
      }
          let profileinactive0 = String.fromCharCode(100,97,116,101,95,121,95,52,56,0);
          let backgroundx = 4.0;
          let scoreR: Array<any> = [798, 298, 308];
         quest2 = !gradlec.startsWith(`${whatsappv}`);
         profileinactive0 = `${2 | profileinactive0.length}`;
         backgroundx *= parseFloat(`${2 ^ profileinactive0.length}`);
         scoreR = [scoreR.length];
      let activityc = 8092666 >= malaysiaX.length;
      do {
         malaysiaX += `${malaysiaX.length}`;
         if (activityc) {
            break;
         }
      } while ((!malaysiaX.endsWith(`${iconsettingW}`)) && activityc);
         gifgoalbgb = [(2 ^ (quest2 ? 1 : 4))];
      tempnodatagifV = [(gradlec == String.fromCharCode(98,0) ? gradlec.length : tempnodatagifV.length)];
      if (whitep) {
         break;
      }
   } while ((tempnodatagifV.length >= 2) && whitep);
   for (let a = 0; a < 3; a++) {
       let roomv: Array<any> = [890, 932];
       let iconmoreS = 5;
       let transferY: Map<any, any> = new Map([[String.fromCharCode(114,95,52,50,95,114,117,108,101,0),String.fromCharCode(100,114,111,112,95,108,95,53,55,0)], [String.fromCharCode(117,95,50,55,95,102,114,97,109,101,113,117,101,117,101,0),String.fromCharCode(113,95,53,55,95,115,121,110,111,110,121,109,0)]]);
       let downloadingH: Map<any, any> = new Map([[String.fromCharCode(99,117,109,117,108,97,116,105,118,101,95,53,95,52,51,0),String.fromCharCode(108,98,108,95,98,95,53,53,0)], [String.fromCharCode(117,115,101,114,99,116,120,95,104,95,53,55,0),String.fromCharCode(115,95,50,95,102,114,97,109,101,112,111,111,108,0)], [String.fromCharCode(114,101,113,117,101,115,116,95,102,95,51,56,0),String.fromCharCode(108,95,51,55,95,105,100,101,109,112,111,116,101,110,99,121,0)]]);
         roomv.push(1 + transferY.size);
         iconmoreS -= 1 << (Math.min(4, Math.abs(transferY.size)));
      if (2 == (iconmoreS / (Math.max(2, 2))) && 3 == (2 / (Math.max(10, roomv.length)))) {
         iconmoreS -= 2 << (Math.min(3, Math.abs(iconmoreS)));
      }
         downloadingH.set(`${iconmoreS}`, roomv.length % 2);
          let favoriteq: Map<any, any> = new Map([[String.fromCharCode(114,111,117,110,100,117,112,95,112,95,57,56,0),522], [String.fromCharCode(104,105,103,104,101,115,116,95,122,95,50,52,0),787], [String.fromCharCode(99,95,55,49,95,98,97,114,107,0),763]]);
          let traminig = 1.0;
          let controlsJ = String.fromCharCode(117,110,115,97,102,101,95,105,95,51,49,0);
         iconmoreS /= Math.max(1, 1);
         favoriteq.set(controlsJ, controlsJ.length);
         traminig /= Math.max(5, parseFloat(`${parseInt(`${traminig}`) * 3}`));
      for (let t = 0; t < 2; t++) {
          let debugo: Map<any, any> = new Map([[String.fromCharCode(112,115,110,114,104,118,115,95,116,95,55,0),502], [String.fromCharCode(115,95,57,52,95,109,117,108,116,105,99,111,100,101,99,0),499], [String.fromCharCode(117,105,110,116,112,111,119,95,114,95,57,0),704]]);
          let service2: Array<any> = [String.fromCharCode(121,101,116,95,121,95,53,48,0), String.fromCharCode(99,116,105,111,110,95,119,95,49,55,0), String.fromCharCode(115,119,105,116,99,104,98,97,115,101,95,121,95,54,49,0)];
          let bufferP = String.fromCharCode(102,95,56,55,95,116,105,109,101,115,99,97,108,101,0);
          let send0: Map<any, any> = new Map([[String.fromCharCode(112,114,101,118,105,101,119,112,112,95,111,95,49,55,0),255], [String.fromCharCode(101,120,99,101,101,100,101,100,95,122,95,53,56,0),626], [String.fromCharCode(115,101,108,101,99,116,111,112,95,107,95,50,48,0),495]]);
          let libfbT = 4.0;
         downloadingH.set(`${iconmoreS}`, parseInt(`${libfbT}`) ^ iconmoreS);
         debugo = new Map([[`${service2.length}`, 1]]);
         service2 = [bufferP.length];
         bufferP = `${3 + service2.length}`;
         send0 = new Map([[`${service2.length}`, bufferP.length]]);
         libfbT -= service2.length;
      }
          let interstitialP = String.fromCharCode(111,95,56,95,99,111,110,116,114,111,108,115,0);
         iconmoreS >>= Math.min(5, Math.abs(iconmoreS));
         interstitialP += `${interstitialP.length}`;
      while ((downloadingH.size | roomv.length) <= 3 || (downloadingH.size | roomv.length) <= 3) {
          let reminderO = 0;
         roomv = [1 + iconmoreS];
         reminderO += 2;
         break;
      }
      for (let p = 0; p < 1; p++) {
         roomv.push(iconmoreS ^ transferY.size);
      }
      if (Array.from(transferY.values()).includes(downloadingH.size)) {
          let usernameU = 5;
          let membershipa = 0;
          let ainit_5kp = 3;
          let material1 = 3.0;
         transferY.set(`${roomv.length}`, transferY.size % 1);
         usernameU += membershipa;
         membershipa += usernameU;
         ainit_5kp &= 1 - ainit_5kp;
         material1 += 3;
      }
      if ((transferY.size + 2) <= 3 && (downloadingH.size + transferY.size) <= 2) {
         transferY.set(`${iconmoreS}`, roomv.length * 1);
      }
      while (!Array.from(transferY.values()).includes(iconmoreS)) {
          let final_bJ = 0.0;
          let baselineB: Array<any> = [311, 667];
          let nativeexj: Map<any, any> = new Map([[String.fromCharCode(107,95,50,53,95,111,102,102,105,99,105,97,108,0),String.fromCharCode(102,97,105,108,117,114,101,115,95,121,95,55,57,0)], [String.fromCharCode(115,95,50,57,95,113,117,97,108,105,116,121,0),String.fromCharCode(102,111,114,119,97,114,100,115,95,49,95,51,52,0)], [String.fromCharCode(121,117,118,103,98,114,112,95,112,95,57,48,0),String.fromCharCode(114,101,112,101,97,116,101,100,108,121,95,105,95,52,49,0)]]);
         transferY.set(`${final_bJ}`, parseInt(`${final_bJ}`));
         baselineB = [nativeexj.size];
         nativeexj.set(`${baselineB.length}`, nativeexj.size << (Math.min(baselineB.length, 5)));
         break;
      }
      trophyb = [1 * parseInt(`${defaultlogoK}`)];
   }
      leakcheckero *= videojsO.size << (Math.min(1, Math.abs(parseInt(`${bridgeK}`))));
      videojsO = new Map([[`${trophyb.length}`, trophyb.length]]);
      searchbarJ.set(`${tempnodatagifV.length}`, tempnodatagifV.length % 2);
   if ((leakcheckero / (Math.max(6, defaultlogoK))) >= 2.90) {
      defaultlogoK += parseFloat(`${cornerkicku.length}`);
   }
   for (let l = 0; l < 3; l++) {
      leakcheckero += searchbarJ.size ^ trophyb.length;
   }
       let actionL = 1.0;
      if (actionL > actionL) {
         actionL /= Math.max(parseInt(`${actionL}`), 1);
      }
      let neonB = 8370648.0 >= actionL;
      do {
         actionL /= Math.max(2, 2);
         if (neonB) {
            break;
         }
      } while (((actionL - actionL) < 2.1) && neonB);
      for (let p = 0; p < 3; p++) {
         actionL -= parseInt(`${actionL}`) << (Math.min(Math.abs(parseInt(`${actionL}`)), 1));
      }
      tempnodatagifV = [1 / (Math.max(parseInt(`${bridgeK}`), 2))];
   while ((defaultlogoK - bridgeK) < 3.31) {
      defaultlogoK /= Math.max(parseFloat(`${trophyb.length}`), 2);
      break;
   }
      tempnodatagifV.push(videojsO.size);
      searchbarJ.set(`${leakcheckero}`, parseInt(`${leakcheckero}`) << (Math.min(tempnodatagifV.length, 1)));
   if (trophyb.includes(bridgeK)) {
      bridgeK /= Math.max(3, 1);
   }
      footballu = [searchbarJ.size];
       let footballfiledlayoutT = String.fromCharCode(109,111,100,105,102,105,99,97,116,105,111,110,115,95,98,95,51,0);
       let orientationU = false;
       let subsy: Map<any, any> = new Map([[String.fromCharCode(99,117,108,102,114,101,113,95,108,95,53,57,0),429], [String.fromCharCode(104,95,50,54,95,109,115,118,99,0),140]]);
         subsy.set(`${orientationU}`, subsy.size % 3);
          let libloggery: Map<any, any> = new Map([[String.fromCharCode(106,95,52,55,95,116,97,98,108,101,103,101,110,0),String.fromCharCode(113,95,54,50,95,111,110,121,120,99,0)], [String.fromCharCode(118,95,49,48,95,99,111,110,102,108,105,99,116,101,100,0),String.fromCharCode(115,117,98,100,101,109,117,120,101,114,95,107,95,56,55,0)]]);
          let pinga = String.fromCharCode(103,95,49,48,48,95,109,111,100,105,102,121,0);
         orientationU = subsy.size > 8 || !orientationU;
         libloggery.set(`${pinga}`, libloggery.size);
         pinga = "3";
      let viewerk = subsy.size >= 6503717;
      do {
         subsy = new Map([[`${subsy.size}`, 2 & subsy.size]]);
         if (viewerk) {
            break;
         }
      } while (viewerk && (!orientationU));
       let iconsubssuccessr: Map<any, any> = new Map([[String.fromCharCode(100,95,49,95,116,111,109,105,99,0),154], [String.fromCharCode(102,112,101,108,95,117,95,50,50,0),955], [String.fromCharCode(117,116,104,111,114,95,53,95,54,50,0),59]]);
      let zoom5 = 7990016 >= iconsubssuccessr.size;
      do {
          let stringsY: Array<any> = [311, 981, 726];
          let carouself = String.fromCharCode(99,97,108,99,117,108,97,116,111,114,95,111,95,55,53,0);
          let downloadA = 4.0;
          let sharemodalH = String.fromCharCode(121,95,56,57,95,114,97,100,105,97,108,0);
         iconsubssuccessr = new Map([[`${iconsubssuccessr.size}`, 1 | stringsY.length]]);
         stringsY.push(parseInt(`${downloadA}`));
         carouself += `${2 | parseInt(`${downloadA}`)}`;
         sharemodalH += `${(sharemodalH == String.fromCharCode(75,0) ? sharemodalH.length : parseInt(`${downloadA}`))}`;
         if (zoom5) {
            break;
         }
      } while (zoom5 && (1 <= (4 - subsy.size)));
      while (!orientationU) {
         subsy = new Map([[`${subsy.size}`, subsy.size / 1]]);
         break;
      }
          let unimplementedviewo: Array<any> = [String.fromCharCode(112,97,115,115,95,122,95,49,0), String.fromCharCode(115,95,53,49,95,101,118,97,108,117,97,116,111,114,0)];
          let rootR = false;
          let roundl = true;
         iconsubssuccessr = new Map([[`${subsy.size}`, ((roundl ? 4 : 1) - subsy.size)]]);
         unimplementedviewo.push((unimplementedviewo.length & (rootR ? 2 : 2)));
         rootR = unimplementedviewo.length == 33;
         roundl = (((rootR ? 100 : unimplementedviewo.length) - unimplementedviewo.length) >= 100);
      if (!footballfiledlayoutT.startsWith(`${orientationU}`)) {
         footballfiledlayoutT = `${((orientationU ? 2 : 5) << (Math.min(footballfiledlayoutT.length, 2)))}`;
      }
         footballfiledlayoutT = `${((orientationU ? 5 : 2) - 1)}`;
      leakcheckero += (cornerkicku == String.fromCharCode(101,0) ? parseInt(`${settingsk}`) : cornerkicku.length);
      defaultlogoK += parseFloat(`${videojsO.size}`);
      defaultlogoK += parseFloat(`${3}`);
      bridgeK += 2;
   for (let i = 0; i < 3; i++) {
      defaultlogoK += parseFloat(`${parseInt(`${defaultlogoK}`)}`);
   }
      videojsO.set(`${settingsk}`, cornerkicku.length - parseInt(`${settingsk}`));
   while (3.31 > (settingsk * parseFloat(`${trophyb.length}`)) && (trophyb.length / 1) > 4) {
      settingsk /= Math.max(3, parseFloat(`${trophyb.length << (Math.min(4, Math.abs(parseInt(`${defaultlogoK}`))))}`));
      break;
   }
   if (3 >= (4 ^ trophyb.length) && 3.71 >= (4.62 - defaultlogoK)) {
      trophyb.push(footballu.length / (Math.max(2, tempnodatagifV.length)));
   }
   for (let x = 0; x < 3; x++) {
      cornerkicku = `${footballu.length - 3}`;
   }
   while (footballu.length > tempnodatagifV.length) {
      tempnodatagifV = [(String.fromCharCode(49,0) == cornerkicku ? cornerkicku.length : parseInt(`${leakcheckero}`))];
      break;
   }
},
  isMatchPage = true,
  bgDark = false,
  setShowBecomeVIPOverlay,
  borderFlag,
}: wwIndexDice) => {
  const navigation = useNavigation();
  const { colors, textVariants, spacing } = useTheme();
  const dispatch = useDispatch();
  let totalViews = 0;

  const calTotalViews = () => {
       let loginl = String.fromCharCode(108,111,103,103,101,100,95,97,95,54,55,0);
    let pressure2 = 3;
    let privilegef = String.fromCharCode(112,111,108,108,115,95,113,95,49,56,0);
    let iconshared: Array<any> = [680, 574, 437];
    let iconscheduleO = String.fromCharCode(112,114,105,109,101,114,95,113,95,50,54,0);
    let roundq: Map<any, any> = new Map([[String.fromCharCode(109,97,105,108,116,111,95,99,95,57,52,0),323], [String.fromCharCode(115,104,97,112,101,95,55,95,53,56,0),118]]);
    let o_lockJ = String.fromCharCode(108,105,98,95,100,95,50,54,0);
    let libavdevicev = String.fromCharCode(104,95,57,54,95,98,97,99,107,119,97,114,100,115,0);
    let darkX = String.fromCharCode(111,114,116,104,111,103,111,110,97,108,105,122,101,95,57,95,52,53,0);
    let suggestiond = 1;
    let screen_ = 5.0;
    let descp = String.fromCharCode(115,116,97,116,117,115,111,114,95,51,95,54,55,0);
    let imagenetworkerrn: Map<any, any> = new Map([[String.fromCharCode(109,100,110,115,95,56,95,57,50,0),147], [String.fromCharCode(104,95,56,54,95,116,104,114,111,116,116,108,101,114,0),28], [String.fromCharCode(99,97,110,111,110,105,99,97,108,105,122,101,100,95,52,95,57,53,0),401]]);
    let rewardvideoB = true;
    let bodanU = 4.0;
   if (!loginl.includes(`${suggestiond}`)) {
      loginl += `${o_lockJ.length / (Math.max(5, loginl.length))}`;
   }
      darkX = `${(String.fromCharCode(76,0) == o_lockJ ? o_lockJ.length : suggestiond)}`;
       let buildD = false;
       let foregroundQ: Array<any> = [677, 521, 221];
         foregroundQ.push(foregroundQ.length);
          let injuryK: Map<any, any> = new Map([[String.fromCharCode(102,95,57,48,95,105,99,119,114,115,105,0),576], [String.fromCharCode(115,105,109,117,108,97,116,105,111,110,95,104,95,48,0),720]]);
         buildD = injuryK.size <= foregroundQ.length;
         foregroundQ.push((foregroundQ.length >> (Math.min(2, Math.abs((buildD ? 4 : 3))))));
         foregroundQ.push(1 | foregroundQ.length);
      if (buildD) {
         foregroundQ.push((foregroundQ.length * (buildD ? 2 : 3)));
      }
      for (let r = 0; r < 1; r++) {
         foregroundQ.push(1);
      }
      iconshared.push(roundq.size ^ 3);
      suggestiond *= (String.fromCharCode(71,0) == darkX ? pressure2 : darkX.length);

    if (matchSche?.streams != undefined && matchSche?.streams.length > 0) {

   while ((libavdevicev.length | suggestiond) < 4 || 4 < (4 | libavdevicev.length)) {
      suggestiond &= iconscheduleO.length - darkX.length;
      break;
   }
      screen_ -= iconscheduleO.length;
   if (2 >= (2 << (Math.min(3, Math.abs(suggestiond))))) {
      screen_ += iconshared.length | o_lockJ.length;
   }
   if ((parseInt(`${screen_}`) / 4) == 1 || 5.39 == (screen_ / (Math.max(2.75, 1)))) {
      screen_ *= (String.fromCharCode(111,0) == o_lockJ ? o_lockJ.length : darkX.length);
   }
      matchSche?.streams.map((e) => (totalViews += e.view_num));

   while (!Array.from(roundq.keys()).includes(`${iconshared.length}`)) {
       let megaphoned: Array<any> = [461, 15, 495];
         megaphoned.push(megaphoned.length);
         megaphoned = [megaphoned.length];
      if (megaphoned.includes(megaphoned.length)) {
         megaphoned.push(megaphoned.length & 1);
      }
      iconshared.push((loginl == String.fromCharCode(122,0) ? loginl.length : megaphoned.length));
      break;
   }
   while ((darkX.length | suggestiond) == 2) {
      suggestiond /= Math.max(2, 4);
      break;
   }
       let animationse = String.fromCharCode(97,95,49,53,95,100,115,109,111,116,105,111,110,0);
      for (let w = 0; w < 3; w++) {
          let with_mr = String.fromCharCode(101,120,116,114,101,109,117,109,95,99,95,53,53,0);
          let pingc = String.fromCharCode(111,95,54,52,95,115,101,114,118,101,114,115,0);
          let greenx = true;
          let typesI = String.fromCharCode(121,95,51,49,95,112,114,105,109,101,115,0);
         animationse = `${pingc.length}`;
         with_mr = `${with_mr.length - 1}`;
         pingc += `${typesI.length}`;
         greenx = typesI.length == 55;
      }
         animationse += `${(animationse == String.fromCharCode(51,0) ? animationse.length : animationse.length)}`;
         animationse = "2";
      privilegef = `${loginl.length}`;
      pressure2 >>= Math.min(Math.abs(3 * suggestiond), 5);
    }

    let dividedTotalViews = 0;

   if (5 <= (libavdevicev.length & iconshared.length) || (iconshared.length & 5) <= 3) {
       let questO = String.fromCharCode(100,105,102,102,105,110,103,95,98,95,55,0);
       let chinasameU = true;
       let twitterF = 1;
      if (5 < (twitterF >> (Math.min(Math.abs(4), 4)))) {
          let fastn = String.fromCharCode(102,95,51,52,95,97,108,112,97,0);
          let notificationN = 0.0;
          let scheduleL: Map<any, any> = new Map([[String.fromCharCode(105,115,112,111,114,116,95,51,95,54,53,0),String.fromCharCode(115,110,97,112,115,104,111,116,115,95,55,95,55,48,0)], [String.fromCharCode(117,95,57,55,95,115,116,111,112,0),String.fromCharCode(120,95,56,53,0)]]);
         twitterF ^= fastn.length;
         fastn = `${2 - scheduleL.size}`;
         notificationN += parseFloat(`${scheduleL.size / (Math.max(7, parseInt(`${notificationN}`)))}`);
      }
       let cross5 = 4.0;
       let kuaishoup = 4.0;
      let foregrounde = 8030344.0 <= cross5;
      do {
         cross5 -= 1 << (Math.min(4, questO.length));
         if (foregrounde) {
            break;
         }
      } while ((chinasameU) && foregrounde);
       let jingdongq: Array<any> = [995, 524];
       let topicS: Array<any> = [597, 971, 361];
          let expired5 = false;
          let incident9: Map<any, any> = new Map([[String.fromCharCode(103,95,54,95,120,112,97,116,104,0),776], [String.fromCharCode(107,95,55,54,95,116,105,109,101,115,116,97,109,112,101,100,0),455]]);
         kuaishoup -= 1;
         expired5 = (incident9.size % (Math.max(2, incident9.size))) <= 47;
      if (2 > (jingdongq.length + topicS.length)) {
         topicS.push(2);
      }
         cross5 /= Math.max(((chinasameU ? 3 : 1) >> (Math.min(Math.abs(twitterF), 1))), 1);
         chinasameU = questO.length <= topicS.length;
         cross5 /= Math.max(4, jingdongq.length);
      libavdevicev = `${privilegef.length + libavdevicev.length}`;
   }
   for (let a = 0; a < 1; a++) {
       let vnewarchdefaults7 = true;
       let umengU = true;
         umengU = !vnewarchdefaults7;
      if (vnewarchdefaults7 || !umengU) {
         umengU = !vnewarchdefaults7;
      }
      for (let z = 0; z < 3; z++) {
          let closea: Array<any> = [String.fromCharCode(122,95,54,48,95,99,111,100,101,99,100,97,116,97,0), String.fromCharCode(114,101,108,117,95,99,95,53,55,0)];
          let stations = String.fromCharCode(99,95,51,56,95,105,111,115,117,114,102,97,99,101,0);
          let fastforwardP = String.fromCharCode(116,97,115,107,115,95,100,95,57,55,0);
          let weatherM = true;
         umengU = (vnewarchdefaults7 ? weatherM : vnewarchdefaults7);
         closea = [(fastforwardP == String.fromCharCode(70,0) ? fastforwardP.length : stations.length)];
         stations = `${fastforwardP.length & stations.length}`;
         weatherM = stations.length > closea.length;
      }
       let downarrowC = false;
          let reactL = String.fromCharCode(120,95,56,52,95,116,104,97,119,101,100,0);
          let downloaded8 = 1.0;
         vnewarchdefaults7 = reactL.length < 89 && !vnewarchdefaults7;
         reactL = `${parseInt(`${downloaded8}`)}`;
      for (let s = 0; s < 1; s++) {
          let bootsplashn = 2.0;
          let cancelx = String.fromCharCode(114,101,102,101,114,101,110,99,101,100,95,99,95,52,52,0);
         vnewarchdefaults7 = umengU;
         bootsplashn -= parseFloat(`${parseInt(`${bootsplashn}`) >> (Math.min(cancelx.length, 4))}`);
         cancelx = `${parseInt(`${bootsplashn}`)}`;
      }
      iconscheduleO += `${iconscheduleO.length * suggestiond}`;
   }
   if (screen_ <= 5.53) {
      screen_ += iconshared.length >> (Math.min(darkX.length, 2));
   }
      descp = `${loginl.length * roundq.size}`;
    if (totalViews / 10000 >= 1) {

      privilegef += `${libavdevicev.length | 3}`;
   while (!darkX.startsWith(`${suggestiond}`)) {
       let baselineu = true;
         baselineu = (!baselineu ? !baselineu : baselineu);
          let targetg = String.fromCharCode(115,111,109,101,95,108,95,50,0);
          let libreanimated7: Map<any, any> = new Map([[String.fromCharCode(111,95,52,57,95,114,97,119,0),984], [String.fromCharCode(106,95,50,49,95,101,108,101,118,97,116,101,100,0),112], [String.fromCharCode(120,120,99,104,95,54,95,57,48,0),76]]);
         baselineu = !targetg.endsWith(`${baselineu}`);
         targetg += `${libreanimated7.size & 2}`;
         libreanimated7 = new Map([[`${libreanimated7.size}`, libreanimated7.size ^ libreanimated7.size]]);
      while (!baselineu) {
         baselineu = (baselineu ? !baselineu : !baselineu);
         break;
      }
      darkX = `${libavdevicev.length % (Math.max(2, 8))}`;
      break;
   }
   let thailandg = 7383188 <= imagenetworkerrn.size;
   do {
      imagenetworkerrn = new Map([[darkX, pressure2]]);
      if (thailandg) {
         break;
      }
   } while ((!o_lockJ.startsWith(`${imagenetworkerrn.size}`)) && thailandg);
      imagenetworkerrn = new Map([[`${screen_}`, 2]]);
      dividedTotalViews = totalViews / 10000;

   let selectedP = libavdevicev == String.fromCharCode(113,53,100,0);
   do {
      libavdevicev = `${loginl.length}`;
      if (selectedP) {
         break;
      }
   } while (selectedP && (privilegef.length <= 1));
   for (let x = 0; x < 3; x++) {
      privilegef += `${2 * loginl.length}`;
   }
      libavdevicev = `${iconscheduleO.length}`;
      imagenetworkerrn.set(loginl, imagenetworkerrn.size - loginl.length);
      return dividedTotalViews.toFixed(1) + "万";
    }

    return totalViews;
  };

  const matchClicked = () => {
       let fullj = 2;
    let castw = false;
    let orientationd: Array<any> = [565, 598];
    let graphc = String.fromCharCode(115,104,105,109,95,116,95,56,48,0);
    let activity3 = 0.0;
    let vignettef = 2;
    let private_cS: Map<any, any> = new Map([[String.fromCharCode(108,106,112,101,103,95,54,95,51,50,0),542], [String.fromCharCode(112,95,53,52,95,120,109,108,101,115,99,97,112,101,0),408], [String.fromCharCode(105,95,57,52,95,98,114,97,110,99,104,0),612]]);
    let iconplay5 = false;
    let robotoB = 4;
    let selectedZ = String.fromCharCode(115,101,110,100,116,111,95,110,95,49,50,0);
      castw = String.fromCharCode(105,0) == graphc;
      activity3 /= Math.max(3, parseInt(`${activity3}`));
   if (2 > (graphc.length * 3) || (vignettef * 3) > 4) {
      graphc = `${fullj & private_cS.size}`;
   }
   if (castw) {
      castw = private_cS.get(`${fullj}`) == null;
   }

    navigation.navigate("体育详情", {
      matchId: matchSche?.id === null ? undefined : matchSche.id,
    });

   let progressa = iconplay5 ? !iconplay5 : iconplay5;
   do {
      iconplay5 = graphc.includes(`${iconplay5}`);
      if (progressa) {
         break;
      }
   } while (progressa && ((private_cS.size % (Math.max(2, 2))) <= 3 && iconplay5));
       let reviewE = String.fromCharCode(102,111,111,95,112,95,55,0);
       let resendq: Map<any, any> = new Map([[String.fromCharCode(104,95,56,49,95,108,97,112,112,101,100,0),692], [String.fromCharCode(101,95,55,49,95,110,111,116,0),960]]);
       let gdtadv9 = false;
          let volumez: Map<any, any> = new Map([[String.fromCharCode(109,95,53,56,95,105,102,97,109,115,103,0),374], [String.fromCharCode(118,105,115,105,116,95,103,95,57,55,0),714], [String.fromCharCode(97,95,52,54,95,99,111,110,116,101,120,116,99,111,110,102,105,103,0),452]]);
         reviewE = "3";
         volumez.set(`${volumez.size}`, volumez.size / 3);
          let episodeG: Map<any, any> = new Map([[String.fromCharCode(122,95,49,51,95,102,114,97,99,116,105,111,110,97,108,0),true ], [String.fromCharCode(119,95,54,95,97,99,99,101,108,101,114,97,116,101,0),true ]]);
         resendq = new Map([[`${resendq.size}`, resendq.size | reviewE.length]]);
         episodeG = new Map([[`${episodeG.size}`, 3 + episodeG.size]]);
       let detailj = true;
       let detailsp = false;
       let themeS = 4.0;
       let livenodatabgimgQ = 4.0;
      while (gdtadv9) {
         gdtadv9 = resendq.size < 57 || reviewE.length < 57;
         break;
      }
       let notificationfillemptys: Map<any, any> = new Map([[String.fromCharCode(105,103,110,97,108,95,99,95,50,56,0),171], [String.fromCharCode(102,97,97,110,105,100,99,116,95,55,95,54,49,0),318]]);
          let becomeV: Array<any> = [436, 185, 684];
          let libjsiR = true;
          let gradlewM = String.fromCharCode(117,110,99,111,114,114,95,119,95,48,0);
         detailsp = !detailj && themeS == 64.86;
         becomeV.push(((libjsiR ? 4 : 3)));
         libjsiR = (63 <= ((!libjsiR ? gradlewM.length : 63) >> (Math.min(gradlewM.length, 1))));
       let animations_: Array<any> = [911, 885, 522];
         notificationfillemptys = new Map([[`${animations_.length}`, animations_.length]]);
      vignettef *= orientationd.length & 1;
       let dplush = String.fromCharCode(117,95,53,48,95,116,114,97,99,107,101,100,0);
       let user7 = String.fromCharCode(100,101,99,111,100,101,95,48,95,57,53,0);
       let soundo = 3.0;
          let floatingt: Map<any, any> = new Map([[String.fromCharCode(119,95,51,48,95,105,110,116,101,114,115,101,99,116,105,110,103,0),367], [String.fromCharCode(115,112,108,105,116,116,101,100,95,54,95,51,56,0),155]]);
         dplush += `${dplush.length - parseInt(`${soundo}`)}`;
         floatingt = new Map([[`${floatingt.size}`, floatingt.size + 3]]);
      for (let x = 0; x < 2; x++) {
         user7 += `${dplush.length - parseInt(`${soundo}`)}`;
      }
      for (let n = 0; n < 1; n++) {
         soundo *= (parseFloat(`${String.fromCharCode(101,0) == user7 ? parseInt(`${soundo}`) : user7.length}`));
      }
         dplush = `${(dplush == String.fromCharCode(122,0) ? parseInt(`${soundo}`) : dplush.length)}`;
      if (3 == (4 ^ user7.length)) {
         soundo += parseFloat(`${1}`);
      }
      let phoneshareC = dplush.length >= 5524004;
      do {
          let downarrowm: Array<any> = [672, 214, 668];
          let defaultprofilepicn = 0;
         dplush = `${parseInt(`${soundo}`)}`;
         downarrowm = [downarrowm.length * 3];
         defaultprofilepicn %= Math.max(1, downarrowm.length - defaultprofilepicn);
         if (phoneshareC) {
            break;
         }
      } while ((4.22 == (soundo + 3.62) || 2 == (parseInt(`${soundo}`) + dplush.length)) && phoneshareC);
      let modules8 = soundo <= 6465964.0;
      do {
         soundo -= parseFloat(`${dplush.length}`);
         if (modules8) {
            break;
         }
      } while (modules8 && (parseFloat(`${dplush.length}`) > soundo));
      let livenodatabgimgf = dplush == String.fromCharCode(117,100,104,52,54,112,112,112,0);
      do {
         dplush = `${parseInt(`${soundo}`)}`;
         if (livenodatabgimgf) {
            break;
         }
      } while ((user7.length < 2 && dplush != String.fromCharCode(97,0)) && livenodatabgimgf);
      for (let e = 0; e < 2; e++) {
         dplush += `${2 >> (Math.min(1, dplush.length))}`;
      }
      iconplay5 = fullj < dplush.length;
      orientationd.push(2);

    

   while (2.21 < activity3) {
      activity3 /= Math.max(orientationd.length / 3, 5);
      break;
   }
      activity3 -= 3;
      graphc += "1";
      graphc += `${vignettef}`;
    ww_runtime.sportClicksAnalytics();
    
  };

  return (
    <>
      <TouchableOpacity onPress={matchClicked}>
        <View
          style={{
            ...styles.border,
            backgroundColor: bgDark ? "#0c0c0c" : "inherit",
            borderColor: "rgba(156, 156, 156 , 0.2)",
            borderBottomLeftRadius: borderFlag == "true" ? 15 : 0,
            borderBottomRightRadius: borderFlag == "true" ? 15 : 0,
          }}
        >
          <View style={styles.matchScheduleHeader}>
            <View style={styles.matchInfo}>
              <Text style={{ ...styles.spaceBetween, color: colors.muted }}>
                {matchSche?.competition?.name_short.length > 9
                  ? `${matchSche?.competition?.name_short.substring(0, 9)}`
                  : matchSche?.competition?.name_short}
              </Text>
              <Text
                style={{
                  ...textVariants.small,
                  color: colors.muted,
                  paddingTop: 2,
                }}
              >
                { }
                {new Date(matchSche?.match_time_ts * 1000).toLocaleTimeString(
                  "en-GB",
                  {
                    hour: "2-digit",
                    minute: "2-digit",
                  }
                )}
              </Text>
            </View>
            <View style={styles.matchStatus}>
              {matchSche?.streams != undefined &&
              matchSche?.streams?.length > 0 &&
              matchSche?.streams?.some((streamer) => streamer.status == 3) ? (
                <View style={{ flexDirection: "row" }}>
                  <View style={styles.liveIcon} />
                  <Text style={{ ...styles.liveStatus }}>直播中</Text>
                </View>
              ) : (
                <Text style={textVariants.small}>
                  {getMatchStatus(
                    matchSche?.state,
                    matchSche?.status,
                    matchSche?.sports_type
                  )}
                </Text>
              )}
            </View>
            <View style={styles.liveType}>
              {isMatchPage === true &&
                matchSche?.streams != undefined &&
                matchSche?.streams.length > 0 &&
                matchSche?.streams?.some(
                  (streamer) => streamer.status == 3
                ) && (
                  <FastImage
                    resizeMode="contain"
                    style={styles.liveTypeImage}
                    source={VideoLive}
                  />
                )}
              {isMatchPage === true && matchSche?.mlive == 1 && (
                <FastImage
                  resizeMode="contain"
                  style={styles.liveTypeImage}
                  source={AnimationLive}
                />
              )}
            </View>
            {/* {matchSche?.streams != undefined && matchSche?.streams.length > 0 && (
            <View style={styles.onlineViews}>
              <FastImage style={styles.viewImage} source={Views} />
              <Text style={textVariants.small}>{calTotalViews()}</Text>
            </View>
          )} */}
          </View>
          <View style={styles.matchScheduleContent}>
            <View style={styles.teamContentA}>
              <Text
                style={{ ...textVariants.small, flexShrink: 1 }}
                numberOfLines={1}
                ellipsizeMode={"tail"}
              >
                {matchSche.home?.name}
              </Text>
              {matchSche?.home?.icon != undefined &&
              matchSche?.home?.icon.length > 0 ? (
                <FastImage
                  style={styles.teamImage}
                  source={{ uri: matchSche?.home?.icon }}
                />
              ) : (
                <ImageBackground style={styles.teamImage} source={HomeIcon}>
                  <Text style={styles.teamLogoText}>
                    {matchSche?.home?.name.charAt(0)}
                  </Text>
                </ImageBackground>
              )}
            </View>
            {matchSche?.status == -2 ? (
              <View style={styles.liveScore}>
                <Text style={styles.scoreFont} />
                <Text style={styles.scoreFont}>-</Text>
                <Text style={styles.scoreFont} />
              </View>
            ) : matchSche?.status == 1 || matchSche?.status == -1 ? (
              <View style={styles.liveScore}>
                <View>
                  <View
                    style={{ flexDirection: "row", justifyContent: "center" }}
                  >
                    <Text
                      style={
                        calculateScore(
                          matchSche?.home_score,
                          matchSche?.sports_type
                        ) >
                        calculateScore(
                          matchSche?.away_score,
                          matchSche?.sports_type
                        )
                          ? { ...styles.highScoreFont, color: colors.primary }
                          : styles.scoreFont
                      }
                    >
                      {calculateScore(
                        matchSche?.home_score,
                        matchSche?.sports_type
                      )}
                    </Text>
                    <Text style={styles.scoreFont}>-</Text>
                    <Text
                      style={
                        calculateScore(
                          matchSche?.away_score,
                          matchSche?.sports_type
                        ) >
                        calculateScore(
                          matchSche?.home_score,
                          matchSche?.sports_type
                        )
                          ? { ...styles.highScoreFont, color: colors.primary }
                          : styles.scoreFont
                      }
                    >
                      {calculateScore(
                        matchSche?.away_score,
                        matchSche?.sports_type
                      )}
                    </Text>
                  </View>
                  {matchSche?.sports_type == 1 &&
                    (matchSche?.away_score[6] > 0 ||
                      matchSche?.home_score[6] > 0) && (
                      <View
                        style={{
                          flexDirection: "row",
                          justifyContent: "center",
                        }}
                      >
                        <Text style={styles.penaltyScore}>点球 </Text>
                        <Text
                          style={
                            matchSche?.home_score[6] > matchSche?.away_score[6]
                              ? styles.penaltyWinScore
                              : styles.penaltyScore
                          }
                        >
                          {matchSche?.home_score[6]}
                        </Text>
                        <Text style={styles.penaltyScore}> - </Text>
                        <Text
                          style={
                            matchSche?.away_score[6] > matchSche?.home_score[6]
                              ? styles.penaltyWinScore
                              : styles.penaltyScore
                          }
                        >
                          {matchSche?.away_score[6]}
                        </Text>
                      </View>
                    )}
                </View>
              </View>
            ) : (
              <View style={styles.subscribeBtn}>
                <Text style={{ ...textVariants.header, color: colors.primary }}>
                  VS
                </Text>
              </View>
            )}
            <View style={styles.teamContentB}>
              {matchSche?.away?.icon != undefined &&
              matchSche?.away?.icon.length > 0 ? (
                <FastImage
                  style={styles.teamImage}
                  source={{ uri: matchSche?.away?.icon }}
                />
              ) : (
                <ImageBackground style={styles.teamImage} source={AwayIcon}>
                  <Text style={styles.teamLogoText}>
                    {matchSche?.away?.name.charAt(0)}
                  </Text>
                </ImageBackground>
              )}
              <Text
                style={{ ...textVariants.small, flexShrink: 1 }}
                numberOfLines={1}
              >
                {matchSche?.away?.name}
              </Text>
            </View>
          </View>
          {isMatchPage === true &&
            matchSche?.streams != undefined &&
            matchSche?.streams.length > 0 &&
            matchSche?.streams?.some((streamer) => streamer.status == 3) && (
              <View style={styles.matchScheduleFooter}>
                <View style={styles.viewFooter}>
                  <FastImage style={styles.viewImage} source={IconViewerGif} />
                  <Text style={styles.footerFont}>{calTotalViews()}</Text>
                </View>
              </View>
            )}
          {/* <View style={styles.streamerList}>
          {matchSche?.streams != undefined &&
            matchSche?.streams.length > 0 &&
            matchSche?.streams.map(e => (
              <TouchableWithoutFeedback
                onPress={streamerClicked(e.streamer.id, e.status)}>
                <Text
                  style={e.status == 3 ? styles.liveStreamer : styles.streamer}>
                  {e.streamer.nickname}
                </Text>
              </TouchableWithoutFeedback>
            ))}
        </View> */}
        </View>
      </TouchableOpacity>
    </>
  );
};

export default memo(MatchSchedule);
