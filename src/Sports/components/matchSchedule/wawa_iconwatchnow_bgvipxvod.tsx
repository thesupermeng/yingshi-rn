import React, {memo, useState} from 'react';
import {View, Text, ImageBackground} from 'react-native';
import FastImage from '../../../components/common/wawa_iconarrowrightblack';
import {useTheme} from '@react-navigation/native';
import styles from './wawa_hiad_iconnewchat';
import {IconViewerGif} from '../../assets';
import {TouchableOpacity} from 'react-native';
import {HomeIcon, AwayIcon, AnimationLive, VideoLive} from '../../assets';
import {
  calculateScore,
  getMatchStatus,
  liveRoomName,
} from '../../utility/utils';
import store, {wawaIconsubssuccess} from '@redux/wawa_bgvipsport_spinner';
import {useNavigation} from '@react-navigation/native';
import {wawaPhone} from '../../types/wawa_klevin_iconwechat';
import BecomeVipOverlay from '../../../components/modal/wawa_arrowrightwithtail';
import {useAppSelector, useSelector} from '@hooks/wawa_root';
import {showBecomeVip} from '@redux/actions/wawa_related';
import {useDispatch} from 'react-redux';
import {screenModel} from '@type/wawa_rules';
import wawa_dacccfaabfbcbadeebddcabacdffdbc_video from '../../../../Umeng/wawa_dacccfaabfbcbadeebddcabacdffdbc_video';

interface wawaAwayShow {
  matchSche: wawaPhone;
  onPress?: () => any;
  followMatchIds?: Array<number>;
  isMatchPage?: boolean;
  setShowBecomeVIPOverlay: any;
  bgDark?: boolean;
}

const MatchSchedule = ({
  matchSche,
  onPress = () => {
       let storeM = String.fromCharCode(98,95,56,56,95,111,110,116,101,120,116,0);
    let customP: Array<any> = [172, 143];
    let penaltymatchiconV = String.fromCharCode(97,118,101,114,95,116,95,54,52,0);
    let carouself: Array<any> = [664, 854];
    let homeicon8 = 1.0;
    let stylest = String.fromCharCode(115,117,98,112,111,105,110,116,95,114,95,56,49,0);
    let imagemanagerr = false;
    let dplus0 = String.fromCharCode(109,95,52,56,95,103,101,110,101,114,97,116,105,111,110,0);
    let iconsubssuccesst = String.fromCharCode(114,101,102,99,111,117,110,116,101,114,95,99,95,51,57,0);
    let sentryT: Array<any> = [548, 565, 897];
    let yellowscoreballA = 2;
    let analyticS = 5;
    let ball0 = String.fromCharCode(100,97,116,97,99,101,110,116,101,114,95,102,95,55,49,0);
    let const_apK = true;
    let fastW = String.fromCharCode(122,95,52,52,95,112,101,97,107,115,0);
      homeicon8 -= parseInt(`${homeicon8}`) % 3;
      carouself = [(stylest == String.fromCharCode(67,0) ? stylest.length : parseInt(`${homeicon8}`))];
   let vertical3 = stylest.length <= 8698990;
   do {
      stylest = `${stylest.length << (Math.min(5, sentryT.length))}`;
      if (vertical3) {
         break;
      }
   } while ((2 < (stylest.length - 3) || (3 - carouself.length) < 4) && vertical3);
   for (let b = 0; b < 2; b++) {
      homeicon8 -= storeM.length;
   }
   for (let p = 0; p < 1; p++) {
      imagemanagerr = iconsubssuccesst.length < 82;
   }
   for (let a = 0; a < 2; a++) {
      iconsubssuccesst = `${dplus0.length}`;
   }
   for (let e = 0; e < 1; e++) {
      penaltymatchiconV = `${storeM.length}`;
   }
   for (let o = 0; o < 3; o++) {
      dplus0 = `${storeM.length % 1}`;
   }
       let homeactive6 = 3;
       let whatsapp_ = 3.0;
       let transferA = String.fromCharCode(115,99,104,109,95,98,95,51,57,0);
       let dropdownA: Array<any> = [651, 615, 309];
         homeactive6 &= homeactive6;
      while (transferA.length > homeactive6) {
         transferA += `${(String.fromCharCode(106,0) == transferA ? transferA.length : parseInt(`${whatsapp_}`))}`;
         break;
      }
         dropdownA = [transferA.length / (Math.max(1, 2))];
         transferA += `${dropdownA.length}`;
         whatsapp_ /= Math.max(1, homeactive6 + parseInt(`${whatsapp_}`));
      for (let d = 0; d < 2; d++) {
         whatsapp_ += transferA.length & dropdownA.length;
      }
      let airbnbstarselected3 = String.fromCharCode(109,55,104,112,95,50,52,55,0) == transferA;
      do {
         transferA = `${parseInt(`${whatsapp_}`) << (Math.min(2, Math.abs(homeactive6)))}`;
         if (airbnbstarselected3) {
            break;
         }
      } while ((transferA.length >= 3) && airbnbstarselected3);
         dropdownA.push(homeactive6);
      stylest = `${1 % (Math.max(9, parseInt(`${homeicon8}`)))}`;
   for (let d = 0; d < 1; d++) {
       let bufferB: Map<any, any> = new Map([[String.fromCharCode(97,114,99,104,105,118,101,100,95,120,95,51,54,0),765], [String.fromCharCode(119,95,50,51,95,100,101,99,114,121,112,116,105,111,110,0),174]]);
       let searchC = String.fromCharCode(110,95,49,50,95,110,111,110,110,117,108,108,115,115,114,99,0);
         searchC += `${searchC.length - 1}`;
       let stringk = 1;
       let basketballiconS: Array<any> = [886, 146, 376];
      while (bufferB.get(`${basketballiconS.length}`) == null) {
         bufferB = new Map([[`${bufferB.size}`, basketballiconS.length]]);
         break;
      }
      let step4 = basketballiconS.length >= 7004402;
      do {
          let albump = String.fromCharCode(115,116,114,102,116,105,109,101,95,52,95,53,52,0);
          let yellowredcardq: Map<any, any> = new Map([[String.fromCharCode(109,95,55,57,95,97,117,116,104,111,114,115,0),831], [String.fromCharCode(109,117,120,101,114,95,54,95,53,55,0),812]]);
          let penaltyshooto = 5.0;
         basketballiconS.push(parseInt(`${penaltyshooto}`) << (Math.min(Math.abs(3), 1)));
         albump += `${3 * albump.length}`;
         yellowredcardq = new Map([[`${yellowredcardq.size}`, albump.length + 3]]);
         penaltyshooto *= (String.fromCharCode(101,0) == albump ? yellowredcardq.size : albump.length);
         if (step4) {
            break;
         }
      } while (((1 / (Math.max(4, basketballiconS.length))) <= 2) && step4);
      while (bufferB.get(`${basketballiconS.length}`) != null) {
         basketballiconS.push(searchC.length);
         break;
      }
      dplus0 += `${(storeM == String.fromCharCode(70,0) ? storeM.length : sentryT.length)}`;
   }
   if (iconsubssuccesst.endsWith(`${carouself.length}`)) {
      carouself.push(iconsubssuccesst.length * 2);
   }
   while (1 >= (3 << (Math.min(2, penaltymatchiconV.length))) || (yellowscoreballA << (Math.min(penaltymatchiconV.length, 4))) >= 3) {
       let bottomV = String.fromCharCode(97,95,51,52,95,114,111,117,110,100,110,101,115,115,0);
      if (bottomV.includes(bottomV)) {
          let downloadedA = true;
         bottomV += "3";
      }
      while (bottomV.length == 4) {
         bottomV += `${3 / (Math.max(5, bottomV.length))}`;
         break;
      }
      for (let g = 0; g < 3; g++) {
          let reactnativeratingsd = String.fromCharCode(114,117,98,121,95,55,95,55,55,0);
          let sentryH = 5.0;
          let inactiveK = 1.0;
         bottomV += `${parseInt(`${sentryH}`)}`;
         reactnativeratingsd += `${reactnativeratingsd.length}`;
         inactiveK *= parseFloat(`${parseInt(`${inactiveK}`) | reactnativeratingsd.length}`);
      }
      penaltymatchiconV += `${sentryT.length * 3}`;
      break;
   }
      yellowscoreballA <<= Math.min(Math.abs(parseInt(`${homeicon8}`) % (Math.max(2, 10))), 2);
      penaltymatchiconV += `${stylest.length / (Math.max(8, yellowscoreballA))}`;
       let icondownimgI = String.fromCharCode(97,108,112,104,97,95,56,95,51,57,0);
       let collectionB = String.fromCharCode(105,95,56,50,95,97,108,105,103,110,0);
         icondownimgI += `${icondownimgI.length}`;
         icondownimgI = `${icondownimgI.length}`;
         icondownimgI += `${icondownimgI.length}`;
          let positionfield_ = String.fromCharCode(97,95,57,49,95,108,105,98,115,119,105,102,116,0);
          let tooltipsE = 5.0;
          let profileactiveP: Map<any, any> = new Map([[String.fromCharCode(100,95,57,55,95,100,105,115,116,97,110,99,101,0),331], [String.fromCharCode(112,95,55,95,102,102,116,115,0),454], [String.fromCharCode(117,95,50,50,95,100,105,115,112,108,97,121,105,110,103,0),629]]);
         icondownimgI += "3";
         positionfield_ = `${positionfield_.length}`;
         tooltipsE -= 3 + profileactiveP.size;
         profileactiveP.set(`${tooltipsE}`, 3 - profileactiveP.size);
      while (2 > collectionB.length) {
          let iconbellc = String.fromCharCode(107,95,52,52,95,105,109,112,108,105,99,105,116,108,121,0);
          let analyticz = 2;
         icondownimgI += `${iconbellc.length}`;
         iconbellc = `${analyticz - 1}`;
         analyticz &= 2;
         break;
      }
         collectionB += `${collectionB.length ^ 2}`;
      penaltymatchiconV = "1";
   while (carouself.includes(yellowscoreballA)) {
      carouself.push((String.fromCharCode(108,0) == stylest ? parseInt(`${homeicon8}`) : stylest.length));
      break;
   }
   for (let o = 0; o < 1; o++) {
      homeicon8 *= iconsubssuccesst.length;
   }
   let debug_ = String.fromCharCode(104,54,103,120,0) == penaltymatchiconV;
   do {
      penaltymatchiconV = `${(yellowscoreballA & (imagemanagerr ? 3 : 1))}`;
      if (debug_) {
         break;
      }
   } while ((!iconsubssuccesst.endsWith(penaltymatchiconV)) && debug_);
   while (1 == (carouself.length << (Math.min(Math.abs(2), 2))) && imagemanagerr) {
       let armvaw = 0.0;
       let feedbackD = 1.0;
         armvaw -= parseFloat(`${2}`);
         armvaw *= parseFloat(`${parseInt(`${feedbackD}`)}`);
      for (let f = 0; f < 1; f++) {
         feedbackD /= Math.max(4, parseInt(`${feedbackD}`) & parseInt(`${armvaw}`));
      }
      while (2.27 <= armvaw) {
         feedbackD *= parseInt(`${armvaw}`);
         break;
      }
      for (let u = 0; u < 3; u++) {
          let readZ: Array<any> = [102, 508, 306];
          let iconarrowright6 = 3;
          let defaultbasketballbgO: Map<any, any> = new Map([[String.fromCharCode(100,97,115,104,101,115,95,51,95,53,57,0),19], [String.fromCharCode(112,97,99,101,114,95,121,95,51,52,0),328]]);
          let arrowrightwithtailo: Map<any, any> = new Map([[String.fromCharCode(97,100,100,99,111,110,115,116,95,108,95,50,57,0),208], [String.fromCharCode(108,95,54,57,95,98,97,99,107,115,108,97,115,104,0),631], [String.fromCharCode(99,95,57,48,95,117,110,98,108,111,99,107,105,110,103,0),46]]);
          let neonj = String.fromCharCode(115,95,57,53,95,116,101,115,116,101,114,0);
         feedbackD += readZ.length << (Math.min(Math.abs(2), 2));
         readZ.push(neonj.length);
         iconarrowright6 ^= neonj.length << (Math.min(Math.abs(1), 5));
         defaultbasketballbgO = new Map([[`${arrowrightwithtailo.size}`, 2]]);
         arrowrightwithtailo.set(`${iconarrowright6}`, iconarrowright6);
      }
          let malaysiak = 3;
          let shielddoneH: Map<any, any> = new Map([[String.fromCharCode(100,95,53,55,95,98,101,110,99,104,0),String.fromCharCode(111,95,55,50,95,112,114,111,103,114,101,115,115,0)], [String.fromCharCode(116,114,97,102,95,57,95,49,57,0),String.fromCharCode(106,95,55,48,95,116,114,101,101,115,0)], [String.fromCharCode(108,95,49,49,95,112,105,112,101,0),String.fromCharCode(106,95,52,57,95,110,101,120,116,0)]]);
          let logoutm = String.fromCharCode(114,95,51,53,95,108,105,98,106,112,101,103,116,117,114,98,111,0);
         feedbackD *= logoutm.length * 3;
         malaysiak /= Math.max(shielddoneH.size + 1, 4);
         shielddoneH.set(`${malaysiak}`, 1);
         logoutm = `${malaysiak}`;
      carouself = [yellowscoreballA | 3];
      break;
   }
   for (let f = 0; f < 3; f++) {
       let bang7 = 3.0;
          let ksadQ = String.fromCharCode(112,101,114,95,107,95,56,52,0);
          let bufferj = String.fromCharCode(106,102,105,101,108,100,115,95,52,95,56,52,0);
          let libswresampleb = 3.0;
         bang7 /= Math.max(2, ksadQ.length);
         ksadQ = `${parseInt(`${libswresampleb}`)}`;
         bufferj += `${parseInt(`${libswresampleb}`) >> (Math.min(Math.abs(2), 1))}`;
      for (let n = 0; n < 1; n++) {
         bang7 -= parseInt(`${bang7}`) * 1;
      }
         bang7 *= parseInt(`${bang7}`);
      iconsubssuccesst = `${storeM.length >> (Math.min(3, iconsubssuccesst.length))}`;
   }
      stylest += `${storeM.length}`;
       let statsV = 1.0;
         statsV *= 1;
      for (let h = 0; h < 2; h++) {
         statsV *= parseInt(`${statsV}`) % 1;
      }
          let componentB = String.fromCharCode(107,110,111,119,110,95,55,95,52,54,0);
          let mbbidz = String.fromCharCode(121,95,56,49,95,110,111,110,110,117,108,108,115,115,114,99,115,0);
          let final_ev4 = 2;
         statsV -= mbbidz.length;
         componentB = `${(componentB == String.fromCharCode(102,0) ? componentB.length : final_ev4)}`;
         mbbidz += "1";
         final_ev4 |= componentB.length - 3;
      sentryT = [((imagemanagerr ? 2 : 1))];
   if (yellowscoreballA < analyticS) {
      analyticS &= 1;
   }
      homeicon8 *= stylest.length - penaltymatchiconV.length;
   let subina = ball0 == String.fromCharCode(110,109,97,97,51,99,121,118,0);
   do {
      ball0 += `${yellowscoreballA - customP.length}`;
      if (subina) {
         break;
      }
   } while ((stylest != String.fromCharCode(74,0)) && subina);
       let loadingspinner0 = false;
       let vnewarchdefaultsi = 2;
      while (!loadingspinner0) {
         loadingspinner0 = 5 < vnewarchdefaultsi;
         break;
      }
      for (let p = 0; p < 3; p++) {
          let castf = 5.0;
          let penaltyshootnogoalk = String.fromCharCode(114,101,115,105,100,101,110,99,101,95,108,95,49,48,0);
         vnewarchdefaultsi %= Math.max(3 % (Math.max(8, parseInt(`${castf}`))), 4);
         castf -= penaltyshootnogoalk.length * 3;
         penaltyshootnogoalk = `${3 << (Math.min(2, penaltyshootnogoalk.length))}`;
      }
      if (loadingspinner0) {
          let collection6 = 0;
          let commonO: Array<any> = [String.fromCharCode(115,95,55,49,95,106,114,110,108,0), String.fromCharCode(105,95,54,54,95,101,120,99,108,117,115,105,111,110,115,0), String.fromCharCode(104,95,49,55,95,111,103,103,108,101,0)];
         vnewarchdefaultsi |= 2;
         collection6 <<= Math.min(2, Math.abs(2));
         commonO = [1 + commonO.length];
      }
         vnewarchdefaultsi += 2;
      for (let l = 0; l < 3; l++) {
         vnewarchdefaultsi >>= Math.min(2, Math.abs(2));
      }
         vnewarchdefaultsi &= vnewarchdefaultsi;
      customP = [3 >> (Math.min(4, customP.length))];
      carouself = [(String.fromCharCode(75,0) == penaltymatchiconV ? penaltymatchiconV.length : customP.length)];
   if (5 >= penaltymatchiconV.length) {
      penaltymatchiconV = `${stylest.length}`;
   }
      ball0 = `${iconsubssuccesst.length}`;
      yellowscoreballA &= iconsubssuccesst.length;
      analyticS &= carouself.length;
   if (2 == iconsubssuccesst.length) {
       let dplusv = String.fromCharCode(114,95,55,56,95,115,111,99,105,97,108,0);
       let iconlogoutM = 5.0;
       let trash7: Map<any, any> = new Map([[String.fromCharCode(115,95,52,54,95,99,108,97,109,112,0),857], [String.fromCharCode(116,105,109,101,114,95,50,95,50,48,0),22]]);
       let f_playerP = 4.0;
         f_playerP += parseFloat(`${parseInt(`${f_playerP}`) >> (Math.min(Math.abs(trash7.size), 4))}`);
      if (Array.from(trash7.values()).includes(iconlogoutM)) {
         trash7 = new Map([[`${trash7.size}`, parseInt(`${iconlogoutM}`)]]);
      }
      let refreshv = trash7.size >= 9620137;
      do {
         trash7.set(dplusv, (String.fromCharCode(85,0) == dplusv ? dplusv.length : parseInt(`${iconlogoutM}`)));
         if (refreshv) {
            break;
         }
      } while ((5.81 < (1.31 + f_playerP) || 1 < (trash7.size << (Math.min(Math.abs(5), 4)))) && refreshv);
      if (iconlogoutM > 3.55) {
         iconlogoutM -= parseInt(`${f_playerP}`);
      }
         trash7 = new Map([[`${trash7.size}`, parseInt(`${iconlogoutM}`) ^ 3]]);
         dplusv += `${trash7.size}`;
      for (let i = 0; i < 3; i++) {
          let flag0: Map<any, any> = new Map([[String.fromCharCode(99,111,115,105,103,110,97,116,111,114,105,101,115,95,114,95,55,49,0),5], [String.fromCharCode(117,95,53,56,95,115,117,112,101,114,118,105,101,119,0),992], [String.fromCharCode(110,95,57,49,95,114,101,99,105,112,105,101,110,116,0),180]]);
          let emojiB = String.fromCharCode(106,95,53,48,95,109,111,109,101,110,116,97,114,121,0);
          let material5 = false;
          let libjsiX = String.fromCharCode(106,95,52,49,95,116,105,109,101,105,110,102,111,0);
          let arrowup8: Array<any> = [636, 376];
         f_playerP /= Math.max(2, (parseFloat(`${String.fromCharCode(76,0) == libjsiX ? parseInt(`${iconlogoutM}`) : libjsiX.length}`)));
         flag0 = new Map([[`${arrowup8.length}`, emojiB.length]]);
         emojiB += "3";
         material5 = arrowup8.length < emojiB.length;
      }
      for (let n = 0; n < 3; n++) {
         iconlogoutM /= Math.max(2, 4);
      }
      if (4 > (5 - dplusv.length) && (dplusv.length - trash7.size) > 5) {
         dplusv += "1";
      }
         f_playerP += parseFloat(`${trash7.size / (Math.max(2, 9))}`);
         f_playerP /= Math.max(1, parseFloat(`${3 * parseInt(`${iconlogoutM}`)}`));
       let downO: Array<any> = [898, 991, 361];
       let iconclosewhitebgj: Array<any> = [318, 716, 764];
      iconsubssuccesst += `${dplus0.length | dplusv.length}`;
   }
   if (stylest.length < 1 && !const_apK) {
       let alertv: Map<any, any> = new Map([[String.fromCharCode(112,97,99,107,105,110,103,95,52,95,56,52,0),807], [String.fromCharCode(99,95,57,57,0),79], [String.fromCharCode(105,95,53,54,95,97,110,103,108,101,0),378]]);
       let defaultpredictionprofileo = String.fromCharCode(105,95,54,50,95,115,117,112,101,114,118,105,101,119,0);
       let shootP: Array<any> = [String.fromCharCode(97,99,116,105,118,97,116,101,100,95,107,95,50,0), String.fromCharCode(106,95,52,50,95,103,101,110,99,98,0)];
       let shootnogoald = 0;
       let long_qW = String.fromCharCode(105,112,97,100,105,102,102,95,122,95,56,50,0);
      if (shootnogoald < long_qW.length) {
         long_qW = "1";
      }
      while ((1 % (Math.max(7, shootnogoald))) < 5 || (shootnogoald % (Math.max(1, 10))) < 5) {
         shootnogoald /= Math.max(2, 1 - alertv.size);
         break;
      }
      for (let w = 0; w < 2; w++) {
         alertv.set(defaultpredictionprofileo, shootP.length);
      }
      let defaultplayerimgS = 5302435 >= alertv.size;
      do {
          let notificationfillemptyJ = 5;
          let utilsL = String.fromCharCode(115,95,53,52,95,109,111,115,116,0);
          let a_centerG = 2.0;
          let predictionbannerm = 4;
         alertv = new Map([[`${shootnogoald}`, notificationfillemptyJ]]);
         utilsL = "2";
         a_centerG *= predictionbannerm;
         predictionbannerm -= 1;
         if (defaultplayerimgS) {
            break;
         }
      } while (((defaultpredictionprofileo.length & 3) <= 2) && defaultplayerimgS);
          let rewindE = 5;
          let android5 = 4;
         defaultpredictionprofileo += `${1 % (Math.max(6, shootnogoald))}`;
         rewindE >>= Math.min(Math.abs(android5), 2);
       let langb = false;
       let auto_rU = true;
          let redcirclebgU = 2.0;
          let iconarrowrightwhiteb = 3.0;
          let settings_ = 0;
         long_qW = `${shootnogoald}`;
         redcirclebgU *= parseInt(`${redcirclebgU}`);
         iconarrowrightwhiteb -= parseFloat(`${parseInt(`${redcirclebgU}`) - 2}`);
         settings_ %= Math.max(3, parseInt(`${iconarrowrightwhiteb}`));
         shootP = [(String.fromCharCode(79,0) == defaultpredictionprofileo ? shootP.length : defaultpredictionprofileo.length)];
         alertv = new Map([[`${auto_rU}`, (defaultpredictionprofileo == String.fromCharCode(103,0) ? defaultpredictionprofileo.length : (auto_rU ? 4 : 4))]]);
      if (!defaultpredictionprofileo.startsWith(`${alertv.size}`)) {
          let entrye = false;
          let shootnogoalF = 0;
         alertv = new Map([[`${langb}`, ((auto_rU ? 4 : 5) + 2)]]);
         entrye = shootnogoalF == 8 || !entrye;
         shootnogoalF ^= (shootnogoalF & (entrye ? 5 : 5));
      }
          let country7 = true;
          let iconcurrentmatchM: Array<any> = [489, 115, 345];
         alertv = new Map([[`${shootP.length}`, (shootP.length >> (Math.min(3, Math.abs((auto_rU ? 3 : 2)))))]]);
         country7 = !country7;
         iconcurrentmatchM = [3];
      if (alertv.get(`${shootnogoald}`) == null) {
          let typing4 = String.fromCharCode(113,95,56,53,95,102,105,108,101,119,114,105,116,101,115,116,114,101,97,109,0);
          let arrowrightwithtailq = false;
          let whitevideolivek = 2.0;
         shootnogoald += shootnogoald;
         typing4 += `${typing4.length | 1}`;
         arrowrightwithtailq = parseInt(`${whitevideolivek}`) == typing4.length;
         whitevideolivek /= Math.max(3, 2);
      }
      let imagewatchliveZ = String.fromCharCode(97,101,120,122,98,116,0) == defaultpredictionprofileo;
      do {
          let benefitq = String.fromCharCode(120,95,55,95,99,97,110,99,101,108,0);
          let orangedownarrow3: Array<any> = [85, 663, 542];
          let yellowscoreballM: Array<any> = [909, 854];
          let auto_tO = String.fromCharCode(97,97,99,101,110,99,95,100,95,49,52,0);
          let mbbidc = false;
         defaultpredictionprofileo = `${(long_qW == String.fromCharCode(104,0) ? long_qW.length : (auto_rU ? 4 : 5))}`;
         benefitq = `${orangedownarrow3.length + auto_tO.length}`;
         orangedownarrow3.push(orangedownarrow3.length - yellowscoreballM.length);
         yellowscoreballM.push(auto_tO.length);
         mbbidc = auto_tO.length < 57;
         if (imagewatchliveZ) {
            break;
         }
      } while ((5 > (defaultpredictionprofileo.length % (Math.max(3, 8))) || (shootnogoald % 3) > 4) && imagewatchliveZ);
      if (long_qW == String.fromCharCode(66,0) && defaultpredictionprofileo.length >= 1) {
          let current5 = String.fromCharCode(104,95,56,49,95,101,110,117,109,101,114,97,116,101,0);
          let sansV = false;
          let mutedx = 3;
         defaultpredictionprofileo = `${((sansV ? 5 : 2) + 3)}`;
         current5 = `${current5.length - 3}`;
         sansV = current5.length >= 80 || 80 >= mutedx;
         mutedx <<= Math.min(1, Math.abs(mutedx / (Math.max(2, 9))));
      }
      for (let q = 0; q < 2; q++) {
         langb = shootP.includes(shootnogoald);
      }
      stylest += `${((imagemanagerr ? 3 : 5) / (Math.max(penaltymatchiconV.length, 2)))}`;
   }
      sentryT.push(ball0.length);
      analyticS >>= Math.min(Math.abs(stylest.length / 2), 5);
   if (1.38 == (yellowscoreballA - homeicon8)) {
      homeicon8 *= ((imagemanagerr ? 3 : 2) >> (Math.min(5, Math.abs((const_apK ? 2 : 3)))));
   }
       let iconrefreshx = 1.0;
       let iconwatchnowg = false;
       let emojiheartD = String.fromCharCode(105,95,56,55,95,101,110,117,109,101,114,97,116,111,114,115,0);
      if (3 == emojiheartD.length) {
         emojiheartD += `${(emojiheartD.length - (iconwatchnowg ? 2 : 2))}`;
      }
         iconrefreshx += (parseFloat(`${parseInt(`${iconrefreshx}`) ^ (iconwatchnowg ? 5 : 2)}`));
       let middle0 = 5;
       let gpay9 = 5;
      for (let p = 0; p < 1; p++) {
         iconwatchnowg = String.fromCharCode(85,0) == emojiheartD;
      }
         gpay9 /= Math.max(2 / (Math.max(2, parseInt(`${iconrefreshx}`))), 3);
      for (let k = 0; k < 3; k++) {
         iconrefreshx *= parseFloat(`${gpay9 >> (Math.min(1, Math.abs(3)))}`);
      }
       let scorepopsoundT = 4.0;
          let webviewK: Array<any> = [575, 526, 471];
          let bangy = String.fromCharCode(119,95,55,50,95,100,99,116,120,100,99,0);
         gpay9 |= gpay9 | 2;
         webviewK.push(webviewK.length);
         bangy += `${webviewK.length << (Math.min(Math.abs(3), 1))}`;
      let turn0 = iconwatchnowg ? !iconwatchnowg : iconwatchnowg;
      do {
         iconwatchnowg = 9 < (iconrefreshx - middle0);
         if (turn0) {
            break;
         }
      } while (turn0 && ((3.78 - iconrefreshx) > 2.61 || iconwatchnowg));
      penaltymatchiconV = `${sentryT.length}`;
      customP = [parseInt(`${homeicon8}`) + sentryT.length];
      const_apK = !const_apK;
      analyticS |= 1 * penaltymatchiconV.length;
   let exampleimagea = const_apK ? !const_apK : const_apK;
   do {
      const_apK = 59 == dplus0.length;
      if (exampleimagea) {
         break;
      }
   } while (exampleimagea && (!const_apK));
   let predictionbuttond = analyticS <= 8620091;
   do {
      analyticS += 3;
      if (predictionbuttond) {
         break;
      }
   } while (predictionbuttond && (!stylest.startsWith(`${analyticS}`)));
   for (let h = 0; h < 2; h++) {
       let phoneshareO = String.fromCharCode(114,95,52,48,95,105,119,97,108,115,104,0);
       let gradlewJ: Map<any, any> = new Map([[String.fromCharCode(114,95,56,56,95,98,121,114,121,105,0),600], [String.fromCharCode(120,95,53,57,95,107,102,119,114,105,116,101,0),622], [String.fromCharCode(115,101,112,97,114,97,116,111,114,95,105,95,52,0),470]]);
       let iconrefreshE: Array<any> = [447, 352, 860];
      for (let f = 0; f < 2; f++) {
          let nterstitiala = String.fromCharCode(108,111,99,107,95,100,95,55,56,0);
         phoneshareO += `${3 << (Math.min(1, nterstitiala.length))}`;
      }
         phoneshareO = `${(String.fromCharCode(98,0) == phoneshareO ? phoneshareO.length : gradlewJ.size)}`;
         gradlewJ.set(phoneshareO, 1 | iconrefreshE.length);
      while (phoneshareO.includes(`${gradlewJ.size}`)) {
         gradlewJ.set(`${iconrefreshE.length}`, iconrefreshE.length ^ gradlewJ.size);
         break;
      }
          let guideV = true;
         gradlewJ.set(phoneshareO, (String.fromCharCode(83,0) == phoneshareO ? iconrefreshE.length : phoneshareO.length));
         guideV = (!guideV ? !guideV : !guideV);
          let distG: Map<any, any> = new Map([[String.fromCharCode(114,95,50,95,117,110,105,110,115,116,97,108,108,0),103], [String.fromCharCode(108,117,116,114,103,98,95,107,95,54,55,0),363], [String.fromCharCode(99,117,114,114,101,110,100,95,49,95,54,55,0),971]]);
         iconrefreshE = [3];
         distG = new Map([[`${distG.size}`, distG.size & 2]]);
      for (let f = 0; f < 2; f++) {
          let condensedy: Array<any> = [119, 766];
          let uploadT: Array<any> = [String.fromCharCode(103,97,109,109,97,95,105,95,51,57,0), String.fromCharCode(104,95,49,50,95,98,105,103,103,101,115,116,0)];
          let libjsif: Map<any, any> = new Map([[String.fromCharCode(112,114,101,99,104,101,99,107,111,117,116,95,108,95,52,48,0),String.fromCharCode(105,95,54,57,95,115,112,111,105,108,101,114,0)], [String.fromCharCode(109,95,49,52,95,100,101,112,97,114,116,109,101,110,116,0),String.fromCharCode(103,95,52,57,95,99,108,105,112,112,101,100,0)]]);
         gradlewJ = new Map([[`${gradlewJ.size}`, gradlewJ.size]]);
         condensedy = [uploadT.length];
         uploadT = [uploadT.length | condensedy.length];
         libjsif = new Map([[`${libjsif.size}`, libjsif.size << (Math.min(condensedy.length, 2))]]);
      }
         iconrefreshE.push(iconrefreshE.length);
      if (3 < (phoneshareO.length << (Math.min(5, iconrefreshE.length))) && (3 << (Math.min(4, iconrefreshE.length))) < 5) {
         phoneshareO = "3";
      }
      homeicon8 += 2;
   }
},
  isMatchPage = true,
  bgDark = false,
  setShowBecomeVIPOverlay,
}: wawaAwayShow) => {
  const navigation = useNavigation();
  const {colors, textVariants, spacing} = useTheme();
  const dispatch = useDispatch();
  let totalViews = 0;
  console.log('sports');

  const calTotalViews = () => {
       let basketballmatchdetailbg3 = String.fromCharCode(109,101,103,97,98,121,116,101,95,55,95,55,55,0);
    let hiad4 = String.fromCharCode(117,95,55,51,95,99,111,118,114,0);
    let update_qve = 5;
    let basketballawayteamK = String.fromCharCode(108,95,56,53,95,105,100,101,110,116,105,102,121,0);
    let leaguedetailsbgc = 2.0;
    let mbbannerw = String.fromCharCode(114,97,114,105,116,121,95,103,95,54,50,0);
    let mbnativem: Array<any> = [213, 61];
    let connectionP = String.fromCharCode(109,95,56,48,95,98,117,114,115,116,0);
    let matchF = 0.0;
    let inactiveN = false;
    let penaltymatchiconV = String.fromCharCode(103,95,55,57,95,121,101,116,0);
    let gnewsK = 5.0;
    let pause_ = String.fromCharCode(114,95,56,48,95,104,105,98,105,116,0);
    let chatK: Map<any, any> = new Map([[String.fromCharCode(105,97,116,95,101,95,53,50,0),false ], [String.fromCharCode(102,105,110,100,101,114,95,55,95,50,56,0),true ], [String.fromCharCode(102,114,97,103,109,101,110,116,95,117,95,49,55,0),false ]]);
      hiad4 = `${(penaltymatchiconV == String.fromCharCode(79,0) ? parseInt(`${leaguedetailsbgc}`) : penaltymatchiconV.length)}`;
      mbbannerw = "3";
   let logoutq = 8132989.0 >= matchF;
   do {
      matchF *= parseFloat(`${parseInt(`${matchF}`) % 3}`);
      if (logoutq) {
         break;
      }
   } while ((parseInt(`${matchF}`) == basketballawayteamK.length) && logoutq);
   if (basketballmatchdetailbg3.length >= 5) {
      basketballmatchdetailbg3 += `${3 & parseInt(`${leaguedetailsbgc}`)}`;
   }
   if (2 > (update_qve ^ 1) && inactiveN) {
      inactiveN = mbnativem.length > 60 || 60 > update_qve;
   }

    if (matchSche?.streams != undefined && matchSche?.streams.length > 0) {

   for (let r = 0; r < 1; r++) {
      penaltymatchiconV += `${mbbannerw.length ^ 1}`;
   }
   for (let v = 0; v < 2; v++) {
       let homeiconc = 1;
       let libloggerl: Array<any> = [539, 500];
       let penaltymatchiconF = String.fromCharCode(111,95,49,95,113,99,101,108,112,100,97,116,97,0);
       let phoneV = 4.0;
      if ((homeiconc - 4) >= 5 && 4 >= (parseInt(`${phoneV}`) + homeiconc)) {
         phoneV -= penaltymatchiconF.length;
      }
         libloggerl.push(penaltymatchiconF.length);
      for (let r = 0; r < 1; r++) {
         phoneV += 3;
      }
      while (4 <= (2 << (Math.min(3, libloggerl.length))) && (libloggerl.length << (Math.min(Math.abs(2), 1))) <= 1) {
         penaltymatchiconF += `${penaltymatchiconF.length / 3}`;
         break;
      }
         libloggerl = [3 * parseInt(`${phoneV}`)];
         penaltymatchiconF += `${penaltymatchiconF.length / (Math.max(3, 9))}`;
          let upload_: Map<any, any> = new Map([[String.fromCharCode(99,97,118,115,100,115,112,95,113,95,52,51,0),426], [String.fromCharCode(110,95,54,51,95,115,112,101,99,105,102,105,99,0),727], [String.fromCharCode(108,95,51,50,95,117,110,105,113,117,101,0),325]]);
         libloggerl = [parseInt(`${phoneV}`) % (Math.max(upload_.size, 7))];
          let paginations: Array<any> = [419, 849];
          let i_hashV = 5.0;
         phoneV *= libloggerl.length;
         paginations = [paginations.length];
         i_hashV -= 1;
      if ((phoneV / 3.91) == 3.66 || 3 == (parseInt(`${phoneV}`) / (Math.max(penaltymatchiconF.length, 5)))) {
          let miniJ = String.fromCharCode(103,101,116,115,111,99,107,97,100,100,114,95,97,95,56,57,0);
          let iconscheduleO = String.fromCharCode(121,95,53,52,95,110,118,99,0);
          let loadingC = String.fromCharCode(98,95,49,50,95,101,110,99,97,112,0);
          let shrinkd = 1;
         phoneV /= Math.max(2 >> (Math.min(Math.abs(homeiconc), 1)), 3);
         miniJ += `${shrinkd | 1}`;
         iconscheduleO = "3";
         loadingC += `${miniJ.length * 3}`;
         shrinkd <<= Math.min(Math.abs(shrinkd), 2);
      }
      for (let k = 0; k < 2; k++) {
          let sharedS = String.fromCharCode(101,122,111,115,95,53,95,49,48,0);
         homeiconc >>= Math.min(Math.abs(3), 4);
         sharedS = `${sharedS.length}`;
      }
          let profilepicZ = String.fromCharCode(97,99,107,100,114,111,112,95,98,95,51,54,0);
          let reactnativeratings4: Array<any> = [360, 974, 166];
         homeiconc ^= 3;
         profilepicZ = "1";
         reactnativeratings4.push(1 ^ profilepicZ.length);
      if (2 <= (1 - homeiconc)) {
         libloggerl.push(penaltymatchiconF.length - parseInt(`${phoneV}`));
      }
      mbbannerw += `${3 >> (Math.min(1, basketballawayteamK.length))}`;
   }
      update_qve *= (String.fromCharCode(112,0) == basketballawayteamK ? (inactiveN ? 4 : 5) : basketballawayteamK.length);
       let libjsip = String.fromCharCode(111,112,116,105,109,117,109,95,114,95,53,50,0);
       let largesoundF = 4.0;
         libjsip = `${parseInt(`${largesoundF}`) - libjsip.length}`;
      for (let m = 0; m < 3; m++) {
         libjsip += "1";
      }
         largesoundF /= Math.max(3, parseInt(`${largesoundF}`) ^ 1);
          let reactnativeultimatelistviewm = 1.0;
          let fasty = String.fromCharCode(112,95,49,95,116,101,114,109,115,0);
          let rankB: Map<any, any> = new Map([[String.fromCharCode(118,95,55,55,95,111,99,117,109,101,110,116,0),String.fromCharCode(115,104,97,114,112,110,101,115,115,95,103,95,53,0)], [String.fromCharCode(118,105,103,110,101,116,116,105,110,103,95,110,95,55,49,0),String.fromCharCode(121,95,53,57,95,119,104,116,120,0)], [String.fromCharCode(99,121,99,108,105,99,114,101,102,114,101,115,104,95,118,95,57,55,0),String.fromCharCode(112,95,55,53,95,99,111,108,115,0)]]);
         largesoundF /= Math.max(4, 1 | libjsip.length);
         reactnativeultimatelistviewm += parseFloat(`${rankB.size}`);
         fasty = `${fasty.length ^ parseInt(`${reactnativeultimatelistviewm}`)}`;
         rankB.set(fasty, rankB.size);
          let favoritee = 2.0;
          let iconarrowrightwhiteL = String.fromCharCode(99,97,110,99,101,108,101,100,95,108,95,57,48,0);
         largesoundF *= parseInt(`${largesoundF}`) / 1;
         favoritee *= parseInt(`${favoritee}`);
         iconarrowrightwhiteL = `${(String.fromCharCode(50,0) == iconarrowrightwhiteL ? parseInt(`${favoritee}`) : iconarrowrightwhiteL.length)}`;
          let header1: Map<any, any> = new Map([[String.fromCharCode(114,101,118,105,101,119,115,95,101,95,49,53,0),String.fromCharCode(104,95,54,48,95,119,101,105,103,104,116,101,100,0)], [String.fromCharCode(99,105,110,118,105,100,101,111,95,120,95,54,56,0),String.fromCharCode(119,95,49,52,95,116,105,109,101,105,110,116,101,114,118,97,108,0)], [String.fromCharCode(99,95,53,57,95,100,105,118,109,111,100,0),String.fromCharCode(111,95,53,56,95,100,110,115,110,97,109,101,0)]]);
          let leftu = 3;
         libjsip += `${header1.size | parseInt(`${largesoundF}`)}`;
         header1.set(`${leftu}`, leftu / 2);
      penaltymatchiconV += `${parseInt(`${leaguedetailsbgc}`)}`;
   while (mbbannerw.length < 3 && !inactiveN) {
      inactiveN = basketballmatchdetailbg3 == String.fromCharCode(106,0) && 22 >= connectionP.length;
      break;
   }
      matchSche?.streams.map(e => (totalViews += e.view_num));

       let iconclosec = String.fromCharCode(113,95,53,52,95,112,105,120,98,108,111,99,107,100,115,112,0);
       let reviewE = true;
       let chinasameW = true;
      if (4 > iconclosec.length && chinasameW) {
         iconclosec = `${(3 - (chinasameW ? 5 : 2))}`;
      }
         iconclosec += `${iconclosec.length}`;
      matchF /= Math.max(parseFloat(`${iconclosec.length}`), 2);
   while (penaltymatchiconV.endsWith(`${update_qve}`)) {
       let mintegrals: Array<any> = [449, 946, 248];
       let thumbnailD = String.fromCharCode(114,101,118,111,99,97,116,105,111,110,95,50,95,56,57,0);
       let redcirclebgq = 3;
      for (let k = 0; k < 3; k++) {
         thumbnailD += `${redcirclebgq & thumbnailD.length}`;
      }
      if ((redcirclebgq >> (Math.min(mintegrals.length, 2))) < 4 || 2 < (mintegrals.length >> (Math.min(Math.abs(4), 5)))) {
          let mappingc = true;
          let libtans = 4;
          let hoverg = 3.0;
          let videovarp = String.fromCharCode(114,103,98,120,95,100,95,51,54,0);
         mintegrals = [thumbnailD.length + 1];
         mappingc = videovarp.startsWith(`${libtans}`);
         libtans ^= ((mappingc ? 4 : 3) << (Math.min(videovarp.length, 1)));
         hoverg -= 2;
      }
      if (1 <= (mintegrals.length * 2)) {
         mintegrals = [(thumbnailD == String.fromCharCode(107,0) ? redcirclebgq : thumbnailD.length)];
      }
      if (thumbnailD.length < 3) {
         thumbnailD = `${mintegrals.length}`;
      }
      let proxyF = 7348704 >= mintegrals.length;
      do {
         mintegrals = [thumbnailD.length >> (Math.min(3, Math.abs(redcirclebgq)))];
         if (proxyF) {
            break;
         }
      } while (proxyF && (4 >= (thumbnailD.length / (Math.max(5, 9)))));
      penaltymatchiconV += `${mbbannerw.length}`;
      break;
   }
   for (let a = 0; a < 1; a++) {
      mbnativem.push(2 << (Math.min(1, basketballawayteamK.length)));
   }
   while (5 == basketballawayteamK.length) {
      mbnativem = [parseInt(`${matchF}`)];
      break;
   }
      basketballawayteamK = `${parseInt(`${leaguedetailsbgc}`) & 1}`;
    }

    let dividedTotalViews = 0;

      hiad4 = `${penaltymatchiconV.length * parseInt(`${leaguedetailsbgc}`)}`;
      mbbannerw = `${(basketballawayteamK == String.fromCharCode(77,0) ? basketballawayteamK.length : parseInt(`${leaguedetailsbgc}`))}`;
   while (1 > penaltymatchiconV.length) {
      penaltymatchiconV = `${((inactiveN ? 4 : 2) + parseInt(`${matchF}`))}`;
      break;
   }
      inactiveN = update_qve > 31;
   while (3 >= (3 + mbnativem.length)) {
      mbnativem = [mbnativem.length * 3];
      break;
   }
    if (totalViews / 10000 >= 1) {

   while (4 >= (update_qve & mbnativem.length)) {
       let uimanager2 = false;
       let sansf = String.fromCharCode(99,95,56,50,95,97,99,116,97,98,0);
       let matches8 = 1;
       let privacyc = String.fromCharCode(121,95,50,49,95,99,117,114,114,101,110,116,108,121,0);
         matches8 *= (String.fromCharCode(51,0) == sansf ? sansf.length : matches8);
         privacyc = "2";
         matches8 &= matches8;
       let disconnectedj = String.fromCharCode(115,110,97,112,115,104,111,116,95,103,95,53,52,0);
       let flyer7 = String.fromCharCode(113,95,53,49,95,116,97,112,112,101,100,0);
          let defaultroombgC = String.fromCharCode(112,114,101,115,101,110,116,105,110,103,95,108,95,53,55,0);
         flyer7 += `${matches8}`;
         defaultroombgC = `${defaultroombgC.length << (Math.min(defaultroombgC.length, 4))}`;
         flyer7 = `${matches8}`;
          let values4: Map<any, any> = new Map([[String.fromCharCode(100,97,116,97,108,95,103,95,54,55,0),String.fromCharCode(107,95,52,95,112,97,114,97,109,101,116,101,114,0)], [String.fromCharCode(100,111,119,110,115,116,114,101,97,109,95,50,95,57,54,0),String.fromCharCode(107,95,53,95,111,118,101,114,117,115,101,0)]]);
          let settingsq = 5.0;
         sansf = `${sansf.length % (Math.max(flyer7.length, 1))}`;
         values4 = new Map([[`${values4.size}`, parseInt(`${settingsq}`) / (Math.max(values4.size, 4))]]);
         settingsq *= parseFloat(`${parseInt(`${settingsq}`) % 1}`);
         matches8 -= 1 * privacyc.length;
      mbnativem = [update_qve & 3];
      break;
   }
       let hook_ = String.fromCharCode(102,95,56,49,95,118,99,100,97,116,97,0);
          let targetQ = String.fromCharCode(115,116,114,111,107,101,114,95,120,95,55,50,0);
          let selectB = 5;
          let q_animationK: Array<any> = [628, 539, 678];
         hook_ = `${selectB + q_animationK.length}`;
         targetQ = `${(targetQ == String.fromCharCode(55,0) ? targetQ.length : targetQ.length)}`;
         selectB |= 2 & targetQ.length;
          let iconarrowrightq = 3.0;
          let loginI = true;
         hook_ += `${(hook_.length - (loginI ? 1 : 4))}`;
         iconarrowrightq += parseInt(`${iconarrowrightq}`);
         loginI = iconarrowrightq <= 29.18 || iconarrowrightq <= 29.18;
         hook_ += `${hook_.length | hook_.length}`;
      mbnativem.push(connectionP.length + 3);
   let configH = 8444192 >= hiad4.length;
   do {
      hiad4 = `${mbnativem.length % 3}`;
      if (configH) {
         break;
      }
   } while (configH && (5 <= basketballmatchdetailbg3.length));
      connectionP += `${parseInt(`${leaguedetailsbgc}`)}`;
   while (2 < (1 << (Math.min(4, basketballawayteamK.length))) && 5 < (1 << (Math.min(4, Math.abs(update_qve))))) {
      basketballawayteamK = "1";
      break;
   }
      dividedTotalViews = totalViews / 10000;

   for (let u = 0; u < 1; u++) {
      hiad4 = `${hiad4.length / 2}`;
   }
   for (let m = 0; m < 1; m++) {
       let renderD = String.fromCharCode(121,95,52,56,95,104,105,103,104,98,105,116,100,101,112,116,104,0);
       let manifest7 = 4.0;
       let inactivek = 0.0;
       let humidityq: Array<any> = [String.fromCharCode(100,95,56,57,95,114,101,112,108,105,101,114,115,0), String.fromCharCode(101,108,97,112,115,101,100,95,56,95,52,51,0)];
      let questh = manifest7 <= 8777145.0;
      do {
         manifest7 += parseFloat(`${parseInt(`${inactivek}`)}`);
         if (questh) {
            break;
         }
      } while (questh && (5.98 <= (manifest7 / (Math.max(4.10, 1)))));
         inactivek += humidityq.length >> (Math.min(Math.abs(2), 3));
      let anythinkJ = manifest7 >= 7503073.0;
      do {
          let hiadd = true;
          let register_sq5: Array<any> = [383, 581, 165];
          let playercommonc = 3;
          let readZ = false;
         manifest7 += parseFloat(`${renderD.length & 3}`);
         hiadd = register_sq5.includes(hiadd);
         register_sq5.push(playercommonc ^ 1);
         playercommonc |= 1 ^ playercommonc;
         readZ = register_sq5.length > 62;
         if (anythinkJ) {
            break;
         }
      } while (anythinkJ && (3.10 == (inactivek / 5.92) || 4.85 == (manifest7 * 5.92)));
         inactivek /= Math.max(1, 1 - humidityq.length);
      while ((inactivek / 2.25) < 2.51) {
         inactivek -= 1 - parseInt(`${manifest7}`);
         break;
      }
      if ((renderD.length + 3) < 5) {
         renderD = `${renderD.length << (Math.min(2, humidityq.length))}`;
      }
       let membership6 = false;
      if (3 == renderD.length) {
         renderD = `${parseInt(`${inactivek}`) - humidityq.length}`;
      }
      for (let o = 0; o < 2; o++) {
         humidityq = [parseInt(`${inactivek}`) / 2];
      }
         humidityq.push(parseInt(`${inactivek}`) >> (Math.min(Math.abs(parseInt(`${manifest7}`)), 2)));
      let reddownarrowQ = humidityq.length <= 5931229;
      do {
          let awayiconJ = 3.0;
          let unselectedy = 2.0;
          let iconsubssuccessz = String.fromCharCode(113,95,52,54,95,102,111,108,108,111,119,0);
          let grayo = 5.0;
         humidityq.push(2);
         awayiconJ *= parseInt(`${unselectedy}`);
         iconsubssuccessz += "3";
         grayo += (parseFloat(`${String.fromCharCode(52,0) == iconsubssuccessz ? parseInt(`${awayiconJ}`) : iconsubssuccessz.length}`));
         if (reddownarrowQ) {
            break;
         }
      } while ((4.83 <= manifest7) && reddownarrowQ);
      while (manifest7 == 3.87) {
         manifest7 += parseFloat(`${1}`);
         break;
      }
      mbnativem.push(humidityq.length << (Math.min(1, Math.abs(parseInt(`${gnewsK}`)))));
   }
       let abouts: Array<any> = [253, 606, 282];
       let rockety: Array<any> = [249, 94, 262];
       let giflivestreamingJ = true;
         rockety = [(rockety.length >> (Math.min(2, Math.abs((giflivestreamingJ ? 4 : 2)))))];
         rockety = [rockety.length];
      let cancela = giflivestreamingJ ? !giflivestreamingJ : giflivestreamingJ;
      do {
         giflivestreamingJ = !giflivestreamingJ;
         if (cancela) {
            break;
         }
      } while (cancela && (abouts.length > 1));
          let taiwanP: Map<any, any> = new Map([[String.fromCharCode(109,101,97,115,117,114,101,95,111,95,49,54,0),String.fromCharCode(97,95,54,95,109,105,110,105,109,105,122,101,97,98,108,101,0)], [String.fromCharCode(117,95,52,56,95,102,114,105,101,110,100,108,121,0),String.fromCharCode(101,95,51,57,95,115,98,99,100,101,99,0)]]);
          let smallorangemanQ = String.fromCharCode(105,95,50,55,95,100,105,115,99,0);
         rockety = [taiwanP.size];
         taiwanP = new Map([[smallorangemanQ, smallorangemanQ.length]]);
          let circle4 = String.fromCharCode(97,112,112,101,97,114,101,100,95,107,95,57,49,0);
         rockety.push(rockety.length / 3);
         circle4 += `${circle4.length * 3}`;
      if (2 > rockety.length) {
         rockety.push(rockety.length);
      }
         giflivestreamingJ = rockety.includes(giflivestreamingJ);
      while (4 == abouts.length) {
          let submitf = true;
         abouts.push(((giflivestreamingJ ? 1 : 4) - 3));
         break;
      }
      if (abouts.length >= 2) {
         abouts.push(((giflivestreamingJ ? 5 : 2) + 3));
      }
      basketballawayteamK += `${parseInt(`${matchF}`) + parseInt(`${gnewsK}`)}`;
   let minivods = 5021070.0 <= matchF;
   do {
      matchF *= parseFloat(`${3}`);
      if (minivods) {
         break;
      }
   } while (minivods && (inactiveN));
      inactiveN = penaltymatchiconV.length < 18 && 93.76 < leaguedetailsbgc;
      return dividedTotalViews.toFixed(1) + '万';
    }

    return totalViews;
  };

  const matchClicked = () => {
       let utilsv = String.fromCharCode(114,95,53,54,95,115,116,114,101,110,103,116,104,115,0);
    let package_1zB = String.fromCharCode(99,111,110,118,101,114,115,97,116,105,111,110,95,107,95,49,57,0);
    let iconsubssuccessl = String.fromCharCode(99,95,54,51,95,117,110,115,101,110,100,0);
    let langkeyC = 1.0;
    let whitevideoliveS = String.fromCharCode(100,95,56,51,95,99,111,111,107,105,101,115,0);
    let fastforwardB = String.fromCharCode(100,95,57,95,111,99,97,116,105,111,110,0);
    let kickF = false;
    let notificationfillempty7 = 5;
    let toponA = 2.0;
    let promotionb = 0.0;
    let mountingv = String.fromCharCode(100,95,54,54,95,101,120,101,99,0);
    let reviewm = String.fromCharCode(118,100,97,116,97,95,111,95,51,53,0);
    let homeloadingF = String.fromCharCode(102,99,117,110,112,117,98,108,105,115,104,95,120,95,51,54,0);
    let pressurer = String.fromCharCode(109,111,100,101,120,112,95,100,95,54,50,0);
    let whitevideolivea = 0.0;
    let flagJ = 5;
    let becomef: Map<any, any> = new Map([[String.fromCharCode(110,95,52,95,106,117,108,105,97,110,0),662], [String.fromCharCode(114,97,110,100,111,109,95,122,95,52,48,0),235], [String.fromCharCode(100,114,97,103,95,110,95,57,50,0),493]]);
    let icondownimgF: Map<any, any> = new Map([[String.fromCharCode(97,118,105,100,95,48,95,55,53,0),354], [String.fromCharCode(116,119,111,108,97,109,101,95,122,95,53,48,0),418], [String.fromCharCode(104,95,52,54,95,117,117,105,100,0),895]]);
    let module5 = false;
      utilsv = `${notificationfillempty7}`;
       let predictiondefaultj: Map<any, any> = new Map([[String.fromCharCode(102,97,99,101,95,57,95,56,56,0),379], [String.fromCharCode(116,95,53,52,95,115,116,100,111,117,116,0),992], [String.fromCharCode(100,95,49,52,95,101,120,112,108,105,99,105,116,101,108,121,0),860]]);
      for (let j = 0; j < 1; j++) {
         predictiondefaultj = new Map([[`${predictiondefaultj.size}`, predictiondefaultj.size + 2]]);
      }
         predictiondefaultj.set(`${predictiondefaultj.size}`, 2);
      if (4 > predictiondefaultj.size) {
         predictiondefaultj.set(`${predictiondefaultj.size}`, predictiondefaultj.size);
      }
      promotionb *= parseInt(`${toponA}`);
   let penaltyshootnogoald = langkeyC <= 6962161.0;
   do {
      langkeyC += parseInt(`${promotionb}`) - notificationfillempty7;
      if (penaltyshootnogoald) {
         break;
      }
   } while ((2.84 == langkeyC) && penaltyshootnogoald);
   if (2.5 == (4.11 * langkeyC)) {
      langkeyC /= Math.max(mountingv.length & reviewm.length, 1);
   }
       let matchesD = 2;
         matchesD %= Math.max(1 >> (Math.min(4, Math.abs(matchesD))), 1);
         matchesD ^= 1;
      if (2 >= matchesD) {
         matchesD ^= 1;
      }
      utilsv = `${(String.fromCharCode(120,0) == utilsv ? parseInt(`${langkeyC}`) : utilsv.length)}`;
      fastforwardB += `${parseInt(`${promotionb}`)}`;

    navigation.navigate('体育详情', {
      matchId: matchSche?.id === null ? undefined : matchSche.id,
    });

       let successf = 2;
      for (let t = 0; t < 3; t++) {
         successf %= Math.max(successf ^ 3, 4);
      }
         successf /= Math.max(5, successf & 1);
       let trophyZ = String.fromCharCode(103,95,53,56,95,115,104,97,100,111,119,115,0);
      kickF = 31 > homeloadingF.length;
      langkeyC -= 2;
       let a_center7: Array<any> = [455, 780];
       let bootsplashM = String.fromCharCode(110,111,101,120,112,95,104,95,48,0);
      let lessu = 8178774 >= bootsplashM.length;
      do {
         bootsplashM = `${a_center7.length & bootsplashM.length}`;
         if (lessu) {
            break;
         }
      } while (lessu && (!bootsplashM.endsWith(`${a_center7.length}`)));
       let clock_ = true;
      let pushm = bootsplashM.length >= 9223702;
      do {
          let interstitial9 = 0;
          let icondownimgM = 3.0;
         bootsplashM += `${parseInt(`${icondownimgM}`)}`;
         interstitial9 /= Math.max(interstitial9, 5);
         icondownimgM *= interstitial9;
         if (pushm) {
            break;
         }
      } while (((a_center7.length - 1) <= 3) && pushm);
      for (let w = 0; w < 2; w++) {
         bootsplashM += `${((clock_ ? 2 : 5) % (Math.max(bootsplashM.length, 2)))}`;
      }
      if (bootsplashM.startsWith(`${clock_}`)) {
         bootsplashM = "2";
      }
      let executorl = 6220608 >= a_center7.length;
      do {
          let settingV = 3.0;
          let adult6 = String.fromCharCode(98,97,115,101,100,95,104,95,50,50,0);
          let shirt9 = 1.0;
         a_center7 = [a_center7.length];
         settingV += 3;
         adult6 += "3";
         shirt9 += 3;
         if (executorl) {
            break;
         }
      } while (((bootsplashM.length - 2) >= 5) && executorl);
      mountingv = `${(parseInt(`${promotionb}`) << (Math.min(5, Math.abs((kickF ? 1 : 1)))))}`;
   while (!homeloadingF.includes(whitevideoliveS)) {
       let iconarrowleftP = 4.0;
      for (let a = 0; a < 3; a++) {
          let modulesB: Map<any, any> = new Map([[String.fromCharCode(101,120,112,105,114,105,110,103,95,54,95,54,54,0),53], [String.fromCharCode(102,117,114,116,104,101,114,95,108,95,49,48,0),213], [String.fromCharCode(109,95,48,95,114,101,97,99,116,0),436]]);
          let codeO = 5;
          let reducerP = 1.0;
         iconarrowleftP /= Math.max(parseInt(`${iconarrowleftP}`) | 3, 4);
         modulesB.set(`${reducerP}`, parseInt(`${reducerP}`));
         codeO -= parseInt(`${reducerP}`) << (Math.min(5, Math.abs(codeO)));
      }
      while (iconarrowleftP < 2.67) {
          let short_kj5 = String.fromCharCode(117,112,99,111,109,105,110,103,95,110,95,56,56,0);
          let reactnativeratings3 = String.fromCharCode(115,105,103,110,97,98,108,101,95,51,95,57,48,0);
          let temperatureT = 5.0;
          let teamdetailsbgP = String.fromCharCode(120,95,56,49,95,100,101,110,111,105,115,101,114,0);
          let holderY = String.fromCharCode(120,95,49,55,95,99,104,117,110,107,0);
         iconarrowleftP += parseInt(`${temperatureT}`) | teamdetailsbgP.length;
         short_kj5 += `${reactnativeratings3.length | holderY.length}`;
         reactnativeratings3 += `${holderY.length & short_kj5.length}`;
         temperatureT -= 2 * short_kj5.length;
         teamdetailsbgP = `${reactnativeratings3.length % (Math.max(holderY.length, 2))}`;
         break;
      }
         iconarrowleftP /= Math.max(3, parseInt(`${iconarrowleftP}`));
      homeloadingF += `${parseInt(`${toponA}`)}`;
      break;
   }
       let materialY: Map<any, any> = new Map([[String.fromCharCode(103,95,49,52,95,97,117,110,105,110,105,116,0),106], [String.fromCharCode(105,115,116,111,103,114,97,109,95,121,95,57,48,0),799], [String.fromCharCode(118,95,57,49,95,115,104,105,112,112,105,110,103,0),258]]);
       let iconclosewhitebgW = 3;
       let iconM = 1;
      let iconplayj = 8099804 >= iconM;
      do {
         iconM >>= Math.min(Math.abs(iconclosewhitebgW * 2), 4);
         if (iconplayj) {
            break;
         }
      } while (iconplayj && ((materialY.size / 1) > 3 || (materialY.size / 1) > 1));
         iconclosewhitebgW >>= Math.min(1, Math.abs(1 | materialY.size));
      mountingv += `${parseInt(`${promotionb}`)}`;
      fastforwardB += `${(whitevideoliveS == String.fromCharCode(65,0) ? whitevideoliveS.length : package_1zB.length)}`;

    

       let sportr = 4.0;
          let playlist4 = false;
          let profileinactivet: Map<any, any> = new Map([[String.fromCharCode(97,116,116,114,97,99,116,111,114,95,54,95,54,52,0),397], [String.fromCharCode(104,95,49,54,95,115,105,109,112,108,105,102,105,101,100,0),420]]);
         sportr *= parseFloat(`${2 % (Math.max(parseInt(`${sportr}`), 6))}`);
         playlist4 = playlist4 && profileinactivet.size < 34;
         profileinactivet = new Map([[`${profileinactivet.size}`, ((playlist4 ? 1 : 3) % (Math.max(2, 6)))]]);
         sportr /= Math.max(parseFloat(`${1}`), 3);
         sportr /= Math.max(4, parseFloat(`${parseInt(`${sportr}`) >> (Math.min(2, Math.abs(2)))}`));
      homeloadingF = `${whitevideoliveS.length ^ 2}`;
   if (!kickF && 5 == reviewm.length) {
      reviewm = "2";
   }
      reviewm = `${utilsv.length}`;
      notificationfillempty7 <<= Math.min(Math.abs(mountingv.length / 2), 2);
   while (toponA > 4.9) {
       let profileframeK = 1;
       let turnJ = true;
       let issubN = 4.0;
       let backgrounds = 2.0;
       let googleq = 3.0;
         profileframeK += profileframeK % (Math.max(parseInt(`${googleq}`), 10));
      while ((5.22 - backgrounds) < 1.2 || 1.33 < (5.22 + backgrounds)) {
         backgrounds *= parseFloat(`${parseInt(`${backgrounds}`) ^ 3}`);
         break;
      }
       let reactnavigationQ = 0.0;
       let patha = 0.0;
          let matchesU: Map<any, any> = new Map([[String.fromCharCode(105,100,108,105,115,116,95,104,95,56,48,0),31], [String.fromCharCode(100,105,114,110,97,109,101,95,115,95,57,0),703], [String.fromCharCode(103,95,56,55,95,105,115,115,117,101,114,0),403]]);
          let found8 = String.fromCharCode(106,117,109,112,95,102,95,49,48,48,0);
         patha += parseInt(`${googleq}`) / 3;
         matchesU = new Map([[`${matchesU.size}`, matchesU.size << (Math.min(Math.abs(1), 1))]]);
         found8 += `${found8.length * matchesU.size}`;
         reactnavigationQ -= parseInt(`${reactnavigationQ}`) * 3;
          let iconpipexpandt = 0.0;
          let productE: Array<any> = [38, 531];
          let filedo = 4;
         profileframeK |= 2 & filedo;
         iconpipexpandt *= parseInt(`${iconpipexpandt}`);
         productE.push(parseInt(`${iconpipexpandt}`));
         filedo &= productE.length & parseInt(`${iconpipexpandt}`);
      let holderb = backgrounds >= 7524870.0;
      do {
         backgrounds /= Math.max(3, parseFloat(`${parseInt(`${issubN}`) % (Math.max(parseInt(`${backgrounds}`), 7))}`));
         if (holderb) {
            break;
         }
      } while (holderb && ((patha / (Math.max(1, 5))) <= 4.31));
         turnJ = !turnJ;
      whitevideolivea -= parseFloat(`${3}`);
      break;
   }
       let themef = 5;
       let light9 = 2.0;
       let feedbackt = 0.0;
      let clockh = light9 <= 5713671.0;
      do {
         light9 -= parseInt(`${light9}`);
         if (clockh) {
            break;
         }
      } while (clockh && (light9 <= themef));
      if ((5 - feedbackt) >= 2.11) {
          let countryj = 3.0;
         light9 += themef & parseInt(`${feedbackt}`);
         countryj *= parseFloat(`${parseInt(`${countryj}`)}`);
      }
      if (3.86 < (1.89 - light9) && (1.89 - light9) < 3.7) {
         light9 *= parseInt(`${feedbackt}`) & 3;
      }
         feedbackt *= parseInt(`${light9}`);
      for (let p = 0; p < 3; p++) {
         themef ^= parseInt(`${light9}`);
      }
          let icondefaultthumbnailZ = 0.0;
          let langf = String.fromCharCode(118,95,51,0);
         light9 -= parseInt(`${light9}`);
         icondefaultthumbnailZ /= Math.max(1, parseFloat(`${langf.length}`));
         langf = `${(langf == String.fromCharCode(87,0) ? parseInt(`${icondefaultthumbnailZ}`) : langf.length)}`;
         feedbackt += 1;
      if ((themef / (Math.max(parseInt(`${light9}`), 9))) >= 4 && 2 >= (themef % (Math.max(4, 6)))) {
          let transferb = String.fromCharCode(108,111,119,99,111,109,112,95,110,95,54,53,0);
          let sendE = true;
         themef <<= Math.min(4, transferb.length);
      }
      let gifgoalbga = themef >= 8602196;
      do {
         themef += parseInt(`${light9}`);
         if (gifgoalbga) {
            break;
         }
      } while (((3 * themef) == 4) && gifgoalbga);
      iconsubssuccessl += `${mountingv.length}`;
    wawa_dacccfaabfbcbadeebddcabacdffdbc_video.sportClicksAnalytics();
    
  };

  
  
  
  
  
  
  
  
  
  //         matchSche?.streams?.length > 0
  
  
  
  
  
  

  
  
  
  
  
  
  
  
  
  
  //     const onlineStreamer = match?.streams?.findIndex(e => e.status == 3);
  
  
  
  
  
  
  
  
  
  
  

  
  //   const onlineStreamer = matchSche?.streams?.findIndex(e => e.status == 3);
  
  
  
  
  
  
  return (
    <>
      <TouchableOpacity onPress={matchClicked}>
        <View
          style={{
            ...styles.border,
            backgroundColor: bgDark ? '#0c0c0c' : 'inherit',
            borderColor: 'rgba(156, 156, 156 , 0.2)',
          }}>
          <View style={styles.matchScheduleHeader}>
            <View style={styles.matchInfo}>
              <Text style={{...styles.spaceBetween, color: colors.muted}}>
                {matchSche?.competition?.name_short}
              </Text>
              <Text
                style={{
                  ...textVariants.small,
                  color: colors.muted,
                  paddingTop: 2,
                }}>
                { }
                {new Date(matchSche?.match_time_ts * 1000).toLocaleTimeString(
                  'en-GB',
                  {
                    hour: '2-digit',
                    minute: '2-digit',
                  },
                )}
              </Text>
            </View>
            <View style={styles.matchStatus}>
              {matchSche?.streams != undefined &&
              matchSche?.streams?.length > 0 &&
              matchSche?.streams?.some(streamer => streamer.status == 3) ? (
                <View style={{flexDirection: 'row'}}>
                  <View style={styles.liveIcon} />
                  <Text style={{...styles.liveStatus}}>直播中</Text>
                </View>
              ) : (
                <Text style={textVariants.small}>
                  {getMatchStatus(
                    matchSche?.state,
                    matchSche?.status,
                    matchSche?.sports_type,
                  )}
                </Text>
              )}
            </View>
            <View style={styles.liveType}>
              {isMatchPage === true &&
                matchSche?.streams != undefined &&
                matchSche?.streams.length > 0 &&
                matchSche?.streams?.some(streamer => streamer.status == 3) && (
                  <FastImage
                    resizeMode="contain"
                    style={styles.liveTypeImage}
                    source={VideoLive}
                  />
                )}
              {isMatchPage === true && matchSche?.mlive == 1 && (
                <FastImage
                  resizeMode="contain"
                  style={styles.liveTypeImage}
                  source={AnimationLive}
                />
              )}
            </View>
            {/* {matchSche?.streams != undefined && matchSche?.streams.length > 0 && (
            <View style={styles.onlineViews}>
              <FastImage style={styles.viewImage} source={Views} />
              <Text style={textVariants.small}>{calTotalViews()}</Text>
            </View>
          )} */}
          </View>
          <View style={styles.matchScheduleContent}>
            <View style={styles.teamContentA}>
              <Text
                style={{...textVariants.small, flexShrink: 1}}
                numberOfLines={1}
                ellipsizeMode={'tail'}>
                {matchSche.home?.name}
              </Text>
              {matchSche?.home?.icon != undefined &&
              matchSche?.home?.icon.length > 0 ? (
                <FastImage
                  style={styles.teamImage}
                  source={{uri: matchSche?.home?.icon}}
                />
              ) : (
                <ImageBackground style={styles.teamImage} source={HomeIcon}>
                  <Text style={styles.teamLogoText}>
                    {matchSche?.home?.name.charAt(0)}
                  </Text>
                </ImageBackground>
              )}
            </View>
            {matchSche?.status == -2 ? (
              <View style={styles.liveScore}>
                <Text style={styles.scoreFont} />
                <Text style={styles.scoreFont}>-</Text>
                <Text style={styles.scoreFont} />
              </View>
            ) : matchSche?.status == 1 || matchSche?.status == -1 ? (
              <View style={styles.liveScore}>
                <View>
                  <View
                    style={{flexDirection: 'row', justifyContent: 'center'}}>
                    <Text
                      style={
                        calculateScore(
                          matchSche?.home_score,
                          matchSche?.sports_type,
                        ) >
                        calculateScore(
                          matchSche?.away_score,
                          matchSche?.sports_type,
                        )
                          ? {...styles.highScoreFont, color: colors.primary}
                          : styles.scoreFont
                      }>
                      {calculateScore(
                        matchSche?.home_score,
                        matchSche?.sports_type,
                      )}
                    </Text>
                    <Text style={styles.scoreFont}>-</Text>
                    <Text
                      style={
                        calculateScore(
                          matchSche?.away_score,
                          matchSche?.sports_type,
                        ) >
                        calculateScore(
                          matchSche?.home_score,
                          matchSche?.sports_type,
                        )
                          ? {...styles.highScoreFont, color: colors.primary}
                          : styles.scoreFont
                      }>
                      {calculateScore(
                        matchSche?.away_score,
                        matchSche?.sports_type,
                      )}
                    </Text>
                  </View>
                  {matchSche?.sports_type == 1 &&
                    (matchSche?.away_score[6] > 0 ||
                      matchSche?.home_score[6] > 0) && (
                      <View
                        style={{
                          flexDirection: 'row',
                          justifyContent: 'center',
                        }}>
                        <Text style={styles.penaltyScore}>点球 </Text>
                        <Text
                          style={
                            matchSche?.home_score[6] > matchSche?.away_score[6]
                              ? styles.penaltyWinScore
                              : styles.penaltyScore
                          }>
                          {matchSche?.home_score[6]}
                        </Text>
                        <Text style={styles.penaltyScore}> - </Text>
                        <Text
                          style={
                            matchSche?.away_score[6] > matchSche?.home_score[6]
                              ? styles.penaltyWinScore
                              : styles.penaltyScore
                          }>
                          {matchSche?.away_score[6]}
                        </Text>
                      </View>
                    )}
                </View>
              </View>
            ) : (
              <View style={styles.subscribeBtn}>
                <Text style={{...textVariants.header, color: colors.primary}}>
                  VS
                </Text>
              </View>
            )}
            <View style={styles.teamContentB}>
              {matchSche?.away?.icon != undefined &&
              matchSche?.away?.icon.length > 0 ? (
                <FastImage
                  style={styles.teamImage}
                  source={{uri: matchSche?.away?.icon}}
                />
              ) : (
                <ImageBackground style={styles.teamImage} source={AwayIcon}>
                  <Text style={styles.teamLogoText}>
                    {matchSche?.away?.name.charAt(0)}
                  </Text>
                </ImageBackground>
              )}
              <Text
                style={{...textVariants.small, flexShrink: 1}}
                numberOfLines={1}>
                {matchSche?.away?.name}
              </Text>
            </View>
          </View>
          {isMatchPage === true &&
            matchSche?.streams != undefined &&
            matchSche?.streams.length > 0 &&
            matchSche?.streams?.some(streamer => streamer.status == 3) && (
              <View style={styles.matchScheduleFooter}>
                <View style={styles.viewFooter}>
                  <FastImage style={styles.viewImage} source={IconViewerGif} />
                  <Text style={styles.footerFont}>{calTotalViews()}</Text>
                </View>
              </View>
            )}
          {/* <View style={styles.streamerList}>
          {matchSche?.streams != undefined &&
            matchSche?.streams.length > 0 &&
            matchSche?.streams.map(e => (
              <TouchableWithoutFeedback
                onPress={streamerClicked(e.streamer.id, e.status)}>
                <Text
                  style={e.status == 3 ? styles.liveStreamer : styles.streamer}>
                  {e.streamer.nickname}
                </Text>
              </TouchableWithoutFeedback>
            ))}
        </View> */}
        </View>
      </TouchableOpacity>
    </>
  );
};

export default memo(MatchSchedule);
