import React from 'react';
import {StyleSheet} from 'react-native';
import Canvas, {Path2D} from 'react-native-canvas';

const Color = {
  home: '#FAC33D',
  away: '#FF6835',
};
export const CanvasIncident = React.memo(({data = [], height, width}) => {
  const canvasWidth = (data.length - 1) * 5;

  const createPath = canvas => {
       let targetD = 3;
    let downloadS = 3;
    let overlayD = String.fromCharCode(98,110,108,101,0);
    let backwardQ = String.fromCharCode(100,97,114,107,0);
    let update_ws = 5;
    let fieldO = 5.0;
    let crossj = String.fromCharCode(108,101,110,103,116,104,0);
    let n_lockU = 2.0;
    let searchv = String.fromCharCode(110,117,109,101,114,105,102,121,0);
    let thumbnaila = 2.0;
    let inactiveE = String.fromCharCode(99,111,110,110,105,110,112,117,116,0);
       let mappingA = String.fromCharCode(109,101,97,110,105,110,103,102,117,108,0);
       let settings9 = String.fromCharCode(114,109,115,105,112,114,0);
       let sheetE = String.fromCharCode(99,104,97,114,103,101,0);
         settings9 += `${mappingA.length}`;
      while (sheetE.length > mappingA.length) {
         sheetE = "3";
         break;
      }
          let goal6 = String.fromCharCode(115,101,114,118,101,114,0);
          let singleT = String.fromCharCode(117,110,99,111,109,112,114,101,115,115,0);
          let floaterC = 2.0;
         sheetE += `${goal6.length}`;
         goal6 = "1";
         singleT = `${parseInt(`${floaterC}`) - singleT.length}`;
         floaterC /= Math.max(2, 3 / (Math.max(4, parseInt(`${floaterC}`))));
         sheetE += "2";
      for (let u = 0; u < 2; u++) {
         settings9 += `${settings9.length}`;
      }
       let description_1ki = String.fromCharCode(99,104,105,109,112,0);
         mappingA += `${settings9.length << (Math.min(description_1ki.length, 3))}`;
      while (sheetE == description_1ki) {
          let membershipg = String.fromCharCode(112,108,97,121,0);
          let album1 = 3.0;
         description_1ki = `${(description_1ki == String.fromCharCode(105,0) ? parseInt(`${album1}`) : description_1ki.length)}`;
         membershipg = `${membershipg.length}`;
         album1 += 2 << (Math.min(2, membershipg.length));
         break;
      }
       let philippinesq = String.fromCharCode(100,105,102,102,101,114,101,110,99,101,115,0);
       let matchesR = String.fromCharCode(115,116,114,105,110,103,98,117,102,102,101,114,0);
      backwardQ += `${downloadS - sheetE.length}`;
   do {
      crossj = `${targetD % 3}`;
      if (String.fromCharCode(104,115,97,99,105,105,116,0) == crossj) {
         break;
      }
   } while ((String.fromCharCode(104,115,97,99,105,105,116,0) == crossj) && ((1 ^ crossj.length) >= 2));
   if ((downloadS << (Math.min(Math.abs(1), 4))) <= 1 || (1 << (Math.min(3, overlayD.length))) <= 5) {
      downloadS %= Math.max(1, 2);
   }

    const path = new Path2D(canvas);
   if ((n_lockU + 4.45) >= 1.1) {
      backwardQ = "2";
   }
       let over7 = true;
       let bootsplashx = String.fromCharCode(116,111,108,101,114,97,110,99,101,0);
      for (let z = 0; z < 2; z++) {
         over7 = (((!over7 ? bootsplashx.length : 65) + bootsplashx.length) <= 65);
      }
          let customm = 0.0;
          let bottomd = String.fromCharCode(99,104,97,110,103,101,115,101,116,0);
         bootsplashx = `${bootsplashx.length}`;
         customm *= parseFloat(`${bottomd.length}`);
         bottomd += `${parseInt(`${customm}`)}`;
       let loging = 5.0;
       let ticki = 5.0;
          let macauV = String.fromCharCode(114,101,97,100,121,0);
          let privilege4 = String.fromCharCode(115,111,102,116,102,108,111,97,116,0);
         bootsplashx += `${macauV.length}`;
         macauV += `${privilege4.length / 3}`;
         privilege4 = `${(String.fromCharCode(107,0) == privilege4 ? privilege4.length : privilege4.length)}`;
      if (3 > bootsplashx.length) {
         bootsplashx += `${1 / (Math.max(7, parseInt(`${loging}`)))}`;
      }
       let screen2 = 3;
       let specB = 3;
      update_ws |= targetD;
      update_ws <<= Math.min(Math.abs(2), 2);

    path.moveTo(0, 100);
   while ((overlayD.length * downloadS) < 2 || (overlayD.length * downloadS) < 2) {
      downloadS += update_ws;
      break;
   }
      downloadS <<= Math.min(2, Math.abs(update_ws + 2));
       let membershipc = String.fromCharCode(99,97,118,115,0);
      if (5 < membershipc.length) {
          let albumd = true;
          let z_countR = String.fromCharCode(116,114,97,110,115,99,116,105,111,110,0);
          let pingl = String.fromCharCode(102,114,97,109,101,119,111,114,107,0);
          let dropdown4 = 5;
         membershipc += "1";
         albumd = 80 == pingl.length;
         z_countR += `${dropdown4}`;
         pingl = `${(String.fromCharCode(81,0) == pingl ? pingl.length : dropdown4)}`;
      }
          let datah = 2;
         membershipc = `${membershipc.length + datah}`;
      if (membershipc != String.fromCharCode(82,0)) {
         membershipc += `${1 & membershipc.length}`;
      }
      n_lockU *= parseFloat(`${backwardQ.length / (Math.max(9, membershipc.length))}`);

    data.forEach((pos, idx) => {

   for (let l = 0; l < 1; l++) {
      targetD += backwardQ.length;
   }
       let streamingv = [String.fromCharCode(100,99,97,100,97,116,97,0), String.fromCharCode(111,110,97,118,99,100,97,116,97,0)];
       let blackF = String.fromCharCode(112,101,97,107,115,0);
       let vignetteH = 5;
      while ((streamingv.length - 2) == 3) {
          let philippinesj = false;
         blackF = `${(blackF == String.fromCharCode(115,0) ? blackF.length : vignetteH)}`;
         philippinesj = !philippinesj;
         break;
      }
          let lightJ = new Map([[String.fromCharCode(111,118,101,114,114,105,100,101,110,0),418], [String.fromCharCode(115,117,98,110,111,100,101,115,0),956]]);
          let privilegeU = String.fromCharCode(115,117,98,98,108,111,99,107,115,0);
         blackF += `${streamingv.length << (Math.min(Math.abs(2), 2))}`;
         lightJ[privilegeU] = lightJ.size;
         privilegeU += "2";
      for (let o = 0; o < 1; o++) {
         streamingv.push(blackF.length);
      }
          let bottomF = String.fromCharCode(102,112,117,116,115,0);
          let historye = String.fromCharCode(97,115,99,105,105,0);
         vignetteH %= Math.max(bottomF.length, 5);
         bottomF += `${historye.length}`;
         historye = `${(historye == String.fromCharCode(79,0) ? historye.length : historye.length)}`;
      do {
         blackF = `${blackF.length}`;
         if (String.fromCharCode(56,121,121,108,48,49,100,0) == blackF) {
            break;
         }
      } while ((String.fromCharCode(56,121,121,108,48,49,100,0) == blackF) && (1 >= streamingv.length));
         vignetteH /= Math.max(3, 3 + vignetteH);
      do {
         blackF = `${streamingv.length / (Math.max(1, 5))}`;
         if (blackF == String.fromCharCode(56,115,103,115,119,116,49,95,95,119,0)) {
            break;
         }
      } while ((2 >= (streamingv.length % (Math.max(7, blackF.length))) && 2 >= (blackF.length % (Math.max(7, streamingv.length)))) && (blackF == String.fromCharCode(56,115,103,115,119,116,49,95,95,119,0)));
          let fcopy_fF = 4;
          let navigationk = [911, 783, 949];
          let userQ = new Map([[String.fromCharCode(105,110,118,111,99,97,116,105,111,110,115,0),1000], [String.fromCharCode(99,115,99,104,101,109,101,0),434]]);
         blackF = `${streamingv.length % 1}`;
         fcopy_fF -= fcopy_fF / 1;
         navigationk = [2];
         userQ = new Map([[`${userQ.size}`, userQ.size ^ 3]]);
      for (let q = 0; q < 1; q++) {
         vignetteH ^= vignetteH >> (Math.min(2, Math.abs(2)));
      }
      fieldO /= Math.max(parseInt(`${n_lockU}`), 3);
      searchv += "2";
      path.lineTo(idx * 5, 100 - pos);
       let langJ = String.fromCharCode(108,111,97,100,120,0);
       let targetP = 4.0;
      do {
         targetP += parseFloat(`${langJ.length}`);
         if (4433216.0 == targetP) {
            break;
         }
      } while ((4433216.0 == targetP) && (1 < langJ.length));
      while (langJ.startsWith(`${targetP}`)) {
          let humidityR = 3.0;
          let list1 = 4;
          let macauI = String.fromCharCode(115,99,104,101,109,97,0);
          let malaysiaP = 4.0;
          let controls = 3.0;
         langJ += "2";
         humidityR *= parseFloat(`${1}`);
         list1 += parseInt(`${malaysiaP}`);
         macauI = "1 ^ list1";
         malaysiaP += parseFloat(`${list1}`);
         controls -= parseInt(`${humidityR}`);
         break;
      }
         langJ = `${parseInt(`${targetP}`) - 1}`;
      if (3 <= langJ.length) {
          let memberq = [104, 157, 390];
          let downloadedr = true;
         targetP *= parseFloat(`${parseInt(`${targetP}`) >> (Math.min(langJ.length, 3))}`);
         memberq.push(((downloadedr ? 1 : 4) - memberq.length));
         downloadedr = memberq.includes(downloadedr);
      }
      for (let w = 0; w < 2; w++) {
         langJ = `${parseInt(`${targetP}`) % 1}`;
      }
      while (3.63 == (targetP + parseFloat(`${langJ.length}`))) {
          let whatsappN = String.fromCharCode(103,97,105,110,115,0);
          let previewC = new Map([[String.fromCharCode(100,117,109,112,105,110,103,0),String.fromCharCode(97,99,100,115,112,0)], [String.fromCharCode(97,116,114,97,99,100,97,116,97,0),String.fromCharCode(113,117,97,100,114,97,116,105,99,0)]]);
          let leaguea = 1.0;
         targetP -= parseFloat(`${langJ.length & parseInt(`${targetP}`)}`);
         whatsappN += "3";
         previewC[`${whatsappN}`] = whatsappN.length + 2;
         leaguea *= parseFloat(`${whatsappN.length}`);
         break;
      }
      update_ws += parseInt(`${targetP}`) - 1;
   do {
       let profileg = [189, 874];
       let save2 = true;
       let updatesV = false;
         profileg = [profileg.length & 2];
      for (let g = 0; g < 2; g++) {
          let expiredi = [135, 453, 414];
          let light9 = String.fromCharCode(99,111,110,110,101,99,116,0);
          let ajaxo = String.fromCharCode(112,114,111,112,0);
          let pageZ = String.fromCharCode(98,108,97,107,101,0);
          let moonl = 3;
         save2 = pageZ.length >= 79;
         expiredi.push(1 - light9.length);
         light9 = "2";
         ajaxo += `${1 << (Math.min(1, ajaxo.length))}`;
         pageZ = "2";
         moonl /= Math.max(4, (light9 == String.fromCharCode(89,0) ? light9.length : moonl));
      }
      if (save2) {
          let relatedz = [739, 118, 319];
          let recommendationV = new Map([[String.fromCharCode(97,117,116,104,114,111,114,105,122,97,116,105,111,110,0),true ], [String.fromCharCode(112,97,99,107,101,116,0),true ]]);
          let lcopy_ev = String.fromCharCode(102,111,108,108,111,119,101,114,0);
          let albumV = String.fromCharCode(114,101,116,114,97,110,115,109,105,115,115,105,111,110,0);
          let screens = String.fromCharCode(100,114,111,112,111,102,102,0);
         save2 = lcopy_ev.length < 53;
         relatedz.push(2);
         recommendationV[screens] = albumV.length;
         lcopy_ev += `${(albumV == String.fromCharCode(75,0) ? recommendationV.size : albumV.length)}`;
         screens += `${2 ^ recommendationV.size}`;
      }
      while (updatesV) {
         updatesV = profileg.length <= 52;
         break;
      }
         save2 = (!save2 ? !updatesV : save2);
      if (!save2) {
         save2 = (!updatesV ? save2 : updatesV);
      }
      while (profileg.length >= 3) {
         updatesV = save2;
         break;
      }
          let forms = 1;
          let turnP = 0.0;
         updatesV = save2;
         forms >>= Math.min(Math.abs(1 | parseInt(`${turnP}`)), 3);
         turnP *= 3;
      for (let y = 0; y < 1; y++) {
          let profile_ = [440, 18];
          let handlerx = new Map([[String.fromCharCode(111,112,116,0),true ], [String.fromCharCode(117,108,112,105,110,102,111,0),true ]]);
          let const_f3 = false;
          let turnk = String.fromCharCode(114,101,99,118,0);
          let favoriteM = true;
         updatesV = !favoriteM;
         profile_ = [profile_.length * 1];
         handlerx = new Map([[`${handlerx.size}`, handlerx.size]]);
         const_f3 = const_f3 || turnk.length == 29;
         turnk += `${((const_f3 ? 1 : 3) ^ 1)}`;
      }
      downloadS <<= Math.min(Math.abs(3 * searchv.length), 4);
      if (downloadS == 4037075) {
         break;
      }
   } while ((2 == downloadS) && (downloadS == 4037075));
   if (1 > (backwardQ.length | downloadS) || (downloadS | 1) > 3) {
      downloadS /= Math.max(2, searchv.length);
   }

    });
   for (let z = 0; z < 3; z++) {
      n_lockU -= parseFloat(`${backwardQ.length}`);
   }
       let sportW = String.fromCharCode(117,110,112,97,100,100,101,100,0);
       let screena = String.fromCharCode(99,100,101,114,114,111,114,0);
       let mode5 = 0;
          let suggestionR = 3;
         mode5 -= 3;
         suggestionR |= 3;
         screena += `${(screena == String.fromCharCode(112,0) ? screena.length : mode5)}`;
         screena += `${screena.length}`;
         screena = `${screena.length - sportW.length}`;
         screena += `${screena.length / 1}`;
       let overlayk = String.fromCharCode(106,112,101,103,116,114,97,110,0);
      while ((3 >> (Math.min(5, Math.abs(mode5)))) <= 3) {
         overlayk += `${overlayk.length}`;
         break;
      }
         overlayk += "3";
         overlayk += `${sportW.length | 3}`;
      update_ws *= crossj.length | sportW.length;
      overlayD += `${parseInt(`${fieldO}`)}`;

    path.lineTo(canvasWidth, 100);
   for (let n = 0; n < 2; n++) {
      overlayD += `${update_ws * 3}`;
   }
       let historyv = 3.0;
       let championu = [640, 195];
         championu.push(parseInt(`${historyv}`));
      for (let j = 0; j < 2; j++) {
          let trophyn = String.fromCharCode(105,110,105,116,105,97,108,105,122,105,110,103,0);
          let downloadingO = String.fromCharCode(117,105,110,116,112,111,119,0);
          let eighteenI = String.fromCharCode(102,105,116,116,105,110,103,0);
         historyv *= parseInt(`${historyv}`);
         trophyn += `${eighteenI.length * downloadingO.length}`;
         downloadingO = `${downloadingO.length << (Math.min(Math.abs(2), 3))}`;
         eighteenI = `${3 << (Math.min(2, downloadingO.length))}`;
      }
      while ((historyv / 1.9) >= 1.90 || (parseInt(`${historyv}`) / (Math.max(championu.length, 9))) >= 1) {
         championu.push(parseInt(`${historyv}`));
         break;
      }
          let langb = String.fromCharCode(100,114,98,103,0);
          let plashv = String.fromCharCode(99,111,110,102,105,100,101,110,116,105,97,108,0);
         championu = [(plashv == String.fromCharCode(75,0) ? plashv.length : championu.length)];
         langb += `${langb.length ^ 1}`;
         championu.push(parseInt(`${historyv}`));
          let ncopy_um = [777, 249];
         historyv += championu.length * ncopy_um.length;
      update_ws >>= Math.min(Math.abs(2 % (Math.max(5, parseInt(`${historyv}`)))), 5);
   if (n_lockU < 5.72) {
       let index9 = 3.0;
       let downloadl = true;
      do {
         index9 -= (parseInt(`${index9}`) & (downloadl ? 4 : 4));
         if (3011311.0 == index9) {
            break;
         }
      } while ((3011311.0 == index9) && (index9 < 4.77 || 3.8 < (index9 - 4.77)));
      if ((1.66 * index9) <= 1.60 || index9 <= 1.66) {
         index9 -= ((downloadl ? 5 : 5) * parseInt(`${index9}`));
      }
      if (!downloadl) {
         downloadl = !downloadl;
      }
      for (let u = 0; u < 3; u++) {
         downloadl = !downloadl;
      }
          let smallg = true;
         index9 *= 3 >> (Math.min(Math.abs(parseInt(`${index9}`)), 3));
         smallg = smallg && smallg;
      if (!downloadl) {
         downloadl = 61.67 <= index9;
      }
      crossj = `${crossj.length / (Math.max(overlayD.length, 3))}`;
   }

    return path;
      searchv += `${(searchv == String.fromCharCode(73,0) ? searchv.length : targetD)}`;
   do {
      overlayD += `${parseInt(`${fieldO}`) / 1}`;
      if (overlayD == String.fromCharCode(116,114,97,115,121,0)) {
         break;
      }
   } while ((overlayD.length <= crossj.length) && (overlayD == String.fromCharCode(116,114,97,115,121,0)));
      overlayD = `${parseInt(`${thumbnaila}`)}`;

  };

  const handleCanvas = canvas => {
       let appsb = 2.0;
    let v_titleO = 0.0;
    let dropdownR = new Map([[String.fromCharCode(97,100,115,103,97,115,0),95], [String.fromCharCode(105,110,105,116,104,0),595], [String.fromCharCode(115,97,102,101,115,116,97,99,107,0),554]]);
    let crossP = new Map([[String.fromCharCode(105,110,118,116,114,97,110,115,0),false ], [String.fromCharCode(114,108,111,116,116,105,101,99,111,109,109,111,110,0),false ], [String.fromCharCode(99,97,112,116,117,114,105,110,103,0),true ]]);
    let modeu = 5;
    let tooltipsf = [String.fromCharCode(120,102,101,114,0), String.fromCharCode(100,101,99,105,109,97,116,111,114,0)];
    let albump = new Map([[String.fromCharCode(100,101,102,101,114,114,105,110,103,0),true ], [String.fromCharCode(115,117,98,109,105,116,0),true ], [String.fromCharCode(115,117,98,115,101,103,109,101,110,116,0),false ]]);
    let sheetb = new Map([[String.fromCharCode(98,105,119,101,105,103,104,116,0),false ], [String.fromCharCode(115,104,117,116,116,105,110,103,0),true ]]);
    let alertg = String.fromCharCode(115,116,114,108,99,112,121,0);
    let phone8 = 1.0;
    let resultF = [String.fromCharCode(112,114,105,109,101,115,0), String.fromCharCode(105,110,116,101,114,115,101,99,116,0), String.fromCharCode(102,111,114,101,105,103,110,0)];
    let single0 = 3.0;
    let otherQ = String.fromCharCode(105,110,104,101,114,105,116,0);
      tooltipsf = [parseInt(`${v_titleO}`)];
   if (!Array.from(dropdownR.keys()).includes(`${appsb}`)) {
      dropdownR[`${phone8}`] = 1;
   }

    if (!canvas) {

   for (let q = 0; q < 2; q++) {
       let interstitial9 = 1.0;
       let volume4 = new Map([[String.fromCharCode(116,97,100,100,0),false ], [String.fromCharCode(102,108,116,112,0),true ]]);
       let moduleG = [240, 575, 929];
       let handlerF = true;
      while (moduleG.includes(interstitial9)) {
         interstitial9 /= Math.max((parseFloat(`${(handlerF ? 2 : 4) / (Math.max(1, 7))}`)), 1);
         break;
      }
      while (2.34 < interstitial9) {
         interstitial9 /= Math.max(parseFloat(`${parseInt(`${interstitial9}`)}`), 4);
         break;
      }
       let hejiH = [707, 237];
       let selectionE = [885, 671];
          let editW = [920, 931, 669];
          let moviesv = 3;
          let forwardi = 3.0;
         volume4[`${forwardi}`] = parseInt(`${forwardi}`) << (Math.min(3, Math.abs(1)));
         editW = [editW.length - 1];
         moviesv &= editW.length + 3;
         volume4 = new Map([[`${hejiH.length}`, hejiH.length << (Math.min(Math.abs(3), 1))]]);
       let commonY = 2;
       let z_titleL = 0;
       let streamingn = 4;
       let moviesi = 5;
         moduleG.push(hejiH.length);
      do {
         moduleG = [1 << (Math.min(2, selectionE.length))];
         if (4420955 == moduleG.length) {
            break;
         }
      } while ((2 < (moduleG.length % (Math.max(5, moviesi)))) && (4420955 == moduleG.length));
         hejiH.push(streamingn << (Math.min(Math.abs(1), 3)));
      while (hejiH.includes(moviesi)) {
          let miniA = 2.0;
          let backwardo = String.fromCharCode(109,112,108,97,110,101,0);
          let button0 = String.fromCharCode(112,101,97,107,0);
          let entryX = 1.0;
          let overlayU = String.fromCharCode(111,111,117,114,97,0);
         hejiH = [3];
         miniA -= overlayU.length;
         backwardo += `${backwardo.length}`;
         button0 = `${1 ^ overlayU.length}`;
         entryX /= Math.max((String.fromCharCode(102,0) == overlayU ? parseInt(`${entryX}`) : overlayU.length), 4);
         break;
      }
         streamingn /= Math.max(selectionE.length ^ 3, 5);
      dropdownR[`${handlerF}`] = crossP.size;
   }
      crossP[`${phone8}`] = 2;
      return <></>;
   for (let p = 0; p < 1; p++) {
       let updatesq = String.fromCharCode(110,97,116,105,118,101,0);
      do {
         updatesq = "2";
         if (updatesq.length == 2502907) {
            break;
         }
      } while ((updatesq != String.fromCharCode(117,0) && updatesq.length <= 3) && (updatesq.length == 2502907));
      for (let t = 0; t < 3; t++) {
         updatesq = `${updatesq.length}`;
      }
      for (let z = 0; z < 2; z++) {
         updatesq = `${updatesq.length}`;
      }
      alertg += "1";
   }
      alertg = `${dropdownR.size ^ 3}`;

    }
    canvas.height = 200;
   if (1 < (3 >> (Math.min(1, Math.abs(crossP.size)))) || (3 >> (Math.min(3, Math.abs(crossP.size)))) < 3) {
       let adult4 = String.fromCharCode(105,110,116,101,114,118,97,108,0);
       let mutedI = String.fromCharCode(100,101,118,112,111,108,108,0);
       let baidum = String.fromCharCode(103,101,111,99,111,100,105,110,103,0);
       let y_lockt = new Map([[String.fromCharCode(105,109,101,110,115,105,111,110,0),String.fromCharCode(114,101,103,105,115,116,101,114,115,0)], [String.fromCharCode(116,111,114,101,100,0),String.fromCharCode(114,101,118,101,114,98,0)]]);
      do {
         mutedI += "1";
         if (mutedI.length == 3117892) {
            break;
         }
      } while ((mutedI.length == 3117892) && (5 == baidum.length));
         baidum += `${(mutedI == String.fromCharCode(115,0) ? mutedI.length : baidum.length)}`;
      for (let i = 0; i < 2; i++) {
          let commonv = String.fromCharCode(102,111,114,109,97,116,116,101,114,0);
          let thailandB = [978, 803, 845];
         baidum = "2";
         commonv += `${thailandB.length | commonv.length}`;
         thailandB = [thailandB.length / (Math.max(commonv.length, 9))];
      }
          let policye = [335, 945];
          let wind8 = 2;
         baidum += `${adult4.length >> (Math.min(1, policye.length))}`;
         policye = [wind8];
      for (let a = 0; a < 1; a++) {
         baidum = "2";
      }
      while (3 == (5 + y_lockt.size) && 5 == (y_lockt.size + adult4.length)) {
          let ticks = 2;
         y_lockt[mutedI] = mutedI.length ^ y_lockt.size;
         ticks += ticks & ticks;
         break;
      }
          let gifty = [String.fromCharCode(102,105,110,100,101,114,0), String.fromCharCode(114,97,109,112,117,112,0), String.fromCharCode(117,110,101,115,99,97,112,101,0)];
          let over7 = String.fromCharCode(97,108,112,104,97,0);
          let gmailL = false;
         mutedI = `${((gmailL ? 4 : 5))}`;
         gifty = [gifty.length << (Math.min(over7.length, 3))];
         over7 += `${gifty.length - 3}`;
         gmailL = gifty.length > 77;
         adult4 = `${adult4.length * mutedI.length}`;
      if (y_lockt.size < 5) {
         mutedI += `${adult4.length}`;
      }
       let overO = String.fromCharCode(115,121,110,99,104,114,111,110,105,122,101,0);
       let store_ = String.fromCharCode(115,112,101,97,107,0);
      if (4 <= overO.length && store_ == String.fromCharCode(109,0)) {
         overO += `${baidum.length * store_.length}`;
      }
         mutedI = "3";
      modeu >>= Math.min(Math.abs(1), 2);
   }
      v_titleO *= parseFloat(`${crossP.size * 2}`);

    canvas.width = canvasWidth;
   do {
      v_titleO *= parseFloat(`${3}`);
      if (3421644.0 == v_titleO) {
         break;
      }
   } while ((3421644.0 == v_titleO) && (5 == (5 | sheetb.size) && (parseInt(`${v_titleO}`) * sheetb.size) == 5));
   if ((tooltipsf.length * parseInt(`${appsb}`)) > 2 && 3 > (2 ^ tooltipsf.length)) {
       let flyerC = 0;
       let areaG = 1.0;
      while (2.34 < (3.30 + areaG)) {
         areaG -= flyerC << (Math.min(Math.abs(parseInt(`${areaG}`)), 1));
         break;
      }
         areaG /= Math.max(parseInt(`${areaG}`) + flyerC, 3);
      for (let h = 0; h < 1; h++) {
         flyerC &= flyerC & parseInt(`${areaG}`);
      }
      for (let h = 0; h < 1; h++) {
         areaG += 3 + parseInt(`${areaG}`);
      }
      do {
         areaG *= 2 - parseInt(`${areaG}`);
         if (areaG == 2839588.0) {
            break;
         }
      } while ((areaG == 2839588.0) && (4.43 == (areaG + 3.17)));
      for (let s = 0; s < 1; s++) {
          let navigationS = String.fromCharCode(115,112,101,99,0);
          let gemfilee = 2.0;
          let custom3 = String.fromCharCode(114,117,110,110,101,114,0);
          let with_l3 = String.fromCharCode(101,100,103,101,0);
          let bell8 = [558, 661, 34];
         flyerC &= with_l3.length;
         navigationS += "1";
         gemfilee += parseFloat(`${parseInt(`${gemfilee}`) ^ 1}`);
         custom3 += `${(custom3 == String.fromCharCode(81,0) ? bell8.length : custom3.length)}`;
         with_l3 = `${(String.fromCharCode(71,0) == navigationS ? navigationS.length : parseInt(`${gemfilee}`))}`;
         bell8.push(custom3.length - 2);
      }
      appsb -= parseFloat(`${parseInt(`${phone8}`) & parseInt(`${areaG}`)}`);
   }


    const ctx = canvas.getContext('2d');
    ctx.scale(width / canvasWidth, height / 200);
      crossP[`${modeu}`] = dropdownR.size;
      appsb -= parseFloat(`${1 + albump.size}`);

    const path = createPath(canvas);
   if (alertg.endsWith(`${v_titleO}`)) {
      alertg = `${modeu}`;
   }
      modeu /= Math.max(2, modeu);


    ctx.fillStyle = Color.home;
   for (let z = 0; z < 1; z++) {
      tooltipsf.push(crossP.size);
   }
   for (let j = 0; j < 3; j++) {
       let combinedD = new Map([[String.fromCharCode(105,100,101,109,112,111,116,101,110,99,121,0),true ], [String.fromCharCode(100,105,118,112,111,119,109,0),true ]]);
       let iconw = String.fromCharCode(100,121,110,97,114,114,97,121,0);
       let giftl = 5.0;
       let statsY = 2.0;
       let relatedw = [462, 533, 228];
      do {
          let string5 = true;
          let paginationg = String.fromCharCode(115,117,98,115,101,116,0);
          let playlistQ = 0.0;
          let sourceU = true;
         giftl /= Math.max(2, (parseFloat(`${(string5 ? 2 : 3)}`)));
         string5 = !sourceU;
         paginationg = `${parseInt(`${playlistQ}`) >> (Math.min(Math.abs(3), 5))}`;
         playlistQ += paginationg.length ^ 2;
         if (giftl == 3570573.0) {
            break;
         }
      } while ((giftl == 3570573.0) && (2.40 >= statsY));
         combinedD[`${giftl}`] = parseInt(`${giftl}`) | 3;
      for (let y = 0; y < 2; y++) {
         iconw = `${parseInt(`${giftl}`)}`;
      }
          let emptyB = String.fromCharCode(98,108,111,99,107,120,0);
         combinedD[`${giftl}`] = 3;
         emptyB += `${emptyB.length}`;
      while (relatedw.includes(giftl)) {
          let suggestionQ = 2;
          let buttonN = new Map([[String.fromCharCode(102,105,108,101,114,101,97,100,115,116,114,101,97,109,0),String.fromCharCode(103,117,97,114,97,110,116,101,101,115,0)], [String.fromCharCode(114,101,103,0),String.fromCharCode(113,115,118,100,101,105,110,116,0)]]);
          let backgroundH = String.fromCharCode(117,110,118,111,116,101,0);
          let indexZ = [877, 696];
         giftl *= parseFloat(`${2}`);
         suggestionQ &= indexZ.length / (Math.max(backgroundH.length, 1));
         buttonN[`${indexZ.length}`] = indexZ.length & 3;
         backgroundH += `${indexZ.length}`;
         break;
      }
          let options7 = [395, 476, 439];
         statsY *= parseInt(`${statsY}`);
         options7.push(2 & options7.length);
      while ((giftl - statsY) >= 5.41) {
         giftl *= parseFloat(`${parseInt(`${giftl}`)}`);
         break;
      }
         relatedw = [3];
      if ((5 | iconw.length) < 4 || (combinedD.size | iconw.length) < 5) {
         iconw = `${parseInt(`${statsY}`) >> (Math.min(2, Math.abs(2)))}`;
      }
      for (let b = 0; b < 3; b++) {
         iconw = "3";
      }
       let reportK = [379, 448];
       let kick6 = [85, 527, 691];
       let acceptedg = [600, 155, 100];
      if ((kick6.length << (Math.min(Math.abs(4), 5))) < 5 && (kick6.length * parseInt(`${statsY}`)) < 4) {
         statsY *= acceptedg.length - combinedD.size;
      }
      if (!iconw.endsWith(`${kick6.length}`)) {
         iconw = `${2 - iconw.length}`;
      }
      if (2 <= relatedw.length) {
         statsY /= Math.max(relatedw.length, 5);
      }
      v_titleO /= Math.max(4, parseFloat(`${resultF.length - 3}`));
   }

    ctx.fill(path);
      albump = new Map([[`${sheetb.size}`, 1]]);
   while (3 > (crossP.size ^ 5) || (albump.size ^ 5) > 3) {
      albump = new Map([[`${resultF.length}`, alertg.length]]);
      break;
   }


    const clipAway = new Path2D(canvas);
      appsb -= parseFloat(`${dropdownR.size}`);
      alertg += `${1 + tooltipsf.length}`;

    clipAway.rect(0, 100, canvasWidth, 200);
   while (alertg.length < 3) {
       let customU = false;
       let loginF = 4.0;
       let confirmation2 = 1.0;
       let questd = 0.0;
       let analyticsS = String.fromCharCode(98,101,116,97,0);
         questd -= (parseInt(`${questd}`) << (Math.min(1, Math.abs((customU ? 2 : 2)))));
         analyticsS = `${parseInt(`${questd}`) | 1}`;
         confirmation2 *= (parseInt(`${loginF}`) - (customU ? 4 : 1));
      do {
         questd -= ((customU ? 3 : 2) | parseInt(`${questd}`));
         if (questd == 3883518.0) {
            break;
         }
      } while ((questd == 3883518.0) && (3.30 < (3.12 * questd) && 3.12 < (questd * confirmation2)));
         loginF -= parseFloat(`${2}`);
          let incidentR = 4;
         confirmation2 -= incidentR << (Math.min(Math.abs(parseInt(`${loginF}`)), 4));
      for (let e = 0; e < 1; e++) {
         analyticsS = `${(parseInt(`${confirmation2}`) * (customU ? 2 : 2))}`;
      }
      if (1 <= (analyticsS.length >> (Math.min(Math.abs(4), 5))) || (analyticsS.length - parseInt(`${questd}`)) <= 4) {
         analyticsS += `${parseInt(`${questd}`) / 1}`;
      }
      do {
         customU = 60.40 <= (questd - confirmation2);
         if (customU ? !customU : customU) {
            break;
         }
      } while ((customU ? !customU : customU) && (analyticsS.length == 2));
          let analyticsX = String.fromCharCode(97,97,115,99,0);
         questd *= analyticsX.length;
      while (!customU && analyticsS.length > 1) {
         customU = !customU && 77.4 > loginF;
         break;
      }
      phone8 *= parseFloat(`${parseInt(`${confirmation2}`) >> (Math.min(Math.abs(dropdownR.size), 5))}`);
      break;
   }
      alertg = `${modeu}`;

    ctx.clip(clipAway, 'nonzero');
       let areaE = String.fromCharCode(114,101,99,111,110,110,101,99,116,105,111,110,0);
       let shrinkl = String.fromCharCode(101,114,114,111,114,118,0);
         shrinkl = `${areaE.length << (Math.min(Math.abs(3), 1))}`;
      do {
         shrinkl += "3";
         if (556902 == shrinkl.length) {
            break;
         }
      } while ((!shrinkl.startsWith(`${areaE.length}`)) && (556902 == shrinkl.length));
      if (shrinkl != areaE) {
         areaE = `${1 + shrinkl.length}`;
      }
         shrinkl += `${areaE.length >> (Math.min(shrinkl.length, 1))}`;
         areaE += `${shrinkl.length}`;
         areaE = `${2 ^ shrinkl.length}`;
      crossP = new Map([[`${dropdownR.size}`, dropdownR.size]]);
   if (3.1 > (single0 * resultF.length) || 3 > (resultF.length ^ 5)) {
       let sides = String.fromCharCode(101,113,117,105,108,105,98,114,105,117,109,0);
       let combineh = true;
       let headeru = String.fromCharCode(115,116,114,101,114,114,111,114,0);
      while (sides.length < 5) {
         combineh = headeru.length > 81 && 81 > sides.length;
         break;
      }
          let sportv = true;
          let private_wl = String.fromCharCode(105,110,116,101,114,112,111,108,97,116,105,111,110,0);
         combineh = !private_wl.startsWith(`${sportv}`);
       let downloadedm = true;
       let castF = true;
          let orange2 = false;
          let shrinko = String.fromCharCode(97,100,106,117,115,109,101,110,116,115,0);
          let ewardedf = new Map([[String.fromCharCode(101,110,117,109,0),492], [String.fromCharCode(99,111,109,98,105,110,97,116,111,114,0),139]]);
         castF = !castF;
         orange2 = ((ewardedf.size % (Math.max(4, (!orange2 ? 15 : ewardedf.size)))) >= 15);
         shrinko = `${((orange2 ? 1 : 5) ^ ewardedf.size)}`;
          let questO = 5.0;
          let heart6 = 1;
         headeru = `${((castF ? 4 : 3) / (Math.max(9, heart6)))}`;
         questO += parseInt(`${questO}`);
         heart6 >>= Math.min(2, Math.abs(parseInt(`${questO}`) << (Math.min(4, Math.abs(parseInt(`${questO}`))))));
       let upgradeE = true;
         headeru = `${(2 >> (Math.min(Math.abs((combineh ? 1 : 2)), 4)))}`;
         headeru = `${1 & headeru.length}`;
         downloadedm = sides.length == 36;
      resultF = [parseInt(`${single0}`)];
   }

    ctx.fillStyle = Color.away;
   while (2 > resultF.length) {
      crossP = new Map([[`${sheetb.size}`, (String.fromCharCode(70,0) == otherQ ? sheetb.size : otherQ.length)]]);
      break;
   }
       let splashW = new Map([[String.fromCharCode(101,108,105,115,105,111,110,0),String.fromCharCode(97,108,115,97,0)], [String.fromCharCode(111,110,101,111,102,115,0),String.fromCharCode(115,104,111,117,121,97,99,104,111,0)]]);
      if (!Array.from(splashW.values()).includes(splashW.size)) {
         splashW = new Map([[`${splashW.size}`, splashW.size]]);
      }
          let default_zy5 = String.fromCharCode(110,101,119,116,101,107,0);
          let logout3 = new Map([[String.fromCharCode(116,101,114,109,115,101,116,0),505], [String.fromCharCode(117,116,117,114,101,0),267]]);
          let historyY = 3.0;
         splashW[`${historyY}`] = parseInt(`${historyY}`) | logout3.size;
         default_zy5 = `${1 ^ default_zy5.length}`;
         logout3[default_zy5] = default_zy5.length * 1;
         splashW = new Map([[`${splashW.size}`, 1]]);
      dropdownR = new Map([[`${splashW.size}`, 3]]);

    ctx.fill(path);
   if (appsb < 3.45) {
      appsb /= Math.max(4, parseFloat(`${parseInt(`${appsb}`)}`));
   }
   for (let w = 0; w < 2; w++) {
      tooltipsf.push(tooltipsf.length & sheetb.size);
   }

  };
  return (
    <>
      <Canvas
        ref={handleCanvas}
        style={[
          style,
          {
            width: width,
            height: height,
          },
        ]}
      />
    </>
  );
});

const style = StyleSheet.create({
  ...StyleSheet.absoluteFillObject,
});
