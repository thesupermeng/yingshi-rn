import React, {memo, useEffect, useState} from 'react';
import {Image, Text, View} from 'react-native';
import DefaultTeamIcon from '../defaultTeamIcon';
import styles from './tt_with_success';
import {
  CornerKick,
  Goal,
  PenaltyGoal,
  RedCard,
  YellowCard,
  YellowToRedCard,
} from '../../../../static/sports';
import {Colors} from '../../global/tt_umeng';
import {CanvasIncident} from './tt_ping_roboto';

const chartHeight = 40;
const iconHeight = 24;

const EventIcon = {
  1: Goal,
  2: CornerKick,
  3: YellowCard,
  4: RedCard,
  15: YellowToRedCard,
  29: PenaltyGoal,
};

const AreaChart = props => {
       let signinupj = 3.0;
    let detail9 = String.fromCharCode(108,97,115,116,110,111,100,101,95,118,95,54,52,0);
    let mimo9 = String.fromCharCode(122,95,50,95,108,111,99,97,108,105,122,97,98,108,101,0);
    let window_kp = [440, 673, 379];
    let catalogQ = 0.0;
    let delegate_pp = String.fromCharCode(112,114,111,116,111,95,104,95,53,55,0);
    let leaguen = 3.0;
    let play8 = 1;
   if ((detail9.length - parseInt(`${signinupj}`)) < 5) {
      signinupj *= parseFloat(`${detail9.length / 2}`);
   }

  const {liveRoomUpdate, chartCallback} = props;
      leaguen /= Math.max(4, parseInt(`${leaguen}`) | parseInt(`${signinupj}`));

  const [rightContentWidth, setRightContentWidth] = useState(0);
       let filled_ = 2.0;
         filled_ += parseFloat(`${parseInt(`${filled_}`)}`);
         filled_ -= parseFloat(`${1}`);
      while (1.57 <= (filled_ - 3)) {
         filled_ += parseFloat(`${3 % (Math.max(2, parseInt(`${filled_}`)))}`);
         break;
      }
      detail9 += `${parseInt(`${signinupj}`) * parseInt(`${catalogQ}`)}`;

  

   while (delegate_pp != String.fromCharCode(122,0)) {
      detail9 += `${2 * detail9.length}`;
      break;
   }
  

   do {
       let malaysiao = new Map([[String.fromCharCode(105,110,118,101,114,116,101,100,95,101,95,52,53,0),true ], [String.fromCharCode(106,95,52,49,95,115,101,110,100,101,114,0),true ]]);
       let libcrashsdkZ = String.fromCharCode(100,105,102,102,120,95,54,95,50,51,0);
       let unselectedL = false;
       let backwardg = false;
       let rulese = String.fromCharCode(117,95,49,52,95,114,101,99,121,99,108,101,0);
       let indexP = [756, 110, 226];
         backwardg = rulese.length >= 33 && unselectedL;
      if (3 >= malaysiao.size) {
         malaysiao[`${unselectedL}`] = 1 - malaysiao.size;
      }
      do {
         unselectedL = !unselectedL;
         if (unselectedL ? !unselectedL : unselectedL) {
            break;
         }
      } while ((!rulese.endsWith(`${unselectedL}`)) && (unselectedL ? !unselectedL : unselectedL));
      for (let e = 0; e < 1; e++) {
          let sentry6 = true;
         malaysiao[`${libcrashsdkZ}`] = malaysiao.size;
         sentry6 = !sentry6;
      }
      while (!backwardg) {
         rulese += "1";
         break;
      }
         malaysiao = new Map([[`${malaysiao.size}`, indexP.length]]);
      if (libcrashsdkZ.length <= 1) {
         malaysiao[`${backwardg}`] = malaysiao.size;
      }
      do {
         indexP.push(malaysiao.size);
         if (indexP.length == 2734478) {
            break;
         }
      } while ((malaysiao.size >= indexP.length) && (indexP.length == 2734478));
         indexP = [((unselectedL ? 3 : 1))];
      for (let q = 0; q < 1; q++) {
          let b_count8 = false;
          let champione = 3;
         rulese += `${champione}`;
         b_count8 = (b_count8 ? !b_count8 : b_count8);
         champione %= Math.max(((b_count8 ? 1 : 2) % (Math.max(3, (b_count8 ? 1 : 2)))), 2);
      }
       let colors4 = String.fromCharCode(109,111,118,101,112,97,103,101,95,48,95,52,55,0);
       let screenm = String.fromCharCode(110,95,57,95,117,110,98,97,110,0);
         malaysiao = new Map([[`${indexP.length}`, ((unselectedL ? 3 : 3) % (Math.max(indexP.length, 6)))]]);
      for (let x = 0; x < 3; x++) {
         colors4 += `${indexP.length}`;
      }
       let rewindw = 1.0;
       let greyP = 5.0;
      signinupj -= parseFloat(`${parseInt(`${catalogQ}`) >> (Math.min(detail9.length, 4))}`);
      if (2681652.0 == signinupj) {
         break;
      }
   } while ((2681652.0 == signinupj) && (!delegate_pp.startsWith(`${signinupj}`)));
  

   while (detail9.length == 1) {
      detail9 = `${mimo9.length + delegate_pp.length}`;
      break;
   }
  

   if (detail9.length > 4) {
      detail9 += `${play8 / (Math.max(3, 8))}`;
   }
  

   for (let e = 0; e < 2; e++) {
      mimo9 += `${parseInt(`${catalogQ}`)}`;
   }
  

      detail9 = `${delegate_pp.length}`;
  

   if ((detail9.length >> (Math.min(Math.abs(2), 2))) > 4 && (1.39 * signinupj) > 2.69) {
      detail9 = `${delegate_pp.length * 1}`;
   }
  

   for (let c = 0; c < 1; c++) {
       let volumem = false;
       let whatsappl = String.fromCharCode(114,95,55,54,95,99,97,110,99,101,108,108,101,100,0);
      do {
         whatsappl = `${((volumem ? 1 : 3))}`;
         if (862274 == whatsappl.length) {
            break;
         }
      } while ((whatsappl.length <= 2 || volumem) && (862274 == whatsappl.length));
      if (whatsappl.startsWith(`${volumem}`)) {
         whatsappl += "3";
      }
          let androida = String.fromCharCode(116,114,97,110,115,102,101,114,95,98,95,49,55,0);
          let sideE = 0.0;
         whatsappl = `${androida.length % (Math.max(whatsappl.length, 4))}`;
         androida += `${parseInt(`${sideE}`)}`;
         volumem = whatsappl.length > 41 && volumem;
         volumem = whatsappl.includes(`${volumem}`);
         whatsappl = `${whatsappl.length / 1}`;
      play8 -= parseInt(`${catalogQ}`) ^ window_kp.length;
   }
  

      catalogQ *= parseFloat(`${detail9.length / (Math.max(2, 9))}`);
  

   while (2 < detail9.length) {
      delegate_pp += "1";
      break;
   }
  const home = liveRoomUpdate?.home;
       let userK = new Map([[String.fromCharCode(104,97,114,100,99,111,100,101,100,95,48,95,56,53,0),948], [String.fromCharCode(101,110,104,97,110,99,101,100,95,119,95,54,55,0),11], [String.fromCharCode(97,112,99,109,95,120,95,54,49,0),146]]);
       let countdown7 = 5;
      if (2 <= (userK.size << (Math.min(4, Math.abs(countdown7)))) && (userK.size << (Math.min(4, Math.abs(countdown7)))) <= 2) {
         countdown7 &= userK.size;
      }
         countdown7 <<= Math.min(3, Math.abs(countdown7));
       let vignettel = false;
       let settingg = true;
       let dialogk = true;
      while (dialogk) {
         dialogk = 10 == countdown7;
         break;
      }
      do {
          let sourceE = 0.0;
          let list1 = new Map([[String.fromCharCode(112,95,56,95,103,101,116,110,101,116,119,111,114,107,112,97,114,97,109,115,0),false ], [String.fromCharCode(112,95,52,50,95,100,105,115,101,109,118,111,119,101,108,101,100,0),true ]]);
          let bellV = String.fromCharCode(102,101,101,95,102,95,56,48,0);
         dialogk = (bellV.length * userK.size) >= 43;
         sourceE *= list1.size;
         list1 = new Map([[`${list1.size}`, 3]]);
         bellV = "2";
         if (dialogk ? !dialogk : dialogk) {
            break;
         }
      } while ((!settingg) && (dialogk ? !dialogk : dialogk));
      detail9 += "1";

  const away = liveRoomUpdate?.away;
      leaguen += window_kp.length >> (Math.min(mimo9.length, 2));

  const chartIncident = Array.prototype.filter
    .call(liveRoomUpdate?.football_match_live?.tlive || [], x => x.main === 1)
    .sort((a, b) => {
      if (a.time === b.time) {

   for (let y = 0; y < 2; y++) {
       let placeholderB = String.fromCharCode(102,97,105,108,115,95,112,95,54,51,0);
       let playK = 0.0;
       let photoK = false;
       let rewardp = 5;
       let foregroundv = String.fromCharCode(105,109,103,95,113,95,56,49,0);
      if ((rewardp * 1) > 1 && 1 > (foregroundv.length * 1)) {
         rewardp <<= Math.min(Math.abs(parseInt(`${playK}`)), 4);
      }
      for (let q = 0; q < 1; q++) {
         placeholderB = `${1 ^ placeholderB.length}`;
      }
      if ((5 >> (Math.min(2, Math.abs(rewardp)))) == 5 || rewardp == 5) {
         rewardp *= rewardp >> (Math.min(1, Math.abs(3)));
      }
         foregroundv += `${placeholderB.length | 1}`;
      for (let c = 0; c < 2; c++) {
          let temperatureK = String.fromCharCode(117,112,108,111,97,100,101,100,95,110,95,55,50,0);
          let greenP = String.fromCharCode(115,95,52,50,95,102,97,117,108,116,121,0);
          let modulek = 4;
         rewardp &= temperatureK.length;
         temperatureK = `${greenP.length}`;
         greenP = `${(String.fromCharCode(71,0) == greenP ? modulek : greenP.length)}`;
         modulek += greenP.length;
      }
         playK -= parseFloat(`${parseInt(`${playK}`) + foregroundv.length}`);
         placeholderB += `${foregroundv.length}`;
      do {
         placeholderB = `${1 * parseInt(`${playK}`)}`;
         if (3883440 == placeholderB.length) {
            break;
         }
      } while ((foregroundv == placeholderB) && (3883440 == placeholderB.length));
      while ((4.66 + playK) == 3.68) {
          let custom_ = [289, 689, 828];
          let scorez = 1;
          let football9 = 2;
          let mbnativeA = String.fromCharCode(109,101,109,100,117,112,95,101,95,49,51,0);
          let selectedv = 5;
         playK /= Math.max(parseFloat(`${foregroundv.length}`), 4);
         custom_ = [scorez % (Math.max(custom_.length, 5))];
         scorez >>= Math.min(Math.abs(selectedv), 5);
         football9 *= 1;
         mbnativeA += `${selectedv & mbnativeA.length}`;
         break;
      }
      if (parseFloat(`${placeholderB.length}`) <= playK) {
         playK += parseFloat(`${3 + placeholderB.length}`);
      }
         playK /= Math.max(1, (parseFloat(`${String.fromCharCode(75,0) == placeholderB ? placeholderB.length : rewardp}`)));
         rewardp += 1 / (Math.max(6, placeholderB.length));
          let megaphoneT = new Map([[String.fromCharCode(99,95,55,48,95,111,118,101,114,119,114,105,116,101,0),true ], [String.fromCharCode(111,95,52,55,95,98,105,111,109,101,116,114,105,99,0),false ], [String.fromCharCode(97,95,52,50,0),true ]]);
         rewardp <<= Math.min(4, parseInt(`${Math.abs(((photoK ? 5 : 1) % (Math.max(1, megaphoneT.size))))}`));
      for (let y = 0; y < 3; y++) {
         playK /= Math.max(1, parseFloat(`${2}`));
      }
      for (let h = 0; h < 2; h++) {
         foregroundv += `${(String.fromCharCode(68,0) == placeholderB ? placeholderB.length : parseInt(`${playK}`))}`;
      }
      leaguen -= (delegate_pp == String.fromCharCode(100,0) ? parseInt(`${catalogQ}`) : delegate_pp.length);
   }
        return 0;
      } else if (a.time > b.time) {

   while (5 == mimo9.length) {
       let rightA = 4.0;
       let placeholderf = String.fromCharCode(103,95,53,95,115,109,112,116,101,0);
       let baiduw = 2.0;
         placeholderf += `${(String.fromCharCode(120,0) == placeholderf ? placeholderf.length : parseInt(`${rightA}`))}`;
          let grayU = 5.0;
         placeholderf += `${(placeholderf == String.fromCharCode(111,0) ? placeholderf.length : parseInt(`${grayU}`))}`;
          let trashc = 5.0;
          let linkW = String.fromCharCode(115,97,116,117,114,97,116,101,95,50,95,49,49,0);
          let interstitialp = String.fromCharCode(99,101,110,116,114,97,108,95,116,95,52,48,0);
         rightA *= parseInt(`${rightA}`) >> (Math.min(Math.abs(2), 2));
         trashc *= 3;
         linkW = `${parseInt(`${trashc}`) % (Math.max(interstitialp.length, 8))}`;
         interstitialp += `${interstitialp.length * linkW.length}`;
      for (let o = 0; o < 3; o++) {
          let greenu = 0;
          let benefita = String.fromCharCode(112,111,115,116,101,110,99,111,100,101,95,122,95,49,48,0);
         baiduw /= Math.max(2, greenu << (Math.min(4, Math.abs(3))));
         greenu &= 3;
         benefita += `${benefita.length & benefita.length}`;
      }
      for (let j = 0; j < 1; j++) {
         rightA *= 1;
      }
      detail9 += `${3 + mimo9.length}`;
      break;
   }
        return 1;
      } else {

   if (5 <= detail9.length) {
       let unreadn = [707, 448];
       let downloadO = 4;
       let shootP = true;
       let filterF = 1.0;
       let footballn = 0;
      while ((downloadO ^ 4) <= 5 || !shootP) {
         shootP = downloadO == footballn;
         break;
      }
      do {
         filterF += (parseFloat(`${(shootP ? 1 : 3) % (Math.max(footballn, 10))}`));
         if (filterF == 1233458.0) {
            break;
         }
      } while ((filterF == 1233458.0) && (filterF > footballn));
          let middlewareX = String.fromCharCode(118,105,115,105,98,105,108,105,116,121,95,51,95,55,55,0);
         downloadO += downloadO % (Math.max(8, unreadn.length));
         middlewareX += "3";
         footballn ^= 1 << (Math.min(3, unreadn.length));
      for (let d = 0; d < 1; d++) {
         shootP = 32 <= downloadO;
      }
      while (shootP && 4 <= (3 | footballn)) {
         shootP = !shootP;
         break;
      }
         shootP = parseFloat(`${footballn}`) < filterF;
          let completeq = false;
         unreadn.push(downloadO);
         completeq = !completeq && completeq;
         filterF /= Math.max(parseFloat(`${parseInt(`${filterF}`) ^ 2}`), 3);
         footballn += footballn;
      for (let r = 0; r < 3; r++) {
          let upgradeR = false;
          let modeq = String.fromCharCode(114,95,52,56,95,114,101,100,101,109,112,116,105,111,110,0);
          let hongkong9 = new Map([[String.fromCharCode(112,115,97,95,103,95,48,0),253], [String.fromCharCode(117,95,52,48,95,116,104,101,114,101,117,109,0),855]]);
         downloadO >>= Math.min(5, Math.abs(2));
         upgradeR = modeq.length == 90;
         modeq += `${(modeq == String.fromCharCode(108,0) ? modeq.length : (upgradeR ? 2 : 1))}`;
         hongkong9[`${upgradeR}`] = 3;
      }
      detail9 += `${parseInt(`${catalogQ}`) | 3}`;
   }
        return -1;
      window_kp.push(parseInt(`${catalogQ}`));

      }
    });
      window_kp = [3];


  const chartData = liveRoomUpdate?.football_match_trend?.data;
   for (let f = 0; f < 1; f++) {
       let unselectedZ = new Map([[String.fromCharCode(109,101,109,111,106,105,95,56,95,56,53,0),582], [String.fromCharCode(116,95,55,54,95,115,111,105,115,99,111,110,110,101,99,116,101,100,0),820]]);
       let ajaxL = 3.0;
      do {
         ajaxL += parseFloat(`${1 >> (Math.min(1, Math.abs(parseInt(`${ajaxL}`))))}`);
         if (ajaxL == 4410991.0) {
            break;
         }
      } while ((ajaxL == 4410991.0) && (2.14 >= (ajaxL * 4.30)));
      do {
          let settingsx = new Map([[String.fromCharCode(112,107,99,115,95,120,95,52,48,0),497], [String.fromCharCode(122,99,111,110,118,111,108,118,101,95,107,95,57,0),337]]);
          let fastforwardc = 3.0;
          let pointi = String.fromCharCode(114,101,116,114,97,110,115,109,105,115,115,105,111,110,95,101,95,56,49,0);
          let androidl = 4;
          let a_unlockL = 1.0;
         unselectedZ = new Map([[`${androidl}`, androidl % (Math.max(parseInt(`${a_unlockL}`), 8))]]);
         settingsx = new Map([[`${settingsx.size}`, 3 | pointi.length]]);
         fastforwardc *= 2;
         pointi += `${pointi.length % (Math.max(3, 4))}`;
         a_unlockL -= settingsx.size % (Math.max(7, parseInt(`${fastforwardc}`)));
         if (4396051 == unselectedZ.size) {
            break;
         }
      } while ((4396051 == unselectedZ.size) && ((2.81 / (Math.max(9, ajaxL))) >= 4.55));
         ajaxL *= parseFloat(`${parseInt(`${ajaxL}`)}`);
      do {
         unselectedZ[`${ajaxL}`] = parseInt(`${ajaxL}`) * 1;
         if (2264512 == unselectedZ.size) {
            break;
         }
      } while ((4.75 >= (5.0 * ajaxL) && 4 >= (unselectedZ.size * 3)) && (2264512 == unselectedZ.size));
      while (!Array.from(unselectedZ.values()).includes(ajaxL)) {
         ajaxL *= parseFloat(`${2}`);
         break;
      }
       let optionsY = String.fromCharCode(115,112,97,110,115,95,111,95,56,48,0);
       let setting7 = String.fromCharCode(117,95,51,52,95,115,101,112,97,114,97,116,101,0);
      catalogQ *= parseFloat(`${2}`);
   }

  if (!chartData || !chartData.length) {
    chartCallback?.(false);
   while (delegate_pp.length == play8) {
      delegate_pp = `${parseInt(`${catalogQ}`)}`;
      break;
   }

    return;
  } else {
    chartCallback?.(true);
      play8 /= Math.max(2, 1 & parseInt(`${signinupj}`));

  }

  const time = chartData?.length;
   if (!detail9.startsWith(`${window_kp.length}`)) {
      window_kp = [parseInt(`${signinupj}`) & detail9.length];
   }

  const totalTime = time <= 90 ? 90 : time;
   do {
      mimo9 = "2";
      if (String.fromCharCode(107,113,113,110,106,51,104,117,0) == mimo9) {
         break;
      }
   } while ((String.fromCharCode(107,113,113,110,106,51,104,117,0) == mimo9) && ((window_kp.length % 4) > 4 || 4 > (window_kp.length % (Math.max(mimo9.length, 1)))));
 

       let spinnerk = 4.0;
       let layoutX = true;
       let filedB = 3.0;
       let down0 = String.fromCharCode(110,117,109,101,114,105,102,121,95,48,95,55,56,0);
       let unselectedX = String.fromCharCode(106,95,48,95,115,117,112,101,114,102,114,97,109,101,0);
      delegate_pp = `${3 * parseInt(`${signinupj}`)}`;
      spinnerk += parseFloat(`${1 * parseInt(`${spinnerk}`)}`);

  const minutes = [0, 15, 30, 45, 60, 75, totalTime];
   if (mimo9.startsWith(`${play8}`)) {
      play8 *= window_kp.length * 2;
   }


  

      window_kp.push(play8);
  const incidentFilter = incidents => {

       let updatesF = new Map([[String.fromCharCode(118,97,114,105,110,116,95,49,95,55,50,0),213], [String.fromCharCode(105,95,49,51,95,117,118,109,118,0),289], [String.fromCharCode(111,102,102,105,99,105,97,108,95,49,95,57,55,0),395]]);
          let albumA = [673, 482];
          let statsf = 3.0;
         updatesF = new Map([[`${updatesF.size}`, parseInt(`${statsf}`)]]);
         albumA.push(albumA.length * 3);
         statsf += albumA.length >> (Math.min(Math.abs(3), 1));
       let model6 = 2;
       let greenK = 4;
      if (1 == model6) {
          let alertC = [951, 117];
          let blacklistm = new Map([[String.fromCharCode(119,97,118,114,101,97,100,101,114,95,106,95,51,53,0),955], [String.fromCharCode(98,114,101,97,100,99,114,117,109,98,115,95,117,95,55,52,0),220], [String.fromCharCode(114,95,56,54,95,115,117,98,108,97,121,111,117,116,0),385]]);
          let heartX = 3.0;
          let minivodT = [946, 693, 15];
          let storem = true;
         greenK |= 1;
         alertC.push(parseInt(`${heartX}`));
         blacklistm[`${minivodT.length}`] = minivodT.length << (Math.min(Math.abs(2), 4));
         heartX /= Math.max(parseFloat(`${2 * blacklistm.size}`), 1);
         storem = (96 >= (minivodT.length * (storem ? minivodT.length : 96)));
      }
      detail9 += `${mimo9.length / 1}`;
    var allocateTime;
       let frame_8Q = [916, 362, 748];
       let serviceL = true;
         serviceL = (((!serviceL ? 53 : frame_8Q.length) ^ frame_8Q.length) < 53);
      while (serviceL) {
         serviceL = serviceL || frame_8Q.length == 37;
         break;
      }
      detail9 += `${(String.fromCharCode(116,0) == delegate_pp ? frame_8Q.length : delegate_pp.length)}`;

    return incidents?.map((x, idx) => {
      const curTime = x.time;
       let indicatory = String.fromCharCode(120,104,116,109,108,95,101,95,51,51,0);
       let roboto_ = String.fromCharCode(116,95,50,56,95,106,115,105,109,100,100,99,116,0);
       let white6 = false;
       let mbbanner5 = true;
       let anythinkt = 5.0;
       let loginY = 0.0;
      for (let c = 0; c < 3; c++) {
         white6 = (!mbbanner5 ? !white6 : mbbanner5);
      }
          let upload1 = String.fromCharCode(101,120,112,111,114,116,95,110,95,49,57,0);
         roboto_ += "3";
         upload1 += `${upload1.length | upload1.length}`;
      for (let f = 0; f < 2; f++) {
         indicatory = `${((white6 ? 3 : 4) - 3)}`;
      }
      while (2 < roboto_.length || white6) {
         roboto_ += "2";
         break;
      }
      play8 ^= 2;

      if (idx > 0) {

   for (let l = 0; l < 1; l++) {
      mimo9 += `${3 * parseInt(`${leaguen}`)}`;
   }
        if (curTime <= allocateTime) {
          return {...x, allocateTime: ++allocateTime};
        } else {
          allocateTime = curTime;
          return {...x, allocateTime: allocateTime};
        }
      } else {
        allocateTime = curTime;
        return {...x, allocateTime: curTime};
      }
    });
  };
  const filteredHomeIncident = incidentFilter(
    chartIncident?.filter(x => x.position === 1),
  );
  const filteredAwayIncident = incidentFilter(
    chartIncident?.filter(x => x.position === 2),
  );

  
  

  const rightContentlayout = e => {
    setRightContentWidth(e.nativeEvent.layout.width);
  };

  const IncidentIcons = ({data}) => {
    return (
      <View style={{...styles.eventContent}}>
        {data?.map((e, index) => {
          const size = e.type === 1 || e.type === 29 ? 8 : 10;
          return (
            <Image
              key={`filteredHomeIncident-${index}`}
              style={{
                width: size,
                height: size,
                position: 'absolute',
                left: (rightContentWidth * e.allocateTime) / totalTime,
                transform: [{translateX: -size / 2}],
              }}
              source={EventIcon[e.type] || ''}
              resizeMode={'contain'}></Image>
          );
        })}
      </View>
    );
  };
  return (
    <View style={styles.container}>
      <View style={styles.rowContent}>
        <View style={styles.leftContent}></View>
        <View
          style={[styles.rightContent, {height: iconHeight}]}
          onLayout={rightContentlayout}>
          <View
            style={{
              position: 'relative',
              flexDirection: 'row',
              paddingBottom: 10,
            }}>
            {minutes.map((e, index) => {
              return (
                <Text
                  key={`footBallAreaChartMinute-${index}`}
                  style={[
                    styles.minuteText,
                    {
                      width: 400,
                      textAlign: 'center',
                      position: 'absolute',
                      left: (e / totalTime) * rightContentWidth,
                      transform: [{translateX: -200}],
                    },
                  ]}>
                  {e === 45 ? 'HT' : ` ${e}'`}
                </Text>
              );
            })}
          </View>
          {/* Home Incident */}
          <IncidentIcons data={filteredHomeIncident || []} />
        </View>
      </View>
      <View style={styles.rowContent}>
        <View style={styles.leftContent}>
          {home?.icon ? (
            <Image style={styles.teamIcon} source={{uri: home?.icon}}></Image>
          ) : (
            <DefaultTeamIcon iconSize={20} isHome={true} text={home?.name} />
          )}
          {away?.icon ? (
            <Image style={styles.teamIcon} source={{uri: away?.icon}}></Image>
          ) : (
            <DefaultTeamIcon iconSize={20} isHome={false} text={away?.name} />
          )}
        </View>
        <View style={[styles.rightContent, {height: chartHeight}]}>
          <View style={{position: 'relative'}}>
            {/* 背景颜色 */}
            <View
              style={{
                position: 'absolute',
                height: chartHeight,
                width: rightContentWidth,
              }}>
              <View
                style={{
                  position: 'absolute',
                  top: 0,
                  bottom: '50%',
                  left: 0,
                  right: 0,
                  backgroundColor: 'rgb(38, 39, 42)',
                }}></View>
              <View
                style={{
                  position: 'absolute',
                  top: '50%',
                  bottom: 0,
                  left: 0,
                  right: 0,
                  backgroundColor: 'rgb(38, 39, 42)',
                }}></View>
            </View>
            {/* 时间指标 */}
            <View
              style={{
                position: 'absolute',
                height: chartHeight,
                width: 1,
                zIndex: 99,
                backgroundColor: Colors.colorPrimary,
                left: (time / totalTime) * rightContentWidth + 1,
              }}
            />
            <CanvasIncident
              data={chartData}
              height={chartHeight}
              width={(time / totalTime) * rightContentWidth}
            />
          </View>
        </View>
      </View>
      <View style={styles.rowContent}>
        <View style={styles.leftContent}></View>
        <View style={[styles.rightContent, {height: iconHeight}]}>
          {/* Away Incident */}
          <IncidentIcons data={filteredAwayIncident || []} />
        </View>
      </View>
    </View>
  );
};

export default memo(AreaChart);
