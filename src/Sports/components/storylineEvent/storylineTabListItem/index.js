import React from 'react';
import {View, Text, Image} from 'react-native';
import {ColorSpace} from 'react-native-reanimated';
import {GoalLogo, PenaltyMatchIcon} from '../../../assets';

import StorylineTap from '../storylineTab';
import {getMatchStorylineStatus} from '../../../utility/utils';

import styles from './wawa_hiad_iconnewchat';

const StorylineTabListItem = props => {
       let castM = false;
    let imagewatchlivej = String.fromCharCode(102,108,97,103,95,117,95,51,53,0);
    let policyo = String.fromCharCode(97,112,112,114,101,99,97,116,105,111,110,104,111,117,114,95,116,95,53,53,0);
    let philippinesZ = [764, 240];
    let privilegeJ = String.fromCharCode(115,105,103,110,97,116,117,114,101,115,95,105,95,57,55,0);
    let corea = 4;
    let iconclosewhiten = 2.0;
    let orangeticky = String.fromCharCode(116,105,108,101,95,113,95,53,0);
    let smallbrightnessP = 4.0;
    let defaultpredictionprofile9 = 1.0;
    let untickI = String.fromCharCode(122,95,49,56,95,115,116,97,114,0);
    let servicei = String.fromCharCode(118,101,114,116,105,99,97,108,108,121,95,49,95,52,48,0);
      privilegeJ = `${((castM ? 4 : 3) * parseInt(`${iconclosewhiten}`))}`;

  const arrayValue = props?.data;
   while (imagewatchlivej == orangeticky) {
      orangeticky += `${2 - philippinesZ.length}`;
      break;
   }

  const dataTesting = {imageName: GoalLogo, textName: '123'};
  // const dataTime = arrayValue?.time > 90 ? 90 : arrayValue?.time;
       let dragZ = 2.0;
       let disconnectedlogom = new Map([[String.fromCharCode(111,102,102,95,112,95,54,55,0),623], [String.fromCharCode(99,111,110,99,104,95,116,95,57,51,0),444]]);
         dragZ /= Math.max(5, disconnectedlogom.size);
      for (let o = 0; o < 2; o++) {
         dragZ *= parseInt(`${dragZ}`);
      }
       let iconcalendarG = 2.0;
      for (let l = 0; l < 3; l++) {
         dragZ -= parseInt(`${iconcalendarG}`);
      }
      if (2 < (disconnectedlogom.size / (Math.max(4, parseInt(`${iconcalendarG}`))))) {
         disconnectedlogom = new Map([[`${disconnectedlogom.size}`, disconnectedlogom.size]]);
      }
      if (disconnectedlogom.size > parseInt(`${dragZ}`)) {
         dragZ -= disconnectedlogom.size;
      }
      corea ^= (3 % (Math.max(7, (castM ? 5 : 5))));

  const dataTime = arrayValue?.time;
   for (let s = 0; s < 2; s++) {
      privilegeJ += `${privilegeJ.length}`;
   }

  const dataType = arrayValue?.type;
       let awayL = String.fromCharCode(110,101,97,114,98,121,95,116,95,56,57,0);
       let attributedstringa = String.fromCharCode(104,95,52,49,95,114,101,99,111,110,102,105,103,117,114,97,116,105,111,110,0);
         awayL = `${awayL.length << (Math.min(Math.abs(2), 5))}`;
      for (let u = 0; u < 2; u++) {
         awayL += `${(attributedstringa == String.fromCharCode(52,0) ? attributedstringa.length : awayL.length)}`;
      }
         awayL = `${3 | awayL.length}`;
      for (let s = 0; s < 2; s++) {
          let bgvipsports = String.fromCharCode(115,95,50,54,95,109,111,100,101,0);
          let inewsE = String.fromCharCode(99,111,110,103,95,107,95,57,49,0);
          let privacyL = new Map([[String.fromCharCode(119,101,120,112,97,110,100,95,99,95,54,51,0),515], [String.fromCharCode(112,97,115,116,95,113,95,51,54,0),3]]);
         attributedstringa = "2";
         bgvipsports += `${inewsE.length}`;
         inewsE = `${inewsE.length % (Math.max(3, bgvipsports.length))}`;
         privacyL[bgvipsports] = bgvipsports.length;
      }
      do {
         attributedstringa = `${attributedstringa.length}`;
         if (String.fromCharCode(103,113,109,122,118,116,56,0) == attributedstringa) {
            break;
         }
      } while ((awayL.length >= 3) && (String.fromCharCode(103,113,109,122,118,116,56,0) == attributedstringa));
         awayL = `${(String.fromCharCode(110,0) == awayL ? attributedstringa.length : awayL.length)}`;
      imagewatchlivej = `${(policyo == String.fromCharCode(79,0) ? policyo.length : (castM ? 3 : 1))}`;

  const matchStorylineStatus = getMatchStorylineStatus(dataType);
       let basketballdetailsbgS = 0;
      for (let g = 0; g < 1; g++) {
         basketballdetailsbgS /= Math.max(5, basketballdetailsbgS);
      }
      if (2 == (basketballdetailsbgS ^ 3)) {
          let basketballtrophy9 = true;
          let whistleorangeQ = [902, 323, 378];
          let ballt5 = String.fromCharCode(121,95,49,54,95,116,97,112,116,0);
         basketballdetailsbgS >>= Math.min(1, Math.abs(((basketballtrophy9 ? 2 : 3) | basketballdetailsbgS)));
         basketballtrophy9 = ballt5.length < whistleorangeQ.length;
         whistleorangeQ = [2 * whistleorangeQ.length];
         ballt5 += `${whistleorangeQ.length}`;
      }
         basketballdetailsbgS >>= Math.min(Math.abs(basketballdetailsbgS), 4);
      smallbrightnessP += 1;

  

      orangeticky += `${parseInt(`${iconclosewhiten}`)}`;
  const isHome = arrayValue?.position == 1 ? true : false;
   do {
      orangeticky += `${corea + 3}`;
      if (orangeticky == String.fromCharCode(102,116,54,110,100,0)) {
         break;
      }
   } while ((orangeticky == String.fromCharCode(102,116,54,110,100,0)) && (!orangeticky.startsWith(policyo)));

  const systemExcluded = [11, 12, 19, 26, 27];
   do {
      smallbrightnessP -= parseInt(`${smallbrightnessP}`) << (Math.min(imagewatchlivej.length, 4));
      if (smallbrightnessP == 2872148.0) {
         break;
      }
   } while ((smallbrightnessP == 2872148.0) && ((parseInt(`${smallbrightnessP}`) * 5) < 5 && (3.98 * smallbrightnessP) < 5.11));

  const panaltyBattleType = [29, 30];
   if ((privilegeJ.length / (Math.max(10, smallbrightnessP))) <= 5.65 || 2.34 <= (smallbrightnessP / 5.65)) {
      smallbrightnessP *= (String.fromCharCode(80,0) == policyo ? orangeticky.length : policyo.length);
   }

  

   if (policyo.length >= imagewatchlivej.length) {
      policyo = `${((castM ? 5 : 5) / 2)}`;
   }
  return (
    <View style={styles.mainContainer}>
      {!isHome && (
        <View
          style={{
            flex: 1,
          }}></View>
      )}
      {!isHome &&
        (systemExcluded.includes(dataType) ? (
          <View style={styles.orangeNumberHT}>
            <Text style={styles.HTtimeTitle}>{matchStorylineStatus}</Text>
          </View>
        ) : (
          <View style={styles.orangeNumber}>
            {panaltyBattleType.includes(dataType) ? (
              <Image
                source={PenaltyMatchIcon}
                resizeMode={'stretch'}
                style={styles.timelineIcon}></Image>
            ) : (
              <Text style={styles.timeTitle}>{dataTime}</Text>
            )}
          </View>
        ))}

      {systemExcluded.includes(dataType) ? (
        <View style={styles.mainTextContainContainer}></View>
      ) : (
        <View style={styles.mainTextContainContainer}>
          {!isHome && <View style={styles.leftArrow}></View>}

          <View style={styles.containContainer}>
            <StorylineTap isHome={isHome} listData={arrayValue}></StorylineTap>
          </View>
          {isHome && <View style={styles.rightArrow}></View>}
        </View>
      )}

      {isHome && (
        <View style={styles.orangeNumber}>
          {panaltyBattleType.includes(dataType) ? (
            <Image
              source={PenaltyMatchIcon}
              resizeMode={'stretch'}
              style={styles.timelineIcon}></Image>
          ) : (
            <Text style={styles.timeTitle}>{dataTime}</Text>
          )}
        </View>
      )}
      {isHome && <View style={{flex: 1}}></View>}
    </View>
  );
   do {
       let distr = new Map([[String.fromCharCode(115,116,114,105,100,95,116,95,54,57,0),428], [String.fromCharCode(112,97,100,100,105,110,103,95,97,95,55,52,0),920]]);
       let filter7 = true;
       let nativel = String.fromCharCode(110,95,53,56,95,100,111,116,108,111,99,107,0);
       let reactnativeratingsq = String.fromCharCode(113,116,97,98,108,101,95,117,95,52,51,0);
       let positionfieldI = true;
      for (let u = 0; u < 1; u++) {
          let baiduads3 = 5;
          let files = 4.0;
          let specx = 3.0;
          let filej = 1.0;
         distr = new Map([[`${filter7}`, 2]]);
         baiduads3 ^= parseInt(`${specx}`);
         files -= 2 + parseInt(`${filej}`);
         filej += parseInt(`${files}`);
      }
         reactnativeratingsq += `${(nativel == String.fromCharCode(76,0) ? nativel.length : distr.size)}`;
      do {
         distr = new Map([[reactnativeratingsq, (nativel == String.fromCharCode(50,0) ? nativel.length : reactnativeratingsq.length)]]);
         if (distr.size == 4218410) {
            break;
         }
      } while ((distr.size == 4218410) && (2 <= distr.size || (distr.size | 2) <= 5));
         reactnativeratingsq += `${reactnativeratingsq.length ^ 3}`;
       let distm = 2;
      for (let r = 0; r < 2; r++) {
         nativel = `${(2 - (filter7 ? 3 : 5))}`;
      }
         reactnativeratingsq += `${distm}`;
          let combineM = String.fromCharCode(114,101,112,101,97,116,105,110,103,95,53,95,55,49,0);
          let libffmpegkits = String.fromCharCode(107,95,55,56,95,115,101,99,111,110,100,0);
          let imagest = 3.0;
         filter7 = libffmpegkits.length < parseInt(`${imagest}`);
         combineM = "1";
         libffmpegkits = `${combineM.length + 3}`;
      if (nativel.includes(reactnativeratingsq)) {
          let sharedW = String.fromCharCode(98,95,54,55,95,98,108,117,114,97,121,0);
          let airbnbstarO = 0;
          let libhermesf = new Map([[String.fromCharCode(119,119,119,95,101,95,51,56,0),false ], [String.fromCharCode(99,104,97,110,103,101,104,95,115,95,51,55,0),false ], [String.fromCharCode(114,108,111,116,116,105,101,95,100,95,51,0),false ]]);
         reactnativeratingsq += `${distm ^ airbnbstarO}`;
         sharedW += `${1 & sharedW.length}`;
         airbnbstarO += libhermesf.size;
         libhermesf = new Map([[`${libhermesf.size}`, 2]]);
      }
      do {
          let mergerv = String.fromCharCode(99,100,120,108,95,55,95,49,48,0);
          let iconrefreshj = 5.0;
         nativel = `${2 ^ distm}`;
         mergerv += `${mergerv.length}`;
         iconrefreshj *= parseFloat(`${parseInt(`${iconrefreshj}`) << (Math.min(Math.abs(3), 5))}`);
         if (String.fromCharCode(121,97,101,122,103,101,118,53,104,0) == nativel) {
            break;
         }
      } while ((String.fromCharCode(121,97,101,122,103,101,118,53,104,0) == nativel) && (nativel.length > distm));
      while (positionfieldI && (distr.size << (Math.min(Math.abs(4), 2))) == 1) {
         positionfieldI = (((positionfieldI ? 86 : reactnativeratingsq.length) - reactnativeratingsq.length) >= 86);
         break;
      }
      do {
          let kickm = new Map([[String.fromCharCode(119,95,50,48,95,97,115,99,101,110,100,105,110,103,0),569], [String.fromCharCode(121,95,52,52,95,114,101,118,111,107,101,100,0),256], [String.fromCharCode(115,99,97,110,110,101,114,95,97,95,55,52,0),499]]);
          let modityA = 2;
          let libloggerT = [String.fromCharCode(105,95,55,49,95,112,97,103,101,105,110,0), String.fromCharCode(97,117,116,104,101,110,116,105,99,97,116,101,100,95,102,95,51,0)];
         positionfieldI = reactnativeratingsq.length < 23 || distm < 23;
         kickm = new Map([[`${kickm.size}`, libloggerT.length]]);
         modityA /= Math.max(5, kickm.size - 2);
         libloggerT = [1 ^ libloggerT.length];
         if (positionfieldI ? !positionfieldI : positionfieldI) {
            break;
         }
      } while ((nativel.length <= 3) && (positionfieldI ? !positionfieldI : positionfieldI));
      do {
         distm *= 1 & nativel.length;
         if (distm == 1520632) {
            break;
         }
      } while ((distm == 1520632) && ((distm / (Math.max(distr.size, 3))) > 5));
      while (2 == (distm % (Math.max(1, reactnativeratingsq.length))) || 1 == (2 % (Math.max(2, reactnativeratingsq.length)))) {
          let mbnativel = false;
          let matchactiveS = String.fromCharCode(99,111,110,102,105,103,117,114,97,98,108,101,95,122,95,51,49,0);
          let time_cnE = new Map([[String.fromCharCode(100,105,115,116,97,110,99,101,95,106,95,49,49,0),true ], [String.fromCharCode(100,95,52,55,95,101,114,97,115,101,100,0),true ], [String.fromCharCode(114,105,103,104,116,95,118,95,55,49,0),true ]]);
          let basketballtrophyk = 5.0;
         reactnativeratingsq += `${(1 ^ (filter7 ? 4 : 3))}`;
         mbnativel = String.fromCharCode(119,0) == matchactiveS;
         matchactiveS = `${3 / (Math.max(3, parseInt(`${basketballtrophyk}`)))}`;
         time_cnE = new Map([[`${time_cnE.size}`, time_cnE.size / 3]]);
         basketballtrophyk += (parseFloat(`${(mbnativel ? 5 : 1) % (Math.max(parseInt(`${basketballtrophyk}`), 2))}`));
         break;
      }
      do {
         distm >>= Math.min(Math.abs((distr.size & (filter7 ? 2 : 1))), 1);
         if (distm == 875058) {
            break;
         }
      } while ((!positionfieldI) && (distm == 875058));
      philippinesZ.push(((castM ? 3 : 5) * parseInt(`${defaultpredictionprofile9}`)));
      if (philippinesZ.length == 3986980) {
         break;
      }
   } while ((philippinesZ.length == 3986980) && (philippinesZ.length > 5));

};

export default StorylineTabListItem;
