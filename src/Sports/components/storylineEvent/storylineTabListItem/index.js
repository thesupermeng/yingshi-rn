import React from 'react';
import {View, Text, Image} from 'react-native';
import {ColorSpace} from 'react-native-reanimated';
import {GoalLogo, PenaltyMatchIcon} from '../../../assets';

import StorylineTap from '../storylineTab';
import {getMatchStorylineStatus} from '../../../utility/utils';

import styles from './ww_utils_orangeclock';

const StorylineTabListItem = props => {
       let robotoC = String.fromCharCode(116,108,111,103,95,103,95,54,54,0);
    let chinasame3 = 5.0;
    let fcdaebecbcbafcdfceaaeccfeacdbC = 5;
    let listM = String.fromCharCode(116,95,52,57,95,99,111,110,115,116,114,117,99,116,111,114,109,97,103,105,99,0);
    let defaultprofilepicP = 4;
    let runtimeA = 3.0;
    let eactm = 1.0;
    let unfillV = String.fromCharCode(108,95,54,50,95,109,100,105,97,0);
   do {
       let iconW = String.fromCharCode(109,95,56,49,95,101,120,112,101,99,116,101,100,0);
         iconW = `${iconW.length}`;
          let catalog9 = String.fromCharCode(118,101,99,95,114,95,56,51,0);
          let defaultroombgJ = true;
         iconW = `${(catalog9.length / (Math.max(9, (defaultroombgJ ? 3 : 3))))}`;
      for (let p = 0; p < 3; p++) {
         iconW = `${iconW.length}`;
      }
      chinasame3 += fcdaebecbcbafcdfceaaeccfeacdbC % (Math.max(6, parseInt(`${runtimeA}`)));
      if (chinasame3 == 2197840.0) {
         break;
      }
   } while (((5.97 * chinasame3) <= 4.78) && (chinasame3 == 2197840.0));

  const arrayValue = props?.data;
      chinasame3 += 1;

  const dataTesting = {imageName: GoalLogo, textName: '123'};
  // const dataTime = arrayValue?.time > 90 ? 90 : arrayValue?.time;
   while ((eactm / 5) == 2.41) {
       let pressureX = new Map([[String.fromCharCode(104,95,52,56,95,97,116,117,114,97,116,105,111,110,0),453], [String.fromCharCode(108,95,56,95,114,101,100,105,114,101,99,116,0),455], [String.fromCharCode(99,111,110,116,114,97,105,110,101,100,95,98,95,54,57,0),877]]);
       let fileD = 4.0;
       let androidw = true;
       let ying8 = String.fromCharCode(111,95,54,48,95,101,118,101,110,97,118,103,0);
      if (!androidw) {
         androidw = pressureX.size > 96;
      }
      do {
          let libavformat2 = [443, 105, 616];
         ying8 += "2";
         libavformat2 = [3];
         if (ying8 == String.fromCharCode(100,117,108,99,109,101,51,105,0)) {
            break;
         }
      } while ((3 >= (ying8.length | 4)) && (ying8 == String.fromCharCode(100,117,108,99,109,101,51,105,0)));
      do {
          let libruntimeexecutort = [185, 54];
         androidw = 51.43 <= fileD && !androidw;
         libruntimeexecutort.push(libruntimeexecutort.length);
         if (androidw ? !androidw : androidw) {
            break;
         }
      } while ((androidw ? !androidw : androidw) && ((fileD - 1.43) < 2.48));
          let modalo = 3.0;
         pressureX[`${fileD}`] = 3;
         modalo *= parseFloat(`${parseInt(`${modalo}`)}`);
         fileD -= (String.fromCharCode(116,0) == ying8 ? ying8.length : (androidw ? 2 : 4));
      while (pressureX.size == 3) {
          let whiteu = 4.0;
          let ksadE = String.fromCharCode(115,101,112,97,114,97,116,111,114,115,95,108,95,53,54,0);
         pressureX = new Map([[`${whiteu}`, 3 - ksadE.length]]);
         break;
      }
      if ((pressureX.size / 3) > 5 || androidw) {
          let nextX = 5;
          let showq = new Map([[String.fromCharCode(104,111,114,105,103,95,116,95,56,56,0),674], [String.fromCharCode(97,103,103,114,101,103,97,116,111,114,95,105,95,49,0),881], [String.fromCharCode(102,108,97,116,110,101,115,115,95,53,95,49,54,0),4]]);
          let successG = String.fromCharCode(98,95,56,55,95,116,108,111,103,0);
         pressureX = new Map([[ying8, parseInt(`${fileD}`) & ying8.length]]);
         nextX %= Math.max(3, showq.size);
         showq[`${nextX}`] = showq.size;
         successG = `${1 % (Math.max(8, successG.length))}`;
      }
       let condensedI = String.fromCharCode(114,101,99,118,118,95,48,95,56,57,0);
      if (4 > condensedI.length) {
         condensedI = `${(ying8 == String.fromCharCode(89,0) ? ying8.length : parseInt(`${fileD}`))}`;
      }
         pressureX[`${androidw}`] = ((androidw ? 2 : 5) | parseInt(`${fileD}`));
          let libffmpegkitd = [String.fromCharCode(118,97,108,105,100,97,116,101,100,95,112,95,51,57,0), String.fromCharCode(101,95,50,53,95,105,110,115,116,97,110,116,0), String.fromCharCode(120,95,54,51,95,99,100,120,108,0)];
          let long_6k = String.fromCharCode(121,95,51,56,95,112,104,111,110,101,115,0);
          let floatingd = String.fromCharCode(103,95,56,53,95,112,101,114,105,111,100,105,99,0);
         condensedI += `${parseInt(`${fileD}`)}`;
         libffmpegkitd = [long_6k.length + 2];
         long_6k = "2";
         floatingd = `${long_6k.length}`;
      for (let l = 0; l < 1; l++) {
          let subtextR = [140, 654];
          let listU = new Map([[String.fromCharCode(100,95,55,53,95,109,115,118,115,0),436], [String.fromCharCode(113,95,57,53,0),531], [String.fromCharCode(107,95,50,57,0),729]]);
         condensedI += "1";
         subtextR.push(3);
         listU[`${subtextR.length}`] = listU.size + subtextR.length;
      }
      chinasame3 -= parseInt(`${fileD}`);
      break;
   }

  const dataTime = arrayValue?.time;
   for (let r = 0; r < 1; r++) {
      listM = `${2 << (Math.min(4, Math.abs(parseInt(`${eactm}`))))}`;
   }

  const dataType = arrayValue?.type;
      chinasame3 /= Math.max(4, defaultprofilepicP + 3);

  const matchStorylineStatus = getMatchStorylineStatus(dataType);
       let nativeexD = [93, 230, 740];
       let modelsn = String.fromCharCode(101,97,116,105,110,103,95,52,95,50,52,0);
       let yingR = [470, 290, 436];
      for (let z = 0; z < 2; z++) {
         modelsn = `${nativeexD.length}`;
      }
          let iconp = 0;
          let room3 = String.fromCharCode(112,114,111,95,55,95,54,56,0);
         yingR.push((modelsn == String.fromCharCode(100,0) ? yingR.length : modelsn.length));
         iconp *= (room3 == String.fromCharCode(79,0) ? room3.length : iconp);
          let infoq = 2;
          let injuryd = true;
         yingR.push(2);
         infoq &= 1;
         injuryd = !injuryd;
         modelsn += `${yingR.length - modelsn.length}`;
      do {
         yingR.push(yingR.length | nativeexD.length);
         if (2787577 == yingR.length) {
            break;
         }
      } while ((2787577 == yingR.length) && (3 > (yingR.length + 2) || 1 > (2 + yingR.length)));
      if (3 > (yingR.length << (Math.min(3, nativeexD.length))) || 4 > (yingR.length << (Math.min(Math.abs(3), 4)))) {
         nativeexD = [2 ^ yingR.length];
      }
      for (let b = 0; b < 1; b++) {
          let tooltipsW = String.fromCharCode(102,117,108,108,121,95,101,95,57,57,0);
          let ballB = true;
         modelsn += `${3 >> (Math.min(5, yingR.length))}`;
         tooltipsW += `${((ballB ? 2 : 2) - 2)}`;
         ballB = tooltipsW.length == 79;
      }
       let streamingC = String.fromCharCode(117,95,54,48,95,119,97,118,101,0);
         streamingC = `${(String.fromCharCode(67,0) == streamingC ? yingR.length : streamingC.length)}`;
      fcdaebecbcbafcdfceaaeccfeacdbC -= listM.length;

  

   if (1.10 <= (runtimeA * 1.64)) {
      unfillV = "1";
   }
  const isHome = arrayValue?.position == 1 ? true : false;
      chinasame3 /= Math.max(1, listM.length);

  const systemExcluded = [11, 12, 19, 26, 27];
   if (listM.length < fcdaebecbcbafcdfceaaeccfeacdbC) {
      listM += `${3 - fcdaebecbcbafcdfceaaeccfeacdbC}`;
   }

  const panaltyBattleType = [29, 30];
      robotoC += `${parseInt(`${runtimeA}`)}`;

  

   while ((unfillV.length % 3) == 4 || (eactm + parseFloat(`${unfillV.length}`)) == 5.91) {
      eactm /= Math.max((parseFloat(`${String.fromCharCode(49,0) == robotoC ? fcdaebecbcbafcdfceaaeccfeacdbC : robotoC.length}`)), 5);
      break;
   }
  return (
    <View style={styles.mainContainer}>
      {!isHome && (
        <View
          style={{
            flex: 1,
          }}></View>
      )}
      {!isHome &&
        (systemExcluded.includes(dataType) ? (
          <View style={styles.orangeNumberHT}>
            <Text style={styles.HTtimeTitle}>{matchStorylineStatus}</Text>
          </View>
        ) : (
          <View style={styles.orangeNumber}>
            {panaltyBattleType.includes(dataType) ? (
              <Image
                source={PenaltyMatchIcon}
                resizeMode={'stretch'}
                style={styles.timelineIcon}></Image>
            ) : (
              <Text style={styles.timeTitle}>{dataTime}</Text>
            )}
          </View>
        ))}

      {systemExcluded.includes(dataType) ? (
        <View style={styles.mainTextContainContainer}></View>
      ) : (
        <View style={styles.mainTextContainContainer}>
          {!isHome && <View style={styles.leftArrow}></View>}

          <View style={styles.containContainer}>
            <StorylineTap isHome={isHome} listData={arrayValue}></StorylineTap>
          </View>
          {isHome && <View style={styles.rightArrow}></View>}
        </View>
      )}

      {isHome && (
        <View style={styles.orangeNumber}>
          {panaltyBattleType.includes(dataType) ? (
            <Image
              source={PenaltyMatchIcon}
              resizeMode={'stretch'}
              style={styles.timelineIcon}></Image>
          ) : (
            <Text style={styles.timeTitle}>{dataTime}</Text>
          )}
        </View>
      )}
      {isHome && <View style={{flex: 1}}></View>}
    </View>
  );
   while (robotoC.length == 5) {
      chinasame3 /= Math.max(4, robotoC.length);
      break;
   }

};

export default StorylineTabListItem;
