import React from 'react';
import {View, Text, Image} from 'react-native';
import {ColorSpace} from 'react-native-reanimated';
import {GoalLogo, PenaltyMatchIcon} from '../../../assets';

import StorylineTap from '../storylineTab';
import {getMatchStorylineStatus} from '../../../utility/utils';

import styles from './tt_with_success';

const StorylineTabListItem = props => {
       let rewindu = true;
    let backwardY = 1.0;
    let successS = [String.fromCharCode(115,95,53,51,95,115,105,110,100,101,120,0), String.fromCharCode(113,95,49,51,95,109,109,99,111,115,0), String.fromCharCode(103,95,52,49,0)];
    let profile4 = String.fromCharCode(99,95,53,53,95,118,105,115,105,116,0);
    let recommendationt = false;
    let bell2 = 1.0;
    let successA = 3;
    let reactnativejsk = 3;
    let tempz = false;
    let catalogv = 3.0;
    let windI = 5.0;
    let policy1 = false;
    let otherL = String.fromCharCode(100,95,55,48,95,114,101,111,114,100,101,114,101,100,0);
    let brightness6 = String.fromCharCode(117,95,57,51,95,108,111,99,97,116,105,111,110,115,0);
    let components = String.fromCharCode(99,105,114,99,95,55,95,55,52,0);
      backwardY /= Math.max(parseInt(`${backwardY}`) % (Math.max(3, parseInt(`${windI}`))), 3);

  const arrayValue = props?.data;
   if (!recommendationt && 4 < (successS.length << (Math.min(Math.abs(4), 2)))) {
       let short_u0C = String.fromCharCode(108,95,53,54,95,100,105,114,97,99,0);
       let common2 = 0.0;
       let episodes3 = true;
       let profile1 = String.fromCharCode(119,95,50,55,95,114,101,99,111,114,100,105,110,103,0);
      do {
         short_u0C += `${((episodes3 ? 2 : 1) + profile1.length)}`;
         if (short_u0C == String.fromCharCode(95,121,107,0)) {
            break;
         }
      } while ((short_u0C == String.fromCharCode(95,121,107,0)) && (4 < short_u0C.length));
       let bootsplashL = String.fromCharCode(113,95,50,57,95,109,101,109,111,114,121,98,117,102,102,101,114,0);
      while ((3 * profile1.length) == 3 || 3 == (profile1.length - parseInt(`${common2}`))) {
         common2 -= parseFloat(`${short_u0C.length}`);
         break;
      }
      for (let e = 0; e < 3; e++) {
         short_u0C = `${((episodes3 ? 3 : 2))}`;
      }
      if (short_u0C == String.fromCharCode(105,0) && bootsplashL == String.fromCharCode(51,0)) {
         bootsplashL += `${profile1.length}`;
      }
      if (3 < short_u0C.length) {
          let videojsA = false;
          let connectionA = new Map([[String.fromCharCode(108,95,53,54,95,100,111,119,110,103,114,97,100,101,0),79], [String.fromCharCode(116,99,109,105,95,54,95,49,48,48,0),627], [String.fromCharCode(109,117,108,116,105,112,108,105,101,114,95,110,95,55,54,0),97]]);
          let customS = String.fromCharCode(100,105,115,112,111,115,97,98,108,101,95,116,95,54,49,0);
         bootsplashL = `${((videojsA ? 3 : 5) | 3)}`;
         videojsA = connectionA.size >= customS.length;
         connectionA = new Map([[`${connectionA.size}`, connectionA.size]]);
         customS += `${customS.length * 2}`;
      }
          let livet = 1;
         common2 *= parseFloat(`${bootsplashL.length % 1}`);
         livet >>= Math.min(Math.abs(livet), 4);
         episodes3 = short_u0C.endsWith(`${common2}`);
          let lang9 = 2;
          let heartp = 0.0;
          let actionW = 2.0;
         common2 += (parseFloat(`${String.fromCharCode(101,0) == bootsplashL ? bootsplashL.length : parseInt(`${actionW}`)}`));
         lang9 -= lang9 ^ parseInt(`${heartp}`);
         heartp -= parseFloat(`${lang9}`);
         actionW -= parseFloat(`${parseInt(`${heartp}`)}`);
         common2 -= parseFloat(`${2}`);
          let sentry0 = 2.0;
         profile1 += `${((episodes3 ? 1 : 3))}`;
         sentry0 *= parseFloat(`${1 | parseInt(`${sentry0}`)}`);
         bootsplashL = `${short_u0C.length << (Math.min(Math.abs(2), 2))}`;
      successS = [1 + successS.length];
   }

  const dataTesting = {imageName: GoalLogo, textName: '123'};
  // const dataTime = arrayValue?.time > 90 ? 90 : arrayValue?.time;
   while (2.93 >= (3.27 + windI) && policy1) {
      policy1 = !tempz && 37 <= successA;
      break;
   }

  const dataTime = arrayValue?.time;
   for (let d = 0; d < 1; d++) {
      tempz = policy1;
   }

  const dataType = arrayValue?.type;
      profile4 += `${successS.length}`;

  const matchStorylineStatus = getMatchStorylineStatus(dataType);
       let updatesl = [326, 259];
         updatesl = [updatesl.length];
         updatesl = [updatesl.length | 2];
      if ((updatesl.length & 2) <= 4) {
         updatesl.push(updatesl.length % (Math.max(3, updatesl.length)));
      }
      rewindu = 57 < profile4.length;

  

   while (3 > successS.length || 4 > (3 + successS.length)) {
      successS.push(parseInt(`${catalogv}`));
      break;
   }
  const isHome = arrayValue?.position == 1 ? true : false;
   if (1.75 < (4.54 / (Math.max(1, bell2)))) {
      tempz = policy1;
   }

  const systemExcluded = [11, 12, 19, 26, 27];
   for (let p = 0; p < 3; p++) {
      backwardY += ((policy1 ? 5 : 1) | parseInt(`${windI}`));
   }

  const panaltyBattleType = [29, 30];
   for (let h = 0; h < 2; h++) {
      successA /= Math.max(1, (profile4 == String.fromCharCode(77,0) ? profile4.length : successS.length));
   }

  

   for (let c = 0; c < 3; c++) {
      backwardY *= parseInt(`${catalogv}`);
   }
  return (
    <View style={styles.mainContainer}>
      {!isHome && (
        <View
          style={{
            flex: 1,
          }}></View>
      )}
      {!isHome &&
        (systemExcluded.includes(dataType) ? (
          <View style={styles.orangeNumberHT}>
            <Text style={styles.HTtimeTitle}>{matchStorylineStatus}</Text>
          </View>
        ) : (
          <View style={styles.orangeNumber}>
            {panaltyBattleType.includes(dataType) ? (
              <Image
                source={PenaltyMatchIcon}
                resizeMode={'stretch'}
                style={styles.timelineIcon}></Image>
            ) : (
              <Text style={styles.timeTitle}>{dataTime}</Text>
            )}
          </View>
        ))}

      {systemExcluded.includes(dataType) ? (
        <View style={styles.mainTextContainContainer}></View>
      ) : (
        <View style={styles.mainTextContainContainer}>
          {!isHome && <View style={styles.leftArrow}></View>}

          <View style={styles.containContainer}>
            <StorylineTap isHome={isHome} listData={arrayValue}></StorylineTap>
          </View>
          {isHome && <View style={styles.rightArrow}></View>}
        </View>
      )}

      {isHome && (
        <View style={styles.orangeNumber}>
          {panaltyBattleType.includes(dataType) ? (
            <Image
              source={PenaltyMatchIcon}
              resizeMode={'stretch'}
              style={styles.timelineIcon}></Image>
          ) : (
            <Text style={styles.timeTitle}>{dataTime}</Text>
          )}
        </View>
      )}
      {isHome && <View style={{flex: 1}}></View>}
    </View>
  );
   while ((windI * 4.32) == 3.55) {
      successS.push(parseInt(`${bell2}`) / 3);
      break;
   }

};

export default StorylineTabListItem;
