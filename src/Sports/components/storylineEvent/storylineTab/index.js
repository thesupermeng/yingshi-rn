import React from 'react';
import {View, Text, Image} from 'react-native';
import {
  YellowCard,
  RedCard,
  YellowToRedCard,
  GreenArrowUp,
  RedDownArrow,
  RedGoal,
  AssistGoal,
  ShootNoGoal,
  ShootYesGoal,
  PenaltyShoot,
  PenaltyShootNoGoal,
  YellowRedCard,
  VideoVAR,
  CornerShoot,
  Goal,
} from '../../../../../static/sports';

import vars from '../../../utility/tt_mbjscommon_source';

import styles from './tt_with_success';

const StorylineTap = props => {
       let constantsx = String.fromCharCode(107,95,51,95,102,114,111,122,101,110,0);
    let whiteV = false;
    let completeI = String.fromCharCode(102,95,49,55,95,114,111,103,114,101,115,115,0);
    let toponq = false;
    let halfZ = [746, 432];
    let megaphone4 = 1.0;
    let o_positionq = String.fromCharCode(99,111,110,102,105,103,117,114,101,100,95,116,95,55,56,0);
    let modalH = 4.0;
    let leftO = [737, 998];
    let action1 = [956, 310];
      completeI = `${parseInt(`${modalH}`) % (Math.max(3, parseInt(`${megaphone4}`)))}`;

  const listData = props?.listData;
      megaphone4 /= Math.max(2, parseFloat(`${leftO.length >> (Math.min(Math.abs(3), 4))}`));

  const textValue = listData.player_name;
   for (let s = 0; s < 3; s++) {
      halfZ.push(halfZ.length << (Math.min(Math.abs(3), 5)));
   }

  const imageValue = listData.imageName;
   for (let r = 0; r < 1; r++) {
      leftO.push((completeI == String.fromCharCode(97,0) ? completeI.length : parseInt(`${megaphone4}`)));
   }

  const isHome = props?.isHome;
   do {
       let uploadK = [928, 35];
       let const_mxR = String.fromCharCode(103,97,116,104,101,114,95,56,95,53,49,0);
      while ((uploadK.length - 1) <= 2 || 1 <= (uploadK.length - const_mxR.length)) {
         const_mxR += `${uploadK.length}`;
         break;
      }
          let countryc = String.fromCharCode(114,95,49,51,95,98,101,116,119,101,101,110,0);
         const_mxR = "3";
         countryc = "3";
      for (let d = 0; d < 3; d++) {
          let backY = 2.0;
          let productQ = String.fromCharCode(114,95,51,51,95,105,110,116,114,105,110,115,105,99,0);
          let frame_yC = String.fromCharCode(111,95,50,52,95,114,101,117,115,101,0);
         const_mxR = `${(const_mxR == String.fromCharCode(74,0) ? const_mxR.length : parseInt(`${backY}`))}`;
         backY *= parseFloat(`${frame_yC.length}`);
         productQ = `${frame_yC.length % (Math.max(8, productQ.length))}`;
      }
         uploadK = [(String.fromCharCode(89,0) == const_mxR ? uploadK.length : const_mxR.length)];
          let const_xvC = 0;
         const_mxR += `${const_mxR.length * uploadK.length}`;
         const_xvC &= 3 | const_xvC;
      do {
          let large3 = String.fromCharCode(114,115,116,110,95,114,95,48,0);
         uploadK.push(large3.length);
         if (1223984 == uploadK.length) {
            break;
         }
      } while ((1223984 == uploadK.length) && ((uploadK.length & 3) <= 2));
      constantsx += `${(const_mxR.length - (whiteV ? 2 : 4))}`;
      if (2852446 == constantsx.length) {
         break;
      }
   } while ((!whiteV || constantsx.length < 2) && (2852446 == constantsx.length));

  const textReason = '';
   for (let q = 0; q < 3; q++) {
      action1.push(halfZ.length | 1);
   }

  const assistPlayer = listData?.assist1_name;
       let sportsY = 4;
         sportsY &= sportsY - 3;
         sportsY ^= sportsY;
      if (4 > (5 & sportsY)) {
          let downloadj = false;
          let inactivev = String.fromCharCode(115,116,97,116,95,107,95,52,57,0);
          let umengd = 4;
          let sound6 = 4.0;
          let mapping1 = 2.0;
         sportsY += parseInt(`${mapping1}`) << (Math.min(1, Math.abs(2)));
         downloadj = umengd > 20 || String.fromCharCode(77,0) == inactivev;
         inactivev += `${(inactivev.length >> (Math.min(1, Math.abs((downloadj ? 5 : 3)))))}`;
         umengd <<= Math.min(Math.abs(3 % (Math.max(10, umengd))), 4);
         sound6 /= Math.max(3, (umengd - (downloadj ? 2 : 3)));
         mapping1 += 3 % (Math.max(parseInt(`${sound6}`), 5));
      }
      action1 = [(leftO.length & (toponq ? 1 : 3))];

  const inPlayerName = listData?.in_player_name;
      whiteV = leftO.length < 89 && 89 < halfZ.length;

  const outPlayerName = listData?.out_player_name;
   if (3 > (halfZ.length * 5) || 5 > halfZ.length) {
      halfZ.push(parseInt(`${megaphone4}`) + 3);
   }

  const typeNumber = listData?.type;
   if (toponq || 1 <= completeI.length) {
       let sentryE = 5;
       let dplusS = 5.0;
      if (dplusS < sentryE) {
         sentryE &= sentryE;
      }
      for (let d = 0; d < 1; d++) {
          let invitei = 0.0;
          let logoB = new Map([[String.fromCharCode(97,95,54,49,95,98,114,101,97,107,0),221], [String.fromCharCode(121,95,54,54,95,97,118,99,111,100,101,99,114,101,115,0),526]]);
         dplusS -= 3;
         invitei += parseInt(`${invitei}`) ^ logoB.size;
         logoB = new Map([[`${logoB.size}`, 3]]);
      }
       let dplusu = [867, 653, 189];
       let huaweiL = [687, 129, 636];
         dplusu = [2];
      while (sentryE > huaweiL.length) {
          let recommendation7 = String.fromCharCode(112,95,50,57,95,112,97,103,101,111,117,116,0);
          let clubF = 0.0;
          let user1 = 0;
         huaweiL.push(1 + user1);
         recommendation7 += `${recommendation7.length}`;
         clubF *= 3 ^ recommendation7.length;
         user1 <<= Math.min(1, Math.abs(recommendation7.length ^ 3));
         break;
      }
       let tailU = 1.0;
       let savee = 4.0;
      toponq = halfZ.length <= sentryE;
   }

  const homeScore = listData?.home_score;
       let action8 = new Map([[String.fromCharCode(99,97,110,116,95,100,95,49,55,0),445], [String.fromCharCode(116,111,103,103,108,101,95,48,95,52,54,0),824]]);
       let fastF = 5;
         fastF &= fastF;
      if (5 >= (fastF * 3) && (action8.size * 3) >= 3) {
          let connectionS = new Map([[String.fromCharCode(116,95,52,48,95,109,112,101,103,97,117,100,105,111,116,97,98,0),639], [String.fromCharCode(101,101,112,95,107,95,57,52,0),497]]);
          let clubA = 5.0;
          let ewarded2 = 5.0;
          let utilsp = 0.0;
         action8[`${ewarded2}`] = 1;
         connectionS = new Map([[`${utilsp}`, parseInt(`${clubA}`) ^ 3]]);
         clubA -= parseFloat(`${1 * connectionS.size}`);
         ewarded2 -= parseInt(`${clubA}`) % (Math.max(9, connectionS.size));
         utilsp -= parseFloat(`${connectionS.size + 3}`);
      }
      if (!Array.from(action8.values()).includes(fastF)) {
          let anytimeU = String.fromCharCode(115,99,101,110,101,95,121,95,57,55,0);
          let philippinesT = new Map([[String.fromCharCode(109,95,50,50,95,102,105,110,110,101,121,0),599], [String.fromCharCode(116,95,56,50,95,107,101,121,115,112,101,99,0),237]]);
          let bottomG = 4.0;
         fastF -= philippinesT.size;
         anytimeU += `${parseInt(`${bottomG}`)}`;
         philippinesT = new Map([[anytimeU, parseInt(`${bottomG}`) * 3]]);
      }
         action8 = new Map([[`${action8.size}`, 3 & action8.size]]);
      if ((action8.size * 3) == 4 && (3 * action8.size) == 4) {
         action8 = new Map([[`${action8.size}`, action8.size]]);
      }
      for (let h = 0; h < 3; h++) {
         action8[`${fastF}`] = action8.size;
      }
      whiteV = o_positionq.length <= 16;

  const awayScore = listData?.away_score;
      o_positionq += `${leftO.length ^ halfZ.length}`;

  const varResult = listData?.var_result;
   for (let c = 0; c < 3; c++) {
      megaphone4 -= parseFloat(`${2 << (Math.min(Math.abs(parseInt(`${modalH}`)), 4))}`);
   }

  const reasonType = listData?.reason_type;
   while (!o_positionq.endsWith(`${whiteV}`)) {
       let fieldP = String.fromCharCode(112,114,101,115,99,97,108,101,95,109,95,56,54,0);
         fieldP = `${fieldP.length & 1}`;
      do {
         fieldP = `${(String.fromCharCode(107,0) == fieldP ? fieldP.length : fieldP.length)}`;
         if (fieldP.length == 437754) {
            break;
         }
      } while ((fieldP.length == 437754) && (fieldP != String.fromCharCode(113,0)));
      for (let j = 0; j < 2; j++) {
          let alertV = 5.0;
          let anytimeC = false;
          let downloady = [297, 553, 754];
          let mbsplashE = 3;
          let refreshp = 3.0;
         fieldP = `${parseInt(`${refreshp}`) << (Math.min(downloady.length, 4))}`;
         alertV *= (parseFloat(`${(anytimeC ? 5 : 4) / (Math.max(8, parseInt(`${alertV}`)))}`));
         anytimeC = alertV <= 74.19;
         downloady = [3 >> (Math.min(Math.abs(parseInt(`${alertV}`)), 1))];
         mbsplashE <<= Math.min(5, Math.abs(parseInt(`${alertV}`) % 1));
         refreshp *= mbsplashE;
      }
      whiteV = !constantsx.includes(`${megaphone4}`);
      break;
   }

  var resultText;
      o_positionq = `${2 + parseInt(`${megaphone4}`)}`;

  var reasonText;
   do {
       let tumbnail9 = String.fromCharCode(111,95,55,57,95,119,105,116,104,105,110,0);
       let spinnerl = String.fromCharCode(110,101,111,110,116,101,115,116,95,117,95,55,50,0);
       let acceptedw = 0;
      for (let m = 0; m < 3; m++) {
          let buffer9 = String.fromCharCode(100,105,115,99,97,114,100,101,100,95,54,95,49,54,0);
         spinnerl += `${buffer9.length}`;
      }
      while ((acceptedw % (Math.max(spinnerl.length, 1))) >= 3 && (acceptedw % (Math.max(3, 7))) >= 3) {
         acceptedw >>= Math.min(Math.abs(1 | tumbnail9.length), 5);
         break;
      }
      if (!spinnerl.endsWith(tumbnail9)) {
         tumbnail9 += `${1 / (Math.max(10, spinnerl.length))}`;
      }
         spinnerl += `${1 >> (Math.min(5, spinnerl.length))}`;
       let modityS = 2.0;
       let downloadinga = 4.0;
          let sourceN = String.fromCharCode(98,95,51,56,95,99,111,109,109,117,110,105,99,97,116,105,111,110,0);
         modityS -= acceptedw | parseInt(`${downloadinga}`);
         sourceN = `${sourceN.length}`;
      while ((tumbnail9.length ^ 4) <= 3 || 4 <= (parseInt(`${downloadinga}`) + tumbnail9.length)) {
         downloadinga *= acceptedw - 1;
         break;
      }
         modityS += 3;
       let penaltyI = 4;
       let reward6 = 1;
      o_positionq += `${(String.fromCharCode(103,0) == o_positionq ? spinnerl.length : o_positionq.length)}`;
      if (o_positionq == String.fromCharCode(114,110,107,120,104,108,102,104,105,104,0)) {
         break;
      }
   } while ((o_positionq == String.fromCharCode(114,110,107,120,104,108,102,104,105,104,0)) && (o_positionq.length == 2));

  if (varResult) {
    resultText = vars?.eventResultType[varResult];
   if (2 < (action1.length * parseInt(`${megaphone4}`)) && 5.25 < (megaphone4 * parseFloat(`${action1.length}`))) {
      megaphone4 += (parseFloat(`${parseInt(`${megaphone4}`) & (toponq ? 2 : 3)}`));
   }

    

      halfZ = [leftO.length];
  }
  if (reasonType) {
    reasonText = vars?.eventReasonType[reasonType];
      completeI = `${(String.fromCharCode(101,0) == completeI ? (toponq ? 5 : 4) : completeI.length)}`;

    

      leftO = [constantsx.length % (Math.max(2, 6))];
  }

  const Type = () => {

   for (let c = 0; c < 1; c++) {
       let middlej = new Map([[String.fromCharCode(115,95,54,49,95,101,114,114,115,116,114,0),712], [String.fromCharCode(117,110,113,117,97,110,116,95,118,95,54,52,0),518], [String.fromCharCode(115,116,114,99,97,116,95,97,95,57,48,0),382]]);
       let filed3 = String.fromCharCode(112,108,97,110,97,114,116,111,117,121,118,121,95,53,95,57,51,0);
       let assistK = true;
       let china7 = 1;
       let clearx = new Map([[String.fromCharCode(116,104,111,117,115,97,110,100,115,95,108,95,56,52,0),String.fromCharCode(114,110,103,115,95,104,95,48,0)], [String.fromCharCode(99,121,117,118,95,55,95,50,54,0),String.fromCharCode(119,95,53,54,95,115,105,122,101,100,0)], [String.fromCharCode(106,111,105,110,101,114,95,115,95,55,51,0),String.fromCharCode(114,103,116,99,95,108,95,56,56,0)]]);
       let indexz = 0;
       let plashU = 4;
       let bodanz = false;
      while ((filed3.length & clearx.size) == 2) {
         filed3 = `${china7 ^ 2}`;
         break;
      }
      for (let r = 0; r < 3; r++) {
          let crownK = 2.0;
         clearx[`${plashU}`] = (plashU << (Math.min(4, Math.abs((assistK ? 5 : 3)))));
         crownK /= Math.max(5, parseFloat(`${3 % (Math.max(parseInt(`${crownK}`), 2))}`));
      }
       let typesR = false;
       let switch_1z = false;
         switch_1z = china7 < 35;
         switch_1z = 77 <= plashU;
         assistK = clearx.size <= 2 || 2 <= indexz;
         plashU /= Math.max((3 >> (Math.min(4, Math.abs((switch_1z ? 5 : 5))))), 3);
      do {
          let stationsG = [373, 937];
          let package_aqi = String.fromCharCode(117,95,54,95,115,116,114,105,107,101,0);
          let leftV = new Map([[String.fromCharCode(103,95,55,56,95,118,114,101,99,116,0),309], [String.fromCharCode(101,95,55,51,95,109,97,102,113,0),208], [String.fromCharCode(109,97,99,104,95,51,95,53,57,0),777]]);
         china7 ^= (String.fromCharCode(98,0) == package_aqi ? package_aqi.length : indexz);
         stationsG.push(leftV.size);
         leftV[`${stationsG.length}`] = stationsG.length ^ leftV.size;
         if (china7 == 2893390) {
            break;
         }
      } while (((3 - china7) < 1) && (china7 == 2893390));
         middlej = new Map([[`${switch_1z}`, (3 / (Math.max(2, (bodanz ? 4 : 3))))]]);
      while (4 == (5 << (Math.min(4, Math.abs(middlej.size))))) {
         bodanz = (!typesR ? assistK : !typesR);
         break;
      }
         typesR = (!switch_1z ? typesR : !switch_1z);
         typesR = indexz <= 56;
         china7 ^= indexz;
      constantsx = `${3 << (Math.min(4, completeI.length))}`;
   }
    const scoreMark = ` (${homeScore}-${awayScore})`;
   for (let x = 0; x < 1; x++) {
      constantsx = `${parseInt(`${megaphone4}`)}`;
   }

    switch (typeNumber) {
      case 1:
        return (
          <View style={styles.verticalContainer}>
            <View
              style={[
                styles.mainContainer,
                isHome
                  ? {flexDirection: 'row-reverse', textAlign: 'right'}
                  : {flexDirection: 'row'},
              ]}>
              <Image
                source={Goal}
                resizeMode={'contain'}
                style={styles.imageStyle}></Image>
              <Text
                style={[
                  styles.titleStyle,
                  isHome
                    ? {flexDirection: 'row-reverse', textAlign: 'right'}
                    : {flexDirection: 'row'},
                ]}>
                {textValue}
                {scoreMark}
              </Text>
            </View>
            {assistPlayer && (
              <View
                style={[
                  styles.mainContainer,
                  isHome
                    ? {flexDirection: 'row-reverse', textAlign: 'right'}
                    : {flexDirection: 'row'},
                ]}>
                <Image
                  source={AssistGoal}
                  resizeMode={'contain'}
                  style={styles.imageStyle}></Image>
                <Text
                  style={[
                    styles.titleStyle,
                    isHome
                      ? {flexDirection: 'row-reverse', textAlign: 'right'}
                      : {flexDirection: 'row'},
                  ]}>
                  {assistPlayer}
                </Text>
              </View>
            )}
          </View>
        );
      completeI = `${parseInt(`${megaphone4}`)}`;

      case 2:
        return (
          <View
            style={[
              styles.mainContainer,
              isHome
                ? {flexDirection: 'row-reverse', textAlign: 'right'}
                : {flexDirection: 'row'},
            ]}>
            <Image
              source={CornerShoot}
              resizeMode={'contain'}
              style={styles.imageStyle}></Image>
            <Text
              style={[
                styles.titleStyle,
                isHome
                  ? {flexDirection: 'row-reverse', textAlign: 'right'}
                  : {flexDirection: 'row'},
              ]}>
              {textValue}
            </Text>
          </View>
        );
       let u_positionm = String.fromCharCode(101,97,114,108,105,101,114,95,112,95,51,52,0);
       let navigationM = 1.0;
       let viewsq = String.fromCharCode(113,95,55,95,99,111,109,109,105,115,115,105,111,110,0);
         u_positionm += `${(String.fromCharCode(120,0) == u_positionm ? parseInt(`${navigationM}`) : u_positionm.length)}`;
      for (let t = 0; t < 2; t++) {
         viewsq += `${parseInt(`${navigationM}`)}`;
      }
      if (5.75 > (2.87 - navigationM)) {
         navigationM /= Math.max(parseInt(`${navigationM}`) << (Math.min(u_positionm.length, 2)), 2);
      }
      for (let w = 0; w < 1; w++) {
         navigationM -= parseInt(`${navigationM}`);
      }
         navigationM /= Math.max(parseInt(`${navigationM}`), 4);
      if (2 == viewsq.length || u_positionm.length == 2) {
         viewsq += "2";
      }
      do {
         u_positionm += `${viewsq.length % (Math.max(3, 8))}`;
         if (u_positionm.length == 2980561) {
            break;
         }
      } while ((3 == u_positionm.length) && (u_positionm.length == 2980561));
      do {
          let subsk = String.fromCharCode(121,95,56,56,95,100,101,99,98,110,0);
          let successn = 5;
          let bridgeM = 4.0;
          let clockQ = String.fromCharCode(104,95,57,48,95,115,116,100,97,116,111,109,105,99,0);
          let floating4 = String.fromCharCode(115,116,114,97,116,101,103,121,95,115,95,54,53,0);
         navigationM -= 3 << (Math.min(4, floating4.length));
         subsk += `${2 ^ subsk.length}`;
         successn &= clockQ.length << (Math.min(Math.abs(2), 2));
         bridgeM -= parseFloat(`${2 | successn}`);
         clockQ = `${2 - clockQ.length}`;
         floating4 = "2";
         if (4450223.0 == navigationM) {
            break;
         }
      } while ((4450223.0 == navigationM) && ((1 >> (Math.min(1, viewsq.length))) <= 4));
      while (u_positionm == String.fromCharCode(72,0) || viewsq == String.fromCharCode(103,0)) {
         viewsq = `${u_positionm.length / 3}`;
         break;
      }
      halfZ.push(1);

      case 3:
        return (
          <View style={styles.verticalContainer}>
            <View
              style={[
                styles.mainContainer,
                isHome
                  ? {flexDirection: 'row-reverse', textAlign: 'right'}
                  : {flexDirection: 'row'},
              ]}>
              <Image
                source={YellowCard}
                resizeMode={'contain'}
                style={styles.imageStyle}></Image>
              <Text
                style={[
                  styles.titleStyle,
                  isHome
                    ? {flexDirection: 'row-reverse', textAlign: 'right'}
                    : {flexDirection: 'row'},
                ]}>
                {textValue}
              </Text>
            </View>
            <Text
              style={[
                styles.titleReasonStyle,
                isHome
                  ? {flexDirection: 'row-reverse', textAlign: 'right'}
                  : {flexDirection: 'row'},
              ]}>
              {reasonText}
            </Text>
          </View>
        );
   while ((action1.length << (Math.min(leftO.length, 3))) < 5 && 4 < (5 << (Math.min(4, leftO.length)))) {
      action1 = [leftO.length];
      break;
   }

      case 4:
        return (
          <View style={styles.verticalContainer}>
            <View
              style={[
                styles.mainContainer,
                isHome
                  ? {flexDirection: 'row-reverse', textAlign: 'right'}
                  : {flexDirection: 'row'},
              ]}>
              <Image
                source={RedCard}
                resizeMode={'contain'}
                style={styles.imageStyle}></Image>
              <Text
                style={[
                  styles.titleStyle,
                  isHome
                    ? {flexDirection: 'row-reverse', textAlign: 'right'}
                    : {flexDirection: 'row'},
                ]}>
                {textValue}
              </Text>
            </View>
            <Text
              style={[
                styles.titleReasonStyle,
                isHome
                  ? {flexDirection: 'row-reverse', textAlign: 'right'}
                  : {flexDirection: 'row'},
              ]}>
              {reasonText}
            </Text>
          </View>
        );
      megaphone4 -= (parseFloat(`${String.fromCharCode(87,0) == o_positionq ? o_positionq.length : halfZ.length}`));

      case 5:
        break;
       let huawei5 = String.fromCharCode(115,121,109,98,111,108,105,99,97,116,97,98,108,101,95,120,95,51,0);
      do {
         huawei5 += `${huawei5.length / (Math.max(9, huawei5.length))}`;
         if (huawei5.length == 3117982) {
            break;
         }
      } while ((huawei5.length == 3117982) && (huawei5.length <= 5));
         huawei5 = `${huawei5.length << (Math.min(Math.abs(2), 1))}`;
          let backB = [162, 150, 592];
          let football3 = [918, 470];
          let over7 = String.fromCharCode(111,110,110,101,99,116,105,111,110,95,105,95,51,51,0);
         huawei5 += `${over7.length * huawei5.length}`;
         backB.push(football3.length);
         football3 = [2];
         over7 += `${1 >> (Math.min(2, backB.length))}`;
      whiteV = (huawei5.length - completeI.length) <= 74;

      case 6:
        break;
   do {
      halfZ.push(((toponq ? 4 : 4)));
      if (494350 == halfZ.length) {
         break;
      }
   } while ((494350 == halfZ.length) && ((o_positionq.length | halfZ.length) < 3));

      case 7:
        break;
      modalH /= Math.max(2, (2 - (toponq ? 1 : 3)));

      case 8:
        return (
          <View
            style={[
              styles.mainContainer,
              isHome
                ? {flexDirection: 'row-reverse', textAlign: 'right'}
                : {flexDirection: 'row'},
            ]}>
            <Image
              source={PenaltyShoot}
              resizeMode={'contain'}
              style={styles.imageStyle}></Image>
            <Text
              style={[
                styles.titleStyle,
                isHome
                  ? {flexDirection: 'row-reverse', textAlign: 'right'}
                  : {flexDirection: 'row'},
              ]}>
              {textValue}
              {scoreMark}
            </Text>
          </View>
        );
      leftO.push(completeI.length * o_positionq.length);

      case 9:
        return (
          <View style={styles.verticalContainer}>
            <View
              style={[
                styles.mainContainer,
                isHome
                  ? {flexDirection: 'row-reverse', textAlign: 'right'}
                  : {flexDirection: 'row'},
              ]}>
              <Image
                source={GreenArrowUp}
                resizeMode={'contain'}
                style={styles.imageStyle}></Image>
              <Text
                style={[
                  styles.titleStyle,
                  isHome
                    ? {flexDirection: 'row-reverse', textAlign: 'right'}
                    : {flexDirection: 'row'},
                ]}>
                {inPlayerName}
              </Text>
            </View>
            {outPlayerName && (
              <View
                style={[
                  styles.mainContainer,
                  isHome
                    ? {flexDirection: 'row-reverse', textAlign: 'right'}
                    : {flexDirection: 'row'},
                ]}>
                <Image
                  source={RedDownArrow}
                  resizeMode={'contain'}
                  style={styles.imageStyle}></Image>
                <Text
                  style={[
                    styles.titleStyle,
                    isHome
                      ? {flexDirection: 'row-reverse', textAlign: 'right'}
                      : {flexDirection: 'row'},
                  ]}>
                  {outPlayerName}
                </Text>
              </View>
            )}
          </View>
        );
      constantsx += `${(parseInt(`${modalH}`) << (Math.min(1, Math.abs((toponq ? 2 : 5)))))}`;

      case 10:
        break;
       let gestureb = 3.0;
      for (let z = 0; z < 1; z++) {
          let bootsplashL = String.fromCharCode(107,95,52,49,95,116,105,102,102,0);
          let viewerf = new Map([[String.fromCharCode(111,95,49,54,95,100,101,115,116,114,117,99,116,111,114,0),945], [String.fromCharCode(97,101,118,97,108,95,101,95,52,48,0),32], [String.fromCharCode(115,116,114,110,108,101,110,95,57,95,52,50,0),745]]);
          let downloadedB = 3.0;
         gestureb /= Math.max(2 | parseInt(`${gestureb}`), 1);
         bootsplashL = `${viewerf.size / 3}`;
         viewerf = new Map([[`${viewerf.size}`, parseInt(`${downloadedB}`) << (Math.min(Math.abs(1), 1))]]);
         downloadedB -= parseFloat(`${parseInt(`${downloadedB}`) / (Math.max(9, viewerf.size))}`);
      }
         gestureb -= parseInt(`${gestureb}`);
          let singleI = String.fromCharCode(100,112,110,97,109,101,95,98,95,57,52,0);
         gestureb += 2 << (Math.min(3, singleI.length));
      whiteV = 58 < leftO.length;

      case 11:
        return <Text style={styles.titleStyle}>HT</Text>;
   if ((modalH + 5.47) < 3.43 || 3.45 < (5.47 + modalH)) {
       let screene = String.fromCharCode(115,95,57,55,95,104,105,103,104,119,97,116,101,114,0);
       let mbjscommon5 = 1;
       let heartW = String.fromCharCode(105,112,99,95,109,95,50,56,0);
       let paginationM = false;
          let yellowE = [String.fromCharCode(114,110,110,111,105,115,101,95,120,95,57,48,0), String.fromCharCode(116,114,97,99,105,110,103,95,101,95,49,51,0), String.fromCharCode(115,112,97,110,115,95,110,95,51,49,0)];
         paginationM = String.fromCharCode(78,0) == heartW;
         yellowE = [yellowE.length | 3];
      do {
         screene = `${heartW.length | 3}`;
         if (screene.length == 4138140) {
            break;
         }
      } while (((mbjscommon5 << (Math.min(screene.length, 5))) > 2 || 2 > (mbjscommon5 << (Math.min(screene.length, 3)))) && (screene.length == 4138140));
      if (screene != String.fromCharCode(53,0)) {
         heartW = `${1 + heartW.length}`;
      }
         mbjscommon5 %= Math.max(1, mbjscommon5);
         mbjscommon5 >>= Math.min(Math.abs(((paginationM ? 4 : 1))), 4);
         mbjscommon5 <<= Math.min(3, screene.length);
      do {
         screene += "3";
         if (screene.length == 944362) {
            break;
         }
      } while ((heartW.includes(screene)) && (screene.length == 944362));
      if (screene.length == 5) {
         mbjscommon5 /= Math.max(3, 3);
      }
      while (mbjscommon5 < 3) {
         mbjscommon5 %= Math.max(1, 1);
         break;
      }
      for (let t = 0; t < 2; t++) {
         heartW = `${mbjscommon5}`;
      }
       let componentH = String.fromCharCode(105,122,101,114,111,95,105,95,57,50,0);
       let entryr = String.fromCharCode(114,101,112,111,114,116,101,100,95,115,95,50,55,0);
         mbjscommon5 &= componentH.length ^ screene.length;
      modalH *= (heartW == String.fromCharCode(108,0) ? o_positionq.length : heartW.length);
   }

      case 12:
        break;
   if ((modalH / (Math.max(1, 6))) > 2.78) {
      megaphone4 -= (parseFloat(`${3 - (whiteV ? 3 : 1)}`));
   }

      case 13:
        break;
   if (!whiteV) {
       let gmail7 = String.fromCharCode(108,105,98,115,119,114,101,115,97,109,112,108,101,95,119,95,50,50,0);
       let modelsp = true;
       let cornerz = 1.0;
       let privilegeX = 4.0;
          let episodesZ = String.fromCharCode(107,95,55,55,95,105,115,115,117,101,114,0);
          let component7 = 0.0;
         modelsp = component7 >= 29.77;
         episodesZ += `${episodesZ.length}`;
         component7 -= parseFloat(`${1 & episodesZ.length}`);
          let danger2 = 5.0;
         gmail7 = `${parseInt(`${cornerz}`) * 2}`;
         danger2 *= 1;
          let v_imageP = 0.0;
         cornerz += parseFloat(`${1 % (Math.max(parseInt(`${v_imageP}`), 4))}`);
      do {
         modelsp = !modelsp;
         if (modelsp ? !modelsp : modelsp) {
            break;
         }
      } while ((!modelsp) && (modelsp ? !modelsp : modelsp));
       let button_ = String.fromCharCode(113,95,57,49,95,112,105,112,101,108,105,110,105,110,103,0);
         button_ = `${parseInt(`${cornerz}`) ^ button_.length}`;
       let descF = 1.0;
      for (let s = 0; s < 1; s++) {
         modelsp = button_.length == 98 && modelsp;
      }
          let serviceo = String.fromCharCode(104,114,101,97,100,95,110,95,51,50,0);
         descF /= Math.max(4, parseFloat(`${serviceo.length}`));
          let bell4 = String.fromCharCode(97,95,53,51,95,98,97,110,110,101,114,0);
         privilegeX /= Math.max(parseFloat(`${3 << (Math.min(5, Math.abs(parseInt(`${privilegeX}`))))}`), 1);
         bell4 = `${bell4.length}`;
          let main_yW = [803, 695];
          let roundm = String.fromCharCode(114,95,57,48,95,99,119,110,100,0);
          let kickB = String.fromCharCode(107,95,52,52,95,112,114,101,102,101,116,99,104,105,110,103,0);
         button_ = `${(parseInt(`${descF}`) >> (Math.min(3, Math.abs((modelsp ? 3 : 3)))))}`;
         main_yW.push(1);
         roundm += `${main_yW.length}`;
         kickB = `${(kickB == String.fromCharCode(109,0) ? kickB.length : main_yW.length)}`;
      do {
         privilegeX /= Math.max(parseFloat(`${parseInt(`${descF}`)}`), 3);
         if (privilegeX == 4775757.0) {
            break;
         }
      } while ((3.73 <= (1.38 + cornerz) && 3.78 <= (1.38 + cornerz)) && (privilegeX == 4775757.0));
      whiteV = modelsp;
   }

      case 14:
        break;
      action1 = [1 + parseInt(`${megaphone4}`)];

      case 15:
        return (
          <View style={styles.verticalContainer}>
            <View
              style={[
                styles.mainContainer,
                isHome
                  ? {flexDirection: 'row-reverse', textAlign: 'right'}
                  : {flexDirection: 'row'},
              ]}>
              <Image
                source={YellowRedCard}
                resizeMode={'contain'}
                style={styles.imageStyle}></Image>
              <Text
                style={[
                  styles.titleStyle,
                  isHome
                    ? {flexDirection: 'row-reverse', textAlign: 'right'}
                    : {flexDirection: 'row'},
                ]}>
                {textValue}
              </Text>
            </View>
            <Text style={styles.titleStyle}>{textReason}</Text>
          </View>
        );
   if (2 < (constantsx.length % 5)) {
      modalH *= 1;
   }

      case 16:
        return (
          <View
            style={[
              styles.mainContainer,
              isHome
                ? {flexDirection: 'row-reverse', textAlign: 'right'}
                : {flexDirection: 'row'},
            ]}>
            <Image
              source={PenaltyShootNoGoal}
              resizeMode={'contain'}
              style={styles.imageStyle}></Image>
            <Text
              style={[
                styles.titleStyle,
                isHome
                  ? {flexDirection: 'row-reverse', textAlign: 'right'}
                  : {flexDirection: 'row'},
              ]}>
              {textValue}
            </Text>
          </View>
        );
       let forwardx = 3.0;
       let gradleC = 5;
          let stringsk = [String.fromCharCode(118,95,56,52,95,116,111,116,97,108,108,121,0), String.fromCharCode(112,95,49,56,95,100,108,105,100,120,0), String.fromCharCode(115,95,56,54,95,115,99,97,110,0)];
          let constantsc = String.fromCharCode(114,95,57,52,0);
         forwardx /= Math.max(1, parseFloat(`${constantsc.length}`));
         stringsk = [stringsk.length ^ stringsk.length];
         constantsc += `${stringsk.length}`;
       let button1 = 3;
      for (let l = 0; l < 2; l++) {
          let flyerA = new Map([[String.fromCharCode(99,95,52,95,102,102,97,116,0),469], [String.fromCharCode(98,95,53,56,95,112,103,110,111,0),680]]);
          let chartt = 1.0;
          let langkeyT = 5;
         forwardx -= parseFloat(`${gradleC * 2}`);
         flyerA = new Map([[`${flyerA.size}`, 1 * parseInt(`${chartt}`)]]);
         chartt *= parseFloat(`${parseInt(`${chartt}`) ^ 1}`);
         langkeyT += flyerA.size >> (Math.min(Math.abs(3), 2));
      }
         forwardx += parseFloat(`${gradleC << (Math.min(3, Math.abs(2)))}`);
      while ((forwardx + 1.39) > 3.90 || (forwardx + 1.39) > 2.14) {
         button1 *= button1;
         break;
      }
       let blacklistJ = 4;
       let type__iG = 4;
      constantsx = `${parseInt(`${forwardx}`) >> (Math.min(Math.abs(parseInt(`${modalH}`)), 3))}`;

      case 17:
        return (
          <View style={styles.verticalContainer}>
            <View
              style={[
                styles.mainContainer,
                isHome
                  ? {flexDirection: 'row-reverse', textAlign: 'right'}
                  : {flexDirection: 'row'},
              ]}>
              <Image
                source={RedGoal}
                resizeMode={'contain'}
                style={styles.imageStyle}></Image>
              <Text
                style={[
                  styles.titleStyle,
                  isHome
                    ? {flexDirection: 'row-reverse', textAlign: 'right'}
                    : {flexDirection: 'row'},
                ]}>
                {textValue}
                {scoreMark}
              </Text>
            </View>
            <Text style={styles.titleStyle}>{textReason}</Text>
          </View>
        );
      case 18:
        break;
      case 19:
        break;
      case 20:
        break;
      case 21:
        break;
      case 22:
        break;
      case 23:
        break;
      case 24:
        break;
      case 25:
        break;
      case 26:
        break;
      case 27:
        break;
      case 28:
        return (
          <View style={styles.verticalContainer}>
            <View
              style={[
                styles.mainContainer,
                isHome
                  ? {flexDirection: 'row-reverse', textAlign: 'right'}
                  : {flexDirection: 'row'},
              ]}>
              <Image
                source={VideoVAR}
                resizeMode={'contain'}
                style={styles.imageStyle}></Image>
              <Text
                style={[
                  styles.titleStyle,
                  isHome
                    ? {flexDirection: 'row-reverse', textAlign: 'right'}
                    : {flexDirection: 'row'},
                ]}>
                {textValue}
              </Text>
            </View>
            <Text
              style={[
                styles.titleReasonStyle,
                isHome
                  ? {flexDirection: 'row-reverse', textAlign: 'right'}
                  : {flexDirection: 'row'},
              ]}>
              {resultText}
            </Text>
          </View>
        );
      case 29:
        return (
          <View
            style={[
              styles.mainContainer,
              isHome
                ? {flexDirection: 'row-reverse', textAlign: 'right'}
                : {flexDirection: 'row'},
            ]}>
            <Image
              source={PenaltyShoot}
              resizeMode={'contain'}
              style={styles.imageStyle}></Image>
            <Text
              style={[
                styles.titleStyle,
                isHome
                  ? {flexDirection: 'row-reverse', textAlign: 'right'}
                  : {flexDirection: 'row'},
              ]}>
              {textValue}
              { }
            </Text>
          </View>
        );
      case 30:
        return (
          <View
            style={[
              styles.mainContainer,
              isHome
                ? {flexDirection: 'row-reverse', textAlign: 'right'}
                : {flexDirection: 'row'},
            ]}>
            <Image
              source={PenaltyShootNoGoal}
              resizeMode={'contain'}
              style={styles.imageStyle}></Image>
            <Text
              style={[
                styles.titleStyle,
                isHome
                  ? {flexDirection: 'row-reverse', textAlign: 'right'}
                  : {flexDirection: 'row'},
              ]}>
              {textValue}
              { }
            </Text>
          </View>
        );
      default:
        break;
    }
  };

  return (
    
    
    
    
    //     ]}>
    
    
    
    //       style={styles.imageStyle}></Image>
    //     <Text style={styles.titleStyle}>{textValue}</Text>
    //   </View>
    <View style={{height: '100%'}}>
      <Type />
    </View>
  );
};

export default StorylineTap;
