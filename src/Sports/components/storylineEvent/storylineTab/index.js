import React from 'react';
import {View, Text, Image} from 'react-native';
import {
  YellowCard,
  RedCard,
  YellowToRedCard,
  GreenArrowUp,
  RedDownArrow,
  RedGoal,
  AssistGoal,
  ShootNoGoal,
  ShootYesGoal,
  PenaltyShoot,
  PenaltyShootNoGoal,
  YellowRedCard,
  VideoVAR,
  CornerShoot,
  Goal,
} from '../../../assets';

import vars from '../../../utility/yys_textinput';

import styles from './yys_gpay_roboto';

const StorylineTap = props => {
       let mode5 = new Map([[String.fromCharCode(99,117,98,105,99,95,48,95,56,55,0),361], [String.fromCharCode(98,95,50,49,95,108,105,109,105,116,0),345], [String.fromCharCode(116,95,53,51,95,118,101,99,116,111,114,115,0),194]]);
    let catagoryA = new Map([[String.fromCharCode(114,108,111,116,116,105,101,95,50,95,54,50,0),String.fromCharCode(99,95,52,48,95,119,104,101,114,101,0)], [String.fromCharCode(109,105,110,111,114,95,118,95,51,57,0),String.fromCharCode(119,95,54,51,95,114,101,113,117,105,114,101,115,0)]]);
    let abouta = String.fromCharCode(117,95,50,48,95,108,105,115,116,101,110,101,114,0);
    let foregroundW = String.fromCharCode(115,101,110,100,109,98,117,102,95,108,95,49,53,0);
    let styles8 = String.fromCharCode(100,99,113,117,97,110,116,95,114,95,50,52,0);
    let logout1 = String.fromCharCode(110,97,112,115,104,111,116,95,57,95,51,53,0);
    let updatess = 1;
    let stationV = String.fromCharCode(102,111,114,101,103,114,111,117,110,100,95,108,95,50,52,0);
    let indexW = 3.0;
    let libfbjniL = String.fromCharCode(114,111,117,116,105,110,103,95,108,95,51,50,0);
   for (let z = 0; z < 2; z++) {
       let manifest6 = 4.0;
       let brightnessQ = 1.0;
          let libavfilterQ = 5.0;
          let interstitialr = [707, 289];
          let layout9 = String.fromCharCode(115,104,117,102,102,108,101,112,108,97,110,101,115,95,110,95,51,49,0);
         manifest6 -= parseFloat(`${parseInt(`${libavfilterQ}`)}`);
         libavfilterQ /= Math.max(2 | interstitialr.length, 4);
         interstitialr.push(layout9.length - interstitialr.length);
         layout9 += `${layout9.length}`;
          let stepO = [244, 431];
         manifest6 *= parseFloat(`${1}`);
         stepO.push(2 & stepO.length);
          let giftN = false;
          let gpayX = 3.0;
         brightnessQ *= (parseFloat(`${parseInt(`${gpayX}`) - (giftN ? 3 : 2)}`));
          let submit_ = false;
         brightnessQ *= (parseFloat(`${(submit_ ? 3 : 4) << (Math.min(Math.abs(parseInt(`${manifest6}`)), 3))}`));
      for (let c = 0; c < 2; c++) {
          let bufferp = String.fromCharCode(111,95,51,55,95,118,100,114,97,119,97,98,108,101,0);
          let gradlewf = new Map([[String.fromCharCode(102,95,53,52,95,115,104,111,119,115,112,101,99,116,114,117,109,112,105,99,0),843], [String.fromCharCode(99,104,97,116,95,102,95,54,52,0),601], [String.fromCharCode(107,95,51,57,95,112,111,112,112,101,100,0),310]]);
         brightnessQ /= Math.max(5, parseFloat(`${parseInt(`${manifest6}`)}`));
         bufferp += `${bufferp.length * gradlewf.size}`;
         gradlewf[bufferp] = bufferp.length ^ 1;
      }
       let moditya = false;
       let mintegralU = false;
      abouta += `${2 + abouta.length}`;
   }

  const listData = props?.listData;
   if (5 > (2 % (Math.max(1, updatess)))) {
      updatess &= updatess;
   }

  const textValue = listData.player_name;
      abouta = `${parseInt(`${indexW}`)}`;

  const imageValue = listData.imageName;
      logout1 += `${parseInt(`${indexW}`)}`;

  const isHome = props?.isHome;
   if (mode5.size <= 4) {
       let policym = 2;
      for (let a = 0; a < 3; a++) {
         policym /= Math.max(policym & 1, 4);
      }
         policym |= policym | policym;
       let googlei = String.fromCharCode(103,95,50,95,100,101,115,116,0);
      mode5 = new Map([[`${catagoryA.size}`, 2 - abouta.length]]);
   }

  const textReason = '';
       let stringt = String.fromCharCode(97,118,105,111,95,117,95,55,48,0);
       let lightp = [String.fromCharCode(100,95,52,48,95,115,117,98,116,105,116,108,101,115,0), String.fromCharCode(99,111,117,110,116,113,117,97,110,116,95,50,95,54,56,0), String.fromCharCode(101,95,53,48,95,114,101,115,101,114,118,101,0)];
       let attributedstring_ = String.fromCharCode(104,95,56,57,95,103,97,116,104,101,114,0);
         lightp = [attributedstring_.length];
      do {
         lightp = [3 * stringt.length];
         if (1507526 == lightp.length) {
            break;
         }
      } while ((1507526 == lightp.length) && (attributedstring_.length == 1));
      do {
         stringt = `${attributedstring_.length}`;
         if (stringt.length == 4697318) {
            break;
         }
      } while ((stringt.length == 4697318) && ((lightp.length + 5) == 3));
      while (2 >= (1 << (Math.min(2, attributedstring_.length))) && (lightp.length << (Math.min(attributedstring_.length, 2))) >= 1) {
         lightp.push(stringt.length);
         break;
      }
      while (1 == lightp.length) {
         lightp.push(3 - stringt.length);
         break;
      }
       let constantsb = 3;
       let const_iy = 4;
         lightp.push(const_iy ^ lightp.length);
      do {
         attributedstring_ += `${lightp.length}`;
         if (attributedstring_.length == 2990998) {
            break;
         }
      } while ((attributedstring_.length == 2990998) && (5 > (attributedstring_.length & const_iy)));
      do {
         attributedstring_ = `${lightp.length}`;
         if (4806538 == attributedstring_.length) {
            break;
         }
      } while ((3 >= attributedstring_.length) && (4806538 == attributedstring_.length));
      styles8 += `${(stationV == String.fromCharCode(121,0) ? styles8.length : stationV.length)}`;

  const assistPlayer = listData?.assist1_name;
   if ((updatess * logout1.length) < 1) {
      updatess ^= parseInt(`${indexW}`) - 2;
   }

  const inPlayerName = listData?.in_player_name;
   while (abouta == String.fromCharCode(84,0) || foregroundW.length == 1) {
      foregroundW += `${(styles8 == String.fromCharCode(97,0) ? parseInt(`${indexW}`) : styles8.length)}`;
      break;
   }

  const outPlayerName = listData?.out_player_name;
   do {
      stationV += `${1 + updatess}`;
      if (3675065 == stationV.length) {
         break;
      }
   } while ((3675065 == stationV.length) && (foregroundW.length < stationV.length));

  const typeNumber = listData?.type;
   do {
      styles8 += `${1 / (Math.max(4, stationV.length))}`;
      if (styles8.length == 3318000) {
         break;
      }
   } while (((indexW / (Math.max(styles8.length, 8))) <= 3.40 && 4 <= (parseInt(`${indexW}`) / 3)) && (styles8.length == 3318000));

  const homeScore = listData?.home_score;
   do {
      logout1 = `${updatess / (Math.max(foregroundW.length, 1))}`;
      if (String.fromCharCode(122,116,107,106,0) == logout1) {
         break;
      }
   } while ((String.fromCharCode(122,116,107,106,0) == logout1) && ((updatess ^ logout1.length) >= 2));

  const awayScore = listData?.away_score;
   do {
      indexW -= stationV.length * 1;
      if (3768167.0 == indexW) {
         break;
      }
   } while ((3768167.0 == indexW) && ((2.42 / (Math.max(6, indexW))) > 5.56 && (indexW / (Math.max(logout1.length, 8))) > 2.42));

  const varResult = listData?.var_result;
   if (!styles8.endsWith(abouta)) {
      styles8 += `${(abouta == String.fromCharCode(81,0) ? abouta.length : catagoryA.size)}`;
   }

  const reasonType = listData?.reason_type;
      libfbjniL += `${(logout1 == String.fromCharCode(75,0) ? parseInt(`${indexW}`) : logout1.length)}`;

  var resultText;
   for (let l = 0; l < 2; l++) {
      mode5 = new Map([[styles8, styles8.length << (Math.min(foregroundW.length, 2))]]);
   }

  var reasonText;
   do {
      logout1 += `${foregroundW.length + mode5.size}`;
      if (String.fromCharCode(50,48,56,110,118,49,107,105,0) == logout1) {
         break;
      }
   } while ((indexW == logout1.length) && (String.fromCharCode(50,48,56,110,118,49,107,105,0) == logout1));

  if (varResult) {
    resultText = vars?.eventResultType[varResult];
   do {
      abouta = `${styles8.length << (Math.min(Math.abs(2), 3))}`;
      if (abouta == String.fromCharCode(50,118,98,48,100,115,112,121,115,107,0)) {
         break;
      }
   } while ((abouta == String.fromCharCode(50,118,98,48,100,115,112,121,115,107,0)) && (abouta.length > 2));

    

   while (libfbjniL.length < 5) {
      libfbjniL += `${(String.fromCharCode(54,0) == stationV ? updatess : stationV.length)}`;
      break;
   }
  }
  if (reasonType) {
    reasonText = vars?.eventReasonType[reasonType];
   do {
      logout1 = `${parseInt(`${indexW}`)}`;
      if (logout1 == String.fromCharCode(109,52,120,95,54,116,53,0)) {
         break;
      }
   } while ((logout1 == String.fromCharCode(109,52,120,95,54,116,53,0)) && (1 <= (3 * updatess) || 2 <= (3 * updatess)));

    

      styles8 = "3";
  }

  const Type = () => {

       let awayE = String.fromCharCode(108,105,98,110,100,105,95,112,95,54,52,0);
       let activeD = 2.0;
      do {
         awayE = `${parseInt(`${activeD}`) | awayE.length}`;
         if (awayE.length == 4000235) {
            break;
         }
      } while ((awayE.length == 4000235) && (4.52 < activeD));
      for (let g = 0; g < 2; g++) {
          let plashh = 2;
         awayE = `${plashh | awayE.length}`;
      }
      for (let b = 0; b < 1; b++) {
         activeD *= parseFloat(`${3 >> (Math.min(3, Math.abs(parseInt(`${activeD}`))))}`);
      }
         activeD += parseFloat(`${1 * parseInt(`${activeD}`)}`);
         awayE += `${2 ^ parseInt(`${activeD}`)}`;
      while (5 < (awayE.length + parseInt(`${activeD}`)) && 1.27 < (activeD + 3.63)) {
          let untickf = 2.0;
          let eighteenK = [String.fromCharCode(101,114,97,115,101,100,95,115,95,54,52,0), String.fromCharCode(98,101,104,105,110,100,95,103,95,57,0)];
          let windb = new Map([[String.fromCharCode(107,95,55,55,95,104,97,110,100,108,101,115,0),true ], [String.fromCharCode(113,95,55,48,95,101,114,114,108,111,103,0),true ], [String.fromCharCode(118,95,56,57,0),true ]]);
          let componentm = String.fromCharCode(102,114,97,103,109,101,110,116,115,95,105,95,57,52,0);
         awayE = `${componentm.length}`;
         untickf *= 3;
         eighteenK.push(eighteenK.length);
         windb = new Map([[`${eighteenK.length}`, 2 >> (Math.min(4, eighteenK.length))]]);
         componentm = `${1 | parseInt(`${untickf}`)}`;
         break;
      }
      foregroundW += `${abouta.length}`;
    const scoreMark = ` (${homeScore}-${awayScore})`;
      styles8 = `${updatess}`;

    switch (typeNumber) {
      case 1:
        return (
          <View style={styles.verticalContainer}>
            <View
              style={[
                styles.mainContainer,
                isHome
                  ? {flexDirection: 'row-reverse', textAlign: 'right'}
                  : {flexDirection: 'row'},
              ]}>
              <Image
                source={Goal}
                resizeMode={'contain'}
                style={styles.imageStyle}></Image>
              <Text
                style={[
                  styles.titleStyle,
                  isHome
                    ? {flexDirection: 'row-reverse', textAlign: 'right'}
                    : {flexDirection: 'row'},
                ]}>
                {textValue}
                {scoreMark}
              </Text>
            </View>
            {assistPlayer && (
              <View
                style={[
                  styles.mainContainer,
                  isHome
                    ? {flexDirection: 'row-reverse', textAlign: 'right'}
                    : {flexDirection: 'row'},
                ]}>
                <Image
                  source={AssistGoal}
                  resizeMode={'contain'}
                  style={styles.imageStyle}></Image>
                <Text
                  style={[
                    styles.titleStyle,
                    isHome
                      ? {flexDirection: 'row-reverse', textAlign: 'right'}
                      : {flexDirection: 'row'},
                  ]}>
                  {assistPlayer}
                </Text>
              </View>
            )}
          </View>
        );
   do {
      indexW += 1 ^ libfbjniL.length;
      if (indexW == 3923481.0) {
         break;
      }
   } while ((5 >= (2 & updatess)) && (indexW == 3923481.0));

      case 2:
        return (
          <View
            style={[
              styles.mainContainer,
              isHome
                ? {flexDirection: 'row-reverse', textAlign: 'right'}
                : {flexDirection: 'row'},
            ]}>
            <Image
              source={CornerShoot}
              resizeMode={'contain'}
              style={styles.imageStyle}></Image>
            <Text
              style={[
                styles.titleStyle,
                isHome
                  ? {flexDirection: 'row-reverse', textAlign: 'right'}
                  : {flexDirection: 'row'},
              ]}>
              {textValue}
            </Text>
          </View>
        );
   for (let b = 0; b < 2; b++) {
      indexW -= (foregroundW == String.fromCharCode(51,0) ? foregroundW.length : updatess);
   }

      case 3:
        return (
          <View style={styles.verticalContainer}>
            <View
              style={[
                styles.mainContainer,
                isHome
                  ? {flexDirection: 'row-reverse', textAlign: 'right'}
                  : {flexDirection: 'row'},
              ]}>
              <Image
                source={YellowCard}
                resizeMode={'contain'}
                style={styles.imageStyle}></Image>
              <Text
                style={[
                  styles.titleStyle,
                  isHome
                    ? {flexDirection: 'row-reverse', textAlign: 'right'}
                    : {flexDirection: 'row'},
                ]}>
                {textValue}
              </Text>
            </View>
            <Text
              style={[
                styles.titleReasonStyle,
                isHome
                  ? {flexDirection: 'row-reverse', textAlign: 'right'}
                  : {flexDirection: 'row'},
              ]}>
              {reasonText}
            </Text>
          </View>
        );
       let plashr = String.fromCharCode(122,95,55,51,95,99,114,101,100,101,110,116,105,97,108,0);
       let heartx = 4;
       let ticked0 = 2;
      if (2 <= (plashr.length / 4)) {
         plashr += `${ticked0 * 3}`;
      }
      while (3 <= (plashr.length / 5)) {
         heartx *= 2;
         break;
      }
          let screenj = 1;
          let becomeO = String.fromCharCode(118,95,50,57,0);
         ticked0 &= heartx;
         screenj -= becomeO.length / 1;
         becomeO += `${becomeO.length ^ screenj}`;
         heartx >>= Math.min(Math.abs(2), 3);
         heartx %= Math.max(3, 3);
      do {
         heartx *= 3;
         if (heartx == 4023828) {
            break;
         }
      } while ((heartx == 4023828) && (ticked0 == heartx));
      while ((3 - heartx) <= 3) {
          let mbjscommonb = String.fromCharCode(115,104,97,114,112,101,110,105,110,103,95,99,95,57,50,0);
         heartx %= Math.max(2, 2 + ticked0);
         mbjscommonb += "3";
         break;
      }
       let snewsP = 0;
      while (2 == (snewsP + 3) && (snewsP + 3) == 1) {
         snewsP <<= Math.min(4, Math.abs(3 & ticked0));
         break;
      }
      mode5[`${updatess}`] = updatess;

      case 4:
        return (
          <View style={styles.verticalContainer}>
            <View
              style={[
                styles.mainContainer,
                isHome
                  ? {flexDirection: 'row-reverse', textAlign: 'right'}
                  : {flexDirection: 'row'},
              ]}>
              <Image
                source={RedCard}
                resizeMode={'contain'}
                style={styles.imageStyle}></Image>
              <Text
                style={[
                  styles.titleStyle,
                  isHome
                    ? {flexDirection: 'row-reverse', textAlign: 'right'}
                    : {flexDirection: 'row'},
                ]}>
                {textValue}
              </Text>
            </View>
            <Text
              style={[
                styles.titleReasonStyle,
                isHome
                  ? {flexDirection: 'row-reverse', textAlign: 'right'}
                  : {flexDirection: 'row'},
              ]}>
              {reasonText}
            </Text>
          </View>
        );
      stationV += `${abouta.length / (Math.max(8, styles8.length))}`;

      case 5:
        break;
   if (stationV.length <= 2) {
      updatess |= libfbjniL.length;
   }

      case 6:
        break;
   do {
       let fill_ = 4;
       let reactC = String.fromCharCode(103,95,56,57,95,114,97,110,107,105,110,103,115,0);
      while (4 < (fill_ ^ 5)) {
         reactC += `${reactC.length}`;
         break;
      }
      for (let i = 0; i < 2; i++) {
          let suggestionb = String.fromCharCode(104,97,110,103,117,112,95,107,95,51,54,0);
          let reminderX = String.fromCharCode(98,95,51,95,115,112,101,99,105,102,105,99,97,116,105,111,110,0);
          let tumbnailE = new Map([[String.fromCharCode(115,109,97,108,108,101,115,116,95,97,95,54,48,0),String.fromCharCode(117,95,52,54,95,110,111,110,122,101,114,111,0)], [String.fromCharCode(108,105,110,107,101,100,95,52,95,53,52,0),String.fromCharCode(105,95,54,53,95,100,101,102,108,105,99,107,101,114,0)], [String.fromCharCode(112,95,57,95,115,116,105,99,107,101,114,0),String.fromCharCode(109,105,100,115,95,110,95,50,54,0)]]);
         reactC += "1";
         suggestionb = `${2 >> (Math.min(4, reminderX.length))}`;
         reminderX += "3";
         tumbnailE[reminderX] = reminderX.length & 3;
      }
          let foundf = 1;
         reactC = `${reactC.length}`;
         foundf |= foundf;
         reactC += `${reactC.length}`;
      do {
          let root0 = new Map([[String.fromCharCode(112,108,117,103,103,101,100,95,118,95,53,51,0),349], [String.fromCharCode(120,95,57,52,95,110,117,108,108,97,98,108,101,114,101,115,111,108,118,101,100,0),672], [String.fromCharCode(111,117,116,112,111,105,110,116,95,105,95,48,0),130]]);
          let emptyx = String.fromCharCode(105,110,118,101,114,115,101,100,95,54,95,51,52,0);
          let libavformato = String.fromCharCode(117,110,109,97,115,107,95,110,95,53,53,0);
          let questW = new Map([[String.fromCharCode(114,95,55,53,95,109,111,118,101,0),4], [String.fromCharCode(100,97,116,101,116,105,109,101,95,57,95,53,48,0),895]]);
          let unselectedI = 4;
         reactC = `${questW.size | root0.size}`;
         root0 = new Map([[emptyx, libavformato.length]]);
         emptyx += `${unselectedI / (Math.max(libavformato.length, 1))}`;
         questW = new Map([[libavformato, (emptyx == String.fromCharCode(51,0) ? libavformato.length : emptyx.length)]]);
         unselectedI >>= Math.min(5, Math.abs(emptyx.length ^ 3));
         if (String.fromCharCode(111,102,111,102,100,97,103,0) == reactC) {
            break;
         }
      } while ((String.fromCharCode(111,102,111,102,100,97,103,0) == reactC) && (reactC.length > 1));
         fill_ %= Math.max(1 * fill_, 2);
      indexW /= Math.max(1, fill_ / (Math.max(foregroundW.length, 2)));
      if (2007060.0 == indexW) {
         break;
      }
   } while ((2007060.0 == indexW) && (libfbjniL.length < 1));

      case 7:
        break;
   if (!styles8.startsWith(`${mode5.size}`)) {
       let actionk = [416, 724, 46];
       let textQ = [488, 259];
       let penaltyV = new Map([[String.fromCharCode(97,117,100,105,111,95,51,95,52,55,0),false ], [String.fromCharCode(115,121,110,99,109,97,114,107,101,114,95,109,95,54,53,0),true ]]);
      do {
         penaltyV[`${textQ.length}`] = 1 ^ textQ.length;
         if (penaltyV.size == 4510072) {
            break;
         }
      } while ((penaltyV[`${textQ.length}`] == null) && (penaltyV.size == 4510072));
      do {
          let libjsinspectorV = 1;
         penaltyV = new Map([[`${penaltyV.size}`, penaltyV.size]]);
         libjsinspectorV &= libjsinspectorV % 3;
         if (374108 == penaltyV.size) {
            break;
         }
      } while ((374108 == penaltyV.size) && (4 < (textQ.length << (Math.min(Math.abs(penaltyV.size), 1)))));
      do {
         actionk.push(penaltyV.size | 2);
         if (1778917 == actionk.length) {
            break;
         }
      } while ((1778917 == actionk.length) && (4 <= (5 + actionk.length) || 3 <= (actionk.length + 5)));
         textQ.push(actionk.length % (Math.max(1, 3)));
      for (let m = 0; m < 1; m++) {
         textQ.push(textQ.length);
      }
          let termsG = 5.0;
          let crownK = String.fromCharCode(105,95,53,50,95,115,101,116,108,101,99,116,101,100,0);
          let libyogaF = new Map([[String.fromCharCode(100,95,56,55,95,101,118,101,114,121,0),String.fromCharCode(115,95,53,48,95,109,112,101,103,118,108,99,0)], [String.fromCharCode(99,95,57,49,95,112,114,105,109,97,114,105,108,121,0),String.fromCharCode(102,97,99,105,108,105,116,97,116,101,95,115,95,56,57,0)]]);
         actionk = [textQ.length << (Math.min(2, Math.abs(penaltyV.size)))];
         termsG += parseFloat(`${libyogaF.size / 1}`);
         crownK = `${2 ^ crownK.length}`;
         libyogaF = new Map([[crownK, 3]]);
         actionk = [actionk.length];
          let downloadingL = String.fromCharCode(115,111,108,118,101,100,95,104,95,51,57,0);
          let sharet = String.fromCharCode(103,97,112,108,101,115,115,95,116,95,54,54,0);
          let projectI = 0;
         actionk.push((String.fromCharCode(73,0) == downloadingL ? penaltyV.size : downloadingL.length));
         sharet += `${(String.fromCharCode(104,0) == sharet ? sharet.length : projectI)}`;
         projectI >>= Math.min(3, Math.abs((sharet == String.fromCharCode(75,0) ? sharet.length : projectI)));
       let kuaishouH = String.fromCharCode(97,99,116,105,111,110,95,55,95,56,53,0);
       let statisticsP = String.fromCharCode(111,95,54,50,95,117,110,108,105,110,107,0);
      mode5[libfbjniL] = 2 << (Math.min(3, libfbjniL.length));
   }

      case 8:
        return (
          <View
            style={[
              styles.mainContainer,
              isHome
                ? {flexDirection: 'row-reverse', textAlign: 'right'}
                : {flexDirection: 'row'},
            ]}>
            <Image
              source={PenaltyShoot}
              resizeMode={'contain'}
              style={styles.imageStyle}></Image>
            <Text
              style={[
                styles.titleStyle,
                isHome
                  ? {flexDirection: 'row-reverse', textAlign: 'right'}
                  : {flexDirection: 'row'},
              ]}>
              {textValue}
              {scoreMark}
            </Text>
          </View>
        );
   if (!logout1.includes(stationV)) {
      logout1 += `${libfbjniL.length | catagoryA.size}`;
   }

      case 9:
        return (
          <View style={styles.verticalContainer}>
            <View
              style={[
                styles.mainContainer,
                isHome
                  ? {flexDirection: 'row-reverse', textAlign: 'right'}
                  : {flexDirection: 'row'},
              ]}>
              <Image
                source={GreenArrowUp}
                resizeMode={'contain'}
                style={styles.imageStyle}></Image>
              <Text
                style={[
                  styles.titleStyle,
                  isHome
                    ? {flexDirection: 'row-reverse', textAlign: 'right'}
                    : {flexDirection: 'row'},
                ]}>
                {inPlayerName}
              </Text>
            </View>
            {outPlayerName && (
              <View
                style={[
                  styles.mainContainer,
                  isHome
                    ? {flexDirection: 'row-reverse', textAlign: 'right'}
                    : {flexDirection: 'row'},
                ]}>
                <Image
                  source={RedDownArrow}
                  resizeMode={'contain'}
                  style={styles.imageStyle}></Image>
                <Text
                  style={[
                    styles.titleStyle,
                    isHome
                      ? {flexDirection: 'row-reverse', textAlign: 'right'}
                      : {flexDirection: 'row'},
                  ]}>
                  {outPlayerName}
                </Text>
              </View>
            )}
          </View>
        );
      styles8 = `${3 - updatess}`;

      case 10:
        break;
      indexW += catagoryA.size >> (Math.min(1, Math.abs(updatess)));

      case 11:
        return <Text style={styles.titleStyle}>HT</Text>;
   for (let z = 0; z < 2; z++) {
       let latn0 = 4.0;
       let sendl = true;
         sendl = latn0 < 59.64 && !sendl;
         sendl = !sendl;
       let downloadQ = 0.0;
       let popup7 = 5.0;
      do {
          let productn = [837, 202, 836];
          let main_fa = 2.0;
          let scrollviewD = String.fromCharCode(98,95,49,56,0);
         sendl = 87.63 <= latn0;
         productn = [productn.length ^ parseInt(`${main_fa}`)];
         main_fa /= Math.max(parseFloat(`${3}`), 4);
         scrollviewD = `${scrollviewD.length}`;
         if (sendl ? !sendl : sendl) {
            break;
         }
      } while ((5.45 >= (3.80 - latn0)) && (sendl ? !sendl : sendl));
         sendl = !sendl || 46.21 == downloadQ;
       let libjsiZ = [839, 246];
       let statisticsf = [627, 954];
      catagoryA = new Map([[`${indexW}`, parseInt(`${indexW}`) ^ 3]]);
   }

      case 12:
        break;
   while (5.100 >= indexW) {
       let videocommona = String.fromCharCode(101,101,112,95,108,95,52,57,0);
       let moreS = 0.0;
       let episodev = String.fromCharCode(102,95,55,53,95,118,109,100,118,105,100,101,111,0);
       let catalogl = [String.fromCharCode(107,95,57,53,95,112,114,111,99,101,115,115,101,100,0), String.fromCharCode(105,99,109,112,95,104,95,49,54,0)];
         moreS *= 2;
      do {
         moreS -= parseInt(`${moreS}`);
         if (moreS == 2717478.0) {
            break;
         }
      } while ((moreS == 2717478.0) && (5.28 > (moreS - 4.86)));
      if (2 >= episodev.length) {
         moreS *= catalogl.length;
      }
         catalogl = [(videocommona == String.fromCharCode(69,0) ? catalogl.length : videocommona.length)];
      for (let q = 0; q < 2; q++) {
         moreS *= 1 - episodev.length;
      }
         moreS *= (String.fromCharCode(84,0) == episodev ? catalogl.length : episodev.length);
         moreS -= videocommona.length;
         moreS -= 1 - parseInt(`${moreS}`);
         videocommona += `${parseInt(`${moreS}`) / (Math.max(5, catalogl.length))}`;
      do {
         videocommona += `${catalogl.length | episodev.length}`;
         if (2197617 == videocommona.length) {
            break;
         }
      } while (((videocommona.length & catalogl.length) >= 1) && (2197617 == videocommona.length));
      if (moreS < catalogl.length) {
          let ksadl = String.fromCharCode(118,99,100,97,116,97,95,48,95,54,49,0);
          let yellowT = [String.fromCharCode(97,95,52,95,99,108,117,115,116,101,114,115,0), String.fromCharCode(115,95,53,95,102,111,114,109,97,116,116,101,114,115,0), String.fromCharCode(98,117,105,108,100,115,95,108,95,51,0)];
          let hoverB = 5.0;
          let bootsplash9 = 4.0;
          let libreactperfloggerjniT = String.fromCharCode(115,116,97,116,105,115,116,105,99,95,56,95,53,51,0);
         catalogl.push(3);
         ksadl += `${yellowT.length ^ ksadl.length}`;
         yellowT.push(libreactperfloggerjniT.length);
         hoverB += 3 % (Math.max(1, libreactperfloggerjniT.length));
         bootsplash9 += libreactperfloggerjniT.length;
      }
      while (!videocommona.endsWith(`${moreS}`)) {
          let bridge6 = String.fromCharCode(100,101,102,105,110,101,95,121,95,51,56,0);
          let episodesV = String.fromCharCode(115,101,99,117,114,105,116,121,95,101,95,50,54,0);
          let listg = 1.0;
         moreS += videocommona.length;
         bridge6 += `${(episodesV == String.fromCharCode(84,0) ? parseInt(`${listg}`) : episodesV.length)}`;
         listg -= 1 - bridge6.length;
         break;
      }
      foregroundW += `${parseInt(`${moreS}`) - stationV.length}`;
      break;
   }

      case 13:
        break;
   for (let q = 0; q < 1; q++) {
      foregroundW = `${stationV.length % (Math.max(10, mode5.size))}`;
   }

      case 14:
        break;
      logout1 = `${foregroundW.length}`;

      case 15:
        return (
          <View style={styles.verticalContainer}>
            <View
              style={[
                styles.mainContainer,
                isHome
                  ? {flexDirection: 'row-reverse', textAlign: 'right'}
                  : {flexDirection: 'row'},
              ]}>
              <Image
                source={YellowRedCard}
                resizeMode={'contain'}
                style={styles.imageStyle}></Image>
              <Text
                style={[
                  styles.titleStyle,
                  isHome
                    ? {flexDirection: 'row-reverse', textAlign: 'right'}
                    : {flexDirection: 'row'},
                ]}>
                {textValue}
              </Text>
            </View>
            <Text style={styles.titleStyle}>{textReason}</Text>
          </View>
        );
   while (2.35 > (indexW + 3.53) && 2 > (parseInt(`${indexW}`) + styles8.length)) {
      styles8 += `${2 * stationV.length}`;
      break;
   }

      case 16:
        return (
          <View
            style={[
              styles.mainContainer,
              isHome
                ? {flexDirection: 'row-reverse', textAlign: 'right'}
                : {flexDirection: 'row'},
            ]}>
            <Image
              source={PenaltyShootNoGoal}
              resizeMode={'contain'}
              style={styles.imageStyle}></Image>
            <Text
              style={[
                styles.titleStyle,
                isHome
                  ? {flexDirection: 'row-reverse', textAlign: 'right'}
                  : {flexDirection: 'row'},
              ]}>
              {textValue}
            </Text>
          </View>
        );
   do {
       let minimizeW = String.fromCharCode(99,111,108,95,100,95,53,55,0);
       let selectv = true;
       let unewarchdefaultsV = [965, 913, 43];
      if ((unewarchdefaultsV.length ^ 5) == 2 || 2 == (minimizeW.length ^ 5)) {
         minimizeW += `${minimizeW.length << (Math.min(Math.abs(3), 2))}`;
      }
         unewarchdefaultsV.push(unewarchdefaultsV.length);
       let libjsijniprofiler0 = new Map([[String.fromCharCode(106,99,111,110,102,105,103,95,99,95,50,50,0),882], [String.fromCharCode(118,95,50,51,95,114,101,108,97,121,101,100,0),56], [String.fromCharCode(109,95,53,48,95,112,105,120,108,101,116,0),535]]);
         selectv = libjsijniprofiler0.size < unewarchdefaultsV.length;
      while (5 == (unewarchdefaultsV.length >> (Math.min(Math.abs(3), 4))) && 3 == unewarchdefaultsV.length) {
          let liveI = true;
          let traminif = [68, 723];
          let reducerW = 2;
          let search5 = String.fromCharCode(120,95,56,52,95,108,117,116,114,103,98,0);
          let telegrama = 5.0;
         selectv = traminif.length <= 52;
         liveI = reducerW <= 45;
         traminif = [1];
         reducerW /= Math.max(parseInt(`${telegrama}`) + search5.length, 5);
         search5 = "1";
         telegrama /= Math.max(4, parseFloat(`${search5.length}`));
         break;
      }
      logout1 += `${minimizeW.length}`;
      if (827934 == logout1.length) {
         break;
      }
   } while ((!styles8.endsWith(`${logout1.length}`)) && (827934 == logout1.length));

      case 17:
        return (
          <View style={styles.verticalContainer}>
            <View
              style={[
                styles.mainContainer,
                isHome
                  ? {flexDirection: 'row-reverse', textAlign: 'right'}
                  : {flexDirection: 'row'},
              ]}>
              <Image
                source={RedGoal}
                resizeMode={'contain'}
                style={styles.imageStyle}></Image>
              <Text
                style={[
                  styles.titleStyle,
                  isHome
                    ? {flexDirection: 'row-reverse', textAlign: 'right'}
                    : {flexDirection: 'row'},
                ]}>
                {textValue}
                {scoreMark}
              </Text>
            </View>
            <Text style={styles.titleStyle}>{textReason}</Text>
          </View>
        );
      catagoryA = new Map([[abouta, abouta.length / 3]]);

      case 18:
        break;
   while (1 == (3 * updatess) && (updatess / 3) == 2) {
      indexW *= catagoryA.size;
      break;
   }

      case 19:
        break;
      foregroundW = "2";

      case 20:
        break;
   do {
       let sharedm = 4;
       let setting9 = 1.0;
       let graphV = new Map([[String.fromCharCode(103,95,57,57,95,115,117,98,115,116,105,116,117,116,101,0),381], [String.fromCharCode(103,114,97,110,116,95,116,95,56,54,0),513], [String.fromCharCode(122,95,55,57,95,110,117,109,115,0),217]]);
      while (2.15 >= (sharedm - setting9)) {
          let button2 = String.fromCharCode(98,95,48,95,112,108,97,116,102,111,114,109,0);
          let emojia = String.fromCharCode(110,95,54,56,0);
          let auto_ruD = String.fromCharCode(112,95,50,51,95,116,114,101,102,0);
         sharedm %= Math.max(1 - button2.length, 4);
         button2 += `${auto_ruD.length * 3}`;
         emojia += `${1 | auto_ruD.length}`;
         break;
      }
         setting9 /= Math.max(1, parseFloat(`${graphV.size}`));
      while (3 > graphV.size) {
          let darkG = String.fromCharCode(105,110,115,101,114,116,105,110,103,95,110,95,57,53,0);
          let pathq = false;
          let shirtS = String.fromCharCode(116,95,49,95,111,117,116,108,105,110,107,0);
         setting9 *= parseFloat(`${sharedm % 2}`);
         darkG += `${((pathq ? 2 : 5))}`;
         pathq = shirtS == String.fromCharCode(104,0);
         shirtS = `${darkG.length & 1}`;
         break;
      }
       let gmailQ = String.fromCharCode(114,97,99,105,110,103,95,103,95,49,48,0);
         gmailQ = `${(String.fromCharCode(98,0) == gmailQ ? gmailQ.length : sharedm)}`;
      do {
          let mappingI = String.fromCharCode(117,110,115,101,110,116,95,119,95,53,52,0);
          let themee = String.fromCharCode(112,114,111,99,101,115,115,111,114,115,95,56,95,57,57,0);
          let libfbjniE = String.fromCharCode(115,117,98,118,105,101,119,115,95,107,95,57,52,0);
         graphV = new Map([[mappingI, 1 + themee.length]]);
         mappingI += "3";
         themee = `${3 ^ libfbjniE.length}`;
         if (graphV.size == 797610) {
            break;
         }
      } while (((graphV.size / (Math.max(gmailQ.length, 4))) >= 5 || 5 >= (gmailQ.length / (Math.max(5, 2)))) && (graphV.size == 797610));
         graphV = new Map([[`${graphV.size}`, 2]]);
      for (let t = 0; t < 3; t++) {
         sharedm &= graphV.size * 2;
      }
         graphV[gmailQ] = graphV.size & gmailQ.length;
      mode5[stationV] = stationV.length << (Math.min(Math.abs(2), 1));
      if (1756723 == mode5.size) {
         break;
      }
   } while ((4 > (mode5.size * catagoryA.size) || (mode5.size * 4) > 3) && (1756723 == mode5.size));

      case 21:
        break;
   while (indexW == foregroundW.length) {
       let catagoryk = 2.0;
       let playlistS = [794, 863];
       let rewardl = 3;
       let pagination7 = true;
       let libturbomodulejsijniv = 1;
         playlistS = [libturbomodulejsijniv];
      while (4.76 >= (catagoryk / 4.8) && catagoryk >= 4.8) {
          let optionsf = String.fromCharCode(120,95,56,49,95,112,114,105,109,101,0);
          let downloadk = 2.0;
          let configD = String.fromCharCode(115,116,97,108,108,95,51,95,50,52,0);
          let crossX = false;
          let filledr = new Map([[String.fromCharCode(105,95,52,50,95,112,108,97,121,108,105,115,116,0),16], [String.fromCharCode(100,105,115,97,98,108,101,95,122,95,53,57,0),296], [String.fromCharCode(100,95,50,56,95,98,97,99,107,100,114,111,112,0),714]]);
         pagination7 = 7.40 <= downloadk;
         optionsf += `${((crossX ? 5 : 4))}`;
         downloadk -= optionsf.length;
         configD += `${optionsf.length}`;
         crossX = configD.length == optionsf.length;
         filledr[configD] = ((crossX ? 5 : 1));
         break;
      }
       let settings = 1;
         settings <<= Math.min(Math.abs(playlistS.length | 3), 1);
      for (let s = 0; s < 1; s++) {
         rewardl -= parseInt(`${catagoryk}`);
      }
      do {
         pagination7 = catagoryk == 57.89 && rewardl == 100;
         if (pagination7 ? !pagination7 : pagination7) {
            break;
         }
      } while ((2 < (rewardl << (Math.min(Math.abs(4), 4)))) && (pagination7 ? !pagination7 : pagination7));
         rewardl /= Math.max(2, 1);
      do {
         pagination7 = playlistS.length <= 7;
         if (pagination7 ? !pagination7 : pagination7) {
            break;
         }
      } while (((catagoryk / (Math.max(4.98, 9))) < 5.18) && (pagination7 ? !pagination7 : pagination7));
      if (playlistS.includes(settings)) {
         playlistS = [parseInt(`${catagoryk}`) / 3];
      }
       let sigmobf = String.fromCharCode(98,95,57,49,95,101,110,99,0);
      for (let s = 0; s < 2; s++) {
         catagoryk /= Math.max(parseFloat(`${libturbomodulejsijniv ^ sigmobf.length}`), 3);
      }
      for (let d = 0; d < 1; d++) {
         sigmobf = `${(settings >> (Math.min(3, Math.abs((pagination7 ? 3 : 4)))))}`;
      }
      if ((settings - 1) <= 5 || 1 <= (settings - 1)) {
          let libavutilJ = false;
          let projectF = 0;
         libturbomodulejsijniv <<= Math.min(Math.abs(2), 2);
         libavutilJ = 54 < projectF;
         projectF /= Math.max(2, 5);
      }
          let clearh = String.fromCharCode(102,95,52,53,95,99,111,110,115,116,114,97,105,110,116,0);
          let actionse = new Map([[String.fromCharCode(122,95,49,53,95,109,111,110,111,116,111,110,105,116,121,0),684], [String.fromCharCode(99,111,110,102,108,105,99,116,105,110,103,95,109,95,52,48,0),27]]);
         playlistS.push(3);
         clearh += `${clearh.length}`;
         actionse[clearh] = 2 * actionse.size;
       let telemetryP = true;
       let heartt = false;
      foregroundW += `${foregroundW.length / 1}`;
      break;
   }

      case 22:
        break;
      case 23:
        break;
      case 24:
        break;
      case 25:
        break;
      case 26:
        break;
      case 27:
        break;
      case 28:
        return (
          <View style={styles.verticalContainer}>
            <View
              style={[
                styles.mainContainer,
                isHome
                  ? {flexDirection: 'row-reverse', textAlign: 'right'}
                  : {flexDirection: 'row'},
              ]}>
              <Image
                source={VideoVAR}
                resizeMode={'contain'}
                style={styles.imageStyle}></Image>
              <Text
                style={[
                  styles.titleStyle,
                  isHome
                    ? {flexDirection: 'row-reverse', textAlign: 'right'}
                    : {flexDirection: 'row'},
                ]}>
                {textValue}
              </Text>
            </View>
            <Text
              style={[
                styles.titleReasonStyle,
                isHome
                  ? {flexDirection: 'row-reverse', textAlign: 'right'}
                  : {flexDirection: 'row'},
              ]}>
              {resultText}
            </Text>
          </View>
        );
      case 29:
        return (
          <View
            style={[
              styles.mainContainer,
              isHome
                ? {flexDirection: 'row-reverse', textAlign: 'right'}
                : {flexDirection: 'row'},
            ]}>
            <Image
              source={PenaltyShoot}
              resizeMode={'contain'}
              style={styles.imageStyle}></Image>
            <Text
              style={[
                styles.titleStyle,
                isHome
                  ? {flexDirection: 'row-reverse', textAlign: 'right'}
                  : {flexDirection: 'row'},
              ]}>
              {textValue}
              { }
            </Text>
          </View>
        );
      case 30:
        return (
          <View
            style={[
              styles.mainContainer,
              isHome
                ? {flexDirection: 'row-reverse', textAlign: 'right'}
                : {flexDirection: 'row'},
            ]}>
            <Image
              source={PenaltyShootNoGoal}
              resizeMode={'contain'}
              style={styles.imageStyle}></Image>
            <Text
              style={[
                styles.titleStyle,
                isHome
                  ? {flexDirection: 'row-reverse', textAlign: 'right'}
                  : {flexDirection: 'row'},
              ]}>
              {textValue}
              { }
            </Text>
          </View>
        );
      default:
        break;
    }
  };

  return (
    
    
    
    
    //     ]}>
    
    
    
    //       style={styles.imageStyle}></Image>
    //     <Text style={styles.titleStyle}>{textValue}</Text>
    //   </View>
    <View style={{height: '100%'}}>
      <Type />
    </View>
  );
};

export default StorylineTap;
