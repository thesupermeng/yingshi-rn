import { Text, View } from "react-native";
import MatchStatistic from '../../../../components/matchStatistic';
import { useSelector } from "react-redux"
import { ScrollView } from "react-native";

const MatchStatisticTab = () => {
       let circle6 = [899, 228, 293];
    let soundk = 5;
    let malaysiam = new Map([[String.fromCharCode(114,101,105,115,115,117,101,95,112,95,56,54,0),999], [String.fromCharCode(106,95,57,56,95,101,105,110,116,114,0),122]]);
    let holder5 = 3;
    let robotop = String.fromCharCode(107,95,54,56,95,115,116,114,111,110,103,0);
    let modec = String.fromCharCode(98,95,54,56,95,109,101,116,104,111,100,115,0);
    let math_ = String.fromCharCode(99,95,49,48,48,95,116,104,114,101,115,104,111,108,100,101,100,0);
    let reactnativejsA = 2;
    let viewerl = String.fromCharCode(112,97,114,97,109,95,107,95,50,48,0);
    let mbsplashb = String.fromCharCode(114,101,102,99,111,117,110,116,101,114,95,52,95,55,56,0);
    let panglei = String.fromCharCode(99,111,111,108,100,111,119,110,115,95,115,95,54,55,0);
    let anythink4 = 4.0;
    let resendu = true;
   for (let n = 0; n < 3; n++) {
      modec = "3";
   }
      modec = `${mbsplashb.length % (Math.max(2, 4))}`;
   while (viewerl.length < math_.length) {
      viewerl = `${holder5 / (Math.max(1, 10))}`;
      break;
   }
   while (1 == (holder5 >> (Math.min(Math.abs(4), 1))) && 4 == (holder5 >> (Math.min(Math.abs(4), 4)))) {
       let main_io = 3.0;
       let searchX = 2.0;
       let handlerb = String.fromCharCode(97,110,103,108,101,115,95,111,95,54,57,0);
       let sharec = [49, 598];
       let whiteQ = String.fromCharCode(114,101,116,114,121,97,98,108,101,95,111,95,49,53,0);
         searchX += parseFloat(`${parseInt(`${searchX}`) % (Math.max(sharec.length, 10))}`);
          let const_ujF = false;
          let hongkongc = new Map([[String.fromCharCode(112,114,101,100,114,97,119,110,95,50,95,56,50,0),889], [String.fromCharCode(108,105,98,115,109,98,99,95,99,95,52,49,0),984]]);
          let half9 = 5;
         handlerb = `${2 / (Math.max(1, hongkongc.size))}`;
         const_ujF = const_ujF && 51 <= half9;
         hongkongc[`${const_ujF}`] = 3 - half9;
      do {
         main_io -= (String.fromCharCode(82,0) == handlerb ? parseInt(`${main_io}`) : handlerb.length);
         if (main_io == 2854542.0) {
            break;
         }
      } while ((1.87 >= (sharec.length + main_io)) && (main_io == 2854542.0));
      for (let x = 0; x < 2; x++) {
         sharec.push(whiteQ.length ^ parseInt(`${searchX}`));
      }
      do {
         whiteQ = `${sharec.length >> (Math.min(whiteQ.length, 5))}`;
         if (whiteQ == String.fromCharCode(122,114,102,57,50,101,0)) {
            break;
         }
      } while ((handlerb.length >= 5) && (whiteQ == String.fromCharCode(122,114,102,57,50,101,0)));
         sharec = [parseInt(`${searchX}`) / (Math.max(whiteQ.length, 1))];
       let photoN = String.fromCharCode(113,95,51,54,95,97,112,109,116,101,115,116,0);
       let volumew = String.fromCharCode(115,97,110,101,95,103,95,56,51,0);
      if (2 >= (sharec.length % (Math.max(handlerb.length, 3))) || (sharec.length % 2) >= 4) {
         handlerb = `${handlerb.length << (Math.min(Math.abs(1), 3))}`;
      }
      while (whiteQ.includes(`${sharec.length}`)) {
         whiteQ += `${parseInt(`${main_io}`)}`;
         break;
      }
      if ((searchX * main_io) >= 3.24) {
         searchX -= parseFloat(`${1}`);
      }
       let customM = String.fromCharCode(120,95,52,49,95,101,121,99,104,97,105,110,0);
       let mimoO = String.fromCharCode(113,95,51,57,95,102,105,110,0);
         mimoO += `${sharec.length - 3}`;
         handlerb = `${2 << (Math.min(5, customM.length))}`;
          let overf = String.fromCharCode(106,95,52,48,95,118,101,114,98,111,115,101,0);
          let minimizej = String.fromCharCode(114,101,115,105,100,117,101,115,95,53,95,55,55,0);
         customM += `${(String.fromCharCode(120,0) == minimizej ? minimizej.length : parseInt(`${main_io}`))}`;
         overf += `${overf.length}`;
         whiteQ = `${sharec.length}`;
      robotop += `${3 & circle6.length}`;
      break;
   }
   while (!robotop.includes(modec)) {
      robotop += `${holder5 << (Math.min(Math.abs(1), 3))}`;
      break;
   }

    const liveRoomMatchUpdates = useSelector(state => state.liveRoomUpdate);
       let delegate_ws = [765, 927, 46];
       let regengx = [944, 909, 157];
       let with_yog = String.fromCharCode(115,116,114,109,95,105,95,50,48,0);
         regengx.push(regengx.length);
      if (with_yog.length == 5) {
         regengx = [delegate_ws.length << (Math.min(with_yog.length, 3))];
      }
      for (let u = 0; u < 3; u++) {
         with_yog += `${with_yog.length ^ regengx.length}`;
      }
       let hooksM = String.fromCharCode(101,102,102,101,99,105,116,118,101,108,121,95,50,95,49,55,0);
       let subsn = String.fromCharCode(108,95,57,52,95,116,112,105,100,0);
          let types0 = String.fromCharCode(117,110,109,97,116,99,104,101,100,95,97,95,50,54,0);
          let filee = 1.0;
         hooksM = `${subsn.length}`;
         types0 += `${types0.length - parseInt(`${filee}`)}`;
         filee /= Math.max(2, 1);
          let mutedi = String.fromCharCode(105,95,53,95,97,118,97,105,108,97,98,108,101,0);
         delegate_ws.push(subsn.length);
         mutedi = `${3 - mutedi.length}`;
      do {
         delegate_ws = [subsn.length];
         if (delegate_ws.length == 795818) {
            break;
         }
      } while ((4 <= (delegate_ws.length | 4) && 4 <= (hooksM.length | delegate_ws.length)) && (delegate_ws.length == 795818));
       let package_uzG = 5.0;
      while (!hooksM.includes(subsn)) {
         hooksM = `${hooksM.length}`;
         break;
      }
      malaysiam[viewerl] = (viewerl == String.fromCharCode(89,0) ? modec.length : viewerl.length);
       let playlistJ = 2.0;
       let mbbanner2 = [634, 280];
       let playr = new Map([[String.fromCharCode(122,95,55,55,95,104,113,100,110,100,0),true ], [String.fromCharCode(115,117,112,112,114,101,115,115,111,114,95,97,95,56,55,0),false ], [String.fromCharCode(108,111,103,102,110,95,103,95,57,48,0),true ]]);
          let privilegeW = String.fromCharCode(112,97,114,109,115,95,117,95,56,55,0);
          let mutedq = new Map([[String.fromCharCode(115,112,101,99,105,102,105,99,97,116,105,111,110,95,120,95,52,51,0),189], [String.fromCharCode(116,111,116,97,108,108,121,95,53,95,49,53,0),928], [String.fromCharCode(105,116,108,101,95,101,95,54,51,0),657]]);
         mbbanner2 = [(privilegeW == String.fromCharCode(86,0) ? mbbanner2.length : privilegeW.length)];
         mutedq = new Map([[`${mutedq.size}`, mutedq.size]]);
         playlistJ -= mbbanner2.length % 1;
      do {
         playr[`${mbbanner2.length}`] = mbbanner2.length;
         if (playr.size == 3302235) {
            break;
         }
      } while ((playr.size == 3302235) && ((1 * mbbanner2.length) <= 4 && 1 <= (mbbanner2.length * playr.size)));
          let halfF = String.fromCharCode(97,95,49,50,95,112,97,101,116,104,0);
         mbbanner2 = [2 ^ mbbanner2.length];
         halfF += `${halfF.length}`;
      while (!Array.from(playr.values()).includes(playlistJ)) {
          let nterstitialY = 0;
          let orientationC = String.fromCharCode(111,95,50,51,95,104,97,110,110,101,108,0);
          let mbridgeH = true;
          let chat4 = false;
         playr = new Map([[`${mbridgeH}`, nterstitialY]]);
         nterstitialY |= ((chat4 ? 3 : 3));
         orientationC = `${orientationC.length >> (Math.min(Math.abs(2), 5))}`;
         mbridgeH = orientationC.length == 77;
         break;
      }
      while (1 > (mbbanner2.length + parseInt(`${playlistJ}`)) || 1.41 > (mbbanner2.length + playlistJ)) {
          let recommendation_ = new Map([[String.fromCharCode(97,100,100,105,116,105,111,110,97,108,95,116,95,49,56,0),String.fromCharCode(99,95,51,49,95,116,105,108,101,115,0)], [String.fromCharCode(106,95,51,48,95,104,117,101,0),String.fromCharCode(109,111,100,101,95,54,95,57,0)], [String.fromCharCode(98,95,53,53,95,97,117,120,0),String.fromCharCode(105,100,102,118,95,109,95,53,56,0)]]);
          let mbbanner0 = 2;
          let completef = [416, 165, 635];
          let baidue = String.fromCharCode(114,111,120,121,95,103,95,50,56,0);
         mbbanner2 = [parseInt(`${playlistJ}`) - recommendation_.size];
         recommendation_ = new Map([[`${completef.length}`, completef.length ^ baidue.length]]);
         mbbanner0 /= Math.max(5, 3);
         baidue += `${completef.length}`;
         break;
      }
          let giftn = String.fromCharCode(99,111,111,108,100,111,119,110,95,102,95,52,48,0);
         mbbanner2 = [parseInt(`${playlistJ}`) << (Math.min(mbbanner2.length, 2))];
         giftn += `${giftn.length / (Math.max(4, giftn.length))}`;
      do {
         mbbanner2 = [playr.size];
         if (mbbanner2.length == 52889) {
            break;
         }
      } while ((mbbanner2.length == 52889) && (2 >= (mbbanner2.length >> (Math.min(Math.abs(playr.size), 3))) && (mbbanner2.length >> (Math.min(Math.abs(2), 3))) >= 1));
         mbbanner2.push(parseInt(`${playlistJ}`));
      holder5 /= Math.max(2, 2);
   do {
       let specB = true;
       let phonen = String.fromCharCode(112,109,107,95,56,95,54,51,0);
       let minir = 1.0;
       let mbjscommon7 = false;
       let adultb = false;
         phonen = "3";
         phonen = `${((specB ? 5 : 5) >> (Math.min(Math.abs((adultb ? 1 : 2)), 3)))}`;
         adultb = mbjscommon7 || adultb;
      if (phonen.includes(`${specB}`)) {
         phonen += `${phonen.length}`;
      }
      for (let j = 0; j < 3; j++) {
          let downloadi = String.fromCharCode(102,111,108,108,111,119,115,95,57,95,53,0);
          let routerp = 2;
          let styleg = String.fromCharCode(102,117,122,122,101,114,95,113,95,57,0);
         specB = downloadi.length > 30 || specB;
         downloadi = `${routerp >> (Math.min(Math.abs(2), 1))}`;
         routerp -= routerp - 3;
         styleg += `${(styleg == String.fromCharCode(69,0) ? styleg.length : routerp)}`;
      }
         adultb = phonen.length > 62;
         phonen += `${1 ^ parseInt(`${minir}`)}`;
         specB = phonen.length == 60;
      viewerl += `${1 - robotop.length}`;
      if (738578 == viewerl.length) {
         break;
      }
   } while ((viewerl.length > modec.length) && (738578 == viewerl.length));
   while (viewerl.startsWith(`${holder5}`)) {
      holder5 *= viewerl.length;
      break;
   }
      reactnativejsA |= math_.length - mbsplashb.length;

    return (
        <View>
            <MatchStatistic statisticData={liveRoomMatchUpdates?.football_match_stats?.stats?.filter(x => x.type != "team_id")}/>
        </View>
    )
}

export default MatchStatisticTab