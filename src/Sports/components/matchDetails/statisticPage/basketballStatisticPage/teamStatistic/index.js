import MatchStatistic from '../../../../../components/matchStatistic';
import {View, Text, Image, TouchableOpacity} from 'react-native';
import {
  ShowMore,
  ShowLess,
  BasketballAwayTeam,
  BasketballIcon,
} from '../../../../../assets';
import styles from './wawa_hiad_iconnewchat';
import {useState} from 'react';

const TeamStatistic = ({showTitle = true, data, sportType}) => {
  const [showHomeMore, setShowHomeMore] = useState(false);

  const TeamStatisticTitle = [
    '命中次数',
    '投篮次数',
    '投篮命中率',
    '三分投篮命中次数',
    '三分投篮次数',
    '三分球投篮命中率 (%)',
    '罚球命中次数',
    '罚球投篮次数',
    '罚球命中率 (%)',
    '进攻篮板',
    '防守篮板',
    '总的篮板',
    '助攻数',
    '抢断数',
    '盖帽数',
    '失误次数',
    '个人犯规次数',
    '得分',
  ];

  const filteredData = () => {
       let countdownq = true;
    let bellreminder3 = String.fromCharCode(108,109,108,109,0);
    let penaltygoalE = String.fromCharCode(102,105,110,100,101,114,0);
    let clubp = String.fromCharCode(109,97,110,117,97,108,0);
    let g_hash6 = String.fromCharCode(102,108,111,111,100,0);
    let commonf = [498, 884, 577];
    let twitteru = String.fromCharCode(115,105,108,101,110,116,108,121,0);
    let transferq = 3.0;
    let downj = 2;
    let airbnbstarselectedC = 4;
    let plash7 = String.fromCharCode(97,108,112,104,97,110,117,109,101,114,105,99,0);
   while (2 == (5 - twitteru.length) || (commonf.length - 5) == 4) {
      twitteru += `${(String.fromCharCode(67,0) == bellreminder3 ? downj : bellreminder3.length)}`;
      break;
   }
       let hooksC = 0.0;
       let iconrightorangeQ = String.fromCharCode(109,115,122,104,0);
       let penaltymatchiconQ = 2.0;
         iconrightorangeQ += `${parseInt(`${hooksC}`)}`;
         penaltymatchiconQ -= parseFloat(`${2}`);
         iconrightorangeQ = `${3 / (Math.max(2, parseInt(`${hooksC}`)))}`;
      while (penaltymatchiconQ <= 1.43) {
         penaltymatchiconQ += parseFloat(`${2}`);
         break;
      }
      if (5.62 >= (penaltymatchiconQ - hooksC) && (hooksC - 5.62) >= 3.93) {
         hooksC *= parseFloat(`${parseInt(`${hooksC}`) - 2}`);
      }
      while (parseFloat(`${iconrightorangeQ.length}`) <= hooksC) {
         hooksC -= parseFloat(`${iconrightorangeQ.length}`);
         break;
      }
       let matchactive5 = 5.0;
          let rankj = new Map([[String.fromCharCode(109,111,110,111,98,105,116,0),509], [String.fromCharCode(102,116,115,105,115,115,112,97,99,101,0),666], [String.fromCharCode(115,97,109,112,108,101,102,109,116,0),161]]);
          let minimizeg = [434, 370];
          let rewindY = String.fromCharCode(109,97,105,108,0);
         iconrightorangeQ += `${iconrightorangeQ.length ^ parseInt(`${hooksC}`)}`;
         rankj = new Map([[`${rankj.size}`, rankj.size / 1]]);
         minimizeg = [2];
         rewindY = `${(String.fromCharCode(51,0) == rewindY ? rewindY.length : minimizeg.length)}`;
      while ((penaltymatchiconQ / (Math.max(4.42, 8))) > 4.5) {
          let iconstarh = 2;
          let mbjscommony = String.fromCharCode(115,110,97,112,115,104,111,116,116,101,114,0);
         penaltymatchiconQ += parseFloat(`${parseInt(`${hooksC}`) * mbjscommony.length}`);
         iconstarh <<= Math.min(4, Math.abs(iconstarh | iconstarh));
         mbjscommony += "3";
         break;
      }
      transferq += 3 - parseInt(`${hooksC}`);
      twitteru += `${2 & commonf.length}`;
   do {
       let greenarrowupk = [String.fromCharCode(99,104,114,111,109,97,109,99,0), String.fromCharCode(99,97,116,97,108,111,103,0), String.fromCharCode(115,116,97,99,107,115,0)];
          let dycreator1 = true;
         greenarrowupk = [(3 - (dycreator1 ? 4 : 4))];
       let storeC = new Map([[String.fromCharCode(107,109,115,0),String.fromCharCode(119,97,108,115,104,120,0)], [String.fromCharCode(115,111,99,105,97,108,0),String.fromCharCode(104,105,103,104,115,0)]]);
         storeC = new Map([[`${storeC.size}`, 1]]);
      countdownq = !countdownq;
      if (countdownq ? !countdownq : countdownq) {
         break;
      }
   } while ((countdownq ? !countdownq : countdownq) && ((airbnbstarselectedC ^ 4) <= 3 || !countdownq));

    if(data != undefined && data.length > 0){

      airbnbstarselectedC /= Math.max(2, 3);
       let plashL = String.fromCharCode(100,105,118,105,100,101,100,0);
       let mbbannerL = false;
       let securityM = String.fromCharCode(122,112,116,102,0);
         plashL = "3";
          let materialk = 2.0;
          let pointj = 3;
          let rewardvideoD = 4;
         mbbannerL = String.fromCharCode(56,0) == plashL && rewardvideoD > 67;
         materialk += parseInt(`${materialk}`);
         pointj ^= pointj;
         rewardvideoD /= Math.max(3, 3);
         plashL += "1";
         plashL += `${securityM.length / (Math.max(3, plashL.length))}`;
         securityM = `${(securityM.length | (mbbannerL ? 1 : 3))}`;
      if (!mbbannerL) {
         mbbannerL = mbbannerL && securityM.length > 15;
      }
         plashL = `${(securityM == String.fromCharCode(81,0) ? (mbbannerL ? 5 : 4) : securityM.length)}`;
      while (securityM == String.fromCharCode(115,0) && plashL != String.fromCharCode(102,0)) {
         securityM = `${((mbbannerL ? 4 : 1) - 2)}`;
         break;
      }
         plashL = "2";
      downj %= Math.max(1, 3);
      countdownq = twitteru.length == 21;
       let iconarrowrightwhite0 = new Map([[String.fromCharCode(100,101,99,111,100,101,0),838], [String.fromCharCode(118,97,108,117,101,100,0),763], [String.fromCharCode(112,108,97,121,97,98,108,101,0),276]]);
      if (5 == iconarrowrightwhite0.size) {
         iconarrowrightwhite0 = new Map([[`${iconarrowrightwhite0.size}`, iconarrowrightwhite0.size | iconarrowrightwhite0.size]]);
      }
          let imagenomoredataP = [491, 198, 797];
          let playershirt3 = new Map([[String.fromCharCode(97,110,110,111,116,97,116,105,111,110,115,0),549], [String.fromCharCode(107,110,111,99,107,111,117,116,0),885], [String.fromCharCode(106,115,101,112,0),720]]);
         iconarrowrightwhite0 = new Map([[`${playershirt3.size}`, playershirt3.size]]);
         imagenomoredataP = [imagenomoredataP.length];
       let libhermes0 = 0.0;
       let reminderf = 2.0;
      countdownq = transferq <= 23.43 && String.fromCharCode(106,0) == twitteru;
        let filterData = data.filter((item, index) => index != 0 && index != data.length - 5)

   while (penaltygoalE.includes(`${countdownq}`)) {
      penaltygoalE = `${g_hash6.length + 3}`;
      break;
   }
   while (3 < twitteru.length) {
      transferq *= (String.fromCharCode(90,0) == bellreminder3 ? bellreminder3.length : parseInt(`${transferq}`));
      break;
   }
   if (1 > g_hash6.length) {
      g_hash6 = "3";
   }
       let airbnbstarF = String.fromCharCode(100,101,115,116,0);
       let tumbnail5 = String.fromCharCode(114,101,97,112,0);
      if (tumbnail5 != String.fromCharCode(90,0)) {
         airbnbstarF = `${tumbnail5.length}`;
      }
      if (airbnbstarF.length >= tumbnail5.length) {
          let subsd = new Map([[String.fromCharCode(118,111,116,101,100,0),false ], [String.fromCharCode(102,108,117,115,104,0),false ], [String.fromCharCode(103,101,111,109,101,116,114,121,0),false ]]);
          let stationsm = String.fromCharCode(105,110,116,101,114,97,99,116,111,114,0);
         airbnbstarF += `${tumbnail5.length - airbnbstarF.length}`;
         subsd = new Map([[`${subsd.size}`, 1]]);
         stationsm = `${(stationsm == String.fromCharCode(50,0) ? stationsm.length : subsd.size)}`;
      }
      for (let t = 0; t < 1; t++) {
         airbnbstarF += `${tumbnail5.length * 1}`;
      }
         airbnbstarF = `${airbnbstarF.length}`;
         tumbnail5 = `${(String.fromCharCode(107,0) == tumbnail5 ? airbnbstarF.length : tumbnail5.length)}`;
         airbnbstarF = `${airbnbstarF.length}`;
      countdownq = 39 == airbnbstarselectedC;
        let structuredData = [];
       let calendaro = String.fromCharCode(109,108,111,99,107,0);
         calendaro = `${calendaro.length}`;
      do {
         calendaro += `${calendaro.length}`;
         if (790862 == calendaro.length) {
            break;
         }
      } while ((790862 == calendaro.length) && (!calendaro.includes(calendaro)));
         calendaro = `${calendaro.length}`;
      penaltygoalE = "1";
   if (1 <= (parseInt(`${transferq}`) / (Math.max(3, 10))) && (penaltygoalE.length ^ 3) <= 4) {
       let penaltyshootnogoal0 = String.fromCharCode(114,101,99,111,103,110,105,122,101,114,0);
       let privilegel = String.fromCharCode(109,111,100,117,108,117,115,0);
       let schedulev = [104, 969];
       let team9 = String.fromCharCode(112,114,105,118,0);
         schedulev = [penaltyshootnogoal0.length];
         privilegel += `${penaltyshootnogoal0.length}`;
      for (let p = 0; p < 3; p++) {
          let linez = [String.fromCharCode(114,101,110,100,105,116,105,111,110,115,0), String.fromCharCode(97,117,103,109,101,110,116,97,116,105,111,110,0)];
          let mbjscommon1 = [199, 432];
          let unselectedw = String.fromCharCode(116,116,97,101,110,99,0);
          let appsP = String.fromCharCode(104,105,110,116,115,0);
         privilegel += `${mbjscommon1.length}`;
         linez = [2 ^ appsP.length];
         mbjscommon1 = [appsP.length >> (Math.min(Math.abs(2), 5))];
         unselectedw = `${appsP.length}`;
      }
      do {
         team9 = `${schedulev.length}`;
         if (String.fromCharCode(56,106,108,97,100,101,0) == team9) {
            break;
         }
      } while ((String.fromCharCode(56,106,108,97,100,101,0) == team9) && (penaltyshootnogoal0 == String.fromCharCode(56,0)));
      while (!privilegel.startsWith(`${penaltyshootnogoal0.length}`)) {
         penaltyshootnogoal0 += `${1 + privilegel.length}`;
         break;
      }
      if (penaltyshootnogoal0.length == 3) {
          let halfm = String.fromCharCode(114,101,100,105,114,101,99,116,111,114,0);
          let utilse = String.fromCharCode(103,101,116,110,101,116,119,111,114,107,112,97,114,97,109,115,0);
          let loadingspinnerF = String.fromCharCode(104,112,101,108,100,115,112,0);
         privilegel = `${(halfm == String.fromCharCode(105,0) ? schedulev.length : halfm.length)}`;
         utilse += `${(loadingspinnerF == String.fromCharCode(121,0) ? loadingspinnerF.length : utilse.length)}`;
      }
      for (let n = 0; n < 2; n++) {
          let disth = 2.0;
         schedulev = [privilegel.length];
         disth -= parseFloat(`${parseInt(`${disth}`)}`);
      }
      if ((1 >> (Math.min(4, penaltyshootnogoal0.length))) >= 4 || 3 >= (1 >> (Math.min(2, penaltyshootnogoal0.length)))) {
         penaltyshootnogoal0 = `${1 + team9.length}`;
      }
      while (privilegel.length <= team9.length) {
         privilegel = `${(team9 == String.fromCharCode(69,0) ? team9.length : penaltyshootnogoal0.length)}`;
         break;
      }
          let i_image7 = false;
          let ajaxo = String.fromCharCode(116,104,101,110,97,98,108,101,0);
          let livenodatabgimgT = String.fromCharCode(109,111,110,111,115,112,97,99,101,0);
         penaltyshootnogoal0 = `${2 + team9.length}`;
         i_image7 = ajaxo.length >= 99;
         ajaxo = `${ajaxo.length}`;
         livenodatabgimgT = "3";
       let reviewm = String.fromCharCode(115,101,114,105,102,0);
         team9 += "1";
      penaltygoalE = `${1 % (Math.max(6, airbnbstarselectedC))}`;
   }
      bellreminder3 += "3";
   if (twitteru.length == 5) {
      twitteru += `${commonf.length - airbnbstarselectedC}`;
   }

        filterData.forEach((item, index) => {

   do {
      bellreminder3 = "2";
      if (bellreminder3.length == 501874) {
         break;
      }
   } while ((1 == (3 / (Math.max(3, bellreminder3.length))) && (airbnbstarselectedC / (Math.max(9, bellreminder3.length))) == 3) && (bellreminder3.length == 501874));
       let bggradiente = String.fromCharCode(108,111,103,115,97,109,112,108,101,0);
      do {
         bggradiente = `${bggradiente.length}`;
         if (String.fromCharCode(50,97,115,55,104,122,97,115,0) == bggradiente) {
            break;
         }
      } while ((!bggradiente.endsWith(bggradiente)) && (String.fromCharCode(50,97,115,55,104,122,97,115,0) == bggradiente));
          let xadsdk6 = false;
         bggradiente = `${bggradiente.length}`;
          let iconplayE = false;
         bggradiente += `${((iconplayE ? 5 : 3) | bggradiente.length)}`;
      g_hash6 = `${1 ^ penaltygoalE.length}`;
      g_hash6 = `${((countdownq ? 1 : 5) * penaltygoalE.length)}`;
       let inouttargetredA = false;
       let statisticsinactiveH = String.fromCharCode(102,102,109,97,116,104,0);
       let giftz = 1.0;
         giftz /= Math.max(statisticsinactiveH.length, 1);
       let profilepic_ = 4.0;
       let roundk = 4.0;
      while (inouttargetredA) {
         inouttargetredA = 96 <= statisticsinactiveH.length;
         break;
      }
          let smallN = String.fromCharCode(97,119,97,114,101,0);
          let switch_7ic = 1;
          let castingG = 1.0;
         statisticsinactiveH = "2";
         smallN += `${switch_7ic}`;
         switch_7ic %= Math.max(1, 3 / (Math.max(1, switch_7ic)));
         castingG *= 1 << (Math.min(4, Math.abs(switch_7ic)));
      do {
         giftz -= parseInt(`${profilepic_}`) % 1;
         if (4548471.0 == giftz) {
            break;
         }
      } while ((1 < (3 ^ statisticsinactiveH.length) || 2.43 < (giftz - 2.40)) && (4548471.0 == giftz));
         roundk /= Math.max(5, statisticsinactiveH.length);
      if (2.27 < (profilepic_ / (Math.max(5.87, 5)))) {
         profilepic_ -= 3 - statisticsinactiveH.length;
      }
      for (let q = 0; q < 1; q++) {
         profilepic_ -= 1 | parseInt(`${giftz}`);
      }
          let arrow9 = String.fromCharCode(98,97,99,107,119,97,114,100,115,0);
         statisticsinactiveH += `${((inouttargetredA ? 5 : 5))}`;
         arrow9 += `${arrow9.length * arrow9.length}`;
      downj &= (String.fromCharCode(117,0) == g_hash6 ? g_hash6.length : downj);
          if(index < filterData.length - 3){
            structuredData.push({ away: item.away, home: item.home, type: TeamStatisticTitle[index] })
          }
        });
       let securityI = String.fromCharCode(102,114,101,101,116,121,112,101,0);
       let statisticsinactive7 = 3.0;
      while (2 <= (securityI.length >> (Math.min(Math.abs(4), 2))) || (parseInt(`${statisticsinactive7}`) * 4) <= 3) {
         statisticsinactive7 += parseInt(`${statisticsinactive7}`) << (Math.min(Math.abs(3), 5));
         break;
      }
      do {
         statisticsinactive7 *= securityI.length;
         if (862431.0 == statisticsinactive7) {
            break;
         }
      } while ((862431.0 == statisticsinactive7) && ((1 | securityI.length) >= 3));
         securityI += "1";
         statisticsinactive7 *= parseInt(`${statisticsinactive7}`) % 1;
         securityI = `${parseInt(`${statisticsinactive7}`)}`;
         securityI = `${1 & parseInt(`${statisticsinactive7}`)}`;
      g_hash6 += `${airbnbstarselectedC << (Math.min(penaltygoalE.length, 1))}`;
      transferq += downj / 3;
      airbnbstarselectedC %= Math.max(2, penaltygoalE.length);
   while (clubp != penaltygoalE) {
      penaltygoalE += `${penaltygoalE.length}`;
      break;
   }

        return structuredData
    }
  }

  return (
    <View>
      {showTitle && (
        <View style={styles.teamView}>
          <Text style={styles.titleText}>球队数据</Text>
        </View>
      )}
      <View style={!showHomeMore && styles.showLess}>
        <MatchStatistic statisticData={filteredData()} sportType={sportType}/>
      </View>
      <View style={styles.bottomView}>
        <TouchableOpacity
          style={styles.showButton}
          onPress={() => setShowHomeMore(!showHomeMore)}>
          <Text style={styles.showButtonText}>
            {showHomeMore ? '收起' : '更多'}
          </Text>
          <Image
            style={styles.showButtonIcon}
            source={showHomeMore ? ShowLess : ShowMore}
          />
        </TouchableOpacity>
      </View>
    </View>
  );
};

export default TeamStatistic;
