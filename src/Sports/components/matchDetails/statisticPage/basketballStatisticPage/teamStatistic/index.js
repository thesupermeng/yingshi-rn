import MatchStatistic from '../../../../../components/matchStatistic';
import {View, Text, Image, TouchableOpacity} from 'react-native';
import {
  ShowMore,
  ShowLess,
  BasketballAwayTeam,
  BasketballIcon,
} from '../../../../../assets';
import styles from './yys_nbatrophy';
import {useState} from 'react';

const TeamStatistic = ({showTitle = true, data, sportType}) => {
  const [showHomeMore, setShowHomeMore] = useState(false);

  const TeamStatisticTitle = [
    '命中次数',
    '投篮次数',
    '投篮命中率',
    '三分投篮命中次数',
    '三分投篮次数',
    '三分球投篮命中率 (%)',
    '罚球命中次数',
    '罚球投篮次数',
    '罚球命中率 (%)',
    '进攻篮板',
    '防守篮板',
    '总的篮板',
    '助攻数',
    '抢断数',
    '盖帽数',
    '失误次数',
    '个人犯规次数',
    '得分',
  ];

  const filteredData = () => {
       let basketballdetailsbgh = String.fromCharCode(112,114,101,101,110,99,111,100,101,95,118,95,57,49,0);
    let colorsE = String.fromCharCode(111,95,52,52,95,105,110,99,108,117,115,105,111,110,115,0);
    let defaultprofilepicL = 4.0;
    let turndownG = 3.0;
    let x_unlockq = true;
    let event0 = 0.0;
    let orangedownarrowu = String.fromCharCode(102,95,53,48,95,108,111,116,116,105,101,118,105,101,119,0);
    let bgvipxvodu = String.fromCharCode(112,117,115,104,101,100,95,102,95,54,50,0);
    let libapminsightam = String.fromCharCode(104,97,114,100,119,97,114,101,95,99,95,56,50,0);
    let yellowtored0 = 0;
    let subbasketballplayer4 = 5;
    let termsK = [941, 261, 340];
   do {
       let targetJ = 5.0;
      do {
          let trashv = 3;
         targetJ += parseFloat(`${3 * parseInt(`${targetJ}`)}`);
         trashv <<= Math.min(Math.abs(trashv), 2);
         if (2496297.0 == targetJ) {
            break;
         }
      } while ((1.4 > (targetJ - targetJ) || 2.26 > (targetJ - 1.4)) && (2496297.0 == targetJ));
      while ((targetJ / 2) > 2.72) {
         targetJ *= parseFloat(`${2}`);
         break;
      }
      for (let t = 0; t < 1; t++) {
          let main_gi = String.fromCharCode(99,100,108,109,115,95,56,95,51,0);
          let alertK = new Map([[String.fromCharCode(121,95,51,48,95,98,114,101,97,107,111,117,116,0),68], [String.fromCharCode(99,97,110,111,110,95,111,95,55,48,0),810]]);
          let countdownN = [912, 762];
          let playV = 0;
          let castingI = true;
         targetJ *= parseFloat(`${countdownN.length}`);
         main_gi += "3";
         alertK[main_gi] = main_gi.length;
         countdownN.push(1 << (Math.min(5, Math.abs(alertK.size))));
         playV >>= Math.min(2, Math.abs(3));
         castingI = 100 == alertK.size || playV == 100;
      }
      colorsE = `${colorsE.length & 1}`;
      if (colorsE == String.fromCharCode(115,120,97,113,100,52,50,119,118,0)) {
         break;
      }
   } while ((!colorsE.startsWith(`${defaultprofilepicL}`)) && (colorsE == String.fromCharCode(115,120,97,113,100,52,50,119,118,0)));
       let vipsportZ = String.fromCharCode(113,117,111,116,101,95,111,95,53,56,0);
       let holderZ = String.fromCharCode(103,95,53,56,95,116,97,98,108,101,112,114,105,110,116,0);
      for (let c = 0; c < 2; c++) {
         vipsportZ = "3";
      }
      while (vipsportZ == holderZ) {
         holderZ += `${vipsportZ.length & 3}`;
         break;
      }
      while (2 <= holderZ.length) {
         vipsportZ += `${(vipsportZ == String.fromCharCode(82,0) ? vipsportZ.length : holderZ.length)}`;
         break;
      }
          let imageactionliveJ = String.fromCharCode(99,111,114,101,105,109,97,103,101,95,110,95,50,0);
          let libreactE = String.fromCharCode(98,95,51,57,95,105,110,116,115,0);
          let screen8 = String.fromCharCode(113,95,49,48,95,105,110,105,116,118,0);
         vipsportZ = "1";
         imageactionliveJ += `${libreactE.length}`;
         libreactE = `${imageactionliveJ.length * 3}`;
         screen8 = `${libreactE.length}`;
      if (4 == vipsportZ.length) {
         vipsportZ = `${vipsportZ.length + 2}`;
      }
      while (vipsportZ.length >= holderZ.length) {
         vipsportZ = `${holderZ.length % (Math.max(vipsportZ.length, 6))}`;
         break;
      }
      turndownG /= Math.max(((x_unlockq ? 5 : 1) >> (Math.min(Math.abs(parseInt(`${defaultprofilepicL}`)), 1))), 5);
   if (x_unlockq) {
      x_unlockq = 41 == basketballdetailsbgh.length;
   }

    if(data != undefined && data.length > 0){

   do {
      basketballdetailsbgh = `${bgvipxvodu.length << (Math.min(Math.abs(2), 5))}`;
      if (3411515 == basketballdetailsbgh.length) {
         break;
      }
   } while ((basketballdetailsbgh.length == 4) && (3411515 == basketballdetailsbgh.length));
   do {
       let libglog2 = true;
         libglog2 = (!libglog2 ? libglog2 : libglog2);
      while (!libglog2) {
         libglog2 = !libglog2;
         break;
      }
      if (libglog2 || !libglog2) {
         libglog2 = !libglog2;
      }
      defaultprofilepicL /= Math.max(parseFloat(`${parseInt(`${defaultprofilepicL}`) | parseInt(`${turndownG}`)}`), 4);
      if (1057191.0 == defaultprofilepicL) {
         break;
      }
   } while ((5.61 > (defaultprofilepicL * 1.61) && 2 > (basketballdetailsbgh.length * parseInt(`${defaultprofilepicL}`))) && (1057191.0 == defaultprofilepicL));
      x_unlockq = 79.39 > defaultprofilepicL && 79.39 > event0;
        let filterData = data.filter((item, index) => index != 0 && index != data.length - 5)

       let termsF = true;
       let suggestionY = [989, 677, 49];
          let videojss = new Map([[String.fromCharCode(114,101,100,115,112,97,114,107,95,106,95,51,50,0),360], [String.fromCharCode(115,116,97,109,112,95,114,95,49,57,0),444], [String.fromCharCode(109,95,50,95,100,101,114,105,118,101,0),725]]);
          let volumeM = 4.0;
          let videobufferloadingM = 0.0;
         termsF = suggestionY.includes(volumeM);
         videojss[`${videobufferloadingM}`] = videojss.size;
         volumeM -= parseInt(`${videobufferloadingM}`) << (Math.min(Math.abs(1), 1));
      for (let r = 0; r < 3; r++) {
         suggestionY = [suggestionY.length];
      }
      for (let w = 0; w < 1; w++) {
         termsF = (71 >= ((!termsF ? 71 : suggestionY.length) % (Math.max(suggestionY.length, 8))));
      }
      while ((suggestionY.length << (Math.min(Math.abs(5), 1))) < 2) {
          let shareO = true;
         termsF = suggestionY.length < 6 || !shareO;
         break;
      }
      if (termsF) {
         termsF = (76 > (suggestionY.length + (termsF ? 76 : suggestionY.length)));
      }
         suggestionY = [3 | suggestionY.length];
      basketballdetailsbgh = `${((x_unlockq ? 1 : 3))}`;
   while (orangedownarrowu != basketballdetailsbgh) {
      basketballdetailsbgh = "1";
      break;
   }
      colorsE += `${2 / (Math.max(parseInt(`${defaultprofilepicL}`), 7))}`;
        let structuredData = [];
       let icondownimgx = 3.0;
       let logouserc = 0.0;
       let securityD = [239, 985, 564];
         logouserc += securityD.length;
      if (4.52 > (logouserc * icondownimgx) || 2.35 > (4.52 * icondownimgx)) {
         icondownimgx += parseFloat(`${parseInt(`${logouserc}`)}`);
      }
      while ((1.74 * logouserc) <= 5.48 || 1.74 <= (logouserc * icondownimgx)) {
         logouserc /= Math.max(3 / (Math.max(2, parseInt(`${logouserc}`))), 3);
         break;
      }
      if (3.71 < (logouserc * 5.35) || 5.35 < (securityD.length * logouserc)) {
         securityD = [securityD.length];
      }
       let description_4ws = String.fromCharCode(97,95,54,51,95,115,98,119,97,105,116,0);
       let sendZ = String.fromCharCode(101,103,114,101,115,115,95,102,95,49,51,0);
      for (let n = 0; n < 1; n++) {
         securityD.push(sendZ.length);
      }
      while (2 == securityD.length) {
          let sound7 = String.fromCharCode(109,105,110,105,109,105,122,101,97,98,108,101,95,100,95,52,53,0);
          let playlistc = new Map([[String.fromCharCode(118,95,49,95,103,101,116,110,97,109,101,105,110,102,111,0),String.fromCharCode(121,101,116,95,112,95,52,56,0)], [String.fromCharCode(117,95,52,51,95,116,103,115,0),String.fromCharCode(114,95,55,50,95,115,116,114,105,107,101,116,104,114,111,117,103,104,0)]]);
          let libtobh = String.fromCharCode(98,108,111,98,115,95,116,95,57,55,0);
          let main_mx = false;
          let file6 = false;
         logouserc *= 2;
         sound7 = `${playlistc.size >> (Math.min(libtobh.length, 4))}`;
         playlistc = new Map([[`${file6}`, (sound7.length + (file6 ? 1 : 1))]]);
         libtobh = `${libtobh.length >> (Math.min(Math.abs(2), 2))}`;
         main_mx = (playlistc.size >> (Math.min(sound7.length, 3))) < 33;
         break;
      }
         securityD = [1 ^ parseInt(`${logouserc}`)];
       let whitei = 0.0;
       let liveshare8 = 1.0;
      orangedownarrowu = `${3 % (Math.max(5, bgvipxvodu.length))}`;
   do {
      colorsE = "3";
      if (String.fromCharCode(104,112,118,110,0) == colorsE) {
         break;
      }
   } while ((String.fromCharCode(104,112,118,110,0) == colorsE) && ((turndownG * colorsE.length) == 5.13 || (turndownG * colorsE.length) == 5.13));
      defaultprofilepicL -= parseFloat(`${bgvipxvodu.length >> (Math.min(colorsE.length, 5))}`);

        filterData.forEach((item, index) => {

      defaultprofilepicL *= parseFloat(`${parseInt(`${event0}`)}`);
   do {
       let zoom4 = false;
         zoom4 = (!zoom4 ? zoom4 : !zoom4);
         zoom4 = zoom4 || zoom4;
      do {
         zoom4 = zoom4 && zoom4;
         if (zoom4 ? !zoom4 : zoom4) {
            break;
         }
      } while ((zoom4 ? !zoom4 : zoom4) && (zoom4 && zoom4));
      orangedownarrowu = `${((x_unlockq ? 5 : 1) << (Math.min(Math.abs(3), 4)))}`;
      if (String.fromCharCode(115,54,53,108,110,105,0) == orangedownarrowu) {
         break;
      }
   } while (((orangedownarrowu.length << (Math.min(Math.abs(3), 3))) == 4) && (String.fromCharCode(115,54,53,108,110,105,0) == orangedownarrowu));
   do {
      defaultprofilepicL += (parseFloat(`${bgvipxvodu == String.fromCharCode(113,0) ? (x_unlockq ? 1 : 2) : bgvipxvodu.length}`));
      if (4234235.0 == defaultprofilepicL) {
         break;
      }
   } while ((4234235.0 == defaultprofilepicL) && (4 > basketballdetailsbgh.length));
          if(index < filterData.length - 3){
            structuredData.push({ away: item.away, home: item.home, type: TeamStatisticTitle[index] })
          }
        });
      orangedownarrowu = `${(colorsE == String.fromCharCode(107,0) ? colorsE.length : yellowtored0)}`;
   while ((turndownG - 4.3) > 4.23 && (turndownG - 4.3) > 2.0) {
      turndownG *= bgvipxvodu.length;
      break;
   }
   while (basketballdetailsbgh.endsWith(`${subbasketballplayer4}`)) {
      basketballdetailsbgh = `${orangedownarrowu.length >> (Math.min(3, colorsE.length))}`;
      break;
   }

        return structuredData
    }
  }

  return (
    <View>
      {showTitle && (
        <View style={styles.teamView}>
          <Text style={styles.titleText}>球队数据</Text>
        </View>
      )}
      <View style={!showHomeMore && styles.showLess}>
        <MatchStatistic statisticData={filteredData()} sportType={sportType}/>
      </View>
      <View style={styles.bottomView}>
        <TouchableOpacity
          style={styles.showButton}
          onPress={() => setShowHomeMore(!showHomeMore)}>
          <Text style={styles.showButtonText}>
            {showHomeMore ? '收起' : '更多'}
          </Text>
          <Image
            style={styles.showButtonIcon}
            source={showHomeMore ? ShowLess : ShowMore}
          />
        </TouchableOpacity>
      </View>
    </View>
  );
};

export default TeamStatistic;
