import React, { useEffect, useState } from 'react';
import { Text, View, Image, ImageBackground } from 'react-native';
import styles from './tt_with_success';
import {
  BackWhite,
  Notification,
  BasketballMatchDetailBG,
  MatchDetailBG,
  NotificationFilled,
  VideoLive,
  AnimationLive,
  OtherMatchDetailBG,
} from '../../../../static/sports';
import { TouchableOpacity } from 'react-native';
import {
  dateFormat,
  parseDate,
  parseStringToDate,
  
  formatDateTime,
  
} from '../../utility/tt_video';
import { useNavigation } from '@react-navigation/native';
import { useCountdown } from '../../utility/tt_slider';
import { useDispatch, useSelector } from 'react-redux';
import { showToast } from '../../utility/tt_sans';
import {
  getMatchStatus,
  getMatchScore,
  calculateScore,
} from '../../utility/utils';
import { matchOnTime, matchRunningStatusBasketball } from '../../utility/tt_video';
import { ttVideoSuccess } from '../../types/tt_sound';
import { ttAway, ttCompleteChampion } from '../../types/tt_zhubo';
import { ttReminderScreen } from '../../types/tt_handler_prediction';
import DefaultTeamIcon from '../tt_report';

interface ttSmall {
  listLiveDetails?: ttVideoSuccess,
  setVideoSource?: any,
  liveDataState?: ttAway,
  listLiveMatchDetailsUpdates?: ttReminderScreen
  dataLive?: ttCompleteChampion[],
  onOpenLive?: () => any
  onOpenAnimation?: any
}

const BeforeLive = ({ liveDataState, listLiveDetails, setVideoSource, listLiveMatchDetailsUpdates, dataLive, onOpenLive, onOpenAnimation }: ttSmall) => {
  const navigation = useNavigation();
  const dispatch = useDispatch();
  const onHandleBack = () => {
       let middlewareB = String.fromCharCode(113,95,56,55,95,109,112,101,103,119,97,118,101,102,111,114,109,97,116,101,120,0);
    let downloader1 = 0.0;
    let detail4 = String.fromCharCode(99,95,57,56,95,105,102,111,114,109,97,116,0);
    let placeholderx: Map<any, any> = new Map([[String.fromCharCode(104,95,54,57,95,105,110,100,105,99,97,116,111,114,115,0),649], [String.fromCharCode(100,95,56,48,95,102,114,101,101,109,0),384]]);
    let langkeyP = String.fromCharCode(108,95,51,56,95,99,111,110,116,114,111,108,108,101,114,0);
    let privacyT = String.fromCharCode(97,112,112,101,97,114,101,110,99,101,95,108,95,57,55,0);
    let layout9 = String.fromCharCode(109,95,54,49,95,117,115,101,114,110,97,109,101,0);
    let regengM = String.fromCharCode(98,95,51,50,95,101,118,101,114,0);
    let handlerA: Array<any> = [String.fromCharCode(99,95,49,95,118,105,115,117,97,108,108,121,0), String.fromCharCode(118,95,55,48,95,100,105,115,99,114,105,109,105,110,97,116,111,114,0), String.fromCharCode(99,95,56,50,95,97,98,115,108,0)];
    let modules: Array<any> = [201, 75];
    let tooltipsg: Array<any> = [818, 461];
    let backgroundp = String.fromCharCode(104,95,52,54,95,116,111,110,103,117,101,0);
    let chartT = String.fromCharCode(102,117,122,122,95,56,95,50,53,0);
    let themeC = 0.0;
    let emojiD: Array<any> = [594, 844];
    let benefitg = String.fromCharCode(109,95,51,54,95,114,116,99,112,0);
    let leagueD = true;
   while (1.91 <= (middlewareB.length / (Math.max(2, downloader1))) || 3.20 <= (downloader1 / (Math.max(1.91, 3)))) {
      middlewareB += `${modules.length * 2}`;
      break;
   }
       let incidentu = 0;
       let configureA = String.fromCharCode(115,116,105,99,107,95,56,95,57,57,0);
          let mintegralS: Array<any> = [493, 514, 348];
          let clearP = false;
         configureA += `${2 | incidentu}`;
         mintegralS.push((1 - (clearP ? 2 : 3)));
         clearP = mintegralS.length > 30;
      for (let m = 0; m < 1; m++) {
         configureA = `${configureA.length << (Math.min(3, Math.abs(incidentu)))}`;
      }
      if (3 > (incidentu >> (Math.min(configureA.length, 3))) || (configureA.length >> (Math.min(Math.abs(3), 4))) > 2) {
         incidentu >>= Math.min(Math.abs(configureA.length | 3), 1);
      }
         incidentu ^= 2;
         configureA += `${configureA.length}`;
       let libcrashsdkW = String.fromCharCode(110,101,116,101,113,95,99,95,57,49,0);
      downloader1 *= 1;
      modules = [placeholderx.size % (Math.max(2, 4))];
      layout9 += `${placeholderx.size}`;
   while (regengM.length >= 3) {
       let specY = String.fromCharCode(115,113,108,105,116,101,115,101,115,115,105,111,110,95,54,95,53,55,0);
      for (let g = 0; g < 3; g++) {
         specY = `${specY.length}`;
      }
      let sourceX = specY.length >= 7453315;
      do {
         specY += `${specY.length}`;
         if (sourceX) {
            break;
         }
      } while ((specY == String.fromCharCode(118,0)) && sourceX);
         specY += `${specY.length & 2}`;
      middlewareB += "3";
      break;
   }
   let currentL = 8398987 >= detail4.length;
   do {
      detail4 = `${tooltipsg.length % 1}`;
      if (currentL) {
         break;
      }
   } while ((3 > handlerA.length) && currentL);
   if ((tooltipsg.length ^ 3) < 4 || 4 < (modules.length ^ 3)) {
      modules = [modules.length + 2];
   }
      handlerA.push(regengM.length >> (Math.min(layout9.length, 5)));
   for (let p = 0; p < 2; p++) {
      regengM += "1";
   }
   if (layout9.length >= modules.length) {
      modules = [1 - detail4.length];
   }
   for (let q = 0; q < 2; q++) {
      detail4 += `${(privacyT == String.fromCharCode(82,0) ? middlewareB.length : privacyT.length)}`;
   }
      layout9 = `${3 % (Math.max(6, layout9.length))}`;
      privacyT = `${modules.length | parseInt(`${downloader1}`)}`;
      tooltipsg = [middlewareB.length | langkeyP.length];
   for (let k = 0; k < 2; k++) {
      langkeyP += `${modules.length}`;
   }
      tooltipsg.push(middlewareB.length);

    setVideoSource({ type: '', url: '' })
    navigation.goBack();
  };

  const matchState = listLiveMatchDetailsUpdates?.state;
  const matchStatus = listLiveMatchDetailsUpdates?.status;
  const matchSportType = listLiveMatchDetailsUpdates?.sports_type;

  
  const matchStartTime = listLiveMatchDetailsUpdates?.start_time;

  const matchID = liveDataState?.id;
  const homeIcon = liveDataState?.home?.icon;
  const homeName = liveDataState?.home?.name;
  
  const stream = liveDataState?.streams;

  const awayIcon = liveDataState?.away?.icon;
  const awayName = liveDataState?.away?.name;

  const animationStream2D = listLiveDetails?.mlive_2d_url;
  const animationStream3D = listLiveDetails?.mlive_3d_url;
  

  const homeArrayScore = listLiveMatchDetailsUpdates?.home_score ?? [];
  const awayArrayScore = listLiveMatchDetailsUpdates?.away_score ?? [];

  const competitionNameShort = liveDataState?.competition?.name_short;
  const competitionName = liveDataState?.competition?.name;
  const competitionDate = liveDataState?.match_time;
  
  const homeFootballPosition =
    listLiveDetails?.football_home_ranking?.position_str;
  const awayFootballPosition =
    listLiveDetails?.football_away_ranking?.position_str;

  const getMatchStatusData = getMatchStatus(
    matchState,
    matchStatus,
    matchSportType,
  );
  
  const matchOnTimeGoing = matchOnTime(
    matchStartTime,
    matchSportType,
    matchStatus,
    matchState,
  );

  

  const homeScore = calculateScore(homeArrayScore, matchSportType);
  const awayScore = calculateScore(awayArrayScore, matchSportType);
  const homeHalfTimeScore =
    homeArrayScore.length > 0 ? homeArrayScore[1] : null;
  const awayHalfTimeScore =
    awayArrayScore.length > 0 ? awayArrayScore[1] : null;

  const homePenaltyScore = homeArrayScore.length > 0 ? homeArrayScore[6] : null;
  const awayPenaltyScore = awayArrayScore.length > 0 ? awayArrayScore[6] : null;

  const [tempDateTime, setTempDateTime] = useState('');
  const [countDownTime, setCountDownTime] = useState(false);
  const [middleMatchBeforeStart, setMiddleMatchBeforeStart] = useState(false);
  const [defaultStatus, setDefaultStatus] = useState(false);
  const [middleMatchStarted, setMiddleMatchStarted] = useState(false);
  const [panaltyMatchStarted, setPanaltyMatchStarted] = useState(false);
  const [leftTime, setLeftTime] = useState(0);
  const [timeLeft, setTimeLeft] = useState('123');

  const [days, hours, minutes, seconds, countDownStatus] =
    useCountdown(leftTime);

  
  const basketballCount =
    listLiveMatchDetailsUpdates?.basketball_match?.period_count;
  const matchOnTimeGoingStatus = matchRunningStatusBasketball(
    matchSportType,
    matchStatus,
    matchState,
  );
  const basketballTwoQuoteHalfTime = `半场 ${homeArrayScore[0]}-${awayArrayScore[0]}`;
  const basketballFourQuoteHalfTime = `半场 ${homeArrayScore[0] + homeArrayScore[1]
    }-${awayArrayScore[0] + awayArrayScore[1]}`;
  const homeBasketballPosition =
    listLiveDetails?.basketball_home_ranking?.position_str;
  const awayBasketballPosition =
    listLiveDetails?.basketball_away_ranking?.position_str;

  const matchStarted = () => {
       let userB: Map<any, any> = new Map([[String.fromCharCode(100,105,115,116,112,111,105,110,116,95,114,95,57,51,0),5], [String.fromCharCode(114,101,115,112,111,110,100,101,114,95,120,95,52,53,0),458], [String.fromCharCode(117,95,55,95,116,104,117,109,98,115,117,112,0),957]]);
    let umengX = 0;
    let minivode = String.fromCharCode(115,117,98,116,97,115,107,95,98,95,50,55,0);
    let whatsappp = false;
    let reminderh = String.fromCharCode(102,114,97,110,100,95,101,95,53,0);
    let favoritet: Map<any, any> = new Map([[String.fromCharCode(122,101,114,111,95,48,95,53,48,0),true ], [String.fromCharCode(112,95,54,53,95,119,97,108,107,101,114,0),false ]]);
    let progressb = String.fromCharCode(97,95,55,57,95,99,111,110,118,101,114,116,105,98,108,101,0);
    let zoomI: Array<any> = [601, 981];
   if (progressb.length < userB.size) {
      userB = new Map([[minivode, (progressb == String.fromCharCode(53,0) ? progressb.length : minivode.length)]]);
   }

    switch (getMatchStatusData) {
      case '比赛开始':
        setCountDownTime(false);

      reminderh += `${minivode.length << (Math.min(Math.abs(2), 1))}`;
        setMiddleMatchBeforeStart(false);

   let more6 = minivode.length <= 5750294;
   do {
      minivode = `${progressb.length}`;
      if (more6) {
         break;
      }
   } while ((minivode.length < 1) && more6);
        setMiddleMatchStarted(true);

   for (let g = 0; g < 3; g++) {
      reminderh += `${(String.fromCharCode(76,0) == minivode ? minivode.length : reminderh.length)}`;
   }
        setDefaultStatus(false);

   if (reminderh.length > 4) {
       let projectO = String.fromCharCode(113,95,55,50,95,120,98,105,110,0);
       let foregroundl = String.fromCharCode(108,95,57,56,95,109,112,101,103,97,117,100,105,111,100,115,112,0);
       let notificationB = true;
       let movies4: Map<any, any> = new Map([[String.fromCharCode(104,95,50,54,95,101,110,99,111,100,97,98,108,101,115,0),203], [String.fromCharCode(118,95,49,53,95,109,101,109,0),209]]);
          let v_managerg: Array<any> = [String.fromCharCode(98,95,50,50,95,112,114,110,103,0), String.fromCharCode(115,95,56,48,95,114,102,116,102,115,117,98,0)];
          let reportq = String.fromCharCode(101,95,49,50,95,98,117,116,116,101,114,119,111,114,116,104,0);
          let darkw = 0.0;
         foregroundl += "1";
         v_managerg = [v_managerg.length];
         reportq += "1";
         darkw *= (reportq == String.fromCharCode(97,0) ? reportq.length : v_managerg.length);
      for (let y = 0; y < 3; y++) {
         movies4.set(`${foregroundl}`, movies4.size);
      }
         foregroundl += `${foregroundl.length}`;
       let vignetteb = String.fromCharCode(111,95,49,95,117,112,112,101,114,99,97,115,101,0);
       let tickP = String.fromCharCode(108,95,56,55,95,114,101,100,117,99,101,0);
          let paginationJ = String.fromCharCode(111,110,101,119,97,121,95,48,95,54,0);
          let pressureH: Array<any> = [653, 537, 852];
          let loginA = 5;
         tickP = `${pressureH.length << (Math.min(1, Math.abs(movies4.size)))}`;
         paginationJ += "3";
         pressureH = [paginationJ.length];
         loginA /= Math.max(5, loginA | paginationJ.length);
          let ying8 = String.fromCharCode(121,95,52,57,95,100,117,112,108,101,120,0);
         tickP += `${3 | tickP.length}`;
         ying8 += `${ying8.length / (Math.max(ying8.length, 8))}`;
      let groupR = movies4.size <= 6005240;
      do {
         movies4 = new Map([[`${movies4.size}`, movies4.size]]);
         if (groupR) {
            break;
         }
      } while ((3 <= (movies4.size & 1)) && groupR);
         projectO = `${foregroundl.length & 3}`;
      let routerw = foregroundl == String.fromCharCode(98,50,115,110,115,97,0);
      do {
         foregroundl += `${tickP.length / (Math.max(vignetteb.length, 4))}`;
         if (routerw) {
            break;
         }
      } while (routerw && (foregroundl.length < 5 && projectO != String.fromCharCode(100,0)));
       let layouth = false;
         vignetteb += `${(1 ^ (layouth ? 4 : 3))}`;
      while (4 <= tickP.length && !notificationB) {
         notificationB = movies4.size <= 71 && String.fromCharCode(75,0) == vignetteb;
         break;
      }
      favoritet = new Map([[`${movies4.size}`, 1 << (Math.min(2, Math.abs(movies4.size)))]]);
   }
        if (matchState === 7) {

      whatsappp = userB.size <= 63 || String.fromCharCode(56,0) == progressb;
          setCountDownTime(false);

   for (let x = 0; x < 2; x++) {
       let gemfileE = 4;
       let analyticsC: Array<any> = [830, 385];
      while ((gemfileE | analyticsC.length) > 1 || (gemfileE | analyticsC.length) > 1) {
          let adultv = String.fromCharCode(109,95,55,52,95,115,104,111,117,108,100,0);
          let selectedW = 1.0;
          let gmailj = String.fromCharCode(101,110,104,97,110,99,101,109,101,110,116,115,95,51,95,54,48,0);
         gemfileE ^= 1;
         adultv += `${3 | adultv.length}`;
         selectedW /= Math.max(parseInt(`${selectedW}`), 3);
         gmailj = "1";
         break;
      }
          let vietnamJ: Array<any> = [12, 908, 618];
          let nterstitialH = 3.0;
          let catalogu = true;
         gemfileE ^= 3;
         vietnamJ.push(((catalogu ? 5 : 5) - vietnamJ.length));
         nterstitialH += (parseFloat(`${(catalogu ? 1 : 3) + parseInt(`${nterstitialH}`)}`));
       let team1 = 3.0;
      for (let a = 0; a < 3; a++) {
         analyticsC.push(2 + analyticsC.length);
      }
      while (!analyticsC.includes(team1)) {
          let accepted2 = 4.0;
          let materialS = String.fromCharCode(102,95,50,50,95,99,97,116,97,108,111,103,0);
          let acceptedR = String.fromCharCode(104,105,103,104,108,105,103,104,116,97,98,108,101,95,114,95,57,52,0);
          let viewsv = 3.0;
         team1 /= Math.max(3, parseFloat(`${parseInt(`${team1}`)}`));
         accepted2 -= materialS.length * 2;
         materialS = `${1 - acceptedR.length}`;
         acceptedR = `${materialS.length}`;
         viewsv /= Math.max(parseFloat(`${3}`), 2);
         break;
      }
       let sendM = true;
      minivode = `${userB.size / 2}`;
   }
          setMiddleMatchBeforeStart(false);

   for (let g = 0; g < 3; g++) {
      favoritet = new Map([[reminderh, (minivode == String.fromCharCode(65,0) ? minivode.length : reminderh.length)]]);
   }
          setMiddleMatchStarted(true);

       let unselected3 = 1;
       let light6: Array<any> = [325, 424];
       let type_ifZ: Array<any> = [908, 324, 916];
      for (let h = 0; h < 3; h++) {
         unselected3 &= unselected3 | light6.length;
      }
       let popupv = 2.0;
      for (let m = 0; m < 1; m++) {
         light6.push(parseInt(`${popupv}`));
      }
         type_ifZ.push(light6.length + unselected3);
         light6 = [3];
      while (type_ifZ.length > 2) {
          let zhengpian5 = true;
          let sideE = 4.0;
          let pressureK = 2.0;
          let analyticsA: Array<any> = [83, 451];
         popupv *= parseFloat(`${parseInt(`${pressureK}`) / (Math.max(analyticsA.length, 7))}`);
         zhengpian5 = !zhengpian5;
         sideE /= Math.max(parseFloat(`${parseInt(`${sideE}`) + 3}`), 3);
         pressureK += parseInt(`${sideE}`) & 3;
         analyticsA.push(parseInt(`${sideE}`) << (Math.min(4, Math.abs(2))));
         break;
      }
       let userL = false;
      let bellc = 5503289.0 >= popupv;
      do {
         popupv += parseFloat(`${1}`);
         if (bellc) {
            break;
         }
      } while ((4.87 == (popupv - 4.82)) && bellc);
         unselected3 %= Math.max(4, (parseInt(`${popupv}`) - (userL ? 2 : 5)));
      whatsappp = (light6.length - reminderh.length) > 89;
          setPanaltyMatchStarted(true);
        }
        break;
      case '已结束':
        setCountDownTime(false);

       let utilsh = String.fromCharCode(118,95,56,50,95,111,102,102,108,111,97,100,0);
       let mimov = String.fromCharCode(120,95,56,49,95,97,114,114,97,121,115,0);
       let foundQ = 5.0;
          let windf = 4.0;
          let activeH = false;
          let crownh = String.fromCharCode(117,95,50,57,95,99,111,110,102,105,100,101,110,116,105,97,108,0);
         utilsh += `${(3 >> (Math.min(5, Math.abs((activeH ? 4 : 3)))))}`;
         windf *= 2;
         activeH = windf <= 40.54;
         crownh = "3";
       let pagel: Array<any> = [78, 849];
       let videojsV: Array<any> = [241, 533];
          let cataloga = 1;
          let context8 = 5.0;
          let type__Q = false;
         pagel.push(((type__Q ? 4 : 5) >> (Math.min(Math.abs(parseInt(`${foundQ}`)), 4))));
         cataloga ^= 1 + parseInt(`${context8}`);
         context8 /= Math.max(4, 1 & cataloga);
         type__Q = cataloga < parseInt(`${context8}`);
         utilsh = `${utilsh.length - videojsV.length}`;
      for (let a = 0; a < 2; a++) {
         pagel = [utilsh.length];
      }
          let reacte = 5.0;
          let playercommonx = String.fromCharCode(97,95,55,50,95,114,116,112,102,98,0);
          let selectedF = String.fromCharCode(108,95,54,55,95,117,110,112,114,101,109,117,108,116,105,112,108,121,121,117,118,0);
         videojsV.push(2);
         reacte += parseFloat(`${parseInt(`${reacte}`)}`);
         playercommonx += `${playercommonx.length}`;
         selectedF += `${(selectedF == String.fromCharCode(108,0) ? playercommonx.length : selectedF.length)}`;
      for (let a = 0; a < 1; a++) {
         foundQ *= parseFloat(`${mimov.length}`);
      }
      let moon_ = 5030258 >= mimov.length;
      do {
         mimov = "3";
         if (moon_) {
            break;
         }
      } while (moon_ && (3 < (mimov.length & pagel.length)));
      while (5.92 >= (foundQ - 5.76)) {
         foundQ -= parseFloat(`${1}`);
         break;
      }
      userB.set(minivode, 3);
        setMiddleMatchBeforeStart(false);

   if (!progressb.endsWith(`${whatsappp}`)) {
       let half3 = String.fromCharCode(115,95,49,56,95,108,105,98,121,117,118,0);
       let watchE: Array<any> = [668, 290, 193];
       let catalogr: Map<any, any> = new Map([[String.fromCharCode(105,110,118,102,95,108,95,49,50,0),String.fromCharCode(97,95,49,54,95,109,100,97,116,0)], [String.fromCharCode(100,101,102,101,114,114,101,114,95,101,95,50,57,0),String.fromCharCode(115,111,114,116,95,115,95,54,48,0)]]);
      let clubk = half3 == String.fromCharCode(57,117,120,112,102,109,48,0);
      do {
         half3 += `${watchE.length ^ half3.length}`;
         if (clubk) {
            break;
         }
      } while ((!half3.endsWith(`${watchE.length}`)) && clubk);
         half3 += `${half3.length - 2}`;
      while (2 < (watchE.length ^ catalogr.size)) {
         catalogr.set(half3, watchE.length & 3);
         break;
      }
      while (half3.length < catalogr.size) {
          let user9: Array<any> = [555, 570, 579];
          let nextV: Array<any> = [314, 398, 643];
          let footballF = true;
          let showd = true;
          let starp = String.fromCharCode(115,108,105,100,101,114,95,119,95,49,57,0);
         half3 = `${nextV.length - 2}`;
         user9.push(3 >> (Math.min(5, starp.length)));
         nextV.push((user9.length << (Math.min(1, Math.abs((footballF ? 5 : 1))))));
         showd = starp.endsWith(`${footballF}`);
         break;
      }
       let sportK = String.fromCharCode(122,95,57,54,95,113,117,97,114,116,122,0);
       let philippinesE = String.fromCharCode(97,95,50,50,95,116,114,97,110,115,108,105,116,101,114,97,116,101,100,0);
      if (philippinesE.startsWith(`${catalogr.size}`)) {
          let linkr = 3;
          let xvodg = String.fromCharCode(100,97,116,97,115,116,111,114,101,95,113,95,57,57,0);
          let chinaC = String.fromCharCode(103,95,52,56,95,101,110,113,117,101,117,101,100,0);
         philippinesE = `${catalogr.size}`;
         linkr <<= Math.min(5, xvodg.length);
         xvodg += "2";
         chinaC = `${(String.fromCharCode(66,0) == xvodg ? xvodg.length : linkr)}`;
      }
         sportK += `${1 / (Math.max(5, half3.length))}`;
      if (!sportK.startsWith(`${catalogr.size}`)) {
         sportK += `${half3.length / (Math.max(6, catalogr.size))}`;
      }
         sportK += "3";
      whatsappp = (umengX | watchE.length) == 40;
   }
        setMiddleMatchStarted(true);

   let clearq = minivode == String.fromCharCode(114,52,106,0);
   do {
       let vietnamg = 5;
       let sliderm = 4.0;
       let closet = 5;
       let casting2 = 5.0;
       let playw = String.fromCharCode(115,95,52,49,95,99,111,110,118,101,114,115,105,111,110,0);
          let faviconX = true;
         sliderm += ((faviconX ? 3 : 5) % 3);
      if (4.90 >= (vietnamg / (Math.max(sliderm, 6)))) {
          let type_ob: Map<any, any> = new Map([[String.fromCharCode(98,95,56,95,120,120,104,97,115,104,0),String.fromCharCode(100,99,116,115,117,98,95,102,95,51,57,0)], [String.fromCharCode(115,104,105,109,115,95,117,95,56,50,0),String.fromCharCode(100,101,116,101,114,109,105,110,101,95,120,95,51,54,0)]]);
          let referrers: Map<any, any> = new Map([[String.fromCharCode(121,95,53,53,95,110,105,109,97,116,101,100,0),true ], [String.fromCharCode(102,95,56,48,95,109,111,118,101,0),true ], [String.fromCharCode(115,95,51,49,95,99,97,108,105,110,103,0),false ]]);
          let stringC = String.fromCharCode(109,95,50,48,95,115,116,117,110,0);
          let upgradeU = true;
          let submitw = 0.0;
         sliderm -= parseInt(`${sliderm}`) >> (Math.min(3, Math.abs(2)));
         type_ob.set(stringC, 1);
         referrers = new Map([[`${upgradeU}`, 3 * parseInt(`${submitw}`)]]);
         stringC = `${stringC.length}`;
         upgradeU = 32.42 >= submitw;
      }
      while (closet > 3) {
         sliderm *= parseInt(`${sliderm}`);
         break;
      }
          let containern = String.fromCharCode(102,114,109,97,95,50,95,57,48,0);
          let gradleF: Array<any> = [738, 780, 801];
         sliderm /= Math.max(3 << (Math.min(3, gradleF.length)), 3);
         containern += `${containern.length}`;
         gradleF.push((containern == String.fromCharCode(97,0) ? containern.length : containern.length));
         closet ^= 2 * playw.length;
         vietnamg |= 1 + parseInt(`${casting2}`);
          let gradlewy = String.fromCharCode(119,95,50,48,95,115,117,98,104,101,97,100,101,114,0);
         playw += `${parseInt(`${sliderm}`)}`;
         gradlewy = `${gradlewy.length % 1}`;
      while (2 == (2 - closet) && (closet - vietnamg) == 2) {
          let stary = 5.0;
          let friendsv: Map<any, any> = new Map([[String.fromCharCode(98,97,116,99,104,95,48,95,53,53,0),808], [String.fromCharCode(114,95,56,56,95,98,108,111,99,107,120,0),366], [String.fromCharCode(122,95,51,57,95,100,120,118,97,0),254]]);
          let countdown6 = 1.0;
          let skip2: Array<any> = [830, 204];
         closet <<= Math.min(Math.abs(vietnamg), 2);
         stary -= 3 << (Math.min(2, Math.abs(friendsv.size)));
         friendsv = new Map([[`${friendsv.size}`, parseInt(`${stary}`) / (Math.max(friendsv.size, 2))]]);
         countdown6 *= 3 * friendsv.size;
         skip2 = [3 ^ skip2.length];
         break;
      }
         casting2 *= parseFloat(`${playw.length & parseInt(`${sliderm}`)}`);
      let logouti = 7810771 >= playw.length;
      do {
         playw = `${2 * vietnamg}`;
         if (logouti) {
            break;
         }
      } while (logouti && (playw.length <= 4));
      let countryc = casting2 >= 5033275.0;
      do {
         casting2 += parseFloat(`${1 ^ parseInt(`${casting2}`)}`);
         if (countryc) {
            break;
         }
      } while (countryc && (2.8 <= casting2));
         casting2 /= Math.max(parseFloat(`${parseInt(`${casting2}`)}`), 5);
      while (3.81 == (casting2 / (Math.max(8, parseFloat(`${playw.length}`)))) || 3 == (playw.length / 1)) {
         casting2 += parseFloat(`${vietnamg}`);
         break;
      }
         casting2 /= Math.max(4, parseFloat(`${parseInt(`${sliderm}`)}`));
      while (5 <= (5 * parseInt(`${casting2}`)) || 4 <= (5 | playw.length)) {
         casting2 /= Math.max((parseFloat(`${String.fromCharCode(69,0) == playw ? playw.length : parseInt(`${casting2}`)}`)), 1);
         break;
      }
      minivode = `${reminderh.length}`;
      if (clearq) {
         break;
      }
   } while (clearq && (minivode.length < 1));
        setDefaultStatus(false);

      umengX ^= reminderh.length;
        if ((homePenaltyScore && awayPenaltyScore) && (homePenaltyScore > 0 || awayPenaltyScore > 0)) {

   if (!whatsappp) {
      whatsappp = !whatsappp && umengX == 70;
   }
          setPanaltyMatchStarted(true);
        }
        break;
      case '未开始':
        if (countDownStatus) {

       let success0 = true;
       let tempf = 3;
       let crossu = String.fromCharCode(110,95,53,55,95,112,114,101,118,118,101,99,0);
         success0 = !success0;
       let text3 = String.fromCharCode(104,97,110,100,108,101,114,115,95,53,95,49,54,0);
       let anytimeT = String.fromCharCode(114,101,97,100,108,110,95,52,95,57,0);
         tempf -= (tempf >> (Math.min(3, Math.abs((success0 ? 2 : 5)))));
      if (3 == text3.length) {
          let policyq = String.fromCharCode(109,105,99,95,55,95,53,50,0);
         success0 = text3.length <= 5;
         policyq += "2";
      }
      let debugH = String.fromCharCode(95,114,98,53,111,111,101,53,0) == anytimeT;
      do {
         anytimeT = `${crossu.length ^ text3.length}`;
         if (debugH) {
            break;
         }
      } while (debugH && (5 < (anytimeT.length - tempf) && (5 - tempf) < 2));
      while (crossu != text3) {
          let chartq: Array<any> = [516, 977, 377];
          let basketballY = String.fromCharCode(117,110,105,113,117,101,100,95,50,95,57,49,0);
          let infog: Map<any, any> = new Map([[String.fromCharCode(109,97,110,97,103,101,109,101,110,116,95,52,95,53,50,0),43], [String.fromCharCode(109,95,50,53,95,99,111,100,101,98,111,111,107,0),288], [String.fromCharCode(97,118,97,116,97,114,115,95,118,95,54,50,0),858]]);
          let navigations: Array<any> = [711, 236];
          let matchG = false;
         text3 = `${(String.fromCharCode(101,0) == anytimeT ? chartq.length : anytimeT.length)}`;
         chartq.push(((matchG ? 4 : 4) ^ basketballY.length));
         basketballY += `${infog.size + 3}`;
         infog = new Map([[`${infog.size}`, 1 & basketballY.length]]);
         navigations = [navigations.length ^ infog.size];
         matchG = 81 < infog.size || 81 < navigations.length;
         break;
      }
       let brightnessR = 2.0;
         anytimeT += `${parseInt(`${brightnessR}`)}`;
          let switch_0h = 1.0;
          let large8 = String.fromCharCode(117,95,50,51,95,110,101,101,100,101,100,0);
         success0 = (crossu.length - parseInt(`${switch_0h}`)) == 27;
         switch_0h -= parseFloat(`${large8.length ^ large8.length}`);
      userB = new Map([[`${success0}`, (String.fromCharCode(76,0) == minivode ? (success0 ? 2 : 2) : minivode.length)]]);
          setCountDownTime(true);

      userB = new Map([[reminderh, 1]]);
          setMiddleMatchBeforeStart(false);

       let ballq = String.fromCharCode(109,97,116,104,95,54,95,50,51,0);
       let d_centerz = String.fromCharCode(109,97,116,99,104,101,95,109,95,53,57,0);
       let ajaxk = 5.0;
         d_centerz = `${parseInt(`${ajaxk}`)}`;
      if (d_centerz != String.fromCharCode(104,0)) {
         ballq = `${d_centerz.length & ballq.length}`;
      }
      while (1 > d_centerz.length) {
         d_centerz += `${ballq.length + 2}`;
         break;
      }
          let miniA = String.fromCharCode(112,117,98,108,105,99,107,101,121,115,95,119,95,55,49,0);
         ajaxk += parseInt(`${ajaxk}`) ^ 3;
         miniA += `${miniA.length ^ miniA.length}`;
      if ((d_centerz.length * ajaxk) >= 1.51) {
         ajaxk += 3;
      }
         ballq += `${d_centerz.length << (Math.min(Math.abs(3), 1))}`;
         ajaxk += 3;
      let clockm = d_centerz == String.fromCharCode(55,50,48,0);
      do {
         d_centerz += `${parseInt(`${ajaxk}`) % (Math.max(3, d_centerz.length))}`;
         if (clockm) {
            break;
         }
      } while (clockm && (3 >= (parseInt(`${ajaxk}`) / (Math.max(6, d_centerz.length))) || 2.84 >= (ajaxk / 1.23)));
      let switch_5ln = 5969039 <= d_centerz.length;
      do {
         d_centerz = `${1 & ballq.length}`;
         if (switch_5ln) {
            break;
         }
      } while (switch_5ln && ((1 + d_centerz.length) >= 4));
      whatsappp = reminderh == String.fromCharCode(54,0);
          setMiddleMatchStarted(false);

       let actionsp = 1;
       let suggestionw = String.fromCharCode(118,97,114,105,110,116,115,95,56,95,51,0);
         actionsp -= suggestionw.length;
       let crowng = String.fromCharCode(98,105,110,100,105,110,103,95,115,95,51,51,0);
      for (let z = 0; z < 2; z++) {
          let rightv = 4.0;
          let philippinesK = 1.0;
          let logoo = String.fromCharCode(112,95,55,49,95,98,111,116,116,111,109,0);
          let animationt = 4.0;
          let resendd = 1.0;
         crowng += "1";
         rightv *= parseInt(`${rightv}`) >> (Math.min(Math.abs(parseInt(`${philippinesK}`)), 4));
         philippinesK /= Math.max(4, parseFloat(`${parseInt(`${rightv}`)}`));
         logoo += `${(logoo == String.fromCharCode(90,0) ? parseInt(`${rightv}`) : logoo.length)}`;
         animationt *= parseInt(`${philippinesK}`);
         resendd += parseInt(`${animationt}`) % 2;
      }
         suggestionw += `${actionsp}`;
      while (!suggestionw.endsWith(`${actionsp}`)) {
          let weibo4: Array<any> = [264, 637];
          let teamg = 1;
         actionsp &= actionsp & teamg;
         weibo4 = [weibo4.length >> (Math.min(Math.abs(2), 2))];
         teamg &= weibo4.length & weibo4.length;
         break;
      }
         actionsp >>= Math.min(2, Math.abs(suggestionw.length % 3));
      favoritet = new Map([[`${whatsappp}`, ((whatsappp ? 2 : 2))]]);
          setDefaultStatus(false);
        } else {

      whatsappp = 37 <= progressb.length || reminderh.length <= 37;
          setCountDownTime(false);

   while ((umengX >> (Math.min(Math.abs(3), 4))) == 4) {
      umengX <<= Math.min(5, Math.abs(3));
      break;
   }
          setMiddleMatchBeforeStart(true);

      favoritet = new Map([[`${whatsappp}`, (umengX - (whatsappp ? 4 : 2))]]);
          setMiddleMatchStarted(false);

   while ((favoritet.size ^ minivode.length) <= 4) {
      minivode = `${progressb.length << (Math.min(Math.abs(1), 1))}`;
      break;
   }
          setDefaultStatus(false);
        }
        break;
      default:
        setCountDownTime(false);

      userB = new Map([[`${zoomI.length}`, 1 | zoomI.length]]);
        setMiddleMatchBeforeStart(false);

   let pageq = 5299954 >= zoomI.length;
   do {
       let shirto = 5.0;
       let thailandK = String.fromCharCode(101,109,111,116,105,99,111,110,95,116,95,49,53,0);
      let turnO = 7923609.0 <= shirto;
      do {
         shirto -= parseFloat(`${parseInt(`${shirto}`)}`);
         if (turnO) {
            break;
         }
      } while ((4.31 <= (3.10 * shirto)) && turnO);
          let clubY = false;
          let appleW: Array<any> = [83, 346];
          let mailt: Array<any> = [64, 528, 946];
         shirto *= parseFloat(`${parseInt(`${shirto}`)}`);
         clubY = mailt.length == 13;
         appleW = [2 >> (Math.min(1, appleW.length))];
         mailt = [2 >> (Math.min(1, appleW.length))];
      let clearb = 5261871.0 >= shirto;
      do {
         shirto += parseFloat(`${thailandK.length % (Math.max(2, 5))}`);
         if (clearb) {
            break;
         }
      } while ((parseFloat(`${thailandK.length}`) <= shirto) && clearb);
         thailandK = `${2 - parseInt(`${shirto}`)}`;
      if (4 <= thailandK.length) {
         shirto /= Math.max(2, parseFloat(`${2 ^ thailandK.length}`));
      }
       let mathU = 1;
       let dragK = 3;
      zoomI.push(1 << (Math.min(3, zoomI.length)));
      if (pageq) {
         break;
      }
   } while (pageq && (Array.from(userB.keys()).includes(`${zoomI.length}`)));
        setMiddleMatchStarted(false);

      minivode = `${reminderh.length}`;
        setDefaultStatus(true);

      progressb += `${2 * zoomI.length}`;
        break;
    }
  };

  
  
  
  
  
  
  
  

  const liveVideo = () => {
       let fastk = String.fromCharCode(109,95,54,57,95,98,121,114,121,0);
    let plus0: Array<any> = [String.fromCharCode(103,95,57,54,95,109,105,110,105,109,105,122,101,100,0), String.fromCharCode(100,111,119,110,108,111,97,100,95,103,95,51,57,0), String.fromCharCode(117,110,107,105,99,107,95,108,95,52,51,0)];
    let untickO = 4.0;
    let shirtr: Array<any> = [438, 667, 351];
    let nalyticsv = 0.0;
    let eighteeno = 4.0;
    let fieldt = String.fromCharCode(103,95,55,53,95,113,117,97,100,114,0);
    let previewg = 4;
    let playp = String.fromCharCode(105,109,112,111,114,116,95,57,95,51,53,0);
    let tick4 = 1;
    let detailI = 2;
    let entryv = 1.0;
    let login1 = 2.0;
   let vignetter = fastk.length <= 7567976;
   do {
      fastk = `${fieldt.length & 3}`;
      if (vignetter) {
         break;
      }
   } while (vignetter && (1 > (shirtr.length << (Math.min(Math.abs(1), 3))) && 4 > (fastk.length << (Math.min(Math.abs(1), 1)))));
   while (previewg < nalyticsv) {
      nalyticsv *= parseFloat(`${parseInt(`${nalyticsv}`) / 2}`);
      break;
   }
   if (5.79 >= (3.39 / (Math.max(2, eighteeno))) || (previewg * 5) >= 3) {
      previewg *= 3 >> (Math.min(4, shirtr.length));
   }
   while (playp.length >= tick4) {
       let tickedL = 3.0;
       let ping7 = 1;
       let selectedG = String.fromCharCode(112,95,49,55,95,118,114,101,99,116,0);
       let configureo = 3.0;
       let bingw = String.fromCharCode(109,101,109,110,95,106,95,54,49,0);
      let predictiont = 6490349.0 <= configureo;
      do {
         configureo *= (String.fromCharCode(72,0) == bingw ? ping7 : bingw.length);
         if (predictiont) {
            break;
         }
      } while (((5 | selectedG.length) > 1 && 4 > (selectedG.length * 5)) && predictiont);
         bingw = `${parseInt(`${tickedL}`)}`;
      while (3.70 >= (ping7 / (Math.max(6, tickedL)))) {
         ping7 -= ping7 | selectedG.length;
         break;
      }
       let playercommonF = 1.0;
       let yellowV = 0.0;
       let dragP = 4.0;
       let terms3 = 3.0;
      let recommendationD = 7642833.0 <= yellowV;
      do {
         yellowV *= parseFloat(`${2 % (Math.max(4, parseInt(`${yellowV}`)))}`);
         if (recommendationD) {
            break;
         }
      } while (((selectedG.length * parseInt(`${yellowV}`)) == 2 || (2 * parseInt(`${yellowV}`)) == 3) && recommendationD);
         selectedG = `${selectedG.length + 1}`;
      if ((selectedG.length * 4) < 2 || (4 + parseInt(`${dragP}`)) < 4) {
         dragP /= Math.max(2, parseFloat(`${1 - selectedG.length}`));
      }
      if (1.17 >= tickedL) {
          let pausel: Map<any, any> = new Map([[String.fromCharCode(106,95,50,56,95,114,105,100,103,101,0),578], [String.fromCharCode(117,95,53,53,95,105,110,99,114,101,109,101,110,116,97,108,0),939], [String.fromCharCode(112,97,114,97,108,108,101,108,95,103,95,54,56,0),910]]);
          let bellp = 4.0;
         tickedL += parseInt(`${configureo}`);
         pausel.set(`${bellp}`, pausel.size);
         bellp += parseFloat(`${parseInt(`${bellp}`)}`);
      }
       let k_imageR = 0.0;
      if ((bingw.length & 2) > 2 || (bingw.length & 2) > 4) {
         bingw += `${bingw.length | 2}`;
      }
       let mimoD: Map<any, any> = new Map([[String.fromCharCode(99,97,114,114,121,95,122,95,54,0),144], [String.fromCharCode(99,108,108,99,95,102,95,57,49,0),648]]);
      for (let c = 0; c < 1; c++) {
         bingw += `${3 >> (Math.min(Math.abs(parseInt(`${yellowV}`)), 1))}`;
      }
       let loading9 = 0;
         terms3 -= parseFloat(`${parseInt(`${tickedL}`) | 3}`);
      tick4 += parseInt(`${eighteeno}`) - selectedG.length;
      break;
   }
      playp += "1";
   for (let x = 0; x < 1; x++) {
       let thailandD = String.fromCharCode(117,112,115,116,114,101,97,109,95,55,95,49,52,0);
       let register_cmy = true;
       let d_viewP: Map<any, any> = new Map([[String.fromCharCode(103,95,49,51,95,97,99,99,101,112,116,97,98,108,101,0),true ], [String.fromCharCode(111,95,50,54,95,112,114,101,99,97,108,99,117,108,97,116,101,0),true ]]);
       let vignetteY = 1.0;
      for (let x = 0; x < 1; x++) {
         thailandD += `${d_viewP.size}`;
      }
         vignetteY -= parseInt(`${vignetteY}`) * 1;
          let editm = 0;
         register_cmy = 36.49 == vignetteY;
         editm %= Math.max(editm / 2, 5);
      while ((5.66 * vignetteY) == 5.57) {
          let singleA = String.fromCharCode(112,95,50,57,95,112,114,101,118,101,110,116,115,0);
          let middleware7 = 4.0;
          let schedules = 3.0;
          let eventE = 0;
         d_viewP.set(`${middleware7}`, parseInt(`${middleware7}`));
         singleA = `${eventE + singleA.length}`;
         schedules += parseFloat(`${parseInt(`${schedules}`) >> (Math.min(singleA.length, 3))}`);
         eventE >>= Math.min(2, Math.abs((singleA == String.fromCharCode(50,0) ? parseInt(`${schedules}`) : singleA.length)));
         break;
      }
         d_viewP.set(`${register_cmy}`, ((register_cmy ? 4 : 4) | d_viewP.size));
         vignetteY -= parseInt(`${vignetteY}`) | d_viewP.size;
      if (3 < d_viewP.size && (3 << (Math.min(1, Math.abs(d_viewP.size)))) < 1) {
         register_cmy = 64 == d_viewP.size;
      }
      let baidu4 = vignetteY <= 6236579.0;
      do {
         vignetteY -= parseInt(`${vignetteY}`) << (Math.min(Math.abs(d_viewP.size), 2));
         if (baidu4) {
            break;
         }
      } while (baidu4 && (register_cmy));
         thailandD += `${thailandD.length}`;
      for (let s = 0; s < 2; s++) {
         d_viewP = new Map([[`${vignetteY}`, 1 - parseInt(`${vignetteY}`)]]);
      }
         register_cmy = 49.93 >= vignetteY;
          let bottomu = 4.0;
          let nalyticsN: Map<any, any> = new Map([[String.fromCharCode(104,119,97,101,115,95,104,95,56,48,0),131], [String.fromCharCode(116,105,116,110,116,95,109,95,52,50,0),622], [String.fromCharCode(116,95,57,57,95,108,115,119,115,117,116,105,108,115,0),331]]);
          let forwardR = String.fromCharCode(105,95,55,57,95,109,101,110,116,105,111,110,0);
         d_viewP = new Map([[thailandD, (thailandD == String.fromCharCode(114,0) ? parseInt(`${vignetteY}`) : thailandD.length)]]);
         bottomu *= parseInt(`${bottomu}`);
         nalyticsN.set(forwardR, (forwardR == String.fromCharCode(119,0) ? forwardR.length : nalyticsN.size));
      eighteeno -= thailandD.length;
   }
   let anytimez = nalyticsv <= 8739518.0;
   do {
       let edith = String.fromCharCode(99,114,101,97,116,101,98,97,108,97,110,99,101,95,108,95,57,0);
          let downV = String.fromCharCode(113,95,55,53,95,108,101,110,103,116,104,0);
         edith += `${1 % (Math.max(6, edith.length))}`;
         downV = `${downV.length}`;
       let f_managerx = 5.0;
       let plusZ = 4.0;
      let line3 = 5565571.0 <= plusZ;
      do {
         plusZ += parseFloat(`${parseInt(`${plusZ}`)}`);
         if (line3) {
            break;
         }
      } while (line3 && ((parseFloat(`${edith.length}`) + plusZ) >= 4.86 || 2.11 >= (4.86 + plusZ)));
      nalyticsv *= parseFloat(`${fastk.length}`);
      if (anytimez) {
         break;
      }
   } while (anytimez && (!shirtr.includes(nalyticsv)));
       let f_viewu = String.fromCharCode(112,95,52,49,95,114,101,116,117,114,110,105,110,103,0);
       let auto_kmS = String.fromCharCode(105,110,99,108,117,100,101,95,111,95,53,49,0);
       let description_oR = String.fromCharCode(101,110,99,105,112,104,101,114,95,54,95,48,0);
         auto_kmS = `${auto_kmS.length >> (Math.min(4, description_oR.length))}`;
      let white0 = f_viewu == String.fromCharCode(107,108,48,109,121,103,0);
      do {
         f_viewu = `${description_oR.length}`;
         if (white0) {
            break;
         }
      } while ((!f_viewu.endsWith(`${auto_kmS.length}`)) && white0);
         description_oR = `${(f_viewu == String.fromCharCode(108,0) ? auto_kmS.length : f_viewu.length)}`;
         f_viewu += `${(description_oR == String.fromCharCode(49,0) ? description_oR.length : auto_kmS.length)}`;
          let upgradeU = false;
          let langkeyU = 2.0;
          let singlet: Array<any> = [547, 64];
         description_oR += `${2 / (Math.max(6, auto_kmS.length))}`;
         upgradeU = !upgradeU && singlet.length >= 30;
         langkeyU += parseFloat(`${singlet.length}`);
         auto_kmS = `${auto_kmS.length + 3}`;
         description_oR = `${(String.fromCharCode(114,0) == description_oR ? description_oR.length : f_viewu.length)}`;
      let mbsplash1 = description_oR.length <= 5874769;
      do {
          let hejiy = true;
         description_oR += `${description_oR.length + auto_kmS.length}`;
         hejiy = hejiy || !hejiy;
         if (mbsplash1) {
            break;
         }
      } while (mbsplash1 && (description_oR.length < 2));
         description_oR = `${description_oR.length * auto_kmS.length}`;
      tick4 %= Math.max(1, 3);
   if ((3.45 / (Math.max(7, nalyticsv))) >= 2.41) {
      nalyticsv -= parseFloat(`${fastk.length >> (Math.min(Math.abs(3), 2))}`);
   }
       let line5 = String.fromCharCode(114,101,115,101,116,95,107,95,56,48,0);
      while (line5 != String.fromCharCode(118,0)) {
          let commong: Array<any> = [489, 654, 118];
         line5 += `${(line5 == String.fromCharCode(80,0) ? line5.length : commong.length)}`;
         break;
      }
      for (let r = 0; r < 2; r++) {
         line5 += "1";
      }
         line5 += "1";
      previewg += parseInt(`${login1}`) / (Math.max(fieldt.length, 8));
      entryv += parseFloat(`${1}`);
      detailI >>= Math.min(fieldt.length, 3);

    if (onOpenLive) {

      entryv *= parseFloat(`${parseInt(`${nalyticsv}`)}`);
      nalyticsv *= parseFloat(`${detailI}`);
   if (login1 <= 2.20) {
      previewg &= 3;
   }
   if (login1 == 5.86) {
      login1 /= Math.max((fieldt == String.fromCharCode(95,0) ? fieldt.length : detailI), 5);
   }
      fieldt = `${(String.fromCharCode(103,0) == fastk ? fastk.length : parseInt(`${entryv}`))}`;
      fastk += `${parseInt(`${untickO}`) % 3}`;
   for (let g = 0; g < 2; g++) {
       let routerc: Map<any, any> = new Map([[String.fromCharCode(112,95,51,54,95,97,112,111,115,0),String.fromCharCode(119,95,56,48,95,99,116,114,0)], [String.fromCharCode(122,95,57,53,95,97,115,115,105,103,110,0),String.fromCharCode(104,114,116,102,95,119,95,51,49,0)], [String.fromCharCode(117,112,112,101,114,99,97,115,101,95,109,95,53,48,0),String.fromCharCode(116,109,112,108,95,50,95,50,0)]]);
       let plusM: Map<any, any> = new Map([[String.fromCharCode(107,95,56,50,95,100,111,119,110,108,111,97,100,0),816], [String.fromCharCode(102,114,97,109,101,102,111,114,109,97,116,95,100,95,51,49,0),181], [String.fromCharCode(114,101,103,105,115,116,101,114,102,100,115,95,49,95,51,52,0),498]]);
         routerc = new Map([[`${routerc.size}`, routerc.size / (Math.max(2, 10))]]);
      for (let b = 0; b < 3; b++) {
         plusM.set(`${plusM.size}`, 2 * routerc.size);
      }
         routerc.set(`${plusM.size}`, routerc.size);
          let membershipR = 3;
          let selectedN = false;
          let reacta: Map<any, any> = new Map([[String.fromCharCode(115,116,99,111,95,121,95,49,57,0),441], [String.fromCharCode(98,95,57,54,95,99,108,117,116,115,0),130]]);
         routerc = new Map([[`${routerc.size}`, routerc.size & 1]]);
         membershipR *= membershipR / (Math.max(reacta.size, 10));
         selectedN = membershipR == reacta.size;
       let mbbidb = String.fromCharCode(112,114,111,100,115,95,50,95,57,50,0);
      while (2 == routerc.size) {
          let networkU = 4.0;
         routerc.set(mbbidb, (String.fromCharCode(71,0) == mbbidb ? mbbidb.length : parseInt(`${networkU}`)));
         break;
      }
      nalyticsv += (parseFloat(`${String.fromCharCode(54,0) == fieldt ? tick4 : fieldt.length}`));
   }
   while ((eighteeno / (Math.max(1, nalyticsv))) > 4.86 && 1.98 > (eighteeno / (Math.max(4.86, 1)))) {
       let models9 = String.fromCharCode(116,95,57,51,0);
       let episode3 = 4.0;
       let hookS = 0.0;
      if ((1.66 - hookS) <= 4.61) {
         hookS -= parseInt(`${episode3}`);
      }
         episode3 *= parseFloat(`${parseInt(`${hookS}`)}`);
      for (let h = 0; h < 1; h++) {
          let signinupE: Map<any, any> = new Map([[String.fromCharCode(103,115,117,98,95,117,95,49,48,48,0),true ], [String.fromCharCode(115,117,98,115,97,109,112,95,97,95,51,52,0),true ]]);
          let configS: Array<any> = [845, 218];
          let quest4 = false;
         episode3 /= Math.max(parseFloat(`${signinupE.size}`), 1);
         signinupE = new Map([[`${configS.length}`, configS.length]]);
      }
       let fcopy_oO: Map<any, any> = new Map([[String.fromCharCode(98,114,101,97,100,99,114,117,109,98,115,95,119,95,56,50,0),507], [String.fromCharCode(109,97,120,95,119,95,54,51,0),460], [String.fromCharCode(98,95,57,54,95,109,111,116,105,111,110,112,105,120,101,108,115,0),825]]);
      while (2.3 < hookS) {
         hookS *= parseInt(`${hookS}`) + models9.length;
         break;
      }
          let borderless8 = 5.0;
          let hoverU: Array<any> = [149, 449];
         models9 += `${parseInt(`${hookS}`)}`;
         borderless8 *= 2 / (Math.max(1, parseInt(`${borderless8}`)));
         hoverU.push(parseInt(`${borderless8}`));
          let googleg = 2.0;
          let videocommonU = String.fromCharCode(113,95,52,52,95,115,119,97,112,121,118,98,117,102,102,101,114,0);
          let benefitu = String.fromCharCode(109,101,115,104,95,55,95,52,48,0);
         models9 += `${benefitu.length >> (Math.min(Math.abs(2), 3))}`;
         googleg -= 3 ^ videocommonU.length;
         videocommonU = "3";
         benefitu += `${parseInt(`${googleg}`)}`;
       let megaphone4 = String.fromCharCode(122,95,50,54,95,114,101,97,108,105,102,121,0);
          let long_17N = false;
         episode3 /= Math.max(1, parseFloat(`${fcopy_oO.size / (Math.max(megaphone4.length, 10))}`));
         long_17N = !long_17N;
      eighteeno -= 1 ^ parseInt(`${hookS}`);
      break;
   }
   let agreementi = 7220618 >= tick4;
   do {
      tick4 %= Math.max(4, parseInt(`${eighteeno}`));
      if (agreementi) {
         break;
      }
   } while (((tick4 / 5) == 3 && 3.12 == (parseFloat(`${tick4}`) / (Math.max(9, nalyticsv)))) && agreementi);
   while (detailI > 1) {
       let selectedo = 5.0;
       let detailr = String.fromCharCode(97,102,116,101,114,108,105,102,101,95,110,95,57,48,0);
       let expiredm = String.fromCharCode(103,95,53,50,0);
      for (let q = 0; q < 1; q++) {
         expiredm = "1";
      }
         detailr += `${detailr.length}`;
       let update_xwR = String.fromCharCode(112,114,111,112,111,115,97,108,95,108,95,54,51,0);
       let screen_ = String.fromCharCode(100,95,55,54,95,97,118,102,105,108,116,101,114,114,101,115,0);
       let loginz = String.fromCharCode(117,95,53,48,0);
       let cornerS = String.fromCharCode(119,97,116,99,104,95,116,95,57,49,0);
         screen_ += "1";
          let zoomi = String.fromCharCode(111,100,105,110,103,95,112,95,50,49,0);
          let aread: Map<any, any> = new Map([[String.fromCharCode(109,98,117,118,101,114,114,111,114,95,110,95,52,54,0),386], [String.fromCharCode(114,111,117,116,105,110,101,115,95,101,95,56,48,0),982], [String.fromCharCode(113,95,53,48,95,108,111,99,107,105,110,103,0),470]]);
          let component2 = String.fromCharCode(97,95,53,54,95,109,117,116,101,120,0);
         expiredm = "1";
         zoomi += "1";
         aread.set(component2, 2);
         component2 = "3";
          let championM = String.fromCharCode(104,119,99,111,110,116,101,120,116,95,115,95,54,54,0);
          let interstitialv = String.fromCharCode(110,95,51,55,95,105,100,115,0);
          let c_centerE = 5;
         cornerS = `${detailr.length}`;
         championM += `${c_centerE * interstitialv.length}`;
         interstitialv = `${championM.length ^ c_centerE}`;
       let mbbannerg = true;
         detailr += `${2 & update_xwR.length}`;
      detailI >>= Math.min(3, detailr.length);
      selectedo *= parseFloat(`${parseInt(`${selectedo}`) >> (Math.min(Math.abs(parseInt(`${selectedo}`)), 1))}`);
      break;
   }
       let borderlessS = String.fromCharCode(119,95,56,57,95,109,105,110,115,0);
      while (borderlessS != borderlessS) {
          let chartp = 2.0;
          let strJ: Array<any> = [943, 36];
          let darkS = 2.0;
         borderlessS = `${parseInt(`${darkS}`)}`;
         chartp *= parseInt(`${chartp}`) & strJ.length;
         strJ = [strJ.length];
         break;
      }
      if (4 < borderlessS.length) {
         borderlessS += "1";
      }
       let applicationg = String.fromCharCode(106,95,53,48,95,108,111,103,0);
       let blacklist5 = String.fromCharCode(109,95,57,49,95,97,112,112,114,111,120,105,109,97,116,105,111,110,0);
      nalyticsv *= parseFloat(`${1 % (Math.max(4, playp.length))}`);
      shirtr = [1];
      onOpenLive();
    }
  };

  const liveAnimationVideo = () => {
       let baiduM = 0;
    let bannerm = String.fromCharCode(110,95,50,56,95,97,108,115,111,0);
    let whatsappi = 1;
    let aboutN = 2.0;
    let applicationo = 5.0;
    let grayz = 1;
    let backgroundO = 4.0;
    let ballx: Array<any> = [42, 586];
    let predictiona = 1;
    let v_unlockb = String.fromCharCode(119,95,56,48,95,109,97,105,110,110,101,116,115,0);
    let eventk = String.fromCharCode(116,105,109,101,105,110,102,111,95,122,95,55,51,0);
   let spinnert = 6933245 <= ballx.length;
   do {
      ballx = [whatsappi];
      if (spinnert) {
         break;
      }
   } while ((3.89 == applicationo) && spinnert);
      ballx = [baiduM - predictiona];
       let liver = String.fromCharCode(110,95,53,51,95,100,101,116,101,99,116,105,111,110,0);
         liver = "1";
      while (liver.endsWith(`${liver.length}`)) {
          let default_thg = true;
          let mathC: Map<any, any> = new Map([[String.fromCharCode(111,99,97,108,105,122,97,116,105,111,110,95,120,95,53,57,0),878], [String.fromCharCode(108,110,110,105,100,95,100,95,57,51,0),927]]);
          let downloaderU = 5.0;
          let benefitO = String.fromCharCode(114,116,109,112,99,114,121,112,116,95,101,95,49,0);
          let bootsplashx = String.fromCharCode(107,95,51,55,95,110,111,104,119,0);
         liver += `${(3 + (default_thg ? 5 : 3))}`;
         default_thg = bootsplashx.startsWith(`${downloaderU}`);
         mathC.set(`${benefitO}`, mathC.size | 1);
         downloaderU *= benefitO.length;
         bootsplashx = `${mathC.size & parseInt(`${downloaderU}`)}`;
         break;
      }
         liver = `${liver.length}`;
      predictiona |= whatsappi;
       let invitey = true;
       let malaysiae = 1;
         invitey = !invitey;
      for (let n = 0; n < 2; n++) {
         malaysiae <<= Math.min(Math.abs(malaysiae), 1);
      }
      if (malaysiae > 2) {
         malaysiae *= 3 & malaysiae;
      }
         invitey = malaysiae <= 41;
      let backK = invitey ? !invitey : invitey;
      do {
          let detailZ = 2;
          let downloadedN = 3;
         invitey = 94 <= malaysiae;
         detailZ ^= downloadedN ^ 1;
         downloadedN ^= 2;
         if (backK) {
            break;
         }
      } while ((5 < (malaysiae << (Math.min(Math.abs(2), 1))) && !invitey) && backK);
      for (let d = 0; d < 2; d++) {
         invitey = invitey && 60 == malaysiae;
      }
      predictiona <<= Math.min(4, Math.abs(2 / (Math.max(10, whatsappi))));
       let catagoryl: Map<any, any> = new Map([[String.fromCharCode(105,100,119,116,95,109,95,49,56,0),239], [String.fromCharCode(112,95,50,49,95,99,97,114,101,116,0),255]]);
         catagoryl = new Map([[`${catagoryl.size}`, catagoryl.size << (Math.min(3, Math.abs(catagoryl.size)))]]);
          let drags = 2.0;
          let orientationU = true;
          let vietnami: Map<any, any> = new Map([[String.fromCharCode(112,114,101,118,105,101,119,115,95,120,95,51,0),false ], [String.fromCharCode(109,97,105,110,115,116,97,103,101,95,114,95,53,49,0),true ], [String.fromCharCode(120,117,116,105,108,95,107,95,53,48,0),false ]]);
         catagoryl = new Map([[`${catagoryl.size}`, 3]]);
         drags /= Math.max(parseInt(`${drags}`), 4);
         orientationU = vietnami.get(`${drags}`) == null;
         vietnami.set(`${drags}`, parseInt(`${drags}`));
      if ((catagoryl.size * catagoryl.size) >= 2 || 2 >= (2 * catagoryl.size)) {
         catagoryl = new Map([[`${catagoryl.size}`, catagoryl.size & 1]]);
      }
      applicationo -= parseFloat(`${parseInt(`${aboutN}`)}`);

    

      predictiona <<= Math.min(Math.abs(baiduM), 4);
   for (let r = 0; r < 2; r++) {
      baiduM /= Math.max(1, 3);
   }
      applicationo *= parseFloat(`${grayz | ballx.length}`);
   for (let f = 0; f < 1; f++) {
       let appsO = false;
       let temperatureN = String.fromCharCode(105,110,116,114,97,120,95,51,95,49,48,48,0);
       let downloaderV = 3.0;
       let stringsw = String.fromCharCode(103,110,114,101,95,107,95,53,52,0);
       let storeK = 2;
       let kicky = 5;
         temperatureN = `${storeK}`;
      let success2 = 5856668 >= temperatureN.length;
      do {
         temperatureN = `${(temperatureN == String.fromCharCode(110,0) ? temperatureN.length : kicky)}`;
         if (success2) {
            break;
         }
      } while ((temperatureN.includes(`${appsO}`)) && success2);
       let trashR = String.fromCharCode(100,95,54,56,95,97,112,116,120,104,100,0);
       let linek = String.fromCharCode(121,95,55,51,95,112,114,101,115,117,98,109,105,116,0);
      for (let b = 0; b < 3; b++) {
         linek += "1";
      }
      let privilegeK = 8635395 >= trashR.length;
      do {
          let register_9s9 = false;
          let sigmobi = 2.0;
          let favoriteo = String.fromCharCode(100,97,116,97,114,95,48,95,50,48,0);
          let castZ = String.fromCharCode(107,95,50,55,95,115,116,97,116,105,115,116,105,99,0);
         trashR += `${parseInt(`${downloaderV}`) / 3}`;
         register_9s9 = favoriteo == castZ;
         sigmobi *= parseFloat(`${favoriteo.length * 2}`);
         castZ = `${((register_9s9 ? 2 : 5) << (Math.min(Math.abs(parseInt(`${sigmobi}`)), 2)))}`;
         if (privilegeK) {
            break;
         }
      } while ((trashR.startsWith(`${stringsw.length}`)) && privilegeK);
         temperatureN += `${stringsw.length & 2}`;
         appsO = linek.endsWith(`${kicky}`);
         downloaderV *= (parseFloat(`${temperatureN == String.fromCharCode(103,0) ? temperatureN.length : (appsO ? 1 : 3)}`));
       let trophyC = 4.0;
      for (let p = 0; p < 1; p++) {
         trashR = `${storeK}`;
      }
      while ((kicky << (Math.min(stringsw.length, 5))) >= 5 && (kicky << (Math.min(Math.abs(5), 1))) >= 4) {
          let with_gI = true;
          let grayB = 4.0;
          let lessX = 0.0;
          let anythinkW: Map<any, any> = new Map([[String.fromCharCode(117,95,57,48,95,104,101,97,100,101,114,0),726], [String.fromCharCode(114,97,105,116,95,50,95,52,50,0),122], [String.fromCharCode(99,95,50,49,95,115,108,111,112,0),685]]);
          let lineW = false;
         kicky ^= 1;
         with_gI = 100.52 == grayB && !with_gI;
         grayB /= Math.max(5, parseFloat(`${parseInt(`${lessX}`) | 3}`));
         lessX -= parseFloat(`${anythinkW.size / 1}`);
         anythinkW = new Map([[`${with_gI}`, parseInt(`${lessX}`) % 1]]);
         lineW = (3 == (anythinkW.size & (lineW ? anythinkW.size : 7)));
         break;
      }
      applicationo -= parseFloat(`${1 & temperatureN.length}`);
   }
       let previewQ: Map<any, any> = new Map([[String.fromCharCode(121,95,52,49,95,97,99,102,102,0),false ], [String.fromCharCode(100,111,97,108,108,95,54,95,55,50,0),true ], [String.fromCharCode(115,107,105,112,95,98,95,51,54,0),false ]]);
       let tempe: Array<any> = [52, 823, 280];
       let bootsplashR: Map<any, any> = new Map([[String.fromCharCode(99,97,110,99,101,108,108,97,98,108,101,95,118,95,49,53,0),666], [String.fromCharCode(109,95,57,50,95,99,110,116,0),440], [String.fromCharCode(108,95,56,49,95,99,101,110,116,101,114,0),313]]);
      let logoutk = tempe.length >= 7125033;
      do {
         tempe.push(previewQ.size);
         if (logoutk) {
            break;
         }
      } while (logoutk && (3 < (bootsplashR.size & tempe.length)));
      for (let o = 0; o < 3; o++) {
         bootsplashR = new Map([[`${previewQ.size}`, previewQ.size * tempe.length]]);
      }
      let screenV = tempe.length >= 7308891;
      do {
         tempe = [1];
         if (screenV) {
            break;
         }
      } while (screenV && (4 >= previewQ.size));
         previewQ.set(`${tempe.length}`, tempe.length | previewQ.size);
      for (let k = 0; k < 2; k++) {
          let sportsN = 4;
          let middleN: Map<any, any> = new Map([[String.fromCharCode(118,95,52,52,95,115,117,98,115,101,113,117,101,110,116,0),144], [String.fromCharCode(116,95,50,95,104,105,115,116,111,103,114,97,109,0),832]]);
          let rankM = 5;
          let footballF = String.fromCharCode(112,114,111,100,95,103,95,54,57,0);
         tempe = [rankM];
         sportsN >>= Math.min(Math.abs((String.fromCharCode(80,0) == footballF ? middleN.size : footballF.length)), 4);
         middleN = new Map([[`${middleN.size}`, 2]]);
         rankM -= 1 * sportsN;
      }
          let skipe = true;
          let nalyticsL = String.fromCharCode(118,108,99,115,112,101,99,95,122,95,57,55,0);
          let disconnected9: Map<any, any> = new Map([[String.fromCharCode(112,111,115,116,105,110,103,95,114,95,50,48,0),String.fromCharCode(97,95,56,56,95,115,116,114,99,97,116,0)], [String.fromCharCode(103,101,111,98,116,97,103,95,115,95,49,51,0),String.fromCharCode(105,95,51,57,95,98,110,104,101,120,0)]]);
         tempe.push(previewQ.size / (Math.max(nalyticsL.length, 6)));
         skipe = !skipe;
         nalyticsL = `${(1 + (skipe ? 2 : 5))}`;
         disconnected9 = new Map([[`${disconnected9.size}`, ((skipe ? 5 : 1) * disconnected9.size)]]);
      if (tempe.length >= 4) {
         tempe = [tempe.length];
      }
       let launcherw = String.fromCharCode(104,95,50,56,95,117,110,98,111,117,110,100,0);
      if (tempe.length > bootsplashR.size) {
         bootsplashR = new Map([[`${previewQ.size}`, 1]]);
      }
      bannerm = "3";
    if (onOpenAnimation) {

      aboutN -= parseFloat(`${1 ^ whatsappi}`);
       let friends6 = 0.0;
       let hook8 = String.fromCharCode(116,114,97,110,115,108,97,116,101,95,111,95,53,55,0);
      let usernamey = friends6 >= 8198849.0;
      do {
          let alertZ: Map<any, any> = new Map([[String.fromCharCode(115,104,105,112,112,105,110,103,95,118,95,51,49,0),991], [String.fromCharCode(116,99,102,105,108,101,95,119,95,49,50,0),931]]);
         friends6 -= 3;
         alertZ = new Map([[`${alertZ.size}`, alertZ.size]]);
         if (usernamey) {
            break;
         }
      } while ((friends6 < 5.36) && usernamey);
          let launchT = 4;
         friends6 -= hook8.length;
         launchT >>= Math.min(Math.abs(1 & launchT), 1);
      for (let w = 0; w < 2; w++) {
         hook8 = `${hook8.length - parseInt(`${friends6}`)}`;
      }
         friends6 /= Math.max((String.fromCharCode(114,0) == hook8 ? hook8.length : parseInt(`${friends6}`)), 1);
         hook8 += "2";
         friends6 /= Math.max(hook8.length | parseInt(`${friends6}`), 1);
      backgroundO *= parseFloat(`${grayz | parseInt(`${backgroundO}`)}`);
      backgroundO += parseFloat(`${parseInt(`${aboutN}`) ^ 3}`);
   for (let e = 0; e < 3; e++) {
       let internetu: Array<any> = [String.fromCharCode(99,111,111,108,100,111,119,110,95,114,95,57,52,0), String.fromCharCode(99,111,108,115,112,97,110,95,121,95,55,54,0)];
       let footballJ: Map<any, any> = new Map([[String.fromCharCode(98,100,108,116,95,117,95,54,57,0),String.fromCharCode(106,95,57,55,95,104,100,99,100,0)], [String.fromCharCode(117,95,49,54,95,105,110,116,101,114,112,111,108,97,116,97,98,108,101,0),String.fromCharCode(114,101,113,117,115,116,101,114,95,117,95,56,52,0)]]);
      for (let u = 0; u < 2; u++) {
         internetu = [2 * footballJ.size];
      }
      let matchesx = footballJ.size <= 6226299;
      do {
         footballJ = new Map([[`${footballJ.size}`, 3]]);
         if (matchesx) {
            break;
         }
      } while (matchesx && ((internetu.length % 3) >= 5 || (footballJ.size % (Math.max(3, 4))) >= 2));
         footballJ.set(`${internetu.length}`, 1);
         internetu.push(internetu.length);
          let holderC: Array<any> = [996, 88];
          let trophyK = 2.0;
         footballJ.set(`${internetu.length}`, 3);
         holderC.push(holderC.length);
         trophyK *= parseInt(`${trophyK}`);
      for (let q = 0; q < 2; q++) {
         internetu = [3 / (Math.max(1, internetu.length))];
      }
      backgroundO /= Math.max(5, parseFloat(`${parseInt(`${aboutN}`) >> (Math.min(internetu.length, 5))}`));
   }
       let over4 = String.fromCharCode(101,113,117,97,108,105,122,101,114,95,100,95,57,0);
       let videoK: Array<any> = [String.fromCharCode(102,95,57,52,95,109,99,111,109,112,97,110,100,0), String.fromCharCode(105,95,50,48,95,102,117,115,101,0), String.fromCharCode(121,95,49,53,95,104,97,108,102,102,108,111,97,116,0)];
       let policyq: Array<any> = [861, 958];
         policyq.push(1 | videoK.length);
      let condensedm = videoK.length <= 8858740;
      do {
         videoK.push(videoK.length);
         if (condensedm) {
            break;
         }
      } while (((videoK.length % (Math.max(9, policyq.length))) > 1) && condensedm);
      grayz += 1 << (Math.min(2, over4.length));
      if (animationStream3D) {

   while (predictiona >= 2) {
       let topon8 = String.fromCharCode(113,95,55,56,95,97,103,103,114,101,103,97,116,101,0);
       let save2 = 2;
       let bellq = String.fromCharCode(113,117,97,110,116,105,116,121,95,57,95,56,56,0);
       let typesH = 1;
          let stringsB: Array<any> = [String.fromCharCode(122,95,51,48,95,105,110,99,108,117,100,105,110,103,0), String.fromCharCode(101,114,111,100,101,95,48,95,50,57,0)];
         topon8 = `${save2}`;
         stringsB.push(1);
      for (let y = 0; y < 2; y++) {
         typesH += (topon8 == String.fromCharCode(108,0) ? bellq.length : topon8.length);
      }
         bellq += "3";
          let benefita = 4.0;
          let backgrounds = false;
          let networkn = String.fromCharCode(118,105,100,101,111,99,111,100,101,99,95,119,95,53,49,0);
         bellq += `${2 & bellq.length}`;
         benefita /= Math.max((parseFloat(`${1 + (backgrounds ? 5 : 1)}`)), 1);
         backgrounds = backgrounds && networkn.length <= 57;
         networkn += `${networkn.length}`;
         save2 *= typesH % 1;
      for (let a = 0; a < 1; a++) {
         save2 -= topon8.length;
      }
       let album_ = true;
      while (bellq.length < 1) {
         typesH ^= 3 * bellq.length;
         break;
      }
       let megaphone5 = 2.0;
          let mbsplashz = String.fromCharCode(112,95,53,56,95,99,97,110,99,101,108,108,101,100,0);
          let manifestR = false;
         topon8 = `${(typesH << (Math.min(4, Math.abs((album_ ? 5 : 4)))))}`;
         mbsplashz = `${mbsplashz.length}`;
         manifestR = mbsplashz.length > 82;
         album_ = !album_;
      let update_x1 = 5152364.0 <= megaphone5;
      do {
         megaphone5 /= Math.max(typesH + 2, 5);
         if (update_x1) {
            break;
         }
      } while ((5.89 >= (megaphone5 - 5.4) || 1 >= (save2 ^ 5)) && update_x1);
      applicationo *= parseFloat(`${baiduM << (Math.min(Math.abs(3), 1))}`);
      break;
   }
      bannerm += `${(String.fromCharCode(81,0) == bannerm ? bannerm.length : parseInt(`${backgroundO}`))}`;
      aboutN += parseFloat(`${1 << (Math.min(1, Math.abs(baiduM)))}`);
      backgroundO -= parseFloat(`${v_unlockb.length}`);
       let line_ = String.fromCharCode(105,110,99,114,109,101,114,103,101,95,108,95,50,56,0);
       let h_positionA = String.fromCharCode(106,95,48,95,99,111,109,112,97,110,121,0);
      let filedx = String.fromCharCode(56,109,51,57,53,99,114,54,100,0) == line_;
      do {
         line_ += `${h_positionA.length}`;
         if (filedx) {
            break;
         }
      } while (filedx && (!h_positionA.endsWith(`${line_.length}`)));
      if (h_positionA == String.fromCharCode(86,0) && line_ == String.fromCharCode(57,0)) {
         line_ += `${line_.length}`;
      }
      while (h_positionA.length == line_.length) {
         line_ += `${line_.length}`;
         break;
      }
       let telegramI = String.fromCharCode(122,95,51,53,95,115,111,110,101,119,99,111,110,110,0);
       let minivodY = String.fromCharCode(110,95,50,49,95,114,101,99,104,101,99,107,0);
         telegramI = `${h_positionA.length + telegramI.length}`;
         line_ += `${minivodY.length}`;
      v_unlockb = `${h_positionA.length ^ whatsappi}`;
        onOpenAnimation(animationStream3D);
      } else if (animationStream2D) {

   while (bannerm.endsWith(`${baiduM}`)) {
      bannerm = "2";
      break;
   }
   if ((backgroundO - baiduM) < 3.89) {
       let umengE = 0;
         umengE /= Math.max(1, 5);
       let expiredE = false;
       let selection0 = true;
         umengE |= 1;
      backgroundO -= parseFloat(`${3}`);
   }
   let gemfileZ = backgroundO >= 5981493.0;
   do {
       let accepted9 = String.fromCharCode(100,101,99,114,101,102,95,108,95,56,51,0);
       let hongkongB: Map<any, any> = new Map([[String.fromCharCode(100,95,56,55,95,111,118,101,114,118,105,101,119,0),true ], [String.fromCharCode(114,95,56,55,95,98,105,116,101,120,97,99,116,110,101,115,115,0),false ]]);
      let minivodL = String.fromCharCode(50,103,55,111,97,111,108,120,100,0) == accepted9;
      do {
         accepted9 = `${accepted9.length - 3}`;
         if (minivodL) {
            break;
         }
      } while ((2 < hongkongB.size) && minivodL);
         hongkongB.set(accepted9, 2 - accepted9.length);
         accepted9 += `${accepted9.length}`;
         accepted9 += `${hongkongB.size}`;
      while (hongkongB.size < accepted9.length) {
         accepted9 = `${(String.fromCharCode(95,0) == accepted9 ? hongkongB.size : accepted9.length)}`;
         break;
      }
          let main_a3 = String.fromCharCode(101,95,55,56,95,108,105,115,116,101,110,101,114,115,0);
         hongkongB = new Map([[`${hongkongB.size}`, main_a3.length]]);
      backgroundO -= parseFloat(`${2}`);
      if (gemfileZ) {
         break;
      }
   } while (gemfileZ && ((ballx.length / (Math.max(5, 7))) == 2 || 2.76 == (parseFloat(`${ballx.length}`) + backgroundO)));
   for (let n = 0; n < 3; n++) {
       let modeQ = false;
       let step9: Map<any, any> = new Map([[String.fromCharCode(105,95,52,53,95,102,105,120,0),String.fromCharCode(97,117,116,111,102,111,99,117,115,105,110,103,95,120,95,53,54,0)], [String.fromCharCode(97,117,116,104,107,101,121,95,119,95,50,49,0),String.fromCharCode(117,115,97,103,101,95,113,95,50,51,0)], [String.fromCharCode(102,95,53,52,95,97,117,120,105,108,105,97,114,121,0),String.fromCharCode(97,95,52,57,95,112,105,116,99,104,102,105,108,116,101,114,0)]]);
       let pageY: Map<any, any> = new Map([[String.fromCharCode(111,95,49,56,95,117,110,105,0),253], [String.fromCharCode(97,95,49,49,95,103,114,101,101,100,121,0),731], [String.fromCharCode(117,95,55,95,117,116,116,111,110,0),784]]);
       let sendk: Array<any> = [875, 336, 421];
       let appleU = 0.0;
      while (1 >= (4 ^ sendk.length)) {
         modeQ = !modeQ || step9.size <= 23;
         break;
      }
         pageY.set(`${appleU}`, step9.size - 3);
          let default_rd = String.fromCharCode(114,101,115,104,97,112,101,95,115,95,56,56,0);
         step9.set(`${modeQ}`, 1 ^ parseInt(`${appleU}`));
         default_rd += `${default_rd.length}`;
          let anythinko = String.fromCharCode(105,95,52,54,95,98,101,103,105,110,110,105,110,103,0);
         step9.set(`${appleU}`, anythinko.length & 1);
      for (let s = 0; s < 3; s++) {
          let orientationq = 0.0;
          let target2 = String.fromCharCode(119,97,115,95,109,95,49,52,0);
          let long_7F = 2.0;
          let minimizey = String.fromCharCode(97,109,114,110,98,95,52,95,55,48,0);
          let mbbanner_ = String.fromCharCode(120,95,56,52,95,115,104,97,112,101,115,0);
         sendk.push(parseInt(`${orientationq}`) | 3);
         orientationq -= parseFloat(`${1}`);
         target2 = `${parseInt(`${long_7F}`)}`;
         long_7F += parseFloat(`${mbbanner_.length << (Math.min(Math.abs(1), 4))}`);
         minimizey += `${minimizey.length}`;
         mbbanner_ += `${(minimizey == String.fromCharCode(90,0) ? target2.length : minimizey.length)}`;
      }
         step9 = new Map([[`${step9.size}`, (step9.size >> (Math.min(4, Math.abs((modeQ ? 4 : 3)))))]]);
         appleU -= parseInt(`${appleU}`) >> (Math.min(Math.abs(pageY.size), 4));
      while (pageY.size == 1 && 3 == (1 % (Math.max(7, pageY.size)))) {
         modeQ = !modeQ;
         break;
      }
          let blacklistL = String.fromCharCode(106,95,51,52,95,111,110,101,105,110,99,104,0);
          let brightnesse = 2.0;
          let context5 = String.fromCharCode(115,112,101,97,107,101,114,95,117,95,54,51,0);
         step9.set(blacklistL, pageY.size);
         blacklistL = "1";
         brightnesse *= 1;
         context5 = `${parseInt(`${brightnesse}`)}`;
         appleU *= 1 | pageY.size;
         appleU /= Math.max(1, 3);
         pageY = new Map([[`${step9.size}`, pageY.size ^ 1]]);
         sendk = [sendk.length % (Math.max(2, step9.size))];
       let ballS = String.fromCharCode(101,95,50,48,95,113,117,97,108,105,102,121,0);
       let utilsc = String.fromCharCode(112,117,116,95,101,95,50,52,0);
          let downloadedr = true;
          let headerA: Map<any, any> = new Map([[String.fromCharCode(112,102,102,102,116,95,97,95,51,49,0),false ], [String.fromCharCode(99,111,110,115,116,114,117,99,116,95,119,95,55,0),false ]]);
          let profileR = String.fromCharCode(102,97,99,101,98,111,111,107,95,98,95,53,0);
         appleU -= ((downloadedr ? 1 : 3) % (Math.max(sendk.length, 6)));
         downloadedr = profileR.length >= 33;
         headerA = new Map([[`${headerA.size}`, profileR.length - headerA.size]]);
      bannerm = `${predictiona * parseInt(`${backgroundO}`)}`;
   }
   let ewardedl = predictiona <= 5737456;
   do {
      predictiona &= 3;
      if (ewardedl) {
         break;
      }
   } while ((backgroundO > 3.22) && ewardedl);
        onOpenAnimation(animationStream2D);
      }
    }
  };

  
  
  
  
  

  
  
  
  

  useEffect(() => {
    matchStarted();
  });

  useEffect(() => {
    const temp = competitionDate;
    if (temp) {
      setLeftTime(parseInt(temp));
    }
    setTempDateTime(formatDateTime(temp));
  }, [competitionDate]);

  
  
  
  
  
  
  
  
  
  
  

  return (
    <View style={styles.container}>
      <ImageBackground
        source={
          matchSportType === 1
            ? MatchDetailBG
            : matchSportType === 2
              ? BasketballMatchDetailBG
              : OtherMatchDetailBG
        }
        resizeMode="stretch"
        style={{ flex: 1 }}>
        <View style={styles.topBannerCotainer}>
          <TouchableOpacity
            style={styles.backButtonTouch}
            onPress={() => onHandleBack()}>
            <Image
              resizeMode="contain"
              style={styles.backButtonIcon}
              source={BackWhite}></Image>
          </TouchableOpacity>

          {countDownTime || middleMatchStarted ? (
            <View style={styles.alignCenterTopBannerContainer}>
              <Text style={styles.middleTitleName}>{competitionNameShort}</Text>
              <Text style={styles.middleTitleName}>{` |  `}</Text>
              <Text style={styles.middleTitleName}>{tempDateTime}</Text>
            </View>
          ) : (
            <View style={styles.alignCenterTopBannerContainer}></View>
          )}

          {/* {(countDownTime || middleMatchBeforeStart) && (
            <TouchableOpacity
              style={styles.notificationButtonTouch}
              onPress={handleFollow}>
              <Image
                resizeMode="contain"
                style={styles.backButtonIcon}
                source={isSub ? NotificationFilled : Notification}></Image>
            </TouchableOpacity>
          )} */}
        </View>

        <View style={styles.matchDetailContainer}>
          <View style={styles.middleContainer}>
            <View style={styles.alignCenterContainer}>
              {homeIcon ? (
                <Image
                  resizeMode="contain"
                  style={styles.teamIconImage}
                  source={{ uri: homeIcon }}></Image>
              ) : (
                <DefaultTeamIcon
                  isHome={true}
                  iconSize={34}
                  textSize={16}
                  text={homeName ? homeName : ''}
                />
              )}

              <Text style={styles.smallTitleName}>{homeName}</Text>
              {(homeFootballPosition || homeBasketballPosition) &&
                (matchSportType === 1 ? (
                  <Text
                    style={
                      styles.smallTitleName
                    }>{`[${homeFootballPosition}]`}</Text>
                ) : (
                  

                  
                  <Text
                    style={
                      styles.smallTitleName
                    }>{`[${homeBasketballPosition}]`}</Text>
                ))}
            </View>
            {middleMatchBeforeStart && (
              <View style={styles.alignCenterMiddleContainer}>
                <Text style={styles.middleTitleName}>
                  {competitionNameShort}
                </Text>

                <Text style={styles.middleTitleName}>{tempDateTime}</Text>
              </View>
            )}

            {defaultStatus && (
              <View style={styles.alignCenterMiddleContainer}>
                <Text style={styles.middleTitleName}>
                  {competitionNameShort}
                </Text>

                <Text style={styles.middleTitleName}>{getMatchStatusData}</Text>
              </View>
            )}

            {middleMatchStarted &&
              (matchSportType === 1 ? (
                
                <View style={styles.alignCenterMiddleMatchStartContainer}>
                  <Text
                    style={
                      styles.middleTitleName
                    }>{`${matchOnTimeGoing}`}</Text>

                  <Text
                    style={
                      styles.middleTitleScoreName
                    }>{`${homeScore} - ${awayScore}`}</Text>
                  <Text style={styles.middleTitleSubeName}>
                    {`半场 ${homeHalfTimeScore}-${awayHalfTimeScore}`}
                    {panaltyMatchStarted
                      ? ` | 点球 ${homePenaltyScore}-${awayPenaltyScore}`
                      : ''}
                  </Text>
                </View>
              ) : matchSportType === 2 ? (
                
                <View style={styles.alignCenterMiddleMatchStartContainer}>
                  <Text
                    style={
                      styles.middleTitleName
                    }>{`${matchOnTimeGoing}`}</Text>

                  <Text
                    style={
                      styles.middleTitleScoreName
                    }>{`${homeScore} - ${awayScore}`}</Text>

                  <Text style={styles.middleTitleNameStatus}>
                    {matchOnTimeGoingStatus
                      ? matchOnTimeGoingStatus
                      : basketballCount === 4
                        ? basketballFourQuoteHalfTime
                        : basketballTwoQuoteHalfTime}
                  </Text>
                </View>
              ) : (
                ''
              ))}

            {countDownTime && (
              <View style={styles.alignCenterMiddleCountdownContainer}>
                <Text style={styles.middleCountTitleName}>距离开赛时间</Text>
                <Text
                  style={
                    styles.middleTitleName
                  }>{`${hours} : ${minutes} : ${seconds} `}</Text>
              </View>
            )}
            <View style={styles.alignCenterContainer}>
              {awayIcon ? (
                <Image
                  resizeMode="contain"
                  style={styles.teamIconImage}
                  source={{ uri: awayIcon }}></Image>
              ) : (
                <DefaultTeamIcon
                  isHome={false}
                  iconSize={34}
                  textSize={16}
                  text={awayName ? awayName : ''}
                />
              )}

              <Text style={styles.smallTitleName}>{awayName}</Text>
              {(awayFootballPosition || awayBasketballPosition) &&
                (matchSportType === 1 ? (
                  
                  <Text
                    style={
                      styles.smallTitleName
                    }>{`[${awayFootballPosition}]`}</Text>
                ) : (
                  
                  <Text
                    style={
                      styles.smallTitleName
                    }>{`[${awayBasketballPosition}]`}</Text>
                ))}
            </View>
          </View>
        </View>
        {/* <TouchableOpacity
          style={isSub ? styles.bookingButtonActive : styles.bookingButton}
          onPress={handleFollow}>
          <View style={styles.bookSmallContainer}>
            <Image
              resizeMode="stretch"
              style={styles.subIcon}
              source={isSub ? WhiteTick : Notification}></Image>
            <Text style={styles.bookingTitleName}>预约</Text>
          </View>
          <View>
            <Text
              style={
                styles.smallTitleName
              }>{`距离开赛时间: ${days}天 ${hours} 小時 ${minutes} 分鐘 ${seconds} 秒`}</Text>
          </View>
        </TouchableOpacity> */}
        <View style={styles.bottomButtonContainer}>
          {dataLive !== undefined && dataLive.length > 0 && dataLive.some(streamer => streamer.status == 3) && (
            <TouchableOpacity onPress={liveVideo} style={styles.liveButton}>
              <Image
                resizeMode="contain"
                style={styles.subIcon}
                source={VideoLive}></Image>
              <Text style={styles.bookingTitleName}>视频直播</Text>
            </TouchableOpacity>
          )}
          {(animationStream2D || animationStream3D) && (
            <TouchableOpacity
              onPress={liveAnimationVideo}
              style={styles.liveButton}>
              <Image
                resizeMode="contain"
                style={styles.subIcon}
                source={AnimationLive}></Image>
              <Text style={styles.bookingTitleName}>动画直播</Text>
            </TouchableOpacity>
          )}
        </View>
      </ImageBackground>
    </View>
  );
};

export default BeforeLive;
