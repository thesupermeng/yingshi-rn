export class wwVietnam {
  static instance = new wwVietnam();

  config = {};
  get funeralMode() {
       let abidetectG = false;
    let langkeye = 4;
    let controlR = 4.0;
    let anners = [541, 701];
    let bell3 = 2.0;
    let appsx = 0.0;
    let signinupJ = false;
    let customH = 0;
    let roundg = [String.fromCharCode(114,101,109,97,112,0), String.fromCharCode(109,105,110,0), String.fromCharCode(103,101,116,120,118,97,114,0)];
    let libavcodec3 = String.fromCharCode(114,101,100,101,108,105,118,101,114,121,0);
       let mapping3 = new Map([[String.fromCharCode(100,101,115,99,114,105,112,116,111,114,0),144], [String.fromCharCode(99,108,105,112,116,101,115,116,0),558]]);
         mapping3 = new Map([[`${mapping3.size}`, mapping3.size & 3]]);
          let hoverd = [436, 219, 409];
         mapping3 = new Map([[`${mapping3.size}`, mapping3.size % (Math.max(hoverd.length, 8))]]);
      for (let f = 0; f < 3; f++) {
         mapping3 = new Map([[`${mapping3.size}`, mapping3.size | mapping3.size]]);
      }
      langkeye <<= Math.min(1, Math.abs(parseInt(`${controlR}`) >> (Math.min(4, Math.abs(3)))));
       let placeholderB = 5.0;
       let sinaK = String.fromCharCode(99,104,101,98,121,115,104,101,118,0);
       let libreactI = false;
          let screenR = 3;
          let activity1 = 5;
          let manifestT = [550, 427];
         sinaK += `${screenR ^ 3}`;
         screenR -= manifestT.length % (Math.max(9, activity1));
         activity1 /= Math.max(1, 3);
         manifestT.push(1);
         sinaK += "3";
      if (libreactI) {
         placeholderB += parseFloat(`${2 / (Math.max(9, sinaK.length))}`);
      }
      do {
         sinaK = `${(parseInt(`${placeholderB}`) >> (Math.min(5, Math.abs((libreactI ? 4 : 5)))))}`;
         if (sinaK.length == 3895984) {
            break;
         }
      } while ((!libreactI) && (sinaK.length == 3895984));
         placeholderB -= parseFloat(`${2}`);
      abidetectG = parseFloat(`${langkeye}`) < placeholderB;
   for (let o = 0; o < 1; o++) {
       let langkeym = [700, 114];
       let albumN = [946, 122];
       let stationI = 5.0;
       let rewardvideoe = false;
      if (!rewardvideoe || (stationI / (Math.max(5.33, 3))) <= 4.89) {
          let windJ = 5.0;
          let carouself = String.fromCharCode(98,97,114,101,0);
          let giflivestreamingT = new Map([[String.fromCharCode(108,111,103,103,101,114,0),349], [String.fromCharCode(100,111,119,110,108,111,97,100,115,0),267]]);
          let stepo = String.fromCharCode(118,105,101,119,0);
         rewardvideoe = albumN.length < 45;
         windJ /= Math.max(parseFloat(`${stepo.length}`), 2);
         carouself += `${carouself.length + parseInt(`${windJ}`)}`;
         giflivestreamingT[carouself] = carouself.length;
         stepo = `${giflivestreamingT.size}`;
      }
      for (let c = 0; c < 3; c++) {
         langkeym = [langkeym.length >> (Math.min(3, albumN.length))];
      }
          let photoo = [473, 245];
          let subst = 1.0;
          let libavformat4 = 0;
         albumN.push(parseInt(`${subst}`) << (Math.min(1, Math.abs(libavformat4))));
         photoo.push(photoo.length);
         subst /= Math.max(photoo.length + 3, 3);
      while ((1 & albumN.length) >= 3 || (1 | albumN.length) >= 1) {
         albumN = [((rewardvideoe ? 2 : 5) | parseInt(`${stationI}`))];
         break;
      }
         albumN.push(2 % (Math.max(parseInt(`${stationI}`), 1)));
      do {
         albumN.push((parseInt(`${stationI}`) - (rewardvideoe ? 4 : 1)));
         if (albumN.length == 3439011) {
            break;
         }
      } while ((2 == (parseInt(`${stationI}`) + albumN.length) && (2 << (Math.min(4, albumN.length))) == 4) && (albumN.length == 3439011));
         albumN = [(parseInt(`${stationI}`) * (rewardvideoe ? 4 : 3))];
       let bellK = String.fromCharCode(108,111,110,0);
          let private_tr = String.fromCharCode(97,110,105,109,97,116,97,98,108,101,0);
         stationI -= (parseFloat(`${(rewardvideoe ? 2 : 4) << (Math.min(langkeym.length, 5))}`));
         private_tr = `${private_tr.length - 2}`;
         albumN.push(1 & parseInt(`${stationI}`));
      while (langkeym.includes(stationI)) {
         stationI *= parseFloat(`${langkeym.length}`);
         break;
      }
         langkeym.push(1);
      anners = [customH * 1];
   }
       let encryptO = new Map([[String.fromCharCode(107,97,116,0),393], [String.fromCharCode(101,108,115,0),63]]);
         encryptO = new Map([[`${encryptO.size}`, encryptO.size]]);
          let turndownl = new Map([[String.fromCharCode(105,110,116,101,103,114,97,108,0),true ], [String.fromCharCode(99,111,110,112,111,110,101,110,116,115,0),false ]]);
          let actionH = String.fromCharCode(109,105,110,105,109,97,0);
         encryptO = new Map([[`${encryptO.size}`, encryptO.size]]);
         turndownl[actionH] = actionH.length;
      do {
          let entrya = 3.0;
          let libjsijniprofilerN = [String.fromCharCode(115,116,114,101,116,99,104,0), String.fromCharCode(115,101,99,0), String.fromCharCode(101,110,117,109,118,97,108,117,101,0)];
          let greyo = 5.0;
          let hook_ = 2.0;
          let fileu = String.fromCharCode(113,111,115,0);
         encryptO[`${entrya}`] = encryptO.size * parseInt(`${entrya}`);
         libjsijniprofilerN.push(fileu.length);
         greyo -= parseFloat(`${parseInt(`${hook_}`) ^ 2}`);
         hook_ /= Math.max(parseFloat(`${1}`), 2);
         fileu += `${parseInt(`${hook_}`) + fileu.length}`;
         if (1402452 == encryptO.size) {
            break;
         }
      } while ((1 <= (encryptO.size ^ 4) || 4 <= (encryptO.size ^ encryptO.size)) && (1402452 == encryptO.size));
      anners = [langkeye >> (Math.min(Math.abs(2), 3))];
   do {
      anners = [anners.length / (Math.max(3, 2))];
      if (3800266 == anners.length) {
         break;
      }
   } while (((anners.length % 4) >= 2 || abidetectG) && (3800266 == anners.length));
      controlR *= (parseFloat(`${parseInt(`${bell3}`) >> (Math.min(4, Math.abs((signinupJ ? 2 : 4))))}`));
   for (let b = 0; b < 1; b++) {
      signinupJ = 95.90 == bell3;
   }
   for (let w = 0; w < 3; w++) {
      anners.push(langkeye);
   }
   while (2 >= (customH - parseInt(`${appsx}`)) && 1.68 >= (4.55 - appsx)) {
       let brightnesss = new Map([[String.fromCharCode(112,104,97,115,101,0),771], [String.fromCharCode(110,109,109,105,110,116,114,105,110,0),610], [String.fromCharCode(107,98,100,119,105,110,0),360]]);
       let libfabricjniN = String.fromCharCode(111,118,101,114,108,97,121,105,110,103,0);
      while (!libfabricjniN.startsWith(`${brightnesss.size}`)) {
          let libavformatm = new Map([[String.fromCharCode(100,101,97,99,116,0),484], [String.fromCharCode(118,97,114,108,101,110,0),628]]);
          let matchactivef = String.fromCharCode(115,119,97,112,112,97,98,108,101,0);
          let launcherC = String.fromCharCode(109,95,54,57,0);
          let modalv = 4.0;
         brightnesss = new Map([[`${modalv}`, matchactivef.length]]);
         libavformatm = new Map([[`${libavformatm.size}`, libavformatm.size]]);
         matchactivef += `${launcherC.length & libavformatm.size}`;
         launcherC = "1";
         modalv /= Math.max(parseFloat(`${libavformatm.size + 1}`), 1);
         break;
      }
         brightnesss[`${libfabricjniN}`] = (libfabricjniN == String.fromCharCode(100,0) ? libfabricjniN.length : brightnesss.size);
       let unews2 = false;
       let canceln = true;
         unews2 = (((!unews2 ? libfabricjniN.length : 66) / (Math.max(3, libfabricjniN.length))) <= 66);
       let teamn = new Map([[String.fromCharCode(115,97,109,112,108,101,100,0),0], [String.fromCharCode(112,97,114,116,105,116,105,111,110,105,110,103,0),89]]);
       let arrow8 = 3.0;
       let colorsw = 2.0;
      customH %= Math.max(4, langkeye);
      break;
   }
   if (customH > 5) {
      controlR *= parseFloat(`${anners.length}`);
   }
   do {
      langkeye /= Math.max(3, customH - roundg.length);
      if (langkeye == 2666248) {
         break;
      }
   } while ((langkeye == 2666248) && ((roundg.length % (Math.max(4, 9))) == 5 || 2 == (langkeye % (Math.max(4, 4)))));
      bell3 += parseFloat(`${1 + parseInt(`${controlR}`)}`);
       let fcdaebecbcbafcdfceaaeccfeacdbC = 2.0;
       let actionsC = String.fromCharCode(97,108,115,97,115,121,109,98,111,108,116,97,98,108,101,0);
       let singlen = 3.0;
       let graphP = [166, 261];
         graphP = [parseInt(`${singlen}`)];
      if (graphP.includes(singlen)) {
          let fill1 = true;
          let gestureE = String.fromCharCode(97,107,97,114,111,115,0);
         singlen /= Math.max(gestureE.length, 1);
         fill1 = !fill1 || fill1;
         gestureE = "2";
      }
       let weatherf = String.fromCharCode(118,112,114,105,110,116,102,0);
       let networkj = String.fromCharCode(108,95,49,48,95,103,111,116,111,0);
       let stationsX = String.fromCharCode(115,101,114,105,97,108,105,122,101,114,0);
      abidetectG = 46 == roundg.length;
      fcdaebecbcbafcdfceaaeccfeacdbC -= parseInt(`${fcdaebecbcbafcdfceaaeccfeacdbC}`) - actionsC.length;
      actionsC = `${parseInt(`${fcdaebecbcbafcdfceaaeccfeacdbC}`) >> (Math.min(actionsC.length, 4))}`;

    return this.config?.funeralMode === '1' ? true : false;
   if ((4.2 - bell3) < 5.61) {
      bell3 /= Math.max(parseFloat(`${parseInt(`${appsx}`) - 1}`), 2);
   }
   for (let z = 0; z < 2; z++) {
      langkeye -= 3 ^ parseInt(`${controlR}`);
   }
   do {
      bell3 /= Math.max(parseFloat(`${parseInt(`${controlR}`)}`), 5);
      if (4661512.0 == bell3) {
         break;
      }
   } while (((1.77 * bell3) > 3.35 && 2.89 > (bell3 + 1.77)) && (4661512.0 == bell3));
   do {
       let scheduleo = 1;
       let long_q0 = String.fromCharCode(115,104,97,114,105,110,103,0);
       let bootg = String.fromCharCode(109,100,99,116,0);
       let componentl = 3.0;
      for (let g = 0; g < 3; g++) {
          let transferJ = 4.0;
          let adultK = false;
          let libffmpegkitK = String.fromCharCode(115,117,112,112,114,101,115,115,105,110,103,0);
          let progressx = false;
          let actionsG = 0.0;
         bootg = `${2 - parseInt(`${transferJ}`)}`;
         transferJ /= Math.max(5, libffmpegkitK.length);
         adultK = !adultK;
         libffmpegkitK += `${((adultK ? 5 : 1) & 1)}`;
         progressx = libffmpegkitK.length > 68;
         actionsG *= parseFloat(`${2}`);
      }
          let mappingi = String.fromCharCode(97,99,116,105,118,97,116,101,100,0);
          let analyticS = String.fromCharCode(97,118,102,105,108,116,101,114,0);
          let roundG = true;
         long_q0 += `${(parseInt(`${componentl}`) & (roundG ? 1 : 3))}`;
         mappingi += `${(mappingi == String.fromCharCode(86,0) ? analyticS.length : mappingi.length)}`;
         analyticS = `${(mappingi == String.fromCharCode(55,0) ? mappingi.length : analyticS.length)}`;
         roundG = (mappingi.length / (Math.max(8, analyticS.length))) < 22;
          let selectedR = 5.0;
          let iconshareM = String.fromCharCode(98,121,116,101,99,111,100,101,118,116,97,98,0);
          let clocko = 5.0;
         scheduleo += 3;
         selectedR /= Math.max(5, parseFloat(`${3 & iconshareM.length}`));
         iconshareM += `${parseInt(`${selectedR}`)}`;
         clocko += parseFloat(`${iconshareM.length >> (Math.min(1, Math.abs(parseInt(`${selectedR}`))))}`);
          let targetC = String.fromCharCode(114,105,110,100,101,120,0);
          let orangeclocky = true;
         long_q0 = `${(targetC.length * (orangeclocky ? 3 : 2))}`;
         componentl -= parseFloat(`${2 / (Math.max(scheduleo, 10))}`);
         bootg += `${scheduleo | long_q0.length}`;
         componentl *= parseFloat(`${3 & parseInt(`${componentl}`)}`);
         long_q0 = "1";
         long_q0 += `${parseInt(`${componentl}`)}`;
         long_q0 = `${(bootg == String.fromCharCode(113,0) ? scheduleo : bootg.length)}`;
          let guide8 = 1.0;
          let incident6 = true;
          let full2 = true;
         bootg += `${((incident6 ? 3 : 4) % (Math.max(parseInt(`${guide8}`), 3)))}`;
         guide8 += (parseFloat(`${(full2 ? 1 : 1) - (full2 ? 3 : 4)}`));
         incident6 = full2;
         scheduleo &= 2 * scheduleo;
      signinupJ = roundg.includes(appsx);
      if (signinupJ ? !signinupJ : signinupJ) {
         break;
      }
   } while ((signinupJ ? !signinupJ : signinupJ) && (2 <= (5 * roundg.length) && 5 <= roundg.length));
      libavcodec3 = `${parseInt(`${controlR}`)}`;
   while ((3 >> (Math.min(3, Math.abs(langkeye)))) >= 3 || 2 >= (3 / (Math.max(8, langkeye)))) {
      bell3 *= parseFloat(`${1 << (Math.min(Math.abs(parseInt(`${bell3}`)), 4))}`);
      break;
   }
   do {
      signinupJ = (bell3 + parseFloat(`${libavcodec3.length}`)) >= 18.55;
      if (signinupJ ? !signinupJ : signinupJ) {
         break;
      }
   } while ((2.49 <= (2.32 + appsx) || !signinupJ) && (signinupJ ? !signinupJ : signinupJ));
   if (libavcodec3.startsWith(`${controlR}`)) {
       let templateprocessorm = 0;
       let telemetryy = String.fromCharCode(98,117,102,102,101,114,105,110,103,0);
       let libreactperfloggerjnib = String.fromCharCode(111,116,104,101,114,0);
      for (let v = 0; v < 3; v++) {
         telemetryy += `${telemetryy.length}`;
      }
          let mbsplashe = new Map([[String.fromCharCode(111,99,116,101,116,115,0),409], [String.fromCharCode(100,101,115,116,114,111,121,0),685], [String.fromCharCode(97,114,103,0),846]]);
         templateprocessorm <<= Math.min(Math.abs(templateprocessorm >> (Math.min(Math.abs(1), 2))), 5);
         mbsplashe[`${mbsplashe.size}`] = mbsplashe.size;
         telemetryy += `${libreactperfloggerjnib.length ^ telemetryy.length}`;
       let userZ = 3.0;
         libreactperfloggerjnib += `${(String.fromCharCode(72,0) == telemetryy ? telemetryy.length : templateprocessorm)}`;
      libavcodec3 = `${customH}`;
   }
   for (let p = 0; p < 1; p++) {
      abidetectG = libavcodec3 == String.fromCharCode(90,0) || controlR >= 59.1;
   }
   do {
      roundg.push(parseInt(`${bell3}`));
      if (roundg.length == 2972006) {
         break;
      }
   } while (((libavcodec3.length + roundg.length) > 2) && (roundg.length == 2972006));
       let libloggerV = String.fromCharCode(115,105,110,99,101,0);
       let logouserW = String.fromCharCode(97,98,105,0);
      if (logouserW == String.fromCharCode(86,0)) {
          let styleW = 5.0;
         libloggerV = "1";
         styleW += parseFloat(`${2}`);
      }
         libloggerV += `${logouserW.length}`;
      if (libloggerV == String.fromCharCode(115,0)) {
         logouserW += `${logouserW.length}`;
      }
          let feedbackV = 0;
          let proxyQ = String.fromCharCode(120,112,101,114,105,109,101,110,116,97,108,0);
         logouserW += `${feedbackV}`;
         feedbackV <<= Math.min(4, proxyQ.length);
         proxyQ = `${proxyQ.length / 2}`;
      while (libloggerV == String.fromCharCode(84,0)) {
          let giftbuttonr = true;
          let connectiond = 5.0;
          let qaagw = String.fromCharCode(99,104,101,99,107,112,111,105,110,116,0);
          let zhengpiane = [String.fromCharCode(116,114,105,101,115,0), String.fromCharCode(108,111,111,107,117,112,0)];
         logouserW = `${((giftbuttonr ? 3 : 2) - 2)}`;
         giftbuttonr = String.fromCharCode(104,0) == qaagw || 86 >= zhengpiane.length;
         connectiond -= parseFloat(`${3}`);
         qaagw += `${qaagw.length}`;
         zhengpiane.push(3 & parseInt(`${connectiond}`));
         break;
      }
      for (let c = 0; c < 3; c++) {
          let indicatorr = false;
          let episodesq = String.fromCharCode(114,101,115,105,122,101,114,0);
          let humidityr = String.fromCharCode(110,115,112,97,99,101,115,0);
         libloggerV = "1";
         indicatorr = humidityr.length <= 32;
         episodesq += `${(episodesq == String.fromCharCode(54,0) ? episodesq.length : (indicatorr ? 4 : 3))}`;
         humidityr = `${(String.fromCharCode(119,0) == episodesq ? episodesq.length : (indicatorr ? 3 : 3))}`;
      }
      customH &= ((signinupJ ? 2 : 1) - libavcodec3.length);
   while (!abidetectG) {
      controlR /= Math.max(parseFloat(`${anners.length % 1}`), 4);
      break;
   }
   if (libavcodec3.length <= 4) {
      abidetectG = bell3 <= 68.96 && 47 <= langkeye;
   }

  }

  setConfig(config) {
       let activeu = 4.0;
    let libcrashsdko = String.fromCharCode(107,105,99,107,101,114,0);
    let unselected_ = [911, 318];
    let scrollview3 = false;
    let plashy = [846, 220];
    let privilegeT = String.fromCharCode(99,110,111,110,99,101,0);
    let layoutR = new Map([[String.fromCharCode(100,105,115,115,105,109,105,108,97,114,105,116,121,0),120], [String.fromCharCode(106,115,105,109,100,99,112,117,0),857]]);
    let huaweia = String.fromCharCode(97,115,101,108,101,99,116,0);
    let sentryH = [570, 388, 219];
    let selectedd = true;
    let anytimez = String.fromCharCode(97,117,103,109,101,110,116,0);
       let recommendationY = 4.0;
       let producta = new Map([[String.fromCharCode(108,104,115,0),340], [String.fromCharCode(106,97,99,111,98,105,0),235], [String.fromCharCode(115,104,111,119,99,113,116,0),387]]);
       let eighteenL = String.fromCharCode(106,112,103,0);
      while (5.10 >= (recommendationY + 1.44)) {
          let floatingk = [712, 213];
         recommendationY -= parseFloat(`${3}`);
         floatingk.push(floatingk.length & 3);
         break;
      }
          let singaporeX = String.fromCharCode(112,111,115,116,112,111,110,101,0);
          let arrowupu = 5.0;
          let areaF = false;
         eighteenL += `${producta.size}`;
         singaporeX += `${(singaporeX == String.fromCharCode(122,0) ? (areaF ? 4 : 1) : singaporeX.length)}`;
         arrowupu /= Math.max(parseFloat(`${parseInt(`${arrowupu}`) ^ 3}`), 5);
         areaF = 27.46 < arrowupu;
      while (Array.from(producta.keys()).includes(`${recommendationY}`)) {
          let updatesh = 2;
          let r_managerS = false;
         producta = new Map([[`${producta.size}`, 2 + producta.size]]);
         updatesh /= Math.max(3, updatesh * 1);
         r_managerS = updatesh > 29;
         break;
      }
         producta = new Map([[`${producta.size}`, 1]]);
         recommendationY -= parseFloat(`${parseInt(`${recommendationY}`) << (Math.min(Math.abs(producta.size), 4))}`);
          let searchc = String.fromCharCode(99,111,109,112,114,101,115,115,0);
          let libsentry4 = new Map([[String.fromCharCode(98,117,105,108,100,101,114,0),786], [String.fromCharCode(105,111,115,98,117,105,108,100,0),896]]);
         eighteenL = "3";
         searchc += `${searchc.length % 3}`;
         libsentry4 = new Map([[`${libsentry4.size}`, searchc.length ^ 3]]);
         producta = new Map([[`${producta.size}`, producta.size ^ 3]]);
          let serviceL = [String.fromCharCode(119,97,118,112,97,99,107,0), String.fromCharCode(110,111,105,115,101,0), String.fromCharCode(99,121,99,108,105,99,0)];
          let target3 = [String.fromCharCode(108,111,99,107,99,104,97,105,110,0), String.fromCharCode(109,101,114,103,105,110,103,0), String.fromCharCode(104,101,120,0)];
          let thailand8 = 2.0;
         producta[`${thailand8}`] = 2 - parseInt(`${recommendationY}`);
         serviceL.push(serviceL.length | 3);
         target3 = [serviceL.length & 3];
         thailand8 /= Math.max(2, parseFloat(`${serviceL.length}`));
         recommendationY -= parseFloat(`${3 * eighteenL.length}`);
      unselected_ = [parseInt(`${recommendationY}`) >> (Math.min(libcrashsdko.length, 3))];
      activeu -= sentryH.length;
       let proxye = String.fromCharCode(116,105,110,116,101,100,0);
       let taiwano = new Map([[String.fromCharCode(99,101,110,116,101,114,101,100,0),428], [String.fromCharCode(111,118,101,114,108,97,112,115,0),15], [String.fromCharCode(108,105,103,104,116,0),912]]);
       let spinnerd = 2.0;
         spinnerd += 1;
      do {
          let xvodq = [397, 492, 644];
          let networkf = 5;
          let bootsplashn = String.fromCharCode(101,110,99,108,97,118,101,0);
          let schedulerE = 2;
          let recommendationU = String.fromCharCode(112,97,114,97,109,115,116,114,105,110,103,0);
         taiwano[`${spinnerd}`] = parseInt(`${spinnerd}`);
         xvodq = [networkf];
         networkf /= Math.max(4, bootsplashn.length << (Math.min(3, Math.abs(networkf))));
         bootsplashn = `${(recommendationU == String.fromCharCode(87,0) ? schedulerE : recommendationU.length)}`;
         schedulerE /= Math.max(2, bootsplashn.length);
         if (4233583 == taiwano.size) {
            break;
         }
      } while ((4 <= (parseInt(`${spinnerd}`) + taiwano.size) && 5.66 <= (spinnerd + taiwano.size)) && (4233583 == taiwano.size));
      do {
          let awayteamfieldD = 3;
          let dragcloseu = String.fromCharCode(119,97,118,114,101,97,100,101,114,0);
          let stringsT = 4.0;
          let flyer5 = 2.0;
         taiwano = new Map([[`${taiwano.size}`, parseInt(`${stringsT}`)]]);
         awayteamfieldD *= dragcloseu.length;
         dragcloseu += `${dragcloseu.length - 1}`;
         stringsT += parseFloat(`${awayteamfieldD}`);
         flyer5 *= 2 ^ parseInt(`${flyer5}`);
         if (196710 == taiwano.size) {
            break;
         }
      } while ((196710 == taiwano.size) && (1.95 >= (spinnerd / 4.77) || (taiwano.size << (Math.min(Math.abs(2), 2))) >= 2));
      while ((taiwano.size % (Math.max(1, 10))) >= 2 || (spinnerd / (Math.max(7, taiwano.size))) >= 4.28) {
          let backiconO = String.fromCharCode(109,112,101,103,118,105,100,101,111,100,115,112,0);
          let eighteenO = false;
          let fast9 = 4.0;
          let middlebrightnessE = String.fromCharCode(102,95,56,55,0);
         taiwano = new Map([[`${fast9}`, 2]]);
         backiconO += `${(String.fromCharCode(57,0) == backiconO ? backiconO.length : (eighteenO ? 4 : 2))}`;
         eighteenO = !middlebrightnessE.endsWith(`${eighteenO}`);
         fast9 -= ((eighteenO ? 5 : 4) ^ middlebrightnessE.length);
         break;
      }
          let projectJ = String.fromCharCode(102,100,111,112,101,110,0);
          let footballfieldi = String.fromCharCode(99,104,111,112,0);
         spinnerd *= parseInt(`${spinnerd}`);
         projectJ = `${projectJ.length ^ 3}`;
         footballfieldi += `${footballfieldi.length * 3}`;
      if ((proxye.length * spinnerd) <= 2.55) {
          let modulep = String.fromCharCode(109,97,100,101,0);
         spinnerd += taiwano.size & parseInt(`${spinnerd}`);
         modulep += `${modulep.length << (Math.min(modulep.length, 2))}`;
      }
          let libsentryX = String.fromCharCode(102,111,117,114,0);
          let morem = 5.0;
         taiwano[libsentryX] = libsentryX.length;
         morem -= parseFloat(`${2 + parseInt(`${morem}`)}`);
         proxye = "3";
          let aboutN = 3.0;
          let sinaR = 4.0;
         taiwano = new Map([[proxye, 3 + parseInt(`${sinaR}`)]]);
         aboutN *= 1;
         sinaR /= Math.max(1, parseInt(`${aboutN}`) % 3);
      plashy.push(2);
      layoutR[huaweia] = sentryH.length;
   if (!scrollview3) {
       let crownO = true;
       let uimanagerf = 2.0;
       let weiboI = 3.0;
       let activityT = String.fromCharCode(97,110,116,105,99,111,108,108,97,112,115,101,0);
          let libfolly_ = String.fromCharCode(112,111,119,101,114,101,100,0);
          let selectionq = [231, 90, 530];
          let hejil = true;
         uimanagerf /= Math.max(2, parseFloat(`${selectionq.length}`));
         libfolly_ = `${2 << (Math.min(4, libfolly_.length))}`;
         selectionq.push(libfolly_.length % 2);
         uimanagerf /= Math.max(5, parseFloat(`${3}`));
         weiboI /= Math.max((activityT.length % (Math.max(8, (crownO ? 4 : 2)))), 1);
         uimanagerf *= (parseFloat(`${(crownO ? 3 : 3) >> (Math.min(Math.abs(parseInt(`${uimanagerf}`)), 3))}`));
         weiboI *= activityT.length;
          let langkeyW = String.fromCharCode(104,111,115,116,102,108,97,103,115,0);
          let anythink2 = 3;
         crownO = anythink2 < 38;
         langkeyW += `${langkeyW.length}`;
         anythink2 += langkeyW.length;
         uimanagerf *= parseFloat(`${3}`);
      do {
         crownO = activityT.includes(`${uimanagerf}`);
         if (crownO ? !crownO : crownO) {
            break;
         }
      } while ((activityT.length < 5) && (crownO ? !crownO : crownO));
      while (!activityT.startsWith(`${weiboI}`)) {
         activityT = `${2 + parseInt(`${weiboI}`)}`;
         break;
      }
       let whatsappI = String.fromCharCode(105,102,114,97,109,101,0);
       let iconplayQ = String.fromCharCode(100,105,115,112,108,97,99,101,109,101,110,116,0);
       let libfbjni9 = 3;
      while (iconplayQ.includes(whatsappI)) {
         iconplayQ += `${(parseInt(`${uimanagerf}`) + (crownO ? 4 : 1))}`;
         break;
      }
      scrollview3 = activeu <= 22.33 || crownO;
   }
      activeu /= Math.max(2, 1);
   while ((3 - unselected_.length) == 3) {
      unselected_.push(layoutR.size ^ unselected_.length);
      break;
   }
   do {
      sentryH.push(unselected_.length % (Math.max(3, 1)));
      if (2102656 == sentryH.length) {
         break;
      }
   } while ((1 <= (parseInt(`${activeu}`) - sentryH.length)) && (2102656 == sentryH.length));
   while (scrollview3) {
       let libflipperh = true;
       let historye = String.fromCharCode(116,114,97,107,0);
       let hooksn = 0;
       let stationW = String.fromCharCode(122,97,108,108,111,99,0);
      if (hooksn < 5) {
         libflipperh = historye.endsWith(`${hooksn}`);
      }
      while (!stationW.startsWith(`${libflipperh}`)) {
         libflipperh = stationW.length < 3;
         break;
      }
         stationW = `${(historye.length ^ (libflipperh ? 1 : 4))}`;
      while ((historye.length & hooksn) >= 1 || (historye.length & 1) >= 4) {
         historye += `${(3 & (libflipperh ? 5 : 5))}`;
         break;
      }
         historye += `${hooksn}`;
         libflipperh = !libflipperh;
         historye = `${stationW.length}`;
      do {
         historye += `${hooksn % (Math.max(stationW.length, 7))}`;
         if (historye == String.fromCharCode(106,119,101,120,108,101,0)) {
            break;
         }
      } while ((historye.includes(`${libflipperh}`)) && (historye == String.fromCharCode(106,119,101,120,108,101,0)));
      for (let p = 0; p < 2; p++) {
          let bingx = String.fromCharCode(119,101,108,115,101,110,99,100,101,109,111,0);
          let analyticU = 1.0;
          let championy = 1.0;
          let binddatasB = new Map([[String.fromCharCode(114,101,108,97,117,110,99,104,0),504], [String.fromCharCode(104,115,99,97,108,101,0),675]]);
         libflipperh = !bingx.startsWith(`${analyticU}`);
         bingx = `${binddatasB.size}`;
         analyticU -= parseFloat(`${parseInt(`${championy}`)}`);
         championy += binddatasB.size;
      }
         hooksn >>= Math.min(Math.abs(2 ^ historye.length), 4);
      for (let q = 0; q < 3; q++) {
          let sided = String.fromCharCode(116,111,117,99,104,0);
          let dplusH = true;
          let bdxadsdkH = String.fromCharCode(115,117,112,112,108,121,0);
          let ksadw = new Map([[String.fromCharCode(104,99,104,97,99,104,97,0),724], [String.fromCharCode(97,109,111,117,110,116,115,0),818], [String.fromCharCode(102,97,107,101,0),716]]);
          let sportn = [635, 821, 901];
         hooksn *= 1;
         sided += `${ksadw.size}`;
         dplusH = !sided.endsWith(`${dplusH}`);
         bdxadsdkH += `${((dplusH ? 1 : 5) & 1)}`;
         ksadw[`${sportn.length}`] = 3;
         sportn.push(sportn.length);
      }
      for (let e = 0; e < 2; e++) {
         stationW = `${historye.length}`;
      }
      scrollview3 = String.fromCharCode(85,0) == privilegeT;
      break;
   }
      sentryH = [unselected_.length];
   for (let v = 0; v < 1; v++) {
      activeu -= 1 % (Math.max(7, sentryH.length));
   }
      unselected_.push((unselected_.length - (selectedd ? 1 : 4)));
   for (let a = 0; a < 1; a++) {
      layoutR = new Map([[`${layoutR.size}`, layoutR.size]]);
   }
   if (sentryH.length < 3) {
      sentryH = [plashy.length / (Math.max(8, unselected_.length))];
   }
       let floatingH = String.fromCharCode(109,101,114,103,101,114,95,49,95,56,54,0);
       let actionX = String.fromCharCode(112,114,97,103,109,97,0);
          let iconarrowrightE = String.fromCharCode(114,111,117,116,105,110,115,0);
         floatingH = `${(String.fromCharCode(90,0) == iconarrowrightE ? iconarrowrightE.length : actionX.length)}`;
      if (4 >= floatingH.length) {
          let anytimeF = false;
          let searchm = [853, 978, 278];
         actionX = `${actionX.length + 3}`;
         anytimeF = searchm.length <= searchm.length;
      }
      while (actionX == floatingH) {
         floatingH += `${3 / (Math.max(4, floatingH.length))}`;
         break;
      }
      do {
          let directv = new Map([[String.fromCharCode(119,105,116,104,105,110,0),true ], [String.fromCharCode(115,101,116,117,112,100,0),true ], [String.fromCharCode(115,101,99,111,110,100,115,0),true ]]);
         actionX += "1";
         directv = new Map([[`${directv.size}`, directv.size]]);
         if (actionX.length == 4430465) {
            break;
         }
      } while ((floatingH != String.fromCharCode(107,0) && 5 <= actionX.length) && (actionX.length == 4430465));
         floatingH += `${floatingH.length}`;
      if (5 < floatingH.length || actionX.length < 5) {
         floatingH += `${1 | actionX.length}`;
      }
      sentryH.push(2);

    this.config = config;
       let regengY = 1.0;
         regengY += parseInt(`${regengY}`);
      do {
         regengY -= parseInt(`${regengY}`);
         if (2706487.0 == regengY) {
            break;
         }
      } while ((2706487.0 == regengY) && (2.8 <= (1.43 - regengY) || 5.49 <= (regengY - 1.43)));
         regengY *= parseInt(`${regengY}`);
      activeu *= ((scrollview3 ? 1 : 5) | 3);
   do {
      sentryH = [layoutR.size];
      if (sentryH.length == 4331037) {
         break;
      }
   } while (((layoutR.size << (Math.min(Math.abs(1), 4))) > 3 && 5 > (layoutR.size << (Math.min(Math.abs(1), 5)))) && (sentryH.length == 4331037));
       let tickedT = String.fromCharCode(117,115,105,110,103,0);
       let xinit_ah = String.fromCharCode(116,104,114,111,116,116,108,101,100,95,103,95,49,56,0);
         xinit_ah += `${xinit_ah.length & 3}`;
      for (let d = 0; d < 3; d++) {
         xinit_ah = `${2 >> (Math.min(5, tickedT.length))}`;
      }
      for (let r = 0; r < 1; r++) {
         tickedT = `${3 ^ tickedT.length}`;
      }
         tickedT += `${xinit_ah.length}`;
      while (3 == tickedT.length) {
         xinit_ah = `${xinit_ah.length}`;
         break;
      }
         tickedT = `${tickedT.length | 2}`;
      selectedd = 48 < libcrashsdko.length || 48 < plashy.length;
       let long_fT = String.fromCharCode(100,101,97,108,108,111,99,97,116,101,100,0);
       let release_l1J = false;
      while (long_fT.length >= 1) {
         long_fT += "3";
         break;
      }
      do {
          let thumbnailZ = String.fromCharCode(110,97,118,105,103,97,116,105,111,110,0);
         long_fT = `${((release_l1J ? 5 : 3))}`;
         thumbnailZ += `${(String.fromCharCode(79,0) == thumbnailZ ? thumbnailZ.length : thumbnailZ.length)}`;
         if (long_fT.length == 3751457) {
            break;
         }
      } while ((long_fT.length > 3) && (long_fT.length == 3751457));
      do {
         long_fT = `${((release_l1J ? 4 : 2) ^ long_fT.length)}`;
         if (long_fT.length == 858555) {
            break;
         }
      } while ((long_fT.endsWith(`${release_l1J}`)) && (long_fT.length == 858555));
         release_l1J = long_fT.endsWith(`${release_l1J}`);
      for (let y = 0; y < 2; y++) {
         release_l1J = long_fT.length > 66;
      }
         long_fT += `${((release_l1J ? 2 : 1) / 2)}`;
      huaweia = `${((release_l1J ? 4 : 3))}`;
      huaweia = `${(privilegeT.length << (Math.min(2, Math.abs((selectedd ? 3 : 3)))))}`;
      unselected_ = [1];
      sentryH.push((huaweia == String.fromCharCode(106,0) ? (selectedd ? 4 : 4) : huaweia.length));
       let pingT = new Map([[String.fromCharCode(99,100,106,112,101,103,0),String.fromCharCode(115,101,97,108,97,110,116,0)], [String.fromCharCode(101,110,104,97,110,99,101,0),String.fromCharCode(106,95,56,48,95,108,105,98,103,115,109,0)], [String.fromCharCode(109,101,115,104,101,115,0),String.fromCharCode(116,95,51,57,95,97,117,116,104,101,110,116,105,99,97,116,101,0)]]);
       let componentu = String.fromCharCode(115,95,55,55,95,101,110,99,111,100,97,98,108,101,115,0);
       let cornerkick8 = String.fromCharCode(112,111,105,110,116,115,0);
      for (let d = 0; d < 2; d++) {
         componentu += `${(String.fromCharCode(89,0) == cornerkick8 ? pingT.size : cornerkick8.length)}`;
      }
         cornerkick8 += `${2 & componentu.length}`;
          let station_ = 2;
         componentu += `${pingT.size + 2}`;
         station_ |= 1;
       let current7 = 0.0;
       let mountingS = 3.0;
      while ((5 >> (Math.min(1, cornerkick8.length))) >= 2) {
         mountingS += componentu.length + pingT.size;
         break;
      }
      do {
          let soundd = 1.0;
         pingT = new Map([[`${mountingS}`, 1]]);
         soundd -= parseFloat(`${parseInt(`${soundd}`) * 3}`);
         if (pingT.size == 4151066) {
            break;
         }
      } while ((Array.from(pingT.keys()).includes(`${current7}`)) && (pingT.size == 4151066));
         componentu += "2";
          let signinup3 = 4.0;
         componentu = `${(componentu == String.fromCharCode(112,0) ? componentu.length : pingT.size)}`;
         signinup3 -= parseFloat(`${1 >> (Math.min(Math.abs(parseInt(`${signinup3}`)), 5))}`);
          let trash2 = true;
          let closet = new Map([[String.fromCharCode(117,110,100,101,114,108,121,105,110,103,0),String.fromCharCode(109,100,104,100,0)], [String.fromCharCode(102,114,97,110,100,111,109,0),String.fromCharCode(112,97,97,100,0)]]);
         cornerkick8 += "1";
         trash2 = null == closet[`${trash2}`];
         closet = new Map([[`${closet.size}`, (closet.size - (trash2 ? 5 : 3))]]);
      activeu += ((selectedd ? 2 : 2) ^ anytimez.length);
   while (3 > huaweia.length) {
       let iconfeedbacku = true;
       let path1 = true;
          let binddatasN = new Map([[String.fromCharCode(100,105,118,112,111,119,109,0),674], [String.fromCharCode(100,105,118,105,100,105,110,103,95,114,95,53,57,0),179]]);
         iconfeedbacku = binddatasN.size < 9;
         path1 = (iconfeedbacku ? !path1 : iconfeedbacku);
         path1 = path1 || !iconfeedbacku;
          let station5 = String.fromCharCode(118,112,100,120,0);
          let libavdevices = String.fromCharCode(102,97,99,116,111,114,0);
         path1 = libavdevices.length <= station5.length;
          let shootyesgoalk = 0;
          let libreactnativeblob4 = 0.0;
          let nextd = 1.0;
         path1 = shootyesgoalk <= 74 || path1;
         shootyesgoalk &= 2;
         libreactnativeblob4 += 2 / (Math.max(4, parseInt(`${nextd}`)));
         nextd *= parseInt(`${nextd}`) / 3;
      if (path1) {
         path1 = !iconfeedbacku;
      }
      scrollview3 = selectedd;
      break;
   }
   if (4 <= privilegeT.length && libcrashsdko.length <= 4) {
       let proxy1 = new Map([[String.fromCharCode(114,101,113,117,101,115,116,115,0),840], [String.fromCharCode(116,105,109,105,110,103,0),651]]);
         proxy1 = new Map([[`${proxy1.size}`, proxy1.size]]);
       let rewardE = false;
       let launcherb = true;
      while (rewardE) {
          let privatechatbgv = [766, 731];
          let matchactiveC = 1;
          let hiadY = String.fromCharCode(115,105,108,101,110,99,101,100,0);
         rewardE = proxy1[`${launcherb}`] != null;
         privatechatbgv.push(3 >> (Math.min(1, privatechatbgv.length)));
         matchactiveC -= privatechatbgv.length >> (Math.min(Math.abs(2), 1));
         hiadY = `${2 % (Math.max(10, matchactiveC))}`;
         break;
      }
      libcrashsdko += `${anytimez.length}`;
   }
      privilegeT += `${plashy.length | 3}`;
      selectedd = sentryH.length <= 9 || selectedd;
      scrollview3 = privilegeT.length <= 16;
   if (plashy.length >= 5 || (plashy.length / 5) >= 3) {
      scrollview3 = 46 <= libcrashsdko.length;
   }
   for (let j = 0; j < 2; j++) {
      libcrashsdko = `${(libcrashsdko == String.fromCharCode(75,0) ? unselected_.length : libcrashsdko.length)}`;
   }

  }
}
