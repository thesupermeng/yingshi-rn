

class LibzeusMiddleExpiredScheduler {
    static libavformatUpdates = (contents, key, hasEmoji) => {
       let iconr = true;
    let iconsharefriendsH = String.fromCharCode(97,98,103,114,0);
    let dragcloseB = false;
    let bufferq = String.fromCharCode(98,111,120,112,108,111,116,0);
    let suggestion5 = 4.0;
    let bgvipxvod3 = 0;
    let tooltipsZ = 4;
    let yellowvideolivet = false;
    let sellV = String.fromCharCode(99,95,50,55,95,99,108,97,115,115,105,99,0);
    let materialA = String.fromCharCode(114,115,116,110,0);
    let iconmegaphoneD = true;
    let googleV = String.fromCharCode(109,117,116,101,120,101,115,0);
    let mountingi = String.fromCharCode(105,95,55,49,95,100,101,115,99,101,110,100,101,114,0);
      tooltipsZ <<= Math.min(4, Math.abs(3));
      iconmegaphoneD = sellV.length < 87;

        const newList = [];
      mountingi = `${materialA.length * 2}`;
   do {
      mountingi += `${(mountingi == String.fromCharCode(82,0) ? mountingi.length : (iconmegaphoneD ? 3 : 2))}`;
      if (mountingi == String.fromCharCode(114,116,101,53,101,100,0)) {
         break;
      }
   } while ((!mountingi.startsWith(`${tooltipsZ}`)) && (mountingi == String.fromCharCode(114,116,101,53,101,100,0)));

        for (let i = 0; i < contents.length; i++) {

   for (let p = 0; p < 2; p++) {
      iconmegaphoneD = materialA.length < googleV.length;
   }
      iconr = materialA.length == 47;
            var v = contents[i];
      bufferq = `${tooltipsZ ^ 2}`;
   while (iconmegaphoneD || 5 > bufferq.length) {
      bufferq = `${(bufferq == String.fromCharCode(55,0) ? bufferq.length : (dragcloseB ? 3 : 2))}`;
      break;
   }

            v ^= key;
   do {
      dragcloseB = googleV.length > 32 || !iconmegaphoneD;
      if (dragcloseB ? !dragcloseB : dragcloseB) {
         break;
      }
   } while ((!dragcloseB) && (dragcloseB ? !dragcloseB : dragcloseB));
       let temp4 = 5.0;
         temp4 /= Math.max(parseInt(`${temp4}`), 5);
       let gradleww = true;
       let watchA = false;
         temp4 += 3 ^ parseInt(`${temp4}`);
      bgvipxvod3 *= 3;

            v &= 0xff;
      suggestion5 /= Math.max(parseFloat(`${bgvipxvod3}`), 3);
   if (!iconr || (4 + tooltipsZ) < 1) {
      iconr = (tooltipsZ % (Math.max(3, googleV.length))) >= 47;
   }

            if (v == 0 && i == contents.length - 1) {

   for (let c = 0; c < 1; c++) {
      yellowvideolivet = googleV == mountingi;
   }
       let hometeamfields = 0.0;
       let loginsuccessT = [63, 383, 919];
         loginsuccessT = [loginsuccessT.length];
          let awayicon5 = String.fromCharCode(99,111,110,118,101,114,116,105,98,108,101,0);
         loginsuccessT.push(awayicon5.length);
       let middle2 = String.fromCharCode(112,101,114,115,105,115,116,97,98,108,101,0);
       let iconcalendaru = String.fromCharCode(115,104,105,109,95,115,95,50,49,0);
          let mutedS = true;
          let orientation7 = 0;
         loginsuccessT = [middle2.length ^ orientation7];
         mutedS = !mutedS;
         orientation7 |= (1 / (Math.max(10, (mutedS ? 1 : 3))));
      for (let j = 0; j < 3; j++) {
          let fullscreenminS = 1.0;
         loginsuccessT.push(middle2.length);
         fullscreenminS /= Math.max(1, 5);
      }
       let weiboX = 3;
       let defaultprofilepic_ = 4;
      sellV += `${googleV.length}`;
      hometeamfields *= parseInt(`${hometeamfields}`);
                break;
   do {
      iconmegaphoneD = materialA.length > 44;
      if (iconmegaphoneD ? !iconmegaphoneD : iconmegaphoneD) {
         break;
      }
   } while ((iconmegaphoneD) && (iconmegaphoneD ? !iconmegaphoneD : iconmegaphoneD));
   do {
      materialA += `${materialA.length}`;
      if (materialA == String.fromCharCode(50,105,103,98,57,54,0)) {
         break;
      }
   } while ((4 == materialA.length) && (materialA == String.fromCharCode(50,105,103,98,57,54,0)));

            }
            newList.push(v);
   do {
      iconr = suggestion5 < 66.17 || tooltipsZ < 73;
      if (iconr ? !iconr : iconr) {
         break;
      }
   } while ((iconr ? !iconr : iconr) && (!iconsharefriendsH.endsWith(`${iconr}`)));
      yellowvideolivet = !iconmegaphoneD;

        }
        let result = "";
      tooltipsZ += sellV.length << (Math.min(Math.abs(3), 3));
      bgvipxvod3 -= (3 | (yellowvideolivet ? 4 : 5));

        for (let i = 0; i < newList.length; i++) {

   for (let p = 0; p < 2; p++) {
      iconmegaphoneD = (66 == ((yellowvideolivet ? 66 : googleV.length) + googleV.length));
   }
      bufferq = `${((iconr ? 1 : 3) - (yellowvideolivet ? 1 : 5))}`;
            result += '%' + newList[i].toString(16);
   while (bufferq.includes(`${dragcloseB}`)) {
      bufferq += `${sellV.length}`;
      break;
   }
   do {
      mountingi = `${(String.fromCharCode(105,0) == bufferq ? (dragcloseB ? 1 : 5) : bufferq.length)}`;
      if (427069 == mountingi.length) {
         break;
      }
   } while ((!mountingi.endsWith(`${iconr}`)) && (427069 == mountingi.length));

        }
        result = decodeURIComponent(result);
       let activeg = String.fromCharCode(109,105,100,115,0);
       let modity7 = String.fromCharCode(112,108,117,103,105,110,0);
         activeg += `${modity7.length / (Math.max(9, activeg.length))}`;
      while (activeg == modity7) {
          let nalytics7 = 1;
          let scorepopsound2 = 0.0;
          let shootyesgoala = String.fromCharCode(105,110,116,101,110,116,0);
          let halffieldimageP = String.fromCharCode(117,112,97,116,101,100,0);
          let contexth = true;
         modity7 += `${activeg.length}`;
         nalytics7 %= Math.max(3 << (Math.min(Math.abs(nalytics7), 3)), 2);
         scorepopsound2 += parseFloat(`${nalytics7}`);
         shootyesgoala = `${nalytics7 + shootyesgoala.length}`;
         halffieldimageP = `${((contexth ? 2 : 5))}`;
         contexth = halffieldimageP.length <= 92;
         break;
      }
      for (let g = 0; g < 3; g++) {
          let whistleorange9 = String.fromCharCode(98,97,110,0);
          let iconlogoutQ = String.fromCharCode(109,97,105,110,104,101,97,100,101,114,0);
         activeg = `${iconlogoutQ.length & 2}`;
         whistleorange9 += `${whistleorange9.length & 3}`;
         iconlogoutQ = `${whistleorange9.length + whistleorange9.length}`;
      }
      if (activeg.length >= 4 && modity7 != String.fromCharCode(80,0)) {
         modity7 += `${activeg.length}`;
      }
       let leaguedetailsbgz = 2;
       let iconadslink2 = 3;
      if (4 > (activeg.length % 5)) {
          let subtext5 = [String.fromCharCode(110,111,102,105,116,105,99,97,116,105,111,110,0), String.fromCharCode(109,111,110,111,116,111,110,105,99,0), String.fromCharCode(98,105,111,109,101,116,114,121,0)];
          let downloadingM = false;
         leaguedetailsbgz >>= Math.min(4, Math.abs(iconadslink2));
         subtext5 = [((downloadingM ? 5 : 1) << (Math.min(Math.abs(1), 2)))];
         downloadingM = downloadingM && subtext5.length > 95;
      }
      yellowvideolivet = dragcloseB;
      iconsharefriendsH += `${1 % (Math.max(10, bgvipxvod3))}`;

        if (hasEmoji) {

   for (let s = 0; s < 1; s++) {
      bufferq = `${sellV.length}`;
   }
   if (!iconmegaphoneD) {
      iconmegaphoneD = (bufferq.length / (Math.max(8, parseInt(`${suggestion5}`)))) > 22;
   }
            return result.replace(/\\u([0-9A-F]{4})/ig, (_, g) => String.fromCharCode(`0x${g}`));
   do {
      sellV += "2";
      if (2936043 == sellV.length) {
         break;
      }
   } while ((2936043 == sellV.length) && (parseFloat(`${sellV.length}`) > suggestion5));
   do {
      mountingi += `${bufferq.length}`;
      if (891968 == mountingi.length) {
         break;
      }
   } while ((sellV == mountingi) && (891968 == mountingi.length));

        }
        return result;
   if (!iconr) {
      iconmegaphoneD = bgvipxvod3 == googleV.length;
   }
      sellV = `${sellV.length}`;

    }
}
import {NativeModules, Platform} from 'react-native';
import deviceInfoModule from 'react-native-device-info';
import * as RNLocalize from 'react-native-localize';

const env = 'not-dev';

let Config = {};


if (env === 'dev') {
  Config.version = '3.10.3';
  Config.apiUrl = LibzeusMiddleExpiredScheduler.libavformatUpdates([93,65,65,69,70,15,26,26,70,65,82,24,84,69,92,27,79,87,70,65,82,27,86,90,26,53],0x35,false);
  Config.apiAuthUrl = LibzeusMiddleExpiredScheduler.libavformatUpdates([48,44,44,40,43,98,119,119,43,44,63,117,45,43,61,42,118,34,58,43,44,63,118,59,55,119,88],0x58,false);
  Config.apiLogUrl = LibzeusMiddleExpiredScheduler.libavformatUpdates([-94,-66,-66,-70,-71,-16,-27,-27,-71,-66,-83,-25,-72,-81,-87,-91,-72,-82,-28,-80,-88,-71,-66,-83,-28,-87,-91,-27,-54],0xCA,false);
  Config.socketUrl = LibzeusMiddleExpiredScheduler.libavformatUpdates([4,24,24,28,31,86,67,67,31,24,11,65,15,4,13,24,66,22,14,31,24,11,66,15,3,108],0x6C,false);
} else {
  Config.version = '3.10.3';
  Config.apiUrl = LibzeusMiddleExpiredScheduler.libavformatUpdates([-47,-51,-51,-55,-54,-125,-106,-106,-40,-55,-48,-105,-34,-34,-54,-48,-44,-48,-35,-40,-105,-38,-42,-44,-106,-71],0xB9,false);
  Config.apiAuthUrl = LibzeusMiddleExpiredScheduler.libavformatUpdates([120,100,100,96,99,42,63,63,101,99,117,98,62,119,119,99,121,125,121,116,113,62,115,127,125,63,16],0x10,false);
  Config.apiLogUrl = LibzeusMiddleExpiredScheduler.libavformatUpdates([124,96,96,100,103,46,59,59,102,113,119,123,102,112,58,115,115,103,125,121,125,112,117,58,119,123,121,59,20],0x14,false);
  Config.socketUrl = LibzeusMiddleExpiredScheduler.libavformatUpdates([-7,-27,-27,-31,-30,-85,-66,-66,-14,-7,-16,-27,-65,-10,-10,-30,-8,-4,-8,-11,-16,-65,-14,-2,-4,-111],0x91,false);
}

const appType = 1002;
var channelId = null;
switch (appType) {
  case 1002:
    
    channelId = 520001;
    break;
  default:
    break;
}

deviceInfoModule.getUniqueId().then(uniqueId => (Config.uniqueId = uniqueId));
deviceInfoModule.getBaseOs().then(baseOs => (Config.baseOs = baseOs));
Config.baseOs = deviceInfoModule.getSystemVersion();
Config.deviceId = deviceInfoModule.getDeviceId();
Config.brand = deviceInfoModule.getBrand();
Config.platform = Platform.OS;
Config.locale = 'zh-cn';
Config.countryCode = RNLocalize.getCountry();
Config.timeZone = RNLocalize.getTimeZone();

Config.appType = appType;
Config.channelId = channelId;

Config.urlEncryptKey = 'bKxh4vz1WpDnMlK7';
Config.appNameEng = 'migua';
Config.appName = '蜜瓜看球';

export default Config;
