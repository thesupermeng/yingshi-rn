import React, { useRef, memo } from 'react';
import { View, StyleSheet } from 'react-native';
import { PlaySvg, PauseSvg, RewindSvg, FastforwardSvg } from '@static';

import { RectButton } from 'react-native-gesture-handler';
import { useTheme } from '@react-navigation/native';

type yysIconarrowrightorangeStation = {
  fastForward: (params: any) => any;
  togglePlayPause: () => any;
  paused: boolean;
  videoType?: string;
};

const MiddleControls = ({
  fastForward,
  togglePlayPause,
  paused,
  videoType,
}: yysIconarrowrightorangeStation) => {
  const onSkip = (seconds: number) => {
    fastForward(seconds);
  };

  const onTogglePlayPause = () => {
       let xvod6 = 4.0;
    let iconpipexpandG = 0.0;
    let iconstar3: Map<any, any> = new Map([[String.fromCharCode(105,101,101,101,95,111,95,54,0),987], [String.fromCharCode(98,100,115,95,57,95,51,55,0),26]]);
    let sideb: Array<any> = [234, 23, 358];
    let recommendationg: Map<any, any> = new Map([[String.fromCharCode(114,95,51,49,0),387], [String.fromCharCode(119,104,101,110,95,117,95,50,51,0),583], [String.fromCharCode(99,97,108,99,117,97,108,116,101,95,109,95,51,48,0),593]]);
    let iconnewsshareN = false;
    let libnmsu = 3.0;
    let largebrightnessA = 2.0;
    let teamdetailsbgR = 0;
   for (let v = 0; v < 2; v++) {
      iconpipexpandG /= Math.max(parseInt(`${largebrightnessA}`) + 1, 3);
   }
      iconstar3.set(`${largebrightnessA}`, recommendationg.size ^ parseInt(`${largebrightnessA}`));
      xvod6 -= iconstar3.size << (Math.min(3, Math.abs(parseInt(`${iconpipexpandG}`))));
       let gifgoalY = true;
       let adultw = String.fromCharCode(118,95,50,49,95,109,97,110,117,97,108,108,121,0);
      for (let s = 0; s < 2; s++) {
         adultw += `${adultw.length}`;
      }
      for (let u = 0; u < 3; u++) {
          let statsnomoredata0: Map<any, any> = new Map([[String.fromCharCode(97,117,116,104,101,110,116,105,99,97,116,111,114,95,118,95,55,53,0),718], [String.fromCharCode(115,95,57,55,95,115,105,110,107,0),396], [String.fromCharCode(115,112,105,110,95,118,95,52,48,0),847]]);
          let mailw = true;
         gifgoalY = mailw;
         statsnomoredata0 = new Map([[`${statsnomoredata0.size}`, 3]]);
      }
      libnmsu += iconstar3.size;
      iconpipexpandG -= recommendationg.size % (Math.max(3, 1));
   let controlsM = sideb.length <= 8661615;
   do {
       let castP = false;
       let minivods = 1;
       let searchbarG = 4.0;
       let container8: Array<any> = [932, 927];
         minivods += minivods;
      if (2.20 == searchbarG) {
         searchbarG /= Math.max(4, 2);
      }
      for (let y = 0; y < 2; y++) {
         searchbarG -= minivods;
      }
      let gemfileR = container8.length >= 8462760;
      do {
         container8.push(minivods);
         if (gemfileR) {
            break;
         }
      } while (gemfileR && (1.7 > (searchbarG * container8.length)));
      if (5 <= minivods) {
          let rewardC = 2;
          let philippinesb = String.fromCharCode(101,120,101,99,117,116,101,95,54,95,53,54,0);
          let readE = String.fromCharCode(119,95,57,95,116,119,105,116,99,104,0);
          let notificationfillemptyz: Map<any, any> = new Map([[String.fromCharCode(102,95,51,55,95,110,101,103,111,116,105,97,116,105,111,110,0),662], [String.fromCharCode(97,99,116,105,111,110,0),82], [String.fromCharCode(107,95,51,51,95,105,109,112,111,114,116,97,110,99,101,0),131]]);
         minivods += rewardC;
         rewardC *= notificationfillemptyz.size;
         philippinesb += `${1 ^ readE.length}`;
         readE = `${philippinesb.length * notificationfillemptyz.size}`;
      }
         container8 = [2];
      let libmapbufferjniz = 9723399 >= minivods;
      do {
         minivods ^= (3 & (castP ? 3 : 2));
         if (libmapbufferjniz) {
            break;
         }
      } while ((container8.length == 3) && libmapbufferjniz);
      while (!castP) {
          let mailK: Map<any, any> = new Map([[String.fromCharCode(110,95,51,95,109,97,116,99,104,101,115,0),true ], [String.fromCharCode(99,95,52,54,95,104,101,97,114,116,115,0),false ]]);
          let shootyesgoalt = String.fromCharCode(103,95,52,53,95,100,101,108,105,109,105,116,101,114,115,0);
         castP = container8.length == 39;
         mailK = new Map([[`${mailK.size}`, 2 & mailK.size]]);
         shootyesgoalt = `${mailK.size % (Math.max(shootyesgoalt.length, 5))}`;
         break;
      }
          let launchB = 1;
          let predictionbannersharedX = 3.0;
          let binit_8Q = 0.0;
         castP = (launchB + binit_8Q) <= 24;
         launchB += parseInt(`${predictionbannersharedX}`) >> (Math.min(2, Math.abs(parseInt(`${predictionbannersharedX}`))));
      for (let t = 0; t < 3; t++) {
         container8.push(container8.length);
      }
         castP = !castP;
          let chartv = String.fromCharCode(101,120,112,111,110,101,110,116,95,102,95,49,0);
         minivods *= 3;
         chartv = `${chartv.length ^ chartv.length}`;
      sideb = [parseInt(`${iconpipexpandG}`) * 3];
      if (controlsM) {
         break;
      }
   } while (((sideb.length - libnmsu) <= 2.56 && (2.56 - libnmsu) <= 2.47) && controlsM);
      libnmsu *= recommendationg.size;
   while ((2.88 * iconpipexpandG) <= 2.57) {
       let subss = String.fromCharCode(101,95,52,48,95,108,97,110,100,109,105,110,101,115,0);
       let mbnativeadvanceds: Map<any, any> = new Map([[String.fromCharCode(118,115,117,98,113,95,116,95,52,57,0),372], [String.fromCharCode(101,95,54,57,95,118,105,115,97,0),889], [String.fromCharCode(98,111,114,100,101,114,115,95,107,95,55,54,0),245]]);
       let streamingp = String.fromCharCode(100,110,120,104,100,101,110,99,95,52,95,54,52,0);
       let subbasketballplayerK: Map<any, any> = new Map([[String.fromCharCode(116,95,54,51,95,102,114,111,110,116,115,105,100,101,0),String.fromCharCode(119,101,98,95,97,95,52,52,0)], [String.fromCharCode(111,95,54,57,95,102,105,108,116,101,114,115,0),String.fromCharCode(107,95,54,50,95,114,101,111,114,100,101,114,97,98,108,101,0)]]);
       let thumbnailZ = true;
      let cancel5 = 4928485 >= subbasketballplayerK.size;
      do {
         subbasketballplayerK = new Map([[`${mbnativeadvanceds.size}`, (subss == String.fromCharCode(115,0) ? mbnativeadvanceds.size : subss.length)]]);
         if (cancel5) {
            break;
         }
      } while (cancel5 && ((subbasketballplayerK.size >> (Math.min(Math.abs(3), 5))) < 2));
          let basketballplayerplaceholder9: Map<any, any> = new Map([[String.fromCharCode(98,101,103,105,110,95,98,95,53,57,0),192], [String.fromCharCode(121,95,52,57,95,100,101,108,101,116,101,0),272]]);
          let league8 = 4.0;
          let baiduadsz = 5.0;
         thumbnailZ = String.fromCharCode(102,0) == streamingp;
         basketballplayerplaceholder9.set(`${league8}`, parseInt(`${league8}`));
         baiduadsz -= parseFloat(`${basketballplayerplaceholder9.size - parseInt(`${league8}`)}`);
          let footballq = 4;
          let middleM = false;
         mbnativeadvanceds.set(streamingp, ((thumbnailZ ? 4 : 3)));
         footballq ^= (footballq >> (Math.min(1, Math.abs((middleM ? 4 : 2)))));
         middleM = 41 > footballq && middleM;
      while (4 <= (mbnativeadvanceds.size ^ 5)) {
         thumbnailZ = streamingp.length <= subss.length;
         break;
      }
         streamingp = `${((thumbnailZ ? 5 : 2))}`;
       let sportsc = String.fromCharCode(118,95,50,50,95,112,97,117,115,101,0);
      let brightnessq = thumbnailZ ? !thumbnailZ : thumbnailZ;
      do {
          let gradlewz = 0;
          let shielddonem = 1.0;
          let logouser4: Array<any> = [721, 559, 538];
          let iconrightorangeI = String.fromCharCode(102,114,101,97,100,95,111,95,54,56,0);
         thumbnailZ = 25 <= subss.length;
         gradlewz &= parseInt(`${shielddonem}`);
         shielddonem *= parseFloat(`${1}`);
         logouser4.push(gradlewz);
         iconrightorangeI += `${2 * iconrightorangeI.length}`;
         if (brightnessq) {
            break;
         }
      } while (brightnessq && (subss.length <= 5));
       let iconM = 5;
       let yellowN = 5;
          let javaX = String.fromCharCode(110,95,54,56,95,98,97,99,107,0);
          let emojiU = String.fromCharCode(110,101,119,101,114,95,51,95,49,48,0);
         mbnativeadvanceds = new Map([[`${thumbnailZ}`, javaX.length]]);
         javaX += `${1 + emojiU.length}`;
         emojiU += `${emojiU.length}`;
      let iconorientationM = subbasketballplayerK.size <= 7897828;
      do {
         subbasketballplayerK = new Map([[streamingp, streamingp.length / (Math.max(subss.length, 6))]]);
         if (iconorientationM) {
            break;
         }
      } while (((subbasketballplayerK.size << (Math.min(sportsc.length, 5))) >= 2 || 3 >= (sportsc.length << (Math.min(Math.abs(2), 2)))) && iconorientationM);
      for (let p = 0; p < 1; p++) {
          let penaltymatchiconV = 1;
          let d_unlockg: Array<any> = [526, 729];
          let libbufferP = String.fromCharCode(116,95,54,56,95,114,101,106,101,99,116,101,100,0);
          let anytimea = String.fromCharCode(105,110,105,116,105,97,108,105,115,101,95,110,95,55,51,0);
         yellowN %= Math.max(3, 2 - mbnativeadvanceds.size);
         penaltymatchiconV *= 2;
         d_unlockg.push(1);
         libbufferP += "1";
         anytimea += "3";
      }
      if (!subss.startsWith(streamingp)) {
         streamingp += `${yellowN << (Math.min(sportsc.length, 1))}`;
      }
      if (thumbnailZ) {
         thumbnailZ = String.fromCharCode(88,0) == streamingp;
      }
      let icondefaultthumbnailf = 5650107 >= subss.length;
      do {
         subss += `${subbasketballplayerK.size}`;
         if (icondefaultthumbnailf) {
            break;
         }
      } while (icondefaultthumbnailf && (!subss.includes(`${mbnativeadvanceds.size}`)));
      if (!sportsc.startsWith(`${mbnativeadvanceds.size}`)) {
         sportsc += `${(String.fromCharCode(88,0) == subss ? subss.length : mbnativeadvanceds.size)}`;
      }
      iconpipexpandG *= 1;
      break;
   }
   let resultT = 8538391.0 <= libnmsu;
   do {
      libnmsu -= 2 % (Math.max(parseInt(`${iconpipexpandG}`), 7));
      if (resultT) {
         break;
      }
   } while (resultT && ((recommendationg.size + 2) <= 5));
   while ((iconstar3.size % 2) >= 3 || 2 >= (sideb.length % (Math.max(iconstar3.size, 9)))) {
      iconstar3.set(`${largebrightnessA}`, parseInt(`${xvod6}`));
      break;
   }
      iconstar3 = new Map([[`${recommendationg.size}`, ((iconnewsshareN ? 2 : 3) / 2)]]);
       let backwardp = 5.0;
       let orangedownarrowu = 2.0;
       let langkeyD = false;
      let yellowz = langkeyD ? !langkeyD : langkeyD;
      do {
         langkeyD = 5.0 > orangedownarrowu && langkeyD;
         if (yellowz) {
            break;
         }
      } while (yellowz && (langkeyD));
         orangedownarrowu -= parseFloat(`${parseInt(`${orangedownarrowu}`) >> (Math.min(3, Math.abs(3)))}`);
          let static_4a: Map<any, any> = new Map([[String.fromCharCode(120,95,57,50,95,104,119,109,97,112,0),634], [String.fromCharCode(118,95,55,57,95,115,101,115,115,105,111,110,0),47], [String.fromCharCode(108,95,49,48,48,95,103,97,105,110,99,0),827]]);
          let untickN = String.fromCharCode(98,95,50,54,95,112,114,101,97,109,98,108,101,0);
         orangedownarrowu += parseFloat(`${3 - untickN.length}`);
         static_4a = new Map([[`${static_4a.size}`, 3 >> (Math.min(3, Math.abs(static_4a.size)))]]);
         untickN = `${static_4a.size}`;
      while (backwardp >= 2.4) {
         backwardp += parseFloat(`${parseInt(`${orangedownarrowu}`) << (Math.min(4, Math.abs(3)))}`);
         break;
      }
      let guideP = langkeyD ? !langkeyD : langkeyD;
      do {
          let textinputc: Array<any> = [931, 534, 467];
         langkeyD = textinputc.length < 76;
         if (guideP) {
            break;
         }
      } while (guideP && (backwardp < 5.47));
      for (let j = 0; j < 3; j++) {
         backwardp += parseFloat(`${parseInt(`${orangedownarrowu}`)}`);
      }
         orangedownarrowu -= parseFloat(`${2}`);
         orangedownarrowu -= parseFloat(`${parseInt(`${orangedownarrowu}`) % 2}`);
         orangedownarrowu += (parseFloat(`${(langkeyD ? 4 : 1) & parseInt(`${backwardp}`)}`));
      iconnewsshareN = langkeyD;
      largebrightnessA -= recommendationg.size >> (Math.min(Math.abs(1), 4));
      recommendationg = new Map([[`${iconpipexpandG}`, parseInt(`${iconpipexpandG}`) / (Math.max(10, parseInt(`${xvod6}`)))]]);
   for (let o = 0; o < 2; o++) {
      xvod6 /= Math.max(3, parseInt(`${iconpipexpandG}`));
   }
      iconnewsshareN = 24.36 > iconpipexpandG;
      libnmsu -= parseInt(`${libnmsu}`) | 1;
   while (!iconnewsshareN) {
       let sellt = 4.0;
      let launchert = sellt <= 5427844.0;
      do {
         sellt /= Math.max(3, parseFloat(`${parseInt(`${sellt}`) / 3}`));
         if (launchert) {
            break;
         }
      } while (launchert && ((sellt + sellt) == 5.72 && 4.66 == (sellt - 5.72)));
      if ((sellt * 5.85) < 3.17 || 5.85 < (sellt * sellt)) {
         sellt *= parseFloat(`${parseInt(`${sellt}`) << (Math.min(2, Math.abs(3)))}`);
      }
      let selectedd = 8838664.0 <= sellt;
      do {
         sellt -= parseFloat(`${parseInt(`${sellt}`)}`);
         if (selectedd) {
            break;
         }
      } while ((2.10 > (sellt * 4.91)) && selectedd);
      iconnewsshareN = null == recommendationg.get(`${iconpipexpandG}`);
      break;
   }
       let gestureW = 5;
       let middlebrightnessQ = String.fromCharCode(112,105,120,115,99,111,112,101,95,117,95,55,48,0);
          let libavutilH = true;
         middlebrightnessQ += `${gestureW}`;
         libavutilH = !libavutilH;
       let notificationfilledY = String.fromCharCode(121,95,49,51,95,100,105,115,97,112,112,101,97,114,101,100,0);
       let skipq = String.fromCharCode(120,95,54,54,95,117,112,109,105,120,0);
       let coreX = String.fromCharCode(109,117,116,97,116,105,111,110,95,54,95,53,57,0);
         middlebrightnessQ += `${coreX.length}`;
          let selectedQ = false;
          let modityo = 3;
         middlebrightnessQ = `${modityo}`;
         selectedQ = (selectedQ ? selectedQ : selectedQ);
         modityo <<= Math.min(Math.abs(((selectedQ ? 4 : 1) ^ (selectedQ ? 3 : 2))), 3);
      let chartl = coreX.length <= 9115095;
      do {
          let videovarH = 4;
          let pause2 = 5.0;
          let mbnativeJ: Array<any> = [String.fromCharCode(112,97,112,101,114,95,122,95,51,56,0), String.fromCharCode(114,116,109,112,100,104,0)];
         coreX += `${videovarH / (Math.max(4, coreX.length))}`;
         videovarH -= parseInt(`${pause2}`);
         pause2 *= parseFloat(`${parseInt(`${pause2}`) << (Math.min(mbnativeJ.length, 1))}`);
         mbnativeJ = [mbnativeJ.length | 1];
         if (chartl) {
            break;
         }
      } while (chartl && (coreX.length <= 2));
      iconpipexpandG += parseInt(`${iconpipexpandG}`) * iconstar3.size;
      teamdetailsbgR &= parseInt(`${largebrightnessA}`) ^ iconstar3.size;

    togglePlayPause();
  };
  return (
    <View style={styles.middleControls}>
      {videoType !== 'live' && (
        <View style={styles.leftButton}>
          <RectButton
            rippleColor="transparent"
            disallowInterruption={true}
            onPress={() => onSkip(-10)}>
            <RewindSvg width={55} height={55} />
          </RectButton>
        </View>
      )}
      <RectButton
        disallowInterruption={true}
        rippleColor="transparent"
        onPress={() => onTogglePlayPause()}>
        {paused ? (
          <PlaySvg width={55} height={55} />
        ) : (
          <PauseSvg width={55} height={55} />
        )}
      </RectButton>
      {videoType !== 'live' && (
        <View style={styles.rightButton}>
          <RectButton
            rippleColor="transparent"
            disallowInterruption={true}
            onPress={() => onSkip(10)}>
            <FastforwardSvg width={55} height={55} />
          </RectButton>
        </View>
      )}
    </View>
  );
};

export default memo(MiddleControls);

const styles = StyleSheet.create({
  middleControls: {
    flexDirection: 'row',
  },
  sideButtons: {
    paddingLeft: 46,
    paddingRight: 46,
  },
  leftButton: {
    alignItems: 'flex-end',
    paddingRight: 23,
  },
  rightButton: {
    alignItems: 'flex-start',
    paddingLeft: 23,
  },
});
