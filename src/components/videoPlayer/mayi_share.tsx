import React, { useRef, memo } from 'react';
import { View, StyleSheet } from 'react-native';
import Play from '@static/images/libreanimatedEwardedDropdown.svg';
import Pause from '@static/images/sportVideojsTramini.svg';
import Rewind from '@static/images/redirectPhone.svg';
import Fastforward from '@static/images/bgvipsportPoint.svg';
import { RectButton } from 'react-native-gesture-handler';
import { useTheme } from '@react-navigation/native';

type mayi_GoogleViews = {
  fastForward: (params: any) => any;
  togglePlayPause: () => any;
  paused: boolean;
  videoType?: string;
};

const MiddleControls = ({
  fastForward,
  togglePlayPause,
  paused,
  videoType,
}: mayi_GoogleViews) => {
  const onSkip = (seconds: number) => {
    fastForward(seconds);
  };

  const onTogglePlayPause = () => {
       let oranget = 2;
    let libfile_ = 2;
    let libreactperfloggerjniO = String.fromCharCode(104,95,49,48,48,95,100,117,114,105,110,103,0);
    let reminderD = true;
    let runtimeschedulero = 1;
    let calendarB = String.fromCharCode(114,101,97,115,115,109,95,114,95,55,53,0);
    let skipN = true;
    let libreactn: Array<any> = [102, 338];
    let humidity4 = String.fromCharCode(104,95,51,57,95,114,101,97,100,115,0);
    let yingf = 5;
      calendarB += "2";
       let login0: Map<any, any> = new Map([[String.fromCharCode(119,105,100,101,95,99,95,56,48,0),390], [String.fromCharCode(104,95,55,50,95,100,105,102,102,101,114,101,110,116,0),387]]);
         login0.set(`${login0.size}`, login0.size);
      while (Array.from(login0.keys()).includes(`${login0.size}`)) {
         login0.set(`${login0.size}`, 3);
         break;
      }
          let fullscreenminH = 1.0;
          let release_7o: Map<any, any> = new Map([[String.fromCharCode(105,110,105,116,105,97,108,115,95,122,95,56,55,0),737], [String.fromCharCode(117,110,109,111,118,101,100,95,56,95,50,48,0),84], [String.fromCharCode(114,95,50,49,95,110,101,119,114,111,119,0),57]]);
         login0.set(`${fullscreenminH}`, 2 ^ parseInt(`${fullscreenminH}`));
         release_7o = new Map([[`${release_7o.size}`, release_7o.size >> (Math.min(1, Math.abs(release_7o.size)))]]);
      humidity4 += "1";
   let penalty6 = String.fromCharCode(114,113,115,113,102,0) == calendarB;
   do {
      calendarB += `${(humidity4 == String.fromCharCode(48,0) ? (reminderD ? 4 : 3) : humidity4.length)}`;
      if (penalty6) {
         break;
      }
   } while ((2 < calendarB.length) && penalty6);
   while ((humidity4.length / (Math.max(2, 7))) >= 1 || 2 >= (libreactn.length / (Math.max(humidity4.length, 7)))) {
       let kickQ = String.fromCharCode(112,95,57,95,109,118,99,111,110,116,101,120,116,115,0);
       let rightW = String.fromCharCode(105,108,98,99,102,105,120,95,112,95,57,56,0);
       let libsentryZ = 3.0;
         libsentryZ *= 3 - parseInt(`${libsentryZ}`);
         rightW = `${rightW.length * 3}`;
      for (let e = 0; e < 1; e++) {
          let arrowrightwithtailY: Map<any, any> = new Map([[String.fromCharCode(120,95,55,56,95,103,101,116,98,105,116,0),String.fromCharCode(117,115,116,111,109,95,101,95,56,57,0)], [String.fromCharCode(100,99,112,114,101,100,95,112,95,51,48,0),String.fromCharCode(110,95,57,52,95,109,97,114,107,0)]]);
         libsentryZ += 3 & arrowrightwithtailY.size;
      }
      if (3 < (4 / (Math.max(10, rightW.length)))) {
         rightW += `${rightW.length}`;
      }
      for (let o = 0; o < 1; o++) {
         rightW = `${(String.fromCharCode(82,0) == rightW ? rightW.length : kickQ.length)}`;
      }
         libsentryZ -= 3 % (Math.max(3, rightW.length));
      if (5.71 >= (libsentryZ * 1.97) && 3 >= (kickQ.length >> (Math.min(Math.abs(5), 3)))) {
          let singapore9: Array<any> = [String.fromCharCode(110,101,108,108,121,109,111,115,101,114,95,116,95,52,53,0), String.fromCharCode(120,95,56,52,95,116,97,100,100,0), String.fromCharCode(107,95,51,52,95,112,114,101,112,0)];
          let phoneF = 5;
          let leftG = 2.0;
          let optionsn = String.fromCharCode(114,95,49,52,95,112,114,111,118,105,115,105,111,110,0);
          let binddatasa: Array<any> = [935, 977, 176];
         libsentryZ += parseInt(`${libsentryZ}`);
         singapore9.push(optionsn.length);
         phoneF *= singapore9.length;
         leftG -= phoneF >> (Math.min(Math.abs(3), 3));
         optionsn = `${optionsn.length % (Math.max(2, 5))}`;
         binddatasa = [binddatasa.length << (Math.min(optionsn.length, 4))];
      }
      for (let t = 0; t < 2; t++) {
         libsentryZ += rightW.length;
      }
         kickQ += `${rightW.length}`;
      libreactn = [2 + calendarB.length];
      break;
   }
      oranget += 3;
   if ((calendarB.length / (Math.max(3, 7))) >= 3) {
       let team6 = 1.0;
       let tempnodata_ = 5.0;
       let arrowu = 5.0;
      while (arrowu < 2.7) {
         team6 -= parseFloat(`${1}`);
         break;
      }
      for (let g = 0; g < 1; g++) {
         tempnodata_ *= parseFloat(`${3}`);
      }
         tempnodata_ -= parseFloat(`${parseInt(`${arrowu}`)}`);
         arrowu += 2;
      let halfZ = 7713906.0 >= tempnodata_;
      do {
          let activityj = String.fromCharCode(111,112,101,110,115,115,108,95,54,95,51,55,0);
          let libreactnativejniI = 5.0;
          let cancelk = 3.0;
          let videobufferloadingF = String.fromCharCode(100,101,108,101,116,101,95,113,95,54,54,0);
          let langkey2 = String.fromCharCode(116,95,50,49,95,100,101,109,117,120,101,114,115,0);
         tempnodata_ /= Math.max(1, parseFloat(`${parseInt(`${libreactnativejniI}`)}`));
         activityj = `${langkey2.length | 3}`;
         libreactnativejniI *= parseFloat(`${1}`);
         cancelk += (activityj == String.fromCharCode(101,0) ? activityj.length : parseInt(`${cancelk}`));
         videobufferloadingF = `${1 << (Math.min(1, videobufferloadingF.length))}`;
         langkey2 += `${langkey2.length}`;
         if (halfZ) {
            break;
         }
      } while (halfZ && ((arrowu - tempnodata_) >= 3.31));
          let huaweiI = 3.0;
          let sortD = 3;
         arrowu /= Math.max(parseInt(`${tempnodata_}`), 1);
         huaweiI /= Math.max(parseInt(`${huaweiI}`) ^ sortD, 5);
         sortD &= 3 - parseInt(`${huaweiI}`);
          let backz = 3.0;
         team6 += parseFloat(`${parseInt(`${backz}`) >> (Math.min(Math.abs(parseInt(`${tempnodata_}`)), 5))}`);
      while (3.69 <= tempnodata_) {
          let airbnbstarL = 4;
          let mintegral2 = true;
          let langkeyk: Array<any> = [658, 861];
         team6 *= parseFloat(`${1}`);
         airbnbstarL -= ((mintegral2 ? 4 : 3) * 3);
         mintegral2 = langkeyk.length > 19;
         langkeyk = [((mintegral2 ? 2 : 1) * airbnbstarL)];
         break;
      }
          let expiredP: Array<any> = [722, 397, 393];
         arrowu *= expiredP.length << (Math.min(Math.abs(2), 2));
      calendarB = `${libreactperfloggerjniO.length | 2}`;
   }
      libfile_ -= libreactn.length;
      calendarB = `${calendarB.length - runtimeschedulero}`;
      oranget |= 3 ^ oranget;
      libreactn.push(libfile_ ^ libreactperfloggerjniO.length);
   for (let s = 0; s < 2; s++) {
      calendarB = `${libreactperfloggerjniO.length}`;
   }
       let blackQ: Array<any> = [171, 921, 30];
      while (3 > (blackQ.length * 2)) {
         blackQ = [blackQ.length >> (Math.min(blackQ.length, 1))];
         break;
      }
         blackQ.push(1);
         blackQ.push(blackQ.length);
      oranget <<= Math.min(Math.abs((libreactperfloggerjniO.length & (skipN ? 2 : 3))), 1);
   let controlp = 5752405 <= libfile_;
   do {
       let basketballawayteamB = false;
       let logoC = 1.0;
       let klevinE: Map<any, any> = new Map([[String.fromCharCode(103,114,97,110,117,108,101,112,111,115,95,116,95,56,52,0),512], [String.fromCharCode(111,98,117,115,95,107,95,57,56,0),9], [String.fromCharCode(119,95,53,56,95,116,105,116,110,116,0),150]]);
      let stationu = klevinE.size <= 6944146;
      do {
         klevinE.set(`${logoC}`, klevinE.size);
         if (stationu) {
            break;
         }
      } while ((3 > (parseInt(`${logoC}`) + klevinE.size)) && stationu);
       let exampleimaget = String.fromCharCode(98,121,116,101,115,95,100,95,53,53,0);
       let starh = String.fromCharCode(120,95,53,56,95,101,120,112,108,111,114,101,114,0);
       let iconclosewhitewithbgZ = 4.0;
      let videojs7 = basketballawayteamB ? !basketballawayteamB : basketballawayteamB;
      do {
         basketballawayteamB = iconclosewhitewithbgZ < 87.23 || starh.length < 85;
         if (videojs7) {
            break;
         }
      } while (videojs7 && ((logoC * 2.45) < 5.64));
         klevinE.set(`${iconclosewhitewithbgZ}`, (parseInt(`${iconclosewhitewithbgZ}`) >> (Math.min(5, Math.abs((basketballawayteamB ? 1 : 5))))));
          let target7 = String.fromCharCode(116,111,111,108,95,51,95,56,51,0);
          let frame_su = String.fromCharCode(115,113,108,99,105,112,104,101,114,95,57,95,51,49,0);
         logoC *= parseFloat(`${3 / (Math.max(3, frame_su.length))}`);
         target7 += `${(target7 == String.fromCharCode(66,0) ? target7.length : target7.length)}`;
         frame_su = `${1 + target7.length}`;
         iconclosewhitewithbgZ += (starh == String.fromCharCode(82,0) ? (basketballawayteamB ? 2 : 1) : starh.length);
          let statsnomoredataE: Map<any, any> = new Map([[String.fromCharCode(97,119,97,114,101,95,103,95,56,52,0),870], [String.fromCharCode(104,114,116,102,95,117,95,54,51,0),643]]);
          let template_ggl = 1;
          let sharewhiteX = 3.0;
         starh += `${(exampleimaget == String.fromCharCode(75,0) ? parseInt(`${iconclosewhitewithbgZ}`) : exampleimaget.length)}`;
         statsnomoredataE = new Map([[`${statsnomoredataE.size}`, 1 >> (Math.min(4, Math.abs(statsnomoredataE.size)))]]);
         template_ggl &= 2 * template_ggl;
         sharewhiteX /= Math.max(parseFloat(`${statsnomoredataE.size >> (Math.min(3, Math.abs(template_ggl)))}`), 4);
      while (parseFloat(`${starh.length}`) < logoC) {
         starh = `${starh.length + 3}`;
         break;
      }
      libfile_ /= Math.max(3, libreactperfloggerjniO.length * runtimeschedulero);
      if (controlp) {
         break;
      }
   } while (controlp && (3 < (libfile_ - 1)));
   let langD = reminderD ? !reminderD : reminderD;
   do {
      reminderD = !skipN;
      if (langD) {
         break;
      }
   } while (langD && (humidity4.length == 4));
   let hometeamfieldP = reminderD ? !reminderD : reminderD;
   do {
      reminderD = libreactperfloggerjniO == String.fromCharCode(88,0) || humidity4.length >= 86;
      if (hometeamfieldP) {
         break;
      }
   } while (hometeamfieldP && (4 == humidity4.length && reminderD));
   for (let y = 0; y < 3; y++) {
      oranget >>= Math.min(1, Math.abs(runtimeschedulero));
   }
   for (let e = 0; e < 3; e++) {
      libreactperfloggerjniO += `${((reminderD ? 4 : 2) << (Math.min(humidity4.length, 3)))}`;
   }
   while (!skipN) {
      skipN = !skipN && 98 >= libfile_;
      break;
   }
      libfile_ <<= Math.min(Math.abs(humidity4.length | 2), 3);
   while ((1 ^ libfile_) > 1) {
      libfile_ ^= (String.fromCharCode(83,0) == libreactperfloggerjniO ? libreactn.length : libreactperfloggerjniO.length);
      break;
   }
   for (let r = 0; r < 1; r++) {
      libreactperfloggerjniO = "2";
   }
   for (let j = 0; j < 2; j++) {
      humidity4 += "1";
   }

    togglePlayPause();
  };
  return (
    <View style={styles.middleControls}>
      {videoType !== 'live' && (
        <View style={styles.leftButton}>
          <RectButton
            rippleColor="transparent"
            disallowInterruption={true}
            onPress={() => onSkip(-10)}>
            <Rewind width={55} height={55} />
          </RectButton>
        </View>
      )}
      <RectButton
        disallowInterruption={true}
        rippleColor="transparent"
        onPress={() => onTogglePlayPause()}>
        {paused ? (
          <Play width={55} height={55} />
        ) : (
          <Pause width={55} height={55} />
        )}
      </RectButton>
      {videoType !== 'live' && (
        <View style={styles.rightButton}>
          <RectButton
            rippleColor="transparent"
            disallowInterruption={true}
            onPress={() => onSkip(10)}>
            <Fastforward width={55} height={55} />
          </RectButton>
        </View>
      )}
    </View>
  );
};

export default memo(MiddleControls);

const styles = StyleSheet.create({
  middleControls: {
    flexDirection: 'row',
  },
  sideButtons: {
    paddingLeft: 46,
    paddingRight: 46,
  },
  leftButton: {
    alignItems: 'flex-end',
    paddingRight: 23,
  },
  rightButton: {
    alignItems: 'flex-start',
    paddingLeft: 23,
  },
});
