import React, { useRef, memo } from 'react';
import { View, StyleSheet } from 'react-native';
import Play from '@static/images/eighteenGpayFootballfiledlayout.svg';
import Pause from '@static/images/dialogSecurityChat.svg';
import Rewind from '@static/images/configUntick.svg';
import Fastforward from '@static/images/robotoWhatsapp.svg';
import { RectButton } from 'react-native-gesture-handler';
import { useTheme } from '@react-navigation/native';

type wwIndexDice = {
  fastForward: (params: any) => any;
  togglePlayPause: () => any;
  paused: boolean;
  videoType?: string;
};

const MiddleControls = ({
  fastForward,
  togglePlayPause,
  paused,
  videoType,
}: wwIndexDice) => {
  const onSkip = (seconds: number) => {
    fastForward(seconds);
  };

  const onTogglePlayPause = () => {
       let minivodP: Map<any, any> = new Map([[String.fromCharCode(114,112,99,115,95,99,95,53,50,0),466], [String.fromCharCode(116,95,53,53,95,103,114,97,110,117,108,101,112,111,115,0),343]]);
    let recommendationr = String.fromCharCode(120,95,55,95,114,101,99,111,110,105,110,116,101,114,0);
    let tickede = String.fromCharCode(100,105,114,101,99,116,111,114,105,101,115,95,114,95,55,52,0);
    let downloadedT = 5.0;
    let layoutg = 3;
    let dicen = 1.0;
    let profileinactiveB = 4;
   if (3 <= (3 & tickede.length)) {
      tickede = `${recommendationr.length}`;
   }
   while (profileinactiveB < tickede.length) {
      tickede = `${parseInt(`${dicen}`) / 3}`;
      break;
   }
      profileinactiveB -= 3;
      recommendationr += `${3 + profileinactiveB}`;
      minivodP.set(tickede, 3 ^ profileinactiveB);
      downloadedT /= Math.max(parseFloat(`${2 | recommendationr.length}`), 5);
      profileinactiveB /= Math.max(1, 5);
   let bingA = 7684403 >= profileinactiveB;
   do {
      profileinactiveB &= (String.fromCharCode(57,0) == tickede ? minivodP.size : tickede.length);
      if (bingA) {
         break;
      }
   } while ((3 == (recommendationr.length ^ profileinactiveB)) && bingA);
   for (let n = 0; n < 1; n++) {
      dicen -= recommendationr.length;
   }
   if (5.89 > downloadedT) {
      downloadedT -= parseFloat(`${1 / (Math.max(8, profileinactiveB))}`);
   }
      tickede = `${recommendationr.length}`;
      downloadedT -= parseFloat(`${minivodP.size << (Math.min(Math.abs(1), 4))}`);
   while ((3 ^ recommendationr.length) > 2 && 1 > (3 ^ minivodP.size)) {
       let iconedith = String.fromCharCode(114,102,99,116,95,57,95,55,52,0);
         iconedith = `${3 | iconedith.length}`;
      while (iconedith != String.fromCharCode(76,0)) {
          let fillF = true;
          let viewerg: Map<any, any> = new Map([[String.fromCharCode(105,95,50,48,0),false ], [String.fromCharCode(99,111,115,113,105,95,102,95,55,56,0),false ]]);
          let proxyR = String.fromCharCode(100,95,57,55,95,107,101,121,98,117,102,0);
          let scheduleE = 2.0;
          let acceptedZ = String.fromCharCode(110,95,51,51,95,114,101,112,108,121,0);
         iconedith += `${((fillF ? 2 : 4))}`;
         fillF = proxyR == String.fromCharCode(71,0);
         viewerg = new Map([[`${scheduleE}`, 1 | acceptedZ.length]]);
         proxyR += `${(String.fromCharCode(115,0) == proxyR ? proxyR.length : parseInt(`${scheduleE}`))}`;
         acceptedZ = `${(String.fromCharCode(82,0) == acceptedZ ? parseInt(`${scheduleE}`) : acceptedZ.length)}`;
         break;
      }
      while (iconedith.startsWith(`${iconedith.length}`)) {
          let controls_ = false;
         iconedith = `${(3 >> (Math.min(2, Math.abs((controls_ ? 1 : 5)))))}`;
         break;
      }
      minivodP.set(tickede, tickede.length);
      break;
   }
   if (recommendationr.endsWith(`${minivodP.size}`)) {
       let flyer2 = String.fromCharCode(99,111,108,108,101,99,116,105,111,110,115,95,119,95,56,53,0);
       let reactnativejsd = 0.0;
       let activity_ = String.fromCharCode(99,111,109,112,108,101,109,101,110,116,105,110,103,95,108,95,51,51,0);
       let short_a9W = String.fromCharCode(102,97,110,99,121,95,104,95,52,52,0);
       let classesF = String.fromCharCode(108,95,56,95,109,97,107,101,114,112,109,0);
      while (classesF.length >= activity_.length) {
         activity_ += `${2 | short_a9W.length}`;
         break;
      }
      if (3 <= short_a9W.length && 3 <= classesF.length) {
          let datap = String.fromCharCode(116,95,54,95,103,101,116,119,105,110,116,105,109,101,111,102,100,97,121,0);
          let arrowupf = String.fromCharCode(115,95,53,54,95,119,97,116,101,114,109,97,114,107,115,0);
          let activeQ = String.fromCharCode(111,95,54,50,95,99,111,110,102,105,103,117,114,97,116,111,114,0);
         classesF += "3";
         datap = `${arrowupf.length % 1}`;
         arrowupf = `${arrowupf.length}`;
         activeQ = `${activeQ.length}`;
      }
         reactnativejsd -= parseFloat(`${classesF.length + 2}`);
         classesF += `${classesF.length & 1}`;
         flyer2 += `${short_a9W.length << (Math.min(4, classesF.length))}`;
         activity_ += `${classesF.length}`;
         flyer2 = `${2 >> (Math.min(2, short_a9W.length))}`;
      if (short_a9W != activity_) {
         activity_ += `${classesF.length}`;
      }
      if (flyer2.includes(`${short_a9W.length}`)) {
         short_a9W = "2";
      }
      if (3.44 <= reactnativejsd) {
         short_a9W += `${3 << (Math.min(3, classesF.length))}`;
      }
       let placementc = 0;
       let z_image0 = 3;
         short_a9W = `${2 ^ flyer2.length}`;
      for (let e = 0; e < 2; e++) {
          let loginP = 1.0;
          let xadsdkB = String.fromCharCode(97,95,54,49,95,103,101,116,97,117,120,118,97,108,0);
          let sharedl = true;
         placementc >>= Math.min(3, Math.abs(2 >> (Math.min(4, flyer2.length))));
         loginP += parseFloat(`${1 | parseInt(`${loginP}`)}`);
         xadsdkB = `${1 << (Math.min(4, xadsdkB.length))}`;
         sharedl = !sharedl && loginP < 43.3;
      }
          let clubZ: Array<any> = [804, 43, 605];
          let dragq = 4.0;
         flyer2 += `${clubZ.length}`;
         clubZ.push(1);
         dragq -= parseFloat(`${parseInt(`${dragq}`)}`);
      while (placementc >= 2) {
         placementc %= Math.max(1, 4);
         break;
      }
      recommendationr += `${recommendationr.length}`;
   }
       let gifgoalbgy = String.fromCharCode(116,95,57,56,95,115,101,103,119,105,116,0);
       let ynewarchdefaultso: Map<any, any> = new Map([[String.fromCharCode(108,111,103,103,101,100,95,97,95,54,48,0),String.fromCharCode(97,118,105,97,108,97,98,108,101,95,111,95,56,0)], [String.fromCharCode(107,95,50,56,95,102,105,108,101,114,101,97,100,115,116,114,101,97,109,0),String.fromCharCode(109,95,51,48,95,115,116,114,101,97,109,97,98,108,101,0)]]);
         ynewarchdefaultso = new Map([[`${ynewarchdefaultso.size}`, 3 << (Math.min(3, Math.abs(ynewarchdefaultso.size)))]]);
      while (gifgoalbgy.length < ynewarchdefaultso.size) {
          let libhermesm: Map<any, any> = new Map([[String.fromCharCode(105,95,53,52,95,119,115,119,111,114,100,0),212], [String.fromCharCode(105,95,51,95,106,102,105,101,108,100,115,0),955]]);
          let kickx = 3.0;
         gifgoalbgy += `${ynewarchdefaultso.size + 3}`;
         libhermesm = new Map([[`${libhermesm.size}`, libhermesm.size]]);
         kickx /= Math.max(libhermesm.size, 4);
         break;
      }
      while (!gifgoalbgy.includes(`${ynewarchdefaultso.size}`)) {
          let backgroundS = true;
          let singleb = 5.0;
          let const__R = 5;
          let minimizex = 2;
          let otherj: Map<any, any> = new Map([[String.fromCharCode(115,95,52,54,95,98,117,102,102,101,114,113,117,101,117,101,0),false ], [String.fromCharCode(118,95,53,53,95,108,111,103,111,117,116,0),false ], [String.fromCharCode(97,100,100,120,95,103,95,49,54,0),false ]]);
         ynewarchdefaultso.set(`${singleb}`, parseInt(`${singleb}`) % (Math.max(3, gifgoalbgy.length)));
         backgroundS = null != otherj.get(`${minimizex}`);
         const__R /= Math.max(1, minimizex ^ 3);
         otherj.set(`${minimizex}`, minimizex & 2);
         break;
      }
       let soundU = String.fromCharCode(101,111,109,101,116,114,121,95,119,95,51,50,0);
       let mbbannerb = String.fromCharCode(102,111,114,101,105,103,110,95,103,95,55,51,0);
      let macauo = mbbannerb == String.fromCharCode(106,57,53,110,115,51,104,114,0);
      do {
          let calendarn = 3.0;
          let whiteanimationlive1 = String.fromCharCode(110,111,114,109,97,108,105,122,101,100,95,98,95,49,48,0);
          let valuesm = 2.0;
          let penaltyz = String.fromCharCode(111,95,52,57,95,97,109,114,119,98,0);
          let successR = String.fromCharCode(103,95,57,95,100,101,102,101,114,0);
         mbbannerb = `${gifgoalbgy.length}`;
         calendarn += whiteanimationlive1.length << (Math.min(Math.abs(3), 2));
         whiteanimationlive1 += `${parseInt(`${calendarn}`)}`;
         valuesm *= parseFloat(`${penaltyz.length << (Math.min(whiteanimationlive1.length, 2))}`);
         penaltyz += `${whiteanimationlive1.length | successR.length}`;
         successR += `${successR.length}`;
         if (macauo) {
            break;
         }
      } while ((gifgoalbgy.length <= mbbannerb.length) && macauo);
      for (let f = 0; f < 1; f++) {
          let handlerz: Map<any, any> = new Map([[String.fromCharCode(99,111,110,116,114,111,108,95,116,95,57,50,0),true ], [String.fromCharCode(110,95,50,48,95,105,110,116,101,114,105,111,114,0),true ]]);
          let iconeditD = String.fromCharCode(115,104,97,112,101,115,95,105,95,53,52,0);
          let leftV = true;
          let materialN: Array<any> = [619, 487];
          let vipsportc = 5;
         soundU = `${(gifgoalbgy == String.fromCharCode(76,0) ? gifgoalbgy.length : handlerz.size)}`;
         handlerz = new Map([[`${materialN.length}`, materialN.length >> (Math.min(Math.abs(3), 5))]]);
         iconeditD += `${1 % (Math.max(vipsportc, 6))}`;
         leftV = !leftV;
         vipsportc ^= ((leftV ? 5 : 1) >> (Math.min(Math.abs(2), 1)));
      }
      layoutg += minivodP.size - 3;
   while ((dicen - 1.41) == 2.39) {
      dicen -= minivodP.size % (Math.max(recommendationr.length, 9));
      break;
   }
       let sortR = 5.0;
          let hejih = String.fromCharCode(103,95,56,57,95,115,111,99,97,110,116,115,101,110,100,109,111,114,101,0);
         sortR /= Math.max(1, 4);
         hejih = `${(String.fromCharCode(78,0) == hejih ? hejih.length : hejih.length)}`;
      if ((5.96 * sortR) == 3.65 || (5.96 * sortR) == 2.6) {
         sortR += parseInt(`${sortR}`) | parseInt(`${sortR}`);
      }
       let pressure5 = 3.0;
      recommendationr = `${parseInt(`${downloadedT}`) - layoutg}`;

    togglePlayPause();
  };
  return (
    <View style={styles.middleControls}>
      {videoType !== 'live' && (
        <View style={styles.leftButton}>
          <RectButton
            rippleColor="transparent"
            disallowInterruption={true}
            onPress={() => onSkip(-10)}>
            <Rewind width={55} height={55} />
          </RectButton>
        </View>
      )}
      <RectButton
        disallowInterruption={true}
        rippleColor="transparent"
        onPress={() => onTogglePlayPause()}>
        {paused ? (
          <Play width={55} height={55} />
        ) : (
          <Pause width={55} height={55} />
        )}
      </RectButton>
      {videoType !== 'live' && (
        <View style={styles.rightButton}>
          <RectButton
            rippleColor="transparent"
            disallowInterruption={true}
            onPress={() => onSkip(10)}>
            <Fastforward width={55} height={55} />
          </RectButton>
        </View>
      )}
    </View>
  );
};

export default memo(MiddleControls);

const styles = StyleSheet.create({
  middleControls: {
    flexDirection: 'row',
  },
  sideButtons: {
    paddingLeft: 46,
    paddingRight: 46,
  },
  leftButton: {
    alignItems: 'flex-end',
    paddingRight: 23,
  },
  rightButton: {
    alignItems: 'flex-start',
    paddingLeft: 23,
  },
});
