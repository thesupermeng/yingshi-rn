import React, { useRef, memo } from 'react';
import { View, StyleSheet } from 'react-native';
import Play from '@static/images/leftBodanHumidity.svg';
import Pause from '@static/images/nterstitialAssistCarousel.svg';
import Rewind from '@static/images/diceLauncher.svg';
import Fastforward from '@static/images/securityAndroid.svg';
import { RectButton } from 'react-native-gesture-handler';
import { useTheme } from '@react-navigation/native';

type ttSmall = {
  fastForward: (params: any) => any;
  togglePlayPause: () => any;
  paused: boolean;
  videoType?: string;
};

const MiddleControls = ({
  fastForward,
  togglePlayPause,
  paused,
  videoType,
}: ttSmall) => {
  const onSkip = (seconds: number) => {
    fastForward(seconds);
  };

  const onTogglePlayPause = () => {
       let assistU = 0;
    let plusg = String.fromCharCode(101,95,54,48,95,103,101,116,115,116,114,0);
    let singleb: Array<any> = [942, 493];
    let videocommonw: Array<any> = [869, 448];
    let navigationV = String.fromCharCode(100,95,57,49,95,97,99,102,102,0);
    let temperaturei = String.fromCharCode(118,101,114,105,102,105,101,100,95,118,95,57,53,0);
    let refresh5 = 0.0;
    let handlerE = 3.0;
    let reportg: Map<any, any> = new Map([[String.fromCharCode(110,95,54,51,95,108,105,98,115,0),847], [String.fromCharCode(101,112,105,115,111,100,101,95,105,95,55,56,0),624], [String.fromCharCode(111,95,50,52,95,102,117,108,108,121,0),374]]);
    let weiboM = 5.0;
    let assistb = 1;
    let networkk = String.fromCharCode(119,95,54,52,95,99,111,110,102,105,103,117,114,101,0);
    let tail0 = false;
    let rewardj: Array<any> = [438, 46];
   if ((handlerE / (Math.max(parseFloat(`${navigationV.length}`), 9))) >= 3.73 || 4 >= (navigationV.length + 1)) {
      handlerE += parseFloat(`${assistU}`);
   }
   let privacyD = assistU >= 9490220;
   do {
      assistU ^= assistU;
      if (privacyD) {
         break;
      }
   } while ((2 >= (assistU & 3) || (singleb.length & 3) >= 5) && privacyD);
       let control6 = false;
       let pointk: Map<any, any> = new Map([[String.fromCharCode(98,117,102,102,95,115,95,49,52,0),93], [String.fromCharCode(114,103,98,116,101,115,116,95,50,95,49,51,0),598], [String.fromCharCode(106,95,50,49,95,100,105,99,116,105,111,110,97,114,121,0),142]]);
       let nextg: Map<any, any> = new Map([[String.fromCharCode(105,99,111,110,105,99,95,101,95,54,49,0),621], [String.fromCharCode(103,95,54,49,95,99,111,110,116,114,97,99,116,0),970]]);
         control6 = (((!control6 ? pointk.size : 74) * pointk.size) <= 7);
         control6 = 27 > nextg.size || 27 > pointk.size;
          let activei = 1.0;
          let delegate_32 = 4;
         nextg = new Map([[`${delegate_32}`, 3 | delegate_32]]);
         activei -= 1;
      while (control6) {
         pointk = new Map([[`${pointk.size}`, nextg.size]]);
         break;
      }
      if ((5 / (Math.max(1, pointk.size))) < 3) {
         control6 = !control6;
      }
          let sortA = 5.0;
         pointk.set(`${sortA}`, parseInt(`${sortA}`));
      if (nextg.size == 4 || 1 == (4 >> (Math.min(5, Math.abs(nextg.size))))) {
          let greyY = 3.0;
          let temperatureL = 3;
          let temperatureM = 4.0;
         nextg = new Map([[`${pointk.size}`, ((control6 ? 3 : 4) | pointk.size)]]);
         greyY -= temperatureL / (Math.max(parseInt(`${temperatureM}`), 7));
         temperatureL ^= parseInt(`${temperatureM}`) * 1;
      }
         pointk.set(`${pointk.size}`, 1 & pointk.size);
         control6 = 52 < pointk.size;
      videocommonw = [1 ^ parseInt(`${handlerE}`)];
      handlerE += parseFloat(`${singleb.length}`);
   for (let t = 0; t < 1; t++) {
      videocommonw.push(parseInt(`${handlerE}`) >> (Math.min(singleb.length, 2)));
   }
   for (let b = 0; b < 2; b++) {
       let utilsN = String.fromCharCode(102,97,113,95,114,95,55,55,0);
       let overlayD: Array<any> = [145, 692];
       let nalyticsA = 2;
       let tailA = String.fromCharCode(114,95,48,95,112,114,101,116,116,121,0);
       let googleh = 0;
         utilsN = `${2 - nalyticsA}`;
      while (!overlayD.includes(nalyticsA)) {
         nalyticsA %= Math.max(4, (utilsN == String.fromCharCode(73,0) ? utilsN.length : googleh));
         break;
      }
          let anythinkL = 3.0;
          let logok: Map<any, any> = new Map([[String.fromCharCode(100,95,55,49,95,106,118,101,114,115,105,111,110,0),826], [String.fromCharCode(106,95,50,95,105,110,116,114,101,112,111,108,0),854]]);
         overlayD = [tailA.length + 2];
         anythinkL *= 2;
         logok = new Map([[`${logok.size}`, parseInt(`${anythinkL}`) ^ 1]]);
         utilsN = "1";
      let vietnamf = 9328348 >= googleh;
      do {
         googleh -= tailA.length >> (Math.min(3, Math.abs(googleh)));
         if (vietnamf) {
            break;
         }
      } while (vietnamf && (tailA.endsWith(`${googleh}`)));
         tailA = `${nalyticsA << (Math.min(Math.abs(1), 2))}`;
         tailA = "2";
          let termsy = 0.0;
          let shirtE = false;
          let relatedK = 4.0;
         tailA = `${googleh}`;
         termsy -= (parseInt(`${relatedK}`) ^ (shirtE ? 1 : 2));
         shirtE = !shirtE;
         relatedK /= Math.max(5, parseFloat(`${parseInt(`${termsy}`) - 2}`));
         overlayD.push(1 << (Math.min(1, tailA.length)));
      while ((1 << (Math.min(3, tailA.length))) == 3) {
         googleh &= tailA.length;
         break;
      }
          let goalK: Map<any, any> = new Map([[String.fromCharCode(112,114,111,109,111,95,55,95,56,53,0),339], [String.fromCharCode(113,95,55,57,95,115,111,99,114,101,97,116,101,0),483]]);
         googleh -= goalK.size;
      singleb.push(tailA.length * navigationV.length);
   }
   if (5.26 == (handlerE / (Math.max(3, refresh5))) && (refresh5 / 5.26) == 4.94) {
      handlerE /= Math.max(3, parseFloat(`${assistU & parseInt(`${handlerE}`)}`));
   }
       let downloaderN: Map<any, any> = new Map([[String.fromCharCode(103,95,57,50,95,115,116,97,116,105,111,110,97,114,121,0),549], [String.fromCharCode(115,105,100,101,115,95,122,95,53,56,0),590]]);
          let sigmobI = false;
         downloaderN = new Map([[`${downloaderN.size}`, 3]]);
      if (!Array.from(downloaderN.values()).includes(downloaderN.size)) {
          let eactV = 4;
          let suggestionp = String.fromCharCode(108,95,57,53,95,120,99,117,114,115,111,114,0);
          let description_fsd: Map<any, any> = new Map([[String.fromCharCode(101,95,56,55,95,97,108,108,101,116,0),853], [String.fromCharCode(107,95,56,51,95,102,105,108,101,102,117,110,99,0),409], [String.fromCharCode(97,100,100,114,95,49,95,51,55,0),439]]);
          let internett = 1;
         downloaderN = new Map([[`${downloaderN.size}`, suggestionp.length / (Math.max(5, downloaderN.size))]]);
         eactV ^= internett - 1;
         suggestionp += "1";
         description_fsd.set(`${internett}`, internett);
      }
      let mbjscommong = downloaderN.size >= 6834450;
      do {
         downloaderN = new Map([[`${downloaderN.size}`, 1]]);
         if (mbjscommong) {
            break;
         }
      } while (mbjscommong && (Array.from(downloaderN.keys()).includes(`${downloaderN.size}`)));
      refresh5 *= parseFloat(`${videocommonw.length / (Math.max(3, 2))}`);
      videocommonw = [1];
       let mbsplashU = String.fromCharCode(109,95,55,54,95,110,105,101,108,115,97,100,100,0);
       let short_xy = String.fromCharCode(100,101,113,117,97,110,116,95,121,95,50,51,0);
         mbsplashU += `${short_xy.length % (Math.max(1, 3))}`;
      while (!short_xy.endsWith(mbsplashU)) {
          let annerz = false;
          let showI = String.fromCharCode(116,95,50,56,95,97,116,111,109,115,0);
         mbsplashU = `${(mbsplashU.length << (Math.min(1, Math.abs((annerz ? 5 : 1)))))}`;
         annerz = (showI.length << (Math.min(showI.length, 2))) >= 77;
         break;
      }
      if (short_xy.includes(`${mbsplashU.length}`)) {
          let uploadK = String.fromCharCode(118,95,56,50,95,100,101,99,101,108,101,114,97,116,101,100,0);
          let roomQ = 2.0;
          let headerI = 3;
         mbsplashU += `${uploadK.length}`;
         uploadK += `${2 << (Math.min(Math.abs(headerI), 2))}`;
         roomQ /= Math.max(parseInt(`${roomQ}`), 1);
         headerI <<= Math.min(3, Math.abs(headerI));
      }
       let regeng_ = 5.0;
       let descc = 0.0;
      if (regeng_ < 3.44) {
         descc -= parseFloat(`${parseInt(`${descc}`)}`);
      }
          let otherw = 4.0;
         short_xy += `${mbsplashU.length}`;
         otherw += parseFloat(`${2}`);
      singleb.push(3);
      plusg = "3";
   while (Array.from(reportg.values()).includes(weiboM)) {
      reportg = new Map([[`${videocommonw.length}`, 3 >> (Math.min(5, videocommonw.length))]]);
      break;
   }
      singleb = [plusg.length];
   for (let j = 0; j < 2; j++) {
       let manifestx = String.fromCharCode(98,95,54,95,114,116,99,100,0);
       let expiredQ = 4;
       let toponO = 5.0;
       let borderlessu = 5.0;
         manifestx = `${expiredQ}`;
      for (let s = 0; s < 2; s++) {
         borderlessu *= expiredQ % (Math.max(6, parseInt(`${toponO}`)));
      }
      if (manifestx.length > 5) {
         toponO *= parseInt(`${borderlessu}`) | manifestx.length;
      }
         expiredQ /= Math.max(4, parseInt(`${toponO}`));
          let privacyg = String.fromCharCode(121,95,55,48,95,105,110,116,114,97,120,98,108,111,99,107,0);
         borderlessu *= privacyg.length;
      assistU -= plusg.length | 3;
   }
   if ((4 << (Math.min(3, Math.abs(reportg.size)))) < 1) {
       let sansl = String.fromCharCode(101,95,56,52,95,101,109,105,116,0);
         sansl = `${3 + sansl.length}`;
      while (sansl.endsWith(sansl)) {
         sansl += `${sansl.length >> (Math.min(Math.abs(2), 4))}`;
         break;
      }
         sansl += "1";
      reportg.set(`${assistU}`, assistU / (Math.max(8, plusg.length)));
   }
   while ((temperaturei.length ^ singleb.length) == 2) {
       let ajaxk = 1.0;
       let description_aeo = 4;
       let googleY = true;
      while (description_aeo >= 4) {
         googleY = ajaxk < 88.16;
         break;
      }
          let more7 = String.fromCharCode(111,100,105,110,103,95,115,95,55,55,0);
         ajaxk -= parseFloat(`${parseInt(`${ajaxk}`)}`);
         more7 += `${more7.length - more7.length}`;
         description_aeo *= description_aeo;
         description_aeo += 1;
      if ((3 & description_aeo) < 2 || 3.10 < (2.58 - ajaxk)) {
          let scoreT: Array<any> = [68, 105];
          let pressureG = 2.0;
          let userK = String.fromCharCode(113,95,57,56,95,118,111,105,99,101,115,0);
          let playY = String.fromCharCode(102,116,97,98,95,122,95,53,51,0);
         description_aeo <<= Math.min(1, Math.abs((description_aeo * (googleY ? 1 : 5))));
         scoreT = [(userK == String.fromCharCode(55,0) ? userK.length : playY.length)];
         pressureG += parseFloat(`${2}`);
         playY += `${2 & parseInt(`${pressureG}`)}`;
      }
         ajaxk -= parseFloat(`${parseInt(`${ajaxk}`) >> (Math.min(5, Math.abs(3)))}`);
         ajaxk -= parseFloat(`${3 ^ parseInt(`${ajaxk}`)}`);
      if (4 == (description_aeo * 5) || 3 == (description_aeo % 5)) {
         description_aeo ^= description_aeo;
      }
          let downloaderM: Array<any> = [967, 852, 728];
          let bottomq = String.fromCharCode(97,114,103,118,95,57,95,56,50,0);
          let greenh = String.fromCharCode(121,95,53,57,95,102,117,115,101,0);
         ajaxk -= parseFloat(`${downloaderM.length}`);
         downloaderM.push(greenh.length - bottomq.length);
         bottomq = `${(greenh == String.fromCharCode(49,0) ? greenh.length : bottomq.length)}`;
      singleb = [(plusg == String.fromCharCode(114,0) ? plusg.length : assistU)];
      break;
   }
      weiboM *= 2 >> (Math.min(4, navigationV.length));

    togglePlayPause();
  };
  return (
    <View style={styles.middleControls}>
      {videoType !== 'live' && (
        <View style={styles.leftButton}>
          <RectButton
            rippleColor="transparent"
            disallowInterruption={true}
            onPress={() => onSkip(-10)}>
            <Rewind width={55} height={55} />
          </RectButton>
        </View>
      )}
      <RectButton
        disallowInterruption={true}
        rippleColor="transparent"
        onPress={() => onTogglePlayPause()}>
        {paused ? (
          <Play width={55} height={55} />
        ) : (
          <Pause width={55} height={55} />
        )}
      </RectButton>
      {videoType !== 'live' && (
        <View style={styles.rightButton}>
          <RectButton
            rippleColor="transparent"
            disallowInterruption={true}
            onPress={() => onSkip(10)}>
            <Fastforward width={55} height={55} />
          </RectButton>
        </View>
      )}
    </View>
  );
};

export default memo(MiddleControls);

const styles = StyleSheet.create({
  middleControls: {
    flexDirection: 'row',
  },
  sideButtons: {
    paddingLeft: 46,
    paddingRight: 46,
  },
  leftButton: {
    alignItems: 'flex-end',
    paddingRight: 23,
  },
  rightButton: {
    alignItems: 'flex-start',
    paddingLeft: 23,
  },
});
