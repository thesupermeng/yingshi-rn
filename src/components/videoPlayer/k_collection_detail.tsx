import React, { useRef, memo } from 'react';
import { View, StyleSheet } from 'react-native';
import Play from '@static/images/starProduct.svg';
import Pause from '@static/images/descHeji.svg';
import Rewind from '@static/images/styleShrinkZhubo.svg';
import Fastforward from '@static/images/actionTemperature.svg';
import { RectButton } from 'react-native-gesture-handler';
import { useTheme } from '@react-navigation/native';

type XFillButton = {
  fastForward: (params: any) => any;
  togglePlayPause: () => any;
  paused: boolean;
  videoType?: string;
};

const MiddleControls = ({
  fastForward,
  togglePlayPause,
  paused,
  videoType,
}: XFillButton) => {
  const onSkip = (seconds: number) => {
    fastForward(seconds);
  };

  const onTogglePlayPause = () => {
       let roomj = 3.0;
    let models8: Map<any, any> = new Map([[String.fromCharCode(112,95,49,53,95,117,110,105,116,121,0),String.fromCharCode(101,95,49,52,95,109,97,112,115,116,114,105,110,103,0)], [String.fromCharCode(115,95,50,50,95,115,107,105,112,0),String.fromCharCode(118,109,97,102,95,117,95,57,48,0)], [String.fromCharCode(103,108,111,98,95,53,95,57,51,0),String.fromCharCode(97,100,97,112,116,115,95,53,95,51,53,0)]]);
    let shirtA = String.fromCharCode(102,108,105,103,104,116,95,119,95,57,48,0);
    let grayN = 3.0;
    let crownT = 0.0;
    let screena = 3.0;
    let tempc = String.fromCharCode(111,95,57,52,95,101,115,101,110,100,0);
    let fillT = 2;
    let miniQ = String.fromCharCode(111,95,50,56,95,116,101,108,108,0);
    let playG = String.fromCharCode(118,95,49,51,95,114,105,110,100,101,120,0);
    let f_imageL = String.fromCharCode(115,95,53,52,95,117,110,114,101,99,111,103,110,105,122,101,100,0);
    let gray7 = 3.0;
    let ajax4 = String.fromCharCode(114,95,52,56,95,100,101,102,97,117,108,116,99,111,101,102,99,111,117,110,116,115,0);
    let modulen = 1;
    let reminderF = String.fromCharCode(114,95,51,53,95,102,117,108,102,105,108,108,0);
   while (fillT < tempc.length) {
      fillT ^= playG.length;
      break;
   }
      tempc += `${parseInt(`${grayN}`)}`;
   for (let p = 0; p < 1; p++) {
      models8.set(playG, tempc.length & playG.length);
   }
      miniQ += `${miniQ.length & parseInt(`${roomj}`)}`;
   while (tempc.length == playG.length) {
      playG += `${(String.fromCharCode(53,0) == miniQ ? miniQ.length : tempc.length)}`;
      break;
   }
   for (let d = 0; d < 2; d++) {
      f_imageL = `${shirtA.length + parseInt(`${roomj}`)}`;
   }
       let n_locki = String.fromCharCode(121,95,51,55,95,115,117,98,116,114,97,99,116,101,100,0);
       let megaphoneW: Array<any> = [77, 771, 687];
       let constantsK: Array<any> = [String.fromCharCode(109,97,99,101,95,109,95,57,50,0), String.fromCharCode(117,95,54,54,95,114,101,115,105,103,110,0)];
      let google2 = megaphoneW.length <= 4973566;
      do {
          let singleo = 5.0;
          let screen6 = 2;
          let long_w9K: Map<any, any> = new Map([[String.fromCharCode(101,114,112,105,99,95,103,95,49,48,48,0),false ], [String.fromCharCode(116,104,114,111,116,116,108,105,110,103,95,100,95,52,0),true ], [String.fromCharCode(108,95,51,95,104,105,110,116,0),false ]]);
          let navigationf: Array<any> = [769, 525, 841];
          let stationsY: Array<any> = [360, 277, 212];
         megaphoneW.push(1 / (Math.max(10, constantsK.length)));
         singleo *= parseFloat(`${1}`);
         screen6 += 2 ^ navigationf.length;
         long_w9K = new Map([[`${long_w9K.size}`, 3 << (Math.min(5, Math.abs(long_w9K.size)))]]);
         navigationf.push(stationsY.length);
         stationsY.push(parseInt(`${singleo}`) ^ screen6);
         if (google2) {
            break;
         }
      } while (google2 && ((n_locki.length & 3) == 3 || (n_locki.length & megaphoneW.length) == 3));
          let topic2 = true;
          let calendarA: Map<any, any> = new Map([[String.fromCharCode(107,95,51,53,95,112,97,99,107,101,116,112,101,101,107,0),835], [String.fromCharCode(116,95,55,54,95,98,108,117,114,114,97,98,108,101,0),363], [String.fromCharCode(101,120,116,110,95,102,95,57,51,0),34]]);
          let active4: Array<any> = [75, 182, 746];
         megaphoneW.push(megaphoneW.length);
         topic2 = active4.length <= calendarA.size;
         calendarA.set(`${topic2}`, ((topic2 ? 4 : 4) << (Math.min(Math.abs(1), 1))));
         active4 = [(2 * (topic2 ? 5 : 2))];
      crownT += 3;
      f_imageL += `${(miniQ == String.fromCharCode(86,0) ? parseInt(`${grayN}`) : miniQ.length)}`;
   while (2 >= miniQ.length) {
       let vignetteM = 5.0;
       let internetO = String.fromCharCode(117,112,108,111,97,100,105,110,103,95,98,95,50,57,0);
       let contextr = String.fromCharCode(116,97,98,108,101,105,110,105,116,95,109,95,52,53,0);
      if (3.30 == (vignetteM * 4.68) || 4.68 == (vignetteM * internetO.length)) {
          let halfn: Array<any> = [210, 494];
          let pagination1: Array<any> = [String.fromCharCode(108,111,103,102,117,110,99,95,50,95,53,0), String.fromCharCode(109,97,112,95,50,95,50,53,0), String.fromCharCode(115,101,97,114,99,104,105,110,103,95,56,95,55,55,0)];
          let sourceW = String.fromCharCode(114,114,116,114,95,98,95,53,53,0);
          let greene = String.fromCharCode(119,95,56,48,95,97,110,105,109,97,116,101,0);
         vignetteM /= Math.max(4, parseInt(`${vignetteM}`));
         halfn = [greene.length / 3];
         pagination1.push(greene.length);
         sourceW = `${(String.fromCharCode(52,0) == sourceW ? halfn.length : sourceW.length)}`;
      }
         contextr = "2";
      miniQ += `${parseInt(`${grayN}`) << (Math.min(Math.abs(3), 1))}`;
      break;
   }
      f_imageL = `${shirtA.length}`;
   while (parseInt(`${crownT}`) > tempc.length) {
       let quest2 = String.fromCharCode(112,95,54,54,95,110,97,109,101,115,101,114,118,101,114,0);
       let refreshu = String.fromCharCode(109,97,115,107,113,95,109,95,57,50,0);
       let uploadU = 0;
       let sansb = 5;
      let a_position1 = uploadU >= 5772384;
      do {
         uploadU += (String.fromCharCode(113,0) == quest2 ? refreshu.length : quest2.length);
         if (a_position1) {
            break;
         }
      } while ((5 < (uploadU << (Math.min(Math.abs(2), 3))) && (quest2.length << (Math.min(1, Math.abs(uploadU)))) < 2) && a_position1);
      for (let n = 0; n < 2; n++) {
         quest2 = `${refreshu.length}`;
      }
      for (let r = 0; r < 2; r++) {
         uploadU ^= (refreshu == String.fromCharCode(82,0) ? refreshu.length : sansb);
      }
      while (4 > (1 & refreshu.length)) {
          let faviconh: Map<any, any> = new Map([[String.fromCharCode(99,104,111,115,101,110,95,53,95,57,55,0),true ], [String.fromCharCode(107,98,105,116,95,116,95,57,56,0),false ], [String.fromCharCode(102,95,57,51,95,97,110,105,109,97,116,105,111,110,115,0),true ]]);
         sansb += sansb;
         faviconh = new Map([[`${faviconh.size}`, 2]]);
         break;
      }
         sansb >>= Math.min(1, Math.abs(2 & quest2.length));
      while (2 <= (refreshu.length ^ 1)) {
         refreshu = "1";
         break;
      }
      if (1 >= uploadU) {
         uploadU *= quest2.length;
      }
         uploadU |= sansb;
      while (2 == sansb) {
          let uploadA = 3;
          let episodesJ = 1;
         sansb %= Math.max(uploadA, 4);
         episodesJ &= 2 ^ episodesJ;
         break;
      }
      while (quest2.length > 1) {
          let mail2 = 2.0;
         quest2 = `${(String.fromCharCode(99,0) == refreshu ? refreshu.length : parseInt(`${mail2}`))}`;
         break;
      }
         refreshu = `${refreshu.length}`;
      if (5 < (5 | quest2.length)) {
          let telegram9: Map<any, any> = new Map([[String.fromCharCode(117,95,51,55,95,112,114,111,100,117,99,101,0),true ], [String.fromCharCode(101,120,97,109,112,108,101,95,122,95,50,57,0),false ], [String.fromCharCode(98,95,57,50,95,100,99,97,109,97,116,104,0),false ]]);
         quest2 += `${uploadU}`;
         telegram9 = new Map([[`${telegram9.size}`, telegram9.size / 2]]);
      }
      tempc = `${(String.fromCharCode(82,0) == refreshu ? uploadU : refreshu.length)}`;
      break;
   }
      tempc += `${fillT - 1}`;
       let rules9: Array<any> = [204, 811];
      if (!rules9.includes(rules9.length)) {
         rules9 = [2 / (Math.max(2, rules9.length))];
      }
      let predictione = rules9.length >= 8577233;
      do {
         rules9.push(rules9.length);
         if (predictione) {
            break;
         }
      } while (predictione && (rules9.length > rules9.length));
         rules9 = [rules9.length << (Math.min(rules9.length, 4))];
      crownT /= Math.max(1 << (Math.min(5, f_imageL.length)), 2);
      miniQ += `${(String.fromCharCode(97,0) == f_imageL ? f_imageL.length : miniQ.length)}`;
       let settings0 = String.fromCharCode(111,118,101,114,108,97,121,95,50,95,49,56,0);
       let temperatureq: Map<any, any> = new Map([[String.fromCharCode(102,101,101,100,98,97,99,107,95,115,95,51,50,0),21], [String.fromCharCode(115,104,114,117,110,107,95,113,95,55,0),964]]);
      if ((2 >> (Math.min(2, Math.abs(temperatureq.size)))) < 3) {
         temperatureq = new Map([[`${temperatureq.size}`, temperatureq.size >> (Math.min(Math.abs(2), 5))]]);
      }
         temperatureq = new Map([[`${temperatureq.size}`, (settings0 == String.fromCharCode(66,0) ? temperatureq.size : settings0.length)]]);
      fillT <<= Math.min(3, Math.abs(3));
   if (miniQ.startsWith(`${playG.length}`)) {
      playG += `${parseInt(`${screena}`) * 3}`;
   }
   for (let o = 0; o < 1; o++) {
      fillT &= 3;
   }
   while (miniQ.length > playG.length) {
      miniQ = `${models8.size - miniQ.length}`;
      break;
   }
       let saveU: Map<any, any> = new Map([[String.fromCharCode(99,111,110,118,101,114,103,101,110,99,101,95,99,95,49,51,0),true ], [String.fromCharCode(122,95,54,55,95,97,105,110,116,105,110,103,0),true ]]);
       let tickF = String.fromCharCode(117,110,109,117,116,101,100,95,106,95,54,0);
         tickF = `${tickF.length}`;
      let promotionF = 5707437 <= tickF.length;
      do {
         tickF += `${3 | saveU.size}`;
         if (promotionF) {
            break;
         }
      } while (promotionF && (tickF.includes(`${saveU.size}`)));
      if (5 >= (saveU.size | tickF.length)) {
         saveU.set(`${tickF}`, tickF.length);
      }
      for (let a = 0; a < 3; a++) {
         tickF += `${saveU.size}`;
      }
         saveU.set(`${tickF}`, saveU.size - 3);
      while (5 >= (tickF.length % (Math.max(7, saveU.size))) && (saveU.size % 5) >= 2) {
         saveU = new Map([[`${saveU.size}`, saveU.size & tickF.length]]);
         break;
      }
      shirtA = `${2 | miniQ.length}`;
      shirtA += `${parseInt(`${roomj}`) | f_imageL.length}`;
   if (f_imageL.length == 1) {
       let singaporeD: Array<any> = [String.fromCharCode(115,111,99,105,97,108,95,99,95,54,53,0), String.fromCharCode(114,95,50,56,95,98,97,100,103,101,115,0), String.fromCharCode(98,117,116,116,111,110,115,95,116,95,53,56,0)];
       let trashr = String.fromCharCode(108,95,51,52,95,117,110,112,114,111,116,101,99,116,0);
      let episodeA = trashr.length >= 6825831;
      do {
          let zhuboE: Array<any> = [62, 288, 128];
          let stations0: Map<any, any> = new Map([[String.fromCharCode(115,117,98,118,105,101,119,115,95,51,95,50,56,0),true ], [String.fromCharCode(97,118,97,105,108,95,102,95,54,48,0),true ], [String.fromCharCode(101,105,110,116,114,95,48,95,52,53,0),false ]]);
         trashr += `${(trashr == String.fromCharCode(88,0) ? zhuboE.length : trashr.length)}`;
         zhuboE.push(3);
         stations0 = new Map([[`${stations0.size}`, stations0.size]]);
         if (episodeA) {
            break;
         }
      } while (episodeA && ((5 ^ singaporeD.length) <= 2 || (5 ^ trashr.length) <= 1));
      let yingn = 6739463 >= trashr.length;
      do {
          let bodan9: Array<any> = [267, 580];
          let agreementz: Map<any, any> = new Map([[String.fromCharCode(99,95,56,48,95,117,112,115,104,105,102,116,101,100,0),436], [String.fromCharCode(99,95,53,50,95,99,111,108,108,101,99,116,97,98,108,101,0),507], [String.fromCharCode(112,95,51,49,95,105,110,116,114,112,0),309]]);
          let animationM = String.fromCharCode(118,95,50,48,95,116,97,117,0);
         trashr = `${3 ^ agreementz.size}`;
         bodan9 = [animationM.length >> (Math.min(4, bodan9.length))];
         agreementz = new Map([[`${bodan9.length}`, animationM.length]]);
         if (yingn) {
            break;
         }
      } while (((singaporeD.length << (Math.min(Math.abs(3), 1))) > 4 || 4 > (trashr.length << (Math.min(Math.abs(3), 3)))) && yingn);
          let filled_ = String.fromCharCode(100,95,57,55,95,115,109,112,116,101,98,97,114,115,0);
         singaporeD = [3 - singaporeD.length];
         filled_ += `${filled_.length ^ filled_.length}`;
         singaporeD.push(trashr.length << (Math.min(1, singaporeD.length)));
          let rankP: Map<any, any> = new Map([[String.fromCharCode(115,116,97,114,116,114,101,101,95,120,95,48,0),849], [String.fromCharCode(112,111,119,116,97,98,108,101,95,111,95,56,52,0),7]]);
          let popupJ = true;
         singaporeD.push((String.fromCharCode(102,0) == trashr ? trashr.length : singaporeD.length));
         rankP.set(`${popupJ}`, (2 - (popupJ ? 5 : 5)));
         trashr = `${trashr.length / (Math.max(3, 3))}`;
      crownT -= parseInt(`${crownT}`) % 3;
   }
       let r_unlockI: Array<any> = [869, 988, 180];
      let clearD = 9270258 >= r_unlockI.length;
      do {
          let searchbari = String.fromCharCode(104,97,114,100,95,103,95,49,48,0);
          let videoN = 0.0;
          let dangere = String.fromCharCode(109,100,104,100,95,50,95,49,53,0);
          let humidityG = String.fromCharCode(113,95,49,48,95,116,98,108,104,100,114,0);
          let indicator2 = true;
         r_unlockI.push(3);
         searchbari += `${3 >> (Math.min(3, humidityG.length))}`;
         videoN /= Math.max(4, parseFloat(`${parseInt(`${videoN}`) & searchbari.length}`));
         dangere += `${humidityG.length}`;
         indicator2 = ((humidityG.length - (indicator2 ? humidityG.length : 61)) >= 61);
         if (clearD) {
            break;
         }
      } while (clearD && (5 > (r_unlockI.length * 3) && 5 > (3 * r_unlockI.length)));
      for (let h = 0; h < 2; h++) {
         r_unlockI = [1 ^ r_unlockI.length];
      }
         r_unlockI = [1 * r_unlockI.length];
      roomj -= parseFloat(`${playG.length}`);
       let typesf = String.fromCharCode(100,121,97,100,105,99,95,97,95,51,49,0);
       let saver = 1;
       let membershipr = String.fromCharCode(114,95,57,49,95,100,105,115,97,112,112,101,97,114,0);
       let default__nb = 3.0;
         saver <<= Math.min(1, Math.abs(saver / (Math.max(1, 9))));
         membershipr += `${(String.fromCharCode(102,0) == membershipr ? membershipr.length : parseInt(`${default__nb}`))}`;
      if (4.34 > (saver * default__nb)) {
         default__nb /= Math.max(1, parseFloat(`${membershipr.length}`));
      }
         membershipr += `${typesf.length >> (Math.min(Math.abs(1), 3))}`;
      if (typesf.startsWith(`${saver}`)) {
         typesf += `${typesf.length + saver}`;
      }
      if (2 <= saver) {
         membershipr = `${(typesf == String.fromCharCode(110,0) ? typesf.length : parseInt(`${default__nb}`))}`;
      }
      if (default__nb > 5.70) {
         default__nb -= parseFloat(`${parseInt(`${default__nb}`) / (Math.max(membershipr.length, 9))}`);
      }
      for (let b = 0; b < 2; b++) {
         typesf = `${membershipr.length}`;
      }
      crownT *= fillT << (Math.min(shirtA.length, 5));
      fillT *= miniQ.length;
   for (let r = 0; r < 2; r++) {
       let sourceX = 1;
       let injuryX = 1;
       let yellow4 = String.fromCharCode(115,117,98,100,101,109,117,120,101,114,95,105,95,56,52,0);
      if ((sourceX ^ injuryX) >= 3) {
          let completeW = true;
          let pageU = 5.0;
          let navigationy: Array<any> = [841, 200, 500];
         injuryX <<= Math.min(Math.abs(parseInt(`${pageU}`)), 5);
         completeW = navigationy.length >= 16;
         pageU /= Math.max((1 * (completeW ? 5 : 2)), 2);
         navigationy.push((1 % (Math.max(8, (completeW ? 5 : 1)))));
      }
      let uploadK = sourceX >= 6315044;
      do {
          let pingx = String.fromCharCode(114,95,54,57,95,99,111,110,100,0);
          let configureI: Array<any> = [902, 190, 518];
          let malaysiaF = 0;
          let configureL = String.fromCharCode(115,119,114,95,110,95,53,0);
          let videoh = String.fromCharCode(119,95,53,54,95,115,109,97,108,108,0);
         sourceX |= malaysiaF & pingx.length;
         pingx = `${(configureL == String.fromCharCode(77,0) ? configureL.length : configureI.length)}`;
         configureI = [(String.fromCharCode(74,0) == configureL ? videoh.length : configureL.length)];
         malaysiaF |= configureL.length % (Math.max(7, configureI.length));
         videoh += `${(String.fromCharCode(74,0) == configureL ? configureI.length : configureL.length)}`;
         if (uploadK) {
            break;
         }
      } while (uploadK && (2 > (sourceX % 4)));
          let auto_7dh: Array<any> = [121, 696, 651];
          let unselectedt = false;
         sourceX %= Math.max(injuryX, 2);
         auto_7dh = [auto_7dh.length + 2];
         unselectedt = auto_7dh.length == 46;
         sourceX += injuryX;
      while (injuryX <= 1) {
         injuryX *= 1;
         break;
      }
         injuryX ^= 1;
          let guidec: Map<any, any> = new Map([[String.fromCharCode(122,95,53,49,95,100,105,115,99,114,105,109,105,110,97,116,111,114,0),8], [String.fromCharCode(121,97,114,110,95,121,95,48,0),54]]);
         yellow4 = `${guidec.size}`;
       let privilegeg = String.fromCharCode(112,114,97,112,97,114,101,95,101,95,52,56,0);
      while (injuryX < sourceX) {
         sourceX &= (String.fromCharCode(66,0) == yellow4 ? yellow4.length : injuryX);
         break;
      }
      screena /= Math.max(2, 2 - injuryX);
   }

    togglePlayPause();
  };
  return (
    <View style={styles.middleControls}>
      {videoType !== 'live' && (
        <View style={styles.leftButton}>
          <RectButton
            rippleColor="transparent"
            disallowInterruption={true}
            onPress={() => onSkip(-10)}>
            <Rewind width={55} height={55} />
          </RectButton>
        </View>
      )}
      <RectButton
        disallowInterruption={true}
        rippleColor="transparent"
        onPress={() => onTogglePlayPause()}>
        {paused ? (
          <Play width={55} height={55} />
        ) : (
          <Pause width={55} height={55} />
        )}
      </RectButton>
      {videoType !== 'live' && (
        <View style={styles.rightButton}>
          <RectButton
            rippleColor="transparent"
            disallowInterruption={true}
            onPress={() => onSkip(10)}>
            <Fastforward width={55} height={55} />
          </RectButton>
        </View>
      )}
    </View>
  );
};

export default memo(MiddleControls);

const styles = StyleSheet.create({
  middleControls: {
    flexDirection: 'row',
  },
  sideButtons: {
    paddingLeft: 46,
    paddingRight: 46,
  },
  leftButton: {
    alignItems: 'flex-end',
    paddingRight: 23,
  },
  rightButton: {
    alignItems: 'flex-start',
    paddingLeft: 23,
  },
});
