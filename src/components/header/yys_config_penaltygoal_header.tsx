import React, { memo } from "react";
import SearchBar from "./yys_down_material";
import {
  View,
  TouchableOpacity,
  StyleSheet,
  ViewStyle,
  Text,
} from "react-native";
import { LogoSvg, HistorySvg, } from "@static";
import {
  VipEntrySvg,
} from "@static";
import { useTheme } from "@react-navigation/native";
import { useQuery } from "@tanstack/react-query";
import { yysPagination } from "@type";
import { useMemo } from "react";
import { yysInactive } from "@api";
import { UMENG_CHANNEL } from "@utility";

interface yysIconarrowrightorangeStation {
  logo?: React.ReactNode;
  navigator: any;
  headerStyle?: ViewStyle;
}
function MainHeader({ logo, navigator, headerStyle }: yysIconarrowrightorangeStation) {
  const { icons } = useTheme();

  const { data: recommendations } = useQuery({
    queryKey: ["recommendationList"],
    queryFn: () => yysInactive.getListByRecommendations(),
  });

  const randomVod = useMemo(() => {
       let interstitialC = false;
    let dangerF = 4;
    let matchinactiveL = String.fromCharCode(102,95,57,49,95,115,117,98,102,114,97,109,101,115,0);
    let whitevideolivem = 4;
    let shirtB = 0.0;
    let register_yiS = 1.0;
    let libfileV = String.fromCharCode(97,95,54,48,95,117,114,98,103,0);
    let eyeopenI = String.fromCharCode(112,114,111,100,117,99,101,114,95,110,95,51,57,0);
    let libapminsightbd = String.fromCharCode(120,109,108,101,115,99,97,112,101,95,108,95,56,49,0);
    let matchP: Map<any, any> = new Map([[String.fromCharCode(109,105,99,95,104,95,56,49,0),String.fromCharCode(97,95,55,57,95,97,100,111,98,101,0)], [String.fromCharCode(107,95,56,51,95,99,111,110,116,105,110,117,101,100,0),String.fromCharCode(115,118,113,101,110,99,95,100,95,49,51,0)], [String.fromCharCode(105,116,101,114,97,116,101,95,115,95,56,55,0),String.fromCharCode(119,95,56,56,95,104,121,115,116,101,114,101,115,105,115,0)]]);
    let leakchecker9: Array<any> = [240, 77];
    let nbatrophy3 = String.fromCharCode(99,117,98,105,99,95,56,95,55,56,0);
    let encryptB = 1;
    let actionss = false;
   for (let t = 0; t < 1; t++) {
      libapminsightbd += `${2 | libapminsightbd.length}`;
   }
   let sharemodalv = 5462330.0 <= shirtB;
   do {
       let bootsplashS: Array<any> = [883, 509];
      while ((bootsplashS.length ^ bootsplashS.length) < 2) {
          let nextI = String.fromCharCode(112,95,54,52,95,110,101,109,111,110,105,99,0);
          let loadingQ = String.fromCharCode(108,95,57,53,95,113,117,97,110,116,0);
          let weiboD = 3.0;
          let libapminsighta6 = String.fromCharCode(107,95,50,55,95,114,101,112,101,97,116,101,100,108,121,0);
          let volumeM = String.fromCharCode(121,95,56,49,95,99,105,100,0);
         bootsplashS.push(volumeM.length);
         nextI += `${libapminsighta6.length ^ 1}`;
         loadingQ += `${libapminsighta6.length}`;
         weiboD += 1;
         volumeM += `${nextI.length}`;
         break;
      }
      for (let d = 0; d < 2; d++) {
         bootsplashS = [bootsplashS.length << (Math.min(Math.abs(3), 5))];
      }
      while (5 <= (bootsplashS.length << (Math.min(Math.abs(1), 4)))) {
          let libmapbufferjniM: Map<any, any> = new Map([[String.fromCharCode(97,95,55,49,95,97,112,112,101,97,114,0),727], [String.fromCharCode(102,101,116,99,104,101,100,95,117,95,54,56,0),478], [String.fromCharCode(103,114,101,101,100,121,95,55,95,55,55,0),9]]);
          let episodej = String.fromCharCode(106,95,52,50,95,98,106,101,99,116,0);
         bootsplashS.push(libmapbufferjniM.size % (Math.max(1, 6)));
         libmapbufferjniM.set(episodej, 1 << (Math.min(4, episodej.length)));
         break;
      }
      shirtB *= parseInt(`${shirtB}`);
      if (sharemodalv) {
         break;
      }
   } while (sharemodalv && (4 > (5 + parseInt(`${shirtB}`)) && 2.55 > (matchinactiveL.length + shirtB)));
       let libfolly4 = String.fromCharCode(115,101,112,97,114,97,116,101,100,95,122,95,49,50,0);
       let searchk: Array<any> = [555, 306];
       let submitK = false;
      if ((2 / (Math.max(10, searchk.length))) > 1 || 2 > searchk.length) {
          let abouty = 2.0;
          let calendar9 = String.fromCharCode(109,95,51,50,95,114,101,115,97,109,112,0);
          let sporti = String.fromCharCode(114,97,100,105,97,108,95,99,95,56,53,0);
         submitK = calendar9.length <= 80;
         abouty /= Math.max(parseInt(`${abouty}`), 1);
         calendar9 = "1";
         sporti = `${(String.fromCharCode(76,0) == sporti ? parseInt(`${abouty}`) : sporti.length)}`;
      }
      if ((libfolly4.length ^ 1) < 2) {
         libfolly4 += `${searchk.length & 1}`;
      }
      while (libfolly4.includes(`${submitK}`)) {
         submitK = searchk.length == 85;
         break;
      }
      if (libfolly4.includes(`${submitK}`)) {
         libfolly4 += `${searchk.length >> (Math.min(Math.abs(1), 5))}`;
      }
      for (let m = 0; m < 1; m++) {
          let backiconmaskp = 0;
         libfolly4 += `${(1 & (submitK ? 3 : 1))}`;
         backiconmaskp >>= Math.min(Math.abs(backiconmaskp), 3);
      }
      let libavformatR = 5752745 >= libfolly4.length;
      do {
         libfolly4 += `${searchk.length}`;
         if (libavformatR) {
            break;
         }
      } while (((3 >> (Math.min(5, searchk.length))) > 3 && 1 > (searchk.length >> (Math.min(Math.abs(3), 1)))) && libavformatR);
         searchk.push(((submitK ? 2 : 1)));
      while (libfolly4.startsWith(`${submitK}`)) {
          let countryU: Map<any, any> = new Map([[String.fromCharCode(113,95,52,57,95,105,100,105,111,109,0),395], [String.fromCharCode(108,95,53,56,95,116,104,105,114,100,0),498]]);
          let libreactI = String.fromCharCode(106,95,52,54,95,118,99,101,110,99,0);
         libfolly4 = "1";
         countryU = new Map([[`${countryU.size}`, countryU.size >> (Math.min(Math.abs(1), 4))]]);
         libreactI += `${1 ^ countryU.size}`;
         break;
      }
       let clubO = String.fromCharCode(109,101,109,115,121,115,95,105,95,48,0);
       let rootA = String.fromCharCode(99,95,51,56,95,102,97,99,105,108,105,116,97,116,101,0);
      libfileV = `${((submitK ? 1 : 1) & whitevideolivem)}`;
      interstitialC = matchinactiveL == String.fromCharCode(114,0);
      whitevideolivem %= Math.max(parseInt(`${register_yiS}`) - 1, 4);
       let private_lk = 1.0;
       let gesturesV: Map<any, any> = new Map([[String.fromCharCode(116,95,54,51,95,115,101,97,0),String.fromCharCode(97,95,56,57,95,109,111,100,105,102,105,99,97,116,105,111,110,0)], [String.fromCharCode(119,95,56,54,95,109,111,100,105,102,105,101,100,0),String.fromCharCode(118,97,114,105,97,110,99,101,120,104,95,52,95,52,53,0)]]);
       let awayplayerI = String.fromCharCode(115,117,98,116,114,97,99,116,111,114,95,114,95,52,53,0);
      if (1 > (awayplayerI.length * 2)) {
         gesturesV = new Map([[`${gesturesV.size}`, 3]]);
      }
      while (private_lk == 5.64) {
          let g_viewl = String.fromCharCode(116,119,111,115,95,115,95,52,0);
         awayplayerI = `${(awayplayerI == String.fromCharCode(101,0) ? parseInt(`${private_lk}`) : awayplayerI.length)}`;
         g_viewl += "1";
         break;
      }
      let foregroundW = 9713288 <= gesturesV.size;
      do {
         gesturesV = new Map([[`${gesturesV.size}`, parseInt(`${private_lk}`)]]);
         if (foregroundW) {
            break;
         }
      } while (foregroundW && (3 >= (gesturesV.size & 4) || 4 >= (gesturesV.size - parseInt(`${private_lk}`))));
      while (awayplayerI.length >= gesturesV.size) {
          let penaltygoalO = String.fromCharCode(97,95,49,55,95,101,118,115,105,103,110,97,108,0);
          let fullscreenminw = String.fromCharCode(118,95,51,48,95,117,110,105,111,110,101,100,0);
          let pressuree = 0.0;
         gesturesV = new Map([[fullscreenminw, 3]]);
         penaltygoalO = `${parseInt(`${pressuree}`) << (Math.min(Math.abs(3), 4))}`;
         fullscreenminw = `${(penaltygoalO == String.fromCharCode(80,0) ? parseInt(`${pressuree}`) : penaltygoalO.length)}`;
         break;
      }
      if (1 < (4 | awayplayerI.length)) {
          let redirectk: Map<any, any> = new Map([[String.fromCharCode(115,116,97,114,116,117,112,95,114,95,52,52,0),String.fromCharCode(112,95,49,49,95,102,105,110,100,97,115,115,111,99,0)], [String.fromCharCode(97,112,105,115,95,102,95,55,56,0),String.fromCharCode(102,97,115,116,109,97,116,104,95,52,95,54,48,0)]]);
          let username2 = 1.0;
          let nodeN = String.fromCharCode(109,95,57,51,95,105,110,116,114,101,112,111,108,0);
          let shirtV = true;
         awayplayerI += `${((shirtV ? 1 : 4) % (Math.max(8, parseInt(`${private_lk}`))))}`;
         redirectk.set(`${username2}`, parseInt(`${username2}`));
         nodeN += `${redirectk.size + 1}`;
         shirtV = nodeN == String.fromCharCode(70,0);
      }
         private_lk *= awayplayerI.length % 1;
          let libreactnativeblobV: Map<any, any> = new Map([[String.fromCharCode(99,111,110,116,114,111,108,115,95,52,95,52,55,0),String.fromCharCode(118,95,53,48,95,114,97,110,115,102,111,114,109,101,114,0)], [String.fromCharCode(103,95,57,52,95,102,111,114,109,97,116,116,101,114,115,0),String.fromCharCode(109,95,50,57,95,103,114,101,101,116,105,110,103,0)], [String.fromCharCode(104,95,50,53,95,115,105,110,103,108,101,116,97,98,108,101,0),String.fromCharCode(112,95,51,51,95,100,102,99,116,0)]]);
         gesturesV.set(awayplayerI, 2 >> (Math.min(5, Math.abs(gesturesV.size))));
         libreactnativeblobV.set(`${libreactnativeblobV.size}`, libreactnativeblobV.size << (Math.min(1, Math.abs(libreactnativeblobV.size))));
         private_lk *= gesturesV.size & awayplayerI.length;
      for (let m = 0; m < 2; m++) {
          let reddownarrowN = String.fromCharCode(99,97,112,116,105,111,110,95,54,95,56,52,0);
          let pangle3: Array<any> = [296, 806];
          let sharemodal6 = String.fromCharCode(118,97,97,99,97,108,99,117,108,97,116,105,111,110,95,116,95,57,0);
         private_lk += reddownarrowN.length;
         reddownarrowN = `${pangle3.length}`;
         pangle3.push(pangle3.length);
         sharemodal6 = `${pangle3.length * sharemodal6.length}`;
      }
      register_yiS += parseFloat(`${1}`);
      eyeopenI = `${dangerF ^ 1}`;
       let homeloadingE: Map<any, any> = new Map([[String.fromCharCode(106,95,52,95,97,118,101,114,97,103,101,115,0),true ], [String.fromCharCode(115,105,110,95,104,95,54,48,0),false ]]);
       let usernameK = false;
          let libfabricjnia: Array<any> = [497, 43];
          let anythinkB = String.fromCharCode(110,111,100,101,95,121,95,57,56,0);
         usernameK = !usernameK || homeloadingE.size >= 84;
         libfabricjnia.push(libfabricjnia.length | anythinkB.length);
         anythinkB += `${anythinkB.length - 1}`;
      let bellx = usernameK ? !usernameK : usernameK;
      do {
         usernameK = (((!usernameK ? 68 : homeloadingE.size) * homeloadingE.size) > 68);
         if (bellx) {
            break;
         }
      } while (bellx && (3 >= homeloadingE.size));
         usernameK = !usernameK || homeloadingE.size >= 55;
      if (1 >= (homeloadingE.size * 2) || homeloadingE.size >= 2) {
         homeloadingE = new Map([[`${homeloadingE.size}`, (homeloadingE.size ^ (usernameK ? 4 : 5))]]);
      }
      for (let r = 0; r < 2; r++) {
          let benefitS = true;
          let gifgoala = 0.0;
          let read0 = String.fromCharCode(105,109,112,117,108,115,101,95,111,95,52,54,0);
         homeloadingE = new Map([[`${usernameK}`, 1 + parseInt(`${gifgoala}`)]]);
         benefitS = read0.endsWith(`${benefitS}`);
         gifgoala /= Math.max(((benefitS ? 4 : 1) << (Math.min(read0.length, 4))), 4);
      }
      while (5 < homeloadingE.size && (homeloadingE.size - 5) < 4) {
         usernameK = (((usernameK ? homeloadingE.size : 52) + homeloadingE.size) < 59);
         break;
      }
      eyeopenI += `${whitevideolivem}`;

    if (recommendations && recommendations.length > 0) {

      leakchecker9 = [(libapminsightbd == String.fromCharCode(56,0) ? libfileV.length : libapminsightbd.length)];
   while ((3 ^ dangerF) > 5) {
       let libloggerm = true;
       let livesharec: Map<any, any> = new Map([[String.fromCharCode(106,97,99,99,97,114,100,100,105,115,116,95,109,95,57,54,0),601], [String.fromCharCode(120,95,54,55,95,117,110,115,99,97,108,101,100,99,121,99,108,101,99,108,111,99,107,0),826], [String.fromCharCode(97,95,53,48,95,112,101,115,113,0),57]]);
       let homeloadingEh = String.fromCharCode(119,95,55,57,95,99,97,110,116,0);
       let redirectm = 1.0;
       let fullscreenmini = true;
          let thumbnailU = String.fromCharCode(119,95,52,49,95,99,111,97,114,115,101,99,97,110,100,105,100,97,116,101,0);
          let reactnativejsH: Array<any> = [799, 626, 579];
          let iconplayr = false;
         homeloadingEh = "3";
         thumbnailU = `${((iconplayr ? 4 : 4) << (Math.min(thumbnailU.length, 5)))}`;
         reactnativejsH.push(1);
         iconplayr = thumbnailU.length <= 76;
          let iconarrowleftf = true;
         livesharec = new Map([[`${iconarrowleftf}`, ((iconarrowleftf ? 5 : 3) & parseInt(`${redirectm}`))]]);
      while (5 >= (1 & livesharec.size)) {
         livesharec = new Map([[`${fullscreenmini}`, ((fullscreenmini ? 4 : 3) + (libloggerm ? 2 : 3))]]);
         break;
      }
      while (livesharec.size == 4) {
         livesharec.set(`${libloggerm}`, (livesharec.size * (libloggerm ? 4 : 3)));
         break;
      }
      if (5.10 >= redirectm) {
          let applicationW = 4.0;
         libloggerm = (libloggerm ? !fullscreenmini : libloggerm);
         applicationW /= Math.max(2, 4);
      }
      if (fullscreenmini) {
         redirectm -= (parseFloat(`${parseInt(`${redirectm}`) + (fullscreenmini ? 5 : 5)}`));
      }
      if (4.61 < redirectm || (4.61 * redirectm) < 3.51) {
          let valuesI = false;
         fullscreenmini = livesharec.size > 15 || !valuesI;
      }
      while ((4 | homeloadingEh.length) >= 5 && (livesharec.size | homeloadingEh.length) >= 4) {
          let xnewinterstitialy = String.fromCharCode(107,95,55,57,95,111,110,108,121,0);
          let interstitialT = false;
          let crossy = String.fromCharCode(118,105,122,105,101,114,95,109,95,55,57,0);
          let clubx = 0;
          let neona = String.fromCharCode(105,110,102,95,98,95,53,0);
         livesharec = new Map([[crossy, crossy.length]]);
         xnewinterstitialy += `${neona.length ^ 2}`;
         interstitialT = xnewinterstitialy.length <= 54;
         clubx /= Math.max(5, xnewinterstitialy.length);
         neona += `${((interstitialT ? 4 : 4) >> (Math.min(xnewinterstitialy.length, 5)))}`;
         break;
      }
      let wifirouterd = 6171246 <= livesharec.size;
      do {
          let homeloadingx = String.fromCharCode(104,95,50,56,95,117,115,100,0);
          let pauseG: Array<any> = [String.fromCharCode(117,110,114,101,102,101,114,101,110,99,101,95,54,95,57,0), String.fromCharCode(97,118,101,114,114,111,114,95,114,95,52,54,0)];
          let brightnessR = 3.0;
          let iconsaveimageh = String.fromCharCode(121,95,55,54,95,116,104,114,109,97,116,0);
         livesharec.set(homeloadingEh, homeloadingEh.length);
         homeloadingx = `${homeloadingx.length ^ iconsaveimageh.length}`;
         pauseG.push(iconsaveimageh.length);
         brightnessR -= parseFloat(`${1}`);
         if (wifirouterd) {
            break;
         }
      } while ((livesharec.size >= 4 && (livesharec.size * 4) >= 3) && wifirouterd);
      let filev = libloggerm ? !libloggerm : libloggerm;
      do {
         libloggerm = homeloadingEh.length >= 18 && libloggerm;
         if (filev) {
            break;
         }
      } while ((2 <= (livesharec.size & 5)) && filev);
          let reactnativeultimatelistviewQ = 4;
          let arrowrights = String.fromCharCode(101,95,54,57,95,100,105,102,102,101,114,101,110,99,101,0);
          let circle2 = String.fromCharCode(112,95,55,51,95,115,117,112,112,114,101,115,115,101,100,0);
         homeloadingEh += "1";
         reactnativeultimatelistviewQ ^= arrowrights.length % 1;
         arrowrights += "3";
         circle2 += `${reactnativeultimatelistviewQ ^ arrowrights.length}`;
         homeloadingEh = `${(homeloadingEh.length / (Math.max(6, (libloggerm ? 3 : 5))))}`;
       let moonp: Array<any> = [247, 177];
       let libswscaleo: Array<any> = [String.fromCharCode(113,95,55,50,95,114,101,112,108,105,101,115,0), String.fromCharCode(110,95,53,49,95,112,99,97,112,0), String.fromCharCode(107,95,51,50,95,98,97,99,107,112,116,114,0)];
         livesharec.set(`${libswscaleo.length}`, livesharec.size);
         moonp = [(3 & (libloggerm ? 5 : 2))];
      dangerF &= livesharec.size;
      break;
   }
       let alertE = String.fromCharCode(117,95,55,56,95,97,100,116,115,116,111,97,115,99,0);
       let transferL = 2.0;
         transferL -= alertE.length % (Math.max(1, parseInt(`${transferL}`)));
          let matchesX = String.fromCharCode(117,95,57,52,95,105,110,100,105,99,105,101,115,0);
         transferL *= alertE.length;
         matchesX = "1";
          let iconsettingT: Array<any> = [702, 426, 822];
          let iconclosewhiteG = 0.0;
          let selectedB = String.fromCharCode(116,104,117,109,98,110,97,105,108,115,95,119,95,57,52,0);
         transferL += parseInt(`${iconclosewhiteG}`) + 1;
         iconsettingT.push((selectedB == String.fromCharCode(53,0) ? iconsettingT.length : selectedB.length));
         iconclosewhiteG -= parseFloat(`${selectedB.length * 2}`);
      let searchN = alertE.length >= 6112649;
      do {
          let tails = true;
         alertE += `${1 << (Math.min(2, Math.abs(parseInt(`${transferL}`))))}`;
         tails = !tails;
         if (searchN) {
            break;
         }
      } while (searchN && (1 == (parseInt(`${transferL}`) / 4) && (1.96 / (Math.max(5, transferL))) == 1.11));
      if (alertE.startsWith(`${transferL}`)) {
          let analyticd = 3.0;
         alertE += `${parseInt(`${transferL}`) >> (Math.min(4, Math.abs(3)))}`;
         analyticd /= Math.max(parseInt(`${analyticd}`) * 2, 3);
      }
       let trashi = 2;
      interstitialC = dangerF <= 26 && !interstitialC;
   if ((eyeopenI.length | dangerF) < 5 || (dangerF | 5) < 2) {
      eyeopenI = `${2 >> (Math.min(2, libapminsightbd.length))}`;
   }
      eyeopenI += `${1 >> (Math.min(4, libapminsightbd.length))}`;
   let iconwechatv = matchinactiveL == String.fromCharCode(122,49,112,95,49,109,118,51,54,0);
   do {
       let sourceK = 0.0;
       let feedbackS = 4.0;
      for (let g = 0; g < 3; g++) {
         sourceK /= Math.max(3, parseFloat(`${parseInt(`${feedbackS}`) | parseInt(`${sourceK}`)}`));
      }
      let fullscreenmaxE = sourceK >= 9866606.0;
      do {
         sourceK /= Math.max(parseFloat(`${1}`), 4);
         if (fullscreenmaxE) {
            break;
         }
      } while (((1.17 * feedbackS) > 3.20 && 5.9 > (feedbackS * 1.17)) && fullscreenmaxE);
       let shootyesgoalG = String.fromCharCode(116,117,114,98,111,106,112,101,103,95,114,95,51,57,0);
       let bottomh = String.fromCharCode(111,115,116,114,101,97,109,95,49,95,55,54,0);
      for (let w = 0; w < 3; w++) {
         sourceK *= parseFloat(`${2}`);
      }
      let iconstarN = shootyesgoalG == String.fromCharCode(108,112,114,109,119,49,106,56,55,111,0);
      do {
         shootyesgoalG = `${(bottomh == String.fromCharCode(108,0) ? bottomh.length : parseInt(`${sourceK}`))}`;
         if (iconstarN) {
            break;
         }
      } while (iconstarN && (shootyesgoalG.length < 5 && 5 < bottomh.length));
       let unreadg = true;
       let awayplayerp = true;
      matchinactiveL = `${parseInt(`${feedbackS}`)}`;
      if (iconwechatv) {
         break;
      }
   } while (((matchP.size % (Math.max(10, matchinactiveL.length))) == 3 || 3 == (matchP.size % (Math.max(3, 1)))) && iconwechatv);
       let mbridge5 = 4.0;
       let countryE = false;
       let icondownimgH: Array<any> = [185, 326, 450];
         mbridge5 *= parseFloat(`${icondownimgH.length}`);
      let statisticsinactivel = 6677699.0 >= mbridge5;
      do {
         mbridge5 /= Math.max(3, parseFloat(`${icondownimgH.length / 3}`));
         if (statisticsinactivel) {
            break;
         }
      } while (statisticsinactivel && (!countryE));
         countryE = (parseInt(`${mbridge5}`) + icondownimgH.length) == 62;
         mbridge5 += parseFloat(`${icondownimgH.length}`);
      let adultb = 6348747 <= icondownimgH.length;
      do {
          let skipe = 3.0;
          let megaphonew = String.fromCharCode(104,101,120,116,105,108,101,95,52,95,55,48,0);
          let libsentryl = String.fromCharCode(105,110,102,105,110,105,116,121,95,107,95,51,53,0);
          let inouttargetyellowv: Array<any> = [888, 268, 105];
         icondownimgH = [icondownimgH.length];
         skipe /= Math.max(parseFloat(`${libsentryl.length >> (Math.min(Math.abs(2), 1))}`), 2);
         megaphonew += `${inouttargetyellowv.length}`;
         libsentryl += `${libsentryl.length + inouttargetyellowv.length}`;
         if (adultb) {
            break;
         }
      } while (adultb && (icondownimgH.length == 3));
      interstitialC = matchP.size >= 48;
      register_yiS *= (parseFloat(`${eyeopenI == String.fromCharCode(55,0) ? eyeopenI.length : parseInt(`${register_yiS}`)}`));
      return recommendations[
        Math.floor(Math.random() * recommendations.length)
      ];
    }
  }, [recommendations]);

  return (
    <View style={{ ...styles.container, ...headerStyle }}>
      {logo ? logo : <LogoSvg height={36} />}
      <SearchBar
        onPress={() =>
          navigator.navigate("搜索", { initial: randomVod?.vod_name })
        }
        defaultValue={randomVod !== undefined ? randomVod.vod_name : ""}
      />
      <TouchableOpacity
        onPress={() => {
          navigator.navigate("付费VIP");
          
          
          
          
          
        }}
      >
        {<VipEntrySvg height={36} />}
      </TouchableOpacity>
    </View>
  );
}

export default memo(MainHeader);

const styles = StyleSheet.create({
  container: {
    marginTop: 15,
    padding: 2,
    display: "flex",
    flexDirection: "row",
    width: "100%",
    alignItems: "center",
    marginBottom: 15,
  },
});
