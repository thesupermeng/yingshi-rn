import {SearchBar} from '@rneui/themed';
import {useTheme} from '@react-navigation/native';
import {TextInput, StyleSheet, View, TouchableOpacity} from 'react-native';
import SearchIcon from '@static/images/basketballhometeamAbidetect.svg';

interface wawaAwayShow {
  defaultValue?: string;
  onPress?: (onPress: any) => any;
}

export default function HomeSearchBar({
  defaultValue = '子弹列车',
  onPress = () => {
       let dependencyK = 2;
    let settingsI: Array<any> = [188, 16];
    let starm: Array<any> = [862, 337];
    let heji_ = 3.0;
    let modulec = String.fromCharCode(100,97,114,119,105,110,0);
    let b_playerG = 4.0;
    let hookso: Array<any> = [840, 881, 66];
    let redcirclebgb = 0.0;
    let pointP = String.fromCharCode(109,105,110,117,116,101,0);
    let libreactn = String.fromCharCode(102,119,104,116,120,0);
    let eyeopenF = false;
    let modaln = 4;
    let championa = false;
    let libfbjnih = String.fromCharCode(115,112,101,99,116,114,97,0);
    let questi: Map<any, any> = new Map([[String.fromCharCode(104,101,97,100,108,105,110,101,0),101], [String.fromCharCode(108,111,110,0),877]]);
   if (hookso.includes(redcirclebgb)) {
       let predictionwinL = 2.0;
       let iconnointernetE = String.fromCharCode(113,115,116,101,112,0);
         iconnointernetE += `${iconnointernetE.length ^ parseInt(`${predictionwinL}`)}`;
       let entry8 = 5.0;
       let holderd = 1;
       let libmapbufferjnia = 4;
         predictionwinL *= parseInt(`${entry8}`) % (Math.max(parseInt(`${predictionwinL}`), 6));
      if ((libmapbufferjnia % (Math.max(iconnointernetE.length, 5))) >= 3 && 3 >= (iconnointernetE.length % (Math.max(10, libmapbufferjnia)))) {
          let whitesmallticku = 1;
          let backW = 2;
         iconnointernetE += "2";
         whitesmallticku %= Math.max(whitesmallticku ^ 1, 5);
         backW /= Math.max(1, whitesmallticku & backW);
      }
      for (let m = 0; m < 3; m++) {
         iconnointernetE += `${iconnointernetE.length & 3}`;
      }
      hookso.push((pointP == String.fromCharCode(73,0) ? pointP.length : starm.length));
   }
   if (4 <= starm.length) {
      starm = [parseInt(`${redcirclebgb}`) >> (Math.min(pointP.length, 3))];
   }
       let calendarx = String.fromCharCode(115,104,111,114,116,99,117,116,115,0);
       let applicatione = 1.0;
          let base3 = false;
          let savev = String.fromCharCode(113,117,101,114,121,0);
          let readP = String.fromCharCode(115,98,97,100,0);
         applicatione += readP.length;
         base3 = savev.startsWith(`${base3}`);
         savev = "1";
         readP = "3";
      while (calendarx.length > parseInt(`${applicatione}`)) {
          let foundm: Array<any> = [854, 394, 800];
          let iconrefreshN = String.fromCharCode(110,97,109,101,115,112,97,99,101,115,0);
         applicatione += iconrefreshN.length & 3;
         foundm.push(foundm.length);
         iconrefreshN += `${foundm.length - 1}`;
         break;
      }
          let iconclosewhitebgy = String.fromCharCode(114,101,99,111,110,105,110,116,114,97,0);
          let libimagepipelineQ = 4.0;
          let agreementu = String.fromCharCode(115,105,112,114,107,100,97,116,97,0);
         calendarx += `${iconclosewhitebgy.length % (Math.max(2, 10))}`;
         iconclosewhitebgy += `${parseInt(`${libimagepipelineQ}`) << (Math.min(Math.abs(1), 5))}`;
         libimagepipelineQ /= Math.max(agreementu.length, 2);
         agreementu = `${agreementu.length}`;
         applicatione -= 3 + calendarx.length;
      let pingA = 7138020 >= calendarx.length;
      do {
         calendarx += `${calendarx.length & 3}`;
         if (pingA) {
            break;
         }
      } while ((2 < (parseInt(`${applicatione}`) / (Math.max(calendarx.length, 6)))) && pingA);
      if (1 >= (calendarx.length * parseInt(`${applicatione}`))) {
          let awayteamfielda = 4.0;
         calendarx = `${calendarx.length | 2}`;
         awayteamfielda *= parseFloat(`${parseInt(`${awayteamfielda}`)}`);
      }
      dependencyK >>= Math.min(Math.abs(parseInt(`${redcirclebgb}`)), 5);
   for (let d = 0; d < 1; d++) {
       let temperatureU = false;
       let countdownb = String.fromCharCode(102,114,97,109,101,114,0);
      if (countdownb.startsWith(`${temperatureU}`)) {
          let backgroundX = String.fromCharCode(116,121,112,101,0);
          let basketballC = 2.0;
          let sansU: Array<any> = [String.fromCharCode(115,97,109,101,0), String.fromCharCode(112,97,99,107,101,116,0)];
          let filed4 = String.fromCharCode(111,115,109,111,115,0);
         temperatureU = String.fromCharCode(107,0) == filed4;
         backgroundX += `${parseInt(`${basketballC}`) >> (Math.min(backgroundX.length, 5))}`;
         basketballC -= parseFloat(`${backgroundX.length * 2}`);
         sansU = [parseInt(`${basketballC}`)];
         filed4 = `${1 * sansU.length}`;
      }
      let components6 = temperatureU ? !temperatureU : temperatureU;
      do {
         temperatureU = !countdownb.includes(`${temperatureU}`);
         if (components6) {
            break;
         }
      } while (components6 && (countdownb.endsWith(`${temperatureU}`)));
         temperatureU = countdownb.length == 13;
          let resultg = 3.0;
          let bridgen = 1.0;
          let sheet1: Array<any> = [String.fromCharCode(97,99,107,0), String.fromCharCode(115,101,101,100,0)];
         temperatureU = 84 <= sheet1.length || String.fromCharCode(52,0) == countdownb;
         resultg += parseInt(`${bridgen}`) ^ parseInt(`${resultg}`);
         bridgen *= parseInt(`${resultg}`) & 1;
         sheet1.push(parseInt(`${bridgen}`) ^ 1);
          let mapbufferM: Array<any> = [String.fromCharCode(114,116,97,100,100,114,115,0), String.fromCharCode(109,106,112,101,103,98,0)];
          let subs9 = 3.0;
         countdownb += `${parseInt(`${subs9}`) * 3}`;
         mapbufferM = [3];
         subs9 /= Math.max(3, parseFloat(`${mapbufferM.length << (Math.min(5, mapbufferM.length))}`));
       let gesture7 = String.fromCharCode(99,97,108,99,117,97,108,116,101,0);
      b_playerG -= parseFloat(`${3 - settingsI.length}`);
   }
   if (1 < (settingsI.length << (Math.min(pointP.length, 1))) && 1 < (pointP.length << (Math.min(Math.abs(1), 1)))) {
       let libruntimeexecutorY = 4.0;
       let blackD = false;
       let bootsplashQ = 3.0;
       let lessJ = 3.0;
      while (4.53 <= (5.97 - bootsplashQ) && (lessJ - 5.97) <= 2.95) {
          let expired1: Map<any, any> = new Map([[String.fromCharCode(119,104,101,114,101,0),705], [String.fromCharCode(107,105,99,107,101,114,0),592]]);
          let arrowdownU: Map<any, any> = new Map([[String.fromCharCode(99,104,97,110,103,101,115,0),false ], [String.fromCharCode(115,116,114,101,110,103,116,104,115,0),false ]]);
          let sellmathbackgroundQ = String.fromCharCode(104,119,99,111,110,116,101,120,116,0);
          let fullscreenminR = true;
         bootsplashQ -= 3;
         expired1 = new Map([[`${expired1.size}`, expired1.size << (Math.min(sellmathbackgroundQ.length, 4))]]);
         arrowdownU = new Map([[`${arrowdownU.size}`, 2]]);
         sellmathbackgroundQ += `${arrowdownU.size}`;
         fullscreenminR = sellmathbackgroundQ == String.fromCharCode(82,0);
         break;
      }
      if (blackD) {
         blackD = !blackD;
      }
      if (libruntimeexecutorY > 4.61) {
          let assets7 = false;
          let register_adh = 3.0;
          let dangerZ: Map<any, any> = new Map([[String.fromCharCode(111,112,116,0),true ], [String.fromCharCode(106,97,105,108,98,114,101,97,107,0),true ]]);
         libruntimeexecutorY *= parseInt(`${register_adh}`) & 2;
         assets7 = dangerZ.size <= 57;
         register_adh /= Math.max(4, ((assets7 ? 4 : 3) / (Math.max(dangerZ.size, 2))));
      }
      if (1.18 == libruntimeexecutorY) {
         blackD = bootsplashQ < 47.46 && blackD;
      }
      if ((bootsplashQ - lessJ) > 3.80) {
          let trophy9 = String.fromCharCode(105,110,99,114,101,97,115,101,0);
          let sportP = true;
          let whistleb = String.fromCharCode(112,97,115,116,101,98,111,97,114,100,0);
          let fullscreenmaxJ = 1.0;
         bootsplashQ += trophy9.length + 3;
         trophy9 = "1";
         sportP = whistleb.length > fullscreenmaxJ;
         whistleb += "2";
         fullscreenmaxJ -= whistleb.length / (Math.max(3, 3));
      }
       let predictionwinG: Map<any, any> = new Map([[String.fromCharCode(105,114,97,110,100,0),863], [String.fromCharCode(97,108,112,97,0),665], [String.fromCharCode(115,111,98,105,110,100,0),702]]);
       let privilegeW: Map<any, any> = new Map([[String.fromCharCode(107,105,110,100,0),401], [String.fromCharCode(99,111,109,112,97,114,101,112,111,119,0),868]]);
         libruntimeexecutorY += parseInt(`${libruntimeexecutorY}`) << (Math.min(4, Math.abs(1)));
          let codegent = 2.0;
          let userp = 4;
         blackD = (privilegeW.size / (Math.max(6, parseInt(`${bootsplashQ}`)))) < 84;
         codegent -= 1 ^ parseInt(`${codegent}`);
         userp -= 1 * parseInt(`${codegent}`);
         predictionwinG = new Map([[`${libruntimeexecutorY}`, parseInt(`${libruntimeexecutorY}`) / (Math.max(3, parseInt(`${bootsplashQ}`)))]]);
      let incidentz = predictionwinG.size <= 7717567;
      do {
         predictionwinG = new Map([[`${bootsplashQ}`, (parseInt(`${bootsplashQ}`) | (blackD ? 1 : 2))]]);
         if (incidentz) {
            break;
         }
      } while (incidentz && (predictionwinG.get(`${libruntimeexecutorY}`) != null));
      let theme5 = lessJ <= 6973351.0;
      do {
         lessJ += 3;
         if (theme5) {
            break;
         }
      } while ((lessJ < predictionwinG.size) && theme5);
          let controlsr = String.fromCharCode(109,111,110,116,0);
         lessJ -= parseInt(`${lessJ}`) - 1;
         controlsr += `${controlsr.length + 3}`;
      settingsI = [hookso.length];
   }
   for (let q = 0; q < 3; q++) {
      libreactn += `${1 / (Math.max(dependencyK, 8))}`;
   }
      modulec += "1";
       let catalogJ = String.fromCharCode(109,116,105,109,101,0);
       let liveendmodallogot: Array<any> = [String.fromCharCode(97,114,109,111,110,121,0), String.fromCharCode(112,105,120,99,116,120,0)];
       let feedbackX = String.fromCharCode(115,116,114,105,110,103,101,110,99,111,100,101,0);
      while ((1 - feedbackX.length) >= 5 || (liveendmodallogot.length - 1) >= 5) {
         liveendmodallogot = [2 >> (Math.min(3, feedbackX.length))];
         break;
      }
      for (let j = 0; j < 1; j++) {
         feedbackX += `${liveendmodallogot.length - feedbackX.length}`;
      }
      if (4 < (4 - catalogJ.length) && 2 < (catalogJ.length - 4)) {
          let shareE = true;
         catalogJ += "1";
         shareE = (!shareE ? !shareE : shareE);
      }
      for (let g = 0; g < 1; g++) {
          let ewardedY: Array<any> = [948, 510, 692];
          let umengm = String.fromCharCode(97,112,110,103,0);
          let album4 = String.fromCharCode(98,97,99,107,114,111,117,110,100,0);
          let footballfieldf = false;
          let sentryO: Map<any, any> = new Map([[String.fromCharCode(109,111,109,101,110,116,0),980], [String.fromCharCode(115,99,104,101,109,101,0),857]]);
         liveendmodallogot = [2];
         ewardedY.push(sentryO.size / (Math.max(album4.length, 5)));
         umengm += "1";
         album4 += `${ewardedY.length}`;
         footballfieldf = (album4.length / (Math.max(umengm.length, 8))) > 72;
         sentryO = new Map([[`${sentryO.size}`, ((footballfieldf ? 3 : 2) ^ sentryO.size)]]);
      }
      while ((liveendmodallogot.length & feedbackX.length) <= 5 && 3 <= (5 & feedbackX.length)) {
         liveendmodallogot = [feedbackX.length ^ catalogJ.length];
         break;
      }
         feedbackX += "3";
         liveendmodallogot = [catalogJ.length ^ 1];
         catalogJ = `${feedbackX.length / 1}`;
          let predictionarrowL: Array<any> = [631, 46, 147];
         feedbackX += "1";
         predictionarrowL = [predictionarrowL.length];
      heji_ -= parseInt(`${b_playerG}`);
   for (let f = 0; f < 1; f++) {
      eyeopenF = 54.1 <= heji_;
   }
      eyeopenF = hookso.length <= 2 || redcirclebgb <= 95.94;
       let whistle2 = 5.0;
       let expiredQ: Map<any, any> = new Map([[String.fromCharCode(104,100,114,115,0),false ], [String.fromCharCode(115,101,116,117,112,100,0),false ], [String.fromCharCode(101,108,101,118,97,116,101,100,0),true ]]);
       let middlesoundi = 5.0;
       let libjsijniprofilerq = 3;
      for (let b = 0; b < 3; b++) {
         whistle2 -= 3 + libjsijniprofilerq;
      }
      if (Array.from(expiredQ.keys()).includes(`${whistle2}`)) {
          let codegen5 = false;
         whistle2 *= 3;
         codegen5 = (!codegen5 ? codegen5 : !codegen5);
      }
       let philippinesz = String.fromCharCode(114,101,110,100,101,114,101,114,0);
       let applicationZ = String.fromCharCode(115,112,114,101,97,100,0);
      let questN = 5161703 >= expiredQ.size;
      do {
         expiredQ = new Map([[`${expiredQ.size}`, 1]]);
         if (questN) {
            break;
         }
      } while (questN && (4 < (2 - expiredQ.size) || (libjsijniprofilerq - 2) < 1));
         libjsijniprofilerq <<= Math.min(5, Math.abs(applicationZ.length / 2));
       let mbridges = String.fromCharCode(102,114,97,99,116,105,111,110,97,108,0);
      let kickL = 8842099 >= libjsijniprofilerq;
      do {
          let middlem = 2;
          let showmore7: Map<any, any> = new Map([[String.fromCharCode(101,99,114,101,99,111,118,101,114,0),48], [String.fromCharCode(109,118,99,111,110,116,101,120,116,115,0),943], [String.fromCharCode(105,109,112,108,101,109,101,110,116,115,0),889]]);
         libjsijniprofilerq ^= 1 << (Math.min(3, Math.abs(middlem)));
         middlem %= Math.max(showmore7.size, 2);
         showmore7 = new Map([[`${showmore7.size}`, showmore7.size]]);
         if (kickL) {
            break;
         }
      } while (kickL && (2 == (3 - libjsijniprofilerq)));
      for (let p = 0; p < 2; p++) {
         whistle2 /= Math.max((applicationZ == String.fromCharCode(90,0) ? applicationZ.length : expiredQ.size), 1);
      }
      redcirclebgb *= 1;
      settingsI.push((String.fromCharCode(110,0) == modulec ? modulec.length : pointP.length));
      b_playerG += (parseFloat(`${String.fromCharCode(56,0) == libreactn ? libreactn.length : parseInt(`${heji_}`)}`));
       let mbjscommonI = String.fromCharCode(104,105,103,104,108,105,103,104,116,105,110,103,0);
       let penaltymatchiconj = 0;
          let tooltipsK = true;
          let stylex = String.fromCharCode(114,101,99,111,103,110,105,122,101,100,0);
         mbjscommonI += `${mbjscommonI.length * 2}`;
         tooltipsK = stylex.length <= 82;
         stylex = `${(stylex == String.fromCharCode(50,0) ? stylex.length : (tooltipsK ? 5 : 1))}`;
         mbjscommonI = `${penaltymatchiconj / (Math.max(4, mbjscommonI.length))}`;
         penaltymatchiconj |= (String.fromCharCode(102,0) == mbjscommonI ? mbjscommonI.length : penaltymatchiconj);
       let goallogo_ = 4;
      for (let a = 0; a < 2; a++) {
         mbjscommonI = `${goallogo_}`;
      }
      while ((1 >> (Math.min(4, mbjscommonI.length))) > 5) {
         goallogo_ += penaltymatchiconj;
         break;
      }
      libreactn += `${((eyeopenF ? 3 : 4) >> (Math.min(Math.abs(parseInt(`${redcirclebgb}`)), 5)))}`;
      redcirclebgb += modulec.length >> (Math.min(1, Math.abs(dependencyK)));
   if (4 >= pointP.length) {
      redcirclebgb *= 2 * parseInt(`${heji_}`);
   }
      redcirclebgb -= starm.length | parseInt(`${b_playerG}`);
       let combinedX: Array<any> = [330, 71];
       let read4 = 2;
          let canvas9 = 5.0;
         combinedX.push(3 << (Math.min(1, combinedX.length)));
         canvas9 += parseFloat(`${parseInt(`${canvas9}`)}`);
         combinedX.push(read4 * combinedX.length);
          let telegramv = String.fromCharCode(114,105,99,101,0);
          let smallC = 0.0;
         read4 *= parseInt(`${smallC}`);
         telegramv += `${telegramv.length % (Math.max(2, 4))}`;
         smallC += (telegramv == String.fromCharCode(57,0) ? telegramv.length : telegramv.length);
      while ((4 & combinedX.length) > 2) {
          let disconnectedlogoO: Map<any, any> = new Map([[String.fromCharCode(102,105,108,116,101,114,101,100,0),707], [String.fromCharCode(114,101,116,114,121,97,98,108,101,0),840]]);
          let predictionW = String.fromCharCode(108,105,98,106,112,101,103,116,117,114,98,111,0);
         read4 /= Math.max(5, combinedX.length);
         disconnectedlogoO = new Map([[`${disconnectedlogoO.size}`, disconnectedlogoO.size % (Math.max(predictionW.length, 1))]]);
         predictionW = `${predictionW.length - disconnectedlogoO.size}`;
         break;
      }
         combinedX = [2];
         combinedX.push(read4);
      eyeopenF = (modulec.length / (Math.max(3, heji_))) == 73.48;
   let iconadslinkm = libreactn == String.fromCharCode(95,108,52,50,0);
   do {
      libreactn = "1";
      if (iconadslinkm) {
         break;
      }
   } while ((eyeopenF || libreactn.length <= 4) && iconadslinkm);
       let kickG: Map<any, any> = new Map([[String.fromCharCode(105,115,102,105,110,105,116,101,0),790], [String.fromCharCode(105,109,112,111,115,116,101,114,0),362], [String.fromCharCode(115,110,97,112,112,101,100,0),290]]);
       let helperD = String.fromCharCode(113,111,115,0);
          let homeK = true;
          let shielddoneV = String.fromCharCode(97,103,103,114,101,103,97,116,111,114,0);
         kickG = new Map([[`${kickG.size}`, (3 >> (Math.min(3, Math.abs((homeK ? 4 : 1)))))]]);
         homeK = shielddoneV.length > shielddoneV.length;
      for (let q = 0; q < 2; q++) {
         helperD = `${kickG.size & 2}`;
      }
          let checkboxA = 1.0;
          let sendm: Map<any, any> = new Map([[String.fromCharCode(100,117,109,112,101,114,0),722], [String.fromCharCode(100,101,115,99,114,105,112,116,105,111,110,115,0),151], [String.fromCharCode(111,117,116,108,105,110,107,0),183]]);
          let liveendmodallogoq: Array<any> = [String.fromCharCode(108,104,97,115,104,0), String.fromCharCode(106,117,109,112,0)];
         kickG.set(`${helperD}`, 2 * helperD.length);
         checkboxA /= Math.max(3, parseFloat(`${sendm.size * 2}`));
         sendm.set(`${liveendmodallogoq.length}`, 1);
         liveendmodallogoq.push(liveendmodallogoq.length ^ sendm.size);
       let neonD = String.fromCharCode(97,117,116,111,115,99,114,111,108,108,105,110,103,0);
       let auto_wr = String.fromCharCode(112,108,97,121,108,105,115,116,0);
         helperD = "1";
      let dialogz = String.fromCharCode(101,106,122,111,113,109,0) == neonD;
      do {
         neonD = `${kickG.size}`;
         if (dialogz) {
            break;
         }
      } while ((1 < neonD.length) && dialogz);
      hookso = [hookso.length - settingsI.length];
   let sigmobN = 5996887 <= dependencyK;
   do {
      dependencyK ^= 1 | parseInt(`${redcirclebgb}`);
      if (sigmobN) {
         break;
      }
   } while (sigmobN && (5 == (dependencyK - 4)));
   for (let b = 0; b < 2; b++) {
       let dark4: Array<any> = [469, 524];
       let hometeamfieldm = 3.0;
       let successp = String.fromCharCode(115,107,101,119,0);
       let unreadH = String.fromCharCode(114,116,109,112,112,107,116,0);
          let chatroombackgroundw = String.fromCharCode(117,110,105,102,105,101,100,0);
          let zhubol = 0.0;
         unreadH = `${(chatroombackgroundw == String.fromCharCode(113,0) ? chatroombackgroundw.length : dark4.length)}`;
         zhubol += parseInt(`${zhubol}`);
      for (let y = 0; y < 3; y++) {
          let homeinactivev: Map<any, any> = new Map([[String.fromCharCode(116,112,101,108,0),true ], [String.fromCharCode(109,117,115,120,0),false ], [String.fromCharCode(105,97,100,115,116,120,0),false ]]);
          let reactnavigationx = String.fromCharCode(112,114,101,118,105,111,117,115,0);
          let sideU = 5.0;
          let delegate_djY = String.fromCharCode(115,119,105,116,99,104,97,98,108,101,0);
         dark4 = [1 * homeinactivev.size];
         homeinactivev = new Map([[delegate_djY, (delegate_djY == String.fromCharCode(95,0) ? parseInt(`${sideU}`) : delegate_djY.length)]]);
         reactnavigationx = `${reactnavigationx.length << (Math.min(5, Math.abs(parseInt(`${sideU}`))))}`;
      }
         hometeamfieldm /= Math.max(3, parseFloat(`${dark4.length / 1}`));
      for (let c = 0; c < 1; c++) {
         dark4 = [3];
      }
      let libreactnativejniU = successp.length >= 7033501;
      do {
          let emojiheartw: Map<any, any> = new Map([[String.fromCharCode(99,112,111,115,0),877], [String.fromCharCode(109,97,120,120,0),79], [String.fromCharCode(114,101,109,97,112,112,101,100,0),882]]);
          let sentryN = true;
          let airbnbstarselectedj: Array<any> = [String.fromCharCode(116,97,108,107,0), String.fromCharCode(100,114,97,119,97,98,108,101,0)];
          let textY = String.fromCharCode(114,105,110,103,116,111,110,101,115,0);
          let privilegeY = String.fromCharCode(99,104,97,114,97,99,116,101,100,0);
         successp += `${parseInt(`${hometeamfieldm}`)}`;
         emojiheartw.set(privilegeY, 2 >> (Math.min(3, Math.abs(emojiheartw.size))));
         sentryN = airbnbstarselectedj.length == 61;
         airbnbstarselectedj = [(String.fromCharCode(122,0) == privilegeY ? emojiheartw.size : privilegeY.length)];
         textY += `${privilegeY.length >> (Math.min(Math.abs(2), 5))}`;
         if (libreactnativejniU) {
            break;
         }
      } while ((4 < unreadH.length) && libreactnativejniU);
         hometeamfieldm -= parseFloat(`${1 >> (Math.min(2, dark4.length))}`);
         hometeamfieldm *= parseFloat(`${dark4.length}`);
         successp += `${unreadH.length % (Math.max(4, successp.length))}`;
      if (3 <= (2 | successp.length) && 1.56 <= (hometeamfieldm + 3.47)) {
          let google9 = String.fromCharCode(116,120,116,111,98,106,0);
          let iconclosewhitep = String.fromCharCode(100,114,105,118,101,110,0);
          let muted8: Map<any, any> = new Map([[String.fromCharCode(98,105,109,97,112,0),992], [String.fromCharCode(118,105,97,98,108,101,0),469]]);
         hometeamfieldm += parseFloat(`${1}`);
         google9 = `${iconclosewhitep.length / (Math.max(6, google9.length))}`;
         iconclosewhitep += `${(iconclosewhitep == String.fromCharCode(122,0) ? muted8.size : iconclosewhitep.length)}`;
         muted8 = new Map([[`${muted8.size}`, 2]]);
      }
      for (let x = 0; x < 1; x++) {
          let previewV = String.fromCharCode(102,112,115,0);
          let file_ = String.fromCharCode(99,111,110,116,97,105,110,97,98,108,101,0);
          let malaysia3 = true;
         unreadH = `${(file_ == String.fromCharCode(84,0) ? (malaysia3 ? 4 : 4) : file_.length)}`;
         previewV = `${1 + previewV.length}`;
      }
          let chatroombackgroundm = String.fromCharCode(99,111,109,112,114,101,115,115,105,111,110,0);
         hometeamfieldm /= Math.max(3, parseFloat(`${dark4.length ^ 3}`));
         chatroombackgroundm += `${chatroombackgroundm.length * chatroombackgroundm.length}`;
       let shootnogoalg = 2.0;
       let hnewarchdefaultsu = 5.0;
      modaln += unreadH.length;
   }
   let malaysiaz = eyeopenF ? !eyeopenF : eyeopenF;
   do {
       let downloadingU = String.fromCharCode(99,111,110,102,111,114,109,97,110,99,101,0);
       let main_jq = 2;
         main_jq *= downloadingU.length << (Math.min(5, Math.abs(main_jq)));
      if ((downloadingU.length / (Math.max(1, 6))) <= 1) {
          let switch_9E = String.fromCharCode(112,97,116,104,109,116,117,0);
         main_jq /= Math.max(1, downloadingU.length / 2);
         switch_9E = `${switch_9E.length | 2}`;
      }
         main_jq >>= Math.min(Math.abs(downloadingU.length ^ 2), 5);
         main_jq /= Math.max(5, main_jq);
      if (main_jq < 1) {
         main_jq %= Math.max((downloadingU == String.fromCharCode(81,0) ? downloadingU.length : main_jq), 2);
      }
      if (2 > (downloadingU.length % (Math.max(4, 5))) && (downloadingU.length % 4) > 1) {
          let huaweix = 2.0;
          let sportZ = String.fromCharCode(98,97,99,107,101,110,100,0);
          let o_playerx = String.fromCharCode(98,111,111,115,116,0);
          let pushA = String.fromCharCode(101,110,116,101,114,101,100,0);
         main_jq *= 2;
         huaweix += parseInt(`${huaweix}`);
         sportZ += `${(String.fromCharCode(105,0) == o_playerx ? parseInt(`${huaweix}`) : o_playerx.length)}`;
         pushA += `${1 % (Math.max(8, parseInt(`${huaweix}`)))}`;
      }
      eyeopenF = settingsI.length < 63;
      if (malaysiaz) {
         break;
      }
   } while (malaysiaz && ((5 & modaln) <= 1 || !eyeopenF));
   for (let e = 0; e < 3; e++) {
      redcirclebgb *= parseInt(`${heji_}`) | 2;
   }
   if (modulec.endsWith(`${hookso.length}`)) {
      modulec = `${settingsI.length}`;
   }
      hookso.push(2 ^ modulec.length);
   if (modulec.includes(`${championa}`)) {
       let acceptedD = false;
       let libfabricjniQ = String.fromCharCode(98,108,111,111,109,0);
       let privatechatbg1 = String.fromCharCode(115,104,97,114,101,115,0);
      while (5 < libfabricjniQ.length && !acceptedD) {
         libfabricjniQ += `${(libfabricjniQ.length >> (Math.min(3, Math.abs((acceptedD ? 5 : 1)))))}`;
         break;
      }
          let lnewsshare2 = true;
         acceptedD = privatechatbg1.length <= 81;
         lnewsshare2 = !lnewsshare2;
         acceptedD = libfabricjniQ.length < 76;
          let arrowrightwithtailB = false;
          let middlesoundD = 4;
          let roota = 0;
         privatechatbg1 = `${roota}`;
         arrowrightwithtailB = !arrowrightwithtailB;
         middlesoundD <<= Math.min(Math.abs(middlesoundD), 1);
         roota /= Math.max(((arrowrightwithtailB ? 4 : 4) + middlesoundD), 3);
          let pressures: Array<any> = [30, 498];
          let typing4 = 2.0;
         libfabricjniQ += "1";
         pressures.push(parseInt(`${typing4}`) % (Math.max(9, pressures.length)));
         typing4 += parseFloat(`${3}`);
      while (!libfabricjniQ.includes(`${acceptedD}`)) {
         libfabricjniQ = `${libfabricjniQ.length}`;
         break;
      }
          let actions2 = String.fromCharCode(105,115,111,0);
          let volume7 = 2.0;
         libfabricjniQ += `${1 & parseInt(`${volume7}`)}`;
         actions2 += `${actions2.length << (Math.min(actions2.length, 1))}`;
         volume7 += actions2.length;
         acceptedD = !acceptedD;
       let main_rB = String.fromCharCode(102,111,114,109,97,116,116,105,110,103,0);
       let leakchecker2 = String.fromCharCode(102,111,117,114,99,99,0);
      modulec += `${settingsI.length << (Math.min(2, Math.abs(parseInt(`${b_playerG}`))))}`;
   }
      b_playerG += parseFloat(`${2}`);
   if ((settingsI.length - 1) <= 4) {
      eyeopenF = settingsI.length == 4 || 4 == libreactn.length;
   }
   if (5 > (libreactn.length | 1)) {
      starm = [hookso.length >> (Math.min(Math.abs(3), 5))];
   }
       let previewb = 1.0;
       let downloadedm = String.fromCharCode(97,112,112,114,101,99,97,116,105,111,110,104,111,117,114,0);
       let questa = String.fromCharCode(107,101,114,110,101,114,0);
      if (5 > (downloadedm.length / (Math.max(2, 1))) && 4.89 > (previewb / 5.26)) {
         previewb *= parseFloat(`${parseInt(`${previewb}`) - 1}`);
      }
       let eventsplasha: Map<any, any> = new Map([[String.fromCharCode(114,101,97,100,113,0),false ], [String.fromCharCode(114,101,112,0),false ]]);
       let whistleorangeQ: Map<any, any> = new Map([[String.fromCharCode(109,111,100,101,99,111,115,116,115,0),false ], [String.fromCharCode(100,105,115,116,97,110,99,101,0),true ], [String.fromCharCode(115,98,115,112,108,105,116,0),false ]]);
         downloadedm += "3";
      let basketballiconk = 6417713 <= whistleorangeQ.size;
      do {
          let shrinkO = String.fromCharCode(113,117,101,117,101,0);
         whistleorangeQ.set(questa, 2 & downloadedm.length);
         shrinkO += "3";
         if (basketballiconk) {
            break;
         }
      } while (basketballiconk && (!Array.from(whistleorangeQ.keys()).includes(`${eventsplasha.size}`)));
      for (let v = 0; v < 1; v++) {
         questa = `${3 | whistleorangeQ.size}`;
      }
         questa += `${parseInt(`${previewb}`)}`;
       let friendsZ = String.fromCharCode(108,97,121,111,121,116,0);
      while ((1.20 + previewb) > 2.53 && (eventsplasha.size + 2) > 1) {
         eventsplasha.set(friendsZ, downloadedm.length + 1);
         break;
      }
      for (let n = 0; n < 2; n++) {
         whistleorangeQ = new Map([[`${whistleorangeQ.size}`, (questa == String.fromCharCode(112,0) ? whistleorangeQ.size : questa.length)]]);
      }
      settingsI = [2];
      heji_ *= modaln;
      settingsI = [libfbjnih.length * settingsI.length];
   while ((parseInt(`${b_playerG}`) + starm.length) < 3 || (b_playerG + 1.28) < 3.50) {
       let penaltymatchicony = String.fromCharCode(97,118,102,111,117,110,100,97,116,105,111,110,0);
       let crownv = String.fromCharCode(98,117,116,116,111,110,115,0);
       let changed = true;
       let pingW = String.fromCharCode(112,97,114,116,105,116,105,111,110,101,100,0);
       let sourceW: Array<any> = [424, 278];
      while (!crownv.endsWith(`${changed}`)) {
         crownv = `${(crownv == String.fromCharCode(54,0) ? penaltymatchicony.length : crownv.length)}`;
         break;
      }
      if (4 <= (penaltymatchicony.length + 2)) {
         sourceW = [2 / (Math.max(7, pingW.length))];
      }
      let submitO = crownv == String.fromCharCode(48,102,52,102,51,110,115,55,0);
      do {
         crownv = `${penaltymatchicony.length % (Math.max(1, 2))}`;
         if (submitO) {
            break;
         }
      } while ((changed || crownv.length == 3) && submitO);
         pingW += `${pingW.length}`;
       let whitebelln: Map<any, any> = new Map([[String.fromCharCode(109,99,100,99,0),381], [String.fromCharCode(114,116,109,112,0),269]]);
       let issubt: Map<any, any> = new Map([[String.fromCharCode(116,121,112,101,115,116,114,0),205], [String.fromCharCode(97,115,115,111,99,105,97,116,101,0),992]]);
         crownv += `${crownv.length}`;
          let temperatureb = 2.0;
          let backwardO = String.fromCharCode(116,101,115,115,101,108,108,97,116,101,0);
          let indicatorp = String.fromCharCode(101,108,97,112,115,101,100,0);
         whitebelln = new Map([[`${whitebelln.size}`, (String.fromCharCode(84,0) == penaltymatchicony ? penaltymatchicony.length : whitebelln.size)]]);
         temperatureb -= (String.fromCharCode(75,0) == backwardO ? backwardO.length : parseInt(`${temperatureb}`));
         indicatorp += `${(indicatorp == String.fromCharCode(107,0) ? indicatorp.length : parseInt(`${temperatureb}`))}`;
         crownv += `${((changed ? 4 : 5) << (Math.min(pingW.length, 1)))}`;
      while ((issubt.size | 5) < 1) {
          let floatingo = 1;
          let iconeditM = 2.0;
          let settingsx = String.fromCharCode(112,97,103,0);
         pingW = `${penaltymatchicony.length * parseInt(`${iconeditM}`)}`;
         floatingo /= Math.max(floatingo * settingsx.length, 4);
         iconeditM += parseFloat(`${floatingo | 2}`);
         settingsx += `${floatingo}`;
         break;
      }
      while (issubt.get(`${sourceW.length}`) != null) {
         sourceW.push(3 - sourceW.length);
         break;
      }
      if (3 > (sourceW.length * 5) || (5 * sourceW.length) > 2) {
          let taiwan2 = false;
          let modity6: Array<any> = [661, 628];
         whitebelln.set(`${taiwan2}`, 2 * modity6.length);
      }
         pingW = `${crownv.length}`;
         penaltymatchicony += `${((changed ? 4 : 5) / 2)}`;
      if (!changed) {
         pingW += `${((changed ? 3 : 4) / (Math.max(3, 7)))}`;
      }
         whitebelln.set(penaltymatchicony, 1 / (Math.max(1, crownv.length)));
      starm.push(parseInt(`${redcirclebgb}`) << (Math.min(sourceW.length, 3)));
      break;
   }
       let memberX = String.fromCharCode(122,114,101,111,114,100,101,114,0);
         memberX += `${memberX.length}`;
         memberX += `${memberX.length | 1}`;
         memberX = `${memberX.length % (Math.max(memberX.length, 4))}`;
      libfbjnih += `${hookso.length % (Math.max(3, 2))}`;
   while ((b_playerG - 1.56) >= 3.19 && b_playerG >= 1.56) {
      eyeopenF = !eyeopenF && modulec.length <= 35;
      break;
   }
   while (championa) {
      championa = modulec.length <= 89;
      break;
   }
       let actionR = String.fromCharCode(99,111,110,115,105,115,116,101,110,99,121,0);
       let libruntimeexecutorZ = 5.0;
      let connection9 = 7010170 >= actionR.length;
      do {
         actionR += `${actionR.length}`;
         if (connection9) {
            break;
         }
      } while ((!actionR.endsWith(`${libruntimeexecutorZ}`)) && connection9);
      for (let k = 0; k < 3; k++) {
          let redirectk = 0;
         libruntimeexecutorZ /= Math.max(1, actionR.length);
         redirectk /= Math.max(1, 2);
      }
          let launchv: Map<any, any> = new Map([[String.fromCharCode(98,111,120,101,100,0),true ], [String.fromCharCode(104,115,116,114,105,110,103,0),false ], [String.fromCharCode(115,117,98,116,114,97,99,116,105,110,103,0),false ]]);
         libruntimeexecutorZ *= launchv.size;
       let flyerq: Array<any> = [764, 440, 200];
      for (let j = 0; j < 1; j++) {
         flyerq.push(flyerq.length);
      }
         actionR = `${parseInt(`${libruntimeexecutorZ}`) >> (Math.min(actionR.length, 3))}`;
      modulec = `${((eyeopenF ? 1 : 3) ^ 1)}`;
   for (let n = 0; n < 3; n++) {
       let heartN = String.fromCharCode(98,115,101,114,118,101,114,0);
       let nativeexX = String.fromCharCode(116,114,101,110,100,105,110,103,0);
       let backwardh = 2;
       let sellmathbackgroundq = 4;
      while (5 == heartN.length) {
         heartN = `${nativeexX.length << (Math.min(heartN.length, 1))}`;
         break;
      }
          let inouttargetyellow9 = 1;
          let activitye = String.fromCharCode(103,101,110,101,114,97,116,105,111,110,0);
          let teamdetailsbgH = String.fromCharCode(112,114,111,99,99,101,115,115,0);
         backwardh -= backwardh & 3;
         inouttargetyellow9 >>= Math.min(Math.abs(activitye.length << (Math.min(Math.abs(3), 5))), 2);
         activitye += `${inouttargetyellow9 & activitye.length}`;
         teamdetailsbgH = `${activitye.length - 3}`;
          let notificationg = String.fromCharCode(116,97,98,108,101,105,110,105,116,0);
          let referrer7 = 3;
         backwardh += 1;
         notificationg = `${notificationg.length << (Math.min(Math.abs(1), 2))}`;
         referrer7 >>= Math.min(3, Math.abs((String.fromCharCode(86,0) == notificationg ? referrer7 : notificationg.length)));
         sellmathbackgroundq |= heartN.length - nativeexX.length;
          let yellowvideoliveo = 2;
          let phonesharek: Map<any, any> = new Map([[String.fromCharCode(97,103,103,105,110,102,111,0),279], [String.fromCharCode(112,105,110,99,104,0),979]]);
          let questv: Array<any> = [655, 569, 793];
         backwardh >>= Math.min(Math.abs((String.fromCharCode(87,0) == nativeexX ? heartN.length : nativeexX.length)), 3);
         yellowvideoliveo &= yellowvideoliveo;
         phonesharek.set(`${questv.length}`, questv.length);
      if (2 <= backwardh) {
         nativeexX = `${backwardh | 2}`;
      }
      if (backwardh > nativeexX.length) {
         nativeexX += `${(String.fromCharCode(53,0) == heartN ? backwardh : heartN.length)}`;
      }
      while (heartN.length <= nativeexX.length) {
         heartN = "2";
         break;
      }
         backwardh ^= sellmathbackgroundq / (Math.max(backwardh, 10));
      if (1 == heartN.length) {
         heartN = `${nativeexX.length}`;
      }
      while (heartN == nativeexX) {
          let friendsf: Map<any, any> = new Map([[String.fromCharCode(115,101,108,101,99,116,105,110,103,0),451], [String.fromCharCode(114,101,113,117,115,116,101,114,0),279]]);
          let subout0: Map<any, any> = new Map([[String.fromCharCode(114,97,108,102,0),485], [String.fromCharCode(98,117,102,115,0),785]]);
          let frame_mv = true;
          let libreactperfloggerjnil: Map<any, any> = new Map([[String.fromCharCode(100,101,116,101,99,116,105,111,110,0),541], [String.fromCharCode(100,114,97,119,0),185]]);
          let phoneo = String.fromCharCode(104,119,114,97,110,100,0);
         nativeexX = `${subout0.size}`;
         friendsf.set(phoneo, (phoneo.length & (frame_mv ? 3 : 4)));
         subout0 = new Map([[`${frame_mv}`, (2 >> (Math.min(5, Math.abs((frame_mv ? 1 : 2)))))]]);
         libreactperfloggerjnil.set(`${phoneo}`, libreactperfloggerjnil.size);
         break;
      }
         sellmathbackgroundq <<= Math.min(Math.abs((String.fromCharCode(74,0) == nativeexX ? nativeexX.length : heartN.length)), 5);
      hookso = [pointP.length - 3];
   }
   let strD = modaln <= 8374158;
   do {
      modaln %= Math.max(starm.length, 1);
      if (strD) {
         break;
      }
   } while ((2.42 > (heji_ + 3.5)) && strD);
      modulec = `${((championa ? 4 : 3) / (Math.max(parseInt(`${b_playerG}`), 4)))}`;
   while (!libfbjnih.endsWith(`${championa}`)) {
       let overu = true;
       let settingsk = String.fromCharCode(116,111,107,101,0);
       let xvod3 = true;
       let yellowtoredy = 0.0;
         xvod3 = !settingsk.startsWith(`${xvod3}`);
         settingsk += `${3 * parseInt(`${yellowtoredy}`)}`;
         xvod3 = overu || !xvod3;
      for (let s = 0; s < 3; s++) {
          let whistleorangeU = 0;
         xvod3 = (27 <= (settingsk.length % (Math.max(6, (!overu ? 27 : settingsk.length)))));
         whistleorangeU >>= Math.min(Math.abs(whistleorangeU >> (Math.min(5, Math.abs(whistleorangeU)))), 3);
      }
         settingsk = `${((overu ? 1 : 3) / (Math.max(parseInt(`${yellowtoredy}`), 7)))}`;
         overu = !xvod3 || 39.28 >= yellowtoredy;
         overu = !overu || 50.52 <= yellowtoredy;
          let sharedv: Array<any> = [701, 180, 407];
          let adultb = String.fromCharCode(110,97,109,101,115,101,114,118,101,114,0);
         yellowtoredy /= Math.max(3, 2 >> (Math.min(2, settingsk.length)));
         sharedv = [sharedv.length / 2];
         adultb = `${sharedv.length / 3}`;
       let imagemanagerQ: Array<any> = [105, 438];
       let playershirtB: Array<any> = [38, 135, 748];
      for (let y = 0; y < 3; y++) {
         yellowtoredy /= Math.max(4, parseInt(`${yellowtoredy}`) & settingsk.length);
      }
          let stylesr = 1.0;
         overu = 83.59 <= stylesr && !overu;
          let watchG = 3;
          let fullV = String.fromCharCode(114,101,112,108,97,99,101,0);
         overu = 95 == playershirtB.length && 95 == watchG;
         watchG &= fullV.length;
         fullV = "3";
      libfbjnih = "3";
      break;
   }
},
}: wawaAwayShow) {
  const {colors, icons, textVariants} = useTheme();
  return (
    <TouchableOpacity style={styles.container} onPress={onPress}>
      <SearchBar
        platform="default"
        containerStyle={styles.containerStyle}
        inputContainerStyle={{
          backgroundColor: colors.search,
          ...styles.inputContainerStyle,
        }}
        inputStyle={{
          ...textVariants.subBody,
          position: 'relative',
          right: 10,
          top: 1,
        }}
        placeholder="输入搜索关键词"
        placeholderTextColor={colors.text}
        round
        searchIcon={<SearchIcon color={colors.text} height={23} width={23} />}
        value={defaultValue}
        
        showLoading={false}
        clearIcon={<></>}
      />
    </TouchableOpacity>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignSelf: 'center',
    marginLeft: 8,
    marginRight: 8,
  },
  containerStyle: {
    padding: 0,
    marginLeft: 0,
    marginRight: 0,
    textAlign: 'left',
    borderRadius: 20,
    borderTopWidth: 0,
    borderBottomWidth: 0,
    
    
    
    backgroundColor: 'transparent',
  },
  inputContainerStyle: {
    borderRadius: 20,
    pointerEvents: 'none',
    height: 32,
    opacity: 0.6,
  },
});
