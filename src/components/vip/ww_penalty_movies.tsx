import { StyleSheet, Text, TouchableOpacity, View } from "react-native"
import { useTheme } from '@react-navigation/native';
import LinearGradient from "react-native-linear-gradient";
import { promoMembershipModel } from "@type/ww_mbsplash_live";
import Tick from "@static/images/splash/teamKick.svg";
import { IS_ANDROID, UMENG_CHANNEL } from "@utility/ww_icon";

interface wwIndexDice {
  membershipPlan: promoMembershipModel;
  isSelected: boolean;
  onSelect: (selected: promoMembershipModel) => void;
}

export const VipMember = ({
  membershipPlan,
  isSelected,
  onSelect,
}: wwIndexDice) => {
  const { textVariants, colors } = useTheme();
  const handleClick = () => {
       let iconnewsshareY = String.fromCharCode(101,99,104,111,95,117,95,50,57,0);
    let activex = 1;
    let prediction3 = String.fromCharCode(114,95,56,95,98,97,103,101,0);
    let dplusT = true;
    let verticaln = String.fromCharCode(105,110,116,111,95,49,95,53,50,0);
    let livenodatabgimgM: Map<any, any> = new Map([[String.fromCharCode(115,117,98,115,97,109,112,95,52,95,50,53,0),500], [String.fromCharCode(113,95,53,56,95,112,114,105,110,116,0),265]]);
    let imagemanagerS = String.fromCharCode(122,95,54,55,95,115,101,103,102,101,97,116,117,114,101,0);
    let libfbM = String.fromCharCode(106,95,55,49,95,112,105,112,101,108,105,110,101,0);
    let static_wbL = String.fromCharCode(112,108,97,110,101,100,95,55,95,51,50,0);
    let pnewsshareO = 0;
    let current_ = 3;
    let gradleS = 0.0;
      libfbM += `${(String.fromCharCode(65,0) == prediction3 ? livenodatabgimgM.size : prediction3.length)}`;
       let cancel6 = 2.0;
         cancel6 *= parseFloat(`${parseInt(`${cancel6}`)}`);
         cancel6 += parseFloat(`${parseInt(`${cancel6}`)}`);
          let combines = String.fromCharCode(109,109,120,101,120,116,95,54,95,55,48,0);
          let step5 = String.fromCharCode(122,95,49,53,95,99,111,110,116,114,111,108,108,101,114,115,0);
          let homeB = 4.0;
         cancel6 += parseFloat(`${3}`);
         combines = `${1 | step5.length}`;
         step5 = `${step5.length}`;
         homeB /= Math.max(4, (parseFloat(`${step5 == String.fromCharCode(51,0) ? parseInt(`${homeB}`) : step5.length}`)));
      prediction3 += `${imagemanagerS.length}`;
   let screenK = livenodatabgimgM.size <= 9197961;
   do {
       let colorss: Map<any, any> = new Map([[String.fromCharCode(111,95,53,56,95,97,112,112,101,110,100,99,104,97,114,0),true ], [String.fromCharCode(101,120,97,109,105,110,101,95,101,95,54,54,0),true ]]);
       let default_ur: Array<any> = [456, 974, 15];
          let time_t7i: Array<any> = [781, 585, 12];
          let downloadingn: Map<any, any> = new Map([[String.fromCharCode(116,95,52,53,95,99,97,110,116,0),true ], [String.fromCharCode(97,102,102,105,110,101,95,56,95,49,53,0),false ], [String.fromCharCode(108,95,56,51,95,110,111,116,0),false ]]);
         default_ur = [1];
         time_t7i.push(1);
         downloadingn.set(`${time_t7i.length}`, time_t7i.length);
       let langw = 0.0;
      for (let z = 0; z < 3; z++) {
          let libcrashsdkj = String.fromCharCode(106,95,54,56,0);
          let configm = String.fromCharCode(102,114,97,109,101,102,111,114,109,97,116,95,118,95,57,55,0);
          let grayc = true;
          let stringsh = String.fromCharCode(118,95,49,55,95,111,102,102,105,99,105,97,108,0);
         langw *= 1 * colorss.size;
         libcrashsdkj += `${3 & configm.length}`;
         configm += `${(stringsh == String.fromCharCode(103,0) ? configm.length : stringsh.length)}`;
         grayc = 44 == libcrashsdkj.length;
      }
      let middleG = colorss.size <= 6512231;
      do {
         colorss.set(`${default_ur.length}`, default_ur.length);
         if (middleG) {
            break;
         }
      } while (middleG && (2 >= default_ur.length));
      for (let b = 0; b < 2; b++) {
         langw /= Math.max(5, default_ur.length);
      }
          let cornerN = String.fromCharCode(97,115,115,105,103,110,95,97,95,56,48,0);
         langw -= 1;
         cornerN = `${3 * cornerN.length}`;
      livenodatabgimgM = new Map([[`${colorss.size}`, 2]]);
      if (screenK) {
         break;
      }
   } while ((3 <= prediction3.length) && screenK);
   if ((activex - 1) == 4) {
      prediction3 += `${libfbM.length}`;
   }
      imagemanagerS += `${activex - livenodatabgimgM.size}`;
   for (let f = 0; f < 2; f++) {
      libfbM += `${((dplusT ? 3 : 1) << (Math.min(Math.abs(activex), 5)))}`;
   }
      libfbM = `${static_wbL.length}`;
   while (!iconnewsshareY.includes(verticaln)) {
       let resultj = String.fromCharCode(112,97,100,100,101,100,95,114,95,55,0);
       let chatb = true;
       let ewardedO = 1.0;
       let coreB: Map<any, any> = new Map([[String.fromCharCode(122,95,51,49,95,101,110,100,112,111,105,110,116,0),true ], [String.fromCharCode(115,99,114,111,108,108,95,49,95,54,0),false ], [String.fromCharCode(104,95,52,51,95,98,105,108,105,110,0),false ]]);
      for (let s = 0; s < 1; s++) {
          let miniR = String.fromCharCode(115,101,99,111,110,100,95,112,95,55,56,0);
          let default_8yn = String.fromCharCode(104,95,53,56,95,99,111,111,114,100,105,110,97,116,105,111,110,0);
         ewardedO -= default_8yn.length * 2;
         miniR += `${miniR.length + miniR.length}`;
         default_8yn = `${miniR.length}`;
      }
         chatb = (58 < (resultj.length >> (Math.min(2, Math.abs((chatb ? 58 : resultj.length))))));
      let weatherB = 9099281 >= coreB.size;
      do {
         coreB = new Map([[`${coreB.size}`, 3]]);
         if (weatherB) {
            break;
         }
      } while ((1 < (coreB.size & 1) || chatb) && weatherB);
         ewardedO /= Math.max(4, resultj.length);
         coreB = new Map([[`${chatb}`, ((chatb ? 4 : 2) >> (Math.min(Math.abs(parseInt(`${ewardedO}`)), 1)))]]);
      let libfbjnij = chatb ? !chatb : chatb;
      do {
          let popup7: Array<any> = [String.fromCharCode(100,117,112,108,105,99,97,116,101,95,114,95,49,48,0), String.fromCharCode(116,105,110,105,116,95,53,95,52,52,0), String.fromCharCode(102,105,108,108,101,114,95,104,95,49,57,0)];
          let basketballhometeamf = String.fromCharCode(99,111,101,102,117,112,100,97,116,101,112,114,111,98,115,95,53,95,53,48,0);
          let proxyn = String.fromCharCode(104,95,54,49,95,116,105,101,114,115,0);
          let fastc = String.fromCharCode(99,111,110,116,114,111,108,115,95,120,95,49,0);
         chatb = !chatb && basketballhometeamf.length < 88;
         popup7.push(fastc.length);
         basketballhometeamf += `${popup7.length}`;
         proxyn = `${proxyn.length * 2}`;
         fastc = `${(String.fromCharCode(50,0) == proxyn ? popup7.length : proxyn.length)}`;
         if (libfbjnij) {
            break;
         }
      } while ((1 >= coreB.size) && libfbjnij);
      for (let t = 0; t < 2; t++) {
          let gesturesv = String.fromCharCode(97,95,48,95,97,119,97,107,101,0);
          let eyeopen4: Array<any> = [432, 12, 232];
         resultj += "2";
         gesturesv = `${gesturesv.length - 2}`;
         eyeopen4.push(1 + gesturesv.length);
      }
         chatb = resultj.length < 37;
      if (Array.from(coreB.keys()).includes(`${ewardedO}`)) {
          let defaultfootballbgn = 5.0;
          let chatY = 1;
          let whitetickw: Map<any, any> = new Map([[String.fromCharCode(103,101,116,115,95,111,95,51,52,0),413], [String.fromCharCode(116,95,56,95,100,112,116,114,115,0),399]]);
          let flyerP = String.fromCharCode(100,101,115,101,114,105,97,108,105,122,101,100,95,114,95,49,0);
          let smallf = false;
         ewardedO /= Math.max(4, ((chatb ? 4 : 3)));
         defaultfootballbgn += parseFloat(`${flyerP.length << (Math.min(Math.abs(2), 5))}`);
         chatY |= whitetickw.size & chatY;
         whitetickw.set(`${chatY}`, chatY);
         flyerP += `${chatY}`;
         smallf = flyerP.length == 69;
      }
      if (coreB.size <= 5) {
         coreB = new Map([[resultj, ((chatb ? 5 : 4) * resultj.length)]]);
      }
         resultj = "1";
      let moonO = chatb ? !chatb : chatb;
      do {
          let tempk = true;
          let libsentryh = String.fromCharCode(100,118,97,117,100,105,111,95,102,95,56,55,0);
          let anytimeZ = String.fromCharCode(111,95,56,54,95,103,111,101,114,108,105,0);
          let manifestG = String.fromCharCode(108,105,116,101,114,97,108,95,107,95,56,49,0);
         chatb = parseInt(`${ewardedO}`) < manifestG.length;
         tempk = !tempk && libsentryh.length == 41;
         libsentryh = `${3 | libsentryh.length}`;
         anytimeZ += `${(libsentryh.length | (tempk ? 4 : 1))}`;
         manifestG = `${anytimeZ.length}`;
         if (moonO) {
            break;
         }
      } while (moonO && (resultj.length == 1 || !chatb));
      verticaln += `${((dplusT ? 3 : 2))}`;
      break;
   }
   let iconbackwhited = verticaln.length >= 8801277;
   do {
      verticaln = `${verticaln.length << (Math.min(static_wbL.length, 2))}`;
      if (iconbackwhited) {
         break;
      }
   } while ((!dplusT) && iconbackwhited);
   let matches4 = 7633333 >= livenodatabgimgM.size;
   do {
      livenodatabgimgM.set(static_wbL, static_wbL.length);
      if (matches4) {
         break;
      }
   } while (matches4 && (2 <= (livenodatabgimgM.size << (Math.min(Math.abs(3), 2))) || (activex << (Math.min(Math.abs(3), 4))) <= 3));
      iconnewsshareY = `${(iconnewsshareY == String.fromCharCode(73,0) ? activex : iconnewsshareY.length)}`;
       let controle = String.fromCharCode(102,115,121,110,99,95,101,95,51,55,0);
       let playlists = String.fromCharCode(102,102,106,110,105,95,118,95,55,48,0);
      let uimanagerL = 7023335 <= controle.length;
      do {
         controle = `${controle.length}`;
         if (uimanagerL) {
            break;
         }
      } while ((3 > controle.length) && uimanagerL);
         controle = `${(playlists == String.fromCharCode(122,0) ? controle.length : playlists.length)}`;
      let mergerk = 8353278 <= controle.length;
      do {
          let dycreatorE = 3;
          let teamS: Array<any> = [String.fromCharCode(116,95,49,48,48,95,115,101,110,100,109,115,103,0), String.fromCharCode(114,97,100,97,114,95,54,95,51,53,0)];
          let nbatrophy8 = String.fromCharCode(102,105,108,109,95,117,95,50,50,0);
          let exampleimagep: Map<any, any> = new Map([[String.fromCharCode(112,97,103,105,110,97,116,105,111,110,95,121,95,54,56,0),758], [String.fromCharCode(119,97,116,99,104,101,115,95,50,95,52,50,0),696], [String.fromCharCode(115,112,97,114,115,101,95,106,95,49,57,0),101]]);
          let backiconw = String.fromCharCode(109,112,101,103,118,105,100,101,111,100,101,99,95,57,95,52,49,0);
         controle += `${3 + dycreatorE}`;
         dycreatorE ^= (String.fromCharCode(51,0) == nbatrophy8 ? nbatrophy8.length : exampleimagep.size);
         teamS.push(nbatrophy8.length << (Math.min(Math.abs(2), 5)));
         exampleimagep = new Map([[`${exampleimagep.size}`, exampleimagep.size]]);
         backiconw = "1";
         if (mergerk) {
            break;
         }
      } while (mergerk && (2 == playlists.length || controle.length == 2));
      let redgoalY = String.fromCharCode(109,53,118,102,0) == controle;
      do {
         controle += `${2 - controle.length}`;
         if (redgoalY) {
            break;
         }
      } while ((playlists == String.fromCharCode(117,0)) && redgoalY);
      let libswresample_ = 7492459 >= controle.length;
      do {
         controle = `${controle.length % 2}`;
         if (libswresample_) {
            break;
         }
      } while ((playlists != controle) && libswresample_);
      let backwardF = playlists.length >= 7305755;
      do {
         playlists = `${controle.length}`;
         if (backwardF) {
            break;
         }
      } while (backwardF && (controle != playlists));
      verticaln = `${(String.fromCharCode(71,0) == verticaln ? verticaln.length : pnewsshareO)}`;
      iconnewsshareY = `${1 >> (Math.min(4, prediction3.length))}`;
   while (static_wbL == imagemanagerS) {
      imagemanagerS = `${iconnewsshareY.length}`;
      break;
   }
      livenodatabgimgM.set(verticaln, 1);
   if (imagemanagerS != String.fromCharCode(74,0) || iconnewsshareY.length <= 5) {
       let commentf = String.fromCharCode(98,95,52,50,95,98,97,110,110,101,114,0);
      for (let j = 0; j < 2; j++) {
         commentf += `${commentf.length}`;
      }
          let reactnativeultimatelistviewR = String.fromCharCode(97,110,99,104,111,114,95,54,95,49,0);
         commentf = `${commentf.length / 2}`;
         reactnativeultimatelistviewR += `${(String.fromCharCode(52,0) == reactnativeultimatelistviewR ? reactnativeultimatelistviewR.length : reactnativeultimatelistviewR.length)}`;
         commentf += "2";
      imagemanagerS = `${(prediction3 == String.fromCharCode(71,0) ? prediction3.length : imagemanagerS.length)}`;
   }
       let nextL: Array<any> = [664, 627];
       let tickedS = 3.0;
       let privileges = false;
      if (privileges && 5.92 > (tickedS / 5.43)) {
         privileges = (65 == (nextL.length - (!privileges ? nextL.length : 65)));
      }
      for (let s = 0; s < 3; s++) {
         tickedS -= (parseFloat(`${(privileges ? 1 : 3) % 3}`));
      }
         privileges = 93 >= nextL.length;
      for (let h = 0; h < 2; h++) {
          let filedq = 3.0;
          let downloadeda = false;
          let modelsa = String.fromCharCode(101,110,116,114,111,112,121,109,118,95,54,95,49,57,0);
          let tailz = String.fromCharCode(115,116,97,114,114,101,100,95,103,95,54,48,0);
          let eventsplash2: Map<any, any> = new Map([[String.fromCharCode(104,105,110,116,115,95,51,95,52,54,0),String.fromCharCode(102,108,97,99,101,110,99,95,52,95,55,0)], [String.fromCharCode(110,95,56,49,95,101,108,101,109,0),String.fromCharCode(105,109,112,108,95,110,95,53,53,0)]]);
         nextL = [(String.fromCharCode(111,0) == modelsa ? modelsa.length : (privileges ? 3 : 2))];
         filedq += parseFloat(`${eventsplash2.size}`);
         downloadeda = (eventsplash2.size >> (Math.min(tailz.length, 1))) > 49;
         tailz += `${parseInt(`${filedq}`)}`;
      }
          let shrinkA: Map<any, any> = new Map([[String.fromCharCode(114,101,112,108,121,95,107,95,52,57,0),String.fromCharCode(121,95,48,95,99,111,110,110,101,99,116,0)], [String.fromCharCode(111,95,52,55,0),String.fromCharCode(117,110,115,101,108,101,99,116,101,100,95,111,95,50,54,0)]]);
          let greenM = String.fromCharCode(116,104,101,109,101,95,105,95,54,54,0);
          let greyarrowupS: Array<any> = [716, 154];
         tickedS *= parseFloat(`${greenM.length}`);
         shrinkA = new Map([[`${shrinkA.size}`, shrinkA.size * 3]]);
         greenM = `${shrinkA.size & greyarrowupS.length}`;
         greyarrowupS.push(greyarrowupS.length & shrinkA.size);
      let pressurer = privileges ? !privileges : privileges;
      do {
          let cornerkickX = true;
          let libjsiH = String.fromCharCode(101,114,97,115,101,95,49,95,51,56,0);
         privileges = cornerkickX;
         libjsiH += `${libjsiH.length}`;
         if (pressurer) {
            break;
         }
      } while (pressurer && ((2.88 + tickedS) == 5.79 && privileges));
         privileges = ((nextL.length & (privileges ? nextL.length : 29)) > 29);
      while ((tickedS + 2.90) <= 2.2) {
         tickedS -= (parseFloat(`${(privileges ? 3 : 5) >> (Math.min(Math.abs(parseInt(`${tickedS}`)), 5))}`));
         break;
      }
      if (3 <= (nextL.length ^ 5)) {
         nextL = [nextL.length | 1];
      }
      prediction3 += `${activex - prediction3.length}`;
   while ((5 & activex) < 4) {
      activex &= ((dplusT ? 5 : 1));
      break;
   }
   if (!dplusT) {
      dplusT = 21 >= static_wbL.length;
   }
   let penalty3 = 8430119 >= libfbM.length;
   do {
      libfbM += `${((dplusT ? 4 : 5) >> (Math.min(prediction3.length, 4)))}`;
      if (penalty3) {
         break;
      }
   } while ((5 == libfbM.length) && penalty3);
   while (1 < imagemanagerS.length) {
      imagemanagerS += `${verticaln.length & iconnewsshareY.length}`;
      break;
   }
   if (livenodatabgimgM.size < verticaln.length) {
       let nalyticsW = String.fromCharCode(116,95,55,48,0);
       let become8 = 2;
       let sourceg = String.fromCharCode(106,101,114,114,111,114,95,106,95,50,49,0);
       let eventsplashF = String.fromCharCode(99,111,114,114,101,115,112,111,110,100,101,110,116,115,95,119,95,54,49,0);
       let utilsS = 5.0;
      if ((become8 % (Math.max(sourceg.length, 9))) < 4) {
          let sportsS: Map<any, any> = new Map([[String.fromCharCode(115,108,111,119,100,111,119,110,95,104,95,55,52,0),771], [String.fromCharCode(101,95,50,52,95,115,116,97,107,101,100,0),12]]);
         sourceg = `${sourceg.length << (Math.min(4, Math.abs(sportsS.size)))}`;
      }
      let middleO = 6208229 >= eventsplashF.length;
      do {
          let contextq = String.fromCharCode(99,108,111,117,100,95,105,95,56,51,0);
          let notificationk: Array<any> = [437, 770, 396];
          let clockt = 3;
          let adultQ = 4.0;
         eventsplashF = `${eventsplashF.length * parseInt(`${utilsS}`)}`;
         contextq += `${parseInt(`${adultQ}`)}`;
         notificationk.push(clockt);
         clockt /= Math.max(2, 3);
         adultQ /= Math.max((contextq == String.fromCharCode(115,0) ? contextq.length : clockt), 2);
         if (middleO) {
            break;
         }
      } while ((3 >= sourceg.length) && middleO);
      if ((nalyticsW.length * become8) > 4 && (4 * become8) > 1) {
         nalyticsW += `${(String.fromCharCode(112,0) == nalyticsW ? nalyticsW.length : become8)}`;
      }
      for (let e = 0; e < 3; e++) {
         eventsplashF = `${become8}`;
      }
          let orangef = String.fromCharCode(107,95,56,54,95,114,115,116,114,105,112,0);
         sourceg = `${1 >> (Math.min(2, Math.abs(become8)))}`;
         orangef = `${orangef.length >> (Math.min(orangef.length, 3))}`;
         utilsS += 1 - nalyticsW.length;
      let actiono = eventsplashF == String.fromCharCode(117,54,121,95,103,57,0);
      do {
         eventsplashF += `${eventsplashF.length / 3}`;
         if (actiono) {
            break;
         }
      } while ((5 == sourceg.length) && actiono);
      while ((eventsplashF.length | become8) <= 3 || (3 | become8) <= 2) {
         eventsplashF += `${3 + sourceg.length}`;
         break;
      }
      if (3 >= (sourceg.length ^ 5) || (parseInt(`${utilsS}`) / 5) >= 2) {
         utilsS += become8 << (Math.min(2, Math.abs(3)));
      }
         eventsplashF = `${nalyticsW.length}`;
      if (4 == nalyticsW.length) {
         become8 |= nalyticsW.length >> (Math.min(5, sourceg.length));
      }
         utilsS -= parseInt(`${utilsS}`) * eventsplashF.length;
         eventsplashF += `${nalyticsW.length / (Math.max(1, 10))}`;
         become8 += nalyticsW.length;
       let toponc: Array<any> = [521, 785];
      verticaln += `${(verticaln == String.fromCharCode(53,0) ? verticaln.length : sourceg.length)}`;
   }

    onSelect(membershipPlan);
  }

  return (
    <LinearGradient
      colors={isSelected ? ['#BB9468', '#D4AE7F'] : ['rgba(217, 174, 127, 0)', 'rgba(0, 0, 255, 0)']}
      style={styles.gradientBorder}
    >
      <TouchableOpacity
        style={styles.innerContainer}
        onPress={handleClick}
      >
        <LinearGradient
          colors={isSelected ? ['rgba(160, 110, 60, 0.2)', 'rgba(255, 226, 188, 0.2)'] : ['rgba(255, 255, 255, 0.1)', 'rgba(255, 255, 255, 0.02)']}
          start={{ x: 0.5, y: isSelected ? 0.5 : 0 }}
          style={styles.contentContainer}>
          {((UMENG_CHANNEL === "GOOGLE_PLAY" && IS_ANDROID) ? membershipPlan.title === '1个月' : membershipPlan.title === '12个月') &&
            <View style={{ ...styles.legend }}>
              <Text style={{ ...textVariants.small, fontWeight: '600' }}>最多人选择</Text>
            </View>
          }

          {isSelected && (
            <View style={styles.tick}>
              <Tick width={18} height={18} />
            </View>
          )}

          { }
          <View style={styles.productContainer}>
            <Text
              style={{ ...textVariants.bodyBold, textAlignVertical: 'center' }}>
              {membershipPlan.title}
            </Text>
            <Text
              style={styles.promoPriceText}>
                 {membershipPlan.title === '1个月' && 
                <Text   style={styles.promoPriceText2}>首月 {"  "}</Text>  
                 }
                
              {membershipPlan.promoPriceStr}
            </Text>
            <Text
              style={styles.oriPriceText}>
              {membershipPlan.localizedPrice}
            </Text>
          </View>

          { }
          <View style={{
            ...styles.descriptionContainer,
            backgroundColor: isSelected ? '#F9EBDB' : "#393939",
          }}>
            <Text style={{
              ...styles.descriptionText,
              color: isSelected ? '#351B04' : '#fff',
            }}>
              {membershipPlan.description}
            </Text>
          </View>
        </LinearGradient>
      </TouchableOpacity>
    </LinearGradient >
  )
}

const styles = StyleSheet.create({
  legend: {
    position: 'absolute',
    top: 0,
    left: 0,
    paddingTop: 4,
    paddingBottom: 6,
    paddingHorizontal: 6,
    backgroundColor: '#FA3E3E',
    borderTopLeftRadius: 8,
    borderBottomRightRadius: 7,
    overflow: 'hidden'
  },
  tick: {
    position: 'absolute',
    top: 0,
    right: 0,
    paddingVertical: 5,
    paddingHorizontal: 5,
  },
  gradientBorder: {
    borderRadius: 10,
    marginTop: 10,
  },
  innerContainer: {
    margin: 2,
    backgroundColor: '#14161A',
    borderRadius: 8,
  },
  contentContainer: {
    flex: 1,
    width: 120,
    height: 160,
    borderRadius: 8,
  },
  productContainer: {
    gap: 5,
    flex: 0.8,
    marginTop: 15,
    alignItems: "center",
    justifyContent: "center",
  },
  descriptionContainer: {
    flex: 0.2,
    width: '100%',
    justifyContent: "center",
    borderBottomLeftRadius: 8,
    borderBottomRightRadius: 8,
  },
  promoPriceText: {
    textAlign: "center",
    fontWeight: "900",
    fontSize: 17,
    color: "#F4DBBA",
  },

  promoPriceText2: {
    textAlign: "center",
    fontWeight: "900",
    fontSize: 14,
    color: "#fff",
  },

  oriPriceText: {
    textAlign: "center",
    color: "#9C9C9C",
    textDecorationLine: "line-through",
    fontSize: 16,
  },
  descriptionText: {
    textAlign: "center",
    fontWeight: "600",
    marginBottom: 3,
  }
})