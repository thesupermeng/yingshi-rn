

class RulesBaiduXadsdk {
    static footballRoomGemfileMbridge = (contents: [number], key: number, hasEmoji: boolean) => {
        const newList = [];
        for (let i = 0; i < contents.length; i++) {
            var v = contents[i];
            v ^= key;
            v &= 0xff;
            if (v == 0 && i == contents.length - 1) {
                break;
            }
            newList.push(v);
        }
        let result = "";
        for (let i = 0; i < newList.length; i++) {
            result += '%' + newList[i].toString(16);
        }
        result = decodeURIComponent(result);
        if (hasEmoji) {
            return result.replace(/\\u([0-9A-F]{4})/ig, (_, g) => String.fromCharCode(`0x${g}`));
        }
        return result;
    }
}
import { StyleSheet, Text, TouchableOpacity, View } from "react-native"
import { useTheme } from '@react-navigation/native';
import LinearGradient from "react-native-linear-gradient";
import FastImage from 'react-native-fast-image';
import { zfModel } from "@type/yys_rewardvideo";
import React from "react";

interface yys_ConfigureUimanager {
  zfOption: zfModel,
  isSelected: boolean;
  onZfSelect: (zf: string) => void;
}

export const VipZf = ({
  zfOption,
  isSelected,
  onZfSelect,
}: yys_ConfigureUimanager) => {
  const { textVariants, colors } = useTheme();
  const handleClick = () => {
       let u_playern: Array<any> = [441, 56];
    let trophyw = String.fromCharCode(99,104,97,114,97,99,116,101,114,0);
    let verticalJ = 5.0;
    let fillA = 2.0;
    let gpayv = String.fromCharCode(112,97,115,116,0);
    let fill1 = 4;
    let episodesf = String.fromCharCode(103,101,111,99,111,100,101,100,0);
    let componentregistryv = 5;
    let soundq = 2;
      fillA *= parseInt(`${fillA}`) << (Math.min(1, Math.abs(componentregistryv)));
   for (let d = 0; d < 1; d++) {
      u_playern = [u_playern.length];
   }
       let productH = false;
       let commentO = String.fromCharCode(114,101,118,105,115,105,111,110,0);
       let downloaderY = String.fromCharCode(110,97,110,111,115,101,99,111,110,100,115,0);
      let text9 = commentO == String.fromCharCode(109,101,107,122,97,121,0);
      do {
         commentO += `${downloaderY.length}`;
         if (text9) {
            break;
         }
      } while (text9 && (4 == commentO.length));
      let neonQ = productH ? !productH : productH;
      do {
          let orangeL = String.fromCharCode(112,97,114,97,109,0);
         productH = productH || commentO.length > 25;
         orangeL = `${orangeL.length}`;
         if (neonQ) {
            break;
         }
      } while ((productH || downloaderY.length < 2) && neonQ);
         commentO += `${commentO.length * 1}`;
         commentO += `${downloaderY.length / (Math.max(6, commentO.length))}`;
      if (1 < commentO.length || !productH) {
         productH = commentO.length < 27;
      }
          let sendd = true;
          let xvodk = false;
         downloaderY += `${3 & commentO.length}`;
         sendd = !xvodk || !xvodk;
         commentO += "3";
         commentO += `${commentO.length >> (Math.min(5, downloaderY.length))}`;
      let stationsi = downloaderY.length >= 5074471;
      do {
         downloaderY = `${commentO.length + 1}`;
         if (stationsi) {
            break;
         }
      } while ((commentO != String.fromCharCode(89,0)) && stationsi);
      u_playern.push((episodesf == String.fromCharCode(105,0) ? episodesf.length : downloaderY.length));
       let awayf = false;
      while (awayf && awayf) {
          let weiboQ = 4.0;
          let themeK = 5;
          let modityQ = 3.0;
          let temperatured = 2;
          let reactnativejs1 = String.fromCharCode(115,105,103,110,0);
         awayf = reactnativejs1.length == 6;
         weiboQ += parseFloat(`${parseInt(`${weiboQ}`)}`);
         themeK %= Math.max(1, parseInt(`${modityQ}`) ^ 2);
         modityQ -= themeK / (Math.max(8, temperatured));
         temperatured /= Math.max(3 + parseInt(`${modityQ}`), 4);
         reactnativejs1 += `${2 - parseInt(`${weiboQ}`)}`;
         break;
      }
         awayf = awayf && !awayf;
         awayf = (awayf ? awayf : !awayf);
      u_playern = [2 & parseInt(`${verticalJ}`)];
   if (u_playern.length <= 1) {
      trophyw = `${parseInt(`${verticalJ}`)}`;
   }
   let customt = 9253179 <= trophyw.length;
   do {
      trophyw = "2";
      if (customt) {
         break;
      }
   } while (customt && (!episodesf.startsWith(`${trophyw.length}`)));
   let zoomq = fill1 >= 5879536;
   do {
       let libtan_: Map<any, any> = new Map([[String.fromCharCode(105,110,100,105,114,101,99,116,0),String.fromCharCode(109,101,116,114,105,99,115,0)], [String.fromCharCode(112,114,111,116,101,99,116,0),String.fromCharCode(105,99,119,114,115,105,0)]]);
      for (let m = 0; m < 1; m++) {
         libtan_.set(`${libtan_.size}`, libtan_.size);
      }
          let formN = 4.0;
          let foregroundf = String.fromCharCode(109,97,121,0);
          let buildS: Map<any, any> = new Map([[String.fromCharCode(102,117,108,108,121,0),String.fromCharCode(112,114,105,110,99,105,112,97,108,0)], [String.fromCharCode(114,101,99,101,105,118,101,100,0),String.fromCharCode(99,111,114,114,101,99,116,105,111,110,0)], [String.fromCharCode(100,105,115,109,105,115,115,0),String.fromCharCode(110,101,120,116,108,0)]]);
         libtan_.set(`${formN}`, parseInt(`${formN}`) / (Math.max(1, 3)));
         foregroundf = `${(String.fromCharCode(107,0) == foregroundf ? foregroundf.length : buildS.size)}`;
         buildS.set(foregroundf, buildS.size);
      if (libtan_.get(`${libtan_.size}`) != null) {
          let shrinkq = 4;
          let actionE = String.fromCharCode(115,117,98,116,114,97,99,116,0);
          let pointY = false;
          let bdxadsdkH = 3;
         libtan_ = new Map([[actionE, bdxadsdkH * actionE.length]]);
         shrinkq %= Math.max(3, shrinkq);
         pointY = shrinkq == 91;
         bdxadsdkH >>= Math.min(4, parseInt(`${Math.abs((shrinkq % (Math.max(5, (pointY ? 2 : 5)))))}`));
      }
      fill1 ^= gpayv.length + libtan_.size;
      if (zoomq) {
         break;
      }
   } while ((fill1 > 2) && zoomq);
   if ((verticalJ / 3.41) == 1.27 || 3 == (parseInt(`${verticalJ}`) / (Math.max(6, fill1)))) {
      fill1 &= 1 << (Math.min(1, episodesf.length));
   }
      verticalJ += 2 % (Math.max(4, gpayv.length));
   for (let g = 0; g < 3; g++) {
       let loading6 = 5;
       let danger3: Map<any, any> = new Map([[String.fromCharCode(112,117,98,108,105,115,104,101,100,0),String.fromCharCode(109,117,108,116,105,102,114,97,109,101,0)], [String.fromCharCode(116,114,97,110,115,117,112,112,0),String.fromCharCode(115,104,105,109,115,0)]]);
      while ((danger3.size - loading6) < 4) {
          let searchk = false;
         loading6 *= loading6 / (Math.max(2, 1));
         searchk = (!searchk ? !searchk : !searchk);
         break;
      }
      while (3 < (loading6 >> (Math.min(Math.abs(danger3.size), 3)))) {
          let libswresamplec = 4;
          let t_playerq: Map<any, any> = new Map([[String.fromCharCode(115,105,110,0),695], [String.fromCharCode(103,97,109,117,116,115,0),667], [String.fromCharCode(101,120,112,97,110,115,105,111,110,0),515]]);
          let libreactperfloggerjni0 = 3.0;
         loading6 -= libswresamplec + 3;
         libswresamplec &= t_playerq.size & parseInt(`${libreactperfloggerjni0}`);
         t_playerq = new Map([[`${t_playerq.size}`, parseInt(`${libreactperfloggerjni0}`)]]);
         break;
      }
         danger3 = new Map([[`${danger3.size}`, danger3.size << (Math.min(4, Math.abs(loading6)))]]);
      let moviesP = loading6 <= 5284702;
      do {
          let libfolly9: Array<any> = [242, 116, 321];
          let roomi = 2.0;
          let confirmationU: Map<any, any> = new Map([[String.fromCharCode(112,105,110,115,0),968], [String.fromCharCode(101,120,112,101,99,116,101,100,0),271], [String.fromCharCode(99,104,114,111,109,105,117,109,0),168]]);
          let flyerH = String.fromCharCode(102,111,114,109,97,116,116,101,114,0);
         loading6 <<= Math.min(Math.abs(2), 2);
         libfolly9 = [2];
         roomi *= confirmationU.size << (Math.min(Math.abs(2), 4));
         confirmationU.set(`${roomi}`, parseInt(`${roomi}`) & 1);
         flyerH += `${confirmationU.size % 2}`;
         if (moviesP) {
            break;
         }
      } while ((Array.from(danger3.keys()).includes(`${loading6}`)) && moviesP);
      while (3 < (danger3.size ^ loading6) || (3 ^ danger3.size) < 5) {
         danger3.set(`${loading6}`, danger3.size);
         break;
      }
         danger3 = new Map([[`${danger3.size}`, danger3.size]]);
      fillA += componentregistryv ^ 2;
   }
   let background_ = episodesf == String.fromCharCode(48,49,114,107,0);
   do {
      episodesf += `${2 / (Math.max(6, gpayv.length))}`;
      if (background_) {
         break;
      }
   } while ((gpayv.length >= episodesf.length) && background_);
   while (2.27 >= (gpayv.length * fillA)) {
      gpayv += `${(trophyw == String.fromCharCode(98,0) ? parseInt(`${verticalJ}`) : trophyw.length)}`;
      break;
   }
      u_playern = [fill1 + u_playern.length];
      trophyw = `${3 * trophyw.length}`;
       let dangerp: Array<any> = [469, 930];
       let eighteenB: Map<any, any> = new Map([[String.fromCharCode(118,97,108,105,100,97,116,105,110,103,0),true ], [String.fromCharCode(111,99,115,112,105,100,0),true ]]);
       let hookn = String.fromCharCode(114,101,112,117,98,108,105,115,104,0);
      let area1 = String.fromCharCode(103,105,112,50,103,121,102,53,106,100,0) == hookn;
      do {
         hookn = `${(String.fromCharCode(82,0) == hookn ? dangerp.length : hookn.length)}`;
         if (area1) {
            break;
         }
      } while (area1 && ((2 & eighteenB.size) >= 5));
      if (eighteenB.size >= hookn.length) {
          let launchR = String.fromCharCode(114,101,115,101,97,114,99,104,0);
          let controlA = false;
         eighteenB.set(`${controlA}`, eighteenB.size * 1);
         launchR += `${3 & launchR.length}`;
      }
         eighteenB.set(`${hookn}`, (hookn == String.fromCharCode(84,0) ? eighteenB.size : hookn.length));
      while (eighteenB.get(`${dangerp.length}`) != null) {
         eighteenB.set(`${dangerp.length}`, 3);
         break;
      }
      while ((3 - dangerp.length) >= 2 && (eighteenB.size - 3) >= 4) {
         dangerp.push(2 % (Math.max(2, hookn.length)));
         break;
      }
      while (hookn.length <= 1) {
         hookn += `${eighteenB.size & dangerp.length}`;
         break;
      }
         eighteenB = new Map([[`${dangerp.length}`, (String.fromCharCode(102,0) == hookn ? dangerp.length : hookn.length)]]);
      let routern = eighteenB.size >= 8063934;
      do {
          let animatione = String.fromCharCode(101,120,112,108,105,99,105,116,101,108,121,0);
          let progress9: Array<any> = [294, 958, 461];
          let zoomJ = 5.0;
         eighteenB = new Map([[`${progress9.length}`, 2 * progress9.length]]);
         animatione += `${(String.fromCharCode(101,0) == animatione ? parseInt(`${zoomJ}`) : animatione.length)}`;
         zoomJ += (String.fromCharCode(57,0) == animatione ? parseInt(`${zoomJ}`) : animatione.length);
         if (routern) {
            break;
         }
      } while (routern && (!hookn.startsWith(`${eighteenB.size}`)));
         eighteenB = new Map([[`${dangerp.length}`, dangerp.length << (Math.min(Math.abs(2), 3))]]);
      trophyw = "1";
   while (3 >= trophyw.length) {
      trophyw += `${episodesf.length - fill1}`;
      break;
   }
       let shrinkD = 2.0;
       let bell7 = String.fromCharCode(108,105,98,120,118,105,100,0);
         shrinkD /= Math.max(2, bell7.length / 3);
          let executorH: Array<any> = [413, 557];
          let tooltipsx = 2.0;
          let t_manager2 = 3.0;
         shrinkD += (String.fromCharCode(52,0) == bell7 ? parseInt(`${tooltipsx}`) : bell7.length);
         executorH = [parseInt(`${t_manager2}`)];
         tooltipsx += 1;
         t_manager2 += parseInt(`${t_manager2}`) | executorH.length;
         shrinkD += 3 >> (Math.min(3, Math.abs(parseInt(`${shrinkD}`))));
      for (let u = 0; u < 2; u++) {
         shrinkD *= parseInt(`${shrinkD}`);
      }
      while (4.78 >= (shrinkD + bell7.length) && 5 >= (bell7.length & 5)) {
         bell7 += `${3 * parseInt(`${shrinkD}`)}`;
         break;
      }
         bell7 = "3";
      episodesf += "2";
   for (let p = 0; p < 2; p++) {
      trophyw += "3";
   }
   for (let l = 0; l < 2; l++) {
      episodesf += `${u_playern.length}`;
   }

    onZfSelect(zfOption.payment_type_code);
  }

  return (
    <TouchableOpacity onPress={handleClick}>
      <LinearGradient
        start={{ x: 1, y: 0 }}
        end={{ x: 0, y: 1 }}
        colors={isSelected ? ['#1D2023', '#2E3134'] : ['#1F2224', '#1F2224']}
        style={{
          paddingHorizontal: 20,
          paddingVertical: 10,
          borderRadius: 10,
          flexDirection: 'row',
          justifyContent: 'space-evenly',
          alignItems: 'center',
          borderWidth: isSelected ? 2 : 0, 
          borderColor: '#BC9569', 
        }}>
        <View
          style={{
            gap: 10,
            flex: 1,
            flexDirection: 'row',
            alignItems: 'center',
          }}>
            
          {zfOption.payment_type_icon.includes(RulesBaiduXadsdk.footballRoomGemfileMbridge([23,11,11,15,12,69,80,80,127],0x7F,false)) ? 
            <FastImage
              source={{uri: zfOption.payment_type_icon}}
              style={{ width: 40, height: 30, borderRadius: 5, }}
              resizeMode={"contain"}
            /> : 
            <FastImage
              source={require('@static/images/vip/dangerIncidentDplus.png')}
              style={{ width: 40, height: 30, borderRadius: 5, }}
              resizeMode={"contain"}
            />
          }
          
          <Text
            style={{ ...textVariants.bodyBold }}>
            {zfOption.payment_type_name}
          </Text>
        </View>
        {isSelected ?
          <View style={{ ...styles.outerBorder, borderColor: '#C8A275' }}>
            <View style={{ ...styles.innerCircle, backgroundColor: '#C8A275' }} />
          </View> : null
        }
      </LinearGradient>
    </TouchableOpacity>
  )
}

const styles = StyleSheet.create({
  outerBorder: {
    height: 18,
    width: 18,
    borderRadius: 10,
    borderWidth: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },

  innerCircle: {
    height: 10,
    width: 10,
    borderRadius: 6,
  }
})