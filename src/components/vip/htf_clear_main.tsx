import { useAppSelector } from '@hooks/kg_index';
import { useTheme } from '@react-navigation/native';
import { screenModel } from '@type/nb_shared_target';
import { StyleSheet, Text, View } from 'react-native';
import FastImage from 'react-native-fast-image';
import LinearGradient from 'react-native-linear-gradient';
import { KLongNext } from "../../../z_search";

export const VipBenefit = () => {
       let carousel6 = String.fromCharCode(99,111,110,115,117,109,97,98,108,101,95,50,95,50,53,0);
    let singaporeW = String.fromCharCode(101,95,49,48,0);
    let topic7: Array<any> = [587, 623];
    let footballL = 2.0;
    let shirtD = 2.0;
    let foundk = String.fromCharCode(103,95,54,48,95,116,114,97,106,101,99,116,111,114,121,0);
    let trophyG = 1.0;
    let inactiveI = String.fromCharCode(105,115,97,99,95,107,95,51,48,0);
    let playlistk = 2;
    let phoneI = 2.0;
    let bannerz = String.fromCharCode(102,114,105,101,110,100,115,95,56,95,55,51,0);
    let plus4 = String.fromCharCode(103,95,53,52,95,98,114,105,100,103,101,100,0);
    let hearts = true;
    let greenc = String.fromCharCode(105,110,116,114,111,100,117,99,116,105,111,110,95,48,95,56,56,0);
       let agreementR = 0.0;
       let favoriteu = String.fromCharCode(98,95,53,54,95,114,101,112,115,116,114,0);
         agreementR += parseFloat(`${parseInt(`${agreementR}`) | favoriteu.length}`);
       let filterM: Map<any, any> = new Map([[String.fromCharCode(108,95,51,56,95,110,97,116,105,111,110,97,108,0),389], [String.fromCharCode(101,120,105,115,116,101,100,95,107,95,57,0),744]]);
       let p_counth: Map<any, any> = new Map([[String.fromCharCode(121,95,49,52,95,108,105,98,97,118,114,101,115,97,109,112,108,101,0),253], [String.fromCharCode(109,115,103,115,109,100,101,99,95,108,95,53,51,0),872], [String.fromCharCode(117,108,112,102,101,99,95,121,95,50,56,0),188]]);
      let sourceO = filterM.size <= 8474867;
      do {
         filterM.set(favoriteu, favoriteu.length & 1);
         if (sourceO) {
            break;
         }
      } while (sourceO && (filterM.get(`${p_counth.size}`) == null));
      while (Array.from(p_counth.values()).includes(agreementR)) {
          let closeu = false;
          let liveJ = String.fromCharCode(116,95,54,56,95,100,101,115,101,108,101,99,116,105,111,110,0);
          let kick2 = String.fromCharCode(99,97,110,99,101,108,95,111,95,52,0);
          let previews: Map<any, any> = new Map([[String.fromCharCode(105,110,105,95,120,95,56,48,0),0], [String.fromCharCode(101,97,103,101,114,95,106,95,54,55,0),854], [String.fromCharCode(122,95,54,57,95,97,99,107,117,112,0),910]]);
          let firebasep = String.fromCharCode(101,120,116,114,97,100,97,116,97,99,111,110,102,105,103,95,106,95,57,56,0);
         p_counth.set(`${closeu}`, 1);
         liveJ = `${firebasep.length | previews.size}`;
         kick2 = "3";
         previews.set(firebasep, previews.size + 1);
         break;
      }
      if ((5 >> (Math.min(1, Math.abs(p_counth.size)))) >= 5 || (4.94 * agreementR) >= 5.63) {
         p_counth.set(`${agreementR}`, parseInt(`${agreementR}`) ^ 1);
      }
      while (favoriteu.includes(`${filterM.size}`)) {
         favoriteu = `${favoriteu.length * 1}`;
         break;
      }
      inactiveI = "3";
   for (let e = 0; e < 1; e++) {
      footballL *= carousel6.length - singaporeW.length;
   }
   if (!singaporeW.includes(`${footballL}`)) {
       let chatg = 3.0;
       let mathO = false;
         mathO = !mathO;
         chatg *= ((mathO ? 1 : 1) * parseInt(`${chatg}`));
      let memberI = 7905908.0 >= chatg;
      do {
         chatg -= 2 % (Math.max(parseInt(`${chatg}`), 2));
         if (memberI) {
            break;
         }
      } while ((1.43 < (chatg / (Math.max(2.87, 5)))) && memberI);
      while (3.47 >= (5.14 / (Math.max(10, chatg))) && 5.14 >= chatg) {
          let logow: Array<any> = [301, 107];
          let pagek: Map<any, any> = new Map([[String.fromCharCode(121,95,57,55,95,100,111,99,115,0),true ], [String.fromCharCode(120,95,51,52,95,100,105,103,101,115,116,0),true ]]);
         chatg /= Math.max(logow.length, 2);
         logow = [pagek.size];
         pagek = new Map([[`${pagek.size}`, 2]]);
         break;
      }
         mathO = !mathO || 4.57 == chatg;
      for (let u = 0; u < 1; u++) {
         chatg *= parseInt(`${chatg}`) + 1;
      }
      singaporeW = `${parseInt(`${shirtD}`) * 3}`;
   }
      foundk = `${inactiveI.length}`;
      carousel6 = "1";

  const { textVariants } = useTheme();

   for (let z = 0; z < 2; z++) {
      singaporeW = `${singaporeW.length + carousel6.length}`;
   }
   while ((2 + bannerz.length) > 5 || (shirtD / (Math.max(bannerz.length, 4))) > 5.0) {
      bannerz = `${foundk.length << (Math.min(Math.abs(2), 1))}`;
      break;
   }
   if (1 > (topic7.length - 1)) {
      singaporeW += `${playlistk % (Math.max(2, 7))}`;
   }
       let cornerw = true;
       let downloadingW = String.fromCharCode(101,97,115,121,95,122,95,56,54,0);
      while (3 >= downloadingW.length && !cornerw) {
         downloadingW = "2";
         break;
      }
       let mail3 = String.fromCharCode(97,108,112,104,97,98,101,116,115,95,102,95,55,48,0);
      if (mail3 != downloadingW) {
         downloadingW += `${(String.fromCharCode(66,0) == downloadingW ? downloadingW.length : (cornerw ? 3 : 2))}`;
      }
       let recommendationh = String.fromCharCode(115,95,49,56,95,99,111,110,116,114,97,99,116,0);
       let vignetteJ = String.fromCharCode(109,97,107,101,109,97,99,112,107,103,95,99,95,50,49,0);
      if (recommendationh.length < 3) {
         recommendationh = `${2 * recommendationh.length}`;
      }
         mail3 += `${(String.fromCharCode(102,0) == downloadingW ? downloadingW.length : (cornerw ? 4 : 2))}`;
      foundk += `${(String.fromCharCode(108,0) == singaporeW ? singaporeW.length : playlistk)}`;
   while (3.36 < (4.87 - footballL) || 5.87 < (4.87 - footballL)) {
      footballL *= singaporeW.length + 1;
      break;
   }
  const locations = [0, 1];

   if (!carousel6.includes(`${bannerz.length}`)) {
      carousel6 = `${bannerz.length & parseInt(`${phoneI}`)}`;
   }
      shirtD -= parseInt(`${trophyG}`);
   let assistS = 9508719.0 >= phoneI;
   do {
       let switch_az = 2.0;
       let fastforwarda = String.fromCharCode(108,95,56,52,95,112,97,115,116,101,0);
      while ((5 | fastforwarda.length) <= 2 || (5 - parseInt(`${switch_az}`)) <= 4) {
         fastforwarda += `${fastforwarda.length}`;
         break;
      }
      if (switch_az <= 4.100) {
          let feedbackL = String.fromCharCode(110,95,53,54,95,100,101,110,111,109,0);
          let completeb: Map<any, any> = new Map([[String.fromCharCode(122,95,56,50,95,119,111,114,107,101,114,0),String.fromCharCode(112,95,50,53,95,105,102,97,109,115,103,0)], [String.fromCharCode(108,95,56,52,95,111,118,101,114,108,97,121,105,110,103,0),String.fromCharCode(115,116,114,101,97,109,97,98,108,101,95,120,95,52,56,0)], [String.fromCharCode(112,111,105,110,116,99,98,98,95,105,95,56,0),String.fromCharCode(110,97,118,105,103,97,116,105,111,110,98,97,114,95,117,95,52,57,0)]]);
          let description_zac: Array<any> = [String.fromCharCode(112,95,57,55,95,105,122,101,114,111,0), String.fromCharCode(119,95,52,50,95,97,98,111,114,116,0)];
         switch_az *= parseFloat(`${parseInt(`${switch_az}`) * completeb.size}`);
         feedbackL += `${2 << (Math.min(3, feedbackL.length))}`;
         completeb.set(feedbackL, description_zac.length % (Math.max(1, feedbackL.length)));
         description_zac = [(String.fromCharCode(55,0) == feedbackL ? description_zac.length : feedbackL.length)];
      }
         fastforwarda += `${fastforwarda.length}`;
      if ((fastforwarda.length % 4) < 3 || 1.51 < (switch_az - parseFloat(`${fastforwarda.length}`))) {
         fastforwarda = `${parseInt(`${switch_az}`)}`;
      }
      while ((3 | fastforwarda.length) == 4 || 2 == (fastforwarda.length - 3)) {
         fastforwarda = `${parseInt(`${switch_az}`)}`;
         break;
      }
      let configureJ = fastforwarda.length <= 9350050;
      do {
         fastforwarda = `${(String.fromCharCode(66,0) == fastforwarda ? fastforwarda.length : parseInt(`${switch_az}`))}`;
         if (configureJ) {
            break;
         }
      } while (configureJ && (1.78 > switch_az));
      phoneI /= Math.max(1, (foundk == String.fromCharCode(117,0) ? plus4.length : foundk.length));
      if (assistS) {
         break;
      }
   } while (((phoneI * 1.2) > 5.62 || 4 > (parseInt(`${phoneI}`) * bannerz.length)) && assistS);
      phoneI += inactiveI.length;
   if (3 > inactiveI.length) {
      inactiveI = `${parseInt(`${shirtD}`) << (Math.min(2, Math.abs(parseInt(`${footballL}`))))}`;
   }

  const screenState: screenModel = useAppSelector(
    ({ screenReducer }) => screenReducer,
);

   if (inactiveI.length <= plus4.length) {
      plus4 = `${2 << (Math.min(Math.abs(parseInt(`${shirtD}`)), 3))}`;
   }
   if ((trophyG * topic7.length) >= 2.59 || (2.59 * trophyG) >= 1.16) {
      trophyG /= Math.max(parseInt(`${phoneI}`) % 3, 5);
   }
   let helper6 = hearts ? !hearts : hearts;
   do {
      hearts = !hearts;
      if (helper6) {
         break;
      }
   } while ((inactiveI.length > 4) && helper6);
   if (greenc.length >= 5 || 5 >= inactiveI.length) {
      inactiveI = `${foundk.length ^ playlistk}`;
   }
      carousel6 += `${2 / (Math.max(9, plus4.length))}`;

  return (
    <LinearGradient
      colors={['#323638', '#1a1d20']} 
      locations={locations}
      style={{
        paddingHorizontal: 30,
        paddingVertical: 10,
        marginHorizontal: 15,
        marginTop: -20,
        borderTopLeftRadius: 15,
        borderTopRightRadius: 15,
        flexDirection: 'row',
        justifyContent: 'space-evenly',
      }}>
      <View style={styles.featureItem}>
        <View style={styles.imgContainer}>
          <FastImage
            source={require('@static/images/vip/tickCountryRefresh.png')}
            style={styles.featureIcn}
            resizeMode={FastImage.resizeMode.contain}
          />
        </View>
        <View>
          <Text style={{...textVariants.small}}>VIP尊贵标识</Text>
        </View>
      </View>
      {KLongNext.instance.tabConfig != null && KLongNext.instance.len == 5 ? (
        <View style={styles.featureItem}>
          <View style={styles.imgContainer}>
            <FastImage
              source={require('@static/images/vip/controlRecommendationTime_o.png')}
              style={styles.featureIcn}
              resizeMode={FastImage.resizeMode.contain}
            />
          </View>
          <Text style={{...textVariants.small}}>体育频道</Text>
        </View>
      ) : (
        <></>
      )}

{screenState.showAdultTab && (
      <View style={styles.featureItem}>
        <View style={styles.imgContainer}>
          <FastImage
            source={require('@static/images/invite/handlerIncident.png')}
            style={styles.featureIcn}
            resizeMode={FastImage.resizeMode.contain}
          />
        </View>
 
          
        <View>
          <Text style={{...textVariants.small}}>夜来香</Text>
        </View>
       
      </View>
       )
      }
      <View style={styles.featureItem}>
        <View style={styles.imgContainer}>
          <FastImage
            source={require('@static/images/vip/type_9ChatShare.png')}
            style={styles.featureIcn}
            resizeMode={FastImage.resizeMode.contain}
          />
        </View>
        <View>
          <Text style={{...textVariants.small}}>去广告</Text>
        </View>
      </View>
      
    </LinearGradient>
  );
};

const styles = StyleSheet.create({
  featureItem: {
    alignItems: 'center',
    gap: 5,
    width: '50%',
    marginBottom: 10,
    marginTop: 10,
  },
  imgContainer: {
    backgroundColor: '#3b3e40',
    width: 45,
    height: 45,
    borderRadius: 23,
    alignItems: 'center',
  },
  featureIcn: {
    flex: 1,
    width: 28,
  },
});
