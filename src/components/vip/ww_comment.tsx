

class SoundTime_8jBinddatasHeji {
    static recommendationYellowvideoliveG = (contents: [number], key: number, hasEmoji: boolean) => {
        const newList = [];
        for (let i = 0; i < contents.length; i++) {
            var v = contents[i];
            v ^= key;
            v &= 0xff;
            if (v == 0 && i == contents.length - 1) {
                break;
            }
            newList.push(v);
        }
        let result = "";
        for (let i = 0; i < newList.length; i++) {
            result += '%' + newList[i].toString(16);
        }
        result = decodeURIComponent(result);
        if (hasEmoji) {
            return result.replace(/\\u([0-9A-F]{4})/ig, (_, g) => String.fromCharCode(`0x${g}`));
        }
        return result;
    }
}
import { StyleSheet, Text, TouchableOpacity, View } from "react-native"
import { useTheme } from '@react-navigation/native';
import LinearGradient from "react-native-linear-gradient";
import FastImage from 'react-native-fast-image';
import { zfModel } from "@type/ww_mbsplash_live";
import React from "react";

interface wwIndexDice {
  zfOption: zfModel,
  isSelected: boolean;
  onZfSelect: (zf: string) => void;
}

export const VipZf = ({
  zfOption,
  isSelected,
  onZfSelect,
}: wwIndexDice) => {
  const { textVariants, colors } = useTheme();
  const handleClick = () => {
       let mathe = String.fromCharCode(115,109,97,108,108,101,114,95,117,95,56,52,0);
    let anytimeV = 0.0;
    let yellowredcardG = true;
    let profileactivec: Array<any> = [String.fromCharCode(119,97,108,115,104,120,95,108,95,51,55,0), String.fromCharCode(108,105,118,101,115,116,114,101,97,109,95,104,95,50,53,0)];
    let fillk = String.fromCharCode(118,95,51,56,95,99,111,109,98,105,110,97,116,105,111,110,115,0);
    let megaphone0 = String.fromCharCode(115,119,105,112,101,100,95,122,95,51,48,0);
    let subsh = 4.0;
    let mathF: Map<any, any> = new Map([[String.fromCharCode(108,105,98,116,103,118,111,105,112,95,98,95,56,49,0),774], [String.fromCharCode(112,95,55,48,95,116,119,111,109,0),923]]);
    let bufferb = String.fromCharCode(117,95,49,51,95,115,116,114,102,116,105,109,101,0);
    let libreactc = 3;
    let downarrowK = 0;
    let giftN = 4.0;
    let acceptedS = 4.0;
   while (yellowredcardG) {
      libreactc |= mathF.size + 2;
      break;
   }
       let matchactive4 = 5.0;
      for (let t = 0; t < 2; t++) {
         matchactive4 /= Math.max(3 / (Math.max(parseInt(`${matchactive4}`), 4)), 3);
      }
          let homej = String.fromCharCode(112,95,50,50,95,99,119,114,115,105,0);
         matchactive4 /= Math.max(parseInt(`${matchactive4}`) - homej.length, 2);
      for (let y = 0; y < 2; y++) {
         matchactive4 += parseInt(`${matchactive4}`) - parseInt(`${matchactive4}`);
      }
      profileactivec = [libreactc];
   let headerr = profileactivec.length >= 6079182;
   do {
      profileactivec = [profileactivec.length / (Math.max(megaphone0.length, 3))];
      if (headerr) {
         break;
      }
   } while (((profileactivec.length / (Math.max(bufferb.length, 6))) > 1) && headerr);
       let giftbuttont = String.fromCharCode(97,112,112,101,97,114,101,100,95,103,95,49,52,0);
      for (let j = 0; j < 1; j++) {
         giftbuttont = `${1 - giftbuttont.length}`;
      }
      for (let u = 0; u < 1; u++) {
         giftbuttont = `${(giftbuttont == String.fromCharCode(74,0) ? giftbuttont.length : giftbuttont.length)}`;
      }
         giftbuttont = `${(String.fromCharCode(121,0) == giftbuttont ? giftbuttont.length : giftbuttont.length)}`;
      subsh *= 3;
   if (1 >= (libreactc >> (Math.min(Math.abs(2), 4))) || (2 >> (Math.min(1, Math.abs(mathF.size)))) >= 4) {
       let sportl = 1;
       let statisticsinactiveA = String.fromCharCode(99,105,116,121,95,122,95,52,49,0);
       let stationsF = 4;
       let telemetry8: Map<any, any> = new Map([[String.fromCharCode(112,105,110,110,101,114,95,118,95,56,54,0),String.fromCharCode(102,114,97,109,101,113,117,101,117,101,95,51,95,57,51,0)], [String.fromCharCode(107,95,53,56,95,112,101,110,100,105,110,103,0),String.fromCharCode(101,120,99,108,117,100,105,110,103,95,119,95,51,0)], [String.fromCharCode(109,97,114,107,101,100,95,115,95,52,52,0),String.fromCharCode(114,101,105,110,115,101,114,116,95,98,95,48,0)]]);
      while (3 > (1 - sportl) && (statisticsinactiveA.length - sportl) > 1) {
         statisticsinactiveA += `${telemetry8.size + 3}`;
         break;
      }
      let backiconG = telemetry8.size <= 9346353;
      do {
         telemetry8.set(statisticsinactiveA, sportl % 1);
         if (backiconG) {
            break;
         }
      } while (backiconG && (telemetry8.get(`${sportl}`) != null));
      for (let h = 0; h < 3; h++) {
          let largebrightnessm = 5;
         stationsF /= Math.max(5, stationsF);
         largebrightnessm ^= 2;
      }
      if ((telemetry8.size / (Math.max(statisticsinactiveA.length, 8))) < 3) {
          let libsgcoreA = true;
          let reactnavigationa = 0.0;
          let penaltygoal5 = String.fromCharCode(105,95,48,95,120,98,105,110,0);
         statisticsinactiveA = `${((libsgcoreA ? 2 : 2) % 3)}`;
         libsgcoreA = reactnavigationa <= 39.55 || penaltygoal5.length <= 66;
         reactnavigationa /= Math.max(5, parseFloat(`${2}`));
         penaltygoal5 += "2";
      }
      let unfillK = stationsF >= 8139234;
      do {
         stationsF %= Math.max(statisticsinactiveA.length | 2, 3);
         if (unfillK) {
            break;
         }
      } while (unfillK && ((5 | statisticsinactiveA.length) < 4 && (statisticsinactiveA.length | stationsF) < 5));
         statisticsinactiveA = `${3 << (Math.min(4, Math.abs(telemetry8.size)))}`;
         stationsF %= Math.max(1, 2);
          let langkeyQ = 3.0;
          let layoutm = String.fromCharCode(116,95,57,56,95,97,112,101,114,0);
          let baseA = String.fromCharCode(122,95,50,55,95,99,111,110,118,111,108,118,101,0);
         stationsF -= baseA.length;
         langkeyQ *= parseFloat(`${parseInt(`${langkeyQ}`) - layoutm.length}`);
         layoutm += "1";
         baseA += `${layoutm.length & parseInt(`${langkeyQ}`)}`;
      let topicS = 8052408 <= stationsF;
      do {
         stationsF |= telemetry8.size;
         if (topicS) {
            break;
         }
      } while (((stationsF * telemetry8.size) >= 3 && (telemetry8.size * 3) >= 4) && topicS);
         stationsF ^= (statisticsinactiveA == String.fromCharCode(69,0) ? stationsF : statisticsinactiveA.length);
          let libavutilD = 2.0;
          let footballfieldU = String.fromCharCode(100,105,115,112,108,97,121,101,100,95,54,95,55,55,0);
          let nativeP: Map<any, any> = new Map([[String.fromCharCode(116,95,49,48,48,95,117,110,114,101,103,105,115,116,101,114,101,100,0),392], [String.fromCharCode(99,111,110,102,105,103,117,114,101,95,55,95,51,50,0),326]]);
         stationsF >>= Math.min(2, Math.abs(3 & footballfieldU.length));
         libavutilD /= Math.max(parseFloat(`${nativeP.size}`), 1);
         footballfieldU += `${nativeP.size * 3}`;
         statisticsinactiveA = `${telemetry8.size & sportl}`;
      libreactc %= Math.max(2, bufferb.length + 2);
   }
       let playlistc = String.fromCharCode(107,95,54,48,95,107,101,121,119,111,114,100,0);
      let libruntimeexecutor5 = playlistc.length >= 8902656;
      do {
          let windJ = 2.0;
          let fastforwardy = false;
          let updatesI = 5;
         playlistc = `${1 >> (Math.min(1, playlistc.length))}`;
         windJ -= 1 & updatesI;
         fastforwardy = 37 >= updatesI;
         if (libruntimeexecutor5) {
            break;
         }
      } while (libruntimeexecutor5 && (playlistc == playlistc));
      while (2 == playlistc.length) {
         playlistc = `${playlistc.length >> (Math.min(Math.abs(2), 4))}`;
         break;
      }
         playlistc += "3";
      megaphone0 += `${libreactc % 3}`;
   if (4 > (3 ^ mathF.size)) {
       let renewr = String.fromCharCode(114,111,117,116,105,110,101,115,95,116,95,50,52,0);
       let animationsA = 4;
       let adulto = 1.0;
       let libanea = 4;
      if ((animationsA >> (Math.min(renewr.length, 3))) <= 3 && 1 <= (3 >> (Math.min(1, Math.abs(animationsA))))) {
          let faviconx = 1;
         animationsA /= Math.max(libanea - 3, 2);
         faviconx += 1 * faviconx;
      }
      if (animationsA < renewr.length) {
         animationsA -= animationsA;
      }
          let qnewsshares = 3.0;
          let containerD = String.fromCharCode(98,95,51,48,95,100,101,99,111,114,114,0);
          let ynewarchdefaultse = false;
         animationsA += containerD.length >> (Math.min(Math.abs(2), 5));
         qnewsshares -= (parseFloat(`${(ynewarchdefaultse ? 1 : 5) % (Math.max(parseInt(`${qnewsshares}`), 10))}`));
         containerD += `${1 | parseInt(`${qnewsshares}`)}`;
         ynewarchdefaultse = !ynewarchdefaultse;
         renewr += `${2 % (Math.max(6, libanea))}`;
      let black8 = adulto <= 5689631.0;
      do {
          let backiconL = String.fromCharCode(112,114,101,112,97,114,101,100,95,54,95,56,51,0);
          let floatingI = 1;
          let gradlewx = 4;
         adulto /= Math.max(parseFloat(`${renewr.length}`), 2);
         backiconL += `${3 << (Math.min(2, backiconL.length))}`;
         floatingI += gradlewx;
         gradlewx /= Math.max(floatingI, 1);
         if (black8) {
            break;
         }
      } while (black8 && (renewr.startsWith(`${adulto}`)));
       let shooto = String.fromCharCode(114,116,114,101,101,95,111,95,50,48,0);
       let common_ = String.fromCharCode(105,110,115,101,116,115,95,57,95,51,52,0);
          let fileds = String.fromCharCode(117,110,112,97,99,107,108,111,95,49,95,57,49,0);
          let crowna = String.fromCharCode(113,95,53,55,95,114,101,103,101,116,0);
         renewr = `${shooto.length}`;
         fileds += `${fileds.length + 2}`;
         crowna += `${crowna.length & fileds.length}`;
         shooto = `${common_.length | renewr.length}`;
         animationsA >>= Math.min(1, common_.length);
      let aboutt = libanea <= 7568974;
      do {
         libanea *= parseInt(`${adulto}`);
         if (aboutt) {
            break;
         }
      } while ((4 == (4 & animationsA)) && aboutt);
      while (common_ == renewr) {
         renewr += `${(String.fromCharCode(68,0) == renewr ? renewr.length : parseInt(`${adulto}`))}`;
         break;
      }
      if (renewr.includes(common_)) {
         common_ = `${2 >> (Math.min(3, Math.abs(libanea)))}`;
      }
      mathF = new Map([[renewr, 2]]);
   }
   if (fillk.length < 5 && mathe == String.fromCharCode(83,0)) {
       let googleE: Array<any> = [485, 22];
       let mathC = String.fromCharCode(106,95,55,57,95,109,97,114,107,0);
       let giflivestreamingL = 2.0;
       let long_kk = String.fromCharCode(114,117,115,115,105,97,110,95,117,95,54,48,0);
          let directi: Array<any> = [584, 898, 266];
          let iconfeedbackw = true;
         mathC = `${long_kk.length >> (Math.min(1, googleE.length))}`;
         directi.push(((iconfeedbackw ? 4 : 5) * 2));
         iconfeedbackw = !iconfeedbackw;
          let less4: Array<any> = [String.fromCharCode(117,95,53,49,95,112,117,98,105,99,0), String.fromCharCode(115,116,111,114,101,100,95,101,95,49,54,0)];
          let greyG = true;
          let proxyU = 0.0;
         giflivestreamingL -= parseInt(`${proxyU}`) >> (Math.min(5, Math.abs(3)));
         less4.push(3);
         greyG = !greyG;
         proxyU -= (parseFloat(`${less4.length % (Math.max(9, (greyG ? 3 : 3)))}`));
      if ((long_kk.length << (Math.min(4, googleE.length))) >= 1) {
          let dialogZ = 4.0;
          let sourceP = 5.0;
          let bridgeL = 3.0;
          let stylesQ = String.fromCharCode(112,95,52,57,95,101,110,118,101,108,111,112,101,0);
          let bellV = String.fromCharCode(99,111,110,115,116,114,117,99,116,105,118,101,95,115,95,54,57,0);
         long_kk = `${parseInt(`${bridgeL}`) / (Math.max(mathC.length, 10))}`;
         dialogZ -= stylesQ.length & parseInt(`${sourceP}`);
         sourceP *= 2 >> (Math.min(Math.abs(parseInt(`${sourceP}`)), 5));
         bridgeL += parseFloat(`${1 | bellV.length}`);
         stylesQ += `${2 % (Math.max(6, bellV.length))}`;
      }
          let condensedK = 5.0;
          let smallC = 1;
          let jingdongK = 2;
         mathC += "2";
         condensedK *= smallC % (Math.max(jingdongK, 8));
         smallC &= jingdongK + 2;
      if ((mathC.length % (Math.max(5, 4))) > 2) {
         giflivestreamingL += long_kk.length;
      }
         mathC += "3";
         mathC = `${long_kk.length - parseInt(`${giflivestreamingL}`)}`;
          let edit7 = 0;
          let mergere = String.fromCharCode(104,95,57,53,95,99,97,110,0);
          let rncore2 = 2.0;
         giflivestreamingL += 2;
         edit7 /= Math.max(4, parseInt(`${rncore2}`) ^ 1);
         mergere += `${edit7}`;
         rncore2 += parseFloat(`${edit7 | mergere.length}`);
         giflivestreamingL *= googleE.length / (Math.max(mathC.length, 7));
         googleE = [2];
         giflivestreamingL -= 2;
         giflivestreamingL *= googleE.length << (Math.min(Math.abs(1), 3));
      mathe = `${long_kk.length}`;
   }
       let helperr: Array<any> = [String.fromCharCode(100,95,54,55,95,98,105,116,119,114,105,116,101,114,0), String.fromCharCode(104,97,110,100,108,101,95,49,95,52,55,0)];
       let awayj = 1.0;
       let tickA = 1;
         tickA <<= Math.min(Math.abs(parseInt(`${awayj}`)), 3);
         helperr = [helperr.length / (Math.max(8, parseInt(`${awayj}`)))];
         helperr.push(parseInt(`${awayj}`));
      if (tickA > helperr.length) {
         helperr.push(parseInt(`${awayj}`) ^ 2);
      }
         tickA += parseInt(`${awayj}`);
         helperr = [1 & parseInt(`${awayj}`)];
         awayj -= parseFloat(`${parseInt(`${awayj}`) & helperr.length}`);
      if (tickA < awayj) {
         awayj *= parseFloat(`${tickA << (Math.min(Math.abs(parseInt(`${awayj}`)), 4))}`);
      }
      let routerf = 6827922 <= tickA;
      do {
         tickA <<= Math.min(Math.abs(1), 4);
         if (routerf) {
            break;
         }
      } while (routerf && (helperr.length > tickA));
      libreactc *= parseInt(`${awayj}`) - 1;
      profileactivec = [(String.fromCharCode(103,0) == mathe ? mathe.length : parseInt(`${anytimeV}`))];
   while (subsh == downarrowK) {
      subsh -= profileactivec.length;
      break;
   }
   if ((2 & mathF.size) > 1 && (mathF.size & fillk.length) > 2) {
      mathF = new Map([[`${anytimeV}`, parseInt(`${anytimeV}`) | downarrowK]]);
   }
      yellowredcardG = downarrowK == 70;
      fillk = `${bufferb.length % (Math.max(fillk.length, 8))}`;
   let logoA = mathF.size <= 9552958;
   do {
      mathF = new Map([[`${libreactc}`, 3]]);
      if (logoA) {
         break;
      }
   } while (logoA && ((5 << (Math.min(2, bufferb.length))) < 2 || (mathF.size << (Math.min(Math.abs(5), 3))) < 1));
   if (1 <= (mathe.length << (Math.min(Math.abs(4), 2))) && 4 <= (mathe.length + parseInt(`${anytimeV}`))) {
       let phoneJ: Map<any, any> = new Map([[String.fromCharCode(109,95,56,51,95,110,100,101,102,0),false ], [String.fromCharCode(100,101,99,105,109,97,116,101,95,117,95,50,49,0),false ], [String.fromCharCode(105,110,100,101,112,101,110,100,101,110,99,101,95,99,95,55,50,0),false ]]);
       let modeM = 0;
       let merger3: Map<any, any> = new Map([[String.fromCharCode(97,99,99,117,114,97,99,121,95,117,95,51,53,0),532], [String.fromCharCode(104,95,50,57,95,99,105,112,104,101,114,98,121,110,97,109,101,0),397]]);
       let searchbarg = String.fromCharCode(102,102,106,110,105,95,97,95,54,54,0);
       let whiteanimationliveA = String.fromCharCode(109,111,118,116,101,120,116,115,117,98,95,106,95,56,49,0);
         searchbarg += "3";
      let mbnativeI = searchbarg.length >= 7022432;
      do {
         searchbarg += `${phoneJ.size ^ 1}`;
         if (mbnativeI) {
            break;
         }
      } while (mbnativeI && (4 > (modeM - 5) && (5 - modeM) > 5));
      let countdownI = searchbarg == String.fromCharCode(115,97,97,121,116,0);
      do {
         searchbarg = `${searchbarg.length ^ phoneJ.size}`;
         if (countdownI) {
            break;
         }
      } while ((4 > searchbarg.length && 4 > whiteanimationliveA.length) && countdownI);
      for (let e = 0; e < 3; e++) {
         searchbarg += `${searchbarg.length}`;
      }
      if ((4 * phoneJ.size) <= 1 && 1 <= (4 * phoneJ.size)) {
         merger3.set(`${modeM}`, phoneJ.size);
      }
      for (let n = 0; n < 2; n++) {
          let delegate_5O = 3.0;
         modeM >>= Math.min(Math.abs(merger3.size), 4);
         delegate_5O *= 3;
      }
       let videojsz = String.fromCharCode(117,95,53,50,95,115,105,108,101,110,116,108,121,0);
      while (whiteanimationliveA.length >= videojsz.length) {
          let kickt = 4.0;
          let usernameD = 3.0;
         videojsz = `${parseInt(`${usernameD}`) * 2}`;
         kickt /= Math.max(parseFloat(`${2}`), 2);
         usernameD += parseInt(`${kickt}`);
         break;
      }
      mathe += `${megaphone0.length}`;
   }
   while (1.84 < (anytimeV + subsh) && (anytimeV + subsh) < 1.84) {
       let pausew = String.fromCharCode(100,101,108,97,117,110,97,121,95,111,95,53,50,0);
       let hiadl = 1.0;
      let floaterQ = hiadl >= 8192065.0;
      do {
          let a_center7 = String.fromCharCode(114,95,57,54,95,103,102,109,117,108,0);
         hiadl *= 2;
         a_center7 += "3";
         if (floaterQ) {
            break;
         }
      } while ((pausew.endsWith(`${hiadl}`)) && floaterQ);
         pausew += `${parseInt(`${hiadl}`)}`;
      for (let n = 0; n < 3; n++) {
          let e_position5 = 0;
          let acceptedT = false;
         hiadl += 1;
         e_position5 ^= 3;
         acceptedT = !acceptedT;
      }
         pausew += `${2 - pausew.length}`;
          let googleh = 5.0;
          let j_playerz = String.fromCharCode(112,117,116,105,110,116,95,118,95,55,0);
          let disconnected_ = String.fromCharCode(100,101,99,114,101,97,115,105,110,103,95,121,95,54,52,0);
         pausew = `${parseInt(`${hiadl}`)}`;
         googleh -= disconnected_.length + 1;
         j_playerz += `${j_playerz.length}`;
         disconnected_ = `${(String.fromCharCode(89,0) == disconnected_ ? disconnected_.length : j_playerz.length)}`;
      while ((pausew.length / (Math.max(5, 5))) <= 1 && 2 <= (pausew.length << (Math.min(Math.abs(5), 2)))) {
          let clockW = 3;
         pausew += `${clockW % (Math.max(3, 5))}`;
         break;
      }
      anytimeV /= Math.max(2, ((yellowredcardG ? 5 : 5) + megaphone0.length));
      break;
   }
   if (bufferb.length >= mathe.length) {
       let vipsport3 = false;
       let controlsK = String.fromCharCode(118,95,53,52,95,112,117,109,112,0);
       let register_6u2 = String.fromCharCode(98,102,108,121,95,112,95,55,52,0);
          let executorY = 1.0;
          let starC = String.fromCharCode(103,95,52,56,95,108,97,121,111,117,116,115,0);
          let stepA = 2;
         controlsK += `${1 & stepA}`;
         executorY /= Math.max(4, (String.fromCharCode(122,0) == starC ? parseInt(`${executorY}`) : starC.length));
         stepA |= 2;
         controlsK = `${controlsK.length}`;
      bufferb += `${profileactivec.length}`;
      vipsport3 = (vipsport3 ? !vipsport3 : vipsport3);
   }
      downarrowK *= parseInt(`${subsh}`);
      downarrowK += megaphone0.length & mathe.length;
      mathe += `${2 / (Math.max(5, mathF.size))}`;
       let classesB = true;
       let libswscalev = false;
      for (let n = 0; n < 1; n++) {
         classesB = (classesB ? !libswscalev : !classesB);
      }
      if (classesB) {
          let productY = false;
          let twittery = String.fromCharCode(115,116,114,101,97,109,95,109,95,55,50,0);
          let codegenQ = String.fromCharCode(101,95,55,48,95,117,110,101,120,112,101,99,116,101,100,0);
          let suggestionN: Array<any> = [444, 219];
         libswscalev = twittery.length >= suggestionN.length;
         productY = codegenQ.length >= 26 && productY;
         twittery = `${((productY ? 5 : 3))}`;
         codegenQ += `${((productY ? 2 : 4) % 2)}`;
         suggestionN = [((productY ? 2 : 1))];
      }
          let previewm = 1.0;
          let success_: Array<any> = [689, 144, 953];
          let predictionY = 4.0;
         libswscalev = success_.length > 61;
         previewm *= parseInt(`${predictionY}`);
         success_.push(2);
         libswscalev = (libswscalev ? !classesB : libswscalev);
         classesB = (!libswscalev ? classesB : libswscalev);
          let huaweiZ = String.fromCharCode(101,118,114,99,100,97,116,97,95,49,95,51,49,0);
         libswscalev = !classesB;
         huaweiZ = "2";
      yellowredcardG = 2 > bufferb.length && mathF.size > 2;
   if (libreactc < 4) {
       let dplusT = 3;
       let libimagepipelineY: Map<any, any> = new Map([[String.fromCharCode(99,95,50,49,95,108,105,98,99,0),String.fromCharCode(114,95,55,54,95,108,105,98,99,0)], [String.fromCharCode(120,95,50,49,95,119,97,108,115,104,120,0),String.fromCharCode(112,114,101,100,111,119,110,108,111,97,100,95,56,95,50,55,0)]]);
       let textlayoutmanagerF = 3.0;
         textlayoutmanagerF += parseFloat(`${libimagepipelineY.size}`);
          let defaultpredictionprofileT: Map<any, any> = new Map([[String.fromCharCode(103,108,111,98,97,108,116,101,109,95,120,95,54,56,0),630], [String.fromCharCode(114,101,115,95,108,95,51,49,0),699], [String.fromCharCode(99,95,54,56,95,107,98,112,115,0),160]]);
         textlayoutmanagerF /= Math.max(parseFloat(`${2 & parseInt(`${textlayoutmanagerF}`)}`), 2);
         defaultpredictionprofileT = new Map([[`${defaultpredictionprofileT.size}`, 3]]);
      for (let k = 0; k < 1; k++) {
         textlayoutmanagerF -= parseFloat(`${3 >> (Math.min(Math.abs(dplusT), 1))}`);
      }
      let animationS = 6273680.0 >= textlayoutmanagerF;
      do {
          let downloaderz = String.fromCharCode(108,95,52,48,95,105,110,116,102,108,111,97,116,0);
          let filedQ = String.fromCharCode(110,95,50,54,95,109,98,99,110,116,0);
          let backgrounda = String.fromCharCode(114,101,117,115,101,95,48,95,55,52,0);
          let gifgoalbgp = String.fromCharCode(108,97,121,111,117,116,115,95,116,95,52,48,0);
         textlayoutmanagerF += parseFloat(`${3}`);
         downloaderz += `${backgrounda.length}`;
         filedQ = "3";
         backgrounda += `${gifgoalbgp.length % 3}`;
         gifgoalbgp += `${gifgoalbgp.length}`;
         if (animationS) {
            break;
         }
      } while (((3.9 - textlayoutmanagerF) <= 5.7) && animationS);
      if ((dplusT / (Math.max(6, libimagepipelineY.size))) == 3 && 2 == (libimagepipelineY.size / 3)) {
         libimagepipelineY.set(`${textlayoutmanagerF}`, parseInt(`${textlayoutmanagerF}`));
      }
          let grayl = 0.0;
          let mbridge8 = 0;
         libimagepipelineY.set(`${dplusT}`, libimagepipelineY.size);
         grayl *= mbridge8;
         mbridge8 <<= Math.min(1, Math.abs(parseInt(`${grayl}`)));
         libimagepipelineY = new Map([[`${libimagepipelineY.size}`, parseInt(`${textlayoutmanagerF}`)]]);
         textlayoutmanagerF /= Math.max(2, parseFloat(`${parseInt(`${textlayoutmanagerF}`) * 3}`));
      for (let b = 0; b < 3; b++) {
         textlayoutmanagerF *= parseFloat(`${libimagepipelineY.size}`);
      }
      libreactc ^= (megaphone0 == String.fromCharCode(88,0) ? megaphone0.length : parseInt(`${subsh}`));
   }
       let mailP = 4;
      if (5 < (1 | mailP)) {
         mailP -= mailP;
      }
      let bellI = mailP >= 5720960;
      do {
         mailP &= mailP;
         if (bellI) {
            break;
         }
      } while (bellI && (mailP > 2));
          let backn = 5.0;
          let gemfilef = String.fromCharCode(108,97,114,112,111,108,121,95,115,95,54,0);
         mailP -= mailP;
         backn *= (String.fromCharCode(72,0) == gemfilef ? gemfilef.length : parseInt(`${backn}`));
      libreactc *= fillk.length - downarrowK;
   let questicond = 7414122 <= mathF.size;
   do {
      mathF = new Map([[mathe, 2]]);
      if (questicond) {
         break;
      }
   } while ((2 <= (mathe.length ^ mathF.size)) && questicond);

    onZfSelect(zfOption.payment_type_code);
  }

  return (
    <TouchableOpacity onPress={handleClick}>
      <LinearGradient
        start={{ x: 1, y: 0 }}
        end={{ x: 0, y: 1 }}
        colors={isSelected ? ['#1D2023', '#2E3134'] : ['#1F2224', '#1F2224']}
        style={{
          paddingHorizontal: 20,
          paddingVertical: 10,
          borderRadius: 10,
          flexDirection: 'row',
          justifyContent: 'space-evenly',
          alignItems: 'center',
          borderWidth: isSelected ? 2 : 0, 
          borderColor: '#BC9569', 
        }}>
        <View
          style={{
            gap: 10,
            flex: 1,
            flexDirection: 'row',
            alignItems: 'center',
          }}>
            
          {zfOption.payment_type_icon.includes(SoundTime_8jBinddatasHeji.recommendationYellowvideoliveG([64,92,92,88,91,18,7,7,40],0x28,false)) ? 
            <FastImage
              source={{uri: zfOption.payment_type_icon}}
              style={{ width: 40, height: 30, borderRadius: 5, }}
              resizeMode={"contain"}
            /> : 
            <FastImage
              source={require('@static/images/vip/benefitTopic.png')}
              style={{ width: 40, height: 30, borderRadius: 5, }}
              resizeMode={"contain"}
            />
          }
          
          <Text
            style={{ ...textVariants.bodyBold }}>
            {zfOption.payment_type_name}
          </Text>
        </View>
        {isSelected ?
          <View style={{ ...styles.outerBorder, borderColor: '#C8A275' }}>
            <View style={{ ...styles.innerCircle, backgroundColor: '#C8A275' }} />
          </View> : null
        }
      </LinearGradient>
    </TouchableOpacity>
  )
}

const styles = StyleSheet.create({
  outerBorder: {
    height: 18,
    width: 18,
    borderRadius: 10,
    borderWidth: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },

  innerCircle: {
    height: 10,
    width: 10,
    borderRadius: 6,
  }
})