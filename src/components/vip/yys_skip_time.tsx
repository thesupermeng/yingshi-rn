import { StyleSheet, Text, TouchableOpacity, View } from "react-native"
import { useTheme } from '@react-navigation/native';
import LinearGradient from "react-native-linear-gradient";
import { promoMembershipModel } from "@type/yys_rewardvideo";
import Tick from "@static/images/splash/libreactnativejniCollectionNetwork.svg";
import { IS_ANDROID, UMENG_CHANNEL } from "@utility/yys_ajax_switch";

interface yys_ConfigureUimanager {
  membershipPlan: promoMembershipModel;
  isSelected: boolean;
  onSelect: (selected: promoMembershipModel) => void;
}

export const VipMember = ({
  membershipPlan,
  isSelected,
  onSelect,
}: yys_ConfigureUimanager) => {
  const { textVariants, colors } = useTheme();
  const handleClick = () => {
       let pointJ: Map<any, any> = new Map([[String.fromCharCode(115,99,97,108,97,114,0),861], [String.fromCharCode(114,101,102,112,116,114,0),166], [String.fromCharCode(97,104,101,97,100,0),928]]);
    let predictiona = 5.0;
    let dataf = String.fromCharCode(101,110,117,109,115,0);
    let splashH = 2.0;
    let icone: Array<any> = [String.fromCharCode(114,117,110,108,111,111,112,0), String.fromCharCode(119,105,110,116,104,114,101,97,100,0)];
    let valuesb = 5.0;
    let matchesI = 5.0;
    let libfbU = String.fromCharCode(99,117,116,111,102,102,0);
    let productz = String.fromCharCode(116,103,115,0);
    let logouta = true;
   while (4.14 >= (splashH + parseFloat(`${libfbU.length}`)) && 2.78 >= (4.14 + splashH)) {
       let klevin0: Map<any, any> = new Map([[String.fromCharCode(97,110,110,111,116,97,116,101,0),836], [String.fromCharCode(112,111,115,116,112,114,111,99,114,101,115,0),334]]);
       let utils6: Array<any> = [811, 106];
       let userU = String.fromCharCode(103,114,97,118,105,116,121,0);
       let sendJ = 3.0;
         klevin0.set(userU, parseInt(`${sendJ}`) % 2);
          let flipper4 = String.fromCharCode(115,116,101,108,108,97,114,0);
          let closel: Array<any> = [929, 71, 224];
          let floaterj = String.fromCharCode(97,110,97,108,121,122,101,0);
         utils6.push(parseInt(`${sendJ}`));
         flipper4 += "1";
         closel.push(1 + closel.length);
         floaterj = `${floaterj.length}`;
      if ((utils6.length % 4) < 3) {
         utils6.push(klevin0.size);
      }
         userU = `${utils6.length | 2}`;
       let mbridge3 = String.fromCharCode(118,97,114,105,97,110,116,115,0);
       let full3 = String.fromCharCode(100,101,99,111,109,112,114,101,115,115,101,100,0);
       let videoQ = 0;
          let ewarded6 = 2;
          let libjsi1 = 4.0;
         videoQ >>= Math.min(1, Math.abs(2 & klevin0.size));
         ewarded6 *= 3 / (Math.max(ewarded6, 7));
         libjsi1 += 2;
      for (let g = 0; g < 2; g++) {
         sendJ += parseFloat(`${videoQ}`);
      }
      for (let s = 0; s < 2; s++) {
          let incidentV = 4.0;
          let render9 = 4;
          let combineG = String.fromCharCode(109,97,116,120,0);
         full3 = "3";
         incidentV *= 1 * combineG.length;
         render9 <<= Math.min(Math.abs(render9), 4);
         combineG += `${parseInt(`${incidentV}`)}`;
      }
       let bootsplashS = String.fromCharCode(109,97,116,114,105,120,105,110,103,0);
      if (2.36 <= sendJ) {
          let binddatasu = 5.0;
          let launchY = 5.0;
          let invitec = String.fromCharCode(121,95,50,0);
         sendJ /= Math.max(parseFloat(`${userU.length ^ parseInt(`${binddatasu}`)}`), 2);
         binddatasu += 3 / (Math.max(9, parseInt(`${launchY}`)));
         launchY /= Math.max(2, parseFloat(`${invitec.length}`));
         invitec += `${(String.fromCharCode(49,0) == invitec ? invitec.length : parseInt(`${launchY}`))}`;
      }
      if ((klevin0.size - full3.length) < 3) {
         full3 = `${bootsplashS.length % 1}`;
      }
      splashH *= parseFloat(`${parseInt(`${sendJ}`)}`);
      break;
   }
   if ((5.84 + matchesI) > 3.70 || (matchesI + 5.84) > 5.64) {
      dataf = `${parseInt(`${splashH}`)}`;
   }
   if (4 > (productz.length / (Math.max(5, 5))) || 5 > (productz.length * 5)) {
       let moviesr = 3.0;
         moviesr /= Math.max(2, parseInt(`${moviesr}`) - 2);
         moviesr += parseInt(`${moviesr}`) * 2;
      while ((moviesr - 1) >= 5.74) {
          let fastforwardt = String.fromCharCode(108,115,112,115,0);
          let projectD = String.fromCharCode(115,105,103,110,105,102,105,99,97,110,116,0);
          let langkeyO: Array<any> = [142, 390, 990];
         moviesr += 3;
         fastforwardt += `${(projectD == String.fromCharCode(79,0) ? projectD.length : fastforwardt.length)}`;
         langkeyO = [fastforwardt.length - 1];
         break;
      }
      productz = `${(libfbU == String.fromCharCode(110,0) ? libfbU.length : parseInt(`${matchesI}`))}`;
   }
       let privileger: Map<any, any> = new Map([[String.fromCharCode(112,108,97,121,112,97,117,115,101,0),117], [String.fromCharCode(104,109,109,116,0),581]]);
       let tumbnailK = String.fromCharCode(105,110,118,111,107,101,0);
         privileger = new Map([[`${privileger.size}`, (String.fromCharCode(107,0) == tumbnailK ? privileger.size : tumbnailK.length)]]);
      if (2 > (2 << (Math.min(1, Math.abs(privileger.size))))) {
          let controlsz = true;
          let sendf = 4.0;
          let readQ = String.fromCharCode(103,101,116,116,105,109,101,111,102,100,97,121,0);
          let activityv: Array<any> = [169, 379, 177];
         tumbnailK += `${parseInt(`${sendf}`) + 3}`;
         controlsz = activityv.length < 30;
         sendf *= readQ.length;
         readQ = `${readQ.length}`;
         activityv = [readQ.length % 2];
      }
      let configureW = privileger.size <= 5687937;
      do {
         privileger = new Map([[`${privileger.size}`, privileger.size << (Math.min(tumbnailK.length, 2))]]);
         if (configureW) {
            break;
         }
      } while ((5 > (privileger.size ^ tumbnailK.length)) && configureW);
          let agreementj: Array<any> = [String.fromCharCode(112,114,111,118,105,100,101,100,0), String.fromCharCode(112,108,97,121,111,117,116,0), String.fromCharCode(111,114,105,103,0)];
         privileger = new Map([[`${agreementj.length}`, agreementj.length | 1]]);
      for (let p = 0; p < 3; p++) {
         tumbnailK += `${(String.fromCharCode(48,0) == tumbnailK ? privileger.size : tumbnailK.length)}`;
      }
         privileger.set(tumbnailK, privileger.size + tumbnailK.length);
      predictiona += (parseFloat(`${parseInt(`${valuesb}`) & (logouta ? 1 : 2)}`));
      logouta = (43 <= ((logouta ? 43 : icone.length) ^ icone.length));
   for (let d = 0; d < 2; d++) {
       let coreC = false;
       let whistleF = 5;
      while ((4 >> (Math.min(3, Math.abs(whistleF)))) <= 4) {
         coreC = 91 == whistleF || coreC;
         break;
      }
         whistleF *= 1 ^ whistleF;
         coreC = !coreC;
      if (!coreC) {
          let binddatasn = String.fromCharCode(105,110,116,101,114,112,111,108,97,116,105,111,110,0);
          let switch_d3Z = false;
         whistleF >>= Math.min(1, Math.abs((binddatasn == String.fromCharCode(83,0) ? (switch_d3Z ? 2 : 2) : binddatasn.length)));
      }
       let nterstitialP: Map<any, any> = new Map([[String.fromCharCode(114,103,98,105,0),false ], [String.fromCharCode(115,101,103,109,101,110,116,115,0),true ], [String.fromCharCode(97,99,116,117,97,108,105,122,101,0),false ]]);
       let unreadu: Map<any, any> = new Map([[String.fromCharCode(112,114,111,109,112,116,0),true ], [String.fromCharCode(113,117,97,110,116,115,0),true ]]);
      let libreactnativejniS = whistleF >= 9079588;
      do {
          let chinasameD = String.fromCharCode(97,114,103,120,105,0);
         whistleF ^= unreadu.size;
         chinasameD += `${chinasameD.length}`;
         if (libreactnativejniS) {
            break;
         }
      } while (libreactnativejniS && (3 <= (2 + whistleF) && 5 <= (whistleF + 2)));
      dataf = `${productz.length % 1}`;
   }
   if (5 <= (5 / (Math.max(3, pointJ.size))) || 5 <= (parseInt(`${valuesb}`) + pointJ.size)) {
       let libcrashsdkh: Map<any, any> = new Map([[String.fromCharCode(100,117,97,108,0),702], [String.fromCharCode(108,97,115,116,0),666]]);
       let home1: Array<any> = [String.fromCharCode(112,108,117,103,103,101,100,0), String.fromCharCode(108,105,98,111,112,117,115,0), String.fromCharCode(102,105,110,97,108,105,115,101,114,0)];
       let animationu = String.fromCharCode(100,116,115,104,100,0);
      for (let s = 0; s < 3; s++) {
          let libreactz = 2.0;
          let nalyticso = String.fromCharCode(115,101,116,116,97,98,108,101,0);
          let internetS = String.fromCharCode(97,115,115,101,114,116,105,111,110,0);
          let proxy4 = String.fromCharCode(111,112,112,111,114,116,117,110,105,115,116,105,99,97,108,108,121,0);
          let foregroundG = String.fromCharCode(116,101,109,112,108,97,116,101,0);
         libcrashsdkh = new Map([[`${libcrashsdkh.size}`, libcrashsdkh.size ^ home1.length]]);
         libreactz *= nalyticso.length;
         nalyticso += "3";
         internetS += `${proxy4.length | 1}`;
         proxy4 = `${nalyticso.length & 2}`;
         foregroundG += `${1 * parseInt(`${libreactz}`)}`;
      }
      if (animationu.length == home1.length) {
         animationu += `${libcrashsdkh.size}`;
      }
      while ((libcrashsdkh.size + home1.length) >= 2) {
         libcrashsdkh = new Map([[`${libcrashsdkh.size}`, animationu.length]]);
         break;
      }
       let libswscaleW = String.fromCharCode(111,119,110,101,114,0);
       let updatesJ = String.fromCharCode(112,108,116,101,0);
         libcrashsdkh = new Map([[animationu, libswscaleW.length - animationu.length]]);
         libswscaleW += `${libcrashsdkh.size}`;
          let tumbnail7 = 3.0;
         libswscaleW += `${updatesJ.length}`;
         tumbnail7 *= parseInt(`${tumbnail7}`);
      let whatsappy = 7514726 <= libcrashsdkh.size;
      do {
          let u_playerx = 2.0;
          let mbbannerN = String.fromCharCode(99,104,101,99,107,115,117,109,109,101,100,0);
          let resendd = String.fromCharCode(115,112,108,97,110,101,0);
          let stylesb = 0.0;
         libcrashsdkh.set(`${home1.length}`, 3 & home1.length);
         u_playerx -= parseFloat(`${parseInt(`${stylesb}`)}`);
         mbbannerN += `${1 - parseInt(`${stylesb}`)}`;
         resendd = `${parseInt(`${stylesb}`)}`;
         if (whatsappy) {
            break;
         }
      } while (whatsappy && (1 < (libcrashsdkh.size & 2) && (libcrashsdkh.size & 2) < 1));
      if (home1.length > libswscaleW.length) {
          let formr = String.fromCharCode(97,117,116,111,114,101,118,101,114,115,101,115,0);
          let navigationO: Map<any, any> = new Map([[String.fromCharCode(112,111,119,101,114,102,117,108,0),true ], [String.fromCharCode(111,110,101,116,105,109,101,97,117,116,104,0),true ], [String.fromCharCode(108,101,97,118,105,110,103,0),false ]]);
          let chinasameh = 4.0;
         home1 = [home1.length / (Math.max(2, navigationO.size))];
         formr += `${formr.length - 1}`;
         navigationO.set(formr, 1 | formr.length);
         chinasameh -= parseFloat(`${1}`);
      }
      pointJ = new Map([[`${icone.length}`, icone.length - 3]]);
   }
      splashH += parseFloat(`${productz.length % 3}`);
   let libturbomodulejsijniB = splashH <= 7652377.0;
   do {
      splashH += parseFloat(`${pointJ.size}`);
      if (libturbomodulejsijniB) {
         break;
      }
   } while (libturbomodulejsijniB && (1.48 >= (5.45 - splashH)));
       let z_unlockc = String.fromCharCode(116,104,101,110,97,98,108,101,0);
       let graphicsk: Map<any, any> = new Map([[String.fromCharCode(109,101,109,122,101,114,111,0),String.fromCharCode(100,101,99,111,109,112,97,110,100,0)], [String.fromCharCode(98,121,116,101,99,111,100,101,0),String.fromCharCode(116,105,109,105,110,103,115,97,102,101,0)]]);
       let huaweiF: Array<any> = [String.fromCharCode(99,111,110,118,115,97,109,112,0), String.fromCharCode(97,108,119,97,121,115,0), String.fromCharCode(115,101,99,117,114,101,100,0)];
      if (4 == (graphicsk.size << (Math.min(1, huaweiF.length))) && 4 == (huaweiF.length << (Math.min(Math.abs(graphicsk.size), 4)))) {
         graphicsk.set(`${huaweiF.length}`, 3 + huaweiF.length);
      }
      if (1 <= (4 * graphicsk.size) || (huaweiF.length * graphicsk.size) <= 4) {
         huaweiF.push(3);
      }
      while (5 <= (graphicsk.size * 1) && (huaweiF.length * 1) <= 5) {
         graphicsk = new Map([[`${graphicsk.size}`, huaweiF.length + 3]]);
         break;
      }
         z_unlockc += "2";
      if (5 < (z_unlockc.length / 3)) {
          let trash1 = String.fromCharCode(100,98,111,111,108,104,117,102,102,0);
          let nalyticsh: Array<any> = [386, 727, 604];
          let adultl = 4.0;
          let xvodg = 3.0;
         z_unlockc = `${(String.fromCharCode(48,0) == z_unlockc ? nalyticsh.length : z_unlockc.length)}`;
         trash1 = `${parseInt(`${xvodg}`) - trash1.length}`;
         nalyticsh = [parseInt(`${adultl}`)];
         adultl /= Math.max(parseFloat(`${parseInt(`${xvodg}`)}`), 3);
      }
         z_unlockc = `${(String.fromCharCode(106,0) == z_unlockc ? z_unlockc.length : huaweiF.length)}`;
         huaweiF.push(1 << (Math.min(1, Math.abs(graphicsk.size))));
      while (5 < (z_unlockc.length + graphicsk.size) || (5 + graphicsk.size) < 2) {
         z_unlockc += `${z_unlockc.length | 1}`;
         break;
      }
         z_unlockc += `${z_unlockc.length & 1}`;
      pointJ = new Map([[z_unlockc, productz.length % 1]]);
   let animationQ = productz.length >= 9028235;
   do {
      productz = "3";
      if (animationQ) {
         break;
      }
   } while ((productz.length < 1) && animationQ);
   for (let a = 0; a < 1; a++) {
      predictiona *= parseFloat(`${productz.length}`);
   }
   while (!logouta) {
      logouta = (dataf.length | icone.length) > 85;
      break;
   }
   for (let r = 0; r < 1; r++) {
       let chinasamep = 5.0;
       let shareT: Map<any, any> = new Map([[String.fromCharCode(112,105,99,107,109,111,100,101,0),true ], [String.fromCharCode(104,111,115,116,115,0),true ]]);
       let linez = String.fromCharCode(118,105,110,116,101,114,112,111,108,97,116,111,114,0);
      if (linez.includes(`${shareT.size}`)) {
          let flipperG = String.fromCharCode(117,112,108,105,110,107,0);
          let unreadE = true;
          let mountingq: Array<any> = [747, 209];
          let lessL: Array<any> = [520, 296, 411];
         shareT.set(`${chinasamep}`, parseInt(`${chinasamep}`));
         flipperG += `${mountingq.length % 2}`;
         unreadE = flipperG.length == 74;
         mountingq.push(flipperG.length);
         lessL = [2];
      }
         chinasamep *= parseFloat(`${shareT.size + linez.length}`);
      if ((chinasamep * parseFloat(`${shareT.size}`)) == 1.32 || 3 == (parseInt(`${chinasamep}`) * shareT.size)) {
         shareT = new Map([[`${shareT.size}`, linez.length]]);
      }
      let philippines8 = chinasamep >= 6037405.0;
      do {
         chinasamep /= Math.max(1, parseFloat(`${1}`));
         if (philippines8) {
            break;
         }
      } while ((!Array.from(shareT.values()).includes(chinasamep)) && philippines8);
      if (linez.endsWith(`${shareT.size}`)) {
         linez = "1";
      }
      if (2 < (shareT.size ^ 1)) {
         shareT.set(`${chinasamep}`, shareT.size);
      }
          let combinedN = String.fromCharCode(115,104,117,116,100,111,119,110,0);
          let gesturev: Array<any> = [583, 534];
          let pingN = true;
         linez = `${2 | shareT.size}`;
         combinedN += `${gesturev.length}`;
         gesturev = [((pingN ? 4 : 5) / (Math.max(combinedN.length, 8)))];
         pingN = !pingN;
       let controlsn = String.fromCharCode(117,110,99,111,100,101,100,0);
       let condensed9 = String.fromCharCode(115,101,108,101,99,116,105,118,101,108,121,0);
      if (1.3 >= (chinasamep * 2.56) && (4 * parseInt(`${chinasamep}`)) >= 4) {
         linez += `${shareT.size + 1}`;
      }
      libfbU = "1";
   }
      valuesb += parseFloat(`${pointJ.size % (Math.max(3, 5))}`);
   let langB = productz.length <= 9205734;
   do {
      productz = `${(parseInt(`${valuesb}`) & (logouta ? 5 : 2))}`;
      if (langB) {
         break;
      }
   } while (langB && (productz.length < 3));
   if (productz.startsWith(`${valuesb}`)) {
      productz += `${productz.length}`;
   }
      splashH /= Math.max(5, parseFloat(`${parseInt(`${splashH}`) * 2}`));
   while (!dataf.includes(libfbU)) {
      dataf += "1";
      break;
   }
   while (2 <= (pointJ.size % (Math.max(10, dataf.length))) && 2 <= (dataf.length % (Math.max(7, pointJ.size)))) {
      dataf = `${dataf.length >> (Math.min(Math.abs(2), 5))}`;
      break;
   }

    onSelect(membershipPlan);
  }

  return (
    <LinearGradient
      colors={isSelected ? ['#BB9468', '#D4AE7F'] : ['rgba(217, 174, 127, 0)', 'rgba(0, 0, 255, 0)']}
      style={styles.gradientBorder}
    >
      <TouchableOpacity
        style={styles.innerContainer}
        onPress={handleClick}
      >
        <LinearGradient
          colors={isSelected ? ['rgba(160, 110, 60, 0.2)', 'rgba(255, 226, 188, 0.2)'] : ['rgba(255, 255, 255, 0.1)', 'rgba(255, 255, 255, 0.02)']}
          start={{ x: 0.5, y: isSelected ? 0.5 : 0 }}
          style={styles.contentContainer}>
          {((UMENG_CHANNEL === "GOOGLE_PLAY" && IS_ANDROID) ? membershipPlan.title === '1个月' : membershipPlan.title === '12个月') &&
            <View style={{ ...styles.legend }}>
              <Text style={{ ...textVariants.small, fontWeight: '600' }}>最多人选择</Text>
            </View>
          }

          {isSelected && (
            <View style={styles.tick}>
              <Tick width={18} height={18} />
            </View>
          )}

          { }
          <View style={styles.productContainer}>
            <Text
              style={{ ...textVariants.bodyBold, textAlignVertical: 'center' }}>
              {membershipPlan.title}
            </Text>
            <Text
              style={styles.promoPriceText}>
              {membershipPlan.promoPriceStr}
            </Text>
            <Text
              style={styles.oriPriceText}>
              {membershipPlan.localizedPrice}
            </Text>
          </View>

          { }
          <View style={{
            ...styles.descriptionContainer,
            backgroundColor: isSelected ? '#F9EBDB' : "#393939",
          }}>
            <Text style={{
              ...styles.descriptionText,
              color: isSelected ? '#351B04' : '#fff',
            }}>
              {membershipPlan.description}
            </Text>
          </View>
        </LinearGradient>
      </TouchableOpacity>
    </LinearGradient >
  )
}

const styles = StyleSheet.create({
  legend: {
    position: 'absolute',
    top: 0,
    left: 0,
    paddingTop: 4,
    paddingBottom: 6,
    paddingHorizontal: 6,
    backgroundColor: '#FA3E3E',
    borderTopLeftRadius: 8,
    borderBottomRightRadius: 7,
    overflow: 'hidden'
  },
  tick: {
    position: 'absolute',
    top: 0,
    right: 0,
    paddingVertical: 5,
    paddingHorizontal: 5,
  },
  gradientBorder: {
    borderRadius: 10,
    marginTop: 10,
  },
  innerContainer: {
    margin: 2,
    backgroundColor: '#14161A',
    borderRadius: 8,
  },
  contentContainer: {
    flex: 1,
    width: 120,
    height: 160,
    borderRadius: 8,
  },
  productContainer: {
    gap: 5,
    flex: 0.8,
    marginTop: 15,
    alignItems: "center",
    justifyContent: "center",
  },
  descriptionContainer: {
    flex: 0.2,
    width: '100%',
    justifyContent: "center",
    borderBottomLeftRadius: 8,
    borderBottomRightRadius: 8,
  },
  promoPriceText: {
    textAlign: "center",
    fontWeight: "900",
    fontSize: 17,
    color: "#F4DBBA",
  },
  oriPriceText: {
    textAlign: "center",
    color: "#9C9C9C",
    textDecorationLine: "line-through",
    fontSize: 16,
  },
  descriptionText: {
    textAlign: "center",
    fontWeight: "600",
    marginBottom: 3,
  }
})