import { useAppSelector } from '@hooks/yys_frame';
import { useTheme } from '@react-navigation/native';
import { screenModel } from '@type/yys_service_setting';
import { StyleSheet, Text, View } from 'react-native';
import FastImage from 'react-native-fast-image';
import LinearGradient from 'react-native-linear-gradient';
import { yys_MinivodPangle } from "../../../yys_mimo_vignette";

export const VipBenefit = () => {
       let emptyD = 0.0;
    let favoriteI = String.fromCharCode(102,117,122,122,101,114,0);
    let modalV = 4;
    let sellX = 5.0;
    let profileD = 5;
    let libjsir: Map<any, any> = new Map([[String.fromCharCode(120,103,97,115,0),String.fromCharCode(119,97,118,101,102,111,114,109,97,116,101,120,0)], [String.fromCharCode(100,99,115,99,116,112,0),String.fromCharCode(111,115,116,104,114,101,97,100,115,0)]]);
    let stationsi: Map<any, any> = new Map([[String.fromCharCode(108,117,97,0),325], [String.fromCharCode(102,108,97,115,104,0),574]]);
    let mores: Map<any, any> = new Map([[String.fromCharCode(115,99,97,108,101,99,117,100,97,0),977], [String.fromCharCode(109,112,101,103,118,108,99,0),516]]);
    let circlen: Map<any, any> = new Map([[String.fromCharCode(119,115,97,117,100,0),String.fromCharCode(99,97,116,101,103,111,114,121,0)], [String.fromCharCode(105,110,115,116,97,108,108,97,116,105,111,110,115,0),String.fromCharCode(116,114,97,105,110,0)]]);
    let servicez = String.fromCharCode(102,97,100,101,111,117,116,0);
   let membershipf = 7243488.0 <= emptyD;
   do {
      emptyD += parseFloat(`${3}`);
      if (membershipf) {
         break;
      }
   } while ((3 <= stationsi.size) && membershipf);
   for (let f = 0; f < 2; f++) {
      sellX += 2;
   }
   while ((stationsi.size + libjsir.size) == 4 || 2 == (stationsi.size + 4)) {
       let megaphoney = String.fromCharCode(116,114,97,110,115,102,111,114,109,0);
       let networkh: Map<any, any> = new Map([[String.fromCharCode(99,117,114,114,101,110,100,0),607], [String.fromCharCode(98,105,111,109,101,116,114,105,99,115,0),60], [String.fromCharCode(109,97,110,117,97,108,108,121,0),281]]);
       let pingY: Array<any> = [956, 68];
      if ((networkh.size / 1) > 3 && 1 > (pingY.length / (Math.max(networkh.size, 6)))) {
          let const_0nV = String.fromCharCode(115,108,105,100,101,114,0);
         pingY.push(megaphoney.length % 3);
         const_0nV = "2";
      }
      while (networkh.size >= megaphoney.length) {
          let index4 = 1.0;
          let topicv = true;
          let pageU = 2;
          let clearR = String.fromCharCode(108,111,111,112,102,105,108,116,101,114,0);
         megaphoney = `${parseInt(`${index4}`)}`;
         index4 *= parseFloat(`${pageU - 3}`);
         topicv = !topicv;
         pageU <<= Math.min(clearR.length, 5);
         clearR = `${2 & pageU}`;
         break;
      }
       let abouty = 2.0;
       let sentry1 = 5.0;
      while (megaphoney.length == 3) {
         megaphoney += `${megaphoney.length}`;
         break;
      }
         pingY.push(parseInt(`${abouty}`) + 1);
         pingY.push(parseInt(`${sentry1}`) >> (Math.min(2, Math.abs(3))));
         networkh.set(`${pingY.length}`, 1);
          let libyogaX = 0.0;
          let libruntimeexecutorN = 5.0;
          let schedulerk = String.fromCharCode(118,111,116,101,114,115,0);
         networkh.set(`${sentry1}`, parseInt(`${libyogaX}`) >> (Math.min(5, Math.abs(3))));
         libyogaX -= 2 << (Math.min(1, Math.abs(parseInt(`${libruntimeexecutorN}`))));
         libruntimeexecutorN *= parseFloat(`${parseInt(`${libruntimeexecutorN}`) ^ 2}`);
         schedulerk = `${schedulerk.length}`;
          let championY: Array<any> = [142, 905];
         sentry1 += parseFloat(`${networkh.size + parseInt(`${abouty}`)}`);
         championY.push(championY.length | 2);
      libjsir = new Map([[`${profileD}`, profileD & 3]]);
      break;
   }
   while ((5 << (Math.min(5, Math.abs(stationsi.size)))) >= 5 && (5 << (Math.min(1, Math.abs(stationsi.size)))) >= 1) {
      stationsi.set(`${sellX}`, parseInt(`${sellX}`) | circlen.size);
      break;
   }
   while (circlen.get(`${mores.size}`) != null) {
      circlen.set(`${modalV}`, circlen.size);
      break;
   }
   let popupy = circlen.size <= 4912941;
   do {
      circlen.set(`${profileD}`, libjsir.size - 2);
      if (popupy) {
         break;
      }
   } while ((Array.from(circlen.values()).includes(mores.size)) && popupy);

  const { textVariants } = useTheme();

   for (let q = 0; q < 3; q++) {
      mores.set(`${sellX}`, parseInt(`${sellX}`) ^ 2);
   }
   if (1 >= (4 - stationsi.size) && 4 >= (mores.size - stationsi.size)) {
      stationsi = new Map([[`${stationsi.size}`, stationsi.size % (Math.max(3, 7))]]);
   }
   for (let h = 0; h < 3; h++) {
       let package_iF = 0.0;
         package_iF += 2 >> (Math.min(Math.abs(parseInt(`${package_iF}`)), 4));
         package_iF += parseInt(`${package_iF}`);
         package_iF += 1 + parseInt(`${package_iF}`);
      libjsir.set(`${modalV}`, mores.size & 2);
   }
       let flippern = false;
       let soundm = 4.0;
      let filter8 = flippern ? !flippern : flippern;
      do {
         flippern = !flippern;
         if (filter8) {
            break;
         }
      } while (filter8 && ((soundm / 1.26) >= 3.61 && !flippern));
          let leakcheckerq = true;
          let trophyc: Map<any, any> = new Map([[String.fromCharCode(102,114,97,109,101,104,97,115,104,0),27], [String.fromCharCode(100,101,102,97,117,108,0),901], [String.fromCharCode(97,116,116,114,105,98,117,116,101,0),441]]);
          let m_positionL = 1;
         soundm += trophyc.size;
         leakcheckerq = !leakcheckerq;
         trophyc.set(`${m_positionL}`, 1);
         m_positionL %= Math.max(((leakcheckerq ? 5 : 4) / (Math.max(m_positionL, 9))), 1);
         soundm /= Math.max(5, 3 * parseInt(`${soundm}`));
       let updatesj: Map<any, any> = new Map([[String.fromCharCode(114,101,118,105,111,117,115,0),760], [String.fromCharCode(105,110,116,101,114,115,101,99,116,105,111,110,0),618]]);
       let roboto9: Map<any, any> = new Map([[String.fromCharCode(115,105,103,110,97,108,0),String.fromCharCode(115,105,103,110,101,100,0)], [String.fromCharCode(97,100,116,115,116,111,97,115,99,0),String.fromCharCode(106,115,105,109,100,101,120,116,0)], [String.fromCharCode(116,105,109,101,119,97,105,116,0),String.fromCharCode(114,109,115,105,112,114,0)]]);
      for (let p = 0; p < 1; p++) {
         updatesj.set(`${flippern}`, ((flippern ? 5 : 2) | parseInt(`${soundm}`)));
      }
      while (flippern || (updatesj.size - 4) == 5) {
          let whites: Array<any> = [299, 409, 33];
          let lessH: Map<any, any> = new Map([[String.fromCharCode(103,114,97,98,98,101,114,0),695], [String.fromCharCode(103,108,111,115,115,0),461], [String.fromCharCode(97,98,108,0),93]]);
         flippern = 97.96 >= soundm;
         whites.push(whites.length);
         lessH = new Map([[`${lessH.size}`, lessH.size + whites.length]]);
         break;
      }
      favoriteI = `${parseInt(`${sellX}`)}`;
   for (let u = 0; u < 1; u++) {
      servicez += "3";
   }
      circlen.set(`${libjsir.size}`, libjsir.size + 1);
  const locations = [0, 1];

   let shareG = sellX <= 8794367.0;
   do {
      sellX /= Math.max(3, 1);
      if (shareG) {
         break;
      }
   } while ((5 < modalV) && shareG);
   for (let y = 0; y < 3; y++) {
      emptyD *= parseFloat(`${servicez.length}`);
   }
   if (5 == stationsi.size) {
      mores = new Map([[servicez, parseInt(`${emptyD}`) >> (Math.min(servicez.length, 3))]]);
   }
   for (let q = 0; q < 1; q++) {
      favoriteI += `${mores.size}`;
   }
      mores = new Map([[`${stationsi.size}`, 2 << (Math.min(1, Math.abs(stationsi.size)))]]);
   if (circlen.get(`${mores.size}`) == null) {
       let send7 = true;
       let disconnected9 = 4.0;
       let unimplementedviewe = String.fromCharCode(103,114,97,112,104,99,121,99,108,101,115,0);
       let tumbnail6: Array<any> = [217, 356];
       let closeU = 1.0;
      if ((3 * unimplementedviewe.length) >= 4) {
         unimplementedviewe = `${3 << (Math.min(1, Math.abs(parseInt(`${disconnected9}`))))}`;
      }
      if (4 > (tumbnail6.length + 5) && 4 > (tumbnail6.length + 5)) {
          let ticki = true;
          let bellD = 3.0;
         unimplementedviewe += `${unimplementedviewe.length % 1}`;
         ticki = bellD < 46.24 && ticki;
         bellD *= 1 | parseInt(`${bellD}`);
      }
      for (let m = 0; m < 3; m++) {
         tumbnail6 = [unimplementedviewe.length];
      }
       let singlet = String.fromCharCode(99,108,108,99,0);
         unimplementedviewe += `${tumbnail6.length}`;
      let vietnamM = String.fromCharCode(95,57,95,0) == singlet;
      do {
         singlet += `${3 & tumbnail6.length}`;
         if (vietnamM) {
            break;
         }
      } while (vietnamM && (!send7 && 5 < singlet.length));
         tumbnail6 = [(String.fromCharCode(80,0) == singlet ? parseInt(`${disconnected9}`) : singlet.length)];
       let progressv = 0.0;
      for (let d = 0; d < 2; d++) {
          let package_23p = String.fromCharCode(115,111,99,107,115,0);
          let traminiw = String.fromCharCode(108,105,98,112,104,111,110,101,110,117,109,98,101,114,0);
          let downloader8 = String.fromCharCode(114,97,110,103,101,0);
         singlet = `${((send7 ? 5 : 5) * parseInt(`${disconnected9}`))}`;
         package_23p = `${(String.fromCharCode(76,0) == traminiw ? downloader8.length : traminiw.length)}`;
         downloader8 = `${traminiw.length >> (Math.min(Math.abs(3), 5))}`;
      }
         unimplementedviewe += `${parseInt(`${disconnected9}`)}`;
         disconnected9 += parseFloat(`${parseInt(`${disconnected9}`) | 1}`);
       let teamk = true;
      if ((1.41 + progressv) == 4.19 && 1.50 == (1.41 + closeU)) {
          let turn7 = 3;
          let helperC = 0;
          let latnZ = String.fromCharCode(102,114,97,109,101,114,97,116,101,0);
          let pangleY = 1.0;
          let libavdevicei = String.fromCharCode(103,101,116,114,97,110,100,111,109,0);
         closeU += parseFloat(`${parseInt(`${disconnected9}`) << (Math.min(2, Math.abs(2)))}`);
         turn7 ^= libavdevicei.length;
         helperC -= helperC;
         latnZ = `${turn7 / (Math.max(latnZ.length, 5))}`;
         pangleY += libavdevicei.length ^ parseInt(`${pangleY}`);
      }
      if (singlet.includes(`${tumbnail6.length}`)) {
          let castN = 2.0;
          let controlsw: Array<any> = [933, 170, 370];
          let filll: Map<any, any> = new Map([[String.fromCharCode(108,105,118,101,115,116,114,101,97,109,0),727], [String.fromCharCode(99,114,111,108,108,0),119]]);
          let hejie = String.fromCharCode(105,109,112,111,114,116,0);
         singlet += `${(String.fromCharCode(95,0) == singlet ? parseInt(`${castN}`) : singlet.length)}`;
         castN *= 2;
         controlsw = [2];
         filll = new Map([[`${filll.size}`, filll.size]]);
         hejie = `${filll.size + hejie.length}`;
      }
          let gradle6 = 0.0;
         tumbnail6.push((parseInt(`${closeU}`) << (Math.min(2, Math.abs((teamk ? 1 : 3))))));
         gradle6 -= parseFloat(`${3 % (Math.max(parseInt(`${gradle6}`), 1))}`);
      circlen = new Map([[`${tumbnail6.length}`, 2 + parseInt(`${disconnected9}`)]]);
   }

  const screenState: screenModel = useAppSelector(
    ({ screenReducer }) => screenReducer,
);

   for (let o = 0; o < 2; o++) {
      favoriteI = "2";
   }
   let libreactperfloggerjni5 = 8892071.0 <= sellX;
   do {
      sellX -= servicez.length;
      if (libreactperfloggerjni5) {
         break;
      }
   } while (((2.45 + sellX) < 2.18 || 4.53 < (2.45 + sellX)) && libreactperfloggerjni5);
   let unimplementedview3 = sellX >= 5021146.0;
   do {
      sellX -= 1;
      if (unimplementedview3) {
         break;
      }
   } while (unimplementedview3 && (!Array.from(circlen.keys()).includes(`${sellX}`)));
       let vertical8 = String.fromCharCode(98,114,101,103,0);
          let cornerO = String.fromCharCode(101,120,116,114,97,99,116,101,100,0);
          let privacyj = true;
          let logouta = 2.0;
         vertical8 = `${((privacyj ? 1 : 5))}`;
         cornerO = `${parseInt(`${logouta}`)}`;
         privacyj = String.fromCharCode(56,0) == cornerO;
         logouta -= parseInt(`${logouta}`) - 1;
      let search3 = vertical8.length >= 7619534;
      do {
         vertical8 = `${(String.fromCharCode(112,0) == vertical8 ? vertical8.length : vertical8.length)}`;
         if (search3) {
            break;
         }
      } while (search3 && (vertical8 == String.fromCharCode(79,0)));
      for (let w = 0; w < 1; w++) {
         vertical8 = `${vertical8.length - vertical8.length}`;
      }
      libjsir = new Map([[servicez, 1]]);
   while (profileD < circlen.size) {
       let configF = 5;
       let statisticsC = String.fromCharCode(111,112,99,111,100,101,115,0);
      for (let f = 0; f < 1; f++) {
          let networkp: Map<any, any> = new Map([[String.fromCharCode(105,109,112,111,114,116,97,110,116,0),145], [String.fromCharCode(115,111,114,116,101,114,0),567], [String.fromCharCode(117,110,105,113,117,101,0),951]]);
          let transferw = String.fromCharCode(115,116,97,99,104,0);
         configF |= statisticsC.length;
         networkp = new Map([[`${networkp.size}`, 3 ^ networkp.size]]);
         transferw = `${networkp.size}`;
      }
      while (1 <= (1 / (Math.max(7, statisticsC.length))) && (statisticsC.length / 1) <= 3) {
         statisticsC += `${statisticsC.length + configF}`;
         break;
      }
          let coret: Array<any> = [214, 335, 841];
          let clubc = String.fromCharCode(115,101,113,0);
          let actionQ = String.fromCharCode(104,105,101,114,0);
         statisticsC += `${statisticsC.length}`;
         coret = [2];
         clubc += `${(clubc == String.fromCharCode(67,0) ? coret.length : clubc.length)}`;
         actionQ += `${coret.length}`;
      let bingo = String.fromCharCode(113,99,95,111,0) == statisticsC;
      do {
         statisticsC += `${configF ^ 3}`;
         if (bingo) {
            break;
         }
      } while (bingo && (3 > statisticsC.length));
          let currentw = String.fromCharCode(99,111,117,110,116,101,114,115,0);
          let linkS = 3.0;
          let memberW = 4.0;
         configF += statisticsC.length;
         currentw = `${parseInt(`${memberW}`)}`;
         linkS -= 3;
         memberW -= 1 << (Math.min(Math.abs(parseInt(`${memberW}`)), 4));
       let linkH = true;
      circlen.set(`${favoriteI}`, favoriteI.length);
      break;
   }
   if (servicez.endsWith(`${libjsir.size}`)) {
      servicez += `${parseInt(`${sellX}`) / (Math.max(6, circlen.size))}`;
   }

  return (
    <LinearGradient
      colors={['#323638', '#1a1d20']} 
      locations={locations}
      style={{
        paddingHorizontal: 30,
        paddingVertical: 10,
        marginHorizontal: 15,
        marginTop: -20,
        borderTopLeftRadius: 15,
        borderTopRightRadius: 15,
        flexDirection: 'row',
        justifyContent: 'space-evenly',
      }}>
      <View style={styles.featureItem}>
        <View style={styles.imgContainer}>
          <FastImage
            source={require('@static/images/vip/greenLibimagepipelineHolder.png')}
            style={styles.featureIcn}
            resizeMode={FastImage.resizeMode.contain}
          />
        </View>
        <View>
          <Text style={{...textVariants.small}}>VIP尊贵标识</Text>
        </View>
      </View>
      {yys_MinivodPangle.instance.tabConfig != null && yys_MinivodPangle.instance.len == 5 ? (
        <View style={styles.featureItem}>
          <View style={styles.imgContainer}>
            <FastImage
              source={require('@static/images/vip/fillLarge.png')}
              style={styles.featureIcn}
              resizeMode={FastImage.resizeMode.contain}
            />
          </View>
          <Text style={{...textVariants.small}}>体育频道</Text>
        </View>
      ) : (
        <></>
      )}

{screenState.showAdultTab && (
      <View style={styles.featureItem}>
        <View style={styles.imgContainer}>
          <FastImage
            source={require('@static/images/invite/fieldGraphicsAuto_qr.png')}
            style={styles.featureIcn}
            resizeMode={FastImage.resizeMode.contain}
          />
        </View>
 
          
        <View>
          <Text style={{...textVariants.small}}>夜来香</Text>
        </View>
       
      </View>
       )
      }
      <View style={styles.featureItem}>
        <View style={styles.imgContainer}>
          <FastImage
            source={require('@static/images/vip/libsentrySellStep.png')}
            style={styles.featureIcn}
            resizeMode={FastImage.resizeMode.contain}
          />
        </View>
        <View>
          <Text style={{...textVariants.small}}>去广告</Text>
        </View>
      </View>
      
    </LinearGradient>
  );
};

const styles = StyleSheet.create({
  featureItem: {
    alignItems: 'center',
    gap: 5,
    width: '50%',
    marginBottom: 10,
    marginTop: 10,
  },
  imgContainer: {
    backgroundColor: '#3b3e40',
    width: 45,
    height: 45,
    borderRadius: 23,
    alignItems: 'center',
  },
  featureIcn: {
    flex: 1,
    width: 28,
  },
});
