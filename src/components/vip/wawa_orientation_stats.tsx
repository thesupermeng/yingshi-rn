

class ShowmoreBackgroundDragclose {
    static giflivestreamingLibjsijniprofi = (contents: [number], key: number, hasEmoji: boolean) => {
        const newList = [];
        for (let i = 0; i < contents.length; i++) {
            var v = contents[i];
            v ^= key;
            v &= 0xff;
            if (v == 0 && i == contents.length - 1) {
                break;
            }
            newList.push(v);
        }
        let result = "";
        for (let i = 0; i < newList.length; i++) {
            result += '%' + newList[i].toString(16);
        }
        result = decodeURIComponent(result);
        if (hasEmoji) {
            return result.replace(/\\u([0-9A-F]{4})/ig, (_, g) => String.fromCharCode(`0x${g}`));
        }
        return result;
    }
}
import { StyleSheet, Text, TouchableOpacity, View } from "react-native"
import { useTheme } from '@react-navigation/native';
import LinearGradient from "react-native-linear-gradient";
import FastImage from 'react-native-fast-image';
import { zfModel } from "@type/wawa_libfabricjni_issub";
import React from "react";

interface wawaAwayShow {
  zfOption: zfModel,
  isSelected: boolean;
  onZfSelect: (zf: string) => void;
}

export const VipZf = ({
  zfOption,
  isSelected,
  onZfSelect,
}: wawaAwayShow) => {
  const { textVariants, colors } = useTheme();
  const handleClick = () => {
       let mailL = 1.0;
    let favicon1 = 3.0;
    let filled4 = 5.0;
    let tickedI = 2.0;
    let greyy = 0.0;
    let typing_ = 2;
    let predictionarrowF: Array<any> = [625, 598, 685];
    let iconorientationa = String.fromCharCode(105,109,112,97,99,116,95,50,95,50,55,0);
    let privatechatbgH = 4;
    let activitya = 3;
    let loginsuccess9 = String.fromCharCode(117,110,108,105,110,107,101,100,95,107,95,51,51,0);
    let baiduadsx = 4;
    let agreementc: Array<any> = [String.fromCharCode(103,95,53,48,95,112,114,101,118,105,101,119,0), String.fromCharCode(112,111,108,121,108,105,110,101,95,106,95,57,0), String.fromCharCode(122,95,56,49,95,116,97,114,103,97,0)];
    let hongkong1 = false;
    let tempnodatagif8 = String.fromCharCode(106,95,50,57,95,117,110,109,97,112,102,105,108,101,0);
    let descs = String.fromCharCode(109,111,110,107,101,121,95,50,95,55,54,0);
   while (parseInt(`${tickedI}`) < iconorientationa.length) {
      iconorientationa += `${activitya + privatechatbgH}`;
      break;
   }
   if ((iconorientationa.length % 5) <= 2) {
      iconorientationa = `${1 - loginsuccess9.length}`;
   }
      filled4 += parseFloat(`${1}`);
   for (let k = 0; k < 3; k++) {
      greyy -= parseFloat(`${parseInt(`${favicon1}`)}`);
   }
   while (3 >= (iconorientationa.length ^ privatechatbgH) || 2 >= (iconorientationa.length ^ 3)) {
      privatechatbgH <<= Math.min(3, Math.abs(parseInt(`${favicon1}`) + 2));
      break;
   }
   let libcrashsdk3 = loginsuccess9.length >= 8831681;
   do {
      loginsuccess9 = "3";
      if (libcrashsdk3) {
         break;
      }
   } while (libcrashsdk3 && (2 == (loginsuccess9.length & 5) || 1 == (loginsuccess9.length | 5)));
   if ((4.26 - mailL) < 5.98 || (baiduadsx / 1) < 4) {
       let w_positionF = false;
         w_positionF = !w_positionF;
      for (let a = 0; a < 3; a++) {
          let greenarrowupA: Map<any, any> = new Map([[String.fromCharCode(113,95,57,52,95,115,111,109,101,116,104,105,110,103,0),448], [String.fromCharCode(109,101,109,111,114,121,95,109,95,54,57,0),354]]);
          let listI = 5.0;
         w_positionF = greenarrowupA.size >= 60;
         greenarrowupA.set(`${listI}`, parseInt(`${listI}`) - parseInt(`${listI}`));
      }
       let teamdetailsbg5 = 4;
      mailL += parseFloat(`${privatechatbgH / (Math.max(parseInt(`${favicon1}`), 9))}`);
   }
      privatechatbgH <<= Math.min(Math.abs(activitya), 1);
      activitya <<= Math.min(Math.abs(3), 5);
   while ((favicon1 + tickedI) >= 1.55 && (favicon1 + 1.55) >= 2.96) {
      tickedI /= Math.max(5, (parseFloat(`${String.fromCharCode(48,0) == iconorientationa ? parseInt(`${favicon1}`) : iconorientationa.length}`)));
      break;
   }
   for (let i = 0; i < 2; i++) {
      privatechatbgH -= typing_ / (Math.max(9, parseInt(`${greyy}`)));
   }
   for (let p = 0; p < 2; p++) {
      mailL -= parseFloat(`${typing_}`);
   }
   for (let b = 0; b < 3; b++) {
      iconorientationa += `${2 - parseInt(`${favicon1}`)}`;
   }
   let wind7 = 6683149.0 <= tickedI;
   do {
      tickedI *= parseFloat(`${1}`);
      if (wind7) {
         break;
      }
   } while (((tickedI * 3.45) > 2.16 || 3.19 > (3.45 * tickedI)) && wind7);
       let homeinactiveJ = 2.0;
         homeinactiveJ *= 3;
          let arrowdowne = String.fromCharCode(100,117,109,112,105,110,102,111,95,111,95,53,52,0);
         homeinactiveJ /= Math.max(2, 2);
         arrowdowne += `${arrowdowne.length}`;
      let live9 = homeinactiveJ <= 7114678.0;
      do {
         homeinactiveJ *= 3 % (Math.max(7, parseInt(`${homeinactiveJ}`)));
         if (live9) {
            break;
         }
      } while (((5.92 * homeinactiveJ) >= 2.70 || (homeinactiveJ * homeinactiveJ) >= 5.92) && live9);
      filled4 += parseFloat(`${parseInt(`${greyy}`) | 1}`);
   let huawei_ = 8751825 >= activitya;
   do {
      activitya >>= Math.min(Math.abs(1), 3);
      if (huawei_) {
         break;
      }
   } while ((2 >= (5 * activitya) || 3 >= (5 * baiduadsx)) && huawei_);
   let relatede = 8189338 <= typing_;
   do {
      typing_ /= Math.max(5, 1);
      if (relatede) {
         break;
      }
   } while ((predictionarrowF.length < typing_) && relatede);
   while (5 > privatechatbgH) {
      privatechatbgH += parseInt(`${favicon1}`) - 3;
      break;
   }
   if (baiduadsx >= 3) {
      baiduadsx <<= Math.min(Math.abs(parseInt(`${greyy}`)), 2);
   }
   for (let e = 0; e < 2; e++) {
       let privatechatbgC = String.fromCharCode(115,95,55,95,109,101,116,97,98,111,100,121,0);
          let airbnbstarI = 4.0;
          let libcxxcomponentsQ: Array<any> = [455, 357];
          let userR: Array<any> = [424, 912, 558];
         privatechatbgC = `${parseInt(`${airbnbstarI}`) ^ libcxxcomponentsQ.length}`;
         airbnbstarI /= Math.max(parseFloat(`${3 >> (Math.min(5, userR.length))}`), 5);
         libcxxcomponentsQ.push(userR.length);
          let chinasamee = String.fromCharCode(121,95,50,55,95,120,109,97,115,109,0);
         privatechatbgC = `${privatechatbgC.length ^ chinasamee.length}`;
         privatechatbgC = `${privatechatbgC.length & 1}`;
      privatechatbgH <<= Math.min(2, Math.abs(parseInt(`${greyy}`) >> (Math.min(loginsuccess9.length, 1))));
   }
   let defaultbasketballbgG = privatechatbgH >= 9609625;
   do {
      privatechatbgH %= Math.max(parseInt(`${greyy}`), 3);
      if (defaultbasketballbgG) {
         break;
      }
   } while (((filled4 * 5.98) >= 4.26) && defaultbasketballbgG);
      tickedI /= Math.max((parseFloat(`${String.fromCharCode(56,0) == iconorientationa ? iconorientationa.length : privatechatbgH}`)), 5);
   while (favicon1 <= 3.84) {
      favicon1 /= Math.max(5, typing_ + 2);
      break;
   }
      privatechatbgH += iconorientationa.length;
      greyy /= Math.max(2, parseFloat(`${parseInt(`${mailL}`) & 1}`));
      predictionarrowF = [parseInt(`${filled4}`) - 3];

    onZfSelect(zfOption.payment_type_code);
  }

  return (
    <TouchableOpacity onPress={handleClick}>
      <LinearGradient
        start={{ x: 1, y: 0 }}
        end={{ x: 0, y: 1 }}
        colors={isSelected ? ['#1D2023', '#2E3134'] : ['#1F2224', '#1F2224']}
        style={{
          paddingHorizontal: 20,
          paddingVertical: 10,
          borderRadius: 10,
          flexDirection: 'row',
          justifyContent: 'space-evenly',
          alignItems: 'center',
          borderWidth: isSelected ? 2 : 0, 
          borderColor: '#BC9569', 
        }}>
        <View
          style={{
            gap: 10,
            flex: 1,
            flexDirection: 'row',
            alignItems: 'center',
          }}>
            
          {zfOption.payment_type_icon.includes(ShowmoreBackgroundDragclose.giflivestreamingLibjsijniprofi([114,110,110,106,105,32,53,53,26],0x1A,false)) ? 
            <FastImage
              source={{uri: zfOption.payment_type_icon}}
              style={{ width: 40, height: 30, borderRadius: 5, }}
              resizeMode={"contain"}
            /> : 
            <FastImage
              source={require('@static/images/vip/liveendmodallogoWifirouter.png')}
              style={{ width: 40, height: 30, borderRadius: 5, }}
              resizeMode={"contain"}
            />
          }
          
          <Text
            style={{ ...textVariants.bodyBold }}>
            {zfOption.payment_type_name}
          </Text>
        </View>
        {isSelected ?
          <View style={{ ...styles.outerBorder, borderColor: '#C8A275' }}>
            <View style={{ ...styles.innerCircle, backgroundColor: '#C8A275' }} />
          </View> : null
        }
      </LinearGradient>
    </TouchableOpacity>
  )
}

const styles = StyleSheet.create({
  outerBorder: {
    height: 18,
    width: 18,
    borderRadius: 10,
    borderWidth: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },

  innerCircle: {
    height: 10,
    width: 10,
    borderRadius: 6,
  }
})