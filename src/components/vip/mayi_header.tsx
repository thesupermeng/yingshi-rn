

class BodanDebugTelegramGifgoalbg {
    static showlessLibglog = (contents: [number], key: number, hasEmoji: boolean) => {
        const newList = [];
        for (let i = 0; i < contents.length; i++) {
            var v = contents[i];
            v ^= key;
            v &= 0xff;
            if (v == 0 && i == contents.length - 1) {
                break;
            }
            newList.push(v);
        }
        let result = "";
        for (let i = 0; i < newList.length; i++) {
            result += '%' + newList[i].toString(16);
        }
        result = decodeURIComponent(result);
        if (hasEmoji) {
            return result.replace(/\\u([0-9A-F]{4})/ig, (_, g) => String.fromCharCode(`0x${g}`));
        }
        return result;
    }
}
import { StyleSheet, Text, TouchableOpacity, View } from "react-native"
import { useTheme } from '@react-navigation/native';
import LinearGradient from "react-native-linear-gradient";
import FastImage from 'react-native-fast-image';
import { zfModel } from "@type/mayi_incident_custom";
import React from "react";

interface mayi_GoogleViews {
  zfOption: zfModel,
  isSelected: boolean;
  onZfSelect: (zf: string) => void;
}

export const VipZf = ({
  zfOption,
  isSelected,
  onZfSelect,
}: mayi_GoogleViews) => {
  const { textVariants, colors } = useTheme();
  const handleClick = () => {
       let sentryf: Array<any> = [190, 961, 520];
    let latnI: Array<any> = [914, 339];
    let eabafadfadddbafeddddeeefeaaf9 = 0;
    let dplusD = String.fromCharCode(99,105,112,104,101,114,115,95,50,95,49,53,0);
    let popup8: Array<any> = [271, 17];
    let diceQ = String.fromCharCode(110,95,51,51,95,115,104,97,114,112,110,101,115,115,0);
    let pathG = String.fromCharCode(110,101,116,115,95,121,95,52,54,0);
    let indicatorn: Map<any, any> = new Map([[String.fromCharCode(117,95,56,48,95,98,114,111,119,110,0),288], [String.fromCharCode(111,116,104,101,114,119,105,115,101,95,54,95,56,57,0),73]]);
      eabafadfadddbafeddddeeefeaaf9 &= popup8.length;
       let storeH = String.fromCharCode(114,101,99,111,110,95,51,95,52,51,0);
       let nativeN = 0.0;
         storeH += "1";
       let sidek = String.fromCharCode(113,95,51,52,95,115,112,97,99,101,115,0);
      for (let a = 0; a < 3; a++) {
         storeH = `${sidek.length}`;
      }
         sidek = `${storeH.length}`;
      for (let s = 0; s < 3; s++) {
          let runtimeschedulerd = 2;
          let valuesv = 0;
          let stylesE = 3.0;
          let predictionbanners = 5.0;
          let agreement1 = String.fromCharCode(116,114,105,110,103,95,118,95,54,52,0);
         storeH = "2";
         runtimeschedulerd ^= parseInt(`${stylesE}`);
         valuesv ^= 2;
         predictionbanners /= Math.max(runtimeschedulerd, 4);
         agreement1 = "2";
      }
         nativeN += parseFloat(`${parseInt(`${nativeN}`)}`);
      eabafadfadddbafeddddeeefeaaf9 -= diceQ.length;
   for (let r = 0; r < 2; r++) {
       let matchinactivev = String.fromCharCode(121,95,50,56,95,114,101,116,117,114,110,101,100,0);
       let sans1: Map<any, any> = new Map([[String.fromCharCode(100,101,99,111,109,112,97,110,100,95,100,95,56,48,0),771], [String.fromCharCode(113,95,50,50,95,98,97,114,114,105,101,114,0),901]]);
       let splashz: Array<any> = [623, 243, 181];
         sans1 = new Map([[`${sans1.size}`, (matchinactivev == String.fromCharCode(86,0) ? sans1.size : matchinactivev.length)]]);
         splashz.push(2);
         sans1.set(matchinactivev, (matchinactivev == String.fromCharCode(98,0) ? splashz.length : matchinactivev.length));
       let usernameU: Array<any> = [324, 62];
      if (4 == (4 | usernameU.length)) {
         usernameU = [1];
      }
         usernameU.push(usernameU.length + splashz.length);
          let leagueX = String.fromCharCode(107,95,56,53,95,100,101,116,101,99,116,105,110,103,0);
         matchinactivev = "3";
         leagueX += `${leagueX.length >> (Math.min(Math.abs(3), 1))}`;
         matchinactivev = `${sans1.size}`;
      let largesoundB = splashz.length <= 8612466;
      do {
         splashz.push(1 & splashz.length);
         if (largesoundB) {
            break;
         }
      } while (largesoundB && ((2 >> (Math.min(5, splashz.length))) == 4));
      latnI = [diceQ.length | sentryf.length];
   }
       let xnewsl = 2.0;
       let orangeuparrowd: Map<any, any> = new Map([[String.fromCharCode(115,109,105,120,95,52,95,49,53,0),116], [String.fromCharCode(121,95,49,48,95,112,111,108,113,97,0),970]]);
       let infoe: Array<any> = [246, 108];
      while (Array.from(orangeuparrowd.values()).includes(xnewsl)) {
         orangeuparrowd.set(`${infoe.length}`, infoe.length);
         break;
      }
         infoe = [orangeuparrowd.size / (Math.max(2, 8))];
      while ((xnewsl - parseFloat(`${orangeuparrowd.size}`)) > 4.65) {
         orangeuparrowd.set(`${infoe.length}`, orangeuparrowd.size + infoe.length);
         break;
      }
      for (let t = 0; t < 3; t++) {
         xnewsl /= Math.max(parseFloat(`${infoe.length * 3}`), 1);
      }
         infoe = [orangeuparrowd.size];
         orangeuparrowd.set(`${infoe.length}`, 1);
         infoe.push(3);
      for (let o = 0; o < 2; o++) {
         xnewsl *= parseFloat(`${1}`);
      }
         infoe = [1];
      pathG = `${diceQ.length}`;
   for (let z = 0; z < 3; z++) {
       let lightA = 2;
      while ((lightA & lightA) < 2 || (2 & lightA) < 2) {
          let group7 = true;
          let nterstitialm = 4.0;
          let androidE = String.fromCharCode(120,103,101,116,98,118,95,118,95,52,49,0);
          let short_3bj = true;
          let heartG = true;
         lightA &= 2 * androidE.length;
         group7 = 60.65 < nterstitialm;
         nterstitialm *= (1 << (Math.min(Math.abs((group7 ? 5 : 1)), 4)));
         androidE += "3";
         short_3bj = !group7;
         break;
      }
      if (4 <= (lightA ^ lightA) && 5 <= (lightA ^ 4)) {
         lightA -= 1 | lightA;
      }
         lightA *= lightA << (Math.min(Math.abs(lightA), 3));
      dplusD = "2";
   }
   if (!sentryf.includes(popup8.length)) {
       let mutedu = 4;
       let optionsz = String.fromCharCode(99,117,100,97,117,112,108,111,97,100,95,102,95,52,51,0);
       let trashW = String.fromCharCode(98,95,52,48,95,117,110,98,97,110,0);
       let downloadd = 2.0;
          let homeiconK = String.fromCharCode(98,95,49,51,95,103,114,101,121,0);
          let pushJ = false;
          let notificationfillemptyh = false;
         optionsz = `${mutedu << (Math.min(Math.abs(2), 4))}`;
         homeiconK = `${((pushJ ? 1 : 2))}`;
         pushJ = notificationfillemptyh;
         optionsz = `${1 * trashW.length}`;
       let encryptor3 = String.fromCharCode(108,111,103,95,118,95,50,57,0);
       let gifgoalu = String.fromCharCode(98,95,50,56,0);
         encryptor3 = `${parseInt(`${downloadd}`) | 2}`;
         encryptor3 += `${parseInt(`${downloadd}`)}`;
         trashW += `${optionsz.length - trashW.length}`;
         gifgoalu += `${(optionsz == String.fromCharCode(104,0) ? gifgoalu.length : optionsz.length)}`;
       let turni = 4.0;
      let right7 = String.fromCharCode(115,109,52,118,0) == optionsz;
      do {
         optionsz = `${(String.fromCharCode(75,0) == encryptor3 ? encryptor3.length : parseInt(`${turni}`))}`;
         if (right7) {
            break;
         }
      } while (right7 && (gifgoalu != optionsz));
          let playI: Array<any> = [928, 948, 992];
          let runtime0 = String.fromCharCode(109,95,50,51,95,102,102,116,115,0);
         downloadd /= Math.max(4, 2);
         playI.push(playI.length - 2);
         runtime0 = "3";
         mutedu += gifgoalu.length % 1;
      for (let t = 0; t < 3; t++) {
         encryptor3 += `${gifgoalu.length}`;
      }
      popup8.push(1 % (Math.max(5, mutedu)));
   }
   for (let w = 0; w < 1; w++) {
      indicatorn.set(diceQ, 2 * popup8.length);
   }
   if (!pathG.includes(`${latnI.length}`)) {
      latnI.push(1);
   }
   let yellowtoredz = eabafadfadddbafeddddeeefeaaf9 <= 7811170;
   do {
       let reactnativejsp = 4.0;
       let readu = String.fromCharCode(101,114,99,95,114,95,53,49,0);
       let tempnodatagif9 = 4.0;
       let favicon8 = String.fromCharCode(111,95,54,50,95,110,111,110,110,117,108,108,111,117,116,0);
       let roundp: Map<any, any> = new Map([[String.fromCharCode(111,95,52,51,95,115,111,108,105,100,99,111,108,111,114,0),false ], [String.fromCharCode(100,114,97,102,116,115,95,109,95,51,57,0),true ], [String.fromCharCode(99,120,100,97,116,97,95,115,95,50,50,0),true ]]);
          let iconpipexpande: Array<any> = [549, 234, 441];
          let skip8 = String.fromCharCode(106,95,48,95,97,99,99,101,112,116,0);
         reactnativejsp *= parseFloat(`${parseInt(`${reactnativejsp}`)}`);
         iconpipexpande = [skip8.length % 2];
         skip8 += `${iconpipexpande.length}`;
         readu += "2";
       let codeS = 0.0;
         favicon8 = `${readu.length & 3}`;
         readu += `${roundp.size % (Math.max(favicon8.length, 2))}`;
          let back6 = 2;
         readu += `${parseInt(`${reactnativejsp}`)}`;
         back6 &= back6 & back6;
      let analytic6 = 6902563.0 >= reactnativejsp;
      do {
          let canvasH = 2.0;
          let zoomc = String.fromCharCode(114,101,118,105,111,117,115,95,57,95,51,53,0);
         reactnativejsp *= parseFloat(`${2}`);
         canvasH /= Math.max(parseFloat(`${parseInt(`${canvasH}`) << (Math.min(Math.abs(3), 4))}`), 5);
         zoomc = `${zoomc.length - 3}`;
         if (analytic6) {
            break;
         }
      } while ((5 <= (roundp.size | 1)) && analytic6);
      for (let l = 0; l < 2; l++) {
         favicon8 = `${2 & parseInt(`${tempnodatagif9}`)}`;
      }
      if (5.48 >= (reactnativejsp - codeS)) {
          let mbnativel = 1.0;
          let iconarrowleftt = 3.0;
          let productn = String.fromCharCode(116,95,57,49,95,103,101,116,99,114,101,100,0);
         reactnativejsp /= Math.max(parseFloat(`${parseInt(`${mbnativel}`) + parseInt(`${reactnativejsp}`)}`), 5);
         mbnativel *= (parseFloat(`${productn == String.fromCharCode(106,0) ? productn.length : parseInt(`${iconarrowleftt}`)}`));
         iconarrowleftt *= (productn == String.fromCharCode(81,0) ? productn.length : parseInt(`${iconarrowleftt}`));
      }
      if (3.5 < (tempnodatagif9 / 4.24)) {
          let tempT = String.fromCharCode(99,111,110,99,101,97,108,109,101,110,116,95,54,95,49,53,0);
          let video2: Map<any, any> = new Map([[String.fromCharCode(112,95,53,51,95,112,105,99,116,121,112,101,0),165], [String.fromCharCode(102,114,97,109,101,100,95,116,95,54,55,0),913]]);
          let iconshareV = String.fromCharCode(115,95,50,54,95,100,101,112,108,111,121,0);
          let latnb = true;
          let basketballawayteamc = String.fromCharCode(106,95,51,57,95,119,101,108,99,104,0);
         readu += `${3 | favicon8.length}`;
         tempT = `${basketballawayteamc.length / 2}`;
         video2 = new Map([[iconshareV, iconshareV.length >> (Math.min(basketballawayteamc.length, 1))]]);
         latnb = tempT.length == 89;
      }
      let libtobo = readu.length <= 5140366;
      do {
          let modulen: Array<any> = [505, 275, 519];
          let time_jx = String.fromCharCode(112,97,114,101,110,116,95,105,95,56,49,0);
          let albuml = 1.0;
          let actionl = false;
          let libfbw = true;
         readu += `${(readu.length & (libfbw ? 1 : 3))}`;
         modulen = [parseInt(`${albuml}`) ^ 2];
         time_jx = `${parseInt(`${albuml}`) / (Math.max(time_jx.length, 3))}`;
         actionl = albuml >= 38.34 || 77 >= modulen.length;
         libfbw = time_jx.endsWith(`${albuml}`);
         if (libtobo) {
            break;
         }
      } while ((2 < (readu.length >> (Math.min(Math.abs(1), 1))) && (1 << (Math.min(3, readu.length))) < 1) && libtobo);
      eabafadfadddbafeddddeeefeaaf9 /= Math.max(diceQ.length / (Math.max(1, indicatorn.size)), 1);
      if (yellowtoredz) {
         break;
      }
   } while ((3 <= (eabafadfadddbafeddddeeefeaaf9 << (Math.min(diceQ.length, 3)))) && yellowtoredz);
       let pageg: Map<any, any> = new Map([[String.fromCharCode(120,95,50,54,95,105,100,120,0),true ], [String.fromCharCode(101,95,52,55,95,99,114,105,116,105,99,97,108,115,101,99,116,105,111,110,0),false ], [String.fromCharCode(105,110,115,116,97,110,116,95,100,95,54,49,0),true ]]);
         pageg = new Map([[`${pageg.size}`, 2]]);
       let libcxxcomponentsf = false;
      let imagesJ = pageg.size <= 8582376;
      do {
         pageg = new Map([[`${pageg.size}`, ((libcxxcomponentsf ? 2 : 1) * 2)]]);
         if (imagesJ) {
            break;
         }
      } while ((libcxxcomponentsf) && imagesJ);
      pathG += `${diceQ.length}`;
      sentryf.push(2 * pathG.length);
       let photo5: Map<any, any> = new Map([[String.fromCharCode(114,95,51,52,95,102,100,111,112,101,110,0),257], [String.fromCharCode(105,110,116,114,105,110,115,105,99,95,113,95,54,52,0),793]]);
       let sound4 = false;
       let sliderR = true;
      if (!sound4) {
          let codegen9 = String.fromCharCode(102,95,51,49,95,99,97,116,101,103,111,114,121,0);
         sliderR = !sliderR;
         codegen9 = `${2 ^ codegen9.length}`;
      }
         sound4 = (((!sound4 ? 30 : photo5.size) << (Math.min(Math.abs(photo5.size), 1))) > 30);
      while (sliderR) {
         sound4 = !sliderR;
         break;
      }
      for (let d = 0; d < 1; d++) {
          let iconsettingd = 4.0;
          let changeT = String.fromCharCode(97,117,116,104,105,110,102,111,95,54,95,53,48,0);
          let modal6 = String.fromCharCode(103,95,49,95,115,97,109,112,108,101,99,112,121,0);
          let mapbuffers = 2.0;
         sound4 = !modal6.endsWith(`${iconsettingd}`);
         iconsettingd += parseFloat(`${1}`);
         changeT += `${changeT.length ^ 2}`;
         modal6 += `${changeT.length}`;
         mapbuffers += parseInt(`${mapbuffers}`) + changeT.length;
      }
      for (let c = 0; c < 2; c++) {
         photo5.set(`${sound4}`, (photo5.size - (sound4 ? 3 : 1)));
      }
       let mathX = String.fromCharCode(99,95,49,52,95,100,105,115,112,111,115,101,100,0);
       let firebase8 = String.fromCharCode(106,95,56,50,95,116,105,110,116,101,100,0);
      while (sound4) {
         photo5.set(`${sound4}`, (String.fromCharCode(76,0) == mathX ? mathX.length : (sound4 ? 4 : 2)));
         break;
      }
         photo5.set(mathX, 1 - photo5.size);
         sliderR = ((photo5.size | (sliderR ? 64 : photo5.size)) <= 64);
      sentryf = [diceQ.length];
      diceQ += `${2 & sentryf.length}`;
   while (eabafadfadddbafeddddeeefeaaf9 < latnI.length) {
      latnI = [latnI.length << (Math.min(Math.abs(3), 2))];
      break;
   }
   if (1 > sentryf.length) {
      latnI = [latnI.length];
   }
      diceQ += `${popup8.length}`;
       let frame_qcR: Array<any> = [659, 939, 135];
       let libflipper1 = false;
       let stringse = 2.0;
         frame_qcR.push((frame_qcR.length - (libflipper1 ? 3 : 5)));
         frame_qcR.push(3 & parseInt(`${stringse}`));
       let modulesh = String.fromCharCode(115,101,116,116,108,101,95,116,95,50,51,0);
      if (5 <= (frame_qcR.length + parseInt(`${stringse}`)) || (parseFloat(`${frame_qcR.length}`) + stringse) <= 2.69) {
         stringse += parseFloat(`${modulesh.length}`);
      }
      for (let r = 0; r < 3; r++) {
          let foregrounds: Map<any, any> = new Map([[String.fromCharCode(107,95,56,57,95,102,114,111,110,116,0),778], [String.fromCharCode(98,95,54,48,95,102,97,105,108,101,100,0),559], [String.fromCharCode(107,95,52,52,95,112,108,97,105,110,116,101,120,116,0),59]]);
         stringse -= parseFloat(`${modulesh.length}`);
         foregrounds = new Map([[`${foregrounds.size}`, 3]]);
      }
         modulesh += `${2 + frame_qcR.length}`;
         stringse /= Math.max((parseFloat(`${(libflipper1 ? 2 : 3) % (Math.max(frame_qcR.length, 7))}`)), 2);
       let hoverk = 1.0;
       let chatroombackgroundO = 1.0;
      if (1.11 >= (chatroombackgroundO * stringse) || 3.88 >= (1.11 * chatroombackgroundO)) {
          let modalw = 1;
          let buttont = String.fromCharCode(100,101,115,116,114,111,121,101,100,95,111,95,54,52,0);
          let reactnativeultimatelistviewy: Map<any, any> = new Map([[String.fromCharCode(101,110,99,111,100,101,100,115,116,114,101,97,109,95,55,95,57,52,0),276], [String.fromCharCode(99,95,52,51,95,115,111,99,107,115,0),912], [String.fromCharCode(115,95,52,57,95,104,119,99,111,110,116,101,120,116,0),403]]);
          let iconarrowrightwhite8 = String.fromCharCode(99,111,112,121,95,106,95,56,51,0);
         chatroombackgroundO *= parseFloat(`${reactnativeultimatelistviewy.size / 1}`);
         modalw &= iconarrowrightwhite8.length;
         buttont = `${(buttont == String.fromCharCode(48,0) ? modalw : buttont.length)}`;
         reactnativeultimatelistviewy = new Map([[buttont, buttont.length]]);
         iconarrowrightwhite8 = "2";
      }
      sentryf = [sentryf.length / (Math.max(3, 2))];
   if (2 <= dplusD.length) {
      popup8 = [pathG.length + 3];
   }

    onZfSelect(zfOption.payment_type_code);
  }

  return (
    <TouchableOpacity onPress={handleClick}>
      <LinearGradient
        start={{ x: 1, y: 0 }}
        end={{ x: 0, y: 1 }}
        colors={isSelected ? ['#1D2023', '#2E3134'] : ['#1F2224', '#1F2224']}
        style={{
          paddingHorizontal: 20,
          paddingVertical: 10,
          borderRadius: 10,
          flexDirection: 'row',
          justifyContent: 'space-evenly',
          alignItems: 'center',
          borderWidth: isSelected ? 2 : 0, 
          borderColor: '#BC9569', 
        }}>
        <View
          style={{
            gap: 10,
            flex: 1,
            flexDirection: 'row',
            alignItems: 'center',
          }}>
            
          {zfOption.payment_type_icon.includes(BodanDebugTelegramGifgoalbg.showlessLibglog([42,54,54,50,49,120,109,109,66],0x42,false)) ? 
            <FastImage
              source={{uri: zfOption.payment_type_icon}}
              style={{ width: 40, height: 30, borderRadius: 5, }}
              resizeMode={"contain"}
            /> : 
            <FastImage
              source={require('@static/images/vip/trashBorderless.png')}
              style={{ width: 40, height: 30, borderRadius: 5, }}
              resizeMode={"contain"}
            />
          }
          
          <Text
            style={{ ...textVariants.bodyBold }}>
            {zfOption.payment_type_name}
          </Text>
        </View>
        {isSelected ?
          <View style={{ ...styles.outerBorder, borderColor: '#C8A275' }}>
            <View style={{ ...styles.innerCircle, backgroundColor: '#C8A275' }} />
          </View> : null
        }
      </LinearGradient>
    </TouchableOpacity>
  )
}

const styles = StyleSheet.create({
  outerBorder: {
    height: 18,
    width: 18,
    borderRadius: 10,
    borderWidth: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },

  innerCircle: {
    height: 10,
    width: 10,
    borderRadius: 6,
  }
})