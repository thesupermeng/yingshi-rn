import { useAppSelector } from '@hooks/mayi_redirect';
import { useTheme } from '@react-navigation/native';
import { screenModel } from '@type/mayi_libfolly_inouttargetred';
import { StyleSheet, Text, View } from 'react-native';
import FastImage from 'react-native-fast-image';
import LinearGradient from 'react-native-linear-gradient';
import { mayi_Librrc } from "../../../mayi_gift";

export const VipBenefit = () => {
       let whistleK = String.fromCharCode(102,95,50,49,95,115,119,97,112,121,118,98,117,102,102,101,114,0);
    let langkeyk = 0.0;
    let loadingS = 0;
    let launcherL: Map<any, any> = new Map([[String.fromCharCode(99,97,114,111,117,115,101,108,95,121,95,54,54,0),true ], [String.fromCharCode(107,95,54,53,95,98,105,112,114,101,100,0),false ]]);
    let applicationZ = 0.0;
    let mbjscommon4 = 5.0;
    let leakchecker2 = String.fromCharCode(104,97,110,100,101,114,95,53,95,57,54,0);
    let sourceE = String.fromCharCode(121,95,51,48,95,97,118,103,0);
   for (let g = 0; g < 2; g++) {
      loadingS <<= Math.min(Math.abs(parseInt(`${applicationZ}`) ^ parseInt(`${langkeyk}`)), 2);
   }
      launcherL = new Map([[`${mbjscommon4}`, whistleK.length]]);
      launcherL.set(whistleK, 2 / (Math.max(7, parseInt(`${mbjscommon4}`))));
      mbjscommon4 *= whistleK.length | leakchecker2.length;

  const { textVariants } = useTheme();

   for (let w = 0; w < 3; w++) {
      mbjscommon4 += parseInt(`${applicationZ}`) | 3;
   }
       let eabafadfadddbafeddddeeefeaafd = String.fromCharCode(116,104,114,101,97,100,112,111,111,108,95,105,95,52,56,0);
       let sellmathbackgroundd = 2.0;
         eabafadfadddbafeddddeeefeaafd = `${parseInt(`${sellmathbackgroundd}`) % (Math.max(eabafadfadddbafeddddeeefeaafd.length, 7))}`;
         sellmathbackgroundd -= parseFloat(`${3}`);
      let libfbjniE = 9565174 >= eabafadfadddbafeddddeeefeaafd.length;
      do {
         eabafadfadddbafeddddeeefeaafd += `${eabafadfadddbafeddddeeefeaafd.length + 2}`;
         if (libfbjniE) {
            break;
         }
      } while ((2.20 <= (parseFloat(`${eabafadfadddbafeddddeeefeaafd.length}`) - sellmathbackgroundd) || (parseFloat(`${eabafadfadddbafeddddeeefeaafd.length}`) - sellmathbackgroundd) <= 2.20) && libfbjniE);
      while (eabafadfadddbafeddddeeefeaafd.length > 4) {
         eabafadfadddbafeddddeeefeaafd += `${eabafadfadddbafeddddeeefeaafd.length}`;
         break;
      }
         eabafadfadddbafeddddeeefeaafd = `${eabafadfadddbafeddddeeefeaafd.length + parseInt(`${sellmathbackgroundd}`)}`;
       let b_lockt = String.fromCharCode(120,95,56,54,95,99,97,110,111,110,105,99,97,108,0);
       let manifestR = String.fromCharCode(120,95,54,54,95,109,105,110,105,109,105,122,101,97,98,108,101,0);
      whistleK = `${launcherL.size | parseInt(`${langkeyk}`)}`;
   let googlei = mbjscommon4 >= 5688367.0;
   do {
      mbjscommon4 *= parseInt(`${mbjscommon4}`) / 2;
      if (googlei) {
         break;
      }
   } while (googlei && ((5 ^ launcherL.size) < 4 || (launcherL.size % 5) < 2));
       let basketballb = 0;
       let gifgoalbge: Map<any, any> = new Map([[String.fromCharCode(100,95,53,50,95,99,104,115,101,116,0),102], [String.fromCharCode(107,95,50,48,95,100,105,103,101,115,116,98,121,110,105,100,0),201]]);
       let crosse: Array<any> = [68, 292, 100];
      let bottomL = gifgoalbge.size >= 8553762;
      do {
          let searchbarr = String.fromCharCode(108,111,119,101,114,95,52,95,54,55,0);
          let constantsc = 2;
          let notification_: Array<any> = [914, 277, 178];
          let codegenZ = 4.0;
         gifgoalbge.set(`${basketballb}`, 1 | basketballb);
         searchbarr += `${constantsc >> (Math.min(notification_.length, 1))}`;
         constantsc <<= Math.min(1, Math.abs(constantsc));
         notification_.push(searchbarr.length);
         codegenZ /= Math.max(1, parseInt(`${codegenZ}`) * 3);
         if (bottomL) {
            break;
         }
      } while ((Array.from(gifgoalbge.keys()).includes(`${basketballb}`)) && bottomL);
      while ((3 - gifgoalbge.size) <= 2 && 5 <= (gifgoalbge.size - 3)) {
          let feedback2 = 2.0;
          let fieldg = 5;
          let defaultroombgI = String.fromCharCode(115,104,114,105,110,107,95,48,95,57,48,0);
         gifgoalbge = new Map([[`${crosse.length}`, crosse.length << (Math.min(Math.abs(1), 1))]]);
         feedback2 *= parseFloat(`${defaultroombgI.length}`);
         fieldg |= fieldg;
         defaultroombgI += "1";
         break;
      }
       let defaultfootballbgx = String.fromCharCode(115,95,55,56,95,116,101,108,0);
       let libjsinspectorZ = String.fromCharCode(116,101,114,109,105,110,97,116,101,95,50,95,56,54,0);
       let smallbrightnessd = String.fromCharCode(101,121,101,95,105,95,54,57,0);
       let elementsf = String.fromCharCode(119,95,57,49,95,114,101,101,110,116,114,97,110,116,0);
      let mutedp = libjsinspectorZ == String.fromCharCode(100,116,114,119,111,115,102,0);
      do {
          let libturbomodulejsijniN = 1;
          let canvasM: Map<any, any> = new Map([[String.fromCharCode(107,95,52,52,95,114,101,115,0),138], [String.fromCharCode(119,95,54,54,95,114,116,99,115,116,97,116,115,0),102], [String.fromCharCode(109,102,104,100,95,117,95,54,49,0),171]]);
          let cancelH = String.fromCharCode(97,110,121,95,109,95,54,54,0);
         libjsinspectorZ += "3";
         libturbomodulejsijniN >>= Math.min(4, Math.abs(libturbomodulejsijniN + 2));
         canvasM = new Map([[`${canvasM.size}`, 1]]);
         cancelH = `${3 ^ libturbomodulejsijniN}`;
         if (mutedp) {
            break;
         }
      } while (((libjsinspectorZ.length % 4) <= 1) && mutedp);
      for (let a = 0; a < 1; a++) {
          let reducerc = 5.0;
          let megaphoned: Map<any, any> = new Map([[String.fromCharCode(107,95,53,55,95,100,105,115,116,114,97,99,116,105,111,110,0),154], [String.fromCharCode(106,114,101,118,100,99,116,95,117,95,56,56,0),76], [String.fromCharCode(110,95,50,51,95,113,100,109,99,0),635]]);
          let bellr = String.fromCharCode(120,95,50,53,95,97,114,101,115,0);
          let information0 = 2.0;
          let tempnodatagifB: Map<any, any> = new Map([[String.fromCharCode(113,95,56,54,95,99,117,114,108,121,0),44], [String.fromCharCode(115,95,56,53,95,111,98,116,97,105,110,101,114,0),219]]);
         gifgoalbge = new Map([[`${crosse.length}`, libjsinspectorZ.length >> (Math.min(3, crosse.length))]]);
         reducerc -= parseInt(`${information0}`);
         megaphoned.set(`${megaphoned.size}`, tempnodatagifB.size ^ 3);
         bellr += `${megaphoned.size}`;
         information0 *= 1;
         tempnodatagifB.set(`${information0}`, parseInt(`${information0}`) ^ 3);
      }
      let hnewarchdefaultsB = libjsinspectorZ.length >= 7368124;
      do {
         libjsinspectorZ = `${(String.fromCharCode(77,0) == libjsinspectorZ ? libjsinspectorZ.length : crosse.length)}`;
         if (hnewarchdefaultsB) {
            break;
         }
      } while (hnewarchdefaultsB && (crosse.length == libjsinspectorZ.length));
      while (basketballb <= 5) {
          let project6 = 4;
          let viewsi = true;
          let rewindy: Map<any, any> = new Map([[String.fromCharCode(116,115,114,95,118,95,54,53,0),false ], [String.fromCharCode(109,98,117,102,95,55,95,54,57,0),true ]]);
          let orangeuparrow5 = String.fromCharCode(101,95,52,54,95,110,97,110,112,97,0);
          let popupV = 2.0;
         basketballb &= basketballb ^ rewindy.size;
         project6 /= Math.max(3, project6);
         viewsi = parseInt(`${popupV}`) >= orangeuparrow5.length;
         rewindy = new Map([[`${popupV}`, project6]]);
         orangeuparrow5 = `${((viewsi ? 2 : 4) / (Math.max(parseInt(`${popupV}`), 8)))}`;
         break;
      }
      let uimanagerX = elementsf == String.fromCharCode(51,122,53,108,52,0);
      do {
         elementsf = "3";
         if (uimanagerX) {
            break;
         }
      } while (uimanagerX && (smallbrightnessd != String.fromCharCode(82,0)));
      applicationZ *= parseFloat(`${crosse.length + 1}`);
  const locations = [0, 1];

      loadingS &= loadingS >> (Math.min(Math.abs(2), 3));
       let ping7: Map<any, any> = new Map([[String.fromCharCode(121,95,49,54,95,112,97,114,116,121,0),353], [String.fromCharCode(116,114,97,118,101,114,115,97,108,95,111,95,50,54,0),896], [String.fromCharCode(104,95,55,51,95,115,109,105,108,105,101,115,0),537]]);
          let iconclosewhite6 = false;
         ping7.set(`${iconclosewhite6}`, ((iconclosewhite6 ? 5 : 4) << (Math.min(Math.abs(ping7.size), 3))));
      while ((3 * ping7.size) >= 3 && 5 >= (ping7.size * 3)) {
         ping7 = new Map([[`${ping7.size}`, ping7.size / 1]]);
         break;
      }
          let pathW = 5.0;
         ping7.set(`${pathW}`, parseInt(`${pathW}`) >> (Math.min(Math.abs(3), 1)));
      loadingS += parseInt(`${langkeyk}`);
      whistleK = `${parseInt(`${langkeyk}`) - whistleK.length}`;
   while ((applicationZ - mbjscommon4) >= 3.53) {
       let greena = 2.0;
       let executorM: Map<any, any> = new Map([[String.fromCharCode(115,95,56,48,95,101,120,110,111,100,101,0),931], [String.fromCharCode(118,95,55,57,95,101,120,99,101,101,100,115,0),697], [String.fromCharCode(121,95,51,49,95,99,108,97,105,109,0),352]]);
      let r_hashH = 6297733 >= executorM.size;
      do {
         executorM.set(`${greena}`, executorM.size * 3);
         if (r_hashH) {
            break;
         }
      } while (r_hashH && (Array.from(executorM.keys()).includes(`${greena}`)));
      let imagesI = greena <= 8297762.0;
      do {
         greena -= parseFloat(`${executorM.size}`);
         if (imagesI) {
            break;
         }
      } while ((2 <= (executorM.size + parseInt(`${greena}`)) || 4.77 <= (5.27 + greena)) && imagesI);
         executorM = new Map([[`${executorM.size}`, executorM.size + parseInt(`${greena}`)]]);
       let playercommonO = String.fromCharCode(115,95,49,53,95,98,105,116,101,120,97,99,116,110,101,115,115,0);
       let datau = String.fromCharCode(102,105,110,105,115,104,101,100,95,52,95,54,57,0);
      if (playercommonO.length < 3) {
          let cnews1: Array<any> = [856, 825, 409];
          let disconnected8 = String.fromCharCode(106,95,54,95,103,114,97,98,98,101,114,0);
          let liveshare7 = 1.0;
          let libglogR: Map<any, any> = new Map([[String.fromCharCode(114,97,100,105,97,110,115,95,97,95,53,51,0),458], [String.fromCharCode(99,95,53,55,95,100,97,116,97,98,97,115,101,115,0),27]]);
         executorM = new Map([[`${libglogR.size}`, playercommonO.length >> (Math.min(2, Math.abs(libglogR.size)))]]);
         cnews1 = [parseInt(`${liveshare7}`)];
         disconnected8 = `${parseInt(`${liveshare7}`)}`;
      }
         greena *= parseFloat(`${datau.length + executorM.size}`);
      applicationZ += parseFloat(`${loadingS << (Math.min(Math.abs(3), 2))}`);
      break;
   }

  const screenState: screenModel = useAppSelector(
    ({ screenReducer }) => screenReducer,
);

   let iconpointscoreg = 7365711 >= leakchecker2.length;
   do {
       let malaysia7 = 0.0;
      while (5.49 > malaysia7) {
         malaysia7 -= parseFloat(`${2 << (Math.min(Math.abs(parseInt(`${malaysia7}`)), 3))}`);
         break;
      }
      let firebaseS = 5701202.0 >= malaysia7;
      do {
          let modey: Array<any> = [619, 58, 722];
          let hoverV = 3.0;
          let mappingN = 0.0;
          let commony: Array<any> = [409, 230, 114];
         malaysia7 *= parseFloat(`${parseInt(`${hoverV}`)}`);
         modey = [2 ^ parseInt(`${mappingN}`)];
         hoverV -= parseFloat(`${2 >> (Math.min(Math.abs(parseInt(`${mappingN}`)), 1))}`);
         commony = [commony.length];
         if (firebaseS) {
            break;
         }
      } while ((malaysia7 > 5.40) && firebaseS);
       let weiboE = String.fromCharCode(115,116,114,105,100,101,97,98,108,101,95,121,95,54,52,0);
      leakchecker2 = "1";
      if (iconpointscoreg) {
         break;
      }
   } while ((1.92 < (langkeyk + 3.63)) && iconpointscoreg);
   for (let l = 0; l < 1; l++) {
      leakchecker2 = `${parseInt(`${applicationZ}`)}`;
   }
       let basketballtrophyw = false;
      while (!basketballtrophyw) {
          let iconwatchnowr = String.fromCharCode(118,95,51,57,95,112,108,117,114,97,108,105,122,97,116,105,111,110,0);
         basketballtrophyw = !basketballtrophyw;
         iconwatchnowr += "2";
         break;
      }
         basketballtrophyw = !basketballtrophyw;
      while (basketballtrophyw && basketballtrophyw) {
         basketballtrophyw = basketballtrophyw && basketballtrophyw;
         break;
      }
      loadingS -= parseInt(`${mbjscommon4}`) >> (Math.min(4, Math.abs(3)));
   for (let u = 0; u < 3; u++) {
       let pings = 3.0;
      for (let a = 0; a < 2; a++) {
         pings /= Math.max(parseFloat(`${parseInt(`${pings}`)}`), 2);
      }
          let volumez = String.fromCharCode(110,95,55,48,95,108,108,100,98,105,110,105,116,0);
          let hooksl = String.fromCharCode(105,95,51,95,115,117,98,112,97,116,104,0);
          let dangero = 3.0;
         pings *= parseFloat(`${hooksl.length}`);
         volumez = `${(volumez == String.fromCharCode(97,0) ? parseInt(`${dangero}`) : volumez.length)}`;
         hooksl += `${(volumez == String.fromCharCode(82,0) ? parseInt(`${dangero}`) : volumez.length)}`;
         pings -= parseFloat(`${parseInt(`${pings}`) / 1}`);
      leakchecker2 = `${loadingS}`;
   }

  return (
    <LinearGradient
      colors={['#323638', '#1a1d20']} 
      locations={locations}
      style={{
        paddingHorizontal: 30,
        paddingVertical: 10,
        marginHorizontal: 15,
        marginTop: -20,
        borderTopLeftRadius: 15,
        borderTopRightRadius: 15,
        flexDirection: 'row',
        justifyContent: 'space-evenly',
      }}>
      <View style={styles.featureItem}>
        <View style={styles.imgContainer}>
          <FastImage
            source={require('@static/images/vip/specCurrent.png')}
            style={styles.featureIcn}
            resizeMode={FastImage.resizeMode.contain}
          />
        </View>
        <View>
          <Text style={{...textVariants.small}}>VIP尊贵标识</Text>
        </View>
      </View>
      {mayi_Librrc.instance.tabConfig != null && mayi_Librrc.instance.len == 5 ? (
        <View style={styles.featureItem}>
          <View style={styles.imgContainer}>
            <FastImage
              source={require('@static/images/vip/iconuserIconclosewhitebg.png')}
              style={styles.featureIcn}
              resizeMode={FastImage.resizeMode.contain}
            />
          </View>
          <Text style={{...textVariants.small}}>体育频道</Text>
        </View>
      ) : (
        <></>
      )}

{screenState.showAdultTab && (
      <View style={styles.featureItem}>
        <View style={styles.imgContainer}>
          <FastImage
            source={require('@static/images/invite/modelsGroup.png')}
            style={styles.featureIcn}
            resizeMode={FastImage.resizeMode.contain}
          />
        </View>
 
          
        <View>
          <Text style={{...textVariants.small}}>夜来香</Text>
        </View>
       
      </View>
       )
      }
      <View style={styles.featureItem}>
        <View style={styles.imgContainer}>
          <FastImage
            source={require('@static/images/vip/inactiveAudience.png')}
            style={styles.featureIcn}
            resizeMode={FastImage.resizeMode.contain}
          />
        </View>
        <View>
          <Text style={{...textVariants.small}}>去广告</Text>
        </View>
      </View>
      
    </LinearGradient>
  );
};

const styles = StyleSheet.create({
  featureItem: {
    alignItems: 'center',
    gap: 5,
    width: '50%',
    marginBottom: 10,
    marginTop: 10,
  },
  imgContainer: {
    backgroundColor: '#3b3e40',
    width: 45,
    height: 45,
    borderRadius: 23,
    alignItems: 'center',
  },
  featureIcn: {
    flex: 1,
    width: 28,
  },
});
