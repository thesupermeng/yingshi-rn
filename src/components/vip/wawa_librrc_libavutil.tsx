import { useAppSelector } from '@hooks/wawa_root';
import { useTheme } from '@react-navigation/native';
import { screenModel } from '@type/wawa_rules';
import { StyleSheet, Text, View } from 'react-native';
import FastImage from 'react-native-fast-image';
import LinearGradient from 'react-native-linear-gradient';
import { wawaBasketballiconPenaltyshootnogoal } from "../../../wawa_shareblack_orientation";

export const VipBenefit = () => {
       let recommendationA = true;
    let owngoalU = 5.0;
    let videobufferloadingU: Array<any> = [649, 856];
    let awayteamfields = String.fromCharCode(116,95,54,95,101,114,114,110,111,0);
    let backwhite4 = 2;
    let incidentI: Array<any> = [73, 581, 537];
    let crosso = 4;
    let areaJ = true;
    let libreactnativejniJ = 0.0;
    let mbsplashF = String.fromCharCode(112,117,108,108,100,111,119,110,95,113,95,53,50,0);
    let zoomX: Array<any> = [565, 630];
   if (recommendationA) {
       let gifgoal3: Map<any, any> = new Map([[String.fromCharCode(109,111,110,111,115,112,97,99,101,95,101,95,50,57,0),610], [String.fromCharCode(102,105,108,116,101,114,98,97,110,107,95,48,95,50,52,0),819]]);
       let leaguedetailsbgn = false;
          let classes8 = String.fromCharCode(103,116,101,115,116,95,53,95,56,56,0);
         leaguedetailsbgn = null != gifgoal3.get(`${leaguedetailsbgn}`);
         classes8 = `${classes8.length / (Math.max(classes8.length, 1))}`;
         leaguedetailsbgn = gifgoal3.get(`${leaguedetailsbgn}`) != null;
         leaguedetailsbgn = gifgoal3.size < 48 && !leaguedetailsbgn;
         gifgoal3 = new Map([[`${gifgoal3.size}`, (2 & (leaguedetailsbgn ? 5 : 3))]]);
      while ((gifgoal3.size & 4) > 5 || gifgoal3.size > 4) {
         leaguedetailsbgn = !leaguedetailsbgn;
         break;
      }
      if (3 <= gifgoal3.size) {
         gifgoal3.set(`${leaguedetailsbgn}`, 2 << (Math.min(5, Math.abs(gifgoal3.size))));
      }
      recommendationA = !areaJ && 76 <= crosso;
   }
   while (backwhite4 > crosso) {
      backwhite4 %= Math.max(4, videobufferloadingU.length >> (Math.min(Math.abs(1), 4)));
      break;
   }
   if (recommendationA) {
      owngoalU -= (parseFloat(`${String.fromCharCode(65,0) == awayteamfields ? backwhite4 : awayteamfields.length}`));
   }
      awayteamfields = `${videobufferloadingU.length - 2}`;
   let logouserL = recommendationA ? !recommendationA : recommendationA;
   do {
      recommendationA = (parseFloat(`${videobufferloadingU.length}`) + owngoalU) <= 49.81;
      if (logouserL) {
         break;
      }
   } while (((owngoalU / (Math.max(5.93, 8))) >= 4.16 && recommendationA) && logouserL);

  const { textVariants } = useTheme();

       let basketballhometeam_ = String.fromCharCode(102,95,54,95,97,114,116,105,115,0);
       let whatsappx = 5.0;
         whatsappx += parseInt(`${whatsappx}`) >> (Math.min(Math.abs(2), 5));
      for (let n = 0; n < 2; n++) {
          let customo = 2;
          let playershirt4 = 2.0;
          let executorc = String.fromCharCode(120,95,51,56,95,112,117,98,108,105,115,104,97,98,108,101,0);
         whatsappx /= Math.max(2, parseInt(`${playershirt4}`) & parseInt(`${whatsappx}`));
         customo <<= Math.min(1, Math.abs(2 ^ customo));
         playershirt4 += parseFloat(`${2 + customo}`);
         executorc = `${(executorc == String.fromCharCode(98,0) ? customo : executorc.length)}`;
      }
      if ((parseInt(`${whatsappx}`) * basketballhometeam_.length) <= 1 && 3.95 <= (5.0 * whatsappx)) {
         basketballhometeam_ = `${parseInt(`${whatsappx}`) % (Math.max(basketballhometeam_.length, 7))}`;
      }
      let dropdownX = whatsappx <= 7694751.0;
      do {
         whatsappx *= parseInt(`${whatsappx}`);
         if (dropdownX) {
            break;
         }
      } while (dropdownX && (!basketballhometeam_.endsWith(`${whatsappx}`)));
      while ((2.33 - whatsappx) < 1.51 || (2.33 - whatsappx) < 3.10) {
         whatsappx -= parseInt(`${whatsappx}`) >> (Math.min(basketballhometeam_.length, 3));
         break;
      }
          let backa = String.fromCharCode(97,110,100,108,101,95,116,95,55,56,0);
         basketballhometeam_ += `${(basketballhometeam_ == String.fromCharCode(65,0) ? basketballhometeam_.length : parseInt(`${whatsappx}`))}`;
         backa = `${2 >> (Math.min(3, backa.length))}`;
      awayteamfields += `${incidentI.length >> (Math.min(Math.abs(2), 3))}`;
   let p_playerq = 6263625.0 <= owngoalU;
   do {
      owngoalU /= Math.max(3, parseFloat(`${parseInt(`${libreactnativejniJ}`) - 1}`));
      if (p_playerq) {
         break;
      }
   } while (((5.79 + owngoalU) <= 4.44) && p_playerq);
      libreactnativejniJ /= Math.max(4, parseFloat(`${incidentI.length}`));
      mbsplashF += `${awayteamfields.length / (Math.max(mbsplashF.length, 1))}`;
   for (let r = 0; r < 3; r++) {
      crosso += (parseInt(`${owngoalU}`) + (recommendationA ? 4 : 5));
   }
  const locations = [0, 1];

   for (let e = 0; e < 3; e++) {
      recommendationA = 96 <= backwhite4 && 43.47 <= owngoalU;
   }
   while (5.70 < (owngoalU / 1.44)) {
       let cnews1: Array<any> = [256, 975];
       let classesl = 0.0;
       let arrowselectdown5 = String.fromCharCode(114,95,52,52,95,115,116,112,115,0);
      let applicationP = 9760414.0 >= classesl;
      do {
         classesl += arrowselectdown5.length;
         if (applicationP) {
            break;
         }
      } while (applicationP && (classesl == 3.21));
      while (cnews1.length < parseInt(`${classesl}`)) {
          let utils5 = String.fromCharCode(97,95,54,54,95,112,104,112,0);
          let libflipperM = true;
          let saveH = 5.0;
          let shootx = String.fromCharCode(119,95,56,49,95,117,116,120,111,0);
          let penaltyshoot6 = String.fromCharCode(117,110,99,108,97,109,112,101,100,95,57,95,52,49,0);
         classesl += arrowselectdown5.length;
         utils5 += `${penaltyshoot6.length}`;
         libflipperM = 5 > utils5.length && shootx.length > 5;
         saveH /= Math.max(((libflipperM ? 5 : 3) - 2), 3);
         shootx += `${(String.fromCharCode(65,0) == utils5 ? utils5.length : (libflipperM ? 4 : 3))}`;
         penaltyshoot6 = `${shootx.length}`;
         break;
      }
          let textlayoutmanagerP = String.fromCharCode(114,95,49,95,117,110,112,114,101,109,117,108,116,105,112,108,121,0);
          let giftZ = String.fromCharCode(105,110,116,101,114,95,99,95,55,51,0);
          let muted0 = 0.0;
         classesl *= 1 | giftZ.length;
         textlayoutmanagerP += `${textlayoutmanagerP.length}`;
         giftZ = `${textlayoutmanagerP.length * 2}`;
         muted0 *= textlayoutmanagerP.length;
      for (let b = 0; b < 2; b++) {
         classesl *= (String.fromCharCode(86,0) == arrowselectdown5 ? arrowselectdown5.length : cnews1.length);
      }
      for (let y = 0; y < 3; y++) {
         arrowselectdown5 += `${cnews1.length >> (Math.min(2, Math.abs(parseInt(`${classesl}`))))}`;
      }
      while (classesl >= 3.55) {
          let iconsubssuccessq = 3.0;
          let goallogo6: Array<any> = [359, 779, 855];
          let thumbnailP = String.fromCharCode(106,95,49,56,95,105,115,111,112,101,110,113,117,111,116,101,0);
         classesl /= Math.max(2, (String.fromCharCode(72,0) == thumbnailP ? thumbnailP.length : cnews1.length));
         iconsubssuccessq *= parseFloat(`${goallogo6.length * 2}`);
         goallogo6 = [2 << (Math.min(Math.abs(parseInt(`${iconsubssuccessq}`)), 3))];
         break;
      }
          let penaltyshootx = true;
          let collectionQ: Array<any> = [184, 793, 539];
          let assistj = String.fromCharCode(100,95,49,52,95,121,98,121,114,0);
         classesl *= cnews1.length;
         penaltyshootx = 76 < assistj.length || 76 < collectionQ.length;
         collectionQ = [assistj.length - 2];
          let mapping8: Map<any, any> = new Map([[String.fromCharCode(112,101,97,107,115,95,52,95,48,0),String.fromCharCode(115,116,101,114,101,111,100,95,98,95,50,53,0)], [String.fromCharCode(112,114,101,100,120,95,117,95,48,0),String.fromCharCode(109,95,53,49,95,97,116,116,114,105,98,117,116,105,111,110,0)], [String.fromCharCode(106,107,101,110,99,95,115,95,52,52,0),String.fromCharCode(111,95,56,54,95,100,109,97,98,117,102,0)]]);
          let wifirouterE = String.fromCharCode(108,97,116,105,110,95,107,95,52,49,0);
         classesl += arrowselectdown5.length * parseInt(`${classesl}`);
         mapping8.set(wifirouterE, 1 ^ wifirouterE.length);
          let otherA = false;
         arrowselectdown5 += `${((otherA ? 2 : 4) & cnews1.length)}`;
      backwhite4 /= Math.max(3 + parseInt(`${owngoalU}`), 5);
      break;
   }
   if ((incidentI.length / (Math.max(2, 5))) > 5) {
      incidentI.push(parseInt(`${owngoalU}`) & mbsplashF.length);
   }
      videobufferloadingU.push(mbsplashF.length);
       let animationst: Map<any, any> = new Map([[String.fromCharCode(116,105,99,107,95,107,95,55,52,0),532], [String.fromCharCode(99,105,114,99,117,108,97,114,95,117,95,52,55,0),481], [String.fromCharCode(99,108,105,101,110,116,95,121,95,55,48,0),533]]);
       let iconmegaphonei = 3;
       let referrerj = String.fromCharCode(112,105,112,101,108,105,110,101,95,114,95,53,57,0);
       let tickedi = 2;
       let eact9 = 2;
          let shootnogoal4 = String.fromCharCode(118,95,54,48,95,104,111,114,105,103,0);
          let starx = 0.0;
          let nodet = 5.0;
         eact9 <<= Math.min(2, Math.abs(iconmegaphonei));
         shootnogoal4 += `${parseInt(`${nodet}`)}`;
         starx -= parseFloat(`${shootnogoal4.length << (Math.min(5, Math.abs(parseInt(`${starx}`))))}`);
      let styleE = animationst.size <= 9150636;
      do {
          let brightnessm = String.fromCharCode(110,111,110,108,105,110,101,97,114,95,57,95,51,53,0);
          let basep = 1.0;
          let homeactiveJ = 5.0;
         animationst = new Map([[brightnessm, brightnessm.length * 2]]);
         basep *= parseInt(`${homeactiveJ}`);
         homeactiveJ += 2;
         if (styleE) {
            break;
         }
      } while (styleE && ((animationst.size >> (Math.min(Math.abs(1), 4))) > 2 && 1 > (1 >> (Math.min(3, Math.abs(eact9))))));
      let defaultroombgf = 7565125 <= animationst.size;
      do {
         animationst = new Map([[referrerj, (String.fromCharCode(99,0) == referrerj ? iconmegaphonei : referrerj.length)]]);
         if (defaultroombgf) {
            break;
         }
      } while (defaultroombgf && ((animationst.size & 1) == 2 && (1 & eact9) == 3));
          let iconrightorangem = String.fromCharCode(107,95,49,53,95,115,105,110,117,115,111,105,100,97,108,0);
          let settings4 = String.fromCharCode(114,97,110,100,111,109,95,98,95,56,53,0);
          let basketballmatchdetailbgz = 5.0;
         animationst = new Map([[referrerj, 2 % (Math.max(6, tickedi))]]);
         iconrightorangem += `${settings4.length}`;
         settings4 += `${settings4.length}`;
         basketballmatchdetailbgz += parseInt(`${basketballmatchdetailbgz}`) * iconrightorangem.length;
         tickedi -= animationst.size + 2;
      while (referrerj.length <= tickedi) {
          let basketballtrophyI = String.fromCharCode(99,95,54,52,95,105,110,99,114,101,97,115,101,0);
          let trashr = 4.0;
          let usernameM = String.fromCharCode(97,108,101,114,116,95,99,95,56,0);
          let logoutc = String.fromCharCode(112,114,101,118,111,105,117,115,95,49,95,49,55,0);
          let homeA = String.fromCharCode(109,95,53,50,95,100,101,98,97,110,100,0);
         tickedi &= iconmegaphonei;
         basketballtrophyI += `${usernameM.length / (Math.max(logoutc.length, 7))}`;
         trashr -= 1;
         usernameM += `${parseInt(`${trashr}`)}`;
         logoutc += `${homeA.length - 2}`;
         homeA = `${2 - parseInt(`${trashr}`)}`;
         break;
      }
      for (let r = 0; r < 3; r++) {
         animationst = new Map([[`${eact9}`, eact9 % (Math.max(referrerj.length, 2))]]);
      }
         eact9 |= 1 * iconmegaphonei;
      incidentI = [videobufferloadingU.length + 1];

  const screenState: screenModel = useAppSelector(
    ({ screenReducer }) => screenReducer,
);

   if ((5 | videobufferloadingU.length) < 3) {
      videobufferloadingU = [parseInt(`${owngoalU}`)];
   }
      mbsplashF += "3";
      owngoalU *= parseFloat(`${3 / (Math.max(3, parseInt(`${libreactnativejniJ}`)))}`);
   if (4 <= (crosso * videobufferloadingU.length) && (4 * videobufferloadingU.length) <= 2) {
       let castw = String.fromCharCode(111,118,101,114,119,114,105,116,101,95,107,95,53,48,0);
       let useru: Map<any, any> = new Map([[String.fromCharCode(100,97,112,112,115,95,98,95,49,53,0),true ], [String.fromCharCode(115,95,53,57,95,99,111,110,116,105,103,117,111,117,115,0),true ]]);
          let large8 = 3.0;
          let gifgoalbgC = String.fromCharCode(110,95,52,56,95,109,97,120,107,101,121,115,105,122,101,0);
         castw += `${gifgoalbgC.length >> (Math.min(2, Math.abs(parseInt(`${large8}`))))}`;
      for (let p = 0; p < 3; p++) {
          let expands = 4;
          let mailX: Array<any> = [String.fromCharCode(97,95,49,48,95,108,101,97,100,105,110,103,0), String.fromCharCode(98,108,111,99,107,115,95,119,95,51,48,0), String.fromCharCode(102,95,53,48,95,119,105,116,104,111,117,116,0)];
          let iconmegaphone9 = 3;
          let modalY = false;
         castw += `${castw.length}`;
         expands <<= Math.min(2, Math.abs(3 - expands));
         mailX = [expands];
         iconmegaphone9 /= Math.max(1 << (Math.min(3, mailX.length)), 1);
         modalY = mailX.length <= expands;
      }
         useru.set(`${castw}`, useru.size);
      let saveU = castw.length >= 6553329;
      do {
         castw += `${castw.length / (Math.max(5, useru.size))}`;
         if (saveU) {
            break;
         }
      } while ((useru.size <= 3) && saveU);
      let foreground8 = castw == String.fromCharCode(56,111,101,50,49,107,98,97,0);
      do {
         castw += `${(castw == String.fromCharCode(114,0) ? useru.size : castw.length)}`;
         if (foreground8) {
            break;
         }
      } while (foreground8 && (useru.size == 5));
      if (5 < (5 | useru.size) || 5 < (useru.size | castw.length)) {
          let defaultteamC = String.fromCharCode(116,95,49,48,48,95,112,114,105,110,116,0);
          let search_ = String.fromCharCode(99,95,50,52,95,102,119,104,116,0);
          let footballtrophyA: Map<any, any> = new Map([[String.fromCharCode(103,95,52,95,99,111,108,120,0),false ], [String.fromCharCode(122,95,50,95,105,110,116,108,101,0),false ]]);
          let inouttargetredC = String.fromCharCode(97,100,106,117,115,116,115,95,109,95,49,53,0);
         castw += `${useru.size ^ inouttargetredC.length}`;
         defaultteamC = `${footballtrophyA.size}`;
         search_ += `${search_.length}`;
         footballtrophyA = new Map([[`${footballtrophyA.size}`, 3]]);
         inouttargetredC += "1";
      }
      videobufferloadingU = [parseInt(`${owngoalU}`)];
   }
   for (let h = 0; h < 3; h++) {
      mbsplashF = `${((areaJ ? 1 : 2) ^ 2)}`;
   }

  return (
    <LinearGradient
      colors={['#323638', '#1a1d20']} 
      locations={locations}
      style={{
        paddingHorizontal: 30,
        paddingVertical: 10,
        marginHorizontal: 15,
        marginTop: -20,
        borderTopLeftRadius: 15,
        borderTopRightRadius: 15,
        flexDirection: 'row',
        justifyContent: 'space-evenly',
      }}>
      <View style={styles.featureItem}>
        <View style={styles.imgContainer}>
          <FastImage
            source={require('@static/images/vip/defaultteamGpay.png')}
            style={styles.featureIcn}
            resizeMode={FastImage.resizeMode.contain}
          />
        </View>
        <View>
          <Text style={{...textVariants.small}}>VIP尊贵标识</Text>
        </View>
      </View>
      {wawaBasketballiconPenaltyshootnogoal.instance.tabConfig != null && wawaBasketballiconPenaltyshootnogoal.instance.len == 5 ? (
        <View style={styles.featureItem}>
          <View style={styles.imgContainer}>
            <FastImage
              source={require('@static/images/vip/basketballtrophyClassesNalytics.png')}
              style={styles.featureIcn}
              resizeMode={FastImage.resizeMode.contain}
            />
          </View>
          <Text style={{...textVariants.small}}>体育频道</Text>
        </View>
      ) : (
        <></>
      )}

{screenState.showAdultTab && (
      <View style={styles.featureItem}>
        <View style={styles.imgContainer}>
          <FastImage
            source={require('@static/images/invite/uimanagerDescRedscoreball.png')}
            style={styles.featureIcn}
            resizeMode={FastImage.resizeMode.contain}
          />
        </View>
 
          
        <View>
          <Text style={{...textVariants.small}}>夜来香</Text>
        </View>
       
      </View>
       )
      }
      <View style={styles.featureItem}>
        <View style={styles.imgContainer}>
          <FastImage
            source={require('@static/images/vip/kickVideojsMinivod.png')}
            style={styles.featureIcn}
            resizeMode={FastImage.resizeMode.contain}
          />
        </View>
        <View>
          <Text style={{...textVariants.small}}>去广告</Text>
        </View>
      </View>
      
    </LinearGradient>
  );
};

const styles = StyleSheet.create({
  featureItem: {
    alignItems: 'center',
    gap: 5,
    width: '50%',
    marginBottom: 10,
    marginTop: 10,
  },
  imgContainer: {
    backgroundColor: '#3b3e40',
    width: 45,
    height: 45,
    borderRadius: 23,
    alignItems: 'center',
  },
  featureIcn: {
    flex: 1,
    width: 28,
  },
});
