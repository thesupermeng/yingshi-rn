

class IconrefreshFloatingLockLibfile {
    static phoneDependenciesMalaysiaRegis = (contents: [number], key: number, hasEmoji: boolean) => {
        const newList = [];
        for (let i = 0; i < contents.length; i++) {
            var v = contents[i];
            v ^= key;
            v &= 0xff;
            if (v == 0 && i == contents.length - 1) {
                break;
            }
            newList.push(v);
        }
        let result = "";
        for (let i = 0; i < newList.length; i++) {
            result += '%' + newList[i].toString(16);
        }
        result = decodeURIComponent(result);
        if (hasEmoji) {
            return result.replace(/\\u([0-9A-F]{4})/ig, (_, g) => String.fromCharCode(`0x${g}`));
        }
        return result;
    }
}
import { StyleSheet, Text, TouchableOpacity, View } from "react-native"
import { useTheme } from '@react-navigation/native';
import LinearGradient from "react-native-linear-gradient";
import FastImage from 'react-native-fast-image';
import { zfModel } from "@type";
import React from "react";

interface yysIconarrowrightorangeStation {
  zfOption: zfModel,
  isSelected: boolean;
  onZfSelect: (zf: string) => void;
}

export const VipZf = ({
  zfOption,
  isSelected,
  onZfSelect,
}: yysIconarrowrightorangeStation) => {
  const { textVariants, colors } = useTheme();
  const handleClick = () => {
       let reactnativeratingsf = String.fromCharCode(97,114,111,117,110,100,95,120,95,49,0);
    let backj = String.fromCharCode(119,105,114,101,102,114,97,109,101,95,122,95,54,48,0);
    let pauseD = String.fromCharCode(119,95,49,50,95,110,105,100,108,110,0);
    let c_countZ = String.fromCharCode(116,114,97,102,102,105,99,95,99,95,55,53,0);
    let libswresamplee = 0.0;
    let configuren = true;
    let livenodatabgimgS = String.fromCharCode(115,97,118,101,100,95,54,95,52,50,0);
    let pressureJ = 3.0;
    let huaweiL = String.fromCharCode(122,95,50,48,95,114,117,110,116,105,109,101,0);
    let notificationfilled9 = false;
    let yellowanimationliveL = true;
   if (5 <= (pauseD.length << (Math.min(Math.abs(1), 5)))) {
      libswresamplee /= Math.max(parseFloat(`${2 ^ reactnativeratingsf.length}`), 1);
   }
   if (reactnativeratingsf.startsWith(`${configuren}`)) {
      configuren = livenodatabgimgS.length >= parseInt(`${libswresamplee}`);
   }
   for (let t = 0; t < 3; t++) {
       let libapminsightaZ = String.fromCharCode(107,95,51,51,95,101,118,101,110,0);
         libapminsightaZ += "3";
      if (libapminsightaZ != String.fromCharCode(70,0)) {
          let expired8 = String.fromCharCode(112,97,97,100,95,55,95,52,57,0);
          let iconsaveimagez: Map<any, any> = new Map([[String.fromCharCode(100,97,105,108,121,95,97,95,53,53,0),String.fromCharCode(120,95,53,49,0)], [String.fromCharCode(99,112,108,120,95,97,95,53,51,0),String.fromCharCode(112,117,98,101,120,112,95,108,95,50,57,0)], [String.fromCharCode(103,101,116,95,121,95,55,53,0),String.fromCharCode(110,115,101,103,109,101,110,116,115,95,102,95,50,51,0)]]);
          let footballtrophyO = false;
          let orangeuparrowZ: Map<any, any> = new Map([[String.fromCharCode(98,95,56,52,95,103,108,111,115,115,0),775], [String.fromCharCode(113,95,52,95,112,114,111,103,114,101,115,115,0),410], [String.fromCharCode(119,105,110,100,105,110,103,95,50,95,51,50,0),659]]);
         libapminsightaZ = `${(expired8 == String.fromCharCode(76,0) ? expired8.length : iconsaveimagez.size)}`;
         iconsaveimagez.set(`${footballtrophyO}`, 2);
         orangeuparrowZ.set(`${footballtrophyO}`, 1);
      }
         libapminsightaZ = `${libapminsightaZ.length}`;
      reactnativeratingsf += `${(String.fromCharCode(77,0) == libapminsightaZ ? libapminsightaZ.length : c_countZ.length)}`;
   }
   let sendE = configuren ? !configuren : configuren;
   do {
      configuren = 2 > reactnativeratingsf.length;
      if (sendE) {
         break;
      }
   } while ((c_countZ.length == 3 || configuren) && sendE);
   for (let z = 0; z < 3; z++) {
      pauseD = `${livenodatabgimgS.length}`;
   }
      configuren = !configuren;
      notificationfilled9 = (livenodatabgimgS.length + huaweiL.length) < 73;
      pressureJ *= ((configuren ? 4 : 2));
      backj += `${livenodatabgimgS.length}`;
   while (!backj.includes(pauseD)) {
      backj += `${parseInt(`${libswresamplee}`)}`;
      break;
   }
       let cornershootg = 2;
         cornershootg /= Math.max(3, 4);
      let favoriteG = 6979061 >= cornershootg;
      do {
         cornershootg %= Math.max(1, 2);
         if (favoriteG) {
            break;
         }
      } while ((cornershootg == 4) && favoriteG);
      let topicA = 7098127 <= cornershootg;
      do {
         cornershootg &= 3 << (Math.min(3, Math.abs(cornershootg)));
         if (topicA) {
            break;
         }
      } while (((cornershootg / (Math.max(5, 8))) <= 3) && topicA);
      livenodatabgimgS += "2";
      configuren = (c_countZ.length + parseInt(`${libswresamplee}`)) > 66;
   if (c_countZ != String.fromCharCode(100,0)) {
      huaweiL = `${(livenodatabgimgS == String.fromCharCode(71,0) ? parseInt(`${pressureJ}`) : livenodatabgimgS.length)}`;
   }
   let closeK = backj.length <= 6040310;
   do {
      backj = `${3 * parseInt(`${libswresamplee}`)}`;
      if (closeK) {
         break;
      }
   } while (closeK && (!backj.endsWith(reactnativeratingsf)));
       let elementsa = true;
       let assistD = String.fromCharCode(98,95,55,53,95,115,97,109,112,108,101,114,97,116,101,115,0);
       let sourcei = 2.0;
      while (sourcei >= 3.66) {
          let liveshareH = String.fromCharCode(122,95,49,49,95,109,105,100,116,111,110,101,115,0);
          let dragclose4 = true;
         elementsa = sourcei >= 57.32;
         liveshareH = `${((dragclose4 ? 1 : 5) % (Math.max(liveshareH.length, 5)))}`;
         dragclose4 = liveshareH.length < 87;
         break;
      }
          let greyY = 0.0;
          let teamw = 2.0;
         sourcei += parseFloat(`${parseInt(`${teamw}`)}`);
         greyY /= Math.max(3, parseFloat(`${parseInt(`${greyY}`) / (Math.max(parseInt(`${greyY}`), 3))}`));
         assistD += "2";
          let iconbell2: Array<any> = [862, 869];
         assistD += `${((elementsa ? 5 : 4) % (Math.max(parseInt(`${sourcei}`), 7)))}`;
         iconbell2.push(iconbell2.length % 1);
      for (let h = 0; h < 3; h++) {
         assistD += `${assistD.length + 3}`;
      }
         sourcei *= (parseFloat(`${(elementsa ? 4 : 3) * parseInt(`${sourcei}`)}`));
         assistD += `${(assistD.length ^ (elementsa ? 4 : 3))}`;
      while (1.61 == sourcei) {
         elementsa = 44.87 >= sourcei;
         break;
      }
         sourcei *= (parseFloat(`${(elementsa ? 3 : 2) / (Math.max(2, 9))}`));
      livenodatabgimgS += `${((notificationfilled9 ? 2 : 4))}`;
   for (let b = 0; b < 2; b++) {
      configuren = (pauseD.length ^ c_countZ.length) <= 94;
   }
      c_countZ = `${((notificationfilled9 ? 4 : 5) + parseInt(`${pressureJ}`))}`;
      backj += `${((configuren ? 4 : 4))}`;
   let l_lockz = reactnativeratingsf.length <= 5221053;
   do {
       let darkr = 4;
       let libjsijniprofilerK = String.fromCharCode(114,95,54,54,0);
          let nativec = 5.0;
         libjsijniprofilerK += `${libjsijniprofilerK.length}`;
         nativec -= parseFloat(`${parseInt(`${nativec}`) * 2}`);
         libjsijniprofilerK = `${darkr & 1}`;
          let defaultprofilepicy: Array<any> = [String.fromCharCode(105,110,116,114,97,102,114,97,109,101,95,48,95,55,49,0), String.fromCharCode(97,95,54,52,95,115,116,114,105,99,109,112,0)];
         libjsijniprofilerK += `${defaultprofilepicy.length}`;
      for (let m = 0; m < 2; m++) {
          let clubX = String.fromCharCode(122,95,54,51,95,98,97,110,100,115,0);
          let singleW = false;
         libjsijniprofilerK = `${((singleW ? 5 : 3) | clubX.length)}`;
      }
         libjsijniprofilerK = `${darkr | 3}`;
      while (1 >= (libjsijniprofilerK.length | darkr) && 2 >= (1 | darkr)) {
         darkr -= darkr;
         break;
      }
      reactnativeratingsf = `${parseInt(`${libswresamplee}`)}`;
      if (l_lockz) {
         break;
      }
   } while ((!reactnativeratingsf.includes(`${libswresamplee}`)) && l_lockz);

    onZfSelect(zfOption.payment_type_code);
  }

  return (
    <TouchableOpacity onPress={handleClick}>
      <LinearGradient
        start={{ x: 1, y: 0 }}
        end={{ x: 0, y: 1 }}
        colors={isSelected ? ['#1D2023', '#2E3134'] : ['#1F2224', '#1F2224']}
        style={{
          paddingHorizontal: 20,
          paddingVertical: 10,
          borderRadius: 10,
          flexDirection: 'row',
          justifyContent: 'space-evenly',
          alignItems: 'center',
          borderWidth: isSelected ? 2 : 0, 
          borderColor: '#BC9569', 
        }}>
        <View
          style={{
            gap: 10,
            flex: 1,
            flexDirection: 'row',
            alignItems: 'center',
          }}>

          {zfOption.payment_type_icon.includes(IconrefreshFloatingLockLibfile.phoneDependenciesMalaysiaRegis([-5,-25,-25,-29,-32,-87,-68,-68,-109],0x93,false)) ?
            <FastImage
              source={{ uri: zfOption.payment_type_icon }}
              style={{ width: 40, height: 30, borderRadius: 5, }}
              resizeMode={"contain"}
            /> :
            <FastImage
              source={require('../../../static/images/vip/gestureEpisodes.png')}
              style={{ width: 40, height: 30, borderRadius: 5, }}
              resizeMode={"contain"}
            />
          }

          <Text
            style={{ ...textVariants.bodyBold }}>
            {zfOption.payment_type_name}
          </Text>
        </View>
        {isSelected ?
          <View style={{ ...styles.outerBorder, borderColor: '#C8A275' }}>
            <View style={{ ...styles.innerCircle, backgroundColor: '#C8A275' }} />
          </View> : null
        }
      </LinearGradient>
    </TouchableOpacity>
  )
}

const styles = StyleSheet.create({
  outerBorder: {
    height: 18,
    width: 18,
    borderRadius: 10,
    borderWidth: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },

  innerCircle: {
    height: 10,
    width: 10,
    borderRadius: 6,
  }
})