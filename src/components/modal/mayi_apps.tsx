import React, { Suspense, memo, useCallback, useEffect, useState } from 'react';
import { View, Text, TouchableOpacity, StyleSheet } from 'react-native';
import VipModal from './mayi_fill';
import { useNavigation, useRoute, useTheme } from '@react-navigation/native';
import AsyncStorage from '@react-native-async-storage/async-storage';
import { useAppDispatch } from '@hooks/mayi_redirect';
import {
  resetBecomeVip,
  showLoginAction,
} from '@redux/actions/mayi_iconorientation_chatroombackground';
import FastImage from 'react-native-fast-image';
import { SHOW_ZF_CONST } from '@utility/mayi_middleware_apps';
import { AdultVipPrivilegeOverlay } from './mayi_hash_image';
import { SportVipPrivilegeOverlay } from './mayi_shielddone_copy';
import { CommonVipPrivilegeOverlay } from './mayi_common';

interface mayi_GoogleViews {
  showBecomeVIPOverlay: boolean;
  setShowBecomeVIPOverlay: any;
  isJustClose?: boolean;
  selectedTab: 'sport' | 'xvod' | 'common',
  onClose?: () => void,
}

function ExpiredOverlay({
  showBecomeVIPOverlay = false,
  setShowBecomeVIPOverlay,
  isJustClose,
  selectedTab,
  onClose,
}: mayi_GoogleViews) {
  const navigator = useNavigation();
  const dispatch = useAppDispatch();
  const route = useRoute();

  
  
  

  const { colors, textVariants, spacing, icons } = useTheme();

  const renderOverlay = () => {
       let resendg = 0;
    let combinedU: Array<any> = [724, 166, 963];
    let materiald = 1.0;
    let lango: Array<any> = [858, 579];
    let videobufferloadingD = String.fromCharCode(97,110,103,108,101,115,95,121,95,54,0);
    let inactiveU: Map<any, any> = new Map([[String.fromCharCode(109,95,57,50,95,117,112,103,114,97,100,101,115,0),true ], [String.fromCharCode(120,95,52,54,95,114,97,119,0),false ], [String.fromCharCode(119,95,56,48,95,117,110,107,101,121,101,100,0),true ]]);
    let predictionactive2 = true;
    let phoneshareh = 1;
    let weibo8 = String.fromCharCode(103,115,116,114,105,110,103,95,55,95,49,48,48,0);
    let controlm = String.fromCharCode(114,95,57,56,95,97,118,115,117,98,116,105,116,108,101,0);
    let mergerc = String.fromCharCode(113,117,97,110,116,105,108,101,95,56,95,55,50,0);
    let countdownZ: Map<any, any> = new Map([[String.fromCharCode(118,95,53,52,95,109,97,112,0),95], [String.fromCharCode(115,101,114,118,105,99,101,95,111,95,57,0),425], [String.fromCharCode(97,112,112,101,110,100,97,108,108,95,107,95,52,56,0),32]]);
    let tickedR: Array<any> = [653, 973, 466];
    let unreadv = 1.0;
      materiald *= parseFloat(`${controlm.length << (Math.min(Math.abs(3), 1))}`);
      weibo8 += "3";
   for (let n = 0; n < 1; n++) {
      controlm = "1";
   }
       let r_positionh = true;
       let airbnbstarR: Array<any> = [400, 608];
      let pressured = 7519330 >= airbnbstarR.length;
      do {
         airbnbstarR = [airbnbstarR.length * 2];
         if (pressured) {
            break;
         }
      } while (pressured && (r_positionh && 5 <= (airbnbstarR.length & 5)));
      while (2 >= airbnbstarR.length) {
          let leagueL = String.fromCharCode(101,95,51,54,95,112,115,115,104,0);
         airbnbstarR.push(((r_positionh ? 1 : 4) & 1));
         leagueL += `${leagueL.length}`;
         break;
      }
      if (airbnbstarR.length >= 4) {
          let kuaishou8 = String.fromCharCode(98,95,51,49,95,109,97,121,0);
          let arrowQ = String.fromCharCode(103,95,49,48,48,95,118,101,114,105,102,105,97,116,105,111,110,0);
         airbnbstarR.push(airbnbstarR.length << (Math.min(kuaishou8.length, 1)));
         kuaishou8 += "1";
         arrowQ += "1";
      }
         r_positionh = airbnbstarR.length > 29 || r_positionh;
      if (airbnbstarR.length >= 1) {
          let background9: Map<any, any> = new Map([[String.fromCharCode(118,95,54,55,95,105,104,116,120,0),865], [String.fromCharCode(112,95,56,55,95,100,105,115,97,98,108,101,114,0),739]]);
          let loadingR = String.fromCharCode(103,95,55,55,95,101,115,116,97,98,108,105,115,104,0);
          let assistN = 2;
          let imagenetworkerrv = 1.0;
         r_positionh = assistN < 40 || loadingR == String.fromCharCode(75,0);
         background9.set(`${imagenetworkerrv}`, parseInt(`${imagenetworkerrv}`));
         loadingR = `${3 >> (Math.min(3, Math.abs(background9.size)))}`;
         assistN |= 2;
      }
          let gradleo: Map<any, any> = new Map([[String.fromCharCode(107,95,57,51,95,122,101,114,111,101,100,0),true ], [String.fromCharCode(105,110,115,101,114,116,115,95,113,95,57,49,0),true ], [String.fromCharCode(99,111,108,111,99,97,116,101,100,95,51,95,49,56,0),false ]]);
          let libbuffero = 4;
          let largew: Array<any> = [503, 166, 548];
         airbnbstarR.push(3 << (Math.min(5, Math.abs(gradleo.size))));
         gradleo.set(`${libbuffero}`, largew.length ^ 3);
         libbuffero /= Math.max(largew.length, 2);
      controlm += `${resendg % 2}`;

    if (selectedTab === 'xvod') {

   if (1 >= (videobufferloadingD.length << (Math.min(Math.abs(4), 3))) || 3 >= (4 << (Math.min(3, Math.abs(inactiveU.size))))) {
       let bootu = 0.0;
         bootu *= 2;
      while (4.7 < bootu) {
         bootu /= Math.max(parseInt(`${bootu}`), 1);
         break;
      }
       let completeC = String.fromCharCode(101,100,105,116,101,100,95,56,95,57,57,0);
      videobufferloadingD = `${parseInt(`${materiald}`)}`;
   }
      videobufferloadingD += `${combinedU.length}`;
   for (let j = 0; j < 3; j++) {
      predictionactive2 = (phoneshareh & lango.length) < 81;
   }
   if (phoneshareh >= lango.length) {
       let renewK = 5.0;
      for (let c = 0; c < 1; c++) {
         renewK -= parseFloat(`${parseInt(`${renewK}`)}`);
      }
       let selectR = 2.0;
       let libreactperfloggerjniz = 4.0;
         libreactperfloggerjniz -= parseFloat(`${2}`);
      lango.push(videobufferloadingD.length >> (Math.min(Math.abs(1), 5)));
   }
      return (
        <AdultVipPrivilegeOverlay
          showCondition={showBecomeVIPOverlay}
          onClose={() => {
            if (onClose) onClose();
            setShowBecomeVIPOverlay(false);
          }}
        />
      );
    } else if (selectedTab === 'sport') {

   while ((lango.length - 2) > 4 || 2 > lango.length) {
      lango.push(1 % (Math.max(parseInt(`${materiald}`), 8)));
      break;
   }
   if (1.18 >= (materiald / 1.84) && 3 >= (combinedU.length >> (Math.min(Math.abs(4), 2)))) {
      materiald -= parseFloat(`${phoneshareh + 3}`);
   }
      resendg ^= 1 >> (Math.min(Math.abs(resendg), 4));
      phoneshareh |= videobufferloadingD.length;
      return (
        <SportVipPrivilegeOverlay
          showCondition={showBecomeVIPOverlay}
          onClose={({ isAutoClose }) => {
            if (!isAutoClose && onClose) onClose();
            setShowBecomeVIPOverlay(false);
          }}
        />
      );
    } else if (selectedTab === 'common') {

   if (2 >= (3 >> (Math.min(1, Math.abs(inactiveU.size)))) && 3 >= inactiveU.size) {
       let a_playerZ: Array<any> = [String.fromCharCode(114,111,117,110,100,101,100,95,109,95,56,0), String.fromCharCode(104,95,51,49,95,120,99,116,101,115,116,0)];
       let searchw = String.fromCharCode(102,108,97,116,116,101,110,95,110,95,51,55,0);
       let plashD = 1;
      let homeplayerO = searchw == String.fromCharCode(97,48,117,52,110,109,117,111,108,0);
      do {
         searchw = `${searchw.length % 3}`;
         if (homeplayerO) {
            break;
         }
      } while (((searchw.length >> (Math.min(Math.abs(3), 5))) == 5 && (3 >> (Math.min(2, searchw.length))) == 2) && homeplayerO);
      for (let i = 0; i < 1; i++) {
         searchw += `${a_playerZ.length | plashD}`;
      }
          let collectionp: Map<any, any> = new Map([[String.fromCharCode(107,95,56,55,95,108,97,118,117,0),248], [String.fromCharCode(107,95,49,53,95,115,116,114,110,115,116,114,0),336]]);
          let floaterk: Map<any, any> = new Map([[String.fromCharCode(99,111,110,115,101,99,117,116,105,118,101,95,98,95,51,50,0),998], [String.fromCharCode(100,95,56,49,95,98,105,116,109,97,112,0),555]]);
         plashD -= searchw.length;
         collectionp = new Map([[`${collectionp.size}`, floaterk.size * 1]]);
         floaterk = new Map([[`${collectionp.size}`, 1 & floaterk.size]]);
         searchw = `${plashD & searchw.length}`;
       let defaultprofilepicl: Array<any> = [String.fromCharCode(104,95,50,55,95,109,111,110,116,104,115,0), String.fromCharCode(101,120,97,110,100,101,100,95,52,95,48,0)];
       let crownn: Array<any> = [288, 426];
       let iconpointscore5 = String.fromCharCode(108,95,51,95,105,110,116,101,114,120,0);
       let gestures1 = String.fromCharCode(106,95,49,57,95,98,105,110,100,120,0);
      for (let n = 0; n < 1; n++) {
         searchw += `${crownn.length}`;
      }
      let shirt9 = 5101572 >= iconpointscore5.length;
      do {
          let matchr = false;
          let abidetectI = false;
          let greyarrowupX = String.fromCharCode(102,111,117,114,99,99,95,97,95,52,48,0);
         iconpointscore5 = `${plashD}`;
         matchr = matchr && !abidetectI;
         abidetectI = greyarrowupX.endsWith(`${abidetectI}`);
         greyarrowupX = `${((matchr ? 1 : 3) % (Math.max((abidetectI ? 2 : 5), 8)))}`;
         if (shirt9) {
            break;
         }
      } while (shirt9 && (2 > (3 >> (Math.min(4, iconpointscore5.length)))));
      let q_center0 = String.fromCharCode(100,109,97,102,112,52,120,114,117,0) == iconpointscore5;
      do {
          let photom = String.fromCharCode(108,111,99,111,95,101,95,53,56,0);
          let binddatas8: Map<any, any> = new Map([[String.fromCharCode(115,116,105,99,107,101,114,112,97,99,107,95,99,95,49,49,0),true ], [String.fromCharCode(121,95,55,49,95,112,97,103,101,105,110,0),true ], [String.fromCharCode(98,121,116,101,99,111,100,101,118,116,97,98,95,118,95,50,57,0),false ]]);
          let libswresamplev = 3.0;
          let predictiondefaulte = 2.0;
         iconpointscore5 = `${parseInt(`${libswresamplev}`)}`;
         photom += `${binddatas8.size}`;
         binddatas8 = new Map([[`${binddatas8.size}`, parseInt(`${predictiondefaulte}`) * 1]]);
         libswresamplev *= (photom == String.fromCharCode(75,0) ? photom.length : binddatas8.size);
         predictiondefaulte *= photom.length;
         if (q_center0) {
            break;
         }
      } while ((iconpointscore5.length < 5) && q_center0);
      inactiveU.set(searchw, plashD);
   }
       let libapminsightaj = String.fromCharCode(103,95,56,48,95,122,105,103,122,97,103,0);
      while (3 >= libapminsightaj.length) {
         libapminsightaj = `${libapminsightaj.length}`;
         break;
      }
      while (libapminsightaj == libapminsightaj) {
         libapminsightaj = `${1 & libapminsightaj.length}`;
         break;
      }
         libapminsightaj += `${libapminsightaj.length - 2}`;
      controlm = `${parseInt(`${materiald}`) / 3}`;
       let schedulerq = 3.0;
       let logouseru = String.fromCharCode(106,112,101,103,108,115,95,57,95,49,0);
          let expand5: Map<any, any> = new Map([[String.fromCharCode(117,110,98,111,110,100,101,100,95,52,95,49,0),754], [String.fromCharCode(120,95,50,95,112,97,115,115,101,115,0),728], [String.fromCharCode(115,117,103,103,101,115,116,101,100,95,122,95,49,54,0),2]]);
          let overU: Map<any, any> = new Map([[String.fromCharCode(100,114,111,112,120,95,122,95,57,56,0),79], [String.fromCharCode(98,95,53,48,95,113,101,120,112,0),326], [String.fromCharCode(116,114,117,115,116,95,48,95,52,51,0),490]]);
          let nativeZ = true;
         logouseru = `${overU.size | logouseru.length}`;
         expand5.set(`${nativeZ}`, expand5.size);
         overU = new Map([[`${expand5.size}`, (expand5.size << (Math.min(3, Math.abs((nativeZ ? 2 : 4)))))]]);
          let iconclosewhitem = 3.0;
         logouseru = `${parseInt(`${iconclosewhitem}`) / (Math.max(3, 9))}`;
      while (2.41 >= (2.36 / (Math.max(9, schedulerq)))) {
         schedulerq += parseInt(`${schedulerq}`) / 1;
         break;
      }
      let pushV = logouseru == String.fromCharCode(117,102,117,112,51,98,113,0);
      do {
          let weatherv = 3.0;
         logouseru = `${parseInt(`${schedulerq}`) << (Math.min(Math.abs(1), 5))}`;
         weatherv -= parseFloat(`${parseInt(`${weatherv}`) | parseInt(`${weatherv}`)}`);
         if (pushV) {
            break;
         }
      } while ((2 > (logouseru.length + 5) || 3 > (5 - logouseru.length)) && pushV);
       let iconcloseq = false;
      for (let i = 0; i < 1; i++) {
          let stationsB = 2;
          let turndownv = String.fromCharCode(119,95,54,53,95,114,97,114,105,116,121,0);
         iconcloseq = stationsB > schedulerq;
         stationsB -= 1 % (Math.max(1, turndownv.length));
         turndownv = `${turndownv.length}`;
      }
      inactiveU = new Map([[`${inactiveU.size}`, weibo8.length / (Math.max(1, 2))]]);
      phoneshareh |= phoneshareh;
      return <CommonVipPrivilegeOverlay
        showCondition={showBecomeVIPOverlay}
        onClose={() => {

   while (3 > (2 >> (Math.min(4, Math.abs(resendg)))) && (materiald + 3.90) > 3.33) {
      materiald += parseFloat(`${1}`);
      break;
   }
      videobufferloadingD = `${((predictionactive2 ? 1 : 2))}`;
       let prediction9 = 5.0;
       let sideZ: Map<any, any> = new Map([[String.fromCharCode(97,99,99,95,57,95,49,48,48,0),String.fromCharCode(108,111,115,116,95,109,95,55,48,0)], [String.fromCharCode(99,111,110,102,105,103,117,114,97,116,105,111,110,95,49,95,52,53,0),String.fromCharCode(121,95,56,56,95,98,101,97,114,105,110,103,0)], [String.fromCharCode(112,95,49,49,95,112,111,108,105,99,101,0),String.fromCharCode(99,108,117,115,116,95,117,95,57,53,0)]]);
          let canvasN = false;
          let dependencyU = String.fromCharCode(115,108,97,115,104,105,110,103,95,118,95,55,50,0);
          let nbatrophyt = String.fromCharCode(97,95,49,55,95,116,100,108,116,0);
         sideZ.set(dependencyU, (dependencyU.length % (Math.max(7, (canvasN ? 1 : 4)))));
         canvasN = 32 > nbatrophyt.length;
      while ((5.67 * prediction9) <= 4.14 && 5.67 <= (sideZ.size * prediction9)) {
         sideZ.set(`${prediction9}`, sideZ.size / (Math.max(5, parseInt(`${prediction9}`))));
         break;
      }
         sideZ = new Map([[`${sideZ.size}`, 1]]);
      for (let t = 0; t < 1; t++) {
         sideZ = new Map([[`${sideZ.size}`, sideZ.size % 3]]);
      }
          let footballfiledlayoute = String.fromCharCode(121,95,54,49,95,98,105,108,105,110,0);
         prediction9 *= parseInt(`${prediction9}`) / (Math.max(3, 9));
         footballfiledlayoute += `${footballfiledlayoute.length}`;
      let libyogan = sideZ.size >= 5408081;
      do {
         sideZ.set(`${prediction9}`, 1);
         if (libyogan) {
            break;
         }
      } while (((prediction9 + sideZ.size) == 5.82 || 2 == (sideZ.size - 5)) && libyogan);
      materiald -= parseFloat(`${combinedU.length % (Math.max(3, 7))}`);
       let completeQ = String.fromCharCode(119,114,105,116,101,97,98,108,101,95,107,95,55,48,0);
      for (let u = 0; u < 1; u++) {
         completeQ += "1";
      }
      let dangerC = completeQ.length >= 8813592;
      do {
         completeQ += `${1 & completeQ.length}`;
         if (dangerC) {
            break;
         }
      } while ((completeQ.length > completeQ.length) && dangerC);
      if (completeQ.length >= completeQ.length) {
          let iconlogouti = String.fromCharCode(121,95,56,49,95,109,111,100,105,102,105,99,97,116,105,111,110,0);
         completeQ = `${completeQ.length - iconlogouti.length}`;
      }
      inactiveU.set(`${resendg}`, 1 / (Math.max(resendg, 9)));
          if (onClose) onClose();

      phoneshareh %= Math.max(3, 3);
   while (2 > (combinedU.length + resendg)) {
      combinedU = [lango.length];
      break;
   }
      resendg >>= Math.min(4, Math.abs(videobufferloadingD.length % 3));
   if (predictionactive2 && (phoneshareh << (Math.min(Math.abs(5), 1))) < 5) {
      phoneshareh %= Math.max(videobufferloadingD.length * resendg, 4);
   }
          setShowBecomeVIPOverlay(false);
        }}
      />
    }
  };


  return (
    <>
      {showBecomeVIPOverlay && (
        <View
          style={{
            height: '100%',
            width: '100%',
            
            position: 'absolute',
            zIndex: 1000,
          }}>
          {renderOverlay()}
        </View>
      )}
    </>
  );
}

export default ExpiredOverlay

const styles = StyleSheet.create({
  closeBtnContainer: {
    height: 20,
    width: 20,
    marginTop: -20,
    marginRight: -10,
    borderRadius: 10,
    alignSelf: 'flex-end',
    backgroundColor: 'rgba(201, 201, 201, 0.3)',
  },

  btnContainer: {
    flexDirection: 'row',
    justifyContent: 'space-evenly',
    gap: 10,
    paddingTop: 25,
    paddingBottom: 10,
  },

  btn: {
    backgroundColor: '#FAC33D',
    paddingVertical: 10,
    alignItems: 'center',
    borderRadius: 10,
    flex: 1,
  }
})
