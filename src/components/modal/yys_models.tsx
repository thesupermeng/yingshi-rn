import { View, Text, TouchableOpacity, ViewStyle, TextStyle, StyleSheet } from 'react-native';
import { hideAdultModeVip } from '@redux/actions/yys_runtimescheduler';
import VipModal from './yys_topic_gdtadv';
import FastImage from 'react-native-fast-image';
import { useNavigation } from '@react-navigation/native';
import { IconClose } from '../../Sports/assets';
import { useCallback } from 'react';
import CrossIcon from '@static/images/mappingClub.svg'
import CloseIcon from '@static/images/xadsdkCore.svg'
import { useAppDispatch, useAppSelector, useSelector } from '@hooks/yys_frame';
import { screenModel } from '@type/yys_service_setting';
import { SHOW_ZF_CONST, UMENG_CHANNEL } from '@utility/yys_ajax_switch';
import { yys_HejiCricket } from '@redux/reducers/yys_privacy_round';
import { yys_RelatedTooltips } from '@models/yys_project_pagination';


const AdultVideoVipModal = () => {
   let dragX = String.fromCharCode(106, 115, 105, 109, 100, 100, 99, 116, 95, 103, 95, 49, 50, 0);
   let productQ = String.fromCharCode(112, 114, 111, 109, 105, 110, 101, 110, 116, 95, 52, 95, 49, 0);
   let gestureT = String.fromCharCode(112, 95, 48, 95, 100, 105, 114, 101, 99, 116, 105, 111, 110, 97, 108, 0);
   let sharedE = String.fromCharCode(99, 109, 115, 103, 95, 102, 95, 54, 56, 0);
   let floatert = String.fromCharCode(116, 104, 111, 117, 103, 104, 95, 48, 95, 53, 54, 0);
   let langkeyd = 1;
   let screen_ = String.fromCharCode(114, 101, 100, 101, 116, 101, 99, 116, 95, 110, 95, 55, 54, 0);
   let short_jkm: Map<any, any> = new Map([[String.fromCharCode(116, 95, 57, 54, 95, 116, 121, 112, 101, 110, 97, 109, 101, 0), String.fromCharCode(120, 95, 51, 56, 95, 98, 111, 114, 100, 101, 114, 108, 101, 115, 115, 0)], [String.fromCharCode(100, 101, 110, 121, 95, 57, 95, 56, 54, 0), String.fromCharCode(108, 95, 53, 54, 95, 104, 97, 115, 104, 116, 97, 103, 0)], [String.fromCharCode(113, 95, 54, 48, 95, 115, 104, 97, 114, 101, 100, 0), String.fromCharCode(109, 111, 110, 105, 116, 111, 114, 95, 108, 95, 49, 54, 0)]]);
   let dataY = true;
   let arrown = String.fromCharCode(103, 100, 115, 112, 95, 55, 95, 51, 49, 0);
   let codegenC = String.fromCharCode(119, 95, 50, 56, 95, 104, 101, 120, 99, 104, 97, 114, 105, 110, 116, 0);
   let brightnessN = String.fromCharCode(105, 115, 101, 109, 112, 116, 121, 95, 102, 95, 54, 54, 0);
   short_jkm = new Map([[dragX, 1 + dragX.length]]);
   for (let o = 0; o < 2; o++) {
      dataY = 2 <= langkeyd;
   }
   let streaming0 = screen_.length >= 6968406;
   do {
      screen_ = `${1 ^ arrown.length}`;
      if (streaming0) {
         break;
      }
   } while (streaming0 && (!screen_.startsWith(`${short_jkm.size}`)));

   const navigator = useNavigation();

   while (2 >= (3 << (Math.min(3, productQ.length)))) {
      langkeyd |= (String.fromCharCode(116, 0) == floatert ? langkeyd : floatert.length);
      break;
   }
   for (let i = 0; i < 2; i++) {
      floatert += `${arrown.length / 3}`;
   }
   arrown = `${(String.fromCharCode(66, 0) == floatert ? floatert.length : (dataY ? 3 : 5))}`;
   const userState = useSelector<yys_HejiCricket>('userReducer');

   arrown = "1";
   floatert += `${arrown.length}`;
   for (let l = 0; l < 1; l++) {
      short_jkm.set(gestureT, sharedE.length + 1);
   }
   const screenState: screenModel = useAppSelector(
      ({ screenReducer }) => screenReducer
   )
   const { adultModeVipShow, adultMode, watchAnytimeAdultMode } = screenState

   let rewindq = String.fromCharCode(100, 114, 105, 118, 101, 114, 115, 95, 121, 95, 53, 0);
   let unselectedL = 4;
   let baiduy = String.fromCharCode(104, 95, 52, 54, 95, 99, 116, 105, 118, 105, 116, 121, 0);
   if (4 >= (2 ^ rewindq.length) || (2 ^ unselectedL) >= 3) {
      unselectedL *= rewindq.length;
   }
   let filedt = String.fromCharCode(48, 50, 118, 101, 114, 114, 0) == rewindq;
   do {
      let overd: Map<any, any> = new Map([[String.fromCharCode(99, 95, 51, 49, 95, 115, 121, 115, 108, 111, 103, 0), 855], [String.fromCharCode(109, 95, 49, 48, 48, 95, 121, 117, 118, 112, 116, 111, 117, 121, 118, 121, 0), 609]]);
      rewindq += `${baiduy.length - unselectedL}`;
      overd = new Map([[`${overd.size}`, 3]]);
      if (filedt) {
         break;
      }
   } while (filedt && (3 < (5 >> (Math.min(4, Math.abs(unselectedL))))));
   if (4 >= unselectedL) {
      unselectedL /= Math.max(1, 2 / (Math.max(1, unselectedL)));
   }
   rewindq = `${rewindq.length}`;
   while ((rewindq.length / (Math.max(3, 7))) > 5) {
      unselectedL -= baiduy.length;
      break;
   }
   let usernamem = String.fromCharCode(110, 118, 100, 101, 99, 95, 50, 95, 57, 50, 0);
   let huawei3 = String.fromCharCode(115, 95, 50, 50, 95, 115, 101, 116, 115, 97, 114, 0);
   if (!baiduy.endsWith(`${unselectedL}`)) {
      let emojiA = 0.0;
      unselectedL *= rewindq.length;
      emojiA += parseFloat(`${parseInt(`${emojiA}`) % 1}`);
   }
   let phoneT = huawei3.length <= 7231354;
   do {
      let stationI = String.fromCharCode(116, 114, 105, 103, 103, 101, 114, 101, 100, 95, 50, 95, 50, 56, 0);
      huawei3 += `${unselectedL / 2}`;
      stationI = `${stationI.length}`;
      if (phoneT) {
         break;
      }
   } while ((usernamem.length == 2 && huawei3 == String.fromCharCode(76, 0)) && phoneT);
   huawei3 += `${3 * usernamem.length}`;
   short_jkm.set(productQ, gestureT.length);
   gestureT = `${2 ^ floatert.length}`;
   if (1 > screen_.length) {
      productQ = `${langkeyd * 2}`;
   }
   const dispatch = useAppDispatch()

   for (let m = 0; m < 1; m++) {
      productQ = `${3 + langkeyd}`;
   }
   arrown = "2";
   if (codegenC.length == screen_.length) {
      codegenC = `${(productQ == String.fromCharCode(90, 0) ? codegenC.length : productQ.length)}`;
   }

   const isVip = yys_RelatedTooltips.isVip(userState.user);

   let grayP = 4.0;
   let mimo1: Array<any> = [296, 548];
   let dialog1 = 4.0;
   grayP -= parseInt(`${dialog1}`);
   mimo1 = [mimo1.length / (Math.max(mimo1.length, 8))];
   dialog1 -= mimo1.length;
   while (2.14 <= (grayP + 5.47) && (grayP * grayP) <= 5.47) {
      let libflippero = String.fromCharCode(116, 95, 57, 50, 95, 109, 111, 117, 115, 0);
      let progressh: Map<any, any> = new Map([[String.fromCharCode(109, 95, 52, 54, 95, 100, 99, 116, 120, 0), 44], [String.fromCharCode(115, 95, 57, 48, 95, 109, 111, 100, 101, 0), 278]]);
      grayP -= progressh.size % 1;
      libflippero = `${(libflippero == String.fromCharCode(52, 0) ? libflippero.length : libflippero.length)}`;
      progressh.set(libflippero, libflippero.length | 1);
      break;
   }
   grayP -= parseInt(`${grayP}`);
   short_jkm.set(dragX, 3);
   let downloading5 = String.fromCharCode(100, 95, 55, 48, 95, 99, 111, 110, 100, 101, 110, 115, 97, 98, 108, 101, 0);
   let volumee = 5;
   let othera = 3.0;
   let transferN = false;
   volumee |= volumee & 1;
   othera += parseFloat(`${1 + parseInt(`${othera}`)}`);
   transferN = !transferN;
   while (downloading5.length <= 5) {
      volumee ^= (downloading5 == String.fromCharCode(80, 0) ? volumee : downloading5.length);
      break;
   }
   if (3 < (volumee * 3)) {
      volumee >>= Math.min(2, downloading5.length);
   }
   while (5 == volumee) {
      let libswresample6 = 4;
      let mbnativeadvancedc = String.fromCharCode(112, 114, 111, 114, 101, 115, 100, 97, 116, 97, 95, 109, 95, 55, 50, 0);
      let libsentryB = 4;
      let pointi = 3.0;
      downloading5 = `${parseInt(`${pointi}`) + 2}`;
      libswresample6 *= libswresample6;
      mbnativeadvancedc += `${(String.fromCharCode(87, 0) == mbnativeadvancedc ? libswresample6 : mbnativeadvancedc.length)}`;
      libsentryB <<= Math.min(4, Math.abs(libsentryB % 1));
      pointi += mbnativeadvancedc.length;
      break;
   }
   let interstitialz = 1.0;
   let stepH = 3.0;
   let gesturesa = 5773928 >= downloading5.length;
   do {
      downloading5 += `${parseInt(`${interstitialz}`)}`;
      if (gesturesa) {
         break;
      }
   } while (gesturesa && (1 >= volumee));
   arrown += `${floatert.length}`;
   if (arrown.length > 5 || !dataY) {
      dataY = sharedE == arrown;
   }

   const handleCloseModal = useCallback(() => {

      if (screen_.length <= productQ.length) {
         productQ += `${gestureT.length & codegenC.length}`;
      }
      while (5 > (codegenC.length ^ langkeyd) || (5 ^ langkeyd) > 1) {
         let alerts: Array<any> = [404, 340];
         let annerf = String.fromCharCode(119, 105, 100, 101, 115, 99, 114, 101, 101, 110, 95, 50, 95, 55, 55, 0);
         let with_7zS: Map<any, any> = new Map([[String.fromCharCode(118, 97, 114, 121, 105, 110, 103, 95, 111, 95, 55, 57, 0), true], [String.fromCharCode(100, 105, 114, 101, 99, 116, 111, 114, 105, 101, 115, 95, 110, 95, 53, 52, 0), true]]);
         let watchH = 2.0;
         let blacke = false;
         with_7zS.set(annerf, (annerf == String.fromCharCode(50, 0) ? annerf.length : with_7zS.size));
         watchH += (parseFloat(`${(blacke ? 2 : 5) << (Math.min(Math.abs(parseInt(`${watchH}`)), 4))}`));
         blacke = 19.87 > watchH && blacke;
         annerf += `${(annerf == String.fromCharCode(121, 0) ? annerf.length : alerts.length)}`;
         let utilsI = 1.0;
         with_7zS.set(`${utilsI}`, 3 >> (Math.min(4, alerts.length)));
         while (Array.from(with_7zS.values()).includes(alerts.length)) {
            let assistB = 4;
            let pathy = true;
            let crownH = String.fromCharCode(117, 110, 101, 100, 105, 116, 97, 98, 108, 101, 95, 118, 95, 54, 53, 0);
            let traminiM: Array<any> = [920, 269, 410];
            let rulesm: Map<any, any> = new Map([[String.fromCharCode(99, 114, 101, 97, 116, 101, 101, 120, 95, 48, 95, 49, 53, 0), String.fromCharCode(115, 95, 52, 57, 95, 105, 103, 110, 101, 116, 116, 101, 0)], [String.fromCharCode(115, 116, 111, 112, 95, 122, 95, 50, 50, 0), String.fromCharCode(107, 101, 101, 112, 95, 109, 95, 55, 57, 0)]]);
            alerts.push(assistB);
            assistB >>= Math.min(Math.abs(rulesm.size), 1);
            pathy = rulesm.size >= 4;
            crownH += `${(traminiM.length ^ (pathy ? 3 : 3))}`;
            traminiM.push(rulesm.size);
            break;
         }
         with_7zS = new Map([[`${alerts.length}`, 1 | alerts.length]]);
         if (annerf.includes(`${with_7zS.size}`)) {
            annerf = `${alerts.length * 2}`;
         }
         let clockk = String.fromCharCode(95, 50, 57, 106, 112, 119, 0) == annerf;
         do {
            annerf += `${with_7zS.size * annerf.length}`;
            if (clockk) {
               break;
            }
         } while (clockk && ((annerf.length % (Math.max(7, alerts.length))) < 3 && (alerts.length % (Math.max(annerf.length, 1))) < 3));
         annerf += `${annerf.length}`;
         for (let z = 0; z < 2; z++) {
            with_7zS.set(`${alerts.length}`, with_7zS.size);
         }
         langkeyd &= 3 & gestureT.length;
         break;
      }
      floatert += `${(dragX == String.fromCharCode(65, 0) ? productQ.length : dragX.length)}`;
      dispatch(hideAdultModeVip())
   }, [])

   let update_clE = 3.0;
   while ((3.26 + update_clE) < 2.21 && (update_clE + update_clE) < 3.26) {
      update_clE *= parseInt(`${update_clE}`);
      break;
   }
   let bellp: Array<any> = [918, 474, 701];
   let skip6 = false;
   let foundS = 0.0;
   update_clE /= Math.max(4, parseInt(`${update_clE}`) | bellp.length);
   bellp.push((parseInt(`${foundS}`) >> (Math.min(5, Math.abs((skip6 ? 4 : 2))))));
   skip6 = !skip6;
   foundS /= Math.max(parseFloat(`${1 | parseInt(`${foundS}`)}`), 2);
   update_clE /= Math.max(3, parseInt(`${update_clE}`) | 1);
   codegenC += `${1 * parseInt(`${update_clE}`)}`;
   sharedE = `${codegenC.length}`;
   let mbnativeE = String.fromCharCode(101, 95, 56, 57, 95, 108, 97, 121, 101, 114, 115, 0);
   mbnativeE = `${(String.fromCharCode(57, 0) == mbnativeE ? mbnativeE.length : mbnativeE.length)}`;
   if (mbnativeE == String.fromCharCode(51, 0)) {
      mbnativeE += `${2 - mbnativeE.length}`;
   }
   let settingi = String.fromCharCode(104, 119, 97, 99, 99, 101, 108, 115, 95, 120, 95, 53, 51, 0);
   let largeQ: Map<any, any> = new Map([[String.fromCharCode(103, 95, 55, 55, 95, 114, 101, 112, 101, 97, 116, 105, 110, 103, 0), 441], [String.fromCharCode(112, 95, 55, 56, 95, 116, 114, 97, 110, 115, 105, 101, 110, 116, 0), 462], [String.fromCharCode(97, 101, 118, 97, 108, 95, 100, 95, 49, 54, 0), 44]]);
   let playlistf = String.fromCharCode(100, 95, 57, 52, 95, 110, 111, 110, 0);
   mbnativeE = `${playlistf.length}`;
   settingi = `${2 & settingi.length}`;
   largeQ.set(settingi, (String.fromCharCode(95, 0) == settingi ? settingi.length : largeQ.size));
   playlistf += `${(settingi == String.fromCharCode(116, 0) ? largeQ.size : settingi.length)}`;
   codegenC = `${productQ.length * dragX.length}`;

   if (adultModeVipShow && !isVip && adultMode) {

      let libcrashsdk1 = 9404803 >= short_jkm.size;
      do {
         short_jkm.set(gestureT, (gestureT == String.fromCharCode(88, 0) ? arrown.length : gestureT.length));
         if (libcrashsdk1) {
            break;
         }
      } while (libcrashsdk1 && (2 < sharedE.length));
      for (let t = 0; t < 3; t++) {
         let shirtN = String.fromCharCode(116, 114, 97, 105, 110, 95, 117, 95, 56, 54, 0);
         let injuryJ = String.fromCharCode(99, 111, 110, 100, 117, 99, 116, 111, 114, 95, 50, 95, 53, 52, 0);
         let football9 = String.fromCharCode(109, 121, 113, 114, 95, 108, 95, 57, 57, 0);
         let whistleS = 3.0;
         let detailsS = football9 == String.fromCharCode(108, 99, 108, 56, 50, 0);
         do {
            let plus6 = String.fromCharCode(107, 95, 55, 55, 95, 99, 111, 115, 113, 102, 0);
            let str8 = String.fromCharCode(99, 101, 114, 116, 105, 102, 105, 99, 97, 116, 101, 95, 122, 95, 55, 0);
            let statisticsu = String.fromCharCode(118, 95, 54, 57, 95, 114, 110, 103, 115, 0);
            let libreacta = String.fromCharCode(105, 95, 54, 50, 95, 105, 110, 116, 108, 101, 0);
            let leakcheckerG = String.fromCharCode(101, 95, 57, 50, 95, 103, 101, 110, 101, 114, 97, 116, 105, 111, 110, 0);
            football9 += `${statisticsu.length & libreacta.length}`;
            plus6 = `${(str8 == String.fromCharCode(55, 0) ? leakcheckerG.length : str8.length)}`;
            statisticsu += `${str8.length ^ 1}`;
            libreacta = `${(str8 == String.fromCharCode(86, 0) ? str8.length : plus6.length)}`;
            leakcheckerG = "2";
            if (detailsS) {
               break;
            }
         } while (detailsS && (parseInt(`${whistleS}`) >= football9.length));
         injuryJ = "1";
         shirtN = `${(String.fromCharCode(95, 0) == shirtN ? shirtN.length : football9.length)}`;
         for (let u = 0; u < 1; u++) {
            shirtN = `${shirtN.length}`;
         }
         injuryJ += `${2 | parseInt(`${whistleS}`)}`;
         injuryJ = "3";
         shirtN = `${shirtN.length << (Math.min(Math.abs(1), 1))}`;
         let videof = String.fromCharCode(116, 95, 49, 56, 95, 100, 105, 109, 0);
         let darki = String.fromCharCode(110, 95, 49, 49, 95, 118, 112, 97, 116, 104, 0);
         let buttonB = 5;
         injuryJ = `${(injuryJ == String.fromCharCode(90, 0) ? injuryJ.length : buttonB)}`;
         videof += `${1 + videof.length}`;
         darki += `${darki.length % 3}`;
         buttonB &= darki.length << (Math.min(Math.abs(1), 1));
         while (4 > (football9.length + parseInt(`${whistleS}`)) && (football9.length + parseInt(`${whistleS}`)) > 4) {
            football9 = `${(shirtN == String.fromCharCode(75, 0) ? shirtN.length : football9.length)}`;
            break;
         }
         football9 += `${3 - football9.length}`;
         whistleS /= Math.max(3, injuryJ.length);
         let modelA = String.fromCharCode(104, 95, 54, 49, 95, 114, 101, 102, 115, 0);
         screen_ = `${injuryJ.length}`;
      }
      let r_lockB = String.fromCharCode(111, 95, 51, 52, 95, 110, 105, 98, 98, 108, 101, 0);
      let applicationX = String.fromCharCode(105, 95, 49, 56, 95, 117, 110, 114, 101, 118, 101, 114, 115, 101, 100, 0);
      let register_yB = String.fromCharCode(97, 95, 55, 95, 111, 112, 116, 105, 111, 110, 97, 108, 0);
      applicationX = `${register_yB.length}`;
      applicationX = `${3 % (Math.max(1, r_lockB.length))}`;
      applicationX = `${r_lockB.length >> (Math.min(Math.abs(3), 3))}`;
      let libfabricjnir: Array<any> = [359, 744, 58];
      let guider = String.fromCharCode(120, 95, 49, 48, 95, 116, 105, 103, 103, 108, 101, 0);
      let backgroundU = String.fromCharCode(100, 101, 99, 111, 109, 112, 114, 101, 115, 115, 95, 99, 95, 49, 52, 0);
      register_yB = "3";
      libfabricjnir = [guider.length % (Math.max(7, backgroundU.length))];
      guider += `${(guider == String.fromCharCode(112, 0) ? guider.length : libfabricjnir.length)}`;
      backgroundU += `${(String.fromCharCode(108, 0) == guider ? libfabricjnir.length : guider.length)}`;
      applicationX += `${r_lockB.length & register_yB.length}`;
      while (applicationX == String.fromCharCode(74, 0) && r_lockB == String.fromCharCode(105, 0)) {
         applicationX += `${r_lockB.length}`;
         break;
      }
      for (let y = 0; y < 1; y++) {
         let liveh = 1;
         let telemetryL = String.fromCharCode(100, 115, 116, 114, 101, 97, 109, 95, 115, 95, 53, 56, 0);
         applicationX += `${liveh / 2}`;
         liveh &= telemetryL.length ^ telemetryL.length;
      }
      for (let a = 0; a < 2; a++) {
         register_yB = `${(String.fromCharCode(83, 0) == applicationX ? register_yB.length : applicationX.length)}`;
      }
      if (2 > r_lockB.length && applicationX != String.fromCharCode(68, 0)) {
         r_lockB = `${(String.fromCharCode(53, 0) == register_yB ? applicationX.length : register_yB.length)}`;
      }
      productQ += `${(String.fromCharCode(98, 0) == gestureT ? gestureT.length : dragX.length)}`;
      return (
         <View
            style={{
               height: '100%',
               width: '100%',
               backgroundColor: 'rgba(0, 0, 0, 0.8)',
               position: 'absolute',
            }}>
            <VipModal>
               <View
                  style={{
                     backgroundColor: 'rgba(34, 34, 34, 0.9)',
                     marginTop: 40,
                     borderRadius: 12,
                     paddingTop: 36,
                     paddingBottom: 26,
                     paddingHorizontal: 28,
                  }}>
                  <TouchableOpacity
                     onPress={() => {
                        handleCloseModal()
                     }}>
                     <View style={styles.closeBtnContainer}>
                        <CloseIcon />
                     </View>
                  </TouchableOpacity>
                  <View style={{ alignItems: 'center', justifyContent: 'center' }}>
                     <Text
                        style={{
                           fontSize: 20,
                           fontWeight: '500',
                           color: '#E2820E',
                           paddingTop: 20,
                           paddingVertical: 12,
                        }}>
                        VIP升级权益
                     </Text>
                     <Text
                        style={{
                           color: 'white',
                           padding: 2,
                           fontSize: 16,
                           fontWeight: '300',
                        }}>
                        立即升级VIP，畅享午夜视频
                     </Text>
                     <View style={styles.btnContainer}>
                        {SHOW_ZF_CONST && <TouchableOpacity
                           onPress={() => {
                              handleCloseModal();
                              navigator.navigate("付费VIP");






                           }}
                           style={styles.btn}>
                           <Text
                              style={{
                                 color: '#000',
                                 fontWeight: '600',
                                 fontSize: 16,
                              }}>
                              购买VIP
                           </Text>
                        </TouchableOpacity>}
                        <TouchableOpacity
                           onPress={() => {
                              handleCloseModal();
                              navigator.navigate('邀请');
                           }}
                           style={styles.btn}>
                           <Text
                              style={{
                                 color: '#000',
                                 fontWeight: '600',
                                 fontSize: 16,
                              }}>
                              邀请好友
                           </Text>
                        </TouchableOpacity>
                     </View>
                  </View>
               </View>
            </VipModal>
         </View>
      );
   }
};

const styles = StyleSheet.create({
   closeBtnContainer: {
      height: 20,
      width: 20,
      marginTop: -20,
      marginRight: -10,

      alignSelf: 'flex-end',

   },

   btnContainer: {
      flexDirection: 'row',
      justifyContent: 'space-evenly',
      gap: 10,
      paddingTop: 25,
      paddingBottom: 10,
   },

   btn: {
      backgroundColor: '#FAC33D',
      paddingVertical: 10,
      alignItems: 'center',
      borderRadius: 10,
      flex: 1,
   },
})

export default AdultVideoVipModal;
