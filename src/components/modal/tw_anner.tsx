import { useNavigation } from "@react-navigation/native";
import VipPrivilegeModal from "./spf_sound"
import { useCallback, useEffect } from "react";
import { useAppDispatch, useAppSelector, useSelector } from "@hooks/kg_index";
import { hideAdultVipPrivilegeMiniVideoAction, showAdultVipPrivilegeMiniVideoAction, showLoginAction } from "@redux/actions/a_switch";
import { View } from "react-native";
import { screenModel } from "@type/nb_shared_target";
import umb_center_carousel from "../../../Umeng/umb_center_carousel";
import { UMENG_CHANNEL } from "@utility/n_subs_interstitial";
import { KWConstants } from "@models/kl_sheet";
import { HDTGesturesList } from "@redux/reducers/f_gmail";

const sportModels = require('@static/images/hooksPromotionBack.png');
const sportBg = require('@static/images/googleActionsZhengpian.png');

interface XFillButton {
  showCondition: boolean;
  onClose: ({ isAutoClose }: { isAutoClose?: boolean }) => void;
  showBlur?: boolean;
}

export const SportVipPrivilegeOverlay = ({ showCondition, onClose, showBlur }: XFillButton) => {

  const navigator = useNavigation()
  const dispatch = useAppDispatch()
  const screenState: screenModel = useAppSelector(
    ({ screenReducer }) => screenReducer
  );
  const userState = useSelector<HDTGesturesList>('userReducer');

  const handleOnPurchase = useCallback(() => {
       let routerC = 3.0;
    let questW = 3;
    let larger = String.fromCharCode(117,95,55,95,98,114,117,115,104,0);
    let team4 = String.fromCharCode(100,108,115,121,109,95,118,95,49,50,0);
    let eighteenR = 3.0;
    let about0 = 2;
    let nalyticsq = String.fromCharCode(115,95,49,56,95,112,98,107,100,102,0);
    let signinupn = String.fromCharCode(121,95,55,50,95,102,111,114,109,0);
    let countryf = String.fromCharCode(103,95,48,95,102,97,110,99,121,0);
    let expiredy = true;
   if (nalyticsq == countryf) {
      countryf += `${signinupn.length - 1}`;
   }
      routerC -= parseFloat(`${parseInt(`${eighteenR}`) & about0}`);
      nalyticsq += `${signinupn.length << (Math.min(Math.abs(2), 3))}`;

    onClose({ isAutoClose: true })
    navigator.navigate("付费VIP");

       let gmailw = String.fromCharCode(114,95,53,48,95,99,105,114,99,108,101,100,0);
       let views6 = false;
         gmailw = `${(gmailw.length + (views6 ? 4 : 1))}`;
       let model = 2.0;
          let mailg = true;
         model += (parseFloat(`${String.fromCharCode(71,0) == gmailw ? (views6 ? 3 : 4) : gmailw.length}`));
         mailg = !mailg;
         gmailw = `${3 >> (Math.min(5, Math.abs(parseInt(`${model}`))))}`;
          let text0 = String.fromCharCode(98,117,105,108,116,95,121,95,51,50,0);
          let containerL = 4.0;
          let userx = String.fromCharCode(100,105,102,102,101,114,101,110,99,101,95,101,95,56,0);
         gmailw = `${parseInt(`${containerL}`) - parseInt(`${model}`)}`;
         text0 += `${(String.fromCharCode(65,0) == userx ? userx.length : text0.length)}`;
         containerL += text0.length;
          let ajaxd: Array<any> = [908, 454];
          let flyerA = false;
          let dragI: Array<any> = [822, 50];
         model *= (parseFloat(`${(views6 ? 5 : 3) / (Math.max(parseInt(`${model}`), 9))}`));
         ajaxd.push(((flyerA ? 4 : 5) | dragI.length));
         flyerA = ajaxd.length < 39;
         dragI = [dragI.length % 2];
      about0 /= Math.max(((views6 ? 5 : 3) ^ signinupn.length), 5);
   let plusL = about0 >= 8008544;
   do {
       let stationsW: Array<any> = [221, 788];
       let layout4 = 1.0;
         stationsW = [stationsW.length];
      if (stationsW.length > parseInt(`${layout4}`)) {
         stationsW = [parseInt(`${layout4}`) * stationsW.length];
      }
      while (stationsW.length == 1) {
          let gmailn = String.fromCharCode(98,95,57,95,109,97,107,101,109,97,99,112,107,103,0);
          let username1: Map<any, any> = new Map([[String.fromCharCode(106,95,49,55,95,105,110,97,99,116,105,118,101,0),137], [String.fromCharCode(115,95,57,48,95,99,111,109,112,111,115,101,0),808]]);
         layout4 -= parseFloat(`${3}`);
         gmailn = `${gmailn.length & username1.size}`;
         username1 = new Map([[`${username1.size}`, username1.size]]);
         break;
      }
      for (let k = 0; k < 3; k++) {
         stationsW = [1];
      }
      for (let f = 0; f < 3; f++) {
         stationsW.push(parseInt(`${layout4}`));
      }
         layout4 *= parseFloat(`${stationsW.length}`);
      about0 <<= Math.min(3, Math.abs(3 + parseInt(`${eighteenR}`)));
      if (plusL) {
         break;
      }
   } while (plusL && ((nalyticsq.length & about0) >= 3));
      questW ^= questW | signinupn.length;
    

       let sliderj = 0.0;
       let networkP = String.fromCharCode(104,97,100,97,109,97,114,100,120,95,102,95,57,56,0);
       let q_playerK = String.fromCharCode(100,101,99,101,108,101,114,97,116,105,111,110,95,103,95,57,51,0);
          let statisticsO: Array<any> = [576, 23];
         sliderj -= parseFloat(`${2 >> (Math.min(2, statisticsO.length))}`);
      if (q_playerK != String.fromCharCode(87,0)) {
          let aboutG = 1.0;
          let penalty_ = 0.0;
          let singapore8 = true;
          let interstitialo = 3.0;
         networkP += `${2 + parseInt(`${interstitialo}`)}`;
         aboutG /= Math.max(parseFloat(`${1 | parseInt(`${penalty_}`)}`), 3);
         penalty_ *= parseFloat(`${parseInt(`${aboutG}`)}`);
         singapore8 = !singapore8;
         interstitialo += (parseFloat(`${(singapore8 ? 2 : 3) + parseInt(`${penalty_}`)}`));
      }
         q_playerK = `${2 ^ networkP.length}`;
         networkP += `${q_playerK.length % (Math.max(6, networkP.length))}`;
      let stepE = 6600633 <= networkP.length;
      do {
          let annerW: Map<any, any> = new Map([[String.fromCharCode(99,95,50,48,95,114,101,108,105,97,98,108,101,0),String.fromCharCode(111,95,57,53,95,100,101,99,105,115,105,111,110,0)], [String.fromCharCode(122,95,55,49,95,115,115,101,116,0),String.fromCharCode(109,111,116,105,111,110,95,109,95,53,0)]]);
         networkP = `${parseInt(`${sliderj}`)}`;
         annerW.set(`${annerW.size}`, annerW.size);
         if (stepE) {
            break;
         }
      } while ((4 < (parseInt(`${sliderj}`) + networkP.length) && 4.41 < (sliderj + parseFloat(`${networkP.length}`))) && stepE);
      if (q_playerK.length == 5) {
         networkP += `${parseInt(`${sliderj}`) >> (Math.min(networkP.length, 4))}`;
      }
         q_playerK += "3";
      for (let i = 0; i < 1; i++) {
          let settingi = String.fromCharCode(120,112,97,108,100,118,95,122,95,54,51,0);
          let whitem = String.fromCharCode(113,95,50,52,95,105,110,116,101,114,115,101,99,116,115,0);
          let weiboh = 1.0;
          let adultZ = false;
          let policyV = 0.0;
         networkP += `${settingi.length << (Math.min(whitem.length, 5))}`;
         settingi = `${((adultZ ? 4 : 3) & parseInt(`${weiboh}`))}`;
         whitem += `${2 & parseInt(`${weiboh}`)}`;
         adultZ = !adultZ;
         policyV -= ((adultZ ? 5 : 3) & parseInt(`${policyV}`));
      }
         sliderj += (parseFloat(`${String.fromCharCode(120,0) == q_playerK ? networkP.length : q_playerK.length}`));
      eighteenR *= parseFloat(`${signinupn.length << (Math.min(Math.abs(1), 2))}`);
   for (let n = 0; n < 3; n++) {
      routerC += parseFloat(`${parseInt(`${routerC}`) << (Math.min(larger.length, 4))}`);
   }
   for (let g = 0; g < 2; g++) {
      larger += `${(String.fromCharCode(57,0) == signinupn ? questW : signinupn.length)}`;
   }
    

   while (eighteenR >= 5.6) {
       let predictiond: Map<any, any> = new Map([[String.fromCharCode(112,97,97,100,95,107,95,55,52,0),399], [String.fromCharCode(104,119,100,101,118,105,99,101,95,117,95,53,50,0),437]]);
       let checkboxL = 3.0;
       let firebase1 = String.fromCharCode(102,95,50,54,95,101,118,101,114,0);
       let play8 = String.fromCharCode(111,103,103,105,110,103,95,102,95,50,0);
       let singaporeU = 4;
          let countryC = 0;
          let u_lockZ = false;
         play8 = `${2 ^ countryC}`;
         countryC <<= Math.min(5, Math.abs(1));
          let eighteenl: Map<any, any> = new Map([[String.fromCharCode(118,95,50,49,0),369], [String.fromCharCode(100,105,115,116,114,97,99,116,105,111,110,95,105,95,49,53,0),111]]);
         predictiond = new Map([[`${predictiond.size}`, predictiond.size >> (Math.min(1, Math.abs(parseInt(`${checkboxL}`))))]]);
         eighteenl = new Map([[`${eighteenl.size}`, 3]]);
          let control2: Array<any> = [764, 415];
          let selectV = 0;
         singaporeU += 1 & predictiond.size;
         control2.push(control2.length - selectV);
         selectV ^= selectV;
      while (predictiond.size >= 2) {
         predictiond.set(`${checkboxL}`, predictiond.size);
         break;
      }
         firebase1 = `${firebase1.length >> (Math.min(Math.abs(2), 4))}`;
          let windz = 1;
          let analyticsH: Map<any, any> = new Map([[String.fromCharCode(99,111,109,98,105,110,101,100,95,98,95,56,54,0),574], [String.fromCharCode(111,95,54,53,95,119,105,116,104,0),254], [String.fromCharCode(112,97,115,115,105,118,101,95,119,95,55,53,0),311]]);
          let update_qT = String.fromCharCode(114,101,103,101,120,112,95,53,95,55,52,0);
         firebase1 += `${singaporeU}`;
         windz += windz * analyticsH.size;
         analyticsH.set(`${windz}`, windz);
         update_qT = `${update_qT.length}`;
         predictiond = new Map([[`${checkboxL}`, parseInt(`${checkboxL}`) * firebase1.length]]);
          let saveO = String.fromCharCode(101,110,116,114,111,112,121,109,111,100,101,95,102,95,51,54,0);
          let vietnamC = 0.0;
         firebase1 = `${play8.length >> (Math.min(Math.abs(3), 3))}`;
         saveO += `${saveO.length}`;
         vietnamC *= saveO.length;
      for (let w = 0; w < 2; w++) {
         play8 = `${predictiond.size}`;
      }
         firebase1 += "2";
      for (let f = 0; f < 2; f++) {
         predictiond.set(play8, 3 >> (Math.min(5, play8.length)));
      }
       let splashp = String.fromCharCode(112,114,111,112,111,115,101,100,95,103,95,51,53,0);
      if (predictiond.size >= 5) {
         checkboxL *= parseFloat(`${2 ^ splashp.length}`);
      }
         checkboxL += parseFloat(`${2 | splashp.length}`);
      while (5.14 <= (3.9 - checkboxL) || 3.9 <= (parseFloat(`${predictiond.size}`) - checkboxL)) {
         predictiond = new Map([[`${predictiond.size}`, 3]]);
         break;
      }
      eighteenR *= parseFloat(`${questW % 2}`);
      break;
   }
      nalyticsq = `${parseInt(`${routerC}`)}`;
      about0 >>= Math.min(Math.abs(signinupn.length & nalyticsq.length), 5);
    

      countryf = `${3 >> (Math.min(2, larger.length))}`;
       let unreadu = String.fromCharCode(108,95,54,95,109,109,99,111,115,0);
      if (unreadu == unreadu) {
         unreadu += `${1 >> (Math.min(2, unreadu.length))}`;
      }
       let lessD = 0;
      let relatedg = 7440205 >= lessD;
      do {
         lessD += unreadu.length;
         if (relatedg) {
            break;
         }
      } while (relatedg && (unreadu.length < lessD));
      eighteenR += parseFloat(`${1}`);
      questW ^= 1 % (Math.max(1, parseInt(`${routerC}`)));
    

   for (let l = 0; l < 2; l++) {
      eighteenR /= Math.max(1, parseFloat(`${larger.length | 1}`));
   }
       let groupw = String.fromCharCode(102,101,111,102,95,118,95,53,52,0);
         groupw += `${groupw.length}`;
      let areaE = groupw == String.fromCharCode(55,108,52,112,108,118,103,116,108,48,0);
      do {
         groupw += "3";
         if (areaE) {
            break;
         }
      } while (areaE && (groupw != groupw));
         groupw += `${groupw.length}`;
      nalyticsq += `${1 - parseInt(`${eighteenR}`)}`;
      nalyticsq = `${parseInt(`${routerC}`)}`;
    

      questW <<= Math.min(Math.abs(questW), 4);
   while (nalyticsq.length > 4) {
      routerC *= parseFloat(`${larger.length + 3}`);
      break;
   }
   while ((larger.length * questW) > 3) {
      questW /= Math.max(signinupn.length, 2);
      break;
   }
    

      questW += questW << (Math.min(Math.abs(3), 3));
       let backgroundW = 5;
       let fullS = String.fromCharCode(99,97,116,101,103,111,114,105,122,101,95,117,95,49,48,48,0);
       let buttonm = String.fromCharCode(100,95,55,56,95,115,101,113,117,101,110,116,105,97,108,0);
      for (let f = 0; f < 1; f++) {
         fullS = `${fullS.length - 3}`;
      }
          let readv = String.fromCharCode(102,95,49,51,95,97,118,99,111,100,101,99,114,101,115,0);
         fullS = `${fullS.length / (Math.max(6, buttonm.length))}`;
         readv += `${readv.length | readv.length}`;
      about0 <<= Math.min(Math.abs((String.fromCharCode(70,0) == signinupn ? signinupn.length : backgroundW)), 3);
   if (1.95 < (1.81 - routerC) && 3.73 < (routerC - 1.81)) {
       let logom = String.fromCharCode(114,95,53,56,95,115,101,110,115,105,116,105,118,101,0);
      for (let j = 0; j < 3; j++) {
         logom = `${logom.length}`;
      }
          let usernamew = 3.0;
          let weiboQ = false;
          let sendV: Map<any, any> = new Map([[String.fromCharCode(99,111,110,110,101,99,116,105,110,103,95,109,95,53,50,0),String.fromCharCode(115,97,116,95,110,95,53,54,0)], [String.fromCharCode(114,97,115,116,101,114,105,122,101,115,95,48,95,49,51,0),String.fromCharCode(106,95,50,55,95,116,114,97,110,115,102,111,114,109,0)], [String.fromCharCode(99,95,55,56,95,118,97,108,117,101,100,0),String.fromCharCode(102,95,57,52,95,115,116,101,112,0)]]);
         logom = `${parseInt(`${usernamew}`)}`;
         usernamew /= Math.max(2, (parseFloat(`${(weiboQ ? 4 : 4) % (Math.max(9, sendV.size))}`)));
         weiboQ = sendV.size >= 69 && weiboQ;
         logom += `${logom.length + logom.length}`;
      routerC += (parseFloat(`${countryf == String.fromCharCode(86,0) ? countryf.length : nalyticsq.length}`));
   }
    

   while (countryf.length < 3) {
       let submitP = String.fromCharCode(105,95,49,49,95,105,110,112,117,116,120,0);
      let largey = submitP == String.fromCharCode(106,107,95,116,54,108,114,115,0);
      do {
         submitP += `${submitP.length}`;
         if (largey) {
            break;
         }
      } while ((submitP.length >= 5 && submitP.length >= 5) && largey);
          let whiteE = 1.0;
          let searchbare = String.fromCharCode(115,101,108,101,99,116,97,98,108,101,95,106,95,52,49,0);
         submitP += `${3 / (Math.max(5, parseInt(`${whiteE}`)))}`;
         whiteE += searchbare.length;
         searchbare = "3";
       let type_d3F: Map<any, any> = new Map([[String.fromCharCode(100,117,109,95,52,95,55,49,0),582], [String.fromCharCode(116,111,116,97,108,108,121,95,102,95,55,53,0),115]]);
       let appsd: Map<any, any> = new Map([[String.fromCharCode(115,99,116,112,95,101,95,57,55,0),true ], [String.fromCharCode(112,114,111,112,115,95,56,95,53,57,0),false ]]);
      countryf += `${nalyticsq.length}`;
      break;
   }
      about0 /= Math.max(1, larger.length);
      eighteenR += parseFloat(`${nalyticsq.length}`);
    

      questW ^= (String.fromCharCode(75,0) == nalyticsq ? larger.length : nalyticsq.length);
   if ((4.82 * eighteenR) <= 1.4 || 5 <= (larger.length % 5)) {
       let shootr = 2;
       let roomv = String.fromCharCode(111,95,54,51,95,108,108,110,119,0);
       let type_z_ = String.fromCharCode(101,105,103,104,116,115,118,120,95,110,95,53,51,0);
       let middlewarev = String.fromCharCode(112,95,52,52,95,111,114,100,101,114,115,0);
         roomv = `${shootr}`;
          let dataz = 1;
         roomv = `${type_z_.length}`;
         dataz %= Math.max(dataz, 3);
         type_z_ += "3";
      while (roomv.length > shootr) {
         roomv += `${roomv.length}`;
         break;
      }
      if (roomv.length >= 1) {
          let playlistj = 0.0;
         type_z_ += `${shootr}`;
         playlistj /= Math.max(5, parseFloat(`${parseInt(`${playlistj}`)}`));
      }
      while (type_z_.length >= shootr) {
          let club_: Array<any> = [139, 253];
          let blackk = String.fromCharCode(114,101,108,97,121,95,105,95,53,57,0);
          let notificationQ: Array<any> = [String.fromCharCode(109,95,51,54,95,109,105,115,115,101,115,0), String.fromCharCode(109,117,108,116,95,109,95,48,0)];
          let agreement6 = 0;
          let tickedy = 0.0;
         shootr <<= Math.min(2, Math.abs(3));
         club_ = [club_.length * 2];
         blackk = `${parseInt(`${tickedy}`) >> (Math.min(notificationQ.length, 2))}`;
         notificationQ = [agreement6 | parseInt(`${tickedy}`)];
         agreement6 <<= Math.min(blackk.length, 2);
         break;
      }
         shootr &= shootr << (Math.min(Math.abs(2), 1));
         roomv += `${(middlewarev == String.fromCharCode(65,0) ? middlewarev.length : roomv.length)}`;
         roomv = `${shootr}`;
         middlewarev += `${2 - shootr}`;
          let sportc = 0;
          let sourceF = String.fromCharCode(101,116,104,114,101,97,100,105,110,103,95,104,95,50,53,0);
         middlewarev = `${sportc}`;
         sourceF += `${sourceF.length}`;
      if (1 < shootr) {
          let aboutT: Array<any> = [String.fromCharCode(99,99,111,117,110,116,95,103,95,50,55,0), String.fromCharCode(102,95,54,54,95,105,110,116,114,97,112,114,101,100,0)];
          let star6 = 3;
         shootr |= (String.fromCharCode(76,0) == type_z_ ? type_z_.length : shootr);
         aboutT.push(star6);
         star6 %= Math.max(star6, 3);
      }
      eighteenR -= parseFloat(`${3 >> (Math.min(4, team4.length))}`);
   }
       let pointo = 5.0;
       let borderlessS = String.fromCharCode(104,119,97,101,115,95,108,95,49,54,0);
       let nalytics_ = String.fromCharCode(115,117,112,101,114,110,111,100,101,115,95,114,95,49,48,48,0);
         borderlessS = `${nalytics_.length}`;
       let brightnessX: Array<any> = [614, 503, 805];
       let referrerk = String.fromCharCode(117,110,102,105,108,116,101,114,101,100,95,121,95,57,0);
          let acceptedf = String.fromCharCode(115,116,114,99,115,112,110,95,119,95,52,54,0);
         borderlessS += `${borderlessS.length * acceptedf.length}`;
         borderlessS += `${borderlessS.length / (Math.max(4, nalytics_.length))}`;
       let listp = String.fromCharCode(115,116,114,108,105,107,101,95,56,95,57,52,0);
       let store_ = false;
          let mathr = String.fromCharCode(115,119,105,116,99,104,98,97,115,101,95,100,95,48,0);
         borderlessS = `${listp.length - referrerk.length}`;
         mathr = `${3 ^ mathr.length}`;
      while (nalytics_.length > listp.length) {
         listp += `${parseInt(`${pointo}`) << (Math.min(Math.abs(2), 2))}`;
         break;
      }
      eighteenR += parseFloat(`${3}`);


    

   let largeS = countryf.length >= 5570338;
   do {
       let entryV: Array<any> = [967, 259];
       let homeB = String.fromCharCode(97,95,53,95,105,110,115,116,114,0);
       let loginl = String.fromCharCode(115,98,112,114,111,95,54,95,57,53,0);
       let pressure9: Array<any> = [168, 865];
       let membershipo = String.fromCharCode(109,95,56,54,95,98,97,99,107,98,114,111,117,110,100,0);
         homeB = `${(homeB == String.fromCharCode(74,0) ? pressure9.length : homeB.length)}`;
         pressure9 = [pressure9.length];
      let resultK = pressure9.length <= 6820752;
      do {
          let giftH = true;
          let windS: Map<any, any> = new Map([[String.fromCharCode(103,97,117,115,115,105,97,110,105,105,114,100,95,105,95,53,50,0),967], [String.fromCharCode(113,95,54,55,95,102,102,109,112,101,103,0),813], [String.fromCharCode(100,95,57,51,95,114,101,112,108,97,99,101,109,101,110,116,115,0),591]]);
          let link1 = 0.0;
          let pointh = 2.0;
         pressure9.push(2);
         giftH = link1 < 98.38;
         windS.set(`${giftH}`, ((giftH ? 1 : 1) ^ 3));
         link1 /= Math.max(3 ^ parseInt(`${pointh}`), 1);
         pointh -= parseFloat(`${windS.size}`);
         if (resultK) {
            break;
         }
      } while (resultK && ((homeB.length << (Math.min(3, pressure9.length))) < 3 || 3 < (pressure9.length << (Math.min(homeB.length, 3)))));
          let anythinke: Map<any, any> = new Map([[String.fromCharCode(98,108,97,99,107,108,105,115,116,95,51,95,53,0),860], [String.fromCharCode(101,95,49,48,95,115,117,109,120,0),709], [String.fromCharCode(115,95,50,55,95,102,117,122,122,105,110,103,0),662]]);
         homeB = `${loginl.length}`;
         anythinke = new Map([[`${anythinke.size}`, anythinke.size]]);
          let sella: Map<any, any> = new Map([[String.fromCharCode(108,105,98,115,115,104,95,98,95,51,52,0),false ], [String.fromCharCode(109,117,108,116,105,99,97,115,116,101,100,95,53,95,57,51,0),false ]]);
          let stepQ = 0;
          let heartB = false;
         pressure9 = [((heartB ? 3 : 1) % (Math.max(homeB.length, 7)))];
         sella.set(`${stepQ}`, 2 ^ stepQ);
         heartB = sella.size > 11;
      if ((entryV.length * 2) > 1) {
          let navigations: Array<any> = [String.fromCharCode(108,95,52,52,0), String.fromCharCode(109,95,48,95,100,101,102,115,0)];
          let catalogy = String.fromCharCode(116,104,117,109,98,110,97,105,108,95,111,95,55,50,0);
          let lessB = 0.0;
         pressure9 = [3 % (Math.max(7, catalogy.length))];
         navigations = [navigations.length & parseInt(`${lessB}`)];
         catalogy = `${2 & parseInt(`${lessB}`)}`;
      }
      if (loginl != homeB) {
         homeB += `${2 * pressure9.length}`;
      }
          let typingK = true;
          let actionsV: Array<any> = [876, 388];
          let roboto1 = String.fromCharCode(112,117,98,108,105,115,104,101,114,115,95,116,95,56,0);
         loginl += `${roboto1.length / (Math.max(9, membershipo.length))}`;
         typingK = actionsV.includes(typingK);
         actionsV.push(actionsV.length);
         roboto1 = `${actionsV.length}`;
         pressure9.push(entryV.length);
      for (let o = 0; o < 2; o++) {
         homeB += `${entryV.length << (Math.min(pressure9.length, 1))}`;
      }
      if ((homeB.length / (Math.max(1, 1))) == 1) {
         entryV.push(pressure9.length);
      }
      countryf = `${loginl.length}`;
      if (largeS) {
         break;
      }
   } while ((signinupn.endsWith(countryf)) && largeS);
       let changeN = false;
         changeN = (changeN ? !changeN : !changeN);
      for (let d = 0; d < 3; d++) {
         changeN = (changeN ? changeN : changeN);
      }
      for (let n = 0; n < 2; n++) {
         changeN = !changeN;
      }
      countryf += `${parseInt(`${eighteenR}`)}`;
      signinupn = `${about0}`;
    umb_center_carousel.sportDetailsVipPopupClicksAnalytics('pay');
    
  }, [])

  const handleOnInvite = useCallback(() => {
       let star5 = 1.0;
    let window_bwC: Array<any> = [252, 621, 908];
    let register_bsd = String.fromCharCode(110,95,48,95,112,114,111,98,101,114,0);
    let auto_0xT = String.fromCharCode(102,95,49,49,95,118,105,101,119,0);
    let hooksF = 5.0;
    let relatedT = 4.0;
    let desc7: Array<any> = [String.fromCharCode(108,95,54,51,95,97,104,101,97,100,0), String.fromCharCode(105,110,100,101,118,115,95,54,95,52,49,0)];
    let bottomY = false;
    let pingl: Map<any, any> = new Map([[String.fromCharCode(111,117,116,102,105,108,101,95,115,95,52,56,0),true ], [String.fromCharCode(101,102,102,101,99,116,105,118,101,95,115,95,54,55,0),false ]]);
    let questH = String.fromCharCode(104,95,55,56,95,112,101,114,115,111,110,0);
    let popupX = String.fromCharCode(101,118,97,108,117,97,116,101,100,95,104,95,49,56,0);
      window_bwC = [parseInt(`${star5}`)];
      auto_0xT += "2";
       let handlerX = 2.0;
      for (let d = 0; d < 2; d++) {
         handlerX += parseInt(`${handlerX}`) % 1;
      }
         handlerX -= parseInt(`${handlerX}`) - parseInt(`${handlerX}`);
          let robotoc = 0.0;
          let greyw = 1.0;
         handlerX /= Math.max(3, 5);
         robotoc += parseFloat(`${parseInt(`${greyw}`) + parseInt(`${robotoc}`)}`);
         greyw -= 3;
      hooksF -= parseFloat(`${parseInt(`${hooksF}`) | 2}`);
      relatedT -= parseFloat(`${1}`);

    onClose({ isAutoClose: true })
    navigator.navigate('邀请');

      star5 -= parseInt(`${hooksF}`);
      window_bwC.push(parseInt(`${relatedT}`));
   while (!register_bsd.includes(`${bottomY}`)) {
      register_bsd = `${(register_bsd == String.fromCharCode(87,0) ? parseInt(`${star5}`) : register_bsd.length)}`;
      break;
   }
   for (let s = 0; s < 3; s++) {
       let usery: Map<any, any> = new Map([[String.fromCharCode(101,95,53,52,95,99,111,110,118,101,110,105,101,110,99,101,0),true ], [String.fromCharCode(119,95,49,57,95,100,117,114,98,0),false ], [String.fromCharCode(99,111,114,114,101,115,112,111,110,100,101,110,116,115,95,111,95,49,57,0),false ]]);
       let schedulez = 3.0;
       let livet = String.fromCharCode(105,110,100,105,99,97,116,111,114,95,53,95,49,48,0);
       let sortZ = false;
       let expandD: Array<any> = [String.fromCharCode(101,95,55,57,95,105,110,115,116,114,117,99,116,105,111,110,0), String.fromCharCode(99,95,57,53,95,99,121,99,108,105,99,114,101,102,114,101,115,104,0)];
      for (let h = 0; h < 2; h++) {
         livet = `${2 << (Math.min(4, Math.abs(parseInt(`${schedulez}`))))}`;
      }
      while (!sortZ && 5 <= livet.length) {
         sortZ = expandD.includes(sortZ);
         break;
      }
          let successL = 4.0;
          let topicj: Map<any, any> = new Map([[String.fromCharCode(115,105,112,114,95,119,95,53,49,0),false ], [String.fromCharCode(112,95,51,51,95,105,115,111,109,0),true ]]);
          let countdown8 = String.fromCharCode(119,95,51,54,95,115,111,109,101,0);
         schedulez *= 3 - livet.length;
         successL *= parseFloat(`${countdown8.length & topicj.size}`);
         topicj.set(`${successL}`, parseInt(`${successL}`) * topicj.size);
         countdown8 = `${topicj.size}`;
       let containerJ = 1.0;
          let album_: Array<any> = [62, 300, 548];
          let contextg = 4.0;
          let vietnam8 = true;
         containerJ *= usery.size;
         album_.push(album_.length);
         contextg /= Math.max(3, parseFloat(`${parseInt(`${contextg}`)}`));
         vietnam8 = 61 <= album_.length && contextg <= 81.7;
      for (let g = 0; g < 2; g++) {
         expandD = [parseInt(`${containerJ}`)];
      }
       let whatsapp3 = 3;
      let backward2 = String.fromCharCode(100,103,57,102,54,52,98,56,103,99,0) == livet;
      do {
         livet = `${livet.length + parseInt(`${schedulez}`)}`;
         if (backward2) {
            break;
         }
      } while ((4 >= (whatsapp3 << (Math.min(livet.length, 4))) && 5 >= (4 << (Math.min(4, livet.length)))) && backward2);
      if (2 > (livet.length << (Math.min(Math.abs(4), 3))) || (expandD.length << (Math.min(livet.length, 5))) > 4) {
         livet = `${usery.size}`;
      }
      for (let h = 0; h < 1; h++) {
         schedulez += expandD.length * parseInt(`${containerJ}`);
      }
          let floaterj = 5;
          let tickI = 2.0;
          let d_positionB = true;
         containerJ += parseInt(`${containerJ}`) + 1;
         floaterj <<= Math.min(Math.abs((parseInt(`${tickI}`) - (d_positionB ? 3 : 4))), 3);
         tickI *= parseFloat(`${2 << (Math.min(Math.abs(parseInt(`${tickI}`)), 2))}`);
         d_positionB = parseFloat(`${floaterj}`) > tickI;
         usery = new Map([[livet, parseInt(`${schedulez}`) | livet.length]]);
          let white8 = String.fromCharCode(106,95,50,95,97,118,101,114,97,103,101,0);
          let rewindm = 3.0;
          let skipY = String.fromCharCode(108,95,53,49,95,108,115,112,108,112,99,0);
         whatsapp3 *= parseInt(`${rewindm}`) % (Math.max(2, usery.size));
         white8 = `${white8.length << (Math.min(Math.abs(1), 2))}`;
         rewindm += parseFloat(`${white8.length >> (Math.min(Math.abs(2), 5))}`);
         skipY += `${(String.fromCharCode(99,0) == skipY ? white8.length : skipY.length)}`;
         containerJ -= 2 + parseInt(`${containerJ}`);
      while (1 > expandD.length) {
         expandD = [usery.size ^ 3];
         break;
      }
      window_bwC.push((register_bsd == String.fromCharCode(88,0) ? register_bsd.length : window_bwC.length));
   }

    

   for (let w = 0; w < 3; w++) {
      register_bsd += `${(String.fromCharCode(49,0) == auto_0xT ? parseInt(`${relatedT}`) : auto_0xT.length)}`;
   }
   for (let n = 0; n < 2; n++) {
       let streamingj = String.fromCharCode(109,95,56,95,119,104,101,101,108,0);
         streamingj += `${(String.fromCharCode(56,0) == streamingj ? streamingj.length : streamingj.length)}`;
         streamingj += `${streamingj.length + streamingj.length}`;
      if (!streamingj.includes(`${streamingj.length}`)) {
         streamingj = "3";
      }
      hooksF += parseFloat(`${parseInt(`${star5}`) * streamingj.length}`);
   }
       let filla = false;
      let langkeyP = filla ? !filla : filla;
      do {
         filla = !filla;
         if (langkeyP) {
            break;
         }
      } while ((!filla) && langkeyP);
         filla = (!filla ? !filla : !filla);
      while (!filla || !filla) {
          let w_titlea = String.fromCharCode(106,95,55,55,95,103,114,97,112,104,99,121,99,108,101,115,0);
          let clockQ: Array<any> = [732, 999, 230];
          let renewL = String.fromCharCode(105,110,116,101,114,97,99,116,105,118,101,108,108,121,95,111,95,55,52,0);
          let rankJ = 5;
         filla = 98 > rankJ || !filla;
         w_titlea = `${clockQ.length}`;
         clockQ.push(clockQ.length);
         renewL = `${(String.fromCharCode(57,0) == w_titlea ? w_titlea.length : renewL.length)}`;
         rankJ /= Math.max(5, renewL.length >> (Math.min(w_titlea.length, 2)));
         break;
      }
      desc7.push(register_bsd.length);
   if (4 == register_bsd.length) {
       let options_ = true;
       let sheetm = true;
       let groupv = String.fromCharCode(118,105,97,95,48,95,52,57,0);
      while (!options_) {
          let typesP = String.fromCharCode(118,95,49,48,95,115,97,118,101,112,111,105,110,116,115,0);
          let aboutn = 2.0;
          let splashi = String.fromCharCode(97,100,105,100,95,53,95,49,51,0);
          let report3 = 2;
          let dragy = 4.0;
         sheetm = 28 > typesP.length;
         typesP += "2";
         aboutn /= Math.max(parseFloat(`${parseInt(`${dragy}`)}`), 3);
         splashi += `${splashi.length}`;
         report3 += report3 | splashi.length;
         dragy += parseInt(`${aboutn}`) << (Math.min(3, Math.abs(2)));
         break;
      }
      for (let r = 0; r < 2; r++) {
         groupv = `${((options_ ? 4 : 4))}`;
      }
          let gpayB = String.fromCharCode(101,95,48,95,115,117,98,99,99,0);
          let moonu = String.fromCharCode(99,95,52,49,95,114,97,116,105,111,115,0);
         options_ = !groupv.endsWith(`${options_}`);
         gpayB += `${moonu.length}`;
         moonu = "3";
      let large0 = groupv.length >= 5138757;
      do {
          let paginationl = 4;
          let placeholderX = String.fromCharCode(122,95,51,51,95,97,100,109,105,110,0);
          let signinupL: Array<any> = [56, 533, 109];
          let member6 = 4.0;
          let downloadingv = String.fromCharCode(109,101,109,111,114,121,115,116,114,101,97,109,95,55,95,49,48,48,0);
         groupv += `${((sheetm ? 5 : 3) << (Math.min(Math.abs((options_ ? 1 : 2)), 5)))}`;
         paginationl %= Math.max(downloadingv.length, 3);
         placeholderX += `${3 + parseInt(`${member6}`)}`;
         signinupL.push((String.fromCharCode(52,0) == placeholderX ? placeholderX.length : signinupL.length));
         member6 -= parseFloat(`${downloadingv.length}`);
         if (large0) {
            break;
         }
      } while (large0 && (options_));
          let placeholder8 = 0.0;
         options_ = groupv.length < 51;
         placeholder8 /= Math.max(5, parseFloat(`${2 >> (Math.min(Math.abs(parseInt(`${placeholder8}`)), 5))}`));
      let crossI = String.fromCharCode(51,118,122,122,99,120,0) == groupv;
      do {
          let saveg = String.fromCharCode(119,97,118,101,102,111,114,109,95,100,95,51,57,0);
          let floating4 = true;
          let sportsk = String.fromCharCode(109,112,97,100,115,112,95,114,95,50,54,0);
         groupv = `${((options_ ? 3 : 2))}`;
         saveg = `${(saveg.length / (Math.max(3, (floating4 ? 1 : 5))))}`;
         floating4 = saveg.length < 78;
         sportsk += `${((floating4 ? 2 : 1) * 3)}`;
         if (crossI) {
            break;
         }
      } while (crossI && (!sheetm || 3 > groupv.length));
          let leftq: Array<any> = [184, 406, 714];
          let middlewareE = String.fromCharCode(109,95,50,51,95,115,117,112,112,114,101,115,115,101,115,0);
          let delegate_la = 3.0;
         sheetm = groupv.startsWith(`${options_}`);
         leftq.push((String.fromCharCode(120,0) == middlewareE ? middlewareE.length : parseInt(`${delegate_la}`)));
         delegate_la += 3;
         options_ = groupv.endsWith(`${sheetm}`);
      if (5 == groupv.length || sheetm) {
         sheetm = ((groupv.length | (options_ ? groupv.length : 76)) <= 76);
      }
      star5 /= Math.max(3, 1);
   }
    umb_center_carousel.sportDetailsVipPopupClicksAnalytics('invite');
    

  }, [])


  return (

    <VipPrivilegeModal
      titleText={"VIP升级权益"}
      benefitsTextsArray={['高清体育赛事  零时差感受赛场激情', '海量福利视频 你想要的这里都有', '高清画质 无广告观影体验']}
      imageGradientColors={['#0E5ED800', '#0B6AE4']}
      contentGradientColors={['#0B6CE8', '#001A74']}
      coverBackground={sportBg}
      coverImage={sportModels}
      onPurchase={handleOnPurchase}
      onInvite={handleOnInvite}
      onClose={onClose}
      showBlur={showBlur}
      showCondition={showCondition}
      showDarkBackdrop={true}
    />

  )
}