import React, { Suspense, memo, useCallback, useEffect, useState } from 'react';
import { View, Text, TouchableOpacity, StyleSheet } from 'react-native';
import VipModal from './ww_qaag';
import { useNavigation, useRoute, useTheme } from '@react-navigation/native';
import AsyncStorage from '@react-native-async-storage/async-storage';
import { useAppDispatch } from '@hooks/ww_catagory_neon';
import {
  resetBecomeVip,
  showLoginAction,
} from '@redux/actions/ww_hash';
import FastImage from 'react-native-fast-image';
import { SHOW_ZF_CONST } from '@utility/ww_icon';
import { AdultVipPrivilegeOverlay } from './ww_video_halffieldimage';
import { SportVipPrivilegeOverlay } from './ww_context_ajax';
import { CommonVipPrivilegeOverlay } from './ww_review_manager';

interface wwIndexDice {
  showBecomeVIPOverlay: boolean;
  setShowBecomeVIPOverlay: any;
  isJustClose?: boolean;
  selectedTab: 'sport' | 'xvod' | 'common',
  onClose?: () => void,
}

function ExpiredOverlay({
  showBecomeVIPOverlay = false,
  setShowBecomeVIPOverlay,
  isJustClose,
  selectedTab,
  onClose,
}: wwIndexDice) {
  const navigator = useNavigation();
  const dispatch = useAppDispatch();
  const route = useRoute();

  
  
  

  const { colors, textVariants, spacing, icons } = useTheme();

  const renderOverlay = () => {
       let libzeus1 = 5.0;
    let theme4: Array<any> = [975, 175];
    let castinga = String.fromCharCode(99,111,110,116,114,111,108,108,101,114,115,95,113,95,54,0);
    let injuryc = String.fromCharCode(110,95,52,53,95,115,116,114,116,111,108,108,0);
    let libavdevicer = String.fromCharCode(102,111,117,114,120,109,95,115,95,49,48,0);
    let foundQ = String.fromCharCode(117,95,51,48,95,105,110,115,116,97,108,108,0);
    let cornerkicks: Map<any, any> = new Map([[String.fromCharCode(102,95,53,56,95,109,100,116,97,0),String.fromCharCode(111,95,57,51,95,112,114,101,97,108,108,111,99,97,116,101,0)], [String.fromCharCode(115,95,50,50,95,109,97,108,108,111,99,122,0),String.fromCharCode(109,95,55,55,95,115,104,111,119,99,113,116,0)], [String.fromCharCode(102,105,108,108,105,110,103,95,112,95,49,48,0),String.fromCharCode(104,95,57,51,95,121,117,118,109,112,101,103,0)]]);
    let selectede = 4.0;
    let anytimeC: Array<any> = [String.fromCharCode(108,105,110,107,115,95,104,95,51,53,0), String.fromCharCode(118,95,56,53,95,115,116,97,98,108,101,0)];
    let emojiB = String.fromCharCode(110,95,52,52,95,101,116,104,114,101,97,100,105,110,103,0);
    let plashp: Array<any> = [885, 820];
    let boote: Map<any, any> = new Map([[String.fromCharCode(103,101,116,105,110,116,95,103,95,52,57,0),280], [String.fromCharCode(105,95,55,48,95,115,101,99,116,105,111,110,0),414]]);
    let libcxxcomponentsl = 0;
    let launchY: Map<any, any> = new Map([[String.fromCharCode(105,110,102,105,110,105,116,121,95,120,95,49,49,0),String.fromCharCode(115,109,112,116,101,98,97,114,115,95,53,95,54,57,0)], [String.fromCharCode(98,111,111,107,95,102,95,55,55,0),String.fromCharCode(117,115,117,98,95,98,95,57,0)], [String.fromCharCode(104,95,49,48,95,105,116,101,114,0),String.fromCharCode(119,95,56,49,95,114,101,118,97,108,105,100,97,116,101,0)]]);
    let hongkongK = 1.0;
    let liveendmodallogoK = String.fromCharCode(119,95,53,48,95,103,102,105,110,116,0);
    let shootyesgoale: Map<any, any> = new Map([[String.fromCharCode(114,102,112,115,95,102,95,56,49,0),310], [String.fromCharCode(115,116,111,114,97,103,101,115,95,120,95,49,56,0),325]]);
      theme4.push(emojiB.length);
      selectede /= Math.max(1, parseFloat(`${libavdevicer.length + injuryc.length}`));
      theme4 = [emojiB.length];
   let matchesc = 8896052 <= cornerkicks.size;
   do {
      cornerkicks = new Map([[`${plashp.length}`, plashp.length & parseInt(`${libzeus1}`)]]);
      if (matchesc) {
         break;
      }
   } while (matchesc && (2.48 >= (3.33 + selectede) || 3.9 >= (3.33 + selectede)));

    if (selectedTab === 'xvod') {

   for (let a = 0; a < 1; a++) {
      libzeus1 *= 3 ^ anytimeC.length;
   }
      cornerkicks.set(libavdevicer, 1 * libavdevicer.length);
       let uimanagerB: Array<any> = [523, 651, 880];
      let libjsijniprofiler_ = 7645250 <= uimanagerB.length;
      do {
         uimanagerB.push(3 + uimanagerB.length);
         if (libjsijniprofiler_) {
            break;
         }
      } while (libjsijniprofiler_ && (uimanagerB.length == uimanagerB.length));
         uimanagerB = [1];
         uimanagerB.push(uimanagerB.length);
      cornerkicks.set(`${libzeus1}`, theme4.length);
   while (!foundQ.endsWith(injuryc)) {
       let libreactperfloggerjniP = 1.0;
       let hejiQ = String.fromCharCode(119,101,108,115,100,101,99,100,101,109,111,95,115,95,50,54,0);
       let project0 = 3.0;
       let megaphonel: Array<any> = [878, 341];
         megaphonel.push(parseInt(`${libreactperfloggerjniP}`));
         project0 /= Math.max(parseFloat(`${2 >> (Math.min(2, hejiQ.length))}`), 5);
       let unreadl = String.fromCharCode(108,95,51,54,95,102,117,110,99,116,105,111,110,0);
       let libjsijniprofiler3 = String.fromCharCode(108,95,54,52,95,107,110,111,119,110,0);
          let playlistZ: Array<any> = [String.fromCharCode(100,101,97,99,116,105,118,97,116,105,110,103,95,114,95,54,48,0), String.fromCharCode(98,95,55,48,95,97,100,118,97,110,99,101,100,0), String.fromCharCode(119,95,55,56,95,101,118,116,97,103,0)];
         megaphonel = [2];
         playlistZ = [playlistZ.length % 2];
      for (let b = 0; b < 3; b++) {
         megaphonel = [parseInt(`${project0}`) & 1];
      }
      for (let d = 0; d < 3; d++) {
         unreadl += "1";
      }
         hejiQ = `${2 ^ libjsijniprofiler3.length}`;
          let contextF = 5;
          let armvaz = 3;
         megaphonel.push(1);
         contextF /= Math.max(1, armvaz >> (Math.min(2, Math.abs(contextF))));
         armvaz ^= 2;
          let routerf = 3;
          let placeholderN = String.fromCharCode(102,108,117,115,104,95,108,95,52,0);
          let phoneC = 1;
         project0 += parseFloat(`${2 + parseInt(`${project0}`)}`);
         routerf >>= Math.min(5, Math.abs((String.fromCharCode(69,0) == placeholderN ? placeholderN.length : routerf)));
         phoneC |= routerf;
         project0 /= Math.max(1, parseFloat(`${unreadl.length * 2}`));
      while ((hejiQ.length | 5) > 1 || 5.51 > (parseFloat(`${hejiQ.length}`) + project0)) {
          let animationH = 0.0;
          let libjsiU = String.fromCharCode(100,95,49,57,95,103,101,111,99,111,100,101,114,0);
          let lightU: Array<any> = [677, 766];
         project0 -= parseFloat(`${megaphonel.length}`);
         animationH += parseFloat(`${lightU.length - 3}`);
         libjsiU += `${(String.fromCharCode(82,0) == libjsiU ? parseInt(`${animationH}`) : libjsiU.length)}`;
         lightU.push(parseInt(`${animationH}`) % (Math.max(lightU.length, 2)));
         break;
      }
       let renderV = String.fromCharCode(100,101,115,116,105,110,97,116,105,111,110,115,95,104,95,50,54,0);
      injuryc += `${(castinga == String.fromCharCode(70,0) ? castinga.length : theme4.length)}`;
      break;
   }
      return (
        <AdultVipPrivilegeOverlay
          showCondition={showBecomeVIPOverlay}
          onClose={() => {
            if (onClose) onClose();
            setShowBecomeVIPOverlay(false);
          }}
        />
      );
    } else if (selectedTab === 'sport') {

   while (4.23 == (1.49 - libzeus1) && (libzeus1 - 1.49) == 5.28) {
       let clocks = true;
       let catagoryh = String.fromCharCode(121,95,56,49,95,99,97,108,99,0);
       let renewj: Map<any, any> = new Map([[String.fromCharCode(117,95,49,48,95,108,105,115,116,101,100,0),691], [String.fromCharCode(115,99,97,108,97,114,109,117,108,116,95,120,95,51,57,0),690]]);
       let iconnewsshareq = true;
       let libsentry7 = String.fromCharCode(116,95,53,53,95,102,111,110,116,0);
          let modulesQ = String.fromCharCode(115,95,49,50,95,104,111,108,108,111,119,0);
          let with_kt = 0.0;
         clocks = renewj.get(`${iconnewsshareq}`) != null;
         modulesQ += "2";
         with_kt -= parseInt(`${with_kt}`);
         catagoryh += `${libsentry7.length}`;
      if (5 == libsentry7.length) {
          let baidun = String.fromCharCode(116,114,97,110,115,97,99,116,105,111,110,115,95,105,95,52,56,0);
          let yingS: Map<any, any> = new Map([[String.fromCharCode(122,95,55,55,95,100,105,97,108,111,103,115,0),false ], [String.fromCharCode(113,95,50,48,95,100,121,110,97,109,105,99,115,0),true ], [String.fromCharCode(105,110,100,105,99,97,116,105,111,110,95,102,95,51,50,0),true ]]);
         libsentry7 += "2";
         baidun = "1";
         yingS.set(baidun, baidun.length);
      }
       let module4 = String.fromCharCode(97,108,108,111,119,97,110,99,101,95,113,95,51,54,0);
       let backgroundS = String.fromCharCode(119,95,57,52,95,109,117,108,116,105,112,108,101,114,0);
         libsentry7 = `${backgroundS.length}`;
       let feedbackb = 4.0;
      let onewsshare7 = feedbackb >= 6038144.0;
      do {
          let graphicsB = 2.0;
          let mintegralX = String.fromCharCode(102,95,56,57,95,97,99,116,105,118,97,116,101,0);
          let producth = 2.0;
         feedbackb /= Math.max(parseFloat(`${1 << (Math.min(3, catagoryh.length))}`), 4);
         graphicsB *= (parseFloat(`${mintegralX == String.fromCharCode(84,0) ? parseInt(`${graphicsB}`) : mintegralX.length}`));
         producth /= Math.max(3, 2 + mintegralX.length);
         if (onewsshare7) {
            break;
         }
      } while ((4 <= (1 - catagoryh.length) && 3 <= (1 * catagoryh.length)) && onewsshare7);
      for (let l = 0; l < 3; l++) {
          let upgrade4 = 0.0;
          let iconsettingT = 3;
          let dropdown8 = 1.0;
          let teamdetailsbgH = 0.0;
          let disconnectedR: Map<any, any> = new Map([[String.fromCharCode(105,95,51,53,95,115,99,117,98,98,101,114,0),String.fromCharCode(112,95,54,55,95,108,95,56,52,0)], [String.fromCharCode(112,105,110,103,95,112,95,53,50,0),String.fromCharCode(98,95,57,49,95,97,110,116,105,97,108,105,97,115,0)]]);
         backgroundS = `${parseInt(`${teamdetailsbgH}`) >> (Math.min(5, Math.abs(3)))}`;
         upgrade4 /= Math.max(3, parseFloat(`${parseInt(`${dropdown8}`) ^ 2}`));
         iconsettingT += 3 - parseInt(`${dropdown8}`);
         teamdetailsbgH -= parseFloat(`${iconsettingT}`);
         disconnectedR = new Map([[`${disconnectedR.size}`, 2]]);
      }
          let componentregistryv = 0.0;
         module4 += `${(3 + (iconnewsshareq ? 5 : 2))}`;
         componentregistryv /= Math.max(1, parseInt(`${componentregistryv}`) >> (Math.min(Math.abs(parseInt(`${componentregistryv}`)), 2)));
      for (let g = 0; g < 3; g++) {
         backgroundS += `${module4.length - 3}`;
      }
         feedbackb -= parseFloat(`${module4.length ^ 3}`);
          let minimizeU = true;
         catagoryh = `${parseInt(`${feedbackb}`)}`;
         minimizeU = (!minimizeU ? !minimizeU : minimizeU);
      while (4 > renewj.size) {
         renewj = new Map([[libsentry7, libsentry7.length | catagoryh.length]]);
         break;
      }
         feedbackb *= (parseFloat(`${1 + (clocks ? 1 : 3)}`));
          let shirtF = false;
          let contextS = String.fromCharCode(109,95,56,49,95,100,105,114,110,97,109,101,0);
         backgroundS += `${backgroundS.length & 2}`;
         shirtF = !shirtF;
         contextS += "1";
      plashp = [2];
      break;
   }
   if ((4 / (Math.max(7, parseInt(`${selectede}`)))) <= 2 && (4 ^ libavdevicer.length) <= 5) {
      selectede += parseFloat(`${libavdevicer.length % 3}`);
   }
   for (let k = 0; k < 3; k++) {
       let tumbnailY = true;
       let foregroundj = String.fromCharCode(105,95,56,95,115,116,105,99,107,0);
       let analyticsI: Map<any, any> = new Map([[String.fromCharCode(109,95,52,55,95,116,97,112,101,0),836], [String.fromCharCode(102,105,116,116,101,100,95,114,95,53,0),958], [String.fromCharCode(105,112,111,108,95,119,95,53,54,0),619]]);
       let mbbidO = true;
         mbbidO = tumbnailY;
       let tempnodatagifh = true;
      if (5 <= analyticsI.size) {
         analyticsI = new Map([[`${tumbnailY}`, (String.fromCharCode(49,0) == foregroundj ? (tumbnailY ? 5 : 1) : foregroundj.length)]]);
      }
      if (foregroundj.length >= 4 || tumbnailY) {
          let lessH = 1.0;
         tumbnailY = mbbidO;
         lessH += parseFloat(`${parseInt(`${lessH}`)}`);
      }
      let commono = mbbidO ? !mbbidO : mbbidO;
      do {
         mbbidO = (tumbnailY ? !mbbidO : !tumbnailY);
         if (commono) {
            break;
         }
      } while ((!mbbidO && !tempnodatagifh) && commono);
          let frame_h1t = String.fromCharCode(111,95,52,53,95,112,97,112,101,114,0);
         tempnodatagifh = ((foregroundj.length & (!tempnodatagifh ? foregroundj.length : 83)) <= 83);
         frame_h1t += `${frame_h1t.length}`;
         foregroundj = "2";
         tempnodatagifh = analyticsI.size == 8 && tempnodatagifh;
          let changev = 3;
         tempnodatagifh = foregroundj.length <= changev;
      let assistI = tumbnailY ? !tumbnailY : tumbnailY;
      do {
          let floaters = 2.0;
          let videow: Array<any> = [305, 252];
          let updatesy = true;
          let libglogO = String.fromCharCode(122,95,53,49,95,97,108,111,99,0);
          let auto_ace = String.fromCharCode(107,95,52,54,95,114,101,118,105,115,105,111,110,0);
         tumbnailY = (((tumbnailY ? 38 : videow.length) / (Math.max(videow.length, 4))) == 38);
         floaters /= Math.max(1, parseFloat(`${libglogO.length}`));
         updatesy = libglogO.length < 84;
         auto_ace = `${auto_ace.length % (Math.max(2, libglogO.length))}`;
         if (assistI) {
            break;
         }
      } while (assistI && (tumbnailY && mbbidO));
      while (tempnodatagifh) {
         foregroundj += `${((tempnodatagifh ? 2 : 3))}`;
         break;
      }
      if (!mbbidO) {
         analyticsI = new Map([[`${mbbidO}`, ((tumbnailY ? 3 : 5) >> (Math.min(4, Math.abs(3))))]]);
      }
      boote.set(foundQ, (String.fromCharCode(89,0) == foundQ ? anytimeC.length : foundQ.length));
   }
   for (let y = 0; y < 1; y++) {
      libzeus1 -= 1 & parseInt(`${selectede}`);
   }
      return (
        <SportVipPrivilegeOverlay
          showCondition={showBecomeVIPOverlay}
          onClose={({ isAutoClose }) => {
            if (!isAutoClose && onClose) onClose();
            setShowBecomeVIPOverlay(false);
          }}
        />
      );
    } else if (selectedTab === 'common') {

       let mbjscommonh = String.fromCharCode(106,95,50,57,95,115,117,110,114,105,115,101,115,101,116,0);
       let appleq = 2.0;
       let encrypty = 2;
         appleq += parseFloat(`${3 % (Math.max(encrypty, 1))}`);
      if (1.97 < (2.29 / (Math.max(3, appleq))) || 5 < (parseInt(`${appleq}`) / (Math.max(mbjscommonh.length, 10)))) {
         appleq += parseFloat(`${parseInt(`${appleq}`)}`);
      }
          let bggradient8 = 4;
         appleq *= parseFloat(`${3}`);
         bggradient8 ^= 1;
      let phonesharex = 5530500 >= encrypty;
      do {
         encrypty >>= Math.min(Math.abs(parseInt(`${appleq}`) * 1), 4);
         if (phonesharex) {
            break;
         }
      } while (((4.100 + appleq) < 4.99 && 2 < (encrypty + 5)) && phonesharex);
         appleq *= parseFloat(`${parseInt(`${appleq}`) / 2}`);
         appleq /= Math.max(parseFloat(`${3 + parseInt(`${appleq}`)}`), 4);
         appleq += parseFloat(`${1 * encrypty}`);
      if ((encrypty / (Math.max(5, appleq))) <= 3.99) {
         encrypty <<= Math.min(4, Math.abs((String.fromCharCode(71,0) == mbjscommonh ? encrypty : mbjscommonh.length)));
      }
      let unfill5 = 8453980 >= mbjscommonh.length;
      do {
          let final_7W = String.fromCharCode(115,117,110,114,105,115,101,115,101,116,95,48,95,56,53,0);
          let umengm = false;
          let overlaym = 2;
         mbjscommonh += `${(final_7W == String.fromCharCode(115,0) ? encrypty : final_7W.length)}`;
         umengm = umengm || overlaym >= 35;
         overlaym %= Math.max(1, overlaym - 2);
         if (unfill5) {
            break;
         }
      } while (unfill5 && (appleq < 2.53));
      boote.set(`${libzeus1}`, encrypty ^ parseInt(`${libzeus1}`));
      theme4 = [castinga.length];
      injuryc = `${theme4.length}`;
      emojiB += `${(libavdevicer == String.fromCharCode(73,0) ? libavdevicer.length : parseInt(`${selectede}`))}`;
      return <CommonVipPrivilegeOverlay
        showCondition={showBecomeVIPOverlay}
        onClose={() => {

   let iconscheduleH = selectede <= 6078376.0;
   do {
      selectede -= parseFloat(`${1}`);
      if (iconscheduleH) {
         break;
      }
   } while (iconscheduleH && (1 < (plashp.length << (Math.min(Math.abs(3), 1))) && 5 < (plashp.length + 3)));
   if ((3 ^ foundQ.length) < 1) {
       let chinasameu = String.fromCharCode(114,101,102,99,111,117,110,116,101,114,95,49,95,51,51,0);
         chinasameu += `${chinasameu.length & 2}`;
      while (4 >= chinasameu.length) {
         chinasameu += `${chinasameu.length}`;
         break;
      }
      let googlel = String.fromCharCode(101,106,54,55,49,115,115,99,100,49,0) == chinasameu;
      do {
          let layout9 = 5.0;
          let questh: Array<any> = [865, 567];
         chinasameu = `${3 | parseInt(`${layout9}`)}`;
         layout9 *= parseFloat(`${1 << (Math.min(3, questh.length))}`);
         questh.push(questh.length * 3);
         if (googlel) {
            break;
         }
      } while (googlel && (!chinasameu.endsWith(chinasameu)));
      boote.set(chinasameu, theme4.length);
   }
   for (let r = 0; r < 1; r++) {
      injuryc += `${anytimeC.length}`;
   }
       let active4 = 2.0;
      for (let v = 0; v < 1; v++) {
         active4 *= 3 & parseInt(`${active4}`);
      }
         active4 += parseInt(`${active4}`) ^ 1;
         active4 += parseInt(`${active4}`);
      libavdevicer += `${cornerkicks.size | parseInt(`${active4}`)}`;
          if (onClose) onClose();

   while (5 >= (theme4.length % 2) || 2 >= (theme4.length % 2)) {
      theme4 = [3];
      break;
   }
   if (2 <= (5 << (Math.min(2, theme4.length)))) {
       let pagination7 = true;
       let componentsn = String.fromCharCode(105,110,116,101,114,97,99,116,105,111,110,95,103,95,55,0);
       let internetb = String.fromCharCode(114,95,52,54,95,105,110,116,101,114,108,101,97,118,101,100,0);
       let favicon2 = false;
       let form2 = String.fromCharCode(115,95,56,49,95,115,117,112,101,114,115,101,116,0);
       let current2: Map<any, any> = new Map([[String.fromCharCode(98,95,53,48,95,118,97,108,105,100,97,116,105,110,103,0),940], [String.fromCharCode(108,105,98,120,109,108,95,119,95,57,50,0),419], [String.fromCharCode(118,101,114,98,97,116,105,109,95,116,95,51,55,0),922]]);
       let handlerX: Map<any, any> = new Map([[String.fromCharCode(110,95,50,53,95,98,111,119,108,105,110,103,0),396], [String.fromCharCode(122,95,54,49,95,116,105,108,105,110,103,0),234], [String.fromCharCode(117,95,54,50,95,116,117,110,110,101,108,101,100,0),680]]);
         form2 = `${internetb.length}`;
         current2.set(componentsn, handlerX.size & 2);
         handlerX.set(form2, (String.fromCharCode(77,0) == form2 ? current2.size : form2.length));
      while (!favicon2) {
         favicon2 = componentsn.length < 19 || current2.size < 19;
         break;
      }
         current2.set(form2, ((favicon2 ? 2 : 5)));
      if ((handlerX.size ^ 3) <= 5 || 4 <= (form2.length ^ 3)) {
         handlerX = new Map([[form2, form2.length]]);
      }
         current2.set(`${pagination7}`, internetb.length);
       let recommendation8 = 0.0;
         favicon2 = parseInt(`${recommendation8}`) < handlerX.size;
      let mappingx = pagination7 ? !pagination7 : pagination7;
      do {
         pagination7 = form2.length < 10 && pagination7;
         if (mappingx) {
            break;
         }
      } while ((!componentsn.includes(`${pagination7}`)) && mappingx);
      while (5 < (form2.length / 4)) {
         form2 = `${1 | form2.length}`;
         break;
      }
      for (let m = 0; m < 1; m++) {
         form2 += `${form2.length}`;
      }
      let statsD = internetb == String.fromCharCode(117,113,107,99,55,0);
      do {
         internetb = "2";
         if (statsD) {
            break;
         }
      } while (statsD && (current2.size == internetb.length));
          let zhuboW: Array<any> = [213, 911, 863];
          let playlisti = String.fromCharCode(98,95,49,49,95,105,99,101,0);
         pagination7 = (current2.size | internetb.length) == 54;
         zhuboW = [zhuboW.length - 2];
         playlisti += "2";
      cornerkicks = new Map([[`${plashp.length}`, parseInt(`${libzeus1}`)]]);
   }
   let dependencyj = 9805578 <= emojiB.length;
   do {
      emojiB = `${parseInt(`${selectede}`)}`;
      if (dependencyj) {
         break;
      }
   } while (((3 - emojiB.length) >= 5 || (3 - emojiB.length) >= 1) && dependencyj);
      cornerkicks = new Map([[`${cornerkicks.size}`, cornerkicks.size]]);
          setShowBecomeVIPOverlay(false);
        }}
      />
    }
  };


  return (
    <>
      {showBecomeVIPOverlay && (
        <View
          style={{
            height: '100%',
            width: '100%',
            
            position: 'absolute',
            zIndex: 1000,
          }}>
          {renderOverlay()}
        </View>
      )}
    </>
  );
}

export default ExpiredOverlay

const styles = StyleSheet.create({
  closeBtnContainer: {
    height: 20,
    width: 20,
    marginTop: -20,
    marginRight: -10,
    borderRadius: 10,
    alignSelf: 'flex-end',
    backgroundColor: 'rgba(201, 201, 201, 0.3)',
  },

  btnContainer: {
    flexDirection: 'row',
    justifyContent: 'space-evenly',
    gap: 10,
    paddingTop: 25,
    paddingBottom: 10,
  },

  btn: {
    backgroundColor: '#FAC33D',
    paddingVertical: 10,
    alignItems: 'center',
    borderRadius: 10,
    flex: 1,
  }
})
