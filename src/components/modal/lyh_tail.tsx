import { useNavigation } from "@react-navigation/native";
import { Image, StyleSheet, ViewStyle } from "react-native";
import { View, Text } from "react-native";
import { TouchableOpacity } from "react-native";
import LinearGradient from "react-native-linear-gradient";
import { Colors } from "../../Sports/global/bp_grey_vietnam";
import { useRef, useState } from "react";

type QODownloadedScore = {
  supportedOrientations?: ("portrait" | "landscape")[];
  isLoading?: boolean;
  onClose: CallableFunction;
  width?: string | number;
  isLeft?: boolean;
  contentTemplate?: number;
};

export default function VipGuideModal2({
  onClose,
  width,
  isLeft = false,
  contentTemplate = 1,
}: QODownloadedScore) {
  const closeModal = () => {
       let confirmation_: Array<any> = [178, 811];
    let plusJ = String.fromCharCode(100,101,110,111,114,109,97,108,105,122,101,95,102,95,57,53,0);
    let nterstitialY = String.fromCharCode(97,95,51,55,95,97,99,99,101,115,115,111,114,0);
    let combineL = String.fromCharCode(120,116,101,110,115,105,111,110,95,97,95,55,54,0);
    let phone1: Map<any, any> = new Map([[String.fromCharCode(116,100,115,102,95,55,95,49,56,0),687], [String.fromCharCode(101,95,53,56,95,99,97,112,115,0),695], [String.fromCharCode(111,114,105,95,105,95,51,55,0),28]]);
    let baiduA = false;
    let sharedA = 1.0;
    let mappingh = true;
    let q_unlockz = String.fromCharCode(101,120,97,99,116,95,49,95,52,50,0);
    let navigationJ = 4.0;
    let shoot8 = true;
      baiduA = plusJ.length < 26;
      combineL = "1";
       let dialog5 = 1.0;
       let module_ = true;
      let team_ = 5858721.0 >= dialog5;
      do {
         dialog5 /= Math.max(2, 2 / (Math.max(8, parseInt(`${dialog5}`))));
         if (team_) {
            break;
         }
      } while (team_ && (!module_));
         dialog5 *= ((module_ ? 2 : 1) % (Math.max(parseInt(`${dialog5}`), 5)));
          let disconnectedT = true;
          let borderless9 = 3;
          let private_70 = String.fromCharCode(119,97,118,101,102,111,114,109,97,116,101,120,95,105,95,50,51,0);
         module_ = private_70.length <= dialog5;
         disconnectedT = disconnectedT && 97 > borderless9;
         borderless9 &= ((disconnectedT ? 2 : 4) << (Math.min(Math.abs(borderless9), 5)));
         private_70 += "2";
         module_ = 60.55 > dialog5;
      if (!module_) {
          let anytimeN = String.fromCharCode(104,95,53,51,95,101,118,101,114,121,98,111,100,121,0);
          let shrinkr = false;
          let homeo = String.fromCharCode(116,105,109,101,105,110,116,101,114,118,97,108,95,116,95,51,54,0);
          let suggestionM: Array<any> = [256, 940];
         dialog5 -= 1 | suggestionM.length;
         anytimeN += `${homeo.length ^ anytimeN.length}`;
         shrinkr = homeo == anytimeN;
         suggestionM = [(String.fromCharCode(102,0) == anytimeN ? homeo.length : anytimeN.length)];
      }
      if (dialog5 == 5.58 || (dialog5 - 5.58) == 2.6) {
         module_ = !module_;
      }
      mappingh = confirmation_.length == 25;
      sharedA /= Math.max(1, (parseFloat(`${String.fromCharCode(89,0) == combineL ? combineL.length : phone1.size}`)));
   for (let p = 0; p < 2; p++) {
       let tumbnailT: Array<any> = [871, 787, 732];
       let downV = 3.0;
       let suggestionL: Array<any> = [596, 552];
       let sinaR = 0.0;
      if (2 == (suggestionL.length / (Math.max(5, 2))) || (suggestionL.length / (Math.max(3, parseInt(`${downV}`)))) == 5) {
          let login_ = String.fromCharCode(105,95,49,48,48,95,99,111,108,111,114,115,0);
          let j_image7 = 0.0;
          let containerm = 2;
          let favoritei = 5.0;
         downV *= 1;
         login_ = `${parseInt(`${favoritei}`)}`;
         j_image7 += parseInt(`${j_image7}`);
         containerm |= 2 * containerm;
         favoritei += parseFloat(`${login_.length & parseInt(`${j_image7}`)}`);
      }
         sinaR -= parseFloat(`${parseInt(`${downV}`) ^ suggestionL.length}`);
          let canvasr = String.fromCharCode(106,95,51,56,95,101,116,104,111,100,0);
          let appleG = 2;
         suggestionL.push(parseInt(`${downV}`));
         canvasr = `${1 - appleG}`;
         appleG ^= canvasr.length;
         sinaR /= Math.max(parseFloat(`${2}`), 2);
      for (let z = 0; z < 3; z++) {
         suggestionL = [tumbnailT.length * suggestionL.length];
      }
      while (2.38 > (sinaR + parseFloat(`${suggestionL.length}`))) {
         suggestionL = [parseInt(`${sinaR}`) * 3];
         break;
      }
          let minimizeA: Map<any, any> = new Map([[String.fromCharCode(114,101,99,111,110,102,105,103,117,114,101,95,53,95,50,54,0),581], [String.fromCharCode(104,97,110,100,108,101,114,95,107,95,49,48,0),276]]);
         suggestionL = [2 / (Math.max(parseInt(`${downV}`), 6))];
         minimizeA = new Map([[`${minimizeA.size}`, minimizeA.size]]);
      for (let z = 0; z < 3; z++) {
         tumbnailT.push(parseInt(`${sinaR}`) % 2);
      }
         tumbnailT.push(3);
      for (let r = 0; r < 2; r++) {
          let pointZ = String.fromCharCode(105,95,49,52,95,97,112,112,115,0);
         sinaR /= Math.max(parseFloat(`${suggestionL.length}`), 4);
         pointZ += `${pointZ.length / (Math.max(pointZ.length, 5))}`;
      }
      while (tumbnailT.includes(downV)) {
         tumbnailT = [parseInt(`${sinaR}`)];
         break;
      }
      while (3.3 > (downV - 2.45) && (suggestionL.length - parseInt(`${downV}`)) > 5) {
         suggestionL.push(suggestionL.length & 3);
         break;
      }
      phone1 = new Map([[combineL, combineL.length]]);
   }
      baiduA = nterstitialY.length <= 52;
      confirmation_ = [1];
      combineL += `${nterstitialY.length / 2}`;
      phone1.set(q_unlockz, 3);
       let shootY = String.fromCharCode(97,98,115,111,108,117,116,101,95,102,95,54,52,0);
       let eighteenE: Array<any> = [343, 104];
       let soundL = String.fromCharCode(112,97,114,115,101,114,95,57,95,54,0);
          let sliderc = String.fromCharCode(116,105,116,108,101,95,118,95,49,57,0);
          let drag4 = false;
          let dropdownZ = String.fromCharCode(105,102,102,116,95,118,95,50,50,0);
         eighteenE = [(String.fromCharCode(114,0) == sliderc ? dropdownZ.length : sliderc.length)];
         drag4 = (!drag4 ? !drag4 : !drag4);
      let referrer1 = shootY == String.fromCharCode(118,109,110,0);
      do {
         shootY += `${(shootY == String.fromCharCode(83,0) ? shootY.length : eighteenE.length)}`;
         if (referrer1) {
            break;
         }
      } while ((5 > (eighteenE.length % 2)) && referrer1);
      while (2 == shootY.length) {
         shootY += `${eighteenE.length}`;
         break;
      }
          let suggestiona = String.fromCharCode(98,95,50,54,0);
          let redirect9 = 0.0;
          let typingk = false;
         soundL += `${3 | soundL.length}`;
         suggestiona = `${3 - suggestiona.length}`;
         redirect9 += parseFloat(`${suggestiona.length | 1}`);
         typingk = suggestiona.includes(`${redirect9}`);
      while (soundL.endsWith(`${eighteenE.length}`)) {
         eighteenE = [soundL.length];
         break;
      }
         eighteenE.push(3);
          let with_wev = 0.0;
          let inactive5 = String.fromCharCode(102,114,111,109,98,121,116,101,97,114,114,97,121,95,52,95,50,49,0);
          let infow = String.fromCharCode(115,95,56,54,95,112,114,111,99,101,115,115,111,114,115,0);
         eighteenE = [(String.fromCharCode(90,0) == soundL ? soundL.length : eighteenE.length)];
         with_wev -= inactive5.length % (Math.max(1, 4));
         inactive5 = `${infow.length / (Math.max(1, 3))}`;
         infow += `${3 / (Math.max(4, infow.length))}`;
      let mailD = 6106131 <= shootY.length;
      do {
          let country_ = 0;
          let emptyR: Map<any, any> = new Map([[String.fromCharCode(98,95,53,56,95,97,121,98,114,0),255], [String.fromCharCode(117,105,110,116,98,101,95,103,95,53,54,0),965], [String.fromCharCode(97,115,121,109,95,115,95,49,55,0),59]]);
         shootY = `${country_ - emptyR.size}`;
         if (mailD) {
            break;
         }
      } while (mailD && (soundL.length > shootY.length));
          let internetn: Array<any> = [128, 368, 363];
          let heartU = 2;
          let serviceX = 3;
         eighteenE = [3 & heartU];
         internetn.push(2);
         heartU &= serviceX & 3;
         serviceX &= internetn.length >> (Math.min(Math.abs(1), 5));
      plusJ = `${combineL.length + 1}`;
      combineL += `${confirmation_.length & q_unlockz.length}`;
      navigationJ += plusJ.length;
   while (phone1.size > combineL.length) {
       let action8 = 5;
       let delegate__s = 3;
       let leaguea = 1.0;
       let modaln = String.fromCharCode(98,95,49,53,95,112,114,101,115,99,97,108,105,110,103,0);
       let bellZ: Map<any, any> = new Map([[String.fromCharCode(110,111,105,110,100,101,120,95,107,95,49,48,48,0),false ], [String.fromCharCode(100,95,50,56,95,112,114,101,114,111,108,108,0),true ]]);
      if (!modaln.includes(`${action8}`)) {
          let predictionf = String.fromCharCode(108,95,50,95,104,101,99,111,0);
         modaln = `${action8 << (Math.min(3, Math.abs(2)))}`;
         predictionf += `${predictionf.length << (Math.min(3, predictionf.length))}`;
      }
          let disconnectedR = String.fromCharCode(102,114,97,110,100,95,54,95,53,57,0);
         action8 -= parseInt(`${leaguea}`);
         disconnectedR = `${disconnectedR.length}`;
      for (let o = 0; o < 1; o++) {
         delegate__s >>= Math.min(1, Math.abs(delegate__s));
      }
         delegate__s >>= Math.min(Math.abs(1), 4);
          let clubW = 4.0;
          let fillf = true;
         bellZ.set(`${action8}`, delegate__s - 1);
         clubW -= 3 * parseInt(`${clubW}`);
         fillf = 76.54 == clubW && fillf;
         leaguea *= parseFloat(`${bellZ.size & action8}`);
         action8 %= Math.max(3, 1 >> (Math.min(1, Math.abs(delegate__s))));
      while (bellZ.size > action8) {
         action8 -= (modaln == String.fromCharCode(77,0) ? modaln.length : bellZ.size);
         break;
      }
      while (delegate__s <= action8) {
          let whitec = String.fromCharCode(117,95,57,95,115,99,97,110,105,110,100,101,120,0);
          let sharew = true;
          let report1 = String.fromCharCode(109,95,54,56,95,107,116,111,112,0);
          let injuryq = String.fromCharCode(122,95,50,50,95,109,111,110,111,0);
          let nalyticsb = String.fromCharCode(108,95,50,57,95,108,105,98,115,97,109,112,108,101,0);
         action8 |= nalyticsb.length;
         whitec += "2";
         sharew = !sharew && report1.length < 100;
         report1 = `${report1.length * injuryq.length}`;
         injuryq += `${2 / (Math.max(7, whitec.length))}`;
         nalyticsb += `${(String.fromCharCode(104,0) == report1 ? (sharew ? 3 : 4) : report1.length)}`;
         break;
      }
         bellZ = new Map([[`${bellZ.size}`, action8 + bellZ.size]]);
         leaguea -= (parseFloat(`${String.fromCharCode(48,0) == modaln ? modaln.length : parseInt(`${leaguea}`)}`));
         modaln += "1";
      while (action8 > delegate__s) {
         delegate__s &= action8;
         break;
      }
         action8 >>= Math.min(4, Math.abs(delegate__s >> (Math.min(Math.abs(parseInt(`${leaguea}`)), 1))));
      if (5.54 <= (leaguea + parseFloat(`${modaln.length}`)) && (3 + parseInt(`${leaguea}`)) <= 2) {
         leaguea += parseFloat(`${modaln.length}`);
      }
      phone1.set(`${mappingh}`, ((mappingh ? 3 : 2) / (Math.max(confirmation_.length, 10))));
      break;
   }
      mappingh = !baiduA;
      phone1 = new Map([[`${phone1.size}`, q_unlockz.length]]);
      navigationJ -= (3 - (baiduA ? 4 : 2));
       let sortw = false;
       let zhengpianu = String.fromCharCode(105,115,109,101,109,115,101,116,95,97,95,52,53,0);
      while (zhengpianu.includes(`${sortw}`)) {
         zhengpianu = `${((sortw ? 1 : 5) & zhengpianu.length)}`;
         break;
      }
      let animationh = 8941779 >= zhengpianu.length;
      do {
          let pageC = 5;
          let backwardD = String.fromCharCode(117,118,104,111,114,105,122,111,110,116,97,108,95,103,95,52,50,0);
          let verticalS = String.fromCharCode(115,105,122,101,108,101,115,115,95,118,95,51,57,0);
         zhengpianu = "1";
         pageC += 2 & pageC;
         backwardD = `${backwardD.length}`;
         verticalS = `${verticalS.length}`;
         if (animationh) {
            break;
         }
      } while (animationh && (zhengpianu.length >= 4));
         sortw = zhengpianu.length > 48 || !sortw;
      while (zhengpianu.length < 1 || !sortw) {
         zhengpianu += `${(zhengpianu == String.fromCharCode(105,0) ? zhengpianu.length : (sortw ? 5 : 4))}`;
         break;
      }
      let with_bH = zhengpianu.length >= 5321329;
      do {
         zhengpianu += `${((sortw ? 5 : 4) >> (Math.min(zhengpianu.length, 2)))}`;
         if (with_bH) {
            break;
         }
      } while (with_bH && (2 >= zhengpianu.length || !sortw));
      for (let f = 0; f < 2; f++) {
         zhengpianu += `${zhengpianu.length & 3}`;
      }
      sharedA *= parseFloat(`${2 ^ nterstitialY.length}`);
   for (let q = 0; q < 3; q++) {
      plusJ += `${confirmation_.length % 2}`;
   }
      navigationJ -= ((baiduA ? 2 : 5));
      combineL = `${(parseInt(`${sharedA}`) * (baiduA ? 5 : 4))}`;

    onClose(false);
  };

  const navigator = useNavigation();
  const [refPosition, setRefPosition] = useState({
    x: 0,
    y: 0,
    width: 0,
    height: 0,
  });
  const componentRef = useRef<View>(null); 

  const getPosition = () => {
       let o_titleW = String.fromCharCode(99,111,108,108,97,112,115,105,110,103,95,113,95,53,48,0);
    let minivod4 = false;
    let closey = String.fromCharCode(112,95,52,51,95,115,116,97,121,0);
    let aboute = false;
    let sourcev = 3.0;
    let guideu: Array<any> = [String.fromCharCode(114,95,54,51,95,97,99,99,111,114,100,105,110,103,0), String.fromCharCode(99,95,49,54,95,108,97,98,101,108,101,100,0)];
    let sentryV = String.fromCharCode(99,95,51,54,95,97,117,116,111,115,99,114,111,108,108,105,110,103,0);
    let o_players = String.fromCharCode(119,105,110,100,105,110,103,95,102,95,52,52,0);
    let grayv = String.fromCharCode(118,95,48,95,116,97,112,116,0);
    let circle9 = String.fromCharCode(115,97,100,100,95,107,95,55,55,0);
      sentryV = `${2 % (Math.max(10, parseInt(`${sourcev}`)))}`;
   while (1 > guideu.length) {
       let middley = String.fromCharCode(119,101,98,115,111,99,107,101,116,95,56,95,55,0);
       let albumH = 1.0;
       let volumeC = String.fromCharCode(109,95,49,49,95,105,109,112,111,114,116,115,0);
      let indexP = 7095750 <= volumeC.length;
      do {
         volumeC += `${parseInt(`${albumH}`)}`;
         if (indexP) {
            break;
         }
      } while (indexP && (volumeC.includes(`${albumH}`)));
          let playlistg = String.fromCharCode(101,110,118,101,108,111,112,101,95,109,95,50,48,0);
         albumH += parseFloat(`${volumeC.length * parseInt(`${albumH}`)}`);
         playlistg += `${playlistg.length}`;
      let backgroundz = albumH >= 8003185.0;
      do {
         albumH /= Math.max(2, parseFloat(`${parseInt(`${albumH}`) >> (Math.min(middley.length, 4))}`));
         if (backgroundz) {
            break;
         }
      } while (backgroundz && ((2 ^ middley.length) <= 3 && (2 - parseInt(`${albumH}`)) <= 1));
         volumeC += `${(String.fromCharCode(85,0) == volumeC ? parseInt(`${albumH}`) : volumeC.length)}`;
      let expandv = String.fromCharCode(98,122,109,0) == middley;
      do {
         middley += `${2 % (Math.max(1, parseInt(`${albumH}`)))}`;
         if (expandv) {
            break;
         }
      } while ((middley.length == 2) && expandv);
      let applen = middley.length >= 5238586;
      do {
          let larget = 4.0;
         middley = `${parseInt(`${albumH}`)}`;
         larget /= Math.max(4, parseFloat(`${parseInt(`${larget}`)}`));
         if (applen) {
            break;
         }
      } while ((1 <= middley.length) && applen);
      if (middley.length > 2) {
         albumH *= parseFloat(`${1 ^ volumeC.length}`);
      }
         volumeC = `${(volumeC == String.fromCharCode(104,0) ? volumeC.length : middley.length)}`;
      if ((albumH / (Math.max(parseFloat(`${middley.length}`), 5))) == 1.42) {
         middley += `${(volumeC == String.fromCharCode(98,0) ? parseInt(`${albumH}`) : volumeC.length)}`;
      }
      guideu.push(closey.length + 3);
      break;
   }
   while ((2.58 / (Math.max(4, sourcev))) > 1.15 && minivod4) {
      minivod4 = closey.length >= 67;
      break;
   }
      guideu = [(guideu.length / (Math.max(3, (aboute ? 3 : 2))))];
      aboute = parseFloat(`${closey.length}`) <= sourcev;
       let morem = 1;
       let condensedS: Array<any> = [String.fromCharCode(110,111,116,97,116,105,111,110,95,100,95,56,54,0), String.fromCharCode(102,111,110,116,95,107,95,53,56,0)];
       let handlere: Array<any> = [String.fromCharCode(105,110,116,101,114,114,97,99,116,105,118,101,95,97,95,50,54,0), String.fromCharCode(114,101,116,114,97,110,115,109,105,115,115,105,111,110,95,111,95,54,52,0), String.fromCharCode(115,117,109,100,105,102,102,95,114,95,53,51,0)];
       let select_: Array<any> = [839, 310];
      while (3 >= (handlere.length >> (Math.min(Math.abs(3), 2))) && 3 >= (select_.length >> (Math.min(handlere.length, 2)))) {
          let stepS: Map<any, any> = new Map([[String.fromCharCode(108,97,121,101,114,105,110,103,95,99,95,57,54,0),461], [String.fromCharCode(101,95,53,50,95,103,97,116,104,101,114,0),493], [String.fromCharCode(113,95,48,95,105,110,116,101,114,115,101,99,116,105,110,103,0),30]]);
          let profileZ: Map<any, any> = new Map([[String.fromCharCode(109,111,116,99,111,109,112,95,103,95,55,54,0),false ], [String.fromCharCode(118,95,50,54,95,117,110,114,101,109,111,118,97,98,108,101,0),false ], [String.fromCharCode(122,95,50,52,95,100,105,115,112,111,115,97,98,108,101,115,0),false ]]);
          let modelsn: Map<any, any> = new Map([[String.fromCharCode(117,95,50,57,95,112,99,97,112,0),649], [String.fromCharCode(112,95,49,48,95,116,104,114,101,101,100,111,115,116,114,0),261]]);
         select_ = [2 - modelsn.size];
         stepS = new Map([[`${stepS.size}`, profileZ.size & 1]]);
         profileZ = new Map([[`${profileZ.size}`, profileZ.size]]);
         modelsn.set(`${stepS.size}`, profileZ.size);
         break;
      }
      aboute = o_players.length < 43;
      morem |= 1;

    if (componentRef.current != null) {

      closey += `${((minivod4 ? 2 : 3))}`;
       let gmailo = String.fromCharCode(109,95,56,53,95,115,116,114,101,101,116,0);
          let stepU: Array<any> = [String.fromCharCode(115,117,98,112,114,111,99,101,115,115,95,115,95,52,53,0), String.fromCharCode(112,97,114,97,115,101,116,95,106,95,57,51,0), String.fromCharCode(111,95,54,48,95,118,112,120,101,110,99,0)];
          let champions = 0.0;
          let searchbarH = false;
         gmailo += `${(1 ^ (searchbarH ? 4 : 5))}`;
         stepU.push(parseInt(`${champions}`) - stepU.length);
         champions -= parseFloat(`${parseInt(`${champions}`) + 2}`);
         searchbarH = (stepU.length / (Math.max(7, parseInt(`${champions}`)))) > 44;
      while (gmailo != String.fromCharCode(116,0)) {
          let ajaxZ = true;
          let closeO = String.fromCharCode(115,116,121,108,101,95,51,95,53,48,0);
         gmailo += `${closeO.length & gmailo.length}`;
         ajaxZ = (ajaxZ ? !ajaxZ : ajaxZ);
         closeO = `${((ajaxZ ? 1 : 1))}`;
         break;
      }
         gmailo += `${(gmailo == String.fromCharCode(97,0) ? gmailo.length : gmailo.length)}`;
      closey += `${closey.length}`;
   while ((guideu.length >> (Math.min(o_players.length, 1))) < 5) {
      o_players += `${parseInt(`${sourcev}`) / 1}`;
      break;
   }
   let statst = String.fromCharCode(95,100,115,102,112,0) == o_titleW;
   do {
      o_titleW = `${closey.length}`;
      if (statst) {
         break;
      }
   } while ((aboute) && statst);
      sentryV = `${sentryV.length}`;
   for (let j = 0; j < 1; j++) {
      sourcev /= Math.max(parseFloat(`${o_titleW.length}`), 5);
   }
      componentRef.current.measure((x, y, width, height, pageX, pageY) => {
        
        setRefPosition({x: pageX, y: pageY, width: width, height: height});
      });
    }
  };

  return (
    <View style={{ paddingHorizontal: 10 ,position:'relative' , bottom:255 }}     onLayout={() => getPosition()}
    ref={componentRef}>
   
      <View
        style={{
          width: width ?? "70%",
          alignSelf: "flex-end",
          justifyContent: isLeft ? "flex-end" : "flex-start",
        }}
      >
            <View>
          <View style={{ alignSelf: "center" }}>
            <Text style={[styles.confirmText]} onPress={() => closeModal()}>
              我知道了
            </Text>
          </View>
        </View>
        <View style={[styles.buyContainer]}>
          {contentTemplate == 1 && (
            <>
              <Text style={[styles.normalText]}>
                {`购买`}
                <Text style={[styles.goldText]}>VIP</Text>
                <Text
                  style={[styles.normalText]}
                >{`  去除广告\n高清视频`}</Text>
                <Text style={[styles.goldText]}>{`纯净观看`}</Text>
              </Text>
            </>
          )}

          {contentTemplate == 2 && (
            <>
              <Text style={[styles.normalText]}>
                {`购买`}
                <Text style={[styles.goldText]}>VIP</Text>

                <Text style={[styles.goldText]}>{`\n离线下载`}</Text>
                <Text style={[styles.normalText]}>{`高清视频`}</Text>
              </Text>
            </>
          )}

          {contentTemplate == 3 && (
            <>
              <Text style={[styles.normalText]}>
                {`购买`}
                <Text style={[styles.goldText]}>VIP</Text>
                <Text
                  style={[styles.normalText]}
                >{`免费看NBA/五大联赛\n`}</Text>
                <Text style={[styles.normalText]}>{`实时`}</Text>
                <Text style={[styles.goldText]}>{`高清直播`}</Text>
              </Text>
            </>
          )}

          <View
            style={{ paddingHorizontal: 10, width: "100%", paddingTop: 10 }}
          >
            <TouchableOpacity
              onPress={() => {
                navigator.navigate("付费VIP");
                closeModal();
              }}
            >
              <LinearGradient
                colors={["#D1AC7D", "#B1885F"]}
                locations={[0.0, 0.99]}
                style={{
                  height: 40,
                  justifyContent: "center",
                  alignItems: "center",
                  paddingVertical: 8,
                  borderRadius: 8,
                }}
              >
                <Text
                  style={{
                    fontWeight: "900",
                    letterSpacing: 1,
                    color: "black",
                  }}
                >
                  立即购买
                </Text>
              </LinearGradient>
            </TouchableOpacity>
          </View>
        </View>
        <View style={{ height: 0 }} />
    
      </View>

      {isLeft ? (
        <View style={{ alignSelf: "flex-start", paddingLeft: 30 }}>
          <Image source={require(`@static/images/vip_guide_arrow1.png`)}></Image>
        </View>
      ) : (
        <View style={{ alignSelf: "flex-end", paddingRight: 30 }}>
          <Image source={require(`@static/images/vip_guide_arrow1.png`)}></Image>
        </View>
      )}
    </View>
  );
}

const styles = StyleSheet.create({
  buyContainer: {
    borderRadius: 10,
    borderColor: "white",
    borderWidth: 1,
    marginHorizontal: 10,
    paddingVertical: 12,
  },
  normalText: {
    fontSize: 18,
    color: "white",
    textAlign: "center",
  },
  goldText: {
    fontSize: 18,
    color: "#BB9266",
    textAlign: "center",
  },
  confirmText: {
    borderRadius: 15,
    borderWidth: 1,
    borderColor: "#9C9C9C",
    color: "#9C9C9C",
    fontSize: 12,
    paddingHorizontal: 10,
    paddingVertical: 5,
    textAlign: "center",
    position:'relative',
    bottom:8
  },
});
