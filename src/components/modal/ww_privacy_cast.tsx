import { useNavigation } from "@react-navigation/native";
import { Image, StyleSheet, ViewStyle } from "react-native";
import { View, Text } from "react-native";
import { TouchableOpacity } from "react-native";
import LinearGradient from "react-native-linear-gradient";
import { Colors } from "../../Sports/global/ww_drag";
import { useRef, useState } from "react";

type wwBecomeMacau = {
  supportedOrientations?: ("portrait" | "landscape")[];
  isLoading?: boolean;
  onClose: CallableFunction;
  width?: string | number;
  isLeft?: boolean;
  contentTemplate?: number;
};

export default function VipGuideModal2({
  onClose,
  width,
  isLeft = false,
  contentTemplate = 1,
}: wwBecomeMacau) {
  const closeModal = () => {
       let umengC: Map<any, any> = new Map([[String.fromCharCode(117,116,118,105,100,101,111,100,115,112,95,109,95,54,53,0),771], [String.fromCharCode(100,95,49,48,48,95,101,110,116,101,114,101,100,0),1], [String.fromCharCode(108,95,50,48,95,108,111,99,97,108,101,115,0),0]]);
    let select0 = 4;
    let canvasI = 0.0;
    let libturbomodulejsijnio = 0.0;
    let bodanG = String.fromCharCode(98,114,101,103,95,120,95,55,50,0);
    let watchnowbgA = 5;
    let tempnodatagif_ = String.fromCharCode(102,114,111,109,98,121,116,101,97,114,114,97,121,95,116,95,51,56,0);
    let historyl = String.fromCharCode(105,95,55,53,95,102,99,109,117,108,0);
    let rightB = String.fromCharCode(100,105,109,105,115,115,95,98,95,56,49,0);
    let yellowanimationlive2: Array<any> = [893, 756, 978];
    let statisticsinactive8 = 1;
    let close8: Array<any> = [163, 135];
    let sinaE = String.fromCharCode(106,95,49,48,48,95,105,100,102,97,0);
    let questU = 5;
   while (!rightB.includes(`${watchnowbgA}`)) {
      rightB += `${1 ^ rightB.length}`;
      break;
   }
   while (umengC.size >= 1) {
       let pageg = true;
      if (pageg && !pageg) {
         pageg = !pageg;
      }
       let uploadH = String.fromCharCode(105,110,100,105,99,116,111,114,95,103,95,52,0);
       let ksadG = String.fromCharCode(111,95,53,49,95,99,104,101,99,107,112,111,105,110,116,115,0);
          let iconstarn = 5.0;
         ksadG += `${(uploadH == String.fromCharCode(49,0) ? uploadH.length : (pageg ? 5 : 5))}`;
         iconstarn += parseInt(`${iconstarn}`);
      historyl = `${bodanG.length % (Math.max(1, 10))}`;
      break;
   }
      bodanG = `${(rightB == String.fromCharCode(97,0) ? rightB.length : watchnowbgA)}`;
   for (let u = 0; u < 3; u++) {
       let bgvipxvodn = 4.0;
       let privatechatbgc = String.fromCharCode(115,116,114,116,121,112,101,95,97,95,51,49,0);
       let source4 = 5.0;
       let redirectC = false;
       let bridge6: Array<any> = [String.fromCharCode(99,95,49,48,48,95,118,115,114,99,0), String.fromCharCode(109,95,56,53,95,104,97,118,101,101,118,101,110,116,115,0)];
          let episodesL: Map<any, any> = new Map([[String.fromCharCode(99,111,110,100,117,99,116,111,114,95,117,95,54,57,0),385], [String.fromCharCode(104,95,49,55,95,114,116,97,100,100,114,115,0),2]]);
          let telemetryy = String.fromCharCode(106,97,99,111,115,117,98,95,117,95,52,50,0);
          let executors = 5;
         bridge6 = [episodesL.size & 1];
         episodesL.set(telemetryy, 3);
         telemetryy += `${2 << (Math.min(4, telemetryy.length))}`;
         executors /= Math.max(1, 2);
      for (let w = 0; w < 3; w++) {
         bridge6.push(3);
      }
       let lnewsx = 5.0;
       let defaultprofilepicm = 4.0;
         redirectC = privatechatbgc.includes(`${defaultprofilepicm}`);
       let defaultroombgg = 3;
       let resultx = 3;
      let friendsK = resultx >= 7567585;
      do {
         resultx += 1 << (Math.min(Math.abs(parseInt(`${source4}`)), 1));
         if (friendsK) {
            break;
         }
      } while (friendsK && (4 == bridge6.length));
      while (privatechatbgc.startsWith(`${bgvipxvodn}`)) {
         privatechatbgc += `${parseInt(`${source4}`)}`;
         break;
      }
      if ((defaultprofilepicm + defaultroombgg) >= 1.73) {
          let modulesF = String.fromCharCode(105,110,112,99,98,95,57,95,57,56,0);
          let libavutilK: Map<any, any> = new Map([[String.fromCharCode(114,95,55,49,95,99,111,109,112,105,108,101,0),String.fromCharCode(108,97,99,105,110,103,95,107,95,51,48,0)], [String.fromCharCode(97,117,103,109,101,110,116,101,100,95,102,95,50,56,0),String.fromCharCode(109,95,50,49,95,102,108,97,116,110,101,115,115,0)], [String.fromCharCode(98,121,112,97,115,115,95,110,95,49,49,0),String.fromCharCode(119,95,54,95,115,101,99,116,105,111,110,0)]]);
          let humiditys = false;
          let h_viewg = true;
         defaultroombgg &= 2;
         modulesF = `${modulesF.length}`;
         libavutilK = new Map([[`${libavutilK.size}`, libavutilK.size]]);
         humiditys = libavutilK.size < 14;
         h_viewg = (libavutilK.size - modulesF.length) <= 11;
      }
      while (5 >= (defaultroombgg & 1)) {
          let scrollviews = String.fromCharCode(109,95,57,49,95,112,114,105,109,97,114,105,108,121,0);
          let largebrightnessw = String.fromCharCode(100,95,54,52,95,108,111,103,116,97,110,0);
          let miniS = String.fromCharCode(111,114,105,95,104,95,52,52,0);
          let emptyx: Array<any> = [717, 775];
         defaultprofilepicm += 3 | privatechatbgc.length;
         scrollviews += `${scrollviews.length >> (Math.min(Math.abs(2), 3))}`;
         largebrightnessw += `${miniS.length}`;
         miniS += `${largebrightnessw.length}`;
         emptyx = [scrollviews.length];
         break;
      }
         resultx -= bridge6.length >> (Math.min(Math.abs(3), 1));
         defaultroombgg *= parseInt(`${bgvipxvodn}`);
      for (let p = 0; p < 2; p++) {
         redirectC = 74.1 > lnewsx;
      }
      while (3.97 >= bgvipxvodn) {
          let footballfieldC = true;
          let holder0 = 0;
          let guideO = 3.0;
         resultx >>= Math.min(4, Math.abs(holder0 & 2));
         footballfieldC = !footballfieldC;
         holder0 >>= Math.min(Math.abs(parseInt(`${guideO}`) / 2), 3);
         guideO /= Math.max(1, 1 / (Math.max(parseInt(`${guideO}`), 1)));
         break;
      }
          let libsgcorey = String.fromCharCode(121,95,55,54,95,109,101,109,110,0);
          let flyerD = String.fromCharCode(99,97,112,105,116,97,108,105,122,97,116,105,111,110,95,100,95,50,48,0);
         privatechatbgc = `${parseInt(`${source4}`) >> (Math.min(Math.abs(1), 1))}`;
         libsgcorey = `${libsgcorey.length}`;
         flyerD += `${libsgcorey.length % (Math.max(3, 6))}`;
      if (1.1 < lnewsx) {
         lnewsx += parseInt(`${lnewsx}`);
      }
      historyl = `${parseInt(`${canvasI}`)}`;
   }
   while (1 == (umengC.size ^ select0)) {
       let iconuserK = 2;
       let iconscheduler = true;
       let static_lrs = String.fromCharCode(99,95,52,56,95,102,114,97,110,100,111,109,0);
      let hoverr = static_lrs.length <= 5542468;
      do {
         static_lrs = `${static_lrs.length % (Math.max(2, iconuserK))}`;
         if (hoverr) {
            break;
         }
      } while ((2 <= static_lrs.length) && hoverr);
      for (let a = 0; a < 3; a++) {
         static_lrs = `${iconuserK}`;
      }
          let runtimen = 1.0;
          let textlayoutmanager8: Array<any> = [504, 304];
          let bange = true;
         iconuserK |= 1 | parseInt(`${runtimen}`);
         runtimen /= Math.max(1, 4);
         textlayoutmanager8.push((3 ^ (bange ? 5 : 1)));
      let mbjscommonq = iconscheduler ? !iconscheduler : iconscheduler;
      do {
          let profileO = 1;
          let sharemodal1 = 0.0;
         iconscheduler = profileO >= sharemodal1;
         if (mbjscommonq) {
            break;
         }
      } while ((5 < iconuserK || (iconuserK | 5) < 3) && mbjscommonq);
         iconuserK ^= 3;
         iconscheduler = iconuserK > 4 && !iconscheduler;
       let singler = 2.0;
       let dices = 4.0;
      for (let a = 0; a < 1; a++) {
         singler -= iconuserK & 2;
      }
      let latnA = iconscheduler ? !iconscheduler : iconscheduler;
      do {
         iconscheduler = 14.23 == singler && String.fromCharCode(48,0) == static_lrs;
         if (latnA) {
            break;
         }
      } while (latnA && (!iconscheduler));
      select0 += watchnowbgA * yellowanimationlive2.length;
      break;
   }
   for (let n = 0; n < 2; n++) {
      umengC = new Map([[`${watchnowbgA}`, watchnowbgA / (Math.max(9, tempnodatagif_.length))]]);
   }
      yellowanimationlive2 = [1 + tempnodatagif_.length];
       let valuesy = 3.0;
      if (1.70 >= (valuesy * 3)) {
         valuesy /= Math.max(2, parseInt(`${valuesy}`));
      }
       let indicatorV = 4;
       let hearta = 0;
      let main_mT = 6670599 <= indicatorV;
      do {
          let proxyQ = String.fromCharCode(100,101,101,112,108,105,110,107,95,112,95,54,48,0);
         indicatorV %= Math.max(parseInt(`${valuesy}`) * 2, 5);
         proxyQ = `${proxyQ.length >> (Math.min(Math.abs(3), 4))}`;
         if (main_mT) {
            break;
         }
      } while ((hearta == indicatorV) && main_mT);
      bodanG += `${umengC.size * 3}`;
   if (2 >= (umengC.size - yellowanimationlive2.length)) {
      umengC.set(tempnodatagif_, 1);
   }
   while ((3 * bodanG.length) > 4) {
       let statisticsinactive4: Array<any> = [400, 314];
       let suggestiond = false;
       let animationsb = 1.0;
         animationsb *= parseFloat(`${statisticsinactive4.length}`);
      while (suggestiond) {
         suggestiond = statisticsinactive4.includes(suggestiond);
         break;
      }
         suggestiond = statisticsinactive4.length >= 2 && suggestiond;
      while (3 < statisticsinactive4.length) {
          let langN = 3.0;
          let gestureH = 4.0;
          let agreemento: Array<any> = [491, 692];
         statisticsinactive4 = [((suggestiond ? 4 : 5) % (Math.max(parseInt(`${langN}`), 3)))];
         langN *= 1 + parseInt(`${gestureH}`);
         gestureH -= parseInt(`${gestureH}`) * 2;
         agreemento = [parseInt(`${gestureH}`)];
         break;
      }
      for (let r = 0; r < 2; r++) {
         suggestiond = statisticsinactive4.length <= 71 && !suggestiond;
      }
         statisticsinactive4.push(2);
          let photoT = String.fromCharCode(100,117,109,112,101,114,95,122,95,57,55,0);
          let themer = 5.0;
          let text3 = 0.0;
         suggestiond = !suggestiond || 72.9 <= themer;
         photoT = `${parseInt(`${text3}`) ^ 2}`;
         themer += photoT.length ^ 1;
         text3 *= parseFloat(`${parseInt(`${text3}`) >> (Math.min(Math.abs(1), 5))}`);
       let libimagepipelineU = String.fromCharCode(118,95,49,57,95,108,101,110,118,108,99,0);
          let buffer1 = 4.0;
         suggestiond = libimagepipelineU == String.fromCharCode(52,0);
         buffer1 -= parseFloat(`${parseInt(`${buffer1}`) + 1}`);
      libturbomodulejsijnio /= Math.max(2, bodanG.length);
      break;
   }
   for (let e = 0; e < 3; e++) {
      bodanG = `${2 * umengC.size}`;
   }
      yellowanimationlive2 = [parseInt(`${libturbomodulejsijnio}`) & 2];
   let ping2 = yellowanimationlive2.length <= 9657038;
   do {
      yellowanimationlive2 = [yellowanimationlive2.length + 1];
      if (ping2) {
         break;
      }
   } while ((yellowanimationlive2.length > 3) && ping2);
      tempnodatagif_ = "1";
      watchnowbgA *= historyl.length << (Math.min(Math.abs(3), 3));
   if ((yellowanimationlive2.length - parseInt(`${canvasI}`)) >= 1 || (parseInt(`${canvasI}`) - yellowanimationlive2.length) >= 1) {
      canvasI += parseFloat(`${bodanG.length + umengC.size}`);
   }
   if (historyl.includes(tempnodatagif_)) {
       let cornerkickw = 2.0;
       let umengE = 2.0;
       let favoriteE = 1.0;
       let codeH = 0.0;
       let backiconT = String.fromCharCode(103,101,110,99,102,103,115,95,113,95,49,48,48,0);
         umengE *= parseInt(`${codeH}`);
         umengE /= Math.max(2, parseInt(`${codeH}`) / 3);
         codeH /= Math.max(4, parseInt(`${umengE}`));
      for (let k = 0; k < 2; k++) {
          let manifestR: Array<any> = [842, 158, 290];
          let iconnewssharek: Array<any> = [315, 162];
         backiconT = `${iconnewssharek.length % (Math.max(2, 8))}`;
         manifestR = [manifestR.length << (Math.min(5, manifestR.length))];
         iconnewssharek = [manifestR.length >> (Math.min(Math.abs(3), 5))];
      }
          let componentregistryJ = true;
         umengE /= Math.max(1, backiconT.length);
         umengE /= Math.max(3, parseInt(`${codeH}`));
      while (favoriteE <= cornerkickw) {
          let abidetect2 = 2;
         favoriteE -= 1;
         abidetect2 += abidetect2;
         break;
      }
         backiconT += `${parseInt(`${codeH}`) % 1}`;
      while (4 <= (5 >> (Math.min(3, backiconT.length)))) {
          let rankm = 3;
          let hoverz = false;
          let starU: Array<any> = [167, 584, 391];
          let redgoalL = 3.0;
         backiconT += `${backiconT.length - 2}`;
         rankm &= 2;
         hoverz = parseFloat(`${rankm}`) >= redgoalL;
         starU.push(rankm & parseInt(`${redgoalL}`));
         break;
      }
      while (5.32 == (codeH * cornerkickw) && 1.10 == (5.32 * cornerkickw)) {
         codeH -= parseInt(`${codeH}`) + 2;
         break;
      }
          let yingy = 3;
          let hookf = 5.0;
         backiconT += `${parseInt(`${favoriteE}`) - 2}`;
         yingy %= Math.max(parseInt(`${hookf}`) | 1, 4);
         hookf /= Math.max(1, 3 * yingy);
         backiconT += `${parseInt(`${codeH}`)}`;
          let resendU = String.fromCharCode(97,100,109,105,110,101,100,95,55,95,55,57,0);
          let rncoreR = 4.0;
         backiconT += `${parseInt(`${codeH}`)}`;
         resendU += "2";
         rncoreR *= (String.fromCharCode(57,0) == resendU ? parseInt(`${rncoreR}`) : resendU.length);
      let middlebrightnessM = backiconT == String.fromCharCode(57,100,50,106,53,57,0);
      do {
         backiconT = `${parseInt(`${codeH}`)}`;
         if (middlebrightnessM) {
            break;
         }
      } while (((backiconT.length / 5) == 2 || (parseInt(`${umengE}`) / (Math.max(backiconT.length, 9))) == 5) && middlebrightnessM);
         cornerkickw -= 2;
      tempnodatagif_ = `${parseInt(`${libturbomodulejsijnio}`) ^ parseInt(`${canvasI}`)}`;
   }
       let member8 = false;
       let result0 = String.fromCharCode(119,114,97,112,112,105,110,103,95,121,95,55,56,0);
       let phoneW: Array<any> = [762, 264, 252];
         phoneW.push(((member8 ? 3 : 5)));
         member8 = result0.length >= 95;
          let topon4 = 4.0;
         member8 = (phoneW.length + topon4) >= 88.56;
      let tempnodatagifL = phoneW.length <= 8524603;
      do {
         phoneW.push(((member8 ? 2 : 4) ^ 2));
         if (tempnodatagifL) {
            break;
         }
      } while ((phoneW.length < 4) && tempnodatagifL);
       let encryptG = String.fromCharCode(116,95,57,51,95,115,110,97,112,104,111,116,0);
       let share3 = 2;
       let albumt = 1;
         member8 = !member8;
          let splashr = 1.0;
         phoneW = [((member8 ? 3 : 2))];
         splashr *= parseInt(`${splashr}`);
      if ((phoneW.length << (Math.min(Math.abs(5), 3))) <= 4) {
         phoneW.push(1);
      }
      libturbomodulejsijnio /= Math.max(1, bodanG.length);
      watchnowbgA ^= select0 * 1;
   for (let k = 0; k < 2; k++) {
      umengC.set(`${watchnowbgA}`, yellowanimationlive2.length - 1);
   }
   while ((select0 & 4) >= 2 && (rightB.length & 4) >= 2) {
      select0 *= historyl.length;
      break;
   }
       let eighteenC = 4.0;
      if (eighteenC >= eighteenC) {
         eighteenC += parseInt(`${eighteenC}`);
      }
      let trophyl = eighteenC <= 9146518.0;
      do {
         eighteenC /= Math.max(parseInt(`${eighteenC}`) << (Math.min(5, Math.abs(3))), 2);
         if (trophyl) {
            break;
         }
      } while (((eighteenC * 1.36) >= 1.2 || (1.36 + eighteenC) >= 2.19) && trophyl);
          let onewsg = String.fromCharCode(115,111,99,105,97,108,95,119,95,50,56,0);
          let bangQ = String.fromCharCode(119,97,116,99,104,101,114,115,95,108,95,53,50,0);
         eighteenC /= Math.max(bangQ.length & onewsg.length, 5);
      tempnodatagif_ += `${statisticsinactive8}`;

    onClose(false);
  };

  const navigator = useNavigation();
  const [refPosition, setRefPosition] = useState({
    x: 0,
    y: 0,
    width: 0,
    height: 0,
  });
  const componentRef = useRef<View>(null); 

  const getPosition = () => {
       let roundD = 4.0;
    let episodeE: Array<any> = [635, 855, 722];
    let tempnodatagif9: Array<any> = [854, 117, 903];
    let questC = String.fromCharCode(119,97,110,116,95,108,95,56,57,0);
    let headero = String.fromCharCode(110,97,118,105,103,97,116,105,111,110,98,97,114,95,109,95,54,53,0);
    let bottome = true;
    let loginsuccessi = 5;
    let leftv: Map<any, any> = new Map([[String.fromCharCode(108,101,118,97,114,105,110,116,95,122,95,56,48,0),548], [String.fromCharCode(99,97,99,104,101,102,108,117,115,104,95,113,95,53,54,0),75], [String.fromCharCode(112,108,97,99,101,115,95,50,95,55,51,0),29]]);
    let league1 = 0;
    let injuryi: Array<any> = [126, 465, 775];
    let iconclosewhitebgG = String.fromCharCode(119,101,98,95,112,95,49,48,48,0);
      roundD /= Math.max(episodeE.length, 3);
      bottome = headero.length <= 10;
       let libloggerQ = String.fromCharCode(112,100,102,95,113,95,55,48,0);
       let footballfieldA = 0;
       let mappingQ: Map<any, any> = new Map([[String.fromCharCode(102,95,54,95,112,104,114,97,115,101,0),84], [String.fromCharCode(111,98,106,99,95,108,95,49,48,0),520], [String.fromCharCode(99,111,114,114,101,99,116,95,118,95,53,54,0),407]]);
          let applications = String.fromCharCode(117,95,53,56,95,116,109,109,98,114,0);
          let play4: Array<any> = [933, 548];
          let searchbarX = 0;
         footballfieldA += applications.length >> (Math.min(Math.abs(3), 3));
         applications = `${searchbarX - 2}`;
         play4.push(play4.length | 3);
         searchbarX >>= Math.min(play4.length, 1);
          let shielddoneR: Array<any> = [976, 738];
          let gifgoalE = String.fromCharCode(104,101,97,100,114,111,111,109,95,114,95,55,48,0);
          let modulesb = 2;
         mappingQ = new Map([[`${modulesb}`, libloggerQ.length & 3]]);
         shielddoneR.push(1 | gifgoalE.length);
         gifgoalE += `${shielddoneR.length + 2}`;
         modulesb %= Math.max(gifgoalE.length, 3);
      while (1 <= (mappingQ.size ^ libloggerQ.length) && (1 ^ libloggerQ.length) <= 1) {
         mappingQ.set(`${libloggerQ}`, libloggerQ.length);
         break;
      }
         footballfieldA *= (String.fromCharCode(113,0) == libloggerQ ? footballfieldA : libloggerQ.length);
       let selectedi = true;
          let defaultprofilepic7 = String.fromCharCode(109,95,55,55,95,100,101,99,108,105,110,101,0);
         selectedi = libloggerQ.length <= 56 || 56 <= defaultprofilepic7.length;
         footballfieldA -= (2 * (selectedi ? 2 : 1));
      for (let y = 0; y < 3; y++) {
         mappingQ = new Map([[`${mappingQ.size}`, mappingQ.size / 2]]);
      }
      if (3 <= (3 << (Math.min(2, libloggerQ.length))) || (3 << (Math.min(3, libloggerQ.length))) <= 5) {
         libloggerQ = "3";
      }
      bottome = 9 > headero.length;
   let bally = loginsuccessi >= 9022072;
   do {
      loginsuccessi |= headero.length;
      if (bally) {
         break;
      }
   } while (((loginsuccessi % (Math.max(leftv.size, 2))) > 4 || 2 > (4 % (Math.max(10, loginsuccessi)))) && bally);
   if ((episodeE.length % (Math.max(4, 7))) >= 2) {
      loginsuccessi ^= (3 & (bottome ? 4 : 4));
   }
       let optionsG = 4.0;
          let libavcodecd = 3.0;
          let connectionM: Map<any, any> = new Map([[String.fromCharCode(103,101,116,95,102,95,49,53,0),791], [String.fromCharCode(109,112,101,103,118,105,100,101,111,95,52,95,55,50,0),955]]);
         optionsG += parseFloat(`${parseInt(`${libavcodecd}`) << (Math.min(Math.abs(2), 2))}`);
         libavcodecd *= connectionM.size - 1;
         connectionM.set(`${connectionM.size}`, connectionM.size * 1);
      if (3.99 >= (optionsG + optionsG)) {
         optionsG /= Math.max(2, parseFloat(`${parseInt(`${optionsG}`) - parseInt(`${optionsG}`)}`));
      }
      if (5.3 < (2 + optionsG)) {
          let f_viewP = 5.0;
         optionsG *= parseFloat(`${2}`);
         f_viewP *= parseFloat(`${1 & parseInt(`${f_viewP}`)}`);
      }
      roundD *= 2;

    if (componentRef.current != null) {

   if (4 >= (episodeE.length + headero.length) && 4 >= (headero.length + 4)) {
       let pauseD = String.fromCharCode(106,95,54,51,95,115,99,97,110,110,101,114,0);
       let confirmationp = 4.0;
       let reminderD: Map<any, any> = new Map([[String.fromCharCode(122,95,55,52,0),650], [String.fromCharCode(111,95,57,95,98,97,115,107,101,116,98,97,108,108,0),992]]);
       let crownk: Map<any, any> = new Map([[String.fromCharCode(115,101,103,109,101,110,116,97,116,105,111,110,95,121,95,55,0),406], [String.fromCharCode(103,101,116,108,97,100,100,114,115,0),952], [String.fromCharCode(99,111,112,121,114,105,103,104,116,95,105,95,57,51,0),16]]);
      if (4 == (reminderD.size % (Math.max(pauseD.length, 2)))) {
          let defaultfootballbgS = true;
          let forwardp = 3.0;
          let sendM = String.fromCharCode(108,105,98,115,119,105,102,116,111,115,95,56,95,52,51,0);
         reminderD.set(`${confirmationp}`, parseInt(`${confirmationp}`));
         defaultfootballbgS = !defaultfootballbgS || 99.39 > forwardp;
         forwardp += (parseFloat(`${(defaultfootballbgS ? 2 : 3) - parseInt(`${forwardp}`)}`));
         sendM = `${3 + parseInt(`${forwardp}`)}`;
      }
          let gesture1: Map<any, any> = new Map([[String.fromCharCode(107,95,54,49,95,99,111,115,105,0),902], [String.fromCharCode(111,95,50,50,95,116,104,114,101,97,100,105,110,103,0),379]]);
          let dplusx = 0.0;
          let catalogs = 3;
         crownk = new Map([[pauseD, (pauseD == String.fromCharCode(113,0) ? parseInt(`${confirmationp}`) : pauseD.length)]]);
         gesture1.set(`${dplusx}`, catalogs - 2);
         dplusx /= Math.max(parseFloat(`${gesture1.size / (Math.max(1, 8))}`), 3);
         catalogs -= parseInt(`${dplusx}`) - gesture1.size;
          let overlayn: Map<any, any> = new Map([[String.fromCharCode(110,95,49,57,95,117,116,103,111,105,110,103,0),273], [String.fromCharCode(98,115,119,97,112,100,115,112,95,117,95,48,0),528]]);
          let downarrown = false;
          let typesa = String.fromCharCode(100,101,109,111,100,117,108,97,116,101,95,119,95,57,50,0);
         pauseD = `${((downarrown ? 2 : 1) | overlayn.size)}`;
         overlayn = new Map([[typesa, typesa.length]]);
         downarrown = 40 == typesa.length;
          let shirt3 = String.fromCharCode(101,95,57,52,95,97,116,116,114,105,98,117,116,101,115,0);
          let dragi = String.fromCharCode(101,95,54,57,95,115,101,112,97,114,97,116,101,0);
          let register__wb = String.fromCharCode(99,108,97,115,115,101,115,95,55,95,57,0);
         crownk = new Map([[`${crownk.size}`, crownk.size << (Math.min(4, Math.abs(reminderD.size)))]]);
         shirt3 = `${(dragi == String.fromCharCode(119,0) ? dragi.length : shirt3.length)}`;
         register__wb = `${(String.fromCharCode(97,0) == shirt3 ? shirt3.length : dragi.length)}`;
         pauseD = `${(pauseD == String.fromCharCode(75,0) ? parseInt(`${confirmationp}`) : pauseD.length)}`;
      episodeE = [questC.length >> (Math.min(Math.abs(1), 5))];
   }
   if (leftv.get(`${loginsuccessi}`) != null) {
      loginsuccessi %= Math.max(questC.length, 5);
   }
       let main_rN: Array<any> = [633, 151];
       let mbbanners = 0;
      let yellowanimationlivec = mbbanners >= 8772794;
      do {
         mbbanners *= main_rN.length << (Math.min(2, Math.abs(mbbanners)));
         if (yellowanimationlivec) {
            break;
         }
      } while (yellowanimationlivec && (!main_rN.includes(mbbanners)));
          let modityJ: Array<any> = [423, 625, 308];
          let castL = 5;
         mbbanners >>= Math.min(3, Math.abs(modityJ.length ^ 3));
         modityJ.push(2);
         castL <<= Math.min(Math.abs(castL), 2);
         mbbanners += mbbanners >> (Math.min(5, Math.abs(2)));
      for (let x = 0; x < 2; x++) {
          let neont = 2.0;
         main_rN = [mbbanners / 2];
         neont += parseInt(`${neont}`);
      }
      let eactz = main_rN.length >= 6823915;
      do {
         main_rN.push(2);
         if (eactz) {
            break;
         }
      } while ((4 > (5 - mbbanners) && (5 - main_rN.length) > 3) && eactz);
      while (!main_rN.includes(mbbanners)) {
         main_rN = [mbbanners];
         break;
      }
      questC += `${(questC == String.fromCharCode(108,0) ? tempnodatagif9.length : questC.length)}`;
   while (4 == (4 & leftv.size) || (leftv.size & loginsuccessi) == 4) {
       let gestures8 = String.fromCharCode(110,95,55,51,95,98,103,109,99,0);
       let privatechatbgm = 2.0;
       let iconwatchnow3 = String.fromCharCode(99,95,52,50,95,102,111,114,103,111,116,0);
       let mooni = 2.0;
       let moviesx = String.fromCharCode(115,99,97,108,97,98,105,108,105,116,121,95,112,95,50,49,0);
      for (let z = 0; z < 2; z++) {
         mooni *= parseFloat(`${1}`);
      }
         privatechatbgm -= parseInt(`${privatechatbgm}`) % 3;
       let default_txK = String.fromCharCode(112,111,97,95,102,95,51,52,0);
       let runtimeschedulerI = String.fromCharCode(98,108,111,99,107,105,101,95,56,95,55,49,0);
          let ocopy_9xM = 1.0;
          let videocommond = String.fromCharCode(110,101,105,103,104,98,111,114,105,110,103,95,55,95,50,56,0);
          let friendsE = 4;
         gestures8 += `${runtimeschedulerI.length}`;
         ocopy_9xM += parseInt(`${ocopy_9xM}`) + 2;
         videocommond += "1";
         friendsE %= Math.max(parseInt(`${ocopy_9xM}`) % 1, 2);
       let incident1 = 0.0;
      let classesa = privatechatbgm >= 5684822.0;
      do {
         privatechatbgm *= parseInt(`${privatechatbgm}`) % (Math.max(iconwatchnow3.length, 9));
         if (classesa) {
            break;
         }
      } while ((default_txK.includes(`${privatechatbgm}`)) && classesa);
          let launcher4 = String.fromCharCode(112,114,101,115,101,114,118,101,115,95,117,95,56,57,0);
         moviesx = `${3 / (Math.max(4, parseInt(`${mooni}`)))}`;
         launcher4 = `${2 | launcher4.length}`;
       let fillh = true;
       let referrerK = false;
         runtimeschedulerI = `${2 - parseInt(`${mooni}`)}`;
      if ((gestures8.length << (Math.min(Math.abs(2), 4))) == 5 || 1 == (2 & gestures8.length)) {
          let descc = 1;
          let footballfiledlayout2 = String.fromCharCode(110,95,56,57,95,101,98,109,108,110,117,109,0);
          let brightnessX = false;
          let rewardvideo1 = true;
          let bootsplash7: Array<any> = [String.fromCharCode(112,114,111,109,111,116,101,95,112,95,49,51,0), String.fromCharCode(109,95,52,49,95,101,120,116,101,110,115,105,111,110,0)];
         incident1 += ((fillh ? 1 : 3) % (Math.max(parseInt(`${mooni}`), 10)));
         descc *= bootsplash7.length;
         footballfiledlayout2 += `${((rewardvideo1 ? 2 : 5) & descc)}`;
         brightnessX = brightnessX || bootsplash7.length > 20;
      }
      while (2.57 == (mooni + 2.94) || 4.12 == (2.94 + mooni)) {
          let bootsplashF = 0.0;
         gestures8 = `${moviesx.length}`;
         bootsplashF /= Math.max(3, 5);
         break;
      }
      while (mooni <= 1.53 && (mooni - 1.53) <= 5.58) {
         fillh = runtimeschedulerI == iconwatchnow3;
         break;
      }
      while (incident1 < 1.2 || 5.93 < (incident1 / 1.2)) {
         incident1 *= 3;
         break;
      }
         runtimeschedulerI = `${parseInt(`${incident1}`)}`;
      for (let s = 0; s < 3; s++) {
         gestures8 += `${default_txK.length / (Math.max(8, parseInt(`${mooni}`)))}`;
      }
      loginsuccessi |= 2 | league1;
      break;
   }
   if (1.4 > roundD) {
       let basketballhometeamD: Map<any, any> = new Map([[String.fromCharCode(99,95,54,52,95,99,111,110,102,105,114,109,101,100,0),String.fromCharCode(111,112,115,95,121,95,50,50,0)], [String.fromCharCode(120,95,56,54,0),String.fromCharCode(105,95,49,53,95,115,104,117,102,102,108,101,112,108,97,110,101,115,0)]]);
      if (2 > (basketballhometeamD.size << (Math.min(Math.abs(basketballhometeamD.size), 3))) || 2 > (basketballhometeamD.size << (Math.min(Math.abs(basketballhometeamD.size), 3)))) {
         basketballhometeamD = new Map([[`${basketballhometeamD.size}`, basketballhometeamD.size]]);
      }
      if ((basketballhometeamD.size / (Math.max(6, basketballhometeamD.size))) == 5 || (basketballhometeamD.size / (Math.max(7, basketballhometeamD.size))) == 5) {
         basketballhometeamD.set(`${basketballhometeamD.size}`, basketballhometeamD.size - basketballhometeamD.size);
      }
      while ((basketballhometeamD.size | basketballhometeamD.size) == 1 && 4 == (1 | basketballhometeamD.size)) {
         basketballhometeamD = new Map([[`${basketballhometeamD.size}`, 1 | basketballhometeamD.size]]);
         break;
      }
      questC = "2";
   }
   let formr = episodeE.length <= 5332833;
   do {
      episodeE.push(episodeE.length);
      if (formr) {
         break;
      }
   } while ((!episodeE.includes(roundD)) && formr);
      componentRef.current.measure((x, y, width, height, pageX, pageY) => {
        
        setRefPosition({x: pageX, y: pageY, width: width, height: height});
      });
    }
  };

  return (
    <View style={{ paddingHorizontal: 10 ,position:'relative' , bottom:255 }}     onLayout={() => getPosition()}
    ref={componentRef}>
   
      <View
        style={{
          width: width ?? "70%",
          alignSelf: "flex-end",
          justifyContent: isLeft ? "flex-end" : "flex-start",
        }}
      >
            <View>
          <View style={{ alignSelf: "center" }}>
            <Text style={[styles.confirmText]} onPress={() => closeModal()}>
              我知道了
            </Text>
          </View>
        </View>
        <View style={[styles.buyContainer]}>
          {contentTemplate == 1 && (
            <>
              <Text style={[styles.normalText]}>
                {`购买`}
                <Text style={[styles.goldText]}>VIP</Text>
                <Text
                  style={[styles.normalText]}
                >{`  去除广告\n高清视频`}</Text>
                <Text style={[styles.goldText]}>{`纯净观看`}</Text>
              </Text>
            </>
          )}

          {contentTemplate == 2 && (
            <>
              <Text style={[styles.normalText]}>
                {`购买`}
                <Text style={[styles.goldText]}>VIP</Text>

                <Text style={[styles.goldText]}>{`\n离线下载`}</Text>
                <Text style={[styles.normalText]}>{`高清视频`}</Text>
              </Text>
            </>
          )}

          {contentTemplate == 3 && (
            <>
              <Text style={[styles.normalText]}>
                {`购买`}
                <Text style={[styles.goldText]}>VIP</Text>
                <Text
                  style={[styles.normalText]}
                >{`免费看NBA/五大联赛\n`}</Text>
                <Text style={[styles.normalText]}>{`实时`}</Text>
                <Text style={[styles.goldText]}>{`高清直播`}</Text>
              </Text>
            </>
          )}

          <View
            style={{ paddingHorizontal: 10, width: "100%", paddingTop: 10 }}
          >
            <TouchableOpacity
              onPress={() => {
                navigator.navigate("付费VIP");
                closeModal();
              }}
            >
              <LinearGradient
                colors={["#D1AC7D", "#B1885F"]}
                locations={[0.0, 0.99]}
                style={{
                  height: 40,
                  justifyContent: "center",
                  alignItems: "center",
                  paddingVertical: 8,
                  borderRadius: 8,
                }}
              >
                <Text
                  style={{
                    fontWeight: "900",
                    letterSpacing: 1,
                    color: "black",
                  }}
                >
                  立即购买
                </Text>
              </LinearGradient>
            </TouchableOpacity>
          </View>
        </View>
        <View style={{ height: 0 }} />
    
      </View>

      {isLeft ? (
        <View style={{ alignSelf: "flex-start", paddingLeft: 30 }}>
          <Image source={require(`@static/images/vip_guide_arrow1.png`)}></Image>
        </View>
      ) : (
        <View style={{ alignSelf: "flex-end", paddingRight: 30 }}>
          <Image source={require(`@static/images/vip_guide_arrow1.png`)}></Image>
        </View>
      )}
    </View>
  );
}

const styles = StyleSheet.create({
  buyContainer: {
    borderRadius: 10,
    borderColor: "white",
    borderWidth: 1,
    marginHorizontal: 10,
    paddingVertical: 12,
  },
  normalText: {
    fontSize: 18,
    color: "white",
    textAlign: "center",
  },
  goldText: {
    fontSize: 18,
    color: "#BB9266",
    textAlign: "center",
  },
  confirmText: {
    borderRadius: 15,
    borderWidth: 1,
    borderColor: "#9C9C9C",
    color: "#9C9C9C",
    fontSize: 12,
    paddingHorizontal: 10,
    paddingVertical: 5,
    textAlign: "center",
    position:'relative',
    bottom:8
  },
});
