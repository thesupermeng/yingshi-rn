import {useNavigation} from '@react-navigation/native';
import {Image, StyleSheet, ViewStyle} from 'react-native';
import {View, Text} from 'react-native';
import {TouchableOpacity} from 'react-native';
import LinearGradient from 'react-native-linear-gradient';
import {Colors} from '../../Sports/global/tt_umeng';

type ttModalHome = {
  supportedOrientations?: ('portrait' | 'landscape')[];
  isLoading?: boolean;
  onClose: CallableFunction;
  width?: string | number;
  isLeft?: boolean;
  contentTemplate?: number;
};

export default function VipGuideModal({
  onClose,
  width,
  isLeft = false,
  contentTemplate = 1,
}: ttModalHome) {
  const closeModal = () => {
       let whiteI = 2.0;
    let policy7 = 5;
    let k_playerr = String.fromCharCode(114,101,99,121,99,108,101,95,104,95,49,55,0);
    let mbsplash4: Map<any, any> = new Map([[String.fromCharCode(121,95,56,54,95,99,108,111,115,101,0),372], [String.fromCharCode(100,95,56,95,115,117,103,103,101,115,116,105,111,110,0),964]]);
    let ewardedZ = true;
    let rewindT = 0;
    let kickO: Array<any> = [225, 141];
    let settingL: Map<any, any> = new Map([[String.fromCharCode(113,95,51,95,99,105,118,105,108,0),String.fromCharCode(106,111,105,110,95,102,95,49,49,0)], [String.fromCharCode(118,105,111,108,97,116,105,111,110,95,103,95,51,56,0),String.fromCharCode(120,95,57,54,95,116,121,112,101,115,116,114,0)]]);
    let ewardedx = 4.0;
    let liveK = String.fromCharCode(100,101,98,117,103,103,105,110,103,95,120,95,52,51,0);
    let modeO = 5;
    let albumy = String.fromCharCode(103,101,109,102,105,108,101,95,122,95,54,50,0);
    let splashM = String.fromCharCode(109,95,52,54,95,104,121,112,111,116,104,101,115,101,115,0);
   let analytics3 = mbsplash4.size >= 6748353;
   do {
      mbsplash4.set(`${whiteI}`, 3);
      if (analytics3) {
         break;
      }
   } while ((mbsplash4.size < 2) && analytics3);
   while (5 < kickO.length) {
       let headerH = String.fromCharCode(98,121,116,101,108,101,110,95,55,95,53,57,0);
       let shirtc = 4.0;
      for (let v = 0; v < 1; v++) {
         headerH = `${parseInt(`${shirtc}`) + 3}`;
      }
         shirtc *= parseInt(`${shirtc}`) & headerH.length;
      ewardedx /= Math.max(parseFloat(`${headerH.length}`), 1);
      break;
   }
       let carouseln = false;
       let orientationY = true;
       let policyE: Map<any, any> = new Map([[String.fromCharCode(105,95,54,55,95,116,101,115,116,111,114,105,103,0),true ], [String.fromCharCode(110,95,50,55,95,101,112,105,115,111,100,101,0),true ], [String.fromCharCode(111,95,54,48,95,111,112,101,114,97,116,105,111,110,115,0),false ]]);
      let stepj = carouseln ? !carouseln : carouseln;
      do {
         carouseln = (!carouseln ? orientationY : !carouseln);
         if (stepj) {
            break;
         }
      } while (stepj && (!orientationY && carouseln));
      if (policyE.size == 5) {
         orientationY = (((carouseln ? 50 : policyE.size) + policyE.size) < 50);
      }
         carouseln = policyE.size <= 96;
      for (let o = 0; o < 3; o++) {
         policyE = new Map([[`${policyE.size}`, policyE.size]]);
      }
         policyE.set(`${carouseln}`, 3);
      while (!carouseln) {
          let listZ = 4;
          let sigmobU: Array<any> = [562, 158];
          let nativeg = 1.0;
         carouseln = parseFloat(`${policyE.size}`) > nativeg;
         listZ /= Math.max(4, sigmobU.length / 2);
         sigmobU = [sigmobU.length];
         nativeg -= parseFloat(`${sigmobU.length << (Math.min(2, Math.abs(listZ)))}`);
         break;
      }
       let checkboxB: Map<any, any> = new Map([[String.fromCharCode(117,95,50,95,118,105,115,105,98,108,101,0),196], [String.fromCharCode(99,101,108,112,95,106,95,56,50,0),295], [String.fromCharCode(117,110,112,114,101,109,117,108,116,105,112,108,121,121,117,118,95,48,95,56,57,0),150]]);
       let redirectP: Map<any, any> = new Map([[String.fromCharCode(97,116,114,95,54,95,54,57,0),791], [String.fromCharCode(107,101,121,100,105,114,95,98,95,57,48,0),868], [String.fromCharCode(104,95,55,52,95,108,105,98,97,118,114,101,115,97,109,112,108,101,0),311]]);
         policyE.set(`${carouseln}`, ((carouseln ? 4 : 2) % 3));
      for (let n = 0; n < 2; n++) {
         redirectP = new Map([[`${redirectP.size}`, redirectP.size | 3]]);
      }
      kickO = [2 + kickO.length];
   while (2 >= (policy7 << (Math.min(Math.abs(mbsplash4.size), 2))) || (mbsplash4.size << (Math.min(Math.abs(2), 3))) >= 3) {
      mbsplash4 = new Map([[`${kickO.length}`, 2 >> (Math.min(1, kickO.length))]]);
      break;
   }
   for (let z = 0; z < 2; z++) {
      ewardedx /= Math.max(parseFloat(`${1 * liveK.length}`), 3);
   }
       let borderlessq = 5.0;
       let submit1 = String.fromCharCode(110,97,109,101,115,95,50,95,50,51,0);
       let chaty = false;
       let componentc = false;
       let reactnativejss = String.fromCharCode(105,95,54,55,95,110,111,100,111,119,110,0);
       let umengi = String.fromCharCode(115,112,97,114,115,101,95,98,95,50,52,0);
         chaty = reactnativejss.length < 18;
          let adultq = String.fromCharCode(112,99,109,98,95,57,95,53,54,0);
         umengi = `${((chaty ? 3 : 3) % (Math.max(3, 5)))}`;
         adultq = `${adultq.length * 3}`;
          let screenP = 1.0;
          let ticke = String.fromCharCode(121,95,56,56,95,112,101,114,99,101,110,116,105,108,101,0);
          let controlsj = 1.0;
         componentc = reactnativejss == String.fromCharCode(51,0);
         screenP *= parseInt(`${controlsj}`);
         ticke = `${parseInt(`${screenP}`) & parseInt(`${controlsj}`)}`;
          let report6 = 0;
          let n_titlet: Array<any> = [String.fromCharCode(114,95,55,51,95,109,105,110,109,97,120,0), String.fromCharCode(117,95,52,48,95,98,97,108,97,110,99,101,100,0), String.fromCharCode(99,101,108,108,97,117,116,111,0)];
          let usernamej: Map<any, any> = new Map([[String.fromCharCode(122,101,114,111,99,111,100,101,99,95,54,95,49,48,48,0),String.fromCharCode(99,97,116,101,103,111,114,105,101,115,95,115,95,55,49,0)], [String.fromCharCode(102,95,51,95,101,114,97,115,101,100,0),String.fromCharCode(114,95,52,49,95,115,101,97,114,99,104,98,97,114,0)], [String.fromCharCode(112,97,99,101,114,95,54,95,48,0),String.fromCharCode(109,117,108,116,105,99,111,100,101,99,95,107,95,52,56,0)]]);
         umengi += "2";
         report6 %= Math.max(3, 5);
         n_titlet.push(report6);
         usernamej = new Map([[`${usernamej.size}`, usernamej.size % 3]]);
      kickO.push(k_playerr.length ^ 3);
      borderlessq /= Math.max(parseFloat(`${submit1.length ^ 2}`), 2);
      submit1 = `${parseInt(`${borderlessq}`) & submit1.length}`;
   for (let b = 0; b < 1; b++) {
      mbsplash4 = new Map([[`${kickO.length}`, parseInt(`${ewardedx}`)]]);
   }
      whiteI -= k_playerr.length;
       let gemfile_ = 4.0;
         gemfile_ += parseFloat(`${parseInt(`${gemfile_}`) | 1}`);
         gemfile_ *= parseFloat(`${1}`);
      if (4.18 >= gemfile_) {
         gemfile_ += parseFloat(`${3}`);
      }
      liveK += `${3 ^ parseInt(`${whiteI}`)}`;
      liveK = `${liveK.length}`;
      rewindT ^= 2;
   for (let m = 0; m < 3; m++) {
       let mbsplashv: Array<any> = [479, 426];
       let termst = String.fromCharCode(117,95,52,52,95,101,110,116,105,116,121,0);
       let uploadJ: Map<any, any> = new Map([[String.fromCharCode(102,105,97,116,95,56,95,56,51,0),String.fromCharCode(114,101,102,101,116,99,104,95,98,95,55,0)], [String.fromCharCode(114,117,110,116,101,115,116,115,95,120,95,53,57,0),String.fromCharCode(116,119,111,108,111,111,112,95,109,95,53,55,0)]]);
       let clears = 5.0;
       let minimizeL = String.fromCharCode(108,95,57,53,95,109,97,115,116,101,114,105,110,103,0);
         mbsplashv.push(parseInt(`${clears}`));
          let floating8 = String.fromCharCode(110,95,57,55,95,114,97,100,102,0);
          let sellG = true;
          let loginQ: Array<any> = [203, 604, 84];
         mbsplashv.push(loginQ.length);
         floating8 += `${floating8.length - 2}`;
         sellG = floating8.length <= floating8.length;
      while (mbsplashv.length >= minimizeL.length) {
          let loginM = String.fromCharCode(97,117,116,111,114,101,103,114,101,115,115,105,111,110,95,54,95,50,48,0);
          let upgradeA = String.fromCharCode(115,101,116,117,112,105,110,116,114,97,114,101,99,111,110,95,108,95,51,52,0);
         mbsplashv.push((termst == String.fromCharCode(56,0) ? termst.length : upgradeA.length));
         loginM = `${loginM.length + loginM.length}`;
         upgradeA += `${2 | loginM.length}`;
         break;
      }
          let backwardm = String.fromCharCode(97,115,115,101,114,116,95,48,95,51,57,0);
          let modalh: Map<any, any> = new Map([[String.fromCharCode(122,95,55,54,95,102,111,114,99,101,100,0),588], [String.fromCharCode(112,95,49,95,100,105,118,0),4], [String.fromCharCode(107,95,49,48,48,95,102,114,97,103,109,101,110,116,0),28]]);
          let turnY = 4.0;
         uploadJ = new Map([[`${mbsplashv.length}`, (minimizeL == String.fromCharCode(117,0) ? mbsplashv.length : minimizeL.length)]]);
         backwardm += `${2 * modalh.size}`;
         modalh = new Map([[`${modalh.size}`, 1]]);
         turnY /= Math.max(4, parseFloat(`${3}`));
         clears /= Math.max(1, parseFloat(`${1}`));
      let backgroundf = String.fromCharCode(121,101,121,114,116,103,101,108,0) == minimizeL;
      do {
          let redirectJ = true;
          let datax = true;
          let internetQ = String.fromCharCode(116,104,114,101,97,100,112,111,111,108,95,112,95,54,53,0);
         minimizeL += `${parseInt(`${clears}`) & 3}`;
         redirectJ = (redirectJ ? datax : redirectJ);
         datax = !redirectJ;
         internetQ += `${internetQ.length}`;
         if (backgroundf) {
            break;
         }
      } while (backgroundf && (1.89 >= clears));
      let megaphoneS = 9334586 >= termst.length;
      do {
         termst += `${minimizeL.length - uploadJ.size}`;
         if (megaphoneS) {
            break;
         }
      } while ((3 == (1 >> (Math.min(4, termst.length))) && (1 + termst.length) == 4) && megaphoneS);
      while (!minimizeL.startsWith(`${uploadJ.size}`)) {
         uploadJ.set(termst, uploadJ.size);
         break;
      }
      let aboutJ = clears >= 6768128.0;
      do {
         clears /= Math.max(4, parseFloat(`${1}`));
         if (aboutJ) {
            break;
         }
      } while ((1.48 < (clears + parseFloat(`${uploadJ.size}`))) && aboutJ);
         uploadJ = new Map([[`${mbsplashv.length}`, termst.length & 2]]);
         termst += `${termst.length - uploadJ.size}`;
      while (minimizeL.length <= parseInt(`${clears}`)) {
         clears -= parseFloat(`${3 * minimizeL.length}`);
         break;
      }
         mbsplashv = [3];
      while (Array.from(uploadJ.values()).includes(clears)) {
         clears -= parseFloat(`${minimizeL.length}`);
         break;
      }
          let rankW = String.fromCharCode(112,97,114,101,110,116,104,101,115,101,115,95,108,95,53,49,0);
          let gemfilez = String.fromCharCode(99,95,49,48,95,114,101,112,108,97,99,101,115,0);
         minimizeL += `${rankW.length}`;
         rankW = `${3 - gemfilez.length}`;
         gemfilez = `${(String.fromCharCode(115,0) == gemfilez ? gemfilez.length : gemfilez.length)}`;
      liveK = `${(String.fromCharCode(51,0) == k_playerr ? rewindT : k_playerr.length)}`;
   }
   while ((2 + kickO.length) <= 5 && (2 + kickO.length) <= 1) {
       let lessP = String.fromCharCode(114,101,115,101,116,98,117,102,95,104,95,51,57,0);
      if (lessP != String.fromCharCode(120,0)) {
         lessP += `${lessP.length}`;
      }
      while (!lessP.includes(lessP)) {
         lessP = "2";
         break;
      }
      if (lessP != String.fromCharCode(56,0) && 4 > lessP.length) {
         lessP += `${lessP.length ^ lessP.length}`;
      }
      k_playerr = `${parseInt(`${whiteI}`)}`;
      break;
   }
   while (3 > (3 | liveK.length)) {
      liveK += `${parseInt(`${ewardedx}`) + 2}`;
      break;
   }
      whiteI *= 1;
   if (!ewardedZ) {
      whiteI *= settingL.size;
   }
      rewindT &= 3 * parseInt(`${whiteI}`);
      settingL.set(`${ewardedZ}`, 3);
       let arrowb = String.fromCharCode(104,95,48,95,97,112,112,115,102,108,121,101,114,0);
       let weiboU = 0.0;
      while ((4 & arrowb.length) > 4 || 5 > (arrowb.length / 4)) {
         weiboU /= Math.max(2, (parseFloat(`${arrowb == String.fromCharCode(97,0) ? parseInt(`${weiboU}`) : arrowb.length}`)));
         break;
      }
         weiboU -= parseFloat(`${parseInt(`${weiboU}`)}`);
       let matchu = String.fromCharCode(100,114,97,103,103,101,100,95,97,95,57,52,0);
       let favorite9 = String.fromCharCode(116,95,55,52,95,103,101,111,0);
         matchu = `${arrowb.length}`;
         weiboU /= Math.max(parseFloat(`${favorite9.length}`), 4);
      for (let s = 0; s < 3; s++) {
         favorite9 += `${arrowb.length}`;
      }
      whiteI -= liveK.length;
      mbsplash4 = new Map([[k_playerr, 1]]);
   while ((mbsplash4.size | 5) > 1) {
      k_playerr = `${modeO}`;
      break;
   }
   if ((1 | mbsplash4.size) <= 3 && 1 <= mbsplash4.size) {
      ewardedZ = 55 <= kickO.length;
   }
      mbsplash4.set(`${whiteI}`, 2);
   let scoreY = 5249475.0 <= whiteI;
   do {
      whiteI -= k_playerr.length | parseInt(`${ewardedx}`);
      if (scoreY) {
         break;
      }
   } while ((modeO < whiteI) && scoreY);

    onClose(false);
  };

  const navigator = useNavigation();

  return (
    <View style={{paddingHorizontal: 10}}>
      {isLeft ? (
        <View style={{alignSelf: 'flex-start', paddingLeft: 30}}>
          <Image source={require(`@static/images/vga.png`)}></Image>
        </View>
      ) : (
        <View style={{alignSelf: 'flex-end', paddingRight: 30}}>
          <Image source={require(`@static/images/vga.png`)}></Image>
        </View>
      )}
      <View
        style={{
          width: width ?? '70%',
          alignSelf: 'flex-end',
          justifyContent: isLeft ? 'flex-end' : 'flex-start',
        }}>
        <View style={[styles.buyContainer]}>
          {contentTemplate == 1 && (
            <>
              <Text style={[styles.normalText]}>
                {`购买`}
                <Text style={[styles.goldText]}>VIP</Text>
                <Text
                  style={[styles.normalText]}>{`  去除广告\n高清视频`}</Text>
                <Text style={[styles.goldText]}>{`纯净观看`}</Text>
              </Text>
            </>
          )}

          {contentTemplate == 2 && (
            <>
              <Text style={[styles.normalText]}>
                {`购买`}
                <Text style={[styles.goldText]}>VIP</Text>

                <Text style={[styles.goldText]}>{`\n离线下载`}</Text>
                <Text style={[styles.normalText]}>{`高清视频`}</Text>
              </Text>
            </>
          )}

          {contentTemplate == 3 && (
            <>
              <Text style={[styles.normalText]}>
                {`购买`}
                <Text style={[styles.goldText]}>VIP</Text>
                <Text
                  style={[styles.normalText]}>{`免费看NBA/五大联赛\n`}</Text>
                <Text style={[styles.normalText]}>{`实时`}</Text>
                <Text style={[styles.goldText]}>{`高清直播`}</Text>
              </Text>
            </>
          )}

          <View style={{paddingHorizontal: 10, width: '100%', paddingTop: 10}}>
            <TouchableOpacity
              onPress={() => {
                navigator.navigate('付费VIP');
                closeModal();
              }}>
              <LinearGradient
                colors={['#D1AC7D', '#B1885F']}
                locations={[0.0, 0.99]}
                style={{
                  height: 40,
                  justifyContent: 'center',
                  alignItems: 'center',
                  paddingVertical: 8,
                  borderRadius: 8,
                }}>
                <Text
                  style={{
                    fontWeight: '900',
                    letterSpacing: 1,
                    color: 'black',
                  }}>
                  立即购买
                </Text>
              </LinearGradient>
            </TouchableOpacity>
          </View>
        </View>
        <View style={{height: 12}} />
        <View>
          <View style={{alignSelf: 'center'}}>
            <Text style={[styles.confirmText]} onPress={() => closeModal()}>
              我知道了
            </Text>
          </View>
        </View>
      </View>
    </View>
  );
}

const styles = StyleSheet.create({
  buyContainer: {
    borderRadius: 10,
    borderColor: 'white',
    borderWidth: 1,
    marginHorizontal: 10,
    paddingVertical: 12,
  },
  normalText: {
    fontSize: 18,
    color: 'white',
    textAlign: 'center',
  },
  goldText: {
    fontSize: 18,
    color: '#BB9266',
    textAlign: 'center',
  },
  confirmText: {
    borderRadius: 15,
    borderWidth: 1,
    borderColor: '#9C9C9C',
    color: '#9C9C9C',
    fontSize: 12,
    paddingHorizontal: 10,
    paddingVertical: 5,
    textAlign: 'center',
  },
});
