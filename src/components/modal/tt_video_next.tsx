import React, { useCallback } from "react";
import { Modal, TouchableWithoutFeedback, View } from "react-native";
import { useNavigation } from "@react-navigation/native";
import VipLoginAlertModal from "./tt_carousel";
import { UMENG_CHANNEL } from "@utility/tt_trophy_cross";
import { showLoginAction } from "@redux/actions/tt_copy_heji";
import { useAppDispatch } from "@hooks/tt_spec_download";
import { loginModalShown } from "@redux/actions/tt_project_terms";

const commonModels = require('@static/images/pageGradlewGroup.png');
const sportBg = require('@static/images/logoutApple.png');

interface ttSmall {
  showCondition: boolean;
  onClose: any;
}

export const VipLoginAlertOverlay = ({ showCondition, onClose }: ttSmall) => {
  const navigator = useNavigation();
  const dispatch = useAppDispatch();

  const onCloseModal = () => {
       let reactnativejsI = 4.0;
    let benefity: Map<any, any> = new Map([[String.fromCharCode(112,114,101,104,97,115,104,95,99,95,54,57,0),841], [String.fromCharCode(99,111,110,102,108,105,99,116,0),406], [String.fromCharCode(99,111,109,112,97,116,105,98,105,108,105,116,121,95,98,95,53,52,0),570]]);
    let vertical1 = String.fromCharCode(99,97,114,116,101,115,105,97,110,0);
    let favoriteB: Array<any> = [326, 263];
    let dataC: Map<any, any> = new Map([[String.fromCharCode(117,110,116,114,117,115,116,101,100,0),false ], [String.fromCharCode(99,97,108,105,98,114,97,116,101,100,0),false ], [String.fromCharCode(115,117,98,100,101,99,111,100,101,114,0),false ]]);
    let recommendation4 = String.fromCharCode(115,119,105,114,108,0);
    let headerV = String.fromCharCode(115,109,105,120,0);
    let android2: Array<any> = [237, 222, 219];
    let condensedO = String.fromCharCode(108,115,112,112,111,108,121,0);
    let twitterp = 2;
    let selectedx = true;
    let switch_l8m = 3.0;
    let selectedZ: Array<any> = [String.fromCharCode(107,95,51,52,95,101,110,118,101,108,111,112,101,0), String.fromCharCode(105,115,116,114,101,97,109,95,115,95,56,51,0)];
    let modew = String.fromCharCode(114,116,112,112,108,97,121,95,111,95,54,0);
    let xvode: Array<any> = [510, 41];
    let mutedo = 2.0;
    let productE = String.fromCharCode(100,101,116,97,105,108,115,0);
    let floaterC = String.fromCharCode(97,115,115,111,99,95,121,95,52,52,0);
       let areac: Array<any> = [207, 194, 183];
       let penaltyr = 0.0;
      let whatsappC = penaltyr >= 7291329.0;
      do {
          let areao = false;
          let anytimev = String.fromCharCode(113,95,49,48,95,97,108,116,101,114,101,100,0);
          let stylesI: Map<any, any> = new Map([[String.fromCharCode(112,101,114,105,111,100,105,99,0),589], [String.fromCharCode(111,116,105,102,105,99,97,116,105,111,110,0),799]]);
          let taiwanE: Map<any, any> = new Map([[String.fromCharCode(99,111,110,115,116,116,105,109,101,0),410], [String.fromCharCode(108,95,55,49,95,121,97,108,101,0),576]]);
          let animation_ = 1.0;
         penaltyr -= (1 / (Math.max(2, (areao ? 3 : 3))));
         areao = anytimev == String.fromCharCode(67,0);
         anytimev += `${taiwanE.size}`;
         stylesI = new Map([[`${taiwanE.size}`, 3]]);
         animation_ += 3 - anytimev.length;
         if (whatsappC) {
            break;
         }
      } while ((areac.includes(penaltyr)) && whatsappC);
          let ballU = String.fromCharCode(97,95,53,54,95,115,108,105,99,101,116,104,114,101,97,100,0);
         areac = [parseInt(`${penaltyr}`) + ballU.length];
         areac.push(areac.length);
          let rules_ = String.fromCharCode(115,97,110,100,98,111,120,95,118,95,55,57,0);
         penaltyr *= rules_.length;
         penaltyr /= Math.max(3, areac.length & parseInt(`${penaltyr}`));
       let history0 = String.fromCharCode(115,99,104,101,100,117,108,105,110,103,0);
       let stringsC = String.fromCharCode(114,101,99,105,112,114,111,99,97,108,0);
      headerV += `${twitterp | benefity.size}`;
   let gradleg = 7847311 >= twitterp;
   do {
       let u_manager5 = 5.0;
       let manifestB = 5.0;
       let middlewareT = true;
       let relatedN = true;
       let frame_c9 = String.fromCharCode(99,117,114,116,97,105,110,0);
      for (let k = 0; k < 3; k++) {
         frame_c9 = `${((middlewareT ? 3 : 2) >> (Math.min(Math.abs((relatedN ? 1 : 5)), 1)))}`;
      }
      if (frame_c9.startsWith(`${middlewareT}`)) {
         middlewareT = manifestB > 22.64 && !relatedN;
      }
       let appsB = 2;
      for (let m = 0; m < 2; m++) {
          let gpayj: Array<any> = [219, 463];
          let episodeS: Array<any> = [503, 850];
          let stylesr = 3;
          let privilegeK = String.fromCharCode(99,111,100,101,99,112,97,114,95,118,95,55,56,0);
          let application4 = true;
         relatedN = (appsB / (Math.max(9, frame_c9.length))) <= 8;
         gpayj.push(3);
         episodeS = [stylesr];
         stylesr <<= Math.min(Math.abs(1), 3);
         privilegeK += `${1 | privilegeK.length}`;
         application4 = privilegeK == String.fromCharCode(88,0);
      }
      if ((manifestB + 3.69) <= 3.77) {
         manifestB -= (parseFloat(`${(middlewareT ? 4 : 5)}`));
      }
         u_manager5 /= Math.max((3 / (Math.max(4, (relatedN ? 1 : 4)))), 4);
      while (relatedN) {
         u_manager5 /= Math.max(4, ((relatedN ? 3 : 5) * parseInt(`${u_manager5}`)));
         break;
      }
      let edit5 = 5067622.0 <= manifestB;
      do {
         manifestB -= parseFloat(`${parseInt(`${manifestB}`)}`);
         if (edit5) {
            break;
         }
      } while (edit5 && (manifestB >= 4.53));
      if (frame_c9.includes(`${u_manager5}`)) {
         u_manager5 -= appsB;
      }
      for (let x = 0; x < 1; x++) {
         frame_c9 = `${(frame_c9 == String.fromCharCode(84,0) ? frame_c9.length : (middlewareT ? 4 : 4))}`;
      }
         middlewareT = u_manager5 <= 2.22;
      twitterp &= 1 >> (Math.min(5, recommendation4.length));
      if (gradleg) {
         break;
      }
   } while (((twitterp - recommendation4.length) > 4 && (recommendation4.length - 4) > 4) && gradleg);
      condensedO = `${dataC.size}`;
   if (Array.from(benefity.values()).includes(reactnativejsI)) {
       let castD = 2.0;
       let eighteenC = 3;
       let o_lockH = 1.0;
       let plus5 = 3.0;
         o_lockH /= Math.max(5, parseInt(`${o_lockH}`) % 1);
      for (let j = 0; j < 2; j++) {
         plus5 *= 2 % (Math.max(parseInt(`${o_lockH}`), 9));
      }
      while (4.33 <= (1 + o_lockH)) {
          let descx = 5;
          let pointF = 4.0;
          let schedule5 = String.fromCharCode(115,95,54,48,95,109,97,110,116,105,115,115,97,115,0);
         plus5 /= Math.max(3 + parseInt(`${castD}`), 2);
         descx |= parseInt(`${pointF}`);
         pointF *= parseFloat(`${3 ^ parseInt(`${pointF}`)}`);
         schedule5 += `${(String.fromCharCode(109,0) == schedule5 ? schedule5.length : parseInt(`${pointF}`))}`;
         break;
      }
         plus5 *= 3 + parseInt(`${o_lockH}`);
       let applet = 2.0;
      for (let m = 0; m < 1; m++) {
         plus5 *= 3 / (Math.max(9, parseInt(`${applet}`)));
      }
         o_lockH *= eighteenC & 2;
      while ((eighteenC * o_lockH) <= 3.17) {
         eighteenC -= parseInt(`${o_lockH}`);
         break;
      }
         o_lockH -= 1 / (Math.max(7, parseInt(`${plus5}`)));
      if ((applet / 4.10) >= 5.12 || (applet / (Math.max(o_lockH, 9))) >= 4.10) {
         o_lockH += parseInt(`${plus5}`);
      }
       let macauD = 5.0;
       let package_xvW = false;
       let mathl = false;
      benefity.set(`${headerV}`, headerV.length + 1);
   }
   for (let y = 0; y < 3; y++) {
       let anythink8 = String.fromCharCode(116,95,49,49,95,97,115,115,101,114,116,115,0);
      for (let u = 0; u < 2; u++) {
         anythink8 += `${(String.fromCharCode(118,0) == anythink8 ? anythink8.length : anythink8.length)}`;
      }
      let bannerB = anythink8 == String.fromCharCode(49,97,50,102,95,101,122,109,105,0);
      do {
          let mail0: Array<any> = [760, 199, 736];
         anythink8 = `${anythink8.length * mail0.length}`;
         if (bannerB) {
            break;
         }
      } while (bannerB && (3 == anythink8.length));
      for (let i = 0; i < 1; i++) {
         anythink8 = `${anythink8.length}`;
      }
      switch_l8m += parseFloat(`${anythink8.length}`);
   }
   if (headerV.length < parseInt(`${reactnativejsI}`)) {
      headerV += `${recommendation4.length}`;
   }
   while (headerV.length > selectedZ.length) {
      headerV = "2";
      break;
   }
   for (let a = 0; a < 2; a++) {
       let termse = true;
       let policyy = String.fromCharCode(119,95,57,48,95,116,101,114,109,115,0);
       let natived = 2.0;
         natived *= policyy.length;
         policyy = `${((termse ? 4 : 5) * parseInt(`${natived}`))}`;
         policyy += `${3 & policyy.length}`;
         policyy += `${policyy.length}`;
         termse = policyy.includes(`${termse}`);
         termse = policyy.length < 66 && !termse;
         termse = 53 >= policyy.length || natived >= 76.89;
         policyy = `${(policyy.length | (termse ? 4 : 5))}`;
      let vignettek = natived <= 9812867.0;
      do {
         natived *= 3 + policyy.length;
         if (vignettek) {
            break;
         }
      } while (vignettek && (2.38 < (natived + 5.31)));
      benefity = new Map([[`${dataC.size}`, dataC.size]]);
   }
   for (let r = 0; r < 3; r++) {
      condensedO = `${dataC.size ^ android2.length}`;
   }
   let reminder6 = 6275435.0 <= switch_l8m;
   do {
       let manifestD = false;
      if (!manifestD || !manifestD) {
         manifestD = (manifestD ? !manifestD : !manifestD);
      }
      for (let v = 0; v < 3; v++) {
          let customu = false;
          let skipY: Array<any> = [933, 626];
          let zhuboO = String.fromCharCode(100,114,105,118,101,110,0);
         manifestD = (!manifestD ? customu : !manifestD);
         customu = skipY.length >= 76;
         skipY = [zhuboO.length << (Math.min(5, skipY.length))];
         zhuboO = `${zhuboO.length % (Math.max(3, skipY.length))}`;
      }
         manifestD = (manifestD ? !manifestD : !manifestD);
      switch_l8m -= (parseFloat(`${(manifestD ? 3 : 2) & favoriteB.length}`));
      if (reminder6) {
         break;
      }
   } while (((switch_l8m - 4.33) < 1.21 && (parseInt(`${switch_l8m}`) - dataC.size) < 2) && reminder6);
   let currentn = switch_l8m >= 7760673.0;
   do {
      switch_l8m -= parseFloat(`${favoriteB.length}`);
      if (currentn) {
         break;
      }
   } while (currentn && (switch_l8m == 5.8));
      condensedO += `${android2.length * 3}`;
       let formX = String.fromCharCode(99,95,55,56,95,112,97,99,107,105,110,103,0);
       let t_unlocku = 3;
         t_unlocku >>= Math.min(4, formX.length);
      let specy = 6104139 <= t_unlocku;
      do {
         t_unlocku <<= Math.min(3, Math.abs(formX.length ^ t_unlocku));
         if (specy) {
            break;
         }
      } while ((4 < (1 << (Math.min(4, formX.length))) || (formX.length << (Math.min(Math.abs(1), 1))) < 4) && specy);
      let firebaseS = formX == String.fromCharCode(51,112,105,0);
      do {
         formX = `${t_unlocku * 2}`;
         if (firebaseS) {
            break;
         }
      } while (firebaseS && (4 == (formX.length >> (Math.min(1, Math.abs(t_unlocku))))));
      if (4 > (formX.length & t_unlocku) && (4 & t_unlocku) > 4) {
          let fileds: Array<any> = [63, 895, 12];
          let shirtu: Map<any, any> = new Map([[String.fromCharCode(102,109,117,108,95,54,95,52,50,0),String.fromCharCode(115,116,114,97,116,101,103,105,101,115,95,114,95,56,54,0)], [String.fromCharCode(115,111,114,116,95,51,95,51,53,0),String.fromCharCode(97,100,100,105,110,103,95,114,95,56,54,0)], [String.fromCharCode(109,97,116,114,105,120,105,110,103,95,106,95,50,57,0),String.fromCharCode(107,110,111,98,95,119,95,49,55,0)]]);
          let next3 = String.fromCharCode(102,97,109,105,108,121,0);
          let twittery = false;
          let livei = String.fromCharCode(102,102,117,114,108,95,109,95,50,48,0);
         formX += `${t_unlocku - 2}`;
         fileds = [next3.length - shirtu.size];
         shirtu.set(`${livei}`, shirtu.size);
         next3 = "1";
         twittery = (next3.length + fileds.length) <= 95;
         livei += `${shirtu.size}`;
      }
      for (let r = 0; r < 3; r++) {
         formX += `${t_unlocku}`;
      }
          let shrinkF: Array<any> = [641, 755, 377];
         formX = `${formX.length}`;
         shrinkF = [shrinkF.length | 2];
      headerV = `${3 | headerV.length}`;
      condensedO += `${(vertical1.length * (selectedx ? 1 : 3))}`;
   let package_iyZ = dataC.size <= 6660535;
   do {
      dataC = new Map([[`${benefity.size}`, 1]]);
      if (package_iyZ) {
         break;
      }
   } while (package_iyZ && (5 <= (1 ^ benefity.size) && (1 ^ dataC.size) <= 1));
      vertical1 = `${2 & parseInt(`${reactnativejsI}`)}`;
   if (benefity.get(`${twitterp}`) != null) {
      benefity = new Map([[`${switch_l8m}`, parseInt(`${switch_l8m}`) - 2]]);
   }
      selectedx = 48 > twitterp;

    onClose();

       let phone9 = 5.0;
       let subsz = 1;
       let policyJ = String.fromCharCode(115,95,52,54,95,101,120,116,114,97,102,105,101,108,100,0);
      let line6 = 9791959 <= subsz;
      do {
          let friendsh: Array<any> = [415, 697, 323];
          let v_playerT = true;
         subsz &= (String.fromCharCode(78,0) == policyJ ? (v_playerT ? 4 : 1) : policyJ.length);
         friendsh.push(friendsh.length);
         v_playerT = (friendsh.length & friendsh.length) == 50;
         if (line6) {
            break;
         }
      } while (line6 && (phone9 >= subsz));
      if (1.51 >= (phone9 - subsz)) {
         subsz >>= Math.min(3, Math.abs(1 ^ parseInt(`${phone9}`)));
      }
         subsz |= policyJ.length;
       let editY = 0.0;
      for (let d = 0; d < 2; d++) {
          let backgroundd: Map<any, any> = new Map([[String.fromCharCode(100,95,57,49,95,120,111,102,102,115,101,116,0),String.fromCharCode(97,95,49,51,95,112,97,117,115,101,0)], [String.fromCharCode(98,108,111,99,107,105,101,115,0),String.fromCharCode(101,110,116,114,111,112,121,109,111,100,101,0)]]);
          let sourceU = String.fromCharCode(114,116,115,112,95,113,95,50,48,0);
         phone9 /= Math.max(4, sourceU.length + 2);
         backgroundd = new Map([[`${backgroundd.size}`, 3]]);
         sourceU = `${backgroundd.size}`;
      }
         policyJ = `${(String.fromCharCode(119,0) == policyJ ? parseInt(`${editY}`) : policyJ.length)}`;
      let placeholder3 = phone9 >= 8546514.0;
      do {
          let sans3 = String.fromCharCode(118,95,54,56,95,114,111,111,116,115,0);
          let casting3 = 5;
         phone9 /= Math.max(3 - casting3, 4);
         sans3 += `${sans3.length}`;
         casting3 %= Math.max(4, 3 - sans3.length);
         if (placeholder3) {
            break;
         }
      } while ((phone9 >= 1.14) && placeholder3);
      if ((2.54 - editY) < 4.99) {
         subsz /= Math.max(policyJ.length, 5);
      }
       let feedback4 = false;
      benefity.set(condensedO, 3 ^ condensedO.length);
   while (5 > (headerV.length / (Math.max(2, parseInt(`${reactnativejsI}`)))) && (parseInt(`${reactnativejsI}`) / 5) > 5) {
      reactnativejsI /= Math.max(4, parseFloat(`${2}`));
      break;
   }
   for (let e = 0; e < 3; e++) {
      android2.push(twitterp);
   }
   for (let g = 0; g < 3; g++) {
       let singlec: Array<any> = [359, 355, 203];
         singlec.push(singlec.length);
         singlec = [singlec.length];
         singlec = [singlec.length / (Math.max(2, 5))];
      benefity.set(vertical1, vertical1.length * 2);
   }
       let g_playerF = 1;
       let gmailo = String.fromCharCode(99,111,110,99,97,116,101,110,97,116,105,111,110,95,116,95,57,0);
         g_playerF &= g_playerF;
       let muted5 = 3;
       let friendsw = 2;
      while (!gmailo.startsWith(`${muted5}`)) {
         gmailo = `${muted5 >> (Math.min(4, Math.abs(friendsw)))}`;
         break;
      }
         friendsw ^= muted5 & 2;
      let nativeexW = String.fromCharCode(119,103,55,108,102,97,0) == gmailo;
      do {
          let utilso = true;
          let checkboxg = false;
          let mbjscommonS = String.fromCharCode(116,114,97,99,101,95,57,95,57,48,0);
          let showj = String.fromCharCode(98,95,51,50,95,101,108,105,109,105,110,97,116,105,111,110,0);
          let kuaishouT = 4.0;
         gmailo += `${showj.length % 3}`;
         utilso = mbjscommonS.length < 76;
         checkboxg = (parseInt(`${kuaishouT}`) * mbjscommonS.length) >= 38;
         showj += `${((utilso ? 4 : 1))}`;
         kuaishouT -= (parseFloat(`${(checkboxg ? 2 : 4) % (Math.max((utilso ? 1 : 5), 7))}`));
         if (nativeexW) {
            break;
         }
      } while ((gmailo.endsWith(`${g_playerF}`)) && nativeexW);
          let statsr = String.fromCharCode(108,95,54,54,95,115,116,105,99,107,101,114,115,101,116,0);
          let tailb: Array<any> = [748, 520, 119];
         friendsw /= Math.max(5, 1);
         statsr = `${tailb.length}`;
         tailb.push(statsr.length * tailb.length);
      favoriteB = [2 - vertical1.length];
      condensedO = `${recommendation4.length}`;
   for (let u = 0; u < 2; u++) {
      selectedx = 11.53 >= (switch_l8m / (Math.max(7, reactnativejsI)));
   }
   while (1 < favoriteB.length) {
      favoriteB = [headerV.length];
      break;
   }
   if ((1 << (Math.min(4, condensedO.length))) == 3 || (1 << (Math.min(5, condensedO.length))) == 2) {
      benefity.set(`${selectedZ.length}`, favoriteB.length / 3);
   }
   while (4 >= (1 << (Math.min(1, modew.length)))) {
      xvode = [benefity.size];
      break;
   }
      headerV = `${dataC.size | 3}`;
   let annerO = String.fromCharCode(116,57,97,106,112,100,0) == modew;
   do {
      modew = `${recommendation4.length & headerV.length}`;
      if (annerO) {
         break;
      }
   } while (annerO && (4 < (android2.length | modew.length)));
   let register_a0Z = 6968334 <= android2.length;
   do {
      android2.push(modew.length);
      if (register_a0Z) {
         break;
      }
   } while (((android2.length & 5) < 4) && register_a0Z);
      benefity = new Map([[`${benefity.size}`, benefity.size]]);
      modew = `${favoriteB.length}`;
      switch_l8m += parseFloat(`${2}`);
   let placeholdere = 7594787 <= twitterp;
   do {
      twitterp >>= Math.min(4, Math.abs(3 >> (Math.min(1, xvode.length))));
      if (placeholdere) {
         break;
      }
   } while ((switch_l8m == 5.87) && placeholdere);
      android2.push(modew.length);
    dispatch(loginModalShown());
  }

  const handleOnPurchase = useCallback(() => {
       let greenh = 3.0;
    let detaili = 2;
    let gradlewQ = String.fromCharCode(116,97,98,108,101,95,54,95,49,51,0);
    let orientationW = String.fromCharCode(105,95,54,51,95,114,101,113,117,97,110,116,0);
    let whistle9 = 2.0;
    let eighteenz = 5.0;
    let dropdownj = String.fromCharCode(119,116,118,102,105,108,101,95,121,95,50,52,0);
    let klevinq = 1.0;
    let typesx = String.fromCharCode(102,105,120,117,112,0);
    let episodesd = 1.0;
      eighteenz /= Math.max((typesx == String.fromCharCode(117,0) ? parseInt(`${whistle9}`) : typesx.length), 2);
   for (let u = 0; u < 3; u++) {
      klevinq += parseFloat(`${parseInt(`${greenh}`)}`);
   }
   let photoW = detaili >= 5156733;
   do {
       let profiled = String.fromCharCode(98,101,110,99,95,115,95,49,0);
       let datau = true;
       let settingsz = true;
       let containery = String.fromCharCode(97,117,116,111,114,101,103,114,101,115,115,105,111,110,95,100,95,52,51,0);
       let bodanC = 4;
          let carouselH = 4.0;
         containery = "1";
         carouselH -= parseFloat(`${parseInt(`${carouselH}`) + 3}`);
      let twitterH = profiled == String.fromCharCode(108,105,51,54,110,0);
      do {
          let whiteA: Array<any> = [10, 198, 818];
          let phoney: Array<any> = [200, 297];
          let sorty = 3;
          let dycreators = 3.0;
         profiled += `${phoney.length}`;
         whiteA.push(3);
         phoney.push(parseInt(`${dycreators}`) / 2);
         sorty &= whiteA.length;
         dycreators *= parseInt(`${dycreators}`) * whiteA.length;
         if (twitterH) {
            break;
         }
      } while (twitterH && (profiled.endsWith(`${datau}`)));
      let readN = datau ? !datau : datau;
      do {
          let settingU = 1.0;
          let default_z1 = String.fromCharCode(112,97,105,100,0);
          let topona = 2.0;
         datau = !datau;
         settingU /= Math.max(5, (default_z1 == String.fromCharCode(53,0) ? parseInt(`${topona}`) : default_z1.length));
         topona /= Math.max(parseFloat(`${default_z1.length}`), 2);
         if (readN) {
            break;
         }
      } while ((!datau) && readN);
      if (profiled.length < 4 || datau) {
         profiled += `${containery.length}`;
      }
      if (settingsz) {
         containery = `${(profiled.length >> (Math.min(4, Math.abs((datau ? 4 : 1)))))}`;
      }
         profiled = `${((settingsz ? 5 : 5) & bodanC)}`;
       let componentm: Array<any> = [String.fromCharCode(119,95,54,48,95,118,97,114,108,101,110,0), String.fromCharCode(114,101,99,101,110,116,108,121,0)];
       let react1: Array<any> = [516, 887];
      while (3 >= bodanC && (3 - bodanC) >= 3) {
         datau = 84 <= react1.length;
         break;
      }
         bodanC &= ((datau ? 5 : 5) + 1);
         componentm = [2 - bodanC];
      if (containery.length >= 2) {
         bodanC %= Math.max(2, ((settingsz ? 2 : 4) | 3));
      }
      let downloadedG = settingsz ? !settingsz : settingsz;
      do {
         settingsz = bodanC > 50;
         if (downloadedG) {
            break;
         }
      } while ((settingsz) && downloadedG);
      let animationE = 6297013 >= bodanC;
      do {
         bodanC &= 2 & react1.length;
         if (animationE) {
            break;
         }
      } while (animationE && (3 > (5 ^ profiled.length) && 5 > (bodanC ^ profiled.length)));
      let yingw = 5242160 <= componentm.length;
      do {
         componentm.push(3 << (Math.min(2, react1.length)));
         if (yingw) {
            break;
         }
      } while ((4 > (componentm.length - bodanC)) && yingw);
      while (!datau && containery.length == 5) {
          let clock1 = 1.0;
          let loginX = String.fromCharCode(111,95,56,55,95,103,114,97,110,117,108,101,112,111,115,0);
         containery += `${2 / (Math.max(4, loginX.length))}`;
         clock1 *= parseInt(`${clock1}`);
         loginX += `${3 | parseInt(`${clock1}`)}`;
         break;
      }
      detaili >>= Math.min(2, Math.abs(1));
      if (photoW) {
         break;
      }
   } while (photoW && ((4.64 * eighteenz) == 4.1 && 5.48 == (eighteenz * 4.64)));
   while ((episodesd / (Math.max(4.24, 6))) == 3.16 && (detaili / (Math.max(9, episodesd))) == 4.24) {
      detaili &= parseInt(`${eighteenz}`);
      break;
   }
   for (let j = 0; j < 2; j++) {
      greenh /= Math.max(2, 2);
   }

    onClose();

      gradlewQ = `${parseInt(`${greenh}`)}`;
       let circlez = 2.0;
         circlez /= Math.max(parseInt(`${circlez}`), 4);
       let dycreatorv = 0.0;
       let dataF = 3.0;
         circlez *= parseInt(`${dataF}`);
      episodesd += typesx.length;
       let videojs7: Map<any, any> = new Map([[String.fromCharCode(115,104,97,114,112,101,110,105,110,103,0),false ], [String.fromCharCode(99,115,115,95,105,95,49,54,0),true ]]);
       let volume1: Map<any, any> = new Map([[String.fromCharCode(100,95,55,54,95,109,97,107,101,99,121,103,119,105,110,112,107,103,0),true ], [String.fromCharCode(116,111,107,101,110,115,0),true ], [String.fromCharCode(102,105,114,101,98,97,115,101,95,112,95,54,48,0),false ]]);
       let selection_ = 3;
       let listN = 5.0;
       let statisticsc = String.fromCharCode(115,107,105,112,112,97,98,108,101,0);
       let sellS = String.fromCharCode(101,100,105,116,101,100,0);
       let pathM = String.fromCharCode(105,115,115,117,101,115,95,102,95,55,52,0);
          let vietnamQ: Array<any> = [325, 499, 567];
          let resultp = String.fromCharCode(108,95,52,55,95,109,115,101,120,0);
          let filter4: Array<any> = [47, 972];
         selection_ >>= Math.min(5, Math.abs(resultp.length * statisticsc.length));
         vietnamQ.push(1 | filter4.length);
         resultp += `${3 - filter4.length}`;
      while (sellS.length >= 2) {
          let canvasd = String.fromCharCode(103,105,102,115,95,109,95,52,57,0);
          let colorsG: Map<any, any> = new Map([[String.fromCharCode(97,95,55,54,95,102,111,111,0),156], [String.fromCharCode(102,95,53,54,95,116,104,114,111,116,116,108,101,114,0),616]]);
         sellS += `${parseInt(`${listN}`)}`;
         canvasd += `${canvasd.length | colorsG.size}`;
         colorsG.set(canvasd, 1);
         break;
      }
         statisticsc = `${(String.fromCharCode(72,0) == pathM ? pathM.length : statisticsc.length)}`;
      for (let v = 0; v < 3; v++) {
         pathM = "1";
      }
      if (3.61 >= listN) {
          let backward5 = 2;
         sellS += `${pathM.length}`;
         backward5 |= backward5;
      }
         pathM = `${videojs7.size % (Math.max(pathM.length, 6))}`;
      gradlewQ = `${parseInt(`${episodesd}`) - videojs7.size}`;
      volume1 = new Map([[`${volume1.size}`, 3]]);
       let circleK: Map<any, any> = new Map([[String.fromCharCode(106,109,101,109,115,121,115,0),660], [String.fromCharCode(115,116,100,97,116,111,109,105,99,0),517], [String.fromCharCode(97,95,53,0),320]]);
       let manifestK: Map<any, any> = new Map([[String.fromCharCode(101,95,50,53,95,99,104,101,99,107,115,116,114,105,100,101,0),578], [String.fromCharCode(97,110,116,105,97,108,105,97,115,105,110,103,0),37], [String.fromCharCode(104,95,55,56,95,109,111,100,105,102,121,0),1]]);
       let controls_ = false;
         manifestK = new Map([[`${manifestK.size}`, ((controls_ ? 3 : 2) * manifestK.size)]]);
         controls_ = manifestK.size <= 5;
          let assistl = String.fromCharCode(101,97,103,101,114,95,114,95,50,48,0);
          let crownd = 0;
          let bootsplashU = String.fromCharCode(101,95,50,56,95,116,111,111,108,0);
         circleK.set(bootsplashU, 3 + crownd);
         assistl += `${assistl.length}`;
         crownd <<= Math.min(3, Math.abs(assistl.length | assistl.length));
      for (let n = 0; n < 3; n++) {
         circleK.set(`${controls_}`, (manifestK.size - (controls_ ? 5 : 3)));
      }
      if ((3 & manifestK.size) == 2 || 1 == (manifestK.size & 3)) {
          let detailsb = String.fromCharCode(105,110,105,116,104,95,115,95,54,56,0);
          let tumbnailz = String.fromCharCode(103,95,56,50,95,108,116,97,98,108,101,0);
         manifestK.set(`${controls_}`, 1 ^ manifestK.size);
         detailsb = `${detailsb.length}`;
         tumbnailz = `${3 + detailsb.length}`;
      }
         controls_ = manifestK.size >= circleK.size;
          let giftv = String.fromCharCode(101,120,115,121,0);
          let reducerw = 5.0;
          let episodesf: Map<any, any> = new Map([[String.fromCharCode(104,95,49,51,95,112,114,101,99,0),false ], [String.fromCharCode(111,98,109,99,95,112,95,52,0),true ]]);
         circleK = new Map([[`${manifestK.size}`, episodesf.size % (Math.max(manifestK.size, 4))]]);
         giftv += "1";
         reducerw *= parseFloat(`${giftv.length ^ 3}`);
         episodesf = new Map([[giftv, parseInt(`${reducerw}`)]]);
       let gradlewZ = String.fromCharCode(109,97,112,112,101,114,95,51,95,55,51,0);
       let lessw = String.fromCharCode(100,105,118,105,100,111,114,115,0);
      while (4 == (2 & circleK.size)) {
         lessw = "3";
         break;
      }
      dropdownj = `${parseInt(`${greenh}`) / (Math.max(3, detaili))}`;
       let nativeexw: Array<any> = [String.fromCharCode(114,101,99,101,105,118,105,110,103,0), String.fromCharCode(101,97,114,108,121,95,117,95,50,50,0)];
       let confirmation_ = 4.0;
          let nativeD = 4;
         confirmation_ *= 3 << (Math.min(Math.abs(parseInt(`${confirmation_}`)), 1));
         nativeD <<= Math.min(2, Math.abs(nativeD));
      if (1 >= nativeexw.length) {
         nativeexw.push(parseInt(`${confirmation_}`) | nativeexw.length);
      }
          let chinasame3 = 5.0;
          let update_ylM = String.fromCharCode(114,111,116,111,0);
          let foregroundn: Array<any> = [18, 619, 787];
         nativeexw = [foregroundn.length];
         chinasame3 += parseFloat(`${parseInt(`${chinasame3}`) / 3}`);
         update_ylM = "2";
         foregroundn = [parseInt(`${chinasame3}`) << (Math.min(update_ylM.length, 1))];
       let dicee = String.fromCharCode(122,95,55,55,95,116,112,105,100,0);
       let footballE = String.fromCharCode(117,95,51,57,95,99,111,101,102,102,0);
      let progressK = String.fromCharCode(111,112,105,49,95,50,113,115,51,121,0) == footballE;
      do {
         footballE = `${(String.fromCharCode(107,0) == footballE ? footballE.length : nativeexw.length)}`;
         if (progressK) {
            break;
         }
      } while (progressK && (dicee.length >= footballE.length));
         confirmation_ += (dicee == String.fromCharCode(74,0) ? footballE.length : dicee.length);
      klevinq *= parseFloat(`${parseInt(`${episodesd}`)}`);
    dispatch(loginModalShown());

   if ((whistle9 / 3.77) >= 2.17) {
      whistle9 -= orientationW.length | parseInt(`${greenh}`);
   }
   while ((gradlewQ.length << (Math.min(Math.abs(5), 2))) > 2) {
      gradlewQ += `${3 / (Math.max(parseInt(`${episodesd}`), 6))}`;
      break;
   }
   if ((whistle9 / 2.12) == 2.5) {
       let promotionX: Array<any> = [418, 431];
       let bodanu = true;
       let hongkongM = 5.0;
      if (bodanu) {
         bodanu = promotionX.length < 36;
      }
      let hookY = 8313470 >= promotionX.length;
      do {
          let kickM: Map<any, any> = new Map([[String.fromCharCode(101,95,54,55,95,117,110,115,101,114,105,97,108,105,122,101,0),340], [String.fromCharCode(100,105,103,105,116,0),106], [String.fromCharCode(97,99,99,101,115,115,95,99,95,56,50,0),688]]);
          let whiteP = 5.0;
          let downloader5 = String.fromCharCode(112,111,111,108,0);
          let update_m2 = String.fromCharCode(118,105,115,105,98,105,108,105,116,105,101,115,0);
          let cross9 = String.fromCharCode(100,95,56,52,95,116,103,99,97,108,108,115,0);
         promotionX = [3 - kickM.size];
         kickM.set(downloader5, downloader5.length & parseInt(`${whiteP}`));
         whiteP += downloader5.length;
         update_m2 += `${downloader5.length}`;
         cross9 = `${(cross9 == String.fromCharCode(52,0) ? cross9.length : parseInt(`${whiteP}`))}`;
         if (hookY) {
            break;
         }
      } while (hookY && (2 > promotionX.length && 1 > (2 | promotionX.length)));
         promotionX = [1];
          let backgroundT = String.fromCharCode(97,115,105,110,107,0);
         hongkongM += parseInt(`${hongkongM}`) - backgroundT.length;
         hongkongM += 1 >> (Math.min(Math.abs(parseInt(`${hongkongM}`)), 4));
      if (promotionX.length < 3) {
         promotionX = [parseInt(`${hongkongM}`) + 3];
      }
          let success0 = String.fromCharCode(118,97,108,105,100,97,116,105,111,110,115,95,114,95,52,54,0);
         hongkongM += 1 >> (Math.min(2, promotionX.length));
         success0 = `${success0.length / (Math.max(2, 1))}`;
          let forward_ = String.fromCharCode(106,112,101,103,108,115,95,54,95,54,55,0);
          let ewardedP = String.fromCharCode(97,118,111,105,100,0);
          let sellt = String.fromCharCode(100,101,115,116,114,111,121,101,100,0);
         bodanu = 91 > sellt.length;
         forward_ += "1";
         ewardedP += `${ewardedP.length * 1}`;
         sellt += `${ewardedP.length >> (Math.min(Math.abs(1), 2))}`;
      let hooksz = hongkongM >= 7701262.0;
      do {
          let bell6 = 0;
          let sellL = 1.0;
          let googlej = 4;
         hongkongM /= Math.max(4, 1);
         bell6 ^= parseInt(`${sellL}`);
         sellL += parseFloat(`${parseInt(`${sellL}`) >> (Math.min(4, Math.abs(3)))}`);
         googlej &= 1;
         if (hooksz) {
            break;
         }
      } while ((hongkongM > 3.2 || 2.64 > (3.2 / (Math.max(2, hongkongM)))) && hooksz);
      whistle9 *= parseInt(`${whistle9}`) << (Math.min(2, Math.abs(1)));
   }
   for (let c = 0; c < 1; c++) {
      klevinq /= Math.max(2, parseFloat(`${detaili}`));
   }
   let homeH = 7562549.0 >= episodesd;
   do {
      episodesd *= 3;
      if (homeH) {
         break;
      }
   } while (homeH && ((5 + orientationW.length) > 4));
    dispatch(showLoginAction());
    
    
    
    
    


  }, []);

  return (
    <Modal
      visible={showCondition}
      transparent={true}
      animationType="fade"
      onRequestClose={onCloseModal}
    >
      <TouchableWithoutFeedback onPress={onCloseModal}>
        <View style={{ flex: 1, backgroundColor: "rgba(0, 0, 0, 0.4)" }}>
          <VipLoginAlertModal
            coverBackground={sportBg}
            coverImage={commonModels}
            onPurchase={handleOnPurchase}
            onClose={onCloseModal}
            showCondition={showCondition}
          />
        </View>
      </TouchableWithoutFeedback>
    </Modal>
  );
};
