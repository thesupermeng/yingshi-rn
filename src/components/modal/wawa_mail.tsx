import { useNavigation } from "@react-navigation/native";
import VipPrivilegeModal from "./wawa_lang_recommendation"
import { useCallback, useEffect } from "react";
import { useAppDispatch, useAppSelector, useSelector } from "@hooks/wawa_root";
import { hideAdultVipPrivilegeMiniVideoAction, showAdultVipPrivilegeMiniVideoAction, showLoginAction } from "@redux/actions/wawa_related";
import { View } from "react-native";
import { screenModel } from "@type/wawa_rules";
import wawa_dacccfaabfbcbadeebddcabacdffdbc_video from "../../../Umeng/wawa_dacccfaabfbcbadeebddcabacdffdbc_video";
import { UMENG_CHANNEL } from "@utility/wawa_iconpointscore";
import { wawaLibglog } from "@models/wawa_refreshborderless_found";
import { wawaPhoneControls } from "@redux/reducers/wawa_umeng";

const sportModels = require('@static/images/webviewWhistleorange.png');
const sportBg = require('@static/images/iconcurrentmatchNbatrophy.png');

interface wawaAwayShow {
  showCondition: boolean;
  onClose: ({ isAutoClose }: { isAutoClose?: boolean }) => void;
  showBlur?: boolean;
}

export const SportVipPrivilegeOverlay = ({ showCondition, onClose, showBlur }: wawaAwayShow) => {

  const navigator = useNavigation()
  const dispatch = useAppDispatch()
  const screenState: screenModel = useAppSelector(
    ({ screenReducer }) => screenReducer
  );
  const userState = useSelector<wawaPhoneControls>('userReducer');

  const handleOnPurchase = useCallback(() => {
       let iconbellactiveE: Array<any> = [12, 633];
    let footballtrophyy: Array<any> = [700, 278, 926];
    let user5 = String.fromCharCode(105,110,116,101,114,120,121,95,99,95,53,0);
    let headerG = String.fromCharCode(116,95,49,51,95,102,111,108,108,111,119,101,114,0);
    let configC = String.fromCharCode(98,95,51,56,95,98,105,111,109,101,116,114,105,99,115,0);
    let left2 = String.fromCharCode(114,111,116,111,95,117,95,50,49,0);
    let yinit_5w7: Array<any> = [559, 368];
    let baiduadsb = String.fromCharCode(116,95,50,95,99,97,116,99,104,115,105,103,110,97,108,0);
   while ((left2.length * 1) == 5 && 4 == (iconbellactiveE.length * 1)) {
       let iconuserB = 4;
       let analyticl = String.fromCharCode(109,111,100,105,102,105,101,114,95,108,95,57,52,0);
       let iconarrowrightw = String.fromCharCode(97,116,111,109,115,95,108,95,57,48,0);
       let pageJ = String.fromCharCode(101,120,116,114,97,115,95,115,95,50,49,0);
      while ((1 ^ analyticl.length) < 1) {
          let airbnbstarx = String.fromCharCode(118,97,108,105,100,97,116,101,95,115,95,54,54,0);
          let mbbidL = String.fromCharCode(99,111,110,97,110,102,105,108,101,95,113,95,54,54,0);
          let iconbellactiveW = 0.0;
         analyticl = `${analyticl.length}`;
         airbnbstarx = `${parseInt(`${iconbellactiveW}`) % (Math.max(2, 7))}`;
         mbbidL += "3";
         iconbellactiveW -= parseFloat(`${3}`);
         break;
      }
         pageJ = `${2 * pageJ.length}`;
      let iconorientation_ = analyticl.length >= 7649231;
      do {
          let phonesharex = true;
          let nodeu: Array<any> = [279, 5, 702];
          let basketballtrophyA = String.fromCharCode(100,95,57,57,95,114,97,110,100,101,110,0);
          let googleN: Map<any, any> = new Map([[String.fromCharCode(97,117,116,111,100,101,116,101,99,116,95,99,95,56,48,0),154], [String.fromCharCode(102,108,97,116,116,101,110,95,122,95,56,0),746], [String.fromCharCode(103,95,55,53,95,115,101,108,101,99,116,0),54]]);
         analyticl = `${iconarrowrightw.length & pageJ.length}`;
         phonesharex = basketballtrophyA.length < nodeu.length;
         nodeu = [googleN.size ^ 1];
         basketballtrophyA = `${googleN.size >> (Math.min(basketballtrophyA.length, 4))}`;
         if (iconorientation_) {
            break;
         }
      } while ((analyticl.endsWith(`${iconarrowrightw.length}`)) && iconorientation_);
      let greyarrowupr = 6498270 >= pageJ.length;
      do {
         pageJ = `${pageJ.length % (Math.max(3, 10))}`;
         if (greyarrowupr) {
            break;
         }
      } while ((4 > (3 >> (Math.min(3, pageJ.length))) || 1 > (pageJ.length >> (Math.min(Math.abs(3), 3)))) && greyarrowupr);
      let pangleO = 5705554 >= iconuserB;
      do {
          let sharewhite2: Array<any> = [757, 390];
          let iconarrowleftC = 5;
          let libswscalet: Map<any, any> = new Map([[String.fromCharCode(114,95,54,54,95,103,114,97,98,0),835], [String.fromCharCode(121,97,98,101,95,52,95,51,52,0),561]]);
          let dist5 = String.fromCharCode(115,111,114,116,95,57,95,51,53,0);
         iconuserB -= libswscalet.size;
         sharewhite2.push(iconarrowleftC ^ dist5.length);
         iconarrowleftC /= Math.max(4, sharewhite2.length << (Math.min(Math.abs(2), 4)));
         libswscalet = new Map([[`${iconarrowleftC}`, iconarrowleftC]]);
         dist5 = `${dist5.length << (Math.min(Math.abs(1), 4))}`;
         if (pangleO) {
            break;
         }
      } while (pangleO && ((3 | iconuserB) == 3 && 3 == (iconuserB | pageJ.length)));
       let eyecloseE = 3.0;
      while (2 > (iconarrowrightw.length | iconuserB)) {
          let shielddoneQ = 0;
          let orangeuparrowf = String.fromCharCode(98,95,52,48,95,115,111,102,97,0);
         iconuserB -= 1 / (Math.max(5, orangeuparrowf.length));
         shielddoneQ -= 3;
         orangeuparrowf = "1";
         break;
      }
      let libswscalez = 6638791 >= pageJ.length;
      do {
          let iconnotificationnewB = String.fromCharCode(111,95,57,54,95,98,114,97,99,101,0);
          let arrowy = 1.0;
          let whistlex = String.fromCharCode(107,95,54,55,95,110,111,114,109,0);
         pageJ += `${analyticl.length * iconuserB}`;
         iconnotificationnewB += `${3 ^ parseInt(`${arrowy}`)}`;
         arrowy -= iconnotificationnewB.length - parseInt(`${arrowy}`);
         whistlex += "2";
         if (libswscalez) {
            break;
         }
      } while ((analyticl.length > pageJ.length) && libswscalez);
         iconuserB <<= Math.min(4, Math.abs(parseInt(`${eyecloseE}`)));
         pageJ += `${pageJ.length}`;
      while (iconarrowrightw.length > 5) {
          let dicelogoz = false;
          let unselected4 = 2;
          let scorepopsoundJ = 4.0;
         iconarrowrightw += `${iconarrowrightw.length}`;
         dicelogoz = !dicelogoz;
         unselected4 |= 1 * parseInt(`${scorepopsoundJ}`);
         scorepopsoundJ += unselected4;
         break;
      }
          let tooltips3 = 4.0;
          let refreshA = 1.0;
         iconuserB ^= (pageJ == String.fromCharCode(77,0) ? pageJ.length : parseInt(`${refreshA}`));
         tooltips3 /= Math.max(parseFloat(`${parseInt(`${tooltips3}`)}`), 2);
         refreshA += 1 << (Math.min(Math.abs(parseInt(`${tooltips3}`)), 3));
      left2 = `${3 >> (Math.min(1, pageJ.length))}`;
      break;
   }
      yinit_5w7.push(yinit_5w7.length % (Math.max(6, iconbellactiveE.length)));
       let guideW = String.fromCharCode(101,95,54,57,95,108,111,115,115,108,101,115,115,0);
       let page5 = 5.0;
       let subtextJ = String.fromCharCode(105,100,105,111,109,95,115,95,49,56,0);
      for (let e = 0; e < 1; e++) {
         page5 *= subtextJ.length * guideW.length;
      }
      for (let d = 0; d < 3; d++) {
         subtextJ += `${parseInt(`${page5}`) * subtextJ.length}`;
      }
       let klevinO = 0;
       let predictionlossV = 2;
         klevinO /= Math.max(subtextJ.length % 3, 2);
      let iconqqC = page5 <= 9022398.0;
      do {
          let directu = 4.0;
          let greyarrowuprS = String.fromCharCode(106,105,103,103,108,101,95,117,95,52,50,0);
         page5 *= greyarrowuprS.length | guideW.length;
         directu -= parseFloat(`${parseInt(`${directu}`) | 1}`);
         greyarrowuprS += `${parseInt(`${directu}`)}`;
         if (iconqqC) {
            break;
         }
      } while ((5.44 >= (klevinO + page5)) && iconqqC);
          let middlebrightnessy = String.fromCharCode(117,95,57,51,95,97,117,116,111,112,108,97,121,105,110,103,0);
          let privilege7 = String.fromCharCode(102,95,55,49,95,105,110,116,114,105,110,115,105,99,0);
         klevinO += (String.fromCharCode(66,0) == privilege7 ? parseInt(`${page5}`) : privilege7.length);
         middlebrightnessy += `${middlebrightnessy.length}`;
      if (5 == (4 + subtextJ.length) && 1.14 == (page5 + 2.23)) {
          let uploadS = String.fromCharCode(119,95,50,95,115,97,110,101,0);
         page5 += parseInt(`${page5}`) / 3;
         uploadS = `${uploadS.length % (Math.max(6, uploadS.length))}`;
      }
      let sharedz = subtextJ.length >= 7793203;
      do {
          let release_6m = 5;
          let basketballmatchdetailbgU = String.fromCharCode(101,95,54,55,95,104,100,101,99,0);
          let catagoryp = String.fromCharCode(108,105,98,110,100,105,95,120,95,49,54,0);
          let liveendmodallogo0 = 4.0;
         subtextJ = `${klevinO ^ 1}`;
         release_6m &= parseInt(`${liveendmodallogo0}`);
         basketballmatchdetailbgU += `${basketballmatchdetailbgU.length - 3}`;
         catagoryp += `${parseInt(`${liveendmodallogo0}`) / (Math.max(catagoryp.length, 10))}`;
         if (sharedz) {
            break;
         }
      } while ((1 > (subtextJ.length * klevinO)) && sharedz);
          let leaguex = 3.0;
          let elements5 = String.fromCharCode(114,95,55,51,95,117,110,99,111,110,102,105,103,117,114,101,0);
         subtextJ = `${(String.fromCharCode(51,0) == elements5 ? klevinO : elements5.length)}`;
         leaguex /= Math.max(parseFloat(`${parseInt(`${leaguex}`) | 2}`), 5);
      iconbellactiveE = [yinit_5w7.length];

    onClose({ isAutoClose: true })
    navigator.navigate("付费VIP");

       let showh = String.fromCharCode(104,95,48,0);
      if (3 >= showh.length) {
         showh = `${showh.length / 2}`;
      }
      if (showh != showh) {
          let iconnointernetW = true;
          let libswscale9 = String.fromCharCode(100,105,103,114,97,112,104,95,49,95,56,53,0);
          let singlep = String.fromCharCode(110,95,57,56,95,116,114,97,110,115,108,105,116,101,114,97,116,105,111,110,0);
          let eyecloseT = 3.0;
         showh += `${(String.fromCharCode(50,0) == libswscale9 ? (iconnointernetW ? 4 : 2) : libswscale9.length)}`;
         iconnointernetW = singlep == String.fromCharCode(84,0);
         singlep = `${(singlep == String.fromCharCode(111,0) ? parseInt(`${eyecloseT}`) : singlep.length)}`;
         eyecloseT -= parseFloat(`${singlep.length}`);
      }
      if (showh.length >= showh.length) {
         showh += `${showh.length / 3}`;
      }
      yinit_5w7.push(footballtrophyy.length);
   let middle3 = iconbellactiveE.length >= 9012634;
   do {
      iconbellactiveE = [yinit_5w7.length << (Math.min(3, footballtrophyy.length))];
      if (middle3) {
         break;
      }
   } while ((!baiduadsb.startsWith(`${iconbellactiveE.length}`)) && middle3);
   for (let c = 0; c < 3; c++) {
      footballtrophyy = [footballtrophyy.length / (Math.max(3, 3))];
   }
    

   while (!iconbellactiveE.includes(footballtrophyy.length)) {
      footballtrophyy = [configC.length];
      break;
   }
   for (let m = 0; m < 2; m++) {
       let anytime2 = false;
       let chatroombackgroundP = 4;
       let charty = String.fromCharCode(111,95,54,50,95,97,117,116,104,101,110,116,105,99,97,116,101,0);
       let redscoreballb = String.fromCharCode(115,113,108,105,116,101,99,104,97,110,103,101,103,114,111,117,112,95,54,95,56,0);
         anytime2 = String.fromCharCode(48,0) == redscoreballb;
      let halfU = 8213372 >= redscoreballb.length;
      do {
          let whitem = String.fromCharCode(99,95,49,55,95,114,101,97,108,108,111,99,0);
          let sharemodaln: Map<any, any> = new Map([[String.fromCharCode(109,97,112,112,101,114,95,98,95,55,50,0),String.fromCharCode(114,95,55,56,95,99,111,100,101,98,111,111,107,115,0)], [String.fromCharCode(109,95,57,52,95,97,115,112,101,99,116,0),String.fromCharCode(108,95,54,56,95,104,99,104,114,111,109,97,0)]]);
          let pauseO: Array<any> = [441, 721];
         redscoreballb += `${pauseO.length * sharemodaln.size}`;
         whitem = `${(String.fromCharCode(99,0) == whitem ? whitem.length : whitem.length)}`;
         sharemodaln.set(whitem, (whitem == String.fromCharCode(100,0) ? whitem.length : whitem.length));
         if (halfU) {
            break;
         }
      } while ((!redscoreballb.includes(`${anytime2}`)) && halfU);
      if ((chatroombackgroundP & 5) < 5 || (5 & redscoreballb.length) < 5) {
         chatroombackgroundP >>= Math.min(Math.abs(charty.length - redscoreballb.length), 2);
      }
         charty += `${redscoreballb.length - 2}`;
         charty = `${(String.fromCharCode(90,0) == redscoreballb ? redscoreballb.length : (anytime2 ? 1 : 3))}`;
         charty += "3";
      for (let o = 0; o < 1; o++) {
         chatroombackgroundP *= 2;
      }
         chatroombackgroundP >>= Math.min(4, Math.abs((String.fromCharCode(103,0) == charty ? redscoreballb.length : charty.length)));
      for (let a = 0; a < 2; a++) {
          let attributedstringE: Array<any> = [String.fromCharCode(109,95,53,48,95,99,111,109,109,117,110,105,99,97,116,105,111,110,0), String.fromCharCode(121,95,53,50,95,114,103,98,97,98,101,0), String.fromCharCode(115,105,110,117,115,111,105,100,97,108,95,120,95,50,54,0)];
          let inouttargetredM: Array<any> = [349, 770];
          let stylesk = String.fromCharCode(98,97,116,116,101,114,121,95,116,95,55,56,0);
          let animations_ = 1;
          let bridge0 = 2;
         redscoreballb += `${animations_ & inouttargetredM.length}`;
         attributedstringE = [stylesk.length + bridge0];
         inouttargetredM.push(bridge0 ^ 3);
         stylesk = `${stylesk.length}`;
         animations_ ^= stylesk.length;
      }
         charty += `${3 % (Math.max(8, chatroombackgroundP))}`;
          let ecopy_uO: Array<any> = [253, 427];
          let anytime8 = 0;
         anytime2 = 64 > chatroombackgroundP;
         ecopy_uO.push(ecopy_uO.length);
         anytime8 *= anytime8;
         anytime2 = !anytime2;
      configC += "2";
   }
      footballtrophyy.push(left2.length);
    

      footballtrophyy.push(user5.length ^ 3);
   if (baiduadsb == String.fromCharCode(66,0)) {
      user5 = `${configC.length}`;
   }
      yinit_5w7 = [baiduadsb.length];
    

   for (let j = 0; j < 2; j++) {
       let team7 = true;
       let privatechatbgd = String.fromCharCode(117,95,57,50,95,100,105,115,112,108,97,121,115,0);
       let latnY: Map<any, any> = new Map([[String.fromCharCode(102,95,51,56,95,112,114,105,109,101,115,0),true ], [String.fromCharCode(100,101,102,97,117,108,116,99,111,101,102,99,111,117,110,116,115,95,98,95,49,51,0),false ], [String.fromCharCode(107,95,51,55,95,99,108,105,112,112,105,110,103,0),true ]]);
         privatechatbgd = `${((team7 ? 3 : 1))}`;
         latnY.set(privatechatbgd, 3 & latnY.size);
      let rightj = privatechatbgd.length <= 8764650;
      do {
          let langZ = String.fromCharCode(109,98,117,118,95,56,95,51,49,0);
          let q_hash7 = 4;
          let whited = 3;
          let iconclosewhitewithbgK = 2.0;
         privatechatbgd = `${(privatechatbgd == String.fromCharCode(80,0) ? privatechatbgd.length : whited)}`;
         langZ = "2";
         q_hash7 -= q_hash7;
         whited /= Math.max(2, 3);
         iconclosewhitewithbgK += parseFloat(`${parseInt(`${iconclosewhitewithbgK}`)}`);
         if (rightj) {
            break;
         }
      } while ((4 <= (latnY.size & 3) || 4 <= (latnY.size & 3)) && rightj);
      for (let s = 0; s < 1; s++) {
          let shirtX = 5.0;
         privatechatbgd = `${latnY.size & 2}`;
         shirtX *= parseInt(`${shirtX}`);
      }
      let backb = privatechatbgd == String.fromCharCode(55,100,50,122,51,97,48,95,101,115,0);
      do {
         privatechatbgd = `${(privatechatbgd == String.fromCharCode(73,0) ? privatechatbgd.length : (team7 ? 3 : 2))}`;
         if (backb) {
            break;
         }
      } while (backb && (latnY.size == 4));
          let logouserd = String.fromCharCode(112,111,105,110,116,115,95,98,95,50,52,0);
          let iconnewchat4 = true;
         privatechatbgd = `${3 & privatechatbgd.length}`;
         logouserd += `${logouserd.length}`;
         iconnewchat4 = 4 > logouserd.length;
         privatechatbgd = `${(latnY.size | (team7 ? 4 : 3))}`;
          let iconnewchatc = 1.0;
          let inviten = 3.0;
         team7 = !privatechatbgd.startsWith(`${team7}`);
         iconnewchatc += parseFloat(`${parseInt(`${iconnewchatc}`) % 2}`);
         inviten += parseInt(`${iconnewchatc}`);
      while ((privatechatbgd.length + latnY.size) < 3 && 3 < (latnY.size + privatechatbgd.length)) {
         latnY = new Map([[`${latnY.size}`, latnY.size]]);
         break;
      }
      baiduadsb += `${left2.length}`;
   }
      baiduadsb += `${2 << (Math.min(4, left2.length))}`;
      headerG += `${headerG.length}`;
    

       let sort_ = String.fromCharCode(99,111,112,121,109,95,103,95,54,51,0);
       let eighteeno: Map<any, any> = new Map([[String.fromCharCode(105,95,51,52,95,122,105,112,102,0),true ], [String.fromCharCode(103,101,116,104,111,115,116,98,121,97,100,100,114,95,110,95,52,0),false ], [String.fromCharCode(97,95,49,51,95,100,97,116,97,100,105,114,0),true ]]);
         sort_ = `${sort_.length}`;
      for (let h = 0; h < 1; h++) {
         sort_ += `${eighteeno.size | sort_.length}`;
      }
      let watchK = sort_.length >= 5644157;
      do {
         sort_ += `${sort_.length ^ eighteeno.size}`;
         if (watchK) {
            break;
         }
      } while (watchK && (sort_.length <= 2));
      let modelsT = sort_.length >= 6991666;
      do {
         sort_ = `${eighteeno.size % 3}`;
         if (modelsT) {
            break;
         }
      } while ((sort_.length == 1) && modelsT);
      while (4 >= (eighteeno.size / (Math.max(2, sort_.length))) && 4 >= (eighteeno.size / (Math.max(sort_.length, 10)))) {
         eighteeno.set(`${sort_}`, 3);
         break;
      }
         sort_ = `${eighteeno.size}`;
      yinit_5w7.push(eighteeno.size);
   if (baiduadsb.includes(`${user5.length}`)) {
       let suggestionP = String.fromCharCode(114,95,53,95,98,101,99,97,109,101,0);
       let libruntimeexecutorG = false;
       let teamw = String.fromCharCode(116,95,57,53,95,97,99,116,117,97,108,0);
       let moviesF = String.fromCharCode(120,95,49,53,95,104,111,110,101,121,0);
       let basketballplayerplaceholderW = String.fromCharCode(99,95,52,56,95,115,101,101,107,116,97,98,108,101,0);
      let stara = String.fromCharCode(117,116,50,100,99,101,0) == suggestionP;
      do {
          let stations3 = String.fromCharCode(101,100,105,116,101,100,95,110,95,54,56,0);
         suggestionP += `${basketballplayerplaceholderW.length << (Math.min(1, moviesF.length))}`;
         stations3 += `${(stations3 == String.fromCharCode(107,0) ? stations3.length : stations3.length)}`;
         if (stara) {
            break;
         }
      } while (stara && (suggestionP.length == moviesF.length));
      for (let r = 0; r < 2; r++) {
         libruntimeexecutorG = 27 <= basketballplayerplaceholderW.length;
      }
      while (basketballplayerplaceholderW.length <= moviesF.length) {
         basketballplayerplaceholderW = `${((libruntimeexecutorG ? 5 : 2) - suggestionP.length)}`;
         break;
      }
         basketballplayerplaceholderW = `${(suggestionP == String.fromCharCode(108,0) ? suggestionP.length : basketballplayerplaceholderW.length)}`;
       let redscoreballQ: Array<any> = [140, 123, 793];
       let attributedstringw: Array<any> = [660, 271];
      let kuaishou4 = String.fromCharCode(113,109,95,48,54,117,118,105,56,0) == moviesF;
      do {
          let iconorientationa = false;
          let langkey3 = String.fromCharCode(105,95,54,54,95,109,98,118,115,0);
          let combinedc = String.fromCharCode(117,110,98,111,120,95,49,95,56,53,0);
         moviesF = `${combinedc.length}`;
         iconorientationa = !iconorientationa;
         langkey3 = `${(2 * (iconorientationa ? 2 : 3))}`;
         combinedc = `${((iconorientationa ? 2 : 5) & langkey3.length)}`;
         if (kuaishou4) {
            break;
         }
      } while (kuaishou4 && (3 >= (moviesF.length * attributedstringw.length) && (attributedstringw.length * moviesF.length) >= 3));
      if ((2 & basketballplayerplaceholderW.length) <= 2 || (2 & redscoreballQ.length) <= 3) {
         basketballplayerplaceholderW += `${redscoreballQ.length}`;
      }
      while (basketballplayerplaceholderW.length > 4) {
          let arrowe = 3.0;
          let hooksU = String.fromCharCode(104,95,56,95,120,118,97,103,0);
          let arrowf = 3;
          let zhengpian4 = 2.0;
          let typingJ = 5;
         basketballplayerplaceholderW += "2";
         arrowe *= parseFloat(`${parseInt(`${zhengpian4}`) >> (Math.min(1, Math.abs(typingJ)))}`);
         hooksU += `${3 * parseInt(`${arrowe}`)}`;
         arrowf /= Math.max(1 - parseInt(`${zhengpian4}`), 4);
         typingJ += arrowf >> (Math.min(hooksU.length, 3));
         break;
      }
         moviesF = `${(teamw == String.fromCharCode(80,0) ? teamw.length : basketballplayerplaceholderW.length)}`;
      while (suggestionP.length <= attributedstringw.length) {
          let settingj = 1.0;
          let eyeopen9 = String.fromCharCode(115,117,98,118,105,101,119,95,120,95,56,50,0);
         attributedstringw.push(basketballplayerplaceholderW.length);
         settingj /= Math.max(3, parseInt(`${settingj}`) % (Math.max(1, eyeopen9.length)));
         eyeopen9 += `${3 - parseInt(`${settingj}`)}`;
         break;
      }
      let libpangleflipped1 = 5239790 <= redscoreballQ.length;
      do {
          let dependencyt = String.fromCharCode(112,103,109,120,95,113,95,52,53,0);
         redscoreballQ.push(((libruntimeexecutorG ? 1 : 2)));
         dependencyt = `${dependencyt.length % (Math.max(3, 2))}`;
         if (libpangleflipped1) {
            break;
         }
      } while (libpangleflipped1 && (1 > (redscoreballQ.length % 3) || 3 > redscoreballQ.length));
         libruntimeexecutorG = (basketballplayerplaceholderW.length + redscoreballQ.length) > 7;
      let agreement8 = attributedstringw.length >= 9864346;
      do {
         attributedstringw = [attributedstringw.length];
         if (agreement8) {
            break;
         }
      } while ((teamw.includes(`${attributedstringw.length}`)) && agreement8);
         moviesF = `${attributedstringw.length / 3}`;
         teamw += `${((libruntimeexecutorG ? 5 : 1) & moviesF.length)}`;
      baiduadsb = `${basketballplayerplaceholderW.length}`;
   }
      configC += `${baiduadsb.length}`;
    

   while (!baiduadsb.endsWith(`${yinit_5w7.length}`)) {
      yinit_5w7.push(1);
      break;
   }
   for (let c = 0; c < 1; c++) {
       let leakcheckerI = true;
       let reddownarrowQ = String.fromCharCode(100,115,116,95,105,95,54,57,0);
      let typesh = leakcheckerI ? !leakcheckerI : leakcheckerI;
      do {
          let libswscaleC = 2.0;
          let baiduadsX = false;
          let expande: Array<any> = [String.fromCharCode(118,95,49,54,95,115,116,114,108,0), String.fromCharCode(97,117,116,104,111,114,105,122,97,116,105,111,110,115,95,108,95,49,55,0)];
          let contextf = 2;
          let controls7 = 2;
         leakcheckerI = expande.length < 24;
         libswscaleC /= Math.max(2, parseFloat(`${controls7 / (Math.max(1, contextf))}`));
         baiduadsX = 40 < contextf || !baiduadsX;
         expande.push(1);
         controls7 /= Math.max(parseInt(`${libswscaleC}`) / 2, 2);
         if (typesh) {
            break;
         }
      } while (typesh && (leakcheckerI));
         reddownarrowQ = `${reddownarrowQ.length | 3}`;
      if (!leakcheckerI && reddownarrowQ.length <= 1) {
         reddownarrowQ = `${reddownarrowQ.length}`;
      }
      for (let a = 0; a < 2; a++) {
          let libcrashsdk3 = String.fromCharCode(104,95,50,53,95,101,110,99,111,100,101,114,115,0);
          let middleZ: Map<any, any> = new Map([[String.fromCharCode(103,101,111,98,116,97,103,95,105,95,52,50,0),247], [String.fromCharCode(121,95,57,55,95,97,110,110,111,116,97,116,105,111,110,0),251], [String.fromCharCode(104,95,54,57,95,99,97,110,99,101,108,0),650]]);
          let dicelogob: Map<any, any> = new Map([[String.fromCharCode(109,111,100,101,115,95,99,95,48,0),875], [String.fromCharCode(110,95,57,52,95,109,117,108,116,105,99,97,115,116,101,100,0),463]]);
         reddownarrowQ += `${libcrashsdk3.length}`;
         libcrashsdk3 += "3";
         middleZ = new Map([[`${dicelogob.size}`, middleZ.size / (Math.max(3, 1))]]);
         dicelogob.set(`${dicelogob.size}`, dicelogob.size);
      }
      if (reddownarrowQ.length >= 4 || leakcheckerI) {
          let contextO = String.fromCharCode(111,102,102,101,114,95,106,95,49,51,0);
          let rewardv = String.fromCharCode(109,111,100,105,102,105,99,97,116,105,111,110,95,113,95,49,52,0);
          let network_ = 1;
          let iconqqZ = 5.0;
          let singaporeo = String.fromCharCode(105,95,50,52,95,117,110,118,111,116,101,0);
         reddownarrowQ = `${3 | singaporeo.length}`;
         contextO = `${network_ >> (Math.min(Math.abs(3), 3))}`;
         rewardv += "1";
         network_ %= Math.max(1, network_);
         iconqqZ *= 3;
         singaporeo += "1";
      }
          let reactnativeratingsa = 0.0;
          let trashM = 1;
         leakcheckerI = String.fromCharCode(107,0) == reddownarrowQ;
         reactnativeratingsa += 1;
         trashM -= trashM;
      configC += `${user5.length}`;
   }
   for (let h = 0; h < 1; h++) {
      yinit_5w7 = [2];
   }
    

       let spinnerW = 2.0;
          let libcxxcomponentsb = false;
          let tooltipsc = String.fromCharCode(116,95,57,53,95,97,112,112,118,101,121,111,114,0);
          let types7 = String.fromCharCode(119,95,52,57,95,100,101,116,101,99,116,99,108,111,115,101,0);
         spinnerW -= parseFloat(`${types7.length}`);
         libcxxcomponentsb = (5 < (tooltipsc.length << (Math.min(4, Math.abs((libcxxcomponentsb ? tooltipsc.length : 5))))));
         types7 = `${(tooltipsc == String.fromCharCode(66,0) ? (libcxxcomponentsb ? 2 : 5) : tooltipsc.length)}`;
       let libmapbufferjniz: Array<any> = [String.fromCharCode(100,95,54,57,95,112,97,114,115,101,100,0), String.fromCharCode(115,116,114,105,107,101,95,114,95,57,54,0)];
      while ((libmapbufferjniz.length / 5) < 4 && 4.100 < (parseFloat(`${libmapbufferjniz.length}`) + spinnerW)) {
         libmapbufferjniz = [libmapbufferjniz.length ^ parseInt(`${spinnerW}`)];
         break;
      }
      baiduadsb += `${parseInt(`${spinnerW}`) * 3}`;
   while (4 <= baiduadsb.length || configC.length <= 4) {
      configC = `${2 >> (Math.min(4, configC.length))}`;
      break;
   }
   if ((iconbellactiveE.length / (Math.max(configC.length, 7))) >= 1 || 4 >= (configC.length / (Math.max(1, 2)))) {
      configC = `${3 % (Math.max(3, user5.length))}`;
   }
    

      yinit_5w7 = [(String.fromCharCode(109,0) == baiduadsb ? baiduadsb.length : configC.length)];
   if (5 >= configC.length) {
      yinit_5w7.push((String.fromCharCode(57,0) == configC ? configC.length : footballtrophyy.length));
   }
      left2 = `${footballtrophyy.length}`;
    

   let statisticsinactiveX = 5446724 >= headerG.length;
   do {
      headerG = `${footballtrophyy.length % 1}`;
      if (statisticsinactiveX) {
         break;
      }
   } while (statisticsinactiveX && (user5 == headerG));
   for (let k = 0; k < 3; k++) {
       let controlx = String.fromCharCode(113,95,52,48,95,110,99,111,110,102,0);
       let mbsplash6 = 2;
       let overy = false;
      while (4 <= mbsplash6) {
          let final_qpY: Array<any> = [432, 646];
          let defaultprofilepicF = true;
          let placeholderG = 5.0;
         overy = String.fromCharCode(117,0) == controlx;
         final_qpY.push(parseInt(`${placeholderG}`) * 2);
         defaultprofilepicF = final_qpY.length == 80;
         placeholderG /= Math.max(parseFloat(`${parseInt(`${placeholderG}`) - 3}`), 3);
         break;
      }
      let arrowF = String.fromCharCode(115,106,109,95,115,53,95,105,108,0) == controlx;
      do {
         controlx = `${(controlx.length * (overy ? 1 : 3))}`;
         if (arrowF) {
            break;
         }
      } while (arrowF && (controlx.startsWith(`${overy}`)));
      while (1 < (mbsplash6 / (Math.max(1, 7))) && (controlx.length / 1) < 5) {
          let turndownE = 0;
          let floatinge = true;
         mbsplash6 += mbsplash6;
         turndownE ^= (turndownE | (floatinge ? 3 : 3));
         floatinge = 35 <= turndownE;
         break;
      }
      while (1 < (5 / (Math.max(4, mbsplash6)))) {
         overy = controlx == String.fromCharCode(108,0) && mbsplash6 <= 32;
         break;
      }
         overy = !overy;
          let mode_ = String.fromCharCode(101,118,101,114,121,98,111,100,121,95,99,95,50,54,0);
          let reminderW = String.fromCharCode(101,95,53,53,95,102,97,110,111,117,116,0);
         mbsplash6 &= 1;
         mode_ += `${mode_.length}`;
         reminderW = `${2 ^ mode_.length}`;
       let backwhiteX = String.fromCharCode(108,95,55,55,95,116,105,99,107,0);
         controlx += `${mbsplash6}`;
         overy = controlx.startsWith(`${overy}`);
      baiduadsb += `${baiduadsb.length}`;
   }
   for (let v = 0; v < 2; v++) {
      configC = `${iconbellactiveE.length}`;
   }


    

   if (2 > iconbellactiveE.length) {
      iconbellactiveE.push(footballtrophyy.length << (Math.min(user5.length, 1)));
   }
      baiduadsb = `${3 & footballtrophyy.length}`;
    wawa_dacccfaabfbcbadeebddcabacdffdbc_video.sportDetailsVipPopupClicksAnalytics('pay');
    
  }, [])

  const handleOnInvite = useCallback(() => {
       let libcrashsdk3: Map<any, any> = new Map([[String.fromCharCode(112,104,111,116,111,95,105,95,52,52,0),String.fromCharCode(114,108,111,116,116,105,101,95,100,95,55,55,0)], [String.fromCharCode(109,95,56,50,95,112,114,111,120,105,101,115,0),String.fromCharCode(115,116,97,99,107,101,100,95,99,95,57,49,0)], [String.fromCharCode(98,95,50,49,95,111,112,116,103,114,111,117,112,0),String.fromCharCode(104,119,109,97,112,95,108,95,52,49,0)]]);
    let catalogS = 2.0;
    let fileR = String.fromCharCode(119,95,50,53,95,114,101,116,97,105,110,101,100,0);
    let redcirclebge = String.fromCharCode(100,114,105,102,116,95,56,95,50,56,0);
    let userb = 3;
    let securityD: Array<any> = [314, 801, 639];
    let videom = 3.0;
    let calendarM = 3;
    let cornerw = 1.0;
    let footballfieldu = 0.0;
    let watchs: Map<any, any> = new Map([[String.fromCharCode(100,95,51,95,116,101,120,105,112,111,100,0),993], [String.fromCharCode(119,95,54,48,95,116,114,97,110,115,108,105,116,101,114,97,116,101,100,0),617], [String.fromCharCode(110,95,50,52,95,101,103,114,101,115,115,0),689]]);
    let basketballtrophyT = String.fromCharCode(122,95,54,56,95,115,101,113,118,105,100,101,111,0);
    let logo5 = 3.0;
    let circleh: Map<any, any> = new Map([[String.fromCharCode(106,95,55,49,95,114,111,117,110,100,0),725], [String.fromCharCode(110,95,49,48,48,95,102,97,108,108,111,102,102,0),842]]);
   let alertN = 5833208 >= securityD.length;
   do {
      securityD.push(2 / (Math.max(7, fileR.length)));
      if (alertN) {
         break;
      }
   } while (alertN && ((securityD.length << (Math.min(fileR.length, 3))) < 2 || 3 < (2 << (Math.min(2, securityD.length)))));
      footballfieldu *= calendarM & 1;
   let notificationc = String.fromCharCode(99,110,120,122,0) == fileR;
   do {
      fileR = `${(fileR == String.fromCharCode(115,0) ? parseInt(`${catalogS}`) : fileR.length)}`;
      if (notificationc) {
         break;
      }
   } while (notificationc && (userb > fileR.length));
   let reactnativeultimatelistviewO = securityD.length <= 9160842;
   do {
      securityD = [parseInt(`${cornerw}`)];
      if (reactnativeultimatelistviewO) {
         break;
      }
   } while (reactnativeultimatelistviewO && (1 < (4 ^ fileR.length)));
   while (5 >= (parseInt(`${footballfieldu}`) - redcirclebge.length)) {
      footballfieldu -= parseInt(`${videom}`);
      break;
   }
      cornerw *= parseFloat(`${3}`);

    onClose({ isAutoClose: true })
    navigator.navigate('邀请');

       let mbbido: Map<any, any> = new Map([[String.fromCharCode(107,95,55,54,95,115,99,114,97,112,101,0),false ], [String.fromCharCode(112,95,54,50,95,102,105,108,116,101,114,98,97,110,107,0),false ], [String.fromCharCode(115,117,112,101,114,110,111,100,101,115,95,110,95,52,57,0),true ]]);
       let applei = String.fromCharCode(115,110,97,112,104,111,116,95,115,95,56,49,0);
       let modald = 0.0;
         applei += `${1 & applei.length}`;
          let onewinterstitialN = String.fromCharCode(99,95,54,56,95,112,117,98,108,105,115,104,97,98,108,101,0);
         modald += onewinterstitialN.length;
         applei += "2";
      while (mbbido.get(`${modald}`) == null) {
         mbbido = new Map([[`${modald}`, parseInt(`${modald}`) | applei.length]]);
         break;
      }
       let qaag5 = 5.0;
         modald /= Math.max(mbbido.size * 1, 5);
          let phoneshareK = 5.0;
          let utils4 = true;
          let stepG: Array<any> = [147, 148];
         applei += `${parseInt(`${modald}`) >> (Math.min(2, Math.abs(3)))}`;
         phoneshareK *= (parseInt(`${phoneshareK}`) >> (Math.min(3, Math.abs((utils4 ? 4 : 4)))));
         utils4 = 35.46 >= phoneshareK;
         stepG.push(stepG.length);
      let libavcodec7 = modald <= 9475323.0;
      do {
         modald *= (applei == String.fromCharCode(66,0) ? applei.length : parseInt(`${qaag5}`));
         if (libavcodec7) {
            break;
         }
      } while (libavcodec7 && (mbbido.get(`${modald}`) == null));
         qaag5 /= Math.max(1, mbbido.size + 3);
      libcrashsdk3 = new Map([[redcirclebge, fileR.length | redcirclebge.length]]);
   if ((2.36 * footballfieldu) <= 4.87 || 4 <= (2 * securityD.length)) {
      securityD.push(3);
   }
      securityD = [3 >> (Math.min(5, Math.abs(watchs.size)))];
      userb *= 2;
   if (!Array.from(watchs.values()).includes(libcrashsdk3.size)) {
       let defaultteamU = 2.0;
       let middle1 = String.fromCharCode(97,119,97,114,101,95,105,95,49,0);
       let whiteanimationlivew = true;
       let showR: Map<any, any> = new Map([[String.fromCharCode(115,105,103,118,101,114,95,51,95,54,50,0),793], [String.fromCharCode(114,101,102,101,114,101,110,101,95,108,95,56,57,0),865], [String.fromCharCode(100,101,109,97,110,103,108,101,95,54,95,57,53,0),91]]);
          let fieldh = String.fromCharCode(101,95,54,48,95,115,116,114,101,101,116,0);
          let moviesR = 1.0;
          let armvao = true;
         middle1 += `${2 * showR.size}`;
         fieldh += `${parseInt(`${moviesR}`) >> (Math.min(5, Math.abs(1)))}`;
         moviesR += parseFloat(`${parseInt(`${moviesR}`) - 3}`);
         armvao = (parseInt(`${moviesR}`) - fieldh.length) <= 16;
      if (!middle1.startsWith(`${whiteanimationlivew}`)) {
         middle1 += `${2 | showR.size}`;
      }
      let components_ = 6561366 <= middle1.length;
      do {
          let y_countC: Map<any, any> = new Map([[String.fromCharCode(114,97,119,95,101,95,55,49,0),415], [String.fromCharCode(116,114,97,110,115,108,97,116,105,111,110,95,109,95,55,50,0),720]]);
          let analyticG = true;
          let typesO = 5.0;
          let huaweiA = true;
         middle1 = `${(parseInt(`${typesO}`) >> (Math.min(1, Math.abs((whiteanimationlivew ? 5 : 5)))))}`;
         y_countC = new Map([[`${analyticG}`, 2]]);
         typesO *= parseFloat(`${2}`);
         if (components_) {
            break;
         }
      } while ((1 <= (4 + middle1.length)) && components_);
         defaultteamU /= Math.max(5, middle1.length);
      if ((1 ^ showR.size) == 2) {
         showR = new Map([[`${showR.size}`, ((whiteanimationlivew ? 5 : 5) * showR.size)]]);
      }
      for (let g = 0; g < 1; g++) {
         middle1 += `${middle1.length >> (Math.min(5, Math.abs(parseInt(`${defaultteamU}`))))}`;
      }
      if (5 == (showR.size << (Math.min(Math.abs(2), 2)))) {
         whiteanimationlivew = !whiteanimationlivew;
      }
         whiteanimationlivew = !whiteanimationlivew;
      for (let b = 0; b < 3; b++) {
         middle1 = `${showR.size}`;
      }
         middle1 = `${((whiteanimationlivew ? 3 : 2) >> (Math.min(Math.abs(showR.size), 1)))}`;
         defaultteamU /= Math.max(4, showR.size);
         defaultteamU *= 1 * middle1.length;
      watchs = new Map([[`${videom}`, 1 + parseInt(`${defaultteamU}`)]]);
   }
      calendarM |= securityD.length;

    

      securityD = [3 / (Math.max(2, parseInt(`${footballfieldu}`)))];
   for (let x = 0; x < 3; x++) {
       let libaneg: Array<any> = [539, 835, 22];
       let carouselY: Array<any> = [142, 150, 532];
       let binddatas1 = String.fromCharCode(116,95,50,57,95,102,111,108,108,111,119,101,114,115,0);
       let whistleorangeI: Map<any, any> = new Map([[String.fromCharCode(118,95,49,56,95,112,114,111,103,114,97,109,109,97,116,105,99,97,108,108,121,0),false ], [String.fromCharCode(106,95,51,54,95,116,98,109,108,0),false ]]);
       let bggradientx = String.fromCharCode(111,98,115,101,114,118,97,116,105,111,110,95,99,95,50,51,0);
         whistleorangeI.set(binddatas1, carouselY.length ^ 3);
         whistleorangeI.set(`${libaneg.length}`, whistleorangeI.size);
          let giflivestreaming9 = true;
          let thailandd = true;
          let list8 = String.fromCharCode(109,112,101,103,95,57,95,57,50,0);
         carouselY.push(1);
         giflivestreaming9 = list8.length <= 92 && !giflivestreaming9;
         thailandd = giflivestreaming9;
         list8 = `${list8.length}`;
         libaneg.push(whistleorangeI.size);
         whistleorangeI = new Map([[`${whistleorangeI.size}`, 2]]);
       let selectede = 2;
       let inactiveu = 1;
       let configure4 = 3.0;
       let videobufferloading4 = 0.0;
         inactiveu -= 3;
      if ((4 | bggradientx.length) <= 4 || (4 | bggradientx.length) <= 4) {
         carouselY.push(bggradientx.length);
      }
      for (let m = 0; m < 2; m++) {
          let launcherC = String.fromCharCode(99,97,110,99,101,108,108,97,98,108,101,95,104,95,57,56,0);
          let qaag_ = String.fromCharCode(111,117,116,108,105,101,114,95,54,95,57,51,0);
          let iconadslinkK = 3.0;
          let close_ = String.fromCharCode(106,95,54,53,95,110,111,116,105,102,105,99,97,116,105,111,110,0);
         videobufferloading4 /= Math.max(whistleorangeI.size ^ 1, 3);
         launcherC = `${parseInt(`${iconadslinkK}`)}`;
         qaag_ = "3";
         iconadslinkK += close_.length;
         close_ = `${launcherC.length}`;
      }
      let greenV = 7099502 >= bggradientx.length;
      do {
          let dacccfaabfbcbadeebddcabacdffdbJ: Array<any> = [127, 878];
          let constantsW = 2.0;
          let episodesD = false;
          let coree = 3.0;
          let searchbar6 = 2.0;
         bggradientx += `${parseInt(`${coree}`)}`;
         dacccfaabfbcbadeebddcabacdffdbJ.push(parseInt(`${constantsW}`) + 3);
         constantsW *= 3 | parseInt(`${constantsW}`);
         episodesD = constantsW == 7.45;
         coree *= (3 >> (Math.min(2, Math.abs((episodesD ? 4 : 2)))));
         searchbar6 += parseFloat(`${1}`);
         if (greenV) {
            break;
         }
      } while ((2 == (bggradientx.length >> (Math.min(Math.abs(2), 5))) && (configure4 + 4.3) == 1.45) && greenV);
         configure4 -= parseInt(`${configure4}`);
      for (let p = 0; p < 3; p++) {
         whistleorangeI = new Map([[`${libaneg.length}`, libaneg.length]]);
      }
      for (let d = 0; d < 1; d++) {
         inactiveu <<= Math.min(3, Math.abs(parseInt(`${videobufferloading4}`) - inactiveu));
      }
      if ((selectede << (Math.min(Math.abs(whistleorangeI.size), 1))) == 2) {
          let sendl = String.fromCharCode(104,95,48,95,114,101,117,115,97,98,108,101,0);
          let imagesq = String.fromCharCode(109,95,49,55,95,111,98,106,99,0);
         selectede += inactiveu;
         sendl = `${imagesq.length}`;
         imagesq += `${sendl.length % 1}`;
      }
      securityD = [(bggradientx == String.fromCharCode(57,0) ? bggradientx.length : parseInt(`${videom}`))];
   }
      basketballtrophyT = `${parseInt(`${footballfieldu}`) << (Math.min(4, Math.abs(1)))}`;
       let assistG = String.fromCharCode(111,114,105,101,110,116,97,116,105,111,110,95,99,95,56,57,0);
      let orientationj = assistG == String.fromCharCode(51,113,115,97,51,112,120,56,0);
      do {
         assistG += `${assistG.length % 3}`;
         if (orientationj) {
            break;
         }
      } while (orientationj && (assistG.length == 3));
      for (let v = 0; v < 2; v++) {
          let iconpipshrinkX = 2;
          let arrowdownR = 4.0;
          let airbnbstarselectedD: Map<any, any> = new Map([[String.fromCharCode(101,100,105,116,111,114,95,99,95,56,50,0),918], [String.fromCharCode(105,118,97,114,95,52,95,49,48,48,0),146]]);
         assistG = `${2 + assistG.length}`;
         iconpipshrinkX /= Math.max(iconpipshrinkX, 4);
         arrowdownR *= parseFloat(`${iconpipshrinkX}`);
         airbnbstarselectedD = new Map([[`${airbnbstarselectedD.size}`, airbnbstarselectedD.size * parseInt(`${arrowdownR}`)]]);
      }
      for (let k = 0; k < 3; k++) {
         assistG += "3";
      }
      footballfieldu += (redcirclebge == String.fromCharCode(69,0) ? userb : redcirclebge.length);
      libcrashsdk3 = new Map([[redcirclebge, parseInt(`${cornerw}`) + 2]]);
      videom *= parseFloat(`${1 ^ watchs.size}`);
    wawa_dacccfaabfbcbadeebddcabacdffdbc_video.sportDetailsVipPopupClicksAnalytics('invite');
    

  }, [])


  return (

    <VipPrivilegeModal
      titleText={"VIP升级权益"}
      benefitsTextsArray={['高清体育赛事  零时差感受赛场激情', '海量福利视频 你想要的这里都有', '高清画质 无广告观影体验']}
      imageGradientColors={['#0E5ED800', '#0B6AE4']}
      contentGradientColors={['#0B6CE8', '#001A74']}
      coverBackground={sportBg}
      coverImage={sportModels}
      onPurchase={handleOnPurchase}
      onInvite={handleOnInvite}
      onClose={onClose}
      showBlur={showBlur}
      showCondition={showCondition}
      showDarkBackdrop={true}
    />

  )
}