import { useNavigation } from "@react-navigation/native";
import VipPrivilegeModal from "./mayi_submit_iconwechat"
import { useCallback } from "react";
import { useAppDispatch, useAppSelector, useSelector } from "@hooks/mayi_redirect";
import { screenModel } from "@type/mayi_libfolly_inouttargetred";
import { UMENG_CHANNEL } from "@utility/mayi_middleware_apps";
import { mayi_Gift } from "@models/mayi_libjsinspector";
import { showLoginAction } from "@redux/actions/mayi_iconorientation_chatroombackground";
import { mayi_Baseline } from "@redux/reducers/mayi_chatroombackground_unlock";

const commonModels = require('@static/images/iconclosewhitewithbgGoogleVideo.png');
const sportBg = require('@static/images/firebaseCancelLang.png');

interface mayi_GoogleViews {
  showCondition: boolean;
  onClose: any;
  showBlur?: boolean;
}

export const CommonVipPrivilegeOverlay = ({ showCondition, onClose, showBlur }: mayi_GoogleViews) => {

  const navigator = useNavigation()
  const dispatch = useAppDispatch()
  const userState = useSelector<mayi_Baseline>('userReducer');

  const handleOnPurchase = useCallback(() => {
       let benefitR: Array<any> = [722, 948];
    let left0 = 5.0;
    let icondefaultthumbnailL = String.fromCharCode(108,111,99,97,108,105,122,101,100,95,104,95,51,56,0);
    let kickW: Array<any> = [828, 81, 977];
    let sendw = true;
    let viewsb = String.fromCharCode(116,114,97,110,115,108,97,116,101,100,95,120,95,49,57,0);
    let embedf = String.fromCharCode(113,95,50,56,95,115,119,105,99,104,0);
    let appsv = 5.0;
    let rewindd = 2.0;
    let audience_ = 5;
    let main_x1 = String.fromCharCode(104,95,54,55,95,114,101,98,117,105,108,100,0);
    let whistleorangeM = 3.0;
    let layoutj: Array<any> = [979, 642];
      embedf += `${2 - kickW.length}`;
      embedf += `${1 - parseInt(`${rewindd}`)}`;
       let t_titlea: Map<any, any> = new Map([[String.fromCharCode(116,99,102,105,108,101,95,107,95,53,49,0),String.fromCharCode(109,111,100,101,114,110,95,110,95,50,53,0)], [String.fromCharCode(114,101,112,95,97,95,53,50,0),String.fromCharCode(98,95,49,53,95,99,97,108,99,117,108,97,116,101,0)]]);
       let pointb = String.fromCharCode(121,95,50,49,95,121,117,118,112,116,111,117,121,118,121,0);
      let contexth = 5676415 >= t_titlea.size;
      do {
         t_titlea = new Map([[`${t_titlea.size}`, t_titlea.size >> (Math.min(pointb.length, 5))]]);
         if (contexth) {
            break;
         }
      } while (((t_titlea.size / (Math.max(pointb.length, 5))) > 4) && contexth);
          let modec = String.fromCharCode(115,95,57,49,95,112,114,101,100,105,99,116,111,114,115,0);
          let searchbarh = 0.0;
         t_titlea.set(pointb, modec.length + 2);
         modec = `${parseInt(`${searchbarh}`) >> (Math.min(Math.abs(parseInt(`${searchbarh}`)), 5))}`;
          let bridger = String.fromCharCode(98,97,116,99,104,101,115,95,106,95,55,52,0);
         t_titlea.set(`${pointb}`, t_titlea.size ^ 2);
         bridger += `${bridger.length + 1}`;
      let pingH = 7155381 <= t_titlea.size;
      do {
         t_titlea.set(`${pointb}`, pointb.length);
         if (pingH) {
            break;
         }
      } while ((!pointb.includes(`${t_titlea.size}`)) && pingH);
      if ((3 | pointb.length) >= 1 || 3 >= (t_titlea.size | 3)) {
          let anners = String.fromCharCode(117,114,105,95,98,95,56,55,0);
          let indicatore = 0;
         t_titlea.set(pointb, indicatore);
         anners += "1";
         indicatore *= anners.length % 2;
      }
         t_titlea.set(pointb, pointb.length << (Math.min(3, Math.abs(t_titlea.size))));
      benefitR = [main_x1.length];
      kickW.push(((sendw ? 3 : 2) << (Math.min(Math.abs(parseInt(`${appsv}`)), 2))));
   for (let p = 0; p < 2; p++) {
      viewsb += `${(String.fromCharCode(65,0) == embedf ? embedf.length : audience_)}`;
   }

    onClose()

       let privacyO = String.fromCharCode(99,95,49,52,95,114,102,102,116,102,0);
         privacyO = `${privacyO.length}`;
      let filledY = privacyO.length >= 7287501;
      do {
          let bufferH: Array<any> = [403, 80];
          let headero: Array<any> = [String.fromCharCode(114,101,97,100,101,114,95,119,95,50,55,0), String.fromCharCode(115,117,98,116,105,116,108,101,115,95,100,95,53,49,0), String.fromCharCode(108,95,55,49,95,98,97,116,116,101,114,121,0)];
          let shirtG = String.fromCharCode(121,95,57,57,95,112,114,105,110,116,99,111,109,112,0);
          let abidetectA: Map<any, any> = new Map([[String.fromCharCode(99,95,54,57,95,120,107,101,101,112,0),383], [String.fromCharCode(112,102,105,108,116,101,114,95,51,95,49,51,0),829]]);
          let kuaishout = 1.0;
         privacyO += `${(shirtG == String.fromCharCode(83,0) ? bufferH.length : shirtG.length)}`;
         bufferH.push(1);
         headero.push(1 | headero.length);
         abidetectA.set(`${headero.length}`, headero.length / 3);
         kuaishout -= parseFloat(`${parseInt(`${kuaishout}`)}`);
         if (filledY) {
            break;
         }
      } while ((!privacyO.includes(`${privacyO.length}`)) && filledY);
      let merger5 = String.fromCharCode(51,109,48,99,115,97,0) == privacyO;
      do {
         privacyO += `${2 ^ privacyO.length}`;
         if (merger5) {
            break;
         }
      } while (merger5 && (privacyO.length < 5));
      icondefaultthumbnailL += `${privacyO.length}`;
       let gpay3 = 3.0;
       let basketballtrophy2 = String.fromCharCode(109,99,111,109,112,97,110,100,95,107,95,51,55,0);
       let adulte: Array<any> = [284, 582, 740];
      let clubn = basketballtrophy2 == String.fromCharCode(95,104,110,0);
      do {
         basketballtrophy2 = `${parseInt(`${gpay3}`) / (Math.max(1, adulte.length))}`;
         if (clubn) {
            break;
         }
      } while (clubn && (adulte.length >= 4));
         basketballtrophy2 += "1";
      for (let j = 0; j < 3; j++) {
         gpay3 += 1;
      }
         adulte = [parseInt(`${gpay3}`)];
         adulte.push(basketballtrophy2.length >> (Math.min(4, Math.abs(parseInt(`${gpay3}`)))));
         gpay3 /= Math.max(1, adulte.length / 3);
      while (2.75 < (gpay3 + 2.2)) {
         basketballtrophy2 = `${basketballtrophy2.length ^ 1}`;
         break;
      }
      if (5 < (basketballtrophy2.length << (Math.min(3, adulte.length)))) {
         adulte.push(parseInt(`${gpay3}`) & 3);
      }
       let leakcheckero = String.fromCharCode(112,97,115,115,102,98,95,112,95,51,57,0);
       let mailw = String.fromCharCode(108,111,97,115,95,119,95,49,56,0);
      appsv += viewsb.length | 3;
      viewsb += `${((sendw ? 2 : 4) - audience_)}`;
   while ((benefitR.length & 2) == 3 && benefitR.length == 2) {
      sendw = (parseInt(`${appsv}`) * embedf.length) <= 100;
      break;
   }
   if ((left0 * 1.67) > 2.40 || 2 > (1 ^ audience_)) {
      audience_ |= benefitR.length / (Math.max(icondefaultthumbnailL.length, 4));
   }
    navigator.navigate("付费VIP");
    
    
    
    
    
    
    
    
  }, [])

  const handleOnInvite = useCallback(() => {
       let resultu = 0.0;
    let settingsI = String.fromCharCode(108,95,54,53,95,99,111,109,112,97,114,101,0);
    let downJ = 4;
    let modityj = true;
    let actionG = 5.0;
    let nbatrophyt: Map<any, any> = new Map([[String.fromCharCode(101,116,104,101,114,115,99,97,110,95,105,95,52,54,0),646], [String.fromCharCode(120,95,52,56,95,105,110,116,101,103,101,114,0),756], [String.fromCharCode(120,95,54,56,95,116,101,108,101,103,114,97,109,0),673]]);
    let clearF: Array<any> = [938, 622, 527];
    let regengr: Array<any> = [849, 796];
    let iconsubssuccess4 = 0.0;
      clearF = [1 + parseInt(`${actionG}`)];
   let movies9 = modityj ? !modityj : modityj;
   do {
       let referrerT = 5;
       let greyticks = String.fromCharCode(98,95,57,49,95,102,111,117,114,99,99,115,0);
       let libswresample7: Map<any, any> = new Map([[String.fromCharCode(115,105,103,104,95,105,95,53,48,0),455], [String.fromCharCode(110,95,51,50,95,101,118,97,108,102,117,110,99,0),842], [String.fromCharCode(105,95,55,50,95,110,112,112,115,99,97,108,101,0),427]]);
       let redscoreballc: Array<any> = [467, 940, 681];
      while (libswresample7.size < 3) {
         libswresample7.set(`${referrerT}`, referrerT);
         break;
      }
      if ((3 ^ redscoreballc.length) > 1 || 3 > (libswresample7.size ^ redscoreballc.length)) {
          let search1 = false;
         redscoreballc.push(2);
         search1 = (!search1 ? search1 : search1);
      }
      while ((greyticks.length << (Math.min(1, Math.abs(libswresample7.size)))) >= 5 || 5 >= (libswresample7.size << (Math.min(greyticks.length, 5)))) {
         greyticks = `${redscoreballc.length}`;
         break;
      }
         referrerT <<= Math.min(3, Math.abs(referrerT + libswresample7.size));
          let baselinej = String.fromCharCode(114,108,111,116,116,105,101,99,111,109,109,111,110,95,56,95,49,48,0);
          let t_titleV = String.fromCharCode(111,95,50,57,95,115,117,98,116,121,112,101,115,0);
          let dialogw = 1;
         libswresample7.set(`${redscoreballc.length}`, redscoreballc.length);
         baselinej = `${2 ^ dialogw}`;
         t_titleV = `${3 << (Math.min(1, baselinej.length))}`;
         dialogw *= t_titleV.length;
         referrerT += referrerT;
      if ((2 & referrerT) < 5 || (redscoreballc.length & referrerT) < 2) {
         referrerT /= Math.max(libswresample7.size % (Math.max(6, redscoreballc.length)), 2);
      }
         referrerT -= 3 + redscoreballc.length;
         greyticks += `${greyticks.length ^ 3}`;
         redscoreballc.push(1);
       let update_hg: Array<any> = [831, 365, 887];
       let hometeamfield2: Array<any> = [117, 509, 91];
      let leakcheckerb = 7755109 <= redscoreballc.length;
      do {
         redscoreballc.push(referrerT);
         if (leakcheckerb) {
            break;
         }
      } while (leakcheckerb && (2 < (redscoreballc.length / 3)));
      modityj = downJ < 75;
      if (movies9) {
         break;
      }
   } while ((!modityj) && movies9);
      modityj = (resultu / (Math.max(parseFloat(`${settingsI.length}`), 9))) < 24.83;
       let googlex = 2.0;
       let link8 = false;
       let libreactperfloggerjniX = false;
         libreactperfloggerjniX = link8;
      while (3.94 > (4.2 + googlex) && googlex > 4.2) {
         googlex += parseInt(`${googlex}`) + 2;
         break;
      }
         link8 = !link8;
      for (let v = 0; v < 1; v++) {
          let bingL: Map<any, any> = new Map([[String.fromCharCode(115,108,105,99,101,100,95,117,95,57,54,0),762], [String.fromCharCode(120,95,53,57,95,99,108,101,97,114,0),427]]);
          let details_ = 5;
          let projectK = String.fromCharCode(118,109,97,102,109,111,116,105,111,110,95,119,95,53,54,0);
          let backwardY = false;
          let promotion2: Map<any, any> = new Map([[String.fromCharCode(99,104,97,112,116,101,114,95,106,95,52,49,0),false ], [String.fromCharCode(115,116,114,105,110,103,115,95,106,95,57,54,0),true ]]);
         libreactperfloggerjniX = 16.84 >= googlex && link8;
         bingL.set(`${details_}`, details_ % 3);
         projectK += `${projectK.length * bingL.size}`;
         backwardY = projectK.length == 44;
         promotion2 = new Map([[`${promotion2.size}`, bingL.size]]);
      }
       let malaysial = String.fromCharCode(105,95,53,95,114,101,112,101,97,116,105,110,103,0);
       let combinedY = String.fromCharCode(103,97,117,103,101,95,108,95,57,54,0);
       let forwardL = String.fromCharCode(118,95,50,52,95,115,101,99,116,105,110,115,0);
         forwardL = "3";
          let mbsplashx: Map<any, any> = new Map([[String.fromCharCode(122,95,54,57,0),402], [String.fromCharCode(112,95,56,48,95,119,97,108,107,105,110,103,0),168]]);
          let libfabricjni7 = String.fromCharCode(105,95,54,55,95,108,105,98,97,118,100,101,118,105,99,101,0);
          let pageO = String.fromCharCode(109,97,120,105,109,105,122,101,100,95,51,95,55,53,0);
         combinedY += `${(libfabricjni7 == String.fromCharCode(51,0) ? libfabricjni7.length : parseInt(`${googlex}`))}`;
         mbsplashx.set(`${pageO}`, 2 ^ mbsplashx.size);
         pageO += `${(String.fromCharCode(114,0) == pageO ? pageO.length : mbsplashx.size)}`;
      while (parseInt(`${googlex}`) < combinedY.length) {
          let stylesw = 5.0;
          let inactiveF = String.fromCharCode(121,95,57,49,0);
          let aboutS: Array<any> = [707, 952];
         googlex /= Math.max(parseInt(`${stylesw}`), 3);
         stylesw -= 2;
         inactiveF += "1";
         aboutS = [inactiveF.length >> (Math.min(4, aboutS.length))];
         break;
      }
      actionG += clearF.length;
   while (clearF.length >= nbatrophyt.size) {
      clearF = [3];
      break;
   }
   for (let d = 0; d < 2; d++) {
      clearF.push((parseInt(`${actionG}`) - (modityj ? 3 : 1)));
   }
      actionG += regengr.length + 1;
      regengr = [3];
   if (settingsI.endsWith(`${clearF.length}`)) {
       let faviconL = String.fromCharCode(121,95,50,49,95,111,112,116,101,100,0);
       let schedulem = 3.0;
       let reactnavigationE: Map<any, any> = new Map([[String.fromCharCode(103,101,111,99,111,100,101,115,95,114,95,54,49,0),262], [String.fromCharCode(114,101,115,112,111,110,100,101,114,95,107,95,57,48,0),596]]);
       let targetc = String.fromCharCode(121,95,50,54,95,101,105,103,104,116,115,118,120,0);
      while (targetc.startsWith(`${reactnavigationE.size}`)) {
          let mathk = 4.0;
          let vipsportF = String.fromCharCode(97,95,49,55,95,109,101,110,116,105,111,110,115,0);
          let phoneshareA = 0.0;
          let sigmob8: Map<any, any> = new Map([[String.fromCharCode(104,117,100,95,115,95,57,50,0),302], [String.fromCharCode(119,105,116,104,100,114,97,119,95,117,95,55,48,0),902]]);
         targetc = "2";
         mathk *= sigmob8.size / 2;
         vipsportF += "3";
         phoneshareA += parseFloat(`${sigmob8.size + parseInt(`${phoneshareA}`)}`);
         break;
      }
         reactnavigationE = new Map([[`${reactnavigationE.size}`, reactnavigationE.size]]);
         reactnavigationE.set(`${schedulem}`, parseInt(`${schedulem}`) + reactnavigationE.size);
       let strings8 = 0;
      if (schedulem < 5.100) {
         strings8 ^= targetc.length | strings8;
      }
       let runtimescheduler2: Array<any> = [544, 934, 25];
         strings8 ^= targetc.length + 3;
      while ((strings8 >> (Math.min(faviconL.length, 3))) > 2 || (strings8 >> (Math.min(faviconL.length, 1))) > 2) {
          let download7 = 3.0;
          let strings1 = String.fromCharCode(116,95,57,51,95,115,116,105,99,107,101,114,0);
          let targetO = String.fromCharCode(119,114,97,112,112,101,100,95,104,95,51,52,0);
          let penaltymatchicont = String.fromCharCode(116,95,49,48,48,95,105,110,103,101,110,105,101,110,116,0);
          let subbasketballplayer_ = true;
         faviconL = `${2 >> (Math.min(2, strings1.length))}`;
         download7 += parseFloat(`${targetO.length}`);
         strings1 = `${targetO.length << (Math.min(Math.abs(1), 1))}`;
         penaltymatchicont = `${penaltymatchicont.length}`;
         subbasketballplayer_ = (((subbasketballplayer_ ? 69 : penaltymatchicont.length) | penaltymatchicont.length) > 69);
         break;
      }
      for (let c = 0; c < 3; c++) {
         targetc = `${strings8}`;
      }
      let langkeyr = 4939387.0 <= schedulem;
      do {
         schedulem *= strings8 << (Math.min(Math.abs(reactnavigationE.size), 5));
         if (langkeyr) {
            break;
         }
      } while (langkeyr && (schedulem > targetc.length));
      if ((strings8 * 2) >= 5 && 5 >= (2 * targetc.length)) {
         targetc += "3";
      }
      for (let i = 0; i < 3; i++) {
          let producti = String.fromCharCode(99,104,97,114,97,99,116,101,114,95,104,95,48,0);
          let qcopy_mwy = String.fromCharCode(120,95,49,49,95,112,105,120,102,109,116,115,0);
          let elements0 = String.fromCharCode(110,111,115,105,109,100,95,116,95,51,50,0);
         reactnavigationE.set(producti, 3 ^ producti.length);
         qcopy_mwy = `${qcopy_mwy.length + 3}`;
         elements0 = `${qcopy_mwy.length}`;
      }
      clearF.push(parseInt(`${actionG}`) & targetc.length);
   }
      resultu /= Math.max(3, parseFloat(`${parseInt(`${actionG}`) % 3}`));

    onClose()

      nbatrophyt = new Map([[`${clearF.length}`, 3]]);
      regengr.push(((modityj ? 2 : 2) + parseInt(`${resultu}`)));
   for (let v = 0; v < 3; v++) {
      settingsI = "2";
   }
   while (downJ < regengr.length) {
      regengr = [parseInt(`${actionG}`)];
      break;
   }
   let libswresampleU = actionG <= 6888851.0;
   do {
       let chart8 = true;
       let libyogax = String.fromCharCode(97,95,49,51,95,115,115,116,104,114,101,115,104,0);
       let audienceY = false;
       let adultS = 2.0;
       let rewind4 = true;
         rewind4 = !rewind4;
       let main_rY: Map<any, any> = new Map([[String.fromCharCode(105,101,101,101,95,116,95,53,53,0),549], [String.fromCharCode(115,95,53,56,95,109,112,101,103,118,108,99,0),228], [String.fromCharCode(116,95,49,48,48,95,111,119,110,108,111,97,100,0),493]]);
         main_rY = new Map([[`${adultS}`, ((audienceY ? 2 : 4) + parseInt(`${adultS}`))]]);
         audienceY = !chart8;
          let recommendation1 = 4.0;
          let playlistT = 4.0;
          let backiconQ = String.fromCharCode(112,97,99,107,101,116,101,100,95,119,95,52,49,0);
         adultS /= Math.max((parseFloat(`${1 & (rewind4 ? 3 : 5)}`)), 3);
         recommendation1 -= parseFloat(`${parseInt(`${recommendation1}`) << (Math.min(3, Math.abs(3)))}`);
         playlistT *= parseInt(`${recommendation1}`) >> (Math.min(Math.abs(parseInt(`${playlistT}`)), 3));
         backiconQ = `${parseInt(`${playlistT}`) * backiconQ.length}`;
      while (4.24 >= (5.81 - adultS)) {
         adultS *= parseFloat(`${1 * parseInt(`${adultS}`)}`);
         break;
      }
      while (rewind4 && (main_rY.size - 2) > 1) {
         rewind4 = libyogax.length >= 78;
         break;
      }
      for (let z = 0; z < 3; z++) {
         chart8 = (57 == ((!chart8 ? libyogax.length : 57) % (Math.max(3, libyogax.length))));
      }
      if (audienceY) {
         chart8 = (((!chart8 ? 21 : libyogax.length) % (Math.max(libyogax.length, 4))) == 21);
      }
          let telemetry3: Map<any, any> = new Map([[String.fromCharCode(99,111,108,111,110,115,95,49,95,50,52,0),String.fromCharCode(100,121,110,108,111,97,100,95,113,95,57,53,0)], [String.fromCharCode(109,95,51,50,95,113,112,101,108,100,115,112,0),String.fromCharCode(116,95,51,57,95,108,111,111,112,105,110,103,0)]]);
          let videocommonm = String.fromCharCode(99,95,52,48,95,102,99,110,116,108,0);
          let roboto6 = 1.0;
         audienceY = main_rY.size >= 63 || !chart8;
         telemetry3 = new Map([[`${telemetry3.size}`, telemetry3.size]]);
         videocommonm += `${1 * videocommonm.length}`;
         roboto6 *= 2;
      for (let m = 0; m < 3; m++) {
         main_rY.set(libyogax, 2);
      }
      if (4 < libyogax.length || chart8) {
         libyogax = `${main_rY.size * parseInt(`${adultS}`)}`;
      }
       let showlessK = 4.0;
       let fullscreenminU = 3.0;
         chart8 = rewind4;
          let ewardedV = String.fromCharCode(105,110,105,116,105,97,116,111,114,95,104,95,52,57,0);
          let downloaderU = String.fromCharCode(119,95,52,55,95,100,105,115,97,112,112,101,97,114,97,110,99,101,0);
          let dycreatorS = String.fromCharCode(115,95,52,52,95,112,97,114,97,109,101,116,101,114,0);
         libyogax = `${((chart8 ? 4 : 2) << (Math.min(Math.abs(parseInt(`${fullscreenminU}`)), 1)))}`;
         ewardedV += `${1 >> (Math.min(5, downloaderU.length))}`;
         downloaderU += `${downloaderU.length}`;
         dycreatorS = `${downloaderU.length}`;
      actionG -= settingsI.length | 1;
      if (libswresampleU) {
         break;
      }
   } while (libswresampleU && (2.90 <= actionG));
      resultu *= parseFloat(`${downJ / (Math.max(5, nbatrophyt.size))}`);
      settingsI += `${1 / (Math.max(4, downJ))}`;
      clearF = [parseInt(`${resultu}`) + parseInt(`${actionG}`)];
   while ((nbatrophyt.size - parseInt(`${actionG}`)) > 2 || (2 >> (Math.min(4, Math.abs(nbatrophyt.size)))) > 1) {
      nbatrophyt.set(`${downJ}`, nbatrophyt.size);
      break;
   }
   let analyticE = modityj ? !modityj : modityj;
   do {
      modityj = (resultu + parseFloat(`${settingsI.length}`)) >= 34.17;
      if (analyticE) {
         break;
      }
   } while ((clearF.length > 4 && 2 > (clearF.length % (Math.max(4, 8)))) && analyticE);
    navigator.navigate('邀请');
  }, [])


  return (

    <VipPrivilegeModal
      titleText={"VIP升级权益"}
      benefitsTextsArray={['高清体育赛事  零时差感受赛场激情', '海量福利视频 你想要的这里都有', '高清画质 无广告观影体验']}
      imageGradientColors={['#D80E0E00', '#E80B0B']}
      contentGradientColors={['#E80B0B', '#740000']}
      coverBackground={sportBg}
      coverImage={commonModels}
      onPurchase={handleOnPurchase}
      onInvite={handleOnInvite}
      onClose={onClose}
      showBlur={showBlur}
      showCondition={showCondition}
      showDarkBackdrop={true}
    />

  )
}