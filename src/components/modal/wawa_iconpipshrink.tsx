import {useNavigation} from '@react-navigation/native';
import {Image, StyleSheet, ViewStyle} from 'react-native';
import {View, Text} from 'react-native';
import {TouchableOpacity} from 'react-native';
import LinearGradient from 'react-native-linear-gradient';
import {Colors} from '../../Sports/global/wawa_armva';

type wawaIconschedule = {
  supportedOrientations?: ('portrait' | 'landscape')[];
  isLoading?: boolean;
  onClose: CallableFunction;
  width?: string | number;
  isLeft?: boolean;
  contentTemplate?: number;
};

export default function VipGuideModal({
  onClose,
  width,
  isLeft = false,
  contentTemplate = 1,
}: wawaIconschedule) {
  const closeModal = () => {
       let temperatureR: Map<any, any> = new Map([[String.fromCharCode(107,109,118,99,95,105,95,56,48,0),false ], [String.fromCharCode(97,108,105,103,110,95,122,95,56,50,0),false ], [String.fromCharCode(117,95,51,53,95,100,101,109,111,0),true ]]);
    let fastforwardZ = String.fromCharCode(105,100,97,115,115,101,116,115,95,104,95,54,55,0);
    let x_hashZ = 2.0;
    let xvodW = true;
    let bgvipxvodo: Map<any, any> = new Map([[String.fromCharCode(100,95,54,57,95,114,101,109,111,118,105,110,103,0),997], [String.fromCharCode(101,114,115,105,111,110,95,56,95,50,57,0),482], [String.fromCharCode(97,95,52,95,117,110,104,105,100,101,0),925]]);
    let iconpipshrink5 = 0.0;
    let watchP: Map<any, any> = new Map([[String.fromCharCode(98,95,53,53,95,112,97,117,115,101,0),279], [String.fromCharCode(108,95,50,49,95,99,111,100,101,99,0),29], [String.fromCharCode(111,98,115,101,114,118,97,116,105,111,110,115,95,50,95,49,57,0),459]]);
    let defaultroombgy = String.fromCharCode(117,95,51,50,95,99,111,112,121,116,111,0);
    let defaultlogoY = false;
    let downloadingF = 4.0;
    let middlesoundq = 4;
      iconpipshrink5 *= parseFloat(`${temperatureR.size}`);
      x_hashZ *= defaultroombgy.length << (Math.min(Math.abs(2), 1));
      xvodW = x_hashZ < 10.97;
   while (fastforwardZ.startsWith(`${iconpipshrink5}`)) {
      fastforwardZ += `${2 - fastforwardZ.length}`;
      break;
   }
      watchP.set(`${xvodW}`, temperatureR.size);
   for (let o = 0; o < 3; o++) {
      iconpipshrink5 /= Math.max(parseFloat(`${bgvipxvodo.size}`), 5);
   }
   for (let k = 0; k < 2; k++) {
       let areag = String.fromCharCode(115,111,99,114,101,97,116,101,95,56,95,49,53,0);
         areag += `${areag.length << (Math.min(Math.abs(1), 2))}`;
          let mintegralA = String.fromCharCode(116,95,56,48,95,112,114,102,116,0);
          let singaporez = 1.0;
         areag = `${mintegralA.length * 2}`;
         mintegralA += "3";
         singaporez -= parseFloat(`${parseInt(`${singaporez}`) >> (Math.min(1, Math.abs(1)))}`);
         areag = `${areag.length}`;
      x_hashZ /= Math.max(2, (defaultroombgy == String.fromCharCode(120,0) ? (xvodW ? 2 : 3) : defaultroombgy.length));
   }
      watchP = new Map([[fastforwardZ, fastforwardZ.length]]);
      x_hashZ -= 3;
      bgvipxvodo = new Map([[fastforwardZ, fastforwardZ.length + parseInt(`${x_hashZ}`)]]);
   for (let r = 0; r < 1; r++) {
      bgvipxvodo = new Map([[`${bgvipxvodo.size}`, 3 + bgvipxvodo.size]]);
   }
   while (iconpipshrink5 == 4.7) {
      iconpipshrink5 *= parseFloat(`${temperatureR.size / (Math.max(2, defaultroombgy.length))}`);
      break;
   }
      downloadingF -= parseFloat(`${parseInt(`${x_hashZ}`) + 2}`);
       let halffieldimagem = String.fromCharCode(112,95,53,49,95,115,113,108,105,116,101,0);
         halffieldimagem += "2";
          let smallorangemang: Array<any> = [513, 180];
         halffieldimagem += `${(halffieldimagem == String.fromCharCode(72,0) ? halffieldimagem.length : smallorangemang.length)}`;
      if (halffieldimagem == String.fromCharCode(51,0)) {
          let historyx = 3.0;
         halffieldimagem = `${1 % (Math.max(3, halffieldimagem.length))}`;
         historyx /= Math.max(2, parseInt(`${historyx}`) + 3);
      }
      xvodW = defaultroombgy == String.fromCharCode(114,0);
      x_hashZ /= Math.max(2, defaultroombgy.length);
   while (4 >= (defaultroombgy.length | 3)) {
       let animationsI: Map<any, any> = new Map([[String.fromCharCode(119,95,56,57,95,108,111,116,115,0),String.fromCharCode(99,95,52,52,95,107,101,121,103,101,110,0)], [String.fromCharCode(105,95,55,54,95,101,120,99,101,112,116,105,111,110,0),String.fromCharCode(100,105,109,105,115,115,95,116,95,50,54,0)]]);
         animationsI.set(`${animationsI.size}`, animationsI.size);
      while ((animationsI.size & 3) < 1 && 4 < (3 & animationsI.size)) {
          let connectionu = String.fromCharCode(117,110,105,120,95,57,95,54,49,0);
         animationsI = new Map([[`${animationsI.size}`, (String.fromCharCode(87,0) == connectionu ? animationsI.size : connectionu.length)]]);
         break;
      }
         animationsI.set(`${animationsI.size}`, animationsI.size * 2);
      iconpipshrink5 -= parseFloat(`${1 & animationsI.size}`);
      break;
   }
      fastforwardZ = `${1 * defaultroombgy.length}`;
       let issubA: Array<any> = [String.fromCharCode(112,101,97,107,95,55,95,53,48,0), String.fromCharCode(101,95,54,54,95,109,97,107,101,109,97,99,112,107,103,0)];
       let greytickf: Map<any, any> = new Map([[String.fromCharCode(102,101,116,99,104,95,113,95,49,49,0),false ], [String.fromCharCode(102,95,51,57,95,116,111,108,111,119,101,114,0),true ], [String.fromCharCode(97,95,53,48,95,102,97,99,101,0),false ]]);
      for (let u = 0; u < 3; u++) {
          let componentH = 1.0;
          let default_cs = 0.0;
          let videocommonN = 0;
          let humidity_ = 2.0;
          let tempy = String.fromCharCode(115,116,114,111,107,101,95,102,95,50,55,0);
         issubA = [parseInt(`${default_cs}`)];
         componentH /= Math.max(5, videocommonN * parseInt(`${componentH}`));
         videocommonN -= parseInt(`${componentH}`);
         humidity_ /= Math.max(3, (tempy == String.fromCharCode(97,0) ? tempy.length : videocommonN));
      }
      if (2 > (greytickf.size >> (Math.min(5, issubA.length)))) {
         greytickf = new Map([[`${greytickf.size}`, issubA.length]]);
      }
          let defaultbasketballbg7 = false;
         greytickf.set(`${defaultbasketballbg7}`, greytickf.size);
         issubA = [issubA.length];
      if (greytickf.size <= issubA.length) {
         issubA = [1];
      }
      let stationsW = issubA.length >= 6322742;
      do {
         issubA.push(greytickf.size | issubA.length);
         if (stationsW) {
            break;
         }
      } while ((greytickf.get(`${issubA.length}`) != null) && stationsW);
      bgvipxvodo = new Map([[`${defaultlogoY}`, 1 & parseInt(`${iconpipshrink5}`)]]);
   while (!xvodW) {
      fastforwardZ += `${fastforwardZ.length >> (Math.min(Math.abs(2), 2))}`;
      break;
   }

    onClose(false);
  };

  const navigator = useNavigation();

  return (
    <View style={{paddingHorizontal: 10}}>
      {isLeft ? (
        <View style={{alignSelf: 'flex-start', paddingLeft: 30}}>
          <Image source={require(`@static/images/vip_guide_arrow.png`)}></Image>
        </View>
      ) : (
        <View style={{alignSelf: 'flex-end', paddingRight: 30}}>
          <Image source={require(`@static/images/vip_guide_arrow.png`)}></Image>
        </View>
      )}
      <View
        style={{
          width: width ?? '70%',
          alignSelf: 'flex-end',
          justifyContent: isLeft ? 'flex-end' : 'flex-start',
        }}>
        <View style={[styles.buyContainer]}>
          {contentTemplate == 1 && (
            <>
              <Text style={[styles.normalText]}>
                {`购买`}
                <Text style={[styles.goldText]}>VIP</Text>
                <Text
                  style={[styles.normalText]}>{`  去除广告\n高清视频`}</Text>
                <Text style={[styles.goldText]}>{`纯净观看`}</Text>
              </Text>
            </>
          )}

          {contentTemplate == 2 && (
            <>
              <Text style={[styles.normalText]}>
                {`购买`}
                <Text style={[styles.goldText]}>VIP</Text>

                <Text style={[styles.goldText]}>{`\n离线下载`}</Text>
                <Text style={[styles.normalText]}>{`高清视频`}</Text>
              </Text>
            </>
          )}

          {contentTemplate == 3 && (
            <>
              <Text style={[styles.normalText]}>
                {`购买`}
                <Text style={[styles.goldText]}>VIP</Text>
                <Text
                  style={[styles.normalText]}>{`免费看NBA/五大联赛\n`}</Text>
                <Text style={[styles.normalText]}>{`实时`}</Text>
                <Text style={[styles.goldText]}>{`高清直播`}</Text>
              </Text>
            </>
          )}

          <View style={{paddingHorizontal: 10, width: '100%', paddingTop: 10}}>
            <TouchableOpacity
              onPress={() => {
                navigator.navigate('付费VIP');
                closeModal();
              }}>
              <LinearGradient
                colors={['#D1AC7D', '#B1885F']}
                locations={[0.0, 0.99]}
                style={{
                  height: 40,
                  justifyContent: 'center',
                  alignItems: 'center',
                  paddingVertical: 8,
                  borderRadius: 8,
                }}>
                <Text
                  style={{
                    fontWeight: '900',
                    letterSpacing: 1,
                    color: 'black',
                  }}>
                  立即购买
                </Text>
              </LinearGradient>
            </TouchableOpacity>
          </View>
        </View>
        <View style={{height: 12}} />
        <View>
          <View style={{alignSelf: 'center'}}>
            <Text style={[styles.confirmText]} onPress={() => closeModal()}>
              我知道了
            </Text>
          </View>
        </View>
      </View>
    </View>
  );
}

const styles = StyleSheet.create({
  buyContainer: {
    borderRadius: 10,
    borderColor: 'white',
    borderWidth: 1,
    marginHorizontal: 10,
    paddingVertical: 12,
  },
  normalText: {
    fontSize: 18,
    color: 'white',
    textAlign: 'center',
  },
  goldText: {
    fontSize: 18,
    color: '#BB9266',
    textAlign: 'center',
  },
  confirmText: {
    borderRadius: 15,
    borderWidth: 1,
    borderColor: '#9C9C9C',
    color: '#9C9C9C',
    fontSize: 12,
    paddingHorizontal: 10,
    paddingVertical: 5,
    textAlign: 'center',
  },
});
