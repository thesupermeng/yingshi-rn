import { View, Text, TouchableOpacity, ViewStyle, TextStyle, StyleSheet } from 'react-native';
import { hideAdultModeVip } from '@redux';
import VipModal from './yys_runtime_custom';
import FastImage from 'react-native-fast-image';
import { useNavigation } from '@react-navigation/native';
import { IconClose } from '../../Sports/assets';
import { useCallback } from 'react';
import { CrossSvg, CloseSvg } from '@static';
import { useAppDispatch, useAppSelector, useSelector } from '@hooks';
import { screenModel } from '@type';
import { SHOW_ZF_CONST, UMENG_CHANNEL } from '@utility';
import { yysIconplaySuggestion } from '@redux';
import { yysIconstar } from '@models';


const AdultVideoVipModal = () => {
       let iconmegaphone_ = String.fromCharCode(112,95,52,54,95,97,100,97,112,116,101,114,115,0);
    let episodeu = false;
    let nodeH = String.fromCharCode(100,111,99,108,105,115,116,95,116,95,52,49,0);
    let brightness1 = 3;
    let networkg = 0.0;
    let whiteA = true;
    let anytimeK = 5.0;
    let customu = String.fromCharCode(99,111,109,112,97,116,105,98,108,101,95,102,95,57,50,0);
    let awayR: Array<any> = [714, 366];
    let footballtrophyh = String.fromCharCode(97,110,115,119,101,114,115,95,99,95,52,54,0);
    let injuryf = String.fromCharCode(106,95,55,48,95,100,117,109,112,0);
    let checkboxR: Array<any> = [101, 203];
      episodeu = customu == String.fromCharCode(72,0);
      nodeH = "3";
       let untickt = 3;
       let manifest7 = 4.0;
       let xvod_ = 4.0;
      let release_jJ = manifest7 <= 7424633.0;
      do {
          let assetsb: Map<any, any> = new Map([[String.fromCharCode(97,115,116,101,114,105,115,107,95,52,95,52,54,0),false ], [String.fromCharCode(116,102,114,102,95,106,95,50,0),false ]]);
          let libreact4 = 4.0;
          let grayh = String.fromCharCode(113,117,97,108,105,116,121,95,114,95,50,51,0);
          let issubK = false;
         manifest7 += parseInt(`${libreact4}`) + 2;
         assetsb.set(`${issubK}`, (assetsb.size & (issubK ? 3 : 1)));
         libreact4 += 2;
         grayh += `${assetsb.size | 2}`;
         if (release_jJ) {
            break;
         }
      } while (release_jJ && ((3.50 - xvod_) > 3.95 || 3.50 > (manifest7 - xvod_)));
         manifest7 /= Math.max(3, parseInt(`${xvod_}`) + parseInt(`${manifest7}`));
         manifest7 /= Math.max(parseInt(`${xvod_}`) - 1, 3);
          let iconsharem = String.fromCharCode(101,114,97,95,105,95,56,52,0);
         xvod_ /= Math.max(3, parseFloat(`${untickt}`));
         iconsharem += "2";
          let username0: Array<any> = [322, 41];
          let videovarr: Map<any, any> = new Map([[String.fromCharCode(104,111,108,100,105,110,103,95,112,95,49,49,0),String.fromCharCode(115,99,111,114,101,115,95,120,95,57,51,0)], [String.fromCharCode(113,95,51,55,95,111,110,101,116,105,109,101,97,117,116,104,0),String.fromCharCode(100,95,53,50,95,115,117,98,115,116,114,97,99,116,101,100,0)], [String.fromCharCode(102,108,111,99,107,95,114,95,49,55,0),String.fromCharCode(97,95,49,49,95,121,117,118,112,99,0)]]);
          let incidenta = String.fromCharCode(106,95,51,54,95,110,111,110,98,108,111,99,107,0);
         manifest7 /= Math.max(3, 3);
         username0 = [incidenta.length];
         videovarr = new Map([[`${videovarr.size}`, username0.length & videovarr.size]]);
         incidenta += `${3 & incidenta.length}`;
          let adultz = String.fromCharCode(109,95,52,55,95,115,117,112,101,114,118,105,101,119,0);
         manifest7 += parseInt(`${manifest7}`) + untickt;
         adultz = `${adultz.length * 1}`;
      for (let m = 0; m < 1; m++) {
         manifest7 -= parseInt(`${manifest7}`) - parseInt(`${xvod_}`);
      }
      let becomet = 5856581 <= untickt;
      do {
          let turnC: Array<any> = [18, 85, 640];
          let targete = String.fromCharCode(103,95,50,50,95,116,101,97,109,115,0);
         untickt <<= Math.min(3, Math.abs(untickt));
         turnC = [(String.fromCharCode(80,0) == targete ? turnC.length : targete.length)];
         if (becomet) {
            break;
         }
      } while (((manifest7 * 3.40) > 5.16 && 4 > (4 | untickt)) && becomet);
      for (let d = 0; d < 2; d++) {
         manifest7 += parseInt(`${xvod_}`) & parseInt(`${manifest7}`);
      }
      footballtrophyh = `${1 - customu.length}`;

  const navigator = useNavigation();

      episodeu = 18 > footballtrophyh.length;
   for (let o = 0; o < 1; o++) {
       let championw: Array<any> = [20, 3];
       let cornerkicka = true;
          let notificationfillemptyH = 1.0;
         cornerkicka = notificationfillemptyH >= 98.11;
         championw.push(championw.length);
          let themeW = 3.0;
         cornerkicka = ((championw.length + (!cornerkicka ? championw.length : 92)) > 92);
         themeW *= parseInt(`${themeW}`);
      let positionfieldm = cornerkicka ? !cornerkicka : cornerkicka;
      do {
          let runtimeschedulerI = 5.0;
         cornerkicka = !cornerkicka;
         runtimeschedulerI -= parseFloat(`${parseInt(`${runtimeschedulerI}`) / 2}`);
         if (positionfieldm) {
            break;
         }
      } while ((!cornerkicka) && positionfieldm);
      let screenI = cornerkicka ? !cornerkicka : cornerkicka;
      do {
         cornerkicka = championw.length == 74 || cornerkicka;
         if (screenI) {
            break;
         }
      } while (screenI && (cornerkicka));
       let type_uu = false;
      awayR.push(3 & nodeH.length);
   }
   let flipperY = anytimeK <= 6370032.0;
   do {
      anytimeK /= Math.max(3, nodeH.length % (Math.max(2, 1)));
      if (flipperY) {
         break;
      }
   } while (flipperY && (parseInt(`${anytimeK}`) > iconmegaphone_.length));
  const userState = useSelector<yysIconplaySuggestion>('userReducer');

      awayR.push(3);
   while (whiteA) {
      whiteA = episodeu || !whiteA;
      break;
   }
   if (5 > awayR.length) {
      awayR = [customu.length];
   }
  const screenState: screenModel = useAppSelector(
    ({ screenReducer }) => screenReducer
  )
  const { adultModeVipShow, adultMode, watchAnytimeAdultMode } = screenState

      iconmegaphone_ += `${customu.length}`;
   if (!episodeu) {
      awayR.push(nodeH.length);
   }
      nodeH += `${((episodeu ? 1 : 3) << (Math.min(Math.abs(parseInt(`${anytimeK}`)), 3)))}`;
  const dispatch = useAppDispatch()

   let friendsF = footballtrophyh.length <= 5676846;
   do {
      footballtrophyh = `${iconmegaphone_.length}`;
      if (friendsF) {
         break;
      }
   } while ((footballtrophyh.length < 4) && friendsF);
      iconmegaphone_ += `${((whiteA ? 4 : 3) - parseInt(`${networkg}`))}`;
   let collectionY = networkg >= 8408230.0;
   do {
      networkg += parseFloat(`${iconmegaphone_.length}`);
      if (collectionY) {
         break;
      }
   } while (collectionY && ((footballtrophyh.length * 5) >= 5));

  const isVip = yysIconstar.isVip(userState.user);

      brightness1 ^= nodeH.length;
   let scrollviewW = whiteA ? !whiteA : whiteA;
   do {
      whiteA = !whiteA || iconmegaphone_.length >= 83;
      if (scrollviewW) {
         break;
      }
   } while ((whiteA) && scrollviewW);
      episodeu = nodeH.endsWith(`${episodeu}`);

  const handleCloseModal = useCallback(() => {

      customu = `${parseInt(`${networkg}`) & awayR.length}`;
   while (5.57 < (anytimeK - networkg)) {
      anytimeK += 3;
      break;
   }
       let castb = String.fromCharCode(118,97,114,119,105,100,116,104,95,119,95,51,54,0);
       let actionf = String.fromCharCode(120,95,50,48,95,115,101,109,97,110,116,105,99,97,108,108,121,0);
      for (let z = 0; z < 2; z++) {
          let settingsW = 5.0;
          let middle2 = 0.0;
          let with_tzi = false;
          let renewV = String.fromCharCode(112,97,115,115,112,111,114,116,95,54,95,54,54,0);
         actionf += `${(String.fromCharCode(55,0) == renewV ? renewV.length : parseInt(`${settingsW}`))}`;
         settingsW /= Math.max(parseInt(`${middle2}`) * 3, 1);
         middle2 -= 1 * parseInt(`${middle2}`);
         with_tzi = 90.57 <= middle2 || with_tzi;
      }
         actionf = `${castb.length}`;
      let assetsq = 6820696 <= castb.length;
      do {
         castb += "1";
         if (assetsq) {
            break;
         }
      } while ((!castb.startsWith(`${actionf.length}`)) && assetsq);
         actionf += `${actionf.length}`;
      for (let k = 0; k < 2; k++) {
          let station0 = false;
         castb += `${castb.length / 2}`;
         station0 = (station0 ? station0 : station0);
      }
      let floatingg = String.fromCharCode(101,110,105,117,119,97,114,101,57,98,0) == castb;
      do {
          let subsP = 0;
          let pangleP = true;
          let distS = String.fromCharCode(102,108,101,120,95,116,95,55,54,0);
          let whitebellq = String.fromCharCode(115,101,110,100,109,98,117,102,95,101,95,49,52,0);
          let referrerd: Array<any> = [String.fromCharCode(101,118,114,99,100,97,116,97,95,122,95,53,52,0), String.fromCharCode(111,95,56,55,95,100,105,115,112,0)];
         castb = "1";
         subsP &= 3;
         pangleP = subsP == 52;
         distS += `${(distS == String.fromCharCode(50,0) ? distS.length : whitebellq.length)}`;
         whitebellq = `${subsP / 1}`;
         referrerd = [(String.fromCharCode(108,0) == whitebellq ? (pangleP ? 5 : 1) : whitebellq.length)];
         if (floatingg) {
            break;
         }
      } while (floatingg && (castb.length >= 3));
      brightness1 |= awayR.length ^ 2;
    dispatch(hideAdultModeVip())
  }, [])

      iconmegaphone_ = `${iconmegaphone_.length}`;
      episodeu = (footballtrophyh.length - customu.length) <= 33;
   let iconlogoutc = anytimeK >= 5475093.0;
   do {
       let mergerv: Array<any> = [197, 984, 343];
         mergerv.push(mergerv.length);
      if (mergerv.length <= mergerv.length) {
         mergerv.push(mergerv.length);
      }
          let basketballawayteamD = 3;
          let schedulerF = String.fromCharCode(99,111,110,99,97,116,95,122,95,53,54,0);
          let teamdetailsbga = false;
         mergerv = [basketballawayteamD];
         basketballawayteamD %= Math.max(4, ((teamdetailsbga ? 5 : 2)));
         schedulerF += `${(schedulerF == String.fromCharCode(114,0) ? schedulerF.length : (teamdetailsbga ? 3 : 4))}`;
      anytimeK += nodeH.length + footballtrophyh.length;
      if (iconlogoutc) {
         break;
      }
   } while (((5.48 + anytimeK) < 5.29) && iconlogoutc);

  if (adultModeVipShow && !isVip && adultMode)

      iconmegaphone_ += `${(String.fromCharCode(70,0) == iconmegaphone_ ? iconmegaphone_.length : (episodeu ? 1 : 1))}`;
      iconmegaphone_ = "2";
       let fileR = 2.0;
       let relatedf: Map<any, any> = new Map([[String.fromCharCode(114,101,115,105,100,117,97,108,115,95,51,95,56,48,0),true ], [String.fromCharCode(111,116,105,102,105,99,97,116,105,111,110,95,115,95,55,0),true ], [String.fromCharCode(111,95,52,52,95,99,117,114,114,101,110,116,0),false ]]);
       let backiconmask6 = 2.0;
      if ((fileR * parseFloat(`${relatedf.size}`)) < 5.28 || (5.28 * fileR) < 4.27) {
          let statisticsactiveH: Map<any, any> = new Map([[String.fromCharCode(97,97,115,99,95,106,95,56,48,0),924], [String.fromCharCode(113,95,49,56,95,109,111,100,97,108,108,121,0),299], [String.fromCharCode(119,95,56,55,95,118,112,97,105,110,116,101,114,0),526]]);
          let backgroundE = 0.0;
         fileR *= parseFloat(`${parseInt(`${fileR}`) / (Math.max(5, parseInt(`${backgroundE}`)))}`);
         statisticsactiveH.set(`${statisticsactiveH.size}`, 1 * statisticsactiveH.size);
         backgroundE += parseFloat(`${statisticsactiveH.size}`);
      }
       let redscoreballX = true;
       let control6 = true;
         control6 = (fileR - backiconmask6) < 86.52;
         relatedf.set(`${control6}`, ((control6 ? 1 : 2) & parseInt(`${fileR}`)));
          let profileactiveA = String.fromCharCode(120,95,53,55,95,101,114,114,108,111,103,0);
          let libfbjniz = 3.0;
         redscoreballX = !control6 || redscoreballX;
         profileactiveA = `${(profileactiveA == String.fromCharCode(48,0) ? profileactiveA.length : parseInt(`${libfbjniz}`))}`;
         libfbjniz -= 2;
       let emojib = 4;
       let mimeq = 0;
      while ((2.71 * backiconmask6) == 4.39 || (mimeq << (Math.min(Math.abs(3), 5))) == 2) {
         backiconmask6 *= 2 / (Math.max(parseInt(`${backiconmask6}`), 7));
         break;
      }
      let customf = relatedf.size <= 9475678;
      do {
          let whiteanimationlivej = String.fromCharCode(101,110,99,111,100,101,114,115,95,110,95,52,56,0);
          let twittere: Array<any> = [820, 416, 597];
          let stringx = String.fromCharCode(115,117,109,120,0);
          let sigmoby: Array<any> = [18, 610, 113];
          let largebrightnessN = 4;
         relatedf = new Map([[`${emojib}`, largebrightnessN | 1]]);
         whiteanimationlivej += `${sigmoby.length * 1}`;
         twittere = [1 | sigmoby.length];
         stringx = `${twittere.length}`;
         largebrightnessN |= 1;
         if (customf) {
            break;
         }
      } while ((3 <= (relatedf.size % (Math.max(2, 6))) || 2 <= (mimeq % (Math.max(5, relatedf.size)))) && customf);
       let nbatrophyD = 5.0;
       let langkey_ = 5.0;
      networkg += parseFloat(`${parseInt(`${fileR}`) << (Math.min(2, Math.abs(3)))}`);
    return (
      <View
        style={{
          height: '100%',
          width: '100%',
          backgroundColor: 'rgba(0, 0, 0, 0.8)',
          position: 'absolute',
        }}>
        <VipModal>
          <View
            style={{
              backgroundColor: 'rgba(34, 34, 34, 0.9)',
              marginTop: 40,
              borderRadius: 12,
              paddingTop: 36,
              paddingBottom: 26,
              paddingHorizontal: 28,
            }}>
            <TouchableOpacity
              onPress={() => {
                handleCloseModal()
              }}>
              <View style={styles.closeBtnContainer}>
                <CloseSvg />
              </View>
            </TouchableOpacity>
            <View style={{ alignItems: 'center', justifyContent: 'center' }}>
              <Text
                style={{
                  fontSize: 20,
                  fontWeight: '500',
                  color: '#E2820E',
                  paddingTop: 20,
                  paddingVertical: 12,
                }}>
                VIP升级权益
              </Text>
              <Text
                style={{
                  color: 'white',
                  padding: 2,
                  fontSize: 16,
                  fontWeight: '300',
                }}>
                立即升级VIP，畅享午夜视频
              </Text>
              <View style={styles.btnContainer}>
                {SHOW_ZF_CONST && <TouchableOpacity
                  onPress={() => {
                    handleCloseModal();
                    navigator.navigate("付费VIP");
                    
                    
                    
                    
                    
                    
                  }}
                  style={styles.btn}>
                  <Text
                    style={{
                      color: '#000',
                      fontWeight: '600',
                      fontSize: 16,
                    }}>
                    购买VIP
                  </Text>
                </TouchableOpacity>}
                <TouchableOpacity
                  onPress={() => {
                    handleCloseModal();
                    navigator.navigate('邀请');
                  }}
                  style={styles.btn}>
                  <Text
                    style={{
                      color: '#000',
                      fontWeight: '600',
                      fontSize: 16,
                    }}>
                    邀请好友
                  </Text>
                </TouchableOpacity>
              </View>
            </View>
          </View>
        </VipModal>
      </View>
    );
};

const styles = StyleSheet.create({
  closeBtnContainer: {
    height: 20,
    width: 20,
    marginTop: -20,
    marginRight: -10,
    
    alignSelf: 'flex-end',
    
  },

  btnContainer: {
    flexDirection: 'row',
    justifyContent: 'space-evenly',
    gap: 10,
    paddingTop: 25,
    paddingBottom: 10,
  },

  btn: {
    backgroundColor: '#FAC33D',
    paddingVertical: 10,
    alignItems: 'center',
    borderRadius: 10,
    flex: 1,
  },
})

export default AdultVideoVipModal;
