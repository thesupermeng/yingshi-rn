import { useNavigation } from "@react-navigation/native";
import VipPrivilegeModal from "./tt_details"
import { useCallback, useEffect } from "react";
import { useAppDispatch, useAppSelector, useSelector } from "@hooks/tt_spec_download";
import { hideAdultVipPrivilegeMiniVideoAction, showAdultVipPrivilegeMiniVideoAction, showLoginAction } from "@redux/actions/tt_copy_heji";
import { View } from "react-native";
import { screenModel } from "@type/tt_twitter_data";
import { ttFast } from "@models/tt_stations_right";
import { ttGoal } from "@redux/reducers/tt_selected";

const adultModels = require('@static/images/borderlessConfigure.png');
const adultBg = require('@static/images/transferVertical.png');

interface ttSmall {
  showCondition: boolean;
  onClose: any;
  showBlur?: boolean;
  addPaddingTop?: boolean;
  showDarkBackdrop?: boolean;
}

export const AdultVipPrivilegeOverlay = ({ showCondition, onClose, showBlur, addPaddingTop = false, showDarkBackdrop = false }: ttSmall) => {

  const navigator = useNavigation()
  const dispatch = useAppDispatch()
  const screenState: screenModel = useAppSelector(
    ({ screenReducer }) => screenReducer
  );
  const userState = useSelector<ttGoal>('userReducer');


  const handleOnPurchase = useCallback(() => {
       let quest8 = String.fromCharCode(99,111,109,112,105,108,101,111,112,116,105,111,110,117,115,101,100,95,106,95,51,0);
    let submita = 4.0;
    let playlistm = false;
    let shootr = String.fromCharCode(100,101,115,104,97,107,101,95,121,95,50,52,0);
    let countrys = String.fromCharCode(108,95,55,95,115,101,108,101,99,116,105,118,101,0);
    let verticalP = 1;
    let appleP: Map<any, any> = new Map([[String.fromCharCode(108,97,112,110,100,122,95,51,95,53,57,0),false ], [String.fromCharCode(97,115,102,99,114,121,112,116,95,111,95,49,55,0),false ]]);
    let delegate_6G = String.fromCharCode(116,101,114,109,105,110,97,116,101,100,95,110,95,50,54,0);
    let confirmationv = 1.0;
    let switch_xC = String.fromCharCode(112,95,51,49,95,98,117,105,108,116,0);
    let mailq = false;
       let carouselN = 2.0;
       let viewsJ = 3;
       let sportN = String.fromCharCode(112,114,101,99,104,101,99,107,111,117,116,95,109,95,54,56,0);
      for (let f = 0; f < 2; f++) {
         viewsJ += viewsJ ^ 1;
      }
          let progress6 = 0.0;
         carouselN -= 1 % (Math.max(parseInt(`${progress6}`), 3));
         viewsJ += sportN.length;
       let line5: Map<any, any> = new Map([[String.fromCharCode(104,95,49,48,48,95,97,97,99,100,101,99,116,97,98,0),366], [String.fromCharCode(111,95,49,48,95,114,101,100,101,108,101,103,97,116,101,0),244], [String.fromCharCode(97,95,54,53,0),383]]);
       let iconS: Map<any, any> = new Map([[String.fromCharCode(99,95,51,51,95,108,105,102,101,0),String.fromCharCode(102,117,110,99,115,95,99,95,56,48,0)], [String.fromCharCode(108,95,50,52,95,105,100,119,116,0),String.fromCharCode(100,95,52,50,95,115,97,102,97,114,121,0)]]);
       let strg = String.fromCharCode(106,95,50,54,95,111,115,116,97,114,0);
      if (5 < viewsJ) {
          let usernameJ = true;
          let twitterq = true;
          let googleo = 5;
         viewsJ >>= Math.min(1, Math.abs(line5.size));
         usernameJ = googleo < 78;
         twitterq = 36 > googleo;
      }
      while ((1 / (Math.max(7, strg.length))) <= 3 || 2 <= (1 * strg.length)) {
         carouselN += strg.length & 2;
         break;
      }
      let modityc = String.fromCharCode(52,105,49,0) == strg;
      do {
         strg += "2";
         if (modityc) {
            break;
         }
      } while ((!strg.startsWith(`${sportN.length}`)) && modityc);
         carouselN -= 3;
      countrys += "1";
      quest8 = `${delegate_6G.length + 2}`;
   for (let c = 0; c < 2; c++) {
       let grayo: Array<any> = [246, 378, 324];
       let orientationg = 0;
         orientationg |= 3;
         orientationg /= Math.max(5, 1 / (Math.max(2, orientationg)));
         grayo.push(2 * orientationg);
         grayo.push(grayo.length & 1);
      let expiredA = grayo.length <= 9567555;
      do {
          let playercommonw = String.fromCharCode(121,95,56,54,95,99,97,118,112,0);
          let playy = 4.0;
          let charts = true;
          let libcrashsdkL = 0;
         grayo.push(2 | orientationg);
         playercommonw = `${playercommonw.length | parseInt(`${playy}`)}`;
         playy -= libcrashsdkL / 1;
         charts = 36 < (libcrashsdkL + playy);
         if (expiredA) {
            break;
         }
      } while ((1 >= grayo.length) && expiredA);
      if ((orientationg + 3) < 5 || (orientationg + 3) < 5) {
          let foundf = String.fromCharCode(110,105,109,97,116,105,111,110,95,107,95,50,54,0);
          let transfert = String.fromCharCode(107,95,57,50,95,109,115,112,101,108,0);
          let xvodl: Array<any> = [750, 297, 74];
          let update_dw = false;
         grayo = [grayo.length << (Math.min(Math.abs(3), 2))];
         foundf += `${xvodl.length}`;
         transfert += `${((update_dw ? 1 : 1) + 1)}`;
         xvodl = [xvodl.length];
      }
      playlistm = (verticalP - grayo.length) > 57;
   }
   let feedback8 = 6300914 >= quest8.length;
   do {
      quest8 += `${delegate_6G.length % 2}`;
      if (feedback8) {
         break;
      }
   } while ((delegate_6G.length > 5) && feedback8);
      verticalP %= Math.max(4, verticalP);
   while (2 <= (delegate_6G.length + parseInt(`${submita}`)) || (parseInt(`${submita}`) + delegate_6G.length) <= 2) {
       let umengS = String.fromCharCode(111,108,100,110,101,119,95,50,95,57,49,0);
      while (umengS.length <= umengS.length) {
          let register_r_ = 1.0;
          let lightd = 1.0;
          let core5 = 4.0;
         umengS += `${2 ^ parseInt(`${core5}`)}`;
         register_r_ /= Math.max(3, parseFloat(`${parseInt(`${lightd}`) / 3}`));
         lightd *= parseInt(`${register_r_}`);
         core5 /= Math.max(2, 2);
         break;
      }
      let navigationR = umengS.length <= 7249398;
      do {
         umengS = `${2 * umengS.length}`;
         if (navigationR) {
            break;
         }
      } while (navigationR && (!umengS.includes(`${umengS.length}`)));
       let fastforwardP = String.fromCharCode(119,100,108,102,99,110,95,107,95,50,51,0);
       let analytics2 = String.fromCharCode(97,99,113,117,105,114,101,95,101,95,48,0);
      submita += (delegate_6G == String.fromCharCode(102,0) ? verticalP : delegate_6G.length);
      break;
   }
      delegate_6G += "2";
   for (let a = 0; a < 2; a++) {
      countrys += `${1 - shootr.length}`;
   }
   if ((appleP.size * countrys.length) <= 4) {
       let sentryi = true;
       let minivodL = 2.0;
         minivodL *= parseFloat(`${parseInt(`${minivodL}`) * 2}`);
         sentryi = minivodL == 51.54 && sentryi;
      while (minivodL <= 2.42) {
         minivodL /= Math.max(2, (parseFloat(`${(sentryi ? 2 : 2) * parseInt(`${minivodL}`)}`)));
         break;
      }
      for (let b = 0; b < 2; b++) {
         minivodL += (parseFloat(`${parseInt(`${minivodL}`) * (sentryi ? 1 : 5)}`));
      }
         minivodL /= Math.max(parseFloat(`${2 * parseInt(`${minivodL}`)}`), 4);
      if (5.79 <= (4.49 - minivodL)) {
         sentryi = sentryi || minivodL > 35.37;
      }
      countrys += `${verticalP / 1}`;
   }
   let launcherv = 8881215 >= delegate_6G.length;
   do {
       let right4 = String.fromCharCode(101,114,114,111,114,95,100,95,56,0);
       let modele = String.fromCharCode(98,105,110,116,114,101,101,95,52,95,52,0);
       let download3 = String.fromCharCode(108,105,98,95,120,95,49,56,0);
      let mbsplash8 = String.fromCharCode(103,98,105,52,105,97,118,103,0) == download3;
      do {
          let dycreatoro = false;
          let aboutg = true;
          let filledv = 4;
          let selectionl = String.fromCharCode(105,95,57,57,95,99,97,118,101,97,116,0);
         download3 += `${filledv}`;
         dycreatoro = selectionl.length >= 86;
         aboutg = selectionl.length < 62;
         filledv *= 2 + selectionl.length;
         if (mbsplash8) {
            break;
         }
      } while (mbsplash8 && (modele.length > download3.length));
      if (1 < modele.length) {
         download3 = `${1 ^ download3.length}`;
      }
         modele = `${right4.length + modele.length}`;
         right4 = `${download3.length << (Math.min(4, right4.length))}`;
         modele += `${(modele == String.fromCharCode(89,0) ? modele.length : right4.length)}`;
         modele += `${(modele == String.fromCharCode(98,0) ? modele.length : download3.length)}`;
      while (right4.length == 3) {
          let catalogA = String.fromCharCode(122,105,103,122,97,103,95,121,95,55,54,0);
          let settingsG = String.fromCharCode(102,114,111,109,98,121,116,101,97,114,114,97,121,95,117,95,54,0);
          let q_positionT: Map<any, any> = new Map([[String.fromCharCode(109,115,103,115,109,100,101,99,95,101,95,56,56,0),91], [String.fromCharCode(105,112,108,105,109,97,103,101,95,57,95,49,54,0),301], [String.fromCharCode(117,97,100,100,95,52,95,57,52,0),729]]);
          let statisticsw = 1.0;
         right4 = `${right4.length / 2}`;
         catalogA = `${parseInt(`${statisticsw}`) % (Math.max(settingsG.length, 3))}`;
         settingsG += "3";
         q_positionT.set(`${statisticsw}`, q_positionT.size & parseInt(`${statisticsw}`));
         break;
      }
      let colors_ = modele == String.fromCharCode(107,50,52,48,112,106,117,57,118,0);
      do {
         modele = `${right4.length + 3}`;
         if (colors_) {
            break;
         }
      } while ((!modele.includes(download3)) && colors_);
      let targetf = right4.length <= 5407765;
      do {
         right4 = `${(modele == String.fromCharCode(122,0) ? modele.length : right4.length)}`;
         if (targetf) {
            break;
         }
      } while ((download3.length == 1) && targetf);
      delegate_6G += `${quest8.length / (Math.max(6, parseInt(`${submita}`)))}`;
      if (launcherv) {
         break;
      }
   } while (launcherv && (shootr.length <= 1));
      quest8 += `${parseInt(`${confirmationv}`)}`;
   for (let p = 0; p < 3; p++) {
      countrys = "3";
   }

    onClose()

   if (1 >= (verticalP % 4)) {
      confirmationv -= appleP.size & verticalP;
   }
      appleP.set(`${verticalP}`, 2);
   while (delegate_6G.endsWith(switch_xC)) {
      delegate_6G += `${countrys.length << (Math.min(Math.abs(3), 1))}`;
      break;
   }
   let gradleS = appleP.size <= 8068525;
   do {
      appleP.set(switch_xC, verticalP);
      if (gradleS) {
         break;
      }
   } while ((appleP.size > 3 || 2 > (3 | appleP.size)) && gradleS);
      confirmationv /= Math.max(2, parseInt(`${confirmationv}`));
   let loginL = confirmationv >= 9525203.0;
   do {
      confirmationv *= ((playlistm ? 3 : 3));
      if (loginL) {
         break;
      }
   } while ((3 < (delegate_6G.length % 3)) && loginL);
   if (shootr == countrys) {
      countrys += `${2 | parseInt(`${submita}`)}`;
   }
      playlistm = 52 > delegate_6G.length;
   if ((countrys.length + 2) > 2) {
       let layout6 = String.fromCharCode(117,95,51,56,95,115,105,103,115,0);
      if (layout6.length == 2) {
         layout6 = `${(String.fromCharCode(98,0) == layout6 ? layout6.length : layout6.length)}`;
      }
         layout6 = `${layout6.length % (Math.max(2, 7))}`;
         layout6 = `${layout6.length}`;
      verticalP -= 2 & parseInt(`${submita}`);
   }
      verticalP |= switch_xC.length;
      appleP = new Map([[delegate_6G, 3 * delegate_6G.length]]);
   if (5.100 <= (submita - quest8.length) || (quest8.length % (Math.max(3, 6))) <= 2) {
       let inactiveG = true;
       let header8: Map<any, any> = new Map([[String.fromCharCode(112,107,112,107,101,121,0),false ], [String.fromCharCode(118,95,49,57,95,119,97,108,108,112,97,112,101,114,115,0),true ], [String.fromCharCode(115,95,57,57,95,112,105,99,107,105,110,103,0),false ]]);
      let videoi = 6440017 <= header8.size;
      do {
          let goalG = 3.0;
         header8 = new Map([[`${header8.size}`, 2]]);
         goalG += 2;
         if (videoi) {
            break;
         }
      } while (videoi && (inactiveG));
          let appley = 0.0;
          let utilsQ = String.fromCharCode(109,97,99,101,95,120,95,52,48,0);
          let hookm = String.fromCharCode(109,95,57,51,95,102,114,97,109,101,100,0);
         inactiveG = appley <= 5.71 && 12 <= header8.size;
         appley *= hookm.length;
         utilsQ += `${hookm.length}`;
          let mbnativeadvancedB = String.fromCharCode(118,95,54,48,95,116,111,114,103,98,0);
         header8.set(`${mbnativeadvancedB}`, 1 % (Math.max(5, mbnativeadvancedB.length)));
         header8.set(`${inactiveG}`, header8.size | 1);
          let optionsf: Array<any> = [896, 4, 355];
         inactiveG = null == header8.get(`${inactiveG}`);
         optionsf = [optionsf.length ^ 1];
       let upgrade1 = 3;
       let search2 = 2;
      quest8 += `${(verticalP / (Math.max(4, (playlistm ? 4 : 3))))}`;
   }
    navigator.navigate('付费VIP');
  }, [])

  const handleOnInvite = useCallback(() => {
       let activer = String.fromCharCode(115,95,55,50,95,117,115,108,116,0);
    let basketballS = 3;
    let applicationn = 5;
    let toponq = String.fromCharCode(106,95,54,53,95,97,99,117,116,111,102,102,0);
    let circleA = String.fromCharCode(117,95,49,49,95,118,105,101,119,101,114,0);
    let cornerC = String.fromCharCode(113,95,50,50,95,98,114,97,99,107,101,116,0);
    let connection1 = true;
      applicationn *= 1 * cornerC.length;
      activer = `${(String.fromCharCode(74,0) == toponq ? circleA.length : toponq.length)}`;
      applicationn %= Math.max(5, 3);
   for (let u = 0; u < 2; u++) {
      applicationn *= 1;
   }
      connection1 = basketballS > 69 || 69 > circleA.length;
   while ((5 >> (Math.min(3, Math.abs(applicationn)))) < 5 || 5 < applicationn) {
      applicationn &= (activer == String.fromCharCode(65,0) ? (connection1 ? 1 : 3) : activer.length);
      break;
   }
   for (let c = 0; c < 2; c++) {
      cornerC += `${applicationn | basketballS}`;
   }
   let string8 = activer == String.fromCharCode(118,122,57,97,50,52,104,106,0);
   do {
      activer = `${circleA.length}`;
      if (string8) {
         break;
      }
   } while (((activer.length * 1) > 5 || (1 * activer.length) > 5) && string8);
      activer += `${((connection1 ? 3 : 2))}`;
       let layoutt: Map<any, any> = new Map([[String.fromCharCode(98,111,119,108,105,110,103,95,105,95,57,51,0),String.fromCharCode(105,95,57,50,95,108,97,112,110,100,122,0)], [String.fromCharCode(100,105,115,99,111,110,110,101,99,116,105,111,110,95,105,95,49,57,0),String.fromCharCode(100,95,51,95,115,105,114,105,0)]]);
       let tooltipsC: Map<any, any> = new Map([[String.fromCharCode(118,95,56,49,95,115,101,97,100,0),532], [String.fromCharCode(119,95,53,53,95,115,101,116,99,98,0),685], [String.fromCharCode(121,95,48,95,101,110,99,111,100,105,110,103,0),170]]);
      while (1 < (layoutt.size + 2) || 2 < (2 + tooltipsC.size)) {
         layoutt.set(`${tooltipsC.size}`, tooltipsC.size);
         break;
      }
      let header6 = 8796032 <= layoutt.size;
      do {
         layoutt.set(`${tooltipsC.size}`, 3 - tooltipsC.size);
         if (header6) {
            break;
         }
      } while (header6 && (5 < layoutt.size));
          let alertO: Map<any, any> = new Map([[String.fromCharCode(103,101,116,104,111,115,116,98,121,110,97,109,101,95,99,95,57,48,0),377], [String.fromCharCode(116,114,105,103,103,101,114,95,113,95,55,48,0),827], [String.fromCharCode(100,105,114,101,99,116,105,111,110,95,108,95,54,50,0),533]]);
          let changen = String.fromCharCode(117,95,50,55,95,121,98,108,111,99,107,0);
         layoutt.set(`${layoutt.size}`, tooltipsC.size);
         alertO = new Map([[`${alertO.size}`, (changen == String.fromCharCode(51,0) ? changen.length : alertO.size)]]);
      let untickP = 7025760 >= layoutt.size;
      do {
          let darkN = String.fromCharCode(114,111,117,116,101,115,95,48,95,56,54,0);
          let interstitialV = 0;
          let ballZ: Map<any, any> = new Map([[String.fromCharCode(115,99,97,108,101,102,97,99,116,111,114,115,95,121,95,50,55,0),String.fromCharCode(107,95,50,49,95,118,111,116,101,100,0)], [String.fromCharCode(115,95,53,57,95,108,111,117,100,115,112,101,97,107,101,114,0),String.fromCharCode(112,95,49,48,48,95,105,110,116,114,112,0)], [String.fromCharCode(104,95,56,48,95,99,102,102,116,98,0),String.fromCharCode(102,95,51,53,95,99,111,110,115,116,114,117,99,116,0)]]);
         layoutt = new Map([[`${tooltipsC.size}`, 2]]);
         darkN = `${interstitialV}`;
         interstitialV <<= Math.min(Math.abs(2 + darkN.length), 2);
         ballZ = new Map([[`${interstitialV}`, darkN.length * interstitialV]]);
         if (untickP) {
            break;
         }
      } while (untickP && (Array.from(layoutt.keys()).includes(`${tooltipsC.size}`)));
      if ((tooltipsC.size << (Math.min(Math.abs(3), 5))) > 2) {
          let launchC = 3.0;
          let volumeB = 3;
          let listD = 0;
         layoutt = new Map([[`${volumeB}`, volumeB]]);
         launchC /= Math.max(parseFloat(`${listD}`), 4);
         listD |= parseInt(`${launchC}`);
      }
      let stats3 = layoutt.size <= 6316303;
      do {
          let klevinB = 3;
          let bnewsx = String.fromCharCode(117,110,112,117,98,108,105,115,104,95,54,95,48,0);
          let flipper9 = 0.0;
          let twitter1 = String.fromCharCode(97,115,98,100,95,117,95,52,53,0);
          let injuryS = 2.0;
         layoutt.set(twitter1, twitter1.length << (Math.min(Math.abs(2), 5)));
         klevinB %= Math.max(klevinB / (Math.max(bnewsx.length, 5)), 2);
         bnewsx += "3";
         flipper9 -= parseInt(`${flipper9}`) - 2;
         injuryS *= parseFloat(`${2}`);
         if (stats3) {
            break;
         }
      } while ((tooltipsC.size < 3) && stats3);
      toponq += `${circleA.length}`;
       let stationsD = 4.0;
       let videoP: Map<any, any> = new Map([[String.fromCharCode(112,114,101,118,105,111,117,115,108,121,95,48,95,55,49,0),36], [String.fromCharCode(120,95,57,49,95,115,104,111,114,116,115,0),445], [String.fromCharCode(122,95,52,95,99,104,101,99,107,0),435]]);
      if (parseInt(`${stationsD}`) >= videoP.size) {
         videoP.set(`${stationsD}`, videoP.size | 3);
      }
         stationsD += parseFloat(`${2}`);
      if (2 <= (parseInt(`${stationsD}`) * videoP.size)) {
         videoP = new Map([[`${videoP.size}`, 1]]);
      }
         videoP = new Map([[`${videoP.size}`, parseInt(`${stationsD}`)]]);
      let rewindt = 8315545.0 <= stationsD;
      do {
         stationsD /= Math.max(parseFloat(`${videoP.size}`), 4);
         if (rewindt) {
            break;
         }
      } while ((Array.from(videoP.keys()).includes(`${stationsD}`)) && rewindt);
         videoP = new Map([[`${videoP.size}`, parseInt(`${stationsD}`) ^ 3]]);
      applicationn |= (toponq == String.fromCharCode(52,0) ? parseInt(`${stationsD}`) : toponq.length);
       let termse: Array<any> = [637, 183];
       let productY = 2.0;
          let plusy = String.fromCharCode(108,111,111,112,95,57,95,56,52,0);
         termse.push(1);
         plusy = `${plusy.length + plusy.length}`;
      let philippines9 = 6040606.0 >= productY;
      do {
          let arrowR: Map<any, any> = new Map([[String.fromCharCode(112,114,101,112,97,114,101,95,102,95,51,51,0),String.fromCharCode(104,95,49,50,95,114,117,108,101,0)], [String.fromCharCode(103,95,57,55,95,114,101,115,101,114,118,101,0),String.fromCharCode(113,95,57,95,111,110,116,97,99,116,115,0)]]);
          let umengW = String.fromCharCode(98,95,51,56,95,97,108,111,99,0);
         productY -= arrowR.size >> (Math.min(Math.abs(2), 5));
         arrowR.set(umengW, 2);
         umengW += `${(umengW == String.fromCharCode(101,0) ? umengW.length : umengW.length)}`;
         if (philippines9) {
            break;
         }
      } while (philippines9 && (!termse.includes(productY)));
      for (let i = 0; i < 3; i++) {
         productY -= 1;
      }
      if (3 < (parseInt(`${productY}`) + termse.length)) {
         productY -= parseInt(`${productY}`) | 2;
      }
         productY += termse.length;
         termse.push(3 >> (Math.min(4, termse.length)));
      applicationn %= Math.max(3, parseInt(`${productY}`));

    onClose()

   for (let j = 0; j < 2; j++) {
       let matcheso: Array<any> = [850, 800];
       let favorited = 4.0;
       let const_0oA = false;
       let m_imageH = String.fromCharCode(97,99,100,101,99,95,56,95,51,56,0);
       let const_qmp = String.fromCharCode(107,95,55,52,95,114,97,100,97,114,0);
      let blacklistR = const_qmp.length >= 8713308;
      do {
          let zoomN = String.fromCharCode(116,95,49,49,95,115,112,97,119,110,0);
          let androida: Array<any> = [67, 640];
         const_qmp = `${const_qmp.length | 1}`;
         zoomN = `${androida.length}`;
         androida = [(zoomN == String.fromCharCode(122,0) ? androida.length : zoomN.length)];
         if (blacklistR) {
            break;
         }
      } while ((!m_imageH.includes(`${const_qmp.length}`)) && blacklistR);
      if (const_qmp == String.fromCharCode(106,0) && m_imageH != String.fromCharCode(116,0)) {
         const_qmp = `${1 | m_imageH.length}`;
      }
      let episodeq = const_0oA ? !const_0oA : const_0oA;
      do {
          let searchg = String.fromCharCode(119,101,108,115,101,110,99,100,101,109,111,95,121,95,49,48,48,0);
          let stationsJ = false;
         const_0oA = stationsJ;
         searchg = `${(searchg == String.fromCharCode(121,0) ? searchg.length : searchg.length)}`;
         if (episodeq) {
            break;
         }
      } while ((const_qmp.length > 1) && episodeq);
      for (let g = 0; g < 3; g++) {
         m_imageH += "1";
      }
      while (const_0oA || const_qmp.length == 4) {
         const_0oA = matcheso.includes(favorited);
         break;
      }
       let modalf = 3;
       let eighteenC = 4;
         matcheso.push(2 + modalf);
          let sportsP = String.fromCharCode(103,109,97,116,99,104,95,102,95,50,51,0);
         m_imageH = `${2 + m_imageH.length}`;
         sportsP += `${sportsP.length * 3}`;
      let mbbanner9 = String.fromCharCode(121,111,57,105,107,98,53,48,0) == m_imageH;
      do {
          let sharea = String.fromCharCode(104,95,54,51,95,111,117,116,101,114,0);
          let incidentd = 3;
          let hook4 = String.fromCharCode(116,102,114,97,95,118,95,53,55,0);
          let middleq = 4.0;
         m_imageH += `${modalf & parseInt(`${favorited}`)}`;
         sharea += `${parseInt(`${middleq}`) >> (Math.min(hook4.length, 1))}`;
         incidentd += parseInt(`${middleq}`);
         hook4 += `${sharea.length}`;
         if (mbbanner9) {
            break;
         }
      } while ((1 >= (matcheso.length & m_imageH.length)) && mbbanner9);
      if ((favorited / (Math.max(parseFloat(`${m_imageH.length}`), 9))) > 4.64) {
         m_imageH = `${parseInt(`${favorited}`) - 1}`;
      }
       let agreement5 = String.fromCharCode(108,95,57,54,95,99,104,97,99,104,97,112,111,108,121,0);
       let invited = String.fromCharCode(109,101,116,114,105,99,95,106,95,53,53,0);
          let combineO = String.fromCharCode(119,114,105,116,97,98,108,101,95,50,95,56,50,0);
          let corex: Array<any> = [String.fromCharCode(101,116,104,101,114,115,99,97,110,95,49,95,52,53,0), String.fromCharCode(110,95,51,55,95,115,117,98,118,105,101,119,0)];
         eighteenC ^= 3;
         combineO = `${combineO.length / 1}`;
         corex.push(corex.length % (Math.max(3, combineO.length)));
       let tumbnailW = String.fromCharCode(98,117,102,95,109,95,53,52,0);
         m_imageH = `${(m_imageH == String.fromCharCode(68,0) ? parseInt(`${favorited}`) : m_imageH.length)}`;
      if (invited.endsWith(`${const_0oA}`)) {
          let filledg = String.fromCharCode(120,95,55,95,105,110,118,0);
         const_0oA = 10.20 <= favorited || matcheso.length <= 16;
         filledg = `${filledg.length ^ 3}`;
      }
      activer += `${(String.fromCharCode(52,0) == const_qmp ? const_qmp.length : (connection1 ? 2 : 5))}`;
   }
   for (let o = 0; o < 3; o++) {
       let anythinkS = String.fromCharCode(117,110,114,101,115,111,108,118,101,100,95,116,95,56,51,0);
       let traminiB = 5.0;
         traminiB -= parseFloat(`${1}`);
          let chinaD = 0;
          let short_wzD = String.fromCharCode(98,95,53,53,95,115,101,99,117,114,101,100,0);
         anythinkS += `${1 & parseInt(`${traminiB}`)}`;
         chinaD += 3 % (Math.max(3, short_wzD.length));
         short_wzD += `${short_wzD.length}`;
         traminiB += parseFloat(`${1 - parseInt(`${traminiB}`)}`);
      for (let k = 0; k < 1; k++) {
          let reactnativejs1 = 5;
          let match8: Array<any> = [337, 592, 653];
          let launcherM = String.fromCharCode(109,97,120,98,117,114,115,116,95,49,95,49,48,0);
          let controlsY = 0.0;
         anythinkS += `${parseInt(`${controlsY}`)}`;
         reactnativejs1 -= 1;
         match8.push((launcherM == String.fromCharCode(71,0) ? match8.length : launcherM.length));
         controlsY -= parseFloat(`${2}`);
      }
      for (let n = 0; n < 3; n++) {
         anythinkS += `${parseInt(`${traminiB}`)}`;
      }
      for (let d = 0; d < 1; d++) {
         traminiB -= parseFloat(`${2 + parseInt(`${traminiB}`)}`);
      }
      applicationn %= Math.max(5, activer.length << (Math.min(toponq.length, 4)));
   }
      toponq += `${toponq.length << (Math.min(5, Math.abs(basketballS)))}`;
   for (let r = 0; r < 2; r++) {
      basketballS *= 2 * basketballS;
   }
       let crossk: Array<any> = [767, 929];
       let entryA = false;
         crossk = [(crossk.length | (entryA ? 5 : 5))];
      while (!entryA) {
         entryA = ((crossk.length & (entryA ? 69 : crossk.length)) == 69);
         break;
      }
         entryA = crossk.length > 56 && !entryA;
         crossk.push(crossk.length);
      if (!entryA) {
         crossk.push(crossk.length);
      }
      let detailsG = entryA ? !entryA : entryA;
      do {
         entryA = crossk.includes(entryA);
         if (detailsG) {
            break;
         }
      } while ((entryA) && detailsG);
      basketballS -= 3;
   for (let q = 0; q < 3; q++) {
      basketballS += 3;
   }
   let brightnesse = activer.length >= 8197453;
   do {
       let mimoG = 5.0;
       let sinaP = 2.0;
       let uploadD = 4;
       let sigmobP = String.fromCharCode(120,95,55,52,95,112,115,110,114,0);
       let fillM = String.fromCharCode(105,110,110,100,101,114,95,114,95,53,51,0);
      let buffer_ = 8122543.0 >= sinaP;
      do {
         sinaP *= parseFloat(`${uploadD}`);
         if (buffer_) {
            break;
         }
      } while (((5.38 + sinaP) <= 2.96 && 1 <= (4 + parseInt(`${sinaP}`))) && buffer_);
         sigmobP += `${1 & uploadD}`;
          let searchgV: Map<any, any> = new Map([[String.fromCharCode(112,108,97,110,97,114,95,54,95,49,48,48,0),false ], [String.fromCharCode(119,95,49,54,95,109,117,116,97,98,108,101,0),true ]]);
          let crownJ = String.fromCharCode(114,97,105,110,95,116,95,50,54,0);
          let selected_ = true;
         sinaP *= parseFloat(`${3}`);
         searchgV = new Map([[`${searchgV.size}`, (String.fromCharCode(75,0) == crownJ ? crownJ.length : searchgV.size)]]);
         selected_ = searchgV.size <= 20;
      while (sigmobP.startsWith(`${mimoG}`)) {
         sigmobP += `${parseInt(`${sinaP}`)}`;
         break;
      }
          let ajax1 = false;
          let flyera = String.fromCharCode(120,95,56,54,95,97,117,116,104,101,110,116,105,99,97,116,105,111,110,0);
         sinaP /= Math.max(3, parseFloat(`${sigmobP.length}`));
         ajax1 = flyera == flyera;
         fillM += `${fillM.length & parseInt(`${mimoG}`)}`;
          let streamingB = String.fromCharCode(112,114,111,116,101,99,116,105,111,110,95,116,95,54,54,0);
         fillM += `${parseInt(`${mimoG}`) * streamingB.length}`;
         mimoG *= 3;
      activer += "3";
      if (brightnesse) {
         break;
      }
   } while ((!circleA.includes(activer)) && brightnesse);
   if (basketballS == 1) {
      basketballS >>= Math.min(Math.abs(((connection1 ? 4 : 1))), 3);
   }
   if ((2 & applicationn) > 5) {
      basketballS &= 3;
   }
   for (let b = 0; b < 3; b++) {
       let temperature3: Array<any> = [512, 861];
       let nterstitialC: Map<any, any> = new Map([[String.fromCharCode(115,112,108,97,116,95,102,95,48,0),String.fromCharCode(97,95,55,52,95,117,110,105,102,111,114,109,0)], [String.fromCharCode(109,97,106,111,114,95,52,95,53,56,0),String.fromCharCode(114,95,53,56,95,112,114,101,99,97,108,99,0)], [String.fromCharCode(104,95,49,49,95,105,99,111,110,0),String.fromCharCode(110,95,52,48,95,101,98,109,108,0)]]);
       let screenu = String.fromCharCode(99,97,99,104,105,110,103,95,99,95,51,52,0);
       let optionsD = String.fromCharCode(99,111,110,116,97,105,110,101,114,95,116,95,51,52,0);
       let applicationG = true;
       let security_ = String.fromCharCode(102,95,50,54,0);
         nterstitialC = new Map([[`${nterstitialC.size}`, (String.fromCharCode(107,0) == screenu ? screenu.length : nterstitialC.size)]]);
      for (let k = 0; k < 1; k++) {
         security_ += "1";
      }
         optionsD += `${temperature3.length + screenu.length}`;
      if (!applicationG && 3 == optionsD.length) {
         applicationG = 43 < optionsD.length;
      }
         optionsD = `${3 ^ security_.length}`;
      let closeh = applicationG ? !applicationG : applicationG;
      do {
         applicationG = (29 < ((!applicationG ? nterstitialC.size : 7) % (Math.max(3, nterstitialC.size))));
         if (closeh) {
            break;
         }
      } while (closeh && (3 < (2 * temperature3.length)));
         security_ = `${temperature3.length}`;
          let mbbannerY = 4;
         optionsD += `${screenu.length}`;
         mbbannerY >>= Math.min(4, Math.abs(1));
      while (!applicationG || screenu.length <= 1) {
         applicationG = !applicationG;
         break;
      }
       let profileb = String.fromCharCode(114,95,49,48,95,115,105,108,101,110,116,0);
       let hoverG = String.fromCharCode(109,105,100,101,113,117,97,108,105,122,101,114,95,104,95,50,53,0);
         nterstitialC.set(profileb, temperature3.length);
         security_ = `${(2 >> (Math.min(4, Math.abs((applicationG ? 2 : 1)))))}`;
       let clockY = String.fromCharCode(111,95,48,95,115,114,116,99,112,0);
       let gesturesU = String.fromCharCode(100,117,112,108,101,120,95,102,95,51,55,0);
      while (optionsD.length < screenu.length) {
          let episodeN = false;
          let episodesm: Array<any> = [String.fromCharCode(117,95,52,49,95,98,110,109,112,105,0), String.fromCharCode(102,97,108,115,101,95,54,95,51,48,0)];
          let androidH: Map<any, any> = new Map([[String.fromCharCode(99,111,117,110,116,101,114,115,95,52,95,49,49,0),341], [String.fromCharCode(108,111,111,115,101,95,48,95,49,57,0),663], [String.fromCharCode(114,101,112,114,101,112,97,114,101,95,118,95,54,0),70]]);
          let weiboL = String.fromCharCode(109,105,120,105,110,95,108,95,53,52,0);
         optionsD = "2";
         episodeN = episodesm.length > 24 && 24 > androidH.size;
         episodesm = [1];
         androidH.set(`${episodeN}`, (androidH.size / (Math.max(1, (episodeN ? 1 : 3)))));
         weiboL += "2";
         break;
      }
      connection1 = !connection1;
   }
      toponq += `${circleA.length * cornerC.length}`;
   for (let x = 0; x < 1; x++) {
      basketballS &= 2;
   }
    navigator.navigate('邀请');
  }, [])


  return (

    <VipPrivilegeModal
      showDarkBackdrop={showDarkBackdrop}
      addPaddingTop={addPaddingTop}
      titleText={"VIP升级权益"}
      benefitsTextsArray={['高清体育赛事  零时差感受赛场激情', '海量福利视频 你想要的这里都有', '高清画质 无广告观影体验']}
      imageGradientColors={['#FF9AD000', '#EB59A7']}
      contentGradientColors={['#EA58A7', '#DE2575']}
      coverBackground={adultBg}
      coverImage={adultModels}
      onPurchase={handleOnPurchase}
      onInvite={handleOnInvite}
      onClose={onClose}
      showBlur={showBlur}
      showCondition={showCondition}
    />

  )
}