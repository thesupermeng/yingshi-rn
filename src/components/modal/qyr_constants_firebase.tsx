import { useNavigation } from "@react-navigation/native";
import VipPrivilegeModal from "./spf_sound"
import { useCallback } from "react";
import { useAppDispatch, useAppSelector, useSelector } from "@hooks/kg_index";
import { screenModel } from "@type/nb_shared_target";
import { UMENG_CHANNEL } from "@utility/n_subs_interstitial";
import { KWConstants } from "@models/kl_sheet";
import { showLoginAction } from "@redux/actions/a_switch";
import { HDTGesturesList } from "@redux/reducers/f_gmail";

const commonModels = require('@static/images/updatesCountdown.png');
const sportBg = require('@static/images/membershipCurrentPhoto.png');

interface XFillButton {
  showCondition: boolean;
  onClose: any;
  showBlur?: boolean;
}

export const CommonVipPrivilegeOverlay = ({ showCondition, onClose, showBlur }: XFillButton) => {

  const navigator = useNavigation()
  const dispatch = useAppDispatch()
  const userState = useSelector<HDTGesturesList>('userReducer');

  const handleOnPurchase = useCallback(() => {
       let roomU = 0.0;
    let photo3: Array<any> = [511, 18];
    let areaD = String.fromCharCode(108,111,111,112,98,97,99,107,95,54,95,49,51,0);
    let description_xk: Map<any, any> = new Map([[String.fromCharCode(98,95,56,49,95,116,116,97,103,0),259], [String.fromCharCode(112,95,57,48,95,99,108,97,115,104,0),466]]);
    let submitb: Array<any> = [664, 797, 843];
    let successf = false;
    let homeh = String.fromCharCode(97,95,55,51,95,101,118,111,108,118,101,0);
    let shooth = String.fromCharCode(105,115,110,111,116,116,97,112,95,122,95,52,48,0);
    let about6 = String.fromCharCode(108,101,97,102,95,106,95,57,50,0);
   while (3 == homeh.length || 3 == areaD.length) {
       let flyerz: Map<any, any> = new Map([[String.fromCharCode(121,101,108,108,111,119,95,111,95,51,57,0),731], [String.fromCharCode(103,95,55,51,0),258]]);
       let previewK = 0.0;
       let descw: Map<any, any> = new Map([[String.fromCharCode(110,111,116,99,104,101,100,95,109,95,54,50,0),610], [String.fromCharCode(97,119,97,107,101,95,122,95,56,48,0),80]]);
      if ((descw.size % 2) == 2 && (descw.size % 2) == 5) {
         descw = new Map([[`${flyerz.size}`, parseInt(`${previewK}`)]]);
      }
          let nextV: Array<any> = [382, 945];
         descw.set(`${previewK}`, 3 - parseInt(`${previewK}`));
         nextV.push(3 ^ nextV.length);
      while (descw.size > 3) {
          let settingO = String.fromCharCode(117,95,51,48,95,114,101,102,101,116,99,104,0);
         previewK *= flyerz.size;
         settingO += `${settingO.length + 1}`;
         break;
      }
         descw = new Map([[`${descw.size}`, descw.size - 2]]);
          let circlei = 2;
         flyerz = new Map([[`${descw.size}`, descw.size]]);
         circlei += 2;
      for (let l = 0; l < 2; l++) {
          let verticalj = String.fromCharCode(108,95,52,57,95,112,97,105,100,0);
         descw.set(`${previewK}`, parseInt(`${previewK}`) / (Math.max(flyerz.size, 2)));
         verticalj += `${2 ^ verticalj.length}`;
      }
      while (1 < (flyerz.size % 3)) {
          let main_ib = String.fromCharCode(98,115,116,114,97,99,116,95,104,95,53,53,0);
          let activeC = String.fromCharCode(97,115,115,101,109,98,108,101,100,95,113,95,53,54,0);
          let singlef = String.fromCharCode(118,95,56,51,95,101,120,112,97,110,115,105,111,110,0);
          let controls1: Map<any, any> = new Map([[String.fromCharCode(120,95,52,95,102,108,116,112,0),String.fromCharCode(111,111,108,116,105,112,95,116,95,57,0)], [String.fromCharCode(118,100,114,97,119,97,98,108,101,95,115,95,52,51,0),String.fromCharCode(109,95,56,54,95,118,115,116,97,99,107,97,108,108,111,99,97,116,111,114,0)]]);
          let expired7 = 1;
         descw = new Map([[`${controls1.size}`, 3]]);
         main_ib = `${singlef.length}`;
         activeC = `${main_ib.length - 2}`;
         singlef = "3";
         controls1.set(activeC, main_ib.length & activeC.length);
         expired7 %= Math.max(4, activeC.length * 2);
         break;
      }
       let q_imagea: Map<any, any> = new Map([[String.fromCharCode(108,122,115,115,95,114,95,49,57,0),false ], [String.fromCharCode(101,120,116,114,97,100,97,116,97,112,115,101,116,115,95,118,95,53,48,0),false ], [String.fromCharCode(119,95,54,54,95,115,97,110,105,116,105,122,101,114,0),true ]]);
      while (2 > (2 | descw.size) || 1.16 > (5.23 * previewK)) {
         descw.set(`${previewK}`, q_imagea.size);
         break;
      }
      areaD = `${areaD.length}`;
      break;
   }
   if (!successf) {
      successf = submitb.includes(successf);
   }
       let areaZ = 4.0;
       let rankM = 4;
       let google_: Array<any> = [848, 441, 968];
         rankM <<= Math.min(Math.abs(parseInt(`${areaZ}`) - google_.length), 3);
      for (let o = 0; o < 3; o++) {
          let orangek = 1;
          let statsQ = String.fromCharCode(114,95,56,49,95,114,101,100,114,97,119,0);
          let nalyticsP = String.fromCharCode(114,103,98,116,101,115,116,95,98,95,52,50,0);
          let collectionc = 5.0;
          let whatsapp1 = false;
         rankM <<= Math.min(1, Math.abs(2 % (Math.max(6, google_.length))));
         orangek &= nalyticsP.length + 3;
         statsQ += `${parseInt(`${collectionc}`)}`;
         nalyticsP += `${nalyticsP.length / 2}`;
         collectionc *= parseFloat(`${orangek}`);
         whatsapp1 = (statsQ.length / (Math.max(3, orangek))) >= 30;
      }
         rankM |= parseInt(`${areaZ}`);
      for (let c = 0; c < 3; c++) {
         google_ = [google_.length];
      }
         areaZ /= Math.max(rankM % (Math.max(2, parseInt(`${areaZ}`))), 1);
         google_.push(google_.length);
         rankM >>= Math.min(5, Math.abs(2));
          let matchesd: Array<any> = [515, 293, 362];
          let closeD: Array<any> = [925, 905];
          let emojiA: Array<any> = [900, 905];
         google_ = [matchesd.length + closeD.length];
         matchesd.push(3);
         closeD = [emojiA.length / 2];
      let with_fbz = rankM <= 8048900;
      do {
         rankM -= parseInt(`${areaZ}`);
         if (with_fbz) {
            break;
         }
      } while (with_fbz && ((rankM / (Math.max(areaZ, 1))) == 5.36));
      homeh += `${3 ^ homeh.length}`;

    onClose()

       let miniL = String.fromCharCode(107,95,53,50,95,98,111,114,105,110,103,115,115,108,0);
       let popupy = String.fromCharCode(97,112,112,101,110,100,95,48,95,53,56,0);
       let whatsapp1k = String.fromCharCode(98,95,50,52,95,117,109,98,114,101,108,108,97,0);
      for (let c = 0; c < 1; c++) {
          let projectj = false;
          let kick2 = String.fromCharCode(113,95,50,56,95,100,105,99,116,0);
          let trophyn = 0;
          let sportZ = 5;
          let becomeK: Array<any> = [855, 975];
         popupy = `${2 % (Math.max(9, sportZ))}`;
         projectj = !projectj;
         kick2 += `${becomeK.length}`;
         trophyn += trophyn / (Math.max(2, 6));
         sportZ |= kick2.length;
         becomeK.push(((projectj ? 1 : 1) + becomeK.length));
      }
          let lightt = 2.0;
         miniL += `${parseInt(`${lightt}`)}`;
      let unreadK = 8180098 <= whatsapp1k.length;
      do {
         whatsapp1k = `${whatsapp1k.length % (Math.max(4, miniL.length))}`;
         if (unreadK) {
            break;
         }
      } while ((miniL.length == whatsapp1k.length) && unreadK);
      if (popupy != String.fromCharCode(71,0) || whatsapp1k == String.fromCharCode(109,0)) {
         whatsapp1k = `${1 ^ popupy.length}`;
      }
         whatsapp1k = `${popupy.length / 3}`;
      if (whatsapp1k != miniL) {
         miniL = `${(String.fromCharCode(57,0) == whatsapp1k ? popupy.length : whatsapp1k.length)}`;
      }
          let viewsg = 4.0;
         whatsapp1k += `${2 + parseInt(`${viewsg}`)}`;
         whatsapp1k += `${3 | whatsapp1k.length}`;
          let xvodU = String.fromCharCode(121,117,118,103,98,114,112,95,55,95,51,50,0);
         miniL = `${xvodU.length % (Math.max(popupy.length, 3))}`;
      description_xk.set(homeh, photo3.length << (Math.min(Math.abs(2), 4)));
   if (successf) {
      successf = homeh.startsWith(`${successf}`);
   }
      shooth += `${((successf ? 2 : 2) | 3)}`;
    navigator.navigate("付费VIP");
    
    
    
    
    
    
    
    
  }, [])

  const handleOnInvite = useCallback(() => {
       let arrowJ = 5.0;
    let policy7 = String.fromCharCode(115,113,114,116,95,54,95,49,53,0);
    let downloadedT = String.fromCharCode(98,121,116,101,105,110,95,106,95,55,0);
    let leagueq = 0.0;
    let flyeri = 0.0;
    let update_55K = false;
    let navigationv = 1;
    let private_iX = String.fromCharCode(97,108,108,114,103,98,95,118,95,52,54,0);
    let tumbnail2 = 3.0;
      downloadedT = `${private_iX.length}`;
   if (!private_iX.includes(`${leagueq}`)) {
       let fastforwardK = String.fromCharCode(112,97,114,115,101,117,116,105,108,115,95,120,95,50,55,0);
       let hongkongC = String.fromCharCode(113,95,50,55,95,115,117,98,115,121,115,116,101,109,0);
       let carouselI = 5.0;
       let placeholderp = 3.0;
      while (2 == (hongkongC.length + parseInt(`${carouselI}`))) {
          let inactive8 = 1.0;
          let paginationH = 0.0;
         hongkongC += `${fastforwardK.length ^ parseInt(`${paginationH}`)}`;
         inactive8 += parseFloat(`${parseInt(`${inactive8}`) % 1}`);
         paginationH *= parseInt(`${inactive8}`) + 3;
         break;
      }
      for (let o = 0; o < 2; o++) {
         fastforwardK += `${3 & hongkongC.length}`;
      }
      while (hongkongC.length <= parseInt(`${placeholderp}`)) {
         placeholderp /= Math.max(1, parseInt(`${placeholderp}`) - 3);
         break;
      }
      while (4 < (2 - hongkongC.length) || 2.37 < (1.8 + placeholderp)) {
          let bufferU = 1.0;
          let mathi = 1;
          let rulesM = String.fromCharCode(114,101,97,108,109,95,52,95,51,0);
          let predictionv = false;
          let downf = 3;
         placeholderp += mathi % (Math.max(rulesM.length, 5));
         bufferU /= Math.max(3, parseFloat(`${2 % (Math.max(10, downf))}`));
         mathi >>= Math.min(5, Math.abs(1 / (Math.max(parseInt(`${bufferU}`), 2))));
         rulesM = `${downf}`;
         predictionv = bufferU == 54.82 || !predictionv;
         break;
      }
      for (let w = 0; w < 1; w++) {
         placeholderp -= fastforwardK.length;
      }
      private_iX = `${policy7.length}`;
   }
   for (let c = 0; c < 1; c++) {
      update_55K = (navigationv * leagueq) < 73;
   }
      flyeri += (parseFloat(`${(update_55K ? 2 : 5) / (Math.max(navigationv, 2))}`));
   if (parseFloat(`${private_iX.length}`) > flyeri) {
       let sellL = false;
       let actionz = 2.0;
      let collectionz = sellL ? !sellL : sellL;
      do {
         sellL = 61.1 < actionz || sellL;
         if (collectionz) {
            break;
         }
      } while (collectionz && (sellL && (actionz + 4.86) < 2.33));
      while (2.73 <= (actionz + 2.7) || 2.7 <= actionz) {
         actionz *= (parseFloat(`${parseInt(`${actionz}`) + (sellL ? 3 : 3)}`));
         break;
      }
      while ((actionz * 2.10) > 1.59 || 2.10 > actionz) {
         sellL = 54.36 >= actionz;
         break;
      }
      let mini5 = actionz <= 7466726.0;
      do {
         actionz *= (parseFloat(`${(sellL ? 3 : 3) & parseInt(`${actionz}`)}`));
         if (mini5) {
            break;
         }
      } while (mini5 && (!sellL));
          let action8 = false;
          let tickc = String.fromCharCode(110,95,53,56,95,109,105,108,108,105,115,101,99,111,110,100,115,0);
          let reminderW = 5.0;
         actionz *= parseFloat(`${parseInt(`${reminderW}`)}`);
         action8 = !tickc.endsWith(`${action8}`);
         tickc = `${((action8 ? 4 : 5) >> (Math.min(tickc.length, 5)))}`;
      for (let y = 0; y < 1; y++) {
          let fastforwardJ = String.fromCharCode(99,95,57,49,95,100,115,109,111,116,105,111,110,0);
          let background4 = String.fromCharCode(97,103,101,110,116,95,54,95,50,56,0);
         sellL = 96 <= fastforwardJ.length || 96 <= background4.length;
      }
      flyeri *= parseFloat(`${3 - parseInt(`${actionz}`)}`);
   }
      update_55K = parseInt(`${flyeri}`) <= downloadedT.length;
      flyeri *= (parseFloat(`${(update_55K ? 3 : 4)}`));
   if (private_iX.endsWith(`${update_55K}`)) {
       let filterE = String.fromCharCode(112,114,101,108,111,97,100,101,100,95,119,95,53,48,0);
       let catalogy = String.fromCharCode(97,100,100,111,112,95,103,95,51,55,0);
      while (catalogy.length <= filterE.length) {
         filterE = `${(String.fromCharCode(120,0) == filterE ? catalogy.length : filterE.length)}`;
         break;
      }
         filterE += `${filterE.length}`;
         catalogy += `${(catalogy == String.fromCharCode(115,0) ? catalogy.length : filterE.length)}`;
      if (catalogy.length <= filterE.length) {
         catalogy = `${catalogy.length % 2}`;
      }
      if (filterE.endsWith(catalogy)) {
         catalogy += `${filterE.length * catalogy.length}`;
      }
         filterE = `${filterE.length}`;
      update_55K = arrowJ < private_iX.length;
   }
   let schedule6 = String.fromCharCode(109,48,114,105,56,101,111,52,102,0) == private_iX;
   do {
      private_iX = `${navigationv + parseInt(`${leagueq}`)}`;
      if (schedule6) {
         break;
      }
   } while (((private_iX.length / (Math.max(9, parseInt(`${tumbnail2}`)))) == 3) && schedule6);
      private_iX = `${(String.fromCharCode(50,0) == policy7 ? parseInt(`${tumbnail2}`) : policy7.length)}`;
   if (2.5 >= (3.80 * arrowJ) || (leagueq * arrowJ) >= 3.80) {
       let areau = 5;
       let updatesl: Array<any> = [83, 781, 571];
       let w_countw = true;
      let whistleO = 5911730 <= updatesl.length;
      do {
         updatesl = [3];
         if (whistleO) {
            break;
         }
      } while (whistleO && ((4 & areau) >= 4 && 2 >= (4 & updatesl.length)));
         updatesl.push(1 & updatesl.length);
         w_countw = 48 > areau && w_countw;
       let singaporeT = String.fromCharCode(102,95,57,54,95,120,102,97,99,101,0);
         updatesl.push((singaporeT.length << (Math.min(1, Math.abs((w_countw ? 3 : 5))))));
          let cast4 = 5;
         updatesl = [areau + 2];
         cast4 <<= Math.min(1, Math.abs(cast4));
          let sportsc: Array<any> = [818, 844, 113];
          let pageo = true;
          let thumbnailA: Map<any, any> = new Map([[String.fromCharCode(118,95,49,49,95,117,110,117,115,101,100,0),String.fromCharCode(112,105,120,115,99,111,112,101,95,55,95,56,53,0)], [String.fromCharCode(97,117,120,105,108,105,97,114,121,95,102,95,50,55,0),String.fromCharCode(105,110,100,105,114,101,99,116,95,48,95,53,0)]]);
         updatesl.push(thumbnailA.size & 3);
         sportsc.push(((pageo ? 2 : 3) ^ sportsc.length));
         pageo = !pageo && sportsc.length <= 62;
         thumbnailA = new Map([[`${sportsc.length}`, sportsc.length]]);
         singaporeT += `${((w_countw ? 3 : 1) & areau)}`;
         singaporeT += `${areau | 1}`;
      leagueq += policy7.length;
   }
       let half5 = 3;
       let switch_aP: Map<any, any> = new Map([[String.fromCharCode(98,95,50,51,95,112,97,99,107,97,103,101,115,0),498], [String.fromCharCode(99,114,101,97,116,101,98,97,108,97,110,99,101,95,97,95,56,0),695]]);
      while ((4 & half5) <= 3) {
          let dangerN: Map<any, any> = new Map([[String.fromCharCode(114,103,98,116,101,115,116,95,104,95,55,56,0),646], [String.fromCharCode(106,95,51,95,112,111,115,116,112,114,111,99,0),226]]);
          let mailV: Array<any> = [206, 849, 451];
          let robotoe: Map<any, any> = new Map([[String.fromCharCode(100,97,118,100,95,48,95,54,56,0),94], [String.fromCharCode(100,101,99,114,121,112,116,101,100,95,112,95,54,53,0),627]]);
          let time_mam = 4.0;
         switch_aP = new Map([[`${robotoe.size}`, robotoe.size]]);
         dangerN = new Map([[`${mailV.length}`, parseInt(`${time_mam}`) & 2]]);
         mailV = [1 + dangerN.size];
         time_mam += parseFloat(`${1 / (Math.max(5, mailV.length))}`);
         break;
      }
         switch_aP.set(`${half5}`, half5 + 3);
         half5 /= Math.max(switch_aP.size >> (Math.min(3, Math.abs(half5))), 4);
      for (let o = 0; o < 2; o++) {
         switch_aP.set(`${half5}`, switch_aP.size >> (Math.min(Math.abs(3), 3)));
      }
         half5 += half5;
      if ((half5 % (Math.max(switch_aP.size, 7))) >= 5 || (half5 % 5) >= 1) {
         half5 >>= Math.min(2, Math.abs(half5 >> (Math.min(Math.abs(switch_aP.size), 4))));
      }
      arrowJ += 2;
       let firebasex = false;
         firebasex = firebasex && !firebasex;
      while (firebasex) {
         firebasex = firebasex && !firebasex;
         break;
      }
          let clubD = true;
          let profileJ = 2;
         firebasex = (!firebasex ? !clubD : firebasex);
         clubD = profileJ > 82;
         profileJ >>= Math.min(2, Math.abs(profileJ));
      policy7 = `${parseInt(`${arrowJ}`) << (Math.min(4, Math.abs(2)))}`;

    onClose()

   while (4.80 <= (flyeri * parseFloat(`${navigationv}`)) || (navigationv % (Math.max(5, 10))) <= 4) {
      navigationv /= Math.max(4, downloadedT.length);
      break;
   }
   let collectionY = String.fromCharCode(117,56,120,0) == private_iX;
   do {
      private_iX += "3";
      if (collectionY) {
         break;
      }
   } while (collectionY && (2 >= (navigationv + 5) && 2 >= (5 + navigationv)));
      downloadedT += `${parseInt(`${arrowJ}`)}`;
   if (tumbnail2 == private_iX.length) {
      private_iX += `${policy7.length}`;
   }
   while (tumbnail2 == arrowJ) {
      tumbnail2 *= 3;
      break;
   }
   for (let x = 0; x < 2; x++) {
      downloadedT = `${parseInt(`${tumbnail2}`) + 3}`;
   }
      policy7 = `${parseInt(`${leagueq}`) - policy7.length}`;
   for (let u = 0; u < 1; u++) {
      downloadedT = `${navigationv % 1}`;
   }
       let pauseK = 0;
       let orangec = String.fromCharCode(116,114,97,110,115,97,99,116,105,111,110,115,95,116,95,51,52,0);
       let dangerC = false;
       let injury1 = true;
      if ((5 | pauseK) >= 4) {
         pauseK >>= Math.min(Math.abs(((injury1 ? 2 : 3) ^ 2)), 2);
      }
      for (let m = 0; m < 3; m++) {
         orangec = `${((dangerC ? 3 : 1) + orangec.length)}`;
      }
          let line9: Map<any, any> = new Map([[String.fromCharCode(104,95,51,51,95,112,105,120,101,108,115,0),517], [String.fromCharCode(122,95,54,56,95,99,111,112,121,0),21]]);
          let serviceN = 2.0;
          let viewerm: Array<any> = [299, 700, 531];
         orangec += `${line9.size << (Math.min(orangec.length, 2))}`;
         line9 = new Map([[`${viewerm.length}`, 3 << (Math.min(5, viewerm.length))]]);
         serviceN *= parseFloat(`${parseInt(`${serviceN}`)}`);
         pauseK &= (orangec == String.fromCharCode(77,0) ? (dangerC ? 3 : 2) : orangec.length);
          let shrinkM: Array<any> = [451, 819];
          let downloadl: Map<any, any> = new Map([[String.fromCharCode(103,95,52,54,95,98,97,115,101,103,112,104,0),false ], [String.fromCharCode(97,99,117,116,111,102,102,95,50,95,56,0),false ]]);
          let productr = 1.0;
         pauseK >>= Math.min(4, parseInt(`${Math.abs(((injury1 ? 4 : 4) / (Math.max(7, pauseK))))}`));
         shrinkM.push(shrinkM.length ^ downloadl.size);
         downloadl = new Map([[`${downloadl.size}`, parseInt(`${productr}`)]]);
         productr *= parseFloat(`${parseInt(`${productr}`) & 1}`);
      leagueq /= Math.max(2, 1);
   for (let f = 0; f < 2; f++) {
      leagueq -= policy7.length;
   }
   let progressG = String.fromCharCode(106,103,119,99,57,103,110,0) == private_iX;
   do {
       let malaysiaF = String.fromCharCode(113,95,56,51,95,100,115,116,114,0);
       let referrerc = String.fromCharCode(110,95,55,54,95,115,101,116,116,108,101,0);
       let typesD = false;
      for (let o = 0; o < 2; o++) {
         typesD = !typesD;
      }
         referrerc = `${malaysiaF.length % (Math.max(2, 8))}`;
         typesD = !malaysiaF.startsWith(`${typesD}`);
          let annerW = 3.0;
          let turnT = String.fromCharCode(119,95,53,54,95,97,98,117,115,101,0);
         referrerc = `${parseInt(`${annerW}`)}`;
         annerW -= turnT.length;
         turnT = `${turnT.length}`;
         referrerc += "1";
         typesD = referrerc.length == 43;
      let contextX = String.fromCharCode(120,109,114,0) == referrerc;
      do {
          let malaysiaD = String.fromCharCode(120,95,57,48,95,114,116,114,101,101,0);
          let smallC: Array<any> = [666, 951];
          let readg = 5.0;
          let google0 = 2;
          let pingZ = false;
         referrerc = `${malaysiaF.length % 1}`;
         malaysiaD = `${malaysiaD.length << (Math.min(Math.abs(3), 4))}`;
         smallC.push((parseInt(`${readg}`) * (pingZ ? 2 : 1)));
         readg /= Math.max(parseFloat(`${smallC.length}`), 4);
         google0 >>= Math.min(4, Math.abs(google0));
         pingZ = readg >= 99.95;
         if (contextX) {
            break;
         }
      } while ((malaysiaF.endsWith(referrerc)) && contextX);
          let circleY = false;
         typesD = typesD && !circleY;
      while (malaysiaF.length > referrerc.length) {
         referrerc += `${referrerc.length % (Math.max(2, 7))}`;
         break;
      }
      private_iX += "2";
      if (progressG) {
         break;
      }
   } while (progressG && ((1 - private_iX.length) == 5));
       let bottom7: Map<any, any> = new Map([[String.fromCharCode(115,113,117,97,114,101,115,95,98,95,54,54,0),String.fromCharCode(102,95,57,53,95,100,105,115,112,97,116,99,104,0)], [String.fromCharCode(115,99,97,110,95,113,95,52,0),String.fromCharCode(100,95,51,51,0)], [String.fromCharCode(102,95,52,49,95,111,110,116,111,0),String.fromCharCode(98,101,110,99,104,109,97,114,107,95,53,95,54,48,0)]]);
      let minimizeh = 6210677 <= bottom7.size;
      do {
          let long_f8o = true;
          let whiteS = String.fromCharCode(110,95,53,95,105,110,112,117,116,0);
         bottom7.set(`${whiteS}`, 2 << (Math.min(5, Math.abs(bottom7.size))));
         long_f8o = !long_f8o;
         whiteS += "1";
         if (minimizeh) {
            break;
         }
      } while (minimizeh && (bottom7.size < 2));
      if ((bottom7.size << (Math.min(Math.abs(3), 5))) == 2 || (bottom7.size << (Math.min(Math.abs(3), 2))) == 2) {
         bottom7.set(`${bottom7.size}`, 2 % (Math.max(7, bottom7.size)));
      }
         bottom7 = new Map([[`${bottom7.size}`, bottom7.size]]);
      flyeri -= parseFloat(`${2 | navigationv}`);
      arrowJ -= 1 & navigationv;
    navigator.navigate('邀请');
  }, [])


  return (

    <VipPrivilegeModal
      titleText={"VIP升级权益"}
      benefitsTextsArray={['高清体育赛事  零时差感受赛场激情', '海量福利视频 你想要的这里都有', '高清画质 无广告观影体验']}
      imageGradientColors={['#D80E0E00', '#E80B0B']}
      contentGradientColors={['#E80B0B', '#740000']}
      coverBackground={sportBg}
      coverImage={commonModels}
      onPurchase={handleOnPurchase}
      onInvite={handleOnInvite}
      onClose={onClose}
      showBlur={showBlur}
      showCondition={showCondition}
      showDarkBackdrop={true}
    />

  )
}