import { View, Text, TouchableOpacity, ViewStyle, TextStyle, StyleSheet } from 'react-native';
import { hideAdultModeVip } from '@redux/actions/wawa_related';
import VipModal from './wawa_scorepopsound';
import FastImage from 'react-native-fast-image';
import { useNavigation } from '@react-navigation/native';
import { IconClose } from '../../Sports/assets';
import { useCallback } from 'react';
import CrossIcon from '@static/images/libavfilterDicelogo.svg'
import CloseIcon from '@static/images/notificationfillemptyWhitetickMbjscommon.svg'
import { useAppDispatch, useAppSelector, useSelector } from '@hooks/wawa_root';
import { screenModel } from '@type/wawa_rules';
import { SHOW_ZF_CONST, UMENG_CHANNEL } from '@utility/wawa_iconpointscore';
import { wawaPhoneControls } from '@redux/reducers/wawa_umeng';
import { wawaLibglog } from '@models/wawa_refreshborderless_found';


const AdultVideoVipModal = () => {
       let libavcodeci = String.fromCharCode(111,95,56,51,95,99,111,100,105,110,103,116,121,112,101,0);
    let description_mX: Array<any> = [329, 701];
    let airbnbstarselectedT = 5.0;
    let hookF = 5.0;
    let utilsc = 3;
    let rightI = String.fromCharCode(119,97,108,107,95,53,95,53,51,0);
    let friendsM = String.fromCharCode(102,95,53,95,103,97,117,115,115,105,97,110,105,105,114,100,0);
    let sigmobT: Array<any> = [384, 6];
    let mutedH: Array<any> = [689, 705];
    let videocommonb = true;
    let episodesI = true;
      rightI = `${friendsM.length}`;
       let invite4 = true;
       let chatbotphotoM = 5.0;
      for (let t = 0; t < 3; t++) {
         invite4 = 45.26 == chatbotphotoM;
      }
          let iconnewchatF: Array<any> = [8, 990, 188];
          let inactivek = true;
         invite4 = (45 <= ((!invite4 ? 45 : iconnewchatF.length) / (Math.max(iconnewchatF.length, 5))));
         inactivek = (inactivek ? inactivek : inactivek);
      let libffmpegkitx = invite4 ? !invite4 : invite4;
      do {
         invite4 = chatbotphotoM >= 63.55;
         if (libffmpegkitx) {
            break;
         }
      } while (libffmpegkitx && ((chatbotphotoM / (Math.max(1.16, 3))) < 4.62 && 1.16 < chatbotphotoM));
      let gesturesm = invite4 ? !invite4 : invite4;
      do {
          let resendu = 3.0;
          let airbnbstarselectedH = 0.0;
          let strings4 = 1.0;
         invite4 = resendu <= airbnbstarselectedH;
         resendu -= parseFloat(`${parseInt(`${strings4}`) ^ 3}`);
         airbnbstarselectedH -= parseFloat(`${parseInt(`${strings4}`) ^ 1}`);
         if (gesturesm) {
            break;
         }
      } while ((!invite4) && gesturesm);
      if (invite4 || 5.62 >= (3.61 * chatbotphotoM)) {
          let arrowB = String.fromCharCode(100,111,108,98,121,95,49,95,57,48,0);
          let mode2 = false;
          let libfollyf = 3.0;
          let background3: Map<any, any> = new Map([[String.fromCharCode(99,111,109,112,108,105,99,97,116,105,111,110,95,108,95,56,54,0),555], [String.fromCharCode(115,105,110,102,95,115,95,50,48,0),469], [String.fromCharCode(100,97,117,98,101,99,104,105,101,115,95,119,95,49,50,0),594]]);
          let navigationX = 2;
         chatbotphotoM -= parseFloat(`${2}`);
         arrowB += `${navigationX}`;
         mode2 = null == background3.get(`${navigationX}`);
         libfollyf += parseFloat(`${2}`);
         background3 = new Map([[`${mode2}`, navigationX]]);
      }
         invite4 = !invite4;
      rightI += `${(rightI == String.fromCharCode(68,0) ? utilsc : rightI.length)}`;

  const navigator = useNavigation();

   let reddownarrowF = rightI == String.fromCharCode(55,50,108,0);
   do {
       let predictiondefault6 = String.fromCharCode(109,95,56,57,95,109,112,101,103,116,115,0);
       let castq = String.fromCharCode(117,95,56,52,95,99,111,109,112,108,101,116,105,111,110,0);
          let custom2 = 1;
          let binit_uzr: Array<any> = [653, 119];
         predictiondefault6 += `${binit_uzr.length & 1}`;
         custom2 /= Math.max(custom2 & custom2, 3);
         binit_uzr = [1];
      for (let m = 0; m < 2; m++) {
         castq = `${predictiondefault6.length}`;
      }
       let largesoundg = 2;
       let other1 = 2;
         largesoundg ^= 3 >> (Math.min(4, Math.abs(largesoundg)));
         largesoundg &= 1 ^ other1;
         largesoundg -= other1;
      rightI = `${parseInt(`${airbnbstarselectedT}`) >> (Math.min(friendsM.length, 1))}`;
      if (reddownarrowF) {
         break;
      }
   } while (reddownarrowF && ((utilsc >> (Math.min(rightI.length, 1))) > 3));
   let debugv = 7960126.0 >= hookF;
   do {
       let componentsH: Map<any, any> = new Map([[String.fromCharCode(112,114,105,110,116,101,114,95,52,95,51,49,0),48], [String.fromCharCode(98,95,57,55,95,114,101,109,105,120,0),510]]);
       let webviewj = String.fromCharCode(99,95,51,55,95,100,119,97,114,102,0);
       let libavformatA = 2.0;
       let datay = 0.0;
       let themen = String.fromCharCode(109,95,49,49,95,114,97,116,105,111,115,0);
         componentsH = new Map([[themen, (webviewj == String.fromCharCode(103,0) ? webviewj.length : themen.length)]]);
      for (let f = 0; f < 3; f++) {
         webviewj += `${webviewj.length & parseInt(`${datay}`)}`;
      }
      while (1 == (webviewj.length - 4) || (webviewj.length - parseInt(`${libavformatA}`)) == 4) {
         webviewj += `${parseInt(`${datay}`) & webviewj.length}`;
         break;
      }
         webviewj += `${themen.length | 3}`;
         datay += 2;
      while (themen.length == 2) {
         themen += `${webviewj.length << (Math.min(Math.abs(3), 3))}`;
         break;
      }
       let star5 = 3;
         libavformatA -= parseFloat(`${2 ^ star5}`);
          let homeinactiveA = 2.0;
          let smallsoundD = 1;
          let mimok = 5.0;
         star5 %= Math.max(3, themen.length + 2);
         homeinactiveA *= parseFloat(`${parseInt(`${mimok}`)}`);
         smallsoundD <<= Math.min(5, Math.abs(parseInt(`${homeinactiveA}`)));
         mimok -= smallsoundD;
         datay *= (String.fromCharCode(95,0) == themen ? themen.length : componentsH.size);
         webviewj += `${webviewj.length}`;
      if (5.22 == libavformatA) {
          let questR = true;
          let reducerI = true;
          let redirectQ = 5.0;
          let downloadingF = true;
         datay += ((questR ? 4 : 2) / (Math.max(7, themen.length)));
         questR = !downloadingF || 29.33 <= redirectQ;
         reducerI = downloadingF;
         redirectQ /= Math.max((parseInt(`${redirectQ}`) * (downloadingF ? 5 : 5)), 4);
      }
      while (4 <= (3 | webviewj.length)) {
         webviewj = `${star5 * themen.length}`;
         break;
      }
         libavformatA /= Math.max(4, parseFloat(`${themen.length >> (Math.min(webviewj.length, 2))}`));
      for (let p = 0; p < 2; p++) {
         themen = `${themen.length | 2}`;
      }
      hookF *= parseFloat(`${description_mX.length}`);
      if (debugv) {
         break;
      }
   } while (debugv && (4 == (description_mX.length % 2) || 1 == (description_mX.length & 2)));
  const userState = useSelector<wawaPhoneControls>('userReducer');

       let homeloadingy = 5.0;
       let auto_o1i = 3;
       let data4 = 0;
      while (1 < auto_o1i) {
         data4 *= parseInt(`${homeloadingy}`);
         break;
      }
          let reactnativeratings9 = true;
          let nativeA = 0.0;
         homeloadingy -= parseFloat(`${1 % (Math.max(parseInt(`${homeloadingy}`), 7))}`);
         reactnativeratings9 = !reactnativeratings9;
         nativeA -= (parseFloat(`${(reactnativeratings9 ? 4 : 5) / (Math.max(7, parseInt(`${nativeA}`)))}`));
         data4 |= data4 % 2;
         data4 >>= Math.min(Math.abs(auto_o1i % 1), 4);
          let condensedW = String.fromCharCode(116,102,117,101,108,95,119,95,56,51,0);
          let jingdongm = 3;
          let giflivestreaming1 = String.fromCharCode(110,105,108,115,95,107,95,52,52,0);
         auto_o1i |= data4;
         condensedW += `${giflivestreaming1.length - 3}`;
         jingdongm -= giflivestreaming1.length | condensedW.length;
      let inactiveV = auto_o1i >= 6529982;
      do {
          let sharep: Array<any> = [String.fromCharCode(113,95,55,54,95,102,99,112,117,98,108,105,115,104,0), String.fromCharCode(121,95,57,49,95,115,101,101,107,0), String.fromCharCode(104,95,49,51,95,115,101,113,117,101,110,116,105,97,108,0)];
         auto_o1i %= Math.max(parseInt(`${homeloadingy}`) | data4, 4);
         sharep.push(2 - sharep.length);
         if (inactiveV) {
            break;
         }
      } while (((4 | data4) < 4 || (data4 | auto_o1i) < 4) && inactiveV);
       let libffmpegkitW = 3.0;
         data4 <<= Math.min(Math.abs(2 - auto_o1i), 5);
      let shootnogoalf = libffmpegkitW <= 6312272.0;
      do {
         libffmpegkitW *= parseFloat(`${data4 | 3}`);
         if (shootnogoalf) {
            break;
         }
      } while (((libffmpegkitW / 1.1) == 1.26) && shootnogoalf);
      airbnbstarselectedT *= parseFloat(`${parseInt(`${hookF}`)}`);
       let libjsio = true;
       let moviesV = String.fromCharCode(106,112,101,103,99,111,109,112,95,53,95,49,52,0);
       let minimizeE = String.fromCharCode(112,114,101,105,118,111,117,115,95,97,95,56,51,0);
      let libflipperT = moviesV.length >= 9832791;
      do {
         moviesV = `${moviesV.length}`;
         if (libflipperT) {
            break;
         }
      } while (libflipperT && (moviesV.endsWith(`${libjsio}`)));
         moviesV = `${(minimizeE == String.fromCharCode(117,0) ? minimizeE.length : (libjsio ? 5 : 1))}`;
       let scheduleO = true;
         minimizeE += "3";
      if (1 >= minimizeE.length) {
          let philippinesN = String.fromCharCode(102,95,53,56,95,114,112,99,115,0);
          let notificationE = 1.0;
         minimizeE = `${((scheduleO ? 2 : 3) >> (Math.min(Math.abs(2), 3)))}`;
         philippinesN = `${(String.fromCharCode(106,0) == philippinesN ? parseInt(`${notificationE}`) : philippinesN.length)}`;
         notificationE *= philippinesN.length;
      }
      while (minimizeE.endsWith(`${scheduleO}`)) {
         minimizeE = `${((libjsio ? 5 : 2))}`;
         break;
      }
      while (!moviesV.startsWith(`${libjsio}`)) {
         libjsio = moviesV.length >= 80;
         break;
      }
         moviesV = `${moviesV.length + 3}`;
          let foregroundQ: Array<any> = [904, 998];
          let iconnotificationnewH: Map<any, any> = new Map([[String.fromCharCode(99,111,101,102,115,95,56,95,57,52,0),true ], [String.fromCharCode(114,95,52,50,95,111,108,100,101,114,0),false ]]);
          let left1 = false;
         scheduleO = foregroundQ.includes(libjsio);
         foregroundQ = [iconnotificationnewH.size & 3];
         iconnotificationnewH.set(`${left1}`, 2);
      friendsM = `${parseInt(`${hookF}`) + libavcodeci.length}`;
  const screenState: screenModel = useAppSelector(
    ({ screenReducer }) => screenReducer
  )
  const { adultModeVipShow, adultMode, watchAnytimeAdultMode } = screenState

       let downarrowZ = String.fromCharCode(121,117,118,103,98,114,112,95,110,95,51,51,0);
      while (5 == downarrowZ.length) {
          let moduleV: Array<any> = [String.fromCharCode(108,114,111,110,100,95,110,95,50,54,0), String.fromCharCode(97,114,114,105,118,97,108,95,116,95,53,50,0)];
          let enewsshare3 = 1.0;
          let attributedstringT = 5;
          let minic = true;
         downarrowZ = `${moduleV.length >> (Math.min(3, Math.abs(parseInt(`${enewsshare3}`))))}`;
         moduleV = [attributedstringT & 3];
         enewsshare3 += 3 >> (Math.min(Math.abs(attributedstringT), 1));
         minic = !minic;
         break;
      }
          let orange3 = false;
         downarrowZ += `${3 >> (Math.min(3, downarrowZ.length))}`;
       let whitevideolivel = 4.0;
      airbnbstarselectedT *= parseFloat(`${1}`);
      utilsc &= description_mX.length >> (Math.min(5, Math.abs(utilsc)));
  const dispatch = useAppDispatch()

   while (friendsM.length <= 5) {
      libavcodeci += `${parseInt(`${airbnbstarselectedT}`) ^ 2}`;
      break;
   }
       let playlistT = String.fromCharCode(101,120,112,111,110,101,110,116,105,97,116,105,111,110,95,57,95,50,49,0);
       let gesturesu: Array<any> = [492, 372, 60];
          let mappingY = String.fromCharCode(113,95,55,52,95,115,101,110,115,105,116,105,118,101,0);
          let libffmpegkitE = String.fromCharCode(98,105,110,100,120,95,54,95,50,57,0);
         playlistT += `${gesturesu.length}`;
         mappingY = "2";
         libffmpegkitE += `${(String.fromCharCode(71,0) == mappingY ? libffmpegkitE.length : mappingY.length)}`;
      if ((playlistT.length | 4) < 3 && 4 < (playlistT.length | gesturesu.length)) {
          let projectF = String.fromCharCode(99,111,110,116,105,110,117,101,95,97,95,54,55,0);
         gesturesu.push(projectF.length / 1);
      }
      for (let i = 0; i < 1; i++) {
         playlistT = `${playlistT.length}`;
      }
         gesturesu = [playlistT.length & gesturesu.length];
         gesturesu.push((playlistT == String.fromCharCode(57,0) ? gesturesu.length : playlistT.length));
      let turndownH = 6041237 <= gesturesu.length;
      do {
         gesturesu.push(playlistT.length);
         if (turndownH) {
            break;
         }
      } while (turndownH && (4 < (gesturesu.length % 1)));
      rightI += `${rightI.length}`;

  const isVip = wawaLibglog.fakeIsVip(userState.user);

   if (5 <= rightI.length) {
      rightI += `${utilsc ^ 2}`;
   }
       let subinV = 4.0;
      if (3.5 >= (subinV * 3.37) && 1.35 >= (3.37 * subinV)) {
          let vipsportT: Array<any> = [863, 322, 945];
         subinV *= 2;
         vipsportT = [vipsportT.length];
      }
      if (subinV >= 2.53) {
         subinV *= parseInt(`${subinV}`) & 2;
      }
      if (2.54 >= subinV) {
          let profileframeO = 2;
          let rewardvideoj: Map<any, any> = new Map([[String.fromCharCode(110,95,54,51,95,112,99,97,99,104,101,0),140], [String.fromCharCode(97,95,51,50,95,109,115,118,115,100,101,112,101,110,100,0),171]]);
          let modules2 = String.fromCharCode(120,95,54,52,95,114,101,109,97,112,0);
          let shootyesgoalW = String.fromCharCode(108,111,119,114,101,115,95,118,95,49,48,0);
         subinV -= rewardvideoj.size;
         profileframeO <<= Math.min(1, Math.abs(profileframeO | 3));
         rewardvideoj = new Map([[`${profileframeO}`, profileframeO / (Math.max(1, 7))]]);
         modules2 += "3";
         shootyesgoalW = "1";
      }
      libavcodeci = `${friendsM.length}`;

  const handleCloseModal = useCallback(() => {

   for (let n = 0; n < 2; n++) {
       let weiboz = String.fromCharCode(111,95,49,50,95,115,101,101,107,105,110,103,0);
       let membershipT = true;
         membershipT = (45 > (weiboz.length * (membershipT ? 45 : weiboz.length)));
         weiboz = `${(weiboz.length + (membershipT ? 2 : 3))}`;
       let matchi: Map<any, any> = new Map([[String.fromCharCode(97,114,107,101,114,95,111,95,49,57,0),795], [String.fromCharCode(108,95,53,54,95,100,97,116,101,0),7], [String.fromCharCode(114,95,57,54,95,116,101,108,101,109,101,116,114,121,0),587]]);
       let searchbarE: Map<any, any> = new Map([[String.fromCharCode(115,117,98,98,108,111,99,107,95,109,95,51,57,0),95], [String.fromCharCode(101,95,52,95,115,117,98,114,97,110,103,101,0),353], [String.fromCharCode(114,101,101,108,95,102,95,50,0),209]]);
          let type_4qJ = String.fromCharCode(97,95,54,51,95,103,108,111,119,0);
          let libzeusM = String.fromCharCode(99,111,117,110,116,114,105,101,115,95,113,95,57,56,0);
          let graphics6 = String.fromCharCode(100,95,56,95,108,111,103,0);
         searchbarE.set(weiboz, (String.fromCharCode(119,0) == weiboz ? (membershipT ? 5 : 4) : weiboz.length));
         type_4qJ = `${libzeusM.length}`;
         libzeusM = `${graphics6.length + type_4qJ.length}`;
         graphics6 = `${libzeusM.length + 2}`;
      for (let s = 0; s < 2; s++) {
         weiboz = `${weiboz.length | 2}`;
      }
      if (5 <= weiboz.length) {
          let vignettey: Array<any> = [55, 199, 17];
          let long__W = String.fromCharCode(108,95,54,95,97,99,114,111,110,121,109,115,0);
          let corep: Array<any> = [817, 893, 334];
          let thumbnailK = 4.0;
          let webview6 = 1;
         membershipT = weiboz == String.fromCharCode(67,0);
         vignettey.push(2 - webview6);
         long__W = "2";
         corep.push(vignettey.length ^ parseInt(`${thumbnailK}`));
         thumbnailK -= parseFloat(`${vignettey.length << (Math.min(long__W.length, 1))}`);
         webview6 ^= webview6;
      }
      airbnbstarselectedT -= parseFloat(`${1 >> (Math.min(1, Math.abs(parseInt(`${hookF}`))))}`);
   }
   if (airbnbstarselectedT <= parseFloat(`${description_mX.length}`)) {
      airbnbstarselectedT += parseFloat(`${libavcodeci.length * description_mX.length}`);
   }
    dispatch(hideAdultModeVip())
  }, [])

   while ((3 * utilsc) >= 3) {
       let rulesa = 1.0;
       let subtextq = 2;
          let episoden = String.fromCharCode(115,97,116,100,95,114,95,51,57,0);
          let profileactiveW = String.fromCharCode(112,95,51,48,95,122,108,105,98,0);
         subtextq >>= Math.min(Math.abs((String.fromCharCode(67,0) == profileactiveW ? profileactiveW.length : episoden.length)), 3);
      for (let f = 0; f < 2; f++) {
         subtextq -= subtextq | parseInt(`${rulesa}`);
      }
         rulesa *= parseFloat(`${subtextq + parseInt(`${rulesa}`)}`);
      for (let l = 0; l < 3; l++) {
          let subbasketballplayer8 = false;
         subtextq &= 1;
         subbasketballplayer8 = subbasketballplayer8 || !subbasketballplayer8;
      }
       let iconQ: Array<any> = [527, 796];
         subtextq %= Math.max(subtextq, 4);
      hookF /= Math.max(parseFloat(`${utilsc - 1}`), 3);
      break;
   }
      libavcodeci = `${friendsM.length}`;

  if (adultModeVipShow && !isVip && adultMode)

   let minimizeX = String.fromCharCode(120,56,55,0) == libavcodeci;
   do {
      libavcodeci += `${utilsc}`;
      if (minimizeX) {
         break;
      }
   } while (((libavcodeci.length / (Math.max(1, 6))) == 5) && minimizeX);
      friendsM = `${parseInt(`${airbnbstarselectedT}`)}`;
    return (
      <View
        style={{
          height: '100%',
          width: '100%',
          backgroundColor: 'rgba(0, 0, 0, 0.8)',
          position: 'absolute',
        }}>
        <VipModal>
          <View
            style={{
              backgroundColor: 'rgba(34, 34, 34, 0.9)',
              marginTop: 40,
              borderRadius: 12,
              paddingTop: 36,
              paddingBottom: 26,
              paddingHorizontal: 28,
            }}>
            <TouchableOpacity
              onPress={() => {
                handleCloseModal()
              }}>
              <View style={styles.closeBtnContainer}>
                <CloseIcon />
              </View>
            </TouchableOpacity>
            <View style={{ alignItems: 'center', justifyContent: 'center' }}>
              <Text
                style={{
                  fontSize: 20,
                  fontWeight: '500',
                  color: '#E2820E',
                  paddingTop: 20,
                  paddingVertical: 12,
                }}>
                VIP升级权益
              </Text>
              <Text
                style={{
                  color: 'white',
                  padding: 2,
                  fontSize: 16,
                  fontWeight: '300',
                }}>
                立即升级VIP，畅享午夜视频
              </Text>
              <View style={styles.btnContainer}>
                {SHOW_ZF_CONST && <TouchableOpacity
                  onPress={() => {
                    handleCloseModal();
                    navigator.navigate("付费VIP");
                    
                    
                    
                    
                    
                    
                  }}
                  style={styles.btn}>
                  <Text
                    style={{
                      color: '#000',
                      fontWeight: '600',
                      fontSize: 16,
                    }}>
                    购买VIP
                  </Text>
                </TouchableOpacity>}
                <TouchableOpacity
                  onPress={() => {
                    handleCloseModal();
                    navigator.navigate('邀请');
                  }}
                  style={styles.btn}>
                  <Text
                    style={{
                      color: '#000',
                      fontWeight: '600',
                      fontSize: 16,
                    }}>
                    邀请好友
                  </Text>
                </TouchableOpacity>
              </View>
            </View>
          </View>
        </VipModal>
      </View>
    );
};

const styles = StyleSheet.create({
  closeBtnContainer: {
    height: 20,
    width: 20,
    marginTop: -20,
    marginRight: -10,
    
    alignSelf: 'flex-end',
    
  },

  btnContainer: {
    flexDirection: 'row',
    justifyContent: 'space-evenly',
    gap: 10,
    paddingTop: 25,
    paddingBottom: 10,
  },

  btn: {
    backgroundColor: '#FAC33D',
    paddingVertical: 10,
    alignItems: 'center',
    borderRadius: 10,
    flex: 1,
  },
})

export default AdultVideoVipModal;
