import { useNavigation } from "@react-navigation/native";
import VipPrivilegeModal from "./tt_details"
import { useCallback, useEffect } from "react";
import { useAppDispatch, useAppSelector, useSelector } from "@hooks/tt_spec_download";
import { hideAdultVipPrivilegeMiniVideoAction, showAdultVipPrivilegeMiniVideoAction, showLoginAction } from "@redux/actions/tt_copy_heji";
import { View } from "react-native";
import { screenModel } from "@type/tt_twitter_data";
import tt_humidity_guide from "../../../Umeng/tt_humidity_guide";
import { UMENG_CHANNEL } from "@utility/tt_trophy_cross";
import { ttFast } from "@models/tt_stations_right";
import { ttGoal } from "@redux/reducers/tt_selected";

const sportModels = require('@static/images/soundSubs.png');
const sportBg = require('@static/images/downloadedExpandTicked.png');

interface ttSmall {
  showCondition: boolean;
  onClose: ({ isAutoClose }: { isAutoClose?: boolean }) => void;
  showBlur?: boolean;
}

export const SportVipPrivilegeOverlay = ({ showCondition, onClose, showBlur }: ttSmall) => {

  const navigator = useNavigation()
  const dispatch = useAppDispatch()
  const screenState: screenModel = useAppSelector(
    ({ screenReducer }) => screenReducer
  );
  const userState = useSelector<ttGoal>('userReducer');

  const handleOnPurchase = useCallback(() => {
       let screenp: Array<any> = [483, 945, 19];
    let injury4 = String.fromCharCode(103,114,101,103,111,114,105,97,110,95,106,95,55,56,0);
    let launchR = 3.0;
    let photoC = 1;
    let config5 = String.fromCharCode(102,109,105,120,95,117,95,49,55,0);
    let bridgew: Array<any> = [51, 999, 580];
    let filterU = 2.0;
    let selectedl = 5.0;
    let successe = String.fromCharCode(100,101,108,116,97,115,95,49,95,54,56,0);
    let yellowp = String.fromCharCode(121,95,49,49,95,106,115,101,112,0);
    let aboutM: Map<any, any> = new Map([[String.fromCharCode(97,118,102,105,108,116,101,114,114,101,115,95,119,95,56,49,0),246], [String.fromCharCode(114,95,57,49,95,109,97,114,107,105,110,103,0),933], [String.fromCharCode(114,101,97,115,111,110,95,104,95,50,48,0),963]]);
    let privilegef = String.fromCharCode(100,95,56,53,95,113,116,97,98,108,101,115,0);
    let showz = 4.0;
    let mimoR = String.fromCharCode(100,95,52,56,95,99,111,109,112,111,115,101,100,0);
   let greyS = 8543220 <= photoC;
   do {
       let selectedV = false;
         selectedV = !selectedV && selectedV;
         selectedV = (!selectedV ? !selectedV : selectedV);
          let viewerk = true;
          let lessY: Map<any, any> = new Map([[String.fromCharCode(101,110,99,95,107,95,54,56,0),false ], [String.fromCharCode(107,97,110,110,97,95,101,95,53,0),false ], [String.fromCharCode(108,95,56,95,115,105,109,112,108,101,0),false ]]);
         selectedV = (selectedV ? viewerk : !selectedV);
         viewerk = (lessY.size * lessY.size) == 35;
      photoC /= Math.max(aboutM.size % 2, 3);
      if (greyS) {
         break;
      }
   } while (greyS && (!bridgew.includes(photoC)));
      config5 = `${screenp.length - 3}`;
       let popupG: Map<any, any> = new Map([[String.fromCharCode(117,110,102,111,99,117,115,101,100,95,101,95,54,50,0),true ], [String.fromCharCode(99,111,110,102,105,114,109,101,100,95,100,95,54,53,0),false ], [String.fromCharCode(113,112,101,108,111,114,95,57,95,53,48,0),true ]]);
       let bridgeb = String.fromCharCode(98,111,111,115,116,101,100,95,98,95,52,52,0);
       let activeg = 5.0;
      let description_b5G = 5144899 >= bridgeb.length;
      do {
          let leftH = true;
          let sansD = String.fromCharCode(99,95,55,52,95,116,108,111,103,0);
          let indicatorl = false;
          let episodes5 = String.fromCharCode(115,112,108,97,115,104,95,108,95,50,57,0);
          let empty4 = String.fromCharCode(101,95,49,48,95,109,98,101,100,116,108,115,0);
         bridgeb = `${popupG.size + 2}`;
         leftH = sansD.length <= 7;
         sansD += "2";
         indicatorl = (41 <= ((leftH ? 41 : episodes5.length) / (Math.max(episodes5.length, 3))));
         empty4 = `${sansD.length}`;
         if (description_b5G) {
            break;
         }
      } while (description_b5G && (bridgeb.length == 4));
      let mbbidg = bridgeb == String.fromCharCode(52,52,121,112,52,110,99,0);
      do {
         bridgeb += `${parseInt(`${activeg}`) + popupG.size}`;
         if (mbbidg) {
            break;
         }
      } while ((!bridgeb.includes(`${activeg}`)) && mbbidg);
       let assist3: Map<any, any> = new Map([[String.fromCharCode(100,95,50,51,95,100,101,115,99,112,114,105,112,116,105,111,110,0),true ], [String.fromCharCode(98,121,116,101,111,117,116,95,98,95,52,0),true ]]);
      while ((bridgeb.length & popupG.size) == 3) {
         popupG = new Map([[`${popupG.size}`, popupG.size * 3]]);
         break;
      }
         assist3.set(`${activeg}`, assist3.size >> (Math.min(Math.abs(1), 1)));
         bridgeb += "3";
      while (4.52 == (3.13 + activeg) || 3.13 == (assist3.size + activeg)) {
         assist3 = new Map([[`${activeg}`, parseInt(`${activeg}`) - bridgeb.length]]);
         break;
      }
      while (2 >= popupG.size) {
          let main_sE = String.fromCharCode(115,117,98,109,105,116,95,55,95,49,48,0);
          let mbbannerp = String.fromCharCode(101,95,57,48,95,100,101,103,114,97,100,97,116,105,111,110,0);
          let homef = true;
         activeg += bridgeb.length << (Math.min(1, Math.abs(assist3.size)));
         main_sE = `${(String.fromCharCode(79,0) == mbbannerp ? mbbannerp.length : (homef ? 3 : 4))}`;
         homef = ((mbbannerp.length & (!homef ? mbbannerp.length : 17)) <= 17);
         break;
      }
         popupG.set(`${activeg}`, 1);
      config5 = `${yellowp.length ^ 3}`;

    onClose({ isAutoClose: true })
    navigator.navigate("付费VIP");

   while ((parseInt(`${selectedl}`) / 5) == 1 || (yellowp.length / (Math.max(8, parseInt(`${selectedl}`)))) == 5) {
      yellowp = `${screenp.length % 2}`;
      break;
   }
      screenp.push(aboutM.size);
      config5 += `${2 << (Math.min(4, yellowp.length))}`;
    

   for (let d = 0; d < 1; d++) {
      config5 = `${injury4.length}`;
   }
   if (2.73 <= (filterU - selectedl) && 3.20 <= (filterU - 2.73)) {
       let buttonA = String.fromCharCode(115,117,112,112,114,101,115,115,101,115,95,50,95,50,48,0);
       let dropdownK = String.fromCharCode(97,118,118,115,95,109,95,53,50,0);
       let selectedVJ = String.fromCharCode(109,97,114,115,104,97,108,0);
       let inviteh: Array<any> = [867, 57, 988];
          let filea = 4.0;
          let backgroundd = String.fromCharCode(107,95,56,50,95,115,112,111,105,108,101,114,0);
          let rulesJ = 4.0;
         selectedVJ += `${2 * dropdownK.length}`;
         filea *= parseFloat(`${2 ^ backgroundd.length}`);
         backgroundd = `${parseInt(`${filea}`) - backgroundd.length}`;
         rulesJ += parseFloat(`${parseInt(`${rulesJ}`) + 2}`);
         buttonA += `${buttonA.length * 2}`;
          let ajaxY = false;
          let statisticsR = String.fromCharCode(108,105,103,104,116,95,110,95,53,57,0);
          let arrow1 = String.fromCharCode(115,111,102,116,116,104,114,101,115,104,95,56,95,54,57,0);
         buttonA += `${dropdownK.length >> (Math.min(Math.abs(2), 4))}`;
         ajaxY = statisticsR.length < 49;
         statisticsR += "1";
         arrow1 += `${arrow1.length}`;
         buttonA += `${selectedVJ.length / 3}`;
       let flipperw = 2.0;
       let network9 = 5.0;
      while ((1.54 - network9) > 2.45 && 3.19 > (network9 - 1.54)) {
          let emptyw = String.fromCharCode(110,95,52,55,95,115,101,99,117,114,101,0);
          let whistle7 = false;
          let eactE = String.fromCharCode(114,95,55,51,95,99,97,116,101,103,111,114,105,101,115,0);
          let stepq = 4.0;
         selectedVJ = `${3 ^ dropdownK.length}`;
         emptyw = `${(eactE == String.fromCharCode(77,0) ? eactE.length : (whistle7 ? 4 : 1))}`;
         whistle7 = eactE.includes(`${whistle7}`);
         stepq -= parseFloat(`${3 ^ emptyw.length}`);
         break;
      }
         selectedVJ += `${(String.fromCharCode(67,0) == dropdownK ? dropdownK.length : buttonA.length)}`;
      for (let s = 0; s < 1; s++) {
         flipperw /= Math.max(inviteh.length / (Math.max(buttonA.length, 8)), 3);
      }
          let goalN: Map<any, any> = new Map([[String.fromCharCode(101,95,57,56,95,115,101,99,112,107,0),106], [String.fromCharCode(116,111,100,97,121,95,106,95,50,53,0),995], [String.fromCharCode(118,95,52,95,109,112,115,117,98,0),547]]);
          let backgroundv: Map<any, any> = new Map([[String.fromCharCode(114,101,115,116,111,114,97,116,105,111,110,95,113,95,55,50,0),810], [String.fromCharCode(109,95,49,56,95,98,105,103,100,105,97,0),355]]);
          let a_managerE = 4.0;
         flipperw /= Math.max(3, (selectedVJ == String.fromCharCode(76,0) ? parseInt(`${network9}`) : selectedVJ.length));
         goalN.set(`${a_managerE}`, parseInt(`${a_managerE}`) % (Math.max(backgroundv.size, 3)));
         backgroundv.set(`${backgroundv.size}`, backgroundv.size);
          let dragb = 3;
          let suggestionK: Map<any, any> = new Map([[String.fromCharCode(117,95,56,54,95,100,111,119,110,108,111,97,100,101,114,0),false ], [String.fromCharCode(99,104,114,111,109,97,95,49,95,56,55,0),false ], [String.fromCharCode(117,95,57,53,95,117,110,112,97,99,107,0),false ]]);
          let contextT: Array<any> = [578, 798, 508];
         buttonA = `${parseInt(`${flipperw}`)}`;
         dragb ^= suggestionK.size;
         suggestionK = new Map([[`${suggestionK.size}`, 3]]);
         contextT = [3];
         buttonA = `${buttonA.length & dropdownK.length}`;
         selectedVJ = "3";
      selectedl *= config5.length;
   }
      aboutM.set(`${selectedl}`, injury4.length);
    

   for (let x = 0; x < 2; x++) {
      aboutM.set(privilegef, (String.fromCharCode(122,0) == privilegef ? aboutM.size : privilegef.length));
   }
   while ((photoC / (Math.max(2, launchR))) > 3.100) {
       let nativeexo: Map<any, any> = new Map([[String.fromCharCode(104,100,114,115,95,109,95,51,49,0),536], [String.fromCharCode(109,101,109,100,117,112,95,55,95,50,51,0),175], [String.fromCharCode(107,95,56,56,95,109,112,101,103,97,117,100,105,111,100,101,99,116,97,98,0),200]]);
       let lcopy_qm = false;
       let nativeex_ = String.fromCharCode(114,97,116,101,115,95,103,95,51,50,0);
      for (let q = 0; q < 2; q++) {
         nativeexo.set(`${lcopy_qm}`, nativeex_.length + 3);
      }
          let constantsC = 3.0;
          let searchbarl = String.fromCharCode(102,101,116,99,104,101,115,95,51,95,55,54,0);
          let actionsy = String.fromCharCode(105,110,118,116,114,97,110,115,95,116,95,49,51,0);
         lcopy_qm = !actionsy.startsWith(`${lcopy_qm}`);
         constantsC -= parseInt(`${constantsC}`);
         searchbarl += `${searchbarl.length}`;
         actionsy = `${parseInt(`${constantsC}`)}`;
       let lessT = 3;
      while (3 < (nativeexo.size << (Math.min(nativeex_.length, 5)))) {
          let reminder8 = 0.0;
         nativeexo = new Map([[`${lessT}`, lessT - 2]]);
         reminder8 *= parseFloat(`${parseInt(`${reminder8}`) + 1}`);
         break;
      }
      while ((3 | nativeex_.length) >= 4 && (lessT | nativeex_.length) >= 3) {
         lessT ^= nativeexo.size % (Math.max(nativeex_.length, 4));
         break;
      }
      for (let g = 0; g < 2; g++) {
         lcopy_qm = nativeexo.size <= 35;
      }
      let regengU = 5132143 >= lessT;
      do {
         lessT *= (String.fromCharCode(76,0) == nativeex_ ? nativeex_.length : lessT);
         if (regengU) {
            break;
         }
      } while (regengU && (!nativeex_.startsWith(`${lessT}`)));
      for (let e = 0; e < 2; e++) {
         lcopy_qm = nativeexo.size <= 77;
      }
      for (let p = 0; p < 3; p++) {
          let typesy = String.fromCharCode(112,97,105,110,116,105,110,103,95,120,95,56,53,0);
          let greenG = String.fromCharCode(119,95,49,54,95,104,116,99,112,0);
         lessT *= nativeex_.length % 3;
         typesy = `${3 | greenG.length}`;
         greenG += `${greenG.length}`;
      }
      launchR /= Math.max(1, parseFloat(`${1 - privilegef.length}`));
      break;
   }
   while (successe == String.fromCharCode(104,0) && yellowp != String.fromCharCode(85,0)) {
       let episodeN = String.fromCharCode(114,95,56,54,95,104,97,115,104,116,97,98,108,101,122,0);
       let largeG = 4.0;
      while (5 < (episodeN.length + 2)) {
         largeG /= Math.max(parseInt(`${largeG}`), 1);
         break;
      }
      for (let a = 0; a < 2; a++) {
          let ewardedU = true;
          let group1: Array<any> = [408, 17];
          let switch_rpq: Map<any, any> = new Map([[String.fromCharCode(112,105,120,101,108,102,108,111,97,116,95,108,95,51,53,0),false ], [String.fromCharCode(120,100,97,105,95,115,95,51,52,0),false ], [String.fromCharCode(110,111,116,105,102,105,101,100,95,53,95,57,55,0),false ]]);
          let sideW = String.fromCharCode(101,120,99,101,112,116,105,111,110,95,111,95,50,49,0);
         largeG -= 1 * switch_rpq.size;
         ewardedU = sideW.length > 38;
         group1 = [3];
         switch_rpq = new Map([[`${group1.length}`, sideW.length | group1.length]]);
      }
      while (largeG > 2.34) {
         largeG *= parseInt(`${largeG}`);
         break;
      }
      while (3 > (episodeN.length | 2) || 2.8 > (largeG * 2.61)) {
         largeG += episodeN.length;
         break;
      }
         largeG /= Math.max(2, episodeN.length);
       let canvasi: Map<any, any> = new Map([[String.fromCharCode(118,95,49,57,95,115,121,115,116,101,109,0),858], [String.fromCharCode(108,115,102,112,111,108,121,95,97,95,57,50,0),982], [String.fromCharCode(120,95,51,52,95,101,109,111,114,121,0),591]]);
       let navigationZ: Map<any, any> = new Map([[String.fromCharCode(99,114,121,115,116,97,108,104,100,95,50,95,56,0),607], [String.fromCharCode(99,111,110,99,101,97,108,95,101,95,50,54,0),4], [String.fromCharCode(102,95,50,51,95,102,116,118,109,108,97,115,116,110,111,100,101,0),244]]);
      successe = "1";
      break;
   }
    

       let matchI = String.fromCharCode(115,111,117,110,100,101,120,95,105,95,54,51,0);
       let scorem = String.fromCharCode(114,116,112,119,95,120,95,53,57,0);
       let circleB = 3;
       let coreT = 3.0;
         coreT *= (String.fromCharCode(82,0) == scorem ? scorem.length : circleB);
         circleB %= Math.max(1 >> (Math.min(5, matchI.length)), 5);
         scorem += `${2 * circleB}`;
          let rankJ = 4.0;
         coreT /= Math.max(1 % (Math.max(parseInt(`${rankJ}`), 5)), 4);
          let action5 = true;
         circleB |= scorem.length / (Math.max(3, 2));
         action5 = !action5;
         scorem += `${1 - circleB}`;
      for (let e = 0; e < 3; e++) {
         matchI = `${1 >> (Math.min(3, Math.abs(parseInt(`${coreT}`))))}`;
      }
      if (2.55 >= coreT) {
         matchI = `${circleB * 2}`;
      }
      photoC -= parseInt(`${launchR}`);
      filterU /= Math.max(5, yellowp.length);
      selectedl *= screenp.length | 1;
    

   while ((5 | privilegef.length) < 3) {
      photoC |= bridgew.length;
      break;
   }
   while (1 == (successe.length + aboutM.size) && (aboutM.size + 1) == 5) {
      aboutM = new Map([[`${screenp.length}`, screenp.length]]);
      break;
   }
      selectedl *= 2;
    

       let crown_ = 0.0;
      while ((crown_ * crown_) <= 3.73) {
         crown_ /= Math.max(1, parseFloat(`${2}`));
         break;
      }
      while (3.40 <= crown_) {
          let lineN = 0.0;
          let dplusA: Map<any, any> = new Map([[String.fromCharCode(117,110,112,114,111,99,101,115,115,101,100,95,117,95,52,53,0),String.fromCharCode(99,111,114,110,101,114,115,95,50,95,49,55,0)], [String.fromCharCode(114,101,109,111,116,101,95,101,95,54,57,0),String.fromCharCode(97,102,102,105,110,105,116,105,101,115,95,113,95,57,55,0)]]);
          let thumbnail7 = String.fromCharCode(121,95,54,50,95,98,105,100,105,114,101,99,116,105,111,110,97,108,0);
          let bannerO = 0.0;
         crown_ /= Math.max(1, parseFloat(`${parseInt(`${lineN}`)}`));
         lineN -= parseFloat(`${dplusA.size / (Math.max(thumbnail7.length, 1))}`);
         dplusA = new Map([[`${dplusA.size}`, thumbnail7.length]]);
         bannerO /= Math.max(3, thumbnail7.length);
         break;
      }
         crown_ -= parseFloat(`${2}`);
      screenp = [successe.length + 3];
   for (let m = 0; m < 2; m++) {
       let orangeQ: Map<any, any> = new Map([[String.fromCharCode(112,114,111,99,101,115,115,95,113,95,55,55,0),292], [String.fromCharCode(112,95,54,55,95,100,111,117,98,108,101,115,115,116,114,0),407], [String.fromCharCode(120,95,52,53,95,114,101,102,99,111,117,110,116,101,114,0),748]]);
       let episodesN = String.fromCharCode(110,95,57,95,117,110,122,105,112,0);
       let clock3 = false;
         episodesN = `${(String.fromCharCode(98,0) == episodesN ? episodesN.length : (clock3 ? 5 : 3))}`;
      if (!episodesN.endsWith(`${orangeQ.size}`)) {
         orangeQ.set(`${episodesN}`, episodesN.length);
      }
         clock3 = (orangeQ.size + episodesN.length) == 41;
      while (2 >= (4 * orangeQ.size) || clock3) {
          let androidU = 2.0;
          let navigationH: Map<any, any> = new Map([[String.fromCharCode(108,95,52,57,95,118,112,108,112,102,0),772], [String.fromCharCode(97,118,105,103,97,116,105,111,110,95,102,95,49,0),39]]);
          let strings5 = 0.0;
          let descN = true;
          let resendy: Array<any> = [754, 886];
         orangeQ.set(`${androidU}`, navigationH.size >> (Math.min(Math.abs(3), 5)));
         androidU /= Math.max(resendy.length >> (Math.min(2, Math.abs(parseInt(`${strings5}`)))), 1);
         navigationH.set(`${descN}`, 1 >> (Math.min(Math.abs(parseInt(`${strings5}`)), 2)));
         descN = !descN && strings5 > 80.2;
         resendy = [(3 & (descN ? 5 : 1))];
         break;
      }
      for (let x = 0; x < 2; x++) {
          let bootsplashE = 3.0;
          let activityD = String.fromCharCode(101,108,101,118,97,116,101,95,114,95,56,56,0);
         episodesN = `${episodesN.length}`;
         bootsplashE *= (parseFloat(`${String.fromCharCode(116,0) == activityD ? activityD.length : parseInt(`${bootsplashE}`)}`));
      }
      if (episodesN.endsWith(`${clock3}`)) {
         episodesN += "3";
      }
      while (!clock3) {
         clock3 = !clock3;
         break;
      }
      let fullH = orangeQ.size >= 7124261;
      do {
         orangeQ.set(episodesN, ((clock3 ? 1 : 2) + episodesN.length));
         if (fullH) {
            break;
         }
      } while (((1 + orangeQ.size) == 4 && clock3) && fullH);
         orangeQ = new Map([[episodesN, (2 | (clock3 ? 1 : 5))]]);
      config5 += `${bridgew.length}`;
   }
       let borderlessu = true;
       let shirt4: Map<any, any> = new Map([[String.fromCharCode(102,95,50,57,95,112,114,111,116,101,99,116,101,100,0),384], [String.fromCharCode(120,120,99,104,95,117,95,57,0),670]]);
         borderlessu = shirt4.size == 17;
      let flipperW = borderlessu ? !borderlessu : borderlessu;
      do {
         borderlessu = !borderlessu;
         if (flipperW) {
            break;
         }
      } while (((shirt4.size / 3) > 5) && flipperW);
         borderlessu = (47 <= ((!borderlessu ? 47 : shirt4.size) & shirt4.size));
         borderlessu = !borderlessu;
      let penaltyb = shirt4.size <= 8166024;
      do {
         shirt4.set(`${borderlessu}`, ((borderlessu ? 5 : 2) >> (Math.min(Math.abs(shirt4.size), 1))));
         if (penaltyb) {
            break;
         }
      } while (penaltyb && (2 >= (shirt4.size ^ 5) || 5 >= shirt4.size));
      if (borderlessu) {
         shirt4 = new Map([[`${shirt4.size}`, (shirt4.size - (borderlessu ? 4 : 3))]]);
      }
      photoC -= parseInt(`${showz}`);
    

       let stepv: Map<any, any> = new Map([[String.fromCharCode(98,95,51,48,95,115,117,98,115,101,116,0),String.fromCharCode(117,109,98,101,114,95,97,95,51,56,0)], [String.fromCharCode(110,95,50,48,95,110,116,101,114,102,97,99,101,0),String.fromCharCode(104,117,109,97,110,95,104,95,51,53,0)]]);
       let mbsplasha: Map<any, any> = new Map([[String.fromCharCode(99,97,115,101,115,95,56,95,50,57,0),24], [String.fromCharCode(99,95,55,57,95,114,101,97,100,101,114,0),143], [String.fromCharCode(112,115,110,114,104,118,115,95,108,95,55,51,0),882]]);
       let viewsC = String.fromCharCode(111,112,116,105,109,105,115,116,105,99,95,55,95,51,52,0);
       let memberW = String.fromCharCode(116,95,55,50,95,103,110,111,0);
      let blacklistI = memberW.length <= 8063648;
      do {
         memberW = `${(viewsC == String.fromCharCode(102,0) ? stepv.size : viewsC.length)}`;
         if (blacklistI) {
            break;
         }
      } while (((mbsplasha.size << (Math.min(memberW.length, 1))) >= 5 || (memberW.length << (Math.min(Math.abs(5), 1))) >= 5) && blacklistI);
         viewsC += `${memberW.length}`;
      if (memberW.length < stepv.size) {
         memberW = `${mbsplasha.size | viewsC.length}`;
      }
         memberW += `${mbsplasha.size}`;
         stepv = new Map([[memberW, memberW.length]]);
      showz += 1;
      aboutM = new Map([[`${screenp.length}`, yellowp.length % 1]]);
   for (let v = 0; v < 1; v++) {
       let sourceS = 0;
       let commonR = 0;
       let debugz = String.fromCharCode(97,110,105,109,97,108,115,95,57,95,49,57,0);
         commonR %= Math.max(2, sourceS);
         sourceS |= commonR;
      for (let l = 0; l < 2; l++) {
         debugz = `${commonR & 2}`;
      }
         commonR &= sourceS >> (Math.min(Math.abs(2), 2));
      let moditym = 5270098 >= commonR;
      do {
         commonR += (debugz == String.fromCharCode(85,0) ? sourceS : debugz.length);
         if (moditym) {
            break;
         }
      } while (((commonR << (Math.min(1, Math.abs(sourceS)))) >= 1) && moditym);
         commonR %= Math.max(sourceS - commonR, 1);
          let viewerN = 0.0;
          let guideZ = true;
         sourceS /= Math.max(2, 5);
         viewerN /= Math.max(parseFloat(`${parseInt(`${viewerN}`) | parseInt(`${viewerN}`)}`), 2);
         guideZ = viewerN > 14.2 || viewerN > 14.2;
      while ((5 & sourceS) < 5 || 4 < (sourceS & 5)) {
         sourceS |= debugz.length & 2;
         break;
      }
      if (debugz.includes(`${sourceS}`)) {
         debugz = `${3 << (Math.min(1, Math.abs(commonR)))}`;
      }
      showz += photoC << (Math.min(2, Math.abs(2)));
   }
    

      injury4 += `${screenp.length >> (Math.min(Math.abs(1), 2))}`;
   let basketballN = launchR <= 6669739.0;
   do {
      launchR -= parseFloat(`${parseInt(`${showz}`)}`);
      if (basketballN) {
         break;
      }
   } while (((yellowp.length % (Math.max(1, 7))) < 3 && (launchR + 2.17) < 2.95) && basketballN);
      photoC ^= screenp.length >> (Math.min(1, Math.abs(aboutM.size)));
    

   for (let d = 0; d < 3; d++) {
      privilegef += `${screenp.length ^ aboutM.size}`;
   }
   if (4 >= (bridgew.length + config5.length) && 1 >= (bridgew.length + 4)) {
      bridgew.push(parseInt(`${selectedl}`) + screenp.length);
   }
       let statisticsw = false;
       let regengb = 3.0;
       let albumk = 4.0;
      for (let v = 0; v < 3; v++) {
          let index1: Array<any> = [String.fromCharCode(122,95,51,48,95,108,111,103,105,99,0), String.fromCharCode(105,95,55,55,95,116,101,108,108,97,114,0)];
          let customJ = String.fromCharCode(108,101,110,103,116,104,115,95,114,95,54,50,0);
          let langkey8 = String.fromCharCode(97,114,101,97,95,103,95,56,0);
          let kickC = 2;
         albumk *= (parseFloat(`${String.fromCharCode(75,0) == customJ ? index1.length : customJ.length}`));
         index1.push(1);
         langkey8 += "3";
         kickC |= 1;
      }
      let memberK = 8986737.0 >= regengb;
      do {
         regengb += ((statisticsw ? 1 : 5) & parseInt(`${regengb}`));
         if (memberK) {
            break;
         }
      } while (memberK && (statisticsw));
      for (let a = 0; a < 1; a++) {
          let targetE = String.fromCharCode(109,95,56,55,95,100,111,101,115,0);
          let plusm: Array<any> = [956, 6];
         albumk *= parseFloat(`${parseInt(`${regengb}`)}`);
         targetE = `${plusm.length * targetE.length}`;
         plusm.push(plusm.length + 1);
      }
      let pauseu = 5726521.0 <= regengb;
      do {
         regengb += 2 / (Math.max(parseInt(`${regengb}`), 10));
         if (pauseu) {
            break;
         }
      } while ((regengb == 2.39 && (regengb + 2.39) == 2.59) && pauseu);
      let mbnativeadvancedQ = 7504235.0 <= regengb;
      do {
         regengb += 2 & parseInt(`${regengb}`);
         if (mbnativeadvancedQ) {
            break;
         }
      } while (mbnativeadvancedQ && (!statisticsw));
      let countdown8 = 5507458.0 >= regengb;
      do {
         regengb -= parseInt(`${albumk}`) & 1;
         if (countdown8) {
            break;
         }
      } while ((4.48 >= (regengb * 4.85) || 5.38 >= (albumk * 4.85)) && countdown8);
         statisticsw = statisticsw && regengb > 53.75;
      if (5.0 == (2.24 / (Math.max(7, regengb))) || !statisticsw) {
         regengb *= ((statisticsw ? 2 : 2) % (Math.max(parseInt(`${regengb}`), 6)));
      }
         albumk *= parseFloat(`${parseInt(`${regengb}`) + 2}`);
      aboutM = new Map([[`${bridgew.length}`, bridgew.length]]);


    

   if (2.62 <= (filterU * showz) && 3.72 <= (2.62 * filterU)) {
       let incident7 = 2.0;
       let backgroundF = String.fromCharCode(113,95,52,54,95,98,111,117,110,100,0);
       let spinnerQ: Map<any, any> = new Map([[String.fromCharCode(111,95,57,54,95,109,105,114,114,111,114,101,100,0),625], [String.fromCharCode(100,97,114,107,95,118,95,50,48,0),806], [String.fromCharCode(107,95,50,52,0),137]]);
          let adultt = 4.0;
          let default_3B = String.fromCharCode(122,95,52,50,95,110,105,99,101,108,121,0);
          let overlayd = String.fromCharCode(116,95,52,95,99,105,114,99,108,101,0);
         backgroundF += `${default_3B.length}`;
         adultt /= Math.max(4, parseFloat(`${parseInt(`${adultt}`)}`));
         default_3B += `${overlayd.length}`;
         overlayd = `${overlayd.length ^ parseInt(`${adultt}`)}`;
      while (spinnerQ.get(`${incident7}`) != null) {
         incident7 *= parseFloat(`${spinnerQ.size}`);
         break;
      }
         incident7 /= Math.max(3, parseFloat(`${spinnerQ.size}`));
          let c_titlej = 5;
         incident7 -= parseFloat(`${spinnerQ.size}`);
         c_titlej &= c_titlej ^ 3;
          let countryq = String.fromCharCode(101,110,99,95,107,95,50,54,0);
          let buildX: Map<any, any> = new Map([[String.fromCharCode(115,111,108,105,100,95,100,95,51,52,0),785], [String.fromCharCode(99,111,110,102,105,103,117,114,101,100,95,114,95,55,50,0),415], [String.fromCharCode(110,95,56,50,95,100,114,105,118,101,110,0),385]]);
          let backZ: Array<any> = [646, 425];
         backgroundF += `${(backgroundF == String.fromCharCode(68,0) ? buildX.size : backgroundF.length)}`;
         countryq += `${3 + backZ.length}`;
         buildX = new Map([[`${backZ.length}`, 3]]);
      if (backgroundF.length > 4) {
         backgroundF = `${backgroundF.length}`;
      }
       let assistw = 1.0;
      for (let f = 0; f < 1; f++) {
         incident7 /= Math.max(3, parseFloat(`${2}`));
      }
      while (incident7 > 4.49) {
          let frame_r4r = 2;
         assistw *= backgroundF.length;
         frame_r4r *= frame_r4r;
         break;
      }
      filterU *= 2;
   }
   for (let z = 0; z < 3; z++) {
      privilegef = `${parseInt(`${selectedl}`) - injury4.length}`;
   }
   for (let h = 0; h < 3; h++) {
      bridgew.push(3);
   }
    tt_humidity_guide.sportDetailsVipPopupClicksAnalytics('pay');
    
  }, [])

  const handleOnInvite = useCallback(() => {
       let sorts = 0.0;
    let mathp = 1;
    let register_zwd: Map<any, any> = new Map([[String.fromCharCode(121,95,56,57,95,115,116,105,99,107,101,114,101,100,0),46], [String.fromCharCode(115,116,97,116,105,111,110,97,114,121,95,118,95,50,56,0),147], [String.fromCharCode(114,101,97,99,116,95,50,95,52,56,0),730]]);
    let commentK = String.fromCharCode(110,95,57,52,95,97,117,120,100,97,116,97,0);
    let draga = String.fromCharCode(116,95,51,54,95,108,111,103,111,0);
    let othery: Array<any> = [978, 654];
    let clock3 = String.fromCharCode(99,95,56,52,95,103,101,110,101,115,105,115,0);
    let private_6g: Map<any, any> = new Map([[String.fromCharCode(118,95,50,57,0),String.fromCharCode(100,101,99,111,100,101,102,95,101,95,51,50,0)], [String.fromCharCode(119,95,52,55,95,105,109,97,103,101,115,0),String.fromCharCode(113,95,49,95,110,115,115,101,0)]]);
    let register_cpO: Map<any, any> = new Map([[String.fromCharCode(98,95,50,56,95,115,116,97,116,0),848], [String.fromCharCode(108,95,52,50,95,97,117,100,105,111,99,111,110,118,101,114,116,0),3]]);
    let reactnativejsD: Map<any, any> = new Map([[String.fromCharCode(105,112,97,100,105,102,102,95,56,95,50,0),34], [String.fromCharCode(114,101,115,105,103,110,105,110,103,95,103,95,56,48,0),653], [String.fromCharCode(109,111,115,97,105,99,95,51,95,54,51,0),345]]);
    let connectioni = false;
    let privilegea: Map<any, any> = new Map([[String.fromCharCode(98,95,49,48,48,95,98,121,116,101,0),3], [String.fromCharCode(105,95,57,52,95,99,115,119,97,112,0),841], [String.fromCharCode(99,111,110,102,108,105,99,116,101,100,95,117,95,53,48,0),116]]);
    let shareR = String.fromCharCode(97,95,56,53,95,104,105,98,105,116,0);
    let yellowM = String.fromCharCode(110,95,54,55,95,104,119,97,99,99,101,108,115,0);
    let update_i9: Array<any> = [String.fromCharCode(114,95,55,55,95,115,101,108,101,99,116,101,100,0), String.fromCharCode(104,95,55,57,95,113,117,97,114,116,0)];
    let grayF = 3;
    let currentw = String.fromCharCode(112,114,111,95,54,95,51,56,0);
    let otherf: Array<any> = [237, 151, 761];
   for (let l = 0; l < 3; l++) {
      draga = `${3 + privilegea.size}`;
   }
   let selection4 = connectioni ? !connectioni : connectioni;
   do {
      connectioni = othery.length > register_zwd.size;
      if (selection4) {
         break;
      }
   } while (((4 ^ reactnativejsD.size) == 4 || 4 == reactnativejsD.size) && selection4);
      mathp *= register_cpO.size;
   let assist6 = 4991614 >= reactnativejsD.size;
   do {
      reactnativejsD = new Map([[draga, parseInt(`${sorts}`) | 3]]);
      if (assist6) {
         break;
      }
   } while ((2 >= (clock3.length * reactnativejsD.size) || 2 >= (clock3.length * reactnativejsD.size)) && assist6);
      reactnativejsD = new Map([[`${reactnativejsD.size}`, private_6g.size - reactnativejsD.size]]);
      commentK = `${(commentK == String.fromCharCode(79,0) ? commentK.length : othery.length)}`;
      sorts *= parseFloat(`${reactnativejsD.size}`);

    onClose({ isAutoClose: true })
    navigator.navigate('邀请');

      othery.push(register_cpO.size);
   for (let i = 0; i < 2; i++) {
      draga += "3";
   }
      commentK += `${parseInt(`${sorts}`) ^ register_zwd.size}`;
      clock3 = `${private_6g.size & 2}`;
   let cornerK = String.fromCharCode(104,122,108,102,119,95,50,48,106,57,0) == draga;
   do {
       let tumbnaile = String.fromCharCode(102,95,51,54,95,121,112,114,101,100,105,99,116,105,111,110,0);
       let settingv = String.fromCharCode(98,95,49,48,95,109,117,108,109,111,100,0);
       let vignetteh = String.fromCharCode(106,95,56,55,95,122,101,116,97,0);
       let textD = 3;
       let pingS = true;
      if (5 == tumbnaile.length) {
          let arrowW = String.fromCharCode(108,97,110,103,117,97,103,101,95,49,95,49,48,48,0);
          let nativeexI = 0.0;
          let inviteg = 2.0;
          let weibo7 = 2;
          let moviesW = String.fromCharCode(98,108,111,99,107,113,117,111,116,101,95,97,95,51,53,0);
         settingv = "2";
         arrowW += `${parseInt(`${inviteg}`)}`;
         nativeexI /= Math.max(2 | parseInt(`${inviteg}`), 4);
         weibo7 /= Math.max(1 - parseInt(`${inviteg}`), 4);
         moviesW = `${1 ^ moviesW.length}`;
      }
         textD ^= (tumbnaile.length * (pingS ? 3 : 1));
         settingv += `${((pingS ? 2 : 4) % (Math.max(tumbnaile.length, 1)))}`;
      for (let u = 0; u < 3; u++) {
         vignetteh = "3";
      }
          let playe: Array<any> = [String.fromCharCode(115,95,53,56,95,99,111,109,112,97,114,97,116,111,114,0), String.fromCharCode(99,104,101,99,107,101,114,95,114,95,51,48,0), String.fromCharCode(111,95,49,55,95,115,104,97,100,101,115,0)];
          let inactive6 = 2.0;
          let lineJ: Array<any> = [187, 862, 312];
         pingS = playe.length > 42;
         playe.push(lineJ.length + 1);
         inactive6 *= parseFloat(`${lineJ.length}`);
      if (3 >= tumbnaile.length) {
         tumbnaile = `${tumbnaile.length}`;
      }
       let corey = false;
       let trophyR = String.fromCharCode(99,114,97,115,104,95,50,95,51,48,0);
         pingS = 72 == settingv.length;
         tumbnaile = `${textD + tumbnaile.length}`;
          let eactI = true;
          let chinasameF: Map<any, any> = new Map([[String.fromCharCode(111,95,49,57,95,99,108,105,112,115,0),String.fromCharCode(111,95,55,56,95,98,97,114,114,97,121,0)], [String.fromCharCode(97,115,115,101,115,115,109,101,110,116,95,100,95,51,50,0),String.fromCharCode(107,95,53,51,95,105,103,110,101,116,116,101,0)]]);
          let cornerU = String.fromCharCode(98,95,49,48,48,95,112,97,103,105,110,103,0);
         pingS = !pingS;
         eactI = (1 == (cornerU.length >> (Math.min(5, Math.abs((eactI ? 1 : cornerU.length))))));
         chinasameF = new Map([[`${chinasameF.size}`, (cornerU == String.fromCharCode(121,0) ? cornerU.length : chinasameF.size)]]);
      let completeC = corey ? !corey : corey;
      do {
          let moonA = 4.0;
          let referrery = String.fromCharCode(119,101,98,118,105,101,119,95,108,95,54,53,0);
          let kickA = 5;
          let applem = false;
         corey = !pingS && referrery.length > 59;
         moonA -= parseFloat(`${parseInt(`${moonA}`)}`);
         referrery += `${((applem ? 4 : 3) - kickA)}`;
         kickA |= kickA;
         applem = parseFloat(`${kickA}`) <= moonA;
         if (completeC) {
            break;
         }
      } while (completeC && (!corey || 2 <= (1 % (Math.max(4, textD)))));
         pingS = ((vignetteh.length % (Math.max(2, (corey ? vignetteh.length : 18)))) >= 18);
      let mintegralw = trophyR.length <= 7617099;
      do {
         trophyR += "2";
         if (mintegralw) {
            break;
         }
      } while (mintegralw && (tumbnaile.includes(trophyR)));
       let taiwane = false;
       let connectionm = true;
      draga += `${((connectioni ? 4 : 1) - 3)}`;
      if (cornerK) {
         break;
      }
   } while (((private_6g.size % 3) < 5 && (private_6g.size % (Math.max(3, 10))) < 4) && cornerK);
       let becomet = 0.0;
       let arrowl = String.fromCharCode(116,114,101,101,114,101,97,100,101,114,95,49,95,53,49,0);
         arrowl += "2";
      let productH = String.fromCharCode(50,119,48,105,57,117,110,0) == arrowl;
      do {
         arrowl = `${parseInt(`${becomet}`) % 1}`;
         if (productH) {
            break;
         }
      } while (((becomet / (Math.max(1.25, 5))) < 4.92 || (arrowl.length / (Math.max(3, parseInt(`${becomet}`)))) < 4) && productH);
      if ((arrowl.length - parseInt(`${becomet}`)) < 1 && 3 < (arrowl.length >> (Math.min(Math.abs(1), 3)))) {
         arrowl += `${arrowl.length & 1}`;
      }
         arrowl = `${arrowl.length + parseInt(`${becomet}`)}`;
      while (1 < arrowl.length) {
          let malaysiae: Array<any> = [127, 595, 768];
          let album1: Map<any, any> = new Map([[String.fromCharCode(115,97,110,105,116,105,122,101,114,95,48,95,53,49,0),false ], [String.fromCharCode(108,100,105,115,116,95,112,95,49,51,0),false ], [String.fromCharCode(110,95,50,56,95,104,113,97,100,115,112,0),false ]]);
          let footballZ = 4.0;
         arrowl = "2";
         malaysiae.push(1);
         album1 = new Map([[`${album1.size}`, malaysiae.length * album1.size]]);
         footballZ += parseFloat(`${malaysiae.length | 2}`);
         break;
      }
      for (let u = 0; u < 3; u++) {
          let middleF: Array<any> = [412, 28];
         arrowl += `${middleF.length}`;
      }
      yellowM += `${(draga == String.fromCharCode(80,0) ? privilegea.size : draga.length)}`;
       let moviesi = String.fromCharCode(99,108,109,117,108,95,50,95,52,52,0);
       let manifestW = String.fromCharCode(114,101,115,105,122,105,110,103,95,103,95,51,52,0);
       let with_dw = String.fromCharCode(109,97,103,110,105,116,117,100,101,115,95,121,95,53,56,0);
         manifestW = `${manifestW.length >> (Math.min(moviesi.length, 1))}`;
       let constantsY: Array<any> = [701, 988, 892];
          let analyticv: Array<any> = [127, 670];
          let servicey = 0.0;
          let pangleZ = String.fromCharCode(115,116,97,108,108,95,104,95,56,52,0);
         with_dw = `${analyticv.length + pangleZ.length}`;
         analyticv = [3];
         servicey /= Math.max(3, parseInt(`${servicey}`) ^ 1);
      for (let x = 0; x < 1; x++) {
         manifestW = `${constantsY.length}`;
      }
         moviesi = `${(String.fromCharCode(70,0) == with_dw ? with_dw.length : constantsY.length)}`;
         moviesi = `${constantsY.length * moviesi.length}`;
         moviesi += `${constantsY.length}`;
      if (moviesi == String.fromCharCode(69,0) || with_dw.length <= 5) {
          let tumbnailW = String.fromCharCode(108,95,55,51,95,116,119,105,116,99,104,0);
          let topicM = String.fromCharCode(101,120,112,105,114,97,116,105,111,110,95,111,95,57,57,0);
          let diced = String.fromCharCode(100,97,116,97,116,121,112,101,115,95,111,95,53,54,0);
          let launcherB = 0.0;
         moviesi = `${parseInt(`${launcherB}`) * diced.length}`;
         tumbnailW += `${(String.fromCharCode(80,0) == tumbnailW ? topicM.length : tumbnailW.length)}`;
         topicM = "2";
         diced += `${tumbnailW.length | 1}`;
         launcherB -= parseFloat(`${tumbnailW.length ^ topicM.length}`);
      }
      while ((with_dw.length ^ constantsY.length) == 3 && 3 == (constantsY.length ^ with_dw.length)) {
         constantsY.push(with_dw.length - constantsY.length);
         break;
      }
      draga += "1";

    

   for (let a = 0; a < 2; a++) {
      reactnativejsD = new Map([[shareR, commentK.length]]);
   }
   let customA = othery.length <= 5092131;
   do {
      othery = [register_zwd.size];
      if (customA) {
         break;
      }
   } while (customA && (4 < othery.length));
   let awayO = private_6g.size >= 5990184;
   do {
      private_6g.set(`${sorts}`, 3 | commentK.length);
      if (awayO) {
         break;
      }
   } while (awayO && (3 < (yellowM.length >> (Math.min(Math.abs(4), 4))) || 3 < (4 >> (Math.min(5, Math.abs(private_6g.size))))));
   for (let p = 0; p < 2; p++) {
      shareR = `${shareR.length}`;
   }
      reactnativejsD.set(`${connectioni}`, register_zwd.size);
      connectioni = 71 >= shareR.length;
   if ((draga.length % 5) <= 3) {
      draga += `${(String.fromCharCode(54,0) == yellowM ? othery.length : yellowM.length)}`;
   }
    tt_humidity_guide.sportDetailsVipPopupClicksAnalytics('invite');
    

  }, [])


  return (

    <VipPrivilegeModal
      titleText={"VIP升级权益"}
      benefitsTextsArray={['高清体育赛事  零时差感受赛场激情', '海量福利视频 你想要的这里都有', '高清画质 无广告观影体验']}
      imageGradientColors={['#0E5ED800', '#0B6AE4']}
      contentGradientColors={['#0B6CE8', '#001A74']}
      coverBackground={sportBg}
      coverImage={sportModels}
      onPurchase={handleOnPurchase}
      onInvite={handleOnInvite}
      onClose={onClose}
      showBlur={showBlur}
      showCondition={showCondition}
      showDarkBackdrop={true}
    />

  )
}