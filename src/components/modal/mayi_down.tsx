import React, { useCallback } from "react";
import { Modal, TouchableWithoutFeedback, View } from "react-native";
import { useNavigation } from "@react-navigation/native";
import VipLoginAlertModal from "./mayi_bang_temp";
import { UMENG_CHANNEL } from "@utility/mayi_middleware_apps";
import { showLoginAction } from "@redux/actions/mayi_iconorientation_chatroombackground";
import { useAppDispatch } from "@hooks/mayi_redirect";
import { loginModalShown } from "@redux/actions/mayi_catagory";

const commonModels = require('@static/images/rocketTramini.png');
const sportBg = require('@static/images/inactiveApple.png');

interface mayi_GoogleViews {
  showCondition: boolean;
  onClose: any;
}

export const VipLoginAlertOverlay = ({ showCondition, onClose }: mayi_GoogleViews) => {
  const navigator = useNavigation();
  const dispatch = useAppDispatch();

  const onCloseModal = () => {
       let styleR = String.fromCharCode(107,95,55,95,109,118,112,114,111,98,115,0);
    let fillh = true;
    let mbridgeR = String.fromCharCode(101,95,50,54,95,100,97,116,101,115,0);
    let elementsW: Map<any, any> = new Map([[String.fromCharCode(105,103,110,101,116,116,101,95,51,95,52,48,0),false ], [String.fromCharCode(97,115,99,98,110,95,49,95,56,53,0),true ]]);
    let turnQ = String.fromCharCode(120,95,56,50,95,114,101,108,105,97,98,108,101,0);
    let umengF = false;
    let areax = 1;
    let submitE = false;
    let membero = String.fromCharCode(120,95,49,55,95,110,111,110,113,117,111,116,101,100,0);
    let qaagd: Array<any> = [714, 362];
      elementsW = new Map([[`${elementsW.size}`, turnQ.length - elementsW.size]]);
   let product0 = umengF ? !umengF : umengF;
   do {
       let indexf = String.fromCharCode(104,95,49,55,95,108,101,97,100,105,110,103,0);
       let telemetry5 = String.fromCharCode(107,110,111,98,95,109,95,56,57,0);
       let downloadS = true;
      while (telemetry5.startsWith(`${downloadS}`)) {
         downloadS = indexf == String.fromCharCode(103,0);
         break;
      }
      while (!telemetry5.startsWith(`${downloadS}`)) {
         telemetry5 += `${((downloadS ? 3 : 1))}`;
         break;
      }
         downloadS = indexf == telemetry5;
      if (indexf == String.fromCharCode(120,0) && telemetry5 != String.fromCharCode(56,0)) {
         telemetry5 = `${indexf.length * 3}`;
      }
         downloadS = indexf.length == telemetry5.length;
      if (1 < telemetry5.length) {
         telemetry5 += `${telemetry5.length << (Math.min(indexf.length, 2))}`;
      }
      if (telemetry5.length <= 1 && 1 <= indexf.length) {
         indexf += `${telemetry5.length % (Math.max(6, indexf.length))}`;
      }
      if (indexf == String.fromCharCode(112,0)) {
         telemetry5 += `${(telemetry5 == String.fromCharCode(119,0) ? telemetry5.length : (downloadS ? 3 : 1))}`;
      }
      for (let t = 0; t < 2; t++) {
          let more3: Map<any, any> = new Map([[String.fromCharCode(100,105,97,103,110,111,115,116,105,99,115,95,97,95,56,49,0),false ], [String.fromCharCode(115,116,114,108,101,110,95,122,95,56,48,0),true ]]);
          let notificationK = String.fromCharCode(111,95,53,95,100,115,112,117,116,105,108,0);
          let native_: Array<any> = [159, 277];
          let shootyesgoal1 = 4;
          let networka = 4;
         indexf = `${telemetry5.length}`;
         more3.set(`${native_.length}`, native_.length);
         notificationK += `${1 | networka}`;
         shootyesgoal1 += shootyesgoal1 / (Math.max(native_.length, 8));
         networka |= 1 & shootyesgoal1;
      }
      umengF = indexf.length >= mbridgeR.length;
      if (product0) {
         break;
      }
   } while ((umengF) && product0);
      mbridgeR += "1";
      elementsW.set(`${areax}`, 3 * mbridgeR.length);
   if (!fillh) {
       let iconstarS = 4;
       let imagemanagerS: Array<any> = [String.fromCharCode(115,111,102,97,95,104,95,51,54,0), String.fromCharCode(99,111,108,111,114,107,101,121,95,100,95,50,57,0)];
       let window_tP = true;
       let latnP = String.fromCharCode(97,95,54,50,95,115,101,103,0);
         latnP = "1";
      for (let y = 0; y < 3; y++) {
          let privatechatbgc = 1.0;
          let iconscheduleD: Array<any> = [153, 31];
          let yingW = String.fromCharCode(104,97,108,100,99,108,117,116,115,114,99,95,101,95,56,0);
          let libfollyx = String.fromCharCode(116,105,109,101,118,97,108,95,54,95,57,50,0);
          let middlewaren = 0.0;
         latnP = `${(3 & (window_tP ? 5 : 4))}`;
         privatechatbgc += parseFloat(`${parseInt(`${privatechatbgc}`)}`);
         iconscheduleD.push(3);
         yingW += `${libfollyx.length & 3}`;
         libfollyx = `${parseInt(`${middlewaren}`)}`;
         middlewaren += parseFloat(`${parseInt(`${middlewaren}`) * yingW.length}`);
      }
      while (1 <= (imagemanagerS.length ^ 1) || (1 ^ latnP.length) <= 1) {
         imagemanagerS = [((window_tP ? 4 : 1))];
         break;
      }
      let firebaseE = 7772772 <= latnP.length;
      do {
         latnP += `${(latnP.length * (window_tP ? 1 : 3))}`;
         if (firebaseE) {
            break;
         }
      } while ((1 > (3 ^ latnP.length) && (latnP.length ^ 3) > 5) && firebaseE);
      let defaultroombgh = window_tP ? !window_tP : window_tP;
      do {
         window_tP = latnP.endsWith(`${window_tP}`);
         if (defaultroombgh) {
            break;
         }
      } while (defaultroombgh && ((imagemanagerS.length / 5) > 4 || imagemanagerS.length > 5));
      if (5 <= (iconstarS ^ latnP.length)) {
         iconstarS >>= Math.min(Math.abs(2), 2);
      }
       let flyerq = 2.0;
       let apple8 = 0.0;
      let videobufferloading0 = apple8 <= 6103668.0;
      do {
         apple8 += parseFloat(`${imagemanagerS.length}`);
         if (videobufferloading0) {
            break;
         }
      } while ((!imagemanagerS.includes(apple8)) && videobufferloading0);
      let defaultteamU = latnP.length >= 6983857;
      do {
          let logoutU: Array<any> = [857, 620];
          let nbatrophyX = String.fromCharCode(100,105,118,105,115,105,111,110,95,121,95,57,51,0);
          let heartE = 0.0;
          let defaultprofilepics = 1;
         latnP = `${parseInt(`${heartE}`) % 3}`;
         logoutU = [(String.fromCharCode(76,0) == nbatrophyX ? defaultprofilepics : nbatrophyX.length)];
         heartE += parseFloat(`${nbatrophyX.length}`);
         defaultprofilepics ^= 3 | logoutU.length;
         if (defaultteamU) {
            break;
         }
      } while (defaultteamU && (latnP.length <= 2));
       let i_centerY = String.fromCharCode(120,95,57,51,95,114,101,113,117,97,110,116,0);
      for (let z = 0; z < 1; z++) {
         window_tP = !window_tP && latnP.length >= 43;
      }
         imagemanagerS = [latnP.length / 1];
      fillh = mbridgeR.length > 14;
   }
   while (!mbridgeR.startsWith(`${umengF}`)) {
       let productG = 4;
       let incidentZ = String.fromCharCode(99,97,118,115,95,107,95,53,57,0);
         incidentZ += `${incidentZ.length >> (Math.min(3, Math.abs(productG)))}`;
          let bingx = 4.0;
         incidentZ = `${productG}`;
         bingx *= parseInt(`${bingx}`);
      let liveZ = productG <= 8104249;
      do {
          let dicew = String.fromCharCode(104,95,55,52,95,102,97,109,105,108,121,0);
          let prediction0: Array<any> = [518, 498, 833];
         productG *= 2;
         dicew += `${(String.fromCharCode(115,0) == dicew ? dicew.length : prediction0.length)}`;
         prediction0.push(2);
         if (liveZ) {
            break;
         }
      } while (liveZ && (5 > (5 ^ productG)));
         productG &= productG;
      while (1 <= productG) {
         incidentZ += `${incidentZ.length ^ productG}`;
         break;
      }
          let liblogger6 = String.fromCharCode(97,95,49,50,95,100,121,110,98,117,102,0);
          let historyB: Array<any> = [553, 926, 621];
          let countdownN = 2.0;
         incidentZ += `${liblogger6.length >> (Math.min(Math.abs(2), 4))}`;
         liblogger6 = `${parseInt(`${countdownN}`)}`;
         historyB.push(parseInt(`${countdownN}`) * 1);
      mbridgeR = `${elementsW.size - turnQ.length}`;
      break;
   }
       let resendx = 1.0;
       let upgrade3 = 4.0;
       let transferl = false;
         upgrade3 /= Math.max(parseFloat(`${parseInt(`${upgrade3}`) << (Math.min(Math.abs(parseInt(`${resendx}`)), 4))}`), 2);
         transferl = transferl || resendx > 76.41;
      let forwarde = resendx >= 5621280.0;
      do {
         resendx *= ((transferl ? 2 : 3) | parseInt(`${resendx}`));
         if (forwarde) {
            break;
         }
      } while (forwarde && (1.81 == resendx));
      if (!transferl) {
         upgrade3 -= parseFloat(`${parseInt(`${resendx}`) - 2}`);
      }
         resendx -= parseInt(`${resendx}`) * 3;
      let splashF = resendx <= 8757481.0;
      do {
          let manifest1 = 2.0;
          let promotionA = String.fromCharCode(100,114,105,118,101,114,95,108,95,55,51,0);
          let penaltyz: Map<any, any> = new Map([[String.fromCharCode(97,95,51,55,95,117,112,108,111,97,100,101,114,0),49], [String.fromCharCode(118,95,48,95,114,101,100,101,101,109,0),104]]);
         resendx /= Math.max((promotionA == String.fromCharCode(69,0) ? promotionA.length : parseInt(`${upgrade3}`)), 5);
         manifest1 *= 3;
         penaltyz.set(`${manifest1}`, penaltyz.size);
         if (splashF) {
            break;
         }
      } while ((2.60 >= upgrade3) && splashF);
         upgrade3 /= Math.max(1, parseFloat(`${parseInt(`${resendx}`) * 3}`));
          let catagoryc = String.fromCharCode(98,101,99,97,117,115,101,95,102,95,54,56,0);
          let maili = String.fromCharCode(105,119,97,108,115,104,95,103,95,53,51,0);
         upgrade3 /= Math.max(5, parseFloat(`${1}`));
         catagoryc = `${maili.length}`;
         maili = `${maili.length | 1}`;
      for (let h = 0; h < 1; h++) {
          let recommendationR = 0.0;
          let libjsijniprofilerv = String.fromCharCode(103,95,54,95,115,105,110,116,105,0);
          let combinev = 2.0;
          let subtextL = String.fromCharCode(109,105,115,117,115,101,95,48,95,55,53,0);
          let logouserm = String.fromCharCode(121,95,56,55,95,97,100,100,114,105,110,102,111,0);
         resendx *= subtextL.length;
         recommendationR += parseFloat(`${libjsijniprofilerv.length}`);
         libjsijniprofilerv += `${2 * parseInt(`${recommendationR}`)}`;
         combinev /= Math.max((parseFloat(`${String.fromCharCode(70,0) == libjsijniprofilerv ? parseInt(`${combinev}`) : libjsijniprofilerv.length}`)), 3);
         subtextL += `${(String.fromCharCode(68,0) == logouserm ? logouserm.length : parseInt(`${recommendationR}`))}`;
      }
      elementsW = new Map([[`${umengF}`, ((umengF ? 4 : 5) | parseInt(`${upgrade3}`))]]);
   let tempnodatae = areax >= 9860888;
   do {
       let libreactnativeblobO = 1.0;
       let iconsubssuccessI = true;
         libreactnativeblobO /= Math.max(((iconsubssuccessI ? 4 : 4) << (Math.min(Math.abs(parseInt(`${libreactnativeblobO}`)), 2))), 3);
      while (4.33 < (1.72 - libreactnativeblobO)) {
          let iconlogout9: Map<any, any> = new Map([[String.fromCharCode(100,95,49,53,95,115,113,108,105,116,101,114,98,117,0),856], [String.fromCharCode(114,95,50,52,95,120,105,110,99,0),215], [String.fromCharCode(109,97,120,98,117,114,115,116,95,103,95,55,52,0),381]]);
          let knewsJ = 2;
         iconsubssuccessI = iconlogout9.size == 57;
         iconlogout9.set(`${knewsJ}`, knewsJ);
         break;
      }
       let tempnodatag = 5;
      let settingQ = iconsubssuccessI ? !iconsubssuccessI : iconsubssuccessI;
      do {
         iconsubssuccessI = !iconsubssuccessI;
         if (settingQ) {
            break;
         }
      } while ((!iconsubssuccessI && (libreactnativeblobO * 5.7) <= 4.29) && settingQ);
         iconsubssuccessI = tempnodatag == 78;
         tempnodatag += tempnodatag % (Math.max(1, parseInt(`${libreactnativeblobO}`)));
      areax /= Math.max(4, ((iconsubssuccessI ? 2 : 3) & 3));
      if (tempnodatae) {
         break;
      }
   } while ((4 == mbridgeR.length) && tempnodatae);
   for (let c = 0; c < 3; c++) {
      submitE = (95 < ((!fillh ? elementsW.size : 83) ^ elementsW.size));
   }
      elementsW = new Map([[`${elementsW.size}`, ((umengF ? 4 : 4) >> (Math.min(Math.abs(3), 4)))]]);
      submitE = (elementsW.size / (Math.max(styleR.length, 6))) == 43;

    onClose();

       let inviteb: Map<any, any> = new Map([[String.fromCharCode(118,95,53,50,95,112,111,112,117,112,115,0),169], [String.fromCharCode(100,101,112,97,99,107,101,116,105,122,101,114,95,57,95,55,55,0),790]]);
       let relatedg = 1.0;
      if (Array.from(inviteb.values()).includes(relatedg)) {
         relatedg /= Math.max(parseFloat(`${inviteb.size}`), 4);
      }
      while ((relatedg * parseFloat(`${inviteb.size}`)) < 2.80) {
          let ewardedA: Array<any> = [String.fromCharCode(118,95,50,51,95,110,111,114,111,117,110,100,0), String.fromCharCode(99,111,108,114,95,113,95,51,51,0)];
          let injuryI = 0.0;
          let sportsY: Map<any, any> = new Map([[String.fromCharCode(113,95,55,55,95,115,105,112,114,0),239], [String.fromCharCode(115,116,114,108,101,110,95,104,95,52,55,0),114], [String.fromCharCode(116,97,112,101,95,117,95,54,51,0),120]]);
          let topic2 = 4.0;
          let feedbackb = String.fromCharCode(103,101,110,101,114,97,116,105,111,110,95,98,95,53,52,0);
         relatedg /= Math.max(3, parseFloat(`${2}`));
         ewardedA = [2 ^ parseInt(`${injuryI}`)];
         injuryI += 1;
         sportsY.set(`${injuryI}`, 2 << (Math.min(Math.abs(parseInt(`${injuryI}`)), 5)));
         topic2 /= Math.max(parseFloat(`${sportsY.size}`), 3);
         feedbackb = `${1 / (Math.max(parseInt(`${topic2}`), 4))}`;
         break;
      }
      let privatechatbgs = inviteb.size >= 8658096;
      do {
          let halffieldimageC = 4;
          let suggestionF = 3.0;
          let mountingz = String.fromCharCode(105,95,51,51,95,110,97,110,111,115,101,99,111,110,100,115,0);
          let textj = String.fromCharCode(110,111,114,109,95,101,95,50,53,0);
         inviteb.set(`${suggestionF}`, parseInt(`${suggestionF}`) * mountingz.length);
         halffieldimageC &= textj.length >> (Math.min(Math.abs(2), 5));
         mountingz += `${halffieldimageC}`;
         textj += `${(String.fromCharCode(65,0) == textj ? halffieldimageC : textj.length)}`;
         if (privatechatbgs) {
            break;
         }
      } while (privatechatbgs && (relatedg < parseFloat(`${inviteb.size}`)));
      for (let i = 0; i < 1; i++) {
         inviteb.set(`${relatedg}`, inviteb.size + parseInt(`${relatedg}`));
      }
      let switch_x0 = 8714433.0 >= relatedg;
      do {
         relatedg *= parseFloat(`${3}`);
         if (switch_x0) {
            break;
         }
      } while ((inviteb.get(`${relatedg}`) == null) && switch_x0);
       let nativeex5 = String.fromCharCode(102,95,54,52,95,99,111,112,121,0);
      styleR += "2";
   if (qaagd.length >= 2 || 1 >= (qaagd.length ^ 2)) {
      fillh = !fillh && umengF;
   }
      styleR += `${membero.length}`;
   if ((areax + turnQ.length) <= 3) {
      turnQ += `${membero.length % 1}`;
   }
   if ((3 << (Math.min(5, mbridgeR.length))) >= 4) {
       let logoj = 3;
       let humidity2 = 5.0;
       let iconshareU = 1.0;
       let securityO = 0.0;
       let reactnativejsL = 2.0;
         humidity2 += 1 ^ parseInt(`${securityO}`);
         securityO += logoj;
       let refreshborderlessA = String.fromCharCode(97,95,57,57,95,97,103,97,116,101,0);
       let mountingK = String.fromCharCode(101,95,49,56,95,112,114,105,111,114,105,116,121,0);
         reactnativejsL += parseFloat(`${parseInt(`${reactnativejsL}`) / (Math.max(8, refreshborderlessA.length))}`);
      for (let x = 0; x < 1; x++) {
         humidity2 *= refreshborderlessA.length * parseInt(`${securityO}`);
      }
         refreshborderlessA = `${parseInt(`${reactnativejsL}`)}`;
         securityO -= refreshborderlessA.length >> (Math.min(2, Math.abs(parseInt(`${securityO}`))));
         refreshborderlessA += `${parseInt(`${iconshareU}`) / (Math.max(refreshborderlessA.length, 6))}`;
         humidity2 *= refreshborderlessA.length << (Math.min(Math.abs(3), 4));
      while (humidity2 == 4.51) {
         humidity2 *= 1 & mountingK.length;
         break;
      }
      for (let l = 0; l < 2; l++) {
          let matchesy = String.fromCharCode(100,95,56,56,95,112,97,99,107,105,110,103,0);
          let stepo: Array<any> = [838, 367, 713];
         logoj %= Math.max(refreshborderlessA.length, 4);
         matchesy = `${matchesy.length / (Math.max(1, 4))}`;
         stepo = [(String.fromCharCode(55,0) == matchesy ? matchesy.length : stepo.length)];
      }
       let libapminsightb4 = String.fromCharCode(98,95,51,95,119,105,100,116,104,0);
       let taiwanN = String.fromCharCode(109,111,100,112,108,117,103,95,99,95,54,54,0);
      if (4.33 < (3.65 + securityO) && 3.65 < (humidity2 * securityO)) {
          let skipI = String.fromCharCode(114,95,53,54,95,118,114,101,103,105,111,110,0);
          let homeactivee = 2.0;
          let iconqqQ: Array<any> = [382, 873];
         securityO *= 2 >> (Math.min(2, Math.abs(logoj)));
         skipI += `${(String.fromCharCode(52,0) == skipI ? parseInt(`${homeactivee}`) : skipI.length)}`;
         homeactivee /= Math.max(skipI.length, 3);
         iconqqQ.push(iconqqQ.length);
      }
      for (let c = 0; c < 3; c++) {
         refreshborderlessA += `${logoj >> (Math.min(Math.abs(parseInt(`${iconshareU}`)), 1))}`;
      }
       let blacklistQ = 4.0;
       let teamdetailsbgD = 3.0;
      areax /= Math.max(4, 2 + parseInt(`${reactnativejsL}`));
   }
   for (let e = 0; e < 2; e++) {
       let whitevideolivem: Map<any, any> = new Map([[String.fromCharCode(114,97,115,116,101,114,105,122,97,116,105,111,110,95,119,95,51,57,0),String.fromCharCode(121,95,50,53,95,112,117,108,115,101,0)], [String.fromCharCode(113,95,54,50,95,104,115,118,97,0),String.fromCharCode(117,95,49,51,95,115,108,105,99,101,0)], [String.fromCharCode(118,95,57,56,95,111,98,106,110,105,100,0),String.fromCharCode(106,95,55,48,95,115,99,101,110,101,99,117,116,0)]]);
       let informationx = String.fromCharCode(107,95,51,56,95,99,116,114,108,0);
         informationx = `${(String.fromCharCode(51,0) == informationx ? informationx.length : whitevideolivem.size)}`;
          let libreactnativejniB = String.fromCharCode(113,117,97,100,95,111,95,49,49,0);
         whitevideolivem = new Map([[libreactnativejniB, libreactnativejniB.length % (Math.max(3, 4))]]);
      while ((informationx.length | whitevideolivem.size) < 2 || 5 < (informationx.length | 2)) {
         informationx = `${1 - informationx.length}`;
         break;
      }
      if ((1 << (Math.min(5, informationx.length))) == 2) {
          let iconscheduleu = 2.0;
          let lightD: Map<any, any> = new Map([[String.fromCharCode(98,121,116,101,115,104,117,109,97,110,95,100,95,52,49,0),false ], [String.fromCharCode(114,101,99,111,103,110,105,122,101,114,115,95,112,95,55,51,0),false ], [String.fromCharCode(118,101,114,105,102,105,97,116,105,111,110,95,114,95,57,54,0),true ]]);
          let basketballtrophyb = String.fromCharCode(106,112,101,103,108,115,95,110,95,57,56,0);
         whitevideolivem.set(`${iconscheduleu}`, informationx.length >> (Math.min(Math.abs(2), 2)));
         iconscheduleu += parseFloat(`${1 ^ basketballtrophyb.length}`);
         lightD.set(`${basketballtrophyb}`, lightD.size / (Math.max(basketballtrophyb.length, 6)));
      }
      if (!informationx.endsWith(`${whitevideolivem.size}`)) {
         whitevideolivem.set(`${informationx}`, informationx.length);
      }
      let pointk = whitevideolivem.size >= 5752770;
      do {
         whitevideolivem.set(informationx, whitevideolivem.size / 3);
         if (pointk) {
            break;
         }
      } while (((whitevideolivem.size + informationx.length) < 5) && pointk);
      turnQ += `${styleR.length << (Math.min(Math.abs(1), 1))}`;
   }
      styleR = `${((submitE ? 1 : 2))}`;
      turnQ += "1";
   let cornerG = fillh ? !fillh : fillh;
   do {
       let yellowA: Array<any> = [270, 508, 301];
       let promotionl: Map<any, any> = new Map([[String.fromCharCode(116,95,50,49,95,102,105,103,104,116,101,114,115,0),String.fromCharCode(102,105,110,97,108,105,115,101,114,95,56,95,56,49,0)], [String.fromCharCode(118,101,114,116,115,95,118,95,49,55,0),String.fromCharCode(104,105,115,116,111,103,114,97,109,115,95,119,95,57,56,0)], [String.fromCharCode(120,95,54,56,95,102,114,97,109,101,102,111,114,109,97,116,0),String.fromCharCode(108,95,51,56,95,114,101,99,111,114,100,0)]]);
       let zhengpiand = false;
       let anytimeD = 2;
       let libfile7: Array<any> = [String.fromCharCode(113,95,52,53,95,114,101,115,111,108,118,101,0), String.fromCharCode(120,95,49,50,95,112,111,108,101,0)];
         anytimeD += 1;
      if (!yellowA.includes(anytimeD)) {
          let reactnativeratingso = String.fromCharCode(112,95,52,50,95,115,121,110,99,104,114,111,110,111,117,115,108,121,0);
          let action_ = 2;
          let anytimed = 1.0;
          let icontransferclub4: Array<any> = [223, 950];
         anytimeD &= 3;
         reactnativeratingso += `${action_ * 3}`;
         action_ -= action_ % 3;
         anytimed -= parseFloat(`${parseInt(`${anytimed}`)}`);
         icontransferclub4.push(action_);
      }
      for (let w = 0; w < 3; w++) {
          let rewardvideoV = 5;
          let gifgoale = true;
          let customp = false;
          let privatechatbg9 = String.fromCharCode(99,108,101,97,110,115,101,95,111,95,54,51,0);
          let iconarrowrightorangek = 0;
         zhengpiand = !gifgoale;
         rewardvideoV &= 3;
         customp = rewardvideoV <= 39;
         privatechatbg9 = `${rewardvideoV}`;
         iconarrowrightorangek >>= Math.min(Math.abs(rewardvideoV ^ 1), 1);
      }
         promotionl = new Map([[`${yellowA.length}`, 1]]);
       let favorite0 = 3.0;
      if (zhengpiand && (5.72 + favorite0) == 4.60) {
          let launchP = 5.0;
          let aboutj = String.fromCharCode(116,95,49,52,95,115,105,109,112,108,101,0);
          let emojij = 0.0;
          let issub1 = false;
          let libjsiX: Array<any> = [223, 211, 949];
         zhengpiand = issub1 && 90.13 > favorite0;
         launchP -= 2;
         aboutj += `${libjsiX.length}`;
         emojij *= parseFloat(`${libjsiX.length}`);
         issub1 = launchP >= 65.82 || 91 >= libjsiX.length;
      }
      for (let a = 0; a < 1; a++) {
          let filled_ = 1.0;
          let traminiQ = false;
          let sourcep = 5;
         favorite0 -= parseFloat(`${parseInt(`${filled_}`)}`);
         filled_ /= Math.max(1 + sourcep, 5);
         traminiQ = !traminiQ;
         sourcep ^= sourcep;
      }
      while ((2 - libfile7.length) <= 2 && 2 <= (anytimeD - libfile7.length)) {
         libfile7.push(yellowA.length << (Math.min(Math.abs(1), 4)));
         break;
      }
      for (let m = 0; m < 1; m++) {
         promotionl = new Map([[`${promotionl.size}`, parseInt(`${favorite0}`)]]);
      }
       let libglog5 = true;
       let zhengpianR = true;
          let whitetickS = 5.0;
         anytimeD *= 2;
         whitetickS /= Math.max(5, parseInt(`${whitetickS}`) & parseInt(`${whitetickS}`));
         zhengpianR = parseFloat(`${libfile7.length}`) < favorite0;
      for (let v = 0; v < 3; v++) {
         favorite0 /= Math.max(parseFloat(`${1}`), 5);
      }
         zhengpiand = !zhengpianR && promotionl.size <= 24;
      for (let j = 0; j < 2; j++) {
         promotionl = new Map([[`${libfile7.length}`, 1]]);
      }
      fillh = 23 == styleR.length;
      if (cornerG) {
         break;
      }
   } while (cornerG && (fillh));
   let philippinesG = umengF ? !umengF : umengF;
   do {
       let filledA = String.fromCharCode(105,95,52,50,95,100,101,116,97,105,108,115,0);
       let modelc = String.fromCharCode(105,103,104,108,105,103,104,116,115,95,49,95,49,50,0);
       let yellowanimationliveI: Map<any, any> = new Map([[String.fromCharCode(115,110,97,112,115,104,111,116,116,101,114,95,106,95,55,56,0),168], [String.fromCharCode(111,95,54,51,95,118,111,119,101,108,115,0),331], [String.fromCharCode(105,95,54,49,95,99,111,112,121,99,111,111,107,101,114,0),930]]);
       let libswresamplec = String.fromCharCode(108,95,55,53,95,100,105,103,101,115,116,115,0);
         modelc = `${libswresamplec.length}`;
      let dice4 = String.fromCharCode(52,116,122,119,98,0) == libswresamplec;
      do {
         libswresamplec += `${(String.fromCharCode(118,0) == modelc ? yellowanimationliveI.size : modelc.length)}`;
         if (dice4) {
            break;
         }
      } while (dice4 && (1 <= modelc.length));
      if (2 <= (1 >> (Math.min(2, Math.abs(yellowanimationliveI.size)))) || 4 <= (1 >> (Math.min(5, Math.abs(yellowanimationliveI.size))))) {
          let mounting4 = String.fromCharCode(108,101,110,103,116,104,95,56,95,55,53,0);
          let yellow4 = String.fromCharCode(115,95,53,52,95,112,97,114,105,116,121,0);
         yellowanimationliveI.set(yellow4, 3 >> (Math.min(4, mounting4.length)));
      }
      let sharewhitei = 9471688 <= yellowanimationliveI.size;
      do {
         yellowanimationliveI = new Map([[`${yellowanimationliveI.size}`, 3]]);
         if (sharewhitei) {
            break;
         }
      } while (sharewhitei && (1 > (yellowanimationliveI.size << (Math.min(Math.abs(2), 2)))));
         yellowanimationliveI = new Map([[`${yellowanimationliveI.size}`, yellowanimationliveI.size | 3]]);
      if (filledA.length <= libswresamplec.length) {
         libswresamplec += `${1 ^ modelc.length}`;
      }
      let yellowscoreballw = 7279049 <= filledA.length;
      do {
          let inviteC: Map<any, any> = new Map([[String.fromCharCode(102,95,51,48,95,110,116,104,0),false ], [String.fromCharCode(114,101,112,108,97,99,105,110,103,95,49,95,53,48,0),true ], [String.fromCharCode(120,95,52,48,95,105,110,99,114,109,101,114,103,101,0),false ]]);
         filledA += "3";
         inviteC = new Map([[`${inviteC.size}`, inviteC.size]]);
         if (yellowscoreballw) {
            break;
         }
      } while (yellowscoreballw && (!modelc.startsWith(`${filledA.length}`)));
       let gmailU = String.fromCharCode(113,95,55,57,95,121,111,103,97,0);
       let shrinko = String.fromCharCode(99,95,57,51,95,105,110,115,105,100,101,0);
      while (!libswresamplec.includes(gmailU)) {
         libswresamplec += `${2 + gmailU.length}`;
         break;
      }
      if (2 <= modelc.length) {
         yellowanimationliveI = new Map([[`${yellowanimationliveI.size}`, 2]]);
      }
         gmailU = `${gmailU.length}`;
         modelc = "3";
      umengF = membero.length == 68;
      if (philippinesG) {
         break;
      }
   } while ((!fillh) && philippinesG);
      areax += (styleR == String.fromCharCode(120,0) ? (umengF ? 4 : 4) : styleR.length);
    dispatch(loginModalShown());
  }

  const handleOnPurchase = useCallback(() => {
       let loadingl = false;
    let vietnamm = String.fromCharCode(115,104,111,117,121,97,99,104,111,95,109,95,49,53,0);
    let sansj = String.fromCharCode(98,95,53,48,95,99,111,110,102,105,103,117,114,97,98,108,101,0);
    let readp = 3.0;
    let catalogG = true;
    let drag_ = 1.0;
    let i_titleJ = 2.0;
    let over1 = true;
    let redirectu = String.fromCharCode(106,95,51,52,95,115,116,101,112,119,105,115,101,0);
    let streamingA: Array<any> = [String.fromCharCode(118,95,49,51,95,110,111,116,104,101,108,100,0), String.fromCharCode(117,110,99,114,111,112,112,101,100,95,112,95,54,52,0)];
    let screen7 = 2.0;
    let homeinactives = String.fromCharCode(115,119,105,112,101,95,54,95,50,0);
   while (!over1) {
      readp -= parseFloat(`${1 + parseInt(`${drag_}`)}`);
      break;
   }
   if ((sansj.length - parseInt(`${drag_}`)) <= 3 && 2.80 <= (drag_ - sansj.length)) {
      drag_ += (redirectu == String.fromCharCode(115,0) ? redirectu.length : parseInt(`${drag_}`));
   }
      readp /= Math.max(1, parseFloat(`${sansj.length % (Math.max(1, redirectu.length))}`));
   while (sansj.length > parseInt(`${drag_}`)) {
       let blacklist5: Array<any> = [String.fromCharCode(98,95,50,50,95,108,105,98,97,118,117,116,105,108,0), String.fromCharCode(103,95,49,54,95,100,101,108,105,109,0), String.fromCharCode(109,117,108,116,105,102,114,97,109,101,95,116,95,52,0)];
       let t_titleG = String.fromCharCode(100,105,114,101,99,116,111,114,121,95,112,95,51,56,0);
       let annerL = String.fromCharCode(114,103,98,121,117,118,95,56,95,55,53,0);
       let fieldC = String.fromCharCode(104,99,108,114,95,52,95,53,51,0);
         fieldC += `${(t_titleG == String.fromCharCode(99,0) ? t_titleG.length : annerL.length)}`;
       let clubB = String.fromCharCode(102,102,105,111,95,49,95,53,54,0);
       let skipi = String.fromCharCode(115,95,55,50,95,99,111,102,97,99,116,111,114,0);
         blacklist5 = [annerL.length | blacklist5.length];
          let bellS = 2.0;
          let libyogay: Array<any> = [365, 738, 279];
          let basketballawayteam6 = true;
         skipi = `${parseInt(`${bellS}`)}`;
         bellS *= (3 + (basketballawayteam6 ? 5 : 5));
         libyogay = [2];
      if (annerL.length == 5) {
         annerL += `${t_titleG.length}`;
      }
         clubB += "1";
      while (blacklist5.length <= t_titleG.length) {
         t_titleG += `${blacklist5.length}`;
         break;
      }
      while (t_titleG == clubB) {
          let bingQ = String.fromCharCode(120,95,49,52,95,115,108,111,119,0);
          let currento = 1;
          let commentX: Map<any, any> = new Map([[String.fromCharCode(100,114,97,102,116,115,95,109,95,57,53,0),108], [String.fromCharCode(115,116,121,108,101,100,95,108,95,49,53,0),340]]);
         clubB += `${fieldC.length}`;
         bingQ = `${bingQ.length}`;
         currento -= currento;
         commentX.set(bingQ, bingQ.length);
         break;
      }
      for (let g = 0; g < 3; g++) {
          let hoverm = false;
         blacklist5 = [fieldC.length - clubB.length];
         hoverm = hoverm || !hoverm;
      }
      for (let z = 0; z < 2; z++) {
         fieldC += "3";
      }
       let score9 = String.fromCharCode(113,95,55,55,95,110,101,105,103,104,98,111,117,114,0);
       let stationo = String.fromCharCode(114,95,55,49,95,116,114,97,110,115,108,97,116,101,0);
          let pingq = 4.0;
         blacklist5 = [t_titleG.length ^ annerL.length];
         pingq += parseFloat(`${parseInt(`${pingq}`) + 2}`);
      drag_ /= Math.max(((over1 ? 5 : 3) | parseInt(`${readp}`)), 5);
      break;
   }

    onClose();

       let iconclosewhitebg7 = true;
       let audiencer = String.fromCharCode(108,111,103,105,115,116,105,99,95,101,95,54,50,0);
       let overy: Array<any> = [887, 967];
      let showlessv = String.fromCharCode(51,117,111,112,48,0) == audiencer;
      do {
         audiencer += "3";
         if (showlessv) {
            break;
         }
      } while ((!iconclosewhitebg7) && showlessv);
      for (let x = 0; x < 1; x++) {
         overy = [((iconclosewhitebg7 ? 1 : 2) + overy.length)];
      }
      for (let c = 0; c < 1; c++) {
          let backwhite7 = 3.0;
         overy = [overy.length];
         backwhite7 -= parseFloat(`${3 + parseInt(`${backwhite7}`)}`);
      }
      if (iconclosewhitebg7) {
         overy = [overy.length | 1];
      }
      let reactx = String.fromCharCode(121,119,115,0) == audiencer;
      do {
         audiencer = `${audiencer.length}`;
         if (reactx) {
            break;
         }
      } while (reactx && (4 > (audiencer.length / (Math.max(2, 2)))));
      let cornerkickZ = 5782720 <= audiencer.length;
      do {
         audiencer = `${audiencer.length}`;
         if (cornerkickZ) {
            break;
         }
      } while (cornerkickZ && (2 >= (audiencer.length / 4)));
       let mini5 = 3;
       let statsnomoredataG = 4.0;
       let firebaseP = 5.0;
         overy.push(3 & audiencer.length);
      vietnamm = `${2 >> (Math.min(3, vietnamm.length))}`;
       let thailandt = String.fromCharCode(109,111,110,105,116,111,114,95,113,95,56,48,0);
       let benefit1 = String.fromCharCode(98,101,103,105,110,95,54,95,51,51,0);
       let analytics1 = true;
         benefit1 = `${thailandt.length}`;
       let libfileO = String.fromCharCode(107,101,121,110,97,109,101,95,108,95,56,53,0);
         thailandt += `${(3 * (analytics1 ? 4 : 2))}`;
         benefit1 += `${((analytics1 ? 3 : 4) / (Math.max(10, benefit1.length)))}`;
       let commonJ = String.fromCharCode(121,95,53,49,95,103,97,108,108,101,114,121,0);
      let philippinesq = String.fromCharCode(116,112,97,53,0) == benefit1;
      do {
         benefit1 += `${benefit1.length + 3}`;
         if (philippinesq) {
            break;
         }
      } while (philippinesq && (!libfileO.endsWith(benefit1)));
      while (!benefit1.includes(`${thailandt.length}`)) {
         benefit1 += `${commonJ.length / (Math.max(1, 4))}`;
         break;
      }
         benefit1 = `${thailandt.length | libfileO.length}`;
         thailandt = `${commonJ.length}`;
      redirectu += `${2 >> (Math.min(3, benefit1.length))}`;
      redirectu = `${(String.fromCharCode(85,0) == redirectu ? redirectu.length : (over1 ? 5 : 3))}`;
   let morex = i_titleJ >= 6093681.0;
   do {
      i_titleJ *= (parseFloat(`${(catalogG ? 4 : 2) & parseInt(`${readp}`)}`));
      if (morex) {
         break;
      }
   } while (morex && ((drag_ + i_titleJ) <= 2.66 && 3.89 <= (drag_ + 2.66)));
    dispatch(loginModalShown());

   if ((i_titleJ - 3.43) > 4.26 && !loadingl) {
       let libreactz = String.fromCharCode(116,95,51,50,95,116,97,110,103,101,110,116,0);
          let libreanimatedA: Array<any> = [470, 805];
          let iconshareR = 5.0;
          let neonM = 2;
         libreactz = "2";
         libreanimatedA.push(libreanimatedA.length ^ 3);
         iconshareR *= 1 | neonM;
         neonM &= parseInt(`${iconshareR}`) % 2;
         libreactz += `${(libreactz == String.fromCharCode(76,0) ? libreactz.length : libreactz.length)}`;
          let xvod7 = String.fromCharCode(100,105,115,115,105,109,95,121,95,49,48,48,0);
          let fastforward5: Array<any> = [418, 877];
          let frame_mk: Array<any> = [String.fromCharCode(108,95,52,54,95,97,98,108,0), String.fromCharCode(112,95,55,53,95,112,117,98,108,105,99,107,101,121,115,0), String.fromCharCode(120,95,53,48,95,115,117,99,99,101,101,100,0)];
         libreactz += "3";
         xvod7 = `${(String.fromCharCode(74,0) == xvod7 ? fastforward5.length : xvod7.length)}`;
         fastforward5.push(1);
         frame_mk.push(fastforward5.length % 3);
      i_titleJ /= Math.max(parseFloat(`${sansj.length << (Math.min(5, libreactz.length))}`), 2);
   }
      i_titleJ -= (parseFloat(`${redirectu == String.fromCharCode(52,0) ? redirectu.length : parseInt(`${drag_}`)}`));
   let pressureJ = over1 ? !over1 : over1;
   do {
      over1 = (catalogG ? over1 : catalogG);
      if (pressureJ) {
         break;
      }
   } while ((!over1 && loadingl) && pressureJ);
      redirectu = `${(parseInt(`${i_titleJ}`) - (over1 ? 2 : 4))}`;
    dispatch(showLoginAction());
    
    
    
    
    


  }, []);

  return (
    <Modal
      visible={showCondition}
      transparent={true}
      animationType="fade"
      onRequestClose={onCloseModal}
    >
      <TouchableWithoutFeedback onPress={onCloseModal}>
        <View style={{ flex: 1, backgroundColor: "rgba(0, 0, 0, 0.4)" }}>
          <VipLoginAlertModal
            coverBackground={sportBg}
            coverImage={commonModels}
            onPurchase={handleOnPurchase}
            onClose={onCloseModal}
            showCondition={showCondition}
          />
        </View>
      </TouchableWithoutFeedback>
    </Modal>
  );
};
