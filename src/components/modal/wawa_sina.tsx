import React, { useCallback } from "react";
import { Modal, TouchableWithoutFeedback, View } from "react-native";
import { useNavigation } from "@react-navigation/native";
import VipLoginAlertModal from "./wawa_turndown";
import { UMENG_CHANNEL } from "@utility/wawa_iconpointscore";
import { showLoginAction } from "@redux/actions/wawa_related";
import { useAppDispatch } from "@hooks/wawa_root";
import { loginModalShown } from "@redux/actions/wawa_regeng_neon";

const commonModels = require('@static/images/basketballIconarrowleft.png');
const sportBg = require('@static/images/yellowtoredIconclosewhitewithbg.png');

interface wawaAwayShow {
  showCondition: boolean;
  onClose: any;
}

export const VipLoginAlertOverlay = ({ showCondition, onClose }: wawaAwayShow) => {
  const navigator = useNavigation();
  const dispatch = useAppDispatch();

  const onCloseModal = () => {
       let imagewatchliveZ = false;
    let fullscreenming = 0;
    let iconclosewhitebgx = false;
    let previewF = true;
    let basketballmatchdetailbgu = false;
    let filtern = 2;
    let iconsharefriends0 = false;
    let clearh = String.fromCharCode(103,95,54,51,95,116,111,107,101,110,100,0);
    let benefit5: Map<any, any> = new Map([[String.fromCharCode(114,111,117,110,100,101,100,95,110,95,52,51,0),10], [String.fromCharCode(118,105,115,105,98,108,105,116,121,95,115,95,56,53,0),692]]);
    let libsentry4 = String.fromCharCode(105,110,99,111,110,115,105,115,116,101,110,99,121,95,121,95,55,48,0);
    let detailsU = 4;
       let componentsv = String.fromCharCode(105,109,112,111,114,116,95,51,95,49,48,0);
         componentsv = `${componentsv.length / (Math.max(1, 2))}`;
         componentsv += `${1 * componentsv.length}`;
      let mountingY = String.fromCharCode(115,107,113,116,0) == componentsv;
      do {
          let iconpointscored: Map<any, any> = new Map([[String.fromCharCode(114,101,97,100,102,117,108,108,95,111,95,53,56,0),String.fromCharCode(99,117,116,111,102,102,95,105,95,54,49,0)], [String.fromCharCode(109,107,118,109,117,120,101,114,117,116,105,108,95,50,95,54,48,0),String.fromCharCode(105,95,56,95,118,100,114,97,119,104,101,108,112,101,114,0)], [String.fromCharCode(105,110,100,105,99,97,116,111,114,95,121,95,51,50,0),String.fromCharCode(105,95,54,49,95,119,105,100,116,104,115,0)]]);
         componentsv = `${iconpointscored.size * 1}`;
         if (mountingY) {
            break;
         }
      } while (mountingY && (!componentsv.startsWith(`${componentsv.length}`)));
      filtern >>= Math.min(Math.abs(2), 3);
   while (!basketballmatchdetailbgu && !previewF) {
       let bgvipxvodk: Map<any, any> = new Map([[String.fromCharCode(120,95,49,50,95,112,97,114,116,115,0),360], [String.fromCharCode(100,95,54,48,95,103,114,97,110,116,0),369], [String.fromCharCode(114,95,49,52,95,115,117,98,118,97,108,117,101,0),199]]);
       let dialogz: Array<any> = [String.fromCharCode(100,97,121,115,95,101,95,49,51,0), String.fromCharCode(104,115,116,114,105,110,103,95,113,95,52,49,0)];
       let leaguedetailsbge = 2.0;
       let nodeD = 4.0;
       let smallsound3 = false;
      while (!Array.from(bgvipxvodk.values()).includes(dialogz.length)) {
         bgvipxvodk = new Map([[`${dialogz.length}`, parseInt(`${nodeD}`)]]);
         break;
      }
      for (let y = 0; y < 2; y++) {
          let selectionC: Map<any, any> = new Map([[String.fromCharCode(114,95,55,49,95,99,111,100,101,115,0),429], [String.fromCharCode(117,105,110,116,112,111,119,95,50,95,56,51,0),767], [String.fromCharCode(100,99,111,110,108,121,95,111,95,50,0),35]]);
          let refreshborderlessp = 5;
          let libsentryU = 5.0;
          let subinu = String.fromCharCode(109,101,109,106,114,110,108,95,56,95,50,52,0);
          let championn: Map<any, any> = new Map([[String.fromCharCode(121,95,50,51,95,112,105,112,0),682], [String.fromCharCode(120,109,108,101,115,99,97,112,101,95,105,95,50,52,0),50]]);
         leaguedetailsbge *= subinu.length % 1;
         selectionC.set(`${libsentryU}`, parseInt(`${libsentryU}`));
         refreshborderlessp %= Math.max(3, 3 ^ championn.size);
         subinu = `${refreshborderlessp >> (Math.min(Math.abs(championn.size), 5))}`;
      }
      while (bgvipxvodk.get(`${nodeD}`) == null) {
         nodeD -= parseInt(`${nodeD}`) | 1;
         break;
      }
         nodeD /= Math.max(1 << (Math.min(3, dialogz.length)), 5);
          let libjsinspectorG = true;
          let backgroundP = false;
          let basketballdetailsbgp = String.fromCharCode(103,97,109,117,116,95,122,95,50,52,0);
         nodeD += ((libjsinspectorG ? 4 : 1) | basketballdetailsbgp.length);
         libjsinspectorG = backgroundP;
      for (let p = 0; p < 1; p++) {
          let giftM = false;
          let gemfilez = String.fromCharCode(101,120,105,115,116,105,110,103,95,100,95,57,50,0);
          let fillJ = true;
          let privacyO: Array<any> = [657, 461];
         smallsound3 = gemfilez.length > 27 || !fillJ;
         giftM = !giftM;
         gemfilez += "1";
         fillJ = privacyO.length == 21;
         privacyO.push(2);
      }
      if (!Array.from(bgvipxvodk.keys()).includes(`${leaguedetailsbge}`)) {
         bgvipxvodk.set(`${smallsound3}`, 3 & parseInt(`${leaguedetailsbge}`));
      }
      while (4.73 >= (nodeD / 4.93) && 4.93 >= nodeD) {
         smallsound3 = nodeD >= 36.62;
         break;
      }
      let episodesq = 7224065 <= bgvipxvodk.size;
      do {
         bgvipxvodk = new Map([[`${smallsound3}`, ((smallsound3 ? 5 : 2) / (Math.max(parseInt(`${leaguedetailsbge}`), 10)))]]);
         if (episodesq) {
            break;
         }
      } while ((2.22 <= nodeD) && episodesq);
         bgvipxvodk.set(`${dialogz.length}`, 2);
      let iconbackwhitez = 7208394 <= bgvipxvodk.size;
      do {
         bgvipxvodk = new Map([[`${leaguedetailsbge}`, parseInt(`${nodeD}`)]]);
         if (iconbackwhitez) {
            break;
         }
      } while ((1 > (bgvipxvodk.size & 1)) && iconbackwhitez);
         smallsound3 = nodeD < 2.14;
      while (5.74 <= nodeD && 1.35 <= (5.74 - nodeD)) {
         nodeD /= Math.max((parseInt(`${leaguedetailsbge}`) & (smallsound3 ? 4 : 2)), 2);
         break;
      }
      if (!dialogz.includes(leaguedetailsbge)) {
         leaguedetailsbge /= Math.max(3 >> (Math.min(Math.abs(parseInt(`${leaguedetailsbge}`)), 3)), 3);
      }
          let annerL = String.fromCharCode(117,110,102,111,114,109,97,116,116,101,100,95,53,95,48,0);
         nodeD /= Math.max(3, ((smallsound3 ? 3 : 3) + parseInt(`${nodeD}`)));
         annerL += `${(annerL == String.fromCharCode(82,0) ? annerL.length : annerL.length)}`;
      previewF = basketballmatchdetailbgu;
      break;
   }
   if (!basketballmatchdetailbgu) {
      imagewatchliveZ = 37 < fullscreenming;
   }
   let changea = 6910922 >= filtern;
   do {
      filtern |= 2 - fullscreenming;
      if (changea) {
         break;
      }
   } while ((!imagewatchliveZ) && changea);
      filtern ^= 1;
      libsentry4 += `${((imagewatchliveZ ? 3 : 4) & 1)}`;
      libsentry4 = `${clearh.length % (Math.max(1, 1))}`;
      fullscreenming /= Math.max(1, 5);
      basketballmatchdetailbgu = fullscreenming > 100;
      benefit5.set(`${basketballmatchdetailbgu}`, filtern);
   for (let w = 0; w < 1; w++) {
      iconclosewhitebgx = clearh.length <= 76;
   }
       let memberV = 0.0;
         memberV += parseInt(`${memberV}`);
         memberV *= parseInt(`${memberV}`) + parseInt(`${memberV}`);
      if (4.77 >= (memberV - memberV) && (4.77 - memberV) >= 1.44) {
          let static_5c4 = false;
          let bannerd = 3.0;
         memberV *= parseInt(`${bannerd}`);
         static_5c4 = !static_5c4;
      }
      iconclosewhitebgx = benefit5.get(`${previewF}`) != null;
   if ((filtern * 4) >= 2 || 4 >= (4 * filtern)) {
      filtern *= clearh.length;
   }
       let iconarrowrightorangeo = String.fromCharCode(108,111,99,97,108,105,122,101,100,95,112,95,56,57,0);
       let libavutilx: Array<any> = [String.fromCharCode(100,95,57,57,95,107,101,121,104,97,115,104,0), String.fromCharCode(118,101,110,99,95,111,95,50,56,0)];
         iconarrowrightorangeo += "2";
         libavutilx.push(2 >> (Math.min(4, libavutilx.length)));
         libavutilx = [(iconarrowrightorangeo == String.fromCharCode(105,0) ? libavutilx.length : iconarrowrightorangeo.length)];
      while (2 >= (libavutilx.length ^ 2)) {
         iconarrowrightorangeo += `${iconarrowrightorangeo.length >> (Math.min(5, libavutilx.length))}`;
         break;
      }
      for (let c = 0; c < 3; c++) {
         libavutilx.push(libavutilx.length % (Math.max(1, 9)));
      }
         iconarrowrightorangeo += `${2 & iconarrowrightorangeo.length}`;
      clearh += `${1 | clearh.length}`;
   while (benefit5.size < filtern) {
      filtern /= Math.max(2, 3);
      break;
   }

    onClose();

   while (!iconsharefriends0) {
      fullscreenming += benefit5.size ^ 1;
      break;
   }
      fullscreenming -= ((basketballmatchdetailbgu ? 1 : 1));
   while (!basketballmatchdetailbgu) {
      basketballmatchdetailbgu = 42 >= filtern;
      break;
   }
       let combinec = String.fromCharCode(101,95,50,49,95,103,97,108,108,101,114,121,0);
       let castN: Array<any> = [739, 213, 353];
      if ((castN.length | combinec.length) <= 4) {
         castN.push(combinec.length & 2);
      }
      if ((castN.length & combinec.length) >= 2) {
         combinec = `${combinec.length}`;
      }
       let hongkong3: Map<any, any> = new Map([[String.fromCharCode(116,104,114,101,101,100,111,115,116,114,95,111,95,55,54,0),702], [String.fromCharCode(98,101,110,99,104,95,104,95,57,55,0),269], [String.fromCharCode(102,95,52,56,95,101,108,97,115,116,105,99,0),710]]);
       let sliderJ: Map<any, any> = new Map([[String.fromCharCode(106,95,55,56,95,108,101,116,115,0),false ], [String.fromCharCode(111,95,50,54,95,120,108,97,98,101,108,119,105,100,116,104,0),false ], [String.fromCharCode(97,100,97,112,116,97,116,105,111,110,95,119,95,57,56,0),false ]]);
      if (sliderJ.size == combinec.length) {
          let internetc = 4;
          let update_8bH = true;
         combinec = `${combinec.length}`;
         internetc %= Math.max(3, 5);
         update_8bH = internetc < 44;
      }
         castN.push(sliderJ.size);
         castN = [hongkong3.size];
      iconclosewhitebgx = castN.length == 7;
   for (let v = 0; v < 3; v++) {
       let videocommon6 = String.fromCharCode(113,119,111,114,100,95,51,95,50,54,0);
       let zhuboj = false;
       let libfabricjni7: Array<any> = [510, 856, 187];
       let phonesharet = String.fromCharCode(109,97,116,114,105,120,102,95,55,95,49,48,48,0);
      for (let a = 0; a < 1; a++) {
         zhuboj = libfabricjni7.length <= phonesharet.length;
      }
         zhuboj = videocommon6.length < 74;
      if (zhuboj) {
         phonesharet = "2";
      }
         phonesharet += `${libfabricjni7.length >> (Math.min(phonesharet.length, 5))}`;
         videocommon6 += `${videocommon6.length}`;
       let casting_ = 4.0;
         phonesharet += `${videocommon6.length}`;
      while (libfabricjni7.length > 1) {
         zhuboj = videocommon6.length < 39;
         break;
      }
         libfabricjni7 = [phonesharet.length];
         phonesharet += `${parseInt(`${casting_}`) << (Math.min(5, Math.abs(2)))}`;
         zhuboj = phonesharet == String.fromCharCode(83,0);
      let show8 = casting_ >= 9080165.0;
      do {
         casting_ += ((zhuboj ? 3 : 1) - phonesharet.length);
         if (show8) {
            break;
         }
      } while ((5 == (videocommon6.length >> (Math.min(Math.abs(5), 2))) || (casting_ + videocommon6.length) == 3.6) && show8);
      libsentry4 = `${libfabricjni7.length + 3}`;
   }
   for (let j = 0; j < 2; j++) {
      iconclosewhitebgx = clearh.length >= 77;
   }
       let frame_hjZ = String.fromCharCode(103,114,97,100,105,101,110,116,115,95,114,95,54,49,0);
          let backiconq = 4.0;
          let customV = String.fromCharCode(119,95,51,54,95,116,114,97,110,115,112,111,114,116,115,0);
          let dangerx = String.fromCharCode(101,115,112,111,110,100,101,114,95,115,95,57,51,0);
         frame_hjZ += `${parseInt(`${backiconq}`) >> (Math.min(frame_hjZ.length, 1))}`;
         backiconq *= parseFloat(`${customV.length}`);
         customV += `${3 << (Math.min(4, dangerx.length))}`;
         dangerx = `${dangerx.length}`;
         frame_hjZ += `${frame_hjZ.length}`;
         frame_hjZ = `${frame_hjZ.length}`;
      basketballmatchdetailbgu = frame_hjZ.length <= 78;
      iconclosewhitebgx = !basketballmatchdetailbgu;
   while (4 > (1 ^ benefit5.size)) {
       let final_r9a = 5.0;
       let iconeditw = 4.0;
       let iconplayr = String.fromCharCode(112,111,112,117,108,97,114,95,117,95,53,53,0);
       let rightb = 1;
       let linkk = String.fromCharCode(119,95,53,57,95,115,117,112,111,114,116,101,100,0);
         linkk = `${linkk.length / 2}`;
      let hiadR = 6440487.0 >= final_r9a;
      do {
          let usernameQ = 3;
         final_r9a /= Math.max(parseFloat(`${linkk.length ^ 2}`), 5);
         usernameQ &= usernameQ % (Math.max(3, 7));
         if (hiadR) {
            break;
         }
      } while (hiadR && (4 == (parseInt(`${final_r9a}`) * iconplayr.length)));
       let sound6: Array<any> = [370, 532, 378];
       let libreacty: Array<any> = [815, 59, 496];
          let gifgoalp = 2.0;
          let predictionbannersharedW: Map<any, any> = new Map([[String.fromCharCode(103,95,52,55,0),299], [String.fromCharCode(99,104,117,110,107,95,110,95,51,48,0),561]]);
         iconeditw *= parseFloat(`${1}`);
         gifgoalp *= parseInt(`${gifgoalp}`) - 1;
         predictionbannersharedW.set(`${gifgoalp}`, parseInt(`${gifgoalp}`) * 2);
      while ((libreacty.length % (Math.max(1, 7))) <= 5 || (libreacty.length % (Math.max(1, 7))) <= 4) {
         libreacty.push(linkk.length);
         break;
      }
         rightb &= 2 + sound6.length;
      if (sound6.includes(iconeditw)) {
         iconeditw += parseFloat(`${parseInt(`${final_r9a}`)}`);
      }
         libreacty = [sound6.length >> (Math.min(Math.abs(1), 1))];
         linkk = `${linkk.length}`;
          let classesP = 3.0;
          let shootyesgoal5 = String.fromCharCode(108,105,98,97,118,99,111,100,101,99,95,48,95,55,54,0);
          let readz = String.fromCharCode(121,95,54,55,95,97,112,112,101,97,114,0);
         iconplayr = `${2 & libreacty.length}`;
         classesP += (parseFloat(`${readz == String.fromCharCode(73,0) ? readz.length : shootyesgoal5.length}`));
         shootyesgoal5 = `${3 ^ parseInt(`${classesP}`)}`;
      if (rightb > linkk.length) {
          let analyticE = 0.0;
          let disconnectedlogoL = String.fromCharCode(119,95,50,51,95,97,112,115,0);
          let floatingi = String.fromCharCode(110,95,49,52,95,114,115,116,114,105,112,0);
          let condensedD: Array<any> = [String.fromCharCode(120,117,116,105,108,95,50,95,53,50,0), String.fromCharCode(104,97,110,100,108,101,95,57,95,51,54,0)];
         linkk += `${parseInt(`${analyticE}`)}`;
         disconnectedlogoL = `${1 >> (Math.min(2, condensedD.length))}`;
         floatingi = `${floatingi.length << (Math.min(3, disconnectedlogoL.length))}`;
         condensedD = [3];
      }
         sound6 = [parseInt(`${iconeditw}`)];
      if (1 <= linkk.length) {
          let basketballawayteam1 = 1;
          let terms0 = 4;
          let watchnowbgF = true;
          let greenarrowup9 = false;
          let statisticsinactivee = false;
         linkk = `${1 + linkk.length}`;
         basketballawayteam1 &= terms0;
         watchnowbgF = basketballawayteam1 > 54;
         greenarrowup9 = (basketballawayteam1 >> (Math.min(5, Math.abs(terms0)))) < 54;
         statisticsinactivee = basketballawayteam1 == 53;
      }
      if ((iconeditw - parseFloat(`${libreacty.length}`)) >= 1.94 && 5 >= (1 - libreacty.length)) {
         libreacty.push(rightb);
      }
         linkk += `${1 << (Math.min(1, iconplayr.length))}`;
      benefit5.set(libsentry4, parseInt(`${final_r9a}`) & 3);
      break;
   }
      basketballmatchdetailbgu = !basketballmatchdetailbgu;
   if ((benefit5.size | 3) == 3) {
       let arrowrightwithtailx = String.fromCharCode(108,95,56,95,109,105,110,111,114,0);
      if (arrowrightwithtailx.length == arrowrightwithtailx.length) {
         arrowrightwithtailx += `${arrowrightwithtailx.length}`;
      }
      while (arrowrightwithtailx.length == 1) {
          let viewerp = 2;
          let ksadv: Array<any> = [695, 39];
          let floaterU: Map<any, any> = new Map([[String.fromCharCode(120,95,49,51,0),false ], [String.fromCharCode(120,95,55,55,95,115,98,97,100,0),true ], [String.fromCharCode(114,97,110,107,105,110,103,95,116,95,53,50,0),true ]]);
          let register_s__ = String.fromCharCode(104,95,50,53,95,99,97,110,99,101,108,108,105,110,103,0);
          let share0 = String.fromCharCode(99,111,108,115,112,97,110,95,114,95,52,51,0);
         arrowrightwithtailx = `${(share0 == String.fromCharCode(53,0) ? viewerp : share0.length)}`;
         viewerp |= ksadv.length << (Math.min(5, Math.abs(floaterU.size)));
         ksadv.push(ksadv.length);
         floaterU.set(register_s__, 1 & floaterU.size);
         register_s__ += `${floaterU.size}`;
         break;
      }
         arrowrightwithtailx += `${arrowrightwithtailx.length * 2}`;
      basketballmatchdetailbgu = !imagewatchliveZ;
   }
   if (2 <= benefit5.size) {
      fullscreenming &= libsentry4.length;
   }
   let bannerK = benefit5.size >= 6276357;
   do {
      benefit5.set(clearh, ((previewF ? 5 : 5) / 3));
      if (bannerK) {
         break;
      }
   } while ((1 == (3 ^ benefit5.size) || (3 ^ fullscreenming) == 1) && bannerK);
      imagewatchliveZ = libsentry4.length < 88 || previewF;
      benefit5.set(`${basketballmatchdetailbgu}`, (3 | (basketballmatchdetailbgu ? 1 : 4)));
    dispatch(loginModalShown());
  }

  const handleOnPurchase = useCallback(() => {
       let mbridgeK = String.fromCharCode(115,112,105,108,108,115,105,122,101,95,113,95,57,51,0);
    let mode8 = String.fromCharCode(115,112,101,101,120,95,97,95,50,53,0);
    let t_hashJ: Array<any> = [606, 827];
    let rewindb = true;
    let giftbutton7 = String.fromCharCode(122,95,51,50,0);
    let wifirouterL = 3.0;
    let playershirtA: Array<any> = [617, 599];
    let orangetickS: Array<any> = [433, 476, 728];
    let listm = 1;
    let libreactperfloggerjnih = String.fromCharCode(104,101,118,99,112,114,101,100,95,106,95,57,52,0);
    let stationB: Array<any> = [347, 9, 57];
    let reminderj = String.fromCharCode(104,95,49,48,48,95,100,99,97,100,115,112,0);
    let matchdetailbgS = 2.0;
   while (mode8.length == 2) {
      giftbutton7 = "3";
      break;
   }
   let acceptedz = giftbutton7.length >= 8020235;
   do {
       let fill9 = 1.0;
       let statsnomoredataz = true;
       let specU: Array<any> = [296, 474];
       let temperaturei = true;
         fill9 += parseFloat(`${specU.length}`);
      while ((specU.length | 2) >= 5) {
         specU = [(parseInt(`${fill9}`) >> (Math.min(5, Math.abs((temperaturei ? 2 : 4)))))];
         break;
      }
          let mathZ = 5.0;
          let libhermesw = String.fromCharCode(117,95,56,50,95,115,116,114,102,0);
          let phoneV: Array<any> = [492, 467, 346];
         specU = [libhermesw.length << (Math.min(2, Math.abs(parseInt(`${fill9}`))))];
         mathZ *= parseFloat(`${parseInt(`${mathZ}`) << (Math.min(3, Math.abs(2)))}`);
         libhermesw = `${parseInt(`${mathZ}`)}`;
         phoneV = [phoneV.length % 1];
         fill9 /= Math.max(4, parseFloat(`${2 << (Math.min(Math.abs(parseInt(`${fill9}`)), 1))}`));
      for (let l = 0; l < 3; l++) {
         specU.push((parseInt(`${fill9}`) + (temperaturei ? 5 : 2)));
      }
       let forwardA: Map<any, any> = new Map([[String.fromCharCode(121,95,57,56,95,121,117,118,121,97,0),378], [String.fromCharCode(110,95,52,55,95,116,101,120,101,108,0),247], [String.fromCharCode(119,97,108,108,112,97,112,101,114,95,119,95,56,53,0),9]]);
       let smallorangemanQ: Map<any, any> = new Map([[String.fromCharCode(121,95,51,54,95,108,105,98,120,0),String.fromCharCode(98,117,115,121,95,100,95,56,50,0)], [String.fromCharCode(121,98,108,111,99,107,95,120,95,50,0),String.fromCharCode(115,108,105,112,112,97,103,101,115,95,118,95,57,49,0)], [String.fromCharCode(103,95,56,50,95,101,108,101,109,0),String.fromCharCode(118,95,51,52,95,112,114,101,115,101,100,0)]]);
      for (let i = 0; i < 3; i++) {
         statsnomoredataz = temperaturei;
      }
      while (!Array.from(smallorangemanQ.keys()).includes(`${specU.length}`)) {
         specU.push(1);
         break;
      }
      while (!temperaturei) {
          let airbnbstarselectedS: Map<any, any> = new Map([[String.fromCharCode(100,95,54,51,95,100,105,97,108,111,103,117,101,0),false ], [String.fromCharCode(98,114,111,119,115,101,95,50,95,49,56,0),false ]]);
          let nalyticsm = String.fromCharCode(101,109,117,108,97,116,101,95,113,95,57,0);
          let episodez: Array<any> = [4, 208, 965];
          let reactnativeultimatelistviewq = 0.0;
          let matchinactiver = true;
         temperaturei = (forwardA.size * smallorangemanQ.size) == 86;
         airbnbstarselectedS.set(`${reactnativeultimatelistviewq}`, 2 & episodez.length);
         nalyticsm = `${airbnbstarselectedS.size << (Math.min(episodez.length, 4))}`;
         reactnativeultimatelistviewq /= Math.max(parseFloat(`${parseInt(`${reactnativeultimatelistviewq}`)}`), 4);
         matchinactiver = 78.81 >= reactnativeultimatelistviewq;
         break;
      }
          let greyw: Array<any> = [String.fromCharCode(105,110,116,101,114,97,99,116,105,118,101,95,100,95,55,55,0), String.fromCharCode(120,95,51,48,95,114,117,110,116,101,114,109,0)];
          let playershirtP = String.fromCharCode(119,104,101,110,95,105,95,55,0);
         statsnomoredataz = !playershirtP.endsWith(`${temperaturei}`);
         greyw.push(3);
         playershirtP += `${greyw.length}`;
         statsnomoredataz = 17 > forwardA.size || fill9 > 82.35;
      if (1 <= (smallorangemanQ.size - 1) && (specU.length - smallorangemanQ.size) <= 1) {
         smallorangemanQ = new Map([[`${smallorangemanQ.size}`, specU.length / (Math.max(9, smallorangemanQ.size))]]);
      }
      giftbutton7 = `${(String.fromCharCode(75,0) == libreactperfloggerjnih ? (rewindb ? 4 : 4) : libreactperfloggerjnih.length)}`;
      if (acceptedz) {
         break;
      }
   } while (acceptedz && (giftbutton7.length <= stationB.length));
      listm += ((rewindb ? 1 : 1));
   while (1 >= (2 << (Math.min(5, libreactperfloggerjnih.length)))) {
      libreactperfloggerjnih = `${listm}`;
      break;
   }
   if (libreactperfloggerjnih == String.fromCharCode(121,0)) {
       let sellA = String.fromCharCode(98,115,119,97,112,95,108,95,51,55,0);
         sellA = `${sellA.length & sellA.length}`;
          let reducerv = String.fromCharCode(113,95,57,48,95,112,117,108,108,117,112,0);
         sellA += "2";
         reducerv += `${2 ^ reducerv.length}`;
      if (sellA.length >= sellA.length) {
          let previewU: Array<any> = [481, 40, 28];
          let backicond = 3.0;
          let crownC: Array<any> = [108, 542];
          let cornershootC = String.fromCharCode(108,97,116,101,115,116,95,107,95,51,49,0);
          let fileW = String.fromCharCode(120,95,56,53,95,109,111,100,101,109,0);
         sellA = `${2 + sellA.length}`;
         previewU.push(previewU.length / (Math.max(2, cornershootC.length)));
         backicond /= Math.max(2, parseFloat(`${2}`));
         crownC.push(crownC.length);
         cornershootC += `${previewU.length}`;
         fileW = `${crownC.length / (Math.max(6, cornershootC.length))}`;
      }
      reminderj = `${3 | stationB.length}`;
   }

    onClose();

   for (let z = 0; z < 3; z++) {
      listm -= mbridgeK.length;
   }
      rewindb = 25 > listm || reminderj.length > 25;
   for (let u = 0; u < 1; u++) {
      wifirouterL += (parseFloat(`${String.fromCharCode(71,0) == mbridgeK ? reminderj.length : mbridgeK.length}`));
   }
      reminderj += `${t_hashJ.length % 2}`;
      rewindb = mode8 == String.fromCharCode(101,0);
    dispatch(loginModalShown());

   while (libreactperfloggerjnih == mbridgeK) {
      mbridgeK += `${2 & reminderj.length}`;
      break;
   }
       let defaultlogoN = 4.0;
      for (let t = 0; t < 3; t++) {
          let homeactiveV = String.fromCharCode(105,111,118,101,99,105,110,95,97,95,57,49,0);
          let flyerh = String.fromCharCode(110,95,54,57,95,105,114,114,101,108,118,97,110,116,0);
         defaultlogoN *= flyerh.length % 3;
         homeactiveV += `${homeactiveV.length % (Math.max(3, 2))}`;
         flyerh = `${homeactiveV.length}`;
      }
      let scrollviewT = 5071310.0 >= defaultlogoN;
      do {
         defaultlogoN -= parseInt(`${defaultlogoN}`);
         if (scrollviewT) {
            break;
         }
      } while ((defaultlogoN < defaultlogoN) && scrollviewT);
         defaultlogoN *= 1;
      libreactperfloggerjnih = `${listm ^ orangetickS.length}`;
   if (1.95 < (2.4 + wifirouterL) || (4 + parseInt(`${wifirouterL}`)) < 3) {
       let component7: Array<any> = [String.fromCharCode(99,119,110,100,95,100,95,53,54,0), String.fromCharCode(116,105,109,101,108,105,110,101,95,53,95,53,57,0), String.fromCharCode(112,95,55,51,95,116,111,116,97,108,0)];
       let inviteG = 4.0;
      for (let v = 0; v < 2; v++) {
          let lightf = false;
          let bellreminder0 = String.fromCharCode(115,100,101,115,95,108,95,54,51,0);
          let gifgoalx = true;
          let leakcheckerB: Array<any> = [String.fromCharCode(99,111,117,110,116,105,110,103,95,50,95,52,50,0), String.fromCharCode(120,95,49,49,95,116,105,99,107,101,116,0), String.fromCharCode(122,95,53,53,95,112,117,98,105,99,0)];
          let mode9 = String.fromCharCode(115,101,103,109,101,110,116,117,114,108,110,111,100,101,95,49,95,52,49,0);
         component7.push(parseInt(`${inviteG}`) % 1);
         lightf = gifgoalx;
         bellreminder0 += `${((gifgoalx ? 2 : 5) | 1)}`;
         leakcheckerB = [3];
         mode9 += `${1 - leakcheckerB.length}`;
      }
       let banner5 = String.fromCharCode(100,105,115,116,114,97,99,116,105,111,110,95,57,95,50,55,0);
       let sport6 = String.fromCharCode(115,113,114,116,95,110,95,51,0);
      for (let j = 0; j < 3; j++) {
          let filtere = false;
          let chatroombackgroundo = String.fromCharCode(120,95,50,50,95,114,108,118,108,99,0);
          let trashA = String.fromCharCode(113,95,49,48,48,95,109,98,115,116,114,105,110,103,0);
         banner5 = `${component7.length}`;
         filtere = 10 >= chatroombackgroundo.length && 10 >= trashA.length;
         chatroombackgroundo = `${(3 % (Math.max(10, (filtere ? 3 : 4))))}`;
         trashA = `${1 + trashA.length}`;
      }
      while (!component7.includes(inviteG)) {
         inviteG -= banner5.length ^ 2;
         break;
      }
         inviteG /= Math.max(parseInt(`${inviteG}`), 3);
       let holderj = String.fromCharCode(114,101,102,114,101,115,104,95,100,95,52,54,0);
       let register_1a = String.fromCharCode(107,95,50,52,95,114,101,103,105,115,116,101,114,115,0);
      wifirouterL /= Math.max(parseFloat(`${parseInt(`${wifirouterL}`) % 3}`), 3);
   }
      t_hashJ.push(2);
   for (let s = 0; s < 3; s++) {
      mbridgeK = `${(reminderj.length << (Math.min(3, Math.abs((rewindb ? 4 : 4)))))}`;
   }
    dispatch(showLoginAction());
    
    
    
    
    


  }, []);

  return (
    <Modal
      visible={showCondition}
      transparent={true}
      animationType="fade"
      onRequestClose={onCloseModal}
    >
      <TouchableWithoutFeedback onPress={onCloseModal}>
        <View style={{ flex: 1, backgroundColor: "rgba(0, 0, 0, 0.4)" }}>
          <VipLoginAlertModal
            coverBackground={sportBg}
            coverImage={commonModels}
            onPurchase={handleOnPurchase}
            onClose={onCloseModal}
            showCondition={showCondition}
          />
        </View>
      </TouchableWithoutFeedback>
    </Modal>
  );
};
