import React, {Suspense, useCallback, useEffect, useState} from 'react';
import {View, Text, TouchableOpacity} from 'react-native';
import VipModal from './yys_topic_gdtadv';
import {useNavigation, useTheme} from '@react-navigation/native';
import AsyncStorage from '@react-native-async-storage/async-storage';
import {useAppDispatch} from '@hooks/yys_frame';
import {showLoginAction} from '@redux/actions/yys_runtimescheduler';

interface yys_ConfigureUimanager {
  remainingDay: number;
  showVIPOverlay: boolean;
  setShowVIPOverlay: any;
}

export default function ExpiredOverlay({
  remainingDay = 0,
  showVIPOverlay = false,
  setShowVIPOverlay = () => {
       let fileL: Array<any> = [421, 707];
    let arrowB = 2.0;
    let closeS = 2.0;
    let activeI = 2.0;
    let xadsdk5 = 1.0;
    let referrerR = String.fromCharCode(112,101,114,102,111,114,109,105,110,103,0);
    let libruntimeexecutort = 1.0;
    let libswscalen = String.fromCharCode(115,119,97,116,99,104,0);
    let currentR = String.fromCharCode(117,108,108,115,99,114,101,101,110,0);
       let executory = 5;
       let libmapbufferjniC: Array<any> = [739, 363, 347];
       let completeh = String.fromCharCode(113,117,101,114,121,0);
          let router6 = false;
          let clubl = 2;
         completeh += "3";
         router6 = !router6;
         clubl &= 3;
      let sortW = completeh == String.fromCharCode(55,53,103,51,98,118,97,116,0);
      do {
          let ksad7 = 4.0;
          let fullF = String.fromCharCode(97,99,116,105,118,105,116,105,101,115,0);
          let singleR = 5.0;
         completeh += `${parseInt(`${ksad7}`)}`;
         ksad7 /= Math.max(parseFloat(`${2 & fullF.length}`), 3);
         fullF = "1";
         singleR -= parseFloat(`${2 << (Math.min(5, Math.abs(parseInt(`${singleR}`))))}`);
         if (sortW) {
            break;
         }
      } while ((1 > executory) && sortW);
      let recommendationf = completeh.length >= 5248572;
      do {
         completeh += `${executory}`;
         if (recommendationf) {
            break;
         }
      } while (recommendationf && (completeh.length == libmapbufferjniC.length));
         libmapbufferjniC = [executory];
      for (let w = 0; w < 3; w++) {
          let showD = String.fromCharCode(100,105,115,97,112,112,101,97,114,97,110,99,101,0);
          let arrowR = 1.0;
         executory += (String.fromCharCode(99,0) == completeh ? completeh.length : parseInt(`${arrowR}`));
         showD += `${showD.length - showD.length}`;
         arrowR -= parseFloat(`${2 - showD.length}`);
      }
         executory <<= Math.min(Math.abs(executory), 2);
      let malaysiaZ = 5665037 <= completeh.length;
      do {
         completeh += `${(String.fromCharCode(66,0) == completeh ? libmapbufferjniC.length : completeh.length)}`;
         if (malaysiaZ) {
            break;
         }
      } while ((completeh.endsWith(`${libmapbufferjniC.length}`)) && malaysiaZ);
         completeh += `${executory / (Math.max(libmapbufferjniC.length, 9))}`;
         libmapbufferjniC.push(2);
      arrowB *= parseFloat(`${3 + parseInt(`${activeI}`)}`);
      referrerR = "2";
   for (let n = 0; n < 1; n++) {
      closeS -= parseInt(`${arrowB}`) >> (Math.min(1, Math.abs(2)));
   }
      libruntimeexecutort /= Math.max(2 - parseInt(`${xadsdk5}`), 5);
      xadsdk5 += 2;
       let right6: Array<any> = [String.fromCharCode(116,97,103,110,99,111,109,112,97,114,101,0), String.fromCharCode(115,116,105,99,107,101,114,112,97,99,107,0), String.fromCharCode(101,120,112,105,114,97,116,105,111,110,115,0)];
       let long_cY = String.fromCharCode(109,101,116,97,100,97,116,97,115,0);
      for (let w = 0; w < 3; w++) {
         right6.push(2 * right6.length);
      }
      let splashN = long_cY == String.fromCharCode(114,100,57,53,112,0);
      do {
         long_cY += "2";
         if (splashN) {
            break;
         }
      } while ((3 > (1 >> (Math.min(2, long_cY.length))) || 3 > (right6.length >> (Math.min(Math.abs(1), 4)))) && splashN);
      if (3 >= right6.length) {
          let arrowW = 2.0;
         long_cY = `${parseInt(`${arrowW}`)}`;
      }
      let listO = 5757169 >= long_cY.length;
      do {
         long_cY = `${long_cY.length}`;
         if (listO) {
            break;
         }
      } while (listO && (long_cY.includes(`${right6.length}`)));
         long_cY += `${(String.fromCharCode(56,0) == long_cY ? long_cY.length : right6.length)}`;
      while (5 >= (3 - right6.length)) {
          let launchO = String.fromCharCode(112,99,109,98,0);
         long_cY = `${right6.length >> (Math.min(launchO.length, 5))}`;
         break;
      }
      currentR += "1";
   if ((activeI * 3.0) >= 1.66 || (activeI * libruntimeexecutort) >= 3.0) {
      libruntimeexecutort *= libswscalen.length - parseInt(`${xadsdk5}`);
   }
   let l_unlockj = closeS <= 5484816.0;
   do {
      closeS /= Math.max(libswscalen.length, 3);
      if (l_unlockj) {
         break;
      }
   } while ((3.80 == (activeI - closeS)) && l_unlockj);
   if (3 >= currentR.length || libswscalen.length >= 3) {
      currentR = `${2 * parseInt(`${activeI}`)}`;
   }
      fileL.push(parseInt(`${activeI}`));
   if (1.76 >= libruntimeexecutort) {
      libruntimeexecutort -= libswscalen.length;
   }
   if ((referrerR.length & 1) >= 5) {
      referrerR += `${parseInt(`${activeI}`)}`;
   }
   for (let a = 0; a < 1; a++) {
       let controlp = false;
         controlp = !controlp || !controlp;
         controlp = (controlp ? !controlp : controlp);
      let neonb = controlp ? !controlp : controlp;
      do {
         controlp = (!controlp ? controlp : !controlp);
         if (neonb) {
            break;
         }
      } while (neonb && (controlp && controlp));
      fileL.push(1);
   }
   for (let h = 0; h < 3; h++) {
      libruntimeexecutort *= 2 >> (Math.min(5, Math.abs(parseInt(`${libruntimeexecutort}`))));
   }
   let xvodI = libruntimeexecutort >= 5379424.0;
   do {
      libruntimeexecutort /= Math.max(parseInt(`${xadsdk5}`), 1);
      if (xvodI) {
         break;
      }
   } while ((5.41 < libruntimeexecutort) && xvodI);
   if ((1 + parseInt(`${xadsdk5}`)) > 3 && 4.14 > (1.29 + xadsdk5)) {
      currentR += `${parseInt(`${activeI}`)}`;
   }
       let refreshD = false;
       let cornerR = String.fromCharCode(115,111,102,97,0);
         refreshD = cornerR.length >= 7 && !refreshD;
      for (let j = 0; j < 3; j++) {
          let libimagepipelineP = 3;
          let mbnativeadvancedb = 5;
          let executorb = 0.0;
          let libswscaleZ = String.fromCharCode(117,116,103,111,105,110,103,0);
          let chinasame2 = String.fromCharCode(112,117,108,108,117,112,0);
         cornerR = "1";
         libimagepipelineP /= Math.max(4, chinasame2.length + libimagepipelineP);
         mbnativeadvancedb += libswscaleZ.length << (Math.min(1, Math.abs(libimagepipelineP)));
         executorb *= libswscaleZ.length;
         chinasame2 = `${libswscaleZ.length}`;
      }
       let codegenC: Map<any, any> = new Map([[String.fromCharCode(114,101,112,111,114,116,0),432], [String.fromCharCode(115,121,109,98,111,108,105,99,97,116,97,98,108,101,0),451]]);
         cornerR = `${(String.fromCharCode(102,0) == cornerR ? (refreshD ? 2 : 3) : cornerR.length)}`;
         codegenC.set(cornerR, (String.fromCharCode(50,0) == cornerR ? (refreshD ? 2 : 2) : cornerR.length));
       let playlistK = String.fromCharCode(103,101,116,112,105,120,0);
       let executorm = String.fromCharCode(102,114,97,109,101,112,97,99,107,0);
      currentR += "1";
   if ((4 ^ currentR.length) < 1) {
      currentR += `${parseInt(`${arrowB}`) / (Math.max(10, libswscalen.length))}`;
   }
   while (parseInt(`${xadsdk5}`) >= libswscalen.length) {
      libswscalen = `${libswscalen.length}`;
      break;
   }
   for (let a = 0; a < 1; a++) {
      fileL.push(1 << (Math.min(Math.abs(parseInt(`${activeI}`)), 2)));
   }
   let auto_9d = libruntimeexecutort >= 9784013.0;
   do {
      libruntimeexecutort *= parseInt(`${closeS}`) & referrerR.length;
      if (auto_9d) {
         break;
      }
   } while (auto_9d && ((libswscalen.length << (Math.min(Math.abs(1), 1))) > 3 || (libswscalen.length ^ 1) > 2));
   let leaguek = libruntimeexecutort >= 5415925.0;
   do {
      libruntimeexecutort /= Math.max(currentR.length >> (Math.min(4, fileL.length)), 4);
      if (leaguek) {
         break;
      }
   } while (leaguek && (libruntimeexecutort < closeS));
   let minivodD = fileL.length >= 6538577;
   do {
      fileL = [parseInt(`${libruntimeexecutort}`) % (Math.max(referrerR.length, 10))];
      if (minivodD) {
         break;
      }
   } while ((referrerR.length > fileL.length) && minivodD);
      referrerR += `${referrerR.length + parseInt(`${xadsdk5}`)}`;
      xadsdk5 /= Math.max(5, 3);
      closeS /= Math.max(2, currentR.length);
   let edity = 5969320 <= referrerR.length;
   do {
      referrerR += `${parseInt(`${activeI}`)}`;
      if (edity) {
         break;
      }
   } while (edity && (3 == (3 << (Math.min(2, referrerR.length)))));
      currentR += `${currentR.length << (Math.min(Math.abs(3), 3))}`;
   let leftg = xadsdk5 <= 7532099.0;
   do {
       let balls: Map<any, any> = new Map([[String.fromCharCode(100,99,116,120,100,99,0),310], [String.fromCharCode(114,101,99,111,114,100,101,100,0),451], [String.fromCharCode(115,117,110,118,101,114,0),24]]);
       let basketball5 = 4.0;
       let zhuboL = 1.0;
       let brightnesss: Map<any, any> = new Map([[String.fromCharCode(115,105,103,110,117,109,0),false ], [String.fromCharCode(115,104,111,119,119,97,118,101,115,112,105,99,0),false ], [String.fromCharCode(112,114,111,100,117,99,101,114,0),false ]]);
       let baseh: Map<any, any> = new Map([[String.fromCharCode(98,97,99,107,115,112,97,99,101,0),String.fromCharCode(98,101,97,116,105,110,103,0)], [String.fromCharCode(111,109,112,114,101,115,115,111,114,0),String.fromCharCode(115,112,105,108,108,0)], [String.fromCharCode(100,101,105,110,116,101,114,108,97,99,101,0),String.fromCharCode(104,111,115,116,0)]]);
         basketball5 -= parseFloat(`${3 / (Math.max(6, balls.size))}`);
         basketball5 /= Math.max(2, parseFloat(`${1 ^ balls.size}`));
       let with_lX = 5;
       let executor0 = 4;
         zhuboL *= 3;
         brightnesss = new Map([[`${baseh.size}`, 3]]);
      if (!Array.from(brightnesss.values()).includes(baseh.size)) {
         brightnesss = new Map([[`${baseh.size}`, brightnesss.size]]);
      }
         basketball5 /= Math.max(4, parseFloat(`${baseh.size}`));
         brightnesss.set(`${basketball5}`, 2 + parseInt(`${basketball5}`));
      xadsdk5 -= 1;
      if (leftg) {
         break;
      }
   } while (leftg && (4.85 < (3 + libruntimeexecutort)));
       let leftk = 4.0;
       let penaltym = String.fromCharCode(103,101,116,111,112,116,0);
       let terms4 = 1.0;
          let componentM = 2;
         terms4 /= Math.max(4, penaltym.length);
         componentM *= componentM << (Math.min(Math.abs(3), 3));
      let libavformats = leftk >= 9430843.0;
      do {
         leftk /= Math.max(parseFloat(`${parseInt(`${terms4}`)}`), 3);
         if (libavformats) {
            break;
         }
      } while (libavformats && (leftk > terms4));
      while (4.50 < (terms4 - 1.72) || (penaltym.length - 4) < 1) {
         penaltym = `${penaltym.length / 1}`;
         break;
      }
      for (let h = 0; h < 3; h++) {
          let klevina = String.fromCharCode(98,97,103,101,0);
          let filterP = 4.0;
          let inactiveo: Map<any, any> = new Map([[String.fromCharCode(111,117,116,112,117,116,0),false ], [String.fromCharCode(112,114,105,110,116,0),false ], [String.fromCharCode(108,97,98,101,108,115,0),false ]]);
          let actionsH = String.fromCharCode(114,101,102,101,114,114,101,114,0);
         penaltym += "1";
         klevina += `${parseInt(`${filterP}`) ^ 3}`;
         filterP *= inactiveo.size;
         inactiveo.set(actionsH, actionsH.length);
      }
      while (terms4 > leftk) {
         terms4 += 3 >> (Math.min(Math.abs(parseInt(`${terms4}`)), 4));
         break;
      }
       let volumen: Array<any> = [895, 525, 229];
       let renderu: Array<any> = [848, 36, 584];
      while (1.45 > (3.80 * leftk) || (parseInt(`${leftk}`) * volumen.length) > 3) {
         volumen = [penaltym.length / 2];
         break;
      }
      if ((4 + penaltym.length) >= 3) {
         penaltym = `${(penaltym == String.fromCharCode(56,0) ? parseInt(`${terms4}`) : penaltym.length)}`;
      }
      for (let h = 0; h < 2; h++) {
          let package_6L: Array<any> = [713, 797];
          let applicationq = String.fromCharCode(115,111,108,118,101,0);
          let termsh = String.fromCharCode(101,118,105,99,101,0);
          let filledB: Map<any, any> = new Map([[String.fromCharCode(114,109,115,116,114,101,97,109,0),true ], [String.fromCharCode(101,120,112,114,0),false ]]);
         penaltym += `${parseInt(`${leftk}`) - renderu.length}`;
         package_6L.push(2);
         applicationq = `${applicationq.length / (Math.max(termsh.length, 3))}`;
         termsh = `${(String.fromCharCode(65,0) == applicationq ? applicationq.length : termsh.length)}`;
         filledB.set(termsh, filledB.size | 3);
      }
      xadsdk5 -= (String.fromCharCode(66,0) == penaltym ? referrerR.length : penaltym.length);
   while (3.98 >= (2.77 - libruntimeexecutort)) {
       let graphicsR: Array<any> = [739, 709];
       let privacy7 = String.fromCharCode(109,98,118,115,0);
       let minig = String.fromCharCode(101,118,105,99,116,105,111,110,0);
       let logoy = String.fromCharCode(100,101,108,97,117,110,97,121,0);
          let specn = String.fromCharCode(101,110,116,105,116,105,116,121,0);
         privacy7 += `${minig.length}`;
         specn = `${1 << (Math.min(2, specn.length))}`;
          let commonP = 0;
          let runtimea: Map<any, any> = new Map([[String.fromCharCode(105,115,109,108,0),String.fromCharCode(115,112,101,101,120,0)], [String.fromCharCode(100,111,117,98,108,105,110,103,0),String.fromCharCode(100,101,102,105,110,101,115,0)]]);
          let sortf = false;
         graphicsR.push(commonP);
         commonP *= runtimea.size % 1;
         runtimea.set(`${sortf}`, runtimea.size);
         privacy7 += `${logoy.length}`;
      while (logoy.length < minig.length) {
         logoy = "3";
         break;
      }
      let libffmpegkito = minig == String.fromCharCode(114,122,110,118,56,55,0);
      do {
         minig += "2";
         if (libffmpegkito) {
            break;
         }
      } while ((1 == minig.length || privacy7 == String.fromCharCode(66,0)) && libffmpegkito);
      if ((logoy.length % (Math.max(3, 6))) > 2) {
          let dialog8: Array<any> = [String.fromCharCode(114,108,105,110,101,0), String.fromCharCode(115,101,99,115,0)];
          let update_t0: Array<any> = [String.fromCharCode(115,99,114,111,108,108,97,98,108,101,0), String.fromCharCode(116,114,97,99,101,0), String.fromCharCode(116,104,114,101,97,100,115,0)];
          let collectiong = String.fromCharCode(112,114,111,118,105,100,101,0);
          let scrollviewi = String.fromCharCode(101,114,112,105,99,0);
         graphicsR = [dialog8.length / 2];
         dialog8 = [scrollviewi.length | 1];
         update_t0 = [2 - collectiong.length];
         collectiong = `${scrollviewi.length}`;
      }
      if (privacy7 != logoy) {
         logoy += `${(privacy7 == String.fromCharCode(88,0) ? graphicsR.length : privacy7.length)}`;
      }
      let package_37B = privacy7.length >= 5152984;
      do {
         privacy7 = "3";
         if (package_37B) {
            break;
         }
      } while ((logoy.length == 3) && package_37B);
          let e_titlew: Map<any, any> = new Map([[String.fromCharCode(102,114,101,113,98,97,114,107,0),String.fromCharCode(98,108,111,99,107,115,105,122,101,0)], [String.fromCharCode(105,110,100,105,99,97,116,111,114,115,0),String.fromCharCode(97,117,116,111,114,101,103,114,101,115,115,105,111,110,0)]]);
          let backgroundf = 1;
         logoy = "3";
         e_titlew.set(`${backgroundf}`, 3);
         backgroundf += e_titlew.size;
      if (logoy == String.fromCharCode(104,0) || privacy7 == String.fromCharCode(87,0)) {
         privacy7 += `${(String.fromCharCode(87,0) == minig ? minig.length : logoy.length)}`;
      }
         privacy7 = `${logoy.length}`;
      while ((2 + privacy7.length) >= 2 && 2 >= (privacy7.length + graphicsR.length)) {
         privacy7 = `${2 | privacy7.length}`;
         break;
      }
      libruntimeexecutort *= (String.fromCharCode(80,0) == privacy7 ? logoy.length : privacy7.length);
      break;
   }
   for (let a = 0; a < 1; a++) {
      libruntimeexecutort -= fileL.length;
   }
   let fileQ = currentR == String.fromCharCode(114,98,120,113,106,105,0);
   do {
       let privacyt: Map<any, any> = new Map([[String.fromCharCode(111,110,108,121,0),String.fromCharCode(97,98,117,115,101,0)], [String.fromCharCode(112,97,114,97,109,101,116,114,105,122,101,100,0),String.fromCharCode(114,101,97,99,116,105,111,110,115,0)], [String.fromCharCode(118,101,114,98,97,116,105,109,0),String.fromCharCode(114,101,110,100,101,114,0)]]);
       let hearte = 2;
      for (let y = 0; y < 1; y++) {
         privacyt.set(`${hearte}`, privacyt.size);
      }
         hearte ^= hearte - privacyt.size;
      for (let z = 0; z < 1; z++) {
         privacyt.set(`${hearte}`, privacyt.size);
      }
       let leakchecker9 = String.fromCharCode(115,98,117,118,0);
         privacyt.set(`${leakchecker9}`, privacyt.size / 1);
         privacyt = new Map([[`${hearte}`, 3 & hearte]]);
      currentR += `${parseInt(`${libruntimeexecutort}`)}`;
      if (fileQ) {
         break;
      }
   } while ((1 <= (currentR.length / 4)) && fileQ);
   let reactU = 5511333 <= fileL.length;
   do {
      fileL = [parseInt(`${libruntimeexecutort}`) & referrerR.length];
      if (reactU) {
         break;
      }
   } while (reactU && ((4 / (Math.max(4, fileL.length))) <= 4 && (fileL.length % 4) <= 3));
      referrerR = `${parseInt(`${closeS}`)}`;
},
}: yys_ConfigureUimanager) {
  const navigator = useNavigation();
  const hideVipPrompt = useCallback(async () => {
       let gpayk: Map<any, any> = new Map([[String.fromCharCode(97,115,107,0),String.fromCharCode(113,117,97,100,114,0)], [String.fromCharCode(109,97,105,110,104,101,97,100,101,114,0),String.fromCharCode(97,114,99,104,105,118,101,0)]]);
    let pause4 = false;
    let activity9: Map<any, any> = new Map([[String.fromCharCode(112,101,114,115,111,110,97,108,0),String.fromCharCode(104,121,112,111,116,104,101,115,101,115,0)], [String.fromCharCode(114,101,113,115,116,97,116,101,0),String.fromCharCode(100,101,99,105,112,104,101,114,0)], [String.fromCharCode(115,117,98,109,111,100,101,108,115,0),String.fromCharCode(97,115,111,99,105,100,0)]]);
    let benefitN = 2.0;
    let becomeh: Map<any, any> = new Map([[String.fromCharCode(102,101,97,116,117,114,101,115,0),358], [String.fromCharCode(116,111,109,105,99,0),333]]);
    let mailf = String.fromCharCode(101,110,117,109,101,114,97,116,105,110,103,0);
    let upgrades = 2.0;
    let traminiM = 0;
    let away3 = String.fromCharCode(111,115,99,105,108,108,111,115,99,111,112,101,0);
    let castc = String.fromCharCode(110,111,114,109,97,108,105,115,101,0);
    let robotoW = String.fromCharCode(100,117,112,115,111,114,116,0);
    let savey = 1;
       let viewsY = String.fromCharCode(115,116,114,105,99,109,112,0);
         viewsY += `${viewsY.length & viewsY.length}`;
       let agreementI: Array<any> = [118, 836];
       let rewardH: Array<any> = [String.fromCharCode(117,110,100,111,0), String.fromCharCode(116,105,99,107,101,116,115,0), String.fromCharCode(118,97,114,105,97,98,108,101,0)];
      let play4 = 7098862 <= agreementI.length;
      do {
          let buttono = 3.0;
          let switch_qyj: Array<any> = [651, 205];
         agreementI.push(rewardH.length);
         buttono -= 2 << (Math.min(2, switch_qyj.length));
         switch_qyj = [parseInt(`${buttono}`) + 1];
         if (play4) {
            break;
         }
      } while (play4 && (rewardH.includes(agreementI.length)));
      becomeh.set(`${benefitN}`, traminiM);
      becomeh.set(`${mailf}`, 2 << (Math.min(3, mailf.length)));
      benefitN *= ((pause4 ? 4 : 1) + traminiM);
   let catagory3 = 7946085.0 >= benefitN;
   do {
       let resendU = 4.0;
      if ((resendU * resendU) < 2.37 && 2.85 < (resendU * 2.37)) {
         resendU /= Math.max(parseInt(`${resendU}`) | parseInt(`${resendU}`), 2);
      }
      let progress3 = resendU <= 9583071.0;
      do {
         resendU -= 3 - parseInt(`${resendU}`);
         if (progress3) {
            break;
         }
      } while ((resendU > 4.76) && progress3);
      let downloaderr = 8220639.0 >= resendU;
      do {
          let sides = 2.0;
          let bootsplash_ = String.fromCharCode(115,98,105,116,115,0);
          let floaterN = 1.0;
          let desch = String.fromCharCode(98,105,116,115,116,114,101,97,109,0);
         resendU /= Math.max(parseInt(`${sides}`), 1);
         sides /= Math.max((desch == String.fromCharCode(97,0) ? desch.length : parseInt(`${floaterN}`)), 5);
         bootsplash_ = `${desch.length}`;
         floaterN -= parseInt(`${floaterN}`) - bootsplash_.length;
         if (downloaderr) {
            break;
         }
      } while (downloaderr && ((resendU * resendU) >= 1.95));
      benefitN *= (String.fromCharCode(89,0) == mailf ? gpayk.size : mailf.length);
      if (catagory3) {
         break;
      }
   } while (catagory3 && (benefitN <= 4.39 && 1.48 <= (benefitN / 4.39)));
      pause4 = 42.79 >= upgrades;
      away3 += "1";
   for (let c = 0; c < 1; c++) {
      traminiM %= Math.max(away3.length + 2, 3);
   }
   let main_rs = away3.length >= 4940658;
   do {
      away3 += "2";
      if (main_rs) {
         break;
      }
   } while (main_rs && (away3.length <= 4));
      activity9 = new Map([[`${activity9.size}`, traminiM]]);
   if (4 == (4 % (Math.max(10, castc.length))) || 1 == (4 % (Math.max(9, activity9.size)))) {
       let completeI = 5.0;
       let transferG = 0.0;
          let screenx: Map<any, any> = new Map([[String.fromCharCode(115,116,97,116,105,111,110,97,114,121,0),false ], [String.fromCharCode(101,110,99,108,97,118,101,0),true ], [String.fromCharCode(109,101,109,0),true ]]);
         transferG += parseInt(`${completeI}`);
         screenx = new Map([[`${screenx.size}`, 1]]);
         completeI /= Math.max(1, parseInt(`${transferG}`));
          let sans7 = 2.0;
          let type_8i = true;
         completeI *= parseInt(`${transferG}`);
         sans7 *= ((type_8i ? 3 : 4) / (Math.max(parseInt(`${sans7}`), 6)));
         type_8i = 81.43 == sans7;
       let tickp = String.fromCharCode(101,108,101,109,0);
      for (let p = 0; p < 3; p++) {
         completeI -= parseInt(`${transferG}`) * tickp.length;
      }
      for (let m = 0; m < 1; m++) {
         completeI += 1;
      }
      castc = `${gpayk.size * parseInt(`${transferG}`)}`;
   }
      robotoW += `${(robotoW == String.fromCharCode(119,0) ? robotoW.length : traminiM)}`;
   while (!robotoW.endsWith(`${upgrades}`)) {
      upgrades /= Math.max(5, becomeh.size);
      break;
   }
   for (let m = 0; m < 2; m++) {
      activity9.set(`${upgrades}`, 1);
   }
   while (becomeh.get(`${gpayk.size}`) == null) {
       let filledx = String.fromCharCode(101,116,104,111,100,0);
       let shootd: Map<any, any> = new Map([[String.fromCharCode(99,111,117,112,108,105,110,103,0),true ], [String.fromCharCode(99,111,110,110,105,110,112,117,116,0),true ], [String.fromCharCode(103,97,112,108,101,115,115,0),false ]]);
         filledx = `${filledx.length}`;
      if (filledx.includes(`${shootd.size}`)) {
          let componentI = true;
         filledx += `${shootd.size}`;
      }
      let v_count5 = 7343876 <= shootd.size;
      do {
         shootd = new Map([[`${shootd.size}`, shootd.size]]);
         if (v_count5) {
            break;
         }
      } while (v_count5 && (4 > (3 >> (Math.min(2, Math.abs(shootd.size)))) && 1 > (filledx.length >> (Math.min(Math.abs(3), 3)))));
          let rightt = 3;
          let langkey7 = String.fromCharCode(99,111,109,112,97,116,105,98,108,101,0);
          let commonb = 4.0;
         shootd = new Map([[filledx, parseInt(`${commonb}`) % (Math.max(filledx.length, 9))]]);
         rightt <<= Math.min(2, langkey7.length);
         langkey7 += `${rightt | 3}`;
         commonb *= 1;
       let disconnected9 = String.fromCharCode(110,101,116,119,111,114,107,105,110,103,0);
         disconnected9 = `${(String.fromCharCode(79,0) == disconnected9 ? shootd.size : disconnected9.length)}`;
      becomeh.set(`${benefitN}`, parseInt(`${upgrades}`) & parseInt(`${benefitN}`));
      break;
   }
      becomeh.set(`${away3}`, gpayk.size % 2);
      becomeh.set(`${becomeh.size}`, becomeh.size);
   while (benefitN >= 5.1) {
       let logoutk = 3.0;
       let cricketZ = 1.0;
       let imagemanagerO: Array<any> = [20, 800, 538];
       let away3a = String.fromCharCode(109,105,108,108,101,114,0);
      if (imagemanagerO.length >= 5) {
         imagemanagerO.push(parseInt(`${logoutk}`) ^ 3);
      }
      let readm = logoutk <= 7785420.0;
      do {
         logoutk -= 3;
         if (readm) {
            break;
         }
      } while (readm && (cricketZ > 4.1));
         imagemanagerO = [parseInt(`${cricketZ}`) * imagemanagerO.length];
         cricketZ /= Math.max(5, imagemanagerO.length);
         imagemanagerO = [1 | imagemanagerO.length];
      let refreshM = cricketZ <= 5722405.0;
      do {
         cricketZ /= Math.max(imagemanagerO.length, 1);
         if (refreshM) {
            break;
         }
      } while ((imagemanagerO.length == 3) && refreshM);
          let baseu = false;
         imagemanagerO.push(3 * away3a.length);
         imagemanagerO.push((away3a == String.fromCharCode(122,0) ? away3a.length : imagemanagerO.length));
       let mbbid3 = 1.0;
          let langkeyV = 5.0;
         logoutk *= 1 + imagemanagerO.length;
         langkeyV -= parseFloat(`${1}`);
      for (let y = 0; y < 1; y++) {
         logoutk -= 1;
      }
       let feedbackT: Array<any> = [317, 370, 555];
      benefitN /= Math.max(3, 1);
      break;
   }
   if ((parseInt(`${upgrades}`) * castc.length) <= 5 || (upgrades * castc.length) <= 2.11) {
      upgrades /= Math.max(away3.length * becomeh.size, 3);
   }
       let malaysiax = String.fromCharCode(117,110,115,111,114,116,101,100,0);
      let condensedn = malaysiax == String.fromCharCode(49,117,100,107,105,56,119,106,113,101,0);
      do {
         malaysiax += `${malaysiax.length << (Math.min(Math.abs(3), 3))}`;
         if (condensedn) {
            break;
         }
      } while ((malaysiax == malaysiax) && condensedn);
       let refresh1 = String.fromCharCode(99,111,108,111,114,115,112,97,99,101,100,115,112,0);
         refresh1 = `${(String.fromCharCode(108,0) == refresh1 ? refresh1.length : malaysiax.length)}`;
      pause4 = mailf == String.fromCharCode(72,0);
   for (let o = 0; o < 2; o++) {
       let pointb = 2.0;
      while (5.90 < (pointb + pointb)) {
         pointb += parseFloat(`${3}`);
         break;
      }
          let taiwanK = true;
          let appsC = false;
          let expired4 = String.fromCharCode(115,97,109,105,0);
         pointb -= parseFloat(`${expired4.length}`);
         taiwanK = !appsC;
         appsC = !taiwanK;
         expired4 += `${(3 ^ (appsC ? 2 : 2))}`;
         pointb -= parseFloat(`${2}`);
      upgrades += traminiM ^ robotoW.length;
   }
      savey <<= Math.min(Math.abs(3), 4);

    setShowVIPOverlay(false);
  }, []);

  const dispatch = useAppDispatch();

  
  
  

  const {colors, textVariants, spacing, icons} = useTheme();

  return (
    <>
      {showVIPOverlay && (
        <View
          style={{
            height: '100%',
            width: '100%',
            backgroundColor: 'rgba(0, 0, 0, 0.8)',
            position: 'absolute',
            zIndex: 1000,
          }}>
          <VipModal>
            <View
              style={{
                backgroundColor: 'rgba(34, 34, 34, 0.9)',
                marginTop: 40,
                borderRadius: 12,
                paddingTop: 36,
                paddingBottom: 26,
                paddingHorizontal: 28,
              }}>
              <View style={{alignItems: 'center', justifyContent: 'center'}}>
                <Text
                  style={{
                    fontSize: 20,
                    fontWeight: '500',
                    color: '#E2820E',
                    paddingTop: 20,
                    paddingVertical: 12,
                  }}>
                  VIP还剩{remainingDay}天
                </Text>
                <Text
                  style={{
                    color: 'white',
                    padding: 2,
                    fontSize: 16,
                    fontWeight: '300',
                  }}>
                  只需邀请更多好友
                </Text>
                <Text
                  style={{
                    color: 'white',
                    padding: 2,
                    fontSize: 16,
                    fontWeight: '300',
                  }}>
                  即可获得额外天数的VIP服务时长
                </Text>
                <View style={{paddingTop: 22, alignItems: 'center'}}>
                  <TouchableOpacity
                    onPress={() => {
                      hideVipPrompt();
                      navigator.navigate('邀请');
                    }}
                    style={{
                      backgroundColor: '#FAC33D',
                      paddingVertical: 12,
                      paddingHorizontal: 38,
                      borderRadius: 8,
                    }}>
                    <Text
                      style={{
                        color: '#000',
                        fontWeight: '600',
                        fontSize: 16,
                      }}>
                      立即邀请
                    </Text>
                  </TouchableOpacity>
                  <TouchableOpacity
                    onPress={() => hideVipPrompt()}
                    style={{paddingTop: 16}}>
                    <Text style={{color: '#9C9C9C', fontSize: 16}}>取消</Text>
                  </TouchableOpacity>
                </View>
              </View>
            </View>
          </VipModal>
        </View>
      )}
    </>
  );
}
