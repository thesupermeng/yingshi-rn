import { View, Text, TouchableOpacity, ViewStyle, TextStyle, StyleSheet } from 'react-native';
import { hideAdultModeVip } from '@redux/actions/mayi_iconorientation_chatroombackground';
import VipModal from './mayi_fill';
import FastImage from 'react-native-fast-image';
import { useNavigation } from '@react-navigation/native';
import { IconClose } from '../../Sports/assets';
import { useCallback } from 'react';
import CrossIcon from '@static/images/heartLibbufferBridge.svg'
import CloseIcon from '@static/images/friendsFieldZoom.svg'
import { useAppDispatch, useAppSelector, useSelector } from '@hooks/mayi_redirect';
import { screenModel } from '@type/mayi_libfolly_inouttargetred';
import { SHOW_ZF_CONST, UMENG_CHANNEL } from '@utility/mayi_middleware_apps';
import { mayi_Baseline } from '@redux/reducers/mayi_chatroombackground_unlock';
import { mayi_Gift } from '@models/mayi_libjsinspector';


const AdultVideoVipModal = () => {
       let stationsN = 2.0;
    let commonK = 1.0;
    let leagueM = String.fromCharCode(97,98,111,117,116,95,120,95,49,54,0);
    let mode3 = String.fromCharCode(101,110,117,109,101,114,97,116,105,111,110,95,104,95,57,49,0);
    let fillB = String.fromCharCode(101,105,110,116,114,95,103,95,49,52,0);
    let orangeuparrowb = 3;
    let holderI = 5.0;
    let cricket_ = true;
    let halffieldimageo = 0.0;
    let bingi: Array<any> = [597, 350, 836];
    let storeV = true;
    let upgradeq = String.fromCharCode(121,95,57,50,95,101,115,115,97,103,101,0);
      stationsN /= Math.max(1, parseFloat(`${1}`));
   if (2 <= (orangeuparrowb % 2) || 4.9 <= (stationsN * 4.91)) {
       let fulle: Array<any> = [264, 431];
       let homeplayerH = 4;
       let china9: Map<any, any> = new Map([[String.fromCharCode(97,108,97,114,109,95,51,95,57,54,0),false ], [String.fromCharCode(110,95,53,52,0),false ]]);
       let crossz = String.fromCharCode(99,111,110,115,117,109,112,116,105,111,110,95,53,95,49,53,0);
       let streamingK = true;
         streamingK = china9.size > 4;
      while (china9.size >= 2) {
          let scheduleW = 0.0;
          let about1 = String.fromCharCode(110,111,116,105,99,101,95,117,95,50,56,0);
          let statisticsinactivee = String.fromCharCode(121,95,56,56,95,100,101,112,97,114,116,109,101,110,116,0);
          let turnc = String.fromCharCode(98,115,102,115,95,120,95,54,0);
          let math4 = String.fromCharCode(119,115,118,113,97,95,54,95,55,52,0);
         streamingK = (turnc.length * china9.size) > 14;
         scheduleW *= parseInt(`${scheduleW}`) & math4.length;
         about1 += `${parseInt(`${scheduleW}`) - 1}`;
         statisticsinactivee += `${statisticsinactivee.length | math4.length}`;
         turnc += `${parseInt(`${scheduleW}`) & 3}`;
         break;
      }
         fulle.push(1);
      if (china9.size >= 4) {
         china9 = new Map([[`${fulle.length}`, (fulle.length >> (Math.min(4, Math.abs((streamingK ? 4 : 5)))))]]);
      }
         china9.set(`${streamingK}`, ((streamingK ? 5 : 1) + 1));
      while (fulle.includes(homeplayerH)) {
         fulle = [fulle.length];
         break;
      }
         china9 = new Map([[`${fulle.length}`, ((streamingK ? 5 : 1) * fulle.length)]]);
      let whistlez = 6397732 <= crossz.length;
      do {
         crossz = `${china9.size >> (Math.min(Math.abs(3), 4))}`;
         if (whistlez) {
            break;
         }
      } while ((homeplayerH < 5) && whistlez);
      let expandt = 7257562 >= fulle.length;
      do {
          let arrowrightwithtail_ = 2.0;
          let points = String.fromCharCode(111,110,116,97,99,116,95,97,95,53,50,0);
         fulle = [points.length + 3];
         arrowrightwithtail_ *= parseFloat(`${1}`);
         points = "3";
         if (expandt) {
            break;
         }
      } while (expandt && (3 <= crossz.length));
      if ((crossz.length | 1) <= 2) {
         china9 = new Map([[`${streamingK}`, 1 & crossz.length]]);
      }
      while (4 > (crossz.length + fulle.length)) {
         fulle.push(3);
         break;
      }
          let editM: Map<any, any> = new Map([[String.fromCharCode(103,95,49,57,95,115,117,98,115,116,114,105,110,103,115,0),244], [String.fromCharCode(115,112,97,99,101,114,115,95,107,95,52,57,0),55], [String.fromCharCode(119,97,110,116,115,95,118,95,51,48,0),626]]);
          let subbasketballplayeru = 2.0;
          let bodanF = 5.0;
         homeplayerH |= parseInt(`${subbasketballplayeru}`) % 2;
         editM = new Map([[`${editM.size}`, parseInt(`${bodanF}`)]]);
         subbasketballplayeru += parseFloat(`${editM.size}`);
         bodanF *= parseFloat(`${editM.size}`);
      for (let o = 0; o < 2; o++) {
         fulle.push(3);
      }
      if ((china9.size & 2) < 1) {
          let stationsT = 5.0;
          let showless4 = 3.0;
          let navigationm: Array<any> = [938, 692];
          let gpayN = 3.0;
         homeplayerH += crossz.length;
         stationsT -= parseInt(`${showless4}`) & parseInt(`${gpayN}`);
         showless4 -= parseFloat(`${navigationm.length ^ parseInt(`${stationsT}`)}`);
         navigationm = [navigationm.length];
         gpayN -= 2 + navigationm.length;
      }
          let sigmobR: Array<any> = [String.fromCharCode(105,95,51,52,95,112,112,102,108,97,103,115,0), String.fromCharCode(103,101,111,103,114,97,112,104,105,99,97,108,95,98,95,55,56,0), String.fromCharCode(102,95,51,55,95,112,114,111,112,101,114,0)];
         homeplayerH ^= 2 << (Math.min(Math.abs(homeplayerH), 1));
         sigmobR.push(sigmobR.length);
      stationsN /= Math.max(parseFloat(`${1 * mode3.length}`), 5);
   }
   let baselinef = stationsN >= 9485462.0;
   do {
      stationsN *= parseFloat(`${fillB.length}`);
      if (baselinef) {
         break;
      }
   } while ((4 >= (parseInt(`${stationsN}`) + mode3.length) || 4 >= (parseInt(`${stationsN}`) + mode3.length)) && baselinef);

  const navigator = useNavigation();

      stationsN *= parseFloat(`${leagueM.length}`);
      orangeuparrowb += parseInt(`${holderI}`) ^ 3;
   while (leagueM == String.fromCharCode(72,0) && fillB == String.fromCharCode(83,0)) {
      fillB += `${parseInt(`${holderI}`) + fillB.length}`;
      break;
   }
  const userState = useSelector<mayi_Baseline>('userReducer');

       let connectiono = 1.0;
       let predictionbannersharedo: Array<any> = [303, 135, 126];
         connectiono -= predictionbannersharedo.length * 1;
       let libnms3: Map<any, any> = new Map([[String.fromCharCode(102,95,56,54,95,116,114,101,97,100,0),421], [String.fromCharCode(114,101,99,111,110,115,116,114,117,99,116,95,104,95,52,57,0),27]]);
         connectiono -= libnms3.size;
      if ((connectiono * 2.68) > 4.87 || 2 > (parseInt(`${connectiono}`) * predictionbannersharedo.length)) {
         connectiono *= predictionbannersharedo.length | libnms3.size;
      }
      if (predictionbannersharedo.includes(connectiono)) {
         connectiono /= Math.max(predictionbannersharedo.length, 1);
      }
          let buildI = 0.0;
          let brightnessO = 4.0;
         predictionbannersharedo = [2];
         buildI /= Math.max(parseFloat(`${parseInt(`${brightnessO}`)}`), 4);
      cricket_ = mode3.includes(`${orangeuparrowb}`);
      cricket_ = fillB.length < orangeuparrowb;
   while (2 <= (mode3.length | 1)) {
      halffieldimageo += mode3.length;
      break;
   }
  const screenState: screenModel = useAppSelector(
    ({ screenReducer }) => screenReducer
  )
  const { adultModeVipShow, adultMode, watchAnytimeAdultMode } = screenState

   for (let r = 0; r < 3; r++) {
      fillB = `${parseInt(`${halffieldimageo}`) / 3}`;
   }
       let feedbackv: Array<any> = [String.fromCharCode(101,120,116,114,97,112,111,108,97,116,111,114,95,115,95,54,50,0), String.fromCharCode(102,105,102,111,95,111,95,54,52,0), String.fromCharCode(121,95,50,56,95,105,110,116,101,103,114,97,116,105,111,110,0)];
       let mode1 = String.fromCharCode(101,99,114,101,99,111,118,101,114,95,99,95,51,50,0);
         mode1 = `${3 - feedbackv.length}`;
         mode1 = `${feedbackv.length}`;
      for (let c = 0; c < 1; c++) {
          let editR = String.fromCharCode(105,95,51,57,95,115,97,110,105,116,105,122,101,0);
          let iconpipexpand1: Array<any> = [123, 718];
          let modity2 = 2;
          let type_8T = String.fromCharCode(109,105,103,114,97,116,101,100,95,111,95,54,0);
          let cancelD = String.fromCharCode(122,95,56,55,95,100,105,109,105,110,115,105,111,110,115,0);
         mode1 += `${type_8T.length >> (Math.min(Math.abs(1), 3))}`;
         editR = `${2 * iconpipexpand1.length}`;
         iconpipexpand1.push(3);
         modity2 *= cancelD.length ^ 1;
         type_8T = `${modity2 / 3}`;
         cancelD += `${modity2}`;
      }
         mode1 = `${mode1.length & feedbackv.length}`;
         mode1 += `${(String.fromCharCode(50,0) == mode1 ? feedbackv.length : mode1.length)}`;
      for (let o = 0; o < 2; o++) {
         feedbackv.push(feedbackv.length + mode1.length);
      }
      cricket_ = holderI > 57.12;
      cricket_ = 69.68 <= stationsN;
  const dispatch = useAppDispatch()

   if ((holderI - 5.8) > 4.32) {
       let w_centerm = String.fromCharCode(120,116,101,110,115,105,111,110,95,119,95,56,52,0);
       let weibor = String.fromCharCode(104,95,53,52,95,114,101,99,111,103,110,105,122,101,114,115,0);
       let runtimeschedulerm = 3.0;
       let productY = 5.0;
       let viewsQ = 2;
      let stations0 = w_centerm.length >= 7249904;
      do {
          let telemetry_ = String.fromCharCode(114,111,117,110,100,95,122,95,50,57,0);
          let runtimey = String.fromCharCode(104,95,57,50,95,99,97,108,108,111,117,116,0);
         w_centerm += `${parseInt(`${runtimeschedulerm}`)}`;
         telemetry_ += `${telemetry_.length}`;
         runtimey = `${runtimey.length}`;
         if (stations0) {
            break;
         }
      } while (stations0 && (1 <= (w_centerm.length * 2) && 2 <= (w_centerm.length * parseInt(`${runtimeschedulerm}`))));
       let internetu = String.fromCharCode(105,109,112,111,114,116,101,114,115,95,102,95,49,48,0);
       let register_lg = String.fromCharCode(98,95,57,95,104,97,115,104,116,97,98,108,101,0);
      while (3.61 <= (runtimeschedulerm + parseFloat(`${weibor.length}`))) {
         runtimeschedulerm += parseFloat(`${parseInt(`${productY}`)}`);
         break;
      }
         viewsQ <<= Math.min(Math.abs(2 % (Math.max(8, register_lg.length))), 3);
         weibor += `${w_centerm.length | 1}`;
      if ((parseFloat(`${register_lg.length}`) * runtimeschedulerm) > 5.81) {
         runtimeschedulerm -= (parseFloat(`${String.fromCharCode(112,0) == weibor ? viewsQ : weibor.length}`));
      }
      for (let g = 0; g < 2; g++) {
         internetu += `${2 | weibor.length}`;
      }
      while (w_centerm.length == viewsQ) {
         w_centerm += `${1 - parseInt(`${productY}`)}`;
         break;
      }
         internetu += `${viewsQ + register_lg.length}`;
          let viewerj = 1;
          let rncoreE = 0.0;
         internetu = "2";
         viewerj += 1;
         rncoreE /= Math.max(4, viewerj / 2);
         runtimeschedulerm *= parseFloat(`${viewsQ}`);
      for (let y = 0; y < 3; y++) {
         weibor += `${weibor.length}`;
      }
      if (3 < (weibor.length >> (Math.min(Math.abs(3), 3))) || 3 < (weibor.length + 3)) {
         weibor += "1";
      }
          let sortr = String.fromCharCode(101,95,56,49,95,111,112,101,110,115,115,108,118,0);
          let emptyo = String.fromCharCode(110,111,109,105,110,97,108,95,99,95,56,51,0);
          let libreanimatedF = 0.0;
         register_lg = `${parseInt(`${productY}`) * register_lg.length}`;
         sortr += `${(emptyo == String.fromCharCode(78,0) ? emptyo.length : parseInt(`${libreanimatedF}`))}`;
         libreanimatedF -= parseFloat(`${sortr.length >> (Math.min(Math.abs(1), 5))}`);
          let mintegralb: Array<any> = [177, 555];
          let schedulerm = false;
         productY *= (register_lg.length / (Math.max(3, (schedulerm ? 2 : 1))));
         mintegralb = [mintegralb.length];
         schedulerm = 46 >= mintegralb.length && 46 >= mintegralb.length;
      mode3 = `${parseInt(`${stationsN}`)}`;
   }
   for (let i = 0; i < 3; i++) {
       let whatsappp: Map<any, any> = new Map([[String.fromCharCode(98,115,111,108,117,116,101,95,108,95,52,57,0),962], [String.fromCharCode(114,111,119,115,95,56,95,56,53,0),173], [String.fromCharCode(110,95,50,50,95,109,97,115,115,0),49]]);
       let ecopy_4nQ: Map<any, any> = new Map([[String.fromCharCode(116,114,97,112,95,104,95,51,52,0),false ], [String.fromCharCode(107,95,51,56,95,103,100,111,99,0),false ], [String.fromCharCode(116,121,112,101,115,101,116,116,101,114,95,121,95,49,56,0),true ]]);
       let iconsaveimageX = String.fromCharCode(107,95,57,95,110,98,105,116,115,0);
       let hometeamfieldj = 2.0;
       let h_unlock3 = 1.0;
          let clubG = String.fromCharCode(120,95,52,54,95,114,101,99,101,105,118,101,0);
          let windr = 2.0;
          let iconnewssharem: Array<any> = [438, 902, 568];
         whatsappp.set(clubG, 1 >> (Math.min(5, clubG.length)));
         windr *= 1 | iconnewssharem.length;
         iconnewssharem = [1];
      let securityR = iconsaveimageX.length >= 9100969;
      do {
          let colorsM = String.fromCharCode(121,95,56,56,95,115,117,98,108,97,121,111,117,116,0);
          let policyi = String.fromCharCode(121,95,49,48,95,114,111,117,110,100,101,100,110,101,115,115,0);
          let runtimescheduler3 = String.fromCharCode(98,95,50,48,95,107,109,118,99,0);
          let eighteen4 = 5.0;
          let whistles: Map<any, any> = new Map([[String.fromCharCode(120,95,49,57,95,116,104,114,101,115,104,0),232], [String.fromCharCode(100,101,99,111,114,97,116,105,111,110,115,95,48,95,53,49,0),828]]);
         iconsaveimageX += `${parseInt(`${h_unlock3}`)}`;
         colorsM += `${parseInt(`${eighteen4}`) * runtimescheduler3.length}`;
         policyi = `${colorsM.length << (Math.min(3, Math.abs(parseInt(`${eighteen4}`))))}`;
         runtimescheduler3 = `${policyi.length}`;
         whistles.set(`${runtimescheduler3}`, runtimescheduler3.length);
         if (securityR) {
            break;
         }
      } while (((iconsaveimageX.length + hometeamfieldj) >= 2.51) && securityR);
       let showlessl = 2.0;
          let redirectg = String.fromCharCode(106,114,110,108,95,98,95,52,57,0);
          let firebaseB = 2.0;
         h_unlock3 *= parseFloat(`${parseInt(`${hometeamfieldj}`) & 2}`);
         redirectg = `${redirectg.length & 3}`;
         firebaseB /= Math.max(3, parseInt(`${firebaseB}`) ^ redirectg.length);
      if (3.37 < (parseFloat(`${iconsaveimageX.length}`) * h_unlock3) || (parseInt(`${h_unlock3}`) * iconsaveimageX.length) < 5) {
          let suggestionr = true;
         iconsaveimageX += `${1 / (Math.max(4, iconsaveimageX.length))}`;
      }
      let logo0 = 5531146.0 >= h_unlock3;
      do {
          let sellU = true;
          let heartq = String.fromCharCode(115,95,54,54,95,114,101,99,111,110,102,105,103,117,114,97,98,108,101,0);
          let questQ: Map<any, any> = new Map([[String.fromCharCode(100,101,108,105,109,105,116,101,100,95,122,95,57,51,0),547], [String.fromCharCode(100,105,118,105,100,101,114,95,110,95,50,49,0),576]]);
          let memberI = 4.0;
         h_unlock3 += parseFloat(`${heartq.length % 3}`);
         sellU = memberI < 47.89;
         heartq = `${parseInt(`${memberI}`)}`;
         questQ = new Map([[`${memberI}`, parseInt(`${memberI}`) + 1]]);
         if (logo0) {
            break;
         }
      } while (((hometeamfieldj / 5.25) == 4.76 && (h_unlock3 / (Math.max(5.25, 5))) == 2.17) && logo0);
       let imagenomoredata0 = 4;
         imagenomoredata0 &= ecopy_4nQ.size;
      for (let j = 0; j < 1; j++) {
         h_unlock3 += parseFloat(`${ecopy_4nQ.size}`);
      }
          let groups = String.fromCharCode(100,101,102,114,97,103,109,101,110,116,95,122,95,50,57,0);
          let index2 = String.fromCharCode(106,95,53,95,99,114,101,97,116,111,114,0);
         iconsaveimageX += `${iconsaveimageX.length}`;
         groups = `${(String.fromCharCode(104,0) == groups ? index2.length : groups.length)}`;
         index2 += `${(index2 == String.fromCharCode(116,0) ? groups.length : index2.length)}`;
          let mbjscommonq = String.fromCharCode(116,95,53,56,95,120,112,101,114,105,109,101,110,116,97,108,0);
         iconsaveimageX += `${parseInt(`${showlessl}`)}`;
         mbjscommonq = `${3 * mbjscommonq.length}`;
      halffieldimageo -= 1;
   }
   if ((commonK - 5.10) <= 4.7 && (5 - parseInt(`${commonK}`)) <= 1) {
      mode3 += `${orangeuparrowb}`;
   }

  const isVip = mayi_Gift.isVip(userState.user);

   while (1 < mode3.length || !cricket_) {
      mode3 = `${1 ^ parseInt(`${commonK}`)}`;
      break;
   }
   while (2 < (leagueM.length | 1) && (parseFloat(`${leagueM.length}`) * holderI) < 2.13) {
      leagueM += `${leagueM.length & mode3.length}`;
      break;
   }
   let scrollview4 = orangeuparrowb >= 6044623;
   do {
      orangeuparrowb >>= Math.min(4, Math.abs(3));
      if (scrollview4) {
         break;
      }
   } while ((4.13 == (halffieldimageo + 4.1)) && scrollview4);

  const handleCloseModal = useCallback(() => {

      commonK *= 3;
      holderI *= parseFloat(`${bingi.length}`);
   while (!mode3.includes(`${stationsN}`)) {
       let libtobp = false;
       let sourceE: Map<any, any> = new Map([[String.fromCharCode(114,101,111,114,100,101,114,105,110,103,95,116,95,50,50,0),490], [String.fromCharCode(109,95,51,49,95,99,104,97,115,101,0),947], [String.fromCharCode(106,95,50,95,97,116,99,104,0),717]]);
       let videoJ = String.fromCharCode(111,95,51,53,95,112,114,111,118,105,100,101,114,115,0);
       let coreL = String.fromCharCode(116,95,50,56,95,99,111,110,102,105,103,117,114,97,116,105,111,110,115,0);
       let roundI = 1.0;
      if ((sourceE.size % 2) <= 4) {
         libtobp = String.fromCharCode(90,0) == coreL;
      }
      if (3 < coreL.length) {
         coreL = `${parseInt(`${roundI}`)}`;
      }
         videoJ = `${videoJ.length >> (Math.min(Math.abs(2), 4))}`;
         coreL = `${(3 ^ (libtobp ? 2 : 1))}`;
       let footballfieldf: Map<any, any> = new Map([[String.fromCharCode(119,95,54,53,95,100,101,110,115,101,0),String.fromCharCode(98,108,111,99,107,115,105,122,101,95,112,95,51,53,0)], [String.fromCharCode(115,105,116,101,109,97,112,95,102,95,57,51,0),String.fromCharCode(117,95,55,56,95,114,97,110,115,102,111,114,109,101,114,0)], [String.fromCharCode(105,110,116,109,97,116,104,95,106,95,55,50,0),String.fromCharCode(112,111,115,116,112,111,110,101,95,98,95,56,49,0)]]);
       let fast7: Map<any, any> = new Map([[String.fromCharCode(104,111,117,114,95,122,95,55,48,0),391], [String.fromCharCode(115,97,116,100,95,54,95,54,52,0),273], [String.fromCharCode(110,95,50,55,95,115,121,110,99,0),952]]);
      while (3 > (sourceE.size << (Math.min(videoJ.length, 5))) || (3 << (Math.min(2, videoJ.length))) > 3) {
         videoJ = `${(coreL == String.fromCharCode(115,0) ? videoJ.length : coreL.length)}`;
         break;
      }
      while (5 == videoJ.length) {
          let stringm: Array<any> = [691, 945, 848];
          let friendsO = 4.0;
          let videovarL = false;
         libtobp = friendsO >= 10.32;
         stringm.push(((videovarL ? 5 : 4) >> (Math.min(stringm.length, 5))));
         friendsO += parseFloat(`${stringm.length}`);
         break;
      }
      while (3 > fast7.size) {
         coreL = `${videoJ.length ^ coreL.length}`;
         break;
      }
      let basketballawayteamW = videoJ == String.fromCharCode(111,121,118,105,0);
      do {
         videoJ += `${footballfieldf.size - fast7.size}`;
         if (basketballawayteamW) {
            break;
         }
      } while (basketballawayteamW && ((1 ^ videoJ.length) < 2 && 3 < (parseInt(`${roundI}`) + 1)));
         libtobp = sourceE.size <= 61 && libtobp;
         roundI /= Math.max(parseFloat(`${footballfieldf.size >> (Math.min(Math.abs(3), 5))}`), 4);
      for (let j = 0; j < 3; j++) {
          let backD: Map<any, any> = new Map([[String.fromCharCode(122,95,52,54,95,97,118,101,114,0),442], [String.fromCharCode(121,95,56,53,95,116,104,117,109,98,115,0),654], [String.fromCharCode(115,116,98,108,95,52,95,53,49,0),872]]);
          let dragQ: Array<any> = [158, 613, 292];
          let refresh1 = false;
          let notificationK = 1;
          let unimplementedview_ = 2;
         sourceE.set(`${notificationK}`, notificationK);
         backD = new Map([[`${backD.size}`, (2 ^ (refresh1 ? 4 : 1))]]);
         dragQ = [unimplementedview_ * 3];
         refresh1 = dragQ.length == 99;
         unimplementedview_ >>= Math.min(5, Math.abs(unimplementedview_));
      }
      while (coreL.length < parseInt(`${roundI}`)) {
         coreL = `${(coreL == String.fromCharCode(49,0) ? footballfieldf.size : coreL.length)}`;
         break;
      }
      for (let w = 0; w < 3; w++) {
         videoJ += `${fast7.size >> (Math.min(Math.abs(1), 5))}`;
      }
         fast7.set(coreL, coreL.length);
      mode3 = `${parseInt(`${roundI}`) % 1}`;
      break;
   }
    dispatch(hideAdultModeVip())
  }, [])

       let predictionarrowX = String.fromCharCode(118,111,114,98,105,115,102,108,111,97,116,102,108,111,97,116,95,57,95,53,49,0);
         predictionarrowX = `${predictionarrowX.length}`;
         predictionarrowX = `${predictionarrowX.length}`;
          let libswresample_ = String.fromCharCode(119,95,55,49,95,119,105,100,101,0);
         predictionarrowX += `${libswresample_.length}`;
      cricket_ = bingi.length > 13 && fillB == String.fromCharCode(87,0);
   for (let h = 0; h < 3; h++) {
      storeV = !cricket_;
   }
       let stylef = false;
      while (stylef) {
          let neon8 = String.fromCharCode(116,114,101,102,95,109,95,54,52,0);
          let malaysia0: Array<any> = [635, 224];
          let short_bv = 1.0;
         stylef = neon8.length < malaysia0.length;
         neon8 += "2";
         malaysia0.push(parseInt(`${short_bv}`) << (Math.min(3, Math.abs(2))));
         break;
      }
      if (!stylef) {
         stylef = !stylef;
      }
      let main_pu = stylef ? !stylef : stylef;
      do {
         stylef = (stylef ? !stylef : stylef);
         if (main_pu) {
            break;
         }
      } while ((!stylef) && main_pu);
      commonK *= (mode3.length - (stylef ? 3 : 5));

  if (adultModeVipShow && !isVip && adultMode)

   while ((orangeuparrowb & mode3.length) >= 1 && (1 & mode3.length) >= 4) {
      orangeuparrowb %= Math.max(3, ((storeV ? 5 : 2) ^ bingi.length));
      break;
   }
   if (4 < (3 << (Math.min(4, leagueM.length)))) {
      leagueM = `${parseInt(`${holderI}`) + parseInt(`${commonK}`)}`;
   }
      mode3 = "2";
    return (
      <View
        style={{
          height: '100%',
          width: '100%',
          backgroundColor: 'rgba(0, 0, 0, 0.8)',
          position: 'absolute',
        }}>
        <VipModal>
          <View
            style={{
              backgroundColor: 'rgba(34, 34, 34, 0.9)',
              marginTop: 40,
              borderRadius: 12,
              paddingTop: 36,
              paddingBottom: 26,
              paddingHorizontal: 28,
            }}>
            <TouchableOpacity
              onPress={() => {
                handleCloseModal()
              }}>
              <View style={styles.closeBtnContainer}>
                <CloseIcon />
              </View>
            </TouchableOpacity>
            <View style={{ alignItems: 'center', justifyContent: 'center' }}>
              <Text
                style={{
                  fontSize: 20,
                  fontWeight: '500',
                  color: '#E2820E',
                  paddingTop: 20,
                  paddingVertical: 12,
                }}>
                VIP升级权益
              </Text>
              <Text
                style={{
                  color: 'white',
                  padding: 2,
                  fontSize: 16,
                  fontWeight: '300',
                }}>
                立即升级VIP，畅享午夜视频
              </Text>
              <View style={styles.btnContainer}>
                {SHOW_ZF_CONST && <TouchableOpacity
                  onPress={() => {
                    handleCloseModal();
                    navigator.navigate("付费VIP");
                    
                    
                    
                    
                    
                    
                  }}
                  style={styles.btn}>
                  <Text
                    style={{
                      color: '#000',
                      fontWeight: '600',
                      fontSize: 16,
                    }}>
                    购买VIP
                  </Text>
                </TouchableOpacity>}
                <TouchableOpacity
                  onPress={() => {
                    handleCloseModal();
                    navigator.navigate('邀请');
                  }}
                  style={styles.btn}>
                  <Text
                    style={{
                      color: '#000',
                      fontWeight: '600',
                      fontSize: 16,
                    }}>
                    邀请好友
                  </Text>
                </TouchableOpacity>
              </View>
            </View>
          </View>
        </VipModal>
      </View>
    );
};

const styles = StyleSheet.create({
  closeBtnContainer: {
    height: 20,
    width: 20,
    marginTop: -20,
    marginRight: -10,
    
    alignSelf: 'flex-end',
    
  },

  btnContainer: {
    flexDirection: 'row',
    justifyContent: 'space-evenly',
    gap: 10,
    paddingTop: 25,
    paddingBottom: 10,
  },

  btn: {
    backgroundColor: '#FAC33D',
    paddingVertical: 10,
    alignItems: 'center',
    borderRadius: 10,
    flex: 1,
  },
})

export default AdultVideoVipModal;
