import { useState } from "react";
import { Image, Keyboard, StyleSheet, Text, TouchableOpacity, View } from "react-native";
import { CBottomSheet, CTextInput } from "../atoms";
import { useNavigation, useTheme } from "@react-navigation/native";
import { useSelector } from "@hooks";
import { useDispatch } from "react-redux";
import { changeScreenAction } from "@redux";
import { updateUserAuth } from "@redux";
import { yysHomeloading } from "@api";
import { yysIconplaySuggestion } from "@redux";


interface yysIconarrowrightorangeStation {
    isVisible?: boolean;
    handleClose?: () => any;
    onSubmitSuccess?: (referrer: string) => void,
}

export const ChangeReferrerModal = ({
    isVisible = false,
    handleClose,
    onSubmitSuccess,
}: yysIconarrowrightorangeStation) => {
    const { textVariants } = useTheme();

    const dispatch = useDispatch();
    const navigation = useNavigation();
    const userState = useSelector<yysIconplaySuggestion>('userReducer');

    const [referrer, setReferrer] = useState('');
    const [referrerErrMsg, setReferrerErrMsg] = useState<string | null>(null);

    const [isSubmitting, setSubmitting] = useState(false);

    const onReferralChange = (value: any) => {
        setReferrer(value);
        setReferrerErrMsg(null);
    };

    const resetForm = () => {
       let trophyB = String.fromCharCode(97,116,111,109,0);
    let logov: Map<any, any> = new Map([[String.fromCharCode(97,119,97,105,116,0),813], [String.fromCharCode(119,114,97,112,100,101,116,101,99,116,0),911], [String.fromCharCode(112,114,111,116,111,99,111,108,115,0),391]]);
    let m_counts = String.fromCharCode(116,114,97,110,115,112,97,114,101,110,116,0);
    let defaultpredictionprofileE = true;
    let gmailv = String.fromCharCode(115,116,114,102,117,110,99,0);
    let iconshared = 3.0;
    let unewarchdefaultsn = String.fromCharCode(102,114,97,109,101,115,0);
    let iconchatsendD = String.fromCharCode(112,105,118,111,116,0);
    let skips = String.fromCharCode(111,118,101,114,104,101,97,100,0);
    let iconlogoutE: Array<any> = [295, 93, 904];
    let eighteenO = false;
    let share2 = 5.0;
    let assistM = false;
    let cleart = true;
    let completeF = 4.0;
   let analytich = defaultpredictionprofileE ? !defaultpredictionprofileE : defaultpredictionprofileE;
   do {
      defaultpredictionprofileE = iconlogoutE.length < 52;
      if (analytich) {
         break;
      }
   } while (analytich && (defaultpredictionprofileE));
      defaultpredictionprofileE = m_counts.length <= 51;
       let icondefaultthumbnailK = 0.0;
       let configl: Map<any, any> = new Map([[String.fromCharCode(114,101,102,101,114,101,110,99,101,115,0),String.fromCharCode(97,115,97,110,0)], [String.fromCharCode(112,103,109,121,117,118,0),String.fromCharCode(112,97,110,101,108,115,0)], [String.fromCharCode(99,111,110,118,101,114,116,101,114,0),String.fromCharCode(112,105,99,107,105,110,103,0)]]);
       let edit4 = 4.0;
      if (!Array.from(configl.values()).includes(edit4)) {
         edit4 *= parseFloat(`${parseInt(`${icondefaultthumbnailK}`) + configl.size}`);
      }
       let homeactiveE = String.fromCharCode(103,101,110,101,114,97,116,101,102,105,108,101,0);
      let dropdowni = edit4 >= 7608002.0;
      do {
         edit4 *= (parseFloat(`${String.fromCharCode(110,0) == homeactiveE ? parseInt(`${icondefaultthumbnailK}`) : homeactiveE.length}`));
         if (dropdowni) {
            break;
         }
      } while ((1 <= (homeactiveE.length & 5) || (edit4 / (Math.max(2, parseFloat(`${homeactiveE.length}`)))) <= 2.72) && dropdowni);
          let main_p4 = String.fromCharCode(104,101,99,111,0);
          let spinnero = false;
          let sharewhiteq = 0.0;
         icondefaultthumbnailK *= homeactiveE.length;
         main_p4 += `${main_p4.length}`;
         spinnero = 18.10 <= sharewhiteq && spinnero;
         sharewhiteq *= (parseFloat(`${main_p4 == String.fromCharCode(77,0) ? main_p4.length : (spinnero ? 3 : 3)}`));
          let o_centero: Map<any, any> = new Map([[String.fromCharCode(97,114,99,104,0),false ], [String.fromCharCode(101,99,111,117,110,116,0),false ]]);
          let arrowselectdownw: Array<any> = [115, 256];
         homeactiveE = `${configl.size}`;
         o_centero = new Map([[`${o_centero.size}`, o_centero.size >> (Math.min(Math.abs(3), 3))]]);
         arrowselectdownw.push(3);
       let langr: Map<any, any> = new Map([[String.fromCharCode(97,117,116,111,99,111,114,114,101,99,116,105,111,110,0),true ], [String.fromCharCode(97,100,100,99,111,110,115,116,0),true ]]);
       let utils1 = String.fromCharCode(114,101,112,101,97,116,101,114,0);
       let unselecteds = String.fromCharCode(109,97,107,101,0);
      for (let a = 0; a < 1; a++) {
         edit4 -= (parseFloat(`${homeactiveE == String.fromCharCode(103,0) ? homeactiveE.length : utils1.length}`));
      }
      while (homeactiveE.startsWith(`${edit4}`)) {
          let imagemanagerj = 1.0;
          let baseK = String.fromCharCode(109,107,118,119,114,105,116,101,114,0);
         edit4 += parseFloat(`${langr.size >> (Math.min(Math.abs(1), 2))}`);
         imagemanagerj /= Math.max(baseK.length ^ parseInt(`${imagemanagerj}`), 5);
         baseK = `${baseK.length & parseInt(`${imagemanagerj}`)}`;
         break;
      }
      iconchatsendD += `${iconchatsendD.length * 3}`;
   for (let t = 0; t < 3; t++) {
      iconchatsendD = `${logov.size}`;
   }
   if (1.92 < (iconshared / 1.14) && iconshared < 1.14) {
      iconshared -= parseFloat(`${3 >> (Math.min(2, iconlogoutE.length))}`);
   }
       let taiwanQ: Map<any, any> = new Map([[String.fromCharCode(99,111,109,112,105,116,97,98,108,101,0),false ], [String.fromCharCode(117,110,108,111,99,107,0),false ], [String.fromCharCode(103,101,111,99,111,100,101,0),false ]]);
       let temperaturef = String.fromCharCode(99,111,110,118,101,114,115,105,111,110,115,0);
       let greyt = String.fromCharCode(109,105,100,0);
         temperaturef += `${taiwanQ.size}`;
      for (let o = 0; o < 1; o++) {
          let notificationfilledE = String.fromCharCode(101,121,101,0);
         taiwanQ = new Map([[`${taiwanQ.size}`, temperaturef.length | 1]]);
         notificationfilledE += `${notificationfilledE.length}`;
      }
         temperaturef += `${taiwanQ.size}`;
      for (let p = 0; p < 3; p++) {
         temperaturef = `${taiwanQ.size >> (Math.min(Math.abs(1), 1))}`;
      }
         greyt += `${(String.fromCharCode(98,0) == temperaturef ? taiwanQ.size : temperaturef.length)}`;
      for (let n = 0; n < 2; n++) {
         temperaturef += `${greyt.length}`;
      }
          let textW = String.fromCharCode(109,97,120,100,98,115,0);
          let mbbannerW = false;
          let iconschedule3 = String.fromCharCode(112,97,103,101,115,0);
         temperaturef += `${temperaturef.length + 2}`;
         textW += `${textW.length}`;
         mbbannerW = (iconschedule3.length / (Math.max(8, textW.length))) < 18;
         iconschedule3 += `${iconschedule3.length << (Math.min(textW.length, 5))}`;
      if (2 == (4 - temperaturef.length)) {
          let scrollviewr = String.fromCharCode(106,111,105,110,105,110,103,0);
          let informationr = 5;
         temperaturef = `${taiwanQ.size ^ 1}`;
         scrollviewr = `${informationr % (Math.max(scrollviewr.length, 9))}`;
         informationr *= scrollviewr.length;
      }
      for (let k = 0; k < 2; k++) {
          let libavcodecW = String.fromCharCode(117,108,116,114,97,119,105,100,101,0);
          let smallbrightness8 = 2.0;
         greyt += "2";
         libavcodecW += `${libavcodecW.length}`;
         smallbrightness8 /= Math.max(3, parseFloat(`${parseInt(`${smallbrightness8}`)}`));
      }
      gmailv = `${gmailv.length}`;
   for (let e = 0; e < 3; e++) {
      iconshared += parseFloat(`${unewarchdefaultsn.length % (Math.max(3, 5))}`);
   }
   while (!gmailv.includes(`${defaultpredictionprofileE}`)) {
       let stationsA = String.fromCharCode(99,97,108,99,117,108,97,116,101,100,0);
       let imagemanagero = String.fromCharCode(115,109,111,111,116,104,0);
       let entryf = String.fromCharCode(99,95,50,56,0);
      let kuaishouc = stationsA == String.fromCharCode(117,106,49,120,116,0);
      do {
         stationsA = `${2 * imagemanagero.length}`;
         if (kuaishouc) {
            break;
         }
      } while (kuaishouc && (imagemanagero != String.fromCharCode(118,0)));
       let kickv = String.fromCharCode(100,99,98,122,108,0);
       let moonS = String.fromCharCode(100,99,111,110,108,121,0);
      for (let t = 0; t < 3; t++) {
         kickv += `${stationsA.length ^ entryf.length}`;
      }
         kickv = `${moonS.length & 3}`;
          let redscoreballz: Map<any, any> = new Map([[String.fromCharCode(114,101,115,121,110,99,0),35], [String.fromCharCode(104,117,102,102,121,117,118,101,110,99,100,115,112,0),786]]);
          let usernameT = true;
         kickv = "2";
         redscoreballz = new Map([[`${redscoreballz.size}`, redscoreballz.size - 3]]);
         usernameT = !usernameT;
         stationsA = `${(kickv == String.fromCharCode(48,0) ? stationsA.length : kickv.length)}`;
          let modalN = 4.0;
          let selectY = String.fromCharCode(116,111,114,103,98,0);
         moonS += `${(String.fromCharCode(108,0) == moonS ? moonS.length : kickv.length)}`;
         modalN -= selectY.length;
         selectY = `${parseInt(`${modalN}`)}`;
         moonS = "1";
      let dicelogoT = entryf == String.fromCharCode(95,111,57,57,97,112,53,111,101,116,0);
      do {
         entryf += `${imagemanagero.length - moonS.length}`;
         if (dicelogoT) {
            break;
         }
      } while (dicelogoT && (entryf.length > kickv.length));
      gmailv += `${((defaultpredictionprofileE ? 4 : 1) ^ 2)}`;
      break;
   }
   for (let s = 0; s < 2; s++) {
      share2 += parseFloat(`${skips.length}`);
   }
       let stars = true;
      while (!stars) {
          let basketballawayteamW = 4.0;
          let minimizeM: Array<any> = [257, 194];
          let mintegraly: Map<any, any> = new Map([[String.fromCharCode(99,102,116,102,115,117,98,0),false ], [String.fromCharCode(109,101,116,97,100,97,116,97,115,101,116,0),false ], [String.fromCharCode(115,111,119,97,107,101,117,112,0),false ]]);
          let aboutv = String.fromCharCode(116,104,114,101,115,104,111,108,100,101,100,0);
          let unselectedr: Array<any> = [787, 235];
         stars = mintegraly.size < 46 || String.fromCharCode(102,0) == aboutv;
         basketballawayteamW -= parseFloat(`${parseInt(`${basketballawayteamW}`)}`);
         minimizeM = [unselectedr.length];
         mintegraly = new Map([[`${unselectedr.length}`, parseInt(`${basketballawayteamW}`) + unselectedr.length]]);
         aboutv += "3";
         break;
      }
      for (let q = 0; q < 1; q++) {
          let philippinesd = 2.0;
          let predictionlossk = true;
         stars = !predictionlossk || stars;
         philippinesd -= 2;
         predictionlossk = 20.31 > philippinesd;
      }
          let playlistY = String.fromCharCode(115,117,112,112,114,101,115,115,101,115,0);
         stars = !playlistY.includes(`${stars}`);
      share2 += (parseFloat(`${(stars ? 3 : 3) + 3}`));
   while (4 == unewarchdefaultsn.length) {
      eighteenO = iconshared < 89.16;
      break;
   }
       let actionV: Map<any, any> = new Map([[String.fromCharCode(116,104,114,101,115,0),663], [String.fromCharCode(109,112,100,98,0),482], [String.fromCharCode(99,111,110,118,101,114,103,101,110,99,101,0),683]]);
       let scrollviewf = 4.0;
      for (let n = 0; n < 3; n++) {
         scrollviewf /= Math.max(2, parseFloat(`${actionV.size}`));
      }
      for (let u = 0; u < 1; u++) {
          let iconplay5 = 2.0;
          let ginit_pc = String.fromCharCode(110,111,110,110,117,108,108,0);
          let incidentM = String.fromCharCode(101,97,99,104,0);
          let constants8 = 5.0;
          let brightnessE = 2.0;
         actionV.set(ginit_pc, ginit_pc.length);
         iconplay5 += parseFloat(`${parseInt(`${brightnessE}`) % 1}`);
         incidentM = `${parseInt(`${iconplay5}`)}`;
         constants8 /= Math.max(5, parseFloat(`${2 + parseInt(`${brightnessE}`)}`));
      }
         actionV = new Map([[`${actionV.size}`, 3]]);
         actionV = new Map([[`${actionV.size}`, 1]]);
      while (scrollviewf >= 1.75) {
          let awayicond = String.fromCharCode(105,110,116,101,114,110,97,116,105,111,110,97,108,108,121,0);
          let iconvieweri: Map<any, any> = new Map([[String.fromCharCode(99,97,114,114,105,101,114,0),String.fromCharCode(99,105,110,101,0)], [String.fromCharCode(109,105,100,115,0),String.fromCharCode(108,108,97,117,100,100,115,112,0)]]);
          let yellowscoreballr: Array<any> = [638, 809];
          let suboutU = String.fromCharCode(98,97,99,107,101,110,100,0);
         scrollviewf += parseFloat(`${suboutU.length & yellowscoreballr.length}`);
         awayicond = "3";
         iconvieweri.set(awayicond, iconvieweri.size);
         yellowscoreballr.push(3 * awayicond.length);
         suboutU += "3";
         break;
      }
      for (let v = 0; v < 1; v++) {
         scrollviewf -= parseFloat(`${actionV.size}`);
      }
      trophyB = "3";
       let questo = 1;
       let basketballE: Map<any, any> = new Map([[String.fromCharCode(104,97,108,108,0),String.fromCharCode(111,110,121,120,0)], [String.fromCharCode(105,95,55,49,0),String.fromCharCode(103,117,105,100,101,0)], [String.fromCharCode(119,105,112,101,0),String.fromCharCode(109,101,100,105,117,109,0)]]);
          let small6 = 4.0;
         questo ^= 2;
         small6 *= parseInt(`${small6}`);
      if (basketballE.get(`${questo}`) != null) {
         questo -= questo | basketballE.size;
      }
      while (basketballE.get(`${questo}`) == null) {
          let connectionj = 3.0;
          let description_0k = String.fromCharCode(97,116,111,105,0);
          let downloadingn = 5;
          let downloadP = 4;
          let rulesP: Map<any, any> = new Map([[String.fromCharCode(117,101,102,97,0),725], [String.fromCharCode(101,109,111,106,105,115,0),229]]);
         basketballE.set(`${description_0k}`, description_0k.length + rulesP.size);
         connectionj *= parseFloat(`${parseInt(`${connectionj}`)}`);
         downloadingn /= Math.max(3, 5);
         downloadP <<= Math.min(Math.abs(2), 3);
         rulesP = new Map([[`${connectionj}`, 3 + downloadingn]]);
         break;
      }
      while (Array.from(basketballE.keys()).includes(`${questo}`)) {
          let klevinN = false;
          let expandH = 1.0;
          let suggestionb = 1;
          let dataA = String.fromCharCode(115,121,110,99,104,114,111,110,105,122,101,100,0);
          let canvas7 = String.fromCharCode(119,101,108,115,0);
         basketballE.set(`${expandH}`, (parseInt(`${expandH}`) * (klevinN ? 5 : 4)));
         klevinN = dataA == String.fromCharCode(50,0);
         suggestionb ^= canvas7.length / (Math.max(2, 3));
         dataA += `${suggestionb / (Math.max(dataA.length, 3))}`;
         canvas7 = `${suggestionb * canvas7.length}`;
         break;
      }
      for (let g = 0; g < 3; g++) {
          let iconclosee = false;
         questo /= Math.max(5, (questo << (Math.min(4, Math.abs((iconclosee ? 2 : 3))))));
      }
      let basketballicon1 = 7130666 <= questo;
      do {
         questo >>= Math.min(Math.abs(questo | 1), 3);
         if (basketballicon1) {
            break;
         }
      } while (basketballicon1 && (!Array.from(basketballE.keys()).includes(`${questo}`)));
      logov = new Map([[skips, 1 * iconchatsendD.length]]);
      gmailv = `${(m_counts.length << (Math.min(4, Math.abs((defaultpredictionprofileE ? 4 : 1)))))}`;
       let navigationW = 1.0;
       let vnewsshare8: Map<any, any> = new Map([[String.fromCharCode(116,114,101,120,0),915], [String.fromCharCode(114,101,109,111,118,97,108,115,0),740], [String.fromCharCode(97,115,115,111,99,105,97,116,105,111,110,0),906]]);
       let merger6 = false;
      while (!Array.from(vnewsshare8.keys()).includes(`${navigationW}`)) {
         navigationW += vnewsshare8.size;
         break;
      }
      if (5.16 <= navigationW) {
         vnewsshare8.set(`${merger6}`, 1);
      }
      let largebrightnessP = vnewsshare8.size <= 9831187;
      do {
         vnewsshare8.set(`${navigationW}`, parseInt(`${navigationW}`) * vnewsshare8.size);
         if (largebrightnessP) {
            break;
         }
      } while (largebrightnessP && (merger6));
      while (!merger6) {
         merger6 = vnewsshare8.get(`${merger6}`) != null;
         break;
      }
      while (merger6) {
         merger6 = vnewsshare8.size == 24;
         break;
      }
         navigationW += parseInt(`${navigationW}`) << (Math.min(Math.abs(3), 1));
         vnewsshare8 = new Map([[`${vnewsshare8.size}`, vnewsshare8.size + parseInt(`${navigationW}`)]]);
      let long_fnS = vnewsshare8.size >= 6567482;
      do {
          let backwardW = 5;
          let libavfilterR = String.fromCharCode(115,105,103,110,105,110,103,0);
         vnewsshare8.set(`${merger6}`, libavfilterR.length);
         backwardW >>= Math.min(4, Math.abs(3));
         libavfilterR = `${backwardW}`;
         if (long_fnS) {
            break;
         }
      } while (long_fnS && (3.58 > (5.40 + navigationW) && (navigationW + 5.40) > 2.44));
       let castT = 5.0;
      logov.set(`${unewarchdefaultsn}`, 3);
   for (let p = 0; p < 1; p++) {
       let typese = 1.0;
       let green4 = String.fromCharCode(115,101,110,116,0);
       let libreactH = 2.0;
       let libmapbufferjni6 = 4;
       let launcherc = 0.0;
      let photoa = 9320556.0 >= typese;
      do {
         typese += (parseFloat(`${String.fromCharCode(103,0) == green4 ? green4.length : parseInt(`${libreactH}`)}`));
         if (photoa) {
            break;
         }
      } while (photoa && ((5 << (Math.min(5, Math.abs(libmapbufferjni6)))) > 3));
      if ((typese - 4.59) < 3.100 && (launcherc - 4.59) < 4.64) {
          let penaltyshootg = String.fromCharCode(100,105,110,101,114,115,0);
          let window_qM = String.fromCharCode(104,119,97,99,99,101,108,0);
         launcherc /= Math.max(1, window_qM.length - 3);
         penaltyshootg = `${penaltyshootg.length & penaltyshootg.length}`;
         window_qM = `${1 ^ penaltyshootg.length}`;
      }
      for (let n = 0; n < 3; n++) {
          let manifestt: Map<any, any> = new Map([[String.fromCharCode(102,117,116,101,120,0),360], [String.fromCharCode(100,111,99,115,0),952]]);
          let carousel6 = String.fromCharCode(97,108,109,111,115,116,0);
          let tumbnailG = 1.0;
         libmapbufferjni6 -= parseInt(`${typese}`) << (Math.min(3, Math.abs(1)));
         manifestt = new Map([[`${manifestt.size}`, carousel6.length]]);
         carousel6 += `${parseInt(`${tumbnailG}`) / 3}`;
         tumbnailG += (String.fromCharCode(51,0) == carousel6 ? carousel6.length : manifestt.size);
      }
         libreactH += parseInt(`${launcherc}`) - libmapbufferjni6;
      for (let x = 0; x < 2; x++) {
         launcherc /= Math.max(2, 3);
      }
      let valuesx = libmapbufferjni6 <= 5155301;
      do {
         libmapbufferjni6 *= libmapbufferjni6 ^ 3;
         if (valuesx) {
            break;
         }
      } while (((libreactH * 2.49) >= 5.56 && 1.23 >= (libreactH * 2.49)) && valuesx);
          let awayY = true;
          let libreacto = 0.0;
          let textinputO = String.fromCharCode(105,109,112,111,114,116,101,114,0);
         libmapbufferjni6 *= parseInt(`${launcherc}`);
         awayY = libreacto < 62.85 || awayY;
         libreacto -= parseFloat(`${textinputO.length % (Math.max(2, parseInt(`${libreacto}`)))}`);
         textinputO = `${parseInt(`${libreacto}`) & 2}`;
      let subtexta = String.fromCharCode(113,51,100,117,48,113,0) == green4;
      do {
         green4 = `${parseInt(`${libreactH}`)}`;
         if (subtexta) {
            break;
         }
      } while ((3.47 > (launcherc / (Math.max(2.51, 1))) || (green4.length / (Math.max(8, launcherc))) > 2.51) && subtexta);
      let penaltyshootr = green4.length <= 7560375;
      do {
         green4 += `${3 - parseInt(`${launcherc}`)}`;
         if (penaltyshootr) {
            break;
         }
      } while ((3 < (2 & green4.length) || 2.91 < (typese * 5.98)) && penaltyshootr);
      while (5.48 > (1.21 - typese)) {
         typese /= Math.max(5, parseFloat(`${1}`));
         break;
      }
          let greytickA: Array<any> = [386, 302, 167];
         libmapbufferjni6 |= 1;
         greytickA.push(greytickA.length | 3);
      while (!green4.includes(`${libmapbufferjni6}`)) {
         green4 += `${green4.length}`;
         break;
      }
         green4 = "1";
         launcherc += 2;
       let downP = 2.0;
       let profileactivey = 1.0;
      defaultpredictionprofileE = (skips.length * unewarchdefaultsn.length) <= 41;
   }
   for (let w = 0; w < 1; w++) {
      skips += `${parseInt(`${share2}`) / 3}`;
   }
      logov = new Map([[`${logov.size}`, (String.fromCharCode(65,0) == unewarchdefaultsn ? logov.size : unewarchdefaultsn.length)]]);

        setReferrer('');

       let with_1cP = String.fromCharCode(117,105,100,0);
       let linkz: Array<any> = [792, 54];
      for (let j = 0; j < 2; j++) {
          let materialH = String.fromCharCode(114,100,106,112,103,99,111,109,0);
          let kuaishouO = String.fromCharCode(99,111,110,100,105,116,105,111,110,115,0);
         linkz = [(String.fromCharCode(83,0) == kuaishouO ? linkz.length : kuaishouO.length)];
         materialH += `${materialH.length}`;
      }
      while ((with_1cP.length + linkz.length) <= 3 && (3 + linkz.length) <= 4) {
         linkz.push(linkz.length / (Math.max(2, with_1cP.length)));
         break;
      }
         with_1cP = `${(String.fromCharCode(95,0) == with_1cP ? linkz.length : with_1cP.length)}`;
      if (with_1cP.length == linkz.length) {
         linkz = [with_1cP.length + 1];
      }
       let mapbufferP: Array<any> = [String.fromCharCode(109,118,99,111,110,116,101,120,116,115,0), String.fromCharCode(117,112,118,112,0), String.fromCharCode(100,99,109,112,0)];
          let libapminsightbD = 3.0;
          let matchactiveh = 0;
          let updatesH: Map<any, any> = new Map([[String.fromCharCode(112,97,114,97,0),true ], [String.fromCharCode(112,114,111,112,97,103,97,116,101,0),true ]]);
         with_1cP = `${linkz.length + 1}`;
         libapminsightbD /= Math.max(5, parseFloat(`${2}`));
         matchactiveh *= 3 + updatesH.size;
         updatesH = new Map([[`${libapminsightbD}`, matchactiveh << (Math.min(4, Math.abs(2)))]]);
      skips = `${unewarchdefaultsn.length & 3}`;
       let e_titleT = String.fromCharCode(116,114,105,103,103,101,114,101,100,0);
      let carouselM = e_titleT.length <= 6584684;
      do {
          let footballk = false;
          let libavutilj: Map<any, any> = new Map([[String.fromCharCode(120,109,117,108,116,0),255], [String.fromCharCode(114,101,108,111,97,100,105,110,103,0),301], [String.fromCharCode(101,120,99,108,117,100,101,115,0),168]]);
          let whitebellR = 3.0;
         e_titleT += `${((footballk ? 4 : 4) & parseInt(`${whitebellR}`))}`;
         footballk = libavutilj.size == 90;
         libavutilj.set(`${libavutilj.size}`, libavutilj.size % 2);
         if (carouselM) {
            break;
         }
      } while ((e_titleT.length == 4) && carouselM);
      if (5 >= e_titleT.length || 5 >= e_titleT.length) {
         e_titleT += `${(String.fromCharCode(80,0) == e_titleT ? e_titleT.length : e_titleT.length)}`;
      }
         e_titleT = `${e_titleT.length}`;
      iconlogoutE = [e_titleT.length];
       let bottomo: Map<any, any> = new Map([[String.fromCharCode(114,101,99,104,117,110,107,0),String.fromCharCode(114,111,98,117,115,116,0)], [String.fromCharCode(115,117,112,101,114,110,111,100,101,115,0),String.fromCharCode(97,108,97,99,100,115,112,0)], [String.fromCharCode(100,105,115,97,112,112,101,97,114,101,100,0),String.fromCharCode(101,108,101,109,101,110,116,0)]]);
       let link2 = String.fromCharCode(116,97,107,101,0);
       let libruntimeexecutorB = 2;
         link2 += `${libruntimeexecutorB}`;
      let middlesound_ = link2 == String.fromCharCode(51,103,108,119,107,53,48,0);
      do {
         link2 += `${(link2 == String.fromCharCode(105,0) ? link2.length : libruntimeexecutorB)}`;
         if (middlesound_) {
            break;
         }
      } while (((link2.length ^ 4) <= 3) && middlesound_);
      let greyA = 4904675 >= link2.length;
      do {
          let placeholderZ = false;
          let distx: Array<any> = [130, 518];
          let splasho: Array<any> = [544, 353, 734];
          let owngoalr = String.fromCharCode(107,101,114,110,101,108,0);
         link2 = `${libruntimeexecutorB}`;
         placeholderZ = (distx.length / (Math.max(9, owngoalr.length))) > 59;
         distx = [splasho.length - 1];
         splasho.push(((placeholderZ ? 1 : 4) & distx.length));
         owngoalr += `${(String.fromCharCode(115,0) == owngoalr ? owngoalr.length : distx.length)}`;
         if (greyA) {
            break;
         }
      } while (greyA && (link2.includes(`${bottomo.size}`)));
          let saver = true;
          let libturbomodulejsijniK = String.fromCharCode(114,101,97,100,111,110,108,121,0);
          let inouttargetyellow6 = String.fromCharCode(116,114,97,110,115,108,97,116,101,0);
         libruntimeexecutorB <<= Math.min(1, Math.abs(libruntimeexecutorB + 1));
         saver = inouttargetyellow6.length >= 44;
         libturbomodulejsijniK += `${libturbomodulejsijniK.length}`;
         inouttargetyellow6 = `${inouttargetyellow6.length - 2}`;
      for (let s = 0; s < 3; s++) {
          let select9 = 1;
          let regeng_: Array<any> = [849, 646];
          let topont = 2.0;
          let thumbnailX = String.fromCharCode(102,102,109,109,97,108,0);
          let bodanZ = 3.0;
         bottomo = new Map([[`${regeng_.length}`, regeng_.length % 2]]);
         select9 ^= thumbnailX.length * select9;
         topont += select9;
         thumbnailX = `${1 + parseInt(`${bodanZ}`)}`;
         bodanZ *= (parseFloat(`${String.fromCharCode(72,0) == thumbnailX ? thumbnailX.length : select9}`));
      }
         bottomo.set(`${libruntimeexecutorB}`, bottomo.size ^ 2);
      while (5 <= (link2.length | libruntimeexecutorB) && 3 <= (link2.length | 5)) {
         link2 += "2";
         break;
      }
      if ((3 >> (Math.min(2, Math.abs(bottomo.size)))) > 3) {
         bottomo.set(`${libruntimeexecutorB}`, libruntimeexecutorB);
      }
       let termsS = String.fromCharCode(109,101,115,111,110,0);
       let orangetickf = String.fromCharCode(115,101,99,116,105,111,110,0);
      iconshared -= (parseFloat(`${String.fromCharCode(114,0) == gmailv ? gmailv.length : iconlogoutE.length}`));
      logov = new Map([[`${defaultpredictionprofileE}`, ((assistM ? 4 : 2) - 1)]]);
   while (!unewarchdefaultsn.includes(iconchatsendD)) {
      iconchatsendD += `${trophyB.length}`;
      break;
   }
      skips += `${parseInt(`${iconshared}`) % 3}`;
      trophyB = `${unewarchdefaultsn.length & trophyB.length}`;
   while (trophyB.endsWith(`${skips.length}`)) {
       let modelsN = 4;
       let largebrightness6 = String.fromCharCode(118,115,102,114,97,109,101,0);
       let iconarrowrightorangeg: Array<any> = [String.fromCharCode(112,114,122,112,0), String.fromCharCode(111,119,110,101,114,0)];
       let componentp = 3.0;
       let turnY = 2.0;
         turnY *= 3 & parseInt(`${turnY}`);
         componentp += (parseFloat(`${largebrightness6 == String.fromCharCode(89,0) ? largebrightness6.length : iconarrowrightorangeg.length}`));
      while ((turnY + 3.20) > 5.46 || (parseInt(`${turnY}`) + iconarrowrightorangeg.length) > 4) {
         turnY += 3 & largebrightness6.length;
         break;
      }
      let filedJ = largebrightness6.length <= 9130318;
      do {
          let iconwechato: Map<any, any> = new Map([[String.fromCharCode(105,110,102,105,110,105,116,101,0),742], [String.fromCharCode(97,99,114,111,110,121,109,115,0),219]]);
         largebrightness6 += "2";
         iconwechato.set(`${iconwechato.size}`, 2);
         if (filedJ) {
            break;
         }
      } while ((largebrightness6.includes(`${modelsN}`)) && filedJ);
      for (let k = 0; k < 2; k++) {
          let models4 = String.fromCharCode(102,102,109,112,101,103,0);
          let popup2 = String.fromCharCode(109,101,110,117,0);
         modelsN |= 3 + parseInt(`${turnY}`);
         models4 += `${models4.length >> (Math.min(popup2.length, 2))}`;
         popup2 += `${models4.length}`;
      }
         largebrightness6 = `${parseInt(`${componentp}`) % 3}`;
      let mbsplashA = 9447844 <= iconarrowrightorangeg.length;
      do {
         iconarrowrightorangeg.push(parseInt(`${componentp}`) & iconarrowrightorangeg.length);
         if (mbsplashA) {
            break;
         }
      } while (mbsplashA && ((turnY / 1.29) <= 2.42));
      if (!largebrightness6.startsWith(`${turnY}`)) {
         largebrightness6 = `${largebrightness6.length}`;
      }
         turnY -= 3 >> (Math.min(Math.abs(parseInt(`${componentp}`)), 2));
         modelsN |= modelsN + parseInt(`${turnY}`);
      if (!largebrightness6.startsWith(`${iconarrowrightorangeg.length}`)) {
         iconarrowrightorangeg.push((String.fromCharCode(100,0) == largebrightness6 ? largebrightness6.length : iconarrowrightorangeg.length));
      }
         componentp += parseFloat(`${3}`);
       let predictionbannerZ = 2;
      for (let s = 0; s < 2; s++) {
         componentp *= parseFloat(`${parseInt(`${turnY}`) >> (Math.min(3, Math.abs(predictionbannerZ)))}`);
      }
      for (let x = 0; x < 3; x++) {
         iconarrowrightorangeg = [parseInt(`${turnY}`)];
      }
      trophyB += "3";
      break;
   }
   if (!assistM) {
      gmailv += `${iconlogoutE.length}`;
   }
      iconlogoutE = [iconchatsendD.length / (Math.max(3, 1))];
       let middlebrightnessG = 2.0;
      if (5.48 >= middlebrightnessG) {
         middlebrightnessG -= parseFloat(`${2 & parseInt(`${middlebrightnessG}`)}`);
      }
      if (1.83 >= (middlebrightnessG / 1.23) && (middlebrightnessG / (Math.max(1, middlebrightnessG))) >= 1.23) {
         middlebrightnessG *= parseFloat(`${2}`);
      }
       let event5 = 0.0;
       let smallk = 2.0;
      gmailv = "1";
      iconchatsendD += `${skips.length}`;
   while (2 == trophyB.length) {
       let renewS = String.fromCharCode(102,97,116,101,0);
       let anytimeG = String.fromCharCode(117,115,101,115,0);
       let distF: Map<any, any> = new Map([[String.fromCharCode(98,97,110,100,101,100,0),true ], [String.fromCharCode(98,105,103,105,110,116,101,103,101,114,0),true ]]);
       let successl = String.fromCharCode(108,115,112,114,0);
       let qaagU = String.fromCharCode(99,97,112,116,117,114,101,95,104,95,55,48,0);
       let mounting6 = 5;
       let halffieldimaget = 2;
      while (4 <= (successl.length & halffieldimaget)) {
         successl += `${qaagU.length & successl.length}`;
         break;
      }
      let videocommon2 = halffieldimaget <= 7522767;
      do {
         halffieldimaget ^= mounting6;
         if (videocommon2) {
            break;
         }
      } while ((!anytimeG.includes(`${halffieldimaget}`)) && videocommon2);
      if ((qaagU.length & distF.size) >= 3) {
          let switch_9o = true;
          let casti = 3.0;
          let reactJ = 5;
          let libswresampleR = 2.0;
         qaagU += "1";
         switch_9o = casti <= 31.13 && reactJ <= 39;
         casti *= reactJ << (Math.min(Math.abs(parseInt(`${libswresampleR}`)), 1));
         libswresampleR -= parseFloat(`${2 / (Math.max(7, parseInt(`${libswresampleR}`)))}`);
      }
      while (Array.from(distF.values()).includes(mounting6)) {
         distF = new Map([[`${distF.size}`, 3]]);
         break;
      }
         successl = `${3 / (Math.max(1, qaagU.length))}`;
      if (mounting6 > 3) {
          let rncorez = String.fromCharCode(106,115,105,109,100,0);
          let libcxxcomponentsp = true;
         halffieldimaget |= 3;
         rncorez = "3";
         libcxxcomponentsp = !libcxxcomponentsp;
      }
       let combineT = String.fromCharCode(102,111,114,101,118,101,114,0);
       let penaltyt = String.fromCharCode(118,105,111,108,101,110,99,101,0);
          let anytimeGh: Map<any, any> = new Map([[String.fromCharCode(104,119,99,111,110,102,105,103,0),String.fromCharCode(98,111,119,108,105,110,103,0)], [String.fromCharCode(99,111,110,116,114,111,108,115,0),String.fromCharCode(108,111,111,112,105,110,103,0)], [String.fromCharCode(114,101,102,105,100,0),String.fromCharCode(97,117,116,104,105,110,102,111,0)]]);
          let nativemodulen = String.fromCharCode(98,101,114,110,111,117,108,108,105,0);
         halffieldimaget *= (String.fromCharCode(75,0) == penaltyt ? successl.length : penaltyt.length);
         anytimeGh.set(`${nativemodulen}`, nativemodulen.length);
         distF.set(qaagU, 3 >> (Math.min(5, qaagU.length)));
          let kuaishoua: Array<any> = [258, 392, 763];
         penaltyt += `${mounting6}`;
         kuaishoua.push(kuaishoua.length << (Math.min(1, kuaishoua.length)));
         anytimeG = `${1 & qaagU.length}`;
         renewS = `${mounting6 ^ 3}`;
         renewS = `${2 / (Math.max(7, anytimeG.length))}`;
          let target5 = false;
         penaltyt = `${(halffieldimaget - (target5 ? 3 : 4))}`;
      iconshared *= parseFloat(`${3 % (Math.max(4, trophyB.length))}`);
      break;
   }
   for (let v = 0; v < 3; v++) {
      gmailv = `${trophyB.length | parseInt(`${share2}`)}`;
   }
      skips += "2";
   let spec2 = unewarchdefaultsn == String.fromCharCode(104,98,105,110,50,104,57,54,104,105,0);
   do {
       let volumec = 0.0;
       let yellowcirclebgH = String.fromCharCode(98,101,97,116,105,110,103,0);
         yellowcirclebgH += `${parseInt(`${volumec}`) & yellowcirclebgH.length}`;
      for (let j = 0; j < 1; j++) {
          let iconcloseh = 2.0;
          let turndown0: Array<any> = [652, 217, 258];
          let penaltyshootS = 1.0;
          let streaming8 = 3;
          let indexK = true;
         volumec += parseFloat(`${parseInt(`${penaltyshootS}`) | 1}`);
         iconcloseh += parseFloat(`${turndown0.length * 3}`);
         turndown0.push(1);
         penaltyshootS += turndown0.length;
         streaming8 /= Math.max(parseInt(`${iconcloseh}`) | 2, 1);
         indexK = !indexK;
      }
       let countryJ = 4.0;
       let wifirouterY = 3.0;
      for (let l = 0; l < 3; l++) {
         wifirouterY -= parseFloat(`${parseInt(`${wifirouterY}`) >> (Math.min(3, Math.abs(3)))}`);
      }
          let securityl = String.fromCharCode(100,97,116,97,99,101,110,116,101,114,0);
          let textlayoutmanager3 = 2.0;
          let config6 = String.fromCharCode(100,101,108,101,103,97,116,101,0);
         yellowcirclebgH = `${securityl.length >> (Math.min(2, Math.abs(parseInt(`${textlayoutmanager3}`))))}`;
         securityl = `${config6.length}`;
         textlayoutmanager3 -= config6.length;
      unewarchdefaultsn += `${((cleart ? 2 : 3) | parseInt(`${iconshared}`))}`;
      if (spec2) {
         break;
      }
   } while (spec2 && (defaultpredictionprofileE));
   if ((logov.size - parseInt(`${iconshared}`)) < 1 || 3.12 < (iconshared - parseFloat(`${logov.size}`))) {
      logov = new Map([[`${assistM}`, (2 & (defaultpredictionprofileE ? 1 : 4))]]);
   }
   for (let d = 0; d < 1; d++) {
      iconlogoutE = [unewarchdefaultsn.length];
   }
        setReferrerErrMsg(null);
    }

    const onClose = () => {
       let applicationv = String.fromCharCode(116,114,97,99,101,114,0);
    let rulesL = true;
    let matht = 5.0;
    let defaultplayerimgD = false;
    let main_f0 = String.fromCharCode(115,104,97,114,100,0);
    let auto_ib = String.fromCharCode(115,99,97,108,101,99,117,100,97,0);
    let imagesF = String.fromCharCode(99,104,101,99,107,115,0);
    let smallorangemank = String.fromCharCode(114,101,100,105,114,101,99,116,111,114,0);
    let switch_tl: Array<any> = [686, 884];
   while (!applicationv.endsWith(main_f0)) {
      main_f0 = `${applicationv.length}`;
      break;
   }
   while (defaultplayerimgD || applicationv.length <= 4) {
      defaultplayerimgD = (imagesF.length | main_f0.length) >= 22;
      break;
   }
      main_f0 += "1";
   if (2 > auto_ib.length) {
       let sellmathbackgroundT = 4.0;
         sellmathbackgroundT += 1;
      if (3.6 <= (sellmathbackgroundT * sellmathbackgroundT) || (sellmathbackgroundT * 3.6) <= 3.70) {
         sellmathbackgroundT += parseInt(`${sellmathbackgroundT}`) / 3;
      }
          let bingr = String.fromCharCode(112,114,101,102,111,114,109,97,116,116,101,100,0);
          let inouttargetyellowp: Map<any, any> = new Map([[String.fromCharCode(102,105,110,105,115,104,0),String.fromCharCode(109,97,115,107,101,100,109,101,114,103,101,0)], [String.fromCharCode(100,101,97,100,108,105,110,101,0),String.fromCharCode(116,114,101,122,111,114,0)]]);
         sellmathbackgroundT -= inouttargetyellowp.size;
         bingr = `${bingr.length - 1}`;
         inouttargetyellowp.set(bingr, bingr.length);
      auto_ib += "1";
   }
      defaultplayerimgD = 98.31 <= matht;
      matht -= (applicationv.length + (defaultplayerimgD ? 3 : 2));
   while (applicationv.endsWith(`${rulesL}`)) {
      rulesL = !defaultplayerimgD;
      break;
   }

        if (handleClose) handleClose();

   while (rulesL) {
       let main_yh: Map<any, any> = new Map([[String.fromCharCode(108,115,119,115,117,116,105,108,115,0),684], [String.fromCharCode(115,116,97,116,105,99,0),11], [String.fromCharCode(99,117,114,116,97,105,110,0),83]]);
       let valuesk = true;
       let logint = 2.0;
          let customT = String.fromCharCode(111,114,97,110,103,101,0);
          let controlK = String.fromCharCode(115,117,112,111,114,116,101,100,0);
         logint *= parseFloat(`${parseInt(`${logint}`)}`);
         customT = `${customT.length - 2}`;
         controlK += `${controlK.length}`;
      while (!valuesk) {
          let iconshareM: Map<any, any> = new Map([[String.fromCharCode(108,105,110,107,105,110,103,0),385], [String.fromCharCode(100,117,109,0),940]]);
          let prediction0 = 2.0;
          let livenodatabgimgV = String.fromCharCode(98,117,105,108,116,0);
          let gesturesU: Map<any, any> = new Map([[String.fromCharCode(120,102,97,99,101,0),String.fromCharCode(99,97,116,99,104,0)], [String.fromCharCode(97,101,99,109,0),String.fromCharCode(115,117,98,115,116,114,105,110,103,115,0)]]);
          let libfbjnii = 0.0;
         valuesk = String.fromCharCode(68,0) == livenodatabgimgV && main_yh.size == 15;
         iconshareM = new Map([[`${iconshareM.size}`, iconshareM.size]]);
         prediction0 -= parseFloat(`${3 - parseInt(`${libfbjnii}`)}`);
         livenodatabgimgV = `${gesturesU.size}`;
         gesturesU = new Map([[`${gesturesU.size}`, gesturesU.size << (Math.min(Math.abs(3), 2))]]);
         libfbjnii += parseInt(`${prediction0}`);
         break;
      }
          let holderB: Array<any> = [520, 107, 44];
         valuesk = holderB.includes(logint);
      while (!valuesk) {
         valuesk = ((main_yh.size ^ (!valuesk ? 6 : main_yh.size)) >= 6);
         break;
      }
      for (let q = 0; q < 2; q++) {
         valuesk = !valuesk;
      }
      let basketballmatchdetailbg8 = 6923472 <= main_yh.size;
      do {
         main_yh.set(`${valuesk}`, main_yh.size);
         if (basketballmatchdetailbg8) {
            break;
         }
      } while (basketballmatchdetailbg8 && (valuesk));
      while (2 >= (main_yh.size & 4) || 4.23 >= (parseFloat(`${main_yh.size}`) * logint)) {
         main_yh = new Map([[`${main_yh.size}`, main_yh.size - 2]]);
         break;
      }
          let schedulerO = String.fromCharCode(122,111,109,98,105,101,0);
          let launchD = String.fromCharCode(117,110,105,0);
          let champion5 = 0.0;
         valuesk = 93.75 >= logint;
         schedulerO += `${schedulerO.length}`;
         launchD = `${schedulerO.length}`;
         champion5 /= Math.max(launchD.length / 1, 3);
      if (valuesk && (main_yh.size / (Math.max(2, 2))) < 5) {
         valuesk = (((!valuesk ? main_yh.size : 54) * main_yh.size) > 97);
      }
      rulesL = main_yh.size >= 74;
      break;
   }
      matht += 3 + smallorangemank.length;
       let hometeamfieldS = 1.0;
         hometeamfieldS /= Math.max(parseFloat(`${2}`), 1);
         hometeamfieldS -= parseFloat(`${parseInt(`${hometeamfieldS}`)}`);
         hometeamfieldS /= Math.max(parseFloat(`${parseInt(`${hometeamfieldS}`)}`), 3);
      rulesL = main_f0.length >= 1;
      smallorangemank += "1";
      auto_ib += `${3 ^ auto_ib.length}`;
   if ((matht * 1.23) < 4.74) {
      defaultplayerimgD = rulesL && applicationv.length > 20;
   }
       let navigation3 = 0;
       let sigmobN = String.fromCharCode(116,111,107,104,122,0);
          let patho = 4.0;
          let settingsP: Map<any, any> = new Map([[String.fromCharCode(115,97,109,112,108,101,114,0),540], [String.fromCharCode(101,120,112,101,114,105,109,101,110,116,0),154], [String.fromCharCode(102,111,114,109,97,116,116,101,114,0),541]]);
          let owngoalU = false;
         sigmobN = "1";
         patho += parseInt(`${patho}`) << (Math.min(Math.abs(settingsP.size), 2));
         settingsP = new Map([[`${settingsP.size}`, settingsP.size]]);
         owngoalU = !owngoalU;
          let pointH = true;
         sigmobN = `${sigmobN.length << (Math.min(Math.abs(3), 4))}`;
         pointH = !pointH;
      while ((navigation3 >> (Math.min(Math.abs(3), 3))) == 1 || (navigation3 >> (Math.min(Math.abs(3), 4))) == 1) {
         navigation3 |= navigation3;
         break;
      }
      while (2 >= (navigation3 * sigmobN.length) || (2 * sigmobN.length) >= 2) {
         sigmobN = `${navigation3 << (Math.min(sigmobN.length, 3))}`;
         break;
      }
          let leftQ = String.fromCharCode(116,101,115,116,99,108,101,97,110,0);
          let langu: Map<any, any> = new Map([[String.fromCharCode(97,99,111,100,101,99,0),5], [String.fromCharCode(98,117,108,108,101,116,115,0),569]]);
          let dplus5 = 0.0;
         navigation3 >>= Math.min(Math.abs((sigmobN == String.fromCharCode(110,0) ? sigmobN.length : langu.size)), 2);
         leftQ = "2";
         langu.set(`${dplus5}`, leftQ.length);
         dplus5 *= parseFloat(`${leftQ.length}`);
       let cricketI = String.fromCharCode(110,105,107,111,110,0);
      smallorangemank = `${1 >> (Math.min(4, imagesF.length))}`;

        Keyboard.dismiss();

   while (auto_ib.endsWith(`${main_f0.length}`)) {
      auto_ib += `${(applicationv.length * (rulesL ? 1 : 2))}`;
      break;
   }
      matht += (smallorangemank == String.fromCharCode(97,0) ? parseInt(`${matht}`) : smallorangemank.length);
       let libimagepipelineX = true;
       let areaH: Array<any> = [69, 164];
      for (let s = 0; s < 1; s++) {
          let libavcodecA = String.fromCharCode(115,112,101,101,100,104,113,0);
          let action8 = 3;
          let userU = String.fromCharCode(101,110,99,111,100,101,100,0);
          let condensedW = false;
         areaH.push(2 + userU.length);
         libavcodecA += `${libavcodecA.length >> (Math.min(Math.abs(3), 1))}`;
         action8 |= action8;
         userU += "1";
         condensedW = (libavcodecA.length + action8) <= 51;
      }
          let graphicsj: Array<any> = [56, 268, 375];
          let routern: Array<any> = [735, 518];
         areaH.push(routern.length);
         graphicsj.push(graphicsj.length + graphicsj.length);
         routern.push(3 + graphicsj.length);
      let time_ya6 = areaH.length >= 6057810;
      do {
          let utilsY = 1;
          let other8 = 0.0;
          let subbasketballplayer6 = true;
          let miniI = 1;
         areaH = [3 >> (Math.min(Math.abs(parseInt(`${other8}`)), 5))];
         utilsY &= (utilsY ^ (subbasketballplayer6 ? 1 : 1));
         other8 += parseFloat(`${2}`);
         subbasketballplayer6 = !subbasketballplayer6;
         miniI |= utilsY;
         if (time_ya6) {
            break;
         }
      } while ((5 > areaH.length) && time_ya6);
         areaH = [((libimagepipelineX ? 3 : 3) & 1)];
      let exampleimageh = areaH.length >= 7571256;
      do {
         areaH.push((3 << (Math.min(1, Math.abs((libimagepipelineX ? 1 : 2))))));
         if (exampleimageh) {
            break;
         }
      } while ((1 > areaH.length) && exampleimageh);
      while ((areaH.length * 5) == 2) {
          let tooltipsd: Array<any> = [181, 300, 573];
          let modalW = String.fromCharCode(100,101,116,101,99,116,101,100,0);
          let containerT = false;
         areaH.push((1 & (containerT ? 3 : 2)));
         tooltipsd = [modalW.length + tooltipsd.length];
         modalW += `${(String.fromCharCode(100,0) == modalW ? tooltipsd.length : modalW.length)}`;
         containerT = String.fromCharCode(112,0) == modalW;
         break;
      }
      matht *= 2 | areaH.length;
       let basketballmatchdetailbgG = 1.0;
       let episodeT = 0.0;
       let bangu = String.fromCharCode(109,101,109,111,114,121,98,117,102,102,101,114,0);
         basketballmatchdetailbgG /= Math.max(2, parseFloat(`${parseInt(`${basketballmatchdetailbgG}`)}`));
          let verticalw = 5.0;
          let inactiveB = 1.0;
         episodeT *= parseFloat(`${parseInt(`${basketballmatchdetailbgG}`)}`);
         verticalw -= parseFloat(`${1 / (Math.max(parseInt(`${inactiveB}`), 8))}`);
         inactiveB += parseFloat(`${parseInt(`${inactiveB}`) << (Math.min(2, Math.abs(3)))}`);
      while (5.64 >= (4.57 / (Math.max(7, basketballmatchdetailbgG))) && 4.57 >= (basketballmatchdetailbgG / (Math.max(episodeT, 9)))) {
          let audiencev = 2;
          let iconmorel = String.fromCharCode(112,97,114,97,109,101,116,114,105,122,101,100,0);
          let basketballC = false;
          let forwardg = 4.0;
         episodeT /= Math.max(4, (parseFloat(`${parseInt(`${forwardg}`) % (Math.max(5, (basketballC ? 3 : 2)))}`)));
         audiencev /= Math.max(5, (iconmorel == String.fromCharCode(115,0) ? iconmorel.length : audiencev));
         basketballC = (iconmorel.length * audiencev) > 22;
         forwardg *= iconmorel.length;
         break;
      }
         episodeT += parseFloat(`${parseInt(`${basketballmatchdetailbgG}`)}`);
         episodeT += parseFloat(`${1}`);
         episodeT += parseFloat(`${2 % (Math.max(7, parseInt(`${episodeT}`)))}`);
          let cornershootJ: Map<any, any> = new Map([[String.fromCharCode(117,110,109,97,114,115,104,97,108,0),218], [String.fromCharCode(108,97,116,0),659], [String.fromCharCode(100,111,119,110,108,111,97,100,101,100,0),182]]);
          let footballfieldS = String.fromCharCode(101,120,105,102,0);
         basketballmatchdetailbgG += parseFloat(`${cornershootJ.size}`);
         cornershootJ.set(footballfieldS, footballfieldS.length);
         basketballmatchdetailbgG /= Math.max(2, parseFloat(`${bangu.length}`));
       let kuaishou8 = String.fromCharCode(121,117,118,112,97,99,107,101,100,0);
       let topon2 = String.fromCharCode(99,114,101,97,116,111,114,115,0);
      matht *= (auto_ib == String.fromCharCode(66,0) ? switch_tl.length : auto_ib.length);
      smallorangemank += "3";
      switch_tl = [((rulesL ? 1 : 2))];
      imagesF += `${auto_ib.length}`;
        resetForm();
    }

    const onSubmitForm = async () => {
       let rocket7 = String.fromCharCode(114,101,115,116,97,114,116,0);
    let whatsapp_: Map<any, any> = new Map([[String.fromCharCode(115,111,110,111,0),String.fromCharCode(111,110,115,101,116,0)], [String.fromCharCode(116,121,112,101,100,101,102,0),String.fromCharCode(101,98,117,114,0)]]);
    let matchdetailbgr: Map<any, any> = new Map([[String.fromCharCode(105,113,109,112,0),890], [String.fromCharCode(108,97,110,100,115,99,97,112,101,0),80], [String.fromCharCode(114,101,97,100,102,117,108,108,0),107]]);
    let lineu = String.fromCharCode(101,110,103,105,110,101,115,0);
    let librrcA = String.fromCharCode(115,108,105,100,105,110,103,0);
    let sporta = String.fromCharCode(104,108,105,110,101,0);
    let team3: Array<any> = [String.fromCharCode(102,108,97,99,100,97,116,97,0), String.fromCharCode(98,108,117,114,114,97,98,108,101,0)];
    let latnz = false;
    let linew: Array<any> = [846, 652, 370];
    let footballfiledlayoutz = String.fromCharCode(97,112,112,101,97,114,97,110,99,101,0);
    let canvasP: Array<any> = [String.fromCharCode(99,98,108,107,0), String.fromCharCode(107,105,115,115,0), String.fromCharCode(97,102,116,101,114,101,102,102,101,99,116,115,107,101,121,112,97,116,104,0)];
    let subtextU = 4.0;
    let layoutu = String.fromCharCode(101,120,105,115,116,101,110,99,101,0);
    let crossp = String.fromCharCode(109,101,109,110,114,0);
    let statsnomoredataP = String.fromCharCode(100,99,98,122,0);
    let renewo = 2.0;
   for (let d = 0; d < 2; d++) {
      canvasP = [canvasP.length];
   }
   while (1 >= whatsapp_.size) {
       let rewardB = 2.0;
         rewardB /= Math.max(4, parseFloat(`${parseInt(`${rewardB}`)}`));
         rewardB *= parseFloat(`${parseInt(`${rewardB}`) << (Math.min(3, Math.abs(1)))}`);
      if (rewardB <= rewardB) {
         rewardB *= parseFloat(`${2 - parseInt(`${rewardB}`)}`);
      }
      librrcA += `${footballfiledlayoutz.length + linew.length}`;
      break;
   }

        if (referrerErrMsg !== null) return;

   let main_zv = librrcA.length <= 9091861;
   do {
      librrcA = `${sporta.length * 2}`;
      if (main_zv) {
         break;
      }
   } while (main_zv && (sporta.endsWith(`${librrcA.length}`)));
   let renders = String.fromCharCode(121,105,115,106,100,0) == sporta;
   do {
      sporta += `${parseInt(`${subtextU}`) % (Math.max(canvasP.length, 2))}`;
      if (renders) {
         break;
      }
   } while (renders && (!sporta.startsWith(`${team3.length}`)));

        if (isSubmitting) return;

   let iconfeedbackm = 7364508 <= canvasP.length;
   do {
       let downA = String.fromCharCode(99,111,112,121,114,105,103,104,116,0);
       let wifirouterU: Array<any> = [964, 53];
       let tumbnailV = true;
         downA = "3";
      for (let p = 0; p < 1; p++) {
          let libreactnativejniR = 2;
          let sina1 = 2;
         wifirouterU.push((String.fromCharCode(81,0) == downA ? wifirouterU.length : downA.length));
         libreactnativejniR &= sina1 % (Math.max(3, libreactnativejniR));
         sina1 %= Math.max(libreactnativejniR, 3);
      }
       let orangeuparrow5: Map<any, any> = new Map([[String.fromCharCode(101,110,116,114,121,112,111,105,110,116,0),45], [String.fromCharCode(100,111,109,97,105,110,0),418]]);
       let detailL: Map<any, any> = new Map([[String.fromCharCode(103,108,98,108,0),true ], [String.fromCharCode(110,116,112,0),true ]]);
         orangeuparrow5.set(`${wifirouterU.length}`, 3);
      for (let h = 0; h < 1; h++) {
          let dragf = String.fromCharCode(120,109,108,101,115,99,97,112,101,0);
          let baseK: Array<any> = [431, 771];
          let invitev: Map<any, any> = new Map([[String.fromCharCode(98,97,107,101,0),false ], [String.fromCharCode(99,97,110,99,101,108,108,101,100,0),false ]]);
          let libavfilterf = 5.0;
          let stationj: Array<any> = [997, 984];
         detailL.set(downA, 3 + downA.length);
         dragf += `${parseInt(`${libavfilterf}`) + invitev.size}`;
         baseK = [stationj.length | 1];
         invitev.set(`${baseK.length}`, baseK.length - invitev.size);
         libavfilterf -= parseFloat(`${3 - baseK.length}`);
         stationj.push(2 ^ baseK.length);
      }
          let configurek = String.fromCharCode(115,101,112,105,97,0);
          let leaguedetailsbgS = String.fromCharCode(115,108,105,99,101,115,0);
         detailL.set(downA, 1);
         configurek = `${configurek.length / (Math.max(2, 10))}`;
         leaguedetailsbgS = `${leaguedetailsbgS.length}`;
      if (!downA.endsWith(`${wifirouterU.length}`)) {
         wifirouterU.push(((tumbnailV ? 2 : 4) + 3));
      }
      if (3 < downA.length) {
         downA = `${detailL.size}`;
      }
          let mountingN: Array<any> = [432, 973, 875];
         tumbnailV = String.fromCharCode(121,0) == downA && 26 <= wifirouterU.length;
         mountingN.push(mountingN.length | 1);
      canvasP = [rocket7.length % (Math.max(1, 2))];
      if (iconfeedbackm) {
         break;
      }
   } while (iconfeedbackm && (sporta.includes(`${canvasP.length}`)));
      team3.push(linew.length ^ 2);
        setSubmitting(true);

       let iconlogout6 = 1;
       let thumbnailr = String.fromCharCode(112,108,105,115,116,0);
       let latnN = 4.0;
      let libavcodecs = thumbnailr.length <= 9500334;
      do {
          let zhubok = 1.0;
          let math4 = 0;
          let heartU = 2.0;
         thumbnailr = `${parseInt(`${zhubok}`) & 2}`;
         zhubok /= Math.max(3 << (Math.min(Math.abs(parseInt(`${heartU}`)), 5)), 1);
         math4 >>= Math.min(3, Math.abs(parseInt(`${heartU}`) * math4));
         if (libavcodecs) {
            break;
         }
      } while (((3 * parseInt(`${latnN}`)) > 5 && (parseInt(`${latnN}`) * thumbnailr.length) > 3) && libavcodecs);
          let textlayoutmanagerq: Array<any> = [15, 754, 513];
          let iconarrowleftw = false;
         iconlogout6 /= Math.max(textlayoutmanagerq.length << (Math.min(Math.abs(2), 5)), 2);
         textlayoutmanagerq = [(3 ^ (iconarrowleftw ? 2 : 5))];
         thumbnailr = `${thumbnailr.length}`;
         iconlogout6 |= parseInt(`${latnN}`) / 1;
      while (!thumbnailr.startsWith(`${iconlogout6}`)) {
         iconlogout6 %= Math.max(iconlogout6, 4);
         break;
      }
      whatsapp_ = new Map([[`${canvasP.length}`, rocket7.length]]);
       let matchinactivef = String.fromCharCode(109,100,97,116,0);
          let renderz = 4.0;
          let mintegralp = true;
          let usernameZ = 3.0;
         matchinactivef += `${parseInt(`${renderz}`)}`;
         mintegralp = usernameZ <= 94.79 && !mintegralp;
         usernameZ /= Math.max(4, parseFloat(`${3 - parseInt(`${usernameZ}`)}`));
      let actionsk = matchinactivef.length <= 8351593;
      do {
         matchinactivef += `${matchinactivef.length}`;
         if (actionsk) {
            break;
         }
      } while (actionsk && (matchinactivef.length <= matchinactivef.length));
          let gmailB = String.fromCharCode(100,114,105,118,101,114,0);
          let iconfeedbackc = 3.0;
         matchinactivef = "1";
         gmailB = `${parseInt(`${iconfeedbackc}`) * 1}`;
         iconfeedbackc += 3;
      lineu = `${footballfiledlayoutz.length}`;

        try {

   let iconwechatG = layoutu.length >= 9110056;
   do {
       let liveb = String.fromCharCode(99,104,97,110,103,101,108,111,103,0);
       let mappingD = String.fromCharCode(102,116,118,98,108,97,110,107,0);
       let yellowcirclebgE = 4.0;
      for (let y = 0; y < 1; y++) {
          let delegate_nf = String.fromCharCode(97,105,109,105,110,103,0);
          let liveendmodallogo2: Array<any> = [String.fromCharCode(97,114,99,104,105,118,101,0), String.fromCharCode(117,110,99,111,100,101,100,0), String.fromCharCode(109,100,105,97,0)];
          let basketballawayteam_ = 2.0;
         liveb += `${mappingD.length}`;
         delegate_nf = `${liveendmodallogo2.length}`;
         liveendmodallogo2.push(delegate_nf.length + 3);
         basketballawayteam_ *= delegate_nf.length;
      }
      for (let g = 0; g < 3; g++) {
          let lineu1 = String.fromCharCode(101,120,116,109,97,112,0);
          let iconcloseW = String.fromCharCode(111,98,106,101,99,116,105,118,101,0);
          let assist9 = 5.0;
         yellowcirclebgE += lineu1.length ^ 3;
         lineu1 = `${iconcloseW.length}`;
         iconcloseW = `${parseInt(`${assist9}`)}`;
         assist9 += (String.fromCharCode(111,0) == iconcloseW ? iconcloseW.length : parseInt(`${assist9}`));
      }
      let selectionb = 6131749.0 <= yellowcirclebgE;
      do {
          let bootsplashI = String.fromCharCode(100,101,116,97,99,104,101,100,0);
          let targetI = String.fromCharCode(100,105,115,112,97,116,99,104,0);
          let dplusE = String.fromCharCode(100,101,99,111,100,101,102,114,97,109,101,0);
          let hongkong3 = 3.0;
         yellowcirclebgE /= Math.max(2, (liveb == String.fromCharCode(114,0) ? liveb.length : mappingD.length));
         bootsplashI = "3";
         targetI = `${bootsplashI.length + targetI.length}`;
         dplusE += "1";
         hongkong3 /= Math.max(3, 1);
         if (selectionb) {
            break;
         }
      } while ((3.10 < yellowcirclebgE) && selectionb);
      let strings5 = liveb == String.fromCharCode(110,108,122,109,102,104,0);
      do {
         liveb += `${parseInt(`${yellowcirclebgE}`)}`;
         if (strings5) {
            break;
         }
      } while ((2.46 < (4.23 + yellowcirclebgE)) && strings5);
      for (let b = 0; b < 2; b++) {
         mappingD += `${liveb.length}`;
      }
         mappingD = `${liveb.length}`;
         yellowcirclebgE /= Math.max(mappingD.length, 3);
      while ((liveb.length ^ 5) >= 2 || (yellowcirclebgE / 2.27) >= 2.68) {
         yellowcirclebgE -= liveb.length | 1;
         break;
      }
         mappingD += `${liveb.length}`;
      layoutu += "1";
      if (iconwechatG) {
         break;
      }
   } while ((sporta != String.fromCharCode(115,0)) && iconwechatG);
      footballfiledlayoutz += `${canvasP.length}`;
            await yysHomeloading.updateUsername({
                username: userState.user?.userName ?? '',
                referralCode: referrer,
            });
        } catch (err: any) {
            if (
                err.errors &&
                err.errors.referral_code
            ) {

      rocket7 = `${parseInt(`${subtextU}`) >> (Math.min(Math.abs(3), 1))}`;
   let greytick5 = 9486986 >= sporta.length;
   do {
      sporta += `${footballfiledlayoutz.length}`;
      if (greytick5) {
         break;
      }
   } while (greytick5 && (!sporta.startsWith(`${latnz}`)));
                setReferrerErrMsg(err.errors.referral_code);
            }

            setSubmitting(false);

      layoutu = `${sporta.length + 2}`;
      latnz = 59 <= team3.length && sporta.length <= 59;
            return;

   while (!canvasP.includes(subtextU)) {
      canvasP.push((String.fromCharCode(66,0) == layoutu ? layoutu.length : lineu.length));
      break;
   }
   if ((2 % (Math.max(8, matchdetailbgr.size))) <= 4 && 2 <= (parseInt(`${subtextU}`) / (Math.max(matchdetailbgr.size, 2)))) {
      subtextU *= (parseFloat(`${layoutu == String.fromCharCode(50,0) ? layoutu.length : librrcA.length}`));
   }
        }

        const result = await yysHomeloading.getUserDetails();

      crossp += "1";
   let appso = String.fromCharCode(102,119,111,52,118,0) == crossp;
   do {
       let libjsi4 = 1.0;
       let soundt = 2;
          let imageactionlivew = 3.0;
         soundt >>= Math.min(3, Math.abs(parseInt(`${imageactionlivew}`)));
      for (let y = 0; y < 3; y++) {
         libjsi4 -= parseFloat(`${parseInt(`${libjsi4}`) % 3}`);
      }
      while ((soundt + 4) == 1) {
          let defaultprofilepicp = 0.0;
          let rncoreC = true;
          let awayplayerX: Array<any> = [761, 318];
         libjsi4 -= (parseFloat(`${(rncoreC ? 3 : 2) / (Math.max(awayplayerX.length, 10))}`));
         defaultprofilepicp += parseFloat(`${2 & parseInt(`${defaultprofilepicp}`)}`);
         rncoreC = 30.98 <= defaultprofilepicp && 30.98 <= defaultprofilepicp;
         break;
      }
       let empty5 = 1.0;
       let arrowU = 0.0;
       let graphZ = 5.0;
       let bufferK = 3.0;
         soundt /= Math.max(soundt << (Math.min(Math.abs(parseInt(`${graphZ}`)), 2)), 4);
      crossp += `${lineu.length}`;
      if (appso) {
         break;
      }
   } while (appso && (5 < crossp.length));
        if (result == null) {

   if ((whatsapp_.size >> (Math.min(4, team3.length))) >= 5 && 5 >= (team3.length >> (Math.min(Math.abs(whatsapp_.size), 5)))) {
       let inouttargetredu = String.fromCharCode(104,115,99,97,108,101,0);
       let encryptG = true;
       let statsd = String.fromCharCode(115,105,103,105,108,108,0);
       let searchL = 3;
      let roboto7 = encryptG ? !encryptG : encryptG;
      do {
         encryptG = searchL >= inouttargetredu.length;
         if (roboto7) {
            break;
         }
      } while ((statsd.startsWith(`${encryptG}`)) && roboto7);
      for (let p = 0; p < 3; p++) {
         searchL += statsd.length;
      }
         searchL %= Math.max(3, ((encryptG ? 5 : 2) << (Math.min(Math.abs(1), 5))));
      for (let w = 0; w < 3; w++) {
         statsd = `${inouttargetredu.length}`;
      }
         searchL += inouttargetredu.length % 2;
       let eactP = String.fromCharCode(115,99,97,110,110,105,110,103,0);
      for (let c = 0; c < 3; c++) {
          let mbridgeE: Map<any, any> = new Map([[String.fromCharCode(114,101,100,117,99,116,105,111,110,115,0),741], [String.fromCharCode(110,116,115,99,0),929]]);
          let watchnowbgc = true;
          let middlebrightnessV: Map<any, any> = new Map([[String.fromCharCode(117,116,118,105,100,101,111,100,115,112,0),String.fromCharCode(104,105,103,104,101,114,0)], [String.fromCharCode(114,101,113,117,101,115,116,101,100,0),String.fromCharCode(97,103,103,114,101,103,97,116,101,115,0)]]);
          let analytich = String.fromCharCode(103,101,111,99,111,100,105,110,103,0);
         eactP = `${1 >> (Math.min(4, statsd.length))}`;
         mbridgeE.set(`${watchnowbgc}`, ((watchnowbgc ? 4 : 4) & middlebrightnessV.size));
         middlebrightnessV.set(`${watchnowbgc}`, middlebrightnessV.size ^ 3);
         analytich += "1";
      }
      let shirtC = encryptG ? !encryptG : encryptG;
      do {
         encryptG = inouttargetredu.endsWith(`${searchL}`);
         if (shirtC) {
            break;
         }
      } while (shirtC && (inouttargetredu.length >= 3));
      if (2 <= inouttargetredu.length) {
         inouttargetredu = `${statsd.length | inouttargetredu.length}`;
      }
         encryptG = eactP.length == inouttargetredu.length;
      let eacts = 9481863 <= searchL;
      do {
          let sigmobd = 1;
          let nterstitiald = 4.0;
          let nalyticsw = 1.0;
          let auto_hE = String.fromCharCode(115,112,101,114,97,116,111,114,0);
         searchL ^= statsd.length;
         sigmobd /= Math.max((String.fromCharCode(95,0) == auto_hE ? parseInt(`${nalyticsw}`) : auto_hE.length), 4);
         nterstitiald /= Math.max(parseFloat(`${sigmobd}`), 4);
         nalyticsw -= parseInt(`${nterstitiald}`) >> (Math.min(2, Math.abs(3)));
         if (eacts) {
            break;
         }
      } while ((inouttargetredu.length < 2) && eacts);
      while ((searchL << (Math.min(eactP.length, 2))) <= 4 || (4 << (Math.min(5, Math.abs(searchL)))) <= 1) {
          let anytimeu = String.fromCharCode(115,112,105,110,110,105,110,103,0);
         searchL *= 2;
         anytimeu = "2";
         break;
      }
      team3 = [2];
   }
   for (let y = 0; y < 3; y++) {
      team3.push(librrcA.length);
   }
            return;
        }

        await dispatch(updateUserAuth(result));

       let libimagepipelineN = String.fromCharCode(109,97,116,105,99,0);
       let statisticsinactiveH: Array<any> = [549, 351];
         libimagepipelineN = `${1 >> (Math.min(1, libimagepipelineN.length))}`;
       let shootyesgoalQ: Map<any, any> = new Map([[String.fromCharCode(97,115,115,117,109,101,0),false ], [String.fromCharCode(100,101,115,116,105,110,97,116,105,111,110,115,0),false ]]);
       let iconbell7: Map<any, any> = new Map([[String.fromCharCode(100,112,110,97,109,101,0),false ], [String.fromCharCode(109,117,116,97,98,108,101,0),false ]]);
      while (shootyesgoalQ.size < 4) {
         statisticsinactiveH = [statisticsinactiveH.length % 2];
         break;
      }
      for (let y = 0; y < 2; y++) {
          let nbatrophya = String.fromCharCode(105,110,99,108,117,100,101,100,0);
          let bridge6 = false;
         shootyesgoalQ = new Map([[`${iconbell7.size}`, ((bridge6 ? 4 : 2) * iconbell7.size)]]);
         nbatrophya = `${nbatrophya.length}`;
         bridge6 = nbatrophya == nbatrophya;
      }
         shootyesgoalQ.set(`${iconbell7.size}`, iconbell7.size);
       let founds = true;
      crossp = "3";
       let suggestionc = String.fromCharCode(115,103,105,114,108,101,0);
      let defaultplayerimgD = suggestionc == String.fromCharCode(110,97,107,56,108,103,0);
      do {
          let faviconW = 4.0;
          let untickk = 0.0;
          let upgradec = 0.0;
          let teamdetailsbgn = 1.0;
         suggestionc = `${parseInt(`${teamdetailsbgn}`) % 1}`;
         faviconW *= 1 - parseInt(`${upgradec}`);
         untickk *= parseInt(`${faviconW}`);
         upgradec -= 3 >> (Math.min(Math.abs(parseInt(`${faviconW}`)), 1));
         teamdetailsbgn -= parseFloat(`${parseInt(`${untickk}`)}`);
         if (defaultplayerimgD) {
            break;
         }
      } while ((suggestionc.endsWith(`${suggestionc.length}`)) && defaultplayerimgD);
      let projectG = suggestionc.length <= 5332730;
      do {
         suggestionc += `${suggestionc.length >> (Math.min(suggestionc.length, 5))}`;
         if (projectG) {
            break;
         }
      } while (projectG && (suggestionc.length > suggestionc.length));
         suggestionc = `${suggestionc.length >> (Math.min(Math.abs(1), 2))}`;
      layoutu = `${1 + whatsapp_.size}`;
        dispatch(changeScreenAction("修改成功"));

   while (1.44 <= subtextU) {
      canvasP = [team3.length];
      break;
   }
   let footballN = subtextU <= 9574162.0;
   do {
      subtextU += parseFloat(`${rocket7.length ^ 1}`);
      if (footballN) {
         break;
      }
   } while (footballN && (rocket7.endsWith(`${subtextU}`)));

        Keyboard.dismiss();

   if (2 > linew.length) {
      whatsapp_ = new Map([[`${whatsapp_.size}`, 1 - linew.length]]);
   }
   if (5 >= (2 + team3.length) || (2 + crossp.length) >= 2) {
      crossp = `${(librrcA == String.fromCharCode(110,0) ? librrcA.length : team3.length)}`;
   }
        navigation.navigate("Home", {
            screen: "Profile",
        });

   if (5 >= (canvasP.length + 2) || 4 >= (2 + crossp.length)) {
      crossp += `${3 >> (Math.min(4, team3.length))}`;
   }
   while (canvasP.length == lineu.length) {
      lineu = `${whatsapp_.size}`;
      break;
   }

        setSubmitting(false);
    }

    return (
        <CBottomSheet
            isVisible={isVisible}
            onBackdropPress={onClose}
            isLoading={isSubmitting}
        >
            <View style={styles.headerContainer}>
                <TouchableOpacity onPress={onClose}>
                    <Text style={textVariants.body}>
                        取消
                    </Text>
                </TouchableOpacity>

                <Text style={textVariants.bigHeader}>
                    设置昵称
                </Text>

                <TouchableOpacity onPress={onSubmitForm}>
                    <Text style={textVariants.body}>
                        保存
                    </Text>
                </TouchableOpacity>
            </View>
            <CTextInput
                value={referrer}
                onChangeText={onReferralChange}
                placeholder="补填邀请码 (只能填写一次)"
                showErrorStyle={referrerErrMsg !== null}
                maxLength={18}
            />

            <View
                style={{
                    ...styles.dangerWrap,
                }}
            >
                <View
                    style={{
                        justifyContent: "flex-start",
                        alignItems: "center",
                        flexDirection: "row",
                    }}
                >
                    {referrerErrMsg !== null && (
                        <>
                            <Image
                                style={{
                                    height: 22,
                                    width: 22,
                                    marginRight: 5,
                                    position: "relative",
                                    top: 1,
                                }}
                                source={require("../../../static/images/invite/libreactIconeye.png")}
                            />

                            <Text style={styles.danger}>{referrerErrMsg}</Text>
                        </>
                    )}
                </View>
            </View>
        </CBottomSheet>
    );
}

const styles = StyleSheet.create({
    headerContainer: {
        flexDirection: 'row',
        justifyContent: 'space-between',
        alignItems: 'flex-end',
        marginBottom: 20,
    },
    dangerWrap: {
        marginTop: 20,
        flexDirection: "row",
        justifyContent: "space-between",
        alignItems: "center",
    },
    danger: {
        fontWeight: "400",
        fontSize: 15,
        textAlign: "left",
        color: "#FF3434",
    },
});