import { useNavigation, useTheme } from "@react-navigation/native";
import { useCallback, useContext, useEffect } from "react";
import { BackHandler, StyleSheet, TouchableOpacity, View } from "react-native";
import { Text } from "react-native";
import RNExitApp from "react-native-exit-app";
import { useAppDispatch } from "@hooks/kg_index";
import { acceptPrivacyPolicy } from "@redux/actions/h_nalytics_typing";
import { TermsAcceptContext } from "../../contexts/fev_injury_sell";
import { IS_OTHER_SKIN } from "@utility/n_subs_interstitial";


interface XFillButton {
  isVisible: boolean,
  setIsVisible: any,
}

export default function PrivacyPolicyOverlay({
  isVisible,
  setIsVisible,
}: XFillButton) {
  const { accepted, setAccepted } = useContext(TermsAcceptContext);
  const { spacing, textVariants, colors } = useTheme();
  const dispatch = useAppDispatch();

  const navigation = useNavigation();

  useEffect(() => {
    setIsVisible(!accepted);
  }, []);

  const handleReject = useCallback(() => {
       let rankz = true;
    let castingo: Map<any, any> = new Map([[String.fromCharCode(112,115,100,115,112,95,105,95,57,0),571], [String.fromCharCode(112,108,97,110,97,114,120,95,110,95,49,56,0),134], [String.fromCharCode(114,101,103,105,115,116,101,114,102,100,115,95,117,95,57,53,0),208]]);
    let actionse: Array<any> = [String.fromCharCode(97,117,116,111,109,97,116,105,99,97,108,108,121,95,119,95,54,0), String.fromCharCode(115,99,104,109,95,51,95,50,57,0)];
    let const_yc = 1.0;
    let useri: Array<any> = [355, 240, 552];
    let championC = String.fromCharCode(112,114,111,99,101,115,115,101,100,95,116,95,52,57,0);
    let injuryV = String.fromCharCode(115,105,103,118,101,114,95,111,95,52,53,0);
    let brightness0 = 3.0;
    let trophyU = 2;
    let analyticsv: Map<any, any> = new Map([[String.fromCharCode(112,116,114,95,52,95,53,51,0),String.fromCharCode(116,114,97,118,101,114,115,97,108,95,112,95,49,52,0)], [String.fromCharCode(110,116,115,115,95,56,95,49,49,0),String.fromCharCode(107,95,49,52,95,115,117,98,99,99,0)]]);
    let backwardf = 4.0;
    let pressureR = String.fromCharCode(112,111,108,108,101,114,95,116,95,56,48,0);
    let starU = true;
    let dark2 = String.fromCharCode(110,95,57,48,95,116,120,116,0);
    let modeR = false;
      rankz = useri.includes(rankz);
   let backgroundI = 7804662.0 <= backwardf;
   do {
      backwardf *= parseInt(`${brightness0}`) % 2;
      if (backgroundI) {
         break;
      }
   } while (backgroundI && (1.31 < backwardf));
   if (4 == (useri.length << (Math.min(Math.abs(castingo.size), 4)))) {
       let albumM = false;
       let inviteq: Array<any> = [String.fromCharCode(115,95,51,95,98,121,116,101,119,111,114,100,0), String.fromCharCode(117,95,53,95,100,105,102,102,101,114,101,110,99,101,115,0)];
       let viewsW = String.fromCharCode(110,95,56,56,95,102,108,117,115,104,0);
       let aboutB = true;
       let playn: Map<any, any> = new Map([[String.fromCharCode(110,95,51,53,95,106,117,108,105,97,110,100,97,121,0),false ], [String.fromCharCode(99,104,111,115,101,95,97,95,49,51,0),true ]]);
       let suggestiont: Map<any, any> = new Map([[String.fromCharCode(100,108,116,97,95,108,95,50,50,0),70], [String.fromCharCode(108,95,53,53,95,99,111,108,115,0),891], [String.fromCharCode(104,95,54,54,0),451]]);
         playn = new Map([[`${inviteq.length}`, inviteq.length]]);
      let termsL = 9465178 >= playn.size;
      do {
          let firebaseu: Array<any> = [757, 908];
          let types5 = String.fromCharCode(118,105,100,101,111,105,110,102,111,104,101,97,100,101,114,95,108,95,51,49,0);
          let hooksy = 5.0;
         playn.set(`${albumM}`, 3);
         firebaseu.push(2);
         types5 = `${parseInt(`${hooksy}`) - firebaseu.length}`;
         hooksy -= types5.length + parseInt(`${hooksy}`);
         if (termsL) {
            break;
         }
      } while (termsL && ((playn.size | 3) <= 1));
      let stepk = aboutB ? !aboutB : aboutB;
      do {
         aboutB = !viewsW.includes(`${aboutB}`);
         if (stepk) {
            break;
         }
      } while (stepk && (3 > (3 - playn.size) && 3 > playn.size));
      while (aboutB) {
         playn = new Map([[`${suggestiont.size}`, 3]]);
         break;
      }
      while (1 >= (playn.size % (Math.max(3, suggestiont.size))) || 5 >= (playn.size % 1)) {
         playn.set(`${aboutB}`, suggestiont.size);
         break;
      }
      for (let s = 0; s < 2; s++) {
          let unreadi = String.fromCharCode(109,111,100,101,95,104,95,54,52,0);
          let bingl = String.fromCharCode(97,95,51,51,95,104,97,110,100,108,101,114,0);
          let teamg = 2;
         inviteq = [viewsW.length * 1];
         unreadi += `${1 << (Math.min(4, bingl.length))}`;
         bingl = `${teamg}`;
         teamg <<= Math.min(5, Math.abs(teamg));
      }
      for (let k = 0; k < 2; k++) {
         albumM = suggestiont.size == 17;
      }
      if (inviteq.length > playn.size) {
          let orientationA = 1;
         inviteq.push(playn.size);
         orientationA += orientationA;
      }
      if (albumM) {
         albumM = suggestiont.size == 44 && !aboutB;
      }
      let downloaded0 = 7047350 >= suggestiont.size;
      do {
         suggestiont.set(viewsW, (viewsW.length - (aboutB ? 1 : 1)));
         if (downloaded0) {
            break;
         }
      } while (downloaded0 && (suggestiont.size > 2));
         albumM = playn.size >= 57;
         playn.set(`${albumM}`, ((aboutB ? 4 : 4)));
         playn.set(`${playn.size}`, playn.size & 3);
      while (viewsW.length == inviteq.length) {
          let sellP = String.fromCharCode(102,105,108,116,101,114,95,116,95,57,51,0);
         inviteq.push(viewsW.length ^ 3);
         sellP = "1";
         break;
      }
      useri.push((1 * (albumM ? 1 : 4)));
   }
      useri.push(parseInt(`${backwardf}`));
   while (rankz) {
      rankz = 97 >= actionse.length || castingo.size >= 97;
      break;
   }
   if ((1 - useri.length) >= 2) {
      rankz = ((castingo.size * (!rankz ? castingo.size : 36)) < 3);
   }
   for (let x = 0; x < 3; x++) {
       let customu: Map<any, any> = new Map([[String.fromCharCode(107,95,49,48,95,98,116,110,0),419], [String.fromCharCode(101,118,97,108,115,95,101,95,50,48,0),440]]);
       let dangerh = 2.0;
       let detailp = 0.0;
       let pagex = false;
       let overlayh = false;
      while (customu.get(`${dangerh}`) != null) {
         dangerh -= parseInt(`${detailp}`) - 3;
         break;
      }
          let context_ = 1.0;
          let philippinesL: Map<any, any> = new Map([[String.fromCharCode(102,95,57,54,95,99,111,117,110,116,114,105,101,115,0),527], [String.fromCharCode(101,95,54,52,95,114,101,117,112,108,111,97,100,105,110,103,0),727], [String.fromCharCode(115,95,57,57,95,108,101,101,119,97,121,0),75]]);
         customu = new Map([[`${customu.size}`, 3]]);
         context_ /= Math.max(4, philippinesL.size / (Math.max(1, 3)));
         philippinesL = new Map([[`${philippinesL.size}`, philippinesL.size + parseInt(`${context_}`)]]);
          let roomY = String.fromCharCode(103,101,116,116,105,109,101,111,102,100,97,121,95,102,95,52,50,0);
          let cast7 = true;
          let pagination3 = String.fromCharCode(107,95,56,95,115,101,101,107,97,98,108,101,0);
         overlayh = detailp > 27.49;
         roomY += `${((cast7 ? 4 : 3) - roomY.length)}`;
         cast7 = !cast7;
         pagination3 += `${((cast7 ? 1 : 5) | 1)}`;
          let sheeth = 5.0;
         overlayh = pagex;
         sheeth -= parseFloat(`${parseInt(`${sheeth}`)}`);
         overlayh = 94.10 >= detailp || customu.size >= 88;
         pagex = (dangerh + detailp) <= 59.41;
         overlayh = !overlayh;
      if (1 >= (3 >> (Math.min(2, Math.abs(customu.size)))) || 4.54 >= (detailp + customu.size)) {
         customu.set(`${dangerh}`, parseInt(`${dangerh}`) ^ 3);
      }
      for (let c = 0; c < 3; c++) {
          let math3: Map<any, any> = new Map([[String.fromCharCode(97,102,102,101,99,116,95,52,95,50,55,0),String.fromCharCode(99,111,110,102,105,103,117,114,101,95,115,95,51,52,0)], [String.fromCharCode(109,95,49,54,95,103,114,111,117,112,101,100,0),String.fromCharCode(109,101,115,111,110,95,104,95,53,0)]]);
          let models7 = String.fromCharCode(98,95,52,55,95,115,101,114,105,97,108,105,122,105,110,103,0);
          let xvoda = String.fromCharCode(97,95,52,51,95,117,110,107,105,99,107,0);
          let auto_vX = 5;
         overlayh = !xvoda.startsWith(`${overlayh}`);
         math3 = new Map([[`${math3.size}`, models7.length << (Math.min(1, Math.abs(math3.size)))]]);
         models7 += `${auto_vX + math3.size}`;
         xvoda += `${math3.size}`;
         auto_vX += math3.size;
      }
         overlayh = 26.73 >= dangerh;
      for (let b = 0; b < 1; b++) {
         dangerh *= 3;
      }
         detailp += ((overlayh ? 4 : 4) >> (Math.min(Math.abs(1), 1)));
         dangerh -= (parseInt(`${dangerh}`) & (overlayh ? 2 : 1));
      for (let e = 0; e < 2; e++) {
         overlayh = 7.15 == detailp;
      }
       let strZ = 4.0;
      backwardf *= injuryV.length;
   }
   let appsf = castingo.size <= 6293021;
   do {
      castingo = new Map([[championC, 1]]);
      if (appsf) {
         break;
      }
   } while ((5 > (2 ^ pressureR.length)) && appsf);
       let faviconm = 1.0;
       let fastforwardj = 2;
       let constantsn: Map<any, any> = new Map([[String.fromCharCode(103,114,111,117,112,105,110,103,95,119,95,49,56,0),829], [String.fromCharCode(122,95,50,52,95,109,112,101,103,97,117,100,105,111,116,97,98,0),749]]);
       let connectionw: Map<any, any> = new Map([[String.fromCharCode(111,118,101,114,108,97,112,95,117,95,53,54,0),474], [String.fromCharCode(115,95,57,57,95,100,105,100,0),778]]);
      let loginR = connectionw.size <= 6455937;
      do {
         connectionw.set(`${constantsn.size}`, 1 / (Math.max(8, connectionw.size)));
         if (loginR) {
            break;
         }
      } while (loginR && (3 <= (connectionw.size | 3)));
      for (let f = 0; f < 2; f++) {
         faviconm += fastforwardj / 2;
      }
          let mailU = String.fromCharCode(104,105,116,95,119,95,54,49,0);
         faviconm += 1;
         mailU += `${mailU.length / (Math.max(1, 5))}`;
       let whistlet = false;
         fastforwardj |= fastforwardj;
      pressureR = `${parseInt(`${backwardf}`)}`;
      rankz = championC.length > 64;
   while ((5 | championC.length) > 1) {
      championC += `${parseInt(`${const_yc}`) % 2}`;
      break;
   }
      injuryV += `${parseInt(`${brightness0}`)}`;
   if (3.44 == (brightness0 - 5.4) && 4.40 == (brightness0 / (Math.max(5.4, 6)))) {
      brightness0 += parseFloat(`${parseInt(`${backwardf}`) % (Math.max(actionse.length, 4))}`);
   }
      actionse.push(2 - parseInt(`${backwardf}`));
       let sell3 = String.fromCharCode(115,116,97,110,100,97,114,100,95,102,95,53,56,0);
       let signinupB = 3.0;
       let infoz = String.fromCharCode(113,95,55,54,95,97,110,116,105,97,108,105,97,115,101,100,0);
          let handlerV = String.fromCharCode(99,108,97,109,112,95,114,95,50,55,0);
          let bottome: Array<any> = [String.fromCharCode(100,95,57,54,95,110,111,110,0), String.fromCharCode(108,95,55,56,95,97,99,99,114,117,101,100,0), String.fromCharCode(100,95,56,55,95,99,111,109,112,101,110,115,97,116,101,100,0)];
          let texte = true;
         signinupB -= ((texte ? 4 : 2) % (Math.max(9, parseInt(`${signinupB}`))));
         handlerV += "3";
         bottome = [bottome.length];
         texte = handlerV.length >= 43;
      let checkboxH = 6124124.0 <= signinupB;
      do {
          let corner6 = 0.0;
          let recommendation4 = String.fromCharCode(104,97,110,100,108,101,114,115,95,120,95,56,51,0);
         signinupB /= Math.max(5, infoz.length);
         corner6 += parseInt(`${corner6}`);
         recommendation4 = `${parseInt(`${corner6}`) % 3}`;
         if (checkboxH) {
            break;
         }
      } while ((4.12 == (signinupB / (Math.max(10, infoz.length)))) && checkboxH);
       let recommendationK = String.fromCharCode(97,95,57,95,111,110,116,111,0);
       let splashU = String.fromCharCode(112,95,55,54,95,118,115,99,97,108,101,0);
       let disconnecteda = false;
       let yingQ = true;
      let y_unlockV = String.fromCharCode(110,52,54,105,116,100,109,98,0) == recommendationK;
      do {
         recommendationK = `${(splashU.length | (disconnecteda ? 4 : 2))}`;
         if (y_unlockV) {
            break;
         }
      } while (y_unlockV && (recommendationK.includes(splashU)));
      let currentG = sell3.length >= 8998417;
      do {
         sell3 += `${(1 ^ (yingQ ? 3 : 5))}`;
         if (currentG) {
            break;
         }
      } while ((parseInt(`${signinupB}`) == sell3.length) && currentG);
      let native7 = disconnecteda ? !disconnecteda : disconnecteda;
      do {
         disconnecteda = disconnecteda && infoz.length < 35;
         if (native7) {
            break;
         }
      } while (native7 && (disconnecteda && 1 >= recommendationK.length));
      while (3.29 <= (4.95 * signinupB) || (sell3.length * parseInt(`${signinupB}`)) <= 5) {
          let cast9: Map<any, any> = new Map([[String.fromCharCode(99,111,109,102,111,114,116,95,55,95,52,57,0),String.fromCharCode(117,110,109,97,112,102,105,108,101,95,55,95,56,48,0)], [String.fromCharCode(115,117,98,108,101,110,103,116,104,95,48,95,50,0),String.fromCharCode(106,95,50,53,95,102,111,114,119,97,114,100,101,100,0)]]);
          let predictionN = 1;
          let details9: Array<any> = [442, 240, 585];
         signinupB *= cast9.size / 3;
         cast9.set(`${predictionN}`, 3);
         predictionN *= 3 * predictionN;
         details9.push(predictionN);
         break;
      }
      while (!disconnecteda && sell3.length >= 1) {
         disconnecteda = recommendationK.length < signinupB;
         break;
      }
      backwardf -= 1 << (Math.min(Math.abs(trophyU), 1));
   while ((parseInt(`${brightness0}`) * pressureR.length) < 2) {
       let gmail4 = true;
       let f_countp = 5;
       let taiwanQ = 0.0;
       let backwardJ: Map<any, any> = new Map([[String.fromCharCode(99,111,109,112,111,110,101,110,116,95,101,95,55,57,0),746], [String.fromCharCode(108,97,117,110,99,104,105,110,103,95,98,95,52,51,0),691]]);
       let teaml = 5;
         taiwanQ /= Math.max(parseFloat(`${teaml - 2}`), 2);
      if (backwardJ.get(`${teaml}`) != null) {
         teaml %= Math.max(3, 5);
      }
         taiwanQ *= parseFloat(`${3 % (Math.max(5, backwardJ.size))}`);
         f_countp %= Math.max(1 + backwardJ.size, 3);
      while (4 > teaml) {
         teaml *= parseInt(`${taiwanQ}`);
         break;
      }
         backwardJ.set(`${teaml}`, 2 >> (Math.min(1, Math.abs(f_countp))));
       let shrinkZ: Array<any> = [701, 469, 8];
       let country3: Array<any> = [String.fromCharCode(115,95,57,52,95,108,111,99,105,0), String.fromCharCode(113,117,111,116,101,115,95,112,95,57,53,0)];
          let about3 = String.fromCharCode(97,117,103,109,101,110,116,95,106,95,55,54,0);
          let albume = 2;
         gmail4 = about3.includes(`${albume}`);
         taiwanQ += parseFloat(`${parseInt(`${taiwanQ}`)}`);
          let downloadL = 2.0;
          let dangerq = String.fromCharCode(104,97,108,102,102,108,111,97,116,95,114,95,56,48,0);
         taiwanQ -= parseFloat(`${3}`);
         downloadL -= parseInt(`${downloadL}`);
         dangerq = `${parseInt(`${downloadL}`)}`;
         f_countp -= country3.length;
      let b_centerk = 7799430 <= f_countp;
      do {
         f_countp %= Math.max(teaml - 1, 5);
         if (b_centerk) {
            break;
         }
      } while (b_centerk && (5 >= (4 / (Math.max(6, backwardJ.size))) && 4 >= (f_countp / (Math.max(backwardJ.size, 5)))));
      let skipK = 5287317 >= teaml;
      do {
          let middle0 = String.fromCharCode(121,95,57,51,95,100,101,114,105,118,97,116,105,118,101,0);
          let gpay_ = String.fromCharCode(105,95,52,57,95,111,112,116,0);
          let singaporeM = String.fromCharCode(99,95,52,49,95,118,99,114,101,97,116,101,0);
          let tickr = String.fromCharCode(100,95,51,95,105,110,102,0);
          let refresha = 2;
         teaml *= 3 * backwardJ.size;
         middle0 += `${tickr.length ^ singaporeM.length}`;
         gpay_ += `${refresha * 3}`;
         singaporeM += `${tickr.length ^ gpay_.length}`;
         refresha >>= Math.min(Math.abs(refresha), 4);
         if (skipK) {
            break;
         }
      } while ((gmail4) && skipK);
      if ((shrinkZ.length + 4) > 5 || 3 > (4 * shrinkZ.length)) {
         shrinkZ = [f_countp];
      }
      if (3 > shrinkZ.length) {
         gmail4 = backwardJ.size >= 77;
      }
      brightness0 *= parseFloat(`${2}`);
      break;
   }
      analyticsv.set(`${const_yc}`, actionse.length << (Math.min(Math.abs(2), 5)));
   for (let j = 0; j < 3; j++) {
      analyticsv.set(`${const_yc}`, useri.length >> (Math.min(1, Math.abs(parseInt(`${const_yc}`)))));
   }
      pressureR = `${actionse.length & 2}`;
       let textA: Array<any> = [637, 184];
      if (textA.length <= textA.length) {
          let showx = 1;
          let macauf = String.fromCharCode(100,95,50,53,95,115,104,97,107,101,0);
         textA.push(showx);
         showx *= macauf.length >> (Math.min(Math.abs(2), 3));
         macauf += `${2 >> (Math.min(4, macauf.length))}`;
      }
         textA = [textA.length];
         textA = [textA.length];
      injuryV += `${castingo.size}`;
       let eactG = String.fromCharCode(115,95,52,55,95,97,114,116,105,102,97,99,116,115,0);
       let verticalC = String.fromCharCode(98,95,55,55,95,98,108,117,114,114,101,100,0);
       let collectionA = 0.0;
       let becomeS = 2.0;
       let malaysia0 = false;
       let plashW = true;
      if (!eactG.startsWith(`${plashW}`)) {
         eactG += `${parseInt(`${becomeS}`)}`;
      }
         collectionA += parseFloat(`${parseInt(`${collectionA}`)}`);
       let hejiB = 0;
          let mail4 = false;
          let kickI = String.fromCharCode(122,95,49,53,95,109,105,109,101,0);
         hejiB &= (eactG == String.fromCharCode(49,0) ? eactG.length : parseInt(`${becomeS}`));
         mail4 = ((kickI.length * (!mail4 ? kickI.length : 55)) < 55);
      championC += "2";
      verticalC = `${verticalC.length}`;
      analyticsv = new Map([[`${castingo.size}`, 2]]);
   while (injuryV.includes(`${castingo.size}`)) {
       let halfv = String.fromCharCode(108,108,118,105,100,100,115,112,101,110,99,95,98,95,52,49,0);
       let popupb: Map<any, any> = new Map([[String.fromCharCode(105,95,50,48,95,101,112,111,99,104,0),String.fromCharCode(116,119,105,110,118,113,95,114,95,53,53,0)], [String.fromCharCode(109,97,108,108,111,99,122,95,117,95,51,54,0),String.fromCharCode(105,95,50,54,95,99,111,99,111,97,0)]]);
       let currentl = 5.0;
       let taiwanr = String.fromCharCode(119,95,55,55,95,101,118,101,110,108,121,0);
      for (let v = 0; v < 3; v++) {
          let sideE = String.fromCharCode(109,95,53,49,95,99,108,111,99,107,100,114,105,102,116,0);
          let macau7: Map<any, any> = new Map([[String.fromCharCode(109,95,52,54,95,105,110,115,101,114,116,105,111,110,0),631], [String.fromCharCode(108,105,115,116,110,101,114,115,95,119,95,57,50,0),328], [String.fromCharCode(101,95,52,51,95,115,110,110,105,100,0),534]]);
         halfv += "1";
         sideE += `${sideE.length}`;
         macau7.set(`${sideE}`, sideE.length - macau7.size);
      }
         popupb.set(halfv, 2);
          let main_px = String.fromCharCode(97,95,57,54,95,98,111,100,105,101,115,0);
         popupb.set(taiwanr, halfv.length | 2);
         main_px = `${main_px.length + main_px.length}`;
         currentl -= parseFloat(`${halfv.length + popupb.size}`);
          let dialogq = String.fromCharCode(101,99,114,101,116,95,120,95,56,51,0);
          let championu = 3.0;
         popupb.set(halfv, (halfv == String.fromCharCode(79,0) ? dialogq.length : halfv.length));
         dialogq += `${parseInt(`${championu}`)}`;
      while (3 >= (halfv.length & popupb.size) || (halfv.length & popupb.size) >= 3) {
          let modalt = String.fromCharCode(122,95,56,53,95,98,121,116,101,111,117,116,0);
          let referrery = 4;
          let update_t4 = 0.0;
          let championE = 4;
         halfv = `${(halfv == String.fromCharCode(53,0) ? modalt.length : halfv.length)}`;
         modalt = `${referrery + 3}`;
         referrery %= Math.max(4, 2 | championE);
         update_t4 += parseInt(`${update_t4}`) & referrery;
         championE *= championE % (Math.max(1, parseInt(`${update_t4}`)));
         break;
      }
         currentl += (parseFloat(`${halfv == String.fromCharCode(97,0) ? halfv.length : popupb.size}`));
      for (let s = 0; s < 3; s++) {
         currentl /= Math.max(parseFloat(`${popupb.size & parseInt(`${currentl}`)}`), 3);
      }
          let buffer2 = false;
          let orangeB = 0.0;
          let dataD = 0;
         halfv = `${2 / (Math.max(7, parseInt(`${orangeB}`)))}`;
         buffer2 = 96 < dataD;
         orangeB += parseFloat(`${3}`);
         dataD <<= Math.min(Math.abs((dataD ^ (buffer2 ? 1 : 2))), 2);
          let typingc = String.fromCharCode(122,95,49,50,95,106,111,98,115,0);
         taiwanr += `${halfv.length ^ taiwanr.length}`;
         typingc = `${typingc.length ^ 3}`;
       let flyerh = 0.0;
       let fullI = 3.0;
      let emptyn = 7176129.0 <= flyerh;
      do {
         flyerh /= Math.max(parseInt(`${currentl}`), 3);
         if (emptyn) {
            break;
         }
      } while (emptyn && ((fullI / (Math.max(flyerh, 6))) == 4.97));
      castingo.set(`${actionse.length}`, useri.length & 1);
      break;
   }
   for (let l = 0; l < 3; l++) {
       let models2 = 3.0;
       let agreementi = String.fromCharCode(98,111,114,100,101,114,115,95,108,95,49,52,0);
      if (5 == (agreementi.length & 2)) {
         agreementi += `${2 << (Math.min(1, agreementi.length))}`;
      }
         agreementi += `${agreementi.length + parseInt(`${models2}`)}`;
      for (let y = 0; y < 2; y++) {
         models2 -= parseFloat(`${1}`);
      }
         models2 /= Math.max(2, parseFloat(`${3}`));
         agreementi += `${agreementi.length}`;
         models2 *= parseFloat(`${agreementi.length | 2}`);
      championC = `${3 * championC.length}`;
   }
       let castQ = String.fromCharCode(97,110,110,111,116,97,116,105,111,110,115,95,114,95,54,55,0);
       let team7 = 4.0;
         team7 /= Math.max(parseInt(`${team7}`), 1);
          let handleri = String.fromCharCode(114,101,108,97,117,110,99,104,95,122,95,54,53,0);
          let bottomu = 2.0;
          let storef: Array<any> = [String.fromCharCode(113,95,49,54,95,114,101,97,100,0), String.fromCharCode(102,95,53,49,95,111,118,101,114,115,99,114,111,108,108,0)];
         team7 /= Math.max(3, parseInt(`${bottomu}`));
         handleri += `${handleri.length + storef.length}`;
         bottomu -= storef.length * 2;
       let selectionM = 3;
      let shirt6 = String.fromCharCode(105,113,108,100,0) == castQ;
      do {
         castQ = `${castQ.length}`;
         if (shirt6) {
            break;
         }
      } while ((team7 <= castQ.length) && shirt6);
      for (let a = 0; a < 3; a++) {
          let bufferN = 3;
          let ying5 = 5.0;
          let update_iaB = 1;
          let commonX = String.fromCharCode(102,95,52,51,95,97,115,102,114,116,112,0);
          let bingN = String.fromCharCode(100,95,56,95,115,97,110,105,116,105,122,101,114,0);
         selectionM |= parseInt(`${ying5}`) - 3;
         bufferN ^= commonX.length;
         ying5 -= parseFloat(`${bingN.length}`);
         update_iaB /= Math.max(2, 2);
         commonX += `${bufferN}`;
         bingN = `${bingN.length - bufferN}`;
      }
      for (let c = 0; c < 3; c++) {
         selectionM -= castQ.length;
      }
      injuryV = `${championC.length}`;
   for (let j = 0; j < 3; j++) {
      const_yc *= parseFloat(`${parseInt(`${const_yc}`)}`);
   }
   let spinnerF = 5145183 >= castingo.size;
   do {
      castingo = new Map([[injuryV, championC.length + injuryV.length]]);
      if (spinnerF) {
         break;
      }
   } while (spinnerF && (!Array.from(castingo.keys()).includes(`${backwardf}`)));
   if ((analyticsv.size * actionse.length) == 4) {
      analyticsv.set(`${backwardf}`, parseInt(`${backwardf}`) * castingo.size);
   }

    RNExitApp.exitApp();
  }, []);

  const handleAccept = useCallback(() => {
       let sansc = 2;
    let scorei = 3;
    let sendZ: Map<any, any> = new Map([[String.fromCharCode(117,95,54,95,115,112,101,101,120,0),false ], [String.fromCharCode(109,95,56,51,95,99,111,111,114,100,105,110,97,116,105,111,110,0),false ]]);
    let annera = 2;
    let control7 = String.fromCharCode(114,101,99,111,103,110,105,122,101,95,99,95,56,55,0);
    let logoutL = 1.0;
    let rewind0 = String.fromCharCode(101,95,54,52,95,101,120,116,114,97,102,105,101,108,100,0);
    let navigationV: Map<any, any> = new Map([[String.fromCharCode(100,97,116,97,99,101,110,116,101,114,95,119,95,54,49,0),361], [String.fromCharCode(112,105,112,101,108,105,110,105,110,103,95,102,95,51,54,0),577], [String.fromCharCode(115,95,52,50,95,100,101,115,99,101,110,100,101,114,0),572]]);
    let tooltipsK = true;
    let friendss = String.fromCharCode(119,95,53,50,95,115,116,114,105,99,116,0);
    let plusx = String.fromCharCode(120,95,54,48,95,100,121,110,108,105,110,107,0);
    let successj = String.fromCharCode(110,101,119,108,105,110,101,115,95,102,95,50,57,0);
    let annerI = 3;
   let volumeo = 8297216.0 >= logoutL;
   do {
       let stept: Array<any> = [443, 3];
       let arrowk = String.fromCharCode(118,95,53,50,95,118,97,108,105,100,97,116,105,111,110,115,0);
       let assists = String.fromCharCode(102,101,101,95,48,95,56,54,0);
      for (let z = 0; z < 1; z++) {
         stept.push(2 << (Math.min(2, arrowk.length)));
      }
       let showH = 1.0;
      logoutL += scorei % 1;
      if (volumeo) {
         break;
      }
   } while ((navigationV.get(`${logoutL}`) != null) && volumeo);
       let less3 = String.fromCharCode(108,95,50,53,95,101,110,116,114,111,112,121,0);
       let combinedq = 0;
         combinedq >>= Math.min(4, Math.abs(less3.length - 2));
      if ((combinedq ^ 1) == 3 && (combinedq ^ 1) == 4) {
          let controlsb = String.fromCharCode(115,104,117,116,100,111,119,110,95,56,95,53,49,0);
          let v_locko = String.fromCharCode(98,105,103,103,101,115,116,95,53,95,55,51,0);
          let refresh0: Map<any, any> = new Map([[String.fromCharCode(115,119,105,122,122,108,101,95,55,95,53,48,0),975], [String.fromCharCode(114,101,97,100,98,121,116,101,95,50,95,49,52,0),120]]);
          let paginationv: Array<any> = [507, 897];
         less3 = `${refresh0.size}`;
         controlsb += `${2 & controlsb.length}`;
         v_locko = `${2 + v_locko.length}`;
         refresh0.set(controlsb, paginationv.length / (Math.max(3, 2)));
         paginationv = [3 << (Math.min(4, controlsb.length))];
      }
      while ((combinedq + 2) > 5) {
         less3 = `${less3.length}`;
         break;
      }
      while ((combinedq >> (Math.min(less3.length, 5))) < 5) {
         combinedq *= less3.length;
         break;
      }
         combinedq <<= Math.min(5, Math.abs((String.fromCharCode(52,0) == less3 ? combinedq : less3.length)));
         combinedq &= 3 * less3.length;
      plusx = `${rewind0.length}`;
   if (5 <= (scorei - control7.length)) {
      scorei -= annera;
   }
       let disconnectedJ = String.fromCharCode(103,95,57,57,95,104,97,110,100,108,101,114,0);
      if (disconnectedJ == disconnectedJ) {
         disconnectedJ = `${(disconnectedJ == String.fromCharCode(84,0) ? disconnectedJ.length : disconnectedJ.length)}`;
      }
      if (disconnectedJ != String.fromCharCode(68,0)) {
         disconnectedJ = `${(String.fromCharCode(90,0) == disconnectedJ ? disconnectedJ.length : disconnectedJ.length)}`;
      }
      for (let n = 0; n < 3; n++) {
         disconnectedJ += `${disconnectedJ.length - 2}`;
      }
      plusx += `${1 * annera}`;
      successj += `${sendZ.size >> (Math.min(control7.length, 5))}`;
      friendss = `${sansc >> (Math.min(Math.abs(scorei), 5))}`;
   if (rewind0.endsWith(`${tooltipsK}`)) {
      tooltipsK = (plusx.length + parseInt(`${logoutL}`)) > 92;
   }
      successj += `${sendZ.size}`;
      tooltipsK = String.fromCharCode(52,0) == successj;
      annera &= sansc;
   while (!rewind0.startsWith(`${scorei}`)) {
       let frame_ef8 = 4;
       let long_4oJ: Map<any, any> = new Map([[String.fromCharCode(115,105,110,113,102,95,101,95,53,0),566], [String.fromCharCode(109,97,120,105,109,117,109,95,117,95,54,56,0),519]]);
         frame_ef8 -= 1 & frame_ef8;
       let termsW = 1.0;
          let untickR = String.fromCharCode(113,117,97,114,116,122,95,114,95,53,55,0);
         long_4oJ.set(untickR, parseInt(`${termsW}`));
      while (3 < (5 - frame_ef8) || 5 < (parseInt(`${termsW}`) * frame_ef8)) {
         termsW += 3;
         break;
      }
      let zhengpianL = 9111129.0 >= termsW;
      do {
         termsW += 2 << (Math.min(4, Math.abs(parseInt(`${termsW}`))));
         if (zhengpianL) {
            break;
         }
      } while (zhengpianL && (4 == (1 ^ frame_ef8)));
       let starw = String.fromCharCode(108,115,98,115,95,52,95,56,52,0);
       let videor = String.fromCharCode(99,95,49,95,97,97,117,100,105,111,0);
      rewind0 = `${navigationV.size | 1}`;
      break;
   }
   while (tooltipsK || friendss.length < 4) {
       let updatesk = String.fromCharCode(111,100,100,95,54,95,49,55,0);
      while (updatesk.length == updatesk.length) {
         updatesk += `${(updatesk == String.fromCharCode(76,0) ? updatesk.length : updatesk.length)}`;
         break;
      }
      while (updatesk.length >= 4) {
          let volumeQ = 0.0;
          let complete9 = true;
          let photo_ = String.fromCharCode(111,95,50,54,95,102,111,111,116,98,97,108,108,0);
         updatesk = `${parseInt(`${volumeQ}`) % 1}`;
         volumeQ -= ((complete9 ? 4 : 5));
         complete9 = photo_.length <= 11;
         photo_ += `${(photo_ == String.fromCharCode(80,0) ? photo_.length : (complete9 ? 1 : 4))}`;
         break;
      }
      let i_centere = updatesk == String.fromCharCode(54,48,116,0);
      do {
         updatesk += `${updatesk.length % 2}`;
         if (i_centere) {
            break;
         }
      } while (i_centere && (!updatesk.includes(`${updatesk.length}`)));
      friendss = `${scorei / (Math.max(2, 3))}`;
      break;
   }
      scorei *= 1;
   while ((2.55 - logoutL) <= 5.90 || 1 <= (scorei - parseInt(`${logoutL}`))) {
       let renewp: Map<any, any> = new Map([[String.fromCharCode(99,95,55,49,95,108,108,118,105,100,100,115,112,0),662], [String.fromCharCode(104,95,53,53,95,103,101,110,101,114,97,116,105,111,110,0),449]]);
       let bingV = 0.0;
       let entryx = String.fromCharCode(118,95,50,48,95,104,111,115,116,110,97,109,101,0);
       let orangev: Array<any> = [361, 107, 452];
      if (orangev.length > 1) {
         bingV += (parseFloat(`${String.fromCharCode(67,0) == entryx ? entryx.length : parseInt(`${bingV}`)}`));
      }
      let refresh0t = 7624011 <= orangev.length;
      do {
         orangev.push(entryx.length);
         if (refresh0t) {
            break;
         }
      } while ((3 <= (2 + orangev.length) || 5.72 <= (bingV / 5.92)) && refresh0t);
       let launchX = String.fromCharCode(105,118,115,101,116,117,112,95,57,95,54,49,0);
       let stepD = String.fromCharCode(112,104,97,115,101,115,95,105,95,55,0);
         stepD = `${orangev.length << (Math.min(5, Math.abs(parseInt(`${bingV}`))))}`;
         launchX = `${renewp.size | 2}`;
         renewp.set(`${bingV}`, renewp.size);
      for (let e = 0; e < 2; e++) {
         bingV /= Math.max(5, parseFloat(`${entryx.length % (Math.max(10, parseInt(`${bingV}`)))}`));
      }
      for (let m = 0; m < 3; m++) {
          let k_unlockw = String.fromCharCode(104,95,55,52,95,114,101,115,117,108,116,115,0);
         launchX += `${k_unlockw.length}`;
      }
       let albumB = String.fromCharCode(101,97,115,121,95,105,95,53,52,0);
       let topicU = String.fromCharCode(117,110,115,104,105,102,116,95,111,95,55,55,0);
      for (let r = 0; r < 2; r++) {
         orangev = [(String.fromCharCode(50,0) == albumB ? orangev.length : albumB.length)];
      }
       let componentH = String.fromCharCode(104,95,54,57,0);
       let membershipa = String.fromCharCode(119,101,105,103,104,116,120,95,119,95,56,50,0);
         stepD = `${(albumB == String.fromCharCode(87,0) ? albumB.length : stepD.length)}`;
      scorei >>= Math.min(Math.abs(renewp.size), 4);
      break;
   }

    setIsVisible(false)

       let rank4 = 5.0;
      let rulesF = rank4 >= 6157926.0;
      do {
         rank4 += parseInt(`${rank4}`) / 2;
         if (rulesF) {
            break;
         }
      } while (rulesF && ((rank4 / (Math.max(5, rank4))) <= 2.1 || 1.52 <= (2.1 / (Math.max(6, rank4)))));
      let sound0 = 9535784.0 <= rank4;
      do {
          let twitterJ = String.fromCharCode(97,115,110,116,95,103,95,53,52,0);
          let layout_ = 0.0;
          let collectionT = 5.0;
          let acceptedL = String.fromCharCode(117,121,118,121,116,111,121,117,118,95,103,95,57,56,0);
         rank4 -= parseInt(`${collectionT}`);
         twitterJ += `${2 & parseInt(`${layout_}`)}`;
         layout_ /= Math.max(3, (twitterJ == String.fromCharCode(120,0) ? twitterJ.length : acceptedL.length));
         acceptedL = "3";
         if (sound0) {
            break;
         }
      } while ((1.52 == (rank4 - rank4)) && sound0);
          let fieldg = String.fromCharCode(116,95,48,95,108,111,99,97,116,101,100,0);
          let expandc = 3;
         rank4 /= Math.max(parseInt(`${rank4}`), 1);
         fieldg += `${fieldg.length}`;
         expandc |= 3;
      tooltipsK = control7 == String.fromCharCode(101,0) && 69.65 >= rank4;
   for (let i = 0; i < 1; i++) {
      control7 = `${sendZ.size | 2}`;
   }
      sendZ.set(control7, 1);
       let privacyB = String.fromCharCode(98,97,115,105,99,95,120,95,56,51,0);
       let onewsn = String.fromCharCode(100,110,115,108,97,98,101,108,95,54,95,51,0);
         privacyB = `${privacyB.length}`;
          let expand9 = String.fromCharCode(109,111,110,111,98,108,97,99,107,95,106,95,50,52,0);
          let telegrama = false;
          let savem = String.fromCharCode(122,95,54,49,95,115,121,115,99,116,108,0);
         onewsn = `${expand9.length | 1}`;
         expand9 = "1";
         telegrama = !telegrama;
         savem = `${((telegrama ? 2 : 4) * savem.length)}`;
         onewsn += "3";
          let sendc: Map<any, any> = new Map([[String.fromCharCode(115,116,114,105,112,101,95,102,95,53,57,0),true ], [String.fromCharCode(105,103,110,112,111,115,116,95,112,95,53,52,0),false ], [String.fromCharCode(115,95,52,49,95,116,111,107,101,0),true ]]);
          let catagory_ = 4.0;
          let rightO = String.fromCharCode(98,95,53,55,95,109,101,115,115,115,97,103,101,0);
         privacyB = `${rightO.length}`;
         sendc = new Map([[`${sendc.size}`, 2 | parseInt(`${catagory_}`)]]);
         catagory_ -= parseInt(`${catagory_}`) & 1;
         rightO = `${sendc.size}`;
         onewsn += `${onewsn.length * 1}`;
         privacyB = `${onewsn.length}`;
      plusx = `${navigationV.size}`;
      sansc |= navigationV.size % (Math.max(10, annera));
   while (1 < sansc) {
       let combineK = 5.0;
       let greenT = 0.0;
      let handler8 = greenT >= 5289287.0;
      do {
         greenT -= 2;
         if (handler8) {
            break;
         }
      } while (handler8 && (5.52 == (combineK - 3.14) && 3.14 == (combineK - greenT)));
      let eighteenG = 6350313.0 <= combineK;
      do {
         combineK -= parseFloat(`${parseInt(`${greenT}`)}`);
         if (eighteenG) {
            break;
         }
      } while (eighteenG && ((greenT / 2) > 4.18));
          let macau3 = 5.0;
          let viewerl: Map<any, any> = new Map([[String.fromCharCode(112,114,105,111,114,105,116,105,101,115,95,48,95,49,56,0),942], [String.fromCharCode(102,95,55,49,95,101,120,112,105,114,97,116,105,111,110,115,0),816], [String.fromCharCode(101,115,115,101,110,99,101,95,111,95,51,48,0),10]]);
          let group_ = 5.0;
         combineK -= parseFloat(`${parseInt(`${macau3}`) / 3}`);
         macau3 /= Math.max(3, parseFloat(`${viewerl.size}`));
         viewerl = new Map([[`${viewerl.size}`, 1]]);
         group_ += parseFloat(`${viewerl.size}`);
      let soundD = 7333880.0 >= greenT;
      do {
         greenT /= Math.max(1, parseInt(`${combineK}`));
         if (soundD) {
            break;
         }
      } while (((greenT - combineK) >= 2.41) && soundD);
      let default_7m = combineK <= 6218872.0;
      do {
          let componentm = String.fromCharCode(104,97,115,95,55,95,51,0);
          let gestureW = String.fromCharCode(105,95,57,54,95,120,118,105,100,105,100,99,116,0);
          let assist1 = 5.0;
          let castw: Map<any, any> = new Map([[String.fromCharCode(120,95,50,49,95,105,110,105,116,105,97,108,108,121,0),String.fromCharCode(119,95,49,48,95,115,117,98,101,120,112,0)], [String.fromCharCode(98,97,110,100,105,110,103,95,118,95,48,0),String.fromCharCode(109,99,111,114,101,95,105,95,55,48,0)]]);
          let twitterp = String.fromCharCode(118,97,114,109,97,115,107,95,55,95,51,49,0);
         combineK -= parseFloat(`${twitterp.length << (Math.min(1, Math.abs(parseInt(`${greenT}`))))}`);
         componentm += `${parseInt(`${assist1}`) & componentm.length}`;
         gestureW = "1";
         assist1 *= 1 + gestureW.length;
         castw = new Map([[componentm, gestureW.length + 3]]);
         twitterp += `${gestureW.length + 2}`;
         if (default_7m) {
            break;
         }
      } while ((3.73 == greenT) && default_7m);
          let router4 = true;
         combineK *= parseFloat(`${parseInt(`${combineK}`) | 3}`);
         router4 = !router4;
      scorei %= Math.max(1, 3);
      break;
   }
      annera <<= Math.min(1, Math.abs(3));
      sansc -= 1 << (Math.min(4, Math.abs(sendZ.size)));
   while (4 == (rewind0.length >> (Math.min(Math.abs(3), 3)))) {
      rewind0 = "1";
      break;
   }
      plusx += "3";
       let filledJ = 1.0;
      let main_ly = 9526479.0 <= filledJ;
      do {
         filledJ += parseFloat(`${parseInt(`${filledJ}`)}`);
         if (main_ly) {
            break;
         }
      } while (main_ly && (filledJ < filledJ));
      while (filledJ > 4.25) {
         filledJ *= parseFloat(`${parseInt(`${filledJ}`)}`);
         break;
      }
          let dialogK = false;
          let areaE = String.fromCharCode(106,95,56,51,95,114,101,97,100,101,114,0);
          let stationB = String.fromCharCode(99,111,109,112,105,108,101,111,112,116,105,111,110,95,106,95,56,52,0);
         filledJ *= parseFloat(`${areaE.length}`);
         dialogK = stationB.length <= stationB.length;
         areaE = `${stationB.length - 1}`;
      plusx += "1";
   if (rewind0.length > sansc) {
      rewind0 += `${sendZ.size}`;
   }
       let eighteen_ = true;
      if (eighteen_) {
          let halfR = String.fromCharCode(115,113,117,97,114,101,115,95,50,95,56,54,0);
          let stepA = String.fromCharCode(114,101,102,105,110,101,100,95,48,95,49,54,0);
          let targetP: Array<any> = [String.fromCharCode(122,95,51,53,95,117,116,120,111,0), String.fromCharCode(109,95,55,48,95,115,116,97,99,104,0), String.fromCharCode(100,95,56,56,95,110,111,110,0)];
          let predictionK = String.fromCharCode(110,95,51,49,95,100,115,104,111,119,0);
          let heartb = true;
         eighteen_ = (((!eighteen_ ? 52 : stepA.length) << (Math.min(stepA.length, 5))) >= 52);
         halfR += `${targetP.length ^ 1}`;
         targetP = [halfR.length];
         predictionK += "2";
      }
      while (!eighteen_) {
         eighteen_ = (!eighteen_ ? !eighteen_ : !eighteen_);
         break;
      }
         eighteen_ = !eighteen_ || !eighteen_;
      control7 = `${3 * sansc}`;
      scorei &= sansc;
    setAccepted(true)

      logoutL *= scorei;
   if (scorei < 1) {
       let leftF = false;
       let awayz = String.fromCharCode(97,117,116,111,109,97,116,105,99,97,108,108,121,95,115,95,57,49,0);
       let viewsy = String.fromCharCode(115,110,97,107,101,95,103,95,56,50,0);
       let tick3 = String.fromCharCode(97,115,115,111,99,108,105,115,116,95,117,95,50,51,0);
      if (leftF) {
         viewsy += `${awayz.length - viewsy.length}`;
      }
      scorei %= Math.max(parseInt(`${logoutL}`) ^ successj.length, 1);
   }
   for (let c = 0; c < 1; c++) {
      logoutL /= Math.max(2, 3);
   }
       let telegram7 = 5;
      if (telegram7 == 4) {
          let sliderd = 5.0;
         telegram7 *= 2 << (Math.min(Math.abs(telegram7), 2));
         sliderd += parseInt(`${sliderd}`);
      }
          let private_wkc = 3;
          let windw = 3;
         telegram7 ^= private_wkc;
         windw |= 2;
          let x_centerv = true;
          let sheetN = 4.0;
         telegram7 |= 3 >> (Math.min(Math.abs(parseInt(`${sheetN}`)), 1));
         x_centerv = (!x_centerv ? x_centerv : x_centerv);
         sheetN -= (parseFloat(`${(x_centerv ? 3 : 4) + (x_centerv ? 2 : 2)}`));
      friendss += `${annera - control7.length}`;
   if ((4 * annera) == 4) {
       let leftw: Array<any> = [String.fromCharCode(114,95,56,50,95,110,111,110,101,0), String.fromCharCode(115,119,105,112,101,100,95,107,95,53,49,0)];
       let backR = 1;
       let acceptedj = String.fromCharCode(97,95,50,55,95,115,104,97,100,111,119,115,0);
          let listd: Map<any, any> = new Map([[String.fromCharCode(115,105,109,105,108,97,114,105,116,121,95,116,95,56,52,0),950], [String.fromCharCode(106,95,53,95,118,105,116,97,108,105,107,108,105,122,101,0),106], [String.fromCharCode(119,95,50,56,95,117,110,109,97,115,107,0),547]]);
          let miniZ: Array<any> = [840, 780];
         acceptedj = "2";
         listd.set(`${miniZ.length}`, miniZ.length);
      for (let k = 0; k < 2; k++) {
         leftw.push((String.fromCharCode(115,0) == acceptedj ? acceptedj.length : backR));
      }
          let shared9 = false;
          let basketballR = 2;
          let indexG = String.fromCharCode(99,114,101,97,116,105,110,103,95,54,95,49,54,0);
         backR >>= Math.min(Math.abs(2), 2);
         shared9 = !shared9;
         basketballR <<= Math.min(Math.abs(1 / (Math.max(basketballR, 5))), 4);
         indexG += `${basketballR}`;
      if (!acceptedj.endsWith(`${leftw.length}`)) {
         leftw = [acceptedj.length];
      }
      while (4 == (backR << (Math.min(leftw.length, 5))) || 4 == (4 << (Math.min(5, Math.abs(backR))))) {
         backR /= Math.max(4, (acceptedj == String.fromCharCode(50,0) ? acceptedj.length : backR));
         break;
      }
      tooltipsK = (friendss.length << (Math.min(4, rewind0.length))) >= 50;
   }
       let diceR = 4;
      while ((diceR ^ diceR) == 2 && (diceR ^ 2) == 5) {
         diceR &= 3 ^ diceR;
         break;
      }
         diceR *= diceR + diceR;
         diceR &= 1;
      plusx += `${diceR | annerI}`;
   while (5 < rewind0.length) {
      rewind0 = "2";
      break;
   }
      navigationV.set(`${annerI}`, navigationV.size);
   while (1 > (annerI / 5) || tooltipsK) {
      annerI |= friendss.length / 3;
      break;
   }
       let catagorya = String.fromCharCode(100,97,114,119,105,110,95,108,95,53,53,0);
       let emojio = 0;
      let gpayv = emojio >= 9771619;
      do {
         emojio /= Math.max(2, emojio);
         if (gpayv) {
            break;
         }
      } while ((catagorya.endsWith(`${emojio}`)) && gpayv);
         emojio &= catagorya.length >> (Math.min(Math.abs(3), 1));
      let guide1 = 9694729 >= emojio;
      do {
         emojio |= 1;
         if (guide1) {
            break;
         }
      } while ((5 >= (catagorya.length << (Math.min(Math.abs(5), 1)))) && guide1);
      let nextx = 7581013 <= emojio;
      do {
         emojio /= Math.max(emojio, 1);
         if (nextx) {
            break;
         }
      } while (nextx && (2 == (catagorya.length | 2)));
      for (let h = 0; h < 2; h++) {
         emojio *= catagorya.length;
      }
      for (let u = 0; u < 1; u++) {
         emojio /= Math.max(catagorya.length, 5);
      }
      annera <<= Math.min(3, Math.abs(friendss.length + catagorya.length));
   if (rewind0.length == 2) {
       let containery = String.fromCharCode(109,105,100,110,105,103,104,116,95,56,95,50,55,0);
       let listd8 = String.fromCharCode(100,105,114,112,95,108,95,56,56,0);
       let moreU = 5.0;
       let ewardedR = String.fromCharCode(112,101,114,109,105,115,115,105,111,110,115,95,122,95,56,53,0);
      if (containery.length <= 4) {
         containery += `${containery.length}`;
      }
       let description_tq: Array<any> = [967, 448, 563];
       let loginr: Array<any> = [550, 157, 374];
       let corneru = 1.0;
       let untickl = 1.0;
      for (let b = 0; b < 1; b++) {
         listd8 = `${2 * parseInt(`${corneru}`)}`;
      }
          let with_lhR = 4.0;
          let hejiR = String.fromCharCode(106,95,54,49,95,102,105,116,116,105,110,103,0);
         listd8 += `${3 & description_tq.length}`;
         with_lhR -= parseFloat(`${parseInt(`${with_lhR}`) * 1}`);
         hejiR += `${hejiR.length & 2}`;
          let stringM = String.fromCharCode(118,95,54,55,95,97,99,111,108,111,114,115,0);
         containery = `${parseInt(`${moreU}`) % (Math.max(3, containery.length))}`;
         stringM += `${stringM.length}`;
      let infoy = 5064130 >= listd8.length;
      do {
         listd8 = `${parseInt(`${moreU}`)}`;
         if (infoy) {
            break;
         }
      } while (infoy && (ewardedR != String.fromCharCode(88,0)));
      if (corneru >= parseFloat(`${containery.length}`)) {
          let club0 = String.fromCharCode(103,95,57,49,95,101,118,105,99,116,105,111,110,0);
          let stationsY = 2.0;
          let datal = 0.0;
          let actionsC: Array<any> = [530, 954];
         containery = `${(String.fromCharCode(88,0) == listd8 ? listd8.length : parseInt(`${stationsY}`))}`;
         club0 += `${(club0 == String.fromCharCode(71,0) ? actionsC.length : club0.length)}`;
         stationsY -= club0.length / 1;
         datal += parseFloat(`${actionsC.length}`);
      }
      let sentryY = 7383006.0 >= corneru;
      do {
         corneru *= parseFloat(`${parseInt(`${untickl}`)}`);
         if (sentryY) {
            break;
         }
      } while ((corneru > parseFloat(`${containery.length}`)) && sentryY);
          let interstitialW = String.fromCharCode(114,101,109,97,105,110,95,108,95,50,52,0);
          let trasho = String.fromCharCode(114,117,110,95,108,95,55,48,0);
          let topicz: Map<any, any> = new Map([[String.fromCharCode(101,118,97,108,117,97,116,111,114,95,110,95,49,0),728], [String.fromCharCode(116,114,101,110,100,108,105,110,101,95,117,95,50,52,0),765]]);
         ewardedR = "2";
         interstitialW = `${topicz.size << (Math.min(trasho.length, 3))}`;
         trasho = `${trasho.length ^ interstitialW.length}`;
         topicz.set(`${interstitialW}`, topicz.size * 2);
       let group8 = 5;
       let umeng7 = 3;
      for (let j = 0; j < 2; j++) {
         moreU -= group8 - parseInt(`${corneru}`);
      }
      rewind0 = "3";
   }
   let acopy_5fa = 7012101 <= annerI;
   do {
      annerI %= Math.max(successj.length, 3);
      if (acopy_5fa) {
         break;
      }
   } while ((4 < plusx.length) && acopy_5fa);
      sendZ = new Map([[`${annerI}`, successj.length]]);
   if (3 == successj.length) {
      successj += `${navigationV.size}`;
   }
    dispatch(acceptPrivacyPolicy())
  }, [])

  const handleReadTermsPrivacy = useCallback((view: '用户协议' | '隐私政策') => {
    const checkGoBack = ({ data }) => {
      if (data.state.routes[data.state.routes.length - 1].name === 'Home') {
        setIsVisible(true);
        navigation.removeListener('state', checkGoBack);
      }
    }
    navigation.addListener('state', checkGoBack);

    navigation.navigate(view);
    setIsVisible(false);
  }, [])
  console.log('textVariants.subBody: ', textVariants.subBody);
  return (
    <>
      {isVisible &&
        <View
          style={{
            height: '100%',
            width: '100%',
            backgroundColor: colors.background + 'BB',
            position: 'absolute',
            justifyContent: 'center',
            alignItems: 'center'
          }}>
          <View style={{
            width: '75%',
            padding: 20,
            gap: spacing.l,
            backgroundColor: colors.background,
            ...styles.overlay,
          }}>
            <Text style={{ ...textVariants.header, textAlign: 'center' }}>服务协议和隐私政策</Text>
            <Text
              style={{
                ...textVariants.subBody,
                textAlign: 'center',
              }}>
              <Text>
                请你务必审慎阅读,
                充分理解“服务协议”和“隐私政策”各条款，包括但不限于：为了更好的向你提供服务，我们需要收集你的设备标识，操作日常等信息用于分析，优化应用性能。你可阅读
              </Text>
              <Text onPress={() => handleReadTermsPrivacy('用户协议')}>
                <Text style={{ color: colors.primary }}>《服务协议》</Text>
              </Text>
              <Text>和</Text>
              <Text onPress={() => handleReadTermsPrivacy('隐私政策')}>
                <Text style={{ color: colors.primary }}>《隐私政策》</Text>
              </Text>
              <Text>
                了解详细信息。如果你同意，请点击下面按钮开始接受我们的服务。
              </Text>
            </Text>
            <View style={styles.btns}>
              <TouchableOpacity style={styles.btn} onPress={handleReject}>
                <Text
                  style={{
                    ...textVariants.body,
                    ...styles.head,
                  }}>
                  暂不同意
                </Text>
              </TouchableOpacity>
              <TouchableOpacity style={styles.btn} onPress={handleAccept}>
                <Text
                  style={{
                    ...textVariants.body,
                    ...styles.head,
                    color: IS_OTHER_SKIN ? colors.confirm : colors.primary,
                  }}>
                  同意并接受
                </Text>
              </TouchableOpacity>
            </View>
          </View>
        </View>
      }
    </>
  )
}

const styles = StyleSheet.create({
  overlay: {
    borderRadius: 14,
  },
  head: {
    textAlign: 'center',
  },
  body: {
    color: 'white',
  },
  link: {
    display: 'flex',
  },
  btns: {
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
  },
  btn: {
    flex: 1,
  },
});
