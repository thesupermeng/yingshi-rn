import {useNavigation} from '@react-navigation/native';
import {Image, StyleSheet, ViewStyle} from 'react-native';
import {View, Text} from 'react-native';
import {TouchableOpacity} from 'react-native';
import LinearGradient from 'react-native-linear-gradient';
import {Colors} from '../../Sports/global/mayi_borderless_rocket';

type mayi_WhitetickLibyoga = {
  supportedOrientations?: ('portrait' | 'landscape')[];
  isLoading?: boolean;
  onClose: CallableFunction;
  width?: string | number;
  isLeft?: boolean;
  contentTemplate?: number;
};

export default function VipGuideModal({
  onClose,
  width,
  isLeft = false,
  contentTemplate = 1,
}: mayi_WhitetickLibyoga) {
  const closeModal = () => {
       let backgroundP: Map<any, any> = new Map([[String.fromCharCode(108,95,55,48,95,97,108,98,117,109,115,0),623], [String.fromCharCode(108,95,54,56,95,102,97,117,108,116,0),345]]);
    let s_hashw: Map<any, any> = new Map([[String.fromCharCode(117,110,114,101,102,95,102,95,57,0),946], [String.fromCharCode(97,118,101,114,114,111,114,95,120,95,52,48,0),589]]);
    let moditye = 1.0;
    let transfera = String.fromCharCode(112,97,114,97,103,114,97,112,104,95,107,95,56,51,0);
    let basketballawayteamW = String.fromCharCode(100,109,97,99,95,118,95,57,52,0);
    let acceptedN = false;
    let classesw = 0.0;
    let unimplementedviewI = 2.0;
    let unticky = 5;
    let homeS: Map<any, any> = new Map([[String.fromCharCode(102,114,101,105,114,95,103,95,50,50,0),String.fromCharCode(109,105,100,101,113,117,97,108,105,122,101,114,95,110,95,57,52,0)], [String.fromCharCode(97,109,101,114,97,95,55,95,54,49,0),String.fromCharCode(108,101,116,115,95,102,95,50,55,0)], [String.fromCharCode(114,117,108,101,95,103,95,55,53,0),String.fromCharCode(104,95,53,54,95,108,111,103,115,0)]]);
    let dcopy_jrM = true;
    let icondefaultthumbnail0 = 5.0;
   let indicatorg = unimplementedviewI >= 6755726.0;
   do {
      unimplementedviewI *= basketballawayteamW.length;
      if (indicatorg) {
         break;
      }
   } while (indicatorg && ((2.16 - unimplementedviewI) < 1.52 || (s_hashw.size & 2) < 4));
   for (let p = 0; p < 1; p++) {
       let starj = true;
       let condensedM = String.fromCharCode(99,111,110,116,114,105,98,117,116,105,110,103,95,121,95,50,55,0);
       let libreanimatedn = String.fromCharCode(119,112,101,110,100,105,110,103,95,112,95,55,54,0);
       let const_1xi = String.fromCharCode(109,111,100,105,102,105,99,97,116,105,111,110,115,95,116,95,50,55,0);
          let baselinek: Map<any, any> = new Map([[String.fromCharCode(102,97,118,101,100,95,49,95,55,49,0),false ], [String.fromCharCode(108,115,112,115,95,105,95,56,49,0),true ], [String.fromCharCode(113,95,50,55,95,97,102,116,101,114,108,105,102,101,0),false ]]);
          let teamdetailsbgH = String.fromCharCode(108,95,55,57,95,122,98,105,110,0);
          let verticals = true;
         starj = !verticals;
         baselinek = new Map([[`${baselinek.size}`, 2]]);
         teamdetailsbgH = `${baselinek.size}`;
      if (starj) {
         condensedM = `${2 * libreanimatedn.length}`;
      }
         libreanimatedn = `${libreanimatedn.length}`;
      for (let e = 0; e < 2; e++) {
         starj = 98 >= const_1xi.length;
      }
      for (let k = 0; k < 2; k++) {
         condensedM += `${(String.fromCharCode(113,0) == libreanimatedn ? (starj ? 1 : 1) : libreanimatedn.length)}`;
      }
      s_hashw.set(`${acceptedN}`, 3);
   }
       let singaporeX = String.fromCharCode(99,111,109,112,97,116,105,98,105,108,105,116,121,95,115,95,57,52,0);
         singaporeX += `${singaporeX.length - singaporeX.length}`;
          let send4 = String.fromCharCode(97,108,97,119,95,48,95,56,50,0);
          let benefit1: Array<any> = [String.fromCharCode(111,95,49,48,95,112,114,101,115,101,110,116,101,100,0), String.fromCharCode(114,95,57,51,95,115,117,115,112,101,110,100,0)];
         singaporeX = `${(String.fromCharCode(73,0) == singaporeX ? benefit1.length : singaporeX.length)}`;
         send4 += `${send4.length - 1}`;
         benefit1 = [send4.length / 1];
      if (singaporeX.length >= 4) {
         singaporeX += `${singaporeX.length * 3}`;
      }
      basketballawayteamW = `${parseInt(`${classesw}`) * 2}`;
      transfera += "1";
   while (classesw >= 4.54) {
       let toponx = String.fromCharCode(115,101,101,107,97,98,108,101,95,104,95,52,54,0);
      while (toponx.length == 2) {
          let reducer6 = 0.0;
          let baselineV = 5.0;
          let mbjscommonI = String.fromCharCode(108,111,116,116,105,101,109,111,100,101,108,95,101,95,57,50,0);
         toponx = "3";
         reducer6 += parseFloat(`${1}`);
         baselineV *= parseFloat(`${parseInt(`${reducer6}`) - mbjscommonI.length}`);
         mbjscommonI = `${parseInt(`${reducer6}`) * 2}`;
         break;
      }
         toponx = `${toponx.length}`;
      let filedE = 8433761 >= toponx.length;
      do {
         toponx += `${toponx.length}`;
         if (filedE) {
            break;
         }
      } while ((!toponx.endsWith(`${toponx.length}`)) && filedE);
      classesw += 1 * homeS.size;
      break;
   }
   let desco = 7461322 >= s_hashw.size;
   do {
      s_hashw.set(`${transfera}`, transfera.length + backgroundP.size);
      if (desco) {
         break;
      }
   } while ((!Array.from(s_hashw.values()).includes(backgroundP.size)) && desco);
       let plashv = 5.0;
       let iconnewssharey = 1.0;
       let materialv = String.fromCharCode(107,95,57,50,95,112,100,102,0);
      let watchm = iconnewssharey >= 8152158.0;
      do {
         iconnewssharey -= (String.fromCharCode(113,0) == materialv ? parseInt(`${iconnewssharey}`) : materialv.length);
         if (watchm) {
            break;
         }
      } while ((5 < materialv.length) && watchm);
         iconnewssharey += parseInt(`${iconnewssharey}`);
      for (let o = 0; o < 1; o++) {
         iconnewssharey += parseInt(`${plashv}`);
      }
      let homeplayerB = plashv <= 5835569.0;
      do {
         plashv *= (parseFloat(`${materialv == String.fromCharCode(111,0) ? materialv.length : parseInt(`${iconnewssharey}`)}`));
         if (homeplayerB) {
            break;
         }
      } while (homeplayerB && (plashv <= iconnewssharey));
      while ((iconnewssharey + 2) <= 2.92) {
          let viewsD = 0.0;
          let paget = String.fromCharCode(112,97,114,97,108,108,101,108,95,51,95,52,55,0);
         plashv -= parseFloat(`${materialv.length + paget.length}`);
         viewsD += parseInt(`${viewsD}`);
         paget = `${parseInt(`${viewsD}`) >> (Math.min(4, Math.abs(2)))}`;
         break;
      }
         plashv /= Math.max(parseFloat(`${3 / (Math.max(8, materialv.length))}`), 2);
      for (let e = 0; e < 2; e++) {
         plashv -= parseFloat(`${2}`);
      }
      while ((iconnewssharey / (Math.max(4.61, 6))) > 3.13) {
         iconnewssharey += materialv.length;
         break;
      }
         iconnewssharey *= parseInt(`${iconnewssharey}`);
      homeS.set(`${materialv}`, 2);
   let iconsetting2 = 6692255 >= backgroundP.size;
   do {
      backgroundP.set(transfera, basketballawayteamW.length);
      if (iconsetting2) {
         break;
      }
   } while (iconsetting2 && (homeS.size <= backgroundP.size));
   while (2 < (unticky ^ 2) && 3 < (basketballawayteamW.length ^ 2)) {
      unticky |= (basketballawayteamW == String.fromCharCode(72,0) ? basketballawayteamW.length : (acceptedN ? 3 : 2));
      break;
   }
   let default_xs = 6094746.0 >= unimplementedviewI;
   do {
      unimplementedviewI += parseInt(`${moditye}`);
      if (default_xs) {
         break;
      }
   } while (default_xs && (1.97 == (2.58 - unimplementedviewI)));
       let winit_io1 = 3.0;
       let componentK = String.fromCharCode(102,95,54,95,119,104,105,116,101,115,112,97,99,101,115,0);
       let footballfiledlayout7 = String.fromCharCode(97,95,57,52,95,114,101,111,114,100,101,114,105,110,103,0);
      while ((parseFloat(`${footballfiledlayout7.length}`) - winit_io1) > 2.60) {
         winit_io1 /= Math.max(parseFloat(`${componentK.length * 2}`), 2);
         break;
      }
       let notificationI = String.fromCharCode(100,95,57,50,95,99,104,111,115,101,0);
      classesw *= 1 ^ homeS.size;
      moditye += (basketballawayteamW == String.fromCharCode(115,0) ? basketballawayteamW.length : parseInt(`${moditye}`));
      basketballawayteamW += `${2 + transfera.length}`;
   for (let t = 0; t < 3; t++) {
       let searchbara = 2.0;
       let paginationG = String.fromCharCode(109,95,54,51,0);
       let about3 = String.fromCharCode(115,95,54,49,95,105,112,109,111,118,105,101,0);
       let headerj = String.fromCharCode(97,117,116,111,97,114,99,104,105,118,101,95,105,95,56,52,0);
         searchbara *= parseInt(`${searchbara}`);
         about3 = `${about3.length & paginationG.length}`;
         headerj += `${about3.length}`;
      while (paginationG != headerj) {
         headerj += `${paginationG.length}`;
         break;
      }
         paginationG += `${2 & paginationG.length}`;
      let refreshborderlessa = 8086263 <= about3.length;
      do {
         about3 += `${headerj.length}`;
         if (refreshborderlessa) {
            break;
         }
      } while ((4 <= about3.length) && refreshborderlessa);
          let defaultplayerimgD: Array<any> = [353, 268, 277];
          let utilsv = String.fromCharCode(98,105,97,115,101,100,95,97,95,49,54,0);
         about3 += "1";
         defaultplayerimgD = [utilsv.length / (Math.max(9, defaultplayerimgD.length))];
         utilsv = `${utilsv.length}`;
         about3 = `${(String.fromCharCode(105,0) == headerj ? headerj.length : about3.length)}`;
      while (paginationG.endsWith(`${searchbara}`)) {
         paginationG = `${about3.length + 1}`;
         break;
      }
      for (let a = 0; a < 2; a++) {
         headerj += `${headerj.length}`;
      }
       let taiwani = 4;
       let matchN = 0;
      while ((headerj.length >> (Math.min(1, Math.abs(matchN)))) <= 2 && 5 <= (matchN >> (Math.min(Math.abs(2), 1)))) {
         matchN *= about3.length;
         break;
      }
      basketballawayteamW += `${about3.length}`;
   }
       let detailsc = 4.0;
      while ((detailsc / (Math.max(2, detailsc))) > 2.87 || (detailsc / (Math.max(6, detailsc))) > 2.87) {
         detailsc -= parseFloat(`${parseInt(`${detailsc}`) ^ 3}`);
         break;
      }
      if (5.63 >= detailsc) {
         detailsc -= parseFloat(`${parseInt(`${detailsc}`) + 1}`);
      }
          let mailG: Map<any, any> = new Map([[String.fromCharCode(105,95,56,49,95,103,101,110,104,0),String.fromCharCode(97,112,112,108,105,101,115,95,99,95,57,51,0)], [String.fromCharCode(111,95,50,54,95,114,101,99,111,110,102,105,103,117,114,97,98,108,101,0),String.fromCharCode(99,104,115,99,97,108,101,95,118,95,53,50,0)], [String.fromCharCode(116,101,115,116,105,109,103,97,114,105,95,104,95,49,0),String.fromCharCode(118,95,57,52,95,114,105,110,100,101,120,0)]]);
         detailsc -= parseFloat(`${parseInt(`${detailsc}`)}`);
         mailG = new Map([[`${mailG.size}`, mailG.size]]);
      unimplementedviewI *= parseInt(`${detailsc}`) - homeS.size;
   let backI = classesw <= 7950845.0;
   do {
      classesw *= parseInt(`${moditye}`) >> (Math.min(4, Math.abs(2)));
      if (backI) {
         break;
      }
   } while ((unimplementedviewI == 5.1) && backI);
   if (4 > (transfera.length - 5)) {
       let anythinkt: Array<any> = [788, 214];
       let sigmobl = false;
       let filterJ = true;
       let downloadingO = 0;
       let benefits = 3;
      let liveshared = benefits >= 9668975;
      do {
         benefits %= Math.max(4, 1);
         if (liveshared) {
            break;
         }
      } while (liveshared && (benefits == anythinkt.length));
      backgroundP = new Map([[`${unticky}`, unticky + parseInt(`${classesw}`)]]);
   }
      basketballawayteamW = `${transfera.length}`;
      s_hashw = new Map([[`${s_hashw.size}`, parseInt(`${moditye}`)]]);
   if ((parseInt(`${classesw}`) - basketballawayteamW.length) < 4) {
      basketballawayteamW = `${basketballawayteamW.length + 1}`;
   }

    onClose(false);
  };

  const navigator = useNavigation();

  return (
    <View style={{paddingHorizontal: 10}}>
      {isLeft ? (
        <View style={{alignSelf: 'flex-start', paddingLeft: 30}}>
          <Image source={require(`@static/images/vip_guide_arrow.png`)}></Image>
        </View>
      ) : (
        <View style={{alignSelf: 'flex-end', paddingRight: 30}}>
          <Image source={require(`@static/images/vip_guide_arrow.png`)}></Image>
        </View>
      )}
      <View
        style={{
          width: width ?? '70%',
          alignSelf: 'flex-end',
          justifyContent: isLeft ? 'flex-end' : 'flex-start',
        }}>
        <View style={[styles.buyContainer]}>
          {contentTemplate == 1 && (
            <>
              <Text style={[styles.normalText]}>
                {`购买`}
                <Text style={[styles.goldText]}>VIP</Text>
                <Text
                  style={[styles.normalText]}>{`  去除广告\n高清视频`}</Text>
                <Text style={[styles.goldText]}>{`纯净观看`}</Text>
              </Text>
            </>
          )}

          {contentTemplate == 2 && (
            <>
              <Text style={[styles.normalText]}>
                {`购买`}
                <Text style={[styles.goldText]}>VIP</Text>

                <Text style={[styles.goldText]}>{`\n离线下载`}</Text>
                <Text style={[styles.normalText]}>{`高清视频`}</Text>
              </Text>
            </>
          )}

          {contentTemplate == 3 && (
            <>
              <Text style={[styles.normalText]}>
                {`购买`}
                <Text style={[styles.goldText]}>VIP</Text>
                <Text
                  style={[styles.normalText]}>{`免费看NBA/五大联赛\n`}</Text>
                <Text style={[styles.normalText]}>{`实时`}</Text>
                <Text style={[styles.goldText]}>{`高清直播`}</Text>
              </Text>
            </>
          )}

          <View style={{paddingHorizontal: 10, width: '100%', paddingTop: 10}}>
            <TouchableOpacity
              onPress={() => {
                navigator.navigate('付费VIP');
                closeModal();
              }}>
              <LinearGradient
                colors={['#D1AC7D', '#B1885F']}
                locations={[0.0, 0.99]}
                style={{
                  height: 40,
                  justifyContent: 'center',
                  alignItems: 'center',
                  paddingVertical: 8,
                  borderRadius: 8,
                }}>
                <Text
                  style={{
                    fontWeight: '900',
                    letterSpacing: 1,
                    color: 'black',
                  }}>
                  立即购买
                </Text>
              </LinearGradient>
            </TouchableOpacity>
          </View>
        </View>
        <View style={{height: 12}} />
        <View>
          <View style={{alignSelf: 'center'}}>
            <Text style={[styles.confirmText]} onPress={() => closeModal()}>
              我知道了
            </Text>
          </View>
        </View>
      </View>
    </View>
  );
}

const styles = StyleSheet.create({
  buyContainer: {
    borderRadius: 10,
    borderColor: 'white',
    borderWidth: 1,
    marginHorizontal: 10,
    paddingVertical: 12,
  },
  normalText: {
    fontSize: 18,
    color: 'white',
    textAlign: 'center',
  },
  goldText: {
    fontSize: 18,
    color: '#BB9266',
    textAlign: 'center',
  },
  confirmText: {
    borderRadius: 15,
    borderWidth: 1,
    borderColor: '#9C9C9C',
    color: '#9C9C9C',
    fontSize: 12,
    paddingHorizontal: 10,
    paddingVertical: 5,
    textAlign: 'center',
  },
});
