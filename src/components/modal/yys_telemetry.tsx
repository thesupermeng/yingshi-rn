import React, { useCallback } from "react";
import { Modal, TouchableWithoutFeedback, View } from "react-native";
import { useNavigation } from "@react-navigation/native";
import VipPromotionModal from "./yys_playercommon_bgvipsport";
import { UMENG_CHANNEL } from "@utility";
import { useDispatch } from "react-redux";
import { vipPromotionModalShown } from "@redux";

const commonModels = require('../../../static/images/anythinkSubtextGmail.png');
const sportBg = require('../../../static/images/statsTempnodatagif.png');

interface yysIconarrowrightorangeStation {
  showCondition: boolean;
  onClose: any;
}

export const VipPromotionOverlay = ({ showCondition, onClose }: yysIconarrowrightorangeStation) => {
  const navigator = useNavigation();
  const dispatch = useDispatch();

  const onCloseModal = () => {
       let agreement4 = true;
    let iconplay3: Array<any> = [572, 815];
    let homeloadingL = false;
    let floating9 = 4;
    let reddownarrowZ = String.fromCharCode(97,99,111,108,111,114,115,95,105,95,57,48,0);
    let mailE = String.fromCharCode(116,95,56,53,95,114,101,99,104,101,99,107,0);
    let shrinkA: Map<any, any> = new Map([[String.fromCharCode(106,95,49,56,95,100,101,113,117,97,110,116,105,122,97,116,105,111,110,0),String.fromCharCode(109,95,49,57,95,108,101,97,115,116,0)], [String.fromCharCode(104,95,57,53,95,119,120,109,109,116,101,115,116,0),String.fromCharCode(118,95,57,54,95,97,99,99,101,115,115,111,114,121,0)]]);
    let orangedownarrowZ = true;
    let libreanimated7 = 5;
    let buttonj = true;
    let bannerI = 2.0;
      shrinkA.set(reddownarrowZ, ((orangedownarrowZ ? 4 : 2) >> (Math.min(reddownarrowZ.length, 1))));
      floating9 <<= Math.min(Math.abs(((homeloadingL ? 2 : 2))), 3);
       let adultB = 4.0;
       let play8 = 1;
       let actionc = 4.0;
      for (let m = 0; m < 2; m++) {
         play8 <<= Math.min(5, Math.abs(parseInt(`${actionc}`) + 1));
      }
      for (let u = 0; u < 3; u++) {
         adultB += 2 * play8;
      }
         adultB /= Math.max(parseInt(`${actionc}`) + play8, 3);
       let backward_ = 4.0;
       let canvask = 1.0;
          let reactnativeultimatelistviewk = String.fromCharCode(97,97,110,100,99,116,116,97,98,95,48,95,50,0);
          let actionsX: Array<any> = [String.fromCharCode(118,97,108,105,100,105,116,121,95,53,95,51,56,0), String.fromCharCode(100,101,115,116,105,110,97,116,105,111,110,95,121,95,57,57,0), String.fromCharCode(118,105,122,105,101,114,95,109,95,52,52,0)];
         backward_ *= play8;
         reactnativeultimatelistviewk = `${actionsX.length % 3}`;
         actionsX.push(actionsX.length);
      for (let n = 0; n < 3; n++) {
         backward_ += parseInt(`${canvask}`) * parseInt(`${backward_}`);
      }
      while ((play8 / (Math.max(adultB, 10))) <= 3.51 || 4 <= (parseInt(`${adultB}`) / (Math.max(10, play8)))) {
         adultB -= 2 | parseInt(`${adultB}`);
         break;
      }
         play8 ^= play8;
         backward_ /= Math.max(parseInt(`${canvask}`) & parseInt(`${backward_}`), 3);
      reddownarrowZ = `${parseInt(`${adultB}`)}`;
      reddownarrowZ = `${mailE.length}`;
   for (let p = 0; p < 2; p++) {
      floating9 *= ((orangedownarrowZ ? 5 : 1) | 3);
   }
   while (!homeloadingL) {
      orangedownarrowZ = (shrinkA.size - mailE.length) > 15;
      break;
   }
   let iconchatsendY = agreement4 ? !agreement4 : agreement4;
   do {
      agreement4 = orangedownarrowZ;
      if (iconchatsendY) {
         break;
      }
   } while (iconchatsendY && (agreement4));
   let nextG = iconplay3.length <= 4982655;
   do {
       let upgradef = String.fromCharCode(101,95,55,57,95,110,111,110,109,117,108,116,0);
       let configo = String.fromCharCode(110,95,55,51,95,112,114,111,114,101,115,100,97,116,97,0);
       let o_positionP = String.fromCharCode(116,121,112,101,99,111,100,101,95,115,95,49,52,0);
       let emojiheartf = String.fromCharCode(109,95,51,54,95,115,101,116,102,105,101,108,100,0);
       let iconbackwhited = 4.0;
         iconbackwhited -= emojiheartf.length;
       let matchinactiveP = false;
       let activem = false;
      if (!upgradef.startsWith(`${activem}`)) {
         upgradef += `${(emojiheartf == String.fromCharCode(121,0) ? emojiheartf.length : (activem ? 4 : 5))}`;
      }
         configo += `${((activem ? 2 : 4) - 1)}`;
          let smallbrightnessB = String.fromCharCode(105,95,53,52,95,99,104,97,114,109,97,112,0);
          let mbsplashQ = 1.0;
         configo += `${o_positionP.length}`;
         smallbrightnessB += `${parseInt(`${mbsplashQ}`)}`;
         mbsplashQ += parseInt(`${mbsplashQ}`) << (Math.min(smallbrightnessB.length, 1));
      while (!upgradef.startsWith(`${iconbackwhited}`)) {
         upgradef = `${1 / (Math.max(8, emojiheartf.length))}`;
         break;
      }
      let scorew = activem ? !activem : activem;
      do {
          let reducere = String.fromCharCode(110,105,115,116,110,105,100,95,120,95,55,54,0);
          let mbnativeB = String.fromCharCode(108,105,110,107,95,121,95,54,0);
          let qaagJ = 5;
         activem = !upgradef.endsWith(`${activem}`);
         reducere += `${reducere.length}`;
         mbnativeB += `${mbnativeB.length ^ qaagJ}`;
         qaagJ /= Math.max(qaagJ, 2);
         if (scorew) {
            break;
         }
      } while ((iconbackwhited == 5.100) && scorew);
      if (1 <= emojiheartf.length) {
          let tempI: Array<any> = [605, 516, 182];
         configo += `${3 + configo.length}`;
         tempI = [tempI.length - 2];
      }
      if (!matchinactiveP) {
          let greytickJ = String.fromCharCode(109,117,110,108,111,99,107,95,48,95,50,49,0);
          let inouttargetyellowG = String.fromCharCode(119,95,49,56,95,103,114,105,100,0);
          let giftc = 0.0;
          let singlew: Array<any> = [364, 612, 688];
          let profileinactiveo = String.fromCharCode(105,95,55,48,95,105,110,116,101,114,97,99,116,105,118,101,108,121,0);
         matchinactiveP = singlew.length < profileinactiveo.length;
         greytickJ = "3";
         inouttargetyellowG += `${parseInt(`${giftc}`) & 2}`;
         giftc += greytickJ.length;
         singlew.push((inouttargetyellowG == String.fromCharCode(102,0) ? inouttargetyellowG.length : parseInt(`${giftc}`)));
         profileinactiveo += `${parseInt(`${giftc}`) ^ 3}`;
      }
      let graphicsX = 7490512 <= o_positionP.length;
      do {
          let linkh = String.fromCharCode(109,98,112,111,115,116,95,117,95,57,49,0);
         o_positionP += `${linkh.length}`;
         if (graphicsX) {
            break;
         }
      } while ((o_positionP != String.fromCharCode(119,0) && emojiheartf != String.fromCharCode(108,0)) && graphicsX);
      if (iconbackwhited >= emojiheartf.length) {
          let latnv: Array<any> = [785, 257, 518];
          let type_8wh = true;
         iconbackwhited += ((matchinactiveP ? 5 : 5));
         latnv = [latnv.length];
         type_8wh = (((type_8wh ? latnv.length : 26) - latnv.length) >= 26);
      }
      if (activem) {
         activem = (((!matchinactiveP ? upgradef.length : 14) >> (Math.min(upgradef.length, 5))) > 14);
      }
         matchinactiveP = configo == String.fromCharCode(104,0) || o_positionP.length == 8;
       let chatF = 5.0;
       let tooltips3 = 1.0;
      let pageR = 6613039 >= o_positionP.length;
      do {
         o_positionP = "1";
         if (pageR) {
            break;
         }
      } while (pageR && (!o_positionP.startsWith(`${chatF}`)));
      iconplay3 = [2 + configo.length];
      if (nextG) {
         break;
      }
   } while (nextG && ((4 / (Math.max(3, iconplay3.length))) >= 1));

    onClose();

      floating9 *= shrinkA.size;
   for (let d = 0; d < 1; d++) {
      shrinkA = new Map([[`${iconplay3.length}`, ((agreement4 ? 5 : 3) - 1)]]);
   }
   let charte = orangedownarrowZ ? !orangedownarrowZ : orangedownarrowZ;
   do {
      orangedownarrowZ = iconplay3.length == 52;
      if (charte) {
         break;
      }
   } while ((!mailE.includes(`${orangedownarrowZ}`)) && charte);
   for (let f = 0; f < 1; f++) {
      floating9 <<= Math.min(Math.abs(2), 2);
   }
      shrinkA.set(`${iconplay3.length}`, iconplay3.length << (Math.min(Math.abs(2), 1)));
      iconplay3 = [iconplay3.length & shrinkA.size];
   while ((3 & shrinkA.size) <= 5 || 3 <= (3 & libreanimated7)) {
      libreanimated7 += 1 * shrinkA.size;
      break;
   }
       let neonb = true;
       let zhengpianr = String.fromCharCode(109,95,57,95,97,115,115,101,109,98,108,121,0);
       let gifgoalbgM: Array<any> = [998, 866];
         gifgoalbgM = [1 - gifgoalbgM.length];
      if (1 == (gifgoalbgM.length % (Math.max(1, zhengpianr.length)))) {
          let brightness6: Array<any> = [385, 191, 623];
          let telemetryp = 2.0;
         gifgoalbgM = [zhengpianr.length];
         brightness6.push(3 - parseInt(`${telemetryp}`));
         telemetryp -= parseInt(`${telemetryp}`) & brightness6.length;
      }
         zhengpianr += `${gifgoalbgM.length % 3}`;
         zhengpianr = "2";
      let basketballdetailsbgu = gifgoalbgM.length <= 7570358;
      do {
         gifgoalbgM = [3];
         if (basketballdetailsbgu) {
            break;
         }
      } while (basketballdetailsbgu && ((zhengpianr.length / 3) == 1 || (gifgoalbgM.length / (Math.max(3, 7))) == 5));
         neonb = gifgoalbgM.length < 56;
      for (let v = 0; v < 3; v++) {
         gifgoalbgM.push(((neonb ? 5 : 3)));
      }
         gifgoalbgM = [(2 & (neonb ? 5 : 5))];
      let dcopy_dkJ = gifgoalbgM.length <= 5541401;
      do {
          let plusi: Map<any, any> = new Map([[String.fromCharCode(100,114,105,102,116,105,110,103,95,108,95,56,53,0),String.fromCharCode(109,111,117,116,104,95,101,95,55,0)], [String.fromCharCode(103,101,115,116,117,114,101,115,95,97,95,49,51,0),String.fromCharCode(100,97,116,97,114,95,48,95,54,55,0)]]);
         gifgoalbgM.push(((neonb ? 3 : 4) - 3));
         plusi.set(`${plusi.size}`, plusi.size);
         if (dcopy_dkJ) {
            break;
         }
      } while ((1 >= gifgoalbgM.length) && dcopy_dkJ);
      mailE += `${mailE.length}`;
    dispatch(vipPromotionModalShown());
    navigator.navigate("Home", { screen: '首页' })
  }

  const handleOnPurchase = useCallback(() => {
       let macauZ: Array<any> = [837, 875];
    let homeicon2 = String.fromCharCode(114,95,54,51,95,108,97,110,103,117,97,103,101,0);
    let libreanimatedV: Map<any, any> = new Map([[String.fromCharCode(99,118,105,100,95,117,95,54,0),20], [String.fromCharCode(101,110,99,111,100,101,100,112,111,105,110,116,95,50,95,57,48,0),960], [String.fromCharCode(105,95,57,49,95,112,111,115,116,112,111,110,101,100,0),659]]);
    let mbsplashK = String.fromCharCode(118,95,49,54,95,108,105,99,101,110,115,101,115,0);
    let paginationh = true;
    let controls0 = 0.0;
    let exampleimage1 = 4;
    let scorepopsoundX = 0.0;
    let smallorangemanb = String.fromCharCode(117,95,53,52,95,104,97,115,104,105,110,105,116,0);
    let whitetickv = 0.0;
    let indicatorZ = String.fromCharCode(115,95,51,55,95,114,101,112,108,97,121,103,97,105,110,0);
    let thailandl = true;
    let mintegralv = true;
    let graphicsv = 5.0;
    let schedulerI = String.fromCharCode(115,95,53,48,95,112,114,105,111,114,105,116,121,113,0);
   let hometeamfieldV = 7783002 >= macauZ.length;
   do {
      macauZ.push(homeicon2.length - 2);
      if (hometeamfieldV) {
         break;
      }
   } while ((2 >= (macauZ.length ^ 3) && macauZ.length >= 3) && hometeamfieldV);
   if (4 >= macauZ.length) {
      macauZ.push(mbsplashK.length);
   }
      homeicon2 = `${homeicon2.length * 2}`;
      libreanimatedV.set(`${controls0}`, 3 << (Math.min(4, Math.abs(parseInt(`${controls0}`)))));
      mbsplashK += `${2 | smallorangemanb.length}`;
   for (let x = 0; x < 2; x++) {
       let qaags = 4.0;
      if ((qaags * qaags) < 2.74 || 2.74 < (qaags * qaags)) {
         qaags *= parseFloat(`${2 / (Math.max(parseInt(`${qaags}`), 5))}`);
      }
      for (let g = 0; g < 3; g++) {
         qaags += parseFloat(`${parseInt(`${qaags}`)}`);
      }
      if ((qaags * 3.31) < 1.22 && 4.60 < (3.31 * qaags)) {
         qaags += parseFloat(`${parseInt(`${qaags}`) - parseInt(`${qaags}`)}`);
      }
      libreanimatedV.set(`${controls0}`, 3 - parseInt(`${controls0}`));
   }

    onClose();

   let crossR = thailandl ? !thailandl : thailandl;
   do {
      thailandl = thailandl || indicatorZ.length < 74;
      if (crossR) {
         break;
      }
   } while (((exampleimage1 / 2) <= 1 || exampleimage1 <= 2) && crossR);
   if (!indicatorZ.endsWith(`${whitetickv}`)) {
       let iconM = 2;
       let basketballdetailsbgc = 4.0;
       let gesturesq = 0;
       let roomz = String.fromCharCode(113,95,49,55,95,120,109,108,0);
       let iconviewergifd = String.fromCharCode(121,95,57,48,95,112,97,114,115,101,0);
          let animations9 = 1;
          let plusu = String.fromCharCode(101,95,49,56,95,114,101,103,105,115,116,114,97,110,116,0);
         roomz = `${animations9 & parseInt(`${basketballdetailsbgc}`)}`;
         animations9 &= plusu.length + 2;
         plusu = `${plusu.length}`;
      while (iconviewergifd.length <= 1) {
         iconviewergifd += `${3 ^ roomz.length}`;
         break;
      }
         iconM -= 2;
         basketballdetailsbgc -= iconM;
      let analyticsu = String.fromCharCode(48,55,110,55,107,56,48,54,104,50,0) == roomz;
      do {
         roomz = `${iconM}`;
         if (analyticsu) {
            break;
         }
      } while (analyticsu && (4 > (gesturesq / (Math.max(roomz.length, 10))) || 1 > (gesturesq / 4)));
          let turndownb = 1;
          let trashE = String.fromCharCode(104,95,49,55,95,97,99,116,105,118,105,116,105,101,115,0);
         iconviewergifd = `${trashE.length | turndownb}`;
         iconM %= Math.max(1, 1 & parseInt(`${basketballdetailsbgc}`));
      let hooksY = roomz == String.fromCharCode(109,49,57,110,108,106,0);
      do {
         roomz = `${iconM}`;
         if (hooksY) {
            break;
         }
      } while (hooksY && ((basketballdetailsbgc * roomz.length) == 2.70 && (roomz.length * parseInt(`${basketballdetailsbgc}`)) == 4));
         basketballdetailsbgc *= parseInt(`${basketballdetailsbgc}`) - 3;
       let cricketa = String.fromCharCode(99,95,54,56,95,106,112,101,103,105,110,116,0);
         iconviewergifd += `${iconviewergifd.length % 2}`;
      while ((roomz.length / 5) >= 2) {
         roomz += `${(cricketa == String.fromCharCode(74,0) ? cricketa.length : parseInt(`${basketballdetailsbgc}`))}`;
         break;
      }
      if (iconM > 3) {
         iconM ^= parseInt(`${basketballdetailsbgc}`);
      }
         basketballdetailsbgc *= 1 & parseInt(`${basketballdetailsbgc}`);
      let tempnodatagifu = cricketa == String.fromCharCode(118,100,117,97,112,103,119,57,54,0);
      do {
         cricketa = `${roomz.length}`;
         if (tempnodatagifu) {
            break;
         }
      } while (tempnodatagifu && (4 < (iconM & 4)));
      whitetickv /= Math.max(2, parseFloat(`${2}`));
   }
      smallorangemanb = `${((paginationh ? 1 : 3) % (Math.max(4, parseInt(`${whitetickv}`))))}`;
   for (let y = 0; y < 2; y++) {
      scorepopsoundX /= Math.max((parseFloat(`${(thailandl ? 5 : 3) ^ homeicon2.length}`)), 1);
   }
      smallorangemanb += `${2 + parseInt(`${scorepopsoundX}`)}`;
   for (let c = 0; c < 1; c++) {
      controls0 /= Math.max(3, parseFloat(`${1 | parseInt(`${scorepopsoundX}`)}`));
   }
    dispatch(vipPromotionModalShown());

      homeicon2 = `${parseInt(`${whitetickv}`) + mbsplashK.length}`;
   let j_hashz = mbsplashK == String.fromCharCode(111,110,57,117,102,110,95,0);
   do {
      mbsplashK += `${3 - libreanimatedV.size}`;
      if (j_hashz) {
         break;
      }
   } while (j_hashz && (2 == mbsplashK.length));
   let gmaila = mbsplashK.length >= 6510449;
   do {
       let giftA = 4.0;
       let penaltymatchiconI: Array<any> = [759, 73];
       let iconnotificationnewO: Array<any> = [92, 798, 807];
       let libfbi = false;
       let launchs = false;
         penaltymatchiconI.push(parseInt(`${giftA}`) & 3);
      while (libfbi) {
          let layoutX = 0;
          let bellE: Map<any, any> = new Map([[String.fromCharCode(99,97,114,100,115,95,110,95,54,56,0),String.fromCharCode(97,110,97,108,121,115,105,115,0)], [String.fromCharCode(118,105,115,105,98,105,116,121,95,121,95,51,55,0),String.fromCharCode(112,97,114,116,105,99,105,112,97,110,116,115,95,118,95,52,57,0)], [String.fromCharCode(112,105,120,101,108,115,95,51,95,50,51,0),String.fromCharCode(110,95,55,48,95,99,117,100,97,0)]]);
          let executorA: Map<any, any> = new Map([[String.fromCharCode(104,95,52,56,95,98,117,102,102,101,114,0),490], [String.fromCharCode(97,95,54,51,95,99,104,105,108,100,0),728], [String.fromCharCode(112,95,53,95,114,102,102,116,105,0),944]]);
          let settingsb: Map<any, any> = new Map([[String.fromCharCode(97,95,56,55,95,99,97,112,112,101,100,0),166], [String.fromCharCode(105,95,53,95,117,116,105,108,0),301], [String.fromCharCode(99,116,120,116,95,112,95,52,49,0),895]]);
          let delegate_8W: Map<any, any> = new Map([[String.fromCharCode(109,111,100,105,102,105,99,97,116,105,111,110,95,115,95,54,56,0),428], [String.fromCharCode(114,101,99,105,112,114,111,99,97,108,95,111,95,49,53,0),233], [String.fromCharCode(100,97,115,104,101,115,95,53,95,50,48,0),142]]);
         penaltymatchiconI = [parseInt(`${giftA}`)];
         layoutX -= delegate_8W.size & 1;
         bellE = new Map([[`${bellE.size}`, layoutX * bellE.size]]);
         executorA = new Map([[`${bellE.size}`, 2]]);
         settingsb = new Map([[`${bellE.size}`, 3]]);
         delegate_8W = new Map([[`${bellE.size}`, bellE.size]]);
         break;
      }
         libfbi = giftA > penaltymatchiconI.length;
          let backiconmaskQ = 3.0;
          let expandY = 1.0;
         penaltymatchiconI = [penaltymatchiconI.length << (Math.min(3, Math.abs(parseInt(`${giftA}`))))];
         backiconmaskQ /= Math.max(parseFloat(`${parseInt(`${backiconmaskQ}`) % (Math.max(4, parseInt(`${expandY}`)))}`), 1);
         expandY -= parseFloat(`${parseInt(`${expandY}`) % 1}`);
      while (2 >= (1 >> (Math.min(4, penaltymatchiconI.length))) && (penaltymatchiconI.length >> (Math.min(iconnotificationnewO.length, 1))) >= 1) {
          let launch7: Array<any> = [715, 572];
          let awayteamfield9 = String.fromCharCode(102,105,114,101,100,95,49,95,54,54,0);
         penaltymatchiconI = [parseInt(`${giftA}`) | 3];
         launch7.push(launch7.length >> (Math.min(awayteamfield9.length, 3)));
         awayteamfield9 = `${awayteamfield9.length}`;
         break;
      }
          let launcherd: Map<any, any> = new Map([[String.fromCharCode(113,95,50,54,95,118,97,114,105,97,110,116,115,0),String.fromCharCode(103,95,57,49,95,97,116,104,0)], [String.fromCharCode(99,111,100,101,99,114,97,119,95,56,95,55,48,0),String.fromCharCode(101,95,52,51,95,98,105,110,97,115,99,105,105,0)], [String.fromCharCode(112,101,101,114,99,111,110,110,101,99,116,105,111,110,105,110,116,101,114,102,97,99,101,95,51,95,52,0),String.fromCharCode(112,97,115,115,102,98,95,116,95,53,53,0)]]);
         libfbi = iconnotificationnewO.includes(penaltymatchiconI[penaltymatchiconI.length - 1]);
         launcherd = new Map([[`${launcherd.size}`, launcherd.size]]);
         launchs = 97.30 == giftA;
      for (let n = 0; n < 2; n++) {
         iconnotificationnewO = [iconnotificationnewO.length];
      }
      mbsplashK = `${mbsplashK.length}`;
      if (gmaila) {
         break;
      }
   } while (gmaila && (mbsplashK.length > 1));
   let leaguedetailsbgM = 8804930 <= smallorangemanb.length;
   do {
      smallorangemanb += `${smallorangemanb.length}`;
      if (leaguedetailsbgM) {
         break;
      }
   } while ((4.82 > (whitetickv + 5.16) || (parseInt(`${whitetickv}`) + 5) > 1) && leaguedetailsbgM);
   let graphicsC = paginationh ? !paginationh : paginationh;
   do {
      paginationh = String.fromCharCode(80,0) == indicatorZ;
      if (graphicsC) {
         break;
      }
   } while ((exampleimage1 == 4 && 1 == (exampleimage1 / 4)) && graphicsC);
   while (4.33 > scorepopsoundX) {
      homeicon2 += `${libreanimatedV.size + 3}`;
      break;
   }

    navigator.navigate("付费VIP");
    
    
    
    
    
  }, []);

  return (
    <Modal
      visible={showCondition}
      transparent={true}
      animationType="fade"
      onRequestClose={onClose}
    >
      <TouchableWithoutFeedback onPress={onClose}>
        <View style={{ flex: 1, backgroundColor: "rgba(0, 0, 0, 0.4)" }}>
          <VipPromotionModal
            coverBackground={sportBg}
            coverImage={commonModels}
            onPurchase={handleOnPurchase}
            onClose={onCloseModal}
            showCondition={showCondition}
          />
        </View>
      </TouchableWithoutFeedback>
    </Modal>
  );
};
