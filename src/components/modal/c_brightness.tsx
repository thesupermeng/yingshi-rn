import React, { Suspense, useCallback, useEffect, useState } from 'react';
import { View, Text, TouchableOpacity } from 'react-native';
import VipModal from './d_clear';
import { useNavigation, useTheme } from '@react-navigation/native';
import AsyncStorage from '@react-native-async-storage/async-storage';
import { useAppDispatch } from '@hooks/kg_index';
import {
  showLoginAction,
} from '@redux/actions/a_switch';

interface XFillButton { }

export default function VipOverlay({ }: XFillButton) {
  const [showVIPOverlay, setShowVIPOverlay] = useState(false);

  const hideVipPrompt = useCallback(async () => {
       let a_imageY = 2;
    let episodesT: Map<any, any> = new Map([[String.fromCharCode(97,95,55,53,95,99,108,111,115,101,115,0),115], [String.fromCharCode(115,95,50,95,97,108,97,99,100,115,112,0),530]]);
    let stepu = false;
    let hongkongA: Array<any> = [838, 442, 989];
    let vietnam6 = String.fromCharCode(98,95,56,51,95,118,99,111,109,98,105,110,101,0);
    let actionv = 1;
    let submitc = String.fromCharCode(100,105,109,109,105,110,103,95,50,95,57,53,0);
    let settingsv = String.fromCharCode(122,95,55,57,95,113,112,101,108,100,115,112,0);
    let stationR = true;
    let gpayF: Array<any> = [131, 588, 156];
    let middlewareT = true;
    let const_fpP: Map<any, any> = new Map([[String.fromCharCode(105,110,99,114,98,108,111,98,95,119,95,50,0),779], [String.fromCharCode(101,120,99,108,117,115,105,118,101,95,122,95,57,49,0),377], [String.fromCharCode(102,97,110,99,121,95,118,95,52,50,0),35]]);
    let pausex = String.fromCharCode(120,95,50,48,95,115,112,108,105,116,0);
    let country7 = false;
      stepu = !stepu;
   for (let j = 0; j < 1; j++) {
       let icon0 = String.fromCharCode(114,101,112,114,101,112,97,114,101,95,56,95,52,48,0);
       let viewsv = String.fromCharCode(115,105,110,103,108,101,116,97,98,108,101,95,107,95,54,48,0);
       let umengi = String.fromCharCode(99,102,101,110,99,95,103,95,57,49,0);
         umengi = `${1 % (Math.max(2, umengi.length))}`;
      while (4 >= viewsv.length || 4 >= icon0.length) {
          let bcopy_ys = String.fromCharCode(112,114,105,109,97,114,105,108,121,95,56,95,51,48,0);
          let auto_4E: Map<any, any> = new Map([[String.fromCharCode(122,95,50,56,95,99,111,100,101,98,111,111,107,115,0),897], [String.fromCharCode(108,111,97,116,95,103,95,56,50,0),736], [String.fromCharCode(117,118,109,118,95,115,95,55,49,0),364]]);
         icon0 += "2";
         bcopy_ys += `${auto_4E.size % (Math.max(bcopy_ys.length, 3))}`;
         auto_4E.set(`${bcopy_ys}`, auto_4E.size);
         break;
      }
      if (umengi == String.fromCharCode(78,0)) {
         viewsv = `${icon0.length & viewsv.length}`;
      }
         icon0 = `${1 << (Math.min(1, icon0.length))}`;
      for (let k = 0; k < 3; k++) {
         viewsv += `${umengi.length}`;
      }
      if (viewsv.length == icon0.length) {
         viewsv = `${viewsv.length | umengi.length}`;
      }
          let moonX: Array<any> = [440, 374];
          let overl = String.fromCharCode(102,108,97,115,104,105,110,103,95,109,95,51,57,0);
          let actionsQ = 4.0;
         viewsv += `${parseInt(`${actionsQ}`)}`;
         moonX = [moonX.length];
         overl = `${moonX.length}`;
         actionsQ *= parseFloat(`${overl.length % (Math.max(8, moonX.length))}`);
         viewsv += "1";
          let leftu = String.fromCharCode(97,95,52,52,95,105,112,97,100,105,102,102,0);
         viewsv += `${viewsv.length}`;
         leftu += `${2 / (Math.max(10, leftu.length))}`;
      settingsv = `${a_imageY}`;
   }
   let typings = stationR ? !stationR : stationR;
   do {
       let whatsappE = String.fromCharCode(99,111,109,112,97,114,101,102,95,114,95,49,51,0);
       let models1 = 1;
      if ((models1 * whatsappE.length) > 1 && (1 * whatsappE.length) > 1) {
          let container6 = true;
          let down5 = String.fromCharCode(99,117,114,114,101,110,116,95,116,95,57,53,0);
         models1 -= 1;
         container6 = down5 == down5;
      }
       let list8 = 2.0;
       let nativeP = 0.0;
      let nalyticsx = whatsappE.length <= 7832739;
      do {
         whatsappE = "2";
         if (nalyticsx) {
            break;
         }
      } while ((whatsappE.length == nativeP) && nalyticsx);
          let sansJ = 0.0;
          let layout3 = false;
         whatsappE = "1";
         sansJ -= ((layout3 ? 2 : 3) % (Math.max(parseInt(`${sansJ}`), 4)));
         layout3 = 60.66 <= sansJ || !layout3;
      while (2 < (1 | whatsappE.length) || 1 < (1 | models1)) {
         models1 >>= Math.min(Math.abs((String.fromCharCode(67,0) == whatsappE ? whatsappE.length : models1)), 3);
         break;
      }
         whatsappE = "1";
      stationR = hongkongA.length == 100 || 100 == settingsv.length;
      if (typings) {
         break;
      }
   } while ((2 >= gpayF.length && (2 | gpayF.length) >= 5) && typings);
      episodesT = new Map([[`${actionv}`, (actionv & (stationR ? 2 : 5))]]);
   for (let o = 0; o < 1; o++) {
      gpayF.push(((stepu ? 4 : 3) - a_imageY));
   }
       let basketball_ = 4.0;
       let right1 = 4;
       let showj: Map<any, any> = new Map([[String.fromCharCode(100,117,112,108,101,120,95,48,95,57,53,0),646], [String.fromCharCode(97,110,99,105,108,108,97,114,121,95,114,95,56,50,0),815], [String.fromCharCode(116,114,97,110,115,102,101,114,97,98,108,101,95,52,95,52,51,0),828]]);
      if (2.46 >= basketball_) {
         right1 <<= Math.min(Math.abs(1 & right1), 4);
      }
      if (showj.size >= right1) {
          let shootV: Map<any, any> = new Map([[String.fromCharCode(119,95,56,52,95,115,108,97,110,116,0),621], [String.fromCharCode(113,95,49,49,95,115,101,114,105,97,108,110,111,0),754]]);
          let fillf = String.fromCharCode(110,95,56,53,95,98,101,110,105,103,110,0);
          let i_unlocks = 3.0;
          let indicator8 = String.fromCharCode(114,95,50,55,95,112,114,111,100,0);
          let privilegeY: Map<any, any> = new Map([[String.fromCharCode(114,111,116,97,116,101,100,95,55,95,54,50,0),617], [String.fromCharCode(117,108,111,110,103,95,55,95,53,50,0),424]]);
         right1 /= Math.max(3, 5);
         shootV = new Map([[`${privilegeY.size}`, indicator8.length]]);
         fillf += `${parseInt(`${i_unlocks}`)}`;
         i_unlocks /= Math.max(5, parseFloat(`${shootV.size}`));
         indicator8 += `${privilegeY.size - 3}`;
      }
      while (5 >= (4 - showj.size) && 2 >= (4 * showj.size)) {
         showj = new Map([[`${right1}`, 1 | parseInt(`${basketball_}`)]]);
         break;
      }
          let detailT = 1.0;
          let plash_: Array<any> = [249, 917];
         showj.set(`${detailT}`, parseInt(`${detailT}`));
         plash_.push(plash_.length);
      if (4.100 == basketball_) {
         right1 &= 1;
      }
      let bootsplash5 = 9565901 <= right1;
      do {
         right1 -= right1 >> (Math.min(Math.abs(2), 1));
         if (bootsplash5) {
            break;
         }
      } while ((5 >= (5 * right1) && (showj.size * 5) >= 2) && bootsplash5);
          let sport7 = 2.0;
          let source3 = 2.0;
         showj.set(`${right1}`, right1 << (Math.min(Math.abs(parseInt(`${basketball_}`)), 4)));
         sport7 -= parseFloat(`${parseInt(`${sport7}`) % (Math.max(8, parseInt(`${source3}`)))}`);
         source3 += parseInt(`${source3}`) | 3;
         right1 ^= showj.size & parseInt(`${basketball_}`);
      for (let f = 0; f < 3; f++) {
         right1 *= right1;
      }
      stationR = String.fromCharCode(114,0) == submitc;
   let emptyy = vietnam6 == String.fromCharCode(55,115,114,109,120,111,114,54,0);
   do {
      vietnam6 = `${3 >> (Math.min(4, settingsv.length))}`;
      if (emptyy) {
         break;
      }
   } while ((const_fpP.size > vietnam6.length) && emptyy);
   while (!stationR) {
      settingsv += `${vietnam6.length + 1}`;
      break;
   }
   for (let p = 0; p < 3; p++) {
       let unselectedg = 1.0;
       let searchd = 1.0;
       let resultO: Array<any> = [280, 535];
       let c_imageI = true;
       let calendarI = 5.0;
         unselectedg += parseFloat(`${resultO.length}`);
      for (let m = 0; m < 1; m++) {
         calendarI *= (parseFloat(`${(c_imageI ? 2 : 5) % (Math.max(parseInt(`${calendarI}`), 2))}`));
      }
      for (let y = 0; y < 1; y++) {
          let gmailX = String.fromCharCode(105,95,49,49,95,97,112,112,101,110,100,105,110,103,0);
          let promotionp = 5.0;
          let regengn = 2.0;
         searchd += parseFloat(`${parseInt(`${calendarI}`) | 2}`);
         gmailX += `${3 ^ parseInt(`${regengn}`)}`;
         promotionp /= Math.max(parseInt(`${regengn}`), 4);
      }
      for (let d = 0; d < 2; d++) {
         searchd -= parseFloat(`${3 * parseInt(`${unselectedg}`)}`);
      }
          let liveg = String.fromCharCode(109,95,56,53,95,97,99,99,101,108,101,114,97,116,101,0);
          let darkF = true;
         searchd *= parseFloat(`${parseInt(`${unselectedg}`) ^ 1}`);
         liveg = `${((darkF ? 1 : 5))}`;
         darkF = !liveg.startsWith(`${darkF}`);
       let fillH = String.fromCharCode(105,95,57,57,95,114,116,108,0);
      let activeg = searchd >= 8370039.0;
      do {
         searchd *= (parseFloat(`${1 + (c_imageI ? 1 : 2)}`));
         if (activeg) {
            break;
         }
      } while (activeg && (4 < (5 * resultO.length) && (resultO.length * parseInt(`${searchd}`)) < 5));
         c_imageI = calendarI <= 20.53 && !c_imageI;
          let main_zN = String.fromCharCode(117,95,55,53,95,114,101,118,101,97,108,101,100,0);
         fillH = "2";
         main_zN += "2";
       let arrowX = String.fromCharCode(101,118,115,105,103,110,97,108,95,55,95,52,52,0);
       let commono = String.fromCharCode(114,101,116,114,105,101,118,101,95,54,95,50,57,0);
      let membershipA = 5805179 <= arrowX.length;
      do {
          let termse: Map<any, any> = new Map([[String.fromCharCode(108,105,98,99,111,100,101,99,95,97,95,53,48,0),81], [String.fromCharCode(120,95,50,50,95,97,100,108,101,114,0),603]]);
          let matchN = String.fromCharCode(122,95,57,48,95,115,116,101,112,0);
          let rewind6 = String.fromCharCode(115,115,105,109,118,95,103,95,56,0);
          let hongkong9 = false;
         arrowX += "2";
         termse = new Map([[matchN, matchN.length & 2]]);
         rewind6 = `${rewind6.length}`;
         hongkong9 = rewind6.length == 59;
         if (membershipA) {
            break;
         }
      } while ((!arrowX.startsWith(`${c_imageI}`)) && membershipA);
      for (let i = 0; i < 1; i++) {
          let gift1 = 5.0;
         fillH += `${parseInt(`${searchd}`)}`;
         gift1 *= parseFloat(`${2}`);
      }
          let dark3 = String.fromCharCode(116,116,97,100,115,112,95,53,95,56,49,0);
         searchd /= Math.max(4, parseFloat(`${1 / (Math.max(7, parseInt(`${calendarI}`)))}`));
         dark3 += `${dark3.length * dark3.length}`;
      if (5.77 <= (1.14 + unselectedg) && 1.14 <= (calendarI + unselectedg)) {
          let inactiveN: Array<any> = [String.fromCharCode(115,117,98,116,108,101,95,111,95,52,55,0), String.fromCharCode(117,95,53,52,95,109,97,115,107,105,110,103,0)];
          let frame_8qZ = true;
          let othery = true;
         unselectedg -= (parseFloat(`${(c_imageI ? 5 : 3) >> (Math.min(Math.abs(parseInt(`${calendarI}`)), 1))}`));
         inactiveN.push(((othery ? 3 : 3) & inactiveN.length));
         frame_8qZ = inactiveN.length == 6;
         othery = (11 >= (inactiveN.length | (frame_8qZ ? inactiveN.length : 11)));
      }
      if ((1 & fillH.length) < 3 || 4 < (1 & fillH.length)) {
          let themeJ: Map<any, any> = new Map([[String.fromCharCode(106,95,54,53,95,115,104,101,101,116,0),776], [String.fromCharCode(119,95,49,48,0),907], [String.fromCharCode(122,95,57,49,95,104,101,97,100,0),729]]);
          let paginationN = String.fromCharCode(98,95,52,51,95,116,111,117,112,112,101,114,0);
         calendarI *= parseFloat(`${resultO.length}`);
         themeJ.set(`${paginationN}`, (paginationN == String.fromCharCode(122,0) ? paginationN.length : themeJ.size));
      }
      hongkongA.push(submitc.length);
   }
   while (Array.from(episodesT.values()).includes(a_imageY)) {
      episodesT.set(`${actionv}`, vietnam6.length);
      break;
   }
   for (let z = 0; z < 2; z++) {
      actionv &= 3;
   }
      stepu = !stepu;
      middlewareT = gpayF.length >= settingsv.length;
      gpayF = [((stationR ? 4 : 1) + gpayF.length)];
   for (let n = 0; n < 2; n++) {
       let canvasd = 4.0;
       let acceptedR = 4;
       let penaltyW: Map<any, any> = new Map([[String.fromCharCode(112,95,56,51,95,108,97,122,105,108,121,0),439], [String.fromCharCode(114,95,50,50,95,99,111,109,98,105,110,97,116,111,114,0),559]]);
       let halfY = String.fromCharCode(115,95,54,53,95,115,113,114,116,0);
          let window_qi9 = String.fromCharCode(114,101,108,97,116,101,100,95,57,95,57,53,0);
          let statsS = String.fromCharCode(101,110,116,114,111,112,121,109,118,95,57,95,56,57,0);
          let matchn = 0.0;
         penaltyW.set(`${matchn}`, halfY.length & 1);
         window_qi9 += `${statsS.length}`;
         statsS += `${(statsS == String.fromCharCode(77,0) ? window_qi9.length : statsS.length)}`;
         matchn += 3;
          let light4 = 0.0;
          let selectionJ = String.fromCharCode(109,105,100,110,105,103,104,116,95,48,95,50,55,0);
         acceptedR >>= Math.min(Math.abs(parseInt(`${light4}`)), 3);
         light4 /= Math.max(3, parseFloat(`${selectionJ.length}`));
         selectionJ += `${2 % (Math.max(2, selectionJ.length))}`;
      for (let u = 0; u < 1; u++) {
          let membershipQ = 5.0;
          let apple8 = String.fromCharCode(97,117,100,105,116,105,110,103,95,103,95,50,53,0);
          let zhengpianp: Array<any> = [814, 977];
          let nalyticss = false;
         halfY += `${2 | parseInt(`${membershipQ}`)}`;
         membershipQ += parseFloat(`${zhengpianp.length + 1}`);
         apple8 = `${apple8.length}`;
         zhengpianp = [((nalyticss ? 5 : 1) + 3)];
      }
      if (penaltyW.get(`${acceptedR}`) != null) {
          let country4 = 1.0;
          let sansa = 4.0;
         acceptedR /= Math.max(1, 1 >> (Math.min(Math.abs(acceptedR), 3)));
         country4 *= parseFloat(`${3 / (Math.max(8, parseInt(`${sansa}`)))}`);
         sansa += parseFloat(`${parseInt(`${country4}`) >> (Math.min(4, Math.abs(2)))}`);
      }
      for (let f = 0; f < 1; f++) {
         acceptedR /= Math.max(3, acceptedR >> (Math.min(Math.abs(3), 2)));
      }
       let listM: Map<any, any> = new Map([[String.fromCharCode(108,95,50,56,95,105,110,102,111,0),389], [String.fromCharCode(115,121,110,116,97,120,95,103,95,53,56,0),337]]);
         listM = new Map([[`${penaltyW.size}`, listM.size & 1]]);
       let scheduleb = 0.0;
         halfY += "1";
          let share4 = String.fromCharCode(118,95,49,57,95,115,112,101,110,100,101,114,0);
          let modea: Array<any> = [512, 599];
          let private_i4 = String.fromCharCode(102,100,101,99,95,100,95,54,54,0);
         penaltyW = new Map([[`${penaltyW.size}`, 3 | listM.size]]);
         share4 += "2";
         modea = [1];
         private_i4 = `${modea.length % 1}`;
         halfY += `${listM.size + parseInt(`${scheduleb}`)}`;
       let sina8 = 2.0;
       let fastP = 0.0;
      stationR = const_fpP.size >= 82 || stationR;
      canvasd -= parseFloat(`${parseInt(`${canvasd}`)}`);
   }
      stepu = const_fpP.size == pausex.length;
      submitc += `${const_fpP.size | 2}`;
      stationR = 78 > const_fpP.size;
   while ((a_imageY * 1) < 2 || !stepu) {
       let middlewareI = String.fromCharCode(117,95,54,57,95,101,97,114,108,105,101,115,116,0);
       let blacklistz = 5.0;
       let plusx = String.fromCharCode(112,97,108,109,95,116,95,50,48,0);
       let philippinesq = 5;
       let greenE = 0;
          let bellt = String.fromCharCode(121,95,54,52,95,100,114,97,103,0);
          let movies6 = 2.0;
          let wnewso = String.fromCharCode(108,95,51,55,95,114,101,100,117,99,101,100,0);
         philippinesq -= bellt.length & wnewso.length;
         bellt = `${2 % (Math.max(parseInt(`${movies6}`), 3))}`;
         movies6 += parseFloat(`${parseInt(`${movies6}`) << (Math.min(5, Math.abs(2)))}`);
         middlewareI += `${3 >> (Math.min(5, plusx.length))}`;
       let lessn: Array<any> = [483, 446, 410];
       let dropdown4: Array<any> = [360, 496, 504];
      let dataa = plusx.length <= 8960545;
      do {
         plusx += "2";
         if (dataa) {
            break;
         }
      } while ((2 <= lessn.length) && dataa);
      for (let y = 0; y < 3; y++) {
         philippinesq <<= Math.min(Math.abs(2 / (Math.max(2, middlewareI.length))), 5);
      }
      let areah = philippinesq <= 6988088;
      do {
          let actionsb = String.fromCharCode(117,108,111,110,103,95,103,95,50,49,0);
          let champion9 = String.fromCharCode(113,95,50,50,95,100,101,102,105,110,101,100,0);
          let dropdownh = String.fromCharCode(102,95,55,49,95,110,101,108,108,121,109,111,115,101,114,0);
          let subsK: Map<any, any> = new Map([[String.fromCharCode(109,111,110,105,116,111,114,105,110,103,95,122,95,53,51,0),871], [String.fromCharCode(101,110,116,114,111,112,121,109,111,100,101,95,48,95,51,51,0),751]]);
         philippinesq >>= Math.min(3, Math.abs(dropdown4.length % 3));
         actionsb += `${3 | actionsb.length}`;
         champion9 += `${actionsb.length}`;
         dropdownh += `${dropdownh.length}`;
         subsK = new Map([[`${subsK.size}`, champion9.length / (Math.max(1, 2))]]);
         if (areah) {
            break;
         }
      } while (areah && (3 == (philippinesq / (Math.max(5, 6)))));
       let changei = true;
      if (1 >= (2 + philippinesq) && (2 + philippinesq) >= 5) {
          let zhengpianw: Array<any> = [String.fromCharCode(105,110,99,114,101,109,101,110,116,95,54,95,51,55,0), String.fromCharCode(104,95,51,54,95,119,114,105,116,101,114,0), String.fromCharCode(114,101,115,111,108,118,101,114,115,95,100,95,57,48,0)];
          let minivodg = String.fromCharCode(115,121,115,108,111,103,95,108,95,54,55,0);
          let backgroundT = true;
          let vertical_ = String.fromCharCode(116,95,56,48,95,117,110,112,97,99,107,104,105,0);
         philippinesq &= vertical_.length & 3;
         zhengpianw = [3];
         minivodg = `${minivodg.length - 3}`;
         backgroundT = zhengpianw.length <= 81;
         vertical_ = `${zhengpianw.length + 1}`;
      }
          let unselectedT = false;
          let episodeB = 3.0;
          let yellowL: Array<any> = [839, 887];
         lessn = [dropdown4.length];
         unselectedT = (87 < ((!unselectedT ? yellowL.length : 87) & yellowL.length));
         episodeB += 2 << (Math.min(Math.abs(parseInt(`${episodeB}`)), 5));
      if (!changei || middlewareI.length > 5) {
         changei = plusx.length > parseInt(`${blacklistz}`);
      }
         plusx += "1";
      let placeholderN = dropdown4.length >= 7477873;
      do {
          let tick4 = 5.0;
         dropdown4.push(dropdown4.length);
         tick4 += parseFloat(`${parseInt(`${tick4}`)}`);
         if (placeholderN) {
            break;
         }
      } while ((4 < (5 << (Math.min(3, dropdown4.length))) && 3 < (5 << (Math.min(4, dropdown4.length)))) && placeholderN);
       let minivodZ = 0.0;
      while (plusx.length >= 4) {
         plusx += "3";
         break;
      }
         greenE |= 2 - parseInt(`${blacklistz}`);
      a_imageY -= vietnam6.length;
      break;
   }
      middlewareT = String.fromCharCode(103,0) == submitc || 56 > hongkongA.length;
      a_imageY <<= Math.min(Math.abs(1), 2);
       let modalM: Array<any> = [66, 512, 124];
       let membershipR = String.fromCharCode(115,116,99,111,95,114,95,49,48,48,0);
       let thumbnailY: Map<any, any> = new Map([[String.fromCharCode(102,95,51,52,95,116,111,120,121,122,0),String.fromCharCode(109,115,101,112,115,110,114,95,55,95,52,53,0)], [String.fromCharCode(116,104,114,101,115,104,95,100,95,57,48,0),String.fromCharCode(105,109,105,116,97,116,101,95,102,95,50,54,0)], [String.fromCharCode(112,95,49,51,95,97,100,97,112,116,97,116,105,111,110,0),String.fromCharCode(98,95,53,57,95,115,101,101,100,0)]]);
      if (Array.from(thumbnailY.keys()).includes(`${modalM.length}`)) {
         modalM = [thumbnailY.size];
      }
      let shootr = thumbnailY.size >= 8544452;
      do {
         thumbnailY = new Map([[`${thumbnailY.size}`, thumbnailY.size << (Math.min(membershipR.length, 4))]]);
         if (shootr) {
            break;
         }
      } while ((4 == (3 | thumbnailY.size) || (3 | thumbnailY.size) == 1) && shootr);
      for (let i = 0; i < 1; i++) {
          let sportc = String.fromCharCode(122,95,50,55,95,109,117,108,116,105,112,108,101,114,0);
          let fastforwardm = 1.0;
          let forwardE: Map<any, any> = new Map([[String.fromCharCode(102,111,110,116,115,105,122,101,95,103,95,52,53,0),530], [String.fromCharCode(109,98,117,102,95,108,95,50,53,0),501], [String.fromCharCode(114,108,111,116,116,105,101,95,112,95,54,51,0),30]]);
         membershipR = `${modalM.length}`;
         sportc = `${parseInt(`${fastforwardm}`)}`;
         fastforwardm -= 3 << (Math.min(4, Math.abs(parseInt(`${fastforwardm}`))));
         forwardE = new Map([[sportc, (sportc == String.fromCharCode(75,0) ? parseInt(`${fastforwardm}`) : sportc.length)]]);
      }
         membershipR += `${(membershipR == String.fromCharCode(100,0) ? modalM.length : membershipR.length)}`;
      for (let t = 0; t < 2; t++) {
         modalM.push(modalM.length - thumbnailY.size);
      }
       let shootQ = 3;
       let upgradeu = String.fromCharCode(103,95,49,54,95,117,110,112,114,111,116,101,99,116,0);
       let searchbarj = String.fromCharCode(112,105,120,102,109,116,95,52,95,50,51,0);
      while (2 == (thumbnailY.size << (Math.min(membershipR.length, 5)))) {
          let langkeys = 2.0;
          let themes = 1;
          let weiboO = String.fromCharCode(112,111,114,116,114,97,105,116,95,121,95,50,51,0);
          let viewerD = 3;
          let trophyh = String.fromCharCode(108,122,109,97,95,101,95,49,56,0);
         thumbnailY.set(`${membershipR}`, membershipR.length & thumbnailY.size);
         langkeys *= (parseFloat(`${trophyh == String.fromCharCode(103,0) ? trophyh.length : weiboO.length}`));
         themes *= themes;
         weiboO += `${parseInt(`${langkeys}`) % 1}`;
         viewerD ^= weiboO.length / (Math.max(1, 5));
         break;
      }
      if (!upgradeu.includes(`${shootQ}`)) {
          let closeb: Array<any> = [811, 235];
          let slider6 = String.fromCharCode(101,95,51,51,95,114,101,115,111,108,118,101,0);
          let thailando: Array<any> = [692, 665];
         shootQ += shootQ ^ 1;
         closeb.push((slider6 == String.fromCharCode(118,0) ? slider6.length : closeb.length));
         thailando.push(slider6.length);
      }
      settingsv += `${actionv + episodesT.size}`;
      country7 = episodesT.size == 7;
       let xvodk = 0.0;
       let commentI = 4.0;
       let langI = String.fromCharCode(101,95,56,54,95,102,114,97,109,101,100,97,116,97,0);
          let routerz = String.fromCharCode(106,95,54,52,95,118,100,112,97,117,0);
          let schedule5: Map<any, any> = new Map([[String.fromCharCode(101,118,112,111,114,116,95,112,95,52,55,0),false ], [String.fromCharCode(121,95,52,48,95,118,105,101,119,0),false ], [String.fromCharCode(115,118,113,101,110,99,95,122,95,53,50,0),false ]]);
          let whistleM = String.fromCharCode(115,117,98,114,101,115,117,108,116,115,95,53,95,55,54,0);
         commentI *= whistleM.length;
         routerz = `${(String.fromCharCode(118,0) == routerz ? routerz.length : schedule5.size)}`;
         schedule5.set(routerz, schedule5.size);
         whistleM += `${routerz.length ^ schedule5.size}`;
       let nalyticsF: Map<any, any> = new Map([[String.fromCharCode(109,95,56,57,95,116,114,97,110,115,108,105,116,101,114,97,116,101,100,0),816], [String.fromCharCode(106,95,52,55,95,100,105,121,102,112,0),358]]);
       let catalogp: Map<any, any> = new Map([[String.fromCharCode(104,95,53,55,95,108,97,121,101,114,115,0),47], [String.fromCharCode(107,95,50,51,95,104,119,109,97,112,0),627], [String.fromCharCode(122,95,51,48,95,108,111,103,105,99,0),782]]);
         catalogp = new Map([[`${catalogp.size}`, 3]]);
         langI = `${parseInt(`${xvodk}`) & 1}`;
          let smallh: Array<any> = [173, 69];
          let placementl = 0;
         commentI *= 2;
         smallh = [2];
         placementl /= Math.max(4, smallh.length);
      let gesturesz = 6942339.0 >= xvodk;
      do {
         xvodk -= 3;
         if (gesturesz) {
            break;
         }
      } while (gesturesz && (xvodk <= langI.length));
       let membershipn: Array<any> = [638, 117];
      while (4.60 == commentI) {
         xvodk -= catalogp.size;
         break;
      }
      while (2.58 >= commentI) {
         nalyticsF.set(langI, 1);
         break;
      }
      submitc += `${const_fpP.size ^ 1}`;
   let watche = const_fpP.size <= 7007891;
   do {
       let otherw = String.fromCharCode(115,104,105,101,108,100,95,101,95,53,50,0);
       let trophyd = String.fromCharCode(112,95,50,57,95,101,120,105,115,116,0);
       let xcopy_iV = 5.0;
       let indexi = 5;
      while (!trophyd.includes(`${xcopy_iV}`)) {
         trophyd = `${indexi + parseInt(`${xcopy_iV}`)}`;
         break;
      }
      while (!otherw.endsWith(trophyd)) {
          let tooltipsL = true;
          let containerU: Map<any, any> = new Map([[String.fromCharCode(97,118,102,111,114,109,97,116,109,97,112,112,101,114,116,101,115,116,115,95,48,95,53,49,0),527], [String.fromCharCode(105,110,105,95,52,95,55,49,0),473]]);
          let controlU = String.fromCharCode(118,95,51,48,0);
         trophyd += "3";
         tooltipsL = tooltipsL && containerU.size > 63;
         containerU = new Map([[controlU, ((tooltipsL ? 1 : 3))]]);
         controlU = `${((tooltipsL ? 2 : 4) >> (Math.min(Math.abs(containerU.size), 4)))}`;
         break;
      }
         indexi <<= Math.min(Math.abs((otherw == String.fromCharCode(119,0) ? otherw.length : parseInt(`${xcopy_iV}`))), 5);
      if (otherw == trophyd) {
          let injuryC = String.fromCharCode(99,104,97,114,103,101,95,100,95,51,53,0);
          let interstitialf = 2.0;
          let renew1 = 5;
          let window_563 = 0.0;
          let config5 = String.fromCharCode(100,101,99,111,114,97,116,105,111,110,95,98,95,49,54,0);
         trophyd = `${indexi & 1}`;
         injuryC = `${renew1 + 1}`;
         interstitialf *= parseFloat(`${renew1}`);
         window_563 -= parseInt(`${window_563}`);
         config5 = `${(config5 == String.fromCharCode(65,0) ? config5.length : renew1)}`;
      }
         xcopy_iV *= trophyd.length;
         xcopy_iV /= Math.max(4, otherw.length + 2);
      for (let q = 0; q < 3; q++) {
          let giftS = 0.0;
          let edit0 = String.fromCharCode(120,95,57,50,95,98,105,116,115,116,114,101,97,109,0);
          let castings = String.fromCharCode(99,111,117,110,116,114,121,95,103,95,54,53,0);
          let macauC = String.fromCharCode(109,95,57,51,95,115,104,111,119,119,97,118,101,115,0);
         xcopy_iV /= Math.max(4, 2);
         giftS += parseFloat(`${macauC.length << (Math.min(castings.length, 1))}`);
         edit0 += `${edit0.length ^ macauC.length}`;
         castings = `${(String.fromCharCode(81,0) == macauC ? edit0.length : macauC.length)}`;
      }
       let long_1p: Map<any, any> = new Map([[String.fromCharCode(97,99,116,105,118,105,116,105,101,115,95,56,95,49,48,0),false ], [String.fromCharCode(112,114,111,108,111,110,103,95,114,95,57,49,0),true ]]);
      let logoutf = long_1p.size >= 6724193;
      do {
          let privacyH = 4;
          let descA: Map<any, any> = new Map([[String.fromCharCode(114,95,56,56,95,108,97,103,97,114,105,116,104,114,97,99,0),962], [String.fromCharCode(103,97,109,117,116,95,117,95,55,57,0),211]]);
          let sans2 = 1.0;
          let country_ = 5.0;
         long_1p.set(`${privacyH}`, privacyH);
         descA.set(`${sans2}`, parseInt(`${country_}`));
         sans2 /= Math.max(parseInt(`${sans2}`) << (Math.min(Math.abs(2), 3)), 5);
         if (logoutf) {
            break;
         }
      } while (logoutf && (1 <= (5 | indexi) || (long_1p.size | indexi) <= 5));
      while (otherw.endsWith(`${indexi}`)) {
          let playo = 5.0;
          let pauseR: Map<any, any> = new Map([[String.fromCharCode(108,95,56,51,95,108,101,114,112,0),257], [String.fromCharCode(107,95,53,54,95,110,105,99,101,108,121,0),681]]);
          let feedback7: Array<any> = [923, 605, 805];
         indexi /= Math.max(1, parseInt(`${xcopy_iV}`) >> (Math.min(1, Math.abs(2))));
         playo /= Math.max(5, parseFloat(`${3}`));
         pauseR = new Map([[`${pauseR.size}`, pauseR.size]]);
         feedback7 = [1];
         break;
      }
      if (otherw.length < indexi) {
          let dropdownK = String.fromCharCode(117,108,112,105,110,102,111,95,104,95,50,0);
         otherw += `${parseInt(`${xcopy_iV}`) % 3}`;
         dropdownK += `${3 | dropdownK.length}`;
      }
          let baidug = 5.0;
         indexi |= trophyd.length;
         baidug += parseFloat(`${parseInt(`${baidug}`) >> (Math.min(1, Math.abs(1)))}`);
      const_fpP.set(`${actionv}`, 1 * const_fpP.size);
      if (watche) {
         break;
      }
   } while (((vietnam6.length >> (Math.min(Math.abs(2), 5))) >= 4 && (vietnam6.length >> (Math.min(2, Math.abs(const_fpP.size)))) >= 2) && watche);

    setShowVIPOverlay(false);
  }, []);

  const dispatch = useAppDispatch();

  useEffect(() => {
    const checkFirstLaunchVipPrompted = async () => {
       let questm = 3.0;
    let giftb = 5.0;
    let miniO: Array<any> = [429, 717, 361];
    let relatedV = true;
    let anythink5: Array<any> = [956, 354];
    let sportsf: Array<any> = [596, 680, 658];
    let nterstitial8 = String.fromCharCode(109,101,116,97,98,111,100,121,95,53,95,50,51,0);
    let changem = 3.0;
    let typing0 = 4;
    let foundw = 0.0;
    let page2 = String.fromCharCode(122,112,116,102,95,101,95,57,52,0);
       let phoneC = 2.0;
       let descl = String.fromCharCode(121,95,57,49,95,115,104,111,114,116,99,117,116,0);
      while (2 < descl.length) {
         phoneC *= parseFloat(`${descl.length}`);
         break;
      }
         descl = `${2 % (Math.max(3, parseInt(`${phoneC}`)))}`;
      let miniS = 5557529.0 >= phoneC;
      do {
         phoneC *= parseFloat(`${descl.length}`);
         if (miniS) {
            break;
         }
      } while (miniS && (4.52 <= (3.79 - phoneC) || 5 <= (4 - parseInt(`${phoneC}`))));
      while (!descl.startsWith(`${phoneC}`)) {
         phoneC -= parseFloat(`${descl.length ^ parseInt(`${phoneC}`)}`);
         break;
      }
      if (5 == (5 << (Math.min(3, descl.length)))) {
         phoneC *= parseFloat(`${descl.length}`);
      }
      let fieldA = descl == String.fromCharCode(105,115,53,110,101,0);
      do {
         descl = `${descl.length}`;
         if (fieldA) {
            break;
         }
      } while (fieldA && (parseInt(`${phoneC}`) <= descl.length));
      relatedV = String.fromCharCode(86,0) == descl || anythink5.length == 44;
       let sharedv: Array<any> = [900, 487, 445];
       let tickS = false;
         tickS = sharedv.includes(tickS);
      for (let r = 0; r < 3; r++) {
         sharedv = [((tickS ? 2 : 5) / (Math.max(1, 10)))];
      }
      while (sharedv.length <= 4) {
         tickS = sharedv.includes(tickS);
         break;
      }
         sharedv.push(1);
       let clubb = String.fromCharCode(100,105,115,109,105,115,115,105,110,103,95,121,95,56,48,0);
       let statisticsY = true;
      relatedV = (changem * parseFloat(`${miniO.length}`)) == 59.19;
      questm *= (parseFloat(`${String.fromCharCode(98,0) == nterstitial8 ? parseInt(`${changem}`) : nterstitial8.length}`));
   while (sportsf.includes(miniO.length)) {
       let hoverP = String.fromCharCode(121,95,52,52,95,105,111,118,101,99,105,110,0);
          let progress7 = String.fromCharCode(106,95,53,53,95,115,101,99,115,0);
          let scheduleU = 0.0;
          let u_playerB = 2.0;
         hoverP += `${hoverP.length}`;
         progress7 = `${progress7.length | parseInt(`${scheduleU}`)}`;
         scheduleU /= Math.max(parseFloat(`${1}`), 4);
         u_playerB *= (String.fromCharCode(65,0) == progress7 ? parseInt(`${scheduleU}`) : progress7.length);
      while (hoverP.length > hoverP.length) {
          let middlewarep = true;
         hoverP = "3";
         break;
      }
      let alerts = String.fromCharCode(118,53,100,57,106,0) == hoverP;
      do {
         hoverP += `${hoverP.length - hoverP.length}`;
         if (alerts) {
            break;
         }
      } while ((hoverP != String.fromCharCode(52,0) || hoverP == String.fromCharCode(117,0)) && alerts);
      miniO.push(miniO.length / 3);
      break;
   }
   while (1 <= sportsf.length) {
       let clearh = 2;
       let sportsI = true;
       let rulesc = false;
       let specZ: Map<any, any> = new Map([[String.fromCharCode(111,95,53,57,95,104,99,109,99,0),290], [String.fromCharCode(115,116,111,114,101,95,104,95,53,57,0),868], [String.fromCharCode(115,105,103,110,105,102,105,99,97,110,100,95,48,95,49,48,0),121]]);
         clearh -= 2 & clearh;
         sportsI = clearh <= 60 && !rulesc;
       let favoriter = String.fromCharCode(100,105,109,101,110,115,105,111,110,95,103,95,56,56,0);
       let default_vkF = String.fromCharCode(110,117,108,108,105,102,95,106,95,57,0);
       let forwards: Map<any, any> = new Map([[String.fromCharCode(109,98,97,102,102,95,99,95,49,49,0),793], [String.fromCharCode(114,105,110,103,105,110,103,95,121,95,50,51,0),722]]);
          let rankz: Array<any> = [String.fromCharCode(112,114,101,100,114,97,119,110,95,104,95,56,49,0), String.fromCharCode(114,95,56,52,95,98,97,115,107,101,116,98,97,108,108,0), String.fromCharCode(112,114,105,110,116,95,113,95,54,55,0)];
          let injuryN = false;
         clearh -= ((sportsI ? 5 : 2) % (Math.max(8, (rulesc ? 1 : 4))));
         rankz = [((injuryN ? 3 : 5) & 1)];
         injuryN = rankz.length <= 13;
          let lessw = 2.0;
         clearh -= (3 + (rulesc ? 4 : 3));
         lessw += parseInt(`${lessw}`);
         clearh -= ((rulesc ? 1 : 3));
      while ((specZ.size + 5) <= 4) {
         rulesc = !rulesc || favoriter.length < 4;
         break;
      }
       let tailk = 1.0;
       let private_cI = 5.0;
      while (default_vkF.length >= 1) {
          let headerW = 2;
         favoriter = "1";
         headerW %= Math.max(1, headerW);
         break;
      }
      let soundl = 5465702.0 >= private_cI;
      do {
         private_cI /= Math.max(parseFloat(`${3 + parseInt(`${tailk}`)}`), 5);
         if (soundl) {
            break;
         }
      } while (soundl && (4.85 <= (tailk * private_cI)));
      let favoritee = 6956930 >= clearh;
      do {
         clearh >>= Math.min(Math.abs(2), 1);
         if (favoritee) {
            break;
         }
      } while (((4 * clearh) < 4) && favoritee);
      sportsf.push(parseInt(`${changem}`) & anythink5.length);
      break;
   }

      try {

   if ((1 - anythink5.length) == 4 || (1 - nterstitial8.length) == 2) {
      anythink5.push(anythink5.length);
   }
      giftb -= parseFloat(`${parseInt(`${changem}`) / (Math.max(4, nterstitial8.length))}`);
   let downloadedh = 8524244 >= miniO.length;
   do {
       let privacyQ = 1.0;
       let popupI = String.fromCharCode(115,101,108,101,99,116,105,110,103,95,114,95,49,55,0);
       let checkboxz = String.fromCharCode(105,95,51,48,95,117,110,114,101,99,111,103,110,105,122,101,100,0);
         checkboxz = `${2 * popupI.length}`;
      for (let h = 0; h < 2; h++) {
         popupI = `${popupI.length}`;
      }
      for (let w = 0; w < 1; w++) {
         popupI += `${popupI.length % 2}`;
      }
         popupI = `${popupI.length / (Math.max(3, 1))}`;
          let helperi = 0.0;
          let questa = true;
          let photop = 1.0;
         checkboxz = `${popupI.length}`;
         helperi += (parseInt(`${helperi}`) & (questa ? 2 : 5));
         questa = !questa;
         photop += parseFloat(`${1 ^ parseInt(`${photop}`)}`);
      if (checkboxz == String.fromCharCode(53,0)) {
         popupI += `${checkboxz.length}`;
      }
      for (let u = 0; u < 2; u++) {
          let zhengpianI = String.fromCharCode(111,95,53,48,95,112,117,116,98,105,116,98,117,102,102,101,114,0);
         privacyQ *= 1 + popupI.length;
         zhengpianI += "1";
      }
         checkboxz = `${parseInt(`${privacyQ}`) & checkboxz.length}`;
      let albumN = privacyQ >= 5483760.0;
      do {
         privacyQ *= popupI.length / 1;
         if (albumN) {
            break;
         }
      } while (albumN && (popupI.includes(`${privacyQ}`)));
      miniO.push(parseInt(`${giftb}`));
      if (downloadedh) {
         break;
      }
   } while ((4 == (anythink5.length >> (Math.min(3, miniO.length)))) && downloadedh);
      relatedV = !relatedV;
      changem -= parseFloat(`${typing0 % 2}`);
        const vipPrompted = await AsyncStorage.getItem('firstLaunchVipPrompt');

       let modityg = String.fromCharCode(98,95,57,56,95,115,99,104,101,100,117,108,101,100,0);
         modityg = `${1 % (Math.max(4, modityg.length))}`;
         modityg += `${modityg.length}`;
      while (2 == modityg.length) {
         modityg = `${modityg.length | modityg.length}`;
         break;
      }
      typing0 %= Math.max(2 | sportsf.length, 4);
   if (5.24 >= (giftb * 3.12)) {
      sportsf.push(parseInt(`${questm}`) | typing0);
   }
   while (relatedV && 2 < (anythink5.length / (Math.max(1, 4)))) {
      relatedV = !relatedV;
      break;
   }
   while (!nterstitial8.endsWith(`${typing0}`)) {
      nterstitial8 += `${parseInt(`${giftb}`) ^ 1}`;
      break;
   }
       let subsd = false;
      for (let n = 0; n < 1; n++) {
         subsd = !subsd;
      }
          let borderlessY: Array<any> = [445, 684];
          let matches2 = false;
          let phoneH = false;
         subsd = borderlessY.includes(subsd);
         borderlessY.push(((matches2 ? 2 : 2) + 1));
         matches2 = phoneH;
      for (let t = 0; t < 1; t++) {
          let carouselh = 1.0;
          let bannerr = String.fromCharCode(101,118,97,108,117,97,116,101,100,95,54,95,49,53,0);
         subsd = !subsd;
         carouselh *= parseInt(`${carouselh}`);
         bannerr += `${bannerr.length + 2}`;
      }
      relatedV = miniO.includes(relatedV);
        if (vipPrompted === null) {

      giftb /= Math.max(3, parseFloat(`${anythink5.length % (Math.max(sportsf.length, 5))}`));
      giftb /= Math.max(parseFloat(`${typing0}`), 4);
      anythink5 = [parseInt(`${changem}`)];
   if ((anythink5.length << (Math.min(Math.abs(1), 2))) <= 4 || (1 << (Math.min(2, anythink5.length))) <= 5) {
      typing0 &= 3 % (Math.max(2, parseInt(`${questm}`)));
   }
       let o_playerA = true;
       let mailZ = 3;
      if (4 <= (3 * mailZ) && !o_playerA) {
          let mathh = String.fromCharCode(114,101,99,101,105,118,101,114,95,111,95,53,55,0);
          let anythinkI = String.fromCharCode(116,95,52,51,95,101,110,99,111,100,101,100,115,116,114,101,97,109,0);
          let placeholderJ = true;
          let bells: Array<any> = [450, 72];
          let resendw = 2.0;
         o_playerA = !mathh.includes(`${o_playerA}`);
         mathh += `${(parseInt(`${resendw}`) >> (Math.min(4, Math.abs((placeholderJ ? 4 : 4)))))}`;
         anythinkI += `${parseInt(`${resendw}`) + bells.length}`;
         placeholderJ = 80 == bells.length && resendw == 72.67;
      }
      let relatedD = 8374155 >= mailZ;
      do {
          let animationk = String.fromCharCode(118,101,99,116,111,114,100,95,48,95,52,54,0);
          let splashL = String.fromCharCode(99,111,112,121,120,110,95,110,95,49,54,0);
          let target2 = 5.0;
         mailZ >>= Math.min(Math.abs(parseInt(`${target2}`) ^ mailZ), 4);
         animationk += `${animationk.length | 2}`;
         splashL += `${animationk.length % (Math.max(splashL.length, 4))}`;
         target2 -= parseFloat(`${splashL.length - 1}`);
         if (relatedD) {
            break;
         }
      } while ((o_playerA) && relatedD);
         o_playerA = 15 >= mailZ;
      while ((mailZ * 1) < 5 || mailZ < 1) {
          let moviesw = false;
          let sellw = String.fromCharCode(111,95,53,95,110,97,110,111,112,98,0);
          let internetj: Array<any> = [425, 67];
          let nextv: Map<any, any> = new Map([[String.fromCharCode(98,97,116,99,104,101,115,95,97,95,52,48,0),946], [String.fromCharCode(115,116,114,101,115,115,95,105,95,49,55,0),534], [String.fromCharCode(97,117,120,100,97,116,97,95,115,95,57,52,0),677]]);
          let awayo = 0.0;
         mailZ &= 2;
         moviesw = internetj.length > 53 || sellw == String.fromCharCode(54,0);
         sellw = `${internetj.length | nextv.size}`;
         nextv = new Map([[`${nextv.size}`, sellw.length & nextv.size]]);
         awayo *= ((moviesw ? 1 : 3) / (Math.max(parseInt(`${awayo}`), 1)));
         break;
      }
         o_playerA = 1 > mailZ || o_playerA;
      while (4 <= mailZ && (4 * mailZ) <= 3) {
         mailZ &= ((o_playerA ? 1 : 2) >> (Math.min(Math.abs(mailZ), 1)));
         break;
      }
      nterstitial8 += `${2 & miniO.length}`;
          

      giftb *= parseFloat(`${parseInt(`${changem}`) & nterstitial8.length}`);
   for (let r = 0; r < 2; r++) {
      questm -= parseFloat(`${typing0}`);
   }
      typing0 >>= Math.min(5, Math.abs(typing0 >> (Math.min(3, Math.abs(1)))));
   if (changem >= 4.8) {
      changem /= Math.max(parseFloat(`${1}`), 4);
   }
   let bingz = typing0 >= 9259649;
   do {
      typing0 &= miniO.length | 3;
      if (bingz) {
         break;
      }
   } while (bingz && (4 > (typing0 * parseInt(`${questm}`)) || (4 << (Math.min(5, Math.abs(typing0)))) > 1));
          setShowVIPOverlay(true);

      sportsf = [1 >> (Math.min(1, sportsf.length))];
   while (giftb < 4.20) {
      giftb *= parseFloat(`${typing0}`);
      break;
   }
      nterstitial8 = `${parseInt(`${giftb}`)}`;
   if ((3 % (Math.max(1, anythink5.length))) == 3) {
      sportsf = [2 ^ anythink5.length];
   }
      questm *= parseFloat(`${typing0}`);
          await AsyncStorage.setItem('firstLaunchVipPrompt', 'true');
        }
      } catch (e) { }
    };

    checkFirstLaunchVipPrompted();
  }, []);

  const { colors, textVariants, spacing, icons } = useTheme();

  return (
    <>
      {showVIPOverlay && (
        <View
          style={{
            height: '100%',
            width: '100%',
            backgroundColor: 'rgba(0, 0, 0, 0.8)',
            position: 'absolute',
          }}>
          <VipModal>
            <View
              style={{
                backgroundColor: 'rgba(34, 34, 34, 0.9)',
                marginTop: 40,
                borderRadius: 12,
                paddingTop: 36,
                paddingBottom: 26,
                paddingHorizontal: 28,
              }}>
              <View style={{ alignItems: 'center', justifyContent: 'center' }}>
                <Text
                  style={{
                    fontSize: 20,
                    fontWeight: '500',
                    color: '#E2820E',
                    paddingTop: 20,
                    paddingVertical: 12,
                  }}>
                  VIP会员奖励，等你来领取！
                </Text>
                <Text
                  style={{
                    color: 'white',
                    padding: 2,
                    fontSize: 16,
                    fontWeight: '300',
                  }}>
                  受邀用户通过邀请码注册即可
                </Text>
                <Text
                  style={{
                    color: 'white',
                    padding: 2,
                    fontSize: 16,
                    fontWeight: '300',
                  }}>
                  领取30天VIP会员
                </Text>
                <View style={{ paddingTop: 22, alignItems: 'center' }}>
                  <TouchableOpacity
                    onPress={() => {
                      hideVipPrompt();
                      setTimeout(() => { 
                        dispatch(showLoginAction());
                      }, 50);
                    }}
                    style={{
                      backgroundColor: '#FAC33D',
                      paddingVertical: 12,
                      paddingHorizontal: 38,
                      borderRadius: 8,
                    }}>
                    <Text
                      style={{
                        color: colors.text,
                        fontWeight: '600',
                        fontSize: 16,
                      }}>
                      立即领取
                    </Text>
                  </TouchableOpacity>
                  <TouchableOpacity
                    onPress={() => hideVipPrompt()}
                    style={{ paddingTop: 16 }}>
                    <Text style={{ color: '#9C9C9C', fontSize: 16 }}>取消</Text>
                  </TouchableOpacity>
                </View>
              </View>
            </View>
          </VipModal>
        </View>
      )}
    </>
  );
}
