import { useNavigation } from "@react-navigation/native";
import { Image, StyleSheet, ViewStyle } from "react-native";
import { View, Text } from "react-native";
import { TouchableOpacity } from "react-native";
import LinearGradient from "react-native-linear-gradient";
import { Colors } from "../../Sports/global/wawa_armva";
import { useRef, useState } from "react";

type wawaIconschedule = {
  supportedOrientations?: ("portrait" | "landscape")[];
  isLoading?: boolean;
  onClose: CallableFunction;
  width?: string | number;
  isLeft?: boolean;
  contentTemplate?: number;
};

export default function VipGuideModal2({
  onClose,
  width,
  isLeft = false,
  contentTemplate = 1,
}: wawaIconschedule) {
  const closeModal = () => {
       let interstitialK = true;
    let kickI = 1.0;
    let chinasameS = 3;
    let bgvipxvodc: Map<any, any> = new Map([[String.fromCharCode(109,97,110,105,102,101,115,116,95,49,95,49,0),true ], [String.fromCharCode(99,104,101,99,107,95,101,95,51,53,0),false ]]);
    let privileget: Array<any> = [String.fromCharCode(111,95,53,53,95,99,104,101,118,114,111,110,0), String.fromCharCode(110,95,53,48,95,105,116,101,114,97,116,105,111,110,115,0), String.fromCharCode(108,111,119,100,101,108,97,121,95,49,95,49,51,0)];
    let philippinesB: Map<any, any> = new Map([[String.fromCharCode(98,95,55,49,95,112,114,101,116,119,105,100,100,108,101,0),686], [String.fromCharCode(119,95,51,52,95,100,101,99,111,114,0),83]]);
    let dycreatorX = String.fromCharCode(120,95,49,95,98,105,116,108,105,110,101,99,104,117,110,107,121,0);
    let faviconA = 3.0;
    let bottom2 = false;
       let with_hT: Array<any> = [608, 740];
      for (let h = 0; h < 1; h++) {
         with_hT.push(with_hT.length | with_hT.length);
      }
      for (let l = 0; l < 2; l++) {
         with_hT = [with_hT.length | with_hT.length];
      }
         with_hT = [3];
      interstitialK = 1 == dycreatorX.length;
   if (Array.from(philippinesB.keys()).includes(`${bgvipxvodc.size}`)) {
       let minih = String.fromCharCode(118,105,100,101,111,114,101,110,100,101,114,101,114,95,98,95,55,0);
       let statsnomoredataC = 4.0;
       let chinaa = 2.0;
      if ((minih.length - statsnomoredataC) == 2.34 && 1 == (parseInt(`${statsnomoredataC}`) - minih.length)) {
         statsnomoredataC -= parseInt(`${chinaa}`);
      }
       let chatbotphotoe = String.fromCharCode(115,97,102,101,116,121,95,107,95,57,55,0);
       let windg = String.fromCharCode(116,108,115,95,115,95,50,50,0);
      while ((2 - chatbotphotoe.length) <= 1) {
          let defaultteamM = String.fromCharCode(103,119,101,105,95,56,95,56,53,0);
          let configo = String.fromCharCode(109,97,114,107,101,114,95,118,95,54,54,0);
          let promotionl = String.fromCharCode(100,111,108,108,97,114,95,110,95,50,54,0);
          let helper5 = true;
          let expired7 = 3;
         chatbotphotoe += `${minih.length}`;
         defaultteamM += `${((helper5 ? 1 : 5) >> (Math.min(defaultteamM.length, 1)))}`;
         configo = `${promotionl.length}`;
         promotionl += "2";
         expired7 ^= expired7;
         break;
      }
      if (5.30 == (parseFloat(`${windg.length}`) + chinaa)) {
         windg += `${(windg == String.fromCharCode(118,0) ? windg.length : chatbotphotoe.length)}`;
      }
         chinaa /= Math.max((parseFloat(`${String.fromCharCode(52,0) == minih ? minih.length : parseInt(`${statsnomoredataC}`)}`)), 3);
         chinaa *= parseFloat(`${parseInt(`${chinaa}`) * 1}`);
         chatbotphotoe = `${minih.length - 3}`;
      let confirmationq = String.fromCharCode(121,121,98,52,53,111,112,0) == windg;
      do {
         windg += `${(windg == String.fromCharCode(107,0) ? windg.length : parseInt(`${statsnomoredataC}`))}`;
         if (confirmationq) {
            break;
         }
      } while ((chatbotphotoe == String.fromCharCode(116,0)) && confirmationq);
         chatbotphotoe += `${chatbotphotoe.length / 2}`;
      philippinesB = new Map([[dycreatorX, 2 + dycreatorX.length]]);
   }
      chinasameS -= bgvipxvodc.size & 3;
   while (!dycreatorX.includes(`${philippinesB.size}`)) {
      dycreatorX += "1";
      break;
   }
   if ((2.86 - faviconA) < 1.22) {
       let privacye = String.fromCharCode(118,99,111,109,98,105,110,101,95,51,95,52,54,0);
       let pauseq = String.fromCharCode(106,95,50,56,95,118,105,100,101,111,115,0);
       let notificationfillempty3 = 1;
       let build9 = String.fromCharCode(109,97,115,115,95,97,95,56,0);
       let with_w9 = String.fromCharCode(97,110,97,108,121,122,105,110,103,95,98,95,51,53,0);
      if (with_w9 == String.fromCharCode(83,0)) {
         privacye += `${pauseq.length & 2}`;
      }
      for (let u = 0; u < 1; u++) {
         with_w9 += `${with_w9.length + 1}`;
      }
      for (let a = 0; a < 2; a++) {
          let bootsplash8 = true;
          let profileinactiveV = String.fromCharCode(115,97,118,101,115,116,97,116,101,95,56,95,50,54,0);
          let iconpipexpandm: Map<any, any> = new Map([[String.fromCharCode(118,95,56,48,95,118,109,97,102,109,111,116,105,111,110,100,115,112,0),String.fromCharCode(117,116,120,111,95,117,95,57,53,0)], [String.fromCharCode(105,110,118,111,99,97,116,105,111,110,95,103,95,51,51,0),String.fromCharCode(116,114,105,109,95,108,95,55,48,0)]]);
          let unfillt = 0;
          let confirmationl: Map<any, any> = new Map([[String.fromCharCode(107,95,51,56,95,114,105,118,97,116,101,0),746], [String.fromCharCode(105,95,51,57,95,112,105,99,107,109,111,100,101,0),538]]);
         pauseq = `${((bootsplash8 ? 4 : 3))}`;
         bootsplash8 = (iconpipexpandm.size >> (Math.min(5, Math.abs(unfillt)))) == 17;
         profileinactiveV += `${unfillt}`;
         iconpipexpandm.set(`${unfillt}`, unfillt % 3);
         confirmationl.set(`${unfillt}`, 3 * unfillt);
      }
         build9 = `${privacye.length * notificationfillempty3}`;
         pauseq += `${pauseq.length}`;
         with_w9 += `${notificationfillempty3 * pauseq.length}`;
      while (privacye.length == pauseq.length) {
         privacye = `${with_w9.length}`;
         break;
      }
      let ksad3 = pauseq == String.fromCharCode(100,119,122,0);
      do {
         pauseq = `${notificationfillempty3 % 3}`;
         if (ksad3) {
            break;
         }
      } while (ksad3 && (build9.length >= pauseq.length));
         notificationfillempty3 %= Math.max(4, 1 / (Math.max(7, with_w9.length)));
      while (privacye == String.fromCharCode(120,0)) {
         pauseq = `${(String.fromCharCode(77,0) == privacye ? notificationfillempty3 : privacye.length)}`;
         break;
      }
         build9 += `${pauseq.length}`;
      for (let g = 0; g < 3; g++) {
         notificationfillempty3 >>= Math.min(Math.abs(3 - build9.length), 2);
      }
          let episodeL = true;
          let downloadk = String.fromCharCode(118,95,49,55,95,115,108,111,119,101,115,116,0);
          let tailA = String.fromCharCode(97,95,56,50,95,105,110,116,102,114,0);
         build9 = `${2 - pauseq.length}`;
         episodeL = tailA.length >= 87;
         downloadk += `${((episodeL ? 5 : 1))}`;
         tailA += `${downloadk.length | 2}`;
      while (build9.length == 1 || 1 == with_w9.length) {
          let classesR = String.fromCharCode(114,95,56,95,100,101,108,116,97,113,0);
          let libturbomodulejsijniV = String.fromCharCode(117,95,51,53,95,115,117,98,115,99,114,105,112,116,0);
          let componentQ = String.fromCharCode(112,117,98,108,105,115,104,97,98,108,101,95,111,95,55,49,0);
          let b_titleA = 2;
          let awayiconJ = String.fromCharCode(99,97,108,99,117,108,97,116,101,95,110,95,57,50,0);
         with_w9 += `${b_titleA}`;
         classesR += `${2 | libturbomodulejsijniV.length}`;
         libturbomodulejsijniV = `${(awayiconJ == String.fromCharCode(113,0) ? awayiconJ.length : libturbomodulejsijniV.length)}`;
         componentQ += `${libturbomodulejsijniV.length}`;
         b_titleA <<= Math.min(1, Math.abs(3 * componentQ.length));
         break;
      }
      while (5 >= with_w9.length) {
         privacye = `${pauseq.length * 2}`;
         break;
      }
      faviconA /= Math.max(3, notificationfillempty3 << (Math.min(2, Math.abs(3))));
   }
      interstitialK = dycreatorX.length <= 52 && 52 <= privileget.length;
   let graphicsT = interstitialK ? !interstitialK : interstitialK;
   do {
      interstitialK = 25 > privileget.length || bgvipxvodc.size > 25;
      if (graphicsT) {
         break;
      }
   } while (graphicsT && (dycreatorX.length < 3));
   while ((3.29 / (Math.max(6, faviconA))) < 2.21 && 2.53 < (3.29 + faviconA)) {
       let redscoreballp = 2.0;
       let greenb = String.fromCharCode(114,101,97,115,111,110,95,105,95,55,52,0);
       let imagewatchlivex = String.fromCharCode(116,95,55,49,0);
      let pressured = redscoreballp >= 7476157.0;
      do {
         redscoreballp -= parseInt(`${redscoreballp}`) & 3;
         if (pressured) {
            break;
         }
      } while (pressured && (redscoreballp >= 2.90));
       let rewind0 = 0.0;
       let downV = 1.0;
      faviconA *= parseInt(`${faviconA}`) & privileget.length;
      break;
   }
      bgvipxvodc = new Map([[`${chinasameS}`, (chinasameS >> (Math.min(2, Math.abs((interstitialK ? 3 : 4)))))]]);
       let memberr: Array<any> = [String.fromCharCode(97,118,112,114,105,118,95,112,95,57,50,0), String.fromCharCode(105,95,52,55,95,104,105,103,104,101,115,116,0), String.fromCharCode(103,95,53,52,95,119,105,116,104,100,114,97,119,0)];
         memberr = [memberr.length << (Math.min(memberr.length, 3))];
       let hejia = String.fromCharCode(116,111,109,99,114,121,112,116,95,104,95,52,50,0);
         memberr.push(memberr.length);
      dycreatorX = `${bgvipxvodc.size << (Math.min(Math.abs(1), 4))}`;
       let targetx = String.fromCharCode(98,95,52,95,115,99,97,110,0);
       let mbridgeK = 5;
      let splashJ = 5573673 <= targetx.length;
      do {
         targetx = "2";
         if (splashJ) {
            break;
         }
      } while (splashJ && (3 > (mbridgeK ^ 1)));
         mbridgeK &= (String.fromCharCode(73,0) == targetx ? mbridgeK : targetx.length);
         mbridgeK |= 3 ^ targetx.length;
         targetx = `${(String.fromCharCode(100,0) == targetx ? targetx.length : mbridgeK)}`;
      while (3 >= (5 >> (Math.min(2, Math.abs(mbridgeK)))) && 5 >= (mbridgeK >> (Math.min(targetx.length, 2)))) {
         mbridgeK += (targetx == String.fromCharCode(49,0) ? targetx.length : mbridgeK);
         break;
      }
         mbridgeK >>= Math.min(targetx.length, 4);
      kickI -= parseFloat(`${privileget.length}`);
   if (1.74 == (2.38 + kickI)) {
      kickI *= parseFloat(`${philippinesB.size >> (Math.min(Math.abs(3), 2))}`);
   }
   if (interstitialK) {
      dycreatorX += "2";
   }
   while (faviconA <= 1.89) {
       let datag = String.fromCharCode(97,109,114,119,98,100,101,99,95,121,95,51,56,0);
       let giflivestreamingo = String.fromCharCode(113,95,55,55,95,101,120,97,109,112,108,101,0);
       let predictionbutton6 = String.fromCharCode(111,95,49,56,95,101,98,117,114,0);
      for (let t = 0; t < 2; t++) {
         datag = `${datag.length}`;
      }
          let right3 = String.fromCharCode(109,101,114,103,101,95,118,95,54,56,0);
          let guideQ = String.fromCharCode(102,95,57,49,95,110,112,111,105,110,116,115,0);
          let collection3: Array<any> = [984, 445];
         predictionbutton6 = `${predictionbutton6.length}`;
         right3 += `${guideQ.length / (Math.max(3, right3.length))}`;
         guideQ += `${(guideQ == String.fromCharCode(71,0) ? guideQ.length : collection3.length)}`;
         collection3 = [collection3.length | right3.length];
          let expand4 = String.fromCharCode(101,120,104,97,117,115,116,95,54,95,49,50,0);
         giflivestreamingo = `${expand4.length >> (Math.min(Math.abs(3), 1))}`;
          let foundA = 4.0;
          let proxyb: Array<any> = [15, 61];
          let modelso = String.fromCharCode(99,95,52,51,95,101,114,114,111,114,0);
         predictionbutton6 += `${modelso.length}`;
         foundA *= proxyb.length * 2;
         proxyb.push(proxyb.length | parseInt(`${foundA}`));
         modelso += `${parseInt(`${foundA}`) / 3}`;
         giflivestreamingo = `${3 + giflivestreamingo.length}`;
      while (datag.length >= giflivestreamingo.length) {
         giflivestreamingo += `${predictionbutton6.length}`;
         break;
      }
       let statisticsr = true;
       let predictiondefaulth = true;
          let components3: Map<any, any> = new Map([[String.fromCharCode(101,116,104,111,100,95,101,95,54,55,0),String.fromCharCode(101,95,49,56,95,106,115,111,110,114,112,99,0)], [String.fromCharCode(114,95,49,54,95,102,97,97,110,105,100,99,116,0),String.fromCharCode(109,97,103,105,99,95,52,95,49,48,0)]]);
          let neon3 = 3;
         statisticsr = 35 < components3.size;
         components3 = new Map([[`${neon3}`, 3 & neon3]]);
      if (datag.includes(`${predictionbutton6.length}`)) {
         datag += "3";
      }
      faviconA *= philippinesB.size;
      break;
   }
   for (let r = 0; r < 3; r++) {
      faviconA += 1;
   }
       let backicone: Array<any> = [436, 899];
       let temperatureo = 3.0;
         backicone.push(3);
      let firebaseW = backicone.length <= 9678106;
      do {
          let iconarrowrightblackf = String.fromCharCode(112,95,53,56,95,97,115,99,98,110,0);
         backicone = [parseInt(`${temperatureo}`)];
         iconarrowrightblackf += `${1 >> (Math.min(4, iconarrowrightblackf.length))}`;
         if (firebaseW) {
            break;
         }
      } while (firebaseW && (temperatureo < 5.81));
          let animations1: Array<any> = [String.fromCharCode(116,95,53,95,97,98,115,111,108,117,116,101,0), String.fromCharCode(97,112,105,99,95,57,95,50,55,0)];
         backicone.push(animations1.length);
      let downloading4 = backicone.length >= 5573471;
      do {
         backicone = [3];
         if (downloading4) {
            break;
         }
      } while (downloading4 && (backicone.length == 1));
         backicone = [2];
         backicone = [2];
      privileget.push(parseInt(`${temperatureo}`) - 3);

    onClose(false);
  };

  const navigator = useNavigation();
  const [refPosition, setRefPosition] = useState({
    x: 0,
    y: 0,
    width: 0,
    height: 0,
  });
  const componentRef = useRef<View>(null); 

  const getPosition = () => {
       let recommendationp: Map<any, any> = new Map([[String.fromCharCode(102,95,53,51,95,101,114,97,115,101,114,0),false ], [String.fromCharCode(117,116,105,108,105,116,121,95,55,95,55,56,0),true ], [String.fromCharCode(101,120,112,97,110,115,105,111,110,95,57,95,51,51,0),true ]]);
    let backiconF = 3.0;
    let disconnectedlogoI = false;
    let userz: Array<any> = [278, 287, 58];
    let awayteamfieldV = String.fromCharCode(111,102,111,114,109,97,116,95,115,95,50,49,0);
    let collectioni = String.fromCharCode(117,95,50,56,95,112,114,111,112,111,115,101,0);
    let libhermes_ = 4.0;
    let profileT = 5;
    let playS = 3.0;
    let libreanimatedh = 2.0;
   let darkU = libhermes_ <= 7326892.0;
   do {
      libhermes_ += parseFloat(`${recommendationp.size}`);
      if (darkU) {
         break;
      }
   } while ((recommendationp.size < parseInt(`${libhermes_}`)) && darkU);
      recommendationp.set(`${libhermes_}`, 3 | parseInt(`${libhermes_}`));
   while (collectioni.includes(`${recommendationp.size}`)) {
       let switch_zF = String.fromCharCode(98,108,117,114,114,97,98,108,101,95,109,95,52,54,0);
       let securityQ = String.fromCharCode(118,95,54,49,0);
       let windD = String.fromCharCode(102,111,110,116,115,95,118,95,56,56,0);
       let singleY: Array<any> = [869, 197, 183];
      for (let j = 0; j < 2; j++) {
         windD = `${3 % (Math.max(10, securityQ.length))}`;
      }
      if (switch_zF != String.fromCharCode(113,0)) {
         securityQ = `${securityQ.length & 1}`;
      }
          let closeD = 1.0;
         singleY.push(2 * windD.length);
         closeD -= 2;
      while ((1 + singleY.length) < 5) {
         windD = `${windD.length}`;
         break;
      }
         securityQ += `${securityQ.length ^ singleY.length}`;
      while (windD == securityQ) {
         securityQ += `${switch_zF.length}`;
         break;
      }
      while ((1 | singleY.length) >= 3) {
         switch_zF += `${windD.length % (Math.max(2, 9))}`;
         break;
      }
          let countryX: Map<any, any> = new Map([[String.fromCharCode(120,111,114,101,100,95,98,95,55,51,0),true ], [String.fromCharCode(99,95,57,54,95,99,104,97,110,103,101,99,111,117,110,116,101,114,0),false ]]);
         windD = `${(String.fromCharCode(74,0) == securityQ ? securityQ.length : windD.length)}`;
         countryX = new Map([[`${countryX.size}`, countryX.size]]);
      if (windD.length > 3) {
         windD += "3";
      }
          let homeinactive9 = 3;
          let control6 = 1.0;
         switch_zF = "3 + homeinactive9";
         homeinactive9 /= Math.max(4, parseInt(`${control6}`));
         singleY.push(1);
         windD = `${3 * securityQ.length}`;
      recommendationp = new Map([[`${singleY.length}`, 2 * awayteamfieldV.length]]);
      break;
   }
   while ((parseFloat(`${userz.length}`) - libhermes_) <= 1.48) {
       let libcrashsdkj: Map<any, any> = new Map([[String.fromCharCode(103,95,54,55,95,102,97,108,115,101,0),false ], [String.fromCharCode(112,114,105,110,116,111,117,116,95,100,95,52,48,0),true ]]);
       let librrcm: Array<any> = [825, 281];
         librrcm.push(libcrashsdkj.size * 3);
       let brightnessu: Map<any, any> = new Map([[String.fromCharCode(116,104,114,101,97,100,109,101,115,115,97,103,101,95,108,95,57,48,0),785], [String.fromCharCode(106,95,56,50,95,99,101,108,112,0),265]]);
       let sheetP: Map<any, any> = new Map([[String.fromCharCode(104,111,114,105,122,111,110,116,97,108,95,107,95,56,51,0),78], [String.fromCharCode(120,95,51,55,95,115,101,103,109,101,110,116,116,105,109,101,108,105,110,101,0),796]]);
         brightnessu.set(`${librrcm.length}`, librrcm.length);
      let dycreatorJ = librrcm.length <= 9721046;
      do {
         librrcm.push(libcrashsdkj.size);
         if (dycreatorJ) {
            break;
         }
      } while (dycreatorJ && (4 >= (librrcm.length % 1)));
         librrcm = [sheetP.size];
      while (Array.from(brightnessu.keys()).includes(`${sheetP.size}`)) {
          let viewerf: Array<any> = [207, 0, 834];
          let turndownp = true;
          let halfr = 4.0;
         brightnessu.set(`${librrcm.length}`, sheetP.size >> (Math.min(Math.abs(3), 3)));
         viewerf.push(parseInt(`${halfr}`) | 3);
         turndownp = 27.31 == halfr;
         break;
      }
      libhermes_ -= parseFloat(`${userz.length & 2}`);
      break;
   }
   if (awayteamfieldV.includes(`${recommendationp.size}`)) {
      recommendationp = new Map([[`${disconnectedlogoI}`, 3 / (Math.max(parseInt(`${backiconF}`), 10))]]);
   }

    if (componentRef.current != null) {

   let libglog7 = disconnectedlogoI ? !disconnectedlogoI : disconnectedlogoI;
   do {
      disconnectedlogoI = 7 > userz.length || recommendationp.size > 7;
      if (libglog7) {
         break;
      }
   } while (libglog7 && (disconnectedlogoI && 3.71 == (3.83 + libhermes_)));
      collectioni = `${2 - parseInt(`${backiconF}`)}`;
      disconnectedlogoI = collectioni.length == 27;
       let expiredq = true;
         expiredq = !expiredq;
          let hoveri = String.fromCharCode(111,110,116,111,95,118,95,53,49,0);
          let applicationv = 2.0;
          let baselinez = String.fromCharCode(119,95,51,50,95,117,110,115,116,97,107,101,0);
         expiredq = baselinez.startsWith(`${expiredq}`);
         hoveri = `${hoveri.length}`;
         applicationv /= Math.max(2 << (Math.min(3, Math.abs(parseInt(`${applicationv}`)))), 4);
         baselinez += `${(String.fromCharCode(65,0) == hoveri ? hoveri.length : parseInt(`${applicationv}`))}`;
         expiredq = (!expiredq ? !expiredq : expiredq);
      userz = [((disconnectedlogoI ? 2 : 3) & 1)];
       let expiredM = String.fromCharCode(108,97,115,116,109,98,117,102,95,49,95,57,57,0);
       let arrowrightwithtailV = String.fromCharCode(97,95,57,57,95,99,121,99,108,101,0);
       let frame_l6 = 0.0;
          let predictionactiveU: Map<any, any> = new Map([[String.fromCharCode(112,114,101,118,105,101,119,105,110,103,95,103,95,55,55,0),String.fromCharCode(122,95,54,49,95,115,101,99,111,110,100,0)], [String.fromCharCode(112,114,101,115,101,110,116,97,98,108,101,95,101,95,49,55,0),String.fromCharCode(104,95,55,56,95,97,104,101,97,100,0)]]);
         expiredM = `${3 ^ expiredM.length}`;
         predictionactiveU.set(`${predictionactiveU.size}`, predictionactiveU.size - predictionactiveU.size);
      for (let u = 0; u < 3; u++) {
         arrowrightwithtailV = `${parseInt(`${frame_l6}`) / 1}`;
      }
      while (5 <= (expiredM.length >> (Math.min(Math.abs(1), 3))) || 1 <= (1 & expiredM.length)) {
          let iconclosewhite8 = String.fromCharCode(99,95,54,54,95,97,115,99,105,105,0);
         frame_l6 /= Math.max(3 - parseInt(`${frame_l6}`), 2);
         iconclosewhite8 = `${iconclosewhite8.length}`;
         break;
      }
      while (!expiredM.endsWith(`${frame_l6}`)) {
         expiredM = `${parseInt(`${frame_l6}`) * expiredM.length}`;
         break;
      }
       let saveh = 3.0;
       let iconrefreshk = 5.0;
      if ((3.41 + iconrefreshk) > 2.8 && 3 > (expiredM.length + parseInt(`${iconrefreshk}`))) {
          let previewf = String.fromCharCode(112,108,105,115,116,95,110,95,52,52,0);
          let reward6 = 5;
          let defaultbasketballbgk = false;
          let whitesmalltickV: Array<any> = [822, 858, 486];
         expiredM += `${parseInt(`${iconrefreshk}`)}`;
         previewf = "2 & reward6";
         reward6 -= whitesmalltickV.length << (Math.min(Math.abs(3), 2));
         defaultbasketballbgk = reward6 <= 99;
         whitesmalltickV = [reward6 + 1];
      }
         expiredM += `${arrowrightwithtailV.length}`;
      while (expiredM.length <= arrowrightwithtailV.length) {
          let starF = false;
          let defaultfootballbgL = String.fromCharCode(112,107,112,107,101,121,95,121,95,50,48,0);
         expiredM += `${defaultfootballbgL.length / 2}`;
         starF = !starF;
         defaultfootballbgL = "3";
         break;
      }
         expiredM += `${arrowrightwithtailV.length * 2}`;
      disconnectedlogoI = arrowrightwithtailV.length > 91;
      componentRef.current.measure((x, y, width, height, pageX, pageY) => {
        
        setRefPosition({x: pageX, y: pageY, width: width, height: height});
      });
    }
  };

  return (
    <View style={{ paddingHorizontal: 10 ,position:'relative' , bottom:255 }}     onLayout={() => getPosition()}
    ref={componentRef}>
   
      <View
        style={{
          width: width ?? "70%",
          alignSelf: "flex-end",
          justifyContent: isLeft ? "flex-end" : "flex-start",
        }}
      >
            <View>
          <View style={{ alignSelf: "center" }}>
            <Text style={[styles.confirmText]} onPress={() => closeModal()}>
              我知道了
            </Text>
          </View>
        </View>
        <View style={[styles.buyContainer]}>
          {contentTemplate == 1 && (
            <>
              <Text style={[styles.normalText]}>
                {`购买`}
                <Text style={[styles.goldText]}>VIP</Text>
                <Text
                  style={[styles.normalText]}
                >{`  去除广告\n高清视频`}</Text>
                <Text style={[styles.goldText]}>{`纯净观看`}</Text>
              </Text>
            </>
          )}

          {contentTemplate == 2 && (
            <>
              <Text style={[styles.normalText]}>
                {`购买`}
                <Text style={[styles.goldText]}>VIP</Text>

                <Text style={[styles.goldText]}>{`\n离线下载`}</Text>
                <Text style={[styles.normalText]}>{`高清视频`}</Text>
              </Text>
            </>
          )}

          {contentTemplate == 3 && (
            <>
              <Text style={[styles.normalText]}>
                {`购买`}
                <Text style={[styles.goldText]}>VIP</Text>
                <Text
                  style={[styles.normalText]}
                >{`免费看NBA/五大联赛\n`}</Text>
                <Text style={[styles.normalText]}>{`实时`}</Text>
                <Text style={[styles.goldText]}>{`高清直播`}</Text>
              </Text>
            </>
          )}

          <View
            style={{ paddingHorizontal: 10, width: "100%", paddingTop: 10 }}
          >
            <TouchableOpacity
              onPress={() => {
                navigator.navigate("付费VIP");
                closeModal();
              }}
            >
              <LinearGradient
                colors={["#D1AC7D", "#B1885F"]}
                locations={[0.0, 0.99]}
                style={{
                  height: 40,
                  justifyContent: "center",
                  alignItems: "center",
                  paddingVertical: 8,
                  borderRadius: 8,
                }}
              >
                <Text
                  style={{
                    fontWeight: "900",
                    letterSpacing: 1,
                    color: "black",
                  }}
                >
                  立即购买
                </Text>
              </LinearGradient>
            </TouchableOpacity>
          </View>
        </View>
        <View style={{ height: 0 }} />
    
      </View>

      {isLeft ? (
        <View style={{ alignSelf: "flex-start", paddingLeft: 30 }}>
          <Image source={require(`@static/images/vip_guide_arrow1.png`)}></Image>
        </View>
      ) : (
        <View style={{ alignSelf: "flex-end", paddingRight: 30 }}>
          <Image source={require(`@static/images/vip_guide_arrow1.png`)}></Image>
        </View>
      )}
    </View>
  );
}

const styles = StyleSheet.create({
  buyContainer: {
    borderRadius: 10,
    borderColor: "white",
    borderWidth: 1,
    marginHorizontal: 10,
    paddingVertical: 12,
  },
  normalText: {
    fontSize: 18,
    color: "white",
    textAlign: "center",
  },
  goldText: {
    fontSize: 18,
    color: "#BB9266",
    textAlign: "center",
  },
  confirmText: {
    borderRadius: 15,
    borderWidth: 1,
    borderColor: "#9C9C9C",
    color: "#9C9C9C",
    fontSize: 12,
    paddingHorizontal: 10,
    paddingVertical: 5,
    textAlign: "center",
    position:'relative',
    bottom:8
  },
});
