import React, { Suspense, useCallback, useEffect, useState } from 'react';
import { View, Text, TouchableOpacity } from 'react-native';
import VipModal from './mayi_fill';
import { useNavigation, useTheme } from '@react-navigation/native';
import AsyncStorage from '@react-native-async-storage/async-storage';
import { useAppDispatch } from '@hooks/mayi_redirect';
import {
  showLoginAction,
} from '@redux/actions/mayi_iconorientation_chatroombackground';

interface mayi_GoogleViews { }

export default function VipOverlay({ }: mayi_GoogleViews) {
  const [showVIPOverlay, setShowVIPOverlay] = useState(false);

  const hideVipPrompt = useCallback(async () => {
       let taiwanI = 3.0;
    let iconfeedback_ = false;
    let z_imageR = String.fromCharCode(121,95,56,57,95,117,110,105,111,110,101,100,0);
    let promotionP = 3.0;
    let iconwechatr: Array<any> = [326, 910, 347];
    let telemetryj: Map<any, any> = new Map([[String.fromCharCode(107,98,112,115,95,101,95,49,54,0),836], [String.fromCharCode(115,101,108,102,105,101,95,52,95,49,55,0),164], [String.fromCharCode(113,95,50,56,95,116,111,108,111,119,101,114,0),536]]);
    let issubm = String.fromCharCode(105,95,54,48,95,109,97,115,115,0);
    let short_uhA: Map<any, any> = new Map([[String.fromCharCode(114,101,115,101,97,114,99,104,95,111,95,50,0),536], [String.fromCharCode(109,97,107,101,119,116,95,110,95,49,54,0),792], [String.fromCharCode(109,105,110,114,95,110,95,50,53,0),42]]);
    let m_hashF = String.fromCharCode(100,105,109,109,105,110,103,95,99,95,49,57,0);
      issubm = `${2 * parseInt(`${promotionP}`)}`;
      telemetryj.set(z_imageR, 2 & issubm.length);
   let footballfiledlayouty = iconfeedback_ ? !iconfeedback_ : iconfeedback_;
   do {
      iconfeedback_ = !iconfeedback_ || iconwechatr.length <= 10;
      if (footballfiledlayouty) {
         break;
      }
   } while (((taiwanI / (Math.max(2.96, 9))) > 5.72 && 2.96 > taiwanI) && footballfiledlayouty);
      promotionP -= (parseFloat(`${(iconfeedback_ ? 2 : 3) % (Math.max(parseInt(`${taiwanI}`), 7))}`));
      issubm = `${3 / (Math.max(4, parseInt(`${taiwanI}`)))}`;
   if (2 >= (5 % (Math.max(9, telemetryj.size))) || 3 >= (z_imageR.length % 5)) {
      telemetryj = new Map([[`${promotionP}`, (parseInt(`${promotionP}`) << (Math.min(3, Math.abs((iconfeedback_ ? 3 : 2)))))]]);
   }
      taiwanI += 1;
      promotionP *= parseFloat(`${parseInt(`${promotionP}`) << (Math.min(Math.abs(telemetryj.size), 1))}`);
   if (z_imageR.length == taiwanI) {
      taiwanI *= (z_imageR == String.fromCharCode(109,0) ? parseInt(`${taiwanI}`) : z_imageR.length);
   }
   for (let l = 0; l < 2; l++) {
      issubm = `${parseInt(`${taiwanI}`)}`;
   }
       let taiwanP = 0.0;
       let desce = 5;
       let mbbanner7 = String.fromCharCode(113,95,50,49,95,115,112,104,101,114,105,99,97,108,0);
         desce |= 3;
         desce &= 1;
       let stringsJ = 2.0;
         stringsJ += parseFloat(`${1 | parseInt(`${stringsJ}`)}`);
       let predictionbutton2: Array<any> = [String.fromCharCode(107,95,49,56,95,98,105,110,104,101,120,0), String.fromCharCode(122,95,56,55,95,113,110,111,115,0)];
       let matchinactiveD = true;
      let closep = matchinactiveD ? !matchinactiveD : matchinactiveD;
      do {
          let flyer6: Map<any, any> = new Map([[String.fromCharCode(104,101,97,100,115,101,116,95,118,95,55,52,0),526], [String.fromCharCode(112,95,54,51,95,112,111,111,112,0),872]]);
          let starG = 2.0;
          let orangeg = String.fromCharCode(108,111,115,115,121,95,105,95,56,53,0);
         matchinactiveD = orangeg.length > parseInt(`${stringsJ}`);
         flyer6 = new Map([[`${flyer6.size}`, 2]]);
         starG *= parseInt(`${starG}`);
         orangeg = "2";
         if (closep) {
            break;
         }
      } while (((predictionbutton2.length << (Math.min(Math.abs(3), 2))) < 3 && 3 < predictionbutton2.length) && closep);
         predictionbutton2 = [2 + mbbanner7.length];
      let unimplementedview3 = matchinactiveD ? !matchinactiveD : matchinactiveD;
      do {
         matchinactiveD = predictionbutton2.includes(matchinactiveD);
         if (unimplementedview3) {
            break;
         }
      } while ((!matchinactiveD) && unimplementedview3);
      taiwanI *= z_imageR.length << (Math.min(Math.abs(3), 1));
      taiwanP -= parseFloat(`${2 - parseInt(`${taiwanP}`)}`);
   if (z_imageR.length <= 5 || !iconfeedback_) {
      z_imageR += `${parseInt(`${taiwanI}`) & issubm.length}`;
   }
      telemetryj.set(`${taiwanI}`, (parseInt(`${taiwanI}`) << (Math.min(1, Math.abs((iconfeedback_ ? 2 : 1))))));
      iconwechatr.push((String.fromCharCode(48,0) == z_imageR ? z_imageR.length : telemetryj.size));
       let targetR: Array<any> = [429, 403];
       let liveshare8 = String.fromCharCode(97,99,114,111,110,121,109,115,95,54,95,55,56,0);
      while (1 <= (liveshare8.length ^ 1) || 1 <= (targetR.length ^ liveshare8.length)) {
         targetR.push(3 + liveshare8.length);
         break;
      }
      let linkU = String.fromCharCode(118,107,117,104,0) == liveshare8;
      do {
          let default_v4 = String.fromCharCode(100,114,105,118,101,114,115,95,54,95,51,56,0);
          let defaultlogo4: Array<any> = [407, 683, 335];
         liveshare8 += `${1 - default_v4.length}`;
         default_v4 += `${defaultlogo4.length}`;
         defaultlogo4.push(defaultlogo4.length / (Math.max(1, defaultlogo4.length)));
         if (linkU) {
            break;
         }
      } while (((liveshare8.length / 4) <= 2 && (targetR.length / 4) <= 2) && linkU);
      if ((5 & targetR.length) > 2) {
         targetR = [targetR.length];
      }
         targetR = [liveshare8.length];
       let membershipq = String.fromCharCode(117,95,55,52,95,105,110,99,111,109,105,110,103,0);
       let yellowcirclebgO = String.fromCharCode(106,95,52,53,95,105,115,115,117,101,115,0);
      let filter3 = yellowcirclebgO == String.fromCharCode(52,108,116,101,120,0);
      do {
         yellowcirclebgO = `${(membershipq == String.fromCharCode(88,0) ? liveshare8.length : membershipq.length)}`;
         if (filter3) {
            break;
         }
      } while (filter3 && (3 >= membershipq.length));
      promotionP += parseFloat(`${liveshare8.length}`);
   let commentu = 9351753 >= telemetryj.size;
   do {
       let iconviewergifl = String.fromCharCode(97,100,100,95,103,95,52,52,0);
       let full_ = String.fromCharCode(116,97,98,108,101,115,95,117,95,57,48,0);
       let profilepicc = String.fromCharCode(100,97,115,104,101,115,95,122,95,49,53,0);
       let taiwanL = String.fromCharCode(99,105,118,105,108,95,117,95,49,49,0);
         full_ = `${profilepicc.length - taiwanL.length}`;
      if (!profilepicc.startsWith(`${full_.length}`)) {
         full_ += `${3 * taiwanL.length}`;
      }
         taiwanL = `${2 + profilepicc.length}`;
          let mappingV = String.fromCharCode(115,95,49,52,95,115,112,101,99,105,102,105,101,114,115,0);
         iconviewergifl += `${profilepicc.length}`;
         mappingV = `${1 << (Math.min(5, mappingV.length))}`;
      if (iconviewergifl == String.fromCharCode(97,0)) {
         full_ = `${(iconviewergifl == String.fromCharCode(117,0) ? iconviewergifl.length : profilepicc.length)}`;
      }
         full_ += `${taiwanL.length}`;
      while (taiwanL != full_) {
          let downarrowp = String.fromCharCode(103,95,52,56,95,114,111,119,115,112,97,110,0);
          let controlf = String.fromCharCode(103,114,97,110,117,108,97,114,105,116,121,95,113,95,54,48,0);
          let whistleu = String.fromCharCode(106,95,51,54,95,98,105,116,114,101,100,117,99,116,105,111,110,0);
         full_ = `${2 * controlf.length}`;
         downarrowp = `${whistleu.length}`;
         controlf += `${downarrowp.length}`;
         whistleu += "2";
         break;
      }
         profilepicc = `${full_.length + taiwanL.length}`;
         full_ += `${3 ^ iconviewergifl.length}`;
         profilepicc = `${full_.length >> (Math.min(Math.abs(1), 5))}`;
         profilepicc = `${full_.length}`;
      if (5 > full_.length) {
         profilepicc += `${full_.length - 1}`;
      }
      telemetryj.set(full_, full_.length);
      if (commentu) {
         break;
      }
   } while ((3 <= (4 | telemetryj.size)) && commentu);
       let airbnbstarG = 5.0;
      let checkboxA = airbnbstarG >= 8513497.0;
      do {
         airbnbstarG *= parseInt(`${airbnbstarG}`) * 3;
         if (checkboxA) {
            break;
         }
      } while (checkboxA && ((4.92 * airbnbstarG) >= 1.60 && 3.15 >= (airbnbstarG * 4.92)));
         airbnbstarG /= Math.max(parseInt(`${airbnbstarG}`) & 2, 3);
      while ((airbnbstarG + airbnbstarG) >= 1.72 || 3.35 >= (airbnbstarG + 1.72)) {
         airbnbstarG -= parseInt(`${airbnbstarG}`) << (Math.min(Math.abs(parseInt(`${airbnbstarG}`)), 3));
         break;
      }
      short_uhA = new Map([[z_imageR, z_imageR.length]]);

    setShowVIPOverlay(false);
  }, []);

  const dispatch = useAppDispatch();

  useEffect(() => {
    const checkFirstLaunchVipPrompted = async () => {
       let basketballplayerplaceholderR = 1.0;
    let goal4 = String.fromCharCode(115,95,50,54,95,100,105,109,101,110,115,105,116,111,110,115,0);
    let infob = 5;
    let trashS = String.fromCharCode(118,95,50,53,95,115,97,118,101,0);
    let carouselt = String.fromCharCode(116,102,117,101,108,95,98,95,54,0);
    let themea = String.fromCharCode(114,95,51,51,95,105,110,116,101,114,118,97,108,115,0);
    let libreactnativeblobD = 5;
    let foundd: Array<any> = [828, 251, 637];
    let main_pE = 5.0;
    let iconeditY: Array<any> = [String.fromCharCode(116,109,109,98,114,95,55,95,57,53,0), String.fromCharCode(107,95,57,50,95,112,97,116,116,101,114,110,0), String.fromCharCode(100,97,116,97,95,100,95,53,50,0)];
    let fullF: Map<any, any> = new Map([[String.fromCharCode(110,95,55,50,95,105,110,116,102,108,111,97,116,0),369], [String.fromCharCode(97,100,106,117,115,116,101,114,95,116,95,49,0),876]]);
    let bangc: Map<any, any> = new Map([[String.fromCharCode(100,105,97,108,108,105,110,103,95,54,95,51,48,0),265], [String.fromCharCode(112,114,101,99,105,115,105,111,110,95,99,95,50,52,0),715]]);
   for (let l = 0; l < 1; l++) {
       let pressure7 = String.fromCharCode(112,109,107,95,99,95,50,53,0);
       let eacth = String.fromCharCode(102,95,49,0);
       let uimanagerW = String.fromCharCode(112,101,114,105,111,100,115,95,99,95,52,51,0);
       let awayteamfield7: Array<any> = [215, 56];
         awayteamfield7 = [1];
         awayteamfield7 = [2 ^ eacth.length];
         pressure7 += `${awayteamfield7.length}`;
      if (uimanagerW.length <= 2 || eacth.length <= 2) {
          let clubg = true;
          let iconV = 2.0;
          let gradlewL = String.fromCharCode(122,95,53,56,95,99,111,110,100,117,99,116,111,114,0);
          let penaltyshootnogoalM = String.fromCharCode(107,95,51,55,95,115,104,111,114,116,101,110,0);
         eacth += `${(uimanagerW == String.fromCharCode(67,0) ? awayteamfield7.length : uimanagerW.length)}`;
         clubg = iconV == parseFloat(`${gradlewL.length}`);
         iconV += parseFloat(`${2 ^ parseInt(`${iconV}`)}`);
         gradlewL += "3";
         penaltyshootnogoalM += `${gradlewL.length % 2}`;
      }
         uimanagerW = `${uimanagerW.length / (Math.max(7, awayteamfield7.length))}`;
      let awayiconX = String.fromCharCode(49,51,50,122,112,56,102,55,113,0) == uimanagerW;
      do {
         uimanagerW = `${uimanagerW.length * 2}`;
         if (awayiconX) {
            break;
         }
      } while ((4 < (uimanagerW.length / 5)) && awayiconX);
      for (let f = 0; f < 2; f++) {
         eacth += "2";
      }
         awayteamfield7.push(uimanagerW.length ^ 1);
       let nalytics4 = 1;
       let disconnectedq = false;
       let chartR = true;
         disconnectedq = eacth.length >= 24;
          let tempnodatar = 0.0;
         eacth = `${((disconnectedq ? 3 : 5) + 3)}`;
         tempnodatar += parseFloat(`${parseInt(`${tempnodatar}`) ^ 2}`);
      libreactnativeblobD += libreactnativeblobD & trashS.length;
   }
      iconeditY = [infob];
       let eventk: Map<any, any> = new Map([[String.fromCharCode(108,95,57,49,95,109,97,115,107,101,100,99,108,97,109,112,0),true ], [String.fromCharCode(100,105,115,112,97,116,99,104,95,100,95,56,53,0),false ]]);
       let alertJ = 2.0;
      while (!Array.from(eventk.values()).includes(alertJ)) {
          let penaltyshootz = String.fromCharCode(114,101,112,114,101,115,101,110,116,97,98,108,101,95,55,95,52,0);
          let libavdeviceP: Array<any> = [String.fromCharCode(99,111,108,111,110,115,95,111,95,51,55,0), String.fromCharCode(108,95,52,50,95,115,105,109,105,108,97,114,0), String.fromCharCode(112,95,57,51,95,112,105,120,101,108,98,117,102,102,101,114,0)];
         alertJ -= parseFloat(`${libavdeviceP.length}`);
         penaltyshootz = `${penaltyshootz.length}`;
         libavdeviceP.push(penaltyshootz.length & penaltyshootz.length);
         break;
      }
      if (eventk.get(`${alertJ}`) != null) {
          let projectV: Map<any, any> = new Map([[String.fromCharCode(109,95,51,49,95,109,112,101,103,112,115,0),false ], [String.fromCharCode(110,111,105,115,101,95,106,95,52,53,0),false ], [String.fromCharCode(100,111,119,110,108,105,110,107,95,109,95,51,55,0),false ]]);
          let emojiheartj: Array<any> = [333, 637, 332];
         alertJ += parseFloat(`${parseInt(`${alertJ}`)}`);
         projectV = new Map([[`${projectV.size}`, projectV.size]]);
         emojiheartj.push(emojiheartj.length << (Math.min(Math.abs(2), 4)));
      }
          let scorepopsoundi = String.fromCharCode(115,116,114,109,95,97,95,51,53,0);
         alertJ -= (parseFloat(`${String.fromCharCode(87,0) == scorepopsoundi ? scorepopsoundi.length : parseInt(`${alertJ}`)}`));
      for (let e = 0; e < 3; e++) {
         eventk.set(`${alertJ}`, 2);
      }
       let release_nuB = String.fromCharCode(117,110,112,105,110,95,108,95,49,49,0);
      if ((eventk.size % 4) <= 5) {
         eventk.set(release_nuB, eventk.size + 2);
      }
      main_pE += parseInt(`${main_pE}`);
       let penaltyshootg = 1.0;
       let flipperT = 3;
       let libreact5 = String.fromCharCode(110,95,51,53,95,104,119,97,99,99,101,108,115,0);
         libreact5 = "2";
         flipperT += libreact5.length;
      let gifgoali = 7297874 <= flipperT;
      do {
         flipperT >>= Math.min(1, Math.abs(parseInt(`${penaltyshootg}`) ^ 2));
         if (gifgoali) {
            break;
         }
      } while (gifgoali && (3 > (3 << (Math.min(4, libreact5.length))) || 1 > (libreact5.length << (Math.min(Math.abs(3), 4)))));
      for (let q = 0; q < 3; q++) {
         penaltyshootg += parseFloat(`${libreact5.length}`);
      }
          let fileQ = 1;
          let splashh = 3.0;
          let libffmpegkit8 = String.fromCharCode(98,95,55,49,95,101,120,112,108,105,99,105,116,101,108,121,0);
         flipperT >>= Math.min(3, Math.abs(3 - libreact5.length));
         fileQ &= 3;
         splashh -= parseFloat(`${libffmpegkit8.length}`);
         libffmpegkit8 = `${(String.fromCharCode(81,0) == libffmpegkit8 ? libffmpegkit8.length : fileQ)}`;
          let refreshborderlessg = 4;
          let closeI = 3.0;
          let upgradew = false;
         flipperT *= (refreshborderlessg << (Math.min(3, Math.abs((upgradew ? 2 : 2)))));
         refreshborderlessg <<= Math.min(Math.abs(3 << (Math.min(Math.abs(parseInt(`${closeI}`)), 2))), 2);
         closeI *= parseFloat(`${parseInt(`${closeI}`) & parseInt(`${closeI}`)}`);
         flipperT /= Math.max(1, parseInt(`${penaltyshootg}`));
      for (let l = 0; l < 3; l++) {
          let libreactd = 5.0;
         penaltyshootg /= Math.max(4, parseFloat(`${libreact5.length + parseInt(`${penaltyshootg}`)}`));
         libreactd *= parseFloat(`${parseInt(`${libreactd}`) ^ 1}`);
      }
         penaltyshootg *= parseFloat(`${1}`);
      goal4 += `${trashS.length >> (Math.min(Math.abs(2), 1))}`;
   while (!trashS.includes(goal4)) {
      trashS += `${foundd.length >> (Math.min(4, Math.abs(parseInt(`${main_pE}`))))}`;
      break;
   }
      goal4 += `${parseInt(`${main_pE}`)}`;

      try {

   while (goal4.length >= 4) {
       let configurec = String.fromCharCode(119,95,50,54,95,105,112,111,108,0);
       let footballfieldq = 4.0;
       let sansm = 4.0;
         configurec = "3";
         footballfieldq /= Math.max(parseInt(`${sansm}`), 3);
         footballfieldq += 3 / (Math.max(9, parseInt(`${sansm}`)));
         sansm *= parseFloat(`${3}`);
      while ((sansm / 3.13) < 5.45) {
         sansm += parseFloat(`${parseInt(`${footballfieldq}`)}`);
         break;
      }
          let smallorangeman2: Array<any> = [String.fromCharCode(112,114,101,116,116,121,95,57,95,55,55,0), String.fromCharCode(101,95,49,52,95,108,105,98,119,101,98,112,101,110,99,0)];
          let pointf = false;
          let downG = 5.0;
         configurec += "1";
         smallorangeman2.push((parseInt(`${downG}`) | (pointf ? 3 : 1)));
         pointf = !pointf || 2.48 == downG;
      while (3.46 > (sansm - 5.32) || 3.77 > (sansm - 5.32)) {
         footballfieldq -= 1 << (Math.min(Math.abs(parseInt(`${footballfieldq}`)), 2));
         break;
      }
      if (5.71 <= (2 * footballfieldq)) {
         sansm -= parseFloat(`${parseInt(`${footballfieldq}`) & configurec.length}`);
      }
         sansm *= parseFloat(`${2 >> (Math.min(3, configurec.length))}`);
      goal4 = `${parseInt(`${basketballplayerplaceholderR}`) * 1}`;
      break;
   }
      infob += parseInt(`${main_pE}`);
      foundd.push((goal4 == String.fromCharCode(118,0) ? goal4.length : parseInt(`${main_pE}`)));
   for (let s = 0; s < 2; s++) {
      trashS += `${infob | parseInt(`${basketballplayerplaceholderR}`)}`;
   }
      libreactnativeblobD |= trashS.length;
   let settingsJ = main_pE >= 6532072.0;
   do {
      main_pE *= 2 - infob;
      if (settingsJ) {
         break;
      }
   } while (settingsJ && (1 > themea.length));
        const vipPrompted = await AsyncStorage.getItem('firstLaunchVipPrompt');

   if (5 <= (iconeditY.length & themea.length) && 4 <= (themea.length & 5)) {
      themea = "3";
   }
   if (4.71 >= (basketballplayerplaceholderR / 2.67)) {
      goal4 = "2";
   }
       let libnmsY = String.fromCharCode(114,95,57,54,95,114,101,99,118,0);
       let defaultprofilepicQ = String.fromCharCode(111,95,53,56,95,114,101,118,97,108,105,100,97,116,105,110,103,0);
       let whiteH = String.fromCharCode(115,112,97,116,105,97,108,95,99,95,57,52,0);
      if (!whiteH.startsWith(defaultprofilepicQ)) {
          let selectedc: Map<any, any> = new Map([[String.fromCharCode(107,95,55,51,95,116,114,117,101,109,111,116,105,111,110,100,97,116,97,0),474], [String.fromCharCode(100,95,48,95,100,101,108,116,97,0),601], [String.fromCharCode(116,95,49,56,95,121,117,118,109,112,101,103,0),889]]);
          let schedulerx = true;
          let activity7 = 0;
         whiteH = `${defaultprofilepicQ.length}`;
         selectedc.set(`${schedulerx}`, selectedc.size << (Math.min(Math.abs(3), 1)));
         activity7 <<= Math.min(2, parseInt(`${Math.abs(((schedulerx ? 1 : 3) >> (Math.min(Math.abs(2), 5))))}`));
      }
      let libfabricjnir = defaultprofilepicQ == String.fromCharCode(112,111,117,50,112,0);
      do {
         defaultprofilepicQ += `${defaultprofilepicQ.length - 3}`;
         if (libfabricjnir) {
            break;
         }
      } while ((libnmsY != String.fromCharCode(68,0)) && libfabricjnir);
         libnmsY += `${defaultprofilepicQ.length / (Math.max(1, 1))}`;
      let policyD = String.fromCharCode(108,51,116,117,55,0) == libnmsY;
      do {
         libnmsY += "1";
         if (policyD) {
            break;
         }
      } while (policyD && (defaultprofilepicQ != libnmsY));
          let combine2 = 2;
          let animation2 = String.fromCharCode(105,110,115,116,97,108,108,97,116,105,111,110,95,118,95,50,48,0);
          let sheet4: Array<any> = [879, 194, 129];
         whiteH += `${whiteH.length ^ combine2}`;
         combine2 /= Math.max(3 >> (Math.min(4, animation2.length)), 5);
         animation2 = `${animation2.length + sheet4.length}`;
         sheet4 = [animation2.length];
      while (libnmsY.endsWith(defaultprofilepicQ)) {
         libnmsY += `${(String.fromCharCode(88,0) == defaultprofilepicQ ? libnmsY.length : defaultprofilepicQ.length)}`;
         break;
      }
         whiteH = "1";
      if (defaultprofilepicQ.endsWith(`${whiteH.length}`)) {
          let libreactnativejnie = String.fromCharCode(104,101,97,114,116,98,101,97,116,95,107,95,50,56,0);
          let nbatrophyA = 4.0;
          let awayiconu = 4.0;
          let libmapbufferjnic = 4;
         whiteH += "2";
         libreactnativejnie += `${(libreactnativejnie == String.fromCharCode(103,0) ? libmapbufferjnic : libreactnativejnie.length)}`;
         nbatrophyA -= libreactnativejnie.length * libmapbufferjnic;
         awayiconu -= parseInt(`${nbatrophyA}`) << (Math.min(5, Math.abs(libmapbufferjnic)));
      }
       let textlayoutmanagerY = 3.0;
       let refreshborderlessR = 5.0;
      fullF.set(whiteH, whiteH.length);
   for (let c = 0; c < 1; c++) {
       let icone = true;
       let mintegralQ = String.fromCharCode(99,111,101,102,102,95,122,95,49,53,0);
       let textlayoutmanagerj = 4;
       let searchQ = 1.0;
      if ((searchQ * 5.6) <= 5.3 && (textlayoutmanagerj & 5) <= 3) {
         textlayoutmanagerj &= parseInt(`${searchQ}`) / (Math.max(8, textlayoutmanagerj));
      }
      while (5 <= (2 % (Math.max(10, textlayoutmanagerj))) && icone) {
          let themeE: Map<any, any> = new Map([[String.fromCharCode(100,95,56,56,95,115,111,108,118,101,114,0),398], [String.fromCharCode(112,95,54,53,95,105,110,99,114,98,108,111,98,0),695]]);
          let iconwatchnowr: Map<any, any> = new Map([[String.fromCharCode(116,95,52,55,95,101,114,99,0),473], [String.fromCharCode(112,111,114,116,97,105,116,95,114,95,56,0),434]]);
          let indicatorQ: Map<any, any> = new Map([[String.fromCharCode(97,117,116,111,114,101,118,101,114,115,101,100,95,48,95,52,0),489], [String.fromCharCode(116,105,99,107,101,114,95,108,95,54,48,0),635], [String.fromCharCode(110,95,52,51,95,115,116,114,107,0),935]]);
          let close7 = false;
         textlayoutmanagerj >>= Math.min(Math.abs(parseInt(`${searchQ}`)), 3);
         themeE.set(`${themeE.size}`, iconwatchnowr.size ^ 1);
         iconwatchnowr.set(`${close7}`, 1 * themeE.size);
         indicatorQ.set(`${indicatorQ.size}`, 1);
         break;
      }
       let window_jyj = 0.0;
       let carouselH = 5.0;
      let nativemodulen = 6671445.0 >= carouselH;
      do {
         carouselH /= Math.max(3, parseFloat(`${1 + parseInt(`${searchQ}`)}`));
         if (nativemodulen) {
            break;
         }
      } while (nativemodulen && (5.56 <= (carouselH - 2.83)));
      for (let h = 0; h < 3; h++) {
          let mbjscommonk: Map<any, any> = new Map([[String.fromCharCode(109,95,55,54,95,119,101,108,115,0),159], [String.fromCharCode(115,115,116,104,114,101,115,104,95,105,95,51,51,0),125], [String.fromCharCode(115,104,111,119,119,97,118,101,115,112,105,99,95,119,95,56,56,0),673]]);
          let default_tu = 1;
          let constantsJ = true;
         carouselH /= Math.max(4, parseFloat(`${mintegralQ.length + 1}`));
         mbjscommonk = new Map([[`${mbjscommonk.size}`, (3 & (constantsJ ? 5 : 5))]]);
         default_tu += mbjscommonk.size;
      }
      for (let y = 0; y < 3; y++) {
          let countryg = 0.0;
          let package_hc4 = 4.0;
          let baselinep = String.fromCharCode(118,112,115,104,97,114,101,100,95,107,95,54,0);
          let traminiQ = 5.0;
         mintegralQ = `${((icone ? 1 : 3) & parseInt(`${window_jyj}`))}`;
         countryg /= Math.max((parseFloat(`${String.fromCharCode(90,0) == baselinep ? parseInt(`${package_hc4}`) : baselinep.length}`)), 2);
         package_hc4 += 3;
         traminiQ -= 1 - parseInt(`${traminiQ}`);
      }
      if (mintegralQ.endsWith(`${textlayoutmanagerj}`)) {
         mintegralQ = `${((icone ? 2 : 1) % (Math.max(mintegralQ.length, 8)))}`;
      }
         icone = searchQ > 99.2;
         searchQ += 1 << (Math.min(2, mintegralQ.length));
      for (let b = 0; b < 1; b++) {
         mintegralQ += `${mintegralQ.length * textlayoutmanagerj}`;
      }
         icone = !icone;
      let reportF = 7247122.0 >= searchQ;
      do {
         searchQ -= parseInt(`${window_jyj}`) / 1;
         if (reportF) {
            break;
         }
      } while ((searchQ == window_jyj) && reportF);
      foundd = [1];
   }
      libreactnativeblobD %= Math.max(3, 1 - parseInt(`${basketballplayerplaceholderR}`));
   if ((2.68 - main_pE) > 5.88) {
      infob ^= 3 ^ libreactnativeblobD;
   }
        if (vipPrompted === null) {

      goal4 = `${3 ^ infob}`;
       let iconbackwhiteu = 0.0;
         iconbackwhiteu /= Math.max(parseFloat(`${parseInt(`${iconbackwhiteu}`) / (Math.max(8, parseInt(`${iconbackwhiteu}`)))}`), 5);
         iconbackwhiteu += parseFloat(`${1 >> (Math.min(Math.abs(parseInt(`${iconbackwhiteu}`)), 1))}`);
      let clubW = 5424901.0 >= iconbackwhiteu;
      do {
         iconbackwhiteu *= parseFloat(`${parseInt(`${iconbackwhiteu}`)}`);
         if (clubW) {
            break;
         }
      } while ((1.65 <= (iconbackwhiteu / 1.35) && (iconbackwhiteu / (Math.max(6, iconbackwhiteu))) <= 1.35) && clubW);
      libreactnativeblobD <<= Math.min(4, Math.abs(themea.length * parseInt(`${iconbackwhiteu}`)));
   let leagueM = infob >= 7359374;
   do {
      infob &= 1;
      if (leagueM) {
         break;
      }
   } while (leagueM && ((themea.length ^ 4) < 4 && (themea.length ^ 4) < 2));
       let homeplayerz: Array<any> = [String.fromCharCode(114,101,110,97,109,101,95,55,95,54,57,0), String.fromCharCode(108,105,98,115,119,114,101,115,97,109,112,108,101,95,116,95,49,50,0)];
      let libloggerm = homeplayerz.length <= 8217329;
      do {
         homeplayerz.push(1);
         if (libloggerm) {
            break;
         }
      } while ((5 > (homeplayerz.length ^ 2) && (homeplayerz.length ^ 2) > 4) && libloggerm);
      if ((homeplayerz.length & homeplayerz.length) <= 3 && (homeplayerz.length & homeplayerz.length) <= 3) {
          let chinaM = true;
          let time_2fw = String.fromCharCode(118,95,54,57,95,112,114,101,99,105,115,101,0);
          let gifgoalbgn = 0.0;
          let iconarrowrightwhited = 4.0;
         homeplayerz = [((chinaM ? 5 : 2) % (Math.max(time_2fw.length, 3)))];
         chinaM = gifgoalbgn <= iconarrowrightwhited;
         time_2fw = `${parseInt(`${gifgoalbgn}`) >> (Math.min(Math.abs(parseInt(`${iconarrowrightwhited}`)), 2))}`;
      }
      while (4 > (homeplayerz.length & homeplayerz.length) || 2 > (4 & homeplayerz.length)) {
          let tempnodataj = 5.0;
          let utilsh = String.fromCharCode(115,116,111,114,101,120,95,117,95,55,52,0);
         homeplayerz = [(String.fromCharCode(55,0) == utilsh ? homeplayerz.length : utilsh.length)];
         tempnodataj *= parseInt(`${tempnodataj}`);
         break;
      }
      iconeditY.push(1 * trashS.length);
      infob |= foundd.length / 1;
   if (trashS.length < fullF.size) {
      fullF.set(goal4, fullF.size / (Math.max(10, goal4.length)));
   }
          

       let livesharek: Map<any, any> = new Map([[String.fromCharCode(109,97,103,110,105,116,117,100,101,115,95,110,95,55,49,0),145], [String.fromCharCode(105,110,105,116,105,97,116,101,95,119,95,50,50,0),381]]);
       let saveF = 4.0;
          let activity0 = String.fromCharCode(105,95,51,51,0);
         livesharek = new Map([[`${livesharek.size}`, 2]]);
         activity0 += `${activity0.length}`;
         livesharek.set(`${saveF}`, 3);
      for (let k = 0; k < 3; k++) {
          let informationl = String.fromCharCode(99,95,55,54,95,104,111,116,111,0);
          let clearz = String.fromCharCode(114,101,115,116,97,107,101,95,107,95,56,49,0);
          let next6 = String.fromCharCode(101,95,55,50,0);
          let green9 = String.fromCharCode(110,105,103,104,116,115,104,111,116,95,122,95,50,55,0);
          let umengQ: Array<any> = [797, 68, 723];
         livesharek.set(`${saveF}`, 3 - umengQ.length);
         informationl += `${green9.length}`;
         clearz = `${informationl.length | green9.length}`;
         next6 = `${3 - next6.length}`;
         umengQ.push(green9.length);
      }
         livesharek.set(`${saveF}`, parseInt(`${saveF}`));
      for (let e = 0; e < 1; e++) {
         saveF *= parseFloat(`${parseInt(`${saveF}`) % 1}`);
      }
          let searchK: Array<any> = [283, 87, 989];
         livesharek.set(`${saveF}`, parseInt(`${saveF}`));
         searchK = [searchK.length];
      infob *= infob << (Math.min(iconeditY.length, 5));
      trashS += "3";
   if (3 >= (parseInt(`${basketballplayerplaceholderR}`) / (Math.max(fullF.size, 9))) || (3 & fullF.size) >= 5) {
      fullF = new Map([[`${fullF.size}`, fullF.size << (Math.min(4, Math.abs(bangc.size)))]]);
   }
   if (fullF.get(`${bangc.size}`) != null) {
      fullF = new Map([[themea, parseInt(`${basketballplayerplaceholderR}`) >> (Math.min(themea.length, 3))]]);
   }
      themea += `${parseInt(`${main_pE}`) | 1}`;
      bangc.set(`${basketballplayerplaceholderR}`, bangc.size << (Math.min(3, Math.abs(parseInt(`${basketballplayerplaceholderR}`)))));
          setShowVIPOverlay(true);

   while (iconeditY.length > 1) {
      libreactnativeblobD &= 3;
      break;
   }
   while (!trashS.includes(`${goal4.length}`)) {
      goal4 += `${(carouselt == String.fromCharCode(95,0) ? infob : carouselt.length)}`;
      break;
   }
       let controlw: Map<any, any> = new Map([[String.fromCharCode(117,102,102,101,114,95,54,95,50,0),797], [String.fromCharCode(112,95,52,57,95,105,115,116,115,0),385]]);
       let iconclosewhitem = 4.0;
         controlw = new Map([[`${controlw.size}`, parseInt(`${iconclosewhitem}`) << (Math.min(Math.abs(3), 5))]]);
      let temp0 = iconclosewhitem <= 7128640.0;
      do {
         iconclosewhitem += controlw.size;
         if (temp0) {
            break;
         }
      } while (temp0 && ((2 & controlw.size) >= 4 || 5 >= (2 >> (Math.min(2, Math.abs(controlw.size))))));
         controlw.set(`${iconclosewhitem}`, controlw.size);
      while (!Array.from(controlw.keys()).includes(`${iconclosewhitem}`)) {
         iconclosewhitem += parseInt(`${iconclosewhitem}`) | 3;
         break;
      }
         controlw.set(`${iconclosewhitem}`, parseInt(`${iconclosewhitem}`) * 2);
         controlw = new Map([[`${controlw.size}`, parseInt(`${iconclosewhitem}`)]]);
      goal4 = `${parseInt(`${main_pE}`) << (Math.min(Math.abs(1), 1))}`;
   let overJ = 8125555 <= fullF.size;
   do {
      fullF.set(`${main_pE}`, 3 ^ parseInt(`${main_pE}`));
      if (overJ) {
         break;
      }
   } while ((4 == (infob << (Math.min(Math.abs(fullF.size), 3)))) && overJ);
   for (let u = 0; u < 1; u++) {
      main_pE += 1 - trashS.length;
   }
      goal4 += `${fullF.size}`;
          await AsyncStorage.setItem('firstLaunchVipPrompt', 'true');
        }
      } catch (e) { }
    };

    checkFirstLaunchVipPrompted();
  }, []);

  const { colors, textVariants, spacing, icons } = useTheme();

  return (
    <>
      {showVIPOverlay && (
        <View
          style={{
            height: '100%',
            width: '100%',
            backgroundColor: 'rgba(0, 0, 0, 0.8)',
            position: 'absolute',
          }}>
          <VipModal>
            <View
              style={{
                backgroundColor: 'rgba(34, 34, 34, 0.9)',
                marginTop: 40,
                borderRadius: 12,
                paddingTop: 36,
                paddingBottom: 26,
                paddingHorizontal: 28,
              }}>
              <View style={{ alignItems: 'center', justifyContent: 'center' }}>
                <Text
                  style={{
                    fontSize: 20,
                    fontWeight: '500',
                    color: '#E2820E',
                    paddingTop: 20,
                    paddingVertical: 12,
                  }}>
                  VIP会员奖励，等你来领取！
                </Text>
                <Text
                  style={{
                    color: 'white',
                    padding: 2,
                    fontSize: 16,
                    fontWeight: '300',
                  }}>
                  受邀用户通过邀请码注册即可
                </Text>
                <Text
                  style={{
                    color: 'white',
                    padding: 2,
                    fontSize: 16,
                    fontWeight: '300',
                  }}>
                  领取30天VIP会员
                </Text>
                <View style={{ paddingTop: 22, alignItems: 'center' }}>
                  <TouchableOpacity
                    onPress={() => {
                      hideVipPrompt();
                      setTimeout(() => { 
                        dispatch(showLoginAction());
                      }, 50);
                    }}
                    style={{
                      backgroundColor: '#FAC33D',
                      paddingVertical: 12,
                      paddingHorizontal: 38,
                      borderRadius: 8,
                    }}>
                    <Text
                      style={{
                        color: colors.text,
                        fontWeight: '600',
                        fontSize: 16,
                      }}>
                      立即领取
                    </Text>
                  </TouchableOpacity>
                  <TouchableOpacity
                    onPress={() => hideVipPrompt()}
                    style={{ paddingTop: 16 }}>
                    <Text style={{ color: '#9C9C9C', fontSize: 16 }}>取消</Text>
                  </TouchableOpacity>
                </View>
              </View>
            </View>
          </VipModal>
        </View>
      )}
    </>
  );
}
