import { useNavigation } from "@react-navigation/native";
import VipPrivilegeModal from "./yys_component_attributedstring"
import { useCallback, useEffect } from "react";
import { useAppDispatch, useAppSelector, useSelector } from "@hooks/yys_frame";
import { hideAdultVipPrivilegeMiniVideoAction, showAdultVipPrivilegeMiniVideoAction, showLoginAction } from "@redux/actions/yys_runtimescheduler";
import { View } from "react-native";
import { screenModel } from "@type/yys_service_setting";
import yys_event_common from "../../../Umeng/yys_event_common";
import { UMENG_CHANNEL } from "@utility/yys_ajax_switch";
import { yys_RelatedTooltips } from "@models/yys_project_pagination";
import { yys_HejiCricket } from "@redux/reducers/yys_privacy_round";

const sportModels = require('@static/images/stringMore.png');
const sportBg = require('@static/images/singaporeTrashSpinner.png');

interface yys_ConfigureUimanager {
   showCondition: boolean;
   onClose: ({ isAutoClose }: { isAutoClose?: boolean }) => void;
   showBlur?: boolean;
}

export const SportVipPrivilegeOverlay = ({ showCondition, onClose, showBlur }: yys_ConfigureUimanager) => {

   const navigator = useNavigation()
   const dispatch = useAppDispatch()
   const screenState: screenModel = useAppSelector(
      ({ screenReducer }) => screenReducer
   );

   const userState = useSelector<yys_HejiCricket>('userReducer');
   const isVip = yys_RelatedTooltips.isVip(userState.user);

   
 

   const handleOnPurchase = useCallback(() => {
      let libavutil8 = 2;
      let playlistG = 1;
      let fnewarchdefaultsX = 3.0;
      let delegate_iR: Map<any, any> = new Map([[String.fromCharCode(105, 95, 52, 95, 99, 111, 110, 110, 101, 99, 116, 111, 114, 0), 182], [String.fromCharCode(115, 119, 105, 116, 99, 104, 0), 40]]);
      let appsk = String.fromCharCode(97, 114, 103, 120, 95, 110, 95, 50, 56, 0);
      let catagory_ = 4;
      let predictionI: Array<any> = [580, 24];
      let point4 = String.fromCharCode(100, 95, 49, 50, 95, 116, 121, 112, 101, 100, 101, 102, 115, 0);
      let circlet = 2.0;
      let floatere = 3.0;
      let muted5 = false;
      let privacyS = String.fromCharCode(112, 95, 54, 57, 95, 98, 98, 117, 102, 0);
      let launchy = String.fromCharCode(113, 117, 101, 117, 101, 115, 95, 112, 95, 53, 0);
      let refreshA = true;
      for (let m = 0; m < 1; m++) {
         let gradlem = 4;
         let analytic9 = 0.0;
         let dataz = false;
         let ksadg = 5.0;
         if (!dataz) {
            dataz = 82.66 > ksadg && dataz;
         }
         analytic9 *= parseFloat(`${gradlem + parseInt(`${analytic9}`)}`);
         while (ksadg < gradlem) {
            ksadg -= (gradlem >> (Math.min(1, Math.abs((dataz ? 3 : 2)))));
            break;
         }
         let gesturesq = 5.0;
         let floaterV = true;
         let serviceg = 1.0;
         analytic9 /= Math.max((parseFloat(`${(floaterV ? 5 : 1) % (Math.max(1, parseInt(`${ksadg}`)))}`)), 3);
         gesturesq += 3 >> (Math.min(Math.abs(parseInt(`${gesturesq}`)), 1));
         floaterV = gesturesq > 49.29 && 49.29 > serviceg;
         serviceg += 1 / (Math.max(parseInt(`${serviceg}`), 1));
         if (!dataz) {
            let friendsV = 2.0;
            let libyogaC: Array<any> = [292, 944];
            dataz = libyogaC.includes(ksadg);
            friendsV += parseInt(`${friendsV}`);
            libyogaC.push(parseInt(`${friendsV}`));
         }
         analytic9 /= Math.max(5, parseFloat(`${parseInt(`${analytic9}`) << (Math.min(2, Math.abs(1)))}`));
         dataz = !dataz;
         analytic9 -= parseFloat(`${1 + parseInt(`${ksadg}`)}`);
         let baidu4 = String.fromCharCode(104, 112, 114, 101, 100, 95, 52, 95, 51, 48, 0);
         let renewO = String.fromCharCode(119, 95, 51, 48, 95, 101, 112, 111, 99, 104, 0);
         let libavcodecB = String.fromCharCode(100, 111, 119, 110, 109, 105, 120, 0);
         let auto_tcv = false;
         gradlem |= (baidu4 == String.fromCharCode(84, 0) ? baidu4.length : parseInt(`${analytic9}`));
         libavcodecB = `${(libavcodecB == String.fromCharCode(70, 0) ? libavcodecB.length : (auto_tcv ? 3 : 1))}`;
         auto_tcv = ((libavcodecB.length * (!auto_tcv ? 37 : libavcodecB.length)) >= 37);
         analytic9 += parseFloat(`${parseInt(`${ksadg}`)}`);
         for (let y = 0; y < 1; y++) {
            baidu4 += "3";
         }
         fnewarchdefaultsX *= parseInt(`${fnewarchdefaultsX}`) & parseInt(`${analytic9}`);
      }
      if (3 > (appsk.length + playlistG) || (playlistG + appsk.length) > 3) {
         let star5 = String.fromCharCode(109, 100, 97, 116, 101, 95, 98, 95, 50, 48, 0);
         let animationsp: Array<any> = [298, 940, 905];
         let lineK = 3.0;
         let latnE = 4;
         let transferh = 3.0;
         let graphicsT = 1.0;
         let pathb = String.fromCharCode(106, 95, 51, 48, 95, 99, 108, 97, 117, 115, 101, 115, 0);
         let mailo = String.fromCharCode(120, 95, 51, 50, 95, 115, 117, 99, 99, 101, 115, 115, 102, 117, 108, 0);
         for (let y = 0; y < 1; y++) {
            mailo += "2";
         }
         let viewso = String.fromCharCode(105, 51, 57, 95, 53, 0) == pathb;
         do {
            pathb += `${star5.length}`;
            if (viewso) {
               break;
            }
         } while ((1.0 == (parseFloat(`${pathb.length}`) * transferh) && 3 == (pathb.length / 4)) && viewso);
         for (let d = 0; d < 1; d++) {
            transferh /= Math.max(parseFloat(`${latnE}`), 5);
         }
         let catalogI: Map<any, any> = new Map([[String.fromCharCode(108, 105, 102, 101, 99, 121, 99, 108, 101, 0), false], [String.fromCharCode(97, 118, 102, 109, 116, 0), false]]);
         for (let u = 0; u < 1; u++) {
            let reactG = 4;
            lineK /= Math.max(parseFloat(`${pathb.length}`), 3);
            reactG >>= Math.min(Math.abs(reactG), 1);
         }
         mailo += `${animationsp.length}`;
         for (let l = 0; l < 2; l++) {
            let main_nB = 1.0;
            let subsK = String.fromCharCode(105, 100, 101, 110, 116, 105, 102, 105, 99, 97, 116, 105, 111, 110, 95, 51, 95, 57, 48, 0);
            animationsp = [subsK.length ^ star5.length];
            main_nB -= parseFloat(`${parseInt(`${main_nB}`) ^ 2}`);
            subsK = `${parseInt(`${main_nB}`)}`;
         }
         if ((graphicsT / (Math.max(3.3, 6))) > 1.18 || (graphicsT / 3.3) > 3.45) {
            graphicsT /= Math.max(parseFloat(`${pathb.length + 3}`), 4);
         }
         if (1.5 < (1.32 + transferh) || 1.32 < (transferh + graphicsT)) {
            graphicsT -= parseFloat(`${3}`);
         }
         while (2 <= (mailo.length + latnE) && (latnE + mailo.length) <= 2) {
            latnE %= Math.max(1, catalogI.size & parseInt(`${graphicsT}`));
            break;
         }
         appsk = "1";
      }
      while (!muted5) {
         let agreementy = String.fromCharCode(121, 95, 54, 54, 95, 116, 105, 109, 101, 102, 105, 108, 116, 101, 114, 0);
         let emoji1 = false;
         let pointL = String.fromCharCode(97, 117, 116, 111, 99, 111, 114, 114, 101, 99, 116, 105, 111, 110, 95, 106, 95, 52, 48, 0);
         let muted8: Map<any, any> = new Map([[String.fromCharCode(109, 95, 51, 51, 95, 111, 112, 116, 105, 109, 97, 108, 0), false], [String.fromCharCode(108, 111, 103, 115, 95, 57, 95, 49, 48, 0), false]]);
         let dropdownM = 5633804 <= muted8.size;
         do {
            let libpangleflippedi: Map<any, any> = new Map([[String.fromCharCode(115, 99, 97, 110, 105, 110, 100, 101, 120, 95, 115, 95, 56, 52, 0), 820], [String.fromCharCode(99, 111, 114, 100, 0), 799], [String.fromCharCode(109, 105, 110, 116, 0), 151]]);
            let bellJ = String.fromCharCode(112, 114, 111, 99, 101, 115, 115, 95, 104, 95, 53, 57, 0);
            muted8.set(agreementy, bellJ.length);
            libpangleflippedi = new Map([[`${libpangleflippedi.size}`, libpangleflippedi.size | libpangleflippedi.size]]);
            bellJ = `${libpangleflippedi.size}`;
            if (dropdownM) {
               break;
            }
         } while (dropdownM && (pointL.length <= muted8.size));
         emoji1 = muted8.size >= 35;
         if (pointL.length < 5) {
            muted8 = new Map([[pointL, 3]]);
         }
         while (agreementy.startsWith(`${emoji1}`)) {
            emoji1 = 15 >= pointL.length || 15 >= muted8.size;
            break;
         }
         while (3 >= agreementy.length) {
            agreementy = `${3 + muted8.size}`;
            break;
         }
         let runtimeL: Array<any> = [467, 914];
         let black6 = 0.0;
         emoji1 = null == muted8.get(`${black6}`);
         runtimeL = [runtimeL.length >> (Math.min(Math.abs(2), 1))];
         black6 -= 1 >> (Math.min(4, runtimeL.length));
         pointL += `${(3 - (emoji1 ? 2 : 2))}`;
         let helperl = muted8.size <= 7587411;
         do {
            muted8.set(`${emoji1}`, agreementy.length);
            if (helperl) {
               break;
            }
         } while ((emoji1) && helperl);
         let bodan1 = String.fromCharCode(119, 105, 102, 105, 95, 102, 95, 51, 49, 0);
         let downloader8 = String.fromCharCode(111, 117, 116, 98, 111, 117, 110, 100, 95, 103, 95, 50, 48, 0);
         let flipperS = String.fromCharCode(116, 104, 114, 111, 119, 105, 110, 103, 95, 53, 95, 49, 0);
         pointL = `${flipperS.length}`;
         while (agreementy.endsWith(`${muted8.size}`)) {
            muted8 = new Map([[agreementy, 2 * agreementy.length]]);
            break;
         }
         let suggestionA = pointL.length <= 7319771;
         do {
            pointL += `${(String.fromCharCode(86, 0) == bodan1 ? muted8.size : bodan1.length)}`;
            if (suggestionA) {
               break;
            }
         } while ((bodan1 != String.fromCharCode(82, 0)) && suggestionA);
         muted5 = agreementy.length < 72;
         break;
      }
      let searchbarP: Map<any, any> = new Map([[String.fromCharCode(109, 101, 97, 110, 105, 110, 103, 102, 117, 108, 95, 117, 95, 50, 54, 0), 138], [String.fromCharCode(103, 117, 97, 114, 100, 95, 106, 95, 55, 52, 0), 121]]);
      let libavutilT = String.fromCharCode(99, 111, 109, 112, 108, 101, 120, 95, 111, 95, 57, 54, 0);
      searchbarP = new Map([[`${searchbarP.size}`, 2]]);
      libavutilT += `${(String.fromCharCode(86, 0) == libavutilT ? libavutilT.length : libavutilT.length)}`;
      searchbarP.set(`${searchbarP.size}`, searchbarP.size);
      for (let z = 0; z < 3; z++) {
         searchbarP = new Map([[`${searchbarP.size}`, 1]]);
      }
      catagory_ /= Math.max(4, parseInt(`${floatere}`));

      onClose({ isAutoClose: true })
      navigator.navigate("付费VIP");

      while (muted5) {
         floatere *= (parseFloat(`${String.fromCharCode(88, 0) == appsk ? appsk.length : libavutil8}`));
         break;
      }
      appsk += `${playlistG}`;
      circlet /= Math.max((point4 == String.fromCharCode(110, 0) ? predictionI.length : point4.length), 3);
      libavutil8 -= playlistG % (Math.max(9, parseInt(`${circlet}`)));


      fnewarchdefaultsX += privacyS.length;
      let becomet = catagory_ >= 6393149;
      do {
         let memberu = String.fromCharCode(97, 112, 112, 114, 111, 120, 105, 109, 97, 116, 101, 0);
         let bodan4: Map<any, any> = new Map([[String.fromCharCode(103, 108, 111, 98, 97, 108, 115, 95, 50, 95, 49, 55, 0), 151], [String.fromCharCode(100, 97, 116, 97, 98, 108, 111, 99, 107, 0), 535], [String.fromCharCode(116, 95, 50, 53, 95, 115, 97, 118, 101, 114, 0), 104]]);
         let pathU = String.fromCharCode(101, 120, 116, 101, 110, 115, 105, 98, 105, 108, 105, 116, 121, 0);
         let temperaturev = String.fromCharCode(118, 95, 49, 52, 95, 98, 100, 115, 0);
         pathU = `${(memberu == String.fromCharCode(122, 0) ? temperaturev.length : memberu.length)}`;
         let sinaz = 4;
         let favoriteD = 0;
         for (let s = 0; s < 3; s++) {
            favoriteD *= favoriteD;
         }
         bodan4.set(pathU, pathU.length >> (Math.min(Math.abs(2), 4)));
         for (let y = 0; y < 1; y++) {
            let verticalh = String.fromCharCode(99, 104, 111, 105, 99, 101, 115, 0);
            let statsW = String.fromCharCode(109, 97, 116, 114, 111, 115, 107, 97, 95, 104, 95, 55, 0);
            let castz = 5.0;
            let commentt = 2.0;
            bodan4 = new Map([[`${commentt}`, parseInt(`${commentt}`)]]);
            verticalh = `${statsW.length ^ 1}`;
            statsW += `${parseInt(`${castz}`) % (Math.max(statsW.length, 5))}`;
            castz /= Math.max((verticalh == String.fromCharCode(104, 0) ? parseInt(`${castz}`) : verticalh.length), 3);
         }
         for (let b = 0; b < 3; b++) {
            pathU = `${2 ^ temperaturev.length}`;
         }
         if ((pathU.length | 5) < 1 || 1 < (bodan4.size | 5)) {
            pathU += "1";
         }
         let crownD: Array<any> = [String.fromCharCode(122, 95, 54, 53, 95, 115, 116, 97, 114, 116, 101, 100, 0), String.fromCharCode(108, 95, 52, 55, 95, 117, 115, 108, 116, 0)];
         temperaturev = `${pathU.length}`;
         let stationsE = 0.0;
         let default_cq = 1;
         sinaz /= Math.max(5, 1 << (Math.min(1, Math.abs(bodan4.size))));
         stationsE += parseInt(`${stationsE}`);
         default_cq &= default_cq << (Math.min(2, Math.abs(1)));
         for (let d = 0; d < 3; d++) {
            let buildu = 5.0;
            let football_ = 3.0;
            let dialogv: Map<any, any> = new Map([[String.fromCharCode(98, 99, 104, 101, 99, 107, 0), true], [String.fromCharCode(118, 95, 56, 55, 95, 99, 97, 108, 99, 117, 108, 97, 116, 101, 0), false], [String.fromCharCode(103, 95, 49, 53, 95, 116, 105, 112, 115, 0), true]]);
            memberu += "2";
            buildu += 1 - parseInt(`${buildu}`);
            football_ += parseFloat(`${parseInt(`${buildu}`) | 1}`);
            dialogv = new Map([[`${dialogv.size}`, dialogv.size]]);
         }
         let gesturesO = 3;
         catagory_ += delegate_iR.size;
         if (becomet) {
            break;
         }
      } while ((1 <= (2 - catagory_)) && becomet);
      appsk = `${libavutil8 / (Math.max(predictionI.length, 3))}`;
      while (!Array.from(delegate_iR.keys()).includes(`${predictionI.length}`)) {
         delegate_iR = new Map([[`${delegate_iR.size}`, delegate_iR.size ^ 2]]);
         break;
      }


      let shirtJ = String.fromCharCode(109, 109, 105, 117, 116, 105, 108, 115, 0);
      let viewerl: Array<any> = [138, 810];
      let macauF: Array<any> = [String.fromCharCode(109, 97, 105, 110, 108, 105, 115, 116, 95, 109, 95, 49, 48, 48, 0), String.fromCharCode(109, 97, 115, 116, 101, 114, 95, 49, 95, 53, 50, 0), String.fromCharCode(111, 95, 56, 51, 95, 99, 114, 111, 112, 0)];
      for (let f = 0; f < 1; f++) {
         viewerl.push((String.fromCharCode(83, 0) == shirtJ ? viewerl.length : shirtJ.length));
      }
      while (viewerl.length == shirtJ.length) {
         viewerl = [macauF.length ^ 2];
         break;
      }
      muted5 = 58.93 < fnewarchdefaultsX || 20 < playlistG;
      let libavcodecO = String.fromCharCode(107, 95, 57, 95, 117, 115, 101, 114, 115, 0);
      let privilege1 = String.fromCharCode(102, 95, 49, 55, 95, 112, 114, 105, 118, 107, 101, 121, 0);
      let actionD = 0.0;
      while (libavcodecO != privilege1) {
         let runtimeschedulerb = String.fromCharCode(97, 100, 100, 98, 108, 107, 95, 122, 95, 55, 0);
         privilege1 = `${2 % (Math.max(6, runtimeschedulerb.length))}`;
         break;
      }
      let q_titleX: Array<any> = [159, 98, 280];
      let redirectN: Array<any> = [259, 737, 917];
      let checkboxn = true;
      let leagueT = false;
      leagueT = 31 >= privilege1.length;
      leagueT = redirectN.length < 70;
      actionD += parseInt(`${actionD}`) | 2;
      let scrollviewA = 1.0;
      let ajax0 = 1.0;
      let logoH: Map<any, any> = new Map([[String.fromCharCode(119, 95, 54, 49, 95, 104, 97, 108, 102, 100, 0), false], [String.fromCharCode(109, 97, 108, 108, 111, 99, 122, 0), false]]);
      let libturbomodulejsijnin: Array<any> = [562, 428, 704];
      let filedG = String.fromCharCode(97, 110, 103, 114, 121, 95, 117, 95, 54, 56, 0);
      privilege1 += `${parseInt(`${actionD}`)}`;
      logoH.set(filedG, filedG.length);
      libturbomodulejsijnin = [(String.fromCharCode(101, 0) == filedG ? libturbomodulejsijnin.length : filedG.length)];
      for (let q = 0; q < 3; q++) {
         privilege1 += `${q_titleX.length}`;
      }
      fnewarchdefaultsX += 1 ^ libavcodecO.length;
      point4 = `${3 << (Math.min(1, Math.abs(playlistG)))}`;
      catagory_ += (String.fromCharCode(52, 0) == privacyS ? appsk.length : privacyS.length);


      let graphicsr = false;
      let zhengpianA = 4.0;
      let rewardx: Map<any, any> = new Map([[String.fromCharCode(121, 95, 50, 55, 95, 115, 116, 101, 112, 112, 101, 100, 0), String.fromCharCode(117, 95, 49, 48, 95, 112, 111, 111, 108, 114, 101, 102, 0)], [String.fromCharCode(97, 118, 114, 101, 115, 97, 109, 112, 108, 101, 114, 101, 115, 95, 115, 95, 50, 57, 0), String.fromCharCode(111, 112, 116, 105, 109, 105, 122, 101, 114, 0)], [String.fromCharCode(116, 111, 110, 101, 0), String.fromCharCode(102, 105, 120, 95, 114, 95, 49, 57, 0)]]);
      let weiboB = String.fromCharCode(117, 115, 101, 99, 0);
      graphicsr = !graphicsr || weiboB.length >= 39;
      zhengpianA *= parseFloat(`${2 % (Math.max(2, rewardx.size))}`);
      rewardx = new Map([[`${rewardx.size}`, parseInt(`${zhengpianA}`)]]);
      weiboB += `${parseInt(`${zhengpianA}`)}`;
      graphicsr = (graphicsr ? !graphicsr : graphicsr);
      graphicsr = (graphicsr ? graphicsr : !graphicsr);
      catagory_ &= libavutil8;
      circlet /= Math.max(playlistG - 1, 2);
      muted5 = !muted5 || 70 <= catagory_;
      for (let j = 0; j < 2; j++) {
         let appsA = 1;
         let gradleg = String.fromCharCode(115, 113, 108, 105, 116, 101, 99, 104, 97, 110, 103, 101, 115, 101, 116, 0);
         let privileger: Array<any> = [647, 4, 408];
         gradleg = `${appsA << (Math.min(gradleg.length, 4))}`;
         for (let w = 0; w < 3; w++) {
            gradleg = `${gradleg.length << (Math.min(Math.abs(2), 4))}`;
         }
         let sliderS = String.fromCharCode(112, 114, 111, 102, 105, 108, 105, 110, 103, 0);
         privileger.push(gradleg.length);
         sliderS += `${sliderS.length}`;
         while (privileger.length <= 2) {
            appsA ^= 3 << (Math.min(1, privileger.length));
            break;
         }
         let loginA = String.fromCharCode(113, 115, 118, 100, 101, 105, 110, 116, 0);
         let stringy = 1.0;
         appsA <<= Math.min(Math.abs(parseInt(`${stringy}`) + loginA.length), 4);
         gradleg = `${privileger.length}`;
         for (let q = 0; q < 1; q++) {
            let membershipp: Map<any, any> = new Map([[String.fromCharCode(116, 114, 97, 110, 115, 113, 117, 97, 110, 116, 0), false], [String.fromCharCode(114, 97, 119, 100, 101, 99, 0), true]]);
            let i_unlockD = String.fromCharCode(119, 105, 116, 104, 111, 117, 116, 0);
            let l_counte: Map<any, any> = new Map([[String.fromCharCode(102, 95, 53, 49, 95, 101, 109, 117, 108, 97, 116, 101, 100, 0), true], [String.fromCharCode(103, 101, 111, 116, 97, 103, 115, 0), true]]);
            let delegate_ovt = String.fromCharCode(115, 95, 49, 48, 95, 109, 101, 109, 100, 98, 0);
            gradleg = `${privileger.length << (Math.min(1, Math.abs(membershipp.size)))}`;
            membershipp.set(i_unlockD, delegate_ovt.length);
            i_unlockD += `${l_counte.size << (Math.min(i_unlockD.length, 4))}`;
            l_counte.set(delegate_ovt, 2 | delegate_ovt.length);
         }
         let unread5 = String.fromCharCode(103, 114, 111, 119, 105, 110, 103, 0);
         let mbnativex = String.fromCharCode(104, 95, 50, 52, 95, 117, 110, 99, 111, 110, 102, 105, 103, 117, 114, 101, 0);
         appsA |= unread5.length ^ 3;
         muted5 = 51 > catagory_;
      }


      muted5 = 8 == libavutil8 && 24.68 == fnewarchdefaultsX;
      while (4.37 < (fnewarchdefaultsX / 3.0) && 3.0 < (fnewarchdefaultsX / (Math.max(privacyS.length, 3)))) {
         privacyS = `${2 & parseInt(`${circlet}`)}`;
         break;
      }
      libavutil8 &= delegate_iR.size;
      while ((privacyS.length % 2) < 2) {
         let baiduG: Map<any, any> = new Map([[String.fromCharCode(105, 110, 116, 101, 114, 110, 97, 116, 105, 111, 110, 97, 108, 0), 183], [String.fromCharCode(112, 95, 55, 55, 95, 98, 101, 99, 97, 117, 115, 101, 0), 770]]);
         let splashU: Array<any> = [String.fromCharCode(102, 97, 99, 116, 111, 114, 121, 0), String.fromCharCode(116, 115, 99, 99, 100, 97, 116, 97, 95, 57, 95, 51, 53, 0), String.fromCharCode(97, 109, 114, 110, 98, 100, 97, 116, 97, 0)];
         let whatsappT = String.fromCharCode(100, 111, 117, 98, 108, 101, 105, 110, 116, 115, 116, 114, 95, 112, 95, 57, 49, 0);
         let sliderU = 0.0;
         sliderU -= parseFloat(`${1 - whatsappT.length}`);
         for (let o = 0; o < 1; o++) {
            whatsappT = `${whatsappT.length - parseInt(`${sliderU}`)}`;
         }
         let favicon6 = 1.0;
         let countryl = String.fromCharCode(112, 114, 111, 95, 122, 95, 56, 53, 0);
         let floatert = false;
         splashU = [baiduG.size & 2];
         favicon6 *= countryl.length & parseInt(`${favicon6}`);
         countryl += `${countryl.length * 1}`;
         floatert = (9 < ((!floatert ? 9 : countryl.length) >> (Math.min(countryl.length, 4))));
         for (let n = 0; n < 3; n++) {
            let lightF = String.fromCharCode(97, 118, 105, 115, 121, 110, 116, 104, 0);
            let streamingx = String.fromCharCode(114, 116, 114, 101, 101, 0);
            let ballm: Map<any, any> = new Map([[String.fromCharCode(112, 101, 114, 115, 105, 115, 116, 97, 98, 108, 101, 0), 378], [String.fromCharCode(102, 95, 52, 54, 95, 114, 101, 113, 117, 101, 115, 116, 105, 110, 103, 0), 419], [String.fromCharCode(119, 105, 110, 115, 111, 99, 107, 0), 71]]);
            let materialX: Array<any> = [String.fromCharCode(97, 100, 97, 112, 116, 105, 118, 101, 0), String.fromCharCode(99, 111, 100, 101, 95, 99, 95, 53, 54, 0)];
            whatsappT += `${materialX.length ^ streamingx.length}`;
            lightF = `${lightF.length | ballm.size}`;
            streamingx += `${ballm.size}`;
            materialX.push(ballm.size | lightF.length);
         }
         splashU = [2];
         for (let g = 0; g < 1; g++) {
            whatsappT += `${parseInt(`${sliderU}`)}`;
         }
         let colors0: Map<any, any> = new Map([[String.fromCharCode(99, 97, 114, 100, 115, 95, 51, 95, 53, 49, 0), 400], [String.fromCharCode(116, 105, 110, 121, 0), 522], [String.fromCharCode(103, 95, 52, 57, 95, 120, 117, 118, 109, 118, 115, 0), 472]]);
         let assist6 = String.fromCharCode(115, 104, 111, 119, 119, 97, 118, 101, 115, 112, 105, 99, 95, 119, 95, 56, 49, 0);
         let umeng6 = String.fromCharCode(100, 101, 97, 99, 116, 105, 118, 97, 116, 105, 111, 110, 0);
         sliderU *= parseFloat(`${baiduG.size}`);
         colors0 = new Map([[umeng6, umeng6.length >> (Math.min(assist6.length, 1))]]);
         assist6 = `${(assist6 == String.fromCharCode(56, 0) ? colors0.size : assist6.length)}`;
         let cornerE = 5.0;
         whatsappT = `${whatsappT.length}`;
         cornerE += 2 + parseInt(`${cornerE}`);
         let crossY = whatsappT.length >= 7102003;
         do {
            whatsappT += `${3 | splashU.length}`;
            if (crossY) {
               break;
            }
         } while (crossY && (whatsappT.length == splashU.length));
         while ((4 << (Math.min(1, Math.abs(baiduG.size)))) < 3 || (splashU.length << (Math.min(Math.abs(4), 2))) < 4) {
            splashU.push(parseInt(`${sliderU}`));
            break;
         }
         let relatedh = true;
         let policy_: Map<any, any> = new Map([[String.fromCharCode(115, 105, 103, 105, 110, 116, 95, 106, 95, 57, 50, 0), true], [String.fromCharCode(101, 95, 57, 55, 95, 97, 108, 114, 101, 97, 100, 121, 0), false], [String.fromCharCode(117, 105, 100, 115, 95, 120, 95, 56, 55, 0), false]]);
         let u_positionP: Array<any> = [842, 518, 128];
         baiduG.set(`${sliderU}`, 3 - parseInt(`${sliderU}`));
         policy_.set(`${u_positionP.length}`, policy_.size);
         u_positionP.push(policy_.size);
         privacyS += `${(String.fromCharCode(75, 0) == whatsappT ? predictionI.length : whatsappT.length)}`;
         break;
      }


      while (predictionI.length >= appsk.length) {
         predictionI = [3];
         break;
      }
      if (5 > (4 ^ appsk.length)) {
         let fasta: Array<any> = [0, 856, 585];
         let langA = String.fromCharCode(116, 114, 105, 109, 109, 101, 100, 0);
         let connectionG: Map<any, any> = new Map([[String.fromCharCode(111, 95, 55, 49, 95, 117, 112, 100, 97, 116, 101, 0), 910], [String.fromCharCode(105, 109, 105, 113, 0), 961]]);
         let refreshX = 2.0;
         langA = `${fasta.length / (Math.max(2, 9))}`;
         connectionG.set(`${refreshX}`, parseInt(`${refreshX}`) + connectionG.size);
         let commentH = fasta.length >= 9760200;
         do {
            fasta.push(2 << (Math.min(1, fasta.length)));
            if (commentH) {
               break;
            }
         } while (commentH && ((fasta.length + langA.length) <= 1 || (langA.length + 1) <= 3));
         langA = `${langA.length}`;
         let utilsO = 5;
         langA = `${(langA == String.fromCharCode(99, 0) ? utilsO : langA.length)}`;
         utilsO |= utilsO - langA.length;
         appsk += `${catagory_}`;
      }
      playlistG *= catagory_;
      let mathc = muted5 ? !muted5 : muted5;
      do {
         let catagoryu: Array<any> = [936, 568];
         let bingi: Array<any> = [562, 975];
         let activeV: Map<any, any> = new Map([[String.fromCharCode(109, 97, 105, 110, 100, 98, 95, 54, 95, 50, 51, 0), 100], [String.fromCharCode(112, 97, 115, 115, 102, 98, 95, 51, 95, 52, 49, 0), 338]]);
         let smallE = 1;
         catagoryu.push(3);
         let nativemoduleZ = 2.0;
         activeV.set(`${nativemoduleZ}`, parseInt(`${nativemoduleZ}`) >> (Math.min(catagoryu.length, 3)));
         activeV.set(`${bingi.length}`, catagoryu.length + 2);
         bingi = [catagoryu.length * 1];
         let minimizeb = String.fromCharCode(100, 97, 117, 100, 95, 52, 95, 55, 56, 0);
         let sell_ = String.fromCharCode(114, 101, 102, 114, 101, 115, 104, 95, 121, 95, 55, 53, 0);
         smallE *= minimizeb.length;
         minimizeb = "1";
         sell_ = "3";
         bingi = [2];
         for (let t = 0; t < 2; t++) {
            let canvasw: Map<any, any> = new Map([[String.fromCharCode(98, 95, 50, 55, 95, 98, 108, 117, 114, 114, 101, 100, 0), 521], [String.fromCharCode(102, 97, 114, 101, 110, 100, 95, 50, 95, 53, 53, 0), 366], [String.fromCharCode(100, 116, 109, 102, 0), 352]]);
            let bases = String.fromCharCode(108, 111, 103, 105, 115, 116, 105, 99, 0);
            catagoryu = [bingi.length];
            canvasw.set(`${bases}`, bases.length & canvasw.size);
         }
         while (activeV.size == bingi.length) {
            activeV.set(`${smallE}`, catagoryu.length);
            break;
         }
         muted5 = 90 < catagory_ || 90 < predictionI.length;
         if (mathc) {
            break;
         }
      } while ((muted5) && mathc);


      fnewarchdefaultsX /= Math.max(playlistG + appsk.length, 5);
      for (let t = 0; t < 3; t++) {
         let inactiveW: Map<any, any> = new Map([[String.fromCharCode(98, 95, 54, 55, 95, 110, 111, 104, 119, 0), 659], [String.fromCharCode(97, 115, 110, 116, 95, 55, 95, 57, 52, 0), 853]]);
         let commentB = String.fromCharCode(113, 95, 56, 51, 95, 115, 99, 114, 97, 112, 101, 0);
         while (4 <= (commentB.length << (Math.min(Math.abs(5), 5))) && 2 <= (5 << (Math.min(2, Math.abs(inactiveW.size))))) {
            commentB = `${2 + inactiveW.size}`;
            break;
         }
         let specv = String.fromCharCode(114, 101, 109, 105, 120, 105, 110, 103, 95, 100, 95, 53, 51, 0);
         let direct6 = String.fromCharCode(116, 111, 111, 108, 99, 104, 97, 105, 110, 95, 118, 95, 52, 55, 0);
         inactiveW.set(commentB, (String.fromCharCode(102, 0) == commentB ? inactiveW.size : commentB.length));
         for (let j = 0; j < 1; j++) {
            commentB = "2";
         }
         for (let i = 0; i < 1; i++) {
            direct6 += `${(direct6 == String.fromCharCode(97, 0) ? inactiveW.size : direct6.length)}`;
         }
         for (let r = 0; r < 3; r++) {
            let mimod = String.fromCharCode(102, 97, 115, 116, 109, 97, 116, 104, 0);
            inactiveW = new Map([[direct6, commentB.length >> (Math.min(5, direct6.length))]]);
            mimod += `${mimod.length << (Math.min(Math.abs(3), 5))}`;
         }
         fnewarchdefaultsX *= libavutil8;
      }
      while (point4.length == delegate_iR.size) {
         point4 = "2";
         break;
      }
      let mappingp = 7881050 >= predictionI.length;
      do {
         predictionI = [(privacyS == String.fromCharCode(113, 0) ? playlistG : privacyS.length)];
         if (mappingp) {
            break;
         }
      } while (mappingp && (3 > (point4.length + 1)));


      for (let f = 0; f < 1; f++) {
         delegate_iR = new Map([[point4, (String.fromCharCode(69, 0) == point4 ? parseInt(`${circlet}`) : point4.length)]]);
      }
      delegate_iR = new Map([[point4, appsk.length ^ 1]]);
      privacyS = `${parseInt(`${fnewarchdefaultsX}`) + 1}`;
      for (let x = 0; x < 1; x++) {
         libavutil8 -= playlistG;
      }


      let cancelu = 3.0;
      let untickV = String.fromCharCode(112, 111, 108, 105, 99, 121, 0);
      let fnewsH: Array<any> = [String.fromCharCode(99, 95, 52, 55, 95, 109, 111, 117, 115, 101, 0), String.fromCharCode(102, 95, 49, 95, 117, 109, 102, 97, 118, 114, 0), String.fromCharCode(98, 95, 49, 49, 0)];
      let chinasameM = untickV.length >= 5987919;
      do {
         let showf = String.fromCharCode(121, 95, 52, 95, 104, 97, 108, 100, 99, 108, 117, 116, 0);
         let cricket6 = String.fromCharCode(114, 101, 102, 112, 108, 97, 110, 101, 0);
         let subs3 = String.fromCharCode(110, 95, 51, 50, 95, 98, 101, 116, 116, 101, 114, 0);
         let sortJ = 2;
         untickV = `${(String.fromCharCode(109, 0) == subs3 ? subs3.length : showf.length)}`;
         showf += `${cricket6.length}`;
         cricket6 += `${2 * cricket6.length}`;
         sortJ &= cricket6.length % 1;
         if (chinasameM) {
            break;
         }
      } while (chinasameM && (3 >= (1 - untickV.length) && 1 >= (fnewsH.length - untickV.length)));
      untickV += `${3 << (Math.min(2, untickV.length))}`;
      let config2 = 5042207.0 <= cancelu;
      do {
         cancelu *= parseFloat(`${fnewsH.length % (Math.max(untickV.length, 10))}`);
         if (config2) {
            break;
         }
      } while ((untickV.endsWith(`${cancelu}`)) && config2);
      while (3 == (fnewsH.length - 5) || (fnewsH.length - untickV.length) == 5) {
         let homeT = String.fromCharCode(112, 95, 53, 56, 95, 104, 115, 98, 0);
         let libzeusA = 5.0;
         let userx: Array<any> = [155, 638, 712];
         let rightF: Array<any> = [940, 393, 649];
         untickV += `${(homeT == String.fromCharCode(121, 0) ? parseInt(`${libzeusA}`) : homeT.length)}`;
         libzeusA -= parseFloat(`${rightF.length % (Math.max(8, userx.length))}`);
         userx.push(rightF.length - 2);
         break;
      }
      while (2.15 < (parseFloat(`${untickV.length}`) + cancelu) && (5 / (Math.max(1, untickV.length))) < 4) {
         let plusu: Array<any> = [934, 217, 402];
         cancelu -= parseFloat(`${plusu.length}`);
         break;
      }
      muted5 = (predictionI.length * playlistG) == 79;
      circlet /= Math.max(3, appsk.length);
      let favoritex: Map<any, any> = new Map([[String.fromCharCode(102, 95, 49, 95, 98, 114, 111, 97, 100, 99, 97, 115, 116, 115, 0), 570], [String.fromCharCode(100, 101, 99, 114, 101, 97, 115, 101, 0), 65]]);
      let fastP = String.fromCharCode(115, 104, 97, 114, 101, 115, 0);
      let const_jkd: Map<any, any> = new Map([[String.fromCharCode(102, 97, 100, 101, 95, 118, 95, 55, 56, 0), String.fromCharCode(103, 95, 52, 50, 95, 99, 109, 100, 117, 116, 105, 108, 115, 0)], [String.fromCharCode(102, 95, 50, 51, 95, 101, 110, 115, 117, 114, 101, 0), String.fromCharCode(104, 95, 56, 55, 95, 101, 105, 103, 104, 116, 115, 118, 120, 0)]]);
      let gestures2 = 3;
      let lives = false;
      favoritex = new Map([[`${const_jkd.size}`, gestures2]]);
      const_jkd.set(`${lives}`, 2);
      gestures2 <<= Math.min(parseInt(`${Math.abs(((lives ? 1 : 1) >> (Math.min(4, Math.abs((lives ? 3 : 3))))))}`), 4);
      fastP = `${favoritex.size ^ 3}`;
      fastP = `${favoritex.size}`;
      fastP += `${favoritex.size & 2}`;
      let guideF = 1.0;
      fastP = `${fastP.length}`;
      guideF -= parseFloat(`${parseInt(`${guideF}`)}`);
      for (let a = 0; a < 1; a++) {
         favoritex = new Map([[`${favoritex.size}`, 3 >> (Math.min(2, fastP.length))]]);
      }
      delegate_iR = new Map([[appsk, appsk.length]]);
      let hoverS = delegate_iR.size <= 6592549;
      do {
         delegate_iR.set(point4, 2 & point4.length);
         if (hoverS) {
            break;
         }
      } while (hoverS && (appsk.length > 2));




      let awayS = appsk.length >= 7181988;
      do {
         appsk = `${catagory_}`;
         if (awayS) {
            break;
         }
      } while ((1 == (appsk.length / (Math.max(3, 10))) || 3 == (libavutil8 / (Math.max(appsk.length, 10)))) && awayS);
      if (!launchy.endsWith(`${refreshA}`)) {
         launchy = `${delegate_iR.size}`;
      }
      let zhengpianH: Map<any, any> = new Map([[String.fromCharCode(112, 95, 55, 0), String.fromCharCode(120, 95, 50, 53, 95, 97, 100, 100, 105, 110, 103, 0)], [String.fromCharCode(115, 101, 103, 117, 101, 0), String.fromCharCode(106, 95, 56, 50, 95, 115, 117, 98, 116, 114, 97, 99, 116, 105, 111, 110, 0)]]);
      let logoutn = 0.0;
      logoutn += 2 * parseInt(`${logoutn}`);
      zhengpianH.set(`${logoutn}`, parseInt(`${logoutn}`));
      libavutil8 <<= Math.min(2, Math.abs(3));
      let with_zcl = libavutil8 <= 5799234;
      do {
         libavutil8 >>= Math.min(Math.abs(1), 4);
         if (with_zcl) {
            break;
         }
      } while (with_zcl && ((5.48 * fnewarchdefaultsX) <= 1.83 || (fnewarchdefaultsX * libavutil8) <= 5.48));
      yys_event_common.sportDetailsVipPopupClicksAnalytics('pay');

   }, [])

   const handleOnInvite = useCallback(() => {
      let typesp = String.fromCharCode(108, 95, 54, 56, 95, 115, 117, 98, 109, 111, 100, 101, 108, 0);
      let mapbufferj = 5.0;
      let taiwan3 = 4.0;
      let register_iU = String.fromCharCode(119, 95, 53, 52, 95, 99, 97, 114, 114, 121, 111, 117, 116, 0);
      let proxyV = String.fromCharCode(115, 110, 105, 112, 112, 101, 116, 0);
      let areaO = 3.0;
      let texth = String.fromCharCode(99, 108, 111, 99, 107, 100, 114, 105, 102, 116, 95, 117, 95, 53, 48, 0);
      let soundD = String.fromCharCode(110, 95, 57, 57, 95, 116, 101, 114, 109, 115, 101, 116, 0);
      let libswscaleG = 0;
      let libsentryC = String.fromCharCode(97, 117, 103, 109, 101, 110, 116, 101, 100, 95, 112, 95, 53, 51, 0);
      let typingR = String.fromCharCode(122, 95, 49, 49, 95, 115, 105, 120, 116, 97, 112, 0);
      let taiwan4: Array<any> = [String.fromCharCode(100, 117, 112, 108, 105, 99, 97, 116, 101, 95, 101, 95, 56, 52, 0), String.fromCharCode(101, 109, 111, 106, 105, 115, 95, 101, 95, 51, 50, 0)];
      let slider9 = 1;
      if (3 >= register_iU.length) {
         register_iU = `${proxyV.length - register_iU.length}`;
      }
      for (let j = 0; j < 2; j++) {
         texth += `${libsentryC.length & 3}`;
      }
      proxyV += `${parseInt(`${mapbufferj}`) + 2}`;
      while ((2 * parseInt(`${areaO}`)) >= 4 || (areaO * 5.30) >= 1.48) {
         areaO -= proxyV.length;
         break;
      }
      mapbufferj += parseFloat(`${libswscaleG}`);
      for (let j = 0; j < 3; j++) {
         taiwan3 *= register_iU.length;
      }
      while (5 > register_iU.length && texth.length > 5) {
         register_iU += `${proxyV.length}`;
         break;
      }
      let langR = proxyV == String.fromCharCode(120, 57, 111, 108, 115, 99, 121, 115, 112, 0);
      do {
         proxyV = `${(String.fromCharCode(114, 0) == typingR ? parseInt(`${areaO}`) : typingR.length)}`;
         if (langR) {
            break;
         }
      } while (langR && (3 < (proxyV.length | 4) && 3 < (proxyV.length + 4)));

      onClose({ isAutoClose: true })
    

      if(yys_RelatedTooltips.isGuest(userState.user))
      {
        // onClose({ isAutoClose: true })
         dispatch(showLoginAction());
      }
      else
      {
         navigator.navigate('邀请');
      }
   
   

      texth = `${typesp.length % (Math.max(4, soundD.length))}`;
      while (2.67 == (libswscaleG * mapbufferj)) {
         let imagemanagerE = true;
         let hookF = false;
         let styles3 = false;
         hookF = (styles3 ? !hookF : !styles3);
         let typing1 = 2.0;
         let splashl = 2.0;
         hookF = !hookF;
         typing1 -= 3 & parseInt(`${splashl}`);
         splashl /= Math.max(3, 3);
         hookF = (!hookF ? styles3 : hookF);
         styles3 = (!hookF ? !styles3 : !hookF);
         let time_6su = 4;
         let storen = String.fromCharCode(106, 95, 57, 54, 95, 116, 114, 97, 110, 115, 99, 116, 105, 111, 110, 0);
         let chath = 0.0;
         hookF = !hookF;
         time_6su += time_6su % (Math.max(7, parseInt(`${chath}`)));
         storen = `${parseInt(`${chath}`) | 1}`;
         let cnewarchdefaults_ = styles3 ? !styles3 : styles3;
         do {
            styles3 = (!imagemanagerE ? styles3 : imagemanagerE);
            if (cnewarchdefaults_) {
               break;
            }
         } while (cnewarchdefaults_ && (!styles3 || imagemanagerE));
         if (!imagemanagerE) {
            let sportse: Array<any> = [512, 884, 138];
            let telegramW = true;
            imagemanagerE = (!telegramW ? styles3 : !telegramW);
            sportse = [sportse.length >> (Math.min(sportse.length, 1))];
         }
         if (hookF && styles3) {
            let matchesf = String.fromCharCode(116, 121, 111, 101, 0);
            let pingN = String.fromCharCode(103, 95, 51, 52, 95, 99, 105, 114, 99, 108, 101, 0);
            let redirectH = String.fromCharCode(112, 95, 57, 54, 95, 97, 99, 115, 107, 105, 112, 0);
            let combineQ = false;
            styles3 = pingN == String.fromCharCode(107, 0);
            matchesf = `${matchesf.length}`;
            pingN = `${matchesf.length}`;
            redirectH = "2";
            combineQ = (89 >= (redirectH.length & (combineQ ? 89 : redirectH.length)));
         }
         let scheduleA = 0;
         let typingu: Map<any, any> = new Map([[String.fromCharCode(116, 111, 112, 105, 99, 115, 0), 557], [String.fromCharCode(100, 106, 112, 101, 103, 95, 100, 95, 56, 57, 0), 275]]);
         let user4 = 3;
         hookF = !imagemanagerE;
         scheduleA %= Math.max(3, user4);
         typingu = new Map([[`${user4}`, scheduleA % (Math.max(user4, 10))]]);
         libswscaleG <<= Math.min(parseInt(`${Math.abs(((styles3 ? 3 : 1) % (Math.max(4, proxyV.length))))}`), 2);
         break;
      }
      proxyV = `${proxyV.length % 2}`;
      typingR += `${3 * typingR.length}`;
      let xvodZ: Array<any> = [853, 847, 888];
      let disconnectedE = 4.0;
      let nativemodule7 = String.fromCharCode(100, 111, 103, 115, 95, 118, 95, 52, 54, 0);
      xvodZ = [xvodZ.length];
      let styleA: Array<any> = [137, 678];
      let securityo: Array<any> = [String.fromCharCode(98, 108, 117, 114, 108, 101, 115, 115, 95, 100, 95, 51, 0), String.fromCharCode(99, 95, 56, 56, 95, 121, 117, 118, 110, 118, 99, 0)];
      let valuesw = 1;
      let twitter8 = String.fromCharCode(104, 95, 55, 56, 95, 103, 100, 97, 116, 97, 0);
      let libswresamplej: Map<any, any> = new Map([[String.fromCharCode(121, 95, 55, 56, 95, 112, 114, 111, 100, 117, 99, 101, 0), 974], [String.fromCharCode(99, 119, 110, 100, 95, 106, 95, 53, 49, 0), 414]]);
      securityo.push(2 + xvodZ.length);
      valuesw <<= Math.min(2, Math.abs(valuesw));
      twitter8 += "2";
      libswresamplej = new Map([[`${libswresamplej.size}`, 3 / (Math.max(7, libswresamplej.size))]]);
      let h_center5 = 2.0;
      nativemodule7 += `${parseInt(`${disconnectedE}`) - 3}`;
      h_center5 += 2 / (Math.max(parseInt(`${h_center5}`), 2));
      let untickP = 4.0;
      styleA = [parseInt(`${untickP}`)];
      disconnectedE *= securityo.length >> (Math.min(Math.abs(2), 2));
      while (1 <= (2 / (Math.max(9, securityo.length)))) {
         let calendarZ = 0.0;
         let reactR = 1.0;
         let streamingl = false;
         let moded: Array<any> = [String.fromCharCode(102, 101, 108, 101, 109, 95, 107, 95, 57, 57, 0), String.fromCharCode(97, 112, 105, 115, 0), String.fromCharCode(116, 114, 97, 110, 115, 102, 101, 114, 114, 105, 110, 103, 0)];
         securityo = [parseInt(`${disconnectedE}`)];
         calendarZ /= Math.max(parseFloat(`${1 + parseInt(`${reactR}`)}`), 2);
         reactR /= Math.max(1, parseFloat(`${parseInt(`${calendarZ}`) & parseInt(`${reactR}`)}`));
         streamingl = 40 >= moded.length && 53.19 >= calendarZ;
         moded = [1];
         break;
      }
      let proxyC = 0;
      disconnectedE /= Math.max(1, 1);
      typesp += `${typesp.length >> (Math.min(Math.abs(3), 4))}`;
      soundD = `${libswscaleG}`;
      while ((3 + proxyV.length) < 4) {
         proxyV += `${(texth == String.fromCharCode(56, 0) ? libswscaleG : texth.length)}`;
         break;
      }
      for (let b = 0; b < 1; b++) {
         areaO /= Math.max((typingR == String.fromCharCode(118, 0) ? libsentryC.length : typingR.length), 4);
      }


      for (let z = 0; z < 1; z++) {
         register_iU += `${proxyV.length}`;
      }
      mapbufferj *= (parseFloat(`${String.fromCharCode(109, 0) == register_iU ? register_iU.length : typingR.length}`));
      if (typesp.length >= 4) {
         proxyV = `${parseInt(`${taiwan3}`) + soundD.length}`;
      }
      let light2 = taiwan3 <= 9466962.0;
      do {
         let libjsim = 5;
         let loadingz = 3;
         let roomV = String.fromCharCode(101, 109, 109, 105, 110, 116, 114, 105, 110, 0);
         let halfq = 2.0;
         let expiredU = 3.0;
         loadingz >>= Math.min(Math.abs(3), 3);
         halfq += parseFloat(`${loadingz * 3}`);
         for (let x = 0; x < 1; x++) {
            loadingz %= Math.max(libjsim, 5);
         }
         let anythinkU = 2.0;
         let bodanx = 1.0;
         let viewsk = String.fromCharCode(101, 95, 57, 48, 95, 116, 101, 115, 116, 114, 97, 110, 115, 0);
         let detailp = String.fromCharCode(101, 95, 56, 50, 95, 109, 101, 116, 97, 98, 111, 100, 121, 0);
         if (3.13 > (2.32 + halfq) && 5.74 > (2.32 - anythinkU)) {
            halfq -= (parseFloat(`${viewsk == String.fromCharCode(79, 0) ? libjsim : viewsk.length}`));
         }
         detailp += `${loadingz}`;
         if (viewsk == detailp) {
            detailp += "2";
         }
         libjsim <<= Math.min(Math.abs(libjsim), 5);
         while (roomV.length < 3) {
            detailp += "1";
            break;
         }
         let buttonX = 3.0;
         detailp += `${detailp.length}`;
         buttonX -= parseFloat(`${parseInt(`${buttonX}`) | parseInt(`${buttonX}`)}`);
         if (viewsk.includes(`${halfq}`)) {
            halfq *= parseFloat(`${parseInt(`${anythinkU}`) / (Math.max(parseInt(`${expiredU}`), 9))}`);
         }
         if ((detailp.length + 1) >= 4 || (detailp.length * anythinkU) >= 4.94) {
            let brightnessU = 5.0;
            detailp = `${libjsim}`;
            brightnessU -= parseFloat(`${parseInt(`${brightnessU}`) % (Math.max(parseInt(`${brightnessU}`), 5))}`);
         }
         for (let t = 0; t < 2; t++) {
            let context1 = String.fromCharCode(100, 105, 109, 0);
            let ballc = false;
            let tickh = 3.0;
            roomV = `${2 | parseInt(`${anythinkU}`)}`;
            context1 += `${parseInt(`${tickh}`)}`;
            ballc = 4 <= context1.length;
            tickh /= Math.max((String.fromCharCode(120, 0) == context1 ? context1.length : (ballc ? 1 : 5)), 5);
         }
         let ycopy_f4v = String.fromCharCode(97, 108, 109, 111, 115, 116, 95, 115, 95, 57, 50, 0);
         let libreanimated9 = 1;
         detailp = "1";
         ycopy_f4v = `${libreanimated9}`;
         libreanimated9 >>= Math.min(5, Math.abs(3));
         taiwan3 += 3;
         if (light2) {
            break;
         }
      } while (light2 && (5.90 < (taiwan3 * mapbufferj) || 5.90 < (taiwan3 * mapbufferj)));
      taiwan4 = [parseInt(`${taiwan3}`) << (Math.min(Math.abs(parseInt(`${areaO}`)), 4))];
      for (let y = 0; y < 1; y++) {
         let detailC = 0.0;
         let libfollyp: Array<any> = [852, 884];
         while (4.89 == (detailC + 1.36) && 3 == (5 << (Math.min(4, libfollyp.length)))) {
            libfollyp = [1];
            break;
         }
         let floatingY = 3;
         if (1 == (libfollyp.length ^ floatingY) && 5 == (libfollyp.length ^ 1)) {
            libfollyp = [libfollyp.length];
         }
         let bnewsn: Map<any, any> = new Map([[String.fromCharCode(110, 95, 57, 49, 95, 108, 111, 119, 101, 114, 0), 82], [String.fromCharCode(114, 101, 110, 100, 105, 116, 105, 111, 110, 0), 643], [String.fromCharCode(115, 112, 101, 99, 105, 102, 105, 99, 0), 636]]);
         let private_5v: Array<any> = [862, 182];
         let searchbarn = String.fromCharCode(111, 95, 56, 52, 95, 99, 111, 110, 110, 105, 110, 112, 117, 116, 0);
         libfollyp.push(3);
         bnewsn.set(searchbarn, 2 + searchbarn.length);
         private_5v = [searchbarn.length - 1];
         let downloaderg = libfollyp.length <= 8656265;
         do {
            libfollyp = [libfollyp.length];
            if (downloaderg) {
               break;
            }
         } while (((detailC * parseFloat(`${libfollyp.length}`)) == 1.75 || (libfollyp.length * 4) == 1) && downloaderg);
         detailC *= parseFloat(`${2 & parseInt(`${detailC}`)}`);
         mapbufferj += parseFloat(`${1 << (Math.min(1, libsentryC.length))}`);
      }
      for (let n = 0; n < 3; n++) {
         typingR += `${taiwan4.length % (Math.max(libsentryC.length, 3))}`;
      }
      let select_: Array<any> = [18, 433, 662];
      let windg = String.fromCharCode(117, 116, 103, 111, 105, 110, 103, 95, 99, 95, 57, 52, 0);
      let chartT = 4.0;
      select_ = [(String.fromCharCode(78, 0) == windg ? windg.length : parseInt(`${chartT}`))];
      if (3 >= select_.length) {
         windg = `${select_.length + parseInt(`${chartT}`)}`;
      }
      let thailandT = windg == String.fromCharCode(107, 119, 110, 110, 120, 111, 56, 119, 102, 0);
      do {
         windg = `${select_.length}`;
         if (thailandT) {
            break;
         }
      } while (((5 + windg.length) == 2) && thailandT);
      for (let o = 0; o < 1; o++) {
         windg += `${select_.length}`;
      }
      let paused = String.fromCharCode(104, 95, 52, 49, 95, 116, 97, 108, 108, 0);
      let tooltipsZ = String.fromCharCode(109, 97, 120, 98, 105, 116, 114, 97, 116, 101, 95, 114, 95, 50, 0);
      let screenz = 5.0;
      chartT /= Math.max(4, 2);
      screenz += parseFloat(`${parseInt(`${screenz}`) - parseInt(`${screenz}`)}`);
      windg += `${parseInt(`${chartT}`) % (Math.max(paused.length, 7))}`;
      if ((1.7 - chartT) < 2.18) {
         chartT += windg.length;
      }
      let k_viewP = String.fromCharCode(111, 116, 104, 101, 114, 95, 52, 95, 50, 48, 0);
      let checkbox1 = String.fromCharCode(112, 95, 49, 52, 95, 98, 117, 109, 112, 0);
      let userX = 5.0;
      paused = `${windg.length / 1}`;
      k_viewP += `${k_viewP.length}`;
      checkbox1 = `${(k_viewP == String.fromCharCode(103, 0) ? k_viewP.length : checkbox1.length)}`;
      userX += (String.fromCharCode(53, 0) == k_viewP ? checkbox1.length : k_viewP.length);
      libsentryC = `${1 * parseInt(`${chartT}`)}`;
      yys_event_common.sportDetailsVipPopupClicksAnalytics('invite');


   }, [])


   return (

      <VipPrivilegeModal
         titleText={"VIP升级权益"}
         benefitsTextsArray={['高清体育赛事  零时差感受赛场激情', '海量福利视频 你想要的这里都有', '高清画质 无广告观影体验']}
         imageGradientColors={['#0E5ED800', '#0B6AE4']}
         contentGradientColors={['#0B6CE8', '#001A74']}
         coverBackground={sportBg}
         coverImage={sportModels}
         onPurchase={handleOnPurchase}
         onInvite={handleOnInvite}
         onClose={onClose}
         showBlur={showBlur}
         showCondition={showCondition}
         showDarkBackdrop={true}
      />

   )
}