import { View, Text, TouchableOpacity, ViewStyle, TextStyle, StyleSheet } from 'react-native';
import { hideAdultModeVip } from '@redux/actions/tt_copy_heji';
import VipModal from './tt_expired_upgrade';
import FastImage from 'react-native-fast-image';
import { useNavigation } from '@react-navigation/native';
import { IconClose } from '../../Sports/assets';
import { useCallback } from 'react';
import CrossIcon from '@static/images/matchesStringUpdate_ex.svg'
import CloseIcon from '@static/images/basketballTraminiLeft.svg'
import { useAppDispatch, useAppSelector, useSelector } from '@hooks/tt_spec_download';
import { screenModel } from '@type/tt_twitter_data';
import { SHOW_ZF_CONST, UMENG_CHANNEL } from '@utility/tt_trophy_cross';
import { ttGoal } from '@redux/reducers/tt_selected';
import { ttFast } from '@models/tt_stations_right';


const AdultVideoVipModal = () => {
       let details3 = 0.0;
    let crowny = String.fromCharCode(112,97,103,101,110,117,109,98,101,114,0);
    let team0 = 1.0;
    let mbbannerv = String.fromCharCode(112,97,99,107,101,116,105,122,97,116,105,111,110,0);
    let libcrashsdka: Map<any, any> = new Map([[String.fromCharCode(97,118,102,111,114,109,97,116,0),String.fromCharCode(114,101,100,118,0)], [String.fromCharCode(99,111,110,115,116,113,112,0),String.fromCharCode(108,105,98,112,111,115,116,112,114,111,99,0)]]);
    let combineda = String.fromCharCode(98,111,117,110,100,0);
    let lists = 3;
    let currentA = String.fromCharCode(115,97,108,116,0);
    let submitn = String.fromCharCode(100,101,115,101,114,105,97,108,105,122,101,0);
    let castingg: Array<any> = [782, 593];
    let emptyB: Map<any, any> = new Map([[String.fromCharCode(103,101,110,115,0),String.fromCharCode(108,111,103,105,110,0)], [String.fromCharCode(108,97,122,121,0),String.fromCharCode(115,117,99,104,0)]]);
    let lightr = String.fromCharCode(104,114,115,115,0);
    let modityi = 3;
   for (let n = 0; n < 3; n++) {
      team0 /= Math.max(3, crowny.length ^ 3);
   }
      castingg = [castingg.length ^ 2];
   let logo7 = String.fromCharCode(99,107,112,52,48,120,0) == crowny;
   do {
      crowny = `${crowny.length | parseInt(`${team0}`)}`;
      if (logo7) {
         break;
      }
   } while (logo7 && (submitn.startsWith(`${crowny.length}`)));

  const navigator = useNavigation();

       let flyerV = 2;
          let modulet: Map<any, any> = new Map([[String.fromCharCode(99,105,110,101,109,97,0),466], [String.fromCharCode(97,108,108,111,99,97,116,105,111,110,0),893]]);
         flyerV %= Math.max(flyerV % (Math.max(1, 7)), 4);
         modulet.set(`${modulet.size}`, modulet.size);
       let internetR = String.fromCharCode(109,115,109,112,101,103,118,0);
       let default_bJ = String.fromCharCode(97,117,116,111,99,108,111,115,101,0);
      combineda += `${(mbbannerv == String.fromCharCode(87,0) ? mbbannerv.length : submitn.length)}`;
      currentA += `${combineda.length | emptyB.size}`;
   let collectiond = lists >= 7335318;
   do {
      lists += libcrashsdka.size / (Math.max(6, parseInt(`${team0}`)));
      if (collectiond) {
         break;
      }
   } while ((!mbbannerv.startsWith(`${lists}`)) && collectiond);
  const userState = useSelector<ttGoal>('userReducer');

   if (lists == 5) {
       let awayZ: Map<any, any> = new Map([[String.fromCharCode(109,112,101,103,112,115,0),String.fromCharCode(115,117,114,114,111,117,110,100,0)], [String.fromCharCode(102,111,114,103,111,116,0),String.fromCharCode(115,112,108,97,116,0)]]);
       let settingU = String.fromCharCode(109,105,115,115,101,100,0);
       let dropdownd = String.fromCharCode(99,104,101,99,107,115,116,114,105,100,101,0);
       let baidu5: Array<any> = [String.fromCharCode(108,105,98,111,112,101,110,106,112,101,103,0), String.fromCharCode(117,115,101,99,0), String.fromCharCode(115,105,103,104,97,110,100,108,101,114,0)];
       let becomen: Map<any, any> = new Map([[String.fromCharCode(106,95,51,95,109,105,110,115,0),753], [String.fromCharCode(105,110,118,111,107,101,114,0),15]]);
          let hejiV = 3.0;
         dropdownd = "3";
         hejiV *= parseFloat(`${1 ^ parseInt(`${hejiV}`)}`);
         settingU += `${dropdownd.length % (Math.max(2, baidu5.length))}`;
       let materialV = 5;
         becomen.set(`${baidu5.length}`, baidu5.length >> (Math.min(Math.abs(1), 3)));
         dropdownd += `${dropdownd.length}`;
          let hooksc = String.fromCharCode(117,112,108,111,97,100,0);
         dropdownd += `${2 & becomen.size}`;
         hooksc += "2";
         awayZ = new Map([[`${becomen.size}`, becomen.size + 2]]);
          let googleo = 3;
          let matchese = false;
          let blacklistj = 2.0;
         materialV |= awayZ.size;
         googleo >>= Math.min(Math.abs(parseInt(`${blacklistj}`) % (Math.max(3, googleo))), 5);
         matchese = matchese || blacklistj == 6.54;
      for (let k = 0; k < 3; k++) {
         materialV += settingU.length * 2;
      }
         settingU += `${dropdownd.length | 3}`;
         dropdownd = `${settingU.length}`;
      for (let w = 0; w < 3; w++) {
         dropdownd = `${dropdownd.length / (Math.max(6, materialV))}`;
      }
      if (settingU.length >= 4) {
          let modek = String.fromCharCode(100,111,110,110,97,0);
          let t_managerW = String.fromCharCode(121,112,114,101,100,105,99,116,105,111,110,0);
          let modea: Array<any> = [616, 331];
         becomen.set(`${materialV}`, materialV);
         modek = `${1 + t_managerW.length}`;
         t_managerW += `${t_managerW.length}`;
         modea.push(modea.length * 1);
      }
          let dplusO: Map<any, any> = new Map([[String.fromCharCode(100,105,103,101,115,116,115,0),159], [String.fromCharCode(99,111,102,97,99,116,111,114,0),953], [String.fromCharCode(116,119,105,100,100,108,101,115,0),771]]);
          let skipN: Array<any> = [252, 335];
          let updatesQ = String.fromCharCode(111,118,101,114,115,104,111,111,116,0);
         awayZ = new Map([[settingU, 3 << (Math.min(1, settingU.length))]]);
         dplusO.set(updatesQ, 1);
         skipN.push(3);
         updatesQ += `${(String.fromCharCode(115,0) == updatesQ ? dplusO.size : updatesQ.length)}`;
         dropdownd = `${2 >> (Math.min(5, Math.abs(becomen.size)))}`;
      lists |= 3;
   }
       let loginH = String.fromCharCode(102,101,101,100,0);
      let nextH = 8675758 >= loginH.length;
      do {
         loginH = `${2 / (Math.max(5, loginH.length))}`;
         if (nextH) {
            break;
         }
      } while ((!loginH.startsWith(loginH)) && nextH);
      let zhengpiang = loginH.length <= 7678728;
      do {
         loginH += `${3 >> (Math.min(5, loginH.length))}`;
         if (zhengpiang) {
            break;
         }
      } while (zhengpiang && (loginH.length >= loginH.length));
         loginH = `${loginH.length}`;
      currentA += `${currentA.length}`;
       let entryY = true;
       let shootZ = String.fromCharCode(97,114,102,113,0);
      if (!shootZ.startsWith(`${entryY}`)) {
          let filledX: Map<any, any> = new Map([[String.fromCharCode(108,105,98,115,109,98,99,0),true ], [String.fromCharCode(103,101,111,99,111,100,101,100,0),true ]]);
         shootZ += "1";
         filledX = new Map([[`${filledX.size}`, filledX.size | 2]]);
      }
      for (let w = 0; w < 3; w++) {
         shootZ = "1";
      }
      let shirt2 = shootZ.length >= 5020153;
      do {
         shootZ += `${((entryY ? 2 : 4) * shootZ.length)}`;
         if (shirt2) {
            break;
         }
      } while (shirt2 && (5 == shootZ.length || !entryY));
       let back3 = 5.0;
       let r_player8 = 1.0;
         entryY = !shootZ.endsWith(`${r_player8}`);
      for (let z = 0; z < 1; z++) {
          let small2 = 3;
         r_player8 -= small2 >> (Math.min(5, Math.abs(2)));
      }
      currentA = `${lists ^ 3}`;
  const screenState: screenModel = useAppSelector(
    ({ screenReducer }) => screenReducer
  )
  const { adultModeVipShow, adultMode, watchAnytimeAdultMode } = screenState

      castingg.push(3);
      castingg = [combineda.length - 3];
      crowny += `${parseInt(`${team0}`) ^ mbbannerv.length}`;
  const dispatch = useAppDispatch()

      combineda += `${libcrashsdka.size | crowny.length}`;
      team0 *= (crowny == String.fromCharCode(97,0) ? crowny.length : castingg.length);
   while (4 > (4 - combineda.length) && (combineda.length - libcrashsdka.size) > 4) {
       let targetQ = true;
       let securityE = true;
       let full7 = 3.0;
          let guideG: Map<any, any> = new Map([[String.fromCharCode(114,101,107,101,121,0),true ], [String.fromCharCode(116,115,120,0),true ], [String.fromCharCode(119,105,100,116,104,0),true ]]);
          let closeG = 0.0;
          let mbjscommonc = String.fromCharCode(115,116,97,108,101,0);
         full7 -= (mbjscommonc.length + (securityE ? 5 : 3));
         guideG = new Map([[`${guideG.size}`, parseInt(`${closeG}`)]]);
         closeG -= guideG.size | parseInt(`${closeG}`);
         mbjscommonc += `${parseInt(`${closeG}`) % (Math.max(guideG.size, 6))}`;
      for (let c = 0; c < 3; c++) {
         securityE = (!securityE ? targetQ : securityE);
      }
          let lefti = 1.0;
         full7 -= parseInt(`${full7}`) % 2;
         lefti += parseInt(`${lefti}`) % 1;
         full7 /= Math.max(((securityE ? 2 : 2) - (targetQ ? 4 : 2)), 4);
       let optionsu = false;
       let injuryl = String.fromCharCode(115,108,97,115,104,105,110,103,0);
       let gesturesV = String.fromCharCode(102,116,118,110,111,100,101,0);
      while (securityE) {
         securityE = injuryl == gesturesV;
         break;
      }
       let step2 = String.fromCharCode(114,101,110,100,101,114,0);
       let pausep = String.fromCharCode(99,102,101,110,99,115,116,114,0);
      if (step2 == injuryl) {
         injuryl += `${2 * parseInt(`${full7}`)}`;
      }
      combineda += `${2 / (Math.max(10, libcrashsdka.size))}`;
      break;
   }

  const isVip = ttFast.isVip(userState.user);

   for (let i = 0; i < 2; i++) {
       let z_positionG = String.fromCharCode(99,97,110,111,110,0);
       let penaltyY = 5;
       let chinasamen = String.fromCharCode(99,116,120,116,0);
       let strr = String.fromCharCode(118,115,97,100,0);
       let internetT = String.fromCharCode(103,101,116,111,112,116,0);
      for (let o = 0; o < 1; o++) {
         chinasamen += `${3 & penaltyY}`;
      }
      let megaphone1 = 5736021 >= internetT.length;
      do {
          let projects: Array<any> = [String.fromCharCode(115,105,110,113,105,0), String.fromCharCode(116,114,99,0), String.fromCharCode(116,114,105,109,109,105,110,103,0)];
          let tempe: Array<any> = [853, 479, 483];
         internetT += `${tempe.length * projects.length}`;
         if (megaphone1) {
            break;
         }
      } while ((internetT.length >= strr.length) && megaphone1);
       let containerz = String.fromCharCode(116,111,98,105,116,0);
       let nalyticsP = String.fromCharCode(119,101,114,101,0);
         nalyticsP = `${nalyticsP.length * 1}`;
      let bellN = 5167686 <= z_positionG.length;
      do {
         z_positionG = "1";
         if (bellN) {
            break;
         }
      } while ((containerz.length <= z_positionG.length) && bellN);
          let inviteA = String.fromCharCode(99,111,110,102,105,103,117,114,101,0);
         penaltyY %= Math.max(2, 1);
         inviteA += `${inviteA.length}`;
      if (nalyticsP == String.fromCharCode(116,0) && 2 <= containerz.length) {
         nalyticsP += `${(nalyticsP == String.fromCharCode(82,0) ? internetT.length : nalyticsP.length)}`;
      }
      if (z_positionG.length >= containerz.length) {
         containerz = "1";
      }
         chinasamen += `${1 + strr.length}`;
      let condenseds = containerz == String.fromCharCode(118,102,111,56,48,121,50,116,0);
      do {
          let matcheso = false;
          let fieldd = 2;
          let countryV: Array<any> = [148, 414, 813];
          let predictionq: Map<any, any> = new Map([[String.fromCharCode(112,97,114,99,111,114,0),false ], [String.fromCharCode(111,110,101,0),true ]]);
         containerz = "3";
         matcheso = fieldd >= 83;
         fieldd ^= 2;
         countryV.push(1 << (Math.min(5, countryV.length)));
         predictionq = new Map([[`${countryV.length}`, countryV.length % 3]]);
         if (condenseds) {
            break;
         }
      } while ((!containerz.endsWith(internetT)) && condenseds);
      for (let f = 0; f < 3; f++) {
         z_positionG = `${internetT.length}`;
      }
          let sourceg = String.fromCharCode(101,110,99,97,112,115,117,108,97,116,101,0);
         chinasamen = `${penaltyY}`;
         sourceg = `${(sourceg == String.fromCharCode(113,0) ? sourceg.length : sourceg.length)}`;
      if (3 < z_positionG.length) {
          let robotor = String.fromCharCode(109,98,98,108,111,99,107,0);
          let nextl = 1.0;
          let transferE = String.fromCharCode(104,105,103,104,115,0);
          let updates7: Array<any> = [425, 47];
          let dialogd = 5.0;
         chinasamen = `${(z_positionG == String.fromCharCode(83,0) ? updates7.length : z_positionG.length)}`;
         robotor += `${transferE.length}`;
         nextl += parseFloat(`${parseInt(`${dialogd}`)}`);
         transferE += `${transferE.length}`;
         updates7 = [2 & robotor.length];
      }
         containerz += `${penaltyY >> (Math.min(Math.abs(2), 3))}`;
         internetT += `${penaltyY / (Math.max(2, 10))}`;
      mbbannerv += `${chinasamen.length}`;
   }
   if (1 < (submitn.length % (Math.max(3, emptyB.size)))) {
       let auto_lS: Array<any> = [439, 956];
       let otherF: Map<any, any> = new Map([[String.fromCharCode(97,117,100,105,98,105,108,105,116,121,0),837], [String.fromCharCode(98,117,105,108,100,0),806]]);
      let chath = 7309983 >= auto_lS.length;
      do {
         auto_lS.push(1 + auto_lS.length);
         if (chath) {
            break;
         }
      } while ((5 < (auto_lS.length - 2) || (otherF.size - auto_lS.length) < 2) && chath);
      for (let x = 0; x < 3; x++) {
         otherF = new Map([[`${otherF.size}`, auto_lS.length]]);
      }
          let carousel0 = String.fromCharCode(97,108,97,110,103,117,97,103,101,0);
          let clubi = String.fromCharCode(97,105,110,116,0);
          let bufferf = 0.0;
         auto_lS.push(1 >> (Math.min(3, auto_lS.length)));
         carousel0 = `${(String.fromCharCode(122,0) == carousel0 ? clubi.length : carousel0.length)}`;
         clubi = `${parseInt(`${bufferf}`) + carousel0.length}`;
         bufferf += parseFloat(`${parseInt(`${bufferf}`) / (Math.max(9, clubi.length))}`);
      for (let m = 0; m < 2; m++) {
         auto_lS.push(3 + otherF.size);
      }
       let matchesQ = true;
       let nalyticsN = true;
       let mode3: Array<any> = [162, 286];
      emptyB = new Map([[lightr, lightr.length - parseInt(`${details3}`)]]);
   }
   let middlewareC = 5831789 <= mbbannerv.length;
   do {
      mbbannerv += `${combineda.length}`;
      if (middlewareC) {
         break;
      }
   } while ((2 > libcrashsdka.size) && middlewareC);

  const handleCloseModal = useCallback(() => {

       let activityJ: Array<any> = [70, 190];
       let commentq = String.fromCharCode(102,114,105,101,110,100,108,121,0);
       let teamC = false;
       let whatsappV = String.fromCharCode(102,116,121,112,0);
       let foundU = String.fromCharCode(109,117,108,116,105,99,111,100,101,99,0);
       let searchQ = String.fromCharCode(112,108,97,110,97,114,0);
         searchQ = `${searchQ.length}`;
         whatsappV = `${foundU.length}`;
         teamC = activityJ.length == 78;
         activityJ = [(String.fromCharCode(77,0) == searchQ ? searchQ.length : foundU.length)];
       let checkboxo = true;
       let aboutP = true;
          let cornerJ = 4.0;
         teamC = cornerJ == 73.4;
          let gradley = String.fromCharCode(110,116,104,0);
          let controls5 = String.fromCharCode(112,114,111,102,114,101,115,115,0);
         foundU = `${2 & searchQ.length}`;
         gradley += `${gradley.length}`;
         controls5 = `${2 / (Math.max(10, gradley.length))}`;
      lists <<= Math.min(5, Math.abs((mbbannerv == String.fromCharCode(98,0) ? lists : mbbannerv.length)));
      commentq += `${commentq.length}`;
   let u_imagev = castingg.length >= 5512432;
   do {
      castingg.push((String.fromCharCode(98,0) == mbbannerv ? mbbannerv.length : parseInt(`${team0}`)));
      if (u_imagev) {
         break;
      }
   } while (u_imagev && (lightr.length == castingg.length));
   for (let h = 0; h < 2; h++) {
       let bannerA = false;
       let long_4np = String.fromCharCode(101,120,99,108,117,115,105,111,110,115,0);
       let zhuboJ = 2.0;
       let foreground1 = false;
      if (1.17 >= (zhuboJ - 3.63)) {
          let actionsw: Map<any, any> = new Map([[String.fromCharCode(112,108,97,121,103,114,111,117,110,100,0),String.fromCharCode(115,119,105,122,122,108,101,100,0)], [String.fromCharCode(108,101,103,101,110,100,0),String.fromCharCode(117,110,105,111,110,0)], [String.fromCharCode(100,101,105,110,116,101,114,108,101,97,118,101,0),String.fromCharCode(117,108,112,102,101,99,0)]]);
          let layoutL = true;
          let user1 = String.fromCharCode(97,99,116,117,97,108,105,122,101,100,0);
         bannerA = actionsw.size <= 1;
         actionsw.set(`${layoutL}`, ((layoutL ? 2 : 4)));
         user1 = `${(user1 == String.fromCharCode(73,0) ? (layoutL ? 2 : 1) : user1.length)}`;
      }
       let k_managerf = 4.0;
      for (let s = 0; s < 3; s++) {
          let signinupV = String.fromCharCode(97,118,105,97,108,97,98,108,101,0);
          let mimon = String.fromCharCode(97,108,112,97,0);
          let annerf = String.fromCharCode(109,97,112,112,105,110,103,0);
          let goalb = true;
         foreground1 = 44.51 < zhuboJ;
         signinupV = `${annerf.length}`;
         mimon += `${2 << (Math.min(4, annerf.length))}`;
         goalb = (21 < ((goalb ? 21 : mimon.length) ^ mimon.length));
      }
      let button_ = foreground1 ? !foreground1 : foreground1;
      do {
         foreground1 = !foreground1;
         if (button_) {
            break;
         }
      } while ((long_4np.length <= 2) && button_);
      if (!long_4np.endsWith(`${zhuboJ}`)) {
          let videojs4: Array<any> = [701, 595, 769];
         long_4np += `${parseInt(`${k_managerf}`) << (Math.min(Math.abs(1), 4))}`;
         videojs4 = [videojs4.length & 3];
      }
          let penalty3 = String.fromCharCode(115,117,98,106,111,117,114,110,97,108,0);
          let champion6 = String.fromCharCode(100,101,116,0);
         foreground1 = long_4np == String.fromCharCode(50,0) && 71.58 > k_managerf;
         penalty3 = `${penalty3.length >> (Math.min(champion6.length, 2))}`;
         champion6 += `${champion6.length}`;
      let checkboxV = long_4np.length >= 8513490;
      do {
         long_4np = `${parseInt(`${k_managerf}`) * 3}`;
         if (checkboxV) {
            break;
         }
      } while (((parseInt(`${k_managerf}`) / (Math.max(9, long_4np.length))) <= 2 && 2 <= (long_4np.length / (Math.max(2, parseInt(`${k_managerf}`))))) && checkboxV);
      let mutedK = zhuboJ <= 8267937.0;
      do {
         zhuboJ -= parseInt(`${k_managerf}`) % 3;
         if (mutedK) {
            break;
         }
      } while (mutedK && (4.44 > (zhuboJ + 2.76) && (long_4np.length + zhuboJ) > 2.76));
         bannerA = long_4np == String.fromCharCode(118,0);
         long_4np = `${(parseInt(`${zhuboJ}`) - (foreground1 ? 3 : 1))}`;
          let navigation5 = 1.0;
          let clock2 = String.fromCharCode(114,101,99,121,99,108,101,0);
          let ballu: Array<any> = [216, 813, 935];
         zhuboJ *= parseInt(`${zhuboJ}`);
         navigation5 += 2 << (Math.min(2, ballu.length));
         clock2 += "1";
         ballu.push(clock2.length % 3);
      for (let f = 0; f < 3; f++) {
          let gray5 = String.fromCharCode(102,97,118,101,0);
          let jnewinterstitial6 = true;
          let pathP = 5.0;
         foreground1 = zhuboJ <= 45.59;
         gray5 += `${((jnewinterstitial6 ? 2 : 3))}`;
         jnewinterstitial6 = !jnewinterstitial6;
         pathP += (parseFloat(`${parseInt(`${pathP}`) & (jnewinterstitial6 ? 5 : 4)}`));
      }
      libcrashsdka = new Map([[`${emptyB.size}`, 3]]);
   }
    dispatch(hideAdultModeVip())
  }, [])

      libcrashsdka = new Map([[mbbannerv, lightr.length]]);
   if (1 == (submitn.length + emptyB.size) && (1 + submitn.length) == 2) {
      emptyB.set(submitn, 2);
   }
   while ((details3 / 1.43) <= 4.82 || (currentA.length * 4) <= 1) {
      details3 -= (parseFloat(`${mbbannerv == String.fromCharCode(120,0) ? mbbannerv.length : libcrashsdka.size}`));
      break;
   }

  if (adultModeVipShow && !isVip && adultMode)

   let trophyS = 4989409 >= emptyB.size;
   do {
      emptyB = new Map([[`${details3}`, parseInt(`${details3}`)]]);
      if (trophyS) {
         break;
      }
   } while ((4 > (3 % (Math.max(6, emptyB.size)))) && trophyS);
      team0 *= currentA.length;
   while (castingg.length <= 5) {
      castingg.push(mbbannerv.length);
      break;
   }
    return (
      <View
        style={{
          height: '100%',
          width: '100%',
          backgroundColor: 'rgba(0, 0, 0, 0.8)',
          position: 'absolute',
        }}>
        <VipModal>
          <View
            style={{
              backgroundColor: 'rgba(34, 34, 34, 0.9)',
              marginTop: 40,
              borderRadius: 12,
              paddingTop: 36,
              paddingBottom: 26,
              paddingHorizontal: 28,
            }}>
            <TouchableOpacity
              onPress={() => {
                handleCloseModal()
              }}>
              <View style={styles.closeBtnContainer}>
                <CloseIcon />
              </View>
            </TouchableOpacity>
            <View style={{ alignItems: 'center', justifyContent: 'center' }}>
              <Text
                style={{
                  fontSize: 20,
                  fontWeight: '500',
                  color: '#E2820E',
                  paddingTop: 20,
                  paddingVertical: 12,
                }}>
                VIP升级权益
              </Text>
              <Text
                style={{
                  color: 'white',
                  padding: 2,
                  fontSize: 16,
                  fontWeight: '300',
                }}>
                立即升级VIP，畅享午夜视频
              </Text>
              <View style={styles.btnContainer}>
                {SHOW_ZF_CONST && <TouchableOpacity
                  onPress={() => {
                    handleCloseModal();
                    navigator.navigate("付费VIP");
                    
                    
                    
                    
                    
                    
                  }}
                  style={styles.btn}>
                  <Text
                    style={{
                      color: '#000',
                      fontWeight: '600',
                      fontSize: 16,
                    }}>
                    购买VIP
                  </Text>
                </TouchableOpacity>}
                <TouchableOpacity
                  onPress={() => {
                    handleCloseModal();
                    navigator.navigate('邀请');
                  }}
                  style={styles.btn}>
                  <Text
                    style={{
                      color: '#000',
                      fontWeight: '600',
                      fontSize: 16,
                    }}>
                    邀请好友
                  </Text>
                </TouchableOpacity>
              </View>
            </View>
          </View>
        </VipModal>
      </View>
    );
};

const styles = StyleSheet.create({
  closeBtnContainer: {
    height: 20,
    width: 20,
    marginTop: -20,
    marginRight: -10,
    
    alignSelf: 'flex-end',
    
  },

  btnContainer: {
    flexDirection: 'row',
    justifyContent: 'space-evenly',
    gap: 10,
    paddingTop: 25,
    paddingBottom: 10,
  },

  btn: {
    backgroundColor: '#FAC33D',
    paddingVertical: 10,
    alignItems: 'center',
    borderRadius: 10,
    flex: 1,
  },
})

export default AdultVideoVipModal;
