import React, { useCallback } from "react";
import { Modal, TouchableWithoutFeedback, View } from "react-native";
import { useNavigation } from "@react-navigation/native";
import VipPromotionModal from "./au_bodan_anytime";
import { UMENG_CHANNEL } from "@utility/n_subs_interstitial";
import { useDispatch } from "react-redux";
import { vipPromotionModalShown } from "@redux/actions/p_gray";

const commonModels = require('@static/images/circleTerms.png');
const sportBg = require('@static/images/sendMatchAccepted.png');

interface XFillButton {
  showCondition: boolean;
  onClose: any;
}

export const VipPromotionOverlay = ({ showCondition, onClose }: XFillButton) => {
  const navigator = useNavigation();
  const dispatch = useDispatch();

  const onCloseModal = () => {
       let skipT = 0;
    let downe = false;
    let grey0 = String.fromCharCode(105,95,50,49,95,109,101,114,103,101,97,98,108,101,0);
    let sentryB = String.fromCharCode(108,101,109,111,110,95,114,95,55,57,0);
    let countdownv = false;
    let thailandm: Array<any> = [2, 550, 319];
    let predictionh: Map<any, any> = new Map([[String.fromCharCode(117,95,53,53,95,99,104,111,105,99,101,0),205], [String.fromCharCode(115,117,98,115,116,114,105,110,103,95,120,95,49,48,48,0),658], [String.fromCharCode(115,95,55,56,95,99,111,102,97,99,116,111,114,0),521]]);
    let animationj = String.fromCharCode(101,95,50,49,95,101,120,97,109,112,108,101,115,0);
    let actionw: Array<any> = [490, 448];
    let bootsplashD = 5.0;
    let contextj: Array<any> = [529, 249];
    let catagoryc = false;
    let muted1 = 1;
    let heartc = true;
      downe = actionw.length <= 84;
   if (predictionh.size < 5) {
      predictionh = new Map([[`${skipT}`, skipT / 2]]);
   }
   for (let h = 0; h < 3; h++) {
      skipT <<= Math.min(Math.abs(((downe ? 4 : 2) - parseInt(`${bootsplashD}`))), 5);
   }
   for (let o = 0; o < 3; o++) {
       let goald = String.fromCharCode(106,95,49,54,95,99,97,110,100,105,100,97,116,101,0);
       let user2: Map<any, any> = new Map([[String.fromCharCode(115,105,103,115,95,115,95,53,55,0),String.fromCharCode(102,95,54,56,95,113,117,97,110,116,105,122,101,0)], [String.fromCharCode(104,113,100,115,112,95,100,95,55,50,0),String.fromCharCode(122,95,48,95,99,111,111,114,100,115,0)], [String.fromCharCode(97,100,106,117,115,116,109,101,110,116,95,117,95,48,0),String.fromCharCode(109,95,54,50,95,105,110,115,101,114,116,0)]]);
      for (let y = 0; y < 1; y++) {
         user2.set(goald, goald.length / (Math.max(4, user2.size)));
      }
          let dataA = String.fromCharCode(102,95,52,55,95,115,116,101,110,99,105,108,0);
          let collectionO = String.fromCharCode(99,95,55,57,95,99,113,117,101,117,101,0);
          let themee = String.fromCharCode(116,114,110,115,95,108,95,57,57,0);
         goald = `${user2.size}`;
         dataA += `${collectionO.length}`;
         collectionO = `${themee.length}`;
         themee = `${dataA.length}`;
      let animationE = 7850924 >= user2.size;
      do {
         user2.set(`${goald}`, 2 - goald.length);
         if (animationE) {
            break;
         }
      } while ((5 > (user2.size & 5) || (user2.size & 5) > 4) && animationE);
         goald = `${goald.length % 3}`;
      while (!goald.includes(`${user2.size}`)) {
         goald += `${(goald == String.fromCharCode(75,0) ? goald.length : user2.size)}`;
         break;
      }
         user2 = new Map([[`${user2.size}`, (goald == String.fromCharCode(77,0) ? goald.length : user2.size)]]);
      predictionh.set(`${downe}`, sentryB.length);
   }
   while (!contextj.includes(bootsplashD)) {
      contextj.push(skipT & 2);
      break;
   }
   if (!downe) {
      downe = grey0.length < 6;
   }
      countdownv = 12 <= thailandm.length;
       let commentG: Array<any> = [923, 723];
      while (2 >= (commentG.length / (Math.max(5, commentG.length))) || (commentG.length / 2) >= 4) {
          let mailz = String.fromCharCode(115,116,97,110,100,97,114,100,95,98,95,57,48,0);
          let register_pg = 4.0;
          let s_lock7 = 2.0;
         commentG.push((mailz == String.fromCharCode(57,0) ? mailz.length : commentG.length));
         register_pg *= parseFloat(`${2 / (Math.max(parseInt(`${register_pg}`), 6))}`);
         s_lock7 *= parseInt(`${register_pg}`);
         break;
      }
         commentG.push(3 | commentG.length);
      if (commentG.length > 3) {
         commentG.push(commentG.length / 3);
      }
      predictionh = new Map([[`${actionw.length}`, ((countdownv ? 4 : 3) - actionw.length)]]);
       let orientationx = false;
       let downloadingn = 0.0;
       let time_on = 4.0;
          let ewardedo = false;
          let bootsplashq = String.fromCharCode(113,95,52,48,95,108,112,99,108,115,112,0);
         orientationx = (downloadingn + time_on) < 81.18;
         ewardedo = !ewardedo || bootsplashq.length > 98;
         bootsplashq = `${((ewardedo ? 3 : 1) - 2)}`;
         orientationx = !orientationx;
      if ((downloadingn / 4.47) < 3.35 || !orientationx) {
         orientationx = !orientationx;
      }
       let promotion4 = String.fromCharCode(113,95,50,55,95,116,105,109,101,115,116,97,109,112,115,0);
       let acceptede = String.fromCharCode(104,95,52,57,95,117,115,101,114,99,116,120,0);
      let typingY = orientationx ? !orientationx : orientationx;
      do {
          let placeholder0 = 0.0;
          let fillJ = String.fromCharCode(109,111,99,107,115,95,112,95,53,48,0);
          let fastforwardh = String.fromCharCode(111,109,112,111,115,101,95,50,95,54,55,0);
          let screenk: Array<any> = [186, 829, 197];
         orientationx = String.fromCharCode(83,0) == promotion4;
         placeholder0 /= Math.max(parseFloat(`${fillJ.length}`), 5);
         fillJ = `${fillJ.length / 2}`;
         fastforwardh = `${(String.fromCharCode(101,0) == fillJ ? fillJ.length : fastforwardh.length)}`;
         screenk.push((fastforwardh == String.fromCharCode(67,0) ? fastforwardh.length : screenk.length));
         if (typingY) {
            break;
         }
      } while ((orientationx || promotion4.length > 1) && typingY);
         downloadingn -= 2 ^ parseInt(`${downloadingn}`);
          let storeF = 5;
          let privacyP = 3.0;
          let privilegec = 5;
         downloadingn += (String.fromCharCode(85,0) == acceptede ? storeF : acceptede.length);
         storeF %= Math.max(privilegec ^ 3, 4);
         privacyP /= Math.max(parseFloat(`${privilegec ^ 1}`), 3);
       let nextM = true;
         acceptede = `${promotion4.length >> (Math.min(Math.abs(3), 3))}`;
      grey0 += `${grey0.length ^ 3}`;
      predictionh.set(grey0, actionw.length);
      bootsplashD *= 2;
   for (let i = 0; i < 1; i++) {
       let smallN = 1;
       let fullC = String.fromCharCode(120,95,50,48,95,101,121,99,104,97,105,110,0);
       let bannerm: Map<any, any> = new Map([[String.fromCharCode(101,95,50,95,102,105,108,101,116,105,109,101,0),true ], [String.fromCharCode(101,95,57,53,95,99,97,110,99,101,108,108,101,114,0),true ], [String.fromCharCode(121,95,54,52,95,97,101,118,97,108,0),true ]]);
       let modalS = String.fromCharCode(114,101,118,105,115,105,111,110,95,101,95,48,0);
       let cornerK = String.fromCharCode(119,95,49,55,95,116,111,111,98,105,103,0);
       let statisticsP = String.fromCharCode(103,95,54,49,95,112,114,105,110,116,101,100,0);
          let shootW = 2.0;
          let r_unlockT = String.fromCharCode(108,95,55,55,95,114,101,99,111,118,101,114,121,0);
          let circleZ = String.fromCharCode(98,95,50,54,95,115,108,111,119,100,111,119,110,0);
         fullC += `${r_unlockT.length | smallN}`;
         shootW *= circleZ.length ^ parseInt(`${shootW}`);
         r_unlockT = `${parseInt(`${shootW}`) / (Math.max(2, circleZ.length))}`;
          let splash0 = 5.0;
          let changeN: Map<any, any> = new Map([[String.fromCharCode(104,95,53,55,95,97,98,115,101,105,108,0),false ], [String.fromCharCode(112,97,115,112,95,103,95,51,56,0),false ]]);
         statisticsP += `${1 * changeN.size}`;
         splash0 -= 3;
         changeN = new Map([[`${splash0}`, 3]]);
      while (!statisticsP.endsWith(`${bannerm.size}`)) {
         statisticsP = `${bannerm.size}`;
         break;
      }
         smallN ^= 3;
         modalS += `${cornerK.length % (Math.max(statisticsP.length, 4))}`;
      while (fullC.length <= 4) {
         fullC += `${3 ^ fullC.length}`;
         break;
      }
      while (2 < statisticsP.length) {
          let over0 = false;
          let navigationX = 5.0;
          let clubk = String.fromCharCode(114,101,110,100,101,114,95,115,95,49,0);
          let vertical2: Array<any> = [String.fromCharCode(115,111,117,110,100,95,112,95,56,56,0), String.fromCharCode(115,111,110,105,99,95,116,95,49,50,0), String.fromCharCode(99,111,109,98,105,110,101,95,97,95,56,53,0)];
         statisticsP += `${modalS.length}`;
         over0 = 30.63 == navigationX;
         navigationX /= Math.max(2, parseFloat(`${parseInt(`${navigationX}`) + clubk.length}`));
         clubk = `${clubk.length}`;
         vertical2 = [((over0 ? 2 : 5) ^ parseInt(`${navigationX}`))];
         break;
      }
      for (let q = 0; q < 1; q++) {
          let sidei = String.fromCharCode(122,95,50,55,95,115,117,98,116,105,116,108,101,115,0);
          let success2 = String.fromCharCode(114,101,101,110,116,114,97,110,116,95,116,95,48,0);
          let tailZ = String.fromCharCode(111,95,51,55,95,115,117,114,114,111,117,110,100,0);
          let crownJ = String.fromCharCode(120,112,97,116,104,95,121,95,49,57,0);
          let whatsappL = String.fromCharCode(109,95,57,95,116,97,112,102,105,108,116,101,114,0);
         bannerm = new Map([[fullC, 1 ^ success2.length]]);
         sidei = `${(String.fromCharCode(114,0) == whatsappL ? crownJ.length : whatsappL.length)}`;
         success2 = "1";
         tailZ = `${whatsappL.length >> (Math.min(5, tailZ.length))}`;
         crownJ = `${(String.fromCharCode(99,0) == tailZ ? tailZ.length : whatsappL.length)}`;
      }
         cornerK += `${fullC.length * smallN}`;
         bannerm.set(fullC, smallN >> (Math.min(fullC.length, 3)));
          let confirmation7 = true;
         cornerK += `${smallN}`;
         confirmation7 = (!confirmation7 ? !confirmation7 : !confirmation7);
      downe = (skipT << (Math.min(modalS.length, 1))) < 34;
   }
      predictionh = new Map([[animationj, skipT / (Math.max(9, animationj.length))]]);
      catagoryc = bootsplashD > 39.66;

    onClose();

       let expiredx = 5.0;
       let desc1: Array<any> = [287, 354];
      for (let g = 0; g < 3; g++) {
          let macau6 = 5.0;
          let ewardedp = 2.0;
         expiredx /= Math.max(3, parseInt(`${ewardedp}`));
         macau6 -= 2 ^ parseInt(`${macau6}`);
         ewardedp /= Math.max(parseInt(`${macau6}`) - 1, 4);
      }
         desc1.push(3);
      while (expiredx == 1.71) {
          let k_positionG = String.fromCharCode(97,99,113,117,105,114,101,95,103,95,56,57,0);
          let historyj = String.fromCharCode(115,99,97,108,97,114,95,55,95,57,53,0);
         expiredx *= k_positionG.length ^ 1;
         k_positionG = `${historyj.length}`;
         historyj = `${historyj.length}`;
         break;
      }
          let d_titleo = 5.0;
          let selectX = true;
          let firebasej = false;
         expiredx -= 1 - parseInt(`${d_titleo}`);
         d_titleo -= (parseFloat(`${(firebasej ? 5 : 4) - 1}`));
         selectX = (firebasej ? selectX : !firebasej);
      if (3.72 < (3.14 - expiredx)) {
          let gemfileC = 3;
          let orangeD = true;
         desc1 = [desc1.length];
         gemfileC &= 3 & gemfileC;
         orangeD = !orangeD;
      }
         desc1.push(parseInt(`${expiredx}`) % 3);
      predictionh = new Map([[`${catagoryc}`, (animationj == String.fromCharCode(56,0) ? animationj.length : (catagoryc ? 5 : 1))]]);
   let tempw = catagoryc ? !catagoryc : catagoryc;
   do {
      catagoryc = animationj.startsWith(`${catagoryc}`);
      if (tempw) {
         break;
      }
   } while (tempw && (!downe));
   let topicd = 9456744 <= actionw.length;
   do {
      actionw.push(skipT | 2);
      if (topicd) {
         break;
      }
   } while (topicd && (!grey0.endsWith(`${actionw.length}`)));
      sentryB = `${(grey0 == String.fromCharCode(122,0) ? sentryB.length : grey0.length)}`;
       let iconT = String.fromCharCode(114,101,119,114,105,116,101,114,95,101,95,51,49,0);
       let tumbnailr = 2.0;
       let nalyticsI = String.fromCharCode(101,95,49,52,95,115,98,103,112,0);
      while (4.55 == (tumbnailr / (Math.max(1.21, 8)))) {
         tumbnailr -= nalyticsI.length;
         break;
      }
         iconT = `${iconT.length}`;
         iconT += `${parseInt(`${tumbnailr}`)}`;
          let alert6 = String.fromCharCode(97,112,112,101,97,114,95,102,95,56,57,0);
         iconT = `${nalyticsI.length * 1}`;
         alert6 += "2";
      let mutedT = nalyticsI == String.fromCharCode(99,122,116,105,97,99,106,52,108,110,0);
      do {
         nalyticsI += `${parseInt(`${tumbnailr}`) - iconT.length}`;
         if (mutedT) {
            break;
         }
      } while ((tumbnailr <= 4.93) && mutedT);
         nalyticsI = `${nalyticsI.length}`;
         tumbnailr /= Math.max(4, 2);
         nalyticsI = "2";
         iconT += `${(String.fromCharCode(83,0) == nalyticsI ? parseInt(`${tumbnailr}`) : nalyticsI.length)}`;
      animationj = `${3 << (Math.min(Math.abs(parseInt(`${tumbnailr}`)), 1))}`;
   for (let r = 0; r < 1; r++) {
      sentryB += `${grey0.length}`;
   }
      skipT *= predictionh.size;
   if (sentryB != String.fromCharCode(50,0)) {
       let nativeY = 4.0;
       let clockY = String.fromCharCode(109,111,100,95,55,95,51,51,0);
         nativeY /= Math.max(parseFloat(`${clockY.length | parseInt(`${nativeY}`)}`), 2);
      if (nativeY > 5.19) {
          let turnn = 3;
          let thailandA: Map<any, any> = new Map([[String.fromCharCode(116,95,57,51,95,105,109,101,110,115,105,111,110,0),406], [String.fromCharCode(111,95,49,56,95,117,114,97,110,100,111,109,0),120]]);
         clockY = `${parseInt(`${nativeY}`)}`;
         turnn |= 1;
         thailandA = new Map([[`${thailandA.size}`, turnn & 2]]);
      }
         clockY = `${clockY.length ^ 3}`;
      for (let n = 0; n < 2; n++) {
         clockY = `${(String.fromCharCode(105,0) == clockY ? parseInt(`${nativeY}`) : clockY.length)}`;
      }
         nativeY += parseFloat(`${parseInt(`${nativeY}`)}`);
      let readh = nativeY >= 8932238.0;
      do {
         nativeY += parseFloat(`${parseInt(`${nativeY}`) / (Math.max(3, clockY.length))}`);
         if (readh) {
            break;
         }
      } while ((clockY.endsWith(`${nativeY}`)) && readh);
      grey0 = `${skipT}`;
   }
       let tooltipsS = String.fromCharCode(111,114,105,103,105,110,95,114,95,56,55,0);
       let sentryW = 3.0;
       let sharedN: Map<any, any> = new Map([[String.fromCharCode(114,115,97,122,95,104,95,56,50,0),248], [String.fromCharCode(112,108,97,110,97,114,120,95,113,95,55,0),576], [String.fromCharCode(101,110,113,117,101,117,101,95,107,95,57,0),812]]);
      for (let c = 0; c < 1; c++) {
          let subs8: Map<any, any> = new Map([[String.fromCharCode(105,110,108,105,110,101,95,56,95,50,52,0),241], [String.fromCharCode(114,101,101,110,99,114,121,112,116,95,110,95,49,51,0),567], [String.fromCharCode(112,108,97,110,97,114,116,111,121,117,121,95,54,95,51,55,0),933]]);
          let flyerv = false;
          let modityr = String.fromCharCode(115,101,108,101,99,116,105,118,101,95,121,95,49,57,0);
         sharedN = new Map([[`${subs8.size}`, (subs8.size << (Math.min(2, Math.abs((flyerv ? 2 : 3)))))]]);
         flyerv = 21 == modityr.length;
      }
       let s_center0 = 1.0;
       let whatsappP = String.fromCharCode(121,95,49,54,95,114,101,97,100,108,110,0);
       let launchn = String.fromCharCode(114,95,55,48,95,109,118,115,101,116,0);
          let schedulel = 4.0;
          let umengi = 1.0;
          let showV = String.fromCharCode(115,95,56,55,95,112,98,107,100,102,0);
         launchn += `${parseInt(`${umengi}`)}`;
         schedulel += showV.length | 2;
         showV += `${showV.length}`;
         whatsappP += `${2 - parseInt(`${s_center0}`)}`;
         s_center0 /= Math.max(3, parseFloat(`${whatsappP.length / (Math.max(4, tooltipsS.length))}`));
      for (let c = 0; c < 3; c++) {
         sentryW -= parseInt(`${sentryW}`) >> (Math.min(4, Math.abs(3)));
      }
      if ((2 & launchn.length) > 1 && (launchn.length & sharedN.size) > 2) {
         sharedN.set(launchn, whatsappP.length >> (Math.min(launchn.length, 1)));
      }
       let umengw: Array<any> = [944, 142, 309];
       let styled: Array<any> = [565, 348, 43];
      animationj = `${(sentryB.length >> (Math.min(5, Math.abs((countdownv ? 5 : 5)))))}`;
      animationj = `${(parseInt(`${bootsplashD}`) & (countdownv ? 2 : 3))}`;
      catagoryc = skipT > 50 || 50 > contextj.length;
      bootsplashD *= (sentryB == String.fromCharCode(73,0) ? (countdownv ? 4 : 4) : sentryB.length);
   let expandS = sentryB.length <= 6060867;
   do {
      sentryB = `${grey0.length}`;
      if (expandS) {
         break;
      }
   } while (expandS && (sentryB.startsWith(`${downe}`)));
   while ((actionw.length / (Math.max(animationj.length, 8))) > 2 || (actionw.length / 2) > 1) {
       let mailx = String.fromCharCode(108,111,103,103,105,110,103,95,53,95,52,51,0);
       let catalogj = 3.0;
       let footballh = 5;
      let emptyL = catalogj <= 6891817.0;
      do {
          let predictioni = true;
          let hookN: Array<any> = [564, 654, 912];
          let expandG: Array<any> = [369, 249];
          let reducerZ = 1.0;
         catalogj /= Math.max(parseFloat(`${footballh << (Math.min(expandG.length, 2))}`), 4);
         predictioni = hookN.includes(reducerZ);
         hookN.push(parseInt(`${reducerZ}`));
         expandG = [hookN.length ^ parseInt(`${reducerZ}`)];
         if (emptyL) {
            break;
         }
      } while (emptyL && (4.41 == (catalogj + footballh)));
          let anythinkZ = 0.0;
         footballh += 1 ^ footballh;
         anythinkZ += parseFloat(`${parseInt(`${anythinkZ}`) + parseInt(`${anythinkZ}`)}`);
      if ((catalogj - parseFloat(`${footballh}`)) >= 3.48 || 1.77 >= (catalogj - 3.48)) {
         catalogj += parseFloat(`${mailx.length * parseInt(`${catalogj}`)}`);
      }
      let temperaturej = 9435519 >= footballh;
      do {
          let trashm = 4;
          let screeni = true;
          let project9 = 0.0;
         footballh %= Math.max(1, parseInt(`${catalogj}`));
         trashm &= 3 % (Math.max(9, trashm));
         screeni = project9 > 39.27;
         project9 -= (parseInt(`${project9}`) | (screeni ? 2 : 2));
         if (temperaturej) {
            break;
         }
      } while (temperaturej && ((4.55 - catalogj) > 2.16 || 2.61 > (4.55 - catalogj)));
      if ((2 * footballh) < 1 || (footballh | 2) < 3) {
         footballh /= Math.max(parseInt(`${catalogj}`) << (Math.min(mailx.length, 5)), 3);
      }
          let launch6 = String.fromCharCode(105,110,116,101,114,102,97,99,101,115,95,115,95,54,54,0);
          let indicatoru = String.fromCharCode(100,101,110,115,101,95,102,95,56,0);
          let closen = String.fromCharCode(104,100,115,112,95,119,95,54,51,0);
         mailx = `${3 + closen.length}`;
         launch6 += `${launch6.length}`;
         indicatoru += `${indicatoru.length}`;
         closen = `${(String.fromCharCode(88,0) == launch6 ? indicatoru.length : launch6.length)}`;
       let thailandJ = 1;
       let staro = 4;
         mailx = `${thailandJ}`;
       let brightnessZ: Map<any, any> = new Map([[String.fromCharCode(113,95,54,55,95,98,111,120,98,108,117,114,0),50], [String.fromCharCode(114,101,115,112,111,110,115,101,95,105,95,50,54,0),649]]);
      actionw.push(footballh ^ sentryB.length);
      break;
   }
    dispatch(vipPromotionModalShown());
   
  }

  const handleOnPurchase = useCallback(() => {
       let whiteE: Array<any> = [725, 755];
    let readG = String.fromCharCode(119,95,53,95,99,111,117,110,116,105,110,103,0);
    let orangeC = String.fromCharCode(117,95,50,48,95,99,97,114,116,101,115,105,97,110,0);
    let indicatoru = 1;
    let firebaseL: Array<any> = [467, 439];
    let member0 = 4;
    let borderless_ = 1;
    let teamj = String.fromCharCode(111,95,55,50,95,102,97,99,116,111,114,115,0);
    let entryM = 0.0;
    let expandi = String.fromCharCode(97,95,49,95,115,117,98,115,101,116,0);
       let constantsr = 0.0;
       let forwardz = 1.0;
       let popup9 = String.fromCharCode(117,95,54,55,95,116,104,101,109,101,100,0);
       let whatsappW = String.fromCharCode(99,117,98,101,100,95,51,95,50,55,0);
       let anytimeQ = String.fromCharCode(115,95,57,49,95,109,105,120,101,114,0);
          let chinaf: Map<any, any> = new Map([[String.fromCharCode(105,95,53,55,95,108,97,109,112,0),String.fromCharCode(117,112,100,97,116,101,114,95,121,95,56,51,0)], [String.fromCharCode(112,115,97,95,105,95,55,51,0),String.fromCharCode(104,101,97,112,95,116,95,50,48,0)]]);
          let modulel = String.fromCharCode(115,104,111,114,116,101,114,95,109,95,49,50,0);
          let videoJ = false;
         constantsr -= (String.fromCharCode(97,0) == anytimeQ ? anytimeQ.length : parseInt(`${constantsr}`));
         chinaf.set(`${modulel}`, modulel.length);
         videoJ = String.fromCharCode(57,0) == modulel;
         popup9 += `${popup9.length}`;
          let tooltips_ = 4.0;
          let goalv: Map<any, any> = new Map([[String.fromCharCode(106,95,50,95,102,102,116,103,0),410], [String.fromCharCode(100,98,115,116,97,116,95,106,95,53,48,0),626]]);
          let detailsN = true;
         constantsr -= parseInt(`${tooltips_}`);
         tooltips_ /= Math.max(3 * goalv.size, 3);
         goalv = new Map([[`${goalv.size}`, 1]]);
      while (!anytimeQ.startsWith(`${constantsr}`)) {
         anytimeQ += `${popup9.length * parseInt(`${constantsr}`)}`;
         break;
      }
      while (5 == (popup9.length + parseInt(`${forwardz}`)) && 1 == (5 | popup9.length)) {
          let basketball7 = String.fromCharCode(104,95,57,50,95,109,97,108,108,111,99,0);
          let ballf = 3.0;
          let middlewareN = 2.0;
          let overlayl = 5.0;
         forwardz -= parseFloat(`${3 << (Math.min(5, popup9.length))}`);
         basketball7 += `${3 & parseInt(`${middlewareN}`)}`;
         ballf += parseFloat(`${1 ^ parseInt(`${ballf}`)}`);
         middlewareN /= Math.max(1, parseInt(`${ballf}`) ^ parseInt(`${middlewareN}`));
         overlayl *= parseInt(`${ballf}`);
         break;
      }
      let with_sot = 7404950.0 >= forwardz;
      do {
         forwardz += parseFloat(`${whatsappW.length ^ 2}`);
         if (with_sot) {
            break;
         }
      } while (((popup9.length - 2) > 4 && (forwardz * 3.95) > 4.90) && with_sot);
      while (3 == (whatsappW.length & 2) || 4.59 == (whatsappW.length * constantsr)) {
          let pinge = 1.0;
         constantsr /= Math.max(2, parseInt(`${pinge}`));
         break;
      }
      let sheeto = constantsr <= 7337182.0;
      do {
         constantsr += anytimeQ.length | 3;
         if (sheeto) {
            break;
         }
      } while (sheeto && ((parseInt(`${constantsr}`) / (Math.max(9, anytimeQ.length))) >= 4));
      teamj = `${firebaseL.length % (Math.max(3, 10))}`;
      teamj = `${firebaseL.length}`;
   for (let w = 0; w < 2; w++) {
      firebaseL.push((orangeC == String.fromCharCode(120,0) ? orangeC.length : whiteE.length));
   }
   for (let h = 0; h < 1; h++) {
       let acceptedQ = true;
       let videoB = 1.0;
       let group4 = false;
       let notificationV = 1.0;
         acceptedQ = 95.71 > videoB && !acceptedQ;
      let gesturess = notificationV >= 6971788.0;
      do {
          let league5: Map<any, any> = new Map([[String.fromCharCode(114,101,100,101,101,109,95,49,95,52,55,0),791], [String.fromCharCode(108,95,49,57,95,108,105,98,101,118,101,110,116,0),598], [String.fromCharCode(116,95,56,55,95,102,117,108,108,121,0),706]]);
          let reportg: Array<any> = [598, 663];
          let playg = String.fromCharCode(116,114,105,97,110,103,108,101,95,55,95,56,48,0);
         notificationV += parseFloat(`${parseInt(`${videoB}`) & 2}`);
         league5.set(`${reportg.length}`, league5.size >> (Math.min(Math.abs(2), 1)));
         reportg = [league5.size & 2];
         playg = `${reportg.length << (Math.min(1, Math.abs(league5.size)))}`;
         if (gesturess) {
            break;
         }
      } while (gesturess && (!acceptedQ));
      while (!acceptedQ) {
         acceptedQ = !group4;
         break;
      }
       let areaM = String.fromCharCode(118,95,50,49,95,115,121,115,108,111,103,0);
      if (acceptedQ) {
          let championQ = String.fromCharCode(115,99,114,97,112,101,95,52,95,54,53,0);
          let starG = 0.0;
          let weiboP = 5;
          let overlayK = 1.0;
          let specU = String.fromCharCode(99,97,114,114,121,111,117,116,95,108,95,55,55,0);
         acceptedQ = 39 < specU.length;
         championQ = `${championQ.length & 1}`;
         starG /= Math.max(4, parseFloat(`${2 & weiboP}`));
         weiboP ^= parseInt(`${overlayK}`);
         overlayK -= parseFloat(`${3 | parseInt(`${starG}`)}`);
         specU = `${weiboP * 1}`;
      }
      while (2.52 < (4.91 / (Math.max(6, videoB))) || 4.91 < videoB) {
          let sorto: Map<any, any> = new Map([[String.fromCharCode(121,95,56,56,95,100,114,97,103,0),912], [String.fromCharCode(115,117,112,112,108,121,95,48,95,53,51,0),448], [String.fromCharCode(103,117,105,100,95,51,95,50,0),728]]);
          let hoverW = 3;
          let dataK: Array<any> = [String.fromCharCode(111,99,97,108,105,122,97,116,105,111,110,95,54,95,49,48,48,0), String.fromCharCode(115,101,109,97,110,116,105,99,95,110,95,49,57,0)];
          let modali = String.fromCharCode(97,118,97,116,97,114,115,95,108,95,56,57,0);
          let langkeyl = String.fromCharCode(109,111,100,101,95,106,95,52,54,0);
         videoB *= parseFloat(`${hoverW % (Math.max(modali.length, 6))}`);
         sorto = new Map([[`${dataK.length}`, dataK.length]]);
         hoverW &= dataK.length ^ sorto.size;
         modali = "2";
         langkeyl += "2";
         break;
      }
      while (acceptedQ && areaM.length <= 5) {
         areaM += "3";
         break;
      }
      if (notificationV >= 4.51) {
         group4 = !acceptedQ;
      }
         group4 = areaM.length < 51;
         acceptedQ = videoB >= 30.84;
      while (group4 || (notificationV - 2.5) < 4.15) {
         group4 = areaM.length < 32;
         break;
      }
      let componento = acceptedQ ? !acceptedQ : acceptedQ;
      do {
         acceptedQ = videoB <= 91.52;
         if (componento) {
            break;
         }
      } while (componento && (!acceptedQ));
      whiteE = [indicatoru];
   }

    onClose();

       let c_unlock9 = String.fromCharCode(98,95,50,54,95,112,111,115,101,115,0);
          let configurej = 3;
         c_unlock9 = `${c_unlock9.length}`;
         configurej |= configurej & configurej;
      for (let t = 0; t < 2; t++) {
         c_unlock9 = "3";
      }
      if (c_unlock9 != c_unlock9) {
          let mappingd = String.fromCharCode(116,101,108,101,112,104,111,116,111,95,101,95,57,54,0);
         c_unlock9 += "1";
         mappingd = `${(String.fromCharCode(71,0) == mappingd ? mappingd.length : mappingd.length)}`;
      }
      borderless_ >>= Math.min(3, c_unlock9.length);
   let textL = 5219610 <= indicatoru;
   do {
      indicatoru ^= parseInt(`${entryM}`);
      if (textL) {
         break;
      }
   } while ((borderless_ < 3) && textL);
   let matchH = readG.length >= 7214218;
   do {
      readG = `${indicatoru}`;
      if (matchH) {
         break;
      }
   } while (matchH && ((4 * readG.length) > 4));
   for (let b = 0; b < 1; b++) {
      teamj = `${parseInt(`${entryM}`) | teamj.length}`;
   }
    dispatch(vipPromotionModalShown());

      borderless_ %= Math.max(firebaseL.length, 2);
   for (let h = 0; h < 3; h++) {
      indicatoru -= firebaseL.length - member0;
   }
      firebaseL.push((String.fromCharCode(98,0) == teamj ? indicatoru : teamj.length));
   while (teamj.includes(`${indicatoru}`)) {
      indicatoru %= Math.max(4, whiteE.length + 2);
      break;
   }

    navigator.navigate("付费VIP");
    
    
    
    
    
  }, []);

  return (
    <Modal
      visible={showCondition}
      transparent={true}
      animationType="fade"
      onRequestClose={onClose}
    >
      <TouchableWithoutFeedback onPress={onClose}>
        <View style={{ flex: 1, backgroundColor: "rgba(0, 0, 0, 0.4)" }}>
          <VipPromotionModal
            coverBackground={sportBg}
            coverImage={commonModels}
            onPurchase={handleOnPurchase}
            onClose={onCloseModal}
            showCondition={showCondition}
          />
        </View>
      </TouchableWithoutFeedback>
    </Modal>
  );
};
