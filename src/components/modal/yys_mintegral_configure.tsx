import { useNavigation, useTheme } from "@react-navigation/native";
import { useCallback, useContext, useEffect } from "react";
import { BackHandler, StyleSheet, TouchableOpacity, View } from "react-native";
import { Text } from "react-native";
import RNExitApp from "react-native-exit-app";
import { useAppDispatch } from "@hooks";
import { acceptPrivacyPolicy } from "@redux";
import { TermsAcceptContext } from "../../contexts/yys_iconarrowrightblack";


interface yysIconarrowrightorangeStation {
  isVisible: boolean,
  setIsVisible: any,
}

export default function PrivacyPolicyOverlay({
  isVisible,
  setIsVisible,
}: yysIconarrowrightorangeStation) {
  const { accepted, setAccepted } = useContext(TermsAcceptContext);
  const { spacing, textVariants, colors } = useTheme();
  const dispatch = useAppDispatch();

  const navigation = useNavigation();

  useEffect(() => {
    setIsVisible(!accepted);
  }, []);

  const handleReject = useCallback(() => {
       let libturbomodulejsijniQ = 0;
    let switch_l9 = String.fromCharCode(113,95,53,54,95,111,112,101,110,115,108,101,115,0);
    let termsm: Map<any, any> = new Map([[String.fromCharCode(115,115,101,100,105,102,102,95,110,95,56,53,0),873], [String.fromCharCode(112,95,51,56,95,98,97,108,97,110,99,101,100,0),895]]);
    let plusp = String.fromCharCode(99,95,51,56,95,115,116,101,110,99,105,108,0);
    let d_playerJ = false;
    let castingg = true;
    let filedc = 4.0;
    let viewsW = 5;
    let membershipC = String.fromCharCode(108,105,102,101,116,105,109,101,95,100,95,53,53,0);
    let securityQ = String.fromCharCode(119,114,105,116,101,95,122,95,49,55,0);
    let theme0 = 5.0;
    let description_pH: Array<any> = [121, 604, 101];
   while (Array.from(termsm.values()).includes(description_pH.length)) {
      description_pH = [description_pH.length % (Math.max(7, parseInt(`${theme0}`)))];
      break;
   }
   for (let s = 0; s < 3; s++) {
       let navigationx = String.fromCharCode(99,95,52,50,95,117,110,99,111,110,102,105,114,109,101,100,0);
       let bannerY = String.fromCharCode(109,95,56,49,95,98,101,116,104,115,111,102,116,118,105,100,0);
       let minivoda = String.fromCharCode(112,95,53,52,95,119,104,97,116,0);
       let termsL: Array<any> = [972, 853];
       let private_37Y: Array<any> = [1000, 946, 802];
      while (3 > bannerY.length) {
         navigationx = `${navigationx.length}`;
         break;
      }
       let telemetryO = 0;
         navigationx += `${2 | minivoda.length}`;
       let leaguedetailsbgd = 5.0;
       let middleS = 4.0;
         telemetryO >>= Math.min(5, Math.abs(1 + termsL.length));
         bannerY += `${bannerY.length + 1}`;
         bannerY = `${parseInt(`${middleS}`)}`;
       let defaultprofilepic9 = 4.0;
      for (let s = 0; s < 2; s++) {
         navigationx += `${termsL.length}`;
      }
          let windD = false;
          let greyarrowupK = 0;
          let bootsplashJ = String.fromCharCode(105,110,112,117,116,116,101,109,0);
         bannerY = `${1 ^ parseInt(`${leaguedetailsbgd}`)}`;
         windD = 69 >= greyarrowupK;
         greyarrowupK += (bootsplashJ == String.fromCharCode(86,0) ? bootsplashJ.length : (windD ? 4 : 2));
          let iconsubssuccesss = String.fromCharCode(116,95,52,55,95,111,109,112,111,115,101,0);
          let castingw = 5.0;
         private_37Y = [minivoda.length];
         iconsubssuccesss += `${iconsubssuccesss.length}`;
         castingw -= parseInt(`${castingw}`);
          let settingsJ = String.fromCharCode(110,95,54,51,95,105,115,115,117,101,0);
         telemetryO /= Math.max(1 >> (Math.min(2, bannerY.length)), 5);
         settingsJ += `${settingsJ.length + 1}`;
         telemetryO <<= Math.min(1, Math.abs(parseInt(`${leaguedetailsbgd}`) % (Math.max(parseInt(`${middleS}`), 1))));
         navigationx += "1";
      for (let b = 0; b < 2; b++) {
         bannerY += `${(minivoda == String.fromCharCode(120,0) ? minivoda.length : termsL.length)}`;
      }
      membershipC = `${plusp.length & 2}`;
   }
   if (2 <= description_pH.length && (description_pH.length & 2) <= 4) {
       let homeactived = String.fromCharCode(115,101,112,105,97,95,117,95,55,50,0);
         homeactived = `${homeactived.length}`;
      for (let k = 0; k < 2; k++) {
         homeactived += `${homeactived.length % (Math.max(homeactived.length, 2))}`;
      }
         homeactived = `${homeactived.length}`;
      description_pH = [libturbomodulejsijniQ * 1];
   }
      viewsW += plusp.length;
       let inouttargetyellowl = String.fromCharCode(97,118,101,114,95,117,95,50,0);
          let mapbufferu = String.fromCharCode(114,101,99,111,118,101,114,121,95,122,95,52,48,0);
         inouttargetyellowl = `${mapbufferu.length | inouttargetyellowl.length}`;
         inouttargetyellowl = `${1 + inouttargetyellowl.length}`;
         inouttargetyellowl += `${inouttargetyellowl.length}`;
      termsm.set(`${securityQ}`, (String.fromCharCode(104,0) == securityQ ? termsm.size : securityQ.length));
   while (5 > (termsm.size - securityQ.length) || (securityQ.length - termsm.size) > 5) {
      securityQ = `${securityQ.length * plusp.length}`;
      break;
   }
      d_playerJ = castingg;
   for (let o = 0; o < 2; o++) {
      description_pH.push(3 - membershipC.length);
   }
       let libruntimeexecutory = true;
      for (let w = 0; w < 3; w++) {
         libruntimeexecutory = !libruntimeexecutory;
      }
      let register_bE = libruntimeexecutory ? !libruntimeexecutory : libruntimeexecutory;
      do {
         libruntimeexecutory = !libruntimeexecutory;
         if (register_bE) {
            break;
         }
      } while ((!libruntimeexecutory) && register_bE);
          let subbasketballplayerd: Map<any, any> = new Map([[String.fromCharCode(100,95,55,55,95,118,97,100,107,104,122,0),467], [String.fromCharCode(111,95,55,51,95,100,105,118,105,100,111,114,115,0),981]]);
         libruntimeexecutory = !libruntimeexecutory;
         subbasketballplayerd.set(`${subbasketballplayerd.size}`, 1);
      switch_l9 = `${((castingg ? 2 : 5) | 2)}`;
       let libreactA = String.fromCharCode(121,95,57,49,95,109,105,114,114,111,114,105,110,103,0);
       let statsd: Map<any, any> = new Map([[String.fromCharCode(116,104,101,105,114,95,118,95,49,55,0),578], [String.fromCharCode(97,116,111,109,105,99,97,108,108,121,95,101,95,55,49,0),284], [String.fromCharCode(109,95,56,51,95,120,109,117,108,0),159]]);
       let orangeclock4 = true;
      for (let x = 0; x < 1; x++) {
          let executor6 = 5.0;
          let footballtrophyo = String.fromCharCode(115,104,97,114,101,100,107,101,121,95,110,95,49,51,0);
          let humidityz = 3.0;
          let resendB = 5.0;
         orangeclock4 = 38 > statsd.size && libreactA.length > 38;
         executor6 += parseFloat(`${parseInt(`${humidityz}`) << (Math.min(4, Math.abs(3)))}`);
         footballtrophyo += `${footballtrophyo.length & 2}`;
         humidityz += parseInt(`${humidityz}`);
         resendB *= 2 - parseInt(`${humidityz}`);
      }
         statsd = new Map([[`${statsd.size}`, statsd.size]]);
         statsd.set(`${libreactA}`, libreactA.length * statsd.size);
      if (!libreactA.startsWith(`${statsd.size}`)) {
         libreactA += `${libreactA.length}`;
      }
      let historyh = libreactA.length >= 9441653;
      do {
         libreactA = `${((orangeclock4 ? 4 : 3) * statsd.size)}`;
         if (historyh) {
            break;
         }
      } while ((5 == (statsd.size << (Math.min(Math.abs(2), 3)))) && historyh);
      while (orangeclock4) {
         orangeclock4 = 38 > statsd.size;
         break;
      }
         orangeclock4 = libreactA.length > 46;
      for (let g = 0; g < 3; g++) {
         statsd.set(`${orangeclock4}`, statsd.size);
      }
          let libjsijniprofilerK = String.fromCharCode(118,95,57,54,95,114,101,97,100,105,110,0);
         statsd.set(libreactA, 2);
         libjsijniprofilerK = `${libjsijniprofilerK.length * 2}`;
      castingg = switch_l9.length > 23;
   let bgvipxvodz = d_playerJ ? !d_playerJ : d_playerJ;
   do {
      d_playerJ = termsm.get(`${filedc}`) != null;
      if (bgvipxvodz) {
         break;
      }
   } while (bgvipxvodz && ((5 + description_pH.length) > 1 || description_pH.length > 5));
       let floaterI = String.fromCharCode(114,97,110,100,111,109,95,107,95,49,51,0);
       let detailw = String.fromCharCode(99,114,108,102,0);
       let iconlogouto: Map<any, any> = new Map([[String.fromCharCode(115,99,116,112,95,122,95,51,50,0),String.fromCharCode(115,112,101,99,95,99,95,57,55,0)], [String.fromCharCode(113,95,49,56,95,100,101,113,117,101,117,101,100,0),String.fromCharCode(112,108,97,99,101,95,110,95,49,55,0)], [String.fromCharCode(110,95,54,52,95,100,110,120,104,100,100,97,116,97,0),String.fromCharCode(108,105,110,107,115,95,98,95,57,48,0)]]);
      for (let j = 0; j < 3; j++) {
          let phonef = String.fromCharCode(98,95,57,48,95,120,109,112,101,103,0);
          let mbsplash_ = 3;
          let progress8 = 5.0;
          let libloggerz = 0.0;
         detailw = "1";
         phonef += `${parseInt(`${progress8}`) - parseInt(`${libloggerz}`)}`;
         mbsplash_ |= parseInt(`${libloggerz}`) >> (Math.min(3, Math.abs(3)));
         progress8 -= phonef.length ^ parseInt(`${progress8}`);
      }
      for (let a = 0; a < 2; a++) {
          let userx = String.fromCharCode(106,95,55,49,95,108,101,110,103,116,104,115,0);
         floaterI = "3";
         userx += `${userx.length}`;
      }
          let materialK = String.fromCharCode(112,95,56,95,115,105,103,110,97,116,117,114,101,0);
         detailw += `${floaterI.length}`;
         materialK = `${materialK.length >> (Math.min(Math.abs(3), 4))}`;
      for (let s = 0; s < 2; s++) {
         detailw += `${floaterI.length << (Math.min(1, Math.abs(iconlogouto.size)))}`;
      }
          let cricketd = 5.0;
          let iconcalendarK = 1;
         iconlogouto = new Map([[`${iconcalendarK}`, 1 - detailw.length]]);
         cricketd -= parseInt(`${cricketd}`) + 1;
         iconcalendarK /= Math.max(4, 1);
      for (let s = 0; s < 3; s++) {
         floaterI += `${1 * floaterI.length}`;
      }
      if (detailw != floaterI) {
         floaterI = `${1 / (Math.max(2, floaterI.length))}`;
      }
      if (floaterI != String.fromCharCode(83,0)) {
         detailw += `${floaterI.length * 2}`;
      }
         iconlogouto = new Map([[detailw, floaterI.length * 3]]);
      termsm = new Map([[detailw, parseInt(`${theme0}`)]]);
   let yellowtored3 = d_playerJ ? !d_playerJ : d_playerJ;
   do {
      d_playerJ = description_pH.includes(filedc);
      if (yellowtored3) {
         break;
      }
   } while ((!plusp.endsWith(`${d_playerJ}`)) && yellowtored3);
   while (description_pH.length > 4) {
      description_pH.push((1 >> (Math.min(4, Math.abs((castingg ? 3 : 2))))));
      break;
   }
      termsm = new Map([[`${viewsW}`, plusp.length]]);
   if (d_playerJ) {
      castingg = (viewsW << (Math.min(membershipC.length, 1))) > 2;
   }
   while (d_playerJ) {
      d_playerJ = securityQ.length <= description_pH.length;
      break;
   }
       let libnms6 = false;
         libnms6 = (libnms6 ? libnms6 : !libnms6);
      for (let i = 0; i < 1; i++) {
         libnms6 = (!libnms6 ? libnms6 : libnms6);
      }
      if (libnms6) {
          let moonW = String.fromCharCode(114,101,100,97,99,116,101,100,95,120,95,49,53,0);
          let signinup0 = 5.0;
          let playershirt9: Map<any, any> = new Map([[String.fromCharCode(111,102,102,101,114,115,95,107,95,56,57,0),766], [String.fromCharCode(109,111,99,107,95,99,95,56,52,0),646], [String.fromCharCode(99,95,49,54,95,109,111,110,111,99,104,114,111,109,101,0),429]]);
         libnms6 = moonW.length > 80;
         moonW = "3";
         signinup0 += playershirt9.size;
         playershirt9.set(`${signinup0}`, 2 / (Math.max(1, playershirt9.size)));
      }
      plusp = `${membershipC.length}`;
      viewsW >>= Math.min(5, Math.abs(2));
   let short_ni = termsm.size <= 8936936;
   do {
      termsm.set(`${d_playerJ}`, ((d_playerJ ? 4 : 2) % (Math.max(1, 10))));
      if (short_ni) {
         break;
      }
   } while (short_ni && (castingg));
      description_pH.push(switch_l9.length >> (Math.min(Math.abs(1), 3)));
       let predictionactiveV = String.fromCharCode(107,95,50,51,95,99,105,112,104,101,114,98,121,110,105,100,0);
       let weatherg = String.fromCharCode(120,95,53,48,95,118,97,114,105,97,116,105,111,110,0);
       let shielddone_ = 5.0;
         shielddone_ += parseInt(`${shielddone_}`);
      let navigationt = String.fromCharCode(103,118,114,0) == predictionactiveV;
      do {
          let icondownimgO = false;
          let profiley = true;
          let greytickS = 1.0;
          let indexh = 5.0;
         predictionactiveV += `${((icondownimgO ? 5 : 4))}`;
         icondownimgO = (greytickS - indexh) >= 8.80;
         profiley = 65.43 < greytickS && indexh < 65.43;
         if (navigationt) {
            break;
         }
      } while ((4 == predictionactiveV.length) && navigationt);
         weatherg += `${(String.fromCharCode(67,0) == weatherg ? weatherg.length : parseInt(`${shielddone_}`))}`;
      for (let n = 0; n < 2; n++) {
         shielddone_ /= Math.max(2, weatherg.length - predictionactiveV.length);
      }
      if (weatherg == predictionactiveV) {
          let mime3 = String.fromCharCode(117,95,55,52,95,101,98,117,108,97,115,0);
          let fieldf = String.fromCharCode(105,110,116,114,97,120,98,108,111,99,107,95,108,95,50,57,0);
          let iconrefresho: Map<any, any> = new Map([[String.fromCharCode(97,95,50,50,95,109,97,108,101,0),false ], [String.fromCharCode(109,111,110,111,115,112,97,99,101,100,95,52,95,56,48,0),false ], [String.fromCharCode(106,95,51,54,95,120,104,116,109,108,0),true ]]);
         predictionactiveV = `${iconrefresho.size ^ 3}`;
         mime3 += `${mime3.length}`;
         fieldf = `${mime3.length}`;
         iconrefresho = new Map([[mime3, mime3.length]]);
      }
         shielddone_ += predictionactiveV.length + parseInt(`${shielddone_}`);
      if ((predictionactiveV.length | 2) > 4) {
         shielddone_ *= parseInt(`${shielddone_}`);
      }
         predictionactiveV = `${weatherg.length >> (Math.min(Math.abs(3), 1))}`;
       let libmapbufferjnid = 5;
       let saveH = 1;
      description_pH.push(libturbomodulejsijniQ);
      plusp = `${parseInt(`${filedc}`) / 1}`;
       let lessG = 2;
       let sansA = 0;
       let nativeexX = String.fromCharCode(100,111,99,117,109,101,110,116,115,95,121,95,50,55,0);
         nativeexX = `${lessG % (Math.max(nativeexX.length, 1))}`;
       let videojsG = 3;
       let emojiheartG = 4;
         emojiheartG <<= Math.min(1, Math.abs(videojsG % (Math.max(10, emojiheartG))));
         videojsG += 3;
      if (2 <= (videojsG | 5) && (videojsG | nativeexX.length) <= 5) {
         videojsG ^= lessG;
      }
      if (3 == (nativeexX.length * sansA)) {
          let stringsw: Array<any> = [String.fromCharCode(101,118,97,108,117,97,116,101,100,95,98,95,51,0), String.fromCharCode(103,95,49,54,95,117,108,116,105,0)];
          let collectiony = String.fromCharCode(105,100,101,116,95,55,95,49,55,0);
          let showmorew = 1.0;
         sansA >>= Math.min(5, Math.abs(emojiheartG));
         stringsw.push((collectiony == String.fromCharCode(106,0) ? collectiony.length : stringsw.length));
         showmorew /= Math.max(2, 5);
      }
      while ((5 * videojsG) == 5 || (sansA * 5) == 5) {
          let modalY = 0;
          let backD = 0.0;
          let philippinesQ: Map<any, any> = new Map([[String.fromCharCode(105,95,57,50,95,112,105,99,107,105,110,103,0),194], [String.fromCharCode(104,95,56,95,107,101,121,101,100,0),365]]);
          let iconbell2 = false;
          let shielddoneM = 5;
         videojsG ^= 3;
         modalY |= 1 * modalY;
         backD -= 3 ^ philippinesQ.size;
         philippinesQ.set(`${shielddoneM}`, philippinesQ.size);
         iconbell2 = shielddoneM <= 24 || 24 <= philippinesQ.size;
         break;
      }
      if (lessG > 5) {
         videojsG &= videojsG * 1;
      }
      let iconpipexpandI = videojsG <= 6441434;
      do {
         videojsG %= Math.max(sansA % (Math.max(7, lessG)), 3);
         if (iconpipexpandI) {
            break;
         }
      } while ((5 < (videojsG ^ 4) && (4 ^ emojiheartG) < 1) && iconpipexpandI);
      theme0 /= Math.max((parseInt(`${theme0}`) / (Math.max(7, (d_playerJ ? 4 : 5)))), 4);
      plusp = "1";
      description_pH = [securityQ.length | 3];
   while (termsm.size <= 2) {
       let middlesoundB = false;
       let historym = false;
       let nativem = true;
         middlesoundB = !nativem;
       let iconnotificationnewE = String.fromCharCode(100,101,115,101,114,105,97,108,105,122,101,95,114,95,57,50,0);
       let middlesoundK = String.fromCharCode(110,95,57,55,95,112,114,111,112,111,115,97,108,0);
      d_playerJ = 76 > membershipC.length && 76 > securityQ.length;
      break;
   }
   if (!description_pH.includes(viewsW)) {
      viewsW >>= Math.min(Math.abs(parseInt(`${theme0}`)), 5);
   }
   let iconbellactive5 = securityQ == String.fromCharCode(115,53,112,108,119,48,57,49,55,0);
   do {
       let downloadingL = 1.0;
       let showmoreJ: Array<any> = [String.fromCharCode(98,105,103,0), String.fromCharCode(114,101,110,97,109,101,95,48,95,53,56,0), String.fromCharCode(105,110,99,108,117,100,101,95,108,95,55,49,0)];
       let playershirt0 = 5.0;
         downloadingL += parseFloat(`${3}`);
         downloadingL += parseFloat(`${3 << (Math.min(Math.abs(parseInt(`${playershirt0}`)), 2))}`);
      if (showmoreJ.length <= parseInt(`${downloadingL}`)) {
          let applicationv = String.fromCharCode(121,95,56,49,95,114,101,99,97,108,99,117,108,97,116,101,0);
          let viewsq = String.fromCharCode(98,95,57,56,95,111,110,101,119,97,121,0);
         downloadingL -= parseFloat(`${showmoreJ.length}`);
         applicationv += `${1 & applicationv.length}`;
         viewsq = `${(String.fromCharCode(67,0) == applicationv ? viewsq.length : applicationv.length)}`;
      }
      let iconschedulel = 9002363.0 >= playershirt0;
      do {
          let entryt: Array<any> = [400, 670, 635];
          let backwhitei = 1.0;
          let shootyesgoalK = String.fromCharCode(112,95,48,95,119,97,118,101,108,101,116,0);
          let robotoR: Array<any> = [413, 511, 332];
         playershirt0 -= parseFloat(`${parseInt(`${playershirt0}`) / (Math.max(parseInt(`${downloadingL}`), 3))}`);
         entryt = [robotoR.length];
         backwhitei /= Math.max(parseFloat(`${robotoR.length * 3}`), 1);
         shootyesgoalK = `${3 | robotoR.length}`;
         if (iconschedulel) {
            break;
         }
      } while (iconschedulel && (3.0 <= (playershirt0 + downloadingL)));
      let shirtb = 5445595 >= showmoreJ.length;
      do {
          let middlebrightnessF = 0.0;
          let libjsinspectorp = 5.0;
          let windC = String.fromCharCode(112,97,110,101,108,95,100,95,57,50,0);
         showmoreJ.push(3 + parseInt(`${libjsinspectorp}`));
         middlebrightnessF *= parseInt(`${middlebrightnessF}`) | windC.length;
         libjsinspectorp *= 1;
         windC = `${parseInt(`${middlebrightnessF}`) - windC.length}`;
         if (shirtb) {
            break;
         }
      } while ((4 >= (showmoreJ.length * parseInt(`${playershirt0}`)) || (parseInt(`${playershirt0}`) * showmoreJ.length) >= 4) && shirtb);
      let attributedstring0 = showmoreJ.length <= 9373272;
      do {
          let hooksS = String.fromCharCode(102,95,53,52,95,102,117,108,108,121,0);
          let singlev = String.fromCharCode(117,95,49,51,95,100,114,97,103,103,97,98,108,101,0);
          let yellowm = String.fromCharCode(108,95,52,57,95,114,101,115,111,117,114,99,101,0);
          let fill7 = String.fromCharCode(116,97,98,108,101,95,99,95,55,0);
          let placeholderq = String.fromCharCode(112,95,50,51,95,105,110,108,105,103,104,116,0);
         showmoreJ.push(parseInt(`${downloadingL}`) * 2);
         hooksS = `${(String.fromCharCode(66,0) == singlev ? yellowm.length : singlev.length)}`;
         yellowm = `${yellowm.length % (Math.max(3, 2))}`;
         fill7 = `${hooksS.length + 1}`;
         placeholderq += `${placeholderq.length << (Math.min(4, hooksS.length))}`;
         if (attributedstring0) {
            break;
         }
      } while ((2 > (showmoreJ.length % 5) && (2.18 + playershirt0) > 2.82) && attributedstring0);
      while ((3.44 * playershirt0) >= 5.69) {
         showmoreJ = [showmoreJ.length];
         break;
      }
         playershirt0 *= parseFloat(`${parseInt(`${downloadingL}`) - 1}`);
         playershirt0 /= Math.max(parseFloat(`${1 & parseInt(`${playershirt0}`)}`), 4);
      securityQ += "1";
      if (iconbellactive5) {
         break;
      }
   } while ((plusp.length <= 2) && iconbellactive5);
   let defaultteamB = switch_l9 == String.fromCharCode(106,107,48,105,56,116,56,106,0);
   do {
      switch_l9 = `${3 / (Math.max(parseInt(`${theme0}`), 7))}`;
      if (defaultteamB) {
         break;
      }
   } while (defaultteamB && (1 < switch_l9.length));

    RNExitApp.exitApp();
  }, []);

  const handleAccept = useCallback(() => {
       let chinaX = String.fromCharCode(114,101,109,97,116,114,105,120,105,110,103,95,113,95,52,50,0);
    let philippinesd = String.fromCharCode(111,112,116,105,109,105,115,116,105,99,95,48,95,53,57,0);
    let dependencyM = 0.0;
    let mbjscommonp = 0.0;
    let turndownU = String.fromCharCode(98,108,111,119,102,105,115,104,95,122,95,57,50,0);
    let inactivew: Map<any, any> = new Map([[String.fromCharCode(117,95,50,56,95,105,100,99,116,100,115,112,0),true ], [String.fromCharCode(101,95,54,54,95,97,100,100,99,111,110,115,116,0),true ]]);
    let orangeO = 4.0;
    let anythinkb = String.fromCharCode(108,111,103,103,101,114,115,95,109,95,53,48,0);
    let arrowrightwithtails = 2.0;
    let iconviewergifd = String.fromCharCode(116,112,99,95,101,95,54,48,0);
    let backm: Array<any> = [576, 969, 626];
   for (let c = 0; c < 1; c++) {
      anythinkb += `${parseInt(`${orangeO}`) - 2}`;
   }
       let yellowscoreball5 = 2.0;
       let agreementT = String.fromCharCode(113,112,102,105,108,101,95,122,95,54,54,0);
         yellowscoreball5 -= parseFloat(`${agreementT.length * 3}`);
      while (!agreementT.startsWith(`${yellowscoreball5}`)) {
         agreementT += `${2 % (Math.max(7, parseInt(`${yellowscoreball5}`)))}`;
         break;
      }
      while (4.17 == (parseFloat(`${agreementT.length}`) - yellowscoreball5)) {
         agreementT = `${parseInt(`${yellowscoreball5}`)}`;
         break;
      }
         agreementT += `${3 | agreementT.length}`;
      if (1 <= (parseInt(`${yellowscoreball5}`) - 5) || 5 <= (parseInt(`${yellowscoreball5}`) - agreementT.length)) {
         agreementT += `${1 + parseInt(`${yellowscoreball5}`)}`;
      }
         yellowscoreball5 /= Math.max(2, parseFloat(`${2 - agreementT.length}`));
      turndownU = `${parseInt(`${mbjscommonp}`)}`;
   for (let n = 0; n < 2; n++) {
      dependencyM /= Math.max(3 - philippinesd.length, 3);
   }
   if ((arrowrightwithtails / 1.64) == 3.95) {
      anythinkb = `${inactivew.size << (Math.min(Math.abs(3), 1))}`;
   }
      inactivew = new Map([[`${inactivew.size}`, inactivew.size]]);
       let topicr = false;
       let loginsuccessd = String.fromCharCode(99,95,51,55,95,103,114,97,100,102,117,110,0);
          let xvodX = false;
          let gifgoalbg0 = 0.0;
         topicr = !xvodX;
         gifgoalbg0 /= Math.max(2, parseInt(`${gifgoalbg0}`) << (Math.min(Math.abs(parseInt(`${gifgoalbg0}`)), 4)));
      let encryptorX = topicr ? !topicr : topicr;
      do {
         topicr = loginsuccessd.endsWith(`${topicr}`);
         if (encryptorX) {
            break;
         }
      } while ((3 == loginsuccessd.length) && encryptorX);
          let imageactionliveF: Map<any, any> = new Map([[String.fromCharCode(114,95,52,55,95,100,97,114,107,0),true ], [String.fromCharCode(110,95,50,56,95,97,99,102,102,0),true ], [String.fromCharCode(112,104,111,110,101,98,111,111,107,95,106,95,50,55,0),false ]]);
          let icontransferclubL = String.fromCharCode(114,101,99,101,105,118,101,95,52,95,49,53,0);
         topicr = 96 == icontransferclubL.length;
         imageactionliveF.set(`${imageactionliveF.size}`, imageactionliveF.size | 2);
         icontransferclubL = `${imageactionliveF.size % (Math.max(9, imageactionliveF.size))}`;
         loginsuccessd = `${loginsuccessd.length}`;
      while (2 < loginsuccessd.length) {
         topicr = (85 == ((!topicr ? loginsuccessd.length : 85) % (Math.max(1, loginsuccessd.length))));
         break;
      }
         topicr = loginsuccessd.length < 92;
      mbjscommonp += (parseFloat(`${philippinesd == String.fromCharCode(108,0) ? parseInt(`${dependencyM}`) : philippinesd.length}`));
   let mathz = mbjscommonp <= 6082258.0;
   do {
      mbjscommonp /= Math.max(4, parseFloat(`${chinaX.length / (Math.max(1, 2))}`));
      if (mathz) {
         break;
      }
   } while (((mbjscommonp * 3.60) == 4.51) && mathz);
   let subtext6 = String.fromCharCode(110,103,120,48,0) == chinaX;
   do {
      chinaX += `${parseInt(`${arrowrightwithtails}`) / (Math.max(turndownU.length, 6))}`;
      if (subtext6) {
         break;
      }
   } while ((3 <= (chinaX.length + 2) && (2 | chinaX.length) <= 5) && subtext6);
   let turn6 = 6812026.0 >= dependencyM;
   do {
      dependencyM *= chinaX.length;
      if (turn6) {
         break;
      }
   } while (turn6 && ((philippinesd.length % 5) > 2 && (philippinesd.length >> (Math.min(Math.abs(5), 2))) > 4));
   for (let q = 0; q < 3; q++) {
      orangeO -= parseFloat(`${parseInt(`${orangeO}`) / (Math.max(6, anythinkb.length))}`);
   }
       let small2 = String.fromCharCode(100,95,53,57,95,99,97,108,99,0);
       let usernamey = 3.0;
          let favorite0: Map<any, any> = new Map([[String.fromCharCode(102,99,109,117,108,0),921], [String.fromCharCode(108,101,114,112,105,110,103,95,109,95,55,56,0),37], [String.fromCharCode(115,112,101,101,99,104,95,119,95,52,57,0),502]]);
         usernamey -= 2 + favorite0.size;
          let ewardedw = true;
          let buttonn = 2;
          let diceg = String.fromCharCode(104,95,50,57,95,114,101,115,105,100,117,101,115,0);
         small2 = `${(diceg == String.fromCharCode(104,0) ? (ewardedw ? 4 : 4) : diceg.length)}`;
         ewardedw = 50 <= buttonn;
         buttonn ^= 1;
          let libcrashsdk6 = String.fromCharCode(114,97,100,105,120,95,56,95,50,50,0);
          let largebrightnessT = 1;
         usernamey /= Math.max(3, small2.length);
         libcrashsdk6 += `${largebrightnessT / 2}`;
         largebrightnessT -= largebrightnessT ^ 3;
      if (usernamey == 5.56) {
         usernamey /= Math.max(3, (String.fromCharCode(75,0) == small2 ? parseInt(`${usernamey}`) : small2.length));
      }
      let icondownimgW = 6242510.0 <= usernamey;
      do {
         usernamey -= small2.length;
         if (icondownimgW) {
            break;
         }
      } while (icondownimgW && ((parseInt(`${usernamey}`) / (Math.max(small2.length, 10))) >= 1 || 2 >= (1 / (Math.max(3, parseInt(`${usernamey}`))))));
      if ((small2.length + parseInt(`${usernamey}`)) < 2) {
          let owngoalA = false;
          let libfile4 = String.fromCharCode(116,95,52,51,95,116,97,107,101,110,0);
          let iconadslinkx: Array<any> = [String.fromCharCode(122,95,54,52,95,119,104,105,116,101,98,108,97,99,107,108,105,115,116,115,0), String.fromCharCode(115,117,98,116,105,116,108,101,95,56,95,49,49,0), String.fromCharCode(99,116,120,112,95,118,95,55,48,0)];
         small2 += `${libfile4.length}`;
         owngoalA = iconadslinkx.includes(owngoalA);
         libfile4 = `${iconadslinkx.length}`;
      }
      mbjscommonp += parseFloat(`${philippinesd.length}`);

    setIsVisible(false)

       let tumbnailD = true;
       let chatbotphotoj = false;
      let customn = tumbnailD ? !tumbnailD : tumbnailD;
      do {
         tumbnailD = (chatbotphotoj ? !tumbnailD : chatbotphotoj);
         if (customn) {
            break;
         }
      } while (customn && (chatbotphotoj));
         chatbotphotoj = !tumbnailD;
      while (chatbotphotoj || !tumbnailD) {
          let shootnogoalA = String.fromCharCode(97,116,111,109,105,99,97,108,108,121,95,118,95,56,49,0);
          let libavdeviceb = 3;
          let mbbid0 = 2.0;
         chatbotphotoj = (shootnogoalA.length - libavdeviceb) > 83;
         shootnogoalA += `${2 & parseInt(`${mbbid0}`)}`;
         libavdeviceb >>= Math.min(5, Math.abs(2 ^ parseInt(`${mbbid0}`)));
         break;
      }
      for (let o = 0; o < 2; o++) {
         tumbnailD = !chatbotphotoj;
      }
      let with_pq9 = chatbotphotoj ? !chatbotphotoj : chatbotphotoj;
      do {
         chatbotphotoj = !tumbnailD;
         if (with_pq9) {
            break;
         }
      } while (with_pq9 && (!tumbnailD && !chatbotphotoj));
       let iconbellactive0: Map<any, any> = new Map([[String.fromCharCode(99,95,49,48,95,112,97,115,115,112,104,114,97,115,101,0),259], [String.fromCharCode(104,111,115,116,112,111,114,116,102,105,108,101,95,108,95,52,48,0),549]]);
       let matchactiveD: Map<any, any> = new Map([[String.fromCharCode(98,95,52,50,95,115,97,109,105,0),String.fromCharCode(111,95,55,52,95,114,101,100,117,99,101,100,0)], [String.fromCharCode(99,97,108,99,95,57,95,55,57,0),String.fromCharCode(103,95,53,55,95,115,117,98,118,105,101,119,101,114,0)]]);
      chinaX = `${parseInt(`${arrowrightwithtails}`) & philippinesd.length}`;
      turndownU += `${parseInt(`${dependencyM}`) ^ parseInt(`${mbjscommonp}`)}`;
      anythinkb += `${(anythinkb == String.fromCharCode(85,0) ? parseInt(`${mbjscommonp}`) : anythinkb.length)}`;
       let leaguedetailsbgN = String.fromCharCode(114,97,112,112,101,114,95,49,95,57,0);
         leaguedetailsbgN = `${leaguedetailsbgN.length}`;
         leaguedetailsbgN += `${leaguedetailsbgN.length % (Math.max(7, leaguedetailsbgN.length))}`;
       let airbnbstary = String.fromCharCode(115,113,108,0);
      inactivew.set(`${orangeO}`, inactivew.size);
   if (!chinaX.endsWith(`${inactivew.size}`)) {
      chinaX = `${parseInt(`${orangeO}`)}`;
   }
   for (let r = 0; r < 1; r++) {
      philippinesd = "1";
   }
   while (!turndownU.startsWith(`${orangeO}`)) {
       let bnewinterstitialO = 5;
       let runtimeschedulerz = String.fromCharCode(101,95,49,54,95,99,97,110,100,105,100,97,116,101,0);
       let imagenomoredataq = false;
       let appsP = String.fromCharCode(111,102,102,101,114,101,100,95,121,95,55,0);
         bnewinterstitialO /= Math.max(3, 1);
      for (let z = 0; z < 1; z++) {
         runtimeschedulerz = `${bnewinterstitialO + 1}`;
      }
         bnewinterstitialO *= (appsP == String.fromCharCode(71,0) ? (imagenomoredataq ? 4 : 3) : appsP.length);
       let privilege9: Map<any, any> = new Map([[String.fromCharCode(115,95,55,54,95,97,108,108,111,119,105,0),true ], [String.fromCharCode(109,97,120,113,95,119,95,53,49,0),true ]]);
         bnewinterstitialO |= ((imagenomoredataq ? 2 : 4) % 1);
          let default_wC = 5;
          let nodef = String.fromCharCode(114,101,103,101,120,95,121,95,53,52,0);
         imagenomoredataq = nodef == String.fromCharCode(54,0);
         default_wC &= 3 & default_wC;
         nodef = `${default_wC}`;
       let libloggerS = String.fromCharCode(98,95,57,53,95,100,101,98,108,111,99,107,0);
      for (let u = 0; u < 1; u++) {
          let downarrowe: Array<any> = [String.fromCharCode(114,95,51,55,95,99,115,115,0), String.fromCharCode(99,97,114,95,48,95,56,50,0)];
          let defaultfootballbgE: Array<any> = [118, 556, 969];
          let issubo = 2.0;
          let middlesoundD = String.fromCharCode(116,105,108,101,120,95,115,95,54,0);
         runtimeschedulerz += `${1 & downarrowe.length}`;
         downarrowe.push(3 + defaultfootballbgE.length);
         defaultfootballbgE = [parseInt(`${issubo}`) / (Math.max(defaultfootballbgE.length, 9))];
         issubo += defaultfootballbgE.length;
         middlesoundD = `${1 + parseInt(`${issubo}`)}`;
      }
      while (2 >= libloggerS.length && !imagenomoredataq) {
          let vipsportE = 3;
         libloggerS += "3";
         vipsportE += 2;
         break;
      }
          let greyN: Map<any, any> = new Map([[String.fromCharCode(104,105,103,104,108,105,103,104,116,115,95,119,95,54,53,0),183], [String.fromCharCode(115,95,53,53,95,105,97,100,115,116,120,0),70], [String.fromCharCode(99,111,109,98,101,100,95,117,95,48,0),643]]);
          let rncoreW = 5.0;
         bnewinterstitialO /= Math.max(3, 2);
         greyN.set(`${rncoreW}`, 1 * parseInt(`${rncoreW}`));
      let whitevideoliveZ = String.fromCharCode(113,55,111,116,48,0) == runtimeschedulerz;
      do {
         runtimeschedulerz = `${((imagenomoredataq ? 4 : 4) / (Math.max(9, privilege9.size)))}`;
         if (whitevideoliveZ) {
            break;
         }
      } while (whitevideoliveZ && (runtimeschedulerz.length >= 2));
      let bggradientP = 9390643 >= bnewinterstitialO;
      do {
         bnewinterstitialO /= Math.max(1 - privilege9.size, 4);
         if (bggradientP) {
            break;
         }
      } while (bggradientP && (Array.from(privilege9.values()).includes(bnewinterstitialO)));
      orangeO -= parseFloat(`${parseInt(`${orangeO}`) % (Math.max(anythinkb.length, 5))}`);
      break;
   }
   let mappingv = orangeO >= 8249289.0;
   do {
       let castY = true;
       let anytimeq = String.fromCharCode(105,95,50,95,114,101,100,97,99,116,101,100,0);
       let iconarrowrightorangeP: Array<any> = [272, 571, 121];
       let yellowF = 4.0;
         iconarrowrightorangeP.push(iconarrowrightorangeP.length / (Math.max(3, 9)));
         iconarrowrightorangeP.push(3 + parseInt(`${yellowF}`));
       let libbuffer2 = String.fromCharCode(98,95,53,57,95,118,108,102,102,0);
         libbuffer2 = `${parseInt(`${yellowF}`)}`;
      let livenodatabgimgI = libbuffer2.length >= 5176034;
      do {
          let mapbufferX = 3;
          let stationsn = 1;
          let tailZ = String.fromCharCode(110,111,110,98,95,97,95,51,55,0);
          let libreactnativeblobt = String.fromCharCode(109,95,51,56,95,114,97,116,105,111,110,97,108,0);
          let linkV = String.fromCharCode(97,95,51,52,95,114,101,102,105,108,108,0);
         libbuffer2 += "3";
         mapbufferX %= Math.max(stationsn ^ 2, 5);
         stationsn &= (libreactnativeblobt == String.fromCharCode(100,0) ? libreactnativeblobt.length : linkV.length);
         tailZ += `${(libreactnativeblobt == String.fromCharCode(83,0) ? libreactnativeblobt.length : stationsn)}`;
         linkV += `${3 | libreactnativeblobt.length}`;
         if (livenodatabgimgI) {
            break;
         }
      } while (((yellowF - 4.2) < 1.9 && 1 < (libbuffer2.length - parseInt(`${yellowF}`))) && livenodatabgimgI);
         yellowF /= Math.max(5, anytimeq.length - iconarrowrightorangeP.length);
      for (let p = 0; p < 2; p++) {
         iconarrowrightorangeP.push(parseInt(`${yellowF}`) + 1);
      }
         yellowF *= ((castY ? 3 : 2) + anytimeq.length);
       let showmoree = String.fromCharCode(98,111,114,100,101,114,108,101,115,115,95,49,95,49,53,0);
       let benefitB = String.fromCharCode(101,109,117,108,97,116,105,111,110,95,50,95,49,56,0);
      while (castY) {
          let constants5 = String.fromCharCode(115,112,101,99,105,97,108,95,120,95,52,0);
          let redscoreballW = 2.0;
          let componentregistryW = 4.0;
          let bgvipsporti = 1.0;
          let iconplayt = 1.0;
         castY = 33 < showmoree.length;
         constants5 = `${constants5.length & parseInt(`${bgvipsporti}`)}`;
         redscoreballW += 2;
         componentregistryW += parseInt(`${iconplayt}`) & 1;
         bgvipsporti *= parseInt(`${iconplayt}`);
         break;
      }
         castY = 6 <= libbuffer2.length;
          let viewerD = 2.0;
          let predictionbannerv = String.fromCharCode(109,105,110,114,95,114,95,55,50,0);
         iconarrowrightorangeP = [3 << (Math.min(5, iconarrowrightorangeP.length))];
         viewerD *= parseFloat(`${1}`);
         predictionbannerv = `${predictionbannerv.length >> (Math.min(Math.abs(1), 2))}`;
      orangeO -= parseFloat(`${philippinesd.length}`);
      if (mappingv) {
         break;
      }
   } while ((chinaX.startsWith(`${orangeO}`)) && mappingv);
      anythinkb = `${anythinkb.length}`;
      philippinesd += `${philippinesd.length}`;
   let icond = 5905728.0 >= mbjscommonp;
   do {
      mbjscommonp += parseFloat(`${3}`);
      if (icond) {
         break;
      }
   } while (icond && (philippinesd.startsWith(`${mbjscommonp}`)));
    setAccepted(true)

   while (arrowrightwithtails <= 3.93) {
       let distD = String.fromCharCode(114,101,110,97,109,101,95,106,95,57,55,0);
       let template_dp = 1.0;
      let playershirt3 = 8767774 <= distD.length;
      do {
         distD = `${distD.length * parseInt(`${template_dp}`)}`;
         if (playershirt3) {
            break;
         }
      } while (playershirt3 && (4.46 >= (template_dp / (Math.max(5.9, 9)))));
       let renderd = true;
         renderd = distD.length <= 76;
      if (distD.length <= 5) {
         renderd = 16.70 > template_dp;
      }
      let optionsz = String.fromCharCode(54,106,57,111,110,52,105,55,118,117,0) == distD;
      do {
         distD = `${2 * distD.length}`;
         if (optionsz) {
            break;
         }
      } while (optionsz && ((template_dp * 3.94) == 5.6));
      if (distD.endsWith(`${renderd}`)) {
         renderd = distD.length < 74;
      }
      anythinkb = `${1 >> (Math.min(5, Math.abs(parseInt(`${orangeO}`))))}`;
      break;
   }
       let shrunkU = 1;
       let iconU = 0.0;
       let subinV = true;
         iconU /= Math.max(2, (parseFloat(`${(subinV ? 4 : 3) ^ parseInt(`${iconU}`)}`)));
      if ((iconU + 3.66) == 5.93 && 3.44 == (3.66 + iconU)) {
         iconU /= Math.max((parseFloat(`${parseInt(`${iconU}`) - (subinV ? 4 : 1)}`)), 1);
      }
      let issubd = 7361673 >= shrunkU;
      do {
          let stringr = String.fromCharCode(117,110,100,101,114,108,121,105,110,103,95,53,95,57,0);
          let iconadslinkn = 0;
          let libnmsr: Array<any> = [String.fromCharCode(97,95,50,48,95,112,97,114,101,110,116,97,103,101,0), String.fromCharCode(101,120,116,101,110,100,105,110,103,95,52,95,54,49,0), String.fromCharCode(103,95,51,56,95,114,101,102,101,116,99,104,0)];
         shrunkU <<= Math.min(Math.abs(1), 1);
         stringr = `${(stringr == String.fromCharCode(81,0) ? stringr.length : libnmsr.length)}`;
         iconadslinkn -= stringr.length;
         libnmsr.push(2 >> (Math.min(1, stringr.length)));
         if (issubd) {
            break;
         }
      } while (issubd && ((shrunkU >> (Math.min(Math.abs(3), 1))) == 5));
       let audiencey = String.fromCharCode(107,95,53,53,95,105,109,99,100,97,116,97,0);
       let libhermes_ = String.fromCharCode(115,111,102,97,0);
          let plusX: Map<any, any> = new Map([[String.fromCharCode(105,95,57,49,95,115,104,97,114,112,101,110,0),false ], [String.fromCharCode(102,114,97,103,109,101,110,116,115,95,50,95,51,0),false ]]);
          let side7: Array<any> = [941, 454, 773];
         iconU *= parseFloat(`${2 << (Math.min(2, side7.length))}`);
         plusX.set(`${plusX.size}`, plusX.size);
         side7 = [plusX.size];
         shrunkU &= (2 ^ (subinV ? 5 : 3));
      for (let j = 0; j < 3; j++) {
         iconU *= parseFloat(`${libhermes_.length}`);
      }
      while (iconU <= 3.100 && (iconU + 3.100) <= 2.41) {
          let statisticsactiveB: Map<any, any> = new Map([[String.fromCharCode(112,105,99,107,101,114,95,121,95,49,48,0),625], [String.fromCharCode(112,95,55,52,95,114,101,99,118,102,114,111,109,0),108]]);
          let textM: Map<any, any> = new Map([[String.fromCharCode(106,95,57,57,95,115,104,97,114,97,98,108,101,0),100], [String.fromCharCode(112,95,56,49,95,114,101,104,97,115,104,0),309]]);
          let relatedo = 4;
         subinV = audiencey.length <= libhermes_.length;
         statisticsactiveB.set(`${relatedo}`, relatedo);
         textM = new Map([[`${textM.size}`, 1 << (Math.min(5, Math.abs(textM.size)))]]);
         break;
      }
      while (libhermes_.length <= audiencey.length) {
         audiencey = "3";
         break;
      }
      mbjscommonp *= parseFloat(`${parseInt(`${dependencyM}`) >> (Math.min(3, Math.abs(3)))}`);
   if (philippinesd != anythinkb) {
      anythinkb = `${parseInt(`${mbjscommonp}`) & 3}`;
   }
   if (mbjscommonp > 5.12) {
      anythinkb += `${turndownU.length}`;
   }
   let rocketp = String.fromCharCode(110,50,116,48,115,108,110,53,107,100,0) == chinaX;
   do {
      chinaX = `${parseInt(`${orangeO}`) / (Math.max(7, turndownU.length))}`;
      if (rocketp) {
         break;
      }
   } while ((3.39 <= (chinaX.length + arrowrightwithtails) || 3 <= (chinaX.length & 1)) && rocketp);
   for (let a = 0; a < 3; a++) {
      chinaX = "2";
   }
      arrowrightwithtails *= inactivew.size;
       let networku = 1;
       let icondefaultthumbnaila = String.fromCharCode(118,109,110,99,95,110,95,54,52,0);
       let alertq = false;
         networku *= ((alertq ? 3 : 4) / (Math.max(5, icondefaultthumbnaila.length)));
         icondefaultthumbnaila += `${icondefaultthumbnaila.length}`;
       let thailandM = String.fromCharCode(112,95,54,54,95,104,97,110,103,0);
       let formy = String.fromCharCode(99,95,49,57,95,105,110,99,108,117,100,101,115,0);
      for (let u = 0; u < 1; u++) {
         formy += `${thailandM.length * 1}`;
      }
      for (let m = 0; m < 1; m++) {
          let sidet = String.fromCharCode(115,95,50,48,95,104,102,108,105,112,0);
          let iconorientationZ: Map<any, any> = new Map([[String.fromCharCode(108,95,56,49,95,110,112,111,105,110,116,115,0),794], [String.fromCharCode(100,114,97,119,95,108,95,51,57,0),203], [String.fromCharCode(99,104,111,112,95,115,95,50,52,0),979]]);
          let starQ = true;
          let assist8 = String.fromCharCode(104,95,49,48,95,99,111,109,109,111,110,0);
          let moviesE = 0.0;
         thailandM = `${icondefaultthumbnaila.length}`;
         sidet += "1";
         iconorientationZ = new Map([[assist8, assist8.length & 2]]);
         starQ = (assist8.length | sidet.length) <= 81;
         moviesE -= (parseInt(`${moviesE}`) * (starQ ? 2 : 3));
      }
         alertq = String.fromCharCode(117,0) == formy;
         thailandM = "1";
      while (icondefaultthumbnaila.length == thailandM.length) {
         thailandM += `${formy.length}`;
         break;
      }
      if (!alertq) {
         alertq = thailandM.length >= 90 && alertq;
      }
      philippinesd = `${3 & parseInt(`${mbjscommonp}`)}`;
      arrowrightwithtails *= 2;
   let ping8 = mbjscommonp <= 5220086.0;
   do {
      mbjscommonp -= parseFloat(`${inactivew.size * parseInt(`${dependencyM}`)}`);
      if (ping8) {
         break;
      }
   } while (((mbjscommonp / (Math.max(5.51, 4))) <= 3.29 && (5.51 / (Math.max(3, mbjscommonp))) <= 5.53) && ping8);
   for (let k = 0; k < 2; k++) {
      dependencyM -= (turndownU == String.fromCharCode(102,0) ? turndownU.length : inactivew.size);
   }
    dispatch(acceptPrivacyPolicy())
  }, [])

  const handleReadTermsPrivacy = useCallback((view: '用户协议' | '隐私政策') => {
    const checkGoBack = ({ data }) => {
      if (data.state.routes[data.state.routes.length - 1].name === 'Home') {
        setIsVisible(true);
        navigation.removeListener('state', checkGoBack);
      }
    }
    navigation.addListener('state', checkGoBack);

    navigation.navigate(view);
    setIsVisible(false);
  }, [])

  return (
    <>
      {isVisible &&
        <View
          style={{
            height: '100%',
            width: '100%',
            backgroundColor: 'rgba(0, 0, 0, 0.8)',
            position: 'absolute',
            justifyContent: 'center',
            alignItems: 'center'
          }}>
          <View style={{
            width: '75%',
            padding: 20,
            gap: spacing.l,
            backgroundColor: 'rgba(34, 34, 34, 1)',
            ...styles.overlay,
          }}>
            <Text style={{ ...textVariants.header, textAlign: 'center' }}>服务协议和隐私政策</Text>
            <Text
              style={{
                ...textVariants.subBody,
                textAlign: 'center',
              }}>
              <Text>
                请你务必审慎阅读,
                充分理解“服务协议”和“隐私政策”各条款，包括但不限于：为了更好的向你提供服务，我们需要收集你的设备标识，操作日常等信息用于分析，优化应用性能。你可阅读
              </Text>
              <Text onPress={() => handleReadTermsPrivacy('用户协议')}>
                <Text style={{ color: colors.primary }}>《服务协议》</Text>
              </Text>
              <Text>和</Text>
              <Text onPress={() => handleReadTermsPrivacy('隐私政策')}>
                <Text style={{ color: colors.primary }}>《隐私政策》</Text>
              </Text>
              <Text>
                了解详细信息。如果你同意，请点击下面按钮开始接受我们的服务。
              </Text>
            </Text>
            <View style={styles.btns}>
              <TouchableOpacity style={styles.btn} onPress={handleReject}>
                <Text
                  style={{
                    ...textVariants.body,
                    ...styles.head,
                  }}>
                  暂不同意
                </Text>
              </TouchableOpacity>
              <TouchableOpacity style={styles.btn} onPress={handleAccept}>
                <Text
                  style={{
                    ...textVariants.body,
                    ...styles.head,
                    color: colors.primary,
                  }}>
                  同意并接受
                </Text>
              </TouchableOpacity>
            </View>
          </View>
        </View>
      }
    </>
  )
}

const styles = StyleSheet.create({
  overlay: {
    borderRadius: 14,
  },
  head: {
    textAlign: 'center',
  },
  body: {
    color: 'white',
  },
  link: {
    display: 'flex',
  },
  btns: {
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
  },
  btn: {
    flex: 1,
  },
});
