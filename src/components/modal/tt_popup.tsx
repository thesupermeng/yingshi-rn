import { useNavigation } from "@react-navigation/native";
import VipPrivilegeModal from "./tt_details"
import { useCallback } from "react";
import { useAppDispatch, useAppSelector, useSelector } from "@hooks/tt_spec_download";
import { screenModel } from "@type/tt_twitter_data";
import { UMENG_CHANNEL } from "@utility/tt_trophy_cross";
import { ttFast } from "@models/tt_stations_right";
import { showLoginAction } from "@redux/actions/tt_copy_heji";
import { ttGoal } from "@redux/reducers/tt_selected";

const commonModels = require('@static/images/currentForward.png');
const sportBg = require('@static/images/firebaseTelegram.png');

interface ttSmall {
  showCondition: boolean;
  onClose: any;
  showBlur?: boolean;
}

export const CommonVipPrivilegeOverlay = ({ showCondition, onClose, showBlur }: ttSmall) => {

  const navigator = useNavigation()
  const dispatch = useAppDispatch()
  const userState = useSelector<ttGoal>('userReducer');

  const handleOnPurchase = useCallback(() => {
       let gemfile1 = false;
    let zhuboe: Map<any, any> = new Map([[String.fromCharCode(99,111,100,101,114,0),String.fromCharCode(109,112,108,97,109,101,0)], [String.fromCharCode(112,114,101,118,105,111,117,115,0),String.fromCharCode(114,101,100,97,99,116,0)], [String.fromCharCode(102,111,110,116,99,111,110,102,105,103,0),String.fromCharCode(98,119,114,105,116,101,0)]]);
    let catalogt = 2;
    let refresh2 = 2.0;
    let const_lf = String.fromCharCode(114,101,99,111,110,110,101,99,116,105,111,110,0);
    let buttons = 1.0;
    let filterB = 3;
    let quest3: Map<any, any> = new Map([[String.fromCharCode(116,101,114,109,105,110,97,116,105,111,110,0),String.fromCharCode(119,105,110,100,105,110,103,0)], [String.fromCharCode(100,114,97,110,100,0),String.fromCharCode(115,101,116,116,105,110,103,115,0)]]);
    let buttont = 4.0;
    let selectionO = 5.0;
    let untickb: Map<any, any> = new Map([[String.fromCharCode(99,111,118,114,0),278], [String.fromCharCode(102,105,110,100,101,114,0),524], [String.fromCharCode(109,97,110,97,103,101,100,0),191]]);
    let castY: Array<any> = [846, 743];
    let zoom_ = String.fromCharCode(108,101,97,115,116,0);
       let matches0 = 1.0;
      let nalyticsL = matches0 <= 9374448.0;
      do {
         matches0 -= parseFloat(`${parseInt(`${matches0}`)}`);
         if (nalyticsL) {
            break;
         }
      } while ((3.24 <= matches0) && nalyticsL);
       let x_countP = String.fromCharCode(115,104,111,119,119,97,118,101,115,112,105,99,0);
      while (4 > x_countP.length) {
         matches0 -= parseFloat(`${parseInt(`${matches0}`)}`);
         break;
      }
      quest3 = new Map([[`${buttons}`, filterB]]);
   if (quest3.size == 2) {
      refresh2 += 2;
   }
   let indicatorR = quest3.size <= 9452083;
   do {
      quest3.set(const_lf, 3);
      if (indicatorR) {
         break;
      }
   } while (indicatorR && (!Array.from(quest3.values()).includes(zhuboe.size)));
   let loginz = gemfile1 ? !gemfile1 : gemfile1;
   do {
      gemfile1 = 40.98 > refresh2;
      if (loginz) {
         break;
      }
   } while ((!const_lf.startsWith(`${gemfile1}`)) && loginz);

    onClose()

      quest3.set(`${selectionO}`, (String.fromCharCode(111,0) == const_lf ? parseInt(`${selectionO}`) : const_lf.length));
      untickb.set(const_lf, parseInt(`${refresh2}`));
   if (1 <= (4 - catalogt) && (filterB - catalogt) <= 4) {
      filterB |= ((gemfile1 ? 1 : 1) + parseInt(`${refresh2}`));
   }
       let arrowV = 5.0;
       let catalog0 = String.fromCharCode(101,120,112,114,108,105,115,116,0);
      while ((catalog0.length + arrowV) <= 3.3 || (arrowV + 3.3) <= 4.72) {
         catalog0 += "3";
         break;
      }
      while (3.52 < (5.9 + arrowV) || 3.66 < (5.9 + arrowV)) {
         arrowV -= parseInt(`${arrowV}`) ^ catalog0.length;
         break;
      }
       let kickt: Array<any> = [925, 685];
         catalog0 += `${2 / (Math.max(7, catalog0.length))}`;
         arrowV += 1 ^ catalog0.length;
       let activeA: Array<any> = [String.fromCharCode(122,111,111,109,101,100,0), String.fromCharCode(109,100,97,116,0)];
       let icont: Array<any> = [203, 788, 582];
      gemfile1 = zhuboe.size == 74;
    navigator.navigate("付费VIP");
    
    
    
    
    
    
    
    
  }, [])

  const handleOnInvite = useCallback(() => {
       let logob = 0;
    let userG = 4.0;
    let slider9 = false;
    let plusb = true;
    let playlisth = String.fromCharCode(100,105,115,97,98,108,101,114,0);
    let yellowZ = String.fromCharCode(115,105,103,115,97,102,101,0);
    let utilso = 4;
    let kleviny = 5;
    let sellg = String.fromCharCode(108,111,99,97,108,105,122,97,116,105,111,110,115,0);
    let moviest: Array<any> = [String.fromCharCode(106,117,115,116,105,102,105,99,97,116,105,111,110,0), String.fromCharCode(109,105,115,115,0), String.fromCharCode(117,110,102,97,105,114,0)];
    let bridgeV = 3.0;
       let detailC = 0.0;
          let t_imagem = String.fromCharCode(105,99,99,112,0);
         detailC /= Math.max(3, parseFloat(`${t_imagem.length}`));
       let rightO = String.fromCharCode(105,114,114,101,108,101,118,97,110,116,0);
       let club8 = String.fromCharCode(102,114,111,110,116,0);
          let xvode = 4.0;
         club8 = `${rightO.length & parseInt(`${detailC}`)}`;
         xvode /= Math.max(3, 4);
      utilso <<= Math.min(2, Math.abs(1 ^ utilso));
   if (slider9) {
       let orientationt = String.fromCharCode(115,110,97,112,112,121,0);
       let fastR = String.fromCharCode(114,101,115,105,100,117,101,115,0);
       let bridgep = 1.0;
       let sell_ = String.fromCharCode(115,100,97,108,108,111,99,120,0);
       let membershipO = String.fromCharCode(99,108,101,97,114,115,0);
         membershipO += `${(orientationt == String.fromCharCode(66,0) ? fastR.length : orientationt.length)}`;
      for (let k = 0; k < 1; k++) {
         orientationt = `${membershipO.length}`;
      }
       let mbnative9 = 3.0;
       let goalj = 4.0;
      let whatsapp5 = 8686352 <= fastR.length;
      do {
         fastR = `${parseInt(`${bridgep}`)}`;
         if (whatsapp5) {
            break;
         }
      } while (whatsapp5 && (!fastR.endsWith(`${bridgep}`)));
         membershipO += "3";
      let disconnectedU = 8586915.0 >= goalj;
      do {
          let rewardvideon = 0;
          let ewardedt = 1.0;
         goalj -= parseFloat(`${3 & parseInt(`${mbnative9}`)}`);
         rewardvideon *= 2 / (Math.max(rewardvideon, 3));
         ewardedt /= Math.max(1, parseFloat(`${3 | rewardvideon}`));
         if (disconnectedU) {
            break;
         }
      } while ((2 > (sell_.length / 2) && (parseFloat(`${sell_.length}`) * goalj) > 2.37) && disconnectedU);
      let teamL = 8901095 >= fastR.length;
      do {
         fastR = `${sell_.length + parseInt(`${bridgep}`)}`;
         if (teamL) {
            break;
         }
      } while ((!fastR.endsWith(`${goalj}`)) && teamL);
      if (orientationt.length < 5) {
         orientationt = `${orientationt.length * 1}`;
      }
          let chinau = false;
          let interstitial8 = 4.0;
          let sidec = true;
         sell_ += `${parseInt(`${goalj}`) % 1}`;
         chinau = !chinau;
         interstitial8 *= ((chinau ? 2 : 1));
         sidec = 57.24 < interstitial8 && sidec;
      for (let i = 0; i < 1; i++) {
          let hookX = String.fromCharCode(118,97,114,105,97,110,99,101,120,0);
          let combinedf = String.fromCharCode(103,101,110,101,114,97,116,105,110,103,0);
          let greenv = String.fromCharCode(99,111,109,98,105,110,97,116,111,114,0);
         fastR = `${parseInt(`${mbnative9}`) >> (Math.min(combinedf.length, 3))}`;
         hookX = `${hookX.length & 2}`;
         combinedf = "1";
         greenv += `${hookX.length}`;
      }
       let firebaseX = String.fromCharCode(116,111,117,99,104,0);
      while (bridgep >= mbnative9) {
         mbnative9 *= parseFloat(`${orientationt.length - fastR.length}`);
         break;
      }
      if ((5 & firebaseX.length) >= 2) {
         firebaseX = `${1 ^ sell_.length}`;
      }
      let promotionW = sell_ == String.fromCharCode(52,103,100,104,100,101,111,0);
      do {
          let twitterY = 0.0;
          let middlej = 1;
          let viewsk = String.fromCharCode(112,114,101,101,110,99,111,100,101,0);
          let editE = String.fromCharCode(99,111,110,118,101,114,116,0);
         sell_ += `${sell_.length & parseInt(`${mbnative9}`)}`;
         twitterY -= parseInt(`${twitterY}`);
         middlej += parseInt(`${twitterY}`) & middlej;
         viewsk = `${parseInt(`${twitterY}`) * editE.length}`;
         editE += `${editE.length}`;
         if (promotionW) {
            break;
         }
      } while (promotionW && (orientationt.length >= sell_.length));
         mbnative9 += parseFloat(`${2}`);
      yellowZ = `${parseInt(`${userG}`)}`;
   }
   for (let j = 0; j < 2; j++) {
       let uploadU = 4.0;
      if (1.21 < (uploadU + 2.29) && 1.7 < (uploadU / 2.29)) {
         uploadU *= parseInt(`${uploadU}`);
      }
      if ((uploadU / (Math.max(6, uploadU))) < 3.6 || (3.6 * uploadU) < 1.18) {
         uploadU *= parseInt(`${uploadU}`) ^ parseInt(`${uploadU}`);
      }
       let linkP = String.fromCharCode(99,108,101,97,110,117,112,0);
      userG *= playlisth.length;
   }
      sellg += `${(String.fromCharCode(88,0) == playlisth ? playlisth.length : parseInt(`${userG}`))}`;
      userG -= 3 * moviest.length;
      sellg = `${((slider9 ? 1 : 2) % (Math.max(sellg.length, 4)))}`;
   for (let u = 0; u < 3; u++) {
      userG -= utilso * moviest.length;
   }
       let modalr = String.fromCharCode(98,101,99,97,109,101,0);
       let taiwanz = String.fromCharCode(114,111,116,97,116,105,111,110,0);
      if (modalr == taiwanz) {
         taiwanz += `${modalr.length}`;
      }
         modalr = "1";
      plusb = parseInt(`${userG}`) > modalr.length;
   if ((userG / (Math.max(1.26, 4))) >= 3.56) {
       let splashY: Array<any> = [String.fromCharCode(115,116,101,108,108,97,114,0), String.fromCharCode(109,111,100,101,109,0), String.fromCharCode(98,97,116,99,104,101,115,0)];
         splashY.push(splashY.length / (Math.max(splashY.length, 6)));
         splashY.push(splashY.length);
         splashY.push(splashY.length >> (Math.min(splashY.length, 1)));
      userG -= 2;
   }
      utilso %= Math.max(yellowZ.length, 3);
      userG /= Math.max(moviest.length & 3, 2);
   let langt = yellowZ == String.fromCharCode(49,53,107,0);
   do {
      yellowZ += "2";
      if (langt) {
         break;
      }
   } while (langt && ((kleviny / (Math.max(yellowZ.length, 4))) >= 4 && 2 >= (kleviny / (Math.max(4, 5)))));
   if (3.48 < bridgeV) {
      bridgeV += logob ^ 2;
   }
       let sideB: Map<any, any> = new Map([[String.fromCharCode(117,110,101,110,99,114,121,112,116,101,100,0),false ], [String.fromCharCode(103,101,111,116,97,103,115,0),false ], [String.fromCharCode(101,120,99,101,101,100,105,110,103,0),true ]]);
      while ((4 | sideB.size) <= 1 || 4 <= (sideB.size | sideB.size)) {
          let tailc = 3;
         sideB = new Map([[`${sideB.size}`, sideB.size]]);
         tailc *= tailc;
         break;
      }
      let canvasl = sideB.size <= 7804938;
      do {
         sideB.set(`${sideB.size}`, sideB.size % (Math.max(1, 9)));
         if (canvasl) {
            break;
         }
      } while (((sideB.size >> (Math.min(Math.abs(sideB.size), 5))) > 3 || (sideB.size >> (Math.min(1, Math.abs(sideB.size)))) > 3) && canvasl);
          let ajaxd: Array<any> = [693, 64, 100];
         sideB = new Map([[`${sideB.size}`, 3 ^ sideB.size]]);
         ajaxd.push(ajaxd.length << (Math.min(Math.abs(1), 1)));
      plusb = 68 < kleviny;
       let macaus = String.fromCharCode(111,118,101,114,119,114,105,116,101,0);
       let watch1 = 5.0;
       let progressO = String.fromCharCode(115,121,110,99,104,114,111,110,105,122,101,0);
          let minivodR = 0;
          let configM = true;
          let xvod9 = String.fromCharCode(114,111,117,110,100,101,100,0);
         progressO = "2";
         minivodR <<= Math.min(4, Math.abs(1));
         configM = 37 == minivodR;
         xvod9 = `${(xvod9 == String.fromCharCode(74,0) ? xvod9.length : (configM ? 3 : 3))}`;
         watch1 *= parseFloat(`${parseInt(`${watch1}`) << (Math.min(Math.abs(1), 1))}`);
      while (4 <= (macaus.length - parseInt(`${watch1}`)) && 1 <= (macaus.length + 4)) {
         watch1 -= parseFloat(`${parseInt(`${watch1}`)}`);
         break;
      }
          let reportT = 0.0;
          let dangerS: Map<any, any> = new Map([[String.fromCharCode(109,98,99,110,116,0),253], [String.fromCharCode(101,122,105,101,114,0),460], [String.fromCharCode(115,117,112,112,111,114,116,97,98,108,101,0),810]]);
         watch1 *= (parseFloat(`${String.fromCharCode(108,0) == progressO ? dangerS.size : progressO.length}`));
         reportT += parseFloat(`${2}`);
         dangerS = new Map([[`${reportT}`, parseInt(`${reportT}`) ^ parseInt(`${reportT}`)]]);
         progressO = `${1 * macaus.length}`;
         watch1 *= parseFloat(`${progressO.length + macaus.length}`);
      while ((watch1 - 5.52) >= 5.86 && (parseInt(`${watch1}`) - 3) >= 5) {
         progressO += `${(String.fromCharCode(119,0) == macaus ? macaus.length : parseInt(`${watch1}`))}`;
         break;
      }
      if (3.87 >= watch1) {
         watch1 /= Math.max((parseFloat(`${macaus == String.fromCharCode(115,0) ? macaus.length : parseInt(`${watch1}`)}`)), 1);
      }
      while (!macaus.startsWith(`${watch1}`)) {
          let reactnativejs2 = String.fromCharCode(108,122,115,115,0);
          let termsE = 4.0;
          let humidityp = String.fromCharCode(97,115,110,0);
         watch1 -= parseFloat(`${reactnativejs2.length / (Math.max(2, 4))}`);
         reactnativejs2 = "2";
         termsE -= humidityp.length % 1;
         humidityp += `${parseInt(`${termsE}`)}`;
         break;
      }
      utilso += 3;
      moviest = [((slider9 ? 1 : 3) & (plusb ? 1 : 2))];

    onClose()

       let animationI = String.fromCharCode(115,116,101,112,115,0);
          let mbjscommon4 = 2;
          let r_countS = 5.0;
         animationI += `${2 + parseInt(`${r_countS}`)}`;
         mbjscommon4 |= 3;
         r_countS -= mbjscommon4;
         animationI += `${animationI.length}`;
         animationI += "3";
      userG -= 2 ^ playlisth.length;
      userG /= Math.max(parseInt(`${bridgeV}`) % 3, 3);
   if (!slider9) {
       let streamingk = 1.0;
       let detaild: Array<any> = [540, 806, 408];
       let y_unlockg = String.fromCharCode(100,101,108,101,116,101,100,0);
       let tempD = 4.0;
       let auto_y4m = 4.0;
         tempD += parseFloat(`${parseInt(`${auto_y4m}`) >> (Math.min(2, Math.abs(parseInt(`${streamingk}`))))}`);
          let privacyO = String.fromCharCode(100,109,97,98,117,102,0);
         tempD *= parseFloat(`${1}`);
         privacyO += `${2 << (Math.min(4, privacyO.length))}`;
      if (!y_unlockg.includes(`${auto_y4m}`)) {
         y_unlockg += `${y_unlockg.length}`;
      }
      while (!detaild.includes(auto_y4m)) {
          let configureK = String.fromCharCode(117,110,114,101,99,111,103,110,105,122,101,100,0);
          let sourceq = String.fromCharCode(115,105,103,0);
         detaild.push(sourceq.length >> (Math.min(5, configureK.length)));
         break;
      }
         detaild.push(parseInt(`${tempD}`) | parseInt(`${streamingk}`));
      for (let n = 0; n < 3; n++) {
          let screenu: Array<any> = [223, 335, 411];
          let playercommonw = 2;
         detaild = [1 >> (Math.min(Math.abs(parseInt(`${auto_y4m}`)), 5))];
         screenu = [screenu.length * 3];
         playercommonw |= playercommonw << (Math.min(screenu.length, 2));
      }
         y_unlockg = `${parseInt(`${tempD}`) / (Math.max(10, parseInt(`${auto_y4m}`)))}`;
      let related9 = tempD >= 6001082.0;
      do {
         tempD /= Math.max(2, parseFloat(`${parseInt(`${streamingk}`) * 1}`));
         if (related9) {
            break;
         }
      } while ((2.1 <= (tempD - auto_y4m) || (auto_y4m - 2.1) <= 3.9) && related9);
      if (3.68 >= (1.18 + auto_y4m)) {
         y_unlockg += `${3 >> (Math.min(1, y_unlockg.length))}`;
      }
          let reactnativejsE = String.fromCharCode(114,101,116,114,105,101,114,0);
         auto_y4m *= parseFloat(`${reactnativejsE.length}`);
      let flipperL = streamingk <= 7179622.0;
      do {
          let hovero = 1;
          let awayb = String.fromCharCode(112,114,111,118,105,100,101,100,0);
          let contextP = 5;
          let private_da = 1;
         streamingk *= contextP * private_da;
         hovero *= hovero / (Math.max(awayb.length, 9));
         awayb = `${2 * hovero}`;
         contextP >>= Math.min(1, awayb.length);
         private_da *= hovero;
         if (flipperL) {
            break;
         }
      } while ((!y_unlockg.startsWith(`${streamingk}`)) && flipperL);
         detaild.push(parseInt(`${tempD}`) + 3);
      let pangleQ = 8288388.0 >= streamingk;
      do {
          let contextq = 1.0;
          let saveA = 1.0;
          let formc: Array<any> = [245, 946, 842];
          let switch_i4I = String.fromCharCode(119,114,105,116,97,98,108,101,0);
         streamingk -= 1 * formc.length;
         contextq *= (switch_i4I == String.fromCharCode(89,0) ? parseInt(`${contextq}`) : switch_i4I.length);
         saveA -= parseFloat(`${1 >> (Math.min(2, switch_i4I.length))}`);
         formc = [switch_i4I.length];
         if (pangleQ) {
            break;
         }
      } while (pangleQ && (3.14 >= (2.31 - streamingk) || (parseInt(`${streamingk}`) - 1) >= 2));
          let mbnativeadvancedc = String.fromCharCode(97,99,99,101,115,115,111,114,121,0);
          let connectionW = String.fromCharCode(119,111,114,107,97,114,111,117,110,100,0);
          let about8 = false;
         tempD *= parseFloat(`${2}`);
         mbnativeadvancedc += `${mbnativeadvancedc.length << (Math.min(1, connectionW.length))}`;
         connectionW += `${mbnativeadvancedc.length}`;
         about8 = (42 < ((!about8 ? connectionW.length : 42) ^ connectionW.length));
       let delegate_gvj = String.fromCharCode(115,99,116,112,117,116,105,108,0);
       let logoB = String.fromCharCode(111,110,97,118,99,100,97,116,97,0);
      slider9 = utilso > 88 || auto_y4m > 93.75;
   }
       let stationsO = String.fromCharCode(117,115,101,114,110,97,109,101,0);
       let frame_568: Map<any, any> = new Map([[String.fromCharCode(102,116,118,109,111,0),String.fromCharCode(101,116,104,101,114,0)], [String.fromCharCode(115,111,114,116,0),String.fromCharCode(114,101,99,111,118,101,114,0)], [String.fromCharCode(105,97,100,115,116,120,0),String.fromCharCode(114,101,112,114,101,115,101,110,116,97,98,108,101,0)]]);
         frame_568 = new Map([[`${frame_568.size}`, frame_568.size & stationsO.length]]);
      if (2 == (3 - stationsO.length)) {
          let rightP = 4.0;
          let resendn = 0.0;
          let adultj: Array<any> = [337, 312];
         stationsO = `${adultj.length}`;
         rightP /= Math.max(1, 3 * parseInt(`${rightP}`));
         resendn /= Math.max(4, parseFloat(`${1}`));
         adultj = [2 - parseInt(`${rightP}`)];
      }
         stationsO += `${stationsO.length >> (Math.min(5, Math.abs(frame_568.size)))}`;
       let full8 = String.fromCharCode(115,117,99,99,101,115,115,111,114,0);
         stationsO = `${3 / (Math.max(2, frame_568.size))}`;
      for (let d = 0; d < 2; d++) {
         full8 = `${stationsO.length}`;
      }
      slider9 = frame_568.size < logob;
   while (kleviny < 4) {
      userG *= 3;
      break;
   }
   for (let l = 0; l < 2; l++) {
      bridgeV *= 2 ^ utilso;
   }
   while ((utilso >> (Math.min(Math.abs(logob), 5))) > 2) {
      logob -= 1 >> (Math.min(2, moviest.length));
      break;
   }
      sellg = `${yellowZ.length + 3}`;
      userG *= playlisth.length | 3;
       let membershipj: Array<any> = [179, 372, 199];
      if ((membershipj.length - 5) < 5) {
          let becomeR = 4.0;
          let fieldX = String.fromCharCode(99,97,112,112,101,100,0);
         membershipj.push(membershipj.length * 1);
         becomeR /= Math.max(parseFloat(`${parseInt(`${becomeR}`) + fieldX.length}`), 1);
         fieldX += `${fieldX.length}`;
      }
         membershipj.push(membershipj.length % (Math.max(2, 9)));
      let reducern = 8403081 <= membershipj.length;
      do {
         membershipj = [membershipj.length >> (Math.min(membershipj.length, 3))];
         if (reducern) {
            break;
         }
      } while ((membershipj.length == membershipj.length) && reducern);
      utilso <<= Math.min(Math.abs(kleviny), 2);
      playlisth += `${((plusb ? 1 : 5) << (Math.min(Math.abs(3), 5)))}`;
   while (bridgeV <= 3.84) {
      slider9 = 23.55 <= bridgeV;
      break;
   }
   for (let o = 0; o < 1; o++) {
      utilso ^= kleviny;
   }
      yellowZ += `${parseInt(`${bridgeV}`)}`;
       let backgroundu: Map<any, any> = new Map([[String.fromCharCode(99,111,115,112,105,0),653], [String.fromCharCode(118,116,97,115,107,113,117,101,117,101,0),740], [String.fromCharCode(102,105,108,116,101,114,115,0),281]]);
       let transferP = 0.0;
      let a_managerh = transferP >= 7439049.0;
      do {
          let vietnami = 1;
          let uploadJ: Array<any> = [492, 883];
          let successX = String.fromCharCode(98,105,110,107,98,0);
          let assistc: Map<any, any> = new Map([[String.fromCharCode(109,117,108,116,105,112,108,101,0),916], [String.fromCharCode(110,116,114,111,0),216], [String.fromCharCode(100,111,116,116,101,100,0),408]]);
          let ball5: Array<any> = [623, 486, 297];
         transferP *= parseFloat(`${successX.length}`);
         vietnami -= 1;
         uploadJ.push(3);
         successX += `${uploadJ.length}`;
         assistc = new Map([[`${uploadJ.length}`, uploadJ.length]]);
         ball5 = [ball5.length << (Math.min(Math.abs(1), 3))];
         if (a_managerh) {
            break;
         }
      } while (a_managerh && (Array.from(backgroundu.values()).includes(transferP)));
      for (let x = 0; x < 1; x++) {
         transferP += parseFloat(`${backgroundu.size << (Math.min(Math.abs(1), 4))}`);
      }
         backgroundu.set(`${transferP}`, backgroundu.size * parseInt(`${transferP}`));
         transferP /= Math.max(2, parseFloat(`${1 - backgroundu.size}`));
         backgroundu = new Map([[`${backgroundu.size}`, parseInt(`${transferP}`) >> (Math.min(Math.abs(backgroundu.size), 5))]]);
      for (let q = 0; q < 3; q++) {
         backgroundu.set(`${transferP}`, parseInt(`${transferP}`));
      }
      userG /= Math.max(4, playlisth.length - 1);
   if ((3.61 * userG) == 3.97) {
       let spinnerG = String.fromCharCode(112,108,97,110,0);
       let episodeP = 5.0;
      let reducerh = 6268303 <= spinnerG.length;
      do {
          let pingi = false;
          let networkY = String.fromCharCode(107,97,110,110,97,0);
         spinnerG = `${spinnerG.length}`;
         pingi = pingi && networkY.length > 26;
         networkY += `${(String.fromCharCode(113,0) == networkY ? networkY.length : (pingi ? 5 : 3))}`;
         if (reducerh) {
            break;
         }
      } while ((!spinnerG.endsWith(`${episodeP}`)) && reducerh);
          let area8: Array<any> = [713, 378];
         episodeP *= 3;
         area8.push(area8.length);
         spinnerG = `${spinnerG.length}`;
      let rewardvideoJ = spinnerG.length >= 7547963;
      do {
          let circle4 = false;
          let fullK = String.fromCharCode(114,101,99,111,110,105,110,116,114,97,0);
          let profileq: Map<any, any> = new Map([[String.fromCharCode(99,111,117,110,116,105,110,103,0),112], [String.fromCharCode(109,118,99,111,117,110,116,0),889]]);
          let update_pyW = String.fromCharCode(97,114,103,117,109,101,110,116,115,0);
         spinnerG += `${(parseInt(`${episodeP}`) * (circle4 ? 2 : 5))}`;
         circle4 = 95 <= update_pyW.length;
         fullK = `${fullK.length}`;
         profileq.set(fullK, 3 << (Math.min(1, Math.abs(profileq.size))));
         update_pyW += `${update_pyW.length}`;
         if (rewardvideoJ) {
            break;
         }
      } while (rewardvideoJ && (3.88 > episodeP));
      if ((spinnerG.length % 1) >= 1 || (1.61 + episodeP) >= 5.6) {
         episodeP += spinnerG.length;
      }
          let umengR = true;
          let networkU = 1;
          let carouselS = 1.0;
         episodeP *= 3;
         umengR = 17.32 < carouselS && umengR;
         networkU |= parseInt(`${carouselS}`) + 3;
      userG += 1;
   }
    navigator.navigate('邀请');
  }, [])


  return (

    <VipPrivilegeModal
      titleText={"VIP升级权益"}
      benefitsTextsArray={['高清体育赛事  零时差感受赛场激情', '海量福利视频 你想要的这里都有', '高清画质 无广告观影体验']}
      imageGradientColors={['#D80E0E00', '#E80B0B']}
      contentGradientColors={['#E80B0B', '#740000']}
      coverBackground={sportBg}
      coverImage={commonModels}
      onPurchase={handleOnPurchase}
      onInvite={handleOnInvite}
      onClose={onClose}
      showBlur={showBlur}
      showCondition={showCondition}
      showDarkBackdrop={true}
    />

  )
}