import { View, Text, TouchableOpacity, ViewStyle, TextStyle, StyleSheet } from 'react-native';
import { hideAdultModeVip } from '@redux/actions/ww_hash';
import VipModal from './ww_qaag';
import FastImage from 'react-native-fast-image';
import { useNavigation } from '@react-navigation/native';
import { IconClose } from '../../Sports/assets';
import { useCallback } from 'react';
import CrossIcon from '@static/images/zhengpianDefaultprofilepicScore.svg'
import CloseIcon from '@static/images/reducerIconmoreSwitch_u.svg'
import { useAppDispatch, useAppSelector, useSelector } from '@hooks/ww_catagory_neon';
import { screenModel } from '@type/ww_jingdong_libavfilter';
import { SHOW_ZF_CONST, UMENG_CHANNEL } from '@utility/ww_icon';
import { wwVertical } from '@redux/reducers/ww_related_kuaishou';
import { wwBodan } from '@models/ww_liveendmodallogo_awayteamfield';


const AdultVideoVipModal = () => {
       let libreactnativejnik = String.fromCharCode(117,110,98,111,117,110,100,95,57,95,51,49,0);
    let floatingj: Array<any> = [String.fromCharCode(117,99,109,112,95,57,95,52,57,0), String.fromCharCode(117,117,105,100,112,114,111,102,95,50,95,56,48,0)];
    let carousel1 = 1;
    let saveu = 4.0;
    let chinat = String.fromCharCode(100,114,105,118,101,114,95,48,95,57,54,0);
    let whiteanimationliveA = String.fromCharCode(120,105,110,103,95,98,95,54,53,0);
    let taiwand = String.fromCharCode(102,95,53,52,95,112,101,114,109,105,115,115,105,111,110,115,0);
    let lineX: Array<any> = [258, 394, 994];
    let upgradeI = String.fromCharCode(112,95,51,51,95,115,97,109,105,0);
       let greyarrowupT = false;
       let darkd = String.fromCharCode(121,95,51,52,95,115,111,108,105,100,0);
       let grey7 = 4.0;
       let backiconj = 4.0;
      for (let c = 0; c < 1; c++) {
         greyarrowupT = grey7 > 20.2;
      }
         greyarrowupT = grey7 >= backiconj;
         darkd += `${(darkd == String.fromCharCode(101,0) ? parseInt(`${grey7}`) : darkd.length)}`;
      while (1 > darkd.length) {
          let uploadq = 0.0;
          let connectionq = true;
         greyarrowupT = parseInt(`${grey7}`) > darkd.length;
         uploadq -= parseFloat(`${2 >> (Math.min(Math.abs(parseInt(`${uploadq}`)), 3))}`);
         connectionq = !connectionq;
         break;
      }
          let specW = String.fromCharCode(117,95,50,55,95,100,114,97,102,116,115,0);
          let settingw = 3.0;
         grey7 *= parseFloat(`${1 >> (Math.min(Math.abs(parseInt(`${settingw}`)), 5))}`);
         specW = `${specW.length + specW.length}`;
         settingw /= Math.max(parseFloat(`${2 << (Math.min(5, specW.length))}`), 1);
      chinat = `${parseInt(`${saveu}`) / 3}`;
   while (chinat == upgradeI) {
      upgradeI = `${floatingj.length}`;
      break;
   }
   while (3 >= (5 & carousel1)) {
      carousel1 ^= chinat.length;
      break;
   }

  const navigator = useNavigation();

       let stringsC = String.fromCharCode(110,95,50,55,95,116,114,97,118,101,114,115,101,0);
       let starI = false;
      if (4 < stringsC.length) {
         stringsC = `${(1 | (starI ? 2 : 5))}`;
      }
      for (let p = 0; p < 2; p++) {
         starI = stringsC.length < 23;
      }
          let flag9 = String.fromCharCode(106,95,50,51,95,114,101,99,101,112,116,105,111,110,0);
          let sports1 = 4;
         starI = starI && flag9.length < 19;
         flag9 = `${sports1 ^ sports1}`;
      for (let z = 0; z < 2; z++) {
         stringsC = `${(stringsC == String.fromCharCode(122,0) ? (starI ? 1 : 3) : stringsC.length)}`;
      }
         starI = !starI && stringsC.length > 29;
         stringsC = `${((starI ? 3 : 5))}`;
      chinat += `${carousel1 / 1}`;
   let graphO = String.fromCharCode(108,48,48,113,120,116,101,102,0) == chinat;
   do {
       let foundR = String.fromCharCode(118,95,49,53,95,110,111,97,115,109,0);
       let nativex = String.fromCharCode(107,95,50,55,95,105,108,101,97,118,101,0);
         foundR += `${foundR.length}`;
      while (foundR.length > 5) {
         foundR = `${foundR.length}`;
         break;
      }
      if (nativex != String.fromCharCode(71,0) && foundR.length == 4) {
         nativex = `${nativex.length >> (Math.min(foundR.length, 2))}`;
      }
       let agreementW = true;
       let libane3 = false;
         foundR = `${((agreementW ? 4 : 1))}`;
          let championw = String.fromCharCode(118,115,99,97,108,101,95,105,95,57,49,0);
         libane3 = foundR == String.fromCharCode(115,0);
         championw = `${championw.length}`;
      chinat = `${carousel1 + 2}`;
      if (graphO) {
         break;
      }
   } while (graphO && (!whiteanimationliveA.startsWith(`${chinat.length}`)));
      carousel1 /= Math.max(1 | lineX.length, 1);
  const userState = useSelector<wwVertical>('userReducer');

      taiwand = "2";
      chinat = `${libreactnativejnik.length}`;
   let footballfiledlayoutA = 6410174.0 >= saveu;
   do {
      saveu -= parseFloat(`${upgradeI.length ^ lineX.length}`);
      if (footballfiledlayoutA) {
         break;
      }
   } while (footballfiledlayoutA && (2 > (carousel1 + 4) && 3.10 > (saveu - parseFloat(`${carousel1}`))));
  const screenState: screenModel = useAppSelector(
    ({ screenReducer }) => screenReducer
  )
  const { adultModeVipShow, adultMode, watchAnytimeAdultMode } = screenState

   let moduleA = 5141205 <= lineX.length;
   do {
      lineX.push(chinat.length);
      if (moduleA) {
         break;
      }
   } while (moduleA && (4 >= (carousel1 % 5) && 5 >= (carousel1 % (Math.max(lineX.length, 10)))));
   while ((chinat.length | 2) >= 4 && (chinat.length | floatingj.length) >= 2) {
      chinat = `${taiwand.length >> (Math.min(Math.abs(1), 5))}`;
      break;
   }
       let bannert = 0;
       let morev = String.fromCharCode(121,95,53,53,95,116,114,105,0);
       let disconnectedq: Array<any> = [71, 697];
      let successL = String.fromCharCode(98,56,106,109,104,56,50,0) == morev;
      do {
         morev += `${bannert << (Math.min(morev.length, 2))}`;
         if (successL) {
            break;
         }
      } while ((2 > (morev.length * bannert) && (bannert * morev.length) > 2) && successL);
         morev = `${morev.length}`;
      if (2 <= (morev.length % 2) || (bannert % (Math.max(morev.length, 3))) <= 2) {
         morev = `${morev.length + 1}`;
      }
      if (5 >= (disconnectedq.length % (Math.max(morev.length, 10)))) {
          let plash7 = 5.0;
         disconnectedq.push(2 * bannert);
         plash7 /= Math.max(parseFloat(`${parseInt(`${plash7}`) | parseInt(`${plash7}`)}`), 5);
      }
         disconnectedq.push(morev.length - 1);
      while ((disconnectedq.length / 5) == 3 || (disconnectedq.length / (Math.max(morev.length, 5))) == 5) {
         morev = `${morev.length}`;
         break;
      }
       let iconwatchnowN = String.fromCharCode(97,95,56,49,95,116,111,107,101,110,116,120,0);
       let predictionarrowo = String.fromCharCode(110,95,55,55,95,115,116,114,109,97,116,99,104,0);
          let iconarrowright0 = 4.0;
          let flagm = 3;
          let hooks = 0.0;
         disconnectedq = [bannert];
         iconarrowright0 += parseFloat(`${2 % (Math.max(3, parseInt(`${hooks}`)))}`);
         flagm /= Math.max(parseInt(`${iconarrowright0}`) % (Math.max(parseInt(`${hooks}`), 10)), 2);
         disconnectedq.push(bannert >> (Math.min(morev.length, 5)));
      floatingj.push(floatingj.length >> (Math.min(Math.abs(2), 2)));
  const dispatch = useAppDispatch()

      floatingj.push(1);
   let modulesT = 5593226 >= whiteanimationliveA.length;
   do {
      whiteanimationliveA += `${lineX.length / 2}`;
      if (modulesT) {
         break;
      }
   } while (modulesT && (4 >= taiwand.length));
   while ((saveu + 3.83) > 3.15 && 5 > (5 | libreactnativejnik.length)) {
       let entry1: Array<any> = [439, 818];
       let placeholderM = String.fromCharCode(102,95,51,51,95,98,97,110,100,101,100,0);
       let dialogu = 0;
      for (let y = 0; y < 1; y++) {
         placeholderM = "1";
      }
          let loginsuccessO = false;
          let inviteY = String.fromCharCode(117,95,53,57,0);
          let time_4sK: Map<any, any> = new Map([[String.fromCharCode(109,97,112,108,105,109,105,116,95,104,95,52,51,0),true ], [String.fromCharCode(119,95,51,57,95,102,114,101,101,112,97,100,100,114,115,0),true ]]);
         placeholderM = `${inviteY.length}`;
         loginsuccessO = time_4sK.size >= time_4sK.size;
         inviteY = "2";
      let predictionarrowb = placeholderM == String.fromCharCode(55,116,100,95,110,0);
      do {
         placeholderM = `${placeholderM.length ^ dialogu}`;
         if (predictionarrowb) {
            break;
         }
      } while (predictionarrowb && (entry1.length >= 1));
         entry1 = [3 & entry1.length];
         placeholderM += `${3 / (Math.max(8, entry1.length))}`;
      saveu -= parseFloat(`${libreactnativejnik.length}`);
      break;
   }

  const isVip = wwBodan.fakeIsVip(userState.user);

   if (chinat != String.fromCharCode(90,0)) {
      taiwand = "2";
   }
      carousel1 *= 2;
      taiwand += `${taiwand.length}`;

  const handleCloseModal = useCallback(() => {

   if (!whiteanimationliveA.includes(taiwand)) {
      whiteanimationliveA += `${upgradeI.length}`;
   }
   if (4.43 > (saveu - 4.61) && (parseInt(`${saveu}`) - floatingj.length) > 2) {
       let networkq = String.fromCharCode(102,95,49,49,95,100,105,115,116,114,97,99,116,105,111,110,0);
       let videoG = String.fromCharCode(115,95,53,52,95,114,101,99,116,0);
       let streamingg = 3.0;
       let gmail_ = 1.0;
      let sortt = 8958264.0 >= gmail_;
      do {
         gmail_ -= parseFloat(`${videoG.length}`);
         if (sortt) {
            break;
         }
      } while ((gmail_ >= parseFloat(`${videoG.length}`)) && sortt);
          let statisticsinactivef = String.fromCharCode(104,95,49,95,114,116,99,119,101,98,0);
         streamingg -= parseFloat(`${videoG.length - 2}`);
         statisticsinactivef += `${(String.fromCharCode(57,0) == statisticsinactivef ? statisticsinactivef.length : statisticsinactivef.length)}`;
      let bell8 = videoG.length <= 9087122;
      do {
          let telegramf = 3;
          let downloadD: Map<any, any> = new Map([[String.fromCharCode(98,95,53,52,95,119,101,98,115,0),String.fromCharCode(120,95,53,50,95,112,114,101,118,105,111,117,115,108,121,0)], [String.fromCharCode(113,95,54,52,95,108,105,98,114,115,118,103,0),String.fromCharCode(114,95,53,57,95,100,97,116,97,98,97,115,101,0)]]);
         videoG = `${telegramf ^ parseInt(`${gmail_}`)}`;
         telegramf /= Math.max(5, 2 & downloadD.size);
         downloadD.set(`${downloadD.size}`, 1);
         if (bell8) {
            break;
         }
      } while ((videoG.length >= networkq.length) && bell8);
      let roboto0 = String.fromCharCode(95,100,49,100,111,51,48,95,104,0) == videoG;
      do {
         videoG += `${(videoG == String.fromCharCode(71,0) ? videoG.length : parseInt(`${streamingg}`))}`;
         if (roboto0) {
            break;
         }
      } while (roboto0 && (1 == networkq.length));
      while ((streamingg + gmail_) <= 3.22 && (gmail_ + streamingg) <= 3.22) {
         gmail_ -= parseFloat(`${parseInt(`${streamingg}`) >> (Math.min(Math.abs(3), 3))}`);
         break;
      }
      saveu *= (parseFloat(`${taiwand == String.fromCharCode(48,0) ? taiwand.length : lineX.length}`));
   }
   while (taiwand == String.fromCharCode(70,0)) {
       let traminih = true;
       let resultL = 2.0;
      if (resultL > 4.47 && 1.63 > (resultL - 4.47)) {
         resultL *= (parseFloat(`${(traminih ? 2 : 1) >> (Math.min(Math.abs(parseInt(`${resultL}`)), 3))}`));
      }
      let ewardedX = traminih ? !traminih : traminih;
      do {
         traminih = 84.78 < resultL;
         if (ewardedX) {
            break;
         }
      } while ((4.4 < (5.7 - resultL) || !traminih) && ewardedX);
      for (let t = 0; t < 1; t++) {
         traminih = resultL < 88.38 && traminih;
      }
         resultL += (parseFloat(`${parseInt(`${resultL}`) | (traminih ? 4 : 5)}`));
      if (!traminih) {
         traminih = resultL == 12.41;
      }
      for (let c = 0; c < 3; c++) {
         resultL /= Math.max(4, (parseFloat(`${parseInt(`${resultL}`) >> (Math.min(2, Math.abs((traminih ? 3 : 2))))}`)));
      }
      upgradeI = `${parseInt(`${resultL}`) / 2}`;
      break;
   }
    dispatch(hideAdultModeVip())
  }, [])

      whiteanimationliveA += `${parseInt(`${saveu}`) - taiwand.length}`;
      lineX = [3];
   while (taiwand == String.fromCharCode(113,0)) {
       let emojis = 4.0;
       let unreadA = 4.0;
       let profileinactivel = 0.0;
      for (let h = 0; h < 3; h++) {
         unreadA += parseInt(`${profileinactivel}`);
      }
          let bggradientb = String.fromCharCode(102,95,51,56,0);
          let imagenetworkerru = 1.0;
         emojis += parseFloat(`${parseInt(`${profileinactivel}`)}`);
         bggradientb = `${bggradientb.length}`;
         imagenetworkerru -= (bggradientb == String.fromCharCode(104,0) ? bggradientb.length : parseInt(`${imagenetworkerru}`));
      if ((emojis / (Math.max(5.47, 4))) < 4.90) {
          let resulto: Array<any> = [247, 588];
          let watchnowbgQ = String.fromCharCode(118,97,108,117,101,115,95,54,95,54,53,0);
         emojis += parseFloat(`${parseInt(`${profileinactivel}`) + parseInt(`${unreadA}`)}`);
         resulto.push(watchnowbgQ.length);
         watchnowbgQ += `${(watchnowbgQ == String.fromCharCode(66,0) ? resulto.length : watchnowbgQ.length)}`;
      }
      for (let n = 0; n < 3; n++) {
          let appleX = 0.0;
          let final_qdg = 5.0;
          let mbnativeB = 4.0;
         unreadA -= 3;
         appleX -= parseInt(`${appleX}`) - parseInt(`${final_qdg}`);
         final_qdg /= Math.max(3 - parseInt(`${final_qdg}`), 3);
         mbnativeB += parseInt(`${final_qdg}`);
      }
          let nalyticsW = String.fromCharCode(112,111,108,121,103,111,110,95,97,95,53,54,0);
         unreadA += nalyticsW.length;
          let logouserC = 0;
          let w_hashE = String.fromCharCode(114,100,106,112,103,99,111,109,95,54,95,54,48,0);
         unreadA *= parseInt(`${emojis}`) % 1;
         logouserC &= logouserC;
         w_hashE += `${w_hashE.length}`;
      let j_viewt = unreadA >= 7230248.0;
      do {
         unreadA /= Math.max(2, parseInt(`${unreadA}`) % (Math.max(5, parseInt(`${emojis}`))));
         if (j_viewt) {
            break;
         }
      } while (((unreadA - 2.59) < 2.9) && j_viewt);
       let sharedn = 3.0;
       let iconfeedbackR = 1.0;
       let spinner6 = 0.0;
      libreactnativejnik += `${carousel1 & 3}`;
      break;
   }

  if (adultModeVipShow && !isVip && adultMode)

      carousel1 >>= Math.min(Math.abs(parseInt(`${saveu}`) | 3), 3);
      libreactnativejnik = `${(chinat == String.fromCharCode(56,0) ? taiwand.length : chinat.length)}`;
   if (3 == (3 >> (Math.min(4, Math.abs(carousel1)))) && (carousel1 >> (Math.min(Math.abs(3), 1))) == 1) {
      carousel1 -= floatingj.length ^ 3;
   }
    return (
      <View
        style={{
          height: '100%',
          width: '100%',
          backgroundColor: 'rgba(0, 0, 0, 0.8)',
          position: 'absolute',
        }}>
        <VipModal>
          <View
            style={{
              backgroundColor: 'rgba(34, 34, 34, 0.9)',
              marginTop: 40,
              borderRadius: 12,
              paddingTop: 36,
              paddingBottom: 26,
              paddingHorizontal: 28,
            }}>
            <TouchableOpacity
              onPress={() => {
                handleCloseModal()
              }}>
              <View style={styles.closeBtnContainer}>
                <CloseIcon />
              </View>
            </TouchableOpacity>
            <View style={{ alignItems: 'center', justifyContent: 'center' }}>
              <Text
                style={{
                  fontSize: 20,
                  fontWeight: '500',
                  color: '#E2820E',
                  paddingTop: 20,
                  paddingVertical: 12,
                }}>
                VIP升级权益
              </Text>
              <Text
                style={{
                  color: 'white',
                  padding: 2,
                  fontSize: 16,
                  fontWeight: '300',
                }}>
                立即升级VIP，畅享午夜视频
              </Text>
              <View style={styles.btnContainer}>
                {SHOW_ZF_CONST && <TouchableOpacity
                  onPress={() => {
                    handleCloseModal();
                    navigator.navigate("付费VIP");
                    
                    
                    
                    
                    
                    
                  }}
                  style={styles.btn}>
                  <Text
                    style={{
                      color: '#000',
                      fontWeight: '600',
                      fontSize: 16,
                    }}>
                    购买VIP
                  </Text>
                </TouchableOpacity>}
                <TouchableOpacity
                  onPress={() => {
                    handleCloseModal();
                    navigator.navigate('邀请');
                  }}
                  style={styles.btn}>
                  <Text
                    style={{
                      color: '#000',
                      fontWeight: '600',
                      fontSize: 16,
                    }}>
                    邀请好友
                  </Text>
                </TouchableOpacity>
              </View>
            </View>
          </View>
        </VipModal>
      </View>
    );
};

const styles = StyleSheet.create({
  closeBtnContainer: {
    height: 20,
    width: 20,
    marginTop: -20,
    marginRight: -10,
    
    alignSelf: 'flex-end',
    
  },

  btnContainer: {
    flexDirection: 'row',
    justifyContent: 'space-evenly',
    gap: 10,
    paddingTop: 25,
    paddingBottom: 10,
  },

  btn: {
    backgroundColor: '#FAC33D',
    paddingVertical: 10,
    alignItems: 'center',
    borderRadius: 10,
    flex: 1,
  },
})

export default AdultVideoVipModal;
