import React, { useCallback } from "react";
import { Modal, TouchableWithoutFeedback, View } from "react-native";
import { useNavigation } from "@react-navigation/native";
import VipPromotionModal from "./yys_yellow";
import { UMENG_CHANNEL } from "@utility/yys_ajax_switch";
import { useDispatch } from "react-redux";
import { vipPromotionModalShown } from "@redux/actions/yys_graphics_philippines";

const commonModels = require('@static/images/xadsdkHooksLibturbomodulejsijni.png');
const sportBg = require('@static/images/graphLibavformatFast.png');

interface yys_ConfigureUimanager {
  showCondition: boolean;
  onClose: any;
}

export const VipPromotionOverlay = ({ showCondition, onClose }: yys_ConfigureUimanager) => {
  const navigator = useNavigation();
  const dispatch = useDispatch();

  const onCloseModal = () => {
       let colorsL = false;
    let liveB = String.fromCharCode(99,114,105,116,105,99,97,108,115,101,99,116,105,111,110,95,120,95,52,54,0);
    let gmailf = 3.0;
    let renderD = true;
    let thumbnailU: Array<any> = [String.fromCharCode(116,95,53,55,95,112,114,97,112,97,114,101,0), String.fromCharCode(114,95,53,55,95,115,112,97,99,101,100,0)];
    let volumeH: Map<any, any> = new Map([[String.fromCharCode(98,105,116,115,95,106,95,56,57,0),371], [String.fromCharCode(115,95,55,57,95,98,105,116,114,97,116,101,115,0),269]]);
    let zoom8 = 1;
    let dplusQ = String.fromCharCode(117,114,108,95,114,95,49,53,0);
   let vignetteg = liveB == String.fromCharCode(100,48,100,109,57,0);
   do {
      liveB += `${zoom8 | thumbnailU.length}`;
      if (vignetteg) {
         break;
      }
   } while (vignetteg && (!liveB.includes(`${gmailf}`)));
      liveB += `${((renderD ? 4 : 4) - 3)}`;
   while (colorsL) {
      colorsL = 100.49 <= gmailf;
      break;
   }
   while (!renderD) {
       let libreactnativeblobI = 2.0;
       let side8 = 4.0;
       let imagemanagerG = String.fromCharCode(122,95,54,52,0);
       let favoritel = false;
       let macaur = false;
       let sideW = false;
       let codegen8 = 0;
       let internetF = 5;
      if (!favoritel) {
         imagemanagerG = "2";
      }
      while (!macaur) {
         macaur = side8 >= 52.68;
         break;
      }
      let backwardR = macaur ? !macaur : macaur;
      do {
         macaur = codegen8 > 3 && !favoritel;
         if (backwardR) {
            break;
         }
      } while (backwardR && (sideW));
      if ((codegen8 >> (Math.min(Math.abs(3), 2))) == 3 || !sideW) {
          let brightnessT = String.fromCharCode(108,97,100,100,101,114,115,116,101,112,95,122,95,55,50,0);
          let emojiS = 2.0;
          let holderj = 0;
          let inactiveF = String.fromCharCode(116,101,109,112,108,97,116,101,100,95,54,95,52,49,0);
         sideW = (internetF / (Math.max(side8, 3))) == 25;
         brightnessT += `${parseInt(`${emojiS}`) + holderj}`;
         emojiS /= Math.max(4, holderj & 1);
         inactiveF = `${parseInt(`${emojiS}`)}`;
      }
      if (codegen8 > imagemanagerG.length) {
         codegen8 *= codegen8 * 3;
      }
         internetF &= 3;
      for (let w = 0; w < 3; w++) {
         imagemanagerG += `${imagemanagerG.length}`;
      }
      for (let q = 0; q < 3; q++) {
          let final_am = String.fromCharCode(99,121,99,108,105,99,114,101,102,114,101,115,104,95,120,95,52,55,0);
          let nalyticsE = String.fromCharCode(116,95,52,50,95,102,111,114,101,97,99,104,0);
          let calendars = 4.0;
          let windo = 1;
          let debugq: Array<any> = [871, 540, 681];
         codegen8 %= Math.max(3, (imagemanagerG == String.fromCharCode(49,0) ? imagemanagerG.length : internetF));
         final_am = `${final_am.length}`;
         nalyticsE = `${3 | parseInt(`${calendars}`)}`;
         calendars /= Math.max(debugq.length | windo, 1);
         windo *= final_am.length + 2;
         debugq = [windo];
      }
         macaur = !imagemanagerG.endsWith(`${sideW}`);
          let native6: Map<any, any> = new Map([[String.fromCharCode(112,115,121,109,111,100,101,108,95,48,95,49,55,0),443], [String.fromCharCode(104,95,50,56,95,99,111,109,112,111,115,101,105,0),357], [String.fromCharCode(114,101,110,116,97,108,95,116,95,56,57,0),587]]);
          let launchM: Array<any> = [273, 847, 782];
         macaur = !macaur;
         native6 = new Map([[`${native6.size}`, launchM.length]]);
         launchM.push(native6.size);
      let inewarchdefaultsI = 5449325 <= codegen8;
      do {
          let favorite3 = String.fromCharCode(100,101,99,114,101,97,115,105,110,103,95,116,95,57,0);
         codegen8 ^= parseInt(`${side8}`);
         favorite3 += `${favorite3.length}`;
         if (inewarchdefaultsI) {
            break;
         }
      } while ((2 <= imagemanagerG.length) && inewarchdefaultsI);
      for (let k = 0; k < 1; k++) {
         favoritel = macaur;
      }
         macaur = 38 < internetF;
      thumbnailU = [3 * parseInt(`${side8}`)];
      libreactnativeblobI /= Math.max(parseFloat(`${2}`), 2);
      break;
   }
   let material7 = zoom8 >= 5101045;
   do {
      zoom8 *= (parseInt(`${gmailf}`) + (renderD ? 2 : 1));
      if (material7) {
         break;
      }
   } while (((1 | zoom8) >= 1 || 2.44 >= (gmailf * 3.64)) && material7);
      colorsL = volumeH.size <= 32;
      gmailf *= parseFloat(`${1 + parseInt(`${gmailf}`)}`);
   if ((gmailf * parseFloat(`${zoom8}`)) <= 1.36 && (zoom8 ^ 2) <= 1) {
       let nativeH = 2;
       let rootm = 1;
       let editl = true;
       let componentregistryB = String.fromCharCode(105,110,99,114,98,108,111,98,95,57,95,53,54,0);
       let heji9 = String.fromCharCode(111,95,49,55,95,109,117,108,115,117,98,0);
         rootm %= Math.max(2, heji9.length);
         editl = !editl;
       let dropdown6 = String.fromCharCode(100,101,115,101,114,105,97,108,105,122,101,95,118,95,52,52,0);
         componentregistryB = `${1 | dropdown6.length}`;
       let androidN = String.fromCharCode(115,111,108,105,100,99,111,108,111,114,95,120,95,49,52,0);
         rootm ^= 3;
      for (let c = 0; c < 1; c++) {
          let libsgcoreN = 1;
          let google8 = 2.0;
          let membershipK = String.fromCharCode(102,105,108,116,101,114,117,118,95,107,95,49,0);
          let sanss = 0.0;
         heji9 = `${3 | nativeH}`;
         libsgcoreN *= libsgcoreN;
         google8 += parseInt(`${sanss}`) / (Math.max(9, membershipK.length));
         membershipK = `${libsgcoreN}`;
         sanss *= libsgcoreN;
      }
         editl = editl || componentregistryB.length < 89;
      for (let d = 0; d < 1; d++) {
         nativeH &= dropdown6.length;
      }
         dropdown6 = `${(String.fromCharCode(68,0) == androidN ? heji9.length : androidN.length)}`;
      let membershipS = editl ? !editl : editl;
      do {
         editl = dropdown6 == String.fromCharCode(86,0);
         if (membershipS) {
            break;
         }
      } while (membershipS && (4 < rootm && (4 % (Math.max(1, rootm))) < 4));
      let g_playerv = componentregistryB.length <= 8069355;
      do {
          let libsgcorec: Map<any, any> = new Map([[String.fromCharCode(116,119,101,97,107,95,115,95,49,57,0),true ], [String.fromCharCode(100,101,99,114,121,112,116,95,56,95,49,55,0),false ]]);
          let inviteq = 1.0;
          let minivodw = String.fromCharCode(121,95,52,57,95,99,108,105,112,98,111,97,114,100,0);
          let windF = String.fromCharCode(101,114,97,95,109,95,52,48,0);
         componentregistryB += `${heji9.length}`;
         libsgcorec.set(`${windF}`, libsgcorec.size);
         inviteq /= Math.max(4, 3 + windF.length);
         minivodw += `${libsgcorec.size - minivodw.length}`;
         if (g_playerv) {
            break;
         }
      } while ((heji9 != componentregistryB) && g_playerv);
      if ((rootm % 5) < 4 && (nativeH % (Math.max(8, rootm))) < 5) {
         nativeH &= 2 << (Math.min(Math.abs(rootm), 2));
      }
      if (dropdown6.length >= componentregistryB.length) {
         componentregistryB = `${(String.fromCharCode(107,0) == heji9 ? androidN.length : heji9.length)}`;
      }
         dropdown6 += "1";
      gmailf += parseFloat(`${volumeH.size}`);
   }
      colorsL = gmailf < 4.96 && renderD;

    onClose();

      gmailf *= parseFloat(`${3 - parseInt(`${gmailf}`)}`);
      zoom8 /= Math.max(2, (parseInt(`${gmailf}`) ^ (renderD ? 1 : 5)));
   while ((gmailf * 1.52) > 4.55) {
      gmailf /= Math.max(1, parseFloat(`${liveB.length - volumeH.size}`));
      break;
   }
      thumbnailU.push(1);
       let cross8 = 2.0;
       let mutedg = String.fromCharCode(97,118,97,105,108,95,49,95,56,50,0);
      for (let l = 0; l < 3; l++) {
          let navigationF = 1;
          let classesI = 4;
          let package_0yT = String.fromCharCode(113,95,53,48,95,98,115,111,108,117,116,101,0);
          let sansI: Map<any, any> = new Map([[String.fromCharCode(119,95,53,53,95,109,97,105,108,0),905], [String.fromCharCode(99,95,53,50,95,100,111,99,117,109,101,110,116,97,116,105,111,110,0),422]]);
          let verticalt: Map<any, any> = new Map([[String.fromCharCode(104,95,56,56,0),663], [String.fromCharCode(114,111,117,110,100,101,100,95,101,95,54,0),203], [String.fromCharCode(103,101,116,101,110,118,95,101,95,53,48,0),321]]);
         mutedg = `${parseInt(`${cross8}`)}`;
         navigationF += verticalt.size % (Math.max(6, sansI.size));
         classesI %= Math.max(2, package_0yT.length);
         package_0yT += "3";
         sansI = new Map([[`${verticalt.size}`, 1 + sansI.size]]);
      }
          let calendarY = false;
          let complete_ = String.fromCharCode(105,110,101,116,95,98,95,53,50,0);
         cross8 -= mutedg.length;
         calendarY = complete_.includes(`${calendarY}`);
         complete_ = `${((calendarY ? 3 : 2) + complete_.length)}`;
      while (!mutedg.includes(`${cross8}`)) {
         cross8 -= mutedg.length;
         break;
      }
          let successZ = String.fromCharCode(120,100,97,105,95,102,95,49,48,0);
         mutedg = `${successZ.length - mutedg.length}`;
       let friendse = 2.0;
      while (mutedg.length > parseInt(`${friendse}`)) {
          let watchF: Array<any> = [201, 977, 477];
          let stationsW = String.fromCharCode(107,101,121,115,116,114,101,97,109,95,48,95,52,52,0);
          let router6 = String.fromCharCode(114,101,103,105,115,116,114,121,95,110,95,51,54,0);
          let foregroundE: Array<any> = [757, 140];
         mutedg += `${parseInt(`${cross8}`)}`;
         watchF = [2 >> (Math.min(2, stationsW.length))];
         stationsW += `${foregroundE.length << (Math.min(watchF.length, 5))}`;
         router6 = "2";
         foregroundE.push(foregroundE.length);
         break;
      }
      gmailf *= parseFloat(`${volumeH.size | parseInt(`${cross8}`)}`);
   if (3 > (3 % (Math.max(10, volumeH.size)))) {
      volumeH = new Map([[`${colorsL}`, ((colorsL ? 3 : 5))]]);
   }
   while (4.7 == (3.27 + gmailf) || gmailf == 3.27) {
      gmailf /= Math.max(parseFloat(`${liveB.length / (Math.max(7, zoom8))}`), 3);
      break;
   }
       let libyogac = String.fromCharCode(113,95,50,56,95,108,97,117,110,99,104,105,110,103,0);
       let binddatasp = false;
       let sourcey = 1.0;
          let sansP = String.fromCharCode(113,95,55,57,95,103,114,111,119,116,104,0);
          let activeN = 4;
          let gdtadvh = 0.0;
         binddatasp = gdtadvh < 66.22 || binddatasp;
         sansP += `${3 >> (Math.min(5, Math.abs(activeN)))}`;
         activeN >>= Math.min(Math.abs(3), 3);
         gdtadvh *= sansP.length + activeN;
      if (!binddatasp && (sourcey * 2.29) > 5.57) {
         sourcey *= ((binddatasp ? 4 : 5) << (Math.min(Math.abs(parseInt(`${sourcey}`)), 4)));
      }
      if (5.47 <= (sourcey * 2.91)) {
          let favoriteP = String.fromCharCode(114,101,103,105,115,116,101,114,101,100,95,110,95,54,50,0);
          let stringsZ = String.fromCharCode(118,105,101,119,112,111,114,116,95,110,95,57,56,0);
         libyogac = "3";
         favoriteP += `${favoriteP.length}`;
         stringsZ += `${stringsZ.length + 1}`;
      }
         sourcey /= Math.max(3, libyogac.length % 1);
       let schedulew = false;
       let trashe = false;
         sourcey /= Math.max(parseInt(`${sourcey}`) << (Math.min(2, Math.abs(1))), 4);
      let base8 = sourcey >= 8212432.0;
      do {
          let temperature7 = 5.0;
          let flyeru = String.fromCharCode(97,95,49,53,95,97,114,99,0);
          let scheduler8: Array<any> = [205, 757, 707];
          let appsA = String.fromCharCode(111,110,101,105,110,99,104,95,49,95,56,0);
         sourcey *= 1;
         temperature7 /= Math.max(4, parseInt(`${temperature7}`));
         flyeru = "1";
         scheduler8.push((flyeru == String.fromCharCode(116,0) ? flyeru.length : scheduler8.length));
         appsA += `${parseInt(`${temperature7}`) % 2}`;
         if (base8) {
            break;
         }
      } while (base8 && (sourcey == 5.15 && (sourcey / (Math.max(5.15, 2))) == 1.48));
      let traminif = sourcey >= 5944669.0;
      do {
         sourcey += parseInt(`${sourcey}`) - 2;
         if (traminif) {
            break;
         }
      } while (traminif && ((sourcey * 3.85) == 3.9 || 3.85 == sourcey));
      while (2.76 == (1.42 + sourcey)) {
          let userC = String.fromCharCode(108,111,110,103,101,114,95,119,95,55,0);
          let taiwan1 = 4.0;
          let annerS = String.fromCharCode(113,95,53,51,95,99,111,101,102,102,115,112,0);
         schedulew = libyogac.length >= 2 || binddatasp;
         userC = `${annerS.length}`;
         taiwan1 += (annerS == String.fromCharCode(85,0) ? annerS.length : userC.length);
         break;
      }
      gmailf -= parseFloat(`${3}`);
   while (1 >= (parseInt(`${gmailf}`) / (Math.max(thumbnailU.length, 3))) && 5.97 >= (gmailf / 5.12)) {
      gmailf -= (parseFloat(`${(renderD ? 1 : 1)}`));
      break;
   }
    dispatch(vipPromotionModalShown());
   
  }

  const handleOnPurchase = useCallback(() => {
       let filedv = 4.0;
    let condensed2: Array<any> = [249, 206];
    let libyogaa = String.fromCharCode(101,116,105,109,101,95,105,95,50,56,0);
    let hoverr = 5.0;
    let canvas2: Map<any, any> = new Map([[String.fromCharCode(121,95,55,57,95,116,100,115,102,0),String.fromCharCode(97,103,97,105,110,95,48,95,53,52,0)], [String.fromCharCode(112,97,108,98,97,114,115,95,52,95,52,52,0),String.fromCharCode(97,95,49,50,95,115,101,116,115,104,97,114,101,0)], [String.fromCharCode(109,97,114,107,95,53,95,52,49,0),String.fromCharCode(100,95,50,56,95,100,115,116,114,0)]]);
    let networkd: Map<any, any> = new Map([[String.fromCharCode(101,112,108,121,95,112,95,53,52,0),160], [String.fromCharCode(103,101,110,99,102,103,115,95,107,95,52,0),978], [String.fromCharCode(115,95,53,54,95,97,118,97,116,97,114,115,0),408]]);
    let libffmpegkitO = String.fromCharCode(119,95,51,95,111,117,116,103,111,105,110,103,0);
    let pnewarchdefaultsx = true;
    let detailu = 2.0;
    let dialogL = 2.0;
    let libimagepipelinew = 2.0;
    let entryD = String.fromCharCode(109,97,116,104,95,107,95,54,50,0);
    let expiredE = String.fromCharCode(117,110,112,114,111,116,101,99,116,95,103,95,54,52,0);
    let basketballw = String.fromCharCode(110,95,57,56,95,115,101,103,102,101,97,116,117,114,101,0);
    let savef = 1.0;
    let launch0: Map<any, any> = new Map([[String.fromCharCode(110,101,116,105,115,114,95,98,95,53,0),false ], [String.fromCharCode(117,95,51,51,95,115,109,112,116,101,104,100,98,97,114,115,0),false ], [String.fromCharCode(112,95,57,56,95,99,111,109,98,105,110,101,114,0),true ]]);
    let dialogy = String.fromCharCode(110,95,49,57,95,109,97,116,114,111,115,107,97,0);
   for (let c = 0; c < 2; c++) {
      libffmpegkitO = `${parseInt(`${filedv}`)}`;
   }
   let orangeY = canvas2.size >= 6013288;
   do {
      canvas2 = new Map([[libyogaa, (String.fromCharCode(98,0) == libyogaa ? parseInt(`${filedv}`) : libyogaa.length)]]);
      if (orangeY) {
         break;
      }
   } while ((Array.from(canvas2.values()).includes(filedv)) && orangeY);
   if (libffmpegkitO.length == libyogaa.length) {
      libffmpegkitO = `${parseInt(`${dialogL}`) / (Math.max(1, parseInt(`${libimagepipelinew}`)))}`;
   }
      libffmpegkitO = `${parseInt(`${filedv}`) - 2}`;
       let singaporeE = String.fromCharCode(110,111,110,110,117,108,108,111,117,116,95,122,95,53,50,0);
       let libhermesA = String.fromCharCode(107,102,114,109,95,121,95,53,48,0);
       let transfer1 = String.fromCharCode(100,95,49,53,95,116,114,97,110,115,108,97,116,101,100,0);
      let video7 = libhermesA.length <= 7169338;
      do {
          let updatesD = String.fromCharCode(114,101,103,105,111,110,95,113,95,49,0);
          let libavfilterf: Array<any> = [943, 951];
          let eact2 = false;
          let details9 = String.fromCharCode(115,111,117,114,99,101,95,114,95,51,54,0);
          let watchn = 3.0;
         libhermesA = `${3 / (Math.max(10, updatesD.length))}`;
         updatesD = `${details9.length}`;
         libavfilterf.push(((eact2 ? 5 : 2) * libavfilterf.length));
         eact2 = !eact2;
         details9 = "1";
         watchn += ((eact2 ? 1 : 2) * 3);
         if (video7) {
            break;
         }
      } while (video7 && (transfer1 != libhermesA));
         singaporeE = `${transfer1.length}`;
      let playlistt = libhermesA == String.fromCharCode(108,53,98,113,0);
      do {
         libhermesA += `${singaporeE.length}`;
         if (playlistt) {
            break;
         }
      } while ((transfer1 == libhermesA) && playlistt);
      for (let u = 0; u < 1; u++) {
         transfer1 = `${singaporeE.length % (Math.max(10, transfer1.length))}`;
      }
       let floater8: Array<any> = [511, 984];
       let guided: Array<any> = [443, 742, 521];
       let gdtadvz = 5.0;
       let infoL = 2.0;
      for (let s = 0; s < 2; s++) {
         guided = [(transfer1 == String.fromCharCode(72,0) ? transfer1.length : floater8.length)];
      }
         guided = [singaporeE.length];
         transfer1 += `${transfer1.length}`;
      entryD = `${entryD.length - canvas2.size}`;
   while (2 >= entryD.length || pnewarchdefaultsx) {
      pnewarchdefaultsx = 66.86 >= dialogL;
      break;
   }

    onClose();

      networkd.set(`${libimagepipelinew}`, parseInt(`${libimagepipelinew}`) ^ 1);
      pnewarchdefaultsx = 55 < entryD.length;
   while (3 <= canvas2.size) {
      libimagepipelinew *= parseFloat(`${parseInt(`${hoverr}`) >> (Math.min(Math.abs(1), 5))}`);
      break;
   }
   let analyticg = 9681404 <= condensed2.length;
   do {
      condensed2.push(2 & libffmpegkitO.length);
      if (analyticg) {
         break;
      }
   } while (analyticg && (entryD.length >= 4));
      dialogL += parseFloat(`${libyogaa.length}`);
       let rewindD = String.fromCharCode(99,101,114,116,115,95,56,95,57,52,0);
       let largeF = 3.0;
         largeF -= parseFloat(`${2}`);
      let actionsh = rewindD.length <= 9337961;
      do {
         rewindD += `${2 & rewindD.length}`;
         if (actionsh) {
            break;
         }
      } while ((largeF < 1.36) && actionsh);
      while (4.77 >= (largeF + parseFloat(`${rewindD.length}`))) {
         rewindD += `${(String.fromCharCode(104,0) == rewindD ? parseInt(`${largeF}`) : rewindD.length)}`;
         break;
      }
       let libjsinspector2 = 3.0;
      for (let x = 0; x < 1; x++) {
          let sheet9 = false;
         largeF *= parseFloat(`${parseInt(`${largeF}`) % 3}`);
         sheet9 = (!sheet9 ? !sheet9 : !sheet9);
      }
         rewindD += `${parseInt(`${largeF}`)}`;
      libffmpegkitO += `${canvas2.size % (Math.max(libyogaa.length, 10))}`;
    dispatch(vipPromotionModalShown());

   while (!libyogaa.endsWith(`${canvas2.size}`)) {
       let progressi = 4;
       let modalv = String.fromCharCode(115,101,113,118,105,100,101,111,95,53,95,57,51,0);
          let nalytics1 = true;
          let currentm: Array<any> = [616, 553, 64];
          let libpangleflippeds = true;
         progressi %= Math.max(2, 5);
         nalytics1 = (!nalytics1 ? !libpangleflippeds : !nalytics1);
         currentm = [currentm.length];
      for (let p = 0; p < 3; p++) {
         modalv += `${3 << (Math.min(3, Math.abs(progressi)))}`;
      }
         modalv = `${(modalv == String.fromCharCode(57,0) ? modalv.length : progressi)}`;
       let friendsV: Map<any, any> = new Map([[String.fromCharCode(109,117,114,109,117,114,95,115,95,50,50,0),153], [String.fromCharCode(105,95,50,48,95,99,97,108,108,98,97,99,107,115,0),659], [String.fromCharCode(105,95,52,56,95,99,108,111,115,101,112,0),435]]);
         friendsV = new Map([[`${friendsV.size}`, (String.fromCharCode(107,0) == modalv ? modalv.length : friendsV.size)]]);
      for (let l = 0; l < 1; l++) {
         modalv = "1";
      }
      libyogaa = `${libyogaa.length << (Math.min(Math.abs(2), 1))}`;
      break;
   }
      condensed2.push(parseInt(`${dialogL}`));
   let indicator6 = 8305773.0 <= filedv;
   do {
       let sansf = 5.0;
       let libturbomodulejsijniX = String.fromCharCode(109,95,54,52,95,116,114,105,110,103,0);
       let navigationn = 1;
       let placement7 = String.fromCharCode(116,105,109,101,102,105,108,116,101,114,95,97,95,53,0);
         libturbomodulejsijniX = "2";
         placement7 = `${(String.fromCharCode(117,0) == libturbomodulejsijniX ? libturbomodulejsijniX.length : parseInt(`${sansf}`))}`;
      if (libturbomodulejsijniX != placement7) {
         placement7 += `${placement7.length}`;
      }
         libturbomodulejsijniX += "2";
       let auto_oml: Map<any, any> = new Map([[String.fromCharCode(114,95,52,55,95,115,98,117,118,0),754], [String.fromCharCode(101,116,104,111,100,95,120,95,52,53,0),395], [String.fromCharCode(105,95,53,48,95,116,104,105,114,100,0),226]]);
         placement7 = `${2 >> (Math.min(1, Math.abs(parseInt(`${sansf}`))))}`;
      if (!libturbomodulejsijniX.endsWith(placement7)) {
         libturbomodulejsijniX += `${3 - auto_oml.size}`;
      }
         placement7 += `${placement7.length}`;
       let weiboR = true;
       let goalR = 2;
          let gdtadvb = String.fromCharCode(108,95,51,57,95,115,97,102,101,0);
          let questB = true;
          let shootn: Array<any> = [513, 139];
         weiboR = 61.34 < sansf && 93 < navigationn;
         gdtadvb += "3";
         questB = !questB;
         shootn.push((String.fromCharCode(108,0) == gdtadvb ? gdtadvb.length : (questB ? 3 : 1)));
         navigationn %= Math.max(navigationn >> (Math.min(2, Math.abs(1))), 5);
      filedv += parseInt(`${dialogL}`) - expiredE.length;
      if (indicator6) {
         break;
      }
   } while (indicator6 && (parseInt(`${filedv}`) > condensed2.length));
   for (let s = 0; s < 2; s++) {
       let vignetteA: Array<any> = [59, 415];
       let nterstitiale = false;
      while (vignetteA.length < 2 || (vignetteA.length ^ 2) < 1) {
         nterstitiale = vignetteA.includes(nterstitiale);
         break;
      }
      while (5 > vignetteA.length || 1 > (vignetteA.length & 5)) {
          let libcxxcomponentsY = 3.0;
          let imagemanagerj = 2.0;
          let debug9 = 5.0;
         nterstitiale = 85.38 < debug9 && 85.38 < libcxxcomponentsY;
         libcxxcomponentsY += parseInt(`${imagemanagerj}`);
         break;
      }
      pnewarchdefaultsx = dialogL >= 65.25;
   }
   for (let l = 0; l < 3; l++) {
      pnewarchdefaultsx = canvas2.size > filedv;
   }
       let anytime1 = 1.0;
       let long_ziD = 0;
       let interstitialr = 5.0;
      for (let q = 0; q < 2; q++) {
         long_ziD -= 1 - parseInt(`${anytime1}`);
      }
      let buildo = interstitialr <= 5557852.0;
      do {
          let orangeU: Array<any> = [193, 793];
          let closeX: Array<any> = [75, 406, 919];
          let ewardedz: Map<any, any> = new Map([[String.fromCharCode(101,95,56,49,95,97,99,101,110,99,0),String.fromCharCode(119,95,50,48,95,100,108,116,97,0)], [String.fromCharCode(109,95,50,50,95,105,110,116,101,114,102,97,99,101,0),String.fromCharCode(121,95,50,49,95,108,111,103,0)]]);
         interstitialr *= parseFloat(`${parseInt(`${interstitialr}`) * 2}`);
         orangeU.push(ewardedz.size);
         closeX = [orangeU.length | 3];
         ewardedz.set(`${closeX.length}`, 3 >> (Math.min(3, closeX.length)));
         if (buildo) {
            break;
         }
      } while (buildo && (2.36 <= (interstitialr * 5.4)));
       let activityj: Array<any> = [832, 306];
         anytime1 *= parseFloat(`${parseInt(`${interstitialr}`) | 2}`);
      while ((1.76 * interstitialr) == 5.9) {
          let predictionb = 5.0;
         interstitialr *= parseFloat(`${activityj.length ^ parseInt(`${predictionb}`)}`);
         break;
      }
      if (activityj.includes(anytime1)) {
         activityj.push(parseInt(`${anytime1}`));
      }
      if (activityj.includes(anytime1)) {
          let libfabricjni0 = 0.0;
          let emptyT = 4;
          let configureF = String.fromCharCode(103,95,56,95,114,101,112,108,105,101,115,0);
         anytime1 /= Math.max(parseFloat(`${activityj.length << (Math.min(Math.abs(3), 4))}`), 1);
         libfabricjni0 /= Math.max(4, parseFloat(`${emptyT}`));
         emptyT += parseInt(`${libfabricjni0}`);
         configureF = `${emptyT | configureF.length}`;
      }
          let armvaF = String.fromCharCode(116,114,97,106,101,99,116,111,114,121,95,57,95,53,49,0);
          let expiredi = false;
         interstitialr -= parseFloat(`${activityj.length}`);
         armvaF = `${1 | armvaF.length}`;
         expiredi = !expiredi || armvaF.length > 23;
          let style3 = String.fromCharCode(119,95,53,51,95,116,105,99,107,105,110,103,0);
          let libavfilterP = String.fromCharCode(113,95,49,48,95,108,115,102,112,111,108,121,0);
          let mbnativeadvancedp = String.fromCharCode(99,95,55,53,95,103,101,110,101,114,97,108,105,122,101,100,116,105,109,101,0);
         long_ziD += parseInt(`${interstitialr}`);
         style3 = `${2 / (Math.max(8, libavfilterP.length))}`;
         libavfilterP += `${1 * libavfilterP.length}`;
         mbnativeadvancedp = `${3 + mbnativeadvancedp.length}`;
      savef -= parseInt(`${libimagepipelinew}`) / (Math.max(6, parseInt(`${anytime1}`)));

    navigator.navigate("付费VIP");
    
    
    
    
    
  }, []);

  return (
    <Modal
      visible={showCondition}
      transparent={true}
      animationType="fade"
      onRequestClose={onClose}
    >
      <TouchableWithoutFeedback onPress={onClose}>
        <View style={{ flex: 1, backgroundColor: "rgba(0, 0, 0, 0.4)" }}>
          <VipPromotionModal
            coverBackground={sportBg}
            coverImage={commonModels}
            onPurchase={handleOnPurchase}
            onClose={onCloseModal}
            showCondition={showCondition}
          />
        </View>
      </TouchableWithoutFeedback>
    </Modal>
  );
};
