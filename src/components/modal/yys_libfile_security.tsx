import React, { useCallback } from "react";
import { Modal, TouchableWithoutFeedback, View } from "react-native";
import { useNavigation } from "@react-navigation/native";
import VipLoginAlertModal from "./yys_position";
import { UMENG_CHANNEL } from "@utility";
import { showLoginAction } from "@redux";
import { useAppDispatch } from "@hooks";
import { loginModalShown } from "@redux";

const commonModels = require('../../../static/images/anythinkSubtextGmail.png');
const sportBg = require('../../../static/images/statsTempnodatagif.png');

interface yysIconarrowrightorangeStation {
  showCondition: boolean;
  onClose: any;
}

export const VipLoginAlertOverlay = ({ showCondition, onClose }: yysIconarrowrightorangeStation) => {
  const navigator = useNavigation();
  const dispatch = useAppDispatch();

  const onCloseModal = () => {
       let encryptor1 = String.fromCharCode(104,113,97,100,115,112,95,108,95,48,0);
    let condensedj: Array<any> = [349, 634];
    let loginU = 1.0;
    let redscoreballY: Array<any> = [159, 833, 587];
    let holderP: Array<any> = [919, 406, 155];
    let brightnesst = String.fromCharCode(116,95,57,57,95,117,114,118,101,0);
    let iconsharefriendsc: Array<any> = [286, 775, 987];
    let crownu = 5.0;
   let subbasketballplayer1 = brightnesst == String.fromCharCode(118,101,103,115,51,106,113,121,53,0);
   do {
      brightnesst = `${iconsharefriendsc.length >> (Math.min(Math.abs(1), 4))}`;
      if (subbasketballplayer1) {
         break;
      }
   } while ((!brightnesst.startsWith(`${encryptor1.length}`)) && subbasketballplayer1);
   let mountingB = 6028862 <= brightnesst.length;
   do {
      brightnesst = `${encryptor1.length}`;
      if (mountingB) {
         break;
      }
   } while (mountingB && ((5 % (Math.max(2, brightnesst.length))) > 4 && 5 > (brightnesst.length + parseInt(`${loginU}`))));
      crownu += condensedj.length;
      loginU += parseFloat(`${3 * iconsharefriendsc.length}`);
   while (1 >= (2 ^ encryptor1.length)) {
       let window_4w2 = 5.0;
       let libreactT = String.fromCharCode(99,95,54,56,95,99,104,101,99,107,101,100,0);
       let blacke = String.fromCharCode(112,111,116,105,115,105,111,110,95,103,95,55,0);
       let episodep = String.fromCharCode(105,110,116,114,97,120,104,117,102,95,49,95,54,50,0);
         blacke = `${(String.fromCharCode(67,0) == blacke ? blacke.length : episodep.length)}`;
      for (let e = 0; e < 1; e++) {
          let carouselv = String.fromCharCode(100,105,115,112,111,115,105,116,105,111,110,95,108,95,50,50,0);
          let orientationX: Array<any> = [525, 910, 61];
          let starj = true;
          let iconbackwhiteu = 2.0;
          let roomU = 4;
         libreactT = "3";
         carouselv += `${(carouselv == String.fromCharCode(79,0) ? orientationX.length : carouselv.length)}`;
         orientationX = [1 >> (Math.min(Math.abs(parseInt(`${iconbackwhiteu}`)), 4))];
         starj = orientationX.length == parseInt(`${iconbackwhiteu}`);
         roomU |= 1;
      }
      if (5 == episodep.length) {
         window_4w2 -= 3 ^ libreactT.length;
      }
          let libreactnativejnig = String.fromCharCode(105,95,54,54,95,114,101,97,100,98,105,116,115,0);
          let volume1 = 2.0;
         libreactT = `${parseInt(`${window_4w2}`)}`;
         libreactnativejnig = "1";
         volume1 -= (parseFloat(`${libreactnativejnig == String.fromCharCode(75,0) ? parseInt(`${volume1}`) : libreactnativejnig.length}`));
      if (4 >= libreactT.length || 4 >= episodep.length) {
         libreactT = `${libreactT.length / (Math.max(episodep.length, 7))}`;
      }
         blacke = `${2 & parseInt(`${window_4w2}`)}`;
         episodep += "3";
      let source0 = window_4w2 >= 9543392.0;
      do {
          let nterstitiala = 0;
         window_4w2 /= Math.max(libreactT.length, 3);
         nterstitiala <<= Math.min(2, Math.abs(2));
         if (source0) {
            break;
         }
      } while (source0 && ((window_4w2 - blacke.length) >= 3.90));
       let gifgoalH = 5.0;
      if (gifgoalH <= parseFloat(`${libreactT.length}`)) {
         libreactT += `${blacke.length}`;
      }
      for (let x = 0; x < 1; x++) {
         gifgoalH /= Math.max(4, parseFloat(`${libreactT.length}`));
      }
       let defaultbasketballbgr = String.fromCharCode(101,118,97,108,117,97,116,101,100,95,100,95,49,52,0);
      encryptor1 = `${parseInt(`${loginU}`) >> (Math.min(redscoreballY.length, 2))}`;
      break;
   }
   for (let i = 0; i < 3; i++) {
      loginU -= parseFloat(`${parseInt(`${crownu}`)}`);
   }
      condensedj.push(encryptor1.length);
      iconsharefriendsc = [1 ^ iconsharefriendsc.length];
   if (!holderP.includes(crownu)) {
      crownu *= 1 - parseInt(`${loginU}`);
   }
      iconsharefriendsc = [3];
      redscoreballY = [holderP.length];
      holderP.push(brightnesst.length - parseInt(`${crownu}`));
   while (brightnesst.length > 1) {
      brightnesst += "1";
      break;
   }
   for (let h = 0; h < 3; h++) {
      encryptor1 += `${(String.fromCharCode(121,0) == brightnesst ? parseInt(`${loginU}`) : brightnesst.length)}`;
   }

    onClose();

      crownu -= 2 >> (Math.min(2, Math.abs(parseInt(`${loginU}`))));
      condensedj = [2];
      redscoreballY.push(parseInt(`${crownu}`) * parseInt(`${loginU}`));
      encryptor1 = `${condensedj.length}`;
       let dicef = String.fromCharCode(112,110,105,101,108,115,95,105,95,57,55,0);
       let calendare = 3;
         calendare &= 2 << (Math.min(4, dicef.length));
         calendare <<= Math.min(dicef.length, 2);
      while (4 <= (dicef.length * calendare)) {
          let plusS = 3.0;
         calendare >>= Math.min(dicef.length, 3);
         plusS /= Math.max(parseInt(`${plusS}`) - 3, 1);
         break;
      }
      let predictionarrowU = 9746947 >= calendare;
      do {
         calendare += calendare + 3;
         if (predictionarrowU) {
            break;
         }
      } while (((5 - dicef.length) <= 4 || 5 <= (5 - calendare)) && predictionarrowU);
      for (let j = 0; j < 3; j++) {
          let xnewsshareb = false;
          let shootyesgoalW = String.fromCharCode(108,105,115,116,110,101,114,115,95,56,95,56,54,0);
          let lightJ = false;
         dicef = "2";
         xnewsshareb = !shootyesgoalW.startsWith(`${lightJ}`);
         shootyesgoalW += `${shootyesgoalW.length}`;
         lightJ = (98 <= ((!xnewsshareb ? shootyesgoalW.length : 98) / (Math.max(shootyesgoalW.length, 8))));
      }
         calendare -= dicef.length / (Math.max(5, calendare));
      crownu -= 3 | encryptor1.length;
       let mapbufferP = 1.0;
      if (2.48 > (mapbufferP - 5.100) || (mapbufferP / (Math.max(mapbufferP, 8))) > 5.100) {
         mapbufferP += parseFloat(`${2}`);
      }
         mapbufferP -= parseFloat(`${parseInt(`${mapbufferP}`)}`);
       let matchesd: Array<any> = [762, 109];
       let informationI: Array<any> = [25, 854, 879];
      brightnesst = `${redscoreballY.length + 2}`;
      crownu -= 2 - parseInt(`${crownu}`);
   let largeh = crownu >= 5161777.0;
   do {
      crownu += parseInt(`${crownu}`) + redscoreballY.length;
      if (largeh) {
         break;
      }
   } while (largeh && ((crownu / (Math.max(2, iconsharefriendsc.length))) > 3.60 || (iconsharefriendsc.length | 5) > 1));
      condensedj = [iconsharefriendsc.length + 3];
       let zoomN: Array<any> = [275, 13, 0];
       let iconorientatione = 4.0;
       let speca = String.fromCharCode(103,95,50,51,95,99,97,117,116,101,114,105,122,101,0);
          let disconnectedlogon = false;
          let network5: Array<any> = [772, 152];
          let filled7 = String.fromCharCode(101,95,53,49,95,100,97,116,97,116,121,112,101,115,0);
         zoomN.push(1);
         disconnectedlogon = (22 <= ((disconnectedlogon ? network5.length : 22) >> (Math.min(network5.length, 2))));
         filled7 = `${(String.fromCharCode(80,0) == filled7 ? network5.length : filled7.length)}`;
      for (let q = 0; q < 1; q++) {
         iconorientatione -= 1 * speca.length;
      }
          let stringsu: Map<any, any> = new Map([[String.fromCharCode(108,95,52,56,95,112,114,101,105,118,111,117,115,0),String.fromCharCode(120,95,56,95,97,118,118,115,0)], [String.fromCharCode(115,95,55,48,95,99,111,101,102,102,115,112,0),String.fromCharCode(107,95,57,49,95,107,105,99,107,101,100,0)]]);
          let mbnativeT = 4;
          let sportu = String.fromCharCode(122,95,52,55,95,109,97,115,107,101,100,99,108,97,109,112,0);
         speca = `${stringsu.size}`;
         stringsu = new Map([[`${mbnativeT}`, mbnativeT]]);
         sportu += `${sportu.length}`;
      while (!speca.startsWith(`${iconorientatione}`)) {
         iconorientatione += speca.length;
         break;
      }
         speca += `${speca.length}`;
          let infoe: Array<any> = [881, 830, 881];
          let anytimed = false;
         speca = `${parseInt(`${iconorientatione}`) ^ 3}`;
         infoe.push(((anytimed ? 4 : 5) % (Math.max(2, 1))));
         anytimed = ((infoe.length | (!anytimed ? 31 : infoe.length)) > 31);
      let pause6 = iconorientatione <= 7036899.0;
      do {
         iconorientatione *= zoomN.length;
         if (pause6) {
            break;
         }
      } while ((4 == (2 + zoomN.length)) && pause6);
         speca = `${2 & zoomN.length}`;
      let sharedu = 6910610 >= zoomN.length;
      do {
          let about8 = String.fromCharCode(117,95,49,50,95,111,118,101,114,114,105,100,105,110,103,0);
          let sellmathbackgroundj = 4;
          let iconwatch8 = false;
          let clubZ = 1.0;
          let modei = 0;
         zoomN.push(about8.length << (Math.min(3, speca.length)));
         about8 = `${modei}`;
         sellmathbackgroundj /= Math.max(parseInt(`${clubZ}`), 2);
         iconwatch8 = clubZ <= 24.93 || !iconwatch8;
         modei %= Math.max(4, sellmathbackgroundj / (Math.max(modei, 4)));
         if (sharedu) {
            break;
         }
      } while (sharedu && (3 < (speca.length + zoomN.length) && 1 < (zoomN.length + 3)));
      loginU += parseFloat(`${encryptor1.length ^ 3}`);
   for (let w = 0; w < 2; w++) {
      redscoreballY.push(1 % (Math.max(6, redscoreballY.length)));
   }
       let fileH = String.fromCharCode(98,95,49,51,95,115,101,109,97,112,104,111,114,101,0);
       let borderlessI: Map<any, any> = new Map([[String.fromCharCode(98,95,49,48,48,95,114,97,112,112,101,114,0),577], [String.fromCharCode(114,116,112,100,101,99,95,53,95,54,54,0),155], [String.fromCharCode(97,116,116,114,98,117,116,101,95,98,95,55,56,0),230]]);
       let moonA = 5.0;
       let libjsi8 = 1;
       let matchesG = 4;
         moonA -= libjsi8 & borderlessI.size;
      let bridgez = 8492239 <= fileH.length;
      do {
         fileH += `${borderlessI.size * 2}`;
         if (bridgez) {
            break;
         }
      } while ((1 <= (matchesG | 2)) && bridgez);
      let bangs = matchesG >= 9189994;
      do {
          let chatbotphoto8 = String.fromCharCode(115,101,114,118,101,114,95,110,95,51,53,0);
          let defaultpredictionprofileN = 0.0;
          let homeinactivet = 2;
         matchesG >>= Math.min(1, Math.abs(1 + parseInt(`${moonA}`)));
         chatbotphoto8 += "1";
         defaultpredictionprofileN += homeinactivet / (Math.max(7, chatbotphoto8.length));
         homeinactivet ^= 1 % (Math.max(9, homeinactivet));
         if (bangs) {
            break;
         }
      } while (((matchesG | 5) > 4 || 5 > (matchesG | libjsi8)) && bangs);
      let playlist4 = fileH == String.fromCharCode(51,120,97,116,104,55,55,99,121,0);
      do {
         fileH = `${libjsi8}`;
         if (playlist4) {
            break;
         }
      } while (playlist4 && (1 > fileH.length));
      if (1 < fileH.length) {
          let libfabricjniG: Map<any, any> = new Map([[String.fromCharCode(113,95,56,56,0),false ], [String.fromCharCode(97,95,53,48,95,104,97,110,100,108,101,0),false ]]);
         fileH = "1";
         libfabricjniG.set(`${libfabricjniG.size}`, 3 + libfabricjniG.size);
      }
      for (let i = 0; i < 3; i++) {
          let iconnotificationnewj = String.fromCharCode(98,95,56,95,109,115,101,99,115,0);
          let liveendmodallogoG = false;
         matchesG %= Math.max(1, 3 - parseInt(`${moonA}`));
         iconnotificationnewj += "3";
         liveendmodallogoG = iconnotificationnewj.startsWith(`${liveendmodallogoG}`);
      }
      let mathu = fileH == String.fromCharCode(121,104,95,109,56,113,50,54,111,110,0);
      do {
         fileH = `${3 >> (Math.min(2, fileH.length))}`;
         if (mathu) {
            break;
         }
      } while (mathu && (!fileH.startsWith(`${borderlessI.size}`)));
         moonA /= Math.max(1, 3 * fileH.length);
      encryptor1 = `${1 + redscoreballY.length}`;
      brightnesst += `${brightnesst.length << (Math.min(Math.abs(2), 3))}`;
      brightnesst += `${2 << (Math.min(Math.abs(parseInt(`${crownu}`)), 3))}`;
    dispatch(loginModalShown());
  }

  const handleOnPurchase = useCallback(() => {
       let iconwechath = 2.0;
    let megaphoneB = String.fromCharCode(111,102,102,115,101,116,95,103,95,51,51,0);
    let hooko = String.fromCharCode(105,99,101,95,109,95,54,54,0);
    let whistleorangeC = String.fromCharCode(116,95,49,57,95,115,116,114,110,115,116,114,0);
    let arrowrightv = String.fromCharCode(114,101,112,108,105,99,97,116,101,95,55,95,50,57,0);
    let g_unlock_ = 1.0;
    let langkeyT = String.fromCharCode(112,97,114,116,105,99,105,112,97,116,105,111,110,95,102,95,51,56,0);
    let usernameC: Map<any, any> = new Map([[String.fromCharCode(97,95,49,49,0),584], [String.fromCharCode(121,95,57,50,95,112,114,101,115,101,100,0),497]]);
    let reactnativeultimatelistview4 = 2.0;
    let modulew = 2;
    let modelsE = 2.0;
    let photob = 4;
    let yellowcirclebgW = true;
    let toponO = String.fromCharCode(120,120,99,104,95,116,95,55,55,0);
    let livenodatabgimg1 = 1.0;
    let whitesmallticky = String.fromCharCode(110,95,54,55,95,115,117,98,114,101,115,117,108,116,0);
    let yellowtoredW = String.fromCharCode(120,95,56,55,95,97,97,99,112,115,121,0);
    let rocketj = String.fromCharCode(103,95,52,56,95,117,110,109,105,120,0);
   if (!langkeyT.startsWith(`${iconwechath}`)) {
       let weatherv = String.fromCharCode(105,95,51,49,95,112,97,105,100,0);
       let ajaxg = String.fromCharCode(98,95,50,53,95,108,105,109,105,116,115,0);
       let appsp = false;
         appsp = (weatherv.length % (Math.max(ajaxg.length, 6))) == 89;
      let notificationfilledG = appsp ? !appsp : appsp;
      do {
         appsp = !appsp;
         if (notificationfilledG) {
            break;
         }
      } while (notificationfilledG && (weatherv.length > 2));
      for (let z = 0; z < 3; z++) {
          let brightnessP = 5;
          let yellowB = 0.0;
         ajaxg = `${1 & brightnessP}`;
         brightnessP ^= parseInt(`${yellowB}`);
      }
      let owngoal9 = appsp ? !appsp : appsp;
      do {
         appsp = ajaxg.length > 45;
         if (owngoal9) {
            break;
         }
      } while (owngoal9 && (!ajaxg.endsWith(`${appsp}`)));
          let homeloadingu = false;
         weatherv += `${(String.fromCharCode(57,0) == weatherv ? weatherv.length : ajaxg.length)}`;
         homeloadingu = !homeloadingu;
       let exampleimagex = 5;
      if (2 <= exampleimagex || (2 & exampleimagex) <= 1) {
          let bcopy_b0 = 4.0;
          let coreO = 5.0;
          let nativemodulej = String.fromCharCode(108,97,110,103,115,95,100,95,56,49,0);
          let bootsplashw: Map<any, any> = new Map([[String.fromCharCode(118,95,51,56,95,97,116,111,105,0),558], [String.fromCharCode(97,95,53,51,95,117,108,116,114,97,0),710], [String.fromCharCode(108,95,49,53,95,115,105,109,117,108,97,116,105,111,110,0),40]]);
          let pressureF = String.fromCharCode(106,95,56,49,95,103,117,105,100,115,0);
         appsp = exampleimagex < parseInt(`${bcopy_b0}`);
         bcopy_b0 /= Math.max((parseFloat(`${nativemodulej == String.fromCharCode(117,0) ? nativemodulej.length : parseInt(`${coreO}`)}`)), 1);
         coreO *= parseFloat(`${parseInt(`${coreO}`) * nativemodulej.length}`);
         bootsplashw = new Map([[`${bootsplashw.size}`, 3 >> (Math.min(3, Math.abs(bootsplashw.size)))]]);
         pressureF = `${pressureF.length ^ bootsplashw.size}`;
      }
         appsp = 56 <= weatherv.length || exampleimagex <= 56;
         appsp = (ajaxg.length >> (Math.min(weatherv.length, 1))) > 23;
      iconwechath /= Math.max(parseFloat(`${3}`), 3);
   }
       let roundQ = 5.0;
       let episodel = true;
       let internetZ = String.fromCharCode(102,95,52,53,95,117,110,105,110,105,116,105,97,108,105,122,101,100,0);
          let sharemodalQ = 1.0;
         internetZ = `${parseInt(`${sharemodalQ}`) ^ 2}`;
      let soundH = internetZ.length >= 9127696;
      do {
         internetZ = `${((episodel ? 4 : 4) << (Math.min(Math.abs(parseInt(`${roundQ}`)), 5)))}`;
         if (soundH) {
            break;
         }
      } while (soundH && (!internetZ.includes(`${episodel}`)));
      if (internetZ.endsWith(`${episodel}`)) {
         episodel = internetZ == String.fromCharCode(111,0);
      }
          let w_positionQ = true;
         episodel = 95.97 >= roundQ && String.fromCharCode(71,0) == internetZ;
         w_positionQ = !w_positionQ;
          let arrowselectdownF = 1.0;
          let kick2 = String.fromCharCode(116,114,97,110,115,108,97,116,101,100,95,102,95,54,50,0);
          let mbbidE = 2.0;
         episodel = 16.94 == arrowselectdownF;
         arrowselectdownF *= parseFloat(`${parseInt(`${mbbidE}`)}`);
         kick2 += `${parseInt(`${mbbidE}`)}`;
         internetZ = `${(internetZ == String.fromCharCode(76,0) ? (episodel ? 3 : 1) : internetZ.length)}`;
      if (!episodel) {
         roundQ *= (internetZ == String.fromCharCode(84,0) ? internetZ.length : parseInt(`${roundQ}`));
      }
         episodel = !episodel;
      let defaultprofilepicL = episodel ? !episodel : episodel;
      do {
         episodel = internetZ.length < 80;
         if (defaultprofilepicL) {
            break;
         }
      } while (defaultprofilepicL && (episodel));
      langkeyT = `${parseInt(`${modelsE}`) ^ 2}`;
   if (modulew >= hooko.length) {
       let libhermesB = String.fromCharCode(105,95,49,53,95,112,101,114,109,105,116,116,101,100,0);
       let diceD = String.fromCharCode(98,95,54,53,95,101,120,112,111,0);
       let flipperN = 0;
       let manifestq = false;
          let modalV = String.fromCharCode(121,109,101,110,99,95,51,95,49,0);
         flipperN += 1 - libhermesB.length;
         modalV += `${modalV.length / (Math.max(modalV.length, 10))}`;
      for (let k = 0; k < 3; k++) {
         diceD = `${2 - flipperN}`;
      }
         libhermesB = `${((manifestq ? 3 : 2) % (Math.max(libhermesB.length, 9)))}`;
          let sharewhite1 = 3;
         flipperN /= Math.max(4, 3);
         sharewhite1 |= sharewhite1 & 3;
         diceD = `${(String.fromCharCode(98,0) == libhermesB ? libhermesB.length : flipperN)}`;
      if (1 > diceD.length && !manifestq) {
         diceD = `${3 % (Math.max(6, diceD.length))}`;
      }
      if (4 < (2 - flipperN)) {
         flipperN &= 2;
      }
      while (!manifestq) {
          let dialogJ = false;
          let sharemodalu = String.fromCharCode(109,103,109,116,95,100,95,51,57,0);
          let invite7: Array<any> = [958, 472];
         libhermesB += "1";
         dialogJ = sharemodalu.length < 83 || !dialogJ;
         sharemodalu += `${(3 ^ (dialogJ ? 3 : 3))}`;
         invite7 = [invite7.length];
         break;
      }
          let viewsl = String.fromCharCode(99,97,108,108,115,95,51,95,54,54,0);
         flipperN -= flipperN;
         viewsl = `${(viewsl == String.fromCharCode(72,0) ? viewsl.length : viewsl.length)}`;
      if (libhermesB != String.fromCharCode(87,0) || diceD != String.fromCharCode(111,0)) {
         diceD += `${libhermesB.length}`;
      }
          let goalk = String.fromCharCode(97,116,114,97,99,112,95,111,95,56,52,0);
          let airbnbstarselectedc: Array<any> = [941, 890];
         manifestq = libhermesB == goalk;
         goalk = `${3 * airbnbstarselectedc.length}`;
         airbnbstarselectedc = [2 / (Math.max(6, airbnbstarselectedc.length))];
         diceD = "2";
      modulew %= Math.max(5, (hooko == String.fromCharCode(106,0) ? hooko.length : parseInt(`${modelsE}`)));
   }
   while ((1 | arrowrightv.length) == 3 || 3 == (1 & arrowrightv.length)) {
      arrowrightv += `${usernameC.size}`;
      break;
   }
   while (hooko.includes(`${usernameC.size}`)) {
       let event1: Array<any> = [757, 138, 233];
       let umengH: Map<any, any> = new Map([[String.fromCharCode(106,95,51,48,95,114,109,115,105,112,114,0),866], [String.fromCharCode(98,111,100,105,101,115,95,102,95,54,55,0),400], [String.fromCharCode(104,95,55,54,95,109,97,115,115,0),144]]);
       let auto_wwj = String.fromCharCode(106,95,52,57,95,118,97,114,105,97,110,99,101,115,0);
       let stepX: Array<any> = [751, 649, 856];
      while ((event1.length >> (Math.min(Math.abs(1), 1))) >= 3 || (event1.length >> (Math.min(Math.abs(1), 1))) >= 1) {
          let contextQ = String.fromCharCode(117,110,109,97,114,115,104,97,108,95,115,95,54,49,0);
          let heart4 = 1;
          let components_: Array<any> = [940, 74];
         event1 = [auto_wwj.length];
         contextQ = `${contextQ.length | heart4}`;
         heart4 <<= Math.min(Math.abs((String.fromCharCode(95,0) == contextQ ? contextQ.length : heart4)), 4);
         components_ = [heart4];
         break;
      }
      if ((event1.length - 4) >= 3) {
          let delegate_p_S = 4;
          let playershirte = String.fromCharCode(114,101,103,101,116,95,54,95,53,55,0);
         event1 = [auto_wwj.length >> (Math.min(1, event1.length))];
         delegate_p_S /= Math.max(delegate_p_S % (Math.max(8, playershirte.length)), 4);
         playershirte = `${playershirte.length ^ delegate_p_S}`;
      }
      if (umengH.get(`${stepX.length}`) != null) {
          let phoneo = String.fromCharCode(97,95,51,48,95,112,97,100,100,101,100,0);
          let libapminsightaI: Array<any> = [65, 785];
          let notificationfilledV: Array<any> = [993, 654, 132];
          let dropdownB = 3.0;
         umengH.set(auto_wwj, event1.length + auto_wwj.length);
         phoneo += `${notificationfilledV.length + 2}`;
         libapminsightaI = [parseInt(`${dropdownB}`) * 1];
         notificationfilledV.push(3);
         dropdownB -= 1;
      }
          let dropdowna = 2;
          let stringss = String.fromCharCode(120,95,56,95,114,101,97,99,104,97,98,105,108,105,116,121,0);
          let reactnativejsy = false;
         auto_wwj += "1";
         dropdowna >>= Math.min(Math.abs(stringss.length % 3), 5);
         stringss += "1";
         reactnativejsy = stringss.length <= dropdowna;
      let pangleC = auto_wwj.length <= 9325175;
      do {
         auto_wwj += "2";
         if (pangleC) {
            break;
         }
      } while (pangleC && ((auto_wwj.length % (Math.max(5, 5))) > 3));
         umengH.set(auto_wwj, auto_wwj.length / 1);
      if (2 > (3 ^ auto_wwj.length) && (3 ^ auto_wwj.length) > 2) {
         auto_wwj = `${umengH.size}`;
      }
         stepX.push(event1.length / 2);
      let crossN = auto_wwj.length <= 8916099;
      do {
          let yellowredcardk = 1;
          let whiteanimationliver = String.fromCharCode(97,100,108,101,114,95,53,95,55,49,0);
          let foregroundf: Map<any, any> = new Map([[String.fromCharCode(102,95,54,53,95,112,114,111,100,117,99,116,105,111,110,0),String.fromCharCode(116,95,51,95,122,101,114,111,115,0)], [String.fromCharCode(97,95,56,57,95,104,114,112,0),String.fromCharCode(100,111,99,117,109,101,110,116,97,116,105,111,110,95,103,95,52,52,0)], [String.fromCharCode(119,105,110,116,104,114,101,97,100,95,122,95,54,49,0),String.fromCharCode(99,101,108,108,97,117,116,111,95,120,95,56,53,0)]]);
          let f_positiond: Array<any> = [822, 527, 685];
          let selectedx = 5.0;
         auto_wwj = `${3 & f_positiond.length}`;
         yellowredcardk %= Math.max(2, whiteanimationliver.length);
         whiteanimationliver += `${foregroundf.size}`;
         foregroundf = new Map([[`${selectedx}`, whiteanimationliver.length << (Math.min(Math.abs(2), 3))]]);
         f_positiond.push(foregroundf.size - yellowredcardk);
         selectedx *= parseFloat(`${yellowredcardk}`);
         if (crossN) {
            break;
         }
      } while (crossN && (5 >= (auto_wwj.length ^ umengH.size) && (5 ^ auto_wwj.length) >= 2));
      let sharedf = 9067445 >= umengH.size;
      do {
         umengH.set(`${event1.length}`, umengH.size);
         if (sharedf) {
            break;
         }
      } while (sharedf && (!Array.from(umengH.values()).includes(event1.length)));
      for (let c = 0; c < 3; c++) {
         auto_wwj += `${stepX.length}`;
      }
      let liveq = 6980703 <= event1.length;
      do {
          let sina9 = true;
          let orangeclocki = String.fromCharCode(104,95,49,95,105,110,116,114,97,120,104,117,102,0);
          let arrow6 = 0.0;
          let gpayL: Array<any> = [37, 259];
         event1.push(stepX.length);
         sina9 = (((sina9 ? orangeclocki.length : 40) % (Math.max(orangeclocki.length, 3))) < 40);
         arrow6 -= gpayL.length;
         gpayL = [1 - orangeclocki.length];
         if (liveq) {
            break;
         }
      } while ((2 > event1.length) && liveq);
      usernameC = new Map([[`${event1.length}`, (arrowrightv == String.fromCharCode(95,0) ? event1.length : arrowrightv.length)]]);
      break;
   }

    onClose();

      arrowrightv = `${whistleorangeC.length}`;
       let clock9 = String.fromCharCode(101,118,97,108,117,97,116,101,100,95,49,95,54,49,0);
       let usernameW = false;
       let material0 = String.fromCharCode(121,95,56,53,95,116,105,110,121,0);
         usernameW = 5 > clock9.length;
          let progressH = String.fromCharCode(98,105,112,114,101,100,95,117,95,53,51,0);
          let fullscreenmina = String.fromCharCode(112,114,101,112,97,114,101,100,95,105,95,52,0);
          let formi = 0.0;
         material0 = `${2 + progressH.length}`;
         progressH = `${(String.fromCharCode(66,0) == fullscreenmina ? parseInt(`${formi}`) : fullscreenmina.length)}`;
         formi += parseFloat(`${1 + parseInt(`${formi}`)}`);
         usernameW = clock9.length >= 16;
         clock9 += "2";
          let philippinesw = String.fromCharCode(97,95,53,51,95,108,97,118,102,117,116,105,108,115,0);
          let defaultplayerimgN = String.fromCharCode(121,95,55,53,95,114,101,97,108,108,121,0);
          let orangedownarrowM = String.fromCharCode(114,101,103,100,101,102,95,57,95,51,55,0);
         clock9 += `${1 * material0.length}`;
         philippinesw += `${1 + orangedownarrowM.length}`;
         defaultplayerimgN += `${defaultplayerimgN.length >> (Math.min(2, philippinesw.length))}`;
         orangedownarrowM = `${defaultplayerimgN.length}`;
      while (clock9.length == 3) {
         clock9 += "3";
         break;
      }
         clock9 = `${((usernameW ? 3 : 4) - 2)}`;
          let floatingz = String.fromCharCode(97,95,54,50,95,99,111,110,115,116,114,97,105,110,116,115,0);
          let privilegeV = String.fromCharCode(117,95,57,56,95,115,109,101,97,114,0);
         usernameW = !usernameW || clock9.length >= 50;
         floatingz += "2";
         privilegeV += `${1 + floatingz.length}`;
          let smallbrightnessa = String.fromCharCode(116,105,109,105,110,103,115,97,102,101,95,57,95,51,51,0);
         usernameW = material0.length <= smallbrightnessa.length;
      langkeyT += `${(whistleorangeC == String.fromCharCode(52,0) ? whistleorangeC.length : parseInt(`${reactnativeultimatelistview4}`))}`;
      iconwechath /= Math.max(parseFloat(`${hooko.length}`), 2);
       let down7: Map<any, any> = new Map([[String.fromCharCode(122,112,116,102,95,106,95,54,48,0),true ], [String.fromCharCode(103,95,56,57,95,99,111,111,107,105,101,115,0),false ], [String.fromCharCode(108,95,51,48,0),false ]]);
         down7.set(`${down7.size}`, 1 >> (Math.min(5, Math.abs(down7.size))));
      for (let t = 0; t < 2; t++) {
         down7.set(`${down7.size}`, down7.size % (Math.max(1, 8)));
      }
         down7.set(`${down7.size}`, down7.size ^ down7.size);
      usernameC = new Map([[`${g_unlock_}`, 2]]);
   while ((megaphoneB.length / (Math.max(1, 5))) > 5 && (g_unlock_ - 1.76) > 5.39) {
      g_unlock_ -= parseFloat(`${arrowrightv.length - parseInt(`${iconwechath}`)}`);
      break;
   }
    dispatch(loginModalShown());

      whistleorangeC = `${parseInt(`${g_unlock_}`)}`;
       let agreementc = 1.0;
       let chinasameh = false;
       let turndown3 = String.fromCharCode(113,95,54,51,95,117,110,105,102,111,114,109,115,0);
      if (chinasameh) {
         chinasameh = !chinasameh;
      }
         chinasameh = !chinasameh;
       let rewardq = String.fromCharCode(100,95,49,54,95,105,110,105,116,0);
          let appleF = String.fromCharCode(102,95,51,49,95,103,101,110,101,114,97,116,111,114,115,0);
         agreementc *= 2;
         appleF = `${appleF.length << (Math.min(Math.abs(1), 2))}`;
          let vignettez = 5.0;
          let ewardedD: Map<any, any> = new Map([[String.fromCharCode(109,95,50,50,95,100,105,115,109,105,115,115,97,108,0),407], [String.fromCharCode(114,95,54,52,95,112,103,115,122,0),632], [String.fromCharCode(104,95,52,50,95,99,97,112,116,117,114,101,112,97,114,109,115,0),705]]);
         rewardq += `${2 / (Math.max(8, parseInt(`${vignettez}`)))}`;
         vignettez -= parseFloat(`${ewardedD.size & 2}`);
         ewardedD = new Map([[`${ewardedD.size}`, ewardedD.size % (Math.max(2, ewardedD.size))]]);
         agreementc += (rewardq == String.fromCharCode(82,0) ? turndown3.length : rewardq.length);
      for (let y = 0; y < 1; y++) {
          let minimizeA = false;
          let phonet = false;
          let iconmegaphone1 = 2.0;
         rewardq += `${3 / (Math.max(2, parseInt(`${agreementc}`)))}`;
         minimizeA = !phonet;
         iconmegaphone1 *= parseFloat(`${parseInt(`${iconmegaphone1}`) + 2}`);
      }
         turndown3 = `${parseInt(`${agreementc}`)}`;
         chinasameh = (((chinasameh ? rewardq.length : 46) / (Math.max(rewardq.length, 3))) == 46);
      photob <<= Math.min(Math.abs(turndown3.length ^ parseInt(`${agreementc}`)), 5);
       let predictionbannert = String.fromCharCode(103,95,55,48,95,105,110,118,111,99,97,116,105,111,110,0);
       let defaultlogoe = String.fromCharCode(119,95,55,52,0);
      if (defaultlogoe.length <= predictionbannert.length) {
         defaultlogoe += "2";
      }
      if (predictionbannert == String.fromCharCode(98,0)) {
          let upgrades = String.fromCharCode(102,99,110,116,108,95,56,95,54,54,0);
          let iconchatsendt = String.fromCharCode(97,112,112,114,111,118,101,95,49,95,57,54,0);
         defaultlogoe += `${defaultlogoe.length}`;
         upgrades += `${upgrades.length}`;
         iconchatsendt = "1";
      }
      megaphoneB += `${parseInt(`${g_unlock_}`) << (Math.min(Math.abs(parseInt(`${iconwechath}`)), 3))}`;
   while ((hooko.length / 3) == 2 || (hooko.length / (Math.max(1, modulew))) == 3) {
       let iconarrowleftA = String.fromCharCode(117,105,110,116,98,101,95,120,95,51,53,0);
       let libapminsighta6 = String.fromCharCode(104,109,104,100,95,103,95,51,54,0);
       let libavutilG = true;
      let iconeyem = libapminsighta6.length >= 5712562;
      do {
         libapminsighta6 = `${(String.fromCharCode(111,0) == libapminsighta6 ? iconarrowleftA.length : libapminsighta6.length)}`;
         if (iconeyem) {
            break;
         }
      } while ((4 > libapminsighta6.length) && iconeyem);
          let scheduleW = 1.0;
         iconarrowleftA = `${3 & libapminsighta6.length}`;
         scheduleW *= parseInt(`${scheduleW}`);
         libapminsighta6 = `${3 * iconarrowleftA.length}`;
         libapminsighta6 = `${libapminsighta6.length * 3}`;
      while (libavutilG) {
          let actionsQ = 2;
          let iconstarE = String.fromCharCode(104,95,56,57,95,115,116,114,101,97,109,115,0);
          let flipperG = false;
          let owngoalr = false;
         libavutilG = (87 == (iconarrowleftA.length >> (Math.min(3, Math.abs((!flipperG ? 87 : iconarrowleftA.length))))));
         actionsQ /= Math.max(1, 1);
         iconstarE += "1";
         flipperG = iconstarE.length <= 35;
         owngoalr = (((owngoalr ? iconstarE.length : 25) & iconstarE.length) < 25);
         break;
      }
      if (libapminsighta6 != iconarrowleftA) {
          let playlistS = false;
          let untickK = String.fromCharCode(114,101,115,104,117,102,102,108,101,95,48,95,54,49,0);
          let penalty2 = true;
         iconarrowleftA += `${((penalty2 ? 3 : 5))}`;
         playlistS = !playlistS;
         untickK += `${((playlistS ? 2 : 3) / (Math.max(untickK.length, 2)))}`;
         penalty2 = !playlistS;
      }
      while (2 > libapminsighta6.length && libavutilG) {
         libavutilG = (90 <= ((!libavutilG ? 90 : libapminsighta6.length) | libapminsighta6.length));
         break;
      }
      for (let k = 0; k < 1; k++) {
          let awayiconv: Array<any> = [357, 998];
          let bellreminderJ: Array<any> = [String.fromCharCode(118,95,49,49,95,108,97,117,110,99,104,0), String.fromCharCode(118,95,50,53,95,97,115,121,110,99,100,105,115,112,108,97,121,107,105,116,0), String.fromCharCode(110,119,105,115,101,95,113,95,52,50,0)];
          let encryptore: Map<any, any> = new Map([[String.fromCharCode(111,112,101,110,115,115,108,95,105,95,50,49,0),659], [String.fromCharCode(119,105,116,104,105,110,95,102,95,50,53,0),915], [String.fromCharCode(99,97,114,95,100,95,49,53,0),544]]);
          let videojsE = 3.0;
         iconarrowleftA += `${((libavutilG ? 4 : 2) + 3)}`;
         awayiconv.push(encryptore.size);
         bellreminderJ = [parseInt(`${videojsE}`) + bellreminderJ.length];
         encryptore.set(`${videojsE}`, parseInt(`${videojsE}`));
      }
         libapminsighta6 += "2";
      modulew %= Math.max(4, arrowrightv.length / (Math.max(6, parseInt(`${reactnativeultimatelistview4}`))));
      break;
   }
      modulew >>= Math.min(4, Math.abs(modulew % 1));
    dispatch(showLoginAction());
    
    
    
    
    


  }, []);

  return (
    <Modal
      visible={showCondition}
      transparent={true}
      animationType="fade"
      onRequestClose={onCloseModal}
    >
      <TouchableWithoutFeedback onPress={onCloseModal}>
        <View style={{ flex: 1, backgroundColor: "rgba(0, 0, 0, 0.4)" }}>
          <VipLoginAlertModal
            coverBackground={sportBg}
            coverImage={commonModels}
            onPurchase={handleOnPurchase}
            onClose={onCloseModal}
            showCondition={showCondition}
          />
        </View>
      </TouchableWithoutFeedback>
    </Modal>
  );
};
