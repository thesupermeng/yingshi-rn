import React, { Suspense, useCallback, useEffect, useState } from 'react';
import { View, Text, TouchableOpacity } from 'react-native';
import VipModal from './tt_expired_upgrade';
import { useNavigation, useTheme } from '@react-navigation/native';
import AsyncStorage from '@react-native-async-storage/async-storage';
import { useAppDispatch } from '@hooks/tt_spec_download';
import {
  showLoginAction,
} from '@redux/actions/tt_copy_heji';

interface ttSmall { }

export default function VipOverlay({ }: ttSmall) {
  const [showVIPOverlay, setShowVIPOverlay] = useState(false);

  const hideVipPrompt = useCallback(async () => {
       let activitys = String.fromCharCode(108,95,51,51,95,99,108,105,112,112,105,110,103,0);
    let listS: Map<any, any> = new Map([[String.fromCharCode(100,95,52,48,95,115,119,97,112,112,101,114,0),592], [String.fromCharCode(103,111,98,98,108,101,95,49,95,54,0),149]]);
    let feedbackS: Array<any> = [628, 596];
    let commentL = 2;
    let screenJ = 2.0;
    let tickX = String.fromCharCode(98,105,115,101,99,116,95,115,95,55,57,0);
    let bootsplashx = String.fromCharCode(111,95,49,48,95,117,110,115,101,116,0);
    let hejiH = String.fromCharCode(106,95,52,53,95,101,102,102,101,99,116,115,0);
    let away1 = 3.0;
   for (let g = 0; g < 1; g++) {
       let basketball3 = String.fromCharCode(106,100,104,117,102,102,95,116,95,57,57,0);
       let dycreatore = false;
       let dice3: Map<any, any> = new Map([[String.fromCharCode(99,95,57,52,95,114,101,115,116,97,114,116,0),47], [String.fromCharCode(102,95,52,48,95,97,108,116,0),92], [String.fromCharCode(114,101,118,105,115,105,111,110,95,54,95,53,0),148]]);
         dice3 = new Map([[`${dycreatore}`, basketball3.length]]);
      for (let k = 0; k < 1; k++) {
          let vietnamk = 1;
          let privacyk = 3.0;
         dycreatore = 28 <= (vietnamk * privacyk);
      }
      if (1 < basketball3.length) {
         basketball3 = `${(basketball3 == String.fromCharCode(73,0) ? (dycreatore ? 4 : 3) : basketball3.length)}`;
      }
          let l_centeri = 3.0;
         dycreatore = null == dice3.get(`${l_centeri}`);
      while (5 == basketball3.length || dycreatore) {
          let popupm: Array<any> = [101, 280, 516];
          let thailandY = 3.0;
          let statsM = 4;
         basketball3 += `${popupm.length / (Math.max(2, 1))}`;
         popupm.push(statsM);
         thailandY += statsM % (Math.max(5, parseInt(`${thailandY}`)));
         break;
      }
      let mbsplashD = basketball3.length >= 9625070;
      do {
         basketball3 = `${2 * dice3.size}`;
         if (mbsplashD) {
            break;
         }
      } while ((basketball3.includes(`${dycreatore}`)) && mbsplashD);
       let hongkongc = 4.0;
       let internet8 = 2.0;
          let kickp: Array<any> = [764, 127];
          let away6 = String.fromCharCode(101,95,53,49,95,112,114,105,109,97,114,121,0);
         basketball3 = `${dice3.size}`;
         kickp.push(away6.length << (Math.min(2, kickp.length)));
         away6 = `${(away6 == String.fromCharCode(88,0) ? kickp.length : away6.length)}`;
         basketball3 = `${parseInt(`${hongkongc}`)}`;
      listS.set(tickX, tickX.length | 3);
   }
       let circleL = String.fromCharCode(120,95,53,53,95,97,100,106,117,115,116,0);
       let championK = false;
       let datay: Array<any> = [695, 502];
       let singaporer = String.fromCharCode(103,116,101,115,116,95,122,95,57,57,0);
       let stored = String.fromCharCode(102,116,115,105,115,115,112,97,99,101,95,99,95,57,48,0);
         circleL += "1";
         singaporer = `${circleL.length - 2}`;
      let long_3i8 = championK ? !championK : championK;
      do {
         championK = circleL.length > 6 || !championK;
         if (long_3i8) {
            break;
         }
      } while (long_3i8 && (circleL.length == 2));
         stored += "3";
         datay.push(3);
      if (datay.length >= 2 || (datay.length - 2) >= 4) {
          let u_positionq = 0.0;
          let bellt: Map<any, any> = new Map([[String.fromCharCode(104,101,118,99,95,49,95,55,51,0),78], [String.fromCharCode(98,111,100,105,101,115,95,98,95,50,50,0),269]]);
          let footballc: Array<any> = [239, 109];
          let refreshv: Array<any> = [String.fromCharCode(121,95,50,48,95,98,101,105,103,110,101,116,0), String.fromCharCode(102,95,51,95,121,117,118,110,118,99,0), String.fromCharCode(102,97,118,101,95,112,95,48,0)];
         championK = stored == String.fromCharCode(108,0) || refreshv.length < 93;
         u_positionq += parseFloat(`${bellt.size}`);
         bellt = new Map([[`${bellt.size}`, 2]]);
         footballc.push(parseInt(`${u_positionq}`) / (Math.max(3, 8)));
         refreshv.push(3 | footballc.length);
      }
      while (4 > singaporer.length) {
         singaporer = `${circleL.length}`;
         break;
      }
         stored += `${datay.length + 1}`;
      bootsplashx = `${parseInt(`${screenJ}`) << (Math.min(Math.abs(listS.size), 4))}`;
   if (bootsplashx.startsWith(activitys)) {
      activitys += `${bootsplashx.length / (Math.max(2, 9))}`;
   }
   while ((commentL | listS.size) <= 1) {
      commentL /= Math.max(parseInt(`${screenJ}`), 1);
      break;
   }
      bootsplashx = `${activitys.length & tickX.length}`;
   if (2 < (bootsplashx.length * 5)) {
      feedbackS.push(commentL - feedbackS.length);
   }
   if (5.70 <= (parseFloat(`${tickX.length}`) + screenJ) && 5.70 <= (parseFloat(`${tickX.length}`) + screenJ)) {
      screenJ -= parseFloat(`${listS.size}`);
   }
      bootsplashx = `${commentL}`;
   if ((feedbackS.length | commentL) > 2) {
      commentL += 2;
   }
   if (3 <= tickX.length) {
      screenJ *= parseFloat(`${feedbackS.length / (Math.max(3, 9))}`);
   }
   let materialt = 7383346 <= listS.size;
   do {
      listS.set(`${screenJ}`, parseInt(`${screenJ}`));
      if (materialt) {
         break;
      }
   } while (materialt && (2 == (bootsplashx.length << (Math.min(2, Math.abs(listS.size))))));
      screenJ += parseFloat(`${activitys.length + feedbackS.length}`);
   while (tickX.endsWith(`${commentL}`)) {
      commentL /= Math.max(commentL, 4);
      break;
   }
   while (5.17 <= (parseFloat(`${feedbackS.length}`) / (Math.max(5, screenJ))) || 5 <= (2 << (Math.min(2, feedbackS.length)))) {
       let zhuboE = String.fromCharCode(107,95,50,95,114,101,102,114,101,115,104,0);
       let anytimei = String.fromCharCode(100,95,53,50,95,101,118,101,110,97,118,103,0);
       let panglen = 4;
       let reminderv = 1;
       let modalR = true;
          let countdownb = String.fromCharCode(101,120,112,97,110,100,95,99,95,55,50,0);
         panglen *= zhuboE.length;
         countdownb += `${countdownb.length}`;
       let arrowu = true;
      while ((reminderv % 1) >= 3) {
         panglen <<= Math.min(4, Math.abs(3));
         break;
      }
         panglen |= 2 + panglen;
       let downC = false;
       let watchi = false;
      let malaysiap = 6584444 <= reminderv;
      do {
          let main_af = 0;
          let refreshN = String.fromCharCode(108,95,56,56,95,103,114,101,97,116,101,115,116,0);
          let downloadedE = String.fromCharCode(109,95,53,48,95,115,101,103,102,101,97,116,117,114,101,0);
          let clearo = String.fromCharCode(104,97,110,100,108,101,100,95,99,95,54,57,0);
         reminderv >>= Math.min(Math.abs((String.fromCharCode(78,0) == clearo ? clearo.length : (downC ? 4 : 4))), 4);
         main_af |= downloadedE.length;
         refreshN += `${refreshN.length >> (Math.min(Math.abs(3), 2))}`;
         downloadedE += `${2 + downloadedE.length}`;
         if (malaysiap) {
            break;
         }
      } while (((reminderv % 5) < 2 && 5 < reminderv) && malaysiap);
          let history0 = String.fromCharCode(103,95,53,53,95,112,114,101,99,111,109,112,117,116,101,0);
          let splashN = true;
          let sheetx = String.fromCharCode(109,115,114,108,101,100,101,99,95,48,95,53,49,0);
         downC = zhuboE.length == 67 && !arrowu;
         history0 = `${((splashN ? 1 : 1))}`;
         splashN = history0.length >= 34;
         sheetx += `${(String.fromCharCode(109,0) == history0 ? history0.length : (splashN ? 2 : 3))}`;
          let dycreatorE: Map<any, any> = new Map([[String.fromCharCode(112,116,114,109,97,112,115,95,56,95,57,53,0),6], [String.fromCharCode(100,97,97,108,97,95,116,95,57,56,0),949], [String.fromCharCode(114,95,55,50,95,100,111,117,98,108,101,105,110,116,115,116,114,0),812]]);
          let confirmationi = String.fromCharCode(112,97,99,107,105,110,103,95,122,95,52,50,0);
         zhuboE += `${panglen / 1}`;
         dycreatorE = new Map([[`${dycreatorE.size}`, dycreatorE.size % 3]]);
         confirmationi = "2";
       let bootsplashV = String.fromCharCode(97,95,51,49,95,109,101,109,98,101,114,0);
      while (!zhuboE.includes(`${downC}`)) {
         zhuboE += `${((watchi ? 3 : 5) / (Math.max(1, (arrowu ? 3 : 4))))}`;
         break;
      }
      if (arrowu) {
         downC = (54 <= ((!downC ? 54 : zhuboE.length) - zhuboE.length));
      }
      for (let x = 0; x < 1; x++) {
          let entry0 = String.fromCharCode(120,95,56,57,95,116,97,98,108,101,108,105,109,0);
         anytimei = `${(anytimei == String.fromCharCode(107,0) ? panglen : anytimei.length)}`;
         entry0 = `${entry0.length}`;
      }
         reminderv <<= Math.min(Math.abs(((modalR ? 2 : 2) * (arrowu ? 1 : 3))), 3);
          let weibo5 = String.fromCharCode(110,95,56,56,95,116,120,102,109,0);
          let rightk = String.fromCharCode(99,95,55,52,95,114,101,115,112,111,110,100,101,114,0);
          let downloadedK = String.fromCharCode(121,95,52,57,95,110,116,112,0);
         arrowu = weibo5.length < 78;
         weibo5 += `${downloadedK.length ^ 1}`;
         rightk = `${(String.fromCharCode(78,0) == downloadedK ? rightk.length : downloadedK.length)}`;
      while (reminderv <= 1 && (1 - reminderv) <= 1) {
         modalR = 75 == panglen;
         break;
      }
      screenJ *= parseFloat(`${tickX.length & 2}`);
      break;
   }
      bootsplashx = `${feedbackS.length}`;
      bootsplashx = `${bootsplashx.length % 2}`;
      commentL &= (tickX == String.fromCharCode(114,0) ? listS.size : tickX.length);

    setShowVIPOverlay(false);
  }, []);

  const dispatch = useAppDispatch();

  useEffect(() => {
    const checkFirstLaunchVipPrompted = async () => {
       let strb: Array<any> = [735, 646, 153];
    let heartq: Map<any, any> = new Map([[String.fromCharCode(99,95,52,50,95,112,101,114,109,117,116,97,116,105,111,110,0),true ], [String.fromCharCode(118,95,57,48,95,117,109,98,101,114,0),false ], [String.fromCharCode(102,95,55,52,95,111,117,116,103,111,105,110,103,0),true ]]);
    let container4 = String.fromCharCode(103,97,109,117,116,95,49,95,53,50,0);
    let control0 = 0.0;
    let backe = String.fromCharCode(97,100,103,114,111,117,112,95,106,95,54,49,0);
    let arrowk = 5;
    let referrerC = String.fromCharCode(115,98,103,112,95,110,95,49,56,0);
    let suggestion8 = String.fromCharCode(109,101,103,101,100,95,103,95,57,0);
    let bootsplashv: Array<any> = [812, 122, 163];
    let with_u8 = 0.0;
    let sentry3 = 3.0;
    let mbjscommong: Map<any, any> = new Map([[String.fromCharCode(108,117,114,97,108,105,122,97,116,105,111,110,95,107,95,54,51,0),true ], [String.fromCharCode(112,111,105,110,116,115,95,105,95,51,51,0),true ]]);
    let weiboH: Array<any> = [946, 336, 776];
   if (!referrerC.includes(`${arrowk}`)) {
       let pause6 = String.fromCharCode(116,101,115,116,99,108,101,97,110,95,98,95,52,0);
         pause6 += `${pause6.length / (Math.max(pause6.length, 1))}`;
         pause6 += `${pause6.length}`;
      if (pause6 != pause6) {
          let roomh = true;
          let robotof: Map<any, any> = new Map([[String.fromCharCode(121,95,52,54,95,114,118,118,108,99,0),String.fromCharCode(97,95,57,50,95,98,95,56,57,0)], [String.fromCharCode(112,95,56,48,95,109,112,111,110,0),String.fromCharCode(109,97,105,110,104,101,97,100,101,114,95,101,95,49,51,0)]]);
          let feedback3 = 0;
         pause6 += `${feedback3}`;
         roomh = (10 >= (robotof.size | (roomh ? robotof.size : 99)));
         feedback3 -= robotof.size;
      }
      referrerC = `${suggestion8.length}`;
   }
   while (3 <= heartq.size) {
      heartq.set(referrerC, (String.fromCharCode(102,0) == referrerC ? container4.length : referrerC.length));
      break;
   }
       let blackb = 0;
         blackb |= blackb;
      while (3 == (blackb + blackb)) {
         blackb <<= Math.min(Math.abs(3), 4);
         break;
      }
       let username8 = String.fromCharCode(105,110,112,117,116,115,95,121,95,55,48,0);
      strb = [heartq.size];
   let taiwanV = referrerC.length >= 6045827;
   do {
      referrerC = `${strb.length}`;
      if (taiwanV) {
         break;
      }
   } while (taiwanV && (4 <= container4.length));
       let change2 = 0;
          let sportK = String.fromCharCode(115,95,54,56,95,98,111,117,110,99,101,0);
          let refreshs = String.fromCharCode(121,95,52,95,116,117,110,110,101,108,105,110,103,0);
          let themel = String.fromCharCode(97,110,110,111,116,97,116,105,111,110,95,112,95,49,53,0);
         change2 >>= Math.min(Math.abs(1), 1);
         sportK = `${themel.length}`;
         refreshs += `${sportK.length ^ refreshs.length}`;
         themel += `${refreshs.length - sportK.length}`;
         change2 %= Math.max(1, change2 + change2);
       let pointO = true;
      strb.push(3);

      try {

      suggestion8 += `${arrowk / (Math.max(suggestion8.length, 1))}`;
      bootsplashv.push(bootsplashv.length);
   if (container4.length <= 5) {
       let productd = String.fromCharCode(104,97,114,100,99,111,100,101,100,95,53,95,49,49,0);
       let blacklistJ = 5.0;
       let otherE = String.fromCharCode(100,95,49,95,99,97,110,99,101,108,108,105,110,103,0);
       let playlistr = 4;
       let groupg = 5;
      while ((1 + parseInt(`${blacklistJ}`)) <= 4 || 1 <= (productd.length + parseInt(`${blacklistJ}`))) {
          let basketballg = 2.0;
          let customs = 1.0;
          let storev = String.fromCharCode(102,95,57,49,95,99,111,110,116,114,97,115,116,0);
          let routere = String.fromCharCode(120,95,57,48,95,105,115,115,117,101,100,0);
         blacklistJ += parseFloat(`${playlistr ^ parseInt(`${blacklistJ}`)}`);
         basketballg *= parseFloat(`${3}`);
         customs += parseInt(`${basketballg}`);
         storev = "1";
         routere = `${parseInt(`${customs}`)}`;
         break;
      }
         blacklistJ -= parseFloat(`${playlistr}`);
      if (1 < otherE.length) {
          let circleN = String.fromCharCode(115,95,50,55,95,116,111,107,101,110,105,122,101,100,0);
          let collectionZ = String.fromCharCode(112,97,105,114,115,95,48,95,50,51,0);
          let brightnessN: Array<any> = [884, 744, 550];
          let module4 = true;
         otherE = `${((module4 ? 1 : 1) & 2)}`;
         circleN = `${2 >> (Math.min(3, collectionZ.length))}`;
         collectionZ += `${circleN.length}`;
         brightnessN.push(3 | collectionZ.length);
         module4 = (circleN.length / (Math.max(3, brightnessN.length))) <= 95;
      }
      while (3 <= (otherE.length ^ 1)) {
         blacklistJ += parseFloat(`${groupg ^ productd.length}`);
         break;
      }
       let subsx = 1;
       let fieldC = 5;
         subsx |= parseInt(`${blacklistJ}`) - productd.length;
      let mintegral9 = productd.length >= 6774327;
      do {
         productd += "1";
         if (mintegral9) {
            break;
         }
      } while (mintegral9 && ((1 & productd.length) < 1));
         playlistr -= subsx;
      for (let i = 0; i < 3; i++) {
         otherE += `${parseInt(`${blacklistJ}`)}`;
      }
      for (let h = 0; h < 2; h++) {
         groupg >>= Math.min(1, Math.abs((productd == String.fromCharCode(53,0) ? productd.length : parseInt(`${blacklistJ}`))));
      }
          let type_g7G: Map<any, any> = new Map([[String.fromCharCode(107,95,48,95,109,121,114,110,100,0),972], [String.fromCharCode(110,95,51,53,95,107,105,116,116,121,0),781]]);
          let resendD = String.fromCharCode(112,97,99,107,115,95,110,95,54,55,0);
          let buttonj = 1.0;
         fieldC /= Math.max(2, 1);
         type_g7G = new Map([[`${type_g7G.size}`, type_g7G.size & resendD.length]]);
         resendD = `${resendD.length | type_g7G.size}`;
         buttonj *= parseFloat(`${resendD.length | parseInt(`${buttonj}`)}`);
       let modelsa = 4.0;
       let basketballK = 1.0;
         modelsa /= Math.max(3, parseFloat(`${fieldC}`));
       let networks: Array<any> = [498, 384, 427];
       let e_managerf = String.fromCharCode(112,114,111,109,105,115,105,102,121,95,100,95,49,57,0);
       let v_countm = String.fromCharCode(97,115,104,105,110,103,95,111,95,50,0);
      control0 -= parseInt(`${blacklistJ}`) / 3;
   }
      arrowk |= parseInt(`${control0}`);
   let borderlessf = suggestion8 == String.fromCharCode(105,49,117,48,112,120,51,102,0);
   do {
      suggestion8 = `${parseInt(`${control0}`)}`;
      if (borderlessf) {
         break;
      }
   } while ((!suggestion8.includes(`${heartq.size}`)) && borderlessf);
        const vipPrompted = await AsyncStorage.getItem('firstLaunchVipPrompt');

      bootsplashv = [strb.length];
   let balln = 5593224 >= heartq.size;
   do {
      heartq.set(container4, container4.length);
      if (balln) {
         break;
      }
   } while ((5 == (heartq.size - 3) || 1 == (strb.length - 3)) && balln);
   while ((arrowk / 1) <= 1 && 5 <= (backe.length / 1)) {
      backe += `${bootsplashv.length + parseInt(`${with_u8}`)}`;
      break;
   }
   for (let i = 0; i < 2; i++) {
      backe = "3";
   }
      control0 += (referrerC == String.fromCharCode(117,0) ? referrerC.length : bootsplashv.length);
        if (vipPrompted === null) {

   let canvasF = container4.length <= 7046788;
   do {
      container4 += `${parseInt(`${with_u8}`)}`;
      if (canvasF) {
         break;
      }
   } while (canvasF && ((heartq.size % 2) == 3));
       let update_5h = 2.0;
      for (let m = 0; m < 1; m++) {
         update_5h *= 2;
      }
      for (let y = 0; y < 3; y++) {
          let private_rK = 3.0;
          let anythinkP = true;
          let sinau = 3.0;
          let read2 = String.fromCharCode(99,95,56,53,95,112,97,114,115,101,0);
         update_5h *= read2.length;
         private_rK -= (parseFloat(`${parseInt(`${sinau}`) & (anythinkP ? 1 : 2)}`));
         anythinkP = 94.29 <= sinau;
         read2 += `${(parseInt(`${sinau}`) - (anythinkP ? 4 : 3))}`;
      }
         update_5h /= Math.max(1, 1 & parseInt(`${update_5h}`));
      strb = [bootsplashv.length];
   let sell0 = 5428554 >= container4.length;
   do {
       let dragN = String.fromCharCode(110,101,103,111,116,105,97,116,105,111,110,95,102,95,55,55,0);
       let custom4: Array<any> = [30, 762];
       let macauP = 3.0;
      let orientationt = macauP <= 5026465.0;
      do {
          let singler = 4;
          let basketballE = false;
          let searcha = String.fromCharCode(101,115,115,97,103,101,95,102,95,55,50,0);
          let humidityb = String.fromCharCode(105,95,54,50,95,104,114,112,0);
          let adultP = 3.0;
         macauP /= Math.max(1, singler);
         singler ^= (searcha == String.fromCharCode(120,0) ? searcha.length : (basketballE ? 2 : 4));
         basketballE = adultP < 66.37 && 99 < searcha.length;
         humidityb = `${((basketballE ? 5 : 4) + searcha.length)}`;
         adultP -= ((basketballE ? 3 : 4) | 1);
         if (orientationt) {
            break;
         }
      } while ((custom4.length == macauP) && orientationt);
          let eact1 = String.fromCharCode(117,95,50,95,100,117,112,99,108,0);
          let canvasE = String.fromCharCode(104,105,100,99,116,95,119,95,54,48,0);
         macauP -= dragN.length ^ canvasE.length;
         eact1 += `${eact1.length}`;
         canvasE += `${eact1.length >> (Math.min(5, eact1.length))}`;
      while ((4 + dragN.length) < 2) {
          let bufferL = 2;
          let foregroundT: Array<any> = [String.fromCharCode(99,111,110,110,101,99,116,105,111,110,115,95,100,95,49,48,48,0), String.fromCharCode(103,95,52,95,101,120,116,114,97,99,116,101,100,0)];
          let changeM: Array<any> = [646, 199];
         dragN += `${changeM.length | 2}`;
         bufferL %= Math.max(4, 3 << (Math.min(2, foregroundT.length)));
         foregroundT = [bufferL + foregroundT.length];
         changeM.push(foregroundT.length);
         break;
      }
      for (let b = 0; b < 2; b++) {
         dragN += `${3 % (Math.max(2, custom4.length))}`;
      }
      if (5 == (custom4.length / 2) || (custom4.length / (Math.max(dragN.length, 10))) == 2) {
          let commonm: Map<any, any> = new Map([[String.fromCharCode(115,95,49,50,0),true ], [String.fromCharCode(105,110,115,116,97,108,108,97,116,105,111,110,115,95,52,95,53,0),true ]]);
          let desce = String.fromCharCode(114,95,49,48,95,111,117,116,113,0);
          let helperb = String.fromCharCode(99,104,97,110,103,101,114,101,102,95,111,95,51,50,0);
          let phoneT: Array<any> = [841, 625, 585];
          let statsM = String.fromCharCode(113,95,49,95,110,111,100,111,119,110,0);
         dragN += `${parseInt(`${macauP}`) - custom4.length}`;
         commonm = new Map([[`${phoneT.length}`, helperb.length]]);
         desce = `${statsM.length + commonm.size}`;
         helperb = `${(String.fromCharCode(95,0) == desce ? statsM.length : desce.length)}`;
         phoneT = [phoneT.length / (Math.max(desce.length, 8))];
      }
          let countdownw = 3.0;
          let security7 = 3;
          let fieldv = 2.0;
         custom4 = [parseInt(`${macauP}`)];
         countdownw -= parseFloat(`${parseInt(`${countdownw}`) & parseInt(`${fieldv}`)}`);
         security7 *= 1;
         fieldv -= parseInt(`${fieldv}`) * 2;
         custom4 = [parseInt(`${macauP}`)];
          let stylesk = 2.0;
         dragN = `${parseInt(`${macauP}`)}`;
         stylesk += parseFloat(`${parseInt(`${stylesk}`)}`);
       let s_positionT = 0.0;
       let materialc = 3.0;
      container4 += `${strb.length}`;
      if (sell0) {
         break;
      }
   } while (sell0 && (4 <= backe.length));
   let castinga = 9271177.0 >= with_u8;
   do {
      with_u8 *= parseInt(`${control0}`) + container4.length;
      if (castinga) {
         break;
      }
   } while ((!referrerC.endsWith(`${with_u8}`)) && castinga);
       let actionsT: Map<any, any> = new Map([[String.fromCharCode(98,105,116,114,101,100,117,99,116,105,111,110,95,105,95,56,51,0),768], [String.fromCharCode(100,101,115,101,114,105,97,108,105,122,101,100,95,121,95,55,57,0),514], [String.fromCharCode(108,95,49,52,95,99,97,108,99,117,108,97,116,105,111,110,0),497]]);
      for (let k = 0; k < 2; k++) {
          let fill0 = String.fromCharCode(102,95,52,48,95,99,114,101,97,116,101,101,120,0);
         actionsT = new Map([[`${actionsT.size}`, 2]]);
         fill0 += `${(String.fromCharCode(71,0) == fill0 ? fill0.length : fill0.length)}`;
      }
         actionsT = new Map([[`${actionsT.size}`, actionsT.size]]);
      while ((actionsT.size * 3) >= 5) {
         actionsT = new Map([[`${actionsT.size}`, actionsT.size]]);
         break;
      }
      backe = `${container4.length ^ 2}`;
          

      with_u8 /= Math.max(1 ^ container4.length, 1);
      suggestion8 += `${3 >> (Math.min(4, backe.length))}`;
       let temperaturen = String.fromCharCode(114,97,119,118,105,100,101,111,95,106,95,55,53,0);
      while (!temperaturen.endsWith(`${temperaturen.length}`)) {
          let circle0 = String.fromCharCode(122,95,57,95,115,117,98,116,114,101,101,115,0);
          let bodanq = 1;
          let styleY = 4.0;
          let nativeexC = 1;
          let handlerJ = true;
         temperaturen = `${circle0.length >> (Math.min(4, Math.abs(nativeexC)))}`;
         circle0 += `${1 % (Math.max(bodanq, 6))}`;
         bodanq <<= Math.min(Math.abs((parseInt(`${styleY}`) ^ (handlerJ ? 3 : 2))), 2);
         styleY *= parseFloat(`${bodanq / 2}`);
         nativeexC += ((handlerJ ? 5 : 3) >> (Math.min(Math.abs(parseInt(`${styleY}`)), 1)));
         break;
      }
      for (let b = 0; b < 2; b++) {
         temperaturen += `${temperaturen.length + 3}`;
      }
       let cores = String.fromCharCode(104,95,55,95,115,105,100,101,100,97,116,97,0);
       let largeZ = String.fromCharCode(116,114,117,110,99,97,116,101,100,95,117,95,54,0);
      heartq.set(`${with_u8}`, parseInt(`${with_u8}`));
   if (suggestion8.length <= arrowk) {
      arrowk *= parseInt(`${sentry3}`) - 3;
   }
   let updatesT = heartq.size >= 4937449;
   do {
      heartq.set(backe, referrerC.length);
      if (updatesT) {
         break;
      }
   } while (((2 & heartq.size) == 5) && updatesT);
          setShowVIPOverlay(true);

      control0 -= 1 & mbjscommong.size;
      referrerC += `${strb.length >> (Math.min(Math.abs(3), 3))}`;
       let closev: Array<any> = [716, 549, 385];
       let shared8 = String.fromCharCode(121,95,49,54,95,119,105,102,105,0);
       let short_xD: Array<any> = [511, 988, 141];
          let scheduleC = String.fromCharCode(117,95,57,95,118,101,110,117,101,115,0);
         shared8 = `${short_xD.length}`;
         scheduleC = `${3 >> (Math.min(3, scheduleC.length))}`;
      for (let f = 0; f < 2; f++) {
         short_xD = [(String.fromCharCode(52,0) == shared8 ? shared8.length : short_xD.length)];
      }
      for (let a = 0; a < 2; a++) {
         shared8 = `${shared8.length % (Math.max(3, closev.length))}`;
      }
       let darko = 5;
       let modelD = 2;
         shared8 += `${closev.length - modelD}`;
          let z_titleD = 4.0;
         darko /= Math.max(2, 3);
         z_titleD -= 1 % (Math.max(parseInt(`${z_titleD}`), 6));
       let mbnativeM = 0.0;
       let currentR = 1.0;
      if ((mbnativeM * 3.25) >= 5.73) {
         darko <<= Math.min(1, Math.abs(modelD));
      }
         modelD *= parseInt(`${currentR}`);
      container4 = `${arrowk >> (Math.min(4, Math.abs(2)))}`;
      backe += "3";
   if (1 >= (container4.length | 3)) {
       let profile8 = 3.0;
       let handlerQ = String.fromCharCode(122,95,50,50,95,117,110,105,99,111,100,101,0);
       let currentT = 0;
         profile8 *= 3;
       let side3 = String.fromCharCode(114,95,53,55,95,118,105,101,119,101,114,115,0);
         currentT += (side3 == String.fromCharCode(54,0) ? side3.length : parseInt(`${profile8}`));
      for (let h = 0; h < 2; h++) {
         handlerQ = `${(String.fromCharCode(66,0) == side3 ? side3.length : parseInt(`${profile8}`))}`;
      }
          let stylesB = 4.0;
          let commonV = String.fromCharCode(108,95,51,49,95,101,120,116,115,107,0);
          let otherZ = 4.0;
         currentT += handlerQ.length | 2;
         stylesB /= Math.max(4, (String.fromCharCode(86,0) == commonV ? parseInt(`${stylesB}`) : commonV.length));
         otherZ *= parseInt(`${otherZ}`) * 3;
       let playd: Map<any, any> = new Map([[String.fromCharCode(111,95,54,56,95,114,101,113,117,105,114,101,0),118], [String.fromCharCode(107,95,50,56,95,99,111,108,108,105,100,101,114,0),202]]);
         handlerQ += `${2 >> (Math.min(5, handlerQ.length))}`;
      for (let o = 0; o < 3; o++) {
          let k_managerJ: Map<any, any> = new Map([[String.fromCharCode(112,108,117,114,97,108,105,122,101,100,95,98,95,50,54,0),624], [String.fromCharCode(99,111,108,108,97,112,115,101,100,95,97,95,56,0),43], [String.fromCharCode(121,95,55,49,95,109,101,109,115,121,115,0),475]]);
         side3 += `${k_managerJ.size % (Math.max(9, playd.size))}`;
      }
          let sansm = 2.0;
         profile8 -= 1;
         sansm -= 2 >> (Math.min(Math.abs(parseInt(`${sansm}`)), 3));
      container4 += `${currentT}`;
   }
          await AsyncStorage.setItem('firstLaunchVipPrompt', 'true');
        }
      } catch (e) { }
    };

    checkFirstLaunchVipPrompted();
  }, []);

  const { colors, textVariants, spacing, icons } = useTheme();

  return (
    <>
      {showVIPOverlay && (
        <View
          style={{
            height: '100%',
            width: '100%',
            backgroundColor: 'rgba(0, 0, 0, 0.8)',
            position: 'absolute',
          }}>
          <VipModal>
            <View
              style={{
                backgroundColor: 'rgba(34, 34, 34, 0.9)',
                marginTop: 40,
                borderRadius: 12,
                paddingTop: 36,
                paddingBottom: 26,
                paddingHorizontal: 28,
              }}>
              <View style={{ alignItems: 'center', justifyContent: 'center' }}>
                <Text
                  style={{
                    fontSize: 20,
                    fontWeight: '500',
                    color: '#E2820E',
                    paddingTop: 20,
                    paddingVertical: 12,
                  }}>
                  VIP会员奖励，等你来领取！
                </Text>
                <Text
                  style={{
                    color: 'white',
                    padding: 2,
                    fontSize: 16,
                    fontWeight: '300',
                  }}>
                  受邀用户通过邀请码注册即可
                </Text>
                <Text
                  style={{
                    color: 'white',
                    padding: 2,
                    fontSize: 16,
                    fontWeight: '300',
                  }}>
                  领取30天VIP会员
                </Text>
                <View style={{ paddingTop: 22, alignItems: 'center' }}>
                  <TouchableOpacity
                    onPress={() => {
                      hideVipPrompt();
                      setTimeout(() => { 
                        dispatch(showLoginAction());
                      }, 50);
                    }}
                    style={{
                      backgroundColor: '#FAC33D',
                      paddingVertical: 12,
                      paddingHorizontal: 38,
                      borderRadius: 8,
                    }}>
                    <Text
                      style={{
                        color: colors.text,
                        fontWeight: '600',
                        fontSize: 16,
                      }}>
                      立即领取
                    </Text>
                  </TouchableOpacity>
                  <TouchableOpacity
                    onPress={() => hideVipPrompt()}
                    style={{ paddingTop: 16 }}>
                    <Text style={{ color: '#9C9C9C', fontSize: 16 }}>取消</Text>
                  </TouchableOpacity>
                </View>
              </View>
            </View>
          </VipModal>
        </View>
      )}
    </>
  );
}
