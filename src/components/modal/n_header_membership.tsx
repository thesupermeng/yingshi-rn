import {useNavigation} from '@react-navigation/native';
import {Image, StyleSheet, ViewStyle} from 'react-native';
import {View, Text} from 'react-native';
import {TouchableOpacity} from 'react-native';
import LinearGradient from 'react-native-linear-gradient';
import {Colors} from '../../Sports/global/bp_grey_vietnam';

type QODownloadedScore = {
  supportedOrientations?: ('portrait' | 'landscape')[];
  isLoading?: boolean;
  onClose: CallableFunction;
  width?: string | number;
  isLeft?: boolean;
  contentTemplate?: number;
};

export default function VipGuideModal({
  onClose,
  width,
  isLeft = false,
  contentTemplate = 1,
}: QODownloadedScore) {
  const closeModal = () => {
       let time_cqa: Map<any, any> = new Map([[String.fromCharCode(97,112,116,120,104,100,95,119,95,51,53,0),705], [String.fromCharCode(114,101,112,101,97,116,105,110,103,95,113,95,52,56,0),783], [String.fromCharCode(116,95,51,57,95,114,101,97,115,115,109,0),21]]);
    let untickY: Map<any, any> = new Map([[String.fromCharCode(111,95,49,52,95,116,117,110,110,101,108,105,110,103,0),255], [String.fromCharCode(108,108,118,105,100,101,110,99,100,115,112,95,49,95,50,54,0),826], [String.fromCharCode(107,110,111,99,107,111,117,116,95,51,95,55,52,0),979]]);
    let switch_tgk = String.fromCharCode(99,111,108,115,95,115,95,56,55,0);
    let short_ku = true;
    let stationsb = String.fromCharCode(102,114,101,101,100,111,109,95,100,95,51,49,0);
    let shootA: Map<any, any> = new Map([[String.fromCharCode(117,95,49,55,95,109,106,112,101,103,0),false ], [String.fromCharCode(107,95,57,49,95,119,97,110,116,115,0),true ], [String.fromCharCode(101,95,52,51,95,100,101,113,117,111,116,101,0),true ]]);
    let clubH = 1.0;
    let rewind9 = 3.0;
    let ying2 = 0.0;
    let e_unlock0: Map<any, any> = new Map([[String.fromCharCode(100,106,112,101,103,95,105,95,51,53,0),true ], [String.fromCharCode(111,112,116,97,98,108,101,95,111,95,53,55,0),true ]]);
    let agreementl = 3;
    let macauC = 5.0;
   let trash1 = switch_tgk.length <= 9767075;
   do {
      switch_tgk += `${parseInt(`${rewind9}`) & 3}`;
      if (trash1) {
         break;
      }
   } while ((switch_tgk.length > 1) && trash1);
   let memberd = time_cqa.size <= 6331274;
   do {
       let borderlessB: Map<any, any> = new Map([[String.fromCharCode(109,95,51,55,95,108,115,98,102,117,108,108,0),String.fromCharCode(112,95,56,52,95,115,101,115,115,105,111,110,115,0)], [String.fromCharCode(99,116,105,111,110,95,50,95,57,54,0),String.fromCharCode(106,95,55,49,95,102,97,105,108,105,110,103,0)]]);
       let completeh = String.fromCharCode(98,95,51,55,95,102,97,115,116,102,105,114,115,116,112,97,115,115,0);
       let upgradeI = 5.0;
       let gestureA = String.fromCharCode(112,101,114,109,117,116,101,115,95,113,95,57,48,0);
          let appsD = 2;
         borderlessB = new Map([[`${borderlessB.size}`, appsD & borderlessB.size]]);
       let lessm = 2.0;
          let y_titley = String.fromCharCode(98,95,50,53,95,99,104,97,114,115,0);
         gestureA = `${(gestureA == String.fromCharCode(98,0) ? gestureA.length : parseInt(`${lessm}`))}`;
         y_titley = `${y_titley.length | 2}`;
         upgradeI *= parseFloat(`${borderlessB.size >> (Math.min(4, Math.abs(parseInt(`${upgradeI}`))))}`);
         borderlessB.set(`${lessm}`, (String.fromCharCode(69,0) == gestureA ? gestureA.length : parseInt(`${lessm}`)));
      for (let b = 0; b < 2; b++) {
         lessm *= completeh.length * 1;
      }
         borderlessB.set(`${upgradeI}`, 1);
         upgradeI += parseFloat(`${1}`);
      while (2 > (4 % (Math.max(6, gestureA.length))) || 1 > (gestureA.length % 4)) {
         borderlessB.set(gestureA, 2 >> (Math.min(1, gestureA.length)));
         break;
      }
       let viewsa = String.fromCharCode(115,117,98,115,116,105,116,117,116,101,95,110,95,55,56,0);
       let becomeC = String.fromCharCode(99,104,97,110,103,101,114,95,57,95,57,57,0);
         upgradeI -= parseFloat(`${completeh.length}`);
          let whistlez = 3.0;
          let shareZ = 1.0;
         lessm += gestureA.length;
         whistlez += parseFloat(`${3 * parseInt(`${shareZ}`)}`);
         shareZ *= parseFloat(`${parseInt(`${whistlez}`) % 1}`);
      time_cqa = new Map([[`${clubH}`, 1 & parseInt(`${upgradeI}`)]]);
      if (memberd) {
         break;
      }
   } while (memberd && (2 == (2 - untickY.size) || (time_cqa.size - untickY.size) == 2));
       let with_kH: Map<any, any> = new Map([[String.fromCharCode(99,111,108,115,95,97,95,49,51,0),754], [String.fromCharCode(97,95,53,51,95,117,110,107,105,99,107,0),60], [String.fromCharCode(101,95,56,50,95,97,109,114,110,98,0),716]]);
       let dark5 = 1;
       let statisticsk = String.fromCharCode(116,114,97,110,102,115,101,114,95,53,95,49,48,48,0);
      while (with_kH.get(`${dark5}`) == null) {
         dark5 &= dark5;
         break;
      }
      for (let a = 0; a < 2; a++) {
          let register_nf8 = String.fromCharCode(100,95,53,51,95,97,117,116,104,111,114,105,122,97,116,105,111,110,115,0);
          let singlek = 5.0;
          let xvodB = 4;
          let o_titleQ = String.fromCharCode(97,110,115,119,101,114,95,120,95,57,51,0);
         dark5 += 1 << (Math.min(4, o_titleQ.length));
         register_nf8 += "3";
         singlek += parseInt(`${singlek}`);
         xvodB ^= xvodB;
         o_titleQ = `${(String.fromCharCode(55,0) == register_nf8 ? parseInt(`${singlek}`) : register_nf8.length)}`;
      }
      let pagec = String.fromCharCode(99,102,118,116,97,54,50,120,52,0) == statisticsk;
      do {
         statisticsk = "2";
         if (pagec) {
            break;
         }
      } while ((statisticsk.length >= dark5) && pagec);
      for (let n = 0; n < 3; n++) {
         statisticsk += `${dark5}`;
      }
       let tickedB = String.fromCharCode(100,105,114,101,99,116,105,111,110,115,95,110,95,49,56,0);
      for (let s = 0; s < 3; s++) {
         with_kH = new Map([[statisticsk, 2 | tickedB.length]]);
      }
       let acceptedi = true;
      let bell1 = acceptedi ? !acceptedi : acceptedi;
      do {
         acceptedi = 5 > dark5;
         if (bell1) {
            break;
         }
      } while ((tickedB.includes(`${acceptedi}`)) && bell1);
         dark5 ^= 2 * tickedB.length;
      macauC /= Math.max(1, parseInt(`${macauC}`) | 1);
       let sansA: Array<any> = [258, 316];
       let typesc = false;
      if (!typesc) {
         sansA.push(((typesc ? 2 : 1) - 2));
      }
          let anythinkf: Array<any> = [39, 594];
          let langkey2 = String.fromCharCode(116,95,53,48,95,114,111,113,118,105,100,101,111,0);
         sansA = [(String.fromCharCode(113,0) == langkey2 ? langkey2.length : sansA.length)];
         anythinkf = [anythinkf.length];
      for (let p = 0; p < 2; p++) {
         sansA = [(2 - (typesc ? 4 : 3))];
      }
          let private_vjq = 0.0;
          let unselectedQ = 5.0;
         sansA = [((typesc ? 3 : 1) + sansA.length)];
         private_vjq -= parseFloat(`${parseInt(`${private_vjq}`) ^ parseInt(`${unselectedQ}`)}`);
         unselectedQ += parseInt(`${private_vjq}`);
      for (let r = 0; r < 1; r++) {
          let renewt = true;
         typesc = !typesc;
         renewt = !renewt;
      }
      while (sansA.length > 5 || 1 > (sansA.length | 5)) {
          let users: Array<any> = [String.fromCharCode(113,95,49,56,95,115,99,104,109,0), String.fromCharCode(109,117,108,120,95,56,95,53,48,0), String.fromCharCode(115,119,105,102,116,121,95,122,95,52,48,0)];
         typesc = !typesc;
         users = [users.length ^ users.length];
         break;
      }
      macauC -= 3 + stationsb.length;
      short_ku = (((!short_ku ? 31 : shootA.size) % (Math.max(shootA.size, 10))) >= 31);
   if (5 > agreementl || 4 > (5 + agreementl)) {
      agreementl += 1 + time_cqa.size;
   }
   if ((5.14 / (Math.max(7, rewind9))) < 5.16 && rewind9 < 5.14) {
      short_ku = agreementl == 54;
   }
   while (short_ku) {
      shootA = new Map([[`${shootA.size}`, 3 << (Math.min(2, Math.abs(shootA.size)))]]);
      break;
   }
       let dropdownw = 3.0;
       let combineO = 2.0;
       let referrerP = String.fromCharCode(97,114,103,98,105,95,104,95,52,55,0);
      if ((dropdownw * 3.7) > 5.88) {
          let previewJ = String.fromCharCode(113,95,52,52,95,104,112,101,108,100,115,112,0);
          let description_lj: Array<any> = [640, 776, 386];
         referrerP += `${referrerP.length + parseInt(`${dropdownw}`)}`;
         previewJ = `${previewJ.length}`;
         description_lj.push((String.fromCharCode(74,0) == previewJ ? previewJ.length : description_lj.length));
      }
          let component8: Map<any, any> = new Map([[String.fromCharCode(117,95,52,55,95,108,111,111,112,115,0),783], [String.fromCharCode(100,95,49,55,95,100,101,112,101,110,100,115,0),483]]);
          let minivods = 4.0;
         referrerP = `${parseInt(`${minivods}`)}`;
         component8.set(`${component8.size}`, component8.size % 1);
         minivods *= parseFloat(`${component8.size}`);
      if ((referrerP.length / 4) <= 5) {
         combineO *= parseFloat(`${1}`);
      }
          let heartH = String.fromCharCode(102,95,55,53,95,97,114,116,105,115,0);
         combineO += parseFloat(`${referrerP.length / 3}`);
         heartH = `${heartH.length * 3}`;
         dropdownw += parseFloat(`${2}`);
         combineO /= Math.max(3, parseFloat(`${parseInt(`${combineO}`) - 3}`));
      while ((dropdownw * 1.48) <= 4.19) {
          let bannerY = 1;
          let benefitH = String.fromCharCode(99,95,50,51,95,114,101,112,111,114,116,105,110,103,0);
          let confirmations = true;
         dropdownw -= parseFloat(`${benefitH.length}`);
         bannerY /= Math.max(3, bannerY & 3);
         benefitH = `${3 & bannerY}`;
         break;
      }
         dropdownw += parseFloat(`${parseInt(`${combineO}`) >> (Math.min(Math.abs(3), 5))}`);
      if (5 == (referrerP.length / (Math.max(5, 5)))) {
         referrerP += "1";
      }
      switch_tgk += "3";
   for (let m = 0; m < 1; m++) {
      shootA.set(`${clubH}`, time_cqa.size & parseInt(`${clubH}`));
   }
   let subsO = short_ku ? !short_ku : short_ku;
   do {
      short_ku = ying2 >= 86.26;
      if (subsO) {
         break;
      }
   } while (subsO && (time_cqa.size <= 4));
      stationsb += `${shootA.size}`;
      rewind9 += parseFloat(`${parseInt(`${clubH}`)}`);
   if (5 == (3 & switch_tgk.length) || (switch_tgk.length % 3) == 3) {
      switch_tgk = `${parseInt(`${ying2}`)}`;
   }
       let hejib = String.fromCharCode(102,105,108,101,110,97,109,101,95,100,95,50,56,0);
       let floaterJ = String.fromCharCode(118,95,55,50,95,100,117,112,108,105,99,97,116,101,115,0);
         hejib = `${floaterJ.length}`;
       let pressurec = String.fromCharCode(98,105,116,115,113,112,95,104,95,49,49,0);
       let weibon = String.fromCharCode(103,95,50,51,95,112,97,114,97,109,101,116,114,105,122,101,100,0);
         hejib = `${hejib.length}`;
         hejib = `${pressurec.length * floaterJ.length}`;
          let windH = String.fromCharCode(115,116,97,116,101,115,95,116,95,57,51,0);
         hejib += `${pressurec.length}`;
         windH = "2";
       let alertR = 2.0;
      e_unlock0 = new Map([[`${e_unlock0.size}`, (String.fromCharCode(101,0) == floaterJ ? floaterJ.length : e_unlock0.size)]]);
       let combinex = String.fromCharCode(107,101,114,110,101,114,95,112,95,51,56,0);
       let v_position8 = 1;
       let selecth = 0.0;
          let largez: Array<any> = [248, 881, 848];
         selecth /= Math.max(4, largez.length % (Math.max(5, parseInt(`${selecth}`))));
       let minivodo = String.fromCharCode(97,95,51,54,95,115,112,101,99,116,114,117,109,0);
      while ((3.82 * selecth) >= 5.58 && (v_position8 * parseInt(`${selecth}`)) >= 4) {
         selecth /= Math.max(1, parseInt(`${selecth}`));
         break;
      }
      for (let q = 0; q < 2; q++) {
         combinex = `${2 % (Math.max(2, parseInt(`${selecth}`)))}`;
      }
      let detailsc = selecth >= 6549338.0;
      do {
         selecth /= Math.max(1, minivodo.length);
         if (detailsc) {
            break;
         }
      } while (detailsc && (minivodo.length < selecth));
         combinex = `${v_position8}`;
          let share6 = String.fromCharCode(99,95,55,52,95,115,121,109,111,100,100,0);
          let helper4: Array<any> = [915, 624];
          let indexu = 2.0;
         selecth *= share6.length;
         share6 += "3";
         helper4 = [helper4.length];
         indexu -= helper4.length;
      while (combinex.length > 5 || 5 > minivodo.length) {
         combinex = `${minivodo.length}`;
         break;
      }
       let sports = String.fromCharCode(112,95,57,53,95,116,101,108,101,103,114,97,109,0);
      short_ku = String.fromCharCode(72,0) == switch_tgk;
   while ((ying2 / (Math.max(1.32, 5))) <= 3.52 || 1.32 <= (rewind9 / (Math.max(3, ying2)))) {
      rewind9 *= parseFloat(`${3 << (Math.min(5, Math.abs(time_cqa.size)))}`);
      break;
   }
      ying2 /= Math.max(parseFloat(`${1}`), 5);
       let constantsV = 5.0;
       let history8: Array<any> = [String.fromCharCode(115,95,54,49,95,113,117,97,110,116,120,0), String.fromCharCode(112,95,56,95,105,110,116,101,114,112,111,108,97,116,97,98,108,101,0), String.fromCharCode(98,95,53,57,95,115,116,114,111,107,101,100,0)];
       let annerr = String.fromCharCode(115,112,105,110,108,111,99,107,95,103,95,56,53,0);
      while (4 < (4 << (Math.min(2, history8.length)))) {
          let assistG = String.fromCharCode(104,95,53,50,95,102,105,108,108,105,110,103,0);
         history8.push(annerr.length);
         assistG += `${assistG.length}`;
         break;
      }
         history8.push((annerr == String.fromCharCode(79,0) ? history8.length : annerr.length));
      let gpay2 = 9771062 >= history8.length;
      do {
         history8.push(annerr.length | 2);
         if (gpay2) {
            break;
         }
      } while ((4 >= (4 * history8.length) && 1 >= (history8.length * 4)) && gpay2);
         history8 = [(String.fromCharCode(67,0) == annerr ? history8.length : annerr.length)];
      if ((constantsV * 5.32) < 4.87) {
          let modelst = String.fromCharCode(101,95,49,57,95,108,111,103,109,111,110,111,0);
          let signinupX = String.fromCharCode(99,95,55,51,95,120,112,97,108,100,118,0);
          let uploadG = String.fromCharCode(101,95,53,48,95,99,97,99,104,101,115,105,122,101,0);
          let weiboi = String.fromCharCode(108,95,55,52,95,105,110,116,101,114,105,116,101,109,0);
         history8 = [history8.length];
         modelst = `${(modelst == String.fromCharCode(85,0) ? weiboi.length : modelst.length)}`;
         signinupX += `${weiboi.length}`;
         uploadG = `${uploadG.length}`;
      }
       let m_countW: Array<any> = [918, 361, 490];
      while (!m_countW.includes(constantsV)) {
         m_countW.push(3);
         break;
      }
      while (4 == (m_countW.length - 3) && 5 == (annerr.length - 3)) {
          let hovero: Array<any> = [404, 928, 358];
          let awayW = 1.0;
          let placeholderV = String.fromCharCode(119,101,98,99,97,109,95,104,95,49,48,48,0);
          let malaysiaQ: Map<any, any> = new Map([[String.fromCharCode(111,95,49,57,95,116,104,97,116,0),true ], [String.fromCharCode(109,95,57,55,95,116,105,109,101,105,110,102,111,0),false ], [String.fromCharCode(104,95,51,49,95,111,98,115,101,114,118,101,0),true ]]);
          let kickq = 0;
         m_countW = [1 - parseInt(`${awayW}`)];
         hovero = [placeholderV.length >> (Math.min(3, Math.abs(malaysiaQ.size)))];
         awayW *= parseFloat(`${malaysiaQ.size}`);
         placeholderV += `${(placeholderV == String.fromCharCode(115,0) ? placeholderV.length : hovero.length)}`;
         kickq &= placeholderV.length;
         break;
      }
      while (4 <= annerr.length) {
          let whiten: Map<any, any> = new Map([[String.fromCharCode(113,95,56,52,95,115,117,98,109,105,116,0),927], [String.fromCharCode(108,97,115,114,95,57,95,52,54,0),428], [String.fromCharCode(122,95,53,52,95,116,114,107,110,0),48]]);
          let detailo = 2;
          let launchj = String.fromCharCode(101,95,48,95,115,108,97,115,104,101,115,0);
         annerr += "3";
         whiten.set(`${detailo}`, 3);
         detailo ^= launchj.length;
         launchj = `${2 ^ whiten.size}`;
         break;
      }
      shootA = new Map([[`${time_cqa.size}`, 1]]);
      untickY = new Map([[`${ying2}`, parseInt(`${ying2}`) - parseInt(`${macauC}`)]]);
       let umeng6 = String.fromCharCode(97,110,110,111,117,110,99,101,95,113,95,49,0);
       let refreshm = 5;
      for (let z = 0; z < 1; z++) {
         refreshm ^= refreshm - umeng6.length;
      }
      if (umeng6.length > refreshm) {
         umeng6 = "2";
      }
         refreshm %= Math.max(1 >> (Math.min(4, umeng6.length)), 1);
       let helperN = 2.0;
       let countrys = 1.0;
       let ajaxT = String.fromCharCode(110,111,114,109,97,108,105,122,101,95,122,95,50,52,0);
      let weibos = refreshm <= 6188000;
      do {
         refreshm -= 3 - ajaxT.length;
         if (weibos) {
            break;
         }
      } while (weibos && (5 > (refreshm - umeng6.length) || (5 - umeng6.length) > 4));
      agreementl &= untickY.size + agreementl;
       let forward_ = 3.0;
       let whatsappE = 1;
      while ((4 | whatsappE) >= 5 || (1.40 - forward_) >= 4.18) {
          let rank9 = 5.0;
          let b_imageH = true;
         forward_ += parseInt(`${rank9}`) << (Math.min(4, Math.abs(3)));
         rank9 *= ((b_imageH ? 2 : 1) - (b_imageH ? 5 : 2));
         break;
      }
      let selectt = whatsappE <= 5386265;
      do {
          let othero = 5.0;
          let trophyr = String.fromCharCode(115,99,104,117,110,99,107,95,120,95,57,49,0);
         whatsappE <<= Math.min(5, Math.abs(whatsappE));
         othero += parseInt(`${othero}`) / (Math.max(1, 8));
         trophyr += `${trophyr.length % 1}`;
         if (selectt) {
            break;
         }
      } while (selectt && (2.26 <= forward_));
         forward_ -= whatsappE % (Math.max(parseInt(`${forward_}`), 1));
       let downloading5: Array<any> = [String.fromCharCode(107,95,55,54,95,110,101,103,0), String.fromCharCode(101,95,50,50,95,102,97,99,101,115,0), String.fromCharCode(98,111,117,110,99,101,95,112,95,57,50,0)];
      while (downloading5.includes(whatsappE)) {
          let ajaxc = 1.0;
          let dropdownn: Map<any, any> = new Map([[String.fromCharCode(98,95,53,51,95,118,111,98,115,117,98,0),428], [String.fromCharCode(103,111,111,100,95,51,95,52,48,0),595], [String.fromCharCode(119,95,50,56,95,97,114,101,115,0),594]]);
          let analytick = String.fromCharCode(114,111,117,116,105,110,101,115,95,101,95,49,57,0);
          let description_8zJ = String.fromCharCode(101,95,49,49,95,98,105,116,114,101,97,100,101,114,0);
         downloading5 = [(String.fromCharCode(84,0) == analytick ? analytick.length : dropdownn.size)];
         ajaxc -= parseFloat(`${description_8zJ.length ^ 2}`);
         dropdownn = new Map([[`${ajaxc}`, 3 << (Math.min(5, Math.abs(parseInt(`${ajaxc}`))))]]);
         description_8zJ = `${parseInt(`${ajaxc}`)}`;
         break;
      }
         whatsappE &= 2 >> (Math.min(Math.abs(parseInt(`${forward_}`)), 5));
      agreementl |= switch_tgk.length;
      macauC += parseInt(`${clubH}`) >> (Math.min(5, Math.abs(1)));
   for (let z = 0; z < 2; z++) {
      rewind9 -= parseFloat(`${time_cqa.size}`);
   }
   if (1.96 == (rewind9 + agreementl)) {
      agreementl *= stationsb.length;
   }
      untickY = new Map([[`${agreementl}`, parseInt(`${macauC}`)]]);
      shootA = new Map([[`${untickY.size}`, untickY.size + 1]]);
      switch_tgk = "2";
      rewind9 += (parseFloat(`${(short_ku ? 1 : 2) + 3}`));
       let privacyH: Map<any, any> = new Map([[String.fromCharCode(115,101,110,100,109,98,117,102,95,121,95,56,49,0),829], [String.fromCharCode(103,97,109,97,95,115,95,54,53,0),398], [String.fromCharCode(112,114,101,97,109,98,117,108,97,95,114,95,54,0),450]]);
       let statsb = 0;
       let with_gvc = 4;
         with_gvc &= privacyH.size >> (Math.min(Math.abs(3), 1));
      while ((statsb ^ 2) <= 5) {
          let mathw = true;
          let preview2 = 2.0;
          let skipe = String.fromCharCode(112,105,120,99,116,120,95,56,95,51,55,0);
         with_gvc |= privacyH.size;
         mathw = preview2 <= 15.45;
         preview2 /= Math.max(3, 1);
         skipe = `${parseInt(`${preview2}`)}`;
         break;
      }
      if (!Array.from(privacyH.values()).includes(with_gvc)) {
         with_gvc >>= Math.min(3, Math.abs(privacyH.size));
      }
      for (let e = 0; e < 2; e++) {
         statsb *= statsb;
      }
      let dragT = 6587948 <= statsb;
      do {
         statsb *= with_gvc * 1;
         if (dragT) {
            break;
         }
      } while (dragT && ((statsb ^ 2) == 1 || 2 == (2 ^ statsb)));
          let assistN = true;
         statsb >>= Math.min(Math.abs(with_gvc << (Math.min(2, Math.abs(2)))), 3);
         assistN = !assistN;
          let sheetY = String.fromCharCode(116,111,112,105,99,97,108,95,115,95,50,56,0);
          let calendarp = String.fromCharCode(98,98,117,102,95,98,95,50,55,0);
         statsb *= statsb;
         sheetY += `${sheetY.length | 2}`;
         calendarp += `${sheetY.length}`;
      if ((statsb - 2) >= 5 && (2 - privacyH.size) >= 3) {
         statsb ^= with_gvc;
      }
         statsb += statsb;
      stationsb = "1";

    onClose(false);
  };

  const navigator = useNavigation();

  return (
    <View style={{paddingHorizontal: 10}}>
      {isLeft ? (
        <View style={{alignSelf: 'flex-start', paddingLeft: 30}}>
          <Image source={require(`@static/images/vip_guide_arrow.png`)}></Image>
        </View>
      ) : (
        <View style={{alignSelf: 'flex-end', paddingRight: 30}}>
          <Image source={require(`@static/images/vip_guide_arrow.png`)}></Image>
        </View>
      )}
      <View
        style={{
          width: width ?? '70%',
          alignSelf: 'flex-end',
          justifyContent: isLeft ? 'flex-end' : 'flex-start',
        }}>
        <View style={[styles.buyContainer]}>
          {contentTemplate == 1 && (
            <>
              <Text style={[styles.normalText]}>
                {`购买`}
                <Text style={[styles.goldText]}>VIP</Text>
                <Text
                  style={[styles.normalText]}>{`  去除广告\n高清视频`}</Text>
                <Text style={[styles.goldText]}>{`纯净观看`}</Text>
              </Text>
            </>
          )}

          {contentTemplate == 2 && (
            <>
              <Text style={[styles.normalText]}>
                {`购买`}
                <Text style={[styles.goldText]}>VIP</Text>

                <Text style={[styles.goldText]}>{`\n离线下载`}</Text>
                <Text style={[styles.normalText]}>{`高清视频`}</Text>
              </Text>
            </>
          )}

          {contentTemplate == 3 && (
            <>
              <Text style={[styles.normalText]}>
                {`购买`}
                <Text style={[styles.goldText]}>VIP</Text>
                <Text
                  style={[styles.normalText]}>{`免费看NBA/五大联赛\n`}</Text>
                <Text style={[styles.normalText]}>{`实时`}</Text>
                <Text style={[styles.goldText]}>{`高清直播`}</Text>
              </Text>
            </>
          )}

          <View style={{paddingHorizontal: 10, width: '100%', paddingTop: 10}}>
            <TouchableOpacity
              onPress={() => {
                navigator.navigate('付费VIP');
                closeModal();
              }}>
              <LinearGradient
                colors={['#D1AC7D', '#B1885F']}
                locations={[0.0, 0.99]}
                style={{
                  height: 40,
                  justifyContent: 'center',
                  alignItems: 'center',
                  paddingVertical: 8,
                  borderRadius: 8,
                }}>
                <Text
                  style={{
                    fontWeight: '900',
                    letterSpacing: 1,
                    color: 'black',
                  }}>
                  立即购买
                </Text>
              </LinearGradient>
            </TouchableOpacity>
          </View>
        </View>
        <View style={{height: 12}} />
        <View>
          <View style={{alignSelf: 'center'}}>
            <Text style={[styles.confirmText]} onPress={() => closeModal()}>
              我知道了
            </Text>
          </View>
        </View>
      </View>
    </View>
  );
}

const styles = StyleSheet.create({
  buyContainer: {
    borderRadius: 10,
    borderColor: 'white',
    borderWidth: 1,
    marginHorizontal: 10,
    paddingVertical: 12,
  },
  normalText: {
    fontSize: 18,
    color: 'white',
    textAlign: 'center',
  },
  goldText: {
    fontSize: 18,
    color: '#BB9266',
    textAlign: 'center',
  },
  confirmText: {
    borderRadius: 15,
    borderWidth: 1,
    borderColor: '#9C9C9C',
    color: '#9C9C9C',
    fontSize: 12,
    paddingHorizontal: 10,
    paddingVertical: 5,
    textAlign: 'center',
  },
});
