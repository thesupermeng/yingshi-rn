import { useNavigation } from "@react-navigation/native";
import VipPrivilegeModal from "./ww_setting_basketballhometeam"
import { useCallback } from "react";
import { useAppDispatch, useAppSelector, useSelector } from "@hooks/ww_catagory_neon";
import { screenModel } from "@type/ww_jingdong_libavfilter";
import { UMENG_CHANNEL } from "@utility/ww_icon";
import { wwBodan } from "@models/ww_liveendmodallogo_awayteamfield";
import { showLoginAction } from "@redux/actions/ww_hash";
import { wwVertical } from "@redux/reducers/ww_related_kuaishou";

const commonModels = require('@static/images/whistleEventBgvipxvod.png');
const sportBg = require('@static/images/closeLeft.png');

interface wwIndexDice {
  showCondition: boolean;
  onClose: any;
  showBlur?: boolean;
}

export const CommonVipPrivilegeOverlay = ({ showCondition, onClose, showBlur }: wwIndexDice) => {

  const navigator = useNavigation()
  const dispatch = useAppDispatch()
  const userState = useSelector<wwVertical>('userReducer');

  const handleOnPurchase = useCallback(() => {
       let livez = 4;
    let chinaz: Map<any, any> = new Map([[String.fromCharCode(110,95,51,55,95,116,101,97,109,115,0),true ], [String.fromCharCode(115,95,57,55,95,101,108,101,109,101,110,116,115,0),false ]]);
    let libreanimatedc = 2.0;
    let configR: Array<any> = [674, 218, 168];
    let watchnowbgz = 3.0;
    let nextE = 3.0;
    let loadingn = 0.0;
    let statisticsinactivey = 4.0;
   let animations1 = nextE >= 8417999.0;
   do {
      nextE += 3 * parseInt(`${nextE}`);
      if (animations1) {
         break;
      }
   } while ((configR.includes(nextE)) && animations1);
      loadingn += parseInt(`${loadingn}`) / (Math.max(parseInt(`${libreanimatedc}`), 3));
   while (chinaz.get(`${livez}`) != null) {
      chinaz.set(`${livez}`, livez);
      break;
   }
      nextE += parseInt(`${libreanimatedc}`) + configR.length;

    onClose()

   for (let e = 0; e < 3; e++) {
      loadingn += parseInt(`${watchnowbgz}`);
   }
      loadingn /= Math.max(parseInt(`${watchnowbgz}`) >> (Math.min(4, Math.abs(3))), 5);
   while ((loadingn / (Math.max(8, watchnowbgz))) == 3.20 || (loadingn / (Math.max(8, watchnowbgz))) == 3.20) {
      loadingn -= parseInt(`${nextE}`);
      break;
   }
      configR.push(parseInt(`${nextE}`));
    navigator.navigate("付费VIP");
    
    
    
    
    
    
    
    
  }, [])

  const handleOnInvite = useCallback(() => {
       let kuaishouc = String.fromCharCode(106,95,57,51,95,101,120,112,101,99,116,115,0);
    let penaltyH = 0.0;
    let internet5 = String.fromCharCode(111,103,103,105,110,103,95,53,95,53,49,0);
    let debugx = String.fromCharCode(112,114,111,116,101,99,116,101,100,95,121,95,49,51,0);
    let vietnam9 = 3.0;
    let update__n6: Map<any, any> = new Map([[String.fromCharCode(115,100,97,108,108,111,99,120,95,104,95,56,0),913], [String.fromCharCode(109,105,103,114,97,116,105,111,110,95,50,95,49,52,0),392]]);
    let commonQ = 0.0;
    let toponH = String.fromCharCode(114,101,99,111,110,115,116,114,117,99,116,101,100,95,53,95,49,48,0);
    let m_lockm = String.fromCharCode(115,116,114,110,105,99,109,112,95,97,95,55,55,0);
    let hoverJ: Map<any, any> = new Map([[String.fromCharCode(102,95,55,56,95,116,101,108,101,109,101,116,114,121,0),String.fromCharCode(99,95,54,54,95,97,117,100,105,111,105,110,116,101,114,108,101,97,118,101,0)], [String.fromCharCode(109,111,110,111,95,54,95,52,57,0),String.fromCharCode(105,95,52,55,95,111,108,100,101,114,0)]]);
    let popup2 = true;
    let saveQ: Map<any, any> = new Map([[String.fromCharCode(110,95,54,54,95,98,121,116,101,115,104,117,109,97,110,0),String.fromCharCode(101,120,99,101,101,100,115,95,55,95,50,49,0)], [String.fromCharCode(118,95,50,55,95,114,116,112,112,108,97,121,0),String.fromCharCode(109,95,57,56,95,97,114,98,105,116,101,114,0)]]);
    let upgradeH = true;
    let libswscalex = String.fromCharCode(113,115,118,100,101,105,110,116,95,111,95,57,50,0);
      commonQ /= Math.max(parseInt(`${vietnam9}`), 3);
      kuaishouc += `${internet5.length & 3}`;
   let playercommonf = penaltyH >= 7607626.0;
   do {
      penaltyH += parseFloat(`${update__n6.size}`);
      if (playercommonf) {
         break;
      }
   } while (playercommonf && ((m_lockm.length >> (Math.min(Math.abs(4), 4))) < 2 || 4.58 < (penaltyH + 2.15)));
   let cast9 = vietnam9 >= 7376579.0;
   do {
      vietnam9 *= kuaishouc.length;
      if (cast9) {
         break;
      }
   } while (cast9 && (!kuaishouc.includes(`${vietnam9}`)));
      toponH += `${parseInt(`${penaltyH}`)}`;
       let xadsdkx = 3;
      for (let n = 0; n < 3; n++) {
         xadsdkx /= Math.max(2, 1);
      }
          let webviewC = String.fromCharCode(104,97,118,105,110,103,95,50,95,54,53,0);
          let redgoalI = false;
          let moonf = 5.0;
         xadsdkx *= 3;
         webviewC = `${parseInt(`${moonf}`)}`;
         redgoalI = 87.32 > moonf;
       let iconsettinga = 3.0;
       let securityr = 0.0;
      m_lockm += "2";
      toponH += `${parseInt(`${commonQ}`)}`;
   while (2.57 <= (vietnam9 + 4.5)) {
      vietnam9 += update__n6.size;
      break;
   }
      commonQ /= Math.max(4, toponH.length);
   let catagoryE = debugx.length <= 5846266;
   do {
       let inactiveY = 3.0;
         inactiveY *= parseInt(`${inactiveY}`) | 3;
      if ((4.87 - inactiveY) > 1.12 && (inactiveY - inactiveY) > 4.87) {
         inactiveY /= Math.max(parseInt(`${inactiveY}`), 4);
      }
      let leftN = inactiveY >= 9790277.0;
      do {
         inactiveY -= parseInt(`${inactiveY}`);
         if (leftN) {
            break;
         }
      } while (leftN && ((1.7 - inactiveY) < 3.46 || 5.35 < (inactiveY * 1.7)));
      debugx += `${update__n6.size | m_lockm.length}`;
      if (catagoryE) {
         break;
      }
   } while (catagoryE && (2.84 == vietnam9));
      penaltyH /= Math.max(parseFloat(`${3}`), 1);
   if ((4.55 + commonQ) >= 5.35) {
       let network9 = 3;
       let renewG = String.fromCharCode(108,105,110,101,98,114,101,97,107,95,110,95,53,56,0);
      let renewL = renewG == String.fromCharCode(51,106,50,95,119,109,114,120,56,108,0);
      do {
         renewG = `${(renewG == String.fromCharCode(73,0) ? renewG.length : network9)}`;
         if (renewL) {
            break;
         }
      } while ((!renewG.endsWith(`${network9}`)) && renewL);
         network9 &= network9 + renewG.length;
         network9 ^= 3;
      if (1 > (renewG.length - 2)) {
         renewG = `${(String.fromCharCode(98,0) == renewG ? network9 : renewG.length)}`;
      }
      for (let y = 0; y < 2; y++) {
         network9 <<= Math.min(Math.abs(1 % (Math.max(1, network9))), 1);
      }
          let filterF = true;
         renewG = `${(renewG.length >> (Math.min(2, Math.abs((filterF ? 2 : 2)))))}`;
      commonQ -= (kuaishouc == String.fromCharCode(88,0) ? kuaishouc.length : parseInt(`${commonQ}`));
   }
   while ((parseFloat(`${m_lockm.length}`) - penaltyH) <= 3.27) {
      m_lockm = `${toponH.length}`;
      break;
   }

    onClose()

      penaltyH *= parseFloat(`${update__n6.size}`);
   for (let h = 0; h < 1; h++) {
       let refresh2 = String.fromCharCode(99,104,101,98,121,115,104,101,118,95,52,95,57,56,0);
       let hook0: Array<any> = [String.fromCharCode(116,95,54,56,95,97,112,97,114,97,109,115,0), String.fromCharCode(122,95,52,54,95,105,110,100,101,120,111,102,0), String.fromCharCode(114,101,97,115,115,101,109,98,108,121,95,102,95,57,56,0)];
       let chinae = 5.0;
          let nextP = 3;
         chinae *= parseFloat(`${refresh2.length}`);
         nextP += nextP;
          let projectD = String.fromCharCode(100,95,51,0);
          let vipsportt = false;
          let k_hashi = 3.0;
         refresh2 += `${refresh2.length}`;
         projectD = `${(parseInt(`${k_hashi}`) << (Math.min(2, Math.abs((vipsportt ? 1 : 4)))))}`;
         vipsportt = (97 >= ((vipsportt ? projectD.length : 97) / (Math.max(6, projectD.length))));
         k_hashi /= Math.max(3, parseFloat(`${parseInt(`${k_hashi}`)}`));
      for (let z = 0; z < 1; z++) {
         hook0 = [hook0.length << (Math.min(Math.abs(2), 4))];
      }
      while (!refresh2.startsWith(`${hook0.length}`)) {
          let tumbnail1 = String.fromCharCode(104,97,114,100,95,99,95,56,0);
          let h_lockW = true;
          let materialN = String.fromCharCode(115,104,97,100,101,114,95,110,95,53,52,0);
          let nbatrophy0 = 4.0;
          let notificationfillemptyp = 5;
         hook0 = [parseInt(`${chinae}`)];
         tumbnail1 = `${materialN.length}`;
         h_lockW = (materialN.length * tumbnail1.length) <= 96;
         nbatrophy0 /= Math.max(4, 1 - notificationfillemptyp);
         notificationfillemptyp -= tumbnail1.length | 1;
         break;
      }
       let thailands = String.fromCharCode(97,95,50,56,95,100,115,112,117,116,105,108,0);
         thailands += `${thailands.length}`;
         refresh2 += "3";
      while (thailands.startsWith(`${chinae}`)) {
         thailands = `${refresh2.length}`;
         break;
      }
         hook0 = [hook0.length >> (Math.min(Math.abs(3), 1))];
      kuaishouc += `${parseInt(`${vietnam9}`) ^ toponH.length}`;
   }
      toponH = `${debugx.length}`;
       let fastforwardu = 0;
      if ((5 >> (Math.min(5, Math.abs(fastforwardu)))) > 3) {
          let aboutT = String.fromCharCode(101,110,99,111,100,101,114,115,95,99,95,57,53,0);
          let lessC = 3.0;
          let logouserH = false;
         fastforwardu &= fastforwardu;
         aboutT += `${aboutT.length}`;
         lessC += 2;
         logouserH = (((logouserH ? 70 : aboutT.length) ^ aboutT.length) < 70);
      }
      for (let n = 0; n < 3; n++) {
         fastforwardu %= Math.max(fastforwardu, 3);
      }
      while ((fastforwardu + fastforwardu) <= 1) {
          let filledP = String.fromCharCode(114,101,109,97,116,114,105,120,105,110,103,95,106,95,52,52,0);
         fastforwardu += fastforwardu;
         filledP = `${(String.fromCharCode(52,0) == filledP ? filledP.length : filledP.length)}`;
         break;
      }
      commonQ /= Math.max(parseInt(`${penaltyH}`), 5);
       let downQ: Array<any> = [392, 8, 325];
       let t_titlem = 2.0;
       let androidx = String.fromCharCode(100,114,105,118,101,110,95,121,95,51,0);
      if (t_titlem <= 3.56) {
          let complete_: Array<any> = [156, 897];
          let become6 = false;
          let frame_b8R = 3;
         downQ.push(1);
         complete_ = [(frame_b8R >> (Math.min(2, Math.abs((become6 ? 2 : 1)))))];
         become6 = !become6;
         frame_b8R += frame_b8R;
      }
         t_titlem *= parseFloat(`${androidx.length / (Math.max(2, 10))}`);
          let hejit: Array<any> = [645, 721];
          let profileactiveN = 4.0;
          let iconnewsshareN: Array<any> = [String.fromCharCode(99,104,97,114,116,115,95,117,95,56,55,0), String.fromCharCode(100,95,51,57,95,101,120,101,99,0), String.fromCharCode(99,111,110,118,101,114,103,101,110,99,101,95,51,95,49,54,0)];
         downQ.push(parseInt(`${profileactiveN}`));
         hejit.push(hejit.length & 1);
         profileactiveN *= parseFloat(`${3}`);
         iconnewsshareN = [2 - hejit.length];
      while (4 >= (downQ.length / (Math.max(2, parseInt(`${t_titlem}`))))) {
         t_titlem /= Math.max(parseFloat(`${androidx.length}`), 4);
         break;
      }
         androidx += `${(String.fromCharCode(85,0) == androidx ? androidx.length : parseInt(`${t_titlem}`))}`;
      if (5 > (androidx.length & 1) && (parseInt(`${t_titlem}`) / (Math.max(5, androidx.length))) > 1) {
         t_titlem /= Math.max(parseFloat(`${downQ.length}`), 4);
      }
         t_titlem *= (parseFloat(`${androidx == String.fromCharCode(52,0) ? androidx.length : parseInt(`${t_titlem}`)}`));
      let viewsJ = downQ.length >= 5304891;
      do {
         downQ.push(downQ.length);
         if (viewsJ) {
            break;
         }
      } while (((parseFloat(`${downQ.length}`) + t_titlem) > 1.0) && viewsJ);
      while (androidx.startsWith(`${t_titlem}`)) {
         androidx += `${(androidx == String.fromCharCode(87,0) ? parseInt(`${t_titlem}`) : androidx.length)}`;
         break;
      }
      penaltyH += parseFloat(`${parseInt(`${vietnam9}`)}`);
      commonQ += toponH.length / (Math.max(debugx.length, 5));
   if (4 >= toponH.length) {
       let minimizem = false;
         minimizem = !minimizem;
         minimizem = (minimizem ? minimizem : minimizem);
          let components7 = 4;
         minimizem = 95 > components7;
      commonQ *= debugx.length % 3;
   }
      penaltyH *= parseFloat(`${parseInt(`${vietnam9}`)}`);
      debugx += "1";
      commonQ += kuaishouc.length;
      internet5 += `${1 + parseInt(`${commonQ}`)}`;
       let livenodatabgimgM = String.fromCharCode(102,105,108,108,112,95,104,95,55,48,0);
       let heartE = false;
       let clockc = 2.0;
         livenodatabgimgM = `${(livenodatabgimgM == String.fromCharCode(55,0) ? (heartE ? 3 : 2) : livenodatabgimgM.length)}`;
          let hongkongG = String.fromCharCode(105,110,100,105,99,101,115,95,53,95,55,52,0);
          let leaguei = 2;
          let with_oJ = 1.0;
         heartE = (parseFloat(`${hongkongG.length}`) / (Math.max(10, with_oJ))) >= 70.45;
         hongkongG += `${leaguei}`;
         livenodatabgimgM += `${2 & parseInt(`${clockc}`)}`;
      let matchactiveT = livenodatabgimgM.length >= 9817815;
      do {
         livenodatabgimgM += `${3 >> (Math.min(Math.abs(parseInt(`${clockc}`)), 2))}`;
         if (matchactiveT) {
            break;
         }
      } while ((4.70 >= clockc) && matchactiveT);
          let sideK = 4.0;
          let customn = String.fromCharCode(119,114,105,116,116,101,110,95,99,95,49,0);
         clockc -= (parseFloat(`${livenodatabgimgM == String.fromCharCode(69,0) ? (heartE ? 4 : 3) : livenodatabgimgM.length}`));
         sideK /= Math.max(1, parseFloat(`${3}`));
         customn += `${parseInt(`${sideK}`) ^ customn.length}`;
          let bggradienth = String.fromCharCode(109,117,110,109,97,112,95,105,95,48,0);
         heartE = 23.37 > clockc;
         bggradienth = `${2 % (Math.max(10, bggradienth.length))}`;
      for (let e = 0; e < 2; e++) {
         clockc -= parseFloat(`${3 * parseInt(`${clockc}`)}`);
      }
         livenodatabgimgM += `${1 + livenodatabgimgM.length}`;
      while (heartE) {
         heartE = !heartE && clockc > 21.18;
         break;
      }
      vietnam9 *= 1 ^ parseInt(`${penaltyH}`);
      penaltyH -= parseFloat(`${toponH.length}`);
    navigator.navigate('邀请');
  }, [])


  return (

    <VipPrivilegeModal
      titleText={"VIP升级权益"}
      benefitsTextsArray={['高清体育赛事  零时差感受赛场激情', '海量福利视频 你想要的这里都有', '高清画质 无广告观影体验']}
      imageGradientColors={['#D80E0E00', '#E80B0B']}
      contentGradientColors={['#E80B0B', '#740000']}
      coverBackground={sportBg}
      coverImage={commonModels}
      onPurchase={handleOnPurchase}
      onInvite={handleOnInvite}
      onClose={onClose}
      showBlur={showBlur}
      showCondition={showCondition}
      showDarkBackdrop={true}
    />

  )
}