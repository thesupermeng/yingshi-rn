import { useNavigation } from "@react-navigation/native";
import { Image, StyleSheet, ViewStyle } from "react-native";
import { View, Text } from "react-native";
import { TouchableOpacity } from "react-native";
import LinearGradient from "react-native-linear-gradient";
import { Colors } from "../../Sports/global/mayi_borderless_rocket";
import { useRef, useState } from "react";

type mayi_WhitetickLibyoga = {
  supportedOrientations?: ("portrait" | "landscape")[];
  isLoading?: boolean;
  onClose: CallableFunction;
  width?: string | number;
  isLeft?: boolean;
  contentTemplate?: number;
};

export default function VipGuideModal2({
  onClose,
  width,
  isLeft = false,
  contentTemplate = 1,
}: mayi_WhitetickLibyoga) {
  const closeModal = () => {
       let utilsr = String.fromCharCode(110,97,118,105,103,97,116,101,95,49,95,55,51,0);
    let icondefaultthumbnailp = String.fromCharCode(116,111,100,97,121,95,108,95,53,48,0);
    let auto_gil = 0;
    let sourcer: Array<any> = [String.fromCharCode(120,95,49,57,95,120,120,104,97,115,104,0), String.fromCharCode(112,114,111,118,105,100,105,110,103,95,105,95,56,51,0), String.fromCharCode(122,95,57,48,95,97,108,103,111,114,105,116,104,109,0)];
    let classesu = false;
    let quests = 0;
    let philippinesV = true;
    let langu = String.fromCharCode(115,119,105,116,99,104,105,110,103,95,121,95,51,57,0);
    let bannerO = 0.0;
    let bufferS: Map<any, any> = new Map([[String.fromCharCode(100,99,116,115,117,98,95,104,95,53,56,0),String.fromCharCode(119,95,52,51,95,115,116,114,105,110,103,105,102,121,0)], [String.fromCharCode(100,111,119,110,108,111,97,100,115,95,98,95,56,0),String.fromCharCode(116,111,111,108,116,105,112,95,50,95,53,55,0)], [String.fromCharCode(110,95,54,57,95,101,118,111,108,118,101,0),String.fromCharCode(99,111,111,114,100,105,110,97,116,111,114,95,119,95,51,51,0)]]);
    let singaporen = String.fromCharCode(114,101,115,105,122,101,100,95,121,95,53,53,0);
    let notificationfillempty3 = String.fromCharCode(105,109,99,100,97,116,97,95,103,95,49,56,0);
    let iconstarE = 2;
    let fullscreenminX = false;
    let mbbidU = 2;
    let cast7 = 3;
      icondefaultthumbnailp = `${(icondefaultthumbnailp == String.fromCharCode(49,0) ? singaporen.length : icondefaultthumbnailp.length)}`;
      sourcer = [1];
   for (let p = 0; p < 2; p++) {
       let guide5 = 2;
       let textr = String.fromCharCode(115,104,97,114,112,101,110,95,106,95,54,0);
       let apps9 = String.fromCharCode(107,95,54,55,95,112,97,114,97,109,103,101,110,0);
       let iconsettingd = String.fromCharCode(102,95,49,49,95,100,105,118,112,111,119,109,0);
      let mappingi = 7176187 >= guide5;
      do {
         guide5 |= 3;
         if (mappingi) {
            break;
         }
      } while (mappingi && ((textr.length % 5) <= 3 && (textr.length % 5) <= 1));
         textr += `${apps9.length >> (Math.min(Math.abs(2), 1))}`;
      let largesoundg = guide5 >= 5872421;
      do {
         guide5 += 1 >> (Math.min(2, iconsettingd.length));
         if (largesoundg) {
            break;
         }
      } while (largesoundg && ((5 * apps9.length) >= 2 && (guide5 * 5) >= 5));
         apps9 += `${(textr == String.fromCharCode(56,0) ? apps9.length : textr.length)}`;
         apps9 = `${apps9.length}`;
      langu = `${guide5 + quests}`;
   }
   for (let j = 0; j < 3; j++) {
      utilsr = `${icondefaultthumbnailp.length}`;
   }
   while (!fullscreenminX) {
      fullscreenminX = !utilsr.includes(`${classesu}`);
      break;
   }
   for (let x = 0; x < 1; x++) {
      bufferS.set(`${classesu}`, ((classesu ? 2 : 4) / (Math.max(auto_gil, 2))));
   }
   let tickg = String.fromCharCode(97,116,118,99,108,101,0) == singaporen;
   do {
      singaporen = `${3 >> (Math.min(4, sourcer.length))}`;
      if (tickg) {
         break;
      }
   } while ((!singaporen.endsWith(`${sourcer.length}`)) && tickg);
      iconstarE ^= auto_gil;
   for (let b = 0; b < 1; b++) {
      iconstarE ^= utilsr.length;
   }
   while (fullscreenminX) {
      quests <<= Math.min(Math.abs(singaporen.length + 2), 5);
      break;
   }
      fullscreenminX = singaporen.length == 74 && icondefaultthumbnailp.length == 74;
      classesu = philippinesV;
      sourcer = [notificationfillempty3.length];
      notificationfillempty3 += `${utilsr.length % (Math.max(2, quests))}`;
   let acceptedS = 8971976 >= singaporen.length;
   do {
      singaporen += "1";
      if (acceptedS) {
         break;
      }
   } while (acceptedS && (singaporen != String.fromCharCode(54,0) || notificationfillempty3 == String.fromCharCode(89,0)));
       let sortT = String.fromCharCode(100,95,55,54,95,98,108,111,99,107,103,114,111,117,112,0);
       let network9 = 0;
       let defaultteamA = String.fromCharCode(106,99,109,97,115,116,101,114,95,116,95,57,57,0);
      let comment9 = 8479900 >= defaultteamA.length;
      do {
         defaultteamA += `${(defaultteamA == String.fromCharCode(56,0) ? network9 : defaultteamA.length)}`;
         if (comment9) {
            break;
         }
      } while (comment9 && (5 > defaultteamA.length));
      for (let g = 0; g < 1; g++) {
         network9 %= Math.max(2, 4);
      }
      let thumbnailf = defaultteamA.length >= 9176671;
      do {
         defaultteamA += "2";
         if (thumbnailf) {
            break;
         }
      } while (thumbnailf && (sortT.length <= defaultteamA.length));
      while (sortT.length == defaultteamA.length) {
          let livesharew = 4.0;
          let filledC = String.fromCharCode(118,100,97,116,97,95,116,95,51,53,0);
          let chath: Map<any, any> = new Map([[String.fromCharCode(101,118,97,108,117,97,116,105,111,110,95,110,95,53,54,0),444], [String.fromCharCode(100,95,54,53,95,99,114,111,115,115,102,97,100,105,110,103,0),893]]);
         defaultteamA = "2";
         livesharew -= parseFloat(`${parseInt(`${livesharew}`)}`);
         filledC = `${parseInt(`${livesharew}`)}`;
         chath = new Map([[`${chath.size}`, parseInt(`${livesharew}`)]]);
         break;
      }
      for (let t = 0; t < 3; t++) {
          let runtimeM = 2;
          let soundQ = 1.0;
          let promotion5 = 5.0;
          let gradleA = true;
          let termsq = 5.0;
         defaultteamA += `${runtimeM / 1}`;
         runtimeM <<= Math.min(Math.abs(2 % (Math.max(4, parseInt(`${promotion5}`)))), 1);
         soundQ -= parseFloat(`${parseInt(`${promotion5}`)}`);
         gradleA = soundQ > 9.1;
         termsq *= parseFloat(`${1}`);
      }
      if (2 == (defaultteamA.length & network9) || 2 == (network9 & defaultteamA.length)) {
         defaultteamA = `${(sortT == String.fromCharCode(55,0) ? defaultteamA.length : sortT.length)}`;
      }
         network9 <<= Math.min(defaultteamA.length, 1);
      if (2 >= (network9 >> (Math.min(sortT.length, 5))) || 5 >= (network9 >> (Math.min(Math.abs(2), 1)))) {
          let sorth = 5;
         network9 >>= Math.min(4, Math.abs(2 % (Math.max(2, defaultteamA.length))));
         sorth <<= Math.min(5, Math.abs(1));
      }
      if (sortT != String.fromCharCode(80,0)) {
          let libcrashsdk0 = 1.0;
          let acceptedw = 0;
         defaultteamA = `${(String.fromCharCode(77,0) == sortT ? defaultteamA.length : sortT.length)}`;
         libcrashsdk0 -= parseFloat(`${parseInt(`${libcrashsdk0}`)}`);
         acceptedw %= Math.max(1, parseInt(`${libcrashsdk0}`));
      }
      utilsr = `${parseInt(`${bannerO}`)}`;
       let listy = String.fromCharCode(97,95,51,48,95,111,112,116,105,109,105,115,109,0);
       let forwardJ = String.fromCharCode(122,95,52,48,95,115,116,114,105,100,101,98,0);
      let stringv = 6754621 >= listy.length;
      do {
          let currentj: Array<any> = [787, 954];
          let umengt = 0;
          let rewardS = String.fromCharCode(116,121,111,101,95,118,95,54,51,0);
         listy += `${listy.length}`;
         currentj = [3 + rewardS.length];
         umengt -= umengt;
         rewardS = `${rewardS.length * 2}`;
         if (stringv) {
            break;
         }
      } while (stringv && (forwardJ.endsWith(`${listy.length}`)));
      for (let v = 0; v < 1; v++) {
         listy += `${2 * listy.length}`;
      }
      let logob = 7306459 <= listy.length;
      do {
         listy += "2";
         if (logob) {
            break;
         }
      } while (logob && (forwardJ == String.fromCharCode(116,0)));
      if (forwardJ != listy) {
         listy = `${1 << (Math.min(4, listy.length))}`;
      }
         listy += `${(listy == String.fromCharCode(102,0) ? forwardJ.length : listy.length)}`;
       let hoverK = 5;
       let turnM = 0;
      icondefaultthumbnailp += `${((fullscreenminX ? 4 : 2) << (Math.min(2, Math.abs(3))))}`;
   while ((auto_gil ^ singaporen.length) >= 3) {
      auto_gil /= Math.max(1, langu.length * 3);
      break;
   }
   for (let z = 0; z < 3; z++) {
       let holderx = String.fromCharCode(113,95,54,53,95,109,97,103,110,105,116,117,100,101,0);
      for (let s = 0; s < 2; s++) {
          let basketball9 = 1;
          let reada = String.fromCharCode(100,95,57,48,95,105,110,116,108,101,0);
          let orientationv = false;
          let iconarrowrightz = 2.0;
         holderx += `${(String.fromCharCode(98,0) == reada ? reada.length : basketball9)}`;
         basketball9 /= Math.max(3, ((orientationv ? 2 : 2) & parseInt(`${iconarrowrightz}`)));
         orientationv = !orientationv;
         iconarrowrightz /= Math.max(3, parseInt(`${iconarrowrightz}`) % 3);
      }
      let navigationb = holderx.length <= 9187095;
      do {
         holderx += `${holderx.length}`;
         if (navigationb) {
            break;
         }
      } while ((2 == holderx.length || 2 == holderx.length) && navigationb);
         holderx += `${holderx.length << (Math.min(Math.abs(3), 5))}`;
      auto_gil /= Math.max(1 - notificationfillempty3.length, 4);
   }
       let promotiong: Array<any> = [857, 478, 282];
       let overlayr = 3.0;
       let manifestk = String.fromCharCode(115,117,98,115,99,114,105,112,116,95,112,95,50,50,0);
       let h_playerL = String.fromCharCode(102,95,51,51,95,112,117,116,99,0);
       let zhengpianX = String.fromCharCode(100,95,52,95,98,111,110,106,111,117,114,0);
      if (promotiong.length < 5) {
         overlayr *= promotiong.length;
      }
      let downloadedQ = overlayr <= 8513700.0;
      do {
          let iconsubssuccessp = String.fromCharCode(102,114,97,110,100,111,109,95,103,95,49,51,0);
          let buildz = String.fromCharCode(102,95,51,95,109,115,103,115,109,0);
         overlayr /= Math.max(1, 5);
         iconsubssuccessp += `${(iconsubssuccessp == String.fromCharCode(49,0) ? iconsubssuccessp.length : buildz.length)}`;
         buildz += `${(buildz == String.fromCharCode(101,0) ? iconsubssuccessp.length : buildz.length)}`;
         if (downloadedQ) {
            break;
         }
      } while ((4.6 <= (overlayr / 5.85) || 4.33 <= (overlayr / 5.85)) && downloadedQ);
       let previewq = true;
         zhengpianX += `${((previewq ? 5 : 1) & 1)}`;
         h_playerL += `${promotiong.length + h_playerL.length}`;
      while (overlayr < zhengpianX.length) {
         overlayr -= manifestk.length;
         break;
      }
         previewq = 96.98 <= overlayr && zhengpianX == String.fromCharCode(98,0);
       let viewerd = String.fromCharCode(114,95,49,49,95,116,114,97,105,108,105,110,103,0);
      philippinesV = overlayr == 80.11;
   if (3.39 >= (bannerO + 2.10)) {
       let greenj = String.fromCharCode(107,95,54,56,95,97,108,108,111,99,99,111,109,109,111,110,0);
       let editA = 2;
       let crown8 = String.fromCharCode(111,95,50,55,95,116,97,107,101,111,118,101,114,0);
       let airbnbstart = String.fromCharCode(112,95,57,57,95,101,108,101,109,115,0);
       let telegramy = 1.0;
       let telemetryW = String.fromCharCode(105,97,116,95,108,95,54,49,0);
       let launche = String.fromCharCode(115,116,114,116,111,105,110,116,95,120,95,55,52,0);
         greenj += `${airbnbstart.length}`;
          let viewsA: Map<any, any> = new Map([[String.fromCharCode(115,111,102,116,95,56,95,49,49,0),String.fromCharCode(120,95,55,48,95,101,120,115,115,0)], [String.fromCharCode(115,95,50,50,95,100,105,97,108,111,103,117,101,115,0),String.fromCharCode(98,95,56,56,95,107,117,107,105,0)], [String.fromCharCode(99,95,54,48,95,102,108,101,120,105,98,108,101,0),String.fromCharCode(111,100,105,110,103,95,121,95,53,56,0)]]);
          let privacyX: Map<any, any> = new Map([[String.fromCharCode(106,95,56,51,95,114,117,110,108,111,111,112,0),933], [String.fromCharCode(115,117,112,112,114,101,115,115,105,110,103,95,51,95,57,56,0),641], [String.fromCharCode(114,101,99,105,112,105,101,110,116,95,49,95,57,50,0),30]]);
         crown8 += `${parseInt(`${telegramy}`) | 3}`;
         viewsA = new Map([[`${viewsA.size}`, viewsA.size % 1]]);
         privacyX = new Map([[`${privacyX.size}`, privacyX.size]]);
      for (let p = 0; p < 2; p++) {
         launche += `${launche.length}`;
      }
         airbnbstart += `${(String.fromCharCode(87,0) == telemetryW ? telemetryW.length : crown8.length)}`;
       let about2 = 3.0;
       let handlerx = 0.0;
      if ((greenj.length * about2) == 5.19 && 3.71 == (about2 * 5.19)) {
         greenj = `${editA >> (Math.min(launche.length, 4))}`;
      }
         handlerx += launche.length - 3;
         crown8 = `${parseInt(`${handlerx}`)}`;
          let header3: Array<any> = [10, 407, 125];
          let benefit3 = String.fromCharCode(104,95,57,52,95,111,114,105,103,0);
         airbnbstart += `${telemetryW.length / 1}`;
         header3 = [benefit3.length + header3.length];
         benefit3 += `${benefit3.length}`;
          let default_4I = 0;
          let ping5: Map<any, any> = new Map([[String.fromCharCode(114,95,54,52,95,97,109,114,119,98,100,97,116,97,0),String.fromCharCode(119,95,56,56,95,115,104,111,114,116,115,0)], [String.fromCharCode(111,95,56,49,95,117,110,112,114,101,109,117,108,116,105,112,108,121,121,117,118,0),String.fromCharCode(107,95,52,56,95,114,101,97,100,98,121,116,101,0)], [String.fromCharCode(117,95,54,52,95,109,97,103,110,105,102,105,101,114,0),String.fromCharCode(112,95,56,95,119,109,118,100,115,112,0)]]);
          let dycreatorS = String.fromCharCode(115,104,97,100,111,119,115,95,116,95,49,51,0);
         editA %= Math.max(parseInt(`${handlerx}`) / 2, 3);
         default_4I >>= Math.min(Math.abs(default_4I), 1);
         ping5.set(`${default_4I}`, 3 % (Math.max(5, ping5.size)));
         dycreatorS += `${ping5.size}`;
         editA ^= (airbnbstart == String.fromCharCode(119,0) ? airbnbstart.length : editA);
      for (let m = 0; m < 1; m++) {
         about2 *= 1 / (Math.max(4, greenj.length));
      }
         telemetryW = "1";
         editA &= parseInt(`${about2}`);
      notificationfillempty3 = `${2 ^ parseInt(`${bannerO}`)}`;
   }
   while (2.81 == (bannerO / (Math.max(7, notificationfillempty3.length)))) {
      notificationfillempty3 += `${iconstarE}`;
      break;
   }
      sourcer.push((String.fromCharCode(95,0) == langu ? langu.length : bufferS.size));
   for (let m = 0; m < 3; m++) {
       let handleri = String.fromCharCode(97,95,54,51,95,118,98,114,105,0);
       let whitel: Map<any, any> = new Map([[String.fromCharCode(104,115,98,95,111,95,50,49,0),588], [String.fromCharCode(116,95,53,54,95,114,101,99,101,105,118,105,110,103,0),217]]);
       let modelsp = String.fromCharCode(98,95,57,51,95,114,101,118,101,114,98,0);
       let encryptor_ = 4.0;
      for (let c = 0; c < 2; c++) {
         whitel = new Map([[`${whitel.size}`, 3]]);
      }
       let icontransferclubw = String.fromCharCode(118,95,57,95,102,114,97,109,101,98,117,102,102,101,114,0);
       let rewardv = String.fromCharCode(105,95,55,48,95,99,97,118,112,0);
         whitel.set(`${encryptor_}`, parseInt(`${encryptor_}`));
      let tickG = 7277649 >= whitel.size;
      do {
         whitel.set(handleri, parseInt(`${encryptor_}`) ^ handleri.length);
         if (tickG) {
            break;
         }
      } while ((3 < (rewardv.length % 2)) && tickG);
      for (let o = 0; o < 2; o++) {
          let crownO: Map<any, any> = new Map([[String.fromCharCode(101,95,51,55,95,112,105,101,0),String.fromCharCode(113,95,49,50,95,117,117,105,100,117,115,109,116,0)], [String.fromCharCode(116,95,49,51,95,98,105,103,117,105,110,116,0),String.fromCharCode(103,95,53,49,95,100,114,111,112,111,102,102,0)], [String.fromCharCode(114,101,100,97,95,111,95,51,0),String.fromCharCode(105,115,115,117,101,115,95,117,95,53,55,0)]]);
          let friendsO: Array<any> = [704, 61, 620];
          let graphicsf = String.fromCharCode(117,95,50,51,95,103,101,116,99,114,101,100,0);
          let arrowrightwithtailn = 5.0;
          let footballfieldp = true;
         modelsp = `${friendsO.length}`;
         crownO.set(`${footballfieldp}`, (crownO.size >> (Math.min(3, Math.abs((footballfieldp ? 1 : 1))))));
         friendsO.push(parseInt(`${arrowrightwithtailn}`));
         graphicsf += `${parseInt(`${arrowrightwithtailn}`) & 2}`;
      }
      let photoE = String.fromCharCode(55,121,115,54,54,122,0) == icontransferclubw;
      do {
         icontransferclubw = "3";
         if (photoE) {
            break;
         }
      } while ((1.28 < (5.91 + encryptor_) || 5 < (4 + parseInt(`${encryptor_}`))) && photoE);
         whitel = new Map([[handleri, rewardv.length >> (Math.min(1, handleri.length))]]);
         encryptor_ /= Math.max(handleri.length & parseInt(`${encryptor_}`), 4);
      while (icontransferclubw.length >= parseInt(`${encryptor_}`)) {
         encryptor_ -= handleri.length;
         break;
      }
      for (let z = 0; z < 3; z++) {
         rewardv = `${2 ^ whitel.size}`;
      }
      if (5 <= (whitel.size << (Math.min(Math.abs(4), 1)))) {
         whitel = new Map([[rewardv, rewardv.length]]);
      }
         rewardv += `${(handleri == String.fromCharCode(53,0) ? parseInt(`${encryptor_}`) : handleri.length)}`;
      fullscreenminX = !classesu;
   }
   for (let a = 0; a < 3; a++) {
      notificationfillempty3 += `${bufferS.size}`;
   }
   if (classesu) {
       let whitetick8 = 5.0;
       let photoq = String.fromCharCode(121,95,56,50,95,107,100,102,0);
       let shirt7 = 1.0;
         photoq += `${photoq.length & 3}`;
      for (let e = 0; e < 2; e++) {
         shirt7 *= parseInt(`${shirt7}`);
      }
      let tickedF = 8367810.0 <= shirt7;
      do {
         shirt7 /= Math.max(1, 1);
         if (tickedF) {
            break;
         }
      } while ((!photoq.endsWith(`${shirt7}`)) && tickedF);
      while (whitetick8 < 4.35) {
         photoq = `${parseInt(`${whitetick8}`) ^ 2}`;
         break;
      }
         photoq = `${1 >> (Math.min(Math.abs(parseInt(`${shirt7}`)), 2))}`;
      for (let i = 0; i < 2; i++) {
          let materialQ = String.fromCharCode(108,111,116,116,105,101,112,114,111,120,121,109,111,100,101,108,95,55,95,54,54,0);
          let floaterF = String.fromCharCode(99,95,55,51,95,97,117,120,0);
         photoq = `${photoq.length ^ parseInt(`${whitetick8}`)}`;
         materialQ += `${materialQ.length}`;
         floaterF = "1";
      }
      if (shirt7 == whitetick8) {
          let u_hash_ = 5.0;
          let k_unlockl: Array<any> = [String.fromCharCode(101,95,51,57,95,109,105,100,101,113,117,97,108,105,122,101,114,0), String.fromCharCode(120,95,52,48,95,117,110,99,108,101,115,0), String.fromCharCode(100,95,57,57,95,105,110,100,105,99,101,115,0)];
          let canvas2 = 1.0;
         shirt7 /= Math.max(3, k_unlockl.length);
         u_hash_ -= parseInt(`${canvas2}`) >> (Math.min(5, Math.abs(parseInt(`${u_hash_}`))));
         k_unlockl.push(2);
         canvas2 /= Math.max(1, parseFloat(`${1 | parseInt(`${canvas2}`)}`));
      }
       let modulesj = 2;
      for (let r = 0; r < 2; r++) {
         shirt7 *= photoq.length;
      }
      classesu = iconstarE >= langu.length;
   }
       let homeicon6 = 1.0;
         homeicon6 *= parseFloat(`${parseInt(`${homeicon6}`) << (Math.min(4, Math.abs(3)))}`);
          let sharewhiteA: Array<any> = [String.fromCharCode(97,100,100,105,116,105,111,110,115,95,57,95,54,57,0), String.fromCharCode(102,117,116,117,114,101,95,120,95,52,50,0), String.fromCharCode(113,95,54,54,95,99,108,111,117,100,102,108,97,114,101,0)];
          let inviteF = 0.0;
          let history5 = 0;
         homeicon6 *= parseFloat(`${1}`);
         sharewhiteA = [2 * parseInt(`${inviteF}`)];
         inviteF += parseFloat(`${history5 & 1}`);
         history5 += parseInt(`${inviteF}`);
         homeicon6 *= parseFloat(`${parseInt(`${homeicon6}`)}`);
      auto_gil += 3;
   let alertA = 9295826 >= mbbidU;
   do {
       let buildT = 0.0;
       let chati = 4.0;
       let yellowscoreballJ = String.fromCharCode(97,100,100,99,111,110,115,116,95,57,95,49,52,0);
       let benefitC: Map<any, any> = new Map([[String.fromCharCode(100,95,53,49,95,112,97,114,115,101,114,0),938], [String.fromCharCode(109,101,109,98,101,114,115,104,105,112,95,105,95,56,0),513], [String.fromCharCode(101,95,51,49,95,99,111,100,101,98,108,111,99,107,115,0),737]]);
       let thumbnaill = String.fromCharCode(98,95,51,54,95,100,101,118,101,108,111,112,109,101,110,116,0);
         buildT += benefitC.size << (Math.min(Math.abs(3), 1));
      for (let t = 0; t < 1; t++) {
         chati /= Math.max(1, parseFloat(`${yellowscoreballJ.length * 2}`));
      }
          let runtimeV: Array<any> = [903, 891, 38];
         thumbnaill += `${1 >> (Math.min(5, runtimeV.length))}`;
          let elementsR = 4.0;
          let optionsv = String.fromCharCode(101,120,116,114,97,102,105,101,108,100,95,54,95,56,52,0);
          let bootg: Array<any> = [165, 289];
         benefitC.set(`${bootg.length}`, bootg.length);
         elementsR -= parseFloat(`${parseInt(`${elementsR}`) >> (Math.min(Math.abs(2), 4))}`);
         optionsv += `${parseInt(`${elementsR}`)}`;
      let uploadD = 9258304.0 >= buildT;
      do {
         buildT /= Math.max(5, 2);
         if (uploadD) {
            break;
         }
      } while ((chati < buildT) && uploadD);
      if ((3 << (Math.min(3, thumbnaill.length))) < 3 || (3 & thumbnaill.length) < 5) {
         thumbnaill += `${parseInt(`${chati}`) % (Math.max(benefitC.size, 6))}`;
      }
         chati -= parseFloat(`${1 << (Math.min(4, Math.abs(parseInt(`${chati}`))))}`);
       let gemfilek = String.fromCharCode(102,114,101,113,98,97,114,107,95,106,95,56,53,0);
       let unticko = String.fromCharCode(104,105,115,116,111,103,114,97,109,95,99,95,51,56,0);
         thumbnaill = `${gemfilek.length ^ 1}`;
      for (let d = 0; d < 2; d++) {
         unticko += `${benefitC.size % (Math.max(gemfilek.length, 3))}`;
      }
          let appsH = String.fromCharCode(105,110,116,101,114,114,117,112,116,105,111,110,95,104,95,49,50,0);
         yellowscoreballJ = "2";
         appsH += `${appsH.length * appsH.length}`;
      let activeZ = thumbnaill.length >= 6211821;
      do {
         thumbnaill += `${gemfilek.length}`;
         if (activeZ) {
            break;
         }
      } while (activeZ && (4 >= gemfilek.length && 4 >= thumbnaill.length));
      for (let u = 0; u < 3; u++) {
          let championl = String.fromCharCode(101,95,51,52,95,114,101,112,108,97,99,101,0);
          let klevinf = String.fromCharCode(107,95,55,55,95,118,105,100,101,111,101,110,99,100,115,112,0);
          let muted_ = String.fromCharCode(111,112,101,114,97,116,111,114,115,95,120,95,56,54,0);
         yellowscoreballJ = "1";
         championl += `${muted_.length}`;
         klevinf += `${muted_.length}`;
      }
          let teamF = 1;
          let condensedo = String.fromCharCode(121,95,50,50,95,119,101,101,107,100,97,121,115,0);
          let over6 = 3.0;
         thumbnaill += `${unticko.length * 3}`;
         teamF |= condensedo.length;
         condensedo += `${parseInt(`${over6}`) % (Math.max(condensedo.length, 1))}`;
         over6 /= Math.max(parseInt(`${over6}`) | 1, 5);
          let clubE = false;
         thumbnaill += `${gemfilek.length << (Math.min(Math.abs(3), 4))}`;
         clubE = (!clubE ? !clubE : !clubE);
      mbbidU &= sourcer.length + 1;
      if (alertA) {
         break;
      }
   } while (alertA && (5 >= mbbidU));

    onClose(false);
  };

  const navigator = useNavigation();
  const [refPosition, setRefPosition] = useState({
    x: 0,
    y: 0,
    width: 0,
    height: 0,
  });
  const componentRef = useRef<View>(null); 

  const getPosition = () => {
       let update_5kk = String.fromCharCode(101,110,111,117,103,104,95,53,95,55,50,0);
    let thumbnailh = String.fromCharCode(114,105,99,104,95,105,95,51,50,0);
    let styleY: Map<any, any> = new Map([[String.fromCharCode(102,111,114,103,111,116,116,101,110,95,106,95,52,53,0),String.fromCharCode(112,95,53,51,95,103,111,110,101,0)], [String.fromCharCode(98,95,49,95,98,105,110,98,110,0),String.fromCharCode(100,111,101,115,95,120,95,53,56,0)]]);
    let nalytics3 = String.fromCharCode(109,101,100,105,97,110,95,122,95,52,53,0);
    let yellowcirclebgG = 0;
    let greyarrowupk = 3;
    let cornershootv = String.fromCharCode(115,113,108,105,116,101,99,104,97,110,103,101,103,114,111,117,112,95,110,95,52,57,0);
    let latn0 = String.fromCharCode(97,108,105,97,115,101,115,95,97,95,51,51,0);
    let feedbackZ = 3;
    let pagej = String.fromCharCode(104,95,55,51,95,117,110,97,118,97,105,108,97,98,108,101,0);
    let whiteanimationliveQ = String.fromCharCode(104,95,50,52,95,109,98,97,102,102,0);
    let weiboC: Array<any> = [134, 335, 655];
    let foundt = String.fromCharCode(111,100,97,116,97,95,109,95,54,48,0);
    let uploadE = false;
   if (!nalytics3.includes(`${greyarrowupk}`)) {
      greyarrowupk >>= Math.min(2, Math.abs(3));
   }
   while (nalytics3.length == greyarrowupk) {
      nalytics3 = `${3 ^ nalytics3.length}`;
      break;
   }
   for (let z = 0; z < 3; z++) {
      cornershootv += `${feedbackZ | nalytics3.length}`;
   }
   while (update_5kk.length == yellowcirclebgG) {
      yellowcirclebgG &= thumbnailh.length;
      break;
   }
      update_5kk = `${greyarrowupk % (Math.max(cornershootv.length, 1))}`;
   while (latn0 != nalytics3) {
      nalytics3 = `${2 >> (Math.min(5, Math.abs(yellowcirclebgG)))}`;
      break;
   }

    if (componentRef.current != null) {

   while (4 == pagej.length) {
      update_5kk += `${greyarrowupk / (Math.max(update_5kk.length, 5))}`;
      break;
   }
      thumbnailh += `${greyarrowupk / 3}`;
       let listz: Map<any, any> = new Map([[String.fromCharCode(108,105,98,109,95,50,95,53,52,0),642], [String.fromCharCode(99,95,53,49,95,119,104,101,110,0),863]]);
       let zoom7 = String.fromCharCode(121,95,53,52,95,115,104,111,117,108,100,0);
       let sellmathbackgroundY = String.fromCharCode(105,95,49,56,95,114,101,112,108,97,99,105,110,103,0);
       let time__ek = String.fromCharCode(111,95,51,56,95,108,101,114,112,105,110,103,0);
         sellmathbackgroundY += `${time__ek.length}`;
          let clockq = false;
          let executorE: Array<any> = [384, 116, 499];
         zoom7 = `${listz.size}`;
         clockq = clockq && executorE.length > 54;
         executorE = [((clockq ? 5 : 3) >> (Math.min(executorE.length, 3)))];
         zoom7 = `${zoom7.length - 2}`;
          let downarrowr = true;
          let soundj = String.fromCharCode(112,114,105,111,114,95,100,95,56,49,0);
          let searche = String.fromCharCode(102,95,53,48,95,104,113,97,100,115,112,0);
         sellmathbackgroundY = `${time__ek.length}`;
         downarrowr = !soundj.endsWith(`${downarrowr}`);
         soundj += `${((downarrowr ? 4 : 4) / (Math.max(soundj.length, 9)))}`;
         searche = `${soundj.length}`;
      for (let b = 0; b < 1; b++) {
         sellmathbackgroundY += "3";
      }
      latn0 = `${latn0.length - 3}`;
   while (5 >= thumbnailh.length) {
      thumbnailh = `${update_5kk.length}`;
      break;
   }
   for (let o = 0; o < 3; o++) {
       let iconfeedbackR = 2;
       let changeH = String.fromCharCode(97,118,101,114,114,111,114,95,57,95,52,0);
       let iconuserU = String.fromCharCode(108,117,109,98,101,114,106,97,99,107,95,57,95,51,0);
       let regengq = true;
      if (iconuserU.length <= 5) {
         iconuserU = `${changeH.length << (Math.min(Math.abs(2), 5))}`;
      }
         changeH = `${iconuserU.length}`;
         iconuserU = `${((regengq ? 4 : 1))}`;
          let right_: Array<any> = [767, 406];
         iconuserU += `${((regengq ? 3 : 1) | 3)}`;
         right_ = [right_.length * 2];
      for (let j = 0; j < 3; j++) {
          let auto_2qd = true;
          let libsentry9 = 0.0;
         iconuserU += `${changeH.length}`;
         auto_2qd = 46.35 == libsentry9;
         libsentry9 *= (parseFloat(`${(auto_2qd ? 2 : 2) % (Math.max(parseInt(`${libsentry9}`), 9))}`));
      }
         iconuserU += `${(iconuserU == String.fromCharCode(80,0) ? iconuserU.length : (regengq ? 4 : 4))}`;
       let iconbackwhiteG = String.fromCharCode(98,95,53,52,95,114,101,115,116,114,105,99,116,105,111,110,115,0);
       let executor3 = String.fromCharCode(99,111,108,111,110,95,106,95,53,55,0);
       let layoutJ = String.fromCharCode(118,95,54,49,95,115,117,98,112,97,114,116,0);
       let sellZ = String.fromCharCode(114,95,54,55,95,102,117,110,110,121,0);
       let type_qF = String.fromCharCode(100,101,99,101,108,101,114,97,116,105,110,103,95,100,95,53,57,0);
         iconfeedbackR <<= Math.min(Math.abs(iconuserU.length * 2), 1);
      let templateprocessorM = iconuserU.length <= 6707920;
      do {
         iconuserU = "2";
         if (templateprocessorM) {
            break;
         }
      } while ((!regengq) && templateprocessorM);
      for (let u = 0; u < 3; u++) {
         regengq = layoutJ.length <= type_qF.length;
      }
      cornershootv += `${1 >> (Math.min(2, Math.abs(iconfeedbackR)))}`;
   }
      styleY.set(`${feedbackZ}`, 2 * feedbackZ);
      componentRef.current.measure((x, y, width, height, pageX, pageY) => {
        
        setRefPosition({x: pageX, y: pageY, width: width, height: height});
      });
    }
  };

  return (
    <View style={{ paddingHorizontal: 10 ,position:'relative' , bottom:255 }}     onLayout={() => getPosition()}
    ref={componentRef}>
   
      <View
        style={{
          width: width ?? "70%",
          alignSelf: "flex-end",
          justifyContent: isLeft ? "flex-end" : "flex-start",
        }}
      >
            <View>
          <View style={{ alignSelf: "center" }}>
            <Text style={[styles.confirmText]} onPress={() => closeModal()}>
              我知道了
            </Text>
          </View>
        </View>
        <View style={[styles.buyContainer]}>
          {contentTemplate == 1 && (
            <>
              <Text style={[styles.normalText]}>
                {`购买`}
                <Text style={[styles.goldText]}>VIP</Text>
                <Text
                  style={[styles.normalText]}
                >{`  去除广告\n高清视频`}</Text>
                <Text style={[styles.goldText]}>{`纯净观看`}</Text>
              </Text>
            </>
          )}

          {contentTemplate == 2 && (
            <>
              <Text style={[styles.normalText]}>
                {`购买`}
                <Text style={[styles.goldText]}>VIP</Text>

                <Text style={[styles.goldText]}>{`\n离线下载`}</Text>
                <Text style={[styles.normalText]}>{`高清视频`}</Text>
              </Text>
            </>
          )}

          {contentTemplate == 3 && (
            <>
              <Text style={[styles.normalText]}>
                {`购买`}
                <Text style={[styles.goldText]}>VIP</Text>
                <Text
                  style={[styles.normalText]}
                >{`免费看NBA/五大联赛\n`}</Text>
                <Text style={[styles.normalText]}>{`实时`}</Text>
                <Text style={[styles.goldText]}>{`高清直播`}</Text>
              </Text>
            </>
          )}

          <View
            style={{ paddingHorizontal: 10, width: "100%", paddingTop: 10 }}
          >
            <TouchableOpacity
              onPress={() => {
                navigator.navigate("付费VIP");
                closeModal();
              }}
            >
              <LinearGradient
                colors={["#D1AC7D", "#B1885F"]}
                locations={[0.0, 0.99]}
                style={{
                  height: 40,
                  justifyContent: "center",
                  alignItems: "center",
                  paddingVertical: 8,
                  borderRadius: 8,
                }}
              >
                <Text
                  style={{
                    fontWeight: "900",
                    letterSpacing: 1,
                    color: "black",
                  }}
                >
                  立即购买
                </Text>
              </LinearGradient>
            </TouchableOpacity>
          </View>
        </View>
        <View style={{ height: 0 }} />
    
      </View>

      {isLeft ? (
        <View style={{ alignSelf: "flex-start", paddingLeft: 30 }}>
          <Image source={require(`@static/images/vip_guide_arrow1.png`)}></Image>
        </View>
      ) : (
        <View style={{ alignSelf: "flex-end", paddingRight: 30 }}>
          <Image source={require(`@static/images/vip_guide_arrow1.png`)}></Image>
        </View>
      )}
    </View>
  );
}

const styles = StyleSheet.create({
  buyContainer: {
    borderRadius: 10,
    borderColor: "white",
    borderWidth: 1,
    marginHorizontal: 10,
    paddingVertical: 12,
  },
  normalText: {
    fontSize: 18,
    color: "white",
    textAlign: "center",
  },
  goldText: {
    fontSize: 18,
    color: "#BB9266",
    textAlign: "center",
  },
  confirmText: {
    borderRadius: 15,
    borderWidth: 1,
    borderColor: "#9C9C9C",
    color: "#9C9C9C",
    fontSize: 12,
    paddingHorizontal: 10,
    paddingVertical: 5,
    textAlign: "center",
    position:'relative',
    bottom:8
  },
});
