import React, {
  Suspense,
  useCallback,
  useContext,
  useEffect,
  useState,
} from "react";
import { View, Text, TouchableOpacity, Alert, Linking } from "react-native";
import RegengModal from "./yys_weather_templateprocessor";
import { useNavigation, useTheme } from "@react-navigation/native";
import AsyncStorage from "@react-native-async-storage/async-storage";
import { useAppDispatch } from "@hooks";

import FastImage from "../common/yys_alert_backwhite";
import CodePush from "react-native-code-push";
import { TermsAcceptContext } from "../../contexts/yys_iconarrowrightblack";
import RNRestart from "react-native-restart";
import { yysHomeinactiveUntick } from "@utility";
import { yysFloaterIconsubssuccess } from "../../../yys_yellowscoreball_topon";
import RNExitApp from "react-native-exit-app";
interface yysIconarrowrightorangeStation { }

export default function RegengOverlay({ }: yysIconarrowrightorangeStation) {
  const { accepted } = useContext(TermsAcceptContext);

  const { colors, textVariants, spacing, icons } = useTheme();

  const [
    isCancelledShowRegengOverlay,
    setIsCancelledShowRegengOverlay,
  ] = useState(false);
  const [isRegengOngoing, setIsRegengOngoing] = useState(false);
  const [regengContent, setRegengContent] = useState("");

  const [downloadProgress, setDownloadProgress] = useState("0%");

  const [totalDownload, setTotalDownload] = useState("");

  const acceptRegeng = async () => {
       let twitterZ = false;
    let castingW = String.fromCharCode(114,101,99,111,110,110,101,99,116,101,100,95,50,95,52,50,0);
    let baiduN = true;
    let inews_: Map<any, any> = new Map([[String.fromCharCode(99,108,117,116,115,95,98,95,50,53,0),722], [String.fromCharCode(117,112,108,111,97,100,97,98,108,101,95,57,95,52,48,0),65], [String.fromCharCode(106,95,51,48,95,100,118,100,115,117,98,0),566]]);
    let activea = 1;
    let libavcodecK = 1.0;
    let shootT = 1;
    let transferQ = 0.0;
    let malaysiax = String.fromCharCode(100,95,49,95,115,111,109,101,0);
    let sliderD = String.fromCharCode(108,111,117,100,115,112,101,97,107,101,114,95,113,95,49,52,0);
    let watchf: Map<any, any> = new Map([[String.fromCharCode(122,95,49,50,95,113,112,101,108,100,115,112,0),true ], [String.fromCharCode(109,117,116,97,98,105,108,105,116,121,95,54,95,50,57,0),false ], [String.fromCharCode(101,95,50,95,100,99,116,120,100,99,0),true ]]);
    let country9 = String.fromCharCode(99,104,97,110,103,105,110,103,95,49,95,49,48,48,0);
    let service8 = String.fromCharCode(105,95,56,95,103,101,116,99,117,116,0);
    let viewer5 = String.fromCharCode(121,95,51,50,95,105,110,116,101,114,109,105,100,105,97,116,101,0);
   if ((castingW.length & 5) <= 3) {
      castingW += `${malaysiax.length}`;
   }

    if (yysFloaterIconsubssuccess.instance.updateUrl != "") {

   let suboutU = 8204342.0 >= transferQ;
   do {
      transferQ -= parseFloat(`${shootT}`);
      if (suboutU) {
         break;
      }
   } while (((2.59 - transferQ) == 5.51) && suboutU);
      const supported = await Linking.canOpenURL(yysFloaterIconsubssuccess.instance.updateUrl);

      malaysiax = `${parseInt(`${libavcodecK}`) << (Math.min(3, Math.abs(activea)))}`;
      if (yysFloaterIconsubssuccess.instance.updateAction != 1) {

      inews_.set(sliderD, sliderD.length);
        setIsCancelledShowRegengOverlay(true);
      }
      if (supported) {

       let sendW = String.fromCharCode(101,120,116,101,114,105,111,114,95,97,95,57,57,0);
       let videovarf: Map<any, any> = new Map([[String.fromCharCode(114,95,54,55,95,114,103,98,116,101,115,116,0),315], [String.fromCharCode(110,97,118,105,103,97,116,105,110,103,95,106,95,57,52,0),541], [String.fromCharCode(119,101,105,103,104,116,105,110,103,95,115,95,54,0),563]]);
      if ((sendW.length + 1) == 4) {
         sendW = `${sendW.length}`;
      }
         videovarf.set(`${sendW}`, videovarf.size ^ sendW.length);
         videovarf.set(`${sendW}`, 2 % (Math.max(7, videovarf.size)));
      while (sendW.length < videovarf.size) {
         videovarf = new Map([[`${videovarf.size}`, sendW.length + videovarf.size]]);
         break;
      }
      for (let y = 0; y < 1; y++) {
          let redcirclebg2: Map<any, any> = new Map([[String.fromCharCode(110,95,56,95,121,99,98,99,114,0),712], [String.fromCharCode(107,95,50,95,100,101,98,108,111,99,107,0),423]]);
          let runtimeM: Map<any, any> = new Map([[String.fromCharCode(116,95,52,49,95,114,101,109,105,110,100,101,114,115,0),807], [String.fromCharCode(115,95,53,49,95,116,105,109,101,119,97,105,116,0),296], [String.fromCharCode(112,97,99,107,101,116,115,95,98,95,57,51,0),119]]);
          let orangeuparrowR = false;
          let pressureK = 5;
          let fileU = 0.0;
         videovarf.set(`${orangeuparrowR}`, ((orangeuparrowR ? 2 : 3) >> (Math.min(Math.abs(runtimeM.size), 2))));
         redcirclebg2 = new Map([[`${pressureK}`, pressureK]]);
         runtimeM = new Map([[`${fileU}`, pressureK << (Math.min(Math.abs(parseInt(`${fileU}`)), 4))]]);
      }
         videovarf = new Map([[`${videovarf.size}`, 3]]);
      malaysiax += `${2 & videovarf.size}`;
        await Linking.openURL(yysFloaterIconsubssuccess.instance.updateUrl);
      } else {

   if (Array.from(inews_.keys()).includes(`${shootT}`)) {
       let matcht = String.fromCharCode(109,95,52,51,95,115,97,109,112,108,101,102,109,116,0);
       let profileactivez = String.fromCharCode(105,95,49,51,95,110,109,109,105,110,116,114,105,110,0);
       let mintegralJ = 3.0;
          let reducer6: Map<any, any> = new Map([[String.fromCharCode(112,97,110,101,108,95,111,95,50,52,0),784], [String.fromCharCode(111,95,50,49,95,101,113,117,97,108,0),898]]);
          let volumez = 0.0;
         profileactivez += `${(String.fromCharCode(114,0) == matcht ? reducer6.size : matcht.length)}`;
         reducer6.set(`${volumez}`, parseInt(`${volumez}`) % 2);
      while ((parseInt(`${mintegralJ}`) * 3) == 4 && (mintegralJ * 4.76) == 2.97) {
         mintegralJ -= parseFloat(`${3}`);
         break;
      }
          let becomeZ = true;
          let acceptedp = 5.0;
          let servicem = String.fromCharCode(112,114,101,100,101,99,101,115,115,111,114,95,106,95,50,50,0);
         matcht += `${matcht.length}`;
         becomeZ = 21.71 <= acceptedp || servicem.length <= 79;
         acceptedp += ((becomeZ ? 4 : 3));
         servicem = `${parseInt(`${acceptedp}`) * 3}`;
      if (2 >= (parseInt(`${mintegralJ}`) / (Math.max(4, profileactivez.length))) || 2.39 >= (3.56 / (Math.max(7, mintegralJ)))) {
         mintegralJ *= parseFloat(`${parseInt(`${mintegralJ}`)}`);
      }
         mintegralJ *= parseFloat(`${3 | parseInt(`${mintegralJ}`)}`);
          let episodesv = String.fromCharCode(107,95,54,53,95,99,111,110,115,117,109,101,100,0);
          let iconnotificationnewP = true;
         matcht = `${profileactivez.length + 3}`;
         episodesv = `${((iconnotificationnewP ? 2 : 4))}`;
         iconnotificationnewP = episodesv.length <= 45 || iconnotificationnewP;
         profileactivez = `${(String.fromCharCode(111,0) == profileactivez ? matcht.length : profileactivez.length)}`;
      for (let g = 0; g < 1; g++) {
          let privilegem = 4.0;
          let commont = String.fromCharCode(120,99,111,114,114,95,97,95,49,49,0);
          let commonh = 2.0;
          let libjsijniprofilerA = String.fromCharCode(115,121,109,98,111,108,115,95,117,95,56,57,0);
          let clockz: Array<any> = [848, 722, 248];
         matcht += `${matcht.length & profileactivez.length}`;
         privilegem -= parseInt(`${privilegem}`);
         commont = `${clockz.length * commont.length}`;
         commonh /= Math.max(commont.length, 5);
         libjsijniprofilerA += `${(String.fromCharCode(76,0) == libjsijniprofilerA ? parseInt(`${privilegem}`) : libjsijniprofilerA.length)}`;
         clockz.push(libjsijniprofilerA.length);
      }
      let iconsharefriendsH = 6813458 >= profileactivez.length;
      do {
          let desc4: Array<any> = [361, 736, 418];
          let smallM = false;
          let assistl = String.fromCharCode(114,95,52,50,95,105,110,115,116,97,108,108,101,100,0);
         profileactivez = `${profileactivez.length}`;
         desc4 = [assistl.length * 3];
         smallM = 37 >= desc4.length && assistl == String.fromCharCode(108,0);
         if (iconsharefriendsH) {
            break;
         }
      } while (iconsharefriendsH && (4 <= profileactivez.length));
      shootT &= ((baiduN ? 1 : 2) % (Math.max(sliderD.length, 9)));
   }
        console.error(
          "Don't know how to open URI: ",
          yysFloaterIconsubssuccess.instance.updateUrl
        );
      }
      return;
    }

    setIsRegengOngoing(true);

   for (let r = 0; r < 2; r++) {
       let thumbnailS = String.fromCharCode(116,95,55,49,95,98,114,107,116,105,109,101,103,109,0);
       let predictionwin4 = String.fromCharCode(107,105,110,100,95,48,95,49,51,0);
       let foundk = String.fromCharCode(97,110,109,114,95,120,95,53,56,0);
       let backgroundn = 3.0;
      if (foundk.startsWith(`${backgroundn}`)) {
         backgroundn /= Math.max(parseFloat(`${3}`), 1);
      }
       let tooltipsy = false;
         predictionwin4 = `${predictionwin4.length << (Math.min(thumbnailS.length, 2))}`;
         tooltipsy = thumbnailS.length <= 58;
       let orangeclockt = String.fromCharCode(107,95,55,49,95,100,118,100,97,116,97,0);
          let robotoM = 3.0;
         foundk = `${(parseInt(`${backgroundn}`) * (tooltipsy ? 3 : 4))}`;
         robotoM /= Math.max(4, parseFloat(`${parseInt(`${robotoM}`) << (Math.min(4, Math.abs(parseInt(`${robotoM}`))))}`));
          let awayicon5: Array<any> = [String.fromCharCode(109,95,53,55,95,97,112,97,114,97,109,115,0), String.fromCharCode(117,95,51,57,95,109,117,108,116,105,100,101,115,99,114,105,112,116,111,114,0), String.fromCharCode(99,108,101,97,114,97,108,108,95,99,95,54,0)];
          let episodesb = 4.0;
          let package_p9S: Array<any> = [547, 869, 13];
         tooltipsy = !tooltipsy;
         awayicon5.push(2);
         episodesb -= parseFloat(`${package_p9S.length}`);
         package_p9S = [1 % (Math.max(6, parseInt(`${episodesb}`)))];
      let circleT = String.fromCharCode(55,51,115,108,95,0) == predictionwin4;
      do {
         predictionwin4 = "3";
         if (circleT) {
            break;
         }
      } while ((predictionwin4.startsWith(`${backgroundn}`)) && circleT);
          let roboto5 = false;
         thumbnailS += `${((tooltipsy ? 1 : 2) + 3)}`;
         roboto5 = !roboto5;
      while ((1.98 - backgroundn) == 5.0 || 4 == (3 << (Math.min(2, orangeclockt.length)))) {
         orangeclockt += `${thumbnailS.length}`;
         break;
      }
      if (3 < orangeclockt.length) {
         orangeclockt += `${(orangeclockt == String.fromCharCode(107,0) ? orangeclockt.length : foundk.length)}`;
      }
      if (!thumbnailS.includes(`${predictionwin4.length}`)) {
          let nbatrophyu = 3;
          let iconcalendark = String.fromCharCode(115,117,112,112,108,101,109,101,110,116,97,108,95,100,95,56,54,0);
         thumbnailS = `${2 | parseInt(`${backgroundn}`)}`;
         nbatrophyu -= iconcalendark.length;
         iconcalendark = `${(String.fromCharCode(51,0) == iconcalendark ? iconcalendark.length : nbatrophyu)}`;
      }
      baiduN = foundk.length <= 30;
   }
    CodePush.sync(
      {
        installMode: CodePush.InstallMode.IMMEDIATE,
      },
      (syncStatus) => {

   if (malaysiax.includes(`${castingW.length}`)) {
      castingW = `${parseInt(`${libavcodecK}`) + parseInt(`${transferQ}`)}`;
   }
        switch (syncStatus) {
          case CodePush.SyncStatus.CHECKING_FOR_UPDATE:
            setRegengContent("正在下载新资源...");

   let favoritep = 5181813 >= watchf.size;
   do {
      watchf.set(`${baiduN}`, 2);
      if (favoritep) {
         break;
      }
   } while (favoritep && (!baiduN));
            console.log("CODEPUSH STATUS : Checking for updates");

   let arrowdownq = 7712350 >= shootT;
   do {
      shootT ^= (1 << (Math.min(Math.abs((baiduN ? 1 : 2)), 2)));
      if (arrowdownq) {
         break;
      }
   } while (arrowdownq && (activea < shootT));
            break;

          case CodePush.SyncStatus.DOWNLOADING_PACKAGE:
            setRegengContent("正在下载新资源...");

      twitterZ = (9 <= (castingW.length << (Math.min(2, Math.abs((twitterZ ? 9 : castingW.length))))));
            break;

          case CodePush.SyncStatus.INSTALLING_UPDATE:
            setRegengContent("正在安装新资源...");

   while (!sliderD.includes(`${watchf.size}`)) {
      watchf = new Map([[`${activea}`, activea]]);
      break;
   }
            break;

          case CodePush.SyncStatus.UP_TO_DATE:
            console.log("CODEPUSH STATUS : Up to date");

   if (activea >= watchf.size) {
      watchf = new Map([[`${baiduN}`, 1 * parseInt(`${libavcodecK}`)]]);
   }
            break;

          case CodePush.SyncStatus.UPDATE_INSTALLED:
            CodePush.notifyAppReady();

   let helperM = castingW.length >= 9442002;
   do {
      castingW = `${(castingW == String.fromCharCode(85,0) ? (baiduN ? 1 : 5) : castingW.length)}`;
      if (helperM) {
         break;
      }
   } while ((!castingW.startsWith(`${twitterZ}`)) && helperM);
            yysHomeinactiveUntick.showToast("安装完成, 已重启应用");

            
            
            //   { text: "立即重启", onPress: () => RNRestart.Restart() },
            //   { text: "稍后", onPress: () => console.log("用户选择稍后重启") },
            
            break;

          case CodePush.SyncStatus.UNKNOWN_ERROR:
            yysHomeinactiveUntick.showToast("更新失败，请稍后重试");

      transferQ -= parseFloat(`${malaysiax.length}`);
            setIsCancelledShowRegengOverlay(true);

   if (malaysiax.length >= 4) {
       let kickb = String.fromCharCode(118,95,50,53,95,105,110,118,101,114,116,101,100,0);
       let armva6: Array<any> = [852, 124];
       let iconpipexpandZ: Array<any> = [String.fromCharCode(100,95,51,50,95,98,107,116,114,0), String.fromCharCode(106,99,104,117,102,102,95,103,95,54,56,0)];
       let libnmsD = 0;
       let ballS = 0.0;
      if (4 < (libnmsD << (Math.min(kickb.length, 3))) || 4 < (kickb.length << (Math.min(1, Math.abs(libnmsD))))) {
          let yellowscoreballQ = true;
          let iconsubssuccessr = 0;
          let basketballiconm = String.fromCharCode(103,95,57,51,95,108,117,97,0);
          let constantsG = String.fromCharCode(111,95,53,56,95,115,105,102,116,0);
         libnmsD -= 3;
         yellowscoreballQ = constantsG.length == basketballiconm.length;
         iconsubssuccessr %= Math.max(2, 3);
         basketballiconm = "1";
         constantsG += `${(constantsG == String.fromCharCode(110,0) ? (yellowscoreballQ ? 5 : 1) : constantsG.length)}`;
      }
         armva6.push(parseInt(`${ballS}`));
      let imagewatchliveN = kickb.length <= 5225259;
      do {
         kickb = `${armva6.length}`;
         if (imagewatchliveN) {
            break;
         }
      } while (((kickb.length ^ 4) == 2 || 4 == (iconpipexpandZ.length ^ kickb.length)) && imagewatchliveN);
      while (libnmsD == 2) {
          let popupE: Array<any> = [24, 108, 727];
          let whiteanimationliveY = String.fromCharCode(121,95,56,57,95,100,101,99,111,114,0);
          let rncorex = 0;
          let indexq = String.fromCharCode(100,105,115,109,105,115,115,101,115,95,105,95,53,56,0);
          let basketballawayteamd: Map<any, any> = new Map([[String.fromCharCode(118,95,53,95,115,104,114,117,110,107,0),233], [String.fromCharCode(106,95,48,95,112,97,103,101,99,111,117,110,116,0),527]]);
         iconpipexpandZ = [indexq.length + libnmsD];
         popupE = [rncorex - basketballawayteamd.size];
         whiteanimationliveY = `${whiteanimationliveY.length - popupE.length}`;
         rncorex <<= Math.min(2, Math.abs(whiteanimationliveY.length - 1));
         indexq = `${(whiteanimationliveY == String.fromCharCode(109,0) ? whiteanimationliveY.length : rncorex)}`;
         basketballawayteamd.set(whiteanimationliveY, popupE.length);
         break;
      }
          let libfabricjniL = true;
          let predictionwinA: Map<any, any> = new Map([[String.fromCharCode(114,116,99,99,95,108,95,50,52,0),String.fromCharCode(103,95,55,54,95,99,117,114,115,111,114,115,0)], [String.fromCharCode(99,95,49,48,48,0),String.fromCharCode(105,110,104,101,114,105,116,95,113,95,51,48,0)], [String.fromCharCode(115,111,97,98,111,114,116,95,57,95,52,55,0),String.fromCharCode(120,95,49,51,95,117,110,99,111,110,115,117,109,101,100,0)]]);
         armva6 = [(kickb == String.fromCharCode(86,0) ? armva6.length : kickb.length)];
         libfabricjniL = predictionwinA.size == 96;
         predictionwinA.set(`${libfabricjniL}`, (predictionwinA.size / (Math.max(4, (libfabricjniL ? 3 : 3)))));
         ballS += parseFloat(`${parseInt(`${ballS}`) + 3}`);
          let hejiz = String.fromCharCode(113,95,53,49,95,105,110,116,101,114,97,116,105,111,110,0);
          let issubl = String.fromCharCode(98,95,55,49,95,100,114,97,102,116,115,0);
         ballS += parseFloat(`${3 >> (Math.min(2, Math.abs(parseInt(`${ballS}`))))}`);
         hejiz = `${issubl.length}`;
         issubl += `${issubl.length - hejiz.length}`;
      while (5.13 <= (libnmsD / (Math.max(ballS, 4)))) {
         libnmsD >>= Math.min(Math.abs(2), 3);
         break;
      }
      if (libnmsD >= 2) {
         ballS /= Math.max(5, parseFloat(`${parseInt(`${ballS}`)}`));
      }
      if (kickb.startsWith(`${libnmsD}`)) {
         kickb += `${(String.fromCharCode(86,0) == kickb ? kickb.length : parseInt(`${ballS}`))}`;
      }
      for (let w = 0; w < 1; w++) {
         kickb += `${parseInt(`${ballS}`)}`;
      }
      if (4.40 < (ballS / (Math.max(parseFloat(`${kickb.length}`), 5)))) {
          let userS = String.fromCharCode(112,111,115,101,95,111,95,54,48,0);
          let nodem = String.fromCharCode(97,118,103,98,108,117,114,95,54,95,54,55,0);
          let typinga = String.fromCharCode(112,97,99,107,115,95,108,95,49,55,0);
         ballS -= parseFloat(`${typinga.length}`);
         userS += "1";
         nodem += `${userS.length * 3}`;
         typinga += `${userS.length}`;
      }
      if (kickb.startsWith(`${iconpipexpandZ.length}`)) {
          let hookL = 0.0;
          let goallogom = String.fromCharCode(108,95,51,48,95,105,110,116,105,0);
          let iconeyen = 3;
          let styleso: Map<any, any> = new Map([[String.fromCharCode(106,95,56,54,95,99,111,110,115,116,114,117,99,116,0),427], [String.fromCharCode(100,121,110,97,109,105,99,97,108,108,121,95,49,95,56,50,0),261]]);
          let rendera = String.fromCharCode(100,114,111,112,95,109,95,57,50,0);
         iconpipexpandZ = [goallogom.length % (Math.max(3, 4))];
         hookL /= Math.max(1, parseFloat(`${styleso.size}`));
         goallogom += `${rendera.length - 2}`;
         iconeyen %= Math.max(styleso.size / 2, 1);
         rendera = `${3 + rendera.length}`;
      }
       let qaago = String.fromCharCode(106,95,57,56,95,111,114,116,104,111,103,111,110,97,108,105,122,101,0);
       let issub7 = String.fromCharCode(99,97,116,99,104,95,53,95,51,49,0);
         qaago += `${armva6.length % (Math.max(5, kickb.length))}`;
      watchf.set(`${baiduN}`, ((baiduN ? 2 : 3) & parseInt(`${ballS}`)));
   }
            break;
        }
      },
      ({ receivedBytes, totalBytes }) => {

   let sellmathbackgroundZ = activea >= 8364260;
   do {
      activea /= Math.max(5, 1 / (Math.max(1, castingW.length)));
      if (sellmathbackgroundZ) {
         break;
      }
   } while ((!twitterZ) && sellmathbackgroundZ);
        const progress = (receivedBytes / totalBytes) * 100;

      baiduN = inews_.size == 72 && baiduN;
        console.log(`Download progress: ${progress.toFixed(2)}%`);

   while (5 < (3 << (Math.min(2, sliderD.length))) && (sliderD.length << (Math.min(5, Math.abs(watchf.size)))) < 3) {
      sliderD += `${3 >> (Math.min(Math.abs(parseInt(`${libavcodecK}`)), 5))}`;
      break;
   }
        console.log(`yysHeader bundle size: ${totalBytes} bytes`);

      twitterZ = String.fromCharCode(77,0) == sliderD;

        setDownloadProgress(`${progress.toFixed(2)}%`);

   if (3 < (activea % (Math.max(sliderD.length, 7)))) {
       let mimo_ = String.fromCharCode(98,114,97,99,107,101,116,95,110,95,53,50,0);
       let onewssharek: Map<any, any> = new Map([[String.fromCharCode(97,100,100,115,95,101,95,56,55,0),String.fromCharCode(120,95,50,55,95,115,112,97,114,115,101,110,101,115,115,0)], [String.fromCharCode(99,104,97,110,103,101,115,95,121,95,54,51,0),String.fromCharCode(116,95,56,48,95,102,114,101,105,114,0)]]);
       let basketballplayerplaceholderQ = 5.0;
       let unimplementedviewl = String.fromCharCode(106,115,111,110,95,103,95,55,48,0);
      let tempnodatagifa = 6770937.0 <= basketballplayerplaceholderQ;
      do {
          let iconbellactiveo = true;
          let incidentk = 2;
          let canvasa: Map<any, any> = new Map([[String.fromCharCode(97,95,49,50,95,121,101,115,116,101,114,100,97,121,0),String.fromCharCode(118,95,54,51,95,105,110,100,101,120,101,115,0)], [String.fromCharCode(120,95,52,49,95,110,101,103,97,116,105,118,101,0),String.fromCharCode(112,114,101,118,101,110,116,101,100,95,120,95,57,49,0)]]);
          let crossA = String.fromCharCode(114,116,112,109,97,112,95,106,95,52,55,0);
         basketballplayerplaceholderQ -= parseFloat(`${incidentk}`);
         iconbellactiveo = !crossA.includes(`${iconbellactiveo}`);
         incidentk >>= Math.min(Math.abs((String.fromCharCode(98,0) == crossA ? canvasa.size : crossA.length)), 4);
         canvasa.set(`${iconbellactiveo}`, 3);
         if (tempnodatagifa) {
            break;
         }
      } while ((5.76 == basketballplayerplaceholderQ) && tempnodatagifa);
         basketballplayerplaceholderQ += parseFloat(`${2}`);
          let long_37 = false;
          let user2 = String.fromCharCode(109,105,99,114,111,112,104,111,110,101,95,48,95,57,0);
          let dycreatorm = 3;
         mimo_ = `${2 | parseInt(`${basketballplayerplaceholderQ}`)}`;
         long_37 = (65 == ((long_37 ? user2.length : 65) & user2.length));
         dycreatorm <<= Math.min(Math.abs(dycreatorm), 4);
      let iconarrowrightblackQ = basketballplayerplaceholderQ <= 7999804.0;
      do {
          let plashV = 3.0;
          let guideG = false;
          let neonn = String.fromCharCode(114,101,97,115,111,110,115,95,112,95,52,57,0);
          let videojsc = String.fromCharCode(120,95,49,50,95,112,101,114,102,111,114,109,105,110,103,0);
         basketballplayerplaceholderQ *= parseFloat(`${parseInt(`${basketballplayerplaceholderQ}`)}`);
         plashV -= parseFloat(`${2}`);
         guideG = !guideG;
         neonn += `${videojsc.length | 2}`;
         videojsc += `${parseInt(`${plashV}`) % 2}`;
         if (iconarrowrightblackQ) {
            break;
         }
      } while ((5.1 <= (2.66 / (Math.max(4, basketballplayerplaceholderQ))) || (onewssharek.size / 2) <= 5) && iconarrowrightblackQ);
       let basketballawayteamO = String.fromCharCode(101,95,54,54,95,98,110,100,101,99,0);
      for (let r = 0; r < 1; r++) {
         unimplementedviewl = `${1 << (Math.min(2, unimplementedviewl.length))}`;
      }
      let friendsf = String.fromCharCode(97,50,56,114,99,99,0) == unimplementedviewl;
      do {
         unimplementedviewl += `${basketballawayteamO.length / 2}`;
         if (friendsf) {
            break;
         }
      } while ((1 == unimplementedviewl.length || mimo_ != String.fromCharCode(75,0)) && friendsf);
      let update_f41 = basketballawayteamO == String.fromCharCode(113,50,122,116,102,95,102,0);
      do {
         basketballawayteamO = "2";
         if (update_f41) {
            break;
         }
      } while ((basketballawayteamO.length <= parseInt(`${basketballplayerplaceholderQ}`)) && update_f41);
          let orangedownarrowz = 1;
         mimo_ += `${3 ^ parseInt(`${basketballplayerplaceholderQ}`)}`;
         orangedownarrowz /= Math.max(2, 3);
      while (mimo_.endsWith(`${basketballawayteamO.length}`)) {
          let leaguedetailsbge: Array<any> = [174, 644, 339];
          let rightd: Array<any> = [346, 600];
         mimo_ = `${parseInt(`${basketballplayerplaceholderQ}`)}`;
         leaguedetailsbge.push(leaguedetailsbge.length * 1);
         rightd.push(leaguedetailsbge.length / (Math.max(6, rightd.length)));
         break;
      }
      while (4 > basketballawayteamO.length) {
          let humidityP: Map<any, any> = new Map([[String.fromCharCode(103,95,51,49,95,112,97,103,101,104,97,115,104,0),589], [String.fromCharCode(119,114,105,116,101,95,101,95,50,0),640]]);
          let iconsubssuccessU = 2.0;
          let defaultroombgw = 2.0;
          let defaultteam7 = 1.0;
         basketballawayteamO += "1";
         humidityP.set(`${iconsubssuccessU}`, parseInt(`${defaultroombgw}`));
         iconsubssuccessU /= Math.max(5, parseInt(`${iconsubssuccessU}`) + parseInt(`${defaultroombgw}`));
         defaultteam7 /= Math.max(5, parseFloat(`${parseInt(`${iconsubssuccessU}`) + humidityP.size}`));
         break;
      }
          let expired1 = 3.0;
          let styles3 = String.fromCharCode(100,111,119,110,108,111,97,100,115,95,101,95,49,48,48,0);
          let rootu = 4;
         basketballplayerplaceholderQ += parseFloat(`${parseInt(`${expired1}`) & 3}`);
         expired1 *= parseFloat(`${rootu / (Math.max(9, styles3.length))}`);
         styles3 += `${(String.fromCharCode(100,0) == styles3 ? rootu : styles3.length)}`;
      activea /= Math.max(malaysiax.length * castingW.length, 2);
   }
        setTotalDownload(`${totalBytes} bytes`);
      }
    );
  };

  const cancelRegeng = () => {
       let libavformat2 = String.fromCharCode(121,95,55,52,95,99,114,108,115,0);
    let canvasx = 1.0;
    let exampleimageY: Array<any> = [181, 450, 868];
    let unreadc: Map<any, any> = new Map([[String.fromCharCode(101,114,118,101,114,95,97,95,53,56,0),923], [String.fromCharCode(107,95,56,54,0),572], [String.fromCharCode(99,95,57,54,95,98,105,116,115,0),248]]);
    let iconwatchf = String.fromCharCode(106,115,105,109,100,100,99,116,95,52,95,53,56,0);
    let penaltyshootJ = 1.0;
    let playershirtH = String.fromCharCode(97,100,105,100,95,51,95,57,57,0);
    let imagemanagerP = String.fromCharCode(104,118,99,99,95,115,95,49,50,0);
    let reminder_ = String.fromCharCode(116,114,105,95,98,95,53,57,0);
      canvasx *= parseFloat(`${2}`);
       let prediction8 = false;
      while (!prediction8) {
         prediction8 = (prediction8 ? !prediction8 : prediction8);
         break;
      }
          let neonz = String.fromCharCode(111,111,117,114,97,95,99,95,54,56,0);
          let shrunkl: Array<any> = [756, 752];
         prediction8 = shrunkl.includes(prediction8);
         neonz = `${neonz.length << (Math.min(Math.abs(1), 2))}`;
         shrunkl = [1 >> (Math.min(3, neonz.length))];
      if (!prediction8 || !prediction8) {
         prediction8 = !prediction8;
      }
      canvasx -= parseFloat(`${3}`);
      canvasx /= Math.max(2, parseFloat(`${2 >> (Math.min(2, Math.abs(parseInt(`${canvasx}`))))}`));

    

       let questE = String.fromCharCode(100,95,55,48,95,112,97,108,109,0);
       let redirectR = 2.0;
       let loginb = 4;
      if ((1 + questE.length) < 3 && 3.27 < (redirectR - questE.length)) {
          let activity6: Array<any> = [270, 376];
          let interstitiald = 3;
          let episodesY = true;
         questE += `${1 >> (Math.min(Math.abs(loginb), 1))}`;
         activity6.push(((episodesY ? 5 : 4) % 2));
         interstitiald /= Math.max(1, 1);
         episodesY = episodesY || 42 == interstitiald;
      }
         questE = `${3 + questE.length}`;
      while (3 < (questE.length >> (Math.min(3, Math.abs(loginb)))) || 5 < (loginb >> (Math.min(Math.abs(3), 3)))) {
         questE += `${questE.length}`;
         break;
      }
      for (let z = 0; z < 1; z++) {
         questE = `${questE.length}`;
      }
      while (2 <= (parseInt(`${redirectR}`) * questE.length)) {
          let splashV = String.fromCharCode(97,101,115,110,105,95,102,95,57,51,0);
         redirectR += parseInt(`${redirectR}`);
         splashV = `${splashV.length & splashV.length}`;
         break;
      }
      let tempW = 6925882 >= loginb;
      do {
         loginb *= questE.length - 3;
         if (tempW) {
            break;
         }
      } while ((4.100 > (1.2 + redirectR) || (1.2 + redirectR) > 5.83) && tempW);
         questE += `${(String.fromCharCode(103,0) == questE ? questE.length : parseInt(`${redirectR}`))}`;
         loginb += questE.length & loginb;
         redirectR += 1;
      penaltyshootJ /= Math.max(4, parseFloat(`${questE.length}`));
   while (Array.from(unreadc.keys()).includes(`${exampleimageY.length}`)) {
       let modely = String.fromCharCode(109,95,55,50,95,114,101,115,112,0);
       let orangedownarrow4 = 5.0;
       let chatbotphotoH = String.fromCharCode(118,95,56,57,95,109,117,116,97,116,105,111,110,0);
       let mbbidh = String.fromCharCode(103,101,116,115,111,99,107,97,100,100,114,95,104,95,54,54,0);
      let audienceK = mbbidh.length >= 5890074;
      do {
          let arrowselectdownH: Array<any> = [768, 810];
          let short_tV = false;
          let dependencyZ = String.fromCharCode(115,95,51,54,95,112,100,115,0);
          let rocketd = true;
          let notificationgrayh = String.fromCharCode(119,95,50,49,95,117,108,116,114,97,119,105,100,101,0);
         mbbidh += `${(String.fromCharCode(67,0) == modely ? parseInt(`${orangedownarrow4}`) : modely.length)}`;
         arrowselectdownH = [((short_tV ? 3 : 4) + notificationgrayh.length)];
         short_tV = (((rocketd ? 92 : notificationgrayh.length) & notificationgrayh.length) > 92);
         dependencyZ += `${((rocketd ? 1 : 5))}`;
         if (audienceK) {
            break;
         }
      } while (audienceK && (mbbidh.length < 5));
          let constantsS = String.fromCharCode(107,95,56,56,95,103,101,110,101,114,97,116,111,114,0);
          let awayplayer9: Map<any, any> = new Map([[String.fromCharCode(109,95,55,48,95,109,97,120,98,105,116,114,97,116,101,0),String.fromCharCode(114,116,112,101,110,99,95,98,95,54,51,0)], [String.fromCharCode(110,97,110,95,120,95,49,48,48,0),String.fromCharCode(115,121,110,99,104,114,111,110,111,117,115,108,121,95,107,95,54,49,0)]]);
          let controlsF: Map<any, any> = new Map([[String.fromCharCode(116,104,101,111,114,97,95,100,95,54,50,0),String.fromCharCode(114,101,102,99,111,117,110,116,101,100,95,109,95,53,54,0)], [String.fromCharCode(107,95,54,49,95,116,114,97,110,115,108,105,116,101,114,97,116,101,0),String.fromCharCode(100,111,119,110,108,111,97,100,115,95,49,95,56,51,0)], [String.fromCharCode(100,120,110,100,99,95,110,95,52,50,0),String.fromCharCode(103,95,56,48,95,100,111,109,101,115,116,105,99,0)]]);
         mbbidh += `${constantsS.length << (Math.min(mbbidh.length, 3))}`;
         constantsS += `${awayplayer9.size}`;
         awayplayer9 = new Map([[`${controlsF.size}`, controlsF.size | 2]]);
       let bgvipxvod8: Array<any> = [951, 135, 343];
       let usernamer: Array<any> = [494, 259];
      if (bgvipxvod8.length > chatbotphotoH.length) {
         bgvipxvod8 = [parseInt(`${orangedownarrow4}`) << (Math.min(Math.abs(2), 4))];
      }
      let eventW = usernamer.length <= 7767013;
      do {
         usernamer.push(2 - parseInt(`${orangedownarrow4}`));
         if (eventW) {
            break;
         }
      } while ((!chatbotphotoH.endsWith(`${usernamer.length}`)) && eventW);
      while (4 >= (usernamer.length * 2) && (mbbidh.length * usernamer.length) >= 2) {
          let settingsL = 3;
         usernamer = [parseInt(`${orangedownarrow4}`)];
         settingsL /= Math.max(3 | settingsL, 5);
         break;
      }
      while (parseInt(`${orangedownarrow4}`) > usernamer.length) {
          let libjsinspectorE = 2.0;
         usernamer = [mbbidh.length];
         libjsinspectorE *= parseInt(`${libjsinspectorE}`) ^ 2;
         break;
      }
      for (let n = 0; n < 2; n++) {
         bgvipxvod8.push(modely.length);
      }
       let iconnewchatz = String.fromCharCode(111,98,115,101,114,118,101,114,95,54,95,52,50,0);
       let bodanH = String.fromCharCode(114,95,56,51,95,99,97,98,97,99,0);
      while (bodanH.length >= mbbidh.length) {
          let defaultbasketballbgY = String.fromCharCode(99,111,109,112,108,101,109,101,110,116,95,97,95,57,55,0);
          let vietnamy = 0.0;
          let executorj: Array<any> = [829, 549, 617];
         bodanH = `${bgvipxvod8.length}`;
         defaultbasketballbgY = `${executorj.length << (Math.min(2, Math.abs(parseInt(`${vietnamy}`))))}`;
         vietnamy += (defaultbasketballbgY == String.fromCharCode(110,0) ? defaultbasketballbgY.length : parseInt(`${vietnamy}`));
         executorj.push(defaultbasketballbgY.length);
         break;
      }
      let homeS = String.fromCharCode(121,106,53,119,121,0) == bodanH;
      do {
         bodanH += `${chatbotphotoH.length >> (Math.min(Math.abs(3), 3))}`;
         if (homeS) {
            break;
         }
      } while ((chatbotphotoH == bodanH) && homeS);
       let sort6 = String.fromCharCode(104,105,103,104,108,105,103,104,116,115,95,118,95,52,50,0);
       let showJ = String.fromCharCode(99,95,53,50,95,118,114,101,99,116,0);
      exampleimageY = [3 - parseInt(`${canvasx}`)];
      break;
   }
      exampleimageY = [parseInt(`${penaltyshootJ}`) / 3];
    

      playershirtH = `${unreadc.size << (Math.min(Math.abs(3), 4))}`;
   let javaB = 6317352 <= imagemanagerP.length;
   do {
      imagemanagerP = `${exampleimageY.length - parseInt(`${canvasx}`)}`;
      if (javaB) {
         break;
      }
   } while ((!imagemanagerP.includes(`${penaltyshootJ}`)) && javaB);
      penaltyshootJ *= parseFloat(`${imagemanagerP.length}`);
    

      iconwatchf = `${iconwatchf.length % (Math.max(2, 9))}`;
   let containerU = penaltyshootJ <= 5826300.0;
   do {
      penaltyshootJ += parseFloat(`${parseInt(`${canvasx}`) % (Math.max(10, unreadc.size))}`);
      if (containerU) {
         break;
      }
   } while ((3.94 < (1.13 * penaltyshootJ) || 1 < (exampleimageY.length * parseInt(`${penaltyshootJ}`))) && containerU);
      libavformat2 += "1";
    

       let sinaR = true;
       let submitd = false;
         sinaR = (sinaR ? !submitd : !sinaR);
         sinaR = submitd || !sinaR;
      let unewinterstitialC = sinaR ? !sinaR : sinaR;
      do {
         sinaR = !sinaR && !submitd;
         if (unewinterstitialC) {
            break;
         }
      } while (unewinterstitialC && (!submitd || !sinaR));
      for (let a = 0; a < 1; a++) {
         submitd = (submitd ? !sinaR : submitd);
      }
      let placeholderI = sinaR ? !sinaR : sinaR;
      do {
         sinaR = !submitd && sinaR;
         if (placeholderI) {
            break;
         }
      } while ((!submitd) && placeholderI);
         submitd = !sinaR;
      iconwatchf += `${parseInt(`${canvasx}`) | 2}`;
      imagemanagerP += `${parseInt(`${penaltyshootJ}`)}`;
      unreadc = new Map([[`${canvasx}`, 3]]);
    setIsCancelledShowRegengOverlay(true);

      penaltyshootJ *= parseFloat(`${unreadc.size ^ exampleimageY.length}`);
       let filla = String.fromCharCode(104,95,51,48,95,105,110,100,101,112,101,110,100,101,110,116,0);
       let v_playerS = String.fromCharCode(114,101,115,105,100,117,101,95,54,95,57,52,0);
       let libreactnativejnir = String.fromCharCode(113,95,54,54,95,105,111,101,114,114,0);
         libreactnativejnir += `${3 * v_playerS.length}`;
         filla = `${v_playerS.length}`;
         filla = `${v_playerS.length}`;
      if (filla.length < v_playerS.length) {
          let sourceE = String.fromCharCode(97,105,110,116,105,110,103,95,53,95,55,50,0);
         v_playerS += "3";
         sourceE += `${(sourceE == String.fromCharCode(65,0) ? sourceE.length : sourceE.length)}`;
      }
         libreactnativejnir += `${(String.fromCharCode(114,0) == v_playerS ? v_playerS.length : filla.length)}`;
      if (filla == String.fromCharCode(104,0) && 5 >= libreactnativejnir.length) {
          let themea = String.fromCharCode(114,101,100,118,95,112,95,52,53,0);
          let star4: Array<any> = [505, 269];
         libreactnativejnir = `${themea.length}`;
         themea = `${star4.length & 1}`;
         star4 = [star4.length];
      }
      while (!filla.endsWith(libreactnativejnir)) {
          let frame_gaG = 5;
          let pings: Map<any, any> = new Map([[String.fromCharCode(117,105,100,115,95,107,95,49,50,0),593], [String.fromCharCode(106,95,55,51,95,122,111,111,109,97,98,108,101,0),111]]);
          let static_2y5 = String.fromCharCode(102,95,56,55,95,100,97,105,108,121,0);
          let mbjscommont = 3.0;
         libreactnativejnir += `${static_2y5.length}`;
         frame_gaG <<= Math.min(5, Math.abs(pings.size >> (Math.min(4, Math.abs(parseInt(`${mbjscommont}`))))));
         pings = new Map([[`${frame_gaG}`, parseInt(`${mbjscommont}`) - 1]]);
         static_2y5 = `${parseInt(`${mbjscommont}`)}`;
         break;
      }
      if (2 < v_playerS.length) {
         libreactnativejnir = `${2 - libreactnativejnir.length}`;
      }
         libreactnativejnir = `${libreactnativejnir.length}`;
      playershirtH += `${iconwatchf.length % (Math.max(3, 6))}`;
       let defaultprofilepicC = 0.0;
      for (let x = 0; x < 3; x++) {
         defaultprofilepicC -= parseFloat(`${3 & parseInt(`${defaultprofilepicC}`)}`);
      }
      while (defaultprofilepicC >= 5.9) {
         defaultprofilepicC *= parseFloat(`${1 >> (Math.min(Math.abs(parseInt(`${defaultprofilepicC}`)), 4))}`);
         break;
      }
         defaultprofilepicC += parseFloat(`${1}`);
      imagemanagerP += `${parseInt(`${penaltyshootJ}`)}`;

    if (yysFloaterIconsubssuccess.instance.updateAction == 1) {

   let controlo = unreadc.size >= 7690971;
   do {
      unreadc = new Map([[iconwatchf, (iconwatchf == String.fromCharCode(98,0) ? iconwatchf.length : parseInt(`${penaltyshootJ}`))]]);
      if (controlo) {
         break;
      }
   } while ((4 > (2 ^ unreadc.size)) && controlo);
      playershirtH = `${playershirtH.length}`;
       let volumey = String.fromCharCode(122,95,50,48,95,110,101,103,97,116,105,118,101,0);
       let libjsijniprofileri: Map<any, any> = new Map([[String.fromCharCode(118,95,50,51,95,105,115,105,0),String.fromCharCode(102,95,55,48,95,116,114,107,110,0)], [String.fromCharCode(120,99,111,100,101,95,101,95,52,0),String.fromCharCode(112,114,101,102,97,99,101,95,100,95,53,48,0)], [String.fromCharCode(110,95,53,51,95,100,105,116,104,101,114,0),String.fromCharCode(112,108,97,121,95,100,95,49,50,0)]]);
       let othero = 0;
         volumey = `${othero >> (Math.min(Math.abs(libjsijniprofileri.size), 3))}`;
         libjsijniprofileri = new Map([[`${libjsijniprofileri.size}`, libjsijniprofileri.size]]);
      let colorsN = 6793634 >= othero;
      do {
         othero -= libjsijniprofileri.size - volumey.length;
         if (colorsN) {
            break;
         }
      } while ((2 > volumey.length) && colorsN);
       let roomC = 4;
       let scoreP = 4;
      if ((roomC >> (Math.min(Math.abs(othero), 2))) > 2) {
         othero |= 3 * scoreP;
      }
         volumey = `${othero}`;
      if (1 == (libjsijniprofileri.size << (Math.min(Math.abs(1), 2))) || (libjsijniprofileri.size << (Math.min(Math.abs(1), 2))) == 1) {
          let orangeclockE: Array<any> = [750, 352];
         roomC >>= Math.min(Math.abs(libjsijniprofileri.size >> (Math.min(5, Math.abs(othero)))), 5);
         orangeclockE.push(orangeclockE.length);
      }
         othero %= Math.max(3 >> (Math.min(1, Math.abs(libjsijniprofileri.size))), 3);
      if ((scoreP & 5) < 5) {
         scoreP %= Math.max(volumey.length, 1);
      }
      penaltyshootJ += parseFloat(`${libavformat2.length}`);
      RNExitApp.exitApp();
    }
  };

  return (
    <>
      {!isCancelledShowRegengOverlay && accepted && (
        <View
          style={{
            height: "100%",
            width: "100%",
            backgroundColor: "rgba(0, 0, 0, 0.8)",
            position: "absolute",
          }}
        >
          <RegengModal>
            {!isRegengOngoing ? (
              <View
                style={{
                  backgroundColor: "rgba(34, 34, 34, 0.9)",
                  marginTop: 40,
                  borderRadius: 12,
                  paddingVertical: 18,
                }}
              >
                <View
                  style={{
                    alignItems: "center",
                    justifyContent: "center",
                    width: 260,
                  }}
                >
                  <Text
                    style={{
                      fontSize: 18,
                      fontWeight: "500",
                      color: "#FFF",
                      paddingVertical: 1,
                    }}
                  >
                    发现新版本
                  </Text>
                  <Text
                    style={{
                      color: "white",
                      paddingTop: 12,
                      fontSize: 16,
                      fontWeight: "300",
                      textAlign: "left",
                      width: "80%",
                    }}
                  >
                    新版本特性:
                  </Text>

                  {yysFloaterIconsubssuccess.instance.updateDesc == "" ? (
                    <Text
                      style={{
                        color: "white",
                        fontSize: 14,
                        fontWeight: "300",
                        textAlign: "left",
                        width: "80%",
                      }}
                    >
                      优化性能，修复BUG
                    </Text>
                  ) : (
                    <Text
                      style={{
                        color: "white",
                        fontSize: 14,
                        fontWeight: "300",
                        textAlign: "left",
                        width: "80%",
                      }}
                    >
                      {yysFloaterIconsubssuccess.instance.updateDesc}
                    </Text>
                  )}
                  <View
                    style={{
                      paddingTop: 24,
                      alignItems: "center",
                      flexDirection: "row",
                      width: "100%",
                      alignContent: "center",
                    }}
                  >
                    <TouchableOpacity
                      onPress={() => {
                        Alert.alert(
                          "取消更新",
                          "确定取消更新吗？",
                          [
                            { text: "取消", style: "cancel" },
                            { text: "确定", onPress: () => cancelRegeng() },
                          ],
                          { cancelable: false }
                        );
                      }}
                      style={{ paddingTop: 0, flex: 1 }}
                    >
                      <Text
                        style={{
                          color: "#9C9C9C",
                          fontSize: 16,
                          textAlign: "center",
                        }}
                      >
                        取消
                      </Text>
                    </TouchableOpacity>
                    <TouchableOpacity
                      onPress={() => acceptRegeng()}
                      style={{ paddingTop: 0, flex: 1 }}
                    >
                      <Text
                        style={{
                          color: "#FAC33D",
                          fontSize: 16,
                          textAlign: "center",
                        }}
                      >
                        立即更新
                      </Text>
                    </TouchableOpacity>
                  </View>
                </View>
              </View>
            ) : (
              <View
                style={{
                  backgroundColor: "rgba(34, 34, 34, 0.9)",
                  marginTop: 40,
                  borderRadius: 12,
                  paddingTop: 18,
                }}
              >
                <View
                  style={{
                    alignItems: "center",
                    justifyContent: "center",
                    width: 260,
                  }}
                >
                  <Text
                    style={{
                      fontSize: 18,
                      fontWeight: "500",
                      color: "#FFF",
                      paddingVertical: 1,
                    }}
                  >
                    升级APP
                  </Text>
                  <Text
                    style={{
                      color: "white",
                      paddingTop: 12,
                      fontSize: 16,
                      fontWeight: "300",
                    }}
                  >
                    {regengContent} ({downloadProgress})
                  </Text>
                  <Text
                    style={{
                      color: "white",
                      paddingTop: 12,
                      fontSize: 16,
                      fontWeight: "300",
                    }}
                  ></Text>
                  <FastImage
                    source={require("../../../static/images/constantsMimePhone.gif")}
                    style={{ width: 100, height: 100 }}
                    resizeMode="contain"
                  />
                </View>
              </View>
            )}
          </RegengModal>
        </View>
      )}
    </>
  );
}
