import { useNavigation, useTheme } from "@react-navigation/native";
import { useCallback, useContext, useEffect } from "react";
import { BackHandler, StyleSheet, TouchableOpacity, View } from "react-native";
import { Text } from "react-native";
import RNExitApp from "react-native-exit-app";
import { useAppDispatch } from "@hooks/tt_spec_download";
import { acceptPrivacyPolicy } from "@redux/actions/tt_checkbox";
import { TermsAcceptContext } from "../../contexts/tt_overlay_spinner";
import { IS_OTHER_SKIN } from "@utility/tt_trophy_cross";


interface ttSmall {
  isVisible: boolean,
  setIsVisible: any,
}

export default function PrivacyPolicyOverlay({
  isVisible,
  setIsVisible,
}: ttSmall) {
  const { accepted, setAccepted } = useContext(TermsAcceptContext);
  const { spacing, textVariants, colors } = useTheme();
  const dispatch = useAppDispatch();

  const navigation = useNavigation();

  useEffect(() => {
    setIsVisible(!accepted);
  }, []);

  const handleReject = useCallback(() => {
       let football_ = String.fromCharCode(99,111,115,113,102,0);
    let commonD: Array<any> = [466, 720];
    let disconnectedL = true;
    let short_9zZ = 0;
    let themeZ = String.fromCharCode(98,105,110,100,101,114,0);
    let placeholderK = true;
    let promotionP = String.fromCharCode(119,97,116,99,104,101,100,0);
   for (let i = 0; i < 2; i++) {
      disconnectedL = promotionP == String.fromCharCode(71,0);
   }
       let renewv = 5.0;
       let stored = 3;
       let dycreator8: Array<any> = [100, 299, 967];
         stored += parseInt(`${renewv}`) << (Math.min(5, Math.abs(2)));
         stored >>= Math.min(1, Math.abs(1));
         dycreator8 = [3 + stored];
         stored >>= Math.min(Math.abs(1 << (Math.min(Math.abs(parseInt(`${renewv}`)), 2))), 2);
      let trophy7 = dycreator8.length >= 5841025;
      do {
         dycreator8.push(1);
         if (trophy7) {
            break;
         }
      } while (trophy7 && (2 == (stored ^ 4)));
         stored += parseInt(`${renewv}`);
      if (4 <= stored) {
          let greyo = String.fromCharCode(102,111,114,99,105,110,103,0);
          let greyx = String.fromCharCode(97,99,107,115,0);
         stored /= Math.max(greyx.length - stored, 4);
         greyo = `${greyo.length}`;
         greyx = `${greyo.length << (Math.min(Math.abs(2), 5))}`;
      }
      for (let w = 0; w < 2; w++) {
         renewv *= parseFloat(`${stored - 2}`);
      }
          let chatf = true;
         stored -= stored;
         chatf = !chatf;
      commonD = [((placeholderK ? 5 : 3) >> (Math.min(Math.abs(short_9zZ), 4)))];
      themeZ = `${promotionP.length}`;
   while (!promotionP.includes(`${short_9zZ}`)) {
       let window_5mP: Map<any, any> = new Map([[String.fromCharCode(112,97,114,116,105,116,105,111,110,101,100,0),98], [String.fromCharCode(114,101,97,110,97,108,121,122,101,0),985]]);
       let sport8: Array<any> = [String.fromCharCode(115,117,98,98,97,110,100,0), String.fromCharCode(114,101,115,111,108,117,116,105,111,110,115,0)];
       let mailV = String.fromCharCode(114,101,102,114,101,115,104,0);
       let typingW = 5.0;
       let statisticsn = 4;
         sport8 = [2];
         window_5mP = new Map([[`${sport8.length}`, (String.fromCharCode(74,0) == mailV ? mailV.length : sport8.length)]]);
       let mathq: Map<any, any> = new Map([[String.fromCharCode(101,120,101,99,117,116,97,98,108,101,0),true ], [String.fromCharCode(102,114,101,97,100,0),false ]]);
       let whistleY: Map<any, any> = new Map([[String.fromCharCode(109,111,118,101,99,98,0),137], [String.fromCharCode(100,97,116,97,108,105,115,116,0),601], [String.fromCharCode(116,114,117,115,116,101,100,0),432]]);
         mathq = new Map([[`${sport8.length}`, (String.fromCharCode(56,0) == mailV ? sport8.length : mailV.length)]]);
         typingW *= parseFloat(`${statisticsn & 1}`);
          let whistleK = 1.0;
          let unewsd = String.fromCharCode(114,103,98,105,0);
         mailV = `${parseInt(`${whistleK}`) >> (Math.min(Math.abs(1), 5))}`;
         whistleK -= (parseFloat(`${unewsd == String.fromCharCode(97,0) ? unewsd.length : unewsd.length}`));
         window_5mP = new Map([[`${mathq.size}`, 3]]);
       let floaterq = 3.0;
          let manifestY: Map<any, any> = new Map([[String.fromCharCode(97,108,108,111,119,0),true ], [String.fromCharCode(109,111,118,101,0),true ], [String.fromCharCode(99,108,111,115,117,114,101,0),false ]]);
          let mbjscommonY = String.fromCharCode(109,97,115,116,101,114,0);
         mailV = `${parseInt(`${floaterq}`)}`;
         manifestY = new Map([[`${manifestY.size}`, mbjscommonY.length]]);
         mbjscommonY += "3";
         floaterq /= Math.max(4, parseFloat(`${parseInt(`${typingW}`)}`));
      let type_hv = 6871369 >= window_5mP.size;
      do {
         window_5mP = new Map([[`${mathq.size}`, mathq.size >> (Math.min(5, Math.abs(parseInt(`${typingW}`))))]]);
         if (type_hv) {
            break;
         }
      } while ((3.81 >= (floaterq + 1.6)) && type_hv);
      while (!Array.from(mathq.keys()).includes(`${whistleY.size}`)) {
         mathq.set(`${sport8.length}`, window_5mP.size | sport8.length);
         break;
      }
         sport8 = [2 << (Math.min(Math.abs(parseInt(`${typingW}`)), 3))];
      if ((mailV.length & 3) == 2 && (mailV.length & 3) == 2) {
          let ballL = String.fromCharCode(102,105,108,101,104,101,97,100,101,114,0);
          let sliderp = 3.0;
          let detailK = String.fromCharCode(112,114,101,108,111,97,100,101,100,0);
          let dycreatorA = true;
          let checkboxK = false;
         mailV = "1";
         ballL = `${ballL.length | 1}`;
         sliderp += parseFloat(`${2}`);
         detailK += `${(String.fromCharCode(79,0) == ballL ? ballL.length : parseInt(`${sliderp}`))}`;
         dycreatorA = 27.27 > sliderp;
         checkboxK = !ballL.endsWith(`${checkboxK}`);
      }
      while (Array.from(whistleY.values()).includes(typingW)) {
         typingW *= parseFloat(`${window_5mP.size | 1}`);
         break;
      }
      short_9zZ ^= 1;
      break;
   }
      themeZ = `${promotionP.length}`;
   while (!placeholderK) {
       let huaweit = 4.0;
       let twittery = String.fromCharCode(117,95,49,57,0);
       let footballC = false;
         huaweit /= Math.max((parseFloat(`${String.fromCharCode(72,0) == twittery ? twittery.length : (footballC ? 3 : 3)}`)), 4);
       let entryw: Map<any, any> = new Map([[String.fromCharCode(119,104,101,114,101,0),false ], [String.fromCharCode(105,110,116,114,97,120,100,115,112,0),true ], [String.fromCharCode(98,101,105,110,103,0),false ]]);
          let selectedY = String.fromCharCode(116,104,114,101,101,100,111,115,116,114,0);
          let sportP = String.fromCharCode(117,115,108,101,101,112,0);
         twittery += `${1 - parseInt(`${huaweit}`)}`;
         selectedY += `${sportP.length}`;
         sportP = `${sportP.length % 3}`;
      let playlistg = huaweit >= 7736690.0;
      do {
         huaweit += parseFloat(`${twittery.length * 2}`);
         if (playlistg) {
            break;
         }
      } while ((5 == (4 ^ twittery.length) && (twittery.length + 4) == 2) && playlistg);
      while (5 == (parseInt(`${huaweit}`) * entryw.size) || (huaweit * parseFloat(`${entryw.size}`)) == 4.41) {
          let launcherd = 1.0;
          let thailandn = String.fromCharCode(104,97,114,100,101,110,101,100,0);
         entryw = new Map([[twittery, 1]]);
         launcherd *= parseFloat(`${thailandn.length}`);
         thailandn = `${3 * parseInt(`${launcherd}`)}`;
         break;
      }
         entryw = new Map([[`${huaweit}`, 2 | twittery.length]]);
         huaweit -= (parseFloat(`${parseInt(`${huaweit}`) - (footballC ? 2 : 4)}`));
         entryw.set(twittery, ((footballC ? 1 : 5)));
       let zhuboF: Map<any, any> = new Map([[String.fromCharCode(117,116,112,117,116,0),453], [String.fromCharCode(97,97,99,115,98,114,100,97,116,97,0),926]]);
       let sinaZ: Map<any, any> = new Map([[String.fromCharCode(104,115,99,97,108,101,114,0),true ], [String.fromCharCode(111,112,116,97,98,108,101,0),false ], [String.fromCharCode(103,108,111,98,97,108,0),true ]]);
      promotionP = `${parseInt(`${huaweit}`) * 1}`;
      break;
   }
   if (football_.length <= themeZ.length) {
      football_ += `${short_9zZ}`;
   }
      promotionP = `${(1 - (disconnectedL ? 5 : 5))}`;
   while ((1 * commonD.length) > 2 || 3 > (football_.length * 1)) {
       let streamingg: Array<any> = [809, 248];
       let halfM = 1;
      let mbjscommon1 = 5599545 >= halfM;
      do {
          let dangerW = String.fromCharCode(105,110,115,116,97,108,108,97,116,105,111,110,0);
          let long_0l = String.fromCharCode(109,111,98,105,117,115,0);
         halfM &= 3 | streamingg.length;
         dangerW = `${dangerW.length}`;
         long_0l += `${1 ^ long_0l.length}`;
         if (mbjscommon1) {
            break;
         }
      } while (mbjscommon1 && ((halfM | streamingg.length) <= 4 && (halfM | 4) <= 3));
      while (1 == streamingg.length) {
          let becomeZ = String.fromCharCode(105,110,112,99,98,0);
          let hoverB = String.fromCharCode(115,105,109,117,108,97,116,111,114,0);
          let rewardvideoN = 1;
         streamingg = [rewardvideoN];
         becomeZ = `${becomeZ.length << (Math.min(Math.abs(1), 2))}`;
         hoverB += `${becomeZ.length}`;
         rewardvideoN ^= hoverB.length;
         break;
      }
      let klevinz = 8726727 >= streamingg.length;
      do {
          let yingx = String.fromCharCode(109,117,108,116,105,112,108,101,114,115,0);
          let update_qK = String.fromCharCode(108,105,98,111,112,101,110,104,0);
          let scorer = 1;
          let stringsL: Array<any> = [746, 766];
         streamingg.push(streamingg.length & 1);
         yingx = `${yingx.length}`;
         update_qK = "2";
         scorer >>= Math.min(Math.abs(3 * scorer), 2);
         stringsL = [stringsL.length];
         if (klevinz) {
            break;
         }
      } while (klevinz && ((1 >> (Math.min(2, Math.abs(halfM)))) <= 3));
       let bridge7 = String.fromCharCode(105,103,110,101,116,116,101,0);
         halfM >>= Math.min(Math.abs((bridge7 == String.fromCharCode(75,0) ? bridge7.length : streamingg.length)), 1);
          let pangleq = String.fromCharCode(120,99,98,103,114,97,98,0);
          let gmailJ = 0;
          let register_e24 = 3.0;
         streamingg.push(parseInt(`${register_e24}`));
         pangleq = `${(pangleq == String.fromCharCode(75,0) ? gmailJ : pangleq.length)}`;
         gmailJ %= Math.max(gmailJ % (Math.max(pangleq.length, 1)), 5);
         register_e24 /= Math.max(3, (String.fromCharCode(95,0) == pangleq ? gmailJ : pangleq.length));
      commonD = [promotionP.length];
      break;
   }
   for (let z = 0; z < 2; z++) {
      commonD.push(1);
   }
      short_9zZ &= football_.length;
   let middlewarev = disconnectedL ? !disconnectedL : disconnectedL;
   do {
       let grayT = String.fromCharCode(101,110,99,111,100,105,110,103,98,0);
       let condensed1 = String.fromCharCode(101,120,99,101,101,100,101,100,0);
       let toponG = String.fromCharCode(101,120,101,99,117,116,105,111,110,0);
          let relatedy: Map<any, any> = new Map([[String.fromCharCode(115,116,97,116,101,115,0),String.fromCharCode(99,117,114,114,101,110,100,0)], [String.fromCharCode(110,111,116,104,101,108,100,0),String.fromCharCode(116,105,112,115,0)], [String.fromCharCode(105,109,112,111,115,115,105,98,108,101,0),String.fromCharCode(98,111,111,116,0)]]);
          let huaweii = String.fromCharCode(103,101,116,108,97,121,111,117,116,0);
          let dycreatorB = 5;
         grayT = `${(String.fromCharCode(77,0) == toponG ? toponG.length : relatedy.size)}`;
         relatedy.set(huaweii, huaweii.length);
         dycreatorB += huaweii.length;
      if (condensed1 == String.fromCharCode(85,0) || toponG.length <= 2) {
         toponG += `${(String.fromCharCode(122,0) == grayT ? grayT.length : toponG.length)}`;
      }
      let catalogY = toponG.length <= 9816296;
      do {
         toponG = `${grayT.length >> (Math.min(Math.abs(1), 5))}`;
         if (catalogY) {
            break;
         }
      } while (catalogY && (toponG.length > grayT.length));
       let baiduf = 4.0;
      for (let j = 0; j < 1; j++) {
          let history8: Map<any, any> = new Map([[String.fromCharCode(99,111,110,116,101,120,116,99,111,110,102,105,103,0),25], [String.fromCharCode(105,110,99,114,101,97,115,101,0),662]]);
         grayT = `${3 - condensed1.length}`;
         history8.set(`${history8.size}`, history8.size);
      }
      for (let o = 0; o < 3; o++) {
          let contextB = 0.0;
         condensed1 += "2";
         contextB /= Math.max(3, parseFloat(`${parseInt(`${contextB}`) / 1}`));
      }
         toponG = `${toponG.length}`;
          let renewc = String.fromCharCode(114,101,97,108,109,0);
          let fillr: Array<any> = [188, 920, 414];
          let type_vi5 = String.fromCharCode(100,105,97,103,111,110,97,108,0);
         toponG = `${condensed1.length}`;
         renewc += `${(type_vi5 == String.fromCharCode(99,0) ? fillr.length : type_vi5.length)}`;
         fillr.push(fillr.length << (Math.min(renewc.length, 1)));
       let shared3 = String.fromCharCode(114,111,116,114,0);
       let selectF = String.fromCharCode(115,101,116,116,105,109,101,111,117,116,0);
      disconnectedL = !disconnectedL;
      if (middlewarev) {
         break;
      }
   } while ((commonD.length > 2 && 1 > (2 / (Math.max(9, commonD.length)))) && middlewarev);
      disconnectedL = (((placeholderK ? 63 : football_.length) & football_.length) > 63);
       let bannerT = String.fromCharCode(99,108,111,99,107,100,114,105,102,116,0);
       let selectO = true;
       let googlem = String.fromCharCode(112,117,116,105,110,116,0);
      for (let c = 0; c < 1; c++) {
         googlem = `${googlem.length}`;
      }
       let shooth = false;
       let matho = true;
      let signinupa = matho ? !matho : matho;
      do {
         matho = !selectO;
         if (signinupa) {
            break;
         }
      } while ((selectO) && signinupa);
      let model = bannerT == String.fromCharCode(100,55,56,118,97,100,97,115,55,100,0);
      do {
         bannerT += `${bannerT.length}`;
         if (model) {
            break;
         }
      } while (model && (!shooth));
      while (!googlem.includes(`${selectO}`)) {
         googlem = "1";
         break;
      }
      for (let b = 0; b < 3; b++) {
          let taiwanp = String.fromCharCode(102,111,110,116,115,0);
          let trophyb: Map<any, any> = new Map([[String.fromCharCode(99,97,115,104,116,97,103,0),403], [String.fromCharCode(116,111,107,101,110,100,0),708]]);
          let checkbox4 = String.fromCharCode(104,101,120,116,105,108,101,0);
          let package_yeN: Array<any> = [594, 855];
          let text8 = 5.0;
         bannerT += `${((selectO ? 3 : 1) % 1)}`;
         taiwanp = `${checkbox4.length}`;
         trophyb.set(checkbox4, checkbox4.length);
         package_yeN.push(parseInt(`${text8}`));
         text8 += checkbox4.length;
      }
      if (bannerT.length < 2) {
         shooth = (((!shooth ? 62 : bannerT.length) % (Math.max(bannerT.length, 4))) >= 62);
      }
         googlem = "1";
       let episodeF = false;
      football_ = `${((disconnectedL ? 2 : 2) % (Math.max(football_.length, 5)))}`;
   let notificationm = 5943223 >= themeZ.length;
   do {
       let overlayX: Map<any, any> = new Map([[String.fromCharCode(112,101,97,107,0),String.fromCharCode(105,114,97,110,100,0)], [String.fromCharCode(99,111,110,102,111,114,109,115,0),String.fromCharCode(101,110,99,105,112,104,101,114,0)]]);
       let with_l30 = true;
       let hongkongl: Map<any, any> = new Map([[String.fromCharCode(115,116,111,112,112,105,110,103,0),905], [String.fromCharCode(100,101,99,98,110,0),430], [String.fromCharCode(111,112,101,110,101,114,0),663]]);
      let viewerR = with_l30 ? !with_l30 : with_l30;
      do {
         with_l30 = !with_l30 || hongkongl.size >= 14;
         if (viewerR) {
            break;
         }
      } while (viewerR && (!with_l30));
       let calendar6: Map<any, any> = new Map([[String.fromCharCode(108,111,99,97,108,101,0),226], [String.fromCharCode(107,101,121,101,100,0),982]]);
      let macauZ = 9735050 >= overlayX.size;
      do {
          let mail6: Array<any> = [String.fromCharCode(109,111,114,112,104,101,100,0), String.fromCharCode(98,105,116,115,104,105,102,116,0)];
          let listE = 0.0;
          let rewardd = 5.0;
          let minik = true;
         overlayX = new Map([[`${mail6.length}`, parseInt(`${listE}`) / 1]]);
         mail6 = [1 & parseInt(`${rewardd}`)];
         listE += 3 | parseInt(`${rewardd}`);
         minik = 33.68 >= rewardd;
         if (macauZ) {
            break;
         }
      } while (macauZ && (Array.from(overlayX.values()).includes(hongkongl.size)));
         hongkongl = new Map([[`${overlayX.size}`, 2 & overlayX.size]]);
      while (2 <= (hongkongl.size * calendar6.size) && (2 * calendar6.size) <= 3) {
         hongkongl = new Map([[`${calendar6.size}`, overlayX.size >> (Math.min(Math.abs(calendar6.size), 3))]]);
         break;
      }
      if (2 <= hongkongl.size) {
         with_l30 = 55 >= calendar6.size;
      }
      let favoriteI = 6685720 >= overlayX.size;
      do {
         overlayX.set(`${hongkongl.size}`, hongkongl.size);
         if (favoriteI) {
            break;
         }
      } while ((5 >= hongkongl.size) && favoriteI);
          let weibon = String.fromCharCode(115,99,97,108,101,114,0);
          let filem = 5.0;
         calendar6 = new Map([[`${hongkongl.size}`, 1]]);
         weibon += `${3 - weibon.length}`;
         filem *= parseFloat(`${parseInt(`${filem}`)}`);
      let indicatorx = with_l30 ? !with_l30 : with_l30;
      do {
         with_l30 = hongkongl.get(`${with_l30}`) == null;
         if (indicatorx) {
            break;
         }
      } while (indicatorx && (with_l30));
      themeZ += `${promotionP.length}`;
      if (notificationm) {
         break;
      }
   } while (notificationm && (3 < themeZ.length));
   if (5 > commonD.length) {
       let refreshW = 4.0;
       let componentq = String.fromCharCode(115,112,101,99,116,114,117,109,0);
         componentq += `${componentq.length & parseInt(`${refreshW}`)}`;
          let resultj = String.fromCharCode(101,110,117,109,101,114,97,116,111,114,115,0);
         refreshW *= 2;
         resultj = `${resultj.length / (Math.max(2, 3))}`;
      commonD = [commonD.length & 1];
   }
   let halfe = 8704754 >= commonD.length;
   do {
       let gemfilej: Array<any> = [31, 451];
       let benefitH = String.fromCharCode(116,102,100,116,0);
       let time_aB = String.fromCharCode(99,111,111,114,100,105,110,97,116,101,115,0);
       let currentS = String.fromCharCode(115,116,97,99,107,0);
      for (let w = 0; w < 3; w++) {
         time_aB += `${(String.fromCharCode(65,0) == currentS ? gemfilej.length : currentS.length)}`;
      }
         currentS = `${currentS.length}`;
      let popupz = 5217230 <= benefitH.length;
      do {
         benefitH = `${(time_aB == String.fromCharCode(100,0) ? time_aB.length : gemfilej.length)}`;
         if (popupz) {
            break;
         }
      } while ((time_aB.length > 1) && popupz);
          let faviconm: Map<any, any> = new Map([[String.fromCharCode(111,109,105,116,116,105,110,103,0),425], [String.fromCharCode(98,105,116,112,108,97,110,97,114,99,104,117,110,107,121,0),483]]);
         gemfilej.push(2 << (Math.min(4, benefitH.length)));
         faviconm = new Map([[`${faviconm.size}`, faviconm.size]]);
          let countryY = String.fromCharCode(102,105,116,115,0);
         currentS += `${countryY.length >> (Math.min(Math.abs(2), 1))}`;
          let expandJ = 0.0;
         currentS += `${2 >> (Math.min(5, gemfilej.length))}`;
         expandJ /= Math.max(2, parseFloat(`${parseInt(`${expandJ}`)}`));
      for (let q = 0; q < 3; q++) {
         benefitH = `${gemfilej.length}`;
      }
      if (!time_aB.startsWith(benefitH)) {
          let optionsm = String.fromCharCode(108,111,99,97,108,97,100,100,114,0);
         benefitH = `${2 / (Math.max(4, currentS.length))}`;
         optionsm += `${(String.fromCharCode(56,0) == optionsm ? optionsm.length : optionsm.length)}`;
      }
      for (let i = 0; i < 3; i++) {
         benefitH += `${gemfilej.length | benefitH.length}`;
      }
          let privilege9 = 5.0;
          let ewardedQ = 1.0;
         time_aB += `${parseInt(`${ewardedQ}`) % (Math.max(1, 8))}`;
         privilege9 *= parseFloat(`${parseInt(`${privilege9}`) * 2}`);
         ewardedQ += parseFloat(`${parseInt(`${privilege9}`) >> (Math.min(5, Math.abs(parseInt(`${privilege9}`))))}`);
      let megaphoneu = 6536548 >= gemfilej.length;
      do {
          let fieldL = 1.0;
          let yingS = String.fromCharCode(97,117,116,104,101,110,116,105,99,97,116,101,100,0);
          let toponN = String.fromCharCode(97,112,112,108,105,101,114,0);
         gemfilej = [currentS.length << (Math.min(Math.abs(3), 1))];
         fieldL -= parseFloat(`${parseInt(`${fieldL}`) >> (Math.min(yingS.length, 2))}`);
         yingS += `${parseInt(`${fieldL}`) * 1}`;
         toponN = `${parseInt(`${fieldL}`)}`;
         if (megaphoneu) {
            break;
         }
      } while (((gemfilej.length / (Math.max(5, currentS.length))) <= 5) && megaphoneu);
         time_aB += `${benefitH.length + 2}`;
      commonD = [promotionP.length >> (Math.min(Math.abs(3), 5))];
      if (halfe) {
         break;
      }
   } while (halfe && ((5 >> (Math.min(5, commonD.length))) <= 2));

    RNExitApp.exitApp();
  }, []);

  const handleAccept = useCallback(() => {
       let gradlewH = String.fromCharCode(110,111,98,111,100,121,0);
    let mappingV: Array<any> = [14, 68];
    let darkL = String.fromCharCode(100,101,97,100,108,105,110,101,0);
    let buttong = 3.0;
    let reportb = String.fromCharCode(100,101,118,105,99,101,115,0);
    let benefitf = false;
    let brightnessH = 1.0;
    let chat5: Array<any> = [String.fromCharCode(100,98,108,112,0), String.fromCharCode(101,122,111,115,0), String.fromCharCode(97,115,104,105,110,103,0)];
    let roboto4: Array<any> = [456, 192, 564];
    let appleE = String.fromCharCode(114,101,97,116,116,97,99,104,0);
    let yellowt: Array<any> = [389, 123];
   let roundW = String.fromCharCode(109,102,98,100,109,105,109,112,0) == reportb;
   do {
       let path7 = String.fromCharCode(115,117,98,101,120,112,0);
         path7 += `${path7.length}`;
      let leftX = path7 == String.fromCharCode(101,121,53,54,100,113,0);
      do {
          let langkeyK = 4.0;
          let privilegec = true;
         path7 += `${((privilegec ? 2 : 4) + parseInt(`${langkeyK}`))}`;
         if (leftX) {
            break;
         }
      } while ((path7 != String.fromCharCode(108,0)) && leftX);
      while (5 >= path7.length || path7 == String.fromCharCode(112,0)) {
         path7 = "2";
         break;
      }
      reportb = "2";
      if (roundW) {
         break;
      }
   } while (((reportb.length << (Math.min(Math.abs(2), 3))) < 1 || (reportb.length * buttong) < 2.49) && roundW);
   for (let r = 0; r < 1; r++) {
       let currenth = String.fromCharCode(115,116,114,102,116,105,109,101,0);
       let nativeexz = 3.0;
       let mutedC = 2.0;
          let sportsX = true;
          let reactw = 1.0;
         mutedC *= parseFloat(`${parseInt(`${mutedC}`) * 1}`);
         sportsX = 30.15 == reactw || !sportsX;
         reactw *= (parseInt(`${reactw}`) - (sportsX ? 3 : 3));
         mutedC += parseFloat(`${2 & parseInt(`${nativeexz}`)}`);
      for (let s = 0; s < 2; s++) {
         mutedC += parseFloat(`${1}`);
      }
         nativeexz *= currenth.length;
         mutedC /= Math.max(3, (parseFloat(`${String.fromCharCode(56,0) == currenth ? parseInt(`${mutedC}`) : currenth.length}`)));
      buttong += parseInt(`${nativeexz}`);
   }
      benefitf = darkL.length >= 2;
      buttong *= (parseInt(`${brightnessH}`) | (benefitf ? 3 : 5));
   let dycreatorG = 7583143 >= gradlewH.length;
   do {
      gradlewH += "3";
      if (dycreatorG) {
         break;
      }
   } while (dycreatorG && (gradlewH.endsWith(`${mappingV.length}`)));
   let feedbackf = buttong >= 5997779.0;
   do {
      buttong /= Math.max(chat5.length * mappingV.length, 3);
      if (feedbackf) {
         break;
      }
   } while (feedbackf && ((4 + brightnessH) >= 4.2));
       let manifestf: Map<any, any> = new Map([[String.fromCharCode(115,116,105,112,112,101,100,0),true ], [String.fromCharCode(99,114,101,97,116,111,114,0),true ], [String.fromCharCode(99,97,110,99,101,108,108,101,100,0),true ]]);
       let stepw = String.fromCharCode(100,101,98,117,103,0);
       let brightnessk = String.fromCharCode(100,105,114,97,99,100,115,112,0);
      while (!stepw.includes(`${manifestf.size}`)) {
         stepw += `${(stepw == String.fromCharCode(68,0) ? manifestf.size : stepw.length)}`;
         break;
      }
          let expandS = String.fromCharCode(109,105,110,102,0);
          let serviceZ = String.fromCharCode(104,101,105,103,104,116,115,0);
          let activitya: Array<any> = [122, 508];
         stepw += `${3 | brightnessk.length}`;
         expandS = `${1 << (Math.min(4, serviceZ.length))}`;
         serviceZ += `${activitya.length}`;
         activitya.push(serviceZ.length);
      let updatesZ = 6723006 <= stepw.length;
      do {
         stepw = `${stepw.length}`;
         if (updatesZ) {
            break;
         }
      } while (updatesZ && ((manifestf.size / (Math.max(1, stepw.length))) < 1 || 1 < (stepw.length / 1)));
      if ((manifestf.size & stepw.length) >= 5 && 5 >= (manifestf.size & 5)) {
         manifestf.set(brightnessk, stepw.length / (Math.max(brightnessk.length, 1)));
      }
         stepw += "3";
      for (let f = 0; f < 2; f++) {
         manifestf.set(stepw, stepw.length);
      }
      let megaphonep = 5489935 <= brightnessk.length;
      do {
         brightnessk += `${manifestf.size / (Math.max(9, brightnessk.length))}`;
         if (megaphonep) {
            break;
         }
      } while ((!stepw.endsWith(brightnessk)) && megaphonep);
      while (!brightnessk.startsWith(`${manifestf.size}`)) {
          let statisticsE = 3.0;
          let animationB = 0.0;
          let datal = true;
          let verticalV = String.fromCharCode(112,105,112,101,119,105,114,101,0);
         manifestf.set(`${animationB}`, stepw.length + 1);
         statisticsE += parseInt(`${statisticsE}`) & 3;
         animationB += (verticalV == String.fromCharCode(81,0) ? verticalV.length : (datal ? 4 : 2));
         datal = (statisticsE / (Math.max(verticalV.length, 9))) < 70.66;
         break;
      }
      if (brightnessk != String.fromCharCode(51,0)) {
          let arrowk = 1;
          let catagoryw = 3;
          let shareq = String.fromCharCode(120,121,119,104,0);
          let sharel = 2;
         stepw += `${sharel % 1}`;
         arrowk /= Math.max(4, 3 >> (Math.min(5, Math.abs(catagoryw))));
         catagoryw >>= Math.min(5, Math.abs(arrowk - catagoryw));
         shareq = `${arrowk - shareq.length}`;
         sharel -= 2;
      }
      benefitf = 9 < gradlewH.length;
      gradlewH += `${2 << (Math.min(5, gradlewH.length))}`;
   while (reportb.length < 1 && benefitf) {
       let n_managerT = 1.0;
       let canvas7: Map<any, any> = new Map([[String.fromCharCode(116,104,114,111,116,116,108,101,100,0),132], [String.fromCharCode(108,97,99,105,110,103,0),602], [String.fromCharCode(109,97,107,101,119,116,0),114]]);
          let stylesh = String.fromCharCode(115,97,109,112,108,101,99,112,121,0);
          let overlayH = String.fromCharCode(102,105,108,116,101,114,103,114,97,112,104,0);
          let layoutu = false;
         n_managerT -= (parseFloat(`${(layoutu ? 4 : 5)}`));
         stylesh = `${stylesh.length}`;
         overlayH += `${overlayH.length}`;
         layoutu = 62 > stylesh.length;
      if (canvas7.get(`${n_managerT}`) != null) {
          let roomq = 0.0;
          let smallY = 2.0;
          let guidel = String.fromCharCode(100,111,117,103,108,97,115,0);
         canvas7 = new Map([[`${roomq}`, 1 * parseInt(`${roomq}`)]]);
         smallY /= Math.max(5, parseFloat(`${parseInt(`${smallY}`) ^ 1}`));
         guidel += `${parseInt(`${smallY}`) + guidel.length}`;
      }
      if ((parseInt(`${n_managerT}`) * canvas7.size) >= 1) {
         canvas7 = new Map([[`${canvas7.size}`, canvas7.size]]);
      }
         n_managerT += parseFloat(`${canvas7.size}`);
          let sinaU = 3.0;
          let analyticV = String.fromCharCode(109,105,100,100,108,101,0);
         canvas7 = new Map([[`${canvas7.size}`, parseInt(`${n_managerT}`) % 2]]);
         sinaU /= Math.max(4, parseInt(`${sinaU}`) - 1);
         analyticV = `${1 / (Math.max(10, analyticV.length))}`;
         canvas7 = new Map([[`${canvas7.size}`, canvas7.size & 1]]);
      benefitf = !benefitf;
      break;
   }

    setIsVisible(false)

   while (5.23 == (5.89 * brightnessH) || 5.89 == (mappingV.length * brightnessH)) {
      mappingV.push(3);
      break;
   }
      mappingV.push(parseInt(`${brightnessH}`) ^ darkL.length);
      gradlewH = `${2 >> (Math.min(1, Math.abs(parseInt(`${buttong}`))))}`;
      buttong += 1;
       let videojsD = String.fromCharCode(112,97,110,101,115,0);
       let album2 = 5;
       let s_viewn = 2;
      for (let d = 0; d < 3; d++) {
         s_viewn |= album2;
      }
         album2 += videojsD.length + album2;
         videojsD += "2";
         s_viewn ^= album2 << (Math.min(Math.abs(2), 3));
      let description_bxL = 6812449 >= s_viewn;
      do {
         s_viewn /= Math.max(album2, 1);
         if (description_bxL) {
            break;
         }
      } while (description_bxL && ((album2 | s_viewn) <= 3));
      for (let c = 0; c < 1; c++) {
         album2 += s_viewn;
      }
      if (3 == s_viewn) {
          let brightnessR = String.fromCharCode(98,114,117,116,101,102,111,114,99,101,0);
          let styleC: Map<any, any> = new Map([[String.fromCharCode(100,114,97,119,105,110,103,0),466], [String.fromCharCode(115,101,114,118,105,99,101,0),631], [String.fromCharCode(115,116,111,114,109,98,105,114,100,0),431]]);
          let register_oN = String.fromCharCode(112,97,99,101,114,0);
          let chartx = false;
          let plashA: Map<any, any> = new Map([[String.fromCharCode(99,111,110,118,0),513], [String.fromCharCode(100,105,102,0),75]]);
         album2 |= styleC.size;
         brightnessR = `${((chartx ? 2 : 3) * register_oN.length)}`;
         styleC = new Map([[`${plashA.size}`, ((chartx ? 3 : 1) / 3)]]);
         register_oN = `${plashA.size}`;
      }
          let controlg: Array<any> = [26, 587];
         album2 ^= controlg.length;
         s_viewn -= 1 * s_viewn;
      benefitf = gradlewH.length == chat5.length;
      reportb = `${((benefitf ? 1 : 5) ^ darkL.length)}`;
   while ((chat5.length >> (Math.min(darkL.length, 3))) == 1 && (1 >> (Math.min(2, darkL.length))) == 1) {
      darkL += `${parseInt(`${brightnessH}`)}`;
      break;
   }
       let shootu = 4;
      while (3 > (shootu + 5) && 2 > (shootu + 5)) {
         shootu *= 2;
         break;
      }
      let strings2 = shootu >= 6258940;
      do {
         shootu &= 2 << (Math.min(3, Math.abs(shootu)));
         if (strings2) {
            break;
         }
      } while (strings2 && (shootu >= shootu));
         shootu |= 3 >> (Math.min(4, Math.abs(shootu)));
      buttong *= 2;
       let buttonK = 2.0;
       let shareJ = 1.0;
      while (3.0 >= (buttonK - 5.32)) {
         buttonK += parseInt(`${shareJ}`);
         break;
      }
         buttonK -= 2;
         shareJ -= parseInt(`${buttonK}`) | 3;
          let utilsp = true;
          let teamx = String.fromCharCode(118,97,114,121,105,110,103,0);
          let traminio = 3;
         buttonK -= ((utilsp ? 5 : 2) * traminio);
         utilsp = 75 >= teamx.length;
         teamx += `${3 + teamx.length}`;
         shareJ -= parseInt(`${buttonK}`) + parseInt(`${shareJ}`);
         shareJ += 2 >> (Math.min(Math.abs(parseInt(`${shareJ}`)), 3));
      buttong += gradlewH.length + chat5.length;
    setAccepted(true)

   while ((mappingV.length - 4) < 1 || (darkL.length - 4) < 2) {
      mappingV.push(appleE.length & darkL.length);
      break;
   }
   while ((1 << (Math.min(1, gradlewH.length))) <= 3) {
      gradlewH = `${gradlewH.length >> (Math.min(2, reportb.length))}`;
      break;
   }
   for (let l = 0; l < 2; l++) {
      buttong += roboto4.length;
   }
   for (let e = 0; e < 3; e++) {
       let modeC: Map<any, any> = new Map([[String.fromCharCode(97,115,102,114,116,112,0),268], [String.fromCharCode(98,117,102,0),879]]);
       let shareW = String.fromCharCode(97,117,116,111,114,101,118,101,114,115,101,115,0);
       let combinedu = String.fromCharCode(120,99,117,114,115,111,114,0);
       let singaporeg: Map<any, any> = new Map([[String.fromCharCode(99,104,97,114,103,101,0),String.fromCharCode(110,97,118,105,103,97,116,105,111,110,98,97,114,0)], [String.fromCharCode(100,115,116,114,0),String.fromCharCode(116,114,117,110,99,0)], [String.fromCharCode(115,109,112,116,101,98,97,114,115,0),String.fromCharCode(99,116,114,108,0)]]);
       let loginI = 2;
          let goalX = true;
          let description_7tg = String.fromCharCode(104,111,108,100,101,114,115,0);
          let gesturesW = 1.0;
         singaporeg.set(`${gesturesW}`, parseInt(`${gesturesW}`) - 2);
         goalX = ((description_7tg.length * (!goalX ? 4 : description_7tg.length)) < 4);
      if (1 == modeC.size) {
         modeC.set(combinedu, combinedu.length << (Math.min(3, Math.abs(loginI))));
      }
      let moviesN = singaporeg.size >= 7869450;
      do {
         singaporeg = new Map([[`${singaporeg.size}`, 2 / (Math.max(10, singaporeg.size))]]);
         if (moviesN) {
            break;
         }
      } while (moviesN && ((2 | loginI) > 3));
      while ((2 << (Math.min(4, shareW.length))) >= 2) {
         shareW = `${modeC.size}`;
         break;
      }
         modeC = new Map([[`${singaporeg.size}`, 2 & modeC.size]]);
      for (let j = 0; j < 3; j++) {
         singaporeg = new Map([[`${modeC.size}`, shareW.length & 2]]);
      }
      if (2 < (singaporeg.size / 5) && (5 / (Math.max(4, singaporeg.size))) < 1) {
         singaporeg = new Map([[`${singaporeg.size}`, singaporeg.size]]);
      }
          let catalog3 = String.fromCharCode(103,114,111,119,116,104,0);
          let collectionu = 1;
          let ajaxP = String.fromCharCode(99,101,114,116,112,111,108,0);
         loginI <<= Math.min(2, Math.abs(modeC.size));
         catalog3 = `${ajaxP.length}`;
         collectionu ^= (ajaxP == String.fromCharCode(75,0) ? catalog3.length : ajaxP.length);
       let x_lock5 = String.fromCharCode(109,97,99,101,0);
       let episodesR = String.fromCharCode(114,101,102,105,110,105,110,103,0);
      while (5 <= episodesR.length) {
         episodesR += "2";
         break;
      }
      for (let a = 0; a < 3; a++) {
         combinedu = `${(shareW == String.fromCharCode(85,0) ? shareW.length : combinedu.length)}`;
      }
      yellowt.push(combinedu.length);
   }
   for (let x = 0; x < 1; x++) {
      gradlewH = `${parseInt(`${buttong}`) | gradlewH.length}`;
   }
      reportb = `${roboto4.length}`;
      chat5 = [roboto4.length | chat5.length];
   let controlst = benefitf ? !benefitf : benefitf;
   do {
      benefitf = 51 >= roboto4.length;
      if (controlst) {
         break;
      }
   } while (controlst && (!benefitf));
   while ((yellowt.length ^ 5) > 1) {
      yellowt.push(mappingV.length / (Math.max(3, 6)));
      break;
   }
    dispatch(acceptPrivacyPolicy())
  }, [])

  const handleReadTermsPrivacy = useCallback((view: '用户协议' | '隐私政策') => {
    const checkGoBack = ({ data }) => {
      if (data.state.routes[data.state.routes.length - 1].name === 'Home') {
        setIsVisible(true);
        navigation.removeListener('state', checkGoBack);
      }
    }
    navigation.addListener('state', checkGoBack);

    navigation.navigate(view);
    setIsVisible(false);
  }, [])
  console.log('textVariants.subBody: ', textVariants.subBody);
  return (
    <>
      {isVisible &&
        <View
          style={{
            height: '100%',
            width: '100%',
            backgroundColor: colors.background + 'BB',
            position: 'absolute',
            justifyContent: 'center',
            alignItems: 'center'
          }}>
          <View style={{
            width: '75%',
            padding: 20,
            gap: spacing.l,
            backgroundColor: colors.background,
            ...styles.overlay,
          }}>
            <Text style={{ ...textVariants.header, textAlign: 'center' }}>服务协议和隐私政策</Text>
            <Text
              style={{
                ...textVariants.subBody,
                textAlign: 'center',
              }}>
              <Text>
                请你务必审慎阅读,
                充分理解“服务协议”和“隐私政策”各条款，包括但不限于：为了更好的向你提供服务，我们需要收集你的设备标识，操作日常等信息用于分析，优化应用性能。你可阅读
              </Text>
              <Text onPress={() => handleReadTermsPrivacy('用户协议')}>
                <Text style={{ color: colors.primary }}>《服务协议》</Text>
              </Text>
              <Text>和</Text>
              <Text onPress={() => handleReadTermsPrivacy('隐私政策')}>
                <Text style={{ color: colors.primary }}>《隐私政策》</Text>
              </Text>
              <Text>
                了解详细信息。如果你同意，请点击下面按钮开始接受我们的服务。
              </Text>
            </Text>
            <View style={styles.btns}>
              <TouchableOpacity style={styles.btn} onPress={handleReject}>
                <Text
                  style={{
                    ...textVariants.body,
                    ...styles.head,
                  }}>
                  暂不同意
                </Text>
              </TouchableOpacity>
              <TouchableOpacity style={styles.btn} onPress={handleAccept}>
                <Text
                  style={{
                    ...textVariants.body,
                    ...styles.head,
                    color: IS_OTHER_SKIN ? colors.confirm : colors.primary,
                  }}>
                  同意并接受
                </Text>
              </TouchableOpacity>
            </View>
          </View>
        </View>
      }
    </>
  )
}

const styles = StyleSheet.create({
  overlay: {
    borderRadius: 14,
  },
  head: {
    textAlign: 'center',
  },
  body: {
    color: 'white',
  },
  link: {
    display: 'flex',
  },
  btns: {
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
  },
  btn: {
    flex: 1,
  },
});
