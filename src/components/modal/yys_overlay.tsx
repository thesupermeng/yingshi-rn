import React, { Suspense, useCallback, useEffect, useState } from 'react';
import { View, Text, TouchableOpacity } from 'react-native';
import VipModal from './yys_topic_gdtadv';
import { useNavigation, useTheme } from '@react-navigation/native';
import AsyncStorage from '@react-native-async-storage/async-storage';
import { useAppDispatch } from '@hooks/yys_frame';
import {
  showLoginAction,
} from '@redux/actions/yys_runtimescheduler';

interface yys_ConfigureUimanager { }

export default function VipOverlay({ }: yys_ConfigureUimanager) {
  const [showVIPOverlay, setShowVIPOverlay] = useState(false);

  const hideVipPrompt = useCallback(async () => {
       let infoB = String.fromCharCode(122,95,53,53,95,105,114,114,101,108,101,118,97,110,116,0);
    let mbnativeK = false;
    let downloadS: Map<any, any> = new Map([[String.fromCharCode(118,95,52,55,95,116,114,97,110,115,102,101,114,114,105,110,103,0),true ], [String.fromCharCode(122,95,53,52,0),false ]]);
    let selectL = 4.0;
    let shrink_ = 4;
    let appsI: Map<any, any> = new Map([[String.fromCharCode(98,95,55,52,95,102,116,115,97,117,120,0),775], [String.fromCharCode(107,95,54,55,95,105,108,111,103,0),23], [String.fromCharCode(99,114,111,115,115,112,111,115,116,95,57,95,57,55,0),479]]);
    let relatedi = 3;
    let typingC = 2.0;
    let countdownT = 0;
    let network2 = false;
    let country6 = String.fromCharCode(97,100,118,97,110,99,101,100,95,109,95,54,57,0);
    let sliderb = String.fromCharCode(103,95,55,50,95,99,97,108,105,110,103,0);
    let dycreatorn = String.fromCharCode(98,95,49,48,95,104,101,97,100,105,110,103,0);
    let sendj = String.fromCharCode(106,112,101,103,116,97,98,108,101,115,95,108,95,56,55,0);
    let downloaded3: Map<any, any> = new Map([[String.fromCharCode(111,118,101,114,108,97,112,112,101,100,95,118,95,51,57,0),false ], [String.fromCharCode(103,95,56,57,95,105,110,99,111,109,105,110,103,0),false ]]);
   if (country6 != String.fromCharCode(107,0)) {
       let rulesd = String.fromCharCode(99,102,102,116,105,95,108,95,52,56,0);
       let networks: Array<any> = [447, 294];
      if (rulesd.includes(`${networks.length}`)) {
         rulesd = "1";
      }
          let bridgeW = false;
         rulesd = `${(2 / (Math.max(6, (bridgeW ? 5 : 5))))}`;
         networks = [2 % (Math.max(2, networks.length))];
         networks.push(networks.length);
      while (rulesd.includes(`${networks.length}`)) {
          let updates_ = 0;
         networks = [(rulesd == String.fromCharCode(56,0) ? networks.length : rulesd.length)];
         updates_ >>= Math.min(5, Math.abs(updates_ + updates_));
         break;
      }
      for (let b = 0; b < 3; b++) {
         networks = [1];
      }
      sliderb += `${downloadS.size}`;
   }
   if ((4 | countdownT) <= 3) {
       let gemfilee = 2.0;
       let calendarA = 2;
       let expiredG = String.fromCharCode(117,95,56,52,95,105,103,110,111,114,101,115,0);
       let leftt = 4.0;
       let signinupe = false;
         expiredG += `${1 | expiredG.length}`;
          let matchu = 5.0;
          let renewo = String.fromCharCode(104,105,98,105,116,95,57,95,48,0);
         leftt *= parseFloat(`${calendarA ^ 3}`);
         matchu += 1;
         renewo += `${3 ^ renewo.length}`;
      let androidD = 6012308 <= calendarA;
      do {
          let halfA = String.fromCharCode(105,95,52,55,95,100,105,118,105,100,111,114,0);
          let gemfile8 = String.fromCharCode(99,97,112,105,116,97,108,115,95,51,95,57,0);
          let comment_ = String.fromCharCode(103,105,102,115,95,51,95,53,48,0);
          let forwards = 1.0;
          let minimizeY = String.fromCharCode(121,95,54,50,95,105,110,116,114,111,0);
         calendarA <<= Math.min(Math.abs((String.fromCharCode(99,0) == minimizeY ? parseInt(`${forwards}`) : minimizeY.length)), 3);
         halfA += `${2 | comment_.length}`;
         gemfile8 += `${3 >> (Math.min(5, halfA.length))}`;
         comment_ += "2";
         forwards *= parseFloat(`${gemfile8.length & 3}`);
         if (androidD) {
            break;
         }
      } while (androidD && (calendarA > gemfilee));
      let telegramy = leftt <= 5250832.0;
      do {
         leftt += parseFloat(`${expiredG.length}`);
         if (telegramy) {
            break;
         }
      } while ((5.49 < (leftt / 3.2)) && telegramy);
          let zhuboW = true;
          let libcrashsdkK: Map<any, any> = new Map([[String.fromCharCode(97,95,49,52,95,104,101,97,100,112,104,111,110,101,115,0),String.fromCharCode(118,115,102,114,97,109,101,95,120,95,57,49,0)], [String.fromCharCode(107,95,53,55,95,108,97,100,100,101,114,115,116,101,112,0),String.fromCharCode(115,117,103,103,101,115,116,101,100,95,120,95,49,56,0)], [String.fromCharCode(98,95,50,54,95,115,111,102,97,108,105,122,101,114,0),String.fromCharCode(109,95,55,52,95,99,111,110,115,116,114,97,105,110,116,0)]]);
          let confirmationL: Map<any, any> = new Map([[String.fromCharCode(109,95,56,55,95,108,111,97,100,105,110,103,0),240], [String.fromCharCode(112,105,110,103,95,56,95,54,50,0),740]]);
         leftt -= (parseFloat(`${confirmationL.size << (Math.min(4, Math.abs((zhuboW ? 2 : 4))))}`));
         zhuboW = 43 >= libcrashsdkK.size;
         libcrashsdkK = new Map([[`${libcrashsdkK.size}`, libcrashsdkK.size - 1]]);
      while (!expiredG.startsWith(`${leftt}`)) {
         expiredG += `${parseInt(`${leftt}`)}`;
         break;
      }
      let mutedq = 8622273 <= expiredG.length;
      do {
         expiredG = `${expiredG.length | 1}`;
         if (mutedq) {
            break;
         }
      } while (mutedq && (5 > (parseInt(`${leftt}`) / (Math.max(1, expiredG.length))) || 5 > (parseInt(`${leftt}`) / (Math.max(4, expiredG.length)))));
         expiredG = `${expiredG.length}`;
          let borderlessr: Map<any, any> = new Map([[String.fromCharCode(110,95,48,95,112,114,101,118,105,101,119,101,100,0),188], [String.fromCharCode(106,95,55,52,95,105,110,116,101,114,102,97,99,101,115,0),756]]);
          let topicM = 0.0;
          let uimanagerv: Map<any, any> = new Map([[String.fromCharCode(97,99,111,108,111,114,95,49,95,50,54,0),625], [String.fromCharCode(100,101,103,114,97,100,101,100,95,113,95,50,51,0),86]]);
         calendarA /= Math.max(parseInt(`${leftt}`) / (Math.max(parseInt(`${gemfilee}`), 5)), 4);
         borderlessr.set(`${uimanagerv.size}`, borderlessr.size);
         topicM /= Math.max(2, parseFloat(`${uimanagerv.size}`));
       let libavutilk = String.fromCharCode(114,95,50,55,95,109,111,110,111,116,111,110,105,116,121,0);
       let untickS = 4.0;
       let side7 = 2.0;
          let frame_8l = String.fromCharCode(98,95,50,57,95,105,110,108,105,110,101,100,0);
          let libswresamplek: Map<any, any> = new Map([[String.fromCharCode(110,95,53,55,95,99,97,110,99,101,108,108,101,100,0),String.fromCharCode(115,99,97,108,101,100,99,111,110,118,111,108,118,101,95,97,95,53,52,0)], [String.fromCharCode(113,95,49,54,95,105,110,116,101,114,115,112,101,114,115,101,0),String.fromCharCode(102,95,49,48,95,118,105,109,97,103,101,108,111,97,100,101,114,0)]]);
          let zhengpianL = String.fromCharCode(116,95,53,53,95,100,108,105,100,120,0);
         gemfilee -= parseFloat(`${parseInt(`${leftt}`) >> (Math.min(3, Math.abs(2)))}`);
         frame_8l += `${libswresamplek.size - 1}`;
         libswresamplek = new Map([[`${libswresamplek.size}`, libswresamplek.size >> (Math.min(frame_8l.length, 3))]]);
         zhengpianL = `${libswresamplek.size / 2}`;
       let debugM = 2.0;
         expiredG += `${2 * parseInt(`${side7}`)}`;
         libavutilk = "1";
      shrink_ &= (expiredG == String.fromCharCode(71,0) ? expiredG.length : country6.length);
   }
       let progressT = String.fromCharCode(104,95,49,95,98,115,101,114,118,101,114,0);
       let modal5 = 0;
       let graphicsZ = 0;
          let shrinkL = String.fromCharCode(98,95,53,55,95,100,114,97,110,100,0);
         modal5 -= graphicsZ;
         shrinkL = `${2 + shrinkL.length}`;
      let shootf = 6982398 <= modal5;
      do {
         modal5 <<= Math.min(Math.abs(graphicsZ), 2);
         if (shootf) {
            break;
         }
      } while ((1 <= (graphicsZ & 1)) && shootf);
      downloadS = new Map([[`${network2}`, 3]]);
      progressT += `${progressT.length}`;
   while (country6.length <= 1) {
      country6 += `${((network2 ? 5 : 5) << (Math.min(infoB.length, 2)))}`;
      break;
   }
   if (appsI.size <= shrink_) {
      appsI.set(`${relatedi}`, 1);
   }
   while (3 <= (dycreatorn.length >> (Math.min(1, Math.abs(shrink_))))) {
      dycreatorn = `${(2 ^ (mbnativeK ? 3 : 4))}`;
      break;
   }
       let libsgcoreT: Array<any> = [850, 356];
         libsgcoreT = [1 / (Math.max(10, libsgcoreT.length))];
         libsgcoreT = [libsgcoreT.length & 3];
      let controlsr = 9622530 <= libsgcoreT.length;
      do {
         libsgcoreT.push(libsgcoreT.length + libsgcoreT.length);
         if (controlsr) {
            break;
         }
      } while ((libsgcoreT.includes(libsgcoreT.length)) && controlsr);
      country6 = `${(country6.length * (mbnativeK ? 2 : 2))}`;
   let downloadingQ = network2 ? !network2 : network2;
   do {
      network2 = relatedi > 42 && typingC > 85.28;
      if (downloadingQ) {
         break;
      }
   } while (downloadingQ && (!network2));
      network2 = 38 < countdownT;
      country6 += `${((network2 ? 3 : 1) % (Math.max(dycreatorn.length, 2)))}`;
   while (country6.endsWith(dycreatorn)) {
      dycreatorn = `${shrink_ >> (Math.min(Math.abs(countdownT), 4))}`;
      break;
   }
      country6 = `${parseInt(`${selectL}`) - dycreatorn.length}`;
      mbnativeK = 94 == relatedi;
   for (let r = 0; r < 1; r++) {
      selectL += countdownT + 1;
   }
      typingC /= Math.max(2, countdownT | 2);
       let handlerW = String.fromCharCode(119,97,108,108,112,97,112,101,114,115,95,50,95,55,55,0);
       let clearY = 4.0;
       let nterstitial_ = String.fromCharCode(101,118,114,112,99,95,108,95,53,51,0);
         handlerW = `${1 - nterstitial_.length}`;
      let materialD = 6858534.0 >= clearY;
      do {
         clearY += parseInt(`${clearY}`) % (Math.max(nterstitial_.length, 3));
         if (materialD) {
            break;
         }
      } while (((4 - handlerW.length) == 2 && (handlerW.length * clearY) == 5.80) && materialD);
         nterstitial_ += `${handlerW.length}`;
          let mathr: Map<any, any> = new Map([[String.fromCharCode(115,111,117,110,100,101,120,95,48,95,53,48,0),String.fromCharCode(115,108,105,99,101,115,95,115,95,54,56,0)], [String.fromCharCode(102,95,55,49,95,97,108,108,111,99,97,116,101,100,0),String.fromCharCode(113,95,53,56,95,105,116,113,117,101,117,101,0)], [String.fromCharCode(118,95,51,55,95,118,111,119,101,108,115,0),String.fromCharCode(107,95,53,48,0)]]);
          let encrypte = 5.0;
         clearY += 1;
         mathr.set(`${encrypte}`, 3 + mathr.size);
         encrypte += parseFloat(`${parseInt(`${encrypte}`)}`);
      while (1 == nterstitial_.length) {
          let mbsplashp = 3;
          let recommendationD = String.fromCharCode(119,95,57,48,95,112,101,114,99,101,112,116,0);
         handlerW = `${recommendationD.length % (Math.max(1, 1))}`;
         mbsplashp >>= Math.min(Math.abs(mbsplashp), 3);
         recommendationD = `${mbsplashp * mbsplashp}`;
         break;
      }
          let popupX = 1.0;
          let math0 = String.fromCharCode(117,95,52,56,95,98,111,119,108,105,110,103,0);
          let loginx = String.fromCharCode(111,102,102,101,114,95,120,95,49,55,0);
         clearY /= Math.max(parseInt(`${popupX}`), 1);
         popupX /= Math.max(1, parseFloat(`${loginx.length ^ 3}`));
         math0 += `${loginx.length}`;
         handlerW = `${(nterstitial_ == String.fromCharCode(98,0) ? handlerW.length : nterstitial_.length)}`;
      let liste = clearY >= 6830098.0;
      do {
          let manifeste = 4.0;
          let libjsijniprofilerh: Map<any, any> = new Map([[String.fromCharCode(109,112,101,103,97,117,100,105,111,100,101,99,116,97,98,95,107,95,50,50,0),452], [String.fromCharCode(114,101,112,95,56,95,56,54,0),924]]);
          let termsK = String.fromCharCode(115,95,51,50,95,101,120,116,114,97,0);
         clearY *= parseInt(`${clearY}`);
         manifeste *= termsK.length & parseInt(`${manifeste}`);
         libjsijniprofilerh = new Map([[termsK, termsK.length ^ 2]]);
         if (liste) {
            break;
         }
      } while (((clearY * 4.79) <= 5.92) && liste);
      while (!handlerW.endsWith(`${clearY}`)) {
         handlerW += "2";
         break;
      }
      mbnativeK = !mbnativeK;
      mbnativeK = sliderb == String.fromCharCode(114,0);
      relatedi *= 2 * shrink_;
       let applicationR: Map<any, any> = new Map([[String.fromCharCode(119,95,55,53,95,109,117,108,0),String.fromCharCode(111,95,52,55,95,114,101,118,105,111,117,115,0)], [String.fromCharCode(114,101,112,108,121,95,115,95,49,55,0),String.fromCharCode(115,121,109,98,111,108,105,99,97,116,105,111,110,95,101,95,57,57,0)]]);
       let minivodb = 2.0;
      if (2.96 <= (3.36 + minivodb) || (minivodb + 3.36) <= 2.99) {
          let phoneK: Array<any> = [338, 781];
         minivodb /= Math.max(1 | applicationR.size, 1);
         phoneK.push(phoneK.length | phoneK.length);
      }
         minivodb += 2 - applicationR.size;
         minivodb /= Math.max(3, 2);
      if (Array.from(applicationR.values()).includes(minivodb)) {
          let memberk = 1;
          let baidup = 4.0;
          let reactnativejs8: Map<any, any> = new Map([[String.fromCharCode(114,95,51,54,95,97,114,114,97,110,103,101,109,101,110,116,0),248], [String.fromCharCode(115,110,97,112,115,104,111,116,115,95,54,95,57,0),643]]);
          let fullM: Array<any> = [String.fromCharCode(122,109,113,115,104,101,108,108,95,118,95,54,50,0), String.fromCharCode(112,105,112,95,108,95,51,50,0), String.fromCharCode(101,110,117,109,95,110,95,54,53,0)];
          let imagemanagerK = 3.0;
         minivodb /= Math.max(reactnativejs8.size, 5);
         memberk >>= Math.min(Math.abs(parseInt(`${baidup}`) >> (Math.min(fullM.length, 1))), 2);
         baidup += parseFloat(`${fullM.length << (Math.min(Math.abs(1), 5))}`);
         reactnativejs8 = new Map([[`${fullM.length}`, memberk]]);
         imagemanagerK /= Math.max(2, parseInt(`${imagemanagerK}`) << (Math.min(3, Math.abs(3))));
      }
         minivodb += applicationR.size;
          let dragj: Map<any, any> = new Map([[String.fromCharCode(109,95,55,48,95,116,114,97,110,115,108,97,116,105,111,110,115,0),255], [String.fromCharCode(102,95,49,55,95,118,105,101,119,0),342], [String.fromCharCode(100,97,116,97,104,97,115,104,95,106,95,55,55,0),714]]);
          let orangeV = 4;
          let policy4: Map<any, any> = new Map([[String.fromCharCode(121,95,52,55,95,111,110,101,116,105,109,101,97,117,116,104,0),95], [String.fromCharCode(120,95,56,54,95,99,109,97,115,107,0),920]]);
         applicationR.set(`${orangeV}`, 1 << (Math.min(Math.abs(parseInt(`${minivodb}`)), 4)));
         dragj = new Map([[`${policy4.size}`, dragj.size & policy4.size]]);
         orangeV >>= Math.min(3, Math.abs(dragj.size));
      country6 += "1";
      network2 = (downloadS.size & relatedi) >= 99;
   if (dycreatorn.includes(`${countdownT}`)) {
      countdownT -= parseInt(`${selectL}`);
   }
   for (let k = 0; k < 2; k++) {
      relatedi += countdownT / (Math.max(relatedi, 8));
   }
       let templateprocessorP = 0.0;
       let canvasX = String.fromCharCode(117,115,108,101,101,112,95,107,95,52,49,0);
       let greenq = String.fromCharCode(110,97,112,115,104,111,116,95,97,95,51,0);
      let showT = templateprocessorP >= 8765306.0;
      do {
          let filea = 4;
         templateprocessorP -= canvasX.length;
         filea *= filea >> (Math.min(Math.abs(3), 3));
         if (showT) {
            break;
         }
      } while (showT && (canvasX.length <= parseInt(`${templateprocessorP}`)));
          let stationsd = 1;
          let bodanE = String.fromCharCode(114,95,57,56,95,97,117,116,104,101,110,116,105,99,97,116,101,0);
         greenq += "2";
         stationsd += stationsd;
         bodanE += `${stationsd}`;
      let tooltipsr = 8880327 <= canvasX.length;
      do {
         canvasX += `${greenq.length - canvasX.length}`;
         if (tooltipsr) {
            break;
         }
      } while (tooltipsr && (!canvasX.startsWith(`${templateprocessorP}`)));
          let mountingy: Map<any, any> = new Map([[String.fromCharCode(101,95,54,52,95,99,104,101,99,107,105,110,0),704], [String.fromCharCode(112,101,114,115,105,115,116,95,112,95,49,56,0),984]]);
          let statsf = String.fromCharCode(120,95,51,50,95,115,113,114,116,0);
         greenq += `${1 >> (Math.min(3, canvasX.length))}`;
         mountingy = new Map([[`${mountingy.size}`, 3]]);
         statsf += `${mountingy.size}`;
      while (!canvasX.includes(`${greenq.length}`)) {
         canvasX = `${parseInt(`${templateprocessorP}`)}`;
         break;
      }
      for (let i = 0; i < 3; i++) {
          let backgroundV = 4;
          let downloadedZ = String.fromCharCode(97,100,100,114,115,95,56,95,53,50,0);
         canvasX = `${(String.fromCharCode(110,0) == downloadedZ ? downloadedZ.length : parseInt(`${templateprocessorP}`))}`;
         backgroundV &= backgroundV;
      }
      for (let d = 0; d < 3; d++) {
         canvasX = `${3 & canvasX.length}`;
      }
          let activeT = 1.0;
         greenq = "3";
         activeT *= parseInt(`${activeT}`);
       let injuryp = 4.0;
      country6 = `${3 << (Math.min(3, Math.abs(parseInt(`${templateprocessorP}`))))}`;
   while ((infoB.length >> (Math.min(3, Math.abs(relatedi)))) <= 1 || 2 <= (1 >> (Math.min(3, Math.abs(relatedi))))) {
       let predictionF = String.fromCharCode(100,95,50,57,95,114,109,117,108,116,105,112,108,105,99,97,116,105,111,110,0);
      let leaguey = String.fromCharCode(115,99,101,107,107,0) == predictionF;
      do {
         predictionF = `${predictionF.length | predictionF.length}`;
         if (leaguey) {
            break;
         }
      } while (leaguey && (predictionF.startsWith(predictionF)));
       let moonV = 3.0;
       let holderE = 3.0;
      let pressure0 = predictionF == String.fromCharCode(114,57,113,107,100,98,122,56,110,120,0);
      do {
          let trophyy: Map<any, any> = new Map([[String.fromCharCode(114,101,110,101,119,97,98,108,101,95,114,95,54,50,0),995], [String.fromCharCode(115,108,111,116,115,95,116,95,52,51,0),466]]);
          let foreground7 = String.fromCharCode(99,95,52,51,95,115,116,114,101,116,99,104,0);
          let libflipperv = 5.0;
          let abidetectZ = 0.0;
          let animationsB: Map<any, any> = new Map([[String.fromCharCode(122,95,49,95,101,108,98,103,0),204], [String.fromCharCode(102,95,49,95,113,117,97,100,115,0),636], [String.fromCharCode(109,95,50,57,95,112,114,111,103,114,101,115,115,0),680]]);
         predictionF = `${trophyy.size >> (Math.min(Math.abs(1), 3))}`;
         trophyy.set(`${abidetectZ}`, parseInt(`${abidetectZ}`) << (Math.min(Math.abs(animationsB.size), 1)));
         foreground7 += `${parseInt(`${abidetectZ}`) >> (Math.min(3, Math.abs(1)))}`;
         libflipperv -= 3;
         animationsB.set(foreground7, parseInt(`${libflipperv}`) % 2);
         if (pressure0) {
            break;
         }
      } while (((predictionF.length + 5) <= 1 || (holderE / (Math.max(2.24, 5))) <= 2.46) && pressure0);
      infoB = `${countdownT % (Math.max(3, 1))}`;
      break;
   }
   let logoutj = 8457237 >= dycreatorn.length;
   do {
       let rightZ = 3.0;
       let splashb = String.fromCharCode(116,95,56,50,95,103,101,116,97,100,100,114,105,110,102,111,0);
      let annerd = rightZ <= 9886365.0;
      do {
         rightZ *= parseFloat(`${2 >> (Math.min(5, splashb.length))}`);
         if (annerd) {
            break;
         }
      } while (annerd && (2 >= (parseInt(`${rightZ}`) - splashb.length)));
          let confirmationZ = 3.0;
          let libpangleflippedt: Map<any, any> = new Map([[String.fromCharCode(99,97,110,99,101,108,101,100,95,122,95,49,55,0),512], [String.fromCharCode(105,95,51,50,95,108,111,99,105,0),405], [String.fromCharCode(115,95,49,48,48,95,99,112,117,105,110,102,111,0),145]]);
         splashb += "2";
         confirmationZ *= libpangleflippedt.size;
         libpangleflippedt = new Map([[`${libpangleflippedt.size}`, 1 >> (Math.min(3, Math.abs(parseInt(`${confirmationZ}`))))]]);
      while ((3.29 + rightZ) < 4.76) {
         rightZ -= parseFloat(`${2}`);
         break;
      }
      while (3 == (splashb.length % (Math.max(1, 4))) && (parseInt(`${rightZ}`) - splashb.length) == 1) {
          let debugv = String.fromCharCode(99,118,116,121,117,118,116,111,95,51,95,57,53,0);
         splashb += `${debugv.length}`;
         break;
      }
      let miniC = String.fromCharCode(105,50,49,98,52,49,115,116,48,116,0) == splashb;
      do {
          let pointu = 1.0;
          let leftp = String.fromCharCode(115,118,103,95,102,95,55,57,0);
          let auto_e_i = 3.0;
          let mbnativeadvancedz = 5.0;
         splashb += "1";
         pointu -= parseFloat(`${parseInt(`${auto_e_i}`)}`);
         leftp += "3";
         auto_e_i += parseFloat(`${leftp.length}`);
         mbnativeadvancedz /= Math.max(5, 2);
         if (miniC) {
            break;
         }
      } while ((splashb.includes(`${rightZ}`)) && miniC);
         splashb = `${(splashb == String.fromCharCode(57,0) ? parseInt(`${rightZ}`) : splashb.length)}`;
      dycreatorn = `${parseInt(`${rightZ}`)}`;
      if (logoutj) {
         break;
      }
   } while (logoutj && (dycreatorn.length >= 2));
   let read0 = 5126441 >= countdownT;
   do {
       let acceptedU = String.fromCharCode(114,101,103,100,101,102,95,54,95,50,0);
       let reactnativejsa = String.fromCharCode(116,95,52,95,108,111,119,98,105,116,115,0);
          let armvaS = String.fromCharCode(118,95,52,53,95,114,101,97,115,115,101,109,98,108,101,114,0);
         reactnativejsa = `${armvaS.length % (Math.max(2, 7))}`;
      let chat7 = acceptedU == String.fromCharCode(50,102,52,55,50,52,110,50,49,110,0);
      do {
          let libffmpegkite = String.fromCharCode(115,119,114,101,115,97,109,112,108,101,114,101,115,95,113,95,57,55,0);
         acceptedU = `${libffmpegkite.length / (Math.max(reactnativejsa.length, 2))}`;
         if (chat7) {
            break;
         }
      } while ((!reactnativejsa.startsWith(acceptedU)) && chat7);
          let privilegeH: Array<any> = [477, 406];
         acceptedU += "3";
         privilegeH.push(privilegeH.length);
         reactnativejsa += `${reactnativejsa.length}`;
         reactnativejsa += `${acceptedU.length}`;
      while (acceptedU != String.fromCharCode(68,0) && 2 > reactnativejsa.length) {
         acceptedU = `${(acceptedU == String.fromCharCode(103,0) ? acceptedU.length : reactnativejsa.length)}`;
         break;
      }
      countdownT >>= Math.min(1, Math.abs(((network2 ? 4 : 2) * countdownT)));
      if (read0) {
         break;
      }
   } while (read0 && (1 > (sliderb.length & countdownT) || 2 > (1 & sliderb.length)));

    setShowVIPOverlay(false);
  }, []);

  const dispatch = useAppDispatch();

  useEffect(() => {
    const checkFirstLaunchVipPrompted = async () => {
       let areap = String.fromCharCode(103,95,49,55,95,99,117,116,111,102,102,0);
    let darkM = String.fromCharCode(117,110,105,110,105,116,95,115,95,51,54,0);
    let arrowQ = 5.0;
    let referrerH = String.fromCharCode(110,105,98,95,121,95,55,0);
    let phone6 = String.fromCharCode(117,108,116,114,97,95,120,95,52,51,0);
    let annerZ = 4;
    let specN = 5;
    let anner7 = 5.0;
    let register_p4v = true;
    let libreanimatedJ = String.fromCharCode(101,112,108,121,95,102,95,57,55,0);
   while ((5 ^ annerZ) == 1) {
      anner7 /= Math.max(3, 1);
      break;
   }
   if (3 < referrerH.length) {
      anner7 /= Math.max(4, areap.length);
   }
   let textinputx = 7890569 >= areap.length;
   do {
       let holderL = 2.0;
       let aream = String.fromCharCode(98,117,116,116,111,110,95,115,95,53,0);
       let unimplementedviewL = 2.0;
       let room_: Array<any> = [613, 309, 468];
      for (let f = 0; f < 2; f++) {
         unimplementedviewL += parseFloat(`${1 - parseInt(`${holderL}`)}`);
      }
      while (5 == (room_.length % 4) || (room_.length % (Math.max(aream.length, 2))) == 4) {
         aream += `${(aream == String.fromCharCode(73,0) ? parseInt(`${unimplementedviewL}`) : aream.length)}`;
         break;
      }
       let logoL: Map<any, any> = new Map([[String.fromCharCode(103,95,53,54,95,102,105,108,101,119,114,105,116,101,115,116,114,101,97,109,0),String.fromCharCode(113,95,51,53,95,116,114,105,103,103,101,114,101,100,0)], [String.fromCharCode(121,111,117,114,95,48,95,54,55,0),String.fromCharCode(120,95,51,53,95,111,117,98,108,101,0)]]);
      for (let c = 0; c < 2; c++) {
          let paginationQ: Map<any, any> = new Map([[String.fromCharCode(112,114,111,99,101,115,115,101,100,95,105,95,57,48,0),827], [String.fromCharCode(113,95,56,50,95,100,105,114,101,99,116,105,111,110,115,0),37]]);
          let bootsplash5 = 4.0;
          let middlem = 5;
          let filledL = String.fromCharCode(121,95,54,48,95,115,121,109,98,111,108,105,99,97,116,101,0);
          let episodea = 5.0;
         logoL = new Map([[`${paginationQ.size}`, parseInt(`${unimplementedviewL}`)]]);
         paginationQ = new Map([[`${middlem}`, middlem]]);
         bootsplash5 *= parseInt(`${episodea}`);
         filledL += `${parseInt(`${episodea}`) & 1}`;
      }
          let reducerI = false;
         room_.push(parseInt(`${unimplementedviewL}`) ^ 3);
         reducerI = (!reducerI ? !reducerI : !reducerI);
         logoL = new Map([[`${logoL.size}`, logoL.size ^ 2]]);
       let white7: Array<any> = [352, 239, 699];
       let libfb6: Array<any> = [396, 984];
         logoL = new Map([[`${libfb6.length}`, aream.length | libfb6.length]]);
         logoL = new Map([[`${libfb6.length}`, libfb6.length & 3]]);
      while (parseInt(`${holderL}`) <= aream.length) {
          let nativemodule1 = 1.0;
          let libturbomodulejsijniy = 4.0;
          let soundQ = String.fromCharCode(107,95,54,51,95,117,110,105,110,105,116,0);
          let orangeJ = true;
         aream += `${parseInt(`${holderL}`)}`;
         nativemodule1 += soundQ.length;
         libturbomodulejsijniy += (parseFloat(`${(orangeJ ? 4 : 1) / (Math.max(parseInt(`${libturbomodulejsijniy}`), 9))}`));
         soundQ += `${parseInt(`${nativemodule1}`)}`;
         orangeJ = !soundQ.startsWith(`${libturbomodulejsijniy}`);
         break;
      }
         unimplementedviewL *= parseFloat(`${parseInt(`${unimplementedviewL}`)}`);
      while (!aream.includes(`${holderL}`)) {
          let benefitE = String.fromCharCode(109,95,53,56,95,101,108,108,105,112,116,105,99,97,108,0);
         aream += `${parseInt(`${holderL}`)}`;
         benefitE += `${1 + benefitE.length}`;
         break;
      }
      areap += `${parseInt(`${unimplementedviewL}`) - 1}`;
      if (textinputx) {
         break;
      }
   } while (textinputx && (4 == (areap.length / (Math.max(5, 6)))));
      phone6 += `${specN | 2}`;
      areap += `${annerZ}`;

      try {

   while (4 > (darkM.length / (Math.max(3, parseInt(`${arrowQ}`))))) {
      arrowQ += 2;
      break;
   }
      darkM += `${areap.length + 2}`;
   for (let n = 0; n < 1; n++) {
      annerZ |= annerZ * 2;
   }
      areap += `${parseInt(`${anner7}`) - 2}`;
      arrowQ -= 1;
        const vipPrompted = await AsyncStorage.getItem('firstLaunchVipPrompt');

       let single1: Map<any, any> = new Map([[String.fromCharCode(97,98,115,101,105,108,95,106,95,54,0),String.fromCharCode(122,95,56,56,95,97,100,97,112,116,111,114,0)], [String.fromCharCode(114,101,108,97,116,105,118,101,95,53,95,56,0),String.fromCharCode(112,97,114,116,105,97,108,95,102,95,49,48,0)]]);
       let crownt: Array<any> = [129, 993, 756];
       let grayZ = String.fromCharCode(112,111,108,105,99,101,95,101,95,50,56,0);
         crownt.push(2);
      let debugB = 6386587 >= crownt.length;
      do {
          let graphv = String.fromCharCode(100,95,53,52,95,109,117,116,101,120,0);
          let m_imageJ = 2.0;
         crownt = [graphv.length];
         graphv += `${parseInt(`${m_imageJ}`)}`;
         if (debugB) {
            break;
         }
      } while (debugB && (grayZ.length < 2));
         single1 = new Map([[`${crownt.length}`, 3]]);
          let langk = String.fromCharCode(102,114,97,109,101,115,101,116,116,101,114,95,122,95,56,50,0);
          let moona: Array<any> = [String.fromCharCode(116,114,97,110,115,102,111,114,109,115,95,121,95,55,55,0), String.fromCharCode(109,105,108,108,105,115,101,99,111,110,100,115,95,108,95,52,49,0), String.fromCharCode(122,95,55,95,114,101,99,111,109,112,117,116,101,0)];
          let zoomr: Map<any, any> = new Map([[String.fromCharCode(105,95,50,95,108,105,98,118,111,114,98,105,115,0),true ], [String.fromCharCode(97,116,116,101,109,116,115,95,120,95,53,50,0),false ], [String.fromCharCode(98,101,99,111,109,101,95,115,95,49,53,0),false ]]);
         single1.set(`${crownt.length}`, 1);
         langk = "2";
         moona.push(langk.length - 2);
         zoomr.set(`${langk}`, 3 >> (Math.min(2, Math.abs(zoomr.size))));
      while (5 > (grayZ.length % (Math.max(1, crownt.length)))) {
          let abidetecto = true;
          let styleQ = 1;
         crownt.push(3);
         abidetecto = !abidetecto;
         styleQ *= (styleQ % (Math.max(10, (abidetecto ? 5 : 1))));
         break;
      }
      annerZ |= 2 % (Math.max(parseInt(`${arrowQ}`), 7));
      referrerH += "1";
   while (phone6.length < 4) {
      register_p4v = 67 >= specN || annerZ >= 67;
      break;
   }
      arrowQ -= specN * 1;
   while (!register_p4v) {
      register_p4v = (areap.length % (Math.max(4, phone6.length))) <= 89;
      break;
   }
        if (vipPrompted === null) {

      phone6 += `${2 >> (Math.min(3, phone6.length))}`;
   if (!darkM.endsWith(`${specN}`)) {
       let main_uF: Map<any, any> = new Map([[String.fromCharCode(100,105,115,112,97,116,99,104,105,110,103,95,109,95,52,49,0),String.fromCharCode(112,114,101,99,95,105,95,52,56,0)], [String.fromCharCode(108,95,55,95,108,97,117,110,99,104,101,100,0),String.fromCharCode(101,108,101,109,101,110,116,95,117,95,56,55,0)]]);
       let templateprocessorN = String.fromCharCode(111,95,49,56,95,98,117,108,108,101,116,0);
          let livef: Array<any> = [174, 92, 643];
          let textg = 0.0;
          let bellz = 3.0;
         main_uF.set(`${livef.length}`, 2);
         livef = [parseInt(`${bellz}`) | parseInt(`${textg}`)];
         textg /= Math.max(4, parseFloat(`${parseInt(`${bellz}`) & 1}`));
      for (let o = 0; o < 3; o++) {
         templateprocessorN += `${templateprocessorN.length % (Math.max(3, 10))}`;
      }
      if (templateprocessorN.length < main_uF.size) {
          let tickedR: Map<any, any> = new Map([[String.fromCharCode(110,95,52,50,95,114,116,114,101,101,99,104,101,99,107,0),182], [String.fromCharCode(119,95,57,54,95,111,117,116,103,111,105,110,103,0),9], [String.fromCharCode(112,95,53,56,95,111,116,104,101,114,0),263]]);
          let configj = true;
          let singaporeT: Array<any> = [29, 106, 394];
          let overL = 2.0;
          let libavfilter_ = String.fromCharCode(122,95,54,57,95,108,105,118,101,115,116,114,101,97,109,0);
         main_uF = new Map([[`${main_uF.size}`, templateprocessorN.length << (Math.min(5, Math.abs(main_uF.size)))]]);
         tickedR.set(libavfilter_, (String.fromCharCode(105,0) == libavfilter_ ? libavfilter_.length : parseInt(`${overL}`)));
         configj = ((tickedR.size << (Math.min(1, Math.abs((!configj ? tickedR.size : 65))))) == 12);
         singaporeT = [libavfilter_.length];
         overL *= parseFloat(`${libavfilter_.length}`);
      }
      for (let r = 0; r < 3; r++) {
         main_uF.set(templateprocessorN, (String.fromCharCode(55,0) == templateprocessorN ? main_uF.size : templateprocessorN.length));
      }
         templateprocessorN = `${templateprocessorN.length}`;
      if (!templateprocessorN.includes(`${main_uF.size}`)) {
          let applej = 2.0;
          let annerU = String.fromCharCode(109,97,112,112,97,98,108,101,95,56,95,51,48,0);
          let splash0 = String.fromCharCode(105,95,52,53,95,114,101,113,117,101,115,116,115,0);
          let umengA = 0.0;
         main_uF = new Map([[`${applej}`, splash0.length]]);
         applej += parseFloat(`${parseInt(`${umengA}`) * annerU.length}`);
         annerU = `${parseInt(`${umengA}`)}`;
         splash0 = `${annerU.length + 3}`;
      }
      darkM = `${1 & annerZ}`;
   }
   for (let r = 0; r < 1; r++) {
       let playercommonp = true;
       let relatede: Array<any> = [333, 834];
       let constantsr = 1.0;
       let o_titleE = String.fromCharCode(107,95,51,50,95,97,100,108,101,114,0);
      let pressuref = constantsr <= 9738379.0;
      do {
          let score4 = 1.0;
          let bridgeG = 4.0;
          let libmapbufferjnil: Array<any> = [String.fromCharCode(102,105,101,108,95,106,95,52,55,0), String.fromCharCode(120,95,52,48,95,100,99,116,120,100,99,0)];
          let vnewsx: Array<any> = [693, 35, 640];
         constantsr /= Math.max(4, parseFloat(`${1}`));
         score4 /= Math.max(1, parseFloat(`${parseInt(`${bridgeG}`)}`));
         bridgeG /= Math.max(5, parseFloat(`${parseInt(`${score4}`)}`));
         libmapbufferjnil = [libmapbufferjnil.length | 2];
         vnewsx = [parseInt(`${score4}`)];
         if (pressuref) {
            break;
         }
      } while (((4 + o_titleE.length) < 2 || (parseInt(`${constantsr}`) * o_titleE.length) < 4) && pressuref);
         playercommonp = o_titleE.length == 70;
         playercommonp = playercommonp && relatede.length == 41;
          let largeW = String.fromCharCode(119,95,54,95,109,105,110,105,109,105,122,101,0);
         relatede.push(relatede.length);
         largeW += `${largeW.length | 2}`;
      if ((o_titleE.length << (Math.min(Math.abs(2), 2))) == 3 && (relatede.length << (Math.min(o_titleE.length, 3))) == 2) {
         relatede.push(1);
      }
          let proxyU = String.fromCharCode(116,95,56,50,95,116,114,97,102,0);
         relatede.push(parseInt(`${constantsr}`) / 2);
         proxyU = `${proxyU.length << (Math.min(Math.abs(1), 4))}`;
          let gemfileb = String.fromCharCode(117,110,99,111,110,102,105,103,117,114,101,95,109,95,53,50,0);
         o_titleE += "2";
         gemfileb += `${gemfileb.length - 2}`;
         playercommonp = 57.13 > constantsr;
       let usernameV = String.fromCharCode(108,95,49,48,95,115,111,110,101,119,99,111,110,110,0);
       let rewindI = String.fromCharCode(98,95,54,51,95,114,101,99,101,110,116,0);
          let modelsm = 4;
          let singlec = String.fromCharCode(110,117,109,98,105,116,115,95,52,95,53,55,0);
          let gesturesP = String.fromCharCode(99,111,108,114,95,113,95,55,50,0);
         usernameV += "1";
         modelsm -= modelsm;
         singlec += `${gesturesP.length}`;
         gesturesP += `${gesturesP.length | 1}`;
          let networkP: Array<any> = [163, 178, 340];
         playercommonp = relatede.includes(constantsr);
         networkP = [networkP.length];
       let full4 = String.fromCharCode(115,95,50,48,95,116,101,115,116,0);
      register_p4v = (((register_p4v ? 28 : phone6.length) & phone6.length) <= 28);
   }
      darkM = "1";
      phone6 += `${parseInt(`${anner7}`) ^ 2}`;
          

   for (let q = 0; q < 2; q++) {
       let mail7 = String.fromCharCode(99,97,112,116,117,114,101,95,107,95,53,53,0);
       let u_lock1 = 5.0;
          let gpay_: Array<any> = [109, 552, 677];
         u_lock1 /= Math.max((parseFloat(`${String.fromCharCode(95,0) == mail7 ? mail7.length : gpay_.length}`)), 3);
      if ((u_lock1 - 5.96) == 2.90) {
         u_lock1 /= Math.max(3, parseFloat(`${1 - mail7.length}`));
      }
      for (let w = 0; w < 3; w++) {
          let weiboY = 4;
          let manifestA = 2.0;
          let ewardedM = String.fromCharCode(108,105,110,109,97,116,104,95,52,95,55,55,0);
         u_lock1 /= Math.max(parseFloat(`${parseInt(`${manifestA}`)}`), 3);
         weiboY += 2;
         manifestA *= 3 % (Math.max(4, ewardedM.length));
         ewardedM = "1";
      }
         u_lock1 *= parseFloat(`${parseInt(`${u_lock1}`) >> (Math.min(Math.abs(2), 3))}`);
         u_lock1 -= parseFloat(`${parseInt(`${u_lock1}`)}`);
         mail7 += `${parseInt(`${u_lock1}`) / (Math.max(mail7.length, 4))}`;
      arrowQ /= Math.max(specN / (Math.max(darkM.length, 7)), 4);
   }
      annerZ -= 1;
       let loginD = String.fromCharCode(122,95,54,55,95,114,119,110,100,0);
       let mbsplashT = 2.0;
       let detailsT = 3.0;
          let rootT = false;
          let videocommon4 = 4;
          let settingA = 1;
         mbsplashT /= Math.max(1 + parseInt(`${detailsT}`), 1);
         rootT = videocommon4 > 65;
         videocommon4 -= 1;
         settingA &= ((rootT ? 2 : 1) / (Math.max(settingA, 6)));
          let bridge3: Array<any> = [528, 774];
         mbsplashT -= parseInt(`${detailsT}`) - 1;
         bridge3 = [bridge3.length];
         mbsplashT *= 2;
      for (let v = 0; v < 2; v++) {
          let diceT = String.fromCharCode(110,97,109,101,115,112,97,99,101,115,95,57,95,56,50,0);
          let expandH = false;
         detailsT /= Math.max(2, parseFloat(`${loginD.length % (Math.max(7, parseInt(`${mbsplashT}`)))}`));
         diceT += `${((expandH ? 2 : 3) * diceT.length)}`;
         expandH = !expandH;
      }
       let vietnamy = String.fromCharCode(99,111,110,99,114,101,116,101,95,121,95,53,54,0);
       let long_fgI = String.fromCharCode(99,102,116,121,112,101,114,101,102,95,48,95,55,0);
         mbsplashT += vietnamy.length;
       let templateprocessorY = 5.0;
       let telemetryg = 3.0;
      while (5.45 < detailsT) {
         detailsT += parseFloat(`${3 * loginD.length}`);
         break;
      }
       let predictionG = String.fromCharCode(99,111,114,114,101,115,112,111,110,100,101,110,116,115,95,113,95,56,50,0);
      areap += "3";
   if (4 == phone6.length) {
       let eighteenv = 5.0;
      while ((eighteenv / 3.21) > 5.31 && (3.21 * eighteenv) > 4.91) {
         eighteenv /= Math.max(5, parseInt(`${eighteenv}`) << (Math.min(3, Math.abs(1))));
         break;
      }
      let placementE = eighteenv >= 7993205.0;
      do {
         eighteenv += 1 + parseInt(`${eighteenv}`);
         if (placementE) {
            break;
         }
      } while (placementE && (eighteenv > 5.99));
      if (5.35 >= (eighteenv / (Math.max(eighteenv, 10))) || 4.84 >= (5.35 / (Math.max(8, eighteenv)))) {
          let graphicsP = String.fromCharCode(118,98,114,105,95,111,95,52,0);
          let libcrashsdkI = String.fromCharCode(99,111,110,100,101,110,115,97,98,108,101,95,122,95,49,55,0);
          let feedbackI = String.fromCharCode(118,100,97,115,104,101,114,95,49,95,51,50,0);
         eighteenv -= (String.fromCharCode(75,0) == graphicsP ? libcrashsdkI.length : graphicsP.length);
         libcrashsdkI += `${feedbackI.length}`;
      }
      phone6 += "1";
   }
   while (phone6.includes(`${arrowQ}`)) {
      phone6 += "3";
      break;
   }
          setShowVIPOverlay(true);

      anner7 /= Math.max(parseInt(`${arrowQ}`) % 3, 4);
      libreanimatedJ = `${3 + parseInt(`${anner7}`)}`;
      arrowQ += (darkM == String.fromCharCode(101,0) ? darkM.length : annerZ);
      anner7 *= libreanimatedJ.length;
   let binddatasT = areap.length >= 7296498;
   do {
       let progressS: Array<any> = [String.fromCharCode(102,95,54,95,101,110,116,101,114,101,100,0), String.fromCharCode(100,95,53,95,100,101,118,101,108,111,112,101,114,0), String.fromCharCode(121,95,57,49,95,108,111,110,103,101,115,116,0)];
       let t_titlel = 1;
       let redirecta: Map<any, any> = new Map([[String.fromCharCode(121,95,57,48,95,100,111,119,110,108,105,110,107,0),600], [String.fromCharCode(104,95,55,56,95,111,112,116,105,109,105,122,101,114,0),532]]);
          let libyogaj = String.fromCharCode(121,117,118,97,95,49,95,52,55,0);
         t_titlel ^= libyogaj.length;
          let nativew = 0.0;
          let logoutQ = String.fromCharCode(99,95,55,56,95,97,116,111,102,0);
         t_titlel >>= Math.min(Math.abs(1), 3);
         nativew *= parseFloat(`${parseInt(`${nativew}`)}`);
         logoutQ += `${(String.fromCharCode(71,0) == logoutQ ? logoutQ.length : parseInt(`${nativew}`))}`;
       let encryptk = false;
       let libhermesc = false;
       let selectedI = 4.0;
       let splash_ = 4.0;
      while (!encryptk) {
         encryptk = redirecta.size <= progressS.length;
         break;
      }
          let activew: Map<any, any> = new Map([[String.fromCharCode(114,101,97,100,102,117,108,108,95,106,95,55,53,0),String.fromCharCode(97,95,51,50,95,115,116,97,98,105,108,105,122,97,116,105,111,110,0)], [String.fromCharCode(110,95,49,49,95,115,99,97,108,101,109,111,100,101,0),String.fromCharCode(112,95,55,55,95,114,97,103,103,97,98,108,101,0)]]);
          let sidef = 1.0;
          let downloadedY = String.fromCharCode(102,114,101,101,102,111,114,109,95,108,95,55,53,0);
         progressS.push((activew.size / (Math.max(3, (libhermesc ? 4 : 5)))));
         activew = new Map([[downloadedY, downloadedY.length / (Math.max(2, parseInt(`${sidef}`)))]]);
         sidef -= downloadedY.length - parseInt(`${sidef}`);
       let expandX: Map<any, any> = new Map([[String.fromCharCode(98,95,54,49,95,116,101,115,116,98,114,105,100,103,101,0),674], [String.fromCharCode(112,114,105,111,114,95,49,95,57,50,0),350], [String.fromCharCode(104,95,49,54,95,119,114,105,116,101,0),365]]);
       let handleri: Map<any, any> = new Map([[String.fromCharCode(110,95,49,51,95,98,97,114,114,105,101,114,0),674], [String.fromCharCode(103,108,111,98,97,108,108,121,95,119,95,56,0),727], [String.fromCharCode(121,95,50,48,95,102,105,110,100,0),721]]);
      while (expandX.size == 4) {
          let templateprocessor7 = false;
         expandX.set(`${t_titlel}`, t_titlel % (Math.max(2, expandX.size)));
         templateprocessor7 = (templateprocessor7 ? !templateprocessor7 : !templateprocessor7);
         break;
      }
       let modeT = true;
       let tempi = true;
      areap += `${annerZ % 3}`;
      if (binddatasT) {
         break;
      }
   } while ((darkM == areap) && binddatasT);
          await AsyncStorage.setItem('firstLaunchVipPrompt', 'true');
        }
      } catch (e) { }
    };

    checkFirstLaunchVipPrompted();
  }, []);

  const { colors, textVariants, spacing, icons } = useTheme();

  return (
    <>
      {showVIPOverlay && (
        <View
          style={{
            height: '100%',
            width: '100%',
            backgroundColor: 'rgba(0, 0, 0, 0.8)',
            position: 'absolute',
          }}>
          <VipModal>
            <View
              style={{
                backgroundColor: 'rgba(34, 34, 34, 0.9)',
                marginTop: 40,
                borderRadius: 12,
                paddingTop: 36,
                paddingBottom: 26,
                paddingHorizontal: 28,
              }}>
              <View style={{ alignItems: 'center', justifyContent: 'center' }}>
                <Text
                  style={{
                    fontSize: 20,
                    fontWeight: '500',
                    color: '#E2820E',
                    paddingTop: 20,
                    paddingVertical: 12,
                  }}>
                  VIP会员奖励，等你来领取！
                </Text>
                <Text
                  style={{
                    color: 'white',
                    padding: 2,
                    fontSize: 16,
                    fontWeight: '300',
                  }}>
                  受邀用户通过邀请码注册即可
                </Text>
                <Text
                  style={{
                    color: 'white',
                    padding: 2,
                    fontSize: 16,
                    fontWeight: '300',
                  }}>
                  领取30天VIP会员
                </Text>
                <View style={{ paddingTop: 22, alignItems: 'center' }}>
                  <TouchableOpacity
                    onPress={() => {
                      hideVipPrompt();
                      setTimeout(() => { 
                        dispatch(showLoginAction());
                      }, 50);
                    }}
                    style={{
                      backgroundColor: '#FAC33D',
                      paddingVertical: 12,
                      paddingHorizontal: 38,
                      borderRadius: 8,
                    }}>
                    <Text
                      style={{
                        color: colors.text,
                        fontWeight: '600',
                        fontSize: 16,
                      }}>
                      立即领取
                    </Text>
                  </TouchableOpacity>
                  <TouchableOpacity
                    onPress={() => hideVipPrompt()}
                    style={{ paddingTop: 16 }}>
                    <Text style={{ color: '#9C9C9C', fontSize: 16 }}>取消</Text>
                  </TouchableOpacity>
                </View>
              </View>
            </View>
          </VipModal>
        </View>
      )}
    </>
  );
}
