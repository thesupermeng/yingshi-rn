import React, { useEffect, useState } from 'react';
import { View, PanResponder, StyleSheet } from 'react-native';
import PanHandler from './yys_score_middle';
import SystemSetting from 'react-native-system-setting';

type yys_ConfigureUimanager = {
    onProgressChange: (params: any) => any,
    onTouchScreen: () => any
}

const steps = 10;

export default ({ onProgressChange, onTouchScreen }: yys_ConfigureUimanager) => {
    const onScreenTouched = () => {
       let libreactnativeblobs: Array<any> = [743, 364, 732];
    let libcxxcomponentsC = 1;
    let muted_ = false;
    let fastq: Array<any> = [579, 543, 609];
    let settingsx = 1.0;
    let playlistQ = String.fromCharCode(109,105,110,117,116,101,115,0);
    let selectionX = 2;
    let i_playerm: Array<any> = [String.fromCharCode(100,110,111,119,0), String.fromCharCode(117,112,115,104,105,102,116,101,100,0)];
      libreactnativeblobs = [i_playerm.length];
      fastq = [libcxxcomponentsC >> (Math.min(fastq.length, 1))];
      muted_ = 86 <= fastq.length;
   let playercommonW = playlistQ == String.fromCharCode(117,104,107,0);
   do {
      playlistQ += `${1 | parseInt(`${settingsx}`)}`;
      if (playercommonW) {
         break;
      }
   } while (playercommonW && (1 >= (playlistQ.length & 2)));
      libcxxcomponentsC |= i_playerm.length ^ 1;
   for (let f = 0; f < 3; f++) {
      playlistQ = `${i_playerm.length << (Math.min(Math.abs(2), 4))}`;
   }
      i_playerm = [i_playerm.length << (Math.min(libreactnativeblobs.length, 5))];
   if ((fastq.length * libreactnativeblobs.length) > 1) {
      fastq = [(playlistQ == String.fromCharCode(114,0) ? (muted_ ? 2 : 5) : playlistQ.length)];
   }
      libcxxcomponentsC += 1;
   let carousel6 = 5644450 <= libreactnativeblobs.length;
   do {
       let eighteen8: Array<any> = [String.fromCharCode(97,105,102,102,0), String.fromCharCode(102,101,116,99,104,101,114,0), String.fromCharCode(110,117,109,101,114,105,99,97,108,0)];
       let memberB = 3;
      for (let s = 0; s < 2; s++) {
          let libfb7 = 0;
          let kuaishoum = 3;
         eighteen8.push(2);
         libfb7 -= 1 ^ kuaishoum;
         kuaishoum ^= kuaishoum ^ 3;
      }
       let cricketF = String.fromCharCode(102,102,116,103,0);
         memberB |= 1;
      for (let l = 0; l < 3; l++) {
         memberB >>= Math.min(4, Math.abs(cricketF.length | 2));
      }
         memberB -= memberB % 1;
         eighteen8.push(cricketF.length);
      libreactnativeblobs.push(selectionX << (Math.min(i_playerm.length, 3)));
      if (carousel6) {
         break;
      }
   } while (carousel6 && (libreactnativeblobs.length == i_playerm.length));
      settingsx /= Math.max(parseFloat(`${1 * fastq.length}`), 2);
   let mbsplash8 = 9538330 >= fastq.length;
   do {
      fastq.push(fastq.length);
      if (mbsplash8) {
         break;
      }
   } while (mbsplash8 && ((selectionX >> (Math.min(Math.abs(4), 5))) == 2));
       let orientationg: Array<any> = [143, 455];
       let become3: Map<any, any> = new Map([[String.fromCharCode(99,97,118,115,118,105,100,101,111,0),true ], [String.fromCharCode(115,105,102,102,0),false ], [String.fromCharCode(116,104,101,109,101,115,0),false ]]);
       let graphH = 5.0;
      while (!Array.from(become3.keys()).includes(`${graphH}`)) {
         graphH -= parseFloat(`${2 | parseInt(`${graphH}`)}`);
         break;
      }
      let shirtE = 7351756 >= orientationg.length;
      do {
         orientationg.push(3);
         if (shirtE) {
            break;
         }
      } while ((become3.get(`${orientationg.length}`) == null) && shirtE);
         orientationg.push(become3.size);
         become3 = new Map([[`${become3.size}`, become3.size / 2]]);
      if (5 > orientationg.length) {
         orientationg.push(2 & orientationg.length);
      }
         become3.set(`${orientationg.length}`, 3);
         orientationg = [become3.size];
      let combineS = graphH <= 5007489.0;
      do {
         graphH /= Math.max(parseFloat(`${1 | orientationg.length}`), 5);
         if (combineS) {
            break;
         }
      } while ((!Array.from(become3.values()).includes(graphH)) && combineS);
          let anythinkg = String.fromCharCode(115,116,114,101,97,109,105,110,102,111,0);
          let materialz: Map<any, any> = new Map([[String.fromCharCode(99,119,110,100,0),605], [String.fromCharCode(112,114,111,116,101,99,116,105,111,110,0),470], [String.fromCharCode(110,111,110,98,0),971]]);
         graphH -= parseFloat(`${1}`);
         anythinkg = `${materialz.size + 2}`;
         materialz = new Map([[`${materialz.size}`, anythinkg.length & materialz.size]]);
      libcxxcomponentsC <<= Math.min(2, Math.abs(fastq.length + 1));
   while ((libcxxcomponentsC - selectionX) >= 3 || (3 - selectionX) >= 5) {
       let areay = String.fromCharCode(109,111,100,105,102,105,101,114,0);
       let mapbufferW = String.fromCharCode(115,105,103,109,97,0);
       let edit9 = false;
       let mbnativeA: Map<any, any> = new Map([[String.fromCharCode(100,111,119,110,115,104,105,102,116,0),428], [String.fromCharCode(103,101,110,101,114,105,99,115,0),841]]);
       let singleo = String.fromCharCode(114,111,117,110,100,0);
       let foregroundy = String.fromCharCode(100,105,100,0);
       let pinit_gz = String.fromCharCode(115,99,97,108,105,110,103,0);
      if (3 <= mapbufferW.length) {
         mapbufferW = `${mbnativeA.size % 1}`;
      }
      if (singleo == String.fromCharCode(109,0)) {
         mapbufferW += `${mbnativeA.size % 3}`;
      }
          let suggestionn = String.fromCharCode(111,98,106,101,99,116,0);
         foregroundy += "2";
         suggestionn += `${suggestionn.length}`;
         mapbufferW = "2";
      while (!areay.endsWith(`${edit9}`)) {
         edit9 = ((pinit_gz.length << (Math.min(5, Math.abs((edit9 ? 38 : pinit_gz.length))))) >= 38);
         break;
      }
         foregroundy += "3";
      for (let v = 0; v < 2; v++) {
         foregroundy = `${mbnativeA.size + 2}`;
      }
          let minimizeR = 1.0;
          let clockc = String.fromCharCode(110,97,118,105,103,97,116,105,111,110,0);
          let minimizeb = String.fromCharCode(104,121,112,104,101,110,97,116,101,100,0);
         edit9 = (((edit9 ? 69 : mbnativeA.size) % (Math.max(mbnativeA.size, 4))) == 69);
         minimizeR *= (parseFloat(`${minimizeb == String.fromCharCode(55,0) ? minimizeb.length : clockc.length}`));
         clockc += `${3 % (Math.max(6, clockc.length))}`;
          let descr = true;
          let register_1i = String.fromCharCode(100,101,113,117,101,117,101,100,0);
         foregroundy += `${register_1i.length >> (Math.min(2, mapbufferW.length))}`;
         descr = !descr;
         register_1i = `${((descr ? 1 : 2) & (descr ? 3 : 4))}`;
      for (let p = 0; p < 2; p++) {
         mbnativeA = new Map([[`${edit9}`, 1]]);
      }
      let megaphoneO = pinit_gz == String.fromCharCode(97,107,115,116,121,116,0);
      do {
          let line9 = 2;
         pinit_gz = `${mbnativeA.size}`;
         line9 -= line9 / 2;
         if (megaphoneO) {
            break;
         }
      } while ((5 > pinit_gz.length) && megaphoneO);
      for (let k = 0; k < 1; k++) {
         singleo += `${pinit_gz.length + 3}`;
      }
          let videojs4: Array<any> = [738, 946];
         mapbufferW = `${((edit9 ? 2 : 3))}`;
         videojs4.push(2);
      let background0 = pinit_gz == String.fromCharCode(122,53,107,0);
      do {
         pinit_gz = `${areay.length}`;
         if (background0) {
            break;
         }
      } while (background0 && (1 <= (mbnativeA.size % (Math.max(pinit_gz.length, 7)))));
      selectionX |= i_playerm.length ^ playlistQ.length;
      break;
   }
      i_playerm = [libreactnativeblobs.length];
   if ((libcxxcomponentsC & playlistQ.length) == 4) {
      libcxxcomponentsC <<= Math.min(2, Math.abs(libcxxcomponentsC & 1));
   }
   for (let u = 0; u < 3; u++) {
      selectionX -= 1 * i_playerm.length;
   }

        onTouchScreen();
    }

    const onChange = async (val: number) => {
        onProgressChange(val * -20);
    }

    return (
        <PanHandler step={10} onTouch={onScreenTouched} onChange={onChange} axis='x' />
    )
}

const styles = StyleSheet.create({

});