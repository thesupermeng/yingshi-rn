import React, { useEffect, useState } from 'react';
import { View, PanResponder, StyleSheet } from 'react-native';
import PanHandler from './tt_become_gray';
import SystemSetting from 'react-native-system-setting';

type ttSmall = {
    onChangeVolume: (params: any) => any,
    onTouchScreen: () => any
}

const steps = 1;

export default ({ onChangeVolume, onTouchScreen }: ttSmall) => {

    const [startY, setStartY] = useState(0)
    const [endY, setEndY] = useState(0)

    const onScreenTouched = () => {
       let awaym = 1;
    let halfL = String.fromCharCode(108,111,119,99,111,109,112,95,120,95,55,56,0);
    let whitev: Array<any> = [629, 993];
    let volume3 = String.fromCharCode(100,95,54,54,95,97,118,99,105,0);
    let penaltyv = String.fromCharCode(100,101,115,116,114,117,99,116,95,120,95,48,0);
    let dycreatorU: Map<any, any> = new Map([[String.fromCharCode(119,95,56,52,95,112,97,114,115,101,0),324], [String.fromCharCode(119,105,110,100,111,119,101,100,95,106,95,49,52,0),23]]);
    let commonp: Array<any> = [78, 797];
    let product0 = String.fromCharCode(112,111,119,101,114,101,100,95,99,95,54,51,0);
    let hongkongI = 3;
    let userK = String.fromCharCode(116,114,97,99,105,110,103,95,109,95,53,49,0);
    let backt = String.fromCharCode(118,95,49,50,95,97,99,99,101,112,116,101,100,0);
    let privacy4 = 4.0;
    let mail3 = 3.0;
       let optionsi = 0.0;
      let entryP = 9328417.0 <= optionsi;
      do {
         optionsi /= Math.max(1, parseFloat(`${1}`));
         if (entryP) {
            break;
         }
      } while ((optionsi <= 2.56) && entryP);
      if (2.17 > (optionsi / 1)) {
         optionsi *= parseFloat(`${2}`);
      }
      while (2.73 > optionsi) {
          let handlerl = String.fromCharCode(106,95,54,56,95,98,105,103,103,101,115,116,0);
          let expired9: Array<any> = [506, 927, 450];
         optionsi -= parseFloat(`${parseInt(`${optionsi}`)}`);
         handlerl = "1";
         expired9 = [handlerl.length + expired9.length];
         break;
      }
      hongkongI &= 1 >> (Math.min(4, Math.abs(hongkongI)));
   if (volume3 == String.fromCharCode(108,0)) {
       let storeo = false;
       let pausel = 1.0;
          let expandr = String.fromCharCode(112,108,97,99,101,115,95,120,95,54,51,0);
          let thumbnail3 = String.fromCharCode(115,116,97,116,101,102,117,108,95,117,95,57,53,0);
         pausel += ((storeo ? 4 : 3) / 3);
         expandr += `${thumbnail3.length + expandr.length}`;
         thumbnail3 += `${1 & thumbnail3.length}`;
          let streamingq = 0.0;
          let frame_sz = 0.0;
         storeo = pausel > 50.8 || 50.8 > streamingq;
         streamingq *= 2;
         frame_sz /= Math.max(2, parseInt(`${frame_sz}`) % 3);
         pausel += (parseInt(`${pausel}`) - (storeo ? 4 : 3));
      while (1.63 == pausel) {
         pausel -= 2 | parseInt(`${pausel}`);
         break;
      }
      while (5.91 < (pausel / (Math.max(5.59, 6))) || 5.59 < pausel) {
         pausel += ((storeo ? 4 : 4) << (Math.min(Math.abs(parseInt(`${pausel}`)), 1)));
         break;
      }
         pausel -= ((storeo ? 2 : 2) - parseInt(`${pausel}`));
      product0 += `${(String.fromCharCode(65,0) == userK ? penaltyv.length : userK.length)}`;
   }
      whitev.push((halfL == String.fromCharCode(55,0) ? halfL.length : whitev.length));
   if (dycreatorU.get(`${awaym}`) != null) {
      awaym <<= Math.min(1, Math.abs(volume3.length % (Math.max(3, product0.length))));
   }
   while ((awaym & 1) < 2 && (awaym & 1) < 5) {
      halfL += `${commonp.length / (Math.max(halfL.length, 7))}`;
      break;
   }
   let fastforwardW = backt == String.fromCharCode(100,51,100,115,109,100,101,57,117,0);
   do {
       let g_positiona = String.fromCharCode(98,105,119,103,116,95,122,95,52,53,0);
       let handlerS = 2.0;
       let saveS = 1.0;
       let stringw = 0.0;
         g_positiona = `${2 >> (Math.min(Math.abs(parseInt(`${saveS}`)), 1))}`;
         saveS -= parseInt(`${stringw}`);
      for (let m = 0; m < 1; m++) {
         saveS += 1;
      }
       let album7: Array<any> = [856, 12];
          let clearX: Array<any> = [290, 327];
          let matchesp = String.fromCharCode(108,111,111,115,101,95,118,95,54,56,0);
         album7.push(1 - matchesp.length);
         clearX.push(clearX.length);
         matchesp = `${clearX.length}`;
      let layoutW = 5305276.0 <= handlerS;
      do {
         handlerS *= parseFloat(`${parseInt(`${handlerS}`)}`);
         if (layoutW) {
            break;
         }
      } while (((parseFloat(`${album7.length}`) - handlerS) < 1.76) && layoutW);
         g_positiona += `${g_positiona.length >> (Math.min(5, Math.abs(parseInt(`${handlerS}`))))}`;
      for (let z = 0; z < 2; z++) {
         saveS += 2;
      }
      while (!album7.includes(stringw)) {
         stringw -= parseInt(`${stringw}`) & 1;
         break;
      }
         album7.push(2);
         album7 = [album7.length];
      while (handlerS > 1.89) {
         handlerS /= Math.max(2, parseFloat(`${2}`));
         break;
      }
      backt = `${backt.length | 2}`;
      if (fastforwardW) {
         break;
      }
   } while (fastforwardW && (3 < (awaym / (Math.max(backt.length, 2))) && (backt.length / (Math.max(3, 9))) < 5));
      halfL = `${volume3.length}`;
   for (let r = 0; r < 3; r++) {
      awaym /= Math.max(halfL.length, 1);
   }
      commonp.push(whitev.length ^ 3);
   if (5 >= (2 ^ penaltyv.length)) {
       let page6 = true;
         page6 = (page6 ? page6 : page6);
         page6 = page6 && page6;
          let dycreatorZ = true;
          let trashR = false;
         page6 = !dycreatorZ;
         trashR = (trashR ? trashR : trashR);
      commonp = [halfL.length];
   }
       let xvodP = 4.0;
       let combineS = String.fromCharCode(115,111,108,117,116,105,111,110,95,56,95,54,57,0);
       let awayS = 1;
       let styleso = 5;
      let countryY = 9057198 >= combineS.length;
      do {
         combineS = `${combineS.length}`;
         if (countryY) {
            break;
         }
      } while (countryY && (4 <= (parseInt(`${xvodP}`) / (Math.max(4, combineS.length)))));
      whitev.push(product0.length);
      hongkongI %= Math.max(3 >> (Math.min(3, product0.length)), 2);
      dycreatorU = new Map([[`${commonp.length}`, 1]]);
   if (halfL.endsWith(`${dycreatorU.size}`)) {
      dycreatorU.set(product0, 3 ^ product0.length);
   }
   if ((4 & awaym) < 4) {
       let streamings: Array<any> = [407, 683, 923];
      for (let r = 0; r < 2; r++) {
         streamings.push(streamings.length | 2);
      }
         streamings = [streamings.length / (Math.max(10, streamings.length))];
         streamings.push(streamings.length ^ 3);
      awaym %= Math.max(4, product0.length % 3);
   }
   while ((penaltyv.length % 4) < 3) {
       let package_eB = String.fromCharCode(114,95,50,57,95,115,116,114,105,115,116,114,0);
       let vietnam6 = String.fromCharCode(100,99,111,110,108,121,95,119,95,55,53,0);
       let thumbnailN = 3.0;
          let inactiveu: Array<any> = [String.fromCharCode(97,99,111,108,111,114,115,95,118,95,54,49,0), String.fromCharCode(116,105,116,110,116,95,54,95,54,51,0)];
          let loginy = String.fromCharCode(97,108,103,111,114,105,116,104,109,95,114,95,49,49,0);
         vietnam6 = `${loginy.length - 1}`;
         inactiveu.push(inactiveu.length);
         loginy = `${inactiveu.length}`;
      for (let l = 0; l < 3; l++) {
         thumbnailN /= Math.max(1, parseFloat(`${2 & package_eB.length}`));
      }
      while ((package_eB.length - parseInt(`${thumbnailN}`)) == 3) {
         thumbnailN -= parseFloat(`${1}`);
         break;
      }
          let photoQ = String.fromCharCode(101,95,51,49,95,115,101,116,114,97,110,103,101,0);
          let mbbannerN = true;
         package_eB += `${((mbbannerN ? 5 : 1) | 2)}`;
         photoQ = `${photoQ.length | 1}`;
         mbbannerN = 40 == photoQ.length && photoQ.length == 40;
         thumbnailN -= parseFloat(`${1}`);
      if (package_eB.includes(`${thumbnailN}`)) {
         thumbnailN += parseFloat(`${parseInt(`${thumbnailN}`)}`);
      }
          let private_lpM = true;
          let combined7: Array<any> = [329, 41, 502];
         vietnam6 += `${vietnam6.length}`;
         private_lpM = !private_lpM;
         combined7 = [combined7.length];
         vietnam6 = `${vietnam6.length >> (Math.min(Math.abs(3), 3))}`;
         thumbnailN += (parseFloat(`${String.fromCharCode(88,0) == vietnam6 ? vietnam6.length : parseInt(`${thumbnailN}`)}`));
      penaltyv = `${(String.fromCharCode(77,0) == halfL ? volume3.length : halfL.length)}`;
      break;
   }
   let signinupL = String.fromCharCode(57,107,102,102,95,0) == halfL;
   do {
      halfL = `${(penaltyv == String.fromCharCode(113,0) ? commonp.length : penaltyv.length)}`;
      if (signinupL) {
         break;
      }
   } while (signinupL && (4 >= (halfL.length % (Math.max(4, dycreatorU.size)))));
   if (1 > (volume3.length * 5) || 4 > (5 * volume3.length)) {
      volume3 = `${backt.length % (Math.max(2, 6))}`;
   }
      volume3 = `${backt.length}`;
   while (!volume3.endsWith(`${halfL.length}`)) {
      volume3 = `${2 - backt.length}`;
      break;
   }
   let groupe = 9278022 >= volume3.length;
   do {
      volume3 = `${(String.fromCharCode(109,0) == volume3 ? dycreatorU.size : volume3.length)}`;
      if (groupe) {
         break;
      }
   } while (groupe && (volume3.length < commonp.length));
   while (2 > (awaym + product0.length)) {
      product0 = `${3 >> (Math.min(3, Math.abs(hongkongI)))}`;
      break;
   }
   let reducerL = penaltyv == String.fromCharCode(107,122,114,0);
   do {
      penaltyv = `${2 * volume3.length}`;
      if (reducerL) {
         break;
      }
   } while ((penaltyv.length > hongkongI) && reducerL);
       let penalty5: Array<any> = [826, 513];
         penalty5 = [penalty5.length ^ penalty5.length];
      if (3 <= (penalty5.length << (Math.min(Math.abs(5), 2))) || (penalty5.length << (Math.min(penalty5.length, 5))) <= 5) {
          let holderZ: Map<any, any> = new Map([[String.fromCharCode(116,101,114,109,95,101,95,57,50,0),335], [String.fromCharCode(109,101,115,111,110,95,110,95,53,56,0),430], [String.fromCharCode(101,95,49,57,95,119,97,116,101,114,109,97,114,107,115,0),965]]);
          let baiduR = 4.0;
          let anner_ = String.fromCharCode(112,95,56,57,95,112,114,101,118,105,101,119,0);
          let renew4 = 1.0;
          let videojsJ = false;
         penalty5.push(parseInt(`${baiduR}`) % 2);
         holderZ.set(`${videojsJ}`, (holderZ.size & (videojsJ ? 4 : 1)));
         baiduR *= parseFloat(`${anner_.length + holderZ.size}`);
         anner_ += `${anner_.length}`;
         renew4 /= Math.max(4, parseInt(`${renew4}`) - 1);
      }
          let turn2 = 1.0;
          let condensedR = 3.0;
          let windt = 4.0;
         penalty5 = [parseInt(`${windt}`) + 2];
         turn2 -= parseInt(`${condensedR}`) * parseInt(`${turn2}`);
         condensedR += parseFloat(`${parseInt(`${condensedR}`) | parseInt(`${turn2}`)}`);
         windt /= Math.max(parseFloat(`${parseInt(`${turn2}`)}`), 4);
      halfL += `${penalty5.length}`;
   while ((3 + dycreatorU.size) == 3 && (3 + volume3.length) == 3) {
      volume3 = "3";
      break;
   }
   if (product0.length <= whitev.length) {
       let filledR = 3.0;
       let activex = 3;
       let playlista = 2.0;
       let renewY = 5.0;
          let redirectd: Map<any, any> = new Map([[String.fromCharCode(104,95,51,48,95,102,97,110,116,111,109,0),714], [String.fromCharCode(112,95,53,57,95,112,97,114,97,103,114,97,112,104,0),633]]);
          let animationf = String.fromCharCode(104,95,57,57,95,118,112,108,112,102,0);
         activex <<= Math.min(Math.abs(parseInt(`${filledR}`)), 4);
         redirectd.set(`${animationf}`, 1 % (Math.max(9, animationf.length)));
         renewY *= parseFloat(`${parseInt(`${playlista}`) & activex}`);
      for (let c = 0; c < 3; c++) {
         renewY -= parseFloat(`${parseInt(`${renewY}`) + 3}`);
      }
          let h_center_ = true;
          let connectionB = String.fromCharCode(99,111,110,110,101,99,116,95,120,95,51,49,0);
         playlista += parseFloat(`${1}`);
         h_center_ = connectionB.length <= 91;
         connectionB += `${((h_center_ ? 3 : 4) >> (Math.min(Math.abs(1), 1)))}`;
         activex %= Math.max(activex << (Math.min(3, Math.abs(1))), 4);
      whitev.push(2);
   }

        onTouchScreen();
    }

    const onVolumeChanged = async (val: number) => {
        const currVolume = await SystemSetting.getVolume();
        val = val > 0 ? currVolume + 1 / steps : currVolume - 1 / steps;
        val = Math.min(1, Math.max(0, val));

        onChangeVolume(parseFloat(val.toFixed(2)));

        SystemSetting.setVolume(val);
        return;
    }

    return (
        <PanHandler step={1} onTouch={onScreenTouched} onChange={onVolumeChanged} />
    )
}

const styles = StyleSheet.create({

});