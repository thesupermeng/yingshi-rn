import React, { useEffect, useState } from 'react';
import { View, PanResponder, StyleSheet } from 'react-native';
import PanHandler from './tt_become_gray';
import SystemSetting from 'react-native-system-setting';

type ttSmall = {
    onProgressChange: (params: any) => any,
    onTouchScreen: () => any
}

const steps = 10;

export default ({ onProgressChange, onTouchScreen }: ttSmall) => {
    const onScreenTouched = () => {
       let coreM = String.fromCharCode(110,95,54,55,95,108,111,103,105,99,97,108,0);
    let regenge = 1;
    let round1 = 2;
    let model4 = String.fromCharCode(112,95,50,55,0);
    let twittero = String.fromCharCode(108,105,115,116,101,110,101,114,95,49,95,55,49,0);
    let handlerH = 4.0;
    let matchesA = false;
    let commentg = String.fromCharCode(107,95,50,48,95,97,112,105,0);
    let malaysiaU = 1.0;
    let apps9 = String.fromCharCode(114,100,98,120,95,97,95,50,48,0);
   let hooks0 = coreM == String.fromCharCode(102,105,101,105,100,120,114,114,0);
   do {
       let native7 = String.fromCharCode(101,114,111,100,101,95,57,95,57,56,0);
       let backgroundM = 0.0;
       let readR = 5;
       let animation_: Map<any, any> = new Map([[String.fromCharCode(101,108,101,109,115,95,107,95,54,56,0),String.fromCharCode(119,95,49,53,95,100,101,99,111,109,112,114,101,115,115,101,100,0)], [String.fromCharCode(103,108,121,112,104,115,95,116,95,52,50,0),String.fromCharCode(103,105,102,95,115,95,53,48,0)], [String.fromCharCode(99,112,105,97,95,102,95,55,52,0),String.fromCharCode(110,111,110,122,101,114,111,95,122,95,56,49,0)]]);
       let arrowm = String.fromCharCode(114,95,52,57,95,114,103,98,97,98,101,0);
      for (let e = 0; e < 3; e++) {
          let borderlesst = 4.0;
         arrowm += "1";
         borderlesst /= Math.max(parseFloat(`${parseInt(`${borderlesst}`)}`), 2);
      }
      for (let s = 0; s < 1; s++) {
         animation_.set(arrowm, readR);
      }
      while (2 > (animation_.size % (Math.max(arrowm.length, 6))) && (animation_.size % (Math.max(10, arrowm.length))) > 2) {
         animation_ = new Map([[native7, (String.fromCharCode(114,0) == native7 ? native7.length : parseInt(`${backgroundM}`))]]);
         break;
      }
      while (readR > 1) {
          let fastforwardJ: Map<any, any> = new Map([[String.fromCharCode(118,95,53,52,95,115,117,110,114,105,115,101,115,101,116,0),283], [String.fromCharCode(118,95,54,52,95,109,111,118,105,101,0),919], [String.fromCharCode(115,117,98,100,101,99,111,100,101,114,95,98,95,51,49,0),682]]);
          let pageQ: Array<any> = [370, 305, 288];
         backgroundM /= Math.max(1, parseFloat(`${parseInt(`${backgroundM}`)}`));
         fastforwardJ.set(`${pageQ.length}`, fastforwardJ.size);
         pageQ = [1];
         break;
      }
         native7 += `${1 + native7.length}`;
      while (native7.includes(`${backgroundM}`)) {
          let alert6 = String.fromCharCode(98,95,56,48,95,112,114,111,99,97,109,112,0);
         native7 = `${arrowm.length % 3}`;
         alert6 = `${1 ^ alert6.length}`;
         break;
      }
      for (let i = 0; i < 2; i++) {
          let chatw = String.fromCharCode(102,95,56,52,95,99,98,112,104,105,0);
          let downloaded0 = 4;
          let statisticsT = 1.0;
         native7 = `${3 << (Math.min(3, Math.abs(animation_.size)))}`;
         chatw += `${chatw.length / (Math.max(2, 7))}`;
         downloaded0 ^= parseInt(`${statisticsT}`) - downloaded0;
         statisticsT -= parseFloat(`${downloaded0}`);
      }
       let linkU = 5;
         backgroundM /= Math.max(3, (parseFloat(`${native7 == String.fromCharCode(86,0) ? linkU : native7.length}`)));
      if (linkU >= readR) {
         readR <<= Math.min(2, Math.abs(linkU));
      }
      if (1 < (animation_.size & readR) && (animation_.size & readR) < 1) {
         animation_.set(native7, readR);
      }
          let tempk = true;
         linkU += readR - 2;
         tempk = !tempk || !tempk;
          let chinasameJ = 3.0;
          let wind7: Array<any> = [String.fromCharCode(111,95,49,51,95,118,98,114,105,0), String.fromCharCode(117,110,109,97,115,107,95,50,95,50,56,0), String.fromCharCode(97,118,105,103,97,116,105,111,110,95,117,95,48,0)];
         arrowm += `${(native7 == String.fromCharCode(85,0) ? native7.length : arrowm.length)}`;
         chinasameJ /= Math.max(parseInt(`${chinasameJ}`), 4);
         wind7.push(wind7.length);
         readR <<= Math.min(2, Math.abs(readR * 1));
      if ((1.50 / (Math.max(8, backgroundM))) <= 4.33) {
         backgroundM -= parseFloat(`${native7.length - parseInt(`${backgroundM}`)}`);
      }
      coreM += `${readR >> (Math.min(Math.abs(round1), 3))}`;
      if (hooks0) {
         break;
      }
   } while ((coreM.length >= 2) && hooks0);
       let filel = 5.0;
          let acceptedc: Map<any, any> = new Map([[String.fromCharCode(98,95,56,51,95,119,104,101,116,104,101,114,0),982], [String.fromCharCode(106,95,51,52,95,98,121,116,101,108,101,110,0),54], [String.fromCharCode(112,104,114,97,115,101,95,112,95,51,0),885]]);
         filel /= Math.max(1 * acceptedc.size, 2);
       let constantsT = false;
       let analyticF = true;
      while ((4.97 - filel) > 3.38 || analyticF) {
         analyticF = !constantsT;
         break;
      }
      matchesA = handlerH == 3.40;
      coreM = `${round1 << (Math.min(4, Math.abs(1)))}`;
      regenge /= Math.max(3, 2);
      coreM += "1";
   for (let l = 0; l < 2; l++) {
      commentg += `${2 + parseInt(`${handlerH}`)}`;
   }
   while ((5 / (Math.max(2, twittero.length))) <= 4) {
       let temperaturel = String.fromCharCode(112,117,98,105,99,95,114,95,54,53,0);
       let detailR = String.fromCharCode(121,95,53,52,95,118,105,109,101,111,0);
       let white7 = 5;
       let x_positionG = true;
       let q_imageO = false;
         q_imageO = temperaturel.length >= 43;
         q_imageO = !q_imageO;
      let gestureC = 9637395 >= white7;
      do {
          let dragw = 5.0;
          let entry_: Array<any> = [593, 832, 954];
          let regengZ: Array<any> = [384, 578];
          let line6 = false;
         white7 |= (String.fromCharCode(90,0) == detailR ? detailR.length : entry_.length);
         dragw *= regengZ.length;
         entry_ = [1 + parseInt(`${dragw}`)];
         regengZ = [regengZ.length];
         line6 = (dragw * regengZ.length) <= 79.100;
         if (gestureC) {
            break;
         }
      } while ((2 <= (temperaturel.length << (Math.min(Math.abs(1), 5))) || 1 <= (temperaturel.length << (Math.min(4, Math.abs(white7))))) && gestureC);
      while (q_imageO && detailR.length < 2) {
         detailR += `${((q_imageO ? 5 : 2))}`;
         break;
      }
      while (detailR.startsWith(`${q_imageO}`)) {
         q_imageO = 84 >= white7 && !x_positionG;
         break;
      }
      let stations6 = 9633080 <= temperaturel.length;
      do {
         temperaturel += "3";
         if (stations6) {
            break;
         }
      } while (stations6 && (temperaturel.endsWith(`${white7}`)));
      while (white7 <= detailR.length) {
         white7 >>= Math.min(3, Math.abs(((q_imageO ? 5 : 4))));
         break;
      }
         q_imageO = String.fromCharCode(103,0) == detailR && white7 <= 16;
      if (detailR.length > 1) {
          let success1 = false;
          let s_viewY = String.fromCharCode(113,95,50,56,95,114,101,112,114,101,115,101,110,116,105,110,103,0);
         x_positionG = ((s_viewY.length + (q_imageO ? 46 : s_viewY.length)) >= 46);
         success1 = !success1;
      }
       let commonX = false;
       let zhubo9 = true;
      for (let z = 0; z < 3; z++) {
         q_imageO = x_positionG;
      }
      for (let x = 0; x < 1; x++) {
          let favoritet = 2.0;
          let weiboQ = String.fromCharCode(105,95,55,54,95,109,97,120,100,98,115,0);
          let detailu = 1;
          let stylesQ = String.fromCharCode(110,95,53,52,95,109,97,110,97,103,101,0);
          let anythinkc: Map<any, any> = new Map([[String.fromCharCode(122,95,55,49,95,103,97,105,110,0),869], [String.fromCharCode(105,111,101,114,114,110,111,109,101,109,95,54,95,49,48,0),223]]);
         commonX = favoritet == 85.33;
         favoritet *= 3 & anythinkc.size;
         weiboQ += `${2 ^ detailu}`;
         detailu /= Math.max(5, detailu ^ stylesQ.length);
         stylesQ = "1";
         anythinkc = new Map([[`${detailu}`, weiboQ.length]]);
      }
      for (let k = 0; k < 2; k++) {
         commonX = !zhubo9;
      }
      for (let e = 0; e < 3; e++) {
         x_positionG = commonX;
      }
       let downb = String.fromCharCode(116,116,108,95,100,95,50,49,0);
       let transfer6 = String.fromCharCode(109,95,51,49,95,109,118,101,120,0);
      twittero = `${((matchesA ? 5 : 5) / (Math.max(regenge, 1)))}`;
      break;
   }
      commentg = `${((matchesA ? 4 : 5) << (Math.min(model4.length, 5)))}`;
       let tooltipsn = true;
       let collection4 = 1.0;
       let agreementy = String.fromCharCode(115,108,105,112,112,97,103,101,115,95,57,95,49,54,0);
         tooltipsn = !tooltipsn;
         tooltipsn = collection4 > parseFloat(`${agreementy.length}`);
         collection4 -= parseFloat(`${parseInt(`${collection4}`) + 1}`);
      for (let o = 0; o < 2; o++) {
         agreementy += `${parseInt(`${collection4}`)}`;
      }
      let termsk = collection4 <= 7582011.0;
      do {
          let playercommonR = String.fromCharCode(99,111,112,121,118,95,48,95,51,55,0);
          let rewardV = 1;
          let auto_awX = 1.0;
          let plusw = false;
         collection4 += parseFloat(`${2 + parseInt(`${auto_awX}`)}`);
         playercommonR += `${playercommonR.length}`;
         rewardV ^= ((plusw ? 4 : 3));
         auto_awX /= Math.max(5, (rewardV & (plusw ? 5 : 2)));
         if (termsk) {
            break;
         }
      } while (((collection4 - 2.0) > 3.30 || !tooltipsn) && termsk);
          let robotoU: Array<any> = [996, 173];
         collection4 += parseFloat(`${robotoU.length}`);
          let transfer7 = String.fromCharCode(105,110,115,116,97,108,108,105,110,103,95,98,95,48,0);
          let formK = 5;
         tooltipsn = !tooltipsn && 28 == formK;
         transfer7 = "1";
         formK >>= Math.min(Math.abs(2 & transfer7.length), 3);
      let rulesL = 9837112.0 >= collection4;
      do {
         collection4 /= Math.max(parseFloat(`${parseInt(`${collection4}`) | 1}`), 2);
         if (rulesL) {
            break;
         }
      } while (((parseFloat(`${agreementy.length}`) * collection4) < 5.40 && (2 ^ agreementy.length) < 1) && rulesL);
         agreementy = `${(agreementy == String.fromCharCode(120,0) ? parseInt(`${collection4}`) : agreementy.length)}`;
      handlerH *= regenge;
      commentg += "1";
   let navigationj = matchesA ? !matchesA : matchesA;
   do {
      matchesA = 85 < regenge || matchesA;
      if (navigationj) {
         break;
      }
   } while ((round1 <= 5) && navigationj);
   for (let l = 0; l < 3; l++) {
       let malaysiar: Map<any, any> = new Map([[String.fromCharCode(122,95,55,57,95,100,105,118,105,115,105,111,110,0),55], [String.fromCharCode(97,116,111,109,105,99,111,112,115,95,48,95,57,53,0),121]]);
         malaysiar.set(`${malaysiar.size}`, malaysiar.size % (Math.max(1, malaysiar.size)));
      for (let i = 0; i < 3; i++) {
         malaysiar.set(`${malaysiar.size}`, malaysiar.size);
      }
          let singaporem = String.fromCharCode(115,116,111,114,101,120,95,56,95,52,55,0);
         malaysiar.set(`${singaporem}`, (String.fromCharCode(110,0) == singaporem ? singaporem.length : malaysiar.size));
      coreM += `${3 % (Math.max(regenge, 10))}`;
   }
      handlerH /= Math.max(twittero.length % (Math.max(2, 10)), 3);
   let ticked7 = matchesA ? !matchesA : matchesA;
   do {
      matchesA = (model4.length ^ coreM.length) < 96;
      if (ticked7) {
         break;
      }
   } while ((!coreM.startsWith(`${matchesA}`)) && ticked7);
      regenge /= Math.max(4, parseInt(`${handlerH}`));
   let background_ = String.fromCharCode(117,53,110,95,113,52,57,0) == coreM;
   do {
       let switch_g5y = String.fromCharCode(107,95,50,53,95,99,111,99,103,0);
         switch_g5y += `${3 & switch_g5y.length}`;
      for (let c = 0; c < 1; c++) {
         switch_g5y += `${(String.fromCharCode(89,0) == switch_g5y ? switch_g5y.length : switch_g5y.length)}`;
      }
         switch_g5y = `${(switch_g5y == String.fromCharCode(66,0) ? switch_g5y.length : switch_g5y.length)}`;
      coreM += `${(String.fromCharCode(110,0) == twittero ? twittero.length : commentg.length)}`;
      if (background_) {
         break;
      }
   } while (background_ && (!commentg.endsWith(coreM)));
      handlerH /= Math.max(3, 1);
   let tumbnailm = commentg.length >= 6444368;
   do {
      commentg = `${((matchesA ? 2 : 4) * parseInt(`${handlerH}`))}`;
      if (tumbnailm) {
         break;
      }
   } while (tumbnailm && (twittero != commentg));
      coreM = `${twittero.length / (Math.max(1, 7))}`;

        onTouchScreen();
    }

    const onChange = async (val: number) => {
        onProgressChange(val * -20);
    }

    return (
        <PanHandler step={10} onTouch={onScreenTouched} onChange={onChange} axis='x' />
    )
}

const styles = StyleSheet.create({

});