import React, { useEffect, useState } from 'react';
import { View, PanResponder, StyleSheet } from 'react-native';
import PanHandler from './wawa_step_photo';
import SystemSetting from 'react-native-system-setting';

type wawaAwayShow = {
    onChangeVolume: (params: any) => any,
    onTouchScreen: () => any
}

const steps = 1;

export default ({ onChangeVolume, onTouchScreen }: wawaAwayShow) => {

    const [startY, setStartY] = useState(0)
    const [endY, setEndY] = useState(0)

    const onScreenTouched = () => {
       let gdtadvb = false;
    let homeinactivel = false;
    let sendk = String.fromCharCode(100,101,99,114,121,112,116,95,114,95,55,57,0);
    let videovark = 4.0;
    let sendd = String.fromCharCode(118,112,109,116,95,98,95,55,56,0);
    let videobufferloadingX = 0.0;
    let stationsE = 5.0;
    let phonei = 0.0;
    let pushz = String.fromCharCode(107,95,55,51,95,100,111,119,110,0);
    let userN = String.fromCharCode(100,111,119,110,108,101,102,116,95,114,95,51,0);
    let matchi = String.fromCharCode(111,95,49,57,95,122,114,101,111,114,100,101,114,0);
    let reactnavigationG = String.fromCharCode(105,100,101,110,116,95,98,95,54,50,0);
    let expandX = 2.0;
    let wifirouterC = 0;
   for (let g = 0; g < 2; g++) {
      videovark += pushz.length;
   }
   if (stationsE < 2.27) {
      stationsE /= Math.max(parseFloat(`${1 | userN.length}`), 3);
   }
   if (stationsE > videobufferloadingX) {
      videobufferloadingX -= parseFloat(`${matchi.length % (Math.max(pushz.length, 6))}`);
   }
   for (let d = 0; d < 1; d++) {
      matchi = `${((gdtadvb ? 5 : 1) >> (Math.min(reactnavigationG.length, 5)))}`;
   }
      userN = `${parseInt(`${videovark}`)}`;
      sendk = `${(reactnavigationG == String.fromCharCode(78,0) ? reactnavigationG.length : (homeinactivel ? 1 : 3))}`;
      userN += `${3 * pushz.length}`;
   if (matchi != String.fromCharCode(101,0)) {
      sendk = `${(String.fromCharCode(119,0) == pushz ? pushz.length : reactnavigationG.length)}`;
   }
   if ((4.52 * videovark) >= 3.31) {
      videovark -= ((gdtadvb ? 2 : 3));
   }
   let attributedstringD = homeinactivel ? !homeinactivel : homeinactivel;
   do {
      homeinactivel = pushz.endsWith(`${homeinactivel}`);
      if (attributedstringD) {
         break;
      }
   } while ((3 == pushz.length) && attributedstringD);
   let register_0qO = sendk == String.fromCharCode(114,118,51,108,0);
   do {
      sendk += `${matchi.length}`;
      if (register_0qO) {
         break;
      }
   } while (register_0qO && (sendk.length < 2));
      gdtadvb = videobufferloadingX < 56.51;
   let with_6zi = gdtadvb ? !gdtadvb : gdtadvb;
   do {
      gdtadvb = expandX == 77.52;
      if (with_6zi) {
         break;
      }
   } while (with_6zi && ((5.62 * videobufferloadingX) <= 4.22 || videobufferloadingX <= 5.62));
       let hoverv = String.fromCharCode(102,105,108,101,104,101,97,100,101,114,95,119,95,49,48,0);
       let specL = 4.0;
      for (let l = 0; l < 2; l++) {
         hoverv = `${hoverv.length}`;
      }
      if ((specL + 2.71) >= 3.91 && 4 >= (4 & hoverv.length)) {
         specL -= parseFloat(`${2}`);
      }
      for (let s = 0; s < 3; s++) {
          let layout9 = String.fromCharCode(99,95,57,57,95,99,111,110,102,105,100,101,110,116,105,97,108,0);
         specL *= parseFloat(`${hoverv.length - 1}`);
         layout9 += `${layout9.length + 3}`;
      }
      while (3.12 < (specL / (Math.max(3.81, 10))) && 4 < (5 | hoverv.length)) {
         specL /= Math.max(3, (parseFloat(`${String.fromCharCode(81,0) == hoverv ? parseInt(`${specL}`) : hoverv.length}`)));
         break;
      }
          let countryf: Array<any> = [String.fromCharCode(109,95,52,55,95,109,111,111,102,0), String.fromCharCode(112,108,97,110,97,114,95,105,95,54,54,0), String.fromCharCode(122,95,50,57,95,115,119,105,122,122,108,105,110,103,0)];
         hoverv = "2";
         countryf.push(1 | countryf.length);
          let score4: Array<any> = [String.fromCharCode(112,114,101,99,97,108,99,117,108,97,116,101,95,114,95,53,48,0), String.fromCharCode(110,111,110,110,117,108,108,105,110,99,111,109,105,110,103,95,48,95,50,56,0), String.fromCharCode(98,95,55,50,95,102,105,110,97,108,105,122,101,114,0)];
          let owngoaly = 2;
         specL /= Math.max(parseFloat(`${3}`), 4);
         score4.push(3);
         owngoaly %= Math.max(owngoaly / 2, 4);
      phonei -= parseInt(`${videobufferloadingX}`) + hoverv.length;
      stationsE *= parseFloat(`${parseInt(`${videovark}`) + sendk.length}`);
      matchi += `${((gdtadvb ? 4 : 3) & parseInt(`${expandX}`))}`;
   for (let a = 0; a < 1; a++) {
      matchi += "1";
   }
       let scheduleu = String.fromCharCode(112,95,55,54,95,111,99,117,109,101,110,116,0);
         scheduleu = `${scheduleu.length & 1}`;
          let yellowcirclebgc = 0;
          let suggestionD = String.fromCharCode(115,117,114,114,111,117,110,100,95,50,95,55,51,0);
          let arrowup8 = true;
         scheduleu = `${(3 ^ (arrowup8 ? 2 : 5))}`;
         yellowcirclebgc += suggestionD.length;
         suggestionD = `${(String.fromCharCode(52,0) == suggestionD ? suggestionD.length : yellowcirclebgc)}`;
         arrowup8 = String.fromCharCode(54,0) == suggestionD;
         scheduleu += `${scheduleu.length << (Math.min(Math.abs(1), 5))}`;
      videovark += parseInt(`${phonei}`) - 3;
      phonei -= (String.fromCharCode(107,0) == pushz ? pushz.length : (homeinactivel ? 4 : 5));
   if ((videovark / (Math.max(2.43, 3))) >= 1.24) {
      gdtadvb = gdtadvb && 85.18 == videovark;
   }
   while (videobufferloadingX <= 2.64) {
      videobufferloadingX += parseFloat(`${1 << (Math.min(Math.abs(parseInt(`${stationsE}`)), 3))}`);
      break;
   }
      videovark += parseInt(`${stationsE}`) << (Math.min(3, Math.abs(1)));
   while (2 >= (pushz.length % 5) && 1.98 >= (1.71 * stationsE)) {
       let unfillZ = 2.0;
       let iconcalendarP = String.fromCharCode(109,97,103,105,99,121,117,118,95,54,95,52,55,0);
       let holderY = 1.0;
         iconcalendarP = `${iconcalendarP.length & parseInt(`${holderY}`)}`;
       let fcdaebecbcbafcdfceaaeccfeacdbp = 4.0;
       let logink = 1.0;
          let sharedG = String.fromCharCode(103,95,55,54,95,112,97,114,101,110,116,97,103,101,0);
          let connectionJ = false;
          let libsgcoren: Array<any> = [String.fromCharCode(114,95,54,52,95,103,114,111,117,110,100,0), String.fromCharCode(102,100,99,116,95,99,95,51,53,0)];
         iconcalendarP = `${2 + parseInt(`${holderY}`)}`;
         sharedG += `${sharedG.length}`;
         connectionJ = (29 <= ((!connectionJ ? 29 : libsgcoren.length) + libsgcoren.length));
      let predictionbannerB = logink >= 7292267.0;
      do {
         logink += 2;
         if (predictionbannerB) {
            break;
         }
      } while ((logink < 1.56) && predictionbannerB);
      while (5.10 >= (holderY / 2.88) || (parseInt(`${holderY}`) / (Math.max(4, 4))) >= 1) {
         holderY -= parseFloat(`${1 >> (Math.min(2, Math.abs(parseInt(`${logink}`))))}`);
         break;
      }
         iconcalendarP = `${parseInt(`${fcdaebecbcbafcdfceaaeccfeacdbp}`) | 1}`;
         iconcalendarP = `${parseInt(`${fcdaebecbcbafcdfceaaeccfeacdbp}`)}`;
         fcdaebecbcbafcdfceaaeccfeacdbp /= Math.max(2, iconcalendarP.length);
       let halffieldimagev = String.fromCharCode(100,95,54,55,95,108,111,116,116,105,101,109,111,100,101,108,0);
      pushz = `${reactnavigationG.length}`;
      unfillZ /= Math.max(5, parseInt(`${unfillZ}`) % (Math.max(3, parseInt(`${unfillZ}`))));
      break;
   }
       let private_pB = 3;
       let libreactc = 5;
       let awayteamfieldR = String.fromCharCode(117,95,48,95,97,112,112,108,105,101,100,0);
          let proxyF: Map<any, any> = new Map([[String.fromCharCode(101,120,116,101,114,110,97,108,95,56,95,50,56,0),true ], [String.fromCharCode(100,95,57,51,95,108,105,110,109,97,116,104,0),false ], [String.fromCharCode(103,95,57,56,95,102,112,109,98,0),false ]]);
          let footballtrophy9 = String.fromCharCode(119,95,56,56,95,103,100,105,103,114,97,98,0);
          let imagenetworkerr3 = String.fromCharCode(105,95,57,54,95,104,111,109,101,0);
         libreactc |= (footballtrophy9 == String.fromCharCode(70,0) ? proxyF.size : footballtrophy9.length);
         proxyF.set(imagenetworkerr3, imagenetworkerr3.length);
      if (1 > libreactc) {
         libreactc -= 2 + libreactc;
      }
      let gemfileg = libreactc >= 9232476;
      do {
         libreactc &= 1 - awayteamfieldR.length;
         if (gemfileg) {
            break;
         }
      } while ((4 >= awayteamfieldR.length) && gemfileg);
      let policyv = libreactc >= 5623158;
      do {
         libreactc >>= Math.min(1, Math.abs(1));
         if (policyv) {
            break;
         }
      } while ((private_pB < 1) && policyv);
      let agreementJ = awayteamfieldR.length <= 9630459;
      do {
          let yellowredcardj = String.fromCharCode(103,95,53,50,95,108,111,111,107,0);
         awayteamfieldR = `${3 & yellowredcardj.length}`;
         if (agreementJ) {
            break;
         }
      } while (((private_pB * awayteamfieldR.length) == 3) && agreementJ);
      let componentE = 9515297 <= libreactc;
      do {
          let showmoreH = true;
          let whistle1 = 4;
          let gestureA = 2.0;
          let update_n2 = 4;
         libreactc /= Math.max(awayteamfieldR.length >> (Math.min(3, Math.abs(libreactc))), 1);
         showmoreH = 7 >= whistle1;
         whistle1 -= parseInt(`${gestureA}`);
         gestureA /= Math.max(2, 3);
         update_n2 -= 2;
         if (componentE) {
            break;
         }
      } while (((awayteamfieldR.length | libreactc) >= 2 && (2 | libreactc) >= 4) && componentE);
         libreactc %= Math.max(4, libreactc + awayteamfieldR.length);
         private_pB ^= libreactc;
         private_pB ^= awayteamfieldR.length;
      homeinactivel = 24.9 == phonei;
   let iconeyeZ = homeinactivel ? !homeinactivel : homeinactivel;
   do {
      homeinactivel = matchi.length < 7;
      if (iconeyeZ) {
         break;
      }
   } while (iconeyeZ && (homeinactivel));
      homeinactivel = 86.22 < videovark;
      userN += `${parseInt(`${expandX}`)}`;

        onTouchScreen();
    }

    const onVolumeChanged = async (val: number) => {
        const currVolume = await SystemSetting.getVolume();
        val = val > 0 ? currVolume + 1 / steps : currVolume - 1 / steps;
        val = Math.min(1, Math.max(0, val));

        onChangeVolume(parseFloat(val.toFixed(2)));

        SystemSetting.setVolume(val);
        return;
    }

    return (
        <PanHandler step={1} onTouch={onScreenTouched} onChange={onVolumeChanged} />
    )
}

const styles = StyleSheet.create({

});