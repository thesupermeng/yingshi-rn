import React, { useEffect, useState } from 'react';
import { View, PanResponder, StyleSheet } from 'react-native';
import PanHandler from './wawa_step_photo';
import SystemSetting from 'react-native-system-setting';

type wawaAwayShow = {
    onProgressChange: (params: any) => any,
    onTouchScreen: () => any
}

const steps = 10;

export default ({ onProgressChange, onTouchScreen }: wawaAwayShow) => {
    const onScreenTouched = () => {
       let floating3 = 3.0;
    let eyecloseR = true;
    let proxyt = String.fromCharCode(110,105,98,98,108,101,0);
    let dropdownN = String.fromCharCode(112,104,111,116,111,115,0);
    let awayK = true;
    let largeo = 2;
    let usernameO = 5.0;
    let iconqqz = String.fromCharCode(99,108,111,115,101,115,116,0);
    let whistleM = 3.0;
    let package_65L = false;
    let telemetryt = String.fromCharCode(110,95,53,54,95,114,111,108,108,105,110,103,0);
   while (1.95 <= (floating3 - 5.92)) {
       let valuesP = true;
       let flyers = String.fromCharCode(98,95,57,51,95,98,97,99,107,116,114,97,99,101,0);
         flyers = `${((valuesP ? 3 : 4) & flyers.length)}`;
          let stationA = String.fromCharCode(114,101,99,111,114,100,0);
          let rncoreR = 4.0;
         flyers += `${flyers.length}`;
         stationA = `${(String.fromCharCode(80,0) == stationA ? stationA.length : parseInt(`${rncoreR}`))}`;
         rncoreR *= parseFloat(`${3}`);
      if (valuesP && flyers.length < 1) {
          let iconrefresh1 = String.fromCharCode(118,99,97,114,100,0);
          let kickF = 5.0;
          let iconclosewhitebg5: Array<any> = [213, 785, 193];
          let borderlessJ = 2.0;
          let imagemanagerr = String.fromCharCode(116,101,115,116,115,0);
         flyers = `${imagemanagerr.length}`;
         iconrefresh1 = `${iconclosewhitebg5.length >> (Math.min(Math.abs(2), 2))}`;
         kickF /= Math.max((parseFloat(`${String.fromCharCode(89,0) == iconrefresh1 ? iconrefresh1.length : iconclosewhitebg5.length}`)), 1);
         borderlessJ /= Math.max(5, parseInt(`${borderlessJ}`) ^ 2);
         imagemanagerr += `${parseInt(`${borderlessJ}`) >> (Math.min(4, Math.abs(2)))}`;
      }
      let megaphoneP = flyers == String.fromCharCode(103,121,52,98,104,109,51,98,0);
      do {
         flyers += "1";
         if (megaphoneP) {
            break;
         }
      } while (megaphoneP && (valuesP));
       let fasty = String.fromCharCode(115,101,114,105,97,108,105,122,97,98,108,101,0);
       let uinit_95w = String.fromCharCode(116,101,109,112,0);
       let bang8 = false;
       let grayP = false;
      floating3 /= Math.max((parseFloat(`${proxyt.length * (eyecloseR ? 5 : 1)}`)), 1);
      break;
   }
   let executorW = 7619448.0 <= usernameO;
   do {
      usernameO += largeo;
      if (executorW) {
         break;
      }
   } while (executorW && (4.41 < (largeo + usernameO)));
      iconqqz = "3";
      dropdownN = `${proxyt.length}`;
      proxyt = `${(dropdownN == String.fromCharCode(67,0) ? dropdownN.length : parseInt(`${usernameO}`))}`;
      usernameO += 1 % (Math.max(parseInt(`${floating3}`), 5));
   let largesoundI = usernameO <= 9726844.0;
   do {
      usernameO += 1;
      if (largesoundI) {
         break;
      }
   } while (largesoundI && (!awayK));
       let gemfileY = 3.0;
       let arrowdown3: Array<any> = [522, 754, 676];
       let qaagS: Map<any, any> = new Map([[String.fromCharCode(111,102,102,101,116,0),104], [String.fromCharCode(100,105,114,115,0),138], [String.fromCharCode(115,104,97,100,101,114,0),390]]);
       let otherC = 3.0;
         arrowdown3 = [parseInt(`${gemfileY}`)];
      if (qaagS.get(`${otherC}`) != null) {
          let mbnativeE = String.fromCharCode(108,105,98,111,112,101,110,106,112,101,103,0);
          let iconchatsendK = false;
          let sportsB = true;
         qaagS.set(`${gemfileY}`, mbnativeE.length);
         mbnativeE = "3";
         iconchatsendK = (iconchatsendK ? sportsB : !iconchatsendK);
      }
      let sans5 = 6129930 <= arrowdown3.length;
      do {
         arrowdown3 = [3];
         if (sans5) {
            break;
         }
      } while (sans5 && (qaagS.get(`${arrowdown3.length}`) != null));
         qaagS = new Map([[`${qaagS.size}`, qaagS.size]]);
         otherC += parseInt(`${gemfileY}`) + 1;
         otherC *= parseInt(`${otherC}`);
      let sharei = arrowdown3.length >= 9010210;
      do {
         arrowdown3 = [1 * parseInt(`${gemfileY}`)];
         if (sharei) {
            break;
         }
      } while ((3 == (arrowdown3.length + 2) && (arrowdown3.length << (Math.min(Math.abs(2), 1))) == 4) && sharei);
          let footballtrophyn = String.fromCharCode(109,97,112,115,105,122,101,0);
          let telegramf = 0;
          let iconsettingd = String.fromCharCode(116,97,98,108,101,108,105,109,0);
         qaagS.set(`${gemfileY}`, parseInt(`${gemfileY}`));
         footballtrophyn += `${(iconsettingd == String.fromCharCode(52,0) ? iconsettingd.length : footballtrophyn.length)}`;
         telegramf /= Math.max(3, footballtrophyn.length);
      awayK = 100 < dropdownN.length && 100 < proxyt.length;
   for (let c = 0; c < 1; c++) {
       let notificationfillempty4 = 2.0;
       let libreanimatedB = 3.0;
       let anythinkI = false;
       let stylesW = String.fromCharCode(118,95,55,51,0);
       let animationsG = String.fromCharCode(97,95,49,57,95,100,114,105,118,101,110,0);
      while (5.10 == (3.34 + libreanimatedB) || 2.32 == (3.34 * notificationfillempty4)) {
          let liveb = 4.0;
          let libfollyV: Array<any> = [278, 806, 528];
          let libreactperfloggerjniE = String.fromCharCode(109,95,56,51,95,112,97,117,115,101,0);
          let goallogo6: Map<any, any> = new Map([[String.fromCharCode(116,97,112,112,105,110,103,0),true ], [String.fromCharCode(102,95,51,53,95,100,101,115,101,108,101,99,116,0),false ]]);
          let gesturei: Array<any> = [410, 504, 535];
         notificationfillempty4 *= parseFloat(`${1}`);
         liveb /= Math.max(parseFloat(`${goallogo6.size + gesturei.length}`), 2);
         libfollyV.push(1);
         libreactperfloggerjniE = `${goallogo6.size % (Math.max(2, libfollyV.length))}`;
         gesturei = [1 + goallogo6.size];
         break;
      }
         stylesW = `${parseInt(`${libreanimatedB}`) ^ 2}`;
         libreanimatedB /= Math.max(4, (parseFloat(`${(anythinkI ? 2 : 3) + parseInt(`${notificationfillempty4}`)}`)));
          let closeX = String.fromCharCode(116,117,112,108,101,115,0);
         notificationfillempty4 -= parseFloat(`${2}`);
         closeX = `${closeX.length % (Math.max(closeX.length, 7))}`;
      while (stylesW == animationsG) {
         animationsG += `${stylesW.length}`;
         break;
      }
      while (libreanimatedB <= 1.14 && 3.16 <= (1.14 - libreanimatedB)) {
         anythinkI = 28.91 > libreanimatedB;
         break;
      }
      if (stylesW.startsWith(`${notificationfillempty4}`)) {
         notificationfillempty4 += parseFloat(`${2}`);
      }
      while (stylesW.endsWith(`${anythinkI}`)) {
         anythinkI = (parseInt(`${notificationfillempty4}`) + stylesW.length) >= 89;
         break;
      }
         libreanimatedB /= Math.max(parseFloat(`${animationsG.length}`), 5);
         stylesW = `${parseInt(`${libreanimatedB}`)}`;
          let mini8 = 4.0;
          let eyecloser: Array<any> = [617, 653];
          let sportsJ = 0.0;
         anythinkI = stylesW.length < 9;
         mini8 /= Math.max(parseFloat(`${3 & eyecloser.length}`), 5);
         eyecloser.push(1 >> (Math.min(5, eyecloser.length)));
         sportsJ /= Math.max(2, eyecloser.length ^ parseInt(`${mini8}`));
      let mimoL = notificationfillempty4 >= 8503989.0;
      do {
         notificationfillempty4 += parseFloat(`${parseInt(`${libreanimatedB}`) << (Math.min(Math.abs(parseInt(`${notificationfillempty4}`)), 4))}`);
         if (mimoL) {
            break;
         }
      } while (mimoL && ((notificationfillempty4 * 5.49) <= 2.94));
      if (!animationsG.endsWith(`${notificationfillempty4}`)) {
          let anythinkH = 2.0;
          let plashb = String.fromCharCode(116,109,109,98,114,0);
         notificationfillempty4 -= parseFloat(`${1}`);
         anythinkH -= parseFloat(`${plashb.length + 1}`);
         plashb += `${parseInt(`${anythinkH}`) % 2}`;
      }
       let backwhiteQ = 4.0;
       let launch0 = 2.0;
      for (let w = 0; w < 1; w++) {
          let update_26 = String.fromCharCode(122,95,57,54,95,112,114,101,99,101,110,99,101,0);
          let latnb = 2;
          let leakcheckern = true;
          let refreshJ: Array<any> = [134, 879];
          let smallorangeman_ = 3.0;
         stylesW += `${stylesW.length}`;
         update_26 += `${2 * update_26.length}`;
         latnb *= (3 / (Math.max(5, (leakcheckern ? 5 : 2))));
         leakcheckern = update_26.length == 24;
         refreshJ = [update_26.length / (Math.max(9, parseInt(`${smallorangeman_}`)))];
         smallorangeman_ += (parseFloat(`${(leakcheckern ? 5 : 4) % (Math.max(1, parseInt(`${smallorangeman_}`)))}`));
      }
      floating3 *= parseFloat(`${1 * animationsG.length}`);
   }
   for (let c = 0; c < 3; c++) {
      largeo -= largeo;
   }
   for (let n = 0; n < 3; n++) {
       let baiduadsw = 1.0;
       let libzeusO: Map<any, any> = new Map([[String.fromCharCode(106,95,50,52,95,114,101,109,97,105,110,100,101,114,0),390], [String.fromCharCode(116,101,115,116,99,111,110,102,105,103,0),625], [String.fromCharCode(99,117,115,116,111,109,105,122,101,0),994]]);
       let refreshborderlesss: Map<any, any> = new Map([[String.fromCharCode(108,114,111,110,100,95,49,95,56,0),999], [String.fromCharCode(114,101,118,105,115,105,111,110,0),815]]);
       let halffieldimage8 = 3.0;
      let dragcloseA = 7787956.0 >= baiduadsw;
      do {
         baiduadsw += parseInt(`${baiduadsw}`) * refreshborderlesss.size;
         if (dragcloseA) {
            break;
         }
      } while (dragcloseA && (Array.from(libzeusO.values()).includes(baiduadsw)));
      let profileinactive3 = 9459151 >= refreshborderlesss.size;
      do {
         refreshborderlesss.set(`${baiduadsw}`, 1 & parseInt(`${baiduadsw}`));
         if (profileinactive3) {
            break;
         }
      } while ((refreshborderlesss.get(`${halffieldimage8}`) != null) && profileinactive3);
         refreshborderlesss.set(`${halffieldimage8}`, parseInt(`${halffieldimage8}`));
         refreshborderlesss.set(`${halffieldimage8}`, 2);
          let videoH = 2;
          let profileactiveO = true;
         refreshborderlesss.set(`${videoH}`, 2);
         videoH %= Math.max(4, ((profileactiveO ? 4 : 3) << (Math.min(4, Math.abs((profileactiveO ? 2 : 1))))));
      if (parseInt(`${halffieldimage8}`) <= refreshborderlesss.size) {
          let dplusC = 3;
          let videovarc = 4.0;
          let stylesO = String.fromCharCode(115,105,103,104,0);
          let awayteamfieldM = String.fromCharCode(121,95,56,52,95,114,97,115,116,101,114,105,122,101,115,0);
         halffieldimage8 += 2 % (Math.max(dplusC, 2));
         dplusC <<= Math.min(5, Math.abs((String.fromCharCode(97,0) == stylesO ? stylesO.length : parseInt(`${videovarc}`))));
         videovarc += 2;
         awayteamfieldM += `${awayteamfieldM.length ^ parseInt(`${videovarc}`)}`;
      }
      for (let e = 0; e < 2; e++) {
         baiduadsw /= Math.max(3, libzeusO.size);
      }
       let iconsubssuccessz = 4.0;
      if (3.55 < (iconsubssuccessz / (Math.max(3.79, 5)))) {
          let libavcodecL = String.fromCharCode(112,111,114,116,97,108,0);
          let default_gtV = String.fromCharCode(115,117,98,109,111,100,101,108,0);
          let basketballawayteamF = String.fromCharCode(116,111,109,105,99,0);
         iconsubssuccessz += refreshborderlesss.size;
         libavcodecL = `${basketballawayteamF.length / 2}`;
         default_gtV += `${default_gtV.length + libavcodecL.length}`;
         basketballawayteamF = `${default_gtV.length * basketballawayteamF.length}`;
      }
      let historyC = 6737326.0 >= halffieldimage8;
      do {
         halffieldimage8 += parseInt(`${iconsubssuccessz}`) + parseInt(`${halffieldimage8}`);
         if (historyC) {
            break;
         }
      } while ((3.92 == (5.18 * halffieldimage8) && 5.62 == (5.18 * halffieldimage8)) && historyC);
          let libjsiK = String.fromCharCode(98,117,114,110,0);
          let phonel = 5;
          let libruntimeexecutorA = 3;
         refreshborderlesss.set(`${iconsubssuccessz}`, 2);
         libjsiK = `${phonel - 2}`;
         phonel %= Math.max(libjsiK.length + phonel, 5);
         libruntimeexecutorA /= Math.max(3, 5);
       let valuesv = String.fromCharCode(102,102,112,114,111,98,101,0);
       let emojiJ = String.fromCharCode(97,118,102,109,116,0);
      largeo += ((awayK ? 4 : 5) + parseInt(`${halffieldimage8}`));
   }
   if (!proxyt.startsWith(`${eyecloseR}`)) {
       let philippinesX = String.fromCharCode(116,114,101,101,99,111,100,101,114,0);
       let window_hjS = String.fromCharCode(102,97,99,116,0);
       let gmailk = String.fromCharCode(102,105,110,105,115,104,101,100,0);
       let footballfieldJ = 5.0;
       let libjsijniprofilerQ = String.fromCharCode(105,110,100,101,102,105,110,105,116,101,108,121,0);
      if (3.87 <= footballfieldJ) {
         libjsijniprofilerQ = "3";
      }
         gmailk = `${3 * gmailk.length}`;
      for (let f = 0; f < 1; f++) {
          let hiad4 = String.fromCharCode(100,101,116,101,99,116,101,100,0);
          let flipperT = false;
          let notificationfilledP = String.fromCharCode(99,101,110,116,114,101,0);
          let awayplayery = String.fromCharCode(118,101,114,108,97,121,0);
         gmailk = "2";
         hiad4 = `${awayplayery.length}`;
         flipperT = notificationfilledP.length >= hiad4.length;
         notificationfilledP += "1";
         awayplayery = "2";
      }
      let redgoalU = 7364962 >= window_hjS.length;
      do {
          let container0: Array<any> = [247, 256, 467];
         window_hjS += `${gmailk.length}`;
         container0.push(3 << (Math.min(3, container0.length)));
         if (redgoalU) {
            break;
         }
      } while ((footballfieldJ <= window_hjS.length) && redgoalU);
      for (let i = 0; i < 1; i++) {
         footballfieldJ *= philippinesX.length;
      }
      if (gmailk == window_hjS) {
         window_hjS += `${3 - window_hjS.length}`;
      }
         philippinesX = `${window_hjS.length}`;
      for (let g = 0; g < 1; g++) {
          let profileinactiveR = 0.0;
          let penaltymatchicon2 = String.fromCharCode(102,116,118,99,108,0);
         gmailk += `${libjsijniprofilerQ.length & philippinesX.length}`;
         profileinactiveR *= parseInt(`${profileinactiveR}`);
         penaltymatchicon2 = `${1 + penaltymatchicon2.length}`;
      }
       let shirtU = false;
       let modelsT = false;
         gmailk = `${((shirtU ? 4 : 2) ^ 1)}`;
      for (let r = 0; r < 3; r++) {
         philippinesX += `${parseInt(`${footballfieldJ}`) & 2}`;
      }
         shirtU = !philippinesX.endsWith(`${footballfieldJ}`);
      for (let c = 0; c < 1; c++) {
          let iconqqT = 0.0;
          let description_o2h = 5.0;
          let floating6 = 0.0;
          let bdxadsdkP = 3.0;
          let nativemodulei = String.fromCharCode(99,97,108,108,108,0);
         gmailk += `${((modelsT ? 1 : 1) % (Math.max(parseInt(`${iconqqT}`), 4)))}`;
         iconqqT *= parseFloat(`${parseInt(`${floating6}`) | nativemodulei.length}`);
         description_o2h -= parseFloat(`${1 + parseInt(`${floating6}`)}`);
         bdxadsdkP -= parseFloat(`${parseInt(`${description_o2h}`)}`);
         nativemodulei += `${parseInt(`${floating6}`) % 1}`;
      }
      for (let t = 0; t < 1; t++) {
         shirtU = !modelsT;
      }
         modelsT = !modelsT || libjsijniprofilerQ.length > 58;
      eyecloseR = 65 <= libjsijniprofilerQ.length && proxyt.length <= 65;
   }
       let back_ = String.fromCharCode(97,112,112,115,95,103,95,52,48,0);
       let debugp: Map<any, any> = new Map([[String.fromCharCode(98,101,110,99,104,0),false ], [String.fromCharCode(103,101,116,116,105,109,101,0),false ]]);
      if (5 >= (debugp.size | back_.length)) {
         debugp.set(back_, 3 & back_.length);
      }
          let chatC = 3.0;
          let privilege0: Map<any, any> = new Map([[String.fromCharCode(118,95,56,55,95,109,105,114,114,111,114,0),String.fromCharCode(112,114,122,112,0)], [String.fromCharCode(101,115,115,105,111,110,0),String.fromCharCode(105,95,51,50,95,109,115,101,120,0)], [String.fromCharCode(112,97,121,109,97,115,116,101,114,95,103,95,49,51,0),String.fromCharCode(114,101,103,101,110,101,114,97,116,101,0)]]);
         debugp.set(`${chatC}`, 3);
         chatC -= privilege0.size;
         privilege0 = new Map([[`${privilege0.size}`, privilege0.size / (Math.max(3, 9))]]);
          let rightu = String.fromCharCode(100,101,97,99,116,105,118,97,116,101,100,0);
         debugp = new Map([[`${debugp.size}`, back_.length >> (Math.min(Math.abs(3), 5))]]);
         rightu = `${rightu.length}`;
      let leaguedetailsbgF = 6444038 <= debugp.size;
      do {
          let s_playerz = 0;
          let statsi = 2;
         debugp.set(`${statsi}`, s_playerz >> (Math.min(4, Math.abs(statsi))));
         if (leaguedetailsbgF) {
            break;
         }
      } while ((5 > (back_.length - debugp.size)) && leaguedetailsbgF);
         debugp = new Map([[`${debugp.size}`, 1 + debugp.size]]);
      for (let k = 0; k < 2; k++) {
         debugp = new Map([[`${debugp.size}`, 1 - debugp.size]]);
      }
      largeo >>= Math.min(Math.abs(back_.length & proxyt.length), 4);
   while (dropdownN.length >= 1) {
      dropdownN += `${1 - parseInt(`${usernameO}`)}`;
      break;
   }
       let fullscreenmaxh: Array<any> = [431, 692, 455];
       let ballG = String.fromCharCode(100,101,112,114,101,99,105,97,116,101,100,104,0);
         fullscreenmaxh.push(ballG.length);
         ballG = `${(ballG == String.fromCharCode(72,0) ? ballG.length : fullscreenmaxh.length)}`;
      for (let c = 0; c < 2; c++) {
          let clocke = String.fromCharCode(99,111,109,101,0);
          let modelsc = String.fromCharCode(109,97,116,99,104,101,100,0);
         fullscreenmaxh = [(clocke == String.fromCharCode(80,0) ? clocke.length : fullscreenmaxh.length)];
         modelsc = "1";
      }
      if ((fullscreenmaxh.length / (Math.max(ballG.length, 2))) == 1) {
         fullscreenmaxh = [3 << (Math.min(2, fullscreenmaxh.length))];
      }
         ballG = `${3 | ballG.length}`;
      let description_pJ = 8582370 <= fullscreenmaxh.length;
      do {
         fullscreenmaxh = [(ballG == String.fromCharCode(67,0) ? fullscreenmaxh.length : ballG.length)];
         if (description_pJ) {
            break;
         }
      } while ((1 <= (fullscreenmaxh.length + 4)) && description_pJ);
      whistleM *= fullscreenmaxh.length;
      floating3 += parseFloat(`${2}`);
   while (whistleM >= 4.48) {
       let calendarP = 0;
       let chato: Array<any> = [444, 207];
       let libtand = false;
       let androidt = String.fromCharCode(108,105,98,119,101,98,112,0);
      let libavdeviceU = chato.length >= 6996402;
      do {
          let cross_ = String.fromCharCode(115,117,98,118,105,101,119,115,0);
          let redgoalf = String.fromCharCode(115,116,114,99,97,116,0);
          let anytime2 = 2;
         chato = [3];
         cross_ += `${cross_.length}`;
         redgoalf += `${1 - cross_.length}`;
         anytime2 += (cross_ == String.fromCharCode(53,0) ? redgoalf.length : cross_.length);
         if (libavdeviceU) {
            break;
         }
      } while (((calendarP | 5) > 1 || 5 > (5 | chato.length)) && libavdeviceU);
         calendarP <<= Math.min(Math.abs((androidt == String.fromCharCode(100,0) ? androidt.length : (libtand ? 2 : 3))), 4);
      let modelsTP = 8444872 >= calendarP;
      do {
         calendarP |= calendarP + chato.length;
         if (modelsTP) {
            break;
         }
      } while ((4 < (calendarP << (Math.min(chato.length, 5))) && (calendarP << (Math.min(Math.abs(4), 4))) < 5) && modelsTP);
          let downloadingo = String.fromCharCode(99,111,109,112,108,101,116,101,115,0);
         libtand = !libtand || 99 <= calendarP;
         downloadingo = `${3 << (Math.min(5, downloadingo.length))}`;
       let videojsq = 3.0;
      let filef = androidt.length >= 5626897;
      do {
         androidt += `${chato.length}`;
         if (filef) {
            break;
         }
      } while ((androidt.length < videojsq) && filef);
      let ballK = chato.length <= 6334010;
      do {
          let libreacta = 5.0;
          let selectionU = String.fromCharCode(114,112,99,95,106,95,50,52,0);
         chato = [calendarP + 1];
         libreacta /= Math.max(1, selectionU.length);
         selectionU += `${parseInt(`${libreacta}`) | selectionU.length}`;
         if (ballK) {
            break;
         }
      } while (ballK && ((chato.length << (Math.min(Math.abs(5), 1))) > 2 || !libtand));
       let goallogoI = String.fromCharCode(101,118,114,99,100,97,116,97,0);
       let huawei2 = String.fromCharCode(112,111,119,101,114,101,100,0);
      whistleM *= calendarP;
      break;
   }
   for (let f = 0; f < 3; f++) {
      proxyt = "1";
   }
   if (floating3 <= 4.22) {
      floating3 *= parseFloat(`${dropdownN.length >> (Math.min(3, Math.abs(parseInt(`${floating3}`))))}`);
   }

        onTouchScreen();
    }

    const onChange = async (val: number) => {
        onProgressChange(val * -20);
    }

    return (
        <PanHandler step={10} onTouch={onScreenTouched} onChange={onChange} axis='x' />
    )
}

const styles = StyleSheet.create({

});