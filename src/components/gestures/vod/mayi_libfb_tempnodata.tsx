import React, { useEffect, useState } from 'react';
import { View, PanResponder, StyleSheet } from 'react-native';
import PanHandler from './mayi_match';
import SystemSetting from 'react-native-system-setting';

type mayi_GoogleViews = {
    onChangeBrightness: (params: any) => any,
    onTouchScreen: () => any
}

const steps = 1

export default ({ onChangeBrightness, onTouchScreen }: mayi_GoogleViews) => {

    const [startY, setStartY] = useState(0)
    const [endY, setEndY] = useState(0)

    const onScreenTouched = () => {
       let libswscaleq = 0.0;
    let sharedU = 1.0;
    let drag4: Array<any> = [952, 786, 400];
    let p_lockj = String.fromCharCode(116,114,105,109,109,105,110,103,95,50,95,52,51,0);
    let statisticsy = 1.0;
    let borderlessh = String.fromCharCode(115,116,100,111,117,116,95,106,95,53,51,0);
    let event1 = 4.0;
    let robotoH = 0.0;
    let larger = false;
    let codegenZ = String.fromCharCode(98,95,51,95,108,111,116,116,105,101,105,116,101,109,0);
    let iconviewergifm = 4.0;
    let downloadingw = String.fromCharCode(122,95,49,52,95,115,117,98,0);
    let mapbufferT = 3.0;
    let penaltymatchiconc = String.fromCharCode(110,97,117,116,105,99,97,108,95,110,95,52,55,0);
       let inewarchdefaultsc = 4.0;
       let dataW = 1;
       let vignetteL = String.fromCharCode(115,95,57,51,95,102,114,97,109,101,112,111,111,108,0);
         dataW /= Math.max(dataW & 3, 2);
      if ((dataW + 4) <= 4) {
          let s_player7 = String.fromCharCode(121,95,57,51,95,115,110,97,112,104,111,116,0);
          let rocketJ = 0;
          let gifgoalbgo: Map<any, any> = new Map([[String.fromCharCode(97,118,114,101,115,97,109,112,108,101,114,101,115,95,56,95,52,57,0),152], [String.fromCharCode(112,95,52,57,95,115,97,116,100,120,0),707]]);
         dataW %= Math.max(2, rocketJ);
         s_player7 = `${(String.fromCharCode(56,0) == s_player7 ? s_player7.length : gifgoalbgo.size)}`;
         rocketJ %= Math.max(s_player7.length, 1);
         gifgoalbgo = new Map([[`${gifgoalbgo.size}`, (s_player7 == String.fromCharCode(49,0) ? s_player7.length : gifgoalbgo.size)]]);
      }
         vignetteL += `${(vignetteL == String.fromCharCode(49,0) ? vignetteL.length : parseInt(`${inewarchdefaultsc}`))}`;
         vignetteL += `${dataW}`;
      while (1 == (vignetteL.length | dataW) && (dataW | 1) == 4) {
         dataW -= dataW * 1;
         break;
      }
      if ((dataW - vignetteL.length) == 4) {
         dataW -= vignetteL.length;
      }
      for (let x = 0; x < 1; x++) {
          let baselinet = 1.0;
          let sport7 = 0.0;
          let predictionbannerg = String.fromCharCode(111,95,51,48,95,115,101,116,116,101,114,0);
          let index1 = String.fromCharCode(100,95,49,57,95,102,111,114,109,97,110,116,0);
         vignetteL = `${predictionbannerg.length + index1.length}`;
         baselinet += parseInt(`${sport7}`) & parseInt(`${baselinet}`);
         sport7 -= parseInt(`${baselinet}`) ^ 2;
         predictionbannerg = `${parseInt(`${sport7}`)}`;
         index1 = `${2 / (Math.max(parseInt(`${baselinet}`), 5))}`;
      }
         vignetteL += "3";
      while (5 > (dataW - parseInt(`${inewarchdefaultsc}`)) && (inewarchdefaultsc - 4.21) > 1.98) {
         inewarchdefaultsc *= 2 >> (Math.min(Math.abs(dataW), 5));
         break;
      }
      codegenZ = `${p_lockj.length >> (Math.min(Math.abs(1), 3))}`;
      event1 -= ((larger ? 1 : 3) / (Math.max(4, parseInt(`${event1}`))));
   for (let t = 0; t < 1; t++) {
      statisticsy /= Math.max(3, (String.fromCharCode(102,0) == borderlessh ? borderlessh.length : parseInt(`${statisticsy}`)));
   }
      sharedU += 3;
   while (3 > (drag4.length * parseInt(`${event1}`))) {
       let episodeD = String.fromCharCode(103,101,116,115,105,103,99,116,120,110,111,95,101,95,53,49,0);
          let confirmationN: Map<any, any> = new Map([[String.fromCharCode(105,95,57,56,95,97,117,116,104,111,114,105,122,101,114,0),true ], [String.fromCharCode(109,97,107,101,115,114,112,109,95,107,95,54,54,0),true ]]);
          let mbsplashA = 4.0;
         episodeD = `${confirmationN.size << (Math.min(Math.abs(3), 1))}`;
         confirmationN.set(`${mbsplashA}`, parseInt(`${mbsplashA}`));
      let coded = String.fromCharCode(102,109,118,97,0) == episodeD;
      do {
          let register_9xa = 2.0;
          let analytic4 = 2;
          let predictionL: Map<any, any> = new Map([[String.fromCharCode(116,104,114,101,97,100,110,97,109,101,95,57,95,55,56,0),117], [String.fromCharCode(102,95,55,50,95,115,101,116,117,112,100,0),493], [String.fromCharCode(103,101,116,120,118,97,114,95,55,95,57,57,0),302]]);
         episodeD += `${2 ^ predictionL.size}`;
         register_9xa /= Math.max(analytic4, 1);
         analytic4 %= Math.max(analytic4, 1);
         predictionL = new Map([[`${analytic4}`, analytic4 << (Math.min(Math.abs(parseInt(`${register_9xa}`)), 1))]]);
         if (coded) {
            break;
         }
      } while (coded && (episodeD != episodeD));
          let yellowanimationlive2 = false;
          let iconC = String.fromCharCode(116,95,54,51,95,112,105,112,0);
         episodeD = `${((yellowanimationlive2 ? 1 : 3) ^ 3)}`;
         yellowanimationlive2 = 64 < iconC.length;
         iconC += `${iconC.length}`;
      drag4.push(drag4.length - 2);
      break;
   }
   while (2.73 > (4.88 * event1) || 3 > (p_lockj.length * 5)) {
      p_lockj += `${(parseInt(`${event1}`) - (larger ? 2 : 5))}`;
      break;
   }
      robotoH /= Math.max(1, parseFloat(`${parseInt(`${statisticsy}`)}`));
      p_lockj = `${((larger ? 2 : 2) - parseInt(`${libswscaleq}`))}`;
   if (p_lockj.includes(`${drag4.length}`)) {
      drag4 = [parseInt(`${libswscaleq}`)];
   }
       let leaguedetailsbgQ = 5.0;
       let imagenetworkerrj = String.fromCharCode(114,95,55,49,95,115,105,120,0);
       let arrowupZ = 5.0;
         arrowupZ *= imagenetworkerrj.length - parseInt(`${leaguedetailsbgQ}`);
      if (arrowupZ == leaguedetailsbgQ) {
          let twitterv: Array<any> = [197, 930, 508];
          let iconsharefriendsI = 3.0;
         arrowupZ *= twitterv.length;
         twitterv = [parseInt(`${iconsharefriendsI}`) & 3];
         iconsharefriendsI *= parseInt(`${iconsharefriendsI}`) & parseInt(`${iconsharefriendsI}`);
      }
         leaguedetailsbgQ -= parseInt(`${leaguedetailsbgQ}`) >> (Math.min(imagenetworkerrj.length, 3));
       let foundc = String.fromCharCode(101,99,100,115,97,95,109,95,53,50,0);
      if (!foundc.includes(`${arrowupZ}`)) {
         foundc = `${(foundc == String.fromCharCode(70,0) ? parseInt(`${arrowupZ}`) : foundc.length)}`;
      }
      let eventK = arrowupZ >= 5989341.0;
      do {
         arrowupZ += 1 * parseInt(`${leaguedetailsbgQ}`);
         if (eventK) {
            break;
         }
      } while (((5.50 + arrowupZ) > 2.15) && eventK);
         arrowupZ *= parseInt(`${leaguedetailsbgQ}`);
         imagenetworkerrj += `${parseInt(`${leaguedetailsbgQ}`) >> (Math.min(1, Math.abs(1)))}`;
      let detailF = foundc.length <= 6428654;
      do {
          let stationm = 4.0;
          let shielddonew = 0;
         foundc = `${(foundc == String.fromCharCode(67,0) ? foundc.length : parseInt(`${stationm}`))}`;
         stationm /= Math.max(5, parseFloat(`${shielddonew << (Math.min(Math.abs(2), 4))}`));
         shielddonew /= Math.max(2, 1);
         if (detailF) {
            break;
         }
      } while ((foundc.endsWith(imagenetworkerrj)) && detailF);
      p_lockj += `${imagenetworkerrj.length}`;
   for (let f = 0; f < 1; f++) {
       let iconarrowrightwhiteO = false;
       let encryptj = 5.0;
       let rewardvideoy: Array<any> = [String.fromCharCode(114,111,116,97,116,105,111,110,97,110,103,108,101,95,98,95,52,52,0), String.fromCharCode(117,95,53,50,95,114,101,115,116,114,105,99,116,105,111,110,0)];
       let submitq = String.fromCharCode(113,95,49,50,95,108,115,112,114,0);
       let nbatrophyf = String.fromCharCode(113,95,53,50,95,115,112,97,99,101,100,0);
      while (!rewardvideoy.includes(encryptj)) {
         rewardvideoy.push(2);
         break;
      }
         iconarrowrightwhiteO = rewardvideoy.length < 100;
      for (let k = 0; k < 2; k++) {
          let rewardR = String.fromCharCode(113,95,56,57,95,116,119,105,100,100,108,101,0);
         submitq += "1";
         rewardR += `${(String.fromCharCode(84,0) == rewardR ? rewardR.length : rewardR.length)}`;
      }
          let kuaishouE = String.fromCharCode(99,111,110,99,117,114,101,110,116,95,48,95,57,0);
         iconarrowrightwhiteO = encryptj == 74.81;
         kuaishouE += "2";
      let a_playerK = submitq == String.fromCharCode(110,114,104,115,109,118,0);
      do {
         submitq += `${parseInt(`${encryptj}`) / 3}`;
         if (a_playerK) {
            break;
         }
      } while ((5 >= (rewardvideoy.length ^ submitq.length) && (rewardvideoy.length ^ 5) >= 3) && a_playerK);
         submitq = `${submitq.length ^ nbatrophyf.length}`;
       let delegate_7j = 0.0;
       let libreactperfloggerjniU = String.fromCharCode(97,95,54,51,95,99,111,109,109,105,116,116,101,114,0);
       let placementC = String.fromCharCode(113,95,53,53,95,110,98,105,116,115,0);
         iconarrowrightwhiteO = 82 == nbatrophyf.length;
      let score3 = String.fromCharCode(53,56,54,106,103,0) == libreactperfloggerjniU;
      do {
          let matchdetailbgx: Array<any> = [696, 164, 833];
          let flipperl: Map<any, any> = new Map([[String.fromCharCode(104,111,115,116,95,122,95,49,55,0),false ], [String.fromCharCode(97,116,111,105,95,100,95,52,49,0),false ], [String.fromCharCode(99,95,50,56,95,111,98,116,97,105,110,101,114,0),false ]]);
          let imagenomoredatay = String.fromCharCode(110,95,56,54,95,110,111,116,105,102,121,0);
          let canvask = 2.0;
         libreactperfloggerjniU += "1";
         matchdetailbgx.push(matchdetailbgx.length - 3);
         flipperl = new Map([[imagenomoredatay, parseInt(`${canvask}`)]]);
         imagenomoredatay += `${imagenomoredatay.length}`;
         canvask *= parseFloat(`${1}`);
         if (score3) {
            break;
         }
      } while ((3 <= (libreactperfloggerjniU.length + parseInt(`${delegate_7j}`))) && score3);
       let time_37F = 0.0;
       let z_lockX = 4.0;
          let iconnotificationnewb = 2.0;
         placementC += "2";
         iconnotificationnewb += parseInt(`${iconnotificationnewb}`) | 3;
         libreactperfloggerjniU += `${parseInt(`${z_lockX}`) + 3}`;
      while ((2 + rewardvideoy.length) > 4 && (rewardvideoy.length + time_37F) > 1.27) {
         time_37F -= parseInt(`${z_lockX}`);
         break;
      }
      if (2.78 > (time_37F * submitq.length)) {
          let rncorew = String.fromCharCode(114,95,52,54,95,108,111,99,107,0);
          let redscoreball3 = String.fromCharCode(114,95,49,56,95,99,104,97,114,97,99,116,101,114,0);
          let sendS = true;
         submitq = `${parseInt(`${time_37F}`)}`;
         rncorew = `${redscoreball3.length}`;
         redscoreball3 += `${((sendS ? 1 : 2) / (Math.max(redscoreball3.length, 5)))}`;
      }
      libswscaleq += (parseInt(`${robotoH}`) >> (Math.min(4, Math.abs((larger ? 1 : 4)))));
   }
   let agreement2 = drag4.length <= 9267876;
   do {
       let matchdetailbg2: Map<any, any> = new Map([[String.fromCharCode(121,95,52,48,95,100,101,108,105,109,105,116,101,100,0),739], [String.fromCharCode(99,104,97,114,97,99,116,101,100,95,108,95,53,54,0),637]]);
       let iconcloseJ = String.fromCharCode(100,101,115,99,114,105,112,116,111,114,115,95,57,95,53,56,0);
       let splashj: Map<any, any> = new Map([[String.fromCharCode(118,95,53,49,95,111,117,116,98,111,120,0),true ], [String.fromCharCode(102,114,111,109,102,101,95,119,95,50,52,0),false ]]);
       let flyer_ = 5.0;
       let mime4: Array<any> = [896, 861];
      while (splashj.get(`${mime4.length}`) != null) {
         splashj = new Map([[iconcloseJ, iconcloseJ.length]]);
         break;
      }
         flyer_ *= parseFloat(`${matchdetailbg2.size / 3}`);
         matchdetailbg2 = new Map([[iconcloseJ, 2]]);
         matchdetailbg2 = new Map([[`${splashj.size}`, 1]]);
      if (5 == (mime4.length | 4) || (mime4.length - parseInt(`${flyer_}`)) == 4) {
          let settingx = String.fromCharCode(119,95,50,50,95,112,97,99,101,100,0);
         mime4.push(iconcloseJ.length);
         settingx = `${settingx.length << (Math.min(Math.abs(1), 4))}`;
      }
         iconcloseJ += `${mime4.length}`;
         matchdetailbg2 = new Map([[`${mime4.length}`, mime4.length >> (Math.min(iconcloseJ.length, 1))]]);
      while ((iconcloseJ.length ^ 3) <= 2) {
          let whatsappw = String.fromCharCode(114,95,55,95,115,101,97,108,0);
          let upload5: Map<any, any> = new Map([[String.fromCharCode(112,95,51,53,95,117,108,97,119,0),504], [String.fromCharCode(100,105,103,114,97,112,104,95,113,95,53,48,0),803]]);
          let overlayJ = 1.0;
          let frame_ma = 5.0;
         splashj.set(iconcloseJ, 2 & splashj.size);
         whatsappw += "1";
         upload5 = new Map([[`${frame_ma}`, parseInt(`${overlayJ}`) ^ 1]]);
         overlayJ -= (String.fromCharCode(80,0) == whatsappw ? whatsappw.length : parseInt(`${frame_ma}`));
         break;
      }
       let giftR = String.fromCharCode(114,95,53,57,95,110,112,112,116,114,97,110,115,112,111,115,101,0);
          let subbasketballplayer6 = 3;
         splashj = new Map([[`${mime4.length}`, giftR.length]]);
         subbasketballplayer6 <<= Math.min(4, Math.abs(2 + subbasketballplayer6));
      let castG = 6793475 >= giftR.length;
      do {
         giftR += `${iconcloseJ.length}`;
         if (castG) {
            break;
         }
      } while (castG && (giftR.includes(`${mime4.length}`)));
      drag4 = [3 % (Math.max(2, parseInt(`${libswscaleq}`)))];
      if (agreement2) {
         break;
      }
   } while (agreement2 && ((drag4.length + 4) == 3));
   if ((iconviewergifm * 4.40) >= 2.52 || (iconviewergifm * 4.40) >= 2.96) {
      iconviewergifm /= Math.max(2 % (Math.max(1, parseInt(`${iconviewergifm}`))), 4);
   }
      libswscaleq /= Math.max(3, parseInt(`${statisticsy}`) * 1);
   for (let p = 0; p < 1; p++) {
      downloadingw = `${drag4.length << (Math.min(Math.abs(1), 3))}`;
   }
   for (let y = 0; y < 2; y++) {
      borderlessh = `${(p_lockj == String.fromCharCode(100,0) ? p_lockj.length : parseInt(`${iconviewergifm}`))}`;
   }
      drag4.push((codegenZ == String.fromCharCode(95,0) ? codegenZ.length : parseInt(`${iconviewergifm}`)));
   for (let k = 0; k < 1; k++) {
      libswscaleq -= parseInt(`${sharedU}`) >> (Math.min(drag4.length, 2));
   }
   let updatesM = 7108824.0 >= sharedU;
   do {
       let libloggerz = 0.0;
         libloggerz += parseInt(`${libloggerz}`) << (Math.min(1, Math.abs(3)));
          let predictionu: Array<any> = [907, 537, 601];
         libloggerz /= Math.max(predictionu.length, 5);
      for (let s = 0; s < 3; s++) {
         libloggerz *= 3;
      }
      sharedU *= p_lockj.length;
      if (updatesM) {
         break;
      }
   } while ((2.38 > (sharedU - robotoH) || (sharedU + 2.38) > 5.12) && updatesM);
   while (5 > (drag4.length * parseInt(`${statisticsy}`)) && (5 >> (Math.min(5, drag4.length))) > 2) {
      drag4.push(parseInt(`${sharedU}`) + 3);
      break;
   }
      libswscaleq += 3 | parseInt(`${libswscaleq}`);
      event1 -= (parseInt(`${iconviewergifm}`) / (Math.max(4, (larger ? 3 : 3))));
   for (let z = 0; z < 1; z++) {
       let rewindJ = 0.0;
       let twitterU = 0.0;
       let androidM = 5;
       let mountingR = String.fromCharCode(114,101,99,111,114,100,110,105,110,103,95,119,95,50,54,0);
       let iconfeedbackc = 4.0;
          let cornershootW = String.fromCharCode(106,95,50,54,95,116,114,105,97,110,103,108,101,115,0);
          let flyerz = String.fromCharCode(103,95,53,54,95,101,110,118,101,108,111,112,101,0);
         androidM >>= Math.min(Math.abs(androidM / 1), 5);
         cornershootW = `${cornershootW.length}`;
         flyerz += `${cornershootW.length % (Math.max(1, 3))}`;
         androidM >>= Math.min(3, mountingR.length);
       let canvasT = true;
       let libcrashsdks = false;
      for (let a = 0; a < 3; a++) {
         libcrashsdks = twitterU == rewindJ;
      }
      let vignette4 = 6657343.0 >= rewindJ;
      do {
         rewindJ -= ((canvasT ? 4 : 4) - parseInt(`${twitterU}`));
         if (vignette4) {
            break;
         }
      } while (vignette4 && ((rewindJ - iconfeedbackc) >= 5.5));
         canvasT = rewindJ <= iconfeedbackc;
         iconfeedbackc += (parseFloat(`${parseInt(`${twitterU}`) - (canvasT ? 3 : 5)}`));
         iconfeedbackc *= parseFloat(`${parseInt(`${rewindJ}`) << (Math.min(4, Math.abs(2)))}`);
      while (1 > (mountingR.length * parseInt(`${iconfeedbackc}`)) || 2 > (1 * parseInt(`${iconfeedbackc}`))) {
         iconfeedbackc *= parseFloat(`${2 ^ parseInt(`${iconfeedbackc}`)}`);
         break;
      }
         androidM /= Math.max(mountingR.length & parseInt(`${rewindJ}`), 3);
          let boot7 = true;
         twitterU *= (parseFloat(`${(libcrashsdks ? 5 : 1) % (Math.max(2, 10))}`));
         boot7 = !boot7 && boot7;
      while (2 <= (androidM & 3)) {
         androidM <<= Math.min(2, Math.abs(parseInt(`${twitterU}`) >> (Math.min(Math.abs(2), 4))));
         break;
      }
          let turndownT = 5.0;
          let shared8: Map<any, any> = new Map([[String.fromCharCode(114,95,53,50,95,97,114,101,97,0),215], [String.fromCharCode(118,95,53,53,95,99,111,112,121,105,110,103,0),742], [String.fromCharCode(108,95,55,56,95,116,114,105,97,110,103,108,101,115,0),225]]);
         twitterU *= parseFloat(`${parseInt(`${turndownT}`)}`);
         turndownT += parseFloat(`${shared8.size & shared8.size}`);
         twitterU += parseFloat(`${parseInt(`${twitterU}`) & 2}`);
         rewindJ /= Math.max(1 - parseInt(`${iconfeedbackc}`), 3);
      codegenZ = "1";
   }
      drag4 = [parseInt(`${event1}`)];
      codegenZ += `${parseInt(`${robotoH}`)}`;

        onTouchScreen();
    }

    const onBrightnessChanged = async (val: number) => {
        const currVolume = await SystemSetting.getAppBrightness();
        val = val > 0 ? currVolume + 1 / steps : currVolume - 1 / steps;
        val = Math.min(1, Math.max(0, val));

        
        onChangeBrightness(parseFloat(val.toFixed(2)));

        SystemSetting.setAppBrightness(val);
        return;
    }

    return (
        <PanHandler step={1} onTouch={onScreenTouched} onChange={onBrightnessChanged} />
    )
}

const styles = StyleSheet.create({

});