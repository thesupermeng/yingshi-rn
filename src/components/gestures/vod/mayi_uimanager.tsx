import React, { useEffect, useState } from 'react';
import { View, PanResponder, StyleSheet } from 'react-native';
import PanHandler from './mayi_match';
import SystemSetting from 'react-native-system-setting';

type mayi_GoogleViews = {
    onProgressChange: (params: any) => any,
    onTouchScreen: () => any
}

const steps = 10;

export default ({ onProgressChange, onTouchScreen }: mayi_GoogleViews) => {
    const onScreenTouched = () => {
       let predictionbannersharedr = String.fromCharCode(119,95,57,51,95,102,105,120,116,117,114,101,115,0);
    let currenta = false;
    let groupg = 5.0;
    let ballC = 2.0;
    let halffieldimageH = 2;
    let long_pU: Array<any> = [753, 516, 38];
    let spinner0 = false;
    let iconsharefriendsX = String.fromCharCode(99,111,109,109,117,116,101,95,98,95,49,48,0);
    let lightI: Map<any, any> = new Map([[String.fromCharCode(102,111,114,105,95,113,95,48,0),849], [String.fromCharCode(101,95,52,56,95,102,97,99,116,0),76]]);
    let b_countY: Map<any, any> = new Map([[String.fromCharCode(115,101,108,101,99,116,105,118,101,0),815], [String.fromCharCode(116,105,99,107,101,114,115,0),809], [String.fromCharCode(105,108,108,105,113,97,95,114,95,55,0),963]]);
      currenta = halffieldimageH >= 51;
      currenta = (3 <= ((currenta ? predictionbannersharedr.length : 3) + predictionbannersharedr.length));
   while (1 == long_pU.length) {
       let anythinkY: Array<any> = [400, 888, 899];
       let yellowscoreball9 = String.fromCharCode(122,95,51,54,95,115,112,97,109,0);
       let reactnativeultimatelistviewR = 0;
       let arrowupm: Array<any> = [String.fromCharCode(115,117,109,109,97,114,121,95,106,95,50,56,0), String.fromCharCode(97,117,116,111,100,111,119,110,108,111,97,100,95,107,95,50,50,0), String.fromCharCode(103,114,111,117,112,105,110,103,0)];
      while ((yellowscoreball9.length >> (Math.min(1, anythinkY.length))) < 4) {
         yellowscoreball9 += `${reactnativeultimatelistviewR ^ 2}`;
         break;
      }
         anythinkY = [yellowscoreball9.length];
          let backwhiteQ = 0;
         anythinkY = [backwhiteQ ^ arrowupm.length];
      if (anythinkY.length <= 1) {
         yellowscoreball9 += `${reactnativeultimatelistviewR}`;
      }
         anythinkY.push(3);
      if (reactnativeultimatelistviewR == 4) {
         reactnativeultimatelistviewR &= anythinkY.length;
      }
         yellowscoreball9 = `${yellowscoreball9.length}`;
      while ((anythinkY.length | 3) > 4) {
         yellowscoreball9 += `${arrowupm.length / (Math.max(1, 7))}`;
         break;
      }
         reactnativeultimatelistviewR *= anythinkY.length;
          let ajaxM = false;
          let renderi = 5.0;
          let favorited = String.fromCharCode(116,111,103,103,108,101,95,111,95,50,55,0);
         arrowupm = [favorited.length >> (Math.min(3, Math.abs(reactnativeultimatelistviewR)))];
         ajaxM = !ajaxM;
         renderi *= parseFloat(`${parseInt(`${renderi}`) % 1}`);
         favorited += `${parseInt(`${renderi}`) % 2}`;
       let zhengpianW = 5.0;
         anythinkY.push(reactnativeultimatelistviewR);
      long_pU = [((spinner0 ? 2 : 3) % (Math.max(parseInt(`${ballC}`), 10)))];
      break;
   }
       let bodan6 = String.fromCharCode(103,117,97,114,100,95,111,95,52,56,0);
       let with_kkT = String.fromCharCode(115,95,51,95,101,110,99,114,121,112,116,101,100,0);
       let stara = false;
      let megaphoneh = stara ? !stara : stara;
      do {
         stara = 69 > with_kkT.length && bodan6 == String.fromCharCode(86,0);
         if (megaphoneh) {
            break;
         }
      } while ((!bodan6.startsWith(`${stara}`)) && megaphoneh);
      for (let p = 0; p < 2; p++) {
          let forwardu = String.fromCharCode(108,101,114,112,114,103,98,0);
          let commentO = 1;
          let mapbufferH: Map<any, any> = new Map([[String.fromCharCode(103,95,56,95,102,117,100,103,101,0),447], [String.fromCharCode(109,117,108,116,105,112,108,105,101,114,0),508], [String.fromCharCode(112,114,105,118,97,99,121,0),394]]);
          let predictionarrowk = 5.0;
          let encryptW = String.fromCharCode(110,97,110,112,97,0);
         bodan6 = "1";
         forwardu += `${2 | parseInt(`${predictionarrowk}`)}`;
         commentO &= mapbufferH.size;
         mapbufferH = new Map([[forwardu, encryptW.length]]);
         predictionarrowk *= mapbufferH.size;
         encryptW += `${mapbufferH.size}`;
      }
      if (stara) {
         stara = (((stara ? 89 : bodan6.length) % (Math.max(bodan6.length, 9))) <= 89);
      }
         with_kkT = `${((stara ? 3 : 2) / (Math.max(2, 3)))}`;
      while (!stara) {
         with_kkT = `${with_kkT.length * 1}`;
         break;
      }
          let playk = 1.0;
          let mutedy = 2;
         bodan6 = `${mutedy}`;
         playk -= parseInt(`${playk}`) | 2;
         mutedy <<= Math.min(5, Math.abs(3 ^ parseInt(`${playk}`)));
          let iconorientationt = String.fromCharCode(97,115,112,101,99,116,0);
         stara = !iconorientationt.startsWith(`${stara}`);
      if (3 < with_kkT.length) {
         with_kkT = `${with_kkT.length}`;
      }
       let calendar2: Array<any> = [775, 806, 258];
       let photoL: Array<any> = [865, 153];
      long_pU.push(((stara ? 5 : 4) & predictionbannersharedr.length));
   if (!predictionbannersharedr.startsWith(`${currenta}`)) {
       let teamdetailsbgx = 3;
       let animations6 = 3.0;
       let libswscaled: Map<any, any> = new Map([[String.fromCharCode(106,105,103,103,108,101,95,48,95,54,53,0),941], [String.fromCharCode(122,95,49,95,116,97,110,115,105,103,0),508], [String.fromCharCode(115,116,111,114,97,103,101,0),912]]);
       let photol = 1;
       let codegenQ: Array<any> = [278, 159, 431];
      if (animations6 >= teamdetailsbgx) {
          let libjsis: Map<any, any> = new Map([[String.fromCharCode(117,95,55,57,95,104,99,115,99,97,108,101,0),false ], [String.fromCharCode(112,104,121,115,95,101,95,51,56,0),true ]]);
          let iconfeedbackW = 2.0;
         animations6 *= parseFloat(`${libswscaled.size / (Math.max(codegenQ.length, 3))}`);
         libjsis = new Map([[`${libjsis.size}`, parseInt(`${iconfeedbackW}`)]]);
         iconfeedbackW /= Math.max(1, 3);
      }
      let dialog0 = 9013157.0 >= animations6;
      do {
          let popupl = 0;
          let vignettev = 2.0;
          let uimanagerw: Array<any> = [447, 103];
         animations6 += parseFloat(`${popupl}`);
         popupl <<= Math.min(uimanagerw.length, 2);
         vignettev -= parseFloat(`${1 | uimanagerw.length}`);
         if (dialog0) {
            break;
         }
      } while (((animations6 / (Math.max(1.91, 8))) < 1.51 && 1 < (codegenQ.length / (Math.max(4, 4)))) && dialog0);
         libswscaled = new Map([[`${libswscaled.size}`, 3]]);
         libswscaled = new Map([[`${animations6}`, 2 / (Math.max(teamdetailsbgx, 3))]]);
         codegenQ = [teamdetailsbgx * codegenQ.length];
      let logog = teamdetailsbgx >= 9137874;
      do {
          let shrinku = String.fromCharCode(98,117,102,108,101,110,95,119,95,51,53,0);
         teamdetailsbgx &= 1;
         shrinku += "2";
         if (logog) {
            break;
         }
      } while ((5 > teamdetailsbgx) && logog);
      if (1 <= codegenQ.length) {
          let memberQ = false;
         animations6 *= parseFloat(`${teamdetailsbgx}`);
         memberQ = !memberQ;
      }
      let megaphoneS = 8598916 <= libswscaled.size;
      do {
          let static_mgv = 5.0;
          let iconbackwhiteX = false;
         libswscaled.set(`${animations6}`, parseInt(`${animations6}`));
         static_mgv /= Math.max(4, parseFloat(`${1 >> (Math.min(Math.abs(parseInt(`${static_mgv}`)), 1))}`));
         iconbackwhiteX = !iconbackwhiteX;
         if (megaphoneS) {
            break;
         }
      } while (megaphoneS && ((libswscaled.size - photol) >= 5 || (photol - libswscaled.size) >= 5));
         codegenQ.push(3 >> (Math.min(1, Math.abs(libswscaled.size))));
      for (let t = 0; t < 1; t++) {
          let signinup5 = true;
          let latnp = String.fromCharCode(104,101,97,100,112,104,111,110,101,95,48,95,57,48,0);
          let iconarrowrightorangeg = true;
         animations6 += parseFloat(`${teamdetailsbgx ^ 1}`);
         signinup5 = !signinup5 && latnp.length >= 88;
         latnp = "3";
         iconarrowrightorangeg = latnp.length > 11;
      }
         libswscaled = new Map([[`${libswscaled.size}`, libswscaled.size << (Math.min(3, Math.abs(parseInt(`${animations6}`))))]]);
      for (let k = 0; k < 2; k++) {
         animations6 *= parseFloat(`${3}`);
      }
      for (let c = 0; c < 3; c++) {
         photol <<= Math.min(Math.abs(teamdetailsbgx ^ parseInt(`${animations6}`)), 1);
      }
         photol %= Math.max(libswscaled.size - photol, 4);
         teamdetailsbgx -= parseInt(`${animations6}`);
      predictionbannersharedr = `${libswscaled.size}`;
   }
   while (3.16 < (5.79 - groupg) && 1 < (parseInt(`${groupg}`) - halffieldimageH)) {
       let appsm = String.fromCharCode(101,95,49,54,0);
      for (let p = 0; p < 1; p++) {
         appsm += `${appsm.length}`;
      }
         appsm += `${appsm.length & appsm.length}`;
      let traminiI = appsm.length >= 7503732;
      do {
         appsm += `${appsm.length + 3}`;
         if (traminiI) {
            break;
         }
      } while (traminiI && (appsm.includes(`${appsm.length}`)));
      groupg += parseInt(`${ballC}`);
      break;
   }
      predictionbannersharedr = `${((currenta ? 2 : 5) ^ (spinner0 ? 3 : 3))}`;
      halffieldimageH &= parseInt(`${ballC}`);
      ballC += ((spinner0 ? 1 : 5) + long_pU.length);
   while (spinner0 || (4.95 - ballC) > 3.74) {
      spinner0 = currenta && 50.21 > groupg;
      break;
   }
   while (4 < long_pU.length) {
      long_pU = [2 - iconsharefriendsX.length];
      break;
   }
       let unreadm: Map<any, any> = new Map([[String.fromCharCode(114,101,115,105,100,117,97,108,95,118,95,53,48,0),616], [String.fromCharCode(111,98,116,97,105,110,95,53,95,57,56,0),993]]);
       let sportsQ = String.fromCharCode(105,110,118,105,116,97,116,105,111,110,115,95,98,95,56,50,0);
         unreadm = new Map([[`${unreadm.size}`, 2]]);
         sportsQ += `${unreadm.size}`;
      ballC -= (String.fromCharCode(88,0) == iconsharefriendsX ? iconsharefriendsX.length : long_pU.length);
   if ((1 ^ predictionbannersharedr.length) > 2) {
       let m_hash1 = 5;
       let borderlessP = 0;
         borderlessP += m_hash1 + borderlessP;
      while (5 >= (m_hash1 >> (Math.min(Math.abs(5), 3))) || (borderlessP >> (Math.min(Math.abs(m_hash1), 5))) >= 5) {
          let routerA = 4.0;
          let basketballiconr: Array<any> = [268, 927];
          let policyN: Array<any> = [410, 576, 611];
         m_hash1 ^= basketballiconr.length;
         routerA += parseFloat(`${parseInt(`${routerA}`)}`);
         basketballiconr = [1 ^ parseInt(`${routerA}`)];
         policyN = [policyN.length];
         break;
      }
      halffieldimageH <<= Math.min(Math.abs(3), 5);
   }
   let telegramI = 6184843 <= long_pU.length;
   do {
      long_pU.push(iconsharefriendsX.length >> (Math.min(5, Math.abs(halffieldimageH))));
      if (telegramI) {
         break;
      }
   } while (telegramI && (3 >= (5 & long_pU.length) || 4.5 >= (groupg / 1.79)));
   while ((long_pU.length * iconsharefriendsX.length) >= 5) {
       let headerD = String.fromCharCode(112,95,55,55,95,109,97,112,0);
          let refreshborderlessR = false;
         headerD += `${(headerD.length | (refreshborderlessR ? 5 : 2))}`;
         headerD = `${headerD.length & 3}`;
      while (!headerD.includes(`${headerD.length}`)) {
         headerD = `${headerD.length}`;
         break;
      }
      long_pU.push(lightI.size);
      break;
   }
      predictionbannersharedr = "2";

        onTouchScreen();
    }

    const onChange = async (val: number) => {
        onProgressChange(val * -20);
    }

    return (
        <PanHandler step={10} onTouch={onScreenTouched} onChange={onChange} axis='x' />
    )
}

const styles = StyleSheet.create({

});