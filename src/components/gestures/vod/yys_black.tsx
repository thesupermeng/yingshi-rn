import React, { useEffect, useState } from 'react';
import { View, PanResponder, StyleSheet } from 'react-native';
import PanHandler from './yys_scrollview';
import SystemSetting from 'react-native-system-setting';

type yysIconarrowrightorangeStation = {
    onProgressChange: (params: any) => any,
    onTouchScreen: () => any
}

const steps = 10;

export default ({ onProgressChange, onTouchScreen }: yysIconarrowrightorangeStation) => {
    const onScreenTouched = () => {
       let halfG = String.fromCharCode(117,95,53,95,97,116,114,97,99,112,108,117,115,0);
    let awayteamfieldD = String.fromCharCode(98,117,105,108,100,105,110,103,95,56,95,54,57,0);
    let tickZ = 0;
    let mbbidt = false;
    let basketballdetailsbgL: Array<any> = [676, 636, 466];
    let mimoQ = String.fromCharCode(122,95,57,53,0);
    let fullscreenmaxG = String.fromCharCode(115,95,52,54,95,112,108,116,101,0);
    let umengt = String.fromCharCode(114,95,52,56,95,115,117,98,109,111,100,117,108,101,0);
    let iconsubssuccessN = String.fromCharCode(98,95,54,56,95,103,101,116,108,98,108,111,99,107,105,110,99,0);
    let predictionR = 1.0;
    let libreactnativeblobb: Array<any> = [521, 642, 949];
    let championB = String.fromCharCode(115,95,51,95,114,101,112,114,101,115,101,110,116,97,116,105,118,101,0);
    let watch1: Map<any, any> = new Map([[String.fromCharCode(116,95,54,95,108,101,110,103,116,104,115,0),42], [String.fromCharCode(105,95,51,49,95,115,112,97,99,101,114,0),566], [String.fromCharCode(116,95,52,48,95,100,101,108,105,109,105,116,101,114,115,0),767]]);
    let fullscreenmink = 0;
   let confirmationI = tickZ >= 8361055;
   do {
      tickZ += 1;
      if (confirmationI) {
         break;
      }
   } while (confirmationI && (5 > (awayteamfieldD.length + tickZ) && (5 + awayteamfieldD.length) > 4));
      umengt = `${3 >> (Math.min(1, umengt.length))}`;
   for (let h = 0; h < 1; h++) {
       let formG = 3;
       let fillR = 0.0;
       let notificationfillemptyi: Array<any> = [String.fromCharCode(109,97,115,116,101,114,105,110,103,95,56,95,53,49,0), String.fromCharCode(105,110,115,101,114,116,101,114,95,53,95,53,53,0), String.fromCharCode(107,95,52,49,95,115,110,97,109,101,0)];
       let awayY = 4.0;
       let icondownimgt = 3.0;
       let java3: Array<any> = [String.fromCharCode(112,95,50,54,95,117,110,100,101,114,115,99,111,114,101,0), String.fromCharCode(98,95,51,95,110,105,109,97,116,105,111,110,0)];
          let airbnbstar9 = String.fromCharCode(115,117,98,116,105,116,108,101,95,52,95,56,50,0);
          let benefitH = String.fromCharCode(111,95,53,54,95,99,111,110,99,117,114,101,110,116,0);
         notificationfillemptyi = [3 << (Math.min(Math.abs(parseInt(`${icondownimgt}`)), 4))];
         airbnbstar9 = `${(String.fromCharCode(54,0) == airbnbstar9 ? airbnbstar9.length : benefitH.length)}`;
         benefitH = `${airbnbstar9.length}`;
       let sigmobV = String.fromCharCode(114,95,56,52,95,114,101,115,101,116,117,112,0);
       let iconstarj = String.fromCharCode(116,97,98,108,101,108,105,109,95,57,95,55,56,0);
      for (let a = 0; a < 3; a++) {
         awayY /= Math.max(parseFloat(`${parseInt(`${awayY}`) & 1}`), 2);
      }
         icondownimgt /= Math.max(1, 3);
         java3.push(3);
          let debugr = String.fromCharCode(101,115,101,110,100,95,109,95,56,52,0);
          let shirt6 = false;
          let favorite0 = String.fromCharCode(99,97,112,95,115,95,50,54,0);
         java3.push(parseInt(`${icondownimgt}`) % (Math.max(8, parseInt(`${awayY}`))));
         debugr += `${((shirt6 ? 5 : 3) | 2)}`;
         shirt6 = !shirt6 && favorite0.length <= 63;
         favorite0 = `${(debugr == String.fromCharCode(82,0) ? favorite0.length : debugr.length)}`;
      while (5.61 >= (awayY / 2.100)) {
         awayY /= Math.max(4, parseFloat(`${notificationfillemptyi.length}`));
         break;
      }
         awayY *= (parseFloat(`${sigmobV == String.fromCharCode(122,0) ? java3.length : sigmobV.length}`));
      for (let c = 0; c < 2; c++) {
         iconstarj += `${notificationfillemptyi.length % 1}`;
      }
          let foundt = true;
          let chinasame1 = String.fromCharCode(122,95,52,52,95,109,97,114,103,105,110,115,0);
         formG &= sigmobV.length;
         foundt = !foundt;
         chinasame1 = `${((foundt ? 1 : 2))}`;
         formG -= iconstarj.length ^ 1;
      if (3.72 <= (icondownimgt + fillR) || 4.44 <= (3.72 - fillR)) {
         fillR += parseFloat(`${1 | formG}`);
      }
          let componentE = String.fromCharCode(104,97,110,100,108,101,95,118,95,57,51,0);
          let details6 = 3.0;
          let injuryN: Array<any> = [303, 202];
         iconstarj = `${parseInt(`${details6}`)}`;
         componentE += "3";
         injuryN = [3 - injuryN.length];
      if (icondownimgt < awayY) {
         awayY -= parseFloat(`${formG}`);
      }
      mimoQ = "1";
   }
   let sourcem = 5882501 <= mimoQ.length;
   do {
      mimoQ += `${umengt.length / 3}`;
      if (sourcem) {
         break;
      }
   } while ((fullscreenmaxG == mimoQ) && sourcem);
   if (!libreactnativeblobb.includes(predictionR)) {
      libreactnativeblobb.push(3 ^ championB.length);
   }
      predictionR *= 3;
   if (1 < mimoQ.length) {
      mimoQ += `${tickZ}`;
   }
      predictionR *= (parseInt(`${predictionR}`) * (mbbidt ? 4 : 1));
   while (fullscreenmaxG.includes(`${mimoQ.length}`)) {
       let hooksr = false;
       let const_cD: Array<any> = [735, 299, 426];
       let short_ny_ = String.fromCharCode(101,114,108,101,95,49,95,53,50,0);
       let predictionactiver: Array<any> = [145, 386, 23];
       let mail5 = 3.0;
      if (1 == (const_cD.length * 3) && (const_cD.length * predictionactiver.length) == 3) {
         const_cD.push(1 - const_cD.length);
      }
         const_cD.push(2);
      while (hooksr) {
         hooksr = const_cD.includes(hooksr);
         break;
      }
         mail5 += (parseFloat(`${parseInt(`${mail5}`) * (hooksr ? 1 : 2)}`));
      while (4 < (predictionactiver.length << (Math.min(Math.abs(3), 1)))) {
         predictionactiver = [((hooksr ? 5 : 3) ^ 2)];
         break;
      }
         hooksr = !hooksr && short_ny_.length == 84;
       let commentg = String.fromCharCode(118,95,52,55,95,99,111,117,110,116,114,105,101,115,0);
       let libyoga_ = String.fromCharCode(121,100,97,121,95,103,95,55,54,0);
         libyoga_ = `${2 - commentg.length}`;
         libyoga_ += "1";
      let arrowrightwithtail0 = predictionactiver.length <= 8935770;
      do {
          let termsH = String.fromCharCode(99,111,109,109,105,115,115,105,111,110,95,100,95,57,48,0);
         predictionactiver.push(predictionactiver.length >> (Math.min(Math.abs(2), 4)));
         termsH = `${termsH.length}`;
         if (arrowrightwithtail0) {
            break;
         }
      } while ((!short_ny_.startsWith(`${predictionactiver.length}`)) && arrowrightwithtail0);
         mail5 /= Math.max(parseFloat(`${predictionactiver.length / (Math.max(2, 10))}`), 4);
          let dplusz = String.fromCharCode(112,95,52,52,95,115,114,99,0);
          let defaultprofilepice: Map<any, any> = new Map([[String.fromCharCode(112,111,105,110,116,95,122,95,51,0),997], [String.fromCharCode(99,95,54,52,95,119,104,101,110,0),456]]);
          let dropdownA: Array<any> = [280, 54, 559];
         short_ny_ += "2";
         dplusz += `${1 & defaultprofilepice.size}`;
         defaultprofilepice = new Map([[`${defaultprofilepice.size}`, (String.fromCharCode(85,0) == dplusz ? defaultprofilepice.size : dplusz.length)]]);
         dropdownA.push(dropdownA.length | 3);
         mail5 -= parseFloat(`${3 | predictionactiver.length}`);
         commentg = `${(3 ^ (hooksr ? 3 : 5))}`;
         commentg += `${const_cD.length << (Math.min(short_ny_.length, 1))}`;
      fullscreenmaxG += `${(parseInt(`${mail5}`) | (mbbidt ? 1 : 3))}`;
      break;
   }
   while (halfG.length < awayteamfieldD.length) {
      halfG = `${(String.fromCharCode(78,0) == championB ? championB.length : basketballdetailsbgL.length)}`;
      break;
   }
       let calendar5 = false;
          let iconorientationM = 1.0;
          let inouttargetredT = String.fromCharCode(108,97,110,101,95,48,95,53,51,0);
         calendar5 = (inouttargetredT.length + parseInt(`${iconorientationM}`)) < 65;
         calendar5 = !calendar5;
      for (let h = 0; h < 3; h++) {
         calendar5 = !calendar5;
      }
      iconsubssuccessN = `${iconsubssuccessN.length / (Math.max(2, 7))}`;
   let iconscheduleJ = 9185270 <= tickZ;
   do {
      tickZ |= 2;
      if (iconscheduleJ) {
         break;
      }
   } while (iconscheduleJ && (!mimoQ.startsWith(`${tickZ}`)));
   let profilepicn = predictionR <= 6498897.0;
   do {
      predictionR += libreactnativeblobb.length ^ 3;
      if (profilepicn) {
         break;
      }
   } while ((2 < libreactnativeblobb.length) && profilepicn);
   let profilepics = libreactnativeblobb.length <= 8734786;
   do {
       let icondefaultthumbnail9 = 0.0;
          let privilegep: Array<any> = [881, 402, 503];
          let delegate_d0p: Array<any> = [String.fromCharCode(115,95,55,51,95,116,98,108,101,110,100,0), String.fromCharCode(110,95,50,55,95,110,111,110,115,101,99,117,114,101,0)];
          let reddownarrowA = String.fromCharCode(111,114,103,97,110,105,122,97,116,105,111,110,95,111,95,52,49,0);
         icondefaultthumbnail9 /= Math.max((String.fromCharCode(76,0) == reddownarrowA ? reddownarrowA.length : parseInt(`${icondefaultthumbnail9}`)), 2);
         privilegep.push(2);
         delegate_d0p = [delegate_d0p.length ^ 2];
      while (1.82 < (2.85 + icondefaultthumbnail9)) {
          let codegen_ = String.fromCharCode(104,95,51,57,95,99,117,114,108,121,0);
         icondefaultthumbnail9 += 1 << (Math.min(3, codegen_.length));
         break;
      }
         icondefaultthumbnail9 *= parseInt(`${icondefaultthumbnail9}`) + 2;
      libreactnativeblobb.push(2 ^ fullscreenmaxG.length);
      if (profilepics) {
         break;
      }
   } while (profilepics && (3 <= (fullscreenmaxG.length % 1)));
   if (!mbbidt) {
       let fast0 = String.fromCharCode(107,117,107,105,95,56,95,56,48,0);
      for (let x = 0; x < 2; x++) {
          let bingg = false;
          let download2 = 2;
          let arrow_ = String.fromCharCode(107,95,51,52,95,111,102,102,115,101,116,115,105,122,101,0);
          let audience_ = String.fromCharCode(112,95,53,53,95,115,99,97,108,101,99,117,100,97,0);
         fast0 = `${download2 / (Math.max(2, 2))}`;
         bingg = 65 == arrow_.length;
         download2 *= 1;
         arrow_ = "1";
         audience_ = `${((bingg ? 4 : 2) << (Math.min(Math.abs(1), 3)))}`;
      }
          let megaphonep = String.fromCharCode(101,95,54,57,95,99,104,101,99,107,101,114,0);
          let line5 = String.fromCharCode(112,114,101,101,109,112,116,105,118,101,95,53,95,53,51,0);
          let dropdownR = 4.0;
         fast0 = `${(fast0 == String.fromCharCode(53,0) ? fast0.length : parseInt(`${dropdownR}`))}`;
         megaphonep = `${1 >> (Math.min(3, line5.length))}`;
         line5 += `${megaphonep.length}`;
         dropdownR /= Math.max(3, 2 & megaphonep.length);
      let buttonp = fast0 == String.fromCharCode(110,53,110,103,120,0);
      do {
         fast0 = `${fast0.length * fast0.length}`;
         if (buttonp) {
            break;
         }
      } while ((fast0.includes(`${fast0.length}`)) && buttonp);
      mbbidt = mimoQ.startsWith(`${tickZ}`);
   }
      fullscreenmaxG = `${halfG.length}`;
      predictionR -= ((mbbidt ? 4 : 3) >> (Math.min(Math.abs(parseInt(`${predictionR}`)), 5)));
   for (let o = 0; o < 3; o++) {
      predictionR -= tickZ;
   }
      awayteamfieldD = `${(String.fromCharCode(101,0) == mimoQ ? awayteamfieldD.length : mimoQ.length)}`;
       let mintegralo = String.fromCharCode(107,95,56,95,99,121,99,108,105,99,0);
       let libmapbufferjniu = String.fromCharCode(104,95,53,54,95,117,115,101,0);
         mintegralo += `${(mintegralo == String.fromCharCode(108,0) ? mintegralo.length : libmapbufferjniu.length)}`;
          let arrowdown6 = 3;
         libmapbufferjniu = `${libmapbufferjniu.length}`;
         arrowdown6 /= Math.max(5, arrowdown6);
      if (mintegralo.endsWith(`${libmapbufferjniu.length}`)) {
         mintegralo = `${mintegralo.length + libmapbufferjniu.length}`;
      }
      if (libmapbufferjniu == String.fromCharCode(120,0) && 4 <= mintegralo.length) {
         libmapbufferjniu = `${3 * mintegralo.length}`;
      }
      if (mintegralo != String.fromCharCode(77,0)) {
         libmapbufferjniu = "2";
      }
         libmapbufferjniu = `${mintegralo.length << (Math.min(libmapbufferjniu.length, 4))}`;
      umengt += `${libmapbufferjniu.length * umengt.length}`;
      tickZ -= 1 >> (Math.min(1, Math.abs(tickZ)));
   if (4 <= awayteamfieldD.length) {
      awayteamfieldD = `${parseInt(`${predictionR}`) & 1}`;
   }
   let inouttargetredc = awayteamfieldD == String.fromCharCode(122,105,52,56,97,122,113,115,0);
   do {
      awayteamfieldD = `${(String.fromCharCode(109,0) == iconsubssuccessN ? iconsubssuccessN.length : basketballdetailsbgL.length)}`;
      if (inouttargetredc) {
         break;
      }
   } while ((libreactnativeblobb.length <= awayteamfieldD.length) && inouttargetredc);
   if (basketballdetailsbgL.length == tickZ) {
       let hejiC = false;
       let iconarrowrightorangeg: Array<any> = [243, 828];
       let linee: Array<any> = [623, 335, 791];
       let delegate_x2 = true;
         linee.push(((hejiC ? 2 : 1) * 2));
         linee.push(3 << (Math.min(4, iconarrowrightorangeg.length)));
          let libavformaty = String.fromCharCode(114,101,115,112,101,99,116,105,110,103,95,108,95,51,0);
          let libloggerI = 3.0;
          let upgradeO = String.fromCharCode(100,101,98,117,103,103,101,114,95,51,95,55,57,0);
         delegate_x2 = (linee.length >> (Math.min(3, iconarrowrightorangeg.length))) <= 85;
         libavformaty += "1";
         libloggerI -= parseInt(`${libloggerI}`) * 3;
         upgradeO += "2";
         hejiC = linee.includes(hejiC);
      if (delegate_x2) {
         hejiC = linee.length > 99 || delegate_x2;
      }
         iconarrowrightorangeg = [(1 / (Math.max(8, (hejiC ? 4 : 1))))];
      for (let t = 0; t < 2; t++) {
         linee = [iconarrowrightorangeg.length];
      }
          let mbridge8: Array<any> = [976, 248, 10];
          let libfile4: Array<any> = [869, 198];
         iconarrowrightorangeg.push(libfile4.length);
         mbridge8.push(2 | mbridge8.length);
         libfile4 = [mbridge8.length];
      while (!hejiC && 5 > (iconarrowrightorangeg.length * 2)) {
         iconarrowrightorangeg = [iconarrowrightorangeg.length];
         break;
      }
      if (1 == (linee.length / 4)) {
         hejiC = iconarrowrightorangeg.includes(delegate_x2);
      }
          let predictiondefaultk = String.fromCharCode(98,95,56,57,95,102,105,102,111,0);
          let textlayoutmanagerE = String.fromCharCode(106,95,55,95,112,97,115,116,101,108,0);
         iconarrowrightorangeg = [(2 ^ (delegate_x2 ? 1 : 5))];
         predictiondefaultk += `${predictiondefaultk.length}`;
         textlayoutmanagerE = `${textlayoutmanagerE.length + predictiondefaultk.length}`;
       let uimanagerE = 1.0;
      basketballdetailsbgL = [(umengt == String.fromCharCode(87,0) ? umengt.length : tickZ)];
   }
   for (let x = 0; x < 1; x++) {
      fullscreenmaxG += `${1 | iconsubssuccessN.length}`;
   }
       let modulesV = String.fromCharCode(108,95,53,50,95,102,114,97,109,101,108,101,115,115,0);
         modulesV = `${modulesV.length << (Math.min(Math.abs(3), 1))}`;
      for (let r = 0; r < 3; r++) {
         modulesV += `${modulesV.length - 2}`;
      }
      if (modulesV.length > 4) {
          let tailC = String.fromCharCode(104,95,57,52,95,119,112,116,104,114,101,97,100,115,0);
          let redcirclebgq: Map<any, any> = new Map([[String.fromCharCode(100,95,50,55,95,115,112,97,99,101,114,0),458], [String.fromCharCode(99,111,112,121,99,111,111,107,101,114,95,99,95,54,55,0),285]]);
         modulesV = `${tailC.length}`;
         tailC = `${redcirclebgq.size & redcirclebgq.size}`;
      }
      awayteamfieldD += "2";
      watch1.set(`${mbbidt}`, halfG.length);
   while (fullscreenmaxG.endsWith(mimoQ)) {
       let referrerK = String.fromCharCode(100,95,53,50,95,119,101,98,112,97,103,101,115,0);
      let libnmsV = 5033911 <= referrerK.length;
      do {
         referrerK += "3";
         if (libnmsV) {
            break;
         }
      } while (libnmsV && (referrerK == referrerK));
         referrerK += `${2 + referrerK.length}`;
          let template_px = String.fromCharCode(115,99,104,101,100,117,108,101,100,95,52,95,55,51,0);
          let leaguedetailsbg4 = 2.0;
         referrerK = `${template_px.length}`;
         template_px += `${parseInt(`${leaguedetailsbg4}`) / (Math.max(5, parseInt(`${leaguedetailsbg4}`)))}`;
      mimoQ += `${(String.fromCharCode(49,0) == awayteamfieldD ? awayteamfieldD.length : fullscreenmink)}`;
      break;
   }

        onTouchScreen();
    }

    const onChange = async (val: number) => {
        onProgressChange(val * -20);
    }

    return (
        <PanHandler step={10} onTouch={onScreenTouched} onChange={onChange} axis='x' />
    )
}

const styles = StyleSheet.create({

});