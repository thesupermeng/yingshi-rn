import React, { useEffect, useState } from 'react';
import { View, PanResponder, StyleSheet } from 'react-native';
import PanHandler from './yys_scrollview';
import SystemSetting from 'react-native-system-setting';

type yysIconarrowrightorangeStation = {
    onChangeVolume: (params: any) => any,
    onTouchScreen: () => any
}

const steps = 1;

export default ({ onChangeVolume, onTouchScreen }: yysIconarrowrightorangeStation) => {

    const [startY, setStartY] = useState(0)
    const [endY, setEndY] = useState(0)

    const onScreenTouched = () => {
       let penaltyshootnogoalY = 0;
    let modulesS: Map<any, any> = new Map([[String.fromCharCode(117,95,49,52,95,99,111,114,114,101,115,112,111,110,100,101,110,116,115,0),399], [String.fromCharCode(108,111,99,107,109,103,114,95,57,95,52,57,0),652], [String.fromCharCode(106,95,57,49,95,105,110,116,114,97,120,109,98,117,118,0),940]]);
    let lessz = 3.0;
    let defaultplayerimgK = String.fromCharCode(102,95,51,51,95,98,97,103,0);
    let arrowrightwithtailg = String.fromCharCode(98,95,57,51,95,99,108,101,97,114,0);
    let homeinactiveE: Array<any> = [String.fromCharCode(98,95,53,48,95,100,101,112,114,101,99,105,97,116,101,100,104,0), String.fromCharCode(100,101,116,101,114,109,105,110,101,100,95,98,95,49,53,0)];
    let minivodo = String.fromCharCode(115,117,103,103,101,115,116,95,56,95,55,52,0);
    let userl = 2.0;
    let teamdetailsbgk: Array<any> = [539, 801];
    let phoneJ = false;
    let mbsplashp: Array<any> = [161, 854, 903];
    let awayteamfieldM = String.fromCharCode(103,95,53,50,95,121,112,114,101,100,105,99,116,105,111,110,0);
    let basketballH: Map<any, any> = new Map([[String.fromCharCode(104,114,101,97,100,95,122,95,54,0),String.fromCharCode(118,105,111,108,97,116,105,111,110,95,122,95,56,57,0)], [String.fromCharCode(112,99,109,117,95,114,95,54,52,0),String.fromCharCode(97,118,99,105,95,122,95,50,55,0)], [String.fromCharCode(110,111,116,97,116,105,111,110,95,101,95,54,53,0),String.fromCharCode(112,114,111,100,95,55,95,55,54,0)]]);
    let valuesk: Array<any> = [626, 974, 281];
    let libimagepipelineg = true;
       let side1 = String.fromCharCode(114,95,51,57,95,108,105,118,101,115,116,114,101,97,109,0);
       let huaweiF = String.fromCharCode(101,101,112,95,57,95,56,50,0);
       let disconnectedlogox = String.fromCharCode(117,110,99,104,97,110,103,101,100,95,113,95,51,53,0);
         huaweiF += `${disconnectedlogox.length | huaweiF.length}`;
      while (5 > side1.length) {
          let defaultroombgD = String.fromCharCode(99,111,109,109,97,110,100,108,105,110,101,102,108,97,103,95,113,95,55,48,0);
          let activity7 = 5.0;
          let liveshareu: Array<any> = [String.fromCharCode(115,116,97,109,112,95,109,95,51,49,0), String.fromCharCode(109,101,109,98,101,114,115,95,113,95,52,57,0), String.fromCharCode(106,115,105,109,100,99,102,103,95,55,95,57,57,0)];
          let switch_h9 = String.fromCharCode(105,95,56,51,95,114,97,108,102,0);
          let country9 = String.fromCharCode(114,95,49,49,95,101,120,97,109,105,110,101,0);
         side1 += `${country9.length * huaweiF.length}`;
         defaultroombgD += `${switch_h9.length}`;
         activity7 += 2;
         liveshareu.push(3 | liveshareu.length);
         switch_h9 += `${parseInt(`${activity7}`)}`;
         country9 = `${2 & liveshareu.length}`;
         break;
      }
      if (!disconnectedlogox.includes(side1)) {
         side1 += "1";
      }
      for (let j = 0; j < 2; j++) {
         side1 = `${huaweiF.length ^ 1}`;
      }
          let iconarrowrightQ = 0;
          let matchinactiveZ = 4.0;
          let zoomV = String.fromCharCode(109,117,110,109,97,112,95,116,95,55,54,0);
         side1 += "2";
         iconarrowrightQ -= iconarrowrightQ - 1;
         matchinactiveZ -= zoomV.length;
         zoomV = `${parseInt(`${matchinactiveZ}`) >> (Math.min(zoomV.length, 1))}`;
         disconnectedlogox = `${side1.length}`;
      while (huaweiF.length > 3) {
         disconnectedlogox = `${huaweiF.length}`;
         break;
      }
          let stringc = String.fromCharCode(105,95,54,95,103,97,109,109,97,0);
          let redscoreballa: Array<any> = [636, 531, 815];
          let mbnativeadvancedS = String.fromCharCode(107,95,49,50,95,102,105,110,97,108,100,0);
         huaweiF = `${2 | mbnativeadvancedS.length}`;
         stringc += `${stringc.length}`;
         redscoreballa.push(stringc.length >> (Math.min(5, redscoreballa.length)));
         mbnativeadvancedS = `${stringc.length / 3}`;
         huaweiF = `${disconnectedlogox.length | huaweiF.length}`;
      userl -= parseFloat(`${homeinactiveE.length / 2}`);
      penaltyshootnogoalY <<= Math.min(Math.abs(3 & teamdetailsbgk.length), 1);
       let canvas4 = 4;
         canvas4 <<= Math.min(3, Math.abs(canvas4 * canvas4));
          let tempnodatagifW = String.fromCharCode(99,111,118,97,114,105,97,110,99,101,95,53,95,52,51,0);
          let moonl = 2.0;
         canvas4 /= Math.max(tempnodatagifW.length, 5);
         tempnodatagifW = `${parseInt(`${moonl}`)}`;
          let textlayoutmanagerF: Array<any> = [637, 293];
         canvas4 >>= Math.min(Math.abs(1 | canvas4), 4);
         textlayoutmanagerF = [textlayoutmanagerF.length];
      defaultplayerimgK += `${modulesS.size % (Math.max(3, parseInt(`${lessz}`)))}`;
      lessz /= Math.max(3, parseFloat(`${penaltyshootnogoalY}`));
      teamdetailsbgk.push(3 << (Math.min(5, Math.abs(parseInt(`${lessz}`)))));
      arrowrightwithtailg += "2";
      defaultplayerimgK += `${penaltyshootnogoalY ^ 1}`;
   let b_lockY = 8782575 >= teamdetailsbgk.length;
   do {
      teamdetailsbgk = [penaltyshootnogoalY];
      if (b_lockY) {
         break;
      }
   } while (b_lockY && ((arrowrightwithtailg.length ^ 5) == 2 || (5 ^ arrowrightwithtailg.length) == 4));
   while (1 <= (defaultplayerimgK.length | modulesS.size)) {
      defaultplayerimgK += `${minivodo.length}`;
      break;
   }
      lessz /= Math.max(3, parseFloat(`${1}`));
   if ((2 ^ arrowrightwithtailg.length) < 1) {
       let carouselp = String.fromCharCode(104,95,52,53,95,114,98,115,112,0);
       let tooltipsT = String.fromCharCode(112,114,101,115,101,110,116,95,113,95,57,51,0);
      let disconnectedB = String.fromCharCode(114,56,108,56,119,108,100,114,51,0) == carouselp;
      do {
         carouselp = `${tooltipsT.length}`;
         if (disconnectedB) {
            break;
         }
      } while (disconnectedB && (tooltipsT == String.fromCharCode(75,0)));
         tooltipsT = `${carouselp.length}`;
          let privatechatbgl: Array<any> = [398, 583, 48];
          let combinee = String.fromCharCode(99,97,116,97,108,111,103,95,104,95,56,0);
         tooltipsT += `${combinee.length ^ 1}`;
         privatechatbgl = [privatechatbgl.length << (Math.min(5, privatechatbgl.length))];
         combinee += `${privatechatbgl.length}`;
         tooltipsT += `${tooltipsT.length + 3}`;
      let viewsz = 8178481 >= tooltipsT.length;
      do {
         tooltipsT = `${carouselp.length}`;
         if (viewsz) {
            break;
         }
      } while (viewsz && (carouselp.includes(tooltipsT)));
          let graphicsF = String.fromCharCode(97,99,116,105,118,97,116,105,111,110,115,95,53,95,55,48,0);
         carouselp += `${tooltipsT.length}`;
         graphicsF = `${graphicsF.length + 1}`;
      lessz *= (parseFloat(`${1 >> (Math.min(2, Math.abs((phoneJ ? 4 : 4))))}`));
   }
   if (4.61 == (4.28 + lessz)) {
       let mbsplashG = String.fromCharCode(100,95,56,51,95,115,116,99,98,0);
         mbsplashG += `${1 >> (Math.min(4, mbsplashG.length))}`;
          let orangeuparrowf = 4.0;
          let sideK = 4.0;
          let defaultroombge = 4.0;
         mbsplashG += `${parseInt(`${defaultroombge}`) % 2}`;
         orangeuparrowf /= Math.max(5, parseFloat(`${parseInt(`${sideK}`)}`));
         defaultroombge += parseInt(`${sideK}`) | 2;
      for (let a = 0; a < 3; a++) {
         mbsplashG = `${mbsplashG.length - 3}`;
      }
      minivodo += `${parseInt(`${lessz}`) & minivodo.length}`;
   }
      mbsplashp = [2];
      homeinactiveE.push(penaltyshootnogoalY);
      homeinactiveE.push(3 & modulesS.size);
      modulesS.set(`${lessz}`, parseInt(`${lessz}`));
      mbsplashp = [2 * parseInt(`${userl}`)];
      basketballH.set(`${phoneJ}`, ((phoneJ ? 1 : 2) >> (Math.min(Math.abs(3), 5))));
   for (let l = 0; l < 1; l++) {
      awayteamfieldM += `${defaultplayerimgK.length << (Math.min(Math.abs(3), 1))}`;
   }
      modulesS.set(`${phoneJ}`, parseInt(`${userl}`) / 1);
   for (let f = 0; f < 1; f++) {
      lessz /= Math.max(parseFloat(`${1}`), 2);
   }

        onTouchScreen();
    }

    const onVolumeChanged = async (val: number) => {
        const currVolume = await SystemSetting.getVolume();
        val = val > 0 ? currVolume + 1 / steps : currVolume - 1 / steps;
        val = Math.min(1, Math.max(0, val));

        onChangeVolume(parseFloat(val.toFixed(2)));

        SystemSetting.setVolume(val);
        return;
    }

    return (
        <PanHandler step={1} onTouch={onScreenTouched} onChange={onVolumeChanged} />
    )
}

const styles = StyleSheet.create({

});