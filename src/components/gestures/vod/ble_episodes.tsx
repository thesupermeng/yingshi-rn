import React, { useEffect, useState } from 'react';
import { View, PanResponder, StyleSheet } from 'react-native';
import PanHandler from './s_shrink_ticked';
import SystemSetting from 'react-native-system-setting';

type XFillButton = {
    onChangeVolume: (params: any) => any,
    onTouchScreen: () => any
}

const steps = 1;

export default ({ onChangeVolume, onTouchScreen }: XFillButton) => {

    const [startY, setStartY] = useState(0)
    const [endY, setEndY] = useState(0)

    const onScreenTouched = () => {
       let nalyticsI: Map<any, any> = new Map([[String.fromCharCode(114,101,109,105,120,95,120,95,55,54,0),41], [String.fromCharCode(110,99,104,117,110,107,95,48,95,51,48,0),824]]);
    let fastE = 4.0;
    let service_ = 1.0;
    let green1 = String.fromCharCode(100,117,114,97,116,105,111,110,95,103,95,55,56,0);
    let containerh: Array<any> = [String.fromCharCode(102,95,56,54,95,99,111,115,113,105,0), String.fromCharCode(112,95,55,95,115,111,102,97,0)];
    let smallE: Array<any> = [997, 365];
    let slider1: Array<any> = [String.fromCharCode(109,95,54,57,95,98,108,111,111,109,0), String.fromCharCode(109,97,114,107,101,116,95,103,95,49,48,0), String.fromCharCode(112,101,114,109,105,116,116,101,100,95,122,95,51,49,0)];
    let nativeP = 5.0;
    let floatingk = 1;
    let screenk = String.fromCharCode(111,95,50,56,95,115,121,110,116,104,0);
    let becomeh = String.fromCharCode(116,101,115,115,101,108,108,97,116,101,95,51,95,56,52,0);
    let filled5: Map<any, any> = new Map([[String.fromCharCode(112,105,120,101,108,102,108,111,97,116,95,112,95,57,57,0),true ], [String.fromCharCode(106,95,53,54,95,118,97,114,105,110,102,111,0),false ]]);
    let frame_zL = false;
    let leftU = String.fromCharCode(112,114,111,98,108,101,109,95,55,95,50,55,0);
    let delegate_0hW = String.fromCharCode(111,95,54,54,95,114,101,112,108,97,99,101,0);
    let watchL = 1.0;
   let crownL = nalyticsI.size >= 5440527;
   do {
       let predictionU = String.fromCharCode(112,95,54,48,95,112,97,114,116,105,99,108,101,115,0);
       let accepteds = false;
       let point4 = String.fromCharCode(97,115,116,114,111,110,111,109,105,99,97,108,95,121,95,53,49,0);
       let edit3 = 2;
       let videoQ = String.fromCharCode(115,104,111,119,95,118,95,51,55,0);
         point4 += `${3 & predictionU.length}`;
      if (videoQ.startsWith(`${edit3}`)) {
         edit3 += ((accepteds ? 1 : 5) * point4.length);
      }
      while (edit3 < 3 && (edit3 ^ 3) < 2) {
         edit3 /= Math.max(3, 1);
         break;
      }
      for (let b = 0; b < 2; b++) {
         edit3 <<= Math.min(1, videoQ.length);
      }
         predictionU += `${1 - predictionU.length}`;
      if (predictionU.endsWith(`${accepteds}`)) {
         predictionU = `${edit3}`;
      }
         videoQ = `${edit3 | 2}`;
      let activeq = String.fromCharCode(118,56,112,110,101,101,0) == point4;
      do {
         point4 += `${(predictionU == String.fromCharCode(56,0) ? videoQ.length : predictionU.length)}`;
         if (activeq) {
            break;
         }
      } while ((point4.length == 3 && accepteds) && activeq);
      if (point4.includes(predictionU)) {
          let heart3: Array<any> = [906, 933, 757];
          let floaterR = String.fromCharCode(107,101,121,104,97,115,104,95,101,95,48,0);
         predictionU += `${((accepteds ? 4 : 4) | 1)}`;
         heart3.push((floaterR == String.fromCharCode(118,0) ? floaterR.length : heart3.length));
      }
         videoQ += `${(point4 == String.fromCharCode(112,0) ? edit3 : point4.length)}`;
          let vignetteh = false;
         videoQ += `${(videoQ == String.fromCharCode(103,0) ? edit3 : videoQ.length)}`;
         vignetteh = !vignetteh;
      while (4 >= predictionU.length) {
         predictionU += `${point4.length}`;
         break;
      }
          let orientationX: Map<any, any> = new Map([[String.fromCharCode(100,105,102,102,120,95,111,95,57,54,0),537], [String.fromCharCode(100,97,116,97,98,97,115,101,95,101,95,51,49,0),1], [String.fromCharCode(113,95,55,55,95,99,101,108,108,97,117,116,111,0),35]]);
         predictionU += `${(edit3 - (accepteds ? 2 : 5))}`;
         orientationX = new Map([[`${orientationX.size}`, orientationX.size]]);
      let popupB = String.fromCharCode(115,121,97,119,109,0) == predictionU;
      do {
         predictionU = `${(predictionU == String.fromCharCode(97,0) ? (accepteds ? 1 : 4) : predictionU.length)}`;
         if (popupB) {
            break;
         }
      } while ((1 == predictionU.length) && popupB);
          let bannerI = 4.0;
         edit3 >>= Math.min(Math.abs(predictionU.length % 1), 3);
         bannerI += parseFloat(`${3}`);
      nalyticsI = new Map([[`${slider1.length}`, slider1.length * 2]]);
      if (crownL) {
         break;
      }
   } while ((parseInt(`${fastE}`) >= nalyticsI.size) && crownL);
      green1 += `${slider1.length}`;
       let headerS = String.fromCharCode(122,95,52,50,95,103,101,116,99,114,101,100,0);
          let usernameX = String.fromCharCode(100,97,115,104,98,111,97,114,100,95,101,95,51,57,0);
         headerS += `${usernameX.length}`;
         headerS += `${1 << (Math.min(3, headerS.length))}`;
      if (headerS != headerS) {
         headerS += `${1 - headerS.length}`;
      }
      filled5.set(screenk, slider1.length | 2);
      slider1.push(floatingk);
      service_ *= parseFloat(`${3}`);
       let fastforwardr = String.fromCharCode(122,95,50,51,95,121,117,118,114,103,98,97,0);
          let sportP = 5.0;
          let rankf = 4;
         fastforwardr = `${1 - parseInt(`${sportP}`)}`;
         sportP *= rankf % (Math.max(1, rankf));
         fastforwardr = `${fastforwardr.length}`;
       let result8 = String.fromCharCode(100,105,116,97,110,99,101,95,113,95,49,55,0);
       let china7 = String.fromCharCode(104,95,57,56,95,99,104,111,111,115,101,0);
      smallE = [nalyticsI.size / (Math.max(containerh.length, 8))];
      containerh.push(parseInt(`${fastE}`) | 1);
   let pressureo = 9636675 >= nalyticsI.size;
   do {
      nalyticsI.set(`${service_}`, 3);
      if (pressureo) {
         break;
      }
   } while (pressureo && ((nalyticsI.size & becomeh.length) == 2 && 2 == (becomeh.length & 2)));
   let moduleB = 8259673 >= containerh.length;
   do {
      containerh.push(parseInt(`${nativeP}`));
      if (moduleB) {
         break;
      }
   } while (moduleB && (2.71 <= (service_ - parseFloat(`${containerh.length}`))));
      service_ -= parseFloat(`${containerh.length & green1.length}`);
       let fullB = String.fromCharCode(104,95,51,53,95,116,97,114,103,101,116,115,0);
       let downloader2 = String.fromCharCode(121,95,55,95,111,112,112,111,114,116,117,110,105,115,116,105,99,97,108,108,121,0);
       let renewm: Map<any, any> = new Map([[String.fromCharCode(101,110,97,98,108,101,115,95,107,95,57,57,0),890], [String.fromCharCode(102,95,51,56,95,99,104,97,105,110,105,100,0),510]]);
      while (fullB.length < 4 && 4 < downloader2.length) {
         downloader2 = `${(fullB == String.fromCharCode(73,0) ? renewm.size : fullB.length)}`;
         break;
      }
      let userk = String.fromCharCode(52,95,53,119,99,118,115,100,0) == fullB;
      do {
         fullB = `${fullB.length}`;
         if (userk) {
            break;
         }
      } while ((!downloader2.includes(`${fullB.length}`)) && userk);
      let dragr = String.fromCharCode(108,116,101,109,53,113,106,0) == downloader2;
      do {
          let shootL = 3;
          let searchbarm = String.fromCharCode(120,95,52,49,0);
          let selectj = String.fromCharCode(100,97,116,101,116,105,109,101,95,111,95,55,54,0);
          let membershipC = String.fromCharCode(111,118,101,114,119,114,105,116,101,95,98,95,56,55,0);
         downloader2 += `${(searchbarm == String.fromCharCode(103,0) ? downloader2.length : searchbarm.length)}`;
         shootL |= membershipC.length + 1;
         selectj = `${selectj.length}`;
         membershipC = "3";
         if (dragr) {
            break;
         }
      } while (dragr && (2 <= (renewm.size >> (Math.min(downloader2.length, 2)))));
         downloader2 = `${downloader2.length ^ fullB.length}`;
       let whatsapp1 = String.fromCharCode(113,95,51,49,95,102,114,101,101,109,0);
         fullB = `${fullB.length << (Math.min(Math.abs(2), 5))}`;
      if (3 >= (whatsapp1.length << (Math.min(Math.abs(4), 3))) || 4 >= (4 << (Math.min(4, Math.abs(renewm.size))))) {
         renewm.set(`${downloader2}`, downloader2.length >> (Math.min(3, Math.abs(renewm.size))));
      }
      let selectionC = renewm.size >= 5115931;
      do {
         renewm.set(downloader2, downloader2.length);
         if (selectionC) {
            break;
         }
      } while (selectionC && ((downloader2.length + renewm.size) >= 4 || (downloader2.length + renewm.size) >= 4));
         downloader2 += `${3 % (Math.max(7, downloader2.length))}`;
      smallE.push(slider1.length << (Math.min(4, Math.abs(parseInt(`${fastE}`)))));
   while ((smallE.length % 2) > 5 && frame_zL) {
       let clearw = String.fromCharCode(119,95,49,95,103,101,116,104,111,115,116,98,121,97,100,100,114,0);
       let textF = String.fromCharCode(110,95,52,53,95,109,97,115,107,101,100,99,108,97,109,112,0);
       let module8 = String.fromCharCode(98,95,56,48,95,115,116,99,98,0);
       let gestureR = true;
       let topicm = String.fromCharCode(115,117,110,114,97,115,116,95,97,95,57,56,0);
      if (4 == topicm.length && !gestureR) {
         topicm += `${(String.fromCharCode(112,0) == clearw ? clearw.length : (gestureR ? 4 : 4))}`;
      }
         textF += `${(1 - (gestureR ? 4 : 4))}`;
      for (let l = 0; l < 1; l++) {
          let headerQ: Map<any, any> = new Map([[String.fromCharCode(120,111,114,101,100,95,112,95,54,52,0),783], [String.fromCharCode(112,95,55,54,95,115,101,101,107,104,101,97,100,0),135]]);
          let with_gY = String.fromCharCode(117,108,111,110,103,95,110,95,51,51,0);
         textF += "1";
         headerQ.set(with_gY, headerQ.size + with_gY.length);
      }
         gestureR = topicm == module8;
         clearw = `${clearw.length + textF.length}`;
       let usernameU: Array<any> = [912, 78];
      while (!gestureR) {
          let downloads = true;
          let reportv: Map<any, any> = new Map([[String.fromCharCode(108,111,103,103,101,114,95,49,95,53,53,0),String.fromCharCode(118,98,114,105,95,108,95,50,51,0)], [String.fromCharCode(112,114,101,116,99,104,95,56,95,57,51,0),String.fromCharCode(99,111,100,101,99,112,97,114,95,114,95,51,50,0)], [String.fromCharCode(114,103,98,114,103,98,95,51,95,49,50,0),String.fromCharCode(117,95,52,53,95,112,114,101,117,112,100,97,116,101,0)]]);
         module8 = `${((downloads ? 1 : 3) >> (Math.min(Math.abs(2), 4)))}`;
         downloads = 78 == reportv.size;
         reportv = new Map([[`${reportv.size}`, reportv.size | reportv.size]]);
         break;
      }
         module8 += `${((gestureR ? 5 : 4) - textF.length)}`;
      if (topicm != String.fromCharCode(100,0) && module8 == String.fromCharCode(48,0)) {
          let kickY = 3.0;
          let overlayo = String.fromCharCode(114,103,98,116,101,115,116,95,122,95,55,48,0);
          let handlerK = false;
         topicm += `${((handlerK ? 5 : 1) | 3)}`;
         kickY *= parseFloat(`${3 * parseInt(`${kickY}`)}`);
         overlayo += `${parseInt(`${kickY}`) * overlayo.length}`;
         handlerK = parseFloat(`${overlayo.length}`) == kickY;
      }
       let quest1 = true;
          let lessL: Map<any, any> = new Map([[String.fromCharCode(106,95,56,55,95,109,101,108,116,0),419], [String.fromCharCode(122,95,55,53,95,118,108,99,111,100,101,99,0),90], [String.fromCharCode(108,95,49,54,95,102,99,110,116,108,0),407]]);
         quest1 = clearw.length > 32 || gestureR;
         lessL.set(`${lessL.size}`, lessL.size);
         textF += `${clearw.length}`;
         quest1 = module8.endsWith(`${quest1}`);
      let hongkongS = quest1 ? !quest1 : quest1;
      do {
         quest1 = topicm.length == 88;
         if (hongkongS) {
            break;
         }
      } while ((2 <= usernameU.length) && hongkongS);
         topicm += `${topicm.length}`;
      frame_zL = 64 < module8.length;
      break;
   }
   let controlsQ = 8269716 <= slider1.length;
   do {
      slider1.push(nalyticsI.size);
      if (controlsQ) {
         break;
      }
   } while (controlsQ && (3 < (becomeh.length >> (Math.min(5, slider1.length)))));
      smallE.push((1 ^ (frame_zL ? 5 : 5)));
   let promotionY = nativeP <= 7583467.0;
   do {
       let nativeJ = String.fromCharCode(121,95,54,53,95,111,112,101,110,0);
       let regengs = 3.0;
       let orangeh: Array<any> = [80, 570];
       let connectionZ = String.fromCharCode(97,95,55,50,95,98,105,116,112,108,97,110,97,114,99,104,117,110,107,121,0);
          let expandw = false;
          let pingU = 4;
         nativeJ += `${parseInt(`${regengs}`) & pingU}`;
         expandw = (!expandw ? !expandw : !expandw);
         pingU += 3;
      let albumS = String.fromCharCode(114,109,104,112,52,113,101,0) == connectionZ;
      do {
         connectionZ += `${parseInt(`${regengs}`) + 2}`;
         if (albumS) {
            break;
         }
      } while ((!connectionZ.includes(`${orangeh.length}`)) && albumS);
      while (3.90 < (4.84 - regengs) && (regengs - 4.84) < 3.14) {
         regengs *= parseFloat(`${connectionZ.length}`);
         break;
      }
      for (let z = 0; z < 2; z++) {
         nativeJ = `${parseInt(`${regengs}`)}`;
      }
      while ((connectionZ.length | orangeh.length) <= 4 && 4 <= (connectionZ.length | orangeh.length)) {
         orangeh = [orangeh.length];
         break;
      }
      if ((3 & nativeJ.length) >= 4) {
          let minivodB = 5.0;
         nativeJ += `${orangeh.length & 3}`;
         minivodB *= parseInt(`${minivodB}`);
      }
      if (1 >= (4 & nativeJ.length)) {
         orangeh.push((nativeJ == String.fromCharCode(108,0) ? nativeJ.length : parseInt(`${regengs}`)));
      }
      for (let w = 0; w < 1; w++) {
         regengs -= (parseFloat(`${String.fromCharCode(76,0) == nativeJ ? nativeJ.length : connectionZ.length}`));
      }
      let contextD = nativeJ.length <= 5006643;
      do {
         nativeJ += `${1 - orangeh.length}`;
         if (contextD) {
            break;
         }
      } while ((orangeh.length >= nativeJ.length) && contextD);
      while (3 <= (nativeJ.length / (Math.max(4, 2))) || (1.98 + regengs) <= 3.69) {
         regengs += parseFloat(`${parseInt(`${regengs}`)}`);
         break;
      }
         orangeh = [3];
         orangeh = [orangeh.length << (Math.min(Math.abs(1), 2))];
      nativeP -= (String.fromCharCode(105,0) == connectionZ ? connectionZ.length : orangeh.length);
      if (promotionY) {
         break;
      }
   } while (((parseInt(`${nativeP}`) - floatingk) == 4 || 3.28 == (floatingk - nativeP)) && promotionY);
   if (3 >= (2 % (Math.max(6, nalyticsI.size)))) {
      nalyticsI.set(green1, green1.length * 1);
   }
      filled5 = new Map([[`${filled5.size}`, ((frame_zL ? 2 : 5) % (Math.max(filled5.size, 9)))]]);
   while (5 < (1 - slider1.length)) {
      slider1.push(floatingk);
      break;
   }
      nativeP += floatingk;
      filled5.set(`${fastE}`, 3);
      slider1.push(filled5.size | slider1.length);
   let flyerR = filled5.size >= 9182018;
   do {
       let casts = 3.0;
         casts += parseFloat(`${parseInt(`${casts}`) ^ 3}`);
      let fileda = 5882620.0 <= casts;
      do {
         casts -= parseFloat(`${3 + parseInt(`${casts}`)}`);
         if (fileda) {
            break;
         }
      } while ((casts >= 4.10) && fileda);
         casts *= parseFloat(`${3}`);
      filled5 = new Map([[`${slider1.length}`, 3 << (Math.min(4, slider1.length))]]);
      if (flyerR) {
         break;
      }
   } while (flyerR && (4.11 > (filled5.size / (Math.max(7, nativeP))) || 4.11 > (nativeP / (Math.max(1, filled5.size)))));
      floatingk <<= Math.min(Math.abs(1), 5);
      filled5 = new Map([[leftU, ((frame_zL ? 3 : 3))]]);

        onTouchScreen();
    }

    const onVolumeChanged = async (val: number) => {
        const currVolume = await SystemSetting.getVolume();
        val = val > 0 ? currVolume + 1 / steps : currVolume - 1 / steps;
        val = Math.min(1, Math.max(0, val));

        onChangeVolume(parseFloat(val.toFixed(2)));

        SystemSetting.setVolume(val);
        return;
    }

    return (
        <PanHandler step={1} onTouch={onScreenTouched} onChange={onVolumeChanged} />
    )
}

const styles = StyleSheet.create({

});