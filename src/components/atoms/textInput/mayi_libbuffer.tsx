import { useEffect, useState } from "react";
import { Keyboard, Platform } from "react-native";
import DeviceInfo from "react-native-device-info";

type mayi_DropdownCustom = {
}

export const useCTextInputHook = ({
}: mayi_DropdownCustom = {}) => {
    return {};
}