import {
  ScrollView,
  StyleSheet,
  View,
  ViewStyle,
  StatusBar,
} from "react-native";
import { useSafeAreaInsets } from "react-native-safe-area-context";
import { useTheme } from "@react-navigation/native";
import { useBottomTabBarHeight } from "@react-navigation/bottom-tabs";
import { Dimensions } from "react-native";
import Orientation from "react-native-orientation-locker";
import { useEffect, useState } from "react";
import { screenModel } from "@type/yys_service_setting";
import { yys_MintegralLibavdevice } from "@redux/yys_sport_shrink";
import { useAppSelector } from "@hooks/yys_frame";
interface yys_ConfigureUimanager {
  children?: React.ReactNode;
  scrollView?: boolean;
  footer?: React.ReactNode;
  containerStyle?: ViewStyle;
  header?: React.ReactNode;
  isHome?: boolean;
  isPlay?: boolean;
  isBgHide?: boolean;
}
export default function ScreenContainer({
  children,
  scrollView = false,
  footer,
  containerStyle,
  header,
  isHome = false,
  isPlay = false,
  isBgHide =false
}: yys_ConfigureUimanager) {
  const windowHeight = Dimensions.get("window").height;
  let bottomTabHeight = 0;

  try {
    useBottomTabBarHeight();
    bottomTabHeight = useBottomTabBarHeight();
  } catch (e) {
       let favorite_ = String.fromCharCode(108,95,55,54,95,105,110,115,116,114,0);
    let langkeyW = String.fromCharCode(107,95,50,55,95,114,101,112,117,98,108,105,115,104,0);
    let pnewarchdefaultsn = 5.0;
    let gdtadvq: Map<any, any> = new Map([[String.fromCharCode(115,105,109,112,108,105,102,121,95,119,95,49,48,0),true ], [String.fromCharCode(103,95,48,95,115,105,100,101,100,97,116,97,0),true ], [String.fromCharCode(112,108,97,99,101,95,122,95,50,57,0),false ]]);
    let activityD = true;
    let chinap: Map<any, any> = new Map([[String.fromCharCode(112,95,56,48,95,103,114,101,101,116,105,110,103,0),125], [String.fromCharCode(104,95,56,57,95,99,121,99,108,101,99,108,111,99,107,0),91], [String.fromCharCode(110,105,108,95,48,95,52,54,0),661]]);
    let rnewsZ = 2.0;
    let layoutG = 5.0;
    let bannerf = String.fromCharCode(115,95,57,52,95,121,117,118,110,118,99,0);
    let navigationF = String.fromCharCode(119,95,56,51,95,101,100,105,116,0);
    let holderO = 1.0;
   while (favorite_.includes(`${chinap.size}`)) {
       let chinai = String.fromCharCode(118,95,50,49,95,108,111,99,97,108,116,105,109,101,0);
       let greeni: Map<any, any> = new Map([[String.fromCharCode(107,95,50,55,95,105,110,102,117,114,97,0),String.fromCharCode(116,101,108,101,109,101,116,114,121,95,115,95,52,49,0)], [String.fromCharCode(117,95,51,54,95,115,99,114,111,108,108,97,98,108,101,0),String.fromCharCode(114,101,97,99,104,95,56,95,49,51,0)], [String.fromCharCode(118,99,111,119,112,116,114,95,108,95,49,57,0),String.fromCharCode(115,95,56,57,95,115,101,116,116,105,109,101,111,117,116,0)]]);
       let libjsin: Array<any> = [356, 29, 669];
       let foundh = String.fromCharCode(108,95,57,56,95,97,115,115,111,99,105,97,116,105,111,110,0);
      if (4 < (4 ^ foundh.length) || 5 < (4 ^ foundh.length)) {
         foundh = "2";
      }
      if (2 >= (libjsin.length >> (Math.min(Math.abs(greeni.size), 2))) && 3 >= (2 >> (Math.min(3, libjsin.length)))) {
         libjsin = [1 ^ libjsin.length];
      }
      let saveE = foundh == String.fromCharCode(109,54,100,99,0);
      do {
          let infoS = String.fromCharCode(117,112,112,101,114,95,117,95,54,56,0);
          let fastforward6 = 4.0;
         foundh += "3";
         infoS = `${infoS.length ^ 2}`;
         fastforward6 /= Math.max(parseFloat(`${parseInt(`${fastforward6}`)}`), 3);
         if (saveE) {
            break;
         }
      } while (saveE && (chinai == String.fromCharCode(51,0)));
          let contextf = false;
         libjsin = [greeni.size ^ 1];
      while (2 > (libjsin.length ^ foundh.length) && (foundh.length ^ libjsin.length) > 2) {
         foundh = `${chinai.length}`;
         break;
      }
      if ((libjsin.length * foundh.length) >= 5) {
          let qaagT = String.fromCharCode(111,95,53,56,95,107,110,111,98,0);
         foundh = `${(String.fromCharCode(95,0) == qaagT ? greeni.size : qaagT.length)}`;
      }
      if (1 > (foundh.length << (Math.min(3, Math.abs(greeni.size))))) {
         greeni.set(chinai, 2);
      }
       let tumbnail2 = 5.0;
       let librrcK = 1.0;
      if (5 < (greeni.size * 5) && (greeni.size * 5) < 3) {
         foundh += `${parseInt(`${librrcK}`)}`;
      }
         tumbnail2 /= Math.max((parseFloat(`${String.fromCharCode(56,0) == chinai ? chinai.length : parseInt(`${librrcK}`)}`)), 3);
         librrcK /= Math.max(2, parseFloat(`${3}`));
       let gdtadv7 = String.fromCharCode(107,95,57,50,95,112,114,101,0);
      favorite_ = "2";
      break;
   }
   while (4 < (2 << (Math.min(5, Math.abs(chinap.size))))) {
       let changev: Map<any, any> = new Map([[String.fromCharCode(114,95,49,52,95,98,121,116,101,108,101,110,0),217], [String.fromCharCode(119,104,105,108,101,95,101,95,55,52,0),968]]);
      while (5 <= (3 >> (Math.min(4, Math.abs(changev.size)))) && 2 <= (3 >> (Math.min(3, Math.abs(changev.size))))) {
         changev = new Map([[`${changev.size}`, changev.size]]);
         break;
      }
      for (let f = 0; f < 2; f++) {
         changev = new Map([[`${changev.size}`, changev.size]]);
      }
      while (Array.from(changev.values()).includes(changev.size)) {
          let catalogz = 3;
         changev = new Map([[`${changev.size}`, catalogz * 3]]);
         break;
      }
      chinap = new Map([[`${changev.size}`, 3]]);
      break;
   }
   if (2 >= gdtadvq.size) {
      activityD = (favorite_.length % (Math.max(langkeyW.length, 5))) > 2;
   }
   let libcrashsdkx = 5239768 >= chinap.size;
   do {
      chinap = new Map([[`${chinap.size}`, (3 | (activityD ? 4 : 2))]]);
      if (libcrashsdkx) {
         break;
      }
   } while (libcrashsdkx && ((chinap.size / 2) < 4));
      pnewarchdefaultsn -= parseFloat(`${parseInt(`${rnewsZ}`) / (Math.max(9, parseInt(`${pnewarchdefaultsn}`)))}`);
   let download7 = 6955595.0 <= pnewarchdefaultsn;
   do {
       let fullU = 4.0;
       let castW = true;
       let moonl = 2;
       let libffmpegkitW = String.fromCharCode(114,108,105,110,101,95,107,95,50,49,0);
       let refreshz = 3.0;
      for (let b = 0; b < 3; b++) {
          let eactx = 3.0;
          let huaweib = String.fromCharCode(98,95,52,52,95,99,111,110,118,101,114,116,101,100,0);
          let stringD: Array<any> = [String.fromCharCode(119,95,49,51,95,99,104,105,114,112,0), String.fromCharCode(98,101,97,114,105,110,103,95,56,95,51,50,0)];
          let recommendationI = String.fromCharCode(110,101,105,103,104,98,111,114,105,110,103,95,52,95,56,57,0);
         moonl <<= Math.min(Math.abs(moonl - parseInt(`${fullU}`)), 1);
         eactx /= Math.max(3, huaweib.length);
         huaweib += `${parseInt(`${eactx}`)}`;
         stringD = [3 % (Math.max(10, recommendationI.length))];
         recommendationI += `${recommendationI.length * stringD.length}`;
      }
         libffmpegkitW += `${((castW ? 1 : 2) - 3)}`;
      let foregroundw = castW ? !castW : castW;
      do {
          let step_: Map<any, any> = new Map([[String.fromCharCode(97,99,99,101,108,101,114,97,116,105,111,110,95,108,95,51,50,0),String.fromCharCode(119,95,53,54,95,114,110,110,111,105,115,101,0)], [String.fromCharCode(102,95,50,48,95,103,101,115,116,117,114,101,0),String.fromCharCode(117,95,56,54,95,118,111,116,101,114,0)], [String.fromCharCode(109,97,116,104,95,48,95,51,48,0),String.fromCharCode(99,105,118,105,108,95,52,95,57,50,0)]]);
          let penaltyE = 1.0;
          let kuaishouP: Array<any> = [495, 784, 628];
         castW = (step_.size >> (Math.min(3, Math.abs(moonl)))) > 19;
         step_ = new Map([[`${kuaishouP.length}`, 3 * kuaishouP.length]]);
         penaltyE *= parseInt(`${penaltyE}`) + kuaishouP.length;
         if (foregroundw) {
            break;
         }
      } while ((5.30 > (2.31 - fullU)) && foregroundw);
      for (let p = 0; p < 3; p++) {
         refreshz *= parseFloat(`${moonl - 2}`);
      }
         castW = refreshz < fullU;
         fullU /= Math.max(3, libffmpegkitW.length ^ 1);
      for (let m = 0; m < 1; m++) {
         castW = 11.60 == fullU;
      }
      let statisticsS = 7126852 >= libffmpegkitW.length;
      do {
         libffmpegkitW += `${2 - parseInt(`${refreshz}`)}`;
         if (statisticsS) {
            break;
         }
      } while (((5 - libffmpegkitW.length) >= 4) && statisticsS);
       let settingf = String.fromCharCode(100,101,112,114,101,99,97,116,105,111,110,115,95,48,95,49,54,0);
       let short_vF = String.fromCharCode(117,110,104,105,103,104,108,105,103,104,116,95,105,95,53,48,0);
         refreshz *= parseFloat(`${parseInt(`${fullU}`)}`);
         castW = refreshz < 36.5 && String.fromCharCode(48,0) == libffmpegkitW;
         refreshz /= Math.max(parseFloat(`${short_vF.length >> (Math.min(3, Math.abs(moonl)))}`), 2);
      while (3.99 > (fullU * settingf.length)) {
          let incidentl = String.fromCharCode(102,116,121,112,95,111,95,49,49,0);
          let libavcodect: Array<any> = [330, 668];
          let mapbufferd = true;
         fullU *= (incidentl == String.fromCharCode(109,0) ? libavcodect.length : incidentl.length);
         libavcodect = [((mapbufferd ? 5 : 2))];
         break;
      }
      for (let d = 0; d < 2; d++) {
         libffmpegkitW += `${moonl % 3}`;
      }
          let typesR = 0.0;
          let topicj = 3.0;
         libffmpegkitW += `${parseInt(`${topicj}`)}`;
         typesR -= parseFloat(`${parseInt(`${typesR}`)}`);
      pnewarchdefaultsn /= Math.max(5, parseFloat(`${parseInt(`${fullU}`)}`));
      if (download7) {
         break;
      }
   } while (((4.55 * pnewarchdefaultsn) == 2.56 || 5.77 == (pnewarchdefaultsn * 4.55)) && download7);
      langkeyW += "1";
      rnewsZ -= parseInt(`${pnewarchdefaultsn}`);
   while ((2 ^ favorite_.length) < 5 || (favorite_.length ^ 2) < 2) {
       let gpayv = 2.0;
       let binddatasv: Map<any, any> = new Map([[String.fromCharCode(112,114,101,109,105,117,109,95,110,95,55,53,0),214], [String.fromCharCode(97,99,116,105,118,97,116,105,111,110,115,95,116,95,49,49,0),286]]);
         gpayv += parseFloat(`${2}`);
          let megaphoner = 5.0;
          let binddatasZ = String.fromCharCode(100,97,116,97,100,105,114,95,56,95,51,52,0);
         binddatasv.set(`${gpayv}`, binddatasv.size - 3);
         megaphoner += parseInt(`${megaphoner}`);
         binddatasZ += `${(String.fromCharCode(75,0) == binddatasZ ? parseInt(`${megaphoner}`) : binddatasZ.length)}`;
         gpayv -= parseFloat(`${parseInt(`${gpayv}`) - binddatasv.size}`);
      if (parseInt(`${gpayv}`) >= binddatasv.size) {
         binddatasv.set(`${gpayv}`, parseInt(`${gpayv}`));
      }
      for (let e = 0; e < 3; e++) {
         binddatasv = new Map([[`${binddatasv.size}`, binddatasv.size]]);
      }
      if (gpayv >= 1.17) {
         binddatasv = new Map([[`${binddatasv.size}`, parseInt(`${gpayv}`)]]);
      }
      gdtadvq.set(`${activityD}`, ((activityD ? 1 : 2) ^ parseInt(`${rnewsZ}`)));
      break;
   }
      favorite_ += `${2 | parseInt(`${pnewarchdefaultsn}`)}`;
       let becomez = String.fromCharCode(116,114,105,109,95,102,95,52,54,0);
       let encrypt8 = String.fromCharCode(111,95,49,51,95,102,99,104,111,119,110,0);
       let middlef: Map<any, any> = new Map([[String.fromCharCode(113,95,50,57,95,109,97,110,105,112,117,108,97,116,111,114,0),646], [String.fromCharCode(111,95,56,54,95,116,114,97,99,107,115,0),222]]);
          let search6 = String.fromCharCode(99,95,52,55,95,119,97,118,101,108,101,116,0);
          let telegramr = String.fromCharCode(99,104,97,105,110,115,95,117,95,50,48,0);
          let statsZ = String.fromCharCode(109,111,100,117,108,101,109,97,112,95,100,95,49,56,0);
         middlef.set(search6, (search6 == String.fromCharCode(114,0) ? telegramr.length : search6.length));
         telegramr = `${statsZ.length}`;
      let taiwan1 = middlef.size <= 5564218;
      do {
          let mbjscommonu = String.fromCharCode(105,116,101,114,97,116,105,118,101,95,118,95,52,0);
          let p_imageT = 5;
          let overlay3: Array<any> = [258, 165, 686];
          let untickX = 5.0;
          let matchQ = String.fromCharCode(122,95,50,48,95,115,104,97,100,101,0);
         middlef.set(encrypt8, (encrypt8 == String.fromCharCode(82,0) ? becomez.length : encrypt8.length));
         mbjscommonu = `${3 - overlay3.length}`;
         p_imageT *= matchQ.length % 3;
         overlay3 = [(mbjscommonu == String.fromCharCode(117,0) ? mbjscommonu.length : p_imageT)];
         untickX -= parseFloat(`${p_imageT >> (Math.min(overlay3.length, 5))}`);
         matchQ += `${matchQ.length / (Math.max(2, 6))}`;
         if (taiwan1) {
            break;
         }
      } while (taiwan1 && (becomez.includes(`${middlef.size}`)));
          let searchbar8 = String.fromCharCode(106,95,50,48,95,116,111,108,111,119,101,114,0);
          let awayK = 5;
          let unimplementedviewC = false;
         encrypt8 = `${encrypt8.length}`;
         searchbar8 = `${(String.fromCharCode(73,0) == searchbar8 ? searchbar8.length : (unimplementedviewC ? 1 : 4))}`;
         awayK <<= Math.min(3, Math.abs(awayK * 2));
         unimplementedviewC = (awayK - searchbar8.length) <= 78;
      let sportsr = encrypt8.length >= 8872511;
      do {
         encrypt8 += `${(encrypt8 == String.fromCharCode(77,0) ? middlef.size : encrypt8.length)}`;
         if (sportsr) {
            break;
         }
      } while ((becomez.startsWith(`${encrypt8.length}`)) && sportsr);
       let templateprocessorC = String.fromCharCode(114,101,109,105,110,100,101,114,95,105,95,57,52,0);
       let orientationW = String.fromCharCode(101,95,57,52,95,99,108,105,101,110,116,0);
         orientationW = `${middlef.size / 2}`;
         middlef.set(becomez, orientationW.length + becomez.length);
      let matchesv = String.fromCharCode(110,54,101,51,104,108,55,97,104,0) == orientationW;
      do {
         orientationW += `${(templateprocessorC == String.fromCharCode(84,0) ? orientationW.length : templateprocessorC.length)}`;
         if (matchesv) {
            break;
         }
      } while (matchesv && (encrypt8 == orientationW));
         encrypt8 += `${middlef.size % (Math.max(encrypt8.length, 1))}`;
      gdtadvq.set(`${activityD}`, 1);
   if (4 < (langkeyW.length / 3) || (3 / (Math.max(7, gdtadvq.size))) < 3) {
      gdtadvq = new Map([[`${chinap.size}`, 2]]);
   }
   if (favorite_.length <= 2 || !activityD) {
       let attributedstringv = String.fromCharCode(97,98,115,120,95,54,95,56,0);
       let userN = 0.0;
       let gradle8 = String.fromCharCode(100,95,56,49,95,112,111,107,101,114,0);
         gradle8 += `${gradle8.length}`;
      while (gradle8.endsWith(`${attributedstringv.length}`)) {
          let f_countH = 5.0;
          let iinit_r0w = 2;
         attributedstringv = `${attributedstringv.length}`;
         f_countH -= parseFloat(`${parseInt(`${f_countH}`)}`);
         iinit_r0w &= parseInt(`${f_countH}`) + 2;
         break;
      }
         attributedstringv += `${parseInt(`${userN}`)}`;
         gradle8 = "3";
      for (let a = 0; a < 3; a++) {
         attributedstringv += `${2 ^ parseInt(`${userN}`)}`;
      }
         gradle8 += `${attributedstringv.length}`;
         attributedstringv += `${1 - attributedstringv.length}`;
      if (gradle8 == attributedstringv) {
         attributedstringv = `${gradle8.length & parseInt(`${userN}`)}`;
      }
       let placeholderN = String.fromCharCode(120,95,56,51,95,115,109,105,108,105,101,115,0);
      activityD = (gdtadvq.size + parseInt(`${pnewarchdefaultsn}`)) > 85;
   }
   let reactX = String.fromCharCode(57,56,49,105,0) == favorite_;
   do {
       let verticalE = 0.0;
       let indicatorX: Array<any> = [473, 794];
       let securityI: Map<any, any> = new Map([[String.fromCharCode(112,105,120,100,101,115,99,95,119,95,49,52,0),true ], [String.fromCharCode(118,95,56,95,115,97,109,112,108,101,115,0),false ], [String.fromCharCode(100,105,97,109,101,116,101,114,95,101,95,51,49,0),true ]]);
       let nativeex5: Map<any, any> = new Map([[String.fromCharCode(120,95,57,53,95,105,115,110,111,110,122,101,114,111,0),true ], [String.fromCharCode(100,101,102,108,105,99,107,101,114,95,99,95,49,49,0),false ], [String.fromCharCode(112,95,52,51,95,112,114,101,112,114,111,99,101,115,115,0),true ]]);
       let statsl = false;
      while (!statsl) {
         statsl = !statsl;
         break;
      }
      while (statsl) {
         nativeex5 = new Map([[`${indicatorX.length}`, indicatorX.length]]);
         break;
      }
      if ((indicatorX.length ^ 3) <= 5 || (nativeex5.size ^ indicatorX.length) <= 3) {
         indicatorX.push(indicatorX.length - 2);
      }
      for (let k = 0; k < 1; k++) {
         nativeex5.set(`${statsl}`, nativeex5.size);
      }
         indicatorX = [nativeex5.size >> (Math.min(Math.abs(3), 3))];
      if (4.29 < verticalE) {
         verticalE *= parseFloat(`${parseInt(`${verticalE}`) % 3}`);
      }
      let volumeJ = securityI.size >= 6709854;
      do {
         securityI = new Map([[`${indicatorX.length}`, ((statsl ? 3 : 2) / (Math.max(indicatorX.length, 7)))]]);
         if (volumeJ) {
            break;
         }
      } while ((4 >= (1 & securityI.size)) && volumeJ);
         nativeex5 = new Map([[`${securityI.size}`, 2 - nativeex5.size]]);
       let headert = String.fromCharCode(98,95,52,95,114,101,102,114,101,115,104,0);
       let trophyj = String.fromCharCode(98,98,117,102,95,106,95,49,55,0);
      if ((nativeex5.size >> (Math.min(Math.abs(1), 5))) > 5 && (nativeex5.size >> (Math.min(1, indicatorX.length))) > 1) {
         nativeex5.set(trophyj, 2 | nativeex5.size);
      }
         headert += `${securityI.size}`;
         statsl = indicatorX.length <= 25;
      if (indicatorX.length == 4 && 1 == (4 & indicatorX.length)) {
         statsl = trophyj == String.fromCharCode(48,0);
      }
      for (let z = 0; z < 2; z++) {
         trophyj += `${securityI.size ^ parseInt(`${verticalE}`)}`;
      }
          let awayw = String.fromCharCode(102,108,117,115,104,95,99,95,52,53,0);
         nativeex5 = new Map([[`${securityI.size}`, securityI.size << (Math.min(awayw.length, 4))]]);
      favorite_ = `${((statsl ? 2 : 1) ^ 1)}`;
      if (reactX) {
         break;
      }
   } while (reactX && ((favorite_.length * parseInt(`${pnewarchdefaultsn}`)) >= 5 || 2.27 >= (5.5 * pnewarchdefaultsn)));
   while (rnewsZ <= 1.77) {
      gdtadvq = new Map([[`${gdtadvq.size}`, gdtadvq.size | parseInt(`${rnewsZ}`)]]);
      break;
   }
      chinap = new Map([[favorite_, favorite_.length]]);

    bottomTabHeight = 0;
  }

  const displayHeight = windowHeight - bottomTabHeight;

  const insets = useSafeAreaInsets();
  const { spacing, colors } = useTheme();

  
  //   ({ screenReducer }: yys_MintegralLibavdevice) => screenReducer
  

  return (
    <>
      {scrollView ? (
        <ScrollView
          style={{
            backgroundColor: isBgHide ? 'inherit' : colors.background,
            paddingTop: insets.top,
            paddingBottom: insets.bottom,
            paddingLeft: insets.left,
            paddingRight: insets.right,
          }}
          stickyHeaderIndices={[0]}
          contentContainerStyle={{ paddingBottom: 30 }}
        >
          <StatusBar backgroundColor={isBgHide ? 'inherit' : 'black'}  barStyle="light-content" />
          {header}
          <View
            style={{
              ...styles.innerContainer,
              paddingLeft: spacing.sideOffset,
              paddingRight: spacing.sideOffset,
              ...containerStyle,
            }}
          >
            {children}
          </View>
          {footer}
        </ScrollView>
      ) : (
        <View
          style={{
            backgroundColor: isBgHide ? 'inherit' : colors.background,
            ...styles.viewContainer,
            paddingTop: isPlay ? 0 : insets.top,
            paddingBottom: !isHome || isPlay ? 0 : insets.bottom,
            
            paddingLeft: isPlay ? 0 : insets.left,
            paddingRight: isPlay ? 0 : insets.right,
            
          }}
        >
          <StatusBar backgroundColor={isBgHide ? 'inherit' : 'black' } barStyle="light-content" />
          <View
            style={{
              ...styles.innerContainer,
              paddingLeft: spacing.sideOffset,
              paddingRight: spacing.sideOffset,
              ...containerStyle,
            }}
          >
            {children}
          </View>
          {footer}
        </View>
      )}
    </>
  );
}

const styles = StyleSheet.create({
  viewContainer: {
    flex: 1,
  },
  innerContainer: {
    flex: 1,
  },
});
