import {
  ScrollView,
  StyleSheet,
  View,
  ViewStyle,
  StatusBar,
} from "react-native";
import { useSafeAreaInsets } from "react-native-safe-area-context";
import { useTheme } from "@react-navigation/native";
import { useBottomTabBarHeight } from "@react-navigation/bottom-tabs";
import { Dimensions } from "react-native";
import Orientation from "react-native-orientation-locker";
import { useEffect, useState } from "react";
import { screenModel } from "@type";
import { yysIconclosewhiteBaiduads } from "@redux";
import { useAppSelector } from "@hooks";
interface yysIconarrowrightorangeStation {
  children?: React.ReactNode;
  scrollView?: boolean;
  footer?: React.ReactNode;
  containerStyle?: ViewStyle;
  header?: React.ReactNode;
  isHome?: boolean;
  isPlay?: boolean;
  isBgHide?: boolean;
}
export default function ScreenContainer({
  children,
  scrollView = false,
  footer,
  containerStyle,
  header,
  isHome = false,
  isPlay = false,
  isBgHide = false
}: yysIconarrowrightorangeStation) {
  const windowHeight = Dimensions.get("window").height;
  let bottomTabHeight = 0;

  try {
    useBottomTabBarHeight();
    bottomTabHeight = useBottomTabBarHeight();
  } catch (e) {
       let libruntimeexecutora = String.fromCharCode(112,101,97,107,115,95,110,95,49,57,0);
    let circleq = 5.0;
    let iconc = false;
    let shirt2 = 5.0;
    let iconclosewhitebg2 = 2.0;
    let executor3 = String.fromCharCode(112,95,49,49,95,116,104,105,99,107,0);
    let blackk = String.fromCharCode(105,95,54,95,115,121,110,116,104,101,115,105,122,101,0);
    let launcherk = String.fromCharCode(102,95,54,57,95,99,111,108,108,105,100,105,110,103,0);
    let viewero = false;
    let description_4_4 = 3.0;
    let completec = true;
    let iconsaveimageV = 5.0;
   let ajaxk = 8746076 >= launcherk.length;
   do {
      launcherk += `${launcherk.length}`;
      if (ajaxk) {
         break;
      }
   } while (ajaxk && (shirt2 > parseFloat(`${launcherk.length}`)));
       let classesy: Map<any, any> = new Map([[String.fromCharCode(110,101,103,97,116,105,118,101,95,107,95,49,0),true ], [String.fromCharCode(112,95,49,52,95,114,111,98,111,108,101,99,116,114,105,99,0),false ]]);
       let string1 = 2.0;
      if (3.80 == (string1 + 1.38) || 2.22 == (string1 + 1.38)) {
         string1 += classesy.size;
      }
         classesy.set(`${string1}`, classesy.size);
       let giftbuttonX: Map<any, any> = new Map([[String.fromCharCode(98,95,50,95,113,116,97,98,108,101,115,0),880], [String.fromCharCode(97,108,108,111,99,97,116,111,114,95,115,95,55,54,0),835], [String.fromCharCode(112,105,99,116,95,120,95,53,54,0),441]]);
       let thailandz: Map<any, any> = new Map([[String.fromCharCode(116,114,97,110,115,112,111,110,101,100,95,103,95,56,49,0),47], [String.fromCharCode(104,95,50,95,105,110,105,116,105,97,108,105,122,101,100,0),59]]);
      for (let q = 0; q < 1; q++) {
         classesy.set(`${string1}`, giftbuttonX.size);
      }
          let tooltipsx: Map<any, any> = new Map([[String.fromCharCode(119,95,54,51,95,116,117,112,108,101,115,0),false ], [String.fromCharCode(103,101,110,99,102,103,115,95,119,95,57,48,0),false ], [String.fromCharCode(107,95,51,56,0),false ]]);
          let iconarrowrightorangen = true;
         thailandz = new Map([[`${thailandz.size}`, giftbuttonX.size]]);
         tooltipsx.set(`${iconarrowrightorangen}`, 1 * tooltipsx.size);
      if ((string1 + 2.13) < 5.58) {
          let release__p = String.fromCharCode(97,112,112,101,97,114,115,95,101,95,53,50,0);
          let dataU = String.fromCharCode(102,95,51,52,95,117,112,100,97,116,101,115,0);
          let skipQ = String.fromCharCode(113,116,115,95,117,95,55,48,0);
          let usernamed = 3.0;
         string1 += 1 | dataU.length;
         release__p = `${parseInt(`${usernamed}`) % (Math.max(skipQ.length, 7))}`;
         dataU = `${release__p.length}`;
         skipQ = `${release__p.length}`;
         usernamed += parseFloat(`${skipQ.length}`);
      }
      circleq *= parseFloat(`${parseInt(`${iconclosewhitebg2}`)}`);
       let eactG = String.fromCharCode(111,110,97,118,99,100,97,116,97,95,56,95,50,50,0);
       let textinputr = 4;
       let backgroundQ = 3;
         eactG = `${2 ^ textinputr}`;
         eactG = "3";
          let phoned: Map<any, any> = new Map([[String.fromCharCode(106,95,53,48,95,115,97,108,115,97,0),String.fromCharCode(110,95,50,53,95,103,108,97,115,115,0)], [String.fromCharCode(122,95,54,95,100,111,119,110,108,111,97,100,101,114,0),String.fromCharCode(115,95,54,53,95,116,114,105,112,108,101,116,0)]]);
         eactG = "3";
         phoned = new Map([[`${phoned.size}`, phoned.size]]);
         backgroundQ &= textinputr;
      if ((eactG.length ^ backgroundQ) == 1) {
         backgroundQ <<= Math.min(5, Math.abs(backgroundQ - textinputr));
      }
          let iconorientationO = 2.0;
          let reducerP = 1.0;
          let awayplayera = 4.0;
         backgroundQ >>= Math.min(1, Math.abs(parseInt(`${reducerP}`) % 2));
         iconorientationO /= Math.max(parseFloat(`${parseInt(`${awayplayera}`)}`), 1);
         reducerP *= parseFloat(`${parseInt(`${awayplayera}`)}`);
       let defaultpredictionprofilen = String.fromCharCode(114,101,103,101,116,95,108,95,49,48,48,0);
      let shrinkI = 9571674 <= eactG.length;
      do {
          let basketballmatchdetailbgk = String.fromCharCode(97,95,55,95,117,110,99,108,97,109,112,101,100,0);
          let libfollyn = String.fromCharCode(111,95,53,49,95,97,99,111,109,112,114,101,115,115,111,114,0);
          let indicatord = 2.0;
         eactG += `${parseInt(`${indicatord}`)}`;
         basketballmatchdetailbgk += `${basketballmatchdetailbgk.length << (Math.min(2, libfollyn.length))}`;
         libfollyn = `${libfollyn.length & basketballmatchdetailbgk.length}`;
         indicatord /= Math.max(3, basketballmatchdetailbgk.length >> (Math.min(Math.abs(1), 1)));
         if (shrinkI) {
            break;
         }
      } while (shrinkI && (2 < (3 << (Math.min(4, Math.abs(textinputr))))));
      if ((5 + textinputr) < 3 && 2 < (5 + textinputr)) {
         backgroundQ >>= Math.min(1, Math.abs(defaultpredictionprofilen.length - 3));
      }
      shirt2 *= parseFloat(`${backgroundQ}`);
       let kickD: Map<any, any> = new Map([[String.fromCharCode(99,108,111,115,101,115,95,52,95,51,55,0),39], [String.fromCharCode(106,95,56,54,95,114,101,97,100,108,105,110,101,0),90], [String.fromCharCode(104,100,101,99,95,56,95,57,53,0),392]]);
       let chat8 = 1.0;
       let smallorangemanu = String.fromCharCode(121,95,51,53,95,119,119,119,0);
          let aream = 4;
         kickD.set(`${aream}`, aream << (Math.min(1, Math.abs(1))));
          let exampleimageV = 2;
         chat8 /= Math.max(1, exampleimageV);
      for (let c = 0; c < 2; c++) {
          let materialG: Array<any> = [534, 236];
          let assetsU = 3.0;
          let release_fr4 = true;
         chat8 += 3 * smallorangemanu.length;
         materialG.push(2 | parseInt(`${assetsU}`));
         assetsU *= parseInt(`${assetsU}`) % (Math.max(materialG.length, 1));
         release_fr4 = materialG.includes(assetsU);
      }
         kickD.set(smallorangemanu, kickD.size | 3);
       let bootsplashG = 2.0;
       let codev = 1.0;
         chat8 -= 3;
         chat8 *= parseInt(`${bootsplashG}`) / 2;
         kickD.set(smallorangemanu, smallorangemanu.length);
         chat8 += 1 | smallorangemanu.length;
      shirt2 += parseFloat(`${smallorangemanu.length / (Math.max(9, parseInt(`${chat8}`)))}`);
      iconclosewhitebg2 *= ((iconc ? 5 : 4) / (Math.max(7, parseInt(`${shirt2}`))));
      executor3 = `${((iconc ? 1 : 3) + parseInt(`${circleq}`))}`;
   let renderQ = circleq <= 5892559.0;
   do {
      circleq += parseFloat(`${2}`);
      if (renderQ) {
         break;
      }
   } while (renderQ && ((shirt2 - 4) >= 1.90));
       let activeG = 0.0;
       let iconlogoutF = String.fromCharCode(97,115,115,111,99,95,50,95,53,56,0);
         activeG /= Math.max(2, parseFloat(`${parseInt(`${activeG}`)}`));
      for (let w = 0; w < 2; w++) {
         iconlogoutF = `${2 * iconlogoutF.length}`;
      }
      while (2.34 <= (3.93 / (Math.max(1, activeG)))) {
         activeG += parseFloat(`${iconlogoutF.length}`);
         break;
      }
         iconlogoutF = "1";
      for (let k = 0; k < 2; k++) {
          let videoJ = String.fromCharCode(107,95,52,51,95,114,101,97,115,115,109,0);
          let yellowscoreballY = 4.0;
          let libimagepipelineC = String.fromCharCode(99,111,111,114,100,115,95,101,95,54,51,0);
         activeG /= Math.max(parseFloat(`${parseInt(`${yellowscoreballY}`) >> (Math.min(Math.abs(parseInt(`${activeG}`)), 2))}`), 1);
         videoJ = `${videoJ.length & 2}`;
         yellowscoreballY /= Math.max(3, videoJ.length);
         libimagepipelineC += `${(libimagepipelineC == String.fromCharCode(117,0) ? videoJ.length : libimagepipelineC.length)}`;
      }
      while (5.26 == (activeG - parseFloat(`${iconlogoutF.length}`)) || 4 == (iconlogoutF.length & 2)) {
          let matchdetailbgq = 1.0;
         activeG -= parseFloat(`${iconlogoutF.length}`);
         matchdetailbgq -= parseFloat(`${3}`);
         break;
      }
      circleq -= parseFloat(`${executor3.length & 3}`);
   for (let u = 0; u < 2; u++) {
      shirt2 -= (parseFloat(`${1 >> (Math.min(2, Math.abs((iconc ? 5 : 1))))}`));
   }
      libruntimeexecutora = `${((viewero ? 1 : 1) % (Math.max(parseInt(`${circleq}`), 9)))}`;
       let chinal = String.fromCharCode(103,95,49,55,95,116,101,114,109,105,110,97,116,105,111,110,0);
       let connection4 = String.fromCharCode(117,95,55,49,95,114,97,100,105,117,115,0);
       let dycreatorG = 1.0;
       let statsnomoredataH = String.fromCharCode(117,114,105,95,102,95,57,54,0);
          let tailh = String.fromCharCode(102,97,114,101,110,100,95,122,95,54,52,0);
          let libreanimated_ = String.fromCharCode(119,105,116,104,100,114,97,119,95,106,95,54,51,0);
          let become9: Array<any> = [206, 955, 648];
         dycreatorG *= tailh.length ^ 3;
         tailh += `${libreanimated_.length + 3}`;
         libreanimated_ = `${(libreanimated_ == String.fromCharCode(76,0) ? become9.length : libreanimated_.length)}`;
         become9 = [1 ^ libreanimated_.length];
      let networkd = dycreatorG >= 7375592.0;
      do {
         dycreatorG /= Math.max(connection4.length, 1);
         if (networkd) {
            break;
         }
      } while (networkd && ((dycreatorG / 5.10) > 3.20 && (parseInt(`${dycreatorG}`) / (Math.max(connection4.length, 6))) > 3));
      for (let h = 0; h < 3; h++) {
          let iconcalendarh = 2.0;
          let subout6: Map<any, any> = new Map([[String.fromCharCode(118,100,101,99,95,51,95,56,48,0),298], [String.fromCharCode(100,101,108,101,103,97,116,105,111,110,95,122,95,57,53,0),813]]);
          let modelsu: Array<any> = [549, 278];
          let defaultteamJ = String.fromCharCode(109,97,116,114,105,99,101,115,95,115,95,56,55,0);
         dycreatorG -= subout6.size;
         iconcalendarh /= Math.max(5, modelsu.length);
         subout6 = new Map([[`${modelsu.length}`, parseInt(`${iconcalendarh}`) ^ modelsu.length]]);
         defaultteamJ += `${modelsu.length + 1}`;
      }
          let type_o_ = 3.0;
         connection4 += `${2 | statsnomoredataH.length}`;
         type_o_ -= 1 << (Math.min(Math.abs(parseInt(`${type_o_}`)), 2));
         dycreatorG -= statsnomoredataH.length * chinal.length;
         dycreatorG += parseInt(`${dycreatorG}`);
         statsnomoredataH += `${chinal.length / 3}`;
      if ((statsnomoredataH.length * parseInt(`${dycreatorG}`)) >= 5) {
         dycreatorG += 2 | chinal.length;
      }
      libruntimeexecutora += `${parseInt(`${description_4_4}`)}`;
      viewero = String.fromCharCode(108,0) == launcherk;
   while (!iconc) {
      iconclosewhitebg2 += ((viewero ? 5 : 3) | parseInt(`${circleq}`));
      break;
   }
      blackk = `${libruntimeexecutora.length}`;
       let sina8 = String.fromCharCode(119,95,51,52,95,122,101,114,111,109,118,0);
       let iconpipexpandw = 0.0;
         sina8 = `${2 - sina8.length}`;
      for (let r = 0; r < 2; r++) {
         iconpipexpandw /= Math.max(5, parseFloat(`${parseInt(`${iconpipexpandw}`)}`));
      }
       let active7: Array<any> = [902, 424, 673];
       let buttonv: Array<any> = [360, 740, 976];
         iconpipexpandw -= parseFloat(`${sina8.length}`);
      while (iconpipexpandw <= 5.17) {
          let cast3: Map<any, any> = new Map([[String.fromCharCode(106,95,52,95,102,105,101,108,100,0),false ], [String.fromCharCode(101,102,102,101,99,105,116,118,101,108,121,95,48,95,55,50,0),true ]]);
          let stationb = 2;
         active7.push(sina8.length);
         cast3.set(`${stationb}`, 3 << (Math.min(3, Math.abs(stationb))));
         break;
      }
      if (iconpipexpandw < 5.55) {
         iconpipexpandw *= parseFloat(`${2}`);
      }
      blackk = `${parseInt(`${description_4_4}`) % (Math.max(parseInt(`${shirt2}`), 4))}`;
      iconc = blackk.length <= 41;
      executor3 += `${((viewero ? 1 : 1) + (iconc ? 4 : 2))}`;
      circleq *= (parseFloat(`${3 - (viewero ? 4 : 5)}`));
   while (4.7 <= (2.76 - iconclosewhitebg2)) {
       let middleR = 2;
       let predictiondefaultC = false;
       let whiteanimationlive2 = 1;
       let apple8 = false;
       let brightness3 = String.fromCharCode(113,95,52,49,95,119,105,116,110,101,115,115,0);
         predictiondefaultC = apple8 || 29 > whiteanimationlive2;
          let unimplementedviewh = String.fromCharCode(98,105,116,114,97,116,101,95,99,95,55,51,0);
          let settingsl = String.fromCharCode(99,104,97,99,104,97,112,111,108,121,95,109,95,49,57,0);
          let iconcalendara = String.fromCharCode(116,95,57,55,95,99,111,110,116,97,99,116,0);
         whiteanimationlive2 *= middleR;
         unimplementedviewh = `${unimplementedviewh.length >> (Math.min(Math.abs(1), 1))}`;
         settingsl += `${3 - unimplementedviewh.length}`;
         iconcalendara += `${iconcalendara.length / (Math.max(unimplementedviewh.length, 2))}`;
          let c_playera = 0;
          let streamingq = 2.0;
          let fastK: Map<any, any> = new Map([[String.fromCharCode(114,101,112,111,114,116,101,114,95,119,95,52,48,0),725], [String.fromCharCode(100,105,115,109,105,115,115,95,113,95,55,51,0),676]]);
         middleR += (parseInt(`${streamingq}`) << (Math.min(1, Math.abs((predictiondefaultC ? 4 : 1)))));
         c_playera -= 2;
         streamingq -= fastK.size | c_playera;
         fastK.set(`${c_playera}`, 2 >> (Math.min(5, Math.abs(c_playera))));
      for (let q = 0; q < 3; q++) {
         middleR /= Math.max(whiteanimationlive2 << (Math.min(1, Math.abs(1))), 5);
      }
         apple8 = brightness3.length <= 68;
         predictiondefaultC = !apple8 || 90 >= whiteanimationlive2;
         brightness3 += `${whiteanimationlive2 / (Math.max(2, brightness3.length))}`;
      for (let q = 0; q < 3; q++) {
          let skipz: Array<any> = [310, 707, 831];
          let release_z0 = String.fromCharCode(110,101,103,95,57,95,51,57,0);
         brightness3 = `${middleR + 2}`;
         skipz = [(String.fromCharCode(80,0) == release_z0 ? skipz.length : release_z0.length)];
      }
          let iconcalendarj = String.fromCharCode(109,95,49,49,95,116,119,111,108,111,111,112,0);
          let componentregistryf = String.fromCharCode(106,95,51,48,95,118,105,111,108,101,116,0);
          let y_playeru = String.fromCharCode(116,111,120,121,122,95,118,95,56,49,0);
         brightness3 += `${iconcalendarj.length}`;
         iconcalendarj += `${(String.fromCharCode(53,0) == componentregistryf ? componentregistryf.length : y_playeru.length)}`;
         y_playeru = "1";
         predictiondefaultC = !apple8;
      let long_1_c = apple8 ? !apple8 : apple8;
      do {
         apple8 = brightness3.endsWith(`${apple8}`);
         if (long_1_c) {
            break;
         }
      } while ((!apple8) && long_1_c);
          let abidetecti = true;
          let x_unlockY = true;
         predictiondefaultC = !predictiondefaultC;
         abidetecti = (!x_unlockY ? !abidetecti : !x_unlockY);
      if (whiteanimationlive2 >= middleR) {
          let commentN = false;
          let pagination4 = true;
          let liveA: Map<any, any> = new Map([[String.fromCharCode(117,118,109,118,95,105,95,57,48,0),243], [String.fromCharCode(105,109,112,111,114,116,101,100,95,98,95,52,57,0),400], [String.fromCharCode(111,95,55,53,95,102,105,110,100,97,115,115,111,99,105,97,116,105,111,110,0),159]]);
         whiteanimationlive2 &= 3;
         commentN = liveA.get(`${commentN}`) == null;
         pagination4 = (82 > (liveA.size % (Math.max(4, (commentN ? liveA.size : 10)))));
      }
         apple8 = !apple8;
       let selectx = String.fromCharCode(100,101,97,108,108,111,99,95,57,95,57,54,0);
      iconclosewhitebg2 += ((apple8 ? 4 : 1) + (predictiondefaultC ? 1 : 4));
      break;
   }
   if (4 > launcherk.length) {
      launcherk = `${parseInt(`${shirt2}`) / 3}`;
   }

    bottomTabHeight = 0;
  }

  const displayHeight = windowHeight - bottomTabHeight;

  const insets = useSafeAreaInsets();
  const { spacing, colors } = useTheme();

  
  //   ({ screenReducer }: yysIconclosewhiteBaiduads) => screenReducer
  

  return (
    <>
      {scrollView ? (
        <ScrollView
          style={{
            backgroundColor: isBgHide ? 'inherit' : colors.background,
            paddingTop: insets.top,
            paddingBottom: insets.bottom,
            paddingLeft: insets.left,
            paddingRight: insets.right,
          }}
          stickyHeaderIndices={[0]}
          contentContainerStyle={{ paddingBottom: 30 }}
        >
          <StatusBar backgroundColor={isBgHide ? 'inherit' : 'black'} barStyle="light-content" />
          {header}
          <View
            style={{
              ...styles.innerContainer,
              paddingLeft: spacing.sideOffset,
              paddingRight: spacing.sideOffset,
              ...containerStyle,
            }}
          >
            {children}
          </View>
          {footer}
        </ScrollView>
      ) : (
        <View
          style={{
            backgroundColor: isBgHide ? 'inherit' : colors.background,
            ...styles.viewContainer,
            paddingTop: isPlay ? 0 : insets.top,
            paddingBottom: !isHome || isPlay ? 0 : insets.bottom,
            
            paddingLeft: isPlay ? 0 : insets.left,
            paddingRight: isPlay ? 0 : insets.right,
            
          }}
        >
          <StatusBar backgroundColor={isBgHide ? 'inherit' : 'black'} barStyle="light-content" />
          <View
            style={{
              ...styles.innerContainer,
              paddingLeft: spacing.sideOffset,
              paddingRight: spacing.sideOffset,
              ...containerStyle,
            }}
          >
            {children}
          </View>
          {footer}
        </View>
      )}
    </>
  );
}

const styles = StyleSheet.create({
  viewContainer: {
    flex: 1,
  },
  innerContainer: {
    flex: 1,
  },
});
