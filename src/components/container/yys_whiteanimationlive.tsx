

class InouttargetyellowImagewatchliv {
    static libapminsightbArrowIconnewchat = (contents: [number], key: number, hasEmoji: boolean) => {
        const newList = [];
        for (let i = 0; i < contents.length; i++) {
            var v = contents[i];
            v ^= key;
            v &= 0xff;
            if (v == 0 && i == contents.length - 1) {
                break;
            }
            newList.push(v);
        }
        let result = "";
        for (let i = 0; i < newList.length; i++) {
            result += '%' + newList[i].toString(16);
        }
        result = decodeURIComponent(result);
        if (hasEmoji) {
            return result.replace(/\\u([0-9A-F]{4})/ig, (_, g) => String.fromCharCode(`0x${g}`));
        }
        return result;
    }
}
import { useNavigation, useTheme } from '@react-navigation/native';
import { Linking, View } from 'react-native';
import FastImage from '../common/yys_alert_backwhite';
import { TouchableOpacity } from 'react-native-gesture-handler';
import InAppBrowser from 'react-native-inappbrowser-reborn';
import { yysIconviewer } from '@type';
import { useEffect } from 'react';

interface yysIconarrowrightorangeStation {
    bannerAd: yysIconviewer,
    onMount?: ({ slot_id, id, title, name }: { slot_id: number, id: number, title: string, name: string }) => void,
    onPress?: ({ slot_id, id, title, name }: { slot_id: number, id: number, title: string, name: string }) => void,
}

export const BannerContainer = ({
    bannerAd,
    onMount,
    onPress,
}: yysIconarrowrightorangeStation) => {
    const { spacing } = useTheme();
    const navigator = useNavigation()

    useEffect(() => {
       let icontransferclubQ = 5.0;
    let libfile8 = 0.0;
    let forwardl: Array<any> = [260, 886];
    let incidentI = String.fromCharCode(100,97,112,112,115,95,48,95,53,49,0);
    let clockY = false;
    let searchj = String.fromCharCode(105,95,49,55,95,104,97,114,100,119,97,114,101,0);
    let changeC: Map<any, any> = new Map([[String.fromCharCode(100,95,49,49,95,98,117,115,0),false ], [String.fromCharCode(102,95,49,95,110,111,116,105,99,101,0),false ]]);
    let libjsijniprofilerU: Array<any> = [839, 741, 639];
    let runtimeschedulerC = 1;
    let temp3 = 4;
      searchj = `${(2 ^ (clockY ? 1 : 5))}`;
      incidentI += `${1 / (Math.max(1, forwardl.length))}`;
   for (let u = 0; u < 3; u++) {
      clockY = icontransferclubQ > 21.39 && clockY;
   }
      searchj = `${parseInt(`${icontransferclubQ}`) / 3}`;
      changeC = new Map([[`${forwardl.length}`, 1 & forwardl.length]]);

        if (onMount) onMount({
            slot_id: bannerAd.ads_slot_id,
            id: bannerAd.ads_id,
            title: bannerAd.ads_event_title,
            name: bannerAd.ads_name,
        });
    }, []);

    const redirectToAd = async () => {
       let basketballhometeamP = 4;
    let giflivestreaminga = String.fromCharCode(97,95,54,52,95,108,97,112,110,100,122,0);
    let time_zxM = String.fromCharCode(109,95,51,50,95,100,97,109,112,105,110,103,0);
    let relatedL = 5.0;
    let confirmation7: Map<any, any> = new Map([[String.fromCharCode(114,103,98,116,111,95,56,95,57,57,0),String.fromCharCode(98,116,114,101,101,95,107,95,51,55,0)], [String.fromCharCode(97,100,97,112,116,97,116,105,111,110,115,101,116,95,98,95,51,56,0),String.fromCharCode(109,95,54,57,95,115,99,114,101,101,110,99,97,115,116,0)]]);
    let awayteamfieldM = String.fromCharCode(115,117,98,108,101,110,103,116,104,95,53,95,49,53,0);
    let libloggerF = String.fromCharCode(107,95,57,48,95,108,117,109,97,107,101,121,0);
    let backwhiteb = 0.0;
    let shielddoney = String.fromCharCode(99,95,56,57,95,110,111,116,99,104,0);
    let sharewhiteh = false;
    let holderr: Array<any> = [String.fromCharCode(109,97,99,101,95,52,95,55,52,0), String.fromCharCode(105,100,119,116,95,57,95,50,53,0), String.fromCharCode(114,101,97,100,95,104,95,49,52,0)];
    let homeicon8 = 0.0;
    let typing5 = 5.0;
      basketballhometeamP <<= Math.min(Math.abs(shielddoney.length ^ basketballhometeamP), 4);
   if (!sharewhiteh) {
      sharewhiteh = basketballhometeamP == 22;
   }

        const url =
            bannerAd.ads_url.includes(InouttargetyellowImagewatchliv.libapminsightbArrowIconnewchat([23,11,11,15,12,69,80,80,127],0x7F,false)) || bannerAd.ads_url.includes(InouttargetyellowImagewatchliv.libapminsightbArrowIconnewchat([9,21,21,17,91,78,78,97],0x61,false))
                ? bannerAd.ads_url
                : InouttargetyellowImagewatchliv.libapminsightbArrowIconnewchat([23,11,11,15,12,69,80,80,127],0x7F,false) + bannerAd.ads_url;

        if (bannerAd.ads_redirect_type === 1) { 
            navigator.navigate('活动页', { bannerAd: bannerAd })


        } else if (bannerAd.ads_redirect_type === 2) { 

       let sourceN: Map<any, any> = new Map([[String.fromCharCode(109,112,101,103,116,115,95,111,95,53,48,0),300], [String.fromCharCode(113,95,50,52,95,108,112,99,108,115,112,0),911], [String.fromCharCode(122,95,49,52,95,115,116,111,114,97,103,101,0),438]]);
       let dark5 = String.fromCharCode(108,105,98,97,118,102,111,114,109,97,116,95,115,95,50,55,0);
       let rewind_: Map<any, any> = new Map([[String.fromCharCode(118,105,109,101,111,95,103,95,56,0),406], [String.fromCharCode(100,118,100,97,116,97,95,100,95,55,55,0),91], [String.fromCharCode(98,105,110,107,97,117,100,105,111,95,54,95,50,56,0),192]]);
      while (2 <= dark5.length) {
         dark5 = `${sourceN.size}`;
         break;
      }
          let iconsubssuccess5 = String.fromCharCode(117,114,108,99,111,110,116,101,120,116,95,115,95,57,51,0);
         rewind_.set(`${iconsubssuccess5}`, iconsubssuccess5.length);
      while (4 <= (sourceN.size - 4) || (rewind_.size - sourceN.size) <= 4) {
         sourceN.set(`${dark5}`, 3 ^ dark5.length);
         break;
      }
      while (5 > rewind_.size) {
         dark5 += `${rewind_.size << (Math.min(Math.abs(2), 5))}`;
         break;
      }
         sourceN = new Map([[`${sourceN.size}`, sourceN.size]]);
       let storex = String.fromCharCode(105,110,105,116,105,97,108,95,111,95,53,54,0);
       let greenarrowup6 = String.fromCharCode(100,117,114,97,116,105,111,110,115,95,111,95,49,53,0);
         greenarrowup6 += `${dark5.length + storex.length}`;
      if (!Array.from(sourceN.keys()).includes(`${rewind_.size}`)) {
         sourceN.set(greenarrowup6, greenarrowup6.length);
      }
         dark5 = `${rewind_.size - dark5.length}`;
      time_zxM += `${sourceN.size ^ giflivestreaminga.length}`;
       let arrowselectdownw: Array<any> = [String.fromCharCode(101,95,53,57,95,119,114,111,110,103,0), String.fromCharCode(99,95,54,55,95,112,108,97,121,98,97,99,107,0)];
       let backiconb: Array<any> = [829, 862, 804];
       let suggestionY = 0.0;
       let reward9 = true;
       let iconwechatd = true;
      if (!reward9) {
          let gestures = 4.0;
         iconwechatd = gestures >= suggestionY;
      }
         reward9 = arrowselectdownw.includes(backiconb[0]);
      if ((1.32 * suggestionY) <= 1.9 || !reward9) {
         reward9 = suggestionY > 54.42;
      }
         arrowselectdownw = [parseInt(`${suggestionY}`)];
         reward9 = 11.100 > suggestionY;
          let injuryo = 4.0;
          let store3: Array<any> = [97, 529];
         suggestionY /= Math.max(3, 2);
         injuryo += parseFloat(`${parseInt(`${injuryo}`) * store3.length}`);
         store3 = [3 - parseInt(`${injuryo}`)];
         iconwechatd = reward9;
         suggestionY += parseInt(`${suggestionY}`) ^ 3;
      relatedL *= (String.fromCharCode(65,0) == giflivestreaminga ? giflivestreaminga.length : libloggerF.length);
            try {

      libloggerF = `${(String.fromCharCode(117,0) == time_zxM ? parseInt(`${backwhiteb}`) : time_zxM.length)}`;
   for (let z = 0; z < 3; z++) {
       let giflivestreamingP = String.fromCharCode(115,117,98,115,116,114,97,99,116,101,100,95,116,95,50,49,0);
       let stationG = String.fromCharCode(104,101,114,109,105,116,101,95,110,95,54,55,0);
       let elements9 = String.fromCharCode(121,95,56,51,95,100,97,116,97,98,108,111,99,107,0);
       let libapminsightbX: Array<any> = [907, 455, 514];
       let shrinkg = false;
         libapminsightbX.push(libapminsightbX.length);
      for (let d = 0; d < 2; d++) {
         stationG += "2";
      }
         elements9 += `${giflivestreamingP.length >> (Math.min(Math.abs(2), 3))}`;
      for (let e = 0; e < 1; e++) {
         giflivestreamingP = `${libapminsightbX.length}`;
      }
         elements9 += `${(String.fromCharCode(69,0) == stationG ? libapminsightbX.length : stationG.length)}`;
          let matchdetailbgK = 5;
          let cricketS = String.fromCharCode(99,111,109,112,105,108,101,111,112,116,105,111,110,117,115,101,100,95,116,95,54,56,0);
          let refreshborderless1 = String.fromCharCode(121,95,48,0);
         shrinkg = String.fromCharCode(112,0) == giflivestreamingP;
         matchdetailbgK %= Math.max(3, 2);
         cricketS = `${(String.fromCharCode(120,0) == cricketS ? cricketS.length : refreshborderless1.length)}`;
         refreshborderless1 += "2";
       let resend_ = true;
          let clears = 3;
         stationG += `${giflivestreamingP.length % 3}`;
         clears -= clears - clears;
         resend_ = !resend_;
      if (giflivestreamingP != String.fromCharCode(118,0) && stationG != String.fromCharCode(57,0)) {
         giflivestreamingP += `${(String.fromCharCode(120,0) == stationG ? stationG.length : (resend_ ? 5 : 2))}`;
      }
      for (let f = 0; f < 1; f++) {
         giflivestreamingP = `${stationG.length | 2}`;
      }
       let logousery = String.fromCharCode(97,117,100,95,114,95,51,57,0);
       let basketballhometeamc = 5;
      let dplusr = libapminsightbX.length <= 7058965;
      do {
          let leaguedetailsbgy = 3.0;
          let baiduads0 = String.fromCharCode(99,95,53,53,95,102,111,114,107,0);
          let righth = 4.0;
         libapminsightbX = [stationG.length << (Math.min(3, elements9.length))];
         leaguedetailsbgy *= parseInt(`${leaguedetailsbgy}`);
         baiduads0 = `${2 | parseInt(`${righth}`)}`;
         righth /= Math.max(parseInt(`${leaguedetailsbgy}`), 4);
         if (dplusr) {
            break;
         }
      } while (dplusr && (logousery.startsWith(`${libapminsightbX.length}`)));
          let utilsY = String.fromCharCode(108,95,54,50,95,116,97,103,103,101,100,0);
          let subtextA = String.fromCharCode(116,101,114,109,105,110,97,108,95,111,95,54,51,0);
          let dangerD = true;
         resend_ = 79 < basketballhometeamc || 79 < libapminsightbX.length;
         utilsY = `${utilsY.length}`;
         subtextA = `${(subtextA.length >> (Math.min(5, Math.abs((dangerD ? 5 : 5)))))}`;
      giflivestreaminga = `${giflivestreamingP.length << (Math.min(Math.abs(2), 3))}`;
   }
                if (await InAppBrowser.isAvailable()) {

      holderr = [giflivestreaminga.length & 1];
      holderr = [1 ^ time_zxM.length];
                    await InAppBrowser.open(url);
                } else {

      sharewhiteh = giflivestreaminga == String.fromCharCode(122,0) || confirmation7.size == 4;
      awayteamfieldM += `${((sharewhiteh ? 5 : 3) | libloggerF.length)}`;
                    Linking.openURL(url);
                }
            } catch (e) {

      awayteamfieldM += `${confirmation7.size / 3}`;
   let defaultprofilepicP = awayteamfieldM.length >= 9013326;
   do {
       let downarrowZ: Array<any> = [491, 266, 458];
       let paginationK = false;
          let yellowscoreballu = 2.0;
         paginationK = !paginationK;
         yellowscoreballu -= parseFloat(`${parseInt(`${yellowscoreballu}`)}`);
         downarrowZ.push(((paginationK ? 3 : 3) / 3));
      for (let b = 0; b < 2; b++) {
          let saveZ = String.fromCharCode(97,116,114,97,99,112,108,117,115,95,103,95,55,53,0);
          let controlsk = 2;
         downarrowZ.push((String.fromCharCode(65,0) == saveZ ? saveZ.length : controlsk));
      }
      if (!paginationK) {
         downarrowZ = [((paginationK ? 5 : 2) % 2)];
      }
          let crossO: Array<any> = [12, 879];
          let basketballawayteamo = 5;
         downarrowZ.push(basketballawayteamo << (Math.min(4, Math.abs(1))));
         crossO.push(crossO.length);
         basketballawayteamo |= 1;
          let whistleQ = false;
          let moon1 = String.fromCharCode(107,95,54,57,95,103,111,101,114,108,105,0);
          let defaultlogoh = String.fromCharCode(105,95,56,48,95,120,99,104,97,99,104,97,0);
         paginationK = defaultlogoh.endsWith(`${whistleQ}`);
         whistleQ = 86 <= moon1.length;
         moon1 = `${(moon1 == String.fromCharCode(109,0) ? moon1.length : moon1.length)}`;
      awayteamfieldM += `${(shielddoney == String.fromCharCode(53,0) ? shielddoney.length : parseInt(`${relatedL}`))}`;
      if (defaultprofilepicP) {
         break;
      }
   } while (defaultprofilepicP && (awayteamfieldM.startsWith(`${relatedL}`)));
                Linking.openURL(url);
            }

        } else { 

      shielddoney += `${confirmation7.size >> (Math.min(Math.abs(2), 4))}`;
   let singaporeu = libloggerF.length >= 5206160;
   do {
      libloggerF = `${1 >> (Math.min(1, libloggerF.length))}`;
      if (singaporeu) {
         break;
      }
   } while ((libloggerF.length < parseInt(`${relatedL}`)) && singaporeu);
            Linking.openURL(url);
        }

        if (onPress) onPress({
            slot_id: bannerAd.ads_slot_id,
            id: bannerAd.ads_id,
            title: bannerAd.ads_event_title,
            name: bannerAd.ads_name,
        });
    };

    return (
        <TouchableOpacity
            style={{
                paddingVertical: 5,
            }}
            onPress={redirectToAd}>
            <FastImage
                source={{
                    uri: bannerAd?.ads_pic,
                }}
                style={{ width: '100%', aspectRatio: 64 / 10, borderRadius: 8 }}
                resizeMode={'contain'}
                useFastImage={true}
            />
        </TouchableOpacity>
    );
};
