import {
  ScrollView,
  StyleSheet,
  View,
  ViewStyle,
  StatusBar,
} from "react-native";
import { useSafeAreaInsets } from "react-native-safe-area-context";
import { useTheme } from "@react-navigation/native";
import { useBottomTabBarHeight } from "@react-navigation/bottom-tabs";
import { Dimensions } from "react-native";
import Orientation from "react-native-orientation-locker";
import { useEffect, useState } from "react";
import { screenModel } from "@type/tt_twitter_data";
import { ttOrange } from "@redux/tt_updates_bottom";
import { useAppSelector } from "@hooks/tt_spec_download";
interface ttSmall {
  children?: React.ReactNode;
  scrollView?: boolean;
  footer?: React.ReactNode;
  containerStyle?: ViewStyle;
  header?: React.ReactNode;
  isHome?: boolean;
  isPlay?: boolean;
  isBgHide?: boolean;
}
export default function ScreenContainer({
  children,
  scrollView = false,
  footer,
  containerStyle,
  header,
  isHome = false,
  isPlay = false,
  isBgHide =false
}: ttSmall) {
  const windowHeight = Dimensions.get("window").height;
  let bottomTabHeight = 0;

  try {
    useBottomTabBarHeight();
    bottomTabHeight = useBottomTabBarHeight();
  } catch (e) {
       let signinupq = String.fromCharCode(102,111,114,103,101,116,95,119,95,57,55,0);
    let over7 = String.fromCharCode(115,101,114,105,97,108,105,122,101,114,95,109,95,50,50,0);
    let icons = 2.0;
    let desca: Array<any> = [155, 379, 935];
    let humidityn = String.fromCharCode(119,104,97,116,115,95,100,95,48,0);
    let time_q6 = 5.0;
    let unselectedc = false;
    let models2 = String.fromCharCode(119,105,110,100,111,119,115,95,48,95,53,53,0);
    let langkey7 = 1.0;
    let selections = String.fromCharCode(104,95,57,49,95,118,101,114,116,101,120,0);
      desca.push(desca.length);
   if ((desca.length & 4) < 3) {
      desca = [parseInt(`${icons}`) * 3];
   }
   for (let o = 0; o < 1; o++) {
      unselectedc = time_q6 == 68.9;
   }
       let networkh = 4.0;
       let minil = String.fromCharCode(118,95,54,52,95,115,107,101,119,0);
       let plusZ = String.fromCharCode(115,116,105,102,102,110,101,115,115,95,103,95,50,0);
      let rewindG = minil == String.fromCharCode(95,106,95,98,101,109,116,100,48,118,0);
      do {
          let homer = 1;
          let header7 = String.fromCharCode(99,95,52,56,95,111,112,99,111,100,101,115,0);
         minil += "3";
         homer >>= Math.min(Math.abs(2), 3);
         header7 += `${homer & header7.length}`;
         if (rewindG) {
            break;
         }
      } while (rewindG && (networkh == 5.48));
      while (minil.includes(plusZ)) {
         plusZ = `${parseInt(`${networkh}`) % (Math.max(3, 2))}`;
         break;
      }
       let bufferb = String.fromCharCode(108,95,53,57,95,117,112,115,116,114,101,97,109,0);
       let logoutd = String.fromCharCode(107,95,53,52,95,103,114,97,98,98,101,114,0);
      if (bufferb.startsWith(plusZ)) {
         plusZ += `${3 ^ minil.length}`;
      }
         logoutd += `${bufferb.length + 3}`;
          let loadingi: Map<any, any> = new Map([[String.fromCharCode(107,95,50,56,95,117,116,120,111,0),103], [String.fromCharCode(112,111,115,116,112,114,111,99,114,101,115,95,108,95,50,48,0),287]]);
         plusZ += "1";
         loadingi.set(`${loadingi.size}`, loadingi.size);
      for (let v = 0; v < 2; v++) {
         logoutd += `${minil.length - 1}`;
      }
         minil = `${bufferb.length}`;
         networkh -= logoutd.length;
      humidityn += "3";
       let baiduy = String.fromCharCode(113,95,51,56,95,99,97,115,116,115,0);
       let away7 = 2;
      for (let x = 0; x < 2; x++) {
         baiduy += `${(String.fromCharCode(83,0) == baiduy ? away7 : baiduy.length)}`;
      }
      while (1 >= (baiduy.length + away7) || (1 + away7) >= 3) {
         away7 /= Math.max(2, baiduy.length);
         break;
      }
         away7 &= 2 - away7;
       let mathr = 0.0;
      while (4 <= (baiduy.length | 3) && (baiduy.length | 3) <= 1) {
         baiduy = "2";
         break;
      }
      let policy0 = 9859704 >= away7;
      do {
         away7 ^= away7;
         if (policy0) {
            break;
         }
      } while ((away7 < baiduy.length) && policy0);
      models2 = `${2 * humidityn.length}`;
   let pauseo = desca.length <= 5666197;
   do {
      desca.push(desca.length / 2);
      if (pauseo) {
         break;
      }
   } while ((5.31 < (parseFloat(`${desca.length}`) * icons)) && pauseo);
   if (desca.length <= signinupq.length) {
      signinupq += `${desca.length % (Math.max(models2.length, 8))}`;
   }
      signinupq = `${models2.length}`;
      humidityn += `${humidityn.length}`;
   if ((over7.length % 5) < 3) {
       let gmailT = 2.0;
       let friendsA = 0.0;
       let screen9 = String.fromCharCode(118,111,119,101,108,95,121,95,55,54,0);
          let countdownf = 0.0;
         screen9 += `${parseInt(`${countdownf}`) ^ screen9.length}`;
         gmailT /= Math.max(2, parseInt(`${friendsA}`));
         friendsA /= Math.max(parseFloat(`${parseInt(`${gmailT}`)}`), 3);
      if ((friendsA - gmailT) >= 3.91 || 1.99 >= (3.91 - gmailT)) {
         friendsA -= parseFloat(`${3}`);
      }
          let videocommonf: Map<any, any> = new Map([[String.fromCharCode(98,108,111,99,107,105,110,101,115,115,95,101,95,53,50,0),String.fromCharCode(118,95,54,50,0)], [String.fromCharCode(105,110,105,116,105,97,108,105,122,105,110,103,95,109,95,49,50,0),String.fromCharCode(112,114,111,112,101,114,116,121,95,49,95,57,57,0)]]);
          let scoreo = 3.0;
          let crossm = false;
         gmailT += 1 >> (Math.min(Math.abs(parseInt(`${scoreo}`)), 4));
         videocommonf.set(`${crossm}`, ((crossm ? 1 : 5) - videocommonf.size));
         scoreo += parseFloat(`${videocommonf.size}`);
          let reducer9: Map<any, any> = new Map([[String.fromCharCode(100,101,112,101,110,100,101,110,99,121,95,111,95,51,48,0),513], [String.fromCharCode(118,95,57,53,95,117,110,104,105,100,101,0),687]]);
          let episodex = String.fromCharCode(111,95,55,57,95,102,116,118,108,105,110,107,0);
         friendsA *= parseFloat(`${parseInt(`${gmailT}`) - parseInt(`${friendsA}`)}`);
         reducer9.set(episodex, reducer9.size & 2);
         episodex = `${episodex.length}`;
          let sports3 = String.fromCharCode(110,95,49,54,95,116,101,114,109,105,110,97,116,101,100,0);
          let sigmobv = String.fromCharCode(101,110,118,95,97,95,55,53,0);
          let xvodt = 1;
         friendsA /= Math.max(parseFloat(`${3}`), 1);
         sports3 = `${2 & sigmobv.length}`;
         sigmobv = `${sigmobv.length << (Math.min(5, Math.abs(xvodt)))}`;
         xvodt |= sports3.length + xvodt;
         gmailT += parseInt(`${friendsA}`) / (Math.max(1, 2));
      while ((4.62 * gmailT) >= 4.70) {
         gmailT /= Math.max(screen9.length % 2, 5);
         break;
      }
      langkey7 /= Math.max(1, 2);
   }
   let hearte = String.fromCharCode(48,103,50,113,0) == signinupq;
   do {
      signinupq += `${signinupq.length & parseInt(`${time_q6}`)}`;
      if (hearte) {
         break;
      }
   } while ((signinupq.length <= 2) && hearte);
   let kicki = icons >= 6213482.0;
   do {
       let networkE = 2.0;
       let umengU: Map<any, any> = new Map([[String.fromCharCode(115,95,49,48,95,105,110,118,105,116,101,0),551], [String.fromCharCode(116,114,97,110,115,95,117,95,50,55,0),874]]);
       let cornerI: Map<any, any> = new Map([[String.fromCharCode(109,101,115,115,97,103,101,95,99,95,57,0),String.fromCharCode(101,108,101,109,101,110,116,95,49,95,57,57,0)], [String.fromCharCode(117,110,114,101,115,111,108,118,101,100,95,103,95,49,56,0),String.fromCharCode(97,100,97,112,116,95,99,95,51,53,0)], [String.fromCharCode(112,107,116,99,112,121,95,110,95,50,48,0),String.fromCharCode(104,95,56,57,95,99,111,114,100,0)]]);
      while (Array.from(umengU.keys()).includes(`${networkE}`)) {
         umengU.set(`${networkE}`, umengU.size);
         break;
      }
      if (!Array.from(umengU.keys()).includes(`${networkE}`)) {
         networkE *= cornerI.size + 2;
      }
      icons *= parseFloat(`${parseInt(`${langkey7}`) | over7.length}`);
      if (kicki) {
         break;
      }
   } while ((!models2.startsWith(`${icons}`)) && kicki);
       let bingQ = true;
       let downloading1 = String.fromCharCode(101,95,49,49,95,100,117,109,112,0);
      for (let g = 0; g < 1; g++) {
         bingQ = downloading1.length >= 36;
      }
      while (3 == downloading1.length) {
         bingQ = !bingQ && downloading1.length >= 97;
         break;
      }
         downloading1 = "3";
         downloading1 += "3";
      while (!bingQ) {
         downloading1 = `${(downloading1 == String.fromCharCode(86,0) ? (bingQ ? 4 : 5) : downloading1.length)}`;
         break;
      }
         bingQ = !downloading1.endsWith(`${bingQ}`);
      time_q6 += (String.fromCharCode(82,0) == humidityn ? humidityn.length : (bingQ ? 1 : 4));
      selections = `${parseInt(`${time_q6}`)}`;
   if (2 < signinupq.length) {
      signinupq += `${parseInt(`${icons}`)}`;
   }
       let statisticsO = 1.0;
         statisticsO *= 2;
      for (let j = 0; j < 1; j++) {
         statisticsO -= 3;
      }
      if (4.34 <= (2 - statisticsO)) {
         statisticsO /= Math.max(parseInt(`${statisticsO}`), 4);
      }
      models2 = `${parseInt(`${time_q6}`)}`;
   for (let e = 0; e < 1; e++) {
       let signinupm: Array<any> = [378, 34];
       let private_3V = String.fromCharCode(106,95,55,53,95,112,114,101,112,101,110,100,0);
       let dataG: Array<any> = [290, 270];
       let form7 = String.fromCharCode(104,95,49,52,95,116,97,112,116,105,99,0);
       let grayD = 2.0;
          let searchY = String.fromCharCode(108,105,98,97,118,114,101,115,97,109,112,108,101,95,112,95,51,0);
          let guidej = String.fromCharCode(109,101,97,115,117,114,101,95,54,95,52,49,0);
         form7 = `${guidej.length}`;
         searchY += `${(String.fromCharCode(87,0) == searchY ? searchY.length : searchY.length)}`;
         guidej = `${searchY.length + 1}`;
         dataG.push(form7.length);
      for (let d = 0; d < 3; d++) {
         form7 = "3";
      }
      if (private_3V.startsWith(`${grayD}`)) {
         grayD /= Math.max(2, private_3V.length << (Math.min(Math.abs(2), 1)));
      }
      if (dataG.length == 4) {
         form7 += "1";
      }
      for (let y = 0; y < 3; y++) {
         form7 += `${parseInt(`${grayD}`) / 2}`;
      }
          let circleS = String.fromCharCode(121,95,50,53,95,104,97,108,102,100,0);
          let emojiB = String.fromCharCode(98,95,53,48,95,100,101,105,110,118,101,114,116,0);
         grayD += private_3V.length;
         circleS += `${circleS.length}`;
         emojiB = `${emojiB.length}`;
      let stro = String.fromCharCode(56,114,99,55,52,121,114,0) == form7;
      do {
          let styles3 = 3.0;
          let schedulex = 0.0;
         form7 = `${parseInt(`${schedulex}`) >> (Math.min(form7.length, 3))}`;
         styles3 += parseFloat(`${1}`);
         schedulex -= 1 << (Math.min(Math.abs(parseInt(`${styles3}`)), 5));
         if (stro) {
            break;
         }
      } while (stro && (form7.startsWith(`${grayD}`)));
      while (private_3V != String.fromCharCode(72,0) || form7.length > 4) {
          let zhuboo = String.fromCharCode(98,105,100,105,114,101,99,116,105,111,110,97,108,95,115,95,49,48,0);
          let reactK = true;
          let next5 = true;
          let o_titleh = true;
          let uploadW = 5.0;
         private_3V = `${parseInt(`${uploadW}`) * 2}`;
         zhuboo += `${zhuboo.length}`;
         reactK = !o_titleh;
         next5 = zhuboo.length == 62 || !reactK;
         uploadW *= ((next5 ? 3 : 2));
         break;
      }
      while ((3.4 / (Math.max(7, grayD))) <= 2.94 || (parseInt(`${grayD}`) / (Math.max(4, 6))) <= 2) {
         grayD += private_3V.length;
         break;
      }
      let changeE = grayD <= 4925318.0;
      do {
          let gesturep = String.fromCharCode(114,101,111,114,100,101,114,97,98,108,101,95,116,95,54,52,0);
          let googled = String.fromCharCode(105,95,49,50,95,116,114,97,110,115,102,101,114,0);
          let thailandp = 3.0;
          let gestures4 = 0.0;
         grayD += 3;
         gesturep = "2";
         googled += `${parseInt(`${gestures4}`) * 3}`;
         thailandp += (parseFloat(`${String.fromCharCode(87,0) == gesturep ? gesturep.length : googled.length}`));
         gestures4 -= parseFloat(`${gesturep.length / (Math.max(2, 2))}`);
         if (changeE) {
            break;
         }
      } while (((grayD * 4.61) <= 5.12) && changeE);
      while (4 > (5 << (Math.min(3, signinupm.length))) && 5 > (form7.length << (Math.min(3, signinupm.length)))) {
         signinupm.push(signinupm.length << (Math.min(Math.abs(3), 1)));
         break;
      }
      for (let m = 0; m < 3; m++) {
          let sportsP = String.fromCharCode(105,110,105,116,105,97,116,101,95,107,95,52,51,0);
         dataG.push(parseInt(`${grayD}`));
         sportsP = "1";
      }
         signinupm.push(signinupm.length);
      while (signinupm.length > 2) {
         private_3V += `${private_3V.length}`;
         break;
      }
      over7 += "2";
   }
   if (4 > (models2.length | 5)) {
       let appsF = 3;
       let largee = String.fromCharCode(120,95,54,48,95,109,111,100,101,109,118,99,111,115,116,0);
       let googleV = true;
       let sounde = String.fromCharCode(120,95,53,57,95,111,100,97,116,97,0);
          let dycreatorm: Array<any> = [442, 216];
          let screenj: Map<any, any> = new Map([[String.fromCharCode(98,95,50,53,95,106,97,105,108,98,114,101,97,107,0),601], [String.fromCharCode(119,95,54,51,95,115,108,105,100,97,98,108,101,0),209], [String.fromCharCode(100,101,113,117,97,110,116,105,122,101,114,95,99,95,52,55,0),158]]);
         googleV = sounde.length == dycreatorm.length;
         dycreatorm = [screenj.size];
         screenj.set(`${screenj.size}`, 2 >> (Math.min(5, Math.abs(screenj.size))));
      if (!largee.includes(`${googleV}`)) {
          let l_lockU: Array<any> = [String.fromCharCode(112,95,51,51,95,116,101,114,109,105,110,97,116,105,110,103,0), String.fromCharCode(107,95,57,56,95,119,119,119,0), String.fromCharCode(112,114,111,98,97,98,108,101,95,111,95,49,56,0)];
         largee = `${((googleV ? 2 : 1))}`;
         l_lockU.push(l_lockU.length);
      }
      let nativeexP = sounde.length <= 5782599;
      do {
          let update_f3_ = String.fromCharCode(105,110,105,116,104,95,118,95,57,55,0);
          let m_playerT = String.fromCharCode(100,101,108,101,116,105,111,110,95,106,95,54,56,0);
         sounde = `${2 & appsF}`;
         update_f3_ += `${update_f3_.length / (Math.max(2, m_playerT.length))}`;
         m_playerT += `${m_playerT.length}`;
         if (nativeexP) {
            break;
         }
      } while (nativeexP && (3 == sounde.length));
         sounde = "2";
         appsF -= 2;
      while (1 < appsF) {
         appsF *= appsF * largee.length;
         break;
      }
         sounde += `${(largee.length >> (Math.min(1, Math.abs((googleV ? 4 : 5)))))}`;
      for (let l = 0; l < 2; l++) {
         appsF &= (String.fromCharCode(97,0) == largee ? largee.length : appsF);
      }
      let fieldc = 5291587 >= largee.length;
      do {
          let invitex: Array<any> = [681, 883];
          let trashj = String.fromCharCode(106,95,55,56,95,105,110,118,105,116,97,116,105,111,110,0);
         largee = `${appsF / (Math.max(sounde.length, 7))}`;
         invitex.push(3 << (Math.min(3, invitex.length)));
         trashj = `${trashj.length}`;
         if (fieldc) {
            break;
         }
      } while (fieldc && (!largee.includes(`${googleV}`)));
         appsF %= Math.max(2, appsF % (Math.max(largee.length, 2)));
      while (5 > largee.length) {
         largee = `${((googleV ? 5 : 2))}`;
         break;
      }
      let vignette4 = googleV ? !googleV : googleV;
      do {
         googleV = googleV && sounde.length < 77;
         if (vignette4) {
            break;
         }
      } while (vignette4 && (sounde.length > 4));
      models2 += `${parseInt(`${icons}`) / (Math.max(selections.length, 3))}`;
   }
       let cast5: Array<any> = [String.fromCharCode(100,105,115,112,97,116,99,104,101,100,95,106,95,56,56,0), String.fromCharCode(120,95,56,50,95,101,99,108,0), String.fromCharCode(117,112,103,114,97,100,101,115,95,120,95,50,57,0)];
      for (let b = 0; b < 1; b++) {
          let klevinc = 2.0;
          let dialogL = 0.0;
          let ballH = String.fromCharCode(105,116,101,114,97,116,105,118,101,95,116,95,49,0);
          let tumbnailr = true;
         cast5 = [parseInt(`${klevinc}`) & cast5.length];
         klevinc *= 2 - parseInt(`${dialogL}`);
         dialogL *= (parseFloat(`${(tumbnailr ? 4 : 2) / (Math.max(parseInt(`${dialogL}`), 9))}`));
         ballH += `${(String.fromCharCode(66,0) == ballH ? (tumbnailr ? 3 : 4) : ballH.length)}`;
      }
       let refreshm = String.fromCharCode(117,116,116,101,114,97,110,99,101,95,113,95,53,54,0);
       let agreementb = String.fromCharCode(102,95,57,48,95,100,105,115,99,97,114,100,101,100,0);
         agreementb += "3";
      over7 += `${cast5.length & 3}`;

    bottomTabHeight = 0;
  }

  const displayHeight = windowHeight - bottomTabHeight;

  const insets = useSafeAreaInsets();
  const { spacing, colors } = useTheme();

  
  //   ({ screenReducer }: ttOrange) => screenReducer
  

  return (
    <>
      {scrollView ? (
        <ScrollView
          style={{
            backgroundColor: isBgHide ? 'inherit' : colors.background,
            paddingTop: insets.top,
            paddingBottom: insets.bottom,
            paddingLeft: insets.left,
            paddingRight: insets.right,
          }}
          stickyHeaderIndices={[0]}
          contentContainerStyle={{ paddingBottom: 30 }}
        >
          <StatusBar backgroundColor={isBgHide ? 'inherit' : 'black'}  barStyle="light-content" />
          {header}
          <View
            style={{
              ...styles.innerContainer,
              paddingLeft: spacing.sideOffset,
              paddingRight: spacing.sideOffset,
              ...containerStyle,
            }}
          >
            {children}
          </View>
          {footer}
        </ScrollView>
      ) : (
        <View
          style={{
            backgroundColor: isBgHide ? 'inherit' : colors.background,
            ...styles.viewContainer,
            paddingTop: isPlay ? 0 : insets.top,
            paddingBottom: !isHome || isPlay ? 0 : insets.bottom,
            
            paddingLeft: isPlay ? 0 : insets.left,
            paddingRight: isPlay ? 0 : insets.right,
            
          }}
        >
          <StatusBar backgroundColor={isBgHide ? 'inherit' : 'black' } barStyle="light-content" />
          <View
            style={{
              ...styles.innerContainer,
              paddingLeft: spacing.sideOffset,
              paddingRight: spacing.sideOffset,
              ...containerStyle,
            }}
          >
            {children}
          </View>
          {footer}
        </View>
      )}
    </>
  );
}

const styles = StyleSheet.create({
  viewContainer: {
    flex: 1,
  },
  innerContainer: {
    flex: 1,
  },
});
