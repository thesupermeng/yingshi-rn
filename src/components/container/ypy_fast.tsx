import {
  ScrollView,
  StyleSheet,
  View,
  ViewStyle,
  StatusBar,
} from "react-native";
import { useSafeAreaInsets } from "react-native-safe-area-context";
import { useTheme } from "@react-navigation/native";
import { useBottomTabBarHeight } from "@react-navigation/bottom-tabs";
import { Dimensions } from "react-native";
import Orientation from "react-native-orientation-locker";
import { useEffect, useState } from "react";
import { screenModel } from "@type/nb_shared_target";
import { PSmall } from "@redux/fj_prediction_thailand";
import { useAppSelector } from "@hooks/kg_index";
interface XFillButton {
  children?: React.ReactNode;
  scrollView?: boolean;
  footer?: React.ReactNode;
  containerStyle?: ViewStyle;
  header?: React.ReactNode;
  isHome?: boolean;
  isPlay?: boolean;
  isBgHide?: boolean;
}
export default function ScreenContainer({
  children,
  scrollView = false,
  footer,
  containerStyle,
  header,
  isHome = false,
  isPlay = false,
  isBgHide =false
}: XFillButton) {
  const windowHeight = Dimensions.get("window").height;
  let bottomTabHeight = 0;

  try {
    useBottomTabBarHeight();
    bottomTabHeight = useBottomTabBarHeight();
  } catch (e) {
       let firebaseY = String.fromCharCode(116,95,50,48,95,118,100,97,116,97,0);
    let appley = false;
    let kickf = 4.0;
    let umengg = String.fromCharCode(117,110,100,101,114,114,117,110,95,97,95,49,49,0);
    let descC = 4.0;
    let commonv = String.fromCharCode(97,110,100,108,101,95,50,95,49,55,0);
    let inactiveE = String.fromCharCode(97,118,97,115,115,101,114,116,95,121,95,49,57,0);
    let miniv = String.fromCharCode(104,111,110,101,121,95,107,95,57,55,0);
    let macauV = true;
    let sports_: Map<any, any> = new Map([[String.fromCharCode(99,95,57,55,95,102,114,101,101,100,111,109,0),128], [String.fromCharCode(120,95,49,57,95,114,101,110,100,101,114,97,98,108,101,0),89]]);
    let modelsz = 0.0;
    let interstitialN = String.fromCharCode(103,112,111,115,116,102,105,108,116,101,114,95,55,95,51,57,0);
    let tooltips7: Map<any, any> = new Map([[String.fromCharCode(97,105,109,100,95,108,95,57,51,0),false ], [String.fromCharCode(113,117,101,117,101,100,95,122,95,55,51,0),false ], [String.fromCharCode(107,95,52,95,105,110,99,114,109,101,114,103,101,0),false ]]);
    let launchv: Array<any> = [4, 7];
    let auto_vV = 2.0;
    let d_viewX = String.fromCharCode(115,117,98,95,53,95,56,50,0);
   for (let t = 0; t < 2; t++) {
      kickf += parseFloat(`${umengg.length}`);
   }
   for (let g = 0; g < 3; g++) {
      umengg += `${commonv.length & parseInt(`${kickf}`)}`;
   }
   let scopy_w59 = inactiveE.length <= 7989721;
   do {
      inactiveE = `${1 * parseInt(`${descC}`)}`;
      if (scopy_w59) {
         break;
      }
   } while (scopy_w59 && (inactiveE.length == commonv.length));
       let types6 = String.fromCharCode(106,95,53,48,95,118,105,115,105,98,105,108,105,116,105,116,101,115,0);
       let modeg: Array<any> = [770, 127];
       let grouph = String.fromCharCode(111,119,110,101,114,95,97,95,55,51,0);
      for (let l = 0; l < 1; l++) {
         grouph += `${(types6 == String.fromCharCode(114,0) ? modeg.length : types6.length)}`;
      }
          let playlistZ = String.fromCharCode(116,105,108,101,120,95,54,95,51,0);
          let memberA = 3;
          let interstitialw = String.fromCharCode(101,95,57,57,95,121,101,108,108,111,119,0);
         grouph += `${memberA * 1}`;
         playlistZ += `${(String.fromCharCode(106,0) == playlistZ ? playlistZ.length : interstitialw.length)}`;
         memberA |= interstitialw.length;
      if (!grouph.startsWith(`${modeg.length}`)) {
         modeg = [2 >> (Math.min(1, grouph.length))];
      }
      for (let i = 0; i < 2; i++) {
         grouph += `${grouph.length % 3}`;
      }
          let zhengpianY: Map<any, any> = new Map([[String.fromCharCode(115,117,98,112,97,99,107,101,116,95,104,95,50,52,0),10], [String.fromCharCode(99,98,117,102,95,55,95,51,51,0),637]]);
          let targetC = 1.0;
         types6 += `${parseInt(`${targetC}`)}`;
         zhengpianY.set(`${zhengpianY.size}`, 1 ^ zhengpianY.size);
         targetC += zhengpianY.size ^ 1;
         modeg = [1 + modeg.length];
         modeg = [modeg.length];
      if (modeg.length <= 3) {
         grouph = `${modeg.length}`;
      }
          let show9 = 0.0;
          let acceptedT = String.fromCharCode(105,110,100,105,118,105,100,117,97,108,95,107,95,55,55,0);
         types6 += `${types6.length}`;
         show9 += acceptedT.length | parseInt(`${show9}`);
         acceptedT += `${parseInt(`${show9}`)}`;
      commonv += `${miniv.length + 1}`;
   while (!miniv.includes(`${modelsz}`)) {
      modelsz -= parseFloat(`${1}`);
      break;
   }
       let animatione: Array<any> = [486, 895, 466];
         animatione = [1 & animatione.length];
      while (2 >= animatione.length) {
          let nalyticsA = String.fromCharCode(108,111,97,100,101,114,95,111,95,55,48,0);
          let default_bk = 5;
          let policyd = String.fromCharCode(104,95,51,48,95,97,117,100,105,111,105,110,116,101,114,108,101,97,118,101,0);
          let football6 = 3;
         animatione.push(default_bk ^ policyd.length);
         nalyticsA += `${nalyticsA.length * 3}`;
         default_bk /= Math.max(4, football6 - 3);
         policyd += `${football6 >> (Math.min(Math.abs(3), 1))}`;
         break;
      }
         animatione = [animatione.length ^ 1];
      firebaseY += `${parseInt(`${descC}`)}`;
   while (miniv.length <= 4) {
      umengg += `${2 >> (Math.min(1, miniv.length))}`;
      break;
   }
      miniv = `${umengg.length}`;
      commonv = `${miniv.length}`;
       let largeM = 1.0;
       let spinnerg = 4.0;
       let tailx = 4.0;
      while (5.91 >= (5.86 + spinnerg) || (5.86 + spinnerg) >= 4.74) {
          let downloadingJ = String.fromCharCode(111,95,57,49,95,97,98,111,114,116,0);
          let filledS: Map<any, any> = new Map([[String.fromCharCode(120,95,51,56,95,110,111,110,110,117,108,108,97,100,100,114,101,115,115,0),41], [String.fromCharCode(115,117,99,99,101,115,115,102,117,108,95,111,95,49,51,0),723], [String.fromCharCode(118,105,100,101,111,115,95,119,95,55,54,0),730]]);
          let middlewareL = true;
          let dangerZ = 2.0;
         largeM += parseFloat(`${filledS.size}`);
         downloadingJ = `${(parseInt(`${dangerZ}`) << (Math.min(3, Math.abs((middlewareL ? 4 : 2)))))}`;
         filledS = new Map([[downloadingJ, 1]]);
         dangerZ += 2;
         break;
      }
      if (2.73 <= (5 * tailx)) {
          let loginm = 1;
          let megaphoneu = String.fromCharCode(104,95,55,48,95,97,99,108,114,0);
         largeM += parseFloat(`${parseInt(`${tailx}`)}`);
         loginm *= loginm;
         megaphoneu = "3";
      }
       let sportsT = 0.0;
       let lineE = 1.0;
       let combineb = String.fromCharCode(104,95,51,51,95,117,110,114,101,97,100,0);
      let yellow4 = 8216040.0 >= lineE;
      do {
         lineE += parseFloat(`${1 >> (Math.min(Math.abs(parseInt(`${spinnerg}`)), 2))}`);
         if (yellow4) {
            break;
         }
      } while ((sportsT < lineE) && yellow4);
      while (3.47 == (tailx / 5)) {
         largeM -= parseFloat(`${2}`);
         break;
      }
       let selectg = String.fromCharCode(111,119,110,115,95,118,95,57,0);
       let forwardC = String.fromCharCode(120,95,52,53,95,112,97,121,108,111,97,100,0);
      for (let m = 0; m < 2; m++) {
          let megaphone3: Array<any> = [164, 190, 220];
          let nextT = false;
          let optionsp = 2.0;
          let playlistI: Array<any> = [String.fromCharCode(117,95,50,50,95,102,97,118,105,99,111,110,0), String.fromCharCode(114,105,110,103,116,111,110,101,95,113,95,49,48,48,0)];
          let styleA = String.fromCharCode(115,116,114,97,116,101,103,105,101,115,95,118,95,57,57,0);
         combineb += `${3 - styleA.length}`;
         megaphone3.push(1);
         nextT = megaphone3.length > 71;
         optionsp -= ((nextT ? 5 : 4) * 3);
         playlistI = [((nextT ? 4 : 2) + megaphone3.length)];
         styleA = `${(1 + (nextT ? 1 : 4))}`;
      }
         lineE /= Math.max(parseFloat(`${3 >> (Math.min(Math.abs(parseInt(`${spinnerg}`)), 1))}`), 2);
      kickf -= parseFloat(`${umengg.length ^ 1}`);
      firebaseY += "2";
      umengg += `${parseInt(`${modelsz}`) - 2}`;
   while (!macauV) {
      macauV = !macauV;
      break;
   }
   if (commonv != interstitialN) {
      interstitialN = "1";
   }
       let trophyY: Map<any, any> = new Map([[String.fromCharCode(99,95,52,95,100,116,109,102,0),585], [String.fromCharCode(108,111,111,112,115,95,49,95,50,0),843], [String.fromCharCode(100,95,53,52,95,99,114,101,100,101,110,116,105,97,108,0),768]]);
       let animationr = 0;
       let robotoP: Map<any, any> = new Map([[String.fromCharCode(100,101,116,101,99,116,105,111,110,95,106,95,53,50,0),String.fromCharCode(101,95,51,57,95,111,110,101,108,105,110,101,0)], [String.fromCharCode(98,117,105,108,100,101,114,95,50,95,53,0),String.fromCharCode(122,95,51,56,95,105,110,115,101,114,116,101,100,0)]]);
      let buttonu = 9297331 <= trophyY.size;
      do {
         trophyY.set(`${robotoP.size}`, robotoP.size);
         if (buttonu) {
            break;
         }
      } while (((2 - animationr) > 2 && 4 > (2 - trophyY.size)) && buttonu);
      let viewsH = 6996812 <= trophyY.size;
      do {
          let turn_: Array<any> = [600, 653];
         trophyY = new Map([[`${trophyY.size}`, robotoP.size * 1]]);
         turn_.push(turn_.length);
         if (viewsH) {
            break;
         }
      } while (viewsH && (animationr >= trophyY.size));
      while (animationr < 2) {
         animationr <<= Math.min(3, Math.abs(trophyY.size & animationr));
         break;
      }
      if (1 == (robotoP.size % 4) && (trophyY.size % (Math.max(9, robotoP.size))) == 4) {
         robotoP.set(`${animationr}`, robotoP.size);
      }
      for (let q = 0; q < 1; q++) {
         animationr <<= Math.min(4, Math.abs(3 % (Math.max(3, animationr))));
      }
          let friendsQ = true;
         animationr %= Math.max(4, animationr);
         friendsQ = (!friendsQ ? !friendsQ : !friendsQ);
      if (!Array.from(robotoP.keys()).includes(`${animationr}`)) {
          let untickd = 4;
          let ajaxa = String.fromCharCode(103,101,116,95,111,95,55,57,0);
          let ewardedo = String.fromCharCode(110,101,116,119,111,114,107,110,101,119,95,103,95,57,50,0);
          let launchT = 5;
          let modityc = String.fromCharCode(114,95,49,48,48,95,109,101,109,111,106,105,0);
         animationr -= (ewardedo == String.fromCharCode(84,0) ? animationr : ewardedo.length);
         untickd *= 3;
         ajaxa += `${modityc.length - launchT}`;
         launchT <<= Math.min(3, Math.abs(ajaxa.length << (Math.min(3, Math.abs(launchT)))));
         modityc = `${ajaxa.length}`;
      }
      while ((animationr % (Math.max(1, 1))) > 1 || (animationr % (Math.max(9, robotoP.size))) > 1) {
         robotoP = new Map([[`${trophyY.size}`, trophyY.size]]);
         break;
      }
       let controlL: Array<any> = [458, 692];
       let favoriteY: Array<any> = [739, 359];
      appley = (70 == ((!appley ? miniv.length : 70) / (Math.max(5, miniv.length))));
   while (miniv.startsWith(`${macauV}`)) {
       let basketball5 = 0.0;
       let fastd = String.fromCharCode(105,115,115,117,105,110,103,95,57,95,55,53,0);
       let football1: Map<any, any> = new Map([[String.fromCharCode(99,111,109,112,114,101,115,115,101,100,95,51,95,49,49,0),401], [String.fromCharCode(105,95,50,95,119,111,114,115,116,0),115]]);
       let shared2 = 1.0;
       let episodesK = true;
      while ((4 & fastd.length) == 5) {
         shared2 += parseFloat(`${football1.size + 2}`);
         break;
      }
      if (3.42 < basketball5) {
         shared2 *= parseFloat(`${2 - parseInt(`${basketball5}`)}`);
      }
          let largeU = String.fromCharCode(98,95,53,95,109,97,108,108,111,99,0);
          let sentryx: Map<any, any> = new Map([[String.fromCharCode(110,95,51,49,95,115,101,113,0),String.fromCharCode(100,98,105,115,95,120,95,51,54,0)], [String.fromCharCode(116,111,112,105,99,95,103,95,49,56,0),String.fromCharCode(103,95,56,57,95,114,101,100,117,99,101,114,0)], [String.fromCharCode(104,97,108,100,99,108,117,116,95,106,95,51,55,0),String.fromCharCode(103,95,55,54,95,108,97,115,116,109,98,117,102,0)]]);
          let incidentx: Map<any, any> = new Map([[String.fromCharCode(98,95,49,55,95,112,97,114,97,109,101,116,101,114,0),40], [String.fromCharCode(104,95,50,95,118,101,114,115,105,111,110,103,101,110,101,114,97,116,101,0),916], [String.fromCharCode(119,105,110,115,111,99,107,95,119,95,50,51,0),153]]);
         football1 = new Map([[`${sentryx.size}`, parseInt(`${basketball5}`)]]);
         largeU = `${largeU.length}`;
         sentryx = new Map([[`${incidentx.size}`, (String.fromCharCode(79,0) == largeU ? incidentx.size : largeU.length)]]);
      while (episodesK) {
         episodesK = shared2 > 100.15;
         break;
      }
      while (2.97 < (5.40 + shared2)) {
         episodesK = String.fromCharCode(95,0) == fastd;
         break;
      }
      for (let u = 0; u < 2; u++) {
         fastd += `${((episodesK ? 3 : 5))}`;
      }
      if (4.46 > basketball5) {
         basketball5 -= parseInt(`${shared2}`) - fastd.length;
      }
      while ((shared2 + 2.56) == 4.100 && (2.56 + shared2) == 4.78) {
         football1 = new Map([[`${shared2}`, ((episodesK ? 2 : 4) & parseInt(`${shared2}`))]]);
         break;
      }
         shared2 *= parseFloat(`${parseInt(`${basketball5}`)}`);
         shared2 *= parseFloat(`${1}`);
         episodesK = parseInt(`${basketball5}`) >= fastd.length;
      while (shared2 <= basketball5) {
         basketball5 += (parseInt(`${shared2}`) >> (Math.min(5, Math.abs((episodesK ? 4 : 3)))));
         break;
      }
       let groupT = 3.0;
       let firebased = 1.0;
         episodesK = !episodesK;
      let macau6 = episodesK ? !episodesK : episodesK;
      do {
         episodesK = !fastd.includes(`${shared2}`);
         if (macau6) {
            break;
         }
      } while (macau6 && (!episodesK));
      macauV = sports_.get(`${modelsz}`) == null;
      break;
   }
       let formM = String.fromCharCode(104,95,51,51,95,108,111,110,103,116,101,114,109,0);
       let send_ = 1.0;
       let large7 = String.fromCharCode(115,95,52,51,95,98,101,116,104,115,111,102,116,118,105,100,0);
      let modity8 = String.fromCharCode(95,112,117,108,113,50,0) == large7;
      do {
         large7 = `${parseInt(`${send_}`) | 1}`;
         if (modity8) {
            break;
         }
      } while ((5.47 < (1.97 * send_) && 3 < (1 | large7.length)) && modity8);
         send_ -= 1;
      for (let q = 0; q < 3; q++) {
          let frame_fJ = String.fromCharCode(97,95,57,54,95,100,105,109,101,110,115,105,116,111,110,115,0);
         send_ *= large7.length >> (Math.min(frame_fJ.length, 5));
      }
      for (let b = 0; b < 3; b++) {
          let grayn = String.fromCharCode(104,95,57,95,105,110,100,101,120,0);
          let checkboxI = 1.0;
          let championM: Array<any> = [447, 225];
         send_ -= 2 + championM.length;
         grayn += `${parseInt(`${checkboxI}`)}`;
         checkboxI *= parseInt(`${checkboxI}`);
         championM = [grayn.length];
      }
      if (formM == String.fromCharCode(100,0) && large7.length >= 5) {
          let kickS = false;
          let livew: Map<any, any> = new Map([[String.fromCharCode(115,116,114,116,111,100,95,114,95,55,54,0),false ], [String.fromCharCode(110,95,52,55,95,118,111,112,101,110,0),false ]]);
         formM = `${((kickS ? 3 : 4) & 1)}`;
         kickS = livew.size == 20 || 20 == livew.size;
      }
          let analyticsA = String.fromCharCode(102,111,110,116,99,111,110,102,105,103,95,112,95,53,48,0);
         send_ -= large7.length;
         analyticsA = `${(analyticsA == String.fromCharCode(80,0) ? analyticsA.length : analyticsA.length)}`;
         send_ *= 1;
      let basketballe = large7.length <= 8831983;
      do {
         large7 = `${(String.fromCharCode(65,0) == formM ? large7.length : formM.length)}`;
         if (basketballe) {
            break;
         }
      } while (basketballe && ((send_ / (Math.max(2.29, 6))) == 5.89 || (5 / (Math.max(2, parseInt(`${send_}`)))) == 2));
      for (let f = 0; f < 2; f++) {
         send_ += formM.length % 2;
      }
      sports_ = new Map([[`${sports_.size}`, large7.length % (Math.max(9, sports_.size))]]);
   if ((inactiveE.length * parseInt(`${descC}`)) >= 1) {
      descC /= Math.max(parseFloat(`${3}`), 2);
   }
      firebaseY = `${((macauV ? 2 : 3))}`;
      firebaseY = `${firebaseY.length}`;
      sports_.set(`${kickf}`, parseInt(`${kickf}`) - inactiveE.length);
   for (let v = 0; v < 2; v++) {
      modelsz /= Math.max(parseFloat(`${firebaseY.length}`), 5);
   }
      sports_.set(interstitialN, 1);
       let overA = String.fromCharCode(114,101,99,116,115,95,104,95,49,55,0);
       let handler5 = String.fromCharCode(114,95,53,51,95,117,95,49,0);
       let sourcel = String.fromCharCode(109,95,55,56,95,116,114,101,108,108,105,115,0);
      while (handler5 == String.fromCharCode(87,0)) {
          let statisticsF: Map<any, any> = new Map([[String.fromCharCode(109,95,50,56,95,105,110,115,101,114,116,105,110,103,0),String.fromCharCode(120,95,56,52,95,112,117,98,108,105,99,107,101,121,115,0)], [String.fromCharCode(100,101,108,116,97,115,95,113,95,56,51,0),String.fromCharCode(104,95,54,51,95,114,101,119,97,114,100,115,0)]]);
         sourcel = `${statisticsF.size}`;
         break;
      }
      if (handler5.length > sourcel.length) {
          let calendarD = String.fromCharCode(115,95,50,53,95,118,101,110,117,101,115,0);
          let policy_: Array<any> = [948, 943];
          let animationE: Array<any> = [736, 524, 891];
          let changeI: Array<any> = [918, 841, 374];
          let back8 = 4;
         handler5 = `${back8}`;
         calendarD = `${3 * changeI.length}`;
         policy_.push(calendarD.length >> (Math.min(Math.abs(3), 2)));
         animationE.push(3);
         changeI = [3 - calendarD.length];
         back8 -= animationE.length / (Math.max(1, changeI.length));
      }
       let soundx = String.fromCharCode(119,95,57,48,95,114,101,99,104,117,110,107,0);
       let trash3 = String.fromCharCode(122,95,50,50,95,111,118,101,114,114,105,100,100,101,110,0);
         handler5 = `${overA.length << (Math.min(sourcel.length, 4))}`;
         soundx = "2";
         handler5 += `${trash3.length + 1}`;
      let albumK = soundx.length >= 8537228;
      do {
          let description_l0n = 5.0;
         soundx += "2";
         description_l0n += 2;
         if (albumK) {
            break;
         }
      } while ((trash3.length < 2) && albumK);
       let baiduw = 1.0;
       let vignette_ = 5.0;
         sourcel += `${(String.fromCharCode(52,0) == soundx ? soundx.length : trash3.length)}`;
      commonv += "3";
   if (2 > (tooltips7.size ^ launchv.length) && 2 > (2 ^ tooltips7.size)) {
       let placementR = true;
       let reducer2 = String.fromCharCode(115,95,53,57,95,114,101,103,105,115,116,114,97,116,105,111,110,0);
          let favicon_ = 0;
          let plashV = 3.0;
          let plusT = false;
         reducer2 = `${(1 | (plusT ? 2 : 2))}`;
         favicon_ *= 2;
         plashV /= Math.max(3, parseFloat(`${favicon_}`));
         plusT = parseFloat(`${favicon_}`) == plashV;
      let playU = placementR ? !placementR : placementR;
      do {
         placementR = reducer2.length < 64 && placementR;
         if (playU) {
            break;
         }
      } while ((placementR) && playU);
         reducer2 = `${(reducer2 == String.fromCharCode(82,0) ? reducer2.length : (placementR ? 3 : 1))}`;
      while (reducer2.startsWith(`${placementR}`)) {
         reducer2 = `${((placementR ? 2 : 5) | 3)}`;
         break;
      }
         reducer2 = `${((placementR ? 2 : 4) | 3)}`;
         placementR = reducer2.startsWith(`${placementR}`);
      launchv = [2];
   }
      commonv = `${((appley ? 3 : 4) % (Math.max(5, parseInt(`${descC}`))))}`;

    bottomTabHeight = 0;
  }

  const displayHeight = windowHeight - bottomTabHeight;

  const insets = useSafeAreaInsets();
  const { spacing, colors } = useTheme();

  
  //   ({ screenReducer }: PSmall) => screenReducer
  

  return (
    <>
      {scrollView ? (
        <ScrollView
          style={{
            backgroundColor: isBgHide ? 'inherit' : colors.background,
            paddingTop: insets.top,
            paddingBottom: insets.bottom,
            paddingLeft: insets.left,
            paddingRight: insets.right,
          }}
          stickyHeaderIndices={[0]}
          contentContainerStyle={{ paddingBottom: 30 }}
        >
          <StatusBar backgroundColor={isBgHide ? 'inherit' : 'black'}  barStyle="light-content" />
          {header}
          <View
            style={{
              ...styles.innerContainer,
              paddingLeft: spacing.sideOffset,
              paddingRight: spacing.sideOffset,
              ...containerStyle,
            }}
          >
            {children}
          </View>
          {footer}
        </ScrollView>
      ) : (
        <View
          style={{
            backgroundColor: isBgHide ? 'inherit' : colors.background,
            ...styles.viewContainer,
            paddingTop: isPlay ? 0 : insets.top,
            paddingBottom: !isHome || isPlay ? 0 : insets.bottom,
            
            paddingLeft: isPlay ? 0 : insets.left,
            paddingRight: isPlay ? 0 : insets.right,
            
          }}
        >
          <StatusBar backgroundColor={isBgHide ? 'inherit' : 'black' } barStyle="light-content" />
          <View
            style={{
              ...styles.innerContainer,
              paddingLeft: spacing.sideOffset,
              paddingRight: spacing.sideOffset,
              ...containerStyle,
            }}
          >
            {children}
          </View>
          {footer}
        </View>
      )}
    </>
  );
}

const styles = StyleSheet.create({
  viewContainer: {
    flex: 1,
  },
  innerContainer: {
    flex: 1,
  },
});
