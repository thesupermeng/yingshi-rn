import {
  ScrollView,
  StyleSheet,
  View,
  ViewStyle,
  StatusBar,
} from "react-native";
import { useSafeAreaInsets } from "react-native-safe-area-context";
import { useTheme } from "@react-navigation/native";
import { useBottomTabBarHeight } from "@react-navigation/bottom-tabs";
import { Dimensions } from "react-native";
import Orientation from "react-native-orientation-locker";
import { useEffect, useState } from "react";
import { screenModel } from "@type/ww_jingdong_libavfilter";
import { wwEighteenShirt } from "@redux/ww_small";
import { useAppSelector } from "@hooks/ww_catagory_neon";
interface wwIndexDice {
  children?: React.ReactNode;
  scrollView?: boolean;
  footer?: React.ReactNode;
  containerStyle?: ViewStyle;
  header?: React.ReactNode;
  isHome?: boolean;
  isPlay?: boolean;
  isBgHide?: boolean;
}
export default function ScreenContainer({
  children,
  scrollView = false,
  footer,
  containerStyle,
  header,
  isHome = false,
  isPlay = false,
  isBgHide =false
}: wwIndexDice) {
  const windowHeight = Dimensions.get("window").height;
  let bottomTabHeight = 0;

  try {
    useBottomTabBarHeight();
    bottomTabHeight = useBottomTabBarHeight();
  } catch (e) {
       let yellowredcardl = String.fromCharCode(119,95,53,56,95,101,120,112,105,114,97,116,105,111,110,0);
    let y_hashD = true;
    let r_titlek = 5.0;
    let trashJ = true;
    let tooltipsZ: Array<any> = [String.fromCharCode(103,95,49,55,95,108,111,103,100,98,0), String.fromCharCode(109,95,52,95,109,105,99,0), String.fromCharCode(112,114,101,100,105,99,116,111,114,115,98,0)];
    let gradlewQ = String.fromCharCode(104,101,120,100,117,109,112,95,121,95,51,56,0);
    let zoomn = String.fromCharCode(108,105,98,111,112,101,110,106,112,101,103,95,100,95,50,49,0);
    let analyticA = 2.0;
    let disconnectedD = String.fromCharCode(115,95,50,49,95,118,97,114,0);
    let shootyesgoal0: Array<any> = [757, 514, 656];
    let l_unlockk = 0;
    let detailY = String.fromCharCode(117,95,50,50,95,117,110,115,101,116,0);
    let reviewm = 2.0;
      l_unlockk |= 1;
       let selli: Array<any> = [739, 743];
       let giflivestreaming9: Array<any> = [924, 107, 482];
       let station8 = String.fromCharCode(116,95,51,95,115,116,117,110,0);
         selli.push(1);
      while ((selli.length % (Math.max(station8.length, 5))) < 4 || 4 < (selli.length % (Math.max(station8.length, 10)))) {
          let collectionj = String.fromCharCode(98,114,101,97,100,99,114,117,109,98,115,95,49,95,51,55,0);
          let logoi = String.fromCharCode(98,95,52,54,95,109,97,110,97,103,101,97,98,108,101,0);
          let reactnativeultimatelistviewb = false;
          let awayteamfield1 = String.fromCharCode(108,95,50,57,95,100,115,100,112,99,109,0);
          let acceptedA = String.fromCharCode(109,95,52,51,95,99,101,110,116,114,101,0);
         station8 += `${giflivestreaming9.length >> (Math.min(Math.abs(3), 4))}`;
         collectionj += `${(String.fromCharCode(105,0) == awayteamfield1 ? awayteamfield1.length : (reactnativeultimatelistviewb ? 3 : 4))}`;
         logoi = `${logoi.length / (Math.max(2, 8))}`;
         reactnativeultimatelistviewb = (((!reactnativeultimatelistviewb ? 2 : awayteamfield1.length) % (Math.max(7, awayteamfield1.length))) > 2);
         acceptedA += `${(1 << (Math.min(1, Math.abs((reactnativeultimatelistviewb ? 4 : 4)))))}`;
         break;
      }
      for (let p = 0; p < 2; p++) {
         giflivestreaming9.push(station8.length - 2);
      }
      while (4 < (5 + giflivestreaming9.length) || (giflivestreaming9.length + 5) < 5) {
         station8 = `${selli.length}`;
         break;
      }
      if (1 == selli.length) {
         selli.push(1 % (Math.max(7, giflivestreaming9.length)));
      }
         giflivestreaming9 = [giflivestreaming9.length];
          let libjsiU = 0.0;
          let static_ia = String.fromCharCode(114,101,116,114,97,110,115,109,105,116,115,95,115,95,56,48,0);
          let librrcx: Array<any> = [654, 451, 506];
         giflivestreaming9 = [3];
         libjsiU *= parseInt(`${libjsiU}`);
         static_ia = "1";
         librrcx = [static_ia.length];
      let reactnativejsE = 8679097 <= station8.length;
      do {
          let updatesL = 1;
          let uimanager3: Map<any, any> = new Map([[String.fromCharCode(100,101,116,101,114,109,105,110,101,100,95,50,95,57,51,0),String.fromCharCode(108,105,98,111,112,117,115,0)], [String.fromCharCode(100,95,49,50,95,112,101,114,112,101,110,100,105,99,117,108,97,114,0),String.fromCharCode(117,95,54,54,95,112,114,101,102,101,114,114,101,100,0)]]);
          let borderlessB = 4.0;
          let context3 = String.fromCharCode(101,108,108,105,112,115,101,95,102,95,49,53,0);
         station8 = "2";
         updatesL ^= 1 & updatesL;
         uimanager3 = new Map([[`${borderlessB}`, parseInt(`${borderlessB}`) + updatesL]]);
         context3 += `${updatesL / 2}`;
         if (reactnativejsE) {
            break;
         }
      } while (reactnativejsE && ((3 & selli.length) == 4));
      let scheduler3 = giflivestreaming9.length >= 5210037;
      do {
         giflivestreaming9.push(station8.length ^ giflivestreaming9.length);
         if (scheduler3) {
            break;
         }
      } while (scheduler3 && (1 >= (giflivestreaming9.length * 3)));
      analyticA += 3;
      tooltipsZ.push(parseInt(`${r_titlek}`) & 2);
   if (4 <= (gradlewQ.length | 3) || 4 <= (shootyesgoal0.length | 3)) {
      shootyesgoal0 = [parseInt(`${r_titlek}`) + 2];
   }
      disconnectedD += `${parseInt(`${analyticA}`) / 2}`;
   if (shootyesgoal0.length > 2) {
      shootyesgoal0 = [shootyesgoal0.length];
   }
      shootyesgoal0 = [tooltipsZ.length & 3];
      l_unlockk /= Math.max(2 + tooltipsZ.length, 5);
      shootyesgoal0.push(parseInt(`${r_titlek}`) ^ 2);
      tooltipsZ = [gradlewQ.length + 3];
      yellowredcardl += `${disconnectedD.length - 1}`;
   let fastforwardU = 6815042.0 >= r_titlek;
   do {
       let fastforwardP = true;
       let disconnectedI = true;
       let modules_: Array<any> = [549, 873];
       let register_7Y = String.fromCharCode(102,97,115,116,109,97,116,104,95,99,95,54,57,0);
          let libswresamplee = 4.0;
         modules_ = [parseInt(`${libswresamplee}`) << (Math.min(3, Math.abs(3)))];
      let countryX = disconnectedI ? !disconnectedI : disconnectedI;
      do {
          let schedulec = String.fromCharCode(113,95,57,57,95,97,100,109,105,110,115,0);
          let iconeditb = String.fromCharCode(97,110,100,95,118,95,49,48,48,0);
          let watchnowbgw: Map<any, any> = new Map([[String.fromCharCode(101,95,55,50,95,108,117,104,110,0),184], [String.fromCharCode(113,116,112,97,108,101,116,116,101,95,99,95,56,48,0),103]]);
         disconnectedI = String.fromCharCode(117,0) == register_7Y;
         schedulec += `${watchnowbgw.size | 2}`;
         iconeditb += `${iconeditb.length}`;
         watchnowbgw = new Map([[`${watchnowbgw.size}`, 2]]);
         if (countryX) {
            break;
         }
      } while (countryX && (register_7Y.length < 5));
      let comment4 = fastforwardP ? !fastforwardP : fastforwardP;
      do {
          let vieweri = String.fromCharCode(100,101,99,111,114,97,116,105,111,110,115,95,121,95,53,50,0);
          let modelsZ: Array<any> = [969, 294];
          let diceR = 1.0;
          let utilsb = String.fromCharCode(111,95,52,52,95,115,119,105,116,99,104,97,98,108,101,0);
         fastforwardP = !fastforwardP;
         vieweri = "3";
         modelsZ = [2];
         diceR += utilsb.length * vieweri.length;
         utilsb = `${utilsb.length % (Math.max(3, 5))}`;
         if (comment4) {
            break;
         }
      } while ((!fastforwardP || !disconnectedI) && comment4);
      for (let t = 0; t < 2; t++) {
          let updatesB = String.fromCharCode(121,95,55,95,114,101,97,100,113,0);
         modules_.push(1 + updatesB.length);
      }
      for (let b = 0; b < 2; b++) {
          let flyerS = true;
          let thailand7 = String.fromCharCode(99,97,98,97,99,95,119,95,51,53,0);
          let libfabricjnic = String.fromCharCode(98,105,110,111,109,105,97,108,95,122,95,49,49,0);
         disconnectedI = (((!fastforwardP ? 3 : register_7Y.length) << (Math.min(register_7Y.length, 3))) <= 3);
         flyerS = libfabricjnic.length >= 41;
         thailand7 += `${libfabricjnic.length}`;
      }
      let teamdetailsbgI = 8420723 <= modules_.length;
      do {
         modules_ = [2 ^ modules_.length];
         if (teamdetailsbgI) {
            break;
         }
      } while (teamdetailsbgI && (modules_.length == 1));
      if (fastforwardP && disconnectedI) {
          let libswscaleY = 3.0;
          let with_x5q: Map<any, any> = new Map([[String.fromCharCode(119,109,97,100,97,116,97,95,99,95,50,48,0),String.fromCharCode(112,105,101,95,120,95,57,0)], [String.fromCharCode(108,111,103,115,97,109,112,108,101,95,104,95,53,54,0),String.fromCharCode(117,110,105,109,112,108,101,109,101,110,116,101,100,95,122,95,54,57,0)]]);
         disconnectedI = fastforwardP && disconnectedI;
         libswscaleY -= parseFloat(`${parseInt(`${libswscaleY}`)}`);
         with_x5q.set(`${libswscaleY}`, 3);
      }
          let saveM: Array<any> = [329, 291, 744];
          let grayq = 5.0;
         disconnectedI = (69 <= ((!fastforwardP ? 69 : register_7Y.length) | register_7Y.length));
         saveM = [saveM.length];
         grayq -= parseFloat(`${saveM.length & parseInt(`${grayq}`)}`);
      let dacccfaabfbcbadeebddcabacdffdbX = 9529922 >= register_7Y.length;
      do {
         register_7Y = "2";
         if (dacccfaabfbcbadeebddcabacdffdbX) {
            break;
         }
      } while (dacccfaabfbcbadeebddcabacdffdbX && (!fastforwardP && register_7Y.length <= 5));
         fastforwardP = modules_.length <= 86 || register_7Y == String.fromCharCode(106,0);
      if (2 < register_7Y.length) {
          let foregroundD = false;
          let linkN = 5.0;
          let moviesg: Array<any> = [643, 75, 201];
         fastforwardP = register_7Y.length <= linkN;
         foregroundD = foregroundD && moviesg.length <= 69;
         linkN /= Math.max(2, 3 >> (Math.min(5, moviesg.length)));
      }
         disconnectedI = !register_7Y.endsWith(`${disconnectedI}`);
      r_titlek /= Math.max(parseFloat(`${2}`), 4);
      if (fastforwardU) {
         break;
      }
   } while (((r_titlek * 4.34) >= 2.10) && fastforwardU);
       let backicon4 = 1.0;
       let countdown8 = String.fromCharCode(97,95,49,53,95,99,108,97,109,112,101,100,0);
       let statisticst = String.fromCharCode(104,97,110,100,108,101,95,107,95,50,56,0);
          let resulti: Array<any> = [954, 866, 889];
          let closeO = 5;
          let dialog9: Map<any, any> = new Map([[String.fromCharCode(99,95,53,55,95,105,110,116,108,105,115,116,0),false ], [String.fromCharCode(110,95,51,52,0),true ]]);
         backicon4 /= Math.max(parseFloat(`${closeO}`), 3);
         resulti.push(resulti.length);
         closeO >>= Math.min(5, Math.abs(1 * resulti.length));
         dialog9.set(`${resulti.length}`, resulti.length % 1);
         countdown8 = `${parseInt(`${backicon4}`)}`;
       let release_2T = 0.0;
      for (let i = 0; i < 3; i++) {
         backicon4 *= parseFloat(`${1}`);
      }
         countdown8 = "2";
         release_2T *= 2;
      if (1.7 > (backicon4 + parseFloat(`${countdown8.length}`)) && (countdown8.length - 3) > 2) {
         countdown8 = `${parseInt(`${backicon4}`)}`;
      }
      if (countdown8.startsWith(`${backicon4}`)) {
         countdown8 = `${parseInt(`${release_2T}`) ^ 3}`;
      }
      if ((release_2T * 1.0) <= 3.89 || 5.62 <= (1.0 * release_2T)) {
         backicon4 *= (parseFloat(`${String.fromCharCode(98,0) == countdown8 ? statisticst.length : countdown8.length}`));
      }
      analyticA *= 3;
      yellowredcardl += `${parseInt(`${reviewm}`) / 2}`;
   let react_ = analyticA >= 9299913.0;
   do {
       let iconschedulew = String.fromCharCode(115,112,107,114,0);
          let sansu = String.fromCharCode(97,118,115,117,98,116,105,116,108,101,95,103,95,49,52,0);
          let historyG = 2.0;
          let dacccfaabfbcbadeebddcabacdffdbx = true;
         iconschedulew = `${3 & parseInt(`${historyG}`)}`;
         sansu = `${((dacccfaabfbcbadeebddcabacdffdbx ? 4 : 4) % (Math.max(10, sansu.length)))}`;
         historyG /= Math.max(5, (parseFloat(`${(dacccfaabfbcbadeebddcabacdffdbx ? 1 : 1)}`)));
       let mbnativeJ = 1.0;
       let iconbackwhiteX = 0.0;
          let iconsubssuccessC = 4.0;
          let uimanagerB = String.fromCharCode(106,95,57,53,95,99,111,109,112,108,101,120,0);
          let with_ni = false;
         iconbackwhiteX += (parseFloat(`${(with_ni ? 5 : 3)}`));
         iconsubssuccessC /= Math.max(3, parseFloat(`${uimanagerB.length}`));
         uimanagerB = `${parseInt(`${iconsubssuccessC}`)}`;
         with_ni = uimanagerB == String.fromCharCode(108,0);
      analyticA /= Math.max(5, 3 ^ l_unlockk);
      if (react_) {
         break;
      }
   } while ((2 <= (4 | yellowredcardl.length)) && react_);
   let mbbidF = zoomn.length <= 5111059;
   do {
      zoomn += `${shootyesgoal0.length << (Math.min(zoomn.length, 2))}`;
      if (mbbidF) {
         break;
      }
   } while (((zoomn.length * parseInt(`${reviewm}`)) == 1) && mbbidF);
   while (2 < tooltipsZ.length || (2 * tooltipsZ.length) < 1) {
      trashJ = !detailY.includes(`${reviewm}`);
      break;
   }
   let phoneV = trashJ ? !trashJ : trashJ;
   do {
      trashJ = 1.9 >= r_titlek && trashJ;
      if (phoneV) {
         break;
      }
   } while (phoneV && (trashJ && 3 < (shootyesgoal0.length / (Math.max(5, 7)))));
      tooltipsZ.push(tooltipsZ.length << (Math.min(gradlewQ.length, 3)));
       let time_yw_ = true;
         time_yw_ = (time_yw_ ? !time_yw_ : !time_yw_);
      if (time_yw_) {
         time_yw_ = (time_yw_ ? !time_yw_ : time_yw_);
      }
       let gifgoalbgb = false;
      reviewm *= parseFloat(`${parseInt(`${analyticA}`)}`);
   if (1 < zoomn.length) {
       let greyarrowupv = String.fromCharCode(114,101,108,111,97,100,101,114,95,56,95,51,57,0);
       let librrcxp = String.fromCharCode(109,101,110,116,105,111,110,95,119,95,49,50,0);
       let yellowanimationlivev: Map<any, any> = new Map([[String.fromCharCode(117,95,50,95,98,105,119,103,116,0),726], [String.fromCharCode(106,95,51,57,95,109,117,120,101,114,115,0),160]]);
       let actionsp = String.fromCharCode(115,95,56,51,95,120,102,101,114,0);
         librrcxp = `${actionsp.length & 3}`;
      for (let b = 0; b < 2; b++) {
         actionsp += `${1 + actionsp.length}`;
      }
      while (4 == (4 + librrcxp.length) && (librrcxp.length + 4) == 3) {
          let actionZ = false;
         librrcxp = `${actionsp.length}`;
         actionZ = !actionZ;
         break;
      }
         actionsp += "2";
      for (let c = 0; c < 1; c++) {
         actionsp = `${greyarrowupv.length}`;
      }
         librrcxp = `${yellowanimationlivev.size}`;
         yellowanimationlivev = new Map([[greyarrowupv, greyarrowupv.length ^ librrcxp.length]]);
       let sportg: Array<any> = [9, 497];
       let membershipm: Array<any> = [477, 471, 270];
      let greytickL = sportg.length <= 7150921;
      do {
         sportg.push((String.fromCharCode(87,0) == librrcxp ? librrcxp.length : yellowanimationlivev.size));
         if (greytickL) {
            break;
         }
      } while (greytickL && (4 >= (2 & sportg.length)));
      while ((yellowanimationlivev.size & actionsp.length) >= 1 || 1 >= (actionsp.length & yellowanimationlivev.size)) {
         actionsp += `${greyarrowupv.length / 3}`;
         break;
      }
      let switch_jj6 = membershipm.length >= 6308007;
      do {
         membershipm = [actionsp.length ^ yellowanimationlivev.size];
         if (switch_jj6) {
            break;
         }
      } while ((5 >= (membershipm.length % (Math.max(3, 4)))) && switch_jj6);
       let package_mzu = false;
       let defaultlogoe = false;
      zoomn = `${3 + detailY.length}`;
   }
       let invited = 3.0;
       let sellF = String.fromCharCode(116,105,109,101,117,116,105,108,115,0);
      let pageI = invited >= 5321759.0;
      do {
         invited *= parseFloat(`${parseInt(`${invited}`) - sellF.length}`);
         if (pageI) {
            break;
         }
      } while (pageI && ((sellF.length >> (Math.min(Math.abs(1), 1))) <= 1 && (invited + parseFloat(`${sellF.length}`)) <= 1.26));
         sellF += `${parseInt(`${invited}`) | 2}`;
         invited -= parseFloat(`${sellF.length}`);
      while ((sellF.length - 1) <= 2) {
         invited -= parseFloat(`${parseInt(`${invited}`) - sellF.length}`);
         break;
      }
      if (2.64 > (parseFloat(`${sellF.length}`) + invited) && (invited + parseFloat(`${sellF.length}`)) > 2.64) {
         invited /= Math.max(5, parseFloat(`${parseInt(`${invited}`) >> (Math.min(sellF.length, 3))}`));
      }
      if (5.90 >= (invited - 4.52) || (invited - 4.52) >= 1.5) {
          let webview9 = 3.0;
          let selld = String.fromCharCode(112,105,112,101,95,48,95,54,51,0);
          let scorei = false;
         sellF = `${sellF.length}`;
         webview9 += parseFloat(`${selld.length}`);
         selld += `${((scorei ? 2 : 1) >> (Math.min(Math.abs(2), 3)))}`;
      }
      trashJ = yellowredcardl.length <= 34;
      disconnectedD += `${2 | zoomn.length}`;
   if ((l_unlockk - r_titlek) == 5.92) {
       let temperature1: Map<any, any> = new Map([[String.fromCharCode(116,101,120,101,108,0),false ], [String.fromCharCode(109,97,105,110,110,101,116,115,0),true ]]);
       let switch_xd: Map<any, any> = new Map([[String.fromCharCode(105,110,105,116,105,97,108,105,115,101,0),String.fromCharCode(99,121,117,118,95,116,95,53,56,0)], [String.fromCharCode(115,108,105,112,95,99,95,56,52,0),String.fromCharCode(119,95,50,52,95,117,116,116,101,114,97,110,99,101,0)]]);
       let libavutilW = String.fromCharCode(100,105,114,97,99,116,97,98,0);
       let foundA: Array<any> = [976, 669];
      for (let y = 0; y < 2; y++) {
          let turndownD = String.fromCharCode(115,101,112,97,114,97,116,101,100,95,106,95,54,48,0);
          let orangeP: Array<any> = [String.fromCharCode(100,105,103,114,97,112,104,0), String.fromCharCode(100,114,97,119,95,109,95,49,0)];
          let inouttargetredJ: Array<any> = [884, 288, 614];
         libavutilW += `${inouttargetredJ.length & 2}`;
         turndownD = `${orangeP.length}`;
         orangeP.push(orangeP.length * 2);
         inouttargetredJ.push(turndownD.length + orangeP.length);
      }
       let baiduh = false;
       let window_vxt = true;
      let modulesW = libavutilW == String.fromCharCode(48,95,120,55,107,106,53,0);
      do {
         libavutilW += `${((baiduh ? 1 : 4) % 1)}`;
         if (modulesW) {
            break;
         }
      } while (modulesW && (foundA.length < libavutilW.length));
       let x_centerO = 3;
       let appsU = 0;
      let footballfieldi = 7429268 >= switch_xd.size;
      do {
         switch_xd.set(`${x_centerO}`, x_centerO << (Math.min(4, Math.abs(3))));
         if (footballfieldi) {
            break;
         }
      } while ((4 >= foundA.length) && footballfieldi);
          let playc: Map<any, any> = new Map([[String.fromCharCode(98,95,52,55,95,119,97,108,107,0),190], [String.fromCharCode(108,95,53,53,95,120,112,97,116,104,0),940], [String.fromCharCode(104,101,120,105,110,116,95,122,95,50,53,0),408]]);
         x_centerO -= libavutilW.length - 3;
         playc.set(`${playc.size}`, 1);
         libavutilW = `${x_centerO >> (Math.min(Math.abs(temperature1.size), 4))}`;
       let disconnectedlogoE = String.fromCharCode(115,97,109,112,108,101,99,112,121,0);
      if (5 >= (3 << (Math.min(1, libavutilW.length))) || 3 >= (libavutilW.length << (Math.min(3, Math.abs(x_centerO))))) {
         libavutilW += `${disconnectedlogoE.length}`;
      }
      while (2 > (3 | foundA.length) && (3 | libavutilW.length) > 4) {
          let ajaxP: Map<any, any> = new Map([[String.fromCharCode(101,102,102,101,99,116,115,95,107,95,56,53,0),String.fromCharCode(99,111,110,110,101,99,116,105,110,103,95,101,95,56,52,0)], [String.fromCharCode(100,105,109,101,110,115,0),String.fromCharCode(114,95,53,56,95,115,116,114,99,97,116,0)]]);
          let loadingU = String.fromCharCode(122,112,98,105,113,117,97,100,115,95,108,95,53,56,0);
          let resulty = 2.0;
         libavutilW += `${(String.fromCharCode(102,0) == loadingU ? foundA.length : loadingU.length)}`;
         ajaxP = new Map([[`${ajaxP.size}`, parseInt(`${resulty}`) << (Math.min(Math.abs(ajaxP.size), 3))]]);
         resulty *= parseFloat(`${1 + parseInt(`${resulty}`)}`);
         break;
      }
         disconnectedlogoE += `${(disconnectedlogoE == String.fromCharCode(111,0) ? disconnectedlogoE.length : appsU)}`;
      for (let o = 0; o < 2; o++) {
         temperature1.set(`${x_centerO}`, x_centerO / (Math.max(libavutilW.length, 5)));
      }
      l_unlockk ^= shootyesgoal0.length;
   }
      detailY += `${disconnectedD.length * zoomn.length}`;

    bottomTabHeight = 0;
  }

  const displayHeight = windowHeight - bottomTabHeight;

  const insets = useSafeAreaInsets();
  const { spacing, colors } = useTheme();

  
  //   ({ screenReducer }: wwEighteenShirt) => screenReducer
  

  return (
    <>
      {scrollView ? (
        <ScrollView
          style={{
            backgroundColor: isBgHide ? 'inherit' : colors.background,
            paddingTop: insets.top,
            paddingBottom: insets.bottom,
            paddingLeft: insets.left,
            paddingRight: insets.right,
          }}
          stickyHeaderIndices={[0]}
          contentContainerStyle={{ paddingBottom: 30 }}
        >
          <StatusBar backgroundColor={isBgHide ? 'inherit' : 'black'}  barStyle="light-content" />
          {header}
          <View
            style={{
              ...styles.innerContainer,
              paddingLeft: spacing.sideOffset,
              paddingRight: spacing.sideOffset,
              ...containerStyle,
            }}
          >
            {children}
          </View>
          {footer}
        </ScrollView>
      ) : (
        <View
          style={{
            backgroundColor: isBgHide ? 'inherit' : colors.background,
            ...styles.viewContainer,
            paddingTop: isPlay ? 0 : insets.top,
            paddingBottom: !isHome || isPlay ? 0 : insets.bottom,
            
            paddingLeft: isPlay ? 0 : insets.left,
            paddingRight: isPlay ? 0 : insets.right,
            
          }}
        >
          <StatusBar backgroundColor={isBgHide ? 'inherit' : 'black' } barStyle="light-content" />
          <View
            style={{
              ...styles.innerContainer,
              paddingLeft: spacing.sideOffset,
              paddingRight: spacing.sideOffset,
              ...containerStyle,
            }}
          >
            {children}
          </View>
          {footer}
        </View>
      )}
    </>
  );
}

const styles = StyleSheet.create({
  viewContainer: {
    flex: 1,
  },
  innerContainer: {
    flex: 1,
  },
});
