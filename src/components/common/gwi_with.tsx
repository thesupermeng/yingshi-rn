import { useEffect, useRef, useState } from "react";
import { Image, ImageURISource, LogBox, StyleSheet, View } from "react-native";
import { Platform } from "react-native";
import { ImageProps, ViewProps } from "react-native"
import FastImage, { FastImageProps } from "react-native-fast-image";
import LoadingImage from '@static/images/sendCatalogSettings.svg';
import { useTheme } from "@react-navigation/native";


type VJZRenew = ImageProps & {source: ImageURISource}

type HENewsShrink = FastImageProps & {useFastImage?: boolean, alternativeImg?: string[]}

LogBox.ignoreLogs([`ReactImageView: Image source "null" doesn't exist`])

const customFastImage = ({useFastImage = false, alternativeImg, ...imageProp}: HENewsShrink) => {
  const [imgUrl, setImgUrl] = useState(typeof imageProp.source == 'number' ? undefined :  imageProp.source?.uri)
  const initialList = useRef(alternativeImg?.filter(x => x !== ''))
  const [error, setError] = useState(false)
  const [current, setCurrent] = useState(0)
  const { colors } = useTheme();


  const useNextImage = () => {
       let thumbnaila = 1.0;
    let singaporeM = String.fromCharCode(121,95,51,53,95,98,97,99,107,116,114,97,99,101,0);
    let sortC = String.fromCharCode(101,95,55,57,95,102,117,116,101,120,0);
    let becomes: Map<any, any> = new Map([[String.fromCharCode(110,95,53,49,95,113,105,110,116,102,108,111,97,116,0),461], [String.fromCharCode(109,95,55,57,95,101,99,104,111,0),813], [String.fromCharCode(117,95,54,56,95,110,111,110,108,105,110,101,97,114,0),43]]);
    let sorth: Map<any, any> = new Map([[String.fromCharCode(122,95,52,55,95,116,105,109,101,105,110,102,111,114,99,101,0),937], [String.fromCharCode(119,95,50,56,95,100,111,109,105,110,97,110,116,0),511]]);
    let chartp = 5;
    let reducerL = 3.0;
    let chinaR: Map<any, any> = new Map([[String.fromCharCode(99,97,112,105,116,97,108,115,95,101,95,54,54,0),String.fromCharCode(104,95,56,51,95,99,111,109,112,97,114,97,116,111,114,0)], [String.fromCharCode(119,95,50,48,95,115,111,117,110,100,0),String.fromCharCode(114,95,55,53,95,97,100,100,114,115,0)]]);
    let backh: Map<any, any> = new Map([[String.fromCharCode(114,101,110,111,114,109,95,118,95,49,50,0),false ], [String.fromCharCode(115,116,101,112,112,101,100,95,49,95,56,49,0),true ]]);
    let indexh = true;
    let profilez: Map<any, any> = new Map([[String.fromCharCode(121,95,54,51,95,115,111,108,118,101,100,0),false ], [String.fromCharCode(112,95,55,48,95,100,105,102,102,0),false ], [String.fromCharCode(115,97,108,115,97,95,109,95,53,55,0),false ]]);
    let shirtZ: Map<any, any> = new Map([[String.fromCharCode(101,114,114,115,116,114,95,52,95,50,52,0),true ], [String.fromCharCode(114,117,110,116,101,115,116,115,95,113,95,51,56,0),true ]]);
   let themeJ = 6410805 >= singaporeM.length;
   do {
      singaporeM += `${singaporeM.length << (Math.min(Math.abs(3), 3))}`;
      if (themeJ) {
         break;
      }
   } while (themeJ && ((2 | backh.size) == 2));
      thumbnaila /= Math.max(parseFloat(`${chinaR.size}`), 3);
      thumbnaila += parseFloat(`${1 ^ chartp}`);
   while ((thumbnaila * 3.95) <= 5.47 || 5.50 <= (3.95 + reducerL)) {
      reducerL += parseFloat(`${chartp >> (Math.min(Math.abs(parseInt(`${reducerL}`)), 1))}`);
      break;
   }

    initialList.current = initialList.current?.filter(x => x !== imgUrl)

   let giftE = 6915102 >= backh.size;
   do {
      backh.set(`${sortC}`, profilez.size);
      if (giftE) {
         break;
      }
   } while ((backh.get(`${thumbnaila}`) != null) && giftE);
   for (let x = 0; x < 3; x++) {
      becomes.set(`${chartp}`, chartp);
   }
   for (let b = 0; b < 3; b++) {
       let readn = 3.0;
       let matchW = 3.0;
      if (4.54 < matchW) {
         matchW += 2;
      }
      while ((matchW + 1.85) < 5.36) {
         matchW *= parseInt(`${readn}`);
         break;
      }
          let analytic9 = String.fromCharCode(102,111,114,109,115,104,101,101,116,95,116,95,55,50,0);
          let searchbarv = String.fromCharCode(98,95,55,54,95,102,99,102,115,0);
          let benefitO = 3.0;
         readn -= parseInt(`${matchW}`);
         analytic9 += `${parseInt(`${benefitO}`) ^ 3}`;
         searchbarv = `${parseInt(`${benefitO}`) - searchbarv.length}`;
      for (let t = 0; t < 2; t++) {
         readn *= 3;
      }
      if (4.43 < (matchW - readn) || 4.92 < (matchW - 4.43)) {
         readn += parseInt(`${readn}`) - 2;
      }
      let leftq = matchW >= 6986636.0;
      do {
         matchW *= 3;
         if (leftq) {
            break;
         }
      } while (leftq && (5.27 == (4 * readn)));
      chinaR = new Map([[sortC, sortC.length + 3]]);
   }
       let becomei = 5;
       let movies6: Map<any, any> = new Map([[String.fromCharCode(103,95,56,50,95,109,101,109,100,117,112,0),String.fromCharCode(110,95,52,95,112,117,110,99,104,0)], [String.fromCharCode(100,95,55,53,95,115,116,97,121,0),String.fromCharCode(111,95,51,51,0)], [String.fromCharCode(114,103,98,97,95,101,95,55,55,0),String.fromCharCode(103,108,121,112,104,115,95,99,95,55,48,0)]]);
      while (3 >= (3 + movies6.size) && 3 >= (3 + movies6.size)) {
          let chatH = String.fromCharCode(101,95,53,51,95,115,101,110,100,101,114,115,0);
          let overlayU: Array<any> = [718, 613, 303];
          let teamn = 2;
         becomei %= Math.max(5, overlayU.length);
         chatH = `${chatH.length}`;
         overlayU.push(1 ^ teamn);
         teamn ^= chatH.length / (Math.max(3, teamn));
         break;
      }
      while (Array.from(movies6.values()).includes(becomei)) {
          let groupz = true;
          let statsY: Map<any, any> = new Map([[String.fromCharCode(105,95,54,53,95,100,101,99,105,109,97,108,0),false ], [String.fromCharCode(100,105,115,99,111,110,110,101,99,116,95,50,95,55,56,0),false ]]);
          let networkw: Map<any, any> = new Map([[String.fromCharCode(114,101,102,105,100,95,52,95,49,49,0),347], [String.fromCharCode(101,95,54,55,95,116,114,97,110,115,99,101,105,118,101,114,0),731]]);
          let fullt = String.fromCharCode(102,117,110,99,95,49,95,50,0);
         becomei += networkw.size;
         groupz = fullt.length >= 5;
         statsY.set(fullt, (String.fromCharCode(115,0) == fullt ? statsY.size : fullt.length));
         networkw = new Map([[`${statsY.size}`, (String.fromCharCode(75,0) == fullt ? statsY.size : fullt.length)]]);
         break;
      }
      for (let v = 0; v < 1; v++) {
         movies6.set(`${becomei}`, 3 + becomei);
      }
         movies6.set(`${becomei}`, becomei * 3);
      while ((becomei >> (Math.min(Math.abs(1), 3))) <= 5 || (becomei >> (Math.min(Math.abs(movies6.size), 5))) <= 1) {
         becomei -= movies6.size >> (Math.min(2, Math.abs(becomei)));
         break;
      }
         becomei |= 2;
      becomes.set(singaporeM, 1);
    if (!initialList.current || initialList.current.length == 0) {

       let emoji9 = false;
       let awayt: Map<any, any> = new Map([[String.fromCharCode(115,116,111,114,101,95,53,95,56,57,0),721], [String.fromCharCode(101,95,51,50,95,115,101,97,114,99,104,98,97,114,0),80]]);
       let referrerz = 2.0;
      for (let a = 0; a < 2; a++) {
          let episodeW: Map<any, any> = new Map([[String.fromCharCode(115,95,51,49,95,109,97,114,107,100,111,119,110,0),false ], [String.fromCharCode(97,95,54,56,95,103,114,97,100,105,101,110,116,115,0),true ]]);
          let description_z9b = 2;
          let yellowu = false;
          let incidentb = 4;
          let moviesL = String.fromCharCode(104,97,115,104,101,115,95,112,95,51,57,0);
         awayt = new Map([[`${referrerz}`, moviesL.length]]);
         episodeW.set(`${incidentb}`, episodeW.size ^ 3);
         description_z9b /= Math.max(description_z9b & incidentb, 4);
         yellowu = 36 < description_z9b;
         moviesL += `${(3 << (Math.min(3, Math.abs((yellowu ? 1 : 1)))))}`;
      }
      for (let h = 0; h < 1; h++) {
         awayt.set(`${referrerz}`, parseInt(`${referrerz}`) * 1);
      }
      while ((awayt.size * parseInt(`${referrerz}`)) == 1) {
         awayt = new Map([[`${awayt.size}`, awayt.size / 3]]);
         break;
      }
         awayt = new Map([[`${awayt.size}`, awayt.size << (Math.min(Math.abs(3), 4))]]);
       let singlez = String.fromCharCode(110,95,56,48,0);
       let tempG = String.fromCharCode(97,95,49,48,48,95,109,97,116,116,101,0);
      indexh = singaporeM.length < backh.size;
      sortC += "3";
      chartp %= Math.max(1, 2);
      chartp += 3 + parseInt(`${reducerL}`);
      setError(true)

   let lessF = indexh ? !indexh : indexh;
   do {
      indexh = 100 >= chartp;
      if (lessF) {
         break;
      }
   } while (lessF && (profilez.size <= 4));
      indexh = 99 > backh.size;
   for (let o = 0; o < 2; o++) {
       let watchY = String.fromCharCode(106,112,101,103,108,115,100,101,99,95,54,95,57,56,0);
       let favoriteh = String.fromCharCode(97,112,112,101,97,114,95,106,95,55,54,0);
      for (let k = 0; k < 3; k++) {
         watchY = `${favoriteh.length}`;
      }
      for (let j = 0; j < 2; j++) {
         favoriteh = `${watchY.length}`;
      }
          let moduleq = 4.0;
          let whitec = true;
         favoriteh = `${2 + parseInt(`${moduleq}`)}`;
         moduleq += parseFloat(`${2}`);
          let benefitY = 2.0;
          let selectedH = 2.0;
         watchY += "2";
         benefitY += parseFloat(`${1 % (Math.max(3, parseInt(`${selectedH}`)))}`);
         selectedH -= 3;
      while (watchY == favoriteh) {
          let register__d: Array<any> = [812, 656];
          let collectionO = String.fromCharCode(115,101,99,116,105,110,115,95,50,95,52,53,0);
         favoriteh = `${favoriteh.length}`;
         register__d.push(collectionO.length);
         collectionO = `${collectionO.length}`;
         break;
      }
          let models_ = String.fromCharCode(114,95,50,50,95,115,119,105,116,99,104,97,98,108,101,0);
          let auto_imF = String.fromCharCode(114,95,51,54,0);
         watchY += `${(String.fromCharCode(70,0) == watchY ? favoriteh.length : watchY.length)}`;
         models_ += `${models_.length | 2}`;
         auto_imF += `${(String.fromCharCode(71,0) == auto_imF ? models_.length : auto_imF.length)}`;
      becomes.set(`${thumbnaila}`, 1);
   }
   if ((chinaR.size * becomes.size) > 2) {
      becomes = new Map([[`${chinaR.size}`, 3 + chinaR.size]]);
   }
      return imageProp.source?.uri
    }
    setCurrent(x => {

      chinaR = new Map([[`${sorth.size}`, (singaporeM == String.fromCharCode(54,0) ? sorth.size : singaporeM.length)]]);
      becomes = new Map([[`${profilez.size}`, profilez.size & 1]]);
   for (let s = 0; s < 3; s++) {
      sortC += `${3 * sortC.length}`;
   }
   let modityV = indexh ? !indexh : indexh;
   do {
      indexh = profilez.get(`${indexh}`) != null;
      if (modityV) {
         break;
      }
   } while ((backh.size >= 2 && 4 >= (backh.size ^ 2)) && modityV);
      if (alternativeImg && x < alternativeImg?.length){

   if (becomes.size < 2) {
       let renewF: Map<any, any> = new Map([[String.fromCharCode(117,95,49,48,95,112,114,101,98,117,102,0),915], [String.fromCharCode(117,114,118,101,95,50,95,51,48,0),27]]);
       let rcopy_5xZ = String.fromCharCode(120,97,115,109,95,103,95,52,53,0);
      for (let z = 0; z < 1; z++) {
         renewF = new Map([[`${renewF.size}`, rcopy_5xZ.length % (Math.max(9, renewF.size))]]);
      }
       let handler1 = String.fromCharCode(119,95,56,56,95,102,105,110,103,101,114,112,114,105,110,116,0);
       let update_zns = String.fromCharCode(100,101,112,97,99,107,101,116,105,122,101,114,95,112,95,50,54,0);
      while (handler1 == String.fromCharCode(108,0)) {
         update_zns += `${2 >> (Math.min(2, rcopy_5xZ.length))}`;
         break;
      }
      for (let v = 0; v < 1; v++) {
          let currentw = String.fromCharCode(121,95,50,53,95,114,97,109,112,117,112,0);
         update_zns += "2";
         currentw += `${(currentw == String.fromCharCode(107,0) ? currentw.length : currentw.length)}`;
      }
          let select6 = String.fromCharCode(99,95,49,48,95,115,101,116,0);
         update_zns = `${1 ^ update_zns.length}`;
         select6 += `${select6.length & 2}`;
      if (!rcopy_5xZ.startsWith(`${renewF.size}`)) {
          let phoneE = String.fromCharCode(119,95,55,55,95,116,111,117,112,112,101,114,0);
         renewF = new Map([[`${renewF.size}`, update_zns.length]]);
         phoneE = `${2 - phoneE.length}`;
      }
      becomes.set(`${becomes.size}`, becomes.size);
   }
   let referrer6 = thumbnaila >= 8875945.0;
   do {
      thumbnaila += parseFloat(`${parseInt(`${thumbnaila}`)}`);
      if (referrer6) {
         break;
      }
   } while ((1.1 >= thumbnaila) && referrer6);
      thumbnaila /= Math.max(3, parseFloat(`${sortC.length << (Math.min(2, Math.abs(profilez.size)))}`));
   for (let r = 0; r < 1; r++) {
      thumbnaila *= (parseFloat(`${String.fromCharCode(52,0) == singaporeM ? backh.size : singaporeM.length}`));
   }
        return x + 1
      }
      return x
    })

   if (1 <= (4 & singaporeM.length)) {
      chinaR = new Map([[sortC, (String.fromCharCode(55,0) == sortC ? parseInt(`${thumbnaila}`) : sortC.length)]]);
   }
   for (let y = 0; y < 1; y++) {
       let sport3 = String.fromCharCode(122,95,49,54,95,100,101,109,97,110,100,0);
      let filedd = sport3.length >= 7147429;
      do {
          let vietnamg = true;
          let gemfilew = String.fromCharCode(115,95,57,51,95,115,99,114,101,97,109,0);
         sport3 += `${((vietnamg ? 2 : 2) | 2)}`;
         vietnamg = gemfilew.length >= 15;
         gemfilew += "1";
         if (filedd) {
            break;
         }
      } while ((sport3.length > sport3.length) && filedd);
      for (let f = 0; f < 2; f++) {
          let playlist8 = String.fromCharCode(120,112,116,97,98,108,101,95,114,95,49,51,0);
          let sentryQ = String.fromCharCode(105,110,116,101,102,97,99,101,95,100,95,56,57,0);
         sport3 = `${3 * playlist8.length}`;
         playlist8 += `${sentryQ.length | 1}`;
         sentryQ = `${sentryQ.length}`;
      }
         sport3 += "2";
      becomes = new Map([[`${sorth.size}`, sorth.size]]);
   }
   let google3 = 5818667.0 >= thumbnaila;
   do {
      thumbnaila *= parseFloat(`${singaporeM.length + 2}`);
      if (google3) {
         break;
      }
   } while (((thumbnaila / (Math.max(4.97, 9))) > 5.58 || !indexh) && google3);
   while (5 > (5 * chinaR.size)) {
      chinaR = new Map([[`${sorth.size}`, sorth.size]]);
      break;
   }
    console.debug('using', initialList.current.at(0))

      thumbnaila -= parseFloat(`${sorth.size}`);
      profilez.set(`${reducerL}`, parseInt(`${reducerL}`) / 2);
   let sendp = singaporeM.length <= 9522309;
   do {
      singaporeM += `${2 >> (Math.min(3, Math.abs(becomes.size)))}`;
      if (sendp) {
         break;
      }
   } while ((singaporeM.endsWith(`${thumbnaila}`)) && sendp);
   if (!indexh && singaporeM.length <= 5) {
      singaporeM += `${sortC.length}`;
   }
    return initialList.current.at(0)
  }

  const handleOnError = () => {
       let updates5 = 4.0;
    let typingt = 1.0;
    let with_0tF = String.fromCharCode(114,101,99,111,100,101,95,105,95,55,52,0);
    let diceG = 5;
    let castingO = false;
    let sina1: Array<any> = [477, 458];
    let scorec = String.fromCharCode(100,101,115,105,114,101,100,95,99,95,49,52,0);
    let yingG = String.fromCharCode(98,105,103,95,101,95,49,50,0);
    let sportsp = String.fromCharCode(114,101,108,97,121,111,117,116,95,57,95,54,54,0);
    let recommendationo = String.fromCharCode(97,118,102,105,108,116,101,114,95,118,95,57,56,0);
    let bootsplash2 = 4.0;
    let usernameO = 0.0;
    let robotoP: Array<any> = [882, 911, 945];
    let flyerW = String.fromCharCode(103,101,110,97,110,110,95,104,95,51,54,0);
    let tumbnailA = true;
    let data0 = false;
    let inviteC = String.fromCharCode(106,95,54,54,95,97,99,116,97,98,0);
   while (with_0tF.includes(`${sina1.length}`)) {
       let chat0 = 2;
         chat0 >>= Math.min(4, Math.abs(1 + chat0));
          let launchY: Array<any> = [963, 294];
          let nterstitialz = 4;
          let overw: Array<any> = [246, 908, 715];
         chat0 |= 1 / (Math.max(chat0, 8));
         launchY.push(launchY.length % 2);
         nterstitialz ^= 1;
         overw = [2 * launchY.length];
         chat0 *= 1;
      sina1.push((String.fromCharCode(95,0) == with_0tF ? parseInt(`${typingt}`) : with_0tF.length));
      break;
   }
   if (scorec.length == 4) {
      scorec += `${parseInt(`${bootsplash2}`) % 3}`;
   }
   let actionsl = castingO ? !castingO : castingO;
   do {
       let googlei: Map<any, any> = new Map([[String.fromCharCode(121,95,51,57,95,112,114,111,98,108,101,109,115,0),632], [String.fromCharCode(116,114,97,110,115,95,110,95,54,53,0),137], [String.fromCharCode(98,95,48,95,97,100,100,105,116,105,111,110,0),518]]);
       let read2: Array<any> = [947, 814];
       let frame_fy: Map<any, any> = new Map([[String.fromCharCode(98,95,56,49,95,118,111,99,97,98,0),445], [String.fromCharCode(113,95,51,57,95,100,118,97,117,100,105,111,0),806], [String.fromCharCode(105,109,112,114,101,115,115,105,111,110,115,95,109,95,52,56,0),179]]);
       let linkx = 3;
       let spinnera = 5;
      let alertu = 5337089 <= googlei.size;
      do {
         googlei.set(`${spinnera}`, 1 ^ spinnera);
         if (alertu) {
            break;
         }
      } while (alertu && (1 > googlei.size));
          let bingy = String.fromCharCode(115,116,114,107,95,104,95,56,48,0);
          let combineh: Array<any> = [291, 931];
         frame_fy = new Map([[`${googlei.size}`, spinnera % (Math.max(googlei.size, 8))]]);
         bingy = `${combineh.length}`;
         combineh.push(combineh.length);
         linkx <<= Math.min(2, Math.abs(spinnera * 3));
      while (read2.length <= frame_fy.size) {
         read2 = [linkx >> (Math.min(Math.abs(googlei.size), 2))];
         break;
      }
       let downloadingR = String.fromCharCode(101,95,52,55,95,101,110,116,114,121,0);
      while (4 >= (2 - frame_fy.size) && (spinnera - frame_fy.size) >= 2) {
         spinnera *= 1 & read2.length;
         break;
      }
       let private_xoP = 1;
       let animation0 = 0;
      while (downloadingR.startsWith(`${linkx}`)) {
         linkx -= googlei.size ^ 3;
         break;
      }
      castingO = sina1.length >= frame_fy.size;
      if (actionsl) {
         break;
      }
   } while ((4.79 > updates5) && actionsl);
      castingO = updates5 <= 10.21;
       let videoa = 4;
          let dropdownW = String.fromCharCode(98,95,57,57,95,116,114,97,105,108,101,114,0);
          let greenl = false;
          let middleN = String.fromCharCode(116,102,114,97,95,107,95,54,55,0);
         videoa &= middleN.length;
         dropdownW += `${3 * dropdownW.length}`;
         greenl = 27 < dropdownW.length;
      for (let i = 0; i < 2; i++) {
         videoa >>= Math.min(Math.abs(videoa & videoa), 4);
      }
         videoa >>= Math.min(Math.abs(1 & videoa), 3);
      bootsplash2 += (parseFloat(`${String.fromCharCode(79,0) == recommendationo ? recommendationo.length : (castingO ? 1 : 1)}`));
   while ((recommendationo.length | diceG) < 2) {
       let countryT = String.fromCharCode(115,104,105,102,116,115,95,56,95,49,55,0);
       let themeO = String.fromCharCode(119,95,55,48,95,109,112,116,111,97,110,110,101,120,98,0);
       let aboute = String.fromCharCode(100,95,53,56,95,100,114,111,112,0);
       let termsd: Array<any> = [527, 694, 126];
       let vignetteC = String.fromCharCode(100,95,55,52,95,120,106,112,101,103,0);
      while (countryT.length <= 3) {
         countryT += `${themeO.length}`;
         break;
      }
       let circles = true;
         themeO = `${1 ^ aboute.length}`;
         aboute += "2";
         termsd = [aboute.length / (Math.max(3, 9))];
          let model_ = true;
          let tail6 = 0;
          let shareO = String.fromCharCode(118,105,101,119,115,95,113,95,53,50,0);
         themeO = "2";
         model_ = shareO == String.fromCharCode(110,0);
         tail6 &= shareO.length ^ 3;
         termsd.push((String.fromCharCode(105,0) == aboute ? aboute.length : themeO.length));
         vignetteC = "2";
       let halfD = 0.0;
       let expands = 4.0;
         halfD -= vignetteC.length;
       let statsr = String.fromCharCode(101,95,57,57,95,115,112,114,105,116,101,115,0);
       let h_centerS = String.fromCharCode(98,101,110,105,103,110,95,113,95,51,57,0);
      while (countryT.length < 1) {
         termsd = [1 << (Math.min(2, themeO.length))];
         break;
      }
      if (vignetteC.length <= 2) {
          let internetg = String.fromCharCode(104,95,54,57,95,109,98,105,110,116,114,97,0);
          let projecta = String.fromCharCode(116,114,117,110,95,121,95,54,52,0);
          let sport7 = 2.0;
          let window_9l: Map<any, any> = new Map([[String.fromCharCode(122,95,49,95,98,108,111,98,115,105,122,101,0),880], [String.fromCharCode(98,95,57,56,95,100,105,115,97,112,112,101,97,114,101,100,0),464]]);
         circles = 93 == themeO.length && 93 == window_9l.size;
         internetg = `${(String.fromCharCode(122,0) == projecta ? internetg.length : projecta.length)}`;
         sport7 -= parseFloat(`${internetg.length}`);
         window_9l.set(internetg, parseInt(`${sport7}`));
      }
         statsr += `${parseInt(`${halfD}`) | 2}`;
      if (h_centerS.length < themeO.length) {
         themeO = `${((circles ? 4 : 4) >> (Math.min(Math.abs(parseInt(`${halfD}`)), 1)))}`;
      }
      diceG ^= 2 << (Math.min(Math.abs(diceG), 5));
      break;
   }
      usernameO += sportsp.length;
   if ((2 + with_0tF.length) >= 3) {
       let upload8 = 4;
       let countryn = 5;
      while ((upload8 % (Math.max(countryn, 8))) <= 1 && 2 <= (countryn % (Math.max(1, 8)))) {
         countryn <<= Math.min(Math.abs(upload8), 5);
         break;
      }
      for (let j = 0; j < 1; j++) {
         countryn ^= upload8;
      }
         upload8 ^= upload8 | 1;
      if ((5 ^ upload8) == 5) {
          let langkey_: Array<any> = [959, 307];
          let thailande: Array<any> = [855, 593];
         upload8 >>= Math.min(3, Math.abs(langkey_.length | 1));
         langkey_.push(thailande.length);
         thailande.push(3 / (Math.max(6, thailande.length)));
      }
         countryn += countryn * upload8;
         countryn <<= Math.min(Math.abs(countryn << (Math.min(Math.abs(upload8), 1))), 2);
      typingt -= ((castingO ? 1 : 2) + parseInt(`${bootsplash2}`));
   }
   while (typingt >= with_0tF.length) {
      with_0tF = `${parseInt(`${updates5}`) / (Math.max(with_0tF.length, 4))}`;
      break;
   }
   let umengy = castingO ? !castingO : castingO;
   do {
      castingO = 1 < diceG;
      if (umengy) {
         break;
      }
   } while (umengy && (!yingG.endsWith(`${castingO}`)));
   if ((diceG % 4) > 5 || 3.71 > (parseFloat(`${diceG}`) / (Math.max(bootsplash2, 6)))) {
      bootsplash2 *= parseFloat(`${diceG}`);
   }
      updates5 *= ((castingO ? 2 : 3) % 2);
   for (let i = 0; i < 2; i++) {
      sportsp = `${with_0tF.length}`;
   }
   let default_qpa = 8108360.0 <= updates5;
   do {
      updates5 += 1 * parseInt(`${typingt}`);
      if (default_qpa) {
         break;
      }
   } while ((5 == (3 & sportsp.length)) && default_qpa);
   let robotoI = String.fromCharCode(116,55,55,51,57,57,0) == recommendationo;
   do {
      recommendationo += `${with_0tF.length - yingG.length}`;
      if (robotoI) {
         break;
      }
   } while (robotoI && (with_0tF.length >= recommendationo.length));
   for (let s = 0; s < 3; s++) {
      diceG *= scorec.length;
   }
       let logoutW = 1;
       let dialogY = String.fromCharCode(106,97,99,99,97,114,100,100,105,115,116,95,119,95,56,48,0);
      if (dialogY.includes(`${logoutW}`)) {
         logoutW ^= dialogY.length;
      }
          let hongkongd = String.fromCharCode(112,116,115,95,103,95,57,49,0);
          let editZ = String.fromCharCode(115,95,50,56,95,114,101,112,117,98,108,105,115,104,0);
          let iconQ = 4.0;
         dialogY = `${(String.fromCharCode(116,0) == dialogY ? dialogY.length : editZ.length)}`;
         hongkongd = `${(hongkongd == String.fromCharCode(74,0) ? hongkongd.length : parseInt(`${iconQ}`))}`;
         editZ += `${(hongkongd == String.fromCharCode(68,0) ? hongkongd.length : parseInt(`${iconQ}`))}`;
         dialogY = `${dialogY.length}`;
         dialogY = `${dialogY.length}`;
          let roboto8: Map<any, any> = new Map([[String.fromCharCode(118,95,56,48,95,100,101,115,116,114,117,99,116,111,114,0),String.fromCharCode(115,105,103,110,105,102,105,99,97,110,100,95,57,95,53,48,0)], [String.fromCharCode(97,118,100,101,118,105,99,101,114,101,115,95,103,95,54,50,0),String.fromCharCode(99,95,56,54,95,110,111,110,98,0)], [String.fromCharCode(103,95,49,51,95,115,99,97,110,110,101,100,0),String.fromCharCode(119,101,105,103,104,116,115,95,101,95,52,51,0)]]);
          let cast8 = 3.0;
          let umengE = 0;
         dialogY = `${logoutW}`;
         roboto8 = new Map([[`${roboto8.size}`, parseInt(`${cast8}`)]]);
         cast8 -= parseFloat(`${parseInt(`${cast8}`) + 1}`);
         umengE &= parseInt(`${cast8}`);
      let orientationm = logoutW >= 6515940;
      do {
         logoutW += 2 << (Math.min(4, dialogY.length));
         if (orientationm) {
            break;
         }
      } while (orientationm && (1 == (2 + dialogY.length) && 1 == (2 + logoutW)));
      sina1 = [sportsp.length | with_0tF.length];
      with_0tF = `${(3 << (Math.min(5, Math.abs((castingO ? 5 : 4)))))}`;
   let eactv = sina1.length >= 8573899;
   do {
      sina1 = [yingG.length];
      if (eactv) {
         break;
      }
   } while ((castingO) && eactv);
      bootsplash2 /= Math.max(2, parseFloat(`${2 * robotoP.length}`));
      usernameO -= parseInt(`${usernameO}`) & 1;
   if (with_0tF.startsWith(`${sina1.length}`)) {
      with_0tF += `${parseInt(`${typingt}`) >> (Math.min(Math.abs(2), 3))}`;
   }
      yingG += `${(recommendationo == String.fromCharCode(105,0) ? recommendationo.length : parseInt(`${usernameO}`))}`;
      yingG = `${sportsp.length}`;

    setImgUrl(useNextImage())
  }

  if (error) { // when all url error, use the default placeholder image
    return (
      <View style={[{ backgroundColor: colors.loading, ...styles.loadingCard}, imageProp.style]}>
          <LoadingImage  width={20}  height={20}/>
      </View>
    )
  }

  if (useFastImage === true || Platform.OS === "android"){
    if (typeof imageProp.source == 'number'){ // if source={require(...)}
    return <FastImage {...imageProp as FastImageProps}/>
    } else {
      return <FastImage key={current.toString() + imgUrl} {...imageProp as FastImageProps} source={{uri: imgUrl}} onError={handleOnError}/>
    }
  } else {
    if (typeof imageProp.source == 'number'){ // if source={require(...)}
      return <Image {...imageProp as VJZRenew}/>
    } else { // if source={{uri:...}}
      return <Image key={current.toString() + imgUrl} {...imageProp as VJZRenew} source={{...(imageProp.source as ImageURISource), uri: imgUrl}} onError={handleOnError}/>
    }
  }
}

const styles = StyleSheet.create({
  loadingCard: {
    marginRight: 20,
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    borderRadius: 10,
},
})

export default customFastImage;