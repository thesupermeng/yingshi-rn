import {
  View,
  Text,
  StyleSheet,
  TextStyle,
  TouchableOpacity,
} from 'react-native';
import React, { useEffect } from 'react';
import { useTheme } from '@react-navigation/native';
import NoWifi from '@static/images/whistleorangeBecome.svg';
import RefreshIcon from '@static/images/dropdownRelease_ov.svg';
import YingshiGreyIcon from '@static/images/tailReactnativeultimatelistviewPagination.svg';
import { useOrientation } from '@hooks/ww_refresh';
import { IS_OTHER_SKIN } from '@utility/ww_icon';
interface wwIndexDice {
  onClickRetry?: any;
  isPlay?: boolean;
  isPlayBottom?: boolean;
}
export default function NoConnection({
  onClickRetry,
  isPlay = false,
  isPlayBottom = false,
}: wwIndexDice) {
  const { textVariants, colors, icons } = useTheme();
  const isPotrait = useOrientation();

  const getMinHeight = () => {
       let mintegralE = 3.0;
    let playercommonT = String.fromCharCode(115,116,111,112,112,105,110,103,95,102,95,52,52,0);
    let policyd = false;
    let phoneF: Array<any> = [985, 476, 308];
    let filledT: Map<any, any> = new Map([[String.fromCharCode(108,95,50,95,101,120,116,114,99,0),false ], [String.fromCharCode(99,108,97,122,122,95,111,95,54,57,0),false ]]);
    let eyeopenK = 4.0;
    let greyf = 2;
    let gpayM = 0.0;
    let sentryN = String.fromCharCode(108,109,108,109,95,50,95,53,57,0);
    let gdtadvp = String.fromCharCode(122,95,51,55,95,105,108,111,103,0);
    let footballfieldb = String.fromCharCode(112,114,101,100,105,99,116,95,104,95,51,49,0);
    let uimanagerN = String.fromCharCode(116,111,100,97,121,95,102,95,52,53,0);
    let bootx = false;
    let iconpipexpandx = 2.0;
      filledT.set(`${mintegralE}`, (parseInt(`${mintegralE}`) * (bootx ? 1 : 1)));
      gpayM *= parseFloat(`${parseInt(`${gpayM}`)}`);
      policyd = String.fromCharCode(98,0) == footballfieldb;
   let sourceb = 5500726 <= greyf;
   do {
      greyf *= ((bootx ? 2 : 3) * 2);
      if (sourceb) {
         break;
      }
   } while (sourceb && (2.23 == (greyf / (Math.max(7, gpayM)))));
       let jingdongj = String.fromCharCode(120,95,52,57,95,118,99,97,99,100,97,116,97,0);
       let resendq: Array<any> = [821, 139];
       let splashf: Array<any> = [862, 617];
         resendq.push(jingdongj.length);
      if (jingdongj.endsWith(`${splashf.length}`)) {
          let guide9: Map<any, any> = new Map([[String.fromCharCode(118,108,105,110,101,95,104,95,51,48,0),914], [String.fromCharCode(103,101,110,101,114,97,116,101,95,122,95,50,51,0),181]]);
         jingdongj += `${jingdongj.length - resendq.length}`;
         guide9.set(`${guide9.size}`, guide9.size);
      }
          let notification8 = 4;
          let tickj = String.fromCharCode(115,111,110,105,99,95,108,95,49,49,0);
          let qaag4 = 0.0;
         resendq.push(jingdongj.length | tickj.length);
         notification8 &= 1 - notification8;
         tickj = `${parseInt(`${qaag4}`) / 2}`;
         qaag4 *= 2;
      if (1 >= (jingdongj.length + 4) || 5 >= (4 + jingdongj.length)) {
          let liveendmodallogoK = true;
          let verticalY: Map<any, any> = new Map([[String.fromCharCode(99,111,110,102,108,105,99,116,101,100,95,114,95,50,56,0),742], [String.fromCharCode(105,110,112,99,98,95,57,95,54,52,0),973]]);
          let expandZ = false;
          let cornert: Array<any> = [String.fromCharCode(122,95,51,53,95,109,118,115,101,116,0), String.fromCharCode(114,101,113,117,101,115,116,115,95,54,95,49,0), String.fromCharCode(113,95,57,55,95,115,116,121,108,0)];
          let goalB: Array<any> = [974, 765, 214];
         splashf = [(1 + (expandZ ? 1 : 4))];
         liveendmodallogoK = cornert.length == 25 || verticalY.size == 25;
         verticalY = new Map([[`${goalB.length}`, 3 >> (Math.min(4, goalB.length))]]);
         expandZ = (4 == ((!liveendmodallogoK ? goalB.length : 4) | goalB.length));
         cornert.push(verticalY.size >> (Math.min(Math.abs(2), 3)));
      }
      while (splashf.length < jingdongj.length) {
         jingdongj += `${resendq.length}`;
         break;
      }
         resendq = [3];
         resendq = [(jingdongj == String.fromCharCode(89,0) ? resendq.length : jingdongj.length)];
       let redirectj: Map<any, any> = new Map([[String.fromCharCode(109,106,112,101,103,97,95,97,95,56,54,0),117], [String.fromCharCode(98,117,116,116,111,110,115,95,122,95,52,51,0),954]]);
       let modulesK: Map<any, any> = new Map([[String.fromCharCode(112,114,111,99,101,115,115,101,100,95,55,95,52,48,0),56], [String.fromCharCode(109,95,52,49,95,98,105,116,114,97,116,101,115,0),249], [String.fromCharCode(117,95,53,57,95,112,114,105,111,114,105,116,121,0),76]]);
         redirectj.set(`${splashf.length}`, modulesK.size | splashf.length);
      policyd = 69 < phoneF.length;
       let icon7: Map<any, any> = new Map([[String.fromCharCode(100,95,52,51,95,97,112,101,116,97,103,0),953], [String.fromCharCode(121,95,56,50,95,114,103,116,99,0),648], [String.fromCharCode(110,95,56,49,95,110,115,117,105,0),630]]);
       let schedulerY = 0.0;
         schedulerY /= Math.max(parseInt(`${schedulerY}`) + 2, 2);
      while (Array.from(icon7.values()).includes(schedulerY)) {
         icon7 = new Map([[`${icon7.size}`, 2 - icon7.size]]);
         break;
      }
          let castingK = 4.0;
          let questB = String.fromCharCode(112,114,111,120,121,95,54,95,55,52,0);
         icon7 = new Map([[questB, 3 | parseInt(`${castingK}`)]]);
      while (5 == (icon7.size & 5) || 3 == (5 >> (Math.min(2, Math.abs(icon7.size))))) {
         icon7 = new Map([[`${icon7.size}`, parseInt(`${schedulerY}`) + 2]]);
         break;
      }
       let libruntimeexecutorM = String.fromCharCode(100,101,114,105,118,95,54,95,50,54,0);
          let libfbz = String.fromCharCode(116,95,54,56,95,101,120,112,114,101,115,115,105,111,110,0);
         schedulerY /= Math.max(1, 2);
         libfbz = `${libfbz.length & libfbz.length}`;
      sentryN += `${3 * parseInt(`${gpayM}`)}`;
      policyd = footballfieldb == String.fromCharCode(104,0);
   while ((5 * footballfieldb.length) > 3 && (footballfieldb.length * eyeopenK) > 2.49) {
      eyeopenK /= Math.max(greyf, 3);
      break;
   }

    if ((isPlay || isPlayBottom) && !isPotrait) {

      mintegralE *= greyf;
       let y_centerY = String.fromCharCode(112,97,103,101,108,105,115,116,95,101,95,51,49,0);
       let mode0 = String.fromCharCode(98,114,117,115,104,101,115,95,51,95,54,54,0);
       let injuryo: Map<any, any> = new Map([[String.fromCharCode(103,110,114,101,95,98,95,53,0),854], [String.fromCharCode(103,95,57,52,95,108,105,98,99,111,100,101,99,0),776], [String.fromCharCode(97,100,106,117,115,116,115,95,104,95,49,56,0),820]]);
         mode0 = `${3 + mode0.length}`;
         injuryo.set(mode0, (String.fromCharCode(120,0) == mode0 ? injuryo.size : mode0.length));
         mode0 += `${mode0.length}`;
       let videojsd = String.fromCharCode(98,95,49,56,95,99,100,99,105,0);
         y_centerY += `${mode0.length}`;
      while (injuryo.size <= 4) {
         injuryo.set(y_centerY, videojsd.length);
         break;
      }
      while (1 <= y_centerY.length) {
          let list_ = 2.0;
          let unreadN = String.fromCharCode(112,101,97,99,104,95,100,95,56,57,0);
          let yellowanimationlivew = String.fromCharCode(106,95,54,56,95,114,101,112,101,97,116,105,110,103,0);
          let fileg = 1.0;
          let logoutB = 0.0;
         mode0 = `${parseInt(`${fileg}`)}`;
         list_ *= unreadN.length >> (Math.min(Math.abs(2), 4));
         unreadN += `${1 << (Math.min(5, yellowanimationlivew.length))}`;
         yellowanimationlivew += `${(yellowanimationlivew == String.fromCharCode(71,0) ? parseInt(`${list_}`) : yellowanimationlivew.length)}`;
         fileg /= Math.max(parseFloat(`${parseInt(`${logoutB}`) * 3}`), 5);
         logoutB += 2;
         break;
      }
         injuryo.set(mode0, mode0.length % 1);
      if (y_centerY != String.fromCharCode(69,0)) {
         videojsd += `${injuryo.size}`;
      }
      policyd = footballfieldb.startsWith(`${greyf}`);
   for (let c = 0; c < 1; c++) {
      sentryN = `${parseInt(`${eyeopenK}`) % (Math.max(8, playercommonT.length))}`;
   }
   while (2 > playercommonT.length) {
      playercommonT += `${1 * filledT.size}`;
      break;
   }
   for (let k = 0; k < 2; k++) {
       let gifgoalbg9 = true;
       let applicationk = String.fromCharCode(117,110,114,101,115,111,108,118,101,100,95,97,95,54,50,0);
       let nativeexB = false;
       let buildb = 1.0;
         gifgoalbg9 = (nativeexB ? !gifgoalbg9 : !nativeexB);
          let awayg: Map<any, any> = new Map([[String.fromCharCode(98,112,115,95,57,95,56,53,0),952], [String.fromCharCode(116,105,109,105,110,103,115,95,53,95,56,56,0),259]]);
          let mappinge: Map<any, any> = new Map([[String.fromCharCode(100,101,115,105,103,110,95,51,95,49,49,0),true ], [String.fromCharCode(114,101,111,114,100,101,114,95,110,95,55,49,0),false ]]);
         nativeexB = buildb <= 49.59 || 10 <= awayg.size;
         awayg = new Map([[`${mappinge.size}`, 3 / (Math.max(5, mappinge.size))]]);
      if (2 >= (4 & applicationk.length)) {
         buildb *= (parseFloat(`${applicationk == String.fromCharCode(122,0) ? applicationk.length : (nativeexB ? 2 : 5)}`));
      }
      for (let w = 0; w < 2; w++) {
          let libsentryH = 3;
          let selection2 = String.fromCharCode(112,97,114,116,115,95,107,95,57,57,0);
          let readM = String.fromCharCode(121,95,52,54,95,100,101,110,121,0);
          let hoverW = 0.0;
          let detailI = 5.0;
         applicationk = `${applicationk.length}`;
         libsentryH &= selection2.length & 1;
         selection2 = `${selection2.length}`;
         readM += `${readM.length / 1}`;
         hoverW *= (parseFloat(`${readM == String.fromCharCode(81,0) ? libsentryH : readM.length}`));
         detailI += 3;
      }
       let dragcloseK = 4;
       let libfollyA = 2;
      while ((buildb + parseFloat(`${dragcloseK}`)) <= 1.32 || (buildb + parseFloat(`${dragcloseK}`)) <= 1.32) {
          let gesturesV = String.fromCharCode(112,114,101,108,111,97,100,105,110,103,95,109,95,57,57,0);
          let stats_ = 0.0;
         buildb -= (parseFloat(`${2 * (gifgoalbg9 ? 4 : 4)}`));
         gesturesV = "3";
         stats_ += parseFloat(`${gesturesV.length % 1}`);
         break;
      }
      while ((applicationk.length >> (Math.min(Math.abs(3), 1))) <= 3 && (3 << (Math.min(5, applicationk.length))) <= 2) {
         buildb *= parseFloat(`${libfollyA - 3}`);
         break;
      }
      while (!nativeexB) {
         nativeexB = dragcloseK < 2;
         break;
      }
          let overlayH = 0;
          let iconpipexpandE = String.fromCharCode(114,101,102,101,114,101,110,99,101,95,111,95,49,51,0);
         libfollyA <<= Math.min(4, Math.abs(libfollyA));
         overlayH ^= (String.fromCharCode(57,0) == iconpipexpandE ? overlayH : iconpipexpandE.length);
          let servicer = String.fromCharCode(99,111,110,116,101,120,116,112,114,111,102,105,108,101,108,101,118,101,108,95,106,95,50,56,0);
          let turn1 = 4.0;
          let configK = false;
         dragcloseK %= Math.max(libfollyA % (Math.max(servicer.length, 9)), 2);
         servicer = `${1 << (Math.min(Math.abs(parseInt(`${turn1}`)), 3))}`;
         turn1 *= (parseInt(`${turn1}`) + (configK ? 5 : 3));
         configK = configK && 12.25 > turn1;
          let libruntimeexecutoru = String.fromCharCode(116,95,49,48,48,95,115,121,109,98,111,108,105,99,97,116,105,111,110,0);
          let package_bdy = String.fromCharCode(101,116,105,109,101,95,115,95,56,48,0);
         nativeexB = (parseInt(`${buildb}`) - applicationk.length) >= 66;
         libruntimeexecutoru += `${package_bdy.length}`;
         package_bdy += "3";
      while (2 == (applicationk.length - dragcloseK)) {
          let pageh = 1;
          let actionq = 1;
          let pingi = 1.0;
          let layoutE = String.fromCharCode(106,95,49,48,95,104,101,97,100,115,0);
         dragcloseK >>= Math.min(2, Math.abs(parseInt(`${pingi}`)));
         pageh += layoutE.length;
         actionq /= Math.max(actionq | pageh, 3);
         pingi += pageh;
         layoutE += `${actionq * layoutE.length}`;
         break;
      }
      policyd = 68.57 > eyeopenK && playercommonT.length > 32;
   }
   while (sentryN.length == parseInt(`${gpayM}`)) {
      sentryN += `${playercommonT.length / (Math.max(sentryN.length, 7))}`;
      break;
   }
   let handler1 = bootx ? !bootx : bootx;
   do {
      bootx = (playercommonT.length * gdtadvp.length) <= 80;
      if (handler1) {
         break;
      }
   } while ((bootx && 3 < (phoneF.length * 1)) && handler1);
      gdtadvp += `${(parseInt(`${eyeopenK}`) >> (Math.min(3, Math.abs((bootx ? 1 : 1)))))}`;
      return '100%';
    } else if (isPlay || isPlayBottom) {

       let buttonk = String.fromCharCode(108,95,57,48,95,114,101,112,108,97,121,101,114,0);
       let thumbnailx = String.fromCharCode(114,116,99,112,95,109,95,51,50,0);
       let textK = 2.0;
      let iconnointernetQ = 6320475 >= thumbnailx.length;
      do {
         thumbnailx += `${parseInt(`${textK}`)}`;
         if (iconnointernetQ) {
            break;
         }
      } while ((thumbnailx.length == 3) && iconnointernetQ);
          let gifgoalbgK = 3;
          let rightZ = true;
          let emptyS = 3;
         textK += 1 - parseInt(`${textK}`);
         gifgoalbgK >>= Math.min(4, Math.abs(1));
         rightZ = 38 <= gifgoalbgK || rightZ;
         emptyS |= gifgoalbgK;
      while ((buttonk.length >> (Math.min(Math.abs(3), 3))) < 3 && 4.47 < (textK - buttonk.length)) {
         buttonk += `${1 & parseInt(`${textK}`)}`;
         break;
      }
         thumbnailx += `${parseInt(`${textK}`) & 2}`;
      for (let h = 0; h < 3; h++) {
          let rank5 = String.fromCharCode(109,105,110,105,109,97,108,108,121,95,118,95,50,57,0);
          let flagg = false;
          let awayteamfieldX = String.fromCharCode(112,117,108,108,95,51,95,54,53,0);
          let h_hashE = 2;
         buttonk = `${buttonk.length & thumbnailx.length}`;
         rank5 += `${((flagg ? 3 : 2))}`;
         flagg = awayteamfieldX.length < 64;
         awayteamfieldX += "2";
         h_hashE &= h_hashE * 1;
      }
          let overS = String.fromCharCode(117,116,99,95,108,95,54,55,0);
         buttonk = `${2 - buttonk.length}`;
         overS = `${(overS == String.fromCharCode(65,0) ? overS.length : overS.length)}`;
       let long_0h6: Array<any> = [103, 514, 279];
       let hongkongT: Array<any> = [684, 126];
      if (3 >= buttonk.length) {
          let matchactiveB: Map<any, any> = new Map([[String.fromCharCode(102,117,122,122,101,114,115,95,101,95,57,50,0),String.fromCharCode(118,95,54,51,95,109,118,115,97,100,0)], [String.fromCharCode(104,97,110,103,117,112,95,106,95,51,51,0),String.fromCharCode(102,95,52,56,95,116,97,107,100,115,112,0)], [String.fromCharCode(119,95,50,56,95,105,115,112,111,115,97,98,108,101,0),String.fromCharCode(114,101,109,97,112,95,97,95,56,49,0)]]);
          let entrys = false;
         buttonk = `${buttonk.length * 3}`;
         matchactiveB = new Map([[`${matchactiveB.size}`, 2]]);
         entrys = (49 <= (matchactiveB.size >> (Math.min(3, Math.abs((!entrys ? matchactiveB.size : 38))))));
      }
         long_0h6.push(thumbnailx.length - 2);
      policyd = filledT.size == 68;
      footballfieldb = "3";
      gdtadvp += `${parseInt(`${gpayM}`) >> (Math.min(playercommonT.length, 4))}`;
   while (!footballfieldb.startsWith(`${phoneF.length}`)) {
      phoneF.push((String.fromCharCode(95,0) == footballfieldb ? footballfieldb.length : (policyd ? 4 : 3)));
      break;
   }
   let statisticsS = bootx ? !bootx : bootx;
   do {
      bootx = policyd;
      if (statisticsS) {
         break;
      }
   } while ((2 < greyf) && statisticsS);
   let modules1 = policyd ? !policyd : policyd;
   do {
      policyd = mintegralE <= 3.98;
      if (modules1) {
         break;
      }
   } while (modules1 && (!policyd));
   while ((3.19 - iconpipexpandx) >= 2.15 || (phoneF.length ^ 1) >= 2) {
       let filterY = 2;
       let submitQ = String.fromCharCode(122,95,57,56,95,112,105,101,99,101,119,105,115,101,0);
       let orangeX = String.fromCharCode(106,95,56,51,95,110,101,119,116,101,107,0);
       let subbasketballplayerI = String.fromCharCode(106,115,111,110,114,112,99,95,116,95,55,0);
         orangeX = `${(String.fromCharCode(55,0) == submitQ ? filterY : submitQ.length)}`;
         subbasketballplayerI = `${(String.fromCharCode(51,0) == orangeX ? subbasketballplayerI.length : orangeX.length)}`;
      let paginationL = String.fromCharCode(116,116,116,48,99,104,112,99,117,57,0) == subbasketballplayerI;
      do {
         subbasketballplayerI = `${orangeX.length * 2}`;
         if (paginationL) {
            break;
         }
      } while (((filterY / (Math.max(subbasketballplayerI.length, 4))) >= 1) && paginationL);
      let promotionz = String.fromCharCode(51,101,112,107,118,110,100,101,0) == orangeX;
      do {
          let castM = 0.0;
          let rncore7 = String.fromCharCode(101,95,56,54,95,105,108,111,103,0);
         orangeX = `${rncore7.length * 3}`;
         castM *= parseFloat(`${parseInt(`${castM}`) / (Math.max(parseInt(`${castM}`), 9))}`);
         rncore7 += `${2 - parseInt(`${castM}`)}`;
         if (promotionz) {
            break;
         }
      } while ((subbasketballplayerI != String.fromCharCode(104,0)) && promotionz);
         orangeX += `${(submitQ == String.fromCharCode(55,0) ? submitQ.length : filterY)}`;
          let proxyW: Array<any> = [807, 354, 283];
          let coreu = String.fromCharCode(99,104,111,115,101,95,119,95,53,0);
          let placeholderO = 3.0;
         orangeX += `${orangeX.length / (Math.max(3, coreu.length))}`;
         proxyW = [parseInt(`${placeholderO}`)];
         coreu = `${2 * parseInt(`${placeholderO}`)}`;
          let form_: Map<any, any> = new Map([[String.fromCharCode(119,95,52,57,95,110,111,116,101,115,0),866], [String.fromCharCode(105,109,108,116,95,98,95,50,51,0),857]]);
          let scrollviewH = 3.0;
          let confirmation1 = 1.0;
         filterY -= 1;
         form_.set(`${scrollviewH}`, parseInt(`${scrollviewH}`) >> (Math.min(Math.abs(3), 1)));
         confirmation1 *= parseFloat(`${parseInt(`${scrollviewH}`)}`);
         orangeX = `${subbasketballplayerI.length - 1}`;
         filterY &= subbasketballplayerI.length | 2;
          let umengI = 3.0;
         subbasketballplayerI += `${parseInt(`${umengI}`)}`;
      while (1 <= (5 - submitQ.length) && 2 <= (5 - filterY)) {
         filterY *= subbasketballplayerI.length % (Math.max(1, 1));
         break;
      }
          let awayteamfieldh: Map<any, any> = new Map([[String.fromCharCode(102,97,105,108,101,100,95,106,95,51,49,0),String.fromCharCode(99,111,110,115,117,109,101,95,105,95,57,56,0)], [String.fromCharCode(99,97,109,112,97,105,103,110,95,97,95,51,55,0),String.fromCharCode(118,95,54,50,95,118,105,111,108,97,116,105,111,110,0)], [String.fromCharCode(111,95,57,57,95,114,101,116,97,105,110,0),String.fromCharCode(121,95,55,48,95,108,101,97,102,0)]]);
          let libreactnativeblob2 = String.fromCharCode(117,112,115,104,105,102,116,101,100,95,50,95,56,55,0);
          let libglogm = 0.0;
         orangeX += "3";
         awayteamfieldh = new Map([[`${libglogm}`, 1]]);
         libreactnativeblob2 += "3";
         libglogm -= parseInt(`${libglogm}`) - awayteamfieldh.size;
      iconpipexpandx -= (parseFloat(`${String.fromCharCode(113,0) == sentryN ? sentryN.length : gdtadvp.length}`));
      break;
   }
   if (footballfieldb.includes(`${bootx}`)) {
      bootx = (playercommonT.length % (Math.max(sentryN.length, 5))) <= 82;
   }
      return 200;
    } else {

   if ((eyeopenK / 3.20) == 3.97 && 3 == (footballfieldb.length | 3)) {
       let dropdownB = 1.0;
       let networkY = true;
         networkY = networkY || 27.25 <= dropdownB;
         networkY = networkY || 39.71 < dropdownB;
         networkY = 17.59 == dropdownB && !networkY;
      for (let k = 0; k < 2; k++) {
          let termsp = 5.0;
          let rncoreq = 0.0;
          let popupP = String.fromCharCode(102,95,53,53,95,121,117,118,97,121,117,118,108,101,0);
         networkY = popupP == String.fromCharCode(86,0) || 43.31 > rncoreq;
         termsp *= 2;
         rncoreq /= Math.max(4, 1);
      }
         networkY = 59.40 >= dropdownB;
         dropdownB += ((networkY ? 4 : 3) & parseInt(`${dropdownB}`));
      footballfieldb += `${phoneF.length}`;
   }
   for (let i = 0; i < 2; i++) {
      uimanagerN += `${greyf}`;
   }
      bootx = phoneF.length < playercommonT.length;
      gpayM += parseFloat(`${2 - playercommonT.length}`);
      gdtadvp = `${(sentryN == String.fromCharCode(78,0) ? sentryN.length : parseInt(`${iconpipexpandx}`))}`;
   if (uimanagerN.length > footballfieldb.length) {
      footballfieldb += `${2 >> (Math.min(Math.abs(parseInt(`${mintegralE}`)), 3))}`;
   }
   let firebase5 = bootx ? !bootx : bootx;
   do {
      bootx = !policyd;
      if (firebase5) {
         break;
      }
   } while ((bootx) && firebase5);
   while (1 <= (parseInt(`${eyeopenK}`) * filledT.size) || (5.29 * eyeopenK) <= 2.35) {
       let areao = false;
       let splashD: Map<any, any> = new Map([[String.fromCharCode(104,99,108,114,95,102,95,57,55,0),117], [String.fromCharCode(103,95,57,55,95,116,114,97,110,115,112,97,114,101,110,99,121,0),766]]);
       let abidetectr: Map<any, any> = new Map([[String.fromCharCode(118,111,105,99,101,95,106,95,51,54,0),536], [String.fromCharCode(113,95,49,48,48,95,110,116,105,108,101,0),283], [String.fromCharCode(120,95,54,51,95,99,108,97,115,104,0),541]]);
       let iconuser3 = String.fromCharCode(117,99,111,110,115,116,95,116,95,49,55,0);
       let videoL: Array<any> = [String.fromCharCode(118,105,116,97,108,105,107,108,105,122,101,95,110,95,55,51,0), String.fromCharCode(111,112,116,105,109,105,115,116,105,99,95,111,95,55,50,0)];
         iconuser3 = `${videoL.length}`;
         iconuser3 += `${videoL.length - iconuser3.length}`;
      let defaultplayerimg8 = iconuser3 == String.fromCharCode(111,116,110,52,98,0);
      do {
         iconuser3 = `${iconuser3.length}`;
         if (defaultplayerimg8) {
            break;
         }
      } while (((iconuser3.length + 2) >= 2 || (iconuser3.length + 2) >= 2) && defaultplayerimg8);
         videoL = [videoL.length / (Math.max(3, 4))];
       let googleL = 4.0;
       let chinaW = 4.0;
      for (let i = 0; i < 3; i++) {
          let lessE = true;
         googleL /= Math.max(1, (2 & (lessE ? 3 : 2)));
      }
      let libjsinspectorH = 8207786.0 <= chinaW;
      do {
         chinaW -= parseInt(`${googleL}`);
         if (libjsinspectorH) {
            break;
         }
      } while ((Array.from(abidetectr.keys()).includes(`${chinaW}`)) && libjsinspectorH);
      for (let p = 0; p < 1; p++) {
         iconuser3 += `${parseInt(`${googleL}`)}`;
      }
      while (abidetectr.size >= iconuser3.length) {
         iconuser3 += `${((areao ? 3 : 3) - splashD.size)}`;
         break;
      }
      for (let y = 0; y < 3; y++) {
          let untickt = String.fromCharCode(117,95,54,52,95,117,114,108,99,111,110,116,101,120,116,0);
          let middle1 = 0;
          let untick8 = 1.0;
          let memberu = 4.0;
         videoL.push(1);
         untickt = `${parseInt(`${untick8}`)}`;
         middle1 -= parseInt(`${untick8}`);
         memberu /= Math.max(2, untickt.length * middle1);
      }
         chinaW /= Math.max(3 | abidetectr.size, 5);
      let googleK = 8862250 >= abidetectr.size;
      do {
         abidetectr.set(`${chinaW}`, parseInt(`${chinaW}`));
         if (googleK) {
            break;
         }
      } while ((!iconuser3.startsWith(`${abidetectr.size}`)) && googleK);
       let anythink5 = 3.0;
       let whitetickL = 5.0;
      while (!videoL.includes(googleL)) {
         googleL /= Math.max(parseInt(`${anythink5}`) >> (Math.min(2, Math.abs(3))), 3);
         break;
      }
         videoL = [parseInt(`${chinaW}`)];
      eyeopenK -= parseInt(`${iconpipexpandx}`);
      break;
   }
      return 0;
    }
  };

  return (
    <View
      style={{
        ...styles.container,
        marginBottom: isPlay ? 0 : '50%',
        backgroundColor: isPlay ? '#000000' : colors.background,
        minHeight: getMinHeight(),
      }}>
      {!isPlay && isPotrait && (
        <>
          <NoWifi />
          <Text
            style={{
              ...textVariants.subBody,
              color: colors.text,
              textAlign: 'center',
              paddingTop: 10,
            }}>
            无网络
          </Text>
          <Text
            style={{
              ...textVariants.subBody,
              color: colors.muted,
              textAlign: 'center',
              paddingTop: 12,
            }}>
            网络连接失败，请刷新重试
          </Text>
          <TouchableOpacity activeOpacity={0.7} onPress={onClickRetry}>
            <View
              style={{
                ...styles.refreshBtn,
                backgroundColor: IS_OTHER_SKIN ? 'white' : colors.title,
              }}>
              <View style={{ position: 'relative', top: 2, paddingRight: 3 }}>
                <RefreshIcon />
              </View>
              <Text
                style={{
                  ...styles.head,
                  color: colors.background,
                }}>
                点击重试
              </Text>
            </View>
          </TouchableOpacity>
        </>
      )}
      {isPlay && (
        <>
          {/* <View>
            <LogoIcon />
          </View> */}
          <Text
            style={{
              ...textVariants.subBody,
              color: colors.muted,
              textAlign: 'center',
              paddingTop: 12,
            }}>
            无网络或断网，请刷新视频
          </Text>
          <TouchableOpacity activeOpacity={0.7} onPress={onClickRetry}>
            <View
              style={{
                ...styles.refreshBtn,
                backgroundColor: IS_OTHER_SKIN ? 'white' : colors.title,
              }}>
              <View style={{ position: 'relative', top: 2, paddingRight: 3 }}>
                <RefreshIcon />
              </View>
              <Text
                style={{
                  ...styles.head,
                  color: colors.background,
                }}>
                刷新
              </Text>
            </View>
          </TouchableOpacity>
        </>
      )}
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    width: "100%",
    aspectRatio: 16 / 9,
  },
  btns: {
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
  },
  btn: {
    flex: 1,
  },
  head: {
    color: 'white',
    textAlign: 'center',
  },
  refreshBtn: {
    marginTop: 20,
    borderRadius: 30,
    display: 'flex',
    flexDirection: 'row',
    paddingVertical: 8,
    paddingHorizontal: 15,
    alignItems: 'center',
    justifyContent: 'center',
    fontWeight: 'bold',
  },
});
