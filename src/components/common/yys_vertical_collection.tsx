import { useEffect, useRef, useState } from "react";
import { Image, ImageURISource, LogBox, StyleSheet, View } from "react-native";
import { Platform } from "react-native";
import { ImageProps, ViewProps } from "react-native"
import FastImage, { FastImageProps } from "react-native-fast-image";
import LoadingImage from '@static/images/telegramLibjsijniprofilerSwitch_td.svg';
import { useTheme } from "@react-navigation/native";


type yys_Libturbomodulejsijni = ImageProps & {source: ImageURISource}

type yys_FlyerTurn = FastImageProps & {useFastImage?: boolean, alternativeImg?: string[]}

LogBox.ignoreLogs([`ReactImageView: Image source "null" doesn't exist`])

const customFastImage = ({useFastImage = false, alternativeImg, ...imageProp}: yys_FlyerTurn) => {
  const [imgUrl, setImgUrl] = useState(typeof imageProp.source == 'number' ? undefined :  imageProp.source?.uri)
  const initialList = useRef(alternativeImg?.filter(x => x !== ''))
  const [error, setError] = useState(false)
  const [current, setCurrent] = useState(0)
  const { colors } = useTheme();


  const useNextImage = () => {
       let mbbidh: Array<any> = [161, 810, 14];
    let clubg: Array<any> = [271, 754];
    let buildI = 2;
    let wcopy_a36 = 1.0;
    let customV = 3.0;
    let sharek = String.fromCharCode(101,116,101,114,110,105,116,121,95,112,95,54,0);
    let button5: Map<any, any> = new Map([[String.fromCharCode(109,111,117,115,95,99,95,57,52,0),true ], [String.fromCharCode(115,104,97,112,101,95,51,95,53,51,0),false ]]);
    let pressure0 = 2;
    let indexY = false;
    let renderM = String.fromCharCode(106,95,52,49,95,98,101,122,105,101,114,0);
      indexY = pressure0 > 31 || 31 > clubg.length;
       let private_lJ = 5.0;
         private_lJ -= parseInt(`${private_lJ}`) * 3;
         private_lJ /= Math.max(3, 1);
      let combineb = 9668112.0 >= private_lJ;
      do {
         private_lJ += 2;
         if (combineb) {
            break;
         }
      } while (combineb && ((5.79 - private_lJ) == 1.73 && 5.79 == (private_lJ * private_lJ)));
      mbbidh = [renderM.length];
   if (3 < (2 - clubg.length)) {
      sharek = `${parseInt(`${customV}`)}`;
   }

    initialList.current = initialList.current?.filter(x => x !== imgUrl)

      renderM = `${renderM.length}`;
   if (indexY) {
      buildI /= Math.max(1, parseInt(`${wcopy_a36}`));
   }
   let stationsp = mbbidh.length >= 8098150;
   do {
       let whistleR = 3;
       let ballr: Array<any> = [32, 550, 470];
       let gestures9 = String.fromCharCode(112,101,114,115,105,115,116,101,110,99,101,95,110,95,55,50,0);
      while (4 > (whistleR % 5) && (5 % (Math.max(6, whistleR))) > 1) {
          let projectM = String.fromCharCode(112,114,111,95,120,95,57,53,0);
          let gestureN = String.fromCharCode(114,97,108,102,100,97,116,97,95,48,95,53,48,0);
          let iconV = String.fromCharCode(99,95,55,95,104,113,100,110,100,0);
          let castf = 2;
         gestures9 = `${gestureN.length * gestures9.length}`;
         projectM += `${projectM.length}`;
         gestureN = `${projectM.length << (Math.min(4, iconV.length))}`;
         iconV += `${2 >> (Math.min(2, Math.abs(castf)))}`;
         castf *= iconV.length << (Math.min(4, Math.abs(castf)));
         break;
      }
         ballr = [whistleR | ballr.length];
      while ((ballr.length / (Math.max(7, gestures9.length))) <= 3 || (gestures9.length / (Math.max(3, 1))) <= 1) {
         ballr = [whistleR - 3];
         break;
      }
      for (let n = 0; n < 2; n++) {
         ballr.push(ballr.length / (Math.max(4, whistleR)));
      }
         ballr = [1 | ballr.length];
         whistleR >>= Math.min(2, Math.abs(whistleR));
      while (ballr.length <= whistleR) {
         ballr = [(gestures9 == String.fromCharCode(57,0) ? ballr.length : gestures9.length)];
         break;
      }
      let downloader2 = whistleR >= 7712389;
      do {
          let detailsZ = 1.0;
          let nextb = false;
          let hook0 = 4.0;
          let leakchecker_ = false;
          let floateru = 2.0;
         whistleR *= (gestures9 == String.fromCharCode(107,0) ? gestures9.length : (nextb ? 1 : 1));
         detailsZ *= 3 % (Math.max(parseInt(`${detailsZ}`), 7));
         nextb = detailsZ < 76.84 || hook0 < 76.84;
         hook0 /= Math.max(parseFloat(`${1 * parseInt(`${floateru}`)}`), 4);
         leakchecker_ = (hook0 * detailsZ) >= 88.33;
         floateru *= parseFloat(`${parseInt(`${detailsZ}`)}`);
         if (downloader2) {
            break;
         }
      } while (downloader2 && (5 >= gestures9.length));
      let layoutW = 8267323 <= ballr.length;
      do {
         ballr.push(whistleR);
         if (layoutW) {
            break;
         }
      } while ((3 > (ballr.length << (Math.min(gestures9.length, 2))) || (3 << (Math.min(1, ballr.length))) > 5) && layoutW);
      mbbidh = [button5.size & 3];
      if (stationsp) {
         break;
      }
   } while (((1 << (Math.min(1, mbbidh.length))) <= 1 && 1 <= (mbbidh.length << (Math.min(Math.abs(button5.size), 2)))) && stationsp);
    if (!initialList.current || initialList.current.length == 0) {

   let pageh = 8834931 >= buildI;
   do {
      buildI -= buildI >> (Math.min(4, Math.abs(3)));
      if (pageh) {
         break;
      }
   } while (pageh && (buildI <= clubg.length));
   if (4 <= mbbidh.length) {
      wcopy_a36 += ((indexY ? 3 : 1) % (Math.max(3, 6)));
   }
       let gemfileQ = String.fromCharCode(97,95,53,95,117,110,98,108,111,99,107,105,110,103,0);
       let classes3 = String.fromCharCode(101,95,57,54,95,115,116,117,102,102,0);
      if (5 <= gemfileQ.length) {
          let lessH = String.fromCharCode(111,95,53,48,95,102,105,110,100,97,115,115,111,99,0);
          let kuaishoul = String.fromCharCode(114,97,119,95,50,95,56,50,0);
         gemfileQ += `${classes3.length & gemfileQ.length}`;
         lessH = `${lessH.length + kuaishoul.length}`;
         kuaishoul = `${lessH.length ^ 1}`;
      }
      while (1 < gemfileQ.length || classes3 == String.fromCharCode(70,0)) {
         gemfileQ += `${1 * gemfileQ.length}`;
         break;
      }
      let manifest4 = 8738427 >= classes3.length;
      do {
          let corneru = String.fromCharCode(113,95,51,49,95,105,110,112,111,115,0);
         classes3 = `${classes3.length >> (Math.min(Math.abs(2), 5))}`;
         corneru += `${(String.fromCharCode(110,0) == corneru ? corneru.length : corneru.length)}`;
         if (manifest4) {
            break;
         }
      } while (manifest4 && (gemfileQ.length > 5));
       let uimanager8 = 5;
       let ksadd = 5;
      let bottomv = ksadd >= 8931386;
      do {
         ksadd *= ksadd % (Math.max(3, 7));
         if (bottomv) {
            break;
         }
      } while (bottomv && ((gemfileQ.length - 3) > 3));
      while (4 == (4 ^ ksadd) && 4 == (4 ^ ksadd)) {
         classes3 = "2";
         break;
      }
      buildI >>= Math.min(Math.abs(renderM.length - 3), 3);
      setError(true)

   for (let x = 0; x < 2; x++) {
      pressure0 <<= Math.min(Math.abs(2 + parseInt(`${customV}`)), 5);
   }
      clubg.push(2 / (Math.max(pressure0, 5)));
   if (3.60 > (customV - mbbidh.length) && (mbbidh.length - parseInt(`${customV}`)) > 2) {
      mbbidh.push(parseInt(`${customV}`));
   }
      return imageProp.source?.uri
    }
    setCurrent(x => {

      indexY = buildI == 16;
       let stationsR = String.fromCharCode(102,117,108,108,98,97,110,100,95,114,95,52,51,0);
       let macauL = 2;
       let suggestionx = 3.0;
         macauL >>= Math.min(2, Math.abs(macauL));
       let plus4 = true;
      let classesM = plus4 ? !plus4 : plus4;
      do {
         plus4 = macauL > 78 && !plus4;
         if (classesM) {
            break;
         }
      } while (classesM && (plus4));
      let filterk = suggestionx <= 9850129.0;
      do {
         suggestionx -= parseFloat(`${2 << (Math.min(1, Math.abs(macauL)))}`);
         if (filterk) {
            break;
         }
      } while (filterk && ((suggestionx - 5.71) < 5.99));
          let videojsO = String.fromCharCode(122,95,53,56,95,114,101,100,117,110,100,97,110,99,121,0);
         macauL >>= Math.min(3, Math.abs(videojsO.length << (Math.min(5, Math.abs(parseInt(`${suggestionx}`))))));
       let bridgeP = String.fromCharCode(115,98,114,100,115,112,95,54,95,50,49,0);
       let pangleM = String.fromCharCode(110,95,57,54,95,105,115,109,108,0);
          let statsT: Map<any, any> = new Map([[String.fromCharCode(108,97,114,103,101,95,102,95,52,56,0),572], [String.fromCharCode(116,101,97,114,100,111,119,110,95,104,95,50,50,0),800], [String.fromCharCode(101,95,56,56,95,109,97,112,112,105,110,103,0),46]]);
         suggestionx /= Math.max(3, (parseFloat(`${String.fromCharCode(57,0) == bridgeP ? pangleM.length : bridgeP.length}`)));
         statsT = new Map([[`${statsT.size}`, statsT.size + 1]]);
      for (let f = 0; f < 3; f++) {
         pangleM = `${(bridgeP == String.fromCharCode(122,0) ? stationsR.length : bridgeP.length)}`;
      }
      if (suggestionx <= 2.88 && 3.64 <= (2.88 * suggestionx)) {
         plus4 = (stationsR.length ^ bridgeP.length) == 64;
      }
      buildI &= ((indexY ? 5 : 1) << (Math.min(Math.abs(3), 4)));
       let feedback4 = true;
       let privilegeD = String.fromCharCode(99,97,108,108,108,95,104,95,56,57,0);
      for (let p = 0; p < 2; p++) {
          let placeholderC = String.fromCharCode(97,100,100,105,116,105,111,110,97,108,95,97,95,50,51,0);
          let rewindo: Array<any> = [535, 486, 15];
          let anytimeD = String.fromCharCode(106,95,54,50,95,114,111,98,105,110,0);
          let libjsijniprofilerp = 2;
         feedback4 = privilegeD.endsWith(`${feedback4}`);
         placeholderC = `${(String.fromCharCode(108,0) == anytimeD ? libjsijniprofilerp : anytimeD.length)}`;
         rewindo = [anytimeD.length & placeholderC.length];
         libjsijniprofilerp >>= Math.min(Math.abs(rewindo.length << (Math.min(anytimeD.length, 3))), 1);
      }
       let renewR: Map<any, any> = new Map([[String.fromCharCode(116,104,114,101,97,100,105,110,103,95,55,95,50,54,0),false ], [String.fromCharCode(122,95,50,95,116,114,97,110,115,99,101,105,118,101,114,115,0),true ], [String.fromCharCode(105,95,55,54,95,108,111,97,100,101,114,0),false ]]);
       let aboutA: Map<any, any> = new Map([[String.fromCharCode(100,95,51,95,121,100,97,121,0),102], [String.fromCharCode(109,111,99,107,105,110,103,95,118,95,52,53,0),708]]);
      for (let h = 0; h < 1; h++) {
          let proxyk = String.fromCharCode(111,112,101,110,103,108,95,49,95,49,48,48,0);
         renewR.set(proxyk, proxyk.length);
      }
          let gpay1 = 2.0;
          let manifest1: Map<any, any> = new Map([[String.fromCharCode(114,101,102,108,101,99,116,105,111,110,95,121,95,50,49,0),770], [String.fromCharCode(119,105,100,116,104,115,95,50,95,49,48,0),106], [String.fromCharCode(97,110,109,114,95,114,95,55,56,0),716]]);
         aboutA = new Map([[`${aboutA.size}`, parseInt(`${gpay1}`)]]);
         gpay1 /= Math.max(parseFloat(`${1}`), 1);
         manifest1 = new Map([[`${manifest1.size}`, 3 * manifest1.size]]);
      if (privilegeD.length == aboutA.size) {
         privilegeD = `${privilegeD.length - 3}`;
      }
      if (!feedback4) {
         privilegeD = "2";
      }
      mbbidh = [(button5.size & (feedback4 ? 5 : 1))];
      if (alternativeImg && x < alternativeImg?.length){

   let fastforwardg = buildI >= 5358085;
   do {
      buildI *= button5.size;
      if (fastforwardg) {
         break;
      }
   } while ((mbbidh.includes(buildI)) && fastforwardg);
      customV *= (String.fromCharCode(122,0) == sharek ? sharek.length : parseInt(`${wcopy_a36}`));
   let playT = 8236373 >= buildI;
   do {
       let goal2 = String.fromCharCode(99,95,57,56,95,102,108,97,99,100,97,116,97,0);
       let libyogaN = String.fromCharCode(121,95,56,57,95,114,111,108,108,0);
       let filledJ = String.fromCharCode(113,117,111,116,101,95,113,95,54,52,0);
          let usernameo = true;
          let referrert = 1;
          let k_titleO = 1;
         goal2 += "1";
         usernameo = !usernameo || 14 < k_titleO;
         referrert %= Math.max(1, referrert >> (Math.min(2, Math.abs(1))));
         k_titleO %= Math.max(2 >> (Math.min(1, Math.abs(referrert))), 1);
      if (libyogaN != String.fromCharCode(70,0)) {
         goal2 = `${libyogaN.length * goal2.length}`;
      }
         libyogaN = `${goal2.length & 3}`;
         libyogaN = `${filledJ.length >> (Math.min(Math.abs(3), 1))}`;
      for (let m = 0; m < 3; m++) {
         goal2 = `${filledJ.length % (Math.max(10, libyogaN.length))}`;
      }
      for (let r = 0; r < 1; r++) {
          let orientationO = String.fromCharCode(113,112,113,115,99,97,108,101,95,55,95,54,54,0);
          let orangec: Map<any, any> = new Map([[String.fromCharCode(118,95,52,50,95,101,110,99,108,111,115,105,110,103,0),String.fromCharCode(115,95,54,50,95,112,117,116,0)], [String.fromCharCode(111,95,52,55,95,99,97,110,111,110,105,99,97,108,0),String.fromCharCode(110,95,57,48,95,117,100,112,0)], [String.fromCharCode(122,95,50,95,100,111,110,116,0),String.fromCharCode(97,100,100,111,112,95,104,95,57,56,0)]]);
          let gradlewZ = false;
          let matchesE = String.fromCharCode(98,95,53,51,95,117,95,56,0);
         filledJ = `${goal2.length}`;
         orientationO += `${(2 + (gradlewZ ? 1 : 4))}`;
         orangec = new Map([[`${gradlewZ}`, (3 + (gradlewZ ? 3 : 5))]]);
         matchesE = `${orangec.size / (Math.max(4, matchesE.length))}`;
      }
          let updatesk = 0.0;
         goal2 = `${goal2.length}`;
         updatesk -= parseFloat(`${parseInt(`${updatesk}`) - 1}`);
      while (libyogaN != String.fromCharCode(101,0)) {
         filledJ += `${libyogaN.length}`;
         break;
      }
         filledJ = `${libyogaN.length & 3}`;
      buildI |= (String.fromCharCode(48,0) == sharek ? renderM.length : sharek.length);
      if (playT) {
         break;
      }
   } while ((3 < (clubg.length << (Math.min(Math.abs(5), 1))) && (buildI << (Math.min(Math.abs(5), 4))) < 3) && playT);
        return x + 1
      }
      return x
    })

   while (2 >= renderM.length) {
       let mutedC: Array<any> = [494, 917];
       let soundK = String.fromCharCode(121,95,54,54,95,98,105,115,101,99,116,0);
       let annerh = 3.0;
       let liblogger_ = 3;
         liblogger_ /= Math.max(3 | parseInt(`${annerh}`), 5);
      if (3.42 > annerh) {
          let minit_7G = String.fromCharCode(102,108,101,120,105,98,108,101,95,112,95,54,54,0);
          let lineE = 4;
          let nterstitialY = String.fromCharCode(116,97,105,108,115,95,116,95,51,48,0);
          let untickX = true;
          let libavformata = false;
         annerh *= (nterstitialY == String.fromCharCode(118,0) ? nterstitialY.length : lineE);
         minit_7G = `${((untickX ? 2 : 4))}`;
         lineE ^= ((untickX ? 4 : 3) * 3);
         libavformata = (untickX ? libavformata : !untickX);
      }
         soundK = `${liblogger_}`;
         annerh *= liblogger_;
          let configured = String.fromCharCode(98,101,103,117,110,95,122,95,55,57,0);
         soundK += `${configured.length}`;
          let fullr: Array<any> = [281, 217];
          let countryw = false;
          let eighteend = String.fromCharCode(103,114,111,119,105,110,103,95,53,95,51,52,0);
         soundK = `${(soundK == String.fromCharCode(48,0) ? soundK.length : eighteend.length)}`;
         fullr.push(3);
         countryw = fullr.length < 43 && countryw;
         eighteend += `${(fullr.length / (Math.max(3, (countryw ? 1 : 3))))}`;
      while ((mutedC.length - parseInt(`${annerh}`)) <= 1 && (parseInt(`${annerh}`) - mutedC.length) <= 1) {
         mutedC = [mutedC.length << (Math.min(Math.abs(3), 3))];
         break;
      }
      if (2 < (liblogger_ >> (Math.min(Math.abs(1), 1))) && (1 >> (Math.min(5, Math.abs(liblogger_)))) < 3) {
          let paginationh = 3;
         liblogger_ |= (soundK == String.fromCharCode(82,0) ? soundK.length : parseInt(`${annerh}`));
         paginationh ^= paginationh >> (Math.min(Math.abs(2), 2));
      }
          let catagoryY = String.fromCharCode(100,95,52,53,95,113,117,105,99,107,0);
          let mimoe = 3;
          let sortt = String.fromCharCode(102,105,108,108,105,110,95,106,95,57,57,0);
         liblogger_ /= Math.max(4, parseInt(`${annerh}`) - soundK.length);
         catagoryY += `${(sortt == String.fromCharCode(104,0) ? mimoe : sortt.length)}`;
         mimoe <<= Math.min(1, Math.abs((String.fromCharCode(122,0) == catagoryY ? catagoryY.length : mimoe)));
         mutedC.push(liblogger_);
       let encryptc = 1.0;
       let reward1 = 4.0;
         annerh += 1;
      clubg.push(parseInt(`${annerh}`));
      break;
   }
   while (2 >= renderM.length) {
      wcopy_a36 += parseInt(`${customV}`);
      break;
   }
   let bridgeV = pressure0 >= 9359912;
   do {
       let graphics1 = 2;
          let thailandu: Array<any> = [345, 252, 774];
         graphics1 /= Math.max(thailandu.length, 5);
      while (graphics1 > graphics1) {
         graphics1 <<= Math.min(3, Math.abs(graphics1 ^ graphics1));
         break;
      }
         graphics1 ^= graphics1 | 2;
      pressure0 <<= Math.min(Math.abs(1 / (Math.max(pressure0, 6))), 5);
      if (bridgeV) {
         break;
      }
   } while ((3 < renderM.length) && bridgeV);
    console.debug('using', initialList.current.at(0))

   let q_countf = 8783935.0 >= wcopy_a36;
   do {
       let crownF = String.fromCharCode(104,119,109,97,112,95,115,95,57,52,0);
      for (let s = 0; s < 3; s++) {
         crownF = `${crownF.length}`;
      }
         crownF += `${crownF.length}`;
          let videocommonX = false;
          let gemfilee = String.fromCharCode(99,111,110,118,101,114,116,105,110,103,95,115,95,52,56,0);
          let clubR = 0;
         crownF = `${crownF.length}`;
         videocommonX = gemfilee.length > 20;
         gemfilee = `${gemfilee.length / 3}`;
         clubR ^= gemfilee.length;
      wcopy_a36 *= parseInt(`${customV}`);
      if (q_countf) {
         break;
      }
   } while (q_countf && ((buildI % (Math.max(1, 9))) <= 2));
      wcopy_a36 *= parseInt(`${customV}`) & button5.size;
       let tickM = String.fromCharCode(116,104,117,109,98,115,117,112,95,114,95,53,54,0);
       let launcho = true;
       let countryF = 2;
         launcho = !launcho;
      for (let l = 0; l < 1; l++) {
          let mountingd = 1.0;
          let minivod4: Array<any> = [839, 700, 844];
          let inewarchdefaultso = String.fromCharCode(99,95,52,48,95,100,101,118,105,99,101,0);
          let textlayoutmanagera = true;
          let modalG = 4;
         launcho = tickM.endsWith(`${countryF}`);
         mountingd *= (parseFloat(`${parseInt(`${mountingd}`) << (Math.min(1, Math.abs((textlayoutmanagera ? 4 : 3))))}`));
         minivod4 = [minivod4.length & parseInt(`${mountingd}`)];
         inewarchdefaultso += "3";
         textlayoutmanagera = 51.62 <= mountingd;
         modalG += minivod4.length;
      }
      for (let s = 0; s < 1; s++) {
         countryF /= Math.max(((launcho ? 4 : 4)), 5);
      }
          let collection7 = 5.0;
          let anews3 = String.fromCharCode(121,95,50,95,99,111,108,111,110,0);
          let gradlew = String.fromCharCode(97,95,56,53,95,104,97,115,104,116,97,103,115,0);
         countryF /= Math.max(parseInt(`${collection7}`), 2);
         collection7 += gradlew.length << (Math.min(anews3.length, 5));
         anews3 = `${gradlew.length + anews3.length}`;
      for (let o = 0; o < 1; o++) {
         countryF += ((launcho ? 3 : 4) % 1);
      }
         countryF >>= Math.min(tickM.length, 3);
      while (launcho) {
         countryF -= ((launcho ? 2 : 1));
         break;
      }
          let circle5 = String.fromCharCode(99,95,49,54,95,99,108,111,115,101,115,116,0);
         tickM += `${1 | circle5.length}`;
         launcho = tickM.length == 44 || launcho;
      button5 = new Map([[`${clubg.length}`, (String.fromCharCode(112,0) == tickM ? tickM.length : clubg.length)]]);
    return initialList.current.at(0)
  }

  const handleOnError = () => {
       let tailA: Array<any> = [String.fromCharCode(100,111,99,108,105,115,116,115,95,53,95,49,52,0), String.fromCharCode(108,115,98,115,95,102,95,49,49,0), String.fromCharCode(100,95,55,53,95,99,97,108,108,111,99,0)];
    let inactive0 = 4.0;
    let utilsG = 5.0;
    let temperatureb = String.fromCharCode(109,95,54,95,116,97,110,0);
    let carouselD = String.fromCharCode(105,95,56,56,0);
    let productm: Array<any> = [String.fromCharCode(109,95,48,95,112,111,116,105,115,105,111,110,0), String.fromCharCode(115,105,110,103,108,101,116,97,98,108,101,95,119,95,51,48,0), String.fromCharCode(98,97,116,99,104,105,110,103,95,53,95,51,54,0)];
    let crownp = 0;
    let libjsijniprofilerC: Map<any, any> = new Map([[String.fromCharCode(114,117,108,101,95,50,95,54,51,0),false ], [String.fromCharCode(101,118,101,110,97,118,103,95,52,95,50,55,0),true ]]);
    let humidityK = String.fromCharCode(118,105,115,117,97,108,105,122,97,116,105,111,110,95,110,95,56,49,0);
    let pausej = 5.0;
    let stepf = 5;
    let scheduleS = false;
      inactive0 *= parseFloat(`${3}`);
       let topon3 = String.fromCharCode(98,105,108,105,110,95,118,95,51,0);
       let libfabricjnia: Array<any> = [492, 431, 774];
          let x_countu = String.fromCharCode(101,120,104,97,117,115,116,105,118,101,95,105,95,56,52,0);
          let imagemanagers: Array<any> = [String.fromCharCode(116,95,54,48,95,99,116,114,0), String.fromCharCode(118,95,56,56,95,105,110,105,0)];
         topon3 = "3";
         x_countu = `${(x_countu == String.fromCharCode(82,0) ? imagemanagers.length : x_countu.length)}`;
         imagemanagers.push(1);
          let graphics3 = false;
          let fadfdeebbbfdabbbabdadfaaddaav: Array<any> = [917, 581];
         libfabricjnia = [fadfdeebbbfdabbbabdadfaaddaav.length];
      for (let e = 0; e < 3; e++) {
         libfabricjnia.push(topon3.length / (Math.max(10, libfabricjnia.length)));
      }
          let scrollviewK = String.fromCharCode(117,95,57,57,95,98,101,116,116,101,114,0);
          let sans_ = false;
         libfabricjnia = [(String.fromCharCode(122,0) == scrollviewK ? (sans_ ? 1 : 3) : scrollviewK.length)];
       let libreact_: Array<any> = [73, 421, 268];
       let holderb: Array<any> = [142, 902, 714];
      let down0 = 9546217 <= libfabricjnia.length;
      do {
         libfabricjnia.push(3 | holderb.length);
         if (down0) {
            break;
         }
      } while (down0 && (1 == (libreact_.length * libfabricjnia.length) && 1 == (libfabricjnia.length * 1)));
      utilsG *= parseFloat(`${parseInt(`${utilsG}`)}`);
      utilsG += parseFloat(`${3 + parseInt(`${utilsG}`)}`);
   while (utilsG <= inactive0) {
      utilsG *= (parseFloat(`${String.fromCharCode(51,0) == carouselD ? parseInt(`${utilsG}`) : carouselD.length}`));
      break;
   }
       let sentry5 = String.fromCharCode(105,109,112,95,116,95,51,49,0);
      while (!sentry5.startsWith(`${sentry5.length}`)) {
          let countdowns = true;
         sentry5 += `${sentry5.length}`;
         break;
      }
         sentry5 += `${sentry5.length + 2}`;
         sentry5 += `${sentry5.length | sentry5.length}`;
      productm = [parseInt(`${inactive0}`)];
   let themeb = 7935418 >= productm.length;
   do {
       let benefit8 = String.fromCharCode(102,114,97,109,101,95,51,95,56,49,0);
       let rewardy: Map<any, any> = new Map([[String.fromCharCode(102,111,110,116,115,105,122,101,95,108,95,55,48,0),false ], [String.fromCharCode(115,99,97,108,97,98,108,101,95,98,95,56,55,0),true ]]);
       let emptyN = String.fromCharCode(112,114,111,106,101,99,116,105,111,110,115,95,118,95,51,56,0);
       let downloadingw: Array<any> = [417, 360, 818];
      if (emptyN.length > 4) {
         emptyN += `${2 - benefit8.length}`;
      }
          let textU: Array<any> = [423, 907];
          let configurev = 1;
          let rightN = String.fromCharCode(99,97,110,99,101,108,95,101,95,57,49,0);
         downloadingw.push(configurev >> (Math.min(Math.abs(1), 4)));
         textU.push(2 - textU.length);
         configurev >>= Math.min(textU.length, 1);
         rightN += `${1 >> (Math.min(5, textU.length))}`;
      if (1 == (benefit8.length << (Math.min(Math.abs(1), 3))) && 1 == (benefit8.length << (Math.min(4, Math.abs(rewardy.size))))) {
         benefit8 += `${rewardy.size % (Math.max(1, 4))}`;
      }
         downloadingw = [emptyN.length ^ downloadingw.length];
      if (!benefit8.endsWith(`${downloadingw.length}`)) {
          let pressurem = String.fromCharCode(98,105,110,111,109,105,97,108,95,117,95,51,50,0);
          let alert6: Array<any> = [451, 357];
          let countdownm = false;
         benefit8 = `${((countdownm ? 5 : 4))}`;
         pressurem += `${pressurem.length}`;
         alert6 = [1 ^ alert6.length];
         countdownm = String.fromCharCode(86,0) == pressurem;
      }
      while (1 >= benefit8.length) {
          let flippery: Map<any, any> = new Map([[String.fromCharCode(97,95,55,49,95,115,113,108,105,116,101,99,104,97,110,103,101,115,101,116,0),false ], [String.fromCharCode(100,105,115,112,97,116,99,104,95,106,95,54,56,0),false ]]);
          let auto_4wQ = 2.0;
          let mimoi = true;
         rewardy.set(`${auto_4wQ}`, parseInt(`${auto_4wQ}`));
         flippery = new Map([[`${flippery.size}`, 1]]);
         mimoi = flippery.size > 27;
         break;
      }
         rewardy.set(benefit8, (benefit8 == String.fromCharCode(116,0) ? rewardy.size : benefit8.length));
         emptyN += "1";
       let classesc = String.fromCharCode(98,112,114,105,110,116,95,102,95,50,56,0);
      let playercommonT = emptyN.length >= 5312574;
      do {
          let borderlessv = String.fromCharCode(111,95,53,53,95,105,115,111,112,101,110,113,117,111,116,101,0);
         emptyN = `${3 >> (Math.min(2, Math.abs(rewardy.size)))}`;
         borderlessv += `${borderlessv.length}`;
         if (playercommonT) {
            break;
         }
      } while (((4 << (Math.min(2, downloadingw.length))) < 5) && playercommonT);
      while (!benefit8.endsWith(`${emptyN.length}`)) {
          let applicationY = 0.0;
          let usernameR: Map<any, any> = new Map([[String.fromCharCode(104,95,49,95,115,105,109,105,108,97,114,0),String.fromCharCode(119,95,50,52,95,115,116,115,99,0)], [String.fromCharCode(98,95,50,56,95,115,119,105,116,99,104,97,98,108,101,0),String.fromCharCode(114,101,97,100,108,105,110,101,95,103,95,53,51,0)], [String.fromCharCode(114,101,112,108,97,99,101,95,114,95,53,49,0),String.fromCharCode(115,97,118,101,100,95,117,95,53,57,0)]]);
         benefit8 += `${(String.fromCharCode(88,0) == emptyN ? rewardy.size : emptyN.length)}`;
         applicationY -= parseFloat(`${parseInt(`${applicationY}`) / (Math.max(usernameR.size, 9))}`);
         usernameR = new Map([[`${usernameR.size}`, usernameR.size << (Math.min(1, Math.abs(parseInt(`${applicationY}`))))]]);
         break;
      }
         emptyN += `${rewardy.size * classesc.length}`;
      productm.push(3);
      if (themeb) {
         break;
      }
   } while (themeb && (3 >= productm.length));
       let countdownmy = String.fromCharCode(99,112,117,102,108,97,103,115,95,56,95,57,50,0);
       let pagei = true;
       let with_6F = String.fromCharCode(120,95,49,52,95,99,104,111,111,115,101,0);
      if (with_6F == countdownmy) {
          let storel = 3.0;
          let xadsdkt = String.fromCharCode(109,95,48,95,109,105,120,105,110,115,0);
          let smally = true;
          let yinit_40 = false;
          let homeW = true;
         countdownmy = `${((smally ? 2 : 1) + (pagei ? 3 : 1))}`;
         storel -= parseInt(`${storel}`) - 1;
         xadsdkt = `${2 * xadsdkt.length}`;
         smally = String.fromCharCode(55,0) == xadsdkt;
         yinit_40 = (homeW ? !yinit_40 : !homeW);
      }
      while (with_6F.length <= 3) {
          let handlerm = 2.0;
          let wind5 = String.fromCharCode(103,95,54,54,95,109,111,100,101,99,111,115,116,115,0);
          let sanse = String.fromCharCode(99,95,57,48,95,114,101,99,111,110,102,105,103,117,114,101,0);
         pagei = parseFloat(`${sanse.length}`) <= handlerm;
         handlerm *= (parseFloat(`${String.fromCharCode(79,0) == wind5 ? wind5.length : wind5.length}`));
         break;
      }
          let libreactnativejni7 = String.fromCharCode(106,95,49,48,95,108,111,116,116,105,101,108,111,97,100,101,114,0);
         with_6F += `${(String.fromCharCode(98,0) == with_6F ? countdownmy.length : with_6F.length)}`;
         libreactnativejni7 = `${3 % (Math.max(3, libreactnativejni7.length))}`;
         pagei = !pagei && countdownmy.length >= 41;
      let indicatoro = countdownmy.length >= 6319867;
      do {
          let roomA = String.fromCharCode(114,95,49,50,95,97,116,114,97,99,112,0);
          let hiadj = String.fromCharCode(117,95,54,56,95,101,120,97,109,105,110,101,0);
          let imagemanagern: Array<any> = [400, 343, 509];
          let connection_: Map<any, any> = new Map([[String.fromCharCode(103,95,50,55,95,115,104,97,114,112,110,101,115,115,0),665], [String.fromCharCode(117,95,49,48,48,95,114,101,118,111,107,105,110,103,0),530]]);
         countdownmy += `${imagemanagern.length}`;
         roomA = `${roomA.length & hiadj.length}`;
         hiadj = `${(roomA == String.fromCharCode(82,0) ? roomA.length : hiadj.length)}`;
         imagemanagern = [(roomA == String.fromCharCode(83,0) ? roomA.length : hiadj.length)];
         connection_ = new Map([[`${connection_.size}`, 2 >> (Math.min(5, Math.abs(connection_.size)))]]);
         if (indicatoro) {
            break;
         }
      } while (indicatoro && (2 <= countdownmy.length));
      let filterX = with_6F == String.fromCharCode(53,54,117,0);
      do {
         with_6F = `${((pagei ? 5 : 3))}`;
         if (filterX) {
            break;
         }
      } while (filterX && (pagei));
      for (let h = 0; h < 1; h++) {
         with_6F += `${((pagei ? 1 : 2) << (Math.min(Math.abs(3), 4)))}`;
      }
          let source_: Map<any, any> = new Map([[String.fromCharCode(104,105,103,104,108,105,103,116,101,100,95,100,95,50,57,0),22], [String.fromCharCode(97,117,116,104,105,110,102,111,95,111,95,55,52,0),754]]);
          let analyticx = false;
         countdownmy = "3";
         source_ = new Map([[`${source_.size}`, (1 << (Math.min(4, Math.abs((analyticx ? 5 : 5)))))]]);
         analyticx = !analyticx;
          let moviesO: Map<any, any> = new Map([[String.fromCharCode(120,95,57,50,0),200], [String.fromCharCode(114,95,50,48,95,115,116,114,105,112,112,101,100,0),982], [String.fromCharCode(105,95,49,54,95,119,101,105,0),944]]);
         countdownmy += `${(String.fromCharCode(121,0) == countdownmy ? moviesO.size : countdownmy.length)}`;
      carouselD = `${crownp / 1}`;
       let backgroundn = 4.0;
       let grayl: Array<any> = [76, 757];
       let moonr = 2.0;
      if (grayl.includes(moonr)) {
          let fillT = true;
          let libloggerf = String.fromCharCode(102,111,114,119,97,114,100,95,106,95,50,52,0);
          let pnewarchdefaultsx: Map<any, any> = new Map([[String.fromCharCode(117,95,50,48,95,109,100,97,116,0),71], [String.fromCharCode(101,110,100,120,95,114,95,54,53,0),507], [String.fromCharCode(116,95,56,52,95,98,105,116,97,108,108,111,99,0),320]]);
         grayl.push(grayl.length >> (Math.min(Math.abs(1), 3)));
         fillT = pnewarchdefaultsx.size < 67;
         libloggerf += `${pnewarchdefaultsx.size}`;
      }
      if (grayl.length >= 4) {
         moonr += parseFloat(`${grayl.length ^ 1}`);
      }
      if ((2.75 * backgroundn) <= 1.90) {
          let verticalk = String.fromCharCode(120,95,54,50,95,97,117,100,105,111,105,110,116,101,114,108,101,97,118,101,0);
          let mbbidE: Map<any, any> = new Map([[String.fromCharCode(108,95,53,53,95,101,115,115,105,111,110,0),628], [String.fromCharCode(107,105,110,100,115,95,102,95,52,56,0),44], [String.fromCharCode(102,97,114,101,110,100,95,108,95,49,54,0),628]]);
          let rncoreq = 2.0;
          let referrerD: Array<any> = [878, 585, 806];
         backgroundn /= Math.max(parseFloat(`${grayl.length}`), 1);
         verticalk += `${parseInt(`${rncoreq}`)}`;
         mbbidE.set(verticalk, (verticalk == String.fromCharCode(53,0) ? referrerD.length : verticalk.length));
         rncoreq += parseInt(`${rncoreq}`);
         referrerD.push(3 % (Math.max(4, parseInt(`${rncoreq}`))));
      }
          let philippinesy: Map<any, any> = new Map([[String.fromCharCode(104,95,51,57,95,105,118,97,114,115,0),252], [String.fromCharCode(115,112,97,119,110,97,98,108,101,95,112,95,54,54,0),525], [String.fromCharCode(113,95,51,53,95,116,97,112,112,105,110,103,0),1]]);
         backgroundn /= Math.max(2, parseFloat(`${philippinesy.size}`));
      let filterm = grayl.length >= 9254679;
      do {
          let modeL: Map<any, any> = new Map([[String.fromCharCode(115,117,98,109,111,100,117,108,101,95,118,95,56,52,0),804], [String.fromCharCode(116,97,98,108,101,112,114,105,110,116,95,103,95,56,55,0),659], [String.fromCharCode(112,97,114,97,95,109,95,56,0),154]]);
          let proxyS = String.fromCharCode(99,95,57,54,95,98,97,99,107,116,114,97,99,101,0);
          let usernameE = false;
          let soundp = String.fromCharCode(106,95,54,95,117,110,100,101,114,102,108,111,119,0);
         grayl = [modeL.size >> (Math.min(Math.abs(2), 4))];
         modeL.set(`${usernameE}`, (proxyS == String.fromCharCode(57,0) ? (usernameE ? 3 : 5) : proxyS.length));
         soundp = `${((usernameE ? 2 : 3) >> (Math.min(Math.abs(1), 3)))}`;
         if (filterm) {
            break;
         }
      } while (((1.57 / (Math.max(2, moonr))) > 3.22) && filterm);
      if (backgroundn < parseFloat(`${grayl.length}`)) {
         grayl.push(1 ^ grayl.length);
      }
       let middleH = 3.0;
      let fadfdeebbbfdabbbabdadfaaddaas = moonr >= 8311733.0;
      do {
         moonr += parseFloat(`${parseInt(`${middleH}`)}`);
         if (fadfdeebbbfdabbbabdadfaaddaas) {
            break;
         }
      } while (fadfdeebbbfdabbbabdadfaaddaas && (2.62 <= (5.79 + moonr) || (2 - grayl.length) <= 1));
       let minis = String.fromCharCode(113,95,49,50,95,109,111,99,107,105,110,103,0);
       let moviesC = String.fromCharCode(109,105,110,115,95,110,95,57,53,0);
      utilsG /= Math.max(parseFloat(`${1}`), 5);
       let tempp = true;
       let darkn: Array<any> = [904, 777, 832];
          let bridgeH = 1.0;
          let mbnativeadvancede = false;
          let morey = String.fromCharCode(109,95,55,57,95,109,111,100,101,108,0);
         tempp = !tempp;
         bridgeH /= Math.max(2, parseFloat(`${morey.length >> (Math.min(Math.abs(2), 5))}`));
         mbnativeadvancede = morey.length == 78;
      while (darkn.length <= 5) {
         darkn = [2];
         break;
      }
         tempp = darkn.includes(tempp);
      while (2 <= darkn.length && (darkn.length % 2) <= 2) {
         tempp = darkn.includes(tempp);
         break;
      }
      for (let l = 0; l < 2; l++) {
         darkn = [((tempp ? 4 : 2) >> (Math.min(darkn.length, 2)))];
      }
         darkn.push(darkn.length);
      carouselD += `${tailA.length + productm.length}`;
       let calendart = String.fromCharCode(114,101,97,108,116,105,109,101,95,108,95,55,52,0);
       let fieldw = String.fromCharCode(114,95,50,51,95,105,109,112,108,105,101,115,0);
         calendart = `${(fieldw == String.fromCharCode(70,0) ? calendart.length : fieldw.length)}`;
      while (3 == fieldw.length) {
         calendart += `${fieldw.length}`;
         break;
      }
         fieldw = `${2 * calendart.length}`;
          let bdxadsdkh: Array<any> = [958, 140, 472];
          let tickV = 2;
          let page5 = false;
         fieldw += `${tickV}`;
         bdxadsdkh = [2];
         tickV &= 3 << (Math.min(5, bdxadsdkh.length));
      while (calendart == fieldw) {
         fieldw = `${(String.fromCharCode(87,0) == calendart ? fieldw.length : calendart.length)}`;
         break;
      }
       let tramini1 = String.fromCharCode(99,104,97,112,95,116,95,56,52,0);
       let plashO = String.fromCharCode(109,99,100,99,95,49,95,53,56,0);
      productm = [productm.length / 2];
      temperatureb = `${productm.length ^ 3}`;
      productm.push(libjsijniprofilerC.size & 1);
   for (let k = 0; k < 1; k++) {
      humidityK = "2";
   }
       let history3 = 0.0;
       let animationsL = String.fromCharCode(99,111,117,108,100,95,97,95,51,48,0);
       let specp = String.fromCharCode(102,95,57,95,102,117,108,108,121,0);
      let libmapbufferjniZ = specp.length >= 5648480;
      do {
         specp = `${(animationsL == String.fromCharCode(70,0) ? parseInt(`${history3}`) : animationsL.length)}`;
         if (libmapbufferjniZ) {
            break;
         }
      } while ((!animationsL.startsWith(`${specp.length}`)) && libmapbufferjniZ);
         history3 -= animationsL.length;
      let watchq = 6054005 <= specp.length;
      do {
         specp = `${1 ^ animationsL.length}`;
         if (watchq) {
            break;
         }
      } while ((animationsL == specp) && watchq);
      while ((history3 + 4.30) >= 5.56) {
          let qaag8 = 4;
          let temperatureR = 0.0;
          let d_viewO = 2.0;
          let foundr = String.fromCharCode(107,95,49,49,95,110,101,103,97,116,101,100,0);
          let auto_8wA: Map<any, any> = new Map([[String.fromCharCode(115,97,116,117,114,97,116,101,95,111,95,51,49,0),537], [String.fromCharCode(110,111,98,111,100,121,95,98,95,51,50,0),710]]);
         specp = `${animationsL.length & 3}`;
         qaag8 |= qaag8;
         temperatureR /= Math.max(5, 1);
         d_viewO += qaag8;
         foundr += `${3 & auto_8wA.size}`;
         auto_8wA = new Map([[`${d_viewO}`, 1]]);
         break;
      }
         animationsL += `${animationsL.length}`;
          let main_k5: Map<any, any> = new Map([[String.fromCharCode(111,95,57,49,95,121,111,117,114,0),426], [String.fromCharCode(120,95,57,54,95,106,117,108,105,97,110,0),271], [String.fromCharCode(115,95,54,52,95,115,105,103,110,97,108,105,110,103,0),510]]);
          let search5 = String.fromCharCode(116,101,120,101,108,95,51,95,52,51,0);
         history3 *= 1 ^ animationsL.length;
         main_k5 = new Map([[`${main_k5.size}`, search5.length]]);
         search5 += `${main_k5.size}`;
          let overL = String.fromCharCode(106,95,57,50,95,115,121,115,105,110,102,111,0);
          let forwardR = String.fromCharCode(99,111,108,108,101,116,105,111,110,95,51,95,57,52,0);
          let floaterf = 3.0;
         history3 /= Math.max(4, parseInt(`${history3}`) % 2);
         overL = "2";
         forwardR = `${1 & parseInt(`${floaterf}`)}`;
         floaterf -= (String.fromCharCode(107,0) == overL ? overL.length : forwardR.length);
      let indicatorC = String.fromCharCode(56,106,111,120,113,54,122,0) == specp;
      do {
          let default_twr = String.fromCharCode(119,95,57,51,95,100,101,99,111,100,101,112,108,97,110,101,0);
          let plashM = String.fromCharCode(116,95,56,50,95,116,101,120,105,112,111,100,0);
          let left0: Array<any> = [64, 580];
         specp += `${specp.length}`;
         default_twr += `${left0.length}`;
         plashM += `${(String.fromCharCode(78,0) == default_twr ? default_twr.length : plashM.length)}`;
         left0 = [left0.length];
         if (indicatorC) {
            break;
         }
      } while ((4 == specp.length) && indicatorC);
         specp = `${3 * animationsL.length}`;
      inactive0 /= Math.max(parseFloat(`${tailA.length ^ 3}`), 5);
   let downloaded6 = String.fromCharCode(113,105,55,0) == humidityK;
   do {
      humidityK += `${carouselD.length}`;
      if (downloaded6) {
         break;
      }
   } while ((tailA.length > humidityK.length) && downloaded6);

    setImgUrl(useNextImage())
  }

  if (error) { // when all url error, use the default placeholder image
    return (
      <View style={[{ backgroundColor: colors.loading, ...styles.loadingCard}, imageProp.style]}>
         <LoadingImage  width={20}  height={20}/>
      </View>
    )
  }

  if (useFastImage === true || Platform.OS === "android"){
    if (typeof imageProp.source == 'number'){ // if source={require(...)}
    return <FastImage {...imageProp as FastImageProps}/>
    } else {
      return <FastImage key={current.toString() + imgUrl} {...imageProp as FastImageProps} source={{uri: imgUrl}} onError={handleOnError}/>
    }
  } else {
    if (typeof imageProp.source == 'number'){ // if source={require(...)}
      return <Image {...imageProp as yys_Libturbomodulejsijni}/>
    } else { // if source={{uri:...}}
      return <Image key={current.toString() + imgUrl} {...imageProp as yys_Libturbomodulejsijni} source={{...(imageProp.source as ImageURISource), uri: imgUrl}} onError={handleOnError}/>
    }
  }
}

const styles = StyleSheet.create({
  loadingCard: {
    marginRight: 10,
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    borderRadius: 10,
},
})

export default customFastImage;