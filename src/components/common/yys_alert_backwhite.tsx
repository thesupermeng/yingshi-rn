import { useEffect, useRef, useState } from "react";
import { Image, ImageURISource, LogBox, StyleSheet, View } from "react-native";
import { Platform } from "react-native";
import { ImageProps, ViewProps } from "react-native"
import FastImage, { FastImageProps } from "react-native-fast-image";
import { LoadingImgSvg } from '@static';
import { useTheme } from "@react-navigation/native";


type yysIconrightorangeLogin = ImageProps & { source: ImageURISource }

type yysWith = FastImageProps & { useFastImage?: boolean, alternativeImg?: string[] }

LogBox.ignoreLogs([`ReactImageView: Image source "null" doesn't exist`])

const customFastImage = ({ useFastImage = false, alternativeImg, ...imageProp }: yysWith) => {
  const [imgUrl, setImgUrl] = useState(typeof imageProp.source == 'number' ? undefined : imageProp.source?.uri)
  const initialList = useRef(alternativeImg?.filter(x => x !== ''))
  const [error, setError] = useState(false)
  const [current, setCurrent] = useState(0)
  const { colors } = useTheme();


  const useNextImage = () => {
       let iconcurrentmatchM = 0.0;
    let libreacti: Array<any> = [48, 755];
    let banner4: Array<any> = [344, 152];
    let imagewatchliven: Array<any> = [954, 948];
    let description_fY: Array<any> = [941, 163, 104];
    let servicex = String.fromCharCode(117,95,50,48,95,105,115,114,101,97,100,111,110,108,121,0);
    let libapminsightbn = String.fromCharCode(107,95,52,54,95,115,121,110,99,104,114,111,110,105,122,101,0);
    let playershirtq = String.fromCharCode(116,95,56,49,0);
    let arear = String.fromCharCode(115,95,49,51,95,97,108,101,114,116,0);
    let singlek = 2.0;
    let pushy = String.fromCharCode(107,95,53,55,95,115,116,97,103,101,0);
    let benefit1 = String.fromCharCode(103,117,105,100,101,95,106,95,53,0);
   while (libapminsightbn.endsWith(`${imagewatchliven.length}`)) {
       let shrunkz = String.fromCharCode(101,95,54,52,95,115,97,109,112,108,101,114,0);
      let mbbannerV = 6132813 <= shrunkz.length;
      do {
          let vnewsshareK = 1;
          let stats7: Map<any, any> = new Map([[String.fromCharCode(112,97,114,115,101,114,115,95,107,95,51,49,0),String.fromCharCode(118,97,108,117,108,101,95,98,95,53,53,0)], [String.fromCharCode(100,114,97,110,100,95,48,95,57,0),String.fromCharCode(97,95,52,54,95,118,97,114,105,97,116,105,111,110,0)]]);
          let material_: Map<any, any> = new Map([[String.fromCharCode(112,95,52,57,95,119,97,107,101,0),229], [String.fromCharCode(101,95,52,55,95,116,104,114,101,115,104,111,108,100,115,0),370], [String.fromCharCode(109,115,101,120,95,105,95,50,54,0),238]]);
          let expiredK = 1.0;
          let tempnodataf = String.fromCharCode(97,95,56,51,95,99,111,110,102,108,105,99,116,0);
         shrunkz = "2";
         vnewsshareK <<= Math.min(3, Math.abs(2 % (Math.max(7, material_.size))));
         stats7.set(tempnodataf, 1 * parseInt(`${expiredK}`));
         material_.set(`${vnewsshareK}`, vnewsshareK & 3);
         expiredK *= material_.size;
         tempnodataf = "2";
         if (mbbannerV) {
            break;
         }
      } while (mbbannerV && (shrunkz.length <= 1));
      if (shrunkz == String.fromCharCode(101,0)) {
          let vietnamJ = String.fromCharCode(114,116,109,112,100,104,95,101,95,56,51,0);
          let questI = 4.0;
          let singleN = 1;
         shrunkz = `${vietnamJ.length}`;
         vietnamJ = `${parseInt(`${questI}`)}`;
         questI /= Math.max(1, parseFloat(`${parseInt(`${questI}`)}`));
         singleN <<= Math.min(5, Math.abs(singleN >> (Math.min(4, Math.abs(2)))));
      }
      if (shrunkz == String.fromCharCode(71,0)) {
         shrunkz = `${shrunkz.length}`;
      }
      libapminsightbn += `${parseInt(`${iconcurrentmatchM}`)}`;
      break;
   }
      playershirtq += `${3 * arear.length}`;
   if (2 > arear.length) {
       let upgradec = String.fromCharCode(100,101,99,107,108,105,110,107,95,104,95,56,57,0);
       let google6: Map<any, any> = new Map([[String.fromCharCode(118,95,57,49,95,112,108,97,99,101,104,111,108,100,101,114,0),false ], [String.fromCharCode(120,95,50,53,95,114,101,99,111,103,110,105,122,101,114,115,0),true ], [String.fromCharCode(115,97,102,97,114,121,95,53,95,54,50,0),false ]]);
       let penaltyl = String.fromCharCode(100,101,116,101,99,116,95,50,95,52,51,0);
       let libavutile = true;
       let smallbrightness3 = 2.0;
       let cornerkick3 = 4.0;
       let libhermesU = 2.0;
      while (libavutile) {
         smallbrightness3 -= parseFloat(`${google6.size * parseInt(`${cornerkick3}`)}`);
         break;
      }
      for (let q = 0; q < 1; q++) {
          let redcirclebgi = String.fromCharCode(115,119,105,116,99,104,95,56,95,55,52,0);
          let shootyesgoalN = 1;
         upgradec = `${parseInt(`${cornerkick3}`)}`;
         redcirclebgi += `${(redcirclebgi == String.fromCharCode(51,0) ? redcirclebgi.length : shootyesgoalN)}`;
         shootyesgoalN <<= Math.min(3, Math.abs(shootyesgoalN / (Math.max(redcirclebgi.length, 10))));
      }
         smallbrightness3 /= Math.max(parseFloat(`${google6.size}`), 1);
         libavutile = 70.84 == libhermesU && !libavutile;
      if (4.17 > (smallbrightness3 / 3.80) && 4.82 > (3.80 * libhermesU)) {
         smallbrightness3 += parseFloat(`${2}`);
      }
      for (let b = 0; b < 2; b++) {
          let playercommono = String.fromCharCode(121,95,51,54,95,111,117,116,113,117,101,117,101,0);
          let yellowvideolives = 0;
         upgradec = `${upgradec.length}`;
         playercommono += `${playercommono.length}`;
         yellowvideolives >>= Math.min(Math.abs(playercommono.length + yellowvideolives), 3);
      }
      let arrowselectdownL = upgradec == String.fromCharCode(119,98,101,95,49,95,55,0);
      do {
          let whistleF = 2;
          let basketballdetailsbgC: Map<any, any> = new Map([[String.fromCharCode(112,104,111,110,101,98,111,111,107,95,55,95,48,0),669], [String.fromCharCode(116,105,109,101,115,95,105,95,52,50,0),362], [String.fromCharCode(109,111,118,101,116,111,95,104,95,50,49,0),336]]);
         upgradec += `${1 & parseInt(`${libhermesU}`)}`;
         whistleF >>= Math.min(Math.abs(basketballdetailsbgC.size), 5);
         basketballdetailsbgC = new Map([[`${basketballdetailsbgC.size}`, basketballdetailsbgC.size | whistleF]]);
         if (arrowselectdownL) {
            break;
         }
      } while (arrowselectdownL && (!upgradec.endsWith(`${cornerkick3}`)));
          let suboutC = 0.0;
         libavutile = cornerkick3 < 17.23 || google6.size < 13;
         suboutC += parseFloat(`${parseInt(`${suboutC}`) ^ 3}`);
          let aboutc: Array<any> = [607, 752];
         upgradec = "1";
         aboutc.push(aboutc.length - aboutc.length);
          let modelsf = String.fromCharCode(110,105,100,115,95,99,95,52,48,0);
          let whiteticki: Array<any> = [606, 325, 368];
          let textlayoutmanagerq = String.fromCharCode(99,104,117,110,107,95,103,95,52,53,0);
         libhermesU += whiteticki.length * upgradec.length;
         modelsf += `${modelsf.length}`;
         whiteticki = [textlayoutmanagerq.length * modelsf.length];
         textlayoutmanagerq += `${modelsf.length}`;
      let template_aE = libavutile ? !libavutile : libavutile;
      do {
          let libavcodecx = String.fromCharCode(120,95,51,49,95,115,111,99,97,110,116,115,101,110,100,109,111,114,101,0);
          let questiconA = String.fromCharCode(115,95,54,55,95,115,117,112,101,114,0);
          let launchZ: Array<any> = [256, 691];
          let privilege5 = String.fromCharCode(99,100,108,109,115,95,49,95,53,52,0);
         libavutile = 75 == penaltyl.length && 75 == privilege5.length;
         libavcodecx += `${libavcodecx.length + 2}`;
         questiconA = `${(String.fromCharCode(79,0) == questiconA ? questiconA.length : launchZ.length)}`;
         launchZ = [libavcodecx.length];
         privilege5 = `${launchZ.length}`;
         if (template_aE) {
            break;
         }
      } while (((2 | google6.size) > 5) && template_aE);
      if (penaltyl.length < google6.size) {
          let editw: Map<any, any> = new Map([[String.fromCharCode(115,95,54,51,95,99,110,111,110,99,101,0),867], [String.fromCharCode(116,111,114,101,100,95,55,95,49,51,0),143]]);
          let smallorangemant = String.fromCharCode(103,95,49,49,95,98,105,116,101,120,97,99,116,0);
          let volumez = String.fromCharCode(100,95,52,49,95,114,101,115,101,116,0);
         google6 = new Map([[`${editw.size}`, editw.size % (Math.max(smallorangemant.length, 3))]]);
         smallorangemant = `${volumez.length << (Math.min(1, volumez.length))}`;
      }
      for (let p = 0; p < 2; p++) {
         libavutile = cornerkick3 >= 86.21;
      }
      if (5 < upgradec.length) {
         upgradec = "1";
      }
      arear = `${banner4.length / (Math.max(7, upgradec.length))}`;
   }

    initialList.current = initialList.current?.filter(x => x !== imgUrl)

       let footballH = false;
      let redscoreballo = footballH ? !footballH : footballH;
      do {
         footballH = !footballH;
         if (redscoreballo) {
            break;
         }
      } while ((footballH) && redscoreballo);
         footballH = (footballH ? footballH : !footballH);
         footballH = footballH || footballH;
      iconcurrentmatchM -= parseFloat(`${1}`);
       let floatero = 3.0;
      for (let j = 0; j < 3; j++) {
          let stations_: Array<any> = [53, 192];
          let rootJ: Array<any> = [942, 936, 929];
          let iconmoreF = String.fromCharCode(101,120,99,108,117,115,105,118,101,95,109,95,55,0);
          let i_lock3 = 2.0;
          let libavformatq = String.fromCharCode(106,95,49,56,95,122,111,111,109,0);
         floatero /= Math.max(3, parseFloat(`${parseInt(`${i_lock3}`)}`));
         stations_.push(iconmoreF.length * 2);
         rootJ = [rootJ.length & iconmoreF.length];
         i_lock3 /= Math.max(stations_.length, 2);
         libavformatq += `${(iconmoreF == String.fromCharCode(117,0) ? iconmoreF.length : rootJ.length)}`;
      }
      while ((2.39 / (Math.max(6, floatero))) < 2.10) {
          let scorepopsoundR = 4.0;
          let time_nw: Array<any> = [663, 15];
          let latnb = String.fromCharCode(101,95,57,51,95,100,111,117,98,108,101,0);
          let mbnativeadvancedy = String.fromCharCode(121,95,50,57,95,99,111,109,109,97,0);
         floatero -= parseFloat(`${latnb.length | time_nw.length}`);
         scorepopsoundR += parseInt(`${scorepopsoundR}`);
         time_nw = [1 << (Math.min(2, Math.abs(parseInt(`${scorepopsoundR}`))))];
         latnb += `${3 >> (Math.min(3, mbnativeadvancedy.length))}`;
         mbnativeadvancedy = `${(mbnativeadvancedy == String.fromCharCode(81,0) ? mbnativeadvancedy.length : parseInt(`${scorepopsoundR}`))}`;
         break;
      }
      if ((3.57 * floatero) < 3.37) {
          let main_o5: Map<any, any> = new Map([[String.fromCharCode(115,112,97,110,115,95,121,95,54,56,0),718], [String.fromCharCode(104,97,108,102,100,95,111,95,50,49,0),799], [String.fromCharCode(117,95,57,55,95,105,110,116,99,104,101,99,107,0),42]]);
         floatero /= Math.max(1, parseFloat(`${1 & parseInt(`${floatero}`)}`));
         main_o5.set(`${main_o5.size}`, main_o5.size);
      }
      servicex += `${(String.fromCharCode(102,0) == libapminsightbn ? imagewatchliven.length : libapminsightbn.length)}`;
      description_fY.push(1 << (Math.min(Math.abs(parseInt(`${iconcurrentmatchM}`)), 2)));
    if (!initialList.current || initialList.current.length == 0) {

   let othermatchdetailbgo = 8547264.0 <= iconcurrentmatchM;
   do {
      iconcurrentmatchM += parseFloat(`${3 << (Math.min(4, servicex.length))}`);
      if (othermatchdetailbgo) {
         break;
      }
   } while (((arear.length % (Math.max(1, 5))) >= 5) && othermatchdetailbgo);
   for (let v = 0; v < 2; v++) {
       let data0: Array<any> = [905, 512, 970];
       let predictiondefault_ = 0.0;
      while (!data0.includes(predictiondefault_)) {
         predictiondefault_ /= Math.max(2, 2 + parseInt(`${predictiondefault_}`));
         break;
      }
      while (predictiondefault_ == 5.33) {
          let fullscreenminP = 0;
          let switch_kd = 1;
          let promotionn = String.fromCharCode(100,117,109,112,101,114,95,56,95,53,0);
         predictiondefault_ += promotionn.length >> (Math.min(Math.abs(2), 5));
         fullscreenminP /= Math.max(2 << (Math.min(2, Math.abs(switch_kd))), 4);
         switch_kd *= 2;
         promotionn = `${3 << (Math.min(2, Math.abs(fullscreenminP)))}`;
         break;
      }
         predictiondefault_ *= parseInt(`${predictiondefault_}`);
         predictiondefault_ *= 2;
      while (2.45 <= (predictiondefault_ / (Math.max(5.56, 1))) || 4 <= (5 * data0.length)) {
         predictiondefault_ *= data0.length >> (Math.min(Math.abs(3), 3));
         break;
      }
          let privatechatbgW = String.fromCharCode(109,97,112,112,101,114,95,102,95,53,0);
          let predictionbannerV = 5;
          let homel = String.fromCharCode(112,101,114,102,111,114,109,101,100,95,105,95,53,56,0);
         predictiondefault_ += parseInt(`${predictiondefault_}`);
         privatechatbgW = `${homel.length}`;
         predictionbannerV += homel.length % 1;
      libreacti.push(banner4.length - data0.length);
   }
   if (5 > libreacti.length) {
      arear = `${libapminsightbn.length % 3}`;
   }
      setError(true)

   while (5 > (libreacti.length / 5) || (libreacti.length / 5) > 5) {
      description_fY = [parseInt(`${iconcurrentmatchM}`)];
      break;
   }
      singlek *= parseFloat(`${2}`);
      servicex = `${banner4.length}`;
      return imageProp.source?.uri
    }
    setCurrent(x => {

       let launch4 = String.fromCharCode(111,98,118,105,111,117,115,108,121,95,107,95,56,56,0);
       let showless0 = String.fromCharCode(122,95,50,51,0);
      while (launch4 == showless0) {
          let defaultlogom = String.fromCharCode(103,95,56,54,95,97,108,115,97,0);
          let arrowrightwithtailR: Array<any> = [366, 743, 568];
          let emojiheartM = String.fromCharCode(106,112,101,103,116,97,98,108,101,115,95,110,95,50,50,0);
          let downloaderQ = String.fromCharCode(104,95,53,51,95,97,117,116,111,100,101,108,101,116,101,0);
          let iconsharefriendsH = 3;
         showless0 = `${1 - showless0.length}`;
         defaultlogom += `${1 << (Math.min(1, defaultlogom.length))}`;
         arrowrightwithtailR.push(1 << (Math.min(3, arrowrightwithtailR.length)));
         emojiheartM += `${(downloaderQ == String.fromCharCode(100,0) ? iconsharefriendsH : downloaderQ.length)}`;
         iconsharefriendsH %= Math.max(emojiheartM.length | 2, 5);
         break;
      }
       let viewero = String.fromCharCode(111,95,52,49,95,115,101,108,0);
       let arrowdownA = String.fromCharCode(108,101,97,102,95,121,95,51,50,0);
      if (viewero.endsWith(`${showless0.length}`)) {
          let pressureQ = String.fromCharCode(104,95,50,49,95,117,110,99,111,110,102,105,114,109,101,100,0);
         showless0 = `${(String.fromCharCode(71,0) == showless0 ? showless0.length : viewero.length)}`;
         pressureQ = `${pressureQ.length & 3}`;
      }
         showless0 += `${viewero.length | showless0.length}`;
      if (showless0.includes(launch4)) {
         launch4 += `${2 << (Math.min(1, launch4.length))}`;
      }
         showless0 += `${3 - arrowdownA.length}`;
      singlek += parseFloat(`${libapminsightbn.length}`);
       let foregroundN = String.fromCharCode(112,95,54,52,95,119,109,118,100,115,112,0);
       let middlebrightnessg: Map<any, any> = new Map([[String.fromCharCode(114,101,99,111,114,100,105,110,103,95,49,95,56,56,0),732], [String.fromCharCode(116,114,97,118,101,108,95,115,95,53,57,0),934], [String.fromCharCode(97,98,115,108,95,121,95,52,49,0),836]]);
       let gradlewX = String.fromCharCode(117,95,57,50,95,103,110,111,115,105,115,0);
         foregroundN += `${foregroundN.length * 1}`;
          let filledm = String.fromCharCode(98,97,99,107,115,105,100,101,95,118,95,50,51,0);
         gradlewX += "1";
         filledm += "2";
      while (!gradlewX.startsWith(`${middlebrightnessg.size}`)) {
         middlebrightnessg.set(foregroundN, foregroundN.length & 1);
         break;
      }
      let placeholderV = 7483348 <= middlebrightnessg.size;
      do {
         middlebrightnessg.set(foregroundN, foregroundN.length ^ 3);
         if (placeholderV) {
            break;
         }
      } while ((4 > (2 ^ middlebrightnessg.size)) && placeholderV);
      let floater_ = 5121209 <= foregroundN.length;
      do {
          let reducerY: Array<any> = [146, 147];
          let specw = 5.0;
          let inactive9 = 3;
          let iconshareO = 2.0;
          let gifgoal4 = 1.0;
         foregroundN = `${inactive9}`;
         reducerY = [reducerY.length];
         specw /= Math.max(parseInt(`${iconshareO}`) + parseInt(`${gifgoal4}`), 1);
         inactive9 >>= Math.min(2, Math.abs(reducerY.length * 2));
         iconshareO -= parseInt(`${gifgoal4}`);
         if (floater_) {
            break;
         }
      } while ((foregroundN.startsWith(`${middlebrightnessg.size}`)) && floater_);
       let fastH = String.fromCharCode(114,101,97,99,104,101,100,95,111,95,51,48,0);
      while (3 <= (foregroundN.length * 1)) {
         foregroundN = `${fastH.length}`;
         break;
      }
         middlebrightnessg.set(gradlewX, gradlewX.length);
         foregroundN = `${middlebrightnessg.size >> (Math.min(foregroundN.length, 4))}`;
      pushy = `${(String.fromCharCode(67,0) == arear ? parseInt(`${iconcurrentmatchM}`) : arear.length)}`;
      imagewatchliven = [libreacti.length + 1];
      if (alternativeImg && x < alternativeImg?.length) {

   for (let k = 0; k < 3; k++) {
      playershirtq = `${imagewatchliven.length}`;
   }
      description_fY.push(pushy.length);
      pushy = "3";
        return x + 1
      }
      return x
    })

       let phoneshare_ = 1;
       let anythinkZ: Array<any> = [String.fromCharCode(119,95,53,95,99,111,110,118,101,114,115,97,116,105,111,110,0), String.fromCharCode(107,95,51,56,95,98,101,97,116,105,110,103,0)];
         phoneshare_ ^= phoneshare_ * anythinkZ.length;
      while (phoneshare_ == anythinkZ.length) {
         phoneshare_ ^= 3;
         break;
      }
          let chinak = String.fromCharCode(100,105,115,99,111,110,110,101,99,116,95,104,95,55,48,0);
         phoneshare_ += 1 & phoneshare_;
         chinak = `${3 & chinak.length}`;
      for (let i = 0; i < 3; i++) {
         phoneshare_ *= 1 & phoneshare_;
      }
         phoneshare_ /= Math.max(2, 3 * anythinkZ.length);
      for (let d = 0; d < 1; d++) {
          let libtobC = 5.0;
          let gradlei = String.fromCharCode(115,95,51,49,95,99,111,110,115,105,115,116,101,110,99,121,0);
          let updatesY = String.fromCharCode(102,101,109,97,108,101,95,116,95,54,56,0);
          let sina2 = String.fromCharCode(115,99,101,110,101,99,117,116,95,117,95,57,50,0);
          let fullscreenminv = true;
         anythinkZ = [(sina2 == String.fromCharCode(86,0) ? sina2.length : gradlei.length)];
         libtobC -= parseFloat(`${parseInt(`${libtobC}`) ^ updatesY.length}`);
         gradlei += `${updatesY.length}`;
         fullscreenminv = 4.26 == libtobC;
      }
      playershirtq += `${(arear == String.fromCharCode(119,0) ? parseInt(`${singlek}`) : arear.length)}`;
      iconcurrentmatchM *= parseFloat(`${3 << (Math.min(5, description_fY.length))}`);
      libreacti.push(2 + libreacti.length);
    console.debug('using', initialList.current.at(0))

   let model6 = arear.length >= 9611478;
   do {
      arear = `${parseInt(`${iconcurrentmatchM}`)}`;
      if (model6) {
         break;
      }
   } while (((3 << (Math.min(4, banner4.length))) > 2) && model6);
      pushy += `${description_fY.length | servicex.length}`;
      libapminsightbn += `${pushy.length}`;
    return initialList.current.at(0)
  }

  const handleOnError = () => {
       let teamdetailsbg1: Map<any, any> = new Map([[String.fromCharCode(118,95,53,53,95,116,121,112,105,110,103,0),true ], [String.fromCharCode(121,95,49,54,95,118,112,115,104,97,114,101,100,0),true ], [String.fromCharCode(97,99,107,110,111,119,108,101,100,103,101,100,95,54,95,51,56,0),true ]]);
    let popupv: Array<any> = [String.fromCharCode(101,118,101,114,121,111,110,101,95,98,95,53,51,0), String.fromCharCode(121,95,49,52,95,115,97,102,97,114,121,0), String.fromCharCode(102,95,52,52,95,105,110,116,101,114,112,111,108,97,116,105,111,110,0)];
    let recommendation9: Array<any> = [818, 600, 173];
    let redgoald = 5.0;
    let iconwatchm: Map<any, any> = new Map([[String.fromCharCode(102,95,57,52,95,116,101,114,109,115,101,116,0),String.fromCharCode(117,95,49,52,95,112,114,101,97,108,108,111,99,0)], [String.fromCharCode(112,105,120,101,108,115,120,95,53,95,57,55,0),String.fromCharCode(97,95,56,51,95,112,114,111,118,105,100,101,115,0)]]);
    let indexa = 3;
    let sentrye = String.fromCharCode(110,95,56,51,95,104,101,97,100,0);
    let transferI = 5.0;
    let roboto1 = true;
    let graphx: Array<any> = [842, 925];
      popupv = [indexa];
      popupv = [3];
       let downarrowZ = String.fromCharCode(115,116,114,101,97,109,105,110,102,111,95,110,95,50,52,0);
       let arrowrightwithtail0 = 0;
       let flipperw = 2.0;
         downarrowZ += "2";
      for (let z = 0; z < 3; z++) {
          let thumbnailf: Map<any, any> = new Map([[String.fromCharCode(119,95,52,48,0),false ], [String.fromCharCode(112,95,53,53,95,99,108,97,115,104,0),true ]]);
          let mintegral_ = String.fromCharCode(108,95,49,48,95,115,116,111,114,97,98,108,101,0);
          let matchinactiveJ = String.fromCharCode(107,95,52,50,95,99,117,100,97,115,99,97,108,101,0);
          let homeloadingH = String.fromCharCode(100,95,56,95,97,99,99,101,115,115,111,114,105,101,115,0);
          let componentregistry9 = String.fromCharCode(100,101,108,97,121,101,100,95,97,95,53,51,0);
         downarrowZ = `${parseInt(`${flipperw}`)}`;
         thumbnailf.set(matchinactiveJ, homeloadingH.length % 2);
         mintegral_ += `${1 >> (Math.min(1, componentregistry9.length))}`;
         matchinactiveJ += `${1 / (Math.max(6, homeloadingH.length))}`;
         componentregistry9 = `${homeloadingH.length}`;
      }
      while ((downarrowZ.length >> (Math.min(Math.abs(2), 5))) < 3) {
         arrowrightwithtail0 -= (downarrowZ == String.fromCharCode(82,0) ? parseInt(`${flipperw}`) : downarrowZ.length);
         break;
      }
      while (flipperw >= 1.91) {
          let footballtrophy5 = 5.0;
         arrowrightwithtail0 *= downarrowZ.length;
         footballtrophy5 /= Math.max(3, parseInt(`${footballtrophy5}`));
         break;
      }
         arrowrightwithtail0 <<= Math.min(downarrowZ.length, 3);
      if (1.97 >= flipperw) {
         flipperw *= (parseFloat(`${downarrowZ == String.fromCharCode(121,0) ? arrowrightwithtail0 : downarrowZ.length}`));
      }
      let smallorangemanW = arrowrightwithtail0 <= 9116502;
      do {
          let filterk = 5;
         arrowrightwithtail0 <<= Math.min(Math.abs(arrowrightwithtail0 % (Math.max(downarrowZ.length, 4))), 1);
         filterk &= filterk << (Math.min(Math.abs(2), 3));
         if (smallorangemanW) {
            break;
         }
      } while (smallorangemanW && (3 > (downarrowZ.length % (Math.max(8, arrowrightwithtail0)))));
         downarrowZ += `${1 / (Math.max(8, parseInt(`${flipperw}`)))}`;
         arrowrightwithtail0 *= parseInt(`${flipperw}`) | 1;
      redgoald *= parseFloat(`${1 / (Math.max(5, recommendation9.length))}`);
   let upgradeq = transferI >= 9332411.0;
   do {
      transferI *= popupv.length;
      if (upgradeq) {
         break;
      }
   } while ((3.35 < transferI) && upgradeq);
      popupv = [parseInt(`${transferI}`) << (Math.min(3, Math.abs(parseInt(`${redgoald}`))))];
   if ((1 ^ recommendation9.length) < 2 && (recommendation9.length * parseInt(`${redgoald}`)) < 1) {
       let embedX = 2;
       let penaltymatchicons = 0.0;
       let mbnativeadvanced9 = String.fromCharCode(113,95,50,55,95,109,105,103,114,97,116,101,100,0);
       let icontransferclub1 = String.fromCharCode(99,111,109,97,110,100,95,113,95,49,56,0);
         icontransferclub1 += `${parseInt(`${penaltymatchicons}`)}`;
       let watchnowbge: Array<any> = [633, 868, 140];
       let predictiona: Array<any> = [String.fromCharCode(108,95,53,49,95,101,108,105,115,116,0), String.fromCharCode(117,110,100,101,108,101,103,97,116,101,95,109,95,54,0), String.fromCharCode(120,95,53,50,95,103,101,110,97,110,110,0)];
         penaltymatchicons -= 2 + watchnowbge.length;
      for (let c = 0; c < 2; c++) {
         embedX >>= Math.min(Math.abs(2), 1);
      }
       let fastforward2 = String.fromCharCode(116,97,99,107,95,112,95,57,49,0);
         embedX += 2 + embedX;
      while (2 > (icontransferclub1.length ^ 3) && 3 > (embedX ^ 3)) {
          let navigationh = String.fromCharCode(112,114,111,112,111,115,101,95,105,95,53,51,0);
          let mimeb = 1.0;
          let footballfiledlayoutS = 3.0;
          let libapminsightbM = String.fromCharCode(106,95,49,95,99,111,110,99,97,116,100,101,99,0);
         icontransferclub1 += `${predictiona.length}`;
         navigationh = `${2 % (Math.max(5, navigationh.length))}`;
         mimeb += parseFloat(`${3}`);
         footballfiledlayoutS += parseFloat(`${parseInt(`${mimeb}`)}`);
         libapminsightbM += `${navigationh.length | 1}`;
         break;
      }
       let clubV = 2.0;
      let iconnewsshareV = String.fromCharCode(98,109,55,109,100,0) == fastforward2;
      do {
          let k_lockK = 1;
         fastforward2 = `${icontransferclub1.length}`;
         k_lockK <<= Math.min(Math.abs(k_lockK >> (Math.min(Math.abs(3), 4))), 4);
         if (iconnewsshareV) {
            break;
         }
      } while (((5 << (Math.min(3, fastforward2.length))) > 5 || 5 > (fastforward2.length - parseInt(`${penaltymatchicons}`))) && iconnewsshareV);
      while (4 < predictiona.length) {
         embedX |= fastforward2.length;
         break;
      }
         embedX >>= Math.min(mbnativeadvanced9.length, 3);
      for (let b = 0; b < 3; b++) {
         penaltymatchicons -= predictiona.length * icontransferclub1.length;
      }
      redgoald -= parseFloat(`${parseInt(`${transferI}`)}`);
   }
      indexa <<= Math.min(Math.abs(parseInt(`${transferI}`) | teamdetailsbg1.size), 2);
   if (1 < popupv.length) {
       let gesturesp = 0.0;
       let assistc = String.fromCharCode(119,101,101,107,95,115,95,57,48,0);
       let nodeL = 5.0;
      for (let u = 0; u < 1; u++) {
         nodeL *= parseFloat(`${parseInt(`${gesturesp}`)}`);
      }
          let profileactiveU = 3;
          let libreactnativejniv = String.fromCharCode(112,97,105,114,119,105,115,101,95,117,95,49,51,0);
         nodeL *= parseFloat(`${3}`);
         profileactiveU |= 3;
         libreactnativejniv = "3";
      for (let x = 0; x < 3; x++) {
         gesturesp += 1;
      }
          let androidp = 5.0;
          let singley = String.fromCharCode(100,97,116,97,98,108,111,99,107,95,119,95,54,50,0);
          let favoriteX = 3.0;
         gesturesp -= parseInt(`${androidp}`);
         singley = `${singley.length - parseInt(`${favoriteX}`)}`;
         favoriteX += (parseFloat(`${String.fromCharCode(66,0) == singley ? parseInt(`${favoriteX}`) : singley.length}`));
      if (!assistc.endsWith(`${nodeL}`)) {
          let bell_ = 1.0;
          let privilegec = String.fromCharCode(115,97,109,112,95,108,95,51,51,0);
          let refreshi: Array<any> = [784, 117, 262];
         nodeL *= parseFloat(`${assistc.length}`);
         bell_ += 2;
         privilegec = `${refreshi.length % 1}`;
         refreshi.push(privilegec.length >> (Math.min(5, refreshi.length)));
      }
      for (let j = 0; j < 1; j++) {
         assistc += `${assistc.length | 2}`;
      }
       let middlej = 3;
       let containerx = 5;
         middlej += 3;
         assistc += "1";
      popupv = [1 & recommendation9.length];
   }
       let gemfiler = String.fromCharCode(114,101,99,105,112,114,111,99,97,108,95,98,95,51,57,0);
       let predictionwinA = true;
      for (let r = 0; r < 2; r++) {
          let league3 = 0.0;
         gemfiler += `${(parseInt(`${league3}`) + (predictionwinA ? 5 : 2))}`;
      }
          let privatechatbgz = String.fromCharCode(118,95,52,56,95,119,114,105,116,101,111,117,116,0);
          let clubq = 1.0;
         gemfiler = `${((predictionwinA ? 1 : 1) / 2)}`;
         privatechatbgz = `${parseInt(`${clubq}`)}`;
         clubq -= (privatechatbgz == String.fromCharCode(88,0) ? privatechatbgz.length : parseInt(`${clubq}`));
         predictionwinA = (63 >= ((!predictionwinA ? 63 : gemfiler.length) * gemfiler.length));
      while (gemfiler.endsWith(`${predictionwinA}`)) {
          let dialogc = String.fromCharCode(119,95,55,56,95,101,110,99,0);
         predictionwinA = dialogc.length < 51;
         break;
      }
      for (let n = 0; n < 1; n++) {
         predictionwinA = !predictionwinA;
      }
         gemfiler = `${(String.fromCharCode(101,0) == gemfiler ? gemfiler.length : (predictionwinA ? 4 : 2))}`;
      popupv = [((predictionwinA ? 2 : 3) / (Math.max(parseInt(`${redgoald}`), 5)))];
      transferI *= indexa;
       let paginationq: Array<any> = [String.fromCharCode(98,95,54,50,95,99,111,110,118,101,114,116,0), String.fromCharCode(115,113,108,99,105,112,104,101,114,95,116,95,53,51,0), String.fromCharCode(115,105,108,101,110,116,108,121,95,106,95,51,52,0)];
       let profilepict = String.fromCharCode(113,95,49,55,95,98,111,114,100,101,114,0);
       let templateprocessora = String.fromCharCode(116,95,57,57,95,115,121,109,101,118,101,110,0);
          let private_82 = false;
          let detaile = false;
         templateprocessora += `${((private_82 ? 4 : 5))}`;
         private_82 = detaile && detaile;
          let iconshareZ: Array<any> = [243, 664];
          let hejib = String.fromCharCode(102,111,117,114,99,99,115,95,52,95,49,49,0);
         templateprocessora = `${1 >> (Math.min(4, templateprocessora.length))}`;
         iconshareZ = [hejib.length & 1];
         hejib = `${hejib.length ^ iconshareZ.length}`;
      if ((5 << (Math.min(5, templateprocessora.length))) >= 5 && (paginationq.length << (Math.min(templateprocessora.length, 4))) >= 5) {
         templateprocessora = "2";
      }
      for (let f = 0; f < 2; f++) {
         paginationq.push(templateprocessora.length ^ 3);
      }
          let airbnbstar6: Array<any> = [980, 671, 720];
          let profileframeY = 3.0;
         profilepict = `${templateprocessora.length}`;
         airbnbstar6 = [parseInt(`${profileframeY}`)];
         profileframeY /= Math.max(parseFloat(`${parseInt(`${profileframeY}`) % 3}`), 4);
      while (!profilepict.startsWith(`${paginationq.length}`)) {
          let privilege9 = 0.0;
          let iconeyev = 5.0;
          let time_iln = true;
          let teamY = false;
         paginationq.push(parseInt(`${iconeyev}`) + 3);
         privilege9 /= Math.max(5, ((teamY ? 4 : 1) - 2));
         iconeyev /= Math.max(1 * parseInt(`${privilege9}`), 3);
         time_iln = privilege9 == 52.87;
         teamY = !time_iln;
         break;
      }
         templateprocessora = `${paginationq.length / 3}`;
      if ((3 | paginationq.length) >= 1 || (profilepict.length | paginationq.length) >= 3) {
         paginationq.push((String.fromCharCode(81,0) == templateprocessora ? templateprocessora.length : paginationq.length));
      }
       let icondefaultthumbnailQ: Map<any, any> = new Map([[String.fromCharCode(115,101,108,101,99,116,105,118,101,108,121,95,51,95,50,48,0),String.fromCharCode(101,95,54,95,101,108,115,101,0)], [String.fromCharCode(114,101,100,97,99,116,95,122,95,54,52,0),String.fromCharCode(118,95,54,49,95,104,97,114,100,119,97,114,101,0)], [String.fromCharCode(101,95,52,57,95,112,115,110,114,104,118,115,0),String.fromCharCode(100,117,114,98,105,110,95,115,95,51,51,0)]]);
      redgoald += parseFloat(`${3}`);
       let runtimescheduler2 = false;
       let bgvipxvod1: Array<any> = [String.fromCharCode(112,95,54,51,95,117,115,101,100,0), String.fromCharCode(119,97,108,95,57,95,51,56,0)];
       let l_centerq = String.fromCharCode(113,95,54,53,95,115,117,105,116,97,98,108,101,0);
      if (!runtimescheduler2) {
         runtimescheduler2 = l_centerq.length == 9;
      }
      let time_88 = 7853454 >= bgvipxvod1.length;
      do {
          let private_eS: Array<any> = [26, 753];
         bgvipxvod1.push(bgvipxvod1.length | l_centerq.length);
         private_eS = [private_eS.length >> (Math.min(5, private_eS.length))];
         if (time_88) {
            break;
         }
      } while (time_88 && (bgvipxvod1.length < 4));
          let edit6 = String.fromCharCode(99,95,50,51,95,116,101,115,116,105,109,103,105,110,116,0);
          let alertY: Map<any, any> = new Map([[String.fromCharCode(118,95,53,95,116,101,108,108,97,114,0),String.fromCharCode(114,110,103,95,54,95,54,48,0)], [String.fromCharCode(116,111,107,101,110,105,122,101,100,95,100,95,50,52,0),String.fromCharCode(100,109,117,108,95,115,95,55,52,0)]]);
         l_centerq = "1";
         edit6 = `${edit6.length * 1}`;
         alertY.set(edit6, alertY.size << (Math.min(Math.abs(1), 4)));
         runtimescheduler2 = bgvipxvod1.includes(runtimescheduler2);
         bgvipxvod1 = [l_centerq.length];
       let humiditye = 0.0;
       let clearb = 0.0;
         runtimescheduler2 = l_centerq.length < 71;
      for (let k = 0; k < 3; k++) {
         bgvipxvod1 = [2];
      }
      let twitterm = 6502008.0 >= humiditye;
      do {
          let predictionwinv = 1.0;
          let navigationt = String.fromCharCode(107,95,50,53,95,109,117,108,116,105,99,97,115,116,101,100,0);
          let transfer8: Map<any, any> = new Map([[String.fromCharCode(107,95,54,54,95,99,111,101,102,0),164], [String.fromCharCode(105,110,108,105,110,101,100,95,48,95,55,55,0),701], [String.fromCharCode(105,95,51,50,95,102,105,110,101,0),260]]);
         humiditye /= Math.max(5, 1 - parseInt(`${predictionwinv}`));
         predictionwinv *= 3;
         navigationt = `${navigationt.length}`;
         transfer8.set(`${navigationt}`, navigationt.length);
         if (twitterm) {
            break;
         }
      } while (twitterm && (4.16 < humiditye));
      popupv.push(bgvipxvod1.length ^ 3);
   while ((iconwatchm.size >> (Math.min(Math.abs(5), 5))) >= 5 || (iconwatchm.size >> (Math.min(Math.abs(5), 1))) >= 3) {
      redgoald /= Math.max(parseFloat(`${indexa - 3}`), 1);
      break;
   }
      redgoald += parseFloat(`${1 * parseInt(`${transferI}`)}`);
      transferI /= Math.max(2, 1 << (Math.min(Math.abs(parseInt(`${transferI}`)), 1)));
   for (let z = 0; z < 2; z++) {
      teamdetailsbg1.set(`${roboto1}`, 3);
   }
      sentrye = `${iconwatchm.size << (Math.min(3, Math.abs(parseInt(`${redgoald}`))))}`;

    setImgUrl(useNextImage())
  }

  if (error) { // when all url error, use the default placeholder image
    return (
      <View style={[{ backgroundColor: colors.loading, ...styles.loadingCard }, imageProp.style]}>
        <LoadingImgSvg />
      </View>
    )
  }

  if (useFastImage === true || Platform.OS === "android") {
    if (typeof imageProp.source == 'number') { // if source={require(...)}
      return <FastImage {...imageProp as FastImageProps} />
    } else {
      return <FastImage key={current.toString() + imgUrl} {...imageProp as FastImageProps} source={{ uri: imgUrl }} onError={handleOnError} />
    }
  } else {
    if (typeof imageProp.source == 'number') { // if source={require(...)}
      return <Image {...imageProp as yysIconrightorangeLogin} />
    } else { // if source={{uri:...}}
      return <Image key={current.toString() + imgUrl} {...imageProp as yysIconrightorangeLogin} source={{ ...(imageProp.source as ImageURISource), uri: imgUrl }} onError={handleOnError} />
    }
  }
}

const styles = StyleSheet.create({
  loadingCard: {
    marginRight: 20,
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    borderRadius: 10,
  },
})

export default customFastImage;