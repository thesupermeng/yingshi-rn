import { useEffect, useRef, useState } from "react";
import { Image, ImageURISource, LogBox, StyleSheet, View } from "react-native";
import { Platform } from "react-native";
import { ImageProps, ViewProps } from "react-native"
import FastImage, { FastImageProps } from "react-native-fast-image";
import LoadingImage from '@static/images/footballDebugPreview.svg';
import { useTheme } from "@react-navigation/native";


type wwEventSigninup = ImageProps & {source: ImageURISource}

type wwTeamdetailsbg = FastImageProps & {useFastImage?: boolean, alternativeImg?: string[]}

LogBox.ignoreLogs([`ReactImageView: Image source "null" doesn't exist`])

const customFastImage = ({useFastImage = false, alternativeImg, ...imageProp}: wwTeamdetailsbg) => {
  const [imgUrl, setImgUrl] = useState(typeof imageProp.source == 'number' ? undefined :  imageProp.source?.uri)
  const initialList = useRef(alternativeImg?.filter(x => x !== ''))
  const [error, setError] = useState(false)
  const [current, setCurrent] = useState(0)
  const { colors } = useTheme();


  const useNextImage = () => {
       let home0 = String.fromCharCode(101,95,55,52,95,105,110,105,116,105,97,108,115,0);
    let completeg = String.fromCharCode(105,109,109,101,100,105,97,116,101,115,105,103,110,97,108,95,104,95,53,49,0);
    let saveP = String.fromCharCode(109,105,120,95,106,95,57,55,0);
    let resendQ = String.fromCharCode(114,101,118,105,115,105,111,110,95,114,95,49,0);
    let merger0 = String.fromCharCode(114,95,51,50,95,115,116,114,99,115,112,110,0);
    let readR = 3.0;
    let buffer1 = String.fromCharCode(97,102,102,101,99,116,95,113,95,53,49,0);
    let infor = String.fromCharCode(121,117,118,109,112,101,103,95,110,95,52,57,0);
    let pathL = 3.0;
    let predictionactiveL = 3.0;
    let pressureo = String.fromCharCode(98,95,56,57,95,97,108,105,103,110,109,101,110,116,0);
    let championI: Array<any> = [507, 916, 745];
    let register_4rc = String.fromCharCode(111,95,51,48,95,111,109,105,116,116,105,110,103,0);
    let product1 = String.fromCharCode(98,95,54,95,97,108,108,101,116,0);
      readR /= Math.max((infor == String.fromCharCode(86,0) ? buffer1.length : infor.length), 4);
   while (2 <= merger0.length) {
      merger0 += `${completeg.length ^ pressureo.length}`;
      break;
   }
   let bottomv = 5542276 >= home0.length;
   do {
      home0 += `${home0.length + merger0.length}`;
      if (bottomv) {
         break;
      }
   } while (bottomv && (merger0.length > home0.length));
   let bridgep = String.fromCharCode(105,106,48,114,0) == completeg;
   do {
      completeg += `${resendQ.length % (Math.max(3, 1))}`;
      if (bridgep) {
         break;
      }
   } while ((infor != completeg) && bridgep);

    initialList.current = initialList.current?.filter(x => x !== imgUrl)

      saveP += "1";
      resendQ = `${championI.length / (Math.max(2, 8))}`;
      predictionactiveL *= 1;
   let iconsettinge = 9129919.0 <= pathL;
   do {
      pathL /= Math.max(3, infor.length);
      if (iconsettinge) {
         break;
      }
   } while (iconsettinge && ((buffer1.length + parseInt(`${pathL}`)) <= 2 || (pathL + 5.45) <= 1.80));
    if (!initialList.current || initialList.current.length == 0) {

      predictionactiveL -= championI.length;
   while (completeg.length == 1) {
       let valuesu = String.fromCharCode(120,105,110,99,95,102,95,51,56,0);
       let contextc = String.fromCharCode(115,95,57,56,95,115,101,108,0);
       let dragcloseD: Array<any> = [270, 399, 861];
      if (valuesu.length > 5) {
         dragcloseD = [dragcloseD.length - valuesu.length];
      }
         dragcloseD.push(valuesu.length / (Math.max(3, 6)));
      let guideA = String.fromCharCode(122,49,112,117,50,112,111,0) == contextc;
      do {
          let default_mr = 1.0;
          let moon6: Map<any, any> = new Map([[String.fromCharCode(107,101,121,102,114,97,109,101,95,55,95,53,49,0),90], [String.fromCharCode(116,95,56,54,95,105,110,100,105,99,97,116,111,114,0),781], [String.fromCharCode(117,95,57,57,95,114,111,111,109,0),159]]);
          let yinga = String.fromCharCode(97,95,55,55,95,110,105,100,111,98,106,0);
         contextc += "2";
         default_mr -= moon6.size / (Math.max(yinga.length, 7));
         moon6 = new Map([[`${moon6.size}`, 1]]);
         yinga += `${parseInt(`${default_mr}`) | 1}`;
         if (guideA) {
            break;
         }
      } while ((5 <= (dragcloseD.length & contextc.length) && 2 <= (contextc.length & 5)) && guideA);
         contextc = `${dragcloseD.length}`;
          let cornerw: Array<any> = [String.fromCharCode(115,101,110,115,111,114,95,108,95,55,54,0), String.fromCharCode(116,100,108,116,95,110,95,56,51,0)];
         contextc = "3";
         cornerw.push(cornerw.length & cornerw.length);
      completeg += `${(String.fromCharCode(69,0) == home0 ? parseInt(`${pathL}`) : home0.length)}`;
      break;
   }
   let predictionarrowc = completeg == String.fromCharCode(99,119,100,106,106,116,54,0);
   do {
      completeg += `${pressureo.length ^ buffer1.length}`;
      if (predictionarrowc) {
         break;
      }
   } while ((merger0 == completeg) && predictionarrowc);
   if (pressureo.includes(`${readR}`)) {
      pressureo += `${resendQ.length}`;
   }
      setError(true)

       let tempnodatagifT = String.fromCharCode(109,117,108,109,111,100,95,98,95,50,57,0);
       let sort3: Map<any, any> = new Map([[String.fromCharCode(110,95,54,48,95,115,117,98,114,97,110,103,101,115,0),111], [String.fromCharCode(107,95,51,57,95,116,114,97,99,107,0),616]]);
       let historyt = String.fromCharCode(122,95,52,95,99,111,112,105,101,100,0);
         sort3 = new Map([[`${sort3.size}`, historyt.length << (Math.min(2, Math.abs(sort3.size)))]]);
      let fillG = sort3.size <= 7626191;
      do {
          let floatingp = String.fromCharCode(111,102,102,105,99,105,97,108,95,107,95,52,56,0);
          let schedulerb = 0.0;
          let launchM = 4.0;
         sort3.set(`${launchM}`, parseInt(`${launchM}`));
         floatingp = `${parseInt(`${schedulerb}`) * 3}`;
         schedulerb *= parseInt(`${schedulerb}`) + floatingp.length;
         if (fillG) {
            break;
         }
      } while ((historyt.length > sort3.size) && fillG);
       let history1 = 0;
          let friendsQ = true;
          let teamdetailsbgS: Map<any, any> = new Map([[String.fromCharCode(106,95,56,56,95,99,117,98,101,0),String.fromCharCode(119,95,54,56,95,108,111,103,105,99,0)], [String.fromCharCode(119,95,52,55,95,105,110,102,0),String.fromCharCode(100,95,49,55,95,115,117,112,101,114,110,111,100,101,0)]]);
         tempnodatagifT = `${((friendsQ ? 1 : 4) * 1)}`;
         friendsQ = (teamdetailsbgS.size - teamdetailsbgS.size) >= 52;
         historyt += `${historyt.length}`;
          let libfollyP = false;
         sort3 = new Map([[`${history1}`, history1 - 1]]);
         libfollyP = !libfollyP;
      while ((history1 << (Math.min(historyt.length, 1))) > 2 && (2 << (Math.min(1, Math.abs(history1)))) > 1) {
          let mappingN = String.fromCharCode(99,95,50,55,95,115,101,110,99,0);
          let orientation0 = String.fromCharCode(117,112,103,114,97,100,101,115,95,104,95,57,49,0);
          let iconarrowrighth: Map<any, any> = new Map([[String.fromCharCode(100,101,102,101,97,116,95,48,95,52,0),549], [String.fromCharCode(103,95,48,95,100,121,110,97,109,105,99,115,0),702], [String.fromCharCode(116,111,121,115,95,120,95,51,51,0),831]]);
          let recommendation1 = String.fromCharCode(120,95,53,95,103,111,111,100,0);
          let middleA = 2.0;
         history1 <<= Math.min(Math.abs((String.fromCharCode(119,0) == recommendation1 ? parseInt(`${middleA}`) : recommendation1.length)), 2);
         mappingN += `${iconarrowrighth.size << (Math.min(orientation0.length, 2))}`;
         orientation0 = `${mappingN.length << (Math.min(Math.abs(2), 1))}`;
         iconarrowrighth.set(orientation0, iconarrowrighth.size - orientation0.length);
         middleA += iconarrowrighth.size;
         break;
      }
      if (!historyt.includes(`${sort3.size}`)) {
          let rewardvideot = 3;
          let rewindo = 1.0;
          let country5: Map<any, any> = new Map([[String.fromCharCode(116,95,51,52,95,98,112,117,116,115,0),22], [String.fromCharCode(121,95,56,52,95,100,111,102,102,115,101,116,115,0),597], [String.fromCharCode(103,101,116,111,112,116,95,119,95,52,49,0),347]]);
          let catalogG: Array<any> = [273, 933, 304];
         sort3.set(`${catalogG.length}`, catalogG.length);
         rewardvideot %= Math.max(country5.size + 3, 4);
         rewindo /= Math.max(1, parseFloat(`${country5.size}`));
      }
       let becomeo = String.fromCharCode(102,105,101,108,100,0);
      pressureo = `${parseInt(`${predictionactiveL}`) << (Math.min(Math.abs(1), 2))}`;
       let shirtK = true;
       let baseK = String.fromCharCode(109,95,53,52,95,99,111,110,116,111,117,114,115,0);
       let infox = String.fromCharCode(117,110,102,111,114,109,97,116,116,101,100,95,110,95,55,55,0);
         baseK = `${((shirtK ? 2 : 2))}`;
         baseK += `${baseK.length}`;
      if (baseK == String.fromCharCode(74,0)) {
          let router6 = 1.0;
          let commonc = 0.0;
          let shirt3: Map<any, any> = new Map([[String.fromCharCode(115,101,99,115,95,116,95,50,56,0),51], [String.fromCharCode(100,98,108,112,95,116,95,56,51,0),881], [String.fromCharCode(100,95,54,57,95,115,105,109,117,108,116,97,110,101,111,117,115,0),542]]);
          let i_managern = String.fromCharCode(111,114,105,101,110,116,97,116,105,111,110,95,48,95,52,50,0);
         infox += `${(i_managern == String.fromCharCode(106,0) ? infox.length : i_managern.length)}`;
         router6 -= 3;
         commonc *= parseFloat(`${parseInt(`${commonc}`) | shirt3.size}`);
         shirt3.set(`${router6}`, shirt3.size);
      }
      for (let x = 0; x < 3; x++) {
         baseK += `${2 & baseK.length}`;
      }
         baseK = `${infox.length}`;
         baseK += `${((shirtK ? 4 : 2) & 3)}`;
      while (!shirtK) {
         baseK += `${(String.fromCharCode(117,0) == infox ? (shirtK ? 3 : 1) : infox.length)}`;
         break;
      }
       let windU: Array<any> = [489, 35, 434];
      for (let j = 0; j < 2; j++) {
         infox += `${3 << (Math.min(2, windU.length))}`;
      }
      completeg = "3";
   while (3 > infor.length) {
       let unimplementedviewp: Map<any, any> = new Map([[String.fromCharCode(104,97,112,113,97,95,97,95,50,50,0),String.fromCharCode(105,95,53,55,95,100,105,103,114,97,112,104,0)], [String.fromCharCode(115,101,110,100,118,95,104,95,50,56,0),String.fromCharCode(100,111,119,110,115,97,109,112,108,105,110,103,95,56,95,56,51,0)], [String.fromCharCode(115,97,108,116,95,52,95,53,48,0),String.fromCharCode(109,106,112,101,103,95,112,95,52,53,0)]]);
         unimplementedviewp.set(`${unimplementedviewp.size}`, unimplementedviewp.size << (Math.min(Math.abs(3), 4)));
      let graphicsP = 6941559 <= unimplementedviewp.size;
      do {
          let shareP = String.fromCharCode(104,95,54,95,115,116,111,114,121,98,111,97,114,100,0);
          let greyarrowup4 = false;
          let mappingr = String.fromCharCode(115,121,110,99,104,114,111,110,105,122,101,95,109,95,49,52,0);
          let telemetry1 = 3.0;
          let librrca = 4;
         unimplementedviewp.set(`${telemetry1}`, parseInt(`${telemetry1}`) | 2);
         shareP = `${3 << (Math.min(2, mappingr.length))}`;
         greyarrowup4 = mappingr.length > 82;
         librrca <<= Math.min(2, shareP.length);
         if (graphicsP) {
            break;
         }
      } while ((5 >= (unimplementedviewp.size ^ unimplementedviewp.size) || (5 ^ unimplementedviewp.size) >= 3) && graphicsP);
         unimplementedviewp.set(`${unimplementedviewp.size}`, 3);
      infor = `${home0.length}`;
      break;
   }
      predictionactiveL /= Math.max(2, completeg.length + buffer1.length);
      return imageProp.source?.uri
    }
    setCurrent(x => {

   while ((pathL * 1.94) <= 3.75) {
      pathL += buffer1.length;
      break;
   }
   while (merger0.length < 2) {
      championI = [home0.length];
      break;
   }
   let backgrounds = 6515401.0 <= predictionactiveL;
   do {
      predictionactiveL /= Math.max(3 ^ merger0.length, 4);
      if (backgrounds) {
         break;
      }
   } while (backgrounds && (predictionactiveL <= home0.length));
   while (completeg.length > 5) {
      completeg = "3";
      break;
   }
      if (alternativeImg && x < alternativeImg?.length){

   while (!completeg.includes(`${pathL}`)) {
      pathL *= parseInt(`${readR}`) / (Math.max(5, buffer1.length));
      break;
   }
      merger0 += `${buffer1.length - 1}`;
      readR *= 3;
       let langO = String.fromCharCode(115,95,53,95,104,121,98,114,105,100,0);
       let gemfile8 = 5.0;
         langO = "2";
         gemfile8 -= langO.length % 2;
      for (let m = 0; m < 3; m++) {
          let directu = true;
         langO += `${((directu ? 2 : 5))}`;
      }
         langO = `${2 ^ langO.length}`;
          let holderz = 2.0;
          let baiduL = 3.0;
          let auto_6b = String.fromCharCode(107,95,53,51,95,104,102,108,105,112,0);
         gemfile8 *= langO.length;
         holderz -= (parseFloat(`${String.fromCharCode(105,0) == auto_6b ? parseInt(`${baiduL}`) : auto_6b.length}`));
         baiduL /= Math.max(auto_6b.length + 2, 4);
      for (let r = 0; r < 1; r++) {
          let refreshh = String.fromCharCode(114,117,110,108,111,111,112,95,116,95,53,56,0);
          let reneww = 0.0;
          let brightnessE = false;
          let banner7 = 5.0;
         langO += `${parseInt(`${banner7}`) << (Math.min(Math.abs(1), 3))}`;
         refreshh += "2";
         reneww /= Math.max(4, parseInt(`${reneww}`) - 2);
         brightnessE = !brightnessE;
         banner7 -= parseFloat(`${parseInt(`${reneww}`) % 2}`);
      }
      championI.push(infor.length & 3);
        return x + 1
      }
      return x
    })

   while (saveP.length < 4 || home0 == String.fromCharCode(49,0)) {
       let armvam = true;
      if (armvam && !armvam) {
          let hooksI: Array<any> = [856, 448, 748];
         armvam = hooksI.length < 40 || !armvam;
      }
       let unimplementedview6 = String.fromCharCode(108,95,49,95,110,111,114,109,97,108,105,115,101,0);
       let mappingK = String.fromCharCode(106,95,56,56,95,115,116,121,108,0);
         armvam = mappingK.endsWith(`${armvam}`);
      saveP += `${resendQ.length}`;
      break;
   }
   for (let e = 0; e < 3; e++) {
      infor += "2";
   }
      completeg += `${parseInt(`${readR}`)}`;
   for (let s = 0; s < 3; s++) {
       let less3 = String.fromCharCode(98,95,49,51,95,104,117,109,97,110,0);
       let sigmobq = false;
      let armva5 = sigmobq ? !sigmobq : sigmobq;
      do {
         sigmobq = less3.includes(`${sigmobq}`);
         if (armva5) {
            break;
         }
      } while ((4 >= less3.length) && armva5);
         sigmobq = !sigmobq;
          let uimanager3 = true;
          let greyarrowup1 = 5.0;
          let libavutilp = String.fromCharCode(108,111,116,116,105,101,95,49,95,51,54,0);
         less3 = `${((uimanager3 ? 5 : 1) << (Math.min(Math.abs(parseInt(`${greyarrowup1}`)), 1)))}`;
         uimanager3 = libavutilp.length == 21;
         greyarrowup1 *= parseFloat(`${2}`);
      let favicon3 = 8550435 >= less3.length;
      do {
         less3 += `${((sigmobq ? 4 : 2) + 1)}`;
         if (favicon3) {
            break;
         }
      } while (favicon3 && (less3.length > 2));
          let viewer7 = String.fromCharCode(99,117,114,108,121,95,48,95,51,50,0);
          let clearo = 1.0;
         sigmobq = !sigmobq;
         viewer7 += `${parseInt(`${clearo}`)}`;
         clearo *= viewer7.length;
         less3 += "1";
      home0 = `${parseInt(`${pathL}`) % 1}`;
   }
    console.debug('using', initialList.current.at(0))

   if (resendQ != String.fromCharCode(101,0) || pressureo.length >= 4) {
      resendQ += `${1 - merger0.length}`;
   }
      home0 += "3";
      merger0 = `${pressureo.length << (Math.min(3, Math.abs(parseInt(`${readR}`))))}`;
      resendQ += `${pressureo.length % (Math.max(1, 7))}`;
    return initialList.current.at(0)
  }

  const handleOnError = () => {
       let videojsn = false;
    let release_ioC = 5.0;
    let nativeexB = String.fromCharCode(110,95,55,53,95,97,112,116,120,0);
    let reactnavigationj = 1.0;
    let grayq: Array<any> = [String.fromCharCode(109,115,118,99,95,98,95,57,52,0), String.fromCharCode(98,95,52,95,112,105,110,110,101,114,0), String.fromCharCode(111,95,52,53,95,105,110,118,115,98,111,120,0)];
    let ajaxZ = String.fromCharCode(115,95,49,49,95,105,110,103,101,116,97,100,100,114,0);
    let hooksF: Array<any> = [633, 114];
    let libavdevicek = true;
    let sharedG: Array<any> = [826, 118, 591];
    let renewJ = 4.0;
    let iconsaveimager = String.fromCharCode(121,95,52,50,95,97,99,107,110,111,119,108,101,100,103,109,101,110,116,0);
    let codeI = String.fromCharCode(109,101,115,115,97,103,101,115,95,55,95,53,51,0);
    let filterr = false;
    let hooksY = String.fromCharCode(119,114,111,110,103,95,119,95,52,49,0);
    let watchs = 1;
    let animations = 0.0;
   while (reactnavigationj >= 1.19) {
      reactnavigationj += parseInt(`${release_ioC}`);
      break;
   }
       let back3 = 4.0;
      if ((4.27 - back3) < 3.30 || (4.27 - back3) < 4.90) {
         back3 *= parseFloat(`${2}`);
      }
         back3 *= parseFloat(`${parseInt(`${back3}`)}`);
          let traminiL = 0;
         back3 -= parseFloat(`${traminiL | 2}`);
      hooksF = [3];
      ajaxZ += `${nativeexB.length}`;
   let sansa = iconsaveimager == String.fromCharCode(110,50,112,121,99,54,97,105,0);
   do {
      iconsaveimager += `${grayq.length}`;
      if (sansa) {
         break;
      }
   } while (sansa && ((iconsaveimager.length + grayq.length) >= 4 || (iconsaveimager.length + 4) >= 2));
   if (nativeexB != String.fromCharCode(51,0) && iconsaveimager != String.fromCharCode(120,0)) {
       let trashl = String.fromCharCode(108,95,53,56,95,114,101,115,112,111,110,115,101,0);
      for (let l = 0; l < 2; l++) {
         trashl += `${trashl.length + 1}`;
      }
          let searchbar1 = String.fromCharCode(115,101,101,107,105,110,103,95,113,95,49,56,0);
          let libglogg = String.fromCharCode(109,111,118,101,100,95,118,95,53,54,0);
         trashl = `${searchbar1.length >> (Math.min(4, trashl.length))}`;
         searchbar1 = `${libglogg.length & libglogg.length}`;
         trashl = `${trashl.length - 3}`;
      iconsaveimager += `${2 - hooksF.length}`;
   }
   if (ajaxZ != String.fromCharCode(119,0)) {
      codeI += `${iconsaveimager.length >> (Math.min(2, hooksF.length))}`;
   }
   while (2 > grayq.length) {
      grayq.push(hooksF.length);
      break;
   }
       let questiconk = 1;
       let classesW = 0;
       let valuesK = String.fromCharCode(100,95,50,55,95,110,99,114,121,112,116,105,111,110,0);
      for (let s = 0; s < 1; s++) {
          let whistleZ = 1;
          let point0 = 0.0;
         classesW -= (String.fromCharCode(116,0) == valuesK ? valuesK.length : classesW);
         whistleZ += 2;
         point0 /= Math.max(5, parseFloat(`${whistleZ - 2}`));
      }
      let policye = String.fromCharCode(56,49,112,0) == valuesK;
      do {
         valuesK += "1";
         if (policye) {
            break;
         }
      } while ((5 <= (questiconk % (Math.max(valuesK.length, 4))) || (valuesK.length % (Math.max(5, 7))) <= 2) && policye);
       let submitU = 2.0;
      if (2 >= (classesW % 2) || 2 >= (questiconk % 2)) {
         questiconk |= parseInt(`${submitU}`) | questiconk;
      }
       let zhengpian4 = String.fromCharCode(112,111,111,108,115,95,104,95,55,55,0);
      while (4 == valuesK.length) {
         valuesK += `${parseInt(`${submitU}`)}`;
         break;
      }
      if ((zhengpian4.length | questiconk) <= 2) {
         zhengpian4 = `${3 / (Math.max(10, classesW))}`;
      }
          let textinput9 = true;
         submitU /= Math.max(parseFloat(`${parseInt(`${submitU}`)}`), 1);
         textinput9 = !textinput9;
          let empty2 = 3.0;
         classesW += 1;
         empty2 += parseInt(`${empty2}`);
      codeI = `${parseInt(`${reactnavigationj}`) | 2}`;
   while (4.94 < renewJ) {
      renewJ /= Math.max(iconsaveimager.length, 1);
      break;
   }
   let codeX = 7078793 <= grayq.length;
   do {
      grayq.push(1 - parseInt(`${renewJ}`));
      if (codeX) {
         break;
      }
   } while (codeX && (!grayq.includes(release_ioC)));
   for (let f = 0; f < 3; f++) {
       let spec2 = String.fromCharCode(117,95,56,53,95,109,117,108,116,105,112,108,105,101,100,0);
       let unselectedy: Array<any> = [618, 518, 870];
       let gdtadvf = String.fromCharCode(108,95,56,55,95,101,118,101,110,116,115,0);
       let libzeusf = String.fromCharCode(102,95,53,54,95,108,116,97,98,108,101,0);
          let mintegral2 = 0.0;
          let libaneF = 0.0;
          let pausem = String.fromCharCode(116,114,97,110,115,112,97,114,101,110,99,121,95,49,95,49,48,0);
         unselectedy = [3 ^ pausem.length];
         mintegral2 += parseFloat(`${parseInt(`${libaneF}`) | parseInt(`${mintegral2}`)}`);
         libaneF -= parseInt(`${mintegral2}`) << (Math.min(3, Math.abs(parseInt(`${libaneF}`))));
         pausem += `${3 + parseInt(`${libaneF}`)}`;
      let utils8 = unselectedy.length <= 6889108;
      do {
         unselectedy = [libzeusf.length - 1];
         if (utils8) {
            break;
         }
      } while (((libzeusf.length ^ unselectedy.length) <= 3) && utils8);
      while (spec2.length <= gdtadvf.length) {
         spec2 += `${libzeusf.length}`;
         break;
      }
         unselectedy.push((libzeusf == String.fromCharCode(118,0) ? libzeusf.length : unselectedy.length));
         unselectedy.push(unselectedy.length);
      while (2 < (spec2.length * 1)) {
         unselectedy.push(gdtadvf.length >> (Math.min(1, spec2.length)));
         break;
      }
         gdtadvf = `${(String.fromCharCode(54,0) == gdtadvf ? gdtadvf.length : unselectedy.length)}`;
         gdtadvf += `${spec2.length}`;
       let layoutM = true;
      while (spec2.length <= gdtadvf.length) {
          let libavcodect: Map<any, any> = new Map([[String.fromCharCode(104,95,55,53,95,114,101,111,114,100,101,114,97,98,108,101,0),327], [String.fromCharCode(103,95,50,53,95,109,105,115,99,0),494]]);
          let turns: Map<any, any> = new Map([[String.fromCharCode(99,104,97,110,103,101,100,104,95,98,95,56,51,0),805], [String.fromCharCode(119,95,55,51,95,109,111,100,101,110,97,109,101,0),158], [String.fromCharCode(117,110,119,114,97,112,95,52,95,50,55,0),939]]);
          let stations1 = 0.0;
          let temperatureF = 2;
         spec2 += `${spec2.length * 2}`;
         libavcodect = new Map([[`${libavcodect.size}`, 2]]);
         turns.set(`${stations1}`, 1);
         stations1 -= temperatureF / (Math.max(libavcodect.size, 9));
         temperatureF <<= Math.min(5, Math.abs(2));
         break;
      }
      let rncore7 = String.fromCharCode(108,95,50,118,111,118,104,57,53,111,0) == libzeusf;
      do {
         libzeusf = `${((layoutM ? 1 : 4) * spec2.length)}`;
         if (rncore7) {
            break;
         }
      } while (((3 - unselectedy.length) == 5) && rncore7);
      let promotion_ = unselectedy.length >= 6261698;
      do {
          let iconbackwhiteS = 1.0;
          let shielddones = 4.0;
         unselectedy.push(((layoutM ? 3 : 4) % 2));
         iconbackwhiteS /= Math.max(1, parseFloat(`${2}`));
         shielddones -= parseInt(`${iconbackwhiteS}`) - parseInt(`${shielddones}`);
         if (promotion_) {
            break;
         }
      } while ((2 >= (unselectedy.length >> (Math.min(Math.abs(4), 5))) && (spec2.length >> (Math.min(3, unselectedy.length))) >= 4) && promotion_);
      nativeexB += `${((libavdevicek ? 1 : 3))}`;
   }
   let hiad6 = grayq.length <= 8701023;
   do {
      grayq = [ajaxZ.length >> (Math.min(Math.abs(3), 1))];
      if (hiad6) {
         break;
      }
   } while (((4 - grayq.length) <= 4 || (4 - codeI.length) <= 3) && hiad6);
   if ((3 << (Math.min(2, ajaxZ.length))) < 3) {
      ajaxZ += `${((videojsn ? 1 : 3) % (Math.max(1, 9)))}`;
   }
       let settingsv = 4.0;
       let carousel4: Array<any> = [884, 8];
       let libavcodecT = String.fromCharCode(120,95,55,56,95,100,105,102,102,115,0);
      let privilege0 = 5138177.0 <= settingsv;
      do {
          let diceQ = String.fromCharCode(109,95,57,57,95,100,105,97,109,101,116,101,114,0);
          let singleP = 0;
          let predictionT = 4.0;
          let attributedstring9 = true;
          let thailand7 = 1;
         settingsv /= Math.max(5, ((attributedstring9 ? 3 : 3) & parseInt(`${settingsv}`)));
         diceQ = `${1 >> (Math.min(1, diceQ.length))}`;
         singleP &= thailand7 ^ 2;
         predictionT /= Math.max(5, (diceQ == String.fromCharCode(83,0) ? singleP : diceQ.length));
         attributedstring9 = 43 >= (singleP / (Math.max(1, predictionT)));
         thailand7 /= Math.max(5, (String.fromCharCode(122,0) == diceQ ? diceQ.length : parseInt(`${predictionT}`)));
         if (privilege0) {
            break;
         }
      } while (privilege0 && ((carousel4.length & 4) <= 2 || 4 <= (4 * carousel4.length)));
         libavcodecT = `${2 * libavcodecT.length}`;
      if (carousel4.length < libavcodecT.length) {
         libavcodecT += `${parseInt(`${settingsv}`) | 3}`;
      }
      while (libavcodecT.length <= carousel4.length) {
         carousel4.push(carousel4.length + 2);
         break;
      }
         settingsv -= parseInt(`${settingsv}`);
         libavcodecT = `${libavcodecT.length}`;
         libavcodecT = `${carousel4.length}`;
         settingsv -= (libavcodecT == String.fromCharCode(66,0) ? libavcodecT.length : carousel4.length);
          let yellowvideoliveb = String.fromCharCode(99,114,105,116,105,99,97,108,95,56,95,51,52,0);
          let libpangleflippedO: Array<any> = [205, 732];
          let librrcH = String.fromCharCode(98,95,56,57,95,97,100,100,0);
         carousel4 = [yellowvideoliveb.length];
         yellowvideoliveb = `${(librrcH == String.fromCharCode(49,0) ? libpangleflippedO.length : librrcH.length)}`;
         libpangleflippedO = [librrcH.length];
      ajaxZ = `${2 - parseInt(`${release_ioC}`)}`;
      videojsn = codeI == String.fromCharCode(74,0);
   if (nativeexB.length > ajaxZ.length) {
      ajaxZ = `${(parseInt(`${renewJ}`) % (Math.max(2, (libavdevicek ? 5 : 3))))}`;
   }
      grayq.push(parseInt(`${release_ioC}`) / 2);
       let baselineh: Map<any, any> = new Map([[String.fromCharCode(119,95,48,95,112,111,115,116,114,101,113,117,101,115,116,0),String.fromCharCode(99,117,114,108,95,115,95,56,52,0)], [String.fromCharCode(102,97,108,108,111,102,102,95,110,95,57,51,0),String.fromCharCode(100,95,51,51,95,115,109,117,115,104,0)], [String.fromCharCode(103,95,56,57,95,114,101,112,115,116,114,0),String.fromCharCode(117,110,105,116,95,100,95,54,0)]]);
       let common2 = String.fromCharCode(114,95,52,55,95,116,122,102,105,108,101,0);
       let preview_ = 1.0;
         common2 += `${common2.length}`;
          let orangeclock0 = String.fromCharCode(109,97,116,99,104,101,95,114,95,57,54,0);
          let statisticsinactiveH = 0.0;
          let bodan9 = 5;
         preview_ *= parseFloat(`${parseInt(`${statisticsinactiveH}`) >> (Math.min(common2.length, 2))}`);
         orangeclock0 = `${bodan9 * 2}`;
         statisticsinactiveH *= parseFloat(`${orangeclock0.length}`);
         bodan9 += 2;
          let shootyesgoal9: Map<any, any> = new Map([[String.fromCharCode(103,95,54,49,95,112,101,114,105,111,100,105,99,0),431], [String.fromCharCode(116,95,51,51,95,118,97,114,105,97,110,99,101,120,0),478]]);
         preview_ /= Math.max(3, parseFloat(`${2 / (Math.max(7, common2.length))}`));
         shootyesgoal9 = new Map([[`${shootyesgoal9.size}`, shootyesgoal9.size]]);
      if (!common2.endsWith(`${preview_}`)) {
         common2 = `${parseInt(`${preview_}`) | common2.length}`;
      }
      let fullA = 7239586 <= baselineh.size;
      do {
         baselineh.set(common2, 2);
         if (fullA) {
            break;
         }
      } while (fullA && (5 > (3 ^ common2.length) || 3 > (common2.length ^ baselineh.size)));
      for (let z = 0; z < 1; z++) {
          let viewer5 = 0;
          let utilsu = String.fromCharCode(102,97,116,101,95,57,95,50,48,0);
          let floatingh = false;
         preview_ += (parseFloat(`${utilsu == String.fromCharCode(55,0) ? viewer5 : utilsu.length}`));
         viewer5 *= 3;
      }
         baselineh = new Map([[common2, parseInt(`${preview_}`)]]);
          let subtexts = String.fromCharCode(112,108,97,99,101,95,111,95,56,53,0);
          let videovarq = String.fromCharCode(108,95,49,48,48,95,114,101,97,115,111,110,115,0);
          let flipperS = 0.0;
         baselineh = new Map([[`${preview_}`, common2.length]]);
         subtexts += `${videovarq.length}`;
         videovarq = `${subtexts.length & 2}`;
         flipperS += videovarq.length;
       let libreactnativeblobT = String.fromCharCode(103,114,97,110,117,108,101,112,111,115,95,119,95,54,48,0);
      release_ioC /= Math.max(5, nativeexB.length & 2);
   if ((renewJ / 3.95) >= 5.17) {
       let searchV = false;
       let skipF = false;
       let foregroundN = 2.0;
      for (let s = 0; s < 1; s++) {
         skipF = (skipF ? searchV : !skipF);
      }
      for (let a = 0; a < 3; a++) {
          let penaltygoalk = String.fromCharCode(109,95,53,51,95,119,97,118,114,101,97,100,101,114,0);
          let fastL = String.fromCharCode(118,95,57,53,95,101,102,102,101,99,116,105,118,101,0);
          let notificationl = String.fromCharCode(99,104,101,99,107,97,115,109,95,99,95,51,54,0);
          let libtanG = String.fromCharCode(114,95,57,48,95,115,101,99,117,114,105,116,121,0);
         skipF = !searchV;
         penaltygoalk = `${fastL.length & notificationl.length}`;
         fastL = `${(libtanG == String.fromCharCode(87,0) ? libtanG.length : notificationl.length)}`;
      }
          let selecteds = false;
         skipF = 84.14 < foregroundN;
         selecteds = !selecteds;
       let flagh = String.fromCharCode(103,101,116,116,105,109,101,111,102,100,97,121,95,97,95,52,54,0);
      while (!searchV || 4 > flagh.length) {
         flagh += `${((skipF ? 5 : 4))}`;
         break;
      }
          let turndown1 = 0;
         flagh = `${3 & flagh.length}`;
         turndown1 &= 3 | turndown1;
          let baiduv = String.fromCharCode(101,120,112,105,114,101,115,95,54,95,49,48,0);
          let readb: Map<any, any> = new Map([[String.fromCharCode(109,111,114,112,104,101,100,95,106,95,50,53,0),false ], [String.fromCharCode(116,95,54,55,95,108,105,98,115,119,105,102,116,111,115,0),false ]]);
         skipF = baiduv.length < flagh.length;
         baiduv = `${readb.size}`;
         readb.set(`${readb.size}`, readb.size);
         foregroundN += 3 * flagh.length;
         flagh = `${flagh.length >> (Math.min(Math.abs(3), 3))}`;
      sharedG = [1];
   }
       let uploadN = 3.0;
       let livenodatabgimgT = 0.0;
       let klevinQ = String.fromCharCode(105,95,56,48,95,102,111,108,108,111,119,101,114,115,0);
      let headerB = livenodatabgimgT >= 7347735.0;
      do {
         livenodatabgimgT += parseInt(`${uploadN}`);
         if (headerB) {
            break;
         }
      } while (headerB && (livenodatabgimgT < uploadN));
          let floater1 = 1.0;
          let baiduM: Array<any> = [948, 500, 195];
          let spinner4: Map<any, any> = new Map([[String.fromCharCode(101,95,53,56,95,105,109,112,108,105,101,115,0),false ], [String.fromCharCode(102,108,97,103,95,53,95,50,51,0),true ], [String.fromCharCode(115,95,56,56,95,99,104,97,110,103,101,104,0),false ]]);
         klevinQ = `${klevinQ.length + parseInt(`${livenodatabgimgT}`)}`;
         floater1 *= parseInt(`${floater1}`) >> (Math.min(3, Math.abs(1)));
         baiduM.push(baiduM.length);
         spinner4.set(`${baiduM.length}`, baiduM.length);
      if (2 >= (parseInt(`${livenodatabgimgT}`) / (Math.max(klevinQ.length, 4)))) {
         klevinQ += "3";
      }
       let over8 = String.fromCharCode(117,110,115,112,105,108,108,95,114,95,49,55,0);
       let backa = String.fromCharCode(98,97,115,107,101,116,98,97,108,108,95,100,95,51,53,0);
      let storeq = String.fromCharCode(97,116,105,99,50,56,0) == klevinQ;
      do {
         klevinQ += `${(klevinQ == String.fromCharCode(73,0) ? backa.length : klevinQ.length)}`;
         if (storeq) {
            break;
         }
      } while (storeq && ((livenodatabgimgT / (Math.max(3.0, 2))) > 4.4));
         over8 += `${parseInt(`${uploadN}`) | 2}`;
          let privacyq = true;
         livenodatabgimgT += ((privacyq ? 4 : 3));
      if (5 < backa.length) {
          let empty4 = String.fromCharCode(101,97,105,100,99,116,95,98,95,49,56,0);
          let langkeyf = 2.0;
          let androidf = false;
          let buildp = String.fromCharCode(119,95,51,50,95,105,99,111,110,0);
          let bootr: Array<any> = [502, 743, 31];
         backa += `${parseInt(`${livenodatabgimgT}`)}`;
         empty4 = `${bootr.length}`;
         langkeyf -= 3 >> (Math.min(4, empty4.length));
         androidf = !androidf;
         buildp += `${3 + parseInt(`${langkeyf}`)}`;
         bootr.push(buildp.length);
      }
      let benefitj = 7283742 <= backa.length;
      do {
         backa += `${parseInt(`${livenodatabgimgT}`)}`;
         if (benefitj) {
            break;
         }
      } while ((!over8.startsWith(`${backa.length}`)) && benefitj);
      reactnavigationj -= parseInt(`${reactnavigationj}`) - parseInt(`${release_ioC}`);
   while (ajaxZ.length < 5) {
      iconsaveimager = "3";
      break;
   }
   for (let i = 0; i < 1; i++) {
      grayq.push(2 * iconsaveimager.length);
   }
       let androidQ = true;
      if (!androidQ) {
         androidQ = !androidQ;
      }
       let shrinky = String.fromCharCode(115,117,98,102,105,101,108,100,115,95,109,95,53,54,0);
       let minivodP = String.fromCharCode(102,97,118,101,100,95,57,95,54,48,0);
      if (!minivodP.startsWith(shrinky)) {
         minivodP = `${minivodP.length}`;
      }
      ajaxZ += `${(2 / (Math.max(9, (androidQ ? 5 : 1))))}`;
   let bootK = release_ioC <= 9479961.0;
   do {
      release_ioC /= Math.max(4, 3 & nativeexB.length);
      if (bootK) {
         break;
      }
   } while ((ajaxZ.length > 5) && bootK);
   let speco = 9230272 <= nativeexB.length;
   do {
      nativeexB = `${codeI.length}`;
      if (speco) {
         break;
      }
   } while ((iconsaveimager == String.fromCharCode(120,0)) && speco);
   while (videojsn && 3 == codeI.length) {
      videojsn = (ajaxZ.length >> (Math.min(nativeexB.length, 3))) >= 98;
      break;
   }
       let fillK = String.fromCharCode(114,101,99,111,114,100,97,98,108,101,95,111,95,56,48,0);
      if (fillK == String.fromCharCode(52,0) && fillK.length >= 3) {
          let faviconG = String.fromCharCode(104,95,55,49,95,114,101,116,114,105,101,118,101,100,0);
          let activeO: Array<any> = [777, 97];
          let sigmobe: Array<any> = [327, 171];
         fillK = `${faviconG.length & fillK.length}`;
         faviconG = `${activeO.length}`;
         activeO = [activeO.length];
         sigmobe = [sigmobe.length << (Math.min(Math.abs(3), 4))];
      }
       let friends2 = String.fromCharCode(105,95,52,51,95,109,118,101,99,0);
      if (5 >= friends2.length) {
         fillK = "2";
      }
      codeI += `${((libavdevicek ? 4 : 4))}`;
      sharedG.push(1);

    setImgUrl(useNextImage())
  }

  if (error) { // when all url error, use the default placeholder image
    return (
      <View style={[{ backgroundColor: colors.loading, ...styles.loadingCard}, imageProp.style]}>
          <LoadingImage  width={20}  height={20}/>
      </View>
    )
  }

  if (useFastImage === true || Platform.OS === "android"){
    if (typeof imageProp.source == 'number'){ // if source={require(...)}
    return <FastImage {...imageProp as FastImageProps}/>
    } else {
      return <FastImage key={current.toString() + imgUrl} {...imageProp as FastImageProps} source={{uri: imgUrl}} onError={handleOnError}/>
    }
  } else {
    if (typeof imageProp.source == 'number'){ // if source={require(...)}
      return <Image {...imageProp as wwEventSigninup}/>
    } else { // if source={{uri:...}}
      return <Image key={current.toString() + imgUrl} {...imageProp as wwEventSigninup} source={{...(imageProp.source as ImageURISource), uri: imgUrl}} onError={handleOnError}/>
    }
  }
}

const styles = StyleSheet.create({
  loadingCard: {
    marginRight: 20,
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    borderRadius: 10,
},
})

export default customFastImage;