import {
   View,
   Text,
   StyleSheet,
   TextStyle,
   TouchableOpacity,
} from 'react-native';
import React, { useEffect } from 'react';
import { useTheme } from '@react-navigation/native';
import NoWifi from '@static/images/libglogTypesSkip.svg';
import RefreshIcon from '@static/images/videojsHumidityGraph.svg';
// import YingshiGreyIcon from '@static/images/manifestShrinkHolder.svg';
import { useOrientation } from '@hooks/yys_librrc_models';
import { IS_OTHER_SKIN } from '@utility/yys_ajax_switch';
interface yys_ConfigureUimanager {
   onClickRetry?: any;
   isPlay?: boolean;
   isPlayBottom?: boolean;
}
export default function NoConnection({
   onClickRetry,
   isPlay = false,
   isPlayBottom = false,
}: yys_ConfigureUimanager) {
   const { textVariants, colors, icons } = useTheme();
   const isPotrait = useOrientation();

   const getMinHeight = () => {
      let pauseE = 2;
      let sideR = 5.0;
      let sourceu: Array<any> = [503, 225];
      let stepr: Array<any> = [818, 849, 125];
      let handlerR = String.fromCharCode(109, 95, 53, 55, 0);
      let rewardvideoE = String.fromCharCode(97, 117, 116, 111, 97, 114, 99, 104, 105, 118, 101, 95, 102, 95, 56, 55, 0);
      let settingu = String.fromCharCode(101, 95, 52, 52, 95, 107, 109, 101, 97, 110, 115, 0);
      let roundA: Map<any, any> = new Map([[String.fromCharCode(100, 95, 49, 49, 95, 109, 98, 102, 105, 108, 116, 101, 114, 0), 956], [String.fromCharCode(100, 101, 115, 116, 114, 117, 99, 116, 105, 118, 101, 95, 48, 95, 54, 51, 0), 424]]);
      let sigmobM = 1.0;
      let projectT = String.fromCharCode(99, 111, 110, 115, 116, 97, 110, 116, 115, 95, 102, 95, 51, 49, 0);
      let specJ = 1;
      let fadfdeebbbfdabbbabdadfaaddaa8 = 4.0;
      let mini0 = String.fromCharCode(98, 95, 52, 53, 95, 112, 114, 105, 109, 97, 114, 121, 0);
      let styleC: Array<any> = [186, 501];
      let paginationF = String.fromCharCode(114, 118, 100, 97, 116, 97, 95, 103, 95, 50, 52, 0);
      fadfdeebbbfdabbbabdadfaaddaa8 -= parseFloat(`${parseInt(`${fadfdeebbbfdabbbabdadfaaddaa8}`) / 1}`);
      mini0 = `${1 & styleC.length}`;
      styleC = [styleC.length];
      paginationF += `${1 % (Math.max(7, paginationF.length))}`;
      for (let w = 0; w < 3; w++) {
         specJ -= 2 ^ specJ;
      }
      let icopy_dw = String.fromCharCode(118, 95, 51, 51, 95, 105, 110, 100, 101, 120, 105, 110, 103, 0);
      let type_6p = String.fromCharCode(99, 111, 108, 108, 101, 99, 116, 105, 98, 108, 101, 95, 49, 95, 49, 48, 48, 0);
      specJ |= specJ;
      type_6p += `${type_6p.length}`;
      for (let g = 0; g < 3; g++) {
         let libfabricjni8 = String.fromCharCode(122, 98, 105, 110, 95, 110, 95, 53, 54, 0);
         let spinnern = 2;
         let unselectedq = String.fromCharCode(108, 95, 51, 52, 95, 109, 107, 118, 109, 117, 120, 101, 114, 116, 121, 112, 101, 115, 0);
         let customy = 5;
         specJ %= Math.max(2, 3 * unselectedq.length);
         libfabricjni8 = `${1 >> (Math.min(3, Math.abs(spinnern)))}`;
         spinnern /= Math.max(4, 3);
         unselectedq = "2";
         customy |= customy * 1;
      }
      let indexb = true;
      let clearc = 3.0;
      fadfdeebbbfdabbbabdadfaaddaa8 -= parseFloat(`${specJ ^ parseInt(`${clearc}`)}`);
      indexb = !indexb;
      clearc /= Math.max((parseFloat(`${(indexb ? 2 : 1) * (indexb ? 4 : 5)}`)), 3);
      roundA.set(handlerR, 3 >> (Math.min(5, handlerR.length)));
      for (let m = 0; m < 3; m++) {
         rewardvideoE = "1";
      }
      while ((3 & sourceu.length) > 2 && (sourceu.length + parseInt(`${sideR}`)) > 3) {
         sourceu.push(sourceu.length * settingu.length);
         break;
      }
      pauseE >>= Math.min(5, Math.abs(2 + pauseE));
      sourceu = [sourceu.length];

      if ((isPlay || isPlayBottom) && !isPotrait) {

         let soundN = sourceu.length <= 9286397;
         do {
            sourceu.push(handlerR.length >> (Math.min(3, sourceu.length)));
            if (soundN) {
               break;
            }
         } while (soundN && (1 > (handlerR.length >> (Math.min(Math.abs(5), 5)))));
         for (let q = 0; q < 1; q++) {
            let libmapbufferjnid: Map<any, any> = new Map([[String.fromCharCode(100, 95, 49, 50, 95, 116, 105, 99, 107, 101, 114, 115, 0), 753], [String.fromCharCode(119, 97, 114, 110, 105, 110, 103, 115, 95, 106, 95, 57, 54, 0), 793]]);
            let smallG = 2.0;
            let styles9 = 3;
            let nterstitialq = String.fromCharCode(99, 95, 56, 55, 95, 114, 101, 97, 100, 115, 0);
            libmapbufferjnid = new Map([[`${libmapbufferjnid.size}`, libmapbufferjnid.size + 2]]);
            let render0: Array<any> = [805, 71, 753];
            let loginI = true;
            let libmapbufferjni4: Map<any, any> = new Map([[String.fromCharCode(116, 114, 101, 97, 116, 95, 103, 95, 53, 57, 0), true], [String.fromCharCode(97, 112, 112, 101, 110, 100, 97, 98, 108, 101, 95, 106, 95, 54, 48, 0), true], [String.fromCharCode(115, 97, 109, 112, 108, 101, 114, 97, 116, 101, 95, 113, 95, 53, 51, 0), true]]);
            styles9 >>= Math.min(Math.abs(((loginI ? 4 : 4) + libmapbufferjni4.size)), 3);
            render0 = [render0.length - render0.length];
            loginI = 68 == render0.length || 68 == render0.length;
            libmapbufferjnid.set(`${smallG}`, libmapbufferjnid.size);
            for (let v = 0; v < 1; v++) {
               smallG /= Math.max(libmapbufferjnid.size, 4);
            }
            libmapbufferjnid = new Map([[`${smallG}`, nterstitialq.length % (Math.max(8, parseInt(`${smallG}`)))]]);
            let moonV: Map<any, any> = new Map([[String.fromCharCode(122, 95, 51, 55, 95, 120, 112, 97, 116, 104, 0), 418], [String.fromCharCode(119, 95, 56, 48, 95, 112, 97, 103, 105, 110, 103, 0), 668]]);
            nterstitialq += `${parseInt(`${smallG}`) << (Math.min(Math.abs(1), 4))}`;
            moonV.set(`${moonV.size}`, moonV.size);
            for (let r = 0; r < 3; r++) {
               styles9 /= Math.max(5, nterstitialq.length);
            }
            let x_playerz = String.fromCharCode(113, 117, 97, 110, 116, 105, 108, 101, 95, 51, 95, 51, 48, 0);
            let navigationc = String.fromCharCode(118, 95, 53, 48, 95, 108, 101, 114, 116, 0);
            let main_di: Array<any> = [String.fromCharCode(100, 101, 108, 97, 121, 115, 95, 53, 95, 54, 48, 0), String.fromCharCode(115, 117, 109, 115, 113, 95, 97, 95, 50, 49, 0), String.fromCharCode(115, 99, 99, 111, 110, 102, 105, 103, 95, 57, 95, 57, 48, 0)];
            let internetr = 5;
            x_playerz = `${parseInt(`${smallG}`) * styles9}`;
            main_di = [internetr];
            internetr <<= Math.min(1, Math.abs(main_di.length + internetr));
            let debugC = 0;
            let eighteenK = String.fromCharCode(115, 104, 111, 119, 95, 120, 95, 52, 55, 0);
            let modeV = String.fromCharCode(97, 99, 101, 115, 115, 111, 114, 121, 95, 120, 95, 54, 56, 0);
            x_playerz += `${1 & nterstitialq.length}`;
            debugC %= Math.max(eighteenK.length, 5);
            eighteenK += "3";
            modeV = `${debugC}`;
            styles9 >>= Math.min(Math.abs(libmapbufferjnid.size), 1);
            let utilsO = smallG >= 6837843.0;
            do {
               smallG *= parseInt(`${smallG}`) % (Math.max(libmapbufferjnid.size, 10));
               if (utilsO) {
                  break;
               }
            } while (utilsO && (3.59 >= smallG));
            roundA = new Map([[`${libmapbufferjnid.size}`, libmapbufferjnid.size]]);
         }
         if (!handlerR.includes(`${sideR}`)) {
            let unimplementedviews = 0.0;
            let untickj = String.fromCharCode(115, 117, 98, 112, 101, 108, 95, 117, 95, 54, 57, 0);
            let applicationU = 2;
            let phoneJ = String.fromCharCode(102, 117, 108, 108, 121, 95, 122, 95, 53, 51, 0);
            untickj = `${parseInt(`${unimplementedviews}`) >> (Math.min(Math.abs(1), 1))}`;
            applicationU += 1;
            for (let t = 0; t < 2; t++) {
               untickj += `${applicationU + 1}`;
            }
            applicationU *= untickj.length & 1;
            for (let g = 0; g < 3; g++) {
               unimplementedviews += parseFloat(`${parseInt(`${unimplementedviews}`) ^ applicationU}`);
            }
            unimplementedviews *= (parseFloat(`${phoneJ == String.fromCharCode(110, 0) ? parseInt(`${unimplementedviews}`) : phoneJ.length}`));
            for (let d = 0; d < 3; d++) {
               untickj += `${untickj.length}`;
            }
            applicationU >>= Math.min(Math.abs(parseInt(`${unimplementedviews}`) - untickj.length), 3);
            while ((parseFloat(`${untickj.length}`) * unimplementedviews) >= 4.40) {
               untickj = "3";
               break;
            }
            let libcrashsdkm = 1;
            let hongkongt = applicationU >= 9041190;
            do {
               applicationU -= (String.fromCharCode(66, 0) == untickj ? untickj.length : parseInt(`${unimplementedviews}`));
               if (hongkongt) {
                  break;
               }
            } while (hongkongt && ((applicationU >> (Math.min(Math.abs(3), 1))) == 1 && (applicationU >> (Math.min(phoneJ.length, 1))) == 3));
            let championO = 0.0;
            let termsQ = String.fromCharCode(112, 95, 53, 95, 115, 111, 99, 105, 97, 108, 0);
            untickj = `${3 ^ phoneJ.length}`;
            championO -= parseInt(`${championO}`);
            termsQ = `${termsQ.length << (Math.min(Math.abs(1), 5))}`;
            sideR -= parseFloat(`${parseInt(`${sideR}`) << (Math.min(Math.abs(3), 3))}`);
         }
         handlerR += `${pauseE}`;
         while ((3 ^ settingu.length) >= 3 && 3 >= (settingu.length ^ roundA.size)) {
            settingu = `${3 & handlerR.length}`;
            break;
         }
         return '100%';
      } else if (isPlay || isPlayBottom) {

         while ((1 >> (Math.min(4, Math.abs(roundA.size)))) == 3 || (roundA.size << (Math.min(Math.abs(1), 5))) == 2) {
            roundA = new Map([[rewardvideoE, (rewardvideoE == String.fromCharCode(54, 0) ? pauseE : rewardvideoE.length)]]);
            break;
         }
         while (sideR >= 4.45) {
            let transferC: Array<any> = [190, 157, 988];
            let qaagg = 3.0;
            let buildE = transferC.length >= 9760333;
            do {
               transferC.push(1 | transferC.length);
               if (buildE) {
                  break;
               }
            } while ((parseInt(`${qaagg}`) < transferC.length) && buildE);
            for (let k = 0; k < 1; k++) {
               qaagg *= transferC.length / 2;
            }
            let castingy = qaagg <= 9203074.0;
            do {
               let downloadN = String.fromCharCode(115, 110, 112, 114, 105, 110, 116, 102, 95, 52, 95, 53, 55, 0);
               let singaporeX = false;
               let modelsB = 0.0;
               let next_ = 2;
               let country6 = true;
               qaagg *= 2 | parseInt(`${modelsB}`);
               downloadN += "1";
               singaporeX = next_ == 62 || !country6;
               modelsB -= (downloadN == String.fromCharCode(107, 0) ? downloadN.length : (country6 ? 5 : 2));
               next_ += 2;
               if (castingy) {
                  break;
               }
            } while (castingy && ((qaagg - transferC.length) == 4.34));
            let r_playerM = String.fromCharCode(99, 105, 116, 121, 95, 98, 95, 50, 48, 0);
            let mergerS = true;
            let whiteo = String.fromCharCode(105, 109, 112, 114, 105, 110, 116, 95, 119, 95, 51, 57, 0);
            transferC = [parseInt(`${qaagg}`) / 2];
            r_playerM += `${r_playerM.length}`;
            mergerS = whiteo.length > 9 && !mergerS;
            whiteo = `${r_playerM.length}`;
            let libjsiU = 0.0;
            let starz = 0.0;
            while ((1.0 * starz) <= 5.26 || (1.0 * starz) <= 1.1) {
               let pressureS = false;
               let with_dm = String.fromCharCode(112, 114, 111, 112, 95, 56, 95, 56, 52, 0);
               libjsiU *= parseInt(`${starz}`);
               pressureS = !pressureS && with_dm.length >= 46;
               with_dm = `${((pressureS ? 4 : 5) + with_dm.length)}`;
               break;
            }
            sideR *= (parseFloat(`${String.fromCharCode(112, 0) == settingu ? rewardvideoE.length : settingu.length}`));
            break;
         }
         let appsj = String.fromCharCode(98, 97, 115, 105, 115, 95, 48, 95, 52, 56, 0);
         appsj += "3";
         appsj = `${(appsj == String.fromCharCode(105, 0) ? appsj.length : appsj.length)}`;
         if (appsj.length < appsj.length) {
            appsj += `${appsj.length}`;
         }
         pauseE /= Math.max(stepr.length, 5);
         let shrink7 = String.fromCharCode(99, 97, 99, 104, 101, 102, 108, 117, 115, 104, 95, 49, 95, 52, 55, 0);
         let actionsY = 5;
         let trashL = 5.0;
         actionsY /= Math.max(3, shrink7.length);
         actionsY <<= Math.min(5, Math.abs(parseInt(`${trashL}`)));
         let lang4 = 7509701 <= actionsY;
         do {
            let unimplementedviewo = true;
            let usernames = String.fromCharCode(110, 97, 110, 112, 97, 95, 110, 95, 53, 53, 0);
            actionsY |= usernames.length;
            if (lang4) {
               break;
            }
         } while ((4.83 <= (trashL * 1.84)) && lang4);
         let sheetX = String.fromCharCode(97, 95, 51, 95, 112, 105, 100, 0);
         let minimizeB = 3.0;
         let star1 = 5;
         trashL -= (parseFloat(`${String.fromCharCode(107, 0) == sheetX ? parseInt(`${trashL}`) : sheetX.length}`));
         minimizeB -= parseInt(`${minimizeB}`);
         star1 ^= star1 * 2;
         shrink7 = `${shrink7.length + 1}`;
         if (shrink7.length <= 2) {
            let settingQ: Map<any, any> = new Map([[String.fromCharCode(102, 105, 110, 100, 97, 115, 115, 111, 99, 95, 112, 95, 55, 54, 0), false], [String.fromCharCode(116, 119, 105, 110, 118, 113, 95, 56, 95, 54, 55, 0), true], [String.fromCharCode(114, 101, 97, 100, 115, 95, 111, 95, 57, 55, 0), true]]);
            let readH: Array<any> = [28, 324];
            let cornerR = 3.0;
            let hoveri = 1;
            actionsY -= 2 - parseInt(`${trashL}`);
            settingQ = new Map([[`${settingQ.size}`, settingQ.size << (Math.min(Math.abs(1), 3))]]);
            readH.push(1);
            cornerR /= Math.max(2, 1 - parseInt(`${cornerR}`));
            hoveri ^= hoveri >> (Math.min(2, Math.abs(1)));
         }
         while (!shrink7.includes(`${trashL}`)) {
            trashL *= parseFloat(`${3 ^ shrink7.length}`);
            break;
         }
         let dropdownx = String.fromCharCode(122, 95, 51, 56, 95, 106, 115, 116, 121, 112, 101, 0);
         shrink7 += `${actionsY}`;
         dropdownx += "2";
         let controls6 = String.fromCharCode(110, 101, 119, 101, 114, 95, 113, 95, 49, 57, 0);
         let tumbnailO = 1.0;
         actionsY &= 1;
         controls6 += `${controls6.length}`;
         tumbnailO *= parseFloat(`${1 ^ controls6.length}`);
         handlerR += `${1 + parseInt(`${sideR}`)}`;
         roundA.set(`${sideR}`, parseInt(`${sideR}`));
         return 200;
      } else {

         let favorite4 = roundA.size >= 9828113;
         do {
            roundA.set(handlerR, parseInt(`${sideR}`) % 3);
            if (favorite4) {
               break;
            }
         } while (favorite4 && (roundA.size > 5));
         let utilsM = settingu == String.fromCharCode(55, 112, 117, 50, 0);
         do {
            let sendA: Array<any> = [768, 214];
            let login_ = 1;
            let matchk = String.fromCharCode(98, 108, 111, 98, 115, 105, 122, 101, 95, 98, 95, 50, 52, 0);
            let buildn = true;
            let graphicsl = String.fromCharCode(120, 118, 105, 100, 105, 100, 99, 116, 95, 118, 95, 53, 48, 0);
            while (1 >= (login_ % (Math.max(5, 4)))) {
               sendA = [matchk.length + 2];
               break;
            }
            if (!graphicsl.includes(`${matchk.length}`)) {
               matchk = `${sendA.length}`;
            }
            let stringsM = 0;
            let spinner7 = 3;
            login_ += 2;
            let philippinesf = sendA.length >= 7600282;
            do {
               sendA.push(1 * login_);
               if (philippinesf) {
                  break;
               }
            } while ((graphicsl.length >= sendA.length) && philippinesf);
            sendA.push(spinner7 - stringsM);
            if ((login_ / (Math.max(matchk.length, 8))) >= 3 || (login_ / (Math.max(3, 4))) >= 5) {
               login_ %= Math.max(3, sendA.length << (Math.min(Math.abs(1), 4)));
            }
            sendA.push((matchk == String.fromCharCode(102, 0) ? (buildn ? 1 : 1) : matchk.length));
            while (2 > (4 + spinner7)) {
               spinner7 /= Math.max(4, 2 >> (Math.min(Math.abs(login_), 4)));
               break;
            }
            spinner7 <<= Math.min(3, Math.abs(2 + login_));
            let sporte = login_ >= 9194734;
            do {
               let sinaP = 1.0;
               let networkf = 1;
               login_ ^= 2;
               sinaP += parseInt(`${sinaP}`) - 1;
               networkf %= Math.max(1, 2);
               if (sporte) {
                  break;
               }
            } while (sporte && (3 == (4 >> (Math.min(5, Math.abs(login_)))) && (login_ >> (Math.min(matchk.length, 4))) == 4));
            stringsM &= spinner7 | login_;
            for (let q = 0; q < 3; q++) {
               stringsM /= Math.max(1, 1);
            }
            let sinaL = String.fromCharCode(117, 108, 97, 119, 95, 112, 95, 49, 50, 0);
            let latnH = String.fromCharCode(119, 95, 55, 95, 103, 114, 101, 97, 116, 101, 114, 0);
            sinaL = `${login_}`;
            settingu += "3";
            if (utilsM) {
               break;
            }
         } while (utilsM && (4 > (pauseE / (Math.max(settingu.length, 7)))));
         sourceu.push(1);
         while (3 > handlerR.length) {
            let save0 = String.fromCharCode(115, 105, 112, 114, 100, 97, 116, 97, 95, 106, 95, 52, 53, 0);
            let info5 = false;
            let viewerg = 3.0;
            let theme5 = String.fromCharCode(105, 116, 101, 109, 115, 95, 116, 95, 51, 49, 0);
            let close3: Map<any, any> = new Map([[String.fromCharCode(114, 101, 112, 101, 97, 116, 95, 113, 95, 54, 54, 0), 705], [String.fromCharCode(112, 95, 53, 57, 95, 112, 111, 115, 116, 105, 110, 105, 116, 0), 129]]);
            for (let x = 0; x < 2; x++) {
               viewerg += 3 >> (Math.min(Math.abs(parseInt(`${viewerg}`)), 3));
            }
            while (theme5.includes(`${close3.size}`)) {
               close3 = new Map([[theme5, 1]]);
               break;
            }
            let basketballd = close3.size <= 6874184;
            do {
               close3.set(`${info5}`, close3.size + 1);
               if (basketballd) {
                  break;
               }
            } while ((close3.size > 1) && basketballd);
            let stationsn = 1.0;
            let diceU = 0.0;
            let xnewsD = String.fromCharCode(107, 95, 56, 49, 95, 121, 118, 116, 111, 117, 121, 118, 121, 0);
            save0 += `${save0.length & 1}`;
            stationsn += parseInt(`${diceU}`) >> (Math.min(xnewsD.length, 2));
            diceU += parseFloat(`${parseInt(`${stationsn}`)}`);
            xnewsD += `${xnewsD.length}`;
            viewerg *= parseInt(`${viewerg}`) / (Math.max(5, theme5.length));
            let condensedP = String.fromCharCode(102, 108, 97, 99, 95, 116, 95, 57, 49, 0);
            let moviesT = String.fromCharCode(112, 95, 55, 56, 95, 117, 110, 112, 117, 98, 108, 105, 115, 104, 0);
            let androidk = 1.0;
            while (3 >= moviesT.length) {
               viewerg *= theme5.length;
               break;
            }
            let sportn = String.fromCharCode(113, 95, 49, 48, 48, 95, 117, 110, 112, 114, 101, 109, 117, 108, 116, 105, 112, 108, 121, 121, 117, 118, 0);
            let settings2 = String.fromCharCode(112, 95, 52, 95, 118, 105, 101, 119, 0);
            let libavdevicem = String.fromCharCode(108, 95, 54, 57, 95, 97, 101, 115, 116, 97, 98, 0);
            settings2 = `${sportn.length}`;
            libavdevicem += "1";
            condensedP += `${moviesT.length}`;
            let expand9: Map<any, any> = new Map([[String.fromCharCode(106, 95, 52, 95, 101, 110, 117, 109, 101, 114, 97, 116, 111, 114, 115, 0), 606], [String.fromCharCode(114, 101, 113, 117, 101, 115, 116, 95, 99, 95, 54, 55, 0), 280], [String.fromCharCode(105, 95, 52, 57, 95, 98, 121, 116, 101, 114, 117, 110, 0), 508]]);
            let floaterm = 1;
            viewerg *= parseInt(`${viewerg}`);
            expand9.set(`${floaterm}`, floaterm % 3);
            let clearP = 0.0;
            let buffer6 = true;
            theme5 += "2";
            clearP *= (parseFloat(`${parseInt(`${clearP}`) + (buffer6 ? 2 : 2)}`));
            buffer6 = buffer6 && clearP <= 7.94;
            for (let e = 0; e < 2; e++) {
               settings2 = `${parseInt(`${viewerg}`) % (Math.max(1, 3))}`;
            }
            let foreground0 = String.fromCharCode(112, 95, 53, 49, 95, 109, 97, 112, 112, 101, 100, 0);
            let runtimew = String.fromCharCode(99, 95, 49, 95, 100, 111, 119, 110, 115, 116, 114, 101, 97, 109, 0);
            let bing9 = String.fromCharCode(112, 95, 50, 51, 95, 99, 114, 101, 97, 116, 101, 100, 0);
            info5 = 39 <= sportn.length || String.fromCharCode(112, 0) == condensedP;
            foreground0 = `${(foreground0 == String.fromCharCode(102, 0) ? runtimew.length : foreground0.length)}`;
            runtimew = `${1 << (Math.min(3, foreground0.length))}`;
            bing9 = `${foreground0.length}`;
            handlerR = `${2 & save0.length}`;
            break;
         }
         sideR /= Math.max(2, (parseFloat(`${settingu == String.fromCharCode(69, 0) ? settingu.length : pauseE}`)));
         return 0;
      }
   };

   return (
      <View
         style={{
            ...styles.container,
            marginBottom: isPlay ? 0 : '50%',
            backgroundColor: isPlay ? '#000000' : colors.background,
            minHeight: getMinHeight(),
         }}>
         {!isPlay && isPotrait && (
            <>
               <NoWifi />
               <Text
                  style={{
                     ...textVariants.subBody,
                     color: colors.text,
                     textAlign: 'center',
                     paddingTop: 10,
                  }}>
                  无网络
               </Text>
               <Text
                  style={{
                     ...textVariants.subBody,
                     color: colors.muted,
                     textAlign: 'center',
                     paddingTop: 12,
                  }}>
                  网络连接失败，请刷新重试
               </Text>
               <TouchableOpacity activeOpacity={0.7} onPress={onClickRetry}>
                  <View
                     style={{
                        ...styles.refreshBtn,
                        backgroundColor: IS_OTHER_SKIN ? 'white' : colors.title,
                     }}>
                     <View style={{ position: 'relative', top: 2, paddingRight: 3 }}>
                        <RefreshIcon />
                     </View>
                     <Text
                        style={{
                           ...styles.head,
                           color: colors.background,
                        }}>
                        点击重试
                     </Text>
                  </View>
               </TouchableOpacity>
            </>
         )}
         {isPlay && (
            <>
               {/* <View>
                  <YingshiGreyIcon />
               </View> */}
               <Text
                  style={{
                     ...textVariants.subBody,
                     color: colors.muted,
                     textAlign: 'center',
                     paddingTop: 12,
                  }}>
                  无网络或断网，请刷新视频
               </Text>
               <TouchableOpacity activeOpacity={0.7} onPress={onClickRetry}>
                  <View
                     style={{
                        ...styles.refreshBtn,
                        backgroundColor: IS_OTHER_SKIN ? 'white' : colors.title,
                     }}>
                     <View style={{ position: 'relative', top: 2, paddingRight: 3 }}>
                        <RefreshIcon />
                     </View>
                     <Text
                        style={{
                           ...styles.head,
                           color: colors.background,
                        }}>
                        刷新
                     </Text>
                  </View>
               </TouchableOpacity>
            </>
         )}
      </View>
   );
}

const styles = StyleSheet.create({
   container: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      width: "100%",
      aspectRatio: 16 / 9,
   },
   btns: {
      display: 'flex',
      flexDirection: 'row',
      alignItems: 'center',
      justifyContent: 'center',
   },
   btn: {
      flex: 1,
   },
   head: {
      color: 'white',
      textAlign: 'center',
   },
   refreshBtn: {
      marginTop: 20,
      borderRadius: 30,
      display: 'flex',
      flexDirection: 'row',
      paddingVertical: 8,
      paddingHorizontal: 15,
      alignItems: 'center',
      justifyContent: 'center',
      fontWeight: 'bold',
   },
});
