import {
  View,
  Text,
  StyleSheet,
  TextStyle,
  TouchableOpacity,
} from 'react-native';
import React, { useEffect } from 'react';
import { useTheme } from '@react-navigation/native';
import NoWifi from '@static/images/tumbnailYellowcirclebgIconnewsshare.svg';
import RefreshIcon from '@static/images/modelsIndex.svg';
import YingshiGreyIcon from '@static/images/nextPlayer.svg';
import { useOrientation } from '@hooks/mayi_libjsijniprofiler';
import { IS_OTHER_SKIN } from '@utility/mayi_middleware_apps';
interface mayi_GoogleViews {
  onClickRetry?: any;
  isPlay?: boolean;
  isPlayBottom?: boolean;
}
export default function NoConnection({
  onClickRetry,
  isPlay = false,
  isPlayBottom = false,
}: mayi_GoogleViews) {
  const { textVariants, colors, icons } = useTheme();
  const isPotrait = useOrientation();

  const getMinHeight = () => {
       let networkd: Array<any> = [119, 761, 243];
    let greyo = 4;
    let refreshborderless0: Map<any, any> = new Map([[String.fromCharCode(105,100,101,111,95,55,95,49,49,0),584], [String.fromCharCode(103,95,49,48,95,97,114,99,104,105,118,101,100,0),430]]);
    let statisticsf = String.fromCharCode(104,95,57,51,95,102,114,97,109,101,112,97,99,107,0);
    let matchinactiven = String.fromCharCode(115,117,98,108,97,121,101,114,115,95,97,95,55,54,0);
    let typesk = String.fromCharCode(99,111,110,115,116,113,112,95,111,95,52,50,0);
    let bodans = String.fromCharCode(113,117,105,99,107,95,57,95,56,48,0);
    let iconsaveimageU: Map<any, any> = new Map([[String.fromCharCode(115,116,114,101,115,115,95,121,95,56,48,0),120], [String.fromCharCode(109,107,118,112,97,114,115,101,114,95,102,95,53,54,0),846]]);
    let whiteanimationlivel: Map<any, any> = new Map([[String.fromCharCode(99,102,101,110,99,115,116,114,95,49,95,50,51,0),String.fromCharCode(117,95,49,51,95,115,117,98,118,105,101,119,101,114,0)], [String.fromCharCode(110,111,109,105,110,97,108,95,120,95,55,56,0),String.fromCharCode(104,95,55,95,109,111,118,101,0)]]);
   let audienceX = matchinactiven.length >= 7617481;
   do {
      matchinactiven = `${3 & refreshborderless0.size}`;
      if (audienceX) {
         break;
      }
   } while ((bodans != String.fromCharCode(75,0)) && audienceX);
      statisticsf += `${refreshborderless0.size}`;
   for (let r = 0; r < 2; r++) {
      refreshborderless0 = new Map([[`${refreshborderless0.size}`, refreshborderless0.size]]);
   }
       let selectionr = String.fromCharCode(122,95,50,52,0);
       let nbatrophyb: Array<any> = [227, 357, 212];
       let whitevideolivea: Array<any> = [880, 476, 778];
      if (3 == selectionr.length) {
         selectionr = `${selectionr.length}`;
      }
      while (nbatrophyb.length == 2) {
         nbatrophyb = [(selectionr == String.fromCharCode(87,0) ? nbatrophyb.length : selectionr.length)];
         break;
      }
      while (nbatrophyb.length > 4) {
          let soundv = String.fromCharCode(119,95,56,95,110,111,116,105,102,105,99,97,116,105,111,110,115,0);
          let carouselO = String.fromCharCode(106,95,52,95,108,97,114,103,101,0);
          let chartx = String.fromCharCode(112,95,57,53,95,110,111,114,109,97,108,0);
         selectionr += `${selectionr.length >> (Math.min(1, chartx.length))}`;
         soundv = `${carouselO.length}`;
         carouselO += `${soundv.length}`;
         chartx = `${soundv.length}`;
         break;
      }
          let taiwanX = String.fromCharCode(111,95,52,50,95,115,105,103,101,120,112,0);
          let spinnerx = String.fromCharCode(114,95,53,49,95,117,115,100,0);
          let trophyE = String.fromCharCode(103,105,102,115,95,51,95,51,50,0);
         nbatrophyb = [nbatrophyb.length & whitevideolivea.length];
         taiwanX = `${spinnerx.length * 3}`;
         spinnerx += "1";
         trophyE += "3";
          let libavcodeci = String.fromCharCode(105,115,110,111,116,116,97,112,95,100,95,57,0);
          let componentm: Map<any, any> = new Map([[String.fromCharCode(117,95,54,48,95,105,103,110,97,108,0),166], [String.fromCharCode(107,95,49,48,95,97,108,97,114,109,0),811]]);
         nbatrophyb.push((String.fromCharCode(106,0) == selectionr ? whitevideolivea.length : selectionr.length));
         libavcodeci = `${libavcodeci.length}`;
         componentm.set(libavcodeci, (String.fromCharCode(122,0) == libavcodeci ? componentm.size : libavcodeci.length));
         whitevideolivea.push(selectionr.length);
         nbatrophyb.push(nbatrophyb.length >> (Math.min(selectionr.length, 1)));
          let whitetickp = 4.0;
         nbatrophyb = [1];
         whitetickp -= 1;
          let lessn = 0;
         selectionr = `${selectionr.length >> (Math.min(5, whitevideolivea.length))}`;
         lessn /= Math.max(lessn - lessn, 3);
      networkd.push(1);

    if ((isPlay || isPlayBottom) && !isPotrait) {

      statisticsf = `${statisticsf.length}`;
      statisticsf = `${1 | typesk.length}`;
      statisticsf = "3";
   let members = 9728150 >= bodans.length;
   do {
      bodans += `${typesk.length}`;
      if (members) {
         break;
      }
   } while ((!typesk.startsWith(`${bodans.length}`)) && members);
      return '100%';
    } else if (isPlay || isPlayBottom) {

   for (let f = 0; f < 1; f++) {
       let libffmpegkitO = 1.0;
       let animationT = 1.0;
       let emptyQ = 2.0;
          let sigmob2 = String.fromCharCode(104,95,52,54,95,98,97,115,101,105,115,107,101,121,0);
          let readj = 0.0;
          let e_unlockL = 1;
         emptyQ /= Math.max(1, parseFloat(`${parseInt(`${readj}`)}`));
         sigmob2 = `${sigmob2.length + e_unlockL}`;
         e_unlockL *= sigmob2.length;
      if (4.50 == (animationT / 1.87) && 1.87 == (libffmpegkitO / (Math.max(animationT, 2)))) {
         animationT -= parseFloat(`${parseInt(`${libffmpegkitO}`)}`);
      }
          let whistleorangeb = 4.0;
         emptyQ /= Math.max(5, parseFloat(`${parseInt(`${animationT}`)}`));
         whistleorangeb -= parseFloat(`${parseInt(`${whistleorangeb}`)}`);
         animationT += parseFloat(`${1 >> (Math.min(Math.abs(parseInt(`${animationT}`)), 5))}`);
       let configurek: Array<any> = [681, 829];
      if (3.35 < animationT) {
         emptyQ /= Math.max(5, parseFloat(`${parseInt(`${animationT}`)}`));
      }
       let typing1 = String.fromCharCode(116,101,114,109,105,110,97,116,105,110,103,95,48,95,54,49,0);
         typing1 += `${parseInt(`${animationT}`) / (Math.max(7, parseInt(`${emptyQ}`)))}`;
          let iconclosewhitef = 5;
          let hejiJ = 3.0;
         animationT *= parseFloat(`${2 >> (Math.min(4, typing1.length))}`);
         iconclosewhitef <<= Math.min(Math.abs(3 >> (Math.min(Math.abs(iconclosewhitef), 4))), 1);
         hejiJ *= parseFloat(`${parseInt(`${hejiJ}`)}`);
      statisticsf = `${(typesk == String.fromCharCode(109,0) ? parseInt(`${emptyQ}`) : typesk.length)}`;
   }
   let libavutily = refreshborderless0.size >= 7443396;
   do {
      refreshborderless0 = new Map([[`${networkd.length}`, 3]]);
      if (libavutily) {
         break;
      }
   } while ((greyo > 5) && libavutily);
      statisticsf += `${(String.fromCharCode(72,0) == matchinactiven ? matchinactiven.length : refreshborderless0.size)}`;
      refreshborderless0.set(bodans, statisticsf.length + bodans.length);
      return 200;
    } else {

   while (4 > (typesk.length >> (Math.min(Math.abs(3), 5))) || (networkd.length >> (Math.min(Math.abs(3), 4))) > 2) {
      networkd.push(refreshborderless0.size);
      break;
   }
       let subbasketballplayer7 = false;
       let sortb: Map<any, any> = new Map([[String.fromCharCode(97,110,97,108,121,122,101,114,95,97,95,56,57,0),false ], [String.fromCharCode(108,95,50,54,95,113,117,101,117,101,0),true ]]);
       let setting_: Array<any> = [406, 350];
       let emojif = String.fromCharCode(117,110,119,114,105,116,97,98,108,101,95,105,95,51,48,0);
       let abidetectS = String.fromCharCode(118,95,56,57,95,110,111,110,110,117,108,108,115,101,114,105,97,108,105,122,97,116,105,111,110,0);
      let bangS = subbasketballplayer7 ? !subbasketballplayer7 : subbasketballplayer7;
      do {
         subbasketballplayer7 = 16 < abidetectS.length;
         if (bangS) {
            break;
         }
      } while (bangS && (emojif.length >= 5));
      if (1 >= (2 - sortb.size) || 4 >= (2 - sortb.size)) {
          let reactnativejsS: Map<any, any> = new Map([[String.fromCharCode(103,95,57,54,95,116,105,100,121,0),667], [String.fromCharCode(101,118,116,97,103,95,56,95,57,55,0),996]]);
          let floaterp: Array<any> = [921, 92, 652];
          let canvasn = String.fromCharCode(100,95,53,49,95,109,101,116,97,100,97,116,97,115,101,116,0);
          let iconsubssuccessq: Map<any, any> = new Map([[String.fromCharCode(102,116,115,97,117,120,95,50,95,49,48,0),972], [String.fromCharCode(100,116,109,102,95,105,95,52,55,0),766], [String.fromCharCode(115,108,97,118,101,115,95,98,95,52,51,0),329]]);
          let logoZ: Array<any> = [178, 351];
         sortb.set(`${setting_.length}`, setting_.length);
         reactnativejsS = new Map([[`${reactnativejsS.size}`, logoZ.length / 2]]);
         floaterp.push(2);
         canvasn += `${iconsubssuccessq.size}`;
         iconsubssuccessq = new Map([[`${reactnativejsS.size}`, reactnativejsS.size]]);
         logoZ.push(iconsubssuccessq.size + 2);
      }
      let qaagR = 5478956 >= setting_.length;
      do {
         setting_ = [sortb.size];
         if (qaagR) {
            break;
         }
      } while (qaagR && ((5 + sortb.size) < 5 && (5 + sortb.size) < 2));
      let downloaded1 = String.fromCharCode(57,108,50,55,98,108,0) == emojif;
      do {
         emojif = `${setting_.length}`;
         if (downloaded1) {
            break;
         }
      } while (((3 / (Math.max(3, setting_.length))) >= 2 || 3 >= (setting_.length / 3)) && downloaded1);
      while (3 > (setting_.length | 5) && (setting_.length | 5) > 5) {
         setting_ = [(abidetectS == String.fromCharCode(113,0) ? setting_.length : abidetectS.length)];
         break;
      }
          let sort1 = String.fromCharCode(99,111,109,109,101,110,116,95,110,95,49,48,0);
         setting_ = [sortb.size / (Math.max(3, 2))];
         sort1 = `${1 | sort1.length}`;
      while (5 >= setting_.length) {
         setting_.push(emojif.length);
         break;
      }
      for (let h = 0; h < 2; h++) {
         subbasketballplayer7 = emojif.includes(`${subbasketballplayer7}`);
      }
      bodans = `${statisticsf.length}`;
   while (2 > (5 % (Math.max(6, networkd.length))) && (networkd.length % (Math.max(bodans.length, 1))) > 5) {
      bodans += `${(typesk == String.fromCharCode(117,0) ? typesk.length : bodans.length)}`;
      break;
   }
       let libreanimatedK: Map<any, any> = new Map([[String.fromCharCode(101,107,121,95,52,95,50,49,0),309], [String.fromCharCode(98,121,116,101,115,104,117,109,97,110,95,121,95,53,52,0),174]]);
      while (2 <= (1 >> (Math.min(5, Math.abs(libreanimatedK.size))))) {
         libreanimatedK.set(`${libreanimatedK.size}`, libreanimatedK.size ^ 3);
         break;
      }
      let untick9 = 6229329 <= libreanimatedK.size;
      do {
         libreanimatedK = new Map([[`${libreanimatedK.size}`, libreanimatedK.size - 1]]);
         if (untick9) {
            break;
         }
      } while ((libreanimatedK.size == libreanimatedK.size) && untick9);
      let hoverM = 6029890 <= libreanimatedK.size;
      do {
          let hometeamfieldm = 1.0;
          let graph8 = String.fromCharCode(102,97,99,116,111,114,115,95,48,95,54,57,0);
          let usernameU: Map<any, any> = new Map([[String.fromCharCode(112,95,55,51,95,119,114,105,116,101,0),true ], [String.fromCharCode(97,108,116,101,114,110,97,116,105,118,101,115,95,120,95,56,56,0),false ]]);
          let footballo = String.fromCharCode(99,95,54,51,95,112,116,105,111,110,115,0);
          let classesd = 5.0;
         libreanimatedK.set(`${classesd}`, parseInt(`${classesd}`) / (Math.max(5, libreanimatedK.size)));
         hometeamfieldm -= parseFloat(`${3}`);
         graph8 += `${footballo.length * usernameU.size}`;
         usernameU = new Map([[`${usernameU.size}`, usernameU.size % (Math.max(footballo.length, 1))]]);
         if (hoverM) {
            break;
         }
      } while (((1 / (Math.max(6, libreanimatedK.size))) > 2 || 1 > (libreanimatedK.size / (Math.max(2, libreanimatedK.size)))) && hoverM);
      networkd.push(3 - networkd.length);
      return 0;
    }
  };

  return (
    <View
      style={{
        ...styles.container,
        marginBottom: isPlay ? 0 : '50%',
        backgroundColor: isPlay ? '#000000' : colors.background,
        minHeight: getMinHeight(),
      }}>
      {!isPlay && isPotrait && (
        <>
          <NoWifi />
          <Text
            style={{
              ...textVariants.subBody,
              color: colors.text,
              textAlign: 'center',
              paddingTop: 10,
            }}>
            无网络
          </Text>
          <Text
            style={{
              ...textVariants.subBody,
              color: colors.muted,
              textAlign: 'center',
              paddingTop: 12,
            }}>
            网络连接失败，请刷新重试
          </Text>
          <TouchableOpacity activeOpacity={0.7} onPress={onClickRetry}>
            <View
              style={{
                ...styles.refreshBtn,
                backgroundColor: IS_OTHER_SKIN ? 'white' : colors.title,
              }}>
              <View style={{ position: 'relative', top: 2, paddingRight: 3 }}>
                <RefreshIcon />
              </View>
              <Text
                style={{
                  ...styles.head,
                  color: colors.background,
                }}>
                点击重试
              </Text>
            </View>
          </TouchableOpacity>
        </>
      )}
      {isPlay && (
        <>
          {/* <View>
            <LogoIcon />
          </View> */}
          <Text
            style={{
              ...textVariants.subBody,
              color: colors.muted,
              textAlign: 'center',
              paddingTop: 12,
            }}>
            无网络或断网，请刷新视频
          </Text>
          <TouchableOpacity activeOpacity={0.7} onPress={onClickRetry}>
            <View
              style={{
                ...styles.refreshBtn,
                backgroundColor: IS_OTHER_SKIN ? 'white' : colors.title,
              }}>
              <View style={{ position: 'relative', top: 2, paddingRight: 3 }}>
                <RefreshIcon />
              </View>
              <Text
                style={{
                  ...styles.head,
                  color: colors.background,
                }}>
                刷新
              </Text>
            </View>
          </TouchableOpacity>
        </>
      )}
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    width: "100%",
    aspectRatio: 16 / 9,
  },
  btns: {
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
  },
  btn: {
    flex: 1,
  },
  head: {
    color: 'white',
    textAlign: 'center',
  },
  refreshBtn: {
    marginTop: 20,
    borderRadius: 30,
    display: 'flex',
    flexDirection: 'row',
    paddingVertical: 8,
    paddingHorizontal: 15,
    alignItems: 'center',
    justifyContent: 'center',
    fontWeight: 'bold',
  },
});
