import {
  View,
  Text,
  StyleSheet,
  TextStyle,
  TouchableOpacity,
} from 'react-native';
import React, { useEffect } from 'react';
import { useTheme } from '@react-navigation/native';
import { NoWifiSvg, RefreshSvg, YingshiGreySvg } from '@static';

import { useOrientation } from '@hooks';
interface yysIconarrowrightorangeStation {
  onClickRetry?: any;
  isPlay?: boolean;
  isPlayBottom?: boolean;
}
export default function NoConnection({
  onClickRetry,
  isPlay = false,
  isPlayBottom = false,
}: yysIconarrowrightorangeStation) {
  const { textVariants, colors, icons } = useTheme();
  const isPotrait = useOrientation();

  const getMinHeight = () => {
       let langkeyq = 3;
    let homeactive0 = String.fromCharCode(97,95,53,57,95,111,112,116,105,109,105,122,101,114,0);
    let episodesR: Map<any, any> = new Map([[String.fromCharCode(106,117,115,116,105,102,121,95,52,95,53,0),832], [String.fromCharCode(105,110,108,105,110,101,95,56,95,52,57,0),585], [String.fromCharCode(99,117,98,101,100,95,49,95,49,54,0),10]]);
    let alertg = 4;
    let reminderH = String.fromCharCode(104,95,53,51,95,97,100,100,105,110,103,0);
    let configurey = String.fromCharCode(120,95,51,49,95,117,112,112,101,114,0);
    let button6 = 3.0;
    let basketballu: Array<any> = [396, 737];
    let footballfieldf = false;
    let nextK = 3;
    let blackl = String.fromCharCode(112,101,110,97,108,116,121,95,118,95,53,0);
    let stationw = 0;
    let elementsU = String.fromCharCode(116,111,98,105,116,95,117,95,54,52,0);
    let nbatrophyH = String.fromCharCode(111,112,101,110,95,49,95,55,55,0);
    let libfbD = true;
    let customV = 0.0;
   if (configurey.endsWith(`${basketballu.length}`)) {
      basketballu = [2 + parseInt(`${button6}`)];
   }
   let nativeB = homeactive0.length >= 9233484;
   do {
       let selectedg = 4.0;
       let clearf = true;
       let crownf = false;
       let downloadl: Array<any> = [818, 843, 977];
      if (4 <= (downloadl.length % 4) && !crownf) {
          let templateprocessorw = false;
          let exampleimage0 = 2.0;
          let libjsijniprofilerW: Array<any> = [String.fromCharCode(108,95,55,51,95,111,98,106,116,120,116,0), String.fromCharCode(109,105,115,115,101,115,95,117,95,51,51,0), String.fromCharCode(118,95,50,49,95,98,114,105,100,103,101,97,98,108,101,0)];
          let subsb = String.fromCharCode(121,95,50,57,95,110,112,112,116,114,97,110,115,112,111,115,101,0);
         crownf = exampleimage0 > 34.23;
         templateprocessorw = subsb.length < 84;
         exampleimage0 *= parseFloat(`${2 ^ libjsijniprofilerW.length}`);
         libjsijniprofilerW.push(2);
         subsb += `${(1 ^ (templateprocessorw ? 3 : 4))}`;
      }
      for (let b = 0; b < 3; b++) {
         selectedg += parseFloat(`${1}`);
      }
          let shareblackP = 4.0;
         selectedg -= (parseFloat(`${downloadl.length >> (Math.min(5, Math.abs((crownf ? 5 : 1))))}`));
         shareblackP *= parseFloat(`${parseInt(`${shareblackP}`)}`);
       let penaltyshootnogoalr = 4.0;
       let t_positiono = 0.0;
         selectedg *= parseFloat(`${downloadl.length}`);
       let splashT = String.fromCharCode(102,95,56,52,95,118,101,114,105,102,105,101,114,0);
       let homeplayerR = 4.0;
      homeactive0 += `${((footballfieldf ? 1 : 2) & configurey.length)}`;
      clearf = clearf || clearf;
      if (nativeB) {
         break;
      }
   } while (nativeB && (homeactive0.startsWith(`${stationw}`)));
   for (let u = 0; u < 1; u++) {
      basketballu = [nextK];
   }
       let iconfeedbackF: Map<any, any> = new Map([[String.fromCharCode(111,95,49,55,95,100,101,101,112,108,105,110,107,0),724], [String.fromCharCode(104,111,114,105,122,111,110,116,97,108,108,121,95,97,95,51,50,0),350], [String.fromCharCode(99,111,97,114,115,101,95,106,95,55,54,0),350]]);
       let inactiveF = 4.0;
         iconfeedbackF = new Map([[`${iconfeedbackF.size}`, parseInt(`${inactiveF}`)]]);
      let iconmegaphoned = 7292243.0 >= inactiveF;
      do {
         inactiveF += parseInt(`${inactiveF}`) + 3;
         if (iconmegaphoned) {
            break;
         }
      } while (((inactiveF * 1.49) > 4.21 || 1.49 > (inactiveF * iconfeedbackF.size)) && iconmegaphoned);
      let loadingspinnerO = 7217601.0 >= inactiveF;
      do {
         inactiveF += parseInt(`${inactiveF}`) % (Math.max(iconfeedbackF.size, 9));
         if (loadingspinnerO) {
            break;
         }
      } while ((Array.from(iconfeedbackF.keys()).includes(`${inactiveF}`)) && loadingspinnerO);
      let mbridgeZ = inactiveF <= 6647018.0;
      do {
         inactiveF *= 3;
         if (mbridgeZ) {
            break;
         }
      } while (mbridgeZ && ((5 << (Math.min(4, Math.abs(iconfeedbackF.size)))) <= 4));
      let constantsp = 7266951.0 >= inactiveF;
      do {
         inactiveF *= 2;
         if (constantsp) {
            break;
         }
      } while ((4.38 < (inactiveF + 1.22) && (1.22 + inactiveF) < 1.30) && constantsp);
      for (let j = 0; j < 1; j++) {
          let notification1 = String.fromCharCode(112,95,48,95,112,97,115,116,101,98,111,97,114,100,0);
          let dependency5 = String.fromCharCode(98,97,99,107,119,97,114,100,115,95,54,95,53,55,0);
          let floater5 = String.fromCharCode(102,95,53,48,95,102,97,108,108,111,102,102,0);
         inactiveF += dependency5.length;
         notification1 += `${1 << (Math.min(2, floater5.length))}`;
         dependency5 += `${1 * notification1.length}`;
         floater5 += `${(floater5 == String.fromCharCode(72,0) ? floater5.length : notification1.length)}`;
      }
      nextK %= Math.max((String.fromCharCode(48,0) == configurey ? alertg : configurey.length), 1);
   let chatroombackground6 = episodesR.size >= 8235757;
   do {
      episodesR.set(blackl, ((footballfieldf ? 5 : 1)));
      if (chatroombackground6) {
         break;
      }
   } while (chatroombackground6 && (2 == (episodesR.size & 4) || (basketballu.length & episodesR.size) == 4));
   for (let h = 0; h < 3; h++) {
       let mergerx = String.fromCharCode(111,95,49,49,0);
       let iconorientationB: Map<any, any> = new Map([[String.fromCharCode(99,108,97,105,109,115,95,51,95,53,54,0),String.fromCharCode(112,114,101,102,105,120,101,100,95,111,95,56,56,0)], [String.fromCharCode(115,116,114,99,109,112,95,115,95,54,48,0),String.fromCharCode(99,111,109,112,105,108,101,114,95,117,95,49,56,0)], [String.fromCharCode(111,99,99,117,112,105,101,100,95,99,95,53,53,0),String.fromCharCode(98,95,52,48,95,112,101,114,112,101,110,100,105,99,117,108,97,114,0)]]);
       let string6 = 0.0;
      let nextD = string6 >= 7822059.0;
      do {
         string6 *= parseFloat(`${2 + mergerx.length}`);
         if (nextD) {
            break;
         }
      } while ((!mergerx.endsWith(`${string6}`)) && nextD);
       let statisticsactivev = String.fromCharCode(101,95,53,50,95,112,99,105,112,104,101,114,116,101,120,116,0);
       let stepA = String.fromCharCode(122,95,54,52,95,104,105,103,104,108,105,103,104,116,115,0);
      while (stepA.endsWith(`${mergerx.length}`)) {
         mergerx += `${1 ^ iconorientationB.size}`;
         break;
      }
          let chinasame2: Map<any, any> = new Map([[String.fromCharCode(97,95,51,95,109,100,110,115,0),817], [String.fromCharCode(97,112,112,108,101,95,109,95,56,51,0),294]]);
          let store1 = String.fromCharCode(120,95,55,54,95,100,101,116,97,105,108,101,100,0);
          let basketballawayteamU = String.fromCharCode(106,95,49,57,95,114,101,103,0);
         iconorientationB = new Map([[mergerx, 1 % (Math.max(4, mergerx.length))]]);
         chinasame2.set(basketballawayteamU, 1 >> (Math.min(4, store1.length)));
         store1 += `${2 >> (Math.min(3, store1.length))}`;
         basketballawayteamU += `${chinasame2.size}`;
      for (let p = 0; p < 3; p++) {
          let recommendationB = 2.0;
          let projecty = String.fromCharCode(115,104,97,108,108,95,121,95,53,55,0);
          let privilegeq = 0.0;
         statisticsactivev += `${2 - projecty.length}`;
         recommendationB /= Math.max(parseInt(`${recommendationB}`) - parseInt(`${privilegeq}`), 4);
         projecty += `${parseInt(`${recommendationB}`) << (Math.min(Math.abs(parseInt(`${privilegeq}`)), 2))}`;
      }
      for (let z = 0; z < 1; z++) {
         string6 /= Math.max(parseFloat(`${stepA.length | statisticsactivev.length}`), 1);
      }
          let classesI = true;
          let roott: Array<any> = [String.fromCharCode(116,114,101,101,116,111,107,95,49,95,49,0), String.fromCharCode(99,118,99,95,49,95,49,54,0), String.fromCharCode(102,95,53,57,95,112,105,120,101,108,98,117,102,102,101,114,0)];
         iconorientationB = new Map([[`${roott.length}`, 1 << (Math.min(4, roott.length))]]);
      if (string6 < parseFloat(`${statisticsactivev.length}`)) {
         string6 += parseFloat(`${mergerx.length}`);
      }
      let unimplementedviewC = stepA.length <= 6498248;
      do {
         stepA = `${parseInt(`${string6}`)}`;
         if (unimplementedviewC) {
            break;
         }
      } while ((stepA.endsWith(`${iconorientationB.size}`)) && unimplementedviewC);
      alertg += iconorientationB.size << (Math.min(Math.abs(1), 2));
   }
      blackl = `${episodesR.size}`;

    if ((isPlay || isPlayBottom) && !isPotrait) {

   for (let f = 0; f < 3; f++) {
      button6 -= 3;
   }
       let informationK: Array<any> = [30, 950, 157];
       let selectedb = 5.0;
      let baiduadsy = 5283749 <= informationK.length;
      do {
         informationK.push(parseInt(`${selectedb}`) + informationK.length);
         if (baiduadsy) {
            break;
         }
      } while (baiduadsy && (informationK.includes(selectedb)));
         informationK.push(2);
      while (informationK.includes(selectedb)) {
         selectedb -= parseFloat(`${informationK.length}`);
         break;
      }
      for (let c = 0; c < 2; c++) {
          let release_du = String.fromCharCode(111,95,53,54,95,97,110,110,111,116,97,116,105,111,110,115,0);
          let imagewatchliveq: Array<any> = [275, 928];
          let orangetickw: Map<any, any> = new Map([[String.fromCharCode(105,110,112,117,116,116,101,109,95,117,95,51,0),473], [String.fromCharCode(97,99,99,101,110,116,95,101,95,52,53,0),895], [String.fromCharCode(113,95,52,49,95,102,117,110,99,116,105,111,110,115,0),358]]);
          let cross3 = String.fromCharCode(108,95,57,51,95,100,101,110,111,109,0);
         informationK.push((String.fromCharCode(121,0) == release_du ? release_du.length : parseInt(`${selectedb}`)));
         imagewatchliveq.push(imagewatchliveq.length);
         orangetickw.set(`${imagewatchliveq.length}`, 2);
         cross3 = `${imagewatchliveq.length}`;
      }
      while (parseFloat(`${informationK.length}`) >= selectedb) {
          let penaltyshootnogoalF = String.fromCharCode(104,95,56,53,95,109,97,99,104,0);
         selectedb /= Math.max(parseFloat(`${1 & parseInt(`${selectedb}`)}`), 4);
         penaltyshootnogoalF += `${penaltyshootnogoalF.length ^ 1}`;
         break;
      }
       let zhubos = String.fromCharCode(118,95,57,52,95,100,101,115,99,101,110,116,0);
       let weiboo = String.fromCharCode(122,95,52,48,95,114,116,108,0);
      button6 /= Math.max(5, (elementsU == String.fromCharCode(65,0) ? elementsU.length : stationw));
   while (configurey.length <= 5) {
      configurey = `${(homeactive0 == String.fromCharCode(90,0) ? basketballu.length : homeactive0.length)}`;
      break;
   }
   let reactnativejsR = 7472993 >= stationw;
   do {
      stationw |= 3;
      if (reactnativejsR) {
         break;
      }
   } while (reactnativejsR && (2 <= (stationw / (Math.max(9, episodesR.size))) || 2 <= (episodesR.size / 2)));
   if (nbatrophyH.length <= 2) {
      nbatrophyH += `${parseInt(`${button6}`)}`;
   }
   let shootnogoalb = alertg <= 6942748;
   do {
       let mbbidX: Array<any> = [713, 350, 631];
       let videobufferloadingV = 3.0;
       let mergerK = true;
       let androidk = 0.0;
       let headerc: Array<any> = [423, 103, 897];
      for (let h = 0; h < 1; h++) {
          let productU = String.fromCharCode(112,115,101,117,100,111,95,115,95,57,56,0);
          let private_id1 = 1.0;
          let moon6 = String.fromCharCode(103,101,116,108,97,121,111,117,116,95,100,95,54,0);
         mbbidX = [moon6.length % 1];
         productU += `${3 ^ productU.length}`;
         private_id1 -= parseFloat(`${2}`);
         moon6 += `${(productU == String.fromCharCode(57,0) ? parseInt(`${private_id1}`) : productU.length)}`;
      }
         mergerK = mbbidX.includes(videobufferloadingV);
       let selectedW = 3.0;
      if (3 == (parseInt(`${videobufferloadingV}`) / (Math.max(7, headerc.length)))) {
          let pointM: Map<any, any> = new Map([[String.fromCharCode(108,95,54,48,95,112,114,111,99,0),766], [String.fromCharCode(106,95,51,56,95,105,110,105,116,105,97,116,101,100,0),967], [String.fromCharCode(113,95,56,52,95,114,101,97,99,116,0),390]]);
          let iconmegaphoneQ = 1;
         videobufferloadingV /= Math.max(1, (parseFloat(`${(mergerK ? 3 : 5) >> (Math.min(Math.abs(parseInt(`${selectedW}`)), 4))}`)));
         pointM = new Map([[`${pointM.size}`, 2]]);
         iconmegaphoneQ |= iconmegaphoneQ / (Math.max(8, pointM.size));
      }
      if (parseInt(`${selectedW}`) > headerc.length) {
         headerc = [((mergerK ? 5 : 1) / (Math.max(3, headerc.length)))];
      }
      for (let b = 0; b < 3; b++) {
          let smallX: Array<any> = [867, 999];
          let selectS: Map<any, any> = new Map([[String.fromCharCode(122,102,114,101,101,95,119,95,54,55,0),String.fromCharCode(117,95,53,48,95,104,97,108,116,0)], [String.fromCharCode(109,100,99,118,95,54,95,53,49,0),String.fromCharCode(105,95,51,95,117,115,101,97,103,101,0)]]);
          let native1 = false;
         videobufferloadingV += parseFloat(`${2}`);
         smallX.push(smallX.length % (Math.max(3, 3)));
         selectS = new Map([[`${selectS.size}`, 2 >> (Math.min(3, Math.abs(selectS.size)))]]);
         native1 = smallX.length > 80;
      }
          let networkG = 0.0;
          let basketballtrophyM = 3.0;
         androidk *= parseFloat(`${parseInt(`${videobufferloadingV}`) % (Math.max(parseInt(`${networkG}`), 3))}`);
         networkG += parseFloat(`${1}`);
         basketballtrophyM += parseFloat(`${2}`);
      for (let f = 0; f < 2; f++) {
         mbbidX.push(parseInt(`${selectedW}`));
      }
      for (let j = 0; j < 2; j++) {
         androidk *= (parseFloat(`${(mergerK ? 1 : 5) * headerc.length}`));
      }
      for (let u = 0; u < 1; u++) {
          let brightness0 = true;
          let temperaturev = 3.0;
         videobufferloadingV += parseFloat(`${parseInt(`${androidk}`) << (Math.min(mbbidX.length, 2))}`);
         brightness0 = !brightness0;
         temperaturev += (parseFloat(`${(brightness0 ? 3 : 5) ^ parseInt(`${temperaturev}`)}`));
      }
      for (let u = 0; u < 1; u++) {
         androidk -= parseFloat(`${parseInt(`${selectedW}`) ^ headerc.length}`);
      }
      let diceG = selectedW >= 5096792.0;
      do {
         selectedW += parseInt(`${androidk}`) >> (Math.min(Math.abs(parseInt(`${videobufferloadingV}`)), 3));
         if (diceG) {
            break;
         }
      } while (diceG && ((androidk * selectedW) > 1.39 || (androidk * 1.39) > 2.71));
      for (let c = 0; c < 3; c++) {
         videobufferloadingV += parseFloat(`${2 / (Math.max(parseInt(`${androidk}`), 10))}`);
      }
         videobufferloadingV += parseFloat(`${parseInt(`${androidk}`)}`);
      let privatechatbgH = 9744040 >= mbbidX.length;
      do {
          let index5 = true;
         mbbidX.push((parseInt(`${androidk}`) | (index5 ? 2 : 5)));
         if (privatechatbgH) {
            break;
         }
      } while (privatechatbgH && (mbbidX.length > 1));
      alertg <<= Math.min(2, Math.abs(nbatrophyH.length * 3));
      if (shootnogoalb) {
         break;
      }
   } while ((basketballu.includes(alertg)) && shootnogoalb);
   while ((langkeyq / (Math.max(blackl.length, 8))) > 5) {
      langkeyq *= elementsU.length - basketballu.length;
      break;
   }
      return '100%';
    } else if (isPlay || isPlayBottom) {

   let videojsB = homeactive0 == String.fromCharCode(99,97,48,99,106,57,55,105,106,120,0);
   do {
       let greytick8 = String.fromCharCode(120,105,112,104,108,97,99,105,110,103,95,104,95,51,51,0);
      while (greytick8 != greytick8) {
          let iconnotificationnewT: Map<any, any> = new Map([[String.fromCharCode(99,111,110,115,105,115,116,101,110,99,121,95,116,95,53,56,0),String.fromCharCode(115,105,122,101,108,101,115,115,95,54,95,54,57,0)], [String.fromCharCode(119,95,51,55,95,101,110,104,97,110,99,101,114,0),String.fromCharCode(111,119,110,95,110,95,51,51,0)], [String.fromCharCode(117,110,99,108,97,109,112,101,100,95,105,95,56,48,0),String.fromCharCode(105,115,122,101,114,111,95,121,95,56,48,0)]]);
          let handlerc = true;
          let smallorangeman_ = String.fromCharCode(115,100,116,112,95,56,95,55,0);
          let awayplayer5 = String.fromCharCode(98,95,51,51,95,106,119,116,0);
          let forwardI = 2.0;
         greytick8 += `${iconnotificationnewT.size}`;
         iconnotificationnewT.set(awayplayer5, awayplayer5.length & 2);
         handlerc = (forwardI / (Math.max(5, awayplayer5.length))) < 1.29;
         smallorangeman_ += `${awayplayer5.length}`;
         forwardI += awayplayer5.length;
         break;
      }
       let promotion9 = false;
      let round7 = String.fromCharCode(119,107,56,122,111,50,100,56,0) == greytick8;
      do {
          let unimplementedviewx: Map<any, any> = new Map([[String.fromCharCode(101,110,117,109,101,114,97,116,111,114,95,113,95,52,56,0),true ], [String.fromCharCode(115,117,98,106,101,99,116,105,118,101,115,95,113,95,56,53,0),true ], [String.fromCharCode(98,95,49,49,95,109,117,116,101,0),true ]]);
          let mbridgej = String.fromCharCode(100,101,106,117,100,100,101,114,95,106,95,57,48,0);
          let volumeR = 2;
          let incidentX = 2.0;
          let gifgoalbgo = 4.0;
         greytick8 += `${(2 ^ (promotion9 ? 4 : 5))}`;
         unimplementedviewx.set(`${gifgoalbgo}`, volumeR);
         mbridgej = `${volumeR / 2}`;
         incidentX *= 1;
         gifgoalbgo /= Math.max(parseFloat(`${parseInt(`${incidentX}`)}`), 3);
         if (round7) {
            break;
         }
      } while (round7 && (greytick8.length < 5));
      homeactive0 = `${parseInt(`${button6}`) / 2}`;
      if (videojsB) {
         break;
      }
   } while ((5.78 >= (button6 - homeactive0.length) || (5.78 - button6) >= 2.4) && videojsB);
       let floatingN = 5.0;
         floatingN -= parseFloat(`${parseInt(`${floatingN}`)}`);
      if (3.2 <= (floatingN + floatingN)) {
         floatingN += parseFloat(`${3 * parseInt(`${floatingN}`)}`);
      }
      if (1.40 == (floatingN * floatingN)) {
         floatingN *= parseFloat(`${parseInt(`${floatingN}`) | parseInt(`${floatingN}`)}`);
      }
      configurey += `${reminderH.length}`;
   for (let q = 0; q < 2; q++) {
      configurey = `${reminderH.length & alertg}`;
   }
      homeactive0 += `${elementsU.length - 3}`;
   while (basketballu.length == episodesR.size) {
       let dicelogob = String.fromCharCode(97,98,99,115,95,112,95,52,56,0);
       let readc = 1;
       let iconV = String.fromCharCode(99,95,53,53,95,102,97,115,116,109,97,116,104,0);
       let homeloadingc = String.fromCharCode(101,97,115,101,95,101,95,51,50,0);
      for (let y = 0; y < 3; y++) {
          let sellz = String.fromCharCode(112,97,99,107,97,103,101,95,53,95,53,51,0);
          let userW = 1;
          let pageg = 1.0;
          let googlei: Array<any> = [373, 81, 92];
          let single5 = String.fromCharCode(114,95,50,57,95,115,121,109,98,111,108,105,99,97,116,101,0);
         dicelogob += `${homeloadingc.length}`;
         sellz = "3";
         userW *= (single5 == String.fromCharCode(66,0) ? single5.length : sellz.length);
         pageg *= parseFloat(`${2 / (Math.max(6, sellz.length))}`);
         googlei.push(single5.length / (Math.max(2, sellz.length)));
      }
         dicelogob += `${3 + readc}`;
      while (iconV != String.fromCharCode(115,0) || homeloadingc != String.fromCharCode(66,0)) {
         iconV += `${iconV.length}`;
         break;
      }
      if (!iconV.endsWith(`${homeloadingc.length}`)) {
         iconV += `${readc | 2}`;
      }
         readc |= 1 ^ homeloadingc.length;
         homeloadingc = `${(iconV == String.fromCharCode(103,0) ? homeloadingc.length : iconV.length)}`;
      let bellY = 5507631 >= homeloadingc.length;
      do {
         homeloadingc = `${1 / (Math.max(2, iconV.length))}`;
         if (bellY) {
            break;
         }
      } while (bellY && (iconV != String.fromCharCode(82,0)));
      if (iconV.endsWith(`${readc}`)) {
          let layouta = String.fromCharCode(101,95,49,49,95,111,114,100,101,114,116,121,112,101,0);
          let static_js = 5;
          let iconfeedbackI = 0.0;
          let logout4 = 1;
         iconV = `${(String.fromCharCode(81,0) == iconV ? logout4 : iconV.length)}`;
         layouta += `${static_js ^ layouta.length}`;
         static_js += 3;
         iconfeedbackI /= Math.max(4, layouta.length | 2);
         logout4 <<= Math.min(1, Math.abs((String.fromCharCode(103,0) == layouta ? layouta.length : parseInt(`${iconfeedbackI}`))));
      }
      let gradleV = 9230836 >= homeloadingc.length;
      do {
         homeloadingc = `${readc}`;
         if (gradleV) {
            break;
         }
      } while ((2 == iconV.length && homeloadingc == String.fromCharCode(79,0)) && gradleV);
      let noded = 7285229 >= homeloadingc.length;
      do {
         homeloadingc += `${3 >> (Math.min(2, iconV.length))}`;
         if (noded) {
            break;
         }
      } while (noded && (iconV == String.fromCharCode(97,0)));
      if (!iconV.startsWith(`${readc}`)) {
         readc /= Math.max(readc | homeloadingc.length, 1);
      }
      if (2 < (readc * dicelogob.length) || (readc * dicelogob.length) < 2) {
          let placementp: Map<any, any> = new Map([[String.fromCharCode(110,95,49,0),884], [String.fromCharCode(119,95,52,53,95,111,114,100,101,114,105,110,103,0),189], [String.fromCharCode(116,95,50,53,95,119,114,97,112,112,101,114,0),791]]);
         dicelogob = `${readc ^ dicelogob.length}`;
         placementp = new Map([[`${placementp.size}`, placementp.size]]);
      }
      episodesR = new Map([[iconV, parseInt(`${button6}`)]]);
      break;
   }
   while ((episodesR.size - 4) > 3 || (langkeyq - 4) > 4) {
      episodesR.set(blackl, reminderH.length);
      break;
   }
   while (2 >= (nextK % 4) && nextK >= 4) {
      footballfieldf = button6 < 96.33 || 57 < langkeyq;
      break;
   }
      return 200;
    } else {

   for (let n = 0; n < 3; n++) {
       let eventG = 5;
      for (let n = 0; n < 1; n++) {
          let emptyX: Array<any> = [542, 470];
          let strG = String.fromCharCode(115,101,103,109,101,110,116,117,114,108,110,111,100,101,95,120,95,50,56,0);
          let dependencyq: Map<any, any> = new Map([[String.fromCharCode(108,97,116,95,49,95,49,56,0),String.fromCharCode(101,95,55,52,95,99,104,97,108,108,101,110,103,101,0)], [String.fromCharCode(122,95,57,48,95,109,97,114,107,101,116,0),String.fromCharCode(99,95,52,95,99,108,111,115,101,115,116,0)]]);
         eventG >>= Math.min(emptyX.length, 2);
         emptyX.push(dependencyq.size);
         strG = `${2 & strG.length}`;
         dependencyq.set(strG, (strG == String.fromCharCode(86,0) ? strG.length : dependencyq.size));
      }
         eventG &= eventG;
      for (let s = 0; s < 3; s++) {
         eventG /= Math.max(3, 3);
      }
      reminderH += `${elementsU.length}`;
   }
   while (basketballu.length < 5) {
       let benefitH = true;
       let libcrashsdkA: Map<any, any> = new Map([[String.fromCharCode(111,95,57,56,95,97,118,102,114,97,109,101,0),String.fromCharCode(112,101,97,107,115,95,115,95,52,48,0)], [String.fromCharCode(105,110,105,116,105,97,108,105,122,101,95,57,95,52,52,0),String.fromCharCode(121,95,51,49,95,116,97,107,101,110,0)], [String.fromCharCode(120,95,53,49,95,101,109,117,108,97,116,105,111,110,0),String.fromCharCode(112,95,53,57,95,112,114,105,109,97,114,105,101,115,0)]]);
       let profilec = 4.0;
       let libavformatU = String.fromCharCode(98,117,102,108,101,110,95,57,95,54,55,0);
       let homeactiveJ = String.fromCharCode(100,95,51,50,95,115,108,101,101,112,0);
      let iconviewere = profilec >= 8745539.0;
      do {
         profilec /= Math.max(1, (parseFloat(`${libavformatU == String.fromCharCode(49,0) ? libavformatU.length : libcrashsdkA.size}`)));
         if (iconviewere) {
            break;
         }
      } while ((libavformatU.length <= 4) && iconviewere);
      if (2 <= (3 | libcrashsdkA.size)) {
         libcrashsdkA = new Map([[`${profilec}`, (homeactiveJ == String.fromCharCode(52,0) ? homeactiveJ.length : parseInt(`${profilec}`))]]);
      }
      while (libavformatU != String.fromCharCode(49,0)) {
         homeactiveJ += "2";
         break;
      }
          let overlayy: Map<any, any> = new Map([[String.fromCharCode(100,95,51,49,95,99,116,120,112,0),33], [String.fromCharCode(117,95,51,50,95,112,97,115,115,101,100,0),642], [String.fromCharCode(109,112,101,103,97,117,100,105,111,100,101,99,116,97,98,95,105,95,55,52,0),981]]);
         libavformatU = `${homeactiveJ.length}`;
         overlayy.set(`${overlayy.size}`, overlayy.size);
          let readl = String.fromCharCode(100,105,99,116,105,111,110,97,114,121,95,51,95,49,54,0);
          let awayiconi: Map<any, any> = new Map([[String.fromCharCode(109,118,112,114,101,100,95,100,95,49,48,48,0),982], [String.fromCharCode(117,95,57,54,95,112,114,101,109,117,108,116,105,112,108,105,101,100,0),430], [String.fromCharCode(111,95,54,48,95,114,116,108,0),112]]);
          let actionsu = true;
         libavformatU = "1";
         readl += `${1 & readl.length}`;
         awayiconi.set(`${actionsu}`, 1);
      let componentu = benefitH ? !benefitH : benefitH;
      do {
          let borderlesst = 1.0;
          let rncoreD = String.fromCharCode(116,101,120,116,97,116,116,114,95,99,95,49,49,0);
         benefitH = String.fromCharCode(55,0) == homeactiveJ;
         borderlesst /= Math.max(parseFloat(`${parseInt(`${borderlesst}`)}`), 2);
         rncoreD = `${1 | rncoreD.length}`;
         if (componentu) {
            break;
         }
      } while (componentu && (homeactiveJ.endsWith(`${benefitH}`)));
         libavformatU = `${((benefitH ? 1 : 5) ^ homeactiveJ.length)}`;
         libavformatU += `${homeactiveJ.length + 2}`;
      for (let f = 0; f < 3; f++) {
          let stylesm = 3.0;
         benefitH = libavformatU.length <= homeactiveJ.length;
         stylesm *= 1;
      }
         benefitH = profilec < 8.98;
          let encryptK: Array<any> = [818, 232, 146];
         libcrashsdkA.set(homeactiveJ, homeactiveJ.length - encryptK.length);
         profilec /= Math.max(2, parseFloat(`${homeactiveJ.length - 3}`));
      if (libcrashsdkA.get(`${profilec}`) == null) {
         profilec -= parseFloat(`${libavformatU.length & homeactiveJ.length}`);
      }
         libcrashsdkA = new Map([[`${libcrashsdkA.size}`, libcrashsdkA.size]]);
          let statsnomoredatal = String.fromCharCode(101,95,52,51,95,97,112,112,108,121,0);
          let specm = true;
         libcrashsdkA.set(statsnomoredatal, homeactiveJ.length - statsnomoredatal.length);
         specm = !specm;
      footballfieldf = !benefitH || elementsU.length == 68;
      break;
   }
      episodesR = new Map([[`${button6}`, nextK & parseInt(`${button6}`)]]);
   for (let p = 0; p < 3; p++) {
      langkeyq >>= Math.min(5, Math.abs(parseInt(`${button6}`)));
   }
   while (Array.from(episodesR.keys()).includes(`${alertg}`)) {
      episodesR.set(configurey, 2 << (Math.min(5, configurey.length)));
      break;
   }
   let unimplementedview8 = reminderH == String.fromCharCode(122,103,110,119,0);
   do {
      reminderH += "3";
      if (unimplementedview8) {
         break;
      }
   } while (((reminderH.length - 5) <= 4 || (reminderH.length - 5) <= 4) && unimplementedview8);
   if (2 <= (configurey.length + alertg) && (configurey.length + 2) <= 2) {
      configurey += `${reminderH.length}`;
   }
      return 0;
    }
  };

  return (
    <View
      style={{
        ...styles.container,
        marginBottom: isPlay ? 0 : '50%',
        backgroundColor: isPlay ? '#000000' : colors.background,
        minHeight: getMinHeight(),
      }}>
      {!isPlay && isPotrait && (
        <>
          <NoWifiSvg />
          <Text
            style={{
              ...textVariants.subBody,
              color: colors.text,
              textAlign: 'center',
              paddingTop: 10,
            }}>
            无网络
          </Text>
          <Text
            style={{
              ...textVariants.subBody,
              color: colors.muted,
              textAlign: 'center',
              paddingTop: 12,
            }}>
            网络连接失败，请刷新重试
          </Text>
          <TouchableOpacity activeOpacity={0.7} onPress={onClickRetry}>
            <View
              style={{
                ...styles.refreshBtn,
                backgroundColor: colors.title,
              }}>
              <View style={{ position: 'relative', top: 2, paddingRight: 3 }}>
                <RefreshSvg />
              </View>
              <Text
                style={{
                  ...styles.head,
                  color: colors.background,
                }}>
                点击重试
              </Text>
            </View>
          </TouchableOpacity>
        </>
      )}
      {isPlay && (
        <>
          <View>
            <YingshiGreySvg />
          </View>
          <Text
            style={{
              ...textVariants.subBody,
              color: colors.muted,
              textAlign: 'center',
              paddingTop: 12,
            }}>
            无网络或断网，请刷新视频
          </Text>
          <TouchableOpacity activeOpacity={0.7} onPress={onClickRetry}>
            <View
              style={{
                ...styles.refreshBtn,
                backgroundColor: colors.title,
              }}>
              <View style={{ position: 'relative', top: 2, paddingRight: 3 }}>
                <RefreshSvg />
              </View>
              <Text
                style={{
                  ...styles.head,
                  color: colors.background,
                }}>
                刷新
              </Text>
            </View>
          </TouchableOpacity>
        </>
      )}
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    width: "100%",
    aspectRatio: 16 / 9,
  },
  btns: {
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
  },
  btn: {
    flex: 1,
  },
  head: {
    color: 'white',
    textAlign: 'center',
  },
  refreshBtn: {
    marginTop: 20,
    borderRadius: 30,
    display: 'flex',
    flexDirection: 'row',
    paddingVertical: 8,
    paddingHorizontal: 15,
    alignItems: 'center',
    justifyContent: 'center',
    fontWeight: 'bold',
  },
});
