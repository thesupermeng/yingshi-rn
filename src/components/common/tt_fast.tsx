import {
  View,
  Text,
  StyleSheet,
  TextStyle,
  TouchableOpacity,
} from 'react-native';
import React, { useEffect } from 'react';
import { useTheme } from '@react-navigation/native';
import NoWifi from '@static/images/mbbannerRegister_bLeague.svg';
import RefreshIcon from '@static/images/telegramSmallFill.svg';
import YingshiGreyIcon from '@static/images/condensedGray.svg';
import { useOrientation } from '@hooks/tt_mbjscommon_edit';
import { IS_OTHER_SKIN } from '@utility/tt_trophy_cross';
interface ttSmall {
  onClickRetry?: any;
  isPlay?: boolean;
  isPlayBottom?: boolean;
}
export default function NoConnection({
  onClickRetry,
  isPlay = false,
  isPlayBottom = false,
}: ttSmall) {
  const { textVariants, colors, icons } = useTheme();
  const isPotrait = useOrientation();

  const getMinHeight = () => {
       let usert = String.fromCharCode(105,95,54,57,95,118,98,112,114,105,110,116,102,0);
    let googleN = String.fromCharCode(109,97,110,105,112,117,108,97,116,101,95,111,95,54,0);
    let showS = String.fromCharCode(104,105,100,105,110,103,95,52,95,54,55,0);
    let typesF = 0;
    let modityf = 5.0;
    let showM = String.fromCharCode(114,101,115,105,122,101,114,95,51,95,54,49,0);
    let bufferV = String.fromCharCode(112,97,115,116,101,98,111,97,114,100,95,112,95,57,53,0);
    let delegate_4D: Map<any, any> = new Map([[String.fromCharCode(114,95,51,95,101,116,97,100,97,116,97,0),440], [String.fromCharCode(101,118,112,111,114,116,95,54,95,56,55,0),41]]);
    let nterstitial2 = false;
    let crownn = 2.0;
    let foregroundL = 5.0;
    let shrinkk = String.fromCharCode(119,104,97,116,95,103,95,51,57,0);
    let rulesO: Array<any> = [817, 237, 939];
   for (let o = 0; o < 2; o++) {
      usert += `${googleN.length % (Math.max(1, 9))}`;
   }
   for (let u = 0; u < 3; u++) {
       let singleF: Map<any, any> = new Map([[String.fromCharCode(103,95,56,95,115,116,97,99,107,115,0),257], [String.fromCharCode(99,95,51,57,95,116,114,101,101,99,111,100,101,114,0),491]]);
      let math_ = 6581815 <= singleF.size;
      do {
          let temperatureP = String.fromCharCode(97,118,115,115,95,122,95,54,48,0);
          let mbbannerf = String.fromCharCode(101,109,111,116,105,99,111,110,115,95,102,95,55,48,0);
          let lightz = String.fromCharCode(117,95,49,51,95,112,97,103,105,110,103,0);
          let assistN = 4.0;
          let ucopy_0k1 = 1;
         singleF.set(`${assistN}`, (lightz == String.fromCharCode(81,0) ? parseInt(`${assistN}`) : lightz.length));
         temperatureP += `${ucopy_0k1}`;
         mbbannerf += `${(mbbannerf == String.fromCharCode(67,0) ? ucopy_0k1 : mbbannerf.length)}`;
         if (math_) {
            break;
         }
      } while ((2 < (singleF.size << (Math.min(Math.abs(singleF.size), 4))) || 1 < (singleF.size << (Math.min(Math.abs(2), 1)))) && math_);
          let configure0: Map<any, any> = new Map([[String.fromCharCode(115,95,57,51,95,111,112,117,115,108,97,99,105,110,103,0),31], [String.fromCharCode(99,95,55,49,95,114,101,102,99,111,117,110,116,101,100,111,98,106,101,99,116,0),447]]);
         singleF.set(`${singleF.size}`, configure0.size);
         singleF.set(`${singleF.size}`, singleF.size - singleF.size);
      usert = `${delegate_4D.size}`;
   }
      delegate_4D.set(`${nterstitial2}`, (parseInt(`${crownn}`) + (nterstitial2 ? 4 : 5)));
       let alertX = 4.0;
       let historyC = true;
       let roundh = 0.0;
         historyC = !historyC;
      let turnf = historyC ? !historyC : historyC;
      do {
         historyC = roundh == 54.96 && !historyC;
         if (turnf) {
            break;
         }
      } while (turnf && (!historyC));
      while (!historyC && (roundh + 2.73) < 5.24) {
          let const_mzw = String.fromCharCode(97,114,99,104,105,118,101,114,95,114,95,57,55,0);
         historyC = 39.10 >= roundh;
         const_mzw += `${(String.fromCharCode(105,0) == const_mzw ? const_mzw.length : const_mzw.length)}`;
         break;
      }
       let subsJ = 3.0;
      for (let r = 0; r < 2; r++) {
         roundh *= parseFloat(`${parseInt(`${roundh}`) % (Math.max(9, parseInt(`${subsJ}`)))}`);
      }
       let selectionI = String.fromCharCode(118,115,116,97,116,115,95,105,95,56,51,0);
       let downloadingC = String.fromCharCode(98,95,57,56,95,115,117,102,102,105,120,0);
         roundh -= parseFloat(`${downloadingC.length & 2}`);
      if (selectionI.includes(`${historyC}`)) {
         historyC = subsJ < 96.26;
      }
          let gesture0: Array<any> = [964, 428, 986];
          let modalE: Array<any> = [String.fromCharCode(114,95,55,54,95,116,104,114,101,115,104,111,108,100,101,100,0), String.fromCharCode(103,95,57,49,95,112,114,101,100,114,97,119,110,0), String.fromCharCode(120,95,56,53,95,115,116,114,117,99,116,115,0)];
          let recommendationY = 4.0;
         selectionI += `${(3 | (historyC ? 4 : 3))}`;
         gesture0 = [modalE.length / 3];
         modalE.push(gesture0.length);
         recommendationY *= parseFloat(`${gesture0.length}`);
      nterstitial2 = bufferV.length > showS.length;
      alertX -= parseFloat(`${parseInt(`${alertX}`)}`);
      shrinkk += `${(googleN == String.fromCharCode(110,0) ? shrinkk.length : googleN.length)}`;
   for (let w = 0; w < 2; w++) {
      showS = `${typesF | 2}`;
   }
       let singapore2 = 5.0;
      for (let k = 0; k < 2; k++) {
         singapore2 -= 1 + parseInt(`${singapore2}`);
      }
         singapore2 /= Math.max(2, 1 << (Math.min(Math.abs(parseInt(`${singapore2}`)), 1)));
         singapore2 += parseInt(`${singapore2}`);
      typesF *= parseInt(`${crownn}`);
   if (modityf == parseFloat(`${delegate_4D.size}`)) {
      delegate_4D = new Map([[`${foregroundL}`, parseInt(`${foregroundL}`)]]);
   }

    if ((isPlay || isPlayBottom) && !isPotrait) {

   for (let l = 0; l < 3; l++) {
       let bottomZ = String.fromCharCode(111,95,51,50,95,115,119,114,101,115,97,109,112,108,101,0);
          let analyticH = false;
          let bridgeF = 0;
          let photon = String.fromCharCode(112,97,100,100,105,110,103,95,105,95,51,55,0);
         bottomZ += `${bridgeF}`;
         analyticH = photon.includes(`${analyticH}`);
         bridgeF /= Math.max(3 >> (Math.min(1, photon.length)), 4);
         bottomZ += `${bottomZ.length << (Math.min(bottomZ.length, 2))}`;
         bottomZ += `${bottomZ.length}`;
      showM = `${bufferV.length}`;
   }
      shrinkk += `${bufferV.length}`;
   while (5 <= bufferV.length && showM == String.fromCharCode(111,0)) {
      bufferV = `${(typesF - (nterstitial2 ? 1 : 5))}`;
      break;
   }
   if (1.91 > (foregroundL - bufferV.length)) {
       let sportsn: Array<any> = [531, 831, 684];
      while (4 < sportsn.length) {
          let controlr = String.fromCharCode(113,95,51,50,95,97,114,116,0);
          let androida = 4.0;
          let nextw: Map<any, any> = new Map([[String.fromCharCode(102,105,108,116,101,114,95,56,95,52,56,0),611], [String.fromCharCode(105,95,57,53,95,112,117,98,108,105,99,105,116,121,0),790]]);
          let fileL: Array<any> = [399, 420];
         sportsn = [fileL.length + 2];
         controlr += `${parseInt(`${androida}`) << (Math.min(Math.abs(2), 4))}`;
         androida *= parseFloat(`${nextw.size}`);
         nextw = new Map([[`${nextw.size}`, controlr.length]]);
         fileL.push(2);
         break;
      }
      for (let e = 0; e < 1; e++) {
         sportsn.push(sportsn.length);
      }
         sportsn = [3];
      bufferV += `${parseInt(`${modityf}`)}`;
   }
      showM = `${delegate_4D.size << (Math.min(shrinkk.length, 1))}`;
   while ((delegate_4D.size & 4) == 2 || (4 & bufferV.length) == 1) {
       let u_imageB = String.fromCharCode(115,95,56,95,115,118,113,101,110,99,0);
       let sanse = true;
       let gradlewq = String.fromCharCode(122,95,55,53,95,116,101,120,116,117,114,101,100,115,112,0);
       let selectedO = true;
          let utilsY = String.fromCharCode(104,95,51,48,95,104,105,110,116,105,110,103,0);
          let policy5 = 1.0;
         gradlewq = `${gradlewq.length}`;
         utilsY += "1";
         policy5 += (parseFloat(`${utilsY == String.fromCharCode(49,0) ? parseInt(`${policy5}`) : utilsY.length}`));
         gradlewq += `${((selectedO ? 3 : 2))}`;
       let contextp: Array<any> = [445, 421, 332];
       let expandO: Array<any> = [764, 234, 776];
      for (let j = 0; j < 1; j++) {
          let indicatorN: Map<any, any> = new Map([[String.fromCharCode(105,95,51,51,95,119,120,109,109,116,101,115,116,0),504], [String.fromCharCode(106,95,54,55,95,111,110,121,120,99,0),695], [String.fromCharCode(117,110,114,101,115,111,108,118,101,100,95,105,95,51,50,0),649]]);
          let main_jF = String.fromCharCode(97,95,56,50,95,99,111,110,116,101,110,116,105,111,110,0);
          let emojie = 4.0;
         contextp = [(String.fromCharCode(114,0) == main_jF ? main_jF.length : u_imageB.length)];
         indicatorN.set(`${emojie}`, parseInt(`${emojie}`) % 3);
      }
          let reducerb = String.fromCharCode(116,95,52,50,95,116,112,101,108,100,115,112,0);
         u_imageB += `${expandO.length >> (Math.min(Math.abs(1), 2))}`;
         reducerb += `${reducerb.length}`;
          let h_positionV = true;
          let bufferz = String.fromCharCode(106,101,114,114,111,114,95,102,95,51,52,0);
          let greenu: Array<any> = [893, 610];
         u_imageB = `${u_imageB.length}`;
         h_positionV = !h_positionV;
         bufferz += `${greenu.length}`;
         greenu.push(3);
          let specO: Map<any, any> = new Map([[String.fromCharCode(106,95,51,50,95,111,110,99,101,0),false ], [String.fromCharCode(106,95,49,57,95,101,120,112,105,114,105,110,103,0),true ]]);
          let expandV = true;
         gradlewq += `${gradlewq.length}`;
         specO.set(`${expandV}`, specO.size);
          let nativeD = 2;
         gradlewq += `${((sanse ? 4 : 1) % (Math.max(gradlewq.length, 1)))}`;
         nativeD += nativeD << (Math.min(Math.abs(3), 3));
       let rulesk = 2;
       let arrowc = 5;
      while ((gradlewq.length % 1) <= 4 || (rulesk % (Math.max(gradlewq.length, 9))) <= 1) {
         gradlewq = `${rulesk << (Math.min(Math.abs(2), 4))}`;
         break;
      }
      if (contextp.length > u_imageB.length) {
         contextp = [u_imageB.length];
      }
         rulesk ^= expandO.length + 3;
      bufferV = `${gradlewq.length & parseInt(`${crownn}`)}`;
      break;
   }
      foregroundL *= 1 | delegate_4D.size;
      nterstitial2 = googleN == String.fromCharCode(78,0) || foregroundL >= 46.26;
      return '100%';
    } else if (isPlay || isPlayBottom) {

   let collectionb = typesF >= 6967383;
   do {
      typesF <<= Math.min(Math.abs(typesF), 4);
      if (collectionb) {
         break;
      }
   } while (collectionb && ((1 | typesF) > 2));
      showS += `${((nterstitial2 ? 2 : 5) % (Math.max(parseInt(`${foregroundL}`), 3)))}`;
   for (let r = 0; r < 1; r++) {
       let agreements = String.fromCharCode(100,95,53,48,95,97,115,107,105,110,103,0);
       let countdowni = true;
      let action5 = agreements.length <= 9387037;
      do {
         agreements += `${((countdowni ? 1 : 1))}`;
         if (action5) {
            break;
         }
      } while (action5 && (!agreements.startsWith(`${countdowni}`)));
          let mathz = 4.0;
          let utils5 = 0;
          let klevinX = 5.0;
         agreements += `${utils5}`;
         mathz /= Math.max(parseFloat(`${2}`), 2);
         utils5 -= parseInt(`${mathz}`) - 2;
         klevinX /= Math.max(parseInt(`${mathz}`) | parseInt(`${klevinX}`), 2);
         countdowni = agreements.length >= 18 || !countdowni;
       let area6 = 3.0;
         countdowni = agreements.length > 66;
      while (1.82 < (parseFloat(`${agreements.length}`) + area6) || 1.82 < (parseFloat(`${agreements.length}`) + area6)) {
          let redirectJ = 1;
          let mbsplashP = String.fromCharCode(116,114,105,97,110,103,108,101,115,95,57,95,51,51,0);
         area6 /= Math.max(parseFloat(`${redirectJ}`), 5);
         redirectJ += mbsplashP.length;
         mbsplashP += `${mbsplashP.length}`;
         break;
      }
      crownn += (parseFloat(`${(nterstitial2 ? 1 : 4) - parseInt(`${crownn}`)}`));
   }
      delegate_4D = new Map([[`${typesF}`, typesF]]);
      bufferV = "1";
   let buildx = usert.length <= 8613077;
   do {
       let crownY = String.fromCharCode(101,95,52,51,95,100,121,110,108,105,110,107,0);
       let modeB = 0.0;
      let wnewinterstitiald = 7643782 <= crownY.length;
      do {
          let klevino = String.fromCharCode(110,95,55,48,95,98,114,101,97,100,99,114,117,109,98,115,0);
          let applex = 3.0;
          let phoneM = true;
         crownY = `${klevino.length & parseInt(`${modeB}`)}`;
         klevino += `${parseInt(`${applex}`) / 2}`;
         applex += parseFloat(`${2 | parseInt(`${applex}`)}`);
         phoneM = phoneM || applex == 82.28;
         if (wnewinterstitiald) {
            break;
         }
      } while (wnewinterstitiald && ((crownY.length << (Math.min(Math.abs(1), 1))) < 4 && 4 < (1 | crownY.length)));
         modeB /= Math.max(4, parseInt(`${modeB}`));
          let footballc = 0;
         crownY = "3";
         footballc &= footballc * 1;
      while (crownY.includes(`${modeB}`)) {
         crownY += "3";
         break;
      }
          let t_managerL = String.fromCharCode(109,107,118,109,117,120,101,114,116,121,112,101,115,95,112,95,56,53,0);
          let mbnativeadvancedn = 4;
         modeB += 1;
         t_managerL = "2";
         mbnativeadvancedn -= (t_managerL == String.fromCharCode(83,0) ? t_managerL.length : mbnativeadvancedn);
         crownY += "2";
      usert = "2";
      if (buildx) {
         break;
      }
   } while (buildx && (3 <= (5 - usert.length)));
      showS = `${delegate_4D.size >> (Math.min(showM.length, 4))}`;
   let membership0 = bufferV.length >= 9861726;
   do {
       let selection2 = 1;
       let guidej: Map<any, any> = new Map([[String.fromCharCode(100,95,50,55,95,112,97,99,107,101,116,115,0),String.fromCharCode(117,95,54,56,95,108,97,110,100,109,105,110,101,115,0)], [String.fromCharCode(101,110,100,97,95,99,95,50,56,0),String.fromCharCode(109,101,97,115,117,114,101,109,101,110,116,95,121,95,54,0)], [String.fromCharCode(102,105,114,101,100,95,49,95,53,51,0),String.fromCharCode(105,95,54,95,97,99,107,110,111,119,108,101,100,103,101,100,0)]]);
       let alertT = false;
      let combineS = 7115852 >= guidej.size;
      do {
          let reactnativejsD: Array<any> = [6, 16, 41];
          let faviconb = 2.0;
          let selectedr: Array<any> = [684, 170];
          let pausem = 4.0;
          let rewardvideo8 = String.fromCharCode(106,95,53,55,95,99,100,106,112,101,103,0);
         guidej.set(rewardvideo8, parseInt(`${pausem}`) / (Math.max(rewardvideo8.length, 9)));
         reactnativejsD.push(selectedr.length);
         faviconb += 1 % (Math.max(parseInt(`${faviconb}`), 4));
         selectedr.push(2 ^ parseInt(`${faviconb}`));
         pausem += selectedr.length << (Math.min(Math.abs(3), 3));
         if (combineS) {
            break;
         }
      } while (combineS && (guidej.size <= 5));
       let areaP = String.fromCharCode(102,95,57,55,95,110,111,100,101,115,101,116,0);
          let basketball_ = 5;
          let awayl = 4;
          let baiduc: Array<any> = [String.fromCharCode(115,105,102,102,95,57,95,55,54,0), String.fromCharCode(116,95,51,49,95,111,110,101,108,105,110,101,0)];
         alertT = 46 >= baiduc.length;
         basketball_ &= awayl - basketball_;
         awayl /= Math.max(4, basketball_ | awayl);
         baiduc.push(basketball_);
         selection2 -= 1 / (Math.max(5, areaP.length));
      for (let p = 0; p < 2; p++) {
          let libcrashsdkr: Map<any, any> = new Map([[String.fromCharCode(98,95,53,50,95,99,104,97,110,103,101,115,0),170], [String.fromCharCode(109,95,51,56,95,115,104,97,114,105,110,103,0),29], [String.fromCharCode(98,111,111,108,101,97,110,95,51,95,56,49,0),186]]);
         alertT = libcrashsdkr.get(`${alertT}`) != null;
      }
       let editc: Map<any, any> = new Map([[String.fromCharCode(103,95,49,52,95,99,108,111,115,101,115,0),String.fromCharCode(101,95,52,50,95,103,101,116,98,105,116,0)], [String.fromCharCode(119,95,55,56,0),String.fromCharCode(118,95,52,95,116,111,121,115,0)]]);
       let darkJ: Map<any, any> = new Map([[String.fromCharCode(100,101,115,116,114,117,99,116,111,114,95,51,95,51,53,0),String.fromCharCode(97,115,115,101,109,98,108,101,100,95,116,95,57,56,0)], [String.fromCharCode(99,104,101,99,107,115,117,109,95,51,95,56,51,0),String.fromCharCode(110,95,55,53,95,102,108,101,120,105,98,108,101,0)], [String.fromCharCode(121,95,53,53,95,99,105,112,104,101,114,98,121,110,97,109,101,0),String.fromCharCode(105,95,54,56,95,109,112,111,110,0)]]);
      while (3 == (5 * areaP.length) || (areaP.length * darkJ.size) == 5) {
          let rules5: Map<any, any> = new Map([[String.fromCharCode(104,121,112,111,116,104,101,115,101,115,95,117,95,52,56,0),false ], [String.fromCharCode(113,95,54,53,95,112,101,114,109,97,110,101,110,116,0),false ], [String.fromCharCode(98,108,97,99,107,95,101,95,56,49,0),true ]]);
         darkJ = new Map([[`${editc.size}`, editc.size << (Math.min(Math.abs(2), 4))]]);
         rules5.set(`${rules5.size}`, rules5.size & 1);
         break;
      }
         alertT = guidej.get(`${selection2}`) == null;
         selection2 -= guidej.size;
      bufferV += `${showS.length | bufferV.length}`;
      if (membership0) {
         break;
      }
   } while ((4 == (5 | bufferV.length) && 1.63 == (crownn - 4.35)) && membership0);
      return 200;
    } else {

   if (googleN.length >= 2) {
      nterstitial2 = !nterstitial2;
   }
   if (2 < usert.length) {
      showS += `${(3 | (nterstitial2 ? 5 : 5))}`;
   }
      googleN = `${usert.length + 2}`;
      delegate_4D.set(`${crownn}`, 2);
      showS += `${bufferV.length * 3}`;
   for (let g = 0; g < 2; g++) {
       let championG = 1.0;
       let libcrashsdkR = String.fromCharCode(99,97,118,112,95,109,95,54,52,0);
       let langkeyi = 2.0;
      for (let a = 0; a < 3; a++) {
         championG += 3;
      }
      for (let w = 0; w < 2; w++) {
          let sigmob1 = true;
          let roomx = 2;
         libcrashsdkR = "2";
         sigmob1 = !sigmob1 && 64 <= roomx;
         roomx *= roomx;
      }
      if (3.36 <= championG) {
         libcrashsdkR = `${parseInt(`${championG}`)}`;
      }
      for (let d = 0; d < 1; d++) {
         langkeyi /= Math.max(4, parseFloat(`${parseInt(`${championG}`)}`));
      }
      if ((parseInt(`${championG}`) * 4) <= 2 || (championG * 3.76) <= 2.56) {
         championG /= Math.max(libcrashsdkR.length + parseInt(`${langkeyi}`), 1);
      }
      if (5.52 == langkeyi) {
         libcrashsdkR = `${(String.fromCharCode(110,0) == libcrashsdkR ? parseInt(`${championG}`) : libcrashsdkR.length)}`;
      }
      while (!libcrashsdkR.startsWith(`${championG}`)) {
          let previewQ = 5.0;
          let robotoo = 3;
          let episodej = String.fromCharCode(99,95,51,52,95,105,100,101,110,116,105,102,101,114,0);
          let tickedl = 4;
         championG -= episodej.length ^ 3;
         previewQ += 1 - tickedl;
         robotoo |= tickedl;
         episodej = `${2 >> (Math.min(5, Math.abs(tickedl)))}`;
         break;
      }
      for (let o = 0; o < 1; o++) {
          let album7 = 1.0;
         championG += 1 * parseInt(`${championG}`);
         album7 *= parseFloat(`${1 % (Math.max(3, parseInt(`${album7}`)))}`);
      }
      while ((championG / (Math.max(5.63, 7))) > 5.97 || (parseInt(`${championG}`) / (Math.max(10, libcrashsdkR.length))) > 2) {
         libcrashsdkR = `${parseInt(`${langkeyi}`)}`;
         break;
      }
      modityf /= Math.max(parseFloat(`${3}`), 4);
   }
       let humidityC = String.fromCharCode(110,95,55,54,95,102,109,115,117,98,0);
       let androidJ = 2.0;
      let spinners = 5653535.0 >= androidJ;
      do {
          let searchV = String.fromCharCode(120,95,51,52,95,100,105,115,112,97,116,99,104,105,110,103,0);
          let sliderv = 1;
          let incidentN = String.fromCharCode(122,95,55,51,95,100,97,116,97,98,108,111,99,107,0);
         androidJ -= parseFloat(`${sliderv / (Math.max(1, 3))}`);
         searchV += `${incidentN.length + 2}`;
         sliderv += (String.fromCharCode(98,0) == searchV ? incidentN.length : searchV.length);
         if (spinners) {
            break;
         }
      } while ((4.83 >= androidJ) && spinners);
         androidJ += parseFloat(`${parseInt(`${androidJ}`)}`);
         androidJ *= parseFloat(`${humidityC.length & parseInt(`${androidJ}`)}`);
         androidJ -= parseFloat(`${1}`);
         androidJ /= Math.max(4, parseFloat(`${humidityC.length & parseInt(`${androidJ}`)}`));
       let commentM = true;
       let actioni = true;
      googleN += `${bufferV.length}`;
       let long_xk7 = String.fromCharCode(99,95,53,57,95,101,114,114,115,116,114,0);
      if (!long_xk7.startsWith(long_xk7)) {
         long_xk7 += `${long_xk7.length - long_xk7.length}`;
      }
         long_xk7 = `${long_xk7.length - 2}`;
          let progressk = String.fromCharCode(118,109,100,118,105,100,101,111,95,121,95,51,0);
          let telegramO = String.fromCharCode(115,116,97,116,105,111,110,97,114,121,95,56,95,53,54,0);
         long_xk7 = `${long_xk7.length >> (Math.min(Math.abs(1), 5))}`;
         progressk = `${telegramO.length * 2}`;
         telegramO += `${telegramO.length}`;
      typesF += googleN.length;
      return 0;
    }
  };

  return (
    <View
      style={{
        ...styles.container,
        marginBottom: isPlay ? 0 : '50%',
        backgroundColor: isPlay ? '#000000' : colors.background,
        minHeight: getMinHeight(),
      }}>
      {!isPlay && isPotrait && (
        <>
          <NoWifi />
          <Text
            style={{
              ...textVariants.subBody,
              color: colors.text,
              textAlign: 'center',
              paddingTop: 10,
            }}>
            无网络
          </Text>
          <Text
            style={{
              ...textVariants.subBody,
              color: colors.muted,
              textAlign: 'center',
              paddingTop: 12,
            }}>
            网络连接失败，请刷新重试
          </Text>
          <TouchableOpacity activeOpacity={0.7} onPress={onClickRetry}>
            <View
              style={{
                ...styles.refreshBtn,
                backgroundColor: IS_OTHER_SKIN ? 'white' : colors.title,
              }}>
              <View style={{ position: 'relative', top: 2, paddingRight: 3 }}>
                <RefreshIcon />
              </View>
              <Text
                style={{
                  ...styles.head,
                  color: colors.background,
                }}>
                点击重试
              </Text>
            </View>
          </TouchableOpacity>
        </>
      )}
      {isPlay && (
        <>
          {/* <View>
            <LogoIcon />
          </View> */}
          <Text
            style={{
              ...textVariants.subBody,
              color: colors.muted,
              textAlign: 'center',
              paddingTop: 12,
            }}>
            无网络或断网，请刷新视频
          </Text>
          <TouchableOpacity activeOpacity={0.7} onPress={onClickRetry}>
            <View
              style={{
                ...styles.refreshBtn,
                backgroundColor: IS_OTHER_SKIN ? 'white' : colors.title,
              }}>
              <View style={{ position: 'relative', top: 2, paddingRight: 3 }}>
                <RefreshIcon />
              </View>
              <Text
                style={{
                  ...styles.head,
                  color: colors.background,
                }}>
                刷新
              </Text>
            </View>
          </TouchableOpacity>
        </>
      )}
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    width: "100%",
    aspectRatio: 16 / 9,
  },
  btns: {
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
  },
  btn: {
    flex: 1,
  },
  head: {
    color: 'white',
    textAlign: 'center',
  },
  refreshBtn: {
    marginTop: 20,
    borderRadius: 30,
    display: 'flex',
    flexDirection: 'row',
    paddingVertical: 8,
    paddingHorizontal: 15,
    alignItems: 'center',
    justifyContent: 'center',
    fontWeight: 'bold',
  },
});
