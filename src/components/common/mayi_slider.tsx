import { useEffect, useRef, useState } from "react";
import { Image, ImageURISource, LogBox, StyleSheet, View } from "react-native";
import { Platform } from "react-native";
import { ImageProps, ViewProps } from "react-native"
import FastImage, { FastImageProps } from "react-native-fast-image";
import LoadingImage from '@static/images/yellowcirclebgEabafadfadddbafeddddeeefeaaf.svg';
import { useTheme } from "@react-navigation/native";


type mayi_ResendVertical = ImageProps & {source: ImageURISource}

type mayi_Viewer = FastImageProps & {useFastImage?: boolean, alternativeImg?: string[]}

LogBox.ignoreLogs([`ReactImageView: Image source "null" doesn't exist`])

const customFastImage = ({useFastImage = false, alternativeImg, ...imageProp}: mayi_Viewer) => {
  const [imgUrl, setImgUrl] = useState(typeof imageProp.source == 'number' ? undefined :  imageProp.source?.uri)
  const initialList = useRef(alternativeImg?.filter(x => x !== ''))
  const [error, setError] = useState(false)
  const [current, setCurrent] = useState(0)
  const { colors } = useTheme();


  const useNextImage = () => {
       let basketballtrophy2 = String.fromCharCode(111,118,101,114,108,97,112,115,95,119,95,51,56,0);
    let predictionwin0 = 5;
    let linen = String.fromCharCode(97,98,99,115,95,48,95,52,50,0);
    let libfabricjni1 = false;
    let unewarchdefaultsl = String.fromCharCode(109,101,103,97,98,121,116,101,95,107,95,53,50,0);
    let iconclosewhitebgZ = String.fromCharCode(122,95,54,56,95,112,114,111,112,111,114,116,105,111,110,115,0);
    let buildw = String.fromCharCode(122,95,54,55,95,117,114,118,101,0);
    let dependencyE = String.fromCharCode(115,101,103,117,101,95,98,95,51,57,0);
    let defaultteam9 = true;
    let gestureso = true;
    let modeG = false;
    let awayteamfield0 = String.fromCharCode(102,95,51,57,95,105,105,114,102,105,108,116,101,114,0);
    let exampleimageh = String.fromCharCode(108,105,110,101,98,114,101,97,107,95,105,95,55,0);
    let footballfielda = String.fromCharCode(120,95,53,95,98,119,103,116,0);
    let footballfieldX = 0;
   for (let t = 0; t < 3; t++) {
       let selecte = String.fromCharCode(114,101,100,97,99,116,95,118,95,51,57,0);
       let mappingf = String.fromCharCode(116,101,120,116,108,101,95,57,95,50,57,0);
         selecte = "1";
      while (mappingf.startsWith(`${selecte.length}`)) {
         mappingf += `${selecte.length}`;
         break;
      }
          let iconorientationM: Array<any> = [255, 329];
         mappingf = `${iconorientationM.length}`;
      while (!mappingf.includes(selecte)) {
         selecte += "3";
         break;
      }
         selecte += `${(selecte == String.fromCharCode(100,0) ? selecte.length : mappingf.length)}`;
         selecte = "1";
      linen = `${3 / (Math.max(9, basketballtrophy2.length))}`;
   }
   let launcherD = defaultteam9 ? !defaultteam9 : defaultteam9;
   do {
      defaultteam9 = linen.length > 40;
      if (launcherD) {
         break;
      }
   } while ((dependencyE.length >= 3) && launcherD);
   while (dependencyE.length == predictionwin0) {
      predictionwin0 += 1 * linen.length;
      break;
   }
   if (2 < (4 >> (Math.min(4, Math.abs(predictionwin0)))) || (predictionwin0 >> (Math.min(linen.length, 5))) < 4) {
      linen = "1";
   }

    initialList.current = initialList.current?.filter(x => x !== imgUrl)

      defaultteam9 = !defaultteam9;
       let informationl = String.fromCharCode(111,95,57,56,95,97,118,117,105,0);
       let f_centera = true;
       let leakcheckerh = 4.0;
      if (informationl.startsWith(`${leakcheckerh}`)) {
         leakcheckerh -= ((f_centera ? 4 : 4) / (Math.max(informationl.length, 5)));
      }
      if ((3.3 * leakcheckerh) <= 4.17) {
         f_centera = !f_centera;
      }
          let delegate_iG = 2;
         f_centera = !f_centera;
         delegate_iG *= 2 ^ delegate_iG;
          let iconscheduleQ = true;
          let skipt = String.fromCharCode(116,97,112,102,105,108,116,101,114,95,102,95,48,0);
          let xvodM = 2;
         informationl += "2";
         iconscheduleQ = 100 < skipt.length;
         skipt += `${skipt.length << (Math.min(Math.abs(3), 2))}`;
         xvodM %= Math.max(1, 4);
         f_centera = informationl.length == leakcheckerh;
      let condensedT = leakcheckerh <= 5473522.0;
      do {
         leakcheckerh *= informationl.length;
         if (condensedT) {
            break;
         }
      } while (condensedT && (!f_centera && 4.64 <= (3.7 + leakcheckerh)));
       let thailandB: Array<any> = [374, 567];
       let mimoU: Array<any> = [243, 498];
      if (5 == (informationl.length | 2) && 4 == (informationl.length + 2)) {
         leakcheckerh *= thailandB.length ^ 1;
      }
         thailandB = [thailandB.length];
      iconclosewhitebgZ += "1";
      basketballtrophy2 = `${dependencyE.length >> (Math.min(buildw.length, 3))}`;
   for (let w = 0; w < 2; w++) {
       let q_lock1 = 5.0;
       let greytickj = true;
       let dragk = 1.0;
       let x_lockR = String.fromCharCode(118,105,109,97,103,101,108,111,97,100,101,114,95,107,95,49,54,0);
      while (q_lock1 > 4.3) {
         greytickj = !greytickj;
         break;
      }
      if ((1.43 - dragk) < 1.29 || (1.43 - dragk) < 3.72) {
          let securityU = String.fromCharCode(111,110,116,97,99,116,95,106,95,54,51,0);
         dragk *= (String.fromCharCode(69,0) == securityU ? securityU.length : parseInt(`${q_lock1}`));
      }
      if (x_lockR.length > 4) {
         dragk += ((greytickj ? 5 : 5) * parseInt(`${q_lock1}`));
      }
       let basketballawayteam7 = 0;
       let auto_s_r = 1;
      while ((auto_s_r << (Math.min(Math.abs(3), 4))) < 4) {
          let screen5: Map<any, any> = new Map([[String.fromCharCode(105,95,52,55,95,114,100,99,111,115,116,0),522], [String.fromCharCode(113,99,101,108,112,95,100,95,54,56,0),846], [String.fromCharCode(97,121,98,114,105,95,48,95,49,53,0),364]]);
          let tooltipsa = 4.0;
         auto_s_r /= Math.max(3, 3 * parseInt(`${tooltipsa}`));
         screen5 = new Map([[`${screen5.size}`, screen5.size / (Math.max(3, 6))]]);
         tooltipsa *= 3;
         break;
      }
       let tumbnail4 = 0.0;
       let latnl = 4.0;
         latnl *= parseFloat(`${x_lockR.length % 3}`);
      if (2.56 == (q_lock1 * dragk) && 1.50 == (dragk * 2.56)) {
         q_lock1 *= parseFloat(`${1 % (Math.max(parseInt(`${tumbnail4}`), 1))}`);
      }
      for (let w = 0; w < 1; w++) {
         dragk += parseInt(`${dragk}`) >> (Math.min(1, Math.abs(2)));
      }
          let entryC: Map<any, any> = new Map([[String.fromCharCode(107,110,111,119,110,95,51,95,52,49,0),769], [String.fromCharCode(116,95,52,52,95,99,108,97,115,115,105,102,115,0),921]]);
          let score7 = true;
          let bottom2 = 4.0;
         basketballawayteam7 /= Math.max(basketballawayteam7, 3);
         entryC.set(`${score7}`, ((score7 ? 1 : 2) >> (Math.min(Math.abs(parseInt(`${bottom2}`)), 5))));
         bottom2 -= parseFloat(`${1 ^ parseInt(`${bottom2}`)}`);
      let libavfilter0 = q_lock1 >= 6411867.0;
      do {
         q_lock1 /= Math.max(parseFloat(`${parseInt(`${dragk}`)}`), 2);
         if (libavfilter0) {
            break;
         }
      } while ((1.84 == (2.82 * q_lock1)) && libavfilter0);
       let analyticS = false;
      awayteamfield0 = "1";
   }
    if (!initialList.current || initialList.current.length == 0) {

   while (!buildw.includes(`${defaultteam9}`)) {
       let episodec: Array<any> = [197, 403, 96];
         episodec = [episodec.length ^ 3];
         episodec = [1];
       let humiditya = String.fromCharCode(117,95,57,54,95,99,97,114,111,117,115,101,108,0);
       let entryD = String.fromCharCode(101,95,53,48,95,115,101,116,115,0);
      defaultteam9 = libfabricjni1;
      break;
   }
       let yellowtoredt = false;
       let libhermesv: Map<any, any> = new Map([[String.fromCharCode(118,95,51,57,95,105,110,118,0),String.fromCharCode(107,101,121,100,105,114,95,117,95,48,0)], [String.fromCharCode(97,101,115,95,49,95,54,56,0),String.fromCharCode(117,95,49,55,95,99,111,108,117,109,115,0)], [String.fromCharCode(114,95,49,54,95,112,97,115,116,101,98,111,97,114,100,0),String.fromCharCode(122,95,55,49,95,108,105,110,107,0)]]);
         yellowtoredt = (59 > ((!yellowtoredt ? libhermesv.size : 13) * libhermesv.size));
      for (let g = 0; g < 2; g++) {
          let descN = 0.0;
          let basketballtrophyI = 5;
          let checkbox7 = 5.0;
          let grey3 = 5;
          let crownB = String.fromCharCode(102,95,50,53,95,109,105,110,105,0);
         yellowtoredt = grey3 >= parseInt(`${descN}`);
         descN *= 1 & basketballtrophyI;
         basketballtrophyI >>= Math.min(Math.abs(1), 5);
         checkbox7 *= parseFloat(`${2 / (Math.max(10, parseInt(`${checkbox7}`)))}`);
         grey3 |= crownB.length - 3;
         crownB = `${crownB.length}`;
      }
      for (let v = 0; v < 1; v++) {
         libhermesv.set(`${yellowtoredt}`, 3);
      }
      for (let p = 0; p < 3; p++) {
         libhermesv = new Map([[`${libhermesv.size}`, libhermesv.size]]);
      }
      while ((libhermesv.size | 1) >= 5 && yellowtoredt) {
         yellowtoredt = libhermesv.size >= 71;
         break;
      }
      while (!yellowtoredt) {
         libhermesv.set(`${yellowtoredt}`, libhermesv.size);
         break;
      }
      gestureso = (unewarchdefaultsl.length | predictionwin0) >= 28;
   let catalogG = libfabricjni1 ? !libfabricjni1 : libfabricjni1;
   do {
       let fastforwardt = 4.0;
       let stepE = String.fromCharCode(118,95,52,56,95,115,116,114,101,116,99,104,97,98,108,101,0);
      if (stepE.length >= parseInt(`${fastforwardt}`)) {
         stepE += `${3 % (Math.max(9, parseInt(`${fastforwardt}`)))}`;
      }
         fastforwardt += parseFloat(`${stepE.length}`);
         fastforwardt += parseFloat(`${2}`);
      while (fastforwardt < parseFloat(`${stepE.length}`)) {
         fastforwardt *= parseFloat(`${stepE.length << (Math.min(Math.abs(2), 1))}`);
         break;
      }
      if (stepE.startsWith(`${fastforwardt}`)) {
         fastforwardt -= parseFloat(`${2}`);
      }
      if (4 <= (stepE.length ^ 4)) {
         stepE += `${2 / (Math.max(6, stepE.length))}`;
      }
      libfabricjni1 = String.fromCharCode(50,0) == stepE;
      if (catalogG) {
         break;
      }
   } while (catalogG && (unewarchdefaultsl.length < 5));
      buildw += `${(String.fromCharCode(73,0) == linen ? linen.length : predictionwin0)}`;
      setError(true)

   for (let j = 0; j < 3; j++) {
      gestureso = awayteamfield0.startsWith(`${modeG}`);
   }
   while (unewarchdefaultsl == String.fromCharCode(74,0) || awayteamfield0 == String.fromCharCode(69,0)) {
      awayteamfield0 += `${awayteamfield0.length * 1}`;
      break;
   }
       let topicK = 4.0;
       let typesn = 2.0;
      let unimplementedviewz = topicK <= 8236329.0;
      do {
         topicK /= Math.max(parseFloat(`${parseInt(`${typesn}`)}`), 3);
         if (unimplementedviewz) {
            break;
         }
      } while ((2.59 >= (5.96 * topicK)) && unimplementedviewz);
         topicK -= parseFloat(`${3}`);
          let libapminsightae = 0;
         typesn *= parseFloat(`${libapminsightae}`);
         topicK -= parseFloat(`${parseInt(`${typesn}`)}`);
      for (let z = 0; z < 1; z++) {
         topicK *= parseFloat(`${parseInt(`${topicK}`) * 3}`);
      }
      while ((typesn * topicK) == 2.13 && 1.64 == (typesn * 2.13)) {
          let yellowl: Map<any, any> = new Map([[String.fromCharCode(121,95,54,57,95,104,97,110,100,108,101,0),640], [String.fromCharCode(107,95,50,53,95,120,115,117,98,0),940], [String.fromCharCode(112,111,108,108,101,114,95,56,95,56,55,0),908]]);
          let detailsK: Map<any, any> = new Map([[String.fromCharCode(102,95,53,54,95,112,97,114,116,110,101,114,0),true ], [String.fromCharCode(100,114,97,119,97,98,108,101,95,117,95,52,57,0),false ]]);
         topicK += parseFloat(`${yellowl.size % (Math.max(9, parseInt(`${typesn}`)))}`);
         yellowl = new Map([[`${detailsK.size}`, 3]]);
         detailsK.set(`${detailsK.size}`, 1 >> (Math.min(5, Math.abs(detailsK.size))));
         break;
      }
      defaultteam9 = basketballtrophy2.length == 13 && !defaultteam9;
   for (let a = 0; a < 2; a++) {
       let fastz = String.fromCharCode(115,111,114,101,115,101,114,118,101,0);
       let predictionbannershared4 = 1;
       let dropdowno = false;
       let header3 = 5.0;
       let animationo: Map<any, any> = new Map([[String.fromCharCode(117,112,100,97,116,101,95,99,95,49,55,0),true ], [String.fromCharCode(114,101,99,111,114,100,110,105,110,103,95,99,95,50,56,0),true ], [String.fromCharCode(111,95,49,57,95,100,97,109,112,105,110,103,0),false ]]);
      while (fastz.length > parseInt(`${header3}`)) {
         fastz = `${predictionbannershared4}`;
         break;
      }
          let routero = String.fromCharCode(97,99,99,117,109,117,108,97,116,101,100,95,107,95,55,49,0);
         header3 -= 2;
         routero += `${routero.length}`;
      let pressure7 = 6234719 >= animationo.size;
      do {
          let rulesz = true;
          let friendsa: Array<any> = [String.fromCharCode(121,95,56,51,95,112,114,111,118,105,115,105,111,110,0), String.fromCharCode(108,105,109,105,116,101,114,95,122,95,49,48,0)];
         animationo = new Map([[`${header3}`, 1]]);
         rulesz = friendsa.length == 89;
         friendsa = [2];
         if (pressure7) {
            break;
         }
      } while (pressure7 && (animationo.size > 3));
         dropdowno = (fastz.length >> (Math.min(5, Math.abs(predictionbannershared4)))) < 91;
      for (let s = 0; s < 1; s++) {
         header3 *= (animationo.size ^ (dropdowno ? 3 : 2));
      }
         header3 += (3 * (dropdowno ? 3 : 1));
         predictionbannershared4 |= animationo.size << (Math.min(Math.abs(2), 4));
         header3 *= predictionbannershared4;
          let smalls = false;
         fastz += `${1 % (Math.max(5, parseInt(`${header3}`)))}`;
         smalls = !smalls;
         predictionbannershared4 %= Math.max(parseInt(`${header3}`) & animationo.size, 5);
         header3 += ((dropdowno ? 1 : 3) * predictionbannershared4);
         animationo = new Map([[fastz, 1]]);
          let forwardO = String.fromCharCode(101,95,56,56,95,105,110,102,111,115,0);
          let plusq: Array<any> = [206, 739];
         fastz += `${predictionbannershared4}`;
         forwardO = `${plusq.length}`;
         plusq.push(plusq.length);
         header3 /= Math.max(5, animationo.size);
         animationo.set(`${dropdowno}`, predictionbannershared4 << (Math.min(2, Math.abs(3))));
      unewarchdefaultsl += `${linen.length % (Math.max(1, 8))}`;
   }
      return imageProp.source?.uri
    }
    setCurrent(x => {

   for (let e = 0; e < 2; e++) {
      linen += `${((libfabricjni1 ? 4 : 3) * 2)}`;
   }
   let layouta = 9493297 >= linen.length;
   do {
      linen = `${(exampleimageh == String.fromCharCode(104,0) ? dependencyE.length : exampleimageh.length)}`;
      if (layouta) {
         break;
      }
   } while (layouta && (!dependencyE.endsWith(`${linen.length}`)));
       let pingU = String.fromCharCode(102,111,117,114,99,99,95,105,95,56,48,0);
       let graphm = String.fromCharCode(102,111,114,101,118,101,114,95,109,95,57,57,0);
       let libfileA = String.fromCharCode(103,95,52,95,100,112,114,105,110,116,0);
         libfileA = `${pingU.length << (Math.min(5, graphm.length))}`;
      while (graphm.startsWith(`${libfileA.length}`)) {
         graphm += `${libfileA.length}`;
         break;
      }
         graphm += `${pingU.length * 1}`;
         pingU = `${2 ^ libfileA.length}`;
          let untickM = 0;
         libfileA += `${libfileA.length}`;
         untickM %= Math.max(untickM, 2);
      if (libfileA.length < 3) {
         graphm = `${libfileA.length}`;
      }
      for (let l = 0; l < 2; l++) {
         libfileA += `${graphm.length % (Math.max(pingU.length, 6))}`;
      }
          let appsc: Map<any, any> = new Map([[String.fromCharCode(110,101,103,111,116,105,97,116,101,100,95,49,95,55,51,0),String.fromCharCode(99,111,109,112,101,110,115,97,116,101,95,102,95,55,57,0)], [String.fromCharCode(120,95,52,49,95,97,115,99,105,105,0),String.fromCharCode(106,95,53,51,95,118,99,100,115,112,0)], [String.fromCharCode(112,114,101,102,101,116,99,104,95,119,95,49,48,48,0),String.fromCharCode(114,101,115,105,103,110,101,100,95,112,95,49,56,0)]]);
         graphm = `${appsc.size - graphm.length}`;
      for (let c = 0; c < 3; c++) {
         pingU += "1";
      }
      dependencyE += `${1 + pingU.length}`;
   for (let z = 0; z < 3; z++) {
      predictionwin0 |= ((defaultteam9 ? 4 : 1));
   }
      if (alternativeImg && x < alternativeImg?.length){

   let tempnodatagifG = String.fromCharCode(120,52,52,97,106,0) == unewarchdefaultsl;
   do {
      unewarchdefaultsl += `${((libfabricjni1 ? 2 : 1) | iconclosewhitebgZ.length)}`;
      if (tempnodatagifG) {
         break;
      }
   } while ((!unewarchdefaultsl.endsWith(`${basketballtrophy2.length}`)) && tempnodatagifG);
       let downloada = String.fromCharCode(109,112,116,111,97,110,110,101,120,98,95,110,95,49,48,0);
         downloada += `${2 >> (Math.min(1, downloada.length))}`;
          let windt = 5.0;
          let popupU = String.fromCharCode(104,97,110,100,115,95,54,95,55,53,0);
         downloada = `${(popupU == String.fromCharCode(57,0) ? popupU.length : parseInt(`${windt}`))}`;
         downloada += "2";
      modeG = footballfieldX < 14;
   while (2 > (4 / (Math.max(7, predictionwin0))) && 4 > predictionwin0) {
      libfabricjni1 = basketballtrophy2.startsWith(`${modeG}`);
      break;
   }
      footballfieldX >>= Math.min(2, Math.abs(3 / (Math.max(6, footballfielda.length))));
        return x + 1
      }
      return x
    })

      dependencyE = `${(String.fromCharCode(100,0) == exampleimageh ? predictionwin0 : exampleimageh.length)}`;
   while (unewarchdefaultsl.length <= 5) {
       let componentregistry7 = String.fromCharCode(116,95,50,52,95,112,108,116,101,0);
      for (let g = 0; g < 1; g++) {
          let alerta = 3;
          let activey = String.fromCharCode(98,105,103,105,110,116,95,54,95,50,52,0);
         componentregistry7 += `${alerta >> (Math.min(componentregistry7.length, 5))}`;
         alerta -= 1;
         activey += `${(activey == String.fromCharCode(51,0) ? activey.length : activey.length)}`;
      }
       let megaphonef = String.fromCharCode(117,95,50,49,95,102,114,97,109,101,102,111,114,109,97,116,0);
         megaphonef += `${(componentregistry7 == String.fromCharCode(71,0) ? componentregistry7.length : megaphonef.length)}`;
      dependencyE += `${awayteamfield0.length + 1}`;
      break;
   }
      modeG = !buildw.startsWith(`${defaultteam9}`);
   let resultP = footballfielda == String.fromCharCode(108,114,107,54,108,107,118,102,107,95,0);
   do {
       let gifgoalbg8: Array<any> = [929, 631];
       let regengo = false;
       let smallbrightnessQ: Array<any> = [String.fromCharCode(101,120,101,99,95,108,95,49,50,0), String.fromCharCode(100,95,50,95,110,111,110,113,117,111,116,101,100,0)];
          let tailF: Map<any, any> = new Map([[String.fromCharCode(112,108,117,103,105,110,115,95,101,95,53,56,0),true ], [String.fromCharCode(117,110,101,109,98,101,100,95,101,95,54,54,0),true ]]);
          let sigmobg = 4;
          let libglogG = 3.0;
         regengo = sigmobg <= parseInt(`${libglogG}`);
         tailF.set(`${tailF.size}`, tailF.size % (Math.max(3, 9)));
         sigmobg -= tailF.size - 2;
      if (gifgoalbg8.length <= 4) {
         gifgoalbg8.push(3);
      }
      if (!regengo) {
         regengo = smallbrightnessQ.includes(gifgoalbg8[gifgoalbg8.length - 1]);
      }
         gifgoalbg8 = [((regengo ? 5 : 2) % (Math.max(1, 8)))];
      for (let z = 0; z < 3; z++) {
         regengo = gifgoalbg8.length < 53;
      }
      for (let l = 0; l < 3; l++) {
         regengo = smallbrightnessQ.includes(regengo);
      }
      let redirectW = regengo ? !regengo : regengo;
      do {
         regengo = !regengo;
         if (redirectW) {
            break;
         }
      } while ((!regengo) && redirectW);
       let traminiP = false;
       let flyeru = false;
      while (!flyeru && !regengo) {
         regengo = !flyeru || gifgoalbg8.length < 89;
         break;
      }
      footballfielda = `${(String.fromCharCode(89,0) == awayteamfield0 ? (defaultteam9 ? 4 : 1) : awayteamfield0.length)}`;
      if (resultP) {
         break;
      }
   } while ((1 >= (footballfielda.length + footballfieldX) || (1 + footballfieldX) >= 2) && resultP);
    console.debug('using', initialList.current.at(0))

   if (exampleimageh.length == 2 && !defaultteam9) {
      exampleimageh = `${footballfielda.length}`;
   }
       let bodanz = String.fromCharCode(116,105,109,101,117,116,105,108,115,95,98,95,57,52,0);
       let volumeg = true;
       let renderh: Array<any> = [7, 613, 708];
      if (!volumeg) {
         volumeg = (((volumeg ? bodanz.length : 29) | bodanz.length) <= 29);
      }
      let sendE = 8542034 <= bodanz.length;
      do {
         bodanz += `${(String.fromCharCode(84,0) == bodanz ? (volumeg ? 1 : 4) : bodanz.length)}`;
         if (sendE) {
            break;
         }
      } while (sendE && (volumeg));
      for (let w = 0; w < 3; w++) {
          let mergerE = String.fromCharCode(100,101,108,97,121,97,98,108,101,95,120,95,52,48,0);
          let exampleimageI = 1.0;
         volumeg = (68 >= (renderh.length - (!volumeg ? 68 : renderh.length)));
         mergerE = `${mergerE.length * 2}`;
         exampleimageI /= Math.max(3, 5);
      }
         bodanz = `${renderh.length}`;
      let libjsiH = bodanz == String.fromCharCode(103,55,99,109,111,0);
      do {
         bodanz += `${1 ^ bodanz.length}`;
         if (libjsiH) {
            break;
         }
      } while (libjsiH && (4 >= (renderh.length + 5) && (5 + bodanz.length) >= 5));
      let foregroundu = renderh.length <= 6800884;
      do {
          let unselected7 = 5.0;
         renderh = [parseInt(`${unselected7}`) + 2];
         if (foregroundu) {
            break;
         }
      } while ((5 >= bodanz.length) && foregroundu);
      if (volumeg) {
         volumeg = bodanz.length == renderh.length;
      }
          let tumbnailu = 2;
          let librrc4 = 3.0;
          let whitetick4 = 3.0;
         volumeg = bodanz.length <= 60;
         tumbnailu <<= Math.min(Math.abs(3), 1);
         librrc4 += tumbnailu ^ 3;
         whitetick4 /= Math.max(4, parseInt(`${librrc4}`));
         renderh.push(renderh.length);
      footballfielda += `${(dependencyE == String.fromCharCode(87,0) ? dependencyE.length : (modeG ? 1 : 2))}`;
   if (!gestureso) {
      exampleimageh += `${((libfabricjni1 ? 2 : 1) | footballfieldX)}`;
   }
      exampleimageh += `${predictionwin0 * 2}`;
    return initialList.current.at(0)
  }

  const handleOnError = () => {
       let filledp = String.fromCharCode(119,114,105,116,101,95,50,95,54,49,0);
    let sharewhitep = String.fromCharCode(111,112,101,110,101,100,95,122,95,57,51,0);
    let username0: Array<any> = [String.fromCharCode(112,95,57,51,95,99,104,111,111,115,101,114,0), String.fromCharCode(115,101,116,105,95,118,95,54,56,0), String.fromCharCode(100,97,109,112,105,110,103,95,97,95,48,0)];
    let icontransferclubb = String.fromCharCode(112,101,101,114,110,97,109,101,95,117,95,49,51,0);
    let iconbackwhiteZ = String.fromCharCode(116,114,97,99,105,110,103,95,98,95,50,49,0);
    let episodeX = String.fromCharCode(111,100,105,110,103,95,115,95,57,52,0);
    let playercommont = String.fromCharCode(116,95,50,53,95,119,101,98,99,97,109,0);
    let adultU: Map<any, any> = new Map([[String.fromCharCode(109,105,108,108,101,114,95,120,95,57,55,0),208], [String.fromCharCode(98,114,97,99,107,101,116,115,95,57,95,53,53,0),801], [String.fromCharCode(112,108,97,99,101,115,95,49,95,53,49,0),749]]);
    let j_imagep = String.fromCharCode(101,120,112,114,101,115,115,105,111,110,115,95,50,95,49,48,48,0);
    let temperatureV = String.fromCharCode(116,111,107,101,95,49,95,50,55,0);
    let spinner8: Map<any, any> = new Map([[String.fromCharCode(110,95,51,56,95,109,118,104,100,0),111], [String.fromCharCode(122,95,52,55,95,109,112,101,103,118,105,100,101,111,101,110,99,100,115,112,0),213]]);
    let showlessu: Map<any, any> = new Map([[String.fromCharCode(116,97,98,115,95,55,95,56,51,0),713], [String.fromCharCode(97,97,99,101,110,99,100,115,112,0),652], [String.fromCharCode(109,97,112,112,105,110,103,115,95,52,95,50,48,0),26]]);
    let sheets = String.fromCharCode(116,95,52,54,95,97,116,116,105,98,117,116,101,115,0);
    let targetF = 0.0;
    let firebasep: Map<any, any> = new Map([[String.fromCharCode(98,95,56,48,95,99,105,114,99,0),String.fromCharCode(106,95,55,54,95,109,98,105,110,116,114,97,0)], [String.fromCharCode(107,95,51,56,95,115,116,114,101,110,103,116,104,115,0),String.fromCharCode(108,97,99,105,110,103,95,105,95,49,49,0)]]);
   for (let u = 0; u < 2; u++) {
      sharewhitep += `${adultU.size >> (Math.min(Math.abs(2), 3))}`;
   }
   for (let g = 0; g < 2; g++) {
       let faviconW: Array<any> = [String.fromCharCode(116,95,54,57,95,97,110,105,109,97,116,101,0), String.fromCharCode(98,105,116,114,118,95,122,95,49,56,0), String.fromCharCode(118,97,114,95,108,95,51,54,0)];
       let mathe = String.fromCharCode(118,95,50,51,95,116,114,117,115,116,0);
      while (!mathe.includes(`${faviconW.length}`)) {
         faviconW.push(mathe.length ^ faviconW.length);
         break;
      }
         mathe += "1";
      if (mathe.length <= 4) {
         faviconW.push(1);
      }
          let mbnativeo = false;
         mathe += `${faviconW.length}`;
         mbnativeo = (mbnativeo ? mbnativeo : mbnativeo);
       let videojsx = 4.0;
      while (5 > (mathe.length + 5) && (parseInt(`${videojsx}`) * mathe.length) > 5) {
          let private_u3: Array<any> = [11, 992, 784];
          let dragB = true;
          let gifgoalbgX = 3.0;
          let middle2 = String.fromCharCode(97,114,109,118,116,101,95,112,95,53,48,0);
          let roundA = false;
         mathe = `${(3 % (Math.max(9, (dragB ? 3 : 4))))}`;
         private_u3.push(middle2.length << (Math.min(Math.abs(3), 4)));
         dragB = (parseInt(`${gifgoalbgX}`) / (Math.max(1, private_u3.length))) > 84;
         gifgoalbgX /= Math.max(5, middle2.length);
         roundA = gifgoalbgX >= 98.98;
         break;
      }
      playercommont += `${faviconW.length / 2}`;
   }
      episodeX = "2";
      episodeX += `${j_imagep.length}`;
      adultU.set(temperatureV, 3 + temperatureV.length);
      playercommont += `${j_imagep.length / (Math.max(2, 7))}`;
   for (let e = 0; e < 2; e++) {
      episodeX = `${(filledp == String.fromCharCode(55,0) ? filledp.length : sharewhitep.length)}`;
   }
   for (let s = 0; s < 2; s++) {
      filledp += `${spinner8.size / (Math.max(username0.length, 3))}`;
   }
   while (icontransferclubb.length > sharewhitep.length) {
      sharewhitep += `${(episodeX == String.fromCharCode(65,0) ? j_imagep.length : episodeX.length)}`;
      break;
   }
      filledp = `${temperatureV.length}`;
   for (let v = 0; v < 1; v++) {
      adultU = new Map([[j_imagep, j_imagep.length - iconbackwhiteZ.length]]);
   }
   while ((2 % (Math.max(7, adultU.size))) <= 3 && (2 % (Math.max(8, j_imagep.length))) <= 1) {
      adultU = new Map([[episodeX, filledp.length / 3]]);
      break;
   }
   while ((showlessu.size >> (Math.min(Math.abs(2), 2))) == 4 || 2 == (showlessu.size >> (Math.min(j_imagep.length, 1)))) {
      showlessu = new Map([[icontransferclubb, filledp.length - 2]]);
      break;
   }
   let faviconY = episodeX == String.fromCharCode(50,105,104,110,100,117,100,114,102,121,0);
   do {
      episodeX += `${username0.length}`;
      if (faviconY) {
         break;
      }
   } while ((sharewhitep != episodeX) && faviconY);
       let photoX = false;
       let predictionactivei = true;
       let hnewarchdefaultsD = 5.0;
      for (let p = 0; p < 3; p++) {
         predictionactivei = photoX;
      }
      let inactive8 = photoX ? !photoX : photoX;
      do {
         photoX = photoX && hnewarchdefaultsD == 97.36;
         if (inactive8) {
            break;
         }
      } while ((predictionactivei) && inactive8);
         photoX = predictionactivei;
      while (!predictionactivei) {
         predictionactivei = !photoX;
         break;
      }
      while (predictionactivei) {
         hnewarchdefaultsD -= parseInt(`${hnewarchdefaultsD}`) / 2;
         break;
      }
          let iconfeedbacko = String.fromCharCode(104,111,108,100,115,95,111,95,57,0);
         predictionactivei = (!predictionactivei ? photoX : !predictionactivei);
         iconfeedbacko = `${iconfeedbacko.length}`;
         predictionactivei = hnewarchdefaultsD > 83.76;
       let footballtrophyX = 2.0;
       let darke = 4.0;
       let homec = 0.0;
      username0 = [episodeX.length];
      spinner8 = new Map([[icontransferclubb, icontransferclubb.length]]);
      temperatureV += `${temperatureV.length}`;
   for (let w = 0; w < 2; w++) {
       let rendery: Array<any> = [941, 680];
         rendery.push(rendery.length ^ rendery.length);
      let styles_ = rendery.length >= 6542011;
      do {
         rendery = [rendery.length / (Math.max(rendery.length, 2))];
         if (styles_) {
            break;
         }
      } while (styles_ && ((rendery.length + rendery.length) >= 5 && (rendery.length + 5) >= 1));
      let predictionwiny = 8994458 <= rendery.length;
      do {
         rendery.push(rendery.length);
         if (predictionwiny) {
            break;
         }
      } while ((rendery.includes(rendery.length)) && predictionwiny);
      sharewhitep += `${2 & episodeX.length}`;
   }
      sharewhitep += `${icontransferclubb.length}`;
      adultU.set(sharewhitep, (sharewhitep == String.fromCharCode(116,0) ? sharewhitep.length : iconbackwhiteZ.length));
   let scorepopsound9 = playercommont == String.fromCharCode(56,49,53,97,108,120,122,122,100,0);
   do {
      playercommont += `${playercommont.length ^ iconbackwhiteZ.length}`;
      if (scorepopsound9) {
         break;
      }
   } while (scorepopsound9 && (2 > sharewhitep.length));
   for (let m = 0; m < 2; m++) {
      username0.push(showlessu.size);
   }
   if ((adultU.size | j_imagep.length) > 2 || (2 | adultU.size) > 3) {
       let gifgoalbgp = String.fromCharCode(98,111,111,107,109,97,114,107,115,95,53,95,54,50,0);
       let tumbnailx: Map<any, any> = new Map([[String.fromCharCode(109,97,105,110,95,110,95,50,0),683], [String.fromCharCode(111,99,116,112,111,105,110,116,95,102,95,50,57,0),349], [String.fromCharCode(113,95,50,56,95,114,116,109,112,99,114,121,112,116,0),287]]);
         tumbnailx = new Map([[`${tumbnailx.size}`, tumbnailx.size + 2]]);
         tumbnailx.set(`${gifgoalbgp}`, gifgoalbgp.length);
         tumbnailx = new Map([[`${tumbnailx.size}`, gifgoalbgp.length]]);
       let schedulee = 1;
       let dropdownl = 4;
         schedulee *= 2 * dropdownl;
       let iconclosewhitewithbg7: Map<any, any> = new Map([[String.fromCharCode(122,95,55,50,95,114,101,99,111,110,115,116,114,117,99,116,0),String.fromCharCode(97,110,103,114,121,95,118,95,55,57,0)], [String.fromCharCode(116,121,111,101,95,53,95,53,57,0),String.fromCharCode(99,111,112,121,116,111,95,102,95,55,53,0)], [String.fromCharCode(112,114,101,104,97,115,104,95,119,95,52,49,0),String.fromCharCode(116,105,100,121,95,97,95,57,55,0)]]);
      j_imagep += "3";
   }
      username0 = [iconbackwhiteZ.length % 2];
   for (let y = 0; y < 2; y++) {
      playercommont += `${spinner8.size & 3}`;
   }
       let hongkong_: Map<any, any> = new Map([[String.fromCharCode(97,95,53,54,95,115,105,110,101,115,0),292], [String.fromCharCode(117,95,50,95,102,108,105,103,104,116,0),11], [String.fromCharCode(101,95,49,53,95,98,111,100,101,114,0),464]]);
       let libapminsightax = false;
      for (let w = 0; w < 1; w++) {
          let turnB = 1.0;
          let searchbarX: Map<any, any> = new Map([[String.fromCharCode(114,111,108,108,95,121,95,55,51,0),String.fromCharCode(109,115,109,112,101,103,118,95,55,95,48,0)], [String.fromCharCode(105,119,104,116,95,57,95,56,53,0),String.fromCharCode(103,114,101,103,111,114,105,97,110,95,111,95,51,49,0)], [String.fromCharCode(108,111,99,97,108,105,122,97,98,108,101,95,51,95,49,57,0),String.fromCharCode(100,95,52,56,95,105,109,112,0)]]);
          let borderlessw = 0;
          let libfabricjnib: Array<any> = [702, 772, 767];
         hongkong_ = new Map([[`${hongkong_.size}`, 3]]);
         turnB *= 1;
         searchbarX = new Map([[`${libfabricjnib.length}`, 1]]);
         borderlessw *= borderlessw ^ 1;
         libfabricjnib.push(parseInt(`${turnB}`) << (Math.min(Math.abs(searchbarX.size), 2)));
      }
          let mbbidy: Array<any> = [String.fromCharCode(100,101,115,116,114,111,121,101,100,95,102,95,54,51,0), String.fromCharCode(101,95,55,56,95,101,120,105,115,116,0)];
          let alertD = 2.0;
          let leaguedetailsbg7 = String.fromCharCode(115,95,55,54,95,100,101,116,101,99,116,111,114,0);
         hongkong_.set(leaguedetailsbg7, ((libapminsightax ? 4 : 2) * 1));
         mbbidy.push(parseInt(`${alertD}`) << (Math.min(mbbidy.length, 4)));
         alertD *= parseInt(`${alertD}`);
         leaguedetailsbg7 += `${3 - parseInt(`${alertD}`)}`;
         libapminsightax = (32 > (hongkong_.size ^ (!libapminsightax ? 32 : hongkong_.size)));
       let defaultlogoA: Array<any> = [String.fromCharCode(116,95,55,54,95,99,111,109,112,105,108,101,114,0), String.fromCharCode(97,99,101,108,112,95,102,95,49,55,0)];
         defaultlogoA.push((1 ^ (libapminsightax ? 2 : 3)));
         defaultlogoA.push(defaultlogoA.length);
      sharewhitep = `${((libapminsightax ? 4 : 4))}`;
      episodeX = `${adultU.size}`;
   while (j_imagep.length <= 3) {
      j_imagep = `${username0.length}`;
      break;
   }

    setImgUrl(useNextImage())
  }

  if (error) { // when all url error, use the default placeholder image
    return (
      <View style={[{ backgroundColor: colors.loading, ...styles.loadingCard}, imageProp.style]}>
          <LoadingImage  width={20}  height={20}/>
      </View>
    )
  }

  if (useFastImage === true || Platform.OS === "android"){
    if (typeof imageProp.source == 'number'){ // if source={require(...)}
    return <FastImage {...imageProp as FastImageProps}/>
    } else {
      return <FastImage key={current.toString() + imgUrl} {...imageProp as FastImageProps} source={{uri: imgUrl}} onError={handleOnError}/>
    }
  } else {
    if (typeof imageProp.source == 'number'){ // if source={require(...)}
      return <Image {...imageProp as mayi_ResendVertical}/>
    } else { // if source={{uri:...}}
      return <Image key={current.toString() + imgUrl} {...imageProp as mayi_ResendVertical} source={{...(imageProp.source as ImageURISource), uri: imgUrl}} onError={handleOnError}/>
    }
  }
}

const styles = StyleSheet.create({
  loadingCard: {
    marginRight: 20,
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    borderRadius: 10,
},
})

export default customFastImage;