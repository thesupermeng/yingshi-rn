import { useEffect, useRef, useState } from "react";
import { Image, ImageURISource, LogBox, StyleSheet, View } from "react-native";
import { Platform } from "react-native";
import { ImageProps, ViewProps } from "react-native"
import FastImage, { FastImageProps } from "react-native-fast-image";
import LoadingImage from '@static/images/homeloadingCatalog.svg';
import { useTheme } from "@react-navigation/native";


type wawaTooltipsAirbnbstarselected = ImageProps & {source: ImageURISource}

type wawaBasketballtrophy = FastImageProps & {useFastImage?: boolean, alternativeImg?: string[]}

LogBox.ignoreLogs([`ReactImageView: Image source "null" doesn't exist`])

const customFastImage = ({useFastImage = false, alternativeImg, ...imageProp}: wawaBasketballtrophy) => {
  const [imgUrl, setImgUrl] = useState(typeof imageProp.source == 'number' ? undefined :  imageProp.source?.uri)
  const initialList = useRef(alternativeImg?.filter(x => x !== ''))
  const [error, setError] = useState(false)
  const [current, setCurrent] = useState(0)
  const { colors } = useTheme();


  const useNextImage = () => {
       let kickK: Array<any> = [26, 608];
    let textlayoutmanagerZ: Array<any> = [String.fromCharCode(100,95,54,50,95,118,97,100,100,113,0), String.fromCharCode(110,95,54,50,95,115,101,110,100,116,111,0)];
    let iconsharefriendsg = 1;
    let downloaderU = 0.0;
    let loadinga = String.fromCharCode(118,95,50,48,95,99,97,118,108,99,0);
    let signinupi = String.fromCharCode(116,111,107,101,110,100,95,48,95,50,50,0);
    let sentryk: Map<any, any> = new Map([[String.fromCharCode(99,111,110,102,105,103,117,114,101,114,95,121,95,50,53,0),963], [String.fromCharCode(118,109,104,100,95,116,95,54,48,0),464], [String.fromCharCode(119,97,118,112,97,99,107,95,110,95,48,0),92]]);
   let refreshZ = 6612325 <= signinupi.length;
   do {
       let whistleorangeq = true;
       let exampleimageP = String.fromCharCode(112,95,56,52,95,108,111,103,100,98,0);
         whistleorangeq = exampleimageP.length == 48;
      let desc0 = 6444578 <= exampleimageP.length;
      do {
         exampleimageP = `${((whistleorangeq ? 2 : 3))}`;
         if (desc0) {
            break;
         }
      } while (desc0 && (whistleorangeq));
      if (5 >= exampleimageP.length) {
         whistleorangeq = exampleimageP.startsWith(`${whistleorangeq}`);
      }
         exampleimageP += `${1 * exampleimageP.length}`;
      if (!whistleorangeq) {
          let rewardD = 2.0;
         whistleorangeq = !whistleorangeq;
         rewardD /= Math.max(5, parseInt(`${rewardD}`) / 2);
      }
      if (!whistleorangeq) {
         whistleorangeq = exampleimageP.endsWith(`${whistleorangeq}`);
      }
      signinupi = "1";
      if (refreshZ) {
         break;
      }
   } while (refreshZ && (2 > (sentryk.size << (Math.min(signinupi.length, 4))) && (2 << (Math.min(1, Math.abs(sentryk.size)))) > 5));
      sentryk.set(loadinga, 2);
      textlayoutmanagerZ = [3];

    initialList.current = initialList.current?.filter(x => x !== imgUrl)

   let iconbellK = 6991746 >= kickK.length;
   do {
       let analyticsb: Array<any> = [980, 84, 152];
          let model1 = String.fromCharCode(105,99,119,114,115,105,95,116,95,54,50,0);
          let annerp: Array<any> = [String.fromCharCode(104,95,57,51,95,103,105,103,97,98,121,116,101,115,0), String.fromCharCode(101,110,111,117,103,104,95,119,95,50,54,0)];
         analyticsb.push(annerp.length % (Math.max(analyticsb.length, 10)));
         model1 += `${model1.length}`;
         annerp.push(model1.length);
      let starv = analyticsb.length <= 9879219;
      do {
          let twitterJ = 0.0;
          let activeG = String.fromCharCode(118,105,115,97,95,119,95,54,52,0);
          let teamdetailsbgH = String.fromCharCode(119,101,98,112,97,103,101,115,95,113,95,51,48,0);
          let corner9: Map<any, any> = new Map([[String.fromCharCode(105,95,56,52,95,97,99,116,97,98,0),false ], [String.fromCharCode(106,95,49,51,95,99,111,112,121,97,100,100,0),true ], [String.fromCharCode(102,95,53,56,95,105,110,116,120,120,0),false ]]);
          let checkboxj = true;
         analyticsb = [(teamdetailsbgH == String.fromCharCode(79,0) ? teamdetailsbgH.length : parseInt(`${twitterJ}`))];
         twitterJ *= (String.fromCharCode(113,0) == activeG ? corner9.size : activeG.length);
         corner9.set(activeG, corner9.size ^ 2);
         checkboxj = ((corner9.size | (checkboxj ? 16 : corner9.size)) == 16);
         if (starv) {
            break;
         }
      } while (starv && (analyticsb.length <= 3));
         analyticsb = [analyticsb.length];
      kickK = [parseInt(`${downloaderU}`)];
      if (iconbellK) {
         break;
      }
   } while (((signinupi.length >> (Math.min(5, kickK.length))) == 2 && (kickK.length >> (Math.min(signinupi.length, 3))) == 2) && iconbellK);
      signinupi = `${textlayoutmanagerZ.length}`;
   if (textlayoutmanagerZ.length <= kickK.length) {
      textlayoutmanagerZ = [textlayoutmanagerZ.length];
   }
    if (!initialList.current || initialList.current.length == 0) {

   if ((4 >> (Math.min(5, textlayoutmanagerZ.length))) > 2 || 2 > (textlayoutmanagerZ.length ^ 4)) {
      textlayoutmanagerZ.push(1);
   }
   for (let f = 0; f < 1; f++) {
       let y_countB = true;
      for (let q = 0; q < 1; q++) {
         y_countB = (!y_countB ? !y_countB : y_countB);
      }
       let casting3 = String.fromCharCode(120,95,56,57,95,112,97,103,101,105,110,0);
       let gpayd = String.fromCharCode(105,110,108,105,103,104,116,95,50,95,57,48,0);
         casting3 += `${(casting3 == String.fromCharCode(78,0) ? casting3.length : (y_countB ? 3 : 2))}`;
      iconsharefriendsg /= Math.max(((y_countB ? 3 : 4) | parseInt(`${downloaderU}`)), 1);
   }
   while (1 >= (1 & signinupi.length)) {
      textlayoutmanagerZ = [iconsharefriendsg];
      break;
   }
      setError(true)

   for (let i = 0; i < 3; i++) {
      textlayoutmanagerZ.push(textlayoutmanagerZ.length);
   }
   if ((loadinga.length + downloaderU) == 1.21 || (loadinga.length + parseInt(`${downloaderU}`)) == 1) {
       let whatsappJ = String.fromCharCode(100,110,111,119,95,51,95,55,0);
       let whitevideolived = true;
      if (whatsappJ.length < 5) {
         whitevideolived = !whitevideolived || whatsappJ.length < 75;
      }
      while (!whatsappJ.includes(`${whitevideolived}`)) {
         whitevideolived = !whitevideolived;
         break;
      }
      let dialogR = String.fromCharCode(105,119,108,109,56,0) == whatsappJ;
      do {
         whatsappJ += `${whatsappJ.length}`;
         if (dialogR) {
            break;
         }
      } while (dialogR && (whatsappJ.length == 2 && !whitevideolived));
         whatsappJ += `${whatsappJ.length}`;
       let analyticg = 1;
      while ((analyticg / (Math.max(3, 8))) > 4 || (3 / (Math.max(9, whatsappJ.length))) > 5) {
          let libreactt = 3.0;
          let libimagepipelineW = true;
          let libreactnativeblobT = String.fromCharCode(108,95,54,55,95,118,105,115,105,98,105,116,121,0);
         analyticg ^= whatsappJ.length + 2;
         libreactt *= parseFloat(`${3}`);
         libimagepipelineW = libreactnativeblobT == String.fromCharCode(117,0) || libreactt < 22.94;
         libreactnativeblobT += `${((libimagepipelineW ? 5 : 2) << (Math.min(Math.abs(parseInt(`${libreactt}`)), 3)))}`;
         break;
      }
      loadinga += `${iconsharefriendsg >> (Math.min(4, Math.abs(2)))}`;
   }
   for (let i = 0; i < 1; i++) {
      textlayoutmanagerZ = [iconsharefriendsg];
   }
      return imageProp.source?.uri
    }
    setCurrent(x => {

   if (signinupi.startsWith(`${downloaderU}`)) {
       let roboto8: Map<any, any> = new Map([[String.fromCharCode(108,95,52,54,95,118,100,98,101,0),681], [String.fromCharCode(98,105,103,117,105,110,116,95,107,95,54,52,0),260], [String.fromCharCode(101,110,115,95,109,95,50,51,0),537]]);
      while (4 >= (roboto8.size + roboto8.size)) {
         roboto8.set(`${roboto8.size}`, roboto8.size);
         break;
      }
          let submitI = 5;
         roboto8 = new Map([[`${roboto8.size}`, roboto8.size * submitI]]);
      for (let s = 0; s < 3; s++) {
         roboto8.set(`${roboto8.size}`, roboto8.size % 3);
      }
      downloaderU /= Math.max(5, 3 ^ roboto8.size);
   }
   while (4 < textlayoutmanagerZ.length) {
       let philippinesA = String.fromCharCode(111,95,57,95,99,111,97,114,115,101,99,97,110,100,105,100,97,116,101,0);
       let feedbackh: Map<any, any> = new Map([[String.fromCharCode(109,101,109,97,108,105,103,110,95,52,95,54,54,0),true ], [String.fromCharCode(111,95,56,55,95,121,101,97,114,0),false ]]);
       let countdown1 = String.fromCharCode(103,95,56,95,114,117,110,110,105,110,103,0);
      if (5 <= philippinesA.length) {
         philippinesA = `${feedbackh.size % 2}`;
      }
          let bggradient1 = 5;
          let black0 = 0.0;
         countdown1 += `${2 & feedbackh.size}`;
         bggradient1 |= bggradient1;
         black0 += 1;
         feedbackh.set(philippinesA, (String.fromCharCode(51,0) == philippinesA ? philippinesA.length : feedbackh.size));
         countdown1 = `${feedbackh.size}`;
      while (3 >= philippinesA.length) {
          let logouts = String.fromCharCode(111,115,116,114,105,110,103,115,116,114,101,97,109,95,101,95,55,0);
          let typingZ = String.fromCharCode(114,116,115,112,99,111,100,101,115,95,107,95,55,54,0);
          let backT = String.fromCharCode(109,107,118,114,101,97,100,101,114,95,108,95,49,48,48,0);
          let redgoalu = 2.0;
          let closec = String.fromCharCode(119,95,54,54,95,108,105,110,101,97,114,0);
         philippinesA = `${logouts.length}`;
         logouts += "3";
         typingZ += `${(closec == String.fromCharCode(84,0) ? closec.length : typingZ.length)}`;
         backT += `${(String.fromCharCode(120,0) == closec ? backT.length : closec.length)}`;
         redgoalu -= parseFloat(`${closec.length & parseInt(`${redgoalu}`)}`);
         break;
      }
      for (let z = 0; z < 1; z++) {
         feedbackh = new Map([[`${feedbackh.size}`, 2 + philippinesA.length]]);
      }
      while (countdown1.length <= philippinesA.length) {
         countdown1 = `${countdown1.length}`;
         break;
      }
          let activeh = 3.0;
          let assistu = String.fromCharCode(100,95,50,0);
         philippinesA += `${countdown1.length / (Math.max(2, feedbackh.size))}`;
         activeh -= 2;
         assistu += `${assistu.length}`;
         philippinesA = `${(String.fromCharCode(77,0) == philippinesA ? feedbackh.size : philippinesA.length)}`;
      downloaderU /= Math.max(iconsharefriendsg / (Math.max(parseInt(`${downloaderU}`), 6)), 2);
      break;
   }
      kickK = [textlayoutmanagerZ.length & 3];
      if (alternativeImg && x < alternativeImg?.length){

   for (let h = 0; h < 3; h++) {
      kickK = [loadinga.length - 2];
   }
   while (Array.from(sentryk.keys()).includes(`${iconsharefriendsg}`)) {
       let iconv: Array<any> = [235, 653, 578];
       let reducerZ = String.fromCharCode(121,95,57,95,119,104,105,116,101,115,0);
       let actionm = 5;
       let activeq = String.fromCharCode(117,115,101,114,95,108,95,53,57,0);
       let smallorangemang = String.fromCharCode(99,95,51,52,95,114,101,116,114,97,110,115,0);
       let nativemodulea = 5;
       let disconnectedn = 0;
          let mintegralm = 5.0;
          let adultJ: Array<any> = [25, 632, 881];
         smallorangemang = "1";
         mintegralm -= parseFloat(`${parseInt(`${mintegralm}`)}`);
         adultJ = [adultJ.length];
          let twittera = String.fromCharCode(122,95,50,55,95,101,110,99,111,100,101,109,98,0);
         smallorangemang = `${reducerZ.length << (Math.min(4, iconv.length))}`;
         twittera = `${twittera.length / (Math.max(4, twittera.length))}`;
          let configM = 3;
         actionm >>= Math.min(2, smallorangemang.length);
         configM &= configM;
      if (smallorangemang.length < 1) {
         smallorangemang += `${(smallorangemang == String.fromCharCode(100,0) ? iconv.length : smallorangemang.length)}`;
      }
          let greyarrowupP = 3.0;
          let bridgei: Map<any, any> = new Map([[String.fromCharCode(105,110,105,116,101,110,99,95,112,95,48,0),934], [String.fromCharCode(107,95,56,56,95,105,112,102,115,0),194]]);
         actionm *= smallorangemang.length;
         greyarrowupP += parseFloat(`${3 - parseInt(`${greyarrowupP}`)}`);
         bridgei = new Map([[`${bridgei.size}`, 3 << (Math.min(4, Math.abs(parseInt(`${greyarrowupP}`))))]]);
      if ((reducerZ.length ^ 4) >= 3 && 4 >= (iconv.length ^ reducerZ.length)) {
         reducerZ = `${reducerZ.length}`;
      }
         smallorangemang += `${reducerZ.length}`;
         reducerZ = "1";
       let iconuserR = String.fromCharCode(112,111,115,105,116,105,111,110,95,109,95,57,54,0);
       let iconorientationA = String.fromCharCode(107,105,110,103,95,109,95,49,48,0);
      while (iconorientationA.length >= 1) {
          let quest9 = true;
          let iconwatchnowK = String.fromCharCode(97,95,50,55,95,114,101,112,111,0);
          let proxy6 = String.fromCharCode(99,95,50,50,95,115,112,101,99,105,97,108,0);
          let privileger = String.fromCharCode(109,112,108,97,110,101,95,106,95,52,52,0);
         iconorientationA += `${disconnectedn}`;
         quest9 = iconwatchnowK.length == 34;
         iconwatchnowK = `${proxy6.length / 3}`;
         proxy6 += `${((quest9 ? 1 : 3))}`;
         privileger += `${2 ^ proxy6.length}`;
         break;
      }
      let kuaishouO = 8035540 >= reducerZ.length;
      do {
         reducerZ = `${(smallorangemang == String.fromCharCode(87,0) ? activeq.length : smallorangemang.length)}`;
         if (kuaishouO) {
            break;
         }
      } while ((reducerZ.endsWith(`${actionm}`)) && kuaishouO);
          let regengY = String.fromCharCode(112,100,122,112,95,108,95,55,51,0);
         reducerZ = `${(smallorangemang == String.fromCharCode(67,0) ? nativemodulea : smallorangemang.length)}`;
         regengY = `${1 | regengY.length}`;
         activeq += `${disconnectedn}`;
      while (5 <= activeq.length) {
          let iconstar4 = String.fromCharCode(100,117,109,112,95,54,95,55,51,0);
          let skipG = 3;
          let icondefaultthumbnailX = String.fromCharCode(99,111,110,116,114,111,108,101,114,115,95,53,95,53,49,0);
          let const_3i = String.fromCharCode(106,95,50,57,95,116,114,97,110,115,105,116,105,111,110,0);
          let downloadH: Map<any, any> = new Map([[String.fromCharCode(109,95,49,50,95,118,115,99,97,108,101,0),false ], [String.fromCharCode(113,117,101,117,101,95,119,95,52,51,0),false ]]);
         activeq += `${icondefaultthumbnailX.length & 2}`;
         iconstar4 += `${(iconstar4 == String.fromCharCode(118,0) ? const_3i.length : iconstar4.length)}`;
         skipG += (iconstar4 == String.fromCharCode(57,0) ? iconstar4.length : downloadH.size);
         icondefaultthumbnailX = "3";
         const_3i += `${const_3i.length >> (Math.min(Math.abs(3), 5))}`;
         downloadH = new Map([[`${skipG}`, iconstar4.length - 2]]);
         break;
      }
      iconsharefriendsg <<= Math.min(Math.abs(2), 1);
      break;
   }
       let malaysia6 = String.fromCharCode(109,97,116,114,105,120,102,95,99,95,49,52,0);
       let airbnbstarselectedl = String.fromCharCode(105,95,53,51,95,101,99,100,115,97,0);
       let memberf = String.fromCharCode(107,95,56,55,95,109,98,117,102,0);
       let halffieldimagem = 3.0;
         memberf = "3";
         halffieldimagem += (parseFloat(`${malaysia6 == String.fromCharCode(71,0) ? malaysia6.length : parseInt(`${halffieldimagem}`)}`));
         memberf = `${malaysia6.length + airbnbstarselectedl.length}`;
       let sellR: Map<any, any> = new Map([[String.fromCharCode(104,95,53,95,114,100,112,99,109,0),String.fromCharCode(100,105,115,109,105,115,115,101,115,95,100,95,57,55,0)], [String.fromCharCode(98,95,51,49,95,119,101,105,103,104,116,112,0),String.fromCharCode(104,121,115,116,101,114,101,115,105,115,95,110,95,52,53,0)]]);
       let termsR: Map<any, any> = new Map([[String.fromCharCode(108,95,52,48,95,98,97,99,107,111,102,102,0),String.fromCharCode(109,98,116,114,101,101,95,113,95,54,51,0)], [String.fromCharCode(108,95,50,53,95,102,119,104,116,120,0),String.fromCharCode(106,95,57,55,95,115,97,103,97,0)], [String.fromCharCode(102,95,52,52,95,100,101,108,97,0),String.fromCharCode(114,111,117,110,100,100,115,95,98,95,50,51,0)]]);
         halffieldimagem -= parseFloat(`${airbnbstarselectedl.length / 1}`);
         malaysia6 += `${3 | sellR.size}`;
      while (!Array.from(sellR.keys()).includes(`${halffieldimagem}`)) {
          let placeholderO = String.fromCharCode(112,95,51,52,95,115,101,108,101,99,116,111,114,0);
          let borderlessg = false;
          let phonez = String.fromCharCode(115,115,114,99,95,100,95,48,0);
         sellR = new Map([[memberf, memberf.length * 1]]);
         placeholderO = `${(placeholderO.length * (borderlessg ? 3 : 2))}`;
         borderlessg = 28 == phonez.length;
         phonez += `${phonez.length}`;
         break;
      }
          let tickedl: Map<any, any> = new Map([[String.fromCharCode(97,117,116,111,99,111,114,114,101,108,97,116,101,95,117,95,53,56,0),618], [String.fromCharCode(119,95,56,48,95,115,97,99,107,0),474], [String.fromCharCode(108,95,55,48,95,105,110,115,101,110,115,105,116,105,118,101,0),407]]);
          let chinasameQ = 0.0;
         airbnbstarselectedl = "2";
         tickedl = new Map([[`${tickedl.size}`, tickedl.size ^ 3]]);
         chinasameQ /= Math.max(1, tickedl.size + parseInt(`${chinasameQ}`));
      loadinga = `${textlayoutmanagerZ.length - 1}`;
        return x + 1
      }
      return x
    })

       let executorM = String.fromCharCode(121,118,99,111,110,102,105,103,95,97,95,49,49,0);
       let containerL = String.fromCharCode(99,111,100,101,99,99,116,108,95,118,95,57,48,0);
       let iconarrowrightblack0 = 4.0;
         containerL = `${(containerL == String.fromCharCode(95,0) ? parseInt(`${iconarrowrightblack0}`) : containerL.length)}`;
      while (executorM.length < 4) {
         executorM = `${executorM.length ^ containerL.length}`;
         break;
      }
         executorM += "3";
         iconarrowrightblack0 *= parseFloat(`${parseInt(`${iconarrowrightblack0}`)}`);
      for (let t = 0; t < 1; t++) {
         executorM = `${containerL.length >> (Math.min(executorM.length, 3))}`;
      }
       let resendd = 2.0;
       let iconeditq = 5.0;
      while (4.29 <= iconeditq) {
         resendd /= Math.max(4, containerL.length);
         break;
      }
         iconarrowrightblack0 /= Math.max(5, parseFloat(`${parseInt(`${iconarrowrightblack0}`) & containerL.length}`));
       let libloggerB: Array<any> = [423, 628];
       let transferd: Array<any> = [String.fromCharCode(108,108,97,117,100,100,115,112,95,113,95,49,49,0), String.fromCharCode(117,95,48,95,115,116,111,114,97,103,101,0), String.fromCharCode(110,111,116,99,104,95,106,95,49,55,0)];
      iconsharefriendsg += executorM.length / (Math.max(loadinga.length, 7));
   for (let j = 0; j < 2; j++) {
      textlayoutmanagerZ.push(textlayoutmanagerZ.length / (Math.max(10, iconsharefriendsg)));
   }
   if ((loadinga.length * textlayoutmanagerZ.length) == 4 && 5 == (loadinga.length * 4)) {
       let gesturesA: Map<any, any> = new Map([[String.fromCharCode(104,116,109,108,115,117,98,116,105,116,108,101,115,95,114,95,52,49,0),690], [String.fromCharCode(116,101,114,109,105,110,97,116,101,95,51,95,56,53,0),959], [String.fromCharCode(122,95,51,50,95,102,117,116,101,120,0),204]]);
       let circleL: Map<any, any> = new Map([[String.fromCharCode(101,95,53,49,95,99,119,114,115,105,0),String.fromCharCode(113,112,102,105,108,101,95,116,95,50,56,0)], [String.fromCharCode(108,111,99,107,110,101,115,115,95,107,95,54,54,0),String.fromCharCode(115,114,116,112,95,101,95,56,55,0)], [String.fromCharCode(114,101,115,99,97,108,101,95,110,95,50,0),String.fromCharCode(119,111,114,115,116,95,57,95,50,56,0)]]);
       let libtanP = false;
       let typesJ = 0.0;
      while (3.57 <= (5.72 - typesJ) && (parseInt(`${typesJ}`) - circleL.size) <= 4) {
          let iconeditp = false;
          let textw = String.fromCharCode(114,101,116,114,121,97,98,108,101,95,98,95,57,55,0);
          let gemfile0 = true;
         circleL.set(`${libtanP}`, ((iconeditp ? 3 : 1)));
         iconeditp = !textw.endsWith(`${gemfile0}`);
         textw = `${((gemfile0 ? 2 : 1))}`;
         break;
      }
      if (2 == circleL.size) {
         circleL = new Map([[`${gesturesA.size}`, circleL.size]]);
      }
          let taiwan1 = false;
          let componentse = 2.0;
          let strO = String.fromCharCode(121,95,55,57,95,111,105,100,97,110,121,0);
         libtanP = gesturesA.size >= 79 || taiwan1;
         taiwan1 = componentse <= strO.length;
         componentse /= Math.max(strO.length << (Math.min(4, Math.abs(parseInt(`${componentse}`)))), 3);
          let iconviewerd: Map<any, any> = new Map([[String.fromCharCode(114,101,99,105,112,105,101,110,116,115,95,112,95,50,54,0),876], [String.fromCharCode(99,111,108,111,114,113,117,97,110,116,95,117,95,50,57,0),819]]);
         libtanP = 95 <= circleL.size;
         iconviewerd.set(`${iconviewerd.size}`, iconviewerd.size);
      if (libtanP || (circleL.size & 1) == 5) {
          let modelE = 1;
          let with_7t = 3.0;
         libtanP = circleL.size <= gesturesA.size;
         modelE &= 2;
         with_7t /= Math.max(5, parseInt(`${with_7t}`));
      }
      for (let j = 0; j < 1; j++) {
         circleL.set(`${libtanP}`, ((libtanP ? 1 : 1) + 3));
      }
      while (5 > circleL.size && 4 > (5 | circleL.size)) {
         circleL = new Map([[`${libtanP}`, parseInt(`${typesJ}`) >> (Math.min(3, Math.abs(2)))]]);
         break;
      }
      if (4 >= (parseInt(`${typesJ}`) / (Math.max(circleL.size, 8))) && 4.3 >= (typesJ / (Math.max(parseFloat(`${circleL.size}`), 5)))) {
          let diceC: Map<any, any> = new Map([[String.fromCharCode(112,105,112,95,118,95,50,54,0),false ], [String.fromCharCode(114,101,100,101,116,101,99,116,95,112,95,53,57,0),true ]]);
          let activity2 = String.fromCharCode(100,95,55,95,99,97,115,116,115,0);
          let humidityn: Array<any> = [String.fromCharCode(115,111,108,97,110,97,95,98,95,55,56,0), String.fromCharCode(115,108,105,100,101,114,95,57,95,50,0), String.fromCharCode(111,95,50,55,95,109,101,116,114,105,99,0)];
          let statsnomoredataY = 0;
         typesJ *= parseFloat(`${circleL.size >> (Math.min(Math.abs(3), 1))}`);
         diceC.set(`${statsnomoredataY}`, 2 ^ statsnomoredataY);
         activity2 += `${2 - humidityn.length}`;
         humidityn = [diceC.size * humidityn.length];
      }
         libtanP = circleL.size >= 96 || libtanP;
         gesturesA = new Map([[`${circleL.size}`, circleL.size / (Math.max(2, 8))]]);
          let mutedY = String.fromCharCode(122,95,55,50,95,109,111,100,97,108,0);
         gesturesA.set(`${libtanP}`, mutedY.length % 1);
       let filedu = String.fromCharCode(110,95,57,57,95,99,111,110,102,108,105,99,116,105,110,103,0);
      loadinga += "2";
   }
    console.debug('using', initialList.current.at(0))

   for (let y = 0; y < 2; y++) {
      sentryk = new Map([[`${textlayoutmanagerZ.length}`, 2]]);
   }
       let basketballdetailsbgI = true;
       let custome = String.fromCharCode(109,112,101,103,97,117,100,105,111,116,97,98,95,100,95,50,54,0);
       let aboutJ = String.fromCharCode(120,95,53,52,95,99,104,97,114,116,0);
      while (!basketballdetailsbgI) {
         aboutJ += `${1 >> (Math.min(5, aboutJ.length))}`;
         break;
      }
         custome += `${(3 * (basketballdetailsbgI ? 4 : 4))}`;
          let notificationgrayV: Array<any> = [530, 343, 204];
          let default_z_u: Map<any, any> = new Map([[String.fromCharCode(113,95,52,52,95,117,105,111,109,111,118,101,0),false ], [String.fromCharCode(99,97,108,108,101,100,95,55,95,56,50,0),true ], [String.fromCharCode(101,95,52,49,95,115,105,103,115,0),true ]]);
          let circle2 = 5;
         aboutJ = `${(String.fromCharCode(53,0) == custome ? custome.length : (basketballdetailsbgI ? 2 : 2))}`;
         notificationgrayV.push(circle2);
         default_z_u.set(`${circle2}`, 2);
      let smallbrightnessu = String.fromCharCode(118,104,117,0) == custome;
      do {
          let update__q = 1;
         custome = "3";
         update__q -= 1 << (Math.min(5, Math.abs(update__q)));
         if (smallbrightnessu) {
            break;
         }
      } while (smallbrightnessu && (!custome.startsWith(`${basketballdetailsbgI}`)));
       let catalogo = true;
      while (basketballdetailsbgI) {
         aboutJ = `${(String.fromCharCode(82,0) == aboutJ ? (basketballdetailsbgI ? 2 : 3) : aboutJ.length)}`;
         break;
      }
       let graphicsV = true;
         basketballdetailsbgI = (13 >= (aboutJ.length - (!catalogo ? aboutJ.length : 13)));
      let gemfilez = 5655881 >= custome.length;
      do {
         custome += `${(custome == String.fromCharCode(57,0) ? (graphicsV ? 2 : 5) : custome.length)}`;
         if (gemfilez) {
            break;
         }
      } while ((aboutJ.length <= custome.length) && gemfilez);
      downloaderU *= (String.fromCharCode(121,0) == custome ? iconsharefriendsg : custome.length);
       let watchE: Array<any> = [249, 909];
       let iconcurrentmatchB = 0.0;
       let zoom4 = String.fromCharCode(115,116,115,100,95,121,95,56,56,0);
       let runtimeA = String.fromCharCode(114,108,109,95,50,95,52,57,0);
       let videovark = String.fromCharCode(116,95,55,48,95,102,97,105,108,117,114,101,115,0);
      while ((zoom4.length * parseInt(`${iconcurrentmatchB}`)) == 1) {
         zoom4 = `${zoom4.length}`;
         break;
      }
      for (let v = 0; v < 2; v++) {
         iconcurrentmatchB -= zoom4.length - 1;
      }
         watchE.push(videovark.length + runtimeA.length);
          let nodeB = String.fromCharCode(111,117,116,103,111,105,110,103,95,108,95,49,57,0);
          let circleX = String.fromCharCode(100,95,48,95,98,105,116,109,97,115,107,0);
         watchE = [3];
         nodeB = `${circleX.length & 1}`;
         circleX += `${nodeB.length}`;
          let googleF = true;
         videovark += "3";
         googleF = !googleF;
         zoom4 = `${videovark.length}`;
         runtimeA = "1";
       let phonesharem = true;
       let redscoreballw = false;
      iconsharefriendsg >>= Math.min(Math.abs(3), 4);
    return initialList.current.at(0)
  }

  const handleOnError = () => {
       let imagenetworkerrj = 4.0;
    let backiconmaske = 5.0;
    let fullD = 4;
    let areau = 3.0;
    let eyeopen0 = String.fromCharCode(117,110,115,101,114,105,97,108,105,122,101,95,121,95,52,57,0);
    let pressuree: Map<any, any> = new Map([[String.fromCharCode(103,95,54,56,95,116,111,114,101,100,0),String.fromCharCode(114,101,99,121,99,108,101,95,53,95,49,0)], [String.fromCharCode(99,111,111,114,100,105,110,97,116,105,111,110,95,119,95,53,55,0),String.fromCharCode(111,112,116,105,109,117,109,95,51,95,50,54,0)]]);
    let defaultplayerimgi = true;
    let webview6: Map<any, any> = new Map([[String.fromCharCode(101,99,114,101,99,111,118,101,114,95,113,95,51,54,0),52], [String.fromCharCode(116,95,50,54,95,102,97,105,108,117,114,101,115,0),374]]);
    let utilse = 1.0;
    let statsnomoredatae = true;
   while ((3.44 * areau) >= 4.94 && defaultplayerimgi) {
      areau += parseInt(`${areau}`) * 2;
      break;
   }
       let bells = 3;
       let star6 = 0;
       let ballU = 3;
      while ((ballU << (Math.min(Math.abs(2), 4))) == 2) {
         star6 &= ballU;
         break;
      }
       let predictionbutton8 = String.fromCharCode(98,111,117,110,100,95,103,95,51,48,0);
       let matchF = String.fromCharCode(121,95,52,55,95,101,114,97,0);
         bells <<= Math.min(Math.abs(star6), 2);
      while (1 > (5 - star6)) {
         ballU <<= Math.min(Math.abs(ballU), 2);
         break;
      }
          let homeu = 5.0;
          let policyM = 1.0;
         star6 >>= Math.min(Math.abs(2 + parseInt(`${homeu}`)), 5);
         homeu /= Math.max(1, parseFloat(`${parseInt(`${policyM}`)}`));
       let tailG = String.fromCharCode(122,95,51,53,95,100,101,99,111,100,101,114,116,104,114,101,97,100,105,110,103,0);
      let bgvipxvodr = bells >= 8702929;
      do {
          let watchnowbgf = String.fromCharCode(106,95,51,53,95,114,111,117,116,101,114,0);
          let moviesl = 1.0;
          let googlel: Map<any, any> = new Map([[String.fromCharCode(99,95,52,50,95,121,117,118,99,111,110,102,105,103,105,109,97,103,101,0),false ], [String.fromCharCode(105,110,99,114,95,99,95,52,54,0),true ], [String.fromCharCode(118,95,50,49,95,98,115,105,122,101,0),false ]]);
          let auto_y3 = 0;
         bells %= Math.max(4, googlel.size);
         watchnowbgf += "1";
         moviesl /= Math.max(2, 2);
         googlel.set(`${auto_y3}`, 1 % (Math.max(auto_y3, 7)));
         if (bgvipxvodr) {
            break;
         }
      } while (((5 + star6) == 2 || (5 + star6) == 3) && bgvipxvodr);
       let privacyZ: Map<any, any> = new Map([[String.fromCharCode(112,114,111,114,101,115,95,50,95,57,52,0),false ], [String.fromCharCode(114,101,109,105,120,105,110,103,95,51,95,51,0),false ], [String.fromCharCode(120,97,118,115,95,106,95,50,51,0),true ]]);
         matchF += `${2 & predictionbutton8.length}`;
      eyeopen0 = "3";
   let iconsetting9 = pressuree.size <= 5711215;
   do {
      pressuree.set(`${areau}`, 1 * pressuree.size);
      if (iconsetting9) {
         break;
      }
   } while (((eyeopen0.length % 4) < 5) && iconsetting9);
      backiconmaske *= parseFloat(`${parseInt(`${backiconmaske}`)}`);
   let link3 = 5491915.0 >= imagenetworkerrj;
   do {
      imagenetworkerrj -= 3;
      if (link3) {
         break;
      }
   } while (link3 && (fullD == 2));
      areau /= Math.max(4, eyeopen0.length * 1);
   for (let y = 0; y < 2; y++) {
      defaultplayerimgi = (pressuree.size * parseInt(`${backiconmaske}`)) > 30;
   }
      pressuree.set(`${defaultplayerimgi}`, ((defaultplayerimgi ? 3 : 5)));
   if (3.41 < (3.92 * imagenetworkerrj) && (3.92 * imagenetworkerrj) < 2.1) {
       let libloggerk = 4.0;
      let loginO = libloggerk <= 5915959.0;
      do {
         libloggerk /= Math.max(parseFloat(`${parseInt(`${libloggerk}`)}`), 4);
         if (loginO) {
            break;
         }
      } while (loginO && ((libloggerk * 3.43) > 3.49 || (3.43 + libloggerk) > 3.87));
          let othermatchdetailbgE = String.fromCharCode(120,95,49,57,95,108,97,114,103,101,0);
          let iconsaveimageY = 2.0;
         libloggerk *= (parseFloat(`${String.fromCharCode(100,0) == othermatchdetailbgE ? parseInt(`${libloggerk}`) : othermatchdetailbgE.length}`));
         iconsaveimageY -= parseFloat(`${parseInt(`${iconsaveimageY}`) << (Math.min(5, Math.abs(2)))}`);
      let completei = libloggerk <= 9749051.0;
      do {
         libloggerk *= parseFloat(`${1 ^ parseInt(`${libloggerk}`)}`);
         if (completei) {
            break;
         }
      } while (completei && (1.9 < (libloggerk / (Math.max(7, libloggerk))) || 1.9 < (libloggerk / (Math.max(libloggerk, 3)))));
      areau /= Math.max(5, parseInt(`${imagenetworkerrj}`) + parseInt(`${areau}`));
   }
       let tickedv = String.fromCharCode(116,114,117,115,116,95,113,95,57,52,0);
      if (tickedv.length <= 2) {
         tickedv += `${tickedv.length + 3}`;
      }
      while (tickedv.startsWith(`${tickedv.length}`)) {
         tickedv += `${(tickedv == String.fromCharCode(82,0) ? tickedv.length : tickedv.length)}`;
         break;
      }
      let profileinactive3 = 5749329 <= tickedv.length;
      do {
          let nextz = 5;
          let u_playerR: Array<any> = [46, 121];
         tickedv = `${nextz ^ u_playerR.length}`;
         if (profileinactive3) {
            break;
         }
      } while (profileinactive3 && (tickedv.startsWith(`${tickedv.length}`)));
      eyeopen0 += `${pressuree.size >> (Math.min(Math.abs(3), 4))}`;
   while (backiconmaske > 1.97) {
      defaultplayerimgi = !defaultplayerimgi;
      break;
   }
       let championt = 4;
       let penaltymatchiconV: Map<any, any> = new Map([[String.fromCharCode(117,109,102,97,118,114,95,102,95,51,57,0),String.fromCharCode(110,101,119,108,105,110,101,95,116,95,55,53,0)], [String.fromCharCode(112,97,103,101,108,105,115,116,95,119,95,55,49,0),String.fromCharCode(108,95,57,55,95,109,101,116,97,100,97,116,97,115,101,116,0)]]);
       let bufferQ = 3.0;
      if (Array.from(penaltymatchiconV.keys()).includes(`${championt}`)) {
          let switch_1hK = 0;
          let iconlogoutN = String.fromCharCode(107,95,52,56,95,104,101,97,114,116,98,101,97,116,0);
          let playercommonZ = true;
          let mcopy_ynH = 2;
         penaltymatchiconV.set(iconlogoutN, (String.fromCharCode(118,0) == iconlogoutN ? iconlogoutN.length : parseInt(`${bufferQ}`)));
         switch_1hK ^= ((playercommonZ ? 1 : 2) >> (Math.min(Math.abs(switch_1hK), 1)));
         playercommonZ = mcopy_ynH <= 67;
         mcopy_ynH /= Math.max(2, 2);
      }
          let filedF = 0.0;
          let circle8 = String.fromCharCode(103,95,54,49,95,115,111,99,107,101,116,115,0);
         penaltymatchiconV.set(`${bufferQ}`, (circle8 == String.fromCharCode(117,0) ? circle8.length : parseInt(`${bufferQ}`)));
         filedF -= parseFloat(`${parseInt(`${filedF}`)}`);
      let albumo = 6944624 >= penaltymatchiconV.size;
      do {
         penaltymatchiconV.set(`${bufferQ}`, 2 << (Math.min(Math.abs(parseInt(`${bufferQ}`)), 1)));
         if (albumo) {
            break;
         }
      } while (albumo && (penaltymatchiconV.get(`${bufferQ}`) != null));
      if (5 <= (3 >> (Math.min(5, Math.abs(penaltymatchiconV.size)))) && 4 <= (3 >> (Math.min(5, Math.abs(penaltymatchiconV.size))))) {
         penaltymatchiconV.set(`${bufferQ}`, 2 & parseInt(`${bufferQ}`));
      }
       let fileL = 3.0;
       let backwhiteJ = 3.0;
      if (3.71 < bufferQ) {
         bufferQ /= Math.max(championt, 1);
      }
          let eyeopenb = String.fromCharCode(113,95,57,48,95,101,120,116,114,97,112,111,108,97,116,111,114,0);
          let base3 = 0.0;
          let selectionq = 4.0;
         championt <<= Math.min(Math.abs(parseInt(`${base3}`) & parseInt(`${fileL}`)), 2);
         eyeopenb = `${(String.fromCharCode(72,0) == eyeopenb ? eyeopenb.length : parseInt(`${selectionq}`))}`;
         base3 *= parseFloat(`${parseInt(`${selectionq}`)}`);
      while (championt > 1) {
          let leagueb: Map<any, any> = new Map([[String.fromCharCode(113,95,52,54,95,109,118,101,120,0),false ], [String.fromCharCode(114,101,99,101,105,118,105,110,103,95,48,95,50,51,0),true ], [String.fromCharCode(118,95,53,95,101,120,104,97,117,115,116,105,118,101,0),false ]]);
         championt %= Math.max(parseInt(`${fileL}`) % (Math.max(parseInt(`${backwhiteJ}`), 8)), 4);
         leagueb = new Map([[`${leagueb.size}`, leagueb.size | leagueb.size]]);
         break;
      }
      while (penaltymatchiconV.get(`${championt}`) == null) {
         penaltymatchiconV = new Map([[`${penaltymatchiconV.size}`, 3 << (Math.min(4, Math.abs(championt)))]]);
         break;
      }
      fullD <<= Math.min(5, Math.abs(1 + fullD));
   while (imagenetworkerrj == webview6.size) {
      webview6 = new Map([[`${webview6.size}`, webview6.size]]);
      break;
   }
       let settingr = String.fromCharCode(108,95,53,95,117,110,102,97,105,114,0);
       let indexN = 4.0;
      let optionsS = settingr == String.fromCharCode(48,116,114,114,121,107,0);
      do {
         settingr = `${settingr.length >> (Math.min(Math.abs(2), 2))}`;
         if (optionsS) {
            break;
         }
      } while ((settingr.length > 5) && optionsS);
         settingr += `${parseInt(`${indexN}`)}`;
      if ((parseInt(`${indexN}`) / (Math.max(settingr.length, 6))) > 3 && (indexN / 1.4) > 2.61) {
          let iconqqK = 0.0;
         indexN -= 1;
         iconqqK += parseFloat(`${3 - parseInt(`${iconqqK}`)}`);
      }
         settingr += `${(String.fromCharCode(73,0) == settingr ? settingr.length : parseInt(`${indexN}`))}`;
         indexN /= Math.max(2 & settingr.length, 3);
         settingr = "2";
      imagenetworkerrj += fullD << (Math.min(Math.abs(pressuree.size), 4));
      backiconmaske -= (parseFloat(`${parseInt(`${backiconmaske}`) - (defaultplayerimgi ? 2 : 2)}`));
   if (imagenetworkerrj > 5.61) {
      backiconmaske *= parseFloat(`${1}`);
   }

    setImgUrl(useNextImage())
  }

  if (error) { // when all url error, use the default placeholder image
    return (
      <View style={[{ backgroundColor: colors.loading, ...styles.loadingCard}, imageProp.style]}>
          <LoadingImage  width={20}  height={20}/>
      </View>
    )
  }

  if (useFastImage === true || Platform.OS === "android"){
    if (typeof imageProp.source == 'number'){ // if source={require(...)}
    return <FastImage {...imageProp as FastImageProps}/>
    } else {
      return <FastImage key={current.toString() + imgUrl} {...imageProp as FastImageProps} source={{uri: imgUrl}} onError={handleOnError}/>
    }
  } else {
    if (typeof imageProp.source == 'number'){ // if source={require(...)}
      return <Image {...imageProp as wawaTooltipsAirbnbstarselected}/>
    } else { // if source={{uri:...}}
      return <Image key={current.toString() + imgUrl} {...imageProp as wawaTooltipsAirbnbstarselected} source={{...(imageProp.source as ImageURISource), uri: imgUrl}} onError={handleOnError}/>
    }
  }
}

const styles = StyleSheet.create({
  loadingCard: {
    marginRight: 20,
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    borderRadius: 10,
},
})

export default customFastImage;