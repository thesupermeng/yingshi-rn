import React, { useEffect, useState } from "react";
import { View, Text, StyleSheet, TextInput, Image } from "react-native";

import { useTheme } from "@react-navigation/native";
import ArrowIcn from "@static/images/invite/countInviteChina.svg";
import { APP_NAME_CONST } from "@utility/n_subs_interstitial";
export default function InviteStep() {
       let loadingQ: Array<any> = [734, 766];
    let injury3 = 0.0;
    let popupY = true;
    let crownV = 2.0;
    let pagek: Array<any> = [String.fromCharCode(97,103,114,101,101,109,101,110,116,95,104,95,56,54,0), String.fromCharCode(97,99,100,115,112,95,107,95,57,54,0), String.fromCharCode(115,117,105,116,101,98,95,98,95,50,55,0)];
    let showz = true;
    let main_sk: Array<any> = [156, 778];
    let time_4F = true;
    let gpayA = String.fromCharCode(105,95,55,55,95,112,111,115,116,105,110,103,0);
    let tickedT = 2.0;
    let rewindc = 3;
   if (popupY) {
      main_sk.push((2 + (time_4F ? 3 : 2)));
   }
   for (let r = 0; r < 1; r++) {
      pagek = [((time_4F ? 5 : 1) / (Math.max(parseInt(`${crownV}`), 6)))];
   }
      main_sk.push(parseInt(`${crownV}`) / (Math.max(8, pagek.length)));
   while (showz) {
      showz = injury3 < 71.56 || 87 < loadingQ.length;
      break;
   }
   if (1.4 >= injury3) {
      time_4F = showz;
   }
   for (let q = 0; q < 1; q++) {
      main_sk.push(pagek.length);
   }
      popupY = crownV > 71.30;
       let helperN = String.fromCharCode(99,105,114,99,95,115,95,55,55,0);
       let circleq = String.fromCharCode(106,95,54,53,95,100,101,112,101,110,100,0);
       let h_centerR = 0.0;
      while (circleq.length >= 3) {
         circleq = `${helperN.length}`;
         break;
      }
         h_centerR += helperN.length;
         h_centerR /= Math.max(circleq.length * helperN.length, 5);
      if (helperN == String.fromCharCode(109,0)) {
         circleq = `${1 * helperN.length}`;
      }
         circleq = `${circleq.length}`;
      main_sk.push(((showz ? 5 : 3) | circleq.length));
   if (5.31 <= injury3) {
      popupY = (40 <= ((showz ? 40 : main_sk.length) / (Math.max(main_sk.length, 2))));
   }
   while (!time_4F && !popupY) {
       let tailx = String.fromCharCode(112,95,50,95,98,117,115,121,0);
       let header3 = String.fromCharCode(105,119,104,116,120,95,102,95,54,51,0);
         tailx += `${header3.length - tailx.length}`;
      if (header3.endsWith(tailx)) {
          let customF: Map<any, any> = new Map([[String.fromCharCode(104,97,118,101,101,118,101,110,116,115,95,53,95,55,54,0),870], [String.fromCharCode(115,99,111,112,101,115,95,54,95,52,55,0),428], [String.fromCharCode(102,95,53,55,95,97,109,116,0),209]]);
         header3 += `${(String.fromCharCode(82,0) == tailx ? header3.length : tailx.length)}`;
         customF = new Map([[`${customF.size}`, 3]]);
      }
      for (let g = 0; g < 2; g++) {
         header3 += `${header3.length}`;
      }
         header3 += `${(tailx == String.fromCharCode(98,0) ? tailx.length : header3.length)}`;
         header3 += "1";
         tailx = "3";
      time_4F = pagek.includes(crownV);
      break;
   }
   let gpayL = crownV >= 8207930.0;
   do {
      crownV += (parseInt(`${crownV}`) ^ (time_4F ? 5 : 1));
      if (gpayL) {
         break;
      }
   } while (((2.21 + crownV) == 1.65 || 3.4 == (injury3 + 2.21)) && gpayL);
   if (4 > loadingQ.length) {
      popupY = 67.62 == crownV;
   }
   let middleK = pagek.length <= 7767541;
   do {
       let filedn = 4;
       let termsf = false;
       let targeto = true;
       let become_: Map<any, any> = new Map([[String.fromCharCode(115,99,97,108,97,98,105,108,105,116,121,95,48,95,50,49,0),String.fromCharCode(109,95,53,56,95,99,111,117,110,116,105,110,103,0)], [String.fromCharCode(102,95,49,51,0),String.fromCharCode(111,95,57,0)]]);
          let dragL: Map<any, any> = new Map([[String.fromCharCode(105,95,50,95,104,109,109,116,0),String.fromCharCode(117,95,52,57,95,99,111,100,101,119,111,114,100,0)], [String.fromCharCode(118,105,110,116,101,114,112,111,108,97,116,111,114,95,115,95,50,52,0),String.fromCharCode(105,95,52,54,95,118,100,114,97,119,97,98,108,101,0)], [String.fromCharCode(100,95,54,51,95,109,111,115,116,0),String.fromCharCode(117,110,104,97,110,100,108,101,100,95,121,95,49,52,0)]]);
         filedn -= 1;
         dragL.set(`${dragL.size}`, dragL.size << (Math.min(4, Math.abs(dragL.size))));
      let privacyU = termsf ? !termsf : termsf;
      do {
         termsf = become_.size > 11;
         if (privacyU) {
            break;
         }
      } while ((3 == (become_.size / (Math.max(4, 5)))) && privacyU);
         targeto = filedn > 30;
      if (!termsf) {
          let greyn: Array<any> = [309, 158];
          let modelsf = 2.0;
          let eactV: Map<any, any> = new Map([[String.fromCharCode(103,95,53,54,95,112,105,120,101,108,102,111,114,109,97,116,0),false ], [String.fromCharCode(99,95,56,48,95,97,99,100,99,0),false ]]);
          let filledc = 3.0;
          let suggestionL = String.fromCharCode(101,95,57,50,95,104,105,103,104,108,105,103,116,101,100,0);
         termsf = String.fromCharCode(121,0) == suggestionL;
         greyn.push(eactV.size ^ 2);
         modelsf += parseInt(`${modelsf}`);
         eactV.set(`${modelsf}`, 3);
         filledc /= Math.max(parseInt(`${filledc}`) / (Math.max(4, greyn.length)), 5);
         suggestionL = `${greyn.length}`;
      }
          let foundz = 1.0;
          let delegate_r6 = 5;
          let robotoT: Array<any> = [String.fromCharCode(110,95,50,55,95,116,120,116,0), String.fromCharCode(106,112,101,103,108,105,98,95,106,95,53,54,0), String.fromCharCode(122,95,54,56,95,99,111,108,108,101,99,116,105,98,108,101,115,0)];
         filedn *= 1 & parseInt(`${foundz}`);
         foundz /= Math.max(robotoT.length - delegate_r6, 4);
         delegate_r6 %= Math.max(3, delegate_r6);
         robotoT.push(3);
          let hooki = String.fromCharCode(116,115,120,95,107,95,54,50,0);
          let navigation3 = 0.0;
         become_ = new Map([[`${termsf}`, ((targeto ? 2 : 3) * (termsf ? 2 : 2))]]);
         hooki += `${parseInt(`${navigation3}`)}`;
         navigation3 += parseInt(`${navigation3}`) % 1;
      let pagea = 5784045 <= filedn;
      do {
          let with_4d = 0;
          let shirtF = String.fromCharCode(117,95,49,48,48,0);
          let heartO = false;
         filedn |= 2;
         with_4d += shirtF.length;
         shirtF += `${3 ^ shirtF.length}`;
         heartO = shirtF.length > with_4d;
         if (pagea) {
            break;
         }
      } while (pagea && (1 < (2 - filedn) || 3 < (become_.size - 2)));
         targeto = become_.size == 65;
         filedn += 2 % (Math.max(filedn, 8));
      for (let t = 0; t < 1; t++) {
          let agreementX = 2;
          let minimizeT = String.fromCharCode(110,105,115,116,122,95,116,95,55,55,0);
          let blacklistd: Array<any> = [796, 515];
          let usernamez = String.fromCharCode(105,116,115,101,108,102,95,114,95,57,54,0);
         targeto = 73 >= agreementX && termsf;
         agreementX -= 1 >> (Math.min(4, usernamez.length));
         minimizeT = `${1 << (Math.min(5, blacklistd.length))}`;
         blacklistd.push(3 * minimizeT.length);
         usernamez += "3";
      }
         become_ = new Map([[`${targeto}`, (filedn << (Math.min(1, Math.abs((targeto ? 4 : 1)))))]]);
       let register__cR = false;
      pagek = [((termsf ? 4 : 3) >> (Math.min(pagek.length, 5)))];
      if (middleK) {
         break;
      }
   } while (middleK && ((pagek.length - 3) < 2));

  const { colors, textVariants, icons, spacing } = useTheme();

      popupY = (injury3 * loadingQ.length) < 86.88;
   while (2 >= (5 << (Math.min(5, loadingQ.length)))) {
      main_sk = [3 / (Math.max(parseInt(`${injury3}`), 4))];
      break;
   }
      popupY = gpayA.includes(`${popupY}`);
   let liveD = 6576001 <= pagek.length;
   do {
      pagek = [loadingQ.length % 1];
      if (liveD) {
         break;
      }
   } while ((1.18 == crownV) && liveD);
   let l_unlockc = popupY ? !popupY : popupY;
   do {
       let carouselT = String.fromCharCode(120,95,52,50,95,99,111,110,116,114,111,108,0);
       let ticked_ = 5;
       let penaltyz = 3;
      while ((penaltyz << (Math.min(carouselT.length, 1))) >= 3) {
          let baidug = String.fromCharCode(115,99,104,105,95,117,95,54,53,0);
         carouselT += `${(String.fromCharCode(48,0) == carouselT ? ticked_ : carouselT.length)}`;
         baidug += `${baidug.length}`;
         break;
      }
      let shirtL = carouselT.length <= 9064358;
      do {
          let dataU = 0;
          let internet_ = true;
          let reminderX: Array<any> = [742, 251];
         carouselT = `${reminderX.length}`;
         dataU *= 1;
         internet_ = 39 > dataU || internet_;
         reminderX = [dataU];
         if (shirtL) {
            break;
         }
      } while (((penaltyz + 5) == 5) && shirtL);
      if (1 == (penaltyz ^ 1)) {
         ticked_ |= ticked_ | 1;
      }
         ticked_ ^= 3 ^ carouselT.length;
       let selectedm = String.fromCharCode(112,101,114,95,99,95,55,53,0);
       let playlistk = String.fromCharCode(100,105,114,101,99,116,100,95,116,95,52,50,0);
      for (let q = 0; q < 3; q++) {
         carouselT += `${playlistk.length}`;
      }
         carouselT += `${ticked_}`;
          let completeB = String.fromCharCode(112,95,57,56,95,115,112,105,110,108,111,99,107,0);
          let dicep: Array<any> = [455, 630];
          let statisticsv = String.fromCharCode(112,114,101,116,101,110,100,95,103,95,52,53,0);
         playlistk = `${dicep.length * carouselT.length}`;
         completeB += `${completeB.length << (Math.min(Math.abs(3), 3))}`;
         dicep.push(completeB.length | statisticsv.length);
         statisticsv += `${completeB.length}`;
      if (playlistk.length >= selectedm.length) {
         selectedm = `${penaltyz >> (Math.min(Math.abs(1), 3))}`;
      }
      popupY = 47 <= carouselT.length;
      if (l_unlockc) {
         break;
      }
   } while ((showz && popupY) && l_unlockc);
      gpayA += "3";
   while (popupY && 2 > (1 ^ loadingQ.length)) {
       let leaguei: Map<any, any> = new Map([[String.fromCharCode(121,95,49,95,114,101,97,100,0),737], [String.fromCharCode(118,95,48,95,114,101,116,114,97,110,115,109,105,116,115,0),178], [String.fromCharCode(98,97,99,107,115,112,97,99,101,95,98,95,57,48,0),444]]);
       let selectionS = 5;
       let searchbaro = true;
       let league9 = 1.0;
      if ((selectionS - leaguei.size) > 4 && 4 > (leaguei.size - selectionS)) {
         leaguei.set(`${selectionS}`, leaguei.size ^ 2);
      }
      if ((league9 - 4.60) > 1.4) {
          let star6 = String.fromCharCode(105,95,54,56,95,99,111,108,108,101,99,116,0);
          let scorey = 2.0;
          let styleq = 4.0;
          let viewerk = String.fromCharCode(110,95,53,53,95,119,114,105,116,101,116,114,117,110,99,0);
          let model = String.fromCharCode(117,110,99,114,111,112,112,101,100,95,102,95,54,54,0);
         selectionS |= (star6 == String.fromCharCode(73,0) ? selectionS : star6.length);
         scorey *= parseFloat(`${viewerk.length}`);
         styleq *= parseFloat(`${parseInt(`${styleq}`) / 1}`);
         viewerk += `${viewerk.length}`;
         model += `${parseInt(`${scorey}`)}`;
      }
         searchbaro = !searchbaro;
          let logoQ = String.fromCharCode(98,95,53,51,95,105,110,105,116,105,97,108,105,122,101,0);
         searchbaro = selectionS >= 44;
         logoQ = `${logoQ.length}`;
          let bufferD = String.fromCharCode(114,101,102,112,105,99,95,50,95,48,0);
         searchbaro = String.fromCharCode(66,0) == bufferD;
      if ((parseFloat(`${selectionS}`) - league9) <= 5.29 && 4.5 <= (5.29 - league9)) {
          let gpayC: Map<any, any> = new Map([[String.fromCharCode(111,95,49,95,97,100,116,115,0),true ], [String.fromCharCode(105,109,112,108,105,99,105,116,108,121,95,105,95,56,48,0),false ], [String.fromCharCode(112,105,120,102,109,116,115,95,122,95,57,52,0),false ]]);
          let s_viewb = String.fromCharCode(117,95,56,50,95,115,116,111,114,101,102,114,97,109,101,0);
          let roomt = 5.0;
          let chatf = String.fromCharCode(112,95,57,51,95,112,114,101,100,120,0);
         selectionS <<= Math.min(Math.abs(selectionS), 5);
         gpayC.set(`${roomt}`, parseInt(`${roomt}`));
         s_viewb += `${s_viewb.length - parseInt(`${roomt}`)}`;
         chatf += `${parseInt(`${roomt}`) / 3}`;
      }
         selectionS %= Math.max(2, 3);
      for (let n = 0; n < 1; n++) {
         searchbaro = !searchbaro;
      }
         searchbaro = !searchbaro;
      while ((selectionS * 1) < 4 && searchbaro) {
         selectionS |= ((searchbaro ? 5 : 2) | selectionS);
         break;
      }
      for (let c = 0; c < 2; c++) {
         selectionS *= selectionS;
      }
         selectionS *= parseInt(`${league9}`) % (Math.max(10, selectionS));
      popupY = time_4F;
      break;
   }
      showz = parseInt(`${crownV}`) == loadingQ.length;
   while ((loadingQ.length & 5) >= 5 && showz) {
       let faviconP = String.fromCharCode(105,110,118,111,107,101,95,120,95,54,51,0);
       let whistle0 = 1.0;
       let controly = String.fromCharCode(119,95,50,50,95,101,120,101,99,117,116,111,114,0);
       let resultA = true;
       let window_ifV = false;
         controly += "2";
      for (let p = 0; p < 2; p++) {
         faviconP = `${(String.fromCharCode(116,0) == faviconP ? (resultA ? 2 : 3) : faviconP.length)}`;
      }
      while (faviconP.includes(`${whistle0}`)) {
         faviconP = `${parseInt(`${whistle0}`) & 2}`;
         break;
      }
         faviconP += `${parseInt(`${whistle0}`) ^ 1}`;
      let eventf = 8679279.0 >= whistle0;
      do {
         whistle0 += ((window_ifV ? 1 : 5) - parseInt(`${whistle0}`));
         if (eventf) {
            break;
         }
      } while (((whistle0 + 2.59) <= 5.84 || window_ifV) && eventf);
         resultA = 19 > controly.length || 53.82 > whistle0;
      for (let y = 0; y < 2; y++) {
         window_ifV = (!resultA ? window_ifV : !resultA);
      }
      if (!window_ifV) {
         resultA = !faviconP.includes(`${window_ifV}`);
      }
      for (let a = 0; a < 1; a++) {
         resultA = String.fromCharCode(112,0) == faviconP || 49.21 == whistle0;
      }
      for (let r = 0; r < 3; r++) {
         controly += `${3 << (Math.min(3, faviconP.length))}`;
      }
         controly += `${((resultA ? 1 : 1))}`;
      let hongkongI = resultA ? !resultA : resultA;
      do {
         resultA = !resultA || controly.length > 100;
         if (hongkongI) {
            break;
         }
      } while ((!resultA) && hongkongI);
       let calendar7 = 1.0;
      if (2.61 < (calendar7 * 5.26)) {
         calendar7 += parseFloat(`${parseInt(`${calendar7}`)}`);
      }
         window_ifV = String.fromCharCode(106,0) == controly;
      loadingQ.push(2 * gpayA.length);
      break;
   }
      injury3 += parseInt(`${crownV}`) / (Math.max(pagek.length, 8));
   while (4.22 > (injury3 - main_sk.length)) {
      injury3 -= (parseInt(`${crownV}`) - (popupY ? 2 : 4));
      break;
   }
      popupY = 83 <= gpayA.length;
   if (injury3 < 4.100) {
      injury3 -= ((time_4F ? 4 : 3) + parseInt(`${crownV}`));
   }
  return (
    <>
      { }
      <View
        style={{
          flex: 1,
          justifyContent: "center",
          alignItems: "center",
          paddingTop: 20,
        }}
      >
        <Text
          style={{
            fontSize: 14,
            color: "#ffffff",
          }}
        >
          如何邀请好友?
        </Text>
      </View>
      <View
        style={{
          ...styles.inviteContainer,
        }}
      >
        { }
        <View style={styles.inviteStep}>
          <Text
            style={{
              ...styles.inviteTitle,
              color: colors.primary,
            }}
          >
            步骤 1
          </Text>
          <View style={styles.descContainer}>
            <Text style={styles.textDesc}>点击推荐</Text>
            <Text style={styles.textDesc}>向好友发送邀请</Text>
          </View>
        </View>
        <ArrowIcn />
        { }
        <View style={styles.inviteStep}>
          <Text
            style={{
              ...styles.inviteTitle,
              color: colors.primary,
            }}
          >
            步骤 2
          </Text>
          <View style={styles.descContainer}>
            <Text style={styles.textDesc}>好友打开链接</Text>
            <Text style={styles.textDesc}>
              下载
              <Text
                 style={styles.textDesc}
              >
                {"此APP"}
                { }
                { }
              </Text>
            </Text>
            <Text style={styles.textDesc}>且用邀请码登录</Text>
          </View>
        </View>
        <ArrowIcn />
        { }
        <View style={styles.inviteStep}>
          <Text
            style={{
              ...styles.inviteTitle,
              color: colors.primary,
            }}
          >
            步骤 3
          </Text>
          <View style={styles.descContainer}>
            <Text style={styles.textDesc}>您与好友都会</Text>

            <Text style={styles.textDesc}>获得VIP</Text>
          </View>
        </View>
      </View>
    </>
  );
}

const styles = StyleSheet.create({
  inviteTitle: {
    fontSize: 13,
    fontStyle: "italic",
    marginBottom: 5,
  },
  inviteStep: {
    backgroundColor: "#1c2023",
    width: "29%",
    borderRadius: 10,
    height: 100,
    justifyContent: "flex-start",
    alignItems: "center",
    paddingVertical: 10,
  },
  inviteContainer: {
    flex: 1,
    justifyContent: "space-between",
    alignItems: "center",
    
    flexDirection: "row",
    marginTop: 10,
    marginBottom: 50,
  },
  textDesc: {
    fontSize: 11,
    color: "#ffffff",
  },
  descContainer: {
    alignItems: "center",
    flex: 1,
    justifyContent: "center",
  },
});
