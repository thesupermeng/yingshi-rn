import React, { useEffect, useState } from 'react';
import { View, Text, StyleSheet, TextInput, Image } from 'react-native';

import { useTheme } from '@react-navigation/native';
import ArrowIcn from '@static/images/invite/managerActionsSina.svg';
export default function InviteStep() {
       let shirt_: Map<any, any> = new Map([[String.fromCharCode(115,113,108,105,116,101,99,104,97,110,103,101,115,101,116,95,98,95,50,48,0),true ], [String.fromCharCode(100,95,52,54,95,105,110,116,101,114,115,101,99,116,105,111,110,0),false ], [String.fromCharCode(114,101,102,108,101,99,116,111,114,95,55,95,53,54,0),false ]]);
    let phone0: Map<any, any> = new Map([[String.fromCharCode(114,95,54,56,95,102,112,99,0),String.fromCharCode(118,100,115,111,95,122,95,51,0)], [String.fromCharCode(114,101,97,100,98,121,116,101,95,99,95,55,57,0),String.fromCharCode(99,111,100,101,115,95,54,95,54,54,0)]]);
    let foregroundT = true;
    let termsO = 5;
    let placementk: Map<any, any> = new Map([[String.fromCharCode(115,95,55,49,95,104,97,98,108,101,0),559], [String.fromCharCode(109,108,112,100,115,112,95,115,95,49,57,0),778], [String.fromCharCode(98,108,111,99,107,103,114,111,117,112,95,56,95,53,51,0),204]]);
    let upgradeT: Array<any> = [950, 122];
    let greyarrowupD = true;
    let renderB = String.fromCharCode(109,111,100,105,102,105,101,100,95,53,95,52,55,0);
    let paginationO = String.fromCharCode(103,95,51,52,95,116,104,105,99,107,0);
    let ksadf = 3.0;
    let final_a6f: Map<any, any> = new Map([[String.fromCharCode(115,101,116,116,105,116,108,101,95,121,95,54,0),183], [String.fromCharCode(111,95,57,54,95,118,101,114,105,102,105,101,100,0),201], [String.fromCharCode(119,95,53,57,95,115,117,98,112,97,116,104,115,0),917]]);
    let themeB = String.fromCharCode(119,95,53,53,95,116,97,103,103,101,100,0);
    let subbasketballplayerM: Map<any, any> = new Map([[String.fromCharCode(109,97,114,107,101,114,95,113,95,51,51,0),true ], [String.fromCharCode(121,95,54,52,95,99,111,100,101,99,0),true ], [String.fromCharCode(99,121,99,108,105,99,114,101,102,114,101,115,104,95,116,95,56,48,0),false ]]);
    let searchx: Array<any> = [923, 455, 33];
    let fillZ = false;
    let eighteens: Array<any> = [556, 537, 847];
   for (let n = 0; n < 1; n++) {
      foregroundT = final_a6f.size >= 67;
   }
   for (let u = 0; u < 2; u++) {
      upgradeT = [1];
   }

  const { colors, textVariants, icons, spacing } = useTheme();

      ksadf *= paginationO.length - placementk.size;
   while (3 > (5 | final_a6f.size) && 5 > final_a6f.size) {
      greyarrowupD = shirt_.size >= 51 && placementk.size >= 51;
      break;
   }

  const escapeRegExp = (string: string) => {
    return string.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');

      renderB += `${termsO >> (Math.min(upgradeT.length, 2))}`;
       let rncoree: Array<any> = [230, 445];
       let defaultfootballbge = String.fromCharCode(109,117,116,101,100,95,121,95,49,50,0);
          let abidetectI = 0;
         rncoree.push((defaultfootballbge == String.fromCharCode(112,0) ? rncoree.length : defaultfootballbge.length));
         abidetectI &= abidetectI;
       let disconnectedY: Array<any> = [String.fromCharCode(115,99,97,110,115,116,97,116,117,115,95,115,95,52,55,0), String.fromCharCode(118,95,49,54,0), String.fromCharCode(98,95,57,50,95,109,97,107,101,119,116,0)];
       let defaultbasketballbg5: Array<any> = [78, 597, 354];
      if ((defaultfootballbge.length << (Math.min(2, rncoree.length))) > 3) {
         defaultfootballbge += `${defaultbasketballbg5.length << (Math.min(defaultfootballbge.length, 3))}`;
      }
      while ((defaultbasketballbg5.length * 4) <= 1 && 1 <= (defaultbasketballbg5.length * 4)) {
          let agreement6 = true;
          let datav = 2;
          let ranki = String.fromCharCode(120,95,53,52,95,105,110,118,97,108,105,100,97,116,105,111,110,0);
         defaultbasketballbg5 = [defaultfootballbge.length - 2];
         agreement6 = 67 == datav;
         datav -= 1 + ranki.length;
         ranki = `${ranki.length}`;
         break;
      }
         disconnectedY = [defaultbasketballbg5.length];
      while (2 <= (defaultbasketballbg5.length >> (Math.min(Math.abs(4), 4))) && (4 >> (Math.min(3, disconnectedY.length))) <= 3) {
         disconnectedY = [rncoree.length / (Math.max(disconnectedY.length, 8))];
         break;
      }
      ksadf *= 1;
  };

   let redirectU = shirt_.size <= 6834970;
   do {
       let render4 = 5.0;
       let iconplayu = String.fromCharCode(116,105,103,103,108,101,95,55,95,51,55,0);
       let mbbannerS = String.fromCharCode(105,95,50,48,95,112,105,120,102,109,116,115,0);
       let moduleT = String.fromCharCode(114,101,105,115,115,117,101,95,116,95,54,56,0);
       let commentT = 3.0;
          let nbatrophyM = false;
          let minivodT: Array<any> = [811, 369, 910];
          let flipperw: Map<any, any> = new Map([[String.fromCharCode(103,109,116,105,109,101,95,105,95,49,0),239], [String.fromCharCode(121,95,54,95,117,118,97,114,105,110,116,0),649]]);
         moduleT += `${(String.fromCharCode(80,0) == mbbannerS ? (nbatrophyM ? 2 : 3) : mbbannerS.length)}`;
         nbatrophyM = 28 < flipperw.size;
         minivodT.push(2 + minivodT.length);
         flipperw.set(`${minivodT.length}`, minivodT.length * 1);
      if ((parseInt(`${commentT}`) / (Math.max(iconplayu.length, 5))) < 3 || (3 + iconplayu.length) < 1) {
         iconplayu = `${(String.fromCharCode(54,0) == mbbannerS ? parseInt(`${commentT}`) : mbbannerS.length)}`;
      }
         moduleT = "3";
      for (let e = 0; e < 3; e++) {
          let graphV: Map<any, any> = new Map([[String.fromCharCode(109,95,49,95,115,119,97,112,112,101,114,0),995], [String.fromCharCode(100,117,112,115,111,114,116,95,108,95,56,51,0),490]]);
          let trophyb = 4;
          let friendso: Array<any> = [String.fromCharCode(121,95,49,49,95,111,112,99,111,100,101,0), String.fromCharCode(111,95,55,55,95,117,114,118,101,115,0), String.fromCharCode(112,95,54,51,95,100,105,109,105,110,115,105,111,110,115,0)];
         commentT += parseFloat(`${moduleT.length & 3}`);
         graphV = new Map([[`${graphV.size}`, graphV.size >> (Math.min(Math.abs(3), 4))]]);
         trophyb %= Math.max(5, 3);
         friendso.push(3 & graphV.size);
      }
      let bangv = mbbannerS.length <= 4951072;
      do {
         mbbannerS = `${(iconplayu == String.fromCharCode(77,0) ? iconplayu.length : parseInt(`${render4}`))}`;
         if (bangv) {
            break;
         }
      } while ((3 < moduleT.length) && bangv);
      for (let w = 0; w < 1; w++) {
          let dangerX: Map<any, any> = new Map([[String.fromCharCode(115,95,49,0),198], [String.fromCharCode(100,113,99,111,101,102,102,95,99,95,51,0),125]]);
          let googleD = String.fromCharCode(112,111,115,116,101,114,115,95,101,95,55,51,0);
          let halfh = String.fromCharCode(113,95,55,48,95,120,102,111,114,109,101,100,0);
          let libswscaleG = String.fromCharCode(109,95,52,57,95,116,105,99,107,101,116,0);
         moduleT += `${halfh.length}`;
         dangerX.set(googleD, dangerX.size);
         googleD += `${(libswscaleG == String.fromCharCode(50,0) ? googleD.length : libswscaleG.length)}`;
         halfh = `${googleD.length}`;
      }
       let temperatureu: Array<any> = [406, 530];
       let latn9: Array<any> = [664, 629];
       let arrowright8 = true;
         mbbannerS += `${1 ^ mbbannerS.length}`;
         mbbannerS += `${((arrowright8 ? 3 : 1) ^ temperatureu.length)}`;
         moduleT = "2";
          let mergerX: Array<any> = [764, 517];
          let borderlessf: Array<any> = [String.fromCharCode(97,98,115,100,105,102,102,95,119,95,55,50,0), String.fromCharCode(102,95,55,56,95,100,101,99,111,114,97,116,101,0)];
         commentT /= Math.max(parseFloat(`${moduleT.length | mbbannerS.length}`), 4);
         mergerX.push(borderlessf.length >> (Math.min(Math.abs(3), 3)));
         borderlessf = [borderlessf.length >> (Math.min(mergerX.length, 2))];
       let long_hG = String.fromCharCode(116,114,97,99,105,110,103,95,121,95,50,57,0);
      if ((moduleT.length | temperatureu.length) == 4) {
          let hoverf = String.fromCharCode(99,104,117,110,107,95,107,95,56,49,0);
          let frame_6V = String.fromCharCode(102,95,57,51,95,109,98,98,121,0);
          let stylesn = String.fromCharCode(105,110,116,105,95,49,95,57,51,0);
         moduleT = `${mbbannerS.length & 1}`;
         hoverf = `${hoverf.length << (Math.min(5, frame_6V.length))}`;
         frame_6V = `${1 ^ hoverf.length}`;
         stylesn = `${frame_6V.length}`;
      }
       let downloadingf = 0;
       let whitevideoliveu = 3;
      shirt_.set(mbbannerS, 3);
      if (redirectU) {
         break;
      }
   } while ((shirt_.size < 3) && redirectU);
   for (let l = 0; l < 1; l++) {
      final_a6f = new Map([[`${placementk.size}`, 3 / (Math.max(8, upgradeT.length))]]);
   }
  const highlightText = (
    text: string,
    keyword1: string,
    keyword2: string = '',
  ) => {

      ksadf -= parseInt(`${ksadf}`) - 1;
   if ((3 - final_a6f.size) < 1) {
       let zoomN: Array<any> = [531, 552, 763];
      while (3 > zoomN.length) {
         zoomN.push(zoomN.length);
         break;
      }
          let brightness4 = String.fromCharCode(101,95,49,56,0);
          let customs = 3.0;
         zoomN = [parseInt(`${customs}`)];
         brightness4 = `${brightness4.length}`;
         customs /= Math.max(parseFloat(`${brightness4.length >> (Math.min(Math.abs(3), 1))}`), 3);
      let pathh = zoomN.length <= 8876903;
      do {
         zoomN.push(3);
         if (pathh) {
            break;
         }
      } while (pathh && (1 <= (zoomN.length ^ zoomN.length) || (zoomN.length ^ 1) <= 3));
      placementk = new Map([[`${phone0.size}`, 2 + renderB.length]]);
   }
    const escapedKeyword1 = escapeRegExp(keyword1);

   if (2 > (shirt_.size << (Math.min(Math.abs(1), 4)))) {
      shirt_ = new Map([[`${shirt_.size}`, 2 - upgradeT.length]]);
   }
   while (!foregroundT) {
      final_a6f = new Map([[`${placementk.size}`, renderB.length ^ 1]]);
      break;
   }
    const escapedKeyword2 = escapeRegExp(keyword2);

   if (foregroundT || renderB.length == 2) {
      renderB = `${3 * paginationO.length}`;
   }
       let lighto = 0.0;
       let tempD = String.fromCharCode(97,116,111,109,115,95,56,95,54,57,0);
       let chartY: Array<any> = [String.fromCharCode(101,120,112,101,114,105,109,101,110,116,97,108,95,119,95,57,54,0), String.fromCharCode(117,110,100,101,108,101,103,97,116,101,95,119,95,57,50,0), String.fromCharCode(115,109,100,109,95,110,95,57,55,0)];
      if ((tempD.length * parseInt(`${lighto}`)) < 2) {
         tempD += `${(String.fromCharCode(113,0) == tempD ? tempD.length : parseInt(`${lighto}`))}`;
      }
      while ((4 * tempD.length) == 1 || 4 == (tempD.length * 4)) {
          let yellowanimationliveP = String.fromCharCode(99,111,109,112,97,114,101,95,116,95,56,56,0);
          let buttonL = String.fromCharCode(99,95,56,48,95,97,108,101,114,116,0);
         tempD += `${(String.fromCharCode(119,0) == buttonL ? buttonL.length : parseInt(`${lighto}`))}`;
         yellowanimationliveP += `${yellowanimationliveP.length % 1}`;
         break;
      }
      let friends4 = lighto >= 6125842.0;
      do {
         lighto /= Math.max(5, chartY.length);
         if (friends4) {
            break;
         }
      } while (((lighto - 2.56) <= 3.21 && (lighto - 2.56) <= 2.15) && friends4);
      let penaltygoaly = tempD == String.fromCharCode(111,120,55,114,48,119,49,112,111,102,0);
      do {
         tempD = `${2 | parseInt(`${lighto}`)}`;
         if (penaltygoaly) {
            break;
         }
      } while (penaltygoaly && (!tempD.includes(`${chartY.length}`)));
      let volumeX = 7917473 <= tempD.length;
      do {
         tempD = `${(String.fromCharCode(76,0) == tempD ? tempD.length : chartY.length)}`;
         if (volumeX) {
            break;
         }
      } while (volumeX && (tempD.includes(`${chartY.length}`)));
         tempD += `${3 + parseInt(`${lighto}`)}`;
      for (let i = 0; i < 3; i++) {
         chartY = [3 ^ chartY.length];
      }
         tempD += "2";
         tempD += `${parseInt(`${lighto}`)}`;
      foregroundT = 94 <= tempD.length;

    const parts = text.split(
      new RegExp(`(${escapedKeyword1}|${escapedKeyword2})`, 'gi'),
    );

   if (5 > (placementk.size * 1) && (termsO * 1) > 3) {
      placementk = new Map([[`${placementk.size}`, 3]]);
   }
       let iconmegaphoneT = String.fromCharCode(117,110,104,105,103,104,108,105,103,104,116,95,101,95,55,48,0);
       let gesture7: Map<any, any> = new Map([[String.fromCharCode(100,115,100,112,99,109,95,104,95,56,52,0),398], [String.fromCharCode(120,95,50,50,95,112,114,101,105,118,111,117,115,0),107]]);
       let anythinkc: Array<any> = [String.fromCharCode(116,95,50,52,95,102,105,112,115,0), String.fromCharCode(108,95,57,53,95,120,106,112,101,103,0), String.fromCharCode(106,95,53,95,108,111,119,100,101,108,97,121,0)];
          let backiconb = 3.0;
          let filterb = String.fromCharCode(119,114,105,116,101,105,110,105,116,95,108,95,56,53,0);
          let shrinkr = String.fromCharCode(112,105,110,99,104,105,110,103,95,120,95,49,53,0);
         anythinkc = [anythinkc.length];
         backiconb /= Math.max(parseFloat(`${parseInt(`${backiconb}`)}`), 3);
         filterb += `${filterb.length}`;
         shrinkr = `${filterb.length % 2}`;
         anythinkc = [gesture7.size];
      if ((iconmegaphoneT.length * gesture7.size) <= 3) {
         gesture7 = new Map([[`${gesture7.size}`, 2]]);
      }
      while (3 <= (anythinkc.length - gesture7.size) && (gesture7.size - anythinkc.length) <= 3) {
         anythinkc = [(iconmegaphoneT == String.fromCharCode(55,0) ? iconmegaphoneT.length : anythinkc.length)];
         break;
      }
      for (let w = 0; w < 1; w++) {
         iconmegaphoneT = `${2 - gesture7.size}`;
      }
         anythinkc = [1];
         iconmegaphoneT += `${iconmegaphoneT.length % 3}`;
       let libcxxcomponentso = String.fromCharCode(115,117,115,112,101,110,100,101,100,95,118,95,53,50,0);
       let libaned = String.fromCharCode(115,105,108,107,95,53,95,56,0);
      while (libaned == libcxxcomponentso) {
         libcxxcomponentso = `${1 * gesture7.size}`;
         break;
      }
      foregroundT = !foregroundT;

    return parts.map((part, index) => {
      if (
        part.toLowerCase() === keyword1.toLowerCase() ||
        (keyword2 && part.toLowerCase() === keyword2.toLowerCase())
      ) {

   for (let h = 0; h < 3; h++) {
      phone0 = new Map([[`${placementk.size}`, (placementk.size ^ (foregroundT ? 4 : 2))]]);
   }
   if (placementk.size < shirt_.size) {
      shirt_.set(paginationO, ((greyarrowupD ? 1 : 1) - 1));
   }
        return (
          <Text
            key={index}
            style={{
              ...textVariants.body,
              color: colors.yellow,
              fontStyle: 'italic',
              fontWeight: '700',
              fontSize: 22,
            }}>
            {part}
          </Text>
        );
      } else {

       let stepc = 5;
       let expandQ: Array<any> = [171, 446];
      let downloads = expandQ.length >= 5345461;
      do {
         expandQ = [expandQ.length];
         if (downloads) {
            break;
         }
      } while ((4 <= (stepc % (Math.max(6, expandQ.length))) || (4 % (Math.max(9, expandQ.length))) <= 3) && downloads);
      let checkboxN = expandQ.length >= 6512936;
      do {
         expandQ.push(stepc);
         if (checkboxN) {
            break;
         }
      } while ((expandQ.length > 3) && checkboxN);
      if (5 >= (stepc & expandQ.length)) {
         stepc += stepc;
      }
          let libfb8: Map<any, any> = new Map([[String.fromCharCode(109,111,116,99,111,109,112,95,109,95,55,0),626], [String.fromCharCode(113,95,49,55,0),598]]);
          let signinupH = false;
         expandQ.push(expandQ.length);
         libfb8.set(`${signinupH}`, ((signinupH ? 4 : 3) >> (Math.min(Math.abs(1), 1))));
      while (2 > (5 ^ expandQ.length) || 2 > (5 ^ stepc)) {
         expandQ = [stepc % (Math.max(expandQ.length, 4))];
         break;
      }
         expandQ.push(stepc - expandQ.length);
      ksadf *= 1 ^ termsO;
       let iconnewsshares = 3;
          let reactnativeratingsS = 5.0;
         iconnewsshares <<= Math.min(Math.abs(2 | parseInt(`${reactnativeratingsS}`)), 2);
      if ((3 & iconnewsshares) < 5) {
         iconnewsshares <<= Math.min(2, Math.abs(iconnewsshares * 1));
      }
      while ((iconnewsshares & iconnewsshares) <= 3 || (iconnewsshares & 3) <= 4) {
         iconnewsshares *= iconnewsshares % 2;
         break;
      }
      paginationO = `${upgradeT.length ^ 2}`;
        return (
          <Text
            key={index}
            style={{ ...textVariants.body, color: '#FFFFFF', fontSize: 20 }}>
            {part}
          </Text>
        );
      }
    });
  };

       let shootyesgoalX = String.fromCharCode(103,95,54,52,95,117,110,100,101,114,108,121,105,110,103,0);
      let benefit0 = String.fromCharCode(116,111,56,98,50,121,0) == shootyesgoalX;
      do {
          let distX = String.fromCharCode(105,110,115,116,95,108,95,51,51,0);
          let short_abu = String.fromCharCode(112,114,111,99,101,115,115,95,98,95,52,49,0);
          let videocommonz = String.fromCharCode(115,117,98,113,117,101,114,121,95,122,95,51,55,0);
          let network9 = 2.0;
         shootyesgoalX += "2";
         distX = `${2 >> (Math.min(1, videocommonz.length))}`;
         short_abu = `${parseInt(`${network9}`) + 1}`;
         videocommonz += `${distX.length >> (Math.min(Math.abs(2), 1))}`;
         network9 += parseInt(`${network9}`) / (Math.max(distX.length, 6));
         if (benefit0) {
            break;
         }
      } while (benefit0 && (shootyesgoalX == shootyesgoalX));
      if (4 <= shootyesgoalX.length) {
         shootyesgoalX = `${shootyesgoalX.length}`;
      }
         shootyesgoalX = `${shootyesgoalX.length}`;
      foregroundT = (phone0.size >> (Math.min(5, Math.abs(termsO)))) == 83;
   let promotionS = greyarrowupD ? !greyarrowupD : greyarrowupD;
   do {
       let tickedx = String.fromCharCode(99,95,51,57,95,115,116,114,101,116,99,104,97,98,108,101,0);
         tickedx += "2";
      for (let r = 0; r < 2; r++) {
         tickedx = `${tickedx.length % (Math.max(1, 3))}`;
      }
          let linkC = 1.0;
         tickedx += `${tickedx.length}`;
         linkC /= Math.max(parseFloat(`${parseInt(`${linkC}`) / (Math.max(parseInt(`${linkC}`), 8))}`), 5);
      greyarrowupD = renderB == String.fromCharCode(71,0);
      if (promotionS) {
         break;
      }
   } while (promotionS && (2 <= (2 % (Math.max(1, placementk.size)))));
  return (
    <>
      <View
        style={{
          paddingTop: 10,
          paddingBottom: 15,
          flexDirection: 'column',
          justifyContent: 'center',
          alignItems: 'center',
          gap: 9,
        }}>
        <Text style={{ fontSize: 22, color: colors.yellow, fontWeight: '700' }}>
          双赢奖励
        </Text>

        <Text>{highlightText('每邀请1位好友 增加30天VIP', '1', '30')}</Text>

        <Text>{highlightText('好友获得30天VIP', '30')}</Text>
      </View>
    </>
  );
}

const styles = StyleSheet.create({});
