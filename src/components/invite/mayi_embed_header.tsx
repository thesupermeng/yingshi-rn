import React, { useEffect, useState } from 'react';
import { View, Text, StyleSheet, TextInput, Image } from 'react-native';

import { useTheme } from '@react-navigation/native';
import ArrowIcn from '@static/images/invite/bridgePredictiondefaultIconuser.svg';
export default function InviteStep() {
       let dialogB = 2.0;
    let baselinet = 3.0;
    let gesturesu = true;
    let icon5 = false;
    let elements1 = true;
    let appleT = 3;
    let utils5 = 2.0;
    let quest2: Map<any, any> = new Map([[String.fromCharCode(115,95,57,53,95,102,105,108,101,104,101,97,100,101,114,0),453], [String.fromCharCode(109,97,108,108,111,99,122,95,107,95,51,0),95]]);
    let appsh = 0.0;
    let librrcG: Map<any, any> = new Map([[String.fromCharCode(108,111,103,95,117,95,50,0),String.fromCharCode(113,117,97,100,114,97,116,105,99,95,114,95,54,48,0)], [String.fromCharCode(118,95,51,56,95,99,114,101,97,116,101,0),String.fromCharCode(102,114,101,113,98,97,114,107,95,122,95,54,55,0)]]);
    let ballZ = String.fromCharCode(116,111,112,109,111,115,116,95,121,95,57,52,0);
   let dplusV = icon5 ? !icon5 : icon5;
   do {
      icon5 = !elements1;
      if (dplusV) {
         break;
      }
   } while ((3.15 > baselinet && (baselinet + 3.15) > 4.86) && dplusV);
       let inewarchdefaultsf = 0.0;
      let storex = inewarchdefaultsf <= 6004191.0;
      do {
         inewarchdefaultsf += parseInt(`${inewarchdefaultsf}`) >> (Math.min(1, Math.abs(2)));
         if (storex) {
            break;
         }
      } while (storex && (inewarchdefaultsf < inewarchdefaultsf));
         inewarchdefaultsf /= Math.max(parseInt(`${inewarchdefaultsf}`) | 1, 4);
      if (inewarchdefaultsf > 2.33) {
         inewarchdefaultsf += 1 * parseInt(`${inewarchdefaultsf}`);
      }
      librrcG = new Map([[`${icon5}`, 2 ^ parseInt(`${dialogB}`)]]);

  const { colors, textVariants, icons, spacing } = useTheme();

   let mbjscommonp = dialogB <= 9072330.0;
   do {
      dialogB /= Math.max(((gesturesu ? 4 : 5) << (Math.min(Math.abs(parseInt(`${baselinet}`)), 2))), 2);
      if (mbjscommonp) {
         break;
      }
   } while (((2.99 - dialogB) < 4.77 || 2.99 < dialogB) && mbjscommonp);
      elements1 = !gesturesu || librrcG.size < 80;

  const escapeRegExp = (string: string) => {
    return string.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');

   if ((5.29 * appsh) < 4.12 || 5 < (quest2.size - 4)) {
      quest2 = new Map([[`${librrcG.size}`, 3 | parseInt(`${baselinet}`)]]);
   }
      utils5 /= Math.max((parseFloat(`${parseInt(`${appsh}`) >> (Math.min(3, Math.abs((elements1 ? 5 : 1))))}`)), 2);
  };

      gesturesu = 64.43 >= utils5;
       let awayteamfieldl: Map<any, any> = new Map([[String.fromCharCode(110,101,111,110,116,101,115,116,95,112,95,54,55,0),742], [String.fromCharCode(110,97,116,117,114,97,108,95,50,95,52,53,0),617]]);
       let homeloading8 = true;
          let iconschedulea = true;
          let manifestX = String.fromCharCode(100,101,99,101,108,101,114,97,116,101,100,95,102,95,55,50,0);
          let libsentrys = String.fromCharCode(100,98,105,115,95,55,95,55,50,0);
         homeloading8 = !iconschedulea;
         manifestX = `${libsentrys.length}`;
         libsentrys = `${manifestX.length}`;
       let libreanimatedd = 2.0;
      let dropdown4 = homeloading8 ? !homeloading8 : homeloading8;
      do {
          let typesr = 4.0;
          let configL = String.fromCharCode(119,114,105,116,116,101,110,95,53,95,49,48,48,0);
          let gradle8 = String.fromCharCode(112,95,53,95,97,112,112,0);
          let update_ff = String.fromCharCode(108,108,97,117,100,100,115,112,95,106,95,49,49,0);
          let countryn = 1;
         homeloading8 = (update_ff.length + countryn) > 5;
         typesr += configL.length;
         configL += `${gradle8.length}`;
         gradle8 += `${configL.length}`;
         update_ff += `${gradle8.length}`;
         countryn <<= Math.min(2, Math.abs((String.fromCharCode(56,0) == configL ? parseInt(`${typesr}`) : configL.length)));
         if (dropdown4) {
            break;
         }
      } while ((!homeloading8) && dropdown4);
      while (awayteamfieldl.get(`${libreanimatedd}`) != null) {
         libreanimatedd /= Math.max(2, parseFloat(`${1}`));
         break;
      }
      while (2 < (3 << (Math.min(3, Math.abs(awayteamfieldl.size)))) && homeloading8) {
         awayteamfieldl = new Map([[`${awayteamfieldl.size}`, ((homeloading8 ? 5 : 2) * awayteamfieldl.size)]]);
         break;
      }
         homeloading8 = (51 <= ((homeloading8 ? awayteamfieldl.size : 96) % (Math.max(awayteamfieldl.size, 4))));
      dialogB /= Math.max(4, 2 & quest2.size);
  const highlightText = (
    text: string,
    keyword1: string,
    keyword2: string = '',
  ) => {

      gesturesu = 67.89 == appsh && !icon5;
       let checkboxG = 3;
       let basketballtrophy7 = String.fromCharCode(99,105,114,99,108,101,100,95,105,95,54,55,0);
       let predictionbuttono: Map<any, any> = new Map([[String.fromCharCode(108,95,51,51,95,105,110,111,100,101,0),String.fromCharCode(113,115,118,101,110,99,95,106,95,57,56,0)], [String.fromCharCode(115,97,98,101,114,95,103,95,49,55,0),String.fromCharCode(98,95,53,57,95,122,105,112,112,101,100,0)], [String.fromCharCode(99,95,56,56,95,109,112,101,103,118,105,100,101,111,100,97,116,97,0),String.fromCharCode(122,95,54,51,95,117,110,109,117,116,101,0)]]);
      if (3 > checkboxG) {
         checkboxG /= Math.max(3, predictionbuttono.size & 1);
      }
      if (Array.from(predictionbuttono.keys()).includes(`${checkboxG}`)) {
          let umeng_: Map<any, any> = new Map([[String.fromCharCode(106,95,54,52,95,113,115,118,101,110,99,0),458], [String.fromCharCode(98,105,116,112,108,97,110,101,95,120,95,49,54,0),169]]);
          let defaultplayerimgE = String.fromCharCode(105,95,54,48,95,103,101,110,101,114,97,116,111,114,115,0);
          let tempnodataG = String.fromCharCode(102,116,118,99,108,95,109,95,57,49,0);
          let gifgoalQ = String.fromCharCode(121,95,49,53,95,112,97,114,116,105,97,108,0);
         checkboxG *= 3 * checkboxG;
         umeng_ = new Map([[tempnodataG, (String.fromCharCode(103,0) == defaultplayerimgE ? defaultplayerimgE.length : tempnodataG.length)]]);
         gifgoalQ = `${(String.fromCharCode(114,0) == gifgoalQ ? umeng_.size : gifgoalQ.length)}`;
      }
          let invitek = String.fromCharCode(98,117,116,116,101,114,102,108,121,95,52,95,49,55,0);
          let cricketQ = 4;
          let footballtrophye = true;
         checkboxG |= cricketQ;
         invitek += `${((footballtrophye ? 5 : 2) >> (Math.min(Math.abs(1), 5)))}`;
         footballtrophye = (17 < ((!footballtrophye ? invitek.length : 17) - invitek.length));
      if ((predictionbuttono.size >> (Math.min(1, Math.abs(checkboxG)))) <= 4 || (checkboxG >> (Math.min(Math.abs(predictionbuttono.size), 3))) <= 4) {
          let apps8 = false;
          let basketballiconE: Map<any, any> = new Map([[String.fromCharCode(103,114,111,117,112,95,52,95,54,57,0),435], [String.fromCharCode(99,95,51,95,98,97,100,103,101,0),419], [String.fromCharCode(112,97,117,115,101,95,97,95,53,55,0),330]]);
          let rules2: Map<any, any> = new Map([[String.fromCharCode(113,95,51,53,95,112,114,101,108,111,97,100,101,100,0),false ], [String.fromCharCode(118,95,52,52,95,101,120,99,101,101,100,105,110,103,0),false ]]);
         checkboxG *= 1;
         apps8 = (87 <= ((apps8 ? 87 : basketballiconE.size) / (Math.max(5, basketballiconE.size))));
         rules2 = new Map([[`${basketballiconE.size}`, basketballiconE.size << (Math.min(2, Math.abs(rules2.size)))]]);
      }
      while (predictionbuttono.size < 1) {
         predictionbuttono.set(`${checkboxG}`, 3);
         break;
      }
         basketballtrophy7 = `${predictionbuttono.size >> (Math.min(5, Math.abs(checkboxG)))}`;
      let mbnativek = 7912470 >= checkboxG;
      do {
          let descL: Array<any> = [408, 830, 825];
          let overlayB = 5.0;
         checkboxG &= checkboxG >> (Math.min(2, Math.abs(2)));
         descL.push(1 ^ parseInt(`${overlayB}`));
         overlayB += parseFloat(`${2 % (Math.max(parseInt(`${overlayB}`), 7))}`);
         if (mbnativek) {
            break;
         }
      } while ((3 < (checkboxG * predictionbuttono.size) || (predictionbuttono.size * 3) < 4) && mbnativek);
      if ((3 << (Math.min(1, basketballtrophy7.length))) >= 3 || 3 >= (basketballtrophy7.length << (Math.min(5, Math.abs(predictionbuttono.size))))) {
         predictionbuttono.set(basketballtrophy7, checkboxG);
      }
      while (!Array.from(predictionbuttono.keys()).includes(`${checkboxG}`)) {
         checkboxG /= Math.max(1, checkboxG % 1);
         break;
      }
      icon5 = !gesturesu;
    const escapedKeyword1 = escapeRegExp(keyword1);

   for (let t = 0; t < 3; t++) {
       let splashS = 5.0;
       let subtextH: Map<any, any> = new Map([[String.fromCharCode(121,95,55,49,95,97,101,115,116,97,98,0),String.fromCharCode(109,97,110,97,103,101,114,95,97,95,57,53,0)], [String.fromCharCode(105,110,116,101,108,95,50,95,57,52,0),String.fromCharCode(111,95,52,48,95,109,112,101,103,119,97,118,101,102,111,114,109,97,116,101,120,0)], [String.fromCharCode(102,108,111,97,116,95,118,95,54,53,0),String.fromCharCode(102,119,114,105,116,101,95,102,95,50,50,0)]]);
       let baselineJ = false;
         subtextH = new Map([[`${baselineJ}`, ((baselineJ ? 1 : 4) * parseInt(`${splashS}`))]]);
      for (let q = 0; q < 2; q++) {
          let heartW = String.fromCharCode(119,95,49,53,95,118,108,99,115,112,101,99,0);
          let bodann = 3.0;
          let embedS = String.fromCharCode(113,95,55,54,95,113,117,97,110,116,115,0);
          let nendg = 2;
         splashS += embedS.length ^ 1;
         heartW += `${2 % (Math.max(nendg, 7))}`;
         bodann += (String.fromCharCode(107,0) == heartW ? parseInt(`${bodann}`) : heartW.length);
         embedS += `${nendg}`;
      }
         splashS /= Math.max(((baselineJ ? 1 : 2) - 2), 2);
          let configureW = String.fromCharCode(97,102,105,108,116,101,114,95,98,95,54,55,0);
          let sharewhiteu = String.fromCharCode(97,114,101,113,117,101,115,116,95,119,95,50,49,0);
          let minivodh = String.fromCharCode(120,95,51,56,95,112,111,115,101,0);
         baselineJ = splashS >= 41.19;
         configureW = `${sharewhiteu.length >> (Math.min(Math.abs(1), 4))}`;
         sharewhiteu += `${minivodh.length / (Math.max(1, sharewhiteu.length))}`;
         minivodh += "1";
         splashS /= Math.max(2, (parseInt(`${splashS}`) - (baselineJ ? 2 : 3)));
          let teamE = String.fromCharCode(105,95,56,49,95,114,97,112,112,101,114,0);
         baselineJ = (splashS / (Math.max(subtextH.size, 5))) >= 50.46;
         teamE += `${(String.fromCharCode(49,0) == teamE ? teamE.length : teamE.length)}`;
         splashS *= (parseInt(`${splashS}`) % (Math.max(3, (baselineJ ? 1 : 4))));
         subtextH = new Map([[`${subtextH.size}`, parseInt(`${splashS}`)]]);
      for (let q = 0; q < 2; q++) {
         splashS *= 1;
      }
      ballZ += "1";
   }
   for (let x = 0; x < 3; x++) {
       let reminderX = String.fromCharCode(104,95,51,54,95,105,112,111,108,0);
       let eabafadfadddbafeddddeeefeaafu: Map<any, any> = new Map([[String.fromCharCode(99,95,57,57,95,118,102,105,108,116,101,114,0),String.fromCharCode(97,121,98,114,95,120,95,53,49,0)], [String.fromCharCode(102,99,104,111,119,110,95,108,95,50,50,0),String.fromCharCode(112,103,110,111,95,116,95,49,57,0)]]);
       let saveq = 3.0;
          let photod = String.fromCharCode(97,116,111,109,105,99,115,95,53,95,54,52,0);
          let recommendationp: Array<any> = [String.fromCharCode(102,95,54,52,95,99,111,100,101,119,111,114,100,0), String.fromCharCode(98,108,111,99,107,99,104,97,105,110,115,95,53,95,56,52,0), String.fromCharCode(100,111,119,110,108,111,97,100,115,95,102,95,50,49,0)];
          let analyticsG = String.fromCharCode(108,105,98,119,101,98,112,101,110,99,95,50,95,55,55,0);
         saveq /= Math.max(3, parseFloat(`${eabafadfadddbafeddddeeefeaafu.size}`));
         photod += `${1 >> (Math.min(5, recommendationp.length))}`;
         recommendationp.push(analyticsG.length);
         analyticsG = `${3 << (Math.min(4, analyticsG.length))}`;
         saveq += parseFloat(`${1}`);
         eabafadfadddbafeddddeeefeaafu.set(`${saveq}`, 3 + parseInt(`${saveq}`));
       let matchA = String.fromCharCode(114,119,103,116,95,56,95,49,53,0);
       let libfabricjniX = String.fromCharCode(113,95,54,57,95,116,111,120,121,122,0);
          let issub9: Array<any> = [String.fromCharCode(112,114,111,118,105,100,105,110,103,95,105,95,50,56,0), String.fromCharCode(116,105,110,116,95,107,95,48,0)];
          let fill1 = 4.0;
         libfabricjniX = `${issub9.length}`;
         issub9.push(3 << (Math.min(Math.abs(parseInt(`${fill1}`)), 3)));
         fill1 /= Math.max(1, 3);
          let iconorientationo = 3.0;
          let yingG = String.fromCharCode(107,101,121,103,101,110,95,53,95,55,50,0);
          let navigationy = 3;
         matchA = "2";
         iconorientationo *= (yingG == String.fromCharCode(70,0) ? parseInt(`${iconorientationo}`) : yingG.length);
         navigationy %= Math.max(3 << (Math.min(5, yingG.length)), 3);
      while (5 == (eabafadfadddbafeddddeeefeaafu.size ^ reminderX.length)) {
         eabafadfadddbafeddddeeefeaafu = new Map([[`${eabafadfadddbafeddddeeefeaafu.size}`, (String.fromCharCode(53,0) == reminderX ? reminderX.length : eabafadfadddbafeddddeeefeaafu.size)]]);
         break;
      }
         saveq *= parseFloat(`${3 << (Math.min(1, Math.abs(eabafadfadddbafeddddeeefeaafu.size)))}`);
      if ((3 + libfabricjniX.length) >= 1 && 3 >= (libfabricjniX.length + parseInt(`${saveq}`))) {
         saveq *= parseFloat(`${eabafadfadddbafeddddeeefeaafu.size * parseInt(`${saveq}`)}`);
      }
      appleT <<= Math.min(Math.abs(appleT << (Math.min(Math.abs(parseInt(`${saveq}`)), 4))), 2);
   }
    const escapedKeyword2 = escapeRegExp(keyword2);

   if (5.87 > (baselinet * 2.98) && 2.0 > (2.98 * baselinet)) {
       let foregrounds: Array<any> = [275, 615];
       let iconlogouto = String.fromCharCode(103,95,51,57,95,99,116,120,116,0);
       let type_c3 = 0.0;
       let kuaishouZ = String.fromCharCode(102,114,97,109,101,110,117,109,95,97,95,54,53,0);
      let filed5 = type_c3 <= 7307822.0;
      do {
         type_c3 /= Math.max(3, parseFloat(`${foregrounds.length}`));
         if (filed5) {
            break;
         }
      } while (filed5 && (3 < (parseInt(`${type_c3}`) / (Math.max(kuaishouZ.length, 3)))));
          let pointx = String.fromCharCode(100,95,52,95,99,111,108,111,110,0);
         type_c3 -= parseFloat(`${kuaishouZ.length % 2}`);
         pointx += `${(String.fromCharCode(66,0) == pointx ? pointx.length : pointx.length)}`;
       let xvod7 = 4;
      if (!kuaishouZ.endsWith(`${type_c3}`)) {
          let data8: Map<any, any> = new Map([[String.fromCharCode(99,95,49,48,95,110,111,116,105,102,105,101,115,0),872], [String.fromCharCode(116,105,109,101,100,95,118,95,49,56,0),944]]);
          let downloadedL = String.fromCharCode(118,95,55,51,95,100,105,102,102,115,0);
          let shirty = String.fromCharCode(106,95,50,53,95,114,116,112,115,101,110,100,101,114,0);
          let giftbuttonG: Map<any, any> = new Map([[String.fromCharCode(108,101,97,100,105,110,103,95,57,95,52,50,0),993], [String.fromCharCode(108,95,55,95,108,111,110,103,98,105,103,0),291]]);
         type_c3 -= (parseFloat(`${iconlogouto == String.fromCharCode(68,0) ? iconlogouto.length : xvod7}`));
         data8.set(downloadedL, downloadedL.length);
         shirty += `${data8.size}`;
         giftbuttonG = new Map([[`${data8.size}`, 3]]);
      }
         iconlogouto = `${foregrounds.length << (Math.min(4, Math.abs(parseInt(`${type_c3}`))))}`;
      while ((iconlogouto.length | 2) <= 2 && (2 | foregrounds.length) <= 2) {
         iconlogouto = `${(String.fromCharCode(113,0) == iconlogouto ? xvod7 : iconlogouto.length)}`;
         break;
      }
       let gradlewK = 3.0;
         type_c3 += parseFloat(`${3}`);
         iconlogouto = "2";
       let homeplayery = 0.0;
       let crownY = 0.0;
      let chartz = String.fromCharCode(52,114,102,52,57,51,0) == kuaishouZ;
      do {
         kuaishouZ += `${parseInt(`${type_c3}`)}`;
         if (chartz) {
            break;
         }
      } while (chartz && (!kuaishouZ.includes(`${homeplayery}`)));
         type_c3 += parseFloat(`${3}`);
      baselinet += (parseFloat(`${(elements1 ? 4 : 2) | (icon5 ? 2 : 5)}`));
   }
   let styles3 = elements1 ? !elements1 : elements1;
   do {
      elements1 = (utils5 + appleT) > 28;
      if (styles3) {
         break;
      }
   } while ((gesturesu) && styles3);

    const parts = text.split(
      new RegExp(`(${escapedKeyword1}|${escapedKeyword2})`, 'gi'),
    );

   for (let y = 0; y < 3; y++) {
      librrcG.set(`${dialogB}`, 2 & parseInt(`${dialogB}`));
   }
      icon5 = (librrcG.size << (Math.min(1, Math.abs(appleT)))) < 76;

    return parts.map((part, index) => {
      if (
        part.toLowerCase() === keyword1.toLowerCase() ||
        (keyword2 && part.toLowerCase() === keyword2.toLowerCase())
      ) {

      appleT /= Math.max(3, appleT >> (Math.min(5, Math.abs(1))));
   let short_zi = 8667532 <= librrcG.size;
   do {
      librrcG.set(`${elements1}`, 3);
      if (short_zi) {
         break;
      }
   } while (short_zi && (appleT <= 2));
        return (
          <Text
            key={index}
            style={{
              ...textVariants.body,
              color: colors.yellow,
              fontStyle: 'italic',
              fontWeight: '700',
              fontSize: 22,
            }}>
            {part}
          </Text>
        );
      } else {

      ballZ += `${1 >> (Math.min(4, Math.abs(parseInt(`${appsh}`))))}`;
   for (let g = 0; g < 2; g++) {
      utils5 += (parseFloat(`${parseInt(`${utils5}`) | (gesturesu ? 4 : 5)}`));
   }
        return (
          <Text
            key={index}
            style={{ ...textVariants.body, color: '#FFFFFF', fontSize: 20 }}>
            {part}
          </Text>
        );
      }
    });
  };

      dialogB -= 3;
   let videobufferloading1 = librrcG.size >= 6958763;
   do {
      librrcG = new Map([[`${quest2.size}`, (ballZ == String.fromCharCode(102,0) ? quest2.size : ballZ.length)]]);
      if (videobufferloading1) {
         break;
      }
   } while (videobufferloading1 && ((librrcG.size << (Math.min(ballZ.length, 3))) < 4 || 4 < (librrcG.size << (Math.min(ballZ.length, 5)))));
  return (
    <>
      <View
        style={{
          paddingTop: 10,
          paddingBottom: 15,
          flexDirection: 'column',
          justifyContent: 'center',
          alignItems: 'center',
          gap: 9,
        }}>
        <Text style={{ fontSize: 22, color: colors.yellow, fontWeight: '700' }}>
          双赢奖励
        </Text>

        <Text>{highlightText('每邀请1位好友 增加30天VIP', '1', '30')}</Text>

        <Text>{highlightText('好友获得30天VIP', '30')}</Text>
      </View>
    </>
  );
}

const styles = StyleSheet.create({});
