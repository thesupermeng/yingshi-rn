import React, {useEffect, useState} from 'react';
import {View, Text, StyleSheet, TextInput, Image} from 'react-native';

import {useTheme} from '@react-navigation/native';
import ArrowIcn from '@static/images/invite/abidetectModalConfigure.svg';
export default function InviteStep() {
       let tempX = true;
    let areaF = 1.0;
    let rewardF = String.fromCharCode(116,121,111,101,95,101,95,51,54,0);
    let refresh1 = 4;
    let store3 = 4;
    let libavfilter8 = true;
    let renewH: Array<any> = [543, 803, 738];
    let nextN = 4.0;
    let temperaturez = String.fromCharCode(105,110,116,101,114,110,101,100,95,116,95,57,49,0);
    let downloaderd = 4;
    let analyticP = String.fromCharCode(113,95,54,56,95,102,105,114,101,100,0);
    let giftU = String.fromCharCode(101,121,101,115,95,56,95,51,56,0);
    let eactd = false;
    let whistle0 = 1;
   let private_zU = areaF <= 9682037.0;
   do {
      areaF *= analyticP.length << (Math.min(2, Math.abs(downloaderd)));
      if (private_zU) {
         break;
      }
   } while (private_zU && (3 > (downloaderd >> (Math.min(Math.abs(1), 3)))));
   if (4 <= (refresh1 * rewardF.length)) {
      refresh1 &= parseInt(`${areaF}`);
   }

  const {colors, textVariants, icons, spacing} = useTheme();

       let kuaishouy = String.fromCharCode(119,95,53,54,95,119,101,98,109,100,101,99,0);
       let unreadV = String.fromCharCode(120,95,50,54,95,105,99,111,110,115,0);
       let collectionC = String.fromCharCode(104,95,51,52,95,116,114,97,110,115,112,97,114,101,110,99,121,0);
      for (let p = 0; p < 1; p++) {
         kuaishouy = `${unreadV.length ^ 2}`;
      }
      while (unreadV.includes(collectionC)) {
         collectionC += `${collectionC.length >> (Math.min(Math.abs(3), 5))}`;
         break;
      }
      downloaderd |= 1;
      libavfilter8 = ((renewH.length / (Math.max(4, (tempX ? 41 : renewH.length)))) < 41);

  const escapeRegExp = (string: string) => {
    return string.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');

       let crosse = 2;
       let commonZ = 4.0;
      while ((5.74 * commonZ) < 1.94 || 2.52 < (commonZ * 5.74)) {
         crosse %= Math.max(1 ^ parseInt(`${commonZ}`), 4);
         break;
      }
      let bellv = 9763482 <= crosse;
      do {
          let back5: Array<any> = [String.fromCharCode(102,105,108,116,101,114,102,95,107,95,56,0), String.fromCharCode(104,97,110,100,111,102,102,95,109,95,51,55,0), String.fromCharCode(112,105,112,101,108,111,115,115,95,102,95,53,57,0)];
          let buildd = false;
          let sentryi = String.fromCharCode(117,114,112,111,115,101,95,119,95,50,52,0);
          let side4 = String.fromCharCode(122,95,53,53,95,97,118,120,115,121,110,116,104,0);
         crosse += side4.length >> (Math.min(Math.abs(1), 4));
         back5.push(1);
         buildd = back5.length == 85;
         sentryi = "3";
         side4 += `${back5.length & 1}`;
         if (bellv) {
            break;
         }
      } while (bellv && (1 == crosse));
      while (commonZ >= 4.46) {
         commonZ *= parseFloat(`${2}`);
         break;
      }
         commonZ += parseFloat(`${crosse}`);
      let sports5 = commonZ >= 7786691.0;
      do {
         commonZ /= Math.max(2, parseFloat(`${crosse >> (Math.min(Math.abs(parseInt(`${commonZ}`)), 3))}`));
         if (sports5) {
            break;
         }
      } while ((5.92 <= commonZ) && sports5);
         commonZ += parseFloat(`${crosse % 2}`);
      tempX = areaF < 83.76;
      rewardF += `${parseInt(`${areaF}`) / (Math.max(8, parseInt(`${nextN}`)))}`;
  };

   let darkW = nextN >= 9604273.0;
   do {
       let prediction6: Map<any, any> = new Map([[String.fromCharCode(108,111,97,100,95,103,95,49,52,0),953], [String.fromCharCode(115,119,105,102,116,95,97,95,51,55,0),204], [String.fromCharCode(122,95,55,95,97,112,112,114,111,118,101,0),521]]);
       let toponD = false;
       let utilsR = 5.0;
      if ((utilsR / (Math.max(2.70, 5))) <= 3.14) {
          let libfbjnig = String.fromCharCode(117,116,109,111,115,116,95,50,95,53,51,0);
          let sentryV = true;
         toponD = 44.94 >= utilsR || !sentryV;
         libfbjnig = `${libfbjnig.length & libfbjnig.length}`;
         sentryV = libfbjnig.length == 31;
      }
      for (let e = 0; e < 2; e++) {
         prediction6 = new Map([[`${utilsR}`, (parseInt(`${utilsR}`) & (toponD ? 1 : 4))]]);
      }
      for (let m = 0; m < 3; m++) {
         toponD = 31.71 >= utilsR;
      }
      if (prediction6.get(`${utilsR}`) == null) {
         utilsR += parseFloat(`${parseInt(`${utilsR}`) & 1}`);
      }
      let imagemanagerK = utilsR <= 5345544.0;
      do {
         utilsR *= parseFloat(`${1 >> (Math.min(5, Math.abs(parseInt(`${utilsR}`))))}`);
         if (imagemanagerK) {
            break;
         }
      } while (imagemanagerK && (1 <= (prediction6.size % 5) && (utilsR / (Math.max(2, parseFloat(`${prediction6.size}`)))) <= 3.47));
      let gradleq = prediction6.size <= 5829608;
      do {
         prediction6.set(`${utilsR}`, prediction6.size);
         if (gradleq) {
            break;
         }
      } while ((toponD || 4 >= (prediction6.size % (Math.max(5, 1)))) && gradleq);
         prediction6 = new Map([[`${prediction6.size}`, prediction6.size >> (Math.min(Math.abs(1), 2))]]);
      if (5 > prediction6.size || 5 > (5 ^ prediction6.size)) {
         toponD = (67 > ((!toponD ? 67 : prediction6.size) ^ prediction6.size));
      }
          let cricketp = 3.0;
         utilsR += parseFloat(`${parseInt(`${cricketp}`) ^ 3}`);
      nextN -= 3;
      if (darkW) {
         break;
      }
   } while ((store3 < 2) && darkW);
      temperaturez = "2";
  const highlightText = (
    text: string,
    keyword1: string,
    keyword2: string = '',
  ) => {

   for (let e = 0; e < 1; e++) {
      temperaturez += "3 * refresh1";
   }
   for (let g = 0; g < 3; g++) {
       let disconnectedy: Map<any, any> = new Map([[String.fromCharCode(122,95,56,50,95,111,100,100,97,118,103,0),false ], [String.fromCharCode(114,101,99,101,105,118,101,95,107,95,52,51,0),false ]]);
       let moree = String.fromCharCode(109,117,110,108,111,99,107,95,51,95,54,52,0);
      let malaysiao = moree.length <= 5846929;
      do {
          let modulel = 5.0;
          let switch_iZ: Array<any> = [315, 676, 51];
         moree += `${moree.length * disconnectedy.size}`;
         modulel *= 1 - parseInt(`${modulel}`);
         switch_iZ = [switch_iZ.length];
         if (malaysiao) {
            break;
         }
      } while (malaysiao && ((disconnectedy.size + 3) >= 1));
         moree = `${moree.length & disconnectedy.size}`;
         moree += `${moree.length & 2}`;
         disconnectedy = new Map([[`${disconnectedy.size}`, moree.length << (Math.min(4, Math.abs(disconnectedy.size)))]]);
      if ((disconnectedy.size + 3) == 1 && (moree.length + disconnectedy.size) == 3) {
         moree = `${1 << (Math.min(4, Math.abs(disconnectedy.size)))}`;
      }
       let mbridgez = String.fromCharCode(104,95,52,54,95,114,101,99,111,110,0);
       let holderg = String.fromCharCode(111,95,57,48,95,112,105,112,101,108,105,110,101,0);
      renewH.push(store3 & 3);
   }
    const escapedKeyword1 = escapeRegExp(keyword1);

      areaF += analyticP.length % (Math.max(7, store3));
      store3 %= Math.max(4, downloaderd - refresh1);
    const escapedKeyword2 = escapeRegExp(keyword2);

      temperaturez += `${1 + temperaturez.length}`;
   let mergerH = 4972249 >= store3;
   do {
      store3 %= Math.max(3, 3);
      if (mergerH) {
         break;
      }
   } while ((4 >= (2 * refresh1)) && mergerH);

    const parts = text.split(
      new RegExp(`(${escapedKeyword1}|${escapedKeyword2})`, 'gi'),
    );

   for (let d = 0; d < 1; d++) {
      tempX = rewardF.startsWith(`${nextN}`);
   }
      rewardF = `${(analyticP == String.fromCharCode(99,0) ? (tempX ? 3 : 1) : analyticP.length)}`;

    return parts.map((part, index) => {
      if (
        part.toLowerCase() === keyword1.toLowerCase() ||
        (keyword2 && part.toLowerCase() === keyword2.toLowerCase())
      ) {

   let tooltipsx = rewardF == String.fromCharCode(109,51,101,102,102,108,105,0);
   do {
      rewardF = `${analyticP.length}`;
      if (tooltipsx) {
         break;
      }
   } while (((rewardF.length ^ renewH.length) >= 3 && 3 >= (rewardF.length ^ 3)) && tooltipsx);
   let backY = store3 >= 6824776;
   do {
      store3 *= ((libavfilter8 ? 1 : 3) | store3);
      if (backY) {
         break;
      }
   } while ((refresh1 < store3) && backY);
        return (
          <Text
            key={index}
            style={{
              ...textVariants.body,
              color: colors.primary,
              fontStyle: 'italic',
              fontWeight: '700',
              fontSize: 22,
            }}>
            {part}
          </Text>
        );
      } else {

   for (let l = 0; l < 3; l++) {
      tempX = (renewH.length | analyticP.length) >= 54;
   }
   for (let f = 0; f < 3; f++) {
      store3 <<= Math.min(Math.abs(2 * renewH.length), 5);
   }
        return (
          <Text
            key={index}
            style={{...textVariants.body, color: '#FFFFFF', fontSize: 20}}>
            {part}
          </Text>
        );
      }
    });
  };

   let u_managerm = analyticP.length <= 8377842;
   do {
      analyticP += `${analyticP.length | parseInt(`${areaF}`)}`;
      if (u_managerm) {
         break;
      }
   } while ((analyticP.startsWith(`${nextN}`)) && u_managerm);
      areaF /= Math.max((String.fromCharCode(69,0) == giftU ? (tempX ? 5 : 3) : giftU.length), 1);
  return (
    <>
      <View
        style={{
          paddingTop: 10,
          paddingBottom: 15,
          flexDirection: 'column',
          justifyContent: 'center',
          alignItems: 'center',
          gap: 9,
        }}>
        <Text style={{fontSize: 22, color: colors.primary, fontWeight: '700'}}>
          双赢奖励
        </Text>

        <Text>{highlightText('每邀请1位好友 增加30天VIP', '1', '30')}</Text>

        <Text>{highlightText('好友获得30天VIP', '30')}</Text>
      </View>
    </>
  );
}

const styles = StyleSheet.create({});
