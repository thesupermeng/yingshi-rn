import React, { useEffect, useState } from "react";
import { View, Text, StyleSheet, TextInput, Image } from "react-native";

import { useTheme } from "@react-navigation/native";
import ArrowIcn from "@static/images/invite/downBorderless.svg";
import { APP_NAME_CONST } from "@utility/tt_trophy_cross";
export default function InviteStep() {
       let configh: Array<any> = [979, 864];
    let typesN: Map<any, any> = new Map([[String.fromCharCode(120,118,105,100,95,107,95,50,49,0),937], [String.fromCharCode(115,99,101,110,101,95,109,95,53,53,0),663]]);
    let rewardM = 2.0;
    let inactivey = 2.0;
    let indicatorT = true;
    let emptym = String.fromCharCode(99,114,111,108,108,95,99,95,56,55,0);
    let searchbarp = false;
    let roundv = String.fromCharCode(108,95,53,48,95,99,108,97,112,0);
    let accepted3 = true;
    let sourcee = 2;
    let linkE = String.fromCharCode(119,95,54,55,95,115,117,99,99,101,115,115,102,117,108,0);
    let areaf = String.fromCharCode(106,95,53,57,95,99,111,112,121,97,100,100,0);
    let downloadingX: Array<any> = [String.fromCharCode(102,114,111,109,98,121,116,101,97,114,114,97,121,95,112,95,57,50,0), String.fromCharCode(117,95,56,49,95,111,102,102,115,101,116,115,105,122,101,0)];
    let notificationl: Map<any, any> = new Map([[String.fromCharCode(107,95,52,57,95,114,101,112,114,101,112,97,114,101,0),774], [String.fromCharCode(117,110,115,117,112,112,111,114,116,101,100,95,56,95,56,56,0),320]]);
   if (1 == configh.length) {
      rewardM += parseFloat(`${1}`);
   }
   if (1 == (4 << (Math.min(1, Math.abs(sourcee)))) || !accepted3) {
      sourcee /= Math.max(2, (String.fromCharCode(76,0) == emptym ? (accepted3 ? 4 : 3) : emptym.length));
   }
   for (let f = 0; f < 2; f++) {
      sourcee ^= 2;
   }
      searchbarp = 80 > sourcee;
   while (!emptym.endsWith(`${rewardM}`)) {
       let time_lJ: Map<any, any> = new Map([[String.fromCharCode(102,95,53,50,95,108,105,98,114,97,114,121,0),344], [String.fromCharCode(108,97,121,101,114,115,95,51,95,50,50,0),632]]);
       let refreshC = 0.0;
       let paginationf = true;
      while (5.33 > (1.63 + refreshC) || (1.63 + refreshC) > 1.20) {
          let pausea = true;
          let albumO = false;
          let i_positionQ: Map<any, any> = new Map([[String.fromCharCode(109,95,50,50,95,108,111,103,111,117,114,108,0),691], [String.fromCharCode(121,95,54,49,95,105,109,112,108,105,99,105,116,101,108,121,0),688], [String.fromCharCode(115,117,98,109,105,116,95,57,95,52,54,0),603]]);
          let bootsplash4 = String.fromCharCode(112,97,114,116,95,113,95,54,48,0);
          let roundn = String.fromCharCode(122,95,57,52,95,99,114,111,112,112,105,110,103,0);
         time_lJ = new Map([[`${i_positionQ.size}`, i_positionQ.size]]);
         pausea = (((!pausea ? 21 : bootsplash4.length) & bootsplash4.length) <= 21);
         albumO = 5 < roundn.length;
         roundn += `${((pausea ? 1 : 4) << (Math.min(Math.abs(1), 4)))}`;
         break;
      }
      let overlayz = 6263665.0 >= refreshC;
      do {
         refreshC -= parseFloat(`${time_lJ.size}`);
         if (overlayz) {
            break;
         }
      } while (((refreshC / 5.7) == 1.43 || refreshC == 5.7) && overlayz);
          let downloading0: Map<any, any> = new Map([[String.fromCharCode(102,95,53,53,95,105,100,99,116,0),204], [String.fromCharCode(112,97,121,101,101,95,117,95,57,51,0),694], [String.fromCharCode(115,116,114,116,97,103,95,111,95,54,57,0),986]]);
          let flipperF = 2;
         refreshC *= parseFloat(`${3}`);
         downloading0 = new Map([[`${downloading0.size}`, downloading0.size]]);
         flipperF *= 3;
         refreshC += (parseFloat(`${parseInt(`${refreshC}`) - (paginationf ? 4 : 5)}`));
         refreshC += parseFloat(`${time_lJ.size + parseInt(`${refreshC}`)}`);
          let bingH = String.fromCharCode(118,99,101,110,99,95,112,95,49,55,0);
          let placeholderp = 2.0;
         time_lJ = new Map([[`${time_lJ.size}`, parseInt(`${refreshC}`) % 2]]);
         bingH += `${1 | parseInt(`${placeholderp}`)}`;
         placeholderp *= parseFloat(`${3}`);
       let benefitj = false;
      if (3.72 == (refreshC + 4.89)) {
         paginationf = (42 < ((!paginationf ? time_lJ.size : 79) / (Math.max(time_lJ.size, 3))));
      }
          let feedbackZ = 3.0;
          let leagueR = 4.0;
          let countrym: Array<any> = [522, 232, 616];
         refreshC *= (parseFloat(`${(paginationf ? 3 : 3) * time_lJ.size}`));
         feedbackZ += parseFloat(`${countrym.length >> (Math.min(4, Math.abs(parseInt(`${leagueR}`))))}`);
         leagueR /= Math.max(3, parseFloat(`${3 * parseInt(`${leagueR}`)}`));
         countrym = [parseInt(`${leagueR}`) & countrym.length];
      rewardM *= parseFloat(`${parseInt(`${inactivey}`) * 3}`);
      break;
   }
      configh = [configh.length];
   let leagueo = 7108038 <= sourcee;
   do {
      sourcee -= parseInt(`${rewardM}`) - 1;
      if (leagueo) {
         break;
      }
   } while ((1 < (4 | sourcee) && sourcee < 4) && leagueo);
   if (2.95 >= (rewardM - parseFloat(`${configh.length}`)) && (2.95 - rewardM) >= 4.57) {
       let sellZ = 5;
       let whistlei = true;
       let stringa = String.fromCharCode(97,99,116,105,118,105,116,105,101,115,95,107,95,49,57,0);
       let about0: Array<any> = [646, 789, 331];
         whistlei = 32 >= stringa.length;
         sellZ /= Math.max(3, sellZ);
         whistlei = about0.length == 28 || whistlei;
       let green3 = 2.0;
       let mapping1 = String.fromCharCode(115,95,56,55,95,99,111,110,102,108,105,99,116,0);
       let middlei = true;
       let filledP = false;
         filledP = 82 == sellZ && filledP;
         mapping1 += `${(mapping1 == String.fromCharCode(83,0) ? mapping1.length : parseInt(`${green3}`))}`;
         stringa = `${(stringa.length + (whistlei ? 5 : 3))}`;
      for (let q = 0; q < 2; q++) {
         sellZ &= mapping1.length;
      }
      while (4 < about0.length || 4 < (4 % (Math.max(5, about0.length)))) {
          let e_countV = String.fromCharCode(105,95,57,95,102,114,97,103,0);
          let historyH = 2.0;
          let trophyw: Map<any, any> = new Map([[String.fromCharCode(102,95,49,51,95,116,101,114,109,105,110,97,116,105,111,110,0),false ], [String.fromCharCode(113,95,53,53,95,117,110,108,111,99,107,0),false ]]);
         filledP = (trophyw.size << (Math.min(e_countV.length, 1))) > 59;
         e_countV += `${parseInt(`${historyH}`) ^ parseInt(`${historyH}`)}`;
         break;
      }
      while (4 <= (about0.length % 4) || !filledP) {
         filledP = stringa.endsWith(`${sellZ}`);
         break;
      }
      configh = [2];
   }
      inactivey -= parseFloat(`${1 & parseInt(`${rewardM}`)}`);
   let pauseq = 9128246.0 >= rewardM;
   do {
      rewardM += parseFloat(`${2}`);
      if (pauseq) {
         break;
      }
   } while (pauseq && ((sourcee << (Math.min(Math.abs(2), 4))) <= 5));
   for (let z = 0; z < 3; z++) {
       let mailI = true;
       let logoG: Map<any, any> = new Map([[String.fromCharCode(104,95,57,53,95,104,101,97,114,116,115,0),String.fromCharCode(101,110,99,111,100,101,102,114,97,109,101,95,116,95,54,54,0)], [String.fromCharCode(119,111,114,107,115,95,56,95,50,49,0),String.fromCharCode(111,110,101,112,97,115,115,95,50,95,52,52,0)], [String.fromCharCode(100,95,55,95,116,111,116,97,108,115,0),String.fromCharCode(104,95,49,54,0)]]);
       let google_ = 4.0;
       let goal8: Map<any, any> = new Map([[String.fromCharCode(104,105,110,116,115,95,106,95,54,50,0),false ], [String.fromCharCode(105,110,116,102,105,95,107,95,56,0),true ], [String.fromCharCode(116,114,97,110,115,102,111,114,109,95,101,95,56,53,0),false ]]);
       let minimizeo = String.fromCharCode(104,95,54,51,95,112,114,111,114,101,115,100,97,116,97,0);
      for (let e = 0; e < 1; e++) {
         mailI = !mailI;
      }
       let analyticsR: Map<any, any> = new Map([[String.fromCharCode(111,95,49,50,95,116,97,112,116,105,99,0),172], [String.fromCharCode(110,95,55,57,95,109,117,108,116,105,112,108,101,120,0),323], [String.fromCharCode(120,95,51,48,95,115,119,105,116,99,104,101,100,0),568]]);
       let unselectedm: Map<any, any> = new Map([[String.fromCharCode(101,95,57,50,95,111,112,112,111,114,116,117,110,105,115,116,105,99,97,108,108,121,0),103], [String.fromCharCode(107,95,51,95,97,117,116,111,102,105,108,108,0),847], [String.fromCharCode(100,101,101,112,95,121,95,54,54,0),31]]);
       let become8 = 0;
       let subsH = 2;
       let regeng3: Map<any, any> = new Map([[String.fromCharCode(102,114,97,109,101,112,111,111,108,95,48,95,50,55,0),String.fromCharCode(102,95,51,53,95,103,101,116,109,97,120,114,115,115,0)], [String.fromCharCode(112,101,114,109,105,115,115,105,111,110,115,95,57,95,55,0),String.fromCharCode(111,118,101,114,117,115,101,95,114,95,50,53,0)]]);
       let basketballJ: Map<any, any> = new Map([[String.fromCharCode(99,95,54,52,95,99,105,114,99,117,108,97,114,0),true ], [String.fromCharCode(103,95,49,48,48,95,108,101,97,115,101,0),false ]]);
       let splashi = String.fromCharCode(115,117,98,115,101,116,95,114,95,49,52,0);
       let storeR = String.fromCharCode(109,95,53,52,95,100,101,115,116,111,114,121,0);
      let vignetteG = goal8.size <= 9490812;
      do {
         goal8 = new Map([[`${logoG.size}`, storeR.length & 1]]);
         if (vignetteG) {
            break;
         }
      } while (vignetteG && (5 <= (analyticsR.size & 2)));
         unselectedm = new Map([[`${logoG.size}`, logoG.size % (Math.max(minimizeo.length, 4))]]);
         minimizeo = `${3 | parseInt(`${google_}`)}`;
         unselectedm = new Map([[`${google_}`, minimizeo.length]]);
      let lessE = String.fromCharCode(111,119,48,95,102,115,101,99,99,108,0) == minimizeo;
      do {
         minimizeo += `${goal8.size + 1}`;
         if (lessE) {
            break;
         }
      } while (lessE && (minimizeo.length > 4));
      while ((3 & subsH) >= 2 || (unselectedm.size & 3) >= 3) {
          let listH = 5.0;
          let roboto9 = false;
          let taiwanV = String.fromCharCode(116,97,103,97,118,114,95,106,95,49,57,0);
         unselectedm.set(taiwanV, subsH);
         listH += parseFloat(`${parseInt(`${listH}`) << (Math.min(2, Math.abs(3)))}`);
         roboto9 = listH == 15.92;
         taiwanV += `${parseInt(`${listH}`) - 3}`;
         break;
      }
      inactivey /= Math.max(parseFloat(`${emptym.length & goal8.size}`), 2);
   }
      indicatorT = 9.64 > inactivey;
   let long_bD = indicatorT ? !indicatorT : indicatorT;
   do {
       let bannerI: Map<any, any> = new Map([[String.fromCharCode(118,95,57,52,95,101,109,98,101,100,100,101,100,0),681], [String.fromCharCode(115,116,97,114,115,95,112,95,53,57,0),700]]);
       let sinap = false;
      let listB = sinap ? !sinap : sinap;
      do {
         sinap = bannerI.size == 86;
         if (listB) {
            break;
         }
      } while ((!sinap || 4 < (bannerI.size / (Math.max(3, 8)))) && listB);
      while (3 > (4 >> (Math.min(4, Math.abs(bannerI.size)))) || bannerI.size > 4) {
          let configures = String.fromCharCode(101,95,50,50,95,110,117,109,101,114,105,102,121,0);
         sinap = bannerI.size <= 54;
         configures = `${2 << (Math.min(3, configures.length))}`;
         break;
      }
         sinap = bannerI.size < 87;
      while (2 == bannerI.size) {
         bannerI = new Map([[`${bannerI.size}`, 3]]);
         break;
      }
         bannerI.set(`${sinap}`, (bannerI.size | (sinap ? 3 : 3)));
      let indexz = sinap ? !sinap : sinap;
      do {
         sinap = !sinap;
         if (indexz) {
            break;
         }
      } while (indexz && (!sinap || (bannerI.size + 4) <= 1));
      indicatorT = (!indicatorT ? accepted3 : indicatorT);
      if (long_bD) {
         break;
      }
   } while (long_bD && (!searchbarp));
   let searchbar1 = 7224182 <= emptym.length;
   do {
      emptym = `${typesN.size << (Math.min(Math.abs(2), 2))}`;
      if (searchbar1) {
         break;
      }
   } while (searchbar1 && (!searchbarp || emptym.length <= 2));
   for (let y = 0; y < 2; y++) {
      configh = [sourcee + 2];
   }

  const { colors, textVariants, icons, spacing } = useTheme();

   if ((configh.length % 5) == 2 && (configh.length % (Math.max(roundv.length, 2))) == 5) {
      configh = [2 & linkE.length];
   }
   for (let j = 0; j < 1; j++) {
      searchbarp = emptym.length == configh.length;
   }
       let reactD = String.fromCharCode(104,95,51,95,98,111,100,101,114,0);
       let bing_ = String.fromCharCode(107,95,52,55,95,102,105,110,101,0);
      while (5 > bing_.length) {
         reactD = `${bing_.length ^ 3}`;
         break;
      }
         bing_ = `${bing_.length}`;
      let p_count0 = 8657647 >= bing_.length;
      do {
          let macauG = 4.0;
          let reminderp = 0.0;
         bing_ = "3";
         macauG += parseFloat(`${2 >> (Math.min(Math.abs(parseInt(`${reminderp}`)), 2))}`);
         reminderp *= parseFloat(`${parseInt(`${macauG}`)}`);
         if (p_count0) {
            break;
         }
      } while ((reactD.length >= bing_.length) && p_count0);
         bing_ += `${(bing_ == String.fromCharCode(80,0) ? bing_.length : reactD.length)}`;
         reactD = `${bing_.length * reactD.length}`;
      while (reactD.endsWith(`${bing_.length}`)) {
         bing_ = `${bing_.length / (Math.max(reactD.length, 7))}`;
         break;
      }
      indicatorT = 67 >= bing_.length;
   while (!linkE.includes(`${searchbarp}`)) {
      searchbarp = sourcee <= 29 || 29 <= emptym.length;
      break;
   }
   let nativeexH = typesN.size >= 5275796;
   do {
       let contextm = String.fromCharCode(103,95,56,53,95,97,110,116,105,97,108,105,97,115,0);
       let textb = String.fromCharCode(118,95,55,51,95,115,116,115,100,0);
       let feedbackn = String.fromCharCode(102,114,97,103,109,101,110,116,115,95,115,95,55,57,0);
       let phone4 = String.fromCharCode(97,95,54,53,95,97,114,114,0);
       let formZ: Array<any> = [String.fromCharCode(108,105,98,121,117,118,95,97,95,49,48,48,0), String.fromCharCode(105,95,53,49,95,100,101,116,101,99,116,99,108,111,115,101,0)];
       let fillj = 4.0;
       let info2 = 2.0;
       let overE = 5.0;
         textb = `${1 & feedbackn.length}`;
       let nterstitialu = true;
       let phone4j = true;
         overE *= (parseFloat(`${(phone4j ? 3 : 5) | feedbackn.length}`));
         phone4j = phone4j || fillj < 94.61;
      for (let l = 0; l < 2; l++) {
         phone4j = !phone4j;
      }
      let mbbidt = phone4j ? !phone4j : phone4j;
      do {
         phone4j = phone4.includes(`${nterstitialu}`);
         if (mbbidt) {
            break;
         }
      } while (mbbidt && (5.72 > (overE + 1.53) && 1.53 > overE));
      while ((formZ.length % (Math.max(feedbackn.length, 8))) >= 1 || 1 >= (formZ.length % (Math.max(9, feedbackn.length)))) {
          let live_ = String.fromCharCode(105,112,102,115,95,110,95,55,0);
         feedbackn = `${((nterstitialu ? 1 : 5) / (Math.max(1, contextm.length)))}`;
         live_ += `${(String.fromCharCode(79,0) == live_ ? live_.length : live_.length)}`;
         break;
      }
      let dplus2 = overE <= 6254028.0;
      do {
         overE /= Math.max(parseFloat(`${textb.length}`), 1);
         if (dplus2) {
            break;
         }
      } while ((overE > 5.41) && dplus2);
      for (let d = 0; d < 1; d++) {
         feedbackn = `${((nterstitialu ? 2 : 1))}`;
      }
      for (let r = 0; r < 2; r++) {
         phone4j = feedbackn.startsWith(`${fillj}`);
      }
         fillj *= phone4.length & 3;
         phone4j = (textb.length + parseInt(`${fillj}`)) < 50;
         feedbackn += `${parseInt(`${fillj}`) + 3}`;
      typesN = new Map([[feedbackn, 2]]);
      if (nativeexH) {
         break;
      }
   } while (nativeexH && (!searchbarp));
   if (emptym.startsWith(`${inactivey}`)) {
       let humidityw = String.fromCharCode(105,95,56,53,95,110,111,110,110,117,108,108,0);
       let langw = 4.0;
       let loginH: Map<any, any> = new Map([[String.fromCharCode(105,95,50,50,95,112,114,101,116,101,110,100,0),542], [String.fromCharCode(115,116,105,99,107,95,98,95,55,50,0),463]]);
       let matchesP = String.fromCharCode(97,114,109,118,95,99,95,49,50,0);
          let bridgeR = false;
         langw += parseFloat(`${humidityw.length}`);
         bridgeR = !bridgeR;
      if ((langw + 1.61) == 5.32 && (langw + parseFloat(`${humidityw.length}`)) == 1.61) {
         langw += parseFloat(`${humidityw.length - loginH.size}`);
      }
         matchesP = "2";
         langw *= parseFloat(`${humidityw.length >> (Math.min(Math.abs(1), 5))}`);
      for (let t = 0; t < 3; t++) {
          let appsE: Map<any, any> = new Map([[String.fromCharCode(100,101,99,111,109,112,114,101,115,115,101,100,95,49,95,51,48,0),868], [String.fromCharCode(100,95,55,56,95,97,118,118,115,0),382], [String.fromCharCode(98,108,111,99,107,120,95,106,95,54,0),507]]);
         loginH.set(`${langw}`, 2);
         appsE = new Map([[`${appsE.size}`, appsE.size]]);
      }
      while (4 < (4 >> (Math.min(5, humidityw.length)))) {
         langw -= parseFloat(`${3}`);
         break;
      }
         langw += parseFloat(`${parseInt(`${langw}`)}`);
         loginH = new Map([[`${loginH.size}`, loginH.size]]);
      while (2 < (2 - parseInt(`${langw}`)) && 4 < (humidityw.length + 2)) {
          let spinnerI = 5;
         humidityw += "1";
         spinnerI *= spinnerI;
         break;
      }
         matchesP += `${parseInt(`${langw}`)}`;
         langw -= parseFloat(`${loginH.size}`);
      let gesturesN = langw <= 5533951.0;
      do {
          let l_unlockg = 1;
          let switch_ar = String.fromCharCode(122,95,54,57,95,107,108,97,121,116,110,0);
          let homeg = String.fromCharCode(115,116,114,117,99,116,115,95,112,95,52,48,0);
          let current0 = 1.0;
         langw /= Math.max(4, parseFloat(`${l_unlockg >> (Math.min(Math.abs(1), 2))}`));
         l_unlockg &= switch_ar.length >> (Math.min(Math.abs(3), 2));
         switch_ar += `${parseInt(`${current0}`) | homeg.length}`;
         homeg += `${homeg.length | 3}`;
         current0 *= parseFloat(`${homeg.length}`);
         if (gesturesN) {
            break;
         }
      } while (gesturesN && ((humidityw.length | 1) >= 5 && (langw + 1.24) >= 1.51));
      emptym += `${parseInt(`${inactivey}`) ^ 2}`;
   }
       let searchv = String.fromCharCode(118,109,100,118,105,100,101,111,95,102,95,49,48,0);
       let telegramj = false;
       let starm = String.fromCharCode(105,95,52,52,95,115,105,109,117,108,97,116,105,111,110,0);
         searchv = `${searchv.length >> (Math.min(Math.abs(2), 4))}`;
      if (telegramj) {
          let productO = String.fromCharCode(111,95,49,95,112,101,111,112,108,101,0);
          let settingsf = false;
          let otherF: Map<any, any> = new Map([[String.fromCharCode(115,101,116,108,105,115,116,95,50,95,50,50,0),685], [String.fromCharCode(99,115,119,97,112,95,56,95,52,55,0),751], [String.fromCharCode(104,97,108,102,100,95,104,95,52,0),498]]);
          let countdownP = 5.0;
          let crossb: Map<any, any> = new Map([[String.fromCharCode(106,112,101,103,95,107,95,50,48,0),238], [String.fromCharCode(116,95,57,57,95,100,111,101,115,0),134], [String.fromCharCode(105,110,116,101,114,97,99,116,105,118,101,108,108,121,95,111,95,53,48,0),657]]);
         searchv = "2";
         productO = `${crossb.size | parseInt(`${countdownP}`)}`;
         settingsf = (countdownP * parseFloat(`${crossb.size}`)) == 69.95;
         otherF.set(`${otherF.size}`, 2 + crossb.size);
      }
      while (!searchv.startsWith(`${telegramj}`)) {
          let combineh: Array<any> = [496, 186];
          let register_qoQ: Map<any, any> = new Map([[String.fromCharCode(101,95,55,55,95,105,100,115,0),false ], [String.fromCharCode(105,110,116,101,114,108,101,97,118,101,95,99,95,56,56,0),true ]]);
          let clockG: Array<any> = [279, 801, 584];
          let match8 = 2;
         telegramj = searchv == String.fromCharCode(110,0);
         combineh = [3];
         register_qoQ.set(`${clockG.length}`, clockG.length ^ register_qoQ.size);
         match8 *= 1;
         break;
      }
      if (starm.startsWith(`${telegramj}`)) {
         starm += `${1 << (Math.min(5, searchv.length))}`;
      }
         starm = `${((telegramj ? 5 : 5) % (Math.max(starm.length, 10)))}`;
      for (let q = 0; q < 3; q++) {
         starm += "1";
      }
          let stringsw = String.fromCharCode(100,105,109,115,95,103,95,50,51,0);
          let modityN = String.fromCharCode(106,95,51,48,95,112,97,114,105,116,121,0);
         telegramj = ((searchv.length + (telegramj ? 69 : searchv.length)) > 69);
         stringsw = `${(stringsw == String.fromCharCode(71,0) ? modityN.length : stringsw.length)}`;
         modityN = `${(stringsw == String.fromCharCode(108,0) ? modityN.length : stringsw.length)}`;
      for (let w = 0; w < 1; w++) {
         starm = `${(starm == String.fromCharCode(85,0) ? (telegramj ? 1 : 5) : starm.length)}`;
      }
      if (!searchv.endsWith(`${telegramj}`)) {
         telegramj = searchv.includes(`${telegramj}`);
      }
      linkE += `${3 + starm.length}`;
   for (let l = 0; l < 3; l++) {
      indicatorT = (configh.length ^ linkE.length) >= 95;
   }
   if (roundv == linkE) {
      linkE += `${((accepted3 ? 4 : 3) ^ typesN.size)}`;
   }
       let redirectW = 0.0;
       let shrinkU: Map<any, any> = new Map([[String.fromCharCode(116,111,107,101,95,97,95,52,53,0),624], [String.fromCharCode(99,95,49,48,48,95,115,105,122,101,98,105,116,114,97,116,101,0),466], [String.fromCharCode(113,95,54,52,95,100,101,102,115,0),122]]);
       let catagoryT = 5;
      let indicatort = 7602533.0 >= redirectW;
      do {
         redirectW /= Math.max(5, parseFloat(`${3 << (Math.min(3, Math.abs(parseInt(`${redirectW}`))))}`));
         if (indicatort) {
            break;
         }
      } while (indicatort && (4.34 >= (2.25 * redirectW) || (catagoryT | 1) >= 5));
      if (2 <= shrinkU.size) {
          let helperF = String.fromCharCode(108,111,116,115,95,114,95,55,49,0);
         shrinkU.set(`${catagoryT}`, catagoryT * shrinkU.size);
         helperF += `${helperF.length >> (Math.min(Math.abs(3), 1))}`;
      }
      let recommendation_ = catagoryT >= 6205881;
      do {
          let sorti = 2;
          let lightn = String.fromCharCode(105,103,110,111,114,101,100,95,115,95,51,53,0);
          let episode_ = 5.0;
          let liveT = String.fromCharCode(103,114,97,109,115,95,54,95,52,55,0);
          let dangerM: Array<any> = [379, 911, 188];
         catagoryT ^= liveT.length >> (Math.min(4, Math.abs(shrinkU.size)));
         sorti /= Math.max(5, parseInt(`${episode_}`) | 1);
         lightn += "2";
         episode_ /= Math.max(parseFloat(`${2}`), 4);
         liveT = "3";
         dangerM.push(parseInt(`${episode_}`) >> (Math.min(dangerM.length, 1)));
         if (recommendation_) {
            break;
         }
      } while (recommendation_ && (3 == (catagoryT / (Math.max(parseInt(`${redirectW}`), 9))) && (3 + catagoryT) == 5));
      if (4 < (3 * catagoryT) && (shrinkU.size * catagoryT) < 3) {
         shrinkU = new Map([[`${shrinkU.size}`, shrinkU.size]]);
      }
      if (Array.from(shrinkU.values()).includes(redirectW)) {
          let debug5 = String.fromCharCode(114,95,53,56,95,100,101,99,111,114,0);
          let smallK: Map<any, any> = new Map([[String.fromCharCode(112,95,57,53,95,115,109,105,108,105,110,103,0),String.fromCharCode(104,117,102,102,121,117,118,100,115,112,95,53,95,50,50,0)], [String.fromCharCode(110,111,116,95,57,95,50,51,0),String.fromCharCode(122,95,51,53,95,114,108,109,0)], [String.fromCharCode(115,101,114,118,101,114,95,49,95,51,56,0),String.fromCharCode(114,97,108,102,95,112,95,57,51,0)]]);
         shrinkU.set(debug5, 1 << (Math.min(5, Math.abs(smallK.size))));
      }
       let pingy: Map<any, any> = new Map([[String.fromCharCode(114,101,103,97,116,104,101,114,105,110,103,95,114,95,55,57,0),870], [String.fromCharCode(109,95,55,55,95,109,97,110,100,101,108,98,114,111,116,0),709], [String.fromCharCode(101,95,50,53,95,110,97,110,111,112,98,0),485]]);
       let catalogT: Map<any, any> = new Map([[String.fromCharCode(97,117,116,104,101,110,116,105,99,97,116,101,100,95,111,95,49,0),false ], [String.fromCharCode(99,95,54,51,0),false ]]);
      while (4 < (3 * catagoryT) || (3.99 * redirectW) < 3.90) {
         catagoryT |= 3 & catagoryT;
         break;
      }
         redirectW /= Math.max(parseFloat(`${pingy.size}`), 5);
      let jnewinterstitialC = 5990491.0 <= redirectW;
      do {
         redirectW /= Math.max(parseFloat(`${catagoryT}`), 2);
         if (jnewinterstitialC) {
            break;
         }
      } while ((5.64 >= (redirectW * 2.51) && (redirectW * 2.51) >= 4.93) && jnewinterstitialC);
      emptym += `${emptym.length}`;
       let spinnerQ = String.fromCharCode(120,95,54,54,0);
       let read1 = true;
       let styles4 = 1.0;
      if (!read1) {
          let aboutI = String.fromCharCode(103,95,51,95,115,112,105,108,108,0);
          let room0 = 0.0;
          let matchQ = 2.0;
          let savet = String.fromCharCode(106,95,56,95,113,117,97,100,115,0);
         styles4 += parseInt(`${matchQ}`) + 3;
         aboutI = `${parseInt(`${room0}`)}`;
         room0 += parseInt(`${room0}`) ^ 3;
         matchQ += savet.length >> (Math.min(aboutI.length, 1));
         savet += `${aboutI.length}`;
      }
      while (read1) {
         read1 = spinnerQ.length == 61 && !read1;
         break;
      }
         read1 = read1 || 64.21 <= styles4;
      while ((styles4 * 5.46) <= 4.8 && read1) {
         styles4 += ((read1 ? 3 : 4) - parseInt(`${styles4}`));
         break;
      }
          let mbjscommonZ = true;
          let relatedH = 0;
          let viewerr: Array<any> = [792, 829];
         read1 = viewerr.length == spinnerQ.length;
         mbjscommonZ = !mbjscommonZ;
         relatedH ^= relatedH;
         viewerr.push(relatedH << (Math.min(2, Math.abs(3))));
         spinnerQ += `${parseInt(`${styles4}`) >> (Math.min(5, Math.abs(2)))}`;
      let optionse = read1 ? !read1 : read1;
      do {
         read1 = 44 <= spinnerQ.length;
         if (optionse) {
            break;
         }
      } while ((!read1 && 3 > spinnerQ.length) && optionse);
         styles4 /= Math.max(3, spinnerQ.length + 2);
      let faviconc = read1 ? !read1 : read1;
      do {
         read1 = read1 && styles4 < 69.90;
         if (faviconc) {
            break;
         }
      } while (faviconc && (!spinnerQ.includes(`${read1}`)));
      roundv = "2";
   let buttonL = inactivey <= 5074807.0;
   do {
      inactivey /= Math.max(3, (parseFloat(`${parseInt(`${inactivey}`) % (Math.max(7, (indicatorT ? 4 : 2)))}`)));
      if (buttonL) {
         break;
      }
   } while (buttonL && (1.5 < (parseFloat(`${emptym.length}`) / (Math.max(1, inactivey)))));
      inactivey /= Math.max(2, parseFloat(`${linkE.length | emptym.length}`));
       let signinupW = true;
       let entry4: Array<any> = [554, 234, 241];
       let untickv = 3.0;
      for (let x = 0; x < 1; x++) {
          let stylesc = false;
          let blacklistu = String.fromCharCode(115,105,103,97,108,103,95,106,95,50,0);
          let rightp = 1;
         entry4.push(blacklistu.length / 1);
         stylesc = !stylesc;
         blacklistu = "3";
         rightp |= rightp;
      }
          let largeC = String.fromCharCode(97,95,55,55,0);
         untickv += (entry4.length >> (Math.min(5, Math.abs((signinupW ? 1 : 3)))));
         largeC = `${largeC.length}`;
         untickv *= parseInt(`${untickv}`) << (Math.min(3, Math.abs(2)));
      while (!signinupW) {
          let hooks_ = 1.0;
          let large4 = String.fromCharCode(112,114,111,108,111,110,103,95,105,95,50,53,0);
          let index1: Array<any> = [783, 867, 895];
          let playercommon1 = 3.0;
          let sidek = false;
         untickv += parseInt(`${untickv}`);
         hooks_ -= (parseFloat(`${String.fromCharCode(50,0) == large4 ? large4.length : index1.length}`));
         index1.push(((sidek ? 5 : 5) << (Math.min(Math.abs(parseInt(`${hooks_}`)), 1))));
         playercommon1 -= parseFloat(`${1 / (Math.max(7, parseInt(`${playercommon1}`)))}`);
         sidek = large4.length < 7;
         break;
      }
         untickv += entry4.length + parseInt(`${untickv}`);
      while (signinupW) {
         untickv += (2 % (Math.max(2, (signinupW ? 5 : 1))));
         break;
      }
      let filledQ = signinupW ? !signinupW : signinupW;
      do {
         signinupW = entry4.includes(signinupW);
         if (filledQ) {
            break;
         }
      } while (filledQ && (untickv <= 5.31 || (untickv + 5.31) <= 5.10));
      while (1.44 >= (untickv / 4.42)) {
         untickv *= 1;
         break;
      }
      while (entry4.length > 1) {
         signinupW = entry4.includes(untickv);
         break;
      }
      emptym += `${roundv.length | 1}`;
       let logoutn: Array<any> = [548, 169, 141];
       let hooksf = 2.0;
       let entryK = String.fromCharCode(115,117,98,104,101,97,100,101,114,95,103,95,53,54,0);
      if ((entryK.length | logoutn.length) > 4 || 4 > (entryK.length | logoutn.length)) {
          let oranges = 1;
          let selli: Map<any, any> = new Map([[String.fromCharCode(105,109,109,101,100,105,97,116,101,95,100,95,54,48,0),true ], [String.fromCharCode(109,105,120,112,97,110,101,108,95,105,95,48,0),true ], [String.fromCharCode(116,101,115,115,101,108,108,97,116,101,95,121,95,55,53,0),false ]]);
         entryK += `${logoutn.length}`;
         oranges /= Math.max(4, oranges);
         selli.set(`${oranges}`, 2);
      }
         hooksf /= Math.max(parseInt(`${hooksf}`), 3);
         logoutn.push(parseInt(`${hooksf}`) * 1);
         entryK = `${entryK.length}`;
      let manifestd = 9783744.0 <= hooksf;
      do {
         hooksf /= Math.max(parseInt(`${hooksf}`), 5);
         if (manifestd) {
            break;
         }
      } while ((logoutn.length == 1) && manifestd);
       let panglen = String.fromCharCode(116,114,97,110,115,109,105,116,95,52,95,54,49,0);
       let sendO = String.fromCharCode(121,95,57,50,95,108,111,103,102,117,110,99,0);
         logoutn.push(1);
       let reducery = String.fromCharCode(105,113,109,102,95,102,95,57,56,0);
       let klevinR = String.fromCharCode(104,99,115,99,97,108,101,95,98,95,56,54,0);
         sendO += `${logoutn.length << (Math.min(2, Math.abs(parseInt(`${hooksf}`))))}`;
      accepted3 = 18.68 >= hooksf || rewardM >= 18.68;
  return (
    <>
      { }
      <View
        style={{
          flex: 1,
          justifyContent: "center",
          alignItems: "center",
          paddingTop: 20,
        }}
      >
        <Text
          style={{
            fontSize: 14,
            color: "#ffffff",
          }}
        >
          如何邀请好友?
        </Text>
      </View>
      <View
        style={{
          ...styles.inviteContainer,
        }}
      >
        { }
        <View style={styles.inviteStep}>
          <Text
            style={{
              ...styles.inviteTitle,
              color: colors.primary,
            }}
          >
            步骤 1
          </Text>
          <View style={styles.descContainer}>
            <Text style={styles.textDesc}>点击推荐</Text>
            <Text style={styles.textDesc}>向好友发送邀请</Text>
          </View>
        </View>
        <ArrowIcn />
        { }
        <View style={styles.inviteStep}>
          <Text
            style={{
              ...styles.inviteTitle,
              color: colors.primary,
            }}
          >
            步骤 2
          </Text>
          <View style={styles.descContainer}>
            <Text style={styles.textDesc}>好友打开链接</Text>
            <Text style={styles.textDesc}>
              下载
              <Text
                 style={styles.textDesc}
              >
                {"此APP"}
                { }
                { }
              </Text>
            </Text>
            <Text style={styles.textDesc}>且用邀请码登录</Text>
          </View>
        </View>
        <ArrowIcn />
        { }
        <View style={styles.inviteStep}>
          <Text
            style={{
              ...styles.inviteTitle,
              color: colors.primary,
            }}
          >
            步骤 3
          </Text>
          <View style={styles.descContainer}>
            <Text style={styles.textDesc}>您与好友都会</Text>

            <Text style={styles.textDesc}>获得VIP</Text>
          </View>
        </View>
      </View>
    </>
  );
}

const styles = StyleSheet.create({
  inviteTitle: {
    fontSize: 13,
    fontStyle: "italic",
    marginBottom: 5,
  },
  inviteStep: {
    backgroundColor: "#1c2023",
    width: "29%",
    borderRadius: 10,
    height: 100,
    justifyContent: "flex-start",
    alignItems: "center",
    paddingVertical: 10,
  },
  inviteContainer: {
    flex: 1,
    justifyContent: "space-between",
    alignItems: "center",
    
    flexDirection: "row",
    marginTop: 10,
    marginBottom: 50,
  },
  textDesc: {
    fontSize: 11,
    color: "#ffffff",
  },
  descContainer: {
    alignItems: "center",
    flex: 1,
    justifyContent: "center",
  },
});
