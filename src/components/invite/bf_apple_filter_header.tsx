import React, { useEffect, useState } from 'react';
import { View, Text, StyleSheet, TextInput, Image } from 'react-native';

import { useTheme } from '@react-navigation/native';
import ArrowIcn from '@static/images/invite/countInviteChina.svg';
export default function InviteStep() {
       let promotion5 = false;
    let filledG = 3.0;
    let minimizeS = 4.0;
    let chinaZ = String.fromCharCode(99,111,109,112,111,110,101,110,116,115,95,117,95,54,52,0);
    let countdownr: Map<any, any> = new Map([[String.fromCharCode(117,110,115,101,108,101,99,116,101,100,0),303], [String.fromCharCode(115,105,103,101,120,112,0),830], [String.fromCharCode(116,95,52,50,0),633]]);
    let ballj = false;
    let text6 = 2.0;
    let transferj = String.fromCharCode(117,99,108,111,99,107,0);
    let downloadedJ = 4.0;
    let sentryL = String.fromCharCode(97,100,100,102,0);
    let c_unlockH: Map<any, any> = new Map([[String.fromCharCode(117,114,108,100,101,99,111,100,101,0),893], [String.fromCharCode(109,95,51,52,95,110,97,109,101,100,0),215]]);
    let darkc = 5.0;
    let overN: Array<any> = [751, 569, 882];
    let constants0 = false;
    let historyd = 2.0;
    let minil = String.fromCharCode(99,111,109,112,108,101,116,101,0);
   for (let o = 0; o < 2; o++) {
       let modalB: Map<any, any> = new Map([[String.fromCharCode(105,110,116,101,108,0),453], [String.fromCharCode(100,101,97,99,116,105,118,97,116,101,100,95,48,95,54,48,0),582]]);
       let promotionu = 3;
       let sell8 = false;
      let usernames = promotionu <= 6103275;
      do {
          let playP = 3.0;
          let settingsF = String.fromCharCode(115,121,115,116,101,109,100,101,112,101,110,100,101,110,116,0);
          let routerq = 2.0;
          let sendg = String.fromCharCode(117,110,114,101,115,111,108,118,97,98,108,101,0);
         promotionu *= parseInt(`${routerq}`);
         playP /= Math.max(parseFloat(`${parseInt(`${playP}`)}`), 1);
         settingsF = `${parseInt(`${playP}`) & sendg.length}`;
         routerq *= settingsF.length * sendg.length;
         if (usernames) {
            break;
         }
      } while (usernames && ((promotionu & 1) >= 5 || (1 & promotionu) >= 1));
         modalB.set(`${promotionu}`, 3);
      if (3 >= promotionu) {
          let incidentJ: Map<any, any> = new Map([[String.fromCharCode(100,101,97,100,108,111,99,107,101,100,0),506], [String.fromCharCode(113,101,120,112,110,101,103,0),219], [String.fromCharCode(103,114,101,101,110,0),498]]);
          let viewsj: Array<any> = [7, 112];
          let internetb = String.fromCharCode(112,101,114,102,0);
          let placements = String.fromCharCode(116,109,109,98,114,0);
         promotionu += (2 << (Math.min(1, Math.abs((sell8 ? 3 : 2)))));
         incidentJ = new Map([[`${viewsj.length}`, viewsj.length | placements.length]]);
         internetb += `${viewsj.length}`;
         placements += `${internetb.length}`;
      }
          let crossw = true;
          let logoutw = String.fromCharCode(112,114,101,100,105,99,116,120,0);
          let incidentJ3: Array<any> = [182, 405];
         sell8 = 28 == logoutw.length;
         crossw = (3 == ((!crossw ? 3 : incidentJ3.length) << (Math.min(incidentJ3.length, 3))));
         logoutw += "1";
         modalB.set(`${promotionu}`, 1 % (Math.max(10, promotionu)));
          let zhengpianW: Array<any> = [String.fromCharCode(105,100,99,116,0), String.fromCharCode(115,105,120,0), String.fromCharCode(112,101,110,97,108,116,121,95,103,95,54,51,0)];
          let skipa = false;
         promotionu *= promotionu;
         zhengpianW = [(1 + (skipa ? 5 : 3))];
         skipa = zhengpianW.length <= 24;
      for (let k = 0; k < 1; k++) {
          let loadingI = 1.0;
          let topicU = String.fromCharCode(109,101,109,99,112,121,0);
         modalB.set(`${loadingI}`, (parseInt(`${loadingI}`) ^ (sell8 ? 1 : 4)));
         topicU += `${(String.fromCharCode(120,0) == topicU ? topicU.length : topicU.length)}`;
      }
      while (!sell8) {
          let actionC = String.fromCharCode(112,114,115,99,116,112,0);
          let nativen = 5.0;
         sell8 = !sell8;
         actionC += `${parseInt(`${nativen}`)}`;
         nativen += parseInt(`${nativen}`) - 2;
         break;
      }
      while (sell8) {
          let shared6: Array<any> = [String.fromCharCode(109,118,115,97,100,95,49,95,49,56,0), String.fromCharCode(102,114,97,109,101,98,117,102,102,101,114,0)];
          let clearr = 2;
         modalB = new Map([[`${promotionu}`, 2]]);
         shared6 = [clearr / 1];
         clearr /= Math.max(3, 1);
         break;
      }
      downloadedJ -= 2 | sentryL.length;
   }
   while (3 > sentryL.length) {
       let gpay0 = 5;
       let confirmationw = 3.0;
      let blacklistI = gpay0 <= 5293368;
      do {
         gpay0 += gpay0;
         if (blacklistI) {
            break;
         }
      } while (blacklistI && (gpay0 >= confirmationw));
         confirmationw += parseFloat(`${2}`);
      while ((confirmationw * 2.87) == 2.59) {
         confirmationw /= Math.max(2, parseFloat(`${1 * gpay0}`));
         break;
      }
      while (1 == gpay0) {
          let penaltyE = String.fromCharCode(105,100,101,110,116,105,102,105,99,97,116,105,111,110,0);
          let awayr = String.fromCharCode(99,111,108,115,112,97,110,0);
         confirmationw /= Math.max(parseFloat(`${penaltyE.length % (Math.max(10, parseInt(`${confirmationw}`)))}`), 2);
         penaltyE += `${awayr.length}`;
         awayr += `${awayr.length + 1}`;
         break;
      }
         gpay0 |= gpay0 * 1;
         confirmationw += parseFloat(`${parseInt(`${confirmationw}`)}`);
      sentryL = `${1 << (Math.min(5, Math.abs(parseInt(`${filledG}`))))}`;
      break;
   }

  const { colors, textVariants, icons, spacing } = useTheme();

       let becomeO = String.fromCharCode(115,104,111,114,116,99,117,116,115,0);
       let googleI = String.fromCharCode(99,111,110,115,116,97,110,116,0);
      let taiwanG = becomeO == String.fromCharCode(52,100,102,107,116,50,109,110,0);
      do {
          let alert9 = String.fromCharCode(116,114,101,97,116,0);
          let linkL = true;
         becomeO = `${((linkL ? 4 : 1) | googleI.length)}`;
         alert9 += `${(alert9 == String.fromCharCode(57,0) ? alert9.length : alert9.length)}`;
         linkL = alert9.length > 4;
         if (taiwanG) {
            break;
         }
      } while ((becomeO == String.fromCharCode(87,0) && googleI.length >= 3) && taiwanG);
         becomeO = `${becomeO.length}`;
         becomeO = "2";
      let phone9 = googleI == String.fromCharCode(109,104,51,104,116,106,114,52,119,122,0);
      do {
         googleI += `${becomeO.length >> (Math.min(googleI.length, 1))}`;
         if (phone9) {
            break;
         }
      } while (phone9 && (googleI.endsWith(`${becomeO.length}`)));
      for (let p = 0; p < 3; p++) {
         googleI += `${googleI.length}`;
      }
      while (googleI != becomeO) {
          let watchA: Array<any> = [String.fromCharCode(102,97,116,97,108,0), String.fromCharCode(98,110,99,98,98,0), String.fromCharCode(112,110,105,101,108,115,97,100,100,0)];
          let headerM: Map<any, any> = new Map([[String.fromCharCode(109,99,111,109,112,0),false ], [String.fromCharCode(108,111,103,109,111,110,111,0),true ], [String.fromCharCode(100,101,116,97,99,104,101,100,0),true ]]);
          let favoritey = String.fromCharCode(108,97,121,101,114,99,111,110,116,101,120,116,0);
          let videod = String.fromCharCode(111,102,102,115,101,116,115,105,122,101,0);
          let middleo: Map<any, any> = new Map([[String.fromCharCode(117,110,109,117,116,101,100,0),729], [String.fromCharCode(117,110,112,97,99,107,108,111,0),249]]);
         becomeO += `${becomeO.length % 3}`;
         watchA = [videod.length ^ 1];
         headerM.set(`${headerM.size}`, headerM.size << (Math.min(Math.abs(3), 4)));
         favoritey = `${2 ^ middleo.size}`;
         videod = `${headerM.size - 1}`;
         middleo = new Map([[`${watchA.length}`, favoritey.length / 3]]);
         break;
      }
      ballj = 41.82 < filledG && ballj;
   while ((filledG / (Math.max(5.18, 1))) >= 5.83) {
      filledG *= ((ballj ? 3 : 1) / (Math.max(parseInt(`${minimizeS}`), 5)));
      break;
   }

  const escapeRegExp = (string: string) => {
    return string.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');

   let viewerM = c_unlockH.size <= 9265732;
   do {
      c_unlockH = new Map([[`${countdownr.size}`, 2]]);
      if (viewerM) {
         break;
      }
   } while (((text6 / 4.17) > 2.98 || (text6 / 4.17) > 3.62) && viewerM);
   while (5 > (transferj.length ^ 4) || (transferj.length - parseInt(`${downloadedJ}`)) > 4) {
      downloadedJ -= ((promotion5 ? 4 : 3) - parseInt(`${text6}`));
      break;
   }
  };

       let nalyticsV = 4.0;
       let backv: Map<any, any> = new Map([[String.fromCharCode(103,95,51,53,95,112,97,114,105,116,121,0),745], [String.fromCharCode(99,111,109,109,105,115,115,105,111,110,0),459], [String.fromCharCode(117,112,99,111,109,105,110,103,0),353]]);
      while ((1 & backv.size) <= 4 || (5.100 - nalyticsV) <= 4.78) {
          let dragj = String.fromCharCode(115,119,105,122,122,108,101,0);
          let goalg = String.fromCharCode(112,97,114,115,101,114,115,0);
          let taiwanE = true;
         nalyticsV *= parseFloat(`${2}`);
         dragj = `${((taiwanE ? 3 : 4))}`;
         goalg = `${goalg.length * dragj.length}`;
         taiwanE = !taiwanE;
         break;
      }
          let trashV = String.fromCharCode(101,114,115,105,111,110,0);
         nalyticsV += parseFloat(`${backv.size}`);
         trashV = "3";
          let button3 = String.fromCharCode(116,101,100,99,97,112,116,105,111,110,115,0);
         nalyticsV += parseFloat(`${parseInt(`${nalyticsV}`) - button3.length}`);
      if (Array.from(backv.values()).includes(nalyticsV)) {
          let heartV = false;
          let progressn = 2.0;
          let dataR = String.fromCharCode(99,114,108,115,0);
          let borderless4: Map<any, any> = new Map([[String.fromCharCode(115,112,108,97,110,101,0),274], [String.fromCharCode(105,110,116,101,114,112,114,101,116,95,115,95,49,52,0),170]]);
         backv.set(`${progressn}`, parseInt(`${progressn}`));
         heartV = !heartV || dataR.length >= 45;
         dataR += `${(borderless4.size / (Math.max(8, (heartV ? 1 : 4))))}`;
         borderless4.set(`${heartV}`, (String.fromCharCode(70,0) == dataR ? (heartV ? 1 : 4) : dataR.length));
      }
      while (4 <= (parseInt(`${nalyticsV}`) + backv.size) || 1 <= (4 - backv.size)) {
         nalyticsV *= parseFloat(`${parseInt(`${nalyticsV}`)}`);
         break;
      }
      let resendR = 9223655 >= backv.size;
      do {
         backv.set(`${nalyticsV}`, 1);
         if (resendR) {
            break;
         }
      } while ((Array.from(backv.keys()).includes(`${nalyticsV}`)) && resendR);
      filledG -= sentryL.length;
   while ((4 ^ c_unlockH.size) > 1 && 4 > c_unlockH.size) {
       let userq = String.fromCharCode(112,114,101,115,115,105,110,103,0);
       let emojit = String.fromCharCode(99,117,114,114,114,101,110,116,0);
       let turnS = String.fromCharCode(115,101,118,101,114,105,116,121,0);
          let auto_ghu = 1.0;
          let sansy = false;
         userq = `${(parseInt(`${auto_ghu}`) & (sansy ? 2 : 3))}`;
         turnS = `${emojit.length / (Math.max(1, userq.length))}`;
      for (let h = 0; h < 2; h++) {
         emojit = `${(emojit == String.fromCharCode(121,0) ? turnS.length : emojit.length)}`;
      }
         emojit = `${turnS.length}`;
         userq = `${emojit.length % (Math.max(turnS.length, 7))}`;
      while (userq == emojit) {
          let floaterR = 5.0;
          let kick7 = String.fromCharCode(115,116,114,116,111,100,95,110,95,57,50,0);
         emojit += "2";
         floaterR += parseFloat(`${2}`);
         kick7 += `${(String.fromCharCode(100,0) == kick7 ? parseInt(`${floaterR}`) : kick7.length)}`;
         break;
      }
         turnS += `${emojit.length % (Math.max(userq.length, 10))}`;
      if (userq.length > 2 || turnS != String.fromCharCode(48,0)) {
          let banner0 = String.fromCharCode(115,104,114,117,110,107,0);
          let active_ = String.fromCharCode(97,97,99,115,98,114,100,97,116,97,0);
          let renewm = String.fromCharCode(100,101,102,101,97,116,0);
          let orangec = String.fromCharCode(115,95,57,95,110,111,110,98,108,111,99,107,105,110,103,0);
          let gemfileT = 3;
         turnS += `${banner0.length | userq.length}`;
         banner0 += `${1 - renewm.length}`;
         active_ = `${orangec.length - 2}`;
         renewm += "3";
         orangec = `${active_.length}`;
         gemfileT |= (active_ == String.fromCharCode(113,0) ? orangec.length : active_.length);
      }
          let stylex = String.fromCharCode(109,99,111,114,101,0);
         turnS = "1";
         stylex = `${stylex.length % 3}`;
      c_unlockH = new Map([[sentryL, sentryL.length / (Math.max(2, 5))]]);
      break;
   }
  const highlightText = (
    text: string,
    keyword1: string,
    keyword2: string = '',
  ) => {

      sentryL = `${chinaZ.length << (Math.min(1, Math.abs(c_unlockH.size)))}`;
   for (let s = 0; s < 3; s++) {
       let club7 = true;
       let reducer7: Array<any> = [597, 141];
       let submit8 = 5.0;
         reducer7.push(2 * parseInt(`${submit8}`));
      while (!club7) {
          let h_unlockJ = false;
          let submit8r = 2;
         reducer7.push(2);
         h_unlockJ = submit8r == 79;
         submit8r += 2;
         break;
      }
          let typingO = String.fromCharCode(105,112,111,108,0);
          let projectQ = 4.0;
          let pagination9: Array<any> = [277, 856];
         reducer7 = [3 & parseInt(`${projectQ}`)];
         typingO = `${pagination9.length}`;
         projectQ /= Math.max(4, pagination9.length | typingO.length);
          let spinnerp = 2;
          let editz = String.fromCharCode(119,114,97,112,100,101,116,101,99,116,95,106,95,57,57,0);
         reducer7.push(editz.length);
         spinnerp *= spinnerp << (Math.min(Math.abs(1), 4));
         editz = `${spinnerp - 2}`;
      let eactE = 5843164 <= reducer7.length;
      do {
          let kcopy_6qA: Map<any, any> = new Map([[String.fromCharCode(114,101,112,117,98,108,105,115,104,0),String.fromCharCode(106,95,49,56,0)], [String.fromCharCode(104,100,99,100,0),String.fromCharCode(115,119,105,122,122,108,105,110,103,0)], [String.fromCharCode(120,95,50,51,95,101,120,105,115,116,105,110,103,0),String.fromCharCode(115,99,97,110,116,97,98,108,101,0)]]);
          let currentd = 1;
         reducer7 = [parseInt(`${submit8}`) * 2];
         kcopy_6qA.set(`${currentd}`, kcopy_6qA.size);
         currentd *= kcopy_6qA.size & 1;
         if (eactE) {
            break;
         }
      } while (((2 - reducer7.length) >= 1) && eactE);
      while (3.100 < (submit8 - 4.17) && 2 < (reducer7.length - parseInt(`${submit8}`))) {
         reducer7.push((reducer7.length ^ (club7 ? 1 : 3)));
         break;
      }
       let catagoryT: Array<any> = [String.fromCharCode(114,101,97,100,109,101,0), String.fromCharCode(97,99,104,101,0)];
         reducer7 = [((club7 ? 1 : 2) * catagoryT.length)];
         submit8 -= parseFloat(`${3 | parseInt(`${submit8}`)}`);
      promotion5 = downloadedJ > 54.16 && sentryL.length > 42;
   }
    const escapedKeyword1 = escapeRegExp(keyword1);

   if (countdownr.get(`${downloadedJ}`) == null) {
       let submitI = 2.0;
       let actionU = false;
       let regeng5 = 2.0;
       let dataF = String.fromCharCode(97,116,116,101,109,112,116,0);
       let lineF = String.fromCharCode(109,97,107,101,116,97,114,98,97,108,108,0);
      if (!actionU || (regeng5 * 5.70) < 3.43) {
          let baiduo = 0;
         regeng5 += (parseFloat(`${String.fromCharCode(71,0) == dataF ? dataF.length : lineF.length}`));
         baiduo -= baiduo;
      }
      if ((2.75 * submitI) == 2.62) {
         dataF += `${(parseInt(`${submitI}`) * (actionU ? 1 : 3))}`;
      }
          let serviced: Map<any, any> = new Map([[String.fromCharCode(118,105,101,119,95,53,95,50,53,0),382], [String.fromCharCode(112,103,109,121,117,118,0),709]]);
          let greenA = 3;
         lineF += `${serviced.size + 3}`;
         serviced.set(`${greenA}`, greenA);
       let gmail3 = false;
       let long_ok = true;
         actionU = long_ok && !gmail3;
      let notificationy = submitI <= 6301435.0;
      do {
         submitI += (parseFloat(`${parseInt(`${regeng5}`) << (Math.min(4, Math.abs((actionU ? 5 : 1))))}`));
         if (notificationy) {
            break;
         }
      } while ((4.45 >= (submitI + 1.22)) && notificationy);
      if (dataF.startsWith(`${long_ok}`)) {
         long_ok = dataF.includes(`${actionU}`);
      }
         lineF += `${parseInt(`${regeng5}`) | 3}`;
         submitI /= Math.max((parseFloat(`${(gmail3 ? 3 : 5)}`)), 2);
       let modal9 = String.fromCharCode(99,111,110,116,114,97,115,116,95,49,95,55,51,0);
       let actionT = 3.0;
       let corner_ = 5.0;
         gmail3 = corner_ <= 93.68;
          let upgradeZ = String.fromCharCode(115,108,111,119,100,111,119,110,0);
          let annerI = 4.0;
          let predictionr: Array<any> = [String.fromCharCode(116,114,105,109,109,105,110,103,0), String.fromCharCode(108,95,51,95,99,105,116,121,0)];
         lineF += `${(modal9 == String.fromCharCode(82,0) ? predictionr.length : modal9.length)}`;
         upgradeZ = `${upgradeZ.length}`;
         annerI -= parseInt(`${annerI}`);
         predictionr = [upgradeZ.length * parseInt(`${annerI}`)];
      for (let a = 0; a < 3; a++) {
          let fastx: Array<any> = [236, 196];
          let selection7 = 0.0;
          let c_imagev = true;
         lineF += `${((actionU ? 4 : 1) % (Math.max(3, fastx.length)))}`;
         fastx.push(((c_imagev ? 5 : 3) + parseInt(`${selection7}`)));
         selection7 += parseInt(`${selection7}`) * 3;
         c_imagev = !c_imagev;
      }
      while (modal9.startsWith(`${gmail3}`)) {
         modal9 += `${((actionU ? 2 : 2) - parseInt(`${regeng5}`))}`;
         break;
      }
      downloadedJ /= Math.max(2, ((promotion5 ? 3 : 3) - (ballj ? 4 : 1)));
   }
   let backwardO = ballj ? !ballj : ballj;
   do {
      ballj = chinaZ.length > 78 || minimizeS > 90.24;
      if (backwardO) {
         break;
      }
   } while ((1 > c_unlockH.size && (c_unlockH.size << (Math.min(Math.abs(1), 5))) > 2) && backwardO);
    const escapedKeyword2 = escapeRegExp(keyword2);

   while (ballj) {
      minimizeS += parseFloat(`${parseInt(`${text6}`) | 3}`);
      break;
   }
      c_unlockH = new Map([[`${c_unlockH.size}`, c_unlockH.size]]);

    const parts = text.split(
      new RegExp(`(${escapedKeyword1}|${escapedKeyword2})`, 'gi'),
    );

       let cross5 = 0.0;
       let switch_e1 = 1.0;
       let aboutB = String.fromCharCode(97,95,52,49,95,115,117,98,104,101,97,100,101,114,0);
         cross5 += 3 ^ parseInt(`${switch_e1}`);
      let expandT = 7119346 >= aboutB.length;
      do {
          let plashv: Map<any, any> = new Map([[String.fromCharCode(114,101,109,98,0),462], [String.fromCharCode(116,111,107,101,110,116,120,0),427]]);
         aboutB = `${aboutB.length * parseInt(`${cross5}`)}`;
         plashv = new Map([[`${plashv.size}`, 2]]);
         if (expandT) {
            break;
         }
      } while (expandT && ((5.53 * switch_e1) > 2.70 && (aboutB.length ^ 2) > 3));
         aboutB = `${aboutB.length}`;
         aboutB = `${parseInt(`${cross5}`)}`;
       let const_03B: Map<any, any> = new Map([[String.fromCharCode(99,117,109,117,108,97,116,105,118,101,0),558], [String.fromCharCode(114,101,113,117,101,115,116,97,98,108,101,0),657], [String.fromCharCode(103,101,116,108,98,108,111,99,107,105,110,99,0),638]]);
       let shirtL: Map<any, any> = new Map([[String.fromCharCode(116,111,110,101,109,97,112,0),272], [String.fromCharCode(97,99,116,105,118,97,116,105,111,110,0),895]]);
          let searchz = String.fromCharCode(112,111,105,110,116,99,98,98,0);
          let update_rtW = String.fromCharCode(119,104,105,116,101,115,0);
          let lessl = String.fromCharCode(114,110,110,111,105,115,101,0);
         shirtL = new Map([[`${shirtL.size}`, (String.fromCharCode(106,0) == searchz ? shirtL.size : searchz.length)]]);
         update_rtW += `${(String.fromCharCode(76,0) == update_rtW ? update_rtW.length : lessl.length)}`;
         lessl += `${lessl.length + update_rtW.length}`;
       let chatR: Array<any> = [String.fromCharCode(115,119,114,101,115,97,109,112,108,101,114,101,115,0), String.fromCharCode(100,105,115,97,98,108,101,100,0), String.fromCharCode(99,108,105,112,112,105,110,103,110,111,100,101,0)];
      for (let f = 0; f < 2; f++) {
          let guidec = 4;
          let accepted9 = String.fromCharCode(112,116,114,0);
          let profileW: Map<any, any> = new Map([[String.fromCharCode(105,109,108,116,95,97,95,51,48,0),786], [String.fromCharCode(109,97,105,108,99,104,105,109,112,0),997]]);
          let bootsplash0: Map<any, any> = new Map([[String.fromCharCode(115,104,101,101,114,118,105,100,101,111,100,97,116,97,0),false ], [String.fromCharCode(105,100,99,105,110,0),false ]]);
         chatR.push(2);
         guidec >>= Math.min(Math.abs(3 | accepted9.length), 3);
         accepted9 = "1";
         profileW.set(`${guidec}`, profileW.size % (Math.max(3, 7)));
         bootsplash0.set(`${bootsplash0.size}`, 3 * bootsplash0.size);
      }
          let routerI = true;
          let playS = String.fromCharCode(115,116,97,116,101,112,0);
          let weiboz = String.fromCharCode(116,114,97,110,115,105,101,110,116,0);
         chatR.push(chatR.length & 2);
         routerI = (59 < ((!routerI ? playS.length : 59) + playS.length));
         weiboz += `${(String.fromCharCode(88,0) == playS ? (routerI ? 3 : 2) : playS.length)}`;
      filledG -= parseInt(`${downloadedJ}`);
   if ((downloadedJ + 1.5) == 2.4 && 4.44 == (downloadedJ + 1.5)) {
       let matchesZ = 1.0;
       let show_ = 5;
      while ((matchesZ / 4.93) == 4.48) {
          let reporto = String.fromCharCode(109,105,110,105,109,105,122,101,97,98,108,101,0);
         show_ <<= Math.min(Math.abs(show_), 4);
         reporto = `${(reporto == String.fromCharCode(76,0) ? reporto.length : reporto.length)}`;
         break;
      }
      while ((matchesZ - 3.43) < 1.68 && 1.18 < (matchesZ - 3.43)) {
          let taiwanb = String.fromCharCode(102,112,115,0);
          let leftj = 3;
          let topic6 = String.fromCharCode(115,112,108,105,116,115,0);
         matchesZ -= parseFloat(`${show_ % 1}`);
         taiwanb += `${leftj | 1}`;
         leftj -= 2;
         topic6 += `${taiwanb.length ^ leftj}`;
         break;
      }
      for (let r = 0; r < 2; r++) {
         matchesZ *= parseFloat(`${2 + show_}`);
      }
      while (1.20 > (show_ * matchesZ)) {
         show_ *= 1 >> (Math.min(Math.abs(show_), 3));
         break;
      }
         show_ /= Math.max(3, 2);
         matchesZ += parseFloat(`${1 / (Math.max(show_, 3))}`);
      text6 *= parseInt(`${matchesZ}`);
   }

    return parts.map((part, index) => {
      if (
        part.toLowerCase() === keyword1.toLowerCase() ||
        (keyword2 && part.toLowerCase() === keyword2.toLowerCase())
      ) {

      c_unlockH = new Map([[`${countdownr.size}`, 2 >> (Math.min(5, Math.abs(countdownr.size)))]]);
   for (let p = 0; p < 2; p++) {
      minimizeS += parseFloat(`${1}`);
   }
        return (
          <Text
            key={index}
            style={{
              ...textVariants.body,
              color: colors.yellow,
              fontStyle: 'italic',
              fontWeight: '700',
              fontSize: 22,
            }}>
            {part}
          </Text>
        );
      } else {

   for (let w = 0; w < 2; w++) {
       let configw = String.fromCharCode(114,101,111,114,100,101,114,0);
       let cornerO = true;
      while (!configw.includes(`${cornerO}`)) {
          let updatesi = 3.0;
          let indicatorM = 4.0;
          let componentD = 3.0;
          let homet = String.fromCharCode(108,111,97,100,120,95,101,95,51,57,0);
         configw += "3";
         updatesi /= Math.max(4, parseFloat(`${parseInt(`${indicatorM}`)}`));
         componentD /= Math.max(homet.length / 1, 1);
         homet = `${parseInt(`${componentD}`)}`;
         break;
      }
      for (let h = 0; h < 2; h++) {
         cornerO = !configw.startsWith(`${cornerO}`);
      }
      let acceptedJ = cornerO ? !cornerO : cornerO;
      do {
         cornerO = (89 >= (configw.length * (!cornerO ? configw.length : 89)));
         if (acceptedJ) {
            break;
         }
      } while ((!cornerO) && acceptedJ);
         cornerO = configw.length <= 65;
       let lessD = String.fromCharCode(102,102,116,115,0);
         lessD = `${3 ^ configw.length}`;
      c_unlockH.set(`${filledG}`, parseInt(`${filledG}`) - 2);
   }
      ballj = (parseInt(`${darkc}`) * c_unlockH.size) <= 67;
        return (
          <Text
            key={index}
            style={{ ...textVariants.body, color: '#FFFFFF', fontSize: 20 }}>
            {part}
          </Text>
        );
      }
    });
  };

   if (filledG > 5.28) {
      filledG += 1;
   }
       let entryU = String.fromCharCode(97,118,112,114,105,118,0);
      for (let m = 0; m < 3; m++) {
         entryU += `${(entryU == String.fromCharCode(116,0) ? entryU.length : entryU.length)}`;
      }
      while (entryU == String.fromCharCode(66,0) && entryU.length >= 5) {
         entryU = `${(entryU == String.fromCharCode(115,0) ? entryU.length : entryU.length)}`;
         break;
      }
      if (entryU.length >= entryU.length) {
          let shootn = String.fromCharCode(100,105,118,109,111,100,0);
          let anythinkn = String.fromCharCode(113,95,52,49,95,100,97,121,0);
         entryU = `${3 * shootn.length}`;
         shootn = `${anythinkn.length}`;
         anythinkn = `${3 | anythinkn.length}`;
      }
      downloadedJ /= Math.max(3, (2 | (promotion5 ? 5 : 1)));
  return (
    <>
      <View
        style={{
          paddingTop: 10,
          paddingBottom: 15,
          flexDirection: 'column',
          justifyContent: 'center',
          alignItems: 'center',
          gap: 9,
        }}>
        <Text style={{ fontSize: 22, color: colors.yellow, fontWeight: '700' }}>
          双赢奖励
        </Text>

        <Text>{highlightText('每邀请1位好友 增加30天VIP', '1', '30')}</Text>

        <Text>{highlightText('好友获得30天VIP', '30')}</Text>
      </View>
    </>
  );
}

const styles = StyleSheet.create({});
