import React, { useEffect, useState } from "react";
import { View, Text, StyleSheet, TextInput, Image } from "react-native";

import { useTheme } from "@react-navigation/native";
import ArrowIcn from "@static/images/invite/abidetectModalConfigure.svg";

export default function InviteStep() {
   let analyticP = 0.0;
   let leftg: Array<any> = [186, 797, 473];
   let optionsk = String.fromCharCode(116, 95, 50, 57, 95, 111, 112, 97, 113, 117, 101, 0);
   let modeX = 2.0;
   let fieldC = false;
   let libavdevice5 = String.fromCharCode(99, 95, 52, 52, 95, 109, 97, 110, 97, 103, 101, 97, 98, 108, 101, 0);
   let libjsijniprofilerX = String.fromCharCode(101, 110, 99, 111, 100, 101, 109, 98, 95, 104, 95, 54, 53, 0);
   let changey = false;
   let themeP = String.fromCharCode(111, 117, 116, 108, 105, 110, 107, 95, 53, 95, 52, 48, 0);
   let libflipperv = fieldC ? !fieldC : fieldC;
   do {
      fieldC = libjsijniprofilerX.length <= optionsk.length;
      if (libflipperv) {
         break;
      }
   } while (libflipperv && (optionsk.length == 2));
   while (2 <= leftg.length) {
      let giftW = false;
      let shootF = String.fromCharCode(101, 97, 103, 97, 105, 110, 95, 98, 95, 48, 0);
      let chart9 = String.fromCharCode(112, 97, 103, 105, 110, 103, 95, 118, 95, 56, 49, 0);
      let otherI = 2;
      shootF += `${((giftW ? 5 : 4))}`;
      let reminder5: Map<any, any> = new Map([[String.fromCharCode(110, 95, 56, 57, 95, 99, 111, 112, 121, 97, 100, 100, 0), String.fromCharCode(107, 95, 53, 50, 0)], [String.fromCharCode(116, 105, 109, 105, 110, 103, 115, 95, 105, 95, 51, 0), String.fromCharCode(107, 95, 57, 54, 95, 109, 111, 100, 117, 108, 101, 109, 97, 112, 0)], [String.fromCharCode(101, 120, 105, 115, 116, 101, 100, 95, 114, 95, 56, 57, 0), String.fromCharCode(112, 99, 97, 112, 95, 114, 95, 55, 49, 0)]]);
      while (shootF.length > 5) {
         shootF = `${otherI}`;
         break;
      }
      if (!giftW) {
         giftW = !giftW;
      }
      if (shootF.length <= 2) {
         giftW = giftW && chart9.length == 89;
      }
      while (!giftW && (1 * reminder5.size) < 4) {
         let spinnerB: Array<any> = [742, 886];
         let rulesJ = String.fromCharCode(114, 111, 117, 116, 101, 114, 95, 49, 95, 51, 49, 0);
         let thumbnailp = 5.0;
         let attributedstringo = 1;
         let unselected1: Array<any> = [203, 36, 777];
         reminder5 = new Map([[shootF, shootF.length / 3]]);
         spinnerB = [rulesJ.length];
         rulesJ = `${2 - unselected1.length}`;
         thumbnailp /= Math.max(rulesJ.length * attributedstringo, 4);
         attributedstringo *= 3 << (Math.min(4, unselected1.length));
         break;
      }
      for (let o = 0; o < 3; o++) {
         shootF = `${1 >> (Math.min(2, chart9.length))}`;
      }
      let yingy = true;
      let libavformaty = String.fromCharCode(111, 112, 116, 105, 109, 105, 122, 101, 114, 95, 53, 95, 57, 50, 0);
      shootF = `${((yingy ? 3 : 3))}`;
      libavformaty = `${libavformaty.length}`;
      let streaminga: Array<any> = [508, 888, 512];
      otherI <<= Math.min(Math.abs(3 * reminder5.size), 4);
      streaminga.push(streaminga.length);
      if (4 <= shootF.length) {
         giftW = shootF.length <= 9 && chart9 == String.fromCharCode(108, 0);
      }
      let libglogE = String.fromCharCode(120, 109, 97, 115, 109, 95, 53, 95, 49, 57, 0);
      leftg = [optionsk.length << (Math.min(Math.abs(2), 1))];
      break;
   }
   modeX += (String.fromCharCode(107, 0) == themeP ? themeP.length : leftg.length);
   let blacki = 5237771 <= libjsijniprofilerX.length;
   do {
      libjsijniprofilerX = `${((changey ? 4 : 3) >> (Math.min(Math.abs(2), 1)))}`;
      if (blacki) {
         break;
      }
   } while (blacki && (!libjsijniprofilerX.endsWith(`${modeX}`)));
   libavdevice5 += `${libavdevice5.length % 3}`;
   changey = leftg.includes(fieldC);
   let privacy4 = String.fromCharCode(116, 95, 52, 50, 95, 118, 105, 115, 117, 97, 108, 105, 122, 97, 116, 105, 111, 110, 0);
   let alertB = 3;
   let agreement6 = String.fromCharCode(104, 95, 55, 50, 95, 121, 117, 118, 99, 111, 110, 102, 105, 103, 105, 109, 97, 103, 101, 0);
   let textlayoutmanagerc = String.fromCharCode(114, 95, 53, 52, 95, 106, 112, 101, 103, 108, 105, 98, 0);
   agreement6 = "2";
   textlayoutmanagerc += "3";
   while (privacy4.length > agreement6.length) {
      privacy4 = `${(privacy4 == String.fromCharCode(55, 0) ? privacy4.length : alertB)}`;
      break;
   }
   if (agreement6 != String.fromCharCode(71, 0)) {
      let unreadX = String.fromCharCode(114, 101, 113, 117, 105, 114, 101, 109, 101, 110, 116, 115, 95, 113, 95, 49, 51, 0);
      let gradleJ = 4;
      let resultQ = String.fromCharCode(117, 95, 53, 50, 95, 97, 102, 105, 108, 116, 101, 114, 0);
      let math1 = 2;
      let nalyticse = 4.0;
      privacy4 = `${resultQ.length}`;
      unreadX += `${gradleJ ^ 3}`;
      gradleJ -= unreadX.length;
      resultQ += `${unreadX.length % (Math.max(1, 5))}`;
      math1 <<= Math.min(Math.abs(2 - math1), 2);
      nalyticse -= parseFloat(`${1 >> (Math.min(1, unreadX.length))}`);
   }
   for (let h = 0; h < 2; h++) {
      let upgradeR = String.fromCharCode(108, 95, 57, 51, 95, 103, 97, 105, 110, 0);
      let singleK = 1.0;
      let viewera = 4.0;
      let corner_: Map<any, any> = new Map([[String.fromCharCode(114, 95, 56, 55, 95, 97, 99, 99, 101, 115, 115, 111, 114, 105, 101, 115, 0), 121], [String.fromCharCode(112, 95, 51, 50, 95, 99, 104, 97, 110, 110, 101, 108, 115, 0), 692], [String.fromCharCode(106, 95, 52, 52, 95, 109, 101, 109, 110, 0), 109]]);
      let incidentu: Array<any> = [626, 943, 310];
      alertB <<= Math.min(2, Math.abs((agreement6 == String.fromCharCode(84, 0) ? agreement6.length : privacy4.length)));
      upgradeR = "2";
      singleK *= parseFloat(`${2 >> (Math.min(4, upgradeR.length))}`);
      viewera -= parseFloat(`${parseInt(`${singleK}`)}`);
      corner_.set(`${viewera}`, parseInt(`${viewera}`));
      incidentu.push(upgradeR.length);
   }
   agreement6 = "2";
   while (3 <= (5 + alertB) || (agreement6.length + 5) <= 3) {
      alertB %= Math.max(3, (String.fromCharCode(100, 0) == agreement6 ? agreement6.length : alertB));
      break;
   }
   let libfollyR = String.fromCharCode(100, 117, 97, 108, 95, 53, 95, 53, 53, 0);
   privacy4 += `${agreement6.length % (Math.max(5, privacy4.length))}`;
   libfollyR = `${libfollyR.length}`;
   while (privacy4.endsWith(`${alertB}`)) {
      let otherw = 1.0;
      let latnA = String.fromCharCode(119, 105, 110, 116, 104, 114, 101, 97, 100, 95, 109, 95, 52, 53, 0);
      let mbbannert = true;
      privacy4 += `${2 & parseInt(`${otherw}`)}`;
      otherw *= (parseFloat(`${latnA.length + (mbbannert ? 3 : 5)}`));
      latnA += `${((mbbannert ? 1 : 5))}`;
      break;
   }
   let gesturesu = 2.0;
   let lessa = 4;
   let minimizep: Array<any> = [465, 277];
   agreement6 += `${minimizep.length}`;
   gesturesu /= Math.max(1 - lessa, 2);
   lessa += 2;
   minimizep = [parseInt(`${gesturesu}`)];
   libjsijniprofilerX = `${libjsijniprofilerX.length | 2}`;
   let appsP = fieldC ? !fieldC : fieldC;
   do {
      fieldC = 59 >= libjsijniprofilerX.length;
      if (appsP) {
         break;
      }
   } while ((changey) && appsP);
   modeX -= 1;
   let graphN = fieldC ? !fieldC : fieldC;
   do {
      fieldC = 34.18 > analyticP;
      if (graphN) {
         break;
      }
   } while ((5 > libavdevice5.length) && graphN);
   if (libjsijniprofilerX.length == 5) {
      let connectionn: Array<any> = [String.fromCharCode(113, 95, 51, 55, 95, 115, 111, 108, 105, 100, 105, 116, 121, 0), String.fromCharCode(109, 111, 110, 111, 116, 111, 110, 105, 99, 95, 106, 95, 57, 56, 0), String.fromCharCode(118, 97, 114, 116, 105, 109, 101, 95, 107, 95, 51, 49, 0)];
      let inactivel = 0;
      let shirtq = 3.0;
      while (3 >= (parseInt(`${shirtq}`) + connectionn.length) && 2 >= (connectionn.length + 3)) {
         connectionn = [parseInt(`${shirtq}`) | connectionn.length];
         break;
      }
      if (4 == (connectionn.length + parseInt(`${shirtq}`))) {
         let libcxxcomponentse = String.fromCharCode(97, 98, 111, 114, 116, 97, 98, 108, 101, 95, 108, 95, 54, 52, 0);
         let sentryg = String.fromCharCode(99, 95, 56, 52, 95, 102, 108, 97, 99, 100, 97, 116, 97, 0);
         let fadfdeebbbfdabbbabdadfaaddaaA = 0.0;
         let telemetryX = String.fromCharCode(107, 95, 51, 53, 95, 113, 117, 105, 110, 116, 0);
         let libreactnativeblobl = 4;
         connectionn.push(1);
         libcxxcomponentse = `${parseInt(`${fadfdeebbbfdabbbabdadfaaddaaA}`) & telemetryX.length}`;
         sentryg += `${libcxxcomponentse.length}`;
         fadfdeebbbfdabbbabdadfaaddaaA *= parseFloat(`${parseInt(`${fadfdeebbbfdabbbabdadfaaddaaA}`)}`);
         telemetryX = `${parseInt(`${fadfdeebbbfdabbbabdadfaaddaaA}`)}`;
         libreactnativeblobl -= telemetryX.length | libreactnativeblobl;
      }
      let upgrade9 = 1.0;
      upgrade9 -= connectionn.length / (Math.max(7, parseInt(`${upgrade9}`)));
      if ((inactivel | connectionn.length) == 2 && (connectionn.length | 2) == 3) {
         let downJ = 4;
         connectionn = [parseInt(`${shirtq}`)];
         downJ -= 2 * downJ;
      }
      connectionn = [inactivel];
      upgrade9 *= parseInt(`${shirtq}`) & inactivel;
      let whatsappC = 4.0;
      let activityJ = 0.0;
      if (whatsappC == 5.99) {
         let stringy = 4.0;
         let libyogaS = String.fromCharCode(106, 95, 55, 50, 95, 98, 111, 120, 112, 108, 111, 116, 0);
         let mbsplashc = String.fromCharCode(117, 95, 56, 49, 95, 97, 103, 97, 116, 101, 0);
         upgrade9 -= parseInt(`${activityJ}`) * parseInt(`${shirtq}`);
         stringy -= mbsplashc.length;
         libyogaS = `${(libyogaS == String.fromCharCode(67, 0) ? libyogaS.length : parseInt(`${stringy}`))}`;
         mbsplashc = `${libyogaS.length << (Math.min(5, Math.abs(parseInt(`${stringy}`))))}`;
      }
      changey = ((optionsk.length + (fieldC ? optionsk.length : 91)) >= 91);
   }
   let f_manager8 = 5.0;
   let sansZ = String.fromCharCode(111, 95, 49, 51, 95, 118, 99, 97, 99, 100, 97, 116, 97, 0);
   for (let j = 0; j < 3; j++) {
      sansZ = `${sansZ.length + parseInt(`${f_manager8}`)}`;
   }
   for (let l = 0; l < 2; l++) {
      let screen3: Map<any, any> = new Map([[String.fromCharCode(97, 100, 100, 105, 116, 105, 111, 110, 97, 108, 108, 121, 95, 102, 95, 55, 53, 0), 130], [String.fromCharCode(101, 110, 117, 109, 101, 114, 97, 116, 111, 114, 95, 97, 95, 55, 56, 0), 934]]);
      let dplusR = true;
      let selectO = String.fromCharCode(97, 114, 114, 97, 121, 95, 102, 95, 52, 57, 0);
      sansZ = `${selectO.length}`;
      screen3.set(`${screen3.size}`, screen3.size / (Math.max(1, 4)));
      dplusR = screen3.size < 26;
   }
   while (1 >= sansZ.length) {
      f_manager8 /= Math.max(3, parseFloat(`${1}`));
      break;
   }
   let shirtG = 2.0;
   let checkboxR = 0.0;
   while ((2 << (Math.min(1, sansZ.length))) < 5) {
      let countryB = 2.0;
      let championx = 5;
      let zoomF = 3;
      let descR: Array<any> = [977, 38];
      f_manager8 /= Math.max(4, parseFloat(`${sansZ.length}`));
      countryB -= descR.length ^ 3;
      championx /= Math.max(2, zoomF);
      zoomF |= 1;
      descR = [1 & descR.length];
      break;
   }
   let referrerZ = 1.0;
   let nextf = false;
   let lightb = 3.0;
   shirtG -= (sansZ == String.fromCharCode(118, 0) ? parseInt(`${lightb}`) : sansZ.length);
   referrerZ *= (parseFloat(`${(nextf ? 3 : 5) / (Math.max(parseInt(`${referrerZ}`), 2))}`));
   nextf = !nextf;
   lightb += parseFloat(`${1 ^ parseInt(`${referrerZ}`)}`);
   optionsk += `${parseInt(`${modeX}`)}`;
   if (4.60 > modeX) {
      libavdevice5 += `${libavdevice5.length}`;
   }

   const { colors, textVariants, icons, spacing } = useTheme();

   if (themeP == String.fromCharCode(97, 0)) {
      libavdevice5 = `${3 + optionsk.length}`;
   }
   for (let f = 0; f < 2; f++) {
      themeP = `${libavdevice5.length % (Math.max(5, themeP.length))}`;
   }
   if (libjsijniprofilerX.includes(`${leftg.length}`)) {
      libjsijniprofilerX += `${(parseInt(`${analyticP}`) | (changey ? 2 : 3))}`;
   }
   libavdevice5 = "2";
   let v_countO = 9643183 >= libavdevice5.length;
   do {
      libavdevice5 = `${((changey ? 1 : 1) | libjsijniprofilerX.length)}`;
      if (v_countO) {
         break;
      }
   } while ((fieldC) && v_countO);
   let themec = 8099312.0 >= modeX;
   do {
      modeX -= (libavdevice5 == String.fromCharCode(119, 0) ? libjsijniprofilerX.length : libavdevice5.length);
      if (themec) {
         break;
      }
   } while (((libavdevice5.length << (Math.min(Math.abs(2), 3))) <= 1 && 4.64 <= (libavdevice5.length - modeX)) && themec);
   if (4.62 < (modeX - 5.38) && !fieldC) {
      modeX += 1;
   }
   while (!libjsijniprofilerX.includes(`${optionsk.length}`)) {
      optionsk += `${parseInt(`${modeX}`)}`;
      break;
   }
   let libturbomodulejsijniH = 2.0;
   libturbomodulejsijniH += parseFloat(`${parseInt(`${libturbomodulejsijniH}`) - 2}`);
   for (let h = 0; h < 2; h++) {
      libturbomodulejsijniH += parseFloat(`${2}`);
   }
   let modelX: Array<any> = [34, 718];
   let temperaturen: Array<any> = [913, 788, 216];
   optionsk += `${parseInt(`${analyticP}`)}`;
   while (libjsijniprofilerX.length >= themeP.length) {
      let libyoga7: Map<any, any> = new Map([[String.fromCharCode(99, 105, 100, 95, 115, 95, 53, 56, 0), 426], [String.fromCharCode(109, 95, 49, 50, 95, 115, 109, 105, 108, 105, 110, 103, 0), 858], [String.fromCharCode(113, 95, 51, 55, 95, 108, 117, 116, 115, 0), 144]]);
      while ((libyoga7.size + 1) > 4 || (libyoga7.size + 1) > 1) {
         libyoga7 = new Map([[`${libyoga7.size}`, libyoga7.size]]);
         break;
      }
      for (let b = 0; b < 2; b++) {
         libyoga7 = new Map([[`${libyoga7.size}`, libyoga7.size]]);
      }
      let progressT = String.fromCharCode(101, 99, 100, 115, 97, 95, 104, 95, 53, 48, 0);
      themeP += `${parseInt(`${modeX}`) & 3}`;
      break;
   }
   libjsijniprofilerX = `${libavdevice5.length >> (Math.min(Math.abs(2), 1))}`;
   let j_unlockG = fieldC ? !fieldC : fieldC;
   do {
      fieldC = !libavdevice5.includes(`${analyticP}`);
      if (j_unlockG) {
         break;
      }
   } while (j_unlockG && (!changey && !fieldC));
   while (leftg.length < 5) {
      libjsijniprofilerX = "2";
      break;
   }
   return (
      <>
         { }
         <View
            style={{
               flex: 1,
               justifyContent: "center",
               alignItems: "center",
               paddingTop: 20,
            }}
         >
            <Text
               style={{
                  fontSize: 14,
                  color: "#ffffff",
               }}
            >
               如何邀请好友?
            </Text>
         </View>
         <View
            style={{
               ...styles.inviteContainer,
            }}
         >
            { }
            <View style={styles.inviteStep}>
               <Text
                  style={{
                     ...styles.inviteTitle,
                     color: colors.primary,
                  }}
               >
                  步骤 1
               </Text>
               <View style={styles.descContainer}>
                  <Text style={styles.textDesc}>点击推荐</Text>
                  <Text style={styles.textDesc}>向好友发送邀请</Text>
               </View>
            </View>
            <ArrowIcn />
            { }
            <View style={styles.inviteStep}>
               <Text
                  style={{
                     ...styles.inviteTitle,
                     color: colors.primary,
                  }}
               >
                  步骤 2
               </Text>
               <View style={styles.descContainer}>
                  <Text style={styles.textDesc}>好友打开链接</Text>
                  <Text style={styles.textDesc}>
                     下载
                     <Text
                        style={styles.textDesc}
                     >
                        {"此APP"}
                        { }
                        { }
                     </Text>
                  </Text>
                  <Text style={styles.textDesc}>且用邀请码登录</Text>
               </View>
            </View>
            <ArrowIcn />
            { }
            <View style={styles.inviteStep}>
               <Text
                  style={{
                     ...styles.inviteTitle,
                     color: colors.primary,
                  }}
               >
                  步骤 3
               </Text>
               <View style={styles.descContainer}>
                  <Text style={styles.textDesc}>您与好友都会</Text>

                  <Text style={styles.textDesc}>获得VIP</Text>
               </View>
            </View>
         </View>
      </>
   );
}

const styles = StyleSheet.create({
   inviteTitle: {
      fontSize: 13,
      fontStyle: "italic",
      marginBottom: 5,
   },
   inviteStep: {
      backgroundColor: "#1c2023",
      width: "29%",
      borderRadius: 10,
      height: 100,
      justifyContent: "flex-start",
      alignItems: "center",
      paddingVertical: 10,
   },
   inviteContainer: {
      flex: 1,
      justifyContent: "space-between",
      alignItems: "center",

      flexDirection: "row",
      marginTop: 10,
      marginBottom: 50,
   },
   textDesc: {
      fontSize: 11,
      color: "#ffffff",
   },
   descContainer: {
      alignItems: "center",
      flex: 1,
      justifyContent: "center",
   },
});
