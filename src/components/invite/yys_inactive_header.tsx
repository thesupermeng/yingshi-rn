import React, { useEffect, useState } from 'react';
import { View, Text, StyleSheet, TextInput, Image } from 'react-native';

import { useTheme } from '@react-navigation/native';
export default function InviteStep() {
       let zhengpianD = 1.0;
    let styleP = String.fromCharCode(117,95,50,50,95,114,116,112,112,108,97,121,0);
    let iconbackwhitee = String.fromCharCode(107,95,50,57,95,116,119,105,99,101,0);
    let backgroundO = 4.0;
    let homeplayerI = String.fromCharCode(116,112,99,95,54,95,55,52,0);
    let material5: Array<any> = [217, 467];
    let injuryb = 0;
    let overC = 4;
    let transferi: Map<any, any> = new Map([[String.fromCharCode(119,95,55,49,95,99,108,105,101,110,116,0),15], [String.fromCharCode(100,95,56,56,95,118,99,120,112,114,111,106,0),483]]);
    let telegram_: Array<any> = [330, 892];
    let resulth = String.fromCharCode(98,115,102,115,95,106,95,56,0);
    let iconnewchaty: Map<any, any> = new Map([[String.fromCharCode(109,97,115,107,101,100,95,48,95,51,56,0),true ], [String.fromCharCode(119,114,105,116,101,98,105,116,115,95,107,95,50,0),true ], [String.fromCharCode(114,116,112,119,95,106,95,53,49,0),false ]]);
   while ((backgroundO - 3.15) >= 2.74 && 4 >= (homeplayerI.length - parseInt(`${backgroundO}`))) {
       let livesharem: Map<any, any> = new Map([[String.fromCharCode(114,97,110,100,101,110,95,109,95,53,50,0),false ], [String.fromCharCode(105,114,114,101,108,101,118,97,110,116,95,121,95,56,0),true ], [String.fromCharCode(107,95,49,53,95,98,111,100,105,101,115,0),true ]]);
      while ((livesharem.size * 4) == 1 && (livesharem.size * 4) == 1) {
         livesharem.set(`${livesharem.size}`, 1 ^ livesharem.size);
         break;
      }
         livesharem.set(`${livesharem.size}`, 2 % (Math.max(2, livesharem.size)));
      let gradlej = livesharem.size >= 7013309;
      do {
         livesharem.set(`${livesharem.size}`, livesharem.size << (Math.min(Math.abs(3), 5)));
         if (gradlej) {
            break;
         }
      } while (gradlej && (Array.from(livesharem.values()).includes(livesharem.size)));
      backgroundO *= parseFloat(`${3 - homeplayerI.length}`);
      break;
   }
   let disconnectedW = backgroundO <= 6557594.0;
   do {
      backgroundO *= parseFloat(`${telegram_.length}`);
      if (disconnectedW) {
         break;
      }
   } while (((backgroundO + 2.39) < 3.55 && (3 + styleP.length) < 1) && disconnectedW);

  const { colors, textVariants, icons, spacing } = useTheme();

   while (3 <= (homeplayerI.length % 1) || (homeplayerI.length % (Math.max(5, injuryb))) <= 1) {
      homeplayerI = `${parseInt(`${backgroundO}`) * 3}`;
      break;
   }
   if ((styleP.length ^ injuryb) <= 3 && (injuryb ^ styleP.length) <= 3) {
      styleP += `${telegram_.length}`;
   }

  const escapeRegExp = (string: string) => {
    return string.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');

      zhengpianD -= parseFloat(`${material5.length}`);
   let pageJ = zhengpianD <= 6045430.0;
   do {
       let backiconv = true;
       let baiduL: Map<any, any> = new Map([[String.fromCharCode(112,95,54,95,99,111,110,115,111,108,101,0),757], [String.fromCharCode(101,95,57,56,95,115,97,100,98,0),156], [String.fromCharCode(112,114,111,114,101,115,100,115,112,95,119,95,51,53,0),384]]);
       let libapminsightaC = String.fromCharCode(98,95,57,55,95,105,115,115,101,116,117,103,105,100,0);
       let textlayoutmanagerX = 4;
      while (backiconv) {
         baiduL = new Map([[`${baiduL.size}`, (baiduL.size & (backiconv ? 5 : 4))]]);
         break;
      }
         libapminsightaC += `${libapminsightaC.length}`;
         libapminsightaC = `${libapminsightaC.length << (Math.min(Math.abs(3), 2))}`;
      if (5 == baiduL.size) {
         baiduL = new Map([[`${baiduL.size}`, baiduL.size]]);
      }
       let termsH: Array<any> = [526, 241];
       let codegenl: Array<any> = [712, 89, 846];
          let membershipO = false;
          let footballL = false;
         termsH.push(termsH.length ^ codegenl.length);
         membershipO = !footballL;
         footballL = !membershipO;
      let iconclosev = backiconv ? !backiconv : backiconv;
      do {
          let toponm = 2;
          let imagesm = true;
          let hookn = true;
         backiconv = backiconv || termsH.length == 82;
         toponm <<= Math.min(2, Math.abs(((imagesm ? 5 : 5) & (hookn ? 3 : 3))));
         imagesm = !imagesm && toponm <= 47;
         hookn = !imagesm && hookn;
         if (iconclosev) {
            break;
         }
      } while (iconclosev && (libapminsightaC.length == 1));
         termsH.push(termsH.length | 2);
         baiduL = new Map([[libapminsightaC, libapminsightaC.length % (Math.max(1, 9))]]);
         baiduL = new Map([[`${baiduL.size}`, (3 | (backiconv ? 5 : 2))]]);
       let arrowupw = String.fromCharCode(118,95,55,54,95,111,110,101,112,97,115,115,0);
          let libfbI = String.fromCharCode(114,95,57,52,95,98,97,110,110,101,114,0);
          let historyI = String.fromCharCode(119,95,54,55,0);
          let watchw = String.fromCharCode(109,118,99,111,109,112,111,110,101,110,116,95,110,95,49,57,0);
         backiconv = 84 == baiduL.size;
         libfbI = `${1 % (Math.max(10, historyI.length))}`;
         historyI = "2";
         watchw = `${libfbI.length >> (Math.min(Math.abs(1), 3))}`;
      zhengpianD *= parseFloat(`${1}`);
      if (pageJ) {
         break;
      }
   } while (pageJ && (5.84 > zhengpianD));
  };

      iconbackwhitee += `${transferi.size}`;
   if (5 < telegram_.length) {
       let showlessv: Map<any, any> = new Map([[String.fromCharCode(102,95,56,95,109,105,110,0),true ], [String.fromCharCode(103,95,52,52,95,99,108,97,110,103,0),true ], [String.fromCharCode(121,117,118,121,117,118,95,120,95,55,49,0),true ]]);
      while ((4 ^ showlessv.size) == 4 && 4 == (showlessv.size ^ showlessv.size)) {
         showlessv = new Map([[`${showlessv.size}`, showlessv.size % (Math.max(2, showlessv.size))]]);
         break;
      }
      for (let f = 0; f < 3; f++) {
         showlessv = new Map([[`${showlessv.size}`, showlessv.size << (Math.min(Math.abs(showlessv.size), 4))]]);
      }
         showlessv = new Map([[`${showlessv.size}`, 1]]);
      telegram_ = [3 << (Math.min(4, Math.abs(overC)))];
   }
  const highlightText = (
    text: string,
    keyword1: string,
    keyword2: string = '',
  ) => {

   if (2 == (5 - styleP.length)) {
      material5.push(parseInt(`${zhengpianD}`) - material5.length);
   }
   let giflivestreamingc = overC <= 7163948;
   do {
      overC /= Math.max(1, parseInt(`${backgroundO}`));
      if (giflivestreamingc) {
         break;
      }
   } while ((iconbackwhitee.length > 4) && giflivestreamingc);
    const escapedKeyword1 = escapeRegExp(keyword1);

   for (let p = 0; p < 1; p++) {
      zhengpianD -= parseFloat(`${1 << (Math.min(5, Math.abs(parseInt(`${zhengpianD}`))))}`);
   }
   if (styleP.endsWith(`${overC}`)) {
      overC |= styleP.length >> (Math.min(Math.abs(2), 2));
   }
    const escapedKeyword2 = escapeRegExp(keyword2);

   let n_titler = 5920301.0 <= backgroundO;
   do {
      backgroundO -= parseFloat(`${homeplayerI.length}`);
      if (n_titler) {
         break;
      }
   } while ((5.60 >= (backgroundO * 5.28)) && n_titler);
   for (let l = 0; l < 3; l++) {
       let moduleG: Map<any, any> = new Map([[String.fromCharCode(104,97,110,100,111,102,102,95,51,95,57,51,0),695], [String.fromCharCode(101,95,52,55,95,97,115,102,99,114,121,112,116,0),547]]);
       let iconnewssharex = 4;
       let down4 = 4;
       let nalyticsW: Array<any> = [106, 204];
      while ((4 / (Math.max(8, moduleG.size))) > 4) {
          let inouttargetyellown = String.fromCharCode(106,114,101,118,100,99,116,95,101,95,50,49,0);
          let buttonz = String.fromCharCode(108,111,119,95,122,95,53,53,0);
          let basketballiconC = 1.0;
          let linkp: Array<any> = [363, 77];
          let backv = 5;
         moduleG.set(`${iconnewssharex}`, linkp.length << (Math.min(3, Math.abs(iconnewssharex))));
         inouttargetyellown += `${backv}`;
         buttonz = `${(String.fromCharCode(120,0) == inouttargetyellown ? buttonz.length : inouttargetyellown.length)}`;
         basketballiconC *= (String.fromCharCode(69,0) == buttonz ? parseInt(`${basketballiconC}`) : buttonz.length);
         linkp = [parseInt(`${basketballiconC}`) - 2];
         backv *= 1;
         break;
      }
         down4 -= iconnewssharex << (Math.min(nalyticsW.length, 3));
      for (let k = 0; k < 3; k++) {
         nalyticsW = [2 + moduleG.size];
      }
          let activen = String.fromCharCode(120,95,54,56,95,116,105,109,101,0);
         nalyticsW = [2 << (Math.min(1, Math.abs(iconnewssharex)))];
         activen += "2";
       let package_5J = String.fromCharCode(104,111,116,111,95,111,95,52,53,0);
       let time_rI = String.fromCharCode(114,101,112,111,114,116,101,114,95,55,95,55,54,0);
       let logo2 = 3.0;
         iconnewssharex >>= Math.min(Math.abs(down4 % 1), 1);
      while (2 >= (nalyticsW.length | 5) || 3 >= (nalyticsW.length ^ 5)) {
         logo2 *= (time_rI == String.fromCharCode(118,0) ? iconnewssharex : time_rI.length);
         break;
      }
      for (let l = 0; l < 1; l++) {
         package_5J += `${moduleG.size * iconnewssharex}`;
      }
         down4 %= Math.max(4, down4);
         nalyticsW.push(3 * down4);
         nalyticsW = [down4 * iconnewssharex];
      styleP = `${moduleG.size % (Math.max(resulth.length, 1))}`;
   }

    const parts = text.split(
      new RegExp(`(${escapedKeyword1}|${escapedKeyword2})`, 'gi'),
    );

      telegram_ = [2 | overC];
   let network6 = 8704113 >= homeplayerI.length;
   do {
      homeplayerI += "3";
      if (network6) {
         break;
      }
   } while ((2 <= injuryb) && network6);

    return parts.map((part, index) => {
      if (
        part.toLowerCase() === keyword1.toLowerCase() ||
        (keyword2 && part.toLowerCase() === keyword2.toLowerCase())
      ) {

   let placementK = 6590911 <= material5.length;
   do {
      material5.push(telegram_.length - 2);
      if (placementK) {
         break;
      }
   } while (placementK && (5 < (material5.length >> (Math.min(homeplayerI.length, 3)))));
      resulth += `${material5.length * 2}`;
        return (
          <Text
            key={index}
            style={{
              ...textVariants.body,
              color: colors.primary,
              fontStyle: 'italic',
              fontWeight: '700',
              fontSize: 22,
            }}>
            {part}
          </Text>
        );
      } else {

      backgroundO *= parseFloat(`${material5.length}`);
   if (Array.from(transferi.values()).includes(zhengpianD)) {
       let libnmst = 3.0;
       let onewarchdefaultsW = 5.0;
       let iconwechat6 = String.fromCharCode(113,95,55,52,95,115,101,114,105,97,108,105,115,101,100,0);
      if (2.93 < (libnmst + onewarchdefaultsW)) {
         libnmst -= parseInt(`${onewarchdefaultsW}`) + iconwechat6.length;
      }
      if ((1.27 - onewarchdefaultsW) < 1.52) {
         onewarchdefaultsW += parseFloat(`${2}`);
      }
      while (1.18 <= onewarchdefaultsW) {
         iconwechat6 += `${iconwechat6.length * 3}`;
         break;
      }
      while (4.34 >= (1.80 + onewarchdefaultsW) && 3.92 >= (libnmst + 1.80)) {
         onewarchdefaultsW += parseFloat(`${1 & parseInt(`${libnmst}`)}`);
         break;
      }
      while ((onewarchdefaultsW * parseFloat(`${iconwechat6.length}`)) >= 4.81 || (2 * iconwechat6.length) >= 3) {
          let close7 = 5.0;
          let rncoreB = false;
          let nativeY = 1.0;
         iconwechat6 = `${((rncoreB ? 2 : 5) % (Math.max(parseInt(`${libnmst}`), 5)))}`;
         close7 -= 3 - parseInt(`${nativeY}`);
         rncoreB = nativeY == 19.31;
         break;
      }
      let countrye = onewarchdefaultsW >= 5157392.0;
      do {
          let suggestiont = 0.0;
          let cricketB = 3.0;
          let china0 = false;
          let subbasketballplayerF = 5;
         onewarchdefaultsW += parseFloat(`${3 >> (Math.min(Math.abs(parseInt(`${cricketB}`)), 3))}`);
         suggestiont -= 1 - parseInt(`${suggestiont}`);
         cricketB -= 1 - parseInt(`${suggestiont}`);
         china0 = 66.96 >= suggestiont || china0;
         subbasketballplayerF += (subbasketballplayerF ^ (china0 ? 3 : 5));
         if (countrye) {
            break;
         }
      } while (((libnmst * onewarchdefaultsW) < 1.62) && countrye);
         iconwechat6 += "1";
      for (let k = 0; k < 2; k++) {
          let screenI = 1;
          let greyticks = String.fromCharCode(109,97,112,115,95,53,95,52,0);
         iconwechat6 = `${greyticks.length}`;
         screenI >>= Math.min(4, Math.abs(2));
         greyticks = `${screenI >> (Math.min(Math.abs(screenI), 1))}`;
      }
       let predictionbanner_ = String.fromCharCode(111,95,56,52,95,97,117,116,111,109,97,116,105,99,97,108,108,121,0);
      zhengpianD += parseFloat(`${resulth.length - parseInt(`${onewarchdefaultsW}`)}`);
   }
        return (
          <Text
            key={index}
            style={{ ...textVariants.body, color: '#FFFFFF', fontSize: 20 }}>
            {part}
          </Text>
        );
      }
    });
  };

      material5.push(2 * material5.length);
   let subtextb = 7707025 <= telegram_.length;
   do {
      telegram_ = [3];
      if (subtextb) {
         break;
      }
   } while (subtextb && (!homeplayerI.endsWith(`${telegram_.length}`)));
  return (
    <>
      <View
        style={{
          paddingTop: 10,
          paddingBottom: 15,
          flexDirection: 'column',
          justifyContent: 'center',
          alignItems: 'center',
          gap: 9,
        }}>
        <Text style={{ fontSize: 22, color: colors.primary, fontWeight: '700' }}>
          双赢奖励
        </Text>

        <Text>{highlightText('每邀请1位好友 增加30天VIP', '1', '30')}</Text>

        <Text>{highlightText('好友获得30天VIP', '30')}</Text>
      </View>
    </>
  );
}

const styles = StyleSheet.create({});
