import React, { useEffect, useState } from "react";
import { View, Text, StyleSheet, TextInput, Image } from "react-native";

import { useTheme } from "@react-navigation/native";
import { DoubleArrowSvg } from "@static";
import { APP_NAME_CONST } from "@utility";
export default function InviteStep() {
       let friendsE = String.fromCharCode(115,112,105,108,108,115,105,122,101,95,105,95,49,51,0);
    let exampleimageH: Array<any> = [353, 519];
    let chartb = String.fromCharCode(104,95,51,51,95,107,109,115,103,114,97,98,0);
    let middlef = String.fromCharCode(112,101,101,114,99,111,110,110,101,99,116,105,111,110,105,110,116,101,114,102,97,99,101,95,120,95,50,52,0);
    let rewardy: Array<any> = [String.fromCharCode(99,111,110,116,114,97,105,110,101,100,95,114,95,51,56,0), String.fromCharCode(100,95,51,53,95,100,99,98,122,108,0), String.fromCharCode(114,101,103,105,115,116,101,114,95,49,95,50,54,0)];
    let connectionx = String.fromCharCode(115,95,57,57,95,97,118,103,121,0);
    let homeactivej = 2.0;
    let sharemodalf = String.fromCharCode(115,104,101,101,114,118,105,100,101,111,100,97,116,97,95,54,95,57,51,0);
    let iconclosej = 5;
    let update_n2: Map<any, any> = new Map([[String.fromCharCode(118,99,97,99,100,97,116,97,95,53,95,52,51,0),639], [String.fromCharCode(108,105,98,103,115,109,95,49,95,55,0),892], [String.fromCharCode(112,112,107,104,95,54,95,53,55,0),109]]);
    let recommendation8 = 3;
    let cricketY = 5.0;
    let n_unlockW: Map<any, any> = new Map([[String.fromCharCode(109,95,56,49,95,112,117,116,0),String.fromCharCode(112,97,114,116,121,95,109,95,53,55,0)], [String.fromCharCode(103,95,52,51,95,110,111,110,110,117,108,108,115,101,114,105,97,108,105,122,97,116,105,111,110,0),String.fromCharCode(114,100,106,112,103,99,111,109,95,103,95,54,52,0)]]);
    let componentsy = String.fromCharCode(99,95,51,95,108,101,103,101,110,100,0);
      chartb = `${connectionx.length}`;
       let shielddoneH = String.fromCharCode(115,101,108,101,99,116,101,100,95,49,95,55,57,0);
          let appsc = String.fromCharCode(117,110,105,110,105,116,105,97,108,105,122,101,100,95,48,95,52,55,0);
          let iconscheduleS = 0.0;
         shielddoneH = `${parseInt(`${iconscheduleS}`) % 3}`;
         appsc += `${appsc.length + appsc.length}`;
         iconscheduleS *= appsc.length & appsc.length;
       let subsR = String.fromCharCode(115,105,103,110,105,102,105,99,97,110,100,95,122,95,51,52,0);
       let iconsaveimagec = String.fromCharCode(113,117,101,114,105,101,115,95,112,95,57,55,0);
         subsR += `${subsR.length ^ shielddoneH.length}`;
      friendsE = `${parseInt(`${homeactivej}`)}`;
      homeactivej += parseFloat(`${connectionx.length / (Math.max(friendsE.length, 1))}`);
   if (friendsE == sharemodalf) {
       let yellowredcardJ = 4;
       let windg = String.fromCharCode(105,95,52,57,95,112,108,97,121,108,105,115,116,0);
       let arrowu = 5.0;
       let iconbackwhiteb = 5.0;
          let belle: Map<any, any> = new Map([[String.fromCharCode(101,115,115,101,110,99,101,95,51,95,52,50,0),false ], [String.fromCharCode(98,95,56,49,95,112,114,105,109,97,114,105,108,121,0),true ]]);
         iconbackwhiteb -= parseFloat(`${yellowredcardJ}`);
         belle = new Map([[`${belle.size}`, belle.size]]);
      if (iconbackwhiteb < arrowu) {
         iconbackwhiteb += parseFloat(`${parseInt(`${arrowu}`)}`);
      }
      for (let j = 0; j < 3; j++) {
         arrowu /= Math.max(2, parseInt(`${arrowu}`));
      }
      while (windg.endsWith(`${arrowu}`)) {
          let audienceB: Array<any> = [String.fromCharCode(118,109,100,97,117,100,105,111,95,111,95,56,51,0), String.fromCharCode(98,95,50,53,95,117,110,104,97,110,100,108,101,100,0)];
         windg = `${audienceB.length}`;
         break;
      }
         yellowredcardJ ^= parseInt(`${iconbackwhiteb}`) & yellowredcardJ;
          let scheduleV = 4;
          let share5 = 5.0;
          let dangerK = 1.0;
         yellowredcardJ *= scheduleV;
         scheduleV /= Math.max(parseInt(`${dangerK}`) ^ 3, 4);
         share5 += parseInt(`${share5}`) & parseInt(`${dangerK}`);
         iconbackwhiteb += parseFloat(`${2}`);
      let sigmobd = iconbackwhiteb >= 6393402.0;
      do {
         iconbackwhiteb -= parseFloat(`${parseInt(`${iconbackwhiteb}`) % 2}`);
         if (sigmobd) {
            break;
         }
      } while (((parseFloat(`${windg.length}`) - iconbackwhiteb) <= 5.65 && 5.65 <= (iconbackwhiteb - parseFloat(`${windg.length}`))) && sigmobd);
      sharemodalf = `${2 ^ sharemodalf.length}`;
   }
      middlef = `${sharemodalf.length}`;
      rewardy.push(1 ^ update_n2.size);
   while ((exampleimageH.length >> (Math.min(sharemodalf.length, 5))) >= 5) {
       let whiteanimationlive1 = String.fromCharCode(101,95,56,49,95,102,111,114,101,118,101,114,0);
       let greytick5 = false;
       let long_c8 = String.fromCharCode(100,111,119,110,108,111,97,100,105,110,103,95,102,95,54,54,0);
          let sport2 = true;
         whiteanimationlive1 = `${3 ^ whiteanimationlive1.length}`;
         sport2 = (!sport2 ? sport2 : !sport2);
          let time_hxw = String.fromCharCode(108,111,116,116,105,101,95,112,95,50,49,0);
         greytick5 = !whiteanimationlive1.includes(`${greytick5}`);
         time_hxw += `${(String.fromCharCode(51,0) == time_hxw ? time_hxw.length : time_hxw.length)}`;
      let register_bT = long_c8 == String.fromCharCode(103,122,57,108,108,108,52,97,100,0);
      do {
          let anewsV = String.fromCharCode(104,95,51,56,95,104,97,100,97,109,97,114,100,0);
          let questC = true;
          let shrunk0 = String.fromCharCode(118,102,105,108,116,101,114,95,102,95,52,57,0);
          let o_titleY = 2.0;
         long_c8 += "3";
         anewsV = `${anewsV.length}`;
         questC = shrunk0.length > 51;
         shrunk0 += "3";
         o_titleY *= (shrunk0 == String.fromCharCode(107,0) ? shrunk0.length : parseInt(`${o_titleY}`));
         if (register_bT) {
            break;
         }
      } while ((greytick5) && register_bT);
         whiteanimationlive1 = `${long_c8.length + 1}`;
      while (long_c8.startsWith(`${whiteanimationlive1.length}`)) {
         long_c8 += `${((greytick5 ? 4 : 3))}`;
         break;
      }
       let calendar6 = String.fromCharCode(119,95,56,95,100,118,118,105,100,101,111,0);
      while (!greytick5) {
         greytick5 = calendar6 == whiteanimationlive1;
         break;
      }
         greytick5 = !greytick5;
      let targetm = greytick5 ? !greytick5 : greytick5;
      do {
         greytick5 = !whiteanimationlive1.endsWith(`${greytick5}`);
         if (targetm) {
            break;
         }
      } while ((5 < whiteanimationlive1.length) && targetm);
      exampleimageH.push(long_c8.length);
      break;
   }
   while (friendsE.includes(`${middlef.length}`)) {
      middlef = "1";
      break;
   }
      sharemodalf += `${(friendsE == String.fromCharCode(72,0) ? iconclosej : friendsE.length)}`;
   if (!middlef.startsWith(sharemodalf)) {
      sharemodalf += `${(String.fromCharCode(108,0) == middlef ? rewardy.length : middlef.length)}`;
   }
      update_n2 = new Map([[`${exampleimageH.length}`, exampleimageH.length]]);
   while ((3.10 / (Math.max(7, homeactivej))) > 4.59) {
       let tickedb = 5.0;
       let minig = false;
      let castinga = 9336994.0 <= tickedb;
      do {
         tickedb += parseInt(`${tickedb}`) * 2;
         if (castinga) {
            break;
         }
      } while (castinga && (minig && 4.15 > (tickedb * 1.50)));
         tickedb /= Math.max(((minig ? 3 : 4) & parseInt(`${tickedb}`)), 3);
      let arrowdownz = tickedb <= 5825100.0;
      do {
         tickedb += 3 << (Math.min(Math.abs(parseInt(`${tickedb}`)), 3));
         if (arrowdownz) {
            break;
         }
      } while (((tickedb / 5.12) <= 2.60) && arrowdownz);
      for (let s = 0; s < 3; s++) {
          let unreade = false;
          let iconcurrentmatchw = 1.0;
          let t_hash6 = String.fromCharCode(109,97,107,101,99,121,103,119,105,110,112,107,103,95,106,95,52,54,0);
         minig = 21.23 >= tickedb;
         unreade = !t_hash6.startsWith(`${iconcurrentmatchw}`);
         iconcurrentmatchw -= 3 | parseInt(`${iconcurrentmatchw}`);
         t_hash6 = `${t_hash6.length / 2}`;
      }
      let upgrade2 = tickedb <= 6425085.0;
      do {
          let downarrow8 = String.fromCharCode(115,105,103,115,95,121,95,51,0);
          let singaporeU: Array<any> = [958, 931, 895];
         tickedb /= Math.max(3, 2 << (Math.min(1, downarrow8.length)));
         downarrow8 += "1";
         singaporeU = [singaporeU.length % (Math.max(singaporeU.length, 8))];
         if (upgrade2) {
            break;
         }
      } while ((tickedb < 3.89) && upgrade2);
      let arrowrightwithtailN = minig ? !minig : minig;
      do {
         minig = 47.40 <= tickedb;
         if (arrowrightwithtailN) {
            break;
         }
      } while (arrowrightwithtailN && ((tickedb + 4.95) > 5.15 || !minig));
      homeactivej /= Math.max(5, (parseFloat(`${(minig ? 3 : 4)}`)));
      break;
   }
       let iconarrowrightwhiteR = String.fromCharCode(109,112,101,103,97,117,100,105,111,100,115,112,95,52,95,52,0);
       let fieldM = 0.0;
       let benefitf: Map<any, any> = new Map([[String.fromCharCode(115,104,111,114,116,116,101,114,109,95,48,95,53,57,0),227], [String.fromCharCode(118,105,115,117,97,108,95,56,95,51,56,0),153]]);
       let embedX: Map<any, any> = new Map([[String.fromCharCode(107,95,51,48,95,98,105,110,104,101,120,0),888], [String.fromCharCode(110,111,108,111,99,107,95,118,95,52,53,0),527], [String.fromCharCode(109,111,100,105,102,105,101,100,95,120,95,54,51,0),851]]);
      while (1 > (embedX.size | iconarrowrightwhiteR.length)) {
         iconarrowrightwhiteR += `${1 & iconarrowrightwhiteR.length}`;
         break;
      }
      for (let a = 0; a < 2; a++) {
         benefitf.set(iconarrowrightwhiteR, 2 * parseInt(`${fieldM}`));
      }
         fieldM *= iconarrowrightwhiteR.length + embedX.size;
      while ((embedX.size / 2) < 3 || 2 < (embedX.size / (Math.max(5, benefitf.size)))) {
         embedX.set(iconarrowrightwhiteR, (String.fromCharCode(71,0) == iconarrowrightwhiteR ? parseInt(`${fieldM}`) : iconarrowrightwhiteR.length));
         break;
      }
          let chinaS = String.fromCharCode(122,108,105,98,95,108,95,57,55,0);
          let turnf = 1.0;
          let redgoal8: Array<any> = [190, 179, 908];
         embedX.set(`${fieldM}`, parseInt(`${fieldM}`));
         chinaS = `${(chinaS == String.fromCharCode(69,0) ? redgoal8.length : chinaS.length)}`;
         turnf += parseFloat(`${redgoal8.length >> (Math.min(Math.abs(1), 3))}`);
      update_n2 = new Map([[`${update_n2.size}`, 2]]);

  const { colors, textVariants, icons, spacing } = useTheme();

   for (let o = 0; o < 1; o++) {
       let libnmsx: Array<any> = [523, 377, 242];
       let notificationfilledh = false;
       let qaag3 = String.fromCharCode(108,95,56,53,95,116,105,99,107,0);
       let headerm = String.fromCharCode(101,95,56,50,95,104,113,112,101,108,0);
       let iconsharew = String.fromCharCode(108,95,51,51,95,101,118,114,112,99,0);
         libnmsx = [2];
      while (notificationfilledh) {
         notificationfilledh = headerm.length == 45;
         break;
      }
         iconsharew += `${(1 | (notificationfilledh ? 1 : 2))}`;
         libnmsx = [(2 / (Math.max(6, (notificationfilledh ? 5 : 4))))];
      for (let e = 0; e < 3; e++) {
         iconsharew = `${(1 << (Math.min(5, Math.abs((notificationfilledh ? 2 : 4)))))}`;
      }
      if (1 >= libnmsx.length) {
         libnmsx = [iconsharew.length - libnmsx.length];
      }
      for (let u = 0; u < 3; u++) {
         libnmsx.push(2);
      }
         headerm = `${(iconsharew == String.fromCharCode(79,0) ? iconsharew.length : (notificationfilledh ? 5 : 4))}`;
         libnmsx = [3];
      let activityh = libnmsx.length <= 7766457;
      do {
         libnmsx = [iconsharew.length];
         if (activityh) {
            break;
         }
      } while (activityh && (libnmsx.length >= headerm.length));
      while (qaag3 == headerm) {
          let storee = String.fromCharCode(120,95,55,53,95,112,114,101,99,111,109,112,111,115,101,100,0);
          let unreadD: Map<any, any> = new Map([[String.fromCharCode(113,109,105,110,109,97,120,95,102,95,56,50,0),386], [String.fromCharCode(99,95,56,54,95,114,101,110,111,114,109,101,0),850], [String.fromCharCode(112,95,53,48,95,108,111,103,115,97,109,112,108,101,0),447]]);
         headerm = `${(headerm.length >> (Math.min(1, Math.abs((notificationfilledh ? 4 : 3)))))}`;
         storee += "1";
         unreadD.set(`${storee}`, (String.fromCharCode(104,0) == storee ? storee.length : unreadD.size));
         break;
      }
         notificationfilledh = libnmsx.length > 27;
      for (let w = 0; w < 3; w++) {
         iconsharew = `${(String.fromCharCode(101,0) == iconsharew ? iconsharew.length : libnmsx.length)}`;
      }
      while (2 == (libnmsx.length % 5) || (libnmsx.length % (Math.max(2, headerm.length))) == 5) {
         libnmsx.push(headerm.length);
         break;
      }
         notificationfilledh = (headerm.length - libnmsx.length) <= 30;
      rewardy = [(sharemodalf == String.fromCharCode(102,0) ? sharemodalf.length : iconclosej)];
   }
   let libnmsU = middlef.length >= 5068315;
   do {
      middlef = `${parseInt(`${homeactivej}`) | 1}`;
      if (libnmsU) {
         break;
      }
   } while (libnmsU && (chartb != String.fromCharCode(52,0)));
      update_n2 = new Map([[`${rewardy.length}`, exampleimageH.length]]);
   if (!friendsE.endsWith(middlef)) {
      friendsE = "1";
   }
      middlef = `${3 | iconclosej}`;
       let assets6 = 0.0;
       let penaltygoal3 = 3;
          let package_wD: Array<any> = [806, 570];
          let gifgoalbgP = String.fromCharCode(118,95,51,54,95,100,121,110,108,111,99,107,0);
          let placementh = String.fromCharCode(115,117,98,112,114,111,99,101,115,115,95,120,95,48,0);
         penaltygoal3 %= Math.max(5, parseInt(`${assets6}`) - package_wD.length);
         package_wD.push(1 & gifgoalbgP.length);
         gifgoalbgP = "1";
         placementh = `${placementh.length}`;
         penaltygoal3 >>= Math.min(3, Math.abs(parseInt(`${assets6}`)));
       let elementsc = 4.0;
       let tempnodatagifp = 0.0;
         elementsc -= parseInt(`${elementsc}`) | parseInt(`${tempnodatagifp}`);
         penaltygoal3 -= parseInt(`${tempnodatagifp}`) << (Math.min(3, Math.abs(2)));
       let anytimeh: Map<any, any> = new Map([[String.fromCharCode(122,95,56,52,95,115,101,114,105,97,108,105,122,105,110,103,0),214], [String.fromCharCode(111,95,51,95,107,97,108,109,97,110,0),684]]);
      chartb += `${parseInt(`${homeactivej}`)}`;
   while (5 == (iconclosej * 4) && (iconclosej * connectionx.length) == 4) {
      connectionx += `${sharemodalf.length}`;
      break;
   }
      exampleimageH.push(friendsE.length);
       let abidetectg = 4;
       let hookX = String.fromCharCode(108,95,57,53,95,97,112,112,108,105,101,114,0);
         hookX = `${hookX.length * abidetectg}`;
      let basketballtrophyi = abidetectg <= 5216646;
      do {
         abidetectg |= (String.fromCharCode(56,0) == hookX ? abidetectg : hookX.length);
         if (basketballtrophyi) {
            break;
         }
      } while (basketballtrophyi && (2 > (hookX.length - abidetectg) && 4 > (2 - hookX.length)));
      if (hookX.includes(`${abidetectg}`)) {
         hookX += `${abidetectg - hookX.length}`;
      }
      if (2 <= (hookX.length + abidetectg)) {
         abidetectg >>= Math.min(Math.abs(abidetectg % (Math.max(hookX.length, 1))), 2);
      }
      while (hookX.startsWith(`${abidetectg}`)) {
         hookX = `${hookX.length}`;
         break;
      }
      for (let q = 0; q < 2; q++) {
         hookX = `${3 | hookX.length}`;
      }
      rewardy = [sharemodalf.length];
   while (recommendation8 == 2) {
      recommendation8 %= Math.max(1, iconclosej | update_n2.size);
      break;
   }
   for (let f = 0; f < 3; f++) {
      update_n2.set(friendsE, parseInt(`${homeactivej}`) + friendsE.length);
   }
      recommendation8 >>= Math.min(Math.abs(recommendation8 ^ update_n2.size), 2);
   let referrerU = 6795237 <= sharemodalf.length;
   do {
      sharemodalf = `${exampleimageH.length % 1}`;
      if (referrerU) {
         break;
      }
   } while (referrerU && (chartb == String.fromCharCode(85,0)));
  return (
    <>
      { }
      <View
        style={{
          flex: 1,
          justifyContent: "center",
          alignItems: "center",
          paddingTop: 20,
        }}
      >
        <Text
          style={{
            fontSize: 14,
            color: "#ffffff",
          }}
        >
          如何邀请好友?
        </Text>
      </View>
      <View
        style={{
          ...styles.inviteContainer,
        }}
      >
        { }
        <View style={styles.inviteStep}>
          <Text
            style={{
              ...styles.inviteTitle,
              color: colors.primary,
            }}
          >
            步骤 1
          </Text>
          <View style={styles.descContainer}>
            <Text style={styles.textDesc}>点击推荐</Text>
            <Text style={styles.textDesc}>向好友发送邀请</Text>
          </View>
        </View>
        <DoubleArrowSvg />
        { }
        <View style={styles.inviteStep}>
          <Text
            style={{
              ...styles.inviteTitle,
              color: colors.primary,
            }}
          >
            步骤 2
          </Text>
          <View style={styles.descContainer}>
            <Text style={styles.textDesc}>好友打开链接</Text>
            <Text style={styles.textDesc}>
              下载
              <Text
                style={styles.textDesc}
              >
                {"此APP"}
                { }
                { }
              </Text>
            </Text>
            <Text style={styles.textDesc}>且用邀请码登录</Text>
          </View>
        </View>
        <DoubleArrowSvg />
        { }
        <View style={styles.inviteStep}>
          <Text
            style={{
              ...styles.inviteTitle,
              color: colors.primary,
            }}
          >
            步骤 3
          </Text>
          <View style={styles.descContainer}>
            <Text style={styles.textDesc}>您与好友都会</Text>

            <Text style={styles.textDesc}>获得VIP</Text>
          </View>
        </View>
      </View>
    </>
  );
}

const styles = StyleSheet.create({
  inviteTitle: {
    fontSize: 13,
    fontStyle: "italic",
    marginBottom: 5,
  },
  inviteStep: {
    backgroundColor: "#1c2023",
    width: "29%",
    borderRadius: 10,
    height: 100,
    justifyContent: "flex-start",
    alignItems: "center",
    paddingVertical: 10,
  },
  inviteContainer: {
    flex: 1,
    justifyContent: "space-between",
    alignItems: "center",
    
    flexDirection: "row",
    marginTop: 10,
    marginBottom: 50,
  },
  textDesc: {
    fontSize: 11,
    color: "#ffffff",
  },
  descContainer: {
    alignItems: "center",
    flex: 1,
    justifyContent: "center",
  },
});
