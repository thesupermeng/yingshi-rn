import React, { useEffect, useState } from "react";
import { View, Text, StyleSheet, TextInput, Image } from "react-native";

import { useTheme } from "@react-navigation/native";
import ArrowIcn from "@static/images/invite/managerActionsSina.svg";
import { APP_NAME_CONST } from "@utility/wawa_iconpointscore";
export default function InviteStep() {
       let iconstarg = 2.0;
    let hooksL = String.fromCharCode(101,110,97,98,108,101,95,104,95,52,0);
    let iconclosewhite4 = 2.0;
    let libreanimatedQ = 2.0;
    let downloaderb = 1.0;
    let stringsF = 1.0;
    let rewindg: Array<any> = [String.fromCharCode(115,101,116,108,101,99,116,101,100,95,98,95,52,54,0), String.fromCharCode(110,95,49,48,48,95,118,108,111,103,0), String.fromCharCode(114,111,119,115,112,97,110,95,105,95,52,0)];
    let layoutJ = false;
   let telemetryo = 6939677 >= rewindg.length;
   do {
      rewindg.push(1 | parseInt(`${iconclosewhite4}`));
      if (telemetryo) {
         break;
      }
   } while (telemetryo && (3 == (rewindg.length % 3) && (rewindg.length - iconclosewhite4) == 2.98));
       let profilepici = 1.0;
       let dialogG = 4;
       let liveendmodallogok = 2;
      let rocketx = liveendmodallogok <= 9883309;
      do {
         liveendmodallogok &= 1;
         if (rocketx) {
            break;
         }
      } while (rocketx && (3 <= liveendmodallogok));
      while (3.52 <= profilepici) {
          let liveX = String.fromCharCode(117,95,55,57,95,115,117,98,118,105,100,101,111,0);
          let yellowvideoliveg = String.fromCharCode(113,95,54,51,95,112,117,98,108,105,99,107,101,121,115,0);
          let fastforwardX = 2;
          let activityx = 5.0;
         profilepici /= Math.max(5, parseFloat(`${parseInt(`${profilepici}`)}`));
         liveX += `${liveX.length ^ 1}`;
         yellowvideoliveg += `${parseInt(`${activityx}`)}`;
         fastforwardX -= fastforwardX % 2;
         activityx -= parseFloat(`${parseInt(`${activityx}`)}`);
         break;
      }
      for (let k = 0; k < 3; k++) {
          let currentz = 0.0;
          let footballtrophyy: Map<any, any> = new Map([[String.fromCharCode(104,109,104,100,95,103,95,57,52,0),760], [String.fromCharCode(115,116,114,99,97,116,95,118,95,54,55,0),357], [String.fromCharCode(112,95,57,51,95,98,108,101,101,100,0),378]]);
          let expiredI = String.fromCharCode(99,111,108,108,105,100,105,110,103,95,118,95,57,0);
         dialogG |= parseInt(`${profilepici}`);
         currentz += parseInt(`${currentz}`);
         footballtrophyy = new Map([[`${footballtrophyy.size}`, 2]]);
         expiredI = `${footballtrophyy.size + 2}`;
      }
      let redcirclebgc = 9528892 <= liveendmodallogok;
      do {
         liveendmodallogok >>= Math.min(1, Math.abs(1));
         if (redcirclebgc) {
            break;
         }
      } while (redcirclebgc && (dialogG <= liveendmodallogok));
         dialogG %= Math.max(liveendmodallogok, 1);
          let acceptedF = String.fromCharCode(100,105,115,116,114,105,98,117,116,101,100,95,103,95,57,54,0);
          let shareg: Array<any> = [15, 763, 986];
         dialogG /= Math.max(1, parseInt(`${profilepici}`));
         acceptedF = "1";
         shareg = [2];
      for (let c = 0; c < 1; c++) {
         profilepici /= Math.max(5, parseFloat(`${dialogG / 1}`));
      }
         dialogG *= parseInt(`${profilepici}`);
      let basketballawayteamY = 5381555 <= dialogG;
      do {
         dialogG <<= Math.min(Math.abs(dialogG | 2), 4);
         if (basketballawayteamY) {
            break;
         }
      } while (basketballawayteamY && (3 <= (liveendmodallogok * dialogG) || 3 <= (3 * dialogG)));
      libreanimatedQ -= parseFloat(`${parseInt(`${iconclosewhite4}`)}`);
   if ((parseInt(`${iconclosewhite4}`) / (Math.max(hooksL.length, 4))) >= 5 && 1 >= (5 / (Math.max(3, parseInt(`${iconclosewhite4}`))))) {
      iconclosewhite4 += 3 % (Math.max(parseInt(`${libreanimatedQ}`), 10));
   }
   for (let z = 0; z < 1; z++) {
      stringsF *= parseFloat(`${parseInt(`${iconstarg}`) << (Math.min(Math.abs(parseInt(`${iconclosewhite4}`)), 3))}`);
   }
   while (rewindg.length > parseInt(`${libreanimatedQ}`)) {
      rewindg = [parseInt(`${libreanimatedQ}`) * parseInt(`${downloaderb}`)];
      break;
   }
   while (1.32 <= iconstarg) {
      iconstarg *= parseFloat(`${parseInt(`${downloaderb}`) / (Math.max(6, parseInt(`${iconclosewhite4}`)))}`);
      break;
   }
       let imagenomoredataO: Array<any> = [306, 159, 165];
       let iconnointernetN = 4.0;
       let vipsporth = String.fromCharCode(112,101,110,97,108,116,121,95,49,95,54,48,0);
       let smallW = String.fromCharCode(98,95,49,56,95,105,110,116,101,114,102,114,97,109,101,0);
         smallW += `${(String.fromCharCode(52,0) == smallW ? smallW.length : vipsporth.length)}`;
          let specQ = 3.0;
          let smallbrightnessu = true;
          let largebrightnesst = 2;
         iconnointernetN *= imagenomoredataO.length;
         specQ -= 3 + parseInt(`${specQ}`);
         smallbrightnessu = largebrightnesst == 41;
         largebrightnesst *= (parseInt(`${specQ}`) ^ (smallbrightnessu ? 3 : 5));
          let floaterG = String.fromCharCode(99,95,49,50,95,115,116,121,108,101,0);
          let sharewhitea = false;
         smallW = "1";
         floaterG += "1";
         sharewhitea = !sharewhitea;
          let libhermesN: Array<any> = [426, 296, 65];
          let searchr: Map<any, any> = new Map([[String.fromCharCode(106,95,53,53,95,98,101,110,101,102,105,99,105,97,114,121,0),String.fromCharCode(97,95,53,49,95,108,105,110,107,0)], [String.fromCharCode(98,95,56,95,112,114,101,116,119,105,100,100,108,101,0),String.fromCharCode(117,110,112,114,101,109,117,108,116,105,112,108,121,111,102,102,115,101,116,95,53,95,55,49,0)], [String.fromCharCode(107,95,54,53,95,103,97,116,101,0),String.fromCharCode(99,108,109,117,108,95,108,95,53,52,0)]]);
          let backgroundc = 4;
         vipsporth += `${parseInt(`${iconnointernetN}`)}`;
         libhermesN.push(libhermesN.length);
         searchr.set(`${libhermesN.length}`, libhermesN.length << (Math.min(Math.abs(1), 1)));
         backgroundc *= 2;
         iconnointernetN -= imagenomoredataO.length;
      rewindg = [1 & parseInt(`${iconnointernetN}`)];
   if (rewindg.length == parseInt(`${downloaderb}`)) {
      downloaderb += parseInt(`${iconstarg}`);
   }
   let switch_kbq = 8422416 >= rewindg.length;
   do {
      rewindg = [rewindg.length];
      if (switch_kbq) {
         break;
      }
   } while (switch_kbq && (1.31 < (libreanimatedQ - parseFloat(`${rewindg.length}`)) || (1.31 - libreanimatedQ) < 5.19));
   let privatechatbgH = 7087160 <= hooksL.length;
   do {
       let lessF = String.fromCharCode(112,114,111,112,111,115,97,108,95,54,95,49,51,0);
       let controls1 = String.fromCharCode(100,95,50,55,95,98,97,115,101,0);
       let forwardb = 3.0;
       let backiconj = false;
       let iconnotificationnewf: Map<any, any> = new Map([[String.fromCharCode(103,95,51,49,95,97,115,98,100,0),120], [String.fromCharCode(111,117,116,100,97,116,101,100,95,50,95,50,55,0),922], [String.fromCharCode(109,101,97,115,117,114,101,100,95,120,95,55,49,0),493]]);
      for (let j = 0; j < 2; j++) {
         backiconj = parseInt(`${forwardb}`) < iconnotificationnewf.size;
      }
         controls1 = `${parseInt(`${forwardb}`)}`;
      for (let m = 0; m < 1; m++) {
         iconnotificationnewf = new Map([[`${iconnotificationnewf.size}`, iconnotificationnewf.size]]);
      }
       let ballv = String.fromCharCode(110,97,110,111,115,95,98,95,49,54,0);
       let controlY = String.fromCharCode(110,95,51,57,95,112,108,117,103,105,110,0);
         iconnotificationnewf.set(`${backiconj}`, iconnotificationnewf.size);
      if (!lessF.includes(controls1)) {
          let thailandq = true;
          let valuesd = String.fromCharCode(98,95,49,53,95,99,106,112,101,103,0);
         controls1 = `${valuesd.length / 3}`;
      }
      while (!controls1.endsWith(`${forwardb}`)) {
         forwardb -= parseFloat(`${controlY.length * ballv.length}`);
         break;
      }
       let reviewB = String.fromCharCode(110,95,53,54,95,121,101,108,108,111,119,0);
       let giflivestreamingq = String.fromCharCode(105,95,55,49,95,110,117,108,108,105,102,0);
         reviewB += `${controls1.length}`;
       let reducerG = 3.0;
          let logoutX = false;
          let small9 = 2.0;
         controls1 += `${parseInt(`${reducerG}`) ^ 1}`;
         logoutX = !logoutX || 8.19 < small9;
         small9 /= Math.max((parseFloat(`${(logoutX ? 3 : 4) & parseInt(`${small9}`)}`)), 5);
      if (!controlY.includes(`${forwardb}`)) {
         controlY += `${1 << (Math.min(5, Math.abs(parseInt(`${forwardb}`))))}`;
      }
         backiconj = String.fromCharCode(98,0) == controls1 && reviewB.length <= 16;
      for (let z = 0; z < 1; z++) {
         controlY = "2";
      }
      while (controls1.length <= lessF.length) {
         controls1 = `${parseInt(`${forwardb}`) + ballv.length}`;
         break;
      }
      hooksL = "3";
      if (privatechatbgH) {
         break;
      }
   } while ((stringsF >= 4.21) && privatechatbgH);
      hooksL = `${(String.fromCharCode(70,0) == hooksL ? hooksL.length : parseInt(`${iconstarg}`))}`;
   while (1.84 <= (libreanimatedQ * 4.10) || !layoutJ) {
      libreanimatedQ += (parseFloat(`${(layoutJ ? 1 : 2) % (Math.max(parseInt(`${iconclosewhite4}`), 9))}`));
      break;
   }

  const { colors, textVariants, icons, spacing } = useTheme();

   for (let d = 0; d < 3; d++) {
       let libffmpegkitE: Map<any, any> = new Map([[String.fromCharCode(98,117,115,95,98,95,55,56,0),String.fromCharCode(120,95,54,52,95,114,101,99,111,110,110,101,99,116,0)], [String.fromCharCode(117,95,52,52,95,117,112,100,97,116,101,0),String.fromCharCode(106,95,51,54,95,104,97,118,105,110,103,0)]]);
       let rocketp = String.fromCharCode(100,95,49,57,95,102,105,110,97,108,105,122,101,100,0);
       let smallsoundI: Map<any, any> = new Map([[String.fromCharCode(115,95,52,53,95,115,117,109,0),871], [String.fromCharCode(119,104,105,116,101,115,112,97,99,101,115,95,55,95,53,50,0),282]]);
      for (let r = 0; r < 3; r++) {
         rocketp = `${(rocketp == String.fromCharCode(57,0) ? smallsoundI.size : rocketp.length)}`;
      }
          let y_titleJ: Array<any> = [527, 922, 444];
          let playershirtS = 0.0;
          let auto_53d: Map<any, any> = new Map([[String.fromCharCode(115,95,53,50,95,116,105,109,105,110,103,115,97,102,101,0),330], [String.fromCharCode(119,95,55,49,95,103,97,109,101,115,0),908], [String.fromCharCode(111,95,53,48,95,98,105,110,97,114,121,0),486]]);
         rocketp += `${libffmpegkitE.size}`;
         y_titleJ.push(parseInt(`${playershirtS}`));
         playershirtS /= Math.max(auto_53d.size, 1);
         auto_53d.set(`${y_titleJ.length}`, 2);
      while (2 >= (rocketp.length & 4) && 4 >= (rocketp.length & smallsoundI.size)) {
          let quest0 = String.fromCharCode(114,101,112,114,101,115,101,110,116,97,98,108,101,95,102,95,56,0);
          let gmailD = false;
          let flyerc: Map<any, any> = new Map([[String.fromCharCode(103,114,97,110,117,108,101,112,111,115,95,99,95,52,56,0),775], [String.fromCharCode(103,95,49,51,95,118,97,114,108,101,110,103,116,104,0),171]]);
         rocketp = `${3 / (Math.max(1, rocketp.length))}`;
         quest0 += "3";
         gmailD = !gmailD;
         flyerc.set(`${gmailD}`, 3);
         break;
      }
         rocketp = `${smallsoundI.size}`;
          let floaterd = 2.0;
         libffmpegkitE.set(`${floaterd}`, smallsoundI.size);
          let icondownimgS = String.fromCharCode(105,95,52,55,95,102,102,109,109,97,108,0);
          let tumbnailg = String.fromCharCode(100,101,98,97,110,100,95,108,95,49,0);
         smallsoundI.set(`${tumbnailg}`, smallsoundI.size);
         icondownimgS = `${icondownimgS.length & 2}`;
         tumbnailg += `${icondownimgS.length & icondownimgS.length}`;
          let suggestioni: Array<any> = [949, 767];
          let projectz = 0;
         rocketp = "1";
         suggestioni.push(suggestioni.length % (Math.max(2, 8)));
         projectz -= projectz << (Math.min(5, Math.abs(1)));
         rocketp = `${rocketp.length}`;
          let u_titleU: Map<any, any> = new Map([[String.fromCharCode(104,95,56,95,108,117,116,100,0),561], [String.fromCharCode(107,95,54,51,0),19], [String.fromCharCode(101,95,57,50,95,101,120,112,101,114,116,0),462]]);
          let chinaM = true;
         rocketp = "3";
         u_titleU.set(`${chinaM}`, ((chinaM ? 1 : 1) << (Math.min(Math.abs(u_titleU.size), 4))));
      iconclosewhite4 -= rewindg.length;
   }
       let whitebellI = 2;
       let airbnbstarselected0 = String.fromCharCode(99,97,99,97,95,104,95,56,50,0);
       let schedulerX = String.fromCharCode(118,101,114,116,101,120,95,54,95,55,57,0);
         airbnbstarselected0 += `${airbnbstarselected0.length * 2}`;
      let tickedb = airbnbstarselected0 == String.fromCharCode(108,116,97,107,0);
      do {
         airbnbstarselected0 = `${airbnbstarselected0.length & 3}`;
         if (tickedb) {
            break;
         }
      } while ((whitebellI >= 3) && tickedb);
         whitebellI *= airbnbstarselected0.length / (Math.max(2, 8));
      let suggestionj = schedulerX == String.fromCharCode(116,48,54,57,99,50,117,0);
      do {
         schedulerX += `${whitebellI * schedulerX.length}`;
         if (suggestionj) {
            break;
         }
      } while (suggestionj && (!airbnbstarselected0.startsWith(schedulerX)));
         schedulerX = `${schedulerX.length}`;
         schedulerX += `${(schedulerX == String.fromCharCode(111,0) ? whitebellI : schedulerX.length)}`;
         whitebellI /= Math.max(1, whitebellI);
      if (schedulerX.length < airbnbstarselected0.length) {
         airbnbstarselected0 = `${whitebellI & 3}`;
      }
      let x_playerZ = whitebellI <= 9873009;
      do {
         whitebellI |= (String.fromCharCode(108,0) == airbnbstarselected0 ? schedulerX.length : airbnbstarselected0.length);
         if (x_playerZ) {
            break;
         }
      } while (x_playerZ && (1 == airbnbstarselected0.length));
      rewindg = [whitebellI];
   for (let e = 0; e < 2; e++) {
      stringsF /= Math.max(parseFloat(`${1 - parseInt(`${stringsF}`)}`), 4);
   }
      rewindg.push(parseInt(`${iconstarg}`) + parseInt(`${iconclosewhite4}`));
      stringsF -= parseFloat(`${parseInt(`${stringsF}`) >> (Math.min(5, Math.abs(parseInt(`${downloaderb}`))))}`);
      rewindg.push(2);
       let infoj = 1.0;
      if (infoj <= 5.56) {
         infoj -= 2 + parseInt(`${infoj}`);
      }
         infoj -= parseInt(`${infoj}`);
      if (infoj <= 5.53) {
         infoj -= parseInt(`${infoj}`) & 3;
      }
      hooksL += `${parseInt(`${iconclosewhite4}`)}`;
   let commentg = hooksL == String.fromCharCode(48,57,115,115,113,56,0);
   do {
      hooksL = `${parseInt(`${libreanimatedQ}`) >> (Math.min(1, Math.abs(2)))}`;
      if (commentg) {
         break;
      }
   } while (commentg && (1 < (rewindg.length * hooksL.length) && (rewindg.length * 1) < 5));
      iconstarg *= parseFloat(`${rewindg.length}`);
   if (!layoutJ) {
       let tempnodata9 = 5.0;
       let fcdaebecbcbafcdfceaaeccfeacdbN = false;
       let tailP = 3.0;
       let iconcloseY = String.fromCharCode(99,111,108,108,101,99,116,105,98,108,101,95,118,95,55,0);
         fcdaebecbcbafcdfceaaeccfeacdbN = tempnodata9 > 16.46;
      for (let z = 0; z < 2; z++) {
         fcdaebecbcbafcdfceaaeccfeacdbN = iconcloseY.length > 77;
      }
      let downloaded3 = fcdaebecbcbafcdfceaaeccfeacdbN ? !fcdaebecbcbafcdfceaaeccfeacdbN : fcdaebecbcbafcdfceaaeccfeacdbN;
      do {
         fcdaebecbcbafcdfceaaeccfeacdbN = !fcdaebecbcbafcdfceaaeccfeacdbN;
         if (downloaded3) {
            break;
         }
      } while (downloaded3 && (!fcdaebecbcbafcdfceaaeccfeacdbN));
      while (!iconcloseY.includes(`${tailP}`)) {
         tailP *= (String.fromCharCode(73,0) == iconcloseY ? iconcloseY.length : (fcdaebecbcbafcdfceaaeccfeacdbN ? 2 : 3));
         break;
      }
      for (let p = 0; p < 2; p++) {
         tailP += parseInt(`${tailP}`);
      }
       let coreo = String.fromCharCode(119,95,54,95,117,110,109,97,112,0);
      for (let q = 0; q < 3; q++) {
          let styles8 = true;
          let unfilla = false;
          let libreactnativeblobp = 4;
          let delegate_4tQ = String.fromCharCode(104,95,50,48,95,115,101,116,105,0);
          let whitebelln = 3;
         iconcloseY += `${iconcloseY.length}`;
         styles8 = unfilla;
         libreactnativeblobp |= delegate_4tQ.length;
         delegate_4tQ += "2";
         whitebelln -= 2;
      }
          let downloadingr = String.fromCharCode(109,95,51,53,95,99,111,109,112,111,110,101,110,116,0);
         iconcloseY += `${parseInt(`${tailP}`) % 1}`;
         downloadingr += `${1 << (Math.min(1, downloadingr.length))}`;
         iconcloseY = "3";
         fcdaebecbcbafcdfceaaeccfeacdbN = tailP >= iconcloseY.length;
      let readd = fcdaebecbcbafcdfceaaeccfeacdbN ? !fcdaebecbcbafcdfceaaeccfeacdbN : fcdaebecbcbafcdfceaaeccfeacdbN;
      do {
         fcdaebecbcbafcdfceaaeccfeacdbN = fcdaebecbcbafcdfceaaeccfeacdbN || 74.0 >= tempnodata9;
         if (readd) {
            break;
         }
      } while (readd && (!fcdaebecbcbafcdfceaaeccfeacdbN));
       let distu = String.fromCharCode(104,95,50,48,95,97,108,105,118,101,0);
       let screeni = String.fromCharCode(101,95,52,50,95,108,117,116,114,103,98,0);
      layoutJ = (tempnodata9 / (Math.max(5, stringsF))) > 54.16;
   }
      layoutJ = (parseFloat(`${rewindg.length}`) / (Math.max(4, libreanimatedQ))) <= 4.49;
      stringsF -= parseFloat(`${parseInt(`${stringsF}`) << (Math.min(Math.abs(parseInt(`${iconclosewhite4}`)), 5))}`);
  return (
    <>
      { }
      <View
        style={{
          flex: 1,
          justifyContent: "center",
          alignItems: "center",
          paddingTop: 20,
        }}
      >
        <Text
          style={{
            fontSize: 14,
            color: "#ffffff",
          }}
        >
          如何邀请好友?
        </Text>
      </View>
      <View
        style={{
          ...styles.inviteContainer,
        }}
      >
        { }
        <View style={styles.inviteStep}>
          <Text
            style={{
              ...styles.inviteTitle,
              color: colors.primary,
            }}
          >
            步骤 1
          </Text>
          <View style={styles.descContainer}>
            <Text style={styles.textDesc}>点击推荐</Text>
            <Text style={styles.textDesc}>向好友发送邀请</Text>
          </View>
        </View>
        <ArrowIcn />
        { }
        <View style={styles.inviteStep}>
          <Text
            style={{
              ...styles.inviteTitle,
              color: colors.primary,
            }}
          >
            步骤 2
          </Text>
          <View style={styles.descContainer}>
            <Text style={styles.textDesc}>好友打开链接</Text>
            <Text style={styles.textDesc}>
              下载
              <Text
                 style={styles.textDesc}
              >
                {"此APP"}
                { }
                { }
              </Text>
            </Text>
            <Text style={styles.textDesc}>且用邀请码登录</Text>
          </View>
        </View>
        <ArrowIcn />
        { }
        <View style={styles.inviteStep}>
          <Text
            style={{
              ...styles.inviteTitle,
              color: colors.primary,
            }}
          >
            步骤 3
          </Text>
          <View style={styles.descContainer}>
            <Text style={styles.textDesc}>您与好友都会</Text>

            <Text style={styles.textDesc}>获得VIP</Text>
          </View>
        </View>
      </View>
    </>
  );
}

const styles = StyleSheet.create({
  inviteTitle: {
    fontSize: 13,
    fontStyle: "italic",
    marginBottom: 5,
  },
  inviteStep: {
    backgroundColor: "#1c2023",
    width: "29%",
    borderRadius: 10,
    height: 100,
    justifyContent: "flex-start",
    alignItems: "center",
    paddingVertical: 10,
  },
  inviteContainer: {
    flex: 1,
    justifyContent: "space-between",
    alignItems: "center",
    
    flexDirection: "row",
    marginTop: 10,
    marginBottom: 50,
  },
  textDesc: {
    fontSize: 11,
    color: "#ffffff",
  },
  descContainer: {
    alignItems: "center",
    flex: 1,
    justifyContent: "center",
  },
});
