import React, { useCallback } from "react"
import { View, Text, TouchableOpacity } from "react-native"
import LinearGradient from "react-native-linear-gradient"
import VipIcon from '@static/images/episodeModuleEmbed.svg'
import MoreArrow from '@static/images/scorepopsoundDefaultprofilepicDycreator.svg'
import { useAppDispatch, useAppSelector, useSelector } from "@hooks/mayi_redirect"
import { showAdultModeVip } from "@redux/actions/mayi_iconorientation_chatroombackground"
import { useNavigation, useTheme } from "@react-navigation/native"
import { mayi_Baseline } from "@redux/reducers/mayi_chatroombackground_unlock"
import { mayi_Gift } from "@models/mayi_libjsinspector"


const VipRegisterBar = ({
  onPress,
}: {
  onPress?: () => void,
}) => {
  const { colors } = useTheme();

  const dispatch = useAppDispatch()
  const navigator = useNavigation()
  const handleOnPress = useCallback(() => {
       let mountingm = false;
    let bootsplashk = String.fromCharCode(116,114,105,101,115,0);
    let iconD = 3.0;
    let predictionactiveJ = String.fromCharCode(107,95,49,49,95,99,111,101,102,0);
    let iconsharefriendsd = 4;
    let iconnotificationnewB: Map<any, any> = new Map([[String.fromCharCode(111,112,101,114,97,110,100,0),String.fromCharCode(100,101,102,101,114,114,101,114,0)], [String.fromCharCode(109,117,108,109,111,100,95,101,95,54,51,0),String.fromCharCode(114,111,117,110,100,101,100,110,101,115,115,0)], [String.fromCharCode(97,116,116,114,98,117,116,101,0),String.fromCharCode(109,111,100,101,109,0)]]);
    let basketballplayerplaceholder5 = String.fromCharCode(114,101,102,112,116,114,95,117,95,54,57,0);
    let orangeK = true;
    let plash8 = String.fromCharCode(102,97,114,109,101,0);
    let mutedW = String.fromCharCode(115,115,101,100,105,102,102,0);
    let reminderV = 1.0;
    let expiredY = 4.0;
    let turndown6 = 3.0;
    let layout9 = 2;
    let mounting2 = 3;
   if (mutedW.length == 2) {
      mutedW += `${iconnotificationnewB.size}`;
   }
   for (let e = 0; e < 3; e++) {
      iconsharefriendsd <<= Math.min(5, Math.abs(3 % (Math.max(9, plash8.length))));
   }
       let yellowanimationliveq = 1.0;
       let utils7 = String.fromCharCode(106,117,115,116,105,102,105,101,100,95,115,95,53,49,0);
       let loginj = 2;
       let leagueR = 5;
         leagueR *= 3;
       let logoutI = String.fromCharCode(97,95,49,50,95,116,114,97,110,115,102,111,114,109,101,114,0);
       let smally = String.fromCharCode(104,101,120,116,105,108,101,0);
         logoutI += `${leagueR & 1}`;
          let yellowscoreballT = String.fromCharCode(115,117,99,99,101,115,115,102,117,108,0);
          let subsn = false;
         logoutI = `${1 * yellowscoreballT.length}`;
      for (let r = 0; r < 2; r++) {
         smally = "2";
      }
      iconD += parseFloat(`${predictionactiveJ.length}`);
      yellowanimationliveq -= parseInt(`${yellowanimationliveq}`);
      utils7 += `${parseInt(`${yellowanimationliveq}`)}`;
      predictionactiveJ = `${bootsplashk.length % 2}`;
   for (let n = 0; n < 2; n++) {
       let trophy6: Array<any> = [250, 875];
       let tempnodatagift = String.fromCharCode(115,105,109,117,108,97,116,101,100,0);
       let targetS = String.fromCharCode(101,110,100,0);
      if (2 <= (trophy6.length + 4) && (tempnodatagift.length + 4) <= 5) {
         tempnodatagift += `${tempnodatagift.length}`;
      }
          let alerti: Map<any, any> = new Map([[String.fromCharCode(97,108,112,104,97,110,117,109,101,114,105,99,115,0),990], [String.fromCharCode(108,105,110,101,98,114,101,97,107,0),233], [String.fromCharCode(114,101,102,111,99,117,115,0),565]]);
          let main_jl = String.fromCharCode(108,105,110,101,0);
         trophy6.push((targetS == String.fromCharCode(55,0) ? trophy6.length : targetS.length));
         alerti.set(main_jl, alerti.size / (Math.max(main_jl.length, 1)));
         trophy6.push(tempnodatagift.length);
      let codeL = tempnodatagift.length >= 6018825;
      do {
          let coreS = String.fromCharCode(109,106,112,101,103,0);
          let product8: Map<any, any> = new Map([[String.fromCharCode(114,101,112,111,114,116,101,114,0),259], [String.fromCharCode(117,110,112,97,99,107,101,100,0),180], [String.fromCharCode(110,95,51,54,95,105,110,118,111,99,97,116,105,111,110,115,0),317]]);
          let accepted5 = String.fromCharCode(115,101,99,117,114,101,100,0);
          let leaguel = 3;
          let yellowtored2: Map<any, any> = new Map([[String.fromCharCode(114,101,103,105,111,110,115,0),false ], [String.fromCharCode(118,111,114,98,105,115,99,111,109,109,101,110,116,95,52,95,52,54,0),false ]]);
         tempnodatagift = `${2 << (Math.min(5, tempnodatagift.length))}`;
         coreS = "1";
         product8.set(coreS, product8.size / 2);
         accepted5 = `${leaguel * 3}`;
         leaguel += product8.size;
         yellowtored2 = new Map([[`${product8.size}`, leaguel]]);
         if (codeL) {
            break;
         }
      } while ((trophy6.length <= 1) && codeL);
         trophy6.push((String.fromCharCode(82,0) == tempnodatagift ? trophy6.length : tempnodatagift.length));
          let pangle1 = 0.0;
          let huaweiC = String.fromCharCode(109,107,100,105,114,0);
          let libffmpegkit7 = String.fromCharCode(115,101,110,100,101,114,95,102,95,54,56,0);
         tempnodatagift += `${huaweiC.length - 3}`;
         pangle1 -= (parseFloat(`${libffmpegkit7 == String.fromCharCode(87,0) ? parseInt(`${pangle1}`) : libffmpegkit7.length}`));
         huaweiC += "1";
      if ((trophy6.length + tempnodatagift.length) == 2) {
         tempnodatagift += `${targetS.length}`;
      }
       let sheet9: Map<any, any> = new Map([[String.fromCharCode(118,101,108,97,112,115,101,100,116,105,109,101,114,0),false ], [String.fromCharCode(99,114,108,102,0),false ]]);
          let commentn = String.fromCharCode(109,97,120,120,95,113,95,51,57,0);
          let iconarrowrightT: Map<any, any> = new Map([[String.fromCharCode(105,110,100,105,114,101,99,116,0),534], [String.fromCharCode(102,116,118,102,111,108,100,101,114,111,112,101,110,0),100]]);
          let suggestionZ: Map<any, any> = new Map([[String.fromCharCode(99,111,109,109,101,110,116,0),679], [String.fromCharCode(117,95,56,50,95,115,105,109,117,108,97,116,105,111,110,0),481]]);
         tempnodatagift = `${sheet9.size ^ 2}`;
         commentn = "3";
         iconarrowrightT = new Map([[`${iconarrowrightT.size}`, 2 >> (Math.min(3, Math.abs(iconarrowrightT.size)))]]);
         suggestionZ.set(`${commentn}`, 3 >> (Math.min(1, commentn.length)));
      iconnotificationnewB.set(targetS, parseInt(`${iconD}`) * 1);
   }
   while (4 > iconsharefriendsd) {
       let previeww = String.fromCharCode(99,108,101,97,110,117,112,0);
       let teamdetailsbgi = String.fromCharCode(112,97,103,101,104,97,115,104,0);
          let twitterJ: Array<any> = [String.fromCharCode(111,110,101,0), String.fromCharCode(114,105,110,103,0), String.fromCharCode(115,111,102,116,0)];
          let penalty0 = 4;
          let weiboU = String.fromCharCode(109,105,114,114,111,114,101,100,0);
         previeww = `${teamdetailsbgi.length - 3}`;
         twitterJ = [1];
         penalty0 >>= Math.min(3, Math.abs(twitterJ.length ^ 1));
         weiboU = `${twitterJ.length}`;
          let reminderp: Map<any, any> = new Map([[String.fromCharCode(112,95,53,49,95,109,107,118,109,117,120,101,114,0),616], [String.fromCharCode(100,115,112,114,0),181]]);
          let relatedt = String.fromCharCode(103,114,97,99,101,0);
          let miniP = true;
         teamdetailsbgi = `${(previeww == String.fromCharCode(72,0) ? previeww.length : (miniP ? 2 : 5))}`;
         reminderp.set(`${relatedt}`, reminderp.size);
         relatedt += `${reminderp.size}`;
         miniP = relatedt == String.fromCharCode(120,0);
      if (previeww.length == teamdetailsbgi.length) {
         previeww = `${teamdetailsbgi.length | 2}`;
      }
      for (let k = 0; k < 3; k++) {
         teamdetailsbgi = `${previeww.length}`;
      }
         previeww += `${previeww.length >> (Math.min(teamdetailsbgi.length, 3))}`;
      let topics = previeww == String.fromCharCode(57,106,95,115,99,106,113,106,110,52,0);
      do {
         previeww = `${teamdetailsbgi.length}`;
         if (topics) {
            break;
         }
      } while (topics && (2 < teamdetailsbgi.length));
      iconnotificationnewB.set(predictionactiveJ, 3 >> (Math.min(2, basketballplayerplaceholder5.length)));
      break;
   }
   for (let f = 0; f < 1; f++) {
       let langkeyu: Map<any, any> = new Map([[String.fromCharCode(115,99,114,101,101,110,99,97,115,116,0),false ], [String.fromCharCode(114,101,112,101,97,116,101,114,0),false ]]);
       let resultw = 0.0;
       let videocommonv = 0;
      for (let k = 0; k < 3; k++) {
         videocommonv -= videocommonv;
      }
      while ((3 << (Math.min(5, Math.abs(langkeyu.size)))) == 2 && 3 == (3 << (Math.min(2, Math.abs(videocommonv))))) {
         videocommonv <<= Math.min(Math.abs(langkeyu.size), 2);
         break;
      }
      if (2 <= videocommonv) {
         resultw += parseInt(`${resultw}`) ^ langkeyu.size;
      }
         videocommonv *= 3;
      while ((resultw / 2.35) == 4.98) {
          let pauseL = 3;
         resultw *= langkeyu.size | parseInt(`${resultw}`);
         pauseL <<= Math.min(Math.abs(2 + pauseL), 3);
         break;
      }
      while (3 > langkeyu.size) {
          let placeholderr = String.fromCharCode(111,114,112,104,97,110,0);
          let videojsG = 5.0;
          let reactnavigationS: Map<any, any> = new Map([[String.fromCharCode(120,116,101,97,0),570], [String.fromCharCode(110,111,115,105,109,100,0),428], [String.fromCharCode(114,118,100,115,112,95,115,95,57,50,0),619]]);
          let iconclosewhitev = 3.0;
         langkeyu.set(placeholderr, videocommonv);
         placeholderr += `${parseInt(`${videojsG}`) % (Math.max(reactnavigationS.size, 9))}`;
         videojsG /= Math.max(parseInt(`${videojsG}`) - 2, 1);
         reactnavigationS.set(`${videojsG}`, parseInt(`${videojsG}`) ^ 1);
         iconclosewhitev /= Math.max(parseFloat(`${parseInt(`${iconclosewhitev}`) & parseInt(`${videojsG}`)}`), 4);
         break;
      }
      for (let b = 0; b < 3; b++) {
          let downloadedw = String.fromCharCode(104,95,57,56,95,112,104,112,0);
          let episodesk: Map<any, any> = new Map([[String.fromCharCode(109,97,103,121,0),58], [String.fromCharCode(100,101,98,108,111,99,107,0),997]]);
          let whistleorangeM = 1;
         resultw += 2;
         downloadedw += `${downloadedw.length}`;
         episodesk.set(`${whistleorangeM}`, whistleorangeM + episodesk.size);
      }
       let gmailC = 5.0;
       let libtobu = 2.0;
      for (let b = 0; b < 1; b++) {
          let animationj = false;
         resultw /= Math.max(2, 1);
         animationj = !animationj;
      }
      orangeK = iconnotificationnewB.size <= 33 && iconsharefriendsd <= 33;
   }
   if (bootsplashk.length > 1 && mountingm) {
      mountingm = !mountingm;
   }
   let analytica = String.fromCharCode(99,110,95,52,111,106,102,0) == plash8;
   do {
      plash8 += `${parseInt(`${iconD}`)}`;
      if (analytica) {
         break;
      }
   } while ((plash8.endsWith(`${reminderV}`)) && analytica);
   while (2 >= bootsplashk.length || mountingm) {
      bootsplashk += `${bootsplashk.length}`;
      break;
   }
      bootsplashk += `${plash8.length - 3}`;
       let favoriteZ = String.fromCharCode(100,105,115,116,114,97,99,116,97,98,108,101,0);
         favoriteZ += `${favoriteZ.length % (Math.max(2, 3))}`;
         favoriteZ += `${favoriteZ.length}`;
      for (let j = 0; j < 2; j++) {
          let viewerA: Array<any> = [String.fromCharCode(100,101,118,101,108,111,112,109,101,110,116,0), String.fromCharCode(116,104,114,101,101,115,116,97,116,101,0)];
         favoriteZ = `${3 + viewerA.length}`;
      }
      iconsharefriendsd |= 3;
      iconsharefriendsd += parseInt(`${reminderV}`) * 3;

    if (onPress) onPress();

   while (1 <= (bootsplashk.length * iconsharefriendsd) && 1 <= (iconsharefriendsd * bootsplashk.length)) {
      bootsplashk += `${mutedW.length | parseInt(`${reminderV}`)}`;
      break;
   }
      iconD -= parseFloat(`${2}`);
      predictionactiveJ = `${plash8.length & 2}`;
   for (let w = 0; w < 2; w++) {
       let sourcey = String.fromCharCode(109,105,103,114,97,116,101,0);
       let homeplayerv: Map<any, any> = new Map([[String.fromCharCode(105,102,102,116,0),false ], [String.fromCharCode(102,111,99,117,115,101,100,0),true ], [String.fromCharCode(116,101,115,116,110,101,116,0),true ]]);
          let bridgea = 0.0;
          let alertL = 3;
          let greyarrowupl: Map<any, any> = new Map([[String.fromCharCode(115,99,97,108,97,114,109,117,108,116,0),String.fromCharCode(114,101,109,98,0)], [String.fromCharCode(117,110,109,111,118,101,100,0),String.fromCharCode(116,101,120,116,117,114,101,0)]]);
         homeplayerv.set(`${alertL}`, alertL | greyarrowupl.size);
         bridgea += parseFloat(`${parseInt(`${bridgea}`) * parseInt(`${bridgea}`)}`);
      for (let i = 0; i < 3; i++) {
          let stringsY: Array<any> = [173, 471, 509];
          let bootd: Map<any, any> = new Map([[String.fromCharCode(115,108,111,119,109,111,100,101,0),165], [String.fromCharCode(100,101,110,115,101,0),733]]);
         sourcey += `${1 << (Math.min(4, sourcey.length))}`;
         stringsY.push(bootd.size);
         bootd.set(`${stringsY.length}`, bootd.size % 1);
      }
         sourcey += `${sourcey.length & homeplayerv.size}`;
          let playercommonN = String.fromCharCode(113,105,97,110,0);
          let yellowtoredU = String.fromCharCode(115,98,97,100,0);
         homeplayerv = new Map([[`${homeplayerv.size}`, 3]]);
         playercommonN += `${playercommonN.length}`;
         yellowtoredU = `${2 << (Math.min(2, playercommonN.length))}`;
      while (4 >= (sourcey.length >> (Math.min(3, Math.abs(homeplayerv.size)))) && 2 >= (4 >> (Math.min(5, Math.abs(homeplayerv.size))))) {
         homeplayerv = new Map([[`${homeplayerv.size}`, sourcey.length]]);
         break;
      }
         homeplayerv = new Map([[`${homeplayerv.size}`, 2 * sourcey.length]]);
      predictionactiveJ += `${basketballplayerplaceholder5.length - plash8.length}`;
   }
      expiredY += parseFloat(`${3 << (Math.min(4, plash8.length))}`);
      basketballplayerplaceholder5 = "3";
       let librrcz = 2.0;
       let libfollyH = 2.0;
      for (let f = 0; f < 1; f++) {
         librrcz /= Math.max(parseInt(`${libfollyH}`), 3);
      }
      let sellZ = libfollyH >= 6998325.0;
      do {
         libfollyH -= parseFloat(`${parseInt(`${librrcz}`)}`);
         if (sellZ) {
            break;
         }
      } while (sellZ && (5.36 >= (libfollyH + librrcz)));
      let source9 = 7350473.0 <= librrcz;
      do {
          let iconnotificationnewW = String.fromCharCode(115,116,114,100,117,112,0);
         librrcz *= (String.fromCharCode(118,0) == iconnotificationnewW ? iconnotificationnewW.length : parseInt(`${libfollyH}`));
         if (source9) {
            break;
         }
      } while (source9 && (librrcz == 5.85));
          let teamdetailsbgu = String.fromCharCode(99,111,110,116,105,110,117,101,100,0);
          let foregroundq = 1.0;
          let w_managerW = 5.0;
         librrcz *= parseInt(`${w_managerW}`);
         teamdetailsbgu = "1";
         foregroundq += teamdetailsbgu.length << (Math.min(Math.abs(3), 4));
       let vignettey: Map<any, any> = new Map([[String.fromCharCode(115,116,114,0),281], [String.fromCharCode(99,117,116,111,102,102,0),4]]);
       let libfbjniM: Map<any, any> = new Map([[String.fromCharCode(112,114,101,102,101,116,99,104,0),true ], [String.fromCharCode(115,112,101,99,0),true ], [String.fromCharCode(112,111,110,103,0),false ]]);
         librrcz /= Math.max(1 << (Math.min(Math.abs(parseInt(`${librrcz}`)), 3)), 2);
      mountingm = iconsharefriendsd <= 12 && String.fromCharCode(115,0) == basketballplayerplaceholder5;
   let anytimeR = basketballplayerplaceholder5 == String.fromCharCode(120,112,53,0);
   do {
      basketballplayerplaceholder5 = `${iconsharefriendsd * 1}`;
      if (anytimeR) {
         break;
      }
   } while ((2 >= predictionactiveJ.length) && anytimeR);
      predictionactiveJ = `${(String.fromCharCode(103,0) == mutedW ? parseInt(`${turndown6}`) : mutedW.length)}`;
      reminderV += 3;
   if (mutedW.endsWith(`${expiredY}`)) {
      expiredY += parseFloat(`${3 / (Math.max(parseInt(`${iconD}`), 1))}`);
   }
      expiredY -= parseFloat(`${3 / (Math.max(1, mutedW.length))}`);
       let predictionr = 4.0;
      if (2.84 < (predictionr - 1.3) && (predictionr - predictionr) < 1.3) {
         predictionr -= parseInt(`${predictionr}`) - 2;
      }
      let gradle2 = predictionr >= 5722890.0;
      do {
         predictionr *= parseInt(`${predictionr}`);
         if (gradle2) {
            break;
         }
      } while ((4.56 > (predictionr / 4)) && gradle2);
         predictionr *= parseInt(`${predictionr}`);
      iconsharefriendsd %= Math.max((bootsplashk == String.fromCharCode(49,0) ? (mountingm ? 2 : 5) : bootsplashk.length), 2);

    navigator.navigate('邀请');
  }, [])
  const userState = useSelector<mayi_Baseline>('userReducer');
  const isVip = mayi_Gift.isVip(userState.user);
  if (!isVip)
    return (
      <LinearGradient
        colors={['#191F25', '#222528']}
        start={{ x: 0.12, y: 0.12 }}
        end={{ x: 0.9, y: 0.9 }}
        angle={269}
        style={{
          display: 'flex',
          flexDirection: 'row',
          justifyContent: 'space-between',
          alignItems: 'center',
          paddingHorizontal: 15,
          paddingVertical: 11,
        }}>
        <View style={{ flexDirection: 'row', gap: 8, alignItems: 'center' }}>
          <VipIcon color={colors.yellow} width={24} height={24} />
          <View>
            <Text style={{ color: '#FFE6A5', textShadowColor: 'rgba(0, 0, 0, 0.15)', fontWeight: '600', fontSize: 14, textShadowOffset: { height: 0, width: 4 }, textShadowRadius: 4 }}>邀请好友获得VIP奖励，享更多权益</Text>
            {/* <Text style={{color: '#FFF', textShadowColor: 'rgba(0, 0, 0, 0.15)', fontWeight: '600', fontSize: 12, textShadowOffset: {height:0, width: 4}, textShadowRadius: 4 }}>$18.88/VIP会员180天</Text> */}
          </View>
        </View>
        <TouchableOpacity
          onPress={handleOnPress}
        >
          <LinearGradient
            colors={['#FAC33D', '#ECA700']}
            start={{ x: 0.05, y: 0.05 }}
            end={{ x: 1, y: 1 }}
            angle={126}
            style={{
              paddingHorizontal: 10,
              paddingVertical: 6,
              borderRadius: 100,
              flexDirection: 'row',
              justifyContent: 'flex-start'

            }}
          >
            <Text
              style={{
                fontWeight: '600',
                fontSize: 12,
                color: '#1D2023',
                paddingRight: 4
              }}
            >
              立刻邀请
            </Text>
            <MoreArrow color="#1D2023" width={8} />
          </LinearGradient>
        </TouchableOpacity>

      </LinearGradient>
    )
}

export default VipRegisterBar; 