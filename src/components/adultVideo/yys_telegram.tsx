import React, { useCallback } from "react"
import { View, Text, TouchableOpacity } from "react-native"
import LinearGradient from "react-native-linear-gradient"
import VipIcon from '@static/images/feedbackComment.svg'
import MoreArrow from '@static/images/toponLang.svg'
import { useAppDispatch, useAppSelector, useSelector } from "@hooks/yys_frame"
import { showAdultModeVip } from "@redux/actions/yys_runtimescheduler"
import { useNavigation } from "@react-navigation/native"
import { yys_HejiCricket } from "@redux/reducers/yys_privacy_round"
import { yys_RelatedTooltips } from "@models/yys_project_pagination"


const VipRegisterBar = ({
   onPress,
}: {
   onPress?: () => void,
}) => {
   const dispatch = useAppDispatch()
   const navigator = useNavigation()
   const handleOnPress = useCallback(() => {
      let liveZ = 3;
      let auto_x3L = 0.0;
      let runtimeschedulerC = 1.0;
      let codegen3 = String.fromCharCode(110, 95, 52, 51, 95, 120, 99, 111, 100, 101, 0);
      let downO = true;
      let shirt5 = 3;
      let countrym = false;
      let fullK = 4.0;
      let benefitp = 2;
      let bufferv = 5.0;
      let whatsappr = 3.0;
      let detailx = 2.0;
      let mimoW = String.fromCharCode(99, 116, 114, 108, 95, 52, 95, 53, 49, 0);
      let filedu: Array<any> = [456, 829];
      detailx -= parseFloat(`${3}`);
      codegen3 += `${shirt5}`;
      codegen3 += `${(mimoW == String.fromCharCode(104, 0) ? liveZ : mimoW.length)}`;
      let gmailK = 4.0;
      let scrollviewC = true;
      let sentryQ = 0;
      scrollviewC = gmailK == 84.75;
      fullK /= Math.max(4, parseInt(`${auto_x3L}`));
      for (let n = 0; n < 2; n++) {
         let pingy = 2.0;
         let forwardy = String.fromCharCode(97, 116, 114, 95, 122, 95, 51, 52, 0);
         let charti = 0;
         let abidetectR = 0.0;
         while (!forwardy.endsWith(`${pingy}`)) {
            let telemetryk = String.fromCharCode(99, 117, 114, 114, 101, 110, 100, 95, 109, 95, 51, 53, 0);
            let dragI = false;
            let kuaishoul = 3;
            let frame__1 = true;
            let analytic2: Array<any> = [String.fromCharCode(99, 97, 109, 101, 108, 108, 105, 97, 95, 122, 95, 55, 57, 0), String.fromCharCode(116, 111, 112, 105, 99, 95, 118, 95, 50, 56, 0)];
            pingy /= Math.max(parseInt(`${pingy}`) & kuaishoul, 4);
            telemetryk += `${analytic2.length}`;
            dragI = analytic2.includes(frame__1);
            kuaishoul += 3;
            break;
         }
         let trophyd = forwardy == String.fromCharCode(107, 108, 105, 120, 55, 100, 116, 100, 105, 0);
         do {
            let tickedd = String.fromCharCode(119, 95, 54, 51, 95, 99, 102, 102, 116, 98, 0);
            let textg = 0.0;
            let storee = String.fromCharCode(120, 95, 52, 56, 95, 111, 110, 108, 121, 0);
            let bannere = String.fromCharCode(97, 110, 97, 108, 121, 122, 105, 110, 103, 95, 115, 95, 54, 53, 0);
            forwardy = `${parseInt(`${pingy}`)}`;
            tickedd += `${tickedd.length + bannere.length}`;
            textg -= parseInt(`${textg}`) & tickedd.length;
            storee = `${storee.length % 2}`;
            bannere = `${tickedd.length}`;
            if (trophyd) {
               break;
            }
         } while (trophyd && ((forwardy.length % 1) <= 2));
         while (abidetectR > 1.89) {
            abidetectR *= parseFloat(`${parseInt(`${abidetectR}`) | 3}`);
            break;
         }
         for (let c = 0; c < 1; c++) {
            let libcrashsdkt = String.fromCharCode(107, 95, 49, 50, 95, 101, 110, 99, 105, 112, 104, 101, 114, 0);
            forwardy = `${2 % (Math.max(10, charti))}`;
            libcrashsdkt = `${libcrashsdkt.length >> (Math.min(Math.abs(2), 1))}`;
         }
         let borderlessd = 0;
         let binddatas0 = 4;
         let thumbnailo: Array<any> = [362, 324, 600];
         pingy /= Math.max(forwardy.length, 2);
         borderlessd &= thumbnailo.length;
         binddatas0 -= thumbnailo.length % (Math.max(2, 10));
         abidetectR /= Math.max(2, parseFloat(`${1 / (Math.max(4, parseInt(`${abidetectR}`)))}`));
         charti <<= Math.min(4, Math.abs(3 + charti));
         while (4.47 >= pingy) {
            let savev = 3;
            let login4 = 3.0;
            let downx = false;
            let qaagB = String.fromCharCode(111, 95, 51, 50, 95, 99, 97, 110, 99, 101, 108, 108, 97, 98, 108, 101, 0);
            let componentQ = String.fromCharCode(107, 95, 53, 52, 95, 112, 101, 114, 112, 101, 110, 100, 105, 99, 117, 108, 97, 114, 0);
            charti |= qaagB.length;
            savev >>= Math.min(5, Math.abs(savev + 2));
            login4 -= (parseFloat(`${componentQ == String.fromCharCode(52, 0) ? (downx ? 2 : 3) : componentQ.length}`));
            downx = 99.52 < login4;
            qaagB = `${(1 ^ (downx ? 1 : 3))}`;
            break;
         }
         charti -= charti;
         while ((charti & 2) > 1) {
            forwardy = `${parseInt(`${pingy}`)}`;
            break;
         }
         abidetectR *= parseFloat(`${3 % (Math.max(parseInt(`${abidetectR}`), 5))}`);
         let h_counta = String.fromCharCode(116, 104, 105, 99, 107, 95, 113, 95, 49, 49, 0);
         let large9 = String.fromCharCode(115, 109, 100, 109, 95, 97, 95, 54, 52, 0);
         pingy += (String.fromCharCode(66, 0) == h_counta ? h_counta.length : large9.length);
         detailx *= parseFloat(`${parseInt(`${runtimeschedulerC}`) << (Math.min(Math.abs(parseInt(`${whatsappr}`)), 4))}`);
      }
      for (let o = 0; o < 1; o++) {
         fullK -= 3;
      }
      while (2.60 > (fullK - codegen3.length)) {
         codegen3 = `${liveZ}`;
         break;
      }
      while (3.71 < (liveZ - whatsappr)) {
         liveZ += parseInt(`${runtimeschedulerC}`) | codegen3.length;
         break;
      }
      for (let r = 0; r < 2; r++) {
         let switch_6n: Array<any> = [297, 431];
         let tooltipsr = 3.0;
         let pauseA: Map<any, any> = new Map([[String.fromCharCode(101, 95, 55, 95, 105, 110, 118, 101, 114, 115, 101, 0), 696], [String.fromCharCode(118, 102, 108, 97, 103, 95, 108, 95, 53, 49, 0), 25], [String.fromCharCode(113, 95, 51, 52, 95, 99, 117, 115, 116, 111, 109, 105, 122, 101, 114, 0), 858]]);
         let libjsijniprofilerT = String.fromCharCode(109, 101, 100, 105, 97, 99, 111, 100, 101, 99, 95, 108, 95, 53, 55, 0);
         let countdownD = String.fromCharCode(106, 95, 49, 48, 95, 103, 101, 110, 100, 101, 114, 0);
         let vignetted = String.fromCharCode(117, 112, 115, 101, 114, 116, 95, 122, 95, 50, 48, 0);
         let modityF = String.fromCharCode(121, 95, 53, 53, 95, 99, 111, 110, 99, 104, 0);
         while (modityF != libjsijniprofilerT) {
            libjsijniprofilerT = `${pauseA.size | libjsijniprofilerT.length}`;
            break;
         }
         let ticked5 = 1;
         let usernameL = false;
         let libreactd = String.fromCharCode(101, 120, 99, 108, 117, 100, 101, 100, 95, 109, 95, 51, 53, 0);
         libjsijniprofilerT += `${ticked5}`;
         ticked5 /= Math.max(3, 5);
         usernameL = (((!usernameL ? 98 : libreactd.length) / (Math.max(libreactd.length, 4))) <= 98);
         let activity0 = libjsijniprofilerT.length <= 6249326;
         do {
            libjsijniprofilerT += `${2 % (Math.max(2, countdownD.length))}`;
            if (activity0) {
               break;
            }
         } while (activity0 && ((pauseA.size << (Math.min(Math.abs(3), 4))) < 4));
         countdownD = `${2 >> (Math.min(5, countdownD.length))}`;
         let reactnativejsw = 0.0;
         let pageP = 2.0;
         libjsijniprofilerT += `${modityF.length * 3}`;
         reactnativejsw /= Math.max(parseFloat(`${parseInt(`${reactnativejsw}`) & 2}`), 1);
         pageP -= parseFloat(`${parseInt(`${reactnativejsw}`)}`);
         let rootM: Map<any, any> = new Map([[String.fromCharCode(121, 101, 115, 116, 101, 114, 100, 97, 121, 95, 108, 95, 52, 53, 0), String.fromCharCode(115, 101, 116, 95, 105, 95, 52, 52, 0)], [String.fromCharCode(120, 95, 53, 49, 95, 99, 116, 120, 105, 100, 120, 105, 110, 99, 0), String.fromCharCode(107, 95, 51, 52, 95, 105, 116, 101, 114, 97, 116, 105, 111, 110, 0)], [String.fromCharCode(108, 97, 115, 116, 109, 98, 117, 102, 95, 119, 95, 53, 57, 0), String.fromCharCode(121, 95, 49, 52, 95, 112, 111, 111, 108, 0)]]);
         switch_6n.push(parseInt(`${tooltipsr}`) >> (Math.min(switch_6n.length, 4)));
         rootM = new Map([[libjsijniprofilerT, libjsijniprofilerT.length]]);
         while (vignetted.length >= 5) {
            modityF += `${3 ^ modityF.length}`;
            break;
         }
         let libzeusf = 5917888 <= libjsijniprofilerT.length;
         do {
            libjsijniprofilerT = `${rootM.size + vignetted.length}`;
            if (libzeusf) {
               break;
            }
         } while (libzeusf && (modityF.length >= libjsijniprofilerT.length));
         let shootk = String.fromCharCode(103, 101, 110, 101, 115, 105, 115, 95, 50, 95, 56, 54, 0);
         let success1 = String.fromCharCode(113, 95, 57, 50, 95, 98, 97, 99, 107, 101, 100, 0);
         while ((countdownD.length ^ rootM.size) >= 2 || (rootM.size ^ 2) >= 5) {
            rootM = new Map([[`${rootM.size}`, (String.fromCharCode(82, 0) == libjsijniprofilerT ? rootM.size : libjsijniprofilerT.length)]]);
            break;
         }
         vignetted += `${pauseA.size}`;
         switch_6n = [modityF.length / (Math.max(1, countdownD.length))];
         runtimeschedulerC += parseInt(`${bufferv}`);
      }
      while (2.80 == (detailx * runtimeschedulerC) || (2.80 * runtimeschedulerC) == 4.33) {
         detailx *= (parseFloat(`${(downO ? 2 : 2) ^ parseInt(`${whatsappr}`)}`));
         break;
      }
      for (let a = 0; a < 2; a++) {
         runtimeschedulerC *= 2;
      }
      codegen3 += `${parseInt(`${fullK}`) * 3}`;
      if ((shirt5 * 2) >= 4 && 2.52 >= (fullK + 5.33)) {
         let modes = String.fromCharCode(119, 95, 54, 52, 95, 99, 111, 110, 115, 105, 115, 116, 101, 110, 99, 121, 0);
         let anythinkU = String.fromCharCode(114, 101, 99, 111, 110, 102, 105, 103, 95, 115, 95, 55, 49, 0);
         let otherP = String.fromCharCode(116, 119, 101, 97, 107, 115, 95, 113, 95, 55, 51, 0);
         if (1 < otherP.length) {
            let matchD = String.fromCharCode(100, 105, 118, 105, 100, 101, 110, 100, 95, 105, 95, 50, 55, 0);
            anythinkU += `${modes.length}`;
            matchD += `${matchD.length}`;
         }
         anythinkU = `${anythinkU.length}`;
         modes += `${3 - otherP.length}`;
         otherP = "3";
         if (anythinkU.length < 5 && otherP.length < 5) {
            otherP += `${2 | modes.length}`;
         }
         let holderj = 9578282 <= anythinkU.length;
         do {
            let securityL = String.fromCharCode(99, 97, 112, 116, 117, 114, 105, 110, 103, 95, 55, 95, 53, 0);
            anythinkU += `${1 | securityL.length}`;
            if (holderj) {
               break;
            }
         } while ((modes == String.fromCharCode(52, 0)) && holderj);
         let backwarda: Array<any> = [457, 883, 653];
         let bridgeg = String.fromCharCode(114, 95, 57, 49, 95, 112, 101, 114, 109, 105, 115, 115, 105, 111, 110, 115, 0);
         let animationsq: Map<any, any> = new Map([[String.fromCharCode(117, 110, 114, 101, 102, 101, 114, 101, 110, 99, 101, 95, 56, 95, 51, 52, 0), 222], [String.fromCharCode(116, 97, 103, 98, 105, 116, 95, 98, 95, 52, 55, 0), 353], [String.fromCharCode(117, 110, 109, 97, 114, 107, 95, 104, 95, 49, 48, 48, 0), 367]]);
         otherP += `${modes.length * 1}`;
         backwarda.push(2);
         bridgeg = `${animationsq.size}`;
         animationsq.set(`${bridgeg}`, animationsq.size);
         let commons = 2.0;
         anythinkU += `${parseInt(`${commons}`)}`;
         while (3 > modes.length || anythinkU != String.fromCharCode(111, 0)) {
            anythinkU = `${otherP.length}`;
            break;
         }
         fullK -= benefitp & 1;
      }
      let logoutw = 0;
      let libmapbufferjnii = String.fromCharCode(121, 95, 54, 55, 95, 105, 110, 100, 105, 99, 101, 115, 0);
      let analytich = 2.0;
      for (let e = 0; e < 1; e++) {
         libmapbufferjnii = `${(String.fromCharCode(106, 0) == libmapbufferjnii ? libmapbufferjnii.length : logoutw)}`;
      }
      let uimanagero = String.fromCharCode(115, 105, 122, 101, 108, 101, 115, 115, 95, 101, 95, 54, 56, 0);
      let panglex = String.fromCharCode(114, 95, 51, 55, 95, 112, 111, 115, 116, 115, 99, 97, 108, 101, 0);
      while (libmapbufferjnii.includes(`${logoutw}`)) {
         libmapbufferjnii += `${libmapbufferjnii.length >> (Math.min(Math.abs(2), 1))}`;
         break;
      }
      let feedbacky = panglex == String.fromCharCode(113, 54, 100, 115, 95, 57, 101, 57, 0);
      do {
         let closed: Map<any, any> = new Map([[String.fromCharCode(99, 111, 109, 112, 111, 115, 101, 95, 57, 95, 54, 54, 0), false], [String.fromCharCode(112, 97, 116, 99, 104, 115, 101, 116, 95, 111, 95, 49, 53, 0), false]]);
         let giftJ = 1.0;
         let philippines9 = 0.0;
         panglex = "2";
         closed.set(`${philippines9}`, parseInt(`${philippines9}`) % (Math.max(closed.size, 7)));
         giftJ /= Math.max(parseFloat(`${parseInt(`${philippines9}`) % (Math.max(2, parseInt(`${giftJ}`)))}`), 1);
         if (feedbacky) {
            break;
         }
      } while ((libmapbufferjnii == String.fromCharCode(69, 0)) && feedbacky);
      libmapbufferjnii = `${parseInt(`${analytich}`) ^ 1}`;
      for (let h = 0; h < 3; h++) {
         let awayB = 5;
         let emojiP = false;
         let showO = String.fromCharCode(103, 95, 53, 49, 95, 112, 101, 114, 109, 101, 97, 116, 101, 0);
         let sendv = 4.0;
         let libjsinspector3 = false;
         uimanagero += `${2 & showO.length}`;
         awayB ^= 1 & parseInt(`${sendv}`);
         emojiP = 93 < awayB;
         showO += `${awayB}`;
         sendv += parseInt(`${sendv}`) << (Math.min(2, Math.abs(2)));
         libjsinspector3 = libjsinspector3 || awayB < 74;
      }
      let suggestionJ = uimanagero.length <= 6085167;
      do {
         uimanagero += `${logoutw}`;
         if (suggestionJ) {
            break;
         }
      } while (suggestionJ && (!uimanagero.startsWith(panglex)));
      uimanagero += `${panglex.length}`;
      let textlayoutmanagerY = 1.0;
      let viewerx = true;
      let libzeusH = 1;
      logoutw ^= 3 + parseInt(`${textlayoutmanagerY}`);
      textlayoutmanagerY /= Math.max(libzeusH, 2);
      viewerx = libzeusH < 12 || viewerx;
      liveZ <<= Math.min(5, Math.abs(shirt5));

      if (onPress) onPress();

      let stationO = 5.0;
      stationO /= Math.max(parseInt(`${stationO}`), 1);
      let gesturesl = stationO <= 9281749.0;
      do {
         stationO -= 2 << (Math.min(Math.abs(parseInt(`${stationO}`)), 3));
         if (gesturesl) {
            break;
         }
      } while (gesturesl && (1.94 >= (stationO + 5.100) || (stationO + 5.100) >= 4.94));
      if (stationO <= stationO) {
         stationO *= 3 << (Math.min(Math.abs(parseInt(`${stationO}`)), 1));
      }
      benefitp <<= Math.min(3, Math.abs(2));
      codegen3 += `${codegen3.length}`;
      for (let y = 0; y < 2; y++) {
         let nativei = 1.0;
         let directc = true;
         let texti = false;
         let logouta: Array<any> = [93, 742, 756];
         texti = !directc;
         while (texti) {
            let libreactq = String.fromCharCode(114, 95, 51, 56, 95, 115, 101, 110, 100, 109, 98, 117, 102, 0);
            let singaporeT = String.fromCharCode(113, 95, 52, 95, 115, 121, 115, 108, 111, 103, 0);
            let modityu: Array<any> = [79, 787];
            directc = directc && !texti;
            libreactq += `${1 * singaporeT.length}`;
            singaporeT = `${libreactq.length + modityu.length}`;
            modityu = [singaporeT.length];
            break;
         }
         for (let c = 0; c < 3; c++) {
            directc = !directc;
         }
         while (!texti || 4.60 < (nativei / (Math.max(3.32, 8)))) {
            nativei *= parseInt(`${nativei}`) % 1;
            break;
         }
         while (4.34 >= (nativei * 3.64)) {
            let selectU = String.fromCharCode(112, 95, 51, 50, 95, 115, 105, 109, 117, 108, 97, 116, 105, 111, 110, 0);
            let privileges = String.fromCharCode(99, 95, 56, 48, 95, 100, 117, 112, 115, 0);
            let whiteO = String.fromCharCode(110, 95, 52, 48, 95, 112, 97, 115, 116, 101, 98, 111, 97, 114, 100, 0);
            let directF: Array<any> = [705, 612, 308];
            let modeT = String.fromCharCode(103, 105, 102, 115, 95, 51, 95, 56, 54, 0);
            texti = directF.length >= 45;
            selectU += `${3 ^ selectU.length}`;
            privileges = `${whiteO.length}`;
            whiteO = `${3 + modeT.length}`;
            directF.push(privileges.length);
            modeT = `${modeT.length}`;
            break;
         }
         directc = directc && logouta.length <= 7;
         for (let s = 0; s < 3; s++) {
            directc = !directc;
         }
         while (logouta.length == nativei) {
            logouta = [2 ^ parseInt(`${nativei}`)];
            break;
         }
         let thailandG = 6435166.0 <= nativei;
         do {
            let taiwank: Array<any> = [50, 703, 30];
            let q_lockB = false;
            let pangleb = 3.0;
            nativei *= 2;
            taiwank.push(taiwank.length);
            q_lockB = taiwank.length <= 43 || !q_lockB;
            pangleb *= 2;
            if (thailandG) {
               break;
            }
         } while (thailandG && ((4 | logouta.length) == 4 && (nativei * 3.28) == 4.10));
         while (5 > (logouta.length / (Math.max(4, 4))) && 4 > (logouta.length - parseInt(`${nativei}`))) {
            let chinaX = 1.0;
            logouta = [logouta.length ^ parseInt(`${chinaX}`)];
            break;
         }
         let matchm = 2;
         let lineo = 3;
         let bottomf = String.fromCharCode(98, 114, 101, 97, 107, 115, 95, 103, 95, 55, 52, 0);
         matchm >>= Math.min(2, Math.abs(lineo & 2));
         bottomf = `${bottomf.length}`;
         benefitp |= 2;
      }
      benefitp /= Math.max(codegen3.length, 3);
      if (liveZ < detailx) {
         liveZ |= 3 % (Math.max(8, codegen3.length));
      }
      if ((detailx / (Math.max(5, auto_x3L))) >= 3.17 || 1.50 >= (auto_x3L / 3.17)) {
         detailx /= Math.max(parseFloat(`${parseInt(`${bufferv}`)}`), 4);
      }
      let incidentd = countrym ? !countrym : countrym;
      do {
         countrym = shirt5 <= 77 && benefitp <= 77;
         if (incidentd) {
            break;
         }
      } while (incidentd && (codegen3.startsWith(`${countrym}`)));
      while (4 < codegen3.length) {
         liveZ <<= Math.min(2, Math.abs(parseInt(`${fullK}`) << (Math.min(codegen3.length, 2))));
         break;
      }
      liveZ >>= Math.min(1, Math.abs(2));
      if ((5.50 - detailx) > 3.74 && (detailx - 5.50) > 2.18) {
         bufferv *= parseFloat(`${parseInt(`${detailx}`)}`);
      }
      countrym = 61 < (benefitp + runtimeschedulerC);
      let private_3mB = 4;
      let minivod9: Array<any> = [716, 148];
      for (let x = 0; x < 1; x++) {
         private_3mB >>= Math.min(3, Math.abs(private_3mB + 3));
      }
      let ewardedM = 2.0;
      let muted7 = 6388779 >= minivod9.length;
      do {
         minivod9 = [minivod9.length / (Math.max(4, parseInt(`${ewardedM}`)))];
         if (muted7) {
            break;
         }
      } while (muted7 && ((4 + private_3mB) > 4 || (private_3mB + minivod9.length) > 4));
      minivod9 = [1 - parseInt(`${ewardedM}`)];
      let minimizeC = String.fromCharCode(99, 95, 50, 95, 109, 98, 101, 100, 0);
      let with_0Q: Map<any, any> = new Map([[String.fromCharCode(111, 110, 116, 111, 95, 100, 95, 50, 54, 0), 384], [String.fromCharCode(101, 110, 99, 108, 97, 118, 101, 95, 106, 95, 54, 50, 0), 581]]);
      minivod9 = [1];
      minimizeC += "2";
      with_0Q = new Map([[`${with_0Q.size}`, with_0Q.size]]);
      for (let y = 0; y < 1; y++) {
         minivod9 = [parseInt(`${ewardedM}`) >> (Math.min(1, Math.abs(private_3mB)))];
      }
      shirt5 &= 3 - parseInt(`${bufferv}`);
      for (let s = 0; s < 2; s++) {
         runtimeschedulerC /= Math.max(((downO ? 4 : 3) ^ shirt5), 3);
      }
      let selecti: Array<any> = [String.fromCharCode(106, 95, 56, 48, 95, 108, 111, 103, 103, 105, 110, 103, 0), String.fromCharCode(98, 95, 49, 50, 95, 97, 117, 116, 104, 111, 114, 105, 122, 101, 0), String.fromCharCode(114, 101, 106, 111, 105, 110, 95, 115, 95, 54, 57, 0)];
      let nativemoduleA: Map<any, any> = new Map([[String.fromCharCode(115, 97, 118, 101, 95, 120, 95, 57, 56, 0), String.fromCharCode(100, 95, 52, 51, 95, 116, 111, 107, 101, 110, 105, 122, 101, 100, 0)], [String.fromCharCode(104, 115, 99, 97, 108, 101, 114, 95, 111, 95, 55, 49, 0), String.fromCharCode(116, 104, 114, 111, 119, 95, 120, 95, 55, 50, 0)], [String.fromCharCode(116, 121, 112, 101, 100, 95, 120, 95, 56, 50, 0), String.fromCharCode(112, 114, 111, 100, 115, 95, 117, 95, 55, 49, 0)]]);
      selecti = [selecti.length];
      nativemoduleA = new Map([[`${nativemoduleA.size}`, 2 + nativemoduleA.size]]);
      let comment4 = 9856148 >= nativemoduleA.size;
      do {
         nativemoduleA.set(`${selecti.length}`, selecti.length / 2);
         if (comment4) {
            break;
         }
      } while ((2 <= (nativemoduleA.size & 3) || 1 <= (selecti.length & 3)) && comment4);
      while ((nativemoduleA.size << (Math.min(3, selecti.length))) > 3 && (nativemoduleA.size << (Math.min(1, selecti.length))) > 3) {
         let expiredf = String.fromCharCode(100, 114, 111, 112, 112, 101, 114, 95, 117, 95, 56, 52, 0);
         let fieldy = String.fromCharCode(101, 108, 101, 109, 115, 95, 98, 95, 57, 48, 0);
         let relatedO = String.fromCharCode(103, 95, 49, 48, 48, 95, 105, 110, 105, 116, 118, 0);
         selecti = [1 & nativemoduleA.size];
         expiredf += `${fieldy.length}`;
         fieldy += "3";
         relatedO += "2";
         break;
      }
      let halfE = String.fromCharCode(102, 95, 57, 95, 107, 97, 105, 115, 101, 114, 0);
      selecti.push(nativemoduleA.size + 1);
      halfE = `${halfE.length - 3}`;
      let libturbomodulejsijnis = 5.0;
      liveZ &= liveZ + parseInt(`${whatsappr}`);

      navigator.navigate('邀请');
   }, [])
   const userState = useSelector<yys_HejiCricket>('userReducer');
   const isVip = yys_RelatedTooltips.isVip(userState.user);
   if (!isVip)
      return (
         <LinearGradient
            colors={['#191F25', '#222528']}
            start={{ x: 0.12, y: 0.12 }}
            end={{ x: 0.9, y: 0.9 }}
            angle={269}
            style={{
               display: 'flex',
               flexDirection: 'row',
               justifyContent: 'space-between',
               alignItems: 'center',
               paddingHorizontal: 15,
               paddingVertical: 11,
            }}>
            <View style={{ flexDirection: 'row', gap: 8, alignItems: 'center' }}>
               <VipIcon width={24} height={24} />
               <View>
                  <Text style={{ color: '#FFE6A5', textShadowColor: 'rgba(0, 0, 0, 0.15)', fontWeight: '600', fontSize: 14, textShadowOffset: { height: 0, width: 4 }, textShadowRadius: 4 }}>邀请好友获得VIP奖励，享更多权益</Text>
                  {/* <Text style={{color: '#FFF', textShadowColor: 'rgba(0, 0, 0, 0.15)', fontWeight: '600', fontSize: 12, textShadowOffset: {height:0, width: 4}, textShadowRadius: 4 }}>$18.88/VIP会员180天</Text> */}
               </View>
            </View>
            {yys_RelatedTooltips.isLogin(userState.user) &&
               <TouchableOpacity
                  onPress={handleOnPress}
               >
                  <LinearGradient
                     colors={['#FAC33D', '#ECA700']}
                     start={{ x: 0.05, y: 0.05 }}
                     end={{ x: 1, y: 1 }}
                     angle={126}
                     style={{
                        paddingHorizontal: 10,
                        paddingVertical: 6,
                        borderRadius: 100,
                        flexDirection: 'row',
                        justifyContent: 'flex-start'

                     }}
                  >
                     <Text
                        style={{
                           fontWeight: '600',
                           fontSize: 12,
                           color: '#1D2023',
                           paddingRight: 4
                        }}
                     >
                        立刻邀请
                     </Text>
                     <MoreArrow color="#1D2023" width={8} />
                  </LinearGradient>
               </TouchableOpacity>
            }
         </LinearGradient>
      )
}

export default VipRegisterBar; 