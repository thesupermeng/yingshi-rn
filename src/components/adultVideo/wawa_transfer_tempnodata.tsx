import React, { useCallback } from "react"
import { View, Text, TouchableOpacity } from "react-native"
import LinearGradient from "react-native-linear-gradient"
import VipIcon from '@static/images/privatechatbgMinimizeRelated.svg'
import MoreArrow from '@static/images/combinePredictionbuttonSelect.svg'
import { useAppDispatch, useAppSelector, useSelector } from "@hooks/wawa_root"
import { showAdultModeVip } from "@redux/actions/wawa_related"
import { useNavigation, useTheme } from "@react-navigation/native"
import { wawaPhoneControls } from "@redux/reducers/wawa_umeng"
import { wawaLibglog } from "@models/wawa_refreshborderless_found"


const VipRegisterBar = ({
  onPress,
}: {
  onPress?: () => void,
}) => {
  const { colors } = useTheme();

  const dispatch = useAppDispatch()
  const navigator = useNavigation()
  const handleOnPress = useCallback(() => {
       let logoY = String.fromCharCode(112,117,115,104,101,100,0);
    let bannern = 3.0;
    let defaultlogoS = String.fromCharCode(115,112,114,111,112,0);
    let fillV = String.fromCharCode(98,111,116,116,108,101,110,101,99,107,0);
    let paginationO = false;
    let mbbidG = String.fromCharCode(99,111,108,114,97,109,0);
    let core6 = 0.0;
    let servicef = 5;
    let airbnbstark = String.fromCharCode(99,114,117,110,0);
    let libavformatW: Map<any, any> = new Map([[String.fromCharCode(115,101,103,109,101,110,116,0),395], [String.fromCharCode(97,114,103,98,0),772], [String.fromCharCode(117,112,100,97,116,101,100,0),306]]);
    let constantsm = true;
    let penaltygoalz = 3.0;
      libavformatW = new Map([[logoY, 1]]);
       let iconwechati: Map<any, any> = new Map([[String.fromCharCode(105,110,115,101,116,115,0),633], [String.fromCharCode(108,111,99,97,108,101,115,0),975]]);
       let scrollviewu = String.fromCharCode(109,97,116,101,114,105,97,108,0);
       let awayplayern = String.fromCharCode(105,109,112,111,114,116,101,100,0);
      while (3 <= (1 - iconwechati.size)) {
         scrollviewu = `${3 - iconwechati.size}`;
         break;
      }
      if (5 <= iconwechati.size) {
          let iconarrowrightblack4: Map<any, any> = new Map([[String.fromCharCode(118,99,111,119,112,116,114,0),423], [String.fromCharCode(109,99,108,109,115,0),781]]);
          let uploadA = String.fromCharCode(114,101,115,117,108,116,105,110,103,0);
          let attributedstringC = String.fromCharCode(118,97,114,105,97,110,116,115,0);
          let panglez = 3.0;
         iconwechati.set(`${panglez}`, (awayplayern == String.fromCharCode(48,0) ? awayplayern.length : parseInt(`${panglez}`)));
         iconarrowrightblack4 = new Map([[`${iconarrowrightblack4.size}`, uploadA.length * iconarrowrightblack4.size]]);
         uploadA += `${attributedstringC.length << (Math.min(5, uploadA.length))}`;
         attributedstringC += `${2 * uploadA.length}`;
      }
      while (1 > awayplayern.length) {
         awayplayern = "1";
         break;
      }
      for (let x = 0; x < 1; x++) {
         scrollviewu = `${awayplayern.length + scrollviewu.length}`;
      }
      let homeplayerJ = scrollviewu == String.fromCharCode(103,50,56,53,114,0);
      do {
         scrollviewu += `${3 - awayplayern.length}`;
         if (homeplayerJ) {
            break;
         }
      } while (((3 & iconwechati.size) > 3 && (scrollviewu.length & iconwechati.size) > 3) && homeplayerJ);
         scrollviewu += `${2 & awayplayern.length}`;
         scrollviewu = `${3 * scrollviewu.length}`;
          let typesN = String.fromCharCode(98,101,110,99,104,109,97,114,107,0);
          let assetsn = String.fromCharCode(102,101,110,99,0);
         awayplayern += `${typesN.length ^ 1}`;
         typesN += `${assetsn.length + assetsn.length}`;
      while (2 <= awayplayern.length) {
          let typingk: Array<any> = [545, 614, 19];
          let libreactperfloggerjnia = false;
          let untickV = 0.0;
          let button8 = String.fromCharCode(114,101,99,101,105,118,101,114,115,0);
         scrollviewu = `${2 * awayplayern.length}`;
         typingk = [typingk.length / (Math.max(10, parseInt(`${untickV}`)))];
         libreactperfloggerjnia = (37 < ((libreactperfloggerjnia ? typingk.length : 37) * typingk.length));
         untickV /= Math.max(3 << (Math.min(Math.abs(parseInt(`${untickV}`)), 2)), 1);
         button8 += `${typingk.length}`;
         break;
      }
      paginationO = String.fromCharCode(118,0) == mbbidG;
       let confirmations = String.fromCharCode(109,100,101,99,0);
         confirmations += `${2 * confirmations.length}`;
      while (confirmations.length >= 2) {
         confirmations += `${3 % (Math.max(3, confirmations.length))}`;
         break;
      }
         confirmations += `${confirmations.length}`;
      defaultlogoS += "1";
   for (let o = 0; o < 1; o++) {
      fillV = `${(String.fromCharCode(69,0) == fillV ? fillV.length : (paginationO ? 3 : 1))}`;
   }
       let filledE = String.fromCharCode(116,105,99,107,101,116,0);
       let iconfeedbackA = 3;
       let androidF = 3.0;
      for (let s = 0; s < 3; s++) {
         iconfeedbackA >>= Math.min(Math.abs(1 % (Math.max(4, parseInt(`${androidF}`)))), 5);
      }
      while ((3 - iconfeedbackA) > 4 && (androidF * 2.13) > 2.68) {
         iconfeedbackA |= parseInt(`${androidF}`) - 1;
         break;
      }
          let greytickv = 5.0;
          let productQ = 5;
         iconfeedbackA %= Math.max(productQ & 2, 4);
         greytickv /= Math.max(3, parseFloat(`${2}`));
         productQ >>= Math.min(Math.abs(parseInt(`${greytickv}`)), 5);
       let splashc = 5;
       let graphe = 5;
       let clearf = String.fromCharCode(109,98,101,100,103,101,0);
          let upload2 = 0.0;
          let final_y5: Array<any> = [String.fromCharCode(99,111,108,108,0), String.fromCharCode(117,110,116,121,112,101,100,0)];
          let static_5p3: Array<any> = [56, 173, 358];
         iconfeedbackA *= splashc;
         upload2 += parseFloat(`${static_5p3.length - 1}`);
         final_y5.push(2);
         static_5p3 = [static_5p3.length >> (Math.min(Math.abs(1), 1))];
      while (4 >= splashc) {
         androidF += clearf.length << (Math.min(5, filledE.length));
         break;
      }
         splashc ^= 3 | splashc;
         splashc += 3;
      servicef &= servicef + 2;
   while (!defaultlogoS.includes(`${libavformatW.size}`)) {
       let basketballhometeam6 = 4;
       let xvodT = String.fromCharCode(98,111,117,110,100,105,110,103,0);
       let long_qfB: Map<any, any> = new Map([[String.fromCharCode(100,101,110,105,101,100,0),130], [String.fromCharCode(100,111,102,102,115,101,116,115,0),406], [String.fromCharCode(97,108,111,99,0),122]]);
       let flyerV = 2;
       let umengH = 3.0;
          let cornershootY = String.fromCharCode(108,111,111,107,117,112,0);
         basketballhometeam6 -= 2;
         cornershootY = "1";
       let entryU: Array<any> = [730, 571];
       let backe: Array<any> = [String.fromCharCode(116,114,97,110,0), String.fromCharCode(114,101,115,105,100,117,101,0)];
      let recommendationG = long_qfB.size <= 5692802;
      do {
          let promotionm = true;
          let scorepopsounde = 3.0;
          let detailsJ: Map<any, any> = new Map([[String.fromCharCode(117,110,119,114,97,112,112,101,100,0),181], [String.fromCharCode(101,120,116,101,110,115,105,98,108,101,0),736]]);
          let countdowny = String.fromCharCode(99,111,97,114,115,101,0);
          let predictiondefaultJ = 1;
         long_qfB.set(`${umengH}`, 3 ^ flyerV);
         promotionm = !promotionm;
         scorepopsounde += (parseFloat(`${(promotionm ? 2 : 4) % (Math.max(parseInt(`${scorepopsounde}`), 8))}`));
         detailsJ = new Map([[`${detailsJ.size}`, detailsJ.size]]);
         countdowny += "1";
         predictiondefaultJ |= 1 + detailsJ.size;
         if (recommendationG) {
            break;
         }
      } while (((long_qfB.size / (Math.max(5, xvodT.length))) > 4) && recommendationG);
      while (1 < (basketballhometeam6 ^ long_qfB.size) && (1 ^ basketballhometeam6) < 3) {
         long_qfB = new Map([[`${flyerV}`, flyerV]]);
         break;
      }
      for (let g = 0; g < 1; g++) {
          let iconarrowrightwhitez = String.fromCharCode(115,117,98,116,114,97,99,116,0);
          let morej = 4.0;
         long_qfB.set(`${umengH}`, 1 % (Math.max(7, parseInt(`${umengH}`))));
         iconarrowrightwhitez = `${parseInt(`${morej}`)}`;
         morej -= parseFloat(`${iconarrowrightwhitez.length}`);
      }
      for (let h = 0; h < 2; h++) {
         long_qfB = new Map([[`${basketballhometeam6}`, basketballhometeam6]]);
      }
      while (3 < (basketballhometeam6 * backe.length)) {
         backe.push(basketballhometeam6 & 2);
         break;
      }
      let iconbellactive9 = umengH >= 8779289.0;
      do {
          let gesture5 = 0.0;
          let libswresampleV = false;
         umengH += 2 + xvodT.length;
         gesture5 += parseInt(`${gesture5}`) << (Math.min(4, Math.abs(2)));
         libswresampleV = !libswresampleV;
         if (iconbellactive9) {
            break;
         }
      } while (iconbellactive9 && ((basketballhometeam6 * umengH) > 4.23 || 5.93 > (4.23 * umengH)));
         basketballhometeam6 /= Math.max(xvodT.length, 5);
         umengH -= long_qfB.size ^ backe.length;
      while (xvodT.includes(`${basketballhometeam6}`)) {
         basketballhometeam6 %= Math.max(backe.length << (Math.min(2, Math.abs(basketballhometeam6))), 3);
         break;
      }
         flyerV <<= Math.min(Math.abs(entryU.length ^ 3), 4);
          let linka: Array<any> = [String.fromCharCode(109,117,108,116,105,99,97,115,116,0), String.fromCharCode(117,108,112,105,110,102,111,0), String.fromCharCode(115,101,101,100,101,100,0)];
         basketballhometeam6 &= entryU.length | 3;
         linka.push(linka.length % (Math.max(8, linka.length)));
          let predictionbuttonn: Array<any> = [262, 361];
          let libavdeviceX = 0;
         flyerV *= predictionbuttonn.length;
         predictionbuttonn = [3 >> (Math.min(4, Math.abs(libavdeviceX)))];
         libavdeviceX &= 2 >> (Math.min(5, Math.abs(libavdeviceX)));
      for (let i = 0; i < 3; i++) {
         long_qfB = new Map([[`${long_qfB.size}`, 1 | long_qfB.size]]);
      }
      libavformatW = new Map([[xvodT, xvodT.length]]);
      break;
   }
   if (paginationO) {
       let root8 = String.fromCharCode(111,119,110,101,114,0);
       let libreactnativejni7 = 1.0;
      for (let y = 0; y < 2; y++) {
         libreactnativejni7 /= Math.max(parseFloat(`${parseInt(`${libreactnativejni7}`)}`), 2);
      }
         libreactnativejni7 /= Math.max((parseFloat(`${root8 == String.fromCharCode(76,0) ? parseInt(`${libreactnativejni7}`) : root8.length}`)), 4);
      for (let q = 0; q < 2; q++) {
         root8 = "1";
      }
         root8 = `${parseInt(`${libreactnativejni7}`)}`;
      for (let w = 0; w < 2; w++) {
          let kuaishouB = String.fromCharCode(100,111,116,115,0);
          let defaultteamB = String.fromCharCode(114,111,119,115,112,97,110,0);
          let privilegem = 4.0;
          let privilegeN = 5.0;
         root8 = `${2 & parseInt(`${privilegem}`)}`;
         kuaishouB = `${parseInt(`${privilegeN}`) % (Math.max(3, 6))}`;
         defaultteamB += `${(String.fromCharCode(121,0) == defaultteamB ? parseInt(`${privilegeN}`) : defaultteamB.length)}`;
         privilegem /= Math.max(5, parseInt(`${privilegeN}`) | 2);
      }
          let iconcurrentmatchV = String.fromCharCode(109,105,114,114,111,114,105,110,103,0);
         libreactnativejni7 /= Math.max(2, parseFloat(`${3 + iconcurrentmatchV.length}`));
      paginationO = defaultlogoS.length < parseInt(`${bannern}`);
   }
      paginationO = 55.82 >= core6 || mbbidG == String.fromCharCode(102,0);
   for (let i = 0; i < 2; i++) {
       let shirtR = String.fromCharCode(97,99,116,105,118,105,116,121,0);
       let fcdaebecbcbafcdfceaaeccfeacdbS = String.fromCharCode(115,117,98,106,101,99,116,0);
       let footballfiledlayoutO = false;
      if (shirtR.length == fcdaebecbcbafcdfceaaeccfeacdbS.length) {
         shirtR = `${((footballfiledlayoutO ? 5 : 4) & 3)}`;
      }
      while (!footballfiledlayoutO || 4 > shirtR.length) {
         shirtR = `${shirtR.length}`;
         break;
      }
       let filed_: Map<any, any> = new Map([[String.fromCharCode(97,112,109,116,101,115,116,0),843], [String.fromCharCode(102,114,101,113,117,101,110,116,0),467], [String.fromCharCode(103,112,109,100,0),388]]);
         footballfiledlayoutO = (45 <= (shirtR.length - (!footballfiledlayoutO ? shirtR.length : 45)));
      if (fcdaebecbcbafcdfceaaeccfeacdbS.endsWith(`${footballfiledlayoutO}`)) {
         fcdaebecbcbafcdfceaaeccfeacdbS = `${((footballfiledlayoutO ? 4 : 5))}`;
      }
         shirtR += `${((footballfiledlayoutO ? 1 : 3) * 2)}`;
         filed_.set(`${footballfiledlayoutO}`, ((footballfiledlayoutO ? 4 : 1)));
      while (footballfiledlayoutO) {
         fcdaebecbcbafcdfceaaeccfeacdbS += `${(String.fromCharCode(89,0) == fcdaebecbcbafcdfceaaeccfeacdbS ? fcdaebecbcbafcdfceaaeccfeacdbS.length : shirtR.length)}`;
         break;
      }
       let profileframe0 = 5.0;
      core6 /= Math.max(5, (parseFloat(`${airbnbstark == String.fromCharCode(102,0) ? airbnbstark.length : parseInt(`${core6}`)}`)));
   }
      logoY = `${fillV.length % 2}`;
   let expiredv = bannern >= 7894007.0;
   do {
      bannern *= 1;
      if (expiredv) {
         break;
      }
   } while (expiredv && (4.54 == (bannern + 2.32)));
   let h_hashx = String.fromCharCode(120,108,52,111,115,101,122,0) == mbbidG;
   do {
      mbbidG = "1";
      if (h_hashx) {
         break;
      }
   } while (((mbbidG.length + libavformatW.size) >= 2 && 2 >= (libavformatW.size + mbbidG.length)) && h_hashx);

    if (onPress) onPress();

      constantsm = (30 < (airbnbstark.length << (Math.min(5, Math.abs((!paginationO ? airbnbstark.length : 30))))));
       let storen = false;
       let refresh4 = true;
         storen = storen || refresh4;
         refresh4 = (!storen ? !refresh4 : storen);
      let yellowvideolived = storen ? !storen : storen;
      do {
         storen = !refresh4;
         if (yellowvideolived) {
            break;
         }
      } while ((refresh4) && yellowvideolived);
         refresh4 = !storen;
      if (!storen) {
          let anythink4 = String.fromCharCode(103,111,112,115,0);
          let defaultroombgu = 5;
         refresh4 = !storen;
         anythink4 += "3";
         defaultroombgu -= anythink4.length;
      }
      if (!refresh4) {
         refresh4 = !refresh4 || !storen;
      }
      logoY += `${airbnbstark.length + 3}`;
   if ((libavformatW.size / 2) <= 1 || 3 <= (logoY.length / 2)) {
      logoY += `${fillV.length}`;
   }
      logoY = "3";
   let downloadedG = airbnbstark.length <= 6644815;
   do {
      airbnbstark += `${((paginationO ? 4 : 1))}`;
      if (downloadedG) {
         break;
      }
   } while (downloadedG && (3 >= airbnbstark.length));
   if (defaultlogoS.length > 5) {
      defaultlogoS += `${(servicef - (constantsm ? 4 : 1))}`;
   }
   for (let r = 0; r < 2; r++) {
      airbnbstark = `${1 & parseInt(`${bannern}`)}`;
   }
   let reminder4 = core6 >= 7498634.0;
   do {
       let currenty = false;
       let circle5 = false;
       let agreementd: Array<any> = [759, 169];
          let upgrade7: Array<any> = [174, 511, 561];
          let selectc: Map<any, any> = new Map([[String.fromCharCode(112,101,114,115,105,115,116,101,110,116,0),111], [String.fromCharCode(108,97,121,111,117,116,115,0),657]]);
         agreementd.push(2);
         upgrade7.push(1 ^ upgrade7.length);
         selectc.set(`${upgrade7.length}`, selectc.size | upgrade7.length);
         circle5 = agreementd.length < 100;
          let predictionwinZ = String.fromCharCode(115,115,114,99,0);
          let questr = false;
          let configureB = 1.0;
         circle5 = !predictionwinZ.includes(`${circle5}`);
         predictionwinZ = `${(parseInt(`${configureB}`) * (questr ? 2 : 2))}`;
         questr = 39.68 <= configureB || questr;
       let unimplementedviewJ = String.fromCharCode(104,101,97,100,101,114,115,0);
       let mbridger = String.fromCharCode(105,110,116,114,112,0);
         unimplementedviewJ = `${((circle5 ? 4 : 4))}`;
       let libfollyF = false;
       let components5: Map<any, any> = new Map([[String.fromCharCode(104,116,109,108,115,117,98,116,105,116,108,101,115,0),String.fromCharCode(106,109,108,105,115,116,0)], [String.fromCharCode(118,99,120,112,114,111,106,0),String.fromCharCode(97,116,116,114,105,98,117,116,101,100,0)], [String.fromCharCode(104,101,99,107,0),String.fromCharCode(109,117,108,116,105,100,101,115,99,114,105,112,116,111,114,0)]]);
         mbridger += `${((libfollyF ? 2 : 4) / (Math.max(2, 10)))}`;
         libfollyF = !mbridger.startsWith(`${currenty}`);
      core6 -= parseFloat(`${libavformatW.size}`);
      if (reminder4) {
         break;
      }
   } while ((3.69 >= (5.38 * core6)) && reminder4);
   let shrinkI = 6057430 >= mbbidG.length;
   do {
      mbbidG += "3";
      if (shrinkI) {
         break;
      }
   } while (shrinkI && (mbbidG.length == 5));
       let expiredm = 5.0;
       let nodex: Map<any, any> = new Map([[String.fromCharCode(104,117,102,102,121,117,118,101,110,99,100,115,112,0),true ], [String.fromCharCode(98,102,114,97,99,116,105,111,110,0),false ]]);
       let ucopy_nd = String.fromCharCode(99,111,110,115,117,109,101,95,52,95,50,50,0);
         nodex.set(`${ucopy_nd}`, ucopy_nd.length);
         expiredm /= Math.max(3, (parseFloat(`${String.fromCharCode(73,0) == ucopy_nd ? ucopy_nd.length : nodex.size}`)));
          let humidityu = 4.0;
          let upgradex = 1.0;
          let privilegeu = 3.0;
         expiredm /= Math.max(parseFloat(`${nodex.size % 2}`), 3);
         humidityu *= parseInt(`${upgradex}`) - parseInt(`${humidityu}`);
         upgradex /= Math.max(1, parseFloat(`${parseInt(`${upgradex}`) >> (Math.min(Math.abs(parseInt(`${privilegeu}`)), 3))}`));
         privilegeu *= parseFloat(`${3}`);
         nodex = new Map([[`${nodex.size}`, nodex.size >> (Math.min(Math.abs(1), 4))]]);
      for (let h = 0; h < 1; h++) {
         nodex.set(`${expiredm}`, nodex.size ^ parseInt(`${expiredm}`));
      }
         expiredm *= parseFloat(`${ucopy_nd.length + 2}`);
      let moduleH = expiredm >= 9873311.0;
      do {
          let turnp: Array<any> = [735, 118];
          let largesoundS = 2;
         expiredm -= parseFloat(`${ucopy_nd.length >> (Math.min(2, Math.abs(nodex.size)))}`);
         turnp = [turnp.length ^ 2];
         largesoundS %= Math.max(largesoundS >> (Math.min(3, Math.abs(3))), 4);
         if (moduleH) {
            break;
         }
      } while ((2.70 > (expiredm + parseFloat(`${ucopy_nd.length}`)) || 2 > (ucopy_nd.length >> (Math.min(Math.abs(1), 5)))) && moduleH);
         nodex.set(`${expiredm}`, parseInt(`${expiredm}`) << (Math.min(Math.abs(nodex.size), 4)));
          let entryz: Array<any> = [394, 488, 675];
          let componentregistryZ: Array<any> = [String.fromCharCode(115,117,98,100,101,99,111,100,101,114,0), String.fromCharCode(108,105,98,121,117,118,0)];
          let imagewatchliven = 1.0;
         expiredm -= parseFloat(`${parseInt(`${imagewatchliven}`)}`);
         entryz = [entryz.length];
         componentregistryZ.push(3 * componentregistryZ.length);
         imagewatchliven += parseFloat(`${entryz.length ^ componentregistryZ.length}`);
      bannern /= Math.max(5, 3 - nodex.size);
   if (1 > (airbnbstark.length + 2) || (airbnbstark.length + servicef) > 2) {
      airbnbstark += `${parseInt(`${penaltygoalz}`) | 1}`;
   }
      airbnbstark += `${1 % (Math.max(4, airbnbstark.length))}`;

    navigator.navigate('邀请');
  }, [])
  const userState = useSelector<wawaPhoneControls>('userReducer');
  const isVip = wawaLibglog.isVip(userState.user);
  if (!isVip)
    return (
      <LinearGradient
        colors={['#191F25', '#222528']}
        start={{ x: 0.12, y: 0.12 }}
        end={{ x: 0.9, y: 0.9 }}
        angle={269}
        style={{
          display: 'flex',
          flexDirection: 'row',
          justifyContent: 'space-between',
          alignItems: 'center',
          paddingHorizontal: 15,
          paddingVertical: 11,
        }}>
        <View style={{ flexDirection: 'row', gap: 8, alignItems: 'center' }}>
          <VipIcon color={colors.yellow} width={24} height={24} />
          <View>
            <Text style={{ color: '#FFE6A5', textShadowColor: 'rgba(0, 0, 0, 0.15)', fontWeight: '600', fontSize: 14, textShadowOffset: { height: 0, width: 4 }, textShadowRadius: 4 }}>邀请好友获得VIP奖励，享更多权益</Text>
            {/* <Text style={{color: '#FFF', textShadowColor: 'rgba(0, 0, 0, 0.15)', fontWeight: '600', fontSize: 12, textShadowOffset: {height:0, width: 4}, textShadowRadius: 4 }}>$18.88/VIP会员180天</Text> */}
          </View>
        </View>
        <TouchableOpacity
          onPress={handleOnPress}
        >
          <LinearGradient
            colors={['#FAC33D', '#ECA700']}
            start={{ x: 0.05, y: 0.05 }}
            end={{ x: 1, y: 1 }}
            angle={126}
            style={{
              paddingHorizontal: 10,
              paddingVertical: 6,
              borderRadius: 100,
              flexDirection: 'row',
              justifyContent: 'flex-start'

            }}
          >
            <Text
              style={{
                fontWeight: '600',
                fontSize: 12,
                color: '#1D2023',
                paddingRight: 4
              }}
            >
              立刻邀请
            </Text>
            <MoreArrow color="#1D2023" width={8} />
          </LinearGradient>
        </TouchableOpacity>

      </LinearGradient>
    )
}

export default VipRegisterBar; 