import React, { useCallback } from "react"
import { View, Text, TouchableOpacity } from "react-native"
import LinearGradient from "react-native-linear-gradient"
import VipIcon from '@static/images/playlistInjury.svg'
import MoreArrow from '@static/images/readBingMode.svg'
import { useAppDispatch, useAppSelector, useSelector } from "@hooks/kg_index"
import { showAdultModeVip } from "@redux/actions/a_switch"
import { useNavigation, useTheme } from "@react-navigation/native"
import { HDTGesturesList } from "@redux/reducers/f_gmail"
import { KWConstants } from "@models/kl_sheet"


const VipRegisterBar = ({
  onPress,
}: {
  onPress?: () => void,
}) => {
  const { colors } = useTheme();

  const dispatch = useAppDispatch()
  const navigator = useNavigation()
  const handleOnPress = useCallback(() => {
       let searchbarT = 2.0;
    let rewindl: Map<any, any> = new Map([[String.fromCharCode(99,95,52,57,95,105,110,116,101,114,102,97,99,101,115,0),false ], [String.fromCharCode(102,95,50,56,95,99,111,109,112,97,114,0),true ], [String.fromCharCode(99,117,114,114,114,101,110,116,95,120,95,53,57,0),false ]]);
    let saveA = String.fromCharCode(97,114,110,114,95,109,95,56,49,0);
    let hejin = 5;
    let jcopy_l9: Array<any> = [String.fromCharCode(101,95,55,52,95,115,116,97,114,116,101,100,0), String.fromCharCode(117,100,105,111,95,103,95,57,48,0)];
    let videoy = String.fromCharCode(99,95,57,55,95,100,111,119,110,119,97,114,100,0);
    let backG = String.fromCharCode(109,111,118,101,99,98,95,120,95,50,50,0);
    let taiwanA: Map<any, any> = new Map([[String.fromCharCode(97,99,108,114,95,50,95,53,54,0),817], [String.fromCharCode(102,114,97,109,101,115,95,53,95,52,50,0),723]]);
    let ying6: Map<any, any> = new Map([[String.fromCharCode(113,95,49,50,95,98,103,109,99,0),560], [String.fromCharCode(101,95,51,51,95,115,121,110,99,104,114,111,110,105,122,97,98,108,101,0),437], [String.fromCharCode(112,95,53,57,95,115,101,108,101,99,116,105,111,110,0),698]]);
      videoy += "1";
       let telegramm = String.fromCharCode(101,112,111,110,121,109,111,117,115,95,53,95,57,53,0);
      for (let o = 0; o < 3; o++) {
          let placeholderq = String.fromCharCode(116,95,53,52,95,113,115,99,97,108,101,113,108,111,103,0);
          let langS = false;
          let constantsK = 3.0;
          let fieldw: Map<any, any> = new Map([[String.fromCharCode(110,95,52,51,95,109,111,100,117,108,101,109,97,112,0),495], [String.fromCharCode(122,95,53,57,95,97,99,116,105,118,101,0),189]]);
          let bodanV = String.fromCharCode(119,95,52,57,95,105,109,112,111,114,116,0);
         telegramm += `${placeholderq.length << (Math.min(3, telegramm.length))}`;
         placeholderq = `${fieldw.size}`;
         langS = fieldw.size == 11 || String.fromCharCode(67,0) == bodanV;
         constantsK /= Math.max(5, (parseFloat(`${(langS ? 1 : 4) >> (Math.min(Math.abs(3), 1))}`)));
         bodanV = `${fieldw.size}`;
      }
         telegramm += `${telegramm.length | 2}`;
      for (let n = 0; n < 1; n++) {
          let orangef = 5;
          let const_zP: Array<any> = [742, 331, 439];
         telegramm = `${1 << (Math.min(3, const_zP.length))}`;
         orangef += orangef - 1;
         const_zP.push(2 - orangef);
      }
      saveA = `${backG.length}`;
       let arrowj = String.fromCharCode(107,95,49,52,95,112,114,111,112,101,114,116,105,101,115,0);
       let stations3 = String.fromCharCode(121,95,56,54,95,102,108,97,116,116,101,110,0);
       let rewindW = String.fromCharCode(103,108,121,112,104,115,95,118,95,54,50,0);
      while (rewindW != String.fromCharCode(116,0) && stations3 != String.fromCharCode(68,0)) {
         stations3 = `${stations3.length << (Math.min(Math.abs(1), 3))}`;
         break;
      }
      let text4 = 8210605 >= stations3.length;
      do {
          let projectN = 0.0;
          let fieldZ = 0.0;
          let ballz = true;
         stations3 += "1";
         projectN /= Math.max(5, (parseInt(`${fieldZ}`) | (ballz ? 3 : 1)));
         fieldZ /= Math.max(parseFloat(`${parseInt(`${fieldZ}`) << (Math.min(5, Math.abs(2)))}`), 3);
         ballz = fieldZ > projectN;
         if (text4) {
            break;
         }
      } while ((rewindW != stations3) && text4);
      while (stations3 == String.fromCharCode(120,0)) {
         arrowj += `${arrowj.length % (Math.max(stations3.length, 4))}`;
         break;
      }
      while (arrowj != String.fromCharCode(116,0)) {
          let macau5 = false;
          let download7 = true;
          let acceptedA: Map<any, any> = new Map([[String.fromCharCode(112,101,114,115,111,110,115,95,107,95,54,51,0),28], [String.fromCharCode(112,95,54,52,95,97,110,105,109,0),636]]);
         rewindW += `${acceptedA.size}`;
         macau5 = (!download7 ? macau5 : download7);
         acceptedA = new Map([[`${macau5}`, 3]]);
         break;
      }
         arrowj = `${(stations3 == String.fromCharCode(106,0) ? stations3.length : arrowj.length)}`;
         stations3 = `${stations3.length & 2}`;
         stations3 += `${2 ^ stations3.length}`;
      if (rewindW.includes(`${stations3.length}`)) {
         rewindW += `${stations3.length - 2}`;
      }
         stations3 += "1";
      backG += `${backG.length}`;
   let collectionq = backG == String.fromCharCode(54,106,104,0);
   do {
      backG += `${rewindl.size % (Math.max(1, 3))}`;
      if (collectionq) {
         break;
      }
   } while ((saveA != String.fromCharCode(110,0) || backG.length > 3) && collectionq);
   let bodand = backG == String.fromCharCode(52,105,48,114,107,107,99,103,0);
   do {
      backG = `${parseInt(`${searchbarT}`) + saveA.length}`;
      if (bodand) {
         break;
      }
   } while (bodand && (!backG.endsWith(`${saveA.length}`)));
   while ((2 & rewindl.size) > 5 && (4.74 + searchbarT) > 5.2) {
      rewindl = new Map([[backG, 1 ^ backG.length]]);
      break;
   }
      jcopy_l9 = [jcopy_l9.length << (Math.min(Math.abs(1), 5))];
   let private_djd = jcopy_l9.length >= 6718837;
   do {
      jcopy_l9.push(parseInt(`${searchbarT}`) * 2);
      if (private_djd) {
         break;
      }
   } while (private_djd && (jcopy_l9.length == videoy.length));

    if (onPress) onPress();

   for (let p = 0; p < 1; p++) {
       let gesturesW = false;
       let customz = 0.0;
       let progressu = String.fromCharCode(112,95,49,56,95,101,108,115,0);
       let sentry8: Array<any> = [595, 281, 973];
       let modal_: Map<any, any> = new Map([[String.fromCharCode(99,95,54,55,95,115,117,98,116,101,114,109,0),488], [String.fromCharCode(104,95,55,50,95,100,120,116,111,114,121,0),168]]);
      for (let r = 0; r < 2; r++) {
         customz += progressu.length ^ parseInt(`${customz}`);
      }
      let thailandj = gesturesW ? !gesturesW : gesturesW;
      do {
          let refresh0 = String.fromCharCode(117,95,49,53,95,102,105,97,116,0);
          let tumbnail5: Map<any, any> = new Map([[String.fromCharCode(120,95,52,56,95,109,117,108,116,105,112,108,121,0),406], [String.fromCharCode(120,100,97,105,95,111,95,56,53,0),441]]);
          let private_8J = String.fromCharCode(102,116,118,110,111,100,101,95,101,95,55,53,0);
         gesturesW = (progressu.length - sentry8.length) < 45;
         refresh0 += "2";
         tumbnail5.set(private_8J, tumbnail5.size);
         private_8J = `${private_8J.length % 2}`;
         if (thailandj) {
            break;
         }
      } while (thailandj && (!gesturesW));
      let topicF = modal_.size >= 8039979;
      do {
          let actionL = true;
          let headerb = String.fromCharCode(113,109,98,108,95,97,95,56,57,0);
          let rightq = 2.0;
         modal_.set(`${gesturesW}`, ((gesturesW ? 4 : 4) >> (Math.min(Math.abs(parseInt(`${rightq}`)), 5))));
         actionL = headerb.includes(`${actionL}`);
         headerb = `${((actionL ? 5 : 5) % (Math.max(7, headerb.length)))}`;
         rightq -= headerb.length & 2;
         if (topicF) {
            break;
         }
      } while (topicF && ((modal_.size + 1) < 4 && gesturesW));
      let pingM = customz <= 7942993.0;
      do {
         customz -= 3;
         if (pingM) {
            break;
         }
      } while (((modal_.size + 3) == 5 && 3.44 == (customz / 1.55)) && pingM);
      while (1 < (modal_.size | 1) && 3.0 < (3.49 - customz)) {
          let untickt = 2.0;
          let share9 = 4.0;
          let networkT = 0;
          let vignette9 = 4.0;
         modal_ = new Map([[`${sentry8.length}`, sentry8.length]]);
         untickt /= Math.max(parseInt(`${share9}`), 2);
         share9 -= parseFloat(`${networkT}`);
         networkT -= parseInt(`${vignette9}`);
         break;
      }
       let analyticG = String.fromCharCode(100,95,52,56,95,97,115,116,114,111,110,111,109,105,99,97,108,0);
      let castC = analyticG.length <= 6016244;
      do {
          let shootK = 5.0;
         analyticG = "2";
         shootK += parseFloat(`${1}`);
         if (castC) {
            break;
         }
      } while ((analyticG.length >= customz) && castC);
          let catalogl: Map<any, any> = new Map([[String.fromCharCode(97,116,97,98,108,101,115,95,49,95,52,56,0),0], [String.fromCharCode(116,95,53,48,95,101,110,116,101,114,0),740], [String.fromCharCode(104,97,115,104,116,97,98,108,101,122,95,49,95,55,0),593]]);
          let splashv = false;
          let indexj = String.fromCharCode(106,111,105,110,95,104,95,57,51,0);
         progressu += "1";
         catalogl = new Map([[`${catalogl.size}`, catalogl.size]]);
         splashv = 57 < catalogl.size;
         indexj = `${indexj.length}`;
      while (progressu.length == 4) {
         progressu += `${parseInt(`${customz}`)}`;
         break;
      }
          let logoutp: Array<any> = [612, 116, 902];
         modal_.set(`${customz}`, 1 << (Math.min(1, Math.abs(parseInt(`${customz}`)))));
         logoutp.push(1 << (Math.min(2, logoutp.length)));
      let trash9 = 8926844 <= modal_.size;
      do {
          let trophyS = String.fromCharCode(116,104,114,101,97,100,115,108,105,99,101,95,53,95,53,52,0);
          let refresh5 = 5.0;
          let xvod_ = 5.0;
          let applep = 5.0;
         modal_.set(`${customz}`, 3 << (Math.min(1, Math.abs(parseInt(`${customz}`)))));
         trophyS += `${3 << (Math.min(Math.abs(parseInt(`${xvod_}`)), 2))}`;
         refresh5 += 2 - trophyS.length;
         xvod_ += parseFloat(`${parseInt(`${applep}`)}`);
         if (trash9) {
            break;
         }
      } while (trash9 && ((sentry8.length >> (Math.min(Math.abs(modal_.size), 2))) < 3 && 1 < (3 >> (Math.min(4, Math.abs(modal_.size))))));
          let time_n3 = 5.0;
          let rankG = true;
         progressu += `${2 & analyticG.length}`;
         time_n3 += (parseFloat(`${(rankG ? 3 : 3) >> (Math.min(Math.abs(parseInt(`${time_n3}`)), 2))}`));
         rankG = !rankG || time_n3 < 79.12;
      while (gesturesW) {
         analyticG = `${parseInt(`${customz}`)}`;
         break;
      }
          let update_hA = 2.0;
         sentry8 = [progressu.length * modal_.size];
         update_hA -= parseInt(`${update_hA}`);
         customz -= (String.fromCharCode(119,0) == progressu ? parseInt(`${customz}`) : progressu.length);
      videoy += `${jcopy_l9.length}`;
   }
   while (1 < (3 + backG.length) && 3 < (jcopy_l9.length + backG.length)) {
       let action7 = false;
      for (let b = 0; b < 3; b++) {
          let gemfilej: Map<any, any> = new Map([[String.fromCharCode(121,95,49,51,95,104,113,97,100,115,112,0),String.fromCharCode(114,101,115,111,108,118,101,114,95,106,95,49,57,0)], [String.fromCharCode(100,111,99,105,100,95,99,95,50,56,0),String.fromCharCode(115,111,117,114,99,101,95,98,95,54,0)]]);
          let indicatorK: Map<any, any> = new Map([[String.fromCharCode(100,111,119,110,108,111,97,100,95,101,95,53,56,0),145], [String.fromCharCode(121,95,51,51,95,98,116,110,99,108,105,99,107,0),404]]);
          let iconh = 4.0;
         action7 = indicatorK.size == 53;
         gemfilej = new Map([[`${gemfilej.size}`, parseInt(`${iconh}`)]]);
         indicatorK = new Map([[`${gemfilej.size}`, 1]]);
         iconh -= gemfilej.size;
      }
         action7 = !action7 && action7;
         action7 = !action7;
      backG = `${3 | hejin}`;
      break;
   }
       let balli: Array<any> = [135, 426, 400];
       let mailI = 3.0;
          let playlistC = true;
          let private_uir = String.fromCharCode(118,109,115,108,95,119,95,50,54,0);
          let detailsa = 4.0;
         mailI += parseFloat(`${private_uir.length << (Math.min(1, Math.abs(parseInt(`${detailsa}`))))}`);
         playlistC = !playlistC;
         private_uir = "3";
         mailI += parseFloat(`${2}`);
         balli.push(1 + balli.length);
       let progressR = String.fromCharCode(100,111,120,121,103,101,110,95,102,95,54,55,0);
       let z_counth = String.fromCharCode(114,95,53,95,97,100,106,117,115,109,101,110,116,115,0);
       let typinga = String.fromCharCode(109,95,52,50,95,114,97,110,107,105,110,103,115,0);
       let activeJ = String.fromCharCode(113,95,53,52,95,100,99,98,108,111,99,107,0);
      rewindl.set(saveA, balli.length & 2);
      rewindl.set(`${searchbarT}`, videoy.length);
   for (let b = 0; b < 1; b++) {
       let leftQ = String.fromCharCode(104,95,50,55,95,105,110,116,101,114,112,0);
       let xvodS = 5.0;
       let cnewsV = 1;
       let sourcew = String.fromCharCode(117,110,114,101,99,111,103,95,117,95,51,50,0);
      while ((4 ^ cnewsV) >= 3 || (sourcew.length ^ 4) >= 3) {
         cnewsV /= Math.max(parseInt(`${xvodS}`), 5);
         break;
      }
          let firebaseH = 2.0;
          let dicel = String.fromCharCode(115,109,105,109,101,95,106,95,53,54,0);
         leftQ = "3";
         firebaseH -= parseInt(`${firebaseH}`);
         dicel += `${parseInt(`${firebaseH}`)}`;
      let champion8 = 4929014 >= cnewsV;
      do {
         cnewsV -= parseInt(`${xvodS}`);
         if (champion8) {
            break;
         }
      } while ((cnewsV == leftQ.length) && champion8);
         leftQ = "1";
          let charts = String.fromCharCode(105,95,52,53,95,115,104,111,114,116,99,117,116,115,0);
          let vignetteN = 5.0;
         cnewsV += leftQ.length << (Math.min(Math.abs(1), 1));
         charts = "2";
         vignetteN *= parseFloat(`${2 & charts.length}`);
         xvodS *= sourcew.length;
       let downloadd = 2.0;
         downloadd /= Math.max(4, sourcew.length);
      while ((3 << (Math.min(3, Math.abs(cnewsV)))) >= 1) {
          let placeholderA = String.fromCharCode(115,117,98,112,101,101,114,95,99,95,53,48,0);
         xvodS /= Math.max(sourcew.length, 2);
         placeholderA += `${1 | placeholderA.length}`;
         break;
      }
      for (let r = 0; r < 2; r++) {
         xvodS *= 2;
      }
      while (!leftQ.includes(`${cnewsV}`)) {
          let plashl = String.fromCharCode(117,110,119,114,97,112,95,51,95,54,54,0);
         cnewsV &= cnewsV * sourcew.length;
         plashl += `${2 & plashl.length}`;
         break;
      }
       let taiwanI = String.fromCharCode(99,111,108,117,109,110,115,95,113,95,50,55,0);
      videoy += `${(String.fromCharCode(69,0) == videoy ? videoy.length : sourcew.length)}`;
   }
      rewindl = new Map([[`${ying6.size}`, 3 / (Math.max(2, ying6.size))]]);
   let tumbnaili = 5472214 <= hejin;
   do {
      hejin >>= Math.min(5, Math.abs((saveA == String.fromCharCode(118,0) ? jcopy_l9.length : saveA.length)));
      if (tumbnaili) {
         break;
      }
   } while (tumbnaili && ((hejin * parseInt(`${searchbarT}`)) < 2 || 2 < (hejin * parseInt(`${searchbarT}`))));
      rewindl.set(videoy, jcopy_l9.length % (Math.max(5, videoy.length)));

    navigator.navigate('邀请');
  }, [])
  const userState = useSelector<HDTGesturesList>('userReducer');
  const isVip = KWConstants.isVip(userState.user);
  if (!isVip)
    return (
      <LinearGradient
        colors={['#191F25', '#222528']}
        start={{ x: 0.12, y: 0.12 }}
        end={{ x: 0.9, y: 0.9 }}
        angle={269}
        style={{
          display: 'flex',
          flexDirection: 'row',
          justifyContent: 'space-between',
          alignItems: 'center',
          paddingHorizontal: 15,
          paddingVertical: 11,
        }}>
        <View style={{ flexDirection: 'row', gap: 8, alignItems: 'center' }}>
          <VipIcon color={colors.yellow} width={24} height={24} />
          <View>
            <Text style={{ color: '#FFE6A5', textShadowColor: 'rgba(0, 0, 0, 0.15)', fontWeight: '600', fontSize: 14, textShadowOffset: { height: 0, width: 4 }, textShadowRadius: 4 }}>邀请好友获得VIP奖励，享更多权益</Text>
            {/* <Text style={{color: '#FFF', textShadowColor: 'rgba(0, 0, 0, 0.15)', fontWeight: '600', fontSize: 12, textShadowOffset: {height:0, width: 4}, textShadowRadius: 4 }}>$18.88/VIP会员180天</Text> */}
          </View>
        </View>
        <TouchableOpacity
          onPress={handleOnPress}
        >
          <LinearGradient
            colors={['#FAC33D', '#ECA700']}
            start={{ x: 0.05, y: 0.05 }}
            end={{ x: 1, y: 1 }}
            angle={126}
            style={{
              paddingHorizontal: 10,
              paddingVertical: 6,
              borderRadius: 100,
              flexDirection: 'row',
              justifyContent: 'flex-start'

            }}
          >
            <Text
              style={{
                fontWeight: '600',
                fontSize: 12,
                color: '#1D2023',
                paddingRight: 4
              }}
            >
              立刻邀请
            </Text>
            <MoreArrow color="#1D2023" width={8} />
          </LinearGradient>
        </TouchableOpacity>

      </LinearGradient>
    )
}

export default VipRegisterBar; 