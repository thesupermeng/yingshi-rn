import React, { useCallback } from "react"
import { View, Text, TouchableOpacity } from "react-native"
import LinearGradient from "react-native-linear-gradient"
import { VipIconSvg, MoreArrowSvg } from '@static';
import { useAppDispatch, useAppSelector, useSelector } from "@hooks"
import { showAdultModeVip } from "@redux"
import { useNavigation } from "@react-navigation/native"
import { yysIconplaySuggestion } from "@redux"
import { yysIconstar } from "@models"


const VipRegisterBar = ({
  onPress,
}: {
  onPress?: () => void,
}) => {
  const dispatch = useAppDispatch()
  const navigator = useNavigation()
  const handleOnPress = useCallback(() => {
       let greenarrowupn = String.fromCharCode(115,101,114,105,102,95,53,95,53,51,0);
    let flagh = 1;
    let iconbell9 = 5.0;
    let shootI = 0;
    let iconarrowrightorangeB = 5.0;
    let canvasa = String.fromCharCode(97,112,109,116,101,115,116,95,103,95,54,55,0);
    let recommendationO = 3;
    let dangerl = String.fromCharCode(104,95,51,52,95,116,101,115,115,0);
    let sourced = 5.0;
    let libavcodecV = String.fromCharCode(109,111,118,105,101,95,113,95,55,0);
    let mappingK = String.fromCharCode(117,95,52,55,95,109,97,107,101,114,112,109,0);
   if (flagh < canvasa.length) {
      canvasa = "1";
   }
   let textlayoutmanagerP = 6235086 <= greenarrowupn.length;
   do {
       let informationB = String.fromCharCode(105,110,116,101,114,118,97,108,95,48,95,51,49,0);
         informationB += "3";
       let iconbackwhiteL: Array<any> = [310, 429, 970];
         informationB = `${informationB.length}`;
      greenarrowupn = `${informationB.length * 2}`;
      if (textlayoutmanagerP) {
         break;
      }
   } while (textlayoutmanagerP && ((greenarrowupn.length % (Math.max(8, flagh))) <= 3 && (flagh % (Math.max(3, 9))) <= 5));
   while (1 <= (flagh - 4)) {
      greenarrowupn = `${parseInt(`${iconarrowrightorangeB}`) * 2}`;
      break;
   }
       let single5 = 4.0;
       let headerC = 4;
       let codegend = true;
      let bufferr = headerC >= 6986189;
      do {
         headerC *= parseInt(`${single5}`);
         if (bufferr) {
            break;
         }
      } while (bufferr && (3 < (headerC / 4)));
         single5 -= parseInt(`${single5}`) ^ 1;
          let iconwatchnowb: Array<any> = [735, 631, 770];
         headerC /= Math.max(4, headerC);
         iconwatchnowb.push(iconwatchnowb.length / (Math.max(iconwatchnowb.length, 5)));
      for (let r = 0; r < 2; r++) {
         codegend = (headerC * single5) <= 17;
      }
         codegend = !codegend && headerC >= 10;
          let disconnectedk = 2.0;
         codegend = 58 >= headerC || disconnectedk >= 52.42;
      let javaX = codegend ? !codegend : codegend;
      do {
         codegend = !codegend;
         if (javaX) {
            break;
         }
      } while (javaX && (headerC > 1));
      if (!codegend) {
          let androidp = String.fromCharCode(116,95,56,54,95,97,99,116,105,118,97,116,111,114,0);
          let black2 = 2;
          let coreT = String.fromCharCode(114,95,53,50,95,115,116,111,112,112,105,110,103,0);
          let libsentryj = String.fromCharCode(99,102,116,98,115,117,98,95,101,95,53,56,0);
         single5 *= (headerC * (codegend ? 1 : 5));
         androidp += `${(String.fromCharCode(89,0) == coreT ? libsentryj.length : coreT.length)}`;
         black2 <<= Math.min(Math.abs(black2), 2);
         libsentryj = "2";
      }
      for (let v = 0; v < 1; v++) {
         headerC <<= Math.min(Math.abs(headerC), 3);
      }
      canvasa += `${libavcodecV.length}`;
      libavcodecV += `${shootI}`;
      greenarrowupn += `${dangerl.length | 2}`;
      iconbell9 += parseFloat(`${3 % (Math.max(flagh, 2))}`);
   if ((greenarrowupn.length | flagh) > 5 || (greenarrowupn.length | 5) > 2) {
       let debugO = String.fromCharCode(105,110,116,101,103,114,105,116,121,95,56,95,55,52,0);
       let leftG = 0.0;
      while ((parseInt(`${leftG}`) - debugO.length) > 1 || 3 > (parseInt(`${leftG}`) - 1)) {
         leftG -= (String.fromCharCode(90,0) == debugO ? parseInt(`${leftG}`) : debugO.length);
         break;
      }
         debugO += `${3 & parseInt(`${leftG}`)}`;
      while ((leftG / (Math.max(4.64, 9))) >= 4.19) {
         debugO += `${debugO.length ^ 2}`;
         break;
      }
         debugO = `${debugO.length}`;
      while (5 >= (debugO.length % 1)) {
          let gifgoalM = String.fromCharCode(102,108,97,115,104,115,118,95,104,95,51,54,0);
          let projecty: Array<any> = [String.fromCharCode(110,95,57,52,95,99,103,117,0), String.fromCharCode(105,110,99,114,109,101,114,103,101,95,109,95,50,54,0), String.fromCharCode(97,95,54,95,114,101,115,116,0)];
          let controlsK = String.fromCharCode(104,99,115,99,97,108,101,95,114,95,52,51,0);
          let eventI = 1.0;
         debugO = `${gifgoalM.length % (Math.max(1, 4))}`;
         gifgoalM = `${3 << (Math.min(3, projecty.length))}`;
         projecty = [controlsK.length * 3];
         controlsK += `${parseInt(`${eventI}`)}`;
         eventI /= Math.max(1, parseFloat(`${projecty.length << (Math.min(Math.abs(1), 1))}`));
         break;
      }
         debugO = `${1 | debugO.length}`;
      flagh |= 2;
   }
   while (3 < (canvasa.length - 5)) {
      sourced *= parseFloat(`${3}`);
      break;
   }
       let predictionloss_ = 4.0;
         predictionloss_ /= Math.max(3, parseFloat(`${parseInt(`${predictionloss_}`) - 2}`));
      let projecth = predictionloss_ <= 6413572.0;
      do {
         predictionloss_ += parseFloat(`${2 & parseInt(`${predictionloss_}`)}`);
         if (projecth) {
            break;
         }
      } while (projecth && (predictionloss_ < predictionloss_));
      for (let y = 0; y < 3; y++) {
         predictionloss_ *= parseFloat(`${parseInt(`${predictionloss_}`)}`);
      }
      dangerl += `${3 & flagh}`;
      dangerl = `${dangerl.length}`;
   while (1 < (greenarrowupn.length - flagh) && (1 - flagh) < 5) {
      greenarrowupn += `${parseInt(`${iconbell9}`)}`;
      break;
   }

    if (onPress) onPress();

      flagh += parseInt(`${sourced}`);
   if (dangerl.endsWith(`${libavcodecV.length}`)) {
      dangerl += `${libavcodecV.length << (Math.min(2, Math.abs(recommendationO)))}`;
   }
   for (let p = 0; p < 1; p++) {
       let iconrefreshm: Array<any> = [912, 385, 91];
       let footballfiledlayoutk: Array<any> = [944, 986];
      for (let c = 0; c < 3; c++) {
          let greytickN = 2.0;
          let iconarrowrighta = 5.0;
          let customC = String.fromCharCode(109,95,54,55,95,114,101,99,101,105,112,116,115,0);
         iconrefreshm = [parseInt(`${greytickN}`)];
         greytickN *= parseFloat(`${customC.length}`);
         iconarrowrighta += 1 * parseInt(`${iconarrowrighta}`);
         customC += `${customC.length % 1}`;
      }
       let targete = String.fromCharCode(108,105,115,116,101,110,101,114,115,95,98,95,51,54,0);
       let elementsa = String.fromCharCode(113,108,111,103,115,95,119,95,52,51,0);
       let android0 = 5;
       let clearG = 3;
       let scoreZ = 3;
      while (targete != String.fromCharCode(116,0) || elementsa == String.fromCharCode(79,0)) {
          let downc = String.fromCharCode(97,108,112,105,110,101,95,101,95,54,50,0);
         targete += `${targete.length >> (Math.min(3, elementsa.length))}`;
         downc += "1";
         break;
      }
      while ((2 - footballfiledlayoutk.length) < 4 && (2 - footballfiledlayoutk.length) < 5) {
         footballfiledlayoutk.push(targete.length);
         break;
      }
      iconarrowrightorangeB /= Math.max(3, iconrefreshm.length);
   }
   while (4.65 <= (iconbell9 - sourced) && (iconbell9 - sourced) <= 4.65) {
      sourced *= parseFloat(`${libavcodecV.length}`);
      break;
   }
      libavcodecV += `${1 * flagh}`;
   let privacyd = iconarrowrightorangeB <= 8545126.0;
   do {
      iconarrowrightorangeB += 3 % (Math.max(7, canvasa.length));
      if (privacyd) {
         break;
      }
   } while (((iconarrowrightorangeB - 5.23) < 1.58 && (5.23 / (Math.max(10, iconarrowrightorangeB))) < 3.45) && privacyd);
      flagh <<= Math.min(Math.abs(parseInt(`${iconarrowrightorangeB}`)), 2);
   while (shootI > 1) {
       let codef = 0.0;
          let dragz: Array<any> = [421, 171, 504];
          let configb: Map<any, any> = new Map([[String.fromCharCode(115,105,109,112,108,101,115,105,103,110,97,108,95,52,95,51,53,0),793], [String.fromCharCode(97,99,99,101,115,115,105,110,103,95,106,95,52,55,0),832]]);
          let sharedA = String.fromCharCode(109,101,115,115,97,103,101,95,117,95,57,57,0);
         codef /= Math.max(parseFloat(`${dragz.length - 3}`), 3);
         dragz = [1];
         configb = new Map([[`${configb.size}`, configb.size]]);
         sharedA += `${sharedA.length << (Math.min(Math.abs(1), 5))}`;
      let championB = codef <= 7255997.0;
      do {
         codef += parseFloat(`${parseInt(`${codef}`)}`);
         if (championB) {
            break;
         }
      } while ((5.62 < (1 * codef)) && championB);
         codef -= parseFloat(`${parseInt(`${codef}`)}`);
      shootI |= (String.fromCharCode(107,0) == canvasa ? flagh : canvasa.length);
      break;
   }
       let iconuserx = 0.0;
         iconuserx /= Math.max(4, 3 << (Math.min(Math.abs(parseInt(`${iconuserx}`)), 1)));
         iconuserx *= parseInt(`${iconuserx}`);
      let nterstitial8 = 8558529.0 <= iconuserx;
      do {
         iconuserx *= 2 * parseInt(`${iconuserx}`);
         if (nterstitial8) {
            break;
         }
      } while (nterstitial8 && (iconuserx < 4.100));
      canvasa = `${parseInt(`${iconarrowrightorangeB}`)}`;
   if (iconarrowrightorangeB <= 3.75) {
      iconarrowrightorangeB *= 1 + parseInt(`${iconarrowrightorangeB}`);
   }
      iconarrowrightorangeB -= greenarrowupn.length & libavcodecV.length;
       let middle0 = 0;
       let imagemanagerb: Array<any> = [298, 933];
       let iconL = String.fromCharCode(111,95,51,53,95,112,114,111,109,111,0);
      let sharemodalZ = imagemanagerb.length <= 6687437;
      do {
         imagemanagerb = [imagemanagerb.length & middle0];
         if (sharemodalZ) {
            break;
         }
      } while ((middle0 > imagemanagerb.length) && sharemodalZ);
      while ((iconL.length * middle0) > 5) {
          let cornerQ = String.fromCharCode(102,95,57,49,95,112,105,99,109,101,109,115,101,116,0);
          let indicator5 = String.fromCharCode(99,95,53,50,95,105,102,97,109,115,103,0);
         middle0 %= Math.max(iconL.length << (Math.min(Math.abs(2), 4)), 2);
         cornerQ += `${indicator5.length}`;
         indicator5 = `${indicator5.length}`;
         break;
      }
         imagemanagerb.push(iconL.length);
      if (iconL.startsWith(`${middle0}`)) {
         iconL = "1";
      }
          let renderE: Map<any, any> = new Map([[String.fromCharCode(102,95,51,50,95,97,114,98,105,116,101,114,0),String.fromCharCode(117,95,49,48,48,95,116,97,117,0)], [String.fromCharCode(108,95,56,54,95,115,111,108,118,101,114,0),String.fromCharCode(119,104,101,110,95,52,95,57,0)], [String.fromCharCode(103,95,54,50,95,118,97,108,105,100,105,116,121,0),String.fromCharCode(120,95,55,51,95,99,111,109,112,108,101,116,101,0)]]);
          let defaultteamN = String.fromCharCode(97,117,116,111,100,101,116,101,99,116,105,111,110,95,100,95,55,50,0);
         iconL += `${renderE.size ^ 3}`;
         renderE = new Map([[defaultteamN, (String.fromCharCode(68,0) == defaultteamN ? defaultteamN.length : defaultteamN.length)]]);
      for (let v = 0; v < 3; v++) {
         iconL = `${iconL.length * 1}`;
      }
       let transferc: Array<any> = [773, 393, 344];
         middle0 |= middle0 - 1;
          let rewardvideoE = 0.0;
          let securityL = String.fromCharCode(119,95,50,53,95,104,101,108,112,101,114,0);
          let shareblacko = String.fromCharCode(121,101,97,114,95,110,95,53,51,0);
         iconL += `${middle0}`;
         rewardvideoE -= 2;
         securityL += "1";
         shareblacko = `${parseInt(`${rewardvideoE}`)}`;
      canvasa = `${imagemanagerb.length << (Math.min(Math.abs(1), 3))}`;

    navigator.navigate('邀请');
  }, [])
  const userState = useSelector<yysIconplaySuggestion>('userReducer');
  const isVip = yysIconstar.isVip(userState.user);
  if (!isVip)
    return (
      <LinearGradient
        colors={['#191F25', '#222528']}
        start={{ x: 0.12, y: 0.12 }}
        end={{ x: 0.9, y: 0.9 }}
        angle={269}
        style={{
          display: 'flex',
          flexDirection: 'row',
          justifyContent: 'space-between',
          alignItems: 'center',
          paddingHorizontal: 15,
          paddingVertical: 11,
        }}>
        <View style={{ flexDirection: 'row', gap: 8, alignItems: 'center' }}>
          <VipIconSvg width={24} height={24} />
          <View>
            <Text style={{ color: '#FFE6A5', textShadowColor: 'rgba(0, 0, 0, 0.15)', fontWeight: '600', fontSize: 14, textShadowOffset: { height: 0, width: 4 }, textShadowRadius: 4 }}>邀请好友获得VIP奖励，享更多权益</Text>
            {/* <Text style={{color: '#FFF', textShadowColor: 'rgba(0, 0, 0, 0.15)', fontWeight: '600', fontSize: 12, textShadowOffset: {height:0, width: 4}, textShadowRadius: 4 }}>$18.88/VIP会员180天</Text> */}
          </View>
        </View>
        <TouchableOpacity
          onPress={handleOnPress}
        >
          <LinearGradient
            colors={['#FAC33D', '#ECA700']}
            start={{ x: 0.05, y: 0.05 }}
            end={{ x: 1, y: 1 }}
            angle={126}
            style={{
              paddingHorizontal: 10,
              paddingVertical: 6,
              borderRadius: 100,
              flexDirection: 'row',
              justifyContent: 'flex-start'

            }}
          >
            <Text
              style={{
                fontWeight: '600',
                fontSize: 12,
                color: '#1D2023',
                paddingRight: 4
              }}
            >
              立刻邀请
            </Text>
            <MoreArrowSvg color="#1D2023" width={8} />
          </LinearGradient>
        </TouchableOpacity>
      </LinearGradient>
    )
}

export default VipRegisterBar; 