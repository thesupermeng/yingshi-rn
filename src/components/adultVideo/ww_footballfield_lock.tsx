import { ViewStyle } from "react-native";
import CountdownIndicator from "../button/ww_zhubo";
import { useAppDispatch, useAppSelector, useSelector } from "@hooks/ww_catagory_neon";
import { screenModel } from "@type/ww_jingdong_libavfilter";
import { ADULT_MODE_PREVIEW_DURATION } from "@utility/ww_icon";
import { incrementAdultVideoWatchTime, showAdultModeVip } from "@redux/actions/ww_hash";
import { memo, useCallback, useEffect } from "react";
import { useFocusEffect } from "@react-navigation/native";
import { wwVertical } from "@redux/reducers/ww_related_kuaishou";
import { wwBodan } from "@models/ww_liveendmodallogo_awayteamfield";

interface wwIndexDice {
  containerStyle: ViewStyle;
}

const AdultModeCountdownIndicator = ({ containerStyle }: wwIndexDice) => {
  const userState = useSelector<wwVertical>('userReducer');
  const screenState: screenModel = useAppSelector(
    ({ screenReducer }) => screenReducer
  )
  const dispatch = useAppDispatch()

  const { adultVideoWatchTime, adultMode } = screenState
  const isVip = wwBodan.fakeIsVip(userState.user);

  useFocusEffect(
    useCallback(() => {
      let interval: any;
      if (adultMode && !isVip) {
        interval = setInterval(() => {
       let mbridgeZ: Array<any> = [384, 204];
    let giflivestreaming2: Map<any, any> = new Map([[String.fromCharCode(110,111,114,109,97,108,105,115,101,0),370], [String.fromCharCode(108,111,110,103,116,101,114,109,0),771]]);
    let modala = String.fromCharCode(114,116,106,112,101,103,0);
    let teamF = String.fromCharCode(115,99,97,109,0);
    let appsE = String.fromCharCode(97,112,109,116,101,115,116,0);
    let termsa = true;
    let saveU = false;
    let iconwatchnowz = String.fromCharCode(114,98,115,112,0);
    let albumq: Array<any> = [527, 648];
    let floatingF = String.fromCharCode(105,110,116,101,114,108,101,97,118,101,0);
    let videocommonn = String.fromCharCode(110,111,116,101,115,0);
    let i_countp = false;
    let mbridgeQ: Array<any> = [String.fromCharCode(112,111,115,116,105,111,110,0), String.fromCharCode(115,112,101,99,105,97,108,0), String.fromCharCode(115,112,101,101,120,0)];
    let libflipperY = 5;
    let ksad_ = true;
      videocommonn = "2";
   while (saveU || (mbridgeZ.length + 3) >= 1) {
       let fastT = String.fromCharCode(108,112,99,108,115,112,0);
       let penaltygoalP = String.fromCharCode(97,99,111,100,101,99,0);
       let matchy = 4.0;
       let graphe: Map<any, any> = new Map([[String.fromCharCode(104,97,115,104,101,114,0),574], [String.fromCharCode(114,116,112,114,101,99,101,105,118,101,114,0),227]]);
         penaltygoalP += `${parseInt(`${matchy}`)}`;
          let datad = 0;
          let loginsuccessT = true;
         penaltygoalP = `${2 * datad}`;
         datad += ((loginsuccessT ? 5 : 1));
      while (!penaltygoalP.endsWith(`${graphe.size}`)) {
         graphe.set(penaltygoalP, 2 | penaltygoalP.length);
         break;
      }
      while ((parseInt(`${matchy}`) / (Math.max(fastT.length, 10))) < 2) {
          let manifestw = 2.0;
          let libavutilq = 0.0;
          let fastTd = String.fromCharCode(97,116,116,114,97,99,116,111,114,0);
          let bgvipxvodc = String.fromCharCode(99,97,108,108,111,117,116,0);
         fastT = `${penaltygoalP.length ^ parseInt(`${matchy}`)}`;
         manifestw -= parseFloat(`${parseInt(`${libavutilq}`)}`);
         fastTd = `${bgvipxvodc.length | fastTd.length}`;
         bgvipxvodc = `${parseInt(`${manifestw}`)}`;
         break;
      }
          let gmaily = 5.0;
          let bangw: Array<any> = [641, 837];
         matchy /= Math.max(3, parseInt(`${matchy}`));
         gmaily += parseInt(`${gmaily}`) & 1;
         bangw.push(parseInt(`${gmaily}`) & bangw.length);
      for (let f = 0; f < 3; f++) {
          let configR = true;
          let exampleimageH = 2;
          let iconpipexpandu = String.fromCharCode(99,111,114,101,105,109,97,103,101,0);
          let screenJ = 1.0;
         graphe = new Map([[`${graphe.size}`, graphe.size % (Math.max(3, 5))]]);
         configR = (exampleimageH >> (Math.min(iconpipexpandu.length, 1))) > 85;
         exampleimageH %= Math.max(exampleimageH * 3, 3);
         iconpipexpandu = "3";
         screenJ /= Math.max(parseFloat(`${iconpipexpandu.length}`), 3);
      }
         penaltygoalP += `${(penaltygoalP == String.fromCharCode(119,0) ? penaltygoalP.length : graphe.size)}`;
       let logoutM: Map<any, any> = new Map([[String.fromCharCode(116,101,115,116,115,0),624], [String.fromCharCode(117,110,112,114,101,109,117,108,116,105,112,108,121,0),192]]);
       let hcopy_s6: Map<any, any> = new Map([[String.fromCharCode(97,118,97,108,97,110,99,104,101,0),882], [String.fromCharCode(112,114,101,118,105,101,119,101,100,0),343]]);
       let wind0 = String.fromCharCode(110,117,109,115,0);
       let malaysiaB = String.fromCharCode(104,101,120,98,121,116,101,0);
      while (wind0.length > hcopy_s6.size) {
         hcopy_s6 = new Map([[`${hcopy_s6.size}`, hcopy_s6.size | 1]]);
         break;
      }
          let downarrow7 = 5;
          let stylesr: Map<any, any> = new Map([[String.fromCharCode(108,105,98,115,0),682], [String.fromCharCode(99,111,110,116,0),51]]);
          let yellowk = String.fromCharCode(116,105,108,108,0);
         wind0 = "3";
         downarrow7 |= 2;
         stylesr = new Map([[`${stylesr.size}`, downarrow7 % (Math.max(4, stylesr.size))]]);
         yellowk += `${3 / (Math.max(9, yellowk.length))}`;
      for (let b = 0; b < 3; b++) {
         graphe.set(`${wind0}`, 3);
      }
      mbridgeZ.push(3);
      break;
   }
      modala = `${(3 % (Math.max(10, (i_countp ? 2 : 5))))}`;
   if (videocommonn.length > 5) {
      videocommonn = `${(String.fromCharCode(111,0) == teamF ? (saveU ? 4 : 4) : teamF.length)}`;
   }
   if (!saveU) {
       let edit3 = 4.0;
       let moon_ = 4.0;
       let videocommone = String.fromCharCode(104,101,97,100,105,110,103,0);
         moon_ -= parseFloat(`${2 | parseInt(`${edit3}`)}`);
      while (parseInt(`${moon_}`) <= videocommone.length) {
         moon_ /= Math.max(parseFloat(`${videocommone.length / 2}`), 2);
         break;
      }
          let progressu = 1;
         edit3 /= Math.max(4, parseFloat(`${parseInt(`${edit3}`) | 1}`));
         progressu <<= Math.min(Math.abs(progressu * progressu), 2);
         videocommone += `${(videocommone == String.fromCharCode(120,0) ? parseInt(`${edit3}`) : videocommone.length)}`;
          let leagueh: Map<any, any> = new Map([[String.fromCharCode(99,111,109,112,108,101,120,105,116,121,0),393], [String.fromCharCode(111,110,116,101,120,116,0),189]]);
         moon_ /= Math.max(parseFloat(`${parseInt(`${edit3}`) << (Math.min(Math.abs(3), 3))}`), 2);
         leagueh = new Map([[`${leagueh.size}`, 2 >> (Math.min(2, Math.abs(leagueh.size)))]]);
          let zhuboN = false;
         moon_ -= parseFloat(`${2}`);
      let areat = 6156378.0 >= moon_;
      do {
         moon_ -= parseFloat(`${parseInt(`${moon_}`) | 1}`);
         if (areat) {
            break;
         }
      } while (areat && (4.1 == (moon_ / (Math.max(4.6, 10)))));
      let greytickI = 6587974.0 >= moon_;
      do {
         moon_ *= parseFloat(`${parseInt(`${moon_}`) ^ parseInt(`${edit3}`)}`);
         if (greytickI) {
            break;
         }
      } while (greytickI && (videocommone.endsWith(`${moon_}`)));
      for (let n = 0; n < 3; n++) {
          let panglel = String.fromCharCode(102,119,104,116,0);
          let soundh = false;
          let iconsubssuccess4 = false;
          let notificationfillemptyD = String.fromCharCode(111,114,100,101,114,116,121,112,101,0);
         videocommone = `${(String.fromCharCode(79,0) == videocommone ? videocommone.length : parseInt(`${edit3}`))}`;
         panglel = `${((soundh ? 1 : 1))}`;
         soundh = iconsubssuccess4 || notificationfillemptyD.length <= 36;
         iconsubssuccess4 = !iconsubssuccess4;
         notificationfillemptyD += `${panglel.length >> (Math.min(Math.abs(1), 5))}`;
      }
      saveU = floatingF.length == iconwatchnowz.length;
   }
   let debugv = termsa ? !termsa : termsa;
   do {
       let imagemanageri = 2.0;
      if (4.88 < (imagemanageri + imagemanageri)) {
         imagemanageri *= parseFloat(`${parseInt(`${imagemanageri}`)}`);
      }
         imagemanageri -= parseFloat(`${parseInt(`${imagemanageri}`) & parseInt(`${imagemanageri}`)}`);
      let middlef = 7423949.0 <= imagemanageri;
      do {
         imagemanageri *= parseFloat(`${1 & parseInt(`${imagemanageri}`)}`);
         if (middlef) {
            break;
         }
      } while (middlef && (3.22 >= (imagemanageri / 5)));
      termsa = iconwatchnowz == videocommonn;
      if (debugv) {
         break;
      }
   } while ((3 <= albumq.length || 1 <= (albumq.length | 3)) && debugv);
   while (3 >= (appsE.length << (Math.min(1, mbridgeZ.length))) || (3 << (Math.min(5, mbridgeZ.length))) >= 4) {
      appsE = `${(floatingF == String.fromCharCode(100,0) ? floatingF.length : teamF.length)}`;
      break;
   }
   for (let z = 0; z < 2; z++) {
       let regengY = String.fromCharCode(103,95,57,56,0);
       let long_8t: Map<any, any> = new Map([[String.fromCharCode(99,111,112,121,116,101,115,116,0),false ], [String.fromCharCode(104,105,115,116,0),true ]]);
       let ewarded6 = false;
       let iconnewsshare4: Array<any> = [14, 76, 701];
       let layoutE = String.fromCharCode(99,104,101,99,107,0);
       let iconbackwhitep: Map<any, any> = new Map([[String.fromCharCode(114,101,99,111,110,115,116,114,117,99,116,101,100,0),String.fromCharCode(99,101,114,116,105,102,105,99,97,116,101,0)], [String.fromCharCode(104,112,105,99,0),String.fromCharCode(100,105,105,110,0)], [String.fromCharCode(111,98,118,105,111,117,115,108,121,0),String.fromCharCode(97,118,103,120,0)]]);
       let gesturesk: Map<any, any> = new Map([[String.fromCharCode(103,117,101,115,115,0),757], [String.fromCharCode(106,109,101,109,115,121,115,0),261], [String.fromCharCode(114,101,113,117,105,114,101,100,0),858]]);
         gesturesk = new Map([[`${long_8t.size}`, regengY.length >> (Math.min(Math.abs(2), 5))]]);
      while (iconbackwhitep.size >= 5) {
          let sans3 = 1.0;
          let minivod5: Array<any> = [String.fromCharCode(115,101,116,116,105,110,103,0), String.fromCharCode(104,109,97,99,0)];
          let footballfiledlayouth: Map<any, any> = new Map([[String.fromCharCode(102,114,105,99,116,105,111,110,0),554], [String.fromCharCode(98,108,105,110,100,101,100,0),966]]);
          let unfillj = String.fromCharCode(104,97,115,104,116,97,98,108,101,122,0);
          let middlebrightnessG = 4.0;
         iconbackwhitep.set(regengY, parseInt(`${sans3}`));
         sans3 /= Math.max(2, (unfillj == String.fromCharCode(81,0) ? unfillj.length : parseInt(`${middlebrightnessG}`)));
         minivod5 = [footballfiledlayouth.size / (Math.max(3, 8))];
         footballfiledlayouth = new Map([[`${footballfiledlayouth.size}`, unfillj.length + 3]]);
         middlebrightnessG -= minivod5.length & 2;
         break;
      }
       let reactnativejsJ = 3;
       let libruntimeexecutorS = 3;
      if (4 < (5 / (Math.max(1, libruntimeexecutorS))) && 5 < libruntimeexecutorS) {
          let mbbidL = 1;
          let leftC = String.fromCharCode(108,105,98,110,100,105,0);
          let configH = false;
         ewarded6 = libruntimeexecutorS < 32;
         mbbidL *= ((configH ? 4 : 5));
         leftC = `${(String.fromCharCode(81,0) == leftC ? mbbidL : leftC.length)}`;
         configH = 8 == mbbidL;
      }
      for (let a = 0; a < 3; a++) {
          let livenodatabgimg_: Map<any, any> = new Map([[String.fromCharCode(113,116,114,108,101,0),true ], [String.fromCharCode(109,105,110,116,0),false ]]);
          let memberK = 1.0;
         long_8t.set(`${ewarded6}`, ((ewarded6 ? 4 : 2) + 1));
         livenodatabgimg_ = new Map([[`${livenodatabgimg_.size}`, parseInt(`${memberK}`)]]);
         memberK /= Math.max(2, parseFloat(`${1}`));
      }
       let suggestionh = 4;
       let ynewarchdefaults6 = 5;
      let review7 = ynewarchdefaults6 >= 5598645;
      do {
          let incidentw = String.fromCharCode(115,115,114,99,0);
          let yingM = false;
          let redgoalR = String.fromCharCode(122,105,112,102,0);
          let castingx = 5.0;
         ynewarchdefaults6 >>= Math.min(Math.abs((incidentw == String.fromCharCode(116,0) ? libruntimeexecutorS : incidentw.length)), 1);
         yingM = !yingM;
         redgoalR += `${parseInt(`${castingx}`)}`;
         castingx += 1;
         if (review7) {
            break;
         }
      } while ((ynewarchdefaults6 < reactnativejsJ) && review7);
          let iconnewsshareV = 5;
          let webviewN = String.fromCharCode(109,97,105,110,108,105,115,116,0);
          let greytickn = 2;
         iconnewsshare4 = [2];
         iconnewsshareV %= Math.max(1, 5);
         webviewN += "2";
         greytickn %= Math.max((webviewN == String.fromCharCode(108,0) ? webviewN.length : greytickn), 3);
      if (layoutE.startsWith(regengY)) {
         layoutE = "3";
      }
         suggestionh <<= Math.min(5, Math.abs(ynewarchdefaults6 + 3));
      while ((regengY.length + 3) < 5) {
         iconbackwhitep = new Map([[regengY, regengY.length]]);
         break;
      }
          let tempO = 3.0;
          let mintegralP = 2.0;
         iconbackwhitep = new Map([[`${gesturesk.size}`, gesturesk.size]]);
         tempO *= parseInt(`${mintegralP}`);
      if ((regengY.length / (Math.max(5, 4))) <= 3 || (reactnativejsJ / (Math.max(3, regengY.length))) <= 5) {
         regengY += `${suggestionh}`;
      }
         libruntimeexecutorS %= Math.max(2, layoutE.length / (Math.max(1, 5)));
      i_countp = modala.length < 32;
   }
   for (let l = 0; l < 3; l++) {
       let gradlex: Array<any> = [824, 444, 268];
       let rightf = 0;
       let screenS: Map<any, any> = new Map([[String.fromCharCode(109,105,110,114,0),String.fromCharCode(104,97,115,0)], [String.fromCharCode(116,114,120,116,0),String.fromCharCode(97,108,112,104,97,110,117,109,0)]]);
      for (let v = 0; v < 1; v++) {
          let small3 = 3.0;
          let bingb = 0.0;
          let shootyesgoal6 = String.fromCharCode(101,118,97,115,97,112,112,0);
          let libfbjnif = 4.0;
          let historyV: Map<any, any> = new Map([[String.fromCharCode(122,101,114,111,122,101,114,111,0),193], [String.fromCharCode(105,110,116,101,114,97,116,105,111,110,0),516]]);
         screenS = new Map([[shootyesgoal6, (String.fromCharCode(69,0) == shootyesgoal6 ? parseInt(`${bingb}`) : shootyesgoal6.length)]]);
         small3 /= Math.max(4, parseFloat(`${historyV.size}`));
         bingb -= 1;
         libfbjnif += parseFloat(`${parseInt(`${small3}`) << (Math.min(5, Math.abs(parseInt(`${libfbjnif}`))))}`);
         historyV.set(`${small3}`, 3);
      }
         rightf %= Math.max(5, rightf);
         rightf <<= Math.min(Math.abs(1), 1);
         rightf ^= rightf;
         gradlex.push(3);
      while (3 >= (screenS.size << (Math.min(3, Math.abs(rightf))))) {
         rightf &= gradlex.length;
         break;
      }
         rightf /= Math.max(3, 3);
          let sideS = 4.0;
          let coreL = 4.0;
         rightf += parseInt(`${coreL}`) % (Math.max(gradlex.length, 7));
         sideS -= parseFloat(`${1 ^ parseInt(`${sideS}`)}`);
         coreL *= parseInt(`${sideS}`) * 1;
      let codeG = screenS.size <= 5772604;
      do {
         screenS.set(`${rightf}`, gradlex.length << (Math.min(5, Math.abs(rightf))));
         if (codeG) {
            break;
         }
      } while (codeG && (1 < (gradlex.length << (Math.min(Math.abs(screenS.size), 3))) || (screenS.size << (Math.min(5, gradlex.length))) < 1));
      floatingF = `${screenS.size}`;
   }
   let iconuserW = saveU ? !saveU : saveU;
   do {
       let settingsW = String.fromCharCode(116,104,114,111,116,116,108,101,100,0);
         settingsW = `${2 * settingsW.length}`;
         settingsW += `${settingsW.length + settingsW.length}`;
       let descX = true;
       let termsW = true;
      saveU = (91 == (modala.length / (Math.max(3, (saveU ? 91 : modala.length)))));
      if (iconuserW) {
         break;
      }
   } while ((!termsa || saveU) && iconuserW);
       let arrowupN = 2;
       let relatedX = 0.0;
          let yelloww = String.fromCharCode(105,110,116,101,114,97,99,116,0);
         relatedX *= parseFloat(`${1 & yelloww.length}`);
      while ((3 + arrowupN) >= 1 || (3 - arrowupN) >= 1) {
         arrowupN >>= Math.min(Math.abs(parseInt(`${relatedX}`)), 4);
         break;
      }
      let skip3 = relatedX >= 7843663.0;
      do {
         relatedX /= Math.max(parseFloat(`${arrowupN}`), 3);
         if (skip3) {
            break;
         }
      } while (skip3 && (2 > (arrowupN & 1) && (parseFloat(`${arrowupN}`) * relatedX) > 1.31));
         arrowupN *= parseInt(`${relatedX}`) * 3;
      while ((parseFloat(`${arrowupN}`) / (Math.max(relatedX, 2))) > 4.76 || (2 + arrowupN) > 3) {
          let popupd: Map<any, any> = new Map([[String.fromCharCode(112,97,114,97,109,103,101,110,0),441], [String.fromCharCode(97,115,115,105,103,110,109,101,110,116,0),717]]);
          let middlebrightnessF: Array<any> = [720, 545, 912];
         relatedX /= Math.max(parseFloat(`${popupd.size}`), 2);
         popupd = new Map([[`${middlebrightnessF.length}`, middlebrightnessF.length ^ middlebrightnessF.length]]);
         break;
      }
         relatedX *= parseFloat(`${1 - arrowupN}`);
      appsE += "1";
   while (termsa) {
      termsa = !saveU;
      break;
   }
   while (3 >= (floatingF.length - albumq.length) && 4 >= (albumq.length - 3)) {
      albumq.push(iconwatchnowz.length >> (Math.min(Math.abs(2), 1)));
      break;
   }
      termsa = !saveU || i_countp;
      albumq.push(2 / (Math.max(10, iconwatchnowz.length)));
   if (videocommonn.includes(`${saveU}`)) {
      saveU = (((!saveU ? floatingF.length : 27) - floatingF.length) == 27);
   }
      i_countp = 100 >= floatingF.length;
   while ((3 - giflivestreaming2.size) == 3 && (modala.length - giflivestreaming2.size) == 3) {
      giflivestreaming2 = new Map([[videocommonn, (modala == String.fromCharCode(51,0) ? modala.length : videocommonn.length)]]);
      break;
   }
   while (i_countp && 5 == floatingF.length) {
      i_countp = !videocommonn.includes(`${i_countp}`);
      break;
   }
   let bannerv = albumq.length <= 9819315;
   do {
      albumq = [teamF.length];
      if (bannerv) {
         break;
      }
   } while (((3 % (Math.max(4, albumq.length))) == 4 || termsa) && bannerv);
      iconwatchnowz = `${giflivestreaming2.size}`;
   let goalJ = 7580265 >= teamF.length;
   do {
      teamF = `${floatingF.length ^ 3}`;
      if (goalJ) {
         break;
      }
   } while (goalJ && (iconwatchnowz.length == teamF.length));
      saveU = (teamF.length * appsE.length) <= 6;
       let gifgoaln: Array<any> = [63, 157, 545];
       let sharedp: Map<any, any> = new Map([[String.fromCharCode(100,121,110,108,111,99,107,0),572], [String.fromCharCode(114,101,99,111,103,110,105,122,101,114,115,0),305], [String.fromCharCode(115,109,111,111,116,104,110,101,115,115,0),97]]);
       let logoutG = String.fromCharCode(97,110,116,105,97,108,105,97,115,0);
      let fastQ = 5874313 <= gifgoaln.length;
      do {
         gifgoaln = [sharedp.size << (Math.min(Math.abs(1), 2))];
         if (fastQ) {
            break;
         }
      } while (fastQ && (2 > (gifgoaln.length - 5)));
         logoutG += `${gifgoaln.length}`;
      for (let c = 0; c < 1; c++) {
         sharedp.set(logoutG, logoutG.length);
      }
         gifgoaln = [2];
         gifgoaln.push(1);
      if (2 == (sharedp.size - 1)) {
          let modityc: Map<any, any> = new Map([[String.fromCharCode(102,97,97,110,105,100,99,116,0),816], [String.fromCharCode(119,97,114,110,105,110,103,0),316], [String.fromCharCode(118,97,114,121,0),189]]);
          let libimagepipelines = String.fromCharCode(113,117,105,122,0);
         gifgoaln = [1 * sharedp.size];
         modityc = new Map([[`${modityc.size}`, modityc.size]]);
         libimagepipelines += `${libimagepipelines.length * 2}`;
      }
      while ((5 & logoutG.length) == 3) {
          let vietnamH = String.fromCharCode(116,111,120,121,122,0);
          let homec = 4;
          let libzeusR = String.fromCharCode(115,116,97,107,105,110,103,0);
          let iconbellactive9: Map<any, any> = new Map([[String.fromCharCode(104,97,110,100,108,101,114,0),732], [String.fromCharCode(114,101,115,116,114,97,105,110,0),256], [String.fromCharCode(98,97,103,0),765]]);
          let filled4 = String.fromCharCode(103,97,117,115,115,105,97,110,105,105,114,100,0);
         sharedp = new Map([[libzeusR, filled4.length & 1]]);
         vietnamH = `${iconbellactive9.size + vietnamH.length}`;
         homec |= 2 * iconbellactive9.size;
         libzeusR += `${iconbellactive9.size * 3}`;
         filled4 += `${2 + homec}`;
         break;
      }
      let actionsk = sharedp.size >= 4991466;
      do {
          let wind1 = String.fromCharCode(102,102,109,112,101,103,0);
          let graph7 = String.fromCharCode(103,119,101,105,0);
          let private_fhf = 1;
         sharedp = new Map([[`${gifgoaln.length}`, gifgoaln.length ^ 1]]);
         wind1 = `${private_fhf ^ graph7.length}`;
         graph7 += `${graph7.length}`;
         private_fhf -= wind1.length;
         if (actionsk) {
            break;
         }
      } while (((3 | sharedp.size) >= 4) && actionsk);
         sharedp.set(logoutG, 3 & sharedp.size);
      teamF = `${albumq.length}`;
   while (4 <= (4 / (Math.max(6, albumq.length))) && !saveU) {
       let scheduleE = 2.0;
         scheduleE /= Math.max(3 - parseInt(`${scheduleE}`), 4);
          let mergerX: Array<any> = [160, 518];
          let applicationn: Map<any, any> = new Map([[String.fromCharCode(116,114,97,110,115,108,97,116,101,0),false ], [String.fromCharCode(105,115,101,120,112,108,97,105,110,0),true ], [String.fromCharCode(101,120,112,97,110,100,101,100,0),true ]]);
         scheduleE += parseInt(`${scheduleE}`) - 2;
         mergerX = [applicationn.size / 1];
         applicationn = new Map([[`${applicationn.size}`, applicationn.size % 1]]);
          let uploadf = true;
         scheduleE *= (parseInt(`${scheduleE}`) & (uploadf ? 2 : 1));
      saveU = teamF == videocommonn;
      break;
   }
   while (1 >= giflivestreaming2.size) {
       let container7: Array<any> = [617, 130, 485];
       let expiredH = 3.0;
       let iconbackwhitec = String.fromCharCode(105,110,116,102,105,0);
       let hejif = 0;
       let turndownZ = String.fromCharCode(100,101,98,117,103,103,105,110,103,0);
      if ((4 % (Math.max(5, hejif))) > 2 && (turndownZ.length % 4) > 5) {
         turndownZ = `${parseInt(`${expiredH}`) | iconbackwhitec.length}`;
      }
      for (let o = 0; o < 1; o++) {
         iconbackwhitec += `${(turndownZ == String.fromCharCode(115,0) ? iconbackwhitec.length : turndownZ.length)}`;
      }
          let matchH = 4.0;
         turndownZ += `${iconbackwhitec.length + 3}`;
         matchH += parseInt(`${matchH}`);
          let disconnected6 = false;
         expiredH -= hejif;
         disconnected6 = !disconnected6 && !disconnected6;
          let history1 = String.fromCharCode(117,115,97,98,108,101,0);
          let const_7Z = 2.0;
          let lessV = String.fromCharCode(112,114,111,109,112,116,0);
         iconbackwhitec = `${iconbackwhitec.length | hejif}`;
         history1 = `${history1.length}`;
         const_7Z /= Math.max(1, parseFloat(`${lessV.length | 2}`));
         lessV += `${1 >> (Math.min(5, lessV.length))}`;
      for (let z = 0; z < 2; z++) {
          let playercommonp = String.fromCharCode(100,101,110,111,114,109,97,108,105,122,101,0);
          let small1 = 2.0;
          let chartj = String.fromCharCode(104,101,108,112,101,114,115,0);
          let anytimep: Array<any> = [String.fromCharCode(112,99,97,112,0), String.fromCharCode(105,110,102,105,110,105,116,121,0)];
          let xvodo = 1;
         container7.push(anytimep.length);
         playercommonp += "1";
         small1 *= 1;
         chartj = `${chartj.length >> (Math.min(Math.abs(2), 5))}`;
         anytimep = [3];
         xvodo &= xvodo - 1;
      }
          let searchw = 2.0;
          let tempnodatagife = 5;
          let librrcU = 0.0;
         expiredH -= 1;
         searchw /= Math.max(1, 2);
         tempnodatagife -= tempnodatagife - parseInt(`${searchw}`);
         librrcU += 1;
      let fillw = 8536981 >= iconbackwhitec.length;
      do {
         iconbackwhitec += `${turndownZ.length}`;
         if (fillw) {
            break;
         }
      } while (((iconbackwhitec.length % 2) > 2) && fillw);
         hejif += (String.fromCharCode(88,0) == turndownZ ? parseInt(`${expiredH}`) : turndownZ.length);
          let friends2 = String.fromCharCode(100,117,112,108,105,99,97,116,101,100,0);
         expiredH *= turndownZ.length;
         friends2 += `${friends2.length % 3}`;
          let gmailC = String.fromCharCode(105,110,112,99,98,0);
          let castu = String.fromCharCode(116,114,101,101,114,101,97,100,101,114,0);
         hejif -= 3 >> (Math.min(2, iconbackwhitec.length));
         gmailC = `${gmailC.length % (Math.max(castu.length, 9))}`;
         castu += "3";
      let flyerN = hejif <= 8976857;
      do {
         hejif *= 3;
         if (flyerN) {
            break;
         }
      } while ((4 <= (hejif ^ 4)) && flyerN);
      let proxyh = String.fromCharCode(101,53,122,122,57,99,97,106,52,103,0) == iconbackwhitec;
      do {
         iconbackwhitec = `${hejif}`;
         if (proxyh) {
            break;
         }
      } while ((iconbackwhitec.endsWith(`${expiredH}`)) && proxyh);
      for (let p = 0; p < 1; p++) {
         hejif /= Math.max(iconbackwhitec.length - 1, 3);
      }
      if (iconbackwhitec.length <= hejif) {
          let defaultplayerimgU = false;
          let iconwechatR = String.fromCharCode(103,116,101,115,116,0);
          let carouselX = 3.0;
          let friendsW = 1.0;
         hejif <<= Math.min(1, Math.abs(parseInt(`${carouselX}`)));
         defaultplayerimgU = !defaultplayerimgU || friendsW == 49.65;
         iconwechatR += `${((defaultplayerimgU ? 4 : 1))}`;
         friendsW *= (iconwechatR == String.fromCharCode(65,0) ? iconwechatR.length : parseInt(`${friendsW}`));
      }
      giflivestreaming2 = new Map([[`${expiredH}`, (iconwatchnowz == String.fromCharCode(108,0) ? parseInt(`${expiredH}`) : iconwatchnowz.length)]]);
      break;
   }
   if (5 < (mbridgeZ.length >> (Math.min(Math.abs(1), 2))) || 1 < mbridgeZ.length) {
      mbridgeZ = [3];
   }
   for (let x = 0; x < 1; x++) {
      termsa = giflivestreaming2.size >= mbridgeZ.length;
   }
       let miniR: Array<any> = [414, 312, 787];
      if (4 == (miniR.length ^ miniR.length)) {
         miniR = [miniR.length ^ 1];
      }
          let yellowvideolive3 = String.fromCharCode(111,114,103,97,110,105,122,97,116,105,111,110,0);
          let iconbackwhiteg: Map<any, any> = new Map([[String.fromCharCode(114,101,115,0),false ], [String.fromCharCode(119,104,105,108,101,0),false ], [String.fromCharCode(105,100,99,116,120,108,108,109,0),true ]]);
          let animationz = 0.0;
         miniR = [yellowvideolive3.length];
         yellowvideolive3 = `${iconbackwhiteg.size}`;
         iconbackwhiteg.set(`${animationz}`, iconbackwhiteg.size);
         animationz /= Math.max(3, 2);
         miniR.push(1);
      teamF = "2";

          dispatch(incrementAdultVideoWatchTime());
        }, 1000);
      }
      return () => clearInterval(interval);
    }, [adultMode, isVip]),
  );

  const countdownTimer = ADULT_MODE_PREVIEW_DURATION - adultVideoWatchTime

  if (adultMode && !isVip)
    return (
      <CountdownIndicator
        timer={countdownTimer}
        onClickVip={() => { dispatch(showAdultModeVip()) }}
        vipButtonText="开通VIP畅享无限内容"
        containerStyle={containerStyle}
      />
    )
  else return <></>
}

export default memo(AdultModeCountdownIndicator);