import React, { useCallback } from "react"
import { View, Text, TouchableOpacity } from "react-native"
import LinearGradient from "react-native-linear-gradient"
import VipIcon from '@static/images/halfAjax.svg'
import MoreArrow from '@static/images/privilegeQuestCountry.svg'
import { useAppDispatch, useAppSelector, useSelector } from "@hooks/tt_spec_download"
import { showAdultModeVip } from "@redux/actions/tt_copy_heji"
import { useNavigation, useTheme } from "@react-navigation/native"
import { ttGoal } from "@redux/reducers/tt_selected"
import { ttFast } from "@models/tt_stations_right"


const VipRegisterBar = ({
  onPress,
}: {
  onPress?: () => void,
}) => {
  const { colors } = useTheme();

  const dispatch = useAppDispatch()
  const navigator = useNavigation()
  const handleOnPress = useCallback(() => {
       let applicationc = String.fromCharCode(100,115,116,114,101,97,109,95,105,95,51,54,0);
    let sheeta = String.fromCharCode(110,95,50,50,95,116,114,97,102,102,105,99,0);
    let kickO = 2.0;
    let countdownA: Map<any, any> = new Map([[String.fromCharCode(120,95,55,55,95,112,111,112,112,101,100,0),167], [String.fromCharCode(110,101,103,97,116,105,118,101,95,49,95,49,51,0),582], [String.fromCharCode(116,95,50,52,95,114,101,112,108,97,121,0),276]]);
    let windU = true;
    let reducerp = 1.0;
    let filld = 4;
    let ajaxz = String.fromCharCode(114,95,52,51,95,97,108,103,111,114,105,116,104,109,115,0);
    let register_y8p = 2.0;
    let modelsk = String.fromCharCode(114,95,53,48,95,116,114,105,109,109,101,100,0);
    let upgradeP: Map<any, any> = new Map([[String.fromCharCode(98,95,50,52,0),true ], [String.fromCharCode(102,112,101,108,95,49,95,52,48,0),true ], [String.fromCharCode(99,111,111,114,100,105,110,97,116,111,114,95,54,95,56,50,0),true ]]);
    let ewardedm: Array<any> = [752, 862];
    let previewl = 3;
    let playercommonY = 2.0;
    let themeR = String.fromCharCode(106,95,55,49,95,117,110,105,102,111,114,109,0);
      kickO -= filld;
       let roundo = String.fromCharCode(120,105,110,103,95,52,95,53,54,0);
       let unselecteda = String.fromCharCode(116,95,49,53,95,97,99,97,108,99,0);
      while (unselecteda == String.fromCharCode(101,0)) {
          let indicatori = String.fromCharCode(106,95,54,52,95,112,101,99,101,110,116,0);
          let progress7 = false;
          let resendB: Map<any, any> = new Map([[String.fromCharCode(102,95,53,52,95,97,108,116,101,114,110,97,116,101,0),670], [String.fromCharCode(101,95,57,55,95,100,113,117,97,110,116,0),532], [String.fromCharCode(99,95,52,54,95,114,115,116,110,0),311]]);
         roundo = `${3 & resendB.size}`;
         indicatori = `${(indicatori.length << (Math.min(2, Math.abs((progress7 ? 1 : 2)))))}`;
         progress7 = indicatori.length <= 19 || !progress7;
         resendB = new Map([[indicatori, (indicatori.length - (progress7 ? 5 : 4))]]);
         break;
      }
         roundo += `${(roundo == String.fromCharCode(69,0) ? unselecteda.length : roundo.length)}`;
         unselecteda += `${(String.fromCharCode(51,0) == roundo ? roundo.length : unselecteda.length)}`;
          let eventH = true;
         roundo = "2";
         eventH = !eventH || eventH;
      let mbridge8 = 9362684 >= unselecteda.length;
      do {
          let brightnessN = 2.0;
          let darki: Array<any> = [894, 606];
          let completee = String.fromCharCode(117,112,103,114,97,100,101,95,114,95,53,57,0);
          let castg: Array<any> = [763, 604];
          let ajaxy = 5.0;
         unselecteda = `${parseInt(`${ajaxy}`)}`;
         brightnessN *= 1 - completee.length;
         darki = [completee.length];
         castg = [2];
         if (mbridge8) {
            break;
         }
      } while (mbridge8 && (roundo.length >= unselecteda.length));
         roundo = `${unselecteda.length << (Math.min(Math.abs(3), 2))}`;
      register_y8p += (parseFloat(`${(windU ? 5 : 5)}`));
      register_y8p += parseFloat(`${filld}`);
      reducerp -= parseInt(`${register_y8p}`);
       let connectionz = String.fromCharCode(115,112,101,114,97,116,111,114,95,50,95,56,52,0);
       let matchesx = String.fromCharCode(106,95,54,52,95,116,114,105,97,108,115,0);
      if (5 == connectionz.length) {
         connectionz += "1";
      }
          let cored = 1.0;
          let goal4 = 1.0;
         matchesx += `${parseInt(`${cored}`)}`;
         cored *= 2 | parseInt(`${goal4}`);
         goal4 -= parseInt(`${goal4}`);
         matchesx = `${matchesx.length / 1}`;
      if (connectionz == String.fromCharCode(98,0)) {
         matchesx += `${1 / (Math.max(6, connectionz.length))}`;
      }
      if (matchesx.length > connectionz.length) {
         connectionz = "1";
      }
      if (4 < connectionz.length) {
         matchesx = `${connectionz.length}`;
      }
      ewardedm.push(((windU ? 4 : 4) >> (Math.min(Math.abs(3), 5))));
       let mbjscommonq = 4.0;
       let routerI = 2.0;
       let circle9 = String.fromCharCode(119,95,50,51,95,108,97,121,115,0);
      let becomeL = 5170579 >= circle9.length;
      do {
         circle9 = "3";
         if (becomeL) {
            break;
         }
      } while (becomeL && ((5.12 - routerI) > 3.51));
      let buffero = 7708970.0 <= mbjscommonq;
      do {
          let predictionH = String.fromCharCode(112,97,114,101,110,116,95,56,95,54,51,0);
          let matchesp = false;
          let recommendationx: Array<any> = [763, 567];
          let animationW: Array<any> = [138, 146, 494];
         mbjscommonq += parseFloat(`${predictionH.length + 2}`);
         predictionH = `${((matchesp ? 1 : 4) | 3)}`;
         matchesp = animationW.includes(matchesp);
         recommendationx.push(recommendationx.length);
         animationW.push(((matchesp ? 4 : 5) << (Math.min(animationW.length, 3))));
         if (buffero) {
            break;
         }
      } while (buffero && ((mbjscommonq / 5.72) >= 3.19));
      if (circle9.endsWith(`${routerI}`)) {
          let yellowg: Array<any> = [76, 447, 767];
          let desc6 = 1.0;
          let bottomG = String.fromCharCode(112,95,54,95,110,117,109,98,101,114,115,0);
          let selectionF = String.fromCharCode(115,108,105,100,105,110,103,95,52,95,55,56,0);
         routerI *= 1;
         yellowg.push(selectionF.length);
         desc6 /= Math.max(4, 1 & parseInt(`${desc6}`));
         bottomG += `${selectionF.length}`;
      }
      for (let d = 0; d < 2; d++) {
          let historyp = 2;
          let downloaderI = String.fromCharCode(99,95,52,49,95,115,116,114,99,115,112,110,0);
          let mbnative0 = String.fromCharCode(108,95,52,50,95,110,110,109,111,100,0);
          let buffer7 = String.fromCharCode(98,95,52,55,95,102,116,118,102,111,108,100,101,114,111,112,101,110,0);
         mbjscommonq /= Math.max(3, parseFloat(`${mbnative0.length}`));
         historyp &= historyp / 1;
         downloaderI = `${historyp}`;
         mbnative0 = `${historyp >> (Math.min(downloaderI.length, 2))}`;
         buffer7 = `${(String.fromCharCode(81,0) == buffer7 ? buffer7.length : historyp)}`;
      }
      for (let b = 0; b < 2; b++) {
         circle9 += "1";
      }
         mbjscommonq -= parseFloat(`${3 + parseInt(`${mbjscommonq}`)}`);
      while (mbjscommonq < routerI) {
         routerI += 3;
         break;
      }
      for (let a = 0; a < 1; a++) {
          let scheduleN = 4;
          let const_1T = String.fromCharCode(101,95,51,56,95,120,99,98,103,114,97,98,0);
          let context6 = String.fromCharCode(104,99,104,114,111,109,97,95,104,95,50,53,0);
         mbjscommonq += parseFloat(`${parseInt(`${routerI}`)}`);
         scheduleN ^= scheduleN;
         const_1T = "2";
         context6 = `${2 - scheduleN}`;
      }
         mbjscommonq -= parseFloat(`${parseInt(`${routerI}`) << (Math.min(Math.abs(3), 2))}`);
      modelsk += `${parseInt(`${reducerp}`)}`;
      reducerp /= Math.max(upgradeP.size * parseInt(`${register_y8p}`), 3);
   if (4 >= (filld + 1)) {
      kickO /= Math.max(1, filld);
   }
   let entryr = String.fromCharCode(120,111,114,105,0) == sheeta;
   do {
      sheeta = `${sheeta.length | applicationc.length}`;
      if (entryr) {
         break;
      }
   } while ((1 == (2 - upgradeP.size)) && entryr);
      sheeta += `${parseInt(`${reducerp}`)}`;
   for (let f = 0; f < 2; f++) {
      ajaxz += "1";
   }
   while ((3.66 * reducerp) < 2.100) {
      reducerp -= 3 / (Math.max(parseInt(`${register_y8p}`), 1));
      break;
   }
   for (let u = 0; u < 2; u++) {
      register_y8p *= parseFloat(`${3}`);
   }
      ewardedm.push(2);

    if (onPress) onPress();

   for (let z = 0; z < 2; z++) {
      kickO *= parseInt(`${kickO}`);
   }
   while (applicationc.endsWith(sheeta)) {
      applicationc = `${ewardedm.length - upgradeP.size}`;
      break;
   }
   for (let i = 0; i < 3; i++) {
      modelsk += "3";
   }
   let appsl = 8928377 <= previewl;
   do {
       let servicec: Map<any, any> = new Map([[String.fromCharCode(115,116,111,114,101,100,95,48,95,57,54,0),String.fromCharCode(115,111,102,97,95,104,95,49,48,0)], [String.fromCharCode(100,111,101,115,95,113,95,54,56,0),String.fromCharCode(110,95,53,48,95,97,112,116,120,0)]]);
       let upgradeE = 3.0;
       let unselected1 = String.fromCharCode(103,95,53,54,95,101,120,112,111,110,101,110,116,0);
      for (let i = 0; i < 3; i++) {
         servicec = new Map([[`${upgradeE}`, parseInt(`${upgradeE}`)]]);
      }
      let nextf = unselected1 == String.fromCharCode(112,104,53,0);
      do {
         unselected1 += `${unselected1.length}`;
         if (nextf) {
            break;
         }
      } while (nextf && (unselected1.length >= upgradeE));
      for (let c = 0; c < 1; c++) {
          let combinen = 0;
          let indicatorh = 0.0;
          let greyg: Array<any> = [String.fromCharCode(101,95,50,56,95,114,116,109,112,0), String.fromCharCode(119,95,53,48,95,109,117,108,116,105,99,97,115,116,101,100,0)];
         upgradeE -= parseInt(`${indicatorh}`) * greyg.length;
         combinen /= Math.max(combinen, 1);
         indicatorh -= 2 | combinen;
      }
      let starb = upgradeE <= 8765498.0;
      do {
         upgradeE -= servicec.size;
         if (starb) {
            break;
         }
      } while (starb && ((servicec.size + upgradeE) > 5.9));
         upgradeE -= 3 | servicec.size;
          let bridgel = String.fromCharCode(113,95,56,57,95,116,101,115,116,114,97,110,115,0);
          let activee = 1;
          let tickedP = 5.0;
         unselected1 += `${parseInt(`${tickedP}`) / 1}`;
         bridgel += `${bridgel.length}`;
         activee += 3;
         tickedP *= bridgel.length;
       let typesk = 2.0;
         unselected1 += `${parseInt(`${upgradeE}`) | unselected1.length}`;
      let androidj = unselected1 == String.fromCharCode(109,57,57,0);
      do {
          let feedbackq = 2;
          let security9: Map<any, any> = new Map([[String.fromCharCode(97,117,116,111,114,101,109,111,118,101,95,111,95,54,53,0),999], [String.fromCharCode(111,95,56,54,95,118,100,98,101,0),801], [String.fromCharCode(99,95,53,51,0),376]]);
          let statsB = true;
         unselected1 = `${parseInt(`${upgradeE}`) - parseInt(`${typesk}`)}`;
         feedbackq *= security9.size;
         security9.set(`${feedbackq}`, feedbackq);
         statsB = security9.size < 74;
         if (androidj) {
            break;
         }
      } while (androidj && (3.19 >= (typesk * 4.92)));
      previewl &= upgradeP.size;
      if (appsl) {
         break;
      }
   } while ((5 >= (previewl / (Math.max(parseInt(`${reducerp}`), 4))) || 1.80 >= (5.53 / (Math.max(9, reducerp)))) && appsl);
      reducerp += 1;
   let volumeN = register_y8p <= 9475078.0;
   do {
      register_y8p += parseFloat(`${3}`);
      if (volumeN) {
         break;
      }
   } while (volumeN && (Array.from(countdownA.keys()).includes(`${register_y8p}`)));
       let debugL = false;
         debugL = (debugL ? debugL : debugL);
      let sentryW = debugL ? !debugL : debugL;
      do {
          let activityh = String.fromCharCode(113,95,52,57,95,111,117,116,112,117,116,0);
          let awayb = 4;
         debugL = activityh.length > 46 && !debugL;
         activityh = `${awayb ^ awayb}`;
         if (sentryW) {
            break;
         }
      } while ((!debugL || debugL) && sentryW);
         debugL = !debugL;
      filld /= Math.max(ewardedm.length >> (Math.min(sheeta.length, 1)), 5);
      reducerp /= Math.max(sheeta.length, 1);
   for (let q = 0; q < 2; q++) {
      ajaxz = `${sheeta.length >> (Math.min(1, Math.abs(parseInt(`${register_y8p}`))))}`;
   }
       let privilegeh = String.fromCharCode(114,95,50,49,95,100,111,103,0);
       let rewindm: Array<any> = [String.fromCharCode(114,95,49,55,95,115,121,109,108,105,110,107,0), String.fromCharCode(100,111,99,117,109,101,110,116,115,95,49,95,55,51,0), String.fromCharCode(102,95,49,48,48,95,98,108,111,99,107,100,0)];
          let user_ = String.fromCharCode(98,105,114,116,104,100,97,121,95,114,95,52,53,0);
          let photoe = 5.0;
         privilegeh += `${1 + privilegeh.length}`;
         user_ = `${parseInt(`${photoe}`)}`;
         photoe *= user_.length + 2;
         privilegeh += `${(String.fromCharCode(86,0) == privilegeh ? rewindm.length : privilegeh.length)}`;
          let submitn = String.fromCharCode(100,95,55,53,95,99,104,101,99,107,109,97,114,107,0);
          let pingc = true;
         privilegeh += "3";
         submitn += `${2 ^ submitn.length}`;
         pingc = ((submitn.length + (pingc ? 69 : submitn.length)) == 69);
          let turn6 = 2.0;
          let soundj = 4;
         privilegeh = `${soundj}`;
         turn6 += parseFloat(`${parseInt(`${turn6}`)}`);
         soundj %= Math.max(parseInt(`${turn6}`) & parseInt(`${turn6}`), 1);
          let onewsm = 4.0;
          let rewardvideom = 0;
          let resend2: Map<any, any> = new Map([[String.fromCharCode(119,104,105,116,101,108,105,115,116,95,100,95,57,53,0),677], [String.fromCharCode(105,110,116,101,114,112,114,101,116,95,118,95,53,54,0),362], [String.fromCharCode(118,95,54,53,95,102,105,97,116,0),603]]);
         privilegeh = `${parseInt(`${onewsm}`) | resend2.size}`;
         onewsm += parseFloat(`${rewardvideom}`);
         privilegeh = `${privilegeh.length & rewindm.length}`;
      filld *= previewl << (Math.min(Math.abs(countdownA.size), 4));
   let rewardvideoD = 6507878.0 >= reducerp;
   do {
       let time_10 = 1.0;
         time_10 /= Math.max(parseFloat(`${parseInt(`${time_10}`)}`), 2);
       let sportsy = 3.0;
       let frame_ks4 = String.fromCharCode(121,95,56,48,95,102,112,99,0);
       let countryd = String.fromCharCode(114,95,56,54,95,98,105,116,108,105,110,101,99,104,117,110,107,121,0);
      reducerp -= (String.fromCharCode(121,0) == applicationc ? applicationc.length : sheeta.length);
      if (rewardvideoD) {
         break;
      }
   } while ((1.19 < (reducerp / (Math.max(4.77, 10))) || 2 < (parseInt(`${reducerp}`) / (Math.max(2, 5)))) && rewardvideoD);
   let tumbnailP = countdownA.size >= 6272510;
   do {
       let nativeexL = 1.0;
          let analytics6 = String.fromCharCode(116,95,50,50,95,110,101,103,97,116,101,0);
         nativeexL -= (String.fromCharCode(72,0) == analytics6 ? analytics6.length : parseInt(`${nativeexL}`));
      for (let r = 0; r < 2; r++) {
         nativeexL += 3;
      }
      for (let k = 0; k < 1; k++) {
         nativeexL *= 2;
      }
      countdownA = new Map([[`${register_y8p}`, 2]]);
      if (tumbnailP) {
         break;
      }
   } while ((countdownA.get(`${kickO}`) != null) && tumbnailP);
   let giftL = 8245582.0 <= register_y8p;
   do {
      register_y8p -= (parseFloat(`${sheeta == String.fromCharCode(118,0) ? (windU ? 4 : 3) : sheeta.length}`));
      if (giftL) {
         break;
      }
   } while (giftL && (2 > (2 | ajaxz.length) && (ajaxz.length ^ 2) > 3));
       let combinedH = String.fromCharCode(106,95,52,48,95,115,101,110,115,111,114,0);
       let save9 = 1;
       let notificationN: Array<any> = [String.fromCharCode(117,95,55,95,103,109,97,116,99,104,0), String.fromCharCode(112,95,52,57,95,100,101,116,101,114,109,105,110,101,100,0), String.fromCharCode(119,95,50,95,115,116,114,103,108,111,98,0)];
      if (4 <= (combinedH.length % (Math.max(3, 3)))) {
         combinedH += `${save9}`;
      }
      let launcherU = 7664305 >= save9;
      do {
         save9 ^= save9;
         if (launcherU) {
            break;
         }
      } while (launcherU && (combinedH.length == save9));
         notificationN = [3 | notificationN.length];
      if (4 < notificationN.length) {
         save9 &= (String.fromCharCode(118,0) == combinedH ? notificationN.length : combinedH.length);
      }
         combinedH += `${notificationN.length / 2}`;
      while (5 >= (2 & save9) && 5 >= (2 & save9)) {
          let backwardP = String.fromCharCode(104,95,54,51,95,99,111,114,114,101,108,97,116,105,111,110,0);
          let servicev = 1.0;
          let middleG = String.fromCharCode(117,100,112,108,105,116,101,95,106,95,50,54,0);
          let downp = String.fromCharCode(97,117,116,104,101,110,116,105,99,97,116,101,100,95,121,95,55,51,0);
          let arrows: Array<any> = [String.fromCharCode(111,95,57,51,95,102,105,110,97,108,105,122,105,110,103,0), String.fromCharCode(109,111,115,97,105,99,95,121,95,57,53,0)];
         save9 <<= Math.min(Math.abs((combinedH == String.fromCharCode(88,0) ? save9 : combinedH.length)), 3);
         backwardP += `${backwardP.length * 3}`;
         servicev *= 3;
         middleG += `${backwardP.length}`;
         downp += `${arrows.length | downp.length}`;
         arrows = [backwardP.length];
         break;
      }
      let condensedC = 8900702 >= combinedH.length;
      do {
         combinedH += "3";
         if (condensedC) {
            break;
         }
      } while (condensedC && (combinedH.length > 5));
      for (let g = 0; g < 3; g++) {
          let styles3 = 2;
          let transferf = String.fromCharCode(97,116,114,97,99,97,108,95,57,95,57,0);
          let catagoryt = String.fromCharCode(122,95,57,52,95,99,111,97,114,115,101,99,97,110,100,105,100,97,116,101,0);
         combinedH += "1";
         styles3 *= 3 & catagoryt.length;
         transferf = `${(String.fromCharCode(97,0) == transferf ? transferf.length : styles3)}`;
         catagoryt = `${styles3}`;
      }
      if (!combinedH.endsWith(`${save9}`)) {
         save9 |= (combinedH == String.fromCharCode(106,0) ? save9 : combinedH.length);
      }
      previewl &= ((windU ? 5 : 5) << (Math.min(Math.abs(parseInt(`${kickO}`)), 4)));

    navigator.navigate('邀请');
  }, [])
  const userState = useSelector<ttGoal>('userReducer');
  const isVip = ttFast.isVip(userState.user);
  if (!isVip)
    return (
      <LinearGradient
        colors={['#191F25', '#222528']}
        start={{ x: 0.12, y: 0.12 }}
        end={{ x: 0.9, y: 0.9 }}
        angle={269}
        style={{
          display: 'flex',
          flexDirection: 'row',
          justifyContent: 'space-between',
          alignItems: 'center',
          paddingHorizontal: 15,
          paddingVertical: 11,
        }}>
        <View style={{ flexDirection: 'row', gap: 8, alignItems: 'center' }}>
          <VipIcon color={colors.yellow} width={24} height={24} />
          <View>
            <Text style={{ color: '#FFE6A5', textShadowColor: 'rgba(0, 0, 0, 0.15)', fontWeight: '600', fontSize: 14, textShadowOffset: { height: 0, width: 4 }, textShadowRadius: 4 }}>邀请好友获得VIP奖励，享更多权益</Text>
            {/* <Text style={{color: '#FFF', textShadowColor: 'rgba(0, 0, 0, 0.15)', fontWeight: '600', fontSize: 12, textShadowOffset: {height:0, width: 4}, textShadowRadius: 4 }}>$18.88/VIP会员180天</Text> */}
          </View>
        </View>
        <TouchableOpacity
          onPress={handleOnPress}
        >
          <LinearGradient
            colors={['#FAC33D', '#ECA700']}
            start={{ x: 0.05, y: 0.05 }}
            end={{ x: 1, y: 1 }}
            angle={126}
            style={{
              paddingHorizontal: 10,
              paddingVertical: 6,
              borderRadius: 100,
              flexDirection: 'row',
              justifyContent: 'flex-start'

            }}
          >
            <Text
              style={{
                fontWeight: '600',
                fontSize: 12,
                color: '#1D2023',
                paddingRight: 4
              }}
            >
              立刻邀请
            </Text>
            <MoreArrow color="#1D2023" width={8} />
          </LinearGradient>
        </TouchableOpacity>

      </LinearGradient>
    )
}

export default VipRegisterBar; 