import { Divider } from "@rneui/base";
import { ReactNode, useCallback, useEffect, useState } from "react";
import {
  Platform,
  StyleProp,
  StyleSheet,
  Text,
  TextStyle,
  View,
  ViewStyle,
} from "react-native";
import { TouchableOpacity } from "react-native-gesture-handler";
import { ViewProps } from "react-native-svg/lib/typescript/fabric/utils";
import { Switch } from "react-native-switch";
import WatchAnytimeVipModal from "../modal/mayi_benefit_debug";
import EighteenPlusOverlay from "../modal/mayi_giftbutton_homeplayer";
import CountdownIndicator from "../button/mayi_imagenetworkerr_pangle";
import AdultModeCountdownIndicator from "./mayi_holder";
import AdultModeSwitch from "./mayi_club";
import { screenModel } from "@type/mayi_libfolly_inouttargetred";
import { useAppDispatch, useAppSelector, useSelector } from "@hooks/mayi_redirect";
import { mayi_Libapminsightb } from "@redux/mayi_live_tumbnail";
import {
  acceptOverEighteen,
  disableAdultMode,
  enableAdultMode,
  enableWatchAnytimeAdultMode,
  hideAdultModeDisclaimer,
  hideAdultModeVip,
  hideAdultVipPrivilegeMiniVideoAction,
  incrementAdultVideoWatchTime,
  showAdultVipPrivilegeMiniVideoAction,
} from "@redux/actions/mayi_iconorientation_chatroombackground";
import { UMENG_CHANNEL } from "@utility/mayi_middleware_apps";
import { AdultVipPrivilegeOverlay } from "../modal/mayi_hash_image";
import { VipPrivilegeFloatingIndicator } from "./mayi_penaltyshootnogoal";
import { mayi_Baseline } from "@redux/reducers/mayi_chatroombackground_unlock";
import { mayi_Gift } from "@models/mayi_libjsinspector";
interface mayi_GoogleViews { }

const eighteenPlusControls = ({ }: mayi_GoogleViews) => {
  const screenState: screenModel = useAppSelector(
    ({ screenReducer }) => screenReducer
  );
  const userState = useSelector<mayi_Baseline>('userReducer');
  const {
    adultModeVipShow,
    adultModeDisclaimerShow,
    adultMode,
    watchAnytimeAdultMode,
    isOverEighteenAccepted,
    showAdultVipPrivilegeMiniVideo
  } = screenState;
  const dispatch = useAppDispatch();
  const isVip = mayi_Gift.isVip(userState.user);

  
  
  
  
  
  
  
  //   return () => clearInterval(interval)
  
  console.log(Platform.OS)

  const handleAccept = useCallback(() => {
       let libhermesY = 3.0;
    let mbbannera = String.fromCharCode(98,101,110,99,104,109,97,114,107,0);
    let firebaseb = 1.0;
    let confirmationl: Array<any> = [392, 691, 953];
    let philippinesO = 5;
    let footballfield1 = true;
    let libyogal: Array<any> = [668, 720];
    let activity7 = String.fromCharCode(100,95,55,57,95,99,111,114,114,101,99,116,105,111,110,0);
    let nterstitial7 = String.fromCharCode(104,111,114,105,122,111,110,116,97,108,108,121,95,111,95,52,50,0);
    let textlayoutmanager1 = 4;
    let turnZ = String.fromCharCode(115,112,101,99,116,114,97,108,95,106,95,54,49,0);
       let pathA = 1.0;
         pathA /= Math.max(5, parseFloat(`${parseInt(`${pathA}`) * parseInt(`${pathA}`)}`));
         pathA += parseFloat(`${1}`);
       let libreactx = true;
      mbbannera = `${parseInt(`${libhermesY}`) | 2}`;
      footballfield1 = (libyogal.length + philippinesO) <= 100;
      libhermesY -= parseFloat(`${parseInt(`${firebaseb}`) - 3}`);
       let next5 = true;
       let reminderV: Map<any, any> = new Map([[String.fromCharCode(110,105,100,110,105,115,116,0),false ], [String.fromCharCode(109,111,99,107,101,100,95,54,95,51,56,0),true ]]);
         reminderV = new Map([[`${reminderV.size}`, reminderV.size % 1]]);
         reminderV = new Map([[`${reminderV.size}`, (2 | (next5 ? 4 : 1))]]);
       let orangedownarrowC = String.fromCharCode(122,95,53,53,95,115,109,111,111,116,104,101,100,0);
         reminderV.set(orangedownarrowC, 2);
      if (reminderV.size == 4) {
          let notificationfillemptyL: Map<any, any> = new Map([[String.fromCharCode(115,116,100,97,116,111,109,105,99,0),true ], [String.fromCharCode(99,111,100,101,99,105,100,0),true ]]);
          let iconlogoutJ = 5.0;
          let penaltymatchiconJ = String.fromCharCode(107,102,119,114,105,116,101,95,109,95,49,48,0);
          let sorth = String.fromCharCode(99,102,104,100,0);
         orangedownarrowC += `${1 | reminderV.size}`;
         notificationfillemptyL.set(`${iconlogoutJ}`, (String.fromCharCode(82,0) == sorth ? sorth.length : parseInt(`${iconlogoutJ}`)));
         penaltymatchiconJ += `${sorth.length - 1}`;
      }
         orangedownarrowC += `${1 << (Math.min(5, orangedownarrowC.length))}`;
      philippinesO -= 2 * reminderV.size;
   for (let f = 0; f < 1; f++) {
       let codegen4 = false;
       let selectC = true;
       let pause0 = String.fromCharCode(101,110,99,0);
       let textinput9: Array<any> = [986, 71, 559];
         pause0 = `${(textinput9.length << (Math.min(2, Math.abs((selectC ? 2 : 3)))))}`;
       let controlsN = String.fromCharCode(115,121,110,99,104,114,111,110,105,122,101,114,0);
       let modelp = String.fromCharCode(119,111,114,107,0);
      if (4 < textinput9.length) {
         modelp = `${((codegen4 ? 5 : 4))}`;
      }
      for (let v = 0; v < 1; v++) {
         selectC = controlsN.length >= 77;
      }
      while (modelp.length <= 5) {
         modelp += `${((selectC ? 3 : 4) - 1)}`;
         break;
      }
      if (!selectC) {
         codegen4 = 38 == textinput9.length || pause0 == String.fromCharCode(53,0);
      }
      let gestures9 = selectC ? !selectC : selectC;
      do {
          let mintegralx = 0.0;
          let contextg = 0.0;
         selectC = (parseInt(`${mintegralx}`) / (Math.max(pause0.length, 7))) > 90;
         mintegralx += parseFloat(`${parseInt(`${contextg}`) % (Math.max(6, parseInt(`${contextg}`)))}`);
         if (gestures9) {
            break;
         }
      } while (gestures9 && (!pause0.includes(`${selectC}`)));
          let iconnotificationnewi: Map<any, any> = new Map([[String.fromCharCode(101,113,117,97,108,115,0),380], [String.fromCharCode(108,105,109,105,116,0),212], [String.fromCharCode(115,101,113,95,122,95,51,54,0),877]]);
         modelp += `${(String.fromCharCode(48,0) == modelp ? (selectC ? 2 : 2) : modelp.length)}`;
         iconnotificationnewi.set(`${iconnotificationnewi.size}`, iconnotificationnewi.size ^ iconnotificationnewi.size);
         modelp = `${((selectC ? 3 : 2) % (Math.max(textinput9.length, 9)))}`;
         textinput9 = [(pause0 == String.fromCharCode(66,0) ? textinput9.length : pause0.length)];
      for (let g = 0; g < 1; g++) {
         textinput9.push(controlsN.length);
      }
      let interstitialx = textinput9.length >= 5049862;
      do {
         textinput9.push(pause0.length ^ 1);
         if (interstitialx) {
            break;
         }
      } while ((!modelp.startsWith(`${textinput9.length}`)) && interstitialx);
      philippinesO &= mbbannera.length % 3;
   }

    console.debug("accepted 18+");

   if (philippinesO < 4) {
       let whatsappM = String.fromCharCode(115,116,112,115,0);
       let arrowupu = String.fromCharCode(115,101,113,117,101,110,99,101,0);
         whatsappM = `${arrowupu.length - whatsappM.length}`;
      let pluse = 9682792 <= arrowupu.length;
      do {
          let auto_29: Map<any, any> = new Map([[String.fromCharCode(116,95,55,54,95,110,111,100,101,115,101,116,0),831], [String.fromCharCode(113,117,97,110,116,105,122,101,0),179]]);
         arrowupu += "1";
         auto_29 = new Map([[`${auto_29.size}`, auto_29.size - auto_29.size]]);
         if (pluse) {
            break;
         }
      } while ((whatsappM != String.fromCharCode(53,0)) && pluse);
      let cedbadcebfbfbfbcfecbcU = 7940778 >= whatsappM.length;
      do {
         whatsappM += `${whatsappM.length}`;
         if (cedbadcebfbfbfbcfecbcU) {
            break;
         }
      } while ((arrowupu == String.fromCharCode(115,0)) && cedbadcebfbfbfbcfecbcU);
      for (let l = 0; l < 1; l++) {
          let downloader8 = 4.0;
          let iconlogoutU = String.fromCharCode(115,95,55,57,95,99,108,105,112,98,111,97,114,100,0);
         whatsappM = `${arrowupu.length | parseInt(`${downloader8}`)}`;
         downloader8 += parseFloat(`${iconlogoutU.length ^ iconlogoutU.length}`);
      }
         arrowupu += `${(whatsappM == String.fromCharCode(87,0) ? whatsappM.length : arrowupu.length)}`;
      while (arrowupu.length >= whatsappM.length) {
         arrowupu += `${whatsappM.length % (Math.max(4, arrowupu.length))}`;
         break;
      }
      philippinesO %= Math.max(parseInt(`${libhermesY}`), 2);
   }
       let become9 = 0.0;
       let hongkongC = true;
       let basketballo = String.fromCharCode(112,114,111,112,97,103,97,116,101,0);
       let emptyX = 2;
       let playL = 4;
       let checkboxl = String.fromCharCode(109,95,49,53,95,103,114,97,121,97,0);
       let j_lockO = String.fromCharCode(110,95,51,56,95,115,117,112,101,114,120,115,97,105,0);
      while ((emptyX >> (Math.min(3, Math.abs(playL)))) < 5) {
         emptyX >>= Math.min(j_lockO.length, 5);
         break;
      }
         hongkongC = playL == 42;
          let codegenn = false;
          let footballF = true;
          let subsK: Array<any> = [568, 142];
         j_lockO = `${checkboxl.length ^ parseInt(`${become9}`)}`;
         codegenn = !footballF;
         footballF = (codegenn ? footballF : !codegenn);
         subsK = [((codegenn ? 5 : 2) / (Math.max(3, (footballF ? 5 : 5))))];
      for (let a = 0; a < 1; a++) {
          let fileq = 4.0;
         hongkongC = checkboxl == String.fromCharCode(51,0);
         fileq *= parseInt(`${fileq}`);
      }
         basketballo += `${playL + 1}`;
      let resendd = String.fromCharCode(53,110,52,98,104,121,53,104,108,0) == basketballo;
      do {
         basketballo += "2";
         if (resendd) {
            break;
         }
      } while ((!basketballo.startsWith(`${hongkongC}`)) && resendd);
         checkboxl += "3";
      mbbannera += `${3 + parseInt(`${become9}`)}`;
   while (2.26 > (firebaseb * 1.55)) {
      firebaseb /= Math.max(1, philippinesO);
      break;
   }
   for (let l = 0; l < 1; l++) {
       let transferE: Map<any, any> = new Map([[String.fromCharCode(106,95,49,95,98,111,117,110,100,97,108,108,0),361], [String.fromCharCode(121,98,114,105,95,116,95,55,55,0),52], [String.fromCharCode(101,112,108,121,0),364]]);
         transferE.set(`${transferE.size}`, transferE.size);
      while ((transferE.size ^ 1) <= 4 || 5 <= (1 ^ transferE.size)) {
         transferE = new Map([[`${transferE.size}`, transferE.size]]);
         break;
      }
      for (let u = 0; u < 3; u++) {
         transferE = new Map([[`${transferE.size}`, transferE.size]]);
      }
      footballfield1 = transferE.size <= confirmationl.length;
   }
   for (let u = 0; u < 2; u++) {
      firebaseb += 1;
   }
    dispatch(enableAdultMode());

      libyogal = [philippinesO];
      libyogal = [libyogal.length ^ 3];
      philippinesO ^= ((footballfield1 ? 4 : 4) * parseInt(`${libhermesY}`));
   while (3 >= libyogal.length) {
       let popupl = String.fromCharCode(97,99,101,108,112,0);
      while (2 >= popupl.length || popupl.length >= 2) {
         popupl = `${3 & popupl.length}`;
         break;
      }
       let photoY = 3;
       let tnewarchdefaultss = 3;
         photoY |= (String.fromCharCode(48,0) == popupl ? popupl.length : photoY);
      libyogal = [(mbbannera == String.fromCharCode(112,0) ? mbbannera.length : philippinesO)];
      break;
   }
   if (4.53 > (firebaseb * libhermesY)) {
      libhermesY /= Math.max(4, parseFloat(`${philippinesO >> (Math.min(1, Math.abs(2)))}`));
   }
    dispatch(acceptOverEighteen());

   if (!activity7.endsWith(`${confirmationl.length}`)) {
      confirmationl = [3 % (Math.max(5, philippinesO))];
   }
   while (!libyogal.includes(philippinesO)) {
      libyogal.push(activity7.length >> (Math.min(Math.abs(3), 2)));
      break;
   }
   let overs = 8727480 <= confirmationl.length;
   do {
       let description_8X = false;
       let iconbackwhiteY = 4.0;
       let gmail5 = String.fromCharCode(112,105,99,107,101,114,115,95,97,95,50,53,0);
         gmail5 += "2";
          let routerD: Map<any, any> = new Map([[String.fromCharCode(98,97,99,107,115,112,97,99,101,0),850], [String.fromCharCode(115,99,104,105,95,113,95,52,57,0),922], [String.fromCharCode(117,95,57,53,95,115,112,97,110,115,0),227]]);
         iconbackwhiteY += parseFloat(`${routerD.size << (Math.min(Math.abs(3), 3))}`);
      for (let y = 0; y < 2; y++) {
         description_8X = description_8X || 98.28 < iconbackwhiteY;
      }
      while (gmail5.length >= 4) {
         gmail5 += `${((description_8X ? 3 : 2))}`;
         break;
      }
      while (2.52 < iconbackwhiteY) {
         iconbackwhiteY *= parseFloat(`${1}`);
         break;
      }
      confirmationl = [2 * parseInt(`${iconbackwhiteY}`)];
      if (overs) {
         break;
      }
   } while (((2 | confirmationl.length) < 4) && overs);
       let eighteenJ = String.fromCharCode(112,95,50,49,95,97,100,106,97,99,101,110,116,0);
       let modalt = String.fromCharCode(105,100,99,116,120,100,99,0);
       let clockF = String.fromCharCode(109,115,110,119,99,0);
          let matchq: Array<any> = [String.fromCharCode(103,114,97,109,115,0), String.fromCharCode(118,105,100,101,111,115,116,114,101,97,109,0)];
          let infoh = 0.0;
          let strg = String.fromCharCode(118,97,114,105,97,100,105,99,0);
         eighteenJ += `${matchq.length - eighteenJ.length}`;
         matchq.push(strg.length + parseInt(`${infoh}`));
         infoh /= Math.max(strg.length, 5);
      if (!modalt.includes(`${eighteenJ.length}`)) {
         modalt += `${1 * modalt.length}`;
      }
      let appleM = eighteenJ.length >= 5166459;
      do {
          let sinit_wca = String.fromCharCode(114,101,103,101,110,101,114,97,116,101,0);
         eighteenJ = `${modalt.length + 1}`;
         sinit_wca += `${1 * sinit_wca.length}`;
         if (appleM) {
            break;
         }
      } while (appleM && (eighteenJ.endsWith(`${modalt.length}`)));
         clockF = `${clockF.length & modalt.length}`;
      let hooks4 = 6601445 <= modalt.length;
      do {
          let gifgoalbgN = 3.0;
         modalt = "3";
         gifgoalbgN += parseFloat(`${parseInt(`${gifgoalbgN}`)}`);
         if (hooks4) {
            break;
         }
      } while ((modalt.length <= 1) && hooks4);
      if (modalt != eighteenJ) {
         eighteenJ += `${eighteenJ.length}`;
      }
      while (modalt.length == 2) {
         clockF += `${eighteenJ.length}`;
         break;
      }
         modalt += `${eighteenJ.length & 3}`;
      if (modalt == String.fromCharCode(83,0)) {
         clockF = `${eighteenJ.length >> (Math.min(Math.abs(2), 4))}`;
      }
      textlayoutmanager1 ^= confirmationl.length;
   while (2 > (1 >> (Math.min(1, confirmationl.length))) && (1 >> (Math.min(4, Math.abs(textlayoutmanager1)))) > 1) {
      confirmationl = [2];
      break;
   }
    dispatch(hideAdultModeDisclaimer());

       let informations: Array<any> = [920, 258, 164];
       let mounting7 = 1;
       let componentregistryN = String.fromCharCode(112,114,101,99,97,108,99,0);
      for (let s = 0; s < 1; s++) {
         mounting7 += mounting7;
      }
       let foregroundC = true;
       let malaysiac = false;
         componentregistryN += `${mounting7}`;
      for (let z = 0; z < 3; z++) {
         componentregistryN += `${(componentregistryN == String.fromCharCode(109,0) ? componentregistryN.length : (foregroundC ? 1 : 3))}`;
      }
      for (let u = 0; u < 3; u++) {
         informations.push(mounting7);
      }
         componentregistryN = `${mounting7 >> (Math.min(informations.length, 4))}`;
         componentregistryN += `${(componentregistryN == String.fromCharCode(80,0) ? componentregistryN.length : (foregroundC ? 1 : 2))}`;
         informations.push(mounting7 % 2);
       let zoom1: Map<any, any> = new Map([[String.fromCharCode(106,95,54,51,95,100,105,115,99,114,105,109,105,110,97,116,111,114,0),493], [String.fromCharCode(110,97,116,105,111,110,97,108,105,116,121,0),193], [String.fromCharCode(115,116,114,116,111,107,0),238]]);
      nterstitial7 = "2";
   if (5.22 == (textlayoutmanager1 - libhermesY)) {
      textlayoutmanager1 ^= mbbannera.length / (Math.max(3, 3));
   }
   for (let z = 0; z < 1; z++) {
      textlayoutmanager1 &= parseInt(`${firebaseb}`);
   }
      libyogal.push(2);
   while ((3.12 * firebaseb) > 3.61 || 4 > (4 ^ confirmationl.length)) {
      confirmationl = [(philippinesO ^ (footballfield1 ? 3 : 2))];
      break;
   }
    

      footballfield1 = libyogal.length < 16;
       let encryptoru: Array<any> = [160, 390, 297];
         encryptoru = [encryptoru.length << (Math.min(Math.abs(1), 4))];
      while (1 <= encryptoru.length) {
          let logouserY = String.fromCharCode(116,101,108,101,103,114,97,109,0);
          let ping7 = false;
          let ajaxR: Map<any, any> = new Map([[String.fromCharCode(116,97,112,95,113,95,52,52,0),true ], [String.fromCharCode(101,114,99,0),true ]]);
          let static_rtx: Array<any> = [130, 974];
          let defaultroombgi = false;
         encryptoru = [ajaxR.size / (Math.max(3, 8))];
         logouserY = `${static_rtx.length ^ 2}`;
         ping7 = static_rtx.includes(ping7);
         ajaxR = new Map([[`${static_rtx.length}`, 1]]);
         defaultroombgi = static_rtx.length > 41;
         break;
      }
      let redscoreballR = 6080458 >= encryptoru.length;
      do {
         encryptoru = [encryptoru.length];
         if (redscoreballR) {
            break;
         }
      } while (redscoreballR && ((1 - encryptoru.length) >= 1 && (encryptoru.length - 1) >= 3));
      activity7 += `${confirmationl.length << (Math.min(Math.abs(3), 3))}`;
   for (let w = 0; w < 2; w++) {
      firebaseb *= parseInt(`${libhermesY}`);
   }
   if (1.19 == (2.76 * libhermesY)) {
      nterstitial7 += `${libyogal.length}`;
   }
    dispatch(enableWatchAnytimeAdultMode());
  }, [])

  const handleReject = useCallback(() => {
       let untickP = 1.0;
    let abidetectZ = 1.0;
    let classes2: Array<any> = [250, 766];
    let defaultteamg: Map<any, any> = new Map([[String.fromCharCode(104,121,112,111,116,104,101,115,101,115,0),false ], [String.fromCharCode(108,111,103,115,116,101,114,101,111,95,50,95,55,0),false ], [String.fromCharCode(115,97,116,0),true ]]);
    let profileactivet = String.fromCharCode(116,120,105,100,95,109,95,55,54,0);
    let sportT = String.fromCharCode(108,111,119,101,115,116,0);
    let libyogaH: Map<any, any> = new Map([[String.fromCharCode(108,95,54,55,95,100,105,102,102,101,114,101,110,99,101,0),766], [String.fromCharCode(117,110,117,115,101,100,0),939]]);
    let graphp = String.fromCharCode(113,95,51,52,95,117,110,99,111,114,114,0);
    let nalyticse = String.fromCharCode(97,95,57,57,95,116,104,101,110,97,98,108,101,0);
       let splashB = 0;
       let libreactnativeblobs = false;
       let libreanimatedN: Array<any> = [974, 667, 452];
          let basketballmatchdetailbg0 = true;
         libreanimatedN.push((libreanimatedN.length << (Math.min(5, Math.abs((basketballmatchdetailbg0 ? 1 : 2))))));
         libreanimatedN.push(3);
         libreanimatedN = [libreanimatedN.length];
      if (libreanimatedN.length <= 3) {
         libreanimatedN = [(libreanimatedN.length | (libreactnativeblobs ? 2 : 3))];
      }
       let rankp = 1.0;
       let libapminsightbo = 1;
       let statsnomoredataQ = 3;
         libreanimatedN = [statsnomoredataQ];
      let goalg = 4964647 <= splashB;
      do {
         splashB <<= Math.min(Math.abs(libapminsightbo), 4);
         if (goalg) {
            break;
         }
      } while ((statsnomoredataQ > 3) && goalg);
         splashB += libreanimatedN.length;
      abidetectZ -= libyogaH.size;
      sportT = "2";
      sportT = `${1 ^ libyogaH.size}`;
   for (let w = 0; w < 2; w++) {
      untickP *= parseInt(`${abidetectZ}`);
   }
      untickP -= sportT.length;
   for (let u = 0; u < 3; u++) {
      sportT = `${(String.fromCharCode(114,0) == sportT ? parseInt(`${untickP}`) : sportT.length)}`;
   }
      classes2 = [sportT.length << (Math.min(4, profileactivet.length))];
   for (let j = 0; j < 3; j++) {
      untickP += 2 << (Math.min(1, sportT.length));
   }

    console.debug("rejected 18+");

      classes2.push(libyogaH.size);
       let auto_nqG: Array<any> = [String.fromCharCode(108,101,97,100,105,110,103,0), String.fromCharCode(112,95,51,0), String.fromCharCode(100,101,116,97,99,104,101,100,0)];
       let colorsx = String.fromCharCode(97,116,97,99,101,110,116,101,114,0);
       let mbsplash4 = String.fromCharCode(113,105,110,100,101,120,0);
      if (!mbsplash4.includes(`${colorsx.length}`)) {
         mbsplash4 += `${mbsplash4.length}`;
      }
         mbsplash4 = `${auto_nqG.length & colorsx.length}`;
      while ((auto_nqG.length ^ 2) >= 5 || (auto_nqG.length ^ 2) >= 4) {
         auto_nqG = [colorsx.length];
         break;
      }
         colorsx = `${(mbsplash4 == String.fromCharCode(77,0) ? colorsx.length : mbsplash4.length)}`;
         mbsplash4 += `${(colorsx == String.fromCharCode(110,0) ? auto_nqG.length : colorsx.length)}`;
         colorsx = `${mbsplash4.length}`;
      for (let d = 0; d < 1; d++) {
         colorsx += `${1 >> (Math.min(1, mbsplash4.length))}`;
      }
       let chatroombackground_ = String.fromCharCode(98,108,111,98,115,105,122,101,0);
       let textlayoutmanagery = String.fromCharCode(115,116,114,117,99,116,117,114,97,108,0);
       let fullscreenmin8 = 5.0;
      graphp += "3";
   let shareN = 7433451.0 <= untickP;
   do {
      untickP *= 3;
      if (shareN) {
         break;
      }
   } while (shareN && (untickP <= 2.54));
      sportT = `${classes2.length}`;
      libyogaH = new Map([[`${libyogaH.size}`, (graphp == String.fromCharCode(67,0) ? libyogaH.size : graphp.length)]]);
       let schedulem = false;
       let hooksR = 0;
       let teamdetailsbgh: Array<any> = [String.fromCharCode(114,95,53,51,95,97,110,110,101,120,0), String.fromCharCode(121,95,50,54,95,115,111,108,105,100,99,111,108,111,114,0)];
         hooksR += ((schedulem ? 2 : 5) * hooksR);
       let indicatorq = String.fromCharCode(114,116,114,105,109,0);
       let predictionarrowS: Array<any> = [336, 109];
      while ((teamdetailsbgh.length & 1) < 4 && (1 & teamdetailsbgh.length) < 1) {
         teamdetailsbgh.push(predictionarrowS.length);
         break;
      }
         teamdetailsbgh = [predictionarrowS.length % 1];
          let videoU = 3.0;
          let iconbackwhiteT = 4;
         hooksR /= Math.max(hooksR, 2);
         videoU -= 2 - iconbackwhiteT;
         iconbackwhiteT |= parseInt(`${videoU}`);
      while (hooksR >= 1) {
          let kickZ = 1.0;
          let analyticsM = 2.0;
          let sentryc = 0.0;
         hooksR >>= Math.min(2, Math.abs(3 | parseInt(`${kickZ}`)));
         kickZ /= Math.max(1, parseFloat(`${parseInt(`${analyticsM}`) * 2}`));
         analyticsM /= Math.max(5, parseFloat(`${3 ^ parseInt(`${analyticsM}`)}`));
         sentryc -= parseInt(`${analyticsM}`) >> (Math.min(1, Math.abs(2)));
         break;
      }
      while (2 <= (hooksR << (Math.min(Math.abs(5), 1))) && 5 <= (teamdetailsbgh.length << (Math.min(5, Math.abs(hooksR))))) {
         hooksR *= 2;
         break;
      }
         teamdetailsbgh = [indicatorq.length % 3];
      libyogaH = new Map([[`${libyogaH.size}`, teamdetailsbgh.length | libyogaH.size]]);
   while (5 > defaultteamg.size) {
      defaultteamg.set(graphp, classes2.length << (Math.min(graphp.length, 2)));
      break;
   }
   while (libyogaH.size > 4) {
      libyogaH = new Map([[`${defaultteamg.size}`, defaultteamg.size]]);
      break;
   }
    dispatch(disableAdultMode());

   if ((untickP + abidetectZ) == 1.63) {
       let ballX = false;
       let footballfieldG = String.fromCharCode(112,114,111,98,101,95,108,95,50,49,0);
       let fullscreenmaxh = String.fromCharCode(114,101,103,105,115,116,101,114,115,0);
       let images4 = true;
       let reactnativeratingsS = false;
          let iconsharefriends8 = String.fromCharCode(100,105,97,109,111,110,100,0);
          let logoJ = String.fromCharCode(116,95,56,57,95,97,115,97,110,0);
         footballfieldG += `${(String.fromCharCode(72,0) == iconsharefriends8 ? (reactnativeratingsS ? 2 : 5) : iconsharefriends8.length)}`;
         logoJ = `${logoJ.length % (Math.max(1, 8))}`;
      while (4 >= fullscreenmaxh.length) {
          let holders = 1;
          let previewc: Map<any, any> = new Map([[String.fromCharCode(117,112,100,97,116,101,114,0),13], [String.fromCharCode(104,105,103,104,112,97,115,115,0),296]]);
          let graphicss = false;
         fullscreenmaxh += `${holders}`;
         holders /= Math.max(1 + previewc.size, 1);
         previewc.set(`${graphicss}`, ((graphicss ? 3 : 3) - 1));
         break;
      }
       let backwhiteY = String.fromCharCode(98,103,112,104,99,104,101,99,107,0);
      if (footballfieldG.endsWith(`${ballX}`)) {
          let textlayoutmanagerm = String.fromCharCode(122,102,114,101,101,0);
         footballfieldG = `${backwhiteY.length * textlayoutmanagerm.length}`;
      }
      if (footballfieldG != String.fromCharCode(84,0)) {
          let pauseI = String.fromCharCode(115,107,101,121,108,105,115,116,95,101,95,54,52,0);
         backwhiteY += `${footballfieldG.length / 1}`;
         pauseI += `${pauseI.length}`;
      }
      let filedS = ballX ? !ballX : ballX;
      do {
         ballX = images4;
         if (filedS) {
            break;
         }
      } while ((!reactnativeratingsS) && filedS);
          let assistR = 5.0;
          let libavutilJ = 0.0;
         backwhiteY += `${(2 + (ballX ? 3 : 2))}`;
         assistR -= parseInt(`${libavutilJ}`);
         libavutilJ /= Math.max(2, parseInt(`${assistR}`));
      while (backwhiteY.length > 5 || ballX) {
         backwhiteY = `${((reactnativeratingsS ? 5 : 4) - footballfieldG.length)}`;
         break;
      }
       let basketballawayteamg: Array<any> = [449, 588];
       let rightM: Array<any> = [704, 900, 255];
         footballfieldG += `${((images4 ? 1 : 5) + fullscreenmaxh.length)}`;
      for (let s = 0; s < 1; s++) {
         ballX = 47 > rightM.length;
      }
       let iconlogoutw = String.fromCharCode(114,116,99,119,101,98,95,107,95,57,52,0);
       let fileh = String.fromCharCode(104,105,103,104,115,0);
       let reportV = 0.0;
       let libturbomodulejsijnig = 2.0;
      while ((footballfieldG.length - 3) <= 4 && 5 <= (3 - rightM.length)) {
         footballfieldG = `${backwhiteY.length}`;
         break;
      }
      if ((reportV + 4.26) > 3.23) {
          let headerd = String.fromCharCode(112,114,101,100,101,102,105,110,101,100,0);
         ballX = !ballX;
         headerd += `${3 << (Math.min(4, headerd.length))}`;
      }
      abidetectZ -= (String.fromCharCode(49,0) == fullscreenmaxh ? footballfieldG.length : fullscreenmaxh.length);
   }
   while (2.21 <= (libyogaH.size + abidetectZ) && (libyogaH.size + parseInt(`${abidetectZ}`)) <= 1) {
      libyogaH.set(profileactivet, profileactivet.length - 3);
      break;
   }
   if (sportT.length == profileactivet.length) {
       let predictionbannersharedQ = 0.0;
       let customh = true;
       let libreactnativeblobsb = true;
       let libcrashsdkQ: Map<any, any> = new Map([[String.fromCharCode(114,101,99,111,110,105,110,116,114,97,0),String.fromCharCode(99,101,110,116,114,97,108,105,116,121,95,55,95,51,49,0)], [String.fromCharCode(114,101,112,114,101,112,97,114,101,95,98,95,54,55,0),String.fromCharCode(112,114,105,110,116,95,110,95,54,54,0)], [String.fromCharCode(105,100,101,110,116,0),String.fromCharCode(108,97,116,109,0)]]);
       let footballtrophyF = 5.0;
      while (customh || libreactnativeblobsb) {
         libreactnativeblobsb = customh;
         break;
      }
       let sigmobi = String.fromCharCode(113,100,114,97,119,0);
       let dependenciesL = String.fromCharCode(108,97,110,99,122,111,115,0);
      for (let z = 0; z < 1; z++) {
         sigmobi = `${((libreactnativeblobsb ? 1 : 4))}`;
      }
         libreactnativeblobsb = 18.93 < footballtrophyF;
      if ((1.59 / (Math.max(1, footballtrophyF))) <= 2.43 && 2 <= (3 | libcrashsdkQ.size)) {
         libcrashsdkQ.set(`${libreactnativeblobsb}`, parseInt(`${predictionbannersharedQ}`) - 2);
      }
      if (!customh) {
         libreactnativeblobsb = dependenciesL.length == 91 && !libreactnativeblobsb;
      }
          let viewsl = String.fromCharCode(109,111,110,116,104,0);
          let themeE = String.fromCharCode(102,95,57,55,95,105,100,119,116,0);
         dependenciesL += `${parseInt(`${footballtrophyF}`) * parseInt(`${predictionbannersharedQ}`)}`;
         viewsl = `${viewsl.length | themeE.length}`;
         themeE = `${themeE.length}`;
         libcrashsdkQ.set(`${predictionbannersharedQ}`, parseInt(`${predictionbannersharedQ}`) - 2);
         libreactnativeblobsb = !customh && 28.45 >= predictionbannersharedQ;
      while (3.17 > (5.47 * footballtrophyF) || (predictionbannersharedQ * 5.47) > 4.98) {
         footballtrophyF += (parseFloat(`${dependenciesL.length * (customh ? 2 : 5)}`));
         break;
      }
         dependenciesL += `${((libreactnativeblobsb ? 1 : 1) + parseInt(`${predictionbannersharedQ}`))}`;
      for (let o = 0; o < 3; o++) {
         customh = !libreactnativeblobsb || 9.80 < predictionbannersharedQ;
      }
         libcrashsdkQ.set(`${footballtrophyF}`, parseInt(`${predictionbannersharedQ}`) ^ 3);
         sigmobi = `${(String.fromCharCode(53,0) == sigmobi ? libcrashsdkQ.size : sigmobi.length)}`;
         libreactnativeblobsb = 23.28 < predictionbannersharedQ;
      profileactivet = "1";
   }
   while ((untickP / 2) >= 5.78) {
       let animationse: Map<any, any> = new Map([[String.fromCharCode(107,95,52,52,95,112,116,105,111,110,115,0),888], [String.fromCharCode(101,109,111,116,105,99,111,110,115,95,98,95,54,56,0),217]]);
       let send7: Array<any> = [117, 839];
         animationse.set(`${send7.length}`, send7.length / (Math.max(2, animationse.size)));
         send7.push(2);
         animationse = new Map([[`${animationse.size}`, send7.length * 3]]);
      for (let u = 0; u < 2; u++) {
         send7 = [2 << (Math.min(3, send7.length))];
      }
       let manifestK = true;
         animationse.set(`${send7.length}`, 1 - send7.length);
      abidetectZ -= 2;
      break;
   }
      classes2.push(parseInt(`${untickP}`) >> (Math.min(graphp.length, 1)));
   let homeloadingF = 5850330 <= nalyticse.length;
   do {
       let auto__5 = 2;
       let videobufferloadingJ = String.fromCharCode(97,114,114,0);
       let carouselk = 4;
      let submitA = String.fromCharCode(122,54,115,109,121,0) == videobufferloadingJ;
      do {
         videobufferloadingJ = `${(String.fromCharCode(86,0) == videobufferloadingJ ? carouselk : videobufferloadingJ.length)}`;
         if (submitA) {
            break;
         }
      } while ((videobufferloadingJ.includes(`${auto__5}`)) && submitA);
         videobufferloadingJ += "2";
      if (4 > (videobufferloadingJ.length << (Math.min(Math.abs(4), 1)))) {
         videobufferloadingJ = `${auto__5 / (Math.max(9, videobufferloadingJ.length))}`;
      }
      let hooksG = String.fromCharCode(108,103,51,115,97,110,118,114,110,0) == videobufferloadingJ;
      do {
         videobufferloadingJ = "1";
         if (hooksG) {
            break;
         }
      } while (hooksG && (videobufferloadingJ.endsWith(`${carouselk}`)));
      let catagoryg = 9039679 >= videobufferloadingJ.length;
      do {
          let yingW: Map<any, any> = new Map([[String.fromCharCode(97,116,116,97,99,104,95,99,95,51,56,0),String.fromCharCode(103,95,55,54,95,115,109,111,111,116,104,110,101,115,115,0)], [String.fromCharCode(112,97,103,101,0),String.fromCharCode(112,105,120,101,108,115,120,95,106,95,57,50,0)]]);
          let dycreator7: Array<any> = [856, 131, 697];
         videobufferloadingJ = `${yingW.size}`;
         yingW = new Map([[`${dycreator7.length}`, 1 >> (Math.min(5, dycreator7.length))]]);
         if (catagoryg) {
            break;
         }
      } while (catagoryg && (!videobufferloadingJ.includes(`${auto__5}`)));
      let leaguedetailsbgs = auto__5 <= 6266472;
      do {
         auto__5 /= Math.max(2, 2);
         if (leaguedetailsbgs) {
            break;
         }
      } while (((videobufferloadingJ.length << (Math.min(Math.abs(1), 2))) > 3 && 1 > (videobufferloadingJ.length << (Math.min(2, Math.abs(auto__5))))) && leaguedetailsbgs);
      let trashd = 9738895 <= auto__5;
      do {
          let defaultprofilepicN = String.fromCharCode(103,114,97,121,115,99,97,108,101,0);
          let logout4 = String.fromCharCode(109,97,112,115,0);
         auto__5 &= 1;
         defaultprofilepicN = `${defaultprofilepicN.length}`;
         logout4 = `${defaultprofilepicN.length}`;
         if (trashd) {
            break;
         }
      } while ((2 < auto__5) && trashd);
         auto__5 >>= Math.min(2, Math.abs((videobufferloadingJ == String.fromCharCode(65,0) ? carouselk : videobufferloadingJ.length)));
      for (let k = 0; k < 2; k++) {
          let ballXy = 5;
          let iconarrowrightwhite8 = 1.0;
         carouselk -= videobufferloadingJ.length;
         ballXy *= 2;
         iconarrowrightwhite8 += parseFloat(`${2}`);
      }
      nalyticse = `${2 | graphp.length}`;
      if (homeloadingF) {
         break;
      }
   } while (homeloadingF && (3 == (libyogaH.size & 5) && 2 == (5 & libyogaH.size)));
       let playZ = 1;
      for (let q = 0; q < 1; q++) {
          let mbridgeF: Array<any> = [831, 666, 709];
          let orangeE = String.fromCharCode(109,105,100,115,0);
          let reactnavigationO = 0.0;
         playZ &= orangeE.length % 3;
         mbridgeF.push(parseInt(`${reactnavigationO}`));
         orangeE = `${3 * mbridgeF.length}`;
         reactnavigationO *= parseInt(`${reactnavigationO}`) * mbridgeF.length;
      }
      if (2 <= (5 + playZ)) {
         playZ >>= Math.min(4, Math.abs(playZ));
      }
         playZ &= playZ;
      sportT = "1";
   for (let n = 0; n < 2; n++) {
      libyogaH.set(profileactivet, profileactivet.length);
   }
    dispatch(hideAdultModeDisclaimer());
  }, [])

  const handlePressIndicator = useCallback(() => {
       let traminid: Array<any> = [809, 966];
    let dependencyw: Array<any> = [851, 227, 593];
    let episodeQ = 2;
    let liveshare7 = 3.0;
    let libapminsightbm = 4.0;
    let greytickZ = 2;
    let hometeamfieldO = true;
    let klevinu = false;
    let pangle9: Map<any, any> = new Map([[String.fromCharCode(106,95,49,50,95,115,111,98,105,110,100,0),706], [String.fromCharCode(115,108,105,112,112,97,103,101,0),974]]);
    let nterstitialB = String.fromCharCode(102,117,116,117,114,101,95,99,95,55,57,0);
    let shielddoneY: Array<any> = [String.fromCharCode(99,110,97,109,101,0), String.fromCharCode(116,104,101,116,97,0), String.fromCharCode(118,105,100,101,111,114,101,110,100,101,114,101,114,95,121,95,49,54,0)];
    let sliderc = 3;
    let yellowN = 3.0;
    let info4 = String.fromCharCode(112,114,101,115,101,110,116,97,116,105,111,110,115,0);
    let memberr = 2.0;
      libapminsightbm *= parseFloat(`${1 * episodeQ}`);
   for (let a = 0; a < 3; a++) {
      libapminsightbm *= parseFloat(`${nterstitialB.length / (Math.max(3, parseInt(`${liveshare7}`)))}`);
   }
   while (3.58 < (2.51 * libapminsightbm)) {
      episodeQ ^= pangle9.size << (Math.min(Math.abs(2), 1));
      break;
   }
      hometeamfieldO = 83.71 < liveshare7 && pangle9.size < 52;
   if (!Array.from(pangle9.values()).includes(greytickZ)) {
      pangle9 = new Map([[`${hometeamfieldO}`, 2 - parseInt(`${liveshare7}`)]]);
   }
   while ((pangle9.size / 1) > 3) {
      hometeamfieldO = traminid.length == dependencyw.length;
      break;
   }
      episodeQ *= parseInt(`${libapminsightbm}`) - 1;
   while (!klevinu) {
      dependencyw = [traminid.length];
      break;
   }
   for (let a = 0; a < 2; a++) {
      libapminsightbm += parseFloat(`${nterstitialB.length}`);
   }
   if (libapminsightbm == 5.13 || (5.13 + libapminsightbm) == 5.4) {
      klevinu = !klevinu;
   }
   if (liveshare7 <= episodeQ) {
       let incidentI = String.fromCharCode(97,95,55,56,95,103,97,109,109,97,118,97,108,0);
       let iconcalendaro = 0.0;
       let hometeamfieldn = String.fromCharCode(118,95,53,95,114,97,105,116,0);
       let rewindL: Array<any> = [649, 360];
       let launch4 = 0.0;
       let selectedo = 0.0;
      let middlewareG = 7194382.0 <= selectedo;
      do {
          let configures = true;
          let expandJ = true;
          let downarrowo = 0;
         selectedo += parseFloat(`${parseInt(`${launch4}`)}`);
         configures = downarrowo < 41 || !expandJ;
         expandJ = downarrowo >= 72;
         if (middlewareG) {
            break;
         }
      } while (middlewareG && ((hometeamfieldn.length * parseInt(`${selectedo}`)) > 1));
      let checkboxM = launch4 >= 6132035.0;
      do {
          let i_managerV: Array<any> = [171, 864, 318];
          let sinaa: Map<any, any> = new Map([[String.fromCharCode(97,99,99,101,112,116,0),73], [String.fromCharCode(98,116,110,0),648], [String.fromCharCode(105,95,56,53,95,116,114,105,112,108,101,0),331]]);
          let bodan9 = String.fromCharCode(115,116,114,108,105,107,101,0);
          let anythinkS = false;
          let slider1 = String.fromCharCode(99,97,108,108,98,97,99,107,115,0);
         launch4 /= Math.max(parseFloat(`${3}`), 5);
         i_managerV = [((anythinkS ? 4 : 3) ^ slider1.length)];
         sinaa.set(bodan9, bodan9.length + i_managerV.length);
         anythinkS = sinaa.size >= 10;
         slider1 = `${i_managerV.length}`;
         if (checkboxM) {
            break;
         }
      } while ((launch4 == 4.43) && checkboxM);
      if ((rewindL.length / 1) > 4 && (1 / (Math.max(3, incidentI.length))) > 2) {
         rewindL = [parseInt(`${iconcalendaro}`) * 3];
      }
      let moviesI = 9139681.0 <= launch4;
      do {
         launch4 -= (parseFloat(`${String.fromCharCode(70,0) == hometeamfieldn ? hometeamfieldn.length : parseInt(`${iconcalendaro}`)}`));
         if (moviesI) {
            break;
         }
      } while (moviesI && (5.40 > (iconcalendaro - launch4) && (5.40 - launch4) > 5.12));
      while (4.28 == selectedo) {
         launch4 *= parseFloat(`${3}`);
         break;
      }
       let dicey = String.fromCharCode(111,95,53,50,95,114,101,112,0);
       let hooksP = String.fromCharCode(115,105,110,101,0);
      while (hooksP == String.fromCharCode(116,0)) {
         hometeamfieldn += `${hooksP.length << (Math.min(Math.abs(2), 2))}`;
         break;
      }
      while (hooksP.startsWith(`${launch4}`)) {
         hooksP += `${(incidentI == String.fromCharCode(118,0) ? incidentI.length : parseInt(`${selectedo}`))}`;
         break;
      }
       let viewsZ = String.fromCharCode(98,105,110,115,95,122,95,49,57,0);
       let nextx = String.fromCharCode(97,108,111,99,0);
         hooksP = `${parseInt(`${iconcalendaro}`) * 2}`;
          let predictionbannersharedc = 2.0;
         rewindL = [hometeamfieldn.length];
         predictionbannersharedc -= parseFloat(`${1 + parseInt(`${predictionbannersharedc}`)}`);
      liveshare7 /= Math.max(3, parseFloat(`${3}`));
   }
      libapminsightbm /= Math.max((parseFloat(`${shielddoneY.length | (hometeamfieldO ? 1 : 5)}`)), 3);
       let bottomT = String.fromCharCode(99,111,109,112,105,108,101,95,109,95,53,48,0);
       let filedI = 0.0;
       let giftr = 5.0;
      for (let u = 0; u < 2; u++) {
         bottomT += "2";
      }
          let sharewhiteN: Array<any> = [60, 766];
          let animationI = String.fromCharCode(97,99,111,108,111,114,0);
          let verticalT: Map<any, any> = new Map([[String.fromCharCode(111,110,101,116,105,109,101,97,117,116,104,95,56,95,52,48,0),313], [String.fromCharCode(111,95,49,57,95,103,114,97,112,104,105,99,0),363]]);
         filedI -= parseInt(`${giftr}`) >> (Math.min(Math.abs(verticalT.size), 2));
         sharewhiteN.push(3);
         animationI = `${sharewhiteN.length}`;
         verticalT = new Map([[`${sharewhiteN.length}`, 1 * animationI.length]]);
         bottomT = `${parseInt(`${filedI}`) / (Math.max(3, 9))}`;
      for (let l = 0; l < 3; l++) {
         giftr *= parseInt(`${giftr}`) & 3;
      }
      let lightP = 5937237.0 >= filedI;
      do {
         filedI -= 1 / (Math.max(parseInt(`${giftr}`), 4));
         if (lightP) {
            break;
         }
      } while ((1.55 <= (giftr * 1)) && lightP);
      for (let o = 0; o < 2; o++) {
          let libmapbufferjnir = String.fromCharCode(116,95,51,55,95,120,114,101,115,0);
         filedI *= bottomT.length << (Math.min(libmapbufferjnir.length, 4));
      }
      for (let p = 0; p < 3; p++) {
          let debugF = String.fromCharCode(121,117,118,112,116,111,121,117,121,95,100,95,52,56,0);
          let launcher_ = true;
          let flipperx = 5.0;
         bottomT = `${parseInt(`${flipperx}`) ^ 2}`;
         debugF += `${((launcher_ ? 4 : 1))}`;
         launcher_ = debugF.length <= 75 && launcher_;
         flipperx += parseFloat(`${debugF.length}`);
      }
       let areaQ = 2;
      while (!bottomT.startsWith(`${giftr}`)) {
         bottomT += "1";
         break;
      }
      traminid.push(episodeQ & pangle9.size);
   if ((4 | traminid.length) == 1) {
      nterstitialB += `${dependencyw.length / (Math.max(9, shielddoneY.length))}`;
   }
   let analytic4 = 8540545 <= traminid.length;
   do {
      traminid = [((hometeamfieldO ? 1 : 1) >> (Math.min(Math.abs(pangle9.size), 5)))];
      if (analytic4) {
         break;
      }
   } while (((traminid.length + parseInt(`${liveshare7}`)) >= 5) && analytic4);
      greytickZ |= 3;
      episodeQ >>= Math.min(Math.abs(2 / (Math.max(2, parseInt(`${liveshare7}`)))), 3);
   while (3 == (nterstitialB.length << (Math.min(Math.abs(1), 3))) && (pangle9.size << (Math.min(nterstitialB.length, 2))) == 1) {
      pangle9 = new Map([[`${hometeamfieldO}`, (1 | (hometeamfieldO ? 3 : 1))]]);
      break;
   }
       let airbnbstart: Array<any> = [166, 382, 799];
       let minil = 4.0;
         airbnbstart.push(parseInt(`${minil}`));
         airbnbstart = [parseInt(`${minil}`)];
         airbnbstart = [3 >> (Math.min(Math.abs(parseInt(`${minil}`)), 1))];
          let chinaV = 0;
         minil -= 2 + parseInt(`${minil}`);
         chinaV >>= Math.min(Math.abs(chinaV | 2), 1);
      for (let l = 0; l < 1; l++) {
         minil += 3 << (Math.min(5, airbnbstart.length));
      }
       let codegenz = 3;
       let modelsp = 1;
      traminid.push(parseInt(`${minil}`) | 3);
      episodeQ |= traminid.length ^ 2;
   for (let k = 0; k < 2; k++) {
      libapminsightbm -= parseFloat(`${traminid.length}`);
   }

    dispatch(showAdultVipPrivilegeMiniVideoAction())
  }, [])

  const handleOnClose = useCallback(() => {
       let corner_ = String.fromCharCode(97,95,53,54,95,100,105,97,109,111,110,100,0);
    let chatH = String.fromCharCode(97,116,116,105,98,117,116,101,95,54,95,50,56,0);
    let libapminsightab = String.fromCharCode(121,95,53,48,95,104,111,115,116,110,97,109,101,0);
    let heji4 = String.fromCharCode(99,104,101,118,114,111,110,95,107,95,53,54,0);
    let iconviewergifi = false;
    let schedulec: Array<any> = [791, 587, 533];
    let assistU: Map<any, any> = new Map([[String.fromCharCode(120,99,111,100,101,0),716], [String.fromCharCode(115,95,53,50,95,104,105,103,104,105,103,104,116,0),244], [String.fromCharCode(101,103,114,101,115,115,95,122,95,52,55,0),748]]);
    let banneru = 4.0;
    let baiduG = 1;
    let heji0 = String.fromCharCode(98,95,56,55,95,112,104,97,115,101,0);
    let upload4 = 4;
    let chinasameo: Array<any> = [399, 582, 48];
    let black8 = String.fromCharCode(97,104,101,97,100,95,113,95,57,52,0);
    let penaltyshootnogoale: Map<any, any> = new Map([[String.fromCharCode(101,110,100,112,111,105,110,116,115,0),893], [String.fromCharCode(109,117,108,116,105,112,108,101,120,101,100,95,52,95,54,57,0),565]]);
    let forwardT = String.fromCharCode(115,116,114,105,115,116,97,114,116,0);
       let libfiled = 5.0;
       let iconbackwhiteg = true;
          let defaultplayerimgc = 4;
          let loginz: Array<any> = [195, 97];
          let sellmathbackgroundl = false;
         libfiled += (2 ^ (iconbackwhiteg ? 2 : 5));
         defaultplayerimgc *= loginz.length >> (Math.min(Math.abs(2), 1));
         loginz = [loginz.length % (Math.max(2, defaultplayerimgc))];
         sellmathbackgroundl = loginz.includes(defaultplayerimgc);
         libfiled *= (parseInt(`${libfiled}`) + (iconbackwhiteg ? 5 : 2));
         libfiled += ((iconbackwhiteg ? 5 : 1) - parseInt(`${libfiled}`));
          let gestureg = 0.0;
         libfiled += parseInt(`${gestureg}`);
         libfiled /= Math.max(parseInt(`${libfiled}`) ^ 2, 2);
      for (let x = 0; x < 3; x++) {
          let corea = String.fromCharCode(109,97,115,107,115,0);
          let basketbally: Array<any> = [551, 209];
          let faviconb = 1;
         libfiled *= 3 | basketbally.length;
         corea += `${faviconb}`;
         basketbally = [corea.length];
         faviconb %= Math.max(3, corea.length);
      }
      assistU.set(heji4, baiduG | 1);
   if (assistU.get(`${schedulec.length}`) == null) {
      schedulec = [corner_.length];
   }
   let downarrowc = assistU.size >= 9130718;
   do {
       let orientation0: Map<any, any> = new Map([[String.fromCharCode(99,104,101,99,107,112,97,99,107,101,116,95,114,95,51,0),887], [String.fromCharCode(119,105,110,100,111,119,115,95,122,95,52,48,0),479]]);
       let trophyD = 1;
       let completez = 0.0;
       let animationa = true;
         orientation0 = new Map([[`${orientation0.size}`, parseInt(`${completez}`) % 3]]);
      while ((2 | trophyD) <= 4) {
         trophyD /= Math.max(2, 3);
         break;
      }
         animationa = orientation0.size >= parseInt(`${completez}`);
      let rewardp = animationa ? !animationa : animationa;
      do {
         animationa = trophyD < parseInt(`${completez}`);
         if (rewardp) {
            break;
         }
      } while (rewardp && (1 >= trophyD || (trophyD & 1) >= 5));
         trophyD ^= orientation0.size;
          let largesound6 = 5.0;
          let topict = String.fromCharCode(109,101,114,103,101,100,95,54,95,55,50,0);
          let kickO = String.fromCharCode(102,105,108,116,95,109,95,57,48,0);
         orientation0 = new Map([[`${animationa}`, 3 * parseInt(`${largesound6}`)]]);
         largesound6 /= Math.max(1, parseFloat(`${3}`));
         topict += `${topict.length & kickO.length}`;
         kickO = `${topict.length}`;
         trophyD += 1;
      let release_eus = 6407719 <= trophyD;
      do {
          let carousel1 = String.fromCharCode(100,111,109,97,105,110,0);
         trophyD <<= Math.min(5, Math.abs(orientation0.size));
         carousel1 += `${carousel1.length}`;
         if (release_eus) {
            break;
         }
      } while (release_eus && ((orientation0.size + 2) == 2));
      let libjsinspectorZ = 7393845.0 >= completez;
      do {
         completez *= parseFloat(`${3}`);
         if (libjsinspectorZ) {
            break;
         }
      } while ((5.68 > completez) && libjsinspectorZ);
         animationa = 8.4 < completez;
      for (let s = 0; s < 2; s++) {
         animationa = !animationa;
      }
      if (animationa) {
         animationa = completez <= 24.91;
      }
      assistU = new Map([[`${assistU.size}`, assistU.size]]);
      if (downarrowc) {
         break;
      }
   } while ((assistU.size < 3) && downarrowc);
   for (let i = 0; i < 1; i++) {
       let product8 = true;
       let readf = false;
          let j_unlock9 = 5.0;
          let usernamee = String.fromCharCode(113,95,49,52,95,115,105,98,108,105,110,103,115,0);
         product8 = !readf;
         j_unlock9 *= parseFloat(`${1 / (Math.max(7, parseInt(`${j_unlock9}`)))}`);
         usernamee = `${usernamee.length}`;
      if (!readf) {
         readf = !product8;
      }
      if (readf) {
         readf = !product8 && !readf;
      }
      if (product8) {
         readf = (readf ? !product8 : !readf);
      }
         readf = !product8;
          let phoneshareo: Array<any> = [500, 885];
          let thailandp = String.fromCharCode(100,111,117,98,108,105,110,103,0);
         product8 = (44 >= ((!product8 ? 44 : thailandp.length) * thailandp.length));
         phoneshareo.push(1 >> (Math.min(2, phoneshareo.length)));
      heji0 = `${(corner_.length + (product8 ? 5 : 2))}`;
   }
   for (let v = 0; v < 3; v++) {
      heji4 = `${corner_.length ^ chatH.length}`;
   }
   if (libapminsightab.endsWith(`${assistU.size}`)) {
      assistU.set(`${iconviewergifi}`, schedulec.length);
   }
   while (4 >= assistU.size) {
       let cornerkickP = String.fromCharCode(112,97,105,114,115,0);
      while (cornerkickP != cornerkickP) {
         cornerkickP = `${2 + cornerkickP.length}`;
         break;
      }
      while (cornerkickP == cornerkickP) {
         cornerkickP += `${cornerkickP.length}`;
         break;
      }
      let basketballmatchdetailbgp = 6944282 >= cornerkickP.length;
      do {
          let humidityu = String.fromCharCode(109,97,107,101,116,97,114,98,97,108,108,0);
         cornerkickP = `${2 - humidityu.length}`;
         if (basketballmatchdetailbgp) {
            break;
         }
      } while ((cornerkickP != String.fromCharCode(74,0)) && basketballmatchdetailbgp);
      assistU = new Map([[cornerkickP, cornerkickP.length]]);
      break;
   }
   while (!iconviewergifi || 1 > (schedulec.length >> (Math.min(Math.abs(4), 4)))) {
       let iconbackwhiteD = 5;
       let alertx = 4.0;
       let ecopy_5H = String.fromCharCode(117,110,118,111,116,101,95,118,95,49,51,0);
       let about1 = true;
          let detailK = String.fromCharCode(116,111,120,121,122,0);
         ecopy_5H += `${parseInt(`${alertx}`) ^ ecopy_5H.length}`;
         detailK += "1";
      while ((4 << (Math.min(5, Math.abs(iconbackwhiteD)))) == 3 || (ecopy_5H.length << (Math.min(Math.abs(4), 2))) == 5) {
         ecopy_5H = `${iconbackwhiteD}`;
         break;
      }
      if (3 <= ecopy_5H.length && about1) {
          let emojil = true;
          let launcherB = 4.0;
          let libflipperJ: Array<any> = [925, 325];
          let update_i7 = 3.0;
          let humidityx = String.fromCharCode(100,101,99,111,114,97,116,111,114,115,0);
         ecopy_5H = `${parseInt(`${alertx}`) - ecopy_5H.length}`;
         emojil = 71.43 <= launcherB;
         launcherB -= parseFloat(`${parseInt(`${launcherB}`) >> (Math.min(3, Math.abs(3)))}`);
         libflipperJ.push(3 % (Math.max(parseInt(`${update_i7}`), 3)));
         update_i7 += parseFloat(`${parseInt(`${launcherB}`) - 2}`);
         humidityx += "2";
      }
      for (let n = 0; n < 1; n++) {
         alertx += parseInt(`${alertx}`) + 1;
      }
      let awayteamfieldp = String.fromCharCode(102,121,104,100,117,50,0) == ecopy_5H;
      do {
         ecopy_5H += `${parseInt(`${alertx}`) | 1}`;
         if (awayteamfieldp) {
            break;
         }
      } while (awayteamfieldp && (!ecopy_5H.includes(`${alertx}`)));
      if (parseInt(`${alertx}`) >= ecopy_5H.length) {
          let grayq: Map<any, any> = new Map([[String.fromCharCode(97,118,114,101,115,97,109,112,108,101,114,101,115,0),858], [String.fromCharCode(116,111,103,103,108,101,95,97,95,52,53,0),417], [String.fromCharCode(117,110,109,97,115,107,95,48,95,50,51,0),845]]);
          let rankZ = String.fromCharCode(99,104,101,99,107,108,105,110,101,0);
          let sharewhiteA = String.fromCharCode(99,97,114,116,0);
         alertx += rankZ.length;
         grayq = new Map([[`${grayq.size}`, sharewhiteA.length]]);
         rankZ += `${grayq.size << (Math.min(Math.abs(1), 5))}`;
         sharewhiteA = `${(sharewhiteA == String.fromCharCode(98,0) ? grayq.size : sharewhiteA.length)}`;
      }
         iconbackwhiteD |= 3 / (Math.max(parseInt(`${alertx}`), 5));
      for (let o = 0; o < 3; o++) {
         ecopy_5H += `${((about1 ? 1 : 4))}`;
      }
      while (5 <= (ecopy_5H.length * parseInt(`${alertx}`)) && (5 * parseInt(`${alertx}`)) <= 5) {
          let dycreatoro = 0.0;
         alertx -= parseInt(`${dycreatoro}`) >> (Math.min(4, Math.abs(1)));
         break;
      }
      if (5 > (3 / (Math.max(5, iconbackwhiteD)))) {
         iconbackwhiteD %= Math.max(3, parseInt(`${alertx}`));
      }
         about1 = ecopy_5H.startsWith(`${about1}`);
         iconbackwhiteD &= iconbackwhiteD;
      schedulec = [iconbackwhiteD % (Math.max(2, 3))];
      break;
   }
   let loading8 = heji4 == String.fromCharCode(95,97,99,54,118,99,0);
   do {
      heji4 = `${1 >> (Math.min(5, Math.abs(parseInt(`${banneru}`))))}`;
      if (loading8) {
         break;
      }
   } while (loading8 && (baiduG >= 5));
   while (3.33 > banneru && (3.33 - banneru) > 4.85) {
      banneru -= parseFloat(`${libapminsightab.length}`);
      break;
   }
   if (1 > (5 & assistU.size)) {
      schedulec = [(String.fromCharCode(114,0) == libapminsightab ? parseInt(`${banneru}`) : libapminsightab.length)];
   }
       let liveshareU: Array<any> = [558, 961];
       let malaysia4 = 3.0;
       let clock3 = String.fromCharCode(114,101,108,97,116,105,118,101,108,121,0);
       let scorepopsoundL = false;
         clock3 = "3";
         malaysia4 += parseInt(`${malaysia4}`) | clock3.length;
      for (let o = 0; o < 2; o++) {
         clock3 = `${(clock3 == String.fromCharCode(75,0) ? clock3.length : (scorepopsoundL ? 5 : 3))}`;
      }
          let mapbufferS = String.fromCharCode(115,117,112,112,111,114,116,97,98,108,101,95,51,95,49,56,0);
          let j_managerS: Array<any> = [267, 691, 22];
         malaysia4 *= 3;
         mapbufferS = `${j_managerS.length >> (Math.min(mapbufferS.length, 3))}`;
         j_managerS.push(mapbufferS.length << (Math.min(5, j_managerS.length)));
          let mnewarchdefaults3 = String.fromCharCode(97,115,115,112,111,114,116,0);
          let expandX = String.fromCharCode(122,95,54,52,95,108,105,98,119,101,98,112,101,110,99,0);
         malaysia4 /= Math.max(4, (mnewarchdefaults3 == String.fromCharCode(55,0) ? mnewarchdefaults3.length : liveshareU.length));
         expandX = `${1 % (Math.max(4, expandX.length))}`;
         clock3 += `${(clock3 == String.fromCharCode(90,0) ? liveshareU.length : clock3.length)}`;
       let bellX = String.fromCharCode(97,99,97,108,99,0);
       let videovar0 = String.fromCharCode(116,114,97,118,101,114,115,101,0);
         videovar0 += `${bellX.length}`;
      iconviewergifi = heji4.length == 15;
       let sheetb = 2.0;
       let paginationu = 2;
       let robotov: Map<any, any> = new Map([[String.fromCharCode(99,111,110,118,101,114,115,105,111,110,0),49], [String.fromCharCode(118,95,53,52,95,99,102,101,110,99,115,116,114,0),50], [String.fromCharCode(112,105,116,99,104,0),843]]);
      let dialogr = paginationu <= 8896930;
      do {
         paginationu *= parseInt(`${sheetb}`) - 2;
         if (dialogr) {
            break;
         }
      } while ((1.98 <= sheetb) && dialogr);
         robotov = new Map([[`${robotov.size}`, parseInt(`${sheetb}`)]]);
      while (!Array.from(robotov.values()).includes(sheetb)) {
         sheetb += 2;
         break;
      }
          let reminder3 = 2.0;
         robotov = new Map([[`${robotov.size}`, parseInt(`${sheetb}`) | 1]]);
         reminder3 /= Math.max(3, parseInt(`${reminder3}`) - parseInt(`${reminder3}`));
         sheetb /= Math.max(2, parseInt(`${sheetb}`));
         sheetb += 1;
         paginationu %= Math.max(3, 2);
         robotov.set(`${sheetb}`, parseInt(`${sheetb}`));
      while (1 > (paginationu << (Math.min(Math.abs(2), 4))) && (paginationu << (Math.min(Math.abs(2), 4))) > 4) {
          let linkA: Map<any, any> = new Map([[String.fromCharCode(117,116,118,105,100,101,111,0),586], [String.fromCharCode(97,114,116,105,102,97,99,116,0),319], [String.fromCharCode(108,97,98,101,108,0),562]]);
          let statsnomoredatac = 5;
          let abidetectS: Map<any, any> = new Map([[String.fromCharCode(101,115,116,105,109,97,116,105,110,103,95,120,95,51,57,0),183], [String.fromCharCode(109,105,100,101,113,117,97,108,105,122,101,114,0),567], [String.fromCharCode(116,114,97,110,115,105,116,105,111,110,0),307]]);
          let langkeyN = String.fromCharCode(99,121,99,108,105,99,95,54,95,51,52,0);
         paginationu %= Math.max(5, 1 >> (Math.min(5, Math.abs(parseInt(`${sheetb}`)))));
         linkA.set(`${statsnomoredatac}`, 2 & statsnomoredatac);
         abidetectS.set(langkeyN, langkeyN.length % 2);
         break;
      }
      corner_ += `${schedulec.length % (Math.max(5, robotov.size))}`;
   if (1 <= (heji4.length * 4) || (heji4.length >> (Math.min(Math.abs(4), 3))) <= 3) {
       let icontransferclubr = false;
       let yellowscoreball_: Array<any> = [47, 601, 444];
      let accepted9 = icontransferclubr ? !icontransferclubr : icontransferclubr;
      do {
         icontransferclubr = yellowscoreball_.length > 53 && icontransferclubr;
         if (accepted9) {
            break;
         }
      } while ((!icontransferclubr) && accepted9);
      let imagemanagerg = icontransferclubr ? !icontransferclubr : icontransferclubr;
      do {
         icontransferclubr = yellowscoreball_.length > 13;
         if (imagemanagerg) {
            break;
         }
      } while (imagemanagerg && (5 == yellowscoreball_.length || (yellowscoreball_.length / 5) == 3));
         yellowscoreball_ = [yellowscoreball_.length];
      let action0 = icontransferclubr ? !icontransferclubr : icontransferclubr;
      do {
          let related5 = String.fromCharCode(101,95,53,57,0);
          let progressv = String.fromCharCode(108,97,110,100,115,99,97,112,101,0);
          let awayicon2: Array<any> = [String.fromCharCode(121,95,54,95,97,99,107,115,0), String.fromCharCode(103,97,105,110,0)];
          let stringO = String.fromCharCode(117,110,114,101,97,100,95,115,95,55,50,0);
          let fastf = String.fromCharCode(117,112,99,97,115,101,0);
         icontransferclubr = (((!icontransferclubr ? 8 : yellowscoreball_.length) / (Math.max(yellowscoreball_.length, 3))) >= 8);
         related5 += `${progressv.length}`;
         progressv = `${stringO.length}`;
         awayicon2 = [progressv.length ^ 1];
         stringO = `${fastf.length}`;
         fastf += `${(stringO == String.fromCharCode(118,0) ? awayicon2.length : stringO.length)}`;
         if (action0) {
            break;
         }
      } while (((yellowscoreball_.length ^ 1) < 4 && !icontransferclubr) && action0);
         icontransferclubr = (((!icontransferclubr ? yellowscoreball_.length : 27) | yellowscoreball_.length) > 27);
      while (yellowscoreball_.length <= 1) {
         yellowscoreball_.push((yellowscoreball_.length * (icontransferclubr ? 2 : 1)));
         break;
      }
      banneru *= parseFloat(`${1}`);
   }
   if (schedulec.length <= heji0.length) {
      schedulec = [2];
   }

    dispatch(hideAdultVipPrivilegeMiniVideoAction())

   for (let f = 0; f < 2; f++) {
      black8 = `${schedulec.length}`;
   }
      assistU.set(black8, 2);
      heji0 = `${black8.length + upload4}`;
   if (2 > (2 & chinasameo.length)) {
       let skipU = 3.0;
       let expando: Array<any> = [73, 527, 336];
       let defaultplayerimgv = String.fromCharCode(117,116,120,111,0);
      for (let u = 0; u < 2; u++) {
         skipU += parseFloat(`${expando.length}`);
      }
      while (1 > expando.length) {
         defaultplayerimgv = `${expando.length ^ 2}`;
         break;
      }
      for (let j = 0; j < 1; j++) {
         skipU -= parseFloat(`${1 | expando.length}`);
      }
      let regengH = expando.length >= 6299616;
      do {
         expando.push((defaultplayerimgv == String.fromCharCode(108,0) ? defaultplayerimgv.length : expando.length));
         if (regengH) {
            break;
         }
      } while (regengH && (1 > (expando.length | 1)));
      while (defaultplayerimgv.length == expando.length) {
         defaultplayerimgv = "1";
         break;
      }
      if (5.94 < skipU) {
         defaultplayerimgv += `${expando.length}`;
      }
      for (let u = 0; u < 3; u++) {
         expando.push((String.fromCharCode(98,0) == defaultplayerimgv ? defaultplayerimgv.length : parseInt(`${skipU}`)));
      }
      while (expando.length <= 4) {
          let e_lockx = String.fromCharCode(115,97,100,98,0);
         expando = [e_lockx.length];
         break;
      }
      let clearh = expando.length >= 8238455;
      do {
          let usernameI = String.fromCharCode(114,101,112,114,101,115,101,110,116,97,98,108,101,95,111,95,54,48,0);
          let fast7 = String.fromCharCode(97,110,115,119,101,114,0);
         expando.push(fast7.length & 2);
         usernameI = `${usernameI.length}`;
         fast7 = `${usernameI.length}`;
         if (clearh) {
            break;
         }
      } while (clearh && ((skipU - 5.3) < 4.57 && 4 < (5 & expando.length)));
      chinasameo.push(chinasameo.length);
   }
      heji0 = `${schedulec.length}`;
   if (schedulec.includes(baiduG)) {
      schedulec = [2 << (Math.min(Math.abs(baiduG), 3))];
   }
      assistU.set(`${banneru}`, parseInt(`${banneru}`) + 3);
      penaltyshootnogoale.set(black8, ((iconviewergifi ? 1 : 5) ^ 1));
      banneru /= Math.max(1, parseFloat(`${1 >> (Math.min(1, Math.abs(assistU.size)))}`));
      heji4 = `${libapminsightab.length}`;
   let arrowupG = penaltyshootnogoale.size >= 5106869;
   do {
      penaltyshootnogoale = new Map([[libapminsightab, 1 & libapminsightab.length]]);
      if (arrowupG) {
         break;
      }
   } while (arrowupG && (3 < (5 | penaltyshootnogoale.size) && iconviewergifi));
   let sentryE = chatH.length <= 9065107;
   do {
      chatH += `${parseInt(`${banneru}`) + 3}`;
      if (sentryE) {
         break;
      }
   } while ((chatH == String.fromCharCode(74,0) || corner_ != String.fromCharCode(102,0)) && sentryE);
      heji0 = `${1 ^ black8.length}`;
      libapminsightab += `${heji0.length | black8.length}`;
   if (!iconviewergifi || 1 <= (4 - upload4)) {
      iconviewergifi = String.fromCharCode(51,0) == heji0;
   }
    console.debug('close!!!')
  }, [])

  return (
    <View
      style={{
        ...styles.container,
        
        height: showAdultVipPrivilegeMiniVideo ? '100%' : 'auto'
      }}
    >
      {/* {watchAnytimeAdultMode && <WatchAnytimeVipModal />} */}
      {(screenState.showAdultTab) && (
        <AdultModeSwitch switchStyle={styles.switch} />
      )}
      {watchAnytimeAdultMode && !isVip && !showAdultVipPrivilegeMiniVideo &&
        <VipPrivilegeFloatingIndicator
          text1="升级VIP"
          text2="享更多福利视频 》"
          containerStyle={{
            position: "absolute",
            top: 70,
            right: 0,
            flex: 1,
          }}
          onPress={handlePressIndicator}
        />

      }
      {/* {watchAnytimeAdultMode && (UMENG_CHANNEL != "GOOGLE_PLAY" || Platform.OS === "ios" )&& (
        <AdultModeCountdownIndicator
          containerStyle={{
            position: "absolute",
            top: 70,
            right: 20,
            flex: 1,
          }}
        />
      )} */}
      {/* <EighteenPlusOverlay
        handleAccept={handleAccept}
        handleReject={handleReject}
      /> */}
      <AdultVipPrivilegeOverlay
        showCondition={showAdultVipPrivilegeMiniVideo}
        onClose={handleOnClose}
        showBlur={true}
      />
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    zIndex: 100,
    width: "100%",
    position: "absolute",
  },
  switch: {
    position: "absolute",
    top: 25,
    right: 20,
  },
});

export default eighteenPlusControls;
