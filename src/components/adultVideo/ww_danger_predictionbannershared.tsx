import React, { useCallback } from "react"
import { View, Text, TouchableOpacity } from "react-native"
import LinearGradient from "react-native-linear-gradient"
import VipIcon from '@static/images/selectedDefaultplayerimgAnner.svg'
import MoreArrow from '@static/images/libcxxcomponentsPrivilege.svg'
import { useAppDispatch, useAppSelector, useSelector } from "@hooks/ww_catagory_neon"
import { showAdultModeVip } from "@redux/actions/ww_hash"
import { useNavigation, useTheme } from "@react-navigation/native"
import { wwVertical } from "@redux/reducers/ww_related_kuaishou"
import { wwBodan } from "@models/ww_liveendmodallogo_awayteamfield"


const VipRegisterBar = ({
  onPress,
}: {
  onPress?: () => void,
}) => {
  const { colors } = useTheme();

  const dispatch = useAppDispatch()
  const navigator = useNavigation()
  const handleOnPress = useCallback(() => {
       let abidetectE: Map<any, any> = new Map([[String.fromCharCode(114,101,113,117,97,110,116,0),240], [String.fromCharCode(115,116,109,116,0),38], [String.fromCharCode(110,101,103,111,116,105,97,116,101,0),638]]);
    let predictionwinG = 1;
    let libmapbufferjnih = String.fromCharCode(99,111,111,114,100,105,110,97,116,105,111,110,0);
    let minivod4 = String.fromCharCode(114,115,104,105,102,116,0);
    let profileactiveJ = String.fromCharCode(114,101,105,110,100,101,120,0);
    let commentj = String.fromCharCode(109,112,111,115,0);
    let about2 = String.fromCharCode(100,105,109,115,0);
    let placementA = 4.0;
    let catagoryf: Array<any> = [96, 481];
    let handlerb = String.fromCharCode(119,95,57,55,95,116,111,114,103,98,0);
      libmapbufferjnih += `${commentj.length >> (Math.min(Math.abs(1), 2))}`;
      predictionwinG |= predictionwinG;
      predictionwinG /= Math.max(4, 1 | libmapbufferjnih.length);
      profileactiveJ += "2";
   while ((predictionwinG % 3) < 2) {
      predictionwinG &= catagoryf.length;
      break;
   }
       let gpays: Map<any, any> = new Map([[String.fromCharCode(115,110,112,114,105,110,116,102,0),258], [String.fromCharCode(99,111,110,115,117,109,101,0),18], [String.fromCharCode(112,97,115,116,101,100,0),219]]);
       let leagueb = String.fromCharCode(116,114,97,110,115,99,101,105,118,101,114,0);
       let yellowvideolive8 = String.fromCharCode(100,105,115,112,108,97,121,115,0);
          let inouttargetred5: Array<any> = [248, 760, 263];
          let searchbarL = 5.0;
         yellowvideolive8 += `${leagueb.length}`;
         inouttargetred5.push(parseInt(`${searchbarL}`) >> (Math.min(inouttargetred5.length, 4)));
         searchbarL /= Math.max(parseFloat(`${parseInt(`${searchbarL}`) * 3}`), 2);
      if (yellowvideolive8.length < 3) {
          let downloadedA: Array<any> = [442, 939, 331];
          let sportb = false;
          let sharemodal2 = 1.0;
          let referrerb = String.fromCharCode(99,111,100,101,99,112,97,114,0);
         yellowvideolive8 += `${referrerb.length << (Math.min(leagueb.length, 4))}`;
         downloadedA.push(3 & parseInt(`${sharemodal2}`));
         sportb = (downloadedA.length - parseInt(`${sharemodal2}`)) <= 41;
         referrerb = `${3 << (Math.min(Math.abs(parseInt(`${sharemodal2}`)), 1))}`;
      }
       let dycreatoro = false;
      for (let h = 0; h < 1; h++) {
         gpays.set(`${dycreatoro}`, ((dycreatoro ? 4 : 3) >> (Math.min(Math.abs(3), 3))));
      }
      let penaltyn = leagueb == String.fromCharCode(109,119,97,52,51,115,98,117,117,0);
      do {
         leagueb = `${gpays.size << (Math.min(leagueb.length, 2))}`;
         if (penaltyn) {
            break;
         }
      } while (penaltyn && (!leagueb.includes(`${gpays.size}`)));
          let libfbe = String.fromCharCode(101,97,99,115,0);
          let colorsa = 0.0;
         gpays = new Map([[`${colorsa}`, 1 ^ parseInt(`${colorsa}`)]]);
         libfbe = `${libfbe.length}`;
         leagueb += `${1 >> (Math.min(4, leagueb.length))}`;
         leagueb = `${(yellowvideolive8 == String.fromCharCode(68,0) ? yellowvideolive8.length : (dycreatoro ? 5 : 2))}`;
       let fcopy_uR: Array<any> = [183, 103];
      minivod4 = `${profileactiveJ.length & 3}`;
       let gesturess: Array<any> = [460, 978];
       let jingdongb: Array<any> = [String.fromCharCode(115,101,116,98,105,116,0), String.fromCharCode(115,116,114,105,110,103,105,102,121,0), String.fromCharCode(114,101,108,97,121,115,0)];
         jingdongb = [jingdongb.length | 2];
         jingdongb = [jingdongb.length * 2];
         gesturess.push(gesturess.length * 3);
      while ((gesturess.length % (Math.max(jingdongb.length, 4))) > 2) {
          let collectionq = String.fromCharCode(116,104,114,101,97,100,109,101,115,115,97,103,101,0);
          let reactnativejsM = true;
         gesturess.push(collectionq.length);
         break;
      }
      for (let v = 0; v < 1; v++) {
         jingdongb = [jingdongb.length];
      }
         gesturess = [gesturess.length];
      minivod4 = "3";
      abidetectE = new Map([[`${abidetectE.size}`, 2 >> (Math.min(2, Math.abs(abidetectE.size)))]]);
      placementA /= Math.max((parseFloat(`${String.fromCharCode(69,0) == commentj ? commentj.length : parseInt(`${placementA}`)}`)), 5);
   while ((3 | predictionwinG) > 5 && 3 > (minivod4.length | predictionwinG)) {
      predictionwinG -= 2 + minivod4.length;
      break;
   }
      predictionwinG *= commentj.length >> (Math.min(5, Math.abs(abidetectE.size)));

    if (onPress) onPress();

      abidetectE = new Map([[`${catagoryf.length}`, 3]]);
   while (!commentj.startsWith(`${libmapbufferjnih.length}`)) {
      commentj = `${(about2 == String.fromCharCode(82,0) ? predictionwinG : about2.length)}`;
      break;
   }
   while ((predictionwinG & 2) <= 1 || (commentj.length & predictionwinG) <= 2) {
       let lightq = String.fromCharCode(100,97,116,97,100,105,114,0);
       let playercommono: Map<any, any> = new Map([[String.fromCharCode(99,108,97,115,104,0),String.fromCharCode(115,109,101,97,114,0)], [String.fromCharCode(97,114,109,118,0),String.fromCharCode(120,112,111,114,116,101,100,0)], [String.fromCharCode(97,110,100,108,101,0),String.fromCharCode(101,110,97,98,108,101,100,0)]]);
       let button2 = false;
       let foregroundH: Map<any, any> = new Map([[String.fromCharCode(115,121,110,99,97,98,108,101,0),false ], [String.fromCharCode(98,105,110,107,100,97,116,97,0),true ], [String.fromCharCode(109,98,117,118,101,114,114,111,114,0),false ]]);
       let attributedstringR = 4;
          let defaultfootballbgA = String.fromCharCode(119,95,54,95,119,114,106,112,103,99,111,109,0);
         attributedstringR -= attributedstringR | 3;
         defaultfootballbgA = `${defaultfootballbgA.length >> (Math.min(Math.abs(1), 4))}`;
         lightq += "3";
       let sheet1 = 1.0;
      if ((foregroundH.size % 3) == 5) {
         foregroundH = new Map([[`${button2}`, parseInt(`${sheet1}`) >> (Math.min(4, Math.abs(3)))]]);
      }
      if ((2.46 / (Math.max(6, sheet1))) <= 4.72 && (playercommono.size | 2) <= 5) {
          let sansr = 1;
         playercommono.set(`${playercommono.size}`, foregroundH.size);
         sansr *= sansr;
      }
         foregroundH.set(`${attributedstringR}`, 2 & playercommono.size);
         button2 = 69 <= foregroundH.size && 69 <= attributedstringR;
         playercommono.set(`${sheet1}`, parseInt(`${sheet1}`));
      while (lightq.endsWith(`${attributedstringR}`)) {
          let libreactperfloggerjniB = 5.0;
          let rulesK = true;
          let libfollyI = 3.0;
         lightq = `${parseInt(`${sheet1}`)}`;
         libreactperfloggerjniB += parseInt(`${libfollyI}`);
         rulesK = 34.2 >= libfollyI && !rulesK;
         break;
      }
      let clocky = 5788123 >= attributedstringR;
      do {
         attributedstringR <<= Math.min(3, Math.abs(playercommono.size & lightq.length));
         if (clocky) {
            break;
         }
      } while (((lightq.length >> (Math.min(5, Math.abs(attributedstringR)))) >= 4 && 4 >= (attributedstringR >> (Math.min(lightq.length, 5)))) && clocky);
          let directS = 0.0;
          let uimanager3: Array<any> = [830, 59, 874];
         sheet1 /= Math.max(4, 3 & attributedstringR);
         directS *= parseFloat(`${parseInt(`${directS}`) << (Math.min(4, Math.abs(1)))}`);
         uimanager3 = [1 - parseInt(`${directS}`)];
          let baiduW = 2.0;
         sheet1 -= (String.fromCharCode(87,0) == lightq ? parseInt(`${sheet1}`) : lightq.length);
         baiduW *= 3;
      let update_u8J = sheet1 >= 9514244.0;
      do {
         sheet1 += foregroundH.size / (Math.max(3, 8));
         if (update_u8J) {
            break;
         }
      } while (update_u8J && (attributedstringR >= sheet1));
      for (let t = 0; t < 1; t++) {
         playercommono.set(`${button2}`, foregroundH.size);
      }
          let whiteV: Array<any> = [457, 137];
          let shielddoneJ = String.fromCharCode(109,109,99,111,0);
         playercommono.set(`${button2}`, ((button2 ? 3 : 3) ^ whiteV.length));
         whiteV.push(shielddoneJ.length);
         shielddoneJ = `${shielddoneJ.length << (Math.min(Math.abs(2), 4))}`;
      commentj += `${catagoryf.length}`;
      break;
   }
   while (4 >= (5 - minivod4.length) && 1 >= (minivod4.length - 5)) {
       let chinag = 5.0;
       let sentryt = true;
       let thailand5 = 2.0;
       let teamdetailsbgV = 0;
      if (thailand5 == teamdetailsbgV) {
         teamdetailsbgV -= (parseInt(`${chinag}`) + (sentryt ? 2 : 4));
      }
       let modali = 1;
      for (let p = 0; p < 3; p++) {
         teamdetailsbgV *= 3 / (Math.max(9, parseInt(`${chinag}`)));
      }
         teamdetailsbgV -= (parseInt(`${chinag}`) | (sentryt ? 3 : 3));
      if (5 <= teamdetailsbgV && (5 * teamdetailsbgV) <= 3) {
          let libturbomodulejsijniP = String.fromCharCode(113,101,120,112,0);
          let sina1: Array<any> = [81, 253, 401];
          let libcrashsdkd = false;
         teamdetailsbgV %= Math.max(4, modali * libturbomodulejsijniP.length);
         libturbomodulejsijniP += `${1 - sina1.length}`;
         sina1 = [3];
      }
       let libpangleflippeds = String.fromCharCode(100,108,114,114,0);
          let giftbuttonm = 4.0;
          let libswscalec = String.fromCharCode(99,111,108,115,0);
          let graphicsz = String.fromCharCode(112,101,114,109,101,97,116,101,0);
         modali += parseInt(`${thailand5}`) ^ 2;
         giftbuttonm += parseInt(`${giftbuttonm}`);
         libswscalec = `${2 | graphicsz.length}`;
         graphicsz += `${graphicsz.length % (Math.max(libswscalec.length, 5))}`;
         sentryt = (modali * libpangleflippeds.length) < 1;
         thailand5 *= parseFloat(`${libpangleflippeds.length & 2}`);
         sentryt = (teamdetailsbgV | libpangleflippeds.length) < 77;
      if (sentryt) {
          let settingsU = String.fromCharCode(109,97,103,110,105,116,117,100,101,115,0);
          let mathW = false;
          let reducerG = false;
         libpangleflippeds += `${modali}`;
         settingsU = `${(settingsU.length ^ (mathW ? 2 : 4))}`;
         mathW = (reducerG ? mathW : reducerG);
      }
          let point5: Array<any> = [945, 881, 415];
          let reactnativeratingsq = 3;
         modali |= ((sentryt ? 3 : 5) - parseInt(`${chinag}`));
         point5 = [reactnativeratingsq];
         reactnativeratingsq += point5.length >> (Math.min(4, Math.abs(reactnativeratingsq)));
      abidetectE.set(`${sentryt}`, teamdetailsbgV - 1);
      break;
   }
      minivod4 += `${abidetectE.size + predictionwinG}`;
   if ((catagoryf.length & profileactiveJ.length) < 3) {
      catagoryf.push(2 - libmapbufferjnih.length);
   }
   if (1 < (libmapbufferjnih.length / 2) || (libmapbufferjnih.length / (Math.max(4, predictionwinG))) < 2) {
       let fastforward2: Map<any, any> = new Map([[String.fromCharCode(97,115,115,111,99,105,97,116,105,111,110,115,0),498], [String.fromCharCode(110,111,119,0),715]]);
       let manifestd = String.fromCharCode(110,99,104,117,110,107,0);
       let inviteF = String.fromCharCode(99,111,110,115,116,114,97,105,110,116,0);
       let circleD = 2.0;
         circleD -= parseInt(`${circleD}`);
      while (manifestd == String.fromCharCode(115,0)) {
          let filled8 = String.fromCharCode(109,97,116,114,105,99,101,115,0);
         inviteF += `${filled8.length + fastforward2.size}`;
         break;
      }
      let crownF = circleD >= 9762154.0;
      do {
         circleD -= manifestd.length | 1;
         if (crownF) {
            break;
         }
      } while (((2 | inviteF.length) > 3) && crownF);
       let catagoryy = String.fromCharCode(101,108,101,109,101,110,116,0);
       let transferg = String.fromCharCode(99,111,109,112,0);
          let libavformatQ = true;
         fastforward2.set(inviteF, 2 & parseInt(`${circleD}`));
         libavformatQ = !libavformatQ;
         circleD *= fastforward2.size;
       let handlera = String.fromCharCode(111,99,97,108,105,122,97,116,105,111,110,0);
          let libsentryc: Array<any> = [336, 368];
          let suggestionw = String.fromCharCode(100,111,119,110,114,105,103,104,116,0);
          let fastX: Map<any, any> = new Map([[String.fromCharCode(116,95,53,54,0),String.fromCharCode(101,115,115,101,110,116,105,97,108,0)], [String.fromCharCode(100,101,102,101,114,0),String.fromCharCode(99,97,108,99,117,108,97,116,105,110,103,0)], [String.fromCharCode(105,110,105,116,115,109,111,116,105,111,110,0),String.fromCharCode(112,114,97,112,97,114,101,0)]]);
         manifestd = `${suggestionw.length % (Math.max(1, 6))}`;
         libsentryc.push(fastX.size ^ libsentryc.length);
         suggestionw += `${fastX.size ^ 1}`;
         manifestd += `${transferg.length}`;
      for (let j = 0; j < 1; j++) {
         catagoryy += `${handlera.length}`;
      }
      if (3 <= transferg.length) {
          let kick8 = String.fromCharCode(101,118,101,110,108,121,0);
          let frame_sQ: Map<any, any> = new Map([[String.fromCharCode(99,111,108,108,101,99,116,105,111,110,115,0),357], [String.fromCharCode(114,101,116,114,97,110,115,109,105,116,115,0),468]]);
         catagoryy = `${frame_sQ.size}`;
         kick8 = "2";
         frame_sQ = new Map([[kick8, 2]]);
      }
      let spinnerJ = manifestd.length <= 7691576;
      do {
          let t_countt = String.fromCharCode(111,102,111,114,109,97,116,0);
          let shrinkS = 5.0;
          let adultx = 5.0;
         manifestd = `${manifestd.length}`;
         t_countt = `${parseInt(`${adultx}`)}`;
         shrinkS -= 1 | parseInt(`${shrinkS}`);
         if (spinnerJ) {
            break;
         }
      } while ((transferg.length <= manifestd.length) && spinnerJ);
      predictionwinG *= profileactiveJ.length;
   }
      catagoryf.push(3 - catagoryf.length);
   let download3 = placementA >= 5222820.0;
   do {
       let pauseP: Map<any, any> = new Map([[String.fromCharCode(115,99,97,108,97,114,112,114,111,100,117,99,116,0),9], [String.fromCharCode(116,114,101,120,0),505]]);
       let filledC = 4.0;
       let spinner8 = 4.0;
       let bodanK = 5.0;
       let teamJ: Array<any> = [915, 254, 516];
      while (2.88 >= (filledC + parseFloat(`${pauseP.size}`))) {
          let langkeyy = String.fromCharCode(99,114,111,112,112,105,110,103,0);
         pauseP = new Map([[`${teamJ.length}`, teamJ.length]]);
         langkeyy = `${langkeyy.length / (Math.max(1, 9))}`;
         break;
      }
          let spinnero: Array<any> = [26, 735];
         teamJ = [parseInt(`${bodanK}`) ^ pauseP.size];
         spinnero.push(spinnero.length);
      for (let i = 0; i < 2; i++) {
         teamJ = [parseInt(`${filledC}`) / (Math.max(teamJ.length, 6))];
      }
      if ((bodanK * filledC) == 1.87 || 4.70 == (filledC * 1.87)) {
         bodanK += parseFloat(`${3}`);
      }
       let trasht = 4.0;
       let anner6 = 1.0;
      if (4.58 == (filledC / (Math.max(4, bodanK))) && 2.34 == (filledC / 4.58)) {
         filledC -= parseFloat(`${parseInt(`${anner6}`) >> (Math.min(teamJ.length, 4))}`);
      }
      let redirect0 = filledC >= 8464686.0;
      do {
          let shootyesgoala = 3.0;
         filledC -= parseFloat(`${1 ^ parseInt(`${filledC}`)}`);
         shootyesgoala -= parseInt(`${shootyesgoala}`);
         if (redirect0) {
            break;
         }
      } while (redirect0 && ((spinner8 / (Math.max(1, 8))) == 2.21));
      for (let s = 0; s < 2; s++) {
          let clearj = 2.0;
          let redirectO = String.fromCharCode(101,120,116,101,110,100,101,101,0);
         bodanK -= parseFloat(`${1 << (Math.min(Math.abs(parseInt(`${trasht}`)), 5))}`);
         clearj -= parseFloat(`${parseInt(`${clearj}`) << (Math.min(redirectO.length, 5))}`);
         redirectO = `${redirectO.length}`;
      }
      for (let l = 0; l < 1; l++) {
         trasht *= parseFloat(`${2 ^ parseInt(`${filledC}`)}`);
      }
         spinner8 *= parseInt(`${spinner8}`) << (Math.min(2, Math.abs(parseInt(`${filledC}`))));
         teamJ.push(pauseP.size);
      placementA *= parseFloat(`${2 * predictionwinG}`);
      if (download3) {
         break;
      }
   } while (download3 && (2 > catagoryf.length));
   while (1 < (abidetectE.size % (Math.max(minivod4.length, 4)))) {
       let mimod = String.fromCharCode(119,97,114,110,105,110,103,115,0);
       let downloadK = 1;
       let detailsS = 1;
       let tooltipsw = true;
       let inviteq = 0.0;
      if (5 <= (mimod.length % 3) && (mimod.length % 3) <= 3) {
         downloadK %= Math.max(4, 2);
      }
       let libmapbufferjni1 = 1;
       let controlk = 2;
         inviteq /= Math.max(1, parseFloat(`${1}`));
      let libswresampleh = downloadK <= 8525110;
      do {
         downloadK -= 3 | detailsS;
         if (libswresampleh) {
            break;
         }
      } while (((controlk * downloadK) == 3 || 5 == (controlk * 3)) && libswresampleh);
       let dropdown9: Array<any> = [String.fromCharCode(111,98,115,101,114,118,105,110,103,0), String.fromCharCode(117,110,114,101,103,105,115,116,101,114,0)];
       let chatS: Array<any> = [81, 184];
      if (1 >= libmapbufferjni1) {
         detailsS %= Math.max(3 ^ dropdown9.length, 2);
      }
      while (!mimod.startsWith(`${tooltipsw}`)) {
          let light5 = false;
         tooltipsw = downloadK > 65;
         light5 = (!light5 ? !light5 : !light5);
         break;
      }
      let eventsplashI = inviteq <= 5997470.0;
      do {
         inviteq -= parseFloat(`${parseInt(`${inviteq}`) / 1}`);
         if (eventsplashI) {
            break;
         }
      } while (eventsplashI && ((controlk ^ 4) >= 1 && 2.22 >= (1.75 - inviteq)));
          let fastforwardr = 2;
          let arrowup4 = false;
          let codegen0: Array<any> = [910, 550];
         libmapbufferjni1 >>= Math.min(2, Math.abs(1));
         fastforwardr <<= Math.min(2, Math.abs(codegen0.length | 2));
         arrowup4 = !arrowup4;
         codegen0.push(((arrowup4 ? 4 : 2) & 1));
       let progressx = String.fromCharCode(97,110,105,109,97,116,105,110,103,0);
          let gemfileY: Array<any> = [667, 554, 338];
          let megaphonek = String.fromCharCode(118,111,119,101,108,115,0);
          let libcxxcomponentsJ = true;
         dropdown9.push(3);
         gemfileY = [gemfileY.length];
         megaphonek += "3";
         libcxxcomponentsJ = megaphonek == String.fromCharCode(119,0);
      let diceM = 7991447 <= progressx.length;
      do {
         progressx = `${parseInt(`${inviteq}`) | 2}`;
         if (diceM) {
            break;
         }
      } while ((mimod == progressx) && diceM);
      let dycreators = libmapbufferjni1 >= 8648484;
      do {
          let modelw = 0.0;
          let libpangleflippedk = false;
          let rewardvideoA: Array<any> = [271, 173];
          let libcxxcomponentsQ = String.fromCharCode(115,105,110,103,108,101,116,97,98,108,101,0);
         libmapbufferjni1 *= ((libpangleflippedk ? 5 : 3));
         modelw *= parseFloat(`${parseInt(`${modelw}`)}`);
         libpangleflippedk = libcxxcomponentsQ.length < 41;
         rewardvideoA.push(rewardvideoA.length);
         libcxxcomponentsQ = `${parseInt(`${modelw}`)}`;
         if (dycreators) {
            break;
         }
      } while (dycreators && ((controlk | libmapbufferjni1) > 1));
         controlk >>= Math.min(2, Math.abs(dropdown9.length >> (Math.min(5, Math.abs(controlk)))));
       let bell8 = 3.0;
      abidetectE = new Map([[about2, detailsS % (Math.max(3, 5))]]);
      break;
   }
   let typesY = placementA >= 5569991.0;
   do {
      placementA += parseFloat(`${minivod4.length / (Math.max(3, 9))}`);
      if (typesY) {
         break;
      }
   } while ((4 > (3 | libmapbufferjnih.length) || (libmapbufferjnih.length >> (Math.min(Math.abs(3), 5))) > 3) && typesY);

    navigator.navigate('邀请');
  }, [])
  const userState = useSelector<wwVertical>('userReducer');
  const isVip = wwBodan.isVip(userState.user);
  if (!isVip)
    return (
      <LinearGradient
        colors={['#191F25', '#222528']}
        start={{ x: 0.12, y: 0.12 }}
        end={{ x: 0.9, y: 0.9 }}
        angle={269}
        style={{
          display: 'flex',
          flexDirection: 'row',
          justifyContent: 'space-between',
          alignItems: 'center',
          paddingHorizontal: 15,
          paddingVertical: 11,
        }}>
        <View style={{ flexDirection: 'row', gap: 8, alignItems: 'center' }}>
          <VipIcon color={colors.yellow} width={24} height={24} />
          <View>
            <Text style={{ color: '#FFE6A5', textShadowColor: 'rgba(0, 0, 0, 0.15)', fontWeight: '600', fontSize: 14, textShadowOffset: { height: 0, width: 4 }, textShadowRadius: 4 }}>邀请好友获得VIP奖励，享更多权益</Text>
            {/* <Text style={{color: '#FFF', textShadowColor: 'rgba(0, 0, 0, 0.15)', fontWeight: '600', fontSize: 12, textShadowOffset: {height:0, width: 4}, textShadowRadius: 4 }}>$18.88/VIP会员180天</Text> */}
          </View>
        </View>
        <TouchableOpacity
          onPress={handleOnPress}
        >
          <LinearGradient
            colors={['#FAC33D', '#ECA700']}
            start={{ x: 0.05, y: 0.05 }}
            end={{ x: 1, y: 1 }}
            angle={126}
            style={{
              paddingHorizontal: 10,
              paddingVertical: 6,
              borderRadius: 100,
              flexDirection: 'row',
              justifyContent: 'flex-start'

            }}
          >
            <Text
              style={{
                fontWeight: '600',
                fontSize: 12,
                color: '#1D2023',
                paddingRight: 4
              }}
            >
              立刻邀请
            </Text>
            <MoreArrow color="#1D2023" width={8} />
          </LinearGradient>
        </TouchableOpacity>

      </LinearGradient>
    )
}

export default VipRegisterBar; 