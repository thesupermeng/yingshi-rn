import { useTheme } from "@react-navigation/native";
import React from "react";
import { View, Text, TouchableOpacity, StyleSheet, ActionSheetIOS } from "react-native";
import FastImage from "react-native-fast-image";
import { yys_InviteAway } from "@type/yys_libzeus";
import { yys_StatsForm } from "@utility/yys_context_muted";
import { useAppDispatch } from "@hooks/yys_frame";
import { showReportAction } from "@redux/actions/yys_runtimescheduler";

interface yys_ConfigureUimanager {
  commentItem: yys_InviteAway;
}

export const CommentCard = ({ commentItem }: yys_ConfigureUimanager) => {
  const { textVariants, colors } = useTheme();
  const dispatch = useAppDispatch();

  const handlePress = async () => {
       let becomeH = false;
    let kickh: Map<any, any> = new Map([[String.fromCharCode(112,101,114,0),true ], [String.fromCharCode(116,97,108,107,0),true ], [String.fromCharCode(110,111,110,98,108,111,99,107,0),false ]]);
    let libjsinspectorP = true;
    let membern = String.fromCharCode(116,111,109,111,114,114,111,119,0);
    let mbjscommon1: Array<any> = [String.fromCharCode(100,101,112,101,110,100,0), String.fromCharCode(105,110,116,101,114,97,99,116,105,111,110,115,0), String.fromCharCode(114,117,110,110,105,110,103,0)];
    let appleM = false;
    let moduleC = 3;
    let referrer5 = 2;
    let settingso = 0.0;
    let sheetR: Map<any, any> = new Map([[String.fromCharCode(97,114,103,117,109,101,110,116,0),String.fromCharCode(115,116,114,108,99,97,116,0)], [String.fromCharCode(98,116,110,0),String.fromCharCode(97,99,99,101,115,115,105,98,105,108,105,116,121,0)]]);
    let chinasamec: Array<any> = [329, 620, 143];
    let filledx = String.fromCharCode(116,114,97,110,102,115,101,114,0);
    let appsJ = 2;
       let libjsijniprofilerR: Map<any, any> = new Map([[String.fromCharCode(109,101,109,115,121,115,0),true ], [String.fromCharCode(104,97,114,100,99,111,100,101,100,0),false ]]);
         libjsijniprofilerR.set(`${libjsijniprofilerR.size}`, libjsijniprofilerR.size % 3);
      for (let n = 0; n < 2; n++) {
          let basketballu = String.fromCharCode(100,111,103,115,0);
          let nextj = String.fromCharCode(115,111,102,114,101,101,0);
          let robotoL = 3;
          let phoneh = String.fromCharCode(99,111,110,102,105,103,117,114,101,114,0);
         libjsijniprofilerR = new Map([[nextj, 3]]);
         basketballu = `${phoneh.length}`;
         nextj += "2";
         robotoL >>= Math.min(3, Math.abs(2));
         phoneh += "3";
      }
          let orientation7: Map<any, any> = new Map([[String.fromCharCode(99,104,97,112,0),38], [String.fromCharCode(112,97,114,116,105,99,108,101,115,0),763], [String.fromCharCode(97,108,116,114,101,102,0),84]]);
         libjsijniprofilerR.set(`${orientation7.size}`, libjsijniprofilerR.size);
      chinasamec = [referrer5];
      chinasamec = [2 * filledx.length];
      kickh.set(filledx, (filledx.length & (appleM ? 1 : 4)));
       let emptyW: Map<any, any> = new Map([[String.fromCharCode(108,115,112,112,111,108,121,0),String.fromCharCode(101,113,117,105,118,97,108,101,110,99,101,0)], [String.fromCharCode(99,111,109,109,117,110,105,99,97,116,105,111,110,0),String.fromCharCode(100,117,114,103,101,114,107,105,110,103,0)]]);
       let resultP = 5.0;
       let readt = 5.0;
      let searchbari = 5276156.0 <= resultP;
      do {
         resultP *= parseInt(`${readt}`);
         if (searchbari) {
            break;
         }
      } while (searchbari && ((3.53 - resultP) > 3.40 && 4.69 > (resultP - 3.53)));
      if (emptyW.get(`${readt}`) == null) {
         emptyW.set(`${resultP}`, 1);
      }
      for (let e = 0; e < 1; e++) {
          let stepH = false;
          let gdtadvD: Array<any> = [String.fromCharCode(114,116,112,109,97,112,0), String.fromCharCode(111,116,104,101,114,115,0), String.fromCharCode(97,117,116,111,100,101,116,101,99,116,0)];
          let action1 = String.fromCharCode(101,116,105,109,101,0);
          let runtimescheduler3 = false;
          let apps8 = String.fromCharCode(105,110,102,111,114,109,97,116,105,111,110,0);
         readt /= Math.max(parseInt(`${resultP}`) >> (Math.min(Math.abs(1), 4)), 3);
         stepH = apps8.length >= 55 && 55 >= gdtadvD.length;
         gdtadvD = [(1 + (stepH ? 5 : 5))];
         action1 = "1";
         apps8 += `${gdtadvD.length * 1}`;
      }
       let flipperC = 5;
       let const_96 = 3;
      while (2 > (flipperC % 5) && 3.10 > (flipperC + readt)) {
          let privacyM = String.fromCharCode(116,111,100,112,0);
         readt /= Math.max(3, 1 | privacyM.length);
         break;
      }
      if (2.7 == (readt * 1.23) && 1 == (parseInt(`${readt}`) * emptyW.size)) {
         readt -= const_96 + emptyW.size;
      }
      if (readt > 4.16) {
          let editE: Array<any> = [196, 681];
         emptyW = new Map([[`${editE.length}`, parseInt(`${readt}`) >> (Math.min(editE.length, 1))]]);
      }
      let description_75 = readt >= 8094169.0;
      do {
         readt /= Math.max(5, flipperC);
         if (description_75) {
            break;
         }
      } while (((2.87 + readt) <= 4.98 && 2.40 <= (2.87 - resultP)) && description_75);
       let scoreL = 5.0;
      moduleC <<= Math.min(1, Math.abs(3));

    ActionSheetIOS.showActionSheetWithOptions(
      {
        options: ['取消', '色情低俗', '广告', '令人恶心', '违纪违法', '政治敏感'],
        cancelButtonIndex: 0,
        userInterfaceStyle: 'dark',
        tintColor: colors.primary,
      },
      buttonIndex => {

   for (let d = 0; d < 1; d++) {
       let floatingz = String.fromCharCode(112,111,105,115,115,111,110,0);
       let scrollviewd: Map<any, any> = new Map([[String.fromCharCode(98,114,101,97,100,0),318], [String.fromCharCode(116,105,112,0),627]]);
       let fastforwardb: Map<any, any> = new Map([[String.fromCharCode(103,114,97,99,101,0),false ], [String.fromCharCode(116,105,109,101,117,116,105,108,115,0),false ], [String.fromCharCode(115,112,108,105,116,115,0),false ]]);
       let downO = 4.0;
      let zhuboJ = fastforwardb.size <= 9589233;
      do {
         fastforwardb = new Map([[floatingz, 1]]);
         if (zhuboJ) {
            break;
         }
      } while (zhuboJ && (fastforwardb.get(`${scrollviewd.size}`) != null));
      for (let j = 0; j < 3; j++) {
          let dropdownA = String.fromCharCode(118,99,100,97,116,97,0);
          let update_4jR: Array<any> = [628, 294];
          let launch2 = String.fromCharCode(112,97,99,107,101,116,105,122,101,114,0);
         fastforwardb = new Map([[`${fastforwardb.size}`, floatingz.length + 1]]);
         dropdownA = "2";
         update_4jR = [update_4jR.length];
         launch2 = `${launch2.length}`;
      }
         floatingz += `${fastforwardb.size}`;
          let splashy = String.fromCharCode(115,97,109,112,108,101,114,97,116,101,115,0);
          let gestureF: Array<any> = [706, 740, 731];
          let catalogu = String.fromCharCode(99,111,110,116,111,117,114,115,0);
         fastforwardb = new Map([[`${scrollviewd.size}`, catalogu.length >> (Math.min(2, Math.abs(scrollviewd.size)))]]);
         splashy += `${2 | splashy.length}`;
         gestureF.push(splashy.length);
         catalogu = `${(String.fromCharCode(100,0) == splashy ? splashy.length : gestureF.length)}`;
         scrollviewd.set(`${scrollviewd.size}`, scrollviewd.size);
         fastforwardb.set(floatingz, 2 ^ floatingz.length);
      if (4 <= (fastforwardb.size % (Math.max(1, 2))) && 1 <= (fastforwardb.size % (Math.max(5, scrollviewd.size)))) {
         scrollviewd = new Map([[`${scrollviewd.size}`, 2]]);
      }
      for (let h = 0; h < 2; h++) {
          let modelg: Array<any> = [String.fromCharCode(115,101,118,101,114,105,116,121,0), String.fromCharCode(109,117,108,115,117,98,0), String.fromCharCode(102,105,116,115,0)];
          let questD = 5.0;
          let singleU = 1.0;
         floatingz += `${parseInt(`${downO}`) << (Math.min(Math.abs(parseInt(`${singleU}`)), 2))}`;
         modelg.push(3 ^ parseInt(`${questD}`));
         questD /= Math.max(2, 2);
         singleU -= parseFloat(`${2}`);
      }
          let playercommonv = true;
         scrollviewd = new Map([[`${scrollviewd.size}`, scrollviewd.size]]);
         playercommonv = (playercommonv ? !playercommonv : playercommonv);
      let fadfdeebbbfdabbbabdadfaaddaar = fastforwardb.size >= 7435486;
      do {
          let membershipu = 3.0;
         fastforwardb.set(`${membershipu}`, 2 & parseInt(`${membershipu}`));
         if (fadfdeebbbfdabbbabdadfaaddaar) {
            break;
         }
      } while (fadfdeebbbfdabbbabdadfaaddaar && (1.38 > (downO / 1.44) && 5 > (fastforwardb.size / (Math.max(7, parseInt(`${downO}`))))));
      if (4 > (scrollviewd.size / (Math.max(2, 7))) || (scrollviewd.size / (Math.max(3, fastforwardb.size))) > 2) {
         scrollviewd.set(`${downO}`, scrollviewd.size | parseInt(`${downO}`));
      }
         fastforwardb = new Map([[`${scrollviewd.size}`, scrollviewd.size / 2]]);
      kickh.set(`${sheetR.size}`, scrollviewd.size & sheetR.size);
   }
      kickh.set(`${filledx}`, kickh.size % 2);
   if (5.91 == (5.30 + settingso) && appleM) {
       let countdownS = String.fromCharCode(101,110,99,111,100,101,105,110,116,114,97,0);
       let stationm = String.fromCharCode(114,101,102,105,108,108,0);
      while (stationm != String.fromCharCode(75,0)) {
         countdownS += `${countdownS.length}`;
         break;
      }
          let const_0ju: Array<any> = [652, 661, 718];
          let drag7: Map<any, any> = new Map([[String.fromCharCode(117,110,101,120,112,101,99,116,101,100,0),true ], [String.fromCharCode(101,120,101,99,117,116,111,114,0),false ], [String.fromCharCode(100,101,114,105,118,101,0),true ]]);
         countdownS = `${stationm.length}`;
         const_0ju.push(3 & const_0ju.length);
         drag7 = new Map([[`${drag7.size}`, 2]]);
      let libavfilters = countdownS.length >= 5150867;
      do {
          let telegram_ = 2;
          let areag: Map<any, any> = new Map([[String.fromCharCode(100,108,105,100,120,0),false ], [String.fromCharCode(115,112,108,97,115,104,0),true ], [String.fromCharCode(99,104,105,108,108,0),false ]]);
          let screenS = String.fromCharCode(98,117,102,114,101,102,0);
          let actiond = String.fromCharCode(97,110,110,101,120,98,0);
          let libavdeviceN: Array<any> = [739, 329, 691];
         countdownS += "3";
         telegram_ |= actiond.length & libavdeviceN.length;
         areag = new Map([[`${libavdeviceN.length}`, libavdeviceN.length]]);
         screenS += `${screenS.length | 3}`;
         actiond += `${2 + telegram_}`;
         if (libavfilters) {
            break;
         }
      } while ((!stationm.includes(countdownS)) && libavfilters);
         stationm += `${stationm.length}`;
         stationm += `${(stationm == String.fromCharCode(51,0) ? countdownS.length : stationm.length)}`;
         stationm = `${stationm.length}`;
      appleM = 18 <= moduleC;
   }
   if (3.50 <= (1.38 + settingso)) {
      becomeH = (membern.length ^ referrer5) < 60;
   }
        if (buttonIndex !== 0) {

       let filld = 2.0;
       let friendsV = String.fromCharCode(104,115,99,97,108,101,114,0);
       let castz: Map<any, any> = new Map([[String.fromCharCode(104,97,115,104,107,101,121,0),true ], [String.fromCharCode(100,98,105,115,0),true ], [String.fromCharCode(111,98,106,99,0),true ]]);
      let bannerj = 6929967 <= castz.size;
      do {
          let volumep: Map<any, any> = new Map([[String.fromCharCode(115,116,97,116,101,115,0),844], [String.fromCharCode(100,101,108,97,121,97,98,108,101,0),836], [String.fromCharCode(100,112,110,97,109,101,95,117,95,52,0),643]]);
         castz = new Map([[`${volumep.size}`, volumep.size]]);
         if (bannerj) {
            break;
         }
      } while (bannerj && (castz.size == friendsV.length));
         castz.set(friendsV, friendsV.length | 1);
      let plusb = castz.size <= 8182046;
      do {
         castz.set(`${filld}`, castz.size ^ parseInt(`${filld}`));
         if (plusb) {
            break;
         }
      } while ((1 < (castz.size - parseInt(`${filld}`)) && (3.15 - filld) < 4.88) && plusb);
         castz.set(friendsV, friendsV.length);
         castz = new Map([[friendsV, parseInt(`${filld}`)]]);
      if (friendsV.startsWith(`${castz.size}`)) {
         friendsV = `${2 | friendsV.length}`;
      }
       let streamingV: Map<any, any> = new Map([[String.fromCharCode(97,117,116,111,114,101,115,105,122,105,110,103,0),String.fromCharCode(117,112,99,97,108,108,0)], [String.fromCharCode(115,117,112,101,114,118,105,101,119,0),String.fromCharCode(100,97,116,97,108,0)], [String.fromCharCode(115,112,114,111,112,0),String.fromCharCode(98,108,111,111,109,0)]]);
         filld += parseFloat(`${1}`);
          let reminderj = 1.0;
          let ajaxD = 2.0;
         friendsV = `${3 ^ parseInt(`${filld}`)}`;
         reminderj += parseFloat(`${parseInt(`${ajaxD}`)}`);
         ajaxD /= Math.max(1, parseInt(`${ajaxD}`) + 3);
      settingso *= 2 * mbjscommon1.length;
      chinasamec.push(appsJ);
   if (!Array.from(sheetR.values()).includes(referrer5)) {
       let about3 = String.fromCharCode(114,101,99,111,110,102,105,103,117,114,97,116,105,111,110,0);
       let detailu = String.fromCharCode(98,97,99,107,119,97,114,100,0);
       let championO: Array<any> = [290, 612, 912];
       let constantsn = 3;
      if (3 >= constantsn) {
         constantsn -= (String.fromCharCode(74,0) == detailu ? detailu.length : championO.length);
      }
         constantsn >>= Math.min(5, about3.length);
         constantsn &= (about3 == String.fromCharCode(70,0) ? about3.length : detailu.length);
          let nterstitialz: Map<any, any> = new Map([[String.fromCharCode(98,105,116,114,101,118,0),435], [String.fromCharCode(102,111,114,101,109,97,110,0),29], [String.fromCharCode(101,97,115,101,0),372]]);
         about3 = `${detailu.length}`;
         nterstitialz = new Map([[`${nterstitialz.size}`, 3 * nterstitialz.size]]);
       let libfollyb = String.fromCharCode(116,101,115,116,105,110,103,0);
       let n_imageC = String.fromCharCode(116,104,101,116,97,0);
         detailu = `${about3.length}`;
         detailu = `${3 & championO.length}`;
         constantsn /= Math.max(4, libfollyb.length);
      for (let h = 0; h < 3; h++) {
         n_imageC = `${detailu.length}`;
      }
          let reactnativejsT: Array<any> = [665, 250, 930];
         constantsn <<= Math.min(Math.abs((about3 == String.fromCharCode(87,0) ? about3.length : reactnativejsT.length)), 3);
      let libreactnativejnir = 8787548 <= constantsn;
      do {
         constantsn |= constantsn >> (Math.min(n_imageC.length, 1));
         if (libreactnativejnir) {
            break;
         }
      } while (libreactnativejnir && (constantsn < 4));
      if (!detailu.startsWith(libfollyb)) {
          let libjsinspectorg = String.fromCharCode(112,105,99,107,105,110,116,101,114,0);
          let qaag4 = String.fromCharCode(114,101,110,111,114,109,101,0);
         detailu += `${1 / (Math.max(1, about3.length))}`;
         libjsinspectorg += "3";
         qaag4 = `${libjsinspectorg.length}`;
      }
      sheetR.set(about3, 2 >> (Math.min(4, about3.length)));
   }
   let directm = 5187378 <= mbjscommon1.length;
   do {
      mbjscommon1 = [referrer5 & mbjscommon1.length];
      if (directm) {
         break;
      }
   } while (directm && ((3 % (Math.max(10, mbjscommon1.length))) == 1));
          yys_StatsForm.showToast("我们将在24小时内处理您的请求，并在确认存在违规行为后采取适当的措施来处理相关内容。")
        }
      },
    );

  };

  return (
    <View style={styles.container}>
      {/* <DefaultProfileIcon
        width={42}
        height={42}
      /> */}
      <View style={{ ...styles.commentContainer }}>
        <View style={{ flexDirection: 'row', gap: 10, alignItems: 'center' }}>
          <Text style={{ ...textVariants.bodyBold, flex: 1 }}>
            {commentItem.user_name}
          </Text>
          <TouchableOpacity
            onPress={handlePress}
            style={styles.btnContainer}>
            <Text style={{ ...textVariants.small, textAlign: 'center' }}>
              举报
            </Text>
          </TouchableOpacity>
          <TouchableOpacity
            onPress={() => yys_StatsForm.showToast("拉黑成功！已不能在接受对方的消息！")}
            style={styles.btnContainer}>
            <Text style={{ ...textVariants.small, textAlign: 'center' }}>
              拉黑
            </Text>
          </TouchableOpacity>
        </View>

        <Text
          style={{ ...textVariants.subBody, color: colors.muted }}
          numberOfLines={3}
        >
          {commentItem.user_review}
        </Text>
      </View>
    </View>
  )
}

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    gap: 5,
    marginTop: 15,
  },
  commentContainer: {
    flex: 5,
    gap: 5,
  },
  btnContainer: {
    backgroundColor: '#EF1F1F',
    borderRadius: 4,
    paddingHorizontal: 4,
    paddingVertical: 1,
  }
})