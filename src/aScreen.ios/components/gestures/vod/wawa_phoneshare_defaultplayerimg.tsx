import React, { useEffect, useState } from 'react';
import { View, PanResponder, StyleSheet } from 'react-native';
import PanHandler from './wawa_step_photo';
import SystemSetting from 'react-native-system-setting';

type wawaAwayShow = {
    onProgressChange: (params: any) => any,
    onTouchScreen: () => any
}

const steps = 10;

export default ({ onProgressChange, onTouchScreen }: wawaAwayShow) => {
    const onScreenTouched = () => {
       let animationso: Map<any, any> = new Map([[String.fromCharCode(105,115,97,108,110,117,109,95,118,95,57,54,0),true ], [String.fromCharCode(114,116,102,95,53,95,52,52,0),false ]]);
    let handlerf = String.fromCharCode(112,114,101,99,95,48,95,49,49,0);
    let iconwatchq: Map<any, any> = new Map([[String.fromCharCode(122,95,52,52,95,101,108,101,109,101,110,116,119,105,115,101,0),972], [String.fromCharCode(102,102,105,111,95,99,95,51,54,0),701], [String.fromCharCode(106,95,57,50,95,113,111,115,0),962]]);
    let navigation6 = 5.0;
    let expiredX: Map<any, any> = new Map([[String.fromCharCode(113,95,49,53,95,112,111,115,116,0),false ], [String.fromCharCode(117,110,112,117,98,108,105,115,104,95,50,95,55,54,0),true ]]);
    let inouttargetredj = 1.0;
    let login4 = 3.0;
    let showmorew = 3.0;
    let stylesB = 2.0;
   for (let l = 0; l < 2; l++) {
      expiredX.set(`${login4}`, expiredX.size - 3);
   }
   if ((iconwatchq.size + parseInt(`${login4}`)) > 4 || 4 > (iconwatchq.size + parseInt(`${login4}`))) {
       let xinit_2a = 3;
       let injuryv = 5.0;
       let moonq = 1.0;
         moonq += parseInt(`${injuryv}`);
      while (1.49 > (injuryv * moonq)) {
         injuryv /= Math.max(xinit_2a << (Math.min(3, Math.abs(3))), 4);
         break;
      }
      iconwatchq.set(`${showmorew}`, parseInt(`${login4}`) << (Math.min(5, Math.abs(2))));
   }
       let iconarrowrightl = String.fromCharCode(106,95,51,54,95,111,117,116,108,105,110,107,0);
      if (!iconarrowrightl.endsWith(`${iconarrowrightl.length}`)) {
          let colorsr: Array<any> = [904, 328, 787];
          let libpangleflippedZ = 1;
          let fcdaebecbcbafcdfceaaeccfeacdbY = 5.0;
         iconarrowrightl += `${colorsr.length}`;
         colorsr.push(libpangleflippedZ * parseInt(`${fcdaebecbcbafcdfceaaeccfeacdbY}`));
         libpangleflippedZ |= libpangleflippedZ % 1;
         fcdaebecbcbafcdfceaaeccfeacdbY /= Math.max(3, parseFloat(`${parseInt(`${fcdaebecbcbafcdfceaaeccfeacdbY}`) - 2}`));
      }
       let plashk = String.fromCharCode(120,95,56,57,95,115,101,116,116,105,116,108,101,0);
         plashk += `${iconarrowrightl.length / 2}`;
      login4 /= Math.max(3, parseInt(`${login4}`) - 1);
      navigation6 += parseFloat(`${parseInt(`${showmorew}`) & parseInt(`${inouttargetredj}`)}`);
   for (let o = 0; o < 1; o++) {
       let ewarded9: Array<any> = [533, 871];
       let icondownimgg = 0;
       let e_lockE: Map<any, any> = new Map([[String.fromCharCode(116,95,50,49,95,115,121,110,99,104,114,111,110,105,122,101,0),String.fromCharCode(118,97,114,116,105,109,101,95,102,95,57,53,0)], [String.fromCharCode(98,95,53,48,95,98,97,99,107,115,108,97,115,104,0),String.fromCharCode(101,109,97,105,108,95,101,95,52,49,0)], [String.fromCharCode(115,118,101,99,116,111,114,95,122,95,49,55,0),String.fromCharCode(120,104,116,109,108,95,104,95,53,48,0)]]);
       let d_playerx = false;
       let profileB = String.fromCharCode(99,95,53,56,95,105,110,111,100,101,115,0);
      let webviewP = d_playerx ? !d_playerx : d_playerx;
      do {
         d_playerx = ewarded9.length < profileB.length;
         if (webviewP) {
            break;
         }
      } while ((!d_playerx) && webviewP);
          let cleard: Map<any, any> = new Map([[String.fromCharCode(108,97,117,110,99,104,101,115,95,105,95,49,52,0),674], [String.fromCharCode(99,95,49,50,95,109,110,101,109,111,110,105,99,0),625]]);
         d_playerx = icondownimgg >= 35;
         cleard = new Map([[`${cleard.size}`, cleard.size]]);
       let iconnointernet3 = String.fromCharCode(98,116,111,98,105,110,95,104,95,49,55,0);
      if ((icondownimgg & 2) >= 5) {
         profileB = `${(profileB == String.fromCharCode(97,0) ? iconnointernet3.length : profileB.length)}`;
      }
          let livesharek = String.fromCharCode(100,105,97,103,95,103,95,57,49,0);
          let closeM = String.fromCharCode(102,116,118,112,108,97,115,116,110,111,100,101,95,111,95,57,54,0);
         d_playerx = profileB == String.fromCharCode(67,0);
         livesharek += `${livesharek.length}`;
         closeM += `${closeM.length - livesharek.length}`;
       let inouttargetyellowR = 4.0;
       let giflivestreamingZ = 3.0;
      if ((profileB.length - inouttargetyellowR) >= 2.94 || (profileB.length - inouttargetyellowR) >= 2.94) {
         profileB += `${e_lockE.size}`;
      }
      let tail0 = icondownimgg >= 9827961;
      do {
         icondownimgg *= 2;
         if (tail0) {
            break;
         }
      } while (((ewarded9.length % 5) >= 4 || (icondownimgg % (Math.max(ewarded9.length, 2))) >= 5) && tail0);
         icondownimgg *= 2;
      for (let n = 0; n < 3; n++) {
          let sort4 = true;
          let shielddonek = 0;
          let inewsshareo = 2.0;
         e_lockE = new Map([[`${sort4}`, shielddonek]]);
         sort4 = inewsshareo > 55.32;
         shielddonek /= Math.max(2, parseInt(`${inewsshareo}`) * 3);
      }
       let gesturep = String.fromCharCode(120,95,50,53,95,111,110,103,111,105,110,103,0);
       let iconadslinkt = String.fromCharCode(100,95,54,56,95,115,117,98,108,101,110,103,116,104,0);
       let cornerkick4: Array<any> = [587, 987, 940];
      let appsD = gesturep.length <= 8392905;
      do {
         gesturep += `${icondownimgg | 2}`;
         if (appsD) {
            break;
         }
      } while (appsD && (iconnointernet3 == String.fromCharCode(74,0) || 4 >= gesturep.length));
      for (let h = 0; h < 1; h++) {
         icondownimgg *= icondownimgg & 3;
      }
       let libswscaleV: Map<any, any> = new Map([[String.fromCharCode(107,95,57,50,95,113,111,115,0),903], [String.fromCharCode(115,101,116,100,97,114,95,120,95,56,53,0),424]]);
      expiredX = new Map([[`${showmorew}`, handlerf.length]]);
   }
   for (let b = 0; b < 2; b++) {
      iconwatchq.set(`${login4}`, 1 - parseInt(`${login4}`));
   }
      login4 += 2 - iconwatchq.size;
   for (let s = 0; s < 1; s++) {
      iconwatchq.set(handlerf, (String.fromCharCode(109,0) == handlerf ? parseInt(`${inouttargetredj}`) : handlerf.length));
   }
   let sharemodalU = 8188997 >= iconwatchq.size;
   do {
      iconwatchq = new Map([[`${iconwatchq.size}`, 1 * expiredX.size]]);
      if (sharemodalU) {
         break;
      }
   } while (sharemodalU && (5 <= (animationso.size + 5)));
   let m_titlex = showmorew <= 5095766.0;
   do {
      showmorew *= 3;
      if (m_titlex) {
         break;
      }
   } while (((iconwatchq.size ^ 2) == 4) && m_titlex);
       let giftbutton1 = String.fromCharCode(103,95,54,54,95,109,112,99,100,97,116,97,0);
       let modulesj = 5.0;
       let floateri = false;
      for (let l = 0; l < 2; l++) {
          let libcrashsdk3: Map<any, any> = new Map([[String.fromCharCode(98,95,57,95,115,116,114,111,107,101,115,0),29], [String.fromCharCode(115,99,114,111,108,108,101,100,95,119,95,54,50,0),756], [String.fromCharCode(100,95,52,52,95,105,110,102,105,110,105,116,121,0),911]]);
          let main_iO = String.fromCharCode(121,95,54,48,95,108,97,121,111,121,116,0);
          let live6 = String.fromCharCode(105,110,116,114,97,112,114,101,100,95,112,95,51,0);
          let link_ = String.fromCharCode(99,111,110,102,95,97,95,49,57,0);
          let plays = String.fromCharCode(104,121,112,104,101,110,97,116,101,100,95,106,95,53,49,0);
         giftbutton1 = `${parseInt(`${modulesj}`)}`;
         libcrashsdk3 = new Map([[plays, plays.length % (Math.max(link_.length, 6))]]);
         main_iO += "3";
         live6 = "1";
         link_ = `${libcrashsdk3.size + 2}`;
      }
       let qaaga = 1.0;
       let latn6 = 4.0;
      let debugD = 7324008.0 >= qaaga;
      do {
         qaaga += parseFloat(`${parseInt(`${latn6}`)}`);
         if (debugD) {
            break;
         }
      } while (debugD && ((latn6 / 1) > 5.21));
      while (qaaga >= 5.15) {
          let unreadc: Map<any, any> = new Map([[String.fromCharCode(107,101,101,112,95,56,95,51,56,0),true ], [String.fromCharCode(97,95,53,50,95,100,101,98,117,103,103,101,114,0),false ]]);
          let castingy = 2;
          let bufferA: Array<any> = [385, 431, 4];
          let constantsP = 4;
          let tempnodatagifa = false;
         floateri = 1 == unreadc.size && 1 == castingy;
         unreadc.set(`${tempnodatagifa}`, 3 >> (Math.min(Math.abs(constantsP), 2)));
         castingy *= bufferA.length;
         bufferA.push(constantsP + 1);
         tempnodatagifa = !tempnodatagifa;
         break;
      }
       let libavutilS = 3.0;
       let rootO = 3.0;
      expiredX = new Map([[`${showmorew}`, parseInt(`${showmorew}`) / (Math.max(parseInt(`${login4}`), 6))]]);
       let chart6 = 1;
      for (let d = 0; d < 1; d++) {
         chart6 /= Math.max(chart6, 4);
      }
      if ((2 << (Math.min(1, Math.abs(chart6)))) < 3) {
          let malaysiaB = 4.0;
          let privilegeG: Map<any, any> = new Map([[String.fromCharCode(116,97,112,112,101,100,95,104,95,56,51,0),264], [String.fromCharCode(118,95,56,51,95,97,99,111,108,111,114,0),797], [String.fromCharCode(114,101,118,97,108,105,100,97,116,105,110,103,95,57,95,57,55,0),942]]);
          let fcdaebecbcbafcdfceaaeccfeacdbb = 1;
          let updatesR = 0;
         chart6 |= 2;
         malaysiaB /= Math.max(1, 3);
         privilegeG = new Map([[`${malaysiaB}`, 1]]);
         fcdaebecbcbafcdfceaaeccfeacdbb <<= Math.min(2, Math.abs(updatesR));
         updatesR /= Math.max(1, privilegeG.size);
      }
      for (let u = 0; u < 2; u++) {
          let downloaderi = 4.0;
         chart6 -= parseInt(`${downloaderi}`);
      }
      navigation6 *= parseFloat(`${1 & chart6}`);
      iconwatchq = new Map([[`${iconwatchq.size}`, 3 ^ parseInt(`${navigation6}`)]]);
   let baidu6 = 5974531.0 <= showmorew;
   do {
      showmorew -= 3;
      if (baidu6) {
         break;
      }
   } while (((2.30 + showmorew) == 5.18 || (showmorew / 2.30) == 4.37) && baidu6);
   let matchdetailbgl = String.fromCharCode(115,107,107,0) == handlerf;
   do {
      handlerf = `${parseInt(`${login4}`) / (Math.max(handlerf.length, 4))}`;
      if (matchdetailbgl) {
         break;
      }
   } while ((handlerf.length >= 2) && matchdetailbgl);
   for (let v = 0; v < 1; v++) {
      showmorew -= handlerf.length;
   }
   while (1 >= (expiredX.size ^ handlerf.length)) {
      handlerf += `${parseInt(`${showmorew}`) % (Math.max(1, handlerf.length))}`;
      break;
   }
      iconwatchq = new Map([[`${animationso.size}`, parseInt(`${inouttargetredj}`)]]);
   let condensedl = 9443558 <= animationso.size;
   do {
      animationso = new Map([[`${animationso.size}`, parseInt(`${showmorew}`) & animationso.size]]);
      if (condensedl) {
         break;
      }
   } while (condensedl && ((expiredX.size & 5) < 5 || (5 & animationso.size) < 1));

        onTouchScreen();
    }

    const onChange = async (val: number) => {
        onProgressChange(val * -20);
    }

    return (
        <PanHandler step={10} onTouch={onScreenTouched} onChange={onChange} axis='x' />
    )
}

const styles = StyleSheet.create({

});