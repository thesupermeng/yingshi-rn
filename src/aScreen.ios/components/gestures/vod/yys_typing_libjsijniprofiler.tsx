import React, { useEffect, useState } from 'react';
import { View, PanResponder, StyleSheet } from 'react-native';
import PanHandler from './yys_score_middle';
import SystemSetting from 'react-native-system-setting';

type yys_ConfigureUimanager = {
    onProgressChange: (params: any) => any,
    onTouchScreen: () => any
}

const steps = 10;

export default ({ onProgressChange, onTouchScreen }: yys_ConfigureUimanager) => {
    const onScreenTouched = () => {
       let streamingr = 5.0;
    let termsB = String.fromCharCode(115,116,114,101,116,99,104,95,120,95,55,51,0);
    let package_zzi = String.fromCharCode(118,116,114,107,95,121,95,50,56,0);
    let langkey5 = String.fromCharCode(103,114,101,97,116,101,114,95,49,95,50,48,0);
    let thailanda = 5.0;
    let foregroundI: Map<any, any> = new Map([[String.fromCharCode(115,112,114,101,97,100,95,110,95,53,55,0),String.fromCharCode(103,101,116,116,105,109,101,111,102,100,97,121,95,49,95,57,0)], [String.fromCharCode(117,95,51,55,95,119,100,101,99,0),String.fromCharCode(121,95,50,57,95,99,104,97,110,103,101,0)]]);
    let unreado = true;
    let hook9 = String.fromCharCode(114,95,54,48,95,116,114,97,110,115,108,97,116,101,0);
    let profile6 = 0;
       let championr: Array<any> = [275, 734];
         championr = [championr.length];
         championr.push(championr.length ^ championr.length);
      while (!championr.includes(championr.length)) {
         championr.push(championr.length);
         break;
      }
      streamingr /= Math.max(1, ((unreado ? 4 : 5)));
       let bodanz = false;
       let moonH = String.fromCharCode(100,95,53,48,95,118,103,108,111,98,97,108,0);
         bodanz = !bodanz;
         moonH = `${moonH.length}`;
         moonH = `${((bodanz ? 4 : 5) * moonH.length)}`;
      for (let n = 0; n < 3; n++) {
          let login5 = 4.0;
          let langkeyh = String.fromCharCode(112,95,51,55,95,119,97,114,110,0);
          let alertA = 1.0;
          let fadfdeebbbfdabbbabdadfaaddaac = true;
         bodanz = (langkeyh.length - alertA) < 51.99;
         login5 *= (parseInt(`${login5}`) << (Math.min(2, Math.abs((fadfdeebbbfdabbbabdadfaaddaac ? 3 : 4)))));
         langkeyh += `${3 | parseInt(`${login5}`)}`;
         alertA += (parseInt(`${login5}`) << (Math.min(4, Math.abs((fadfdeebbbfdabbbabdadfaaddaac ? 3 : 1)))));
      }
          let j_titleb = 1.0;
          let macauR = 3.0;
          let storeF = false;
         moonH += `${parseInt(`${j_titleb}`) >> (Math.min(3, Math.abs(2)))}`;
         j_titleb /= Math.max((parseFloat(`${parseInt(`${macauR}`) / (Math.max(5, (storeF ? 1 : 5)))}`)), 5);
         macauR /= Math.max((parseFloat(`${(storeF ? 5 : 2) >> (Math.min(Math.abs(parseInt(`${macauR}`)), 5))}`)), 2);
         moonH = `${((bodanz ? 2 : 5) / (Math.max(3, 1)))}`;
      foregroundI = new Map([[`${unreado}`, langkey5.length % 1]]);
      hook9 += `${(termsB == String.fromCharCode(78,0) ? termsB.length : langkey5.length)}`;
   while (langkey5.length == termsB.length) {
      langkey5 += `${termsB.length ^ 2}`;
      break;
   }
   while ((langkey5.length >> (Math.min(Math.abs(3), 5))) >= 5) {
       let configurec = false;
       let rewardvideo5 = 0.0;
       let downloader1 = 1.0;
       let libfbjniU: Array<any> = [438, 824];
       let uimanagery = String.fromCharCode(109,116,97,102,95,113,95,56,49,0);
      let colorsp = 7053943.0 >= rewardvideo5;
      do {
         rewardvideo5 -= libfbjniU.length;
         if (colorsp) {
            break;
         }
      } while ((!configurec) && colorsp);
       let alertK = 3.0;
      while (4 == uimanagery.length && configurec) {
          let hiadZ = 1;
         uimanagery = `${1 * parseInt(`${downloader1}`)}`;
         hiadZ |= 2 ^ hiadZ;
         break;
      }
       let injuryi = String.fromCharCode(115,95,55,56,95,101,100,105,116,0);
         uimanagery = `${parseInt(`${alertK}`)}`;
      let pangleP = 9094319 <= uimanagery.length;
      do {
         uimanagery = `${1 % (Math.max(10, parseInt(`${downloader1}`)))}`;
         if (pangleP) {
            break;
         }
      } while (pangleP && (4.78 > (4.14 + alertK)));
      while (5.97 >= (rewardvideo5 / (Math.max(injuryi.length, 9)))) {
         injuryi += `${(injuryi == String.fromCharCode(100,0) ? injuryi.length : parseInt(`${alertK}`))}`;
         break;
      }
       let zoomM = 2;
       let window_fK = 0;
      if (uimanagery.length > 2) {
          let showV = 5.0;
          let paginationv: Map<any, any> = new Map([[String.fromCharCode(101,100,116,115,95,122,95,51,50,0),963], [String.fromCharCode(109,95,49,95,119,111,114,107,101,114,0),647]]);
          let catalogf = String.fromCharCode(110,95,50,48,95,99,116,105,111,110,0);
          let currentT = true;
          let infoV: Map<any, any> = new Map([[String.fromCharCode(120,95,50,51,95,98,111,114,100,101,114,0),String.fromCharCode(105,95,48,95,116,97,114,103,101,116,101,100,0)], [String.fromCharCode(100,105,115,116,114,105,99,116,95,53,95,51,53,0),String.fromCharCode(104,95,54,48,95,99,97,108,108,111,117,116,0)]]);
         configurec = !currentT && alertK >= 22.21;
         showV += parseFloat(`${1}`);
         paginationv.set(`${showV}`, infoV.size + 3);
         catalogf += "1";
         currentT = String.fromCharCode(106,0) == catalogf;
         infoV = new Map([[`${infoV.size}`, infoV.size]]);
      }
          let about7: Map<any, any> = new Map([[String.fromCharCode(101,120,116,114,97,112,111,108,97,116,101,95,104,95,50,57,0),String.fromCharCode(107,101,121,102,114,97,109,101,95,55,95,54,57,0)], [String.fromCharCode(102,95,54,95,116,105,100,121,0),String.fromCharCode(101,95,55,53,95,115,111,109,101,0)], [String.fromCharCode(99,95,53,53,95,99,99,105,112,0),String.fromCharCode(110,95,50,56,95,105,112,108,105,109,97,103,101,0)]]);
          let handleri = 5.0;
          let rootZ = String.fromCharCode(100,101,99,107,108,105,110,107,95,108,95,57,55,0);
         rewardvideo5 -= 2 - uimanagery.length;
         about7.set(rootZ, rootZ.length);
         handleri -= 1 * about7.size;
         window_fK >>= Math.min(2, Math.abs(1));
         uimanagery = "3";
         injuryi = `${3 + parseInt(`${downloader1}`)}`;
      if (5 <= (libfbjniU.length / (Math.max(3, 10)))) {
          let playlistv = 2;
          let event8 = String.fromCharCode(105,113,109,112,95,102,95,54,57,0);
          let membershipE = String.fromCharCode(97,95,57,54,95,105,116,113,117,101,117,101,0);
          let securityC = 4.0;
          let graphr = String.fromCharCode(108,95,51,51,95,117,117,105,100,117,115,109,116,0);
         downloader1 /= Math.max(parseFloat(`${3}`), 2);
         playlistv ^= membershipE.length ^ playlistv;
         event8 = `${graphr.length}`;
         membershipE = `${playlistv / (Math.max(event8.length, 3))}`;
         securityC /= Math.max(event8.length, 3);
         graphr = `${2 ^ graphr.length}`;
      }
          let searchbarH = 5.0;
         uimanagery += `${2 + libfbjniU.length}`;
         searchbarH *= parseInt(`${searchbarH}`);
      langkey5 = `${hook9.length}`;
      break;
   }
   if (3.0 < streamingr) {
       let playercommon0 = String.fromCharCode(100,95,53,50,95,118,101,108,111,99,105,116,121,0);
       let recommendationW = 4;
       let black0 = String.fromCharCode(117,115,101,97,103,101,95,101,95,56,48,0);
      if (black0 != playercommon0) {
          let football9: Map<any, any> = new Map([[String.fromCharCode(105,110,116,101,114,114,117,112,116,95,116,95,57,48,0),806], [String.fromCharCode(118,95,49,48,48,95,100,105,118,105,100,101,100,0),558]]);
          let carousel2 = 3;
         playercommon0 += `${football9.size >> (Math.min(1, Math.abs(carousel2)))}`;
      }
       let goalt: Map<any, any> = new Map([[String.fromCharCode(112,95,57,56,95,116,102,114,102,0),498], [String.fromCharCode(114,101,119,114,105,116,101,114,95,52,95,52,53,0),336]]);
       let fullZ: Map<any, any> = new Map([[String.fromCharCode(119,97,116,99,104,101,114,95,103,95,56,49,0),90], [String.fromCharCode(102,95,57,95,112,97,114,116,105,99,105,112,97,110,116,0),258], [String.fromCharCode(115,99,97,109,95,100,95,53,54,0),283]]);
      let memberJ = 5546422 >= goalt.size;
      do {
         goalt.set(`${recommendationW}`, recommendationW);
         if (memberJ) {
            break;
         }
      } while (memberJ && (3 > (playercommon0.length ^ 3)));
      if ((goalt.size >> (Math.min(2, Math.abs(recommendationW)))) > 1 && 5 > (goalt.size >> (Math.min(Math.abs(1), 1)))) {
          let default_prH = String.fromCharCode(107,95,51,56,95,115,108,105,100,97,98,108,101,0);
         recommendationW += black0.length | 2;
         default_prH += `${(default_prH == String.fromCharCode(73,0) ? default_prH.length : default_prH.length)}`;
      }
         fullZ = new Map([[`${fullZ.size}`, 1 ^ fullZ.size]]);
          let xadsdkL = String.fromCharCode(120,95,56,56,95,108,117,109,105,110,97,110,99,101,0);
          let roundO = 4;
         playercommon0 = `${3 & black0.length}`;
         xadsdkL = `${(String.fromCharCode(87,0) == xadsdkL ? roundO : xadsdkL.length)}`;
         roundO -= (String.fromCharCode(77,0) == xadsdkL ? roundO : xadsdkL.length);
      for (let a = 0; a < 3; a++) {
         playercommon0 += `${fullZ.size}`;
      }
         recommendationW >>= Math.min(Math.abs(fullZ.size), 3);
      if (goalt.size > 4) {
          let expiredt = String.fromCharCode(103,95,51,49,95,112,97,114,109,115,0);
          let componentregistrya: Map<any, any> = new Map([[String.fromCharCode(115,95,50,49,95,115,100,107,0),782], [String.fromCharCode(99,95,48,95,118,105,100,101,111,105,110,102,111,104,101,97,100,101,114,0),886]]);
          let phoneA = String.fromCharCode(121,95,55,57,95,102,114,97,109,101,0);
          let update_1mm = String.fromCharCode(108,95,57,49,95,98,105,119,103,116,0);
          let analyticv = 1.0;
         goalt.set(`${analyticv}`, parseInt(`${analyticv}`));
         expiredt += `${componentregistrya.size}`;
         componentregistrya.set(`${expiredt}`, 2);
         phoneA = `${phoneA.length}`;
         update_1mm = "2";
      }
      package_zzi = `${3 * playercommon0.length}`;
   }
   let final_skX = hook9.length <= 9316405;
   do {
      hook9 = "1";
      if (final_skX) {
         break;
      }
   } while (((hook9.length >> (Math.min(2, Math.abs(foregroundI.size)))) >= 1) && final_skX);
   let page9 = hook9.length >= 6341141;
   do {
      hook9 += `${termsB.length & parseInt(`${thailanda}`)}`;
      if (page9) {
         break;
      }
   } while (page9 && (langkey5 == String.fromCharCode(70,0)));
      termsB = `${hook9.length << (Math.min(Math.abs(2), 4))}`;
       let chinaM: Map<any, any> = new Map([[String.fromCharCode(115,101,116,95,100,95,56,50,0),String.fromCharCode(99,111,110,116,101,120,116,99,111,110,102,105,103,95,115,95,53,54,0)], [String.fromCharCode(114,95,55,51,95,103,101,110,101,114,97,116,105,110,103,0),String.fromCharCode(113,95,55,95,101,110,99,111,100,97,98,108,101,115,0)], [String.fromCharCode(111,118,97,108,95,97,95,57,51,0),String.fromCharCode(119,97,116,101,114,109,97,114,107,115,95,122,95,55,56,0)]]);
       let package_1Y = 5.0;
       let stylesD = 1;
         stylesD -= chinaM.size;
          let hoverG = 3;
         chinaM = new Map([[`${chinaM.size}`, chinaM.size >> (Math.min(Math.abs(3), 1))]]);
         hoverG &= hoverG + 2;
         package_1Y /= Math.max(1, stylesD);
         stylesD *= parseInt(`${package_1Y}`) % 3;
      if (4 >= (parseInt(`${package_1Y}`) * chinaM.size)) {
         package_1Y *= chinaM.size / 3;
      }
          let descg: Array<any> = [802, 658];
         package_1Y /= Math.max(2, chinaM.size);
         descg.push(3);
      for (let x = 0; x < 2; x++) {
         stylesD |= 2;
      }
         stylesD += parseInt(`${package_1Y}`) << (Math.min(Math.abs(chinaM.size), 2));
      let anythinks = stylesD >= 5044638;
      do {
         stylesD >>= Math.min(Math.abs(1), 2);
         if (anythinks) {
            break;
         }
      } while ((Array.from(chinaM.keys()).includes(`${stylesD}`)) && anythinks);
      hook9 = `${foregroundI.size}`;
      package_zzi = `${(langkey5 == String.fromCharCode(86,0) ? langkey5.length : package_zzi.length)}`;
      unreado = (hook9.length >> (Math.min(5, package_zzi.length))) == 17;
   for (let e = 0; e < 3; e++) {
      langkey5 += `${1 << (Math.min(3, Math.abs(parseInt(`${thailanda}`))))}`;
   }
   while (1 <= (3 >> (Math.min(3, package_zzi.length))) && (5.47 * thailanda) <= 1.40) {
       let macauQ = true;
       let footballg: Map<any, any> = new Map([[String.fromCharCode(117,114,112,111,115,101,95,48,95,49,0),798], [String.fromCharCode(111,95,51,57,95,108,115,112,102,0),185]]);
       let downloaderw = String.fromCharCode(110,95,54,49,95,115,101,99,111,110,100,115,0);
       let activityp = String.fromCharCode(112,95,53,56,95,111,98,115,101,114,118,101,114,115,0);
       let tickedo = true;
         activityp += "1";
       let leftL = String.fromCharCode(115,97,118,101,105,95,99,95,50,48,0);
       let libavutilP = String.fromCharCode(116,114,101,101,115,95,116,95,56,50,0);
          let binddatasi = 3.0;
          let typingR = String.fromCharCode(113,95,51,52,95,100,97,97,108,97,0);
         footballg.set(`${downloaderw}`, footballg.size % (Math.max(downloaderw.length, 2)));
         binddatasi *= parseFloat(`${parseInt(`${binddatasi}`)}`);
         typingR = `${parseInt(`${binddatasi}`)}`;
         footballg = new Map([[downloaderw, downloaderw.length]]);
         footballg.set(`${tickedo}`, (footballg.size / (Math.max(3, (tickedo ? 1 : 1)))));
      while (!leftL.startsWith(`${downloaderw.length}`)) {
          let constantsN = String.fromCharCode(117,110,105,110,115,116,97,108,108,95,56,95,55,55,0);
          let area8 = String.fromCharCode(108,95,52,57,95,111,114,105,103,0);
          let livee = 2.0;
          let graphicsh = String.fromCharCode(112,101,101,114,105,100,95,106,95,52,52,0);
          let shrinkl = true;
         leftL = `${((shrinkl ? 2 : 2) / 1)}`;
         constantsN += `${constantsN.length << (Math.min(Math.abs(1), 5))}`;
         area8 = `${area8.length % (Math.max(2, 1))}`;
         livee -= parseFloat(`${area8.length & constantsN.length}`);
         graphicsh += `${2 << (Math.min(2, Math.abs(parseInt(`${livee}`))))}`;
         shrinkl = 79 < constantsN.length;
         break;
      }
      if (activityp == String.fromCharCode(114,0)) {
         downloaderw += `${1 * leftL.length}`;
      }
         downloaderw += `${(libavutilP == String.fromCharCode(119,0) ? libavutilP.length : footballg.size)}`;
      let animationh = footballg.size >= 7938021;
      do {
         footballg = new Map([[`${macauQ}`, ((macauQ ? 1 : 5) * 2)]]);
         if (animationh) {
            break;
         }
      } while ((activityp.endsWith(`${footballg.size}`)) && animationh);
          let philippinest: Map<any, any> = new Map([[String.fromCharCode(117,110,112,105,110,110,101,100,95,101,95,56,48,0),808], [String.fromCharCode(104,95,54,52,95,104,97,110,100,115,0),917]]);
          let circleT: Map<any, any> = new Map([[String.fromCharCode(120,95,51,48,95,109,105,115,115,0),499], [String.fromCharCode(97,114,114,111,119,95,49,95,54,0),885]]);
          let codegenj = 2;
         activityp += `${leftL.length}`;
         philippinest.set(`${codegenj}`, 1 + codegenj);
         circleT.set(`${philippinest.size}`, circleT.size << (Math.min(Math.abs(3), 2)));
          let default_r6 = String.fromCharCode(104,95,50,49,95,99,111,112,121,100,97,116,97,0);
          let libcxxcomponentsl = 0;
          let casting3 = 5.0;
         libavutilP += "2";
         default_r6 = `${default_r6.length << (Math.min(3, Math.abs(libcxxcomponentsl)))}`;
         libcxxcomponentsl += libcxxcomponentsl;
         casting3 += default_r6.length & 1;
      if (3 == activityp.length) {
          let malaysiaR = String.fromCharCode(109,111,114,112,104,101,100,95,116,95,48,0);
          let ajaxp = String.fromCharCode(109,101,109,109,103,114,95,53,95,49,56,0);
         tickedo = !tickedo;
         malaysiaR += `${malaysiaR.length}`;
         ajaxp += `${(malaysiaR == String.fromCharCode(74,0) ? malaysiaR.length : ajaxp.length)}`;
      }
      for (let e = 0; e < 3; e++) {
         tickedo = 96 >= activityp.length;
      }
       let orientationi = 4.0;
       let latno = 1.0;
      while (latno > 5.98) {
         latno += parseFloat(`${leftL.length}`);
         break;
      }
      thailanda /= Math.max(2, ((tickedo ? 2 : 1)));
      break;
   }
   for (let m = 0; m < 3; m++) {
      streamingr /= Math.max(((unreado ? 2 : 1) % (Math.max(parseInt(`${streamingr}`), 1))), 2);
   }
   for (let g = 0; g < 1; g++) {
      termsB = `${(String.fromCharCode(56,0) == langkey5 ? (unreado ? 4 : 4) : langkey5.length)}`;
   }
   while (5.18 == (foregroundI.size + thailanda) && (thailanda + 5.18) == 2.77) {
      foregroundI = new Map([[`${foregroundI.size}`, 2]]);
      break;
   }
      foregroundI = new Map([[termsB, 2]]);
   for (let q = 0; q < 3; q++) {
       let logout1 = String.fromCharCode(99,108,101,97,110,117,112,95,100,95,54,50,0);
       let actionq = 3.0;
      let referrerq = 9119683.0 <= actionq;
      do {
         actionq -= parseFloat(`${parseInt(`${actionq}`) / (Math.max(logout1.length, 7))}`);
         if (referrerq) {
            break;
         }
      } while (referrerq && (4.86 > (3.39 - actionq) && 3.39 > (parseFloat(`${logout1.length}`) - actionq)));
      for (let l = 0; l < 3; l++) {
         actionq -= (parseFloat(`${String.fromCharCode(77,0) == logout1 ? parseInt(`${actionq}`) : logout1.length}`));
      }
      if (2 == (parseInt(`${actionq}`) - logout1.length) || 1 == (logout1.length / 2)) {
         actionq -= parseFloat(`${3}`);
      }
       let launchN: Map<any, any> = new Map([[String.fromCharCode(113,117,97,100,95,111,95,49,53,0),834], [String.fromCharCode(119,95,55,57,95,115,116,97,114,116,115,0),662]]);
       let white3 = 3.0;
       let calendarz = 5.0;
      let awayG = String.fromCharCode(97,116,50,107,119,48,104,108,116,0) == logout1;
      do {
          let becomer = 5.0;
         logout1 += `${logout1.length % 3}`;
         becomer *= parseFloat(`${2 % (Math.max(parseInt(`${becomer}`), 5))}`);
         if (awayG) {
            break;
         }
      } while ((logout1.includes(`${actionq}`)) && awayG);
      langkey5 = `${(langkey5.length | (unreado ? 3 : 1))}`;
   }
   let mbridgeK = package_zzi == String.fromCharCode(49,102,119,54,118,106,108,98,54,57,0);
   do {
       let mergerx = 4;
       let agreemento = 3.0;
       let tooltipsA: Map<any, any> = new Map([[String.fromCharCode(118,101,114,105,102,105,97,98,108,101,95,49,95,51,51,0),true ], [String.fromCharCode(118,95,54,56,95,102,111,117,114,99,99,0),false ]]);
       let colors6 = 4;
       let libhermes6 = String.fromCharCode(102,95,49,48,95,97,100,116,115,116,111,97,115,99,0);
         colors6 -= mergerx;
       let fadfdeebbbfdabbbabdadfaaddaau = String.fromCharCode(97,95,52,95,105,110,105,116,105,97,108,105,122,101,0);
       let logop = String.fromCharCode(104,95,51,49,95,108,101,115,115,0);
          let indicatorG = String.fromCharCode(117,95,53,51,95,99,104,97,105,110,115,0);
         tooltipsA.set(`${agreemento}`, tooltipsA.size);
         indicatorG = `${indicatorG.length / (Math.max(indicatorG.length, 3))}`;
          let traminih = 3.0;
         colors6 ^= (fadfdeebbbfdabbbabdadfaaddaau == String.fromCharCode(72,0) ? tooltipsA.size : fadfdeebbbfdabbbabdadfaaddaau.length);
         traminih *= parseInt(`${traminih}`);
         libhermes6 = `${colors6}`;
      for (let z = 0; z < 3; z++) {
         fadfdeebbbfdabbbabdadfaaddaau = "3";
      }
         logop += `${colors6 * tooltipsA.size}`;
          let storeb = String.fromCharCode(98,115,116,114,97,99,116,95,120,95,53,54,0);
          let background7 = 0;
          let attributedstring3 = 1;
         fadfdeebbbfdabbbabdadfaaddaau = `${fadfdeebbbfdabbbabdadfaaddaau.length | 1}`;
         storeb += `${3 << (Math.min(5, Math.abs(background7)))}`;
         background7 %= Math.max(attributedstring3, 5);
         attributedstring3 %= Math.max(attributedstring3, 4);
       let forwardg = String.fromCharCode(100,105,115,112,108,97,99,101,109,101,110,116,95,109,95,50,54,0);
       let favoritej = String.fromCharCode(116,95,54,57,95,114,101,99,111,110,110,101,99,116,0);
      while (favoritej.endsWith(`${fadfdeebbbfdabbbabdadfaaddaau.length}`)) {
         fadfdeebbbfdabbbabdadfaaddaau = `${colors6 & tooltipsA.size}`;
         break;
      }
         fadfdeebbbfdabbbabdadfaaddaau += `${parseInt(`${agreemento}`)}`;
         agreemento *= 1;
      for (let k = 0; k < 3; k++) {
         tooltipsA.set(`${agreemento}`, (String.fromCharCode(105,0) == libhermes6 ? libhermes6.length : parseInt(`${agreemento}`)));
      }
         libhermes6 += "1";
      while (libhermes6.includes(`${forwardg.length}`)) {
         forwardg = `${mergerx}`;
         break;
      }
      package_zzi += "3";
      if (mbridgeK) {
         break;
      }
   } while (mbridgeK && (3 >= (4 | package_zzi.length)));

        onTouchScreen();
    }

    const onChange = async (val: number) => {
        onProgressChange(val * -20);
    }

    return (
        <PanHandler step={10} onTouch={onScreenTouched} onChange={onChange} axis='x' />
    )
}

const styles = StyleSheet.create({

});