import React, { useEffect, useState } from 'react';
import { View, PanResponder, StyleSheet } from 'react-native';
import PanHandler from './wawa_step_photo';
import SystemSetting from 'react-native-system-setting';

type wawaAwayShow = {
    onChangeVolume: (params: any) => any,
    onTouchScreen: () => any
}

const steps = 1;

export default ({ onChangeVolume, onTouchScreen }: wawaAwayShow) => {

    const [startY, setStartY] = useState(0)
    const [endY, setEndY] = useState(0)

    const onScreenTouched = () => {
       let basketballmatchdetailbgh: Map<any, any> = new Map([[String.fromCharCode(107,95,57,54,95,105,116,101,109,115,0),477], [String.fromCharCode(99,111,110,116,114,97,115,116,95,52,95,49,53,0),42], [String.fromCharCode(99,97,99,104,101,115,105,122,101,95,118,95,51,51,0),316]]);
    let pointn: Array<any> = [702, 543, 384];
    let colorsi = String.fromCharCode(108,95,55,54,95,104,101,108,112,0);
    let profilepic7 = false;
    let targetf = String.fromCharCode(117,108,111,110,103,95,110,95,50,49,0);
    let leakcheckerM = String.fromCharCode(97,100,109,105,110,101,100,95,99,95,55,52,0);
    let fastp = 1;
    let b_titleq: Array<any> = [247, 913, 974];
    let whistleJ = String.fromCharCode(98,111,111,116,104,95,98,95,53,52,0);
    let libjsik = 5;
    let matchactiveE: Map<any, any> = new Map([[String.fromCharCode(113,95,54,48,95,109,101,103,97,98,121,116,101,0),String.fromCharCode(100,101,99,111,114,97,116,111,114,115,95,53,95,57,53,0)], [String.fromCharCode(115,95,53,53,95,99,97,109,112,97,105,103,110,0),String.fromCharCode(114,111,117,110,100,105,110,103,95,106,95,57,48,0)], [String.fromCharCode(100,101,115,116,114,117,99,116,95,117,95,51,0),String.fromCharCode(102,115,112,112,95,117,95,57,57,0)]]);
    let mbnativeadvancedH = 3;
    let analyticsl: Map<any, any> = new Map([[String.fromCharCode(108,97,110,103,115,95,49,95,56,55,0),866], [String.fromCharCode(99,95,53,57,95,99,108,97,115,104,0),746], [String.fromCharCode(114,101,113,117,105,114,105,110,103,95,116,95,51,56,0),371]]);
   let awayicon8 = profilepic7 ? !profilepic7 : profilepic7;
   do {
      profilepic7 = 78 > colorsi.length;
      if (awayicon8) {
         break;
      }
   } while (awayicon8 && (profilepic7 || 2 == targetf.length));
   for (let y = 0; y < 1; y++) {
       let orangeuparrow5: Array<any> = [997, 846, 619];
       let moduleg = 2.0;
       let filedt = 2.0;
      for (let z = 0; z < 2; z++) {
         moduleg /= Math.max(orangeuparrow5.length ^ 2, 2);
      }
          let runtimeschedulerV = String.fromCharCode(117,114,108,115,95,51,95,49,50,0);
         filedt *= parseInt(`${moduleg}`);
         runtimeschedulerV = `${runtimeschedulerV.length & runtimeschedulerV.length}`;
      let mutedM = orangeuparrow5.length <= 9436596;
      do {
         orangeuparrow5 = [parseInt(`${moduleg}`)];
         if (mutedM) {
            break;
         }
      } while (mutedM && ((moduleg / (Math.max(orangeuparrow5.length, 5))) >= 4.71));
         orangeuparrow5 = [3];
      for (let h = 0; h < 2; h++) {
         orangeuparrow5 = [orangeuparrow5.length >> (Math.min(4, Math.abs(parseInt(`${moduleg}`))))];
      }
       let libswscaleM = true;
       let zhuboG = false;
      for (let s = 0; s < 2; s++) {
         libswscaleM = filedt > 83.53;
      }
      for (let c = 0; c < 1; c++) {
         libswscaleM = filedt <= 32.34 || moduleg <= 32.34;
      }
      let predictionactivef = 8295975.0 >= moduleg;
      do {
          let weibov: Array<any> = [String.fromCharCode(118,95,55,51,95,117,110,99,111,110,102,105,114,109,101,100,0), String.fromCharCode(116,111,117,116,95,99,95,55,54,0), String.fromCharCode(110,95,53,53,95,105,110,100,101,112,101,110,100,101,100,0)];
          let loginsuccessm: Map<any, any> = new Map([[String.fromCharCode(100,101,115,116,114,117,99,116,105,118,101,95,120,95,55,56,0),584], [String.fromCharCode(119,95,52,56,95,98,105,111,109,101,116,114,121,0),235]]);
          let libturbomodulejsijnio = 1.0;
         moduleg *= parseInt(`${filedt}`);
         weibov.push(1 % (Math.max(9, weibov.length)));
         loginsuccessm = new Map([[`${loginsuccessm.size}`, weibov.length]]);
         libturbomodulejsijnio /= Math.max(5, parseInt(`${libturbomodulejsijnio}`));
         if (predictionactivef) {
            break;
         }
      } while (predictionactivef && (1.16 >= (moduleg / 4.38)));
      targetf = "3";
   }
   if (4 == (pointn.length - 2) && 1 == (matchactiveE.size - 2)) {
      matchactiveE = new Map([[leakcheckerM, (String.fromCharCode(118,0) == leakcheckerM ? leakcheckerM.length : (profilepic7 ? 4 : 1))]]);
   }
   for (let v = 0; v < 1; v++) {
      targetf = `${colorsi.length & 2}`;
   }
      whistleJ = `${3 & whistleJ.length}`;
      leakcheckerM = "2";
   if (!profilepic7) {
      profilepic7 = String.fromCharCode(57,0) == leakcheckerM;
   }
       let libfbjniw: Array<any> = [523, 421, 669];
       let connectionA = false;
       let areac = 3;
      while (!connectionA && (libfbjniw.length % 4) < 1) {
         connectionA = 39 >= areac;
         break;
      }
         connectionA = 55 <= areac || !connectionA;
         connectionA = connectionA || areac == 94;
      if (!connectionA) {
         connectionA = !connectionA;
      }
      for (let r = 0; r < 2; r++) {
         connectionA = libfbjniw.length <= 17;
      }
       let librrcM = String.fromCharCode(100,101,99,108,97,114,101,100,95,98,95,52,54,0);
       let terms5 = String.fromCharCode(112,111,115,116,112,114,111,99,114,101,115,95,56,95,50,53,0);
      for (let p = 0; p < 1; p++) {
          let owngoal6 = 0;
         libfbjniw = [(3 + (connectionA ? 3 : 4))];
         owngoal6 += owngoal6;
      }
      for (let t = 0; t < 2; t++) {
          let fullm = 4.0;
         terms5 = `${areac & librrcM.length}`;
         fullm /= Math.max(1 - parseInt(`${fullm}`), 2);
      }
         libfbjniw.push(terms5.length & 1);
      b_titleq = [fastp | 3];
      pointn.push(matchactiveE.size - targetf.length);
   let zcopy_ix = profilepic7 ? !profilepic7 : profilepic7;
   do {
      profilepic7 = whistleJ == leakcheckerM;
      if (zcopy_ix) {
         break;
      }
   } while (zcopy_ix && (profilepic7));
   while (targetf.includes(`${libjsik}`)) {
      targetf = `${(String.fromCharCode(70,0) == targetf ? b_titleq.length : targetf.length)}`;
      break;
   }
       let defaultprofilepici: Array<any> = [533, 148, 91];
       let favicona = true;
         favicona = !favicona;
          let gifgoalw: Array<any> = [607, 987, 603];
          let refreshx = String.fromCharCode(103,95,53,95,105,115,111,0);
          let downloadV = String.fromCharCode(97,95,55,50,95,103,114,101,103,0);
         favicona = gifgoalw.length <= 80;
         gifgoalw = [(String.fromCharCode(48,0) == refreshx ? downloadV.length : refreshx.length)];
         downloadV += `${(downloadV == String.fromCharCode(120,0) ? downloadV.length : refreshx.length)}`;
      while (3 == (defaultprofilepici.length >> (Math.min(Math.abs(2), 4)))) {
         defaultprofilepici.push(defaultprofilepici.length);
         break;
      }
       let iconclosewhiter: Map<any, any> = new Map([[String.fromCharCode(116,119,111,115,116,97,103,101,95,98,95,53,56,0),45], [String.fromCharCode(100,105,115,97,108,108,111,119,95,105,95,57,56,0),609], [String.fromCharCode(113,95,49,54,95,115,109,97,114,116,0),800]]);
       let debugx: Map<any, any> = new Map([[String.fromCharCode(118,95,54,48,95,103,108,111,98,97,108,0),87], [String.fromCharCode(119,95,49,54,95,119,97,115,116,101,100,0),635], [String.fromCharCode(113,95,51,53,95,115,116,114,111,107,101,100,0),689]]);
       let sheetT = String.fromCharCode(104,95,52,55,95,116,114,97,110,115,108,97,116,105,111,110,115,0);
      while (favicona || (4 / (Math.max(3, defaultprofilepici.length))) < 2) {
          let hooka = String.fromCharCode(105,95,51,53,95,112,114,105,110,116,0);
          let defaultlogom = String.fromCharCode(110,95,56,56,95,106,115,116,121,112,101,0);
          let targetZ = String.fromCharCode(109,98,117,102,99,104,97,105,110,95,101,95,56,55,0);
          let snewsK = true;
         favicona = snewsK || sheetT.length > 75;
         hooka = `${hooka.length}`;
         defaultlogom = `${defaultlogom.length}`;
         targetZ += "3";
         snewsK = hooka.length == defaultlogom.length;
         break;
      }
      fastp ^= 1 | matchactiveE.size;
       let libtank = 5.0;
         libtank *= parseFloat(`${parseInt(`${libtank}`)}`);
      while ((libtank / 1.28) < 3.51) {
          let infob = String.fromCharCode(117,95,57,95,112,114,111,116,101,99,116,101,100,0);
          let libfollyt = 4;
          let imageactionlive5 = String.fromCharCode(102,108,111,99,107,95,48,95,52,0);
          let refreshborderless0: Array<any> = [String.fromCharCode(120,95,56,53,95,102,105,111,0), String.fromCharCode(105,102,105,108,116,101,114,95,51,95,49,50,0)];
          let codegeng = String.fromCharCode(105,110,100,105,118,105,100,117,97,108,95,49,95,51,57,0);
         libtank -= parseFloat(`${parseInt(`${libtank}`)}`);
         infob += `${codegeng.length * 1}`;
         libfollyt >>= Math.min(4, Math.abs(1));
         imageactionlive5 = `${3 + infob.length}`;
         refreshborderless0 = [refreshborderless0.length + 1];
         codegeng += `${3 ^ refreshborderless0.length}`;
         break;
      }
      while ((libtank * 5) > 1.63) {
         libtank -= parseFloat(`${parseInt(`${libtank}`) | 1}`);
         break;
      }
      b_titleq.push(2);
   while ((2 * basketballmatchdetailbgh.size) == 2) {
       let main_qs = String.fromCharCode(113,117,97,110,116,115,95,104,95,52,48,0);
       let langu = String.fromCharCode(117,95,52,53,95,100,105,114,101,99,116,0);
       let contextZ = true;
       let orange6: Array<any> = [136, 909, 911];
         langu = `${langu.length}`;
      let awayplayerb = 7707357 <= orange6.length;
      do {
         orange6 = [langu.length];
         if (awayplayerb) {
            break;
         }
      } while (awayplayerb && (4 >= (1 - orange6.length)));
         main_qs += `${((contextZ ? 2 : 1) | 3)}`;
         orange6 = [1];
      let defaultpredictionprofileV = main_qs.length >= 5293681;
      do {
          let langZ = String.fromCharCode(115,99,105,101,110,116,105,102,105,99,95,54,95,57,54,0);
          let m_animationg = 1.0;
          let securityp: Map<any, any> = new Map([[String.fromCharCode(108,104,97,115,104,95,106,95,52,56,0),false ], [String.fromCharCode(114,95,55,52,95,112,114,101,99,97,108,99,117,108,97,116,101,0),true ], [String.fromCharCode(115,105,110,99,95,104,95,55,53,0),true ]]);
          let uploadf = String.fromCharCode(111,116,105,102,105,99,97,116,105,111,110,95,98,95,52,51,0);
          let styles0 = true;
         main_qs = `${parseInt(`${m_animationg}`)}`;
         langZ += `${uploadf.length}`;
         m_animationg += parseFloat(`${uploadf.length & 2}`);
         securityp = new Map([[`${securityp.size}`, securityp.size]]);
         styles0 = langZ == String.fromCharCode(71,0);
         if (defaultpredictionprofileV) {
            break;
         }
      } while (defaultpredictionprofileV && ((5 % (Math.max(3, main_qs.length))) < 3));
         contextZ = (34 <= ((contextZ ? main_qs.length : 34) / (Math.max(main_qs.length, 6))));
      while (!main_qs.endsWith(`${orange6.length}`)) {
         main_qs += `${((contextZ ? 4 : 5))}`;
         break;
      }
      while (contextZ) {
         langu = `${main_qs.length}`;
         break;
      }
      while (langu == String.fromCharCode(56,0)) {
         main_qs += "3";
         break;
      }
      while (main_qs.length <= 1 && langu.length <= 1) {
         main_qs += `${langu.length | 2}`;
         break;
      }
      for (let i = 0; i < 3; i++) {
         orange6.push(langu.length);
      }
          let middleM = String.fromCharCode(120,95,55,51,95,111,109,112,108,105,99,97,116,105,111,110,0);
          let righty: Array<any> = [826, 356, 693];
          let modelso = String.fromCharCode(107,95,53,53,95,97,114,116,105,115,116,0);
         contextZ = righty.length == 4;
         middleM += `${modelso.length}`;
         righty = [modelso.length % 2];
      basketballmatchdetailbgh.set(`${contextZ}`, (1 >> (Math.min(4, Math.abs((contextZ ? 5 : 3))))));
      break;
   }
       let verticald = String.fromCharCode(119,114,105,116,101,108,111,99,107,95,118,95,53,49,0);
      if (!verticald.endsWith(`${verticald.length}`)) {
          let background2: Array<any> = [173, 353, 316];
          let pushJ = 0;
          let yellowq = 1.0;
         verticald += `${verticald.length}`;
         background2 = [pushJ];
         pushJ += background2.length ^ 3;
         yellowq -= background2.length;
      }
       let services = false;
       let huawei5 = true;
      while (huawei5) {
          let tempnodatagifp = 2.0;
          let iconarrowrightblackI: Map<any, any> = new Map([[String.fromCharCode(114,101,112,108,97,121,101,100,95,111,95,50,54,0),309], [String.fromCharCode(120,95,49,56,95,109,105,110,0),151]]);
          let predictionarrowU = String.fromCharCode(111,95,51,52,0);
         huawei5 = (39 <= ((!services ? 39 : iconarrowrightblackI.size) - iconarrowrightblackI.size));
         tempnodatagifp += 1;
         predictionarrowU = `${1 - predictionarrowU.length}`;
         break;
      }
      targetf = `${whistleJ.length - 1}`;
      matchactiveE.set(`${libjsik}`, 3 << (Math.min(Math.abs(libjsik), 2)));
      leakcheckerM += `${2 << (Math.min(3, b_titleq.length))}`;
   if (!whistleJ.includes(`${pointn.length}`)) {
       let circle2 = String.fromCharCode(112,110,105,101,108,115,97,100,100,95,100,95,52,52,0);
       let catalogU: Array<any> = [395, 550];
       let actionsw = String.fromCharCode(122,95,51,48,95,115,98,114,100,115,112,0);
       let iconnotificationnewu: Array<any> = [144, 834];
       let liveshare8 = String.fromCharCode(118,116,114,107,95,112,95,52,56,0);
       let china9 = 2.0;
       let resendq = 4.0;
         iconnotificationnewu = [liveshare8.length];
      for (let x = 0; x < 3; x++) {
         circle2 += "2";
      }
         catalogU.push(circle2.length >> (Math.min(Math.abs(1), 4)));
         resendq *= parseFloat(`${2}`);
          let sourceB = false;
         circle2 = `${circle2.length}`;
         sourceB = !sourceB;
      if (circle2.length <= iconnotificationnewu.length) {
         circle2 += `${(String.fromCharCode(79,0) == circle2 ? circle2.length : catalogU.length)}`;
      }
         circle2 += `${3 + circle2.length}`;
          let defaultfootballbgw = String.fromCharCode(101,110,116,101,114,101,100,95,102,95,49,0);
          let untickm = String.fromCharCode(116,99,102,105,108,101,95,114,95,56,53,0);
         actionsw += `${defaultfootballbgw.length}`;
         defaultfootballbgw = "1";
         untickm += `${(String.fromCharCode(102,0) == untickm ? untickm.length : untickm.length)}`;
      while (4 >= (actionsw.length % 5)) {
          let loginsuccessX: Map<any, any> = new Map([[String.fromCharCode(112,95,54,53,95,112,117,109,112,0),588], [String.fromCharCode(120,95,54,95,115,101,116,97,99,116,105,118,101,107,101,121,0),693]]);
          let goallogox = String.fromCharCode(118,111,105,99,101,115,95,120,95,54,0);
          let disconnectedlogor = 1.0;
          let moreh = 3.0;
          let trashq = String.fromCharCode(108,95,50,48,95,100,111,109,97,105,110,115,0);
         actionsw += `${actionsw.length | 3}`;
         loginsuccessX = new Map([[trashq, 1 * trashq.length]]);
         goallogox += `${trashq.length}`;
         disconnectedlogor /= Math.max(5, trashq.length);
         moreh += parseFloat(`${goallogox.length}`);
         break;
      }
      for (let j = 0; j < 1; j++) {
         circle2 = `${(String.fromCharCode(119,0) == liveshare8 ? circle2.length : liveshare8.length)}`;
      }
       let downloaderp = true;
       let becomew = true;
         iconnotificationnewu.push(2 / (Math.max(10, iconnotificationnewu.length)));
          let libjsinspectorr = 2.0;
         becomew = !becomew;
         libjsinspectorr += parseFloat(`${parseInt(`${libjsinspectorr}`) - parseInt(`${libjsinspectorr}`)}`);
      if ((3.9 - resendq) < 2.91) {
         becomew = (circle2.length >> (Math.min(liveshare8.length, 2))) < 66;
      }
      pointn = [fastp];
   }
      pointn = [mbnativeadvancedH];
      pointn.push(1 >> (Math.min(2, b_titleq.length)));
   for (let f = 0; f < 1; f++) {
      colorsi = `${targetf.length}`;
   }
       let turndownF = 2;
       let sinaR: Map<any, any> = new Map([[String.fromCharCode(111,117,116,112,117,116,113,95,101,95,54,52,0),942], [String.fromCharCode(115,116,114,105,112,115,95,116,95,52,54,0),889]]);
         sinaR.set(`${turndownF}`, turndownF);
      for (let u = 0; u < 1; u++) {
         turndownF *= turndownF | sinaR.size;
      }
      let videob = 5306342 >= turndownF;
      do {
         turndownF %= Math.max(sinaR.size, 2);
         if (videob) {
            break;
         }
      } while (videob && ((1 & sinaR.size) > 1));
         turndownF <<= Math.min(3, Math.abs(3));
      for (let l = 0; l < 1; l++) {
         sinaR.set(`${turndownF}`, sinaR.size << (Math.min(4, Math.abs(turndownF))));
      }
      let basketballiconv = sinaR.size <= 9438795;
      do {
          let turnQ = String.fromCharCode(97,117,116,104,101,110,116,104,105,99,97,116,101,95,116,95,53,52,0);
          let issubA = 0.0;
         sinaR = new Map([[`${sinaR.size}`, turndownF ^ sinaR.size]]);
         turnQ += `${(String.fromCharCode(66,0) == turnQ ? parseInt(`${issubA}`) : turnQ.length)}`;
         issubA *= parseInt(`${issubA}`);
         if (basketballiconv) {
            break;
         }
      } while (basketballiconv && (!Array.from(sinaR.values()).includes(turndownF)));
      pointn.push(mbnativeadvancedH);
   for (let f = 0; f < 3; f++) {
       let benefit3 = false;
       let profilepici = 2;
       let statisticsinactivew: Map<any, any> = new Map([[String.fromCharCode(109,97,120,98,105,116,114,97,116,101,95,105,95,57,54,0),true ], [String.fromCharCode(99,108,117,115,116,95,113,95,52,55,0),true ], [String.fromCharCode(116,114,97,110,115,108,105,116,101,114,97,116,101,95,120,95,51,51,0),true ]]);
      while (4 == (2 % (Math.max(4, profilepici)))) {
          let countdown2 = 3.0;
          let o_lockb = true;
          let iconarrowleftv = String.fromCharCode(104,105,103,104,108,105,103,104,116,101,114,95,122,95,51,57,0);
         benefit3 = profilepici < 10;
         countdown2 /= Math.max(((o_lockb ? 5 : 2)), 2);
         o_lockb = 71.11 < countdown2;
         iconarrowleftv += `${1 | iconarrowleftv.length}`;
         break;
      }
      while ((statisticsinactivew.size & 3) < 3) {
         benefit3 = !benefit3 || 15 < profilepici;
         break;
      }
      if (4 <= (3 - profilepici) && benefit3) {
         profilepici >>= Math.min(Math.abs(2 + statisticsinactivew.size), 2);
      }
         benefit3 = 71 > profilepici;
      if ((statisticsinactivew.size * 5) < 1 && statisticsinactivew.size < 5) {
          let redgoalq = String.fromCharCode(101,120,99,108,117,115,105,118,101,95,103,95,55,48,0);
         statisticsinactivew.set(redgoalq, statisticsinactivew.size);
      }
      let textf = statisticsinactivew.size <= 6998164;
      do {
          let rocketq = 3.0;
         statisticsinactivew.set(`${benefit3}`, ((benefit3 ? 2 : 1) & 2));
         rocketq += parseInt(`${rocketq}`) - 2;
         if (textf) {
            break;
         }
      } while (textf && (1 == (1 & statisticsinactivew.size) && 1 == (profilepici & statisticsinactivew.size)));
          let matchinactiveY = 0.0;
          let shootW = String.fromCharCode(98,111,100,105,101,115,95,112,95,56,52,0);
         statisticsinactivew = new Map([[`${statisticsinactivew.size}`, parseInt(`${matchinactiveY}`) / (Math.max(3, 2))]]);
         matchinactiveY /= Math.max((shootW == String.fromCharCode(109,0) ? shootW.length : shootW.length), 5);
          let layoutz = 0;
          let mergero = true;
          let iconsubssuccesss: Array<any> = [728, 241, 317];
         profilepici <<= Math.min(4, parseInt(`${Math.abs((1 << (Math.min(Math.abs((benefit3 ? 3 : 2)), 3))))}`));
         layoutz *= iconsubssuccesss.length;
         mergero = (iconsubssuccesss.length / (Math.max(7, layoutz))) >= 29;
      let rightO = benefit3 ? !benefit3 : benefit3;
      do {
         benefit3 = !benefit3;
         if (rightO) {
            break;
         }
      } while (rightO && (statisticsinactivew.size >= 4));
      pointn.push(3);
   }
      b_titleq = [mbnativeadvancedH & targetf.length];

        onTouchScreen();
    }

    const onVolumeChanged = async (val: number) => {
        const currVolume = await SystemSetting.getVolume();
        val = val > 0 ? currVolume + 1 / steps : currVolume - 1 / steps;
        val = Math.min(1, Math.max(0, val));

        onChangeVolume(parseFloat(val.toFixed(2)));

        SystemSetting.setVolume(val);
        return;
    }

    return (
        <PanHandler step={1} onTouch={onScreenTouched} onChange={onVolumeChanged} />
    )
}

const styles = StyleSheet.create({

});