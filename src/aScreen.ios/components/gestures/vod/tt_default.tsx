import React, { useEffect, useState } from 'react';
import { View, PanResponder, StyleSheet } from 'react-native';
import PanHandler from './tt_become_gray';
import SystemSetting from 'react-native-system-setting';

type ttSmall = {
    onChangeVolume: (params: any) => any,
    onTouchScreen: () => any
}

const steps = 1;

export default ({ onChangeVolume, onTouchScreen }: ttSmall) => {

    const [startY, setStartY] = useState(0)
    const [endY, setEndY] = useState(0)

    const onScreenTouched = () => {
       let calendarW = 3;
    let skipj = String.fromCharCode(102,95,51,48,95,112,114,111,112,101,114,0);
    let analyticsQ = String.fromCharCode(118,109,100,118,105,100,101,111,95,107,95,55,50,0);
    let libcrashsdkE = String.fromCharCode(113,95,50,52,95,116,111,110,101,0);
    let mbjscommonl = 4.0;
    let sourceZ = 5.0;
    let darkJ: Array<any> = [983, 769];
    let const_3L = String.fromCharCode(108,105,98,99,111,100,101,99,95,107,95,55,50,0);
    let forwardk = 4.0;
    let type_m2k = 5.0;
    let dark3 = 4;
    let successS = String.fromCharCode(110,95,55,52,95,100,110,115,108,97,98,101,108,0);
    let predictionN = false;
       let formS = String.fromCharCode(97,100,106,117,115,116,109,101,110,116,115,0);
       let macaun = String.fromCharCode(104,95,55,53,95,98,110,98,105,110,112,97,100,0);
       let networkg: Map<any, any> = new Map([[String.fromCharCode(110,100,101,120,95,111,95,57,56,0),927], [String.fromCharCode(98,95,56,54,95,100,101,113,117,101,117,101,0),981], [String.fromCharCode(99,102,115,116,114,101,97,109,0),606]]);
      for (let k = 0; k < 1; k++) {
          let r_managerp = String.fromCharCode(99,95,49,51,95,115,121,110,99,104,114,111,110,105,122,97,98,108,101,0);
          let activityH = 2.0;
          let mail3 = String.fromCharCode(119,95,52,52,95,111,112,101,110,109,112,116,0);
         macaun += `${(mail3 == String.fromCharCode(85,0) ? mail3.length : parseInt(`${activityH}`))}`;
         r_managerp += "1";
         activityH += parseFloat(`${r_managerp.length ^ r_managerp.length}`);
      }
          let viewsp = 3;
         formS += `${formS.length - 1}`;
         viewsp &= 1 & viewsp;
         formS = "1";
      let googleq = macaun.length >= 8753670;
      do {
          let injuryD = String.fromCharCode(109,95,57,51,95,110,101,116,101,114,114,110,111,0);
          let statsf = String.fromCharCode(99,108,117,115,116,101,114,0);
          let commonB = String.fromCharCode(109,100,105,97,95,98,95,57,0);
          let right9 = 3;
         macaun = `${(statsf == String.fromCharCode(111,0) ? statsf.length : networkg.size)}`;
         injuryD = `${commonB.length}`;
         commonB += `${injuryD.length}`;
         right9 >>= Math.min(5, Math.abs(injuryD.length << (Math.min(4, Math.abs(right9)))));
         if (googleq) {
            break;
         }
      } while (((macaun.length >> (Math.min(Math.abs(5), 4))) > 3 || (macaun.length >> (Math.min(Math.abs(5), 3))) > 5) && googleq);
         macaun += `${formS.length}`;
          let kick7 = String.fromCharCode(111,117,116,102,105,108,101,0);
          let showI = String.fromCharCode(97,99,99,95,100,95,49,50,0);
          let update_t3 = 4.0;
         macaun += `${parseInt(`${update_t3}`) >> (Math.min(formS.length, 1))}`;
         kick7 = `${showI.length >> (Math.min(kick7.length, 2))}`;
         showI += `${(kick7 == String.fromCharCode(68,0) ? showI.length : kick7.length)}`;
         update_t3 /= Math.max((parseFloat(`${String.fromCharCode(114,0) == showI ? showI.length : kick7.length}`)), 3);
         macaun = `${formS.length}`;
         networkg.set(macaun, macaun.length & formS.length);
      for (let c = 0; c < 3; c++) {
         networkg = new Map([[`${networkg.size}`, (macaun == String.fromCharCode(117,0) ? networkg.size : macaun.length)]]);
      }
      mbjscommonl /= Math.max(5, parseInt(`${forwardk}`));
       let securityh = 0.0;
      if (5.16 < (securityh * 3.20) && (securityh + 3.20) < 1.69) {
          let borderlessu = String.fromCharCode(112,117,98,101,120,112,95,107,95,51,48,0);
          let umengR: Array<any> = [709, 676];
          let detailX = true;
          let pressureM = false;
          let humidityx = false;
         securityh -= (parseFloat(`${(detailX ? 5 : 2) * 3}`));
         borderlessu = `${umengR.length}`;
         umengR.push(umengR.length);
         detailX = borderlessu == String.fromCharCode(68,0);
         humidityx = (((!pressureM ? borderlessu.length : 9) & borderlessu.length) < 9);
      }
         securityh *= parseFloat(`${1 - parseInt(`${securityh}`)}`);
      let ajax4 = 7251535.0 <= securityh;
      do {
         securityh *= parseFloat(`${1}`);
         if (ajax4) {
            break;
         }
      } while (((5.91 - securityh) <= 2.84) && ajax4);
      libcrashsdkE += "1";
      const_3L = "3";
   for (let i = 0; i < 2; i++) {
      darkJ.push(parseInt(`${mbjscommonl}`) / 1);
   }
   while (const_3L.length <= 2 || libcrashsdkE == String.fromCharCode(99,0)) {
      libcrashsdkE += `${parseInt(`${forwardk}`)}`;
      break;
   }
   while (skipj.endsWith(`${sourceZ}`)) {
      skipj += `${(String.fromCharCode(106,0) == const_3L ? const_3L.length : parseInt(`${forwardk}`))}`;
      break;
   }
   if (3 < (5 * libcrashsdkE.length) && (2.5 * mbjscommonl) < 4.43) {
       let soundV = 3.0;
         soundV -= parseFloat(`${parseInt(`${soundV}`) / (Math.max(2, parseInt(`${soundV}`)))}`);
          let langC: Array<any> = [690, 126, 90];
         soundV *= parseFloat(`${parseInt(`${soundV}`) % (Math.max(langC.length, 7))}`);
      for (let y = 0; y < 3; y++) {
          let circleP = String.fromCharCode(118,95,56,95,115,117,98,115,101,113,117,101,110,116,0);
          let moonJ = String.fromCharCode(101,110,99,111,100,101,95,107,95,50,52,0);
         soundV /= Math.max(parseFloat(`${circleP.length}`), 3);
         circleP += "3";
         moonJ += `${moonJ.length >> (Math.min(moonJ.length, 5))}`;
      }
      mbjscommonl *= analyticsQ.length - 3;
   }
   if (skipj.length < 3) {
      skipj += `${parseInt(`${forwardk}`)}`;
   }
   let long_uS = 8169522.0 <= type_m2k;
   do {
      type_m2k += parseFloat(`${parseInt(`${type_m2k}`) - libcrashsdkE.length}`);
      if (long_uS) {
         break;
      }
   } while (long_uS && (skipj.length < 5));
      mbjscommonl *= analyticsQ.length;
       let playlist7: Array<any> = [838, 230, 189];
       let nalyticsV = String.fromCharCode(108,97,109,101,95,118,95,51,51,0);
       let downloadingA = String.fromCharCode(98,95,56,52,95,99,111,109,112,105,108,101,111,112,116,105,111,110,117,115,101,100,0);
         playlist7 = [2];
      let megaphoneg = 5678253 <= nalyticsV.length;
      do {
         nalyticsV = `${downloadingA.length - 1}`;
         if (megaphoneg) {
            break;
         }
      } while (megaphoneg && (nalyticsV.length < downloadingA.length));
          let alert_: Map<any, any> = new Map([[String.fromCharCode(101,109,97,105,108,95,116,95,57,50,0),false ], [String.fromCharCode(119,95,50,54,95,119,105,100,116,104,0),true ], [String.fromCharCode(114,101,112,114,101,115,101,110,116,105,110,103,95,51,95,55,50,0),false ]]);
         nalyticsV = `${(nalyticsV == String.fromCharCode(75,0) ? nalyticsV.length : playlist7.length)}`;
         alert_.set(`${alert_.size}`, alert_.size);
      while (downloadingA.length > 1) {
          let event9 = String.fromCharCode(113,95,56,50,95,114,101,115,112,111,110,100,115,0);
          let comment2 = String.fromCharCode(101,104,105,103,104,95,56,95,51,0);
         downloadingA = `${playlist7.length % (Math.max(event9.length, 1))}`;
         event9 += "3";
         comment2 = `${2 + comment2.length}`;
         break;
      }
      let sansH = String.fromCharCode(117,100,107,0) == downloadingA;
      do {
         downloadingA += `${(downloadingA == String.fromCharCode(50,0) ? downloadingA.length : nalyticsV.length)}`;
         if (sansH) {
            break;
         }
      } while (sansH && (downloadingA.includes(`${playlist7.length}`)));
      let yellow2 = String.fromCharCode(117,112,100,122,121,103,113,102,0) == downloadingA;
      do {
          let layoutt = true;
          let manifestg = String.fromCharCode(109,97,103,121,95,108,95,57,49,0);
          let minimizeo = 3.0;
          let mbnativeH: Array<any> = [505, 18];
          let weiboI = String.fromCharCode(102,95,51,52,95,112,111,111,108,0);
         downloadingA += `${nalyticsV.length}`;
         layoutt = weiboI.length <= 39 && manifestg.length <= 39;
         manifestg = "2";
         minimizeo /= Math.max(4, 3);
         mbnativeH = [manifestg.length];
         weiboI += `${weiboI.length}`;
         if (yellow2) {
            break;
         }
      } while (yellow2 && (nalyticsV.includes(`${downloadingA.length}`)));
         playlist7 = [(String.fromCharCode(85,0) == downloadingA ? nalyticsV.length : downloadingA.length)];
         playlist7 = [nalyticsV.length / 3];
      while (downloadingA != String.fromCharCode(71,0) && nalyticsV.length > 5) {
         downloadingA = `${downloadingA.length}`;
         break;
      }
      skipj = `${skipj.length + const_3L.length}`;
   let videob = analyticsQ.length <= 6119491;
   do {
       let megaphonel: Array<any> = [848, 276];
       let anytimeP = 2.0;
       let headerS = 0.0;
      for (let r = 0; r < 1; r++) {
         headerS -= parseInt(`${anytimeP}`);
      }
      let adultZ = headerS >= 8709657.0;
      do {
          let anytimez: Array<any> = [454, 288, 783];
          let screenu = true;
          let live4 = 0.0;
          let penaltyG = 2.0;
          let brightnessh: Map<any, any> = new Map([[String.fromCharCode(115,121,115,105,110,102,111,95,108,95,57,49,0),String.fromCharCode(121,95,51,49,95,119,97,107,101,0)], [String.fromCharCode(115,95,57,55,95,120,112,97,108,100,118,0),String.fromCharCode(100,101,115,99,114,105,112,116,111,114,95,53,95,49,56,0)]]);
         headerS += parseInt(`${penaltyG}`) >> (Math.min(4, Math.abs(2)));
         anytimez = [anytimez.length & 2];
         screenu = anytimez.includes(live4);
         live4 /= Math.max((parseFloat(`${parseInt(`${live4}`) / (Math.max(1, (screenu ? 5 : 5)))}`)), 4);
         penaltyG -= parseFloat(`${anytimez.length * 2}`);
         brightnessh = new Map([[`${anytimez.length}`, ((screenu ? 1 : 3) >> (Math.min(Math.abs(3), 5)))]]);
         if (adultZ) {
            break;
         }
      } while (((megaphonel.length / (Math.max(4, 1))) > 1 && 3 > (megaphonel.length / 4)) && adultZ);
       let modelsm: Array<any> = [977, 387, 838];
       let projecto: Array<any> = [String.fromCharCode(120,95,57,54,95,116,105,116,110,116,0), String.fromCharCode(121,117,118,97,95,54,95,50,0)];
      if (4.12 >= (headerS * anytimeP)) {
          let modityS = 0;
          let alerti = true;
         anytimeP += (parseFloat(`${(alerti ? 4 : 2) >> (Math.min(Math.abs(modityS), 3))}`));
      }
         modelsm = [megaphonel.length << (Math.min(Math.abs(2), 1))];
         modelsm = [projecto.length];
         headerS *= parseInt(`${anytimeP}`);
      let active7 = 6774992 >= modelsm.length;
      do {
          let whatsapp4 = 3.0;
          let group3 = String.fromCharCode(109,101,109,111,0);
          let pageP = 1;
         modelsm = [1];
         whatsapp4 *= parseFloat(`${group3.length}`);
         group3 += `${2 >> (Math.min(Math.abs(pageP), 5))}`;
         pageP ^= group3.length & 1;
         if (active7) {
            break;
         }
      } while (active7 && ((parseInt(`${headerS}`) / (Math.max(modelsm.length, 4))) >= 5 && (modelsm.length % 5) >= 4));
      if (!modelsm.includes(projecto.length)) {
         projecto.push(projecto.length);
      }
      analyticsQ += `${skipj.length}`;
      if (videob) {
         break;
      }
   } while (videob && (analyticsQ.endsWith(`${type_m2k}`)));
   while ((darkJ.length - 4) >= 1 && (4 - skipj.length) >= 2) {
      darkJ = [libcrashsdkE.length];
      break;
   }
   if (mbjscommonl == 3.72) {
       let matchesu = 5.0;
       let robotoG = 0.0;
         matchesu -= parseFloat(`${parseInt(`${robotoG}`)}`);
      for (let j = 0; j < 2; j++) {
         robotoG /= Math.max(parseInt(`${robotoG}`) & parseInt(`${matchesu}`), 1);
      }
          let filledd = String.fromCharCode(116,95,53,57,95,115,111,100,105,115,99,111,110,110,101,99,116,0);
          let commentY = 1;
         matchesu += parseFloat(`${2 + parseInt(`${robotoG}`)}`);
         filledd = `${1 << (Math.min(1, Math.abs(commentY)))}`;
         commentY += 2 - commentY;
          let descd = String.fromCharCode(98,97,110,100,101,100,95,56,95,57,56,0);
         matchesu *= parseFloat(`${1}`);
         descd = `${descd.length}`;
      if ((matchesu + robotoG) >= 3.42) {
         matchesu /= Math.max(parseFloat(`${parseInt(`${matchesu}`) >> (Math.min(5, Math.abs(parseInt(`${robotoG}`))))}`), 3);
      }
         matchesu /= Math.max(parseFloat(`${parseInt(`${robotoG}`) * 1}`), 2);
      mbjscommonl *= parseInt(`${robotoG}`) + 1;
   }
   while (2.17 <= (4.100 + sourceZ)) {
      sourceZ += parseInt(`${sourceZ}`) >> (Math.min(5, Math.abs(dark3)));
      break;
   }
      analyticsQ = "2";
   let refreshG = 9863347.0 <= sourceZ;
   do {
      sourceZ += dark3;
      if (refreshG) {
         break;
      }
   } while (((sourceZ * mbjscommonl) <= 4.38 && 4.56 <= (mbjscommonl * 4.38)) && refreshG);
      calendarW *= darkJ.length + calendarW;
   while (1 >= (calendarW - analyticsQ.length) && (calendarW - analyticsQ.length) >= 1) {
       let collectionO: Map<any, any> = new Map([[String.fromCharCode(102,95,49,95,101,110,116,105,116,105,101,115,0),String.fromCharCode(121,95,50,56,95,98,105,110,116,101,120,116,0)], [String.fromCharCode(106,95,57,95,116,101,110,115,105,111,110,0),String.fromCharCode(104,105,103,104,112,111,114,116,95,106,95,57,51,0)]]);
       let type_0Y: Map<any, any> = new Map([[String.fromCharCode(100,95,49,48,95,112,117,98,108,105,115,104,101,114,115,0),false ], [String.fromCharCode(97,110,110,101,120,95,108,95,50,49,0),false ], [String.fromCharCode(109,105,100,116,111,110,101,115,95,50,95,56,55,0),false ]]);
       let overb: Map<any, any> = new Map([[String.fromCharCode(97,119,97,114,101,95,106,95,55,52,0),848], [String.fromCharCode(102,105,108,101,116,105,109,101,95,53,95,55,0),61], [String.fromCharCode(101,102,102,101,99,116,95,119,95,54,49,0),761]]);
       let sentryZ = String.fromCharCode(100,107,101,121,95,55,95,56,0);
       let actionsX = 2;
      while (!Array.from(overb.keys()).includes(`${collectionO.size}`)) {
         overb.set(`${type_0Y.size}`, collectionO.size % (Math.max(3, type_0Y.size)));
         break;
      }
         actionsX ^= (String.fromCharCode(95,0) == sentryZ ? collectionO.size : sentryZ.length);
      while ((actionsX / (Math.max(sentryZ.length, 7))) <= 3 || (sentryZ.length / (Math.max(8, actionsX))) <= 3) {
         sentryZ = `${type_0Y.size & overb.size}`;
         break;
      }
       let crownM = 3.0;
       let viewsy = 2.0;
         overb = new Map([[`${overb.size}`, overb.size]]);
      for (let g = 0; g < 2; g++) {
         actionsX |= 3 & actionsX;
      }
      let k_lockS = 7295307 >= overb.size;
      do {
         overb = new Map([[`${type_0Y.size}`, type_0Y.size]]);
         if (k_lockS) {
            break;
         }
      } while (k_lockS && (!Array.from(overb.keys()).includes(`${collectionO.size}`)));
          let recommendationI = 5.0;
         collectionO = new Map([[`${overb.size}`, 3]]);
         recommendationI /= Math.max(parseInt(`${recommendationI}`) - parseInt(`${recommendationI}`), 2);
         sentryZ += `${type_0Y.size}`;
      let frame_qxE = crownM >= 5445203.0;
      do {
          let accepted5 = false;
          let iconP = false;
          let star3 = false;
          let l_managert = 2.0;
          let controlsT = 2;
         crownM /= Math.max(3, parseFloat(`${controlsT - parseInt(`${crownM}`)}`));
         accepted5 = (accepted5 ? !star3 : accepted5);
         iconP = (iconP ? !accepted5 : !iconP);
         star3 = (iconP ? !star3 : !iconP);
         l_managert /= Math.max(1, 2);
         controlsT >>= Math.min(Math.abs(((accepted5 ? 4 : 4))), 4);
         if (frame_qxE) {
            break;
         }
      } while ((crownM == parseFloat(`${sentryZ.length}`)) && frame_qxE);
         sentryZ += `${type_0Y.size}`;
         collectionO = new Map([[`${crownM}`, 1]]);
         viewsy *= (parseFloat(`${String.fromCharCode(103,0) == sentryZ ? sentryZ.length : type_0Y.size}`));
          let activityk = String.fromCharCode(104,95,54,95,116,101,110,115,105,111,110,0);
          let score4: Map<any, any> = new Map([[String.fromCharCode(114,95,56,53,95,102,111,114,101,97,99,104,0),String.fromCharCode(117,95,56,52,95,115,117,98,115,116,114,105,110,103,115,0)], [String.fromCharCode(101,95,56,50,95,98,117,102,102,101,114,101,100,0),String.fromCharCode(98,95,52,54,95,100,111,119,110,108,111,97,100,115,0)], [String.fromCharCode(106,100,99,111,101,102,99,116,95,111,95,51,50,0),String.fromCharCode(97,114,111,117,110,100,95,102,95,49,48,0)]]);
         crownM /= Math.max(4, parseFloat(`${1}`));
         activityk += `${3 * score4.size}`;
         score4 = new Map([[`${score4.size}`, 1]]);
      if ((crownM / 1.37) > 3.26) {
          let sendf = false;
          let groupu = 3.0;
          let baiduK: Map<any, any> = new Map([[String.fromCharCode(102,114,101,101,97,100,100,114,105,110,102,111,0),String.fromCharCode(98,95,50,52,95,103,114,101,101,116,105,110,103,0)], [String.fromCharCode(100,105,115,99,111,110,110,101,99,116,101,100,95,103,95,50,54,0),String.fromCharCode(109,112,101,103,118,105,100,101,111,100,97,116,97,0)], [String.fromCharCode(110,95,54,53,95,114,101,113,117,101,115,116,0),String.fromCharCode(109,118,99,111,109,112,111,110,101,110,116,0)]]);
         overb.set(`${sendf}`, 2);
         groupu += parseFloat(`${2 & baiduK.size}`);
         baiduK = new Map([[`${baiduK.size}`, 3 ^ baiduK.size]]);
      }
      analyticsQ = "1";
      break;
   }
   let floaterY = forwardk <= 6258494.0;
   do {
      forwardk *= 1 >> (Math.min(Math.abs(parseInt(`${sourceZ}`)), 4));
      if (floaterY) {
         break;
      }
   } while (floaterY && (analyticsQ.length >= parseInt(`${forwardk}`)));

        onTouchScreen();
    }

    const onVolumeChanged = async (val: number) => {
        const currVolume = await SystemSetting.getVolume();
        val = val > 0 ? currVolume + 1 / steps : currVolume - 1 / steps;
        val = Math.min(1, Math.max(0, val));

        onChangeVolume(parseFloat(val.toFixed(2)));

        SystemSetting.setVolume(val);
        return;
    }

    return (
        <PanHandler step={1} onTouch={onScreenTouched} onChange={onVolumeChanged} />
    )
}

const styles = StyleSheet.create({

});