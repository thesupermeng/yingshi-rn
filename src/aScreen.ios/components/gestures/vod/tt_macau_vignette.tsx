import React, { useEffect, useState } from 'react';
import { View, PanResponder, StyleSheet } from 'react-native';
import PanHandler from './tt_become_gray';
import SystemSetting from 'react-native-system-setting';

type ttSmall = {
    onProgressChange: (params: any) => any,
    onTouchScreen: () => any
}

const steps = 10;

export default ({ onProgressChange, onTouchScreen }: ttSmall) => {
    const onScreenTouched = () => {
       let gifta = 2.0;
    let commonO = false;
    let episodey: Map<any, any> = new Map([[String.fromCharCode(97,98,111,114,116,95,50,95,49,56,0),164], [String.fromCharCode(100,95,54,53,95,97,114,114,111,119,0),559], [String.fromCharCode(114,101,116,114,105,101,118,101,100,95,54,95,52,50,0),844]]);
    let switch_z0 = String.fromCharCode(109,109,99,111,115,95,120,95,57,57,0);
    let lesss = 1.0;
    let suggestionL: Array<any> = [219, 812, 53];
    let tickh = String.fromCharCode(114,95,57,50,95,103,101,116,110,112,97,115,115,101,115,0);
    let blackI = String.fromCharCode(104,97,98,108,101,95,103,95,50,53,0);
    let roomI = 1;
    let internetI = String.fromCharCode(112,97,114,115,101,95,110,95,56,48,0);
    let trasho = 4.0;
    let actionD: Map<any, any> = new Map([[String.fromCharCode(115,95,49,50,95,99,114,101,100,101,110,116,105,97,108,0),784], [String.fromCharCode(111,95,55,50,95,97,97,99,115,98,114,100,97,116,97,0),360]]);
    let hooksC = String.fromCharCode(109,117,108,116,105,99,111,100,101,99,95,101,95,49,51,0);
   while (!commonO || tickh.length > 2) {
       let borderlessh = String.fromCharCode(112,115,101,117,100,111,99,111,108,111,114,95,104,95,55,50,0);
       let streamingb = 5.0;
       let rewardY = String.fromCharCode(108,105,109,105,116,115,95,104,95,50,50,0);
       let combinef = 2.0;
       let scheduleM = String.fromCharCode(105,110,112,111,115,95,107,95,57,56,0);
          let mini5: Array<any> = [150, 681];
          let plashQ = String.fromCharCode(115,111,114,116,95,49,95,55,56,0);
          let g_positionv = 4.0;
         scheduleM += `${borderlessh.length}`;
         mini5.push(2 - parseInt(`${g_positionv}`));
         plashQ = `${mini5.length}`;
         g_positionv -= parseInt(`${g_positionv}`) % (Math.max(mini5.length, 7));
       let blacki = 4;
          let showE: Array<any> = [47, 884, 771];
         scheduleM = `${parseInt(`${combinef}`)}`;
         showE.push(showE.length + showE.length);
          let clear0: Array<any> = [708, 593];
          let screen9 = String.fromCharCode(117,95,49,50,95,99,97,115,99,97,100,101,100,0);
         blacki -= (scheduleM == String.fromCharCode(113,0) ? scheduleM.length : borderlessh.length);
         clear0.push(clear0.length | screen9.length);
         screen9 = `${clear0.length}`;
      if (scheduleM.startsWith(`${combinef}`)) {
         scheduleM = "2";
      }
      let verticalD = 6144577 >= scheduleM.length;
      do {
         scheduleM = `${blacki}`;
         if (verticalD) {
            break;
         }
      } while ((scheduleM.startsWith(`${combinef}`)) && verticalD);
      let typingD = rewardY.length <= 5385581;
      do {
         rewardY += `${rewardY.length}`;
         if (typingD) {
            break;
         }
      } while ((rewardY.endsWith(`${combinef}`)) && typingD);
          let w_centerC = String.fromCharCode(116,95,51,54,95,122,108,105,98,112,114,105,109,101,0);
         rewardY += `${parseInt(`${combinef}`)}`;
         w_centerC += `${w_centerC.length + w_centerC.length}`;
      while (1.46 < (1.16 / (Math.max(1, streamingb)))) {
          let favoriteD = String.fromCharCode(107,95,54,95,101,105,103,104,116,0);
          let customC = 2.0;
          let refreshR = 4.0;
          let canvasf = 2;
          let helper2 = 3.0;
         blacki &= blacki & parseInt(`${helper2}`);
         favoriteD += `${favoriteD.length}`;
         customC += parseInt(`${refreshR}`);
         refreshR /= Math.max(parseFloat(`${canvasf % (Math.max(1, 8))}`), 1);
         canvasf >>= Math.min(Math.abs(canvasf % (Math.max(6, favoriteD.length))), 4);
         helper2 *= parseFloat(`${1 * favoriteD.length}`);
         break;
      }
      while (1 < (parseInt(`${streamingb}`) - scheduleM.length)) {
         scheduleM = `${rewardY.length & 1}`;
         break;
      }
      for (let r = 0; r < 3; r++) {
          let telegramf: Array<any> = [390, 284];
          let switch_v7l = 4.0;
          let pangleO: Array<any> = [830, 314];
         combinef /= Math.max(parseFloat(`${1}`), 3);
         telegramf.push(pangleO.length);
         switch_v7l -= parseFloat(`${pangleO.length}`);
      }
         rewardY += `${3 / (Math.max(3, blacki))}`;
         blacki &= 2;
          let rewindn: Array<any> = [String.fromCharCode(115,95,54,57,95,115,108,97,115,104,101,115,0), String.fromCharCode(111,115,109,111,115,95,98,95,57,48,0), String.fromCharCode(114,101,102,101,114,114,97,108,95,104,95,57,52,0)];
         rewardY = `${borderlessh.length}`;
         rewindn = [2 & rewindn.length];
         rewardY = `${parseInt(`${combinef}`)}`;
      tickh = `${parseInt(`${streamingb}`) + parseInt(`${gifta}`)}`;
      break;
   }
   for (let u = 0; u < 3; u++) {
      trasho /= Math.max(5, (String.fromCharCode(68,0) == internetI ? switch_z0.length : internetI.length));
   }
   let chartg = internetI.length <= 9095187;
   do {
      internetI += `${((commonO ? 4 : 4) << (Math.min(Math.abs(parseInt(`${trasho}`)), 4)))}`;
      if (chartg) {
         break;
      }
   } while ((4 < internetI.length) && chartg);
      tickh += "3";
   if (2.85 == (gifta / (Math.max(2.73, 10))) && !commonO) {
       let change1 = false;
       let paginationv = 1.0;
       let action2 = false;
         paginationv *= (parseFloat(`${(action2 ? 3 : 5) + parseInt(`${paginationv}`)}`));
      while (change1 && 2.93 <= (paginationv - 2.64)) {
         paginationv *= parseFloat(`${3 - parseInt(`${paginationv}`)}`);
         break;
      }
          let thumbnailW = String.fromCharCode(119,95,51,51,95,108,105,102,101,0);
          let videoq = String.fromCharCode(104,95,51,53,95,112,117,114,103,101,0);
         action2 = change1;
         thumbnailW = `${thumbnailW.length}`;
         videoq = `${videoq.length}`;
         change1 = action2;
      let sheetu = paginationv >= 6853829.0;
      do {
         paginationv += (parseFloat(`${(action2 ? 1 : 3) - parseInt(`${paginationv}`)}`));
         if (sheetu) {
            break;
         }
      } while ((paginationv < 2.60) && sheetu);
         change1 = change1 || paginationv > 63.92;
       let sinad: Array<any> = [709, 541];
      while (!change1) {
         change1 = 25.7 >= paginationv || !change1;
         break;
      }
         action2 = !action2;
      gifta /= Math.max(roomI, 2);
   }
      lesss -= parseFloat(`${suggestionL.length}`);
   while ((internetI.length * roomI) > 3 && 3 > (roomI * internetI.length)) {
       let hook9 = true;
       let googlea = 0.0;
       let math8 = String.fromCharCode(101,95,54,55,95,109,101,101,116,117,112,0);
       let twitterv = String.fromCharCode(104,101,116,97,95,112,95,50,50,0);
       let filterG = String.fromCharCode(111,95,55,52,95,118,108,98,117,102,0);
         googlea += parseFloat(`${3 >> (Math.min(4, filterG.length))}`);
          let overlay_ = 4.0;
          let confirmationS: Array<any> = [315, 223, 847];
         twitterv += `${(String.fromCharCode(90,0) == math8 ? math8.length : confirmationS.length)}`;
         overlay_ /= Math.max(parseInt(`${overlay_}`), 5);
         confirmationS = [2 << (Math.min(Math.abs(parseInt(`${overlay_}`)), 4))];
      let catalogq = googlea >= 9846223.0;
      do {
          let submitc = String.fromCharCode(105,110,116,109,97,116,104,95,103,95,51,0);
         googlea += parseFloat(`${2}`);
         submitc += `${submitc.length}`;
         if (catalogq) {
            break;
         }
      } while (catalogq && ((2 >> (Math.min(3, twitterv.length))) <= 5));
      while ((3 >> (Math.min(3, math8.length))) == 5) {
         math8 = `${filterG.length}`;
         break;
      }
      if (math8.length < 4) {
         googlea -= parseFloat(`${math8.length >> (Math.min(Math.abs(2), 1))}`);
      }
          let catagoryT = String.fromCharCode(110,95,49,48,95,109,101,97,115,117,114,101,114,0);
          let user2 = String.fromCharCode(111,95,56,50,95,99,111,109,112,101,110,115,97,116,101,100,0);
         math8 = `${1 * math8.length}`;
         catagoryT += `${3 | catagoryT.length}`;
         user2 += `${(user2 == String.fromCharCode(68,0) ? user2.length : catagoryT.length)}`;
      for (let u = 0; u < 2; u++) {
          let nnewsx = 3.0;
          let subsK = 4;
         googlea *= parseFloat(`${subsK}`);
         nnewsx -= parseFloat(`${3 << (Math.min(Math.abs(parseInt(`${nnewsx}`)), 3))}`);
         subsK |= parseInt(`${nnewsx}`) | parseInt(`${nnewsx}`);
      }
      for (let c = 0; c < 1; c++) {
          let libcrashsdkM = 3;
          let playO = 5.0;
          let smallD = 5;
          let phoneE = String.fromCharCode(99,111,111,114,100,95,107,95,49,48,0);
          let window_326 = 2.0;
         math8 += `${math8.length - 2}`;
         libcrashsdkM /= Math.max(parseInt(`${window_326}`), 2);
         playO *= parseFloat(`${1}`);
         smallD |= parseInt(`${playO}`) | parseInt(`${window_326}`);
         phoneE = `${phoneE.length}`;
      }
          let sharede = 2.0;
          let smallC = String.fromCharCode(102,105,110,110,101,121,95,50,95,51,55,0);
         twitterv += `${filterG.length * parseInt(`${googlea}`)}`;
         sharede += parseFloat(`${parseInt(`${sharede}`) - 2}`);
         smallC += `${parseInt(`${sharede}`) + smallC.length}`;
          let humidityD = String.fromCharCode(104,95,54,49,95,120,105,110,103,0);
          let benefitG = 2;
         filterG += "1";
         humidityD = `${(String.fromCharCode(104,0) == humidityD ? benefitG : humidityD.length)}`;
         benefitG <<= Math.min(Math.abs(humidityD.length >> (Math.min(5, Math.abs(benefitG)))), 1);
      while (!hook9) {
         googlea *= parseFloat(`${math8.length}`);
         break;
      }
          let lineU = String.fromCharCode(109,101,100,105,117,109,95,115,95,54,48,0);
          let readO: Map<any, any> = new Map([[String.fromCharCode(114,101,115,104,117,102,102,108,101,95,117,95,50,54,0),String.fromCharCode(105,95,53,51,95,117,121,118,121,0)], [String.fromCharCode(122,95,49,53,95,105,110,100,101,118,115,0),String.fromCharCode(107,95,51,53,95,118,105,97,98,108,101,0)]]);
          let championo = String.fromCharCode(112,95,56,49,95,99,97,115,116,101,100,0);
         hook9 = (readO.size * twitterv.length) == 69;
         lineU = `${lineU.length ^ 2}`;
         readO = new Map([[championo, championo.length]]);
      while (!hook9 && filterG.length < 4) {
         filterG = `${((hook9 ? 4 : 4) / (Math.max(parseInt(`${googlea}`), 3)))}`;
         break;
      }
         twitterv = "1";
      if (2 < twitterv.length) {
         twitterv = `${parseInt(`${googlea}`) & 2}`;
      }
      roomI |= ((hook9 ? 4 : 5) * parseInt(`${trasho}`));
      break;
   }
      gifta *= parseInt(`${gifta}`);
       let macaub: Map<any, any> = new Map([[String.fromCharCode(99,95,49,55,95,107,105,110,100,0),208], [String.fromCharCode(111,95,48,95,111,112,116,97,98,108,101,0),37]]);
       let canvasb = 2;
      let list9 = 9192051 >= canvasb;
      do {
         canvasb += canvasb ^ macaub.size;
         if (list9) {
            break;
         }
      } while (list9 && ((5 >> (Math.min(5, Math.abs(canvasb)))) > 3));
         macaub.set(`${canvasb}`, macaub.size ^ 3);
         canvasb <<= Math.min(2, Math.abs(canvasb / (Math.max(macaub.size, 4))));
         macaub.set(`${canvasb}`, macaub.size);
      for (let s = 0; s < 3; s++) {
         macaub.set(`${canvasb}`, 1 % (Math.max(3, canvasb)));
      }
       let downloadedL = String.fromCharCode(100,101,108,115,117,112,101,114,95,106,95,54,57,0);
      tickh = `${1 % (Math.max(4, parseInt(`${gifta}`)))}`;
   for (let x = 0; x < 2; x++) {
      trasho *= parseInt(`${lesss}`);
   }
   if ((suggestionL.length << (Math.min(5, Math.abs(roomI)))) < 1) {
       let championF: Map<any, any> = new Map([[String.fromCharCode(115,116,114,109,95,51,95,50,57,0),93], [String.fromCharCode(99,95,57,54,95,112,101,101,108,111,102,102,0),313], [String.fromCharCode(108,101,118,101,108,115,95,121,95,54,49,0),107]]);
      while (championF.get(`${championF.size}`) != null) {
         championF.set(`${championF.size}`, championF.size);
         break;
      }
       let store3: Map<any, any> = new Map([[String.fromCharCode(97,95,49,53,95,100,115,116,114,0),884], [String.fromCharCode(118,95,54,95,99,111,109,112,117,116,101,0),395]]);
          let plusM = String.fromCharCode(105,114,99,97,109,95,56,95,50,48,0);
         store3 = new Map([[`${store3.size}`, store3.size]]);
         plusM = `${3 << (Math.min(2, plusM.length))}`;
      roomI |= 3;
   }
      actionD.set(tickh, 3 - internetI.length);
      tickh += "2";
   while ((roomI * 1) < 1 || (roomI * 1) < 4) {
       let list7: Array<any> = [751, 651, 409];
       let debugE = String.fromCharCode(106,112,101,103,99,111,109,112,95,105,95,57,54,0);
       let q_centerk = 4;
       let debugf = 1;
      if ((debugE.length % 5) >= 1 && 2 >= (debugE.length % 5)) {
         debugf -= debugE.length;
      }
          let blacklisty = 0;
         q_centerk |= debugE.length << (Math.min(1, Math.abs(q_centerk)));
         blacklisty <<= Math.min(5, Math.abs(blacklisty));
      while (!debugE.endsWith(`${debugf}`)) {
          let dragr = String.fromCharCode(115,113,108,105,116,101,101,120,116,95,98,95,57,52,0);
          let loadingq = String.fromCharCode(119,95,50,95,98,108,97,99,107,111,117,116,0);
          let humidityd = 1;
         debugf /= Math.max(4, q_centerk);
         dragr += `${loadingq.length - humidityd}`;
         loadingq = `${1 << (Math.min(4, Math.abs(humidityd)))}`;
         break;
      }
          let historyJ = String.fromCharCode(118,112,108,112,102,95,116,95,55,55,0);
         debugE = "2";
         historyJ = `${(String.fromCharCode(116,0) == historyJ ? historyJ.length : historyJ.length)}`;
         debugE += `${list7.length << (Math.min(Math.abs(1), 2))}`;
       let lines = 4.0;
       let foregroundN = 4.0;
         lines += parseFloat(`${q_centerk}`);
      for (let t = 0; t < 3; t++) {
         q_centerk >>= Math.min(Math.abs(q_centerk + debugE.length), 5);
      }
      while ((5.56 + lines) >= 3.96 && (5.56 + foregroundN) >= 5.4) {
         foregroundN -= parseFloat(`${debugE.length}`);
         break;
      }
          let lightN = String.fromCharCode(113,95,54,51,95,99,111,101,102,102,105,99,105,101,110,116,0);
          let pangle5 = String.fromCharCode(105,95,56,49,95,100,105,114,97,99,0);
         list7.push(2);
         lightN += `${pangle5.length}`;
         pangle5 += `${pangle5.length}`;
      while (4 == (debugf / (Math.max(3, 5)))) {
         debugE = `${1 ^ parseInt(`${lines}`)}`;
         break;
      }
       let bridge1: Array<any> = [315, 476, 416];
       let configure0: Array<any> = [943, 653];
      hooksC += `${episodey.size / (Math.max(1, 7))}`;
      break;
   }
      tickh += `${episodey.size & 1}`;
   if (switch_z0.length < tickh.length) {
       let gpayZ: Map<any, any> = new Map([[String.fromCharCode(112,114,101,102,101,116,99,104,95,110,95,50,0),false ], [String.fromCharCode(110,111,119,95,113,95,52,52,0),true ], [String.fromCharCode(114,101,97,100,95,54,95,51,51,0),true ]]);
       let benefith = String.fromCharCode(113,117,97,110,116,120,95,118,95,54,56,0);
       let foundn = String.fromCharCode(116,95,51,49,95,100,105,115,112,111,115,97,98,108,101,115,0);
          let commonx = 0.0;
          let clockL = String.fromCharCode(116,117,110,110,101,108,95,118,95,53,49,0);
         benefith = `${parseInt(`${commonx}`) >> (Math.min(Math.abs(2), 4))}`;
         commonx += parseFloat(`${3 + clockL.length}`);
         clockL += `${clockL.length}`;
      if (benefith == foundn) {
         foundn += `${gpayZ.size % (Math.max(benefith.length, 8))}`;
      }
         gpayZ = new Map([[benefith, 2 ^ foundn.length]]);
      for (let v = 0; v < 1; v++) {
         gpayZ.set(`${benefith}`, 3);
      }
      let stringse = 9228652 <= gpayZ.size;
      do {
          let injuryX = String.fromCharCode(117,116,105,109,101,95,101,95,53,0);
          let nativeex2 = String.fromCharCode(115,95,54,55,95,116,111,116,97,108,0);
          let roomF = String.fromCharCode(112,95,49,50,95,110,101,97,114,115,101,116,0);
          let indexx = String.fromCharCode(115,107,101,116,99,104,95,104,95,48,0);
         gpayZ = new Map([[`${gpayZ.size}`, indexx.length]]);
         injuryX = `${roomF.length * nativeex2.length}`;
         nativeex2 = `${roomF.length}`;
         indexx = `${injuryX.length}`;
         if (stringse) {
            break;
         }
      } while ((2 >= (gpayZ.size - 4)) && stringse);
      tickh = `${(String.fromCharCode(108,0) == internetI ? internetI.length : parseInt(`${gifta}`))}`;
   }
   let bodank = String.fromCharCode(57,120,56,95,117,109,113,0) == hooksC;
   do {
       let blacklistN = String.fromCharCode(105,100,115,95,98,95,51,56,0);
       let philippines5 = String.fromCharCode(117,95,52,48,95,115,121,109,98,111,108,105,99,97,116,97,98,108,101,0);
       let ajaxH = String.fromCharCode(104,95,55,54,95,115,116,97,98,105,108,105,122,101,0);
         blacklistN += `${(String.fromCharCode(82,0) == philippines5 ? blacklistN.length : philippines5.length)}`;
       let skip6 = String.fromCharCode(107,95,50,51,95,107,101,121,110,97,109,101,0);
       let right8 = String.fromCharCode(121,95,55,48,95,111,118,101,114,108,97,112,0);
      for (let d = 0; d < 2; d++) {
         philippines5 += `${skip6.length}`;
      }
      let sportm = right8 == String.fromCharCode(106,103,99,55,109,109,104,53,114,117,0);
      do {
         right8 = `${right8.length}`;
         if (sportm) {
            break;
         }
      } while (sportm && (ajaxH != String.fromCharCode(80,0)));
      let networkq = 9189912 >= blacklistN.length;
      do {
          let targetA: Array<any> = [482, 682];
          let eactP = String.fromCharCode(97,97,114,99,104,95,111,95,53,50,0);
          let animationC = String.fromCharCode(99,108,97,115,115,105,102,115,95,99,95,49,54,0);
         blacklistN = `${3 >> (Math.min(2, eactP.length))}`;
         targetA.push((animationC == String.fromCharCode(53,0) ? animationC.length : targetA.length));
         eactP = "3";
         if (networkq) {
            break;
         }
      } while ((philippines5 == String.fromCharCode(89,0)) && networkq);
      while (skip6 != philippines5) {
          let background9 = String.fromCharCode(98,95,55,55,95,99,97,110,99,101,108,108,101,100,0);
          let condensedL = String.fromCharCode(115,99,97,108,101,102,97,99,116,111,114,115,95,53,95,53,51,0);
          let bingS = 0.0;
          let button_ = 2.0;
          let filterY = String.fromCharCode(114,95,50,52,95,114,101,97,108,108,111,99,0);
         philippines5 += `${(background9 == String.fromCharCode(101,0) ? parseInt(`${button_}`) : background9.length)}`;
         condensedL += `${filterY.length}`;
         bingS += 3;
         button_ /= Math.max(2, (parseFloat(`${String.fromCharCode(95,0) == filterY ? parseInt(`${bingS}`) : filterY.length}`)));
         break;
      }
          let more0 = false;
          let current_ = true;
         right8 += `${ajaxH.length >> (Math.min(1, right8.length))}`;
         more0 = current_;
      for (let y = 0; y < 2; y++) {
          let statisticsH = true;
          let macaug = String.fromCharCode(112,97,103,95,122,95,53,54,0);
          let signinup8: Map<any, any> = new Map([[String.fromCharCode(116,95,53,51,95,111,112,101,114,97,110,100,0),false ], [String.fromCharCode(106,95,56,57,95,109,97,112,112,105,110,103,0),true ]]);
         philippines5 = `${2 * right8.length}`;
         statisticsH = signinup8.size <= 93;
         macaug = `${signinup8.size >> (Math.min(macaug.length, 4))}`;
      }
         right8 = `${blacklistN.length}`;
      hooksC += `${suggestionL.length}`;
      if (bodank) {
         break;
      }
   } while (((2.24 - gifta) == 4.78) && bodank);
   if (blackI.length <= 5 && 5 <= switch_z0.length) {
      blackI = `${(String.fromCharCode(83,0) == switch_z0 ? switch_z0.length : parseInt(`${trasho}`))}`;
   }
   while (1 == (2 << (Math.min(3, suggestionL.length))) || 2 == (actionD.size << (Math.min(1, suggestionL.length)))) {
      suggestionL.push(2 ^ parseInt(`${trasho}`));
      break;
   }
   for (let c = 0; c < 2; c++) {
      lesss -= parseFloat(`${episodey.size << (Math.min(4, Math.abs(parseInt(`${trasho}`))))}`);
   }
       let bingk = 1.0;
       let terms2 = false;
       let trophyl = String.fromCharCode(121,95,49,56,95,116,101,109,112,108,97,116,101,100,0);
          let chartL = String.fromCharCode(98,114,111,97,100,99,97,115,116,105,110,103,95,114,95,50,56,0);
          let event1: Map<any, any> = new Map([[String.fromCharCode(109,106,112,101,103,98,95,101,95,54,56,0),386], [String.fromCharCode(102,114,101,113,117,101,110,99,121,95,52,95,50,54,0),90]]);
          let searchbarS = 0.0;
         terms2 = searchbarS >= 89.54 || event1.size >= 20;
         chartL += `${chartL.length % 2}`;
         event1 = new Map([[chartL, 1 * chartL.length]]);
      for (let n = 0; n < 2; n++) {
          let brightnessv: Array<any> = [754, 496];
          let entryc = String.fromCharCode(98,95,53,54,95,100,114,111,112,112,101,114,0);
          let phoneJ = true;
          let playlistl = 0;
          let splashx = false;
         terms2 = !splashx;
         brightnessv.push((String.fromCharCode(79,0) == entryc ? entryc.length : brightnessv.length));
         phoneJ = !phoneJ;
         playlistl <<= Math.min(Math.abs(1), 3);
      }
          let foundt: Array<any> = [62, 466, 121];
         trophyl += `${1 & parseInt(`${bingk}`)}`;
         foundt.push(1 >> (Math.min(3, foundt.length)));
      while (trophyl.includes(`${terms2}`)) {
         terms2 = bingk < 79.35;
         break;
      }
      if ((bingk + 1.27) >= 4.37 && bingk >= 1.27) {
          let long_iy = 2.0;
          let tailI = false;
          let reactnativejsG: Map<any, any> = new Map([[String.fromCharCode(104,95,49,56,95,98,117,116,116,101,114,102,108,105,101,115,0),String.fromCharCode(108,115,98,115,95,122,95,51,48,0)], [String.fromCharCode(99,97,110,99,101,108,95,119,95,50,48,0),String.fromCharCode(118,95,54,50,95,116,114,117,110,107,0)], [String.fromCharCode(118,101,99,95,118,95,52,50,0),String.fromCharCode(118,95,51,49,95,100,99,111,110,108,121,0)]]);
          let chinak = String.fromCharCode(97,95,51,55,95,97,115,111,99,0);
          let belly = 3.0;
         terms2 = 90 <= trophyl.length;
         long_iy -= parseFloat(`${chinak.length}`);
         tailI = (67 == ((!tailI ? 67 : reactnativejsG.size) * reactnativejsG.size));
         chinak = `${parseInt(`${long_iy}`) | parseInt(`${belly}`)}`;
         belly /= Math.max(4, parseFloat(`${reactnativejsG.size / 3}`));
      }
         bingk += (parseInt(`${bingk}`) + (terms2 ? 5 : 1));
      if (3 < trophyl.length) {
         terms2 = (60 >= (trophyl.length << (Math.min(3, Math.abs((!terms2 ? 60 : trophyl.length))))));
      }
      for (let o = 0; o < 3; o++) {
         bingk *= 1 % (Math.max(2, parseInt(`${bingk}`)));
      }
       let reneww = 4.0;
       let greeni = 0.0;
      commonO = (parseInt(`${lesss}`) - trophyl.length) > 53;
   let langkeyL = 9424857 >= suggestionL.length;
   do {
      suggestionL = [1 & actionD.size];
      if (langkeyL) {
         break;
      }
   } while (langkeyL && (1 < (5 * suggestionL.length)));
   while (4 == tickh.length) {
       let aboutT: Map<any, any> = new Map([[String.fromCharCode(117,110,102,101,116,99,104,95,121,95,53,54,0),true ], [String.fromCharCode(122,95,56,56,95,102,105,110,0),true ]]);
       let modityi = String.fromCharCode(112,114,101,100,120,108,95,98,95,50,55,0);
       let clubZ = 5;
       let gpayT: Map<any, any> = new Map([[String.fromCharCode(120,115,117,98,95,120,95,55,57,0),63], [String.fromCharCode(118,97,114,121,95,107,95,52,51,0),963]]);
       let appleU = 1.0;
          let bodanA = 0;
          let modulek = String.fromCharCode(100,101,99,95,97,95,56,0);
          let ewardedl = 0.0;
         clubZ %= Math.max(5, parseInt(`${appleU}`) % (Math.max(1, 7)));
         bodanA /= Math.max(bodanA * 3, 2);
         modulek += `${modulek.length % 3}`;
         ewardedl -= 1 - modulek.length;
      let commentK = 6084623 >= aboutT.size;
      do {
         aboutT.set(modityi, (modityi == String.fromCharCode(112,0) ? parseInt(`${appleU}`) : modityi.length));
         if (commentK) {
            break;
         }
      } while ((5 >= (aboutT.size / 2) && (aboutT.size / (Math.max(2, 6))) >= 4) && commentK);
      for (let g = 0; g < 2; g++) {
         clubZ -= 1 / (Math.max(5, parseInt(`${appleU}`)));
      }
         gpayT = new Map([[`${aboutT.size}`, aboutT.size]]);
      for (let t = 0; t < 1; t++) {
          let logoutj: Array<any> = [26, 568];
          let lang8 = false;
          let networkS = String.fromCharCode(105,95,50,51,95,105,110,102,111,114,109,97,116,105,118,101,0);
         aboutT = new Map([[`${gpayT.size}`, aboutT.size >> (Math.min(Math.abs(gpayT.size), 5))]]);
         logoutj = [1];
         lang8 = networkS.length < 37;
         networkS = "3";
      }
      while ((modityi.length ^ gpayT.size) < 3 && 4 < (3 ^ modityi.length)) {
          let videoH = String.fromCharCode(105,110,117,115,101,95,49,95,56,57,0);
         gpayT.set(modityi, modityi.length);
         videoH += `${2 * videoH.length}`;
         break;
      }
       let downloadingC = String.fromCharCode(113,95,52,53,95,115,112,114,105,116,101,115,0);
      while (Array.from(gpayT.values()).includes(clubZ)) {
          let with__9P = 4.0;
          let sentryG: Map<any, any> = new Map([[String.fromCharCode(98,101,105,103,110,101,116,95,108,95,57,54,0),String.fromCharCode(116,95,49,55,95,103,101,116,112,116,114,0)], [String.fromCharCode(116,95,50,56,95,99,117,100,97,0),String.fromCharCode(97,95,55,50,95,108,97,121,101,114,0)], [String.fromCharCode(103,95,53,54,95,98,108,97,107,101,98,0),String.fromCharCode(115,105,103,110,112,111,115,116,95,50,95,51,56,0)]]);
          let countdown2 = false;
         clubZ ^= 3;
         with__9P *= parseFloat(`${2 << (Math.min(2, Math.abs(sentryG.size)))}`);
         sentryG.set(`${with__9P}`, parseInt(`${with__9P}`) | 2);
         countdown2 = !countdown2;
         break;
      }
      for (let q = 0; q < 3; q++) {
         downloadingC = `${parseInt(`${appleU}`)}`;
      }
      while (appleU > 3.12) {
         gpayT = new Map([[`${aboutT.size}`, 1]]);
         break;
      }
         modityi = "2";
         aboutT.set(downloadingC, 1);
         appleU /= Math.max(5, parseFloat(`${1 * clubZ}`));
      if (3 <= gpayT.size) {
         appleU -= parseFloat(`${downloadingC.length % 2}`);
      }
         downloadingC = `${2 + clubZ}`;
      tickh = `${2 >> (Math.min(Math.abs(roomI), 1))}`;
      break;
   }
      commonO = 22 <= switch_z0.length;

        onTouchScreen();
    }

    const onChange = async (val: number) => {
        onProgressChange(val * -20);
    }

    return (
        <PanHandler step={10} onTouch={onScreenTouched} onChange={onChange} axis='x' />
    )
}

const styles = StyleSheet.create({

});