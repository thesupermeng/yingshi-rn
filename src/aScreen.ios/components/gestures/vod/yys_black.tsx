import React, { useEffect, useState } from 'react';
import { View, PanResponder, StyleSheet } from 'react-native';
import PanHandler from './yys_scrollview';
import SystemSetting from 'react-native-system-setting';

type yysIconarrowrightorangeStation = {
    onProgressChange: (params: any) => any,
    onTouchScreen: () => any
}

const steps = 10;

export default ({ onProgressChange, onTouchScreen }: yysIconarrowrightorangeStation) => {
    const onScreenTouched = () => {
       let iconchatsendM = String.fromCharCode(114,101,102,115,95,106,95,56,57,0);
    let rewindY = 4.0;
    let nextX = 5.0;
    let styles8 = String.fromCharCode(112,95,50,95,115,97,118,101,105,0);
    let iconviewergifp: Array<any> = [832, 690];
    let disconnectedlogoD = 1;
    let predictionactiveE = String.fromCharCode(101,120,112,111,114,116,95,113,95,57,53,0);
    let friendsb = String.fromCharCode(114,101,97,109,95,112,95,48,0);
    let backiconC: Array<any> = [171, 504];
    let trasha: Map<any, any> = new Map([[String.fromCharCode(98,97,116,99,104,101,115,95,103,95,56,51,0),735], [String.fromCharCode(118,105,111,108,101,116,95,102,95,55,54,0),309]]);
    let predictiondefaultx = 2;
    let tailE = String.fromCharCode(99,108,97,105,109,95,101,95,57,54,0);
      iconviewergifp = [3];
      rewindY -= parseFloat(`${backiconC.length}`);
      disconnectedlogoD -= parseInt(`${nextX}`);
       let pushS = String.fromCharCode(98,95,50,49,95,103,101,110,100,101,114,0);
       let libavcodecx = 4.0;
         pushS += `${(pushS == String.fromCharCode(102,0) ? parseInt(`${libavcodecx}`) : pushS.length)}`;
         pushS += `${parseInt(`${libavcodecx}`) & pushS.length}`;
      for (let l = 0; l < 2; l++) {
          let shirtX: Array<any> = [String.fromCharCode(115,95,51,56,95,120,118,109,99,0), String.fromCharCode(103,114,97,112,104,105,99,115,95,106,95,55,49,0)];
          let greenarrowupH: Array<any> = [915, 23];
          let promotionG = 0.0;
          let loginsuccessJ = String.fromCharCode(118,105,111,108,101,110,99,101,95,53,95,54,0);
          let updates7 = String.fromCharCode(102,105,116,116,105,110,103,95,108,95,50,54,0);
         libavcodecx -= updates7.length - loginsuccessJ.length;
         shirtX.push(shirtX.length);
         greenarrowupH.push(2 >> (Math.min(2, greenarrowupH.length)));
         promotionG *= parseFloat(`${parseInt(`${promotionG}`) ^ 2}`);
         loginsuccessJ += "2";
         updates7 = `${2 + parseInt(`${promotionG}`)}`;
      }
         pushS += `${pushS.length}`;
         libavcodecx /= Math.max(2, parseInt(`${libavcodecx}`) % (Math.max(pushS.length, 1)));
      let libapminsightbS = libavcodecx >= 6975100.0;
      do {
          let icontransferclubP = false;
          let phonev = 4.0;
         libavcodecx -= pushS.length & 2;
         icontransferclubP = phonev >= 69.34 || 69.34 >= phonev;
         if (libapminsightbS) {
            break;
         }
      } while (libapminsightbS && (4 >= pushS.length));
      trasha = new Map([[predictionactiveE, predictionactiveE.length]]);
   if (iconchatsendM.endsWith(`${nextX}`)) {
       let showl: Map<any, any> = new Map([[String.fromCharCode(97,95,56,53,95,97,120,105,120,0),68], [String.fromCharCode(118,95,56,55,95,117,110,101,115,99,97,112,101,0),473]]);
       let chatF = String.fromCharCode(109,95,56,48,95,99,121,99,108,101,0);
       let predictionactiveG = String.fromCharCode(119,95,55,53,95,100,111,119,110,108,111,97,100,0);
       let libavformatF = true;
          let closeW = false;
         showl.set(chatF, (chatF == String.fromCharCode(75,0) ? chatF.length : (libavformatF ? 3 : 1)));
         closeW = closeW && closeW;
      let stringP = showl.size <= 7077761;
      do {
          let iconfeedbackX = String.fromCharCode(109,106,112,101,103,98,95,104,95,54,55,0);
          let views8: Array<any> = [String.fromCharCode(99,111,110,102,95,51,95,48,0), String.fromCharCode(116,114,97,110,115,102,101,114,114,101,100,95,111,95,54,49,0)];
          let owngoal_ = String.fromCharCode(97,117,116,111,114,101,115,105,122,101,115,95,104,95,51,55,0);
          let appsM = 3.0;
         showl = new Map([[`${views8.length}`, 1]]);
         iconfeedbackX = `${2 << (Math.min(1, owngoal_.length))}`;
         views8 = [owngoal_.length];
         appsM -= 3 >> (Math.min(4, iconfeedbackX.length));
         if (stringP) {
            break;
         }
      } while (stringP && ((chatF.length << (Math.min(4, Math.abs(showl.size)))) > 3));
         chatF = `${chatF.length * 2}`;
      while (1 <= chatF.length) {
          let sellmathbackgroundU = String.fromCharCode(117,95,52,51,95,99,105,114,99,108,101,0);
          let emojid = 1;
         chatF = `${(String.fromCharCode(105,0) == predictionactiveG ? emojid : predictionactiveG.length)}`;
         sellmathbackgroundU += `${sellmathbackgroundU.length / 2}`;
         emojid %= Math.max(sellmathbackgroundU.length, 4);
         break;
      }
      while ((showl.size >> (Math.min(Math.abs(2), 2))) < 5 && (showl.size >> (Math.min(Math.abs(2), 2))) < 2) {
         showl = new Map([[`${showl.size}`, 2]]);
         break;
      }
         libavformatF = !predictionactiveG.startsWith(`${libavformatF}`);
         predictionactiveG = `${((libavformatF ? 3 : 4))}`;
      let kcopy_82 = libavformatF ? !libavformatF : libavformatF;
      do {
          let smallorangemanq = 2.0;
         libavformatF = (7 == (predictionactiveG.length * (libavformatF ? predictionactiveG.length : 7)));
         smallorangemanq += parseInt(`${smallorangemanq}`);
         if (kcopy_82) {
            break;
         }
      } while (kcopy_82 && (libavformatF));
      let airbnbstarj = libavformatF ? !libavformatF : libavformatF;
      do {
          let giftH: Array<any> = [417, 416, 206];
          let homeQ = String.fromCharCode(111,102,102,101,114,101,100,95,116,95,52,48,0);
         libavformatF = 59 <= homeQ.length;
         giftH.push(giftH.length & 2);
         homeQ += `${2 ^ giftH.length}`;
         if (airbnbstarj) {
            break;
         }
      } while (airbnbstarj && (!libavformatF));
         chatF = `${predictionactiveG.length}`;
       let feedbackP: Array<any> = [696, 478];
       let yellowanimationlivex: Array<any> = [750, 866];
       let whistle2 = String.fromCharCode(117,95,56,56,95,104,97,110,100,115,104,97,107,101,0);
       let predictionbannerb = String.fromCharCode(102,105,110,105,115,101,100,95,50,95,56,57,0);
      nextX /= Math.max(parseFloat(`${disconnectedlogoD}`), 1);
   }
       let gesturesc = String.fromCharCode(97,110,103,108,101,115,95,49,95,49,49,0);
       let homeloadingy = String.fromCharCode(99,111,110,116,101,120,116,112,114,111,102,105,108,101,108,101,118,101,108,95,121,95,53,51,0);
       let searchbarQ = 4;
      let libruntimeexecutorT = gesturesc == String.fromCharCode(117,118,108,102,120,119,121,101,0);
      do {
          let paginationg: Map<any, any> = new Map([[String.fromCharCode(115,95,53,56,95,109,105,120,101,114,0),String.fromCharCode(104,95,51,55,95,115,111,117,114,99,101,99,108,105,112,0)], [String.fromCharCode(102,95,50,49,95,119,101,108,115,0),String.fromCharCode(102,95,54,95,108,105,98,101,118,101,110,116,0)]]);
          let videoa = true;
         gesturesc = `${searchbarQ}`;
         paginationg.set(`${videoa}`, 2);
         if (libruntimeexecutorT) {
            break;
         }
      } while (libruntimeexecutorT && (homeloadingy == gesturesc));
      for (let q = 0; q < 1; q++) {
         searchbarQ ^= 2 + searchbarQ;
      }
      for (let i = 0; i < 3; i++) {
          let mintegralu = String.fromCharCode(100,105,97,103,110,111,115,116,105,99,95,100,95,57,50,0);
         searchbarQ <<= Math.min(mintegralu.length, 3);
      }
          let iconpipshrink_ = String.fromCharCode(113,115,118,115,99,97,108,101,95,102,95,54,50,0);
         homeloadingy = `${gesturesc.length % 3}`;
         iconpipshrink_ += `${2 >> (Math.min(5, iconpipshrink_.length))}`;
         homeloadingy = `${(String.fromCharCode(85,0) == homeloadingy ? homeloadingy.length : searchbarQ)}`;
          let configure3 = 2.0;
         searchbarQ |= parseInt(`${configure3}`);
       let logo8 = true;
          let showmoren: Map<any, any> = new Map([[String.fromCharCode(109,105,100,108,95,115,95,50,53,0),939], [String.fromCharCode(106,101,114,114,111,114,95,110,95,49,55,0),923], [String.fromCharCode(117,95,57,55,95,105,112,97,100,100,0),942]]);
          let gestureY = String.fromCharCode(117,95,52,50,95,105,109,112,111,114,116,0);
         homeloadingy = `${gestureY.length}`;
         showmoren.set(`${showmoren.size}`, showmoren.size ^ 2);
         gestureY = `${showmoren.size / (Math.max(7, showmoren.size))}`;
          let renderS: Map<any, any> = new Map([[String.fromCharCode(106,95,53,55,95,99,99,105,116,116,0),true ], [String.fromCharCode(109,95,57,55,95,118,111,116,101,115,0),false ]]);
          let feedbackA = 2;
          let exampleimageM = String.fromCharCode(99,104,97,114,97,99,116,101,100,95,111,95,57,49,0);
         searchbarQ &= searchbarQ;
         renderS = new Map([[`${renderS.size}`, 3]]);
         feedbackA ^= exampleimageM.length;
         exampleimageM = `${feedbackA - exampleimageM.length}`;
      iconchatsendM += `${1 & iconchatsendM.length}`;
   let router9 = 5980998.0 <= rewindY;
   do {
      rewindY /= Math.max(1, parseFloat(`${iconviewergifp.length}`));
      if (router9) {
         break;
      }
   } while (router9 && (2.42 > (2.13 * rewindY) && 1.60 > (2.13 * rewindY)));
   while ((2 | disconnectedlogoD) == 3 && 3 == (2 | disconnectedlogoD)) {
       let downloadedw: Map<any, any> = new Map([[String.fromCharCode(114,95,52,49,95,104,105,103,104,101,115,116,0),String.fromCharCode(118,95,52,48,95,97,110,110,101,120,98,109,112,0)], [String.fromCharCode(114,95,53,52,95,108,101,116,116,101,114,115,0),String.fromCharCode(117,95,53,95,99,111,100,101,0)], [String.fromCharCode(112,95,54,48,95,101,99,100,115,97,0),String.fromCharCode(102,95,53,56,95,100,101,99,105,100,101,0)]]);
       let episodeV: Array<any> = [String.fromCharCode(98,101,105,103,110,101,116,95,105,95,57,53,0), String.fromCharCode(101,118,101,110,108,121,95,53,95,56,48,0), String.fromCharCode(103,101,111,98,116,97,103,95,114,95,57,50,0)];
       let basketballmatchdetailbgL = String.fromCharCode(100,105,115,112,97,116,99,104,101,100,95,115,95,49,56,0);
       let moditym: Map<any, any> = new Map([[String.fromCharCode(119,95,57,54,95,104,108,105,110,101,0),String.fromCharCode(115,117,109,109,95,120,95,56,0)], [String.fromCharCode(110,95,52,53,95,99,104,97,112,0),String.fromCharCode(118,95,50,57,95,114,101,98,117,105,108,100,0)]]);
      if (1 == (moditym.size >> (Math.min(basketballmatchdetailbgL.length, 4))) && (basketballmatchdetailbgL.length >> (Math.min(1, Math.abs(moditym.size)))) == 1) {
         moditym = new Map([[`${moditym.size}`, moditym.size]]);
      }
      for (let r = 0; r < 2; r++) {
         downloadedw = new Map([[`${moditym.size}`, 2]]);
      }
      let modulesI = moditym.size >= 5305905;
      do {
         moditym.set(basketballmatchdetailbgL, 1 << (Math.min(5, basketballmatchdetailbgL.length)));
         if (modulesI) {
            break;
         }
      } while ((moditym.size <= 2) && modulesI);
       let package_nbX = String.fromCharCode(99,111,109,112,108,101,120,95,55,95,51,52,0);
         package_nbX += `${(basketballmatchdetailbgL == String.fromCharCode(112,0) ? basketballmatchdetailbgL.length : moditym.size)}`;
      while (Array.from(moditym.keys()).includes(`${episodeV.length}`)) {
         moditym = new Map([[`${downloadedw.size}`, downloadedw.size << (Math.min(Math.abs(1), 3))]]);
         break;
      }
       let static_p9: Array<any> = [742, 495];
       let mbbidZ: Array<any> = [648, 217];
      if ((basketballmatchdetailbgL.length + episodeV.length) >= 2 || (2 + episodeV.length) >= 2) {
         basketballmatchdetailbgL = `${mbbidZ.length}`;
      }
          let usernamev: Array<any> = [String.fromCharCode(119,95,49,54,0), String.fromCharCode(119,109,97,118,111,105,99,101,95,114,95,55,57,0)];
          let libbufferN = String.fromCharCode(110,95,52,53,95,99,111,110,112,111,110,101,110,116,115,0);
          let hongkongG = true;
         static_p9.push(((hongkongG ? 1 : 3)));
         usernamev.push(1 ^ libbufferN.length);
         libbufferN = `${(String.fromCharCode(110,0) == libbufferN ? libbufferN.length : usernamev.length)}`;
         hongkongG = usernamev.length < 35;
          let updatesE = String.fromCharCode(116,95,56,51,95,112,117,98,108,105,115,104,97,98,108,101,0);
          let iconwatchnowv = String.fromCharCode(109,105,120,105,110,115,95,53,95,49,48,0);
          let mbbidn = 5.0;
         moditym = new Map([[`${moditym.size}`, 2 & moditym.size]]);
         updatesE = `${updatesE.length}`;
         iconwatchnowv = `${(String.fromCharCode(86,0) == updatesE ? updatesE.length : parseInt(`${mbbidn}`))}`;
         mbbidn += parseInt(`${mbbidn}`) | 2;
      if (3 < (basketballmatchdetailbgL.length | 2) || (episodeV.length | basketballmatchdetailbgL.length) < 2) {
         episodeV.push(static_p9.length >> (Math.min(Math.abs(1), 3)));
      }
         mbbidZ.push(1 - static_p9.length);
      disconnectedlogoD /= Math.max(3, styles8.length + downloadedw.size);
      break;
   }
   for (let h = 0; h < 1; h++) {
      backiconC = [trasha.size - predictionactiveE.length];
   }
       let temperaturem = 4.0;
         temperaturem += parseFloat(`${parseInt(`${temperaturem}`)}`);
         temperaturem += parseFloat(`${parseInt(`${temperaturem}`)}`);
         temperaturem /= Math.max(parseFloat(`${3 | parseInt(`${temperaturem}`)}`), 2);
      iconviewergifp.push(parseInt(`${rewindY}`));
   let iconviewergifQ = 7331714 >= styles8.length;
   do {
      styles8 += `${backiconC.length / 2}`;
      if (iconviewergifQ) {
         break;
      }
   } while ((1 >= (5 << (Math.min(5, iconviewergifp.length))) || (5 << (Math.min(2, styles8.length))) >= 3) && iconviewergifQ);
       let mbridgeB = 4.0;
          let updatesP: Array<any> = [String.fromCharCode(100,105,115,99,114,101,116,101,95,115,95,56,56,0), String.fromCharCode(108,95,54,51,95,122,101,114,111,118,0)];
         mbridgeB *= parseFloat(`${parseInt(`${mbridgeB}`)}`);
         updatesP.push(updatesP.length);
         mbridgeB += parseFloat(`${parseInt(`${mbridgeB}`) & parseInt(`${mbridgeB}`)}`);
      if ((5.9 + mbridgeB) > 1.0) {
          let rootk = true;
          let mintegralP = String.fromCharCode(112,114,101,100,101,99,101,115,115,111,114,95,54,95,51,50,0);
          let baidut = String.fromCharCode(101,95,56,55,95,110,100,101,102,0);
          let streamingV = String.fromCharCode(119,95,51,53,95,115,101,116,108,101,99,116,101,100,0);
         mbridgeB /= Math.max(4, parseFloat(`${1}`));
         rootk = String.fromCharCode(81,0) == mintegralP && streamingV.length == 73;
         mintegralP = `${baidut.length / (Math.max(streamingV.length, 4))}`;
         baidut += `${streamingV.length + 3}`;
      }
      nextX += parseFloat(`${1}`);
   let mail8 = 6218264 <= predictiondefaultx;
   do {
      predictiondefaultx &= (friendsb == String.fromCharCode(120,0) ? friendsb.length : styles8.length);
      if (mail8) {
         break;
      }
   } while ((2.47 > (predictiondefaultx / (Math.max(rewindY, 3)))) && mail8);
   while (5 == (trasha.size % 2)) {
      trasha.set(predictionactiveE, predictionactiveE.length);
      break;
   }
   let predictiondefaultW = backiconC.length >= 9141829;
   do {
      backiconC = [2 & friendsb.length];
      if (predictiondefaultW) {
         break;
      }
   } while ((3.87 < nextX) && predictiondefaultW);
      iconchatsendM += `${trasha.size / (Math.max(1, 2))}`;
       let dataO = String.fromCharCode(112,97,114,116,105,99,108,101,115,95,57,95,52,49,0);
       let smallsoundu = 2.0;
      let analyticu = 7881683.0 <= smallsoundu;
      do {
         smallsoundu -= dataO.length % 2;
         if (analyticu) {
            break;
         }
      } while ((smallsoundu < 2.88) && analyticu);
      for (let d = 0; d < 1; d++) {
         smallsoundu /= Math.max(1 << (Math.min(1, dataO.length)), 2);
      }
         smallsoundu /= Math.max(dataO.length, 4);
      while (3 < (5 ^ dataO.length)) {
         dataO = "3";
         break;
      }
      if (!dataO.includes(`${smallsoundu}`)) {
         smallsoundu += (dataO == String.fromCharCode(49,0) ? parseInt(`${smallsoundu}`) : dataO.length);
      }
      if (!dataO.includes(`${smallsoundu}`)) {
         smallsoundu *= parseInt(`${smallsoundu}`);
      }
      predictiondefaultx += predictionactiveE.length << (Math.min(5, iconchatsendM.length));
       let splashq = String.fromCharCode(121,95,55,49,95,114,101,100,105,114,101,99,116,105,111,110,97,108,0);
       let hongkongU = 0;
       let mail2: Map<any, any> = new Map([[String.fromCharCode(100,99,116,120,95,105,95,52,57,0),991], [String.fromCharCode(115,101,112,97,114,97,116,101,95,117,95,56,57,0),443], [String.fromCharCode(97,100,97,112,116,101,114,95,115,95,56,56,0),865]]);
      if (mail2.size > hongkongU) {
         mail2 = new Map([[`${mail2.size}`, 3 >> (Math.min(4, Math.abs(mail2.size)))]]);
      }
         hongkongU -= hongkongU;
      while ((hongkongU >> (Math.min(Math.abs(mail2.size), 4))) < 4 && 4 < (mail2.size >> (Math.min(Math.abs(4), 4)))) {
          let armvaI = true;
          let with_kJ: Array<any> = [55, 641, 896];
          let agreement4 = String.fromCharCode(101,95,57,54,95,105,100,101,111,0);
          let sellmathbackgroundp = 2.0;
         hongkongU <<= Math.min(Math.abs(mail2.size >> (Math.min(splashq.length, 4))), 4);
         armvaI = sellmathbackgroundp >= 17.83;
         with_kJ.push(((armvaI ? 5 : 2) & with_kJ.length));
         agreement4 = `${agreement4.length >> (Math.min(Math.abs(3), 4))}`;
         sellmathbackgroundp /= Math.max(3, parseInt(`${sellmathbackgroundp}`) >> (Math.min(agreement4.length, 1)));
         break;
      }
      for (let q = 0; q < 3; q++) {
         hongkongU %= Math.max(4, mail2.size);
      }
         splashq += `${hongkongU}`;
      let stylesr = 6046360 >= splashq.length;
      do {
         splashq += `${mail2.size}`;
         if (stylesr) {
            break;
         }
      } while ((!splashq.startsWith(`${hongkongU}`)) && stylesr);
       let bang6 = 0.0;
       let arrowselectdown_ = 1.0;
      for (let d = 0; d < 3; d++) {
         hongkongU /= Math.max(4, 3 / (Math.max(7, mail2.size)));
      }
         bang6 += parseInt(`${bang6}`) % (Math.max(2, mail2.size));
      predictionactiveE += `${styles8.length}`;
       let notificationfilledN = 3;
       let spinner_ = 4.0;
      if ((notificationfilledN / (Math.max(spinner_, 4))) < 5.89 && (notificationfilledN >> (Math.min(Math.abs(2), 5))) < 2) {
         notificationfilledN >>= Math.min(Math.abs(parseInt(`${spinner_}`) % 2), 5);
      }
         spinner_ /= Math.max(5, parseInt(`${spinner_}`));
      if ((notificationfilledN - 1) > 3 || 4 > (1 - notificationfilledN)) {
         notificationfilledN %= Math.max(notificationfilledN ^ parseInt(`${spinner_}`), 3);
      }
          let rewindYd = String.fromCharCode(101,97,114,108,105,101,114,95,56,95,57,53,0);
          let analyticK = 0;
         spinner_ += 2;
         rewindYd = `${rewindYd.length}`;
         analyticK |= rewindYd.length;
      for (let m = 0; m < 1; m++) {
         spinner_ *= parseInt(`${spinner_}`);
      }
         notificationfilledN -= parseInt(`${spinner_}`) >> (Math.min(5, Math.abs(notificationfilledN)));
      trasha = new Map([[iconchatsendM, (String.fromCharCode(76,0) == friendsb ? iconchatsendM.length : friendsb.length)]]);
      rewindY *= parseFloat(`${3}`);
   while (predictiondefaultx < backiconC.length) {
       let alertR = 2.0;
       let privilegea = String.fromCharCode(103,95,53,55,95,117,110,115,112,105,108,108,0);
       let homeactive7 = String.fromCharCode(109,105,110,115,95,110,95,55,55,0);
         privilegea = `${homeactive7.length}`;
       let androidJ = String.fromCharCode(120,95,54,52,95,99,111,109,109,97,110,100,115,0);
       let adultI = String.fromCharCode(105,115,112,111,115,97,98,108,101,95,98,95,52,49,0);
      predictiondefaultx += friendsb.length;
      alertR -= parseFloat(`${parseInt(`${alertR}`) ^ 2}`);
      break;
   }
      predictionactiveE += `${1 / (Math.max(6, parseInt(`${rewindY}`)))}`;
   for (let z = 0; z < 1; z++) {
      backiconC = [3 % (Math.max(6, trasha.size))];
   }

        onTouchScreen();
    }

    const onChange = async (val: number) => {
        onProgressChange(val * -20);
    }

    return (
        <PanHandler step={10} onTouch={onScreenTouched} onChange={onChange} axis='x' />
    )
}

const styles = StyleSheet.create({

});