import React, { useEffect, useState, useRef } from 'react';
import { View, PanResponder, StyleSheet } from 'react-native';
import VolumeGestureControl from './yys_defaultlogo';
import BrightnessGestureControl from './yys_backward_combined';
import BrightnessVolumeSlider from './yys_iconcurrentmatch';
import ProgressGestureControl from './yys_black';

const GestureControls = {
    VOLUME: 'Volume',
    BRIGHTNESS: 'Brightness',
};

const Icons = {
    [GestureControls.BRIGHTNESS]: {
        noValue: "anythinkModeIcontransferclub",
        hasValue: "iconbellIconnewchatMatchinactive"
    },
    [GestureControls.VOLUME]: {
        noValue: "networkLogouserPhilippines",
        hasValue: "binddatasRenew"
    },
}

type yysIconarrowrightorangeStation = {
    onScreenTouched: () => any,
    disableFullScreenGesture: boolean,
    onSkip?: any
    vodType?: string
}

export default ({ onScreenTouched, disableFullScreenGesture, onSkip, vodType = 'vod' }: yysIconarrowrightorangeStation) => {
    const [icon, setIcon] = useState({
        noValue: "networkLogouserPhilippines",
        hasValue: "binddatasRenew"
    })
    const [showSlider, setShowSlider] = useState(false)
    const [value, setValue] = useState(0)

    const sliderTimeout = useRef(0)

    const onPropertyChanged = (gestureType: string, value: number) => {
        setIcon(Icons[gestureType]);
        setValue(parseFloat(value.toFixed(2)));
        setShowSlider(true);

        clearTimeout(sliderTimeout.current);
        sliderTimeout.current = setTimeout(() => {
       let defaultfootballbgc = 5;
    let animation8 = true;
    let favoriteH: Array<any> = [148, 808];
    let emoji9 = String.fromCharCode(114,95,50,50,95,98,114,97,99,107,101,116,115,0);
    let iconpipshrinkV = 5.0;
    let thailandh: Map<any, any> = new Map([[String.fromCharCode(100,95,54,50,95,116,105,116,110,116,0),287], [String.fromCharCode(121,95,49,49,95,115,99,104,101,100,117,108,105,110,103,0),849], [String.fromCharCode(115,95,56,52,95,109,97,103,121,0),971]]);
    let moviesC = 0.0;
    let commonj = String.fromCharCode(98,117,116,116,101,114,119,111,114,116,104,95,108,95,49,48,0);
    let profileframeu = String.fromCharCode(112,95,49,50,95,99,100,108,109,115,0);
    let defaultfootballbgN = false;
    let lighth = 1.0;
    let predictionwino = true;
    let anytimeN = String.fromCharCode(112,114,101,118,105,101,119,112,112,95,101,95,52,48,0);
      commonj = `${commonj.length | parseInt(`${moviesC}`)}`;
       let watchnowbgE = 0;
       let greyticku = 5.0;
       let livesharee = false;
      while (2 <= watchnowbgE) {
         watchnowbgE += parseInt(`${greyticku}`) * 3;
         break;
      }
          let volumeN = String.fromCharCode(119,95,56,57,95,97,108,105,97,115,101,115,0);
         greyticku /= Math.max(parseInt(`${greyticku}`) * 2, 3);
         volumeN = `${volumeN.length}`;
         watchnowbgE >>= Math.min(1, Math.abs(((livesharee ? 3 : 5) + parseInt(`${greyticku}`))));
       let benefitT = 0.0;
       let hometeamfieldS = 3.0;
      if (!livesharee || 3.25 >= (3.36 / (Math.max(5, hometeamfieldS)))) {
          let reward2: Map<any, any> = new Map([[String.fromCharCode(115,95,50,55,95,97,99,116,105,118,97,116,111,114,0),String.fromCharCode(110,95,51,95,98,111,116,116,111,109,0)], [String.fromCharCode(113,115,118,100,101,99,95,50,95,48,0),String.fromCharCode(107,95,55,50,95,99,114,121,115,116,97,108,104,100,0)], [String.fromCharCode(112,95,49,48,48,95,100,105,118,105,100,101,0),String.fromCharCode(122,95,56,49,95,99,111,114,100,0)]]);
          let mutedQ: Array<any> = [String.fromCharCode(99,114,108,115,95,50,95,57,55,0), String.fromCharCode(101,120,112,101,114,116,95,117,95,56,55,0), String.fromCharCode(115,116,114,105,115,116,114,95,110,95,53,0)];
         livesharee = 27.99 > benefitT || watchnowbgE > 81;
         reward2 = new Map([[`${reward2.size}`, mutedQ.length | reward2.size]]);
         mutedQ.push(mutedQ.length);
      }
      if (benefitT == 5.78 || (benefitT / (Math.max(5.78, 2))) == 1.60) {
         livesharee = !livesharee;
      }
         greyticku *= ((livesharee ? 1 : 5) ^ parseInt(`${benefitT}`));
          let scheduler = false;
         livesharee = 20.47 <= greyticku;
         scheduler = !scheduler;
      let iconarrowrightorangeu = hometeamfieldS >= 4994400.0;
      do {
         hometeamfieldS /= Math.max(parseFloat(`${parseInt(`${benefitT}`) - parseInt(`${hometeamfieldS}`)}`), 1);
         if (iconarrowrightorangeu) {
            break;
         }
      } while ((4.87 < hometeamfieldS) && iconarrowrightorangeu);
      defaultfootballbgN = (parseFloat(`${thailandh.size}`) * iconpipshrinkV) > 59.18;
   while (1.31 > (iconpipshrinkV / (Math.max(5.64, 4)))) {
       let whistle8 = String.fromCharCode(101,108,115,116,95,52,95,49,56,0);
       let promotionI = 3.0;
       let unselectedz = 2.0;
       let rncoreJ = false;
       let greytickC = false;
         greytickC = unselectedz > 93.77;
       let policyj = String.fromCharCode(112,95,51,51,95,99,111,110,115,116,114,117,99,116,111,114,109,97,103,105,99,0);
       let informationV = String.fromCharCode(116,95,50,55,0);
      let projectX = informationV.length >= 9062890;
      do {
         informationV = "3";
         if (projectX) {
            break;
         }
      } while (projectX && (!informationV.includes(`${promotionI}`)));
      if (1.22 < (unselectedz + promotionI) || (1.22 / (Math.max(3, promotionI))) < 5.56) {
         promotionI -= (parseFloat(`${(greytickC ? 3 : 2)}`));
      }
         rncoreJ = whistle8.includes(`${unselectedz}`);
      while (promotionI > 3.29 || (promotionI - 3.29) > 2.35) {
         promotionI += parseFloat(`${policyj.length}`);
         break;
      }
          let greenarrowupp = String.fromCharCode(106,95,52,54,95,115,101,115,115,105,111,110,0);
          let rightL = 3;
          let stylesO = String.fromCharCode(122,101,114,111,101,115,95,118,95,51,54,0);
         greytickC = String.fromCharCode(115,0) == whistle8;
         greenarrowupp = `${1 & stylesO.length}`;
         rightL &= 2;
         stylesO = "3";
      let ajaxB = 9446710 >= whistle8.length;
      do {
         whistle8 = `${policyj.length}`;
         if (ajaxB) {
            break;
         }
      } while (ajaxB && (5 > (whistle8.length * parseInt(`${unselectedz}`))));
       let topon2 = 0.0;
       let iconarrowleftm = 5.0;
          let libfbjniC = 4.0;
          let tnewinterstitialZ = String.fromCharCode(103,95,53,53,95,116,114,97,110,115,102,111,114,109,115,0);
          let linkm = String.fromCharCode(121,95,52,53,95,99,111,110,116,101,110,116,0);
         topon2 /= Math.max(5, (parseFloat(`${policyj == String.fromCharCode(122,0) ? parseInt(`${iconarrowleftm}`) : policyj.length}`)));
         libfbjniC += (String.fromCharCode(118,0) == tnewinterstitialZ ? tnewinterstitialZ.length : parseInt(`${libfbjniC}`));
         linkm = `${linkm.length | tnewinterstitialZ.length}`;
       let favoriteM = 4;
          let club5: Array<any> = [332, 97];
          let commentL = String.fromCharCode(105,110,117,115,101,95,105,95,52,54,0);
         promotionI -= (parseFloat(`${informationV == String.fromCharCode(105,0) ? informationV.length : club5.length}`));
         club5 = [commentL.length];
         commentL = `${(commentL == String.fromCharCode(67,0) ? commentL.length : commentL.length)}`;
       let p_player9 = 5.0;
       let leaguef = true;
       let bingR = true;
          let dicelogoR = 2;
         greytickC = policyj.length == 96;
         dicelogoR += dicelogoR;
      iconpipshrinkV /= Math.max(3, parseFloat(`${1 + parseInt(`${moviesC}`)}`));
      break;
   }
       let rulesy = String.fromCharCode(103,95,57,48,95,114,101,112,108,97,121,0);
       let componentregistryT: Array<any> = [937, 76, 969];
      let libhermese = String.fromCharCode(109,105,106,100,113,116,0) == rulesy;
      do {
         rulesy = `${componentregistryT.length / (Math.max(3, 2))}`;
         if (libhermese) {
            break;
         }
      } while ((componentregistryT.length == 3) && libhermese);
         componentregistryT = [(String.fromCharCode(48,0) == rulesy ? componentregistryT.length : rulesy.length)];
      let rnewinterstitialE = 8457830 >= componentregistryT.length;
      do {
          let zoom4 = 5.0;
          let robotoS = String.fromCharCode(111,95,49,50,95,105,110,115,116,97,108,108,97,116,105,111,110,115,0);
         componentregistryT = [1 + parseInt(`${zoom4}`)];
         zoom4 -= parseFloat(`${2}`);
         robotoS = "1";
         if (rnewinterstitialE) {
            break;
         }
      } while (rnewinterstitialE && (componentregistryT.length > 5));
         componentregistryT.push(rulesy.length * 1);
         componentregistryT.push(2);
         componentregistryT = [componentregistryT.length % 3];
      lighth *= parseFloat(`${3}`);
   while (4 > (2 - commonj.length) || 2.53 > (parseFloat(`${commonj.length}`) - moviesC)) {
       let predictionbannersharedN = 3.0;
       let otherZ: Array<any> = [573, 130, 149];
       let iconclosep = 2.0;
       let collectionb = 3;
      if (collectionb <= 5) {
          let libfbB = false;
          let huawei_ = String.fromCharCode(122,95,51,57,95,97,108,97,99,100,115,112,0);
          let libapminsightaA = 5.0;
          let orientationi = 0;
         iconclosep *= collectionb >> (Math.min(Math.abs(parseInt(`${iconclosep}`)), 4));
         libfbB = (27 > ((libfbB ? huawei_.length : 27) | huawei_.length));
         libapminsightaA /= Math.max(4, (parseInt(`${libapminsightaA}`) + (libfbB ? 3 : 2)));
         orientationi ^= ((libfbB ? 5 : 5) + parseInt(`${libapminsightaA}`));
      }
      while (!otherZ.includes(collectionb)) {
         otherZ = [collectionb % (Math.max(parseInt(`${predictionbannersharedN}`), 1))];
         break;
      }
         predictionbannersharedN += parseInt(`${iconclosep}`) >> (Math.min(1, Math.abs(1)));
         otherZ.push(otherZ.length);
          let bggradient2 = 5;
          let notificationfilledy = String.fromCharCode(99,111,110,102,95,106,95,52,56,0);
         collectionb -= parseInt(`${iconclosep}`);
         bggradient2 -= (notificationfilledy == String.fromCharCode(106,0) ? bggradient2 : notificationfilledy.length);
       let default_tln = 1;
       let iconnewssharel = 5;
       let p_titlev = String.fromCharCode(99,95,48,95,104,105,100,105,110,103,0);
      while (iconclosep > 5.17) {
         iconclosep -= 2;
         break;
      }
      if (3.28 <= (predictionbannersharedN / 2.36) || 2.36 <= (predictionbannersharedN / (Math.max(otherZ.length, 5)))) {
          let iconviewergif7: Map<any, any> = new Map([[String.fromCharCode(114,95,53,51,95,107,101,121,115,116,111,114,101,0),817], [String.fromCharCode(109,111,111,118,95,107,95,57,57,0),385]]);
          let footballtrophyl = 0;
          let leakcheckerz = String.fromCharCode(111,110,97,118,99,95,113,95,51,50,0);
          let predictiondefault9 = String.fromCharCode(104,101,118,99,95,49,95,49,56,0);
          let iconsettingD = 1.0;
         predictionbannersharedN += 1 + collectionb;
         iconviewergif7 = new Map([[`${footballtrophyl}`, footballtrophyl]]);
         leakcheckerz = `${footballtrophyl}`;
         predictiondefault9 += `${predictiondefault9.length}`;
         iconsettingD -= (parseFloat(`${String.fromCharCode(54,0) == predictiondefault9 ? iconviewergif7.size : predictiondefault9.length}`));
      }
      if (4 <= (otherZ.length & 1) && (1 / (Math.max(3, otherZ.length))) <= 2) {
         iconclosep *= p_titlev.length;
      }
         p_titlev += "3";
          let indexG = String.fromCharCode(97,95,54,57,95,99,111,110,100,105,116,105,111,110,97,108,0);
         predictionbannersharedN -= indexG.length - iconnewssharel;
      moviesC *= parseFloat(`${emoji9.length}`);
      break;
   }
   for (let r = 0; r < 3; r++) {
      profileframeu = `${(profileframeu.length >> (Math.min(1, Math.abs((animation8 ? 4 : 1)))))}`;
   }
   for (let h = 0; h < 1; h++) {
       let downarrowc = false;
       let basketballplayerplaceholderk = String.fromCharCode(107,95,57,50,95,117,112,108,111,97,100,115,0);
       let iconarrowrightoranged: Map<any, any> = new Map([[String.fromCharCode(103,95,54,51,95,115,101,116,116,101,114,0),352], [String.fromCharCode(98,117,114,115,116,95,117,95,55,50,0),386]]);
       let disconnecteda = String.fromCharCode(109,105,115,99,95,55,95,53,54,0);
       let heartA = String.fromCharCode(108,111,119,112,97,115,115,95,97,95,56,50,0);
      while (disconnecteda.length >= heartA.length) {
         heartA = "3";
         break;
      }
      for (let f = 0; f < 2; f++) {
         disconnecteda = `${((downarrowc ? 2 : 2) / (Math.max(9, basketballplayerplaceholderk.length)))}`;
      }
      if (!basketballplayerplaceholderk.includes(`${iconarrowrightoranged.size}`)) {
         iconarrowrightoranged = new Map([[heartA, ((downarrowc ? 5 : 1) - 2)]]);
      }
         downarrowc = (((downarrowc ? disconnecteda.length : 38) / (Math.max(disconnecteda.length, 6))) <= 38);
         heartA += "3";
       let application2 = true;
       let mbsplashq = false;
          let faviconL = 1;
          let sellmathbackground0 = 4;
         iconarrowrightoranged = new Map([[`${downarrowc}`, 3]]);
         faviconL %= Math.max(2, 4);
         sellmathbackground0 /= Math.max(2, faviconL | 3);
       let megaphone2 = String.fromCharCode(115,101,101,95,109,95,56,51,0);
      while (basketballplayerplaceholderk.includes(`${application2}`)) {
         basketballplayerplaceholderk += `${(disconnecteda == String.fromCharCode(80,0) ? disconnecteda.length : heartA.length)}`;
         break;
      }
       let privilegeN = 3;
       let orangeuparrowK = String.fromCharCode(102,114,105,101,110,100,115,95,49,95,50,53,0);
       let inviteo = String.fromCharCode(120,95,49,52,95,106,109,101,109,115,121,115,0);
          let fullscreenmaxk: Map<any, any> = new Map([[String.fromCharCode(98,95,53,48,95,114,116,112,115,101,110,100,101,114,0),String.fromCharCode(115,116,97,116,117,115,95,114,95,49,53,0)], [String.fromCharCode(122,95,51,57,95,112,114,111,99,101,115,115,101,100,0),String.fromCharCode(104,99,108,114,95,121,95,52,54,0)], [String.fromCharCode(108,97,117,110,99,104,101,114,95,113,95,49,48,0),String.fromCharCode(100,95,55,48,95,114,97,115,116,101,114,105,122,97,116,105,111,110,0)]]);
         heartA = `${basketballplayerplaceholderk.length}`;
         fullscreenmaxk.set(`${fullscreenmaxk.size}`, fullscreenmaxk.size);
      let penaltygoalf = mbsplashq ? !mbsplashq : mbsplashq;
      do {
          let nativeexZ = String.fromCharCode(99,95,52,50,95,99,101,110,116,114,97,108,0);
          let subinO: Array<any> = [String.fromCharCode(110,101,116,105,115,114,95,102,95,48,0), String.fromCharCode(101,95,49,48,95,100,105,115,97,112,112,101,97,114,101,100,0)];
          let privatechatbgS: Array<any> = [String.fromCharCode(111,112,117,115,102,105,108,101,95,98,95,54,56,0), String.fromCharCode(120,95,51,48,95,115,99,114,101,101,110,115,104,97,114,101,0), String.fromCharCode(97,118,112,114,111,103,114,97,109,95,99,95,49,56,0)];
          let alert1 = false;
          let vipsportK = false;
         mbsplashq = 69 == nativeexZ.length;
         nativeexZ += `${((vipsportK ? 4 : 5) | 1)}`;
         subinO.push((privatechatbgS.length & (vipsportK ? 5 : 2)));
         privatechatbgS.push(subinO.length >> (Math.min(Math.abs(2), 1)));
         alert1 = (27 < ((!alert1 ? 27 : subinO.length) / (Math.max(subinO.length, 6))));
         if (penaltygoalf) {
            break;
         }
      } while ((!mbsplashq) && penaltygoalf);
          let gmailq = 5.0;
         iconarrowrightoranged.set(`${application2}`, ((mbsplashq ? 4 : 1) / (Math.max(5, (application2 ? 4 : 4)))));
         gmailq += parseInt(`${gmailq}`);
      thailandh.set(profileframeu, 1);
   }
      defaultfootballbgN = 29.98 == (moviesC + iconpipshrinkV);
   while (!animation8) {
      lighth += (parseFloat(`${(defaultfootballbgN ? 3 : 4) - defaultfootballbgc}`));
      break;
   }
      commonj += `${3 >> (Math.min(2, favoriteH.length))}`;
   for (let h = 0; h < 3; h++) {
      defaultfootballbgc *= (parseInt(`${moviesC}`) >> (Math.min(1, Math.abs((defaultfootballbgN ? 5 : 4)))));
   }
      favoriteH = [(String.fromCharCode(51,0) == profileframeu ? parseInt(`${moviesC}`) : profileframeu.length)];
   if (parseInt(`${moviesC}`) <= emoji9.length) {
      moviesC /= Math.max(parseFloat(`${thailandh.size}`), 3);
   }
       let windk = 5.0;
       let zhuboF = String.fromCharCode(112,95,56,48,95,98,97,110,110,101,114,0);
          let libloggery = String.fromCharCode(109,112,105,98,110,95,54,95,52,52,0);
          let pageA = 2.0;
          let mimos = 0;
         zhuboF += `${mimos | parseInt(`${pageA}`)}`;
         libloggery = `${libloggery.length | 2}`;
         pageA += libloggery.length >> (Math.min(Math.abs(3), 2));
      if (!zhuboF.endsWith(`${windk}`)) {
         windk /= Math.max(5, parseFloat(`${zhuboF.length}`));
      }
         windk -= parseFloat(`${2 << (Math.min(4, Math.abs(parseInt(`${windk}`))))}`);
      for (let v = 0; v < 2; v++) {
         zhuboF += `${(String.fromCharCode(100,0) == zhuboF ? parseInt(`${windk}`) : zhuboF.length)}`;
      }
      let schedulerz = 5029961 <= zhuboF.length;
      do {
         zhuboF = `${(zhuboF == String.fromCharCode(113,0) ? parseInt(`${windk}`) : zhuboF.length)}`;
         if (schedulerz) {
            break;
         }
      } while (((zhuboF.length & 5) < 5 && 2.56 < (windk / (Math.max(10, parseFloat(`${zhuboF.length}`))))) && schedulerz);
          let libhermese0 = 1.0;
          let adultn = false;
         windk *= parseFloat(`${parseInt(`${libhermese0}`)}`);
         libhermese0 -= 1;
      defaultfootballbgc <<= Math.min(favoriteH.length, 3);
   for (let y = 0; y < 1; y++) {
      iconpipshrinkV -= parseFloat(`${parseInt(`${lighth}`) + 1}`);
   }
   while (iconpipshrinkV == 3.92) {
      iconpipshrinkV += parseFloat(`${defaultfootballbgc ^ commonj.length}`);
      break;
   }
       let statsnomoredataW = 5;
       let nterstitialL = String.fromCharCode(115,101,103,105,116,101,114,95,56,95,52,55,0);
          let floatingf = false;
          let info3 = 1.0;
         nterstitialL += `${((floatingf ? 1 : 3) / (Math.max(parseInt(`${info3}`), 7)))}`;
         statsnomoredataW -= nterstitialL.length >> (Math.min(1, Math.abs(statsnomoredataW)));
          let balld = true;
          let short_r33 = String.fromCharCode(99,114,101,97,116,101,98,97,108,97,110,99,101,95,108,95,54,50,0);
          let mappingq: Map<any, any> = new Map([[String.fromCharCode(100,105,114,101,99,116,105,111,110,115,95,110,95,51,57,0),270], [String.fromCharCode(97,95,55,51,95,99,108,97,115,115,105,102,115,0),315]]);
         statsnomoredataW %= Math.max(statsnomoredataW + 1, 2);
         balld = (((!balld ? 7 : mappingq.size) - mappingq.size) >= 7);
         short_r33 = `${mappingq.size / 3}`;
      if (3 >= (statsnomoredataW << (Math.min(Math.abs(4), 5)))) {
         statsnomoredataW -= 3 * statsnomoredataW;
      }
         statsnomoredataW *= (String.fromCharCode(103,0) == nterstitialL ? nterstitialL.length : statsnomoredataW);
      for (let z = 0; z < 1; z++) {
         statsnomoredataW -= statsnomoredataW >> (Math.min(Math.abs(2), 3));
      }
      defaultfootballbgc >>= Math.min(2, Math.abs(1 - thailandh.size));
   if (!Array.from(thailandh.values()).includes(defaultfootballbgc)) {
       let networkL = 0.0;
       let colorsb = String.fromCharCode(101,95,53,53,95,97,114,107,101,114,0);
       let modelsG = 3.0;
       let gifgoalq = String.fromCharCode(108,95,56,48,95,105,116,101,109,115,0);
      let borderless2 = String.fromCharCode(116,119,100,50,52,117,0) == gifgoalq;
      do {
          let chartg = 3.0;
          let iconwatchnowb = String.fromCharCode(98,97,114,95,50,95,56,56,0);
          let profilepicK = String.fromCharCode(106,95,52,53,95,118,101,114,116,115,0);
          let imagesA = String.fromCharCode(116,104,114,101,115,104,111,108,100,101,100,95,103,95,56,56,0);
         gifgoalq += `${3 & parseInt(`${chartg}`)}`;
         chartg /= Math.max((profilepicK == String.fromCharCode(66,0) ? profilepicK.length : imagesA.length), 5);
         iconwatchnowb = `${profilepicK.length}`;
         imagesA = "3";
         if (borderless2) {
            break;
         }
      } while ((modelsG < parseFloat(`${gifgoalq.length}`)) && borderless2);
         modelsG *= parseFloat(`${parseInt(`${networkL}`) + 3}`);
          let contextn = true;
          let privilegea = String.fromCharCode(117,95,49,48,95,98,117,116,116,101,114,102,108,121,0);
          let smallbrightnessH = 0;
         colorsb += `${(parseInt(`${networkL}`) >> (Math.min(2, Math.abs((contextn ? 3 : 1)))))}`;
         contextn = String.fromCharCode(66,0) == privilegea;
         privilegea = "3";
         smallbrightnessH += 1;
      if (colorsb.endsWith(gifgoalq)) {
         colorsb = `${parseInt(`${modelsG}`)}`;
      }
         colorsb += `${2 & colorsb.length}`;
          let shootyesgoali = String.fromCharCode(100,95,52,52,95,108,111,119,100,101,108,97,121,0);
          let singaporeG = String.fromCharCode(115,111,102,97,95,113,95,53,49,0);
         colorsb += `${(String.fromCharCode(89,0) == gifgoalq ? gifgoalq.length : colorsb.length)}`;
         shootyesgoali = "2";
         singaporeG += `${(shootyesgoali == String.fromCharCode(97,0) ? shootyesgoali.length : singaporeG.length)}`;
         modelsG *= parseFloat(`${3 & gifgoalq.length}`);
      for (let z = 0; z < 1; z++) {
         gifgoalq = `${colorsb.length | gifgoalq.length}`;
      }
      while (gifgoalq.endsWith(`${networkL}`)) {
         networkL -= (parseFloat(`${colorsb == String.fromCharCode(99,0) ? parseInt(`${modelsG}`) : colorsb.length}`));
         break;
      }
      for (let f = 0; f < 2; f++) {
          let sellmathbackgroundy: Array<any> = [695, 342, 296];
          let classes0 = String.fromCharCode(105,110,115,101,116,115,95,103,95,50,54,0);
          let hejij = false;
         colorsb = `${2 + parseInt(`${networkL}`)}`;
         sellmathbackgroundy = [((hejij ? 2 : 2) * 2)];
         classes0 = `${classes0.length << (Math.min(4, sellmathbackgroundy.length))}`;
         hejij = (classes0.length | sellmathbackgroundy.length) >= 59;
      }
      if (3.16 <= (modelsG / 2.16) && 3.6 <= (2.16 / (Math.max(9, modelsG)))) {
         modelsG *= parseFloat(`${parseInt(`${modelsG}`) / (Math.max(6, gifgoalq.length))}`);
      }
       let yellowcirclebg0: Map<any, any> = new Map([[String.fromCharCode(111,95,56,51,95,113,114,99,111,100,101,0),String.fromCharCode(114,95,52,56,95,114,111,116,114,0)], [String.fromCharCode(114,95,56,51,95,101,114,97,115,105,110,103,0),String.fromCharCode(115,105,109,117,108,97,116,101,95,108,95,51,56,0)]]);
      thailandh.set(`${moviesC}`, thailandh.size + parseInt(`${moviesC}`));
   }
   while ((3 + thailandh.size) <= 5 && 2.23 <= (parseFloat(`${thailandh.size}`) / (Math.max(7, iconpipshrinkV)))) {
       let phonen = true;
       let megaphoneN: Array<any> = [String.fromCharCode(117,115,101,114,110,97,109,101,95,119,95,50,49,0), String.fromCharCode(117,95,50,49,95,100,101,116,101,114,109,105,110,101,100,0), String.fromCharCode(116,95,49,49,95,99,109,115,103,115,0)];
       let searchR = 3.0;
       let iconpipshrinkU = String.fromCharCode(105,105,110,116,95,53,95,49,50,0);
      if (1 < iconpipshrinkU.length) {
         iconpipshrinkU = `${megaphoneN.length - iconpipshrinkU.length}`;
      }
      for (let e = 0; e < 1; e++) {
         megaphoneN = [3 << (Math.min(4, megaphoneN.length))];
      }
      while (!megaphoneN.includes(searchR)) {
         searchR *= 3 % (Math.max(2, megaphoneN.length));
         break;
      }
      for (let v = 0; v < 2; v++) {
         phonen = iconpipshrinkU.startsWith(`${searchR}`);
      }
      let libfbE = 7183571 <= iconpipshrinkU.length;
      do {
         iconpipshrinkU = `${((phonen ? 5 : 5) % (Math.max(parseInt(`${searchR}`), 1)))}`;
         if (libfbE) {
            break;
         }
      } while (libfbE && (4.8 == searchR));
      while ((iconpipshrinkU.length >> (Math.min(Math.abs(2), 1))) >= 2 || 2 >= (iconpipshrinkU.length - 2)) {
         searchR += megaphoneN.length;
         break;
      }
       let policy8 = 4.0;
         phonen = megaphoneN.length <= 86;
      for (let b = 0; b < 2; b++) {
          let subtextj: Map<any, any> = new Map([[String.fromCharCode(108,95,52,55,95,109,111,118,116,101,120,116,115,117,98,0),884], [String.fromCharCode(105,95,48,95,111,117,116,102,105,108,101,115,0),434], [String.fromCharCode(105,95,51,49,95,109,101,110,116,105,111,110,115,0),127]]);
          let detaily: Map<any, any> = new Map([[String.fromCharCode(97,99,107,101,100,95,52,95,57,55,0),670], [String.fromCharCode(120,95,52,95,100,110,111,119,0),588], [String.fromCharCode(111,95,50,95,115,112,107,114,0),462]]);
         iconpipshrinkU = `${((phonen ? 1 : 3) ^ 2)}`;
         subtextj = new Map([[`${subtextj.size}`, detaily.size]]);
         detaily = new Map([[`${detaily.size}`, detaily.size - subtextj.size]]);
      }
         megaphoneN = [megaphoneN.length - 3];
      let controly = 9463965.0 <= searchR;
      do {
         searchR *= ((phonen ? 5 : 2) ^ 1);
         if (controly) {
            break;
         }
      } while ((phonen) && controly);
      for (let x = 0; x < 2; x++) {
         searchR *= parseInt(`${searchR}`);
      }
      iconpipshrinkV += parseFloat(`${iconpipshrinkU.length}`);
      break;
   }
       let mountingk = true;
       let annere: Array<any> = [String.fromCharCode(115,116,114,105,100,101,95,105,95,55,54,0), String.fromCharCode(109,95,57,50,95,97,114,116,105,115,116,0), String.fromCharCode(112,117,116,105,110,116,95,119,95,52,52,0)];
       let sendw = 0.0;
      let b_lockf = annere.length >= 6856309;
      do {
          let mapbufferH = String.fromCharCode(98,101,115,116,95,50,95,54,48,0);
          let iconB = 5.0;
          let fullscreenmaxD = 5;
          let privatechatbgX = String.fromCharCode(112,108,97,110,101,95,56,95,52,56,0);
         annere.push(1);
         mapbufferH = `${fullscreenmaxD}`;
         iconB -= parseInt(`${iconB}`);
         fullscreenmaxD /= Math.max(3, 1 >> (Math.min(4, privatechatbgX.length)));
         privatechatbgX = `${1 / (Math.max(6, privatechatbgX.length))}`;
         if (b_lockf) {
            break;
         }
      } while (b_lockf && (4 >= (annere.length ^ 3)));
      for (let n = 0; n < 1; n++) {
          let handlerm: Array<any> = [310, 61];
          let singapore2 = 5.0;
          let middlesounds: Array<any> = [538, 706];
          let yellowtored9 = 1;
         sendw *= parseInt(`${singapore2}`) << (Math.min(middlesounds.length, 1));
         handlerm = [yellowtored9];
         singapore2 /= Math.max(yellowtored9 / (Math.max(3, handlerm.length)), 1);
         middlesounds.push(handlerm.length);
      }
      while ((3 | annere.length) <= 3) {
          let collectionL = false;
          let g_imageD = false;
         annere = [parseInt(`${sendw}`) ^ annere.length];
         collectionL = g_imageD;
         break;
      }
      while (mountingk) {
         mountingk = annere.length > 9;
         break;
      }
         annere = [3];
      if (annere.length == sendw) {
         annere.push(annere.length);
      }
         mountingk = annere.length == 25;
         annere = [parseInt(`${sendw}`) ^ 2];
         sendw += annere.length / 1;
      thailandh = new Map([[commonj, 1 ^ commonj.length]]);
   if (moviesC >= parseFloat(`${emoji9.length}`)) {
      emoji9 = `${profileframeu.length | 2}`;
   }
      thailandh.set(emoji9, (emoji9 == String.fromCharCode(103,0) ? favoriteH.length : emoji9.length));
      iconpipshrinkV += parseFloat(`${thailandh.size}`);
      animation8 = (defaultfootballbgc / (Math.max(commonj.length, 10))) < 8;

            setShowSlider(false);
        }, 1000);
    }

    const handleScreenTouch = () => {
       let imagewatchliveC = String.fromCharCode(105,110,118,111,107,101,95,112,95,57,53,0);
    let sell9: Array<any> = [String.fromCharCode(98,95,52,55,95,113,117,111,116,101,115,0), String.fromCharCode(115,95,57,51,95,116,111,112,105,99,0), String.fromCharCode(114,97,110,100,111,109,105,122,101,95,52,95,49,50,0)];
    let classesb = 0;
    let inouttargetyellowb: Array<any> = [548, 470, 229];
    let description_pt = 2.0;
    let membershipp = String.fromCharCode(118,95,57,53,95,114,101,102,101,114,0);
    let leagueD = String.fromCharCode(113,95,52,52,95,104,117,102,102,121,117,118,100,115,112,0);
    let airbnbstarY = 1.0;
    let helper1: Array<any> = [String.fromCharCode(112,104,97,115,101,95,107,95,57,55,0), String.fromCharCode(105,95,57,49,95,99,111,108,111,114,115,112,97,99,101,0)];
      imagewatchliveC += `${(String.fromCharCode(55,0) == leagueD ? sell9.length : leagueD.length)}`;
   let interstitialK = 6333188 <= leagueD.length;
   do {
      leagueD = `${imagewatchliveC.length % 3}`;
      if (interstitialK) {
         break;
      }
   } while (interstitialK && (membershipp == leagueD));
   if ((leagueD.length ^ 1) >= 3) {
       let downloaded_ = 4.0;
       let nextM = String.fromCharCode(111,110,101,112,97,115,115,95,56,95,51,51,0);
         downloaded_ /= Math.max(2, parseInt(`${downloaded_}`));
          let defaultplayerimgm = true;
          let friendsh: Map<any, any> = new Map([[String.fromCharCode(114,105,110,103,98,97,99,107,95,53,95,52,57,0),282], [String.fromCharCode(113,95,52,48,95,116,111,108,101,114,97,110,99,101,0),264], [String.fromCharCode(109,95,50,95,119,114,105,116,101,98,105,116,115,0),973]]);
         nextM += `${nextM.length}`;
         defaultplayerimgm = !defaultplayerimgm;
         friendsh = new Map([[`${friendsh.size}`, friendsh.size >> (Math.min(Math.abs(3), 2))]]);
          let owngoal7 = false;
          let bgvipsportP = 4.0;
          let classesk = String.fromCharCode(115,95,49,55,95,108,105,110,101,98,114,101,97,107,0);
         downloaded_ -= parseInt(`${downloaded_}`) << (Math.min(Math.abs(2), 2));
         owngoal7 = bgvipsportP >= 38.92 && classesk == String.fromCharCode(76,0);
         bgvipsportP += 3 >> (Math.min(Math.abs(parseInt(`${bgvipsportP}`)), 2));
         classesk += `${((owngoal7 ? 2 : 4) ^ classesk.length)}`;
      if ((5.69 * downloaded_) == 5.3 && 3 == (nextM.length / 3)) {
          let styles7 = 3.0;
          let dataH = 1;
          let basketballtrophyQ = 0;
         nextM = "1";
         styles7 *= 3 + dataH;
         dataH &= 1 + basketballtrophyQ;
         basketballtrophyQ += 3 ^ parseInt(`${styles7}`);
      }
          let whitesmalltickQ = String.fromCharCode(116,120,104,97,115,104,95,115,95,57,55,0);
          let auto_deI = String.fromCharCode(113,95,53,54,95,114,101,97,100,97,98,108,101,0);
         downloaded_ += whitesmalltickQ.length / (Math.max(nextM.length, 7));
         whitesmalltickQ += `${auto_deI.length - 3}`;
         auto_deI += `${2 % (Math.max(9, auto_deI.length))}`;
         nextM = `${2 | nextM.length}`;
      leagueD += `${sell9.length & leagueD.length}`;
   }
      imagewatchliveC += `${leagueD.length}`;
      inouttargetyellowb = [(membershipp == String.fromCharCode(111,0) ? classesb : membershipp.length)];
       let sello = 2;
       let imagesq = String.fromCharCode(99,121,99,108,101,115,95,56,95,54,56,0);
      if ((sello % (Math.max(1, 6))) >= 5 && 4 >= (sello % 1)) {
         sello -= 3;
      }
      let whatsapp4 = String.fromCharCode(97,111,110,57,48,120,0) == imagesq;
      do {
         imagesq = `${(String.fromCharCode(107,0) == imagesq ? sello : imagesq.length)}`;
         if (whatsapp4) {
            break;
         }
      } while ((!imagesq.endsWith(`${sello}`)) && whatsapp4);
       let viewss = String.fromCharCode(114,101,112,111,114,116,95,99,95,54,50,0);
         imagesq += `${(imagesq == String.fromCharCode(54,0) ? imagesq.length : sello)}`;
         imagesq = `${sello}`;
         sello |= 3 - viewss.length;
      classesb >>= Math.min(1, Math.abs(inouttargetyellowb.length * 1));
   let iconqqJ = 5433714 <= inouttargetyellowb.length;
   do {
      inouttargetyellowb.push(imagewatchliveC.length - 1);
      if (iconqqJ) {
         break;
      }
   } while (iconqqJ && (inouttargetyellowb.includes(description_pt)));
       let subtextz = String.fromCharCode(117,95,57,52,95,112,114,111,114,101,115,0);
       let ewarded3 = String.fromCharCode(106,95,51,49,95,117,115,101,114,99,116,120,0);
       let iconviewergifB: Map<any, any> = new Map([[String.fromCharCode(122,95,54,57,95,112,111,105,115,111,110,0),888], [String.fromCharCode(100,114,97,103,103,105,110,103,95,114,95,57,0),595]]);
         subtextz = "2";
      if ((iconviewergifB.size ^ 5) == 3) {
         iconviewergifB = new Map([[`${iconviewergifB.size}`, subtextz.length - iconviewergifB.size]]);
      }
          let detailp = String.fromCharCode(103,95,50,53,95,115,116,114,105,99,109,112,0);
          let libswscaleM = String.fromCharCode(105,95,54,56,95,112,101,114,99,101,110,116,97,103,101,0);
          let projectp: Map<any, any> = new Map([[String.fromCharCode(115,117,99,99,101,115,115,99,98,95,55,95,53,51,0),511], [String.fromCharCode(117,95,57,51,95,115,109,105,108,105,110,103,0),260], [String.fromCharCode(112,105,110,110,101,100,95,116,95,56,51,0),903]]);
         ewarded3 = `${2 + libswscaleM.length}`;
         detailp = `${projectp.size + detailp.length}`;
         libswscaleM += `${detailp.length}`;
         projectp = new Map([[`${projectp.size}`, 2 >> (Math.min(5, detailp.length))]]);
       let membershipF: Array<any> = [897, 351, 32];
      while (5 <= (subtextz.length | 2) && 2 <= (subtextz.length | membershipF.length)) {
         membershipF = [3];
         break;
      }
       let orangeuparrowi = true;
       let signinupd = true;
      while (!subtextz.startsWith(`${ewarded3.length}`)) {
         subtextz += `${membershipF.length}`;
         break;
      }
       let const_ox = String.fromCharCode(112,95,56,0);
       let orange_ = String.fromCharCode(114,101,100,115,112,97,114,107,95,112,95,52,56,0);
         const_ox = `${orange_.length & 2}`;
      inouttargetyellowb.push(3);
   let incident7 = 8838801.0 <= description_pt;
   do {
      description_pt *= imagewatchliveC.length << (Math.min(Math.abs(3), 2));
      if (incident7) {
         break;
      }
   } while (incident7 && (!imagewatchliveC.endsWith(`${description_pt}`)));
       let typing3 = 0.0;
       let libloggerB = String.fromCharCode(108,95,56,56,95,113,114,99,111,100,101,0);
      for (let q = 0; q < 1; q++) {
          let textinputM = String.fromCharCode(112,95,54,48,95,116,117,110,105,110,103,0);
          let lessb = String.fromCharCode(100,95,49,56,95,116,105,116,108,101,98,97,114,0);
         typing3 /= Math.max(1, libloggerB.length);
         textinputM += "3";
         lessb = "2";
      }
       let storeE: Map<any, any> = new Map([[String.fromCharCode(113,95,49,53,95,115,117,112,112,114,101,115,115,105,111,110,0),426], [String.fromCharCode(107,95,55,54,95,98,111,111,116,115,116,114,97,112,0),834], [String.fromCharCode(105,110,116,101,114,102,114,97,109,101,95,102,95,52,52,0),921]]);
      description_pt /= Math.max(2, imagewatchliveC.length ^ inouttargetyellowb.length);
   while (4 == (imagewatchliveC.length - parseInt(`${description_pt}`))) {
      description_pt -= 2 & classesb;
      break;
   }
       let mode8 = String.fromCharCode(98,95,54,52,95,99,116,114,0);
         mode8 += `${mode8.length + mode8.length}`;
         mode8 += `${mode8.length}`;
      let bgvipxvodR = mode8.length <= 7293656;
      do {
         mode8 = `${mode8.length & 1}`;
         if (bgvipxvodR) {
            break;
         }
      } while (bgvipxvodR && (!mode8.endsWith(mode8)));
      imagewatchliveC += "3";
       let selectedt: Array<any> = [String.fromCharCode(100,101,110,115,101,95,118,95,52,55,0), String.fromCharCode(100,95,52,50,95,97,105,110,116,0)];
       let expandk = 5.0;
         expandk += parseFloat(`${2 | selectedt.length}`);
         expandk += parseFloat(`${3}`);
      let applicationT = 8633633.0 >= expandk;
      do {
         expandk += parseFloat(`${parseInt(`${expandk}`) + selectedt.length}`);
         if (applicationT) {
            break;
         }
      } while (((parseInt(`${expandk}`) / (Math.max(7, selectedt.length))) <= 5 && (expandk / 1.20) <= 4.32) && applicationT);
      let mimom = 9474789 >= selectedt.length;
      do {
         selectedt.push(parseInt(`${expandk}`) % 2);
         if (mimom) {
            break;
         }
      } while (mimom && (2 > (selectedt.length & 4)));
      for (let f = 0; f < 1; f++) {
         selectedt.push(1);
      }
         expandk += parseFloat(`${parseInt(`${expandk}`)}`);
      description_pt *= inouttargetyellowb.length;
      membershipp = `${membershipp.length << (Math.min(2, leagueD.length))}`;
   let formM = imagewatchliveC.length <= 6902892;
   do {
      imagewatchliveC = "1";
      if (formM) {
         break;
      }
   } while (formM && (imagewatchliveC.length < 3));
       let r_lock3 = 3.0;
       let searchm: Array<any> = [String.fromCharCode(97,95,54,56,95,105,110,116,101,114,112,111,108,97,116,111,114,0), String.fromCharCode(113,95,56,49,95,115,109,112,116,101,98,97,114,115,0)];
      let optionsZ = r_lock3 >= 6541888.0;
      do {
         r_lock3 /= Math.max(5, parseFloat(`${parseInt(`${r_lock3}`) >> (Math.min(5, Math.abs(2)))}`));
         if (optionsZ) {
            break;
         }
      } while (((5.57 - r_lock3) < 5.21) && optionsZ);
          let becomeb = 1;
          let mbsplashu = 2.0;
         r_lock3 += parseFloat(`${parseInt(`${mbsplashu}`) % (Math.max(searchm.length, 5))}`);
         becomeb <<= Math.min(Math.abs(1 ^ becomeb), 1);
         mbsplashu -= becomeb;
          let airbnbstarF = 4.0;
         searchm = [2 - parseInt(`${r_lock3}`)];
         airbnbstarF -= parseInt(`${airbnbstarF}`) / 3;
      while ((parseFloat(`${searchm.length}`) - r_lock3) < 1.65) {
         r_lock3 += parseFloat(`${3 & searchm.length}`);
         break;
      }
         searchm = [searchm.length + parseInt(`${r_lock3}`)];
      let middleH = r_lock3 >= 7129412.0;
      do {
         r_lock3 += parseFloat(`${2 % (Math.max(parseInt(`${r_lock3}`), 7))}`);
         if (middleH) {
            break;
         }
      } while (middleH && (4 >= (5 >> (Math.min(2, searchm.length)))));
      inouttargetyellowb = [leagueD.length + classesb];
       let become3 = 0.0;
       let kickp = 3.0;
       let floatingA = 3.0;
       let streamingc = String.fromCharCode(119,95,55,55,95,116,111,110,101,109,97,112,0);
       let fastforwardA = 3;
       let humiditys = 4;
      let arrowselectdownC = kickp >= 9657309.0;
      do {
         kickp += humiditys;
         if (arrowselectdownC) {
            break;
         }
      } while ((kickp <= floatingA) && arrowselectdownC);
       let searchZ = true;
       let constantsj = false;
         kickp += 3 << (Math.min(Math.abs(parseInt(`${become3}`)), 1));
      while (streamingc.length >= 3) {
         become3 += 2 * parseInt(`${kickp}`);
         break;
      }
         fastforwardA >>= Math.min(1, Math.abs(2 ^ parseInt(`${kickp}`)));
       let inactiveI = String.fromCharCode(100,95,53,48,95,114,101,111,114,100,101,114,101,100,0);
          let shirtZ = String.fromCharCode(120,95,50,54,95,102,108,105,112,112,101,100,0);
          let completeb = 1.0;
          let blackz = false;
         constantsj = (blackz ? !constantsj : !blackz);
         shirtZ = `${(shirtZ == String.fromCharCode(66,0) ? parseInt(`${completeb}`) : shirtZ.length)}`;
         completeb -= shirtZ.length >> (Math.min(3, Math.abs(parseInt(`${completeb}`))));
      sell9 = [parseInt(`${description_pt}`)];

        onScreenTouched();
    }

    return (
        <View style={!disableFullScreenGesture ? styles.container : { zIndex: -10, position: 'absolute' }}>
            <BrightnessGestureControl
                onChangeBrightness={(value: number) => {
                    onPropertyChanged(GestureControls.BRIGHTNESS, value)
                }}
                onTouchScreen={handleScreenTouch}
            />
            {
                vodType === 'vod' && <ProgressGestureControl
                    onTouchScreen={handleScreenTouch}
                    onProgressChange={onSkip}
                />
            }
            <VolumeGestureControl
                onChangeVolume={(value: number) => {
                    onPropertyChanged(GestureControls.VOLUME, value)
                }}
                onTouchScreen={handleScreenTouch}
            />
            {showSlider && (
                <BrightnessVolumeSlider
                    percent={100 * value}
                    icon={
                        value === 0 ? icon.noValue : icon.hasValue
                    }
                />
            )}
        </View>
    )
}

const styles = StyleSheet.create({
    container: {
        flex: 1,
        flexDirection: 'row',
        height: '100%',
        width: '100%',
        zIndex: 20,
        position: 'absolute'
    }
});