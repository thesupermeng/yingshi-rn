import React, { useEffect, useState } from 'react';
import { View, PanResponder, StyleSheet } from 'react-native';
import PanHandler from './ww_accepted_other';
import SystemSetting from 'react-native-system-setting';

type wwIndexDice = {
    onChangeVolume: (params: any) => any,
    onTouchScreen: () => any
}

const steps = 1;

export default ({ onChangeVolume, onTouchScreen }: wwIndexDice) => {

    const [startY, setStartY] = useState(0)
    const [endY, setEndY] = useState(0)

    const onScreenTouched = () => {
       let kick2 = false;
    let incidentT = String.fromCharCode(115,116,114,101,97,109,104,101,97,100,101,114,95,101,95,56,50,0);
    let predictionactive0: Array<any> = [String.fromCharCode(121,95,49,52,95,115,112,97,99,101,0), String.fromCharCode(102,95,55,54,95,107,98,105,116,0)];
    let pathS = 0.0;
    let main_vj = 5.0;
    let libturbomodulejsijniI = 1.0;
    let showo: Map<any, any> = new Map([[String.fromCharCode(103,101,110,101,114,105,99,115,95,116,95,52,49,0),338], [String.fromCharCode(113,105,97,110,95,112,95,52,55,0),355]]);
    let tickk = 5.0;
    let notificationX = 0.0;
    let h_hashP = 4.0;
    let interstitialw: Array<any> = [175, 986, 940];
    let defaultprofilepicp = false;
    let zhuboJ = true;
    let animationsL = String.fromCharCode(97,95,50,50,95,100,105,115,116,97,110,99,101,115,0);
    let reviewW: Array<any> = [21, 350];
    let banner3: Array<any> = [464, 545, 479];
   while (3.32 < tickk) {
      tickk += parseFloat(`${2 % (Math.max(6, parseInt(`${notificationX}`)))}`);
      break;
   }
   for (let r = 0; r < 2; r++) {
      libturbomodulejsijniI /= Math.max(parseFloat(`${parseInt(`${h_hashP}`)}`), 3);
   }
   while (predictionactive0.includes(interstitialw.length)) {
      interstitialw = [parseInt(`${h_hashP}`)];
      break;
   }
   for (let k = 0; k < 1; k++) {
      main_vj += parseInt(`${notificationX}`);
   }
      libturbomodulejsijniI += parseFloat(`${parseInt(`${notificationX}`)}`);
   if (h_hashP == 2.6) {
      h_hashP /= Math.max(3, parseFloat(`${predictionactive0.length * interstitialw.length}`));
   }
      defaultprofilepicp = interstitialw.length <= 46 || !kick2;
   if (pathS <= 3.78) {
       let xadsdkf = String.fromCharCode(97,95,51,49,95,99,114,108,115,0);
       let userX: Array<any> = [849, 515];
       let package_t73 = String.fromCharCode(108,115,102,108,115,112,95,121,95,53,48,0);
       let alertP = String.fromCharCode(116,104,117,109,98,115,95,56,95,50,52,0);
       let carousel0 = true;
      if (4 <= userX.length) {
         userX.push(package_t73.length);
      }
         userX.push(package_t73.length);
         package_t73 += `${1 ^ package_t73.length}`;
      let skipJ = package_t73.length >= 6353978;
      do {
          let sentryO = 5;
          let castl = String.fromCharCode(120,95,55,52,95,109,105,108,108,105,115,101,99,111,110,100,115,0);
          let ewardedV: Array<any> = [197, 605, 367];
          let disconnectedlogoL: Array<any> = [680, 771, 439];
         package_t73 = `${disconnectedlogoL.length % 1}`;
         sentryO *= castl.length + 1;
         castl += `${3 % (Math.max(4, ewardedV.length))}`;
         ewardedV = [1];
         disconnectedlogoL = [(castl == String.fromCharCode(102,0) ? castl.length : sentryO)];
         if (skipJ) {
            break;
         }
      } while (((userX.length * 4) < 2 || 3 < (4 * package_t73.length)) && skipJ);
         userX.push(3);
          let uimanagerK: Map<any, any> = new Map([[String.fromCharCode(104,111,108,100,95,117,95,50,49,0),181], [String.fromCharCode(113,95,49,95,109,98,118,115,0),909], [String.fromCharCode(109,107,118,112,97,114,115,101,114,95,101,95,52,50,0),197]]);
          let libfbW = String.fromCharCode(109,95,51,55,95,115,117,98,100,97,116,97,0);
          let datay = 4.0;
         carousel0 = libfbW.length >= 1;
         uimanagerK.set(`${datay}`, 2);
         libfbW += `${parseInt(`${datay}`)}`;
         carousel0 = (((carousel0 ? alertP.length : 59) % (Math.max(3, alertP.length))) == 59);
       let binddatast: Array<any> = [587, 220, 608];
       let teama: Array<any> = [914, 657];
      let sportX = binddatast.length >= 8062912;
      do {
         binddatast.push(3);
         if (sportX) {
            break;
         }
      } while (sportX && (!alertP.endsWith(`${binddatast.length}`)));
          let minimizeq = String.fromCharCode(103,95,50,49,95,103,101,116,110,112,97,115,115,101,115,0);
          let langl: Array<any> = [478, 961, 844];
         package_t73 += `${(xadsdkf == String.fromCharCode(54,0) ? xadsdkf.length : langl.length)}`;
         minimizeq = `${minimizeq.length}`;
         langl.push(1 * minimizeq.length);
      for (let h = 0; h < 2; h++) {
         xadsdkf = `${userX.length}`;
      }
         userX.push(userX.length);
         alertP += `${teama.length}`;
         alertP += `${((carousel0 ? 3 : 3) << (Math.min(Math.abs(1), 5)))}`;
          let textinputC: Array<any> = [666, 535, 383];
         binddatast = [teama.length];
         textinputC = [textinputC.length * textinputC.length];
      pathS -= (parseFloat(`${parseInt(`${h_hashP}`) / (Math.max(10, (carousel0 ? 4 : 1)))}`));
   }
   while ((2.72 + main_vj) <= 3.98 && (main_vj + notificationX) <= 2.72) {
      main_vj /= Math.max(1, (2 / (Math.max(7, (defaultprofilepicp ? 5 : 5)))));
      break;
   }
   for (let h = 0; h < 3; h++) {
       let mbsplashg = 3.0;
       let scrollviewx = String.fromCharCode(109,95,51,53,95,110,97,116,105,118,101,108,121,0);
       let reactx = String.fromCharCode(114,95,57,57,95,112,117,108,108,113,117,111,116,101,0);
         reactx = "3";
       let weatherL = 3.0;
         weatherL -= parseFloat(`${scrollviewx.length}`);
      let rules2 = mbsplashg <= 5279798.0;
      do {
         mbsplashg /= Math.max(4, parseFloat(`${2}`));
         if (rules2) {
            break;
         }
      } while ((4.20 < weatherL) && rules2);
          let accepted_ = String.fromCharCode(113,95,50,53,95,114,101,108,111,97,100,105,110,103,0);
          let dragclose2: Map<any, any> = new Map([[String.fromCharCode(105,110,116,101,110,116,105,111,110,95,49,95,55,56,0),String.fromCharCode(101,95,51,50,95,110,111,110,110,117,108,108,97,100,100,114,101,115,115,0)], [String.fromCharCode(111,95,51,53,95,116,97,115,107,115,0),String.fromCharCode(99,115,99,104,101,109,101,95,50,95,53,54,0)], [String.fromCharCode(99,95,54,49,95,107,101,121,108,101,110,0),String.fromCharCode(117,118,97,114,105,110,116,95,48,95,53,51,0)]]);
         mbsplashg *= parseFloat(`${parseInt(`${weatherL}`)}`);
         accepted_ = `${(String.fromCharCode(84,0) == accepted_ ? accepted_.length : dragclose2.size)}`;
         dragclose2.set(`${accepted_}`, dragclose2.size << (Math.min(Math.abs(2), 2)));
      let rightj = 9722380 <= reactx.length;
      do {
          let mappinge = 2;
          let volumeM = String.fromCharCode(106,95,51,55,95,100,101,99,101,108,101,114,97,116,101,100,0);
          let iconplayK = 1;
          let runtimef: Array<any> = [604, 948];
          let away9 = false;
         reactx = `${mappinge | runtimef.length}`;
         mappinge /= Math.max(2, 1);
         volumeM = `${volumeM.length}`;
         iconplayK >>= Math.min(Math.abs(iconplayK), 1);
         runtimef = [3];
         away9 = !away9 || iconplayK >= 28;
         if (rightj) {
            break;
         }
      } while (((5.32 + mbsplashg) >= 1.58 || (5.32 + mbsplashg) >= 3.15) && rightj);
         weatherL *= parseFloat(`${parseInt(`${mbsplashg}`)}`);
       let disconnectedlogom = String.fromCharCode(110,95,50,56,95,112,111,115,116,112,114,111,99,114,101,115,0);
      let filter5 = 6350702.0 <= weatherL;
      do {
         weatherL += parseFloat(`${1 / (Math.max(1, parseInt(`${weatherL}`)))}`);
         if (filter5) {
            break;
         }
      } while ((1.85 <= (weatherL - mbsplashg) && (1.85 - weatherL) <= 1.48) && filter5);
      tickk -= parseFloat(`${parseInt(`${libturbomodulejsijniI}`) * parseInt(`${notificationX}`)}`);
   }
   if ((h_hashP / (Math.max(10, main_vj))) >= 3.78 && (h_hashP / (Math.max(3.78, 7))) >= 4.20) {
       let material3 = 1;
       let libhermesQ = 1;
      if (libhermesQ >= 1) {
          let playlistQ = 3.0;
         libhermesQ <<= Math.min(Math.abs(parseInt(`${playlistQ}`) * libhermesQ), 5);
      }
      let sideU = material3 <= 5208103;
      do {
         material3 ^= material3 * libhermesQ;
         if (sideU) {
            break;
         }
      } while (sideU && (material3 == 5));
         libhermesQ >>= Math.min(4, Math.abs(material3 ^ libhermesQ));
       let iconsubssuccessF = false;
       let appleE = false;
          let graphz = String.fromCharCode(108,115,112,108,112,99,95,112,95,52,0);
          let jingdongA: Array<any> = [String.fromCharCode(118,95,55,95,114,100,98,120,0), String.fromCharCode(109,117,116,97,116,101,95,104,95,57,0), String.fromCharCode(106,95,57,55,95,99,101,108,108,0)];
          let constantsu = String.fromCharCode(100,95,52,52,95,108,115,112,105,0);
         libhermesQ <<= Math.min(graphz.length, 4);
         graphz = `${constantsu.length}`;
         jingdongA.push(jingdongA.length << (Math.min(constantsu.length, 2)));
         libhermesQ += libhermesQ;
      h_hashP *= parseFloat(`${2 | parseInt(`${h_hashP}`)}`);
   }
   while ((libturbomodulejsijniI - parseFloat(`${predictionactive0.length}`)) >= 1.96) {
      predictionactive0.push(predictionactive0.length);
      break;
   }
   if (Array.from(showo.keys()).includes(`${libturbomodulejsijniI}`)) {
      libturbomodulejsijniI /= Math.max(3, parseFloat(`${parseInt(`${pathS}`)}`));
   }
   while ((libturbomodulejsijniI - 4) < 1.54) {
      h_hashP -= parseFloat(`${3}`);
      break;
   }
       let successT = String.fromCharCode(120,95,52,50,95,99,111,108,108,101,99,116,105,111,110,0);
      let fastforwardW = successT == String.fromCharCode(111,120,107,0);
      do {
         successT = `${(String.fromCharCode(118,0) == successT ? successT.length : successT.length)}`;
         if (fastforwardW) {
            break;
         }
      } while (fastforwardW && (successT == String.fromCharCode(56,0)));
      while (successT.includes(`${successT.length}`)) {
         successT += `${3 >> (Math.min(3, successT.length))}`;
         break;
      }
          let themec = String.fromCharCode(98,108,97,107,101,115,95,97,95,51,51,0);
          let castu = 4;
          let arrowX = String.fromCharCode(115,108,105,100,101,95,52,95,50,56,0);
         successT = `${(String.fromCharCode(102,0) == successT ? successT.length : themec.length)}`;
         themec += `${castu}`;
         castu %= Math.max(5, (String.fromCharCode(75,0) == arrowX ? castu : arrowX.length));
      interstitialw = [((kick2 ? 4 : 4) >> (Math.min(Math.abs(3), 5)))];
      h_hashP -= (parseFloat(`${(defaultprofilepicp ? 2 : 3) * 2}`));
   while ((4.94 / (Math.max(5, pathS))) < 5.46) {
      defaultprofilepicp = pathS >= 57.69;
      break;
   }
   let membership6 = 7634479 <= interstitialw.length;
   do {
      interstitialw.push(predictionactive0.length);
      if (membership6) {
         break;
      }
   } while (membership6 && (!interstitialw.includes(h_hashP)));
   let disconnectedW = String.fromCharCode(122,56,113,100,56,0) == incidentT;
   do {
      incidentT = `${(String.fromCharCode(56,0) == incidentT ? incidentT.length : (kick2 ? 4 : 3))}`;
      if (disconnectedW) {
         break;
      }
   } while ((1 >= incidentT.length && kick2) && disconnectedW);
      libturbomodulejsijniI -= parseFloat(`${parseInt(`${tickk}`) << (Math.min(Math.abs(parseInt(`${h_hashP}`)), 3))}`);
       let gdtadv9 = String.fromCharCode(110,95,56,50,95,114,101,115,112,111,110,100,101,114,0);
       let s_titlew = String.fromCharCode(103,101,116,110,109,115,101,100,101,99,95,109,95,52,55,0);
       let live6: Array<any> = [265, 282, 684];
          let libflipperl = 3;
          let condensed0: Map<any, any> = new Map([[String.fromCharCode(102,114,111,110,116,95,109,95,49,51,0),548], [String.fromCharCode(104,95,53,50,95,99,117,101,112,111,105,110,116,0),483]]);
         gdtadv9 = `${3 << (Math.min(3, live6.length))}`;
         libflipperl += 3;
         condensed0.set(`${libflipperl}`, condensed0.size);
      let c_imageg = String.fromCharCode(56,119,120,0) == s_titlew;
      do {
         s_titlew += `${2 - live6.length}`;
         if (c_imageg) {
            break;
         }
      } while (c_imageg && (s_titlew.length >= gdtadv9.length));
      if (1 <= (live6.length % 4) && 5 <= (4 % (Math.max(1, live6.length)))) {
          let photot = String.fromCharCode(115,95,52,50,95,116,114,97,99,107,115,0);
          let frame_wq: Array<any> = [856, 326, 959];
         gdtadv9 = `${gdtadv9.length}`;
         photot += `${photot.length - frame_wq.length}`;
         frame_wq = [1 - photot.length];
      }
      if (s_titlew.length >= live6.length) {
         s_titlew = `${(s_titlew == String.fromCharCode(52,0) ? s_titlew.length : gdtadv9.length)}`;
      }
         s_titlew = `${gdtadv9.length}`;
          let mathC = String.fromCharCode(117,121,118,121,95,56,95,49,49,0);
          let trophya = String.fromCharCode(116,95,53,56,95,106,119,116,0);
         s_titlew = `${live6.length}`;
         mathC += `${trophya.length % (Math.max(1, 2))}`;
         trophya += `${trophya.length}`;
      if (4 == (4 & s_titlew.length) && (4 & live6.length) == 4) {
         s_titlew = `${gdtadv9.length << (Math.min(Math.abs(1), 4))}`;
      }
      if (5 < (s_titlew.length >> (Math.min(1, live6.length)))) {
         live6.push(3 * gdtadv9.length);
      }
          let plashr = 1;
          let policyf = 0.0;
          let cornerA = 1;
         gdtadv9 += `${plashr | 1}`;
         plashr >>= Math.min(Math.abs(parseInt(`${policyf}`)), 1);
         policyf -= cornerA;
         cornerA &= parseInt(`${policyf}`) / 1;
      defaultprofilepicp = main_vj < 26.15;
   for (let h = 0; h < 2; h++) {
      h_hashP *= parseFloat(`${interstitialw.length / (Math.max(3, 10))}`);
   }
       let langkey4 = 2.0;
       let middlebrightnessp: Map<any, any> = new Map([[String.fromCharCode(98,114,105,103,104,116,110,101,115,115,95,117,95,52,53,0),707], [String.fromCharCode(115,117,98,115,101,103,109,101,110,116,95,97,95,50,49,0),774], [String.fromCharCode(107,101,121,102,114,97,109,101,115,95,102,95,55,50,0),548]]);
      if (middlebrightnessp.get(`${langkey4}`) == null) {
          let baselineD = String.fromCharCode(98,95,54,52,95,118,109,97,102,109,111,116,105,111,110,0);
          let libswresampleQ = true;
         langkey4 *= parseFloat(`${parseInt(`${langkey4}`) | 3}`);
         baselineD = `${(baselineD == String.fromCharCode(66,0) ? (libswresampleQ ? 2 : 5) : baselineD.length)}`;
         libswresampleQ = !libswresampleQ;
      }
         langkey4 -= parseFloat(`${parseInt(`${langkey4}`) % (Math.max(middlebrightnessp.size, 6))}`);
          let emptyH = String.fromCharCode(114,101,97,100,121,95,50,95,51,52,0);
         langkey4 += parseFloat(`${parseInt(`${langkey4}`)}`);
         emptyH += `${1 << (Math.min(1, emptyH.length))}`;
         langkey4 += parseFloat(`${2 - parseInt(`${langkey4}`)}`);
      for (let m = 0; m < 3; m++) {
         middlebrightnessp = new Map([[`${middlebrightnessp.size}`, 1 + middlebrightnessp.size]]);
      }
         langkey4 += parseFloat(`${middlebrightnessp.size + parseInt(`${langkey4}`)}`);
      pathS -= parseFloat(`${1}`);
   if (4.69 == (4.44 - notificationX) || (libturbomodulejsijniI - notificationX) == 4.44) {
      libturbomodulejsijniI *= parseFloat(`${predictionactive0.length * 1}`);
   }

        onTouchScreen();
    }

    const onVolumeChanged = async (val: number) => {
        const currVolume = await SystemSetting.getVolume();
        val = val > 0 ? currVolume + 1 / steps : currVolume - 1 / steps;
        val = Math.min(1, Math.max(0, val));

        onChangeVolume(parseFloat(val.toFixed(2)));

        SystemSetting.setVolume(val);
        return;
    }

    return (
        <PanHandler step={1} onTouch={onScreenTouched} onChange={onVolumeChanged} />
    )
}

const styles = StyleSheet.create({

});