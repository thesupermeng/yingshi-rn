import { FlatList, StyleSheet, Text, View } from "react-native";
import CommentIcon from '@static/images/statisticsBasketballicon.svg';
import BlacklistIcon from '@static/images/baiduHandler.svg';
import ReportIcon from '@static/images/eabafadfadddbafeddddeeefeaafHash.svg';
import { TouchableOpacity } from "react-native-gesture-handler";
import { mayi_Attributedstring } from "@utility/mayi_source";
import { CBottomSheet } from "../../components/atoms";
import { useMemo, useState } from "react";
import { useTheme } from "@react-navigation/native";
import { ReportBottomSheet } from "../bottomSheet/mayi_videobufferloading_whistle";
import { mayi_TrashBootsplash } from "../../../routes/mayi_topon";

enum HWIconnotificationnewSigmob {
    色情低俗 = '色情低俗',
    广告 = '广告',
    令人恶心 = '令人恶心',
    违纪违法 = '违纪违法',
    政治敏感 = '政治敏感',
}

type mayi_GoogleViews = {
    vodId: string,
    vodName: string,
    comments?: {
        douban_reviews: any[],
        total_review: number,
    },
}

export const IconGroup = ({
    vodId,
    vodName,
    comments,
}: mayi_GoogleViews) => {
    const { colors, textVariants } = useTheme();
    const styles = useMemo(() => createStyles({ colors }), [colors]);

    const [showReportModel, setShowReportModel] = useState(false);

    const onCommentPress = () => {
       let backicons = 4.0;
    let iconclosewhitewithbgj = false;
    let group2 = String.fromCharCode(114,101,102,115,95,119,95,50,57,0);
    let weatherA = String.fromCharCode(116,95,50,51,95,97,110,97,108,111,103,0);
    let blacklistF = false;
    let overi = String.fromCharCode(116,95,56,95,118,111,114,98,105,115,99,111,109,109,101,110,116,0);
    let sell3 = String.fromCharCode(108,111,103,116,97,110,95,102,95,55,0);
    let transferu = false;
   let viewerH = String.fromCharCode(103,102,56,101,55,106,107,99,0) == overi;
   do {
      overi = `${((blacklistF ? 2 : 4))}`;
      if (viewerH) {
         break;
      }
   } while (viewerH && (overi.startsWith(`${blacklistF}`)));
      backicons -= parseFloat(`${group2.length % (Math.max(2, 4))}`);
      overi += `${(String.fromCharCode(122,0) == weatherA ? (iconclosewhitewithbgj ? 4 : 2) : weatherA.length)}`;
      sell3 += `${1 % (Math.max(4, parseInt(`${backicons}`)))}`;

        mayi_TrashBootsplash.toName('全部评论', {
            params: {
                vod_id: vodId,
                vod_name: vodName,
                commentItems: comments?.douban_reviews ?? [],
            },
        });
    }

    const onBlackListPress = () => {
       let shown = 2.0;
    let filedc = String.fromCharCode(107,95,57,54,95,97,116,111,110,0);
    let filed6 = 4;
    let stringm = String.fromCharCode(112,114,111,100,117,99,116,115,95,56,95,52,55,0);
    let iconbackwhitel = 4.0;
    let holderV = 5.0;
    let libruntimeexecutorJ = String.fromCharCode(99,97,108,108,115,95,112,95,50,54,0);
    let recommendationb = 5.0;
    let username6 = String.fromCharCode(102,117,108,108,121,95,107,95,52,54,0);
    let bridgeT = 3.0;
    let turndownC = true;
    let collectionn = 4;
      filedc = "3 + filed6";
       let configW = 3.0;
       let videocommony = 4.0;
       let componentC = true;
          let libswscalej = 0.0;
         videocommony *= parseInt(`${configW}`);
         libswscalej -= parseInt(`${libswscalej}`) - parseInt(`${libswscalej}`);
         videocommony -= parseInt(`${configW}`);
       let mergerT = String.fromCharCode(99,111,108,108,101,99,116,105,111,110,95,112,95,52,50,0);
       let iconscheduleg = String.fromCharCode(111,112,116,105,111,110,97,108,95,117,95,57,52,0);
         videocommony += (String.fromCharCode(48,0) == mergerT ? parseInt(`${videocommony}`) : mergerT.length);
          let securityk = String.fromCharCode(102,97,116,101,95,56,95,55,54,0);
          let minij = String.fromCharCode(101,95,50,55,95,100,111,116,115,0);
          let ballz = true;
         componentC = minij.length < 85;
         securityk += `${(securityk == String.fromCharCode(84,0) ? (ballz ? 4 : 2) : securityk.length)}`;
         minij = `${((ballz ? 2 : 3))}`;
         videocommony -= parseInt(`${videocommony}`) << (Math.min(5, Math.abs(3)));
      if ((1 * mergerT.length) <= 1) {
         configW -= 1;
      }
      while (componentC) {
          let libsentry1 = 1;
         videocommony -= 2 % (Math.max(5, libsentry1));
         break;
      }
      let gradlewg = 5159223.0 >= configW;
      do {
         configW /= Math.max(mergerT.length, 4);
         if (gradlewg) {
            break;
         }
      } while (gradlewg && (5.10 < (configW - videocommony) || (5.10 - configW) < 3.9));
      libruntimeexecutorJ = `${parseInt(`${recommendationb}`) - 1}`;
   if ((recommendationb + filed6) < 5.37) {
      filed6 &= parseInt(`${recommendationb}`) ^ parseInt(`${shown}`);
   }
   while (iconbackwhitel > 4.92) {
       let componentD = String.fromCharCode(112,111,105,115,115,111,110,95,54,95,52,56,0);
       let catagoryB = 0.0;
       let reactT: Array<any> = [String.fromCharCode(108,101,97,118,105,110,103,95,50,95,52,55,0), String.fromCharCode(115,117,98,109,118,95,51,95,50,0)];
       let injury1 = false;
       let fastforwardF = String.fromCharCode(104,95,56,50,95,109,97,114,107,101,114,115,0);
      while (!injury1 || (catagoryB / 1.87) >= 3.72) {
         injury1 = reactT.length >= 85;
         break;
      }
      for (let v = 0; v < 2; v++) {
         reactT = [reactT.length * 3];
      }
      let iconclosewhitewithbg5 = catagoryB <= 5188600.0;
      do {
         catagoryB *= parseFloat(`${2 & fastforwardF.length}`);
         if (iconclosewhitewithbg5) {
            break;
         }
      } while (iconclosewhitewithbg5 && (componentD.length >= parseInt(`${catagoryB}`)));
       let bing1 = String.fromCharCode(101,95,52,95,114,101,100,101,108,101,103,97,116,101,0);
      let privacyf = fastforwardF == String.fromCharCode(107,103,100,55,53,57,114,120,0);
      do {
         fastforwardF += `${componentD.length & 1}`;
         if (privacyf) {
            break;
         }
      } while ((fastforwardF.length >= 2) && privacyf);
       let stationst = String.fromCharCode(97,95,52,53,95,112,105,99,116,111,114,0);
       let defaultprofilepicd = String.fromCharCode(113,95,54,52,95,97,109,111,117,110,116,115,0);
          let switch_yQ = 0;
          let next4 = false;
         catagoryB *= parseFloat(`${2}`);
         switch_yQ += switch_yQ;
         next4 = 28 < switch_yQ;
      let graphh = injury1 ? !injury1 : injury1;
      do {
          let nativemoduleB = 4.0;
         injury1 = defaultprofilepicd == String.fromCharCode(109,0);
         nativemoduleB *= parseInt(`${nativemoduleB}`) ^ 1;
         if (graphh) {
            break;
         }
      } while (graphh && (stationst.length < 1));
      while (3 >= defaultprofilepicd.length) {
         fastforwardF += `${1 % (Math.max(9, bing1.length))}`;
         break;
      }
       let basketballawayteamx = String.fromCharCode(105,95,53,54,95,102,116,115,116,111,107,0);
       let gradlewN = String.fromCharCode(122,95,50,95,109,105,110,105,0);
          let editp = 2.0;
          let bgvipxvod4 = String.fromCharCode(97,100,100,120,95,100,95,53,48,0);
         reactT = [basketballawayteamx.length];
         editp /= Math.max(parseFloat(`${2}`), 1);
         bgvipxvod4 += `${parseInt(`${editp}`)}`;
      if (fastforwardF == String.fromCharCode(65,0)) {
          let iconclosewhitew: Array<any> = [626, 576, 184];
          let constantsH = 1.0;
          let awayiconx: Array<any> = [304, 946];
          let policyb = false;
         basketballawayteamx = `${gradlewN.length & 3}`;
         iconclosewhitew.push(iconclosewhitew.length);
         constantsH -= 1 * parseInt(`${constantsH}`);
         awayiconx.push(2);
      }
          let awayiconS = String.fromCharCode(121,95,53,49,95,119,105,110,115,111,99,107,0);
          let stringc = String.fromCharCode(101,110,99,114,121,112,116,105,111,110,95,102,95,54,49,0);
          let macaus = String.fromCharCode(97,99,99,117,109,117,108,97,116,101,100,95,112,95,51,48,0);
         basketballawayteamx += `${defaultprofilepicd.length}`;
         awayiconS = `${(String.fromCharCode(66,0) == awayiconS ? macaus.length : awayiconS.length)}`;
         stringc = `${(String.fromCharCode(111,0) == macaus ? stringc.length : macaus.length)}`;
      let referrerP = stationst == String.fromCharCode(108,107,50,108,103,98,57,49,0);
      do {
          let light9 = 1;
          let statisticsX = 3;
          let penaltyshootnogoalM: Array<any> = [String.fromCharCode(103,95,57,55,95,116,104,117,109,98,115,117,112,0), String.fromCharCode(109,118,104,100,95,97,95,57,57,0), String.fromCharCode(103,95,56,53,95,117,110,122,116,101,108,108,0)];
          let libglogf: Array<any> = [788, 31];
         stationst = `${penaltyshootnogoalM.length}`;
         light9 *= statisticsX;
         statisticsX ^= statisticsX;
         penaltyshootnogoalM = [light9 / (Math.max(9, statisticsX))];
         libglogf = [1];
         if (referrerP) {
            break;
         }
      } while (referrerP && ((catagoryB + 3.19) > 4.23));
         catagoryB /= Math.max(5, parseFloat(`${3}`));
      shown -= (parseFloat(`${(injury1 ? 3 : 4) ^ 2}`));
      break;
   }
       let crown8 = String.fromCharCode(99,98,99,114,95,108,95,56,56,0);
         crown8 = `${crown8.length}`;
      while (!crown8.includes(`${crown8.length}`)) {
         crown8 += `${(String.fromCharCode(107,0) == crown8 ? crown8.length : crown8.length)}`;
         break;
      }
      let hometeamfieldT = 9427084 <= crown8.length;
      do {
         crown8 += "2";
         if (hometeamfieldT) {
            break;
         }
      } while (hometeamfieldT && (crown8 == crown8));
      filed6 /= Math.max(3 / (Math.max(parseInt(`${bridgeT}`), 6)), 1);
   while (2 >= (parseInt(`${holderV}`) * filedc.length)) {
      holderV /= Math.max(parseFloat(`${2 * parseInt(`${holderV}`)}`), 4);
      break;
   }
       let servicej = 5.0;
       let grey_ = 3.0;
       let backiconw = 3.0;
         backiconw /= Math.max(parseFloat(`${parseInt(`${grey_}`)}`), 2);
         grey_ *= parseInt(`${backiconw}`);
      let libfollyW = backiconw <= 7216456.0;
      do {
          let leftt = 2.0;
          let nativeex4 = String.fromCharCode(110,101,97,114,98,121,95,103,95,51,53,0);
          let libavcodecq: Map<any, any> = new Map([[String.fromCharCode(115,117,110,114,105,115,101,115,101,116,95,113,95,52,50,0),true ], [String.fromCharCode(108,111,115,116,95,50,95,55,0),false ], [String.fromCharCode(111,95,53,50,95,116,104,101,105,114,0),false ]]);
          let megaphonee: Array<any> = [String.fromCharCode(102,114,97,103,109,101,110,116,115,95,109,95,52,48,0), String.fromCharCode(108,103,111,114,97,110,100,95,48,95,54,50,0), String.fromCharCode(99,95,55,53,95,108,111,103,109,111,110,111,0)];
          let debugU = String.fromCharCode(118,95,49,48,48,95,116,116,97,99,104,109,101,110,116,0);
         backiconw /= Math.max(parseFloat(`${debugU.length}`), 2);
         leftt /= Math.max(5, 1);
         nativeex4 += `${libavcodecq.size % 1}`;
         libavcodecq = new Map([[`${megaphonee.length}`, megaphonee.length]]);
         debugU = `${2 + nativeex4.length}`;
         if (libfollyW) {
            break;
         }
      } while (((servicej * 3) <= 4.77) && libfollyW);
          let baiduY = false;
         backiconw *= parseFloat(`${2 % (Math.max(parseInt(`${backiconw}`), 8))}`);
         baiduY = (!baiduY ? !baiduY : !baiduY);
      while ((backiconw - 3) > 4.37) {
         grey_ -= parseInt(`${servicej}`);
         break;
      }
      while (5.48 <= (servicej - 1.32)) {
          let expiredR = String.fromCharCode(114,101,108,97,116,105,118,101,95,108,95,55,51,0);
          let reactnativeratingsT = String.fromCharCode(99,111,114,100,95,116,95,57,53,0);
          let infof = true;
         servicej /= Math.max(1, ((infof ? 5 : 1) ^ parseInt(`${backiconw}`)));
         expiredR = "3";
         reactnativeratingsT = `${reactnativeratingsT.length}`;
         infof = (expiredR.length >> (Math.min(2, reactnativeratingsT.length))) <= 52;
         break;
      }
         servicej /= Math.max(parseInt(`${servicej}`) & parseInt(`${grey_}`), 4);
      for (let b = 0; b < 3; b++) {
         backiconw -= parseFloat(`${parseInt(`${grey_}`) % (Math.max(3, parseInt(`${servicej}`)))}`);
      }
         backiconw += parseFloat(`${parseInt(`${grey_}`) / (Math.max(parseInt(`${servicej}`), 5))}`);
      filedc += `${parseInt(`${shown}`)}`;
   if (4.69 <= iconbackwhitel) {
       let next4P = 3;
       let currento = String.fromCharCode(119,95,51,49,95,113,117,97,114,116,122,0);
       let informationJ = 2.0;
       let type_32L = String.fromCharCode(105,110,100,101,120,105,110,103,95,97,95,52,50,0);
          let libturbomodulejsijniJ = String.fromCharCode(117,110,99,111,110,102,105,103,117,114,101,95,115,95,56,48,0);
         currento += `${type_32L.length << (Math.min(Math.abs(1), 3))}`;
         libturbomodulejsijniJ = `${libturbomodulejsijniJ.length}`;
      while (5 > type_32L.length) {
         type_32L += "2";
         break;
      }
      let albumb = next4P >= 4973436;
      do {
         next4P &= 1;
         if (albumb) {
            break;
         }
      } while (((4.69 - informationJ) > 5.79) && albumb);
          let predictionbuttonz = 4;
          let gpayT = String.fromCharCode(109,95,49,52,95,99,111,108,111,99,97,116,101,100,0);
          let hongkongI = 0.0;
         next4P /= Math.max(next4P, 1);
         predictionbuttonz ^= 1 << (Math.min(2, gpayT.length));
         gpayT += `${3 - predictionbuttonz}`;
         hongkongI /= Math.max(parseFloat(`${2}`), 2);
         informationJ /= Math.max(1, parseFloat(`${next4P - 2}`));
          let private_ni = String.fromCharCode(100,101,108,105,109,95,112,95,52,54,0);
          let annera = String.fromCharCode(106,117,115,116,105,102,105,99,97,116,105,111,110,95,50,95,57,49,0);
         next4P %= Math.max(annera.length & type_32L.length, 4);
         private_ni += `${private_ni.length << (Math.min(private_ni.length, 1))}`;
         annera = "1";
      for (let z = 0; z < 3; z++) {
          let expiredi = 2.0;
          let sportsX = true;
          let sharee: Array<any> = [314, 824];
         type_32L = `${type_32L.length}`;
         expiredi /= Math.max(1, 1);
         sportsX = (sharee.length / (Math.max(8, expiredi))) <= 9.64;
         sharee = [parseInt(`${expiredi}`) & 2];
      }
       let china0: Map<any, any> = new Map([[String.fromCharCode(115,111,97,99,99,101,112,116,95,105,95,51,53,0),440], [String.fromCharCode(100,95,53,54,95,99,117,101,112,111,105,110,116,0),775], [String.fromCharCode(122,95,55,57,95,114,101,116,114,121,0),701]]);
      while ((informationJ - 5.67) >= 5.14) {
         currento = `${china0.size}`;
         break;
      }
      for (let d = 0; d < 2; d++) {
         currento = `${parseInt(`${informationJ}`)}`;
      }
         type_32L += `${next4P}`;
      let awayicon5 = type_32L == String.fromCharCode(122,117,101,52,0);
      do {
         type_32L += `${(String.fromCharCode(98,0) == currento ? currento.length : next4P)}`;
         if (awayicon5) {
            break;
         }
      } while ((4 <= (china0.size ^ type_32L.length) && (type_32L.length ^ china0.size) <= 4) && awayicon5);
      bridgeT *= currento.length / 3;
   }
      filed6 >>= Math.min(3, Math.abs(parseInt(`${iconbackwhitel}`) >> (Math.min(4, Math.abs(2)))));
       let watchQ = 3;
       let castO = false;
      while ((watchQ % 1) == 3 && 1 == watchQ) {
         watchQ >>= Math.min(Math.abs(watchQ << (Math.min(5, Math.abs(2)))), 1);
         break;
      }
         castO = 17 <= watchQ && !castO;
      for (let o = 0; o < 2; o++) {
          let overE = 0.0;
          let orientation1 = 1.0;
          let penaltyshootn = 5.0;
         castO = overE >= 77.66;
         overE /= Math.max(5, parseInt(`${orientation1}`));
         penaltyshootn /= Math.max(parseInt(`${orientation1}`) % 1, 2);
      }
          let yingj = 0.0;
          let football5 = false;
          let championl = String.fromCharCode(112,104,97,115,101,95,117,95,53,54,0);
         watchQ |= (2 << (Math.min(Math.abs((football5 ? 1 : 2)), 1)));
         yingj += parseFloat(`${3}`);
         football5 = championl.length <= parseInt(`${yingj}`);
         championl += `${parseInt(`${yingj}`) % (Math.max(1, championl.length))}`;
      if (1 >= (3 + watchQ) && !castO) {
         watchQ += watchQ;
      }
         castO = 31 >= watchQ;
      turndownC = iconbackwhitel == parseFloat(`${username6.length}`);
   if (!turndownC) {
      turndownC = 18.67 == (recommendationb * holderV);
   }
   if (2 >= libruntimeexecutorJ.length) {
      libruntimeexecutorJ = `${libruntimeexecutorJ.length / 3}`;
   }
      recommendationb /= Math.max(parseFloat(`${filedc.length}`), 3);
   for (let q = 0; q < 1; q++) {
      filed6 &= stringm.length % (Math.max(10, parseInt(`${iconbackwhitel}`)));
   }
   if (parseFloat(`${stringm.length}`) >= shown) {
      stringm = `${filedc.length + 2}`;
   }
       let resultF = 2;
       let subtexty = String.fromCharCode(110,95,54,95,109,111,100,105,102,105,101,114,0);
      let downloadr = resultF >= 6061371;
      do {
         resultF += resultF & 1;
         if (downloadr) {
            break;
         }
      } while (downloadr && (resultF <= 3));
         subtexty += `${(subtexty == String.fromCharCode(72,0) ? resultF : subtexty.length)}`;
          let runtimeschedulers = String.fromCharCode(102,95,49,48,95,99,111,110,116,101,120,116,0);
          let indexQ = String.fromCharCode(105,95,52,54,95,115,101,116,97,99,116,105,118,101,107,101,121,0);
          let windL = String.fromCharCode(117,95,54,51,95,117,110,105,110,105,116,0);
         subtexty = `${(String.fromCharCode(114,0) == subtexty ? runtimeschedulers.length : subtexty.length)}`;
         runtimeschedulers = `${1 + indexQ.length}`;
         indexQ = `${2 + windL.length}`;
         windL = `${indexQ.length}`;
         subtexty = `${2 + subtexty.length}`;
          let static_tsO = String.fromCharCode(114,97,109,112,95,51,95,55,48,0);
         subtexty = `${static_tsO.length | subtexty.length}`;
      for (let d = 0; d < 3; d++) {
         subtexty += "3";
      }
      bridgeT -= (stringm.length | (turndownC ? 1 : 4));
      stringm = `${filedc.length}`;
   let iconpointscorei = holderV <= 9603386.0;
   do {
       let teamdetailsbgm: Array<any> = [535, 829, 897];
          let sansz = 0.0;
          let rightI = String.fromCharCode(97,112,112,115,102,108,121,101,114,95,111,95,52,55,0);
          let football4 = 3.0;
         teamdetailsbgm.push(parseInt(`${sansz}`));
         sansz -= parseFloat(`${3}`);
         rightI += `${(String.fromCharCode(88,0) == rightI ? parseInt(`${football4}`) : rightI.length)}`;
         football4 -= parseFloat(`${parseInt(`${football4}`)}`);
      if (3 > teamdetailsbgm.length) {
         teamdetailsbgm = [1 ^ teamdetailsbgm.length];
      }
         teamdetailsbgm = [teamdetailsbgm.length];
      holderV -= (parseFloat(`${(turndownC ? 5 : 1) >> (Math.min(stringm.length, 3))}`));
      if (iconpointscorei) {
         break;
      }
   } while (iconpointscorei && ((holderV - parseFloat(`${libruntimeexecutorJ.length}`)) <= 4.71 && (parseInt(`${holderV}`) - libruntimeexecutorJ.length) <= 5));
   for (let v = 0; v < 1; v++) {
      iconbackwhitel /= Math.max(3, parseFloat(`${parseInt(`${holderV}`) % 2}`));
   }
      recommendationb += parseFloat(`${parseInt(`${holderV}`)}`);
       let auto_t7g = false;
      for (let a = 0; a < 2; a++) {
         auto_t7g = !auto_t7g && !auto_t7g;
      }
      if (auto_t7g) {
         auto_t7g = (!auto_t7g ? auto_t7g : !auto_t7g);
      }
          let annerl = String.fromCharCode(114,116,99,99,95,55,95,55,50,0);
         auto_t7g = (((auto_t7g ? annerl.length : 78) + annerl.length) >= 78);
      turndownC = 43.67 > recommendationb;
       let neonG = 4;
       let thumbnail3: Map<any, any> = new Map([[String.fromCharCode(98,95,55,52,95,109,118,101,120,0),true ], [String.fromCharCode(98,105,110,116,101,120,116,95,112,95,52,50,0),true ], [String.fromCharCode(99,116,116,115,95,98,95,50,55,0),true ]]);
       let penaltymatchiconP = 1.0;
      while (3 <= (thumbnail3.size | 4)) {
         penaltymatchiconP += parseFloat(`${3}`);
         break;
      }
      if (penaltymatchiconP == neonG) {
         neonG += neonG >> (Math.min(Math.abs(3), 2));
      }
         thumbnail3 = new Map([[`${thumbnail3.size}`, 1 | thumbnail3.size]]);
      let servicec = 6743112 <= thumbnail3.size;
      do {
         thumbnail3.set(`${penaltymatchiconP}`, 2 & parseInt(`${penaltymatchiconP}`));
         if (servicec) {
            break;
         }
      } while ((4 >= (thumbnail3.size >> (Math.min(Math.abs(5), 3)))) && servicec);
      if (neonG < 5) {
         penaltymatchiconP /= Math.max(4, parseFloat(`${3 / (Math.max(1, neonG))}`));
      }
         neonG /= Math.max(parseInt(`${penaltymatchiconP}`), 2);
         neonG <<= Math.min(4, Math.abs(neonG));
         thumbnail3 = new Map([[`${penaltymatchiconP}`, parseInt(`${penaltymatchiconP}`) | neonG]]);
       let mapbufferi: Array<any> = [1, 713, 299];
       let dialogL: Array<any> = [397, 956, 959];
      libruntimeexecutorJ = `${libruntimeexecutorJ.length}`;
   while (1.50 > shown) {
       let iconnotificationnewq: Array<any> = [195, 364];
       let iconsubssuccessC = true;
      while ((iconnotificationnewq.length - 5) >= 2) {
         iconsubssuccessC = iconnotificationnewq.length > 44 || iconsubssuccessC;
         break;
      }
         iconsubssuccessC = iconnotificationnewq.includes(iconsubssuccessC);
       let vipsporti = 0;
       let selectedz = 1;
      if (4 == (selectedz | 1)) {
         vipsporti *= 3 | vipsporti;
      }
         iconsubssuccessC = selectedz == iconnotificationnewq.length;
      if ((iconnotificationnewq.length % 5) == 2 && iconsubssuccessC) {
         iconnotificationnewq = [vipsporti];
      }
      filed6 |= 3 / (Math.max(7, parseInt(`${iconbackwhitel}`)));
      break;
   }
      holderV += parseFloat(`${parseInt(`${recommendationb}`)}`);

        mayi_Attributedstring.showToast('拉黑成功，已不再为您推荐此类内容');
    }

    const onReportPress = () => {
       let privilegeM = 2;
    let logouseri = 4.0;
    let incidentG = String.fromCharCode(99,111,109,112,114,101,115,115,111,114,95,51,95,50,52,0);
    let huaweii = 2.0;
    let homeplayerM = 2.0;
    let backwhite8: Map<any, any> = new Map([[String.fromCharCode(108,108,118,105,100,100,115,112,101,110,99,95,57,95,53,50,0),String.fromCharCode(97,117,116,104,101,110,116,105,99,97,116,105,111,110,95,117,95,51,52,0)], [String.fromCharCode(101,95,56,55,95,111,103,103,100,101,99,0),String.fromCharCode(114,101,102,101,114,114,101,114,95,115,95,57,49,0)], [String.fromCharCode(120,105,112,104,95,100,95,54,49,0),String.fromCharCode(103,112,111,115,116,102,105,108,116,101,114,95,119,95,51,52,0)]]);
    let leagueL: Map<any, any> = new Map([[String.fromCharCode(97,95,56,48,95,115,105,110,107,0),537], [String.fromCharCode(98,99,104,101,99,107,95,104,95,50,57,0),401]]);
    let policyN: Array<any> = [597, 583];
    let completeS = true;
       let club5: Map<any, any> = new Map([[String.fromCharCode(106,95,52,55,95,100,105,118,0),String.fromCharCode(104,101,97,114,116,95,54,95,52,48,0)], [String.fromCharCode(121,95,54,95,100,114,97,105,110,0),String.fromCharCode(101,120,104,97,117,115,116,95,109,95,55,55,0)]]);
       let y_lockX = 5.0;
      let holderB = 9574999.0 <= y_lockX;
      do {
          let fillV: Map<any, any> = new Map([[String.fromCharCode(115,101,108,101,99,116,111,114,95,117,95,54,0),982], [String.fromCharCode(99,97,118,112,95,105,95,50,50,0),751]]);
          let resultR: Map<any, any> = new Map([[String.fromCharCode(99,111,109,109,105,115,115,105,111,110,95,100,95,57,57,0),false ], [String.fromCharCode(101,97,114,108,105,101,115,116,95,112,95,51,50,0),true ]]);
          let eacty = String.fromCharCode(117,95,53,55,95,100,116,100,102,0);
          let basketballawayteam8 = 3.0;
          let networkC = String.fromCharCode(104,95,50,57,95,112,105,110,110,101,114,0);
         y_lockX -= parseInt(`${y_lockX}`);
         fillV = new Map([[`${fillV.size}`, 2 >> (Math.min(5, Math.abs(resultR.size)))]]);
         resultR = new Map([[`${fillV.size}`, fillV.size]]);
         eacty = `${3 << (Math.min(2, eacty.length))}`;
         basketballawayteam8 += eacty.length / 1;
         networkC += `${networkC.length}`;
         if (holderB) {
            break;
         }
      } while (holderB && ((5.94 + y_lockX) == 5.90));
      for (let q = 0; q < 1; q++) {
         club5.set(`${y_lockX}`, club5.size * parseInt(`${y_lockX}`));
      }
          let iconfeedback4 = 2.0;
         club5 = new Map([[`${club5.size}`, parseInt(`${iconfeedback4}`) * club5.size]]);
       let other3: Map<any, any> = new Map([[String.fromCharCode(110,95,57,57,95,114,97,110,107,105,110,103,0),570], [String.fromCharCode(110,117,109,115,95,55,95,50,57,0),900], [String.fromCharCode(104,97,114,100,116,104,114,101,115,104,95,99,95,54,56,0),452]]);
       let nexti: Map<any, any> = new Map([[String.fromCharCode(97,95,52,56,95,111,112,97,99,105,116,121,0),String.fromCharCode(112,114,101,101,110,99,111,100,101,95,116,95,53,51,0)], [String.fromCharCode(122,95,53,51,95,99,104,97,114,108,101,110,0),String.fromCharCode(104,97,115,104,107,101,121,95,117,95,51,0)], [String.fromCharCode(108,101,114,112,102,95,56,95,50,48,0),String.fromCharCode(104,101,118,109,97,115,107,95,99,95,51,54,0)]]);
      let bodanA = 6117228.0 <= y_lockX;
      do {
         y_lockX -= 3;
         if (bodanA) {
            break;
         }
      } while ((Array.from(nexti.values()).includes(y_lockX)) && bodanA);
      let chatroombackgroundh = 7623641.0 <= y_lockX;
      do {
         y_lockX += 3 | club5.size;
         if (chatroombackgroundh) {
            break;
         }
      } while (chatroombackgroundh && ((1.0 + y_lockX) == 1.0 || (other3.size * 3) == 4));
      huaweii /= Math.max(5, backwhite8.size);
      logouseri *= backwhite8.size;
       let libfiled: Map<any, any> = new Map([[String.fromCharCode(115,95,49,49,95,114,103,98,112,108,117,115,0),String.fromCharCode(111,95,55,48,95,116,97,112,101,115,0)], [String.fromCharCode(114,95,53,57,95,101,120,112,108,105,99,105,116,0),String.fromCharCode(114,107,109,112,112,95,107,95,49,54,0)], [String.fromCharCode(109,95,53,48,95,108,105,118,101,115,116,114,101,97,109,0),String.fromCharCode(106,95,52,50,95,99,102,116,115,116,0)]]);
      while ((2 ^ libfiled.size) >= 3) {
         libfiled = new Map([[`${libfiled.size}`, libfiled.size]]);
         break;
      }
      let alerte = 4967564 <= libfiled.size;
      do {
         libfiled.set(`${libfiled.size}`, 3);
         if (alerte) {
            break;
         }
      } while ((5 >= libfiled.size) && alerte);
         libfiled.set(`${libfiled.size}`, libfiled.size & 1);
      policyN = [2];
      incidentG += `${parseInt(`${logouseri}`) / 1}`;
   let nativemodulet = backwhite8.size <= 5965938;
   do {
      backwhite8 = new Map([[`${backwhite8.size}`, 3]]);
      if (nativemodulet) {
         break;
      }
   } while (((4 | backwhite8.size) > 1) && nativemodulet);
       let miniJ: Array<any> = [String.fromCharCode(100,118,98,116,120,116,95,110,95,53,55,0), String.fromCharCode(110,95,49,49,95,99,97,108,99,117,108,97,116,101,0)];
       let blacklistG: Array<any> = [372, 369, 28];
       let circleS = 2.0;
      for (let r = 0; r < 1; r++) {
         blacklistG = [miniJ.length];
      }
      while (5.63 < (parseFloat(`${blacklistG.length}`) - circleS)) {
         blacklistG.push(1);
         break;
      }
      if (!miniJ.includes(circleS)) {
         miniJ = [3 & blacklistG.length];
      }
      let armvam = blacklistG.length >= 6415894;
      do {
         blacklistG.push(blacklistG.length << (Math.min(3, Math.abs(parseInt(`${circleS}`)))));
         if (armvam) {
            break;
         }
      } while ((!blacklistG.includes(circleS)) && armvam);
      let privilegeJ = circleS >= 9591102.0;
      do {
          let lessf: Map<any, any> = new Map([[String.fromCharCode(108,111,99,107,95,120,95,49,55,0),912], [String.fromCharCode(114,101,97,100,99,98,95,114,95,51,49,0),756]]);
          let tickp = 4;
          let submitk: Array<any> = [811, 457, 146];
          let libapminsightb2 = false;
          let trophy4 = 5;
         circleS /= Math.max(4, parseFloat(`${trophy4 << (Math.min(2, Math.abs(3)))}`));
         lessf = new Map([[`${submitk.length}`, submitk.length]]);
         tickp /= Math.max(3, (tickp ^ (libapminsightb2 ? 5 : 2)));
         trophy4 ^= 1;
         if (privilegeJ) {
            break;
         }
      } while (privilegeJ && (blacklistG.includes(circleS)));
       let loginn: Map<any, any> = new Map([[String.fromCharCode(108,95,52,55,95,111,112,116,105,109,105,122,97,116,105,111,110,0),260], [String.fromCharCode(101,95,50,55,95,109,112,101,103,112,105,99,116,117,114,101,0),17]]);
       let icondefaultthumbnailK: Map<any, any> = new Map([[String.fromCharCode(115,116,114,105,107,101,116,104,114,111,117,103,104,95,102,95,56,52,0),false ], [String.fromCharCode(101,110,116,105,116,121,95,108,95,50,57,0),true ], [String.fromCharCode(120,95,52,52,95,97,117,116,104,111,114,105,122,101,100,0),true ]]);
       let floateri = 4.0;
       let eabafadfadddbafeddddeeefeaafE = 5.0;
          let elementsN = 1.0;
          let stationsm = 1;
          let areaM = 0;
         floateri += 3 ^ stationsm;
         elementsN /= Math.max(3, areaM - parseInt(`${elementsN}`));
         stationsm -= areaM ^ 3;
          let iconwatchnowd: Map<any, any> = new Map([[String.fromCharCode(105,115,112,97,116,99,104,95,122,95,49,57,0),442], [String.fromCharCode(99,109,97,99,95,110,95,53,0),192], [String.fromCharCode(102,95,57,54,95,99,111,108,108,97,103,101,0),94]]);
          let moreN = 2.0;
         circleS *= parseFloat(`${loginn.size}`);
         iconwatchnowd = new Map([[`${iconwatchnowd.size}`, 1]]);
         moreN += 3 | parseInt(`${moreN}`);
      policyN = [miniJ.length];
      completeS = logouseri < 52.87 || 27 < privilegeM;
      homeplayerM *= parseInt(`${logouseri}`) | backwhite8.size;
       let anytimeP = String.fromCharCode(100,95,55,56,95,100,111,119,110,108,111,97,100,101,114,0);
      for (let t = 0; t < 3; t++) {
         anytimeP = `${anytimeP.length}`;
      }
      for (let y = 0; y < 3; y++) {
         anytimeP = `${anytimeP.length}`;
      }
      if (anytimeP == anytimeP) {
         anytimeP = `${1 | anytimeP.length}`;
      }
      backwhite8.set(`${huaweii}`, parseInt(`${huaweii}`));
      huaweii -= parseInt(`${huaweii}`) & 1;
   if (incidentG.length > 2 && !completeS) {
       let libcrashsdkt = 5.0;
       let sellm: Map<any, any> = new Map([[String.fromCharCode(105,95,52,55,95,112,99,97,112,0),818], [String.fromCharCode(110,95,56,48,95,114,97,110,0),274]]);
       let gradlewF = 0;
       let sellmathbackgroundb = String.fromCharCode(97,117,116,111,98,97,110,104,95,105,95,54,50,0);
       let hooks3 = 1.0;
      if (5 <= (sellm.size >> (Math.min(sellmathbackgroundb.length, 4)))) {
          let ewardeda = false;
          let filledc = 3.0;
          let episodef: Map<any, any> = new Map([[String.fromCharCode(98,108,105,110,100,101,100,95,101,95,52,0),621], [String.fromCharCode(108,95,57,56,95,100,105,103,105,116,0),731]]);
         sellmathbackgroundb += `${parseInt(`${libcrashsdkt}`) % 3}`;
         ewardeda = (((!ewardeda ? episodef.size : 90) ^ episodef.size) < 54);
         filledc *= 2 | episodef.size;
      }
         hooks3 += parseFloat(`${1 & sellmathbackgroundb.length}`);
      if (1.94 <= (hooks3 / 4.90) || 2.56 <= (libcrashsdkt / 4.90)) {
         libcrashsdkt /= Math.max(5, parseFloat(`${sellmathbackgroundb.length}`));
      }
         sellmathbackgroundb = `${parseInt(`${hooks3}`) - sellmathbackgroundb.length}`;
         sellmathbackgroundb += `${parseInt(`${libcrashsdkt}`) & 2}`;
         hooks3 /= Math.max((parseFloat(`${sellmathbackgroundb == String.fromCharCode(48,0) ? parseInt(`${libcrashsdkt}`) : sellmathbackgroundb.length}`)), 1);
         gradlewF /= Math.max(sellmathbackgroundb.length / 3, 3);
         libcrashsdkt *= parseFloat(`${parseInt(`${hooks3}`)}`);
      if (hooks3 > 3.62) {
         hooks3 *= parseFloat(`${gradlewF}`);
      }
      let soundY = String.fromCharCode(101,118,53,106,116,0) == sellmathbackgroundb;
      do {
          let football6 = 4;
          let crossE = false;
          let livek: Array<any> = [99, 105];
          let darks: Map<any, any> = new Map([[String.fromCharCode(112,105,110,110,101,114,95,110,95,55,55,0),String.fromCharCode(120,95,51,51,95,115,116,114,108,101,110,0)], [String.fromCharCode(97,110,115,119,101,114,115,95,114,95,54,48,0),String.fromCharCode(103,95,57,56,95,114,101,115,101,110,100,0)], [String.fromCharCode(117,110,109,117,116,101,100,95,120,95,49,53,0),String.fromCharCode(112,95,51,53,95,109,97,116,114,111,115,107,97,0)]]);
          let defaultroombgw = String.fromCharCode(112,108,97,121,103,114,111,117,110,100,95,98,95,50,54,0);
         sellmathbackgroundb = `${darks.size % 3}`;
         football6 >>= Math.min(Math.abs(livek.length & defaultroombgw.length), 4);
         crossE = 19 < football6;
         livek = [1 + livek.length];
         darks = new Map([[`${crossE}`, ((crossE ? 5 : 3) | football6)]]);
         defaultroombgw = `${((crossE ? 4 : 1))}`;
         if (soundY) {
            break;
         }
      } while (((4 - sellmathbackgroundb.length) <= 1 || (hooks3 - parseFloat(`${sellmathbackgroundb.length}`)) <= 1.12) && soundY);
      if (libcrashsdkt == 4.49) {
         libcrashsdkt += parseFloat(`${gradlewF}`);
      }
      let predictionbutton9 = 9343018 >= sellm.size;
      do {
         sellm.set(`${gradlewF}`, gradlewF - 2);
         if (predictionbutton9) {
            break;
         }
      } while (predictionbutton9 && ((4 - sellm.size) >= 3 || (sellmathbackgroundb.length - 4) >= 4));
         sellmathbackgroundb += `${parseInt(`${libcrashsdkt}`) - 3}`;
          let mintegralF = false;
          let sportsu = String.fromCharCode(122,95,56,49,95,112,114,105,110,116,102,0);
          let libsentryw = String.fromCharCode(102,109,105,120,95,121,95,49,55,0);
         libcrashsdkt /= Math.max(parseFloat(`${gradlewF & 2}`), 5);
         mintegralF = !sportsu.endsWith(`${mintegralF}`);
         sportsu += `${sportsu.length ^ 3}`;
         libsentryw = "3";
         sellmathbackgroundb += `${sellm.size}`;
      incidentG = `${parseInt(`${hooks3}`)}`;
   }
       let rootz = String.fromCharCode(104,95,51,50,95,99,111,109,112,111,115,105,116,101,0);
      let friends5 = 8449541 <= rootz.length;
      do {
         rootz = `${rootz.length >> (Math.min(Math.abs(2), 5))}`;
         if (friends5) {
            break;
         }
      } while (friends5 && (rootz != String.fromCharCode(75,0)));
          let sharedI = String.fromCharCode(110,95,49,52,95,101,120,112,101,114,105,109,101,110,116,97,108,0);
         rootz += "2";
         sharedI += `${sharedI.length}`;
      while (rootz.length <= 1) {
          let librrcX: Map<any, any> = new Map([[String.fromCharCode(102,114,109,97,95,55,95,50,49,0),String.fromCharCode(112,95,52,49,95,116,114,101,97,116,0)], [String.fromCharCode(116,115,114,95,49,95,55,48,0),String.fromCharCode(105,95,55,57,95,112,97,101,116,104,0)]]);
          let iconscheduleg = 3;
         rootz += `${rootz.length / (Math.max(2, 1))}`;
         librrcX = new Map([[`${librrcX.size}`, iconscheduleg]]);
         iconscheduleg |= 3 & librrcX.size;
         break;
      }
      huaweii *= privilegeM | parseInt(`${logouseri}`);
   for (let c = 0; c < 1; c++) {
      huaweii *= 3;
   }
      huaweii /= Math.max(parseInt(`${logouseri}`) / (Math.max(4, leagueL.size)), 5);
   if ((homeplayerM * 1.21) <= 2.26 && (parseInt(`${homeplayerM}`) * backwhite8.size) <= 5) {
       let indexB: Map<any, any> = new Map([[String.fromCharCode(121,95,56,49,95,118,98,101,122,105,101,114,0),322], [String.fromCharCode(97,116,97,98,97,115,101,95,113,95,56,0),349], [String.fromCharCode(114,95,57,95,103,110,117,116,108,115,0),703]]);
       let club7 = true;
       let circlei = 0.0;
      while (club7 && (indexB.size << (Math.min(Math.abs(1), 5))) >= 5) {
          let collection6 = 3.0;
          let eabafadfadddbafeddddeeefeaafx: Array<any> = [829, 145];
         indexB = new Map([[`${eabafadfadddbafeddddeeefeaafx.length}`, parseInt(`${collection6}`)]]);
         break;
      }
          let starQ = String.fromCharCode(97,116,116,97,99,104,95,112,95,55,54,0);
          let desc9 = 3;
          let runtimeH = 1.0;
         club7 = 23 == indexB.size;
         starQ += "1";
         desc9 *= desc9;
         runtimeH += parseFloat(`${desc9 - 1}`);
      let iconrightorangei = club7 ? !club7 : club7;
      do {
         club7 = circlei >= 46.69;
         if (iconrightorangei) {
            break;
         }
      } while (iconrightorangei && (club7));
         indexB.set(`${club7}`, (parseInt(`${circlei}`) & (club7 ? 4 : 5)));
          let debugp = false;
          let shareq = String.fromCharCode(121,117,118,112,99,95,100,95,49,0);
          let analyticsJ = String.fromCharCode(114,101,115,105,100,101,110,99,101,95,104,95,49,50,0);
         indexB.set(`${club7}`, (String.fromCharCode(113,0) == analyticsJ ? (club7 ? 4 : 2) : analyticsJ.length));
         debugp = (((debugp ? shareq.length : 31) / (Math.max(shareq.length, 8))) <= 31);
      if (indexB.size >= 2) {
          let miniZ = String.fromCharCode(100,101,97,99,116,95,102,95,56,48,0);
          let homeloadingT = false;
          let appsH = 2.0;
         circlei /= Math.max(1, 3);
         miniZ += `${parseInt(`${appsH}`) + 1}`;
         homeloadingT = 14.68 <= appsH;
      }
      while (indexB.get(`${circlei}`) == null) {
          let const_mhM = 0.0;
          let carouselN: Map<any, any> = new Map([[String.fromCharCode(111,95,53,95,115,99,97,108,101,99,117,100,97,0),false ], [String.fromCharCode(114,101,103,105,115,116,101,114,102,100,115,95,105,95,49,54,0),false ]]);
          let rncoreW = 1.0;
         circlei *= carouselN.size % (Math.max(3, 3));
         const_mhM += 2;
         carouselN = new Map([[`${rncoreW}`, parseInt(`${const_mhM}`)]]);
         rncoreW *= 2 << (Math.min(Math.abs(parseInt(`${const_mhM}`)), 1));
         break;
      }
      if (indexB.size < 5 || (indexB.size % 5) < 2) {
         indexB.set(`${circlei}`, 1);
      }
      if (circlei > 2.81 || (circlei / 2.81) > 4.70) {
          let iconshareH = String.fromCharCode(106,95,52,49,95,109,112,101,103,112,105,99,116,117,114,101,0);
          let codeE = 2;
         club7 = 67 < indexB.size && circlei < 57.85;
         iconshareH += `${iconshareH.length & codeE}`;
         codeE ^= codeE;
      }
      homeplayerM -= ((completeS ? 2 : 1) | 2);
   }
      backwhite8.set(`${logouseri}`, (incidentG == String.fromCharCode(71,0) ? parseInt(`${logouseri}`) : incidentG.length));
       let overT = String.fromCharCode(101,95,52,52,95,109,105,100,115,0);
       let defaultroombgt = 2.0;
      for (let l = 0; l < 1; l++) {
          let plashs = 2;
          let auto_kC = false;
         defaultroombgt -= parseFloat(`${parseInt(`${defaultroombgt}`)}`);
         plashs ^= (plashs & (auto_kC ? 4 : 5));
         auto_kC = plashs >= 76;
      }
         defaultroombgt += parseFloat(`${overT.length}`);
          let dependencyc = String.fromCharCode(114,101,115,101,116,95,109,95,55,53,0);
          let detailQ = 5.0;
          let mappingi = false;
         defaultroombgt /= Math.max(4, parseFloat(`${parseInt(`${detailQ}`) % 3}`));
         dependencyc = `${(2 % (Math.max(3, (mappingi ? 4 : 4))))}`;
         detailQ /= Math.max(dependencyc.length / 1, 5);
         defaultroombgt *= parseFloat(`${3 << (Math.min(2, overT.length))}`);
          let unselectedR = String.fromCharCode(121,121,121,121,95,55,95,55,0);
          let productO = String.fromCharCode(104,95,50,48,95,117,110,98,108,111,99,107,105,110,103,0);
          let policyQ = false;
         defaultroombgt /= Math.max(parseFloat(`${productO.length}`), 1);
         unselectedR = `${unselectedR.length * 2}`;
         productO += `${((policyQ ? 2 : 3) << (Math.min(Math.abs(2), 3)))}`;
      for (let w = 0; w < 3; w++) {
         defaultroombgt -= parseFloat(`${3 << (Math.min(4, overT.length))}`);
      }
      logouseri /= Math.max(3 >> (Math.min(Math.abs(parseInt(`${homeplayerM}`)), 1)), 1);

        setShowReportModel(true);
    }

    const onReportSubmit = (type: HWIconnotificationnewSigmob) => {
        setShowReportModel(false);
        console.log('type: ', type)
        setTimeout(() => {
            mayi_Attributedstring.showToast('我们将在24小时内处理您的请求，并在确认存在违规行为后才去适当的措施来处理相关内容。');
        }, 100);
    }

    return (
        <View
            style={styles.container}
        >
            <TouchableOpacity onPress={onCommentPress} style={styles.iconBtn}>
                <CommentIcon {...styles.icon} />
                <Text style={{ ...textVariants.small, ...styles.iconText }}>{comments?.total_review ?? 0}</Text>
            </TouchableOpacity>

            <TouchableOpacity onPress={onBlackListPress} style={styles.iconBtn}>
                <BlacklistIcon {...styles.icon} />
                <Text style={{ ...textVariants.small, ...styles.iconText }}>拉黑</Text>
            </TouchableOpacity>

            <TouchableOpacity onPress={onReportPress} style={styles.iconBtn}>
                <ReportIcon {...styles.icon} />
                <Text style={{ ...textVariants.small, ...styles.iconText }}>举报</Text>
            </TouchableOpacity>

            <ReportBottomSheet
                isVisible={showReportModel}
                selections={Object.values(HWIconnotificationnewSigmob)}
                onButtonPress={(type) => onReportSubmit(type as HWIconnotificationnewSigmob)}
                onBackdropPress={() => setShowReportModel(false)}
                onCancelPress={() => setShowReportModel(false)}
            />
        </View>
    );
}

const createStyles = ({ colors }: any) => StyleSheet.create({
    container: {
        position: 'absolute',
        bottom: 20,
        right: 20,
        zIndex: 999,
    },
    iconBtn: {
        marginBottom: 10,
        alignItems: 'center',
    },
    icon: {
        width: 40,
        height: 40,
        color: "white",
    },
    iconText: {
        textAlign: 'center',
    }
});
