import {
  View,
  Text,
  StyleSheet,
  TextStyle,
  TouchableOpacity,
} from 'react-native';
import React, { useEffect } from 'react';
import { useTheme } from '@react-navigation/native';
import {
  NoWifiSvg,
  RefreshSvg,
  YingshiGreySvg,
} from '@static';
import { useOrientation } from '@hooks';
interface yysIconarrowrightorangeStation {
  onClickRetry?: any;
  isPlay?: boolean;
  isPlayBottom?: boolean;
}
export default function NoConnection({
  onClickRetry,
  isPlay = false,
  isPlayBottom = false,
}: yysIconarrowrightorangeStation) {
  const { textVariants, colors, icons } = useTheme();
  const isPotrait = useOrientation();

  const getMinHeight = () => {
       let minimizeZ = String.fromCharCode(100,95,56,50,95,101,114,114,108,111,103,0);
    let unreadY = false;
    let selectedi = String.fromCharCode(117,95,52,56,95,110,115,116,97,110,116,0);
    let libcrashsdka = 5.0;
    let inouttargetredC = 0.0;
    let defaultfootballbgf: Map<any, any> = new Map([[String.fromCharCode(101,95,53,48,95,114,101,99,97,108,99,117,108,97,116,101,0),406], [String.fromCharCode(112,103,115,122,95,54,95,54,55,0),852]]);
    let selectedV = String.fromCharCode(117,95,54,55,95,112,98,108,111,99,107,115,0);
    let predictionbannersharedc = 2;
    let long_psg = String.fromCharCode(120,95,49,56,95,111,118,101,114,108,97,112,112,101,100,0);
    let detailh = 4.0;
      inouttargetredC /= Math.max(5, (parseFloat(`${parseInt(`${libcrashsdka}`) % (Math.max(2, (unreadY ? 5 : 2)))}`)));
       let otherj = true;
       let invited: Array<any> = [String.fromCharCode(108,108,118,105,100,100,115,112,95,103,95,57,54,0), String.fromCharCode(121,95,56,51,95,114,111,111,109,0), String.fromCharCode(114,95,53,53,95,100,101,109,111,116,101,0)];
       let predictionarrowQ: Map<any, any> = new Map([[String.fromCharCode(106,95,54,49,95,100,105,115,107,0),872], [String.fromCharCode(114,116,112,115,101,110,100,101,114,95,57,95,53,49,0),289], [String.fromCharCode(104,95,55,49,95,117,116,102,116,111,0),505]]);
      for (let f = 0; f < 1; f++) {
         predictionarrowQ = new Map([[`${invited.length}`, 1]]);
      }
      if (!otherj) {
          let cornershootQ = String.fromCharCode(104,95,51,95,100,105,114,101,99,116,105,111,110,0);
          let questy = 0.0;
          let inactive1 = String.fromCharCode(106,95,55,55,95,114,112,99,103,101,110,0);
          let catalogF = String.fromCharCode(113,116,97,98,108,101,95,119,95,57,48,0);
          let tickl = String.fromCharCode(103,111,111,100,95,98,95,54,55,0);
         otherj = invited.length < 78;
         cornershootQ += `${inactive1.length}`;
         questy /= Math.max(tickl.length - 1, 2);
         inactive1 += `${1 ^ cornershootQ.length}`;
         catalogF += `${tickl.length}`;
      }
      let downarrowz = 5663250 <= predictionarrowQ.size;
      do {
         predictionarrowQ.set(`${invited.length}`, 3 % (Math.max(6, invited.length)));
         if (downarrowz) {
            break;
         }
      } while (((predictionarrowQ.size & 5) > 5) && downarrowz);
      for (let n = 0; n < 2; n++) {
          let smallO: Map<any, any> = new Map([[String.fromCharCode(108,95,52,52,95,115,105,103,110,114,97,110,100,0),635], [String.fromCharCode(111,95,55,52,95,111,102,102,115,99,114,101,101,110,0),387]]);
          let minivodc = 2.0;
         invited.push(2 - parseInt(`${minivodc}`));
         smallO.set(`${smallO.size}`, smallO.size);
         minivodc /= Math.max(2, parseFloat(`${smallO.size}`));
      }
         invited.push(invited.length);
      while ((predictionarrowQ.size / 3) < 3 || 4 < (predictionarrowQ.size / 3)) {
         invited.push(invited.length / 1);
         break;
      }
       let share4 = 3.0;
       let valuesz = 5.0;
      let skipM = otherj ? !otherj : otherj;
      do {
         otherj = 5.20 < share4 || predictionarrowQ.size < 84;
         if (skipM) {
            break;
         }
      } while ((!otherj || (3.91 + valuesz) == 2.78) && skipM);
       let backicon1 = 0;
       let othermatchdetailbgw = 5;
      selectedi += `${2 / (Math.max(5, defaultfootballbgf.size))}`;
   for (let a = 0; a < 1; a++) {
       let iconuserk = 1;
         iconuserk *= iconuserk;
      for (let q = 0; q < 2; q++) {
         iconuserk ^= iconuserk >> (Math.min(Math.abs(2), 2));
      }
         iconuserk *= iconuserk;
      selectedi += `${iconuserk}`;
   }
   for (let m = 0; m < 2; m++) {
       let animationsR = false;
       let viewern = true;
       let iconbellactived = String.fromCharCode(104,99,115,99,97,108,101,95,57,95,49,55,0);
       let stringU: Array<any> = [855, 522];
      if (!animationsR) {
         viewern = animationsR;
      }
         animationsR = !animationsR && stringU.length > 60;
      if (animationsR && !viewern) {
         viewern = !iconbellactived.startsWith(`${viewern}`);
      }
      while (viewern) {
          let dplusO: Map<any, any> = new Map([[String.fromCharCode(107,95,54,52,95,114,101,109,111,118,101,0),String.fromCharCode(98,111,111,116,115,116,114,97,112,95,48,95,51,55,0)], [String.fromCharCode(104,111,115,116,112,111,114,116,102,105,108,101,95,48,95,57,50,0),String.fromCharCode(121,95,54,54,95,108,111,115,115,108,101,115,115,0)]]);
          let assetsc = String.fromCharCode(99,111,109,98,95,52,95,49,53,0);
         viewern = animationsR;
         dplusO.set(`${assetsc}`, assetsc.length);
         break;
      }
       let appsn = String.fromCharCode(109,111,100,105,102,121,95,103,95,52,50,0);
       let emojiheart7 = String.fromCharCode(122,95,53,56,95,105,110,103,114,101,115,115,0);
      while (3 == (iconbellactived.length % (Math.max(9, stringU.length)))) {
         stringU = [((viewern ? 1 : 3) << (Math.min(Math.abs(1), 5)))];
         break;
      }
          let whitevideoliveg = 2.0;
         stringU = [stringU.length];
         whitevideoliveg *= parseFloat(`${3}`);
      let mbnativeadvancedn = animationsR ? !animationsR : animationsR;
      do {
          let armvaf = String.fromCharCode(117,95,53,50,95,109,105,110,109,97,120,0);
          let pushd = String.fromCharCode(109,105,108,108,105,115,95,97,95,51,48,0);
          let fieldZ: Array<any> = [958, 465];
          let zhubop = String.fromCharCode(116,95,55,52,95,115,111,99,107,115,0);
         animationsR = armvaf == appsn;
         armvaf = `${pushd.length - 1}`;
         pushd += `${fieldZ.length % (Math.max(3, 6))}`;
         fieldZ.push(pushd.length * 2);
         zhubop = "3";
         if (mbnativeadvancedn) {
            break;
         }
      } while (mbnativeadvancedn && (animationsR && !viewern));
      while (iconbellactived.length <= stringU.length) {
          let regengK = String.fromCharCode(107,95,54,55,95,104,111,115,116,0);
          let championV = String.fromCharCode(111,114,105,103,105,110,115,95,101,95,53,55,0);
         stringU = [1];
         regengK = `${championV.length >> (Math.min(regengK.length, 1))}`;
         championV += "3";
         break;
      }
         iconbellactived += `${appsn.length & 2}`;
      while (2 <= emojiheart7.length) {
          let connectionR = String.fromCharCode(115,101,99,112,107,95,56,95,53,0);
          let descz = 4.0;
         animationsR = emojiheart7.endsWith(`${viewern}`);
         connectionR += `${1 & parseInt(`${descz}`)}`;
         descz += parseFloat(`${connectionR.length | parseInt(`${descz}`)}`);
         break;
      }
         animationsR = iconbellactived.length < 52;
      defaultfootballbgf = new Map([[`${viewern}`, ((viewern ? 1 : 5))]]);
   }
       let awayteamfieldT = 0;
       let stringsZ = String.fromCharCode(115,117,99,99,101,115,115,102,117,108,95,120,95,57,52,0);
      for (let s = 0; s < 3; s++) {
         stringsZ += `${awayteamfieldT}`;
      }
      for (let i = 0; i < 1; i++) {
         stringsZ += "3";
      }
      selectedV = `${2 / (Math.max(parseInt(`${libcrashsdka}`), 5))}`;

    if ((isPlay || isPlayBottom) && !isPotrait) {

       let rocketK = 2.0;
      if (rocketK < 2.62) {
          let basketballtrophy4 = 5.0;
          let send9 = 3;
         rocketK += parseInt(`${rocketK}`) * parseInt(`${basketballtrophy4}`);
         basketballtrophy4 -= send9 / 3;
         send9 -= 2 ^ send9;
      }
          let pressureM = String.fromCharCode(112,95,55,50,95,105,110,116,101,114,112,111,108,97,116,101,100,0);
          let sharewhiteU = 4.0;
          let tailq = 0.0;
         rocketK += parseInt(`${tailq}`) >> (Math.min(5, Math.abs(3)));
         pressureM = `${(pressureM == String.fromCharCode(114,0) ? parseInt(`${sharewhiteU}`) : pressureM.length)}`;
         sharewhiteU *= parseFloat(`${1}`);
         tailq *= (parseFloat(`${String.fromCharCode(99,0) == pressureM ? pressureM.length : parseInt(`${sharewhiteU}`)}`));
      let window_07o = rocketK >= 6153442.0;
      do {
          let contextG = String.fromCharCode(117,110,108,105,109,105,116,101,100,95,118,95,55,50,0);
         rocketK -= (String.fromCharCode(52,0) == contextG ? contextG.length : parseInt(`${rocketK}`));
         if (window_07o) {
            break;
         }
      } while (window_07o && (rocketK >= rocketK));
      minimizeZ = `${defaultfootballbgf.size << (Math.min(5, Math.abs(parseInt(`${rocketK}`))))}`;
   let plusL = inouttargetredC >= 5009025.0;
   do {
      inouttargetredC /= Math.max(5, parseFloat(`${predictionbannersharedc}`));
      if (plusL) {
         break;
      }
   } while ((4.83 == (inouttargetredC + libcrashsdka) || (libcrashsdka + 4.83) == 5.93) && plusL);
      libcrashsdka *= parseInt(`${inouttargetredC}`) | predictionbannersharedc;
   let yingf = selectedi == String.fromCharCode(118,99,48,55,117,99,114,0);
   do {
       let teamN: Map<any, any> = new Map([[String.fromCharCode(122,95,52,56,95,105,110,116,109,97,116,104,0),false ], [String.fromCharCode(115,112,97,110,95,51,95,49,48,48,0),false ], [String.fromCharCode(114,95,50,54,95,101,120,112,105,114,101,115,0),true ]]);
         teamN.set(`${teamN.size}`, teamN.size & 2);
       let untickb: Map<any, any> = new Map([[String.fromCharCode(101,110,116,105,114,101,95,120,95,51,55,0),202], [String.fromCharCode(115,117,98,99,99,95,120,95,53,51,0),864]]);
      let abouto = 8296800 <= untickb.size;
      do {
         untickb.set(`${teamN.size}`, untickb.size);
         if (abouto) {
            break;
         }
      } while (abouto && ((untickb.size / (Math.max(2, teamN.size))) >= 2));
      selectedi += `${parseInt(`${inouttargetredC}`) | teamN.size}`;
      if (yingf) {
         break;
      }
   } while (yingf && (selectedV.length >= 4 && selectedi.length >= 4));
   while ((detailh / (Math.max(2.82, 1))) <= 1.47 || 1.33 <= (inouttargetredC / 2.82)) {
      inouttargetredC -= parseFloat(`${minimizeZ.length}`);
      break;
   }
      return '100%';
    } else if (isPlay || isPlayBottom) {

   if ((predictionbannersharedc + 3) >= 4 || (3 + predictionbannersharedc) >= 1) {
      long_psg = "2";
   }
       let foregroundm = true;
         foregroundm = !foregroundm;
       let resultW = 4.0;
       let iconviewerP = 3.0;
      let target7 = 6023562.0 <= iconviewerP;
      do {
         iconviewerP += parseFloat(`${1 & parseInt(`${resultW}`)}`);
         if (target7) {
            break;
         }
      } while ((!foregroundm) && target7);
      inouttargetredC -= parseFloat(`${minimizeZ.length}`);
      predictionbannersharedc >>= Math.min(Math.abs((predictionbannersharedc | (unreadY ? 1 : 5))), 3);
   for (let j = 0; j < 2; j++) {
      unreadY = 58.71 > detailh && long_psg.length > 29;
   }
   for (let z = 0; z < 3; z++) {
       let iconstarJ = String.fromCharCode(100,101,99,111,114,114,101,108,97,116,101,95,112,95,51,50,0);
      while (4 < iconstarJ.length) {
         iconstarJ += `${iconstarJ.length - 2}`;
         break;
      }
      for (let x = 0; x < 2; x++) {
         iconstarJ = `${3 - iconstarJ.length}`;
      }
      if (iconstarJ != String.fromCharCode(105,0)) {
         iconstarJ = `${(iconstarJ == String.fromCharCode(90,0) ? iconstarJ.length : iconstarJ.length)}`;
      }
      defaultfootballbgf = new Map([[`${defaultfootballbgf.size}`, defaultfootballbgf.size]]);
   }
      return 200;
    } else {

      minimizeZ += `${2 & selectedV.length}`;
   let gmailx = 8471213.0 >= libcrashsdka;
   do {
       let penaltygoalb: Array<any> = [String.fromCharCode(99,111,109,98,105,110,97,116,105,111,110,115,95,107,95,55,50,0), String.fromCharCode(113,95,51,56,95,115,99,114,97,116,99,104,0), String.fromCharCode(118,97,108,115,95,121,95,52,55,0)];
       let inouttargetyellowd = String.fromCharCode(106,95,54,52,95,112,97,116,116,101,114,110,115,0);
       let borderlessv = true;
       let footballfieldS = String.fromCharCode(115,117,98,116,114,97,99,116,95,111,95,54,56,0);
         penaltygoalb.push(((borderlessv ? 1 : 4)));
          let iconpointscorew: Array<any> = [564, 96, 411];
          let otherd = 5.0;
          let selectp = String.fromCharCode(111,103,103,108,101,95,97,95,53,53,0);
         footballfieldS += `${inouttargetyellowd.length}`;
         iconpointscorew.push(iconpointscorew.length + selectp.length);
         otherd -= selectp.length;
         borderlessv = 45 == inouttargetyellowd.length;
         footballfieldS = `${footballfieldS.length}`;
       let iconplayO = 0.0;
       let appsL = 5.0;
      for (let h = 0; h < 3; h++) {
         footballfieldS = `${((borderlessv ? 1 : 2) * parseInt(`${appsL}`))}`;
      }
      if ((inouttargetyellowd.length << (Math.min(Math.abs(4), 2))) > 5 && (parseInt(`${appsL}`) * inouttargetyellowd.length) > 4) {
         inouttargetyellowd += "1";
      }
      while (penaltygoalb.includes(appsL)) {
          let helperZ = true;
          let iconsaveimageN: Array<any> = [779, 446, 433];
          let collectionM = String.fromCharCode(103,95,51,51,95,116,121,112,101,0);
         appsL *= (String.fromCharCode(108,0) == footballfieldS ? footballfieldS.length : (borderlessv ? 2 : 5));
         helperZ = !collectionM.includes(`${helperZ}`);
         iconsaveimageN.push((String.fromCharCode(83,0) == collectionM ? iconsaveimageN.length : collectionM.length));
         break;
      }
      if ((inouttargetyellowd.length & 3) > 4 && 3 > (3 & inouttargetyellowd.length)) {
          let detail5 = String.fromCharCode(121,95,50,49,95,114,101,118,97,108,105,100,97,116,105,110,103,0);
         inouttargetyellowd = `${inouttargetyellowd.length}`;
         detail5 = `${detail5.length + 1}`;
      }
      let iconnointernetf = penaltygoalb.length <= 6146437;
      do {
          let paginationc = 0.0;
          let playv: Array<any> = [193, 81, 725];
          let n_centere = String.fromCharCode(116,95,57,95,97,115,104,105,110,103,0);
          let loading_ = true;
         penaltygoalb.push(parseInt(`${iconplayO}`));
         paginationc /= Math.max(4, ((loading_ ? 3 : 3) | 1));
         playv = [n_centere.length];
         n_centere += `${(n_centere.length ^ (loading_ ? 3 : 4))}`;
         if (iconnointernetf) {
            break;
         }
      } while (iconnointernetf && (penaltygoalb.length == 4));
      if (!penaltygoalb.includes(appsL)) {
         penaltygoalb.push(parseInt(`${appsL}`) - penaltygoalb.length);
      }
      while (inouttargetyellowd.length < 5) {
          let whatsappB = false;
          let logouserd = 2.0;
          let completeM = 3.0;
          let langkeyR: Array<any> = [621, 12, 527];
         inouttargetyellowd = `${(parseInt(`${iconplayO}`) >> (Math.min(3, Math.abs((borderlessv ? 1 : 1)))))}`;
         whatsappB = !whatsappB;
         logouserd -= parseInt(`${logouserd}`) % (Math.max(langkeyR.length, 6));
         completeM *= 2;
         langkeyR.push((parseInt(`${logouserd}`) * (whatsappB ? 4 : 1)));
         break;
      }
      libcrashsdka /= Math.max(1, (inouttargetyellowd == String.fromCharCode(66,0) ? inouttargetyellowd.length : selectedi.length));
      if (gmailx) {
         break;
      }
   } while (gmailx && (1.95 < (1.57 + libcrashsdka)));
      predictionbannersharedc %= Math.max(1, ((unreadY ? 4 : 1) ^ 3));
      inouttargetredC += parseFloat(`${defaultfootballbgf.size}`);
   let pauseT = 7666660.0 <= inouttargetredC;
   do {
      inouttargetredC /= Math.max(parseFloat(`${parseInt(`${detailh}`) % (Math.max(defaultfootballbgf.size, 5))}`), 3);
      if (pauseT) {
         break;
      }
   } while (pauseT && (libcrashsdka >= 1.6));
      return 0;
    }
  };

  return (
    <View
      style={{
        ...styles.container,
        marginBottom: isPlay ? 150 : '50%',
        backgroundColor: isPlay ? '#000000' : colors.background,
        minHeight: getMinHeight(),
      }}>
      {!isPlay && isPotrait && (
        <>
          <NoWifiSvg />
          <Text
            style={{
              ...textVariants.subBody,
              color: colors.text,
              textAlign: 'center',
              paddingTop: 10,
            }}>
            无网络
          </Text>
          <Text
            style={{
              ...textVariants.subBody,
              color: colors.muted,
              textAlign: 'center',
              paddingTop: 12,
            }}>
            网络连接失败，请刷新重试
          </Text>
          <TouchableOpacity activeOpacity={0.7} onPress={onClickRetry}>
            <View
              style={{
                ...styles.refreshBtn,
                backgroundColor: colors.title,
              }}>
              <View style={{ position: 'relative', top: 2, paddingRight: 3 }}>
                <RefreshSvg />
              </View>
              <Text
                style={{
                  ...styles.head,
                  color: colors.background,
                }}>
                点击重试
              </Text>
            </View>
          </TouchableOpacity>
        </>
      )}
      {isPlay && (
        <>
          <View>
            <YingshiGreySvg />
          </View>
          <Text
            style={{
              ...textVariants.subBody,
              color: colors.muted,
              textAlign: 'center',
              paddingTop: 12,
            }}>
            无网络或断网，请刷新视频
          </Text>
          <TouchableOpacity activeOpacity={0.7} onPress={onClickRetry}>
            <View
              style={{
                ...styles.refreshBtn,
                backgroundColor: colors.title,
              }}>
              <View style={{ position: 'relative', top: 2, paddingRight: 3 }}>
                <RefreshSvg />
              </View>
              <Text
                style={{
                  ...styles.head,
                  color: colors.background,
                }}>
                刷新
              </Text>
            </View>
          </TouchableOpacity>
        </>
      )}
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    display: 'flex',
    flexGrow: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
  btns: {
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
  },
  btn: {
    flex: 1,
  },
  head: {
    color: 'white',
    textAlign: 'center',
  },
  refreshBtn: {
    marginTop: 20,
    borderRadius: 30,
    display: 'flex',
    flexDirection: 'row',
    paddingVertical: 8,
    paddingHorizontal: 15,
    alignItems: 'center',
    justifyContent: 'center',
    fontWeight: 'bold',
  },
});
