import {
  View,
  Text,
  StyleSheet,
  TextStyle,
  TouchableOpacity,
} from 'react-native';
import React, { useEffect } from 'react';
import { useTheme } from '@react-navigation/native';
import NoWifi from '@static/images/tumbnailYellowcirclebgIconnewsshare.svg';
import RefreshIcon from '@static/images/modelsIndex.svg';
import YingshiGreyIcon from '@static/images/nextPlayer.svg';
import { useOrientation } from '@hooks/mayi_libjsijniprofiler';
import { IS_OTHER_SKIN } from '@utility/mayi_middleware_apps';
interface mayi_GoogleViews {
  onClickRetry?: any;
  isPlay?: boolean;
  isPlayBottom?: boolean;
}
export default function NoConnection({
  onClickRetry,
  isPlay = false,
  isPlayBottom = false,
}: mayi_GoogleViews) {
  const { textVariants, colors, icons } = useTheme();
  const isPotrait = useOrientation();

  const getMinHeight = () => {
       let armvat = 3;
    let unimplementedviewh = String.fromCharCode(111,95,55,50,95,98,105,115,101,99,116,0);
    let defaultroombgh = String.fromCharCode(104,97,115,104,0);
    let predictionbuttony = true;
    let cancelD = String.fromCharCode(117,115,97,98,108,101,95,48,95,53,50,0);
    let subs9 = 4.0;
    let placeholdern: Map<any, any> = new Map([[String.fromCharCode(98,101,101,110,95,48,95,55,51,0),true ], [String.fromCharCode(120,95,50,50,95,112,108,97,121,0),false ]]);
    let resultQ: Array<any> = [String.fromCharCode(104,111,108,100,105,110,103,95,57,95,49,51,0), String.fromCharCode(107,95,57,52,95,110,117,109,98,105,116,115,0), String.fromCharCode(98,117,102,102,101,114,101,100,95,53,95,56,55,0)];
    let predictionY = String.fromCharCode(110,95,53,55,95,108,97,112,110,100,122,0);
    let arrowrightwithtailS = true;
    let detailr = 1.0;
    let moreI = 3.0;
    let yellowscoreballr = String.fromCharCode(107,95,49,55,95,102,97,100,101,100,0);
   while (unimplementedviewh != String.fromCharCode(82,0)) {
      cancelD = `${cancelD.length}`;
      break;
   }
      placeholdern = new Map([[cancelD, parseInt(`${subs9}`)]]);
   for (let r = 0; r < 3; r++) {
       let containera = false;
       let iconsetting7: Array<any> = [561, 801];
       let reminderb = String.fromCharCode(99,111,109,109,111,110,95,121,95,56,53,0);
       let frame__i = String.fromCharCode(103,122,105,112,112,101,100,95,115,95,55,0);
          let scorepopsound_ = String.fromCharCode(112,95,53,50,95,101,120,112,105,114,101,0);
          let awayteamfield5: Map<any, any> = new Map([[String.fromCharCode(97,116,114,97,99,112,95,109,95,50,49,0),360], [String.fromCharCode(120,95,57,57,95,115,116,100,0),547]]);
          let filedg: Array<any> = [562, 684, 193];
         frame__i += `${(scorepopsound_ == String.fromCharCode(115,0) ? frame__i.length : scorepopsound_.length)}`;
         awayteamfield5.set(`${filedg.length}`, awayteamfield5.size | 3);
         filedg = [awayteamfield5.size * 1];
      let manifestH = reminderb == String.fromCharCode(118,98,102,50,101,0);
      do {
         reminderb = `${iconsetting7.length}`;
         if (manifestH) {
            break;
         }
      } while ((frame__i == String.fromCharCode(90,0)) && manifestH);
          let defaultprofilepicl = String.fromCharCode(102,95,50,95,114,101,99,111,110,102,105,103,117,114,97,98,108,101,0);
         reminderb = `${((containera ? 5 : 2))}`;
         defaultprofilepicl = `${defaultprofilepicl.length / 3}`;
          let ball1 = String.fromCharCode(99,101,114,116,112,111,108,95,101,95,50,0);
          let whatsappV = 3.0;
          let libjsinspectorw: Array<any> = [771, 578, 899];
         frame__i += `${(String.fromCharCode(80,0) == reminderb ? parseInt(`${whatsappV}`) : reminderb.length)}`;
         ball1 += `${libjsinspectorw.length | 1}`;
         whatsappV *= parseFloat(`${1 << (Math.min(5, libjsinspectorw.length))}`);
      if (!containera && (iconsetting7.length - 5) < 1) {
          let backgroundo: Map<any, any> = new Map([[String.fromCharCode(108,105,98,97,118,102,111,114,109,97,116,95,122,95,52,0),160], [String.fromCharCode(114,101,99,114,101,97,116,101,95,109,95,50,48,0),406], [String.fromCharCode(101,95,53,95,114,103,98,97,108,101,0),989]]);
          let inactiveh = String.fromCharCode(99,111,100,101,99,114,97,119,95,103,95,56,57,0);
          let agreement3 = String.fromCharCode(107,95,55,52,95,115,104,97,114,101,100,100,111,119,110,108,111,97,100,0);
          let nodej = String.fromCharCode(99,111,109,112,111,115,105,116,101,95,100,95,49,57,0);
         containera = String.fromCharCode(98,0) == frame__i;
         backgroundo.set(nodej, inactiveh.length);
         inactiveh = `${nodej.length}`;
         agreement3 += "2";
      }
       let relatede = 3.0;
      for (let j = 0; j < 2; j++) {
         relatede -= (parseFloat(`${reminderb == String.fromCharCode(90,0) ? reminderb.length : iconsetting7.length}`));
      }
         containera = iconsetting7.length >= 36 || reminderb.length >= 36;
       let pathb = String.fromCharCode(100,95,57,48,95,112,108,111,116,0);
      while (!containera) {
          let searchbarJ = String.fromCharCode(97,114,109,116,104,95,103,95,52,48,0);
         iconsetting7.push((searchbarJ == String.fromCharCode(114,0) ? searchbarJ.length : parseInt(`${relatede}`)));
         break;
      }
         relatede += parseFloat(`${frame__i.length}`);
         pathb += `${((containera ? 3 : 2) | iconsetting7.length)}`;
      resultQ = [((containera ? 3 : 5))];
   }
   let collectionm = resultQ.length <= 7584043;
   do {
      resultQ.push(1);
      if (collectionm) {
         break;
      }
   } while ((!defaultroombgh.startsWith(`${resultQ.length}`)) && collectionm);
      resultQ = [predictionY.length % 1];

    if ((isPlay || isPlayBottom) && !isPotrait) {

   for (let o = 0; o < 2; o++) {
      defaultroombgh += `${cancelD.length}`;
   }
   while (defaultroombgh.startsWith(`${unimplementedviewh.length}`)) {
      unimplementedviewh += `${((predictionbuttony ? 4 : 3) | 3)}`;
      break;
   }
   for (let s = 0; s < 1; s++) {
      defaultroombgh = `${cancelD.length | resultQ.length}`;
   }
       let kuaishouo = String.fromCharCode(117,110,114,101,99,111,103,110,105,122,101,100,95,105,95,54,51,0);
       let targetp = 0.0;
       let typesI = true;
       let libapminsightaL = String.fromCharCode(111,95,50,49,95,99,105,114,99,117,108,97,114,0);
      if (typesI) {
         targetp /= Math.max(1, (parseFloat(`${(typesI ? 3 : 2) ^ 2}`)));
      }
      for (let f = 0; f < 1; f++) {
          let penaltyshootJ = String.fromCharCode(117,95,51,48,95,112,108,97,121,103,114,111,117,110,100,0);
          let pangle5 = String.fromCharCode(100,95,57,51,95,115,121,115,116,101,109,0);
          let awayiconX = String.fromCharCode(121,113,117,97,110,116,95,56,95,49,51,0);
          let libreanimatedh: Array<any> = [903, 268, 123];
          let cedbadcebfbfbfbcfecbcg = String.fromCharCode(114,101,109,105,120,105,110,103,95,56,95,48,0);
         typesI = pangle5 == String.fromCharCode(109,0);
         penaltyshootJ = `${libreanimatedh.length}`;
         pangle5 = `${penaltyshootJ.length}`;
         awayiconX += `${penaltyshootJ.length % 1}`;
         libreanimatedh = [(String.fromCharCode(69,0) == awayiconX ? penaltyshootJ.length : awayiconX.length)];
         cedbadcebfbfbfbcfecbcg += `${penaltyshootJ.length + 3}`;
      }
      if (libapminsightaL.includes(`${targetp}`)) {
         targetp -= (parseFloat(`${(typesI ? 4 : 1) - parseInt(`${targetp}`)}`));
      }
      for (let c = 0; c < 1; c++) {
         targetp -= parseFloat(`${1}`);
      }
      for (let t = 0; t < 3; t++) {
         kuaishouo = `${((typesI ? 1 : 2) ^ parseInt(`${targetp}`))}`;
      }
         libapminsightaL += `${kuaishouo.length}`;
         typesI = 92.19 == targetp;
      for (let g = 0; g < 1; g++) {
         targetp += (parseFloat(`${String.fromCharCode(66,0) == kuaishouo ? parseInt(`${targetp}`) : kuaishouo.length}`));
      }
      cancelD += `${placeholdern.size}`;
   if ((subs9 - 5.84) < 2.54) {
       let libruntimeexecutork = false;
       let catalog2: Map<any, any> = new Map([[String.fromCharCode(115,95,52,54,95,100,105,115,112,111,115,101,0),String.fromCharCode(114,101,100,117,110,100,97,110,116,95,52,95,55,49,0)], [String.fromCharCode(99,95,52,51,95,112,114,111,112,111,115,101,114,0),String.fromCharCode(117,110,99,104,101,99,107,101,100,95,113,95,51,0)], [String.fromCharCode(105,95,56,48,95,117,105,111,109,111,118,101,0),String.fromCharCode(121,95,49,48,95,115,98,114,101,115,101,114,118,101,0)]]);
       let bellC = String.fromCharCode(108,105,110,107,115,95,101,95,50,54,0);
      if ((catalog2.size * 5) < 2) {
          let schedule0 = String.fromCharCode(100,101,99,111,100,101,95,104,95,51,53,0);
          let airbnbstarX = 0.0;
          let mbbannerg = String.fromCharCode(103,95,50,53,95,103,101,110,101,114,97,108,0);
          let twitterh = String.fromCharCode(100,105,97,103,114,97,109,95,51,95,54,51,0);
         bellC = `${schedule0.length}`;
         schedule0 += "2";
         airbnbstarX += parseFloat(`${3}`);
         mbbannerg = "1";
         twitterh = `${twitterh.length | 2}`;
      }
      let hometeamfieldy = 4926798 <= catalog2.size;
      do {
         catalog2.set(`${libruntimeexecutork}`, 1);
         if (hometeamfieldy) {
            break;
         }
      } while ((libruntimeexecutork) && hometeamfieldy);
          let basketballawayteamu: Map<any, any> = new Map([[String.fromCharCode(98,95,56,52,95,99,111,108,111,114,101,100,0),false ], [String.fromCharCode(104,95,51,55,95,109,97,105,110,115,116,97,103,101,0),false ], [String.fromCharCode(101,108,101,118,97,116,101,95,112,95,54,48,0),false ]]);
          let textlayoutmanagerc = 3.0;
          let libcrashsdkt = 5.0;
         catalog2.set(`${libruntimeexecutork}`, 3 / (Math.max(8, parseInt(`${libcrashsdkt}`))));
         basketballawayteamu = new Map([[`${basketballawayteamu.size}`, 3 * basketballawayteamu.size]]);
         textlayoutmanagerc += parseFloat(`${parseInt(`${textlayoutmanagerc}`)}`);
         libcrashsdkt *= 1;
      let iconrightorangej = String.fromCharCode(113,95,54,0) == bellC;
      do {
         bellC = "1";
         if (iconrightorangej) {
            break;
         }
      } while (iconrightorangej && (bellC.endsWith(`${catalog2.size}`)));
      if (libruntimeexecutork && 2 > bellC.length) {
         libruntimeexecutork = libruntimeexecutork && catalog2.size < 70;
      }
      for (let j = 0; j < 1; j++) {
         libruntimeexecutork = bellC.length == 20;
      }
      let valuesd = 8592205 <= catalog2.size;
      do {
         catalog2 = new Map([[`${catalog2.size}`, catalog2.size]]);
         if (valuesd) {
            break;
         }
      } while (valuesd && (2 > (catalog2.size >> (Math.min(Math.abs(5), 5)))));
      while (5 > catalog2.size) {
         catalog2 = new Map([[`${catalog2.size}`, 3 >> (Math.min(5, Math.abs(catalog2.size)))]]);
         break;
      }
         catalog2 = new Map([[`${catalog2.size}`, 1]]);
      subs9 -= parseFloat(`${armvat - parseInt(`${subs9}`)}`);
   }
      return '100%';
    } else if (isPlay || isPlayBottom) {

   for (let l = 0; l < 1; l++) {
      subs9 -= parseFloat(`${unimplementedviewh.length / 3}`);
   }
      placeholdern.set(cancelD, 2);
   if (4 <= armvat || (4 * armvat) <= 2) {
      armvat += 2;
   }
       let borderless4 = String.fromCharCode(112,97,116,104,115,95,106,95,55,50,0);
       let promotion9 = 2.0;
       let step1 = 1.0;
      while ((step1 * 1.2) == 4.84 && 5 == (borderless4.length ^ 2)) {
         step1 /= Math.max(1 % (Math.max(8, borderless4.length)), 3);
         break;
      }
         borderless4 += `${(borderless4 == String.fromCharCode(117,0) ? parseInt(`${step1}`) : borderless4.length)}`;
      let referrert = step1 >= 8990167.0;
      do {
         step1 /= Math.max(5, 3);
         if (referrert) {
            break;
         }
      } while (((step1 - promotion9) <= 1.51) && referrert);
      let turnF = promotion9 <= 6329060.0;
      do {
          let cornerkickP = String.fromCharCode(107,95,52,56,95,117,110,105,113,117,101,100,0);
          let basketballawayteamR = false;
          let profileK: Array<any> = [617, 302];
         promotion9 -= ((basketballawayteamR ? 3 : 3));
         cornerkickP += "3";
         basketballawayteamR = cornerkickP.length <= profileK.length;
         profileK.push(cornerkickP.length | 2);
         if (turnF) {
            break;
         }
      } while (turnF && (borderless4.length >= 1));
         borderless4 = `${parseInt(`${step1}`)}`;
       let expandD = String.fromCharCode(122,95,55,55,95,102,101,108,101,109,0);
       let notificationfillemptyW = String.fromCharCode(111,110,116,97,99,116,95,98,95,49,50,0);
         expandD += `${2 & parseInt(`${promotion9}`)}`;
      let scorepopsoundx = String.fromCharCode(51,107,118,0) == borderless4;
      do {
         borderless4 = `${parseInt(`${promotion9}`) & 1}`;
         if (scorepopsoundx) {
            break;
         }
      } while (scorepopsoundx && (!borderless4.includes(expandD)));
       let liveshare2 = 5.0;
       let privacyT = 3.0;
      unimplementedviewh += `${parseInt(`${step1}`)}`;
      cancelD += `${defaultroombgh.length << (Math.min(3, Math.abs(armvat)))}`;
      return 200;
    } else {

      placeholdern.set(`${subs9}`, cancelD.length ^ parseInt(`${subs9}`));
      arrowrightwithtailS = placeholdern.size < 68 || 43.32 < subs9;
   while (cancelD.endsWith(`${subs9}`)) {
       let settingg = false;
       let mutedz: Map<any, any> = new Map([[String.fromCharCode(107,95,55,54,95,101,120,101,99,117,116,111,114,0),false ], [String.fromCharCode(115,101,116,116,105,110,103,115,95,112,95,57,0),true ], [String.fromCharCode(97,95,51,50,95,110,111,116,105,102,105,101,114,0),false ]]);
       let bgvipsportO = false;
       let arrowQ = false;
         arrowQ = (settingg ? arrowQ : settingg);
      cancelD += "3";
      break;
   }
   if (4 > (2 + predictionY.length) || (predictionY.length + 2) > 5) {
      predictionY = "2";
   }
      cancelD = `${(cancelD == String.fromCharCode(79,0) ? cancelD.length : placeholdern.size)}`;
      return 0;
    }
  };

  return (
    <View
      style={{
        ...styles.container,
        marginBottom: isPlay ? 150 : '50%',
        backgroundColor: isPlay ? '#000000' : colors.background,
        minHeight: getMinHeight(),
      }}>
      {!isPlay && isPotrait && (
        <>
          <NoWifi />
          <Text
            style={{
              ...textVariants.subBody,
              color: colors.text,
              textAlign: 'center',
              paddingTop: 10,
            }}>
            无网络
          </Text>
          <Text
            style={{
              ...textVariants.subBody,
              color: colors.muted,
              textAlign: 'center',
              paddingTop: 12,
            }}>
            网络连接失败，请刷新重试
          </Text>
          <TouchableOpacity activeOpacity={0.85} onPress={onClickRetry}>
            <View
              style={{
                ...styles.refreshBtn,
                backgroundColor: IS_OTHER_SKIN ? 'white' : colors.title,
              }}>
              <View style={{ position: 'relative', top: 2, paddingRight: 3 }}>
                <RefreshIcon />
              </View>
              <Text
                style={{
                  ...styles.head,
                  color: IS_OTHER_SKIN ? colors.text : colors.background,
                }}>
                点击重试
              </Text>
            </View>
          </TouchableOpacity>
        </>
      )}
      {isPlay && (
        <>
          {/* <View>
            <YingshiGreyIcon />
          </View> */}
          <Text
            style={{
              ...textVariants.subBody,
              color: colors.muted,
              textAlign: 'center',
              paddingTop: 12,
            }}>
            无网络或断网，请刷新视频
          </Text>
          <TouchableOpacity activeOpacity={0.85} onPress={onClickRetry}>
            <View
              style={{
                ...styles.refreshBtn,
                backgroundColor: IS_OTHER_SKIN ? 'white' : colors.title,
              }}>
              <View style={{ position: 'relative', top: 2, paddingRight: 3 }}>
                <RefreshIcon />
              </View>
              <Text
                style={{
                  ...styles.head,
                  color: IS_OTHER_SKIN ? colors.text : colors.background,
                }}>
                刷新
              </Text>
            </View>
          </TouchableOpacity>
        </>
      )}
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    display: 'flex',
    flexGrow: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
  btns: {
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
  },
  btn: {
    flex: 1,
  },
  head: {
    color: 'white',
    textAlign: 'center',
  },
  refreshBtn: {
    marginTop: 20,
    borderRadius: 30,
    display: 'flex',
    flexDirection: 'row',
    paddingVertical: 8,
    paddingHorizontal: 15,
    alignItems: 'center',
    justifyContent: 'center',
    fontWeight: 'bold',
  },
});
