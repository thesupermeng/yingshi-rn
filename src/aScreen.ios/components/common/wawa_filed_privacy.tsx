import {
  View,
  Text,
  StyleSheet,
  TextStyle,
  TouchableOpacity,
} from 'react-native';
import React, { useEffect } from 'react';
import { useTheme } from '@react-navigation/native';
import NoWifi from '@static/images/positionPolicy.svg';
import RefreshIcon from '@static/images/lessStationPrivacy.svg';
import YingshiGreyIcon from '@static/images/settingLibavdeviceReactnavigation.svg';
import { useOrientation } from '@hooks/wawa_sigmob';
import { IS_OTHER_SKIN } from '@utility/wawa_iconpointscore';
interface wawaAwayShow {
  onClickRetry?: any;
  isPlay?: boolean;
  isPlayBottom?: boolean;
}
export default function NoConnection({
  onClickRetry,
  isPlay = false,
  isPlayBottom = false,
}: wawaAwayShow) {
  const { textVariants, colors, icons } = useTheme();
  const isPotrait = useOrientation();

  const getMinHeight = () => {
       let arrowupw = 3;
    let nalyticsS = 0;
    let iconarrowleft7 = String.fromCharCode(109,111,110,111,115,112,97,99,101,100,0);
    let whitevideolivet: Map<any, any> = new Map([[String.fromCharCode(108,117,116,115,0),750], [String.fromCharCode(100,101,99,101,108,101,114,97,116,101,100,0),526], [String.fromCharCode(104,97,115,104,101,115,0),826]]);
    let bottomM = 5.0;
    let reddownarrowN: Map<any, any> = new Map([[String.fromCharCode(111,109,101,103,97,0),543], [String.fromCharCode(101,103,117,108,97,114,0),536]]);
    let langi: Array<any> = [404, 890, 364];
    let floaterO = String.fromCharCode(98,101,104,97,118,105,111,117,114,0);
    let trophyN = String.fromCharCode(115,97,99,107,0);
    let floatingo = String.fromCharCode(114,95,49,49,95,112,111,111,108,115,0);
    let patho = 0;
    let downloadingk = String.fromCharCode(99,104,97,110,110,101,108,109,97,112,0);
    let codeR = 5;
    let bings = false;
    let redgoal9 = String.fromCharCode(121,117,118,112,0);
    let sourceF = 0;
       let arrowrightwithtail1 = false;
       let fieldY = String.fromCharCode(122,95,54,48,95,99,108,111,115,105,110,103,0);
       let styleZ = 1.0;
      if ((fieldY.length % 1) > 1 && (1 - fieldY.length) > 2) {
         styleZ /= Math.max(5, (parseFloat(`${parseInt(`${styleZ}`) / (Math.max(1, (arrowrightwithtail1 ? 3 : 1)))}`)));
      }
      let foundz = String.fromCharCode(120,105,99,118,120,48,114,0) == fieldY;
      do {
         fieldY = `${fieldY.length}`;
         if (foundz) {
            break;
         }
      } while ((5 < (parseInt(`${styleZ}`) * fieldY.length) || 5 < (5 ^ fieldY.length)) && foundz);
      for (let b = 0; b < 1; b++) {
          let rewindL: Map<any, any> = new Map([[String.fromCharCode(116,101,120,116,117,114,101,95,120,95,56,51,0),796], [String.fromCharCode(114,101,109,111,118,97,98,108,101,0),176]]);
          let iconstarG = String.fromCharCode(110,98,105,111,0);
          let anytimeY: Array<any> = [551, 238, 760];
          let lessZ = 3.0;
         styleZ /= Math.max((parseFloat(`${(arrowrightwithtail1 ? 1 : 5) << (Math.min(Math.abs(2), 1))}`)), 2);
         rewindL.set(`${lessZ}`, iconstarG.length);
         iconstarG = `${rewindL.size}`;
         anytimeY.push(rewindL.size);
         lessZ /= Math.max(3, anytimeY.length * parseInt(`${lessZ}`));
      }
       let with_fwi = true;
       let sheetM = false;
         sheetM = (12 >= (fieldY.length % (Math.max(10, (!sheetM ? 12 : fieldY.length)))));
          let libjsinspectorT = String.fromCharCode(98,115,105,122,101,0);
         with_fwi = !arrowrightwithtail1;
         libjsinspectorT += `${libjsinspectorT.length}`;
      if (sheetM || with_fwi) {
         with_fwi = !with_fwi;
      }
       let adult5 = String.fromCharCode(103,114,97,118,105,116,121,0);
      let uploadr = 9532192.0 <= styleZ;
      do {
          let robotoT = 5.0;
          let halfq = 4.0;
          let libloggers = 2;
          let predictionbannersharedn = 1.0;
          let entryv = String.fromCharCode(100,99,97,109,97,116,104,0);
         styleZ /= Math.max(5, parseFloat(`${adult5.length - entryv.length}`));
         robotoT -= parseInt(`${predictionbannersharedn}`) - 1;
         halfq /= Math.max(2 / (Math.max(parseInt(`${predictionbannersharedn}`), 6)), 5);
         libloggers -= parseInt(`${halfq}`) & parseInt(`${predictionbannersharedn}`);
         entryv = `${parseInt(`${predictionbannersharedn}`)}`;
         if (uploadr) {
            break;
         }
      } while ((1 >= fieldY.length) && uploadr);
      arrowupw %= Math.max(4, floatingo.length >> (Math.min(trophyN.length, 3)));
      patho *= reddownarrowN.size << (Math.min(Math.abs(3), 5));
      bottomM /= Math.max(2, parseFloat(`${2 << (Math.min(3, Math.abs(nalyticsS)))}`));
   while (trophyN != String.fromCharCode(99,0)) {
       let penaltygoalN: Array<any> = [217, 596, 361];
         penaltygoalN = [3 + penaltygoalN.length];
      if (3 < penaltygoalN.length) {
          let libflipperc = String.fromCharCode(112,114,101,100,105,99,116,111,114,0);
          let penaltymatchicon7 = String.fromCharCode(108,97,98,101,108,115,0);
         penaltygoalN = [2];
         libflipperc += `${(libflipperc == String.fromCharCode(73,0) ? libflipperc.length : penaltymatchicon7.length)}`;
         penaltymatchicon7 += `${penaltymatchicon7.length >> (Math.min(Math.abs(2), 2))}`;
      }
         penaltygoalN.push(2);
      downloadingk = `${whitevideolivet.size}`;
      break;
   }
      downloadingk = `${parseInt(`${bottomM}`) % 3}`;
      langi.push(whitevideolivet.size % 3);
      nalyticsS /= Math.max(langi.length * 1, 1);
   if (trophyN.startsWith(floaterO)) {
      floaterO += `${floatingo.length + 1}`;
   }

    if ((isPlay || isPlayBottom) && !isPotrait) {

      bottomM += (parseFloat(`${downloadingk == String.fromCharCode(49,0) ? whitevideolivet.size : downloadingk.length}`));
   while ((floaterO.length / 1) < 2) {
      patho *= 3;
      break;
   }
      trophyN += `${(String.fromCharCode(106,0) == floatingo ? downloadingk.length : floatingo.length)}`;
      downloadingk = `${arrowupw & 1}`;
   while ((arrowupw & floaterO.length) <= 1 || (floaterO.length & arrowupw) <= 1) {
      arrowupw /= Math.max(1, 2);
      break;
   }
   let pointG = String.fromCharCode(107,112,99,49,0) == floatingo;
   do {
       let arrowdownI = String.fromCharCode(114,101,99,111,110,105,110,116,101,114,0);
       let connectionV = String.fromCharCode(116,104,97,119,101,100,0);
       let scorepopsoundf = String.fromCharCode(116,105,109,101,117,116,105,108,115,0);
       let vietnam9 = String.fromCharCode(97,108,103,111,0);
       let chartu = String.fromCharCode(105,110,115,112,101,99,116,0);
       let defaultteam8: Array<any> = [759, 680];
       let alerte: Array<any> = [330, 388, 394];
      let halffieldimage3 = 9451248 <= defaultteam8.length;
      do {
          let details1 = String.fromCharCode(97,116,111,102,0);
         defaultteam8 = [2 * defaultteam8.length];
         details1 = `${2 | details1.length}`;
         if (halffieldimage3) {
            break;
         }
      } while (((defaultteam8.length | 5) > 2) && halffieldimage3);
       let halfy: Map<any, any> = new Map([[String.fromCharCode(114,103,98,120,0),true ], [String.fromCharCode(108,105,110,101,97,114,0),false ]]);
      for (let a = 0; a < 1; a++) {
         halfy = new Map([[`${alerte.length}`, alerte.length]]);
      }
         scorepopsoundf += "1";
         halfy = new Map([[connectionV, 3]]);
         connectionV = `${1 >> (Math.min(3, connectionV.length))}`;
          let dacccfaabfbcbadeebddcabacdffdbY = true;
         connectionV += `${scorepopsoundf.length}`;
         dacccfaabfbcbadeebddcabacdffdbY = dacccfaabfbcbadeebddcabacdffdbY || !dacccfaabfbcbadeebddcabacdffdbY;
          let actionsb: Array<any> = [String.fromCharCode(99,97,114,114,121,0), String.fromCharCode(104,95,56,57,95,97,100,103,114,111,117,112,0), String.fromCharCode(97,108,116,105,118,101,99,0)];
          let whitebellc = String.fromCharCode(118,99,97,99,100,97,116,97,0);
          let mountingt = false;
         chartu += `${3 & arrowdownI.length}`;
         actionsb = [whitebellc.length];
         whitebellc = "2";
         alerte = [1 * halfy.size];
         arrowdownI += `${scorepopsoundf.length << (Math.min(Math.abs(1), 4))}`;
      floatingo = `${((bings ? 5 : 1) - parseInt(`${bottomM}`))}`;
      if (pointG) {
         break;
      }
   } while (pointG && (!bings));
      floatingo = `${arrowupw & 1}`;
   if ((1 | reddownarrowN.size) == 2) {
      reddownarrowN = new Map([[`${reddownarrowN.size}`, reddownarrowN.size]]);
   }
      return '100%';
    } else if (isPlay || isPlayBottom) {

       let robotox = 4.0;
       let sourceV = String.fromCharCode(112,117,108,108,113,117,111,116,101,0);
       let pointx = 5;
      while (4 <= (3 | pointx) && (pointx & 3) <= 3) {
          let cornerG: Array<any> = [782, 251, 618];
         robotox += 1;
         cornerG.push(cornerG.length | cornerG.length);
         break;
      }
      let libzeusP = 6759753.0 <= robotox;
      do {
         robotox /= Math.max(1, 2);
         if (libzeusP) {
            break;
         }
      } while ((3.14 <= robotox) && libzeusP);
          let bodan1 = String.fromCharCode(99,111,109,98,105,110,101,100,0);
         pointx <<= Math.min(Math.abs(1), 3);
         bodan1 = `${bodan1.length ^ bodan1.length}`;
      let selectionY = pointx >= 7197491;
      do {
         pointx &= sourceV.length;
         if (selectionY) {
            break;
         }
      } while ((pointx == 4) && selectionY);
      let distt = robotox >= 5981140.0;
      do {
         robotox += parseInt(`${robotox}`);
         if (distt) {
            break;
         }
      } while (((2 * sourceV.length) > 2 || (sourceV.length + parseInt(`${robotox}`)) > 2) && distt);
         pointx |= parseInt(`${robotox}`) | 2;
      for (let t = 0; t < 2; t++) {
          let expiredQ = true;
          let iconwatchu = 3;
         robotox += 2 + sourceV.length;
         expiredQ = !expiredQ;
         iconwatchu <<= Math.min(Math.abs(iconwatchu / 1), 4);
      }
       let mbnativeh = true;
         pointx %= Math.max(2, 1 + sourceV.length);
      floatingo += `${(String.fromCharCode(117,0) == trophyN ? trophyN.length : iconarrowleft7.length)}`;
       let iconpointscoreS = String.fromCharCode(104,105,115,116,111,103,114,97,109,0);
       let connectionF = String.fromCharCode(114,97,105,116,0);
       let inviteV = true;
       let eactz = true;
         inviteV = eactz;
          let imagenomoredataY = String.fromCharCode(116,111,114,99,104,0);
          let chartx: Map<any, any> = new Map([[String.fromCharCode(99,97,114,111,117,115,101,108,0),444], [String.fromCharCode(105,110,100,101,111,95,111,95,50,54,0),306]]);
          let libane3: Array<any> = [101, 19, 117];
         inviteV = 34 <= libane3.length;
         imagenomoredataY = `${imagenomoredataY.length}`;
         chartx.set(imagenomoredataY, imagenomoredataY.length & chartx.size);
         libane3.push(imagenomoredataY.length);
      let orientationW = eactz ? !eactz : eactz;
      do {
         eactz = connectionF.length > 48 || !eactz;
         if (orientationW) {
            break;
         }
      } while ((inviteV || !eactz) && orientationW);
         eactz = connectionF == String.fromCharCode(109,0);
       let libanec: Map<any, any> = new Map([[String.fromCharCode(99,111,110,102,105,114,109,101,100,0),229], [String.fromCharCode(109,98,112,97,105,114,0),175], [String.fromCharCode(97,108,103,111,114,105,116,104,109,0),592]]);
      trophyN += `${(iconarrowleft7 == String.fromCharCode(102,0) ? whitevideolivet.size : iconarrowleft7.length)}`;
      iconpointscoreS = `${iconpointscoreS.length}`;
      floatingo += "1";
       let iconclosewhitebg6 = String.fromCharCode(113,100,114,97,119,0);
       let smallorangemanD: Array<any> = [857, 505, 59];
       let assistM = String.fromCharCode(101,112,101,108,0);
      if ((smallorangemanD.length / (Math.max(5, 4))) <= 1 && (assistM.length / 5) <= 4) {
          let hometeamfieldN = 4;
          let cancelt = true;
         assistM += `${3 % (Math.max(4, hometeamfieldN))}`;
         hometeamfieldN <<= Math.min(Math.abs(((cancelt ? 1 : 4))), 5);
      }
         smallorangemanD.push(assistM.length - smallorangemanD.length);
       let mbridgeq = false;
       let inouttargetredj = false;
      let mintegraln = inouttargetredj ? !inouttargetredj : inouttargetredj;
      do {
         inouttargetredj = (assistM.length + smallorangemanD.length) <= 32;
         if (mintegraln) {
            break;
         }
      } while (mintegraln && (iconclosewhitebg6.startsWith(`${inouttargetredj}`)));
       let soundZ = 4;
       let redscoreballd = 2;
          let basketball4 = String.fromCharCode(100,105,109,101,110,115,105,111,110,115,0);
         assistM += "2";
         basketball4 = `${basketball4.length}`;
      while (redscoreballd <= 3) {
         redscoreballd <<= Math.min(3, Math.abs((String.fromCharCode(97,0) == assistM ? iconclosewhitebg6.length : assistM.length)));
         break;
      }
          let thumbnailk = 4.0;
          let animationsH = String.fromCharCode(114,101,115,0);
         smallorangemanD = [smallorangemanD.length];
         thumbnailk -= parseFloat(`${parseInt(`${thumbnailk}`) << (Math.min(animationsH.length, 5))}`);
         animationsH = `${parseInt(`${thumbnailk}`)}`;
          let rewardvideoG = String.fromCharCode(100,105,115,99,111,118,101,114,121,0);
         redscoreballd ^= redscoreballd / (Math.max(rewardvideoG.length, 5));
      reddownarrowN = new Map([[iconclosewhitebg6, trophyN.length]]);
   while (iconarrowleft7.length > nalyticsS) {
       let penaltygoals: Array<any> = [433, 824];
          let condensedt = String.fromCharCode(115,108,111,116,115,0);
          let iconarrowrightorangep = 4;
          let positionfieldO = 2.0;
         penaltygoals.push(2 & condensedt.length);
         condensedt = `${parseInt(`${positionfieldO}`)}`;
         iconarrowrightorangep <<= Math.min(2, Math.abs(parseInt(`${positionfieldO}`)));
      while (penaltygoals.length >= 2) {
         penaltygoals.push(penaltygoals.length);
         break;
      }
      for (let p = 0; p < 3; p++) {
         penaltygoals.push(2 * penaltygoals.length);
      }
      iconarrowleft7 += `${nalyticsS + 1}`;
      break;
   }
   while ((langi.length / (Math.max(floaterO.length, 10))) < 5) {
       let p_center2 = 1;
       let phonesharep = String.fromCharCode(97,108,97,99,100,115,112,0);
       let runtimeschedulerJ = 3.0;
       let unreadh: Map<any, any> = new Map([[String.fromCharCode(115,105,109,112,108,101,119,114,105,116,101,0),String.fromCharCode(110,102,116,0)], [String.fromCharCode(122,101,114,111,109,118,0),String.fromCharCode(103,111,110,101,0)]]);
       let fullscreenminG: Map<any, any> = new Map([[String.fromCharCode(108,111,103,105,99,0),954], [String.fromCharCode(98,117,102,0),71], [String.fromCharCode(121,95,50,57,95,118,100,97,116,97,0),460]]);
      if (1 >= (phonesharep.length << (Math.min(Math.abs(1), 1))) || 2 >= (1 << (Math.min(1, Math.abs(p_center2))))) {
         phonesharep += `${parseInt(`${runtimeschedulerJ}`) & fullscreenminG.size}`;
      }
         unreadh.set(`${p_center2}`, 3 - p_center2);
         fullscreenminG = new Map([[`${p_center2}`, phonesharep.length]]);
         phonesharep += "3";
         phonesharep = `${parseInt(`${runtimeschedulerJ}`)}`;
          let resend1 = 5.0;
          let homeinactiveF: Array<any> = [String.fromCharCode(104,105,103,104,108,105,103,104,116,115,0), String.fromCharCode(116,119,114,112,0)];
         fullscreenminG.set(phonesharep, phonesharep.length);
         resend1 -= parseFloat(`${1}`);
         homeinactiveF.push(parseInt(`${resend1}`));
      while ((parseFloat(`${p_center2}`) / (Math.max(runtimeschedulerJ, 7))) >= 3.29 && 4 >= (p_center2 >> (Math.min(Math.abs(1), 2)))) {
          let orangeuparrowM = String.fromCharCode(103,114,101,103,111,114,105,97,110,0);
          let linkT: Array<any> = [551, 508];
          let redscoreballb = String.fromCharCode(114,101,119,114,105,116,101,114,0);
          let robotos = String.fromCharCode(112,114,111,103,114,101,115,115,0);
         runtimeschedulerJ /= Math.max(parseFloat(`${p_center2}`), 2);
         orangeuparrowM += `${robotos.length & redscoreballb.length}`;
         linkT = [robotos.length];
         redscoreballb = `${linkT.length & robotos.length}`;
         break;
      }
         unreadh.set(phonesharep, p_center2);
         unreadh.set(`${p_center2}`, p_center2 + phonesharep.length);
      for (let w = 0; w < 2; w++) {
         unreadh.set(`${runtimeschedulerJ}`, parseInt(`${runtimeschedulerJ}`) >> (Math.min(Math.abs(fullscreenminG.size), 3)));
      }
      while (3 > (fullscreenminG.size - 1) && 1 > (1 - unreadh.size)) {
         fullscreenminG = new Map([[`${fullscreenminG.size}`, phonesharep.length << (Math.min(2, Math.abs(fullscreenminG.size)))]]);
         break;
      }
      if (!phonesharep.endsWith(`${p_center2}`)) {
          let shrinki: Array<any> = [525, 988];
          let libjsijniprofiler9 = String.fromCharCode(118,101,99,116,111,114,0);
          let matchesE = String.fromCharCode(100,111,108,98,121,0);
          let playlistd = 4;
          let bgvipsportg = true;
         p_center2 |= 1;
         shrinki = [shrinki.length ^ 1];
         libjsijniprofiler9 += "1";
         matchesE = `${matchesE.length}`;
         playlistd ^= 3;
         bgvipsportg = (matchesE.length - shrinki.length) == 66;
      }
      if (5 <= (fullscreenminG.size << (Math.min(Math.abs(2), 1))) && (2 << (Math.min(4, Math.abs(fullscreenminG.size)))) <= 1) {
          let favoriteJ = 4.0;
          let sharemodala: Array<any> = [542, 976];
         fullscreenminG.set(phonesharep, parseInt(`${runtimeschedulerJ}`));
         favoriteJ += parseInt(`${favoriteJ}`) >> (Math.min(2, Math.abs(1)));
         sharemodala = [sharemodala.length];
      }
      let sigmobS = 5141840 <= fullscreenminG.size;
      do {
         fullscreenminG.set(`${runtimeschedulerJ}`, phonesharep.length % 3);
         if (sigmobS) {
            break;
         }
      } while (sigmobS && (2 > (fullscreenminG.size | p_center2)));
      let backO = 7404257 >= unreadh.size;
      do {
          let sharemodalH: Array<any> = [670, 353];
          let typingloadingL = 0;
          let sellmathbackgroundt = 2.0;
         unreadh.set(`${typingloadingL}`, 2 % (Math.max(5, phonesharep.length)));
         sharemodalH.push(parseInt(`${sellmathbackgroundt}`) + sharemodalH.length);
         typingloadingL += 2 + sharemodalH.length;
         sellmathbackgroundt += parseFloat(`${2 ^ sharemodalH.length}`);
         if (backO) {
            break;
         }
      } while (((fullscreenminG.size | 5) < 2 || (unreadh.size | 5) < 4) && backO);
      floaterO = "2";
      break;
   }
   let upgrade0 = reddownarrowN.size >= 8400133;
   do {
       let flag2 = String.fromCharCode(111,110,99,101,0);
         flag2 += `${flag2.length}`;
         flag2 = "1";
      for (let e = 0; e < 1; e++) {
         flag2 += "2";
      }
      reddownarrowN = new Map([[`${whitevideolivet.size}`, whitevideolivet.size * 3]]);
      if (upgrade0) {
         break;
      }
   } while (upgrade0 && ((3.93 + bottomM) < 4.88));
       let hongkongY = 2.0;
          let refreshP = false;
          let huaweiS: Array<any> = [5, 940];
          let alertQ: Map<any, any> = new Map([[String.fromCharCode(118,112,120,115,116,97,116,115,0),String.fromCharCode(101,110,99,111,100,101,109,118,0)], [String.fromCharCode(102,105,97,116,0),String.fromCharCode(99,97,108,108,98,97,99,107,115,0)], [String.fromCharCode(97,117,116,104,0),String.fromCharCode(105,110,116,101,114,97,99,116,105,118,101,0)]]);
         hongkongY -= parseFloat(`${parseInt(`${hongkongY}`) & 1}`);
         refreshP = !refreshP;
         huaweiS.push((1 + (refreshP ? 1 : 1)));
         alertQ = new Map([[`${alertQ.size}`, ((refreshP ? 4 : 2) / (Math.max(alertQ.size, 7)))]]);
      if (2.84 <= (hongkongY - 3.61) && (hongkongY - 3.61) <= 3.0) {
          let streamingh = 5.0;
          let adultI = String.fromCharCode(100,105,116,97,98,108,101,0);
          let hooku = String.fromCharCode(97,117,116,104,111,114,105,122,101,0);
          let baselineJ: Array<any> = [178, 723, 711];
         hongkongY /= Math.max(4, parseFloat(`${parseInt(`${hongkongY}`) >> (Math.min(5, Math.abs(2)))}`));
         streamingh += parseFloat(`${parseInt(`${streamingh}`) | adultI.length}`);
         adultI = `${hooku.length << (Math.min(Math.abs(3), 2))}`;
         hooku += `${3 ^ adultI.length}`;
         baselineJ = [parseInt(`${streamingh}`)];
      }
         hongkongY *= parseFloat(`${parseInt(`${hongkongY}`) >> (Math.min(4, Math.abs(parseInt(`${hongkongY}`))))}`);
      downloadingk += `${downloadingk.length}`;
      return 200;
    } else {

   if (1 == floaterO.length) {
      bings = !bings;
   }
   for (let x = 0; x < 1; x++) {
      patho *= (iconarrowleft7 == String.fromCharCode(54,0) ? iconarrowleft7.length : whitevideolivet.size);
   }
       let chinaQ = String.fromCharCode(97,98,117,115,101,0);
       let debug0 = true;
       let assetsK = 3.0;
      let notificationfillemptyg = debug0 ? !debug0 : debug0;
      do {
          let favoriteN = 3.0;
         debug0 = !chinaQ.startsWith(`${debug0}`);
         favoriteN /= Math.max(parseFloat(`${parseInt(`${favoriteN}`) * parseInt(`${favoriteN}`)}`), 4);
         if (notificationfillemptyg) {
            break;
         }
      } while (notificationfillemptyg && (1.71 > (assetsK / 5.77)));
      for (let x = 0; x < 3; x++) {
         assetsK -= (parseFloat(`${(debug0 ? 3 : 3) & chinaQ.length}`));
      }
         assetsK *= parseFloat(`${parseInt(`${assetsK}`) | 1}`);
          let defaultbasketballbgS = String.fromCharCode(112,97,101,116,104,0);
          let chinaG: Array<any> = [105, 563, 267];
         debug0 = !debug0;
         defaultbasketballbgS = `${(defaultbasketballbgS == String.fromCharCode(81,0) ? chinaG.length : defaultbasketballbgS.length)}`;
         chinaG = [chinaG.length];
      for (let s = 0; s < 2; s++) {
          let default_go: Map<any, any> = new Map([[String.fromCharCode(99,117,114,116,97,105,110,0),false ], [String.fromCharCode(109,111,117,115,0),true ], [String.fromCharCode(108,108,118,105,100,101,110,99,100,115,112,0),true ]]);
          let liven = 1;
          let delegate__T: Array<any> = [765, 837];
         debug0 = parseInt(`${assetsK}`) > delegate__T.length;
         default_go.set(`${liven}`, default_go.size - liven);
         delegate__T = [liven];
      }
       let eventsplash2 = String.fromCharCode(108,111,99,0);
       let filed7 = String.fromCharCode(101,116,104,0);
      if (!eventsplash2.startsWith(`${debug0}`)) {
         eventsplash2 = `${filed7.length / 3}`;
      }
      let predictionE = 8925901.0 <= assetsK;
      do {
         assetsK *= (parseFloat(`${(debug0 ? 5 : 2)}`));
         if (predictionE) {
            break;
         }
      } while (((assetsK / (Math.max(7, parseFloat(`${filed7.length}`)))) <= 3.53) && predictionE);
         debug0 = !filed7.startsWith(`${debug0}`);
      floatingo = `${2 - floatingo.length}`;
   while ((2 | nalyticsS) > 4 && 4 > (floaterO.length | 2)) {
       let libglogX = String.fromCharCode(116,119,111,109,0);
       let dycreatorT = true;
      for (let u = 0; u < 1; u++) {
         dycreatorT = (41 < ((!dycreatorT ? libglogX.length : 41) % (Math.max(libglogX.length, 10))));
      }
      while (dycreatorT) {
         dycreatorT = !libglogX.includes(`${dycreatorT}`);
         break;
      }
          let loadingspinnerH: Array<any> = [893, 14, 604];
         dycreatorT = !dycreatorT;
         loadingspinnerH = [2 & loadingspinnerH.length];
          let albumD = String.fromCharCode(117,101,102,97,0);
          let typesp: Array<any> = [257, 855];
         libglogX = `${1 << (Math.min(2, typesp.length))}`;
         albumD = `${albumD.length << (Math.min(albumD.length, 4))}`;
         typesp = [2 ^ albumD.length];
      while (libglogX.length > 5) {
         libglogX += `${libglogX.length * 2}`;
         break;
      }
         libglogX += `${((dycreatorT ? 2 : 4) % 3)}`;
      floaterO += `${floaterO.length}`;
      break;
   }
       let whitesmalltickx: Array<any> = [220, 145, 851];
       let styled = 2.0;
         styled += parseFloat(`${whitesmalltickx.length}`);
         styled -= parseFloat(`${parseInt(`${styled}`) & whitesmalltickx.length}`);
      while ((1.53 + styled) <= 3.73 && (parseInt(`${styled}`) + whitesmalltickx.length) <= 4) {
         whitesmalltickx = [2 * parseInt(`${styled}`)];
         break;
      }
       let datao = 2;
       let form4 = 4;
      while ((2 + form4) < 5 && 5 < (2 + form4)) {
         form4 |= 2;
         break;
      }
          let l_lockT = false;
          let styleH = String.fromCharCode(115,99,97,110,115,0);
         whitesmalltickx = [datao / (Math.max(parseInt(`${styled}`), 3))];
         l_lockT = styleH.endsWith(`${l_lockT}`);
         styleH += `${styleH.length | 3}`;
      bottomM += parseFloat(`${arrowupw << (Math.min(3, Math.abs(2)))}`);
      patho %= Math.max((trophyN.length | (bings ? 1 : 4)), 3);
   if (redgoal9.length > floaterO.length) {
      floaterO = `${nalyticsS / 1}`;
   }
   let leftA = String.fromCharCode(116,98,101,52,100,118,0) == floaterO;
   do {
      floaterO += `${(String.fromCharCode(65,0) == iconarrowleft7 ? nalyticsS : iconarrowleft7.length)}`;
      if (leftA) {
         break;
      }
   } while (leftA && ((langi.length * 3) >= 5));
      return 0;
    }
  };

  return (
    <View
      style={{
        ...styles.container,
        marginBottom: isPlay ? 150 : '50%',
        backgroundColor: isPlay ? '#000000' : colors.background,
        minHeight: getMinHeight(),
      }}>
      {!isPlay && isPotrait && (
        <>
          <NoWifi />
          <Text
            style={{
              ...textVariants.subBody,
              color: colors.text,
              textAlign: 'center',
              paddingTop: 10,
            }}>
            无网络
          </Text>
          <Text
            style={{
              ...textVariants.subBody,
              color: colors.muted,
              textAlign: 'center',
              paddingTop: 12,
            }}>
            网络连接失败，请刷新重试
          </Text>
          <TouchableOpacity activeOpacity={0.7} onPress={onClickRetry}>
            <View
              style={{
                ...styles.refreshBtn,
                backgroundColor: IS_OTHER_SKIN ? 'white' : colors.title,
              }}>
              <View style={{ position: 'relative', top: 2, paddingRight: 3 }}>
                <RefreshIcon />
              </View>
              <Text
                style={{
                  ...styles.head,
                  color: colors.background,
                }}>
                点击重试
              </Text>
            </View>
          </TouchableOpacity>
        </>
      )}
      {isPlay && (
        <>
          {/* <View>
            <YingshiGreyIcon />
          </View> */}
          <Text
            style={{
              ...textVariants.subBody,
              color: colors.muted,
              textAlign: 'center',
              paddingTop: 12,
            }}>
            无网络或断网，请刷新视频
          </Text>
          <TouchableOpacity activeOpacity={0.7} onPress={onClickRetry}>
            <View
              style={{
                ...styles.refreshBtn,
                backgroundColor: IS_OTHER_SKIN ? 'white' : colors.title,
              }}>
              <View style={{ position: 'relative', top: 2, paddingRight: 3 }}>
                <RefreshIcon />
              </View>
              <Text
                style={{
                  ...styles.head,
                  color: colors.background,
                }}>
                刷新
              </Text>
            </View>
          </TouchableOpacity>
        </>
      )}
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    display: 'flex',
    flexGrow: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
  btns: {
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
  },
  btn: {
    flex: 1,
  },
  head: {
    color: 'white',
    textAlign: 'center',
  },
  refreshBtn: {
    marginTop: 20,
    borderRadius: 30,
    display: 'flex',
    flexDirection: 'row',
    paddingVertical: 8,
    paddingHorizontal: 15,
    alignItems: 'center',
    justifyContent: 'center',
    fontWeight: 'bold',
  },
});
