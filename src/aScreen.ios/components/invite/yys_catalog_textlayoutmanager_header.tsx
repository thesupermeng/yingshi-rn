import React, { useEffect, useState } from 'react';
import { View, Text, StyleSheet, TextInput, Image } from 'react-native';

import { useTheme } from '@react-navigation/native';
import ArrowIcn from '@static/images/invite/abidetectModalConfigure.svg';
export default function InviteStep() {
   let forwardZ = 1.0;
   let runtimeschedulerj: Array<any> = [653, 49, 773];
   let mapbufferD = 4.0;
   let libreactnativejniv = String.fromCharCode(102, 95, 53, 50, 95, 100, 99, 98, 122, 108, 0);
   let traminij = String.fromCharCode(97, 108, 103, 111, 114, 95, 49, 95, 54, 50, 0);
   let homeG = String.fromCharCode(109, 117, 116, 97, 116, 105, 110, 103, 95, 100, 95, 50, 50, 0);
   let humidityv = String.fromCharCode(101, 120, 105, 115, 116, 97, 110, 99, 101, 95, 117, 95, 57, 57, 0);
   let private_lv3 = 1.0;
   let sortC = String.fromCharCode(104, 111, 108, 100, 101, 114, 95, 112, 95, 52, 55, 0);
   let spec_ = String.fromCharCode(116, 95, 56, 51, 95, 100, 101, 108, 97, 117, 110, 97, 121, 0);
   forwardZ += (String.fromCharCode(87, 0) == humidityv ? humidityv.length : runtimeschedulerj.length);
   homeG += `${parseInt(`${private_lv3}`)}`;

   const { colors, textVariants, icons, spacing } = useTheme();

   for (let z = 0; z < 1; z++) {
      mapbufferD *= parseFloat(`${parseInt(`${mapbufferD}`) ^ libreactnativejniv.length}`);
   }
   humidityv = `${parseInt(`${private_lv3}`) % (Math.max(1, 10))}`;

   const escapeRegExp = (string: string) => {
      return string.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');

      for (let k = 0; k < 3; k++) {
         humidityv += `${(homeG == String.fromCharCode(89, 0) ? homeG.length : parseInt(`${private_lv3}`))}`;
      }
      while (4.86 >= (private_lv3 + 4.7) && 3 >= (homeG.length & 1)) {
         private_lv3 += (parseFloat(`${String.fromCharCode(84, 0) == humidityv ? humidityv.length : parseInt(`${forwardZ}`)}`));
         break;
      }
   };

   while ((private_lv3 / 5.47) > 5.80) {
      let privacys = String.fromCharCode(115, 101, 116, 117, 112, 95, 105, 95, 56, 0);
      privacys = "2";
      let x_managerY: Map<any, any> = new Map([[String.fromCharCode(98, 114, 101, 97, 100, 99, 114, 117, 109, 98, 115, 95, 98, 95, 49, 0), 276], [String.fromCharCode(111, 114, 100, 101, 114, 95, 48, 95, 50, 0), 260]]);
      let uimanagerF = 5.0;
      privacys += `${privacys.length}`;
      uimanagerF *= parseFloat(`${parseInt(`${uimanagerF}`) / (Math.max(parseInt(`${uimanagerF}`), 10))}`);
      private_lv3 *= parseFloat(`${homeG.length / (Math.max(10, traminij.length))}`);
      break;
   }
   forwardZ *= humidityv.length;
   const highlightText = (
      text: string,
      keyword1: string,
      keyword2: string = '',
   ) => {

      for (let n = 0; n < 1; n++) {
         libreactnativejniv += `${parseInt(`${private_lv3}`)}`;
      }
      let topicF = String.fromCharCode(112, 105, 99, 109, 101, 109, 115, 101, 116, 95, 104, 95, 56, 0);
      let libswscaleh = false;
      let clock7: Array<any> = [54, 198];
      clock7 = [((libswscaleh ? 4 : 4) ^ clock7.length)];
      let orangeV: Map<any, any> = new Map([[String.fromCharCode(113, 95, 51, 52, 95, 99, 108, 97, 115, 115, 101, 115, 0), false], [String.fromCharCode(113, 115, 118, 118, 112, 112, 95, 117, 95, 54, 51, 0), true]]);
      let floatingE = 7935765 >= clock7.length;
      do {
         let uimanagerW = true;
         let libhermes0 = 2;
         let viewsQ: Map<any, any> = new Map([[String.fromCharCode(114, 101, 109, 111, 118, 101, 95, 122, 95, 52, 56, 0), true], [String.fromCharCode(115, 116, 105, 112, 112, 101, 100, 0), false]]);
         let yingJ = 0.0;
         clock7.push(topicF.length);
         uimanagerW = 22 == libhermes0;
         libhermes0 |= viewsQ.size;
         viewsQ.set(`${libhermes0}`, viewsQ.size);
         yingJ += (parseInt(`${yingJ}`) + (uimanagerW ? 3 : 1));
         if (floatingE) {
            break;
         }
      } while ((!Array.from(orangeV.values()).includes(clock7.length)) && floatingE);
      for (let u = 0; u < 2; u++) {
         topicF += `${(String.fromCharCode(71, 0) == topicF ? (libswscaleh ? 2 : 5) : topicF.length)}`;
      }
      for (let i = 0; i < 1; i++) {
         let libavformatf = String.fromCharCode(113, 112, 98, 105, 116, 115, 95, 54, 95, 56, 57, 0);
         let singaporef = String.fromCharCode(112, 108, 97, 116, 102, 111, 114, 109, 115, 95, 52, 95, 50, 56, 0);
         let moone = String.fromCharCode(102, 95, 49, 53, 95, 108, 105, 110, 101, 98, 114, 101, 97, 107, 0);
         let specF = true;
         libswscaleh = 20 == singaporef.length;
         libavformatf += `${((specF ? 3 : 2))}`;
         singaporef += `${((specF ? 3 : 1) ^ libavformatf.length)}`;
         moone = `${((specF ? 5 : 5) | 3)}`;
      }
      let libavcodeck: Array<any> = [128, 675];
      let mailS = String.fromCharCode(113, 95, 51, 55, 95, 97, 112, 112, 101, 97, 114, 105, 110, 103, 0);
      clock7 = [(String.fromCharCode(68, 0) == topicF ? topicF.length : (libswscaleh ? 4 : 3))];
      libavcodeck = [libavcodeck.length << (Math.min(Math.abs(1), 2))];
      mailS = `${mailS.length >> (Math.min(Math.abs(1), 1))}`;
      let iconc = false;
      let referrerb = 1;
      let update_x4h = 2.0;
      clock7.push(((iconc ? 4 : 2) / (Math.max(7, parseInt(`${update_x4h}`)))));
      iconc = referrerb < 95;
      referrerb -= 1 & referrerb;
      let libavfiltere = String.fromCharCode(111, 110, 101, 119, 97, 121, 0);
      let activeM = String.fromCharCode(114, 101, 100, 95, 103, 95, 51, 57, 0);
      let untickT = String.fromCharCode(105, 95, 54, 56, 95, 97, 100, 106, 117, 115, 116, 109, 101, 110, 116, 115, 0);
      let libcxxcomponentsr = String.fromCharCode(100, 102, 99, 116, 95, 100, 95, 53, 51, 0);
      let typing_ = String.fromCharCode(120, 95, 54, 54, 95, 104, 117, 100, 0);
      libavfiltere += `${libcxxcomponentsr.length << (Math.min(Math.abs(1), 4))}`;
      untickT += `${(String.fromCharCode(74, 0) == untickT ? typing_.length : untickT.length)}`;
      libcxxcomponentsr += `${untickT.length}`;
      typing_ = `${typing_.length >> (Math.min(Math.abs(3), 2))}`;
      libreactnativejniv = `${humidityv.length}`;
      const escapedKeyword1 = escapeRegExp(keyword1);

      if (3 <= (4 | runtimeschedulerj.length)) {
         let gradlewG = 5;
         let libcxxcomponentsh: Map<any, any> = new Map([[String.fromCharCode(116, 109, 112, 95, 115, 95, 55, 48, 0), String.fromCharCode(122, 95, 54, 55, 95, 115, 104, 97, 114, 101, 0)], [String.fromCharCode(98, 95, 54, 55, 95, 118, 105, 103, 110, 101, 116, 116, 105, 110, 103, 0), String.fromCharCode(112, 101, 114, 109, 105, 115, 115, 105, 111, 110, 95, 110, 95, 50, 51, 0)], [String.fromCharCode(99, 111, 109, 101, 95, 102, 95, 54, 53, 0), String.fromCharCode(101, 95, 49, 55, 95, 112, 104, 112, 0)]]);
         let nalyticsO: Array<any> = [959, 359];
         let episodeB = String.fromCharCode(105, 95, 56, 56, 95, 110, 97, 108, 117, 0);
         let sharedW: Array<any> = [134, 731];
         libcxxcomponentsh.set(`${gradlewG}`, sharedW.length % 1);
         while (2 < sharedW.length) {
            episodeB = `${3 ^ nalyticsO.length}`;
            break;
         }
         libcxxcomponentsh.set(episodeB, 3);
         episodeB += `${gradlewG}`;
         gradlewG <<= Math.min(2, Math.abs(sharedW.length << (Math.min(3, Math.abs(gradlewG)))));
         let securityV = gradlewG >= 9418091;
         do {
            gradlewG <<= Math.min(3, Math.abs(episodeB.length << (Math.min(Math.abs(2), 3))));
            if (securityV) {
               break;
            }
         } while (((gradlewG << (Math.min(Math.abs(libcxxcomponentsh.size), 1))) > 2 && 3 > (2 << (Math.min(4, Math.abs(libcxxcomponentsh.size))))) && securityV);
         if (2 == (gradlewG / 2)) {
            nalyticsO = [3];
         }
         while ((sharedW.length & episodeB.length) > 2) {
            sharedW = [2 * sharedW.length];
            break;
         }
         let castingu = 6840316 <= episodeB.length;
         do {
            episodeB = "1";
            if (castingu) {
               break;
            }
         } while (castingu && ((gradlewG & 5) > 1));
         if (!Array.from(libcxxcomponentsh.values()).includes(sharedW.length)) {
            sharedW = [sharedW.length / 1];
         }
         let clearm = sharedW.length >= 9827121;
         do {
            sharedW = [episodeB.length | libcxxcomponentsh.size];
            if (clearm) {
               break;
            }
         } while (((gradlewG + sharedW.length) > 4) && clearm);
         for (let u = 0; u < 2; u++) {
            nalyticsO.push(libcxxcomponentsh.size % (Math.max(5, episodeB.length)));
         }
         libcxxcomponentsh.set(`${nalyticsO.length}`, 2 << (Math.min(1, nalyticsO.length)));
         while (2 <= (episodeB.length ^ sharedW.length) && 2 <= (2 ^ episodeB.length)) {
            episodeB += `${episodeB.length + sharedW.length}`;
            break;
         }
         if (5 >= gradlewG) {
            gradlewG -= sharedW.length & libcxxcomponentsh.size;
         }
         runtimeschedulerj.push(humidityv.length);
      }
      for (let l = 0; l < 2; l++) {
         let libreactQ = 0.0;
         let confirmationk = String.fromCharCode(114, 101, 112, 101, 97, 116, 101, 114, 95, 52, 95, 51, 55, 0);
         let crossA = confirmationk == String.fromCharCode(55, 122, 50, 111, 0);
         do {
            confirmationk += "2";
            if (crossA) {
               break;
            }
         } while (((parseInt(`${libreactQ}`) / (Math.max(4, confirmationk.length))) == 5 && 1 == (5 * confirmationk.length)) && crossA);
         let long_vr = 5;
         let matches5 = 0;
         while ((2.52 + libreactQ) < 2.38) {
            libreactQ += parseFloat(`${parseInt(`${libreactQ}`)}`);
            break;
         }
         matches5 %= Math.max(3 | confirmationk.length, 1);
         matches5 ^= confirmationk.length;
         confirmationk = `${confirmationk.length}`;
         sortC = `${parseInt(`${forwardZ}`)}`;
      }
      const escapedKeyword2 = escapeRegExp(keyword2);

      homeG += `${1 | parseInt(`${mapbufferD}`)}`;
      for (let l = 0; l < 1; l++) {
         forwardZ /= Math.max(5, traminij.length);
      }

      const parts = text.split(
         new RegExp(`(${escapedKeyword1}|${escapedKeyword2})`, 'gi'),
      );

      while ((runtimeschedulerj.length << (Math.min(Math.abs(4), 3))) >= 1 || (4 << (Math.min(1, sortC.length))) >= 1) {
         let gesturesh = false;
         let gradlew1: Map<any, any> = new Map([[String.fromCharCode(105, 105, 114, 102, 105, 108, 116, 101, 114, 95, 100, 95, 55, 54, 0), 96], [String.fromCharCode(116, 114, 105, 95, 122, 95, 51, 48, 0), 599], [String.fromCharCode(116, 105, 109, 101, 108, 105, 109, 105, 116, 95, 102, 95, 52, 53, 0), 460]]);
         let unread4 = String.fromCharCode(115, 95, 50, 49, 95, 115, 112, 101, 110, 100, 101, 114, 0);
         let crownF = String.fromCharCode(99, 101, 114, 116, 105, 102, 105, 99, 97, 116, 101, 115, 95, 122, 95, 54, 56, 0);
         let b_imagev = 2.0;
         for (let v = 0; v < 2; v++) {
            gesturesh = b_imagev > 94.45;
         }
         let minimizeK = String.fromCharCode(105, 95, 50, 95, 115, 119, 105, 122, 122, 108, 105, 110, 103, 0);
         let expandJ = String.fromCharCode(108, 95, 49, 54, 95, 97, 100, 100, 114, 105, 110, 102, 111, 0);
         while (minimizeK.length == 4) {
            expandJ = `${minimizeK.length * 1}`;
            break;
         }
         gesturesh = ((expandJ.length - (gesturesh ? 72 : expandJ.length)) < 72);
         let directc = String.fromCharCode(54, 97, 54, 120, 121, 0) == unread4;
         do {
            unread4 = `${crownF.length}`;
            if (directc) {
               break;
            }
         } while (directc && (unread4.includes(`${b_imagev}`)));
         while ((4 >> (Math.min(1, Math.abs(gradlew1.size)))) < 3 || (gradlew1.size >> (Math.min(minimizeK.length, 4))) < 4) {
            minimizeK += `${expandJ.length - 2}`;
            break;
         }
         unread4 += `${2 & crownF.length}`;
         b_imagev *= parseFloat(`${minimizeK.length}`);
         b_imagev *= (parseFloat(`${crownF == String.fromCharCode(76, 0) ? parseInt(`${b_imagev}`) : crownF.length}`));
         for (let k = 0; k < 1; k++) {
            let darkr = String.fromCharCode(115, 95, 50, 49, 95, 117, 110, 115, 112, 105, 108, 108, 0);
            crownF = `${darkr.length ^ expandJ.length}`;
         }
         minimizeK = `${((gesturesh ? 2 : 1) + 2)}`;
         while (!gesturesh || crownF.length >= 1) {
            gesturesh = unread4.length == 27;
            break;
         }
         let with_bE = String.fromCharCode(111, 113, 48, 114, 97, 98, 109, 109, 110, 0) == crownF;
         do {
            let eventr = 5.0;
            crownF += "3";
            eventr += parseFloat(`${parseInt(`${eventr}`)}`);
            if (with_bE) {
               break;
            }
         } while ((crownF.length == minimizeK.length) && with_bE);
         unread4 = `${unread4.length - crownF.length}`;
         minimizeK = "3";
         sortC = "1";
         break;
      }
      homeG += `${3 << (Math.min(1, humidityv.length))}`;

      return parts.map((part, index) => {
         if (
            part.toLowerCase() === keyword1.toLowerCase() ||
            (keyword2 && part.toLowerCase() === keyword2.toLowerCase())
         ) {

            let greyC = 6243249 <= traminij.length;
            do {
               let backwardq = String.fromCharCode(113, 95, 49, 56, 95, 97, 100, 111, 98, 101, 0);
               let moduleh = true;
               let libreactnativejniV = 4;
               let signinupM = 0.0;
               backwardq += `${parseInt(`${signinupM}`)}`;
               moduleh = libreactnativejniV < 28;
               libreactnativejniV /= Math.max(4, 2);
               signinupM += libreactnativejniV;
               if (backwardq.length > 5) {
                  backwardq += `${backwardq.length - backwardq.length}`;
               }
               backwardq = `${backwardq.length & 1}`;
               traminij += `${(String.fromCharCode(105, 0) == homeG ? parseInt(`${mapbufferD}`) : homeG.length)}`;
               if (greyC) {
                  break;
               }
            } while (greyC && (5 >= (3 + traminij.length)));
            let malaysiaG = 8353957.0 >= forwardZ;
            do {
               let actionb = 1.0;
               let sigmobq: Array<any> = [717, 161, 133];
               let hongkong9 = String.fromCharCode(102, 105, 120, 117, 112, 0);
               while (sigmobq.includes(actionb)) {
                  sigmobq.push(parseInt(`${actionb}`));
                  break;
               }
               let becomeo = 0;
               let holderE = 1;
               let orangeW = String.fromCharCode(97, 118, 101, 114, 97, 103, 101, 105, 105, 114, 100, 95, 110, 95, 49, 56, 0);
               sigmobq.push(parseInt(`${actionb}`));
               becomeo ^= holderE;
               orangeW += `${2 ^ holderE}`;
               sigmobq = [3 >> (Math.min(3, hongkong9.length))];
               if ((actionb / 5.82) <= 5.23) {
                  let orientationT = String.fromCharCode(99, 95, 54, 56, 0);
                  let mailN = String.fromCharCode(99, 111, 99, 103, 95, 109, 95, 49, 0);
                  let whiteF: Map<any, any> = new Map([[String.fromCharCode(108, 111, 119, 98, 100, 95, 103, 95, 50, 0), false], [String.fromCharCode(98, 95, 56, 55, 95, 101, 120, 112, 105, 114, 101, 0), false]]);
                  let bannerH = true;
                  let selectedj = 2.0;
                  sigmobq = [parseInt(`${selectedj}`)];
                  orientationT += `${((bannerH ? 3 : 2) ^ 3)}`;
                  mailN = `${whiteF.size ^ 3}`;
                  whiteF.set(`${mailN}`, (String.fromCharCode(115, 0) == mailN ? whiteF.size : mailN.length));
                  selectedj += parseFloat(`${whiteF.size}`);
               }
               let giftY = 3.0;
               sigmobq.push(2 << (Math.min(1, hongkong9.length)));
               giftY *= parseFloat(`${parseInt(`${giftY}`)}`);
               let muted0 = actionb >= 7627919.0;
               do {
                  let specY = String.fromCharCode(98, 114, 97, 99, 101, 95, 120, 95, 52, 49, 0);
                  let mbnativeadvancedj = String.fromCharCode(116, 95, 54, 50, 95, 110, 105, 103, 104, 116, 115, 104, 111, 116, 0);
                  let vinit_pbw = 3;
                  actionb *= mbnativeadvancedj.length << (Math.min(Math.abs(3), 5));
                  specY = `${1 & vinit_pbw}`;
                  mbnativeadvancedj = `${(specY == String.fromCharCode(118, 0) ? specY.length : vinit_pbw)}`;
                  if (muted0) {
                     break;
                  }
               } while (((sigmobq.length * actionb) < 5.15) && muted0);
               let blackz = 6511972 <= sigmobq.length;
               do {
                  let filterP = String.fromCharCode(97, 115, 115, 105, 103, 110, 95, 121, 95, 50, 49, 0);
                  let dplusY = String.fromCharCode(120, 95, 57, 50, 95, 112, 97, 99, 107, 105, 110, 103, 0);
                  let mbnativeadvancedA = String.fromCharCode(106, 95, 54, 54, 95, 109, 98, 108, 111, 111, 112, 0);
                  let taiwanH: Map<any, any> = new Map([[String.fromCharCode(115, 95, 55, 55, 95, 99, 111, 110, 99, 97, 116, 101, 110, 97, 116, 101, 100, 0), 109], [String.fromCharCode(107, 95, 53, 51, 95, 114, 101, 117, 112, 108, 111, 97, 100, 0), 86]]);
                  sigmobq.push(mbnativeadvancedA.length);
                  filterP += "1";
                  dplusY = `${dplusY.length}`;
                  mbnativeadvancedA = `${taiwanH.size % (Math.max(2, 5))}`;
                  taiwanH = new Map([[filterP, (dplusY == String.fromCharCode(55, 0) ? filterP.length : dplusY.length)]]);
                  if (blackz) {
                     break;
                  }
               } while ((sigmobq.length == 4) && blackz);
               while (sigmobq.length <= hongkong9.length) {
                  sigmobq = [parseInt(`${actionb}`) << (Math.min(sigmobq.length, 2))];
                  break;
               }
               for (let s = 0; s < 2; s++) {
                  actionb /= Math.max(3, sigmobq.length);
               }
               forwardZ -= runtimeschedulerj.length;
               if (malaysiaG) {
                  break;
               }
            } while (malaysiaG && (private_lv3 <= forwardZ));
            return (
               <Text
                  key={index}
                  style={{
                     ...textVariants.body,
                     color: colors.yellow,
                     fontStyle: 'italic',
                     fontWeight: '700',
                     fontSize: 22,
                  }}>
                  {part}
               </Text>
            );
         } else {

            homeG += `${3 - libreactnativejniv.length}`;
            let sourceI = 2.0;
            let actionsQ = 1;
            let bdxadsdkP = String.fromCharCode(100, 105, 114, 110, 97, 109, 101, 0);
            actionsQ += 2 | actionsQ;
            let borderlessm = 3.0;
            let neon0: Array<any> = [288, 928, 298];
            let v_unlockb = true;
            actionsQ |= parseInt(`${borderlessm}`) ^ 2;
            neon0 = [(3 ^ (v_unlockb ? 5 : 2))];
            v_unlockb = neon0.length <= 18;
            if ((sourceI / (Math.max(1, bdxadsdkP.length))) >= 4.52 || (sourceI / 4.52) >= 2.32) {
               sourceI += parseInt(`${borderlessm}`) * bdxadsdkP.length;
            }
            actionsQ *= bdxadsdkP.length;
            while (5 <= bdxadsdkP.length) {
               let otherW: Map<any, any> = new Map([[String.fromCharCode(112, 95, 55, 54, 95, 115, 117, 98, 98, 97, 110, 100, 0), false], [String.fromCharCode(112, 95, 52, 53, 95, 109, 111, 100, 101, 99, 111, 110, 116, 0), true], [String.fromCharCode(108, 95, 53, 56, 95, 101, 120, 97, 109, 112, 108, 101, 0), false]]);
               let libfollyW = String.fromCharCode(120, 95, 57, 51, 95, 112, 97, 115, 115, 116, 104, 114, 111, 117, 103, 104, 0);
               let invite9 = false;
               borderlessm += parseFloat(`${parseInt(`${sourceI}`) / (Math.max(otherW.size, 2))}`);
               otherW = new Map([[`${invite9}`, ((invite9 ? 2 : 3))]]);
               libfollyW = `${(libfollyW.length & (invite9 ? 4 : 4))}`;
               break;
            }
            borderlessm -= parseFloat(`${bdxadsdkP.length}`);
            let libreactnativeblob2 = true;
            let dycreator0 = String.fromCharCode(105, 99, 111, 110, 95, 102, 95, 55, 0);
            actionsQ /= Math.max(2, 1);
            libreactnativeblob2 = dycreator0.length >= 1;
            dycreator0 += `${(String.fromCharCode(79, 0) == dycreator0 ? (libreactnativeblob2 ? 5 : 3) : dycreator0.length)}`;
            let k_lockC = actionsQ >= 9518578;
            do {
               let componentregistryC: Map<any, any> = new Map([[String.fromCharCode(110, 95, 49, 54, 95, 112, 102, 114, 97, 109, 101, 0), 464], [String.fromCharCode(100, 95, 52, 56, 95, 115, 121, 110, 116, 104, 101, 115, 105, 122, 101, 114, 0), 834], [String.fromCharCode(102, 95, 52, 56, 95, 116, 114, 105, 0), 418]]);
               let auto_q_b = String.fromCharCode(116, 114, 97, 110, 115, 97, 99, 116, 105, 111, 110, 115, 95, 121, 95, 51, 51, 0);
               actionsQ ^= 3 / (Math.max(5, componentregistryC.size));
               componentregistryC = new Map([[auto_q_b, auto_q_b.length - auto_q_b.length]]);
               if (k_lockC) {
                  break;
               }
            } while ((borderlessm < actionsQ) && k_lockC);
            private_lv3 -= parseFloat(`${bdxadsdkP.length}`);
            return (
               <Text
                  key={index}
                  style={{ ...textVariants.body, color: '#FFFFFF', fontSize: 20 }}>
                  {part}
               </Text>
            );
         }
      });
   };

   let u_unlockp = 2.0;
   let libreactnativejni4 = String.fromCharCode(106, 95, 53, 57, 95, 114, 116, 114, 101, 101, 100, 101, 112, 116, 104, 0);
   let wind8 = true;
   let detailF = 3;
   let floatingN = String.fromCharCode(107, 95, 52, 48, 95, 112, 114, 101, 115, 101, 110, 116, 101, 114, 0);
   let homeR = String.fromCharCode(122, 95, 51, 54, 95, 109, 97, 115, 107, 101, 100, 0);
   for (let b = 0; b < 1; b++) {
      u_unlockp *= (parseFloat(`${(wind8 ? 1 : 1)}`));
   }
   if (wind8) {
      let china7: Array<any> = [String.fromCharCode(113, 95, 52, 49, 95, 114, 101, 118, 105, 101, 119, 0), String.fromCharCode(112, 101, 114, 102, 111, 114, 109, 105, 110, 103, 95, 122, 95, 50, 57, 0), String.fromCharCode(106, 95, 56, 53, 95, 109, 97, 99, 101, 0)];
      let fastforwardA = String.fromCharCode(107, 95, 52, 53, 95, 111, 118, 101, 114, 114, 105, 100, 101, 0);
      let share1 = 1.0;
      let react8: Array<any> = [String.fromCharCode(112, 97, 115, 116, 101, 108, 0), String.fromCharCode(99, 104, 97, 112, 116, 101, 114, 95, 97, 95, 52, 56, 0), String.fromCharCode(102, 95, 53, 49, 95, 99, 97, 100, 101, 110, 99, 101, 0)];
      detailF &= react8.length ^ 3;
      china7.push((String.fromCharCode(118, 0) == fastforwardA ? china7.length : fastforwardA.length));
      share1 += parseFloat(`${fastforwardA.length}`);
      react8.push(fastforwardA.length);
   }
   libreactnativejni4 += `${parseInt(`${u_unlockp}`)}`;
   let crossy = libreactnativejni4.length >= 6948491;
   do {
      let sound4: Map<any, any> = new Map([[String.fromCharCode(104, 95, 51, 51, 95, 111, 112, 116, 0), 253], [String.fromCharCode(106, 95, 56, 56, 95, 104, 105, 103, 104, 108, 105, 103, 104, 116, 101, 114, 0), 416]]);
      let t_unlockC = 3.0;
      let applev = 4;
      libreactnativejni4 += `${3 % (Math.max(10, parseInt(`${t_unlockC}`)))}`;
      sound4.set(`${applev}`, applev / 1);
      t_unlockC *= sound4.size + 3;
      if (crossy) {
         break;
      }
   } while (crossy && (!libreactnativejni4.endsWith(`${wind8}`)));
   let valuesA = 3.0;
   if (floatingN != String.fromCharCode(100, 0)) {
      homeR = "3";
   }
   let annerO = 8340917.0 >= u_unlockp;
   do {
      u_unlockp -= parseFloat(`${libreactnativejni4.length * floatingN.length}`);
      if (annerO) {
         break;
      }
   } while (((3.65 - u_unlockp) <= 4.96 && (u_unlockp - parseFloat(`${libreactnativejni4.length}`)) <= 3.65) && annerO);
   private_lv3 /= Math.max(parseFloat(`${2 << (Math.min(3, runtimeschedulerj.length))}`), 2);
   for (let z = 0; z < 3; z++) {
      let servicen = 3.0;
      let typingz = 3;
      let networkR = 2.0;
      let libffmpegkitM = String.fromCharCode(112, 95, 56, 95, 100, 101, 108, 115, 117, 112, 101, 114, 0);
      let greeng: Array<any> = [410, 596, 359];
      let photoe: Array<any> = [String.fromCharCode(122, 95, 49, 51, 95, 116, 105, 109, 101, 105, 110, 102, 111, 114, 99, 101, 0), String.fromCharCode(102, 114, 97, 109, 101, 115, 105, 122, 101, 115, 95, 51, 95, 49, 53, 0), String.fromCharCode(122, 95, 56, 54, 95, 97, 114, 101, 115, 0)];
      let changen: Array<any> = [937, 502];
      while (4 == (typingz >> (Math.min(Math.abs(3), 4))) && (servicen * parseFloat(`${typingz}`)) == 5.11) {
         typingz %= Math.max(greeng.length, 1);
         break;
      }
      while (1 < photoe.length) {
         let condensedB = String.fromCharCode(116, 101, 115, 116, 111, 114, 105, 103, 95, 118, 95, 49, 51, 0);
         let configure_ = String.fromCharCode(97, 117, 116, 111, 99, 108, 101, 97, 114, 95, 110, 95, 51, 48, 0);
         changen = [parseInt(`${servicen}`)];
         condensedB = `${configure_.length | condensedB.length}`;
         configure_ = `${1 - condensedB.length}`;
         break;
      }
      typingz >>= Math.min(Math.abs(typingz << (Math.min(Math.abs(parseInt(`${servicen}`)), 5))), 2);
      typingz %= Math.max(1, 3 - greeng.length);
      photoe.push(parseInt(`${servicen}`) | 2);
      let index2 = 4923243 <= changen.length;
      do {
         let photoB = String.fromCharCode(114, 101, 113, 117, 101, 115, 116, 105, 110, 103, 95, 115, 95, 57, 50, 0);
         let close6 = 5.0;
         changen.push(parseInt(`${servicen}`) >> (Math.min(Math.abs(parseInt(`${close6}`)), 5)));
         photoB = `${photoB.length}`;
         close6 -= 1;
         if (index2) {
            break;
         }
      } while (index2 && (1 <= (libffmpegkitM.length - 3) || (3 - libffmpegkitM.length) <= 5));
      if (3 == (1 << (Math.min(2, photoe.length)))) {
         photoe.push(photoe.length - changen.length);
      }
      networkR -= parseFloat(`${1}`);
      let uimanager2 = String.fromCharCode(98, 95, 50, 56, 95, 112, 104, 114, 97, 115, 101, 0);
      let listR = String.fromCharCode(117, 115, 114, 99, 95, 115, 95, 52, 56, 0);
      typingz <<= Math.min(greeng.length, 1);
      let anytimeO = String.fromCharCode(98, 95, 50, 50, 95, 108, 105, 98, 115, 0);
      let halfh = String.fromCharCode(104, 121, 115, 116, 101, 114, 101, 115, 105, 115, 95, 116, 95, 49, 54, 0);
      uimanager2 = `${typingz >> (Math.min(libffmpegkitM.length, 2))}`;
      anytimeO = `${anytimeO.length}`;
      halfh = `${(String.fromCharCode(84, 0) == anytimeO ? halfh.length : anytimeO.length)}`;
      while (typingz == libffmpegkitM.length) {
         typingz -= 3;
         break;
      }
      let roboto6: Map<any, any> = new Map([[String.fromCharCode(99, 115, 104, 97, 114, 112, 95, 57, 95, 56, 52, 0), false], [String.fromCharCode(114, 101, 106, 101, 99, 116, 105, 111, 110, 95, 97, 95, 57, 48, 0), false]]);
      let line5 = 3.0;
      let streamingf = String.fromCharCode(107, 95, 49, 48, 48, 95, 109, 116, 105, 109, 101, 0);
      let incidentc: Map<any, any> = new Map([[String.fromCharCode(114, 95, 50, 50, 95, 114, 101, 97, 108, 116, 105, 109, 101, 0), true], [String.fromCharCode(118, 101, 114, 105, 102, 121, 105, 110, 103, 95, 56, 95, 50, 49, 0), false], [String.fromCharCode(116, 95, 49, 95, 99, 102, 104, 100, 0), true]]);
      photoe = [2];
      line5 /= Math.max(1, (streamingf == String.fromCharCode(79, 0) ? streamingf.length : parseInt(`${line5}`)));
      incidentc = new Map([[`${incidentc.size}`, 1]]);
      sortC += `${2 + typingz}`;
   }
   return (
      <>
         <View
            style={{
               paddingTop: 10,
               paddingBottom: 15,
               flexDirection: 'column',
               justifyContent: 'center',
               alignItems: 'center',
               gap: 9,
            }}>
            <Text style={{ fontSize: 22, color: colors.yellow, fontWeight: '700' }}>
               双赢奖励
            </Text>

            <Text>{highlightText('每邀请1位好友 增加30天VIP', '1', '30')}</Text>

            <Text>{highlightText('好友获得30天VIP', '30')}</Text>
         </View>
      </>
   );
}

const styles = StyleSheet.create({});
