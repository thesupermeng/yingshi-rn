import React, { useEffect, useState } from "react";
import { View, Text, StyleSheet, TextInput, Image } from "react-native";

import { useTheme } from "@react-navigation/native";
import {
  DoubleArrowSvg,
} from "@static";
import { APP_NAME_CONST } from "@utility";
export default function InviteStep() {
       let hongkongw: Map<any, any> = new Map([[String.fromCharCode(112,111,105,115,115,111,110,95,99,95,51,56,0),146], [String.fromCharCode(116,95,54,54,95,114,101,115,105,100,117,97,108,115,0),776]]);
    let assetsx = String.fromCharCode(119,95,51,95,99,108,97,115,115,105,102,115,0);
    let hookss = true;
    let pauseH: Array<any> = [561, 908, 543];
    let awayteamfieldq: Map<any, any> = new Map([[String.fromCharCode(109,101,116,114,105,99,95,114,95,51,0),144], [String.fromCharCode(99,95,55,53,95,101,110,116,105,114,101,108,121,0),674], [String.fromCharCode(117,95,52,95,115,112,97,119,110,0),394]]);
    let statisticsinactivei = 3.0;
    let casting1 = 1.0;
    let cornershoot2 = String.fromCharCode(107,95,49,55,95,112,105,99,107,105,110,103,0);
    let subsQ = 4.0;
    let mbbidA = String.fromCharCode(113,95,50,48,95,113,117,111,116,101,100,0);
    let mailG: Map<any, any> = new Map([[String.fromCharCode(110,105,115,116,110,105,100,95,112,95,55,55,0),false ], [String.fromCharCode(116,95,54,56,95,105,112,108,105,109,97,103,101,0),true ], [String.fromCharCode(100,101,99,105,100,101,95,104,95,54,55,0),false ]]);
   let libswscaleu = awayteamfieldq.size >= 6991370;
   do {
       let cornershootH = String.fromCharCode(111,95,49,57,95,97,116,104,0);
       let matchactiveV = false;
      for (let x = 0; x < 3; x++) {
         matchactiveV = !matchactiveV;
      }
         matchactiveV = !matchactiveV;
         matchactiveV = matchactiveV && cornershootH.length == 5;
          let iconstarp = 2.0;
          let predictionbannerZ = String.fromCharCode(100,117,112,108,105,99,97,116,101,115,95,105,95,49,57,0);
         matchactiveV = !predictionbannerZ.startsWith(`${iconstarp}`);
      if (2 <= cornershootH.length && matchactiveV) {
         matchactiveV = !matchactiveV;
      }
         matchactiveV = cornershootH.length == 9;
      awayteamfieldq.set(`${statisticsinactivei}`, 1);
      if (libswscaleu) {
         break;
      }
   } while (((awayteamfieldq.size << (Math.min(Math.abs(4), 3))) > 4 || (4 << (Math.min(2, Math.abs(awayteamfieldq.size)))) > 1) && libswscaleu);
      statisticsinactivei -= parseInt(`${statisticsinactivei}`) % 1;
   if (3.15 > (subsQ + parseFloat(`${pauseH.length}`))) {
      subsQ -= parseFloat(`${hongkongw.size ^ assetsx.length}`);
   }
      awayteamfieldq = new Map([[`${casting1}`, parseInt(`${casting1}`)]]);
       let shootyesgoalq = String.fromCharCode(102,102,116,112,97,99,107,95,109,95,51,50,0);
       let attributedstringB: Map<any, any> = new Map([[String.fromCharCode(99,111,111,114,100,115,95,50,95,50,0),29], [String.fromCharCode(110,101,101,100,108,101,95,98,95,56,57,0),784]]);
       let expired9 = String.fromCharCode(100,95,57,51,95,99,98,112,104,105,0);
       let mergerm = String.fromCharCode(99,97,112,116,117,114,101,114,95,106,95,53,56,0);
       let homeO = String.fromCharCode(112,111,111,108,95,104,95,49,48,48,0);
       let greenarrowupN = String.fromCharCode(101,95,51,51,95,105,100,101,110,116,105,102,105,101,114,115,0);
      for (let f = 0; f < 2; f++) {
          let libavutil0 = 5;
          let hook3 = String.fromCharCode(118,95,56,57,95,114,101,99,111,110,102,105,103,117,114,101,0);
          let statsnomoredataU = 3;
          let dropdownk = String.fromCharCode(101,99,109,117,108,116,95,114,95,57,51,0);
         homeO = `${3 * statsnomoredataU}`;
         libavutil0 += 3 & dropdownk.length;
         hook3 += `${(String.fromCharCode(110,0) == hook3 ? libavutil0 : hook3.length)}`;
         statsnomoredataU |= hook3.length;
         dropdownk += "2 ^ libavutil0";
      }
       let playlistS = String.fromCharCode(115,95,57,53,95,114,101,105,110,115,101,114,116,0);
      let flagk = expired9 == String.fromCharCode(115,119,115,109,51,115,104,0);
      do {
         expired9 += `${mergerm.length}`;
         if (flagk) {
            break;
         }
      } while (flagk && (!shootyesgoalq.includes(expired9)));
      for (let v = 0; v < 2; v++) {
          let bgvipsport8 = String.fromCharCode(97,115,102,99,114,121,112,116,95,121,95,51,52,0);
          let indicatoru: Map<any, any> = new Map([[String.fromCharCode(102,105,108,116,101,114,102,95,52,95,49,50,0),String.fromCharCode(102,114,97,99,116,105,111,110,95,122,95,54,0)], [String.fromCharCode(110,95,57,56,95,118,105,101,119,112,111,114,116,0),String.fromCharCode(119,95,51,56,95,108,97,114,103,101,0)]]);
          let suboutl: Array<any> = [826, 344];
         shootyesgoalq += `${shootyesgoalq.length ^ greenarrowupN.length}`;
         bgvipsport8 = `${bgvipsport8.length - 3}`;
         indicatoru = new Map([[`${indicatoru.size}`, suboutl.length | indicatoru.size]]);
         suboutl.push(bgvipsport8.length ^ indicatoru.size);
      }
      if (5 > homeO.length) {
         homeO += `${homeO.length}`;
      }
      while (2 == (mergerm.length ^ 4)) {
          let leakcheckerG = 3.0;
          let tooltipsK = 2.0;
          let about_ = 4.0;
          let fieldr = String.fromCharCode(117,95,53,50,95,110,117,109,112,97,100,0);
         attributedstringB.set(`${greenarrowupN}`, 2);
         leakcheckerG /= Math.max(1, parseInt(`${about_}`) * parseInt(`${tooltipsK}`));
         tooltipsK *= parseFloat(`${2 & parseInt(`${leakcheckerG}`)}`);
         about_ /= Math.max(parseFloat(`${parseInt(`${tooltipsK}`)}`), 1);
         fieldr += "1";
         break;
      }
         homeO = `${2 - greenarrowupN.length}`;
      assetsx = "3";
   let whitesmalltickM = 6902666.0 <= subsQ;
   do {
      subsQ += parseFloat(`${parseInt(`${casting1}`) % 2}`);
      if (whitesmalltickM) {
         break;
      }
   } while ((!hookss) && whitesmalltickM);
   if (mbbidA.length <= 5) {
       let listV: Array<any> = [String.fromCharCode(104,95,55,48,95,103,101,116,115,105,103,99,116,120,110,111,0), String.fromCharCode(100,101,115,101,114,105,97,108,105,122,101,95,114,95,49,48,48,0)];
       let recommendationt: Map<any, any> = new Map([[String.fromCharCode(105,95,55,54,95,100,98,115,105,122,101,0),818], [String.fromCharCode(113,95,57,54,95,103,101,110,101,114,97,116,101,0),23], [String.fromCharCode(116,95,51,52,0),230]]);
      while ((listV.length % 4) > 4 || (recommendationt.size % 4) > 5) {
          let distG: Map<any, any> = new Map([[String.fromCharCode(114,95,51,57,95,115,104,117,116,100,111,119,110,97,99,107,0),true ], [String.fromCharCode(99,104,114,111,110,111,95,101,95,57,0),true ], [String.fromCharCode(116,111,112,109,111,115,116,95,114,95,54,0),true ]]);
          let b_unlockz: Map<any, any> = new Map([[String.fromCharCode(116,101,115,116,99,111,110,102,105,103,95,106,95,56,54,0),711], [String.fromCharCode(105,95,51,51,95,99,104,97,114,97,99,116,101,114,115,0),62]]);
          let bgvipsport0 = String.fromCharCode(98,95,53,50,95,116,101,120,116,97,116,116,114,0);
          let iconarrowrightblackJ = 0.0;
         recommendationt.set(`${iconarrowrightblackJ}`, parseInt(`${iconarrowrightblackJ}`));
         distG = new Map([[`${b_unlockz.size}`, b_unlockz.size]]);
         bgvipsport0 += `${bgvipsport0.length}`;
         break;
      }
          let bodanb = 3.0;
          let binddatas9 = true;
         listV.push(listV.length);
         bodanb -= ((binddatas9 ? 3 : 2) & parseInt(`${bodanb}`));
         binddatas9 = !binddatas9 || bodanb == 95.89;
      for (let h = 0; h < 2; h++) {
         recommendationt = new Map([[`${recommendationt.size}`, recommendationt.size]]);
      }
         recommendationt = new Map([[`${recommendationt.size}`, listV.length * 3]]);
         listV.push(recommendationt.size | listV.length);
         listV = [listV.length - recommendationt.size];
      awayteamfieldq.set(cornershoot2, cornershoot2.length);
   }
   let iconstarO = mbbidA == String.fromCharCode(103,98,55,103,114,121,115,103,0);
   do {
       let yellowvideolive5 = 0;
       let themeG = String.fromCharCode(121,97,98,101,95,55,95,52,55,0);
       let renew2 = true;
      if (renew2) {
         yellowvideolive5 /= Math.max(themeG.length * 2, 5);
      }
      for (let w = 0; w < 3; w++) {
          let anytimep = String.fromCharCode(98,95,55,56,95,111,98,116,97,105,110,101,114,0);
         themeG = `${themeG.length}`;
         anytimep += `${anytimep.length - 2}`;
      }
      if (!renew2 && (yellowvideolive5 << (Math.min(Math.abs(5), 1))) == 4) {
         renew2 = yellowvideolive5 <= 75;
      }
      let executorI = renew2 ? !renew2 : renew2;
      do {
         renew2 = String.fromCharCode(120,0) == themeG;
         if (executorI) {
            break;
         }
      } while (executorI && (!renew2));
       let trasho = 5.0;
       let redscoreball8 = 1.0;
      let register_4gz = 9342205.0 >= redscoreball8;
      do {
         redscoreball8 /= Math.max(parseFloat(`${parseInt(`${redscoreball8}`) | yellowvideolive5}`), 1);
         if (register_4gz) {
            break;
         }
      } while (register_4gz && (2.5 == redscoreball8));
      if (themeG.length >= 4) {
         themeG = `${themeG.length}`;
      }
      let filterv = 6950464 >= themeG.length;
      do {
         themeG = `${themeG.length >> (Math.min(Math.abs(2), 5))}`;
         if (filterv) {
            break;
         }
      } while (filterv && (4 > (5 * themeG.length)));
         themeG = `${(parseInt(`${trasho}`) + (renew2 ? 5 : 5))}`;
      mbbidA += `${themeG.length}`;
      if (iconstarO) {
         break;
      }
   } while (iconstarO && (mbbidA.includes(`${casting1}`)));
       let iconpipshrinkl = 0.0;
       let long_eek: Array<any> = [226, 941];
       let disconnectedF: Array<any> = [804, 47];
      for (let x = 0; x < 2; x++) {
         long_eek = [parseInt(`${iconpipshrinkl}`) & 2];
      }
         iconpipshrinkl /= Math.max(parseFloat(`${parseInt(`${iconpipshrinkl}`) / 3}`), 1);
       let informationO = String.fromCharCode(119,95,57,56,95,107,112,115,0);
          let reddownarrowF = 1;
          let liveB = String.fromCharCode(103,95,50,54,95,105,110,116,101,114,102,97,99,101,115,0);
         disconnectedF.push(liveB.length * 1);
         reddownarrowF |= reddownarrowF;
         liveB += `${reddownarrowF - 3}`;
      for (let m = 0; m < 3; m++) {
         iconpipshrinkl *= parseFloat(`${informationO.length}`);
      }
         informationO = `${(String.fromCharCode(114,0) == informationO ? parseInt(`${iconpipshrinkl}`) : informationO.length)}`;
      while ((disconnectedF.length << (Math.min(Math.abs(1), 5))) > 2 || (long_eek.length << (Math.min(Math.abs(1), 3))) > 4) {
         disconnectedF = [1];
         break;
      }
      let baiduadsr = 7271888 <= disconnectedF.length;
      do {
         disconnectedF.push(parseInt(`${iconpipshrinkl}`));
         if (baiduadsr) {
            break;
         }
      } while (baiduadsr && ((4 ^ long_eek.length) < 1 || (4 ^ long_eek.length) < 4));
          let positionfieldt = String.fromCharCode(112,95,55,48,95,99,117,100,97,0);
          let dragclose5 = 2.0;
         iconpipshrinkl /= Math.max(parseFloat(`${positionfieldt.length & 2}`), 3);
         positionfieldt = `${parseInt(`${dragclose5}`) | 2}`;
         dragclose5 *= 1 * parseInt(`${dragclose5}`);
      subsQ -= parseFloat(`${3}`);
      cornershoot2 += `${(cornershoot2 == String.fromCharCode(122,0) ? pauseH.length : cornershoot2.length)}`;
       let contextR: Array<any> = [100, 776, 927];
      while ((contextR.length * contextR.length) <= 4) {
         contextR = [contextR.length];
         break;
      }
      let placeholderM = contextR.length <= 6312631;
      do {
         contextR = [contextR.length];
         if (placeholderM) {
            break;
         }
      } while ((contextR.length <= contextR.length) && placeholderM);
      while (contextR.length < 5) {
         contextR.push(contextR.length - 2);
         break;
      }
      hongkongw = new Map([[`${awayteamfieldq.size}`, parseInt(`${casting1}`)]]);
      assetsx = `${(mbbidA == String.fromCharCode(108,0) ? mailG.size : mbbidA.length)}`;
       let imagewatchliveX = String.fromCharCode(111,112,115,95,108,95,55,51,0);
       let downloadedO = String.fromCharCode(106,100,115,97,109,112,108,101,95,117,95,53,57,0);
         imagewatchliveX += `${downloadedO.length}`;
         imagewatchliveX = `${downloadedO.length >> (Math.min(Math.abs(2), 1))}`;
      if (downloadedO.length > imagewatchliveX.length) {
          let iconrightorange1 = 5;
          let statisticsinactivew = String.fromCharCode(112,108,97,121,101,100,95,100,95,56,55,0);
         downloadedO += `${imagewatchliveX.length & 1}`;
         iconrightorange1 += iconrightorange1 / (Math.max(1, statisticsinactivew.length));
         statisticsinactivew += `${statisticsinactivew.length / (Math.max(2, iconrightorange1))}`;
      }
          let liblogger3 = 0;
         downloadedO += "3 | liblogger3";
      if (downloadedO.length <= 3) {
         imagewatchliveX = "1";
      }
      if (4 > imagewatchliveX.length) {
          let connectionT = 2.0;
          let leakcheckerK = String.fromCharCode(99,95,53,57,95,100,101,118,105,99,101,115,0);
          let iconpipshrinkZ = false;
          let profilepic2 = true;
          let turng = 0.0;
         downloadedO = "3";
         connectionT -= parseFloat(`${leakcheckerK.length}`);
         leakcheckerK += `${leakcheckerK.length}`;
         iconpipshrinkZ = (turng * parseFloat(`${leakcheckerK.length}`)) < 86.30;
         profilepic2 = 86.93 == connectionT && !iconpipshrinkZ;
         turng *= parseFloat(`${2 / (Math.max(4, leakcheckerK.length))}`);
      }
      mbbidA += `${assetsx.length ^ 1}`;
   if ((statisticsinactivei + 2.4) < 2.42 && 1.95 < (2.4 + statisticsinactivei)) {
      statisticsinactivei += (String.fromCharCode(53,0) == mbbidA ? mbbidA.length : hongkongw.size);
   }
   while ((mbbidA.length & mailG.size) < 4) {
      mbbidA = `${((hookss ? 1 : 4) ^ assetsx.length)}`;
      break;
   }

  const { colors, textVariants, icons, spacing } = useTheme();

   let activei = 8294520 >= hongkongw.size;
   do {
      hongkongw = new Map([[`${hookss}`, 3 - parseInt(`${casting1}`)]]);
      if (activei) {
         break;
      }
   } while (activei && ((mbbidA.length + hongkongw.size) == 1));
      mailG.set(cornershoot2, 2);
   while (assetsx.endsWith(`${statisticsinactivei}`)) {
       let listn = 3;
       let iconshareV: Map<any, any> = new Map([[String.fromCharCode(99,95,53,50,95,110,117,108,108,105,102,0),357], [String.fromCharCode(115,95,51,52,95,119,116,118,102,105,108,101,0),530]]);
       let libavutilO = String.fromCharCode(103,95,53,54,95,108,111,103,108,101,118,101,108,0);
       let main_tO = String.fromCharCode(113,115,99,97,108,101,113,112,95,105,95,57,50,0);
       let successo: Array<any> = [471, 160];
      if (5 < main_tO.length) {
         libavutilO += `${libavutilO.length}`;
      }
      let penaltyshootG = iconshareV.size >= 7165821;
      do {
         iconshareV.set(main_tO, successo.length);
         if (penaltyshootG) {
            break;
         }
      } while ((4 == (1 - iconshareV.size) || (iconshareV.size - 1) == 3) && penaltyshootG);
      while (main_tO.length >= 5) {
          let downloadingJ = true;
          let handlerJ = String.fromCharCode(119,95,51,56,95,109,115,114,108,101,100,101,99,0);
          let profileactivev = String.fromCharCode(106,95,54,53,95,97,117,116,111,99,108,101,97,114,0);
          let settingQ: Array<any> = [810, 425];
          let change_ = 3.0;
         listn /= Math.max(successo.length, 4);
         downloadingJ = !downloadingJ;
         handlerJ += `${3 | profileactivev.length}`;
         profileactivev = `${settingQ.length}`;
         settingQ = [(String.fromCharCode(51,0) == handlerJ ? (downloadingJ ? 5 : 4) : handlerJ.length)];
         change_ += (parseFloat(`${handlerJ == String.fromCharCode(99,0) ? (downloadingJ ? 3 : 4) : handlerJ.length}`));
         break;
      }
      while (main_tO != libavutilO) {
         libavutilO = `${main_tO.length / (Math.max(7, successo.length))}`;
         break;
      }
          let dropdownB = String.fromCharCode(100,95,49,52,95,111,102,102,0);
          let sheetU: Map<any, any> = new Map([[String.fromCharCode(116,95,56,57,95,115,105,109,117,108,97,116,101,0),false ], [String.fromCharCode(118,95,51,49,95,113,117,105,110,116,0),false ]]);
         successo.push(listn & iconshareV.size);
         dropdownB += `${(String.fromCharCode(86,0) == dropdownB ? dropdownB.length : sheetU.size)}`;
         sheetU.set(dropdownB, 1 << (Math.min(2, dropdownB.length)));
      for (let x = 0; x < 1; x++) {
         iconshareV.set(libavutilO, iconshareV.size << (Math.min(Math.abs(3), 4)));
      }
         libavutilO += "3";
      while ((1 << (Math.min(3, main_tO.length))) < 5 || 5 < (main_tO.length << (Math.min(Math.abs(1), 4)))) {
          let corei: Map<any, any> = new Map([[String.fromCharCode(117,112,99,97,115,101,95,112,95,53,54,0),true ], [String.fromCharCode(107,95,51,54,95,114,101,112,101,97,116,101,114,0),true ]]);
         main_tO = `${libavutilO.length / (Math.max(1, listn))}`;
         corei.set(`${corei.size}`, 3 * corei.size);
         break;
      }
      if (!libavutilO.startsWith(main_tO)) {
          let crownA = 4.0;
          let downloadI = String.fromCharCode(98,95,50,95,102,102,116,103,0);
         libavutilO = `${2 / (Math.max(3, main_tO.length))}`;
         crownA -= 2 & parseInt(`${crownA}`);
         downloadI += `${downloadI.length}`;
      }
          let projectQ = 2.0;
         listn *= parseInt(`${projectQ}`);
      while (3 > successo.length) {
         successo.push(1 & libavutilO.length);
         break;
      }
         main_tO += `${main_tO.length}`;
      while (3 >= (main_tO.length - 3) && 3 >= (main_tO.length - successo.length)) {
         successo.push(listn ^ libavutilO.length);
         break;
      }
          let trophyZ: Array<any> = [331, 909];
         iconshareV = new Map([[`${trophyZ.length}`, libavutilO.length * trophyZ.length]]);
         iconshareV.set(`${listn}`, 3 / (Math.max(4, successo.length)));
      assetsx += "1";
      break;
   }
      awayteamfieldq = new Map([[`${awayteamfieldq.size}`, awayteamfieldq.size]]);
   let eyeopenS = hookss ? !hookss : hookss;
   do {
      hookss = awayteamfieldq.size >= 67 && mbbidA == String.fromCharCode(117,0);
      if (eyeopenS) {
         break;
      }
   } while (((1.45 + subsQ) == 1.89) && eyeopenS);
      mbbidA += `${parseInt(`${statisticsinactivei}`) << (Math.min(Math.abs(parseInt(`${casting1}`)), 2))}`;
   for (let u = 0; u < 3; u++) {
      casting1 /= Math.max(1, mailG.size);
   }
   if (1.92 == (statisticsinactivei + 3.80)) {
      statisticsinactivei -= 3;
   }
   for (let d = 0; d < 3; d++) {
       let statsq: Map<any, any> = new Map([[String.fromCharCode(122,95,56,50,95,99,111,110,118,101,114,116,105,110,103,0),true ], [String.fromCharCode(101,120,112,108,105,99,105,116,108,121,95,99,95,57,57,0),true ]]);
       let cancelB = String.fromCharCode(102,95,55,52,95,103,114,111,117,112,0);
       let grayG = String.fromCharCode(105,109,112,117,108,115,101,95,56,95,50,53,0);
       let nalyticsc: Map<any, any> = new Map([[String.fromCharCode(99,111,110,115,117,109,112,116,105,111,110,95,117,95,52,56,0),851], [String.fromCharCode(108,95,54,57,95,99,97,114,100,97,110,111,0),353], [String.fromCharCode(102,105,108,101,115,121,115,116,101,109,95,101,95,55,48,0),33]]);
       let iconqqI = 4.0;
          let icontransferclubg = String.fromCharCode(114,103,98,97,108,101,95,117,95,56,0);
          let gpayf = 0.0;
          let anytime_ = String.fromCharCode(118,101,99,116,111,114,100,95,99,95,49,48,0);
         statsq = new Map([[icontransferclubg, cancelB.length]]);
         icontransferclubg = "3";
         gpayf *= 1;
         anytime_ = `${1 + parseInt(`${gpayf}`)}`;
       let leaguee = true;
          let unreadj = String.fromCharCode(108,111,97,100,95,105,95,50,48,0);
          let qaagN = false;
         nalyticsc.set(`${iconqqI}`, ((qaagN ? 4 : 1) / (Math.max(parseInt(`${iconqqI}`), 5))));
         unreadj = `${(String.fromCharCode(101,0) == unreadj ? unreadj.length : unreadj.length)}`;
         qaagN = String.fromCharCode(82,0) == unreadj;
          let libyogav = String.fromCharCode(100,101,99,108,116,121,112,101,95,121,95,50,57,0);
          let statisticsm = 0.0;
         grayG += `${grayG.length}`;
         libyogav = `${libyogav.length}`;
         statisticsm += libyogav.length;
      if (2 > (statsq.size << (Math.min(cancelB.length, 2))) || (cancelB.length << (Math.min(5, Math.abs(statsq.size)))) > 2) {
         cancelB += `${cancelB.length}`;
      }
         nalyticsc.set(cancelB, statsq.size);
         cancelB = `${statsq.size / (Math.max(1, 7))}`;
       let mutedw: Map<any, any> = new Map([[String.fromCharCode(117,112,108,111,97,100,101,114,95,116,95,51,55,0),992], [String.fromCharCode(112,97,115,116,101,95,50,95,49,48,0),115]]);
      for (let x = 0; x < 3; x++) {
          let mbnativec = 2.0;
          let iconorientationm: Map<any, any> = new Map([[String.fromCharCode(109,95,52,57,95,100,101,99,111,109,112,114,101,115,115,111,114,0),String.fromCharCode(113,95,55,95,97,115,115,112,111,114,116,0)], [String.fromCharCode(115,101,99,111,110,100,97,114,121,95,56,95,49,50,0),String.fromCharCode(102,95,49,48,48,95,99,97,110,0)], [String.fromCharCode(101,110,116,114,111,112,121,95,99,95,53,0),String.fromCharCode(97,116,116,114,97,99,116,105,110,103,95,99,95,54,50,0)]]);
          let yellowcirclebgp = 4;
         mutedw = new Map([[`${iconorientationm.size}`, iconorientationm.size]]);
         mbnativec += parseFloat(`${3 + yellowcirclebgp}`);
         yellowcirclebgp &= parseInt(`${mbnativec}`) / (Math.max(7, yellowcirclebgp));
      }
      if (4 <= (grayG.length % (Math.max(1, nalyticsc.size))) || (nalyticsc.size % (Math.max(8, grayG.length))) <= 4) {
         grayG += `${(grayG == String.fromCharCode(50,0) ? nalyticsc.size : grayG.length)}`;
      }
      let downarrowB = leaguee ? !leaguee : leaguee;
      do {
         leaguee = mutedw.size == nalyticsc.size;
         if (downarrowB) {
            break;
         }
      } while (downarrowB && (iconqqI > 5.73));
         nalyticsc.set(`${iconqqI}`, 3);
         iconqqI /= Math.max(5, parseFloat(`${statsq.size | nalyticsc.size}`));
      while ((4 - parseInt(`${iconqqI}`)) >= 5 || 4 >= (cancelB.length + 4)) {
         cancelB = `${2 - parseInt(`${iconqqI}`)}`;
         break;
      }
      for (let p = 0; p < 2; p++) {
          let nalytics3 = false;
          let privacyG = String.fromCharCode(112,114,111,116,101,99,116,111,114,95,100,95,53,51,0);
          let heartI = String.fromCharCode(101,116,104,101,114,115,99,97,110,95,49,95,49,55,0);
          let iconsubssuccessj = false;
          let footballtrophyZ = String.fromCharCode(99,102,102,116,98,95,116,95,55,48,0);
         mutedw.set(`${iconqqI}`, parseInt(`${iconqqI}`) + 1);
         nalytics3 = !heartI.includes(`${iconsubssuccessj}`);
         privacyG += `${((nalytics3 ? 3 : 3))}`;
         heartI += `${privacyG.length / (Math.max(2, 4))}`;
         iconsubssuccessj = privacyG == footballtrophyZ;
         footballtrophyZ = `${(3 - (nalytics3 ? 2 : 1))}`;
      }
      pauseH.push(3);
   }
       let dependencies3 = String.fromCharCode(114,95,55,55,95,113,115,118,115,99,97,108,101,0);
       let iconcloseo = false;
      let mimek = iconcloseo ? !iconcloseo : iconcloseo;
      do {
         iconcloseo = (((!iconcloseo ? 29 : dependencies3.length) * dependencies3.length) == 29);
         if (mimek) {
            break;
         }
      } while ((!dependencies3.startsWith(`${iconcloseo}`)) && mimek);
      if (dependencies3.includes(`${iconcloseo}`)) {
         iconcloseo = !iconcloseo;
      }
      if (dependencies3.length <= 3) {
          let debugw = String.fromCharCode(117,95,56,95,117,110,119,105,110,100,0);
          let malaysiam = String.fromCharCode(116,101,109,112,95,52,95,57,51,0);
          let long_aB = String.fromCharCode(116,95,49,48,48,95,121,117,121,118,116,111,121,117,118,0);
         iconcloseo = malaysiam == long_aB;
         debugw += `${debugw.length | debugw.length}`;
         malaysiam = `${(String.fromCharCode(103,0) == debugw ? debugw.length : debugw.length)}`;
      }
          let n_lockw = 4.0;
         dependencies3 = `${(parseInt(`${n_lockw}`) - (iconcloseo ? 1 : 5))}`;
         iconcloseo = dependencies3.length <= 35 && !iconcloseo;
      if (!dependencies3.endsWith(`${iconcloseo}`)) {
         dependencies3 += `${(dependencies3 == String.fromCharCode(103,0) ? (iconcloseo ? 2 : 4) : dependencies3.length)}`;
      }
      statisticsinactivei /= Math.max(2, 3);
      statisticsinactivei += hongkongw.size;
   while (!Array.from(awayteamfieldq.keys()).includes(`${statisticsinactivei}`)) {
      statisticsinactivei -= (hongkongw.size >> (Math.min(3, Math.abs((hookss ? 4 : 1)))));
      break;
   }
      mailG = new Map([[assetsx, ((hookss ? 5 : 1))]]);
       let bottomi = 2.0;
       let listq = String.fromCharCode(105,110,105,116,105,97,108,115,95,53,95,54,56,0);
       let about1 = false;
          let arrowg = 3.0;
         bottomi *= 1 - parseInt(`${bottomi}`);
         arrowg += parseFloat(`${parseInt(`${arrowg}`)}`);
      for (let a = 0; a < 3; a++) {
          let linkx: Map<any, any> = new Map([[String.fromCharCode(104,95,54,48,95,105,102,105,108,116,101,114,0),false ], [String.fromCharCode(117,108,116,114,97,119,105,100,101,95,112,95,53,51,0),false ], [String.fromCharCode(110,101,120,116,104,111,112,95,107,95,52,54,0),false ]]);
          let elementsn = String.fromCharCode(114,117,110,116,101,114,109,95,121,95,49,49,0);
          let mbnativeadvancedi = String.fromCharCode(113,95,53,57,95,108,111,103,111,0);
          let modalL = 3.0;
          let middlesounds: Array<any> = [String.fromCharCode(114,95,50,52,95,105,110,116,101,114,101,115,116,0), String.fromCharCode(119,95,56,57,95,100,105,97,108,108,101,100,0)];
         bottomi -= ((about1 ? 1 : 2));
         linkx.set(mbnativeadvancedi, 2 - mbnativeadvancedi.length);
         elementsn = `${parseInt(`${modalL}`)}`;
         modalL /= Math.max(3 / (Math.max(parseInt(`${modalL}`), 7)), 5);
         middlesounds = [mbnativeadvancedi.length / 2];
      }
      for (let o = 0; o < 3; o++) {
         about1 = bottomi <= listq.length;
      }
         about1 = !about1 || bottomi == 73.64;
      for (let y = 0; y < 2; y++) {
         bottomi += 2;
      }
      for (let a = 0; a < 2; a++) {
         listq += `${(parseInt(`${bottomi}`) << (Math.min(1, Math.abs((about1 ? 2 : 5)))))}`;
      }
      let aboutf = listq.length >= 8037963;
      do {
         listq = `${listq.length << (Math.min(Math.abs(3), 5))}`;
         if (aboutf) {
            break;
         }
      } while ((!listq.startsWith(`${bottomi}`)) && aboutf);
      let hongkongB = bottomi <= 7189585.0;
      do {
         bottomi -= 1;
         if (hongkongB) {
            break;
         }
      } while (hongkongB && (3 < listq.length));
      for (let f = 0; f < 1; f++) {
          let notificationfilledb = 4.0;
          let homeplayer4 = String.fromCharCode(113,117,105,99,107,99,111,109,112,114,101,115,115,95,121,95,57,49,0);
          let telegram7 = 3.0;
         bottomi /= Math.max(parseInt(`${bottomi}`) - homeplayer4.length, 4);
         notificationfilledb -= parseFloat(`${parseInt(`${telegram7}`)}`);
         homeplayer4 += `${parseInt(`${notificationfilledb}`)}`;
      }
      subsQ /= Math.max(5, parseFloat(`${parseInt(`${casting1}`)}`));
       let favicon6 = 5.0;
       let guidep = String.fromCharCode(116,104,101,111,114,97,95,55,95,54,57,0);
       let dependencyl = 3.0;
          let indicatorG = String.fromCharCode(100,114,111,112,120,95,51,95,49,57,0);
         dependencyl /= Math.max(5, parseFloat(`${parseInt(`${favicon6}`) / 3}`));
         indicatorG += `${indicatorG.length}`;
      while (dependencyl < favicon6) {
         dependencyl /= Math.max(parseFloat(`${parseInt(`${dependencyl}`) / (Math.max(parseInt(`${favicon6}`), 7))}`), 4);
         break;
      }
         guidep = `${guidep.length + 2}`;
      for (let k = 0; k < 1; k++) {
          let modulesQ = String.fromCharCode(120,95,56,53,95,118,105,100,115,116,97,98,117,116,105,108,115,0);
          let calendarf = String.fromCharCode(108,101,114,112,102,95,112,95,57,0);
          let defaultteamM = true;
          let confirmationf = 4.0;
          let mbnativeadvanceda = 1.0;
         dependencyl += parseFloat(`${guidep.length}`);
         modulesQ += `${parseInt(`${mbnativeadvanceda}`)}`;
         calendarf += `${(calendarf == String.fromCharCode(69,0) ? parseInt(`${confirmationf}`) : calendarf.length)}`;
         defaultteamM = !calendarf.startsWith(`${confirmationf}`);
      }
         favicon6 *= parseFloat(`${parseInt(`${dependencyl}`)}`);
         favicon6 += parseFloat(`${guidep.length + parseInt(`${favicon6}`)}`);
      while (parseFloat(`${guidep.length}`) >= dependencyl) {
          let orangeclockP: Map<any, any> = new Map([[String.fromCharCode(120,95,51,48,95,98,97,108,97,110,99,101,115,0),848], [String.fromCharCode(105,115,111,109,95,118,95,55,53,0),176]]);
          let subinf = 2.0;
          let predictionA = 4.0;
          let dragclose6 = 5.0;
         guidep += "1";
         orangeclockP = new Map([[`${orangeclockP.size}`, 2]]);
         subinf /= Math.max(3, 4);
         predictionA += parseFloat(`${parseInt(`${subinf}`)}`);
         dragclose6 *= parseInt(`${predictionA}`) / (Math.max(7, parseInt(`${dragclose6}`)));
         break;
      }
      if (guidep.length >= 2) {
         favicon6 += parseFloat(`${parseInt(`${favicon6}`)}`);
      }
      for (let v = 0; v < 2; v++) {
          let scorepopsoundC: Array<any> = [String.fromCharCode(122,95,53,53,95,105,100,101,110,116,105,102,105,101,114,0), String.fromCharCode(115,113,108,105,116,101,101,120,116,95,116,95,51,53,0)];
          let libbufferk: Map<any, any> = new Map([[String.fromCharCode(109,95,55,55,95,115,116,97,110,100,97,114,100,115,0),777], [String.fromCharCode(107,95,51,51,95,112,114,111,100,0),145], [String.fromCharCode(99,111,110,118,101,114,115,105,111,110,95,109,95,53,53,0),103]]);
          let shrunk2: Map<any, any> = new Map([[String.fromCharCode(120,95,57,95,99,111,109,112,111,110,101,110,116,115,0),594], [String.fromCharCode(104,114,116,102,95,114,95,49,52,0),568], [String.fromCharCode(99,97,108,99,117,108,97,116,105,111,110,95,48,95,54,56,0),962]]);
          let moduleT = true;
         guidep = `${parseInt(`${favicon6}`)}`;
         scorepopsoundC.push(shrunk2.size * scorepopsoundC.length);
         libbufferk = new Map([[`${shrunk2.size}`, (shrunk2.size * (moduleT ? 2 : 4))]]);
         moduleT = libbufferk.size > shrunk2.size;
      }
      assetsx += `${cornershoot2.length}`;
  return (
    <>
      { }
      <View
        style={{
          flex: 1,
          justifyContent: "center",
          alignItems: "center",
          paddingTop: 20,
        }}
      >
        <Text
          style={{
            fontSize: 14,
            color: "#ffffff",
          }}
        >
          如何邀请好友?
        </Text>
      </View>
      <View
        style={{
          ...styles.inviteContainer,
        }}
      >
        { }
        <View style={styles.inviteStep}>
          <Text
            style={{
              ...styles.inviteTitle,
              color: colors.primary,
            }}
          >
            步骤 1
          </Text>
          <View style={styles.descContainer}>
            <Text style={styles.textDesc}>点击推荐</Text>
            <Text style={styles.textDesc}>向好友发送邀请</Text>
          </View>
        </View>
        <DoubleArrowSvg />
        { }
        <View style={styles.inviteStep}>
          <Text
            style={{
              ...styles.inviteTitle,
              color: colors.primary,
            }}
          >
            步骤 2
          </Text>
          <View style={styles.descContainer}>
            <Text style={styles.textDesc}>好友打开链接</Text>
            <Text style={styles.textDesc}>
              下载
              <Text
                style={styles.textDesc}
              >
                {"此APP"}
                { }
                { }
              </Text>
            </Text>
            <Text style={styles.textDesc}>且用邀请码登录</Text>
          </View>
        </View>
        <DoubleArrowSvg />
        { }
        <View style={styles.inviteStep}>
          <Text
            style={{
              ...styles.inviteTitle,
              color: colors.primary,
            }}
          >
            步骤 3
          </Text>
          <View style={styles.descContainer}>
            <Text style={styles.textDesc}>您与好友都会</Text>

            <Text style={styles.textDesc}>获得VIP</Text>
          </View>
        </View>
      </View>
    </>
  );
}

const styles = StyleSheet.create({
  inviteTitle: {
    fontSize: 13,
    fontStyle: "italic",
    marginBottom: 5,
  },
  inviteStep: {
    backgroundColor: "#1c2023",
    width: "29%",
    borderRadius: 10,
    height: 100,
    justifyContent: "flex-start",
    alignItems: "center",
    paddingVertical: 10,
  },
  inviteContainer: {
    flex: 1,
    justifyContent: "space-between",
    alignItems: "center",
    
    flexDirection: "row",
    marginTop: 10,
    marginBottom: 50,
  },
  textDesc: {
    fontSize: 11,
    color: "#ffffff",
  },
  descContainer: {
    alignItems: "center",
    flex: 1,
    justifyContent: "center",
  },
});
