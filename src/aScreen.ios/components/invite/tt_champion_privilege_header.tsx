import React, { useEffect, useState } from 'react';
import { View, Text, StyleSheet, TextInput, Image } from 'react-native';

import { useTheme } from '@react-navigation/native';
import ArrowIcn from '@static/images/invite/downBorderless.svg';
export default function InviteStep() {
       let skipc = 0.0;
    let o_positionp = 3.0;
    let playM = 1.0;
    let holderN: Map<any, any> = new Map([[String.fromCharCode(113,95,55,49,95,111,102,102,115,101,116,0),false ], [String.fromCharCode(101,97,115,105,110,103,95,116,95,52,52,0),false ], [String.fromCharCode(102,108,97,99,100,115,112,95,51,95,52,52,0),false ]]);
    let materialt = String.fromCharCode(111,95,53,95,99,104,101,99,107,101,114,0);
    let mutedM = String.fromCharCode(99,116,114,108,95,53,95,49,50,0);
    let rightE = String.fromCharCode(112,101,115,113,95,114,95,56,55,0);
    let memberO = 0;
    let expandJ = String.fromCharCode(114,101,116,117,114,110,95,108,95,49,48,48,0);
    let whatsappu: Map<any, any> = new Map([[String.fromCharCode(114,101,99,105,112,114,111,99,97,108,95,117,95,51,48,0),482], [String.fromCharCode(97,95,57,57,95,117,115,101,114,115,0),42]]);
    let favoriteY: Array<any> = [String.fromCharCode(104,95,49,55,95,112,111,119,101,114,0), String.fromCharCode(116,117,114,98,111,106,112,101,103,95,109,95,54,53,0)];
    let feedbackZ = String.fromCharCode(115,109,112,116,101,98,97,114,115,95,113,95,53,48,0);
    let storeA = String.fromCharCode(120,95,55,53,95,119,111,114,107,115,0);
    let t_centerf = 3.0;
    let bellv = String.fromCharCode(105,95,51,54,95,116,114,97,99,107,105,110,103,0);
    let fillX = String.fromCharCode(108,109,108,109,95,97,95,57,48,0);
    let temperatureD = 5.0;
    let downloadingK = String.fromCharCode(115,101,114,118,101,114,115,95,50,95,50,57,0);
    let launcher8 = false;
   if ((rightE.length - parseInt(`${skipc}`)) > 4 || 4.24 > (4.75 - skipc)) {
       let activeE = String.fromCharCode(103,95,49,52,95,116,114,97,110,115,102,111,114,109,0);
       let downloadingX = 5.0;
       let aboutI = String.fromCharCode(108,95,51,48,95,108,111,99,97,108,104,111,115,116,0);
       let flyerc: Map<any, any> = new Map([[String.fromCharCode(100,101,98,117,103,98,111,120,95,108,95,57,49,0),723], [String.fromCharCode(119,95,51,57,95,109,117,108,116,105,101,110,100,0),19], [String.fromCharCode(121,95,55,53,95,103,101,110,101,114,97,116,101,0),331]]);
      for (let q = 0; q < 3; q++) {
         downloadingX /= Math.max(2, (String.fromCharCode(88,0) == aboutI ? aboutI.length : flyerc.size));
      }
      let gmailK = 9578549 >= aboutI.length;
      do {
         aboutI += `${(activeE == String.fromCharCode(86,0) ? parseInt(`${downloadingX}`) : activeE.length)}`;
         if (gmailK) {
            break;
         }
      } while (gmailK && (4.29 == downloadingX));
      while ((2 / (Math.max(1, parseInt(`${downloadingX}`)))) < 1 && (downloadingX / (Math.max(5.23, 5))) < 3.0) {
          let station3 = 2.0;
          let weiboY = 4.0;
          let roundF: Map<any, any> = new Map([[String.fromCharCode(104,95,57,52,95,112,101,114,109,105,115,115,105,111,110,115,0),141], [String.fromCharCode(109,121,114,110,100,95,111,95,55,49,0),259]]);
          let animation6 = 3.0;
         aboutI = `${parseInt(`${downloadingX}`)}`;
         station3 -= 2;
         weiboY *= parseFloat(`${3 ^ roundF.size}`);
         roundF = new Map([[`${weiboY}`, 2 / (Math.max(2, parseInt(`${weiboY}`)))]]);
         animation6 += parseFloat(`${parseInt(`${weiboY}`) % (Math.max(3, 10))}`);
         break;
      }
      let index8 = 9883185 >= flyerc.size;
      do {
          let hongkongE = String.fromCharCode(115,119,97,112,112,101,114,95,48,95,49,56,0);
          let overlayV = 1.0;
          let borderlessq = 1.0;
          let footballT = 2;
         flyerc.set(aboutI, (String.fromCharCode(90,0) == aboutI ? parseInt(`${borderlessq}`) : aboutI.length));
         hongkongE += `${footballT - parseInt(`${overlayV}`)}`;
         overlayV += hongkongE.length * footballT;
         borderlessq /= Math.max(parseFloat(`${parseInt(`${overlayV}`) + 2}`), 2);
         if (index8) {
            break;
         }
      } while (index8 && (2 > flyerc.size));
         flyerc.set(`${downloadingX}`, parseInt(`${downloadingX}`) >> (Math.min(aboutI.length, 3)));
          let constantsg = 4;
         activeE += "3";
         constantsg >>= Math.min(Math.abs(constantsg), 1);
         activeE += `${activeE.length & parseInt(`${downloadingX}`)}`;
       let rewardvideov: Map<any, any> = new Map([[String.fromCharCode(101,95,50,53,95,115,111,102,116,102,108,111,97,116,0),String.fromCharCode(97,112,112,108,105,101,100,95,108,95,57,55,0)], [String.fromCharCode(119,114,105,116,101,97,98,108,101,95,110,95,52,55,0),String.fromCharCode(97,112,112,101,97,114,105,110,103,95,113,95,54,52,0)], [String.fromCharCode(97,112,112,115,102,108,121,101,114,95,119,95,56,57,0),String.fromCharCode(108,95,49,54,95,109,100,116,97,0)]]);
         downloadingX -= activeE.length % 1;
          let default_rz = 5;
         aboutI += `${aboutI.length % (Math.max(3, activeE.length))}`;
         default_rz &= default_rz | default_rz;
         downloadingX += flyerc.size;
          let tailt = 0.0;
         activeE += `${parseInt(`${tailt}`)}`;
      rightE += `${rightE.length}`;
   }
      materialt = "1";

  const { colors, textVariants, icons, spacing } = useTheme();

      whatsappu.set(`${memberO}`, memberO * rightE.length);
   if (5 > (5 / (Math.max(7, whatsappu.size)))) {
      favoriteY.push(parseInt(`${o_positionp}`) - 1);
   }

  const escapeRegExp = (string: string) => {
    return string.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');

      rightE += `${holderN.size | rightE.length}`;
      skipc -= parseFloat(`${3}`);
  };

       let blacklistg = 2.0;
       let holderj = 3;
       let recommendation6 = String.fromCharCode(103,97,108,108,101,114,121,95,107,95,52,55,0);
          let videocommonK: Map<any, any> = new Map([[String.fromCharCode(98,121,112,97,115,115,95,52,95,52,48,0),602], [String.fromCharCode(109,95,55,49,95,104,101,120,98,110,0),356], [String.fromCharCode(99,95,51,55,95,100,105,115,99,117,115,115,105,111,110,0),318]]);
          let catalogL: Map<any, any> = new Map([[String.fromCharCode(102,95,51,53,95,114,101,106,101,99,116,105,111,110,115,0),258], [String.fromCharCode(101,95,50,53,95,106,115,105,109,100,0),70], [String.fromCharCode(102,99,112,117,98,108,105,115,104,95,122,95,54,48,0),837]]);
         holderj *= 1 % (Math.max(6, videocommonK.size));
         videocommonK.set(`${catalogL.size}`, 1);
         catalogL = new Map([[`${catalogL.size}`, catalogL.size & 1]]);
      if (3 >= holderj) {
         recommendation6 = `${parseInt(`${blacklistg}`) / 2}`;
      }
      for (let i = 0; i < 1; i++) {
          let umengS: Map<any, any> = new Map([[String.fromCharCode(102,95,52,53,95,97,116,116,101,109,116,115,0),String.fromCharCode(97,109,116,95,57,95,56,53,0)], [String.fromCharCode(99,95,53,51,95,114,116,115,112,99,111,100,101,115,0),String.fromCharCode(105,95,56,52,95,115,112,111,116,108,105,103,104,116,0)]]);
          let filed = String.fromCharCode(100,97,114,107,101,110,105,110,103,95,102,95,52,52,0);
          let whistleg: Map<any, any> = new Map([[String.fromCharCode(105,95,55,57,95,114,101,103,101,120,112,0),974], [String.fromCharCode(111,95,57,53,95,117,116,105,108,0),280], [String.fromCharCode(112,97,114,116,105,116,105,111,110,105,110,103,95,102,95,54,56,0),180]]);
         holderj *= holderj / (Math.max(2, 4));
         umengS = new Map([[`${umengS.size}`, umengS.size << (Math.min(Math.abs(1), 4))]]);
         filed = `${umengS.size / (Math.max(1, 9))}`;
         whistleg.set(`${filed}`, filed.length + 3);
      }
      let relatedK = 5606277.0 >= blacklistg;
      do {
          let nextl = 2;
         blacklistg /= Math.max(4, (parseFloat(`${String.fromCharCode(50,0) == recommendation6 ? recommendation6.length : parseInt(`${blacklistg}`)}`)));
         nextl >>= Math.min(5, Math.abs(nextl * nextl));
         if (relatedK) {
            break;
         }
      } while (relatedK && (1.82 == (blacklistg * parseFloat(`${holderj}`)) || 3 == (holderj * parseInt(`${blacklistg}`))));
         blacklistg *= parseFloat(`${parseInt(`${blacklistg}`)}`);
      while ((holderj << (Math.min(recommendation6.length, 1))) > 2 || 4 > (2 << (Math.min(4, recommendation6.length)))) {
         holderj %= Math.max(3 % (Math.max(8, parseInt(`${blacklistg}`))), 2);
         break;
      }
      while (1 >= (holderj & 1)) {
         blacklistg /= Math.max(parseFloat(`${1 | parseInt(`${blacklistg}`)}`), 5);
         break;
      }
      for (let d = 0; d < 1; d++) {
         blacklistg *= parseFloat(`${1}`);
      }
      while (3.28 >= (blacklistg * 3.83)) {
         blacklistg += parseFloat(`${holderj}`);
         break;
      }
      rightE += `${parseInt(`${o_positionp}`)}`;
       let previewa = 4;
       let membert = String.fromCharCode(100,95,50,49,95,101,108,101,109,0);
       let user6 = String.fromCharCode(99,111,101,102,102,115,112,95,118,95,54,54,0);
         membert = `${previewa}`;
       let handleri = String.fromCharCode(97,95,56,56,95,116,105,107,101,114,0);
      if (5 <= (4 * handleri.length) || 1 <= (handleri.length * 4)) {
         handleri = `${handleri.length | 2}`;
      }
      while (handleri.length > user6.length) {
         user6 += "3";
         break;
      }
         handleri = `${(String.fromCharCode(70,0) == user6 ? previewa : user6.length)}`;
      let downloaderl = previewa <= 6628469;
      do {
          let renewB: Map<any, any> = new Map([[String.fromCharCode(115,116,97,99,107,101,100,95,106,95,51,54,0),397], [String.fromCharCode(103,95,49,53,95,108,97,121,111,117,116,115,0),269], [String.fromCharCode(119,95,51,54,95,102,111,114,109,97,116,116,105,110,103,0),652]]);
         previewa -= 3 * membert.length;
         renewB = new Map([[`${renewB.size}`, renewB.size]]);
         if (downloaderl) {
            break;
         }
      } while ((previewa == membert.length) && downloaderl);
         membert = `${handleri.length << (Math.min(user6.length, 4))}`;
         membert = "2";
         user6 = `${previewa >> (Math.min(Math.abs(2), 2))}`;
      playM -= holderN.size % (Math.max(1, 4));
  const highlightText = (
    text: string,
    keyword1: string,
    keyword2: string = '',
  ) => {

   for (let z = 0; z < 3; z++) {
      feedbackZ = `${whatsappu.size}`;
   }
   let questn = 4937317 >= materialt.length;
   do {
      materialt += "3";
      if (questn) {
         break;
      }
   } while (questn && (expandJ.length <= materialt.length));
    const escapedKeyword1 = escapeRegExp(keyword1);

      feedbackZ = `${rightE.length}`;
      whatsappu.set(`${memberO}`, favoriteY.length | memberO);
    const escapedKeyword2 = escapeRegExp(keyword2);

   let stationn = 8828025 >= memberO;
   do {
      memberO >>= Math.min(3, Math.abs((String.fromCharCode(116,0) == feedbackZ ? feedbackZ.length : holderN.size)));
      if (stationn) {
         break;
      }
   } while (stationn && (skipc == 1.3));
      playM *= parseInt(`${playM}`) / 3;

    const parts = text.split(
      new RegExp(`(${escapedKeyword1}|${escapedKeyword2})`, 'gi'),
    );

   for (let e = 0; e < 2; e++) {
      mutedM += `${(String.fromCharCode(112,0) == feedbackZ ? feedbackZ.length : parseInt(`${o_positionp}`))}`;
   }
   if (expandJ.endsWith(`${skipc}`)) {
      skipc /= Math.max(1, parseFloat(`${materialt.length}`));
   }

    return parts.map((part, index) => {
      if (
        part.toLowerCase() === keyword1.toLowerCase() ||
        (keyword2 && part.toLowerCase() === keyword2.toLowerCase())
      ) {

      mutedM = `${rightE.length >> (Math.min(4, mutedM.length))}`;
   while (4 < rightE.length) {
       let completeF: Map<any, any> = new Map([[String.fromCharCode(108,105,98,97,111,109,95,110,95,49,56,0),353], [String.fromCharCode(113,95,49,56,95,118,105,100,101,111,101,110,99,100,115,112,0),649]]);
       let statsw = String.fromCharCode(114,95,57,55,95,105,109,112,111,115,115,105,98,108,101,0);
         completeF.set(`${statsw}`, (String.fromCharCode(76,0) == statsw ? statsw.length : completeF.size));
       let component0: Map<any, any> = new Map([[String.fromCharCode(106,95,56,48,95,105,110,105,116,0),27], [String.fromCharCode(116,95,53,48,95,97,115,101,108,101,99,116,0),310], [String.fromCharCode(103,101,110,101,114,97,108,95,121,95,57,51,0),868]]);
         statsw += `${component0.size | statsw.length}`;
      let desc8 = component0.size >= 5893274;
      do {
          let zoomT = 2.0;
          let bing0 = String.fromCharCode(115,105,109,112,108,101,115,105,103,110,97,108,95,110,95,52,51,0);
          let viewsD = 4;
         component0.set(`${zoomT}`, viewsD + parseInt(`${zoomT}`));
         bing0 = `${bing0.length % 1}`;
         if (desc8) {
            break;
         }
      } while (desc8 && (3 == (component0.size / 3) && 1 == (component0.size / 3)));
         completeF = new Map([[`${completeF.size}`, completeF.size]]);
          let downloadedL = 1.0;
          let trashl: Array<any> = [String.fromCharCode(105,102,102,116,95,108,95,56,56,0), String.fromCharCode(116,95,49,52,95,118,111,99,97,98,0), String.fromCharCode(100,95,54,52,95,100,105,115,97,98,108,101,100,0)];
          let hooks4 = 1.0;
         component0.set(`${hooks4}`, parseInt(`${hooks4}`) ^ 1);
         downloadedL += parseFloat(`${1 | trashl.length}`);
         trashl = [parseInt(`${downloadedL}`)];
      rightE = `${2 / (Math.max(3, rightE.length))}`;
      break;
   }
        return (
          <Text
            key={index}
            style={{
              ...textVariants.body,
              color: colors.yellow,
              fontStyle: 'italic',
              fontWeight: '700',
              fontSize: 22,
            }}>
            {part}
          </Text>
        );
      } else {

      playM /= Math.max(3, expandJ.length);
      holderN.set(`${playM}`, holderN.size ^ 1);
        return (
          <Text
            key={index}
            style={{ ...textVariants.body, color: '#FFFFFF', fontSize: 20 }}>
            {part}
          </Text>
        );
      }
    });
  };

      favoriteY.push(favoriteY.length);
      o_positionp *= parseInt(`${playM}`);
  return (
    <>
      <View
        style={{
          paddingTop: 10,
          paddingBottom: 15,
          flexDirection: 'column',
          justifyContent: 'center',
          alignItems: 'center',
          gap: 9,
        }}>
        <Text style={{ fontSize: 22, color: colors.yellow, fontWeight: '700' }}>
          双赢奖励
        </Text>

        <Text>{highlightText('每邀请1位好友 增加30天VIP', '1', '30')}</Text>

        <Text>{highlightText('好友获得30天VIP', '30')}</Text>
      </View>
    </>
  );
}

const styles = StyleSheet.create({});
