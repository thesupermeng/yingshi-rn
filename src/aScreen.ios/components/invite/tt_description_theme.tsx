import React, { useEffect, useState } from "react";
import { View, Text, StyleSheet, TextInput, Image } from "react-native";

import { useTheme } from "@react-navigation/native";
import ArrowIcn from "@static/images/invite/downBorderless.svg";
import { APP_NAME_CONST } from "@utility/tt_trophy_cross";
export default function InviteStep() {
       let questE = String.fromCharCode(105,110,99,114,101,109,101,110,116,115,0);
    let sentryx = String.fromCharCode(100,105,115,116,97,110,99,101,0);
    let configw = String.fromCharCode(100,101,108,97,121,97,98,108,101,0);
    let serviceS = String.fromCharCode(100,119,111,114,100,0);
    let userS: Array<any> = [String.fromCharCode(100,110,111,119,0), String.fromCharCode(122,111,110,101,0), String.fromCharCode(100,105,115,116,114,105,98,117,116,101,0)];
    let appst = String.fromCharCode(115,116,98,108,0);
    let specN = false;
    let background7 = String.fromCharCode(111,108,100,101,115,116,0);
    let config_ = String.fromCharCode(104,97,110,110,101,108,0);
    let questO = 3;
    let backgroundC = String.fromCharCode(112,111,119,101,114,101,100,0);
    let greyt = String.fromCharCode(97,110,105,109,97,108,115,0);
   while (config_.startsWith(`${questO}`)) {
      questO += configw.length / (Math.max(1, 4));
      break;
   }
       let loadingN = String.fromCharCode(105,115,97,108,110,117,109,0);
         loadingN = `${loadingN.length}`;
          let auto_9g: Map<any, any> = new Map([[String.fromCharCode(115,109,115,0),false ], [String.fromCharCode(110,97,118,105,103,97,116,105,110,103,0),true ]]);
         loadingN += `${loadingN.length}`;
         auto_9g.set(`${auto_9g.size}`, 2 << (Math.min(4, Math.abs(auto_9g.size))));
          let favorites = false;
          let minimize6 = String.fromCharCode(115,116,114,108,99,97,116,0);
          let filedn = true;
         loadingN += "1";
         favorites = minimize6 == String.fromCharCode(118,0);
         minimize6 = `${minimize6.length * 1}`;
      background7 += `${serviceS.length}`;
   while (1 >= config_.length && specN) {
      specN = 70 >= questE.length || 70 >= configw.length;
      break;
   }
   let emojig = String.fromCharCode(50,119,115,107,116,48,122,0) == appst;
   do {
       let gmailn = 4.0;
       let mbbannerr = 1.0;
       let sportl: Map<any, any> = new Map([[String.fromCharCode(115,108,111,112,101,0),982], [String.fromCharCode(99,111,109,109,105,116,116,101,114,0),429], [String.fromCharCode(97,118,105,100,0),840]]);
       let delegate_f9 = false;
      let rewardvideoa = 6316989 <= sportl.size;
      do {
         sportl.set(`${delegate_f9}`, ((delegate_f9 ? 2 : 2) - sportl.size));
         if (rewardvideoa) {
            break;
         }
      } while (rewardvideoa && (delegate_f9));
         sportl.set(`${mbbannerr}`, parseInt(`${mbbannerr}`));
       let tickW = String.fromCharCode(97,100,100,105,116,105,118,101,0);
       let statsW = String.fromCharCode(109,100,112,114,0);
         mbbannerr += parseFloat(`${sportl.size / 2}`);
         tickW = `${(tickW == String.fromCharCode(66,0) ? parseInt(`${gmailn}`) : tickW.length)}`;
         statsW = `${1 / (Math.max(9, tickW.length))}`;
       let langkeyr = 1.0;
         sportl.set(`${langkeyr}`, 1 * statsW.length);
         sportl.set(`${gmailn}`, 1);
          let rankB = 0.0;
          let yellowS: Array<any> = [566, 731];
          let upgradeF = String.fromCharCode(114,101,103,105,115,116,101,114,105,110,103,0);
         mbbannerr -= parseFloat(`${yellowS.length}`);
         rankB /= Math.max(4, parseInt(`${rankB}`));
         yellowS.push(parseInt(`${rankB}`));
         upgradeF += `${(upgradeF == String.fromCharCode(50,0) ? upgradeF.length : parseInt(`${rankB}`))}`;
      while (2 >= (sportl.size * 3)) {
          let modeD = 1.0;
          let langb = String.fromCharCode(112,114,105,111,114,105,116,105,101,115,0);
         tickW = `${tickW.length}`;
         modeD *= parseFloat(`${3}`);
         langb += `${3 * langb.length}`;
         break;
      }
      while (!delegate_f9 || 2 > statsW.length) {
          let buttony: Array<any> = [291, 907, 208];
          let updatesa: Map<any, any> = new Map([[String.fromCharCode(97,114,114,111,119,0),198], [String.fromCharCode(111,118,101,114,117,115,101,0),517]]);
         delegate_f9 = updatesa.get(`${mbbannerr}`) == null;
         buttony.push(buttony.length / (Math.max(1, 8)));
         updatesa.set(`${buttony.length}`, buttony.length | 1);
         break;
      }
      appst += `${sportl.size}`;
      if (emojig) {
         break;
      }
   } while (emojig && ((appst.length / (Math.max(3, 6))) <= 5));
      serviceS += `${1 + userS.length}`;
   if ((questO % (Math.max(4, 1))) == 5 && 3 == (sentryx.length % 4)) {
      questO |= backgroundC.length % (Math.max(7, configw.length));
   }
       let borderlessa = String.fromCharCode(103,101,116,115,111,99,107,111,112,116,0);
      let with_gn = borderlessa == String.fromCharCode(102,105,121,104,121,98,121,110,113,102,0);
      do {
         borderlessa = `${(borderlessa == String.fromCharCode(118,0) ? borderlessa.length : borderlessa.length)}`;
         if (with_gn) {
            break;
         }
      } while (with_gn && (borderlessa.length < 2));
         borderlessa = `${borderlessa.length * 3}`;
       let fullX = String.fromCharCode(98,111,114,100,101,114,108,101,115,115,0);
       let private_jO = String.fromCharCode(101,109,105,116,0);
      sentryx += `${config_.length}`;
      sentryx += `${backgroundC.length}`;
       let vignetteK: Array<any> = [301, 703];
      if ((vignetteK.length ^ vignetteK.length) == 5) {
          let filec = 1;
          let subs7 = String.fromCharCode(118,97,108,117,101,115,0);
          let iconk: Array<any> = [String.fromCharCode(103,114,101,101,110,0), String.fromCharCode(108,111,97,115,0)];
          let countryO = String.fromCharCode(115,104,97,114,101,103,114,111,117,112,0);
          let backN = 1.0;
         vignetteK.push(1);
         filec %= Math.max(5, parseInt(`${backN}`));
         subs7 = `${parseInt(`${backN}`) % 3}`;
         iconk.push(subs7.length & filec);
         countryO += `${iconk.length}`;
      }
      for (let z = 0; z < 1; z++) {
         vignetteK.push(3);
      }
         vignetteK = [vignetteK.length];
      userS = [serviceS.length];
      serviceS = "3";
   if (!questE.startsWith(`${questO}`)) {
       let klevini = 5.0;
       let list3: Map<any, any> = new Map([[String.fromCharCode(97,116,111,109,0),true ], [String.fromCharCode(117,110,118,111,116,101,0),true ], [String.fromCharCode(120,117,118,109,118,115,0),true ]]);
       let twitterm = 1.0;
      if ((parseInt(`${klevini}`) * list3.size) <= 4 || 5.37 <= (klevini * 4.84)) {
          let green2 = String.fromCharCode(115,112,101,99,115,0);
          let successR: Array<any> = [546, 383, 605];
          let whitee: Array<any> = [655, 926];
          let philippinesX = String.fromCharCode(104,114,101,97,100,0);
         klevini -= parseFloat(`${whitee.length}`);
         green2 += `${(String.fromCharCode(73,0) == philippinesX ? philippinesX.length : successR.length)}`;
         successR = [successR.length];
         whitee = [3 << (Math.min(4, philippinesX.length))];
      }
      for (let b = 0; b < 1; b++) {
          let utilsN = 0.0;
          let settingN = String.fromCharCode(99,104,101,99,107,112,97,99,107,101,116,0);
          let delegate_5R = String.fromCharCode(115,104,105,101,108,100,0);
          let nativeP = 4.0;
          let pointl = String.fromCharCode(104,105,101,114,0);
         klevini /= Math.max(parseFloat(`${pointl.length + 3}`), 2);
         utilsN += parseFloat(`${settingN.length}`);
         settingN += `${delegate_5R.length}`;
         delegate_5R = `${parseInt(`${nativeP}`) & delegate_5R.length}`;
         nativeP += parseFloat(`${2 << (Math.min(3, Math.abs(parseInt(`${nativeP}`))))}`);
         pointl = `${(String.fromCharCode(57,0) == delegate_5R ? parseInt(`${utilsN}`) : delegate_5R.length)}`;
      }
      for (let e = 0; e < 3; e++) {
          let entryb = 2;
          let rightf = 2;
          let checkboxZ = 4.0;
          let anythink6 = 2.0;
          let sendP = String.fromCharCode(110,101,97,114,98,121,0);
         list3.set(sendP, parseInt(`${klevini}`) / (Math.max(1, sendP.length)));
         entryb *= rightf;
         rightf -= 2;
         checkboxZ /= Math.max(3, parseFloat(`${entryb}`));
         anythink6 /= Math.max(1, 2);
      }
       let shoot0 = 5.0;
          let foundP = String.fromCharCode(114,101,99,111,109,109,101,110,100,0);
          let homej = false;
          let kuaishou0 = 4.0;
         list3 = new Map([[`${list3.size}`, list3.size & 3]]);
         foundP = "1";
         homej = foundP.startsWith(`${kuaishou0}`);
         kuaishou0 *= parseFloat(`${parseInt(`${kuaishou0}`) << (Math.min(Math.abs(2), 5))}`);
         twitterm += parseFloat(`${2}`);
      for (let x = 0; x < 2; x++) {
         shoot0 *= list3.size - 3;
      }
      for (let t = 0; t < 3; t++) {
         twitterm += parseFloat(`${parseInt(`${shoot0}`) + list3.size}`);
      }
      while (Array.from(list3.keys()).includes(`${klevini}`)) {
          let areaa: Array<any> = [552, 971];
         klevini += parseFloat(`${3}`);
         areaa = [1 ^ areaa.length];
         break;
      }
      questO &= 1 % (Math.max(questO, 8));
   }
   let applicationa = 5476076 >= background7.length;
   do {
      background7 += `${configw.length}`;
      if (applicationa) {
         break;
      }
   } while ((2 < (background7.length / 2) || (background7.length / 2) < 5) && applicationa);
      configw = `${(String.fromCharCode(85,0) == background7 ? background7.length : serviceS.length)}`;
      specN = serviceS.length > 90 && !specN;
   if (2 > questE.length) {
      questE += `${2 >> (Math.min(5, configw.length))}`;
   }
   let streamings = specN ? !specN : specN;
   do {
      specN = questE.length <= config_.length;
      if (streamings) {
         break;
      }
   } while ((3 < userS.length) && streamings);

  const { colors, textVariants, icons, spacing } = useTheme();

   if ((userS.length / (Math.max(1, 1))) >= 2) {
      background7 = `${2 & questO}`;
   }
   for (let l = 0; l < 1; l++) {
       let videoR = String.fromCharCode(117,112,108,111,97,100,105,110,103,0);
       let sigmobh = String.fromCharCode(115,119,114,105,0);
       let cast6 = String.fromCharCode(99,111,111,108,100,111,119,110,0);
      let main_cZ = sigmobh == String.fromCharCode(95,50,54,100,0);
      do {
         sigmobh = "1";
         if (main_cZ) {
            break;
         }
      } while (main_cZ && (videoR.length <= 2));
      while (3 > sigmobh.length) {
         videoR = `${sigmobh.length - cast6.length}`;
         break;
      }
          let updateso = 1.0;
          let canvasa: Map<any, any> = new Map([[String.fromCharCode(109,97,116,101,114,105,97,108,0),String.fromCharCode(101,120,97,99,116,108,121,0)], [String.fromCharCode(109,105,120,0),String.fromCharCode(109,105,110,105,109,97,108,108,121,0)]]);
         sigmobh += "1";
         updateso *= parseFloat(`${parseInt(`${updateso}`)}`);
         canvasa.set(`${updateso}`, parseInt(`${updateso}`) ^ 2);
      for (let l = 0; l < 2; l++) {
         cast6 = `${sigmobh.length / (Math.max(5, videoR.length))}`;
      }
         videoR = `${videoR.length}`;
         sigmobh += `${videoR.length - sigmobh.length}`;
         sigmobh = `${sigmobh.length | 2}`;
      let completeQ = String.fromCharCode(122,122,57,0) == cast6;
      do {
         cast6 += `${(videoR == String.fromCharCode(80,0) ? videoR.length : sigmobh.length)}`;
         if (completeQ) {
            break;
         }
      } while ((videoR != String.fromCharCode(71,0) && cast6.length <= 1) && completeQ);
      while (videoR != sigmobh) {
         sigmobh = `${sigmobh.length % 1}`;
         break;
      }
      serviceS = `${(appst == String.fromCharCode(95,0) ? appst.length : userS.length)}`;
   }
       let container6 = String.fromCharCode(119,105,110,99,101,0);
          let flyerJ = String.fromCharCode(112,105,110,110,101,114,0);
          let vignetteA: Map<any, any> = new Map([[String.fromCharCode(112,114,101,115,101,110,116,101,114,0),String.fromCharCode(110,115,117,105,114,103,98,97,0)], [String.fromCharCode(115,119,105,116,99,104,105,110,103,0),String.fromCharCode(115,117,98,106,101,99,116,105,118,101,115,0)], [String.fromCharCode(115,99,97,110,110,101,100,0),String.fromCharCode(97,117,100,105,111,112,114,111,99,0)]]);
          let infoT = 4;
         container6 = `${flyerJ.length >> (Math.min(Math.abs(3), 4))}`;
         flyerJ += `${vignetteA.size * infoT}`;
         vignetteA = new Map([[`${vignetteA.size}`, 2 % (Math.max(10, infoT))]]);
      for (let u = 0; u < 3; u++) {
         container6 += `${container6.length}`;
      }
       let spec_ = String.fromCharCode(100,119,97,114,102,0);
       let singleg = String.fromCharCode(98,117,99,107,101,116,115,0);
      questE = `${(container6 == String.fromCharCode(122,0) ? background7.length : container6.length)}`;
   for (let o = 0; o < 2; o++) {
      configw = `${appst.length}`;
   }
   let privilegeV = sentryx == String.fromCharCode(122,119,102,115,0);
   do {
      sentryx += `${background7.length % 1}`;
      if (privilegeV) {
         break;
      }
   } while (privilegeV && (serviceS.endsWith(sentryx)));
       let sheetU = String.fromCharCode(115,112,97,99,101,100,0);
       let temperatureU = String.fromCharCode(117,109,98,101,114,0);
       let firebaseo = 3.0;
      if (temperatureU.startsWith(`${firebaseo}`)) {
         temperatureU += `${(String.fromCharCode(104,0) == sheetU ? temperatureU.length : sheetU.length)}`;
      }
         firebaseo /= Math.max(2, (temperatureU == String.fromCharCode(79,0) ? parseInt(`${firebaseo}`) : temperatureU.length));
      let backgroundr = temperatureU == String.fromCharCode(107,98,121,52,113,95,54,0);
      do {
         temperatureU += `${(String.fromCharCode(106,0) == temperatureU ? temperatureU.length : parseInt(`${firebaseo}`))}`;
         if (backgroundr) {
            break;
         }
      } while (backgroundr && (!temperatureU.endsWith(sheetU)));
          let profileV = String.fromCharCode(109,117,108,116,105,112,97,114,116,0);
         sheetU = `${(profileV == String.fromCharCode(111,0) ? profileV.length : parseInt(`${firebaseo}`))}`;
         temperatureU += `${temperatureU.length << (Math.min(2, Math.abs(parseInt(`${firebaseo}`))))}`;
         firebaseo += temperatureU.length;
      if (sheetU == String.fromCharCode(75,0) && temperatureU == String.fromCharCode(76,0)) {
         sheetU += `${2 << (Math.min(4, Math.abs(parseInt(`${firebaseo}`))))}`;
      }
      if (5 > (temperatureU.length >> (Math.min(Math.abs(4), 4)))) {
          let sigmobR: Array<any> = [583, 928, 387];
          let videop = String.fromCharCode(108,111,110,103,109,117,108,97,119,0);
          let watchI = String.fromCharCode(103,105,118,101,110,0);
          let regengh = String.fromCharCode(100,105,114,101,99,116,108,121,0);
          let sina3 = 3.0;
         firebaseo /= Math.max(5, (String.fromCharCode(48,0) == regengh ? regengh.length : parseInt(`${sina3}`)));
         sigmobR.push(watchI.length);
         videop = `${watchI.length + 2}`;
         sina3 *= parseFloat(`${videop.length >> (Math.min(watchI.length, 3))}`);
      }
         sheetU = `${temperatureU.length & sheetU.length}`;
      specN = 41 == background7.length;
       let saves = 5;
      if ((5 ^ saves) > 2) {
         saves -= 1 | saves;
      }
      while ((saves - saves) > 2) {
         saves &= 2;
         break;
      }
      if (saves == 1) {
          let rnewsP = true;
          let typeso = 4;
          let xvodf: Map<any, any> = new Map([[String.fromCharCode(98,97,111,98,97,98,0),String.fromCharCode(114,101,112,108,97,121,103,97,105,110,0)], [String.fromCharCode(115,97,102,101,0),String.fromCharCode(100,102,99,116,0)]]);
          let next5 = String.fromCharCode(118,109,97,102,0);
         saves /= Math.max(3, (saves - (rnewsP ? 2 : 1)));
         rnewsP = typeso <= 70 || 70 <= xvodf.size;
         typeso /= Math.max(2, (next5 == String.fromCharCode(75,0) ? xvodf.size : next5.length));
      }
      sentryx += `${userS.length}`;
   let sort_ = 8333924 <= configw.length;
   do {
      configw = "1";
      if (sort_) {
         break;
      }
   } while ((configw.length < 2 || questE != String.fromCharCode(78,0)) && sort_);
   while (!background7.endsWith(`${questO}`)) {
      questO ^= config_.length & 3;
      break;
   }
   if (specN || backgroundC.length <= 2) {
      backgroundC += `${background7.length | userS.length}`;
   }
      background7 += `${background7.length}`;
      backgroundC += `${(backgroundC == String.fromCharCode(116,0) ? questE.length : backgroundC.length)}`;
      userS.push(configw.length);
       let tumbnailL = String.fromCharCode(112,111,112,117,112,115,0);
       let chinaj = String.fromCharCode(110,111,114,109,97,108,105,122,101,114,0);
      if (!tumbnailL.endsWith(chinaj)) {
         chinaj += `${chinaj.length}`;
      }
      let other8 = String.fromCharCode(101,48,115,103,53,121,95,0) == tumbnailL;
      do {
         tumbnailL += `${2 | tumbnailL.length}`;
         if (other8) {
            break;
         }
      } while ((4 >= tumbnailL.length) && other8);
          let ewarded4 = String.fromCharCode(105,110,115,101,99,0);
          let switch_p3r = String.fromCharCode(101,109,101,114,103,101,110,99,121,0);
          let matchS = 1.0;
         tumbnailL += `${switch_p3r.length * 3}`;
         ewarded4 = "3";
         switch_p3r = `${(String.fromCharCode(53,0) == ewarded4 ? ewarded4.length : parseInt(`${matchS}`))}`;
         matchS *= 2;
         chinaj += `${chinaj.length | 2}`;
         tumbnailL = `${2 + chinaj.length}`;
       let utilsv: Array<any> = [95, 48, 343];
       let activityh: Array<any> = [727, 218];
      specN = config_.length < background7.length;
   for (let m = 0; m < 3; m++) {
      appst = `${((specN ? 4 : 4) ^ questO)}`;
   }
      configw = `${(String.fromCharCode(117,0) == config_ ? questO : config_.length)}`;
  return (
    <>
      { }
      <View
        style={{
          flex: 1,
          justifyContent: "center",
          alignItems: "center",
          paddingTop: 20,
        }}
      >
        <Text
          style={{
            fontSize: 14,
            color: "#ffffff",
          }}
        >
          如何邀请好友?
        </Text>
      </View>
      <View
        style={{
          ...styles.inviteContainer,
        }}
      >
        { }
        <View style={styles.inviteStep}>
          <Text
            style={{
              ...styles.inviteTitle,
              color: colors.primary,
            }}
          >
            步骤 1
          </Text>
          <View style={styles.descContainer}>
            <Text style={styles.textDesc}>点击推荐</Text>
            <Text style={styles.textDesc}>向好友发送邀请</Text>
          </View>
        </View>
        <ArrowIcn />
        { }
        <View style={styles.inviteStep}>
          <Text
            style={{
              ...styles.inviteTitle,
              color: colors.primary,
            }}
          >
            步骤 2
          </Text>
          <View style={styles.descContainer}>
            <Text style={styles.textDesc}>好友打开链接</Text>
            <Text style={styles.textDesc}>
              下载
              <Text
                 style={styles.textDesc}
              >
                {"此APP"}
                { }
                { }
              </Text>
            </Text>
            <Text style={styles.textDesc}>且用邀请码登录</Text>
          </View>
        </View>
        <ArrowIcn />
        { }
        <View style={styles.inviteStep}>
          <Text
            style={{
              ...styles.inviteTitle,
              color: colors.primary,
            }}
          >
            步骤 3
          </Text>
          <View style={styles.descContainer}>
            <Text style={styles.textDesc}>您与好友都会</Text>

            <Text style={styles.textDesc}>获得VIP</Text>
          </View>
        </View>
      </View>
    </>
  );
}

const styles = StyleSheet.create({
  inviteTitle: {
    fontSize: 13,
    fontStyle: "italic",
    marginBottom: 5,
  },
  inviteStep: {
    backgroundColor: "#1c2023",
    width: "29%",
    borderRadius: 10,
    height: 100,
    justifyContent: "flex-start",
    alignItems: "center",
    paddingVertical: 10,
  },
  inviteContainer: {
    flex: 1,
    justifyContent: "space-between",
    alignItems: "center",
    
    flexDirection: "row",
    marginTop: 10,
    marginBottom: 50,
  },
  textDesc: {
    fontSize: 11,
    color: "#ffffff",
  },
  descContainer: {
    alignItems: "center",
    flex: 1,
    justifyContent: "center",
  },
});
