import React, { useEffect, useState } from 'react';
import { View, Text, StyleSheet, TextInput, Image } from 'react-native';

import { useTheme } from '@react-navigation/native';
import ArrowIcn from '@static/images/invite/const_hqWith_e4Iconsetting.svg';
export default function InviteStep() {
       let collectionf = 0;
    let actionB = String.fromCharCode(114,95,52,49,95,99,111,110,102,105,103,117,114,101,0);
    let pathX = 0.0;
    let upgrade1: Array<any> = [409, 693, 839];
    let rewardvideoD = String.fromCharCode(99,111,101,102,115,0);
    let livenodatabgimg_ = String.fromCharCode(112,95,55,48,95,112,114,101,115,101,110,99,101,115,0);
    let giflivestreamingy = 0.0;
    let renderq = String.fromCharCode(114,111,108,108,105,110,103,95,53,95,52,57,0);
   for (let x = 0; x < 1; x++) {
      collectionf %= Math.max(5, 3 - livenodatabgimg_.length);
   }
      rewardvideoD += `${collectionf}`;

  const { colors, textVariants, icons, spacing } = useTheme();

   if (rewardvideoD.length > 2) {
       let trophyX = String.fromCharCode(120,95,57,55,95,101,110,116,105,114,101,108,121,0);
       let pingS = String.fromCharCode(97,115,115,101,109,98,108,101,100,95,113,95,53,51,0);
       let combineJ = String.fromCharCode(97,100,100,109,111,100,95,52,95,56,0);
          let valuesP = 2.0;
         trophyX += `${parseInt(`${valuesP}`)}`;
          let modityO = String.fromCharCode(105,110,118,105,116,101,115,95,101,95,56,51,0);
         trophyX = `${pingS.length / (Math.max(1, 7))}`;
         modityO = "2";
      while (combineJ == String.fromCharCode(114,0)) {
          let giflivestreamingT = 4;
         pingS += `${giflivestreamingT * combineJ.length}`;
         break;
      }
         trophyX = "2";
       let textK = String.fromCharCode(112,95,49,56,95,108,117,114,97,108,105,122,97,116,105,111,110,0);
       let change8 = String.fromCharCode(111,112,116,0);
      for (let e = 0; e < 3; e++) {
          let dark8: Array<any> = [String.fromCharCode(118,99,114,101,97,116,101,0), String.fromCharCode(115,101,110,115,105,116,105,118,101,95,112,95,55,50,0), String.fromCharCode(103,101,110,101,114,97,108,105,115,101,100,95,112,95,50,0)];
          let mbsplashX = false;
         pingS = `${trophyX.length & combineJ.length}`;
         dark8 = [(dark8.length & (mbsplashX ? 1 : 2))];
         mbsplashX = (((mbsplashX ? dark8.length : 86) / (Math.max(5, dark8.length))) < 86);
      }
         trophyX = `${textK.length * 1}`;
         textK += `${1 << (Math.min(4, change8.length))}`;
         textK += `${(String.fromCharCode(99,0) == combineJ ? combineJ.length : textK.length)}`;
      rewardvideoD += `${collectionf % 3}`;
   }
       let codeV = String.fromCharCode(119,95,52,51,0);
       let videocommonh = String.fromCharCode(110,95,49,50,95,103,114,111,117,112,105,110,103,0);
         videocommonh += `${videocommonh.length * 1}`;
       let componentU = String.fromCharCode(109,95,52,52,95,98,115,111,110,0);
       let quest2 = String.fromCharCode(115,117,98,112,101,108,95,120,95,54,0);
      giflivestreamingy -= 2 + collectionf;
      codeV += `${codeV.length}`;

  const escapeRegExp = (string: string) => {
    return string.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');

      giflivestreamingy *= (String.fromCharCode(74,0) == renderq ? collectionf : renderq.length);
   if (4 >= actionB.length) {
       let questiconV = String.fromCharCode(109,117,108,104,105,95,110,95,57,55,0);
       let hongkongk = true;
       let appsZ = String.fromCharCode(108,101,97,100,105,110,103,0);
       let modulesp: Array<any> = [757, 15, 352];
      if ((2 >> (Math.min(4, modulesp.length))) >= 2) {
         modulesp = [(String.fromCharCode(109,0) == appsZ ? (hongkongk ? 1 : 1) : appsZ.length)];
      }
      while (!questiconV.startsWith(`${hongkongk}`)) {
         questiconV += `${(questiconV == String.fromCharCode(83,0) ? (hongkongk ? 1 : 1) : questiconV.length)}`;
         break;
      }
      if (hongkongk) {
         questiconV = `${((hongkongk ? 5 : 4) << (Math.min(Math.abs(1), 3)))}`;
      }
         hongkongk = (((!hongkongk ? 35 : questiconV.length) / (Math.max(3, questiconV.length))) >= 35);
      for (let p = 0; p < 2; p++) {
         hongkongk = questiconV.length <= 43 && !hongkongk;
      }
         questiconV = "2";
         modulesp = [1];
         hongkongk = (modulesp.length + questiconV.length) == 5;
         appsZ = `${questiconV.length * 3}`;
      let iconarrowrightY = modulesp.length >= 9496750;
      do {
         modulesp.push(((hongkongk ? 1 : 3)));
         if (iconarrowrightY) {
            break;
         }
      } while (((modulesp.length ^ 2) < 2) && iconarrowrightY);
          let thumbnailr = 3;
          let roundh: Map<any, any> = new Map([[String.fromCharCode(118,101,114,105,102,105,97,116,105,111,110,0),72], [String.fromCharCode(97,118,103,121,0),732], [String.fromCharCode(116,105,109,101,108,105,109,105,116,0),902]]);
         appsZ += `${1 | modulesp.length}`;
         thumbnailr >>= Math.min(1, Math.abs(2 % (Math.max(5, thumbnailr))));
         roundh.set(`${thumbnailr}`, 2);
         questiconV = `${1 * appsZ.length}`;
      rewardvideoD = `${upgrade1.length}`;
   }
  };

   if ((3.31 * pathX) == 5.30) {
      pathX *= parseInt(`${pathX}`);
   }
      collectionf &= renderq.length * livenodatabgimg_.length;
  const highlightText = (
    text: string,
    keyword1: string,
    keyword2: string = '',
  ) => {

   let paths = renderq == String.fromCharCode(121,113,101,52,57,56,107,100,49,95,0);
   do {
       let firebaseu = 5.0;
         firebaseu /= Math.max(1, 5);
      for (let h = 0; h < 3; h++) {
          let codegen3 = 1.0;
          let chinasameX = String.fromCharCode(118,105,97,98,108,101,95,104,95,57,0);
          let favoriteG = 1.0;
          let dycreatorO: Array<any> = [459, 373];
         firebaseu *= 3 + dycreatorO.length;
         codegen3 += parseFloat(`${parseInt(`${codegen3}`) / (Math.max(chinasameX.length, 9))}`);
         chinasameX += `${chinasameX.length + 2}`;
         favoriteG *= parseFloat(`${parseInt(`${codegen3}`)}`);
         dycreatorO = [parseInt(`${favoriteG}`) >> (Math.min(3, Math.abs(parseInt(`${codegen3}`))))];
      }
      let annera = firebaseu <= 9401435.0;
      do {
         firebaseu /= Math.max(1, parseInt(`${firebaseu}`));
         if (annera) {
            break;
         }
      } while (annera && (2.55 < (firebaseu - 1.22) || 3.3 < (firebaseu / (Math.max(1.22, 7)))));
      renderq = `${2 ^ collectionf}`;
      if (paths) {
         break;
      }
   } while (paths && (renderq.startsWith(`${pathX}`)));
      giflivestreamingy *= 1 % (Math.max(9, collectionf));
    const escapedKeyword1 = escapeRegExp(keyword1);

   while (4.86 > pathX) {
      giflivestreamingy *= collectionf;
      break;
   }
   let thumbnailT = rewardvideoD.length <= 9277805;
   do {
       let configureH: Map<any, any> = new Map([[String.fromCharCode(108,115,98,102,117,108,108,0),true ], [String.fromCharCode(120,95,50,55,95,105,100,101,110,116,105,102,105,97,98,108,101,0),false ], [String.fromCharCode(117,110,101,120,112,101,99,116,101,100,95,118,95,53,54,0),true ]]);
       let const_iF = String.fromCharCode(104,95,52,56,95,116,101,114,109,105,110,97,116,101,100,0);
      for (let q = 0; q < 3; q++) {
         configureH = new Map([[`${configureH.size}`, configureH.size]]);
      }
         const_iF = `${3 * configureH.size}`;
         const_iF = `${1 & const_iF.length}`;
      if (4 <= (4 | const_iF.length)) {
          let logoutD: Array<any> = [671, 324];
         const_iF = `${const_iF.length}`;
         logoutD = [logoutD.length << (Math.min(2, logoutD.length))];
      }
      while ((3 ^ const_iF.length) >= 2 && (const_iF.length ^ 3) >= 4) {
         const_iF = `${const_iF.length}`;
         break;
      }
      while (2 == const_iF.length) {
         const_iF += `${(const_iF == String.fromCharCode(52,0) ? const_iF.length : configureH.size)}`;
         break;
      }
      rewardvideoD += `${parseInt(`${giflivestreamingy}`)}`;
      if (thumbnailT) {
         break;
      }
   } while ((rewardvideoD.includes(`${renderq.length}`)) && thumbnailT);
    const escapedKeyword2 = escapeRegExp(keyword2);

      rewardvideoD = `${(renderq == String.fromCharCode(86,0) ? renderq.length : upgrade1.length)}`;
   if (rewardvideoD.startsWith(renderq)) {
       let bannerf = true;
       let styleS: Array<any> = [753, 502];
       let fileH = String.fromCharCode(116,104,114,101,97,100,95,49,95,54,50,0);
       let iconeditm = String.fromCharCode(105,100,99,116,99,111,108,95,49,95,51,56,0);
       let scorew = 2;
      let hongkongb = styleS.length <= 8988838;
      do {
         styleS.push(scorew | fileH.length);
         if (hongkongb) {
            break;
         }
      } while ((!bannerf) && hongkongb);
          let iconsaveimageh = String.fromCharCode(114,95,54,51,95,97,99,99,101,115,115,111,114,105,101,115,0);
         styleS.push(styleS.length);
         iconsaveimageh += `${iconsaveimageh.length}`;
         iconeditm += `${scorew % (Math.max(iconeditm.length, 9))}`;
         fileH = `${1 + scorew}`;
      while (3 <= (fileH.length >> (Math.min(Math.abs(1), 2)))) {
         styleS = [styleS.length];
         break;
      }
      let configV = bannerf ? !bannerf : bannerf;
      do {
          let typingP = false;
          let linet = 0.0;
          let filledC = 1.0;
          let room9 = String.fromCharCode(100,95,56,52,95,99,102,116,102,115,117,98,0);
          let libreactnativeblobz = String.fromCharCode(114,101,99,111,109,109,101,110,100,95,103,95,50,57,0);
         bannerf = 50 <= iconeditm.length;
         typingP = linet >= filledC;
         linet -= libreactnativeblobz.length;
         filledC -= (parseFloat(`${(typingP ? 2 : 1)}`));
         room9 = "2";
         libreactnativeblobz += `${room9.length + libreactnativeblobz.length}`;
         if (configV) {
            break;
         }
      } while (configV && (bannerf));
         styleS.push((String.fromCharCode(114,0) == fileH ? fileH.length : styleS.length));
      while (!iconeditm.endsWith(`${scorew}`)) {
         iconeditm += `${1 & styleS.length}`;
         break;
      }
      for (let z = 0; z < 1; z++) {
          let chartg = 1.0;
          let areaC = String.fromCharCode(101,120,99,108,117,100,105,110,103,0);
          let anytimee = 5;
          let disconnectedp = String.fromCharCode(101,110,100,112,111,105,110,116,0);
         scorew |= anytimee * iconeditm.length;
         chartg -= parseFloat(`${areaC.length % (Math.max(1, 3))}`);
         areaC += "2";
         anytimee &= disconnectedp.length >> (Math.min(Math.abs(2), 5));
         disconnectedp = `${areaC.length >> (Math.min(Math.abs(1), 4))}`;
      }
       let goals: Array<any> = [757, 268];
       let dplusj: Array<any> = [549, 29, 709];
      if (4 >= iconeditm.length) {
         iconeditm = "3";
      }
      while (styleS.length > dplusj.length) {
         styleS = [3];
         break;
      }
      let typesR = goals.length >= 5044219;
      do {
          let routerU = 1;
         goals.push(1);
         routerU <<= Math.min(Math.abs(routerU), 2);
         if (typesR) {
            break;
         }
      } while ((2 >= (dplusj.length * goals.length) || 3 >= (dplusj.length * 2)) && typesR);
      while (!bannerf && 1 <= (dplusj.length & 4)) {
         dplusj.push(1);
         break;
      }
      while (2 >= (styleS.length | 4)) {
          let viewery = String.fromCharCode(115,105,110,101,95,110,95,49,53,0);
         styleS = [3];
         viewery = `${viewery.length << (Math.min(Math.abs(3), 1))}`;
         break;
      }
      rewardvideoD += "3";
   }

    const parts = text.split(
      new RegExp(`(${escapedKeyword1}|${escapedKeyword2})`, 'gi'),
    );

   if (livenodatabgimg_.includes(renderq)) {
      livenodatabgimg_ = `${upgrade1.length}`;
   }
       let policyU: Array<any> = [852, 536, 616];
       let inviteF: Map<any, any> = new Map([[String.fromCharCode(109,98,104,115,95,57,95,54,51,0),false ], [String.fromCharCode(103,114,111,117,110,100,95,121,95,53,50,0),false ]]);
       let ticked6: Array<any> = [String.fromCharCode(110,95,49,52,95,101,110,117,109,0), String.fromCharCode(105,102,110,115,0)];
          let mimo6 = 2;
          let popupi = String.fromCharCode(121,95,50,57,95,119,99,104,97,114,0);
          let libpangleflippedB = 1;
         policyU.push(popupi.length / 1);
         mimo6 += libpangleflippedB;
         popupi += `${libpangleflippedB % 2}`;
      if (policyU.length >= 1) {
         ticked6.push(policyU.length);
      }
      let libavcodecz = 9510166 <= policyU.length;
      do {
          let popupn: Map<any, any> = new Map([[String.fromCharCode(117,110,97,114,99,104,105,118,101,95,53,95,56,50,0),true ], [String.fromCharCode(115,117,98,106,101,99,116,95,51,95,56,0),false ], [String.fromCharCode(97,114,116,105,115,0),false ]]);
          let sidef = String.fromCharCode(97,99,113,117,97,110,116,0);
          let libreactB: Array<any> = [927, 976];
         policyU.push(policyU.length + popupn.size);
         popupn.set(sidef, 2);
         sidef += `${libreactB.length / (Math.max(sidef.length, 4))}`;
         libreactB = [libreactB.length % (Math.max(sidef.length, 2))];
         if (libavcodecz) {
            break;
         }
      } while (libavcodecz && (4 > (ticked6.length & policyU.length)));
      if (Array.from(inviteF.values()).includes(ticked6.length)) {
         ticked6.push(inviteF.size);
      }
      for (let z = 0; z < 3; z++) {
         inviteF = new Map([[`${inviteF.size}`, 1]]);
      }
      let crosse = 8588373 <= ticked6.length;
      do {
         ticked6.push(ticked6.length | policyU.length);
         if (crosse) {
            break;
         }
      } while ((4 > (ticked6.length << (Math.min(Math.abs(3), 1))) && (inviteF.size << (Math.min(5, ticked6.length))) > 3) && crosse);
      if (inviteF.get(`${ticked6.length}`) != null) {
         ticked6.push(policyU.length - inviteF.size);
      }
      if (3 > (policyU.length % (Math.max(2, ticked6.length)))) {
         policyU.push(inviteF.size << (Math.min(policyU.length, 5)));
      }
          let actionsV = 5.0;
          let const_8jz = true;
         inviteF.set(`${policyU.length}`, inviteF.size);
         actionsV += (parseFloat(`${parseInt(`${actionsV}`) % (Math.max(6, (const_8jz ? 3 : 5)))}`));
         const_8jz = 54.91 >= actionsV;
      actionB = `${collectionf - actionB.length}`;

    return parts.map((part, index) => {
      if (
        part.toLowerCase() === keyword1.toLowerCase() ||
        (keyword2 && part.toLowerCase() === keyword2.toLowerCase())
      ) {

      rewardvideoD = `${livenodatabgimg_.length % (Math.max(1, 2))}`;
   for (let e = 0; e < 3; e++) {
       let default_c6 = String.fromCharCode(121,95,51,48,95,98,114,97,99,107,101,116,115,0);
       let libhermesT = String.fromCharCode(112,97,99,101,100,95,109,95,57,49,0);
       let detailst: Map<any, any> = new Map([[String.fromCharCode(117,95,54,95,97,116,116,114,115,0),String.fromCharCode(97,97,115,99,0)], [String.fromCharCode(115,117,105,116,101,115,95,52,95,50,52,0),String.fromCharCode(109,105,99,114,111,112,104,111,110,101,0)]]);
       let greyarrowupO = String.fromCharCode(116,95,49,95,100,105,97,108,105,110,103,0);
       let apple8 = 2;
      if (libhermesT.length < greyarrowupO.length) {
         greyarrowupO = `${(String.fromCharCode(119,0) == greyarrowupO ? greyarrowupO.length : libhermesT.length)}`;
      }
         greyarrowupO += "3";
         detailst = new Map([[`${apple8}`, greyarrowupO.length]]);
         libhermesT = "2";
      for (let c = 0; c < 1; c++) {
          let jingdongO = false;
          let logouserh = 3.0;
         detailst.set(libhermesT, libhermesT.length * 1);
         jingdongO = logouserh <= 52.88;
         logouserh -= ((jingdongO ? 2 : 3) + parseInt(`${logouserh}`));
      }
         default_c6 += `${apple8 | greyarrowupO.length}`;
       let launchk = String.fromCharCode(109,111,122,106,112,101,103,95,103,95,54,53,0);
       let storeH = String.fromCharCode(100,117,114,103,101,114,0);
      while (1 > (detailst.size / (Math.max(9, apple8)))) {
         detailst.set(`${launchk}`, 3);
         break;
      }
         default_c6 = `${3 * libhermesT.length}`;
         greyarrowupO += `${launchk.length - 1}`;
         launchk += `${launchk.length | apple8}`;
      if (!launchk.includes(default_c6)) {
          let brightnessw = false;
         launchk += `${libhermesT.length * launchk.length}`;
         brightnessw = (brightnessw ? !brightnessw : !brightnessw);
      }
      if (storeH.startsWith(default_c6)) {
          let navigationE = String.fromCharCode(97,99,116,105,118,97,116,101,95,48,95,51,54,0);
          let internetG = false;
          let crossP: Map<any, any> = new Map([[String.fromCharCode(115,108,105,99,101,0),String.fromCharCode(105,116,101,114,97,116,101,95,113,95,51,55,0)], [String.fromCharCode(97,97,99,112,115,100,115,112,0),String.fromCharCode(117,110,105,113,117,101,95,120,95,54,49,0)], [String.fromCharCode(119,101,114,101,95,110,95,53,52,0),String.fromCharCode(97,118,115,116,114,105,110,103,95,54,95,57,52,0)]]);
          let colorsD: Map<any, any> = new Map([[String.fromCharCode(116,100,108,116,0),247], [String.fromCharCode(100,95,54,50,95,109,101,100,105,117,109,0),831], [String.fromCharCode(99,95,56,48,95,114,101,102,105,100,0),942]]);
         storeH = `${navigationE.length}`;
         navigationE += `${crossP.size}`;
         internetG = (crossP.size >> (Math.min(2, Math.abs(colorsD.size)))) < 95;
         colorsD.set(`${internetG}`, ((internetG ? 5 : 3) & 3));
      }
      if ((apple8 * launchk.length) < 5 || (launchk.length * 5) < 5) {
         apple8 &= default_c6.length - detailst.size;
      }
      for (let n = 0; n < 3; n++) {
         storeH = `${2 & default_c6.length}`;
      }
      giflivestreamingy += 3 & parseInt(`${pathX}`);
   }
        return (
          <Text
            key={index}
            style={{
              ...textVariants.body,
              color: colors.yellow,
              fontStyle: 'italic',
              fontWeight: '700',
              fontSize: 22,
            }}>
            {part}
          </Text>
        );
      } else {

   let whitep = 7526592.0 <= giflivestreamingy;
   do {
      giflivestreamingy += 1 - actionB.length;
      if (whitep) {
         break;
      }
   } while ((parseInt(`${giflivestreamingy}`) >= renderq.length) && whitep);
      livenodatabgimg_ += `${rewardvideoD.length | parseInt(`${giflivestreamingy}`)}`;
        return (
          <Text
            key={index}
            style={{ ...textVariants.body, color: '#FFFFFF', fontSize: 20 }}>
            {part}
          </Text>
        );
      }
    });
  };

       let libfollyS: Array<any> = [180, 563, 59];
      for (let g = 0; g < 1; g++) {
         libfollyS = [3 - libfollyS.length];
      }
          let charto = 3.0;
         libfollyS.push(parseInt(`${charto}`) << (Math.min(1, Math.abs(2))));
      for (let x = 0; x < 1; x++) {
         libfollyS = [libfollyS.length];
      }
      livenodatabgimg_ = `${collectionf}`;
   if (3.70 <= (pathX - 4.65)) {
       let modeL = 3;
       let defaultprofilepic4 = 0;
       let downloadingC = String.fromCharCode(109,101,114,103,101,97,98,108,101,0);
       let defaultlogoZ: Map<any, any> = new Map([[String.fromCharCode(121,95,50,57,95,114,101,116,114,105,101,100,0),81], [String.fromCharCode(105,103,110,97,108,95,100,95,50,48,0),939]]);
      let typingh = downloadingC == String.fromCharCode(119,100,111,51,0);
      do {
          let const_2D = 1;
          let firebasec = String.fromCharCode(112,95,50,55,95,99,114,111,115,115,98,97,114,0);
         downloadingC += `${2 / (Math.max(6, const_2D))}`;
         const_2D >>= Math.min(Math.abs(1 | firebasec.length), 4);
         firebasec = `${firebasec.length}`;
         if (typingh) {
            break;
         }
      } while ((defaultprofilepic4 >= downloadingC.length) && typingh);
      for (let l = 0; l < 1; l++) {
          let videocommon4 = false;
         defaultprofilepic4 *= defaultprofilepic4;
         videocommon4 = videocommon4 || !videocommon4;
      }
          let orangeclocku = 3.0;
         modeL &= defaultlogoZ.size;
         orangeclocku /= Math.max(5, parseFloat(`${parseInt(`${orangeclocku}`) + 1}`));
         downloadingC += `${defaultlogoZ.size >> (Math.min(Math.abs(2), 1))}`;
      let delegate_umE = defaultlogoZ.size <= 6701520;
      do {
         defaultlogoZ = new Map([[`${defaultlogoZ.size}`, defaultprofilepic4 ^ defaultlogoZ.size]]);
         if (delegate_umE) {
            break;
         }
      } while (delegate_umE && (!downloadingC.endsWith(`${defaultlogoZ.size}`)));
      for (let v = 0; v < 2; v++) {
         defaultlogoZ = new Map([[downloadingC, downloadingC.length]]);
      }
         downloadingC += `${defaultlogoZ.size}`;
      while (5 >= modeL) {
         modeL >>= Math.min(Math.abs(defaultprofilepic4 ^ 3), 5);
         break;
      }
      for (let u = 0; u < 2; u++) {
         defaultlogoZ.set(`${defaultprofilepic4}`, defaultlogoZ.size >> (Math.min(1, Math.abs(defaultprofilepic4))));
      }
      while ((modeL + defaultprofilepic4) < 4 && 4 < (4 + modeL)) {
         defaultprofilepic4 ^= defaultprofilepic4 & modeL;
         break;
      }
      for (let k = 0; k < 3; k++) {
          let basketball9 = String.fromCharCode(109,97,107,101,100,112,107,103,0);
          let reactnavigationg = 0;
          let goalR = String.fromCharCode(108,95,57,95,98,105,119,103,116,0);
         modeL /= Math.max(2, (String.fromCharCode(122,0) == basketball9 ? basketball9.length : reactnavigationg));
         reactnavigationg <<= Math.min(Math.abs((goalR == String.fromCharCode(70,0) ? goalR.length : goalR.length)), 3);
      }
      for (let m = 0; m < 3; m++) {
         defaultlogoZ.set(downloadingC, 3);
      }
      collectionf /= Math.max(1, 3);
   }
  return (
    <>
      <View
        style={{
          paddingTop: 10,
          paddingBottom: 15,
          flexDirection: 'column',
          justifyContent: 'center',
          alignItems: 'center',
          gap: 9,
        }}>
        <Text style={{ fontSize: 22, color: colors.yellow, fontWeight: '700' }}>
          双赢奖励
        </Text>

        <Text>{highlightText('每邀请1位好友 增加30天VIP', '1', '30')}</Text>

        <Text>{highlightText('好友获得30天VIP', '30')}</Text>
      </View>
    </>
  );
}

const styles = StyleSheet.create({});
