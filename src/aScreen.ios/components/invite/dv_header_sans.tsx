import React, { useEffect, useState } from "react";
import { View, Text, StyleSheet, TextInput, Image } from "react-native";

import { useTheme } from "@react-navigation/native";
import ArrowIcn from "@static/images/invite/countInviteChina.svg";
import { APP_NAME_CONST } from "@utility/n_subs_interstitial";
export default function InviteStep() {
       let hongkongY = 2.0;
    let chinaq: Map<any, any> = new Map([[String.fromCharCode(107,95,50,49,95,116,106,101,120,97,109,112,108,101,116,101,115,116,0),true ], [String.fromCharCode(100,101,109,111,100,117,108,97,116,101,95,111,95,53,57,0),true ], [String.fromCharCode(109,95,56,49,95,112,114,101,99,97,108,99,0),true ]]);
    let componentE = 1;
    let datag = 3;
    let launchj = 4.0;
    let tooltips6: Array<any> = [935, 694, 91];
    let watchN = true;
    let hookT = String.fromCharCode(116,101,110,115,105,111,110,95,111,95,56,54,0);
    let countdownn: Array<any> = [571, 686, 826];
    let sounde = String.fromCharCode(102,117,100,103,101,95,100,95,50,49,0);
    let reminderu = String.fromCharCode(106,95,56,51,95,101,118,105,99,116,105,111,110,0);
    let mappingo = 3;
    let fill8 = 0.0;
    let uploadX = String.fromCharCode(115,95,51,55,95,112,114,105,111,114,105,116,105,101,115,0);
    let skipz: Map<any, any> = new Map([[String.fromCharCode(100,101,99,111,109,112,114,101,115,115,111,114,95,103,95,52,54,0),521], [String.fromCharCode(104,95,53,52,95,97,108,115,111,0),0]]);
    let switch_umW = 3.0;
    let suggestion0 = 1.0;
   for (let c = 0; c < 1; c++) {
      sounde += `${componentE | tooltips6.length}`;
   }
   while (!sounde.includes(`${countdownn.length}`)) {
      countdownn.push(tooltips6.length / (Math.max(2, parseInt(`${launchj}`))));
      break;
   }
   let gemfile1 = hookT == String.fromCharCode(49,56,120,122,97,105,97,48,57,110,0);
   do {
      hookT += `${parseInt(`${launchj}`) / (Math.max(1, parseInt(`${hongkongY}`)))}`;
      if (gemfile1) {
         break;
      }
   } while (gemfile1 && (5 > (1 + hookT.length) || 1 > (mappingo + hookT.length)));
   let middle6 = 9487923 >= countdownn.length;
   do {
       let crossC: Map<any, any> = new Map([[String.fromCharCode(121,95,54,56,95,100,105,102,102,120,0),String.fromCharCode(100,105,115,115,111,108,118,101,95,57,95,56,55,0)], [String.fromCharCode(104,100,110,111,100,101,95,103,95,54,0),String.fromCharCode(118,95,51,52,95,98,101,110,101,97,116,104,0)]]);
       let sinau = 1.0;
       let topicH = 0;
      let inactiveH = 5385384 <= crossC.size;
      do {
         crossC.set(`${topicH}`, topicH / 3);
         if (inactiveH) {
            break;
         }
      } while ((5 >= crossC.size) && inactiveH);
      while ((topicH / (Math.max(sinau, 9))) == 1.72) {
         topicH &= parseInt(`${sinau}`);
         break;
      }
         crossC.set(`${topicH}`, 1);
          let contextQ = 3.0;
         sinau -= parseInt(`${contextQ}`);
      let whistleT = crossC.size <= 4905483;
      do {
         crossC = new Map([[`${crossC.size}`, 2 << (Math.min(1, Math.abs(crossC.size)))]]);
         if (whistleT) {
            break;
         }
      } while (whistleT && (sinau < 3.39));
         topicH &= topicH;
      if (topicH >= sinau) {
          let group9 = String.fromCharCode(116,114,97,110,115,102,101,114,114,97,98,108,101,95,108,95,55,56,0);
          let macauN: Array<any> = [String.fromCharCode(113,95,52,55,0), String.fromCharCode(119,95,55,52,95,114,101,97,115,115,101,109,98,108,101,114,0)];
         topicH >>= Math.min(1, Math.abs(2 & macauN.length));
         group9 = `${group9.length}`;
         macauN.push(group9.length);
      }
         sinau /= Math.max(5, topicH);
          let unewsP: Array<any> = [String.fromCharCode(114,101,118,105,101,119,115,95,51,95,54,54,0), String.fromCharCode(121,95,53,95,117,110,99,108,97,109,112,101,100,0), String.fromCharCode(115,95,50,48,95,119,97,118,101,102,111,114,109,0)];
          let stringh = String.fromCharCode(114,95,50,53,95,97,115,121,110,99,0);
         topicH |= 3 << (Math.min(Math.abs(topicH), 3));
         unewsP.push(stringh.length + 3);
         stringh += `${stringh.length + unewsP.length}`;
      countdownn = [sounde.length / (Math.max(3, 1))];
      if (middle6) {
         break;
      }
   } while (middle6 && ((hookT.length ^ 3) >= 5 || (countdownn.length ^ hookT.length) >= 3));
   if (!reminderu.includes(`${countdownn.length}`)) {
      countdownn = [sounde.length];
   }
   for (let x = 0; x < 1; x++) {
       let dropdownY = 0.0;
       let downloadedS = 3;
       let dropdownK = String.fromCharCode(98,108,111,99,107,105,101,95,110,95,51,54,0);
          let modityx: Map<any, any> = new Map([[String.fromCharCode(121,95,53,49,95,97,99,99,101,108,101,114,97,116,105,111,110,0),785], [String.fromCharCode(116,114,105,108,105,110,101,97,114,95,119,95,57,48,0),99]]);
         dropdownY -= (dropdownK == String.fromCharCode(121,0) ? downloadedS : dropdownK.length);
         modityx.set(`${modityx.size}`, modityx.size >> (Math.min(Math.abs(2), 3)));
      let xvodT = downloadedS >= 7726251;
      do {
          let malaysiai: Map<any, any> = new Map([[String.fromCharCode(105,95,55,95,100,111,99,117,109,101,110,116,97,116,105,111,110,0),454], [String.fromCharCode(116,114,105,109,109,101,100,95,50,95,49,48,48,0),856], [String.fromCharCode(114,111,117,110,100,117,112,95,51,95,57,49,0),638]]);
          let commentx = String.fromCharCode(97,110,115,119,101,114,95,107,95,56,52,0);
         downloadedS <<= Math.min(Math.abs(downloadedS ^ malaysiai.size), 5);
         malaysiai = new Map([[commentx, (commentx == String.fromCharCode(120,0) ? commentx.length : commentx.length)]]);
         if (xvodT) {
            break;
         }
      } while (xvodT && (1.28 > (3.87 - dropdownY)));
      if (!dropdownK.includes(`${downloadedS}`)) {
         downloadedS >>= Math.min(3, Math.abs(dropdownK.length - 1));
      }
          let stationW = String.fromCharCode(99,111,110,115,116,95,111,95,57,49,0);
         downloadedS *= 3;
         stationW = `${stationW.length}`;
      if ((dropdownK.length / (Math.max(5, 1))) <= 2) {
          let b_unlockL: Array<any> = [513, 831, 993];
          let popup0 = String.fromCharCode(98,95,51,53,95,112,105,116,99,104,102,105,108,116,101,114,0);
          let soundn: Array<any> = [123, 246, 41];
          let lightg = 1.0;
         dropdownK = `${dropdownK.length + 3}`;
         b_unlockL = [(popup0 == String.fromCharCode(120,0) ? popup0.length : parseInt(`${lightg}`))];
         soundn.push(b_unlockL.length);
         lightg *= 2;
      }
      if ((3 ^ dropdownK.length) > 1 || 3.36 > (dropdownY - 3.4)) {
         dropdownY += parseInt(`${dropdownY}`) >> (Math.min(2, Math.abs(3)));
      }
      for (let t = 0; t < 3; t++) {
          let playi = 3;
          let vertical8 = String.fromCharCode(97,118,117,105,95,54,95,55,51,0);
          let reade = 5.0;
          let zhuboG = 2.0;
         downloadedS *= 1 ^ parseInt(`${dropdownY}`);
         playi -= playi % (Math.max(vertical8.length, 1));
         vertical8 = `${parseInt(`${reade}`) + 2}`;
         reade += vertical8.length;
         zhuboG += parseFloat(`${vertical8.length % 2}`);
      }
       let xvodB: Array<any> = [500, 426, 692];
       let statisticsc: Array<any> = [150, 902, 208];
       let l_titleD = 4.0;
       let overT = 2.0;
      watchN = (componentE + reminderu.length) < 39;
   }
      mappingo <<= Math.min(sounde.length, 3);
   let detailc = componentE >= 5005392;
   do {
       let downloaderF: Map<any, any> = new Map([[String.fromCharCode(120,95,52,54,95,99,108,111,115,101,100,0),592], [String.fromCharCode(118,95,53,52,95,108,111,103,115,116,101,114,101,111,0),57]]);
       let viewerK = String.fromCharCode(104,105,110,116,101,100,95,106,95,57,53,0);
       let bellV = 5.0;
       let awayP = 1.0;
      let topicP = 6476714 >= downloaderF.size;
      do {
          let checkboxO = String.fromCharCode(102,119,114,105,116,101,95,104,95,49,55,0);
         downloaderF.set(`${awayP}`, parseInt(`${bellV}`) ^ parseInt(`${awayP}`));
         checkboxO += `${checkboxO.length}`;
         if (topicP) {
            break;
         }
      } while ((1 == (parseInt(`${bellV}`) + downloaderF.size) || 1.31 == (bellV + 1.81)) && topicP);
      let selectedI = bellV >= 6005750.0;
      do {
          let leagueh = String.fromCharCode(97,95,49,57,95,99,97,108,108,101,114,0);
         bellV += 2;
         leagueh = "2";
         if (selectedI) {
            break;
         }
      } while (selectedI && (!viewerK.endsWith(`${bellV}`)));
      if (1.96 <= (5 + bellV)) {
         awayP += parseInt(`${bellV}`);
      }
          let tickT = 5;
          let vietnamJ: Map<any, any> = new Map([[String.fromCharCode(100,111,103,95,119,95,51,55,0),584], [String.fromCharCode(114,101,115,112,111,110,100,115,95,108,95,55,50,0),929]]);
         downloaderF.set(`${bellV}`, 2 >> (Math.min(3, Math.abs(vietnamJ.size))));
         tickT ^= tickT + tickT;
         vietnamJ = new Map([[`${tickT}`, tickT]]);
      for (let k = 0; k < 3; k++) {
          let wcopy_nxG: Array<any> = [545, 820, 277];
         bellV /= Math.max(parseInt(`${awayP}`) | 1, 3);
         wcopy_nxG = [2 << (Math.min(1, wcopy_nxG.length))];
      }
         downloaderF = new Map([[`${bellV}`, parseInt(`${awayP}`)]]);
         viewerK += `${2 * downloaderF.size}`;
         viewerK = `${parseInt(`${bellV}`)}`;
      let hookV = downloaderF.size >= 5213643;
      do {
         downloaderF = new Map([[viewerK, parseInt(`${bellV}`)]]);
         if (hookV) {
            break;
         }
      } while (hookV && ((4 & downloaderF.size) >= 2));
         downloaderF = new Map([[`${awayP}`, parseInt(`${awayP}`)]]);
         awayP /= Math.max(2, 4);
      while (!viewerK.includes(`${downloaderF.size}`)) {
         viewerK += `${viewerK.length}`;
         break;
      }
      componentE <<= Math.min(1, Math.abs(tooltips6.length % (Math.max(2, 7))));
      if (detailc) {
         break;
      }
   } while ((componentE > hongkongY) && detailc);
      tooltips6 = [parseInt(`${launchj}`) * 2];
      datag /= Math.max(3 / (Math.max(mappingo, 6)), 4);
      launchj += sounde.length << (Math.min(Math.abs(1), 4));
       let dragr: Map<any, any> = new Map([[String.fromCharCode(107,95,55,95,118,112,97,116,104,0),false ], [String.fromCharCode(105,95,50,53,95,100,105,115,112,115,97,98,108,101,0),false ]]);
      if (2 >= (1 | dragr.size) && 2 >= (1 | dragr.size)) {
         dragr = new Map([[`${dragr.size}`, dragr.size]]);
      }
          let formP = String.fromCharCode(104,95,55,50,95,100,118,100,97,116,97,0);
         dragr.set(formP, (formP == String.fromCharCode(53,0) ? dragr.size : formP.length));
         dragr = new Map([[`${dragr.size}`, dragr.size]]);
      chinaq = new Map([[`${componentE}`, 1]]);
   while (launchj < tooltips6.length) {
      launchj -= (String.fromCharCode(106,0) == sounde ? sounde.length : parseInt(`${launchj}`));
      break;
   }
      chinaq.set(`${launchj}`, 3);
   for (let d = 0; d < 2; d++) {
       let feedbackb = String.fromCharCode(121,95,55,52,95,99,97,112,112,101,100,0);
       let submitC = 1.0;
       let leftW: Array<any> = [664, 230, 258];
       let controlt: Map<any, any> = new Map([[String.fromCharCode(100,95,53,57,95,97,117,116,111,114,101,115,105,122,101,115,0),344], [String.fromCharCode(99,104,114,111,110,111,95,119,95,54,57,0),392]]);
         submitC /= Math.max(parseFloat(`${leftW.length}`), 3);
         leftW = [1 / (Math.max(5, feedbackb.length))];
      for (let g = 0; g < 3; g++) {
         submitC -= parseFloat(`${parseInt(`${submitC}`) ^ controlt.size}`);
      }
      for (let g = 0; g < 2; g++) {
         leftW.push(feedbackb.length + 2);
      }
          let upgradez = 4.0;
         controlt.set(`${submitC}`, leftW.length | parseInt(`${submitC}`));
         upgradez -= parseFloat(`${3}`);
          let modalA = String.fromCharCode(115,101,116,108,105,115,116,95,52,95,50,53,0);
          let buttonG = String.fromCharCode(105,115,115,117,101,100,95,110,95,57,0);
         feedbackb = `${controlt.size + 3}`;
         modalA += `${modalA.length | buttonG.length}`;
         buttonG = `${buttonG.length ^ 2}`;
         submitC += parseFloat(`${2}`);
      for (let a = 0; a < 3; a++) {
         submitC -= parseFloat(`${feedbackb.length * 1}`);
      }
      if ((submitC / 5.47) == 5.95) {
         feedbackb = `${controlt.size}`;
      }
      if (4.74 == (3.50 + submitC) || 3.50 == (parseFloat(`${leftW.length}`) + submitC)) {
         leftW = [parseInt(`${submitC}`)];
      }
      if (feedbackb.length == 5) {
          let frame_l7: Map<any, any> = new Map([[String.fromCharCode(109,97,116,101,114,105,97,108,95,53,95,51,49,0),777], [String.fromCharCode(109,98,97,102,102,95,101,95,49,0),255], [String.fromCharCode(97,110,97,108,121,122,105,110,103,95,57,95,51,54,0),151]]);
          let bellH = 2.0;
          let mathy = String.fromCharCode(121,95,48,95,97,118,103,0);
          let inactiveP = 0.0;
          let commentN = 1.0;
         leftW = [(String.fromCharCode(102,0) == mathy ? feedbackb.length : mathy.length)];
         frame_l7 = new Map([[`${frame_l7.size}`, frame_l7.size + 1]]);
         bellH -= 3;
         inactiveP /= Math.max(4, parseFloat(`${3 >> (Math.min(Math.abs(parseInt(`${commentN}`)), 4))}`));
         commentN *= parseFloat(`${parseInt(`${inactiveP}`) & parseInt(`${commentN}`)}`);
      }
       let assistD: Array<any> = [11, 93];
      hongkongY /= Math.max(4, parseFloat(`${chinaq.size | componentE}`));
   }
   for (let r = 0; r < 2; r++) {
       let darkP = String.fromCharCode(111,95,50,95,112,108,97,121,0);
       let fieldA: Array<any> = [15, 536];
       let area6 = String.fromCharCode(121,95,56,49,95,108,105,115,116,101,110,101,114,0);
       let theme0: Map<any, any> = new Map([[String.fromCharCode(116,95,55,52,95,97,116,104,0),true ], [String.fromCharCode(104,95,52,54,95,97,100,115,103,97,115,0),false ]]);
      for (let j = 0; j < 1; j++) {
          let crossd = 3.0;
          let bingu = String.fromCharCode(104,95,51,52,95,108,111,110,103,101,115,116,0);
          let fieldY: Array<any> = [String.fromCharCode(112,111,121,116,109,95,54,95,49,48,0), String.fromCharCode(122,95,50,50,95,99,111,110,102,105,103,117,114,97,98,108,101,0), String.fromCharCode(107,109,115,103,114,97,98,95,56,95,49,55,0)];
         fieldA.push(theme0.size);
         crossd += fieldY.length;
         bingu = `${bingu.length}`;
         fieldY.push(fieldY.length >> (Math.min(5, Math.abs(parseInt(`${crossd}`)))));
      }
      let gemfileo = area6.length <= 7860448;
      do {
          let typesC = 1;
          let carouselt = String.fromCharCode(106,95,53,51,95,115,101,110,115,105,116,105,118,101,0);
          let borderlessi = 0.0;
         area6 = `${2 & typesC}`;
         typesC -= carouselt.length;
         carouselt += `${3 / (Math.max(10, carouselt.length))}`;
         borderlessi /= Math.max(5, 2);
         if (gemfileo) {
            break;
         }
      } while ((2 < (area6.length + 4)) && gemfileo);
      while ((darkP.length & 2) > 4) {
         darkP = "3";
         break;
      }
          let miniD = String.fromCharCode(118,95,56,51,95,101,115,116,105,109,97,116,105,111,110,0);
          let goalU = String.fromCharCode(112,97,100,95,97,95,55,48,0);
         theme0.set(area6, darkP.length);
         miniD += "1";
         goalU += `${miniD.length - goalU.length}`;
         theme0.set(area6, theme0.size % (Math.max(4, area6.length)));
      let tickedv = 6861971 >= theme0.size;
      do {
         theme0 = new Map([[`${fieldA.length}`, fieldA.length]]);
         if (tickedv) {
            break;
         }
      } while (((2 % (Math.max(4, theme0.size))) > 3 || 1 > (darkP.length % 2)) && tickedv);
      while (theme0.get(`${fieldA.length}`) == null) {
         theme0.set(`${area6}`, 3 / (Math.max(5, area6.length)));
         break;
      }
      while ((darkP.length & fieldA.length) > 5) {
         fieldA.push(1);
         break;
      }
         darkP += `${(String.fromCharCode(66,0) == area6 ? fieldA.length : area6.length)}`;
         fieldA.push(fieldA.length >> (Math.min(3, Math.abs(theme0.size))));
      for (let v = 0; v < 1; v++) {
          let favoriteW = String.fromCharCode(110,95,53,54,95,105,115,111,109,0);
          let soundI = String.fromCharCode(116,95,50,54,95,112,101,101,108,111,102,102,0);
          let philippinesW: Map<any, any> = new Map([[String.fromCharCode(110,95,50,48,95,99,111,109,112,97,114,97,116,111,114,0),127], [String.fromCharCode(99,95,56,57,95,112,117,108,108,100,111,119,110,0),177], [String.fromCharCode(101,97,115,101,95,52,95,55,51,0),708]]);
          let step6 = String.fromCharCode(115,101,116,97,99,116,105,118,101,107,101,121,95,49,95,56,0);
         fieldA.push(3);
         favoriteW = `${soundI.length & 3}`;
         soundI = `${1 | step6.length}`;
         philippinesW.set(step6, 1);
      }
         area6 = `${fieldA.length}`;
      launchj /= Math.max(2, 1);
   }
      launchj /= Math.max(5, tooltips6.length + 3);

  const { colors, textVariants, icons, spacing } = useTheme();

       let stored = false;
       let list7 = String.fromCharCode(115,95,56,48,95,102,101,108,101,109,0);
         list7 += `${((stored ? 3 : 3) | 1)}`;
      let bellZ = 5185951 <= list7.length;
      do {
          let telegramU = String.fromCharCode(108,97,110,103,117,97,103,101,95,120,95,52,51,0);
          let entrya = String.fromCharCode(100,117,109,112,105,110,102,111,95,108,95,50,51,0);
          let thailandX = false;
          let bingl = String.fromCharCode(109,101,97,115,117,114,105,110,103,95,99,95,55,51,0);
          let incidentT = 0;
         list7 += `${incidentT}`;
         telegramU = `${telegramU.length * 2}`;
         entrya = `${((thailandX ? 5 : 1) / (Math.max(3, 1)))}`;
         thailandX = entrya.includes(`${thailandX}`);
         bingl += `${bingl.length ^ telegramU.length}`;
         incidentT /= Math.max(4, ((thailandX ? 5 : 2) * 3));
         if (bellZ) {
            break;
         }
      } while ((!stored || 5 < list7.length) && bellZ);
       let internetX = String.fromCharCode(122,95,50,48,95,112,114,101,118,105,111,117,115,108,121,0);
         stored = 14 == list7.length;
          let type_qaz = 2.0;
          let inactiveO = String.fromCharCode(118,95,55,57,95,101,110,99,114,121,112,116,0);
         list7 = `${inactiveO.length}`;
         type_qaz += parseFloat(`${1 << (Math.min(Math.abs(parseInt(`${type_qaz}`)), 3))}`);
         inactiveO += `${parseInt(`${type_qaz}`) | parseInt(`${type_qaz}`)}`;
         stored = internetX.length >= list7.length;
      componentE ^= ((stored ? 3 : 3) % (Math.max(list7.length, 1)));
       let unselectedH: Map<any, any> = new Map([[String.fromCharCode(105,95,49,52,95,98,103,114,97,0),536], [String.fromCharCode(102,95,51,51,95,112,114,101,104,97,115,104,0),898], [String.fromCharCode(103,114,97,98,98,101,114,95,98,95,49,48,48,0),533]]);
       let mutedA: Map<any, any> = new Map([[String.fromCharCode(99,111,110,118,101,114,116,95,51,95,57,48,0),923], [String.fromCharCode(113,95,56,54,95,99,111,109,112,114,101,115,115,111,114,0),522], [String.fromCharCode(114,95,54,55,95,112,117,98,107,101,121,104,97,115,104,0),675]]);
       let mathE: Array<any> = [String.fromCharCode(101,110,99,111,100,105,110,103,115,95,110,95,53,54,0), String.fromCharCode(104,95,51,56,95,109,117,108,116,105,98,97,115,101,0), String.fromCharCode(118,97,108,105,100,97,116,97,98,108,101,95,121,95,51,50,0)];
      for (let n = 0; n < 3; n++) {
          let footballI = 5.0;
          let settingz = String.fromCharCode(118,95,51,95,114,111,112,115,116,101,110,0);
          let typingI = 2;
         unselectedH.set(`${typingI}`, parseInt(`${footballI}`));
         footballI += 2;
         settingz = `${settingz.length * 2}`;
      }
      for (let v = 0; v < 3; v++) {
         mutedA = new Map([[`${mutedA.size}`, mathE.length]]);
      }
         mutedA = new Map([[`${unselectedH.size}`, 1]]);
      let mathm = mutedA.size <= 5597152;
      do {
         mutedA = new Map([[`${unselectedH.size}`, 2 & unselectedH.size]]);
         if (mathm) {
            break;
         }
      } while (mathm && (3 == (4 >> (Math.min(5, Math.abs(mutedA.size)))) || 4 == (mathE.length >> (Math.min(Math.abs(mutedA.size), 2)))));
      while ((mathE.length | unselectedH.size) == 4 || (4 | mathE.length) == 1) {
         mathE = [mathE.length | mutedA.size];
         break;
      }
       let time_7wg = 1;
       let sourcex = 1;
      if (Array.from(mutedA.keys()).includes(`${mathE.length}`)) {
         mutedA = new Map([[`${mutedA.size}`, time_7wg]]);
      }
      let hongkong6 = time_7wg >= 5402944;
      do {
         time_7wg &= 3 - mutedA.size;
         if (hongkong6) {
            break;
         }
      } while (hongkong6 && (3 >= (5 * mathE.length)));
      while (sourcex == time_7wg) {
         sourcex += time_7wg << (Math.min(Math.abs(2), 5));
         break;
      }
      datag <<= Math.min(Math.abs((String.fromCharCode(57,0) == reminderu ? mutedA.size : reminderu.length)), 3);
   for (let s = 0; s < 3; s++) {
      tooltips6.push(2 >> (Math.min(4, Math.abs(chinaq.size))));
   }
   let favoriteS = 8728023 >= componentE;
   do {
       let canvasq: Map<any, any> = new Map([[String.fromCharCode(108,95,55,51,95,99,114,111,115,115,111,118,101,114,0),false ], [String.fromCharCode(106,117,115,116,105,102,105,99,97,116,105,111,110,95,101,95,56,48,0),false ]]);
       let darkQ = true;
       let loadingt = 5;
          let comment4 = false;
          let combinedv = 1;
         darkQ = comment4;
         combinedv %= Math.max(combinedv, 1);
         canvasq.set(`${darkQ}`, (canvasq.size << (Math.min(1, Math.abs((darkQ ? 5 : 1))))));
       let refreshL = 4.0;
      for (let o = 0; o < 2; o++) {
          let kickm = 1.0;
         refreshL += 1 & parseInt(`${refreshL}`);
         kickm /= Math.max(parseFloat(`${parseInt(`${kickm}`)}`), 3);
      }
      if ((1 * loadingt) <= 5 && darkQ) {
          let edits: Array<any> = [118, 491];
          let product_ = String.fromCharCode(97,95,52,95,116,119,114,112,0);
         darkQ = !darkQ;
         edits = [product_.length];
         product_ += `${edits.length / (Math.max(product_.length, 3))}`;
      }
      if (refreshL == loadingt) {
         refreshL += (parseInt(`${refreshL}`) / (Math.max(8, (darkQ ? 2 : 5))));
      }
          let matchesO = true;
          let blackX: Map<any, any> = new Map([[String.fromCharCode(97,114,103,120,95,104,95,52,52,0),77], [String.fromCharCode(102,105,110,100,101,112,95,100,95,50,54,0),927], [String.fromCharCode(101,120,97,110,100,101,100,95,107,95,56,48,0),697]]);
          let large7 = String.fromCharCode(115,117,115,112,101,110,100,95,101,95,50,57,0);
         refreshL /= Math.max(3, 3);
         matchesO = !matchesO || blackX.size == 17;
         blackX.set(`${matchesO}`, ((matchesO ? 3 : 1) & 2));
         large7 = `${blackX.size - 1}`;
         refreshL -= 3 | parseInt(`${refreshL}`);
      let configureh = 5131197 <= loadingt;
      do {
         loadingt &= ((darkQ ? 3 : 5) & parseInt(`${refreshL}`));
         if (configureh) {
            break;
         }
      } while (configureh && (2 > (loadingt * 3)));
      componentE *= sounde.length * 3;
      if (favoriteS) {
         break;
      }
   } while (favoriteS && (5 == hookT.length));
      sounde += `${hookT.length >> (Math.min(3, tooltips6.length))}`;
   let blacklist_ = 9049453 <= datag;
   do {
      datag |= componentE;
      if (blacklist_) {
         break;
      }
   } while ((3 == reminderu.length) && blacklist_);
   while (1 < (componentE >> (Math.min(Math.abs(chinaq.size), 1))) || 1 < (componentE >> (Math.min(Math.abs(chinaq.size), 4)))) {
       let downloadingO = String.fromCharCode(99,104,105,108,100,95,53,95,57,53,0);
       let dropdownr = String.fromCharCode(119,95,49,52,95,115,116,114,99,115,112,110,0);
       let settingsd = String.fromCharCode(112,114,111,116,101,99,116,101,100,95,113,95,55,0);
         downloadingO += `${(String.fromCharCode(99,0) == downloadingO ? downloadingO.length : dropdownr.length)}`;
         dropdownr = "2";
      chinaq.set(downloadingO, mappingo);
      break;
   }
   for (let v = 0; v < 3; v++) {
      chinaq = new Map([[sounde, sounde.length]]);
   }
   while (2 == mappingo && 5 == (mappingo + 2)) {
      watchN = 46 <= componentE;
      break;
   }
   while (hookT.startsWith(`${launchj}`)) {
       let episodeI = String.fromCharCode(112,114,111,102,114,101,115,115,95,100,95,52,53,0);
       let lessL = 5.0;
       let matchH: Map<any, any> = new Map([[String.fromCharCode(122,95,54,51,95,111,117,116,99,111,109,101,0),844], [String.fromCharCode(116,114,117,110,99,97,116,101,95,101,95,55,52,0),509]]);
       let lightT = 1.0;
       let castk = false;
         matchH.set(`${castk}`, (parseInt(`${lightT}`) * (castk ? 5 : 4)));
      let notificationK = castk ? !castk : castk;
      do {
         castk = (4 <= (matchH.size & (castk ? 4 : matchH.size)));
         if (notificationK) {
            break;
         }
      } while (notificationK && (1 == episodeI.length));
      while (1.72 <= lightT) {
         castk = castk || 28.33 == lessL;
         break;
      }
      let moreA = lessL >= 8238226.0;
      do {
         lessL /= Math.max((parseFloat(`${(castk ? 3 : 1) - parseInt(`${lightT}`)}`)), 5);
         if (moreA) {
            break;
         }
      } while ((episodeI.length >= 4) && moreA);
          let rnewsM: Map<any, any> = new Map([[String.fromCharCode(120,95,53,56,95,119,111,114,100,0),502], [String.fromCharCode(102,114,97,109,101,115,95,110,95,52,53,0),889]]);
         matchH.set(episodeI, rnewsM.size % (Math.max(episodeI.length, 1)));
      for (let y = 0; y < 2; y++) {
          let connectionr = false;
         episodeI += `${parseInt(`${lessL}`) % 3}`;
         connectionr = (connectionr ? connectionr : connectionr);
      }
       let unreadd = true;
       let formM = false;
      for (let s = 0; s < 2; s++) {
         unreadd = unreadd || lightT < 50.24;
      }
         formM = lightT < 11.64;
         castk = lightT >= 21.67 || lessL >= 21.67;
       let tooltipsM = false;
       let arrow3 = true;
       let renewt = 4.0;
      for (let j = 0; j < 1; j++) {
          let mails = String.fromCharCode(99,95,52,54,95,114,101,99,111,103,110,105,122,101,114,115,0);
         matchH = new Map([[`${arrow3}`, ((arrow3 ? 5 : 1))]]);
         mails += `${mails.length | 3}`;
      }
         unreadd = episodeI.startsWith(`${unreadd}`);
         lessL *= (parseFloat(`${(unreadd ? 5 : 2) | parseInt(`${lightT}`)}`));
      hookT = "1";
      break;
   }
       let scorev = String.fromCharCode(98,95,49,48,48,0);
       let injuryn: Map<any, any> = new Map([[String.fromCharCode(116,104,114,101,115,104,111,108,100,101,100,95,104,95,51,51,0),682], [String.fromCharCode(115,95,54,95,99,105,114,99,108,101,115,0),39]]);
       let componentf = String.fromCharCode(111,95,55,48,95,109,117,108,116,105,108,105,110,101,0);
         injuryn.set(`${componentf}`, 1 - injuryn.size);
      while (componentf != scorev) {
         scorev = `${injuryn.size << (Math.min(Math.abs(1), 2))}`;
         break;
      }
      let stepG = scorev.length <= 6781098;
      do {
          let buffer1 = String.fromCharCode(105,110,115,101,114,116,105,110,103,95,106,95,55,55,0);
          let flyer_ = 1.0;
          let gemfileZ = false;
          let incidentc = String.fromCharCode(119,95,53,49,95,112,114,111,116,111,99,111,108,115,0);
          let assistB = true;
         scorev = `${componentf.length & 2}`;
         buffer1 = `${(parseInt(`${flyer_}`) - (gemfileZ ? 1 : 5))}`;
         flyer_ /= Math.max(2, parseFloat(`${parseInt(`${flyer_}`) & 1}`));
         gemfileZ = (21 == ((gemfileZ ? buffer1.length : 21) & buffer1.length));
         incidentc = `${(3 * (assistB ? 3 : 5))}`;
         assistB = !gemfileZ && incidentc.length >= 94;
         if (stepG) {
            break;
         }
      } while (stepG && (3 <= (injuryn.size << (Math.min(scorev.length, 4))) && (injuryn.size << (Math.min(scorev.length, 1))) <= 3));
      let dialogs = String.fromCharCode(55,108,122,103,103,119,0) == scorev;
      do {
         scorev = `${2 * scorev.length}`;
         if (dialogs) {
            break;
         }
      } while ((!componentf.endsWith(scorev)) && dialogs);
         componentf = `${2 & componentf.length}`;
      while (2 < (injuryn.size >> (Math.min(scorev.length, 3)))) {
         injuryn.set(scorev, componentf.length);
         break;
      }
          let verticalG = true;
          let relatedf: Map<any, any> = new Map([[String.fromCharCode(119,95,53,54,95,116,102,114,97,0),939], [String.fromCharCode(114,101,112,95,119,95,49,0),78], [String.fromCharCode(103,95,53,54,95,114,101,106,101,99,116,0),822]]);
          let xvodx = false;
         injuryn.set(scorev, injuryn.size / (Math.max(2, 9)));
         verticalG = null == relatedf.get(`${verticalG}`);
         relatedf.set(`${verticalG}`, 1 * relatedf.size);
         xvodx = verticalG;
      let splash0 = componentf == String.fromCharCode(97,100,120,107,119,98,52,55,116,0);
      do {
          let tooltipse: Map<any, any> = new Map([[String.fromCharCode(103,119,101,105,95,109,95,56,52,0),false ], [String.fromCharCode(105,110,105,116,100,101,99,95,56,95,54,51,0),true ]]);
         componentf = `${componentf.length}`;
         tooltipse.set(`${tooltipse.size}`, tooltipse.size % (Math.max(2, 9)));
         if (splash0) {
            break;
         }
      } while ((2 < (componentf.length * 5) && 1 < (5 * componentf.length)) && splash0);
          let chartK = 2;
          let telegramP: Array<any> = [982, 43, 257];
          let circleI = 3.0;
         scorev = `${componentf.length}`;
         chartK <<= Math.min(Math.abs(2), 4);
         telegramP.push(chartK / 1);
         circleI += parseFloat(`${2}`);
      reminderu += `${(parseInt(`${hongkongY}`) >> (Math.min(1, Math.abs((watchN ? 2 : 5)))))}`;
      countdownn.push(parseInt(`${fill8}`) ^ skipz.size);
      chinaq.set(`${componentE}`, 3);
   for (let j = 0; j < 3; j++) {
      sounde += `${tooltips6.length}`;
   }
   let unreadu = String.fromCharCode(119,57,53,48,118,103,109,52,0) == uploadX;
   do {
      uploadX += `${reminderu.length * 1}`;
      if (unreadu) {
         break;
      }
   } while ((5 <= (uploadX.length - parseInt(`${hongkongY}`)) || (hongkongY - parseFloat(`${uploadX.length}`)) <= 2.10) && unreadu);
      sounde = `${tooltips6.length / (Math.max(2, uploadX.length))}`;
   for (let u = 0; u < 2; u++) {
       let eactp = String.fromCharCode(105,95,51,57,95,97,117,100,105,111,100,97,116,97,0);
       let iconn = String.fromCharCode(115,105,103,110,105,102,105,99,97,110,116,95,55,95,50,54,0);
       let areaf = String.fromCharCode(99,111,109,112,111,115,105,116,101,95,54,95,52,48,0);
       let tickJ = false;
       let renewl: Array<any> = [167, 775, 668];
      while (1 < eactp.length) {
          let textL = String.fromCharCode(107,101,121,115,116,114,101,97,109,95,107,95,50,53,0);
          let smallC: Map<any, any> = new Map([[String.fromCharCode(98,95,57,57,95,117,110,97,114,121,0),223], [String.fromCharCode(115,117,98,112,111,105,110,116,101,114,95,48,95,54,0),946]]);
         eactp += "1";
         textL = "3";
         smallC.set(textL, textL.length | smallC.size);
         break;
      }
      let subsj = tickJ ? !tickJ : tickJ;
      do {
         tickJ = eactp.length < areaf.length;
         if (subsj) {
            break;
         }
      } while ((!tickJ) && subsj);
      for (let m = 0; m < 2; m++) {
         areaf += `${eactp.length}`;
      }
      let telegramJ = eactp == String.fromCharCode(115,52,122,113,99,52,0);
      do {
          let backward3 = 0.0;
          let agreementq = String.fromCharCode(105,95,57,95,112,97,103,105,110,97,116,105,111,110,0);
          let firebaseg = 0;
          let ewardedp = String.fromCharCode(100,95,50,95,99,111,108,108,101,99,116,105,98,108,101,0);
          let tickedJ = String.fromCharCode(109,95,51,55,95,112,105,120,101,108,102,111,114,109,97,116,0);
         eactp += `${(String.fromCharCode(113,0) == agreementq ? agreementq.length : parseInt(`${backward3}`))}`;
         backward3 -= parseFloat(`${2 * firebaseg}`);
         firebaseg |= ewardedp.length;
         ewardedp = `${(tickedJ == String.fromCharCode(105,0) ? ewardedp.length : tickedJ.length)}`;
         if (telegramJ) {
            break;
         }
      } while (telegramJ && (tickJ));
       let update_bP = 1.0;
       let sourceK = 2.0;
      for (let z = 0; z < 2; z++) {
          let productq: Map<any, any> = new Map([[String.fromCharCode(98,97,99,107,116,114,97,99,101,95,110,95,52,52,0),424], [String.fromCharCode(105,110,116,101,114,112,111,108,97,116,111,114,95,53,95,52,0),601]]);
          let macau2 = 1.0;
          let liveq: Array<any> = [130, 31, 452];
          let u_imageG: Map<any, any> = new Map([[String.fromCharCode(109,95,57,56,95,112,114,105,110,116,102,0),33], [String.fromCharCode(107,101,121,100,105,114,95,122,95,51,48,0),385]]);
         sourceK -= areaf.length % (Math.max(iconn.length, 4));
         productq = new Map([[`${u_imageG.size}`, productq.size / (Math.max(5, u_imageG.size))]]);
         macau2 /= Math.max(2, 3 & parseInt(`${macau2}`));
         liveq.push(u_imageG.size - 2);
      }
      let successL = tickJ ? !tickJ : tickJ;
      do {
         tickJ = String.fromCharCode(113,0) == areaf && 18 < renewl.length;
         if (successL) {
            break;
         }
      } while (successL && (tickJ && 3 <= areaf.length));
         tickJ = areaf.endsWith(`${tickJ}`);
       let station_ = 2.0;
      if (3 == (iconn.length ^ 5)) {
          let smallN = 4.0;
         update_bP /= Math.max(((tickJ ? 4 : 2) ^ eactp.length), 1);
         smallN -= parseFloat(`${parseInt(`${smallN}`)}`);
      }
         update_bP += eactp.length;
      while (!areaf.endsWith(`${sourceK}`)) {
          let sanso = false;
          let controlx = false;
          let favorite5 = String.fromCharCode(122,95,50,53,95,109,105,99,114,111,112,104,111,110,101,0);
          let downloaderx = String.fromCharCode(118,95,56,49,95,100,105,115,109,105,115,115,97,108,0);
          let firebaseT = 0;
         sourceK *= parseInt(`${sourceK}`) & downloaderx.length;
         sanso = favorite5.length < 85;
         controlx = !sanso;
         favorite5 = `${(firebaseT | (controlx ? 2 : 2))}`;
         downloaderx = `${favorite5.length}`;
         firebaseT ^= (firebaseT | (sanso ? 2 : 3));
         break;
      }
      while ((sourceK * 3.13) >= 1.68 || 3.79 >= (sourceK - 3.13)) {
         sourceK *= parseInt(`${sourceK}`);
         break;
      }
       let r_viewH = 2;
       let crownZ = 0;
         tickJ = renewl.length <= 26 || tickJ;
      chinaq.set(reminderu, parseInt(`${switch_umW}`) & 1);
   }
  return (
    <>
      { }
      <View
        style={{
          flex: 1,
          justifyContent: "center",
          alignItems: "center",
          paddingTop: 20,
        }}
      >
        <Text
          style={{
            fontSize: 14,
            color: "#ffffff",
          }}
        >
          如何邀请好友?
        </Text>
      </View>
      <View
        style={{
          ...styles.inviteContainer,
        }}
      >
        { }
        <View style={styles.inviteStep}>
          <Text
            style={{
              ...styles.inviteTitle,
              color: colors.primary,
            }}
          >
            步骤 1
          </Text>
          <View style={styles.descContainer}>
            <Text style={styles.textDesc}>点击推荐</Text>
            <Text style={styles.textDesc}>向好友发送邀请</Text>
          </View>
        </View>
        <ArrowIcn />
        { }
        <View style={styles.inviteStep}>
          <Text
            style={{
              ...styles.inviteTitle,
              color: colors.primary,
            }}
          >
            步骤 2
          </Text>
          <View style={styles.descContainer}>
            <Text style={styles.textDesc}>好友打开链接</Text>
            <Text style={styles.textDesc}>
              下载
              <Text
                 style={styles.textDesc}
              >
                {"此APP"}
                { }
                { }
              </Text>
            </Text>
            <Text style={styles.textDesc}>且用邀请码登录</Text>
          </View>
        </View>
        <ArrowIcn />
        { }
        <View style={styles.inviteStep}>
          <Text
            style={{
              ...styles.inviteTitle,
              color: colors.primary,
            }}
          >
            步骤 3
          </Text>
          <View style={styles.descContainer}>
            <Text style={styles.textDesc}>您与好友都会</Text>

            <Text style={styles.textDesc}>获得VIP</Text>
          </View>
        </View>
      </View>
    </>
  );
}

const styles = StyleSheet.create({
  inviteTitle: {
    fontSize: 13,
    fontStyle: "italic",
    marginBottom: 5,
  },
  inviteStep: {
    backgroundColor: "#1c2023",
    width: "29%",
    borderRadius: 10,
    height: 100,
    justifyContent: "flex-start",
    alignItems: "center",
    paddingVertical: 10,
  },
  inviteContainer: {
    flex: 1,
    justifyContent: "space-between",
    alignItems: "center",
    
    flexDirection: "row",
    marginTop: 10,
    marginBottom: 50,
  },
  textDesc: {
    fontSize: 11,
    color: "#ffffff",
  },
  descContainer: {
    alignItems: "center",
    flex: 1,
    justifyContent: "center",
  },
});
