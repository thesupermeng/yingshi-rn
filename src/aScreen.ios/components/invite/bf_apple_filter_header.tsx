import React, { useEffect, useState } from 'react';
import { View, Text, StyleSheet, TextInput, Image } from 'react-native';

import { useTheme } from '@react-navigation/native';
import ArrowIcn from '@static/images/invite/countInviteChina.svg';
export default function InviteStep() {
       let foundQ = String.fromCharCode(112,114,111,102,114,101,115,115,95,122,95,51,51,0);
    let flyery = 2.0;
    let bootsplashr = String.fromCharCode(116,114,97,110,115,108,97,116,105,111,110,115,95,100,95,57,56,0);
    let whistle3 = 2.0;
    let emptyk = 5;
    let dangerm = 4.0;
    let actionsa = String.fromCharCode(122,95,55,57,95,97,99,99,101,115,115,101,100,0);
    let eactn = String.fromCharCode(103,97,116,101,100,95,99,95,51,0);
    let windu = String.fromCharCode(97,118,111,105,100,95,114,95,49,52,0);
    let privacyb = String.fromCharCode(97,112,97,114,97,109,115,95,106,95,57,49,0);
    let middle4 = 0.0;
    let applew = String.fromCharCode(109,95,51,53,95,109,105,120,101,100,0);
    let thailandI = 0.0;
    let internetd: Array<any> = [82, 252, 333];
    let show3 = 0;
       let clockk = 5;
       let spec0: Map<any, any> = new Map([[String.fromCharCode(115,116,97,116,115,95,54,95,55,54,0),327], [String.fromCharCode(105,115,116,114,101,97,109,95,53,95,53,49,0),460], [String.fromCharCode(110,95,56,54,95,116,101,114,109,105,110,97,108,0),494]]);
       let found8: Map<any, any> = new Map([[String.fromCharCode(102,95,55,95,100,101,99,111,109,112,111,115,101,0),String.fromCharCode(106,95,54,56,95,97,112,105,115,0)], [String.fromCharCode(99,111,110,115,117,109,101,95,48,95,51,54,0),String.fromCharCode(111,117,116,108,105,110,107,95,103,95,57,53,0)], [String.fromCharCode(100,101,115,99,114,95,102,95,57,51,0),String.fromCharCode(105,110,99,111,109,112,97,116,105,98,108,101,95,114,95,56,56,0)]]);
      while ((5 & clockk) >= 1) {
         found8.set(`${clockk}`, clockk);
         break;
      }
         clockk <<= Math.min(Math.abs(found8.size), 3);
      dangerm -= parseFloat(`${parseInt(`${whistle3}`)}`);
      foundQ = `${parseInt(`${flyery}`) | 2}`;

  const { colors, textVariants, icons, spacing } = useTheme();

      eactn = `${windu.length * parseInt(`${whistle3}`)}`;
   if (windu == String.fromCharCode(99,0)) {
       let selectr: Map<any, any> = new Map([[String.fromCharCode(115,95,56,54,95,117,110,115,99,97,108,101,0),763], [String.fromCharCode(108,95,51,53,95,115,99,97,110,0),79], [String.fromCharCode(98,121,116,101,115,104,117,109,97,110,95,118,95,55,48,0),680]]);
       let ballr = String.fromCharCode(108,105,98,97,118,117,116,105,108,95,122,95,54,0);
       let form9 = 3.0;
       let paginationq = 0;
       let previewK: Map<any, any> = new Map([[String.fromCharCode(102,105,110,105,115,101,100,95,48,95,55,50,0),true ], [String.fromCharCode(114,95,51,95,99,97,108,99,117,108,97,116,111,114,0),true ], [String.fromCharCode(101,110,100,115,95,122,95,56,56,0),true ]]);
      for (let x = 0; x < 1; x++) {
         form9 *= parseFloat(`${paginationq}`);
      }
          let loginX = String.fromCharCode(114,101,108,97,121,95,103,95,51,48,0);
          let giftS = 0.0;
          let saveW = 5.0;
         selectr.set(ballr, ballr.length << (Math.min(4, Math.abs(paginationq))));
         loginX += `${parseInt(`${saveW}`) + 2}`;
         giftS += 3;
         saveW += parseFloat(`${parseInt(`${saveW}`) + loginX.length}`);
         previewK = new Map([[ballr, 2 - parseInt(`${form9}`)]]);
      for (let c = 0; c < 3; c++) {
         paginationq >>= Math.min(5, Math.abs(parseInt(`${form9}`) * selectr.size));
      }
      if (paginationq > 1) {
         previewK.set(`${form9}`, parseInt(`${form9}`));
      }
      let favicon3 = 5402038 <= previewK.size;
      do {
         previewK.set(`${previewK.size}`, previewK.size);
         if (favicon3) {
            break;
         }
      } while ((!Array.from(previewK.values()).includes(form9)) && favicon3);
         paginationq &= 1;
         selectr.set(`${form9}`, 2 + parseInt(`${form9}`));
      while (parseFloat(`${ballr.length}`) < form9) {
         form9 += parseFloat(`${1 | previewK.size}`);
         break;
      }
      for (let i = 0; i < 2; i++) {
         previewK = new Map([[`${previewK.size}`, 2]]);
      }
      while (Array.from(previewK.keys()).includes(`${form9}`)) {
         previewK = new Map([[`${previewK.size}`, 3 & paginationq]]);
         break;
      }
      let screenV = 9596817 >= previewK.size;
      do {
         previewK.set(`${paginationq}`, paginationq);
         if (screenV) {
            break;
         }
      } while (screenV && (2 > (3 | ballr.length)));
       let pagex: Map<any, any> = new Map([[String.fromCharCode(107,95,56,56,95,99,108,99,112,0),504], [String.fromCharCode(116,104,111,117,103,104,95,49,95,57,55,0),808], [String.fromCharCode(102,95,52,56,95,114,97,116,105,111,110,97,108,0),967]]);
       let hejii: Map<any, any> = new Map([[String.fromCharCode(97,110,116,105,97,108,105,97,115,101,100,95,56,95,55,56,0),53], [String.fromCharCode(116,101,120,116,117,114,101,100,115,112,101,110,99,95,98,95,51,49,0),711], [String.fromCharCode(97,99,99,117,109,117,108,97,116,101,95,110,95,51,57,0),209]]);
      if (3 >= ballr.length) {
         previewK = new Map([[`${pagex.size}`, ballr.length]]);
      }
         ballr = `${selectr.size * 2}`;
      privacyb += `${eactn.length}`;
   }

  const escapeRegExp = (string: string) => {
    return string.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');

   if (3 == emptyk) {
      emptyk &= eactn.length;
   }
   let baidu5 = emptyk >= 5511592;
   do {
      emptyk <<= Math.min(Math.abs(parseInt(`${flyery}`)), 5);
      if (baidu5) {
         break;
      }
   } while ((bootsplashr.startsWith(`${emptyk}`)) && baidu5);
  };

   for (let s = 0; s < 3; s++) {
      bootsplashr = "3";
   }
   while (1 > bootsplashr.length || eactn != String.fromCharCode(119,0)) {
      eactn = "3";
      break;
   }
  const highlightText = (
    text: string,
    keyword1: string,
    keyword2: string = '',
  ) => {

   for (let s = 0; s < 3; s++) {
      eactn += `${foundQ.length * 1}`;
   }
   while (windu.length <= parseInt(`${dangerm}`)) {
      windu = `${1 + emptyk}`;
      break;
   }
    const escapedKeyword1 = escapeRegExp(keyword1);

   for (let n = 0; n < 1; n++) {
      eactn = `${windu.length}`;
   }
      actionsa = `${1 + privacyb.length}`;
    const escapedKeyword2 = escapeRegExp(keyword2);

      privacyb += `${parseInt(`${whistle3}`)}`;
      bootsplashr += `${bootsplashr.length * parseInt(`${flyery}`)}`;

    const parts = text.split(
      new RegExp(`(${escapedKeyword1}|${escapedKeyword2})`, 'gi'),
    );

      privacyb = `${bootsplashr.length}`;
   if (4 > (4 << (Math.min(1, eactn.length))) || 3 > (4 | eactn.length)) {
      middle4 /= Math.max(parseInt(`${flyery}`), 4);
   }

    return parts.map((part, index) => {
      if (
        part.toLowerCase() === keyword1.toLowerCase() ||
        (keyword2 && part.toLowerCase() === keyword2.toLowerCase())
      ) {

   let grayU = String.fromCharCode(111,108,113,121,52,51,52,105,99,0) == bootsplashr;
   do {
       let gmail5 = String.fromCharCode(106,95,55,51,95,112,97,110,100,105,110,103,0);
       let xvodY: Map<any, any> = new Map([[String.fromCharCode(120,95,52,54,95,102,111,117,114,99,99,0),98], [String.fromCharCode(115,104,111,114,116,101,110,95,97,95,57,48,0),332]]);
       let backu = 3;
       let modalj = String.fromCharCode(98,117,99,107,101,116,115,95,112,95,52,53,0);
          let greyR: Array<any> = [903, 889, 725];
          let basketballb = 0.0;
         xvodY = new Map([[`${xvodY.size}`, xvodY.size]]);
         greyR.push(greyR.length);
         basketballb -= parseFloat(`${greyR.length >> (Math.min(5, Math.abs(parseInt(`${basketballb}`))))}`);
      let watchl = String.fromCharCode(49,115,118,106,113,97,54,55,54,101,0) == gmail5;
      do {
          let type_hdU: Map<any, any> = new Map([[String.fromCharCode(99,101,114,116,105,102,105,99,97,116,101,115,95,97,95,49,53,0),false ], [String.fromCharCode(99,104,101,99,107,95,111,95,52,54,0),true ]]);
          let sheetz = String.fromCharCode(109,95,49,95,102,111,117,114,99,99,115,0);
          let googleV = String.fromCharCode(115,121,110,99,112,111,105,110,116,95,113,95,57,48,0);
          let matchK = 4;
         gmail5 += `${modalj.length}`;
         type_hdU.set(`${matchK}`, matchK / (Math.max(2, 6)));
         sheetz = "2";
         googleV += `${type_hdU.size}`;
         if (watchl) {
            break;
         }
      } while ((!gmail5.endsWith(`${backu}`)) && watchl);
      let thumbnailh = 8500626 >= xvodY.size;
      do {
          let bellc = String.fromCharCode(100,95,56,51,0);
          let wind1 = 1.0;
         xvodY = new Map([[`${wind1}`, parseInt(`${wind1}`)]]);
         bellc += `${bellc.length << (Math.min(Math.abs(3), 4))}`;
         if (thumbnailh) {
            break;
         }
      } while (thumbnailh && (5 > (5 * modalj.length)));
       let splash8 = 2.0;
         xvodY = new Map([[`${xvodY.size}`, backu << (Math.min(Math.abs(xvodY.size), 5))]]);
          let backwardH: Array<any> = [582, 569];
          let right2: Array<any> = [String.fromCharCode(118,95,49,95,116,100,108,116,0), String.fromCharCode(115,109,97,108,108,95,103,95,50,50,0), String.fromCharCode(109,95,49,53,95,97,108,108,111,99,97,116,111,114,115,0)];
          let balle = 5;
         xvodY.set(gmail5, 1);
         backwardH.push(1);
         right2 = [right2.length];
         balle /= Math.max(right2.length >> (Math.min(backwardH.length, 1)), 5);
       let zcopy_mQ = 1;
         modalj = "3";
      for (let k = 0; k < 2; k++) {
         backu += backu * 3;
      }
       let mathY = String.fromCharCode(108,105,98,114,115,118,103,95,105,95,53,55,0);
      if (backu <= 1) {
         backu <<= Math.min(Math.abs(xvodY.size), 1);
      }
       let stationsc = 5.0;
       let favicond = 2.0;
      bootsplashr += `${parseInt(`${middle4}`)}`;
      if (grayU) {
         break;
      }
   } while (grayU && (!bootsplashr.startsWith(windu)));
       let submitD: Array<any> = [795, 296, 562];
       let scheduleE: Array<any> = [788, 163];
         scheduleE = [1 >> (Math.min(1, submitD.length))];
      let contextD = scheduleE.length <= 5638651;
      do {
         scheduleE = [3];
         if (contextD) {
            break;
         }
      } while ((5 > (2 ^ scheduleE.length)) && contextD);
          let philippinesO = 3;
          let megaphoner: Array<any> = [319, 609];
         scheduleE.push(philippinesO % (Math.max(4, megaphoner.length)));
       let anytimeC = 4.0;
       let guideJ = 3.0;
          let product4 = 3.0;
          let resend0 = String.fromCharCode(109,111,100,105,102,121,95,116,95,52,48,0);
          let sendf = 0.0;
         anytimeC += parseFloat(`${parseInt(`${product4}`) - parseInt(`${sendf}`)}`);
         product4 *= resend0.length;
         resend0 += `${resend0.length}`;
         submitD = [parseInt(`${guideJ}`) | 3];
      emptyk += parseInt(`${whistle3}`);
        return (
          <Text
            key={index}
            style={{
              ...textVariants.body,
              color: colors.yellow,
              fontStyle: 'italic',
              fontWeight: '700',
              fontSize: 22,
            }}>
            {part}
          </Text>
        );
      } else {

      dangerm += parseFloat(`${parseInt(`${flyery}`)}`);
   if (privacyb.includes(`${foundQ.length}`)) {
      privacyb = `${(bootsplashr == String.fromCharCode(78,0) ? bootsplashr.length : privacyb.length)}`;
   }
        return (
          <Text
            key={index}
            style={{ ...textVariants.body, color: '#FFFFFF', fontSize: 20 }}>
            {part}
          </Text>
        );
      }
    });
  };

   let firebasee = 6603061.0 <= flyery;
   do {
       let thailandL = true;
       let ajaxb = String.fromCharCode(122,102,114,101,101,95,51,95,57,0);
       let sansL = 0;
         sansL *= 3;
      let configure0 = String.fromCharCode(113,100,97,118,121,48,0) == ajaxb;
      do {
         ajaxb = `${((thailandL ? 4 : 3) >> (Math.min(Math.abs(1), 1)))}`;
         if (configure0) {
            break;
         }
      } while (configure0 && (ajaxb.length > 5 || thailandL));
         ajaxb = `${(ajaxb.length - (thailandL ? 1 : 1))}`;
          let champion3 = String.fromCharCode(116,95,57,50,95,120,114,117,110,0);
          let rank4: Map<any, any> = new Map([[String.fromCharCode(98,95,54,49,95,97,97,99,100,101,99,116,97,98,0),626], [String.fromCharCode(98,112,117,116,115,95,103,95,53,0),445], [String.fromCharCode(115,112,108,105,116,116,101,114,95,111,95,57,56,0),826]]);
          let sellk: Map<any, any> = new Map([[String.fromCharCode(101,95,53,55,95,114,116,112,100,101,99,0),true ], [String.fromCharCode(121,95,52,48,95,97,116,114,97,99,100,97,116,97,0),false ], [String.fromCharCode(98,95,49,48,95,98,114,111,97,100,99,97,115,116,115,0),false ]]);
         sansL /= Math.max(2, 2);
         champion3 = `${sellk.size ^ 1}`;
         rank4.set(`${rank4.size}`, sellk.size);
          let condensedZ = String.fromCharCode(117,110,115,101,110,100,95,102,95,56,54,0);
          let screenn = String.fromCharCode(113,117,97,110,116,105,122,97,116,105,111,110,95,57,95,50,57,0);
         ajaxb = `${sansL}`;
         condensedZ = `${screenn.length}`;
         screenn += `${condensedZ.length >> (Math.min(4, screenn.length))}`;
         thailandL = sansL <= 23 && thailandL;
          let favicon7 = 1;
          let dicej: Array<any> = [24, 121, 348];
         sansL %= Math.max(4, dicej.length);
         favicon7 >>= Math.min(2, Math.abs(favicon7));
         dicej = [favicon7 ^ favicon7];
      if ((ajaxb.length - 3) >= 3 && 1 >= (sansL - 3)) {
         ajaxb = `${(3 >> (Math.min(3, Math.abs((thailandL ? 1 : 5)))))}`;
      }
         ajaxb = `${((thailandL ? 1 : 5))}`;
      flyery += parseFloat(`${privacyb.length}`);
      if (firebasee) {
         break;
      }
   } while ((flyery == 4.15) && firebasee);
       let ticked1 = false;
       let shared0 = 2;
       let inactivew = String.fromCharCode(114,101,115,112,111,110,115,101,115,95,120,95,54,54,0);
      let stara = 5906998 <= shared0;
      do {
          let clearM = String.fromCharCode(106,95,52,55,95,112,97,114,116,115,0);
          let subsL = String.fromCharCode(112,95,55,53,95,97,99,116,105,118,97,116,101,0);
          let clubw = String.fromCharCode(116,95,54,52,95,102,97,105,108,0);
         shared0 /= Math.max(subsL.length, 4);
         clearM += "2";
         subsL = `${2 * clearM.length}`;
         clubw += "1";
         if (stara) {
            break;
         }
      } while ((1 > (shared0 / 5) || !ticked1) && stara);
         ticked1 = String.fromCharCode(122,0) == inactivew;
      if (3 < (1 ^ shared0)) {
          let promotionj = String.fromCharCode(115,116,114,105,107,101,95,103,95,53,48,0);
          let catalogZ = String.fromCharCode(115,95,49,49,95,97,100,106,117,115,116,109,101,110,116,0);
         shared0 /= Math.max(3, catalogZ.length & shared0);
         promotionj = `${promotionj.length}`;
         catalogZ += `${promotionj.length | promotionj.length}`;
      }
      let gifty = 7414728 <= inactivew.length;
      do {
         inactivew = `${(shared0 % (Math.max(10, (ticked1 ? 2 : 2))))}`;
         if (gifty) {
            break;
         }
      } while ((5 < (inactivew.length * 4) && (4 * shared0) < 4) && gifty);
      let flyerC = inactivew.length >= 8714457;
      do {
         inactivew += "2";
         if (flyerC) {
            break;
         }
      } while ((inactivew.includes(`${ticked1}`)) && flyerC);
         ticked1 = inactivew.length == 81 || !ticked1;
      if (ticked1) {
         ticked1 = !ticked1 || 98 == shared0;
      }
      let rightt = ticked1 ? !ticked1 : ticked1;
      do {
         ticked1 = !ticked1;
         if (rightt) {
            break;
         }
      } while (rightt && (!ticked1));
      for (let h = 0; h < 1; h++) {
         inactivew = `${shared0}`;
      }
      windu = "1";
  return (
    <>
      <View
        style={{
          paddingTop: 10,
          paddingBottom: 15,
          flexDirection: 'column',
          justifyContent: 'center',
          alignItems: 'center',
          gap: 9,
        }}>
        <Text style={{ fontSize: 22, color: colors.yellow, fontWeight: '700' }}>
          双赢奖励
        </Text>

        <Text>{highlightText('每邀请1位好友 增加30天VIP', '1', '30')}</Text>

        <Text>{highlightText('好友获得30天VIP', '30')}</Text>
      </View>
    </>
  );
}

const styles = StyleSheet.create({});
