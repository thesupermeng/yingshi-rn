import React, { useEffect, useState } from 'react';
import { View, Text, StyleSheet, TextInput, Image } from 'react-native';

import { useTheme } from '@react-navigation/native';
export default function InviteStep() {
       let private_7u = 1;
    let networkh = String.fromCharCode(118,105,100,101,111,95,50,95,56,51,0);
    let libavcodec9 = 1.0;
    let zhengpianb: Map<any, any> = new Map([[String.fromCharCode(97,95,51,56,95,119,111,114,107,105,110,103,0),String.fromCharCode(114,95,50,56,95,100,115,100,112,99,109,0)], [String.fromCharCode(99,95,50,55,95,117,112,116,105,109,101,0),String.fromCharCode(108,95,56,95,108,105,98,115,112,101,101,120,0)]]);
    let penaltyh = String.fromCharCode(112,117,116,115,95,99,95,50,51,0);
    let backiconl = String.fromCharCode(120,95,56,53,95,108,101,115,115,0);
    let megaphone7: Map<any, any> = new Map([[String.fromCharCode(104,108,115,101,110,99,95,122,95,53,48,0),false ], [String.fromCharCode(115,95,49,49,95,115,116,97,116,105,115,116,105,99,0),true ]]);
    let arrowrightwithtailz = 2.0;
    let fullP: Map<any, any> = new Map([[String.fromCharCode(114,95,57,51,95,101,120,116,114,97,100,97,116,97,112,115,101,116,115,0),false ], [String.fromCharCode(112,101,101,114,99,111,110,110,101,99,116,105,111,110,95,101,95,49,0),true ]]);
    let usernamet = 0.0;
    let project8 = false;
    let darky: Map<any, any> = new Map([[String.fromCharCode(97,117,100,105,98,105,108,105,116,121,95,98,95,56,51,0),13], [String.fromCharCode(103,114,101,121,115,95,106,95,51,55,0),19], [String.fromCharCode(109,95,51,53,95,115,112,97,109,0),365]]);
    let langkey3 = 4;
    let commentZ = 4;
   if (!Array.from(zhengpianb.keys()).includes(`${private_7u}`)) {
      private_7u |= (String.fromCharCode(99,0) == networkh ? networkh.length : fullP.size);
   }
      networkh = `${megaphone7.size & zhengpianb.size}`;

  const { colors, textVariants, icons, spacing } = useTheme();

   if (1 == (private_7u + 4) && (arrowrightwithtailz + 3.62) == 5.55) {
      private_7u ^= megaphone7.size;
   }
      penaltyh = `${2 >> (Math.min(5, Math.abs(zhengpianb.size)))}`;

  const escapeRegExp = (string: string) => {
    return string.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');

   for (let w = 0; w < 2; w++) {
       let disconnectedM = String.fromCharCode(101,95,52,51,95,101,120,112,111,110,101,110,116,105,97,108,0);
       let fillA = String.fromCharCode(112,97,115,116,101,95,122,95,49,0);
      let assistn = fillA == String.fromCharCode(118,120,53,97,105,113,0);
      do {
          let iconcalendarq: Array<any> = [239, 292];
          let photor = String.fromCharCode(97,99,113,117,105,114,101,95,101,95,49,48,48,0);
         fillA = `${fillA.length + 3}`;
         iconcalendarq = [(photor == String.fromCharCode(104,0) ? photor.length : iconcalendarq.length)];
         if (assistn) {
            break;
         }
      } while ((disconnectedM != String.fromCharCode(75,0)) && assistn);
         disconnectedM += `${(String.fromCharCode(79,0) == fillA ? fillA.length : disconnectedM.length)}`;
         fillA = `${fillA.length / 2}`;
         disconnectedM += `${disconnectedM.length / 1}`;
      for (let j = 0; j < 2; j++) {
         disconnectedM = `${fillA.length}`;
      }
      while (disconnectedM.length == fillA.length) {
         disconnectedM += `${fillA.length - disconnectedM.length}`;
         break;
      }
      project8 = 27 >= private_7u || darky.size >= 27;
   }
   if (!penaltyh.includes(`${zhengpianb.size}`)) {
       let station8 = 5.0;
       let tempF = 2;
       let renewy: Array<any> = [422, 23, 747];
       let kickF: Array<any> = [530, 812];
       let heartR = 3;
         heartR ^= kickF.length;
      while ((5 / (Math.max(3, tempF))) > 3 || 1 > (kickF.length / (Math.max(5, 3)))) {
         tempF |= parseInt(`${station8}`) & 2;
         break;
      }
       let applex = String.fromCharCode(108,105,115,116,101,110,105,110,103,95,108,95,52,52,0);
      for (let j = 0; j < 1; j++) {
         renewy = [heartR];
      }
      zhengpianb.set(`${libavcodec9}`, 2);
   }
  };

   if (5.9 <= (usernamet * 1.2) && 4 <= (parseInt(`${usernamet}`) * private_7u)) {
      private_7u /= Math.max(((project8 ? 2 : 5) | parseInt(`${libavcodec9}`)), 1);
   }
   while (!Array.from(megaphone7.keys()).includes(`${darky.size}`)) {
      megaphone7.set(`${arrowrightwithtailz}`, 3 << (Math.min(5, Math.abs(zhengpianb.size))));
      break;
   }
  const highlightText = (
    text: string,
    keyword1: string,
    keyword2: string = '',
  ) => {

   while (Array.from(darky.keys()).includes(`${langkey3}`)) {
      darky.set(`${usernamet}`, 1 | parseInt(`${usernamet}`));
      break;
   }
      arrowrightwithtailz += parseFloat(`${backiconl.length}`);
    const escapedKeyword1 = escapeRegExp(keyword1);

      fullP = new Map([[`${zhengpianb.size}`, 1]]);
       let starL: Map<any, any> = new Map([[String.fromCharCode(108,105,98,115,114,116,95,53,95,52,49,0),String.fromCharCode(115,95,54,49,95,109,105,110,115,0)], [String.fromCharCode(116,95,50,51,95,115,99,97,110,0),String.fromCharCode(97,95,54,49,95,107,101,121,0)], [String.fromCharCode(97,114,99,104,105,118,105,110,103,95,106,95,52,55,0),String.fromCharCode(120,95,52,51,95,109,111,110,103,111,0)]]);
       let yellowredcardO = String.fromCharCode(102,111,117,114,120,109,95,122,95,55,54,0);
       let backiconZ = String.fromCharCode(114,95,50,49,0);
      for (let b = 0; b < 2; b++) {
         backiconZ += `${starL.size - 3}`;
      }
      for (let l = 0; l < 1; l++) {
         backiconZ = `${3 + starL.size}`;
      }
      let modulesS = String.fromCharCode(100,109,95,116,55,99,55,0) == yellowredcardO;
      do {
         yellowredcardO += `${2 << (Math.min(3, Math.abs(starL.size)))}`;
         if (modulesS) {
            break;
         }
      } while (modulesS && ((starL.size * yellowredcardO.length) <= 1 || 1 <= (yellowredcardO.length * starL.size)));
      if (yellowredcardO.length < 4) {
         yellowredcardO = `${backiconZ.length & yellowredcardO.length}`;
      }
          let topicb = 5.0;
         backiconZ += `${1 << (Math.min(4, Math.abs(starL.size)))}`;
         topicb += parseFloat(`${parseInt(`${topicb}`) * 3}`);
         yellowredcardO = `${starL.size >> (Math.min(yellowredcardO.length, 4))}`;
         starL = new Map([[`${starL.size}`, 2]]);
          let dependenciest: Array<any> = [String.fromCharCode(120,95,51,49,95,116,108,117,116,0), String.fromCharCode(101,95,52,54,95,103,111,98,98,108,101,0)];
          let tooltipsg = String.fromCharCode(119,95,53,55,95,97,99,101,108,112,0);
         backiconZ = `${backiconZ.length}`;
         dependenciest = [dependenciest.length / (Math.max(1, 1))];
         tooltipsg = `${(tooltipsg == String.fromCharCode(82,0) ? tooltipsg.length : dependenciest.length)}`;
      if (yellowredcardO != String.fromCharCode(78,0)) {
          let configure4 = String.fromCharCode(103,95,51,53,95,100,111,110,116,0);
          let stylesR: Map<any, any> = new Map([[String.fromCharCode(100,101,115,112,105,108,108,95,54,95,53,48,0),604], [String.fromCharCode(111,95,53,50,95,97,97,99,100,101,99,0),556]]);
          let basketballhometeamc = String.fromCharCode(118,95,52,55,95,99,111,101,102,115,0);
          let blacke: Map<any, any> = new Map([[String.fromCharCode(118,95,50,51,95,100,120,116,121,0),581], [String.fromCharCode(106,95,56,53,95,100,105,118,105,100,105,110,103,0),510], [String.fromCharCode(112,95,52,49,95,111,110,108,105,110,101,115,0),199]]);
         backiconZ = "3";
         configure4 = "3";
         stylesR = new Map([[`${blacke.size}`, blacke.size & configure4.length]]);
         basketballhometeamc = `${stylesR.size | configure4.length}`;
      }
      project8 = String.fromCharCode(72,0) == backiconZ;
    const escapedKeyword2 = escapeRegExp(keyword2);

   let basketballplayerplaceholder6 = networkh == String.fromCharCode(102,57,52,118,0);
   do {
       let username7 = String.fromCharCode(101,95,52,56,95,99,101,108,101,98,114,97,116,101,0);
       let iconeditj = String.fromCharCode(111,103,103,100,101,99,95,100,95,50,51,0);
       let dark5: Map<any, any> = new Map([[String.fromCharCode(121,95,57,53,95,100,109,97,99,0),511], [String.fromCharCode(99,104,97,108,108,101,110,103,101,95,103,95,48,0),720]]);
       let iconpipshrink0 = String.fromCharCode(115,121,109,101,118,101,110,95,112,95,56,52,0);
       let iconviewer2: Array<any> = [656, 833, 635];
         dark5.set(iconpipshrink0, iconeditj.length);
      let buffer_ = iconpipshrink0.length <= 8894444;
      do {
          let hook1 = 3.0;
          let loadingspinnerX = 3.0;
         iconpipshrink0 += `${(iconeditj == String.fromCharCode(50,0) ? iconeditj.length : username7.length)}`;
         hook1 -= parseFloat(`${parseInt(`${loadingspinnerX}`)}`);
         if (buffer_) {
            break;
         }
      } while ((1 > (dark5.size & iconpipshrink0.length) || (iconpipshrink0.length & 1) > 2) && buffer_);
         iconeditj += `${iconviewer2.length >> (Math.min(iconpipshrink0.length, 4))}`;
          let episodeB = 0.0;
          let stepq = 0;
          let langB = String.fromCharCode(109,97,114,107,101,100,95,48,95,57,57,0);
         iconpipshrink0 = `${stepq}`;
         episodeB /= Math.max(5, parseFloat(`${1}`));
         stepq += 3 % (Math.max(1, parseInt(`${episodeB}`)));
         langB = `${1 + parseInt(`${episodeB}`)}`;
          let teamdetailsbgK = String.fromCharCode(112,116,111,110,95,118,95,53,55,0);
          let iconadslinkL = true;
         username7 += `${iconeditj.length}`;
         teamdetailsbgK += `${teamdetailsbgK.length}`;
         iconadslinkL = (25 >= ((!iconadslinkL ? teamdetailsbgK.length : 25) / (Math.max(7, teamdetailsbgK.length))));
          let referrera = 0.0;
         iconpipshrink0 += `${iconeditj.length}`;
         referrera -= 1 + parseInt(`${referrera}`);
      for (let g = 0; g < 2; g++) {
          let main_l4: Array<any> = [755, 286];
          let successQ: Map<any, any> = new Map([[String.fromCharCode(120,95,50,57,95,100,105,115,97,98,108,101,115,0),570], [String.fromCharCode(116,97,107,101,95,105,95,56,56,0),500]]);
         iconpipshrink0 += `${iconviewer2.length}`;
         main_l4.push(3 + successQ.size);
         successQ.set(`${main_l4.length}`, main_l4.length ^ 2);
      }
      for (let y = 0; y < 3; y++) {
          let chinasameu = 2.0;
          let sourcep = String.fromCharCode(116,111,103,103,108,101,95,113,95,50,50,0);
          let whitebellI = String.fromCharCode(112,95,52,49,95,112,114,111,103,114,101,115,115,105,118,101,0);
         iconviewer2.push((iconeditj == String.fromCharCode(78,0) ? iconviewer2.length : iconeditj.length));
         chinasameu -= parseInt(`${chinasameu}`) << (Math.min(Math.abs(2), 1));
         sourcep += `${parseInt(`${chinasameu}`)}`;
         whitebellI = `${2 - parseInt(`${chinasameu}`)}`;
      }
      if (iconeditj.length < 3 || username7.length < 3) {
         iconeditj += `${2 | username7.length}`;
      }
         iconviewer2 = [(String.fromCharCode(57,0) == username7 ? iconviewer2.length : username7.length)];
      if (!iconpipshrink0.endsWith(`${dark5.size}`)) {
          let middleC = 0.0;
          let championk = 5.0;
          let traminiV: Map<any, any> = new Map([[String.fromCharCode(115,95,50,55,95,99,114,101,97,116,101,0),true ], [String.fromCharCode(112,98,108,111,99,107,115,95,99,95,57,49,0),false ], [String.fromCharCode(113,95,57,53,95,102,105,108,101,110,97,109,101,115,0),false ]]);
          let fillm = false;
          let kuaishou7 = 0;
         dark5.set(`${championk}`, parseInt(`${championk}`));
         middleC -= ((fillm ? 3 : 1) << (Math.min(Math.abs(3), 2)));
         traminiV.set(`${fillm}`, (kuaishou7 & (fillm ? 5 : 4)));
         kuaishou7 += kuaishou7 % 3;
      }
      for (let j = 0; j < 2; j++) {
          let renewo = 3;
          let informationu = String.fromCharCode(109,95,53,57,95,114,101,99,111,118,101,114,0);
          let whatsappA = true;
         dark5.set(iconpipshrink0, 2);
         renewo >>= Math.min(5, Math.abs(((whatsappA ? 5 : 5) & renewo)));
         informationu += `${informationu.length}`;
         whatsappA = (informationu.length + renewo) > 76;
      }
          let networkhM = 3.0;
          let quest_ = 4;
         iconviewer2.push(1 - iconeditj.length);
         networkhM -= parseFloat(`${3}`);
         quest_ /= Math.max(quest_ % (Math.max(parseInt(`${networkhM}`), 6)), 4);
       let scrollviewP = 0;
       let termsu = 0;
      if ((3 + termsu) > 4 && (3 + termsu) > 3) {
          let shareb = String.fromCharCode(105,110,110,101,114,95,112,95,51,55,0);
         termsu >>= Math.min(iconpipshrink0.length, 5);
         shareb += `${shareb.length}`;
      }
      networkh += `${(String.fromCharCode(51,0) == username7 ? parseInt(`${libavcodec9}`) : username7.length)}`;
      if (basketballplayerplaceholder6) {
         break;
      }
   } while ((networkh.endsWith(`${libavcodec9}`)) && basketballplayerplaceholder6);
   let positionfieldG = 5500730 <= darky.size;
   do {
      darky = new Map([[penaltyh, langkey3 - 1]]);
      if (positionfieldG) {
         break;
      }
   } while ((!Array.from(darky.keys()).includes(`${libavcodec9}`)) && positionfieldG);

    const parts = text.split(
      new RegExp(`(${escapedKeyword1}|${escapedKeyword2})`, 'gi'),
    );

      fullP = new Map([[`${langkey3}`, langkey3]]);
   if (!backiconl.endsWith(`${libavcodec9}`)) {
      libavcodec9 *= (langkey3 / (Math.max(8, (project8 ? 1 : 1))));
   }

    return parts.map((part, index) => {
      if (
        part.toLowerCase() === keyword1.toLowerCase() ||
        (keyword2 && part.toLowerCase() === keyword2.toLowerCase())
      ) {

   if (2.32 >= (arrowrightwithtailz * 4.58)) {
      arrowrightwithtailz += parseFloat(`${parseInt(`${arrowrightwithtailz}`) - megaphone7.size}`);
   }
   for (let i = 0; i < 2; i++) {
       let volumeV = String.fromCharCode(97,114,101,118,101,114,115,101,95,56,95,53,48,0);
         volumeV += `${volumeV.length + 2}`;
      for (let b = 0; b < 2; b++) {
         volumeV += `${(volumeV == String.fromCharCode(70,0) ? volumeV.length : volumeV.length)}`;
      }
         volumeV += `${(String.fromCharCode(77,0) == volumeV ? volumeV.length : volumeV.length)}`;
      commentZ -= fullP.size ^ 2;
   }
        return (
          <Text
            key={index}
            style={{
              ...textVariants.body,
              color: colors.primary,
              fontStyle: 'italic',
              fontWeight: '700',
              fontSize: 22,
            }}>
            {part}
          </Text>
        );
      } else {

      fullP.set(`${commentZ}`, 2);
      project8 = (zhengpianb.size >> (Math.min(5, Math.abs(langkey3)))) >= 83;
        return (
          <Text
            key={index}
            style={{ ...textVariants.body, color: '#FFFFFF', fontSize: 20 }}>
            {part}
          </Text>
        );
      }
    });
  };

   for (let s = 0; s < 1; s++) {
      commentZ &= 1 >> (Math.min(5, networkh.length));
   }
      megaphone7.set(`${langkey3}`, darky.size * langkey3);
  return (
    <>
      <View
        style={{
          paddingTop: 10,
          paddingBottom: 15,
          flexDirection: 'column',
          justifyContent: 'center',
          alignItems: 'center',
          gap: 9,
        }}>
        <Text style={{ fontSize: 22, color: colors.primary, fontWeight: '700' }}>
          双赢奖励
        </Text>

        <Text>{highlightText('每邀请1位好友 增加30天VIP', '1', '30')}</Text>

        <Text>{highlightText('好友获得30天VIP', '30')}</Text>
      </View>
    </>
  );
}

const styles = StyleSheet.create({});
