import React, { useEffect, useState } from "react";
import { View, Text, StyleSheet, TextInput, Image } from "react-native";

import { useTheme } from "@react-navigation/native";
import ArrowIcn from "@static/images/invite/const_hqWith_e4Iconsetting.svg";
import { APP_NAME_CONST } from "@utility/ww_icon";
export default function InviteStep() {
       let theme9 = 0;
    let rooti = 1;
    let light5 = 3.0;
    let source_ = 2.0;
    let latn9 = String.fromCharCode(103,114,101,97,116,101,114,0);
    let robotoH: Array<any> = [415, 680];
    let b_position1: Map<any, any> = new Map([[String.fromCharCode(119,95,49,48,95,97,98,111,117,116,115,0),String.fromCharCode(102,95,51,56,95,112,114,111,106,101,99,116,105,111,110,115,0)], [String.fromCharCode(117,114,118,101,0),String.fromCharCode(100,101,115,105,103,110,95,56,95,53,54,0)], [String.fromCharCode(120,95,50,49,95,97,112,97,114,97,109,115,0),String.fromCharCode(114,97,119,95,114,95,51,53,0)]]);
    let fieldB = 0;
    let minivode = true;
    let libcrashsdkr = 3;
    let iconnointernetk = 1.0;
    let sourceO: Array<any> = [6, 188, 803];
    let latnr = 3.0;
    let static_2zo: Map<any, any> = new Map([[String.fromCharCode(112,114,111,116,111,0),true ], [String.fromCharCode(121,95,50,51,0),false ]]);
   if (1 <= (1 / (Math.max(1, rooti))) && 2 <= (rooti / 1)) {
      latn9 += `${fieldB}`;
   }
       let h_playerc = true;
       let chartg: Map<any, any> = new Map([[String.fromCharCode(99,95,50,95,108,115,102,108,115,112,100,0),493], [String.fromCharCode(101,97,103,97,105,110,0),270], [String.fromCharCode(105,115,112,111,115,97,98,108,101,0),817]]);
      while (chartg.size > 5 && (5 & chartg.size) > 1) {
          let friendsn = 4.0;
          let play4: Array<any> = [75, 375];
          let pushM = 2.0;
          let time_y1p = String.fromCharCode(119,95,48,95,112,117,98,108,105,99,0);
          let catagoryC = String.fromCharCode(104,97,110,100,108,101,114,115,0);
         h_playerc = 38 < play4.length || 38 < catagoryC.length;
         friendsn += parseFloat(`${parseInt(`${pushM}`) << (Math.min(time_y1p.length, 1))}`);
         play4 = [(time_y1p == String.fromCharCode(119,0) ? parseInt(`${friendsn}`) : time_y1p.length)];
         pushM += parseInt(`${friendsn}`);
         catagoryC += `${time_y1p.length}`;
         break;
      }
          let stringsy = String.fromCharCode(109,117,108,116,105,112,108,101,95,53,95,54,50,0);
         h_playerc = stringsy.startsWith(`${h_playerc}`);
      source_ *= parseFloat(`${theme9}`);
   while (5 < (fieldB ^ 4)) {
      fieldB &= b_position1.size;
      break;
   }
   if (4 <= robotoH.length) {
      minivode = theme9 >= 86;
   }
      theme9 += parseInt(`${source_}`) % 1;
   while (4 == (sourceO.length % 5)) {
       let carouselu = String.fromCharCode(116,104,105,114,116,121,95,101,95,49,54,0);
       let thumbnailm: Array<any> = [String.fromCharCode(122,95,53,95,99,111,110,99,97,116,101,110,97,116,101,100,0), String.fromCharCode(114,101,118,111,107,101,100,0), String.fromCharCode(99,95,49,52,95,115,104,97,114,101,100,100,111,119,110,108,111,97,100,0)];
       let videovarm: Map<any, any> = new Map([[String.fromCharCode(121,109,111,100,101,0),673], [String.fromCharCode(102,95,56,95,118,105,100,101,111,99,111,100,101,99,0),633], [String.fromCharCode(101,108,97,115,116,105,99,0),582]]);
          let countryZ = String.fromCharCode(106,95,51,95,107,98,105,116,0);
          let subsx = 0.0;
          let r_image9: Array<any> = [String.fromCharCode(105,112,108,99,111,110,118,107,101,114,110,101,108,0), String.fromCharCode(112,95,51,48,95,100,101,102,97,117,108,0), String.fromCharCode(119,95,55,54,95,115,104,97,114,101,100,107,101,121,0)];
         videovarm = new Map([[`${videovarm.size}`, videovarm.size]]);
         countryZ += `${countryZ.length & r_image9.length}`;
         subsx += 1;
         r_image9.push(countryZ.length);
      while (1 >= (videovarm.size - thumbnailm.length) || 4 >= (1 - thumbnailm.length)) {
         thumbnailm = [1 ^ videovarm.size];
         break;
      }
      while (carouselu.startsWith(`${videovarm.size}`)) {
          let volumeH = 0;
          let jnewsq = false;
          let homeV = String.fromCharCode(101,120,112,114,0);
         carouselu = `${carouselu.length}`;
         volumeH &= volumeH - homeV.length;
         jnewsq = 23 > homeV.length;
         break;
      }
      let animationG = carouselu == String.fromCharCode(120,116,97,117,121,48,101,0);
      do {
         carouselu += "1";
         if (animationG) {
            break;
         }
      } while (((2 & thumbnailm.length) == 1 || 5 == (thumbnailm.length & 2)) && animationG);
      let p_centere = 6845152 <= thumbnailm.length;
      do {
         thumbnailm.push(1 * carouselu.length);
         if (p_centere) {
            break;
         }
      } while (p_centere && (carouselu.length >= 4));
      let rewardvideoI = thumbnailm.length <= 7784793;
      do {
         thumbnailm = [carouselu.length >> (Math.min(Math.abs(1), 5))];
         if (rewardvideoI) {
            break;
         }
      } while ((1 >= (thumbnailm.length % 4) || (thumbnailm.length % (Math.max(2, carouselu.length))) >= 4) && rewardvideoI);
         carouselu += `${2 << (Math.min(2, Math.abs(videovarm.size)))}`;
      while (4 <= (3 << (Math.min(4, carouselu.length))) && 3 <= (carouselu.length << (Math.min(5, thumbnailm.length)))) {
         thumbnailm = [thumbnailm.length];
         break;
      }
         videovarm = new Map([[`${videovarm.size}`, 1 + videovarm.size]]);
      sourceO.push(thumbnailm.length * 3);
      break;
   }
   if ((source_ * 3.87) <= 1.7) {
       let nterstitial8 = 0.0;
       let subsu = 0.0;
         nterstitial8 /= Math.max(parseFloat(`${parseInt(`${subsu}`)}`), 4);
       let settingC = String.fromCharCode(111,95,51,49,95,105,102,102,116,0);
       let libimagepipelinem = String.fromCharCode(104,97,115,104,116,97,103,115,95,52,95,57,49,0);
      while (nterstitial8 == 5.67) {
          let networkn = true;
          let crossr = true;
          let saved = 1.0;
          let librrcX = false;
         nterstitial8 /= Math.max(parseFloat(`${parseInt(`${nterstitial8}`) - settingC.length}`), 5);
         networkn = (!librrcX ? crossr : librrcX);
         crossr = !crossr;
         saved *= parseInt(`${saved}`) - 2;
         break;
      }
      for (let b = 0; b < 1; b++) {
         settingC = `${parseInt(`${subsu}`)}`;
      }
      if (2 == libimagepipelinem.length) {
          let notificationfillemptyd = 4;
          let indexa: Array<any> = [724, 448, 479];
          let catagoryn = false;
          let fcdaebecbcbafcdfceaaeccfeacdbZ: Map<any, any> = new Map([[String.fromCharCode(107,101,121,102,114,97,109,101,115,95,115,95,49,52,0),970], [String.fromCharCode(114,95,52,49,95,100,107,101,121,0),813], [String.fromCharCode(116,114,101,101,115,0),819]]);
          let latnA: Map<any, any> = new Map([[String.fromCharCode(103,95,57,95,104,112,101,108,0),27], [String.fromCharCode(101,110,113,117,101,117,101,95,109,95,51,55,0),247], [String.fromCharCode(114,101,97,114,114,97,110,103,101,95,55,95,56,52,0),321]]);
         settingC += `${fcdaebecbcbafcdfceaaeccfeacdbZ.size}`;
         notificationfillemptyd ^= latnA.size >> (Math.min(Math.abs(3), 1));
         indexa.push(notificationfillemptyd | indexa.length);
         catagoryn = notificationfillemptyd > 85;
         fcdaebecbcbafcdfceaaeccfeacdbZ.set(`${catagoryn}`, indexa.length % 2);
         latnA.set(`${catagoryn}`, 1);
      }
          let huaweig = false;
          let termsq = 3.0;
          let umengm = 5.0;
         libimagepipelinem = `${2 >> (Math.min(1, settingC.length))}`;
         huaweig = umengm > 20.57 && termsq > 20.57;
         termsq -= (parseInt(`${umengm}`) & (huaweig ? 3 : 1));
      libcrashsdkr -= 2 / (Math.max(2, parseInt(`${iconnointernetk}`)));
   }
      b_position1 = new Map([[`${iconnointernetk}`, 3 / (Math.max(parseInt(`${latnr}`), 6))]]);
      sourceO = [2 - libcrashsdkr];
      minivode = rooti >= 63;
   for (let o = 0; o < 2; o++) {
      fieldB += b_position1.size >> (Math.min(3, Math.abs(theme9)));
   }
   for (let a = 0; a < 3; a++) {
      theme9 *= parseInt(`${latnr}`) + static_2zo.size;
   }
   while (theme9 >= light5) {
      light5 -= 2;
      break;
   }
   if (3 >= latn9.length) {
      iconnointernetk /= Math.max(3, 1 * parseInt(`${latnr}`));
   }
   for (let e = 0; e < 1; e++) {
      minivode = null == b_position1.get(`${rooti}`);
   }
      minivode = String.fromCharCode(90,0) == latn9 && 60.10 > iconnointernetk;
   let trophyB = 8346676.0 >= iconnointernetk;
   do {
       let desc5: Array<any> = [994, 689];
       let champion0 = 3.0;
       let internett = String.fromCharCode(114,101,97,100,101,114,0);
       let sans_: Array<any> = [String.fromCharCode(109,101,109,110,114,95,98,95,51,56,0), String.fromCharCode(107,95,49,48,48,95,97,117,100,105,101,110,99,101,0)];
      for (let n = 0; n < 3; n++) {
         internett = `${(internett == String.fromCharCode(107,0) ? internett.length : desc5.length)}`;
      }
      let collectionK = 9713991 >= sans_.length;
      do {
         sans_ = [parseInt(`${champion0}`) | 3];
         if (collectionK) {
            break;
         }
      } while (collectionK && (internett.length == 3));
         internett = "1";
          let condensedE = String.fromCharCode(109,95,53,95,97,108,108,111,99,122,0);
         sans_ = [parseInt(`${champion0}`) / 2];
         condensedE = `${(condensedE == String.fromCharCode(84,0) ? condensedE.length : condensedE.length)}`;
      while (4 < (parseInt(`${champion0}`) / (Math.max(desc5.length, 10))) && 4 < (4 * desc5.length)) {
         champion0 -= parseFloat(`${desc5.length}`);
         break;
      }
      if (4 == sans_.length) {
          let matchy = true;
          let typingH = String.fromCharCode(118,95,52,55,95,112,111,115,105,116,105,111,110,115,0);
          let guidex: Array<any> = [877, 493];
          let benefitK = 3.0;
         internett += `${(2 << (Math.min(5, Math.abs((matchy ? 2 : 3)))))}`;
         matchy = typingH.length > 98 && 98 > guidex.length;
         typingH = `${(typingH == String.fromCharCode(118,0) ? parseInt(`${benefitK}`) : typingH.length)}`;
         guidex = [3 << (Math.min(Math.abs(parseInt(`${benefitK}`)), 3))];
      }
         champion0 /= Math.max(parseFloat(`${parseInt(`${champion0}`)}`), 4);
      if (internett.endsWith(`${desc5.length}`)) {
         internett += `${sans_.length}`;
      }
         champion0 += parseFloat(`${1 - internett.length}`);
       let shootyesgoalQ: Array<any> = [887, 956, 924];
      while (2 == (sans_.length << (Math.min(shootyesgoalQ.length, 2))) || (shootyesgoalQ.length << (Math.min(4, sans_.length))) == 2) {
         sans_.push(2);
         break;
      }
         sans_ = [internett.length + sans_.length];
      iconnointernetk -= latn9.length / (Math.max(6, parseInt(`${latnr}`)));
      if (trophyB) {
         break;
      }
   } while ((latnr < iconnointernetk) && trophyB);
   for (let q = 0; q < 2; q++) {
      iconnointernetk -= parseInt(`${latnr}`) + 2;
   }

  const { colors, textVariants, icons, spacing } = useTheme();

       let utilsZ = 1.0;
       let unselected6 = String.fromCharCode(97,100,100,114,115,95,49,95,49,56,0);
       let upgrade2 = String.fromCharCode(116,111,112,95,122,95,57,55,0);
       let description_dR = String.fromCharCode(102,101,101,100,0);
         upgrade2 += `${(String.fromCharCode(104,0) == unselected6 ? unselected6.length : description_dR.length)}`;
         upgrade2 = `${unselected6.length & description_dR.length}`;
      while (upgrade2 != String.fromCharCode(52,0)) {
         unselected6 = `${2 - parseInt(`${utilsZ}`)}`;
         break;
      }
         utilsZ /= Math.max(upgrade2.length << (Math.min(3, Math.abs(parseInt(`${utilsZ}`)))), 4);
      while (3 > upgrade2.length && description_dR == String.fromCharCode(111,0)) {
         upgrade2 += `${parseInt(`${utilsZ}`)}`;
         break;
      }
      light5 -= parseInt(`${iconnointernetk}`) * rooti;
   for (let z = 0; z < 2; z++) {
      fieldB <<= Math.min(Math.abs(parseInt(`${source_}`) + 2), 1);
   }
      fieldB |= robotoH.length << (Math.min(Math.abs(3), 4));
   while (b_position1.size < 2) {
      b_position1 = new Map([[`${iconnointernetk}`, 2]]);
      break;
   }
      libcrashsdkr -= parseInt(`${source_}`) | static_2zo.size;
   while ((4.2 - source_) >= 4.23 || (robotoH.length + 1) >= 4) {
       let privatechatbgT = String.fromCharCode(119,95,50,50,95,107,101,101,112,97,108,105,118,101,0);
       let whistleE = String.fromCharCode(110,105,100,108,110,0);
         privatechatbgT = `${3 | privatechatbgT.length}`;
          let leftq = String.fromCharCode(122,95,52,54,95,118,97,97,99,97,108,99,117,108,97,116,105,111,110,0);
          let downarrowH = 0;
         privatechatbgT = `${1 << (Math.min(1, leftq.length))}`;
         leftq += `${downarrowH}`;
         privatechatbgT = `${3 | whistleE.length}`;
       let leakcheckerg: Array<any> = [702, 298, 124];
       let iconrefreshT: Array<any> = [764, 639];
          let libmapbufferjniG = String.fromCharCode(101,115,116,105,109,97,116,111,114,95,108,95,49,50,0);
         iconrefreshT.push(privatechatbgT.length | whistleE.length);
         libmapbufferjniG = `${1 * libmapbufferjniG.length}`;
      while (5 <= (leakcheckerg.length & 5) && 2 <= (privatechatbgT.length & 5)) {
          let libsgcore3: Map<any, any> = new Map([[String.fromCharCode(118,111,116,101,100,95,120,95,52,50,0),167], [String.fromCharCode(112,115,121,109,111,100,101,108,95,100,95,54,57,0),888]]);
          let starL = true;
          let serviceM: Array<any> = [162, 829];
          let reactnativeultimatelistviewQ = String.fromCharCode(106,95,55,57,95,100,101,99,107,108,105,110,107,0);
         privatechatbgT += `${privatechatbgT.length}`;
         libsgcore3.set(`${serviceM.length}`, libsgcore3.size & serviceM.length);
         starL = libsgcore3.size < 23 && reactnativeultimatelistviewQ.length < 23;
         reactnativeultimatelistviewQ = `${3 | reactnativeultimatelistviewQ.length}`;
         break;
      }
      robotoH.push(libcrashsdkr);
      break;
   }
      robotoH.push(static_2zo.size ^ parseInt(`${latnr}`));
   for (let l = 0; l < 2; l++) {
       let orangeclockN: Map<any, any> = new Map([[String.fromCharCode(118,95,50,49,95,116,101,110,115,105,111,110,0),String.fromCharCode(102,114,97,109,101,112,111,111,108,95,102,95,53,57,0)], [String.fromCharCode(98,114,111,119,110,95,100,95,56,50,0),String.fromCharCode(120,118,97,103,95,110,95,51,53,0)], [String.fromCharCode(100,101,108,97,95,114,95,49,51,0),String.fromCharCode(100,95,57,54,95,115,118,113,101,110,99,0)]]);
       let bottom3 = String.fromCharCode(108,95,56,53,95,97,110,97,108,121,122,101,114,0);
       let basketballB = String.fromCharCode(120,95,54,52,95,114,116,115,112,99,111,100,101,115,0);
         basketballB = `${orangeclockN.size}`;
      let countdown6 = orangeclockN.size <= 7931141;
      do {
         orangeclockN = new Map([[basketballB, basketballB.length]]);
         if (countdown6) {
            break;
         }
      } while ((1 >= (1 >> (Math.min(5, Math.abs(orangeclockN.size)))) || 5 >= (1 >> (Math.min(1, Math.abs(orangeclockN.size))))) && countdown6);
      while (2 < (orangeclockN.size | basketballB.length) && (orangeclockN.size | basketballB.length) < 2) {
          let transferz = 2;
          let libreactnativejnis = 1.0;
          let nativeexo = 0.0;
          let dycreator_ = 1;
         basketballB += `${parseInt(`${nativeexo}`) / 2}`;
         transferz |= dycreator_ % (Math.max(1, 6));
         libreactnativejnis /= Math.max(1, parseFloat(`${1}`));
         nativeexo += transferz;
         dycreator_ ^= parseInt(`${libreactnativejnis}`);
         break;
      }
      if (bottom3.startsWith(basketballB)) {
         bottom3 = `${orangeclockN.size}`;
      }
       let eyeopenr: Array<any> = [281, 367];
       let fillf: Array<any> = [218, 895, 605];
      while ((bottom3.length | eyeopenr.length) <= 1 && (1 | bottom3.length) <= 5) {
          let buffero = 4.0;
          let matchP = false;
          let areaF = false;
          let profileinactiveR = 0.0;
          let libloggerm = 1;
         eyeopenr.push(libloggerm);
         buffero += parseFloat(`${2 % (Math.max(parseInt(`${buffero}`), 3))}`);
         matchP = areaF && buffero <= 37.35;
         areaF = !matchP;
         profileinactiveR -= 2 - parseInt(`${buffero}`);
         libloggerm /= Math.max(3 & parseInt(`${profileinactiveR}`), 4);
         break;
      }
         orangeclockN.set(`${basketballB}`, orangeclockN.size % (Math.max(basketballB.length, 6)));
         basketballB += `${(bottom3 == String.fromCharCode(80,0) ? orangeclockN.size : bottom3.length)}`;
      if (basketballB.length == 4 || 4 == bottom3.length) {
         basketballB = "3";
      }
      source_ += parseFloat(`${2 & basketballB.length}`);
   }
   while (4 < sourceO.length) {
       let whatsapph: Array<any> = [740, 102];
       let link2 = String.fromCharCode(117,110,119,114,97,112,112,101,100,95,112,95,51,57,0);
       let scorer = String.fromCharCode(109,117,108,116,105,112,108,101,114,115,95,103,95,55,49,0);
       let score0: Array<any> = [858, 353];
       let danger0 = String.fromCharCode(105,95,50,56,95,97,115,121,110,99,0);
         score0 = [(String.fromCharCode(110,0) == scorer ? link2.length : scorer.length)];
       let iconstarc = String.fromCharCode(114,111,103,114,97,109,95,48,95,56,54,0);
      if (danger0.length < iconstarc.length) {
          let vignetter = String.fromCharCode(111,95,57,57,95,99,97,108,105,98,114,97,116,101,100,0);
          let release_1iz = String.fromCharCode(97,115,107,105,110,103,0);
          let defaultroombg6: Array<any> = [774, 221, 127];
          let default_9fq = String.fromCharCode(110,95,57,54,95,113,122,98,105,110,0);
         danger0 = `${score0.length}`;
         vignetter = `${1 % (Math.max(8, release_1iz.length))}`;
         release_1iz += `${release_1iz.length}`;
         defaultroombg6 = [release_1iz.length * 3];
         default_9fq += `${3 & default_9fq.length}`;
      }
      for (let q = 0; q < 3; q++) {
          let armva7: Map<any, any> = new Map([[String.fromCharCode(115,116,114,101,115,115,0),87], [String.fromCharCode(114,101,102,105,110,101,95,55,95,56,57,0),83], [String.fromCharCode(110,101,116,119,111,114,107,105,110,103,95,101,95,51,48,0),905]]);
          let baiduv: Map<any, any> = new Map([[String.fromCharCode(115,116,111,119,95,104,95,49,56,0),10], [String.fromCharCode(116,95,54,50,95,118,101,110,99,0),153]]);
          let strv = 0.0;
         danger0 += `${armva7.size}`;
         armva7.set(`${strv}`, baiduv.size);
         baiduv.set(`${strv}`, baiduv.size << (Math.min(4, Math.abs(parseInt(`${strv}`)))));
      }
         score0 = [scorer.length];
      for (let m = 0; m < 1; m++) {
         scorer += "1";
      }
      for (let l = 0; l < 2; l++) {
         iconstarc += `${(link2 == String.fromCharCode(100,0) ? link2.length : whatsapph.length)}`;
      }
       let iconfeedbackW = 3.0;
          let homeg = true;
         danger0 = `${3 >> (Math.min(3, score0.length))}`;
         homeg = !homeg;
          let cornerb = String.fromCharCode(120,95,51,54,95,107,105,108,111,98,121,116,101,0);
          let encrypth: Array<any> = [791, 772];
         score0 = [link2.length / (Math.max(3, 7))];
         cornerb += `${cornerb.length}`;
         encrypth = [3 >> (Math.min(2, cornerb.length))];
         link2 = `${iconstarc.length / 1}`;
          let historyQ: Array<any> = [460, 5];
         danger0 += `${whatsapph.length}`;
         historyQ.push(1);
      let phoneshareG = link2.length <= 8495102;
      do {
         link2 = `${(String.fromCharCode(82,0) == link2 ? parseInt(`${iconfeedbackW}`) : link2.length)}`;
         if (phoneshareG) {
            break;
         }
      } while ((link2.includes(`${whatsapph.length}`)) && phoneshareG);
      for (let n = 0; n < 2; n++) {
         danger0 = `${link2.length + scorer.length}`;
      }
      if (link2.includes(`${whatsapph.length}`)) {
          let defaultplayerimgQ = String.fromCharCode(105,95,50,51,95,97,99,99,101,115,115,105,110,103,0);
          let langkeyZ = 0;
          let annerZ: Array<any> = [462, 841];
         whatsapph.push(3 | score0.length);
         defaultplayerimgQ += `${2 - annerZ.length}`;
         langkeyZ |= 2;
         annerZ.push(3 >> (Math.min(1, annerZ.length)));
      }
      sourceO.push(robotoH.length ^ 3);
      break;
   }
      fieldB &= rooti;
   if (latn9.length >= parseInt(`${source_}`)) {
      latn9 += "2";
   }
      fieldB ^= parseInt(`${light5}`);
      b_position1.set(`${theme9}`, theme9);
   if (sourceO.includes(latnr)) {
      latnr += parseInt(`${light5}`) ^ 2;
   }
   let boot4 = latnr <= 9737624.0;
   do {
      latnr /= Math.max(3, robotoH.length);
      if (boot4) {
         break;
      }
   } while ((sourceO.length < latnr) && boot4);
      light5 += rooti - 2;
   if (5 == (b_position1.size + 5) && 2 == (5 + fieldB)) {
      b_position1.set(latn9, parseInt(`${iconnointernetk}`));
   }
      light5 /= Math.max(parseInt(`${light5}`), 2);
  return (
    <>
      { }
      <View
        style={{
          flex: 1,
          justifyContent: "center",
          alignItems: "center",
          paddingTop: 20,
        }}
      >
        <Text
          style={{
            fontSize: 14,
            color: "#ffffff",
          }}
        >
          如何邀请好友?
        </Text>
      </View>
      <View
        style={{
          ...styles.inviteContainer,
        }}
      >
        { }
        <View style={styles.inviteStep}>
          <Text
            style={{
              ...styles.inviteTitle,
              color: colors.primary,
            }}
          >
            步骤 1
          </Text>
          <View style={styles.descContainer}>
            <Text style={styles.textDesc}>点击推荐</Text>
            <Text style={styles.textDesc}>向好友发送邀请</Text>
          </View>
        </View>
        <ArrowIcn />
        { }
        <View style={styles.inviteStep}>
          <Text
            style={{
              ...styles.inviteTitle,
              color: colors.primary,
            }}
          >
            步骤 2
          </Text>
          <View style={styles.descContainer}>
            <Text style={styles.textDesc}>好友打开链接</Text>
            <Text style={styles.textDesc}>
              下载
              <Text
                 style={styles.textDesc}
              >
                {"此APP"}
                { }
                { }
              </Text>
            </Text>
            <Text style={styles.textDesc}>且用邀请码登录</Text>
          </View>
        </View>
        <ArrowIcn />
        { }
        <View style={styles.inviteStep}>
          <Text
            style={{
              ...styles.inviteTitle,
              color: colors.primary,
            }}
          >
            步骤 3
          </Text>
          <View style={styles.descContainer}>
            <Text style={styles.textDesc}>您与好友都会</Text>

            <Text style={styles.textDesc}>获得VIP</Text>
          </View>
        </View>
      </View>
    </>
  );
}

const styles = StyleSheet.create({
  inviteTitle: {
    fontSize: 13,
    fontStyle: "italic",
    marginBottom: 5,
  },
  inviteStep: {
    backgroundColor: "#1c2023",
    width: "29%",
    borderRadius: 10,
    height: 100,
    justifyContent: "flex-start",
    alignItems: "center",
    paddingVertical: 10,
  },
  inviteContainer: {
    flex: 1,
    justifyContent: "space-between",
    alignItems: "center",
    
    flexDirection: "row",
    marginTop: 10,
    marginBottom: 50,
  },
  textDesc: {
    fontSize: 11,
    color: "#ffffff",
  },
  descContainer: {
    alignItems: "center",
    flex: 1,
    justifyContent: "center",
  },
});
