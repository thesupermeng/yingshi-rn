import React, { useEffect, useState } from "react";
import { View, Text, StyleSheet, TextInput, Image } from "react-native";

import { useTheme } from "@react-navigation/native";
import ArrowIcn from "@static/images/invite/bridgePredictiondefaultIconuser.svg";
import { APP_NAME_CONST } from "@utility/mayi_middleware_apps";
export default function InviteStep() {
       let moviesj = 4;
    let backwhitet = 4.0;
    let malaysiaz = String.fromCharCode(101,95,51,95,120,109,117,108,116,0);
    let popupu = String.fromCharCode(100,95,49,51,95,97,116,116,97,99,104,0);
    let libglogy = String.fromCharCode(114,97,100,98,95,110,95,55,56,0);
    let footballtrophy7 = String.fromCharCode(119,95,49,54,95,97,115,115,105,103,110,101,100,0);
    let iconstarX = String.fromCharCode(99,111,110,102,111,114,109,115,95,53,95,49,48,0);
    let smallK = 3;
    let streamingZ = false;
    let libbufferT: Array<any> = [632, 114];
    let gestureA = String.fromCharCode(109,117,116,101,120,95,100,95,52,56,0);
   for (let e = 0; e < 1; e++) {
       let navigationI = 3;
         navigationI -= navigationI >> (Math.min(Math.abs(navigationI), 5));
      let binddatasF = navigationI >= 9573894;
      do {
          let slider_ = 4;
         navigationI -= slider_ << (Math.min(Math.abs(navigationI), 5));
         if (binddatasF) {
            break;
         }
      } while ((4 <= navigationI) && binddatasF);
       let statse: Array<any> = [String.fromCharCode(104,101,120,98,115,95,56,95,55,50,0), String.fromCharCode(102,95,55,56,95,112,114,105,109,0), String.fromCharCode(102,108,97,116,110,101,115,115,95,54,95,50,50,0)];
      footballtrophy7 += `${parseInt(`${backwhitet}`) % (Math.max(malaysiaz.length, 3))}`;
   }
       let informationK = String.fromCharCode(102,95,57,95,114,97,100,97,114,0);
       let private_twV = true;
       let defaultroombgw: Map<any, any> = new Map([[String.fromCharCode(114,95,55,52,95,97,99,99,101,112,116,97,98,108,101,0),438], [String.fromCharCode(112,95,51,48,95,103,114,101,103,0),166]]);
       let otherh: Map<any, any> = new Map([[String.fromCharCode(119,95,57,56,95,112,97,99,107,97,103,101,115,0),990], [String.fromCharCode(108,95,50,55,95,105,110,116,102,114,0),178], [String.fromCharCode(117,100,105,111,95,51,95,56,49,0),405]]);
      let middleg = private_twV ? !private_twV : private_twV;
      do {
         private_twV = informationK.length >= defaultroombgw.size;
         if (middleg) {
            break;
         }
      } while (middleg && (informationK.length <= 1));
      for (let x = 0; x < 1; x++) {
          let umengO = true;
          let groupS = String.fromCharCode(104,105,115,116,111,114,105,101,115,95,102,95,57,53,0);
          let gesturesn = false;
          let button8 = String.fromCharCode(103,111,101,114,108,105,95,57,95,50,0);
         otherh = new Map([[`${otherh.size}`, otherh.size - 2]]);
         umengO = button8.length < 44;
         groupS = `${button8.length * 3}`;
         gesturesn = !umengO;
      }
      if (2 > defaultroombgw.size && 5 > (defaultroombgw.size & 2)) {
         defaultroombgw = new Map([[`${private_twV}`, ((private_twV ? 4 : 1))]]);
      }
      if (informationK.length >= 3 && !private_twV) {
          let basketballmatchdetailbg5: Map<any, any> = new Map([[String.fromCharCode(108,111,99,97,108,101,95,118,95,53,54,0),false ], [String.fromCharCode(109,117,109,98,97,105,95,106,95,56,56,0),true ]]);
          let shootyesgoalC = false;
          let malaysiaF = true;
          let homeloading4 = false;
          let p_playerN = String.fromCharCode(122,95,54,55,95,112,104,97,115,101,115,0);
         private_twV = p_playerN == String.fromCharCode(50,0) || basketballmatchdetailbg5.size <= 42;
         basketballmatchdetailbg5.set(`${malaysiaF}`, ((shootyesgoalC ? 1 : 2)));
         shootyesgoalC = homeloading4;
         p_playerN += `${((homeloading4 ? 3 : 3))}`;
      }
         defaultroombgw = new Map([[`${defaultroombgw.size}`, defaultroombgw.size]]);
      malaysiaz = `${malaysiaz.length << (Math.min(Math.abs(1), 5))}`;
   if (popupu != String.fromCharCode(109,0)) {
      footballtrophy7 = `${3 ^ moviesj}`;
   }
   if (5 > malaysiaz.length && popupu.length > 5) {
       let terms5 = String.fromCharCode(119,95,53,49,95,100,114,105,102,116,0);
       let starc: Array<any> = [631, 146];
         starc.push((String.fromCharCode(72,0) == terms5 ? starc.length : terms5.length));
          let fieldi: Array<any> = [560, 843, 783];
          let showA = 1.0;
         starc.push(terms5.length);
         fieldi.push(fieldi.length);
         showA /= Math.max(2, parseFloat(`${fieldi.length | parseInt(`${showA}`)}`));
       let libhermesz = 2;
         starc = [3];
          let activity7: Array<any> = [639, 753, 548];
          let modeo = 5.0;
          let iconarrowrightwhite3 = 3.0;
         libhermesz += 3;
         activity7.push(2 % (Math.max(9, parseInt(`${iconarrowrightwhite3}`))));
         modeo += activity7.length ^ parseInt(`${modeo}`);
         iconarrowrightwhite3 -= parseFloat(`${activity7.length}`);
          let detailD = String.fromCharCode(106,95,50,51,95,112,97,100,0);
          let subinD = String.fromCharCode(113,95,53,49,95,120,112,97,108,100,118,0);
         terms5 = `${subinD.length}`;
         detailD += "1";
         subinD += "3";
      popupu = `${1 ^ parseInt(`${backwhitet}`)}`;
   }
      footballtrophy7 = `${moviesj}`;
      popupu = `${3 & moviesj}`;
   if (2 > (moviesj ^ iconstarX.length)) {
       let mountingO: Map<any, any> = new Map([[String.fromCharCode(102,108,111,97,116,95,110,95,55,51,0),83], [String.fromCharCode(116,111,111,108,95,52,95,53,48,0),116], [String.fromCharCode(110,117,109,101,114,97,108,115,95,104,95,51,51,0),325]]);
       let valuesN = 3.0;
       let launchS = String.fromCharCode(112,95,57,95,104,101,118,99,100,101,99,0);
       let rewardO = String.fromCharCode(111,98,106,101,99,116,115,95,113,95,55,49,0);
          let canvasf = 1;
          let libruntimeexecutorV = 2;
          let cornerkickv = String.fromCharCode(103,95,56,49,95,117,110,97,114,121,0);
         valuesN /= Math.max(parseFloat(`${2 + parseInt(`${valuesN}`)}`), 2);
         canvasf %= Math.max(2, 4);
         libruntimeexecutorV >>= Math.min(4, cornerkickv.length);
         cornerkickv = `${libruntimeexecutorV}`;
         valuesN /= Math.max(2, parseFloat(`${launchS.length / (Math.max(3, 3))}`));
       let iconwatcha = 5;
       let defaultlogoS = 4;
         rewardO += `${rewardO.length & 3}`;
         valuesN -= parseFloat(`${parseInt(`${valuesN}`) ^ defaultlogoS}`);
         valuesN += parseFloat(`${1}`);
      while (4 > (5 & defaultlogoS)) {
         iconwatcha <<= Math.min(rewardO.length, 2);
         break;
      }
         iconwatcha /= Math.max(3, 2);
          let footballC = String.fromCharCode(98,95,56,95,97,97,100,0);
         valuesN -= parseFloat(`${parseInt(`${valuesN}`) * 2}`);
         footballC = `${footballC.length}`;
      let homeiconU = mountingO.size >= 5496024;
      do {
         mountingO.set(`${iconwatcha}`, mountingO.size);
         if (homeiconU) {
            break;
         }
      } while ((5.23 > (4.31 / (Math.max(1, valuesN))) && 2 > (mountingO.size ^ 4)) && homeiconU);
       let livesharex = 0;
       let reward1 = 3;
      for (let s = 0; s < 1; s++) {
         rewardO = "2";
      }
      iconstarX = "3";
   }
      malaysiaz += `${2 >> (Math.min(3, malaysiaz.length))}`;
   while (1.99 == (backwhitet / (Math.max(4.27, 1)))) {
      backwhitet *= popupu.length >> (Math.min(iconstarX.length, 1));
      break;
   }
      malaysiaz = `${(String.fromCharCode(80,0) == popupu ? parseInt(`${backwhitet}`) : popupu.length)}`;

  const { colors, textVariants, icons, spacing } = useTheme();

      footballtrophy7 = `${2 - footballtrophy7.length}`;
      backwhitet *= 3 % (Math.max(5, libglogy.length));
   while (!libglogy.endsWith(footballtrophy7)) {
      libglogy += `${moviesj % (Math.max(10, parseInt(`${backwhitet}`)))}`;
      break;
   }
      iconstarX += "2";
      malaysiaz += `${popupu.length + 2}`;
      backwhitet -= popupu.length;
       let subsp = 4;
         subsp >>= Math.min(Math.abs(1), 3);
         subsp ^= subsp & subsp;
      for (let j = 0; j < 1; j++) {
          let carousel5: Map<any, any> = new Map([[String.fromCharCode(104,112,105,99,95,118,95,57,52,0),1000], [String.fromCharCode(121,95,52,52,95,98,121,116,101,99,111,100,101,0),608]]);
          let matchesC = String.fromCharCode(103,95,49,52,95,101,110,99,111,100,97,98,108,101,115,0);
         subsp += carousel5.size;
         carousel5.set(matchesC, matchesC.length ^ matchesC.length);
      }
      footballtrophy7 = `${smallK}`;
      popupu = `${footballtrophy7.length}`;
       let forwardE = String.fromCharCode(97,114,114,97,121,95,102,95,49,54,0);
       let predictionbannerX = String.fromCharCode(114,95,52,48,95,100,101,113,117,97,110,116,0);
      for (let y = 0; y < 1; y++) {
         predictionbannerX += "1";
      }
         predictionbannerX = "2";
      let liveR = predictionbannerX == String.fromCharCode(48,51,120,104,111,0);
      do {
         predictionbannerX = `${1 >> (Math.min(2, forwardE.length))}`;
         if (liveR) {
            break;
         }
      } while (liveR && (forwardE != String.fromCharCode(106,0) && predictionbannerX == String.fromCharCode(104,0)));
       let gemfileV = 0.0;
       let icondefaultthumbnail2 = 2.0;
      for (let d = 0; d < 2; d++) {
         predictionbannerX = `${forwardE.length}`;
      }
      if (forwardE == String.fromCharCode(121,0)) {
         predictionbannerX = `${parseInt(`${gemfileV}`) % 3}`;
      }
      moviesj |= 2;
   let description_xa = 9852343 >= malaysiaz.length;
   do {
      malaysiaz = `${1 * popupu.length}`;
      if (description_xa) {
         break;
      }
   } while ((malaysiaz.includes(`${moviesj}`)) && description_xa);
  return (
    <>
      { }
      <View
        style={{
          flex: 1,
          justifyContent: "center",
          alignItems: "center",
          paddingTop: 20,
        }}
      >
        <Text
          style={{
            fontSize: 14,
            color: "#ffffff",
          }}
        >
          如何邀请好友?
        </Text>
      </View>
      <View
        style={{
          ...styles.inviteContainer,
        }}
      >
        { }
        <View style={styles.inviteStep}>
          <Text
            style={{
              ...styles.inviteTitle,
              color: colors.primary,
            }}
          >
            步骤 1
          </Text>
          <View style={styles.descContainer}>
            <Text style={styles.textDesc}>点击推荐</Text>
            <Text style={styles.textDesc}>向好友发送邀请</Text>
          </View>
        </View>
        <ArrowIcn />
        { }
        <View style={styles.inviteStep}>
          <Text
            style={{
              ...styles.inviteTitle,
              color: colors.primary,
            }}
          >
            步骤 2
          </Text>
          <View style={styles.descContainer}>
            <Text style={styles.textDesc}>好友打开链接</Text>
            <Text style={styles.textDesc}>
              下载
              <Text
                 style={styles.textDesc}
              >
                {"此APP"}
                { }
                { }
              </Text>
            </Text>
            <Text style={styles.textDesc}>且用邀请码登录</Text>
          </View>
        </View>
        <ArrowIcn />
        { }
        <View style={styles.inviteStep}>
          <Text
            style={{
              ...styles.inviteTitle,
              color: colors.primary,
            }}
          >
            步骤 3
          </Text>
          <View style={styles.descContainer}>
            <Text style={styles.textDesc}>您与好友都会</Text>

            <Text style={styles.textDesc}>获得VIP</Text>
          </View>
        </View>
      </View>
    </>
  );
}

const styles = StyleSheet.create({
  inviteTitle: {
    fontSize: 13,
    fontStyle: "italic",
    marginBottom: 5,
  },
  inviteStep: {
    backgroundColor: "#1c2023",
    width: "29%",
    borderRadius: 10,
    height: 100,
    justifyContent: "flex-start",
    alignItems: "center",
    paddingVertical: 10,
  },
  inviteContainer: {
    flex: 1,
    justifyContent: "space-between",
    alignItems: "center",
    
    flexDirection: "row",
    marginTop: 10,
    marginBottom: 50,
  },
  textDesc: {
    fontSize: 11,
    color: "#ffffff",
  },
  descContainer: {
    alignItems: "center",
    flex: 1,
    justifyContent: "center",
  },
});
