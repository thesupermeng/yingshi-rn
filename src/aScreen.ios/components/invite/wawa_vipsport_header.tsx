import React, { useEffect, useState } from 'react';
import { View, Text, StyleSheet, TextInput, Image } from 'react-native';

import { useTheme } from '@react-navigation/native';
import ArrowIcn from '@static/images/invite/managerActionsSina.svg';
export default function InviteStep() {
       let topon8: Map<any, any> = new Map([[String.fromCharCode(103,101,116,119,105,110,116,105,109,101,111,102,100,97,121,0),0], [String.fromCharCode(119,105,110,115,111,99,107,0),817]]);
    let mutedN = 2.0;
    let androidV = 1.0;
    let iconlogoutr = 3.0;
    let m_unlockF = String.fromCharCode(114,101,116,117,114,110,101,100,0);
    let iconstarh = 4.0;
    let become1: Map<any, any> = new Map([[String.fromCharCode(99,111,110,100,0),String.fromCharCode(115,117,109,0)], [String.fromCharCode(112,108,97,105,110,0),String.fromCharCode(101,110,99,111,100,97,98,108,101,115,0)], [String.fromCharCode(112,107,101,121,0),String.fromCharCode(98,105,114,116,104,100,97,116,101,0)]]);
    let dialogy = String.fromCharCode(99,108,97,115,115,105,99,0);
    let smallorangemanX = 3.0;
    let inouttargetyellowG = String.fromCharCode(97,108,101,114,116,0);
      m_unlockF = `${2 << (Math.min(3, m_unlockF.length))}`;
      m_unlockF = "1";

  const { colors, textVariants, icons, spacing } = useTheme();

      androidV *= parseInt(`${androidV}`) % (Math.max(parseInt(`${smallorangemanX}`), 3));
      become1 = new Map([[m_unlockF, dialogy.length]]);

  const escapeRegExp = (string: string) => {
    return string.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');

      iconstarh += (parseFloat(`${m_unlockF == String.fromCharCode(117,0) ? parseInt(`${iconlogoutr}`) : m_unlockF.length}`));
      iconstarh *= parseFloat(`${parseInt(`${iconlogoutr}`) * parseInt(`${androidV}`)}`);
  };

   if (!Array.from(topon8.keys()).includes(`${become1.size}`)) {
      topon8 = new Map([[`${topon8.size}`, parseInt(`${iconlogoutr}`)]]);
   }
   let react3 = iconlogoutr <= 8314672.0;
   do {
      iconlogoutr -= parseInt(`${mutedN}`) * 3;
      if (react3) {
         break;
      }
   } while (react3 && (5.40 >= (mutedN / (Math.max(iconlogoutr, 1))) || (mutedN / (Math.max(9, iconlogoutr))) >= 5.40));
  const highlightText = (
    text: string,
    keyword1: string,
    keyword2: string = '',
  ) => {

      m_unlockF = `${parseInt(`${mutedN}`)}`;
      androidV /= Math.max(3, 2);
    const escapedKeyword1 = escapeRegExp(keyword1);

      smallorangemanX *= 3 >> (Math.min(3, Math.abs(parseInt(`${mutedN}`))));
       let lessn = String.fromCharCode(108,111,103,115,116,101,114,101,111,0);
      if (lessn != String.fromCharCode(52,0) || lessn != String.fromCharCode(74,0)) {
         lessn = `${(lessn == String.fromCharCode(75,0) ? lessn.length : lessn.length)}`;
      }
       let baiduadsX = String.fromCharCode(111,117,116,108,105,110,101,0);
          let runtimeschedulerH = 3.0;
          let humidityL = String.fromCharCode(112,108,97,105,110,116,101,120,116,0);
         baiduadsX += `${baiduadsX.length * humidityL.length}`;
         runtimeschedulerH *= parseFloat(`${parseInt(`${runtimeschedulerH}`)}`);
         humidityL = `${parseInt(`${runtimeschedulerH}`)}`;
      iconlogoutr -= parseInt(`${smallorangemanX}`) - inouttargetyellowG.length;
    const escapedKeyword2 = escapeRegExp(keyword2);

   if (1 == (topon8.size ^ m_unlockF.length)) {
      topon8.set(`${iconstarh}`, 1 / (Math.max(10, parseInt(`${mutedN}`))));
   }
   while (2.93 > (2 - iconstarh)) {
      androidV -= 2 + become1.size;
      break;
   }

    const parts = text.split(
      new RegExp(`(${escapedKeyword1}|${escapedKeyword2})`, 'gi'),
    );

      mutedN += parseInt(`${iconstarh}`) / (Math.max(10, m_unlockF.length));
       let livenodatabgimgh: Array<any> = [33, 162, 198];
       let wifirouterM = String.fromCharCode(102,97,118,111,114,105,116,101,0);
       let countdownc: Map<any, any> = new Map([[String.fromCharCode(98,117,102,101,114,0),442], [String.fromCharCode(97,115,100,107,0),482]]);
          let jnews0 = String.fromCharCode(112,108,97,110,97,114,0);
          let renewK: Array<any> = [144, 701];
          let dialog4 = String.fromCharCode(115,97,108,116,0);
         livenodatabgimgh.push(3 >> (Math.min(1, wifirouterM.length)));
         jnews0 += `${dialog4.length << (Math.min(Math.abs(3), 1))}`;
         renewK.push(dialog4.length);
      let tooltipsp = countdownc.size >= 8723645;
      do {
         countdownc = new Map([[`${countdownc.size}`, countdownc.size | wifirouterM.length]]);
         if (tooltipsp) {
            break;
         }
      } while ((Array.from(countdownc.values()).includes(livenodatabgimgh.length)) && tooltipsp);
      for (let k = 0; k < 3; k++) {
         wifirouterM += `${wifirouterM.length ^ 1}`;
      }
         countdownc.set(wifirouterM, 1 + wifirouterM.length);
      let related1 = String.fromCharCode(53,111,118,0) == wifirouterM;
      do {
         wifirouterM = `${livenodatabgimgh.length}`;
         if (related1) {
            break;
         }
      } while ((5 == (3 + countdownc.size) || 4 == (countdownc.size + 3)) && related1);
      let homeloadingA = 9436740 <= livenodatabgimgh.length;
      do {
         livenodatabgimgh.push(wifirouterM.length * livenodatabgimgh.length);
         if (homeloadingA) {
            break;
         }
      } while ((3 >= countdownc.size) && homeloadingA);
         wifirouterM += `${livenodatabgimgh.length & 2}`;
      if (3 >= (wifirouterM.length / 1) || (wifirouterM.length / 1) >= 5) {
         countdownc.set(`${livenodatabgimgh.length}`, countdownc.size >> (Math.min(livenodatabgimgh.length, 1)));
      }
      if ((wifirouterM.length - livenodatabgimgh.length) < 1 && (wifirouterM.length - livenodatabgimgh.length) < 1) {
         wifirouterM = `${(String.fromCharCode(99,0) == wifirouterM ? countdownc.size : wifirouterM.length)}`;
      }
      m_unlockF = `${dialogy.length}`;

    return parts.map((part, index) => {
      if (
        part.toLowerCase() === keyword1.toLowerCase() ||
        (keyword2 && part.toLowerCase() === keyword2.toLowerCase())
      ) {

   if (2.2 > androidV) {
      mutedN += (m_unlockF == String.fromCharCode(80,0) ? m_unlockF.length : parseInt(`${iconlogoutr}`));
   }
      become1.set(m_unlockF, 1);
        return (
          <Text
            key={index}
            style={{
              ...textVariants.body,
              color: colors.yellow,
              fontStyle: 'italic',
              fontWeight: '700',
              fontSize: 22,
            }}>
            {part}
          </Text>
        );
      } else {

   for (let r = 0; r < 2; r++) {
      m_unlockF += `${parseInt(`${iconstarh}`) * become1.size}`;
   }
   while (dialogy.startsWith(`${topon8.size}`)) {
      topon8.set(`${iconstarh}`, parseInt(`${iconstarh}`));
      break;
   }
        return (
          <Text
            key={index}
            style={{ ...textVariants.body, color: '#FFFFFF', fontSize: 20 }}>
            {part}
          </Text>
        );
      }
    });
  };

   let typesZ = iconlogoutr <= 8764452.0;
   do {
       let direct5: Map<any, any> = new Map([[String.fromCharCode(114,100,111,112,116,0),String.fromCharCode(105,109,112,111,114,116,97,98,108,101,100,0)], [String.fromCharCode(115,109,101,97,114,0),String.fromCharCode(115,117,98,106,101,99,116,115,0)]]);
       let jingdongB = false;
         jingdongB = direct5.get(`${jingdongB}`) == null;
      for (let r = 0; r < 3; r++) {
          let searchr = String.fromCharCode(99,97,116,101,103,111,114,121,0);
         jingdongB = (((!jingdongB ? 51 : searchr.length) * searchr.length) <= 51);
      }
          let modulesG = String.fromCharCode(98,108,117,101,116,111,111,116,104,0);
          let pangleE = String.fromCharCode(108,111,110,103,116,101,114,109,0);
         jingdongB = (72 < ((!jingdongB ? modulesG.length : 72) % (Math.max(modulesG.length, 2))));
         pangleE += `${pangleE.length}`;
       let lang7 = 4;
       let liveendmodallogoo = 5;
       let iconclosewhitebgO = 4.0;
          let skipl = String.fromCharCode(99,117,116,111,117,116,0);
          let backiconc = 1.0;
         iconclosewhitebgO -= 1;
         skipl += `${parseInt(`${backiconc}`) | skipl.length}`;
         backiconc += (String.fromCharCode(104,0) == skipl ? skipl.length : parseInt(`${backiconc}`));
      iconlogoutr *= topon8.size;
      if (typesZ) {
         break;
      }
   } while ((Array.from(topon8.values()).includes(iconlogoutr)) && typesZ);
   if (Array.from(topon8.values()).includes(mutedN)) {
       let playlist4 = String.fromCharCode(120,117,116,105,108,0);
       let sinaL = 2.0;
       let clubS: Map<any, any> = new Map([[String.fromCharCode(105,115,112,111,114,116,0),243], [String.fromCharCode(110,112,114,111,98,101,0),419]]);
      for (let y = 0; y < 1; y++) {
         sinaL *= parseFloat(`${playlist4.length}`);
      }
         sinaL -= parseFloat(`${1 << (Math.min(1, Math.abs(clubS.size)))}`);
      while (parseFloat(`${playlist4.length}`) < sinaL) {
         sinaL += parseFloat(`${3 | playlist4.length}`);
         break;
      }
         playlist4 = "1";
      for (let d = 0; d < 3; d++) {
          let libreactnativejnin = 5.0;
         playlist4 = `${1 % (Math.max(3, parseInt(`${sinaL}`)))}`;
         libreactnativejnin -= parseFloat(`${parseInt(`${libreactnativejnin}`)}`);
      }
       let floaterV = String.fromCharCode(115,100,97,108,108,111,99,120,0);
          let benefitJ: Map<any, any> = new Map([[String.fromCharCode(102,97,118,101,0),224], [String.fromCharCode(101,121,101,115,0),717]]);
          let notificationfilledX = 2;
         clubS = new Map([[playlist4, playlist4.length / 1]]);
         benefitJ = new Map([[`${benefitJ.size}`, benefitJ.size * notificationfilledX]]);
         notificationfilledX ^= benefitJ.size * notificationfilledX;
      if ((playlist4.length >> (Math.min(Math.abs(4), 3))) >= 4 || 5 >= (4 + parseInt(`${sinaL}`))) {
          let rightq = String.fromCharCode(101,116,104,101,114,0);
         playlist4 += `${3 - clubS.size}`;
         rightq += `${rightq.length}`;
      }
          let arrowrightwithtailU = 5.0;
          let gradlew0 = String.fromCharCode(103,101,110,101,115,105,115,0);
         playlist4 = `${clubS.size << (Math.min(Math.abs(3), 5))}`;
         arrowrightwithtailU /= Math.max(3, parseFloat(`${1 ^ parseInt(`${arrowrightwithtailU}`)}`));
         gradlew0 = `${(gradlew0 == String.fromCharCode(51,0) ? gradlew0.length : parseInt(`${arrowrightwithtailU}`))}`;
      topon8.set(`${smallorangemanX}`, parseInt(`${smallorangemanX}`) | 3);
   }
  return (
    <>
      <View
        style={{
          paddingTop: 10,
          paddingBottom: 15,
          flexDirection: 'column',
          justifyContent: 'center',
          alignItems: 'center',
          gap: 9,
        }}>
        <Text style={{ fontSize: 22, color: colors.yellow, fontWeight: '700' }}>
          双赢奖励
        </Text>

        <Text>{highlightText('每邀请1位好友 增加30天VIP', '1', '30')}</Text>

        <Text>{highlightText('好友获得30天VIP', '30')}</Text>
      </View>
    </>
  );
}

const styles = StyleSheet.create({});
