import React, { useEffect, useState } from 'react';
import { View, Text, StyleSheet, TextInput, Image } from 'react-native';

import { useTheme } from '@react-navigation/native';
import ArrowIcn from '@static/images/invite/bridgePredictiondefaultIconuser.svg';
export default function InviteStep() {
       let iconclosewhitewithbgo = 5.0;
    let appleW = String.fromCharCode(106,112,101,103,105,110,116,95,110,95,51,56,0);
    let downloader2 = true;
    let optionse = String.fromCharCode(121,95,56,95,112,111,115,108,105,115,116,0);
    let appleZ: Array<any> = [516, 980];
    let k_position9: Array<any> = [548, 894];
    let greenf = 2.0;
    let otherv = String.fromCharCode(98,97,99,107,103,114,117,110,100,95,53,95,53,52,0);
    let node0 = 4;
    let phone1: Map<any, any> = new Map([[String.fromCharCode(114,95,53,55,95,107,101,101,112,0),732], [String.fromCharCode(100,99,116,120,95,112,95,56,48,0),349]]);
    let register_jwo = 3.0;
    let ajaxi: Array<any> = [859, 821];
    let iconclosewhitebgR = 2.0;
    let zooml = 3;
      greenf *= ((downloader2 ? 3 : 2));
       let pathE = String.fromCharCode(115,105,109,112,108,101,116,105,109,101,111,117,116,95,110,95,56,57,0);
      for (let j = 0; j < 2; j++) {
         pathE += `${(String.fromCharCode(104,0) == pathE ? pathE.length : pathE.length)}`;
      }
      while (5 > pathE.length) {
         pathE += `${pathE.length}`;
         break;
      }
      for (let h = 0; h < 2; h++) {
          let readI = 5;
          let episodeD = true;
          let mbbannerk = 0.0;
          let libreactperfloggerjniX = 1;
         pathE += `${readI}`;
         readI ^= libreactperfloggerjniX;
         episodeD = mbbannerk >= 100.30 && 62 >= libreactperfloggerjniX;
         mbbannerk -= parseInt(`${mbbannerk}`);
      }
      register_jwo -= parseFloat(`${3 >> (Math.min(1, Math.abs(parseInt(`${iconclosewhitewithbgo}`))))}`);

  const { colors, textVariants, icons, spacing } = useTheme();

   if (4 == (parseInt(`${register_jwo}`) + 2) && (register_jwo + 4.48) == 2.45) {
      optionse += `${appleW.length << (Math.min(Math.abs(1), 4))}`;
   }
       let termsi = true;
       let banners = 1.0;
          let scheduler: Array<any> = [978, 16];
         termsi = 1.24 < banners;
         scheduler = [scheduler.length];
          let privatechatbgq = 4.0;
         termsi = !termsi;
         privatechatbgq *= parseFloat(`${1}`);
         termsi = termsi && banners > 74.86;
       let launcherc = 3.0;
       let malaysia1 = 4.0;
      for (let p = 0; p < 3; p++) {
          let accepted7: Map<any, any> = new Map([[String.fromCharCode(119,95,53,53,95,110,117,108,108,97,98,108,101,114,101,115,111,108,118,101,100,0),true ], [String.fromCharCode(115,116,114,102,95,111,95,50,0),false ]]);
         banners += parseFloat(`${parseInt(`${launcherc}`)}`);
         accepted7.set(`${accepted7.size}`, accepted7.size | 3);
      }
         launcherc *= parseInt(`${banners}`) >> (Math.min(3, Math.abs(2)));
      appleZ = [node0 ^ phone1.size];

  const escapeRegExp = (string: string) => {
    return string.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');

   for (let g = 0; g < 3; g++) {
      register_jwo /= Math.max((parseFloat(`${optionse == String.fromCharCode(66,0) ? parseInt(`${register_jwo}`) : optionse.length}`)), 2);
   }
   if (optionse.length < k_position9.length) {
      optionse += `${node0}`;
   }
  };

      iconclosewhitewithbgo -= (parseFloat(`${String.fromCharCode(51,0) == otherv ? parseInt(`${register_jwo}`) : otherv.length}`));
      node0 <<= Math.min(5, Math.abs(otherv.length << (Math.min(Math.abs(2), 1))));
  const highlightText = (
    text: string,
    keyword1: string,
    keyword2: string = '',
  ) => {

   for (let r = 0; r < 2; r++) {
      k_position9.push((phone1.size >> (Math.min(2, Math.abs((downloader2 ? 5 : 4))))));
   }
       let reactnavigationW = String.fromCharCode(114,101,103,105,115,116,101,114,95,106,95,49,50,0);
       let controlL: Map<any, any> = new Map([[String.fromCharCode(97,112,109,116,101,115,116,95,49,95,54,54,0),true ], [String.fromCharCode(102,105,108,116,101,114,115,95,103,95,50,56,0),true ], [String.fromCharCode(108,95,53,51,95,110,111,99,104,101,99,107,0),true ]]);
      for (let d = 0; d < 2; d++) {
          let blacklist_ = false;
          let arrowrightwithtaild = String.fromCharCode(117,118,108,99,0);
         reactnavigationW += `${reactnavigationW.length ^ 1}`;
         blacklist_ = arrowrightwithtaild.length > 71 && arrowrightwithtaild.length > 71;
      }
         controlL.set(`${reactnavigationW}`, controlL.size - reactnavigationW.length);
       let moviesb = 4.0;
       let videobufferloadingJ = 0.0;
          let mimoz: Array<any> = [String.fromCharCode(108,95,51,48,95,99,109,105,111,0), String.fromCharCode(111,112,116,105,111,110,95,48,95,54,51,0)];
         controlL.set(`${videobufferloadingJ}`, (String.fromCharCode(52,0) == reactnavigationW ? parseInt(`${videobufferloadingJ}`) : reactnavigationW.length));
         mimoz.push(mimoz.length);
         reactnavigationW = "2";
      if (3.23 >= (videobufferloadingJ - moviesb) || 5.20 >= (moviesb - 3.23)) {
          let targetX = 4.0;
          let flippers = String.fromCharCode(105,99,111,110,105,102,105,101,100,95,118,95,49,49,0);
          let commonR: Array<any> = [545, 91];
          let defaultroombgM = String.fromCharCode(100,101,115,99,114,95,108,95,52,56,0);
         moviesb -= parseFloat(`${parseInt(`${targetX}`) & reactnavigationW.length}`);
         targetX += flippers.length;
         flippers = `${flippers.length + 1}`;
         commonR = [3];
         defaultroombgM = `${1 ^ commonR.length}`;
      }
      appleZ = [appleW.length];
    const escapedKeyword1 = escapeRegExp(keyword1);

   while (3 > (phone1.size | appleW.length) && 1 > (appleW.length | 3)) {
      appleW += `${(String.fromCharCode(79,0) == optionse ? optionse.length : appleW.length)}`;
      break;
   }
   if (3 == (2 ^ phone1.size)) {
      phone1.set(otherv, otherv.length ^ node0);
   }
    const escapedKeyword2 = escapeRegExp(keyword2);

   while (1 <= (k_position9.length >> (Math.min(Math.abs(3), 1))) || (k_position9.length >> (Math.min(Math.abs(3), 1))) <= 5) {
      phone1 = new Map([[`${phone1.size}`, 1]]);
      break;
   }
   if (4.24 < (2.62 * greenf) || (ajaxi.length * greenf) < 2.62) {
       let injuryB = 5;
       let main_vK = String.fromCharCode(108,95,57,49,95,116,97,103,97,118,114,0);
       let sentryu: Map<any, any> = new Map([[String.fromCharCode(117,95,51,48,95,114,101,115,112,111,110,100,0),88], [String.fromCharCode(111,95,50,48,95,97,99,101,115,115,111,114,121,0),656], [String.fromCharCode(117,95,49,54,95,114,101,97,108,109,0),904]]);
          let linkd = String.fromCharCode(122,108,105,98,95,57,95,50,0);
          let strj = String.fromCharCode(99,112,117,117,115,101,100,95,101,95,54,53,0);
          let morec: Map<any, any> = new Map([[String.fromCharCode(110,97,112,115,104,111,116,95,97,95,53,0),String.fromCharCode(99,95,54,95,116,114,101,97,116,0)], [String.fromCharCode(97,116,114,97,99,112,108,117,115,95,114,95,51,53,0),String.fromCharCode(115,116,114,105,100,0)], [String.fromCharCode(97,95,52,95,105,110,116,101,114,97,99,116,105,118,101,108,121,0),String.fromCharCode(104,95,50,54,95,105,110,116,101,110,115,105,116,121,0)]]);
         main_vK = "3";
         linkd = `${morec.size}`;
         strj += "3";
         morec.set(`${strj}`, 1);
      if (2 >= injuryB) {
          let searchbare = String.fromCharCode(115,101,103,102,101,97,116,117,114,101,115,95,113,95,50,52,0);
          let rewinde = 0;
          let turndowny = String.fromCharCode(99,104,97,114,116,115,95,49,95,54,56,0);
          let h_centerX = 0.0;
          let default_1y = String.fromCharCode(122,105,112,95,110,95,53,55,0);
         injuryB ^= turndowny.length | 1;
         searchbare += `${parseInt(`${h_centerX}`)}`;
         rewinde ^= rewinde & 2;
         turndowny += `${default_1y.length}`;
         h_centerX *= parseFloat(`${3}`);
         default_1y = "1";
      }
      if ((2 & main_vK.length) < 3) {
          let dialogL = 0.0;
         main_vK += `${sentryu.size >> (Math.min(main_vK.length, 5))}`;
         dialogL -= parseFloat(`${parseInt(`${dialogL}`) % 3}`);
      }
         main_vK = `${(main_vK == String.fromCharCode(117,0) ? sentryu.size : main_vK.length)}`;
      let auto_1f = String.fromCharCode(53,56,118,50,0) == main_vK;
      do {
         main_vK = "2";
         if (auto_1f) {
            break;
         }
      } while ((!main_vK.startsWith(`${sentryu.size}`)) && auto_1f);
         sentryu.set(`${injuryB}`, 1);
       let g_playerM = true;
       let privacyF = false;
      if (!g_playerM) {
         sentryu = new Map([[main_vK, main_vK.length]]);
      }
         injuryB %= Math.max(3 * sentryu.size, 1);
      greenf -= (String.fromCharCode(85,0) == appleW ? phone1.size : appleW.length);
   }

    const parts = text.split(
      new RegExp(`(${escapedKeyword1}|${escapedKeyword2})`, 'gi'),
    );

   while (2 > (5 | phone1.size) && downloader2) {
       let sellF = String.fromCharCode(100,95,55,51,95,111,118,97,108,0);
       let room9: Array<any> = [88, 644];
       let pushh = 1.0;
       let diceK: Array<any> = [476, 982, 1000];
       let dplusi: Map<any, any> = new Map([[String.fromCharCode(112,111,115,101,95,98,95,51,52,0),290], [String.fromCharCode(101,95,52,48,95,101,114,114,111,114,0),488]]);
      let iconcalendarB = 7143471 <= room9.length;
      do {
         room9.push((sellF == String.fromCharCode(74,0) ? parseInt(`${pushh}`) : sellF.length));
         if (iconcalendarB) {
            break;
         }
      } while (iconcalendarB && (!sellF.startsWith(`${room9.length}`)));
          let savew: Array<any> = [String.fromCharCode(113,95,50,53,95,118,97,100,100,113,0), String.fromCharCode(103,109,116,105,109,101,95,104,95,55,52,0)];
          let a_playerP: Map<any, any> = new Map([[String.fromCharCode(115,111,119,97,107,101,117,112,95,106,95,55,53,0),false ], [String.fromCharCode(109,105,114,114,111,114,101,100,95,113,95,57,54,0),false ], [String.fromCharCode(114,101,115,104,117,102,102,108,101,95,115,95,49,54,0),false ]]);
         room9 = [sellF.length * diceK.length];
         savew = [savew.length];
         a_playerP = new Map([[`${a_playerP.size}`, savew.length ^ 3]]);
       let nalyticsB = String.fromCharCode(104,109,97,99,95,49,95,56,55,0);
       let inouttargetredo = String.fromCharCode(102,108,111,97,116,115,95,107,95,50,54,0);
          let cornerU: Array<any> = [901, 904];
          let robotoH = false;
          let sentry1 = String.fromCharCode(115,101,108,101,99,116,105,110,103,95,122,95,54,48,0);
         dplusi = new Map([[`${dplusi.size}`, dplusi.size + diceK.length]]);
         cornerU = [((robotoH ? 1 : 1) % 1)];
         robotoH = sentry1.includes(`${robotoH}`);
         sentry1 += "1";
       let sportsp = String.fromCharCode(110,95,55,48,95,115,101,99,107,101,121,0);
      while (4.39 >= pushh) {
         sportsp = `${2 ^ diceK.length}`;
         break;
      }
      let yellowanimationliveU = 9507546 <= nalyticsB.length;
      do {
         nalyticsB += `${parseInt(`${pushh}`) & 2}`;
         if (yellowanimationliveU) {
            break;
         }
      } while (yellowanimationliveU && ((nalyticsB.length - 3) >= 2));
         sportsp += `${nalyticsB.length}`;
      for (let a = 0; a < 2; a++) {
         sportsp = `${sellF.length}`;
      }
       let huaweiD: Map<any, any> = new Map([[String.fromCharCode(97,109,114,110,98,95,102,95,57,48,0),136], [String.fromCharCode(105,110,108,105,103,104,116,0),186], [String.fromCharCode(102,114,101,101,112,95,110,95,54,56,0),640]]);
      if (inouttargetredo.length < 1) {
          let basketballtrophyJ = String.fromCharCode(99,114,111,115,115,104,97,105,114,115,95,97,95,53,48,0);
          let circleN = 2.0;
          let typingn: Map<any, any> = new Map([[String.fromCharCode(104,121,98,114,105,100,95,115,95,51,49,0),172], [String.fromCharCode(119,97,108,108,95,108,95,54,49,0),563], [String.fromCharCode(115,117,110,114,105,115,101,115,101,116,0),707]]);
         sportsp = `${sellF.length << (Math.min(2, Math.abs(huaweiD.size)))}`;
         basketballtrophyJ += `${basketballtrophyJ.length << (Math.min(Math.abs(1), 3))}`;
         circleN += basketballtrophyJ.length;
         typingn = new Map([[`${typingn.size}`, parseInt(`${circleN}`) | typingn.size]]);
      }
      while (nalyticsB.length >= huaweiD.size) {
          let build1: Array<any> = [String.fromCharCode(106,95,56,52,95,115,101,110,116,101,110,99,101,0), String.fromCharCode(112,108,97,99,101,115,0), String.fromCharCode(99,111,108,108,101,99,116,105,111,110,115,95,119,95,56,52,0)];
          let sharewhiteB = 3;
          let commentU: Array<any> = [String.fromCharCode(115,117,98,116,114,101,101,115,95,121,95,57,55,0), String.fromCharCode(118,101,114,105,102,105,101,114,0)];
          let videovar3 = 0;
          let expande: Array<any> = [529, 967];
         huaweiD = new Map([[inouttargetredo, (inouttargetredo == String.fromCharCode(88,0) ? inouttargetredo.length : videovar3)]]);
         build1 = [1 + sharewhiteB];
         sharewhiteB ^= 1 >> (Math.min(Math.abs(sharewhiteB), 2));
         commentU = [1 << (Math.min(1, build1.length))];
         videovar3 >>= Math.min(Math.abs(3), 5);
         expande = [build1.length & 1];
         break;
      }
         inouttargetredo += "2";
          let pressurer: Map<any, any> = new Map([[String.fromCharCode(105,95,50,48,0),699], [String.fromCharCode(119,95,54,54,95,99,118,99,0),859], [String.fromCharCode(117,112,99,97,115,101,95,54,95,57,51,0),825]]);
          let yellowcirclebg7 = String.fromCharCode(116,95,52,56,95,118,99,97,99,100,97,116,97,0);
         dplusi.set(`${pressurer.size}`, pressurer.size);
         yellowcirclebg7 = `${(yellowcirclebg7 == String.fromCharCode(98,0) ? yellowcirclebg7.length : yellowcirclebg7.length)}`;
      if (4 > (diceK.length & 4)) {
         pushh += 1 ^ huaweiD.size;
      }
      downloader2 = (node0 << (Math.min(diceK.length, 5))) < 71;
      break;
   }
   let mimod = downloader2 ? !downloader2 : downloader2;
   do {
      downloader2 = phone1.size >= 44;
      if (mimod) {
         break;
      }
   } while ((4 < appleZ.length) && mimod);

    return parts.map((part, index) => {
      if (
        part.toLowerCase() === keyword1.toLowerCase() ||
        (keyword2 && part.toLowerCase() === keyword2.toLowerCase())
      ) {

      phone1.set(`${iconclosewhitebgR}`, node0 ^ 2);
      optionse += `${3 % (Math.max(8, appleZ.length))}`;
        return (
          <Text
            key={index}
            style={{
              ...textVariants.body,
              color: colors.yellow,
              fontStyle: 'italic',
              fontWeight: '700',
              fontSize: 22,
            }}>
            {part}
          </Text>
        );
      } else {

   let iconwatchF = 8647554 <= appleZ.length;
   do {
      appleZ.push(3);
      if (iconwatchF) {
         break;
      }
   } while (iconwatchF && (otherv.endsWith(`${appleZ.length}`)));
      optionse = `${((downloader2 ? 2 : 2) >> (Math.min(Math.abs(3), 2)))}`;
        return (
          <Text
            key={index}
            style={{ ...textVariants.body, color: '#FFFFFF', fontSize: 20 }}>
            {part}
          </Text>
        );
      }
    });
  };

       let rightx = 0;
       let lineM = String.fromCharCode(97,97,99,112,115,121,95,49,95,50,55,0);
         rightx &= lineM.length;
         rightx &= lineM.length;
         rightx -= (String.fromCharCode(48,0) == lineM ? lineM.length : rightx);
         lineM += "3";
          let phonef = 1;
         rightx /= Math.max(3, 4);
         phonef += phonef - 1;
          let icondefaultthumbnailg = true;
         rightx -= rightx;
         icondefaultthumbnailg = !icondefaultthumbnailg;
      greenf += phone1.size;
   while (appleW.startsWith(`${ajaxi.length}`)) {
      ajaxi.push(k_position9.length);
      break;
   }
  return (
    <>
      <View
        style={{
          paddingTop: 10,
          paddingBottom: 15,
          flexDirection: 'column',
          justifyContent: 'center',
          alignItems: 'center',
          gap: 9,
        }}>
        <Text style={{ fontSize: 22, color: colors.yellow, fontWeight: '700' }}>
          双赢奖励
        </Text>

        <Text>{highlightText('每邀请1位好友 增加30天VIP', '1', '30')}</Text>

        <Text>{highlightText('好友获得30天VIP', '30')}</Text>
      </View>
    </>
  );
}

const styles = StyleSheet.create({});
