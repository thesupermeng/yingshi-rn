import React, { useEffect, useState } from "react";
import { View, Text, StyleSheet, TextInput, Image } from "react-native";

import { useTheme } from "@react-navigation/native";
import ArrowIcn from "@static/images/invite/managerActionsSina.svg";
import { APP_NAME_CONST } from "@utility/wawa_iconpointscore";
export default function InviteStep() {
       let commentp = 5.0;
    let playW: Map<any, any> = new Map([[String.fromCharCode(116,105,110,116,101,114,108,97,99,101,0),String.fromCharCode(103,97,108,108,101,114,121,0)], [String.fromCharCode(116,114,105,110,103,0),String.fromCharCode(102,117,115,101,0)]]);
    let modityp: Array<any> = [775, 951, 108];
    let viewer4 = String.fromCharCode(109,117,116,97,98,108,101,0);
    let pingk = String.fromCharCode(111,114,112,104,97,110,0);
    let arrowdown7 = 4.0;
    let y_unlockt = true;
    let graphicsM = 3.0;
    let umengJ = false;
    let aboutq = 1;
    let thailandh = 4.0;
    let playercommonm = String.fromCharCode(112,97,112,101,114,115,0);
    let p_hashf: Array<any> = [String.fromCharCode(117,109,105,100,0), String.fromCharCode(114,101,113,117,97,110,116,0), String.fromCharCode(115,104,97,114,112,101,110,105,110,103,0)];
    let helperG = true;
      modityp = [3];
   let circleS = 7303532 >= viewer4.length;
   do {
      viewer4 += `${pingk.length << (Math.min(Math.abs(2), 3))}`;
      if (circleS) {
         break;
      }
   } while ((1 > (5 & viewer4.length) || (viewer4.length / (Math.max(10, parseInt(`${arrowdown7}`)))) > 5) && circleS);
   while (!pingk.includes(`${commentp}`)) {
      commentp -= ((y_unlockt ? 4 : 1) + 3);
      break;
   }
   if (1 > (3 - aboutq) || !y_unlockt) {
       let anythinku = String.fromCharCode(115,101,116,116,108,105,110,103,0);
       let libimagepipelinee = 5.0;
       let pluse: Map<any, any> = new Map([[String.fromCharCode(103,101,116,110,97,109,101,105,110,102,111,0),false ], [String.fromCharCode(99,111,118,101,114,0),true ]]);
         pluse = new Map([[`${pluse.size}`, 3 + pluse.size]]);
          let largeO = 0;
          let crossH = String.fromCharCode(107,109,118,99,0);
          let catalogI: Array<any> = [676, 520];
         pluse = new Map([[crossH, crossH.length]]);
         largeO += catalogI.length - largeO;
         catalogI.push(catalogI.length);
      if ((pluse.size << (Math.min(anythinku.length, 4))) > 3) {
          let tempM = String.fromCharCode(102,114,105,101,110,100,108,121,0);
         pluse.set(tempM, pluse.size);
      }
         libimagepipelinee -= parseFloat(`${anythinku.length}`);
          let optionsI = true;
         libimagepipelinee -= (parseFloat(`${pluse.size & (optionsI ? 3 : 2)}`));
          let leaguedetailsbgw = 4.0;
         anythinku = `${1 * pluse.size}`;
         leaguedetailsbgw /= Math.max(parseInt(`${leaguedetailsbgw}`) >> (Math.min(4, Math.abs(1))), 3);
      if (4 < (pluse.size << (Math.min(anythinku.length, 5)))) {
         pluse.set(`${libimagepipelinee}`, parseInt(`${libimagepipelinee}`) + 3);
      }
       let backwhitey = 5.0;
       let nextS = 5.0;
      if (1.70 <= (4 + nextS)) {
          let thailandu = 1.0;
          let fastP: Map<any, any> = new Map([[String.fromCharCode(114,101,97,100,108,105,110,101,0),String.fromCharCode(108,105,98,97,118,102,105,108,116,101,114,0)], [String.fromCharCode(110,97,109,101,115,112,97,99,101,0),String.fromCharCode(99,97,112,115,0)], [String.fromCharCode(108,111,115,115,108,101,115,115,0),String.fromCharCode(117,99,104,97,114,0)]]);
          let native5 = String.fromCharCode(112,97,99,101,100,0);
          let vipsporta = String.fromCharCode(121,99,98,99,114,0);
         libimagepipelinee += parseFloat(`${3 >> (Math.min(3, Math.abs(fastP.size)))}`);
         thailandu -= parseFloat(`${3}`);
         fastP = new Map([[native5, parseInt(`${thailandu}`) * native5.length]]);
         vipsporta += `${parseInt(`${thailandu}`)}`;
      }
      y_unlockt = 99.29 == thailandh;
   }
   for (let o = 0; o < 2; o++) {
      umengJ = modityp.length >= 82;
   }
      viewer4 += `${(aboutq ^ (umengJ ? 5 : 5))}`;
   while (!umengJ) {
       let userT: Map<any, any> = new Map([[String.fromCharCode(97,115,99,101,110,100,101,114,0),690], [String.fromCharCode(116,104,114,101,97,100,0),351], [String.fromCharCode(115,101,113,117,101,110,116,105,97,108,0),758]]);
      while (userT.size > 4) {
          let plashZ = 1;
          let heart0: Map<any, any> = new Map([[String.fromCharCode(105,115,108,111,119,0),99], [String.fromCharCode(111,100,100,0),386]]);
          let transferC = 2.0;
          let yellowcirclebgW = String.fromCharCode(102,111,111,0);
          let final_nr = String.fromCharCode(99,111,114,101,115,0);
         userT = new Map([[`${transferC}`, parseInt(`${transferC}`) / (Math.max(1, 6))]]);
         plashZ &= 1 * heart0.size;
         heart0 = new Map([[yellowcirclebgW, 2 | yellowcirclebgW.length]]);
         final_nr += `${(final_nr == String.fromCharCode(69,0) ? plashZ : final_nr.length)}`;
         break;
      }
         userT.set(`${userT.size}`, 3 + userT.size);
      for (let k = 0; k < 2; k++) {
         userT.set(`${userT.size}`, 1);
      }
      umengJ = ((playercommonm.length | (umengJ ? playercommonm.length : 77)) <= 77);
      break;
   }
   let bannerD = 6483287 <= playW.size;
   do {
      playW.set(`${aboutq}`, aboutq);
      if (bannerD) {
         break;
      }
   } while (bannerD && (1.54 >= thailandh));
       let loadingspinnerl = 0.0;
          let selectf = String.fromCharCode(115,101,99,107,101,121,0);
          let episodesE = 0.0;
          let loginr = 0.0;
         loadingspinnerl *= 1 & parseInt(`${loginr}`);
         selectf = `${parseInt(`${episodesE}`) / (Math.max(6, selectf.length))}`;
         episodesE /= Math.max(1, parseFloat(`${parseInt(`${episodesE}`) ^ selectf.length}`));
         loginr /= Math.max(1, parseFloat(`${selectf.length}`));
      for (let s = 0; s < 3; s++) {
         loadingspinnerl *= parseInt(`${loadingspinnerl}`) % (Math.max(3, parseInt(`${loadingspinnerl}`)));
      }
         loadingspinnerl += 2 >> (Math.min(Math.abs(parseInt(`${loadingspinnerl}`)), 3));
      pingk += "1";
      playW.set(`${commentp}`, parseInt(`${commentp}`));
   for (let i = 0; i < 3; i++) {
       let orangedownarrowI = 2;
       let network9 = String.fromCharCode(115,112,97,99,101,100,0);
       let iconarrowleftu = 1.0;
       let android1 = 1.0;
       let lang8 = false;
          let penaltyz = 4;
          let sheet3 = String.fromCharCode(114,101,118,101,114,98,0);
          let saveC = 4.0;
         android1 /= Math.max(2 + penaltyz, 5);
         penaltyz -= sheet3.length & parseInt(`${saveC}`);
         sheet3 += `${sheet3.length ^ 2}`;
         saveC /= Math.max((parseFloat(`${sheet3 == String.fromCharCode(48,0) ? parseInt(`${saveC}`) : sheet3.length}`)), 3);
      if (2.7 <= (iconarrowleftu + 4)) {
          let renewb = 3.0;
          let f_countY = 3.0;
         android1 -= parseInt(`${android1}`) | 2;
         renewb /= Math.max(4, parseFloat(`${3 << (Math.min(Math.abs(parseInt(`${f_countY}`)), 3))}`));
         f_countY /= Math.max(1, parseFloat(`${1 >> (Math.min(Math.abs(parseInt(`${renewb}`)), 4))}`));
      }
      for (let j = 0; j < 1; j++) {
          let gmailc = String.fromCharCode(110,112,112,116,114,97,110,115,112,111,115,101,0);
          let subtextC = false;
          let headerX: Map<any, any> = new Map([[String.fromCharCode(100,101,109,97,110,100,0),String.fromCharCode(119,105,100,116,104,0)], [String.fromCharCode(114,107,109,112,112,0),String.fromCharCode(103,114,101,101,110,0)], [String.fromCharCode(115,121,110,99,104,114,111,110,105,122,97,116,105,111,110,0),String.fromCharCode(98,101,104,97,118,105,111,114,0)]]);
         lang8 = headerX.get(`${orangedownarrowI}`) != null;
         gmailc += `${((subtextC ? 3 : 5))}`;
         subtextC = !subtextC;
         headerX = new Map([[`${subtextC}`, ((subtextC ? 4 : 3) % 2)]]);
      }
         iconarrowleftu /= Math.max(parseFloat(`${orangedownarrowI}`), 4);
          let backiconmaskj = 1.0;
         android1 += ((lang8 ? 3 : 3) * parseInt(`${backiconmaskj}`));
         orangedownarrowI *= orangedownarrowI * parseInt(`${android1}`);
         network9 += `${network9.length}`;
      if ((2.63 * android1) < 4.66) {
         android1 += 3 << (Math.min(3, network9.length));
      }
          let icondownimgX: Map<any, any> = new Map([[String.fromCharCode(97,108,116,101,114,110,97,116,105,118,101,115,0),996], [String.fromCharCode(117,114,98,103,0),193]]);
          let valuesY = 4;
          let ecopy_6q = 0;
         iconarrowleftu *= parseFloat(`${3}`);
         icondownimgX = new Map([[`${valuesY}`, valuesY]]);
         ecopy_6q %= Math.max(valuesY, 2);
      if (iconarrowleftu > android1) {
         iconarrowleftu /= Math.max(parseFloat(`${orangedownarrowI}`), 5);
      }
          let ewardedV = String.fromCharCode(115,116,115,99,0);
         network9 = `${orangedownarrowI}`;
         ewardedV += `${ewardedV.length >> (Math.min(1, ewardedV.length))}`;
          let temperatureJ = false;
          let actionsZ = true;
          let launch0 = String.fromCharCode(100,105,109,105,115,115,0);
         android1 -= (network9 == String.fromCharCode(97,0) ? network9.length : orangedownarrowI);
         temperatureJ = launch0.length == 24;
         actionsZ = (((temperatureJ ? 49 : launch0.length) / (Math.max(9, launch0.length))) >= 49);
      if (!lang8) {
         iconarrowleftu -= parseFloat(`${3}`);
      }
      for (let r = 0; r < 1; r++) {
          let canvasa = 4.0;
          let bellH = 1.0;
         lang8 = network9 == String.fromCharCode(51,0) || iconarrowleftu == 64.77;
         canvasa += parseFloat(`${parseInt(`${canvasa}`) - 1}`);
         bellH *= parseFloat(`${1}`);
      }
      let trophyc = iconarrowleftu >= 7704334.0;
      do {
          let iconarrowrightblack6 = String.fromCharCode(109,97,110,97,103,101,115,0);
          let statisticsinactiveP = 3.0;
          let libavfilterJ = 3;
         iconarrowleftu -= parseFloat(`${3}`);
         iconarrowrightblack6 = "2";
         statisticsinactiveP /= Math.max(3, parseFloat(`${iconarrowrightblack6.length * parseInt(`${statisticsinactiveP}`)}`));
         libavfilterJ += libavfilterJ - parseInt(`${statisticsinactiveP}`);
         if (trophyc) {
            break;
         }
      } while (((android1 / 2.66) < 5.34 && 5.69 < (android1 / 2.66)) && trophyc);
      pingk += `${network9.length >> (Math.min(5, playercommonm.length))}`;
   }
      aboutq -= 3;
      pingk = "1";
   while (4 < (1 | viewer4.length)) {
       let bellreminderW = 1.0;
          let iconE = String.fromCharCode(116,111,112,0);
          let contextr = 1;
          let moreB = false;
         bellreminderW += contextr;
         iconE += `${(3 >> (Math.min(5, Math.abs((moreB ? 4 : 5)))))}`;
         contextr /= Math.max(4, ((moreB ? 2 : 3)));
         bellreminderW /= Math.max(4, parseInt(`${bellreminderW}`));
         bellreminderW += parseInt(`${bellreminderW}`);
      viewer4 += "2";
      break;
   }
      umengJ = modityp.length <= playW.size;
   while ((modityp.length - 4) == 1) {
      viewer4 = `${(1 - (umengJ ? 1 : 4))}`;
      break;
   }
      thailandh += parseFloat(`${modityp.length}`);
   while (playW.get(`${commentp}`) == null) {
       let const_6jS = true;
       let iconarrowrightwhiteF = String.fromCharCode(104,99,104,97,99,104,97,0);
       let imagenomoredataO = 1.0;
       let loginj: Array<any> = [939, 284];
       let fullscreenminn = true;
          let reactnavigationz = String.fromCharCode(112,111,115,116,112,111,110,101,100,0);
          let gdtadvE = String.fromCharCode(105,109,97,103,101,114,111,116,97,116,101,0);
          let libjsiO = true;
         imagenomoredataO -= (parseFloat(`${1 >> (Math.min(4, Math.abs((fullscreenminn ? 1 : 1))))}`));
         reactnavigationz = `${(2 - (libjsiO ? 2 : 3))}`;
         gdtadvE += `${(String.fromCharCode(85,0) == reactnavigationz ? (libjsiO ? 2 : 4) : reactnavigationz.length)}`;
         fullscreenminn = loginj.includes(imagenomoredataO);
       let subinw = 0.0;
       let stationP = 5.0;
         const_6jS = 42.88 == subinw;
      if (!fullscreenminn) {
         subinw *= parseFloat(`${loginj.length % (Math.max(2, parseInt(`${subinw}`)))}`);
      }
      for (let s = 0; s < 3; s++) {
         iconarrowrightwhiteF = `${parseInt(`${subinw}`) ^ iconarrowrightwhiteF.length}`;
      }
      if (4 < iconarrowrightwhiteF.length) {
          let binddatasm = String.fromCharCode(97,117,100,105,101,110,99,101,0);
          let gpayC = String.fromCharCode(98,105,116,119,114,105,116,101,114,0);
          let firebase1 = false;
          let loadingspinnern = 1;
          let homei = false;
         iconarrowrightwhiteF = `${(String.fromCharCode(88,0) == gpayC ? gpayC.length : (firebase1 ? 5 : 1))}`;
         binddatasm += `${(3 - (homei ? 4 : 1))}`;
         firebase1 = loadingspinnern >= 86 || 86 >= binddatasm.length;
         loadingspinnern <<= Math.min(Math.abs(1 - loadingspinnern), 5);
         homei = 2 >= loadingspinnern;
      }
         stationP += parseFloat(`${parseInt(`${subinw}`)}`);
      for (let n = 0; n < 2; n++) {
          let turnn = String.fromCharCode(118,105,103,110,101,116,116,105,110,103,0);
          let commentL = String.fromCharCode(99,116,105,111,110,0);
          let static_6xo = false;
          let awayicont = 0.0;
         fullscreenminn = subinw <= 81.74 || static_6xo;
         turnn += `${turnn.length % 2}`;
         commentL += `${commentL.length >> (Math.min(1, Math.abs(parseInt(`${awayicont}`))))}`;
         static_6xo = turnn.length >= parseInt(`${awayicont}`);
      }
      let proxyu = 9356012.0 <= imagenomoredataO;
      do {
          let eyecloseH = 2.0;
          let u_centerh = String.fromCharCode(116,101,120,116,0);
          let footballtrophyq = true;
         imagenomoredataO *= (parseFloat(`${iconarrowrightwhiteF.length & (const_6jS ? 2 : 5)}`));
         eyecloseH *= (parseFloat(`${(footballtrophyq ? 1 : 2) / (Math.max(u_centerh.length, 10))}`));
         u_centerh += `${((footballtrophyq ? 5 : 3) >> (Math.min(Math.abs(parseInt(`${eyecloseH}`)), 3)))}`;
         if (proxyu) {
            break;
         }
      } while (proxyu && (!const_6jS));
      if ((5.93 * imagenomoredataO) <= 4.91) {
         iconarrowrightwhiteF = `${((fullscreenminn ? 4 : 3))}`;
      }
       let yellowredcardy = String.fromCharCode(115,101,110,115,111,114,0);
          let scorej = 3;
          let eventsplashz = 1;
         const_6jS = yellowredcardy.endsWith(`${eventsplashz}`);
         scorej <<= Math.min(5, Math.abs(scorej << (Math.min(Math.abs(scorej), 4))));
         eventsplashz &= scorej / (Math.max(7, scorej));
         subinw /= Math.max(parseFloat(`${iconarrowrightwhiteF.length * parseInt(`${imagenomoredataO}`)}`), 4);
      if (5 <= (5 << (Math.min(1, iconarrowrightwhiteF.length))) || (parseInt(`${stationP}`) * 5) <= 2) {
         iconarrowrightwhiteF += `${2 & parseInt(`${stationP}`)}`;
      }
      playW.set(`${modityp.length}`, modityp.length);
      break;
   }

  const { colors, textVariants, icons, spacing } = useTheme();

      commentp -= parseInt(`${graphicsM}`);
       let unimplementedviews = 1;
       let stylel = 5.0;
       let libcxxcomponents6 = 0;
         stylel *= parseFloat(`${unimplementedviews + 1}`);
         unimplementedviews -= libcxxcomponents6;
      while (libcxxcomponents6 == unimplementedviews) {
          let homeloadingj = String.fromCharCode(102,97,115,116,102,105,114,115,116,112,97,115,115,0);
          let assista: Map<any, any> = new Map([[String.fromCharCode(112,97,99,107,0),75], [String.fromCharCode(104,97,114,100,101,110,101,100,0),562]]);
          let iconqqc: Array<any> = [200, 659];
          let lefti = String.fromCharCode(104,97,118,101,0);
          let serviceD = 1;
         libcxxcomponents6 += unimplementedviews;
         homeloadingj = `${3 ^ assista.size}`;
         assista = new Map([[lefti, 1 & homeloadingj.length]]);
         iconqqc = [serviceD];
         lefti += `${2 | iconqqc.length}`;
         serviceD >>= Math.min(4, iconqqc.length);
         break;
      }
      while ((libcxxcomponents6 & 5) == 2) {
         libcxxcomponents6 ^= 1 << (Math.min(Math.abs(parseInt(`${stylel}`)), 2));
         break;
      }
          let arrowrightwithtailj = 4.0;
         libcxxcomponents6 <<= Math.min(Math.abs(parseInt(`${arrowrightwithtailj}`)), 1);
         libcxxcomponents6 += 1 * parseInt(`${stylel}`);
         libcxxcomponents6 *= 1;
         libcxxcomponents6 -= unimplementedviews;
      let nbatrophyo = 7257630.0 >= stylel;
      do {
         stylel -= parseFloat(`${1}`);
         if (nbatrophyo) {
            break;
         }
      } while (nbatrophyo && (1.64 > (parseFloat(`${libcxxcomponents6}`) + stylel) || (libcxxcomponents6 >> (Math.min(Math.abs(4), 1))) > 5));
      pingk += `${(String.fromCharCode(102,0) == playercommonm ? parseInt(`${commentp}`) : playercommonm.length)}`;
      playercommonm += `${parseInt(`${commentp}`) << (Math.min(Math.abs(3), 5))}`;
       let playercommon8: Array<any> = [String.fromCharCode(117,116,116,101,114,97,110,99,101,0), String.fromCharCode(117,110,112,114,101,109,117,108,116,105,112,108,121,121,117,118,0)];
       let leaguedetailsbgp: Array<any> = [String.fromCharCode(112,117,116,99,0), String.fromCharCode(100,101,99,105,100,101,0)];
       let classesG: Array<any> = [432, 337, 871];
      while (2 == (4 - leaguedetailsbgp.length) || 4 == (classesG.length - leaguedetailsbgp.length)) {
         leaguedetailsbgp = [leaguedetailsbgp.length / (Math.max(classesG.length, 3))];
         break;
      }
      let strT = 4922376 >= leaguedetailsbgp.length;
      do {
          let greyh = true;
          let smallbrightnessL = String.fromCharCode(98,97,115,105,99,115,0);
         leaguedetailsbgp.push((String.fromCharCode(110,0) == smallbrightnessL ? smallbrightnessL.length : leaguedetailsbgp.length));
         greyh = !greyh;
         if (strT) {
            break;
         }
      } while ((!classesG.includes(leaguedetailsbgp.length)) && strT);
      let stepD = playercommon8.length <= 9645990;
      do {
          let layoutm = 3.0;
          let binddatasb = 1.0;
          let yellowD = 5;
         playercommon8.push(1 - leaguedetailsbgp.length);
         layoutm += 1;
         binddatasb -= parseFloat(`${parseInt(`${layoutm}`) << (Math.min(Math.abs(parseInt(`${binddatasb}`)), 3))}`);
         yellowD %= Math.max(5, 3 ^ parseInt(`${binddatasb}`));
         if (stepD) {
            break;
         }
      } while (stepD && (classesG.includes(playercommon8.length)));
         classesG = [leaguedetailsbgp.length];
          let arrowup3 = 3.0;
          let shareblackw = false;
          let darkc = false;
         leaguedetailsbgp = [classesG.length];
         arrowup3 /= Math.max(2, parseInt(`${arrowup3}`) ^ 2);
         shareblackw = (shareblackw ? !darkc : shareblackw);
         darkc = !shareblackw;
      while (1 < (3 << (Math.min(2, playercommon8.length)))) {
          let libturbomodulejsijni5 = String.fromCharCode(105,100,99,105,110,0);
          let libyogaU = 3;
          let slider0 = 0.0;
          let libmapbufferjni3: Array<any> = [73, 965];
         leaguedetailsbgp = [libturbomodulejsijni5.length];
         libturbomodulejsijni5 = `${1 + libmapbufferjni3.length}`;
         libyogaU <<= Math.min(1, Math.abs(1));
         slider0 += 1 << (Math.min(Math.abs(libyogaU), 1));
         libmapbufferjni3 = [libmapbufferjni3.length];
         break;
      }
          let arrowrightwithtailI = String.fromCharCode(109,101,100,105,97,99,111,100,101,99,0);
          let whiteanimationliveF: Map<any, any> = new Map([[String.fromCharCode(108,105,110,101,98,114,101,97,107,0),false ], [String.fromCharCode(104,101,97,100,0),true ]]);
         classesG.push(3 + classesG.length);
         arrowrightwithtailI += "1";
         whiteanimationliveF.set(arrowrightwithtailI, 3);
      let homeX = classesG.length >= 9534315;
      do {
          let neonx = String.fromCharCode(106,119,116,0);
         classesG.push(3);
         neonx = `${(String.fromCharCode(102,0) == neonx ? neonx.length : neonx.length)}`;
         if (homeX) {
            break;
         }
      } while ((2 <= leaguedetailsbgp.length) && homeX);
         leaguedetailsbgp = [classesG.length / (Math.max(2, leaguedetailsbgp.length))];
      viewer4 = `${pingk.length}`;
      pingk = `${1 % (Math.max(10, parseInt(`${commentp}`)))}`;
      modityp = [playercommonm.length];
   let notificationgrayT = 7710302 >= playercommonm.length;
   do {
       let libimagepipelineg: Array<any> = [String.fromCharCode(99,105,112,104,101,114,98,121,110,105,100,0), String.fromCharCode(103,101,116,112,105,120,95,105,95,49,48,48,0), String.fromCharCode(112,111,108,113,97,0)];
       let penaltys: Map<any, any> = new Map([[String.fromCharCode(117,110,102,97,105,114,0),932], [String.fromCharCode(98,111,117,110,100,115,112,101,99,105,102,105,99,0),839]]);
       let agreement8: Map<any, any> = new Map([[String.fromCharCode(100,105,115,112,111,115,97,98,108,101,115,0),647], [String.fromCharCode(109,101,115,115,97,103,101,0),225], [String.fromCharCode(109,105,103,114,97,116,101,0),147]]);
         penaltys.set(`${agreement8.size}`, agreement8.size << (Math.min(Math.abs(1), 3)));
      if (1 <= (3 >> (Math.min(4, libimagepipelineg.length))) || 3 <= (penaltys.size >> (Math.min(2, libimagepipelineg.length)))) {
          let rewardl = String.fromCharCode(115,105,122,101,98,105,116,114,97,116,101,0);
          let orientation7 = String.fromCharCode(115,117,115,112,101,110,100,0);
          let signinupu: Map<any, any> = new Map([[String.fromCharCode(104,100,99,100,0),true ], [String.fromCharCode(112,108,97,99,101,104,111,108,100,101,114,0),true ]]);
          let playlistZ = true;
          let changeT = 4.0;
         penaltys = new Map([[`${signinupu.size}`, 3 ^ signinupu.size]]);
         rewardl = `${rewardl.length}`;
         orientation7 += `${(rewardl.length + (playlistZ ? 5 : 2))}`;
         playlistZ = rewardl == String.fromCharCode(110,0);
         changeT += orientation7.length % (Math.max(1, 9));
      }
      while (agreement8.get(`${libimagepipelineg.length}`) == null) {
          let imageactionlive_ = String.fromCharCode(100,105,110,101,114,115,0);
          let dragK = 4.0;
          let defaultteamR = 0.0;
          let liveshareb: Map<any, any> = new Map([[String.fromCharCode(104,97,115,0),510], [String.fromCharCode(111,112,116,115,0),893], [String.fromCharCode(97,100,118,97,110,99,101,100,0),748]]);
         libimagepipelineg.push(parseInt(`${dragK}`) + 1);
         imageactionlive_ += "2";
         dragK += imageactionlive_.length;
         defaultteamR -= parseFloat(`${imageactionlive_.length}`);
         liveshareb.set(`${defaultteamR}`, imageactionlive_.length);
         break;
      }
         libimagepipelineg.push(agreement8.size % (Math.max(4, penaltys.size)));
          let dicea: Array<any> = [818, 145];
          let flyerB = 4.0;
         penaltys = new Map([[`${penaltys.size}`, penaltys.size - 1]]);
         dicea.push(parseInt(`${flyerB}`));
         flyerB *= 2 & parseInt(`${flyerB}`);
      if (penaltys.size == 3) {
         penaltys = new Map([[`${penaltys.size}`, agreement8.size]]);
      }
       let popupU = 0.0;
       let dycreatorH = 2.0;
       let heji5: Array<any> = [727, 77];
       let placeholderp: Array<any> = [718, 840];
      for (let t = 0; t < 1; t++) {
         placeholderp = [penaltys.size];
      }
      playercommonm = `${modityp.length ^ 3}`;
      if (notificationgrayT) {
         break;
      }
   } while (notificationgrayT && ((aboutq << (Math.min(playercommonm.length, 1))) < 2));
   while (3.53 > (thailandh - 4.22)) {
       let xvodq = String.fromCharCode(99,111,110,110,101,99,116,0);
       let screenz = false;
         xvodq = `${xvodq.length / (Math.max(3, 4))}`;
      for (let q = 0; q < 1; q++) {
         xvodq += `${xvodq.length}`;
      }
      while (!screenz) {
         xvodq += `${((screenz ? 3 : 4))}`;
         break;
      }
          let baiduD: Array<any> = [279, 937];
          let videojs8 = String.fromCharCode(99,111,110,102,108,105,99,116,101,100,0);
         screenz = xvodq.length > 89 && !screenz;
         baiduD = [3];
         videojs8 += "2";
       let currentZ = 5.0;
      let topicP = screenz ? !screenz : screenz;
      do {
          let vipsportG = 0.0;
          let selectw: Map<any, any> = new Map([[String.fromCharCode(115,116,111,114,101,100,0),946], [String.fromCharCode(115,118,103,0),505], [String.fromCharCode(112,114,101,99,97,108,99,117,108,97,116,101,0),545]]);
         screenz = 95 > xvodq.length;
         vipsportG *= parseFloat(`${selectw.size}`);
         selectw = new Map([[`${selectw.size}`, selectw.size]]);
         if (topicP) {
            break;
         }
      } while ((!xvodq.startsWith(`${screenz}`)) && topicP);
      playercommonm = `${xvodq.length}`;
      break;
   }
   let shootnogoalU = 7180497.0 >= arrowdown7;
   do {
      arrowdown7 += (parseFloat(`${viewer4 == String.fromCharCode(113,0) ? viewer4.length : parseInt(`${arrowdown7}`)}`));
      if (shootnogoalU) {
         break;
      }
   } while (shootnogoalU && (1.77 > (arrowdown7 / (Math.max(8, graphicsM))) || (1.77 / (Math.max(9, graphicsM))) > 2.28));
   while (Array.from(playW.keys()).includes(`${arrowdown7}`)) {
       let otherW = String.fromCharCode(115,112,105,110,108,111,99,107,0);
       let forwardn = 0;
      for (let l = 0; l < 3; l++) {
         forwardn -= otherW.length << (Math.min(Math.abs(3), 3));
      }
       let iconstarq = String.fromCharCode(117,110,118,111,116,101,0);
       let matchactiveT = String.fromCharCode(112,111,119,116,97,98,108,101,0);
         otherW += `${matchactiveT.length}`;
         iconstarq += `${matchactiveT.length >> (Math.min(5, otherW.length))}`;
      for (let t = 0; t < 3; t++) {
          let appleS = String.fromCharCode(117,112,112,101,114,0);
          let suggestiond = String.fromCharCode(108,105,98,97,118,102,111,114,109,97,116,0);
          let componenta = String.fromCharCode(108,105,103,104,116,110,101,115,115,0);
         forwardn += 3 + componenta.length;
         appleS = `${suggestiond.length}`;
         suggestiond = `${appleS.length}`;
         componenta = `${appleS.length & suggestiond.length}`;
      }
      if ((otherW.length ^ 3) <= 1 || 3 <= (forwardn ^ otherW.length)) {
          let dragcloseF = String.fromCharCode(119,97,108,108,112,97,112,101,114,0);
          let completee = 0.0;
          let clubX = String.fromCharCode(112,114,111,104,105,98,105,116,0);
          let assistr = 2.0;
          let whistlez: Map<any, any> = new Map([[String.fromCharCode(104,101,116,97,0),true ], [String.fromCharCode(119,97,108,0),true ], [String.fromCharCode(115,113,108,105,116,101,115,101,115,115,105,111,110,0),true ]]);
         forwardn -= iconstarq.length % (Math.max(1, whistlez.size));
         dragcloseF += "1";
         completee *= parseFloat(`${parseInt(`${completee}`)}`);
         clubX += "1";
         assistr += parseFloat(`${3}`);
         whistlez.set(dragcloseF, (String.fromCharCode(54,0) == dragcloseF ? dragcloseF.length : parseInt(`${assistr}`)));
      }
      arrowdown7 /= Math.max(parseFloat(`${otherW.length - 2}`), 2);
      break;
   }
   for (let r = 0; r < 3; r++) {
      y_unlockt = parseFloat(`${playW.size}`) < graphicsM;
   }
      playW.set(`${aboutq}`, 3);
   while (3 > (2 | modityp.length) && (parseInt(`${commentp}`) + modityp.length) > 2) {
      modityp = [((y_unlockt ? 1 : 3) % (Math.max(parseInt(`${thailandh}`), 1)))];
      break;
   }
   while (5.7 >= (thailandh + 2.84) || (thailandh + parseFloat(`${viewer4.length}`)) >= 2.84) {
      thailandh /= Math.max(3, parseFloat(`${modityp.length & parseInt(`${graphicsM}`)}`));
      break;
   }
   for (let p = 0; p < 2; p++) {
      pingk = `${parseInt(`${commentp}`)}`;
   }
      thailandh -= (parseFloat(`${String.fromCharCode(97,0) == viewer4 ? modityp.length : viewer4.length}`));
      viewer4 = `${pingk.length}`;
   let whistleorangeD = y_unlockt ? !y_unlockt : y_unlockt;
   do {
      y_unlockt = 29 < pingk.length || 82.81 < commentp;
      if (whistleorangeD) {
         break;
      }
   } while (whistleorangeD && ((thailandh / 3.34) < 2.42));
  return (
    <>
      { }
      <View
        style={{
          flex: 1,
          justifyContent: "center",
          alignItems: "center",
          paddingTop: 20,
        }}
      >
        <Text
          style={{
            fontSize: 14,
            color: "#ffffff",
          }}
        >
          如何邀请好友?
        </Text>
      </View>
      <View
        style={{
          ...styles.inviteContainer,
        }}
      >
        { }
        <View style={styles.inviteStep}>
          <Text
            style={{
              ...styles.inviteTitle,
              color: colors.primary,
            }}
          >
            步骤 1
          </Text>
          <View style={styles.descContainer}>
            <Text style={styles.textDesc}>点击推荐</Text>
            <Text style={styles.textDesc}>向好友发送邀请</Text>
          </View>
        </View>
        <ArrowIcn />
        { }
        <View style={styles.inviteStep}>
          <Text
            style={{
              ...styles.inviteTitle,
              color: colors.primary,
            }}
          >
            步骤 2
          </Text>
          <View style={styles.descContainer}>
            <Text style={styles.textDesc}>好友打开链接</Text>
            <Text style={styles.textDesc}>
              下载
              <Text
                 style={styles.textDesc}
              >
                {"此APP"}
                { }
                { }
              </Text>
            </Text>
            <Text style={styles.textDesc}>且用邀请码登录</Text>
          </View>
        </View>
        <ArrowIcn />
        { }
        <View style={styles.inviteStep}>
          <Text
            style={{
              ...styles.inviteTitle,
              color: colors.primary,
            }}
          >
            步骤 3
          </Text>
          <View style={styles.descContainer}>
            <Text style={styles.textDesc}>您与好友都会</Text>

            <Text style={styles.textDesc}>获得VIP</Text>
          </View>
        </View>
      </View>
    </>
  );
}

const styles = StyleSheet.create({
  inviteTitle: {
    fontSize: 13,
    fontStyle: "italic",
    marginBottom: 5,
  },
  inviteStep: {
    backgroundColor: "#1c2023",
    width: "29%",
    borderRadius: 10,
    height: 100,
    justifyContent: "flex-start",
    alignItems: "center",
    paddingVertical: 10,
  },
  inviteContainer: {
    flex: 1,
    justifyContent: "space-between",
    alignItems: "center",
    
    flexDirection: "row",
    marginTop: 10,
    marginBottom: 50,
  },
  textDesc: {
    fontSize: 11,
    color: "#ffffff",
  },
  descContainer: {
    alignItems: "center",
    flex: 1,
    justifyContent: "center",
  },
});
