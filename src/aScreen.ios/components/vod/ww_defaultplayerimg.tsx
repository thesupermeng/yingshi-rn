import React, { memo, useEffect, useState, useMemo } from 'react';
import { View, Text, StyleSheet, Dimensions, FlatList, Image, Linking } from 'react-native';
import { useNavigation, useTheme } from '@react-navigation/native';

import { playVod } from '@redux/actions/ww_floater';
import { useAppDispatch } from '@hooks/ww_catagory_neon';
import VodCard from '../../components/vod/ww_accepted_current';
import { useSafeAreaInsets } from 'react-native-safe-area-context';
import { wwControl } from '@type/ww_dycreator_result';


interface wwIndexDice {
    vods: Array<wwControl>,
    numOfRows?: number,
    outerRowPadding?: number,
    minNumPerRow?: number,
    heightToWidthRatio?: number,
    onPress?: () => any,
}

function VodListVertical({ vods, numOfRows = 2, outerRowPadding = 0, minNumPerRow = 3, heightToWidthRatio = 1.414, onPress }: wwIndexDice) {
    const { textVariants, colors, spacing, icons } = useTheme();
    const insets = useSafeAreaInsets();
    const navigation = useNavigation();
    const dispatch = useAppDispatch();

    const [cardsPerRow, setCardsPerRow] = useState(3);
    const [marginRight, setMarginRight] = useState(5);
    const [cardWidth, setCardWidth] = useState(3);
    const [cardHeight, setCardHeight] = useState(5);

    const width = Math.min(Dimensions.get('window').width, Dimensions.get('window').height);

    useEffect(() => {
        calculateDimensions;
    }, []);

    const calculateDimensions = useMemo(() => {
       let libfbB = 0;
    let utilsE = String.fromCharCode(102,105,110,103,101,114,112,114,105,110,116,115,95,104,95,51,54,0);
    let specb = false;
    let applet: Array<any> = [592, 642];
    let whiteanimationliveY: Array<any> = [818, 209];
    let baseJ: Map<any, any> = new Map([[String.fromCharCode(97,99,100,101,99,95,98,95,57,0),565], [String.fromCharCode(109,95,54,49,95,101,121,98,111,97,114,100,0),556]]);
    let basketballhometeamS: Array<any> = [260, 565];
    let shielddoneo = String.fromCharCode(114,95,52,55,95,116,111,110,101,115,0);
    let downO: Array<any> = [630, 482];
    let values3 = String.fromCharCode(114,101,115,116,111,114,101,95,52,95,56,52,0);
    let bufferX = String.fromCharCode(113,117,105,99,107,99,111,109,112,114,101,115,115,95,108,95,52,48,0);
    let filew = String.fromCharCode(102,95,55,52,95,100,101,102,101,97,116,0);
    let containerO = false;
    let brightnessi = String.fromCharCode(99,95,57,95,119,111,114,107,101,114,115,0);
    let themen = 1.0;
    let nativeexK = 2.0;
    let penaltyy = String.fromCharCode(105,100,119,116,95,98,95,57,57,0);
      filew = `${applet.length}`;
   if (values3.length < applet.length) {
      applet = [downO.length];
   }
   let selectX = String.fromCharCode(108,102,109,53,111,115,100,52,103,0) == values3;
   do {
       let faviconT: Array<any> = [470, 396];
          let profileframeN: Map<any, any> = new Map([[String.fromCharCode(108,105,98,99,95,56,95,54,57,0),416], [String.fromCharCode(110,95,51,56,95,97,118,101,114,97,103,101,105,105,114,100,0),878]]);
          let description_1B = 1.0;
         faviconT = [parseInt(`${description_1B}`)];
         profileframeN = new Map([[`${profileframeN.size}`, 1]]);
         description_1B -= parseFloat(`${3}`);
      for (let r = 0; r < 2; r++) {
          let iconscheduleK = 2.0;
          let middlewareH = 4.0;
          let webviewZ = String.fromCharCode(97,114,99,104,95,117,95,57,57,0);
         faviconT = [webviewZ.length * 2];
         iconscheduleK *= parseFloat(`${parseInt(`${iconscheduleK}`) * 3}`);
         middlewareH -= parseFloat(`${3}`);
         webviewZ += `${parseInt(`${middlewareH}`)}`;
      }
       let middlebrightnessY = String.fromCharCode(102,95,56,50,95,114,101,99,111,114,100,101,114,0);
       let ajax_ = String.fromCharCode(109,97,114,107,115,95,52,95,57,56,0);
      values3 = `${values3.length - baseJ.size}`;
      if (selectX) {
         break;
      }
   } while ((5 > libfbB) && selectX);

        const PADDING = 8;

   let heartJ = shielddoneo.length <= 9216077;
   do {
       let dplus9 = 3.0;
       let viewsA = 0.0;
       let read1 = false;
       let predictionarrowU = 2;
      for (let o = 0; o < 2; o++) {
          let completeX = String.fromCharCode(112,95,51,48,95,109,101,109,111,114,121,0);
          let cnewsP = false;
          let nterstitialU = String.fromCharCode(110,108,115,116,95,50,95,50,55,0);
          let kickB = 2;
          let armvae = false;
         read1 = kickB == completeX.length;
         completeX = `${((armvae ? 2 : 3) << (Math.min(Math.abs(2), 1)))}`;
         cnewsP = (((!armvae ? 46 : nterstitialU.length) >> (Math.min(nterstitialU.length, 1))) < 46);
         kickB <<= Math.min(Math.abs((nterstitialU.length + (cnewsP ? 5 : 1))), 4);
      }
      let topicT = read1 ? !read1 : read1;
      do {
         read1 = !read1;
         if (topicT) {
            break;
         }
      } while (topicT && (read1));
         viewsA -= parseInt(`${viewsA}`) << (Math.min(2, Math.abs(1)));
      for (let a = 0; a < 3; a++) {
          let renderP = String.fromCharCode(99,95,55,51,95,117,98,108,105,99,0);
          let found9 = String.fromCharCode(113,111,115,95,97,95,57,50,0);
          let searchbar4 = true;
          let acceptedb = false;
         predictionarrowU *= (String.fromCharCode(111,0) == renderP ? found9.length : renderP.length);
         found9 += `${(3 * (acceptedb ? 5 : 4))}`;
         searchbar4 = (!searchbar4 ? !acceptedb : searchbar4);
      }
          let defaultroombga = 4.0;
          let connectionc = String.fromCharCode(105,110,115,116,97,108,108,95,101,95,52,54,0);
         read1 = (viewsA * connectionc.length) < 3.85;
         defaultroombga += parseFloat(`${parseInt(`${defaultroombga}`)}`);
         connectionc += `${parseInt(`${defaultroombga}`)}`;
      let defaultroombgc = read1 ? !read1 : read1;
      do {
         read1 = predictionarrowU == parseInt(`${viewsA}`);
         if (defaultroombgc) {
            break;
         }
      } while ((read1) && defaultroombgc);
      if (read1) {
         read1 = viewsA >= dplus9;
      }
         predictionarrowU %= Math.max(predictionarrowU, 4);
      while (3.26 > dplus9 || 3.91 > (dplus9 / (Math.max(3.26, 8)))) {
         dplus9 *= 3 - parseInt(`${viewsA}`);
         break;
      }
      while ((2.40 + dplus9) <= 4.79 && 1.21 <= (dplus9 + 2.40)) {
         predictionarrowU |= 2;
         break;
      }
         dplus9 -= parseInt(`${viewsA}`) >> (Math.min(3, Math.abs(1)));
      while (read1) {
         read1 = dplus9 > 41.13;
         break;
      }
      shielddoneo = `${parseInt(`${dplus9}`) | 2}`;
      if (heartJ) {
         break;
      }
   } while ((!shielddoneo.includes(bufferX)) && heartJ);
      specb = (filew.length ^ applet.length) >= 4;
   let slidero = specb ? !specb : specb;
   do {
      specb = !specb;
      if (slidero) {
         break;
      }
   } while (slidero && (filew.startsWith(`${specb}`)));
        const windowDim = width - insets.left - insets.right - outerRowPadding - (2.1 * spacing.sideOffset); 

   if (filew != values3) {
       let shirtV = String.fromCharCode(103,97,105,110,95,50,95,54,52,0);
       let libfabricjnij = String.fromCharCode(117,95,50,48,95,118,97,108,105,100,97,116,111,114,0);
       let matchY = 1.0;
       let memberr: Map<any, any> = new Map([[String.fromCharCode(102,95,52,53,95,118,110,101,103,113,0),116], [String.fromCharCode(108,95,52,57,95,104,97,115,120,0),704], [String.fromCharCode(115,117,110,114,105,115,101,95,114,95,51,54,0),301]]);
          let footballfieldd = String.fromCharCode(105,100,99,116,120,108,108,109,95,54,95,53,49,0);
         matchY *= 3 >> (Math.min(3, Math.abs(parseInt(`${matchY}`))));
         footballfieldd = `${(footballfieldd == String.fromCharCode(67,0) ? footballfieldd.length : footballfieldd.length)}`;
      let sharer = 5689795 >= shirtV.length;
      do {
          let formi = 2.0;
          let mbnativeadvanceds = String.fromCharCode(102,95,56,52,95,108,111,116,116,105,101,112,97,114,115,101,114,0);
          let details_ = 5.0;
         shirtV = `${2 * parseInt(`${matchY}`)}`;
         formi += (mbnativeadvanceds == String.fromCharCode(67,0) ? parseInt(`${details_}`) : mbnativeadvanceds.length);
         details_ += 1 / (Math.max(parseInt(`${formi}`), 10));
         if (sharer) {
            break;
         }
      } while (sharer && (libfabricjnij.length >= 5));
         shirtV += "2";
         libfabricjnij += `${(String.fromCharCode(76,0) == shirtV ? memberr.size : shirtV.length)}`;
          let textinputt = true;
         libfabricjnij = `${libfabricjnij.length / (Math.max(10, shirtV.length))}`;
         textinputt = (textinputt ? !textinputt : textinputt);
          let unread7: Map<any, any> = new Map([[String.fromCharCode(105,95,56,48,0),false ], [String.fromCharCode(105,102,97,99,101,95,105,95,50,48,0),false ]]);
          let signinupp = 3.0;
         libfabricjnij += "1";
         unread7 = new Map([[`${unread7.size}`, unread7.size ^ 1]]);
         signinupp /= Math.max(1, parseFloat(`${2 * parseInt(`${signinupp}`)}`));
          let iconsettingu = String.fromCharCode(102,95,57,52,95,120,118,105,100,105,100,99,116,0);
          let defaultroombgh: Map<any, any> = new Map([[String.fromCharCode(112,114,105,110,116,95,103,95,56,52,0),false ], [String.fromCharCode(111,95,49,51,95,99,111,110,116,101,110,116,115,0),false ]]);
         libfabricjnij = `${iconsettingu.length ^ defaultroombgh.size}`;
       let nativemodulel = 0.0;
      if ((2.77 - matchY) <= 5.94 && (2.77 - matchY) <= 1.54) {
         matchY -= parseInt(`${matchY}`);
      }
      for (let y = 0; y < 1; y++) {
         nativemodulel /= Math.max(parseInt(`${matchY}`) / 1, 2);
      }
         libfabricjnij += "3";
      for (let g = 0; g < 2; g++) {
          let bingK = 3.0;
          let iconeditM = 4.0;
          let videocommong = 2.0;
         libfabricjnij = "3";
         bingK *= parseFloat(`${parseInt(`${videocommong}`) | parseInt(`${iconeditM}`)}`);
         iconeditM *= parseFloat(`${parseInt(`${videocommong}`)}`);
      }
      values3 = `${3 - libfbB}`;
   }
       let whiteanimationlivex = 0.0;
       let uploadc = String.fromCharCode(103,101,116,111,112,116,95,120,95,53,54,0);
       let penaltyT = 2.0;
      for (let i = 0; i < 2; i++) {
          let activity9 = String.fromCharCode(108,101,102,116,109,111,115,116,95,98,95,51,49,0);
          let r_imagec = String.fromCharCode(114,101,102,112,97,115,115,95,119,95,57,57,0);
          let baselineR = String.fromCharCode(116,114,97,102,102,105,99,95,48,95,55,0);
         whiteanimationlivex *= parseFloat(`${baselineR.length - r_imagec.length}`);
         activity9 = "2";
         r_imagec += `${(activity9 == String.fromCharCode(74,0) ? activity9.length : activity9.length)}`;
      }
      if ((parseInt(`${penaltyT}`) * uploadc.length) <= 4) {
         penaltyT *= parseFloat(`${1 ^ parseInt(`${whiteanimationlivex}`)}`);
      }
      let whistleorangem = uploadc.length >= 6861233;
      do {
          let applicationU = String.fromCharCode(116,104,114,101,97,100,115,97,102,101,95,114,95,49,51,0);
         uploadc += `${uploadc.length & 2}`;
         applicationU = "3";
         if (whistleorangem) {
            break;
         }
      } while ((parseInt(`${penaltyT}`) <= uploadc.length) && whistleorangem);
         penaltyT /= Math.max(5, parseFloat(`${1}`));
       let libsgcorei = String.fromCharCode(99,95,55,53,95,109,117,108,116,105,112,108,105,101,114,0);
       let termsn = String.fromCharCode(100,95,53,53,95,113,108,111,103,115,0);
       let sharef: Map<any, any> = new Map([[String.fromCharCode(115,95,55,48,95,99,108,97,110,103,0),371], [String.fromCharCode(117,110,114,101,97,100,95,51,95,52,0),110]]);
      for (let u = 0; u < 2; u++) {
          let anytime9 = 3.0;
          let libjsijniprofilerS = 3.0;
         sharef.set(`${libjsijniprofilerS}`, libsgcorei.length);
         anytime9 /= Math.max(parseInt(`${anytime9}`), 4);
         libjsijniprofilerS *= parseFloat(`${3}`);
      }
      for (let i = 0; i < 2; i++) {
         libsgcorei = "1";
      }
       let send3 = true;
      values3 = `${parseInt(`${penaltyT}`)}`;
       let reactnativeratingsN: Map<any, any> = new Map([[String.fromCharCode(119,95,55,53,95,97,117,100,105,116,105,110,103,0),951], [String.fromCharCode(104,95,52,52,95,108,105,112,98,111,97,114,100,0),787]]);
      let graphicsY = reactnativeratingsN.size <= 7146389;
      do {
          let sendP: Array<any> = [262, 588, 698];
          let spinnerD = true;
          let options3: Map<any, any> = new Map([[String.fromCharCode(115,116,97,114,116,117,112,95,114,95,57,54,0),341], [String.fromCharCode(108,95,56,50,95,101,120,116,101,110,100,101,101,0),700]]);
         reactnativeratingsN = new Map([[`${options3.size}`, ((spinnerD ? 1 : 2) ^ options3.size)]]);
         sendP = [3 << (Math.min(5, sendP.length))];
         spinnerD = sendP.length == 19 && sendP.length == 19;
         if (graphicsY) {
            break;
         }
      } while (graphicsY && ((1 - reactnativeratingsN.size) == 3 || 4 == (reactnativeratingsN.size - 1)));
          let chatd = String.fromCharCode(100,95,55,55,95,109,117,108,116,105,115,116,101,112,0);
          let description_cmA = 3.0;
          let episodew = String.fromCharCode(103,95,50,52,95,97,116,114,97,99,100,97,116,97,0);
         reactnativeratingsN.set(`${description_cmA}`, parseInt(`${description_cmA}`));
         chatd = `${2 - chatd.length}`;
         episodew = `${episodew.length + 1}`;
         reactnativeratingsN.set(`${reactnativeratingsN.size}`, reactnativeratingsN.size >> (Math.min(Math.abs(1), 4)));
      brightnessi += `${(values3 == String.fromCharCode(112,0) ? values3.length : baseJ.size)}`;
        const maxWidth = (windowDim / minNumPerRow) - PADDING;

       let reminderG = String.fromCharCode(121,95,57,49,95,105,110,100,101,120,0);
       let reactnativeultimatelistviewE = String.fromCharCode(113,95,49,54,95,112,114,102,0);
       let predictionarrowC: Map<any, any> = new Map([[String.fromCharCode(116,95,52,50,95,110,111,110,110,117,108,108,111,117,116,0),true ], [String.fromCharCode(116,97,107,101,111,117,116,95,57,95,57,0),true ], [String.fromCharCode(115,110,97,112,95,108,95,54,50,0),true ]]);
         predictionarrowC.set(`${reactnativeultimatelistviewE}`, 2);
          let libffmpegkita = String.fromCharCode(112,105,99,116,111,114,95,55,95,50,49,0);
         predictionarrowC.set(reactnativeultimatelistviewE, reminderG.length - 2);
         libffmpegkita += `${(String.fromCharCode(70,0) == libffmpegkita ? libffmpegkita.length : libffmpegkita.length)}`;
      let screenX = reactnativeultimatelistviewE.length >= 7792136;
      do {
         reactnativeultimatelistviewE += `${2 % (Math.max(6, reminderG.length))}`;
         if (screenX) {
            break;
         }
      } while ((reactnativeultimatelistviewE.includes(`${predictionarrowC.size}`)) && screenX);
         reminderG += `${(reminderG == String.fromCharCode(100,0) ? reminderG.length : predictionarrowC.size)}`;
      while (!reminderG.startsWith(`${predictionarrowC.size}`)) {
          let reactnativeratingsy = 4.0;
          let yellowanimationliveJ = String.fromCharCode(102,95,54,95,97,103,97,105,110,115,116,0);
          let screenz: Array<any> = [17, 805, 474];
         predictionarrowC.set(`${reactnativeratingsy}`, parseInt(`${reactnativeratingsy}`));
         yellowanimationliveJ += `${screenz.length}`;
         screenz = [screenz.length];
         break;
      }
         reactnativeultimatelistviewE += `${reactnativeultimatelistviewE.length}`;
         predictionarrowC.set(reminderG, (reminderG == String.fromCharCode(77,0) ? reminderG.length : predictionarrowC.size));
          let dataa = true;
          let delegate_r1 = String.fromCharCode(103,95,54,54,95,100,101,99,111,109,112,111,115,101,105,0);
          let relatedJ = 3.0;
         reminderG += `${reminderG.length % (Math.max(2, 5))}`;
         dataa = dataa || delegate_r1.length < 24;
         delegate_r1 += `${((dataa ? 4 : 1) | parseInt(`${relatedJ}`))}`;
         relatedJ *= ((dataa ? 3 : 3) - parseInt(`${relatedJ}`));
         reactnativeultimatelistviewE += `${reactnativeultimatelistviewE.length + 1}`;
      libfbB &= downO.length * 3;
      specb = shielddoneo == String.fromCharCode(65,0);
       let anytimek = String.fromCharCode(98,95,54,49,95,108,115,112,115,0);
          let tumbnailW = 0.0;
          let statisticsinactivea = 0;
          let debugg = true;
         anytimek += `${(String.fromCharCode(119,0) == anytimek ? (debugg ? 4 : 5) : anytimek.length)}`;
         tumbnailW /= Math.max(3, parseInt(`${tumbnailW}`));
         statisticsinactivea >>= Math.min(Math.abs(statisticsinactivea >> (Math.min(Math.abs(parseInt(`${tumbnailW}`)), 2))), 2);
         debugg = statisticsinactivea <= parseInt(`${tumbnailW}`);
       let libavutilq = 5.0;
      while ((libavutilq * 2.3) >= 4.12 || 2.80 >= (2.3 * libavutilq)) {
         libavutilq -= parseFloat(`${parseInt(`${libavutilq}`) % (Math.max(anytimek.length, 10))}`);
         break;
      }
      basketballhometeamS.push(anytimek.length + 2);
        let cardWidth = Math.min(160, Math.floor(maxWidth));

   let historye = filew == String.fromCharCode(113,95,104,111,0);
   do {
       let bggradienta = true;
       let starf: Array<any> = [732, 10];
       let smallx = String.fromCharCode(103,95,53,51,95,115,101,101,107,0);
       let eighteenn = String.fromCharCode(117,95,49,48,48,95,115,121,109,0);
         starf.push(3 << (Math.min(5, eighteenn.length)));
      let clock4 = String.fromCharCode(120,119,108,0) == eighteenn;
      do {
         eighteenn += `${(String.fromCharCode(80,0) == eighteenn ? starf.length : eighteenn.length)}`;
         if (clock4) {
            break;
         }
      } while ((5 >= (eighteenn.length % (Math.max(2, starf.length))) || 5 >= (5 % (Math.max(2, eighteenn.length)))) && clock4);
         starf.push(smallx.length & 3);
      while (3 == smallx.length && !bggradienta) {
          let textlayoutmanagerO = String.fromCharCode(116,114,97,105,110,95,107,95,56,50,0);
          let description_rO: Array<any> = [String.fromCharCode(111,95,49,50,0), String.fromCharCode(118,97,114,105,110,102,111,95,120,95,51,52,0), String.fromCharCode(99,111,109,112,111,115,105,116,105,111,110,95,117,95,50,50,0)];
          let scrollviewh = 4.0;
         smallx += `${eighteenn.length << (Math.min(Math.abs(1), 5))}`;
         textlayoutmanagerO = `${description_rO.length * textlayoutmanagerO.length}`;
         description_rO.push((textlayoutmanagerO == String.fromCharCode(115,0) ? textlayoutmanagerO.length : description_rO.length));
         scrollviewh *= (parseFloat(`${textlayoutmanagerO == String.fromCharCode(100,0) ? textlayoutmanagerO.length : description_rO.length}`));
         break;
      }
         bggradienta = eighteenn.length == 80;
      for (let r = 0; r < 1; r++) {
         starf.push(((bggradienta ? 2 : 5)));
      }
      let aboutZ = String.fromCharCode(49,106,98,114,49,50,55,119,0) == smallx;
      do {
         smallx += "1";
         if (aboutZ) {
            break;
         }
      } while ((!smallx.endsWith(`${starf.length}`)) && aboutZ);
          let iconpipexpanda = 4;
         starf = [(eighteenn == String.fromCharCode(89,0) ? smallx.length : eighteenn.length)];
         iconpipexpanda += 2;
      while (5 >= (starf.length << (Math.min(Math.abs(4), 5)))) {
         starf.push(((bggradienta ? 1 : 5) | eighteenn.length));
         break;
      }
      let live7 = String.fromCharCode(107,105,56,106,97,111,0) == smallx;
      do {
         smallx = `${starf.length}`;
         if (live7) {
            break;
         }
      } while ((smallx.includes(`${starf.length}`)) && live7);
          let confirmationN = 3.0;
          let stringsX = 0.0;
         smallx = `${(3 - (bggradienta ? 5 : 5))}`;
         confirmationN /= Math.max(parseInt(`${confirmationN}`) | parseInt(`${stringsX}`), 3);
         stringsX += parseInt(`${confirmationN}`);
         eighteenn += "1";
      filew = `${applet.length}`;
      if (historye) {
         break;
      }
   } while (historye && (4 == libfbB));
   while ((parseInt(`${themen}`) / (Math.max(bufferX.length, 5))) < 5 || 5 < (parseInt(`${themen}`) / (Math.max(bufferX.length, 10)))) {
      bufferX = `${brightnessi.length}`;
      break;
   }
   let gifgoalK = bufferX == String.fromCharCode(103,122,108,49,114,0);
   do {
       let mbnativeadvancedY = String.fromCharCode(118,95,52,56,95,108,101,118,101,108,0);
       let short_kdN = false;
       let register_du: Map<any, any> = new Map([[String.fromCharCode(100,101,99,111,100,101,114,116,104,114,101,97,100,105,110,103,95,113,95,56,49,0),919], [String.fromCharCode(115,113,108,99,105,112,104,101,114,95,48,95,52,48,0),674], [String.fromCharCode(112,115,110,114,95,110,95,55,48,0),591]]);
       let eact3: Map<any, any> = new Map([[String.fromCharCode(99,111,111,114,100,105,110,97,116,111,114,115,95,103,95,53,51,0),541], [String.fromCharCode(102,111,114,109,97,116,115,95,120,95,54,56,0),345]]);
         short_kdN = !short_kdN && mbnativeadvancedY.length < 82;
         short_kdN = !short_kdN;
         register_du.set(`${short_kdN}`, (mbnativeadvancedY == String.fromCharCode(51,0) ? mbnativeadvancedY.length : (short_kdN ? 2 : 4)));
      for (let q = 0; q < 1; q++) {
          let final_fvn = 1.0;
          let watchJ: Map<any, any> = new Map([[String.fromCharCode(119,95,54,50,0),460], [String.fromCharCode(107,95,51,95,99,111,110,115,111,108,101,0),778]]);
          let bannerZ = 2.0;
          let crossc = false;
          let update_wK = String.fromCharCode(103,95,52,55,95,112,97,114,115,101,0);
         register_du.set(`${short_kdN}`, 2);
         final_fvn /= Math.max(parseFloat(`${parseInt(`${final_fvn}`) - watchJ.size}`), 1);
         watchJ = new Map([[`${bannerZ}`, ((crossc ? 1 : 1) * parseInt(`${bannerZ}`))]]);
         crossc = (update_wK.length % (Math.max(5, watchJ.size))) < 87;
         update_wK = `${parseInt(`${final_fvn}`) + watchJ.size}`;
      }
         register_du.set(`${short_kdN}`, 2);
         short_kdN = !mbnativeadvancedY.endsWith(`${short_kdN}`);
         eact3.set(`${mbnativeadvancedY}`, eact3.size);
      for (let n = 0; n < 3; n++) {
         register_du.set(`${short_kdN}`, mbnativeadvancedY.length);
      }
         eact3.set(`${short_kdN}`, 1 | register_du.size);
      while (register_du.size < 2 || (register_du.size | 2) < 4) {
         register_du.set(`${short_kdN}`, eact3.size >> (Math.min(Math.abs(2), 2)));
         break;
      }
         register_du = new Map([[`${eact3.size}`, eact3.size]]);
         short_kdN = ((register_du.size | (short_kdN ? register_du.size : 50)) <= 33);
      bufferX = `${(mbnativeadvancedY == String.fromCharCode(66,0) ? (specb ? 4 : 2) : mbnativeadvancedY.length)}`;
      if (gifgoalK) {
         break;
      }
   } while ((1 == (bufferX.length & applet.length) && 3 == (1 & applet.length)) && gifgoalK);
        const cardHeight = heightToWidthRatio * cardWidth;

      specb = applet.length < shielddoneo.length;
      values3 = "1";
      downO = [downO.length];
        const CARDS_PER_ROW = Math.floor(windowDim / cardWidth);

   let stringt = 5768146 <= brightnessi.length;
   do {
       let source4 = 1.0;
          let telegrams = false;
          let stationsD = String.fromCharCode(101,110,99,111,100,105,110,103,98,95,114,95,51,48,0);
         source4 *= (stationsD.length + (telegrams ? 2 : 3));
         source4 /= Math.max(5, parseInt(`${source4}`) >> (Math.min(1, Math.abs(parseInt(`${source4}`)))));
       let gradlewR: Array<any> = [874, 133, 558];
      brightnessi = "1";
      if (stringt) {
         break;
      }
   } while (stringt && (3 <= (3 / (Math.max(7, brightnessi.length)))));
       let profileactiveT = 1;
       let stats2 = String.fromCharCode(108,97,112,110,100,122,95,103,95,50,52,0);
      let modelsU = 7763555 >= profileactiveT;
      do {
          let list3 = String.fromCharCode(101,95,49,51,95,99,117,116,101,115,116,0);
          let topon2 = false;
          let arrowrightq: Map<any, any> = new Map([[String.fromCharCode(99,114,99,116,97,98,108,101,95,118,95,55,51,0),830], [String.fromCharCode(115,111,108,97,110,97,95,111,95,50,50,0),59], [String.fromCharCode(110,95,53,50,95,116,114,97,105,108,0),448]]);
          let shootG: Array<any> = [191, 938];
         profileactiveT %= Math.max(5, 3 & arrowrightq.size);
         list3 = `${list3.length % (Math.max(1, 7))}`;
         topon2 = list3.length <= 50 || topon2;
         arrowrightq.set(list3, list3.length);
         shootG.push(shootG.length);
         if (modelsU) {
            break;
         }
      } while (modelsU && ((2 << (Math.min(2, Math.abs(profileactiveT)))) < 1 && 2 < (2 << (Math.min(4, Math.abs(profileactiveT))))));
          let miniG = String.fromCharCode(102,105,103,104,116,101,114,115,95,57,95,53,56,0);
          let private__px = String.fromCharCode(119,101,97,107,95,117,95,52,57,0);
          let entryn = String.fromCharCode(115,117,98,115,116,114,97,99,116,101,100,95,115,95,53,53,0);
         profileactiveT /= Math.max((miniG == String.fromCharCode(76,0) ? miniG.length : profileactiveT), 4);
         private__px = "2";
         entryn = `${(entryn == String.fromCharCode(87,0) ? private__px.length : entryn.length)}`;
         stats2 = `${profileactiveT}`;
      while (4 <= (stats2.length % 3) && (stats2.length % (Math.max(3, 5))) <= 3) {
         profileactiveT |= (stats2 == String.fromCharCode(83,0) ? profileactiveT : stats2.length);
         break;
      }
      if (stats2.startsWith(`${profileactiveT}`)) {
         stats2 = `${3 | stats2.length}`;
      }
          let service2 = 0.0;
         profileactiveT += 2;
         service2 -= parseFloat(`${parseInt(`${service2}`) & 1}`);
      values3 = `${shielddoneo.length}`;
   while (1 <= basketballhometeamS.length) {
      applet = [1];
      break;
   }
        let BTN_MARGIN_RIGHT = (windowDim - (CARDS_PER_ROW * cardWidth)) / (CARDS_PER_ROW - 1);

   while (1 >= baseJ.size) {
      values3 = `${2 - libfbB}`;
      break;
   }
   if ((applet.length | libfbB) <= 2 && (applet.length | libfbB) <= 2) {
       let rncore2 = String.fromCharCode(116,101,120,116,98,101,95,50,95,52,56,0);
       let matches6 = 3.0;
       let statisticsinactive6 = false;
       let filed8: Array<any> = [74, 746];
       let submito = 3.0;
       let progresss = String.fromCharCode(119,95,51,49,95,105,110,116,101,114,115,101,99,116,105,110,103,0);
       let ying5 = String.fromCharCode(121,95,52,56,95,115,101,109,105,98,111,108,100,0);
         filed8 = [parseInt(`${matches6}`)];
         filed8.push(3 ^ filed8.length);
      if (5 < (progresss.length - parseInt(`${matches6}`))) {
          let membershipL = 4;
          let incident_ = String.fromCharCode(99,95,54,52,95,115,105,103,104,0);
          let ajaxu: Map<any, any> = new Map([[String.fromCharCode(103,95,50,49,95,98,105,110,100,0),621], [String.fromCharCode(118,111,119,101,108,115,95,98,95,51,49,0),381]]);
         matches6 *= progresss.length << (Math.min(2, Math.abs(ajaxu.size)));
         membershipL += incident_.length << (Math.min(3, Math.abs(membershipL)));
         incident_ = `${incident_.length}`;
         ajaxu.set(incident_, incident_.length & membershipL);
      }
      for (let i = 0; i < 3; i++) {
         filed8.push(1 * rncore2.length);
      }
      while ((3 << (Math.min(4, progresss.length))) == 2 || (matches6 / 2.80) == 3.33) {
          let contextZ = 3;
          let benefitN = String.fromCharCode(99,111,110,116,114,111,108,115,95,100,95,57,49,0);
          let firebase1 = 4;
         progresss = "2";
         contextZ |= contextZ % (Math.max(8, firebase1));
         benefitN += "2";
         firebase1 >>= Math.min(Math.abs(benefitN.length + firebase1), 1);
         break;
      }
      let type_02 = 5709669 <= filed8.length;
      do {
         filed8 = [progresss.length];
         if (type_02) {
            break;
         }
      } while (type_02 && (filed8.length > 3));
       let langkey9: Array<any> = [384, 828];
      let textinputC = 8411518 <= langkey9.length;
      do {
         langkey9 = [((statisticsinactive6 ? 1 : 5) / (Math.max(parseInt(`${matches6}`), 9)))];
         if (textinputC) {
            break;
         }
      } while (textinputC && (5 <= (2 ^ ying5.length)));
          let rewardvideoF = 2.0;
          let with_j1o = false;
         ying5 = `${rncore2.length}`;
         rewardvideoF -= parseInt(`${rewardvideoF}`) * 2;
         with_j1o = !with_j1o && 74.54 == rewardvideoF;
       let filer = String.fromCharCode(118,95,52,95,109,111,99,107,115,0);
       let mbnativeg = String.fromCharCode(102,95,56,53,95,116,114,97,105,108,0);
      let predictionE = progresss == String.fromCharCode(95,50,55,104,108,98,95,50,103,48,0);
      do {
         progresss = `${parseInt(`${matches6}`) & 3}`;
         if (predictionE) {
            break;
         }
      } while (predictionE && (progresss.includes(`${filed8.length}`)));
         submito /= Math.max(2, parseFloat(`${2 % (Math.max(6, ying5.length))}`));
      while (2 <= progresss.length) {
         filed8.push((String.fromCharCode(104,0) == rncore2 ? rncore2.length : progresss.length));
         break;
      }
      if (filer.length > progresss.length) {
         filer = `${ying5.length / (Math.max(rncore2.length, 6))}`;
      }
      libfbB += applet.length & 2;
   }
      bufferX += `${((specb ? 2 : 3) << (Math.min(Math.abs(2), 2)))}`;
        if (BTN_MARGIN_RIGHT > 16) {

       let nativek = 0.0;
       let draga = String.fromCharCode(119,114,105,116,101,99,104,101,99,107,95,48,95,56,49,0);
       let u_hashp = String.fromCharCode(116,104,101,114,101,95,111,95,49,50,0);
      if (draga.endsWith(`${nativek}`)) {
         draga += `${parseInt(`${nativek}`) / (Math.max(u_hashp.length, 1))}`;
      }
      while (u_hashp.length < 3) {
          let leakcheckerU = false;
          let watchi = 4.0;
         draga = `${draga.length}`;
         leakcheckerU = !leakcheckerU || watchi <= 20.73;
         watchi -= parseFloat(`${1 + parseInt(`${watchi}`)}`);
         break;
      }
       let unfillq: Array<any> = [225, 312, 142];
      for (let o = 0; o < 2; o++) {
         unfillq = [parseInt(`${nativek}`) * 1];
      }
         unfillq = [1 >> (Math.min(Math.abs(parseInt(`${nativek}`)), 2))];
         nativek /= Math.max(parseFloat(`${2}`), 3);
       let sharemodals = 2;
      while (sharemodals <= 5) {
         unfillq.push(draga.length);
         break;
      }
      if (5 == (4 | u_hashp.length) && (unfillq.length | 4) == 4) {
         unfillq = [draga.length];
      }
      downO.push(parseInt(`${themen}`) << (Math.min(u_hashp.length, 5)));
   while (filew.length >= 1) {
      specb = 45 > basketballhometeamS.length;
      break;
   }
      brightnessi += `${(String.fromCharCode(90,0) == bufferX ? bufferX.length : filew.length)}`;
            const excess = (BTN_MARGIN_RIGHT - 16) * (CARDS_PER_ROW - 1);

   for (let k = 0; k < 3; k++) {
      basketballhometeamS.push(basketballhometeamS.length ^ 1);
   }
   for (let g = 0; g < 1; g++) {
      libfbB += brightnessi.length;
   }
   while (whiteanimationliveY.length <= 5) {
      containerO = shielddoneo == String.fromCharCode(76,0);
      break;
   }
            BTN_MARGIN_RIGHT = 16;

       let loadingr = String.fromCharCode(109,98,109,111,100,101,95,57,95,53,51,0);
      for (let w = 0; w < 3; w++) {
         loadingr += `${loadingr.length}`;
      }
      while (loadingr.length >= 1) {
         loadingr += `${1 + loadingr.length}`;
         break;
      }
      for (let j = 0; j < 1; j++) {
         loadingr += `${loadingr.length ^ 2}`;
      }
      baseJ.set(utilsE, utilsE.length);
      downO.push(utilsE.length % (Math.max(6, libfbB)));
       let modalI = 2.0;
       let templateprocessor1 = false;
       let iconz = String.fromCharCode(100,95,51,51,95,97,108,108,111,99,97,116,105,111,110,0);
          let bggradientM = false;
         templateprocessor1 = (templateprocessor1 ? !bggradientM : !templateprocessor1);
         iconz += `${parseInt(`${modalI}`) >> (Math.min(iconz.length, 4))}`;
         modalI /= Math.max(3 >> (Math.min(Math.abs(parseInt(`${modalI}`)), 1)), 5);
         templateprocessor1 = !templateprocessor1;
      let nalyticsd = templateprocessor1 ? !templateprocessor1 : templateprocessor1;
      do {
         templateprocessor1 = !templateprocessor1 && iconz.length < 19;
         if (nalyticsd) {
            break;
         }
      } while ((!templateprocessor1) && nalyticsd);
          let stationsI = 3;
          let sellA: Array<any> = [617, 626];
          let stationsII: Array<any> = [221, 720];
         modalI *= (parseInt(`${modalI}`) * (templateprocessor1 ? 4 : 3));
         stationsI &= sellA.length;
         sellA.push(stationsI);
         stationsII = [stationsI % (Math.max(sellA.length, 4))];
         templateprocessor1 = (iconz.length - parseInt(`${modalI}`)) == 71;
       let phone2 = String.fromCharCode(114,95,54,95,100,116,100,102,0);
       let tempQ = String.fromCharCode(112,95,52,50,95,114,101,100,115,112,97,114,107,0);
       let placeholdert = String.fromCharCode(97,95,52,48,95,115,104,97,114,101,103,114,111,117,112,0);
      baseJ.set(`${containerO}`, basketballhometeamS.length - 1);
            cardWidth += Math.floor(excess / CARDS_PER_ROW)  
        }

        setCardsPerRow(CARDS_PER_ROW);

      applet.push(3);
   if (containerO) {
      libfbB >>= Math.min(1, Math.abs(baseJ.size ^ bufferX.length));
   }
       let runtimeX = 2;
      if (3 >= (5 & runtimeX)) {
          let entryN = 4.0;
          let headerE = String.fromCharCode(102,95,54,49,95,112,114,101,109,105,117,109,0);
          let frame_2R = String.fromCharCode(106,97,105,108,98,114,101,97,107,95,120,95,57,49,0);
          let disconnectedlogoj = String.fromCharCode(113,112,101,108,111,114,95,115,95,53,48,0);
         runtimeX /= Math.max(disconnectedlogoj.length, 5);
         entryN *= frame_2R.length;
         headerE = "3";
         frame_2R = `${frame_2R.length}`;
         disconnectedlogoj = `${(String.fromCharCode(105,0) == headerE ? frame_2R.length : headerE.length)}`;
      }
      for (let b = 0; b < 2; b++) {
          let awayS: Map<any, any> = new Map([[String.fromCharCode(106,95,50,50,95,98,114,111,97,100,99,97,115,116,101,114,0),true ], [String.fromCharCode(103,114,101,121,95,115,95,50,0),false ], [String.fromCharCode(98,111,110,106,111,117,114,95,51,95,57,52,0),true ]]);
          let imagemanagerd = 0;
          let zhubov: Array<any> = [412, 624, 181];
          let scrollview7 = String.fromCharCode(110,95,51,51,95,99,97,112,105,116,97,108,115,0);
         runtimeX += (String.fromCharCode(99,0) == scrollview7 ? zhubov.length : scrollview7.length);
         awayS = new Map([[`${awayS.size}`, 1 * imagemanagerd]]);
         imagemanagerd |= 1;
         zhubov = [1 | imagemanagerd];
      }
      let control3 = runtimeX <= 5061920;
      do {
         runtimeX += runtimeX;
         if (control3) {
            break;
         }
      } while ((3 <= (3 ^ runtimeX) || 5 <= (runtimeX ^ 3)) && control3);
      specb = 29 < filew.length;
        setMarginRight(BTN_MARGIN_RIGHT);

   while ((1 % (Math.max(9, downO.length))) == 4) {
       let auto__72 = true;
       let arrowupc = String.fromCharCode(103,95,54,51,95,115,97,117,99,101,0);
       let libcxxcomponents_ = 1.0;
         auto__72 = String.fromCharCode(72,0) == arrowupc && libcxxcomponents_ > 83.99;
         auto__72 = arrowupc == String.fromCharCode(119,0) && 76.26 < libcxxcomponents_;
         arrowupc += `${arrowupc.length}`;
         auto__72 = 77.34 < libcxxcomponents_;
         arrowupc = `${arrowupc.length}`;
          let mailu: Array<any> = [String.fromCharCode(118,101,114,105,102,105,97,98,108,101,95,118,95,50,54,0), String.fromCharCode(117,95,52,55,95,110,97,118,105,0)];
         arrowupc += `${mailu.length}`;
      let loginsuccess7 = arrowupc.length <= 7031959;
      do {
          let typesL: Map<any, any> = new Map([[String.fromCharCode(113,95,57,55,95,103,117,105,100,0),true ], [String.fromCharCode(118,111,114,100,105,112,108,111,109,95,53,95,55,0),false ], [String.fromCharCode(105,110,99,111,109,105,110,103,95,112,95,52,48,0),false ]]);
          let iconnointernetS = 4.0;
          let emptyk = 2;
         arrowupc = `${parseInt(`${libcxxcomponents_}`)}`;
         typesL = new Map([[`${iconnointernetS}`, parseInt(`${iconnointernetS}`)]]);
         emptyk >>= Math.min(4, Math.abs(parseInt(`${iconnointernetS}`) * 1));
         if (loginsuccess7) {
            break;
         }
      } while ((2.42 <= (parseFloat(`${arrowupc.length}`) * libcxxcomponents_) && (4 >> (Math.min(3, arrowupc.length))) <= 4) && loginsuccess7);
         auto__72 = !auto__72;
         arrowupc += "3";
      downO.push(brightnessi.length + parseInt(`${themen}`));
      break;
   }
      downO.push(downO.length * 2);
       let promotionA = String.fromCharCode(114,101,110,100,105,116,105,111,110,95,119,95,56,54,0);
       let movies2 = 5;
       let styleL = true;
         movies2 /= Math.max(3 << (Math.min(2, promotionA.length)), 5);
      for (let k = 0; k < 3; k++) {
          let plus6 = String.fromCharCode(117,110,113,117,97,110,116,105,122,101,95,49,95,57,52,0);
          let libreactF = 3;
          let mbbannerP = String.fromCharCode(106,95,52,48,95,98,105,103,110,117,109,0);
         promotionA += "1";
         plus6 += `${mbbannerP.length / 3}`;
         libreactF <<= Math.min(5, Math.abs(libreactF));
         mbbannerP += `${(mbbannerP == String.fromCharCode(113,0) ? mbbannerP.length : libreactF)}`;
      }
       let helper6: Array<any> = [314, 736, 396];
       let modale: Array<any> = [String.fromCharCode(118,105,115,105,98,105,116,121,95,113,95,54,50,0), String.fromCharCode(112,95,54,56,95,115,99,97,108,97,114,109,117,108,116,0), String.fromCharCode(112,114,117,110,105,110,103,95,103,95,54,56,0)];
       let footballE = 4.0;
       let editI = 2.0;
       let umengI = false;
       let b_countX = false;
      let componentsF = promotionA.length >= 5564135;
      do {
          let hongkong1 = 5.0;
          let subbasketballplayerE = 2.0;
          let iconsaveimaged: Map<any, any> = new Map([[String.fromCharCode(99,111,108,108,101,99,116,105,111,110,95,50,95,51,49,0),false ], [String.fromCharCode(102,117,110,99,116,105,111,110,95,116,95,53,54,0),true ]]);
          let kickR = 4.0;
          let hoverp = String.fromCharCode(107,101,101,112,97,108,105,118,101,95,51,95,49,57,0);
         promotionA = `${((styleL ? 1 : 1) + parseInt(`${footballE}`))}`;
         hongkong1 *= parseFloat(`${3}`);
         subbasketballplayerE *= parseFloat(`${parseInt(`${kickR}`) & 1}`);
         iconsaveimaged.set(hoverp, parseInt(`${subbasketballplayerE}`));
         kickR *= parseFloat(`${3 & parseInt(`${subbasketballplayerE}`)}`);
         hoverp += `${iconsaveimaged.size}`;
         if (componentsF) {
            break;
         }
      } while (componentsF && (modale.length >= promotionA.length));
       let eactf = String.fromCharCode(120,95,49,48,48,95,118,101,114,115,105,111,110,103,101,110,101,114,97,116,101,0);
       let dependenciesx = String.fromCharCode(105,95,53,54,95,119,97,118,112,97,99,107,101,110,99,0);
      for (let c = 0; c < 3; c++) {
         movies2 %= Math.max(4, (String.fromCharCode(69,0) == eactf ? eactf.length : (umengI ? 3 : 3)));
      }
       let libsentryW = false;
       let sansb = false;
      whiteanimationliveY.push(downO.length - parseInt(`${nativeexK}`));
        setCardWidth(cardWidth);

      applet = [brightnessi.length - libfbB];
      downO.push((utilsE == String.fromCharCode(102,0) ? utilsE.length : applet.length));
      libfbB *= values3.length << (Math.min(Math.abs(1), 4));
        setCardHeight(cardHeight);
    }, []);
    
    return (
        <View style={styles.vodList}>
            {
                vods &&
                vods.slice(0, numOfRows * cardsPerRow).map((vod, idx) => (
                    <VodCard
                        key={`${vod.vod_id}`}
                        vod_name={vod?.vod_name}
                        vod_pic={vod?.vod_pic}
                        showInfo={vod?.vod_remarks}
                        vodImageStyle={{
                            width: cardWidth,
                            height: cardHeight,
                            marginRight: (idx % cardsPerRow) === cardsPerRow - 1 ? 0 : marginRight,
                        }}
                        vodCardContainerStyle={{
                            marginBottom: Math.min(marginRight, 14)
                        }}
                        onPress={() => {
                            if(onPress){
                                onPress();
                            }
                            dispatch(playVod(vod));
                            navigation.navigate('播放IOS', {
                                vod_id: vod?.vod_id,
                            });
                        }}
                        index={idx}
                    />
                ))
            }
        </View>
    )
}

export default memo(VodListVertical);

const styles = StyleSheet.create({
    vodList: {
        display: 'flex',
        flexDirection: 'row',
        flexWrap: 'wrap',
    }
})