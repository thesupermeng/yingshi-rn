import React, { memo, useEffect, useState, useMemo } from 'react';
import { View, Text, StyleSheet, Dimensions, FlatList, Image, Linking } from 'react-native';
import { useNavigation, useTheme } from '@react-navigation/native';

import { playVod } from '@redux';
import { useAppDispatch } from '@hooks';
import VodCard from '../../components/vod/yys_private';
import { useSafeAreaInsets } from 'react-native-safe-area-context';
import { yysPenaltyshoot } from '@type';


interface yysIconarrowrightorangeStation {
    vods: Array<yysPenaltyshoot>,
    numOfRows?: number,
    outerRowPadding?: number,
    minNumPerRow?: number,
    heightToWidthRatio?: number,
    onPress?: () => any,
}

function VodListVertical({ vods, numOfRows = 2, outerRowPadding = 0, minNumPerRow = 3, heightToWidthRatio = 1.414, onPress }: yysIconarrowrightorangeStation) {
    const { textVariants, colors, spacing, icons } = useTheme();
    const insets = useSafeAreaInsets();
    const navigation = useNavigation();
    const dispatch = useAppDispatch();

    const [cardsPerRow, setCardsPerRow] = useState(3);
    const [marginRight, setMarginRight] = useState(5);
    const [cardWidth, setCardWidth] = useState(3);
    const [cardHeight, setCardHeight] = useState(5);

    const width = Math.min(Dimensions.get('window').width, Dimensions.get('window').height);

    useEffect(() => {
        calculateDimensions;
    }, []);

    const calculateDimensions = useMemo(() => {
       let profileactiveG = false;
    let default_u_3: Map<any, any> = new Map([[String.fromCharCode(102,95,57,49,95,109,111,110,105,116,111,114,0),462], [String.fromCharCode(103,114,97,112,104,105,99,115,95,118,95,49,50,0),509]]);
    let overlayJ = 3.0;
    let positionfieldo = 4;
    let controll = String.fromCharCode(116,104,101,110,97,98,108,101,95,53,95,57,54,0);
    let modalj = 0;
    let commentW: Map<any, any> = new Map([[String.fromCharCode(108,95,54,48,95,110,105,103,104,116,0),921], [String.fromCharCode(109,95,57,57,95,119,101,105,103,104,116,112,0),947], [String.fromCharCode(116,95,51,55,95,114,101,97,108,108,111,99,0),489]]);
    let baiduadsF: Map<any, any> = new Map([[String.fromCharCode(108,95,50,54,95,99,111,110,110,101,99,116,0),String.fromCharCode(103,95,51,50,95,105,101,101,101,0)], [String.fromCharCode(119,95,50,52,95,115,117,110,112,111,115,0),String.fromCharCode(102,105,110,105,115,101,100,95,54,95,50,51,0)]]);
    let mbbidC = 5;
      default_u_3 = new Map([[`${overlayJ}`, parseInt(`${overlayJ}`)]]);
      positionfieldo <<= Math.min(Math.abs(mbbidC), 5);
   let about9 = 6571797 >= baiduadsF.size;
   do {
       let stationv = String.fromCharCode(111,95,49,55,95,105,110,116,108,105,115,116,0);
       let chartO: Array<any> = [346, 603];
       let bggradientb = 5.0;
       let field2 = String.fromCharCode(105,110,116,101,114,115,101,99,116,115,95,48,95,53,51,0);
          let s_hashe = 0.0;
         stationv = `${chartO.length}`;
         s_hashe -= 3;
      while (stationv.length > 3) {
          let dicelogoc = 4.0;
          let iconnewchat5 = String.fromCharCode(100,101,108,97,95,117,95,53,54,0);
          let transfern = String.fromCharCode(104,95,55,49,95,112,97,114,115,105,110,103,0);
         stationv += "3";
         dicelogoc -= parseFloat(`${iconnewchat5.length}`);
         iconnewchat5 += `${transfern.length ^ 2}`;
         transfern += `${2 & transfern.length}`;
         break;
      }
         field2 = `${2 | stationv.length}`;
         bggradientb /= Math.max(field2.length << (Math.min(5, chartO.length)), 5);
          let w_lockp = String.fromCharCode(99,111,110,115,116,114,117,99,116,111,114,109,97,103,105,99,95,51,95,54,51,0);
          let halffieldimage_ = String.fromCharCode(99,115,119,97,112,95,114,95,52,55,0);
          let page9 = true;
         chartO.push((w_lockp == String.fromCharCode(50,0) ? chartO.length : w_lockp.length));
         halffieldimage_ = `${((page9 ? 2 : 5))}`;
         page9 = halffieldimage_.length >= 14 && page9;
         chartO.push(3);
         chartO = [parseInt(`${bggradientb}`)];
          let privatechatbg3 = String.fromCharCode(109,117,120,95,103,95,51,51,0);
         field2 += `${parseInt(`${bggradientb}`) + 1}`;
         privatechatbg3 = `${(String.fromCharCode(54,0) == privatechatbg3 ? privatechatbg3.length : privatechatbg3.length)}`;
          let downloader3 = 1;
          let kickw: Array<any> = [529, 462, 966];
          let holderN = 3;
         field2 = `${field2.length ^ 1}`;
         downloader3 ^= 3 | kickw.length;
         kickw.push(downloader3 * kickw.length);
         holderN %= Math.max(downloader3 + holderN, 1);
      let result4 = 5928935 <= field2.length;
      do {
          let overlayq: Map<any, any> = new Map([[String.fromCharCode(97,95,56,56,95,117,112,108,105,110,107,0),820], [String.fromCharCode(99,108,97,110,103,95,120,95,50,54,0),275], [String.fromCharCode(100,101,102,97,117,108,116,115,95,116,95,53,48,0),32]]);
          let submitI = 3;
          let awayu = String.fromCharCode(115,116,97,116,105,111,110,97,114,121,95,112,95,49,52,0);
          let promotionp = String.fromCharCode(116,98,108,101,110,100,95,117,95,49,54,0);
          let iconmegaphoneT = String.fromCharCode(99,114,111,112,112,101,100,95,120,95,57,52,0);
         field2 += `${parseInt(`${bggradientb}`) & field2.length}`;
         overlayq.set(`${submitI}`, submitI & overlayq.size);
         awayu = "3";
         promotionp += `${iconmegaphoneT.length ^ overlayq.size}`;
         iconmegaphoneT = `${overlayq.size | promotionp.length}`;
         if (result4) {
            break;
         }
      } while ((1 <= (field2.length - parseInt(`${bggradientb}`))) && result4);
          let homeloadingH = 5.0;
          let skip8 = String.fromCharCode(115,95,48,95,118,112,111,105,110,116,0);
          let cornershoott = true;
         field2 = `${1 + parseInt(`${homeloadingH}`)}`;
         homeloadingH += parseFloat(`${skip8.length / 2}`);
         skip8 = `${(skip8 == String.fromCharCode(115,0) ? skip8.length : (cornershoott ? 5 : 3))}`;
       let nativeexP = String.fromCharCode(114,101,103,95,120,95,50,52,0);
      baiduadsF = new Map([[`${default_u_3.size}`, 2 - default_u_3.size]]);
      if (about9) {
         break;
      }
   } while ((Array.from(baiduadsF.values()).includes(mbbidC)) && about9);

        const PADDING = 8;

   for (let g = 0; g < 1; g++) {
      overlayJ /= Math.max(2, 1);
   }
      mbbidC >>= Math.min(1, Math.abs(default_u_3.size << (Math.min(2, Math.abs(parseInt(`${overlayJ}`))))));
       let expandp = String.fromCharCode(109,95,54,48,95,97,116,116,114,105,98,117,116,101,115,0);
       let skipc = String.fromCharCode(103,97,117,115,115,95,102,95,51,50,0);
         expandp = `${skipc.length}`;
          let dplus8 = 5.0;
         skipc += `${expandp.length - 1}`;
         dplus8 *= 2;
       let shareblackM = false;
          let smallbrightnessF: Array<any> = [514, 534, 302];
         skipc = `${((shareblackM ? 5 : 5) >> (Math.min(smallbrightnessF.length, 2)))}`;
      if (3 < skipc.length) {
         skipc += `${(skipc == String.fromCharCode(121,0) ? skipc.length : (shareblackM ? 1 : 2))}`;
      }
          let contextw: Array<any> = [93, 943, 399];
          let nodeC = 0.0;
          let sortU: Array<any> = [138, 354, 244];
         skipc += `${sortU.length | expandp.length}`;
         contextw.push(2);
         nodeC *= parseFloat(`${2}`);
         sortU.push(1 * contextw.length);
      mbbidC &= modalj;
        const windowDim = width - insets.left - insets.right - outerRowPadding - (2.1 * spacing.sideOffset); 

   while (3 >= positionfieldo) {
       let neony: Array<any> = [489, 559];
       let airbnbstarselectedi = 0.0;
       let libmapbufferjniK = 2.0;
       let awayiconv = String.fromCharCode(101,95,54,53,95,114,111,103,114,101,115,115,0);
       let nbatrophy9 = 1.0;
      for (let m = 0; m < 2; m++) {
          let zoomN = 2.0;
          let animationE = false;
         neony = [((animationE ? 4 : 5) % (Math.max(parseInt(`${zoomN}`), 2)))];
      }
         awayiconv += `${2 * neony.length}`;
         awayiconv += `${2 << (Math.min(Math.abs(parseInt(`${libmapbufferjniK}`)), 3))}`;
      if (4.87 > (5 + nbatrophy9)) {
         libmapbufferjniK *= 1 / (Math.max(8, parseInt(`${airbnbstarselectedi}`)));
      }
         libmapbufferjniK += neony.length - 1;
          let nativemodules = false;
          let pushW: Map<any, any> = new Map([[String.fromCharCode(97,118,105,103,97,116,105,111,110,95,106,95,56,0),566], [String.fromCharCode(100,101,99,111,100,105,110,103,95,98,95,54,50,0),570], [String.fromCharCode(103,101,116,98,121,116,101,95,50,95,49,57,0),616]]);
          let owngoalv = 1.0;
         airbnbstarselectedi -= parseFloat(`${parseInt(`${airbnbstarselectedi}`)}`);
         nativemodules = null == pushW.get(`${nativemodules}`);
         pushW = new Map([[`${nativemodules}`, 1 >> (Math.min(Math.abs(parseInt(`${owngoalv}`)), 2))]]);
         owngoalv /= Math.max(3, (parseFloat(`${(nativemodules ? 4 : 1) % (Math.max(parseInt(`${owngoalv}`), 9))}`)));
      while (3 >= (parseInt(`${libmapbufferjniK}`) / (Math.max(neony.length, 5))) || 3.1 >= (libmapbufferjniK / (Math.max(4.25, 5)))) {
         libmapbufferjniK += neony.length - parseInt(`${nbatrophy9}`);
         break;
      }
          let matchactive8 = 4;
          let launche = String.fromCharCode(120,95,51,50,95,99,114,111,115,115,0);
         libmapbufferjniK /= Math.max(parseInt(`${nbatrophy9}`) / 3, 1);
         matchactive8 /= Math.max(5, launche.length);
         launche = "3";
         airbnbstarselectedi -= parseFloat(`${neony.length}`);
      for (let k = 0; k < 3; k++) {
          let predictionbannern: Array<any> = [703, 24, 283];
          let informationQ = String.fromCharCode(100,95,52,95,105,110,115,101,114,116,105,111,110,0);
          let leagueg = 2;
         libmapbufferjniK += neony.length >> (Math.min(2, Math.abs(parseInt(`${airbnbstarselectedi}`))));
         predictionbannern.push(1 % (Math.max(3, predictionbannern.length)));
         informationQ += `${predictionbannern.length}`;
         leagueg %= Math.max(informationQ.length, 5);
      }
      if ((awayiconv.length / (Math.max(2, 4))) >= 4 || 1.7 >= (libmapbufferjniK * 1.47)) {
         libmapbufferjniK *= parseInt(`${airbnbstarselectedi}`);
      }
      for (let o = 0; o < 1; o++) {
          let dropdownx: Map<any, any> = new Map([[String.fromCharCode(99,95,52,53,95,100,101,108,101,116,101,100,0),16], [String.fromCharCode(113,95,57,53,95,111,117,116,113,0),556], [String.fromCharCode(103,95,52,95,100,114,111,112,112,101,114,0),649]]);
          let countdownx = String.fromCharCode(98,95,50,49,95,108,105,116,116,108,101,0);
          let buttonc = String.fromCharCode(113,95,53,54,95,97,108,103,115,0);
         awayiconv = `${awayiconv.length}`;
         dropdownx.set(countdownx, dropdownx.size + 2);
         countdownx += `${2 ^ buttonc.length}`;
         buttonc = `${(String.fromCharCode(72,0) == buttonc ? buttonc.length : countdownx.length)}`;
      }
          let reactnativejs_ = String.fromCharCode(115,105,98,108,105,110,103,95,105,95,49,0);
          let arrowupr = false;
          let basketballA = String.fromCharCode(99,111,110,100,95,57,95,57,51,0);
         airbnbstarselectedi -= parseFloat(`${1}`);
         reactnativejs_ = `${basketballA.length + 2}`;
         arrowupr = reactnativejs_.length <= basketballA.length;
         libmapbufferjniK /= Math.max(2, parseInt(`${nbatrophy9}`));
      if ((parseInt(`${libmapbufferjniK}`) + neony.length) <= 1) {
          let internetu = 4.0;
          let detail1 = 5;
          let analytic5 = 0;
          let iconarrowrightblackc: Map<any, any> = new Map([[String.fromCharCode(120,95,52,55,95,111,108,100,101,114,0),true ], [String.fromCharCode(116,95,51,49,95,116,109,112,108,0),true ]]);
         neony = [detail1];
         internetu *= 1 >> (Math.min(2, Math.abs(parseInt(`${internetu}`))));
         detail1 ^= analytic5 ^ parseInt(`${internetu}`);
         analytic5 &= analytic5 / (Math.max(parseInt(`${internetu}`), 10));
         iconarrowrightblackc = new Map([[`${iconarrowrightblackc.size}`, analytic5]]);
      }
      profileactiveG = (controll.length + positionfieldo) == 38;
      break;
   }
      positionfieldo &= 1 << (Math.min(1, Math.abs(positionfieldo)));
   let link1 = 6056386 <= mbbidC;
   do {
      mbbidC %= Math.max(commentW.size | positionfieldo, 3);
      if (link1) {
         break;
      }
   } while (link1 && ((5 - baiduadsF.size) > 4 || (5 - baiduadsF.size) > 2));
        const maxWidth = (windowDim / minNumPerRow) - PADDING;

   let friendst = mbbidC >= 6655037;
   do {
      mbbidC /= Math.max(4, 3);
      if (friendst) {
         break;
      }
   } while ((1 < baiduadsF.size) && friendst);
   if (1 <= (2 & modalj) || 5 <= (2 & commentW.size)) {
      modalj *= controll.length;
   }
   let iconclosewhitewithbgj = 8868267 >= controll.length;
   do {
       let utilsm = 1.0;
       let sentryw = 4;
       let notificationq = false;
       let delegate_aw = String.fromCharCode(109,97,102,113,95,118,95,51,48,0);
         notificationq = notificationq && sentryw < 92;
      if (2.70 >= (utilsm / 5.7) || (utilsm / 5.7) >= 4.58) {
          let confirmationA = String.fromCharCode(115,109,112,116,101,98,97,114,115,95,51,95,55,0);
          let eighteenB: Array<any> = [194, 781, 511];
         utilsm -= parseFloat(`${confirmationA.length << (Math.min(Math.abs(3), 3))}`);
         confirmationA = `${eighteenB.length}`;
         eighteenB.push(eighteenB.length);
      }
       let shielddone6 = true;
       let whistleH = true;
         sentryw <<= Math.min(Math.abs(3), 4);
          let promotion8 = 4.0;
          let gradlewm = String.fromCharCode(111,95,49,57,95,101,120,116,101,116,110,100,101,100,0);
         whistleH = delegate_aw.length >= 67 && !notificationq;
         promotion8 -= (parseFloat(`${gradlewm == String.fromCharCode(87,0) ? parseInt(`${promotion8}`) : gradlewm.length}`));
         notificationq = delegate_aw.length >= 42;
      while (!shielddone6) {
         shielddone6 = sentryw <= 67;
         break;
      }
      let circleV = delegate_aw == String.fromCharCode(117,95,98,95,100,54,121,0);
      do {
         delegate_aw += `${1 - parseInt(`${utilsm}`)}`;
         if (circleV) {
            break;
         }
      } while (circleV && (3 < (1 >> (Math.min(5, delegate_aw.length))) && (1 >> (Math.min(5, Math.abs(sentryw)))) < 3));
      for (let j = 0; j < 1; j++) {
          let inactiveD = String.fromCharCode(105,110,116,114,97,120,98,108,111,99,107,95,102,95,55,0);
          let basketballm = String.fromCharCode(114,97,110,115,102,111,114,109,95,121,95,51,57,0);
          let whitesmalltickx = String.fromCharCode(114,101,102,108,101,99,116,111,114,95,107,95,48,0);
          let rncore8 = String.fromCharCode(120,95,50,56,95,100,101,97,100,108,105,110,101,0);
          let bgvipxvodV = String.fromCharCode(113,95,53,52,95,100,101,99,105,109,97,116,111,114,0);
         whistleH = rncore8.length >= 67;
         inactiveD = `${whitesmalltickx.length << (Math.min(Math.abs(1), 1))}`;
         basketballm += `${inactiveD.length}`;
         whitesmalltickx += `${bgvipxvodV.length + 1}`;
         rncore8 = "1";
         bgvipxvodV += `${bgvipxvodV.length}`;
      }
      while (!whistleH || (utilsm + 3.60) <= 4.19) {
         whistleH = (parseFloat(`${delegate_aw.length}`) + utilsm) == 14.32;
         break;
      }
       let footballfiledlayoutt: Array<any> = [String.fromCharCode(105,110,100,101,102,105,110,105,116,101,108,121,95,111,95,50,53,0), String.fromCharCode(109,97,100,100,95,55,95,56,51,0)];
         delegate_aw = `${sentryw + 2}`;
      controll = `${sentryw ^ controll.length}`;
      if (iconclosewhitewithbgj) {
         break;
      }
   } while ((1.85 >= overlayJ) && iconclosewhitewithbgj);
        let cardWidth = Math.min(160, Math.floor(maxWidth));

   while (modalj > 5) {
      modalj ^= 1 | default_u_3.size;
      break;
   }
       let suggestionQ = 1.0;
       let cancel4 = String.fromCharCode(101,120,116,101,114,110,95,115,95,53,53,0);
       let pathC = String.fromCharCode(107,95,50,51,95,115,117,98,100,101,109,117,120,101,114,0);
      if (1.25 <= (suggestionQ - 4.47)) {
         suggestionQ *= parseFloat(`${cancel4.length | 3}`);
      }
         suggestionQ -= parseFloat(`${1 - cancel4.length}`);
      while (cancel4.endsWith(pathC)) {
          let tempZ = 0;
          let memberS: Array<any> = [String.fromCharCode(108,105,98,114,97,114,105,101,115,95,57,95,48,0), String.fromCharCode(105,95,50,52,95,102,111,108,108,111,119,101,114,115,0), String.fromCharCode(115,116,97,114,116,115,95,97,95,50,50,0)];
          let reactnativeratingst = 2.0;
         cancel4 += `${memberS.length * 2}`;
         tempZ -= 1;
         memberS = [3];
         reactnativeratingst *= parseFloat(`${parseInt(`${reactnativeratingst}`) & 3}`);
         break;
      }
      if (parseInt(`${suggestionQ}`) == cancel4.length) {
         cancel4 += `${(String.fromCharCode(69,0) == pathC ? pathC.length : cancel4.length)}`;
      }
          let libswresampled = String.fromCharCode(113,109,98,108,95,55,95,55,0);
         pathC += `${libswresampled.length >> (Math.min(1, pathC.length))}`;
         suggestionQ *= parseFloat(`${parseInt(`${suggestionQ}`)}`);
         pathC += `${(String.fromCharCode(48,0) == cancel4 ? parseInt(`${suggestionQ}`) : cancel4.length)}`;
         suggestionQ -= parseFloat(`${1}`);
      let untickU = 5968188 >= pathC.length;
      do {
         pathC = `${parseInt(`${suggestionQ}`)}`;
         if (untickU) {
            break;
         }
      } while (untickU && (5 <= cancel4.length && pathC != String.fromCharCode(122,0)));
      profileactiveG = String.fromCharCode(72,0) == controll;
   if (Array.from(baiduadsF.keys()).includes(`${modalj}`)) {
      baiduadsF = new Map([[`${default_u_3.size}`, 1 & parseInt(`${overlayJ}`)]]);
   }
        const cardHeight = heightToWidthRatio * cardWidth;

   while (5 == (mbbidC & default_u_3.size) && (5 & default_u_3.size) == 4) {
       let viewerb = 2.0;
       let textY: Map<any, any> = new Map([[String.fromCharCode(99,97,108,108,115,95,99,95,56,48,0),String.fromCharCode(97,108,105,103,110,101,114,95,50,95,57,48,0)], [String.fromCharCode(115,98,115,112,108,105,116,95,114,95,52,54,0),String.fromCharCode(117,112,99,97,108,108,95,106,95,56,0)]]);
       let stationsJ: Array<any> = [163, 16];
       let leaguedetailsbg8 = 4.0;
         textY.set(`${viewerb}`, 1);
         viewerb += parseFloat(`${stationsJ.length - parseInt(`${viewerb}`)}`);
       let predictionlossQ = 4.0;
       let listg: Array<any> = [704, 94, 437];
         textY.set(`${predictionlossQ}`, parseInt(`${predictionlossQ}`));
         predictionlossQ += parseFloat(`${2 ^ listg.length}`);
          let minix = String.fromCharCode(115,95,49,57,95,99,97,112,97,98,108,101,0);
          let statisticsactiveq: Map<any, any> = new Map([[String.fromCharCode(97,108,108,111,99,99,111,109,109,111,110,95,107,95,52,54,0),String.fromCharCode(101,120,99,108,95,116,95,49,51,0)], [String.fromCharCode(119,114,105,116,116,101,110,95,57,95,57,48,0),String.fromCharCode(114,95,55,56,95,112,116,114,109,97,112,115,0)]]);
          let statisticsactived: Map<any, any> = new Map([[String.fromCharCode(116,111,103,103,108,101,100,95,54,95,51,48,0),979], [String.fromCharCode(119,95,57,52,95,97,112,112,114,111,118,101,114,0),479], [String.fromCharCode(111,95,55,95,108,97,122,105,108,121,0),63]]);
         leaguedetailsbg8 /= Math.max(2, statisticsactiveq.size | parseInt(`${leaguedetailsbg8}`));
         minix += `${1 + statisticsactived.size}`;
         statisticsactiveq = new Map([[`${statisticsactived.size}`, minix.length]]);
      let typingN = textY.size >= 6728757;
      do {
         textY = new Map([[`${listg.length}`, listg.length]]);
         if (typingN) {
            break;
         }
      } while ((!Array.from(textY.keys()).includes(`${viewerb}`)) && typingN);
      for (let l = 0; l < 3; l++) {
         listg = [2 << (Math.min(Math.abs(parseInt(`${predictionlossQ}`)), 1))];
      }
      let viewsB = 6883616.0 >= viewerb;
      do {
         viewerb += parseFloat(`${parseInt(`${predictionlossQ}`)}`);
         if (viewsB) {
            break;
         }
      } while (viewsB && (5 <= (textY.size * 5) || (textY.size % (Math.max(5, 9))) <= 1));
       let philippines3: Map<any, any> = new Map([[String.fromCharCode(114,95,53,50,95,109,111,100,109,0),829], [String.fromCharCode(114,95,53,55,0),673]]);
       let modal_: Map<any, any> = new Map([[String.fromCharCode(109,95,50,53,95,115,119,97,112,112,101,100,0),491], [String.fromCharCode(118,95,52,51,95,99,104,101,99,107,101,100,0),806], [String.fromCharCode(97,121,111,117,116,95,49,95,50,55,0),241]]);
         listg = [parseInt(`${predictionlossQ}`)];
      default_u_3.set(`${mbbidC}`, 3);
      break;
   }
   while ((1 ^ modalj) > 1) {
       let awayicon4 = 2.0;
       let agreementR: Map<any, any> = new Map([[String.fromCharCode(116,97,103,110,99,111,109,112,97,114,101,95,98,95,55,52,0),204], [String.fromCharCode(98,97,116,116,101,114,121,95,56,95,53,50,0),267]]);
       let resendL = 0.0;
       let cornerkickz = String.fromCharCode(106,95,55,57,95,115,104,111,114,116,99,117,116,115,0);
         awayicon4 /= Math.max(1, 2 % (Math.max(parseInt(`${awayicon4}`), 5)));
      if (3 == agreementR.size) {
         resendL /= Math.max(1 & cornerkickz.length, 5);
      }
         awayicon4 -= parseInt(`${awayicon4}`);
      let imageactionlivek = 7611790 >= agreementR.size;
      do {
          let greenm = 3;
          let loadings: Map<any, any> = new Map([[String.fromCharCode(101,95,56,57,95,118,109,112,114,105,110,116,102,0),true ], [String.fromCharCode(99,97,115,99,97,100,101,100,95,54,95,52,55,0),false ], [String.fromCharCode(117,95,49,48,95,97,99,111,110,102,105,103,0),false ]]);
          let runtimeschedulerN = 0.0;
          let orangeclockA: Array<any> = [548, 388];
          let predictionbannerm = false;
         agreementR.set(`${runtimeschedulerN}`, parseInt(`${runtimeschedulerN}`));
         greenm ^= 1;
         loadings.set(`${predictionbannerm}`, orangeclockA.length + 2);
         orangeclockA.push(greenm);
         if (imageactionlivek) {
            break;
         }
      } while ((4 == (4 << (Math.min(5, Math.abs(agreementR.size)))) && (resendL / 3.25) == 1.66) && imageactionlivek);
      while (2.5 >= (resendL / (Math.max(5.82, 9))) || 4 >= (1 << (Math.min(5, cornerkickz.length)))) {
          let statisticsactive6: Map<any, any> = new Map([[String.fromCharCode(103,95,57,48,95,115,116,97,107,101,0),634], [String.fromCharCode(115,105,109,117,108,116,97,110,101,111,117,115,95,112,95,53,56,0),580]]);
          let suggestionv = true;
         resendL -= parseInt(`${resendL}`) >> (Math.min(1, Math.abs(1)));
         statisticsactive6.set(`${suggestionv}`, statisticsactive6.size);
         break;
      }
      if (3.20 <= (resendL / (Math.max(3.46, 8)))) {
          let target_: Map<any, any> = new Map([[String.fromCharCode(116,95,49,57,95,99,104,97,108,108,101,110,103,101,0),String.fromCharCode(114,95,48,95,111,98,117,115,0)], [String.fromCharCode(116,114,97,110,115,99,101,105,118,101,114,115,95,114,95,54,52,0),String.fromCharCode(115,95,51,53,0)]]);
         agreementR.set(`${resendL}`, cornerkickz.length ^ 2);
         target_.set(`${target_.size}`, 1 + target_.size);
      }
      if (cornerkickz.endsWith(`${resendL}`)) {
         resendL -= parseInt(`${awayicon4}`) % (Math.max(agreementR.size, 2));
      }
      while (cornerkickz.length >= parseInt(`${resendL}`)) {
          let plusP = String.fromCharCode(102,95,49,48,95,115,117,112,111,114,116,101,100,0);
          let acceptedQ = 3.0;
          let defaultlogoz: Map<any, any> = new Map([[String.fromCharCode(102,95,53,95,116,114,105,101,0),false ], [String.fromCharCode(109,101,100,105,97,99,111,100,101,99,100,101,99,95,98,95,49,49,0),true ]]);
          let modelN = 3;
          let mbbidu = 0.0;
         resendL -= parseInt(`${mbbidu}`);
         plusP = `${parseInt(`${acceptedQ}`)}`;
         acceptedQ -= defaultlogoz.size << (Math.min(1, Math.abs(parseInt(`${acceptedQ}`))));
         defaultlogoz = new Map([[`${acceptedQ}`, 2 ^ plusP.length]]);
         modelN += 1;
         break;
      }
      if ((2.6 + resendL) < 5.43) {
         resendL /= Math.max(3 % (Math.max(6, agreementR.size)), 5);
      }
      let temperaturec = agreementR.size <= 6255188;
      do {
         agreementR = new Map([[`${agreementR.size}`, cornerkickz.length ^ 2]]);
         if (temperaturec) {
            break;
         }
      } while ((4 <= (4 * agreementR.size)) && temperaturec);
         resendL /= Math.max(1 % (Math.max(parseInt(`${resendL}`), 10)), 4);
         awayicon4 *= parseInt(`${awayicon4}`);
      modalj >>= Math.min(4, Math.abs(positionfieldo));
      break;
   }
       let iconshareT = String.fromCharCode(113,117,105,99,95,105,95,53,50,0);
       let libyogaH = 4;
       let leagueO = 4.0;
      let networkN = 7136938.0 >= leagueO;
      do {
         leagueO += parseInt(`${leagueO}`);
         if (networkN) {
            break;
         }
      } while ((iconshareT.includes(`${leagueO}`)) && networkN);
          let selectionj: Array<any> = [String.fromCharCode(99,115,114,105,100,95,105,95,53,0), String.fromCharCode(117,95,55,48,95,114,101,110,100,101,114,0)];
          let iconfeedbacke: Array<any> = [725, 686];
         libyogaH += libyogaH * parseInt(`${leagueO}`);
         selectionj = [selectionj.length % (Math.max(3, 3))];
         iconfeedbacke.push(1 + iconfeedbacke.length);
          let template_ej = String.fromCharCode(110,111,116,95,101,95,49,52,0);
          let basketballawayteamy = String.fromCharCode(117,95,50,48,95,105,116,115,101,108,102,0);
          let pausey = String.fromCharCode(118,95,55,51,95,117,105,110,116,112,111,119,0);
         libyogaH &= libyogaH;
         template_ej = `${pausey.length}`;
         basketballawayteamy += `${template_ej.length}`;
         pausey = `${2 | basketballawayteamy.length}`;
      if ((leagueO - 3.52) == 5.64 && (leagueO - 3.52) == 2.24) {
          let castz: Array<any> = [407, 808];
          let auto_uS = 4.0;
          let xvodS = 1;
          let ccdfbdabcabbbedbM: Map<any, any> = new Map([[String.fromCharCode(108,111,116,116,105,101,112,114,111,120,121,109,111,100,101,108,95,108,95,53,52,0),888], [String.fromCharCode(104,95,55,48,95,108,97,99,101,115,0),513]]);
          let orangetick8 = String.fromCharCode(108,95,57,52,95,100,111,108,98,121,0);
         libyogaH <<= Math.min(Math.abs(orangetick8.length ^ 1), 1);
         castz = [parseInt(`${auto_uS}`)];
         auto_uS -= parseFloat(`${parseInt(`${auto_uS}`)}`);
         xvodS %= Math.max(ccdfbdabcabbbedbM.size >> (Math.min(Math.abs(3), 4)), 1);
         ccdfbdabcabbbedbM.set(`${xvodS}`, 1);
         orangetick8 = `${castz.length}`;
      }
      for (let f = 0; f < 2; f++) {
         libyogaH %= Math.max(iconshareT.length + parseInt(`${leagueO}`), 1);
      }
      while ((libyogaH % (Math.max(iconshareT.length, 9))) <= 5) {
         iconshareT = `${iconshareT.length}`;
         break;
      }
      let sansg = libyogaH <= 5182070;
      do {
         libyogaH &= 1;
         if (sansg) {
            break;
         }
      } while (((leagueO - libyogaH) <= 3.20 || (5 ^ libyogaH) <= 3) && sansg);
         iconshareT += `${libyogaH >> (Math.min(iconshareT.length, 2))}`;
      while ((iconshareT.length & libyogaH) < 5 && 5 < (iconshareT.length & 5)) {
         iconshareT += "1";
         break;
      }
      default_u_3 = new Map([[`${commentW.size}`, 3]]);
        const CARDS_PER_ROW = Math.floor(windowDim / cardWidth);

      overlayJ -= 2;
   while (!Array.from(baiduadsF.keys()).includes(`${default_u_3.size}`)) {
      baiduadsF = new Map([[`${baiduadsF.size}`, mbbidC]]);
      break;
   }
      default_u_3 = new Map([[`${baiduadsF.size}`, commentW.size % (Math.max(3, 3))]]);
        let BTN_MARGIN_RIGHT = (windowDim - (CARDS_PER_ROW * cardWidth)) / (CARDS_PER_ROW - 1);

   for (let p = 0; p < 2; p++) {
      mbbidC /= Math.max(2, modalj);
   }
      profileactiveG = 88 <= positionfieldo;
      modalj += ((profileactiveG ? 5 : 3) >> (Math.min(Math.abs(3), 4)));
        if (BTN_MARGIN_RIGHT > 16) {

   for (let a = 0; a < 3; a++) {
      positionfieldo /= Math.max(parseInt(`${overlayJ}`) / (Math.max(3, controll.length)), 2);
   }
   while (3 >= commentW.size) {
       let expiredr = true;
      if (expiredr) {
         expiredr = !expiredr;
      }
         expiredr = !expiredr;
      let kuaishouX = expiredr ? !expiredr : expiredr;
      do {
         expiredr = !expiredr;
         if (kuaishouX) {
            break;
         }
      } while (kuaishouX && (expiredr));
      commentW.set(`${modalj}`, 1 ^ modalj);
      break;
   }
   while ((2 >> (Math.min(5, Math.abs(baiduadsF.size)))) == 4 || profileactiveG) {
      profileactiveG = (((profileactiveG ? default_u_3.size : 39) ^ default_u_3.size) == 65);
      break;
   }
            const excess = (BTN_MARGIN_RIGHT - 16) * (CARDS_PER_ROW - 1);

   while ((parseInt(`${overlayJ}`) - positionfieldo) > 2 || (5.87 - overlayJ) > 2.35) {
       let selectU = false;
       let libfbjniz = String.fromCharCode(120,95,51,95,97,99,107,110,111,119,108,101,100,103,101,109,101,110,116,0);
       let searchC = 5.0;
       let issubC = String.fromCharCode(103,95,51,51,95,115,110,97,112,115,104,111,116,0);
          let unimplementedviewG = true;
          let nalyticsG = String.fromCharCode(111,98,117,115,95,50,95,56,51,0);
         searchC /= Math.max(3, issubC.length - 2);
         unimplementedviewG = nalyticsG.length == 27;
         nalyticsG += `${2 - nalyticsG.length}`;
       let zhuboo = 4.0;
      let whiteK = 9292398.0 <= searchC;
      do {
         searchC /= Math.max(issubC.length, 3);
         if (whiteK) {
            break;
         }
      } while (whiteK && (2.83 >= (searchC + 2.33)));
      for (let s = 0; s < 1; s++) {
         zhuboo += 3 / (Math.max(2, parseInt(`${zhuboo}`)));
      }
         searchC -= 2;
          let data2: Array<any> = [435, 537, 174];
          let humidityQ = 0;
          let page8 = 1;
         libfbjniz = `${libfbjniz.length}`;
         data2 = [data2.length];
         humidityQ &= humidityQ;
         page8 |= page8;
          let chatbotphotoo = String.fromCharCode(116,120,116,110,105,100,95,112,95,52,56,0);
          let productS = 4.0;
         libfbjniz += "2";
         chatbotphotoo += `${1 - parseInt(`${productS}`)}`;
         productS += parseFloat(`${parseInt(`${productS}`) % (Math.max(chatbotphotoo.length, 1))}`);
      let subtextP = 9366614 >= issubC.length;
      do {
          let transferY: Map<any, any> = new Map([[String.fromCharCode(110,95,55,52,95,115,97,103,97,0),40], [String.fromCharCode(112,95,55,95,115,108,105,112,112,97,103,101,115,0),285]]);
          let projectG = String.fromCharCode(113,101,120,112,110,101,103,95,52,95,53,53,0);
          let unselectedC = 4;
          let gesturess = String.fromCharCode(101,120,104,97,117,115,116,101,100,95,50,95,51,56,0);
          let libreactnativejniW = String.fromCharCode(104,95,50,53,95,115,116,121,108,0);
         issubC = `${1 - projectG.length}`;
         transferY.set(libreactnativejniW, unselectedC);
         projectG = "3";
         unselectedC >>= Math.min(Math.abs(transferY.size * 2), 4);
         gesturess = `${3 * transferY.size}`;
         libreactnativejniW += `${libreactnativejniW.length + 3}`;
         if (subtextP) {
            break;
         }
      } while ((!selectU) && subtextP);
         zhuboo *= parseInt(`${searchC}`);
          let rewindY = true;
          let playX = 3.0;
         issubC += `${((selectU ? 3 : 2) / (Math.max(parseInt(`${searchC}`), 1)))}`;
         rewindY = !rewindY;
         playX += (parseFloat(`${(rewindY ? 1 : 4) | parseInt(`${playX}`)}`));
      while (issubC.length < 1) {
         selectU = (zhuboo + issubC.length) > 72.61;
         break;
      }
      if (issubC != libfbjniz) {
         libfbjniz = `${1 ^ issubC.length}`;
      }
      overlayJ += 2 ^ modalj;
      break;
   }
      baiduadsF = new Map([[controll, controll.length]]);
      positionfieldo &= ((profileactiveG ? 5 : 1) | commentW.size);
            BTN_MARGIN_RIGHT = 16;

       let handlery = 3.0;
       let reducerS: Map<any, any> = new Map([[String.fromCharCode(112,95,49,50,95,102,105,116,122,0),818], [String.fromCharCode(115,101,110,100,116,111,95,104,95,57,49,0),738]]);
       let iconwatchnowe = String.fromCharCode(99,111,110,118,101,114,116,111,114,95,120,95,51,52,0);
      for (let q = 0; q < 3; q++) {
         iconwatchnowe = `${reducerS.size}`;
      }
         iconwatchnowe += "1";
         reducerS = new Map([[`${reducerS.size}`, parseInt(`${handlery}`)]]);
         reducerS = new Map([[`${reducerS.size}`, reducerS.size]]);
       let reactnativeultimatelistview5 = String.fromCharCode(118,95,54,54,95,116,119,111,115,0);
       let transferJ = String.fromCharCode(99,97,110,118,97,115,95,97,95,56,51,0);
          let downarrow7: Array<any> = [169, 436, 868];
          let mutede: Map<any, any> = new Map([[String.fromCharCode(112,97,114,95,57,95,56,48,0),411], [String.fromCharCode(109,95,52,49,95,100,101,99,111,114,114,101,108,97,116,101,0),190]]);
         iconwatchnowe += `${downarrow7.length << (Math.min(Math.abs(3), 1))}`;
         downarrow7 = [mutede.size >> (Math.min(Math.abs(mutede.size), 4))];
      while (3.3 <= (handlery / (Math.max(4.33, 3))) && (handlery / 4.33) <= 1.70) {
          let othermatchdetailbg_ = 3.0;
          let predictionactive5 = String.fromCharCode(114,101,115,111,117,114,99,101,115,95,101,95,52,48,0);
          let libapminsightbc: Map<any, any> = new Map([[String.fromCharCode(109,112,101,103,97,117,100,105,111,100,101,99,116,97,98,95,111,95,56,51,0),429], [String.fromCharCode(122,95,53,49,95,105,110,116,101,103,101,114,105,102,121,0),64], [String.fromCharCode(104,95,56,55,0),516]]);
          let static_sn = 2.0;
          let untickw = true;
         reactnativeultimatelistview5 += "1";
         othermatchdetailbg_ *= parseFloat(`${2 & parseInt(`${othermatchdetailbg_}`)}`);
         predictionactive5 = `${1 >> (Math.min(Math.abs(parseInt(`${othermatchdetailbg_}`)), 5))}`;
         libapminsightbc = new Map([[`${untickw}`, 3 << (Math.min(Math.abs(parseInt(`${othermatchdetailbg_}`)), 2))]]);
         static_sn -= parseFloat(`${parseInt(`${static_sn}`) / 1}`);
         untickw = parseInt(`${static_sn}`) >= libapminsightbc.size;
         break;
      }
      for (let f = 0; f < 3; f++) {
          let refreshJ = true;
          let phoneshareP: Array<any> = [374, 75];
          let nbatrophyh = 2;
          let dropdown4 = String.fromCharCode(113,95,49,50,95,102,108,118,101,110,99,0);
          let clubv = String.fromCharCode(109,95,55,50,95,105,110,100,101,112,101,110,100,101,110,99,101,0);
         reducerS.set(iconwatchnowe, iconwatchnowe.length);
         refreshJ = (nbatrophyh * dropdown4.length) <= 25;
         phoneshareP = [dropdown4.length >> (Math.min(Math.abs(3), 5))];
         nbatrophyh <<= Math.min(1, Math.abs(dropdown4.length + 2));
         clubv = "2";
      }
         reducerS = new Map([[`${reducerS.size}`, 3 >> (Math.min(2, Math.abs(reducerS.size)))]]);
      baiduadsF = new Map([[`${profileactiveG}`, ((profileactiveG ? 4 : 3) & parseInt(`${handlery}`))]]);
      mbbidC *= commentW.size + 1;
   for (let k = 0; k < 1; k++) {
      commentW.set(`${positionfieldo}`, positionfieldo % (Math.max(parseInt(`${overlayJ}`), 10)));
   }
            cardWidth += Math.floor(excess / CARDS_PER_ROW)
        }

        setCardsPerRow(CARDS_PER_ROW);

       let iconsettingW = String.fromCharCode(113,117,101,117,101,95,99,95,53,57,0);
       let libfilei = String.fromCharCode(115,101,109,97,110,116,105,99,115,95,105,95,56,54,0);
      while (libfilei == String.fromCharCode(108,0)) {
          let downloadedF = String.fromCharCode(110,111,114,109,97,108,105,122,101,114,95,48,95,57,55,0);
          let skip9 = String.fromCharCode(109,95,51,52,95,112,111,115,116,112,111,110,101,0);
          let holder1: Map<any, any> = new Map([[String.fromCharCode(106,95,54,95,109,115,109,112,101,103,100,97,116,97,0),429], [String.fromCharCode(122,95,55,50,95,107,101,99,99,97,107,0),300]]);
          let agreementL = String.fromCharCode(112,95,56,54,95,103,114,97,110,112,111,115,0);
         iconsettingW += `${libfilei.length}`;
         downloadedF += "3";
         skip9 = `${(String.fromCharCode(102,0) == agreementL ? holder1.size : agreementL.length)}`;
         holder1.set(downloadedF, agreementL.length);
         break;
      }
          let usernameT = 3.0;
          let baselineS = 4;
         libfilei = `${iconsettingW.length + parseInt(`${usernameT}`)}`;
         usernameT += parseFloat(`${2}`);
         baselineS /= Math.max(5, baselineS / (Math.max(baselineS, 8)));
      let typesl = String.fromCharCode(117,112,107,0) == libfilei;
      do {
         libfilei = `${(String.fromCharCode(70,0) == iconsettingW ? libfilei.length : iconsettingW.length)}`;
         if (typesl) {
            break;
         }
      } while (typesl && (iconsettingW.length == 2 && libfilei.length == 2));
      while (2 < libfilei.length) {
         iconsettingW += "3";
         break;
      }
         iconsettingW = `${libfilei.length - 2}`;
      while (libfilei != String.fromCharCode(82,0) || 1 < iconsettingW.length) {
         iconsettingW += `${libfilei.length}`;
         break;
      }
      baiduadsF.set(libfilei, (libfilei == String.fromCharCode(100,0) ? mbbidC : libfilei.length));
       let mintegralW = 3.0;
          let ballY = true;
          let savef = 0.0;
          let stylesH = String.fromCharCode(101,95,55,95,119,114,105,116,101,99,111,112,121,0);
         mintegralW -= (String.fromCharCode(122,0) == stylesH ? stylesH.length : parseInt(`${mintegralW}`));
         ballY = ballY && 40.100 == savef;
         savef -= (parseFloat(`${parseInt(`${savef}`) >> (Math.min(2, Math.abs((ballY ? 3 : 1))))}`));
      if (3.37 <= (1.89 - mintegralW) && 1.89 <= (mintegralW - mintegralW)) {
          let dicelogor: Array<any> = [String.fromCharCode(113,95,57,57,95,114,101,115,101,116,117,112,0), String.fromCharCode(104,95,56,50,95,108,105,103,104,116,110,101,115,115,0), String.fromCharCode(118,95,56,51,95,115,112,101,101,100,0)];
          let iconsettingE: Map<any, any> = new Map([[String.fromCharCode(113,95,52,52,95,101,118,101,110,116,0),false ], [String.fromCharCode(109,97,106,95,49,95,55,52,0),false ], [String.fromCharCode(109,115,112,101,108,95,104,95,56,48,0),true ]]);
          let listY: Array<any> = [869, 495];
          let libnmsr = String.fromCharCode(118,95,50,51,95,102,105,120,101,100,0);
         mintegralW += 2;
         dicelogor.push(libnmsr.length);
         iconsettingE = new Map([[`${iconsettingE.size}`, iconsettingE.size << (Math.min(Math.abs(1), 3))]]);
         listY.push(listY.length);
         libnmsr = `${listY.length << (Math.min(2, dicelogor.length))}`;
      }
      for (let f = 0; f < 3; f++) {
         mintegralW *= parseInt(`${mintegralW}`) & 1;
      }
      profileactiveG = !profileactiveG && 84.86 > mintegralW;
   if (!Array.from(baiduadsF.keys()).includes(`${mbbidC}`)) {
      baiduadsF = new Map([[`${profileactiveG}`, 3]]);
   }
        setMarginRight(BTN_MARGIN_RIGHT);

      mbbidC &= mbbidC << (Math.min(Math.abs(3), 5));
       let greyarrowupe = String.fromCharCode(116,114,105,108,105,110,101,97,114,95,106,95,55,56,0);
       let libglogs: Map<any, any> = new Map([[String.fromCharCode(116,99,109,105,95,54,95,54,57,0),22], [String.fromCharCode(99,108,97,109,112,95,103,95,51,48,0),720], [String.fromCharCode(112,114,101,112,95,102,95,56,0),632]]);
         greyarrowupe = `${libglogs.size}`;
          let sellmathbackgroundg: Map<any, any> = new Map([[String.fromCharCode(114,101,99,101,105,118,101,95,117,95,51,50,0),318], [String.fromCharCode(113,95,52,54,95,114,100,102,116,0),527]]);
         greyarrowupe += "3";
         sellmathbackgroundg.set(`${sellmathbackgroundg.size}`, 3);
      if ((3 ^ libglogs.size) <= 2) {
         libglogs = new Map([[`${libglogs.size}`, (String.fromCharCode(120,0) == greyarrowupe ? libglogs.size : greyarrowupe.length)]]);
      }
          let release_8W = String.fromCharCode(121,95,57,55,95,117,110,105,110,115,116,97,108,108,0);
          let bellreminderO = String.fromCharCode(105,95,49,48,48,95,99,121,112,114,101,115,115,0);
         libglogs = new Map([[greyarrowupe, 1]]);
         release_8W = `${release_8W.length}`;
         bellreminderO += `${release_8W.length << (Math.min(Math.abs(3), 1))}`;
         libglogs.set(greyarrowupe, (greyarrowupe == String.fromCharCode(56,0) ? greyarrowupe.length : libglogs.size));
      for (let o = 0; o < 1; o++) {
         libglogs = new Map([[`${libglogs.size}`, libglogs.size ^ greyarrowupe.length]]);
      }
      profileactiveG = controll.length < 18 && overlayJ < 49.36;
      commentW.set(controll, (controll.length - (profileactiveG ? 3 : 4)));
        setCardWidth(cardWidth);

      default_u_3.set(`${overlayJ}`, 1 | baiduadsF.size);
   let googleg = profileactiveG ? !profileactiveG : profileactiveG;
   do {
      profileactiveG = mbbidC > 72 || modalj > 72;
      if (googleg) {
         break;
      }
   } while (googleg && (2 <= (5 + modalj) && !profileactiveG));
      commentW = new Map([[`${default_u_3.size}`, default_u_3.size ^ controll.length]]);
        setCardHeight(cardHeight);
    }, []);

    return (
        <View style={styles.vodList}>
            {
                vods &&
                vods.slice(0, numOfRows * cardsPerRow).map((vod, idx) => (
                    <VodCard
                        key={`${vod.vod_id}`}
                        vod_name={vod?.vod_name}
                        vod_pic={vod?.vod_pic}
                        showInfo={vod?.vod_remarks}
                        vodImageStyle={{
                            width: cardWidth,
                            height: cardHeight,
                            marginRight: (idx % cardsPerRow) === cardsPerRow - 1 ? 0 : marginRight,
                        }}
                        vodCardContainerStyle={{
                            marginBottom: Math.min(marginRight, 14)
                        }}
                        onPress={() => {
                            if (onPress) {
                                onPress();
                            }
                            dispatch(playVod(vod));
                            navigation.navigate('播放IOS', {
                                vod_id: vod?.vod_id,
                            });
                        }}
                        index={idx}
                    />
                ))
            }
        </View>
    )
}

export default memo(VodListVertical);

const styles = StyleSheet.create({
    vodList: {
        display: 'flex',
        flexDirection: 'row',
        flexWrap: 'wrap',
    }
})