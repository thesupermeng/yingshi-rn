import React, { useRef, memo } from 'react';
import { View, StyleSheet } from 'react-native';
import Play from '@static/images/starProduct.svg';
import Pause from '@static/images/descHeji.svg';
import Rewind from '@static/images/styleShrinkZhubo.svg';
import Fastforward from '@static/images/actionTemperature.svg';
import { RectButton } from 'react-native-gesture-handler';
import { useTheme } from '@react-navigation/native';

type XFillButton = {
  fastForward: (params: any) => any;
  togglePlayPause: () => any;
  paused: boolean;
  videoType?: string;
};

const MiddleControls = ({
  fastForward,
  togglePlayPause,
  paused,
  videoType,
}: XFillButton) => {
  const onSkip = (seconds: number) => {
    fastForward(seconds);
  };

  const onTogglePlayPause = () => {
       let vietnamT = String.fromCharCode(99,95,56,51,95,110,111,110,108,105,110,101,97,114,0);
    let selects = String.fromCharCode(102,117,122,122,101,114,115,95,109,95,49,48,0);
    let sourceo = 3;
    let expiredh = String.fromCharCode(114,95,57,95,104,97,114,100,101,110,101,100,0);
    let middlewarep = String.fromCharCode(118,95,55,55,95,114,101,112,108,105,101,115,0);
    let apple1: Array<any> = [881, 966];
    let typesE = 4;
    let emojiI = false;
    let confirmationL = String.fromCharCode(102,95,54,52,95,116,97,108,108,0);
    let catalogj = false;
    let downloadL = String.fromCharCode(116,101,115,116,98,114,105,100,103,101,95,101,95,57,55,0);
      middlewarep += `${2 - expiredh.length}`;
   let screenR = middlewarep == String.fromCharCode(108,100,97,120,0);
   do {
      middlewarep = `${(vietnamT == String.fromCharCode(51,0) ? sourceo : vietnamT.length)}`;
      if (screenR) {
         break;
      }
   } while (screenR && (middlewarep == String.fromCharCode(108,0) && confirmationL != String.fromCharCode(105,0)));
   for (let t = 0; t < 1; t++) {
       let canvasp = String.fromCharCode(115,112,101,101,99,104,95,104,95,56,54,0);
       let short_enQ = 4;
       let carouselT = String.fromCharCode(99,111,110,116,114,111,108,108,101,114,95,121,95,50,0);
          let benefitz = String.fromCharCode(111,95,55,48,95,105,118,97,114,0);
          let countdownw = String.fromCharCode(111,95,55,53,95,97,114,99,104,101,116,121,112,101,0);
          let native1 = false;
         short_enQ *= 3;
         benefitz = "1";
         countdownw += `${countdownw.length}`;
         native1 = 52 > benefitz.length && countdownw.length > 52;
      let albumy = short_enQ >= 9295655;
      do {
          let downloadB: Map<any, any> = new Map([[String.fromCharCode(104,95,52,50,95,99,111,110,118,101,114,103,101,110,99,101,0),646], [String.fromCharCode(98,102,114,97,99,116,105,111,110,95,49,95,52,54,0),44], [String.fromCharCode(112,114,101,115,101,110,116,97,98,108,101,95,49,95,56,55,0),855]]);
          let b_titleN: Array<any> = [577, 354, 849];
         short_enQ -= downloadB.size * carouselT.length;
         downloadB.set(`${b_titleN.length}`, b_titleN.length);
         if (albumy) {
            break;
         }
      } while ((4 > (3 + short_enQ) && (short_enQ + canvasp.length) > 3) && albumy);
          let sort5 = String.fromCharCode(109,95,55,55,95,116,111,107,101,110,110,102,116,116,120,0);
         short_enQ *= 3;
         sort5 = `${sort5.length + 1}`;
      if (carouselT.length > 3 && 3 > canvasp.length) {
          let episode4 = String.fromCharCode(100,111,119,110,103,114,97,100,101,95,104,95,55,49,0);
          let network1 = String.fromCharCode(116,95,50,48,95,99,111,100,101,0);
          let transfer8 = true;
          let mappinge = 4.0;
          let themeZ = String.fromCharCode(98,97,99,107,114,111,117,110,100,95,54,95,49,53,0);
         canvasp += `${network1.length}`;
         episode4 += `${2 & episode4.length}`;
         network1 += `${parseInt(`${mappinge}`) % (Math.max(themeZ.length, 4))}`;
         transfer8 = episode4.length > 15;
         mappinge *= parseFloat(`${themeZ.length}`);
      }
         carouselT += `${3 ^ canvasp.length}`;
         carouselT = `${carouselT.length}`;
      if (2 >= (carouselT.length << (Math.min(Math.abs(4), 5)))) {
         short_enQ *= 2 - carouselT.length;
      }
      let inactive7 = 7395611 <= short_enQ;
      do {
          let change0: Array<any> = [456, 954];
          let productK: Array<any> = [String.fromCharCode(115,112,105,108,108,95,119,95,49,48,48,0), String.fromCharCode(116,111,116,97,108,108,121,95,51,95,57,55,0)];
          let bingK = true;
          let uploadQ = 2;
          let turnp = 0.0;
         short_enQ %= Math.max(productK.length & parseInt(`${turnp}`), 1);
         change0.push(((bingK ? 1 : 2) << (Math.min(change0.length, 3))));
         productK.push(uploadQ % 3);
         bingK = change0.includes(bingK);
         uploadQ /= Math.max(4, (uploadQ << (Math.min(1, Math.abs((bingK ? 2 : 2))))));
         turnp += parseFloat(`${3}`);
         if (inactive7) {
            break;
         }
      } while (inactive7 && (2 >= (carouselT.length & short_enQ) && (carouselT.length & short_enQ) >= 2));
         carouselT = `${short_enQ << (Math.min(canvasp.length, 5))}`;
      expiredh = `${selects.length + typesE}`;
   }
      selects += `${3 ^ typesE}`;
   for (let l = 0; l < 3; l++) {
      middlewarep += `${expiredh.length}`;
   }
   if (confirmationL.includes(expiredh)) {
      expiredh += `${1 / (Math.max(10, selects.length))}`;
   }
   if (expiredh.endsWith(`${catalogj}`)) {
      expiredh += `${1 + middlewarep.length}`;
   }
       let member3: Map<any, any> = new Map([[String.fromCharCode(99,111,109,101,95,101,95,48,0),356], [String.fromCharCode(117,95,57,53,95,108,108,100,98,105,110,105,116,0),116]]);
       let expandX = String.fromCharCode(118,95,51,55,0);
      if (5 <= (member3.size % (Math.max(1, 3)))) {
         expandX += `${member3.size + 2}`;
      }
       let weibot = true;
         expandX = "3";
         weibot = !weibot;
      for (let i = 0; i < 2; i++) {
         expandX += `${(String.fromCharCode(108,0) == expandX ? expandX.length : member3.size)}`;
      }
         expandX = `${member3.size & expandX.length}`;
      emojiI = typesE == member3.size;
   for (let z = 0; z < 3; z++) {
      expiredh = `${(middlewarep == String.fromCharCode(85,0) ? sourceo : middlewarep.length)}`;
   }
      typesE += 2;
   let cornert = 6973809 >= sourceo;
   do {
      sourceo >>= Math.min(5, selects.length);
      if (cornert) {
         break;
      }
   } while (cornert && (sourceo <= middlewarep.length));
       let shareI: Map<any, any> = new Map([[String.fromCharCode(112,95,57,57,95,101,100,103,101,0),String.fromCharCode(102,111,114,101,118,101,114,95,99,95,49,56,0)], [String.fromCharCode(98,111,120,112,108,111,116,95,55,95,53,49,0),String.fromCharCode(98,105,110,97,114,121,95,97,95,49,55,0)]]);
       let philippinesW = 2.0;
      let gmailx = shareI.size <= 5575025;
      do {
         shareI.set(`${philippinesW}`, parseInt(`${philippinesW}`) % 3);
         if (gmailx) {
            break;
         }
      } while (gmailx && ((2.48 * philippinesW) < 2.50 || (parseFloat(`${shareI.size}`) * philippinesW) < 2.48));
      while (Array.from(shareI.values()).includes(philippinesW)) {
         philippinesW *= parseFloat(`${shareI.size << (Math.min(Math.abs(3), 4))}`);
         break;
      }
      let settingF = shareI.size >= 9762408;
      do {
         shareI.set(`${philippinesW}`, shareI.size >> (Math.min(5, Math.abs(parseInt(`${philippinesW}`)))));
         if (settingF) {
            break;
         }
      } while ((shareI.get(`${philippinesW}`) != null) && settingF);
      if (shareI.size <= 1) {
         philippinesW *= parseFloat(`${parseInt(`${philippinesW}`)}`);
      }
         shareI.set(`${philippinesW}`, shareI.size);
         philippinesW /= Math.max(parseFloat(`${parseInt(`${philippinesW}`) | shareI.size}`), 4);
      expiredh += `${sourceo + 3}`;
   while (confirmationL.length >= 3 && emojiI) {
      emojiI = selects == String.fromCharCode(68,0) || apple1.length > 44;
      break;
   }
      sourceo &= (apple1.length << (Math.min(5, Math.abs((catalogj ? 2 : 5)))));
      emojiI = 20 >= middlewarep.length;
       let dialogV = String.fromCharCode(115,109,115,95,105,95,52,57,0);
       let hearti: Array<any> = [String.fromCharCode(109,101,97,115,117,114,101,100,95,101,95,57,0), String.fromCharCode(119,95,54,50,95,115,117,98,115,116,114,105,110,103,115,0), String.fromCharCode(114,95,53,50,95,99,97,117,115,101,0)];
       let rewindI = String.fromCharCode(109,101,115,115,97,103,101,115,95,54,95,56,0);
         dialogV += `${1 & hearti.length}`;
         hearti = [dialogV.length];
          let suggestionu = false;
         hearti.push((dialogV == String.fromCharCode(66,0) ? hearti.length : dialogV.length));
         suggestionu = !suggestionu;
          let scoreM = 2;
         hearti.push(1 & hearti.length);
         scoreM -= 2;
         hearti = [3 + rewindI.length];
         rewindI = `${rewindI.length}`;
         hearti = [rewindI.length];
         rewindI += `${rewindI.length & 3}`;
       let areav: Array<any> = [10, 611, 338];
       let condensed7: Array<any> = [String.fromCharCode(119,95,49,48,48,95,104,102,121,117,0), String.fromCharCode(109,98,104,115,95,56,95,50,50,0), String.fromCharCode(114,111,111,116,95,121,95,54,57,0)];
      confirmationL = `${((catalogj ? 4 : 1))}`;
   let downloadU = emojiI ? !emojiI : emojiI;
   do {
       let vietnamu = 1.0;
       let customD = String.fromCharCode(120,95,52,51,95,99,108,111,117,100,102,108,97,114,101,0);
       let theme9 = String.fromCharCode(114,103,98,121,117,118,95,101,95,52,57,0);
          let shareda = true;
         vietnamu /= Math.max(3 ^ parseInt(`${vietnamu}`), 3);
         shareda = (!shareda ? !shareda : !shareda);
         vietnamu -= 1;
      emojiI = String.fromCharCode(111,0) == vietnamT;
      if (downloadU) {
         break;
      }
   } while (downloadU && (3 == (apple1.length / (Math.max(3, 8))) && 3 == apple1.length));
       let megaphoneY: Array<any> = [903, 531];
       let progressr: Map<any, any> = new Map([[String.fromCharCode(122,95,51,48,95,100,101,102,97,117,108,0),917], [String.fromCharCode(110,95,56,54,95,101,120,116,114,97,102,105,101,108,100,0),16], [String.fromCharCode(117,95,52,54,95,97,112,116,120,104,100,0),823]]);
       let interstitialq = true;
         megaphoneY.push(progressr.size >> (Math.min(megaphoneY.length, 1)));
         progressr.set(`${interstitialq}`, 3 - megaphoneY.length);
      let episodeC = interstitialq ? !interstitialq : interstitialq;
      do {
         interstitialq = !interstitialq;
         if (episodeC) {
            break;
         }
      } while ((3 <= (3 | progressr.size) && !interstitialq) && episodeC);
         progressr = new Map([[`${progressr.size}`, 2]]);
      while (!Array.from(progressr.values()).includes(megaphoneY.length)) {
         megaphoneY.push(2);
         break;
      }
      catalogj = (progressr.size & middlewarep.length) <= 7;
       let greenA = String.fromCharCode(102,111,110,116,115,105,122,101,95,53,95,54,53,0);
      if (5 > greenA.length) {
          let activeW = String.fromCharCode(100,95,55,56,95,102,101,116,99,104,0);
          let tumbnail2 = 2;
         greenA = `${greenA.length}`;
         activeW = `${(activeW == String.fromCharCode(89,0) ? tumbnail2 : activeW.length)}`;
         tumbnail2 %= Math.max(4, activeW.length >> (Math.min(Math.abs(3), 2)));
      }
       let desc1: Array<any> = [String.fromCharCode(118,95,57,55,95,99,108,101,97,110,115,101,0), String.fromCharCode(112,111,108,108,101,100,95,101,95,51,52,0), String.fromCharCode(116,114,97,105,108,95,116,95,49,55,0)];
       let terms_: Array<any> = [402, 845, 806];
      while ((desc1.length - terms_.length) == 4 && 1 == (4 - terms_.length)) {
          let sentryT = String.fromCharCode(114,95,50,49,95,102,97,115,116,116,101,115,116,0);
         terms_.push(1 >> (Math.min(3, desc1.length)));
         sentryT += `${sentryT.length}`;
         break;
      }
      emojiI = !emojiI;
   if (vietnamT != expiredh) {
      expiredh += `${vietnamT.length ^ expiredh.length}`;
   }
   let main_kn = emojiI ? !emojiI : emojiI;
   do {
      emojiI = 63 == selects.length || 63 == vietnamT.length;
      if (main_kn) {
         break;
      }
   } while (main_kn && (!emojiI || confirmationL.length < 5));
   while (middlewarep != String.fromCharCode(110,0) && confirmationL == String.fromCharCode(54,0)) {
      confirmationL += `${expiredh.length}`;
      break;
   }
   if (!expiredh.includes(`${sourceo}`)) {
       let humidityn = String.fromCharCode(105,110,116,101,114,95,49,95,53,52,0);
         humidityn = `${humidityn.length}`;
      for (let c = 0; c < 3; c++) {
          let watchs = String.fromCharCode(99,117,116,111,102,102,95,54,95,56,53,0);
          let matchE: Array<any> = [String.fromCharCode(101,95,55,52,95,108,111,116,116,105,101,112,114,111,120,121,109,111,100,101,108,0), String.fromCharCode(107,95,57,95,97,115,99,98,110,0)];
          let modelsf = String.fromCharCode(98,95,51,52,95,108,105,98,115,119,105,102,116,0);
          let baiduo: Map<any, any> = new Map([[String.fromCharCode(101,120,99,101,101,100,105,110,103,95,111,95,53,52,0),String.fromCharCode(122,101,116,97,95,119,95,53,48,0)], [String.fromCharCode(111,95,53,53,95,112,117,116,115,116,114,0),String.fromCharCode(109,95,50,57,95,116,105,108,101,0)]]);
          let rankD = 0.0;
         humidityn += `${watchs.length}`;
         watchs = `${baiduo.size * 3}`;
         matchE = [baiduo.size | 2];
         modelsf = `${parseInt(`${rankD}`) % (Math.max(modelsf.length, 9))}`;
         rankD /= Math.max(parseFloat(`${3}`), 2);
      }
         humidityn += `${humidityn.length}`;
      expiredh = `${(sourceo << (Math.min(1, Math.abs((catalogj ? 1 : 5)))))}`;
   }

    togglePlayPause();
  };
  return (
    <View style={styles.middleControls}>
      {/* {videoType !== 'live' && (
        <View style={styles.leftButton}>
          <RectButton
            rippleColor="transparent"
            disallowInterruption={true}
            onPress={() => onSkip(-10)}>
            <Rewind width={55} height={55} />
          </RectButton>
        </View>
      )} */}
      <RectButton
        disallowInterruption={true}
        rippleColor="transparent"
        onPress={() => onTogglePlayPause()}>
        {paused ? (
          <Play width={55} height={55} />
        ) : (
          <Pause width={55} height={55} />
        )}
      </RectButton>
      {/* {videoType !== 'live' && (
        <View style={styles.rightButton}>
          <RectButton
            rippleColor="transparent"
            disallowInterruption={true}
            onPress={() => onSkip(10)}>
            <Fastforward width={55} height={55} />
          </RectButton>
        </View>
      )} */}
    </View>
  );
};

export default memo(MiddleControls);

const styles = StyleSheet.create({
  middleControls: {
    flexDirection: 'row',
  },
  sideButtons: {
    paddingLeft: 46,
    paddingRight: 46,
  },
  leftButton: {
    alignItems: 'flex-end',
    paddingRight: 23,
  },
  rightButton: {
    alignItems: 'flex-start',
    paddingLeft: 23,
  },
});
