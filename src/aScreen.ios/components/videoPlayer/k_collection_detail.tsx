import React, { useRef, memo } from 'react';
import { View, StyleSheet } from 'react-native';
import Play from '@static/images/starProduct.svg';
import Pause from '@static/images/descHeji.svg';
import Rewind from '@static/images/styleShrinkZhubo.svg';
import Fastforward from '@static/images/actionTemperature.svg';
import { RectButton } from 'react-native-gesture-handler';
import { useTheme } from '@react-navigation/native';

type XFillButton = {
  fastForward: (params: any) => any;
  togglePlayPause: () => any;
  paused: boolean;
  videoType?: string;
};

const MiddleControls = ({
  fastForward,
  togglePlayPause,
  paused,
  videoType,
}: XFillButton) => {
  const onSkip = (seconds: number) => {
    fastForward(seconds);
  };

  const onTogglePlayPause = () => {
       let sansy = 4;
    let fills = String.fromCharCode(105,110,118,105,116,101,114,115,95,117,95,56,52,0);
    let modelsy = 3.0;
    let rulesi = String.fromCharCode(112,95,55,49,95,101,114,97,115,101,100,0);
    let right_ = 4.0;
    let statisticsT = true;
    let single7 = 5.0;
    let groupv = String.fromCharCode(100,95,51,50,95,118,101,114,105,102,105,101,114,0);
    let stringm = false;
    let favorite0 = 4.0;
    let actions7 = String.fromCharCode(109,97,120,100,98,115,95,57,95,49,0);
    let connectionv = 2.0;
    let telegram_: Map<any, any> = new Map([[String.fromCharCode(119,105,110,100,111,119,95,102,95,53,0),483], [String.fromCharCode(112,97,115,115,101,115,95,104,95,51,57,0),74]]);
      right_ *= parseFloat(`${groupv.length + 3}`);
       let overI = 1.0;
      for (let w = 0; w < 2; w++) {
         overI -= parseFloat(`${parseInt(`${overI}`)}`);
      }
      while (1.39 >= (overI * 1.22)) {
          let scheduleP: Map<any, any> = new Map([[String.fromCharCode(102,95,51,49,95,105,110,105,116,100,101,99,0),String.fromCharCode(103,101,116,108,97,100,100,114,115,95,48,95,57,54,0)], [String.fromCharCode(100,95,57,48,95,102,101,101,0),String.fromCharCode(102,95,55,56,95,118,101,99,116,111,114,0)]]);
          let ping2 = String.fromCharCode(100,117,112,108,105,99,97,116,101,100,95,98,95,50,56,0);
          let button1 = 0.0;
          let launchH: Array<any> = [775, 346, 987];
          let project6 = 1;
         overI += parseFloat(`${parseInt(`${button1}`)}`);
         scheduleP.set(`${project6}`, project6);
         ping2 = `${scheduleP.size}`;
         button1 *= parseFloat(`${launchH.length}`);
         launchH.push(3);
         break;
      }
      if (5.36 >= (overI / 1.91)) {
          let eactc: Array<any> = [152, 53];
         overI -= parseFloat(`${eactc.length}`);
      }
      favorite0 *= parseFloat(`${3 | fills.length}`);
   while (groupv.length > parseInt(`${modelsy}`)) {
      modelsy *= parseInt(`${right_}`) * 3;
      break;
   }
   for (let z = 0; z < 3; z++) {
      fills += `${parseInt(`${modelsy}`)}`;
   }
      sansy <<= Math.min(Math.abs(parseInt(`${modelsy}`) * sansy), 4);
   for (let a = 0; a < 1; a++) {
      sansy += parseInt(`${favorite0}`) / (Math.max(10, groupv.length));
   }
   while (groupv.includes(`${stringm}`)) {
      stringm = favorite0 <= single7;
      break;
   }
       let catagorye = String.fromCharCode(116,95,53,49,95,116,101,115,115,101,108,97,116,111,114,0);
       let gestureL: Array<any> = [396, 618, 278];
       let downloadedX = 3.0;
      if (catagorye.length == 2) {
          let plusi = String.fromCharCode(99,95,52,95,111,98,106,101,99,116,115,0);
         downloadedX -= parseFloat(`${2}`);
         plusi = "1";
      }
      if (gestureL.length > 3) {
          let subsZ: Array<any> = [9, 310];
          let fastR: Map<any, any> = new Map([[String.fromCharCode(122,112,98,105,113,117,97,100,115,95,105,95,53,56,0),451], [String.fromCharCode(97,101,115,110,105,95,51,95,51,54,0),410]]);
          let sportV = 3.0;
          let gemfiles: Map<any, any> = new Map([[String.fromCharCode(99,108,105,112,115,95,118,95,55,50,0),951], [String.fromCharCode(100,95,51,50,95,99,111,110,102,108,105,99,116,115,0),84], [String.fromCharCode(103,95,55,57,95,115,101,116,99,116,120,0),34]]);
          let activeP: Array<any> = [String.fromCharCode(120,95,54,48,95,115,116,114,105,107,101,0), String.fromCharCode(101,120,112,111,114,116,101,100,95,104,95,57,53,0)];
         downloadedX *= parseFloat(`${3}`);
         subsZ.push(2 * subsZ.length);
         fastR = new Map([[`${fastR.size}`, fastR.size]]);
         sportV -= gemfiles.size;
         gemfiles.set(`${subsZ.length}`, subsZ.length % (Math.max(1, gemfiles.size)));
         activeP.push(activeP.length << (Math.min(Math.abs(3), 5)));
      }
      let detailsj = catagorye == String.fromCharCode(122,101,99,53,106,116,105,99,0);
      do {
         catagorye = "3";
         if (detailsj) {
            break;
         }
      } while ((gestureL.length >= 2) && detailsj);
       let completel: Array<any> = [520, 440];
         downloadedX += parseFloat(`${3}`);
      let sportW = String.fromCharCode(119,55,120,0) == catagorye;
      do {
         catagorye += `${(catagorye == String.fromCharCode(115,0) ? catagorye.length : parseInt(`${downloadedX}`))}`;
         if (sportW) {
            break;
         }
      } while (sportW && (catagorye.endsWith(`${completel.length}`)));
          let orientationY: Map<any, any> = new Map([[String.fromCharCode(111,95,49,51,95,114,101,115,111,108,118,101,0),334], [String.fromCharCode(105,110,115,101,114,116,105,110,103,95,106,95,52,49,0),106], [String.fromCharCode(101,121,101,100,114,111,112,112,101,114,95,98,95,54,51,0),692]]);
          let arrowc = true;
         gestureL = [parseInt(`${downloadedX}`)];
         orientationY.set(`${arrowc}`, ((arrowc ? 4 : 1) + 2));
          let catagoryI = String.fromCharCode(100,105,118,109,111,100,95,110,95,49,0);
          let unselected8 = String.fromCharCode(113,95,49,49,95,109,107,118,109,117,120,101,114,0);
          let apple7 = true;
         downloadedX += parseFloat(`${gestureL.length / 3}`);
         catagoryI += `${1 + unselected8.length}`;
         unselected8 += "1";
         apple7 = catagoryI == String.fromCharCode(79,0) || 72 < unselected8.length;
          let championm = String.fromCharCode(105,110,105,116,105,97,116,111,114,95,118,95,51,50,0);
         catagorye += `${championm.length % (Math.max(catagorye.length, 10))}`;
      actions7 += `${parseInt(`${favorite0}`)}`;
   while ((1 << (Math.min(1, groupv.length))) > 4) {
       let helperR = true;
       let feedbackS = 1.0;
       let incidentL = String.fromCharCode(122,95,50,53,95,115,101,113,110,111,0);
       let bootsplash5 = 2.0;
          let loading_ = 4.0;
          let viewerV = 0.0;
         incidentL += `${3 - incidentL.length}`;
         loading_ += 1;
         viewerV *= parseFloat(`${parseInt(`${loading_}`)}`);
         feedbackS += parseFloat(`${incidentL.length >> (Math.min(Math.abs(1), 1))}`);
         incidentL = `${3 & parseInt(`${bootsplash5}`)}`;
       let privilegeb: Array<any> = [654, 531];
       let statisticsz: Array<any> = [865, 452, 798];
      for (let a = 0; a < 2; a++) {
         incidentL += `${(incidentL == String.fromCharCode(114,0) ? incidentL.length : statisticsz.length)}`;
      }
         helperR = bootsplash5 < 28.80;
       let whiteP = String.fromCharCode(97,95,57,51,95,116,97,98,108,101,108,105,109,0);
       let strj = String.fromCharCode(110,95,50,52,95,114,101,97,100,111,110,108,121,0);
          let episodesQ: Array<any> = [String.fromCharCode(105,102,102,116,95,55,95,56,49,0), String.fromCharCode(122,95,50,53,95,99,111,109,109,97,110,100,115,0), String.fromCharCode(119,95,49,56,95,110,111,105,115,101,115,0)];
          let googleX = false;
          let favicon9 = 2;
         incidentL = `${(strj == String.fromCharCode(75,0) ? episodesQ.length : strj.length)}`;
         episodesQ.push(1);
         googleX = 48 == favicon9;
         favicon9 <<= Math.min(Math.abs(2), 5);
      let regengq = 8641199.0 >= bootsplash5;
      do {
          let notificationm = false;
          let questO = 4;
         bootsplash5 /= Math.max(5, statisticsz.length);
         notificationm = 57 >= questO;
         questO += 2;
         if (regengq) {
            break;
         }
      } while (regengq && (3 >= incidentL.length));
      let roomd = 8422456 >= privilegeb.length;
      do {
          let analyticR: Array<any> = [String.fromCharCode(101,110,115,95,119,95,53,56,0), String.fromCharCode(114,95,50,53,95,102,105,114,101,0)];
          let selectU = 5.0;
          let middleM: Map<any, any> = new Map([[String.fromCharCode(116,111,112,109,111,115,116,95,114,95,52,48,0),true ], [String.fromCharCode(107,95,55,53,95,114,101,103,117,108,97,116,101,0),false ]]);
         privilegeb.push((strj == String.fromCharCode(54,0) ? strj.length : analyticR.length));
         analyticR = [1];
         selectU += middleM.size;
         middleM.set(`${selectU}`, 3 / (Math.max(6, middleM.size)));
         if (roomd) {
            break;
         }
      } while (((privilegeb.length / (Math.max(3, 2))) < 5) && roomd);
      let fieldQ = 6512191 >= privilegeb.length;
      do {
         privilegeb.push(parseInt(`${feedbackS}`));
         if (fieldQ) {
            break;
         }
      } while (((privilegeb.length / (Math.max(statisticsz.length, 10))) >= 2) && fieldQ);
      while (1 >= (strj.length & 1) && 2 >= (statisticsz.length & 1)) {
         strj += `${strj.length}`;
         break;
      }
      modelsy -= fills.length | 3;
      break;
   }
   if (actions7 != String.fromCharCode(116,0)) {
      groupv += `${groupv.length ^ parseInt(`${favorite0}`)}`;
   }
      groupv += `${3 << (Math.min(4, Math.abs(sansy)))}`;
       let gemfilex = 0;
       let enewsT = String.fromCharCode(122,95,56,48,95,116,114,101,97,100,0);
       let chartq = false;
       let selectionr: Array<any> = [732, 458];
      while (1 >= gemfilex) {
         gemfilex %= Math.max(5, (2 >> (Math.min(2, Math.abs((chartq ? 4 : 2))))));
         break;
      }
         selectionr = [selectionr.length & 3];
          let smallX: Array<any> = [944, 863, 947];
          let matchesS: Map<any, any> = new Map([[String.fromCharCode(118,95,54,56,95,114,97,115,116,101,114,105,122,101,0),285], [String.fromCharCode(105,110,99,114,109,101,114,103,101,95,55,95,56,0),551], [String.fromCharCode(121,95,53,52,95,114,101,99,118,102,114,111,109,0),83]]);
         enewsT += `${enewsT.length}`;
         smallX.push(matchesS.size);
         matchesS = new Map([[`${matchesS.size}`, 1]]);
      if ((selectionr.length % 5) > 3 && 5 > selectionr.length) {
         selectionr = [2];
      }
      for (let h = 0; h < 3; h++) {
         selectionr.push(gemfilex);
      }
       let inviteg = true;
       let mailD = false;
      if (!inviteg && !mailD) {
          let castingh = 1.0;
          let aboutb = String.fromCharCode(97,95,56,95,98,103,114,97,0);
          let with_goo = String.fromCharCode(108,115,112,112,111,108,121,95,100,95,57,0);
          let topicc: Array<any> = [34, 81, 175];
         inviteg = 13 < selectionr.length;
         castingh /= Math.max(4, aboutb.length << (Math.min(Math.abs(3), 4)));
         aboutb = `${(with_goo == String.fromCharCode(82,0) ? topicc.length : with_goo.length)}`;
         topicc.push(aboutb.length << (Math.min(with_goo.length, 3)));
      }
      for (let a = 0; a < 3; a++) {
         selectionr = [(3 | (inviteg ? 2 : 1))];
      }
      fills = "2";
      single7 *= parseFloat(`${2 >> (Math.min(Math.abs(parseInt(`${favorite0}`)), 3))}`);
      statisticsT = !rulesi.includes(`${statisticsT}`);
   let mappingF = 9150631 <= rulesi.length;
   do {
      rulesi = `${parseInt(`${right_}`) | sansy}`;
      if (mappingF) {
         break;
      }
   } while ((rulesi.startsWith(actions7)) && mappingF);
       let feedbacku = String.fromCharCode(97,95,51,50,95,117,110,105,99,111,100,101,0);
       let countdownc = 5.0;
       let sidet = String.fromCharCode(101,114,97,95,103,95,52,0);
         countdownc += feedbacku.length;
      let turnS = feedbacku == String.fromCharCode(56,103,56,99,122,117,57,55,0);
      do {
         feedbacku = `${3 + sidet.length}`;
         if (turnS) {
            break;
         }
      } while ((5 <= (feedbacku.length - 1)) && turnS);
          let tickS = String.fromCharCode(104,95,51,56,95,97,114,114,0);
         countdownc -= sidet.length & tickS.length;
          let awayA = false;
          let policye = 4;
          let confirmationm = String.fromCharCode(99,111,109,112,111,115,101,95,119,95,51,55,0);
         sidet += `${(String.fromCharCode(65,0) == confirmationm ? confirmationm.length : policye)}`;
         awayA = !awayA;
         policye >>= Math.min(parseInt(`${Math.abs(((awayA ? 4 : 3) << (Math.min(4, Math.abs((awayA ? 1 : 4))))))}`), 2);
      for (let q = 0; q < 1; q++) {
         feedbacku = `${(sidet == String.fromCharCode(111,0) ? sidet.length : parseInt(`${countdownc}`))}`;
      }
       let ballR: Array<any> = [499, 29, 838];
       let customy: Array<any> = [881, 398, 998];
      let viewsK = countdownc >= 7267180.0;
      do {
         countdownc /= Math.max((feedbacku == String.fromCharCode(106,0) ? customy.length : feedbacku.length), 3);
         if (viewsK) {
            break;
         }
      } while ((5.6 >= (customy.length * countdownc)) && viewsK);
      while (5 == sidet.length) {
         ballR.push(ballR.length * 1);
         break;
      }
      for (let q = 0; q < 3; q++) {
          let guideB = true;
          let action9: Map<any, any> = new Map([[String.fromCharCode(106,95,56,57,95,98,114,105,100,103,101,0),false ], [String.fromCharCode(105,95,52,57,95,105,115,116,114,101,97,109,0),false ]]);
          let readn = String.fromCharCode(98,95,55,50,95,100,101,99,105,109,97,108,115,0);
          let delegate_o4g = 0;
          let tickedt = true;
         ballR.push(((guideB ? 4 : 5) % (Math.max(feedbacku.length, 6))));
         guideB = !tickedt || 52 <= delegate_o4g;
         action9.set(`${tickedt}`, (3 & (tickedt ? 5 : 1)));
         readn = `${readn.length % (Math.max(3, 7))}`;
         delegate_o4g &= 1;
      }
      groupv = `${actions7.length}`;
   let minimize5 = sansy >= 5319205;
   do {
       let filterl = 1;
       let gemfileX = String.fromCharCode(111,95,53,55,95,117,110,112,111,105,115,111,110,0);
       let weiboD: Map<any, any> = new Map([[String.fromCharCode(120,100,99,97,109,95,118,95,51,56,0),14], [String.fromCharCode(98,117,116,116,111,110,115,95,114,95,52,56,0),749], [String.fromCharCode(119,111,114,107,95,49,95,51,56,0),158]]);
         gemfileX += `${filterl | gemfileX.length}`;
         weiboD.set(gemfileX, gemfileX.length ^ 3);
      while (5 < (weiboD.size / (Math.max(gemfileX.length, 9)))) {
         gemfileX = `${filterl - 2}`;
         break;
      }
         gemfileX += `${filterl}`;
         filterl |= 1;
         gemfileX += `${gemfileX.length}`;
      if ((gemfileX.length ^ filterl) == 2 && (filterl ^ gemfileX.length) == 2) {
         filterl >>= Math.min(Math.abs(weiboD.size), 1);
      }
       let floaterL = 4.0;
      if (5 <= (filterl * gemfileX.length)) {
         gemfileX += `${filterl * 1}`;
      }
      sansy &= (weiboD.size >> (Math.min(2, Math.abs((stringm ? 3 : 5)))));
      if (minimize5) {
         break;
      }
   } while (minimize5 && (single7 <= sansy));
   if (5 >= sansy) {
       let actionsM = false;
       let tailg = 4.0;
       let footballG = 2.0;
         actionsM = !actionsM && tailg > 58.32;
       let combinedE = 0.0;
       let send2 = 4.0;
         send2 *= parseInt(`${combinedE}`);
      for (let s = 0; s < 2; s++) {
         tailg += parseFloat(`${parseInt(`${send2}`)}`);
      }
      for (let d = 0; d < 1; d++) {
          let shrinkP = 1.0;
          let unselectedg = 0;
          let chart3: Map<any, any> = new Map([[String.fromCharCode(100,101,99,111,114,97,116,101,95,103,95,57,49,0),401], [String.fromCharCode(113,95,52,55,95,112,105,110,110,101,114,0),327]]);
          let anythinkw = String.fromCharCode(110,111,110,110,117,108,108,115,115,114,99,95,121,95,56,54,0);
          let tickedH = String.fromCharCode(97,108,105,103,110,105,110,103,95,112,95,49,53,0);
         combinedE += tickedH.length;
         shrinkP -= parseInt(`${shrinkP}`) >> (Math.min(Math.abs(chart3.size), 1));
         unselectedg -= parseInt(`${shrinkP}`);
         chart3.set(`${shrinkP}`, anythinkw.length + 2);
         anythinkw += `${chart3.size}`;
         tickedH += `${parseInt(`${shrinkP}`) | 1}`;
      }
         send2 += parseInt(`${combinedE}`) << (Math.min(1, Math.abs(2)));
          let switch_nC = String.fromCharCode(119,95,53,54,95,99,104,101,99,107,98,111,120,0);
          let untick8: Map<any, any> = new Map([[String.fromCharCode(102,114,97,103,109,101,110,116,95,103,95,55,52,0),140], [String.fromCharCode(103,102,109,117,108,95,51,95,51,56,0),388]]);
         send2 *= 3 / (Math.max(6, parseInt(`${footballG}`)));
         switch_nC = `${switch_nC.length / (Math.max(6, untick8.size))}`;
         untick8.set(switch_nC, 3);
       let main_qP = String.fromCharCode(109,105,112,115,100,115,112,95,114,95,53,53,0);
       let specZ = String.fromCharCode(100,95,56,54,95,105,104,116,120,0);
          let d_lockg = String.fromCharCode(103,95,56,56,95,114,101,102,101,114,114,97,108,0);
          let windN = 1.0;
         combinedE *= specZ.length % (Math.max(3, 9));
         d_lockg = "3";
         windN += parseFloat(`${parseInt(`${windN}`) >> (Math.min(d_lockg.length, 1))}`);
      single7 += parseFloat(`${rulesi.length}`);
   }
       let sharedX: Array<any> = [609, 45];
         sharedX = [sharedX.length];
          let moreM = 5.0;
          let downloadc = String.fromCharCode(97,118,117,116,105,108,114,101,115,95,49,95,57,57,0);
         sharedX = [(String.fromCharCode(89,0) == downloadc ? parseInt(`${moreM}`) : downloadc.length)];
         sharedX = [3];
      sansy >>= Math.min(2, Math.abs(actions7.length | sansy));
      right_ *= (parseFloat(`${parseInt(`${favorite0}`) << (Math.min(4, Math.abs((statisticsT ? 3 : 2))))}`));
   for (let n = 0; n < 2; n++) {
       let reducerd = 1.0;
       let screenn: Array<any> = [630, 986];
       let brightnessC = String.fromCharCode(121,111,117,95,108,95,52,49,0);
       let borderlessW = 3.0;
       let tumbnail3: Array<any> = [String.fromCharCode(112,114,101,99,104,101,99,107,111,117,116,95,106,95,49,55,0), String.fromCharCode(102,95,49,51,95,98,111,111,116,0), String.fromCharCode(112,114,117,110,101,95,102,95,51,55,0)];
          let icond = 0;
          let expandC = String.fromCharCode(101,120,108,117,100,101,100,95,101,95,54,56,0);
         borderlessW += brightnessC.length / 2;
         icond %= Math.max(expandC.length, 5);
         expandC = `${icond - 3}`;
      let submit2 = tumbnail3.length >= 7078938;
      do {
         tumbnail3 = [parseInt(`${reducerd}`)];
         if (submit2) {
            break;
         }
      } while (((tumbnail3.length / 4) >= 1) && submit2);
         brightnessC = `${parseInt(`${reducerd}`) ^ 1}`;
          let countdown8 = 5.0;
         borderlessW += brightnessC.length;
         countdown8 -= parseInt(`${countdown8}`) - parseInt(`${countdown8}`);
      while (2.90 >= (2.18 + reducerd) && (brightnessC.length / 2) >= 4) {
         brightnessC += `${brightnessC.length}`;
         break;
      }
      if (4 > (2 | brightnessC.length) && (reducerd * 1.70) > 2.58) {
         brightnessC = "3";
      }
      if (brightnessC.length <= 5) {
         borderlessW += 3 >> (Math.min(5, tumbnail3.length));
      }
      while (1.10 > borderlessW) {
         borderlessW /= Math.max(3 << (Math.min(2, tumbnail3.length)), 1);
         break;
      }
      for (let u = 0; u < 2; u++) {
          let crossL: Array<any> = [323, 634, 341];
          let gmailk: Array<any> = [545, 711];
          let dialog7 = 4.0;
         brightnessC = "3";
         crossL.push(gmailk.length);
         gmailk.push(crossL.length & 2);
         dialog7 /= Math.max(crossL.length & gmailk.length, 2);
      }
          let gestureu = 4.0;
          let handlerB: Array<any> = [120, 174];
         reducerd += parseFloat(`${parseInt(`${gestureu}`) - 3}`);
         gestureu -= parseFloat(`${handlerB.length & handlerB.length}`);
         borderlessW += (String.fromCharCode(103,0) == brightnessC ? parseInt(`${borderlessW}`) : brightnessC.length);
      let anner3 = 7761429 >= screenn.length;
      do {
         screenn.push(1 * tumbnail3.length);
         if (anner3) {
            break;
         }
      } while (anner3 && ((4 | screenn.length) > 1));
      if (2 <= (5 ^ brightnessC.length) || (reducerd * parseFloat(`${brightnessC.length}`)) <= 4.41) {
         reducerd /= Math.max(parseFloat(`${tumbnail3.length / 1}`), 5);
      }
       let otherA = String.fromCharCode(98,95,52,56,0);
       let matchV = String.fromCharCode(105,110,99,108,117,100,101,95,55,95,51,51,0);
      for (let q = 0; q < 3; q++) {
          let yellowE = 5;
         reducerd /= Math.max(parseFloat(`${yellowE}`), 4);
      }
      rulesi = `${((stringm ? 3 : 2) << (Math.min(tumbnail3.length, 1)))}`;
   }
   let z_titleY = right_ <= 8902287.0;
   do {
      right_ += (parseFloat(`${(stringm ? 3 : 2)}`));
      if (z_titleY) {
         break;
      }
   } while (z_titleY && (single7 > right_));
      rulesi = `${((stringm ? 1 : 5) * parseInt(`${modelsy}`))}`;
      rulesi += `${parseInt(`${single7}`) % (Math.max(groupv.length, 2))}`;
       let basketball7 = 4.0;
       let sendR = 0.0;
         sendR -= parseInt(`${sendR}`) << (Math.min(1, Math.abs(2)));
      for (let e = 0; e < 3; e++) {
          let backf = 0.0;
          let regengq5 = String.fromCharCode(115,95,52,49,95,102,117,122,122,0);
          let resultd: Array<any> = [685, 5];
          let singaporeZ = 4;
          let xvod9 = 0;
         basketball7 /= Math.max(regengq5.length, 5);
         backf += parseInt(`${backf}`) + xvod9;
         regengq5 = "2 | xvod9";
         resultd.push(3 | resultd.length);
         singaporeZ /= Math.max(parseInt(`${backf}`) - xvod9, 2);
      }
      if (4.22 > basketball7) {
         sendR += parseInt(`${basketball7}`);
      }
          let lineN = 2.0;
          let back9: Map<any, any> = new Map([[String.fromCharCode(103,95,56,95,114,101,109,105,120,0),996], [String.fromCharCode(110,95,51,52,0),383]]);
         sendR += back9.size ^ 2;
         lineN += parseInt(`${lineN}`);
         back9 = new Map([[`${lineN}`, parseInt(`${lineN}`)]]);
         sendR += 2 / (Math.max(parseInt(`${basketball7}`), 3));
          let annern = String.fromCharCode(110,95,57,48,95,112,114,101,100,105,99,116,111,114,115,0);
         basketball7 /= Math.max(2, 2);
         annern += "3";
      groupv = `${parseInt(`${right_}`)}`;
   while (sansy == 4) {
      fills = `${(fills == String.fromCharCode(109,0) ? fills.length : parseInt(`${favorite0}`))}`;
      break;
   }

    togglePlayPause();
  };
  return (
    <View style={styles.middleControls}>
      {videoType !== 'live' && (
        <View style={styles.leftButton}>
          <RectButton
            rippleColor="transparent"
            disallowInterruption={true}
            onPress={() => onSkip(-10)}>
            <Rewind width={55} height={55} />
          </RectButton>
        </View>
      )}
      <RectButton
        disallowInterruption={true}
        rippleColor="transparent"
        onPress={() => onTogglePlayPause()}>
        {paused ? (
          <Play width={55} height={55} />
        ) : (
          <Pause width={55} height={55} />
        )}
      </RectButton>
      {videoType !== 'live' && (
        <View style={styles.rightButton}>
          <RectButton
            rippleColor="transparent"
            disallowInterruption={true}
            onPress={() => onSkip(10)}>
            <Fastforward width={55} height={55} />
          </RectButton>
        </View>
      )}
    </View>
  );
};

export default memo(MiddleControls);

const styles = StyleSheet.create({
  middleControls: {
    flexDirection: 'row',
  },
  sideButtons: {
    paddingLeft: 46,
    paddingRight: 46,
  },
  leftButton: {
    alignItems: 'flex-end',
    paddingRight: 23,
  },
  rightButton: {
    alignItems: 'flex-start',
    paddingLeft: 23,
  },
});
