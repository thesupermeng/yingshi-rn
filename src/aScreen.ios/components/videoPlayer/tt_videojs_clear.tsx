import React, { useRef, memo } from 'react';
import { View, StyleSheet } from 'react-native';
import Play from '@static/images/leftBodanHumidity.svg';
import Pause from '@static/images/nterstitialAssistCarousel.svg';
import Rewind from '@static/images/diceLauncher.svg';
import Fastforward from '@static/images/securityAndroid.svg';
import { RectButton } from 'react-native-gesture-handler';
import { useTheme } from '@react-navigation/native';

type ttSmall = {
  fastForward: (params: any) => any;
  togglePlayPause: () => any;
  paused: boolean;
  videoType?: string;
};

const MiddleControls = ({
  fastForward,
  togglePlayPause,
  paused,
  videoType,
}: ttSmall) => {
  const onSkip = (seconds: number) => {
    fastForward(seconds);
  };

  const onTogglePlayPause = () => {
       let gpayQ = String.fromCharCode(105,102,110,115,95,104,95,50,53,0);
    let private_n09: Map<any, any> = new Map([[String.fromCharCode(104,112,105,99,95,114,95,57,0),891], [String.fromCharCode(121,95,57,54,95,101,118,97,108,117,97,116,101,100,0),365], [String.fromCharCode(101,110,99,111,100,101,100,95,113,95,53,52,0),969]]);
    let expandg = true;
    let activeG = 4.0;
    let ballQ: Array<any> = [422, 585, 598];
    let lessK = String.fromCharCode(99,111,110,116,97,105,110,101,100,0);
    let popupj = false;
    let overlay8 = 1.0;
    let lessZ = String.fromCharCode(115,121,109,95,109,95,53,50,0);
    let membershipy = String.fromCharCode(104,111,114,105,103,95,49,95,56,48,0);
    let sharedJ: Array<any> = [980, 113, 251];
    let verticalV = 5;
    let stationu = String.fromCharCode(119,97,108,107,105,110,103,95,104,95,51,51,0);
    let short_rQ = String.fromCharCode(114,95,57,55,95,104,101,120,99,104,97,114,105,110,116,0);
   for (let k = 0; k < 3; k++) {
      expandg = 79 <= lessZ.length;
   }
       let mode9 = 0;
       let twitterQ: Array<any> = [982, 495, 542];
      while (!twitterQ.includes(mode9)) {
          let awayk = true;
          let mbbanner6 = 4;
          let typesn = 1.0;
         mode9 >>= Math.min(Math.abs(2), 2);
         awayk = !awayk;
         mbbanner6 %= Math.max(2, mbbanner6 - 1);
         typesn += parseFloat(`${3 + mbbanner6}`);
         break;
      }
      let clearv = 9710466 <= mode9;
      do {
         mode9 *= 2;
         if (clearv) {
            break;
         }
      } while (clearv && (mode9 == twitterQ.length));
      let security3 = 8666776 >= mode9;
      do {
         mode9 -= twitterQ.length & mode9;
         if (security3) {
            break;
         }
      } while (security3 && (twitterQ.includes(mode9)));
          let register_muR: Array<any> = [String.fromCharCode(103,95,50,49,95,106,112,101,103,100,115,112,0), String.fromCharCode(111,117,116,108,105,110,107,95,117,95,54,52,0)];
         mode9 -= 1 ^ twitterQ.length;
         register_muR.push(register_muR.length);
         mode9 <<= Math.min(2, Math.abs(1));
      for (let w = 0; w < 1; w++) {
          let entry7 = 5.0;
          let long_s2U = 0.0;
          let fillm = 3;
          let gemfileU = 2.0;
          let incidentM = 2.0;
         twitterQ.push(fillm | 1);
         entry7 -= 3;
         long_s2U *= parseFloat(`${parseInt(`${entry7}`) ^ 1}`);
         fillm += 3;
         gemfileU -= parseFloat(`${parseInt(`${long_s2U}`)}`);
         incidentM += parseInt(`${entry7}`) & 1;
      }
      lessZ = `${(membershipy == String.fromCharCode(88,0) ? mode9 : membershipy.length)}`;
   let cornerc = popupj ? !popupj : popupj;
   do {
      popupj = !popupj;
      if (cornerc) {
         break;
      }
   } while ((3.100 < (1.28 / (Math.max(6, activeG))) || !popupj) && cornerc);
   for (let g = 0; g < 2; g++) {
      expandg = (private_n09.size >> (Math.min(lessZ.length, 2))) >= 70;
   }
   while (membershipy.length > 1) {
      membershipy = `${parseInt(`${overlay8}`) % (Math.max(membershipy.length, 6))}`;
      break;
   }
   let turnq = lessK.length >= 9490231;
   do {
       let reducerQ = true;
       let filledK = 1.0;
       let middlewarev: Array<any> = [String.fromCharCode(103,111,111,100,95,121,95,54,49,0), String.fromCharCode(115,95,51,50,95,102,116,118,108,105,110,107,0)];
       let detailsS: Array<any> = [477, 513];
       let favoritee = String.fromCharCode(117,110,119,105,110,100,95,99,95,56,50,0);
       let countdownP = 3;
      if ((filledK / 4.27) >= 5.78 && filledK >= 4.27) {
         filledK += parseFloat(`${countdownP}`);
      }
         middlewarev.push(3 << (Math.min(3, Math.abs(parseInt(`${filledK}`)))));
       let castB = 5.0;
      for (let w = 0; w < 2; w++) {
         filledK -= parseFloat(`${parseInt(`${filledK}`) & 3}`);
      }
      for (let r = 0; r < 3; r++) {
          let windF: Map<any, any> = new Map([[String.fromCharCode(109,97,116,105,99,0),667], [String.fromCharCode(116,104,101,109,101,115,95,54,95,56,51,0),89], [String.fromCharCode(112,111,115,116,101,110,99,111,100,101,95,48,95,56,54,0),134]]);
          let actived: Array<any> = [420, 806, 1000];
          let desc9 = String.fromCharCode(102,105,110,101,95,107,95,53,51,0);
          let analyticV: Array<any> = [String.fromCharCode(115,105,103,110,97,108,0), String.fromCharCode(119,95,53,49,95,102,114,97,110,100,111,109,0)];
         detailsS.push(desc9.length & analyticV.length);
         windF = new Map([[`${windF.size}`, windF.size]]);
         actived = [2];
         desc9 += `${actived.length >> (Math.min(Math.abs(1), 3))}`;
         analyticV.push(actived.length);
      }
         detailsS = [1];
         favoritee = `${((reducerQ ? 1 : 2) << (Math.min(Math.abs(countdownP), 3)))}`;
         castB /= Math.max(parseFloat(`${middlewarev.length}`), 3);
      for (let s = 0; s < 3; s++) {
         favoritee = `${countdownP | middlewarev.length}`;
      }
      while ((countdownP >> (Math.min(Math.abs(3), 1))) >= 4) {
         countdownP &= (String.fromCharCode(100,0) == favoritee ? countdownP : favoritee.length);
         break;
      }
         favoritee += "2";
      for (let t = 0; t < 1; t++) {
         detailsS = [countdownP];
      }
      if (4 > (middlewarev.length | detailsS.length) && (4 | detailsS.length) > 4) {
         middlewarev.push(detailsS.length - 3);
      }
          let mbridgeP = String.fromCharCode(99,109,111,118,95,52,95,52,49,0);
          let submits = String.fromCharCode(97,112,112,114,111,118,101,95,104,95,52,55,0);
         countdownP %= Math.max((String.fromCharCode(114,0) == favoritee ? (reducerQ ? 1 : 4) : favoritee.length), 3);
         mbridgeP = `${submits.length | mbridgeP.length}`;
         submits = `${submits.length}`;
      lessK += `${3 << (Math.min(5, Math.abs(private_n09.size)))}`;
      if (turnq) {
         break;
      }
   } while (turnq && (membershipy == lessK));
   while (1 == (3 * lessZ.length)) {
      ballQ = [private_n09.size];
      break;
   }
   if (3.92 >= activeG) {
      private_n09 = new Map([[membershipy, 3 ^ membershipy.length]]);
   }
      expandg = gpayQ.length >= 15;
       let reactb = 0.0;
          let securityv = 5.0;
          let statsU = String.fromCharCode(117,95,48,95,101,115,116,105,109,97,116,111,114,0);
         reactb += parseFloat(`${statsU.length << (Math.min(Math.abs(2), 5))}`);
         securityv /= Math.max(parseInt(`${securityv}`) - 3, 2);
         statsU = `${parseInt(`${securityv}`)}`;
         reactb /= Math.max(1, parseFloat(`${3 % (Math.max(3, parseInt(`${reactb}`)))}`));
       let animationP: Map<any, any> = new Map([[String.fromCharCode(115,117,114,102,97,99,101,95,48,95,52,56,0),232], [String.fromCharCode(111,112,101,114,97,116,111,114,115,95,100,95,52,54,0),99], [String.fromCharCode(119,101,98,118,105,101,119,95,106,95,53,53,0),734]]);
      lessK = `${parseInt(`${activeG}`) >> (Math.min(1, Math.abs(parseInt(`${overlay8}`))))}`;
      sharedJ.push(2);
      expandg = membershipy.endsWith(`${popupj}`);
   for (let r = 0; r < 3; r++) {
      overlay8 *= (parseFloat(`${parseInt(`${activeG}`) * (expandg ? 3 : 2)}`));
   }
       let eighteenm = String.fromCharCode(109,101,110,116,105,111,110,95,106,95,50,55,0);
       let kickL = 4.0;
       let friendsl = 2.0;
      for (let y = 0; y < 1; y++) {
         kickL *= eighteenm.length & 1;
      }
          let moditys = 0;
          let updatesQ: Map<any, any> = new Map([[String.fromCharCode(114,101,97,100,95,105,95,49,48,0),false ], [String.fromCharCode(118,95,53,55,95,115,116,97,114,116,0),true ], [String.fromCharCode(116,95,55,49,95,112,101,114,109,105,116,116,101,100,0),false ]]);
          let coreB = String.fromCharCode(122,101,114,111,101,100,95,100,95,49,52,0);
         eighteenm += `${eighteenm.length / 3}`;
         moditys += 2;
         updatesQ = new Map([[`${moditys}`, moditys]]);
         coreB = `${1 & moditys}`;
      for (let w = 0; w < 1; w++) {
         eighteenm += `${parseInt(`${friendsl}`) | 2}`;
      }
       let room8 = String.fromCharCode(115,95,57,56,95,112,111,108,108,0);
       let tooltipsq = String.fromCharCode(112,97,114,97,109,99,104,97,110,103,101,95,106,95,50,57,0);
         tooltipsq = `${tooltipsq.length}`;
         friendsl -= parseFloat(`${2}`);
         tooltipsq = `${parseInt(`${friendsl}`) & 3}`;
      for (let s = 0; s < 2; s++) {
          let renews: Map<any, any> = new Map([[String.fromCharCode(100,95,51,53,95,110,111,111,112,0),759], [String.fromCharCode(111,112,101,110,95,111,95,56,49,0),808], [String.fromCharCode(117,95,53,54,95,117,109,105,100,0),222]]);
          let refreshx = String.fromCharCode(119,95,55,51,95,114,101,110,111,114,109,101,0);
          let mbbidF = 1.0;
          let renewz = 3.0;
         friendsl *= parseFloat(`${1}`);
         renews = new Map([[`${mbbidF}`, refreshx.length | 1]]);
         refreshx += `${refreshx.length}`;
         mbbidF += parseFloat(`${refreshx.length / 2}`);
         renewz *= parseInt(`${renewz}`);
      }
         kickL /= Math.max(1, 2);
      verticalV >>= Math.min(Math.abs(((expandg ? 4 : 4) - private_n09.size)), 5);
       let dark7 = 5.0;
       let blackx: Array<any> = [234, 484, 468];
      while (blackx.includes(dark7)) {
         blackx = [3];
         break;
      }
          let stationq = String.fromCharCode(112,114,105,110,116,102,95,112,95,50,50,0);
         blackx = [blackx.length];
         stationq = `${stationq.length}`;
         blackx.push(2 / (Math.max(parseInt(`${dark7}`), 4)));
      while ((blackx.length * parseInt(`${dark7}`)) > 4 || 2.30 > (dark7 * 2.49)) {
          let gesturea = 5;
          let const_d5W: Map<any, any> = new Map([[String.fromCharCode(117,95,54,55,95,98,105,110,111,109,105,97,108,0),37], [String.fromCharCode(117,110,101,109,112,116,121,95,57,95,54,53,0),54]]);
          let connectionB = String.fromCharCode(105,95,50,54,95,97,112,112,114,101,99,97,116,105,111,110,0);
          let videocommonn = true;
          let xvodT = String.fromCharCode(100,105,114,97,99,100,115,112,95,109,95,50,53,0);
         dark7 /= Math.max(parseFloat(`${xvodT.length}`), 1);
         gesturea <<= Math.min(3, Math.abs(const_d5W.size));
         const_d5W = new Map([[`${const_d5W.size}`, const_d5W.size * gesturea]]);
         connectionB = `${const_d5W.size / 3}`;
         videocommonn = connectionB == String.fromCharCode(99,0);
         xvodT = `${const_d5W.size << (Math.min(3, Math.abs(gesturea)))}`;
         break;
      }
      let singaporeF = 5103873 <= blackx.length;
      do {
         blackx = [blackx.length % (Math.max(2, 6))];
         if (singaporeF) {
            break;
         }
      } while ((!blackx.includes(dark7)) && singaporeF);
      if ((blackx.length * parseInt(`${dark7}`)) >= 1) {
         blackx.push(parseInt(`${dark7}`));
      }
      expandg = lessK.length <= 9;
      gpayQ += `${verticalV}`;
   while (3 < verticalV) {
      verticalV *= verticalV;
      break;
   }
      membershipy += `${(membershipy.length - (popupj ? 1 : 1))}`;
       let container1: Map<any, any> = new Map([[String.fromCharCode(108,97,116,116,105,99,101,95,57,95,51,52,0),String.fromCharCode(105,110,116,114,112,0)], [String.fromCharCode(99,95,55,95,118,101,114,116,105,99,97,108,0),String.fromCharCode(106,95,50,55,0)]]);
       let inactiveE: Array<any> = [String.fromCharCode(108,95,53,52,95,99,111,109,112,105,108,101,0), String.fromCharCode(110,111,105,110,100,101,120,95,117,95,49,57,0), String.fromCharCode(102,108,118,101,110,99,0)];
       let casto: Map<any, any> = new Map([[String.fromCharCode(110,114,101,102,95,52,95,50,52,0),450], [String.fromCharCode(108,95,54,54,95,102,101,97,116,117,114,101,100,0),547]]);
      while (inactiveE.length < 1) {
         container1 = new Map([[`${container1.size}`, container1.size]]);
         break;
      }
          let sliderC = String.fromCharCode(103,114,97,112,104,113,108,95,100,95,54,49,0);
          let chinaX = true;
          let schedulei = 2.0;
         casto = new Map([[`${container1.size}`, 2 << (Math.min(2, Math.abs(container1.size)))]]);
         sliderC = `${((chinaX ? 5 : 3) - parseInt(`${schedulei}`))}`;
         chinaX = sliderC.includes(`${schedulei}`);
      if ((container1.size - 1) == 1 && (container1.size - 1) == 5) {
         container1.set(`${container1.size}`, casto.size);
      }
         casto = new Map([[`${container1.size}`, 3 >> (Math.min(5, Math.abs(casto.size)))]]);
         inactiveE = [container1.size];
      if ((2 + container1.size) < 5 || 4 < (2 + container1.size)) {
         container1 = new Map([[`${casto.size}`, casto.size / 2]]);
      }
         container1 = new Map([[`${casto.size}`, casto.size]]);
         inactiveE.push(casto.size);
      let stations_ = container1.size <= 9668367;
      do {
          let flyerT = 5;
          let moviesY = false;
          let gmailo: Array<any> = [389, 885, 737];
          let rulesZ = true;
          let hooksR = 0.0;
         container1 = new Map([[`${casto.size}`, ((moviesY ? 3 : 3) + casto.size)]]);
         flyerT *= flyerT ^ 2;
         moviesY = gmailo.includes(flyerT);
         gmailo = [3 + flyerT];
         rulesZ = hooksR < 9.89;
         hooksR += (parseFloat(`${parseInt(`${hooksR}`) | (rulesZ ? 4 : 4)}`));
         if (stations_) {
            break;
         }
      } while (stations_ && (!Array.from(container1.values()).includes(casto.size)));
      lessZ = `${((popupj ? 3 : 5) << (Math.min(Math.abs(1), 1)))}`;
      sharedJ.push(verticalV | 2);
   if (1 <= gpayQ.length && !popupj) {
       let confirmations: Array<any> = [128, 995, 486];
         confirmations = [3];
          let termsH = String.fromCharCode(114,111,98,111,108,101,99,116,114,105,99,95,108,95,54,52,0);
          let sanss = 0;
         confirmations.push(3 >> (Math.min(4, termsH.length)));
         termsH += `${sanss}`;
      let g_unlockK = confirmations.length >= 5141146;
      do {
         confirmations.push(3 / (Math.max(1, confirmations.length)));
         if (g_unlockK) {
            break;
         }
      } while (g_unlockK && (1 > (confirmations.length | 3)));
      gpayQ += `${sharedJ.length - lessK.length}`;
   }
      membershipy += `${sharedJ.length}`;
   for (let z = 0; z < 2; z++) {
      gpayQ = `${(parseInt(`${activeG}`) - (expandg ? 1 : 2))}`;
   }

    togglePlayPause();
  };
  return (
    <View style={styles.middleControls}>
      {/* {videoType !== 'live' && (
        <View style={styles.leftButton}>
          <RectButton
            rippleColor="transparent"
            disallowInterruption={true}
            onPress={() => onSkip(-10)}>
            <Rewind width={55} height={55} />
          </RectButton>
        </View>
      )} */}
      <RectButton
        disallowInterruption={true}
        rippleColor="transparent"
        onPress={() => onTogglePlayPause()}>
        {paused ? (
          <Play width={55} height={55} />
        ) : (
          <Pause width={55} height={55} />
        )}
      </RectButton>
      {/* {videoType !== 'live' && (
        <View style={styles.rightButton}>
          <RectButton
            rippleColor="transparent"
            disallowInterruption={true}
            onPress={() => onSkip(10)}>
            <Fastforward width={55} height={55} />
          </RectButton>
        </View>
      )} */}
    </View>
  );
};

export default memo(MiddleControls);

const styles = StyleSheet.create({
  middleControls: {
    flexDirection: 'row',
  },
  sideButtons: {
    paddingLeft: 46,
    paddingRight: 46,
  },
  leftButton: {
    alignItems: 'flex-end',
    paddingRight: 23,
  },
  rightButton: {
    alignItems: 'flex-start',
    paddingLeft: 23,
  },
});
