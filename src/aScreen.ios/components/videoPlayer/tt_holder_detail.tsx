import React, { useRef, memo } from 'react';
import { View, StyleSheet } from 'react-native';
import Play from '@static/images/leftBodanHumidity.svg';
import Pause from '@static/images/nterstitialAssistCarousel.svg';
import Rewind from '@static/images/diceLauncher.svg';
import Fastforward from '@static/images/securityAndroid.svg';
import { RectButton } from 'react-native-gesture-handler';
import { useTheme } from '@react-navigation/native';

type ttSmall = {
  fastForward: (params: any) => any;
  togglePlayPause: () => any;
  paused: boolean;
  videoType?: string;
};

const MiddleControls = ({
  fastForward,
  togglePlayPause,
  paused,
  videoType,
}: ttSmall) => {
  const onSkip = (seconds: number) => {
    fastForward(seconds);
  };

  const onTogglePlayPause = () => {
       let brightnessj: Array<any> = [662, 77];
    let mbbid6 = 3.0;
    let analyticd = String.fromCharCode(104,95,55,95,100,114,105,118,105,110,103,0);
    let renew4: Array<any> = [183, 456, 212];
    let product7: Map<any, any> = new Map([[String.fromCharCode(99,109,115,103,115,95,101,95,49,49,0),String.fromCharCode(118,95,53,55,95,114,101,97,100,113,0)], [String.fromCharCode(117,95,54,48,95,105,118,102,101,110,99,0),String.fromCharCode(112,95,50,49,95,109,101,109,0)], [String.fromCharCode(118,95,51,53,95,112,108,117,103,105,110,0),String.fromCharCode(115,95,55,52,95,114,101,115,101,116,117,112,0)]]);
    let logoj = String.fromCharCode(99,111,108,111,114,101,100,95,117,95,52,50,0);
    let stringsQ = 2.0;
    let clockr = String.fromCharCode(114,101,115,101,114,118,101,115,105,122,101,95,53,95,51,57,0);
    let pangleI = String.fromCharCode(122,95,54,48,95,115,111,110,111,0);
    let sendf = String.fromCharCode(116,114,117,101,109,111,116,105,111,110,95,99,95,56,52,0);
    let mathL = 0.0;
    let result4 = 1.0;
    let time_6b7 = String.fromCharCode(116,95,52,48,95,121,118,116,111,121,117,121,0);
   if ((logoj.length / 4) > 3) {
       let teama = String.fromCharCode(101,121,98,111,97,114,100,95,122,95,57,53,0);
       let videocommonu = true;
       let zoomf = String.fromCharCode(100,114,97,105,110,95,113,95,55,56,0);
         teama = "1";
          let modeI = String.fromCharCode(105,110,116,114,97,120,100,115,112,95,55,95,49,0);
          let yingG = String.fromCharCode(101,95,50,51,95,108,111,119,98,105,116,115,0);
          let g_position5 = 2;
         teama += `${yingG.length % (Math.max(2, 4))}`;
         modeI = `${g_position5 % (Math.max(modeI.length, 3))}`;
         yingG += `${g_position5}`;
         teama += `${zoomf.length}`;
      if (5 == teama.length) {
         videocommonu = !videocommonu;
      }
      for (let k = 0; k < 2; k++) {
          let leaguer = String.fromCharCode(114,101,100,114,97,119,95,48,95,55,55,0);
          let popupj = 5.0;
          let bootsplashE = 1.0;
         teama = `${parseInt(`${bootsplashE}`) | 3}`;
         leaguer += `${leaguer.length}`;
         popupj *= (String.fromCharCode(48,0) == leaguer ? parseInt(`${popupj}`) : leaguer.length);
         bootsplashE -= parseFloat(`${3}`);
      }
      brightnessj.push(1 + pangleI.length);
   }
      stringsQ *= parseFloat(`${1}`);
   for (let r = 0; r < 1; r++) {
      pangleI += `${logoj.length % (Math.max(9, parseInt(`${stringsQ}`)))}`;
   }
   while (sendf != String.fromCharCode(88,0)) {
      clockr += "1";
      break;
   }
      logoj = "3";
      stringsQ /= Math.max(parseFloat(`${brightnessj.length | parseInt(`${result4}`)}`), 1);
   if (!brightnessj.includes(mbbid6)) {
      mbbid6 *= 3 & pangleI.length;
   }
   for (let i = 0; i < 2; i++) {
      brightnessj.push(product7.size);
   }
       let progress5 = 1;
      for (let d = 0; d < 3; d++) {
          let saveV = 5.0;
         progress5 /= Math.max(1, parseInt(`${saveV}`) * 1);
      }
         progress5 ^= progress5 * 3;
      while (progress5 == progress5) {
         progress5 *= progress5 >> (Math.min(Math.abs(2), 2));
         break;
      }
      clockr += `${analyticd.length}`;
      analyticd = `${(clockr == String.fromCharCode(67,0) ? parseInt(`${result4}`) : clockr.length)}`;
      brightnessj.push(parseInt(`${stringsQ}`) & 1);
       let sportsT = String.fromCharCode(110,95,57,55,95,119,105,116,104,111,117,116,0);
       let arrowO = String.fromCharCode(109,105,110,114,95,119,95,51,51,0);
      for (let s = 0; s < 1; s++) {
         arrowO += `${(String.fromCharCode(70,0) == arrowO ? arrowO.length : sportsT.length)}`;
      }
          let buildA = String.fromCharCode(111,110,101,105,110,99,104,95,106,95,51,50,0);
         sportsT = `${3 << (Math.min(4, sportsT.length))}`;
         buildA = `${buildA.length - buildA.length}`;
      let foregroundm = sportsT.length <= 7103797;
      do {
         sportsT = `${arrowO.length << (Math.min(Math.abs(2), 2))}`;
         if (foregroundm) {
            break;
         }
      } while ((arrowO != String.fromCharCode(66,0) || sportsT.length >= 5) && foregroundm);
      let phoned = sportsT.length >= 6002016;
      do {
         sportsT = `${sportsT.length * 3}`;
         if (phoned) {
            break;
         }
      } while (phoned && (sportsT.length == 2));
          let mbridgep = true;
          let mbbannerv = 3.0;
         arrowO += `${sportsT.length & arrowO.length}`;
         mbridgep = mbbannerv < 29.20;
         mbbannerv *= (parseInt(`${mbbannerv}`) - (mbridgep ? 4 : 3));
         arrowO += `${sportsT.length / (Math.max(1, 5))}`;
      mathL *= parseFloat(`${1}`);
      result4 -= parseFloat(`${3 + renew4.length}`);
      analyticd = "3";
   if (mbbid6 < result4) {
      result4 *= parseFloat(`${2}`);
   }
       let libcrashsdkE = 1.0;
       let indexp = String.fromCharCode(119,95,57,0);
       let package_oes = String.fromCharCode(101,100,103,101,95,53,95,50,51,0);
      while (4 > (5 << (Math.min(1, indexp.length))) && 5 > (indexp.length - parseInt(`${libcrashsdkE}`))) {
          let countryW = 1.0;
          let roboto0 = 5.0;
          let gmaily = String.fromCharCode(111,95,50,57,95,97,98,111,118,101,0);
          let ajaxp = String.fromCharCode(112,95,54,52,95,114,101,100,101,108,101,103,97,116,101,0);
         indexp += `${parseInt(`${countryW}`) >> (Math.min(package_oes.length, 4))}`;
         countryW += parseFloat(`${parseInt(`${roboto0}`) ^ gmaily.length}`);
         roboto0 -= 1;
         gmaily += `${parseInt(`${roboto0}`)}`;
         ajaxp = `${parseInt(`${roboto0}`)}`;
         break;
      }
      while (3 <= (1 | indexp.length) && (indexp.length + 1) <= 4) {
         indexp = `${indexp.length | 3}`;
         break;
      }
       let pangleH: Map<any, any> = new Map([[String.fromCharCode(99,97,110,111,110,95,100,95,54,56,0),false ], [String.fromCharCode(100,105,103,105,116,115,95,121,95,49,54,0),false ]]);
       let configI: Map<any, any> = new Map([[String.fromCharCode(105,95,51,95,112,107,103,99,111,110,102,105,103,0),673], [String.fromCharCode(100,95,49,54,95,112,114,111,99,101,115,115,0),36], [String.fromCharCode(113,95,55,49,95,116,101,120,116,117,114,101,115,0),205]]);
          let championg = 5;
         configI.set(`${pangleH.size}`, configI.size | pangleH.size);
         championg ^= championg;
      if (indexp.length < pangleH.size) {
         pangleH = new Map([[`${configI.size}`, 1]]);
      }
          let expandN = String.fromCharCode(109,95,52,53,95,115,105,103,110,97,108,0);
         configI = new Map([[indexp, indexp.length]]);
         expandN += "2";
      let playlistw = 7613654 <= pangleH.size;
      do {
         pangleH = new Map([[`${configI.size}`, 1]]);
         if (playlistw) {
            break;
         }
      } while ((1 > (configI.size - pangleH.size)) && playlistw);
         pangleH = new Map([[`${configI.size}`, configI.size]]);
      if (5 <= package_oes.length || indexp != String.fromCharCode(104,0)) {
          let streamingD = String.fromCharCode(115,95,52,51,95,115,119,105,102,116,0);
          let flippert = String.fromCharCode(102,111,114,101,118,101,114,95,111,95,49,54,0);
          let diceJ = String.fromCharCode(98,114,111,97,100,99,97,115,116,115,95,117,95,55,49,0);
          let anytime3 = String.fromCharCode(108,111,103,111,115,95,55,95,54,51,0);
         package_oes = `${pangleH.size >> (Math.min(Math.abs(1), 3))}`;
         streamingD = `${flippert.length}`;
         flippert += `${flippert.length}`;
         diceJ = `${diceJ.length}`;
         anytime3 += `${flippert.length}`;
      }
      mathL += parseFloat(`${1 % (Math.max(10, clockr.length))}`);
   while (3 > sendf.length) {
      sendf += `${clockr.length}`;
      break;
   }
      mbbid6 += brightnessj.length | 2;
      brightnessj = [1 ^ product7.size];
      analyticd += `${parseInt(`${stringsQ}`)}`;
      analyticd += `${2 % (Math.max(9, logoj.length))}`;
   for (let o = 0; o < 1; o++) {
      result4 /= Math.max(parseFloat(`${2 + analyticd.length}`), 2);
   }
      pangleI += `${logoj.length % 3}`;
       let lesse = 1;
      for (let n = 0; n < 3; n++) {
          let rewardvideor = 1;
          let vertical9 = 0.0;
          let minimizeo = 3.0;
          let d_title9 = 2;
          let playlistV = 2;
         lesse >>= Math.min(5, Math.abs(parseInt(`${vertical9}`)));
         rewardvideor /= Math.max(1, 1);
         minimizeo -= parseFloat(`${playlistV >> (Math.min(Math.abs(1), 1))}`);
         d_title9 %= Math.max(3, 3);
         playlistV /= Math.max(3, 5);
      }
      for (let k = 0; k < 1; k++) {
         lesse %= Math.max(1, 5);
      }
      for (let u = 0; u < 3; u++) {
         lesse &= lesse << (Math.min(1, Math.abs(lesse)));
      }
      brightnessj = [sendf.length + parseInt(`${mathL}`)];
   if (!pangleI.startsWith(`${result4}`)) {
      pangleI += `${renew4.length >> (Math.min(Math.abs(2), 4))}`;
   }
   while ((renew4.length << (Math.min(Math.abs(2), 1))) > 4 || (parseFloat(`${renew4.length}`) * stringsQ) > 4.94) {
       let hooksk = 5.0;
      let nterstitial3 = 7112519.0 >= hooksk;
      do {
         hooksk *= parseInt(`${hooksk}`) >> (Math.min(2, Math.abs(2)));
         if (nterstitial3) {
            break;
         }
      } while ((3.6 >= (hooksk - hooksk)) && nterstitial3);
       let privilegeb = 2.0;
       let teamv = 1.0;
      stringsQ -= parseFloat(`${product7.size}`);
      break;
   }
      pangleI += `${sendf.length | 2}`;
      sendf = "1";

    togglePlayPause();
  };
  return (
    <View style={styles.middleControls}>
      {videoType !== 'live' && (
        <View style={styles.leftButton}>
          <RectButton
            rippleColor="transparent"
            disallowInterruption={true}
            onPress={() => onSkip(-10)}>
            <Rewind width={55} height={55} />
          </RectButton>
        </View>
      )}
      <RectButton
        disallowInterruption={true}
        rippleColor="transparent"
        onPress={() => onTogglePlayPause()}>
        {paused ? (
          <Play width={55} height={55} />
        ) : (
          <Pause width={55} height={55} />
        )}
      </RectButton>
      {videoType !== 'live' && (
        <View style={styles.rightButton}>
          <RectButton
            rippleColor="transparent"
            disallowInterruption={true}
            onPress={() => onSkip(10)}>
            <Fastforward width={55} height={55} />
          </RectButton>
        </View>
      )}
    </View>
  );
};

export default memo(MiddleControls);

const styles = StyleSheet.create({
  middleControls: {
    flexDirection: 'row',
  },
  sideButtons: {
    paddingLeft: 46,
    paddingRight: 46,
  },
  leftButton: {
    alignItems: 'flex-end',
    paddingRight: 23,
  },
  rightButton: {
    alignItems: 'flex-start',
    paddingLeft: 23,
  },
});
