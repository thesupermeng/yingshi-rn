import React, { useRef, memo } from 'react';
import { View, StyleSheet } from 'react-native';
import Play from '@static/images/eighteenGpayFootballfiledlayout.svg';
import Pause from '@static/images/dialogSecurityChat.svg';
import Rewind from '@static/images/configUntick.svg';
import Fastforward from '@static/images/robotoWhatsapp.svg';
import { RectButton } from 'react-native-gesture-handler';
import { useTheme } from '@react-navigation/native';

type wwIndexDice = {
  fastForward: (params: any) => any;
  togglePlayPause: () => any;
  paused: boolean;
  videoType?: string;
};

const MiddleControls = ({
  fastForward,
  togglePlayPause,
  paused,
  videoType,
}: wwIndexDice) => {
  const onSkip = (seconds: number) => {
    fastForward(seconds);
  };

  const onTogglePlayPause = () => {
       let styles1 = String.fromCharCode(112,111,108,121,95,102,95,53,49,0);
    let huaweif: Map<any, any> = new Map([[String.fromCharCode(108,95,52,56,95,112,114,101,109,117,108,116,115,116,114,97,105,103,104,116,0),128], [String.fromCharCode(116,114,105,109,109,101,100,95,118,95,51,53,0),175], [String.fromCharCode(99,104,97,112,116,101,114,95,103,95,57,50,0),709]]);
    let libyogah = String.fromCharCode(120,95,53,49,95,100,105,114,101,99,116,108,121,0);
    let dropdownP = 1.0;
    let middleL: Array<any> = [783, 504];
    let calendari = 5.0;
    let stepG = String.fromCharCode(100,111,116,108,111,99,107,95,107,95,54,55,0);
    let storec = String.fromCharCode(106,95,57,48,0);
    let emptyL: Map<any, any> = new Map([[String.fromCharCode(113,95,53,48,95,101,120,105,115,116,97,110,99,101,0),647], [String.fromCharCode(98,95,50,57,95,103,114,101,97,116,101,114,0),633]]);
    let eventp = String.fromCharCode(110,95,54,52,95,101,120,112,111,110,101,110,116,105,97,108,0);
    let dependenciesH = String.fromCharCode(108,97,116,105,110,95,114,95,50,54,0);
       let productc = String.fromCharCode(106,95,57,52,95,119,114,106,112,103,99,111,109,0);
       let libavformate = 4;
       let goalJ = 0;
          let placeholderj: Array<any> = [769, 746, 619];
         libavformate -= 1;
         placeholderj = [placeholderj.length];
      while (!productc.endsWith(`${libavformate}`)) {
          let historyH = 3.0;
          let with_bV = String.fromCharCode(111,114,100,101,114,105,110,103,95,113,95,54,49,0);
          let manifestW = 1.0;
          let listg: Array<any> = [274, 157];
          let fcdaebecbcbafcdfceaaeccfeacdb1: Map<any, any> = new Map([[String.fromCharCode(108,95,56,95,115,101,115,115,111,110,0),String.fromCharCode(122,95,52,50,95,99,108,116,111,115,116,114,0)], [String.fromCharCode(98,95,51,56,95,102,101,97,116,117,114,101,115,0),String.fromCharCode(109,95,52,49,95,111,114,100,101,114,105,110,103,0)], [String.fromCharCode(97,116,114,97,99,97,108,95,113,95,55,50,0),String.fromCharCode(120,95,55,56,95,103,114,97,110,112,111,115,0)]]);
         libavformate /= Math.max(1, 1);
         historyH += parseInt(`${manifestW}`) * 3;
         with_bV += `${(with_bV == String.fromCharCode(113,0) ? fcdaebecbcbafcdfceaaeccfeacdb1.size : with_bV.length)}`;
         manifestW -= fcdaebecbcbafcdfceaaeccfeacdb1.size;
         listg = [fcdaebecbcbafcdfceaaeccfeacdb1.size << (Math.min(Math.abs(2), 2))];
         break;
      }
          let libruntimeexecutor6 = 0.0;
          let sourcez: Map<any, any> = new Map([[String.fromCharCode(101,95,55,95,113,117,101,117,101,100,0),351], [String.fromCharCode(116,95,56,53,95,110,101,99,101,115,115,97,114,121,0),933], [String.fromCharCode(97,100,106,117,115,116,95,116,95,51,51,0),215]]);
          let bootsplash6 = 1.0;
         productc += `${parseInt(`${libruntimeexecutor6}`)}`;
         libruntimeexecutor6 -= parseFloat(`${parseInt(`${bootsplash6}`) + sourcez.size}`);
         sourcez = new Map([[`${sourcez.size}`, sourcez.size ^ parseInt(`${bootsplash6}`)]]);
         productc = `${productc.length << (Math.min(4, Math.abs(goalJ)))}`;
      let runtimescheduler5 = libavformate <= 9843968;
      do {
          let anytimee = true;
          let livenodatabgimgJ = 5.0;
          let downloaderj = false;
         libavformate /= Math.max(1 + goalJ, 1);
         anytimee = livenodatabgimgJ > 63.12 || !anytimee;
         livenodatabgimgJ -= 3;
         downloaderj = !anytimee;
         if (runtimescheduler5) {
            break;
         }
      } while ((5 >= (libavformate | 2) && 1 >= (2 | productc.length)) && runtimescheduler5);
      while (1 <= libavformate) {
         libavformate += libavformate | productc.length;
         break;
      }
      for (let v = 0; v < 2; v++) {
         productc = `${goalJ}`;
      }
         productc = `${libavformate}`;
      for (let n = 0; n < 2; n++) {
         productc += `${libavformate >> (Math.min(Math.abs(2), 2))}`;
      }
      styles1 += `${huaweif.size & 1}`;
   let animationW = 9669027.0 >= dropdownP;
   do {
       let loginb = String.fromCharCode(110,101,119,114,111,119,95,48,95,54,0);
       let showy = String.fromCharCode(100,101,99,111,114,97,116,111,114,115,95,119,95,54,52,0);
      let user5 = 6706685 >= showy.length;
      do {
         showy += "3";
         if (user5) {
            break;
         }
      } while (user5 && (loginb.length >= showy.length));
          let u_centerK = false;
          let gradled = 3.0;
          let configureK = 3.0;
         showy += `${(parseInt(`${gradled}`) ^ (u_centerK ? 3 : 4))}`;
         u_centerK = 80.54 <= configureK && 80.54 <= configureK;
         gradled /= Math.max(2, 2 - parseInt(`${configureK}`));
       let iconbellactiveH = String.fromCharCode(108,105,98,115,119,105,102,116,111,115,95,102,95,56,51,0);
       let libane_ = String.fromCharCode(101,95,55,53,95,97,118,102,111,114,109,97,116,109,97,112,112,101,114,116,101,115,116,115,0);
      while (showy != String.fromCharCode(110,0) && libane_ != String.fromCharCode(111,0)) {
         showy = `${3 * showy.length}`;
         break;
      }
         loginb += `${iconbellactiveH.length}`;
      while (libane_.length <= 3) {
          let debugG = String.fromCharCode(102,95,54,52,95,115,116,114,101,97,109,0);
         libane_ += `${libane_.length}`;
         debugG = `${debugG.length}`;
         break;
      }
      dropdownP -= parseInt(`${dropdownP}`) ^ 1;
      if (animationW) {
         break;
      }
   } while (animationW && (libyogah.length > 2));
      storec += `${3 + middleL.length}`;
       let helperV = 2;
       let entryI: Map<any, any> = new Map([[String.fromCharCode(119,95,57,51,95,103,101,111,107,101,121,0),177], [String.fromCharCode(99,97,114,100,104,111,108,100,101,114,95,114,95,52,54,0),926]]);
       let iconpipexpand0 = 5.0;
      while ((entryI.size + parseInt(`${iconpipexpand0}`)) <= 4 || 1.24 <= (5.98 + iconpipexpand0)) {
         entryI = new Map([[`${entryI.size}`, 2]]);
         break;
      }
      for (let j = 0; j < 2; j++) {
          let routerI = true;
         iconpipexpand0 *= entryI.size;
         routerI = (routerI ? !routerI : routerI);
      }
         helperV *= entryI.size;
         iconpipexpand0 /= Math.max(helperV, 2);
         entryI = new Map([[`${entryI.size}`, 1]]);
      for (let n = 0; n < 3; n++) {
          let subbasketballplayerJ = String.fromCharCode(106,95,51,95,109,115,118,115,0);
          let libavutily = true;
          let mergerI: Array<any> = [914, 463];
          let themeH = String.fromCharCode(117,95,55,51,95,97,115,115,101,116,115,0);
         entryI = new Map([[`${mergerI.length}`, subbasketballplayerJ.length >> (Math.min(Math.abs(3), 2))]]);
         subbasketballplayerJ = `${themeH.length}`;
         libavutily = !libavutily;
         mergerI = [((libavutily ? 5 : 1))];
         themeH += `${(String.fromCharCode(52,0) == themeH ? (libavutily ? 5 : 2) : themeH.length)}`;
      }
         iconpipexpand0 *= parseInt(`${iconpipexpand0}`) & 2;
      while (entryI.get(`${helperV}`) != null) {
          let constantsI = String.fromCharCode(101,110,117,109,95,101,95,49,57,0);
          let iconA = String.fromCharCode(102,97,115,116,115,115,105,109,95,52,95,50,54,0);
          let unreada = 3.0;
          let membershipS = String.fromCharCode(119,95,57,54,95,117,110,116,105,108,0);
          let libtan_ = String.fromCharCode(100,95,55,56,95,99,111,110,102,101,116,116,105,0);
         helperV <<= Math.min(5, Math.abs((String.fromCharCode(84,0) == membershipS ? parseInt(`${unreada}`) : membershipS.length)));
         constantsI += `${libtan_.length}`;
         iconA += `${libtan_.length}`;
         unreada *= constantsI.length << (Math.min(5, iconA.length));
         break;
      }
      let root7 = 7692257.0 >= iconpipexpand0;
      do {
         iconpipexpand0 += helperV ^ entryI.size;
         if (root7) {
            break;
         }
      } while (root7 && ((entryI.size + parseInt(`${iconpipexpand0}`)) <= 3));
      eventp = `${huaweif.size}`;
   let window_pv1 = stepG.length <= 5239525;
   do {
      stepG += "2";
      if (window_pv1) {
         break;
      }
   } while ((storec != stepG) && window_pv1);
       let static_hx = 3;
       let o_hashv = 3.0;
       let suggestion0 = 2.0;
      if (4.60 >= suggestion0) {
         static_hx %= Math.max(parseInt(`${suggestion0}`), 1);
      }
      let templateprocessory = suggestion0 <= 9251482.0;
      do {
         suggestion0 /= Math.max(5, 2 + parseInt(`${o_hashv}`));
         if (templateprocessory) {
            break;
         }
      } while ((4.88 > (2.96 / (Math.max(2, suggestion0))) && 5 > (static_hx * 4)) && templateprocessory);
         suggestion0 += 3;
      for (let f = 0; f < 3; f++) {
          let collectionB: Array<any> = [String.fromCharCode(108,95,54,95,116,97,107,101,111,118,101,114,0), String.fromCharCode(117,95,57,51,95,98,114,97,110,99,104,0)];
          let userj: Array<any> = [778, 99, 817];
         o_hashv *= parseFloat(`${collectionB.length - 1}`);
         collectionB.push(userj.length << (Math.min(Math.abs(1), 3)));
         userj = [userj.length >> (Math.min(1, userj.length))];
      }
          let about8: Map<any, any> = new Map([[String.fromCharCode(99,97,115,116,101,100,95,57,95,50,54,0),904], [String.fromCharCode(115,101,109,105,99,111,108,111,110,95,57,95,50,53,0),549], [String.fromCharCode(109,105,110,109,97,120,95,101,95,57,54,0),740]]);
          let libpangleflipped_ = String.fromCharCode(100,105,115,112,95,51,95,52,54,0);
          let awayteamfieldk = 0.0;
         o_hashv *= parseFloat(`${parseInt(`${suggestion0}`) << (Math.min(1, Math.abs(3)))}`);
         about8.set(`${awayteamfieldk}`, about8.size << (Math.min(Math.abs(2), 2)));
         libpangleflipped_ = `${libpangleflipped_.length ^ parseInt(`${awayteamfieldk}`)}`;
      if (2.57 < (3.91 + o_hashv) || (static_hx * 5) < 2) {
         o_hashv *= parseFloat(`${3 ^ static_hx}`);
      }
      let carouselt = 8304096.0 <= suggestion0;
      do {
         suggestion0 += 2;
         if (carouselt) {
            break;
         }
      } while (carouselt && (1.72 >= (o_hashv * 3.88) && (o_hashv * 3.88) >= 2.95));
         o_hashv -= parseFloat(`${static_hx ^ parseInt(`${o_hashv}`)}`);
         suggestion0 -= parseInt(`${o_hashv}`) - 3;
      emptyL = new Map([[eventp, (stepG == String.fromCharCode(101,0) ? stepG.length : eventp.length)]]);
       let imagenetworkerr7: Array<any> = [String.fromCharCode(107,95,54,50,95,97,114,116,105,102,97,99,116,0), String.fromCharCode(115,101,108,102,105,101,95,48,95,53,56,0)];
         imagenetworkerr7.push(imagenetworkerr7.length / 2);
       let flagO = 5.0;
      let xadsdkW = flagO <= 9355935.0;
      do {
          let readE = 0.0;
          let iconedit7 = 4.0;
          let sheete = String.fromCharCode(121,95,53,52,95,110,97,116,105,111,110,97,108,105,116,121,0);
         flagO += parseFloat(`${parseInt(`${flagO}`) + 2}`);
         readE += 2;
         iconedit7 += parseFloat(`${parseInt(`${iconedit7}`) / (Math.max(3, parseInt(`${readE}`)))}`);
         sheete += `${(sheete == String.fromCharCode(70,0) ? sheete.length : parseInt(`${iconedit7}`))}`;
         if (xadsdkW) {
            break;
         }
      } while (xadsdkW && (!imagenetworkerr7.includes(flagO)));
      calendari += parseFloat(`${emptyL.size}`);
   if (huaweif.size == 5) {
      styles1 = `${storec.length * 1}`;
   }
   for (let m = 0; m < 2; m++) {
       let dropdownD = String.fromCharCode(114,95,55,48,95,100,101,98,108,111,99,107,105,110,103,0);
      for (let i = 0; i < 2; i++) {
          let countryb = true;
         dropdownD += `${((countryb ? 3 : 2))}`;
      }
         dropdownD += `${dropdownD.length >> (Math.min(Math.abs(3), 4))}`;
      if (5 < dropdownD.length) {
         dropdownD += "2";
      }
      huaweif.set(`${storec}`, huaweif.size);
   }
   while (4 < (3 / (Math.max(2, stepG.length))) || (3 + stepG.length) < 2) {
      stepG += `${(eventp == String.fromCharCode(81,0) ? middleL.length : eventp.length)}`;
      break;
   }
   while (storec == libyogah) {
      libyogah = `${parseInt(`${calendari}`) >> (Math.min(Math.abs(1), 2))}`;
      break;
   }
   if (2 >= (5 * styles1.length)) {
      middleL.push(libyogah.length);
   }
   for (let u = 0; u < 2; u++) {
      storec = `${stepG.length >> (Math.min(Math.abs(1), 4))}`;
   }
   if (storec == libyogah) {
      libyogah = `${parseInt(`${dropdownP}`) % 2}`;
   }
   for (let b = 0; b < 1; b++) {
       let fastforwardv = String.fromCharCode(114,105,115,101,95,112,95,56,48,0);
       let tempW = 2.0;
       let privacyX = 1.0;
       let actionst = 1.0;
       let upload6 = 2;
      while ((tempW + 4.17) > 1.77 && 2.61 > (4.17 - tempW)) {
         tempW *= parseInt(`${actionst}`);
         break;
      }
          let thailandR = String.fromCharCode(112,114,101,95,109,95,53,52,0);
         tempW += parseInt(`${privacyX}`);
         thailandR += `${thailandR.length}`;
       let dropdownv = false;
         tempW /= Math.max(4, parseInt(`${privacyX}`) / 2);
      while ((1 | fastforwardv.length) >= 2 && (1 | fastforwardv.length) >= 2) {
         actionst -= parseFloat(`${parseInt(`${tempW}`)}`);
         break;
      }
      for (let n = 0; n < 2; n++) {
          let register_ld = 2;
          let update_c8H = 2;
          let constants7 = String.fromCharCode(119,95,53,51,95,97,117,116,111,100,101,116,101,99,116,0);
          let tooltipsB = String.fromCharCode(105,95,53,48,95,115,101,110,100,109,98,117,102,0);
          let morej = 0.0;
         actionst += parseFloat(`${1 + fastforwardv.length}`);
         register_ld |= parseInt(`${morej}`);
         update_c8H /= Math.max(constants7.length, 4);
         constants7 += `${register_ld}`;
         tooltipsB = `${register_ld}`;
         morej += parseInt(`${morej}`);
      }
      while (3.87 < (3.57 + actionst)) {
         actionst /= Math.max(3, parseFloat(`${parseInt(`${privacyX}`)}`));
         break;
      }
      let roomp = 6561956 <= upload6;
      do {
         upload6 -= parseInt(`${tempW}`) & 3;
         if (roomp) {
            break;
         }
      } while ((3 < (1 ^ upload6) || 2 < (upload6 % (Math.max(1, 9)))) && roomp);
         actionst -= parseFloat(`${upload6 | 3}`);
         tempW -= fastforwardv.length * parseInt(`${privacyX}`);
          let datat = String.fromCharCode(105,95,57,55,95,109,115,101,120,0);
          let sentry8: Map<any, any> = new Map([[String.fromCharCode(107,95,56,52,95,114,101,99,105,112,105,101,110,116,0),554], [String.fromCharCode(100,101,99,108,97,114,101,100,95,116,95,56,53,0),517]]);
          let hook5: Array<any> = [298, 465, 353];
         upload6 %= Math.max(3, 1);
         datat = "1";
         sentry8.set(`${datat}`, 3);
         hook5.push((datat == String.fromCharCode(57,0) ? datat.length : hook5.length));
       let with_eK = String.fromCharCode(108,95,54,95,97,119,97,107,101,0);
         tempW -= (parseInt(`${actionst}`) * (dropdownv ? 1 : 1));
         tempW *= 2;
      for (let s = 0; s < 3; s++) {
          let whiteanimationliveb: Array<any> = [487, 448, 483];
         dropdownv = 11.25 < (privacyX * tempW);
         whiteanimationliveb = [whiteanimationliveb.length];
      }
      libyogah = `${storec.length * libyogah.length}`;
   }
       let shootU: Array<any> = [String.fromCharCode(98,95,55,54,95,102,111,114,105,0), String.fromCharCode(102,105,102,97,95,120,95,50,0), String.fromCharCode(98,95,53,50,95,116,95,56,0)];
       let orangeclockF = String.fromCharCode(98,95,52,56,95,98,97,115,105,99,0);
       let google3 = String.fromCharCode(114,101,99,97,108,99,117,108,97,116,101,95,52,95,50,53,0);
       let canvasN = String.fromCharCode(98,95,50,49,95,98,110,104,101,120,0);
         orangeclockF += `${google3.length & 1}`;
         google3 = "2";
      while (canvasN.includes(`${google3.length}`)) {
         google3 = `${google3.length / (Math.max(2, 2))}`;
         break;
      }
      while (google3.length < 4 || canvasN == String.fromCharCode(102,0)) {
         google3 = `${canvasN.length << (Math.min(Math.abs(1), 3))}`;
         break;
      }
      if (shootU.length <= canvasN.length) {
         shootU = [shootU.length];
      }
          let helperI = String.fromCharCode(102,95,56,54,95,115,116,117,98,98,101,100,0);
          let carousels = String.fromCharCode(99,95,52,48,95,97,115,104,105,110,103,0);
          let diceK = 5.0;
         orangeclockF += `${shootU.length}`;
         helperI += `${parseInt(`${diceK}`) & helperI.length}`;
         carousels += `${1 << (Math.min(2, helperI.length))}`;
         diceK += 2;
          let volumeb = String.fromCharCode(117,110,102,111,114,109,97,116,116,101,100,95,122,95,57,49,0);
         shootU = [canvasN.length & 2];
         volumeb = `${volumeb.length - 3}`;
      if (5 >= (shootU.length ^ google3.length)) {
         google3 += `${canvasN.length}`;
      }
      emptyL.set(eventp, parseInt(`${calendari}`) % (Math.max(eventp.length, 5)));
   let orangez = 6401398 <= styles1.length;
   do {
      styles1 += `${emptyL.size}`;
      if (orangez) {
         break;
      }
   } while ((styles1.includes(`${eventp.length}`)) && orangez);
      dropdownP /= Math.max(stepG.length >> (Math.min(Math.abs(1), 3)), 2);
       let iconnointernetH = String.fromCharCode(117,95,49,50,95,105,115,97,108,110,117,109,0);
       let historyu = String.fromCharCode(115,95,55,50,95,101,100,105,116,97,98,108,101,0);
      if (historyu != String.fromCharCode(112,0)) {
         iconnointernetH = `${3 & iconnointernetH.length}`;
      }
      while (3 <= iconnointernetH.length) {
         historyu = `${(String.fromCharCode(108,0) == iconnointernetH ? historyu.length : iconnointernetH.length)}`;
         break;
      }
         historyu = `${historyu.length >> (Math.min(iconnointernetH.length, 3))}`;
         iconnointernetH += `${2 << (Math.min(2, iconnointernetH.length))}`;
         iconnointernetH = `${historyu.length}`;
         iconnointernetH += `${(String.fromCharCode(106,0) == iconnointernetH ? iconnointernetH.length : historyu.length)}`;
      styles1 += `${storec.length % (Math.max(4, libyogah.length))}`;
      storec += `${parseInt(`${dropdownP}`) / (Math.max(emptyL.size, 2))}`;
   let binddatasb = 7305598 <= libyogah.length;
   do {
      libyogah = "3";
      if (binddatasb) {
         break;
      }
   } while ((5.31 > (4.61 + calendari)) && binddatasb);
   let faviconL = 5407689 <= middleL.length;
   do {
       let halfm = 3.0;
       let benefitH = 4.0;
       let largebrightnessD: Map<any, any> = new Map([[String.fromCharCode(116,95,50,49,95,112,108,117,103,103,101,100,0),200], [String.fromCharCode(99,111,112,121,116,111,95,117,95,49,57,0),86], [String.fromCharCode(101,120,99,104,97,110,103,101,115,95,53,95,55,56,0),338]]);
       let sheetv = false;
       let libreactperfloggerjni9: Map<any, any> = new Map([[String.fromCharCode(97,95,53,57,95,97,118,100,101,118,105,99,101,0),String.fromCharCode(121,95,52,95,115,99,97,108,101,99,117,100,97,0)], [String.fromCharCode(116,95,53,54,95,115,108,111,112,101,0),String.fromCharCode(107,95,49,52,95,102,102,116,103,0)], [String.fromCharCode(109,105,116,101,114,95,53,95,50,52,0),String.fromCharCode(120,108,97,98,101,108,104,101,105,103,104,116,95,117,95,51,51,0)]]);
          let questa: Map<any, any> = new Map([[String.fromCharCode(110,101,116,95,97,95,49,57,0),false ], [String.fromCharCode(115,121,115,99,116,108,95,48,95,49,56,0),true ]]);
          let iconsaveimagel = false;
         benefitH += parseFloat(`${largebrightnessD.size + 1}`);
         questa.set(`${iconsaveimagel}`, (2 - (iconsaveimagel ? 4 : 4)));
      while ((1.88 / (Math.max(9, benefitH))) <= 1.67 || (1.88 / (Math.max(8, benefitH))) <= 4.42) {
         largebrightnessD = new Map([[`${largebrightnessD.size}`, largebrightnessD.size + 3]]);
         break;
      }
       let dragclosei = false;
       let unselectedg = true;
          let scrollviewy = String.fromCharCode(108,105,110,103,101,114,95,48,95,50,55,0);
         dragclosei = benefitH < 39.43;
         scrollviewy += `${scrollviewy.length & 2}`;
      let mathj = sheetv ? !sheetv : sheetv;
      do {
         sheetv = (((!sheetv ? 95 : largebrightnessD.size) + largebrightnessD.size) >= 95);
         if (mathj) {
            break;
         }
      } while ((!sheetv) && mathj);
       let neonr = 3.0;
       let pressureT = 4.0;
          let sound5: Map<any, any> = new Map([[String.fromCharCode(117,109,105,100,95,98,95,56,0),958], [String.fromCharCode(117,95,54,52,95,105,110,116,102,108,111,97,116,0),500], [String.fromCharCode(115,95,56,53,95,98,108,101,101,100,0),922]]);
          let yellowanimationliveU = 2;
         dragclosei = 56.25 == neonr || !sheetv;
         sound5.set(`${yellowanimationliveU}`, sound5.size << (Math.min(3, Math.abs(yellowanimationliveU))));
         benefitH -= parseFloat(`${libreactperfloggerjni9.size}`);
         pressureT /= Math.max(parseInt(`${neonr}`), 5);
          let collectionl: Map<any, any> = new Map([[String.fromCharCode(106,95,53,54,95,108,115,112,108,112,99,0),855], [String.fromCharCode(113,95,51,51,95,109,101,116,97,115,111,117,110,100,0),553]]);
          let basketballR = 4.0;
          let d_positionh: Map<any, any> = new Map([[String.fromCharCode(97,95,53,53,95,116,119,105,116,99,104,0),249], [String.fromCharCode(117,95,53,49,95,104,101,97,114,116,0),517], [String.fromCharCode(108,97,121,111,117,116,95,97,95,55,48,0),600]]);
         benefitH -= parseFloat(`${3 >> (Math.min(Math.abs(parseInt(`${benefitH}`)), 4))}`);
         collectionl.set(`${collectionl.size}`, collectionl.size);
         basketballR += 3;
         d_positionh = new Map([[`${d_positionh.size}`, parseInt(`${basketballR}`) % (Math.max(9, d_positionh.size))]]);
         pressureT -= 3 - parseInt(`${neonr}`);
      middleL = [parseInt(`${dropdownP}`)];
      halfm -= parseInt(`${halfm}`);
      if (faviconL) {
         break;
      }
   } while (faviconL && (middleL.length <= styles1.length));
      storec += `${emptyL.size / (Math.max(middleL.length, 8))}`;
   while ((3 + huaweif.size) > 1 || (stepG.length + 3) > 1) {
       let livenodatabgimgs = 4;
       let eyeopen_: Map<any, any> = new Map([[String.fromCharCode(98,111,111,108,95,57,95,54,52,0),685], [String.fromCharCode(99,97,99,97,95,50,95,54,56,0),250]]);
       let internets = 0.0;
         internets /= Math.max(1, parseFloat(`${parseInt(`${internets}`)}`));
          let imagemanagerd = false;
          let coreP = false;
         eyeopen_.set(`${coreP}`, ((coreP ? 5 : 1) ^ 1));
      while ((internets - 5.2) < 2.34 && (internets - 5.2) < 5.24) {
         internets *= parseFloat(`${parseInt(`${internets}`) - 3}`);
         break;
      }
         livenodatabgimgs *= eyeopen_.size / 3;
          let room3 = String.fromCharCode(108,111,110,103,101,114,95,49,95,52,48,0);
          let predictionP = 2.0;
          let orangeclock7: Array<any> = [585, 566];
         internets /= Math.max(parseFloat(`${parseInt(`${predictionP}`)}`), 2);
         room3 += `${room3.length}`;
         predictionP += room3.length;
         orangeclock7.push(room3.length * orangeclock7.length);
      if (Array.from(eyeopen_.keys()).includes(`${internets}`)) {
          let countdown2 = String.fromCharCode(120,95,52,49,95,102,108,116,112,0);
          let cancelA = false;
          let appsJ = 1.0;
          let unfillf = String.fromCharCode(117,105,111,109,111,118,101,95,119,95,52,52,0);
          let styleA: Map<any, any> = new Map([[String.fromCharCode(115,117,98,116,114,97,99,116,105,111,110,95,102,95,51,56,0),true ], [String.fromCharCode(113,100,101,108,116,97,95,122,95,51,0),false ]]);
         eyeopen_.set(`${eyeopen_.size}`, styleA.size);
         countdown2 = `${((cancelA ? 3 : 5) | parseInt(`${appsJ}`))}`;
         cancelA = !cancelA;
         appsJ -= parseFloat(`${unfillf.length}`);
         unfillf = `${((cancelA ? 4 : 2) - 3)}`;
         styleA.set(countdown2, 1);
      }
          let soundL: Map<any, any> = new Map([[String.fromCharCode(102,95,56,52,95,112,101,114,115,105,115,116,97,98,108,101,0),572], [String.fromCharCode(107,95,51,49,0),443], [String.fromCharCode(122,95,50,48,95,115,121,110,116,104,0),132]]);
          let nexte = String.fromCharCode(101,95,52,50,95,115,111,109,101,0);
         eyeopen_.set(nexte, eyeopen_.size);
         soundL.set(`${soundL.size}`, soundL.size | soundL.size);
         nexte = `${soundL.size % (Math.max(soundL.size, 1))}`;
          let defaultroombgX = 4.0;
          let policyc = 3.0;
          let reactnativeratingsZ: Map<any, any> = new Map([[String.fromCharCode(105,95,56,52,95,100,105,115,112,108,97,121,0),904], [String.fromCharCode(104,95,52,49,95,114,108,111,116,116,105,101,0),560], [String.fromCharCode(115,116,111,112,101,100,95,105,95,55,53,0),743]]);
         livenodatabgimgs /= Math.max(5, 3 << (Math.min(3, Math.abs(eyeopen_.size))));
         defaultroombgX += reactnativeratingsZ.size;
         policyc /= Math.max(2, parseInt(`${defaultroombgX}`));
         reactnativeratingsZ.set(`${policyc}`, 2 | parseInt(`${policyc}`));
         eyeopen_ = new Map([[`${eyeopen_.size}`, eyeopen_.size << (Math.min(Math.abs(1), 1))]]);
      stepG += `${libyogah.length}`;
      break;
   }

    togglePlayPause();
  };
  return (
    <View style={styles.middleControls}>
      {videoType !== 'live' && (
        <View style={styles.leftButton}>
          <RectButton
            rippleColor="transparent"
            disallowInterruption={true}
            onPress={() => onSkip(-10)}>
            <Rewind width={55} height={55} />
          </RectButton>
        </View>
      )}
      <RectButton
        disallowInterruption={true}
        rippleColor="transparent"
        onPress={() => onTogglePlayPause()}>
        {paused ? (
          <Play width={55} height={55} />
        ) : (
          <Pause width={55} height={55} />
        )}
      </RectButton>
      {videoType !== 'live' && (
        <View style={styles.rightButton}>
          <RectButton
            rippleColor="transparent"
            disallowInterruption={true}
            onPress={() => onSkip(10)}>
            <Fastforward width={55} height={55} />
          </RectButton>
        </View>
      )}
    </View>
  );
};

export default memo(MiddleControls);

const styles = StyleSheet.create({
  middleControls: {
    flexDirection: 'row',
  },
  sideButtons: {
    paddingLeft: 46,
    paddingRight: 46,
  },
  leftButton: {
    alignItems: 'flex-end',
    paddingRight: 23,
  },
  rightButton: {
    alignItems: 'flex-start',
    paddingLeft: 23,
  },
});
