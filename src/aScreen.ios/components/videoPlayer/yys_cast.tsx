import React, { useRef, memo } from 'react';
import { View, StyleSheet } from 'react-native';
import { PlaySvg, PauseSvg, RewindSvg, FastforwardSvg, } from '@static';

import { RectButton } from 'react-native-gesture-handler';
import { useTheme } from '@react-navigation/native';

type yysIconarrowrightorangeStation = {
  fastForward: (params: any) => any;
  togglePlayPause: () => any;
  paused: boolean;
  videoType?: string;
};

const MiddleControls = ({
  fastForward,
  togglePlayPause,
  paused,
  videoType,
}: yysIconarrowrightorangeStation) => {
  const onSkip = (seconds: number) => {
    fastForward(seconds);
  };

  const onTogglePlayPause = () => {
       let renderE = String.fromCharCode(103,95,50,53,95,117,112,100,97,116,105,110,103,0);
    let helperh = String.fromCharCode(98,111,110,100,95,56,95,55,56,0);
    let teamV = 5.0;
    let nbatrophy3 = String.fromCharCode(100,95,54,55,95,110,111,110,110,117,108,108,0);
    let cornershootI: Map<any, any> = new Map([[String.fromCharCode(109,95,51,48,95,110,97,110,111,115,101,99,111,110,100,115,0),619], [String.fromCharCode(99,95,56,52,95,115,115,105,109,118,0),394]]);
    let predictiondefaultQ = 4.0;
    let iconviewergifJ = 2.0;
    let condensedt = String.fromCharCode(104,95,53,48,95,97,114,98,105,116,114,97,114,121,0);
    let iconqqy: Map<any, any> = new Map([[String.fromCharCode(107,95,49,55,95,112,101,110,100,105,110,103,115,0),183], [String.fromCharCode(98,97,99,107,114,111,117,110,100,95,99,95,49,50,0),540]]);
    let pressurep = String.fromCharCode(112,95,57,55,95,115,101,116,116,108,105,110,103,0);
    let bootsplashw = String.fromCharCode(111,115,116,114,101,97,109,119,114,97,112,112,101,114,95,112,95,57,53,0);
    let leagueJ: Map<any, any> = new Map([[String.fromCharCode(106,95,54,55,95,119,97,115,116,101,100,0),String.fromCharCode(98,95,51,53,95,98,105,116,101,0)], [String.fromCharCode(100,95,50,56,95,99,109,115,103,0),String.fromCharCode(109,95,57,95,99,108,111,115,101,0)]]);
    let fillede: Map<any, any> = new Map([[String.fromCharCode(117,110,101,110,99,114,121,112,116,101,100,95,99,95,52,0),567], [String.fromCharCode(102,95,52,53,95,105,116,101,114,97,116,101,0),637]]);
      helperh = `${renderE.length ^ iconqqy.size}`;
      iconviewergifJ -= 2 * parseInt(`${iconviewergifJ}`);
      iconqqy.set(`${predictiondefaultQ}`, parseInt(`${predictiondefaultQ}`));
      teamV += 3 & renderE.length;
   let encryptZ = nbatrophy3.length >= 5076373;
   do {
      nbatrophy3 = `${renderE.length}`;
      if (encryptZ) {
         break;
      }
   } while (encryptZ && (nbatrophy3.startsWith(helperh)));
      iconqqy = new Map([[helperh, helperh.length ^ renderE.length]]);
   for (let v = 0; v < 3; v++) {
       let shootnogoal8 = true;
         shootnogoal8 = !shootnogoal8;
          let corez = String.fromCharCode(114,100,111,112,116,95,105,95,51,53,0);
          let shareY = 3.0;
         shootnogoal8 = !shootnogoal8;
         corez = `${corez.length - parseInt(`${shareY}`)}`;
         shareY *= corez.length;
          let login7: Map<any, any> = new Map([[String.fromCharCode(118,105,115,105,116,95,120,95,54,51,0),false ], [String.fromCharCode(110,95,52,50,95,115,116,97,98,105,108,105,122,101,0),false ], [String.fromCharCode(115,117,98,112,97,99,107,101,116,95,101,95,56,54,0),false ]]);
         shootnogoal8 = !shootnogoal8;
         login7.set(`${login7.size}`, login7.size);
      condensedt = "1";
   }
      renderE += "1";
       let libavutila: Array<any> = [String.fromCharCode(102,108,97,103,95,105,95,57,52,0), String.fromCharCode(104,108,105,110,101,95,97,95,54,56,0)];
       let nterstitialC: Array<any> = [555, 370];
          let iconqqz = String.fromCharCode(99,97,116,101,103,111,114,105,115,101,100,95,106,95,56,52,0);
          let vipsportd = 1.0;
          let tempR = String.fromCharCode(102,111,117,110,100,95,98,95,49,51,0);
         libavutila = [iconqqz.length];
         iconqqz = `${1 - parseInt(`${vipsportd}`)}`;
         vipsportd += tempR.length;
         tempR += `${tempR.length | parseInt(`${vipsportd}`)}`;
          let backwardq = 0.0;
         libavutila.push(nterstitialC.length >> (Math.min(Math.abs(3), 1)));
         backwardq /= Math.max(parseFloat(`${parseInt(`${backwardq}`) * parseInt(`${backwardq}`)}`), 3);
       let imagesC = String.fromCharCode(97,95,50,53,95,105,110,116,101,114,112,111,108,97,116,111,114,115,0);
       let othermatchdetailbgP = String.fromCharCode(101,95,50,56,95,116,105,109,101,114,0);
      let modeB = 7182288 >= imagesC.length;
      do {
         imagesC = `${libavutila.length}`;
         if (modeB) {
            break;
         }
      } while (((nterstitialC.length / (Math.max(imagesC.length, 1))) == 3 && (nterstitialC.length / (Math.max(2, imagesC.length))) == 3) && modeB);
         othermatchdetailbgP += "3";
      if ((2 ^ nterstitialC.length) == 4) {
         nterstitialC = [nterstitialC.length];
      }
      helperh += "3";
      pressurep += `${helperh.length}`;
      predictiondefaultQ += 2;
       let orangeclockO = 4.0;
       let bridge1 = 3.0;
       let become8 = 3.0;
          let livew = String.fromCharCode(114,101,112,108,105,101,115,95,52,95,51,50,0);
         become8 *= 1;
         livew += `${(String.fromCharCode(81,0) == livew ? livew.length : livew.length)}`;
      if (become8 >= 5.38) {
         bridge1 -= parseInt(`${become8}`);
      }
      condensedt += `${parseInt(`${predictiondefaultQ}`) ^ 2}`;
      orangeclockO += parseFloat(`${parseInt(`${orangeclockO}`) + 3}`);
      bootsplashw += "3";
      predictiondefaultQ += bootsplashw.length ^ 1;
   if (renderE != pressurep) {
       let embedp = 1.0;
       let activity5 = true;
       let uimanagerJ = true;
          let downL = String.fromCharCode(116,114,97,118,101,108,95,107,95,57,51,0);
         activity5 = embedp < 11.61;
         downL += `${downL.length}`;
      if (uimanagerJ) {
         embedp += (parseFloat(`${(activity5 ? 5 : 3) << (Math.min(Math.abs(parseInt(`${embedp}`)), 1))}`));
      }
         activity5 = !uimanagerJ;
          let whitew = 3;
         uimanagerJ = !uimanagerJ;
         whitew |= whitew >> (Math.min(Math.abs(whitew), 3));
      while ((3.41 - embedp) >= 5.87 || 3.41 >= embedp) {
         activity5 = !uimanagerJ;
         break;
      }
         uimanagerJ = 77.11 > embedp && !activity5;
          let downp = 2.0;
          let headerp = String.fromCharCode(109,101,116,97,100,97,116,97,115,95,119,95,57,56,0);
         activity5 = uimanagerJ;
         downp += parseFloat(`${headerp.length % (Math.max(2, parseInt(`${downp}`)))}`);
         headerp = `${parseInt(`${downp}`) % 1}`;
         activity5 = embedp == 37.26 && !uimanagerJ;
      for (let r = 0; r < 1; r++) {
          let stringh = 1.0;
          let emptyN = 2.0;
          let whatsappQ = 1.0;
          let showH = true;
         uimanagerJ = whatsappQ <= stringh;
         stringh += parseFloat(`${parseInt(`${emptyN}`) | 3}`);
         emptyN /= Math.max(parseFloat(`${3 + parseInt(`${emptyN}`)}`), 1);
         whatsappQ -= (parseInt(`${emptyN}`) + (showH ? 1 : 5));
         showH = 39.92 > emptyN;
      }
      pressurep += `${((activity5 ? 5 : 2) * parseInt(`${embedp}`))}`;
   }
   for (let x = 0; x < 2; x++) {
      bootsplashw = `${pressurep.length | 2}`;
   }
   if (1 > (3 + condensedt.length) || 4 > (3 + condensedt.length)) {
       let emptyu = String.fromCharCode(108,111,115,116,95,110,95,55,56,0);
       let suggestionE = String.fromCharCode(116,95,50,51,95,115,116,114,105,110,103,117,116,105,108,115,0);
      let stepI = 7805509 >= emptyu.length;
      do {
          let friendsQ = String.fromCharCode(103,95,55,57,95,112,101,114,115,105,115,116,101,110,99,101,0);
         emptyu += `${friendsQ.length - suggestionE.length}`;
         if (stepI) {
            break;
         }
      } while ((emptyu.length == 3 && suggestionE.length == 3) && stepI);
      while (suggestionE == String.fromCharCode(88,0) && emptyu == String.fromCharCode(101,0)) {
         suggestionE = `${2 - suggestionE.length}`;
         break;
      }
       let service2: Map<any, any> = new Map([[String.fromCharCode(109,95,49,49,95,99,111,112,121,114,105,103,104,116,0),76], [String.fromCharCode(103,95,54,55,95,110,101,115,116,101,100,0),332], [String.fromCharCode(99,97,99,104,101,102,108,117,115,104,95,120,95,52,52,0),228]]);
       let connectiona: Map<any, any> = new Map([[String.fromCharCode(106,95,51,57,95,108,97,112,110,100,122,0),309], [String.fromCharCode(112,97,121,108,111,97,100,115,95,97,95,50,52,0),411]]);
      if ((1 % (Math.max(1, service2.size))) < 3) {
         service2.set(emptyu, suggestionE.length);
      }
       let utilsH = 2.0;
       let button3 = 4.0;
      for (let g = 0; g < 1; g++) {
          let mbnativeU = String.fromCharCode(115,95,51,57,95,98,108,97,99,107,108,105,115,116,0);
          let yellowf = 3.0;
          let refreshH = true;
         utilsH += parseFloat(`${1}`);
         mbnativeU += `${(parseInt(`${yellowf}`) ^ (refreshH ? 2 : 1))}`;
         yellowf += parseFloat(`${3 - parseInt(`${yellowf}`)}`);
         refreshH = (parseFloat(`${mbnativeU.length}`) - yellowf) < 65.17;
      }
      condensedt = `${emptyu.length}`;
   }
   while ((predictiondefaultQ / (Math.max(bootsplashw.length, 10))) > 5.100 && (bootsplashw.length / 3) > 3) {
      predictiondefaultQ += parseInt(`${iconviewergifJ}`);
      break;
   }
      nbatrophy3 = `${1 & parseInt(`${iconviewergifJ}`)}`;
   if ((parseInt(`${teamV}`) + leagueJ.size) == 3 || (leagueJ.size << (Math.min(Math.abs(3), 4))) == 4) {
      leagueJ.set(helperh, (String.fromCharCode(81,0) == helperh ? helperh.length : fillede.size));
   }
   for (let p = 0; p < 1; p++) {
      bootsplashw += `${parseInt(`${teamV}`)}`;
   }

    togglePlayPause();
  };
  return (
    <View style={styles.middleControls}>
      {videoType !== 'live' && (
        <View style={styles.leftButton}>
          <RectButton
            rippleColor="transparent"
            disallowInterruption={true}
            onPress={() => onSkip(-10)}>
            <RewindSvg width={55} height={55} />
          </RectButton>
        </View>
      )}
      <RectButton
        disallowInterruption={true}
        rippleColor="transparent"
        onPress={() => onTogglePlayPause()}>
        {paused ? (
          <PlaySvg width={55} height={55} />
        ) : (
          <PauseSvg width={55} height={55} />
        )}
      </RectButton>
      {videoType !== 'live' && (
        <View style={styles.rightButton}>
          <RectButton
            rippleColor="transparent"
            disallowInterruption={true}
            onPress={() => onSkip(10)}>
            <FastforwardSvg width={55} height={55} />
          </RectButton>
        </View>
      )}
    </View>
  );
};

export default memo(MiddleControls);

const styles = StyleSheet.create({
  middleControls: {
    flexDirection: 'row',
  },
  sideButtons: {
    paddingLeft: 46,
    paddingRight: 46,
  },
  leftButton: {
    alignItems: 'flex-end',
    paddingRight: 23,
  },
  rightButton: {
    alignItems: 'flex-start',
    paddingLeft: 23,
  },
});
