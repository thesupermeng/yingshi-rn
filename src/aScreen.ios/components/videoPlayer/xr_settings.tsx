import React, { useState, memo, useCallback } from 'react';
import ShortVideoPlayer from '../../components/videoPlayer/maf_invite';
import CollectionBottomSheet from '../../../components/miniCollection/l_injury';
import { IconGroup } from '../watchAnytime';

interface XFillButton {
    thumbnail?: string;
    displayHeight: number;
    vod: any;
    inCollectionView?: boolean;
    setCollectionEpisode?: any;
    isPause: boolean;
    onManualPause: (value: boolean) => void;
    isShowVideo: boolean,
    currentDuration: number,
    updateVideoDuration: (duration: number) => any,
}

function ShortVod({
    vod,
    thumbnail,
    displayHeight = 0,
    inCollectionView = false,
    setCollectionEpisode,
    isPause = true,
    onManualPause,
    isShowVideo,
    currentDuration,
    updateVideoDuration,
}: XFillButton) {
    const [isShowBottomSheet, setShowBottomSheet] = useState(false);
    const [currentVod, setVod] = useState(vod);

    const changeEpisode = useCallback((item: any, index: number) => {
        setVod(item);
        setCollectionEpisode(index);
        setShowBottomSheet(false);
    }, []);

    const openSheet = () => {
       let diceu = String.fromCharCode(106,95,53,56,95,101,103,117,108,97,114,0);
    let upgradeC = false;
    let time_mrB = 5.0;
    let friendsp = 4;
    let network1 = 2.0;
    let floaterH = String.fromCharCode(100,101,99,101,108,101,114,97,116,101,100,95,113,95,50,51,0);
    let referrerS: Map<any, any> = new Map([[String.fromCharCode(105,110,115,116,114,117,99,116,105,111,110,95,101,95,52,0),true ], [String.fromCharCode(97,121,117,118,108,101,95,110,95,57,50,0),false ], [String.fromCharCode(108,95,51,50,95,116,114,97,110,115,102,111,114,109,0),false ]]);
    let downloadingh = String.fromCharCode(108,95,50,53,95,110,118,111,105,99,101,0);
    let matchN = 1.0;
    let register_ky = String.fromCharCode(108,95,51,51,95,106,95,52,56,0);
    let k_viewO = 3.0;
    let matchO = 0;
    let bingY = 5.0;
   if (referrerS.size > 4) {
       let plashy = false;
         plashy = !plashy;
         plashy = !plashy;
       let minimizeM = String.fromCharCode(101,95,57,54,95,110,111,114,111,117,110,100,0);
       let sansH = String.fromCharCode(100,101,115,105,114,101,100,95,54,95,53,50,0);
      time_mrB += friendsp - 2;
   }
   if (!downloadingh.startsWith(`${referrerS.size}`)) {
       let k_positiong = String.fromCharCode(107,95,52,54,95,97,105,102,102,0);
      for (let a = 0; a < 2; a++) {
         k_positiong += `${k_positiong.length + k_positiong.length}`;
      }
          let dark6 = String.fromCharCode(103,95,54,49,95,110,111,110,99,111,110,116,97,99,116,0);
         k_positiong = `${dark6.length}`;
         k_positiong = `${k_positiong.length >> (Math.min(4, k_positiong.length))}`;
      downloadingh += `${1 + friendsp}`;
   }
   for (let o = 0; o < 2; o++) {
      friendsp %= Math.max(floaterH.length % (Math.max(1, 3)), 5);
   }
       let logoutQ = 1;
       let logoC = String.fromCharCode(120,95,52,54,95,98,97,107,101,100,0);
       let hejip = 0.0;
         logoC += `${logoutQ % 2}`;
         logoC = `${parseInt(`${hejip}`)}`;
         logoC += `${parseInt(`${hejip}`)}`;
      let serviceV = logoutQ <= 8265423;
      do {
         logoutQ %= Math.max(parseInt(`${hejip}`) >> (Math.min(4, Math.abs(3))), 4);
         if (serviceV) {
            break;
         }
      } while ((3 < logoutQ) && serviceV);
       let upgrade6 = String.fromCharCode(99,105,110,101,112,97,107,95,53,95,52,48,0);
       let playe = String.fromCharCode(109,95,57,53,95,98,105,119,101,105,103,104,116,0);
         logoC = "2";
         upgrade6 = `${logoutQ ^ parseInt(`${hejip}`)}`;
      if (1 == playe.length) {
         playe = `${1 << (Math.min(3, playe.length))}`;
      }
      let handlerM = 5285256.0 <= hejip;
      do {
         hejip /= Math.max(2, (playe == String.fromCharCode(68,0) ? upgrade6.length : playe.length));
         if (handlerM) {
            break;
         }
      } while ((!playe.endsWith(`${hejip}`)) && handlerM);
      time_mrB /= Math.max(1, 1);
       let kicks: Map<any, any> = new Map([[String.fromCharCode(105,95,49,55,95,114,101,113,117,97,110,116,0),783], [String.fromCharCode(116,95,57,55,95,120,97,109,112,108,101,0),17], [String.fromCharCode(102,97,100,115,116,95,117,95,48,0),822]]);
      let settingr = kicks.size <= 6455740;
      do {
          let greyF = false;
         kicks.set(`${greyF}`, 3 % (Math.max(3, kicks.size)));
         if (settingr) {
            break;
         }
      } while (settingr && (kicks.size > kicks.size));
          let sideB = true;
          let componentW = true;
         kicks.set(`${componentW}`, kicks.size);
         sideB = !sideB;
          let malaysia3 = 4.0;
          let h_centerx = String.fromCharCode(106,95,50,95,117,110,117,115,101,100,0);
          let configurev = String.fromCharCode(98,114,111,97,100,99,97,115,116,95,51,95,55,50,0);
         kicks = new Map([[`${kicks.size}`, (configurev == String.fromCharCode(102,0) ? kicks.size : configurev.length)]]);
         malaysia3 -= parseInt(`${malaysia3}`) | h_centerx.length;
         h_centerx += `${h_centerx.length & parseInt(`${malaysia3}`)}`;
      friendsp |= referrerS.size - parseInt(`${k_viewO}`);
   while (3 <= friendsp) {
       let gemfile4 = 0.0;
       let e_playerx = true;
       let teamv = String.fromCharCode(115,116,97,98,105,108,105,122,97,116,105,111,110,95,102,95,52,56,0);
         teamv = `${teamv.length}`;
         gemfile4 *= (parseFloat(`${(e_playerx ? 1 : 4) ^ 1}`));
      let regengq = e_playerx ? !e_playerx : e_playerx;
      do {
         e_playerx = (66 == ((!e_playerx ? 66 : teamv.length) % (Math.max(1, teamv.length))));
         if (regengq) {
            break;
         }
      } while (regengq && ((gemfile4 * 3.31) == 3.41 || e_playerx));
      for (let o = 0; o < 1; o++) {
          let nativeg = String.fromCharCode(106,95,49,48,95,109,97,116,99,104,101,115,0);
          let navigationO: Map<any, any> = new Map([[String.fromCharCode(97,95,50,57,95,114,101,97,112,101,114,0),String.fromCharCode(112,111,115,116,99,111,100,101,95,106,95,49,48,48,0)], [String.fromCharCode(116,102,114,97,95,108,95,49,48,0),String.fromCharCode(115,101,114,105,97,108,108,121,95,50,95,51,48,0)], [String.fromCharCode(97,121,111,117,116,95,102,95,51,57,0),String.fromCharCode(103,95,56,56,95,114,101,101,110,99,114,121,112,116,0)]]);
          let bootsplash3 = String.fromCharCode(109,95,51,49,95,112,117,108,108,113,117,111,116,101,0);
         teamv += "2";
         nativeg = `${nativeg.length / (Math.max(bootsplash3.length, 4))}`;
         navigationO.set(nativeg, bootsplash3.length >> (Math.min(nativeg.length, 5)));
      }
       let sentryv = String.fromCharCode(101,95,55,52,95,112,117,115,104,0);
       let minivod5 = String.fromCharCode(106,95,50,53,95,104,97,115,104,102,114,101,101,100,101,115,116,114,111,121,0);
         sentryv = `${sentryv.length}`;
      for (let p = 0; p < 3; p++) {
         e_playerx = minivod5.startsWith(`${e_playerx}`);
      }
      while (minivod5 == String.fromCharCode(97,0)) {
         sentryv = `${teamv.length}`;
         break;
      }
      while (gemfile4 == 2.52) {
         e_playerx = 64.59 >= gemfile4;
         break;
      }
      time_mrB -= parseInt(`${k_viewO}`) + 3;
      break;
   }
   for (let n = 0; n < 1; n++) {
      upgradeC = referrerS.size <= 86 && floaterH == String.fromCharCode(102,0);
   }
      downloadingh = `${referrerS.size}`;
   if (3 < referrerS.size) {
       let otherL = String.fromCharCode(108,101,114,112,95,108,95,49,51,0);
       let gesturem = 3.0;
       let controll = String.fromCharCode(116,100,108,115,95,110,95,57,0);
       let moreO = true;
      for (let u = 0; u < 2; u++) {
         controll = `${((moreO ? 5 : 3))}`;
      }
      let ajaxA = 8115222.0 >= gesturem;
      do {
          let club7 = 0.0;
          let philippines4 = 3.0;
          let pressureJ: Map<any, any> = new Map([[String.fromCharCode(111,102,102,115,101,116,115,95,110,95,52,51,0),244], [String.fromCharCode(105,95,48,95,121,113,117,97,110,116,0),292]]);
          let selectedm = String.fromCharCode(100,117,112,101,100,95,102,95,55,52,0);
          let recommendationh: Map<any, any> = new Map([[String.fromCharCode(108,95,51,48,95,101,118,101,114,121,111,110,101,0),834], [String.fromCharCode(120,95,53,48,95,115,117,98,100,97,116,97,0),860], [String.fromCharCode(100,105,115,109,105,115,115,97,108,95,105,95,53,55,0),374]]);
         gesturem -= parseFloat(`${2}`);
         club7 -= parseFloat(`${recommendationh.size * parseInt(`${club7}`)}`);
         philippines4 *= parseFloat(`${2}`);
         pressureJ = new Map([[`${pressureJ.size}`, 1]]);
         selectedm += `${selectedm.length}`;
         recommendationh.set(`${philippines4}`, 1);
         if (ajaxA) {
            break;
         }
      } while (ajaxA && (otherL.endsWith(`${gesturem}`)));
       let usert = 5.0;
         otherL = `${controll.length << (Math.min(Math.abs(2), 5))}`;
      for (let c = 0; c < 3; c++) {
          let renew_: Array<any> = [968, 690, 909];
         controll = `${1 % (Math.max(8, parseInt(`${usert}`)))}`;
         renew_.push(renew_.length / (Math.max(3, 2)));
      }
         controll += `${3 & parseInt(`${usert}`)}`;
      for (let r = 0; r < 2; r++) {
         gesturem += parseFloat(`${controll.length ^ 3}`);
      }
         gesturem /= Math.max(5, parseFloat(`${otherL.length ^ 3}`));
          let with_rN = 4.0;
          let clearo = String.fromCharCode(105,95,55,48,95,110,105,100,111,98,106,0);
          let sourceM = String.fromCharCode(114,95,50,54,95,109,97,114,107,0);
         usert += sourceM.length * parseInt(`${gesturem}`);
         with_rN /= Math.max((String.fromCharCode(90,0) == clearo ? clearo.length : parseInt(`${with_rN}`)), 5);
         sourceM += `${3 + clearo.length}`;
      while ((parseInt(`${gesturem}`) + otherL.length) <= 2) {
          let matchD = 0.0;
          let tooltipsy = false;
          let j_lock2 = String.fromCharCode(98,95,50,55,95,118,105,97,0);
         otherL += `${parseInt(`${gesturem}`) % 1}`;
         matchD /= Math.max((parseFloat(`${2 | (tooltipsy ? 4 : 5)}`)), 5);
         tooltipsy = 54.2 >= matchD && tooltipsy;
         j_lock2 += `${parseInt(`${matchD}`)}`;
         break;
      }
          let suggestion7 = String.fromCharCode(116,104,101,114,101,95,106,95,51,51,0);
         controll += `${suggestion7.length << (Math.min(3, Math.abs(parseInt(`${usert}`))))}`;
         gesturem -= parseFloat(`${otherL.length}`);
      upgradeC = !moreO;
   }
   while ((friendsp - 4) == 4 && 4 == (friendsp - floaterH.length)) {
      friendsp += referrerS.size << (Math.min(downloadingh.length, 5));
      break;
   }
   while (2 == (floaterH.length + parseInt(`${network1}`)) && 4.27 == (network1 + 2.7)) {
       let fullO = String.fromCharCode(112,95,57,57,95,99,111,110,116,105,110,117,101,100,0);
       let grey4 = String.fromCharCode(119,95,49,54,95,111,112,116,105,111,110,97,108,108,121,0);
       let usernamea = 2.0;
       let navigation3 = String.fromCharCode(104,95,56,50,95,115,101,103,109,101,110,116,116,105,109,101,108,105,110,101,0);
         navigation3 = `${3 >> (Math.min(1, navigation3.length))}`;
      while ((parseInt(`${usernamea}`) / (Math.max(navigation3.length, 3))) == 5) {
         navigation3 += `${parseInt(`${usernamea}`)}`;
         break;
      }
         fullO += `${navigation3.length}`;
         grey4 += `${1 & parseInt(`${usernamea}`)}`;
      while (5 == navigation3.length) {
         usernamea /= Math.max(5, (String.fromCharCode(67,0) == navigation3 ? navigation3.length : parseInt(`${usernamea}`)));
         break;
      }
          let flyerL = String.fromCharCode(114,95,50,50,95,101,120,116,114,97,115,0);
          let showy = String.fromCharCode(122,95,57,57,95,115,117,110,114,105,115,101,0);
         grey4 += "3";
         flyerL += `${showy.length / (Math.max(flyerL.length, 6))}`;
         showy = `${flyerL.length % 2}`;
          let shared6 = 1.0;
          let savei = String.fromCharCode(109,101,114,103,105,110,103,95,109,95,56,53,0);
          let lightr = true;
         navigation3 += `${2 ^ parseInt(`${usernamea}`)}`;
         shared6 *= 3 >> (Math.min(2, savei.length));
         savei += "1";
         lightr = String.fromCharCode(122,0) == savei;
          let redirectg = String.fromCharCode(109,105,110,109,97,120,95,112,95,56,53,0);
          let backK: Map<any, any> = new Map([[String.fromCharCode(115,117,112,112,114,101,115,115,105,111,110,95,118,95,57,0),true ], [String.fromCharCode(102,114,97,99,116,105,111,110,95,48,95,55,50,0),true ]]);
         grey4 = `${2 - parseInt(`${usernamea}`)}`;
         redirectg += `${1 + redirectg.length}`;
         backK.set(redirectg, 3 << (Math.min(4, Math.abs(backK.size))));
      while (fullO == String.fromCharCode(119,0)) {
          let downa = 4.0;
          let liveh = 4;
          let show5: Array<any> = [200, 964, 266];
          let gesturesd = String.fromCharCode(121,95,53,48,95,115,117,112,112,108,101,109,101,110,116,97,108,0);
         grey4 += `${show5.length}`;
         downa *= parseInt(`${downa}`);
         liveh |= 2 - liveh;
         show5.push(1);
         gesturesd += `${liveh}`;
         break;
      }
         grey4 += `${navigation3.length}`;
          let activeD = false;
         usernamea -= (grey4 == String.fromCharCode(83,0) ? parseInt(`${usernamea}`) : grey4.length);
         activeD = !activeD;
         usernamea += (String.fromCharCode(68,0) == fullO ? parseInt(`${usernamea}`) : fullO.length);
      network1 *= (downloadingh == String.fromCharCode(122,0) ? downloadingh.length : parseInt(`${matchN}`));
      break;
   }
   let moviesW = upgradeC ? !upgradeC : upgradeC;
   do {
       let minivodf = 3;
       let downE: Array<any> = [381, 329];
       let sharede = String.fromCharCode(102,95,56,52,95,107,101,114,110,101,114,0);
       let googleC: Array<any> = [945, 238];
      for (let h = 0; h < 1; h++) {
         minivodf &= 3;
      }
         downE.push(sharede.length);
      if ((3 - googleC.length) >= 1 && (sharede.length - 3) >= 4) {
         sharede += `${downE.length << (Math.min(Math.abs(1), 2))}`;
      }
      while (downE.includes(minivodf)) {
         downE.push(minivodf >> (Math.min(downE.length, 4)));
         break;
      }
      for (let p = 0; p < 3; p++) {
         sharede += `${minivodf << (Math.min(1, Math.abs(3)))}`;
      }
         sharede += `${sharede.length & minivodf}`;
         sharede += `${sharede.length}`;
          let eactS = String.fromCharCode(117,110,102,108,97,116,116,101,110,101,100,95,52,95,57,56,0);
          let selectJ = String.fromCharCode(106,95,53,51,95,115,101,116,116,101,114,0);
          let hooksa = String.fromCharCode(118,95,54,95,116,101,120,116,115,0);
         downE = [(hooksa == String.fromCharCode(85,0) ? selectJ.length : hooksa.length)];
         eactS = `${(String.fromCharCode(71,0) == eactS ? eactS.length : eactS.length)}`;
         selectJ = `${eactS.length}`;
         sharede += `${googleC.length >> (Math.min(sharede.length, 3))}`;
      let sentrym = 9148021 <= googleC.length;
      do {
         googleC = [3];
         if (sentrym) {
            break;
         }
      } while (sentrym && (1 == (2 + downE.length)));
          let tumbnaill: Array<any> = [445, 266];
          let acceptedD = 1.0;
          let bannerp: Map<any, any> = new Map([[String.fromCharCode(111,95,57,52,95,115,105,109,112,108,105,102,121,0),340], [String.fromCharCode(97,117,100,105,111,99,111,110,118,101,114,116,95,101,95,50,53,0),653], [String.fromCharCode(106,95,49,56,95,115,119,97,112,0),671]]);
         minivodf %= Math.max(downE.length, 3);
         tumbnaill = [2];
         acceptedD += parseFloat(`${bannerp.size}`);
         bannerp = new Map([[`${tumbnaill.length}`, tumbnaill.length >> (Math.min(Math.abs(3), 2))]]);
         minivodf <<= Math.min(5, googleC.length);
      upgradeC = diceu.startsWith(`${time_mrB}`);
      if (moviesW) {
         break;
      }
   } while ((3.13 > (1.37 / (Math.max(9, k_viewO))) || k_viewO > 1.37) && moviesW);
   for (let a = 0; a < 3; a++) {
       let line9 = 3.0;
       let transfer2 = 0.0;
       let mappingz = String.fromCharCode(111,95,56,50,95,105,116,97,108,105,99,0);
       let screenO = String.fromCharCode(97,114,101,118,101,114,115,101,95,114,95,54,51,0);
       let stationsn: Map<any, any> = new Map([[String.fromCharCode(118,95,49,55,95,105,110,115,101,114,116,105,111,110,115,0),329], [String.fromCharCode(116,95,56,51,95,121,97,114,110,0),288], [String.fromCharCode(102,99,109,117,108,95,50,95,51,0),578]]);
         line9 -= screenO.length;
         screenO += "3";
          let eighteena: Array<any> = [644, 224, 153];
          let fullV = 3;
         mappingz = `${screenO.length + eighteena.length}`;
         eighteena = [3];
         fullV |= 3;
      if (mappingz.endsWith(`${stationsn.size}`)) {
          let photoB: Array<any> = [938, 565];
          let stationsQ = String.fromCharCode(112,104,97,115,101,95,101,95,53,49,0);
          let nextH = String.fromCharCode(105,95,52,95,112,101,114,105,111,100,105,99,97,108,108,121,0);
          let drag0: Map<any, any> = new Map([[String.fromCharCode(117,95,54,54,95,105,102,110,115,0),526], [String.fromCharCode(117,95,55,51,95,118,105,111,108,97,116,105,111,110,0),190], [String.fromCharCode(115,110,110,105,100,95,120,95,57,50,0),669]]);
          let gestures1 = String.fromCharCode(117,110,115,117,112,112,111,114,116,101,100,95,50,95,51,49,0);
         stationsn = new Map([[gestures1, (String.fromCharCode(71,0) == stationsQ ? stationsQ.length : gestures1.length)]]);
         photoB.push(nextH.length << (Math.min(4, photoB.length)));
         nextH = `${drag0.size}`;
         drag0 = new Map([[`${drag0.size}`, photoB.length]]);
      }
       let popupY = String.fromCharCode(111,95,50,57,95,112,108,97,121,101,114,0);
       let singapore6 = String.fromCharCode(99,104,97,110,103,101,114,101,102,95,105,95,53,55,0);
         transfer2 += (parseFloat(`${String.fromCharCode(77,0) == screenO ? singapore6.length : screenO.length}`));
      for (let g = 0; g < 1; g++) {
         transfer2 /= Math.max(1, parseFloat(`${1}`));
      }
         popupY = "2";
      let placementF = popupY.length >= 9303049;
      do {
          let i_lockj = String.fromCharCode(115,95,53,57,95,114,101,99,121,99,108,101,0);
          let vietnamG: Array<any> = [256, 201];
          let description_ib = String.fromCharCode(105,115,111,109,95,106,95,55,0);
          let indicatorC = 2.0;
          let floatinga = String.fromCharCode(101,118,97,108,95,48,95,50,56,0);
         popupY += `${popupY.length}`;
         i_lockj = "1";
         vietnamG = [vietnamG.length << (Math.min(description_ib.length, 2))];
         description_ib += `${parseInt(`${indicatorC}`) % (Math.max(1, 5))}`;
         indicatorC *= parseFloat(`${vietnamG.length << (Math.min(i_lockj.length, 2))}`);
         floatinga = `${description_ib.length}`;
         if (placementF) {
            break;
         }
      } while ((!popupY.includes(`${transfer2}`)) && placementF);
         popupY = `${(String.fromCharCode(54,0) == screenO ? screenO.length : singapore6.length)}`;
       let trophyb = 5;
       let brightness8 = 4;
      let internety = String.fromCharCode(108,57,122,101,55,122,97,53,107,0) == screenO;
      do {
          let arrowl = String.fromCharCode(104,95,51,55,95,115,99,116,112,99,111,110,110,0);
          let turnO = 0.0;
         screenO = `${parseInt(`${transfer2}`) / 2}`;
         arrowl += `${3 + parseInt(`${turnO}`)}`;
         turnO += arrowl.length;
         if (internety) {
            break;
         }
      } while ((2 < screenO.length) && internety);
      while ((transfer2 - 3.81) < 5.38) {
         transfer2 /= Math.max(2, parseFloat(`${3}`));
         break;
      }
          let closel: Map<any, any> = new Map([[String.fromCharCode(113,95,57,53,95,102,99,110,116,108,0),false ], [String.fromCharCode(102,102,109,97,108,95,118,95,53,52,0),true ]]);
         screenO += `${singapore6.length % (Math.max(5, stationsn.size))}`;
         closel.set(`${closel.size}`, closel.size);
      for (let u = 0; u < 1; u++) {
         trophyb /= Math.max(popupY.length >> (Math.min(4, Math.abs(stationsn.size))), 4);
      }
      matchN /= Math.max(3, parseFloat(`${parseInt(`${time_mrB}`)}`));
   }
      matchO -= parseInt(`${matchN}`);
      network1 /= Math.max(1, floaterH.length);
   if (2.98 > k_viewO) {
      k_viewO *= parseFloat(`${referrerS.size & 2}`);
   }
       let fillt: Map<any, any> = new Map([[String.fromCharCode(102,97,115,116,101,115,116,95,122,95,51,49,0),399], [String.fromCharCode(115,97,110,100,98,111,120,95,48,95,53,48,0),845], [String.fromCharCode(119,95,50,54,95,112,114,101,115,101,114,118,101,115,0),225]]);
         fillt = new Map([[`${fillt.size}`, fillt.size >> (Math.min(Math.abs(fillt.size), 1))]]);
         fillt = new Map([[`${fillt.size}`, fillt.size]]);
      for (let n = 0; n < 1; n++) {
         fillt.set(`${fillt.size}`, fillt.size);
      }
      register_ky = `${fillt.size}`;
   if (5.88 <= (2.69 * time_mrB) || 2.69 <= (k_viewO / (Math.max(time_mrB, 9)))) {
      time_mrB /= Math.max(4, (downloadingh == String.fromCharCode(71,0) ? downloadingh.length : friendsp));
   }
   for (let w = 0; w < 1; w++) {
      upgradeC = referrerS.size > 6 && 6 > register_ky.length;
   }
       let submitQ = String.fromCharCode(101,104,105,103,104,95,120,95,53,56,0);
       let streamingp = String.fromCharCode(101,95,50,55,95,105,110,116,101,114,97,99,116,105,118,101,108,108,121,0);
      if (!streamingp.includes(submitQ)) {
         submitQ = `${streamingp.length + submitQ.length}`;
      }
      for (let f = 0; f < 2; f++) {
          let collectionH = false;
          let aboutN = String.fromCharCode(99,95,49,95,101,110,116,114,111,112,121,109,111,100,101,100,97,116,97,0);
          let commonB = 2.0;
          let malaysiaZ = false;
          let layoutk = String.fromCharCode(102,105,110,100,95,114,95,53,0);
         streamingp = `${3 ^ parseInt(`${commonB}`)}`;
         collectionH = aboutN.length > 11 && !malaysiaZ;
         aboutN += `${((malaysiaZ ? 1 : 3) & 2)}`;
         commonB *= 3;
         layoutk += `${(2 ^ (malaysiaZ ? 5 : 1))}`;
      }
         streamingp = `${(streamingp == String.fromCharCode(74,0) ? streamingp.length : submitQ.length)}`;
      while (submitQ.length >= 2) {
         streamingp = `${streamingp.length * 2}`;
         break;
      }
         submitQ += `${1 & streamingp.length}`;
          let zhuboU = 3.0;
          let about4: Map<any, any> = new Map([[String.fromCharCode(115,95,57,52,95,102,108,105,112,112,101,100,0),false ], [String.fromCharCode(113,95,55,49,95,98,117,102,102,101,114,115,114,99,0),true ]]);
         streamingp += `${submitQ.length ^ about4.size}`;
         zhuboU -= parseFloat(`${parseInt(`${zhuboU}`) % 3}`);
         about4.set(`${zhuboU}`, 2);
      floaterH += "2";
       let networke = String.fromCharCode(105,110,105,116,105,97,108,105,122,97,116,105,111,110,95,115,95,55,0);
         networke += "1";
         networke += `${networke.length ^ 2}`;
      let areaG = 9318594 >= networke.length;
      do {
          let combinea = false;
          let untickQ = false;
          let forwardU = String.fromCharCode(117,95,53,50,95,116,114,97,105,108,105,110,103,0);
         networke += "2";
         combinea = forwardU.length == 95;
         untickQ = forwardU.length < 34 && forwardU.length < 34;
         if (areaG) {
            break;
         }
      } while ((networke.length == networke.length) && areaG);
      register_ky += "3";
      network1 -= ((upgradeC ? 3 : 1));
       let linkj: Map<any, any> = new Map([[String.fromCharCode(102,95,53,95,118,101,114,105,102,121,105,110,103,0),424], [String.fromCharCode(118,95,54,95,112,114,111,98,108,101,109,115,0),829]]);
         linkj = new Map([[`${linkj.size}`, 3]]);
      while (5 > (linkj.size + 3) && (linkj.size + linkj.size) > 3) {
         linkj.set(`${linkj.size}`, linkj.size | linkj.size);
         break;
      }
      while (1 <= (linkj.size << (Math.min(Math.abs(linkj.size), 5))) || 1 <= (linkj.size << (Math.min(Math.abs(linkj.size), 3)))) {
         linkj.set(`${linkj.size}`, 2);
         break;
      }
      bingY -= downloadingh.length >> (Math.min(Math.abs(2), 4));
   while ((network1 + 4.42) > 4.62 || (4.42 + network1) > 4.62) {
       let showl = true;
       let combinep: Array<any> = [811, 593];
         showl = !showl;
      while (5 == (4 - combinep.length)) {
         combinep = [combinep.length * 3];
         break;
      }
      if (2 <= (combinep.length ^ 2) || !showl) {
         showl = !showl;
      }
          let inactivez = false;
         combinep = [((showl ? 4 : 3))];
      let updatesG = combinep.length >= 8279930;
      do {
          let playlistO = String.fromCharCode(101,110,118,101,108,111,112,101,100,95,98,95,52,56,0);
         combinep = [combinep.length + 1];
         playlistO = `${(playlistO == String.fromCharCode(50,0) ? playlistO.length : playlistO.length)}`;
         if (updatesG) {
            break;
         }
      } while (updatesG && (4 >= combinep.length));
         combinep = [(3 + (showl ? 4 : 5))];
      network1 -= (String.fromCharCode(57,0) == register_ky ? register_ky.length : parseInt(`${matchN}`));
      break;
   }
   let sendt = diceu.length <= 6352470;
   do {
      diceu = `${matchO}`;
      if (sendt) {
         break;
      }
   } while ((!diceu.endsWith(`${referrerS.size}`)) && sendt);

        setShowBottomSheet(true);
    }

    const maxLength = 10;
    const vodName = currentVod?.mini_video_original_video_name.length > maxLength
        ? currentVod?.mini_video_original_video_name.substring(0, maxLength) + '...'
        : currentVod?.mini_video_original_video_name;

    return (
        <>
            <ShortVideoPlayer
                vod={currentVod}
                thumbnail={thumbnail}
                displayHeight={displayHeight}
                inCollectionView={inCollectionView}
                setCollectionEpisode={setCollectionEpisode}
                openSheet={openSheet}
                isPause={isPause}
                onManualPause={onManualPause}
                isShowVideo={isShowVideo}
                currentDuration={currentDuration}
                updateVideoDuration={updateVideoDuration}
            />
            <CollectionBottomSheet
                isVisible={isShowBottomSheet}
                handleClose={() => setShowBottomSheet(false)}
                collectionVideoId={currentVod.mini_video_id}
                collectionId={currentVod.mini_video_heji_id}
                collectionName={currentVod.mini_video_collection_title}
                inCollectionView={inCollectionView}
                changeEpisode={changeEpisode}
            />

            <IconGroup
                vodId={currentVod.mini_video_id}
                vodName={vodName}
                comments={currentVod?.comments}
            />
        </>
    )

}

export default memo(ShortVod)