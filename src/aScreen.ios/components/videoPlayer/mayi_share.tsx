import React, { useRef, memo } from 'react';
import { View, StyleSheet } from 'react-native';
import Play from '@static/images/libreanimatedEwardedDropdown.svg';
import Pause from '@static/images/sportVideojsTramini.svg';
import Rewind from '@static/images/redirectPhone.svg';
import Fastforward from '@static/images/bgvipsportPoint.svg';
import { RectButton } from 'react-native-gesture-handler';
import { useTheme } from '@react-navigation/native';

type mayi_GoogleViews = {
  fastForward: (params: any) => any;
  togglePlayPause: () => any;
  paused: boolean;
  videoType?: string;
};

const MiddleControls = ({
  fastForward,
  togglePlayPause,
  paused,
  videoType,
}: mayi_GoogleViews) => {
  const onSkip = (seconds: number) => {
    fastForward(seconds);
  };

  const onTogglePlayPause = () => {
       let androidE = 0.0;
    let executor9 = 2;
    let stationsQ = String.fromCharCode(116,97,114,103,101,116,115,95,106,95,49,49,0);
    let dycreatorg = String.fromCharCode(100,105,115,116,105,110,99,116,95,116,95,54,54,0);
    let iconsharex = String.fromCharCode(119,104,105,108,101,95,114,95,52,54,0);
    let iconsettingy = false;
    let iconmorex = 2;
    let static_osr = String.fromCharCode(119,95,50,51,95,110,112,117,116,0);
   while (!iconsharex.startsWith(`${iconsettingy}`)) {
       let basketballplayerplaceholdera = 4;
       let long_b7D = String.fromCharCode(108,95,53,51,0);
       let goalG = String.fromCharCode(101,95,52,57,95,105,111,115,117,114,102,97,99,101,0);
       let liblogger4 = 3;
      if (1 == liblogger4) {
         liblogger4 >>= Math.min(Math.abs(liblogger4 / (Math.max(2, goalG.length))), 2);
      }
      for (let y = 0; y < 2; y++) {
         basketballplayerplaceholdera -= 2 / (Math.max(1, long_b7D.length));
      }
      for (let m = 0; m < 1; m++) {
         basketballplayerplaceholdera %= Math.max(long_b7D.length + 3, 3);
      }
         goalG = `${basketballplayerplaceholdera}`;
      let foreground4 = liblogger4 <= 6291952;
      do {
         liblogger4 |= (String.fromCharCode(110,0) == goalG ? liblogger4 : goalG.length);
         if (foreground4) {
            break;
         }
      } while ((3 < (liblogger4 >> (Math.min(long_b7D.length, 1))) || 4 < (long_b7D.length >> (Math.min(Math.abs(3), 2)))) && foreground4);
      while (goalG.endsWith(`${basketballplayerplaceholdera}`)) {
         basketballplayerplaceholdera /= Math.max((String.fromCharCode(73,0) == long_b7D ? liblogger4 : long_b7D.length), 2);
         break;
      }
          let contextC = String.fromCharCode(110,95,49,57,95,115,109,97,108,108,0);
         basketballplayerplaceholdera -= long_b7D.length + 3;
         contextC = `${contextC.length}`;
      while ((liblogger4 + long_b7D.length) <= 2 || 4 <= (2 + long_b7D.length)) {
         liblogger4 -= 2;
         break;
      }
         liblogger4 ^= goalG.length - 3;
      if (!goalG.startsWith(`${long_b7D.length}`)) {
          let iconadslinkS: Array<any> = [816, 559];
          let styleO = 2.0;
          let collections: Map<any, any> = new Map([[String.fromCharCode(103,95,51,54,95,100,105,115,97,98,108,101,100,0),true ], [String.fromCharCode(102,97,108,108,98,97,99,107,95,57,95,56,50,0),false ]]);
         long_b7D += `${(long_b7D == String.fromCharCode(102,0) ? parseInt(`${styleO}`) : long_b7D.length)}`;
         iconadslinkS = [collections.size & 3];
         styleO -= 3 ^ iconadslinkS.length;
         collections.set(`${iconadslinkS.length}`, iconadslinkS.length);
      }
         goalG = `${long_b7D.length}`;
      while ((liblogger4 * 3) == 1) {
         liblogger4 |= (String.fromCharCode(67,0) == goalG ? goalG.length : long_b7D.length);
         break;
      }
      iconsettingy = stationsQ.length < iconsharex.length;
      break;
   }
   while (1 < iconsharex.length && !iconsettingy) {
      iconsettingy = dycreatorg == String.fromCharCode(86,0);
      break;
   }
   for (let e = 0; e < 1; e++) {
      iconsharex += `${stationsQ.length - 1}`;
   }
   let favicont = androidE >= 9646342.0;
   do {
      androidE += (parseInt(`${androidE}`) & (iconsettingy ? 2 : 4));
      if (favicont) {
         break;
      }
   } while (favicont && ((3.82 + androidE) > 3.17));
   while ((executor9 | 1) == 5 || (executor9 | 1) == 1) {
      executor9 &= 3 + parseInt(`${androidE}`);
      break;
   }
   if (stationsQ == dycreatorg) {
       let nodey = String.fromCharCode(97,95,50,52,95,112,108,97,121,0);
       let adultn = String.fromCharCode(97,99,116,111,114,115,95,99,95,52,52,0);
       let iconwatchnowN = 1.0;
      while (adultn.includes(`${iconwatchnowN}`)) {
         adultn = `${(adultn == String.fromCharCode(108,0) ? adultn.length : parseInt(`${iconwatchnowN}`))}`;
         break;
      }
          let condensedv = false;
          let bootH = String.fromCharCode(109,112,101,103,112,105,99,116,117,114,101,95,97,95,52,57,0);
         nodey += `${(bootH == String.fromCharCode(89,0) ? parseInt(`${iconwatchnowN}`) : bootH.length)}`;
         condensedv = !condensedv;
          let mode7 = String.fromCharCode(107,115,101,116,95,119,95,56,50,0);
         adultn = `${1 + adultn.length}`;
         mode7 = `${1 + mode7.length}`;
          let redirectE = 3.0;
          let statsm = 5;
          let progressK = 0.0;
         adultn = `${3 | adultn.length}`;
         redirectE += parseFloat(`${1}`);
         statsm %= Math.max(3, statsm);
         progressK /= Math.max(1, 5);
          let resendV = String.fromCharCode(111,95,53,56,0);
         adultn += `${nodey.length}`;
         resendV = "1";
         adultn += `${(String.fromCharCode(121,0) == adultn ? parseInt(`${iconwatchnowN}`) : adultn.length)}`;
         adultn += `${parseInt(`${iconwatchnowN}`)}`;
      while (!nodey.startsWith(`${iconwatchnowN}`)) {
         iconwatchnowN /= Math.max(4, parseFloat(`${parseInt(`${iconwatchnowN}`) | nodey.length}`));
         break;
      }
         adultn = `${1 + adultn.length}`;
      dycreatorg += `${((iconsettingy ? 4 : 4))}`;
   }
       let clockh = 3.0;
       let awayteamfieldy = false;
       let libavutilQ: Map<any, any> = new Map([[String.fromCharCode(116,95,54,56,95,109,97,103,105,99,121,117,118,0),768], [String.fromCharCode(100,95,53,53,95,100,97,116,101,115,0),595], [String.fromCharCode(104,95,51,54,95,117,110,108,111,99,107,0),44]]);
      for (let w = 0; w < 2; w++) {
         clockh /= Math.max(4, parseFloat(`${2}`));
      }
         awayteamfieldy = null != libavutilQ.get(`${awayteamfieldy}`);
         libavutilQ = new Map([[`${libavutilQ.size}`, (3 * (awayteamfieldy ? 5 : 5))]]);
          let libnmsQ = 3.0;
          let relatedo: Map<any, any> = new Map([[String.fromCharCode(97,95,51,55,95,116,114,97,110,115,105,116,105,111,110,0),617], [String.fromCharCode(100,117,97,108,105,110,112,117,116,95,119,95,55,49,0),957], [String.fromCharCode(100,101,109,117,120,101,114,115,95,122,95,57,54,0),527]]);
         awayteamfieldy = libnmsQ == 46.75;
         libnmsQ += parseFloat(`${relatedo.size - 2}`);
         relatedo.set(`${relatedo.size}`, relatedo.size);
      for (let i = 0; i < 2; i++) {
         libavutilQ.set(`${clockh}`, libavutilQ.size + 1);
      }
       let langR = true;
         libavutilQ.set(`${langR}`, 1);
         clockh -= parseFloat(`${1 | parseInt(`${clockh}`)}`);
       let yellowtorede: Map<any, any> = new Map([[String.fromCharCode(101,113,117,105,118,97,108,101,110,99,101,95,104,95,50,54,0),992], [String.fromCharCode(109,111,98,105,117,115,95,122,95,56,52,0),318], [String.fromCharCode(115,117,98,106,101,99,116,105,118,101,115,95,56,95,57,55,0),247]]);
      iconsharex = `${libavutilQ.size}`;
   while (1 == (dycreatorg.length * executor9)) {
      dycreatorg = `${executor9 << (Math.min(Math.abs(1), 2))}`;
      break;
   }
   for (let w = 0; w < 1; w++) {
       let iconnotificationnewB = 0.0;
       let libjsiM: Map<any, any> = new Map([[String.fromCharCode(122,95,56,52,95,99,104,101,99,107,97,115,109,0),330], [String.fromCharCode(99,95,54,54,95,112,97,103,101,115,101,101,107,0),435], [String.fromCharCode(110,95,52,56,95,99,97,108,99,0),607]]);
         libjsiM.set(`${iconnotificationnewB}`, 3 - parseInt(`${iconnotificationnewB}`));
       let cornershootH = 0.0;
       let soundw = 0.0;
         iconnotificationnewB *= parseInt(`${cornershootH}`) ^ libjsiM.size;
         libjsiM = new Map([[`${cornershootH}`, parseInt(`${soundw}`) % 3]]);
         soundw -= parseInt(`${iconnotificationnewB}`) << (Math.min(2, Math.abs(parseInt(`${soundw}`))));
      for (let j = 0; j < 3; j++) {
         soundw *= libjsiM.size;
      }
      executor9 |= iconsharex.length % (Math.max(9, executor9));
   }
      iconsharex = `${dycreatorg.length + 3}`;
       let placementd = String.fromCharCode(115,112,97,114,115,101,110,101,115,115,95,101,95,51,51,0);
      let libjsijniprofilerC = String.fromCharCode(104,95,121,55,97,97,102,101,51,108,0) == placementd;
      do {
         placementd = `${placementd.length % (Math.max(placementd.length, 9))}`;
         if (libjsijniprofilerC) {
            break;
         }
      } while ((placementd == String.fromCharCode(118,0) && placementd.length >= 3) && libjsijniprofilerC);
         placementd += `${placementd.length + 1}`;
      let window_c60 = placementd == String.fromCharCode(54,104,99,109,54,116,97,97,0);
      do {
         placementd += `${placementd.length}`;
         if (window_c60) {
            break;
         }
      } while (window_c60 && (2 > placementd.length || placementd == String.fromCharCode(48,0)));
      executor9 ^= (String.fromCharCode(110,0) == iconsharex ? iconmorex : iconsharex.length);
      executor9 /= Math.max(5, 1 * dycreatorg.length);
      stationsQ = `${(String.fromCharCode(67,0) == dycreatorg ? dycreatorg.length : (iconsettingy ? 1 : 2))}`;
   let footballg = dycreatorg.length <= 7706920;
   do {
      dycreatorg += `${iconsharex.length / (Math.max(stationsQ.length, 6))}`;
      if (footballg) {
         break;
      }
   } while (footballg && (!dycreatorg.startsWith(`${iconmorex}`)));
      executor9 |= executor9;
       let profileactivez = String.fromCharCode(115,95,56,52,95,106,111,98,0);
       let defaultlogoi = 5;
       let libavcodecc = true;
      while (4 >= (defaultlogoi << (Math.min(Math.abs(2), 3)))) {
         libavcodecc = 43 == defaultlogoi;
         break;
      }
         defaultlogoi ^= profileactivez.length;
         profileactivez = `${profileactivez.length % (Math.max(3, 2))}`;
          let success6: Array<any> = [656, 732];
          let textlayoutmanagers: Map<any, any> = new Map([[String.fromCharCode(102,95,48,95,105,115,105,0),true ], [String.fromCharCode(114,101,117,115,97,98,108,101,95,108,95,51,56,0),true ]]);
         defaultlogoi &= (String.fromCharCode(119,0) == profileactivez ? profileactivez.length : textlayoutmanagers.size);
         success6 = [success6.length * success6.length];
         textlayoutmanagers.set(`${success6.length}`, success6.length | success6.length);
      let reddownarrowV = libavcodecc ? !libavcodecc : libavcodecc;
      do {
          let annero = String.fromCharCode(97,95,55,56,95,100,97,98,97,115,101,0);
          let leakcheckerI = String.fromCharCode(119,95,51,52,95,119,101,101,107,0);
          let completeS = 1.0;
          let owngoalx = 0.0;
         libavcodecc = completeS <= 18.97 || 50 <= annero.length;
         annero = `${parseInt(`${owngoalx}`)}`;
         leakcheckerI = `${parseInt(`${owngoalx}`) ^ 3}`;
         completeS *= parseFloat(`${leakcheckerI.length}`);
         if (reddownarrowV) {
            break;
         }
      } while (reddownarrowV && (libavcodecc));
      while (libavcodecc) {
          let heartr = String.fromCharCode(99,102,115,116,114,101,97,109,95,54,95,51,53,0);
          let basketballplayerplaceholder4 = String.fromCharCode(105,95,54,55,95,121,117,118,0);
          let usert = 5;
          let hooksK = 0.0;
          let traminiD = 2.0;
         libavcodecc = libavcodecc && hooksK <= 76.91;
         heartr += `${1 - heartr.length}`;
         basketballplayerplaceholder4 = `${parseInt(`${traminiD}`)}`;
         usert /= Math.max(3, heartr.length);
         hooksK *= usert;
         traminiD -= (String.fromCharCode(65,0) == heartr ? usert : heartr.length);
         break;
      }
         profileactivez += `${2 | profileactivez.length}`;
      let infoH = profileactivez.length <= 5089494;
      do {
          let basketballtrophy2 = false;
          let hometeamfieldv = 4;
          let photoC = true;
          let iconarrowleftQ = String.fromCharCode(107,95,57,49,95,117,100,116,97,0);
         profileactivez += `${((photoC ? 3 : 1) | 3)}`;
         basketballtrophy2 = 79 < hometeamfieldv;
         hometeamfieldv |= ((basketballtrophy2 ? 1 : 4) | hometeamfieldv);
         photoC = iconarrowleftQ.length < 16 && basketballtrophy2;
         iconarrowleftQ += `${(String.fromCharCode(106,0) == iconarrowleftQ ? hometeamfieldv : iconarrowleftQ.length)}`;
         if (infoH) {
            break;
         }
      } while (infoH && (5 == defaultlogoi));
      if (!libavcodecc) {
          let imagemanagerD = String.fromCharCode(106,95,52,54,95,112,114,101,97,109,98,117,108,97,0);
          let tempg = 0;
          let iconqqL = String.fromCharCode(117,95,53,53,95,114,101,99,101,105,118,101,0);
          let grayl = String.fromCharCode(110,117,109,101,114,105,102,121,95,110,95,52,52,0);
          let layoutm: Array<any> = [801, 124, 532];
         libavcodecc = layoutm.length > 9 || defaultlogoi > 9;
         imagemanagerD = "3";
         tempg /= Math.max(1, 3);
         iconqqL = "3";
         grayl = `${(String.fromCharCode(53,0) == grayl ? tempg : grayl.length)}`;
         layoutm.push(3);
      }
      androidE /= Math.max(parseInt(`${androidE}`), 1);
       let condensedb = String.fromCharCode(115,101,109,105,95,100,95,50,56,0);
       let activeY = 4.0;
       let dycreatorL = String.fromCharCode(119,104,105,116,101,115,95,52,95,57,55,0);
         dycreatorL += `${dycreatorL.length}`;
         activeY *= dycreatorL.length - condensedb.length;
         dycreatorL += `${parseInt(`${activeY}`)}`;
      while ((condensedb.length / 2) <= 4 || (2 & condensedb.length) <= 5) {
          let backiconr = 0.0;
          let predictiondefaultB = String.fromCharCode(97,100,97,112,116,115,95,99,95,54,0);
          let detail1 = true;
          let helperD = true;
          let maily = String.fromCharCode(97,117,116,104,105,110,102,111,95,112,95,53,53,0);
         activeY += ((detail1 ? 5 : 4) / 2);
         backiconr += parseFloat(`${maily.length << (Math.min(1, Math.abs(parseInt(`${backiconr}`))))}`);
         predictiondefaultB = `${1 & parseInt(`${backiconr}`)}`;
         detail1 = predictiondefaultB.length > 23 || maily.length > 23;
         helperD = 23 < predictiondefaultB.length || maily == String.fromCharCode(110,0);
         break;
      }
      if (2 >= condensedb.length) {
          let resendk = false;
          let dycreatorb: Map<any, any> = new Map([[String.fromCharCode(112,97,115,115,95,120,95,51,53,0),96], [String.fromCharCode(102,95,50,57,95,112,114,101,118,0),54]]);
          let libyoga2 = 4.0;
          let paginationU = 3.0;
         condensedb = "1";
         resendk = null == dycreatorb.get(`${resendk}`);
         dycreatorb.set(`${resendk}`, parseInt(`${libyoga2}`) * 2);
         libyoga2 -= parseFloat(`${parseInt(`${paginationU}`)}`);
      }
          let yellowanimationliveZ = true;
          let twitterL: Array<any> = [57, 225];
         activeY *= parseInt(`${activeY}`);
         yellowanimationliveZ = twitterL.includes(yellowanimationliveZ);
         twitterL.push(twitterL.length);
         condensedb += "2";
          let chinav = 3.0;
          let fastforwardh = String.fromCharCode(112,95,56,55,95,115,109,112,116,101,104,100,98,97,114,115,0);
          let productP: Array<any> = [560, 500, 580];
         condensedb += `${parseInt(`${chinav}`)}`;
         chinav *= parseFloat(`${fastforwardh.length}`);
         fastforwardh += `${productP.length & 3}`;
         productP = [3];
      let whiteanimationliveu = dycreatorL.length >= 9630813;
      do {
          let videocommon3: Map<any, any> = new Map([[String.fromCharCode(110,101,99,101,115,115,97,114,121,95,102,95,52,50,0),false ], [String.fromCharCode(103,95,51,57,95,98,111,114,100,101,114,0),true ], [String.fromCharCode(105,110,116,101,108,95,121,95,56,49,0),false ]]);
          let hearto = String.fromCharCode(111,95,54,50,95,115,101,108,101,99,116,0);
          let upgradeh = 1;
         dycreatorL = "2";
         videocommon3.set(hearto, 2 / (Math.max(9, videocommon3.size)));
         hearto = `${(hearto == String.fromCharCode(120,0) ? upgradeh : hearto.length)}`;
         upgradeh += 1 ^ hearto.length;
         if (whiteanimationliveu) {
            break;
         }
      } while ((condensedb.length <= 1) && whiteanimationliveu);
      iconsharex = `${3 | dycreatorL.length}`;

    togglePlayPause();
  };
  return (
    <View style={styles.middleControls}>
      {videoType !== 'live' && (
        <View style={styles.leftButton}>
          <RectButton
            rippleColor="transparent"
            disallowInterruption={true}
            onPress={() => onSkip(-10)}>
            <Rewind width={55} height={55} />
          </RectButton>
        </View>
      )}
      <RectButton
        disallowInterruption={true}
        rippleColor="transparent"
        onPress={() => onTogglePlayPause()}>
        {paused ? (
          <Play width={55} height={55} />
        ) : (
          <Pause width={55} height={55} />
        )}
      </RectButton>
      {videoType !== 'live' && (
        <View style={styles.rightButton}>
          <RectButton
            rippleColor="transparent"
            disallowInterruption={true}
            onPress={() => onSkip(10)}>
            <Fastforward width={55} height={55} />
          </RectButton>
        </View>
      )}
    </View>
  );
};

export default memo(MiddleControls);

const styles = StyleSheet.create({
  middleControls: {
    flexDirection: 'row',
  },
  sideButtons: {
    paddingLeft: 46,
    paddingRight: 46,
  },
  leftButton: {
    alignItems: 'flex-end',
    paddingRight: 23,
  },
  rightButton: {
    alignItems: 'flex-start',
    paddingLeft: 23,
  },
});
