import React, { useRef, memo } from 'react';
import { View, StyleSheet } from 'react-native';
import Play from '@static/images/librrcWhiteanimationlive.svg';
import Pause from '@static/images/refreshStringsLibjsi.svg';
import Rewind from '@static/images/leagueIconpipexpand.svg';
import Fastforward from '@static/images/networkSentry.svg';
import { RectButton } from 'react-native-gesture-handler';
import { useTheme } from '@react-navigation/native';

type wawaAwayShow = {
  fastForward: (params: any) => any;
  togglePlayPause: () => any;
  paused: boolean;
  videoType?: string;
};

const MiddleControls = ({
  fastForward,
  togglePlayPause,
  paused,
  videoType,
}: wawaAwayShow) => {
  const onSkip = (seconds: number) => {
    fastForward(seconds);
  };

  const onTogglePlayPause = () => {
       let tnewsshareX: Array<any> = [342, 880];
    let source8: Map<any, any> = new Map([[String.fromCharCode(122,95,55,56,95,115,101,116,98,105,116,115,0),464], [String.fromCharCode(120,99,108,105,95,99,95,53,55,0),688], [String.fromCharCode(118,95,52,52,95,97,108,108,111,99,97,116,101,0),426]]);
    let inviteE = String.fromCharCode(112,108,97,99,101,95,120,95,54,57,0);
    let splashz: Map<any, any> = new Map([[String.fromCharCode(100,97,105,108,121,95,115,95,52,52,0),false ], [String.fromCharCode(114,101,97,112,101,114,95,119,95,49,52,0),false ], [String.fromCharCode(107,95,51,51,95,105,110,118,105,116,101,114,115,0),true ]]);
    let filedO = String.fromCharCode(115,116,114,101,115,115,95,118,95,51,52,0);
    let iconrightorangeY = String.fromCharCode(116,95,57,55,95,97,118,97,116,97,114,115,0);
    let direct8 = String.fromCharCode(112,114,101,102,101,114,114,101,100,95,50,95,52,0);
    let homeloadingL = String.fromCharCode(115,112,105,110,108,111,99,107,95,118,95,52,55,0);
    let taiwanw = 4;
    let iconsettingJ = 4;
    let baiduadsR: Array<any> = [760, 622, 787];
    let baiduQ = String.fromCharCode(101,120,116,114,97,100,97,116,97,112,115,101,116,115,95,52,95,54,49,0);
    let catagoryT = 4.0;
       let profileC = 3.0;
       let kuaishouY = 2;
       let libglogw: Map<any, any> = new Map([[String.fromCharCode(101,95,56,48,95,115,117,112,112,111,114,116,115,0),845], [String.fromCharCode(101,95,52,50,95,100,101,110,111,114,109,97,108,0),446], [String.fromCharCode(103,95,55,57,95,101,110,99,108,111,115,105,110,103,0),579]]);
          let elementsq = String.fromCharCode(120,95,57,54,95,98,101,104,97,118,105,111,114,0);
          let alertt = String.fromCharCode(99,95,52,51,95,102,105,101,108,100,109,97,116,99,104,0);
         kuaishouY += kuaishouY % (Math.max(1, alertt.length));
         elementsq = `${2 + elementsq.length}`;
         alertt += `${2 & elementsq.length}`;
      for (let a = 0; a < 2; a++) {
          let resendf = String.fromCharCode(97,95,51,52,95,106,112,101,103,116,114,97,110,0);
          let malaysia8: Array<any> = [369, 792];
          let xadsdkx = String.fromCharCode(97,110,100,108,101,95,109,95,49,56,0);
          let promotionD = String.fromCharCode(100,101,97,100,105,110,101,95,52,95,56,57,0);
         kuaishouY &= 3;
         resendf += `${(promotionD == String.fromCharCode(67,0) ? promotionD.length : malaysia8.length)}`;
         malaysia8.push((resendf == String.fromCharCode(80,0) ? resendf.length : xadsdkx.length));
         xadsdkx += `${1 / (Math.max(2, malaysia8.length))}`;
      }
         kuaishouY ^= libglogw.size - kuaishouY;
         kuaishouY &= parseInt(`${profileC}`);
      if ((profileC - kuaishouY) < 5.30) {
          let borderlessf = 2;
         kuaishouY *= 3;
         borderlessf /= Math.max(3, 3);
      }
      if (5 > (libglogw.size << (Math.min(1, Math.abs(kuaishouY)))) || 2 > (libglogw.size << (Math.min(Math.abs(5), 2)))) {
         libglogw.set(`${kuaishouY}`, 2);
      }
      if (5 == (3 * kuaishouY) && (kuaishouY / (Math.max(parseInt(`${profileC}`), 2))) == 3) {
         profileC /= Math.max(3 % (Math.max(8, parseInt(`${profileC}`))), 3);
      }
       let basketballplayerplaceholderB = 3.0;
       let linkI = 4.0;
      let iconclosewhitewithbgV = linkI <= 8236634.0;
      do {
         linkI -= parseFloat(`${2 + libglogw.size}`);
         if (iconclosewhitewithbgV) {
            break;
         }
      } while ((parseFloat(`${libglogw.size}`) == linkI) && iconclosewhitewithbgV);
      splashz = new Map([[`${iconsettingJ}`, 1 << (Math.min(2, Math.abs(taiwanw)))]]);
       let nativemodulek = false;
       let searchbarN: Map<any, any> = new Map([[String.fromCharCode(111,117,116,108,105,101,114,95,113,95,49,48,48,0),String.fromCharCode(101,95,57,95,112,99,109,119,98,0)], [String.fromCharCode(105,95,56,49,95,116,101,108,101,103,114,97,112,104,0),String.fromCharCode(100,121,110,97,109,105,99,115,95,122,95,50,52,0)]]);
       let unreade = String.fromCharCode(118,95,50,51,95,108,111,111,112,105,110,103,0);
         searchbarN.set(`${nativemodulek}`, ((nativemodulek ? 5 : 4) % 3));
         nativemodulek = searchbarN.get(`${nativemodulek}`) == null;
          let league6 = 1.0;
         nativemodulek = null != searchbarN.get(`${league6}`);
      let main_wQ = 5638549 <= searchbarN.size;
      do {
          let footballS = 0.0;
          let drag7 = 5.0;
          let eventsplashy = 5.0;
          let roomy = String.fromCharCode(115,104,111,114,116,99,117,116,95,116,95,50,55,0);
          let bello = String.fromCharCode(105,112,104,111,110,101,95,54,95,54,48,0);
         searchbarN = new Map([[`${searchbarN.size}`, 2]]);
         footballS += parseFloat(`${2}`);
         drag7 -= parseFloat(`${2}`);
         eventsplashy *= (parseFloat(`${roomy == String.fromCharCode(101,0) ? parseInt(`${eventsplashy}`) : roomy.length}`));
         bello += "3";
         if (main_wQ) {
            break;
         }
      } while (main_wQ && (5 <= (searchbarN.size >> (Math.min(Math.abs(2), 3))) && nativemodulek));
      for (let v = 0; v < 1; v++) {
         nativemodulek = unreade == String.fromCharCode(87,0);
      }
      while (!unreade.endsWith(`${searchbarN.size}`)) {
         searchbarN = new Map([[`${searchbarN.size}`, (unreade == String.fromCharCode(108,0) ? searchbarN.size : unreade.length)]]);
         break;
      }
          let componentK = 3.0;
          let service6 = String.fromCharCode(116,114,97,110,115,108,97,116,101,95,119,95,54,49,0);
          let xnews4 = String.fromCharCode(105,110,99,111,110,115,105,115,116,101,110,99,121,95,111,95,55,52,0);
         searchbarN.set(`${nativemodulek}`, ((nativemodulek ? 1 : 1) | 2));
         componentK -= parseFloat(`${service6.length + xnews4.length}`);
         service6 = `${xnews4.length ^ service6.length}`;
      while (searchbarN.size == 2) {
         searchbarN = new Map([[`${nativemodulek}`, 1 - unreade.length]]);
         break;
      }
      while (nativemodulek) {
         nativemodulek = ((searchbarN.size ^ (!nativemodulek ? searchbarN.size : 96)) == 10);
         break;
      }
      direct8 += "1";
   if (2 > (2 - taiwanw) && 2 > (2 - taiwanw)) {
      taiwanw >>= Math.min(1, iconrightorangeY.length);
   }
   if (5 <= (5 & taiwanw)) {
       let footballfiledlayoutH: Map<any, any> = new Map([[String.fromCharCode(100,111,103,115,95,106,95,51,57,0),707], [String.fromCharCode(109,112,101,103,112,115,95,105,95,52,52,0),984], [String.fromCharCode(117,99,104,97,114,95,106,95,57,56,0),410]]);
      if (3 == (footballfiledlayoutH.size * footballfiledlayoutH.size) || 1 == (footballfiledlayoutH.size * 3)) {
         footballfiledlayoutH.set(`${footballfiledlayoutH.size}`, 1);
      }
      while (footballfiledlayoutH.get(`${footballfiledlayoutH.size}`) != null) {
          let defaultfootballbgp = 4.0;
          let product3 = String.fromCharCode(98,95,51,49,95,97,103,101,110,116,0);
          let iconclose9 = true;
         footballfiledlayoutH = new Map([[product3, 2 << (Math.min(2, product3.length))]]);
         defaultfootballbgp -= parseInt(`${defaultfootballbgp}`) / (Math.max(parseInt(`${defaultfootballbgp}`), 6));
         break;
      }
      let arrowselectdowny = footballfiledlayoutH.size <= 5482132;
      do {
         footballfiledlayoutH = new Map([[`${footballfiledlayoutH.size}`, footballfiledlayoutH.size / (Math.max(6, footballfiledlayoutH.size))]]);
         if (arrowselectdowny) {
            break;
         }
      } while ((!Array.from(footballfiledlayoutH.keys()).includes(`${footballfiledlayoutH.size}`)) && arrowselectdowny);
      iconsettingJ *= taiwanw;
   }
       let crossb = String.fromCharCode(113,95,56,50,95,109,101,114,103,105,110,103,0);
         crossb = "2";
      let issub1 = String.fromCharCode(111,121,51,122,106,105,103,0) == crossb;
      do {
         crossb = `${crossb.length}`;
         if (issub1) {
            break;
         }
      } while ((crossb.length == 4) && issub1);
       let dependenciesi: Map<any, any> = new Map([[String.fromCharCode(114,95,53,54,95,114,101,113,117,101,115,116,0),0], [String.fromCharCode(97,95,55,48,95,100,120,116,97,0),524]]);
      iconrightorangeY += "2";
   for (let x = 0; x < 2; x++) {
       let applicationH = String.fromCharCode(112,114,111,118,105,100,101,115,95,54,95,52,0);
       let dependencies4: Map<any, any> = new Map([[String.fromCharCode(104,97,115,104,101,115,95,48,95,51,53,0),383], [String.fromCharCode(103,95,56,52,95,109,111,100,105,102,105,101,100,0),282], [String.fromCharCode(116,111,107,101,110,105,122,101,114,95,119,95,52,49,0),495]]);
       let collection8: Map<any, any> = new Map([[String.fromCharCode(98,95,49,52,95,99,112,112,108,105,110,116,0),String.fromCharCode(120,95,48,95,112,114,111,116,101,99,116,0)], [String.fromCharCode(112,97,100,100,101,100,95,49,95,50,53,0),String.fromCharCode(97,95,54,48,95,116,112,105,100,0)], [String.fromCharCode(112,105,120,100,101,115,99,95,117,95,53,50,0),String.fromCharCode(104,95,52,95,98,121,116,101,99,111,100,101,118,116,97,98,0)]]);
       let short_a5N: Map<any, any> = new Map([[String.fromCharCode(119,105,110,100,105,110,103,95,99,95,57,53,0),5], [String.fromCharCode(98,95,56,49,95,112,97,114,116,0),469], [String.fromCharCode(109,95,56,54,95,113,115,111,114,116,0),726]]);
       let scheduleB = 0;
      while (collection8.size > 3) {
          let libfollyg = 0.0;
          let matchesU = 2.0;
          let modulesA: Map<any, any> = new Map([[String.fromCharCode(119,104,105,99,104,95,117,95,53,51,0),true ], [String.fromCharCode(100,111,119,110,108,111,97,100,97,98,108,101,95,49,95,52,54,0),false ], [String.fromCharCode(115,112,101,99,105,102,105,99,95,120,95,54,53,0),false ]]);
          let whiteanimationlive2 = String.fromCharCode(104,95,51,57,95,110,111,110,114,100,0);
          let roundO = 4.0;
         scheduleB += dependencies4.size;
         libfollyg += parseFloat(`${parseInt(`${roundO}`)}`);
         matchesU += parseFloat(`${2}`);
         modulesA = new Map([[`${roundO}`, parseInt(`${libfollyg}`)]]);
         whiteanimationlive2 += "2";
         break;
      }
         short_a5N = new Map([[`${collection8.size}`, collection8.size]]);
          let yellowanimationlivel = String.fromCharCode(106,95,52,57,95,97,112,112,101,97,114,101,100,0);
          let themep = true;
         short_a5N.set(`${themep}`, collection8.size);
         yellowanimationlivel = `${yellowanimationlivel.length}`;
       let twitterP: Map<any, any> = new Map([[String.fromCharCode(104,97,100,97,109,97,114,100,95,113,95,51,56,0),false ], [String.fromCharCode(116,97,105,108,95,98,95,55,0),false ]]);
       let huaweiH: Map<any, any> = new Map([[String.fromCharCode(97,108,115,97,95,107,95,52,49,0),true ], [String.fromCharCode(114,95,55,51,95,109,101,114,103,101,114,0),true ]]);
      while (!applicationH.startsWith(`${scheduleB}`)) {
         scheduleB ^= 3;
         break;
      }
      let iconuserp = 8921597 <= collection8.size;
      do {
         collection8.set(`${scheduleB}`, 3);
         if (iconuserp) {
            break;
         }
      } while (iconuserp && ((short_a5N.size | 3) <= 4));
      if (applicationH.length >= 5) {
         short_a5N.set(`${short_a5N.size}`, dependencies4.size / (Math.max(3, short_a5N.size)));
      }
          let awayiconT = String.fromCharCode(99,95,49,51,95,98,105,103,100,105,97,0);
          let downarrowu = String.fromCharCode(117,110,115,104,97,114,112,95,97,95,53,49,0);
          let iconclosewhitewithbgy = 1;
         scheduleB &= twitterP.size | 2;
         awayiconT += `${iconclosewhitewithbgy}`;
         downarrowu = `${downarrowu.length}`;
         iconclosewhitewithbgy ^= downarrowu.length % 3;
      while ((2 | applicationH.length) > 5) {
         scheduleB <<= Math.min(2, Math.abs(dependencies4.size));
         break;
      }
      for (let q = 0; q < 3; q++) {
         twitterP = new Map([[`${dependencies4.size}`, dependencies4.size + huaweiH.size]]);
      }
         dependencies4.set(`${collection8.size}`, twitterP.size & collection8.size);
      let whitebella = short_a5N.size >= 8366106;
      do {
         short_a5N.set(`${collection8.size}`, collection8.size / (Math.max(twitterP.size, 10)));
         if (whitebella) {
            break;
         }
      } while (((short_a5N.size ^ 2) > 3) && whitebella);
         twitterP = new Map([[`${collection8.size}`, 2]]);
      if (Array.from(short_a5N.values()).includes(scheduleB)) {
         short_a5N = new Map([[`${short_a5N.size}`, scheduleB | 2]]);
      }
          let indicatorN = String.fromCharCode(105,100,101,110,116,105,102,101,114,95,112,95,56,51,0);
          let arrowupb: Array<any> = [164, 808, 588];
          let expandk: Array<any> = [886, 101];
         twitterP = new Map([[`${short_a5N.size}`, short_a5N.size]]);
         indicatorN = "3";
         arrowupb.push((String.fromCharCode(48,0) == indicatorN ? expandk.length : indicatorN.length));
         expandk.push(expandk.length / 2);
      source8.set(`${taiwanw}`, 3);
   }
   if (source8.size >= 4) {
      source8.set(`${iconsettingJ}`, iconrightorangeY.length);
   }
      source8.set(direct8, inviteE.length - 3);
   for (let l = 0; l < 1; l++) {
      inviteE += `${source8.size ^ inviteE.length}`;
   }
   while (iconrightorangeY != String.fromCharCode(73,0)) {
      filedO += `${3 + tnewsshareX.length}`;
      break;
   }
   while (1 > homeloadingL.length) {
       let executor1 = String.fromCharCode(119,105,116,104,105,110,95,55,95,50,55,0);
       let defaultbasketballbga = 4.0;
       let clear9 = 1;
       let componentregistryt = String.fromCharCode(98,95,48,95,112,114,101,99,111,109,112,111,115,101,100,0);
      let indicatorH = clear9 >= 5720838;
      do {
         clear9 *= executor1.length;
         if (indicatorH) {
            break;
         }
      } while (indicatorH && (componentregistryt.includes(`${clear9}`)));
      if (4 == executor1.length) {
         executor1 += `${(executor1 == String.fromCharCode(107,0) ? parseInt(`${defaultbasketballbga}`) : executor1.length)}`;
      }
         defaultbasketballbga += (String.fromCharCode(52,0) == componentregistryt ? componentregistryt.length : parseInt(`${defaultbasketballbga}`));
      let sideM = componentregistryt.length <= 9031772;
      do {
         componentregistryt += `${(String.fromCharCode(84,0) == executor1 ? executor1.length : parseInt(`${defaultbasketballbga}`))}`;
         if (sideM) {
            break;
         }
      } while (sideM && (clear9 < 1));
          let detailK = 3;
          let frame_n72 = 4.0;
          let mbsplashQ = 4.0;
         executor1 += `${parseInt(`${frame_n72}`)}`;
         detailK |= parseInt(`${mbsplashQ}`);
         frame_n72 -= parseFloat(`${parseInt(`${mbsplashQ}`)}`);
         executor1 += `${parseInt(`${defaultbasketballbga}`) + 1}`;
      let iconorientationT = clear9 <= 8617262;
      do {
         clear9 -= clear9 >> (Math.min(3, Math.abs(3)));
         if (iconorientationT) {
            break;
         }
      } while ((!executor1.includes(`${clear9}`)) && iconorientationT);
      while (4 <= (executor1.length + 3) && (3 + executor1.length) <= 1) {
         clear9 /= Math.max(4, componentregistryt.length << (Math.min(Math.abs(3), 3)));
         break;
      }
      for (let v = 0; v < 2; v++) {
         componentregistryt += `${executor1.length >> (Math.min(Math.abs(1), 3))}`;
      }
       let dialogy = String.fromCharCode(97,99,116,117,97,108,105,122,97,116,105,111,110,95,120,95,48,0);
       let greenf = String.fromCharCode(101,95,54,48,95,116,97,103,103,101,100,0);
      if (executor1.length <= componentregistryt.length) {
         componentregistryt = `${parseInt(`${defaultbasketballbga}`) / 1}`;
      }
          let notificationgrayf = true;
          let fieldm: Map<any, any> = new Map([[String.fromCharCode(116,101,120,116,102,105,108,101,95,54,95,56,51,0),972], [String.fromCharCode(119,101,98,109,100,101,99,95,109,95,50,52,0),568]]);
         componentregistryt += `${parseInt(`${defaultbasketballbga}`) | 1}`;
         notificationgrayf = (((notificationgrayf ? 43 : fieldm.size) >> (Math.min(Math.abs(fieldm.size), 3))) <= 43);
      homeloadingL = `${source8.size & 1}`;
      break;
   }
   for (let m = 0; m < 3; m++) {
      taiwanw &= (homeloadingL == String.fromCharCode(81,0) ? splashz.size : homeloadingL.length);
   }
      inviteE += `${(homeloadingL == String.fromCharCode(113,0) ? homeloadingL.length : baiduadsR.length)}`;
   let castV = source8.size <= 5528368;
   do {
      source8 = new Map([[`${splashz.size}`, 2 << (Math.min(5, Math.abs(splashz.size)))]]);
      if (castV) {
         break;
      }
   } while (castV && (5 < (3 + source8.size)));
       let profileframei = String.fromCharCode(117,112,116,105,109,101,95,55,95,55,49,0);
      while (profileframei.includes(`${profileframei.length}`)) {
         profileframei += `${profileframei.length / 3}`;
         break;
      }
      for (let b = 0; b < 2; b++) {
         profileframei += `${(String.fromCharCode(48,0) == profileframei ? profileframei.length : profileframei.length)}`;
      }
      if (profileframei != String.fromCharCode(75,0)) {
         profileframei = `${2 * profileframei.length}`;
      }
      taiwanw += source8.size;
   for (let y = 0; y < 1; y++) {
      baiduadsR.push(2 - splashz.size);
   }
   let bgvipxvodO = String.fromCharCode(52,118,107,113,55,50,116,55,107,0) == direct8;
   do {
       let textg = String.fromCharCode(116,95,55,49,95,98,108,97,107,101,0);
       let dragu = 2.0;
       let tooltipsq = String.fromCharCode(99,114,105,116,105,99,97,108,115,101,99,116,105,111,110,95,52,95,54,56,0);
      if (3.83 == (1.64 + dragu)) {
         dragu -= parseInt(`${dragu}`) >> (Math.min(Math.abs(2), 4));
      }
      while (textg.startsWith(`${dragu}`)) {
         dragu += tooltipsq.length << (Math.min(4, Math.abs(parseInt(`${dragu}`))));
         break;
      }
          let checkbox8 = true;
          let mbsplashe: Map<any, any> = new Map([[String.fromCharCode(108,95,51,95,114,101,97,100,120,98,108,111,99,107,0),834], [String.fromCharCode(114,101,99,111,110,115,116,114,117,99,116,95,107,95,52,54,0),501]]);
          let feedbacks = String.fromCharCode(105,110,100,101,102,105,110,105,116,101,108,121,95,104,95,55,54,0);
         textg = `${tooltipsq.length}`;
         checkbox8 = !checkbox8 && mbsplashe.size <= 98;
         mbsplashe = new Map([[`${mbsplashe.size}`, (feedbacks == String.fromCharCode(90,0) ? feedbacks.length : mbsplashe.size)]]);
      while (3 >= (textg.length / (Math.max(10, parseInt(`${dragu}`)))) || (dragu / (Math.max(textg.length, 6))) >= 1.96) {
         dragu *= parseInt(`${dragu}`) % (Math.max(tooltipsq.length, 1));
         break;
      }
      for (let i = 0; i < 3; i++) {
          let libswscaleX = 0.0;
          let private_tM: Array<any> = [157, 102, 71];
          let componentregistrym = String.fromCharCode(102,97,117,108,116,121,95,105,95,52,50,0);
         dragu /= Math.max(componentregistrym.length ^ tooltipsq.length, 1);
         libswscaleX /= Math.max(parseFloat(`${parseInt(`${libswscaleX}`) & private_tM.length}`), 3);
         private_tM.push(private_tM.length + 2);
         componentregistrym += `${parseInt(`${libswscaleX}`) + 1}`;
      }
      direct8 = `${(inviteE == String.fromCharCode(120,0) ? inviteE.length : splashz.size)}`;
      if (bgvipxvodO) {
         break;
      }
   } while (bgvipxvodO && (taiwanw < direct8.length));
   while (4 <= filedO.length) {
       let bootsplash4 = String.fromCharCode(103,95,53,54,95,110,111,110,110,117,108,108,0);
       let components8: Array<any> = [40, 185, 113];
      let statisticsA = 6965418 >= bootsplash4.length;
      do {
         bootsplash4 = `${bootsplash4.length / (Math.max(6, components8.length))}`;
         if (statisticsA) {
            break;
         }
      } while ((4 <= (bootsplash4.length / (Math.max(8, components8.length))) || (components8.length / 4) <= 4) && statisticsA);
         bootsplash4 = "3";
      let anythink4 = components8.length >= 8033239;
      do {
         components8 = [components8.length - 2];
         if (anythink4) {
            break;
         }
      } while (anythink4 && (components8.length == 2));
      let hooksi = 7036155 >= components8.length;
      do {
         components8 = [1];
         if (hooksi) {
            break;
         }
      } while ((bootsplash4.length >= 1) && hooksi);
      let livesharee = bootsplash4.length <= 6582552;
      do {
         bootsplash4 += `${bootsplash4.length}`;
         if (livesharee) {
            break;
         }
      } while ((bootsplash4.length == 2) && livesharee);
         bootsplash4 += "1";
      direct8 += `${baiduadsR.length}`;
      break;
   }
   while ((direct8.length - splashz.size) <= 3 && (splashz.size - 3) <= 4) {
       let smallP: Array<any> = [String.fromCharCode(111,95,55,48,95,118,99,111,109,98,105,110,101,0), String.fromCharCode(113,95,51,95,100,101,110,111,105,115,101,0), String.fromCharCode(116,105,109,101,100,95,121,95,52,48,0)];
       let stepa = String.fromCharCode(102,111,114,99,105,110,103,95,97,95,54,49,0);
       let indexq = 0.0;
       let dialogB = 5;
       let profileframe2 = false;
         indexq /= Math.max(5, smallP.length);
          let greenarrowupw: Map<any, any> = new Map([[String.fromCharCode(103,95,55,56,95,97,112,112,114,111,112,114,105,97,116,101,0),549], [String.fromCharCode(118,95,53,54,95,101,120,112,97,110,100,101,114,0),811]]);
          let privatechatbgn: Array<any> = [348, 744];
         smallP = [smallP.length + greenarrowupw.size];
         greenarrowupw = new Map([[`${privatechatbgn.length}`, 2]]);
         privatechatbgn.push(3);
      for (let h = 0; h < 1; h++) {
         stepa = `${dialogB}`;
      }
          let iconuserd: Array<any> = [593, 55];
          let iconfeedbackM: Array<any> = [636, 158, 338];
         dialogB ^= 1;
         iconuserd.push(iconfeedbackM.length);
         iconfeedbackM = [iconfeedbackM.length - 3];
      if (dialogB < 3) {
         profileframe2 = smallP.length > dialogB;
      }
         stepa = `${stepa.length + smallP.length}`;
          let catalogT = false;
         stepa += `${parseInt(`${indexq}`) + stepa.length}`;
         catalogT = !catalogT || catalogT;
      if ((4 | dialogB) < 3 && 1 < (4 | dialogB)) {
         stepa = `${(stepa == String.fromCharCode(72,0) ? (profileframe2 ? 2 : 3) : stepa.length)}`;
      }
      if ((3.28 / (Math.max(4, indexq))) > 2.58) {
         indexq -= 1 << (Math.min(Math.abs(dialogB), 5));
      }
          let headerV: Map<any, any> = new Map([[String.fromCharCode(98,121,116,101,115,116,114,101,97,109,95,57,95,52,52,0),160], [String.fromCharCode(108,95,49,52,95,99,111,110,100,101,110,115,101,100,0),694]]);
          let rulesH = 5.0;
         smallP.push(((profileframe2 ? 4 : 1) | 1));
         headerV = new Map([[`${headerV.size}`, parseInt(`${rulesH}`) << (Math.min(Math.abs(2), 3))]]);
         rulesH += parseFloat(`${1}`);
      if ((stepa.length * dialogB) <= 4 && (stepa.length * 4) <= 1) {
         stepa += `${stepa.length - 2}`;
      }
         indexq -= 1;
          let langkeye = 0.0;
          let logouser7 = String.fromCharCode(106,95,54,53,95,101,114,97,115,101,114,0);
         smallP = [stepa.length * 3];
         langkeye += parseInt(`${langkeye}`) & logouser7.length;
         logouser7 += `${logouser7.length}`;
         profileframe2 = stepa.length >= 43;
       let bottom2 = 4.0;
       let orangeZ = 1.0;
      direct8 = `${inviteE.length | direct8.length}`;
      break;
   }
   while (3 <= (iconrightorangeY.length - 2) || (2 - iconrightorangeY.length) <= 4) {
       let basketballawayteamn: Array<any> = [809, 509];
       let logox: Map<any, any> = new Map([[String.fromCharCode(104,95,57,51,95,111,118,101,114,105,100,100,101,110,0),false ], [String.fromCharCode(108,95,54,51,95,102,97,105,108,115,0),false ], [String.fromCharCode(115,117,109,95,104,95,49,48,0),true ]]);
       let subout7: Array<any> = [459, 948, 642];
       let logoT = String.fromCharCode(118,95,56,95,110,101,97,114,115,101,116,0);
         subout7.push(logoT.length ^ 1);
      let datae = 7776093 <= logox.size;
      do {
         logox = new Map([[`${logox.size}`, logox.size % 3]]);
         if (datae) {
            break;
         }
      } while (datae && ((3 << (Math.min(3, Math.abs(logox.size)))) <= 4 && 3 <= (basketballawayteamn.length << (Math.min(Math.abs(logox.size), 4)))));
      if (logoT.length <= 3) {
         logoT = `${subout7.length & 1}`;
      }
         basketballawayteamn = [2];
      if ((basketballawayteamn.length - logoT.length) < 2 || 2 < (logoT.length - basketballawayteamn.length)) {
         logoT = `${logoT.length}`;
      }
          let inouttargetredm = String.fromCharCode(113,95,51,53,95,115,101,116,0);
          let remindere = 2.0;
         logox = new Map([[logoT, (String.fromCharCode(113,0) == inouttargetredm ? inouttargetredm.length : logoT.length)]]);
         remindere -= parseInt(`${remindere}`) % 3;
         logox = new Map([[`${subout7.length}`, 3]]);
       let profileframef = 5;
       let scrollviewk = 1;
      let logoo = logoT.length <= 9743728;
      do {
         logoT = `${subout7.length ^ scrollviewk}`;
         if (logoo) {
            break;
         }
      } while ((5 < (subout7.length + logoT.length) || 1 < (5 + subout7.length)) && logoo);
      let appleg = String.fromCharCode(107,104,56,118,106,112,104,52,104,54,0) == logoT;
      do {
          let smallorangemanA: Map<any, any> = new Map([[String.fromCharCode(104,95,51,50,95,97,108,97,110,103,117,97,103,101,0),String.fromCharCode(116,116,108,95,102,95,50,51,0)], [String.fromCharCode(97,95,49,52,95,100,105,102,102,101,114,101,110,116,0),String.fromCharCode(97,99,99,101,115,115,111,114,105,101,115,95,50,95,49,50,0)]]);
          let t_unlockN = 0;
          let mbsplashm = 5.0;
          let awayS = false;
         logoT = `${logox.size}`;
         smallorangemanA.set(`${t_unlockN}`, (t_unlockN - (awayS ? 3 : 5)));
         mbsplashm += 3;
         awayS = smallorangemanA.size > 54;
         if (appleg) {
            break;
         }
      } while (appleg && (profileframef <= 2));
      let profileinactivej = 5360509 >= scrollviewk;
      do {
         scrollviewk -= 2;
         if (profileinactivej) {
            break;
         }
      } while (profileinactivej && (5 < (2 & profileframef) || (profileframef & scrollviewk) < 2));
       let memberQ = 3;
      iconsettingJ &= basketballawayteamn.length * homeloadingL.length;
      break;
   }
   while (splashz.size < 1) {
       let bangJ: Array<any> = [String.fromCharCode(115,95,52,49,95,109,105,100,105,0), String.fromCharCode(115,104,111,114,116,99,117,116,115,95,120,95,55,54,0), String.fromCharCode(101,95,52,57,95,98,117,108,107,0)];
       let becomeH = true;
       let eactV = 4.0;
      if (eactV > 1.32) {
         becomeH = 81.13 > eactV;
      }
          let reactnavigation0 = String.fromCharCode(112,95,51,49,95,117,112,108,111,97,100,0);
         eactV /= Math.max(parseFloat(`${bangJ.length}`), 1);
         reactnavigation0 += `${(reactnavigation0 == String.fromCharCode(106,0) ? reactnavigation0.length : reactnavigation0.length)}`;
      if (becomeH || (5.92 + eactV) == 3.6) {
          let vignette3 = String.fromCharCode(109,111,118,101,110,99,95,105,95,51,52,0);
          let iconclosewhitebgN = 3;
          let subbasketballplayerV = String.fromCharCode(116,95,52,50,95,111,99,116,101,116,115,0);
         becomeH = iconclosewhitebgN >= vignette3.length;
         vignette3 = "3";
         iconclosewhitebgN &= 3 | subbasketballplayerV.length;
      }
          let paginationK: Array<any> = [84, 111];
          let prediction9 = 2.0;
         eactV -= parseFloat(`${3 / (Math.max(parseInt(`${eactV}`), 7))}`);
         paginationK = [paginationK.length - 3];
         prediction9 /= Math.max(1, paginationK.length * parseInt(`${prediction9}`));
      while (3 <= bangJ.length || 5 <= (3 & bangJ.length)) {
         becomeH = (((!becomeH ? 66 : bangJ.length) * bangJ.length) == 66);
         break;
      }
         becomeH = !becomeH;
         eactV /= Math.max(parseFloat(`${parseInt(`${eactV}`) << (Math.min(bangJ.length, 4))}`), 4);
      if (3 < bangJ.length || 4 < (bangJ.length + 3)) {
         bangJ.push(parseInt(`${eactV}`) | 2);
      }
      for (let j = 0; j < 2; j++) {
         becomeH = bangJ.length <= 31 || becomeH;
      }
      inviteE = `${tnewsshareX.length}`;
      break;
   }
   for (let u = 0; u < 3; u++) {
       let balll = false;
       let playershirts = String.fromCharCode(115,111,110,101,119,99,111,110,110,95,117,95,55,54,0);
       let bufferY = String.fromCharCode(108,122,111,120,95,108,95,51,57,0);
         playershirts = `${bufferY.length}`;
         bufferY += `${bufferY.length}`;
      if (!bufferY.startsWith(playershirts)) {
         playershirts += `${((balll ? 2 : 1))}`;
      }
         bufferY += `${((balll ? 3 : 4))}`;
         balll = playershirts.length > 85;
          let abidetects = 2;
          let subbasketballplayere = true;
          let traminis = 4;
         balll = bufferY.length < traminis;
         abidetects &= abidetects << (Math.min(5, Math.abs(1)));
         subbasketballplayere = !subbasketballplayere;
         traminis %= Math.max(5, abidetects >> (Math.min(2, Math.abs(1))));
         playershirts += `${(3 & (balll ? 1 : 1))}`;
      let iconcloser = String.fromCharCode(100,55,109,103,120,99,109,122,114,106,0) == bufferY;
      do {
         bufferY = `${bufferY.length}`;
         if (iconcloser) {
            break;
         }
      } while (iconcloser && (!playershirts.endsWith(`${bufferY.length}`)));
          let regengu = String.fromCharCode(98,114,111,119,115,101,95,116,95,50,50,0);
          let reminderJ = String.fromCharCode(112,108,117,114,97,108,105,122,97,116,105,111,110,95,121,95,57,57,0);
          let proxyS = String.fromCharCode(107,95,57,95,109,97,115,107,113,0);
         playershirts = `${bufferY.length ^ 2}`;
         regengu = `${reminderJ.length}`;
         reminderJ = `${reminderJ.length / 3}`;
         proxyS = `${regengu.length % 1}`;
      iconrightorangeY += `${splashz.size << (Math.min(baiduadsR.length, 5))}`;
   }
      tnewsshareX.push(direct8.length >> (Math.min(Math.abs(1), 5)));

    togglePlayPause();
  };
  return (
    <View style={styles.middleControls}>
      {videoType !== 'live' && (
        <View style={styles.leftButton}>
          <RectButton
            rippleColor="transparent"
            disallowInterruption={true}
            onPress={() => onSkip(-10)}>
            <Rewind width={55} height={55} />
          </RectButton>
        </View>
      )}
      <RectButton
        disallowInterruption={true}
        rippleColor="transparent"
        onPress={() => onTogglePlayPause()}>
        {paused ? (
          <Play width={55} height={55} />
        ) : (
          <Pause width={55} height={55} />
        )}
      </RectButton>
      {videoType !== 'live' && (
        <View style={styles.rightButton}>
          <RectButton
            rippleColor="transparent"
            disallowInterruption={true}
            onPress={() => onSkip(10)}>
            <Fastforward width={55} height={55} />
          </RectButton>
        </View>
      )}
    </View>
  );
};

export default memo(MiddleControls);

const styles = StyleSheet.create({
  middleControls: {
    flexDirection: 'row',
  },
  sideButtons: {
    paddingLeft: 46,
    paddingRight: 46,
  },
  leftButton: {
    alignItems: 'flex-end',
    paddingRight: 23,
  },
  rightButton: {
    alignItems: 'flex-start',
    paddingLeft: 23,
  },
});
