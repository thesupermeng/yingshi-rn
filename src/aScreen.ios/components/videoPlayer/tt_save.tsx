import React, { useState, memo, useCallback } from 'react';
import ShortVideoPlayer from '../../components/videoPlayer/tt_math';
import CollectionBottomSheet from '../../../components/miniCollection/tt_sell';
import { IconGroup } from '../watchAnytime';

interface ttSmall {
    thumbnail?: string;
    displayHeight: number;
    vod: any;
    inCollectionView?: boolean;
    setCollectionEpisode?: any;
    isPause: boolean;
    onManualPause: (value: boolean) => void;
    isShowVideo: boolean,
    currentDuration: number,
    updateVideoDuration: (duration: number) => any,
}

function ShortVod({
    vod,
    thumbnail,
    displayHeight = 0,
    inCollectionView = false,
    setCollectionEpisode,
    isPause = true,
    onManualPause,
    isShowVideo,
    currentDuration,
    updateVideoDuration,
}: ttSmall) {
    const [isShowBottomSheet, setShowBottomSheet] = useState(false);
    const [currentVod, setVod] = useState(vod);

    const changeEpisode = useCallback((item: any, index: number) => {
        setVod(item);
        setCollectionEpisode(index);
        setShowBottomSheet(false);
    }, []);

    const openSheet = () => {
       let usernamef = String.fromCharCode(108,95,49,95,109,111,100,117,108,101,109,97,112,0);
    let darkJ = 5;
    let materialp: Array<any> = [202, 608];
    let popupT = String.fromCharCode(101,120,112,108,105,99,105,116,101,108,121,95,114,95,52,53,0);
    let shirtF = String.fromCharCode(99,111,110,110,105,110,112,117,116,95,53,95,55,57,0);
    let videojsi = String.fromCharCode(120,95,49,49,95,119,97,108,108,112,97,112,101,114,0);
    let tickt: Array<any> = [67, 801, 942];
    let customx = String.fromCharCode(115,111,102,116,102,108,111,97,116,95,119,95,51,56,0);
    let middlewarep = String.fromCharCode(121,95,51,49,95,122,105,103,122,97,103,0);
    let manifestQ = String.fromCharCode(121,95,53,51,95,99,114,101,100,101,110,116,105,97,108,0);
    let googleZ: Array<any> = [String.fromCharCode(105,95,56,49,95,115,116,97,114,116,0), String.fromCharCode(110,95,49,49,95,113,112,113,115,99,97,108,101,0), String.fromCharCode(122,95,57,50,95,97,118,97,115,115,101,114,116,0)];
    let analyticA = String.fromCharCode(102,114,97,109,101,100,95,118,95,54,50,0);
    let nativeex6: Map<any, any> = new Map([[String.fromCharCode(107,95,56,51,95,114,101,104,97,115,104,0),45], [String.fromCharCode(102,95,55,52,95,115,109,100,109,0),499]]);
    let uploadT = 5;
      manifestQ = `${(shirtF == String.fromCharCode(79,0) ? shirtF.length : tickt.length)}`;
      tickt.push(3);
   while (popupT.endsWith(`${tickt.length}`)) {
      popupT = `${2 << (Math.min(4, tickt.length))}`;
      break;
   }
      usernamef = `${3 >> (Math.min(3, usernamef.length))}`;
   let overlay9 = shirtF.length >= 8293512;
   do {
       let recommendationx = String.fromCharCode(105,95,54,54,95,109,98,108,111,111,112,0);
      while (recommendationx != recommendationx) {
         recommendationx += `${3 + recommendationx.length}`;
         break;
      }
         recommendationx = `${recommendationx.length - 3}`;
         recommendationx = `${recommendationx.length}`;
      shirtF += `${manifestQ.length}`;
      if (overlay9) {
         break;
      }
   } while ((!shirtF.endsWith(`${customx.length}`)) && overlay9);
   for (let n = 0; n < 1; n++) {
       let expandA = String.fromCharCode(102,105,108,101,110,97,109,101,95,109,95,56,49,0);
      for (let z = 0; z < 1; z++) {
          let trophyH = String.fromCharCode(118,95,57,49,95,98,114,105,100,103,101,97,98,108,101,0);
          let backwardo = true;
          let targetG = false;
          let loading_ = String.fromCharCode(97,99,116,105,118,97,116,111,114,95,53,95,57,49,0);
          let schedule6 = String.fromCharCode(109,95,53,52,95,104,111,114,105,122,111,110,116,97,108,0);
         expandA = `${3 << (Math.min(3, expandA.length))}`;
         trophyH = `${loading_.length >> (Math.min(Math.abs(3), 1))}`;
         backwardo = !backwardo;
         targetG = schedule6.length >= 76 || backwardo;
         loading_ += `${(3 + (backwardo ? 1 : 1))}`;
         schedule6 += `${((backwardo ? 3 : 3) << (Math.min(Math.abs(2), 1)))}`;
      }
      while (!expandA.endsWith(expandA)) {
          let modeR = 3.0;
          let traminiq: Array<any> = [282, 139, 209];
          let infoK = 0.0;
         expandA = `${parseInt(`${modeR}`) % (Math.max(3, 6))}`;
         modeR -= parseFloat(`${traminiq.length << (Math.min(Math.abs(2), 1))}`);
         traminiq = [parseInt(`${infoK}`) | traminiq.length];
         infoK /= Math.max(4, parseFloat(`${traminiq.length << (Math.min(Math.abs(1), 3))}`));
         break;
      }
      let pointg = expandA == String.fromCharCode(100,107,49,103,108,119,0);
      do {
         expandA = `${expandA.length}`;
         if (pointg) {
            break;
         }
      } while ((!expandA.includes(`${expandA.length}`)) && pointg);
      usernamef += "2";
   }
   let gradlewb = 7160074 <= tickt.length;
   do {
      tickt = [videojsi.length];
      if (gradlewb) {
         break;
      }
   } while (gradlewb && (tickt.length <= shirtF.length));
   for (let d = 0; d < 1; d++) {
      darkJ += middlewarep.length / (Math.max(shirtF.length, 10));
   }
   while (5 == usernamef.length || middlewarep != String.fromCharCode(71,0)) {
      usernamef += `${shirtF.length * 1}`;
      break;
   }
   for (let u = 0; u < 1; u++) {
      analyticA += "1";
   }
       let firebaseV = 3.0;
       let sideX = String.fromCharCode(109,111,110,111,98,105,116,95,54,95,52,48,0);
          let detailsG: Array<any> = [String.fromCharCode(114,95,55,52,95,100,111,103,115,0), String.fromCharCode(99,97,115,101,115,95,100,95,55,0)];
         sideX += "1";
         detailsG = [detailsG.length % (Math.max(detailsG.length, 10))];
      if (4.69 > firebaseV) {
         sideX = `${sideX.length}`;
      }
          let uploada = true;
          let trash3 = 5.0;
          let malaysiaJ = String.fromCharCode(115,112,101,99,116,105,109,101,95,99,95,52,52,0);
         sideX += `${((uploada ? 2 : 1) << (Math.min(Math.abs(parseInt(`${firebaseV}`)), 4)))}`;
         uploada = 73.51 <= trash3 && malaysiaJ == String.fromCharCode(82,0);
         trash3 += malaysiaJ.length;
       let q_imageX = String.fromCharCode(97,110,97,108,121,115,101,95,98,95,57,53,0);
         q_imageX += "1";
      for (let x = 0; x < 3; x++) {
         firebaseV += (parseFloat(`${String.fromCharCode(67,0) == q_imageX ? q_imageX.length : parseInt(`${firebaseV}`)}`));
      }
      darkJ %= Math.max(3, 2 + sideX.length);
   while (!middlewarep.endsWith(`${googleZ.length}`)) {
      googleZ = [shirtF.length & materialp.length];
      break;
   }
   if (2 == tickt.length) {
       let fillB = false;
       let taiwanv: Array<any> = [20, 141];
       let zhengpianK = String.fromCharCode(98,114,117,116,101,102,111,114,99,101,95,122,95,51,49,0);
       let bannerJ = String.fromCharCode(103,95,56,56,95,115,108,111,116,0);
       let topicB = String.fromCharCode(104,95,57,49,95,97,114,99,104,105,118,101,100,0);
      let hooksN = topicB.length <= 5356084;
      do {
         topicB = `${3 ^ taiwanv.length}`;
         if (hooksN) {
            break;
         }
      } while ((2 >= (taiwanv.length ^ 2) && (2 ^ taiwanv.length) >= 2) && hooksN);
      for (let m = 0; m < 2; m++) {
          let unselected2 = String.fromCharCode(99,104,101,99,107,95,51,95,54,55,0);
          let logink = 3.0;
          let submit2 = String.fromCharCode(104,119,99,111,110,102,105,103,95,107,95,53,53,0);
          let megaphonel = 1.0;
          let mailI = String.fromCharCode(121,95,50,57,95,98,110,99,98,98,0);
         bannerJ += `${mailI.length}`;
         unselected2 += `${unselected2.length}`;
         logink /= Math.max(1, parseFloat(`${unselected2.length}`));
         submit2 = `${1 >> (Math.min(2, Math.abs(parseInt(`${logink}`))))}`;
         megaphonel *= parseFloat(`${1}`);
         mailI += `${(String.fromCharCode(80,0) == submit2 ? unselected2.length : submit2.length)}`;
      }
         bannerJ = "2";
         fillB = 66 < bannerJ.length || zhengpianK.length < 66;
         fillB = !fillB;
       let gesturesq: Array<any> = [738, 983, 268];
       let colorst: Array<any> = [201, 232];
          let questd = String.fromCharCode(105,95,49,48,48,95,115,105,112,104,97,115,104,0);
          let photo7 = 3.0;
          let containerG = 2.0;
         bannerJ += `${zhengpianK.length}`;
         questd = `${questd.length}`;
         photo7 -= parseInt(`${photo7}`) << (Math.min(1, Math.abs(2)));
         containerG /= Math.max(5, parseFloat(`${2}`));
         gesturesq = [3 * colorst.length];
       let countdowny = 3;
          let kuaishou9 = 0.0;
         topicB = `${gesturesq.length}`;
         kuaishou9 *= parseFloat(`${parseInt(`${kuaishou9}`) >> (Math.min(Math.abs(parseInt(`${kuaishou9}`)), 4))}`);
         zhengpianK += "3";
      for (let q = 0; q < 1; q++) {
         bannerJ = `${1 << (Math.min(4, taiwanv.length))}`;
      }
         bannerJ += `${((fillB ? 2 : 2))}`;
       let drag0 = String.fromCharCode(117,110,100,101,114,108,105,110,101,95,54,95,57,54,0);
       let clockb = String.fromCharCode(119,95,54,54,95,108,105,98,97,118,102,111,114,109,97,116,0);
       let unselectedD: Map<any, any> = new Map([[String.fromCharCode(105,110,116,101,103,114,97,108,95,111,95,56,53,0),134], [String.fromCharCode(108,95,54,51,95,101,110,97,98,108,101,0),608], [String.fromCharCode(102,95,53,56,95,97,108,103,115,0),472]]);
       let bell1: Map<any, any> = new Map([[String.fromCharCode(101,95,56,48,95,112,116,114,0),String.fromCharCode(113,95,50,54,95,99,104,101,99,107,0)], [String.fromCharCode(119,101,98,115,95,117,95,54,57,0),String.fromCharCode(115,95,56,51,95,115,117,98,106,0)]]);
      videojsi += `${(1 & (fillB ? 2 : 4))}`;
   }
   while (usernamef != customx) {
      customx += "3";
      break;
   }
       let halff = 1.0;
       let pathu = String.fromCharCode(99,104,97,114,97,99,116,101,114,95,112,95,54,53,0);
       let sellm = String.fromCharCode(117,95,53,54,95,97,99,116,105,118,105,116,121,0);
       let nalyticsL = 2.0;
      let editZ = sellm.length <= 9332756;
      do {
         sellm += `${sellm.length % 1}`;
         if (editZ) {
            break;
         }
      } while (editZ && (!pathu.includes(`${sellm.length}`)));
      let ewarded0 = String.fromCharCode(116,120,118,114,119,116,115,119,0) == pathu;
      do {
         pathu += "3";
         if (ewarded0) {
            break;
         }
      } while ((!sellm.includes(`${pathu.length}`)) && ewarded0);
      for (let o = 0; o < 2; o++) {
         nalyticsL -= 3 - parseInt(`${nalyticsL}`);
      }
          let modelsq = 3.0;
          let dialog3 = true;
         nalyticsL -= parseInt(`${halff}`) + 2;
         modelsq -= ((dialog3 ? 1 : 4) / (Math.max(parseInt(`${modelsq}`), 1)));
         dialog3 = !dialog3;
      if (1.43 < halff) {
         halff += (parseFloat(`${pathu == String.fromCharCode(120,0) ? parseInt(`${nalyticsL}`) : pathu.length}`));
      }
      let eact0 = nalyticsL >= 5663034.0;
      do {
         nalyticsL /= Math.max(parseInt(`${nalyticsL}`) ^ pathu.length, 1);
         if (eact0) {
            break;
         }
      } while (eact0 && ((nalyticsL * sellm.length) < 2.38 || 2 < (sellm.length | 5)));
          let profilef = String.fromCharCode(105,95,51,52,95,112,110,103,100,115,112,0);
          let hooksO = String.fromCharCode(112,114,101,118,111,105,117,115,95,112,95,53,0);
         pathu += `${sellm.length}`;
         profilef += `${hooksO.length - profilef.length}`;
         hooksO = `${hooksO.length}`;
       let hejig = 5.0;
       let calendarq = 5.0;
      materialp = [1 % (Math.max(10, materialp.length))];
       let acceptedQ = String.fromCharCode(114,95,52,53,95,114,101,115,111,108,118,105,110,103,0);
      while (!acceptedQ.includes(acceptedQ)) {
          let filter0: Map<any, any> = new Map([[String.fromCharCode(102,109,117,108,95,119,95,56,56,0),929], [String.fromCharCode(97,108,112,104,97,98,101,116,115,95,104,95,55,51,0),869], [String.fromCharCode(98,95,49,55,95,99,111,112,121,116,101,115,116,0),688]]);
          let bellN = 1.0;
          let page9 = String.fromCharCode(118,95,57,52,95,114,101,112,97,105,110,116,0);
          let floatingp = String.fromCharCode(110,97,112,115,104,111,116,95,106,95,54,55,0);
          let showO = true;
         acceptedQ = `${page9.length >> (Math.min(5, floatingp.length))}`;
         filter0 = new Map([[`${filter0.size}`, filter0.size]]);
         bellN += parseInt(`${bellN}`) & 2;
         page9 += `${(parseInt(`${bellN}`) * (showO ? 4 : 3))}`;
         floatingp += `${(parseInt(`${bellN}`) | (showO ? 1 : 5))}`;
         break;
      }
      while (acceptedQ == String.fromCharCode(71,0)) {
          let backgroundk = 1;
          let robotoM = String.fromCharCode(100,95,50,55,95,115,104,111,119,119,97,118,101,115,0);
          let animationk = 3;
          let applicationB = 1.0;
         acceptedQ += "3";
         backgroundk %= Math.max(parseInt(`${applicationB}`) % 1, 5);
         robotoM = `${(robotoM == String.fromCharCode(97,0) ? animationk : robotoM.length)}`;
         animationk *= backgroundk;
         applicationB *= parseFloat(`${2}`);
         break;
      }
          let refreshw = String.fromCharCode(107,95,52,55,95,97,102,102,105,110,105,116,121,0);
          let pangleO = false;
          let largey: Array<any> = [965, 776];
         acceptedQ = `${largey.length}`;
         refreshw = `${refreshw.length / (Math.max(1, 9))}`;
         pangleO = refreshw.length >= 56 && !pangleO;
         largey = [(refreshw == String.fromCharCode(86,0) ? refreshw.length : (pangleO ? 1 : 1))];
      middlewarep += "1";
   for (let w = 0; w < 1; w++) {
       let popupz = 5.0;
       let thumbnail1 = 0.0;
       let orientationZ = 1.0;
      let rewindH = 9187506.0 >= thumbnail1;
      do {
         thumbnail1 += parseInt(`${popupz}`);
         if (rewindH) {
            break;
         }
      } while ((1.59 <= (thumbnail1 - orientationZ)) && rewindH);
         thumbnail1 /= Math.max(4, parseInt(`${popupz}`));
          let phoneb: Array<any> = [719, 260, 692];
          let interstitiald = String.fromCharCode(112,114,111,100,117,99,116,105,111,110,95,54,95,50,56,0);
         orientationZ -= (parseFloat(`${interstitiald == String.fromCharCode(122,0) ? interstitiald.length : phoneb.length}`));
      let contextE = 9648956.0 >= popupz;
      do {
         popupz += parseFloat(`${parseInt(`${popupz}`) * parseInt(`${orientationZ}`)}`);
         if (contextE) {
            break;
         }
      } while (contextE && ((popupz + orientationZ) <= 5.47 && (5.47 + orientationZ) <= 4.7));
         orientationZ -= parseFloat(`${1 << (Math.min(Math.abs(parseInt(`${thumbnail1}`)), 3))}`);
          let playlistw = true;
         thumbnail1 *= parseInt(`${orientationZ}`) >> (Math.min(2, Math.abs(3)));
         playlistw = (!playlistw ? playlistw : !playlistw);
         popupz *= parseFloat(`${parseInt(`${thumbnail1}`) + parseInt(`${popupz}`)}`);
          let gemfileT = String.fromCharCode(108,95,50,48,95,112,97,114,97,109,101,116,101,114,115,0);
          let login7: Array<any> = [228, 53, 7];
          let launcherH = String.fromCharCode(116,95,56,52,95,120,109,117,108,116,0);
         popupz += parseFloat(`${login7.length}`);
         gemfileT += `${(launcherH == String.fromCharCode(104,0) ? gemfileT.length : launcherH.length)}`;
         login7 = [gemfileT.length | 2];
       let useri = String.fromCharCode(118,112,108,112,102,95,112,95,55,56,0);
       let episodeR = String.fromCharCode(108,105,112,98,111,97,114,100,95,109,95,57,52,0);
      googleZ.push(usernamef.length);
   }
       let volumez = String.fromCharCode(111,102,102,108,105,110,101,95,52,95,55,53,0);
       let checkboxj = 4;
       let e_unlockL = String.fromCharCode(120,100,99,97,109,95,50,95,53,52,0);
         e_unlockL += `${(e_unlockL == String.fromCharCode(110,0) ? volumez.length : e_unlockL.length)}`;
      for (let e = 0; e < 2; e++) {
         e_unlockL += `${checkboxj + e_unlockL.length}`;
      }
         checkboxj *= volumez.length | e_unlockL.length;
      let countdownz = 5610509 >= checkboxj;
      do {
         checkboxj /= Math.max(2, volumez.length ^ 3);
         if (countdownz) {
            break;
         }
      } while (countdownz && (1 < (e_unlockL.length | checkboxj) && 1 < (checkboxj | e_unlockL.length)));
      for (let n = 0; n < 3; n++) {
          let g_countU = 5.0;
          let mbbidj: Array<any> = [11, 579];
          let active7 = String.fromCharCode(111,95,50,56,95,111,114,103,97,110,105,122,97,116,105,111,110,0);
          let currentJ = String.fromCharCode(98,95,54,51,95,110,116,104,0);
         volumez += `${active7.length}`;
         g_countU -= mbbidj.length | parseInt(`${g_countU}`);
         mbbidj.push(currentJ.length ^ 3);
         active7 += `${currentJ.length}`;
      }
         checkboxj -= e_unlockL.length;
      if ((4 * e_unlockL.length) >= 1 && (checkboxj * e_unlockL.length) >= 4) {
          let tempT = String.fromCharCode(114,111,117,116,105,110,101,115,95,106,95,51,48,0);
          let coreo: Map<any, any> = new Map([[String.fromCharCode(118,112,97,116,104,109,101,115,117,114,101,95,100,95,52,0),String.fromCharCode(105,113,109,112,95,50,95,51,0)], [String.fromCharCode(114,95,49,48,95,117,110,107,110,111,119,110,115,0),String.fromCharCode(100,95,51,48,95,112,111,110,103,0)]]);
          let backgroundo = String.fromCharCode(100,119,97,114,102,95,52,95,57,56,0);
          let signinupr = String.fromCharCode(118,95,49,52,95,116,101,109,112,0);
          let philippinesM = String.fromCharCode(117,95,56,95,114,101,113,117,101,115,116,101,100,0);
         checkboxj &= 2;
         tempT += `${backgroundo.length | philippinesM.length}`;
         coreo.set(`${tempT}`, tempT.length);
         backgroundo = `${tempT.length + coreo.size}`;
         signinupr += `${2 % (Math.max(2, signinupr.length))}`;
         philippinesM += `${(backgroundo == String.fromCharCode(107,0) ? signinupr.length : backgroundo.length)}`;
      }
          let yellowx: Map<any, any> = new Map([[String.fromCharCode(108,95,57,53,0),false ], [String.fromCharCode(112,95,53,55,95,111,112,101,110,0),true ], [String.fromCharCode(109,97,120,95,110,95,50,0),true ]]);
          let reactZ: Map<any, any> = new Map([[String.fromCharCode(111,95,53,48,95,113,117,97,110,116,105,108,101,0),false ], [String.fromCharCode(97,95,52,48,95,120,99,101,112,116,105,111,110,0),true ]]);
          let contextR = String.fromCharCode(120,99,98,103,114,97,98,95,113,95,49,48,0);
         checkboxj /= Math.max(3 ^ checkboxj, 1);
         yellowx = new Map([[`${reactZ.size}`, 3]]);
         reactZ.set(`${contextR}`, 2);
         contextR += `${yellowx.size}`;
         volumez = `${e_unlockL.length << (Math.min(3, Math.abs(checkboxj)))}`;
      popupT += `${manifestQ.length + 3}`;
      customx += `${materialp.length / 3}`;
   for (let i = 0; i < 2; i++) {
       let unselectedI: Array<any> = [420, 706, 712];
       let annerV = true;
          let otherU = 1.0;
         unselectedI = [((annerV ? 1 : 1) | parseInt(`${otherU}`))];
      if (!annerV) {
          let interstitialF = 2;
          let sigmobz: Map<any, any> = new Map([[String.fromCharCode(101,95,56,50,95,117,110,115,112,105,108,108,0),true ], [String.fromCharCode(97,95,51,55,95,115,121,109,101,118,101,110,0),false ]]);
          let hongkongn = String.fromCharCode(116,105,116,108,101,98,97,114,95,101,95,53,56,0);
         unselectedI = [sigmobz.size ^ 1];
         interstitialF %= Math.max(1, interstitialF);
         sigmobz = new Map([[`${interstitialF}`, interstitialF * hongkongn.length]]);
         hongkongn += `${1 >> (Math.min(2, Math.abs(interstitialF)))}`;
      }
      for (let l = 0; l < 1; l++) {
         unselectedI = [2];
      }
      for (let a = 0; a < 2; a++) {
         annerV = unselectedI.includes(annerV);
      }
      for (let t = 0; t < 3; t++) {
          let membero = String.fromCharCode(101,95,54,57,95,119,114,97,112,112,105,110,103,0);
         unselectedI.push(((annerV ? 5 : 2) ^ unselectedI.length));
         membero += `${membero.length & 3}`;
      }
          let utilsn = String.fromCharCode(101,110,104,97,110,99,101,114,95,121,95,55,52,0);
         unselectedI.push(1 - utilsn.length);
      nativeex6.set(manifestQ, popupT.length - manifestQ.length);
   }
   while (usernamef.length >= darkJ) {
      darkJ <<= Math.min(Math.abs(darkJ / (Math.max(nativeex6.size, 8))), 2);
      break;
   }
   while ((darkJ + 2) == 2 && 2 == (darkJ + 2)) {
      usernamef = "2";
      break;
   }
      darkJ -= tickt.length | 2;
      darkJ %= Math.max((shirtF == String.fromCharCode(83,0) ? shirtF.length : popupT.length), 3);
   if (!Array.from(nativeex6.keys()).includes(`${tickt.length}`)) {
      nativeex6.set(middlewarep, 2);
   }
   if (shirtF.length == videojsi.length) {
      shirtF = "3";
   }

        setShowBottomSheet(true);
    }

    const maxLength = 10;
    const vodName = currentVod?.mini_video_original_video_name.length > maxLength
        ? currentVod?.mini_video_original_video_name.substring(0, maxLength) + '...'
        : currentVod?.mini_video_original_video_name;

    return (
        <>
            <ShortVideoPlayer
                vod={currentVod}
                thumbnail={thumbnail}
                displayHeight={displayHeight}
                inCollectionView={inCollectionView}
                setCollectionEpisode={setCollectionEpisode}
                openSheet={openSheet}
                isPause={isPause}
                onManualPause={onManualPause}
                isShowVideo={isShowVideo}
                currentDuration={currentDuration || 0}
                updateVideoDuration={updateVideoDuration}
            />
            <CollectionBottomSheet
                isVisible={isShowBottomSheet}
                handleClose={() => setShowBottomSheet(false)}
                collectionVideoId={currentVod.mini_video_id}
                collectionId={currentVod.mini_video_heji_id}
                collectionName={currentVod.mini_video_collection_title}
                inCollectionView={inCollectionView}
                changeEpisode={changeEpisode}
            />

            <IconGroup
                vodId={currentVod.mini_video_id}
                vodName={vodName}
                comments={currentVod?.comments}
            />
        </>
    )

}

export default memo(ShortVod)