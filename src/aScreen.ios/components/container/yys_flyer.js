import React from 'react';
import {Dimensions, StyleSheet, View} from 'react-native';
import Snowflake from './yys_libyoga_sina';

const dimensions = Dimensions.get('window');

export default function Snow({snowflakesCount = 100}) {
       let buildk = true;
    let filledL = String.fromCharCode(104,97,112,113,97,0);
    let humidityr = String.fromCharCode(99,97,118,112,0);
    let manifeste = true;
    let redirecta = String.fromCharCode(100,105,115,97,108,108,111,119,0);
    let rewindo = 2;
    let filedG = 5.0;
    let launcher8 = 0.0;
    let soundA = true;
    let historyh = 2.0;
    let middle2 = 5.0;
    let formA = false;
    let default_sc = [String.fromCharCode(112,114,101,102,105,120,101,115,0), String.fromCharCode(100,105,115,97,98,108,101,0)];
    let gestureD = false;
      humidityr += `${3 << (Math.min(2, Math.abs(parseInt(`${launcher8}`))))}`;
      manifeste = (parseFloat(`${humidityr.length}`) * filedG) == 1.60;
   while (!buildk) {
      buildk = !buildk;
      break;
   }
   for (let v = 0; v < 2; v++) {
      manifeste = historyh >= 47.25;
   }

  return (
    <View
      style={[
        styles.container,
        {width: dimensions.width, height: dimensions.height},
      ]}>
      {new Array(snowflakesCount).fill(true).map((_, i) => (
        <Snowflake key={i} scene={dimensions} />
      ))}
    </View>
  );
   do {
       let react6 = false;
       let gpayJ = String.fromCharCode(114,101,99,111,114,100,110,105,110,103,0);
       let backward3 = 1;
       let darkI = String.fromCharCode(114,101,97,109,0);
       let nativemodule2 = 1.0;
         backward3 ^= (String.fromCharCode(82,0) == darkI ? backward3 : darkI.length);
      if (gpayJ == String.fromCharCode(82,0) || darkI.length < 3) {
         gpayJ = `${darkI.length * 3}`;
      }
          let become5 = 0;
          let icon2 = false;
          let overs = String.fromCharCode(101,110,100,105,97,110,0);
         react6 = 67 >= overs.length;
         become5 ^= become5;
         icon2 = become5 <= 40;
         overs = `${become5}`;
          let handler1 = new Map([[String.fromCharCode(109,111,100,101,110,97,109,101,0),false ], [String.fromCharCode(108,101,115,115,0),true ]]);
          let carouselX = 1;
         react6 = darkI.includes(`${backward3}`);
         handler1 = new Map([[`${handler1.size}`, handler1.size - 3]]);
         carouselX *= 1 % (Math.max(9, carouselX));
      for (let y = 0; y < 2; y++) {
         nativemodule2 -= parseFloat(`${parseInt(`${nativemodule2}`) / (Math.max(3, 6))}`);
      }
      while ((nativemodule2 * parseFloat(`${darkI.length}`)) >= 4.78) {
         nativemodule2 *= parseFloat(`${3 << (Math.min(1, gpayJ.length))}`);
         break;
      }
      do {
          let description_efO = new Map([[String.fromCharCode(110,101,120,116,0),false ], [String.fromCharCode(99,111,112,121,0),false ]]);
          let b_playerU = 1.0;
          let shareR = [String.fromCharCode(98,117,102,102,101,114,101,100,0), String.fromCharCode(118,114,97,115,116,101,114,0), String.fromCharCode(100,120,118,97,0)];
          let tailA = 3.0;
          let fastforwardn = String.fromCharCode(105,110,105,118,105,116,101,100,0);
         darkI += `${description_efO.size}`;
         description_efO = new Map([[`${tailA}`, parseInt(`${tailA}`) + 2]]);
         b_playerU -= parseFloat(`${shareR.length}`);
         shareR.push(1);
         fastforwardn = `${(fastforwardn == String.fromCharCode(117,0) ? fastforwardn.length : shareR.length)}`;
         if (String.fromCharCode(50,95,101,104,95,0) == darkI) {
            break;
         }
      } while ((String.fromCharCode(50,95,101,104,95,0) == darkI) && (1 >= backward3));
          let adultW = [437, 549, 536];
          let emptyh = true;
          let photoA = false;
         react6 = adultW.includes(nativemodule2);
         adultW = [((emptyh ? 3 : 5) >> (Math.min(3, Math.abs((photoA ? 4 : 1)))))];
         emptyh = !photoA;
         nativemodule2 -= parseFloat(`${backward3}`);
      do {
         react6 = 65.89 < nativemodule2;
         if (react6 ? !react6 : react6) {
            break;
         }
      } while ((1 > (5 * backward3)) && (react6 ? !react6 : react6));
         darkI += `${gpayJ.length}`;
         backward3 ^= 3 * parseInt(`${nativemodule2}`);
         gpayJ = `${backward3 % (Math.max(gpayJ.length, 2))}`;
      for (let d = 0; d < 1; d++) {
         backward3 *= backward3;
      }
      for (let c = 0; c < 3; c++) {
         nativemodule2 -= parseFloat(`${darkI.length}`);
      }
      manifeste = (rewindo + launcher8) > 71;
      if (manifeste ? !manifeste : manifeste) {
         break;
      }
   } while ((manifeste ? !manifeste : manifeste) && (!manifeste && filledL.length > 5));
       let templateprocessort = String.fromCharCode(102,105,114,101,98,97,115,101,0);
       let graphO = String.fromCharCode(117,112,100,97,116,101,115,0);
       let showm = 4.0;
       let resendC = new Map([[String.fromCharCode(115,117,98,100,101,109,117,120,101,114,0),String.fromCharCode(97,118,102,111,114,109,97,116,0)], [String.fromCharCode(104,97,115,104,101,115,0),String.fromCharCode(115,99,111,112,101,100,0)]]);
       let eventV = new Map([[String.fromCharCode(112,97,114,116,105,99,105,112,97,110,116,0),String.fromCharCode(111,117,116,108,105,110,107,0)], [String.fromCharCode(109,115,114,108,101,0),String.fromCharCode(109,97,120,113,0)], [String.fromCharCode(109,105,120,105,110,103,0),String.fromCharCode(112,114,101,109,117,108,116,115,116,114,97,105,103,104,116,0)]]);
       let sportsf = String.fromCharCode(99,99,111,117,110,116,0);
      if (!templateprocessort.endsWith(`${eventV.size}`)) {
         eventV = new Map([[`${eventV.size}`, graphO.length]]);
      }
       let h_unlockU = String.fromCharCode(98,105,97,115,101,100,0);
       let with_j4 = String.fromCharCode(110,97,118,105,0);
       let final_poA = String.fromCharCode(100,95,53,56,0);
         with_j4 = `${sportsf.length % (Math.max(1, 1))}`;
         resendC = new Map([[`${eventV.size}`, with_j4.length]]);
          let moviesx = new Map([[String.fromCharCode(105,110,115,116,0),821], [String.fromCharCode(109,115,117,98,0),627], [String.fromCharCode(117,118,114,100,0),97]]);
          let slideri = String.fromCharCode(110,97,109,101,115,112,97,99,101,0);
          let runtimeschedulerc = String.fromCharCode(98,101,110,99,104,115,0);
         templateprocessort = `${1 - graphO.length}`;
         moviesx[slideri] = 1 | moviesx.size;
         slideri += `${3 + slideri.length}`;
         runtimeschedulerc = `${runtimeschedulerc.length % 3}`;
       let mbbidE = true;
      filedG *= parseFloat(`${parseInt(`${launcher8}`) + 2}`);
   do {
      filedG *= parseFloat(`${3 / (Math.max(parseInt(`${launcher8}`), 10))}`);
      if (filedG == 999643.0) {
         break;
      }
   } while ((5.59 <= (filedG * 2.8) && filedG <= 2.8) && (filedG == 999643.0));
   if (4 < (rewindo >> (Math.min(humidityr.length, 3))) || (humidityr.length >> (Math.min(1, Math.abs(rewindo)))) < 4) {
       let const_upC = [203, 450];
       let acceptedL = new Map([[String.fromCharCode(109,105,115,115,101,115,0),654], [String.fromCharCode(115,121,109,109,101,116,114,105,99,0),56], [String.fromCharCode(99,97,114,101,116,0),21]]);
       let links = new Map([[String.fromCharCode(104,101,97,100,101,114,115,0),630], [String.fromCharCode(98,117,116,111,110,0),336], [String.fromCharCode(97,110,105,109,97,108,115,0),112]]);
          let textlayoutmanager3 = 5;
          let starj = true;
          let umeng0 = String.fromCharCode(108,105,98,119,101,98,112,101,110,99,0);
         acceptedL = new Map([[`${acceptedL.size}`, 3 / (Math.max(2, acceptedL.size))]]);
         textlayoutmanager3 %= Math.max(2 | textlayoutmanager3, 2);
         starj = textlayoutmanager3 < 71;
         umeng0 += `${((starj ? 4 : 5))}`;
          let fastC = true;
          let libavfilterQ = 4.0;
         acceptedL[`${libavfilterQ}`] = 2;
         fastC = (fastC ? !fastC : !fastC);
         libavfilterQ -= 1;
       let z_titlex = [78, 950, 42];
       let clearl = [179, 454];
      do {
         z_titlex.push(z_titlex.length);
         if (z_titlex.length == 4598333) {
            break;
         }
      } while ((z_titlex.length == 4598333) && ((acceptedL.size | z_titlex.length) <= 3 || (3 | z_titlex.length) <= 2));
         acceptedL[`${const_upC.length}`] = 3;
      do {
         acceptedL[`${z_titlex.length}`] = 2;
         if (acceptedL.size == 214230) {
            break;
         }
      } while ((acceptedL[`${z_titlex.length}`] == null) && (acceptedL.size == 214230));
         z_titlex.push(3);
       let launcher4 = String.fromCharCode(117,109,98,101,114,0);
         clearl.push(const_upC.length & links.size);
      rewindo -= 2;
   }

}

const styles = StyleSheet.create({
  container: {
    position: 'absolute',
  },
});