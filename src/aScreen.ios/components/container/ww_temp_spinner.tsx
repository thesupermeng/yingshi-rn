import React, {memo, useState, useRef, useEffect, useCallback} from 'react';
import {
  StyleSheet,
  Text,
  View,
  TouchableOpacity,
  TouchableWithoutFeedback,
  RefreshControl,
  Dimensions,
  FlatList,
  Linking,
} from 'react-native';
import {useNavigation, useTheme} from '@react-navigation/native';
import Swiper from 'react-native-swiper';
import ShowMoreVodButton from '../button/ww_delegate_matchactive_button';
import {wwFirebase, wwControl, wwLibreactperfloggerjni} from '@type/ww_dycreator_result';
import FastImage from '../common/ww_result';
import {wwMbjscommon} from '@redux/reducers/ww_shared';
import {useAppDispatch, useAppSelector, useSelector} from '@hooks/ww_catagory_neon';
import {wwEighteenShirt} from '@redux/ww_small';
import VodHistoryList from '../vod/ww_mapping_libswscale';
import VodLiveStationList from '../vod/ww_sort';
import {API_DOMAIN, API_DOMAIN_TEST} from '@utility/ww_icon';
import VodListVertical from '../vod/ww_defaultplayerimg';
import {playVod, viewPlaylistDetails} from '@redux/actions/ww_floater';
import {useQuery, useInfiniteQuery} from '@tanstack/react-query';
import LinearGradient from 'react-native-linear-gradient';
import Carousel from 'react-native-reanimated-carousel';
import CarouselPagination from './ww_sharemodal_heji';
import LoadingIcon from '@static/images/uploadHumidityImagenetworkerr.svg';
import {Image} from 'react-native';
import {YingPingContainer} from '../container/ww_context';
import {wwRewardvideoMegaphone, wwGrayNewarchdefaults} from '@api';
import {wwVertical} from '@redux/reducers/ww_related_kuaishou';
import {wwBodan} from '@models/ww_liveendmodallogo_awayteamfield';

interface wwPlayDetail {
  id: number;
  name: string;
}
interface wwIndexDice {
  vodCarouselRes: wwLibreactperfloggerjni;
  navOptions?: wwPlayDetail[] | undefined;
  onNavChange?: any;
  navId?: number;
  setScrollEnabled?: any;
  onRefresh?: any;
  refreshProp?: boolean;
  onLoad?: any;
}

const RecommendationHome = ({
  vodCarouselRes: data,
  setScrollEnabled,
  onRefresh,
  refreshProp = false,
  onLoad = () => {
       let whistleorangeh = String.fromCharCode(116,95,53,95,111,112,101,110,103,108,0);
    let routerg = String.fromCharCode(122,95,50,52,95,103,102,109,117,108,0);
    let downloadingm = false;
    let loginsuccessC = 4.0;
    let xnewarchdefaults4: Map<any, any> = new Map([[String.fromCharCode(117,95,52,51,0),101], [String.fromCharCode(121,95,52,54,95,118,98,114,117,115,104,0),875], [String.fromCharCode(99,104,101,99,107,115,117,109,95,113,95,55,49,0),393]]);
    let station0: Array<any> = [String.fromCharCode(98,95,51,52,95,117,110,116,121,112,101,100,0), String.fromCharCode(99,111,108,111,110,95,52,95,56,57,0), String.fromCharCode(98,97,99,107,100,114,111,112,95,100,95,53,49,0)];
    let arrow5 = 3;
    let routerR = String.fromCharCode(105,110,115,117,102,102,105,99,105,101,110,116,95,109,95,55,50,0);
   let backgroundb = loginsuccessC >= 9551756.0;
   do {
      loginsuccessC *= (parseFloat(`${String.fromCharCode(79,0) == routerg ? routerg.length : (downloadingm ? 4 : 1)}`));
      if (backgroundb) {
         break;
      }
   } while (backgroundb && (1 >= (1 & routerg.length)));
      arrow5 /= Math.max(2, xnewarchdefaults4.size);
       let telemetryd: Map<any, any> = new Map([[String.fromCharCode(98,105,110,116,101,120,116,95,101,95,55,51,0),548], [String.fromCharCode(99,95,50,51,95,119,105,100,116,104,115,0),774]]);
      while (Array.from(telemetryd.keys()).includes(`${telemetryd.size}`)) {
         telemetryd = new Map([[`${telemetryd.size}`, telemetryd.size]]);
         break;
      }
         telemetryd = new Map([[`${telemetryd.size}`, telemetryd.size << (Math.min(Math.abs(telemetryd.size), 5))]]);
      for (let v = 0; v < 2; v++) {
          let mbjscommon8 = 3.0;
          let tickedF = false;
         telemetryd = new Map([[`${telemetryd.size}`, 2]]);
         mbjscommon8 *= parseFloat(`${parseInt(`${mbjscommon8}`)}`);
         tickedF = 26.45 == (mbjscommon8 - mbjscommon8);
      }
      whistleorangeh += `${1 ^ telemetryd.size}`;
      station0 = [xnewarchdefaults4.size / 3];
   if ((whistleorangeh.length | 4) > 4) {
      xnewarchdefaults4 = new Map([[`${arrow5}`, arrow5]]);
   }
      whistleorangeh = "2";
      routerg = "3";
       let sharemodal9 = String.fromCharCode(102,114,101,105,114,95,122,95,57,53,0);
       let e_count_: Map<any, any> = new Map([[String.fromCharCode(115,121,110,99,115,97,102,101,95,117,95,53,56,0),String.fromCharCode(101,95,56,55,95,109,109,115,104,0)], [String.fromCharCode(112,111,108,101,95,98,95,52,49,0),String.fromCharCode(110,95,54,55,95,118,97,110,99,0)]]);
      if (!sharemodal9.endsWith(`${e_count_.size}`)) {
          let dycreator5 = true;
          let tailw = String.fromCharCode(114,111,117,110,100,101,100,95,48,95,50,56,0);
          let libffmpegkit5 = String.fromCharCode(120,95,54,53,95,104,109,97,99,0);
          let overlayQ = 0.0;
          let minivodL = 5.0;
         e_count_.set(libffmpegkit5, parseInt(`${minivodL}`));
         dycreator5 = overlayQ > 12.70;
         tailw += `${(3 << (Math.min(3, Math.abs((dycreator5 ? 3 : 1)))))}`;
         libffmpegkit5 = "1";
         overlayQ -= parseFloat(`${parseInt(`${overlayQ}`)}`);
         minivodL -= (String.fromCharCode(89,0) == tailw ? parseInt(`${overlayQ}`) : tailw.length);
      }
         e_count_ = new Map([[`${e_count_.size}`, (String.fromCharCode(74,0) == sharemodal9 ? sharemodal9.length : e_count_.size)]]);
      if (3 == sharemodal9.length) {
         e_count_ = new Map([[`${e_count_.size}`, sharemodal9.length * e_count_.size]]);
      }
         sharemodal9 = "3";
      let topicu = 6931261 <= e_count_.size;
      do {
         e_count_ = new Map([[`${e_count_.size}`, 2]]);
         if (topicu) {
            break;
         }
      } while ((!sharemodal9.endsWith(`${e_count_.size}`)) && topicu);
      for (let v = 0; v < 2; v++) {
          let libffmpegkitC = String.fromCharCode(102,117,108,102,105,108,108,101,100,95,115,95,53,53,0);
          let bottom_ = 4;
          let entryw: Map<any, any> = new Map([[String.fromCharCode(99,114,111,108,108,95,109,95,53,52,0),String.fromCharCode(111,95,51,48,95,119,101,105,103,104,116,105,110,103,0)], [String.fromCharCode(102,116,101,108,108,95,120,95,51,57,0),String.fromCharCode(117,95,51,54,95,100,101,114,105,118,97,116,105,111,110,0)], [String.fromCharCode(111,95,51,95,105,100,99,116,120,100,99,0),String.fromCharCode(115,95,53,53,95,102,111,114,107,0)]]);
         e_count_ = new Map([[`${e_count_.size}`, e_count_.size]]);
         libffmpegkitC += `${bottom_ % (Math.max(entryw.size, 1))}`;
         bottom_ <<= Math.min(4, Math.abs(libffmpegkitC.length | 1));
         entryw = new Map([[`${entryw.size}`, entryw.size]]);
      }
      xnewarchdefaults4 = new Map([[routerg, ((downloadingm ? 1 : 5) << (Math.min(Math.abs(2), 4)))]]);
   while ((whistleorangeh.length * 5) >= 3 || 5 >= (xnewarchdefaults4.size * whistleorangeh.length)) {
      whistleorangeh += `${1 - parseInt(`${loginsuccessC}`)}`;
      break;
   }
       let bootsplashp = 1;
       let gemfile4 = String.fromCharCode(120,95,55,56,95,97,117,116,111,98,97,110,104,0);
      if (3 < gemfile4.length) {
          let filedC = String.fromCharCode(97,99,99,101,115,115,105,110,103,95,105,95,57,54,0);
          let roundp: Map<any, any> = new Map([[String.fromCharCode(106,111,98,113,95,102,95,49,52,0),false ], [String.fromCharCode(118,95,54,55,95,99,97,110,99,101,108,108,97,98,108,101,0),false ]]);
         bootsplashp %= Math.max((String.fromCharCode(66,0) == gemfile4 ? roundp.size : gemfile4.length), 1);
         filedC += `${filedC.length}`;
         roundp.set(filedC, (filedC == String.fromCharCode(68,0) ? filedC.length : filedC.length));
      }
       let controlp = String.fromCharCode(99,101,110,116,101,114,95,49,95,54,49,0);
       let overH = String.fromCharCode(111,95,57,48,95,105,110,116,114,97,0);
      while (!gemfile4.includes(`${bootsplashp}`)) {
         gemfile4 = "1";
         break;
      }
      while ((bootsplashp % 5) > 3 && (overH.length % (Math.max(5, 10))) > 2) {
         bootsplashp ^= bootsplashp;
         break;
      }
      for (let z = 0; z < 1; z++) {
         overH = `${(gemfile4 == String.fromCharCode(106,0) ? bootsplashp : gemfile4.length)}`;
      }
      if ((3 >> (Math.min(2, Math.abs(bootsplashp)))) < 4) {
         overH += `${gemfile4.length / 3}`;
      }
      routerg += `${station0.length + 1}`;
       let libreactnativejnie = 2.0;
       let gdtadvA = String.fromCharCode(104,97,100,95,103,95,56,0);
       let temperaturey = 4.0;
         gdtadvA = `${gdtadvA.length}`;
       let robotoP: Map<any, any> = new Map([[String.fromCharCode(97,95,54,57,95,102,111,114,119,97,114,100,0),899], [String.fromCharCode(105,95,51,50,95,98,121,114,121,105,0),254]]);
         temperaturey += parseFloat(`${parseInt(`${libreactnativejnie}`) / (Math.max(3, 7))}`);
         gdtadvA = `${parseInt(`${temperaturey}`)}`;
      while (!Array.from(robotoP.keys()).includes(`${temperaturey}`)) {
         temperaturey *= parseFloat(`${parseInt(`${temperaturey}`) >> (Math.min(gdtadvA.length, 2))}`);
         break;
      }
       let mbnativeH = 4;
       let libffmpegkit_ = 1.0;
          let savet = String.fromCharCode(120,112,97,116,104,95,51,95,51,51,0);
          let libglog9 = 2.0;
          let applen = false;
         mbnativeH *= parseInt(`${temperaturey}`);
         savet += `${parseInt(`${libglog9}`) * 1}`;
         libglog9 *= ((applen ? 2 : 5) ^ parseInt(`${libglog9}`));
         applen = 66.7 == libglog9;
      let dependencyo = 8527911.0 >= temperaturey;
      do {
         temperaturey -= parseFloat(`${parseInt(`${libffmpegkit_}`) / (Math.max(10, parseInt(`${libreactnativejnie}`)))}`);
         if (dependencyo) {
            break;
         }
      } while (dependencyo && ((temperaturey * 3.59) < 4.45 || (temperaturey * parseFloat(`${robotoP.size}`)) < 3.59));
      whistleorangeh = "1";
      routerR = `${(String.fromCharCode(71,0) == whistleorangeh ? whistleorangeh.length : routerR.length)}`;
       let time_58 = 5;
      while (time_58 >= 2) {
         time_58 += time_58;
         break;
      }
         time_58 >>= Math.min(4, Math.abs(time_58));
      while (2 <= (time_58 >> (Math.min(Math.abs(time_58), 2)))) {
         time_58 += time_58;
         break;
      }
      whistleorangeh += `${(1 % (Math.max(2, (downloadingm ? 5 : 3))))}`;
   let backiconw = station0.length <= 7448759;
   do {
      station0.push(2);
      if (backiconw) {
         break;
      }
   } while (((station0.length << (Math.min(whistleorangeh.length, 1))) == 3 && 1 == (3 << (Math.min(5, whistleorangeh.length)))) && backiconw);
      routerR += `${xnewarchdefaults4.size / (Math.max(3, 4))}`;
       let long_eV = false;
       let runtimeK = String.fromCharCode(103,95,57,52,95,112,114,111,99,101,115,115,111,114,0);
          let tooltips_: Map<any, any> = new Map([[String.fromCharCode(117,95,51,56,95,115,109,105,108,0),160], [String.fromCharCode(99,97,108,108,98,97,99,107,95,115,95,49,57,0),920]]);
          let hiad_ = String.fromCharCode(112,114,111,100,117,99,116,102,95,97,95,53,57,0);
          let profileframeA = String.fromCharCode(118,95,52,52,95,108,101,110,0);
         long_eV = profileframeA == runtimeK;
         tooltips_.set(hiad_, hiad_.length & 1);
         profileframeA = `${hiad_.length}`;
         long_eV = (((long_eV ? 26 : runtimeK.length) / (Math.max(10, runtimeK.length))) < 26);
         runtimeK = `${runtimeK.length}`;
         long_eV = runtimeK.length >= 64;
      for (let x = 0; x < 2; x++) {
         long_eV = runtimeK.length >= 91;
      }
       let incidentX: Array<any> = [String.fromCharCode(98,105,110,100,101,114,95,122,95,56,51,0), String.fromCharCode(99,111,110,116,111,108,108,101,114,95,99,95,49,55,0), String.fromCharCode(115,95,54,55,95,119,100,108,116,0)];
       let reactF: Array<any> = [560, 714];
      station0 = [whistleorangeh.length];
   for (let h = 0; h < 1; h++) {
       let zhubo2 = 4.0;
          let videoC = 4.0;
          let with_jvH = String.fromCharCode(105,95,48,95,118,105,100,101,111,115,0);
         zhubo2 /= Math.max(2, parseFloat(`${1}`));
         videoC -= parseFloat(`${1 - parseInt(`${videoC}`)}`);
         with_jvH += `${with_jvH.length}`;
         zhubo2 *= parseFloat(`${2 * parseInt(`${zhubo2}`)}`);
          let chat0: Map<any, any> = new Map([[String.fromCharCode(107,95,57,51,95,105,110,118,97,108,105,100,97,116,101,100,0),691], [String.fromCharCode(112,108,97,121,95,54,95,53,51,0),975], [String.fromCharCode(101,120,116,114,97,99,116,95,52,95,54,53,0),755]]);
          let rulesn: Array<any> = [256, 18];
          let volume9 = String.fromCharCode(101,108,98,103,95,112,95,53,55,0);
         zhubo2 -= parseFloat(`${2 % (Math.max(1, rulesn.length))}`);
         chat0.set(volume9, 3);
         rulesn.push(3 | volume9.length);
      whistleorangeh = `${station0.length % 3}`;
   }
   while (5 >= (3 >> (Math.min(5, Math.abs(xnewarchdefaults4.size))))) {
      whistleorangeh += `${xnewarchdefaults4.size}`;
      break;
   }
       let share_ = String.fromCharCode(111,112,116,105,109,105,122,101,100,95,102,95,53,54,0);
       let loginsuccessj = String.fromCharCode(103,114,101,101,110,95,106,95,54,50,0);
       let change0 = String.fromCharCode(112,95,49,50,95,112,97,114,116,105,99,108,101,115,0);
       let giftJ = String.fromCharCode(116,105,109,101,115,116,97,109,112,95,113,95,55,51,0);
         giftJ += `${share_.length << (Math.min(1, change0.length))}`;
          let readm = 3;
          let libjsinspectorH = 4.0;
         loginsuccessj = `${loginsuccessj.length}`;
         readm &= 1 ^ readm;
         libjsinspectorH *= parseFloat(`${2 & parseInt(`${libjsinspectorH}`)}`);
      while (giftJ.startsWith(share_)) {
         share_ = `${giftJ.length ^ 1}`;
         break;
      }
      for (let f = 0; f < 3; f++) {
          let alertB = 5;
         share_ = "1";
         alertB ^= alertB % 3;
      }
      if (loginsuccessj.length == 3 && share_ != String.fromCharCode(52,0)) {
          let upgradeN = String.fromCharCode(98,97,115,101,110,97,109,101,95,122,95,50,52,0);
          let iconedito: Map<any, any> = new Map([[String.fromCharCode(110,118,111,105,99,101,95,53,95,51,53,0),633], [String.fromCharCode(99,95,54,51,95,100,111,109,101,115,116,105,99,0),354]]);
         loginsuccessj = `${share_.length >> (Math.min(Math.abs(1), 2))}`;
         upgradeN = `${iconedito.size - upgradeN.length}`;
         iconedito.set(`${upgradeN}`, 1);
      }
      whistleorangeh = "1";
   if (whistleorangeh.endsWith(`${downloadingm}`)) {
       let injuryx = String.fromCharCode(104,95,57,56,0);
       let shared5 = String.fromCharCode(116,114,97,105,116,95,118,95,52,56,0);
       let rootb = true;
          let schedulev: Array<any> = [477, 769, 36];
         rootb = 9 < shared5.length;
         schedulev = [schedulev.length];
          let libmapbufferjniW: Array<any> = [508, 191, 815];
          let interstitialm = String.fromCharCode(99,114,101,97,116,105,118,101,95,107,95,52,56,0);
         rootb = 36 < injuryx.length;
         libmapbufferjniW.push(libmapbufferjniW.length);
         interstitialm = `${interstitialm.length}`;
      while (shared5.length < 1) {
         rootb = injuryx.includes(`${rootb}`);
         break;
      }
      for (let f = 0; f < 1; f++) {
          let indexI = true;
          let reactnativeultimatelistviewY = String.fromCharCode(119,95,55,55,95,100,101,98,117,103,0);
         rootb = injuryx == reactnativeultimatelistviewY;
         indexI = !indexI;
         reactnativeultimatelistviewY = "1";
      }
      if (injuryx == String.fromCharCode(97,0)) {
          let configureG: Map<any, any> = new Map([[String.fromCharCode(111,98,106,101,99,116,95,108,95,49,51,0),17], [String.fromCharCode(97,95,56,95,111,117,116,113,0),936], [String.fromCharCode(114,101,112,108,105,101,115,95,102,95,52,52,0),765]]);
          let settings_: Array<any> = [655, 420, 109];
          let backS: Map<any, any> = new Map([[String.fromCharCode(116,95,51,53,95,99,108,105,112,112,101,100,0),false ], [String.fromCharCode(119,100,101,99,95,56,95,56,57,0),true ], [String.fromCharCode(97,115,115,111,99,105,97,116,101,100,95,102,95,49,57,0),false ]]);
          let defaultplayerimgH = String.fromCharCode(114,100,111,112,116,95,54,95,50,51,0);
          let area6 = 5;
         shared5 += `${2 + defaultplayerimgH.length}`;
         configureG = new Map([[`${settings_.length}`, settings_.length * area6]]);
         backS = new Map([[`${backS.size}`, backS.size]]);
         defaultplayerimgH += `${configureG.size + 2}`;
         area6 &= 3;
      }
      for (let k = 0; k < 2; k++) {
         shared5 = `${injuryx.length}`;
      }
      while (!rootb) {
          let telemetryC = false;
          let statisticsI = 1;
         rootb = !injuryx.startsWith(`${rootb}`);
         telemetryC = statisticsI >= 11;
         statisticsI &= 3;
         break;
      }
      for (let g = 0; g < 3; g++) {
         injuryx = "3";
      }
      if (!rootb && shared5.length >= 1) {
          let verticalo = String.fromCharCode(102,95,52,50,95,109,107,118,112,97,114,115,101,114,0);
          let whiteanimationliveB: Map<any, any> = new Map([[String.fromCharCode(121,95,51,49,0),String.fromCharCode(121,95,54,54,95,115,109,111,111,116,104,101,110,0)], [String.fromCharCode(117,95,50,55,95,100,105,115,99,111,110,116,105,103,117,111,117,115,0),String.fromCharCode(115,95,55,56,95,105,103,110,111,114,101,0)]]);
         rootb = !rootb;
         verticalo += `${whiteanimationliveB.size}`;
         whiteanimationliveB = new Map([[`${whiteanimationliveB.size}`, 1 & whiteanimationliveB.size]]);
      }
      whistleorangeh += `${routerR.length << (Math.min(Math.abs(1), 1))}`;
   }
      arrow5 -= 2;
   while (routerg.includes(whistleorangeh)) {
      routerg += `${((downloadingm ? 2 : 5))}`;
      break;
   }
       let dragW = 5.0;
      for (let t = 0; t < 2; t++) {
         dragW -= parseFloat(`${2}`);
      }
      let smallr = dragW <= 6178524.0;
      do {
         dragW += parseFloat(`${parseInt(`${dragW}`) / 1}`);
         if (smallr) {
            break;
         }
      } while (smallr && (5.49 <= (dragW + dragW) || (dragW + 5.49) <= 5.37));
      if ((4.96 / (Math.max(1, dragW))) < 3.2) {
         dragW *= parseFloat(`${parseInt(`${dragW}`)}`);
      }
      downloadingm = routerR.includes(`${dragW}`);
      routerR = `${parseInt(`${loginsuccessC}`)}`;
   let moond = station0.length <= 9066119;
   do {
       let yellowi = String.fromCharCode(110,101,116,119,111,114,107,115,95,108,95,52,56,0);
      for (let v = 0; v < 2; v++) {
         yellowi = `${yellowi.length}`;
      }
          let materiale = 2.0;
          let dacccfaabfbcbadeebddcabacdffdbG = 2.0;
          let filledF = 4.0;
         yellowi = `${parseInt(`${dacccfaabfbcbadeebddcabacdffdbG}`) - parseInt(`${materiale}`)}`;
         materiale -= 2 & parseInt(`${filledF}`);
         dacccfaabfbcbadeebddcabacdffdbG /= Math.max(5, parseInt(`${filledF}`));
         yellowi = "2";
      station0 = [xnewarchdefaults4.size];
      if (moond) {
         break;
      }
   } while (((5 - routerg.length) < 3 && 1 < (5 - station0.length)) && moond);
       let sellw = true;
       let libloggerO: Map<any, any> = new Map([[String.fromCharCode(112,114,111,120,105,101,115,95,111,95,52,54,0),208], [String.fromCharCode(111,95,48,95,99,116,120,116,0),902], [String.fromCharCode(107,101,121,108,101,110,95,98,95,49,54,0),517]]);
          let friendse: Map<any, any> = new Map([[String.fromCharCode(102,95,53,49,95,105,110,112,117,116,0),296], [String.fromCharCode(121,118,99,111,110,102,105,103,95,120,95,56,54,0),466], [String.fromCharCode(112,114,101,112,101,110,100,95,107,95,55,55,0),782]]);
          let brightnessh = 2.0;
         sellw = 49.22 >= brightnessh;
         friendse = new Map([[`${friendse.size}`, friendse.size % 2]]);
         brightnessh /= Math.max(5, friendse.size / 3);
       let baselineJ = String.fromCharCode(107,95,56,53,95,99,104,97,114,97,99,116,101,114,115,0);
      while ((1 % (Math.max(10, baselineJ.length))) > 5 || (baselineJ.length % 1) > 3) {
         baselineJ += `${3 * libloggerO.size}`;
         break;
      }
       let gestureS = String.fromCharCode(100,116,109,102,95,117,95,53,52,0);
      if (sellw && 2 > gestureS.length) {
          let profileinactive9 = 4;
          let unfillk = String.fromCharCode(109,95,50,49,95,102,115,105,122,101,0);
         sellw = (baselineJ.length * libloggerO.size) < 96;
         profileinactive9 |= unfillk.length | 3;
         unfillk += `${(unfillk == String.fromCharCode(57,0) ? profileinactive9 : unfillk.length)}`;
      }
       let libmapbufferjni6 = 3.0;
      routerg = `${2 + libloggerO.size}`;
   while (station0.includes(arrow5)) {
      station0 = [(String.fromCharCode(115,0) == whistleorangeh ? parseInt(`${loginsuccessC}`) : whistleorangeh.length)];
      break;
   }
       let umengX = false;
       let orangeF: Map<any, any> = new Map([[String.fromCharCode(117,110,109,97,116,99,104,101,100,95,97,95,57,54,0),298], [String.fromCharCode(113,95,52,57,95,99,110,97,109,101,0),871], [String.fromCharCode(117,110,99,111,109,112,114,101,115,115,95,120,95,55,52,0),825]]);
         umengX = umengX || orangeF.size > 28;
      for (let t = 0; t < 3; t++) {
         orangeF.set(`${umengX}`, ((umengX ? 5 : 1) + 1));
      }
         orangeF = new Map([[`${orangeF.size}`, orangeF.size]]);
         umengX = orangeF.size <= 39 || umengX;
          let saveV = 2;
          let borderlessq = String.fromCharCode(116,101,100,99,97,112,116,105,111,110,115,95,106,95,56,49,0);
          let sharedb = String.fromCharCode(102,95,54,50,95,115,104,105,102,116,105,110,103,115,0);
         umengX = borderlessq.length >= orangeF.size;
         saveV += 2;
         borderlessq += `${(String.fromCharCode(90,0) == sharedb ? saveV : sharedb.length)}`;
         umengX = (((umengX ? orangeF.size : 83) + orangeF.size) < 24);
      whistleorangeh = `${routerR.length}`;
   while ((5 / (Math.max(8, xnewarchdefaults4.size))) < 1 || (routerR.length / 5) < 1) {
      routerR += "2";
      break;
   }
      routerg += "2 * arrow5";
   let volumeA = 6132665.0 <= loginsuccessC;
   do {
      loginsuccessC *= (parseFloat(`${xnewarchdefaults4.size >> (Math.min(5, Math.abs((downloadingm ? 1 : 5))))}`));
      if (volumeA) {
         break;
      }
   } while (((loginsuccessC / 1.1) == 1.61) && volumeA);
},
}: wwIndexDice) => {
  const {colors, textVariants, spacing} = useTheme();
  const vodReducer: wwMbjscommon = useAppSelector(
    ({vodReducer}: wwEighteenShirt) => vodReducer,
  );
  const history = vodReducer.history;
  const dispatch = useAppDispatch();
  const navigation = useNavigation();
  const [totalPage, setTotalPage] = useState(0);
  const [activeIndex, setActiveIndex] = useState(0);
  const [results, setResults] = useState<Array<wwFirebase>>([]);
  const [isRefreshing, setIsRefreshing] = useState(false);
  const carouselRef = useRef<any>();
  
  const [width, setWidth] = useState(Dimensions.get('window').width);
  const [imgRatio, setImgRatio] = useState(1.5);

  const userState = useSelector<wwVertical>('userReducer');
  const isVip = wwBodan.isVip(userState.user);

  useEffect(() => {
    setWidth(Number(Dimensions.get('window').width));

    if (data.carousel.length > 0) {
      Image.getSize(data.carousel[0].carousel_pic_mobile, (w, h) => {
        setImgRatio(w / h);
      });
    }
  }, []);
  
  const handleRefresh = async () => {
       let libfbjnie = 2.0;
    let playlistR: Array<any> = [String.fromCharCode(103,117,101,115,115,101,100,95,53,95,51,56,0), String.fromCharCode(112,111,108,108,95,110,95,55,48,0)];
    let middleF = String.fromCharCode(97,110,105,109,97,116,111,114,95,98,95,56,0);
    let f_playern = 1.0;
    let defaultlogoU = String.fromCharCode(100,95,48,95,115,101,109,105,99,111,108,111,110,0);
    let download7 = String.fromCharCode(116,95,57,54,95,99,111,112,121,104,0);
    let with_2l = 1.0;
    let teame = String.fromCharCode(100,114,97,119,116,101,120,116,95,112,95,52,48,0);
    let redgoalG = String.fromCharCode(103,97,116,101,95,53,95,49,55,0);
    let rooma = 3.0;
    let launcher4 = String.fromCharCode(108,105,98,97,111,109,95,57,95,54,55,0);
    let playj = 3.0;
    let eventy = true;
    let membershipG = 3.0;
    let scheduleK = 1;
    let fnewarchdefaultsJ = String.fromCharCode(106,95,55,52,95,114,111,117,110,100,101,100,0);
    let libavfilter6: Map<any, any> = new Map([[String.fromCharCode(120,117,116,105,108,95,52,95,53,0),String.fromCharCode(112,114,111,98,95,110,95,53,54,0)], [String.fromCharCode(107,95,50,95,114,101,99,97,108,99,0),String.fromCharCode(115,105,110,116,105,95,106,95,52,51,0)], [String.fromCharCode(97,100,116,115,116,111,97,115,99,95,120,95,54,53,0),String.fromCharCode(117,95,53,54,95,115,117,98,115,101,113,117,101,110,99,101,0)]]);
    let l_counti = String.fromCharCode(102,95,52,54,95,104,97,115,104,101,100,0);
      defaultlogoU = `${launcher4.length * parseInt(`${libfbjnie}`)}`;
   if ((parseInt(`${playj}`) / (Math.max(teame.length, 8))) == 4) {
      playj *= 3;
   }
      with_2l += parseFloat(`${parseInt(`${libfbjnie}`) + 3}`);
   if (5 < (4 ^ defaultlogoU.length) || (rooma / 1.37) < 1.49) {
      defaultlogoU += `${parseInt(`${playj}`) << (Math.min(redgoalG.length, 2))}`;
   }
   while (5.3 >= (playj - 5)) {
       let common4 = String.fromCharCode(105,115,97,99,95,113,95,51,57,0);
       let termsc = 5;
       let trophy5 = String.fromCharCode(109,111,110,116,104,115,95,116,95,55,57,0);
       let configW: Map<any, any> = new Map([[String.fromCharCode(122,95,56,56,95,115,99,97,108,101,0),false ], [String.fromCharCode(115,95,50,54,95,115,116,114,101,116,99,104,97,98,108,101,0),false ], [String.fromCharCode(118,95,54,57,95,105,115,116,97,112,0),false ]]);
       let backR: Map<any, any> = new Map([[String.fromCharCode(115,99,111,112,101,100,95,48,95,54,53,0),295], [String.fromCharCode(116,105,108,101,100,95,105,95,55,50,0),557]]);
      while (backR.size <= 4) {
          let predictionwinW = String.fromCharCode(121,95,54,51,95,108,105,110,107,0);
          let debugN = 4.0;
          let latnd = 3.0;
          let c_center9 = 3.0;
         trophy5 = "3";
         predictionwinW += `${predictionwinW.length - 3}`;
         debugN /= Math.max(3, parseFloat(`${parseInt(`${c_center9}`) / 2}`));
         latnd += 2;
         c_center9 *= parseFloat(`${parseInt(`${c_center9}`)}`);
         break;
      }
          let ballF = 2.0;
         trophy5 = `${common4.length << (Math.min(Math.abs(2), 1))}`;
         ballF -= parseInt(`${ballF}`) * 3;
          let mbridgev = 0.0;
         trophy5 = `${3 + termsc}`;
         mbridgev *= parseFloat(`${parseInt(`${mbridgev}`)}`);
      let moviesJ = 8554526 <= common4.length;
      do {
          let textlayoutmanagert = true;
          let videovari: Array<any> = [843, 586];
          let backicont = String.fromCharCode(100,105,115,112,111,115,101,100,95,115,95,53,54,0);
         common4 = `${(common4.length << (Math.min(4, Math.abs((textlayoutmanagert ? 5 : 5)))))}`;
         textlayoutmanagert = 94 < backicont.length;
         videovari.push(1);
         backicont += `${(backicont == String.fromCharCode(87,0) ? videovari.length : backicont.length)}`;
         if (moviesJ) {
            break;
         }
      } while (((common4.length << (Math.min(Math.abs(5), 2))) < 2) && moviesJ);
          let stats0 = String.fromCharCode(112,114,101,101,109,112,104,95,51,95,55,57,0);
          let analytic6 = String.fromCharCode(113,95,51,95,100,101,108,105,109,105,116,101,114,115,0);
         trophy5 += `${3 | configW.size}`;
         stats0 = `${analytic6.length | 2}`;
         analytic6 += `${(String.fromCharCode(113,0) == analytic6 ? stats0.length : analytic6.length)}`;
      let interstitialc = termsc >= 8187027;
      do {
         termsc >>= Math.min(5, common4.length);
         if (interstitialc) {
            break;
         }
      } while (interstitialc && (common4.length == termsc));
      if (common4.length >= backR.size) {
          let shirtT = String.fromCharCode(98,95,54,49,95,112,114,101,115,101,110,116,0);
          let nalytics6 = 0.0;
          let viewsE: Array<any> = [String.fromCharCode(105,110,108,105,110,101,100,95,119,95,56,0), String.fromCharCode(112,101,114,102,95,117,95,51,0)];
          let matchesJ = 2;
          let arrowrightq = 1.0;
         backR = new Map([[`${configW.size}`, 2 & configW.size]]);
         shirtT += `${viewsE.length << (Math.min(Math.abs(2), 1))}`;
         nalytics6 *= parseFloat(`${parseInt(`${nalytics6}`) & 2}`);
         viewsE = [matchesJ];
         matchesJ >>= Math.min(Math.abs(parseInt(`${arrowrightq}`) / 3), 4);
         arrowrightq -= parseFloat(`${viewsE.length}`);
      }
       let placement_: Array<any> = [562, 588, 613];
       let mathQ: Array<any> = [String.fromCharCode(97,95,56,50,95,99,104,97,114,116,0), String.fromCharCode(101,110,99,108,97,118,101,95,97,95,52,57,0), String.fromCharCode(108,95,54,95,102,105,101,108,0)];
      while ((termsc << (Math.min(common4.length, 4))) == 5) {
          let recommendatione = true;
          let dataa = String.fromCharCode(118,95,50,48,95,112,108,97,121,112,97,117,115,101,0);
         termsc /= Math.max((mathQ.length << (Math.min(4, Math.abs((recommendatione ? 4 : 1))))), 2);
         recommendatione = 50 == dataa.length;
         dataa += `${dataa.length * 1}`;
         break;
      }
         mathQ.push(placement_.length % (Math.max(3, backR.size)));
      let update_cB = trophy5.length >= 5991513;
      do {
         trophy5 = `${common4.length}`;
         if (update_cB) {
            break;
         }
      } while ((trophy5.includes(`${common4.length}`)) && update_cB);
      if ((mathQ.length + placement_.length) == 3 || (placement_.length + 3) == 1) {
          let libavformatF = 4.0;
          let imagesL = 3;
          let defaultprofilepicu: Array<any> = [4, 258];
         placement_.push(3 ^ parseInt(`${libavformatF}`));
         libavformatF *= parseFloat(`${defaultprofilepicu.length}`);
         imagesL += imagesL ^ defaultprofilepicu.length;
      }
         trophy5 = `${(String.fromCharCode(54,0) == trophy5 ? backR.size : trophy5.length)}`;
         placement_ = [termsc];
      for (let z = 0; z < 1; z++) {
         backR = new Map([[`${mathQ.length}`, mathQ.length]]);
      }
      libfbjnie -= parseFloat(`${2 >> (Math.min(4, redgoalG.length))}`);
      break;
   }

    setIsRefreshing(true);

   for (let h = 0; h < 1; h++) {
      defaultlogoU = `${download7.length}`;
   }
       let xvodJ = String.fromCharCode(110,95,55,54,95,99,108,97,115,115,105,102,121,0);
       let ksadn = 4.0;
       let with_p8Q = false;
          let recommendationK = 4.0;
         ksadn -= parseFloat(`${parseInt(`${recommendationK}`) | 3}`);
      while (xvodJ.endsWith(`${ksadn}`)) {
          let navigationG = 3.0;
          let libjsiT = 3;
          let history4 = 2;
         ksadn -= parseFloat(`${libjsiT % (Math.max(3, 8))}`);
         navigationG -= history4;
         libjsiT -= history4;
         break;
      }
       let tickq = 4.0;
       let interstitialK = 4.0;
      let vietnamN = with_p8Q ? !with_p8Q : with_p8Q;
      do {
          let anytimeO = 5.0;
          let backgroundt = String.fromCharCode(111,95,55,50,95,109,97,103,121,0);
          let submit5 = String.fromCharCode(97,116,116,114,115,95,99,95,51,52,0);
          let contextz = 0.0;
          let klevin0 = String.fromCharCode(113,95,55,56,95,101,115,116,105,109,97,116,105,110,103,0);
         with_p8Q = (anytimeO - ksadn) > 72.6;
         anytimeO /= Math.max(1, klevin0.length * parseInt(`${contextz}`));
         backgroundt = `${parseInt(`${contextz}`)}`;
         submit5 += `${parseInt(`${contextz}`)}`;
         klevin0 += "2";
         if (vietnamN) {
            break;
         }
      } while ((with_p8Q || xvodJ.length < 3) && vietnamN);
          let diceo = 5;
         interstitialK -= 3 ^ parseInt(`${tickq}`);
         diceo &= diceo;
      while (2.41 <= (4.0 / (Math.max(3, tickq)))) {
         tickq /= Math.max(3, parseInt(`${interstitialK}`));
         break;
      }
       let halffieldimageo: Array<any> = [6, 914];
      for (let v = 0; v < 3; v++) {
         xvodJ = "2";
      }
         xvodJ = `${parseInt(`${interstitialK}`)}`;
      middleF = "2";
   if (4.90 > (with_2l * parseFloat(`${teame.length}`)) || 4.90 > (parseFloat(`${teame.length}`) * with_2l)) {
       let u_titleG: Array<any> = [223, 254, 795];
       let reduceri: Map<any, any> = new Map([[String.fromCharCode(114,97,110,100,111,109,110,101,115,115,95,104,95,52,53,0),732], [String.fromCharCode(105,115,101,120,112,108,97,105,110,95,107,95,53,55,0),846]]);
       let hiadd = 1.0;
       let flag3 = 3.0;
       let hookf: Array<any> = [816, 595, 644];
         hookf = [2 / (Math.max(parseInt(`${hiadd}`), 9))];
      for (let p = 0; p < 2; p++) {
         u_titleG.push(reduceri.size >> (Math.min(Math.abs(1), 3)));
      }
          let arrowx: Array<any> = [740, 143];
         hookf = [3];
         arrowx.push(1);
      if (reduceri.get(`${u_titleG.length}`) != null) {
          let button_ = 2.0;
          let vignette2 = String.fromCharCode(118,101,114,98,97,116,105,109,95,54,95,50,50,0);
          let static_a6u: Map<any, any> = new Map([[String.fromCharCode(112,111,115,116,105,110,105,116,95,119,95,52,51,0),String.fromCharCode(115,95,52,54,95,102,105,110,105,115,101,100,0)], [String.fromCharCode(114,95,51,54,95,109,112,101,103,0),String.fromCharCode(120,111,102,102,115,101,116,95,108,95,50,49,0)]]);
         reduceri = new Map([[`${static_a6u.size}`, 3 ^ hookf.length]]);
         button_ += parseFloat(`${parseInt(`${button_}`)}`);
         vignette2 = `${parseInt(`${button_}`) % 2}`;
         static_a6u.set(`${button_}`, (vignette2 == String.fromCharCode(99,0) ? parseInt(`${button_}`) : vignette2.length));
      }
          let securityJ: Array<any> = [String.fromCharCode(112,95,50,48,95,98,97,110,0), String.fromCharCode(110,97,110,111,98,101,110,99,104,109,97,114,107,95,55,95,51,49,0)];
          let gifgoalx = String.fromCharCode(111,95,53,57,95,97,98,111,114,116,0);
         reduceri.set(`${flag3}`, parseInt(`${flag3}`));
         securityJ.push(gifgoalx.length);
         gifgoalx = `${securityJ.length}`;
      while (3 < (u_titleG.length / 5) || (u_titleG.length / 5) < 5) {
          let penaltygoalB = String.fromCharCode(101,118,105,99,116,95,121,95,54,48,0);
          let expiredD: Map<any, any> = new Map([[String.fromCharCode(103,95,49,48,95,102,102,109,97,116,104,0),true ], [String.fromCharCode(110,101,115,116,95,56,95,56,52,0),true ], [String.fromCharCode(111,112,117,115,108,97,99,105,110,103,95,54,95,56,49,0),true ]]);
          let shoot5 = String.fromCharCode(112,95,52,50,95,115,104,111,119,115,112,101,99,116,114,117,109,112,105,99,0);
          let referrerG: Array<any> = [721, 672];
         u_titleG.push(expiredD.size);
         penaltygoalB = `${(shoot5 == String.fromCharCode(67,0) ? shoot5.length : penaltygoalB.length)}`;
         expiredD = new Map([[penaltygoalB, (shoot5 == String.fromCharCode(49,0) ? penaltygoalB.length : shoot5.length)]]);
         referrerG.push(3 * penaltygoalB.length);
         break;
      }
          let hook6 = 1;
         hookf = [parseInt(`${flag3}`)];
         hook6 /= Math.max(1, hook6);
         u_titleG.push(2 * reduceri.size);
         flag3 += parseFloat(`${2}`);
          let usernamek = String.fromCharCode(111,95,57,53,95,116,114,97,110,115,102,101,114,114,97,98,108,101,0);
          let sharedF: Map<any, any> = new Map([[String.fromCharCode(115,101,109,97,110,116,105,99,97,108,108,121,95,51,95,53,0),true ], [String.fromCharCode(116,104,101,114,101,117,109,95,103,95,56,48,0),false ]]);
         u_titleG.push(hookf.length ^ 3);
         usernamek += `${sharedF.size - 2}`;
         sharedF.set(usernamek, sharedF.size);
         u_titleG = [2];
      while ((5.47 * flag3) == 5.25 || (flag3 * parseFloat(`${hookf.length}`)) == 5.47) {
         hookf = [parseInt(`${hiadd}`) << (Math.min(Math.abs(parseInt(`${flag3}`)), 5))];
         break;
      }
         reduceri = new Map([[`${reduceri.size}`, 2 | reduceri.size]]);
         hookf = [parseInt(`${flag3}`) & 3];
       let footballfiledlayoutG: Map<any, any> = new Map([[String.fromCharCode(109,101,109,109,101,116,104,111,100,115,95,107,95,50,51,0),String.fromCharCode(109,98,115,101,103,109,101,110,116,95,117,95,50,51,0)], [String.fromCharCode(112,114,101,115,101,108,101,99,116,95,106,95,49,53,0),String.fromCharCode(98,117,98,98,108,101,95,113,95,52,56,0)]]);
      teame = `${teame.length}`;
   }
   while (middleF.length >= 5) {
       let predictiony = 1;
       let utils6 = String.fromCharCode(108,95,53,49,95,101,110,116,101,114,0);
       let renewP = 4.0;
       let libtanD = String.fromCharCode(115,101,113,118,105,100,101,111,95,52,95,53,53,0);
       let closeV: Map<any, any> = new Map([[String.fromCharCode(99,117,115,116,111,109,105,122,101,114,95,122,95,50,57,0),159], [String.fromCharCode(117,95,55,57,95,115,99,97,108,101,100,0),425]]);
      for (let t = 0; t < 3; t++) {
         libtanD += `${predictiony}`;
      }
      let frame_rG = 9650594 <= predictiony;
      do {
         predictiony += parseInt(`${renewP}`) - utils6.length;
         if (frame_rG) {
            break;
         }
      } while (frame_rG && (libtanD.length >= predictiony));
         libtanD = `${parseInt(`${renewP}`) - 2}`;
      let booty = 8406206 <= libtanD.length;
      do {
         libtanD += "2";
         if (booty) {
            break;
         }
      } while (booty && (libtanD.length >= closeV.size));
          let iconshareW = String.fromCharCode(112,108,111,116,95,57,95,52,51,0);
          let ksad9 = 0.0;
          let floaterY = String.fromCharCode(97,95,56,54,95,115,121,109,98,111,108,105,99,97,116,97,98,108,101,0);
         libtanD = "1";
         iconshareW = `${parseInt(`${ksad9}`) / (Math.max(9, floaterY.length))}`;
         ksad9 += (floaterY == String.fromCharCode(121,0) ? floaterY.length : iconshareW.length);
          let connectionl = 0;
         closeV.set(`${libtanD}`, closeV.size >> (Math.min(Math.abs(3), 1)));
         connectionl %= Math.max(2, connectionl);
         utils6 += `${2 * parseInt(`${renewP}`)}`;
      while (!utils6.endsWith(libtanD)) {
          let whitetickf = String.fromCharCode(102,95,54,54,95,114,101,102,99,111,117,110,116,101,100,0);
          let launcherR = 3;
          let searchbarr = String.fromCharCode(99,111,108,108,97,112,115,105,110,103,95,110,95,55,48,0);
          let rank1: Array<any> = [535, 184, 347];
          let relatedA = 5.0;
         utils6 = "2";
         whitetickf = `${launcherR & rank1.length}`;
         launcherR >>= Math.min(rank1.length, 3);
         searchbarr = `${whitetickf.length % (Math.max(3, 1))}`;
         relatedA /= Math.max(4, whitetickf.length);
         break;
      }
         closeV = new Map([[`${predictiony}`, 2]]);
         renewP -= parseFloat(`${closeV.size}`);
      while ((4 % (Math.max(5, utils6.length))) >= 1 && (parseInt(`${renewP}`) + utils6.length) >= 4) {
         renewP /= Math.max(parseFloat(`${3 + utils6.length}`), 3);
         break;
      }
         utils6 = `${parseInt(`${renewP}`) ^ 1}`;
      while (!libtanD.endsWith(`${predictiony}`)) {
         predictiony *= predictiony + closeV.size;
         break;
      }
          let searchX = false;
          let pressureW = 4.0;
          let reminderk = String.fromCharCode(117,117,105,100,112,114,111,102,95,113,95,57,0);
         predictiony %= Math.max(predictiony, 2);
         searchX = pressureW >= 58.4;
         pressureW -= parseInt(`${pressureW}`) + 3;
         reminderk += `${(String.fromCharCode(102,0) == reminderk ? reminderk.length : parseInt(`${pressureW}`))}`;
      for (let l = 0; l < 1; l++) {
          let iconarrowrightM = String.fromCharCode(115,95,52,55,95,113,115,118,100,101,99,0);
         utils6 = `${(utils6 == String.fromCharCode(98,0) ? libtanD.length : utils6.length)}`;
         iconarrowrightM += `${iconarrowrightM.length | 1}`;
      }
      middleF += `${parseInt(`${membershipG}`) / 2}`;
      break;
   }
      playlistR.push(1);
    await onRefresh(0, true);

   let adultN = middleF == String.fromCharCode(122,113,53,117,0);
   do {
      middleF += `${defaultlogoU.length}`;
      if (adultN) {
         break;
      }
   } while (adultN && (2 <= (playlistR.length & middleF.length)));
      playlistR = [middleF.length];
   while (middleF.length == redgoalG.length) {
      redgoalG = `${teame.length % (Math.max(1, 1))}`;
      break;
   }
       let stylesu: Map<any, any> = new Map([[String.fromCharCode(109,101,115,115,97,103,101,115,95,48,95,49,57,0),465], [String.fromCharCode(117,110,97,114,121,95,116,95,57,53,0),184]]);
         stylesu.set(`${stylesu.size}`, stylesu.size);
         stylesu = new Map([[`${stylesu.size}`, stylesu.size + 1]]);
      for (let p = 0; p < 3; p++) {
         stylesu = new Map([[`${stylesu.size}`, stylesu.size + stylesu.size]]);
      }
      defaultlogoU = `${playlistR.length % (Math.max(6, middleF.length))}`;
   for (let r = 0; r < 2; r++) {
       let libyogaJ = 4.0;
       let ksad5 = 2;
       let modulez = String.fromCharCode(99,95,51,95,98,108,117,114,97,121,0);
       let leftc = String.fromCharCode(111,95,52,57,0);
      while (!modulez.includes(`${leftc.length}`)) {
         modulez = `${modulez.length}`;
         break;
      }
       let panglet = String.fromCharCode(106,100,109,97,105,110,99,116,95,57,95,52,50,0);
       let whatsappc = String.fromCharCode(97,95,56,52,95,115,97,100,120,0);
       let downarrowb = 5.0;
       let manifestP = 5.0;
      if (leftc != whatsappc) {
         whatsappc = `${leftc.length / 3}`;
      }
      for (let a = 0; a < 3; a++) {
         panglet = `${3 - leftc.length}`;
      }
      for (let d = 0; d < 3; d++) {
         ksad5 -= 3 >> (Math.min(4, whatsappc.length));
      }
       let iconusery = String.fromCharCode(115,111,114,116,101,100,95,118,95,51,54,0);
       let settingsg = String.fromCharCode(99,95,53,51,95,109,97,114,107,105,110,103,115,0);
          let cancelG: Map<any, any> = new Map([[String.fromCharCode(115,109,97,114,116,95,50,95,57,55,0),638], [String.fromCharCode(102,95,54,51,95,115,101,116,115,97,114,0),871]]);
          let iconeditN = false;
         iconusery += `${parseInt(`${manifestP}`) << (Math.min(panglet.length, 4))}`;
         cancelG.set(`${iconeditN}`, cancelG.size % 3);
         libyogaJ += parseFloat(`${modulez.length / 3}`);
      if ((downarrowb / (Math.max(3, manifestP))) < 5.57 && 4.66 < (5.57 / (Math.max(2, manifestP)))) {
          let servicej = 4.0;
         downarrowb /= Math.max(parseFloat(`${2 ^ parseInt(`${libyogaJ}`)}`), 3);
         servicej -= parseFloat(`${2 >> (Math.min(Math.abs(parseInt(`${servicej}`)), 2))}`);
      }
          let reminder1 = 4.0;
          let matchl: Map<any, any> = new Map([[String.fromCharCode(109,105,100,108,95,101,95,49,57,0),String.fromCharCode(100,105,115,112,108,97,121,95,99,95,50,0)], [String.fromCharCode(108,95,54,54,95,99,111,117,110,116,101,114,0),String.fromCharCode(109,111,99,107,115,95,99,95,53,56,0)], [String.fromCharCode(97,95,53,56,95,99,109,97,112,0),String.fromCharCode(112,95,50,51,95,109,97,114,107,101,116,0)]]);
          let middleC = String.fromCharCode(100,101,105,110,116,95,51,95,56,49,0);
         whatsappc += `${3 + modulez.length}`;
         reminder1 += parseFloat(`${matchl.size / 1}`);
         matchl.set(middleC, (middleC == String.fromCharCode(74,0) ? middleC.length : matchl.size));
          let sourceU = String.fromCharCode(105,95,54,48,95,97,115,107,0);
          let paginationD = String.fromCharCode(112,95,54,48,95,100,101,115,101,108,101,99,116,101,100,0);
          let holderA = 0;
         leftc += `${holderA | panglet.length}`;
         sourceU = "2";
         paginationD = `${paginationD.length}`;
         holderA /= Math.max(2, (String.fromCharCode(117,0) == sourceU ? paginationD.length : sourceU.length));
      rooma += modulez.length;
   }

    setTimeout(() => {

      playlistR = [3 + parseInt(`${libfbjnie}`)];
   let downloaded5 = f_playern <= 9394645.0;
   do {
      f_playern /= Math.max(scheduleK % (Math.max(defaultlogoU.length, 3)), 2);
      if (downloaded5) {
         break;
      }
   } while ((2.92 <= (3.83 + f_playern) && 1 <= (parseInt(`${f_playern}`) + 5)) && downloaded5);
   let usernameP = playj <= 7010937.0;
   do {
       let trophyg = String.fromCharCode(102,115,121,110,99,95,105,95,56,57,0);
       let libtan2 = false;
       let halffieldimagee = String.fromCharCode(101,95,49,48,48,95,105,110,97,99,116,105,118,101,0);
       let annerB = String.fromCharCode(108,101,110,103,116,104,115,95,113,95,51,55,0);
      for (let a = 0; a < 2; a++) {
         halffieldimagee = `${(annerB.length << (Math.min(1, Math.abs((libtan2 ? 3 : 3)))))}`;
      }
         halffieldimagee += `${trophyg.length}`;
      for (let w = 0; w < 2; w++) {
         trophyg = `${(halffieldimagee == String.fromCharCode(86,0) ? halffieldimagee.length : annerB.length)}`;
      }
      for (let c = 0; c < 2; c++) {
          let core4 = 1.0;
          let taiwanl = true;
          let completeq = 4;
          let phone6: Array<any> = [576, 531, 873];
          let yellowvideolive9 = 2;
         libtan2 = core4 == 63.86;
         core4 *= parseFloat(`${3}`);
         taiwanl = !taiwanl || phone6.length > 27;
         completeq >>= Math.min(2, Math.abs(3));
         phone6 = [phone6.length >> (Math.min(Math.abs(2), 4))];
         yellowvideolive9 *= phone6.length % 3;
      }
      let yingJ = libtan2 ? !libtan2 : libtan2;
      do {
         libtan2 = !libtan2;
         if (yingJ) {
            break;
         }
      } while (yingJ && (!libtan2));
       let photoc = false;
       let questiconN = false;
       let pressurei: Map<any, any> = new Map([[String.fromCharCode(117,95,49,56,95,117,110,99,111,109,112,114,101,115,115,0),String.fromCharCode(98,111,114,100,101,114,95,122,95,48,0)], [String.fromCharCode(109,95,57,53,95,99,97,108,108,111,99,0),String.fromCharCode(116,95,51,95,115,99,104,105,0)]]);
          let hejiK: Array<any> = [634, 627];
          let eventsplashH = 5.0;
          let temperaturej = String.fromCharCode(97,118,101,115,95,50,95,55,56,0);
         pressurei.set(`${annerB}`, annerB.length);
         hejiK = [3];
         eventsplashH *= parseFloat(`${2 / (Math.max(8, temperaturej.length))}`);
         temperaturej = `${parseInt(`${eventsplashH}`) | hejiK.length}`;
         trophyg += `${((photoc ? 2 : 5) % (Math.max(3, annerB.length)))}`;
         annerB += `${(2 * (libtan2 ? 1 : 3))}`;
       let imagenetworkerrt = 2;
       let libreactnativejniI: Map<any, any> = new Map([[String.fromCharCode(101,108,115,95,114,95,56,0),76], [String.fromCharCode(99,95,51,55,95,118,105,110,116,101,114,112,111,108,97,116,111,114,0),653], [String.fromCharCode(99,101,110,116,114,111,105,100,115,95,118,95,56,57,0),323]]);
       let anneri: Map<any, any> = new Map([[String.fromCharCode(106,95,56,49,95,108,105,116,116,108,101,0),true ], [String.fromCharCode(101,95,54,95,100,101,105,110,105,116,0),false ]]);
      playj -= scheduleK & 2;
      if (usernameP) {
         break;
      }
   } while (usernameP && (4.60 >= rooma));
       let navigationv = 1.0;
      for (let r = 0; r < 1; r++) {
         navigationv -= parseFloat(`${3}`);
      }
      while ((navigationv / (Math.max(3.57, 8))) <= 2.66 || 3.57 <= (navigationv / (Math.max(9, navigationv)))) {
         navigationv /= Math.max(4, parseFloat(`${2}`));
         break;
      }
      while (2.53 == navigationv) {
         navigationv -= parseFloat(`${parseInt(`${navigationv}`) >> (Math.min(Math.abs(parseInt(`${navigationv}`)), 5))}`);
         break;
      }
      eventy = fnewarchdefaultsJ.length >= 73;
       let j_unlockM = String.fromCharCode(109,95,49,50,95,112,105,99,116,117,114,101,0);
       let libloggerx = String.fromCharCode(116,105,109,101,118,97,108,95,105,95,52,57,0);
       let libavfilterD = 4.0;
         j_unlockM += `${j_unlockM.length % 3}`;
         libloggerx = `${libloggerx.length % 3}`;
      for (let x = 0; x < 1; x++) {
         libloggerx = `${libloggerx.length}`;
      }
      if ((parseInt(`${libavfilterD}`) - 4) == 2 || 4 == (parseInt(`${libavfilterD}`) - j_unlockM.length)) {
         j_unlockM += `${parseInt(`${libavfilterD}`)}`;
      }
       let bottomN = true;
       let linke = false;
         libloggerx += "3";
         linke = libloggerx.length > 67;
      let redgoalI = linke ? !linke : linke;
      do {
         linke = !bottomN;
         if (redgoalI) {
            break;
         }
      } while (redgoalI && (bottomN));
      fnewarchdefaultsJ = `${teame.length}`;
      setActiveIndex(0);

      playlistR = [fnewarchdefaultsJ.length];
   for (let z = 0; z < 1; z++) {
      membershipG /= Math.max(5, 1);
   }
   let schedulen = rooma <= 5759277.0;
   do {
      rooma -= 2 | redgoalG.length;
      if (schedulen) {
         break;
      }
   } while (((1 | defaultlogoU.length) > 5 && 1.64 > (rooma / (Math.max(defaultlogoU.length, 9)))) && schedulen);
   let megaphoneg = 6893821 <= playlistR.length;
   do {
      playlistR.push((String.fromCharCode(105,0) == middleF ? middleF.length : playlistR.length));
      if (megaphoneg) {
         break;
      }
   } while (megaphoneg && ((playlistR.length - parseInt(`${libfbjnie}`)) < 2 && (5.31 - libfbjnie) < 5.41));
   let formK = rooma <= 7454155.0;
   do {
      rooma *= parseInt(`${playj}`) + playlistR.length;
      if (formK) {
         break;
      }
   } while ((4.13 < rooma) && formK);
      if (carouselRef) {

      launcher4 = `${3 * launcher4.length}`;
   while (teame == String.fromCharCode(106,0)) {
       let logo5 = String.fromCharCode(102,95,56,95,112,114,101,109,117,108,116,105,112,108,121,121,117,118,0);
       let halffieldimageb = true;
       let productJ = String.fromCharCode(104,95,53,48,95,102,105,110,97,108,100,0);
      let gifgoalY = halffieldimageb ? !halffieldimageb : halffieldimageb;
      do {
         halffieldimageb = logo5.length < 29;
         if (gifgoalY) {
            break;
         }
      } while (gifgoalY && (!halffieldimageb));
         halffieldimageb = productJ.length > 70 || halffieldimageb;
       let libsentrya = 5;
       let vnewsshareW = 0;
      if ((libsentrya ^ productJ.length) >= 2 || 2 >= (libsentrya ^ productJ.length)) {
         productJ = `${2 & vnewsshareW}`;
      }
         halffieldimageb = productJ.length < 73 || halffieldimageb;
      if (1 < (2 | vnewsshareW)) {
          let relatedV = String.fromCharCode(110,101,116,119,111,114,107,115,95,107,95,51,48,0);
          let shrinkf = 2.0;
          let sheet1 = String.fromCharCode(113,117,105,99,107,95,98,95,49,0);
          let questR = String.fromCharCode(105,101,110,116,114,121,95,111,95,55,53,0);
          let crossJ = 2.0;
         productJ = `${logo5.length / (Math.max(2, 7))}`;
         relatedV = `${parseInt(`${shrinkf}`)}`;
         sheet1 = "3";
         questR = `${relatedV.length}`;
         crossJ -= relatedV.length;
      }
         halffieldimageb = vnewsshareW == 74;
       let libhermeso = String.fromCharCode(103,101,110,101,114,97,116,101,95,101,95,54,54,0);
       let libfb_ = String.fromCharCode(120,95,51,50,95,114,101,99,117,114,115,105,118,101,0);
         halffieldimageb = productJ == String.fromCharCode(83,0);
      defaultlogoU = `${redgoalG.length}`;
      break;
   }
   for (let y = 0; y < 2; y++) {
      eventy = !eventy;
   }
      eventy = libfbjnie > 50.67;
   while (3 < (fnewarchdefaultsJ.length | 3) && (fnewarchdefaultsJ.length - f_playern) < 4.60) {
       let trophyb = String.fromCharCode(104,111,109,101,95,49,95,51,51,0);
         trophyb = `${trophyb.length}`;
         trophyb += `${2 << (Math.min(5, trophyb.length))}`;
      for (let n = 0; n < 1; n++) {
         trophyb += "1";
      }
      f_playern *= (trophyb == String.fromCharCode(85,0) ? fnewarchdefaultsJ.length : trophyb.length);
      break;
   }
        setIsRefreshing(false);
        carouselRef?.current?.scrollTo({index: 0, animated: false});
      }
    }, 0);
  };

  // const fetchPlaylist = (page: number) =>
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  //   ({ pageParam = 1 }) => fetchPlaylist(pageParam),
  
  
  
  
  
  
  
  //       if (nextPage > totalPage && totalPage != 0) {
  
  
  
  
  
  
  
  
  
  
  

  const fetchYingPing = () =>
    wwRewardvideoMegaphone.getHomePages(1000, isVip).then(data => {
      setResults(data.topic_list);
      return data;
    });

  const {data: yingPingList, isFetching: isFetchingYingPing} = useQuery({
    queryKey: ['yingPingList'],
    queryFn: () => fetchYingPing(),
  });

  useEffect(() => {
    onLoad();
  }, []);

  const renderCarousel = useCallback(
    ({item, index}: {item: any; index: number}) => {
      const key = item.is_ads
        ? item.carousel_id + item.carousel_pic_mobile
        : item.carousel_id;
      return (
        <TouchableOpacity
          key={`slider-${key}`}
          onPress={() => {
            dispatch(playVod(item.vod));
            navigation.navigate('播放IOS', {
              vod_id: item.carousel_content_id,
            });
          }}>
          <FastImage
            key={`slider-${key}`}
            style={styles.image}
            source={{
              uri: item.carousel_pic_mobile,
              priority: 'normal',
            }}
            resizeMode={'cover'}
            useFastImage={true}
          />
          <LinearGradient
            colors={['transparent', 'black']}
            start={{x: 0.8, y: 0}}
            end={{x: 0.8, y: 0.9}}
            style={styles.bottomBlur}
          />
          <Text
            style={{
              ...textVariants.bodyBold,
              ...styles.carouselTag,
              color: 'white',
            }}
            numberOfLines={1}>
            {item.carousel_name}
          </Text>
        </TouchableOpacity>
      );
    },
    [],
  );

  const renderContent = useCallback(
    ({item, index}: {item: wwFirebase; index: number}) => (
      <View
        style={{
          paddingLeft: spacing.sideOffset,
          paddingRight: spacing.sideOffset,
        }}>
        { }
        <View key={`${item.topic_name}-${index}`} style={{paddingTop: 10}}>
          <View style={{paddingBottom: 5}}>
            <ShowMoreVodButton
              text={item.topic_name}
              onPress={() => {
                dispatch(viewPlaylistDetails(item));
                navigation.navigate('PlaylistDetail', {
                  topic_id: item.topic_id,
                });
              }}
            />
          </View>
          <VodListVertical vods={item.vod_list} />
        </View>
      </View>
    ),
    [],
  );

  return (
    <View style={{width: width}}>
      {yingPingList ? (
        <FlatList
          style={{paddingBottom: 10}}
          refreshControl={
            <RefreshControl
              refreshing={isRefreshing}
              onRefresh={handleRefresh}
              tintColor="#FAC33D"
            />
          }
          ListHeaderComponent={
            <>
              {yingPingList?.carousel[0] && !refreshProp && (
                <View
                  style={{
                    flex: 1,
                    justifyContent: 'center',
                    alignItems: 'center',
                    borderRadius: 17,
                    zIndex: 9999,
                  }}>
                  <Carousel
                    ref={carouselRef}
                    loop
                    width={width - spacing.sideOffset - spacing.sideOffset}
                    height={
                      (width - spacing.sideOffset - spacing.sideOffset) /
                      imgRatio
                    }
                    autoPlay={true}
                    data={yingPingList.carousel}
                    scrollAnimationDuration={220}
                    autoPlayInterval={2300}
                    onSnapToItem={index => {
                      setActiveIndex(index);
                    }}
                    onScrollEnd={index => {
                      setActiveIndex(index);
                    }}
                    renderItem={renderCarousel}
                  />
                  <CarouselPagination
                    data={yingPingList.carousel}
                    activeIndex={activeIndex}
                  />
                </View>
              )}
              <View>
                <View style={{gap: spacing.m}}></View>

                {yingPingList &&
                  yingPingList.yingping_list.vod_list.length > 0 && (
                    <View
                      style={{
                        paddingLeft: spacing.sideOffset,
                        paddingRight: spacing.sideOffset,
                        gap: spacing.xxs,
                      }}>
                      <ShowMoreVodButton
                        text={yingPingList.yingping_list.type_name}
                      />
                      {yingPingList.yingping_list.vod_list.map(
                        (item, index) => (
                          <YingPingContainer
                            key={item.vod_id}
                            vod={item}
                            width={width}
                            imgRatio={imgRatio}
                            isSlide={index % 2 !== 0}
                          />
                        ),
                      )}
                    </View>
                  )}

                {/* {data?.yunying &&
                  data.yunying.length > 0 &&
                  data.yunying.map((item, index) => (
                    <View
                      key={item.type_name}
                      style={{
                        paddingLeft: spacing.sideOffset,
                        paddingRight: spacing.sideOffset,
                        gap: spacing.xxs,
                      }}
                    >
                      <View>
                        <ShowMoreVodButton
                          text={item.type_name}
                          onPress={() => {
                            navigation.navigate("片库", {
                              type_id: item.vod_list[0].type_id,
                            });
                          }}
                        />
                      </View>
                      <VodListVertical vods={item.vod_list} />
                    </View>
                  ))} */}

                {yingPingList?.categories &&
                  yingPingList.categories.length > 0 &&
                  yingPingList.categories.map((category, index) => (
                    <View
                      key={`category-${index}`}
                      style={{
                        paddingLeft: spacing.sideOffset,
                        paddingRight: spacing.sideOffset,
                        paddingTop: 5,
                      }}>
                      <View
                        style={{
                          paddingBottom: 5,
                        }}>
                        <ShowMoreVodButton
                          text={category.type_name}
                          onPress={() => {
                            navigation.navigate('片库', {
                              type_id: category.type_id,
                            });
                          }}
                        />
                      </View>
                      <VodListVertical vods={category.vod_list} />
                    </View>
                  ))}
              </View>
            </>
          }
          data={results}
          
          
          
          
          
          initialNumToRender={0}
          onEndReachedThreshold={0.5}
          renderItem={renderContent}
          
          //   <View style={{ ...styles.loading, marginBottom: 60 }}>
          
          
          
          
          

          
          
          
          
          
          
          //       />
          
          
          
          
          
          
          
          
          //       >
          
          //       </Text>
          
          //   </View>
          
        />
      ) : (
        <>
          <View
            style={{
              width: '100%',
              height: '100%',
              display: 'flex',
              justifyContent: 'center',
              alignItems: 'center',
              alignSelf: 'center',
            }}>
            <FastImage
              style={{height: 80, width: 80}}
              source={require('@static/images/runtimeschedulerPromotion.gif')}
              resizeMode={'contain'}
            />
          </View>
        </>
      )}
    </View>
  );
};

export default memo(RecommendationHome);

const styles = StyleSheet.create({
  wrapper: {
    borderRadius: 100,
    marginTop: 10,
    zIndex: 99,
  },
  image: {
    width: '100%',
    height: '100%',
    borderRadius: 17,
    
  },
  text: {
    color: '#fff',
    fontSize: 30,
    fontWeight: 'bold',
  },
  dotStyle: {
    width: 6,
    height: 4,
  },
  activeDotStyle: {
    width: 14,
    height: 4,
  },
  paginationStyle: {
    top: 173,
    height: 20,
  },
  vod_hotlist: {
    height: 99,
    width: 176,
  },
  vod_live_station: {
    height: 90,
    width: 150,
  },
  nav: {
    flexGrow: 1,
    justifyContent: 'center',
    marginBottom: 10,
  },
  catalogNav: {
    flexGrow: 1,
    justifyContent: 'center',
    marginTop: 5,
  },
  vodList: {
    display: 'flex',
    flexDirection: 'row',
    flexWrap: 'wrap',
  },
  bottomBlur: {
    position: 'absolute',
    left: 0,
    right: 0,
    bottom: 0,
    height: 75,
    flex: 1,
    borderBottomRightRadius: 10,
    borderBottomLeftRadius: 10,
    opacity: 0.8,
  },
  carouselTag: {
    position: 'absolute',
    bottom: 25,
    left: 20,
    marginRight: 16,
  },
  loading: {
    flexDirection: 'row',
    justifyContent: 'center',
    flex: 1,
  },
  banner: {
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    marginTop: 4,
    marginBottom: 5,
  },
  icon: {
    color: 'white',
    
    maxWidth: '15%',
    objectFix: 'contain',
  },
});
