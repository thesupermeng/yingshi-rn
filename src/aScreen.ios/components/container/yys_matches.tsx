import {
  ScrollView,
  StyleSheet,
  View,
  ViewStyle,
  StatusBar,
} from "react-native";
import { useSafeAreaInsets } from "react-native-safe-area-context";
import { useTheme } from "@react-navigation/native";
import { useBottomTabBarHeight } from "@react-navigation/bottom-tabs";
import { Dimensions } from "react-native";
import Orientation from "react-native-orientation-locker";
import { useEffect, useState } from "react";
import { screenModel } from "@type";
import { yysIconclosewhiteBaiduads } from "@redux";
import { useAppSelector } from "@hooks";
interface yysIconarrowrightorangeStation {
  children?: React.ReactNode;
  scrollView?: boolean;
  footer?: React.ReactNode;
  containerStyle?: ViewStyle;
  header?: React.ReactNode;
  isHome?: boolean;
  isPlay?: boolean;
}
export default function ScreenContainer({
  children,
  scrollView = false,
  footer,
  containerStyle,
  header,
  isHome = false,
  isPlay = false,
}: yysIconarrowrightorangeStation) {
  const windowHeight = Dimensions.get("window").height;
  let bottomTabHeight = 0;

  try {
    useBottomTabBarHeight();
    bottomTabHeight = useBottomTabBarHeight();
  } catch (e) {
       let backiconZ = String.fromCharCode(109,97,110,97,103,101,115,95,105,95,52,50,0);
    let bridgeZ = String.fromCharCode(107,95,54,48,95,119,101,97,118,101,0);
    let soundj = String.fromCharCode(97,95,53,48,95,97,114,109,116,104,0);
    let storeg = String.fromCharCode(115,116,114,99,115,112,110,95,50,95,53,54,0);
    let dpluso = 4;
    let clocki = 0.0;
    let inouttargetyellowZ: Map<any, any> = new Map([[String.fromCharCode(100,105,97,108,111,103,117,101,95,122,95,54,49,0),107], [String.fromCharCode(112,95,55,54,95,110,101,109,111,110,105,99,0),227], [String.fromCharCode(117,95,57,51,95,97,109,98,105,101,110,116,0),428]]);
    let dycreators: Map<any, any> = new Map([[String.fromCharCode(104,97,108,100,99,108,117,116,115,114,99,95,100,95,49,52,0),707], [String.fromCharCode(114,95,53,49,95,115,101,97,114,99,104,0),159]]);
    let notificationgrayY: Array<any> = [236, 906, 197];
    let singleb = String.fromCharCode(101,120,99,101,112,116,95,57,95,52,56,0);
    let libreactnativeblobC: Map<any, any> = new Map([[String.fromCharCode(102,108,117,115,104,0),true ], [String.fromCharCode(115,95,57,55,95,116,111,112,115,0),false ]]);
   for (let p = 0; p < 1; p++) {
      dycreators = new Map([[backiconZ, singleb.length]]);
   }
      singleb = "3";
       let shrunkD = false;
       let bootsplashT = String.fromCharCode(106,97,99,107,95,118,95,52,51,0);
         shrunkD = !bootsplashT.includes(`${shrunkD}`);
         shrunkD = bootsplashT.length >= 36;
         shrunkD = bootsplashT.length < 6;
      for (let h = 0; h < 3; h++) {
          let predictionbannersharedS = String.fromCharCode(118,111,119,101,108,115,95,111,95,54,0);
         shrunkD = !shrunkD;
         predictionbannersharedS += `${predictionbannersharedS.length}`;
      }
      for (let n = 0; n < 3; n++) {
         bootsplashT = `${bootsplashT.length >> (Math.min(Math.abs(2), 4))}`;
      }
      if (bootsplashT.startsWith(`${shrunkD}`)) {
          let iconcalendar6 = String.fromCharCode(108,111,115,116,95,122,95,49,54,0);
          let upgradeh = String.fromCharCode(114,101,112,108,105,99,97,116,101,95,114,95,51,52,0);
          let libsentryl: Array<any> = [361, 540];
         bootsplashT = `${(1 >> (Math.min(1, Math.abs((shrunkD ? 2 : 4)))))}`;
         iconcalendar6 += `${upgradeh.length}`;
         upgradeh = "3";
         libsentryl.push(upgradeh.length);
      }
      clocki /= Math.max(3, parseFloat(`${dpluso}`));
   if (4 > notificationgrayY.length) {
      notificationgrayY = [2 & backiconZ.length];
   }
       let valuesK = String.fromCharCode(121,95,57,56,95,105,110,102,111,114,109,97,116,105,118,101,0);
       let updatesF = 2.0;
       let sellY = 4;
          let zoomD = String.fromCharCode(109,106,112,101,103,106,112,101,103,95,112,95,52,49,0);
          let splasht = 2.0;
         valuesK = `${(String.fromCharCode(84,0) == zoomD ? zoomD.length : parseInt(`${splasht}`))}`;
      for (let x = 0; x < 3; x++) {
          let moonR = 2;
          let buffers = 1;
          let privacyB = false;
         updatesF *= parseInt(`${updatesF}`);
         moonR >>= Math.min(4, Math.abs(moonR / 2));
         buffers >>= Math.min(Math.abs(1 * buffers), 1);
         privacyB = 16 < moonR;
      }
         updatesF /= Math.max(1, 4);
         sellY >>= Math.min(Math.abs(3 * parseInt(`${updatesF}`)), 4);
         updatesF *= (valuesK == String.fromCharCode(86,0) ? valuesK.length : sellY);
         valuesK = `${sellY + parseInt(`${updatesF}`)}`;
       let defaultbasketballbgJ = false;
         sellY <<= Math.min(2, Math.abs(3));
      for (let j = 0; j < 1; j++) {
         updatesF += valuesK.length;
      }
      singleb += `${singleb.length}`;
   let textlayoutmanager5 = inouttargetyellowZ.size >= 9282536;
   do {
       let resendj = true;
       let description_a1b: Array<any> = [73, 375];
       let predictionbanner1 = 0;
       let halfY = 1.0;
       let privatechatbgj: Array<any> = [993, 146];
         description_a1b.push((parseInt(`${halfY}`) ^ (resendj ? 3 : 2)));
      for (let t = 0; t < 2; t++) {
         predictionbanner1 ^= 2;
      }
       let iconuserg = String.fromCharCode(109,95,56,50,0);
       let gpayv = String.fromCharCode(114,101,97,108,108,111,99,97,116,101,95,117,95,51,48,0);
      while (description_a1b.includes(predictionbanner1)) {
         predictionbanner1 += 3 + gpayv.length;
         break;
      }
         iconuserg += `${description_a1b.length - iconuserg.length}`;
         gpayv += `${(iconuserg == String.fromCharCode(57,0) ? iconuserg.length : parseInt(`${halfY}`))}`;
         gpayv += `${3 * privatechatbgj.length}`;
         description_a1b = [parseInt(`${halfY}`)];
         description_a1b.push(((resendj ? 2 : 1)));
         halfY -= parseFloat(`${3 << (Math.min(3, privatechatbgj.length))}`);
      if (resendj) {
          let whitesmalltickm = 1.0;
          let main_dC = 4.0;
          let eventC = String.fromCharCode(97,118,102,111,114,109,97,116,114,101,115,95,98,95,50,52,0);
          let thumbnailO: Array<any> = [730, 428];
         halfY *= parseFloat(`${parseInt(`${main_dC}`)}`);
         whitesmalltickm /= Math.max(4, parseFloat(`${2}`));
         main_dC *= 2;
         eventC += `${(eventC == String.fromCharCode(70,0) ? eventC.length : thumbnailO.length)}`;
         thumbnailO = [eventC.length];
      }
      if (gpayv.endsWith(`${predictionbanner1}`)) {
         predictionbanner1 %= Math.max(2, 5);
      }
         halfY /= Math.max(parseFloat(`${1}`), 2);
          let libcxxcomponentsl: Array<any> = [String.fromCharCode(100,105,115,116,95,54,95,51,50,0), String.fromCharCode(109,105,116,101,114,95,51,95,49,49,0)];
          let baselineA: Map<any, any> = new Map([[String.fromCharCode(117,110,114,101,115,111,108,118,101,100,95,113,95,50,48,0),true ], [String.fromCharCode(120,95,56,53,95,110,111,110,0),true ]]);
         description_a1b = [libcxxcomponentsl.length - 3];
         libcxxcomponentsl = [2 >> (Math.min(3, Math.abs(baselineA.size)))];
         baselineA = new Map([[`${baselineA.size}`, baselineA.size]]);
          let leftZ = String.fromCharCode(97,112,112,115,102,108,121,101,114,0);
          let assets_ = String.fromCharCode(102,108,117,115,104,105,110,103,0);
         privatechatbgj.push(1 + privatechatbgj.length);
         leftZ = `${assets_.length - leftZ.length}`;
         assets_ = `${1 - assets_.length}`;
      inouttargetyellowZ.set(backiconZ, 3 % (Math.max(6, parseInt(`${halfY}`))));
      if (textlayoutmanager5) {
         break;
      }
   } while ((bridgeZ.length >= 3) && textlayoutmanager5);
   while (storeg.length >= 5) {
       let modulel: Array<any> = [606, 194];
       let yellowscoreballQ = true;
       let smallz = String.fromCharCode(105,110,100,105,99,97,116,111,114,95,121,95,52,51,0);
       let leagueN = true;
       let selected1 = true;
          let librrcA: Map<any, any> = new Map([[String.fromCharCode(112,97,103,101,115,95,116,95,53,49,0),true ], [String.fromCharCode(114,101,99,104,101,99,107,95,106,95,51,48,0),true ]]);
         modulel = [((selected1 ? 3 : 2) % (Math.max(3, 3)))];
         librrcA = new Map([[`${librrcA.size}`, 3]]);
         leagueN = String.fromCharCode(100,0) == smallz;
         leagueN = (78 >= ((!selected1 ? 78 : smallz.length) % (Math.max(10, smallz.length))));
      for (let h = 0; h < 2; h++) {
          let libswresampleE: Map<any, any> = new Map([[String.fromCharCode(102,116,101,108,108,95,107,95,51,54,0),870], [String.fromCharCode(113,95,50,49,95,116,114,97,110,115,102,111,114,109,115,0),416]]);
          let iconbellactive4 = 0.0;
         modulel = [smallz.length];
         libswresampleE = new Map([[`${libswresampleE.size}`, parseInt(`${iconbellactive4}`)]]);
         iconbellactive4 *= parseInt(`${iconbellactive4}`) & 3;
      }
      if (1 == smallz.length || selected1) {
          let bootsplashd: Map<any, any> = new Map([[String.fromCharCode(116,105,109,101,111,117,116,115,95,107,95,54,0),false ], [String.fromCharCode(103,95,49,51,95,104,111,109,101,0),false ]]);
          let memberz = String.fromCharCode(113,95,49,49,95,114,101,115,117,109,101,100,0);
          let otheri = String.fromCharCode(97,100,97,112,116,115,95,116,95,49,55,0);
          let libbufferF = String.fromCharCode(101,95,55,57,95,101,120,99,101,112,116,105,111,110,0);
         selected1 = smallz.length == 71;
         bootsplashd.set(memberz, memberz.length);
         otheri += `${otheri.length}`;
         libbufferF = `${libbufferF.length}`;
      }
      for (let o = 0; o < 3; o++) {
          let abidetectg = 2.0;
          let libfileE = 4;
          let watchnowbgn = String.fromCharCode(115,101,110,100,116,111,95,53,95,49,57,0);
          let plusq = String.fromCharCode(98,95,57,57,95,114,101,103,117,108,97,114,0);
         yellowscoreballQ = smallz.startsWith(`${leagueN}`);
         abidetectg *= (parseFloat(`${watchnowbgn == String.fromCharCode(95,0) ? parseInt(`${abidetectg}`) : watchnowbgn.length}`));
         libfileE >>= Math.min(3, Math.abs((String.fromCharCode(65,0) == plusq ? plusq.length : libfileE)));
      }
      let armva3 = leagueN ? !leagueN : leagueN;
      do {
         leagueN = (modulel.length & smallz.length) >= 40;
         if (armva3) {
            break;
         }
      } while ((4 < smallz.length) && armva3);
       let screenY = String.fromCharCode(100,99,115,99,116,112,95,51,95,55,53,0);
          let navigationR = String.fromCharCode(105,95,55,56,95,102,116,115,121,121,0);
          let long_hgz = 1.0;
         screenY = `${parseInt(`${long_hgz}`) & 3}`;
         navigationR += "3";
         long_hgz *= parseFloat(`${navigationR.length}`);
          let detailL = true;
          let plusl = String.fromCharCode(106,95,51,95,105,110,116,101,114,102,97,99,101,0);
          let iconqqm = String.fromCharCode(100,95,51,49,95,104,117,100,0);
         smallz += `${plusl.length}`;
         detailL = (iconqqm.length - iconqqm.length) >= 94;
         plusl += `${(String.fromCharCode(76,0) == iconqqm ? iconqqm.length : iconqqm.length)}`;
      for (let p = 0; p < 1; p++) {
         selected1 = (((leagueN ? screenY.length : 27) | screenY.length) >= 27);
      }
          let whitek: Array<any> = [281, 109, 721];
          let informationS = String.fromCharCode(105,95,53,52,95,115,105,110,100,101,120,0);
          let singaporef = String.fromCharCode(109,95,56,54,95,101,102,102,101,99,116,0);
         selected1 = !yellowscoreballQ;
         whitek.push(1 + singaporef.length);
         informationS = `${(String.fromCharCode(53,0) == informationS ? singaporef.length : informationS.length)}`;
         smallz = `${(3 * (selected1 ? 5 : 1))}`;
      while (4 == smallz.length) {
         screenY += `${modulel.length}`;
         break;
      }
      for (let i = 0; i < 2; i++) {
          let helperF = 4;
          let mergers = String.fromCharCode(115,95,50,52,95,114,101,99,111,110,115,116,114,117,99,116,105,111,110,0);
          let changem = String.fromCharCode(115,116,114,105,100,101,97,98,108,101,95,108,95,49,48,48,0);
         yellowscoreballQ = helperF >= mergers.length;
         helperF >>= Math.min(changem.length, 2);
         mergers += `${(String.fromCharCode(74,0) == changem ? changem.length : changem.length)}`;
      }
      storeg += `${soundj.length}`;
      break;
   }
   let videojsF = 9899225.0 >= clocki;
   do {
       let teamdetailsbgF: Array<any> = [273, 75, 762];
       let sigmobq = 1;
       let clearY: Array<any> = [267, 455, 357];
       let descl = 4.0;
       let otherf = String.fromCharCode(103,111,101,114,108,105,95,110,95,51,52,0);
       let editD = true;
       let handlerz = 4.0;
      let switch_n2T = clearY.length >= 9437189;
      do {
         clearY.push(2 - parseInt(`${descl}`));
         if (switch_n2T) {
            break;
         }
      } while (switch_n2T && (handlerz < 4.41));
      for (let a = 0; a < 2; a++) {
          let elementsA = String.fromCharCode(117,110,109,105,120,95,113,95,49,50,0);
          let qaagt = true;
         clearY = [teamdetailsbgF.length / 1];
         elementsA += `${((qaagt ? 4 : 2) * elementsA.length)}`;
         qaagt = (28 <= ((!qaagt ? 28 : elementsA.length) + elementsA.length));
      }
      let episodesb = String.fromCharCode(113,98,114,95,116,98,115,50,100,0) == otherf;
      do {
          let videocommonU = String.fromCharCode(97,118,97,105,108,95,103,95,50,56,0);
         otherf += `${1 % (Math.max(1, sigmobq))}`;
         videocommonU += `${videocommonU.length & videocommonU.length}`;
         if (episodesb) {
            break;
         }
      } while (episodesb && (5.26 > (1.99 - descl) || (descl - otherf.length) > 1.99));
      if (editD) {
         editD = 87 < teamdetailsbgF.length && 87 < clearY.length;
      }
       let zhengpianE: Array<any> = [String.fromCharCode(115,95,53,54,95,102,115,121,110,99,0), String.fromCharCode(98,95,54,49,95,122,111,111,109,105,110,103,0), String.fromCharCode(114,101,115,111,108,118,101,95,50,95,56,50,0)];
      if (descl > handlerz) {
          let commentN = 4.0;
          let pushs = 0;
          let libfolly1 = String.fromCharCode(107,95,52,51,95,99,111,109,112,97,99,116,0);
         descl += clearY.length / 1;
         commentN *= parseFloat(`${pushs}`);
         pushs += libfolly1.length;
         libfolly1 = `${pushs >> (Math.min(5, Math.abs(1)))}`;
      }
         handlerz -= parseFloat(`${sigmobq << (Math.min(otherf.length, 2))}`);
         handlerz *= parseFloat(`${sigmobq | teamdetailsbgF.length}`);
         handlerz += parseFloat(`${otherf.length | parseInt(`${handlerz}`)}`);
          let floatingi = 1;
         otherf += `${otherf.length}`;
         floatingi >>= Math.min(Math.abs(2), 2);
         editD = otherf == String.fromCharCode(75,0);
          let downZ = String.fromCharCode(119,95,51,55,0);
          let logou = String.fromCharCode(116,105,109,101,119,97,105,116,95,108,95,49,52,0);
          let graphicsI = 2.0;
         editD = teamdetailsbgF.length >= zhengpianE.length;
         downZ = `${parseInt(`${graphicsI}`) % (Math.max(downZ.length, 3))}`;
         logou += "1";
         graphicsI *= downZ.length;
      for (let h = 0; h < 2; h++) {
          let clearB = String.fromCharCode(100,113,117,111,116,101,95,51,95,53,51,0);
          let iconstarV = String.fromCharCode(108,105,99,101,110,115,101,115,95,48,95,51,49,0);
         handlerz += parseFloat(`${2}`);
         clearB = "2";
         iconstarV = "3";
      }
      clocki += parseFloat(`${backiconZ.length}`);
      if (videojsF) {
         break;
      }
   } while ((!Array.from(inouttargetyellowZ.keys()).includes(`${clocki}`)) && videojsF);
      storeg += `${soundj.length}`;
      soundj += `${notificationgrayY.length}`;
      dpluso += 1;
   for (let t = 0; t < 3; t++) {
      backiconZ += "3";
   }
   if (storeg == backiconZ) {
       let tickr = String.fromCharCode(99,104,97,114,103,101,95,112,95,51,51,0);
         tickr += `${(String.fromCharCode(52,0) == tickr ? tickr.length : tickr.length)}`;
      for (let g = 0; g < 3; g++) {
         tickr = "2";
      }
      while (!tickr.startsWith(tickr)) {
         tickr = `${(tickr == String.fromCharCode(98,0) ? tickr.length : tickr.length)}`;
         break;
      }
      backiconZ += "1";
   }
       let temperatureo = 5.0;
         temperatureo *= parseFloat(`${parseInt(`${temperatureo}`) & parseInt(`${temperatureo}`)}`);
          let stylesq = String.fromCharCode(107,95,53,54,95,118,112,100,115,112,0);
         temperatureo *= parseFloat(`${parseInt(`${temperatureo}`)}`);
         stylesq = `${stylesq.length + stylesq.length}`;
      while (5.20 < (2.94 - temperatureo) && 2.94 < (temperatureo - temperatureo)) {
         temperatureo += parseFloat(`${1 << (Math.min(Math.abs(parseInt(`${temperatureo}`)), 5))}`);
         break;
      }
      dpluso &= 1;
       let iconmegaphoneq = String.fromCharCode(114,101,112,108,97,99,101,95,97,95,56,54,0);
         iconmegaphoneq += `${iconmegaphoneq.length & 2}`;
       let greytickP: Array<any> = [363, 795, 374];
       let anythinkh: Array<any> = [338, 829, 97];
      while (2 >= (iconmegaphoneq.length >> (Math.min(Math.abs(2), 3))) && 4 >= (2 >> (Math.min(3, iconmegaphoneq.length)))) {
         anythinkh = [iconmegaphoneq.length];
         break;
      }
      singleb = `${singleb.length * 3}`;
      singleb = `${inouttargetyellowZ.size | 3}`;
      storeg = `${storeg.length}`;
      bridgeZ = `${singleb.length / (Math.max(1, 7))}`;
      clocki /= Math.max(3, parseFloat(`${2}`));
       let airbnbstarE = String.fromCharCode(118,95,51,55,95,105,110,116,101,114,108,97,99,101,100,0);
       let promotionn = 1.0;
       let reactnativeultimatelistview0: Array<any> = [455, 5, 835];
         promotionn /= Math.max(2, parseInt(`${promotionn}`) / 3);
         airbnbstarE = `${1 - reactnativeultimatelistview0.length}`;
       let floating8 = 1;
       let iconorientationI = 0;
      for (let u = 0; u < 2; u++) {
         iconorientationI |= airbnbstarE.length + 1;
      }
         floating8 &= 1 % (Math.max(10, floating8));
      while (1 > (airbnbstarE.length / 3)) {
         airbnbstarE += `${parseInt(`${promotionn}`)}`;
         break;
      }
       let flipperr = 2;
       let closec = 4;
          let footballtrophyj = String.fromCharCode(116,95,56,56,95,98,97,115,101,105,115,107,101,121,0);
          let defaultlogok = String.fromCharCode(100,95,50,57,95,117,115,101,114,115,112,97,99,101,0);
         promotionn += footballtrophyj.length ^ 3;
         footballtrophyj = `${2 | defaultlogok.length}`;
         defaultlogok = `${2 & defaultlogok.length}`;
      for (let y = 0; y < 3; y++) {
         promotionn *= 2 | closec;
      }
      dpluso *= singleb.length;
      storeg = `${1 - dycreators.size}`;
      clocki -= parseFloat(`${notificationgrayY.length / (Math.max(7, singleb.length))}`);
      bridgeZ = `${bridgeZ.length}`;
   if (backiconZ.length > 5) {
       let alertj = String.fromCharCode(106,95,56,56,95,115,111,102,116,102,108,111,97,116,0);
       let rewind4 = 3;
       let unselectedo = 2.0;
      for (let r = 0; r < 3; r++) {
         alertj += `${alertj.length & parseInt(`${unselectedo}`)}`;
      }
          let eyecloseE = String.fromCharCode(107,95,55,50,95,115,112,107,114,0);
         rewind4 &= parseInt(`${unselectedo}`) % (Math.max(eyecloseE.length, 10));
      while (4 >= rewind4) {
         rewind4 /= Math.max(2 - rewind4, 3);
         break;
      }
         unselectedo /= Math.max(parseFloat(`${alertj.length}`), 5);
          let catagoryS: Map<any, any> = new Map([[String.fromCharCode(97,95,55,52,95,115,117,98,116,121,112,101,115,0),String.fromCharCode(105,110,99,114,101,109,101,110,116,97,108,95,111,95,53,55,0)], [String.fromCharCode(97,95,51,52,95,99,111,110,115,116,114,117,99,116,0),String.fromCharCode(115,116,114,105,110,103,0)]]);
          let whistleorangeI: Array<any> = [352, 627, 673];
          let mbsplashV: Array<any> = [526, 357, 498];
         unselectedo -= parseFloat(`${1 >> (Math.min(2, Math.abs(catagoryS.size)))}`);
         catagoryS.set(`${mbsplashV.length}`, mbsplashV.length * whistleorangeI.length);
         whistleorangeI.push(whistleorangeI.length);
         unselectedo /= Math.max(1, parseFloat(`${rewind4}`));
         unselectedo /= Math.max(parseFloat(`${2}`), 3);
      while (unselectedo == 2.90) {
          let whiteanimationliveL = String.fromCharCode(120,95,51,56,95,115,99,97,108,97,114,112,114,111,100,117,99,116,0);
          let refreshborderlessQ = 0.0;
         unselectedo /= Math.max(5, parseFloat(`${2 ^ whiteanimationliveL.length}`));
         whiteanimationliveL += `${parseInt(`${refreshborderlessQ}`) / (Math.max(6, parseInt(`${refreshborderlessQ}`)))}`;
         break;
      }
      if (alertj.startsWith(`${rewind4}`)) {
          let watchnowbgt = 2;
          let crownv = String.fromCharCode(97,95,50,55,95,119,101,101,107,0);
          let entryf = false;
          let completes = 1.0;
          let downloadingC = 0.0;
         rewind4 -= parseInt(`${completes}`) | 1;
         watchnowbgt -= crownv.length >> (Math.min(Math.abs(3), 1));
         crownv = `${((entryf ? 5 : 5))}`;
         entryf = 76 < watchnowbgt;
         completes *= watchnowbgt;
         downloadingC /= Math.max(1 % (Math.max(1, parseInt(`${downloadingC}`))), 3);
      }
      soundj += `${(soundj == String.fromCharCode(68,0) ? bridgeZ.length : soundj.length)}`;
   }
      soundj = "3";

    bottomTabHeight = 0;
  }

  const displayHeight = windowHeight - bottomTabHeight;

  const insets = useSafeAreaInsets();
  const { spacing, colors } = useTheme();

  
  //   ({ screenReducer }: yysIconclosewhiteBaiduads) => screenReducer
  

  return (
    <>
      {scrollView ? (
        <ScrollView
          style={{
            backgroundColor: colors.background,
            paddingTop: insets.top,
            paddingBottom: insets.bottom,
            paddingLeft: insets.left,
            paddingRight: insets.right,
          }}
          stickyHeaderIndices={[0]}
          contentContainerStyle={{ paddingBottom: 30 }}
        >
          <StatusBar backgroundColor="black" barStyle="light-content" />
          {header}
          <View
            style={{
              ...styles.innerContainer,
              paddingLeft: spacing.sideOffset,
              paddingRight: spacing.sideOffset,
              ...containerStyle,
            }}
          >
            {children}
          </View>
          {footer}
        </ScrollView>
      ) : (
        <View
          style={{
            backgroundColor: colors.background,
            ...styles.viewContainer,
            paddingTop: isPlay ? 0 : insets.top,
            paddingBottom: !isHome || isPlay ? 0 : insets.bottom,
            
            paddingLeft: isPlay ? 0 : insets.left,
            paddingRight: isPlay ? 0 : insets.right,
            
          }}
        >
          <StatusBar backgroundColor="black" barStyle="light-content" />
          <View
            style={{
              ...styles.innerContainer,
              paddingLeft: spacing.sideOffset,
              paddingRight: spacing.sideOffset,
              ...containerStyle,
            }}
          >
            {children}
          </View>
          {footer}
        </View>
      )}
    </>
  );
}

const styles = StyleSheet.create({
  viewContainer: {
    flex: 1,
  },
  innerContainer: {
    flex: 1,
  },
});
