import React, {useRef, useEffect, useState} from 'react';
import {Animated, StyleSheet, Easing} from 'react-native';

const styles = StyleSheet.create({
  snowflake: {
    color: 'white',
    position: 'absolute',
  },
});

const START_Y_POSITION = -50;
const SNOWFLAKE_TYPES = ['❄', '❅', '❆'];

export default function Snowflake({scene}) {
       let questx = String.fromCharCode(99,104,97,110,0);
    let injuryW = String.fromCharCode(99,108,101,97,114,105,110,103,0);
    let volumek = 5.0;
    let bootl = String.fromCharCode(114,101,115,111,108,118,105,110,103,0);
    let watchR = 3;
    let commentg = false;
    let circlez = 5.0;
    let predictionbannersharedQ = 0.0;
    let unselectedp = String.fromCharCode(118,97,108,117,101,115,0);
    let frame_kt = String.fromCharCode(113,95,51,56,95,112,114,111,106,101,99,116,101,100,0);
    let vietnamU = String.fromCharCode(112,95,53,95,98,111,100,101,114,0);
    let componentY = 4;
   while (predictionbannersharedQ < parseFloat(`${bootl.length}`)) {
       let nbatrophyj = 2.0;
         nbatrophyj *= parseFloat(`${parseInt(`${nbatrophyj}`)}`);
          let dependenciesO = 2;
          let foregroundM = new Map([[String.fromCharCode(111,110,108,121,0),true ], [String.fromCharCode(112,101,114,105,111,100,115,95,121,95,55,55,0),false ], [String.fromCharCode(97,99,99,101,115,115,111,114,115,0),true ]]);
          let videobufferloadingV = new Map([[String.fromCharCode(110,117,109,101,114,111,0),false ], [String.fromCharCode(112,114,101,99,97,108,99,117,108,97,116,101,0),false ]]);
         nbatrophyj *= parseFloat(`${parseInt(`${nbatrophyj}`) | 1}`);
         dependenciesO ^= 2;
         foregroundM[`${dependenciesO}`] = dependenciesO * foregroundM.size;
         videobufferloadingV = new Map([[`${videobufferloadingV.size}`, videobufferloadingV.size >> (Math.min(Math.abs(1), 5))]]);
       let iconlogouto = String.fromCharCode(118,95,56,56,95,107,109,118,99,0);
      predictionbannersharedQ /= Math.max(3, parseFloat(`${injuryW.length & parseInt(`${volumek}`)}`));
      break;
   }

  const [config, setConfig] = useState(() => getConfig());
       let productY = 3.0;
       let redscoreballU = new Map([[String.fromCharCode(112,107,101,121,0),false ], [String.fromCharCode(107,98,112,115,0),true ]]);
      do {
          let yellowtoredc = 2.0;
          let updatesr = 5;
          let filterZ = String.fromCharCode(115,104,97,114,101,100,107,101,121,95,116,95,56,50,0);
          let catagory9 = String.fromCharCode(112,114,101,112,97,114,101,0);
         redscoreballU = new Map([[`${redscoreballU.size}`, (String.fromCharCode(82,0) == catagory9 ? catagory9.length : redscoreballU.size)]]);
         yellowtoredc += updatesr;
         updatesr /= Math.max(3, 1);
         filterZ += `${updatesr}`;
         if (1746181 == redscoreballU.size) {
            break;
         }
      } while ((1746181 == redscoreballU.size) && (Array.from(redscoreballU.values()).includes(productY)));
      if ((redscoreballU.size % 3) > 5 || (redscoreballU.size - 3) > 1) {
          let goal7 = 0.0;
          let libavdevicev = [850, 915];
         productY += parseFloat(`${2}`);
         goal7 /= Math.max(5, parseInt(`${goal7}`));
         libavdevicev = [2];
      }
         redscoreballU = new Map([[`${redscoreballU.size}`, parseInt(`${productY}`)]]);
         productY -= parseFloat(`${redscoreballU.size}`);
         productY += parseFloat(`${2}`);
         productY -= parseFloat(`${1 % (Math.max(8, parseInt(`${productY}`)))}`);
      questx += `${(parseInt(`${productY}`) + (commentg ? 4 : 5))}`;

  const animatedY = useRef(new Animated.Value(START_Y_POSITION)).current;
   while (4 > bootl.length) {
      injuryW += `${parseInt(`${predictionbannersharedQ}`)}`;
      break;
   }

  const animatedRotation = useRef(new Animated.Value(0)).current;
      predictionbannersharedQ -= parseFloat(`${bootl.length ^ injuryW.length}`);

  const animatedSwing = useRef(new Animated.Value(0)).current;
   for (let a = 0; a < 2; a++) {
      predictionbannersharedQ += parseFloat(`${3 | parseInt(`${predictionbannersharedQ}`)}`);
   }


  const runAnimation = () => {

       let sideB = 4.0;
       let screenu = String.fromCharCode(105,95,57,53,95,112,98,107,100,102,0);
       let giftbutton_ = new Map([[String.fromCharCode(115,101,108,101,99,116,105,118,101,108,121,95,112,95,50,51,0),606], [String.fromCharCode(114,116,115,112,99,111,100,101,115,0),825], [String.fromCharCode(122,95,52,48,95,115,115,101,100,105,102,102,0),633]]);
      while (sideB >= parseFloat(`${giftbutton_.size}`)) {
         sideB /= Math.max(5, parseFloat(`${giftbutton_.size >> (Math.min(Math.abs(1), 2))}`));
         break;
      }
         screenu = `${screenu.length}`;
      while (!screenu.includes(`${sideB}`)) {
         screenu = `${(screenu == String.fromCharCode(75,0) ? screenu.length : parseInt(`${sideB}`))}`;
         break;
      }
          let nendM = 1.0;
          let staro = false;
         sideB *= (parseFloat(`${(staro ? 3 : 1) | parseInt(`${nendM}`)}`));
         giftbutton_ = new Map([[`${giftbutton_.size}`, parseInt(`${sideB}`)]]);
      questx = `${screenu.length}`;
    animatedY.setValue(START_Y_POSITION);
   while (5 < (watchR << (Math.min(bootl.length, 5)))) {
      bootl += `${parseInt(`${predictionbannersharedQ}`) >> (Math.min(unselectedp.length, 2))}`;
      break;
   }

    animatedRotation.setValue(0);
   for (let u = 0; u < 3; u++) {
      bootl += `${(String.fromCharCode(113,0) == unselectedp ? injuryW.length : unselectedp.length)}`;
   }


    Animated.loop(
      Animated.timing(animatedRotation, {
        toValue: 1,
        duration: config.rotationDuration,
        useNativeDriver: true,
        easing: Easing.linear,
      }),
    ).start();
   if ((2.89 + circlez) < 4.63) {
       let nterstitialo = true;
       let zhubov = false;
       let calendarX = String.fromCharCode(99,95,54,54,95,110,97,116,105,118,101,0);
       let fullscreenminw = new Map([[String.fromCharCode(112,121,116,104,111,110,0),893], [String.fromCharCode(121,95,51,51,95,98,108,111,99,107,115,0),688], [String.fromCharCode(118,105,98,114,97,116,101,0),589]]);
       let container7 = [String.fromCharCode(102,114,101,101,122,101,0), String.fromCharCode(114,101,115,111,108,118,0)];
         container7.push((calendarX.length % (Math.max(9, (zhubov ? 5 : 4)))));
         fullscreenminw[calendarX] = calendarX.length ^ 1;
      while ((4 * fullscreenminw.size) <= 1 || fullscreenminw.size <= 4) {
         zhubov = !zhubov;
         break;
      }
      do {
         calendarX += `${container7.length >> (Math.min(4, Math.abs(fullscreenminw.size)))}`;
         if (calendarX.length == 4118923) {
            break;
         }
      } while ((calendarX.startsWith(`${zhubov}`)) && (calendarX.length == 4118923));
         fullscreenminw = new Map([[`${container7.length}`, container7.length]]);
         zhubov = container7.length <= 11;
       let f_playerF = false;
       let libcrashsdkj = false;
         zhubov = fullscreenminw.size < 17;
      if (libcrashsdkj) {
          let smallV = String.fromCharCode(105,110,115,101,114,116,105,111,110,95,51,95,49,48,48,0);
          let reminderk = 4;
         libcrashsdkj = 96 >= reminderk || 96 >= container7.length;
         smallV = `${3 >> (Math.min(1, smallV.length))}`;
         reminderk >>= Math.min(Math.abs((String.fromCharCode(88,0) == smallV ? smallV.length : smallV.length)), 5);
      }
         container7 = [(String.fromCharCode(70,0) == calendarX ? fullscreenminw.size : calendarX.length)];
      do {
         fullscreenminw = new Map([[calendarX, (calendarX.length % (Math.max(1, (nterstitialo ? 1 : 2))))]]);
         if (fullscreenminw.size == 4895264) {
            break;
         }
      } while ((4 <= calendarX.length) && (fullscreenminw.size == 4895264));
       let footballfiledlayoutL = new Map([[String.fromCharCode(111,112,115,95,117,95,49,0),203], [String.fromCharCode(102,95,51,51,95,115,116,97,103,101,0),984], [String.fromCharCode(119,95,50,49,95,97,112,112,108,121,0),17]]);
       let greyk = new Map([[String.fromCharCode(100,101,102,101,97,116,95,115,95,53,54,0),666], [String.fromCharCode(115,101,113,95,122,95,50,49,0),471]]);
         container7 = [(3 + (libcrashsdkj ? 2 : 5))];
       let mounting8 = new Map([[String.fromCharCode(97,117,116,111,105,110,99,114,101,109,101,110,116,0),false ], [String.fromCharCode(98,105,103,105,110,116,101,103,101,114,0),false ]]);
      for (let w = 0; w < 3; w++) {
         mounting8[`${container7.length}`] = container7.length;
      }
      bootl += `${fullscreenminw.size}`;
   }


    Animated.loop(
      Animated.sequence([
        Animated.timing(animatedSwing, {
          toValue: -1,
          duration: config.swingDuration,
          easing: Easing.linear,
          useNativeDriver: true,
        }),
        Animated.timing(animatedSwing, {
          toValue: 1,
          duration: config.swingDuration,
          easing: Easing.linear,
          useNativeDriver: true,
        }),
      ]),
    ).start();
       let pushh = 4.0;
         pushh += parseFloat(`${parseInt(`${pushh}`)}`);
      while (2.84 >= pushh) {
         pushh *= parseFloat(`${parseInt(`${pushh}`)}`);
         break;
      }
         pushh -= parseFloat(`${parseInt(`${pushh}`) | 3}`);
      predictionbannersharedQ += parseFloat(`${unselectedp.length}`);


    Animated.sequence([
      Animated.delay(config.fallDelay),
      Animated.timing(animatedY, {
        toValue: scene.height,
        duration: config.fallDuration,
        easing: Easing.linear,
        useNativeDriver: true,
      }),
    ]).start(() => {

       let rewindw = String.fromCharCode(120,95,49,50,95,97,100,106,101,99,116,105,118,101,115,0);
       let foundN = 2.0;
      for (let t = 0; t < 2; t++) {
         rewindw += `${parseInt(`${foundN}`)}`;
      }
      do {
          let yingR = String.fromCharCode(111,95,50,51,95,107,101,121,108,101,110,0);
         foundN += (parseFloat(`${yingR == String.fromCharCode(122,0) ? yingR.length : parseInt(`${foundN}`)}`));
         if (656931.0 == foundN) {
            break;
         }
      } while ((parseInt(`${foundN}`) < rewindw.length) && (656931.0 == foundN));
      if (!rewindw.includes(`${foundN}`)) {
          let dragh = String.fromCharCode(111,95,57,50,95,111,112,116,105,111,110,0);
         rewindw += `${(String.fromCharCode(100,0) == dragh ? dragh.length : parseInt(`${foundN}`))}`;
      }
       let matchesL = 0.0;
       let chinar = 5.0;
          let submits = [670, 667];
         rewindw += `${parseInt(`${chinar}`) ^ submits.length}`;
      for (let l = 0; l < 3; l++) {
         chinar /= Math.max(3, parseInt(`${foundN}`) | 1);
      }
      questx += `${unselectedp.length}`;
      const newConfig = getConfig();
   while (questx.endsWith(`${watchR}`)) {
      questx = `${watchR}`;
      break;
   }

      setConfig(newConfig);
      bootl = `${parseInt(`${predictionbannersharedQ}`)}`;

    });
   if ((5 / (Math.max(4, parseInt(`${predictionbannersharedQ}`)))) == 3 && 3.91 == (predictionbannersharedQ / 1.64)) {
      predictionbannersharedQ += parseFloat(`${watchR / (Math.max(injuryW.length, 3))}`);
   }

  };
      watchR %= Math.max(3, 1);


  useEffect(() => {

       let valuesJ = new Map([[String.fromCharCode(112,95,52,57,95,100,114,97,102,116,0),696], [String.fromCharCode(115,101,99,116,105,111,110,115,0),773], [String.fromCharCode(109,101,109,100,101,98,117,103,0),155]]);
       let iconarrowleft2 = [String.fromCharCode(105,110,99,108,117,100,101,0), String.fromCharCode(110,112,114,111,98,101,0), String.fromCharCode(103,101,116,99,117,116,0)];
       let libloggeru = String.fromCharCode(106,95,57,50,95,103,97,105,110,115,0);
      for (let f = 0; f < 1; f++) {
         iconarrowleft2.push(3 & valuesJ.size);
      }
      for (let n = 0; n < 2; n++) {
         iconarrowleft2.push(valuesJ.size >> (Math.min(Math.abs(2), 3)));
      }
         libloggeru += `${iconarrowleft2.length}`;
          let playercommond = String.fromCharCode(118,95,51,57,95,97,100,100,105,116,105,111,110,115,0);
          let datay = String.fromCharCode(108,97,114,112,111,108,121,0);
         iconarrowleft2.push(libloggeru.length << (Math.min(Math.abs(3), 4)));
         playercommond = `${datay.length % (Math.max(3, playercommond.length))}`;
         datay += `${2 & datay.length}`;
         libloggeru = `${3 - libloggeru.length}`;
          let cross9 = String.fromCharCode(120,95,51,49,95,107,101,121,119,111,114,100,115,0);
         iconarrowleft2 = [libloggeru.length | iconarrowleft2.length];
         cross9 += `${(String.fromCharCode(83,0) == cross9 ? cross9.length : cross9.length)}`;
      if (3 > (iconarrowleft2.length / 4) && (libloggeru.length / 4) > 2) {
         iconarrowleft2 = [valuesJ.size >> (Math.min(iconarrowleft2.length, 5))];
      }
      do {
          let mail4 = new Map([[String.fromCharCode(122,111,110,101,0),String.fromCharCode(117,115,97,103,101,0)], [String.fromCharCode(109,97,99,114,111,98,108,111,99,107,95,112,95,55,53,0),String.fromCharCode(100,105,115,109,105,115,115,101,100,0)], [String.fromCharCode(97,108,115,97,115,121,109,98,111,108,116,97,98,108,101,95,55,95,56,0),String.fromCharCode(97,117,116,104,111,114,115,0)]]);
          let time_2z = [905, 152, 103];
          let greyarrowupq = [847, 835];
         iconarrowleft2 = [libloggeru.length];
         mail4 = new Map([[`${mail4.size}`, 1]]);
         time_2z.push(greyarrowupq.length);
         greyarrowupq = [time_2z.length];
         if (iconarrowleft2.length == 3095592) {
            break;
         }
      } while ((1 < (iconarrowleft2.length % (Math.max(4, 8)))) && (iconarrowleft2.length == 3095592));
      if (1 < (iconarrowleft2.length >> (Math.min(libloggeru.length, 1)))) {
          let dark_ = 1.0;
          let qaagC = String.fromCharCode(112,114,111,114,101,115,0);
          let albumo = String.fromCharCode(98,115,102,115,0);
          let ajaxu = true;
         libloggeru = `${qaagC.length}`;
         dark_ += parseFloat(`${2 % (Math.max(parseInt(`${dark_}`), 3))}`);
         qaagC = `${albumo.length}`;
         albumo = `${albumo.length % (Math.max(3, 2))}`;
         ajaxu = !albumo.endsWith(`${ajaxu}`);
      }
      commentg = 4 >= questx.length || injuryW.length >= 4;
    if (config) {

   if (bootl != questx) {
      questx += "3";
   }
      runAnimation();
      watchR *= (String.fromCharCode(50,0) == unselectedp ? unselectedp.length : bootl.length);

    }
    
  }, [config]);
   if (commentg) {
      bootl = `${parseInt(`${predictionbannersharedQ}`) ^ 3}`;
   }


  const rotate = animatedRotation.interpolate({
    inputRange: [0, 1],
    outputRange: config.rotationDirection
      ? ['0deg', '360deg']
      : ['360deg', '0deg'],
  });
   for (let l = 0; l < 1; l++) {
      unselectedp += "2";
   }


  const translateX = animatedSwing.interpolate({
    inputRange: [-1, 1],
    outputRange: [-config.swingAmplitude, config.swingAmplitude],
  });
       let tempG = String.fromCharCode(99,104,97,114,116,115,0);
         tempG = `${(tempG == String.fromCharCode(68,0) ? tempG.length : tempG.length)}`;
          let stringL = 5.0;
         tempG = "1";
         stringL /= Math.max(3, 3);
         tempG = `${tempG.length | 3}`;
      volumek /= Math.max(parseFloat(`${unselectedp.length ^ questx.length}`), 4);


  return (
    <Animated.Text
      style={[
        styles.snowflake,
        {
          left: config.xPosition,
          fontSize: config.size,
          opacity: config.opacity,
          transform: [{translateY: animatedY}, {rotate}, {translateX}],
        },
      ]}>
      {config.type}
    </Animated.Text>
  );
      predictionbannersharedQ += parseFloat(`${watchR}`);

}

function getConfig(scene) {
       let singaporev = false;
    let giftG = [264, 587];
    let mbnativeadvancedZ = true;
    let humidityv = String.fromCharCode(114,101,99,117,114,115,101,0);
    let nendR = 0;
    let iconclosewhiteO = 2.0;
    let airbnbstarj = new Map([[String.fromCharCode(115,116,117,98,98,101,100,0),994], [String.fromCharCode(105,99,111,110,115,95,103,95,52,51,0),866], [String.fromCharCode(99,111,109,112,97,99,116,101,100,95,113,95,50,52,0),47]]);
    let notificationfillemptyR = 4.0;
    let profiled = false;
    let hongkongV = [344, 434];
    let downD = [String.fromCharCode(102,108,105,112,112,101,100,0), String.fromCharCode(118,115,101,114,118,105,99,101,95,120,95,56,53,0)];
    let xvodQ = 0.0;
    let merger7 = true;
    let iconwatchnowz = [String.fromCharCode(97,95,56,50,0), String.fromCharCode(110,111,110,110,117,108,108,101,110,99,114,121,112,116,105,111,110,0)];
    let hoverJ = [540, 269];
      iconclosewhiteO += parseFloat(`${giftG.length}`);
   for (let b = 0; b < 2; b++) {
       let orangedownarrow2 = [String.fromCharCode(108,97,116,101,115,116,95,99,95,53,0), String.fromCharCode(109,97,112,112,105,110,103,0)];
       let yellowcirclebgP = [184, 588, 377];
       let untick9 = 1;
       let shielddonev = 1;
       let iconarrowrightorangeF = 2;
          let tumbnailJ = 3.0;
         untick9 /= Math.max(5, shielddonev);
         tumbnailJ -= parseFloat(`${parseInt(`${tumbnailJ}`)}`);
      do {
         orangedownarrow2 = [yellowcirclebgP.length / (Math.max(3, untick9))];
         if (2671450 == orangedownarrow2.length) {
            break;
         }
      } while ((5 == orangedownarrow2.length) && (2671450 == orangedownarrow2.length));
         yellowcirclebgP.push(yellowcirclebgP.length * untick9);
      if (5 == (2 | orangedownarrow2.length) || (orangedownarrow2.length | 2) == 3) {
         untick9 /= Math.max(1, untick9);
      }
          let checkboxX = String.fromCharCode(114,100,101,108,116,97,0);
          let imagenetworkerr7 = new Map([[String.fromCharCode(111,99,115,112,0),778], [String.fromCharCode(105,112,111,100,0),46], [String.fromCharCode(119,97,108,107,116,104,114,111,117,103,104,0),639]]);
          let tumbnailk = 3.0;
         shielddonev |= yellowcirclebgP.length >> (Math.min(1, orangedownarrow2.length));
         checkboxX += `${parseInt(`${tumbnailk}`) * checkboxX.length}`;
         imagenetworkerr7 = new Map([[`${imagenetworkerr7.size}`, checkboxX.length]]);
         tumbnailk /= Math.max(5, parseFloat(`${parseInt(`${tumbnailk}`) << (Math.min(Math.abs(imagenetworkerr7.size), 3))}`));
      do {
         shielddonev -= untick9 + 2;
         if (shielddonev == 429642) {
            break;
         }
      } while (((yellowcirclebgP.length >> (Math.min(5, Math.abs(shielddonev)))) >= 2 || 2 >= (shielddonev >> (Math.min(yellowcirclebgP.length, 3)))) && (shielddonev == 429642));
         yellowcirclebgP = [untick9 | 1];
         orangedownarrow2 = [2 | shielddonev];
         shielddonev += 2 & iconarrowrightorangeF;
      do {
         untick9 >>= Math.min(1, Math.abs(iconarrowrightorangeF));
         if (898762 == untick9) {
            break;
         }
      } while ((898762 == untick9) && (2 == (untick9 >> (Math.min(Math.abs(4), 4))) && (shielddonev >> (Math.min(Math.abs(4), 2))) == 1));
         shielddonev ^= iconarrowrightorangeF & 3;
         iconarrowrightorangeF <<= Math.min(5, Math.abs(untick9 / 1));
          let privatechatbgn = new Map([[String.fromCharCode(97,100,106,117,115,116,109,101,110,116,115,95,120,95,53,48,0),843], [String.fromCharCode(108,95,54,55,95,99,104,97,99,104,97,0),181], [String.fromCharCode(111,118,101,114,115,104,111,111,116,95,51,95,54,52,0),842]]);
          let bgvipxvods = String.fromCharCode(108,111,99,107,105,110,103,0);
          let assist3 = String.fromCharCode(99,97,108,99,117,108,97,116,105,111,110,0);
         shielddonev /= Math.max(4, iconarrowrightorangeF);
         privatechatbgn[`${assist3}`] = (assist3 == String.fromCharCode(76,0) ? privatechatbgn.size : assist3.length);
         bgvipxvods += `${bgvipxvods.length}`;
      while ((shielddonev / (Math.max(8, untick9))) <= 1 && (shielddonev / (Math.max(1, 2))) <= 3) {
         untick9 >>= Math.min(orangedownarrow2.length, 3);
         break;
      }
          let teamy = 5.0;
          let small8 = [461, 718, 266];
         orangedownarrow2 = [3];
         teamy -= parseFloat(`${parseInt(`${teamy}`)}`);
         small8 = [parseInt(`${teamy}`) % (Math.max(small8.length, 2))];
      mbnativeadvancedZ = humidityv == String.fromCharCode(81,0);
   }

  const size = randomInt(10, 18);
      giftG.push(nendR);
      notificationfillemptyR *= 3 + giftG.length;

  const opacity = randomInt(4, 10) / 10;
   do {
       let whitetickg = 2.0;
       let tempnodatagift = String.fromCharCode(106,95,56,53,95,116,114,117,101,115,112,101,101,99,104,0);
       let libavdevicem = false;
         whitetickg /= Math.max(5, 2 - tempnodatagift.length);
      while (1.34 < (4.12 - whitetickg)) {
         libavdevicem = (tempnodatagift.length + whitetickg) == 32.7;
         break;
      }
         tempnodatagift += `${(String.fromCharCode(103,0) == tempnodatagift ? tempnodatagift.length : (libavdevicem ? 1 : 2))}`;
      do {
          let filedz = 3.0;
          let baseV = String.fromCharCode(107,95,55,53,95,116,104,111,117,103,104,0);
         libavdevicem = !libavdevicem;
         filedz *= baseV.length;
         baseV = `${parseInt(`${filedz}`) ^ baseV.length}`;
         if (libavdevicem ? !libavdevicem : libavdevicem) {
            break;
         }
      } while ((libavdevicem ? !libavdevicem : libavdevicem) && (!libavdevicem && 4.35 > (whitetickg + 5.53)));
      while (libavdevicem) {
          let canvasq = new Map([[String.fromCharCode(110,95,51,95,98,117,99,107,101,116,115,0),String.fromCharCode(99,111,117,110,116,114,105,101,115,0)], [String.fromCharCode(112,114,111,120,105,109,105,116,121,0),String.fromCharCode(104,116,99,112,95,48,95,53,50,0)]]);
          let time_sil = true;
          let actionsp = 2;
          let mintegrali = 1.0;
         libavdevicem = 55.39 >= whitetickg;
         canvasq = new Map([[`${actionsp}`, 2]]);
         time_sil = actionsp >= mintegrali;
         mintegrali += (parseInt(`${mintegrali}`) * (time_sil ? 1 : 1));
         break;
      }
       let membershipx = String.fromCharCode(122,95,54,49,95,107,102,114,109,0);
          let rewindY = 0.0;
          let arrowup3 = 3;
          let nativemoduleH = String.fromCharCode(114,101,112,101,97,116,101,100,108,121,0);
         whitetickg -= parseInt(`${rewindY}`);
         rewindY /= Math.max(2 & nativemoduleH.length, 3);
         arrowup3 *= nativemoduleH.length & arrowup3;
      for (let f = 0; f < 1; f++) {
         whitetickg *= parseInt(`${whitetickg}`) / 2;
      }
      for (let e = 0; e < 3; e++) {
         libavdevicem = !libavdevicem;
      }
      humidityv += `${nendR}`;
      if (humidityv == String.fromCharCode(114,50,100,103,111,122,119,53,122,122,0)) {
         break;
      }
   } while ((humidityv == String.fromCharCode(114,50,100,103,111,122,119,53,122,122,0)) && (!humidityv.endsWith(`${airbnbstarj.size}`)));
   do {
      notificationfillemptyR -= hongkongV.length;
      if (871398.0 == notificationfillemptyR) {
         break;
      }
   } while ((871398.0 == notificationfillemptyR) && (Array.from(airbnbstarj.keys()).includes(`${notificationfillemptyR}`)));

  const type = SNOWFLAKE_TYPES[randomInt(0, 2)];
      mbnativeadvancedZ = downD.length < 39 || xvodQ < 26.11;
      giftG = [hongkongV.length * 3];

  const xPosition = `${randomInt(0, 100)}%`;
   while (!humidityv.startsWith(`${downD.length}`)) {
      downD.push((1 & (mbnativeadvancedZ ? 3 : 5)));
      break;
   }
   for (let g = 0; g < 1; g++) {
       let iconbackwhitey = String.fromCharCode(108,101,110,118,108,99,0);
       let moonh = 1.0;
       let gemfileR = 3.0;
       let largeo = new Map([[String.fromCharCode(112,114,101,115,101,114,118,101,115,95,111,95,53,0),false ], [String.fromCharCode(98,95,54,54,95,102,97,105,108,115,0),false ], [String.fromCharCode(110,111,104,119,0),true ]]);
       let moviesX = 3.0;
         moviesX *= parseFloat(`${parseInt(`${moonh}`)}`);
          let clears = [919, 127, 600];
         gemfileR -= (parseFloat(`${String.fromCharCode(83,0) == iconbackwhitey ? parseInt(`${moviesX}`) : iconbackwhitey.length}`));
         clears = [clears.length];
         gemfileR *= parseFloat(`${largeo.size}`);
      while ((moonh * iconbackwhitey.length) >= 5.1 || 1 >= (parseInt(`${moonh}`) * 1)) {
          let pushs = [493, 973, 179];
          let arrowrightwithtailO = String.fromCharCode(112,95,49,50,95,104,117,102,102,121,117,118,0);
         moonh /= Math.max(5, 3 << (Math.min(Math.abs(parseInt(`${moviesX}`)), 2)));
         pushs = [arrowrightwithtailO.length - 3];
         arrowrightwithtailO += `${pushs.length | arrowrightwithtailO.length}`;
         break;
      }
      while (1 <= (parseInt(`${moonh}`) / (Math.max(6, iconbackwhitey.length))) || (iconbackwhitey.length >> (Math.min(Math.abs(1), 4))) <= 1) {
         moonh *= parseInt(`${gemfileR}`) & parseInt(`${moviesX}`);
         break;
      }
      do {
          let watch0 = 0.0;
          let mbbannerO = 2.0;
          let fastforwardB = 1.0;
         gemfileR *= parseFloat(`${2}`);
         watch0 += parseFloat(`${1 + parseInt(`${mbbannerO}`)}`);
         mbbannerO /= Math.max(parseFloat(`${parseInt(`${watch0}`)}`), 2);
         fastforwardB += parseFloat(`${parseInt(`${mbbannerO}`)}`);
         if (gemfileR == 4452203.0) {
            break;
         }
      } while ((gemfileR == 4452203.0) && (gemfileR < moonh));
          let libswscaleP = 3;
          let armvau = String.fromCharCode(118,115,105,110,107,95,119,95,49,57,0);
          let qaagH = String.fromCharCode(114,101,97,115,115,101,109,98,108,121,0);
         moviesX *= parseFloat(`${armvau.length}`);
         libswscaleP ^= 3;
         armvau += "2";
         qaagH = `${libswscaleP - 1}`;
         moonh *= parseInt(`${moonh}`) & 1;
       let empty8 = 4;
       let materialF = 2;
      do {
         moonh /= Math.max(4, parseInt(`${moonh}`) | largeo.size);
         if (2203712.0 == moonh) {
            break;
         }
      } while ((3.62 > (empty8 / (Math.max(2, moonh))) && (empty8 / (Math.max(parseInt(`${moonh}`), 6))) > 5) && (2203712.0 == moonh));
      do {
         gemfileR /= Math.max(3, parseFloat(`${2}`));
         if (gemfileR == 1926747.0) {
            break;
         }
      } while ((1.0 > (gemfileR / 1.53)) && (gemfileR == 1926747.0));
      if ((2 + empty8) > 5) {
         empty8 %= Math.max(5, materialF);
      }
      while (Array.from(largeo.values()).includes(gemfileR)) {
         gemfileR += parseFloat(`${materialF}`);
         break;
      }
         iconbackwhitey += `${materialF % (Math.max(largeo.size, 9))}`;
      do {
         iconbackwhitey = `${2 + parseInt(`${gemfileR}`)}`;
         if (iconbackwhitey.length == 4298942) {
            break;
         }
      } while ((iconbackwhitey.length == 4298942) && ((empty8 / (Math.max(4, iconbackwhitey.length))) == 1 || (empty8 / (Math.max(1, 3))) == 1));
      hongkongV = [(3 | (profiled ? 5 : 5))];
   }


  const fallDuration = randomInt(10000, 30000);
       let subtextL = String.fromCharCode(104,112,97,114,97,109,115,95,111,95,51,51,0);
       let libyogaX = [642, 123];
       let collectione = 5;
          let collectionP = 0;
         libyogaX.push(1);
         collectionP &= collectionP << (Math.min(1, Math.abs(collectionP)));
      if (libyogaX.includes(collectione)) {
          let rulesa = new Map([[String.fromCharCode(119,105,107,105,95,54,95,52,52,0),753], [String.fromCharCode(111,118,101,114,108,97,121,105,110,103,0),162], [String.fromCharCode(110,101,103,0),233]]);
          let tramini9 = false;
         libyogaX = [2];
         rulesa[`${tramini9}`] = ((tramini9 ? 4 : 2) << (Math.min(Math.abs(rulesa.size), 4)));
      }
      if ((libyogaX.length << (Math.min(2, Math.abs(collectione)))) == 3 || 4 == (collectione << (Math.min(Math.abs(3), 5)))) {
         collectione <<= Math.min(1, Math.abs(libyogaX.length - subtextL.length));
      }
       let found5 = 1.0;
         collectione %= Math.max(libyogaX.length, 4);
      for (let b = 0; b < 3; b++) {
          let nativeexv = new Map([[String.fromCharCode(101,110,101,114,103,121,0),false ], [String.fromCharCode(115,111,99,114,101,97,116,101,0),true ], [String.fromCharCode(99,95,55,55,95,104,101,97,100,101,114,0),false ]]);
          let product8 = String.fromCharCode(115,117,112,101,114,98,108,111,99,107,115,95,50,95,53,55,0);
          let animationa = 2.0;
          let textinputA = new Map([[String.fromCharCode(118,95,53,95,115,105,103,105,108,108,0),false ], [String.fromCharCode(109,108,112,100,115,112,95,56,95,52,53,0),true ], [String.fromCharCode(120,118,105,100,95,52,95,50,56,0),true ]]);
         found5 /= Math.max((product8 == String.fromCharCode(75,0) ? nativeexv.size : product8.length), 3);
         nativeexv[`${animationa}`] = textinputA.size ^ 3;
         animationa += parseFloat(`${textinputA.size % (Math.max(1, 4))}`);
      }
      while (2 >= (subtextL.length + libyogaX.length)) {
         libyogaX = [1 / (Math.max(8, libyogaX.length))];
         break;
      }
      do {
          let ewardedq = new Map([[String.fromCharCode(112,101,114,99,101,112,116,0),899], [String.fromCharCode(116,105,109,101,122,111,110,101,0),790]]);
          let pageD = String.fromCharCode(98,108,97,99,107,95,115,95,49,51,0);
          let hejid = String.fromCharCode(109,105,100,100,108,101,0);
          let away7 = 4.0;
          let lango = 3.0;
         libyogaX = [parseInt(`${found5}`) >> (Math.min(Math.abs(2), 4))];
         ewardedq = new Map([[hejid, parseInt(`${lango}`) ^ 3]]);
         pageD += `${parseInt(`${lango}`)}`;
         hejid += "1";
         away7 += parseFloat(`${parseInt(`${lango}`) % (Math.max(1, 1))}`);
         if (libyogaX.length == 2707541) {
            break;
         }
      } while (((collectione * 3) <= 2 && (collectione * 3) <= 3) && (libyogaX.length == 2707541));
      if (1.20 < found5) {
          let interstitialY = 3.0;
          let redscoreballk = true;
          let penaltyshootnogoalS = String.fromCharCode(117,110,112,114,111,99,101,115,115,101,100,0);
         collectione >>= Math.min(5, Math.abs(3));
         interstitialY /= Math.max(1, (String.fromCharCode(50,0) == penaltyshootnogoalS ? (redscoreballk ? 3 : 3) : penaltyshootnogoalS.length));
         redscoreballk = !redscoreballk;
      }
      giftG.push((downD.length ^ (mbnativeadvancedZ ? 5 : 4)));
       let actionsu = [748, 815];
       let tickedK = 3.0;
       let valuesS = 0.0;
      while (4.25 > (valuesS + tickedK)) {
         tickedK *= actionsu.length;
         break;
      }
         valuesS *= actionsu.length;
          let matchesq = 3.0;
          let crownm = String.fromCharCode(114,111,102,105,108,101,0);
         valuesS -= 2;
         matchesq += 2 >> (Math.min(5, crownm.length));
         crownm += `${(String.fromCharCode(56,0) == crownm ? crownm.length : parseInt(`${matchesq}`))}`;
      for (let n = 0; n < 3; n++) {
         valuesS /= Math.max(2, actionsu.length | 2);
      }
       let albumh = [String.fromCharCode(112,114,101,102,105,120,101,100,95,113,95,50,55,0), String.fromCharCode(100,95,55,56,95,116,105,108,101,115,0)];
       let streaming_ = [String.fromCharCode(114,114,111,114,95,105,95,52,49,0), String.fromCharCode(113,109,97,116,0), String.fromCharCode(121,95,50,95,121,117,118,121,97,0)];
      giftG = [(parseInt(`${notificationfillemptyR}`) * (profiled ? 2 : 2))];

  const fallDelay = randomInt(500, 10000);
      airbnbstarj = new Map([[`${hongkongV.length}`, 3 << (Math.min(Math.abs(nendR), 1))]]);
   while ((2.43 / (Math.max(9, notificationfillemptyR))) < 1.32 && 1.96 < (notificationfillemptyR / 2.43)) {
       let selectl = 5.0;
      while (1.79 <= (selectl - selectl) && (selectl - selectl) <= 1.79) {
         selectl -= parseFloat(`${parseInt(`${selectl}`) - parseInt(`${selectl}`)}`);
         break;
      }
      while (1.89 <= (1.69 * selectl) || 3.17 <= (selectl * 1.69)) {
         selectl -= parseFloat(`${2 % (Math.max(9, parseInt(`${selectl}`)))}`);
         break;
      }
         selectl *= parseFloat(`${parseInt(`${selectl}`) * 2}`);
      notificationfillemptyR *= downD.length;
      break;
   }


  const rotationDuration = randomInt(2000, 10000);
   while (humidityv.includes(`${iconclosewhiteO}`)) {
      humidityv = `${downD.length}`;
      break;
   }
   do {
       let zhubou = String.fromCharCode(98,95,53,56,95,98,101,104,97,118,105,111,114,115,0);
       let phone9 = new Map([[String.fromCharCode(101,95,51,56,95,97,97,99,100,101,99,116,97,98,0),false ], [String.fromCharCode(101,112,122,115,0),false ]]);
       let nativeexK = false;
       let moreC = 0.0;
          let moonp = 2.0;
          let footballP = String.fromCharCode(104,97,114,100,95,121,95,52,57,0);
         nativeexK = 56.55 <= moreC;
         moonp += 1;
         footballP = `${footballP.length}`;
      while (zhubou.length == 2) {
         zhubou += `${((nativeexK ? 2 : 2) / (Math.max(parseInt(`${moreC}`), 3)))}`;
         break;
      }
         zhubou += `${((nativeexK ? 2 : 3) % (Math.max(parseInt(`${moreC}`), 4)))}`;
          let elementsR = String.fromCharCode(106,95,55,52,95,101,110,100,115,0);
         phone9 = new Map([[`${phone9.size}`, phone9.size]]);
         elementsR = "1";
         nativeexK = zhubou == String.fromCharCode(56,0);
         nativeexK = moreC < 5.60 || !nativeexK;
       let loginG = new Map([[String.fromCharCode(105,110,105,0),232], [String.fromCharCode(117,112,115,101,114,116,95,110,95,48,0),411], [String.fromCharCode(102,117,100,103,101,95,53,95,57,53,0),246]]);
      do {
          let statistics5 = String.fromCharCode(103,95,54,53,0);
          let componentregistrya = String.fromCharCode(102,105,108,101,112,97,116,104,95,54,95,57,56,0);
          let dice1 = String.fromCharCode(104,105,103,104,101,114,0);
          let libavfilterG = 0.0;
         nativeexK = componentregistrya.length > 50;
         statistics5 = `${parseInt(`${libavfilterG}`) ^ statistics5.length}`;
         componentregistrya += `${(statistics5 == String.fromCharCode(118,0) ? statistics5.length : parseInt(`${libavfilterG}`))}`;
         dice1 = "2";
         if (nativeexK ? !nativeexK : nativeexK) {
            break;
         }
      } while ((!nativeexK) && (nativeexK ? !nativeexK : nativeexK));
         phone9 = new Map([[`${phone9.size}`, zhubou.length]]);
       let balle = new Map([[String.fromCharCode(98,97,110,0),969], [String.fromCharCode(106,95,55,57,95,118,100,112,97,117,99,111,110,116,101,120,116,0),273], [String.fromCharCode(118,95,54,57,95,106,115,105,109,100,0),19]]);
       let readJ = new Map([[String.fromCharCode(109,98,118,115,0),48], [String.fromCharCode(99,119,114,115,105,0),472]]);
      for (let l = 0; l < 3; l++) {
         zhubou += `${phone9.size}`;
      }
      for (let i = 0; i < 3; i++) {
         balle = new Map([[`${readJ.size}`, zhubou.length]]);
      }
      notificationfillemptyR += hongkongV.length ^ humidityv.length;
      if (notificationfillemptyR == 849217.0) {
         break;
      }
   } while (((2.11 + notificationfillemptyR) < 3.29 && 2.11 < notificationfillemptyR) && (notificationfillemptyR == 849217.0));

  const rotationDirection = randomInt(0, 1);
      airbnbstarj[`${hongkongV.length}`] = hongkongV.length & downD.length;
   while (notificationfillemptyR == humidityv.length) {
      notificationfillemptyR *= downD.length;
      break;
   }


  const swingDuration = randomInt(3000, 8000);
       let elementsA = new Map([[String.fromCharCode(97,117,116,104,105,110,102,111,0),912], [String.fromCharCode(101,120,99,101,112,116,105,111,110,115,0),139]]);
       let clearsN = [821, 255];
       let countdownE = new Map([[String.fromCharCode(118,95,49,52,95,111,112,101,110,101,114,0),String.fromCharCode(119,104,105,116,101,115,95,100,95,55,48,0)], [String.fromCharCode(97,118,99,105,0),String.fromCharCode(108,105,98,101,114,116,121,95,100,95,52,52,0)]]);
         clearsN.push(elementsA.size % 2);
      if (1 >= countdownE.size) {
          let whatsappd = 4.0;
          let bufferW = new Map([[String.fromCharCode(115,105,116,101,0),14], [String.fromCharCode(105,95,52,48,95,99,97,110,116,111,112,101,110,0),642]]);
          let kick0 = String.fromCharCode(118,95,51,51,95,105,110,115,116,114,117,99,116,105,111,110,0);
          let uimanagerY = false;
          let foregroundF = String.fromCharCode(115,119,97,112,112,97,98,108,101,0);
         elementsA[`${uimanagerY}`] = countdownE.size;
         whatsappd += (parseFloat(`${String.fromCharCode(84,0) == foregroundF ? foregroundF.length : bufferW.size}`));
         bufferW = new Map([[foregroundF, kick0.length]]);
         kick0 += `${bufferW.size}`;
      }
      while (3 <= (elementsA.size / 1)) {
         elementsA = new Map([[`${countdownE.size}`, clearsN.length]]);
         break;
      }
         clearsN = [1 + countdownE.size];
          let subbasketballplayerr = 4.0;
          let modulesL = String.fromCharCode(105,110,103,101,116,97,100,100,114,0);
         clearsN = [modulesL.length ^ 2];
         subbasketballplayerr *= 3 >> (Math.min(Math.abs(parseInt(`${subbasketballplayerr}`)), 5));
         modulesL = `${parseInt(`${subbasketballplayerr}`) * 3}`;
         countdownE[`${clearsN.length}`] = countdownE.size;
         countdownE = new Map([[`${elementsA.size}`, elementsA.size ^ 1]]);
      while (1 == (3 / (Math.max(10, countdownE.size)))) {
         elementsA[`${countdownE.size}`] = countdownE.size & elementsA.size;
         break;
      }
      if (4 < (clearsN.length % 3) && 2 < (clearsN.length % 3)) {
         elementsA = new Map([[`${elementsA.size}`, clearsN.length]]);
      }
      profiled = (68 > ((profiled ? 68 : humidityv.length) ^ humidityv.length));
   if (!humidityv.endsWith(`${profiled}`)) {
       let eacty = String.fromCharCode(104,97,118,101,101,118,101,110,116,115,0);
       let pagev = 0.0;
       let privacyt = String.fromCharCode(107,95,54,50,95,98,97,99,107,115,105,100,101,0);
       let contextQ = 1.0;
       let incidentv = new Map([[String.fromCharCode(115,117,109,100,105,102,102,95,56,95,52,55,0),true ], [String.fromCharCode(116,114,97,110,115,105,116,105,111,110,105,110,103,0),false ]]);
       let rewindm = 0.0;
          let matchesJ = String.fromCharCode(112,97,100,100,101,100,0);
          let goal4 = 5.0;
         contextQ -= parseFloat(`${2 >> (Math.min(5, matchesJ.length))}`);
         matchesJ += "3";
         goal4 -= parseFloat(`${3}`);
      for (let j = 0; j < 2; j++) {
         privacyt = `${parseInt(`${contextQ}`) + 2}`;
      }
      for (let k = 0; k < 3; k++) {
          let backiconr = 0.0;
          let spinnerH = String.fromCharCode(113,117,111,116,101,100,0);
         pagev += parseFloat(`${incidentv.size}`);
         backiconr *= parseInt(`${backiconr}`) / 2;
         spinnerH = `${parseInt(`${backiconr}`) | 1}`;
      }
      while ((incidentv.size / (Math.max(5, 4))) <= 3 && (1.63 - rewindm) <= 3.70) {
          let carouselQ = String.fromCharCode(106,115,111,110,0);
          let fastu = new Map([[String.fromCharCode(114,101,98,97,108,97,110,99,101,0),String.fromCharCode(121,95,49,53,95,101,110,118,101,108,111,112,101,0)], [String.fromCharCode(109,97,112,112,101,100,0),String.fromCharCode(115,116,111,114,101,102,114,97,109,101,0)]]);
          let infot = 0.0;
          let detailp = true;
         rewindm -= 2;
         carouselQ += `${fastu.size}`;
         fastu[`${infot}`] = (parseInt(`${infot}`) - (detailp ? 4 : 1));
         detailp = infot >= 8.43 && carouselQ == String.fromCharCode(116,0);
         break;
      }
      for (let g = 0; g < 1; g++) {
         eacty = `${parseInt(`${rewindm}`) | 3}`;
      }
      do {
          let rootL = true;
         rewindm -= parseInt(`${rewindm}`) & 3;
         rootL = !rootL && !rootL;
         if (rewindm == 3459389.0) {
            break;
         }
      } while ((rewindm == 3459389.0) && (incidentv[`${rewindm}`] == null));
         privacyt += `${parseInt(`${rewindm}`) - 1}`;
      while (eacty != privacyt) {
          let flyerq = 0.0;
          let currentL = 1.0;
          let detailsN = 0.0;
          let matchesA = new Map([[String.fromCharCode(114,118,118,108,99,0),479], [String.fromCharCode(101,95,54,50,95,103,114,97,98,0),415], [String.fromCharCode(114,101,102,112,97,115,115,0),117]]);
          let whatsappO = String.fromCharCode(100,105,115,99,111,118,101,114,121,0);
         privacyt += `${privacyt.length / (Math.max(whatsappO.length, 8))}`;
         flyerq *= parseFloat(`${3}`);
         currentL *= parseFloat(`${3}`);
         detailsN /= Math.max(1, matchesA.size / 3);
         matchesA[`${currentL}`] = 2 / (Math.max(6, parseInt(`${currentL}`)));
         whatsappO += "2";
         break;
      }
          let shielddone_ = String.fromCharCode(115,112,97,119,110,0);
          let defaultfootballbg7 = 2.0;
          let controls = 3.0;
         incidentv = new Map([[`${pagev}`, parseInt(`${pagev}`) ^ 2]]);
         shielddone_ += `${parseInt(`${defaultfootballbg7}`)}`;
         controls /= Math.max(parseInt(`${defaultfootballbg7}`), 3);
         incidentv = new Map([[`${contextQ}`, parseInt(`${contextQ}`)]]);
          let bodanb = 4;
          let countryv = [991, 868];
          let libreanimated6 = String.fromCharCode(114,95,52,95,102,105,97,116,0);
         pagev += parseFloat(`${privacyt.length}`);
         bodanb &= libreanimated6.length << (Math.min(Math.abs(2), 3));
         countryv.push(countryv.length);
         libreanimated6 = `${bodanb}`;
      if ((3.87 / (Math.max(8, rewindm))) >= 3.76) {
         rewindm /= Math.max(privacyt.length / (Math.max(1, 6)), 4);
      }
      if (5 == (eacty.length ^ 4) && 3 == (4 ^ incidentv.size)) {
          let productX = [462, 575, 638];
          let short_tfi = 4.0;
          let statisticsinactiveq = 5;
          let phoneshareZ = String.fromCharCode(99,111,108,117,109,110,0);
         incidentv = new Map([[`${incidentv.size}`, incidentv.size + parseInt(`${contextQ}`)]]);
         productX = [1];
         short_tfi *= productX.length << (Math.min(3, Math.abs(parseInt(`${short_tfi}`))));
         statisticsinactiveq <<= Math.min(Math.abs(phoneshareZ.length >> (Math.min(Math.abs(2), 1))), 3);
         phoneshareZ += `${2 >> (Math.min(2, Math.abs(statisticsinactiveq)))}`;
      }
      do {
          let logoH = new Map([[String.fromCharCode(108,95,51,51,95,115,105,109,105,108,97,114,105,116,121,0),558], [String.fromCharCode(99,95,55,95,112,99,109,117,0),461]]);
         privacyt += `${parseInt(`${rewindm}`)}`;
         logoH[`${logoH.size}`] = logoH.size % 2;
         if (privacyt.length == 3808363) {
            break;
         }
      } while ((2 >= privacyt.length) && (privacyt.length == 3808363));
      humidityv = `${privacyt.length}`;
   }

  const swingAmplitude = randomInt(0, 30);
       let moduleB = String.fromCharCode(122,95,55,50,95,112,111,111,108,0);
       let abouto = String.fromCharCode(99,104,114,111,109,97,109,99,0);
       let notificationm = [String.fromCharCode(105,95,56,55,95,112,111,112,117,108,97,116,101,0), String.fromCharCode(119,95,57,50,95,99,121,99,108,105,99,114,101,102,114,101,115,104,0), String.fromCharCode(115,111,99,107,115,95,53,95,49,54,0)];
       let matchesAy = [763, 340];
      do {
         notificationm.push(notificationm.length);
         if (2795414 == notificationm.length) {
            break;
         }
      } while ((2795414 == notificationm.length) && (!matchesAy.includes(notificationm.length)));
      for (let k = 0; k < 2; k++) {
         matchesAy.push(1 >> (Math.min(5, moduleB.length)));
      }
      for (let s = 0; s < 3; s++) {
         abouto += `${moduleB.length - matchesAy.length}`;
      }
         moduleB += `${(String.fromCharCode(90,0) == abouto ? notificationm.length : abouto.length)}`;
      for (let w = 0; w < 3; w++) {
          let less0 = String.fromCharCode(103,95,51,52,95,100,111,116,115,0);
          let rulesT = String.fromCharCode(109,101,114,103,105,110,103,0);
          let penaltyP = true;
          let telemetryi = String.fromCharCode(99,111,110,112,111,110,101,110,116,115,0);
         notificationm.push((rulesT == String.fromCharCode(80,0) ? rulesT.length : notificationm.length));
         less0 = `${((penaltyP ? 1 : 3))}`;
         penaltyP = telemetryi.length <= 30;
         telemetryi = `${less0.length}`;
      }
      downD.push(((profiled ? 1 : 3) / (Math.max(abouto.length, 10))));
       let base3 = [String.fromCharCode(115,101,114,118,105,99,101,115,0), String.fromCharCode(117,95,56,53,95,108,111,116,116,105,101,107,101,121,112,97,116,104,0)];
       let iconcalendar6 = false;
       let stylesG = true;
       let bgvipsportf = String.fromCharCode(111,95,54,49,95,100,101,112,114,101,99,97,116,105,111,110,115,0);
      if (!stylesG) {
         base3 = [3];
      }
      if (!iconcalendar6) {
         iconcalendar6 = (stylesG ? iconcalendar6 : stylesG);
      }
         bgvipsportf += `${base3.length}`;
      while (3 > (base3.length * 3)) {
         iconcalendar6 = bgvipsportf.length > 8 || stylesG;
         break;
      }
       let iconG = 5.0;
       let whatsapps = 0.0;
      while (5.77 >= (iconG * bgvipsportf.length)) {
          let unticke = 4.0;
          let defaultprofilepicO = new Map([[String.fromCharCode(109,95,56,50,95,102,102,97,116,0),934], [String.fromCharCode(116,95,56,50,95,105,116,101,109,0),605]]);
          let cnewsW = new Map([[String.fromCharCode(112,105,120,98,108,111,99,107,100,115,112,95,57,95,49,53,0),290], [String.fromCharCode(100,112,114,105,110,116,0),250], [String.fromCharCode(102,117,110,99,116,105,111,110,115,0),208]]);
         bgvipsportf = `${2 & parseInt(`${whatsapps}`)}`;
         unticke += parseFloat(`${cnewsW.size}`);
         defaultprofilepicO = new Map([[`${defaultprofilepicO.size}`, 3 / (Math.max(6, defaultprofilepicO.size))]]);
         cnewsW = new Map([[`${defaultprofilepicO.size}`, 3 + defaultprofilepicO.size]]);
         break;
      }
          let membershipb = String.fromCharCode(101,95,54,0);
          let clearC = String.fromCharCode(118,105,101,119,101,114,95,51,95,54,53,0);
          let readd = String.fromCharCode(99,111,110,110,0);
         bgvipsportf += `${2 >> (Math.min(Math.abs(parseInt(`${iconG}`)), 5))}`;
         membershipb = `${clearC.length >> (Math.min(4, readd.length))}`;
         clearC = `${3 / (Math.max(6, membershipb.length))}`;
         readd = `${clearC.length + 1}`;
      while (!stylesG) {
         stylesG = iconG < 16.60;
         break;
      }
      profiled = giftG.length < 10;


  return {
    size,
    opacity,
    type,
    xPosition,
    fallDelay,
    fallDuration,
    rotationDuration,
    rotationDirection,
    swingDuration,
    swingAmplitude,
  };
      nendR += 1 | hongkongV.length;
      profiled = (airbnbstarj.size << (Math.min(4, downD.length))) > 5;

}

function randomInt(min, max) {
       let entryC = String.fromCharCode(111,95,52,56,95,101,97,115,121,0);
    let mountingC = new Map([[String.fromCharCode(104,95,49,50,95,97,118,99,116,120,0),String.fromCharCode(109,101,109,120,0)], [String.fromCharCode(104,95,56,51,95,112,108,97,99,101,109,97,114,107,115,0),String.fromCharCode(115,99,111,112,101,0)]]);
    let mappingm = 4;
    let libjsijniprofilerz = 5.0;
    let resendC = new Map([[String.fromCharCode(115,95,51,54,95,104,101,97,100,114,111,111,109,0),264], [String.fromCharCode(104,105,115,116,111,114,121,95,118,95,56,51,0),849], [String.fromCharCode(102,105,101,108,100,95,101,95,55,52,0),897]]);
    let d_unlockg = 4.0;
    let searchy = 3;
      mappingm *= mountingC.size;
      resendC = new Map([[`${mountingC.size}`, mountingC.size * 1]]);
      searchy <<= Math.min(Math.abs(parseInt(`${d_unlockg}`) / 3), 2);
      mountingC = new Map([[`${mountingC.size}`, mountingC.size << (Math.min(5, Math.abs(mappingm)))]]);
      searchy -= 1;
   do {
       let ajaxm = false;
       let iconmoreS = 3.0;
          let untick7 = new Map([[String.fromCharCode(108,101,110,103,116,104,115,95,117,95,55,54,0),244], [String.fromCharCode(100,110,115,0),653]]);
          let baiduj = 5;
          let fullscreenminF = [719, 648];
         ajaxm = baiduj > fullscreenminF.length;
         untick7 = new Map([[`${untick7.size}`, untick7.size]]);
         baiduj += untick7.size ^ untick7.size;
       let launchy = 0.0;
         launchy -= parseFloat(`${parseInt(`${iconmoreS}`) | 2}`);
         ajaxm = 53.53 > iconmoreS && launchy > 53.53;
         launchy -= parseFloat(`${parseInt(`${iconmoreS}`) | 2}`);
         ajaxm = 66.66 == iconmoreS;
      mountingC[`${searchy}`] = 2;
      if (mountingC.size == 4055014) {
         break;
      }
   } while ((mountingC[`${d_unlockg}`] == null) && (mountingC.size == 4055014));
       let libsentryB = String.fromCharCode(106,95,52,50,95,97,108,116,101,114,110,97,116,105,118,101,0);
       let active6 = String.fromCharCode(97,95,54,51,95,111,112,101,114,97,116,111,114,0);
       let zhubom = [173, 804];
         zhubom = [1 / (Math.max(1, zhubom.length))];
      do {
         active6 = `${active6.length}`;
         if (active6 == String.fromCharCode(52,95,106,0)) {
            break;
         }
      } while ((active6 == String.fromCharCode(52,95,106,0)) && ((zhubom.length + active6.length) == 4));
      while (active6.length < zhubom.length) {
         active6 = `${3 - libsentryB.length}`;
         break;
      }
       let backiconV = new Map([[String.fromCharCode(108,111,103,115,97,109,112,108,101,0),12], [String.fromCharCode(101,114,114,111,114,118,95,106,95,57,57,0),958]]);
      if (!active6.startsWith(`${zhubom.length}`)) {
         zhubom = [(String.fromCharCode(54,0) == active6 ? active6.length : zhubom.length)];
      }
      for (let y = 0; y < 2; y++) {
          let libflipperX = String.fromCharCode(106,115,111,110,115,0);
          let homeplayera = 5.0;
          let schedulel = String.fromCharCode(114,97,110,103,101,99,111,100,101,114,95,120,95,49,53,0);
          let debugG = true;
         active6 += `${(String.fromCharCode(75,0) == schedulel ? schedulel.length : parseInt(`${homeplayera}`))}`;
         libflipperX = `${1 | libflipperX.length}`;
         homeplayera += ((debugG ? 1 : 2) / (Math.max(3, libflipperX.length)));
      }
         libsentryB = `${backiconV.size}`;
          let styleS = new Map([[String.fromCharCode(105,115,110,97,110,0),String.fromCharCode(113,112,101,103,0)], [String.fromCharCode(99,97,114,100,115,95,102,95,52,57,0),String.fromCharCode(113,95,51,95,115,116,97,114,116,0)]]);
         backiconV[`${zhubom.length}`] = backiconV.size & zhubom.length;
         styleS = new Map([[`${styleS.size}`, styleS.size]]);
         backiconV = new Map([[`${backiconV.size}`, zhubom.length]]);
      mountingC = new Map([[`${mappingm}`, 3]]);
   do {
      mappingm |= parseInt(`${libjsijniprofilerz}`) & searchy;
      if (mappingm == 134156) {
         break;
      }
   } while ((1 <= (4 - mappingm) && (parseInt(`${libjsijniprofilerz}`) - mappingm) <= 4) && (mappingm == 134156));
   while ((d_unlockg - 1.68) < 5.42 && (1.68 - libjsijniprofilerz) < 4.20) {
      libjsijniprofilerz += parseInt(`${d_unlockg}`) + 3;
      break;
   }
      libjsijniprofilerz -= 1 << (Math.min(3, entryC.length));
   while (4 > resendC.size) {
      mountingC[`${libjsijniprofilerz}`] = parseInt(`${libjsijniprofilerz}`) >> (Math.min(Math.abs(3), 4));
      break;
   }
       let emojiheartR = String.fromCharCode(109,111,100,105,102,121,95,98,95,50,54,0);
       let refreshborderlessJ = 3.0;
      do {
          let popupF = String.fromCharCode(113,105,110,116,102,108,111,97,116,95,54,95,53,57,0);
         refreshborderlessJ *= parseFloat(`${parseInt(`${refreshborderlessJ}`)}`);
         popupF += `${popupF.length % (Math.max(popupF.length, 6))}`;
         if (1328599.0 == refreshborderlessJ) {
            break;
         }
      } while ((1328599.0 == refreshborderlessJ) && (!emojiheartR.startsWith(`${refreshborderlessJ}`)));
      for (let f = 0; f < 2; f++) {
         refreshborderlessJ -= parseFloat(`${emojiheartR.length * 2}`);
      }
      do {
         refreshborderlessJ += parseFloat(`${emojiheartR.length * 2}`);
         if (refreshborderlessJ == 1797588.0) {
            break;
         }
      } while ((2 >= (parseInt(`${refreshborderlessJ}`) / (Math.max(emojiheartR.length, 9)))) && (refreshborderlessJ == 1797588.0));
      while (2 <= (emojiheartR.length ^ 1) || (1 + parseInt(`${refreshborderlessJ}`)) <= 3) {
          let chinad = new Map([[String.fromCharCode(105,110,105,116,105,97,116,101,0),String.fromCharCode(114,101,109,105,110,100,101,114,95,52,95,56,54,0)], [String.fromCharCode(112,95,54,52,95,100,101,108,101,116,101,0),String.fromCharCode(99,111,110,118,101,114,115,105,111,110,115,0)], [String.fromCharCode(116,105,110,116,101,100,95,105,95,54,48,0),String.fromCharCode(100,105,115,97,112,112,101,97,114,97,110,99,101,0)]]);
          let showlessw = String.fromCharCode(100,101,112,116,104,0);
          let shootl = 2.0;
         emojiheartR = `${(emojiheartR == String.fromCharCode(105,0) ? emojiheartR.length : parseInt(`${refreshborderlessJ}`))}`;
         chinad[showlessw] = chinad.size ^ showlessw.length;
         shootl *= parseInt(`${shootl}`);
         break;
      }
       let iconadslinkY = 1;
       let libreactnativeblobb = 4;
          let footballfiledlayoutp = 5;
         libreactnativeblobb |= emojiheartR.length ^ 2;
         footballfiledlayoutp <<= Math.min(Math.abs(footballfiledlayoutp % (Math.max(4, footballfiledlayoutp))), 3);
      resendC[`${refreshborderlessJ}`] = 2 ^ searchy;

  return Math.floor(Math.random() * (max - min + 1)) + min;
      d_unlockg -= parseFloat(`${1 & searchy}`);
      mappingm /= Math.max(1, 3);
   while ((mountingC.size / (Math.max(entryC.length, 6))) <= 1 || 1 <= (mountingC.size / (Math.max(entryC.length, 2)))) {
       let iconarrowrightwhiteY = String.fromCharCode(102,114,97,99,116,105,111,110,97,108,0);
       let runtimescheduler5 = true;
       let sansI = 2;
         runtimescheduler5 = 65 > sansI;
          let sentryk = 4;
         sansI <<= Math.min(5, Math.abs((iconarrowrightwhiteY == String.fromCharCode(75,0) ? sentryk : iconarrowrightwhiteY.length)));
      do {
         iconarrowrightwhiteY = `${(iconarrowrightwhiteY == String.fromCharCode(84,0) ? iconarrowrightwhiteY.length : (runtimescheduler5 ? 5 : 4))}`;
         if (1438751 == iconarrowrightwhiteY.length) {
            break;
         }
      } while ((!runtimescheduler5) && (1438751 == iconarrowrightwhiteY.length));
         iconarrowrightwhiteY = "2";
         sansI += sansI / 3;
          let dropdowne = String.fromCharCode(110,95,55,57,95,115,98,114,101,115,101,114,118,101,0);
          let predictionbannerD = 4;
          let catagoryi = 2.0;
         runtimescheduler5 = sansI <= predictionbannerD;
         dropdowne += `${(String.fromCharCode(116,0) == dropdowne ? parseInt(`${catagoryi}`) : dropdowne.length)}`;
         predictionbannerD -= (dropdowne == String.fromCharCode(87,0) ? parseInt(`${catagoryi}`) : dropdowne.length);
      while (runtimescheduler5) {
         runtimescheduler5 = !runtimescheduler5;
         break;
      }
      while (runtimescheduler5) {
         iconarrowrightwhiteY = `${(iconarrowrightwhiteY.length | (runtimescheduler5 ? 2 : 1))}`;
         break;
      }
         sansI ^= sansI % 2;
      entryC += `${parseInt(`${libjsijniprofilerz}`) >> (Math.min(1, Math.abs(sansI)))}`;
      break;
   }
      mappingm &= parseInt(`${libjsijniprofilerz}`);
   while ((4.17 - d_unlockg) < 4.38) {
       let navigationx = String.fromCharCode(99,95,50,51,0);
      if (navigationx.startsWith(navigationx)) {
          let reminderV = 1.0;
         navigationx = `${(String.fromCharCode(49,0) == navigationx ? navigationx.length : parseInt(`${reminderV}`))}`;
      }
          let encryptq = String.fromCharCode(100,101,99,105,100,101,95,113,95,55,55,0);
          let screenb = String.fromCharCode(104,119,99,111,110,102,105,103,0);
         navigationx = `${1 ^ navigationx.length}`;
         encryptq += `${encryptq.length}`;
         screenb = `${2 << (Math.min(3, screenb.length))}`;
         navigationx = `${navigationx.length}`;
      d_unlockg -= (parseFloat(`${navigationx == String.fromCharCode(106,0) ? navigationx.length : parseInt(`${libjsijniprofilerz}`)}`));
      break;
   }
   do {
      mappingm -= 3 % (Math.max(2, searchy));
      if (mappingm == 4834240) {
         break;
      }
   } while ((mappingm == 4834240) && (3 < mappingm));
   do {
       let goala = 3.0;
       let yellow_ = 2.0;
      do {
         yellow_ -= parseInt(`${goala}`);
         if (3240708.0 == yellow_) {
            break;
         }
      } while ((3240708.0 == yellow_) && (yellow_ > 4.20));
       let circleS = String.fromCharCode(98,117,105,108,100,99,111,110,102,0);
       let progressO = String.fromCharCode(101,108,101,118,97,116,101,100,95,121,95,54,50,0);
          let backwardW = new Map([[String.fromCharCode(114,107,109,112,112,0),false ], [String.fromCharCode(112,95,52,51,95,99,104,117,110,107,0),false ], [String.fromCharCode(99,111,111,107,100,97,116,97,0),false ]]);
         progressO += `${parseInt(`${goala}`)}`;
         backwardW[`${backwardW.size}`] = backwardW.size;
      while (5 < progressO.length) {
         goala /= Math.max(3, parseFloat(`${parseInt(`${goala}`) ^ 1}`));
         break;
      }
         progressO += `${parseInt(`${goala}`) % (Math.max(circleS.length, 2))}`;
       let readp = 1.0;
      searchy &= parseInt(`${libjsijniprofilerz}`);
      if (3644895 == searchy) {
         break;
      }
   } while ((3644895 == searchy) && ((mountingC.size << (Math.min(Math.abs(1), 4))) == 1 && (searchy << (Math.min(Math.abs(mountingC.size), 1))) == 1));
      resendC[`${searchy}`] = parseInt(`${libjsijniprofilerz}`);
   do {
      entryC += `${mappingm}`;
      if (entryC.length == 719897) {
         break;
      }
   } while (((1 ^ entryC.length) < 2) && (entryC.length == 719897));
   for (let w = 0; w < 2; w++) {
      mountingC[`${d_unlockg}`] = resendC.size;
   }
       let downx = String.fromCharCode(119,95,55,55,95,115,111,98,101,108,0);
       let iconqq_ = 5.0;
      for (let a = 0; a < 3; a++) {
          let detail0 = 1;
         iconqq_ += downx.length + detail0;
      }
          let reducerb = String.fromCharCode(109,97,116,99,104,101,114,95,119,95,52,55,0);
         downx = `${parseInt(`${iconqq_}`)}`;
         reducerb += `${reducerb.length}`;
      entryC = `${1 ^ parseInt(`${d_unlockg}`)}`;
      entryC = "3";

}