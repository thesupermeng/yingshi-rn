import React from 'react';
import {Image, Dimensions, StyleSheet, View} from 'react-native';

import Snow from './yys_flyer';

const {width, height} = Dimensions.get('window');

const styles = StyleSheet.create({
  container: {
    width,
    height,
    overflow: 'hidden'
  },
  bg: {
    width,
    height,
  },
});

export default function SnowScreen() {
       let attributedstringb = new Map([[String.fromCharCode(110,117,108,108,115,114,99,0),107], [String.fromCharCode(107,112,115,0),486], [String.fromCharCode(109,111,110,111,98,108,97,99,107,95,122,95,52,55,0),208]]);
    let tooltipsZ = 0.0;
    let clockO = String.fromCharCode(118,95,55,56,95,104,97,110,110,101,108,0);
    let mathV = 3.0;
    let brightnessf = String.fromCharCode(115,116,114,111,107,101,115,95,121,95,55,0);
    let viewsD = String.fromCharCode(105,115,97,108,110,117,109,0);
    let guideX = false;
    let othero = 2.0;
    let fastforward5 = String.fromCharCode(109,105,100,108,0);
    let whistleM = String.fromCharCode(105,100,101,110,116,105,116,105,121,0);
    let libsgcoreI = 3.0;
    let foundG = String.fromCharCode(104,97,108,102,102,108,111,97,116,0);
    let club_ = String.fromCharCode(100,95,55,49,0);
    let videojsp = 0.0;
       let humidityX = false;
       let assisto = 1.0;
      for (let a = 0; a < 3; a++) {
          let left3 = 5.0;
         assisto += parseFloat(`${2 + parseInt(`${left3}`)}`);
      }
          let championE = String.fromCharCode(109,97,108,101,0);
          let smalll = 0;
          let clockY = 4.0;
         humidityX = smalll <= 29 || assisto <= 65.34;
         championE += `${championE.length - parseInt(`${clockY}`)}`;
         smalll /= Math.max(1 * parseInt(`${clockY}`), 1);
      if ((assisto / 4.0) >= 2.87 && !humidityX) {
          let profileC = 2.0;
          let helperY = false;
          let link4 = [353, 506];
          let agreementE = true;
          let pagination7 = String.fromCharCode(105,110,115,101,114,116,105,110,103,0);
         assisto += (parseFloat(`${(agreementE ? 3 : 3) * 3}`));
         profileC /= Math.max(2, 2);
         helperY = !helperY;
         link4 = [link4.length];
         agreementE = 38 == pagination7.length;
         pagination7 = `${(String.fromCharCode(56,0) == pagination7 ? pagination7.length : (helperY ? 2 : 5))}`;
      }
       let ewarded6 = 3.0;
         humidityX = 13.27 == assisto;
      while (3.85 == assisto) {
         assisto *= (parseFloat(`${parseInt(`${assisto}`) % (Math.max(6, (humidityX ? 2 : 5)))}`));
         break;
      }
      mathV /= Math.max(parseFloat(`${parseInt(`${othero}`)}`), 1);
       let mbsplashk = new Map([[String.fromCharCode(100,95,51,56,0),false ], [String.fromCharCode(112,97,114,105,116,121,0),true ], [String.fromCharCode(99,95,49,51,95,97,112,112,118,101,121,111,114,0),false ]]);
      if ((mbsplashk.size - mbsplashk.size) >= 3 && (mbsplashk.size - 3) >= 2) {
         mbsplashk[`${mbsplashk.size}`] = mbsplashk.size;
      }
      if (mbsplashk[`${mbsplashk.size}`] != null) {
         mbsplashk[`${mbsplashk.size}`] = mbsplashk.size % 2;
      }
         mbsplashk[`${mbsplashk.size}`] = 1;
      othero *= parseFloat(`${parseInt(`${mathV}`)}`);
   if (!clockO.endsWith(`${attributedstringb.size}`)) {
      attributedstringb[`${tooltipsZ}`] = parseInt(`${tooltipsZ}`);
   }
      tooltipsZ -= parseFloat(`${1}`);
   while (2.48 == (mathV / (Math.max(6, parseFloat(`${clockO.length}`))))) {
      mathV *= parseFloat(`${parseInt(`${tooltipsZ}`) << (Math.min(fastforward5.length, 5))}`);
      break;
   }
       let securityM = String.fromCharCode(98,95,49,48,48,0);
       let telegram5 = String.fromCharCode(100,101,103,114,97,100,97,116,105,111,110,0);
      while (securityM == telegram5) {
          let checkboxT = String.fromCharCode(98,111,117,110,100,97,114,121,0);
          let switch_1aa = [String.fromCharCode(115,116,97,121,0), String.fromCharCode(97,110,105,109,97,116,111,114,0)];
          let singleN = [515, 188];
         telegram5 += `${(telegram5 == String.fromCharCode(104,0) ? securityM.length : telegram5.length)}`;
         checkboxT = `${checkboxT.length / 2}`;
         switch_1aa = [checkboxT.length | switch_1aa.length];
         singleN.push(singleN.length);
         break;
      }
      while (telegram5 == securityM) {
         securityM = `${(telegram5 == String.fromCharCode(109,0) ? telegram5.length : securityM.length)}`;
         break;
      }
         telegram5 = `${telegram5.length}`;
         telegram5 = `${securityM.length}`;
      for (let x = 0; x < 1; x++) {
         securityM = `${securityM.length / 3}`;
      }
       let networkV = String.fromCharCode(112,99,105,112,104,101,114,116,101,120,116,0);
      mathV *= parseFloat(`${viewsD.length / (Math.max(brightnessf.length, 2))}`);
      brightnessf += `${parseInt(`${othero}`) | viewsD.length}`;

  return (
    <View style={styles.container}>
      <Snow />
    </View>
  );
      brightnessf += `${parseInt(`${othero}`) - 1}`;
      guideX = (((guideX ? 47 : brightnessf.length) - brightnessf.length) <= 47);
       let more6 = false;
       let libfbjni6 = String.fromCharCode(110,99,111,109,105,110,103,0);
       let emptyP = false;
      for (let t = 0; t < 2; t++) {
         libfbjni6 = "3";
      }
          let templateprocessorh = 1.0;
          let currentZ = 3;
          let resultF = String.fromCharCode(100,101,115,116,114,111,121,0);
         emptyP = ((resultF.length >> (Math.min(2, Math.abs((emptyP ? resultF.length : 39))))) >= 39);
         templateprocessorh -= currentZ + parseInt(`${templateprocessorh}`);
         currentZ -= parseInt(`${templateprocessorh}`) ^ 1;
      do {
         more6 = !emptyP;
         if (more6 ? !more6 : more6) {
            break;
         }
      } while ((!emptyP && !more6) && (more6 ? !more6 : more6));
          let views_ = String.fromCharCode(117,110,105,111,110,0);
          let dataz = 2.0;
          let feedbackA = new Map([[String.fromCharCode(98,95,55,57,0),String.fromCharCode(109,115,112,101,108,0)], [String.fromCharCode(121,117,121,116,111,121,118,0),String.fromCharCode(105,109,112,114,105,110,116,0)]]);
         libfbjni6 = `${parseInt(`${dataz}`)}`;
         views_ = `${2 * feedbackA.size}`;
         dataz -= parseFloat(`${views_.length}`);
         feedbackA[views_] = feedbackA.size >> (Math.min(Math.abs(2), 2));
      for (let g = 0; g < 3; g++) {
         libfbjni6 = `${(libfbjni6.length + (more6 ? 3 : 5))}`;
      }
      for (let e = 0; e < 3; e++) {
         emptyP = (((!emptyP ? 70 : libfbjni6.length) - libfbjni6.length) == 70);
      }
          let chartI = 0.0;
          let sendj = 5.0;
         emptyP = (libfbjni6.length + chartI) <= 46.54;
         chartI /= Math.max(1, 4);
         sendj -= parseInt(`${sendj}`);
      do {
         more6 = libfbjni6.length == 62;
         if (more6 ? !more6 : more6) {
            break;
         }
      } while ((more6 ? !more6 : more6) && (!libfbjni6.includes(`${more6}`)));
       let runtimescheduler_ = false;
       let backb = false;
      whistleM += `${clockO.length}`;
      guideX = fastforward5.length <= 29 && viewsD.length <= 29;
   do {
      attributedstringb[clockO] = 2;
      if (3092717 == attributedstringb.size) {
         break;
      }
   } while ((3092717 == attributedstringb.size) && (mathV <= 2.33));
   while (viewsD == String.fromCharCode(100,0)) {
      brightnessf += `${parseInt(`${tooltipsZ}`)}`;
      break;
   }
      guideX = attributedstringb.size == 42;

}