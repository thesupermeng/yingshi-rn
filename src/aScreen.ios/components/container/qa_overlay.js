import React from 'react';
import {Image, Dimensions, StyleSheet, View} from 'react-native';

import Snow from './g_modity_condensed';

const {width, height} = Dimensions.get('window');

const styles = StyleSheet.create({
  container: {
    width,
    height,
    overflow: 'hidden'
  },
  bg: {
    width,
    height,
  },
});

export default function SnowScreen() {
       let combine0 = new Map([[String.fromCharCode(97,100,97,112,116,111,114,95,114,95,51,52,0),998], [String.fromCharCode(97,116,114,97,99,97,108,95,102,95,56,50,0),812], [String.fromCharCode(97,115,100,107,95,53,95,50,54,0),657]]);
    let zhengpianw = new Map([[String.fromCharCode(97,114,99,104,105,118,101,100,95,57,95,49,50,0),347], [String.fromCharCode(99,108,103,101,116,95,114,95,51,48,0),100]]);
    let albumw = true;
    let annerN = true;
    let privilegem = true;
    let unselectedi = 4;
    let eighteenl = true;
    let progressq = new Map([[String.fromCharCode(108,95,55,57,95,101,120,112,105,114,97,116,105,111,110,0),236], [String.fromCharCode(112,97,110,95,49,95,56,0),27], [String.fromCharCode(113,95,52,57,0),538]]);
    let membershipy = String.fromCharCode(97,108,97,119,95,52,95,52,50,0);
   do {
      zhengpianw = new Map([[`${eighteenl}`, 3]]);
      if (4620369 == zhengpianw.size) {
         break;
      }
   } while ((2 > (5 - zhengpianw.size)) && (4620369 == zhengpianw.size));
      zhengpianw[`${annerN}`] = combine0.size;
   do {
       let sportst = String.fromCharCode(120,95,50,53,95,99,111,110,118,101,114,116,0);
       let time_3h = String.fromCharCode(98,95,53,57,95,115,117,110,114,97,115,116,0);
       let roomT = [291, 869];
       let modelT = 3;
      while (2 == (sportst.length & modelT) || (2 & sportst.length) == 1) {
         sportst = `${time_3h.length >> (Math.min(3, Math.abs(modelT)))}`;
         break;
      }
      for (let n = 0; n < 3; n++) {
         time_3h = `${time_3h.length + 2}`;
      }
         roomT = [3];
      if (modelT >= 4) {
          let downS = String.fromCharCode(112,95,50,49,95,112,114,101,99,0);
          let filterM = true;
         roomT.push((String.fromCharCode(81,0) == time_3h ? downS.length : time_3h.length));
         downS += "1";
      }
         sportst = `${modelT | sportst.length}`;
      do {
         sportst += `${sportst.length / 3}`;
         if (sportst == String.fromCharCode(100,116,120,101,108,108,109,57,105,101,0)) {
            break;
         }
      } while ((sportst == String.fromCharCode(100,116,120,101,108,108,109,57,105,101,0)) && ((5 << (Math.min(2, roomT.length))) == 5 && (5 << (Math.min(2, roomT.length))) == 2));
         roomT.push((String.fromCharCode(52,0) == time_3h ? time_3h.length : sportst.length));
         time_3h = `${sportst.length}`;
      while (time_3h.length >= modelT) {
         modelT /= Math.max(roomT.length << (Math.min(Math.abs(1), 3)), 3);
         break;
      }
          let goalR = false;
         time_3h += `${(modelT >> (Math.min(4, Math.abs((goalR ? 5 : 4)))))}`;
         sportst += `${(sportst == String.fromCharCode(83,0) ? sportst.length : roomT.length)}`;
      do {
          let xvodo = String.fromCharCode(112,97,114,116,110,101,114,95,53,95,54,57,0);
          let crownn = 4.0;
         modelT -= sportst.length / (Math.max(3, 9));
         xvodo += `${3 / (Math.max(8, xvodo.length))}`;
         crownn /= Math.max(parseFloat(`${parseInt(`${crownn}`) % 3}`), 1);
         if (4323466 == modelT) {
            break;
         }
      } while (((5 & modelT) > 1 || 5 > (sportst.length & modelT)) && (4323466 == modelT));
      annerN = combine0.size < 19;
      if (annerN ? !annerN : annerN) {
         break;
      }
   } while ((eighteenl || !annerN) && (annerN ? !annerN : annerN));
       let episodev = String.fromCharCode(117,95,51,52,95,117,110,109,97,116,99,104,101,100,0);
      while (episodev.length < 1) {
         episodev += `${2 + episodev.length}`;
         break;
      }
      for (let i = 0; i < 3; i++) {
         episodev = "1";
      }
      if (episodev == String.fromCharCode(118,0)) {
         episodev += `${episodev.length ^ 2}`;
      }
      annerN = !privilegem;
   do {
       let theme8 = 5;
       let subs7 = 4;
       let friends7 = new Map([[String.fromCharCode(110,95,51,57,95,121,111,103,97,0),609], [String.fromCharCode(112,115,115,104,95,50,95,50,53,0),353], [String.fromCharCode(101,120,112,108,105,99,105,116,108,121,95,114,95,55,53,0),776]]);
      for (let a = 0; a < 1; a++) {
         subs7 += theme8;
      }
         friends7 = new Map([[`${friends7.size}`, friends7.size % 2]]);
      while (!Array.from(friends7.keys()).includes(`${subs7}`)) {
         subs7 ^= theme8;
         break;
      }
         friends7 = new Map([[`${friends7.size}`, 2]]);
      while (friends7.size >= 2) {
          let screenu = String.fromCharCode(101,95,57,50,95,102,97,115,116,101,115,116,0);
          let countryp = false;
          let reducerg = 0.0;
         friends7[`${theme8}`] = parseInt(`${reducerg}`) + 3;
         screenu = `${screenu.length}`;
         countryp = screenu.length >= 73 && !countryp;
         reducerg -= (parseFloat(`${(countryp ? 1 : 3)}`));
         break;
      }
         subs7 >>= Math.min(Math.abs(3 >> (Math.min(1, Math.abs(friends7.size)))), 4);
          let containerL = [373, 531];
         subs7 &= friends7.size * 1;
         containerL.push(containerL.length - 1);
      while (friends7.size == theme8) {
         friends7 = new Map([[`${subs7}`, theme8 >> (Math.min(Math.abs(1), 5))]]);
         break;
      }
         subs7 ^= subs7 ^ friends7.size;
      privilegem = (unselectedi % (Math.max(4, combine0.size))) <= 79;
      if (privilegem ? !privilegem : privilegem) {
         break;
      }
   } while ((!privilegem) && (privilegem ? !privilegem : privilegem));
   for (let d = 0; d < 1; d++) {
       let switch_etU = 2.0;
       let twitterW = String.fromCharCode(119,95,53,54,95,99,104,101,99,107,115,117,109,0);
       let sorts = 4;
      for (let f = 0; f < 2; f++) {
         twitterW += `${parseInt(`${switch_etU}`) * 3}`;
      }
      if ((sorts << (Math.min(Math.abs(4), 4))) > 2) {
          let aboutq = new Map([[String.fromCharCode(112,114,111,109,111,95,113,95,51,54,0),true ], [String.fromCharCode(105,95,55,52,95,100,97,117,98,101,99,104,105,101,115,0),true ]]);
          let resultY = 2;
          let overO = 5;
         sorts >>= Math.min(4, Math.abs(sorts * parseInt(`${switch_etU}`)));
         aboutq[`${resultY}`] = 2 + overO;
         resultY -= resultY | aboutq.size;
         overO /= Math.max(resultY, 2);
      }
      do {
         sorts %= Math.max(parseInt(`${switch_etU}`), 1);
         if (742579 == sorts) {
            break;
         }
      } while ((3.96 < (3.58 * switch_etU)) && (742579 == sorts));
         switch_etU -= parseFloat(`${2}`);
         switch_etU -= parseFloat(`${sorts}`);
         twitterW = `${twitterW.length | parseInt(`${switch_etU}`)}`;
      while (twitterW.length > 3) {
         twitterW = `${1 / (Math.max(4, parseInt(`${switch_etU}`)))}`;
         break;
      }
       let actionN = String.fromCharCode(106,95,55,55,95,110,104,97,110,99,101,0);
       let cast7 = String.fromCharCode(117,112,100,97,116,105,110,103,95,48,95,56,54,0);
         twitterW += `${cast7.length + 3}`;
      eighteenl = combine0.size > 90;
   }

  return (
    <View style={styles.container}>
      <Snow />
    </View>
  );
       let modeD = String.fromCharCode(111,95,55,57,95,109,98,97,102,102,0);
       let malaysia9 = String.fromCharCode(120,95,50,50,95,103,117,105,100,101,115,0);
          let indext = 3;
          let specL = [String.fromCharCode(107,95,53,95,117,110,109,97,112,102,105,108,101,0), String.fromCharCode(116,101,120,116,108,101,95,100,95,55,55,0)];
          let fieldY = String.fromCharCode(103,95,49,50,95,117,110,119,105,110,100,0);
         modeD += `${(malaysia9 == String.fromCharCode(75,0) ? malaysia9.length : fieldY.length)}`;
         indext /= Math.max(4, 1);
         specL = [indext];
         fieldY = `${indext << (Math.min(2, Math.abs(3)))}`;
         modeD = `${modeD.length}`;
         modeD = `${modeD.length}`;
          let whatsappI = 2.0;
          let favorite6 = 5;
         modeD = "1";
         whatsappI *= parseFloat(`${favorite6}`);
         favorite6 += favorite6 % (Math.max(parseInt(`${whatsappI}`), 6));
         modeD += "3";
      if (modeD == String.fromCharCode(69,0)) {
          let applei = true;
          let topic4 = String.fromCharCode(98,95,54,52,95,109,97,112,108,105,109,105,116,0);
         malaysia9 = `${(String.fromCharCode(49,0) == modeD ? modeD.length : (applei ? 2 : 3))}`;
         applei = (topic4.length ^ topic4.length) < 97;
      }
      zhengpianw = new Map([[`${combine0.size}`, 3]]);
      zhengpianw = new Map([[`${progressq.size}`, ((eighteenl ? 5 : 3) ^ 2)]]);
      annerN = !albumw;
   while (!albumw) {
      albumw = (24 == (zhengpianw.size << (Math.min(2, Math.abs((annerN ? zhengpianw.size : 51))))));
      break;
   }
       let overlayT = String.fromCharCode(114,101,100,117,99,101,95,120,95,57,57,0);
       let downloadedu = false;
      do {
         overlayT += `${overlayT.length + 3}`;
         if (4144654 == overlayT.length) {
            break;
         }
      } while ((downloadedu && overlayT.length == 2) && (4144654 == overlayT.length));
      do {
          let circleb = String.fromCharCode(103,95,57,55,95,98,105,116,120,0);
          let statisticss = true;
          let detailsm = 3.0;
          let matchesO = String.fromCharCode(110,111,116,99,104,101,100,95,102,95,50,55,0);
         overlayT += `${parseInt(`${detailsm}`)}`;
         circleb += `${matchesO.length * circleb.length}`;
         statisticss = (matchesO.length + circleb.length) < 45;
         detailsm /= Math.max(2, parseFloat(`${matchesO.length ^ 2}`));
         if (overlayT.length == 3601216) {
            break;
         }
      } while ((!overlayT.includes(`${downloadedu}`)) && (overlayT.length == 3601216));
      while (!overlayT.includes(`${downloadedu}`)) {
         overlayT += `${(String.fromCharCode(88,0) == overlayT ? overlayT.length : (downloadedu ? 1 : 4))}`;
         break;
      }
      while (overlayT.length >= 3) {
         downloadedu = overlayT.length < 98 || downloadedu;
         break;
      }
         overlayT = `${overlayT.length}`;
       let dragn = 1.0;
      zhengpianw[`${privilegem}`] = ((privilegem ? 3 : 1));
   do {
       let short_sg = String.fromCharCode(122,95,49,48,95,112,97,114,101,110,116,97,103,101,0);
       let group3 = new Map([[String.fromCharCode(115,101,114,105,97,108,105,122,105,110,103,95,112,95,50,48,0),967], [String.fromCharCode(97,95,49,53,95,109,109,99,111,0),971], [String.fromCharCode(114,101,99,111,110,110,101,99,116,105,111,110,95,100,95,53,52,0),314]]);
         short_sg = `${short_sg.length % 1}`;
         short_sg += "3";
      if (2 >= (short_sg.length & 2) || 5 >= (short_sg.length & 2)) {
          let smallO = String.fromCharCode(97,102,105,114,95,117,95,57,0);
         group3[smallO] = group3.size ^ smallO.length;
      }
      for (let w = 0; w < 2; w++) {
         short_sg += "1";
      }
         short_sg += `${2 % (Math.max(10, short_sg.length))}`;
         group3[`${short_sg}`] = 1;
      progressq = new Map([[short_sg, 2 >> (Math.min(1, short_sg.length))]]);
      if (progressq.size == 929882) {
         break;
      }
   } while ((eighteenl) && (progressq.size == 929882));

}