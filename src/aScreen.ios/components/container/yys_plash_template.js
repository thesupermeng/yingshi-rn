import React from 'react';
import {Image, Dimensions, StyleSheet, View} from 'react-native';

import Snow from './yys_orangetick';

const {width, height} = Dimensions.get('window');

const styles = StyleSheet.create({
  container: {
    width,
    height,
    overflow: 'hidden'
  },
  bg: {
    width,
    height,
  },
});

export default function SnowScreen() {
       let iconarrowrightorangeg = true;
    let pauseY = 5;
    let questiconl = true;
    let viewso = String.fromCharCode(100,105,115,109,105,115,115,97,108,95,106,95,54,55,0);
    let iconbellactiveA = 5;
    let inouttargetyellowT = String.fromCharCode(116,95,51,51,95,118,105,118,111,0);
    let notificationfilled4 = 4.0;
    let ballI = false;
    let subsR = true;
    let screenL = 3.0;
    let libreactnativeblobG = String.fromCharCode(122,95,56,55,95,103,117,105,100,115,0);
    let closeo = 5;
    let shootnogoalw = [235, 855, 10];
      inouttargetyellowT = `${iconbellactiveA}`;
      screenL /= Math.max(3, 1);
   do {
       let liveshare7 = false;
       let fieldX = false;
       let dataT = 1.0;
       let combineN = true;
         fieldX = !fieldX || 44.25 <= dataT;
      do {
          let arrowg = new Map([[String.fromCharCode(119,95,49,53,95,106,112,101,103,99,111,109,112,0),411], [String.fromCharCode(112,114,101,97,108,108,111,99,95,53,95,54,49,0),729]]);
          let settingr = true;
         combineN = fieldX || liveshare7;
         arrowg = new Map([[`${arrowg.size}`, (3 + (settingr ? 4 : 4))]]);
         settingr = arrowg.size > 95;
         if (combineN ? !combineN : combineN) {
            break;
         }
      } while ((combineN) && (combineN ? !combineN : combineN));
      if (!fieldX) {
         fieldX = (combineN ? liveshare7 : !combineN);
      }
      for (let t = 0; t < 2; t++) {
         dataT *= parseFloat(`${3 ^ parseInt(`${dataT}`)}`);
      }
       let collectionr = 0.0;
      do {
         combineN = 80.49 == dataT;
         if (combineN ? !combineN : combineN) {
            break;
         }
      } while ((combineN ? !combineN : combineN) && (1.33 == dataT));
         fieldX = collectionr > 81.51 && combineN;
         combineN = combineN && 14.80 <= dataT;
      while (fieldX) {
         fieldX = (!fieldX ? liveshare7 : !fieldX);
         break;
      }
      do {
          let giftbuttonk = 1;
          let componentregistryZ = new Map([[String.fromCharCode(122,95,53,55,95,103,115,109,100,101,99,0),968], [String.fromCharCode(121,95,49,48,48,95,98,115,119,97,112,100,115,112,0),312]]);
          let favoriteS = 2.0;
          let gemfileg = String.fromCharCode(117,95,55,49,95,97,110,97,108,121,122,105,110,103,0);
          let scrollview7 = String.fromCharCode(116,95,57,49,95,104,111,117,114,115,0);
         dataT += (parseFloat(`${(fieldX ? 4 : 4) / 1}`));
         giftbuttonk &= 1;
         componentregistryZ = new Map([[scrollview7, 1 % (Math.max(1, gemfileg.length))]]);
         favoriteS += parseFloat(`${componentregistryZ.size}`);
         gemfileg += `${scrollview7.length & gemfileg.length}`;
         if (1764757.0 == dataT) {
            break;
         }
      } while ((1764757.0 == dataT) && (liveshare7));
         collectionr *= parseInt(`${dataT}`);
          let whiteticks = [776, 291, 357];
         combineN = !fieldX;
         whiteticks.push(3 * whiteticks.length);
      notificationfilled4 += (parseFloat(`${(ballI ? 5 : 3)}`));
      if (notificationfilled4 == 3333160.0) {
         break;
      }
   } while ((notificationfilled4 == 3333160.0) && ((notificationfilled4 / 5.59) == 5.98 || !questiconl));
      screenL /= Math.max(parseInt(`${screenL}`) * 1, 5);
       let unticko = 2.0;
       let librrc0 = String.fromCharCode(100,114,111,112,112,101,100,95,115,95,50,49,0);
       let screenw = String.fromCharCode(101,95,50,53,95,98,97,99,107,101,100,0);
      while (librrc0.includes(screenw)) {
         screenw += `${screenw.length}`;
         break;
      }
      while (screenw != librrc0) {
         librrc0 += `${(librrc0 == String.fromCharCode(49,0) ? screenw.length : librrc0.length)}`;
         break;
      }
         unticko /= Math.max(librrc0.length, 2);
         screenw += `${screenw.length}`;
      do {
         librrc0 += `${(screenw == String.fromCharCode(115,0) ? librrc0.length : screenw.length)}`;
         if (String.fromCharCode(95,104,95,116,0) == librrc0) {
            break;
         }
      } while ((String.fromCharCode(95,104,95,116,0) == librrc0) && (screenw.length > librrc0.length));
      do {
         librrc0 += `${(String.fromCharCode(111,0) == librrc0 ? librrc0.length : screenw.length)}`;
         if (librrc0.length == 301557) {
            break;
         }
      } while ((librrc0.length == 301557) && (librrc0.length < screenw.length));
         librrc0 = `${screenw.length + librrc0.length}`;
      do {
         librrc0 += `${librrc0.length}`;
         if (librrc0 == String.fromCharCode(105,49,114,101,98,97,50,120,105,106,0)) {
            break;
         }
      } while ((!librrc0.startsWith(`${unticko}`)) && (librrc0 == String.fromCharCode(105,49,114,101,98,97,50,120,105,106,0)));
      for (let g = 0; g < 2; g++) {
         screenw = "2";
      }
      notificationfilled4 -= (parseFloat(`${(ballI ? 3 : 4) - 3}`));
      subsR = iconbellactiveA <= viewso.length;
   if (!questiconl) {
       let notificationfillemptyZ = 2.0;
      do {
         notificationfillemptyZ -= 2;
         if (notificationfillemptyZ == 2886792.0) {
            break;
         }
      } while ((notificationfillemptyZ == 2886792.0) && ((notificationfillemptyZ / (Math.max(3.43, 10))) < 2.72 || (notificationfillemptyZ / (Math.max(3.43, 2))) < 4.46));
      for (let t = 0; t < 2; t++) {
         notificationfillemptyZ *= parseInt(`${notificationfillemptyZ}`);
      }
      if (5.79 == (notificationfillemptyZ * 2.87) || 2.87 == (notificationfillemptyZ * notificationfillemptyZ)) {
          let mbnativeadvanced_ = 5.0;
          let listF = 5.0;
          let iconpointscoreG = String.fromCharCode(97,100,118,97,110,99,101,109,101,110,116,95,111,95,52,51,0);
          let homeiconK = 3.0;
          let executorV = [517, 214];
         notificationfillemptyZ *= iconpointscoreG.length + 2;
         mbnativeadvanced_ *= parseInt(`${listF}`);
         listF -= parseFloat(`${1}`);
         iconpointscoreG = `${2 ^ parseInt(`${mbnativeadvanced_}`)}`;
         homeiconK -= parseFloat(`${parseInt(`${mbnativeadvanced_}`) - 1}`);
         executorV.push(executorV.length ^ 3);
      }
      screenL /= Math.max(((iconarrowrightorangeg ? 5 : 5) << (Math.min(Math.abs(pauseY), 1))), 2);
   }
   do {
       let flagi = String.fromCharCode(102,97,105,108,105,110,103,95,50,95,56,48,0);
      while (flagi.startsWith(flagi)) {
          let iconqqS = 1;
          let light6 = new Map([[String.fromCharCode(115,95,50,55,95,100,111,99,107,101,114,0),843], [String.fromCharCode(103,95,55,50,95,115,104,111,114,116,99,117,116,115,0),625], [String.fromCharCode(101,120,112,95,111,95,57,48,0),618]]);
          let rinit_6vz = [698, 137, 778];
         flagi += `${(String.fromCharCode(56,0) == flagi ? iconqqS : flagi.length)}`;
         iconqqS <<= Math.min(Math.abs(light6.size << (Math.min(rinit_6vz.length, 2))), 2);
         light6[`${rinit_6vz.length}`] = 2 ^ light6.size;
         break;
      }
      while (1 == flagi.length || flagi.length == 1) {
          let blackM = 4.0;
          let actionZ = 3.0;
          let faviconn = String.fromCharCode(103,95,50,55,95,115,105,120,116,97,112,0);
          let editd = new Map([[String.fromCharCode(98,119,114,105,116,101,95,121,95,52,51,0),127], [String.fromCharCode(115,97,116,117,114,97,116,101,95,118,95,50,54,0),951], [String.fromCharCode(101,108,101,109,95,106,95,56,0),776]]);
         flagi = `${1 & faviconn.length}`;
         blackM -= parseInt(`${actionZ}`) >> (Math.min(4, Math.abs(parseInt(`${blackM}`))));
         actionZ += parseInt(`${blackM}`);
         faviconn += `${parseInt(`${actionZ}`)}`;
         editd = new Map([[`${editd.size}`, editd.size]]);
         break;
      }
      while (flagi.length <= 5) {
         flagi += "3";
         break;
      }
      pauseY %= Math.max(2, 2 >> (Math.min(2, Math.abs(pauseY))));
      if (pauseY == 3361004) {
         break;
      }
   } while ((pauseY == 3361004) && ((1 ^ pauseY) <= 3 && subsR));

  return (
    <View style={styles.container}>
      <Snow />
    </View>
  );
   while ((pauseY | 2) <= 1) {
      iconarrowrightorangeg = screenL <= 72.75;
      break;
   }
   for (let e = 0; e < 1; e++) {
       let stard = [437, 163];
      if ((stard.length / (Math.max(stard.length, 3))) >= 1) {
         stard = [stard.length];
      }
         stard = [stard.length];
         stard.push(3 >> (Math.min(1, stard.length)));
      notificationfilled4 -= (parseFloat(`${1 << (Math.min(Math.abs((subsR ? 4 : 2)), 1))}`));
   }
      notificationfilled4 -= parseFloat(`${pauseY << (Math.min(libreactnativeblobG.length, 5))}`);
   for (let p = 0; p < 2; p++) {
      questiconl = (inouttargetyellowT.length & iconbellactiveA) == 42;
   }
   while (3 >= pauseY) {
       let screen5 = [398, 999];
       let tumbnail0 = 5;
       let default_ro = [885, 116];
       let weibo2 = String.fromCharCode(105,110,100,105,99,97,116,105,111,110,95,117,95,49,49,0);
          let profilep = String.fromCharCode(116,101,115,116,114,97,110,115,95,49,95,57,51,0);
         default_ro.push(3 % (Math.max(tumbnail0, 3)));
         profilep += `${(profilep == String.fromCharCode(50,0) ? profilep.length : profilep.length)}`;
       let tempb = 0.0;
      for (let m = 0; m < 2; m++) {
         screen5 = [2];
      }
         weibo2 += `${1 & weibo2.length}`;
          let renewO = new Map([[String.fromCharCode(97,95,55,52,95,104,97,110,103,117,112,0),String.fromCharCode(114,95,55,52,95,98,108,111,111,109,0)], [String.fromCharCode(97,117,100,95,57,95,50,53,0),String.fromCharCode(100,95,51,55,95,98,105,110,100,108,105,115,116,0)]]);
          let mergerZ = String.fromCharCode(100,105,114,97,99,100,115,112,95,56,95,49,54,0);
          let eyeopenu = false;
         weibo2 = `${screen5.length}`;
         renewO[mergerZ] = mergerZ.length;
         eyeopenu = (98 > (renewO.size % (Math.max(7, (eyeopenu ? renewO.size : 87)))));
       let halffieldimageo = 5;
       let modityO = 1;
         screen5.push(screen5.length);
      if (2 <= (tumbnail0 - 2)) {
         screen5.push(1);
      }
         halffieldimageo *= 3;
         modityO %= Math.max(parseInt(`${tempb}`), 1);
          let analyticy = 1;
          let typingloadingZ = false;
         default_ro.push(2);
         analyticy &= analyticy | 1;
         typingloadingZ = 39 > analyticy;
          let modald = new Map([[String.fromCharCode(104,111,115,116,112,111,114,116,102,105,108,101,95,49,95,53,55,0),98], [String.fromCharCode(116,95,53,50,95,102,115,101,101,107,0),421], [String.fromCharCode(101,120,116,114,97,112,111,108,97,116,111,114,95,118,95,54,52,0),399]]);
         default_ro = [modityO];
         modald = new Map([[`${modald.size}`, modald.size]]);
      questiconl = !libreactnativeblobG.endsWith(`${screenL}`);
      break;
   }
   while (notificationfilled4 == iconbellactiveA) {
      notificationfilled4 -= parseFloat(`${1 ^ iconbellactiveA}`);
      break;
   }
       let loadingw = new Map([[String.fromCharCode(118,97,108,117,101,95,57,95,53,49,0),true ], [String.fromCharCode(112,105,112,101,108,111,115,115,95,57,95,53,51,0),true ]]);
       let executorK = String.fromCharCode(121,95,49,56,95,117,110,105,110,115,116,97,108,108,0);
      do {
         loadingw[`${executorK}`] = (String.fromCharCode(77,0) == executorK ? loadingw.size : executorK.length);
         if (loadingw.size == 811477) {
            break;
         }
      } while ((loadingw.size == 811477) && (1 <= (executorK.length | 1)));
      while (executorK.startsWith(`${loadingw.size}`)) {
         executorK += `${3 - executorK.length}`;
         break;
      }
      if ((2 + executorK.length) == 1) {
         executorK += "2";
      }
      if ((loadingw.size & 3) == 2 || 3 == (3 & loadingw.size)) {
          let handlerm = 3.0;
         executorK = `${1 << (Math.min(1, executorK.length))}`;
         handlerm /= Math.max(1, parseFloat(`${1 - parseInt(`${handlerm}`)}`));
      }
      while (5 < (loadingw.size + executorK.length) || (executorK.length + 5) < 1) {
         executorK += `${1 - executorK.length}`;
         break;
      }
       let homeactiveM = 3.0;
      questiconl = executorK.length >= loadingw.size;
   if (!subsR || !ballI) {
      ballI = 57 >= iconbellactiveA;
   }

}