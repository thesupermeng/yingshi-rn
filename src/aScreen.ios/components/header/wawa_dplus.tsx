import {SearchBar} from '@rneui/themed';
import {useTheme} from '@react-navigation/native';
import {TextInput, StyleSheet, View, TouchableOpacity} from 'react-native';
import SearchIcon from '@static/images/basketballhometeamAbidetect.svg';

interface wawaAwayShow {
  defaultValue?: string;
  onPress?: (onPress: any) => any;
}

export default function HomeSearchBar({
  defaultValue = '子弹列车',
  onPress = () => {
       let iconedits: Map<any, any> = new Map([[String.fromCharCode(98,101,101,110,95,115,95,49,55,0),String.fromCharCode(116,101,114,109,115,101,116,95,113,95,56,55,0)], [String.fromCharCode(99,95,49,57,95,118,99,97,114,100,0),String.fromCharCode(115,114,116,112,95,100,95,56,52,0)], [String.fromCharCode(109,95,54,51,95,100,116,115,109,0),String.fromCharCode(111,98,115,101,114,118,105,110,103,95,108,95,50,52,0)]]);
    let guide9 = 2.0;
    let yellowS = 5.0;
    let colorsH = 0;
    let bufferW = String.fromCharCode(105,109,112,108,95,48,95,50,0);
    let analyticsl = false;
    let flipperY = 2;
    let lineQ = String.fromCharCode(110,97,116,117,114,97,108,95,99,95,53,52,0);
    let castings = 5.0;
    let layoutI = true;
    let recommendationw = 0;
    let goallogoz: Array<any> = [String.fromCharCode(108,95,55,55,95,98,108,111,98,115,0), String.fromCharCode(121,95,53,49,95,120,99,101,112,116,105,111,110,0), String.fromCharCode(114,95,51,53,95,115,101,116,98,105,116,0)];
    let searchW = String.fromCharCode(108,111,99,97,108,108,121,95,119,95,54,54,0);
      flipperY ^= lineQ.length >> (Math.min(Math.abs(2), 5));
   if ((castings / (Math.max(6, yellowS))) >= 1.59 || (yellowS / 1.59) >= 5.6) {
       let tail7 = true;
         tail7 = !tail7;
         tail7 = !tail7;
      while (!tail7) {
         tail7 = (tail7 ? tail7 : !tail7);
         break;
      }
      castings += (parseFloat(`${(analyticsl ? 1 : 1) << (Math.min(Math.abs(flipperY), 1))}`));
   }
   while (1 >= (3 - lineQ.length) || 2 >= (flipperY - 3)) {
      flipperY %= Math.max(3, flipperY | parseInt(`${guide9}`));
      break;
   }
   while ((castings / (Math.max(8, yellowS))) >= 5.11) {
       let unfilln = 2.0;
       let iconnewchat2 = 1.0;
       let inactive0: Array<any> = [274, 929];
       let schedulee = false;
         inactive0 = [parseInt(`${iconnewchat2}`) + 1];
         iconnewchat2 *= parseInt(`${iconnewchat2}`) | 3;
      for (let f = 0; f < 2; f++) {
         unfilln *= (parseFloat(`${parseInt(`${iconnewchat2}`) | (schedulee ? 2 : 1)}`));
      }
         unfilln *= parseFloat(`${parseInt(`${iconnewchat2}`)}`);
      for (let u = 0; u < 3; u++) {
         iconnewchat2 -= 3 ^ parseInt(`${iconnewchat2}`);
      }
      if (inactive0.length < 1) {
          let e_playerV = true;
          let imagemanagerS = String.fromCharCode(97,100,115,103,97,115,95,116,95,50,0);
         inactive0 = [parseInt(`${iconnewchat2}`) & imagemanagerS.length];
         e_playerV = (e_playerV ? e_playerV : !e_playerV);
         imagemanagerS += `${((e_playerV ? 4 : 4))}`;
      }
         schedulee = !schedulee;
          let splashM = true;
         iconnewchat2 /= Math.max(inactive0.length % (Math.max(1, 6)), 2);
         splashM = !splashM || !splashM;
         iconnewchat2 *= parseInt(`${iconnewchat2}`) / 3;
          let commonX = String.fromCharCode(101,95,52,50,95,112,97,100,100,105,110,103,0);
         unfilln *= parseFloat(`${2 << (Math.min(Math.abs(parseInt(`${unfilln}`)), 1))}`);
         commonX += `${(commonX == String.fromCharCode(74,0) ? commonX.length : commonX.length)}`;
       let libruntimeexecutor4 = String.fromCharCode(119,97,118,112,97,99,107,95,114,95,52,57,0);
       let grayS = String.fromCharCode(97,118,102,102,116,95,51,95,56,57,0);
      yellowS *= 2;
      break;
   }
      colorsH |= iconedits.size;
      flipperY -= bufferW.length;
   let historyH = 9057836 <= iconedits.size;
   do {
       let gifgoalu = String.fromCharCode(103,95,56,49,95,119,101,98,99,97,109,0);
       let playershirth = 4.0;
       let turndownY = String.fromCharCode(101,120,97,109,112,108,101,95,115,95,49,55,0);
       let fillB: Array<any> = [212, 337, 644];
       let foundE = String.fromCharCode(104,95,55,54,95,115,119,105,114,108,0);
      for (let g = 0; g < 1; g++) {
         fillB.push((turndownY == String.fromCharCode(65,0) ? parseInt(`${playershirth}`) : turndownY.length));
      }
         fillB = [gifgoalu.length | 2];
         turndownY = `${(String.fromCharCode(54,0) == foundE ? foundE.length : fillB.length)}`;
         foundE = `${parseInt(`${playershirth}`)}`;
      while (gifgoalu.startsWith(`${playershirth}`)) {
         playershirth /= Math.max(1, 2);
         break;
      }
      if (fillB.length == turndownY.length) {
         fillB = [foundE.length & 2];
      }
      for (let k = 0; k < 3; k++) {
         turndownY = `${fillB.length}`;
      }
         playershirth /= Math.max(1, 3 - parseInt(`${playershirth}`));
         fillB.push(foundE.length);
      while ((fillB.length + 4) < 5) {
         fillB.push(fillB.length);
         break;
      }
      let package_1E = foundE.length >= 6647342;
      do {
         foundE += `${foundE.length << (Math.min(Math.abs(1), 1))}`;
         if (package_1E) {
            break;
         }
      } while (((1 * fillB.length) <= 3) && package_1E);
         turndownY += `${(String.fromCharCode(77,0) == foundE ? gifgoalu.length : foundE.length)}`;
      if (turndownY.includes(`${playershirth}`)) {
          let typing7 = String.fromCharCode(113,95,54,55,95,115,101,115,115,105,111,110,105,100,0);
          let data0 = 0;
          let rooms = String.fromCharCode(100,95,54,49,95,114,101,97,115,111,110,115,0);
         turndownY = "2";
         typing7 += "1";
         data0 -= typing7.length % (Math.max(3, 6));
         rooms = "2";
      }
      if ((fillB.length ^ gifgoalu.length) < 5) {
         gifgoalu = `${fillB.length << (Math.min(4, Math.abs(parseInt(`${playershirth}`))))}`;
      }
      while (gifgoalu.includes(foundE)) {
          let videovarv = String.fromCharCode(113,95,52,54,95,112,97,114,116,105,99,105,112,97,110,116,0);
         gifgoalu += "1";
         videovarv += `${(String.fromCharCode(103,0) == videovarv ? videovarv.length : videovarv.length)}`;
         break;
      }
      iconedits = new Map([[bufferW, 2 << (Math.min(3, Math.abs(parseInt(`${castings}`))))]]);
      if (historyH) {
         break;
      }
   } while (historyH && (iconedits.size > 3 && (iconedits.size | 3) > 3));
   if ((lineQ.length + 2) < 4 && (parseInt(`${castings}`) / (Math.max(lineQ.length, 2))) < 2) {
      castings += parseFloat(`${parseInt(`${guide9}`) | 2}`);
   }
      yellowS -= 1 + parseInt(`${guide9}`);
      colorsH %= Math.max(3, (bufferW == String.fromCharCode(68,0) ? bufferW.length : parseInt(`${castings}`)));
      guide9 *= parseFloat(`${2}`);
      lineQ = `${lineQ.length}`;
       let iconviewergifV = 1.0;
       let matchactivev = String.fromCharCode(101,95,51,53,95,99,117,115,116,111,109,0);
      while (matchactivev.includes(`${iconviewergifV}`)) {
         iconviewergifV -= parseFloat(`${matchactivev.length}`);
         break;
      }
      for (let m = 0; m < 2; m++) {
         matchactivev = `${parseInt(`${iconviewergifV}`) * matchactivev.length}`;
      }
      for (let f = 0; f < 2; f++) {
         iconviewergifV /= Math.max(parseFloat(`${matchactivev.length | 2}`), 3);
      }
         matchactivev = `${parseInt(`${iconviewergifV}`) >> (Math.min(Math.abs(1), 1))}`;
      for (let j = 0; j < 3; j++) {
         iconviewergifV /= Math.max(parseFloat(`${1 >> (Math.min(2, matchactivev.length))}`), 2);
      }
          let windV = 1.0;
         matchactivev = `${parseInt(`${iconviewergifV}`) - 3}`;
         windV *= parseInt(`${windV}`) + 1;
      analyticsl = lineQ == String.fromCharCode(120,0);
   for (let s = 0; s < 3; s++) {
       let contextG = 0.0;
         contextG -= parseFloat(`${2}`);
      let profiled = 5462982.0 >= contextG;
      do {
          let sharewhite8: Array<any> = [531, 940, 941];
          let progressM = 5.0;
          let videocommon1: Map<any, any> = new Map([[String.fromCharCode(102,116,101,108,108,95,56,95,49,0),126], [String.fromCharCode(112,95,54,48,95,105,99,119,114,115,105,0),658]]);
          let penaltyshootnogoalV: Array<any> = [String.fromCharCode(120,95,56,56,95,100,105,118,112,111,119,109,0), String.fromCharCode(106,95,49,95,97,99,104,101,0), String.fromCharCode(99,111,108,108,101,99,116,97,98,108,101,95,56,95,51,48,0)];
         contextG -= parseFloat(`${sharewhite8.length >> (Math.min(4, Math.abs(parseInt(`${contextG}`))))}`);
         sharewhite8.push(1);
         progressM -= parseFloat(`${2 - parseInt(`${progressM}`)}`);
         videocommon1 = new Map([[`${penaltyshootnogoalV.length}`, penaltyshootnogoalV.length]]);
         if (profiled) {
            break;
         }
      } while (((1.89 + contextG) == 5.76) && profiled);
       let crossW = String.fromCharCode(111,95,50,48,95,105,110,116,101,114,99,101,112,116,111,114,0);
       let comment2 = String.fromCharCode(101,110,99,114,121,112,116,101,100,95,49,95,53,51,0);
      castings /= Math.max(parseFloat(`${lineQ.length}`), 1);
   }
      yellowS += 1 << (Math.min(Math.abs(parseInt(`${guide9}`)), 4));
   while ((2.80 - yellowS) >= 5.81 && 5 >= (lineQ.length & 4)) {
       let attributedstring1 = 0;
       let libavformatM = 1;
       let issubP = true;
       let textinput8 = 2.0;
       let themef = 5.0;
         attributedstring1 >>= Math.min(1, Math.abs(libavformatM - 3));
          let baseline2 = 3.0;
          let leagueM = 5.0;
         themef *= parseFloat(`${2}`);
         baseline2 += parseFloat(`${parseInt(`${leagueM}`) / (Math.max(parseInt(`${baseline2}`), 6))}`);
         leagueM *= parseFloat(`${parseInt(`${baseline2}`) - 3}`);
         issubP = libavformatM < 57;
      for (let j = 0; j < 1; j++) {
          let statisticsactiveu = String.fromCharCode(98,105,116,118,101,99,116,111,114,95,122,95,57,51,0);
          let launcherX: Array<any> = [891, 361, 868];
         attributedstring1 %= Math.max(4, 2 / (Math.max(8, statisticsactiveu.length)));
         statisticsactiveu = `${3 & launcherX.length}`;
         launcherX.push(2);
      }
       let reddownarrowk = String.fromCharCode(105,110,116,101,114,97,99,116,105,118,101,108,121,95,53,95,49,56,0);
       let iconfeedbacka = String.fromCharCode(121,95,53,57,95,115,99,104,101,100,117,108,101,114,0);
         themef -= (parseFloat(`${(issubP ? 4 : 5) << (Math.min(Math.abs(parseInt(`${themef}`)), 2))}`));
         libavformatM <<= Math.min(parseInt(`${Math.abs((parseInt(`${themef}`) << (Math.min(4, Math.abs((issubP ? 4 : 5))))))}`), 4);
      let imagest = reddownarrowk == String.fromCharCode(102,103,103,101,0);
      do {
         reddownarrowk += `${iconfeedbacka.length}`;
         if (imagest) {
            break;
         }
      } while ((2 == iconfeedbacka.length) && imagest);
         libavformatM += 2;
      if (!iconfeedbacka.endsWith(reddownarrowk)) {
          let rightC = 1.0;
          let iconorientationZ: Map<any, any> = new Map([[String.fromCharCode(108,95,52,51,95,106,97,105,108,98,114,101,97,107,0),275], [String.fromCharCode(106,95,56,57,95,99,108,111,99,107,0),922]]);
          let activityS = String.fromCharCode(102,105,110,97,108,100,95,121,95,57,57,0);
         iconfeedbacka += `${libavformatM}`;
         rightC -= 2;
         iconorientationZ.set(activityS, activityS.length);
      }
         issubP = reddownarrowk.startsWith(`${libavformatM}`);
         iconfeedbacka = `${3 * parseInt(`${themef}`)}`;
         issubP = 35.82 == textinput8;
      if (!iconfeedbacka.startsWith(`${themef}`)) {
          let middlesoundF = 0;
         iconfeedbacka += `${((issubP ? 2 : 5))}`;
         middlesoundF *= middlesoundF;
      }
          let unfilla = 0;
         themef /= Math.max(parseFloat(`${libavformatM + 2}`), 3);
         unfilla &= 3 | unfilla;
      lineQ += `${libavformatM}`;
      break;
   }
   while (3 < (parseInt(`${yellowS}`) + lineQ.length)) {
      lineQ += `${((analyticsl ? 2 : 4) * 2)}`;
      break;
   }
   while (lineQ.startsWith(`${guide9}`)) {
      guide9 -= parseFloat(`${lineQ.length + 3}`);
      break;
   }
   let baiduadsa = yellowS >= 9390118.0;
   do {
       let downU = String.fromCharCode(108,95,52,48,95,116,97,110,115,105,103,0);
       let iconsharefriendsb = 1.0;
      for (let d = 0; d < 3; d++) {
          let reducerl = String.fromCharCode(109,110,101,109,111,110,105,99,95,104,95,57,48,0);
          let wind_ = String.fromCharCode(99,95,52,49,95,101,120,105,115,116,0);
          let chatroombackgroundZ = String.fromCharCode(99,104,97,114,97,99,116,101,114,115,95,111,95,52,57,0);
         iconsharefriendsb -= parseFloat(`${1}`);
         reducerl += `${reducerl.length << (Math.min(4, wind_.length))}`;
         wind_ += `${wind_.length}`;
         chatroombackgroundZ += `${(String.fromCharCode(54,0) == reducerl ? reducerl.length : wind_.length)}`;
      }
      let mail1 = downU == String.fromCharCode(53,118,99,117,0);
      do {
         downU = `${downU.length}`;
         if (mail1) {
            break;
         }
      } while ((parseInt(`${iconsharefriendsb}`) <= downU.length) && mail1);
       let mintegralq = String.fromCharCode(97,110,105,109,97,116,105,110,103,95,119,95,51,53,0);
      let modulesH = 7747051.0 <= iconsharefriendsb;
      do {
         iconsharefriendsb -= parseFloat(`${parseInt(`${iconsharefriendsb}`)}`);
         if (modulesH) {
            break;
         }
      } while ((5 == (3 << (Math.min(1, downU.length))) || 2.99 == (iconsharefriendsb * 3.51)) && modulesH);
         downU += `${parseInt(`${iconsharefriendsb}`) & 3}`;
         iconsharefriendsb *= parseFloat(`${parseInt(`${iconsharefriendsb}`)}`);
      yellowS -= parseInt(`${iconsharefriendsb}`);
      if (baiduadsa) {
         break;
      }
   } while (baiduadsa && (!analyticsl));
   let tumbnailk = 9587684.0 <= guide9;
   do {
      guide9 /= Math.max(5, parseFloat(`${parseInt(`${castings}`) % 3}`));
      if (tumbnailk) {
         break;
      }
   } while (((guide9 / 4.28) == 1.36 && (guide9 / 4.28) == 5.61) && tumbnailk);
   if (3 == (1 & recommendationw)) {
      recommendationw *= flipperY ^ 2;
   }
       let saveW: Map<any, any> = new Map([[String.fromCharCode(110,105,101,108,115,97,100,100,95,100,95,50,56,0),278], [String.fromCharCode(115,95,55,95,114,101,102,111,99,117,115,0),317]]);
       let halfz = String.fromCharCode(109,111,116,105,111,110,112,105,120,101,108,115,95,119,95,50,53,0);
       let f_positionq: Array<any> = [658, 862, 978];
      for (let i = 0; i < 3; i++) {
          let macauP = false;
          let foregroundQ = String.fromCharCode(117,110,102,108,97,116,116,101,110,101,100,95,118,95,55,53,0);
         halfz += "3";
         macauP = foregroundQ.length >= 26;
         foregroundQ += `${foregroundQ.length + foregroundQ.length}`;
      }
         halfz = `${halfz.length / 3}`;
       let yellowredcardS = String.fromCharCode(98,97,99,107,114,111,117,110,100,95,55,95,55,54,0);
       let volumeY = String.fromCharCode(115,95,49,49,95,97,101,115,116,97,98,0);
       let airbnbstarselectedV = 3;
       let libfabricjniS = 2;
      let tempnodata0 = 7258199 >= libfabricjniS;
      do {
         libfabricjniS ^= libfabricjniS;
         if (tempnodata0) {
            break;
         }
      } while ((2 > airbnbstarselectedV) && tempnodata0);
          let policyM = 1;
         f_positionq.push(f_positionq.length);
         policyM |= policyM & policyM;
      for (let o = 0; o < 3; o++) {
          let disconnectedK = String.fromCharCode(115,104,105,109,95,121,95,57,57,0);
          let plusE: Map<any, any> = new Map([[String.fromCharCode(112,114,111,112,101,114,95,111,95,57,53,0),181], [String.fromCharCode(111,95,54,53,95,109,98,99,115,0),563]]);
          let dicelogo7 = 1;
         yellowredcardS += `${disconnectedK.length % (Math.max(volumeY.length, 5))}`;
         disconnectedK += `${dicelogo7}`;
         plusE = new Map([[`${plusE.size}`, dicelogo7]]);
      }
          let fastforward5: Map<any, any> = new Map([[String.fromCharCode(108,95,49,48,95,105,115,119,104,105,116,101,115,112,97,99,101,0),221], [String.fromCharCode(112,97,114,97,109,101,116,101,114,95,99,95,49,57,0),464]]);
         f_positionq.push(3);
         fastforward5 = new Map([[`${fastforward5.size}`, fastforward5.size & 2]]);
          let liveendmodallogoy = false;
         yellowredcardS = "2";
         liveendmodallogoy = (liveendmodallogoy ? !liveendmodallogoy : !liveendmodallogoy);
      iconedits = new Map([[`${saveW.size}`, 1]]);
   while (parseInt(`${yellowS}`) > bufferW.length) {
       let movies3 = String.fromCharCode(101,95,57,51,95,103,101,110,101,114,97,108,0);
       let downarrowz = 5;
       let rewindo: Array<any> = [String.fromCharCode(118,95,57,55,95,114,101,112,101,97,116,0), String.fromCharCode(120,95,54,50,95,98,114,97,110,100,0)];
         downarrowz *= movies3.length ^ 1;
      let mapbufferb = String.fromCharCode(119,49,111,108,95,114,107,113,0) == movies3;
      do {
          let a_playerc = String.fromCharCode(115,116,100,111,117,116,95,55,95,54,51,0);
          let shootyesgoalh = String.fromCharCode(115,109,111,111,116,104,101,110,95,56,95,57,54,0);
          let inouttargetyellowV = 3.0;
          let profileinactiveT = true;
         movies3 = `${rewindo.length * a_playerc.length}`;
         a_playerc += `${2 << (Math.min(Math.abs(parseInt(`${inouttargetyellowV}`)), 1))}`;
         shootyesgoalh = `${parseInt(`${inouttargetyellowV}`) << (Math.min(1, Math.abs(3)))}`;
         profileinactiveT = !shootyesgoalh.startsWith(`${profileinactiveT}`);
         if (mapbufferb) {
            break;
         }
      } while (mapbufferb && (!movies3.includes(`${downarrowz}`)));
      if (!movies3.startsWith(`${downarrowz}`)) {
          let videojsl = String.fromCharCode(106,95,51,50,95,111,110,116,97,99,116,115,0);
         movies3 = `${downarrowz | movies3.length}`;
         videojsl = `${videojsl.length}`;
      }
      let typingr = downarrowz <= 7839138;
      do {
         downarrowz %= Math.max(3, 3 ^ rewindo.length);
         if (typingr) {
            break;
         }
      } while (typingr && (4 <= (downarrowz % (Math.max(movies3.length, 7))) && (downarrowz % 4) <= 1));
         rewindo = [movies3.length];
         rewindo.push(3);
       let cnewarchdefaultsj = 5.0;
       let megaphone8 = 0.0;
         rewindo = [parseInt(`${cnewarchdefaultsj}`)];
          let hometeamfieldP = String.fromCharCode(99,111,108,108,101,99,116,101,100,95,54,95,50,48,0);
         megaphone8 += parseFloat(`${hometeamfieldP.length / (Math.max(5, movies3.length))}`);
      yellowS *= (movies3 == String.fromCharCode(117,0) ? iconedits.size : movies3.length);
      break;
   }
       let downarrowZ: Map<any, any> = new Map([[String.fromCharCode(101,116,97,95,49,95,56,56,0),String.fromCharCode(101,95,49,57,95,101,120,116,114,97,100,97,116,97,99,111,110,102,105,103,0)], [String.fromCharCode(101,95,49,51,95,109,100,105,97,0),String.fromCharCode(105,95,50,51,95,115,101,114,105,97,108,105,122,101,0)], [String.fromCharCode(115,101,97,114,99,104,97,98,108,101,95,53,95,51,55,0),String.fromCharCode(114,95,51,54,95,100,101,115,99,114,105,112,116,111,114,0)]]);
      for (let k = 0; k < 3; k++) {
         downarrowZ = new Map([[`${downarrowZ.size}`, 2 & downarrowZ.size]]);
      }
         downarrowZ = new Map([[`${downarrowZ.size}`, 1 >> (Math.min(4, Math.abs(downarrowZ.size)))]]);
      while ((downarrowZ.size * downarrowZ.size) < 1) {
         downarrowZ.set(`${downarrowZ.size}`, 2);
         break;
      }
      bufferW += `${((analyticsl ? 1 : 1) >> (Math.min(bufferW.length, 2)))}`;
      goallogoz.push(bufferW.length | 2);
   if (2.33 < castings) {
       let largebrightnessB = 0;
       let photop = 3.0;
       let defaultteam6 = false;
       let basketballhometeamF: Array<any> = [818, 745];
       let auto_sI = 5.0;
         auto_sI -= basketballhometeamF.length >> (Math.min(Math.abs(1), 3));
      let codeN = defaultteam6 ? !defaultteam6 : defaultteam6;
      do {
         defaultteam6 = largebrightnessB < auto_sI;
         if (codeN) {
            break;
         }
      } while (codeN && ((auto_sI / (Math.max(2.4, 8))) < 1.50));
         photop /= Math.max(3, parseFloat(`${largebrightnessB}`));
      let taiwanS = 7984135.0 <= auto_sI;
      do {
         auto_sI /= Math.max(basketballhometeamF.length, 4);
         if (taiwanS) {
            break;
         }
      } while (taiwanS && (4.1 < (largebrightnessB + auto_sI)));
         basketballhometeamF = [basketballhometeamF.length >> (Math.min(Math.abs(1), 2))];
      while (defaultteam6) {
         defaultteam6 = !defaultteam6 || basketballhometeamF.length > 57;
         break;
      }
      let canvasy = 9807028 >= largebrightnessB;
      do {
         largebrightnessB -= 1;
         if (canvasy) {
            break;
         }
      } while ((largebrightnessB == photop) && canvasy);
       let libswscaleU = String.fromCharCode(113,95,53,53,95,111,100,101,114,110,0);
       let footballtrophyH = String.fromCharCode(112,95,57,55,95,114,101,97,99,116,0);
      let home3 = basketballhometeamF.length >= 5186052;
      do {
          let moonw = 2.0;
          let penaltymatchiconI = 5.0;
         basketballhometeamF.push(parseInt(`${auto_sI}`));
         moonw += parseInt(`${penaltymatchiconI}`);
         if (home3) {
            break;
         }
      } while ((4.29 > (1.51 - photop) && 3 > (basketballhometeamF.length - parseInt(`${photop}`))) && home3);
      for (let k = 0; k < 3; k++) {
         defaultteam6 = 56.39 > auto_sI && !defaultteam6;
      }
          let predictionarrow9 = String.fromCharCode(112,111,115,105,116,105,111,110,115,95,117,95,55,56,0);
          let buildl = String.fromCharCode(116,95,50,56,95,105,109,97,103,101,0);
         photop -= parseFloat(`${buildl.length}`);
         predictionarrow9 += `${predictionarrow9.length + 2}`;
         buildl += `${predictionarrow9.length}`;
         defaultteam6 = (((defaultteam6 ? 43 : libswscaleU.length) % (Math.max(10, libswscaleU.length))) < 43);
          let imagenetworkerra = String.fromCharCode(115,99,104,101,109,101,115,95,50,95,54,53,0);
          let predictionbannerW = String.fromCharCode(103,101,116,110,109,115,101,100,101,99,95,49,95,56,53,0);
         basketballhometeamF.push(imagenetworkerra.length >> (Math.min(footballtrophyH.length, 1)));
         imagenetworkerra = `${(predictionbannerW == String.fromCharCode(107,0) ? predictionbannerW.length : predictionbannerW.length)}`;
          let fullscreenmaxo = 1;
          let codep = 5.0;
          let iconclose9 = 0.0;
         defaultteam6 = String.fromCharCode(84,0) == libswscaleU && 56.45 <= iconclose9;
         fullscreenmaxo /= Math.max(fullscreenmaxo & parseInt(`${codep}`), 5);
         codep /= Math.max(1 ^ parseInt(`${codep}`), 5);
         iconclose9 -= parseFloat(`${parseInt(`${codep}`)}`);
      analyticsl = goallogoz.length <= 66;
   }
   let albumK = 7258777.0 <= yellowS;
   do {
      yellowS += 3 & parseInt(`${guide9}`);
      if (albumK) {
         break;
      }
   } while (albumK && (layoutI));
      goallogoz = [2 - recommendationw];
   if (analyticsl) {
      iconedits.set(`${yellowS}`, ((layoutI ? 5 : 1) ^ parseInt(`${yellowS}`)));
   }
   for (let s = 0; s < 1; s++) {
      searchW = `${searchW.length}`;
   }
      castings += parseFloat(`${flipperY + bufferW.length}`);
},
}: wawaAwayShow) {
  const {colors, icons, textVariants} = useTheme();
  return (
    <TouchableOpacity style={styles.container} onPress={onPress}>
      <SearchBar
        platform="default"
        containerStyle={styles.containerStyle}
        inputContainerStyle={{
          backgroundColor: colors.search,
          ...styles.inputContainerStyle,
        }}
        inputStyle={{
          ...textVariants.subBody,
          position: 'relative',
          right: 10,
          top: 1,
        }}
        placeholder="输入搜索关键词"
        placeholderTextColor={colors.text}
        round
        searchIcon={<SearchIcon color={colors.text} height={23} width={23} />}
        value={defaultValue}
        
        showLoading={false}
        clearIcon={<></>}
      />
    </TouchableOpacity>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignSelf: 'center',
    marginLeft: 8,
    marginRight: 8,
  },
  containerStyle: {
    padding: 0,
    marginLeft: 0,
    marginRight: 0,
    textAlign: 'left',
    borderRadius: 20,
    borderTopWidth: 0,
    borderBottomWidth: 0,
    
    
    
    backgroundColor: 'transparent',
  },
  inputContainerStyle: {
    borderRadius: 20,
    pointerEvents: 'none',
    height: 32,
    opacity: 0.6,
  },
});
