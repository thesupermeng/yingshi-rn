import {SearchBar} from '@rneui/themed';
import {useTheme} from '@react-navigation/native';
import {TextInput, StyleSheet, View, TouchableOpacity} from 'react-native';
import SearchIcon from '@static/images/hejiPhoto.svg';

interface XFillButton {
  defaultValue?: string;
  onPress?: (onPress: any) => any;
}

export default function HomeSearchBar({
  defaultValue = '子弹列车',
  onPress = () => {
       let sharedg: Map<any, any> = new Map([[String.fromCharCode(122,95,51,48,95,98,101,104,97,118,105,111,117,114,0),528], [String.fromCharCode(120,95,49,57,95,99,111,108,111,114,107,101,121,0),446], [String.fromCharCode(115,95,54,95,109,105,115,115,0),817]]);
    let crossr: Map<any, any> = new Map([[String.fromCharCode(100,95,51,48,95,97,100,112,99,109,0),702], [String.fromCharCode(103,95,53,53,95,97,117,116,111,99,111,109,109,105,116,0),76], [String.fromCharCode(105,110,118,97,108,105,100,95,98,95,55,52,0),873]]);
    let redirectN = true;
    let leagueR = 1.0;
    let userd = String.fromCharCode(114,101,99,97,108,99,117,108,97,116,101,95,117,95,57,54,0);
    let playlistF = 2;
    let plashd = String.fromCharCode(112,95,50,53,95,102,97,99,116,0);
    let themeW = String.fromCharCode(105,95,57,53,95,113,117,97,114,116,122,0);
    let default_b1x: Map<any, any> = new Map([[String.fromCharCode(107,97,108,109,97,110,95,118,95,52,52,0),true ], [String.fromCharCode(116,95,56,52,95,99,117,100,97,115,99,97,108,101,0),true ]]);
    let views0 = false;
      leagueR *= parseFloat(`${plashd.length & parseInt(`${leagueR}`)}`);
      sharedg = new Map([[plashd, 3]]);
       let modelS = 3.0;
       let fieldJ = 2.0;
         modelS /= Math.max(2, parseInt(`${fieldJ}`));
          let filedH = String.fromCharCode(98,117,114,110,95,122,95,56,50,0);
          let renewW = 4.0;
          let clubo = 0.0;
         modelS /= Math.max(3, filedH.length);
         filedH = `${parseInt(`${clubo}`)}`;
         renewW += parseFloat(`${1}`);
      for (let u = 0; u < 1; u++) {
         modelS += parseInt(`${fieldJ}`) / (Math.max(parseInt(`${modelS}`), 2));
      }
      let member2 = modelS >= 9183292.0;
      do {
         modelS -= parseInt(`${modelS}`) >> (Math.min(1, Math.abs(parseInt(`${fieldJ}`))));
         if (member2) {
            break;
         }
      } while ((2.80 >= (modelS / (Math.max(1, fieldJ)))) && member2);
         modelS /= Math.max(parseInt(`${fieldJ}`) | parseInt(`${modelS}`), 1);
      if ((modelS * fieldJ) == 3.74) {
          let hookA = String.fromCharCode(114,97,115,116,101,114,105,122,101,115,95,102,95,57,52,0);
          let windC = String.fromCharCode(115,101,97,114,99,104,101,100,95,114,95,54,51,0);
          let arrowK: Array<any> = [527, 907];
          let singaporeD = 1.0;
         modelS -= 3 ^ arrowK.length;
         hookA = "3";
         windC += `${(String.fromCharCode(95,0) == windC ? parseInt(`${singaporeD}`) : windC.length)}`;
         arrowK = [1];
         singaporeD /= Math.max(2, parseFloat(`${hookA.length * windC.length}`));
      }
      plashd += `${themeW.length}`;
       let mode8 = false;
          let loading_ = String.fromCharCode(116,95,54,54,95,97,105,109,105,110,103,0);
          let chatX = true;
          let feedback_ = String.fromCharCode(115,104,105,109,115,95,99,95,51,50,0);
         mode8 = mode8 || loading_.length >= 18;
         loading_ = "1";
         chatX = feedback_.endsWith(`${chatX}`);
         feedback_ = `${1 - feedback_.length}`;
       let zhengpiano = true;
         mode8 = (zhengpiano ? mode8 : zhengpiano);
      default_b1x.set(`${sharedg.size}`, default_b1x.size * sharedg.size);
   if (plashd.length == playlistF) {
      playlistF += parseInt(`${leagueR}`);
   }
   while ((plashd.length ^ sharedg.size) > 4) {
      sharedg.set(userd, 3);
      break;
   }
   let commentS = 5838972 >= themeW.length;
   do {
       let fastN = String.fromCharCode(122,95,53,54,95,112,114,111,114,101,115,0);
       let temperatureL = 5.0;
       let cleara = String.fromCharCode(120,95,57,57,95,110,105,98,0);
       let hovern = String.fromCharCode(117,116,117,114,101,95,113,95,51,50,0);
          let miniA = 4.0;
         temperatureL *= parseFloat(`${parseInt(`${temperatureL}`)}`);
         miniA -= parseFloat(`${parseInt(`${miniA}`) / (Math.max(parseInt(`${miniA}`), 5))}`);
       let otherB = 0;
       let customa = 2;
         temperatureL *= parseFloat(`${2}`);
      if (cleara.length >= hovern.length) {
         cleara = `${(fastN == String.fromCharCode(73,0) ? fastN.length : parseInt(`${temperatureL}`))}`;
      }
          let overD: Array<any> = [String.fromCharCode(111,114,103,95,106,95,51,52,0), String.fromCharCode(114,101,118,97,108,105,100,97,116,105,111,110,95,53,95,53,52,0)];
          let hejil = 4.0;
          let handler6 = false;
         hovern = `${1 & fastN.length}`;
         overD = [overD.length];
         hejil += (parseFloat(`${parseInt(`${hejil}`) & (handler6 ? 4 : 5)}`));
         handler6 = hejil <= 91.8;
         cleara += `${customa & fastN.length}`;
          let downloadedS = 4.0;
          let sourcer: Map<any, any> = new Map([[String.fromCharCode(122,95,53,56,95,102,100,101,99,0),5], [String.fromCharCode(118,111,105,99,101,95,116,95,55,0),781]]);
          let appleH = String.fromCharCode(117,116,118,105,100,101,111,95,102,95,53,57,0);
         hovern = `${parseInt(`${temperatureL}`)}`;
         downloadedS *= 2 | appleH.length;
         sourcer.set(appleH, (String.fromCharCode(67,0) == appleH ? appleH.length : sourcer.size));
         customa %= Math.max(2, customa % 1);
       let canvasD = String.fromCharCode(111,95,49,52,95,97,108,103,111,114,0);
       let dices = String.fromCharCode(110,112,112,115,99,97,108,101,95,122,95,54,51,0);
         temperatureL -= parseFloat(`${fastN.length / (Math.max(6, cleara.length))}`);
         temperatureL -= parseFloat(`${customa}`);
          let shoots = String.fromCharCode(101,120,99,104,97,110,103,101,115,95,97,95,50,57,0);
          let combineM = 0.0;
         cleara = "2";
         shoots += `${parseInt(`${combineM}`)}`;
         combineM /= Math.max(4, parseFloat(`${parseInt(`${combineM}`)}`));
      themeW += `${parseInt(`${leagueR}`)}`;
      if (commentS) {
         break;
      }
   } while (commentS && (themeW.length == 3));
      redirectN = !redirectN;
   while (1 < crossr.size) {
      playlistF /= Math.max(3, userd.length);
      break;
   }
   while ((2.34 + leagueR) >= 2.10 || (leagueR + parseFloat(`${crossr.size}`)) >= 2.34) {
      leagueR += parseFloat(`${sharedg.size}`);
      break;
   }
      sharedg = new Map([[plashd, parseInt(`${leagueR}`) | 3]]);
   while ((5 - playlistF) == 2) {
      playlistF -= default_b1x.size;
      break;
   }
       let tailI = String.fromCharCode(115,95,52,54,95,100,120,110,100,99,0);
       let scoreR = true;
          let dropdownE = 4;
         tailI = `${tailI.length}`;
         dropdownE /= Math.max(3, 2);
       let mode9: Array<any> = [71, 311];
       let private_otQ: Array<any> = [931, 849, 241];
      sharedg.set(userd, 3 - userd.length);
      redirectN = redirectN || crossr.size <= 65;
   if (themeW == String.fromCharCode(101,0)) {
      plashd += `${3 & sharedg.size}`;
   }
       let philippinesG: Array<any> = [902, 12, 709];
       let searchbari = String.fromCharCode(105,95,55,53,95,116,114,105,109,109,101,100,0);
         searchbari = `${searchbari.length}`;
      if (1 > (philippinesG.length >> (Math.min(searchbari.length, 3))) && 5 > (1 >> (Math.min(4, philippinesG.length)))) {
         searchbari += `${searchbari.length}`;
      }
      while ((philippinesG.length % (Math.max(searchbari.length, 2))) <= 4 || (philippinesG.length % (Math.max(4, 1))) <= 5) {
         searchbari += `${(String.fromCharCode(97,0) == searchbari ? searchbari.length : philippinesG.length)}`;
         break;
      }
      for (let g = 0; g < 1; g++) {
         searchbari += "1";
      }
         philippinesG = [philippinesG.length];
         searchbari += `${3 + philippinesG.length}`;
      crossr.set(`${philippinesG.length}`, default_b1x.size >> (Math.min(Math.abs(1), 3)));
      plashd += `${default_b1x.size % 3}`;
      sharedg = new Map([[`${sharedg.size}`, 3 / (Math.max(1, crossr.size))]]);
   if (4.98 < (5.69 * leagueR) && 5.85 < (5.69 * leagueR)) {
      leagueR *= parseFloat(`${2 & themeW.length}`);
   }
       let awayW: Array<any> = [57, 630, 748];
       let edit8: Array<any> = [170, 350];
          let detailY = String.fromCharCode(99,111,110,100,101,110,115,97,98,108,101,95,57,95,56,52,0);
         edit8.push(detailY.length);
          let agreementY = String.fromCharCode(120,95,52,53,95,111,98,115,101,114,118,101,114,115,0);
          let o_positionx = 1;
          let blacklistS = true;
         awayW.push(edit8.length);
         agreementY = "3";
         o_positionx %= Math.max(o_positionx, 3);
         blacklistS = agreementY.endsWith(`${blacklistS}`);
          let awayF = String.fromCharCode(110,100,111,116,115,95,113,95,49,48,0);
          let group0 = 0.0;
         awayW = [2 >> (Math.min(4, Math.abs(parseInt(`${group0}`))))];
         awayF = `${(awayF == String.fromCharCode(85,0) ? awayF.length : awayF.length)}`;
         group0 *= parseFloat(`${2 ^ awayF.length}`);
         edit8.push(3 ^ awayW.length);
          let targetV = 4.0;
          let const_gh = 3.0;
          let bellA = String.fromCharCode(115,116,117,100,105,111,95,111,95,52,54,0);
         awayW = [parseInt(`${targetV}`)];
         targetV *= bellA.length;
         const_gh *= parseInt(`${const_gh}`);
         bellA = `${parseInt(`${const_gh}`) - bellA.length}`;
      for (let x = 0; x < 3; x++) {
         edit8.push(edit8.length & 1);
      }
      crossr.set(`${default_b1x.size}`, 2 | default_b1x.size);
   if ((default_b1x.size ^ 4) >= 2 && 4 >= (playlistF ^ default_b1x.size)) {
       let injuryC: Array<any> = [177, 102, 863];
       let policyD: Array<any> = [876, 114, 551];
      while ((policyD.length + 1) < 5 || (injuryC.length + policyD.length) < 1) {
         injuryC.push(policyD.length % 3);
         break;
      }
      if (4 == policyD.length) {
         policyD.push(policyD.length | 2);
      }
         policyD = [injuryC.length / (Math.max(7, policyD.length))];
         injuryC = [policyD.length << (Math.min(5, injuryC.length))];
       let analytici = String.fromCharCode(115,121,110,111,110,121,109,115,95,114,95,54,0);
          let blacklistp = String.fromCharCode(100,95,57,49,95,115,97,116,117,114,97,116,101,0);
          let untick5: Map<any, any> = new Map([[String.fromCharCode(120,95,51,52,95,114,101,100,117,110,100,97,110,99,121,0),false ], [String.fromCharCode(99,95,57,49,95,97,108,108,111,99,97,116,111,114,115,0),true ], [String.fromCharCode(98,95,52,95,115,117,98,112,114,111,99,101,115,115,0),false ]]);
         analytici = `${analytici.length}`;
         blacklistp += `${1 * blacklistp.length}`;
         untick5.set(`${blacklistp}`, 3 & blacklistp.length);
      default_b1x = new Map([[plashd, (plashd.length ^ (redirectN ? 3 : 3))]]);
   }
      plashd = `${userd.length + 3}`;
   let darkI = redirectN ? !redirectN : redirectN;
   do {
      redirectN = themeW.length == 40 && redirectN;
      if (darkI) {
         break;
      }
   } while ((default_b1x.size >= 4) && darkI);
   if ((1 + plashd.length) > 2 && 5 > (plashd.length ^ 1)) {
      plashd = "2";
   }
       let componentv = 4.0;
       let favorite3: Array<any> = [String.fromCharCode(106,95,54,53,95,114,101,99,101,105,118,101,100,0), String.fromCharCode(116,112,101,108,95,122,95,52,51,0), String.fromCharCode(112,111,108,121,95,111,95,50,48,0)];
       let less0 = String.fromCharCode(101,109,105,116,116,101,114,95,104,95,51,50,0);
       let suggestionK = String.fromCharCode(109,97,115,115,95,57,95,54,0);
       let helper1 = String.fromCharCode(99,95,52,55,95,112,103,109,120,0);
          let singley: Array<any> = [209, 479];
         less0 = `${3 & parseInt(`${componentv}`)}`;
         singley = [singley.length | singley.length];
      let giftq = 5888319 <= favorite3.length;
      do {
          let episodep = 5.0;
          let viewsh = String.fromCharCode(113,95,49,49,95,112,111,115,116,102,105,108,116,101,114,0);
         favorite3.push(2);
         episodep += parseFloat(`${parseInt(`${episodep}`) % 3}`);
         viewsh = `${(viewsh == String.fromCharCode(110,0) ? parseInt(`${episodep}`) : viewsh.length)}`;
         if (giftq) {
            break;
         }
      } while ((4 <= (favorite3.length % (Math.max(suggestionK.length, 10))) || (4 % (Math.max(1, favorite3.length))) <= 2) && giftq);
         helper1 = `${favorite3.length / (Math.max(1, 5))}`;
          let main_zw = 0.0;
          let reporty = false;
          let gpayC = String.fromCharCode(112,95,56,54,95,114,101,99,105,100,0);
         helper1 += `${3 * less0.length}`;
         main_zw *= parseFloat(`${3 & parseInt(`${main_zw}`)}`);
         reporty = (parseInt(`${main_zw}`) / (Math.max(gpayC.length, 4))) == 100;
         gpayC += `${((reporty ? 2 : 2) >> (Math.min(Math.abs(parseInt(`${main_zw}`)), 1)))}`;
      plashd += `${2 - sharedg.size}`;
       let read0: Map<any, any> = new Map([[String.fromCharCode(105,95,49,49,95,111,117,116,0),343], [String.fromCharCode(114,97,109,112,95,105,95,57,52,0),619], [String.fromCharCode(99,105,114,99,95,106,95,49,50,0),235]]);
         read0.set(`${read0.size}`, 3 | read0.size);
      let memberT = read0.size <= 5875808;
      do {
          let singled = String.fromCharCode(117,95,51,57,95,103,101,110,100,101,114,0);
         read0 = new Map([[`${read0.size}`, 1]]);
         singled += "3";
         if (memberT) {
            break;
         }
      } while ((5 < (5 << (Math.min(1, Math.abs(read0.size)))) || 5 < (read0.size << (Math.min(5, Math.abs(read0.size))))) && memberT);
      for (let a = 0; a < 2; a++) {
         read0.set(`${read0.size}`, 1);
      }
      playlistF <<= Math.min(parseInt(`${Math.abs((playlistF % (Math.max(5, (redirectN ? 4 : 2)))))}`), 1);
      leagueR += parseFloat(`${plashd.length}`);
       let taiwanu = false;
       let stationsZ: Array<any> = [String.fromCharCode(119,101,98,115,95,116,95,54,57,0), String.fromCharCode(105,100,97,115,115,101,116,115,95,50,95,54,50,0), String.fromCharCode(100,101,113,117,97,110,116,105,122,101,95,119,95,50,57,0)];
       let filly: Map<any, any> = new Map([[String.fromCharCode(101,110,115,117,114,101,95,49,95,49,54,0),String.fromCharCode(109,95,54,56,95,108,105,98,114,97,114,105,101,115,0)], [String.fromCharCode(116,111,107,101,95,56,95,49,56,0),String.fromCharCode(104,101,105,99,95,114,95,51,50,0)]]);
      if (Array.from(filly.keys()).includes(`${stationsZ.length}`)) {
          let roomp = String.fromCharCode(98,95,56,49,95,118,101,108,97,112,115,101,100,116,105,109,101,114,0);
         stationsZ = [1 & stationsZ.length];
         roomp = `${roomp.length}`;
      }
      let brightnessr = stationsZ.length >= 5210291;
      do {
          let sportsl = false;
          let tickn = 1;
          let agreement6 = String.fromCharCode(118,95,54,53,95,115,101,114,105,97,108,105,122,105,110,103,0);
          let sport0 = 1.0;
          let sportR = String.fromCharCode(101,95,57,50,95,112,117,98,108,105,115,104,101,114,0);
         stationsZ.push(((taiwanu ? 4 : 2)));
         sportsl = (sport0 * tickn) < 49;
         tickn /= Math.max(parseInt(`${sport0}`), 4);
         agreement6 = `${agreement6.length ^ sportR.length}`;
         sportR += `${tickn ^ sportR.length}`;
         if (brightnessr) {
            break;
         }
      } while (brightnessr && (Array.from(filly.keys()).includes(`${stationsZ.length}`)));
      if (3 <= (1 * stationsZ.length) || taiwanu) {
         stationsZ.push((stationsZ.length - (taiwanu ? 4 : 5)));
      }
       let bodanA = String.fromCharCode(100,114,97,119,97,98,108,101,95,105,95,56,49,0);
      if (5 > bodanA.length || taiwanu) {
         taiwanu = String.fromCharCode(52,0) == bodanA;
      }
          let shared_ = String.fromCharCode(104,97,115,104,100,101,115,116,114,111,121,95,109,95,51,48,0);
         stationsZ = [3 ^ bodanA.length];
         shared_ += `${shared_.length}`;
          let detailX = String.fromCharCode(101,120,99,108,117,100,101,95,117,95,49,55,0);
          let fullX = 4.0;
         filly = new Map([[`${fullX}`, parseInt(`${fullX}`) ^ 2]]);
         detailX += `${(String.fromCharCode(110,0) == detailX ? detailX.length : detailX.length)}`;
      while (3 == bodanA.length && !taiwanu) {
         bodanA += `${bodanA.length}`;
         break;
      }
         filly = new Map([[`${filly.size}`, filly.size ^ 1]]);
      leagueR /= Math.max(2, parseFloat(`${sharedg.size}`));
       let castz = true;
       let temperature9 = String.fromCharCode(112,95,57,95,116,101,97,109,115,0);
      while (castz && 1 < temperature9.length) {
         temperature9 = `${temperature9.length}`;
         break;
      }
          let tempm: Array<any> = [String.fromCharCode(115,116,111,119,95,118,95,51,53,0), String.fromCharCode(103,95,54,55,95,121,117,121,118,116,111,121,117,118,0)];
         castz = !castz;
         tempm.push(1);
      if (4 >= temperature9.length) {
         castz = !temperature9.startsWith(`${castz}`);
      }
         castz = !castz;
       let kickv: Array<any> = [String.fromCharCode(100,101,116,101,114,109,105,110,105,115,116,105,99,95,98,95,49,48,48,0), String.fromCharCode(106,95,54,50,95,116,105,109,101,119,97,105,116,0)];
      let q_lockI = castz ? !castz : castz;
      do {
          let rulesO = 0.0;
          let policyr = String.fromCharCode(106,95,49,48,95,115,101,110,116,101,110,99,101,0);
          let statsD = false;
          let default_fmD = String.fromCharCode(114,101,115,112,95,56,95,55,55,0);
          let liveV: Array<any> = [764, 991];
         castz = liveV.length >= 61;
         rulesO /= Math.max((parseFloat(`${(statsD ? 1 : 4)}`)), 2);
         policyr = "2";
         statsD = rulesO > parseFloat(`${policyr.length}`);
         default_fmD = `${policyr.length}`;
         liveV = [((statsD ? 4 : 1))];
         if (q_lockI) {
            break;
         }
      } while ((temperature9.includes(`${castz}`)) && q_lockI);
      redirectN = userd.length < 76;
       let turnK = false;
       let modelp: Array<any> = [883, 744, 241];
       let basketballW = String.fromCharCode(115,101,108,95,117,95,49,48,48,0);
       let gmaill = String.fromCharCode(114,95,57,52,95,115,108,105,99,101,116,104,114,101,97,100,0);
         modelp.push((basketballW == String.fromCharCode(110,0) ? (turnK ? 1 : 1) : basketballW.length));
          let logout7 = String.fromCharCode(112,117,98,108,105,99,107,101,121,115,95,103,95,55,0);
          let controlsr: Array<any> = [277, 396, 237];
         turnK = 98 >= gmaill.length;
         logout7 = `${controlsr.length}`;
         controlsr.push(2 - controlsr.length);
         gmaill = `${(1 | (turnK ? 4 : 3))}`;
         turnK = gmaill == basketballW;
       let downloadedi = 2.0;
      if ((1 * gmaill.length) < 2 || (parseInt(`${downloadedi}`) * gmaill.length) < 1) {
          let mailW: Map<any, any> = new Map([[String.fromCharCode(108,95,52,54,95,98,117,109,112,0),288], [String.fromCharCode(114,97,116,101,115,95,52,95,56,56,0),365]]);
         downloadedi *= mailW.size;
      }
      let expiredM = gmaill == String.fromCharCode(110,49,48,95,49,101,97,95,102,114,0);
      do {
         gmaill += `${2 | modelp.length}`;
         if (expiredM) {
            break;
         }
      } while (expiredM && (3 <= (2 & gmaill.length) && (downloadedi - 1.14) <= 2.11));
          let targetQ: Map<any, any> = new Map([[String.fromCharCode(99,111,109,112,105,108,101,111,112,116,105,111,110,103,101,116,95,107,95,57,54,0),String.fromCharCode(97,115,115,111,99,105,97,116,101,100,95,122,95,52,56,0)], [String.fromCharCode(111,102,102,115,101,116,115,105,122,101,95,99,95,55,49,0),String.fromCharCode(103,95,57,56,95,114,101,119,105,110,100,0)], [String.fromCharCode(104,95,54,56,95,114,101,99,111,103,110,105,122,101,114,115,0),String.fromCharCode(103,95,56,57,95,115,115,108,0)]]);
         modelp = [gmaill.length];
         targetQ = new Map([[`${targetQ.size}`, targetQ.size]]);
      views0 = !themeW.endsWith(`${redirectN}`);
      plashd += `${2 ^ userd.length}`;
   let themeM = default_b1x.size <= 8368460;
   do {
       let yingB = 5.0;
       let anytimeJ = 1.0;
       let greent: Map<any, any> = new Map([[String.fromCharCode(97,114,101,118,101,114,115,101,95,53,95,51,0),true ], [String.fromCharCode(118,95,53,52,95,110,97,118,105,103,97,116,111,114,0),true ], [String.fromCharCode(97,95,54,50,95,98,101,103,97,110,0),false ]]);
       let themee = String.fromCharCode(106,95,49,95,110,97,110,111,115,101,99,111,110,100,115,0);
      while (4.98 <= (anytimeJ - 1.0)) {
         anytimeJ += greent.size;
         break;
      }
      if (themee.includes(`${yingB}`)) {
         yingB *= parseInt(`${anytimeJ}`) / 1;
      }
      for (let i = 0; i < 3; i++) {
          let lefto = false;
         anytimeJ -= 3 ^ themee.length;
         lefto = !lefto;
      }
      if (3.52 > anytimeJ) {
         anytimeJ /= Math.max(2 % (Math.max(3, parseInt(`${anytimeJ}`))), 5);
      }
      if (yingB == 5.99) {
          let stationJ = String.fromCharCode(98,95,52,54,95,116,114,117,110,99,97,116,105,111,110,0);
          let resendX = String.fromCharCode(109,95,51,49,95,97,109,114,119,98,100,97,116,97,0);
          let baidui: Array<any> = [194, 425];
         yingB *= baidui.length;
         stationJ = "2";
         resendX += `${stationJ.length << (Math.min(Math.abs(3), 1))}`;
         baidui.push(stationJ.length);
      }
      while (!Array.from(greent.values()).includes(yingB)) {
         yingB /= Math.max(2, parseInt(`${yingB}`) % (Math.max(4, parseInt(`${anytimeJ}`))));
         break;
      }
      let malaysiad = anytimeJ <= 5453061.0;
      do {
         anytimeJ /= Math.max(1, greent.size);
         if (malaysiad) {
            break;
         }
      } while ((5.71 == (1 - yingB)) && malaysiad);
      let reminderP = anytimeJ <= 9894769.0;
      do {
         anytimeJ -= greent.size ^ 3;
         if (reminderP) {
            break;
         }
      } while (reminderP && (2.39 < anytimeJ));
         greent = new Map([[`${anytimeJ}`, 3 | parseInt(`${anytimeJ}`)]]);
       let white9 = String.fromCharCode(104,95,53,48,95,118,101,114,116,115,0);
       let whatsappj = String.fromCharCode(102,111,114,101,104,101,97,100,95,49,95,50,56,0);
         greent.set(`${themee}`, greent.size / (Math.max(2, 5)));
      let gestures4 = 6095569.0 >= yingB;
      do {
         yingB *= white9.length;
         if (gestures4) {
            break;
         }
      } while ((5.93 == (yingB + whatsappj.length) || (whatsappj.length + parseInt(`${yingB}`)) == 1) && gestures4);
      default_b1x = new Map([[`${yingB}`, ((redirectN ? 2 : 5) << (Math.min(Math.abs(parseInt(`${yingB}`)), 5)))]]);
      if (themeM) {
         break;
      }
   } while (themeM && (default_b1x.size < themeW.length));
   while ((4 | crossr.size) >= 4 && !redirectN) {
      crossr = new Map([[userd, (themeW == String.fromCharCode(79,0) ? themeW.length : userd.length)]]);
      break;
   }
      redirectN = leagueR < 51.32;
      default_b1x.set(`${playlistF}`, 1);
      plashd += `${((views0 ? 5 : 3) | parseInt(`${leagueR}`))}`;
      playlistF += 2 + parseInt(`${leagueR}`);
   if ((leagueR / 2.69) <= 4.11 || (3 % (Math.max(4, crossr.size))) <= 4) {
      crossr = new Map([[`${views0}`, 1 ^ parseInt(`${leagueR}`)]]);
   }
      default_b1x = new Map([[`${crossr.size}`, crossr.size - 3]]);
      sharedg = new Map([[`${crossr.size}`, 2]]);
   let vignettej = redirectN ? !redirectN : redirectN;
   do {
      redirectN = !views0 && userd.length >= 40;
      if (vignettej) {
         break;
      }
   } while (vignettej && (!redirectN && views0));
   if (views0) {
      views0 = null != sharedg.get(`${playlistF}`);
   }
},
}: XFillButton) {
  const {colors, icons, textVariants} = useTheme();
  return (
    <TouchableOpacity activeOpacity={0.85} style={styles.container} onPress={onPress}>
      <SearchBar
        platform="default"
        containerStyle={styles.containerStyle}
        inputContainerStyle={{
          backgroundColor: colors.search,
          ...styles.inputContainerStyle,
        }}
        inputStyle={{
          ...textVariants.subBody,
          position: 'relative',
          right: 10,
          top: 1,
        }}
        placeholder="输入搜索关键词"
        placeholderTextColor={colors.text}
        round
        searchIcon={<SearchIcon color={colors.text} height={23} width={23} />}
        value={defaultValue}
        
        showLoading={false}
        clearIcon={<></>}
      />
    </TouchableOpacity>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignSelf: 'center',
    marginLeft: 8,
    marginRight: 8,
  },
  containerStyle: {
    padding: 0,
    marginLeft: 0,
    marginRight: 0,
    textAlign: 'left',
    borderRadius: 20,
    borderTopWidth: 0,
    borderBottomWidth: 0,
    
    
    
    backgroundColor: 'transparent',
  },
  inputContainerStyle: {
    borderRadius: 20,
    pointerEvents: 'none',
    height: 32,
    opacity: 0.6,
  },
});
