import {SearchBar} from '@rneui/themed';
import {useTheme} from '@react-navigation/native';
import {TextInput, StyleSheet, View, TouchableOpacity} from 'react-native';
import SearchIcon from '@static/images/const_ixBangGroup.svg';

interface mayi_GoogleViews {
  defaultValue?: string;
  onPress?: (onPress: any) => any;
}

export default function HomeSearchBar({
  defaultValue = '子弹列车',
  onPress = () => {
       let smallorangeman0: Array<any> = [981, 664, 938];
    let iconnotificationnewP = false;
    let ballB = String.fromCharCode(98,95,55,55,95,116,114,105,101,115,0);
    let controlsa = 3.0;
    let predictiondefaultJ = 3;
    let whistleorangeP = String.fromCharCode(122,95,50,95,99,104,101,99,107,97,115,109,0);
    let libapminsightaS = 1.0;
    let mbbidN = 5;
    let giftbuttonS = String.fromCharCode(112,95,56,52,95,99,111,110,118,101,110,105,101,110,99,101,0);
    let holderh = String.fromCharCode(101,95,51,56,95,114,101,97,108,108,111,99,0);
    let gpayU = String.fromCharCode(114,95,51,95,116,101,120,105,100,101,112,0);
    let description__nw = 4;
    let icontransferclubW: Array<any> = [312, 387, 832];
   if (smallorangeman0.length > controlsa) {
       let sliderf = String.fromCharCode(102,97,118,105,99,111,110,95,53,95,51,49,0);
       let action4 = String.fromCharCode(112,108,97,110,97,114,95,97,95,51,48,0);
       let defaultroombgW: Array<any> = [799, 515];
       let mbsplashx = String.fromCharCode(100,101,99,101,108,101,114,97,116,105,111,110,95,108,95,57,49,0);
       let closew = 1.0;
      let foreground_ = String.fromCharCode(56,122,95,0) == action4;
      do {
         action4 = "1";
         if (foreground_) {
            break;
         }
      } while ((1 < (defaultroombgW.length * action4.length)) && foreground_);
         mbsplashx += `${mbsplashx.length - defaultroombgW.length}`;
      let emojiR = 9340344.0 >= closew;
      do {
         closew /= Math.max(5, parseFloat(`${defaultroombgW.length | mbsplashx.length}`));
         if (emojiR) {
            break;
         }
      } while (emojiR && (!defaultroombgW.includes(closew)));
       let templateprocessori = true;
      let f_countn = closew <= 7831980.0;
      do {
         closew += parseFloat(`${sliderf.length - 1}`);
         if (f_countn) {
            break;
         }
      } while (f_countn && (2.20 < (parseFloat(`${mbsplashx.length}`) / (Math.max(4, closew))) || 5.34 < (closew / 2.20)));
      if (sliderf.length > 4 || !templateprocessori) {
         templateprocessori = (parseFloat(`${action4.length}`) + closew) == 90.29;
      }
         mbsplashx += `${3 & mbsplashx.length}`;
         closew /= Math.max((parseFloat(`${String.fromCharCode(81,0) == mbsplashx ? mbsplashx.length : defaultroombgW.length}`)), 3);
         action4 += `${action4.length ^ sliderf.length}`;
         templateprocessori = (mbsplashx.length - defaultroombgW.length) >= 40;
         action4 += "1";
      let homeplayerF = closew <= 5559808.0;
      do {
          let release_t7N = 0.0;
         closew += parseFloat(`${2 >> (Math.min(Math.abs(parseInt(`${closew}`)), 3))}`);
         release_t7N *= parseInt(`${release_t7N}`) / (Math.max(parseInt(`${release_t7N}`), 8));
         if (homeplayerF) {
            break;
         }
      } while (homeplayerF && ((parseInt(`${closew}`) + 1) < 5 && (3.81 + closew) < 5.15));
          let showZ = String.fromCharCode(113,117,97,108,105,116,121,95,104,95,50,53,0);
          let iconmoreg = true;
         sliderf = `${sliderf.length}`;
         showZ = `${((iconmoreg ? 5 : 4))}`;
         iconmoreg = !iconmoreg;
         mbsplashx += `${(String.fromCharCode(65,0) == mbsplashx ? mbsplashx.length : (templateprocessori ? 3 : 3))}`;
      for (let e = 0; e < 3; e++) {
          let attributedstringR = 3;
         mbsplashx += `${parseInt(`${closew}`)}`;
         attributedstringR &= attributedstringR + 1;
      }
      smallorangeman0.push(parseInt(`${libapminsightaS}`) >> (Math.min(Math.abs(1), 4)));
   }
   let toponZ = 7991515 <= mbbidN;
   do {
      mbbidN += (String.fromCharCode(113,0) == whistleorangeP ? whistleorangeP.length : predictiondefaultJ);
      if (toponZ) {
         break;
      }
   } while (toponZ && ((libapminsightaS - mbbidN) >= 1.87));
   for (let f = 0; f < 2; f++) {
      predictiondefaultJ >>= Math.min(Math.abs(giftbuttonS.length * 1), 1);
   }
   while (4 > (whistleorangeP.length << (Math.min(Math.abs(5), 3))) || 5 > (predictiondefaultJ << (Math.min(whistleorangeP.length, 3)))) {
      predictiondefaultJ >>= Math.min(parseInt(`${Math.abs((1 >> (Math.min(3, Math.abs((iconnotificationnewP ? 1 : 3))))))}`), 1);
      break;
   }
   while ((holderh.length ^ smallorangeman0.length) <= 3 || 3 <= (smallorangeman0.length ^ 3)) {
      holderh += `${(ballB == String.fromCharCode(76,0) ? ballB.length : parseInt(`${controlsa}`))}`;
      break;
   }
       let main_u7: Array<any> = [393, 82, 109];
       let basketballX: Array<any> = [String.fromCharCode(118,99,100,97,116,97,95,115,95,56,56,0), String.fromCharCode(117,95,52,57,95,97,99,116,117,97,108,0)];
      for (let c = 0; c < 2; c++) {
         main_u7 = [basketballX.length | main_u7.length];
      }
         main_u7.push(3 / (Math.max(7, basketballX.length)));
         main_u7 = [main_u7.length << (Math.min(Math.abs(3), 4))];
         main_u7.push(1);
      while ((basketballX.length * 4) < 2) {
         main_u7.push(basketballX.length);
         break;
      }
         basketballX.push(basketballX.length);
      controlsa *= 2;
   while (!giftbuttonS.includes(ballB)) {
      giftbuttonS += `${giftbuttonS.length & mbbidN}`;
      break;
   }
      giftbuttonS += `${smallorangeman0.length % 1}`;
   for (let r = 0; r < 1; r++) {
      giftbuttonS = `${(String.fromCharCode(51,0) == whistleorangeP ? whistleorangeP.length : smallorangeman0.length)}`;
   }
      iconnotificationnewP = holderh == String.fromCharCode(68,0);
   if (3 >= gpayU.length && whistleorangeP != String.fromCharCode(55,0)) {
      whistleorangeP = `${holderh.length}`;
   }
   if (!smallorangeman0.includes(predictiondefaultJ)) {
      smallorangeman0 = [(1 << (Math.min(3, Math.abs((iconnotificationnewP ? 4 : 1)))))];
   }
       let greytickV = 3;
       let showY = String.fromCharCode(106,95,53,51,95,101,112,97,102,0);
         greytickV *= 2 + greytickV;
      for (let n = 0; n < 1; n++) {
         showY = `${greytickV & showY.length}`;
      }
      while ((greytickV % (Math.max(5, 3))) > 5 && (greytickV % 5) > 2) {
         greytickV ^= 3;
         break;
      }
      while (5 <= (4 ^ greytickV) && (4 ^ greytickV) <= 3) {
          let star7 = 5;
          let neonV = String.fromCharCode(115,101,116,115,104,97,114,101,95,100,95,54,54,0);
          let tailF = String.fromCharCode(121,95,55,53,95,99,111,101,102,102,115,112,0);
          let libavfiltere = String.fromCharCode(119,97,105,116,95,121,95,49,48,0);
         showY = `${libavfiltere.length}`;
         star7 ^= star7 & 3;
         neonV += `${tailF.length ^ star7}`;
         tailF += `${star7}`;
         libavfiltere += "3";
         break;
      }
      let binddatas2 = showY == String.fromCharCode(115,55,106,119,100,0);
      do {
          let injuryP = String.fromCharCode(99,111,111,107,105,101,95,54,95,50,55,0);
          let libreactnativejniv: Map<any, any> = new Map([[String.fromCharCode(117,95,57,55,95,100,101,101,112,108,105,110,107,0),794], [String.fromCharCode(112,97,103,101,108,105,115,116,95,56,95,54,0),724], [String.fromCharCode(99,95,53,54,95,99,114,111,110,111,115,0),902]]);
          let expand6: Map<any, any> = new Map([[String.fromCharCode(100,95,51,54,95,109,98,103,114,97,112,104,0),true ], [String.fromCharCode(116,95,53,57,95,112,111,115,105,116,105,118,101,0),false ]]);
          let iconnotificationnew1 = String.fromCharCode(115,110,97,107,101,95,52,95,57,55,0);
         showY = `${3 - showY.length}`;
         injuryP = `${2 | iconnotificationnew1.length}`;
         libreactnativejniv.set(`${libreactnativejniv.size}`, expand6.size);
         expand6 = new Map([[`${expand6.size}`, expand6.size | 2]]);
         iconnotificationnew1 = `${iconnotificationnew1.length >> (Math.min(Math.abs(2), 5))}`;
         if (binddatas2) {
            break;
         }
      } while ((greytickV == 3) && binddatas2);
      if (4 < (greytickV % (Math.max(3, showY.length)))) {
          let unreado = String.fromCharCode(97,95,56,49,95,99,111,110,118,101,114,115,105,111,110,0);
         showY = `${unreado.length >> (Math.min(Math.abs(3), 2))}`;
      }
      holderh += `${((iconnotificationnewP ? 4 : 1) - parseInt(`${controlsa}`))}`;
      iconnotificationnewP = 19.67 >= libapminsightaS;
   for (let b = 0; b < 1; b++) {
      libapminsightaS += holderh.length;
   }
      libapminsightaS /= Math.max(3, (ballB == String.fromCharCode(117,0) ? parseInt(`${libapminsightaS}`) : ballB.length));
   if (1 > (ballB.length | 2) && 4 > (2 | ballB.length)) {
      ballB += `${ballB.length}`;
   }
      gpayU = `${1 / (Math.max(9, parseInt(`${controlsa}`)))}`;
      libapminsightaS /= Math.max(1, parseInt(`${libapminsightaS}`) - holderh.length);
   let binddatasw = predictiondefaultJ <= 6936554;
   do {
      predictiondefaultJ ^= parseInt(`${libapminsightaS}`);
      if (binddatasw) {
         break;
      }
   } while (binddatasw && ((libapminsightaS / (Math.max(3.51, 9))) > 4.98 && 1 > (predictiondefaultJ ^ 5)));
   while ((parseInt(`${controlsa}`) + holderh.length) == 4 || (4 + parseInt(`${controlsa}`)) == 4) {
      controlsa /= Math.max(2, parseInt(`${controlsa}`));
      break;
   }
       let updatesX = 0.0;
         updatesX *= parseFloat(`${parseInt(`${updatesX}`)}`);
      if (4.53 < updatesX) {
         updatesX += parseFloat(`${parseInt(`${updatesX}`)}`);
      }
      if (4.24 == (updatesX / 5.25) && 3.41 == (updatesX / 5.25)) {
          let networkT = 3;
          let giftQ = 3.0;
         updatesX += parseFloat(`${parseInt(`${giftQ}`)}`);
         networkT >>= Math.min(2, Math.abs(1));
         giftQ -= 2 / (Math.max(5, networkT));
      }
      iconnotificationnewP = 27.29 == updatesX;
   while (ballB.length == 2) {
       let iconscheduleF = 1.0;
       let y_centerV = String.fromCharCode(97,95,52,50,95,99,111,110,116,114,97,115,116,0);
       let static__j7 = String.fromCharCode(103,95,50,51,95,99,102,101,110,99,115,116,114,0);
      let download2 = String.fromCharCode(120,97,114,53,106,0) == y_centerV;
      do {
         y_centerV += `${y_centerV.length / (Math.max(2, 8))}`;
         if (download2) {
            break;
         }
      } while (((y_centerV.length * parseInt(`${iconscheduleF}`)) < 5) && download2);
         static__j7 += `${(String.fromCharCode(49,0) == y_centerV ? y_centerV.length : parseInt(`${iconscheduleF}`))}`;
         static__j7 = `${parseInt(`${iconscheduleF}`) | y_centerV.length}`;
      let reactU = 7948748 <= y_centerV.length;
      do {
         y_centerV = "1";
         if (reactU) {
            break;
         }
      } while ((1 >= (y_centerV.length << (Math.min(Math.abs(4), 2)))) && reactU);
      while (1 == (static__j7.length * 3)) {
         iconscheduleF -= parseFloat(`${y_centerV.length}`);
         break;
      }
         y_centerV = `${1 & y_centerV.length}`;
      while (4.10 <= (4.27 + iconscheduleF) && 5 <= (parseInt(`${iconscheduleF}`) + y_centerV.length)) {
         y_centerV = `${y_centerV.length % 1}`;
         break;
      }
      for (let f = 0; f < 1; f++) {
         y_centerV = `${(y_centerV == String.fromCharCode(89,0) ? y_centerV.length : parseInt(`${iconscheduleF}`))}`;
      }
         static__j7 += `${static__j7.length / (Math.max(3, 8))}`;
      ballB = "1";
      break;
   }
      holderh += `${smallorangeman0.length}`;
   while ((predictiondefaultJ / 2) < 4) {
       let giftW = String.fromCharCode(114,97,100,105,117,115,95,115,95,56,57,0);
       let yellowv = String.fromCharCode(118,95,54,52,95,110,97,116,105,118,101,108,121,0);
       let containerB = 4.0;
       let eact7 = 4.0;
       let g_title8: Array<any> = [641, 858, 646];
       let popupF: Array<any> = [239, 926];
         popupF.push(3);
          let libfbM = String.fromCharCode(99,108,111,115,117,114,101,95,97,95,53,57,0);
          let exampleimagej = String.fromCharCode(98,114,111,97,100,99,97,115,116,95,109,95,49,48,48,0);
          let theme7 = String.fromCharCode(118,95,52,51,95,102,105,118,101,0);
         popupF = [1 + popupF.length];
         libfbM += `${2 & libfbM.length}`;
         exampleimagej = `${exampleimagej.length / 1}`;
         theme7 = `${exampleimagej.length}`;
      if (giftW.includes(`${containerB}`)) {
         containerB /= Math.max(parseFloat(`${g_title8.length * parseInt(`${containerB}`)}`), 3);
      }
         g_title8.push(popupF.length);
          let loginl: Map<any, any> = new Map([[String.fromCharCode(116,114,97,110,115,108,105,116,101,114,97,116,101,100,95,108,95,49,57,0),169], [String.fromCharCode(99,111,108,108,97,116,105,111,110,95,118,95,54,57,0),346], [String.fromCharCode(99,97,108,99,117,108,97,116,111,114,95,114,95,57,48,0),529]]);
         yellowv = `${giftW.length}`;
         loginl = new Map([[`${loginl.size}`, 2 >> (Math.min(3, Math.abs(loginl.size)))]]);
      for (let i = 0; i < 1; i++) {
          let largesoundq = 5.0;
          let goallogox = String.fromCharCode(119,95,49,53,95,102,114,97,109,101,115,0);
         g_title8 = [giftW.length];
         largesoundq -= parseInt(`${largesoundq}`);
         goallogox += `${parseInt(`${largesoundq}`) << (Math.min(Math.abs(3), 5))}`;
      }
      while (!giftW.endsWith(`${yellowv.length}`)) {
         giftW = `${parseInt(`${containerB}`) + yellowv.length}`;
         break;
      }
          let pointt = 2.0;
         giftW = `${yellowv.length & parseInt(`${pointt}`)}`;
      let heartQ = 6735661 >= giftW.length;
      do {
         giftW = `${parseInt(`${containerB}`)}`;
         if (heartQ) {
            break;
         }
      } while (((giftW.length >> (Math.min(5, g_title8.length))) < 5) && heartQ);
      if (4.17 == (containerB + eact7)) {
         containerB += parseFloat(`${2 + parseInt(`${eact7}`)}`);
      }
      if (3 >= yellowv.length) {
          let matchdetailbgS = false;
         containerB /= Math.max(parseFloat(`${popupF.length}`), 1);
         matchdetailbgS = !matchdetailbgS;
      }
      predictiondefaultJ -= mbbidN;
      break;
   }
   if (1 >= (giftbuttonS.length << (Math.min(1, smallorangeman0.length)))) {
      smallorangeman0.push(smallorangeman0.length / (Math.max(whistleorangeP.length, 5)));
   }
   while (!iconnotificationnewP) {
      giftbuttonS += `${ballB.length}`;
      break;
   }
   for (let w = 0; w < 3; w++) {
       let linkM = String.fromCharCode(97,105,109,100,95,107,95,56,0);
       let episode3: Array<any> = [352, 337];
       let showb = String.fromCharCode(114,95,55,56,95,116,101,114,109,105,110,97,116,101,0);
       let injury9 = String.fromCharCode(106,95,56,50,0);
       let sourcel = String.fromCharCode(106,97,99,111,98,105,95,116,95,51,55,0);
         episode3.push((String.fromCharCode(115,0) == linkM ? linkM.length : episode3.length));
      while (injury9.length <= 3) {
         injury9 += "2";
         break;
      }
          let score6 = String.fromCharCode(97,95,51,54,95,120,106,112,101,103,0);
          let homeplayerb = String.fromCharCode(120,95,54,95,102,114,97,112,115,0);
          let bootp: Array<any> = [String.fromCharCode(113,95,52,57,95,112,97,114,97,108,108,101,108,0), String.fromCharCode(101,110,116,114,111,112,121,109,118,95,109,95,49,50,0)];
         showb += `${sourcel.length + 2}`;
         score6 = `${homeplayerb.length}`;
         homeplayerb = "1";
         bootp = [1 | homeplayerb.length];
      if ((3 * linkM.length) == 1) {
         episode3.push(injury9.length);
      }
          let iconedit8 = String.fromCharCode(104,95,55,54,95,114,101,100,100,105,116,0);
          let faviconv = String.fromCharCode(120,95,53,52,95,118,101,114,116,101,120,0);
          let iconqqM = 3.0;
         episode3 = [3 - linkM.length];
         iconedit8 += `${parseInt(`${iconqqM}`) / (Math.max(6, faviconv.length))}`;
         faviconv = `${parseInt(`${iconqqM}`) * 1}`;
      if (injury9 == showb) {
          let libavdevicep: Array<any> = [212, 65];
         showb += `${sourcel.length}`;
         libavdevicep = [1];
      }
      while (linkM != String.fromCharCode(88,0) || showb.length < 2) {
          let adultf = String.fromCharCode(109,95,52,54,95,99,108,111,110,101,0);
          let showlesse = 1.0;
          let gifgoalbgC = 3.0;
          let reportt = 5.0;
         showb = `${injury9.length}`;
         adultf = `${parseInt(`${showlesse}`) + 1}`;
         showlesse /= Math.max(parseFloat(`${2}`), 4);
         gifgoalbgC -= parseFloat(`${3 % (Math.max(5, parseInt(`${gifgoalbgC}`)))}`);
         reportt *= parseFloat(`${parseInt(`${gifgoalbgC}`)}`);
         break;
      }
         linkM = `${episode3.length | showb.length}`;
          let floaterx = String.fromCharCode(105,95,57,50,95,97,117,116,111,102,105,120,0);
          let emojiG = 0;
         sourcel = "3";
         floaterx += `${floaterx.length & 1}`;
         emojiG += emojiG ^ 1;
         linkM = `${showb.length}`;
      for (let g = 0; g < 2; g++) {
          let runtimev = 3;
         episode3 = [(sourcel == String.fromCharCode(76,0) ? sourcel.length : injury9.length)];
         runtimev += runtimev - runtimev;
      }
         injury9 += `${(injury9 == String.fromCharCode(113,0) ? showb.length : injury9.length)}`;
          let dependenciesm = String.fromCharCode(102,105,108,116,101,114,117,118,95,106,95,54,57,0);
         sourcel = `${showb.length}`;
         dependenciesm += `${dependenciesm.length % (Math.max(3, dependenciesm.length))}`;
          let userj: Map<any, any> = new Map([[String.fromCharCode(105,95,48,95,122,111,110,101,105,110,102,111,0),843], [String.fromCharCode(101,120,112,111,114,116,101,100,95,112,95,53,55,0),934]]);
          let graph5 = true;
          let bufferj: Array<any> = [938, 462];
         showb += `${3 / (Math.max(10, episode3.length))}`;
         userj.set(`${graph5}`, userj.size >> (Math.min(Math.abs(2), 5)));
         bufferj.push(bufferj.length + 3);
         linkM += `${1 * showb.length}`;
      giftbuttonS += `${giftbuttonS.length}`;
   }
       let specH = String.fromCharCode(106,95,57,52,95,115,99,105,105,0);
       let trophyf = 5.0;
       let nalyticsg = true;
       let libffmpegkitV = false;
       let libglog2 = true;
         libglog2 = (!libffmpegkitV ? !libglog2 : !libffmpegkitV);
         trophyf += specH.length;
      for (let c = 0; c < 3; c++) {
         specH = `${parseInt(`${trophyf}`) - 1}`;
      }
       let popupl = String.fromCharCode(111,95,49,48,95,105,110,105,118,105,116,101,100,0);
       let storeB = String.fromCharCode(122,95,57,95,110,105,100,111,98,106,0);
      mbbidN *= ballB.length;
      whistleorangeP = `${ballB.length >> (Math.min(Math.abs(2), 5))}`;
      holderh += `${parseInt(`${libapminsightaS}`) & 1}`;
   let successv = mbbidN >= 7003134;
   do {
       let spect = 5.0;
       let incidentH = false;
       let uimanagerA = 2;
       let signinup1 = true;
       let matchS = 0.0;
         matchS += ((incidentH ? 1 : 4) << (Math.min(Math.abs(parseInt(`${spect}`)), 4)));
          let default_sk = String.fromCharCode(112,95,57,52,95,99,114,111,115,115,102,97,100,105,110,103,0);
          let nativemodulep = String.fromCharCode(122,95,51,48,95,115,116,99,98,0);
          let g_viewG = String.fromCharCode(97,100,109,105,110,115,95,117,95,57,50,0);
         signinup1 = !signinup1;
         default_sk = `${nativemodulep.length / 3}`;
         nativemodulep += `${(String.fromCharCode(76,0) == nativemodulep ? nativemodulep.length : default_sk.length)}`;
         g_viewG = `${default_sk.length | 2}`;
          let blacklists = String.fromCharCode(97,95,51,51,95,102,97,105,108,117,114,101,0);
          let configo = true;
         signinup1 = configo || !signinup1;
         blacklists += `${blacklists.length % (Math.max(blacklists.length, 9))}`;
         configo = String.fromCharCode(87,0) == blacklists;
      let smallbrightnessM = signinup1 ? !signinup1 : signinup1;
      do {
          let regengb = 1.0;
          let iconpointscorem = String.fromCharCode(120,95,50,55,95,99,97,112,116,117,114,101,100,0);
          let overlayN = String.fromCharCode(111,95,54,53,95,104,111,108,100,115,0);
          let iconnotificationnewi = 4.0;
          let actionsm: Array<any> = [118, 828];
         signinup1 = 58.68 == iconnotificationnewi;
         regengb += parseFloat(`${parseInt(`${regengb}`) ^ 2}`);
         iconpointscorem += "1";
         overlayN = `${actionsm.length}`;
         iconnotificationnewi += iconpointscorem.length;
         actionsm.push(1);
         if (smallbrightnessM) {
            break;
         }
      } while (smallbrightnessM && (!signinup1));
      while (!incidentH && 5.71 == (spect * 2.23)) {
         incidentH = matchS < 72.88 && signinup1;
         break;
      }
         spect += (parseFloat(`${(signinup1 ? 2 : 4)}`));
       let basketballawayteamH = false;
       let libflipperA = false;
      if ((5 & uimanagerA) >= 2 && uimanagerA >= 5) {
          let rncoreM = String.fromCharCode(120,95,51,49,95,101,110,99,111,100,101,0);
          let mbjscommonP = 3.0;
          let mbridgei = true;
          let login4 = String.fromCharCode(100,95,55,95,115,101,108,101,99,116,111,114,0);
          let penaltyshoot9 = String.fromCharCode(115,116,98,108,95,109,95,53,51,0);
         signinup1 = !signinup1;
         rncoreM = `${rncoreM.length}`;
         mbjscommonP += parseInt(`${mbjscommonP}`) ^ penaltyshoot9.length;
         mbridgei = !mbridgei && login4.length > 49;
         login4 += `${((mbridgei ? 2 : 4))}`;
         penaltyshoot9 = `${login4.length - 3}`;
      }
       let fileE = String.fromCharCode(121,95,49,52,95,99,111,110,116,101,110,116,0);
          let completey = 4.0;
          let targetF: Array<any> = [71, 989];
          let yellowcirclebgm = String.fromCharCode(114,95,53,48,95,115,99,97,110,110,105,110,103,0);
         libflipperA = basketballawayteamH;
         completey -= parseFloat(`${1 | parseInt(`${completey}`)}`);
         targetF = [parseInt(`${completey}`)];
         yellowcirclebgm = `${parseInt(`${completey}`)}`;
      let encrypt5 = signinup1 ? !signinup1 : signinup1;
      do {
          let adultC = true;
          let q_lockE = 5;
          let rules7 = String.fromCharCode(102,105,108,116,101,114,98,97,110,107,95,118,95,49,55,0);
          let unimplementedviewu = 3.0;
         signinup1 = rules7.startsWith(`${adultC}`);
         adultC = 38 <= q_lockE;
         q_lockE ^= parseInt(`${unimplementedviewu}`);
         rules7 += `${q_lockE}`;
         unimplementedviewu -= parseFloat(`${parseInt(`${unimplementedviewu}`)}`);
         if (encrypt5) {
            break;
         }
      } while (encrypt5 && (1.74 == (matchS * 1.71)));
         basketballawayteamH = (83 >= (fileE.length | (!incidentH ? fileE.length : 83)));
       let checkbox7 = String.fromCharCode(115,116,121,112,95,119,95,57,49,0);
       let otherM = String.fromCharCode(122,95,49,50,95,114,101,109,111,118,101,0);
       let helperE = 0;
       let notificationf = 3;
      for (let g = 0; g < 1; g++) {
         libflipperA = notificationf < parseInt(`${matchS}`);
      }
      mbbidN -= 3;
      if (successv) {
         break;
      }
   } while (((mbbidN | predictiondefaultJ) == 2) && successv);
   if (predictiondefaultJ == mbbidN) {
      mbbidN <<= Math.min(Math.abs(holderh.length << (Math.min(giftbuttonS.length, 4))), 4);
   }
      ballB = `${whistleorangeP.length * parseInt(`${libapminsightaS}`)}`;
   for (let l = 0; l < 3; l++) {
      mbbidN &= 3;
   }
      ballB += "1";
   for (let q = 0; q < 2; q++) {
       let reddownarrowb = String.fromCharCode(112,97,114,116,105,116,105,111,110,101,100,95,55,95,57,48,0);
       let sendc = String.fromCharCode(99,97,112,116,117,114,101,114,95,118,95,49,48,48,0);
       let catagorym = String.fromCharCode(111,95,55,54,95,108,111,99,97,108,97,100,100,114,0);
       let coreC = String.fromCharCode(99,97,108,99,117,97,108,116,101,95,109,95,53,49,0);
      while (catagorym.endsWith(reddownarrowb)) {
         reddownarrowb += `${catagorym.length >> (Math.min(Math.abs(3), 4))}`;
         break;
      }
         catagorym += `${1 | sendc.length}`;
      while (!sendc.includes(catagorym)) {
         sendc += `${(String.fromCharCode(75,0) == catagorym ? catagorym.length : coreC.length)}`;
         break;
      }
      if (reddownarrowb == sendc) {
          let arrowE = 1.0;
          let inactivew = 1.0;
          let textlayoutmanagerZ: Map<any, any> = new Map([[String.fromCharCode(115,104,111,119,95,114,95,51,56,0),772], [String.fromCharCode(116,111,110,95,109,95,56,49,0),57], [String.fromCharCode(113,95,53,54,95,105,109,109,117,116,97,98,108,101,0),757]]);
          let unreadQ = 0;
         sendc += "1";
         arrowE /= Math.max(parseFloat(`${parseInt(`${arrowE}`) * 1}`), 5);
         inactivew += parseFloat(`${parseInt(`${arrowE}`) * textlayoutmanagerZ.size}`);
         textlayoutmanagerZ = new Map([[`${textlayoutmanagerZ.size}`, parseInt(`${arrowE}`) | 3]]);
         unreadQ += 3 / (Math.max(4, parseInt(`${inactivew}`)));
      }
         reddownarrowb += "1";
      for (let o = 0; o < 3; o++) {
          let basketballawayteamf: Map<any, any> = new Map([[String.fromCharCode(120,95,54,56,95,104,97,110,100,101,114,0),857], [String.fromCharCode(116,105,99,107,101,114,115,95,108,95,49,49,0),839], [String.fromCharCode(99,95,55,54,95,114,101,97,100,101,114,0),363]]);
          let scorepopsoundF = String.fromCharCode(111,95,51,55,0);
          let hoverG = 1;
          let detailsY = false;
          let referrer7 = true;
         sendc = `${hoverG * scorepopsoundF.length}`;
         basketballawayteamf.set(`${referrer7}`, basketballawayteamf.size);
         scorepopsoundF = `${(1 + (detailsY ? 3 : 2))}`;
         hoverG >>= Math.min(2, parseInt(`${Math.abs((3 >> (Math.min(4, Math.abs((referrer7 ? 1 : 2))))))}`));
         detailsY = (detailsY ? referrer7 : !detailsY);
      }
          let libfbh = String.fromCharCode(105,110,118,105,116,101,114,95,100,95,52,57,0);
          let lessK = String.fromCharCode(115,111,114,116,101,114,95,48,95,51,51,0);
         reddownarrowb += `${sendc.length % 3}`;
         libfbh += `${lessK.length | 1}`;
         lessK = `${(libfbh == String.fromCharCode(104,0) ? lessK.length : libfbh.length)}`;
      for (let h = 0; h < 2; h++) {
         catagorym = `${catagorym.length}`;
      }
          let footballtrophyP = false;
         sendc = `${catagorym.length}`;
      let orientationU = coreC.length <= 8897727;
      do {
         coreC += `${1 / (Math.max(9, sendc.length))}`;
         if (orientationU) {
            break;
         }
      } while ((reddownarrowb == coreC) && orientationU);
      while (coreC == String.fromCharCode(78,0)) {
          let controll = false;
          let backwardi = String.fromCharCode(104,95,52,50,95,116,101,114,109,105,110,97,116,101,0);
          let iconclosewhitebg5 = 2.0;
         sendc = `${((controll ? 4 : 3) % 3)}`;
         controll = 60 >= backwardi.length;
         backwardi += `${backwardi.length}`;
         iconclosewhitebg5 /= Math.max(parseInt(`${iconclosewhitebg5}`), 1);
         break;
      }
         catagorym += `${sendc.length % (Math.max(5, coreC.length))}`;
      ballB += `${1 << (Math.min(1, Math.abs(parseInt(`${controlsa}`))))}`;
   }
      giftbuttonS += `${description__nw + 1}`;
      mbbidN |= gpayU.length * 1;
       let langv = 4;
       let sharewhitei = String.fromCharCode(106,95,54,95,99,111,111,114,100,105,110,97,116,111,114,0);
       let libruntimeexecutor8 = String.fromCharCode(102,116,118,110,111,100,101,95,102,95,56,50,0);
      if (sharewhitei.length >= 3 || libruntimeexecutor8 == String.fromCharCode(110,0)) {
         sharewhitei += `${sharewhitei.length ^ 3}`;
      }
         libruntimeexecutor8 = `${3 << (Math.min(3, libruntimeexecutor8.length))}`;
          let episodes7 = String.fromCharCode(102,95,54,52,95,112,114,101,116,119,105,100,100,108,101,0);
         sharewhitei += `${(episodes7 == String.fromCharCode(120,0) ? episodes7.length : langv)}`;
          let cast7 = String.fromCharCode(105,110,115,101,114,116,115,95,113,95,52,56,0);
          let libfabricjnio: Map<any, any> = new Map([[String.fromCharCode(108,95,57,50,95,109,97,116,99,104,105,110,103,0),String.fromCharCode(99,104,97,114,97,99,116,101,114,115,95,117,95,56,52,0)], [String.fromCharCode(102,95,49,56,95,104,102,108,105,112,0),String.fromCharCode(97,95,49,48,95,109,111,118,101,0)], [String.fromCharCode(110,95,48,95,112,114,101,0),String.fromCharCode(117,95,49,49,95,98,117,105,108,116,105,110,0)]]);
         langv /= Math.max(2, 2 | sharewhitei.length);
         cast7 += "3";
         libfabricjnio = new Map([[`${libfabricjnio.size}`, 2]]);
      for (let m = 0; m < 1; m++) {
         sharewhitei = `${(String.fromCharCode(56,0) == sharewhitei ? sharewhitei.length : libruntimeexecutor8.length)}`;
      }
         sharewhitei += `${(libruntimeexecutor8 == String.fromCharCode(50,0) ? libruntimeexecutor8.length : sharewhitei.length)}`;
      for (let j = 0; j < 1; j++) {
          let libapminsightag = 5.0;
          let questZ: Array<any> = [108, 722, 548];
          let homeplayerv = String.fromCharCode(118,95,53,56,95,115,97,103,97,0);
          let penaltyshootnogoal1 = 2.0;
         langv &= parseInt(`${penaltyshootnogoal1}`) << (Math.min(1, Math.abs(2)));
         libapminsightag += parseFloat(`${parseInt(`${libapminsightag}`) * 1}`);
         questZ.push(parseInt(`${libapminsightag}`) / 1);
         homeplayerv += `${parseInt(`${libapminsightag}`)}`;
         penaltyshootnogoal1 += parseFloat(`${parseInt(`${libapminsightag}`)}`);
      }
         sharewhitei += `${langv * 1}`;
         sharewhitei += `${sharewhitei.length}`;
      iconnotificationnewP = (giftbuttonS.length - controlsa) == 71.8;
   for (let i = 0; i < 2; i++) {
      smallorangeman0.push(smallorangeman0.length ^ 2);
   }
   for (let g = 0; g < 2; g++) {
      description__nw -= parseInt(`${controlsa}`) << (Math.min(1, Math.abs(mbbidN)));
   }
   if ((1 << (Math.min(3, holderh.length))) == 4) {
      mbbidN |= icontransferclubW.length;
   }
   if ((4 / (Math.max(4, holderh.length))) > 2) {
       let footballtrophyu = String.fromCharCode(116,101,120,101,108,95,120,95,52,51,0);
       let singley = 4.0;
       let bgvipxvod8 = 3.0;
      for (let o = 0; o < 1; o++) {
          let empty8 = 4.0;
          let phoneb: Map<any, any> = new Map([[String.fromCharCode(99,102,101,110,99,95,119,95,56,55,0),158], [String.fromCharCode(98,95,57,53,95,105,119,104,116,0),153], [String.fromCharCode(113,95,52,50,95,112,97,110,101,0),730]]);
         bgvipxvod8 *= footballtrophyu.length;
         empty8 /= Math.max(3, parseFloat(`${1}`));
         phoneb = new Map([[`${phoneb.size}`, phoneb.size]]);
      }
      while ((footballtrophyu.length & 1) >= 2 || 5.75 >= (singley / 3.61)) {
          let greytickl = String.fromCharCode(113,95,51,50,95,112,114,111,109,111,0);
          let signinupb = String.fromCharCode(112,101,114,115,105,115,116,95,54,95,49,57,0);
          let iconarrowrightorange3 = 3.0;
         singley += parseFloat(`${3}`);
         greytickl += `${parseInt(`${iconarrowrightorange3}`)}`;
         signinupb += `${signinupb.length}`;
         iconarrowrightorange3 += parseFloat(`${1 | signinupb.length}`);
         break;
      }
          let basketballiconE: Array<any> = [353, 745];
         singley += parseFloat(`${3}`);
         basketballiconE = [basketballiconE.length & 3];
      let stylesi = singley <= 5482094.0;
      do {
         singley /= Math.max(parseFloat(`${2 >> (Math.min(5, Math.abs(parseInt(`${singley}`))))}`), 5);
         if (stylesi) {
            break;
         }
      } while ((1.63 >= singley) && stylesi);
         bgvipxvod8 /= Math.max(5, parseInt(`${singley}`));
          let libmapbufferjniO = 1.0;
         bgvipxvod8 /= Math.max(1, 5);
         libmapbufferjniO /= Math.max(parseFloat(`${3}`), 4);
      while ((parseInt(`${singley}`) / (Math.max(1, 9))) >= 4 && (1 | footballtrophyu.length) >= 4) {
         singley /= Math.max(parseFloat(`${parseInt(`${bgvipxvod8}`)}`), 2);
         break;
      }
      if (3.53 <= (bgvipxvod8 + footballtrophyu.length) || (bgvipxvod8 + 3.53) <= 1.84) {
          let downloadk = 4;
          let submitM = String.fromCharCode(108,111,99,107,109,103,114,95,102,95,50,54,0);
         footballtrophyu = `${parseInt(`${singley}`)}`;
         downloadk %= Math.max(submitM.length, 2);
         submitM += `${downloadk}`;
      }
       let nativemoduleR = String.fromCharCode(115,101,114,118,101,114,95,99,95,52,49,0);
      description__nw %= Math.max(ballB.length % (Math.max(3, 10)), 3);
   }
      description__nw |= icontransferclubW.length;
   if (smallorangeman0.length >= controlsa) {
      smallorangeman0.push(whistleorangeP.length >> (Math.min(Math.abs(2), 3)));
   }
      libapminsightaS += 2 & parseInt(`${controlsa}`);
       let applicationE = String.fromCharCode(110,95,48,95,115,116,114,105,107,101,116,104,114,111,117,103,104,0);
         applicationE = `${applicationE.length}`;
      while (5 < applicationE.length) {
         applicationE = "2";
         break;
      }
      let stationt = applicationE.length <= 7732559;
      do {
          let penaltyE = String.fromCharCode(115,119,105,122,122,108,105,110,103,95,103,95,56,48,0);
          let spinnerO = 4.0;
          let largesoundX = 3.0;
         applicationE += `${parseInt(`${spinnerO}`) ^ parseInt(`${largesoundX}`)}`;
         penaltyE = `${penaltyE.length}`;
         spinnerO += penaltyE.length;
         if (stationt) {
            break;
         }
      } while ((applicationE.length < 3 || 3 < applicationE.length) && stationt);
      giftbuttonS += `${whistleorangeP.length << (Math.min(Math.abs(2), 1))}`;
   if ((giftbuttonS.length & 1) >= 4 || (1 & giftbuttonS.length) >= 5) {
       let iconmore3 = String.fromCharCode(100,95,50,50,95,109,111,118,101,116,111,0);
       let interstitialB = true;
       let dragi = String.fromCharCode(97,99,116,117,97,108,105,122,101,95,118,95,49,51,0);
       let libffmpegkitm: Map<any, any> = new Map([[String.fromCharCode(113,95,57,54,95,115,116,97,116,101,0),86], [String.fromCharCode(106,95,49,48,48,95,109,111,115,97,105,99,0),996]]);
       let libavcodec0 = false;
      while (!interstitialB || 4 > dragi.length) {
         interstitialB = !interstitialB;
         break;
      }
      let refreshj = 5993656 <= libffmpegkitm.size;
      do {
          let iconnotificationnewA = 5.0;
          let predictionwiny = String.fromCharCode(116,105,109,105,110,103,115,97,102,101,95,122,95,53,57,0);
          let libsentrys = String.fromCharCode(101,95,56,53,95,112,111,115,105,116,105,111,110,105,110,103,0);
         libffmpegkitm = new Map([[`${libavcodec0}`, dragi.length]]);
         iconnotificationnewA /= Math.max(parseInt(`${iconnotificationnewA}`), 5);
         predictionwiny = `${2 & parseInt(`${iconnotificationnewA}`)}`;
         libsentrys += `${libsentrys.length | predictionwiny.length}`;
         if (refreshj) {
            break;
         }
      } while ((libffmpegkitm.size <= iconmore3.length) && refreshj);
       let update_asU: Array<any> = [String.fromCharCode(114,101,112,115,116,114,95,49,95,54,53,0), String.fromCharCode(111,114,105,101,110,116,97,116,105,111,110,95,108,95,50,55,0), String.fromCharCode(115,117,109,109,101,100,95,109,95,49,54,0)];
      let emojiu = libavcodec0 ? !libavcodec0 : libavcodec0;
      do {
         libavcodec0 = (22 == (dragi.length - (!interstitialB ? 22 : dragi.length)));
         if (emojiu) {
            break;
         }
      } while ((!libavcodec0 && (update_asU.length + 1) >= 4) && emojiu);
         iconmore3 = `${((interstitialB ? 2 : 3))}`;
      while (libffmpegkitm.size <= 1) {
         libffmpegkitm = new Map([[`${libffmpegkitm.size}`, update_asU.length]]);
         break;
      }
         libavcodec0 = !interstitialB && iconmore3.length >= 85;
      let resendv = 8837815 <= iconmore3.length;
      do {
         iconmore3 = `${(2 | (libavcodec0 ? 4 : 5))}`;
         if (resendv) {
            break;
         }
      } while (((iconmore3.length + 3) <= 1 || 2 <= (update_asU.length + 3)) && resendv);
      let iconarrowlefts = libffmpegkitm.size >= 7526740;
      do {
          let update_hw = 3.0;
          let handlerY = false;
          let encryptor8 = String.fromCharCode(105,95,53,49,95,97,115,115,101,109,98,108,121,0);
          let layout7 = 3.0;
          let animationN = String.fromCharCode(122,95,55,54,95,105,110,118,105,115,105,98,108,101,0);
         libffmpegkitm = new Map([[`${update_asU.length}`, update_asU.length]]);
         update_hw /= Math.max(5, parseInt(`${layout7}`));
         handlerY = !handlerY;
         encryptor8 = `${((handlerY ? 3 : 5) | parseInt(`${update_hw}`))}`;
         layout7 /= Math.max(encryptor8.length % 3, 5);
         animationN = `${1 & parseInt(`${update_hw}`)}`;
         if (iconarrowlefts) {
            break;
         }
      } while (iconarrowlefts && (2 < (5 << (Math.min(4, Math.abs(libffmpegkitm.size))))));
          let launcha = 3.0;
          let helperj: Map<any, any> = new Map([[String.fromCharCode(119,111,114,100,95,98,95,50,55,0),12], [String.fromCharCode(120,95,49,52,95,116,105,109,101,115,116,97,109,112,115,0),705], [String.fromCharCode(115,121,109,98,111,108,105,99,95,97,95,50,53,0),108]]);
         interstitialB = dragi.length < 20;
         launcha -= 2;
         helperj = new Map([[`${helperj.size}`, parseInt(`${launcha}`) >> (Math.min(Math.abs(helperj.size), 1))]]);
      for (let n = 0; n < 3; n++) {
          let librrcW = true;
          let mbnativeadvancedZ = 4.0;
          let untickM = String.fromCharCode(106,95,51,50,95,112,117,108,108,0);
          let activee: Array<any> = [350, 665, 499];
          let bannerZ = String.fromCharCode(117,95,53,95,109,105,103,114,97,116,105,110,103,0);
         update_asU.push(iconmore3.length * libffmpegkitm.size);
         librrcW = bannerZ.length == 16;
         mbnativeadvancedZ -= 2 % (Math.max(9, bannerZ.length));
         untickM = `${1 % (Math.max(parseInt(`${mbnativeadvancedZ}`), 10))}`;
         activee.push(activee.length - bannerZ.length);
      }
      for (let h = 0; h < 3; h++) {
         libavcodec0 = libffmpegkitm.get(`${libavcodec0}`) == null;
      }
          let layouth = false;
          let libavdevicek: Array<any> = [633, 382];
          let stringI = String.fromCharCode(114,95,54,56,95,116,104,114,101,97,100,112,111,111,108,0);
         interstitialB = iconmore3.includes(`${interstitialB}`);
         layouth = !stringI.includes(`${layouth}`);
         libavdevicek = [(String.fromCharCode(121,0) == stringI ? libavdevicek.length : stringI.length)];
         libffmpegkitm.set(iconmore3, iconmore3.length & 1);
         dragi += `${libffmpegkitm.size}`;
      giftbuttonS += "2";
   }
   if ((description__nw | 5) == 1) {
      description__nw += icontransferclubW.length * parseInt(`${libapminsightaS}`);
   }
},
}: mayi_GoogleViews) {
  const {colors, icons, textVariants} = useTheme();
  return (
    <TouchableOpacity activeOpacity={0.85} style={styles.container} onPress={onPress}>
      <SearchBar
        platform="default"
        containerStyle={styles.containerStyle}
        inputContainerStyle={{
          backgroundColor: colors.search,
          ...styles.inputContainerStyle,
        }}
        inputStyle={{
          ...textVariants.subBody,
          position: 'relative',
          right: 10,
          top: 1,
        }}
        placeholder="输入搜索关键词"
        placeholderTextColor={colors.text}
        round
        searchIcon={<SearchIcon color={colors.text} height={23} width={23} />}
        value={defaultValue}
        
        showLoading={false}
        clearIcon={<></>}
      />
    </TouchableOpacity>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignSelf: 'center',
    marginLeft: 8,
    marginRight: 8,
  },
  containerStyle: {
    padding: 0,
    marginLeft: 0,
    marginRight: 0,
    textAlign: 'left',
    borderRadius: 20,
    borderTopWidth: 0,
    borderBottomWidth: 0,
    
    
    
    backgroundColor: 'transparent',
  },
  inputContainerStyle: {
    borderRadius: 20,
    pointerEvents: 'none',
    height: 32,
    opacity: 0.6,
  },
});
