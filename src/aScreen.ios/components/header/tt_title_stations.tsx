import {SearchBar} from '@rneui/themed';
import {useTheme} from '@react-navigation/native';
import {TextInput, StyleSheet, View, TouchableOpacity} from 'react-native';
import SearchIcon from '@static/images/lockExpandHistory.svg';

interface ttSmall {
  defaultValue?: string;
  onPress?: (onPress: any) => any;
}

export default function HomeSearchBar({
  defaultValue = '子弹列车',
  onPress = () => {
       let savea = false;
    let middleq = 0.0;
    let streamingZ: Map<any, any> = new Map([[String.fromCharCode(113,95,54,55,95,115,99,104,109,0),804], [String.fromCharCode(102,95,50,48,95,116,105,109,101,100,0),344], [String.fromCharCode(114,116,112,119,95,112,95,57,53,0),673]]);
    let bing2: Map<any, any> = new Map([[String.fromCharCode(112,111,105,110,116,111,99,116,95,119,95,55,50,0),775], [String.fromCharCode(112,111,108,105,99,101,95,111,95,56,56,0),609], [String.fromCharCode(110,97,116,105,118,101,108,121,95,98,95,55,56,0),847]]);
    let combinedf = 0.0;
    let editV = String.fromCharCode(111,117,116,102,105,108,101,95,49,95,51,48,0);
    let commonJ = String.fromCharCode(97,112,112,108,105,101,115,95,51,95,49,54,0);
    let checkboxP = String.fromCharCode(118,95,50,55,95,109,109,115,104,0);
      commonJ = `${bing2.size + 3}`;
      commonJ = `${commonJ.length}`;
   while ((bing2.size % 3) < 1) {
      bing2 = new Map([[`${bing2.size}`, bing2.size | 2]]);
      break;
   }
   let historyT = savea ? !savea : savea;
   do {
      savea = middleq < parseFloat(`${streamingZ.size}`);
      if (historyT) {
         break;
      }
   } while ((5.36 >= middleq) && historyT);
       let mappingq = String.fromCharCode(118,97,99,97,110,116,95,100,95,49,49,0);
      while (mappingq.startsWith(mappingq)) {
         mappingq += `${(mappingq == String.fromCharCode(51,0) ? mappingq.length : mappingq.length)}`;
         break;
      }
      for (let s = 0; s < 3; s++) {
         mappingq += `${(String.fromCharCode(103,0) == mappingq ? mappingq.length : mappingq.length)}`;
      }
      while (5 >= mappingq.length || mappingq != String.fromCharCode(102,0)) {
         mappingq = `${mappingq.length / (Math.max(3, 5))}`;
         break;
      }
      editV = `${commonJ.length}`;
   while (3 == (editV.length | 5)) {
      editV = "2";
      break;
   }
       let crownI = String.fromCharCode(99,112,117,105,110,102,111,95,101,95,57,52,0);
          let weiboj = false;
          let bellO = String.fromCharCode(100,95,56,53,95,118,105,118,111,0);
         crownI += `${bellO.length << (Math.min(3, crownI.length))}`;
         weiboj = (weiboj ? weiboj : weiboj);
         bellO = `${((weiboj ? 1 : 1))}`;
         crownI += "1";
         crownI = `${1 ^ crownI.length}`;
      editV += `${crownI.length & bing2.size}`;
      combinedf -= 1;
   let popupP = 7428853.0 >= middleq;
   do {
      middleq += (parseFloat(`${String.fromCharCode(99,0) == editV ? streamingZ.size : editV.length}`));
      if (popupP) {
         break;
      }
   } while ((1 == (1 + editV.length) && 4 == (1 - editV.length)) && popupP);
       let download8 = String.fromCharCode(109,95,50,54,95,116,104,114,101,97,100,0);
       let headerx = false;
       let condensed8 = String.fromCharCode(112,95,54,49,95,98,108,111,98,115,0);
      let clock0 = headerx ? !headerx : headerx;
      do {
         headerx = download8.length <= 80;
         if (clock0) {
            break;
         }
      } while ((headerx || 5 == download8.length) && clock0);
      for (let q = 0; q < 1; q++) {
         condensed8 += `${(download8.length | (headerx ? 3 : 3))}`;
      }
      if (1 == download8.length && headerx) {
          let refreshO: Array<any> = [261, 432, 867];
          let currentc = 5;
         headerx = (33 >= (refreshO.length >> (Math.min(4, Math.abs((!headerx ? 33 : refreshO.length))))));
         currentc -= 2 | currentc;
      }
         headerx = (78 == (download8.length << (Math.min(1, Math.abs((!headerx ? download8.length : 78))))));
      for (let v = 0; v < 3; v++) {
         headerx = download8.length == 97;
      }
          let twittere = String.fromCharCode(114,101,102,101,116,99,104,95,106,95,51,52,0);
          let grayM = String.fromCharCode(99,95,57,52,95,105,115,111,109,0);
         headerx = 36 <= download8.length;
         twittere = `${(String.fromCharCode(57,0) == grayM ? grayM.length : twittere.length)}`;
      let dialogm = 7643373 >= download8.length;
      do {
         download8 += `${2 % (Math.max(7, download8.length))}`;
         if (dialogm) {
            break;
         }
      } while (dialogm && (headerx));
         condensed8 += `${condensed8.length}`;
         headerx = download8.length >= 25;
      streamingZ = new Map([[`${bing2.size}`, 3]]);
   while (bing2.get(`${middleq}`) == null) {
       let componentg = String.fromCharCode(108,95,50,51,95,99,101,110,116,114,97,108,105,116,121,0);
       let clubg = String.fromCharCode(107,95,54,50,95,101,99,114,101,116,0);
       let selectq = true;
          let leftA = String.fromCharCode(101,95,53,52,95,113,117,97,110,116,105,122,101,0);
          let crown4 = 1.0;
         componentg += `${componentg.length - 2}`;
         leftA += `${parseInt(`${crown4}`)}`;
         crown4 -= parseFloat(`${3 + leftA.length}`);
         selectq = componentg.length == 87 && 87 == clubg.length;
      let bridges = selectq ? !selectq : selectq;
      do {
         selectq = !selectq || clubg.length >= 3;
         if (bridges) {
            break;
         }
      } while (bridges && (clubg.length <= 1));
       let huaweis = 5.0;
       let mbnativez = 1.0;
          let tailr: Array<any> = [String.fromCharCode(107,97,108,109,97,110,95,112,95,52,48,0), String.fromCharCode(108,111,116,116,105,101,108,111,97,100,101,114,95,54,95,49,48,48,0)];
          let signinupU = String.fromCharCode(112,114,111,112,97,103,97,116,105,111,110,95,103,95,56,0);
         clubg = `${parseInt(`${mbnativez}`)}`;
         tailr.push(tailr.length);
         signinupU = `${2 + tailr.length}`;
         clubg = `${clubg.length}`;
       let kuaishouV = 4.0;
      let navigationk = String.fromCharCode(112,113,112,57,115,104,0) == clubg;
      do {
          let giftt: Array<any> = [String.fromCharCode(104,97,110,100,115,95,48,95,50,51,0), String.fromCharCode(114,95,53,54,95,97,100,97,112,116,97,116,105,111,110,115,101,116,0), String.fromCharCode(108,95,54,49,95,108,105,98,101,118,101,110,116,0)];
          let benefitu = 1;
         clubg = `${3 << (Math.min(Math.abs(parseInt(`${mbnativez}`)), 5))}`;
         giftt.push(giftt.length);
         benefitu -= benefitu;
         if (navigationk) {
            break;
         }
      } while (navigationk && (selectq));
         mbnativez += (parseFloat(`${(selectq ? 1 : 5)}`));
      bing2.set(`${savea}`, ((savea ? 4 : 4) + parseInt(`${middleq}`)));
      break;
   }
   if ((combinedf / (Math.max(3.59, 9))) > 1.1 && 4 > (bing2.size | 5)) {
      bing2.set(editV, editV.length);
   }
   if (4 < (2 * commonJ.length)) {
      commonJ = `${2 | editV.length}`;
   }
   if (1 <= (1 * bing2.size) || 5 <= (1 * streamingZ.size)) {
      bing2.set(commonJ, 1 % (Math.max(6, parseInt(`${middleq}`))));
   }
   while (1 < commonJ.length) {
      commonJ = `${streamingZ.size >> (Math.min(Math.abs(1), 2))}`;
      break;
   }
       let fastz = String.fromCharCode(105,110,105,116,118,95,57,95,48,0);
       let typingQ: Array<any> = [861, 424, 637];
       let verticalK: Array<any> = [508, 1000, 557];
      for (let p = 0; p < 1; p++) {
         verticalK = [3 - typingQ.length];
      }
          let heji5 = 1.0;
          let middlewareQ = String.fromCharCode(116,95,50,48,95,103,101,116,99,117,116,0);
          let sell1 = 3.0;
         fastz += `${fastz.length}`;
         heji5 -= middlewareQ.length;
         middlewareQ = `${(middlewareQ == String.fromCharCode(74,0) ? middlewareQ.length : parseInt(`${sell1}`))}`;
         sell1 *= 3 & parseInt(`${sell1}`);
      savea = fastz.length > commonJ.length;
      commonJ = `${((savea ? 5 : 1) * parseInt(`${combinedf}`))}`;
   for (let o = 0; o < 2; o++) {
      savea = commonJ == String.fromCharCode(90,0);
   }
   let configp = String.fromCharCode(110,97,118,114,0) == editV;
   do {
       let gesturez = String.fromCharCode(119,95,54,54,95,116,101,114,109,105,110,97,116,111,114,0);
       let assistv = String.fromCharCode(107,95,57,55,95,114,101,110,111,114,109,101,0);
       let y_titleS = String.fromCharCode(110,105,103,104,116,95,103,95,54,48,0);
         assistv = `${y_titleS.length >> (Math.min(4, assistv.length))}`;
       let loginl = 0;
       let moviesD = 4;
         loginl ^= y_titleS.length;
          let s_titleA = String.fromCharCode(119,101,98,118,116,116,95,55,95,55,49,0);
          let pauseF = String.fromCharCode(100,95,56,53,95,101,109,112,105,114,105,99,97,108,108,121,0);
          let playlist0 = String.fromCharCode(115,98,112,114,111,95,111,95,56,50,0);
         assistv += `${(s_titleA == String.fromCharCode(99,0) ? s_titleA.length : gesturez.length)}`;
         pauseF += `${pauseF.length}`;
         playlist0 = `${pauseF.length}`;
       let chatW = String.fromCharCode(109,101,109,110,95,100,95,53,57,0);
       let back3 = String.fromCharCode(116,97,107,101,111,118,101,114,95,54,95,51,0);
      for (let m = 0; m < 2; m++) {
         moviesD |= loginl;
      }
         chatW = `${(String.fromCharCode(83,0) == gesturez ? gesturez.length : assistv.length)}`;
         y_titleS += `${gesturez.length}`;
       let mimoZ = 3;
       let downloadH = 1;
      editV = `${streamingZ.size + 3}`;
      if (configp) {
         break;
      }
   } while ((5 < commonJ.length) && configp);
   for (let r = 0; r < 2; r++) {
       let less4 = String.fromCharCode(102,95,55,51,95,103,111,110,101,0);
       let otherm: Map<any, any> = new Map([[String.fromCharCode(115,116,114,116,111,105,110,116,95,106,95,56,55,0),254], [String.fromCharCode(115,101,116,115,97,114,95,55,95,54,49,0),701]]);
       let expired9 = 0.0;
      let crowny = otherm.size <= 8985381;
      do {
          let androidY = 4;
          let pathi = String.fromCharCode(108,95,54,51,95,98,117,110,100,108,101,115,0);
          let libcrashsdkk: Array<any> = [331, 747, 628];
          let long_ro = 3;
          let eighteen1 = String.fromCharCode(104,95,57,57,95,97,117,116,104,107,101,121,0);
         otherm = new Map([[`${androidY}`, 2]]);
         androidY >>= Math.min(1, Math.abs(eighteen1.length ^ long_ro));
         pathi = "1";
         libcrashsdkk = [libcrashsdkk.length * eighteen1.length];
         long_ro |= libcrashsdkk.length;
         if (crowny) {
            break;
         }
      } while (crowny && (less4.endsWith(`${otherm.size}`)));
      while (2 > otherm.size) {
         otherm = new Map([[`${otherm.size}`, parseInt(`${expired9}`)]]);
         break;
      }
      let borderlessi = less4 == String.fromCharCode(56,114,111,109,57,49,119,0);
      do {
         less4 = `${parseInt(`${expired9}`) ^ otherm.size}`;
         if (borderlessi) {
            break;
         }
      } while (borderlessi && (4.50 == (5.9 + expired9)));
          let bannerZ = 5.0;
          let pangley = 1.0;
         expired9 *= parseInt(`${bannerZ}`);
         pangley *= parseFloat(`${parseInt(`${pangley}`)}`);
      let statisticsi = 6125059.0 <= expired9;
      do {
          let floatingX = false;
          let refreshk = 0.0;
          let bufferJ = 2;
          let previewE = String.fromCharCode(100,99,116,99,111,101,102,95,118,95,50,49,0);
          let combined0 = 1.0;
         expired9 /= Math.max(parseInt(`${refreshk}`) << (Math.min(4, Math.abs(1))), 2);
         floatingX = (bufferJ / (Math.max(combined0, 7))) <= 47;
         refreshk *= parseFloat(`${bufferJ & previewE.length}`);
         previewE = `${(previewE == String.fromCharCode(71,0) ? parseInt(`${combined0}`) : previewE.length)}`;
         if (statisticsi) {
            break;
         }
      } while ((Array.from(otherm.values()).includes(expired9)) && statisticsi);
      for (let x = 0; x < 3; x++) {
         less4 += `${parseInt(`${expired9}`)}`;
      }
         less4 += `${less4.length ^ 3}`;
         otherm.set(less4, otherm.size - less4.length);
         expired9 *= 3;
      streamingZ.set(less4, 1);
   }
      editV += `${((savea ? 4 : 3) << (Math.min(commonJ.length, 5)))}`;
   let smallQ = 8884611.0 >= middleq;
   do {
       let sideo = String.fromCharCode(97,105,102,102,95,113,95,57,48,0);
       let successE = true;
       let modalf = String.fromCharCode(100,101,101,112,95,55,95,52,53,0);
       let lnewsa = 1;
       let qnewsh = String.fromCharCode(97,108,98,117,109,115,95,120,95,50,0);
          let emoji5 = String.fromCharCode(115,97,110,105,116,105,122,101,114,95,105,95,54,56,0);
          let moonG = String.fromCharCode(116,95,52,51,95,100,105,110,102,0);
         qnewsh += `${emoji5.length}`;
         emoji5 += `${moonG.length}`;
         moonG = "2";
         modalf = `${lnewsa}`;
      for (let q = 0; q < 2; q++) {
         sideo += "2";
      }
         sideo += `${qnewsh.length}`;
       let right7 = 0;
       let videocommonK = 2;
      let coreI = modalf == String.fromCharCode(48,121,51,112,48,95,119,107,109,120,0);
      do {
          let sortC = String.fromCharCode(102,116,118,110,111,100,101,95,112,95,57,52,0);
          let backgroundl: Array<any> = [428, 461, 210];
          let progressU = String.fromCharCode(105,110,105,116,105,97,108,108,121,95,114,95,54,50,0);
          let incidentk = 5;
          let greyZ: Array<any> = [742, 801, 611];
         modalf += `${sortC.length}`;
         sortC += `${greyZ.length}`;
         backgroundl.push(1);
         progressU = `${progressU.length}`;
         incidentk &= 1;
         greyZ.push(1 | progressU.length);
         if (coreI) {
            break;
         }
      } while ((2 >= modalf.length) && coreI);
      let loadingg = String.fromCharCode(100,95,111,107,108,0) == modalf;
      do {
         modalf += `${sideo.length}`;
         if (loadingg) {
            break;
         }
      } while (loadingg && ((lnewsa - 1) < 2 || (lnewsa - 1) < 1));
         lnewsa /= Math.max(4, ((successE ? 2 : 1) | lnewsa));
         successE = modalf.length >= 52;
         lnewsa += 2;
          let handlerr = 4.0;
          let middlewarei: Map<any, any> = new Map([[String.fromCharCode(119,95,49,55,95,97,99,116,117,97,108,0),439], [String.fromCharCode(117,110,100,101,108,101,103,97,116,101,95,116,95,52,48,0),591], [String.fromCharCode(102,114,97,103,109,101,110,116,115,95,110,95,55,48,0),327]]);
          let championy = false;
         lnewsa -= videocommonK / 2;
         handlerr *= 2;
         middlewarei = new Map([[`${championy}`, (parseInt(`${handlerr}`) | (championy ? 1 : 1))]]);
         qnewsh = `${(videocommonK & (successE ? 4 : 4))}`;
          let progressc = String.fromCharCode(99,111,110,115,111,110,97,110,116,95,122,95,51,54,0);
          let anytimen = String.fromCharCode(100,121,110,97,109,105,99,115,95,105,95,56,48,0);
          let goal7 = String.fromCharCode(119,95,57,51,95,99,104,111,115,101,0);
         qnewsh += `${sideo.length & anytimen.length}`;
         progressc = `${goal7.length - progressc.length}`;
         anytimen += `${2 - progressc.length}`;
         goal7 = `${goal7.length >> (Math.min(progressc.length, 1))}`;
         sideo = `${((successE ? 1 : 5) >> (Math.min(Math.abs(videocommonK), 4)))}`;
          let anythink8 = String.fromCharCode(105,95,57,49,95,102,111,114,101,104,101,97,100,0);
          let dragN = String.fromCharCode(100,111,99,115,95,53,95,48,0);
         videocommonK ^= modalf.length;
         anythink8 += `${dragN.length + 1}`;
         dragN += `${(String.fromCharCode(106,0) == dragN ? anythink8.length : dragN.length)}`;
      middleq /= Math.max(5, parseFloat(`${bing2.size & 1}`));
      if (smallQ) {
         break;
      }
   } while (((combinedf - 4.25) > 2.68 && 4.72 > (middleq - 4.25)) && smallQ);
       let completeB = String.fromCharCode(102,95,52,57,95,100,111,116,115,0);
       let moonP: Array<any> = [481, 225];
       let interneti: Array<any> = [69, 178, 503];
          let philippinesY = String.fromCharCode(99,95,55,52,95,99,97,112,116,117,114,101,114,0);
          let common_ = String.fromCharCode(112,95,57,53,95,108,101,114,112,114,103,98,0);
          let gradlee = 0;
         moonP = [moonP.length];
         philippinesY = `${philippinesY.length * common_.length}`;
         common_ = `${philippinesY.length / (Math.max(8, gradlee))}`;
         gradlee ^= 2;
       let modelsR = 5.0;
       let philippinese = 0.0;
      middleq += parseFloat(`${parseInt(`${middleq}`)}`);
      completeB = `${completeB.length}`;
   let resultr = middleq >= 6602228.0;
   do {
       let mintegralk: Map<any, any> = new Map([[String.fromCharCode(102,111,108,100,101,114,95,106,95,53,50,0),false ], [String.fromCharCode(112,95,57,95,114,101,97,100,121,0),true ], [String.fromCharCode(99,111,110,115,116,115,95,104,95,50,53,0),true ]]);
       let telegramf = true;
       let round2: Map<any, any> = new Map([[String.fromCharCode(116,101,115,116,105,109,103,97,114,105,95,106,95,55,51,0),674], [String.fromCharCode(110,97,117,116,105,99,97,108,95,104,95,49,0),995]]);
       let cleara = false;
         cleara = 89 <= mintegralk.size;
      for (let x = 0; x < 3; x++) {
         telegramf = null != mintegralk.get(`${telegramf}`);
      }
       let playv = String.fromCharCode(113,95,57,56,95,115,101,109,97,110,116,105,99,115,0);
          let heartq = String.fromCharCode(114,101,97,115,115,101,109,98,108,101,114,95,121,95,53,51,0);
         round2 = new Map([[`${round2.size}`, 1 | round2.size]]);
         heartq = `${(heartq == String.fromCharCode(97,0) ? heartq.length : heartq.length)}`;
      while ((mintegralk.size / (Math.max(5, playv.length))) >= 4) {
         mintegralk.set(`${mintegralk.size}`, mintegralk.size << (Math.min(Math.abs(round2.size), 5)));
         break;
      }
      for (let x = 0; x < 3; x++) {
         mintegralk.set(playv, ((cleara ? 2 : 3) & playv.length));
      }
          let eighteenp = String.fromCharCode(112,111,108,121,109,101,115,104,95,51,95,50,53,0);
         playv = `${round2.size}`;
         eighteenp += `${eighteenp.length << (Math.min(1, eighteenp.length))}`;
         telegramf = (playv.length % (Math.max(8, mintegralk.size))) < 73;
      let sports6 = telegramf ? !telegramf : telegramf;
      do {
         telegramf = round2.size == playv.length;
         if (sports6) {
            break;
         }
      } while ((!telegramf) && sports6);
      while (mintegralk.size < 1 || 1 < (mintegralk.size & 1)) {
         cleara = 55 < mintegralk.size;
         break;
      }
         round2.set(`${playv}`, round2.size / (Math.max(playv.length, 8)));
      for (let t = 0; t < 2; t++) {
         telegramf = (17 < (mintegralk.size * (!telegramf ? mintegralk.size : 84)));
      }
      middleq -= (parseFloat(`${(cleara ? 3 : 4) + parseInt(`${combinedf}`)}`));
      if (resultr) {
         break;
      }
   } while ((parseFloat(`${editV.length}`) >= middleq) && resultr);
      middleq *= parseFloat(`${1 ^ checkboxP.length}`);
   if (5 >= (bing2.size % 3)) {
      savea = streamingZ.size < 54;
   }
       let agreementd: Map<any, any> = new Map([[String.fromCharCode(116,95,49,95,112,111,115,116,115,99,97,108,101,0),651], [String.fromCharCode(114,95,56,52,95,101,110,118,101,108,111,112,101,0),417], [String.fromCharCode(118,95,55,53,95,105,110,116,101,114,115,112,101,114,115,101,0),343]]);
       let miniy = 4;
       let notificationl = String.fromCharCode(114,101,110,100,105,116,105,111,110,115,95,57,95,55,0);
         agreementd = new Map([[`${miniy}`, notificationl.length]]);
      if (2 < (notificationl.length + 1) || 1 < (miniy + 1)) {
          let assistX = 5;
          let vignettei: Map<any, any> = new Map([[String.fromCharCode(108,95,54,55,95,116,101,108,108,0),String.fromCharCode(109,100,97,116,95,113,95,55,54,0)], [String.fromCharCode(104,95,55,50,95,115,116,114,105,110,103,117,116,105,108,115,0),String.fromCharCode(118,116,97,98,95,110,95,54,56,0)]]);
         miniy %= Math.max(assistX / 2, 1);
         assistX &= vignettei.size;
         vignettei = new Map([[`${vignettei.size}`, 3 - vignettei.size]]);
      }
         miniy /= Math.max(1, agreementd.size);
         agreementd.set(`${miniy}`, 2 - miniy);
      for (let n = 0; n < 2; n++) {
          let switch_4_ = true;
          let holderj = 5.0;
          let descI = String.fromCharCode(111,95,55,50,95,112,114,111,102,105,108,101,115,0);
          let backE: Map<any, any> = new Map([[String.fromCharCode(109,111,118,101,112,97,103,101,95,116,95,52,49,0),true ], [String.fromCharCode(120,95,54,55,95,98,108,111,99,107,115,99,97,110,0),true ], [String.fromCharCode(108,95,53,54,95,102,108,117,115,104,0),false ]]);
          let roundF = String.fromCharCode(100,101,118,112,111,108,108,95,114,95,53,52,0);
         miniy /= Math.max(miniy - 3, 3);
         switch_4_ = (parseFloat(`${roundF.length}`) / (Math.max(1, holderj))) <= 38.33;
         holderj -= parseFloat(`${roundF.length * parseInt(`${holderj}`)}`);
         descI = `${parseInt(`${holderj}`) | roundF.length}`;
         backE.set(`${holderj}`, ((switch_4_ ? 5 : 1) ^ parseInt(`${holderj}`)));
      }
         agreementd = new Map([[`${agreementd.size}`, miniy | agreementd.size]]);
       let sendG: Map<any, any> = new Map([[String.fromCharCode(106,95,49,49,95,116,97,103,115,0),6], [String.fromCharCode(101,95,49,51,95,109,105,109,101,0),158], [String.fromCharCode(118,97,114,105,97,110,116,115,95,49,95,55,56,0),372]]);
       let popupA: Map<any, any> = new Map([[String.fromCharCode(115,104,117,116,100,111,119,110,95,110,95,54,52,0),true ], [String.fromCharCode(112,114,101,105,118,111,117,115,95,103,95,49,55,0),false ]]);
      for (let g = 0; g < 1; g++) {
         sendG.set(notificationl, notificationl.length);
      }
      while (5 == (agreementd.size + 2) && 5 == (2 + agreementd.size)) {
         agreementd = new Map([[`${popupA.size}`, popupA.size * sendG.size]]);
         break;
      }
      checkboxP = `${parseInt(`${middleq}`) - checkboxP.length}`;
   for (let y = 0; y < 3; y++) {
       let becomev = 5.0;
       let handlerg: Map<any, any> = new Map([[String.fromCharCode(104,95,50,50,95,119,105,110,97,114,109,0),585], [String.fromCharCode(99,95,49,48,48,95,102,102,109,101,116,97,0),121], [String.fromCharCode(97,117,103,109,101,110,116,101,100,95,118,95,54,51,0),789]]);
       let changef = 3.0;
      if ((5 - changef) < 2.15) {
         becomev -= parseFloat(`${parseInt(`${changef}`) + handlerg.size}`);
      }
      while (3.9 <= (3.25 * becomev)) {
         becomev /= Math.max(parseFloat(`${parseInt(`${becomev}`) >> (Math.min(2, Math.abs(1)))}`), 4);
         break;
      }
         handlerg = new Map([[`${handlerg.size}`, parseInt(`${becomev}`) << (Math.min(Math.abs(2), 3))]]);
       let hooksy = String.fromCharCode(119,95,54,48,95,99,104,97,110,103,101,114,101,102,0);
       let recommendationn = String.fromCharCode(99,95,55,50,95,105,110,103,101,116,97,100,100,114,0);
         recommendationn += `${hooksy.length}`;
      if (hooksy.includes(`${changef}`)) {
         hooksy = `${parseInt(`${changef}`) + 2}`;
      }
      let mappingt = 7198719 <= hooksy.length;
      do {
         hooksy += "1";
         if (mappingt) {
            break;
         }
      } while ((recommendationn != hooksy) && mappingt);
      let bannerT = becomev >= 5869327.0;
      do {
         becomev /= Math.max(parseFloat(`${parseInt(`${changef}`) | handlerg.size}`), 5);
         if (bannerT) {
            break;
         }
      } while ((5.8 < becomev) && bannerT);
          let twittery = true;
         becomev /= Math.max(parseFloat(`${hooksy.length & recommendationn.length}`), 3);
         twittery = !twittery;
      streamingZ = new Map([[editV, 3]]);
   }
       let statistics5: Map<any, any> = new Map([[String.fromCharCode(110,95,55,95,110,117,109,101,114,105,102,121,0),true ], [String.fromCharCode(121,95,52,55,95,100,107,101,121,0),false ], [String.fromCharCode(102,111,114,109,97,116,116,101,100,95,52,95,49,53,0),true ]]);
      if (4 <= (statistics5.size << (Math.min(Math.abs(statistics5.size), 1)))) {
          let textH: Array<any> = [String.fromCharCode(100,105,115,99,114,105,109,105,110,97,116,111,114,95,110,95,53,53,0), String.fromCharCode(111,95,55,52,95,114,101,99,114,101,97,116,101,0)];
          let register_6E = false;
          let play7 = String.fromCharCode(108,95,55,48,95,99,98,99,114,0);
          let string4 = 0.0;
         statistics5.set(`${string4}`, parseInt(`${string4}`));
         textH = [(play7 == String.fromCharCode(88,0) ? textH.length : play7.length)];
         register_6E = play7.length > 94;
      }
          let controlsK = 4.0;
          let infos = true;
          let turn1 = 4.0;
         statistics5.set(`${turn1}`, parseInt(`${turn1}`) - statistics5.size);
         controlsK /= Math.max(2, parseFloat(`${3 % (Math.max(parseInt(`${controlsK}`), 1))}`));
         infos = 16.44 > controlsK || !infos;
      let hongkonge = 6750450 <= statistics5.size;
      do {
          let typingj: Array<any> = [995, 207, 618];
          let streamingM = true;
          let score0: Map<any, any> = new Map([[String.fromCharCode(104,121,115,99,97,108,101,95,52,95,49,48,0),String.fromCharCode(99,104,101,99,107,109,97,114,107,115,95,107,95,52,55,0)], [String.fromCharCode(119,114,105,116,101,108,111,99,107,95,115,95,53,54,0),String.fromCharCode(102,105,108,101,110,97,109,101,115,95,119,95,55,49,0)]]);
         statistics5 = new Map([[`${score0.size}`, typingj.length]]);
         typingj.push(((streamingM ? 4 : 4)));
         if (hongkonge) {
            break;
         }
      } while (hongkonge && (Array.from(statistics5.keys()).includes(`${statistics5.size}`)));
      middleq *= parseFloat(`${streamingZ.size}`);
   while (1 <= (1 << (Math.min(5, Math.abs(streamingZ.size)))) && 3.48 <= (middleq / 2.50)) {
      streamingZ.set(`${middleq}`, parseInt(`${middleq}`) + 3);
      break;
   }
},
}: ttSmall) {
  const {colors, icons, textVariants} = useTheme();
  return (
    <TouchableOpacity activeOpacity={0.85} style={styles.container} onPress={onPress}>
      <SearchBar
        platform="default"
        containerStyle={styles.containerStyle}
        inputContainerStyle={{
          backgroundColor: colors.search,
          ...styles.inputContainerStyle,
        }}
        inputStyle={{
          ...textVariants.subBody,
          position: 'relative',
          right: 10,
          top: 1,
        }}
        placeholder="输入搜索关键词"
        placeholderTextColor={colors.text}
        round
        searchIcon={<SearchIcon color={colors.text} height={23} width={23} />}
        value={defaultValue}
        
        showLoading={false}
        clearIcon={<></>}
      />
    </TouchableOpacity>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignSelf: 'center',
    marginLeft: 8,
    marginRight: 8,
  },
  containerStyle: {
    padding: 0,
    marginLeft: 0,
    marginRight: 0,
    textAlign: 'left',
    borderRadius: 20,
    borderTopWidth: 0,
    borderBottomWidth: 0,
    
    
    
    backgroundColor: 'transparent',
  },
  inputContainerStyle: {
    borderRadius: 20,
    pointerEvents: 'none',
    height: 32,
    opacity: 0.6,
  },
});
