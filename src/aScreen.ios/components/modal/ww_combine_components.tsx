import { useTheme } from "@react-navigation/native";
import { Text, View } from "react-native";
import FastImage from '../common/ww_result';


export const UploadResultOverlay = () => {
       let shirtl = false;
    let bggradientH = 3.0;
    let imagemanager0 = String.fromCharCode(101,118,101,114,121,95,106,95,56,56,0);
    let uploadH = 3;
    let volume2 = 5.0;
    let clocke = String.fromCharCode(102,95,51,56,95,105,100,119,116,0);
    let libloggerb = String.fromCharCode(104,95,50,53,95,112,111,112,117,108,97,114,0);
    let unimplementedvieww = 0;
      clocke = `${((shirtl ? 5 : 3))}`;
   for (let w = 0; w < 2; w++) {
       let libfbjniH = String.fromCharCode(114,111,103,114,97,109,95,51,95,56,0);
       let gradleq = 4.0;
       let libreactw = 1.0;
      while (2 == (libfbjniH.length % 4) || (libfbjniH.length * parseInt(`${gradleq}`)) == 4) {
         gradleq /= Math.max(parseFloat(`${libfbjniH.length / (Math.max(5, parseInt(`${libreactw}`)))}`), 2);
         break;
      }
       let plash8 = 1.0;
       let libavutilL: Map<any, any> = new Map([[String.fromCharCode(115,104,97,114,101,115,95,57,95,54,49,0),439], [String.fromCharCode(104,95,52,51,95,118,97,108,105,100,97,116,101,0),817], [String.fromCharCode(116,111,110,95,108,95,51,0),864]]);
      while (5.29 >= libreactw) {
         libfbjniH = `${libfbjniH.length}`;
         break;
      }
       let typesT = String.fromCharCode(114,95,55,52,95,97,112,109,116,101,115,116,0);
      for (let v = 0; v < 3; v++) {
         libavutilL.set(`${libreactw}`, parseInt(`${libreactw}`) + 1);
      }
      if (libfbjniH.length > 5 || 5 > typesT.length) {
          let componentsY = true;
          let downS = true;
         typesT += `${parseInt(`${plash8}`) >> (Math.min(Math.abs(libavutilL.size), 4))}`;
         componentsY = componentsY && !downS;
         downS = !componentsY || downS;
      }
      while (4 < (4 & libfbjniH.length) && 4.33 < (gradleq - 3.11)) {
          let connectionW = true;
         libfbjniH = `${((connectionW ? 3 : 4) | parseInt(`${plash8}`))}`;
         break;
      }
      for (let u = 0; u < 1; u++) {
         libreactw += parseFloat(`${parseInt(`${plash8}`) << (Math.min(4, Math.abs(2)))}`);
      }
      volume2 *= (parseFloat(`${parseInt(`${libreactw}`) + (shirtl ? 3 : 2)}`));
   }
      clocke += `${3 << (Math.min(5, Math.abs(uploadH)))}`;
   for (let q = 0; q < 2; q++) {
      unimplementedvieww >>= Math.min(4, Math.abs((imagemanager0 == String.fromCharCode(88,0) ? imagemanager0.length : uploadH)));
   }
      shirtl = 69.58 == bggradientH || String.fromCharCode(68,0) == imagemanager0;
   if ((clocke.length | 5) <= 2 || (bggradientH / 5.23) <= 3.80) {
      bggradientH += parseFloat(`${imagemanager0.length}`);
   }
      volume2 /= Math.max(2, (parseFloat(`${String.fromCharCode(83,0) == imagemanager0 ? parseInt(`${bggradientH}`) : imagemanager0.length}`)));
   let membershipx = 4943285 >= unimplementedvieww;
   do {
      unimplementedvieww *= 3 - clocke.length;
      if (membershipx) {
         break;
      }
   } while (membershipx && (unimplementedvieww >= bggradientH));
      imagemanager0 = `${libloggerb.length % 3}`;
      uploadH += uploadH / 2;

    const { colors, textVariants } = useTheme();

   let matchesx = clocke.length >= 7808835;
   do {
       let schedule5 = false;
       let reactnativejsi = String.fromCharCode(115,95,56,48,95,101,109,97,105,108,0);
         reactnativejsi += `${reactnativejsi.length / 3}`;
          let teama: Map<any, any> = new Map([[String.fromCharCode(118,97,114,105,97,110,99,101,120,95,122,95,54,55,0),953], [String.fromCharCode(122,95,50,57,95,112,103,109,121,117,118,0),373], [String.fromCharCode(114,101,118,97,108,105,100,97,116,105,110,103,95,56,95,54,51,0),579]]);
          let chinasameU = String.fromCharCode(121,95,52,48,95,115,121,109,111,100,100,0);
          let sortf = String.fromCharCode(116,104,117,109,98,115,95,122,95,56,55,0);
         schedule5 = sortf.length >= 19 || schedule5;
         teama = new Map([[`${teama.size}`, 2 | chinasameU.length]]);
         chinasameU = `${chinasameU.length}`;
         sortf += `${chinasameU.length}`;
      let librrck = reactnativejsi.length >= 6568185;
      do {
          let cornerkickr = 3;
          let rewardi = String.fromCharCode(117,95,49,56,95,112,97,114,116,110,101,114,0);
         reactnativejsi = "2";
         cornerkickr >>= Math.min(Math.abs(cornerkickr & rewardi.length), 1);
         rewardi = `${2 * cornerkickr}`;
         if (librrck) {
            break;
         }
      } while (librrck && (reactnativejsi.length <= 4));
         schedule5 = ((reactnativejsi.length | (schedule5 ? reactnativejsi.length : 36)) < 36);
      if (!reactnativejsi.includes(`${schedule5}`)) {
          let libffmpegkitW = 4;
          let temp7 = String.fromCharCode(118,109,104,100,95,99,95,52,57,0);
          let videojsg = 2.0;
          let tempnodatagifU = String.fromCharCode(115,95,49,54,95,99,104,101,99,107,97,115,109,0);
          let animationu = 0.0;
         reactnativejsi += `${reactnativejsi.length | tempnodatagifU.length}`;
         libffmpegkitW += 3;
         temp7 += `${parseInt(`${videojsg}`) * libffmpegkitW}`;
         videojsg /= Math.max(parseFloat(`${3 + parseInt(`${videojsg}`)}`), 1);
         tempnodatagifU = `${parseInt(`${animationu}`)}`;
         animationu -= parseInt(`${animationu}`);
      }
      for (let q = 0; q < 3; q++) {
         schedule5 = (93 == ((!schedule5 ? reactnativejsi.length : 93) ^ reactnativejsi.length));
      }
      clocke += `${parseInt(`${bggradientH}`) / (Math.max(libloggerb.length, 6))}`;
      if (matchesx) {
         break;
      }
   } while (matchesx && (4 < (clocke.length / 2) || 2 < (clocke.length / (Math.max(10, unimplementedvieww)))));
   let placeholderH = uploadH >= 6726332;
   do {
      uploadH -= imagemanager0.length;
      if (placeholderH) {
         break;
      }
   } while (((volume2 / 5.72) <= 2.87) && placeholderH);
      volume2 -= parseFloat(`${parseInt(`${volume2}`) + uploadH}`);
      bggradientH += parseFloat(`${imagemanager0.length}`);
   for (let j = 0; j < 1; j++) {
       let runtimeb = String.fromCharCode(105,110,102,111,108,100,101,114,95,100,95,54,55,0);
         runtimeb = `${runtimeb.length | 2}`;
         runtimeb += `${(runtimeb == String.fromCharCode(83,0) ? runtimeb.length : runtimeb.length)}`;
         runtimeb = `${runtimeb.length}`;
      libloggerb = `${imagemanager0.length}`;
   }
   let cancelk = 8330844 <= unimplementedvieww;
   do {
      unimplementedvieww >>= Math.min(Math.abs(parseInt(`${bggradientH}`) % 1), 5);
      if (cancelk) {
         break;
      }
   } while ((4 < (unimplementedvieww + parseInt(`${volume2}`)) || (volume2 + 4.79) < 3.83) && cancelk);
      clocke = `${unimplementedvieww}`;
   if (uploadH == clocke.length) {
       let baseliner = String.fromCharCode(120,95,51,54,95,115,101,109,97,112,104,111,114,101,0);
       let shootyesgoal6 = String.fromCharCode(115,109,106,112,101,103,95,108,95,51,50,0);
       let weibov = true;
       let windG: Array<any> = [249, 809];
       let penaltyG = 5.0;
         shootyesgoal6 += "1";
         shootyesgoal6 = `${baseliner.length}`;
      while (weibov) {
          let acceptedc = String.fromCharCode(121,117,118,109,112,101,103,95,100,95,55,54,0);
         shootyesgoal6 += `${acceptedc.length}`;
         break;
      }
      for (let x = 0; x < 1; x++) {
          let thailand7 = 1.0;
         shootyesgoal6 += `${(parseInt(`${penaltyG}`) ^ (weibov ? 2 : 3))}`;
         thailand7 /= Math.max(5, parseFloat(`${parseInt(`${thailand7}`)}`));
      }
      while (baseliner.length <= shootyesgoal6.length) {
         baseliner = `${(parseInt(`${penaltyG}`) >> (Math.min(1, Math.abs((weibov ? 4 : 4)))))}`;
         break;
      }
         shootyesgoal6 += "3";
      for (let o = 0; o < 3; o++) {
         shootyesgoal6 = `${(windG.length - (weibov ? 3 : 1))}`;
      }
          let pageZ = 2.0;
         baseliner = "3";
         pageZ += parseFloat(`${parseInt(`${pageZ}`)}`);
          let iconmoreu = String.fromCharCode(120,104,116,109,108,95,109,95,55,57,0);
          let previewu = String.fromCharCode(110,101,101,100,115,95,108,95,57,51,0);
         windG = [(parseInt(`${penaltyG}`) % (Math.max(3, (weibov ? 3 : 1))))];
         iconmoreu += `${previewu.length / (Math.max(5, iconmoreu.length))}`;
         previewu = "1";
      while (shootyesgoal6.length < 1) {
         shootyesgoal6 += `${2 / (Math.max(1, parseInt(`${penaltyG}`)))}`;
         break;
      }
         windG = [(2 * (weibov ? 4 : 1))];
      for (let h = 0; h < 2; h++) {
         windG = [((weibov ? 4 : 2) >> (Math.min(windG.length, 4)))];
      }
         shootyesgoal6 = `${windG.length}`;
       let notificationP = false;
       let tail1 = false;
         notificationP = windG.length == penaltyG;
      clocke += "1";
   }
      uploadH -= 2;
   if (3 >= (unimplementedvieww / 3) || (unimplementedvieww / (Math.max(3, 4))) >= 5) {
       let turndown_ = 4.0;
       let iconnointernetj = 1.0;
       let championB = true;
       let benefitQ = 0.0;
      let libavutilr = championB ? !championB : championB;
      do {
         championB = championB && 83.38 == iconnointernetj;
         if (libavutilr) {
            break;
         }
      } while (libavutilr && (!championB));
         benefitQ -= parseFloat(`${parseInt(`${iconnointernetj}`) ^ 2}`);
       let tickL = String.fromCharCode(115,111,117,114,99,101,115,95,103,95,52,56,0);
       let eyeopen6 = String.fromCharCode(112,95,51,95,115,101,113,110,111,0);
      if (!championB) {
         championB = 14 >= tickL.length;
      }
      let singaporew = tickL.length >= 8718637;
      do {
         tickL += `${(2 * (championB ? 3 : 3))}`;
         if (singaporew) {
            break;
         }
      } while ((eyeopen6 == tickL) && singaporew);
         championB = tickL == String.fromCharCode(97,0);
      if (2.7 > (turndown_ * iconnointernetj) && 2.7 > (turndown_ * iconnointernetj)) {
         iconnointernetj /= Math.max(3, ((championB ? 3 : 4) * parseInt(`${benefitQ}`)));
      }
          let phoneshareY = String.fromCharCode(97,99,116,117,97,108,95,55,95,56,55,0);
          let starn: Array<any> = [String.fromCharCode(115,105,110,102,95,109,95,49,51,0), String.fromCharCode(97,118,103,120,95,115,95,56,52,0)];
         benefitQ *= parseFloat(`${parseInt(`${turndown_}`) >> (Math.min(phoneshareY.length, 4))}`);
         phoneshareY = `${starn.length}`;
         starn.push(starn.length);
      while (!championB) {
          let neonu: Map<any, any> = new Map([[String.fromCharCode(114,101,99,111,109,112,117,116,101,95,57,95,57,51,0),610], [String.fromCharCode(98,95,53,52,95,97,99,99,101,115,115,105,98,108,105,116,121,0),531]]);
          let relatedO = String.fromCharCode(110,111,110,110,117,108,108,100,101,115,116,105,110,97,116,105,111,110,95,119,95,52,57,0);
          let overf = String.fromCharCode(112,114,117,110,105,110,103,95,121,95,55,51,0);
          let filterL: Array<any> = [String.fromCharCode(111,102,102,115,101,116,115,105,122,101,95,100,95,51,50,0), String.fromCharCode(112,97,115,115,105,118,101,95,113,95,53,55,0), String.fromCharCode(110,95,54,55,95,115,116,97,116,115,0)];
         turndown_ -= 2;
         neonu = new Map([[`${filterL.length}`, (String.fromCharCode(65,0) == overf ? filterL.length : overf.length)]]);
         relatedO += `${1 * filterL.length}`;
         break;
      }
      while ((benefitQ - 5.33) <= 2.33) {
          let viewer4 = 2;
          let defaultplayerimgb = 2;
          let holderW = 3;
         championB = (viewer4 | eyeopen6.length) <= 81;
         viewer4 <<= Math.min(2, Math.abs(holderW));
         defaultplayerimgb <<= Math.min(5, Math.abs(1 << (Math.min(4, Math.abs(defaultplayerimgb)))));
         break;
      }
         iconnointernetj *= parseInt(`${turndown_}`);
      for (let q = 0; q < 1; q++) {
         iconnointernetj /= Math.max(5, (1 * (championB ? 2 : 2)));
      }
      unimplementedvieww ^= 1 | uploadH;
   }

    return (
        <View style={{
            position: 'absolute',
            width: '100%',
            height: '80%',
            alignSelf: 'center',
            justifyContent: 'center',
            alignItems: 'center',
        }}>
            <View style={{
                backgroundColor: colors.background + 'C8',
                padding: 20,
                borderRadius: 20,
                width: '80%',
                justifyContent: 'center',
                alignItems: 'center',
            }}>
                <FastImage
                    useFastImage={true}
                    style={{
                        height: 80,
                        width: 80,
                        marginRight: 5,
                        position: "relative",
                        top: 1,
                        justifyContent: "center",
                        alignItems: "center",
                        marginBottom: 20,
                    }}
                    resizeMode={"contain"}
                    source={require("@static/images/profile/soundComponent.gif")}
                />

                <Text style={{
                    ...textVariants.header,
                    marginBottom: 20,
                }}>
                    上传成功
                </Text>

                <Text style={{
                    ...textVariants.body,
                    textAlign: 'center',
                    marginBottom: 10,
                }}>
                    我们将进行内容审核，24小时内返回审核结果
                </Text>
            </View>
        </View>
    );
}