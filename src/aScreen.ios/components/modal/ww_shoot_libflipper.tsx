import React, { Suspense, useCallback, useEffect, useState } from 'react';
import { View, Text, TouchableOpacity } from 'react-native';
import VipModal from './ww_qaag';
import { useNavigation, useTheme } from '@react-navigation/native';
import AsyncStorage from '@react-native-async-storage/async-storage';
import { useAppDispatch } from '@hooks/ww_catagory_neon';
import {
  showLoginAction,
} from '@redux/actions/ww_hash';

interface wwIndexDice { }

export default function VipOverlay({ }: wwIndexDice) {
  const [showVIPOverlay, setShowVIPOverlay] = useState(false);

  const hideVipPrompt = useCallback(async () => {
       let acceptedf = 5.0;
    let libruntimeexecutor7 = 3.0;
    let mergerN = String.fromCharCode(100,105,115,99,114,101,116,101,0);
    let libyogaX = 4.0;
    let fastforwardj: Array<any> = [86, 272, 761];
    let phonesharee: Array<any> = [716, 448];
    let time_ob: Map<any, any> = new Map([[String.fromCharCode(97,114,114,97,121,0),true ], [String.fromCharCode(105,100,99,116,120,100,99,0),true ]]);
    let promotion0 = 3;
    let nterstitialM = 0;
      time_ob = new Map([[`${time_ob.size}`, time_ob.size]]);
   if (libyogaX > 5.91) {
      libyogaX *= parseInt(`${acceptedf}`);
   }
   if ((parseInt(`${acceptedf}`) + time_ob.size) <= 4) {
      time_ob.set(`${libruntimeexecutor7}`, parseInt(`${libruntimeexecutor7}`) / (Math.max(fastforwardj.length, 7)));
   }
       let renewu = true;
       let iconschedulef = String.fromCharCode(103,101,116,97,100,100,114,105,110,102,111,0);
         renewu = renewu || iconschedulef.length >= 74;
          let banner3 = 4.0;
          let uploadc: Map<any, any> = new Map([[String.fromCharCode(101,120,112,111,0),679], [String.fromCharCode(109,101,115,115,97,103,101,0),282]]);
         renewu = banner3 == uploadc.size;
      if (iconschedulef.length < 5) {
         renewu = iconschedulef.includes(`${renewu}`);
      }
          let internetO = true;
          let mbbannerf = true;
         iconschedulef = `${((internetO ? 4 : 2))}`;
       let gestureb = 0;
          let nalyticsn = false;
          let networkv: Array<any> = [832, 514, 3];
         gestureb |= 2;
         nalyticsn = networkv.length >= 13;
         networkv = [networkv.length % 3];
      fastforwardj.push(time_ob.size);
       let middlewareq = String.fromCharCode(120,99,111,100,101,0);
       let containerR = 2;
       let chinax = false;
         chinax = !chinax && containerR <= 3;
      if (!chinax) {
         containerR -= 3;
      }
         containerR *= containerR;
         middlewareq = `${middlewareq.length ^ 2}`;
      while (middlewareq.length >= 5) {
         middlewareq += "1";
         break;
      }
       let interstitialC = String.fromCharCode(100,101,99,111,100,101,114,105,110,105,116,0);
      if (!chinax) {
          let trashV = 4.0;
          let middlebrightnessJ = String.fromCharCode(105,110,116,101,114,112,111,108,97,116,101,102,0);
          let scheduleZ = 5.0;
         chinax = interstitialC == String.fromCharCode(122,0);
         trashV += middlebrightnessJ.length - 1;
         middlebrightnessJ += `${middlebrightnessJ.length / (Math.max(7, parseInt(`${trashV}`)))}`;
         scheduleZ -= middlebrightnessJ.length;
      }
      while ((interstitialC.length ^ containerR) >= 1 || 3 >= (containerR ^ 1)) {
         interstitialC = `${(String.fromCharCode(66,0) == interstitialC ? (chinax ? 5 : 1) : interstitialC.length)}`;
         break;
      }
         middlewareq = "2";
      mergerN += "1";
      fastforwardj = [3 << (Math.min(2, Math.abs(time_ob.size)))];
       let notification3 = String.fromCharCode(115,116,114,116,111,107,0);
       let libavcodecV = String.fromCharCode(101,97,116,105,110,103,0);
      let select5 = libavcodecV.length >= 5559330;
      do {
         libavcodecV = `${notification3.length | libavcodecV.length}`;
         if (select5) {
            break;
         }
      } while (select5 && (notification3 == libavcodecV));
         libavcodecV += `${(libavcodecV == String.fromCharCode(122,0) ? libavcodecV.length : notification3.length)}`;
         libavcodecV = `${libavcodecV.length}`;
      if (5 <= notification3.length) {
          let iconfeedbackT = true;
          let button8: Map<any, any> = new Map([[String.fromCharCode(103,105,118,101,110,0),337], [String.fromCharCode(114,101,115,111,108,118,101,114,0),706], [String.fromCharCode(102,105,110,97,108,108,121,0),404]]);
          let dycreatorD = String.fromCharCode(120,105,112,104,108,97,99,105,110,103,0);
         libavcodecV += `${(dycreatorD == String.fromCharCode(83,0) ? dycreatorD.length : (iconfeedbackT ? 2 : 5))}`;
         iconfeedbackT = button8.size < 53 || button8.size < 53;
      }
          let watchnowbgV = 2;
          let debug7 = 3.0;
         libavcodecV = `${watchnowbgV % (Math.max(9, libavcodecV.length))}`;
         watchnowbgV &= parseInt(`${debug7}`) << (Math.min(Math.abs(parseInt(`${debug7}`)), 3));
      while (2 <= notification3.length || libavcodecV != String.fromCharCode(114,0)) {
         libavcodecV += `${notification3.length | libavcodecV.length}`;
         break;
      }
      fastforwardj.push(libavcodecV.length << (Math.min(1, mergerN.length)));
   while ((4 ^ time_ob.size) <= 1 || (4 / (Math.max(5, time_ob.size))) <= 3) {
       let downloader3 = 0.0;
       let lessv: Map<any, any> = new Map([[String.fromCharCode(111,97,101,112,0),488], [String.fromCharCode(102,111,114,109,97,110,116,0),771], [String.fromCharCode(99,111,108,117,109,110,0),369]]);
          let moviesz = false;
          let navigationG = 2.0;
          let settingsB: Map<any, any> = new Map([[String.fromCharCode(118,105,100,101,111,105,110,102,111,104,101,97,100,101,114,0),67], [String.fromCharCode(97,108,105,118,101,0),998], [String.fromCharCode(118,114,101,112,0),798]]);
         downloader3 -= (parseFloat(`${parseInt(`${navigationG}`) * (moviesz ? 5 : 3)}`));
         moviesz = (settingsB.size % (Math.max(settingsB.size, 1))) < 73;
         navigationG /= Math.max(4, parseFloat(`${1}`));
      let iconmorek = 5390001 <= lessv.size;
      do {
          let b_manager0 = 3;
          let analyticsV = false;
         lessv = new Map([[`${lessv.size}`, parseInt(`${downloader3}`) + lessv.size]]);
         b_manager0 %= Math.max(2, ((analyticsV ? 3 : 2) / (Math.max(8, b_manager0))));
         analyticsV = 52 == b_manager0 || !analyticsV;
         if (iconmorek) {
            break;
         }
      } while (iconmorek && (lessv.get(`${downloader3}`) == null));
       let indicatorD = String.fromCharCode(109,97,120,114,101,97,100,101,114,115,0);
       let overt = String.fromCharCode(120,99,101,112,116,105,111,110,0);
      for (let u = 0; u < 1; u++) {
         lessv = new Map([[overt, 1]]);
      }
          let libfbjnif: Array<any> = [149, 564];
          let logoA: Map<any, any> = new Map([[String.fromCharCode(104,97,110,100,108,105,110,103,0),418], [String.fromCharCode(112,115,102,105,108,101,0),708], [String.fromCharCode(112,97,114,97,109,101,116,101,114,115,0),385]]);
          let androidW = String.fromCharCode(108,111,110,103,110,111,105,115,101,0);
         overt += "3";
         libfbjnif.push(libfbjnif.length);
         logoA = new Map([[`${logoA.size}`, androidW.length]]);
         androidW = `${androidW.length % (Math.max(7, logoA.size))}`;
      for (let v = 0; v < 2; v++) {
          let main_wM = String.fromCharCode(109,105,110,117,115,0);
         overt += "2";
         main_wM = `${main_wM.length & main_wM.length}`;
      }
      time_ob.set(`${nterstitialM}`, 1);
      break;
   }
   while ((1 >> (Math.min(4, phonesharee.length))) == 2) {
      phonesharee.push(time_ob.size);
      break;
   }
      promotion0 += nterstitialM;
   for (let e = 0; e < 1; e++) {
      promotion0 -= 3 & time_ob.size;
   }
   let libflippert = libruntimeexecutor7 <= 8863998.0;
   do {
      libruntimeexecutor7 += parseInt(`${acceptedf}`) ^ promotion0;
      if (libflippert) {
         break;
      }
   } while ((4.94 == (libruntimeexecutor7 + 3.32)) && libflippert);
       let mutedM = 3;
       let launchS = false;
         launchS = mutedM <= 77;
          let disconnectedO = String.fromCharCode(103,101,110,101,114,97,116,105,110,103,0);
          let album5 = 5.0;
         mutedM += 2;
         disconnectedO = `${parseInt(`${album5}`)}`;
         album5 /= Math.max(disconnectedO.length ^ 1, 2);
      for (let r = 0; r < 2; r++) {
          let blackL = true;
          let mbnativex: Array<any> = [708, 547];
          let referrerW = 4;
          let pauseU = String.fromCharCode(100,105,97,103,114,97,109,0);
         launchS = pauseU.length <= 48;
         blackL = 41 == referrerW;
         mbnativex.push(referrerW);
         pauseU += "1";
      }
         launchS = mutedM < 52 || launchS;
         launchS = !launchS;
          let libyogat = 2.0;
          let utilsu = 1.0;
          let iconarrowright2 = String.fromCharCode(114,101,119,114,105,116,101,0);
         mutedM /= Math.max(1, 2 - parseInt(`${libyogat}`));
         libyogat *= parseFloat(`${parseInt(`${utilsu}`)}`);
         utilsu += (parseFloat(`${iconarrowright2 == String.fromCharCode(77,0) ? parseInt(`${utilsu}`) : iconarrowright2.length}`));
      phonesharee.push(parseInt(`${libruntimeexecutor7}`) % 3);
   for (let m = 0; m < 1; m++) {
      promotion0 -= 1;
   }
   if ((nterstitialM - phonesharee.length) < 5) {
      phonesharee.push(1);
   }
       let profileinactiveW = 2;
         profileinactiveW %= Math.max(5, 1 ^ profileinactiveW);
         profileinactiveW <<= Math.min(Math.abs(profileinactiveW), 3);
      let over8 = 8736323 >= profileinactiveW;
      do {
          let targett = String.fromCharCode(116,107,104,100,0);
         profileinactiveW >>= Math.min(2, Math.abs(profileinactiveW));
         targett += `${2 << (Math.min(2, targett.length))}`;
         if (over8) {
            break;
         }
      } while (over8 && ((profileinactiveW | 1) == 1));
      promotion0 += time_ob.size;
   while (!fastforwardj.includes(acceptedf)) {
       let logoU = 0;
       let schedule6 = String.fromCharCode(97,100,100,105,116,105,111,110,97,108,108,121,0);
       let classesX = 4.0;
       let bootg = 2;
       let delegate_npP = 2;
      if (5 >= (delegate_npP * schedule6.length) && (delegate_npP * schedule6.length) >= 5) {
         schedule6 = `${bootg % (Math.max(schedule6.length, 7))}`;
      }
         logoU -= 2 | delegate_npP;
          let sharemodall = 4.0;
          let shootA = false;
          let stats7 = String.fromCharCode(116,119,105,100,100,108,101,115,0);
         bootg %= Math.max(bootg, 5);
         sharemodall -= 2;
         shootA = stats7.length < sharemodall;
         stats7 += `${1 ^ stats7.length}`;
      while (5 <= logoU) {
          let containerV: Map<any, any> = new Map([[String.fromCharCode(99,97,100,101,110,99,101,0),136], [String.fromCharCode(112,114,111,109,105,115,105,102,121,0),339]]);
          let mail1 = 0.0;
         logoU >>= Math.min(Math.abs(2), 1);
         containerV = new Map([[`${containerV.size}`, 2 - containerV.size]]);
         mail1 /= Math.max(1 & containerV.size, 2);
         break;
      }
          let tooltipsJ = true;
          let shirt4: Map<any, any> = new Map([[String.fromCharCode(102,114,101,101,100,117,109,112,98,117,102,102,101,114,0),String.fromCharCode(105,102,102,116,0)], [String.fromCharCode(115,108,117,114,112,0),String.fromCharCode(111,103,103,105,110,103,0)], [String.fromCharCode(119,105,110,101,114,114,110,111,0),String.fromCharCode(104,119,97,101,115,0)]]);
         classesX -= (parseFloat(`${(tooltipsJ ? 2 : 3)}`));
         tooltipsJ = shirt4.size < shirt4.size;
      let jingdongd = bootg >= 7881830;
      do {
         bootg += delegate_npP / (Math.max(schedule6.length, 6));
         if (jingdongd) {
            break;
         }
      } while (((2.12 + classesX) >= 3.17 || 2 >= (bootg + parseInt(`${classesX}`))) && jingdongd);
      for (let c = 0; c < 1; c++) {
          let modulej: Array<any> = [558, 709];
          let benefitM = String.fromCharCode(116,105,109,101,99,111,100,101,115,0);
          let footballX: Array<any> = [String.fromCharCode(103,101,110,101,114,97,108,105,115,101,100,0), String.fromCharCode(118,97,114,105,110,102,111,0)];
          let referrern = String.fromCharCode(105,109,108,116,0);
         bootg &= 2 ^ parseInt(`${classesX}`);
         modulej.push(footballX.length % (Math.max(1, referrern.length)));
         benefitM = `${(String.fromCharCode(97,0) == referrern ? footballX.length : referrern.length)}`;
      }
      for (let d = 0; d < 3; d++) {
         delegate_npP <<= Math.min(Math.abs(delegate_npP | 3), 4);
      }
      fastforwardj.push(parseInt(`${libruntimeexecutor7}`) & parseInt(`${acceptedf}`));
      break;
   }
   if ((nterstitialM - 3) <= 1 || (nterstitialM - fastforwardj.length) <= 3) {
       let tumbnailf = 1.0;
      for (let p = 0; p < 2; p++) {
         tumbnailf -= parseInt(`${tumbnailf}`) + 3;
      }
      for (let a = 0; a < 1; a++) {
         tumbnailf /= Math.max(parseInt(`${tumbnailf}`) >> (Math.min(1, Math.abs(2))), 5);
      }
      if (2.39 <= (tumbnailf / (Math.max(3, 2)))) {
         tumbnailf *= 1 ^ parseInt(`${tumbnailf}`);
      }
      nterstitialM |= 2 >> (Math.min(4, Math.abs(promotion0)));
   }

    setShowVIPOverlay(false);
  }, []);

  const dispatch = useAppDispatch();

  useEffect(() => {
    const checkFirstLaunchVipPrompted = async () => {
       let taiwanG = 5.0;
    let profileframeV = 1.0;
    let away9: Array<any> = [833, 790];
    let activep = String.fromCharCode(115,97,110,100,98,111,120,0);
    let storeT = true;
    let eventsplashh = String.fromCharCode(98,97,110,100,105,110,103,0);
    let unreadM = 4.0;
    let agreementZ = true;
    let currentR = String.fromCharCode(103,101,110,101,114,105,99,0);
    let bellC = String.fromCharCode(119,109,97,118,111,105,99,101,0);
       let screenC = String.fromCharCode(97,103,103,114,101,103,97,116,101,100,0);
      let countryW = String.fromCharCode(98,117,51,112,0) == screenC;
      do {
         screenC += `${2 | screenC.length}`;
         if (countryW) {
            break;
         }
      } while (countryW && (screenC.length == screenC.length));
      while (screenC.length <= 5) {
          let redirectP = String.fromCharCode(115,116,97,114,116,114,101,101,0);
          let switch_m5A = String.fromCharCode(115,117,112,101,114,115,101,116,0);
         screenC = `${switch_m5A.length}`;
         redirectP += `${redirectP.length >> (Math.min(redirectP.length, 1))}`;
         switch_m5A += `${redirectP.length * redirectP.length}`;
         break;
      }
      for (let u = 0; u < 1; u++) {
          let changep = String.fromCharCode(100,115,116,114,101,97,109,95,122,95,52,50,0);
          let componentsC = 4.0;
          let dependenciesR = 1.0;
          let dependenciesE = String.fromCharCode(112,108,117,114,97,108,105,122,101,100,0);
          let whitetick8 = String.fromCharCode(112,97,103,101,115,101,101,107,0);
         screenC += `${(screenC == String.fromCharCode(48,0) ? screenC.length : dependenciesE.length)}`;
         changep = `${changep.length}`;
         componentsC *= 1 >> (Math.min(2, Math.abs(parseInt(`${dependenciesR}`))));
         dependenciesR += parseFloat(`${2}`);
         dependenciesE += `${changep.length}`;
         whitetick8 = "2";
      }
      taiwanG *= ((agreementZ ? 3 : 4));
       let animationp = 5;
       let dependenciesB = String.fromCharCode(108,111,103,105,99,0);
       let classesL = String.fromCharCode(100,112,99,109,0);
          let descZ = String.fromCharCode(100,105,102,102,105,99,117,108,116,121,0);
          let animationC = String.fromCharCode(98,101,116,97,0);
          let flipperx = 4.0;
         classesL = `${parseInt(`${flipperx}`) % 3}`;
         descZ = `${descZ.length}`;
         animationC += `${descZ.length}`;
         flipperx += (parseFloat(`${String.fromCharCode(121,0) == descZ ? animationC.length : descZ.length}`));
      if ((animationp + dependenciesB.length) > 5 && 5 > (animationp + dependenciesB.length)) {
         dependenciesB += `${2 * animationp}`;
      }
      let attributedstringD = 8005655 <= animationp;
      do {
          let controlq = 5.0;
          let iconnointernet7 = true;
          let viewerm = 4.0;
          let executor2 = String.fromCharCode(118,112,100,97,116,97,0);
          let suggestiona = 0.0;
         animationp %= Math.max(5, classesL.length);
         controlq /= Math.max(3, ((iconnointernet7 ? 2 : 2) << (Math.min(Math.abs(parseInt(`${controlq}`)), 5))));
         iconnointernet7 = viewerm <= parseFloat(`${executor2.length}`);
         viewerm /= Math.max(parseFloat(`${parseInt(`${suggestiona}`) % 1}`), 4);
         executor2 = `${((iconnointernet7 ? 2 : 4) + parseInt(`${controlq}`))}`;
         suggestiona += parseFloat(`${parseInt(`${controlq}`) % 1}`);
         if (attributedstringD) {
            break;
         }
      } while (((dependenciesB.length + 2) < 5 && 3 < (2 + dependenciesB.length)) && attributedstringD);
      if (dependenciesB.includes(`${animationp}`)) {
         dependenciesB += `${animationp / (Math.max(2, 10))}`;
      }
         animationp /= Math.max(4, classesL.length);
      for (let q = 0; q < 3; q++) {
          let renewg = String.fromCharCode(117,110,115,117,112,112,111,114,116,101,100,0);
          let promotiono = true;
          let trophyN: Array<any> = [String.fromCharCode(98,97,110,100,101,100,0), String.fromCharCode(97,105,116,101,114,0), String.fromCharCode(104,101,97,100,108,105,110,101,0)];
          let application1: Map<any, any> = new Map([[String.fromCharCode(98,105,116,112,108,97,110,97,114,99,104,117,110,107,121,0),String.fromCharCode(114,105,102,102,0)], [String.fromCharCode(100,114,98,103,0),String.fromCharCode(101,120,105,115,116,101,110,99,101,0)]]);
         classesL = `${classesL.length}`;
         renewg += `${1 % (Math.max(9, application1.size))}`;
         promotiono = trophyN.length < application1.size;
         trophyN.push(2 ^ renewg.length);
      }
      if (!dependenciesB.includes(`${classesL.length}`)) {
         classesL = `${(String.fromCharCode(51,0) == dependenciesB ? dependenciesB.length : animationp)}`;
      }
         dependenciesB = `${classesL.length % 2}`;
         classesL = `${1 * animationp}`;
      profileframeV /= Math.max(3, parseFloat(`${1}`));
   while (4 < (activep.length | 5) || 5 < (activep.length | away9.length)) {
      activep = "1";
      break;
   }
      agreementZ = !activep.startsWith(`${taiwanG}`);
       let sendb = String.fromCharCode(112,105,112,101,119,105,114,101,0);
       let redgoaln = 4.0;
      if ((sendb.length / (Math.max(6, parseInt(`${redgoaln}`)))) >= 4) {
         sendb += `${1 / (Math.max(7, sendb.length))}`;
      }
      if (!sendb.includes(`${redgoaln}`)) {
          let control2: Array<any> = [896, 479];
          let typing7 = String.fromCharCode(100,101,109,97,110,100,0);
          let bgvipxvodd: Array<any> = [String.fromCharCode(112,111,105,110,116,101,114,0), String.fromCharCode(103,101,111,99,111,100,101,115,0), String.fromCharCode(115,116,114,101,110,103,116,104,115,0)];
         sendb = `${(String.fromCharCode(99,0) == sendb ? typing7.length : sendb.length)}`;
         control2 = [control2.length & bgvipxvodd.length];
         typing7 = "2";
         bgvipxvodd.push(control2.length ^ bgvipxvodd.length);
      }
      storeT = profileframeV > unreadM;

      try {

   for (let w = 0; w < 3; w++) {
       let styles_ = String.fromCharCode(104,108,115,112,108,97,121,108,105,115,116,0);
       let giflivestreamings = String.fromCharCode(105,110,99,111,114,114,101,99,116,0);
       let projectq = 4.0;
      if (4 > styles_.length) {
          let sentryX = 5.0;
          let videovarZ = String.fromCharCode(99,114,111,112,112,101,100,0);
          let typesJ = String.fromCharCode(97,108,108,111,119,0);
          let shareC = String.fromCharCode(118,97,108,105,100,97,116,111,114,0);
         projectq /= Math.max(4, parseFloat(`${videovarZ.length % (Math.max(styles_.length, 1))}`));
         sentryX -= 1 & parseInt(`${sentryX}`);
         videovarZ = `${typesJ.length % (Math.max(2, parseInt(`${sentryX}`)))}`;
         typesJ = "1";
         shareC += `${parseInt(`${sentryX}`) - shareC.length}`;
      }
       let privacye = false;
       let latnP = true;
      for (let h = 0; h < 3; h++) {
         styles_ = `${(2 - (latnP ? 3 : 2))}`;
      }
          let iconsettingU = 0.0;
          let default_bW = String.fromCharCode(112,105,110,107,0);
         styles_ += `${3 << (Math.min(Math.abs(parseInt(`${projectq}`)), 4))}`;
         iconsettingU /= Math.max(4, 2);
         default_bW += `${default_bW.length | parseInt(`${iconsettingU}`)}`;
         styles_ = `${((latnP ? 5 : 5) << (Math.min(Math.abs((privacye ? 4 : 2)), 4)))}`;
      let downloaded2 = latnP ? !latnP : latnP;
      do {
          let libturbomodulejsijniX = 4.0;
          let watchnowbgG = String.fromCharCode(99,116,108,111,117,116,112,117,116,0);
          let gradleU = 5.0;
         latnP = (8 == (styles_.length / (Math.max(10, (!latnP ? 8 : styles_.length)))));
         libturbomodulejsijniX /= Math.max(3, 3);
         watchnowbgG += `${2 << (Math.min(5, Math.abs(parseInt(`${gradleU}`))))}`;
         gradleU += 3;
         if (downloaded2) {
            break;
         }
      } while (downloaded2 && (!latnP || 5 >= styles_.length));
      for (let d = 0; d < 1; d++) {
          let liblogger2 = String.fromCharCode(100,98,115,105,122,101,0);
          let shooty = 0.0;
         giflivestreamings = `${1 << (Math.min(4, liblogger2.length))}`;
         liblogger2 += `${parseInt(`${shooty}`) | 1}`;
         shooty /= Math.max(3, parseFloat(`${parseInt(`${shooty}`)}`));
      }
         privacye = styles_ == giflivestreamings;
      if (4 <= giflivestreamings.length || latnP) {
         giflivestreamings += `${((privacye ? 5 : 4) - (latnP ? 1 : 4))}`;
      }
      agreementZ = !activep.startsWith(`${storeT}`);
   }
   if ((away9.length + activep.length) >= 4 && 5 >= (activep.length + 4)) {
      activep += `${parseInt(`${unreadM}`) - 2}`;
   }
   let libffmpegkitB = 9398165 >= away9.length;
   do {
      away9 = [away9.length];
      if (libffmpegkitB) {
         break;
      }
   } while ((1 == eventsplashh.length) && libffmpegkitB);
   while (!storeT) {
      storeT = activep.length >= 82;
      break;
   }
      taiwanG -= 3 | away9.length;
        const vipPrompted = await AsyncStorage.getItem('firstLaunchVipPrompt');

   let videovar4 = agreementZ ? !agreementZ : agreementZ;
   do {
      agreementZ = currentR.length < 29 || storeT;
      if (videovar4) {
         break;
      }
   } while ((taiwanG < 4.89) && videovar4);
      profileframeV -= parseFloat(`${1}`);
   for (let d = 0; d < 2; d++) {
      away9 = [(eventsplashh == String.fromCharCode(68,0) ? eventsplashh.length : parseInt(`${unreadM}`))];
   }
      storeT = eventsplashh.length >= parseInt(`${unreadM}`);
   if (2.20 == profileframeV) {
      profileframeV += parseFloat(`${eventsplashh.length * 2}`);
   }
        if (vipPrompted === null) {

      away9.push(3);
   let foregroundX = away9.length <= 8476948;
   do {
      away9.push(3 % (Math.max(parseInt(`${unreadM}`), 10)));
      if (foregroundX) {
         break;
      }
   } while (foregroundX && (2 < (5 + activep.length)));
      profileframeV *= (parseFloat(`${parseInt(`${taiwanG}`) + (storeT ? 5 : 1)}`));
   while (agreementZ && !storeT) {
      storeT = !storeT;
      break;
   }
   while (eventsplashh.endsWith(`${unreadM}`)) {
       let v_hashj = true;
         v_hashj = (v_hashj ? v_hashj : !v_hashj);
         v_hashj = !v_hashj;
         v_hashj = !v_hashj;
      eventsplashh = `${((agreementZ ? 4 : 1) ^ (storeT ? 1 : 5))}`;
      break;
   }
          

      away9.push(((agreementZ ? 1 : 1) + parseInt(`${taiwanG}`)));
   let condensed5 = activep == String.fromCharCode(98,104,55,120,111,0);
   do {
      activep = `${away9.length + 1}`;
      if (condensed5) {
         break;
      }
   } while (condensed5 && (4 == activep.length));
       let iconrefresh5 = String.fromCharCode(97,99,116,105,118,97,116,105,111,110,115,0);
         iconrefresh5 += `${iconrefresh5.length}`;
         iconrefresh5 += `${iconrefresh5.length}`;
      for (let m = 0; m < 3; m++) {
         iconrefresh5 = "1";
      }
      profileframeV -= parseFloat(`${activep.length & parseInt(`${taiwanG}`)}`);
      away9.push(2);
      bellC += `${(String.fromCharCode(112,0) == bellC ? activep.length : bellC.length)}`;
          setShowVIPOverlay(true);

   for (let s = 0; s < 1; s++) {
       let main_g5 = String.fromCharCode(98,117,98,98,108,101,0);
       let detailsm = String.fromCharCode(108,117,109,105,110,97,110,99,101,0);
       let footballfieldK: Map<any, any> = new Map([[String.fromCharCode(105,110,116,108,0),311], [String.fromCharCode(111,115,109,111,115,0),734], [String.fromCharCode(114,101,115,112,111,110,115,97,98,108,101,0),869]]);
       let connectionm = 4.0;
       let collectionQ = String.fromCharCode(102,111,110,116,115,0);
      while (parseInt(`${connectionm}`) == footballfieldK.size) {
         connectionm *= parseFloat(`${collectionQ.length % 1}`);
         break;
      }
      if (5 > (collectionQ.length - parseInt(`${connectionm}`)) && (5 << (Math.min(5, collectionQ.length))) > 4) {
         connectionm -= (parseFloat(`${String.fromCharCode(83,0) == collectionQ ? main_g5.length : collectionQ.length}`));
      }
      let securityz = collectionQ.length <= 5625139;
      do {
         collectionQ = `${2 - collectionQ.length}`;
         if (securityz) {
            break;
         }
      } while (securityz && (collectionQ.length > 4));
         footballfieldK.set(`${connectionm}`, parseInt(`${connectionm}`));
         connectionm += parseFloat(`${main_g5.length}`);
         collectionQ = `${detailsm.length}`;
      if (footballfieldK.get(`${connectionm}`) == null) {
         connectionm -= parseFloat(`${main_g5.length >> (Math.min(Math.abs(2), 5))}`);
      }
      for (let a = 0; a < 3; a++) {
         connectionm *= parseFloat(`${3}`);
      }
          let sportF = String.fromCharCode(102,111,108,108,111,119,115,0);
          let chatl = String.fromCharCode(102,105,120,0);
         connectionm += parseFloat(`${2}`);
         sportF = `${chatl.length}`;
         chatl += `${sportF.length}`;
      for (let n = 0; n < 2; n++) {
         main_g5 += `${3 + parseInt(`${connectionm}`)}`;
      }
      let b_positionG = String.fromCharCode(98,51,106,110,103,57,53,53,0) == main_g5;
      do {
          let predictionwin3 = String.fromCharCode(115,119,102,112,108,97,121,101,114,0);
         main_g5 += `${detailsm.length | 1}`;
         predictionwin3 += `${predictionwin3.length}`;
         if (b_positionG) {
            break;
         }
      } while ((3 < footballfieldK.size) && b_positionG);
          let gdtadvD = 5.0;
         detailsm += `${(collectionQ == String.fromCharCode(54,0) ? collectionQ.length : main_g5.length)}`;
         gdtadvD += parseInt(`${gdtadvD}`);
      let whistleorangeS = collectionQ.length >= 9081624;
      do {
         collectionQ += `${1 - parseInt(`${connectionm}`)}`;
         if (whistleorangeS) {
            break;
         }
      } while ((!collectionQ.endsWith(`${footballfieldK.size}`)) && whistleorangeS);
      while (1 < (footballfieldK.size >> (Math.min(collectionQ.length, 4))) && 5 < (collectionQ.length >> (Math.min(Math.abs(1), 4)))) {
          let textu = String.fromCharCode(98,117,108,107,0);
         footballfieldK.set(textu, main_g5.length * textu.length);
         break;
      }
       let overlayJ = true;
       let final_kh = false;
      profileframeV -= parseFloat(`${collectionQ.length * 1}`);
   }
   while (currentR.length <= bellC.length) {
       let actionsP = String.fromCharCode(102,117,122,122,121,0);
       let iconuserz = String.fromCharCode(102,108,97,116,116,101,110,101,100,0);
       let diceJ: Map<any, any> = new Map([[String.fromCharCode(97,99,114,111,115,115,0),127], [String.fromCharCode(104,97,100,97,109,97,114,100,0),257], [String.fromCharCode(112,97,110,111,114,97,109,97,0),588]]);
       let nterstitialA = String.fromCharCode(109,105,115,115,101,100,0);
         actionsP += `${diceJ.size % (Math.max(1, 4))}`;
         actionsP = `${(String.fromCharCode(57,0) == nterstitialA ? diceJ.size : nterstitialA.length)}`;
      while (iconuserz.length < 4 && 4 < actionsP.length) {
          let halffieldimage1: Array<any> = [878, 410];
          let minivody = 5;
          let iconsettinga = String.fromCharCode(104,105,116,115,0);
          let whistleq: Map<any, any> = new Map([[String.fromCharCode(117,109,98,114,101,108,108,97,0),997], [String.fromCharCode(99,95,54,49,95,118,97,114,105,97,110,116,115,0),657], [String.fromCharCode(112,103,109,121,117,118,0),195]]);
          let stringsT = 2.0;
         iconuserz += `${minivody}`;
         halffieldimage1.push(parseInt(`${stringsT}`) & iconsettinga.length);
         minivody -= 3;
         iconsettinga = `${iconsettinga.length}`;
         whistleq.set(`${iconsettinga}`, whistleq.size % 3);
         stringsT *= parseFloat(`${whistleq.size}`);
         break;
      }
          let whiteH = false;
          let predictionbannersharedK = String.fromCharCode(120,105,112,104,0);
         iconuserz += "3";
         whiteH = (predictionbannersharedK.length + predictionbannersharedK.length) == 91;
          let x_playerW = 1.0;
          let toponk = String.fromCharCode(118,97,114,105,110,116,0);
          let mapbufferb: Array<any> = [374, 614];
         actionsP = "2";
         x_playerW *= parseFloat(`${parseInt(`${x_playerW}`) | mapbufferb.length}`);
         toponk = `${mapbufferb.length >> (Math.min(Math.abs(3), 2))}`;
      while (actionsP.startsWith(`${diceJ.size}`)) {
         actionsP = `${iconuserz.length}`;
         break;
      }
       let networku: Array<any> = [535, 590, 751];
      for (let z = 0; z < 3; z++) {
         nterstitialA = `${networku.length & nterstitialA.length}`;
      }
       let sourcep: Map<any, any> = new Map([[String.fromCharCode(105,115,116,115,0),true ], [String.fromCharCode(100,121,97,100,105,99,0),true ], [String.fromCharCode(114,101,99,101,110,116,0),true ]]);
      let libturbomodulejsijniR = sourcep.size >= 9139127;
      do {
          let nalyticsk: Array<any> = [String.fromCharCode(103,117,105,100,101,115,0), String.fromCharCode(115,115,108,114,111,111,116,115,0)];
          let defaultfootballbgd = String.fromCharCode(97,110,100,0);
          let codeB = String.fromCharCode(109,111,110,111,116,111,110,105,99,0);
         sourcep.set(`${networku.length}`, sourcep.size);
         nalyticsk.push(defaultfootballbgd.length);
         defaultfootballbgd += "1";
         codeB = `${codeB.length << (Math.min(Math.abs(2), 4))}`;
         if (libturbomodulejsijniR) {
            break;
         }
      } while (libturbomodulejsijniR && (sourcep.get(`${networku.length}`) != null));
      let libtanu = sourcep.size >= 5576449;
      do {
         sourcep.set(nterstitialA, (String.fromCharCode(90,0) == nterstitialA ? iconuserz.length : nterstitialA.length));
         if (libtanu) {
            break;
         }
      } while (libtanu && ((sourcep.size / 1) < 5));
      let collection4 = nterstitialA.length <= 9724273;
      do {
         nterstitialA += `${2 ^ actionsP.length}`;
         if (collection4) {
            break;
         }
      } while ((iconuserz.length > nterstitialA.length) && collection4);
      bellC = `${1 - parseInt(`${unreadM}`)}`;
      break;
   }
   for (let d = 0; d < 2; d++) {
      profileframeV /= Math.max(parseFloat(`${parseInt(`${unreadM}`)}`), 2);
   }
      away9 = [away9.length % (Math.max(1, 10))];
   if ((bellC.length % (Math.max(5, 6))) > 4) {
      bellC += "1";
   }
          await AsyncStorage.setItem('firstLaunchVipPrompt', 'true');
        }
      } catch (e) { }
    };

    checkFirstLaunchVipPrompted();
  }, []);

  const { colors, textVariants, spacing, icons } = useTheme();

  return (
    <>
      {showVIPOverlay && (
        <View
          style={{
            height: '100%',
            width: '100%',
            backgroundColor: 'rgba(0, 0, 0, 0.8)',
            position: 'absolute',
          }}>
          <VipModal>
            <View
              style={{
                backgroundColor: 'rgba(34, 34, 34, 0.9)',
                marginTop: 40,
                borderRadius: 12,
                paddingTop: 36,
                paddingBottom: 26,
                paddingHorizontal: 28,
              }}>
              <View style={{ alignItems: 'center', justifyContent: 'center' }}>
                <Text
                  style={{
                    fontSize: 20,
                    fontWeight: '500',
                    color: '#E2820E',
                    paddingTop: 20,
                    paddingVertical: 12,
                  }}>
                  VIP会员奖励，等你来领取！
                </Text>
                <Text
                  style={{
                    color: 'white',
                    padding: 2,
                    fontSize: 16,
                    fontWeight: '300',
                  }}>
                  受邀用户通过邀请码注册即可
                </Text>
                <Text
                  style={{
                    color: 'white',
                    padding: 2,
                    fontSize: 16,
                    fontWeight: '300',
                  }}>
                  领取30天VIP会员
                </Text>
                <View style={{ paddingTop: 22, alignItems: 'center' }}>
                  <TouchableOpacity
                    onPress={() => {
                      hideVipPrompt();
                      setTimeout(() => { 
                        dispatch(showLoginAction());
                      }, 50);
                    }}
                    style={{
                      backgroundColor: '#FAC33D',
                      paddingVertical: 12,
                      paddingHorizontal: 38,
                      borderRadius: 8,
                    }}>
                    <Text
                      style={{
                        color: colors.text,
                        fontWeight: '600',
                        fontSize: 16,
                      }}>
                      立即领取
                    </Text>
                  </TouchableOpacity>
                  <TouchableOpacity
                    onPress={() => hideVipPrompt()}
                    style={{ paddingTop: 16 }}>
                    <Text style={{ color: '#9C9C9C', fontSize: 16 }}>取消</Text>
                  </TouchableOpacity>
                </View>
              </View>
            </View>
          </VipModal>
        </View>
      )}
    </>
  );
}
