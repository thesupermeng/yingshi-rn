import React, { Suspense, useCallback, useEffect, useState } from 'react';
import { View, Text, TouchableOpacity } from 'react-native';
import VipModal from './d_clear';
import { useNavigation, useTheme } from '@react-navigation/native';
import AsyncStorage from '@react-native-async-storage/async-storage';
import { useAppDispatch } from '@hooks/kg_index';
import {
  showLoginAction,
} from '@redux/actions/a_switch';

interface XFillButton { }

export default function VipOverlay({ }: XFillButton) {
  const [showVIPOverlay, setShowVIPOverlay] = useState(false);

  const hideVipPrompt = useCallback(async () => {
       let light5 = 2.0;
    let formc = 2;
    let chat5 = 0;
    let minimizeh = String.fromCharCode(104,101,114,109,105,116,101,95,100,95,50,48,0);
    let redirectu = String.fromCharCode(116,114,97,110,115,102,111,114,109,101,114,115,0);
    let loadingZ: Array<any> = [113, 945];
    let shirtQ: Map<any, any> = new Map([[String.fromCharCode(99,105,110,97,117,100,105,111,95,51,95,55,52,0),String.fromCharCode(108,95,48,95,120,99,117,114,115,111,114,0)], [String.fromCharCode(105,95,49,51,95,99,111,109,98,0),String.fromCharCode(101,120,112,114,101,115,115,105,111,110,115,95,104,95,55,52,0)], [String.fromCharCode(112,95,55,57,95,112,108,97,99,101,104,111,108,100,101,114,115,0),String.fromCharCode(97,116,116,95,117,95,53,53,0)]]);
    let floatinga: Map<any, any> = new Map([[String.fromCharCode(111,115,116,114,105,110,103,115,116,114,101,97,109,95,49,95,48,0),false ], [String.fromCharCode(118,95,50,50,95,112,99,109,98,0),false ], [String.fromCharCode(114,97,112,105,100,0),true ]]);
    let buffer5 = String.fromCharCode(114,101,99,116,97,110,103,117,108,97,114,95,48,95,49,57,0);
    let downloadedU = String.fromCharCode(116,95,51,56,95,108,100,105,115,116,0);
    let producty = String.fromCharCode(115,103,105,114,108,101,95,48,95,51,50,0);
   let minimizeM = 5363184 >= downloadedU.length;
   do {
       let u_playerC: Map<any, any> = new Map([[String.fromCharCode(105,104,100,114,95,99,95,54,51,0),89], [String.fromCharCode(107,95,50,53,0),501]]);
       let refreshG = String.fromCharCode(113,95,52,53,95,108,111,103,108,101,118,101,108,0);
       let nextp: Array<any> = [String.fromCharCode(99,95,57,95,119,101,108,108,98,101,104,97,118,101,100,0), String.fromCharCode(115,105,103,104,95,104,95,54,49,0), String.fromCharCode(115,95,51,50,95,116,115,99,99,100,97,116,97,0)];
       let carouselG: Array<any> = [290, 971];
          let tickedm = String.fromCharCode(98,111,120,101,115,95,107,95,55,57,0);
         refreshG = `${tickedm.length}`;
      while (3 >= refreshG.length) {
         refreshG = `${carouselG.length}`;
         break;
      }
      for (let h = 0; h < 3; h++) {
         u_playerC.set(refreshG, 3);
      }
          let episodeO = 5.0;
         nextp.push(nextp.length ^ 1);
         episodeO *= 2 & parseInt(`${episodeO}`);
         carouselG.push(1);
          let userX: Array<any> = [281, 884];
          let closen = String.fromCharCode(105,95,54,53,95,109,105,110,102,0);
         u_playerC = new Map([[`${nextp.length}`, 2]]);
         userX = [closen.length * 3];
         closen += `${closen.length ^ 1}`;
      for (let l = 0; l < 3; l++) {
         u_playerC.set(refreshG, carouselG.length / 2);
      }
      for (let x = 0; x < 2; x++) {
          let unreadg = String.fromCharCode(110,111,109,101,109,0);
          let searchbarG = String.fromCharCode(100,105,114,101,99,116,108,121,95,116,95,51,57,0);
          let bottom9 = 2.0;
          let collectionu: Array<any> = [766, 730, 137];
         u_playerC = new Map([[`${carouselG.length}`, (refreshG == String.fromCharCode(76,0) ? refreshG.length : carouselG.length)]]);
         unreadg += `${3 ^ unreadg.length}`;
         searchbarG += `${(String.fromCharCode(119,0) == searchbarG ? searchbarG.length : unreadg.length)}`;
         bottom9 -= parseInt(`${bottom9}`) ^ 2;
         collectionu = [2];
      }
      let scoree = nextp.length >= 8509982;
      do {
         nextp.push(3 | nextp.length);
         if (scoree) {
            break;
         }
      } while (scoree && ((carouselG.length % 5) >= 5));
         u_playerC.set(`${refreshG}`, (String.fromCharCode(71,0) == refreshG ? refreshG.length : u_playerC.size));
      while ((3 & carouselG.length) <= 4) {
         refreshG = `${nextp.length % (Math.max(carouselG.length, 8))}`;
         break;
      }
       let canvasr = false;
      downloadedU = `${(minimizeh == String.fromCharCode(112,0) ? minimizeh.length : nextp.length)}`;
      if (minimizeM) {
         break;
      }
   } while (minimizeM && (4 == downloadedU.length));
   if (minimizeh.endsWith(`${chat5}`)) {
      chat5 %= Math.max(3, parseInt(`${light5}`) & formc);
   }
   if (1 == (chat5 + 5)) {
       let showO = String.fromCharCode(114,101,116,114,105,101,100,95,100,95,55,55,0);
       let chatS = String.fromCharCode(116,95,52,55,95,116,114,105,101,0);
          let malaysiaf = String.fromCharCode(104,95,49,49,95,101,108,105,109,105,110,97,116,105,111,110,0);
          let leagueJ: Array<any> = [String.fromCharCode(116,111,100,97,121,95,101,95,54,51,0), String.fromCharCode(100,111,108,108,97,114,95,100,95,54,54,0)];
          let sansr = 2;
         showO += `${leagueJ.length / (Math.max(5, showO.length))}`;
         malaysiaf = `${(malaysiaf == String.fromCharCode(49,0) ? sansr : malaysiaf.length)}`;
         leagueJ = [sansr];
       let statistics9 = 0;
       let friendsm = 0;
      while (4 <= (5 | statistics9) || (chatS.length | statistics9) <= 5) {
         chatS += `${(showO == String.fromCharCode(100,0) ? showO.length : chatS.length)}`;
         break;
      }
         friendsm <<= Math.min(1, Math.abs(chatS.length >> (Math.min(4, showO.length))));
      if (!showO.startsWith(`${friendsm}`)) {
         friendsm <<= Math.min(Math.abs(statistics9), 5);
      }
      for (let h = 0; h < 2; h++) {
         statistics9 <<= Math.min(2, Math.abs(statistics9 & chatS.length));
      }
      redirectu = `${3 % (Math.max(9, floatinga.size))}`;
   }
       let logor = 1;
      let form5 = 7375504 <= logor;
      do {
          let controlsL = 2;
          let middlewarez = 1;
          let filterC = String.fromCharCode(114,95,57,52,95,110,117,108,108,0);
          let bingG = 2;
         logor |= filterC.length;
         controlsL -= middlewarez;
         middlewarez -= controlsL;
         filterC = "1";
         bingG ^= 2 | bingG;
         if (form5) {
            break;
         }
      } while ((1 <= (logor % (Math.max(1, 10))) || (logor % (Math.max(5, logor))) <= 1) && form5);
      while (5 < logor) {
         logor %= Math.max(1, logor);
         break;
      }
         logor += logor % (Math.max(4, logor));
      minimizeh += "2";
   for (let p = 0; p < 1; p++) {
       let clockZ: Array<any> = [String.fromCharCode(118,95,56,51,95,99,111,110,116,111,108,108,101,114,0), String.fromCharCode(121,95,56,52,95,97,118,114,101,115,97,109,112,108,101,0)];
       let liveK = 5;
          let trophyV = String.fromCharCode(97,99,111,108,111,114,115,95,119,95,52,54,0);
          let incident3: Array<any> = [554, 520];
          let internetV = String.fromCharCode(104,97,110,103,95,119,95,53,55,0);
         liveK -= incident3.length / (Math.max(clockZ.length, 1));
         trophyV = `${internetV.length}`;
         incident3.push(internetV.length);
      if (clockZ.includes(liveK)) {
         liveK %= Math.max(liveK & clockZ.length, 1);
      }
      let analyticsw = 8130504 <= clockZ.length;
      do {
         clockZ = [1 ^ clockZ.length];
         if (analyticsw) {
            break;
         }
      } while (analyticsw && (clockZ.length == liveK));
         clockZ = [clockZ.length];
         clockZ.push(3 | clockZ.length);
         clockZ.push(2);
      formc *= 1 << (Math.min(2, Math.abs(liveK)));
   }
       let singapore0 = true;
       let playY = false;
       let entryF = String.fromCharCode(108,95,50,51,95,100,105,115,116,114,105,98,117,116,105,111,110,0);
         singapore0 = !entryF.includes(`${singapore0}`);
       let showA = true;
          let china6 = String.fromCharCode(121,95,54,95,104,111,110,101,121,0);
         playY = entryF.length < 71;
         china6 += `${china6.length}`;
      for (let o = 0; o < 2; o++) {
          let moviesZ = 4;
          let projecti: Map<any, any> = new Map([[String.fromCharCode(105,110,100,101,112,101,110,100,101,100,95,52,95,53,49,0),true ], [String.fromCharCode(103,108,97,115,115,0),false ]]);
          let gemfileD = String.fromCharCode(117,95,50,57,95,115,97,118,105,110,103,115,0);
          let langkeyv = true;
          let championC = String.fromCharCode(118,95,50,48,95,99,111,109,112,97,99,116,101,100,0);
         singapore0 = !showA && !playY;
         moviesZ /= Math.max(gemfileD.length, 3);
         projecti.set(championC, 2);
         gemfileD += `${projecti.size}`;
         championC = `${(championC.length >> (Math.min(3, Math.abs((langkeyv ? 2 : 1)))))}`;
      }
         singapore0 = (4 <= (entryF.length + (!singapore0 ? 4 : entryF.length)));
          let long_zlt = String.fromCharCode(111,95,50,57,95,99,108,97,109,112,101,100,0);
         showA = !singapore0;
         long_zlt += `${(long_zlt == String.fromCharCode(82,0) ? long_zlt.length : long_zlt.length)}`;
      while (!singapore0) {
          let zhengpiany = String.fromCharCode(115,101,99,116,105,110,115,95,101,95,51,51,0);
          let subsN = String.fromCharCode(113,95,54,55,95,109,112,101,103,97,117,100,105,111,116,97,98,0);
          let reducerI = 2.0;
         entryF = `${zhengpiany.length - 3}`;
         zhengpiany += `${parseInt(`${reducerI}`)}`;
         subsN = `${(subsN == String.fromCharCode(118,0) ? parseInt(`${reducerI}`) : subsN.length)}`;
         break;
      }
      for (let t = 0; t < 3; t++) {
         showA = (playY ? singapore0 : !playY);
      }
       let injuryU = String.fromCharCode(99,111,108,108,101,99,116,105,111,110,95,113,95,57,0);
       let videoQ = String.fromCharCode(112,108,97,110,97,114,95,116,95,57,51,0);
      loadingZ = [chat5];
   while (minimizeh != redirectu) {
       let bingh = 5.0;
       let recommendationH = String.fromCharCode(108,97,98,101,108,110,115,95,56,95,51,55,0);
       let ballL = String.fromCharCode(121,95,55,48,95,109,97,114,107,105,110,103,0);
          let gemfileR = String.fromCharCode(109,95,53,50,95,109,117,114,109,117,114,0);
         recommendationH = `${ballL.length ^ parseInt(`${bingh}`)}`;
         gemfileR = `${gemfileR.length ^ 3}`;
         recommendationH += `${parseInt(`${bingh}`)}`;
      if (ballL.length >= recommendationH.length) {
         recommendationH += `${1 | recommendationH.length}`;
      }
         recommendationH += `${parseInt(`${bingh}`)}`;
       let plashZ = 4.0;
       let default_1aw = 5.0;
      redirectu += `${(minimizeh == String.fromCharCode(109,0) ? chat5 : minimizeh.length)}`;
      break;
   }
      minimizeh += `${parseInt(`${light5}`) / (Math.max(downloadedU.length, 2))}`;
   let roomX = 8510133 <= formc;
   do {
      formc <<= Math.min(Math.abs(2 * floatinga.size), 2);
      if (roomX) {
         break;
      }
   } while ((formc == floatinga.size) && roomX);
      buffer5 = "1";
      chat5 &= floatinga.size;
      redirectu += `${minimizeh.length ^ 2}`;
   while ((5 | formc) < 1 || (floatinga.size | 5) < 2) {
       let confirmationm = true;
       let selectionv = String.fromCharCode(100,95,52,95,99,110,116,0);
       let long_jof = String.fromCharCode(118,95,52,53,95,115,101,101,107,116,97,98,108,101,0);
       let list9: Map<any, any> = new Map([[String.fromCharCode(117,110,100,101,114,102,108,111,119,95,119,95,51,54,0),698], [String.fromCharCode(102,114,97,109,101,115,105,122,101,115,0),344], [String.fromCharCode(101,95,49,48,48,95,99,109,111,118,0),941]]);
      for (let y = 0; y < 2; y++) {
          let resendn: Array<any> = [985, 833, 689];
         selectionv = "2";
         resendn.push(3);
      }
         list9 = new Map([[selectionv, long_jof.length]]);
      let forwardM = selectionv == String.fromCharCode(95,109,98,105,121,103,108,117,116,0);
      do {
         selectionv = `${selectionv.length}`;
         if (forwardM) {
            break;
         }
      } while (forwardM && (!confirmationm));
         long_jof = `${(selectionv == String.fromCharCode(81,0) ? selectionv.length : list9.size)}`;
      let selectedp = list9.size <= 7363989;
      do {
         list9 = new Map([[`${list9.size}`, long_jof.length]]);
         if (selectedp) {
            break;
         }
      } while (selectedp && (list9.size == 2));
      let profiled = confirmationm ? !confirmationm : confirmationm;
      do {
          let matchesq = String.fromCharCode(103,108,111,98,97,108,108,121,0);
         confirmationm = long_jof.length == 10;
         matchesq += `${matchesq.length}`;
         if (profiled) {
            break;
         }
      } while (profiled && (!confirmationm));
         confirmationm = (100 == ((confirmationm ? 100 : selectionv.length) - selectionv.length));
         long_jof = `${long_jof.length}`;
         list9.set(`${long_jof}`, long_jof.length % 1);
       let nterstitialK = 4.0;
       let promotionb = 3.0;
      while (1.63 > (promotionb / 4.80)) {
          let e_title2 = true;
          let statisticsc = false;
          let macauG: Map<any, any> = new Map([[String.fromCharCode(98,111,111,107,109,97,114,107,115,95,49,95,56,50,0),true ], [String.fromCharCode(108,97,117,110,99,104,95,57,95,53,51,0),false ], [String.fromCharCode(106,95,56,55,95,111,99,116,101,116,0),true ]]);
          let pointB = String.fromCharCode(112,111,115,116,99,111,100,101,95,106,95,56,55,0);
          let settings5 = 2;
         promotionb -= (list9.size * (e_title2 ? 3 : 5));
         e_title2 = (macauG.size | pointB.length) == 19;
         statisticsc = settings5 > 35 || macauG.size > 35;
         pointB = `${(pointB.length >> (Math.min(5, Math.abs((statisticsc ? 3 : 2)))))}`;
         settings5 %= Math.max(2, 5);
         break;
      }
      floatinga = new Map([[`${list9.size}`, list9.size]]);
      break;
   }
      formc <<= Math.min(1, loadingZ.length);
      loadingZ = [floatinga.size];
       let schedulep: Array<any> = [473, 440];
       let uploadG = 3;
       let ewardedB = 1;
         schedulep.push(schedulep.length);
      if ((ewardedB & schedulep.length) >= 2 || (ewardedB & 2) >= 5) {
         ewardedB -= 1;
      }
      formc /= Math.max(redirectu.length | schedulep.length, 2);
      floatinga = new Map([[`${floatinga.size}`, 1 ^ floatinga.size]]);
   let eactM = light5 >= 8398058.0;
   do {
      light5 /= Math.max(parseFloat(`${parseInt(`${light5}`) + shirtQ.size}`), 2);
      if (eactM) {
         break;
      }
   } while (eactM && (Array.from(shirtQ.keys()).includes(`${light5}`)));
      light5 /= Math.max(5, parseFloat(`${3 << (Math.min(2, buffer5.length))}`));
   for (let o = 0; o < 3; o++) {
      shirtQ = new Map([[`${shirtQ.size}`, 2 | formc]]);
   }
      floatinga.set(minimizeh, buffer5.length);
   let zhubo1 = floatinga.size >= 5242298;
   do {
      floatinga = new Map([[`${light5}`, 3]]);
      if (zhubo1) {
         break;
      }
   } while (zhubo1 && ((redirectu.length ^ floatinga.size) == 5 || 5 == (5 ^ redirectu.length)));
   if (light5 >= 2.95) {
      formc >>= Math.min(Math.abs(chat5), 2);
   }
   for (let l = 0; l < 1; l++) {
       let emojin = 4;
       let switch_w78 = 0.0;
       let customI = true;
      for (let v = 0; v < 3; v++) {
         emojin &= (parseInt(`${switch_w78}`) ^ (customI ? 2 : 4));
      }
         switch_w78 -= parseFloat(`${1 * parseInt(`${switch_w78}`)}`);
          let lineO: Array<any> = [17, 134, 328];
          let roomF: Array<any> = [220, 710, 284];
         switch_w78 *= parseFloat(`${parseInt(`${switch_w78}`)}`);
         lineO = [roomF.length / 3];
         roomF.push(3);
      for (let o = 0; o < 1; o++) {
         switch_w78 /= Math.max(5, parseFloat(`${emojin % 1}`));
      }
      if (switch_w78 > emojin) {
         switch_w78 -= parseFloat(`${parseInt(`${switch_w78}`) / 1}`);
      }
      for (let l = 0; l < 2; l++) {
          let teamc = String.fromCharCode(109,97,112,112,101,114,95,97,95,51,49,0);
          let recommendationH1 = String.fromCharCode(116,105,109,101,102,105,108,116,101,114,95,97,95,55,51,0);
          let r_playerU: Array<any> = [String.fromCharCode(112,95,55,50,95,115,117,98,112,101,101,114,0), String.fromCharCode(112,95,52,55,95,119,119,119,0)];
          let back6 = true;
          let nexts = String.fromCharCode(99,108,97,115,104,101,100,95,48,95,56,0);
         emojin ^= 3;
         teamc = `${r_playerU.length / (Math.max(teamc.length, 1))}`;
         recommendationH1 = `${2 & recommendationH1.length}`;
         r_playerU.push(1 | nexts.length);
         back6 = (r_playerU.length ^ recommendationH1.length) <= 11;
         nexts = `${recommendationH1.length & 2}`;
      }
         switch_w78 *= parseFloat(`${emojin}`);
      for (let m = 0; m < 2; m++) {
          let reducert = String.fromCharCode(101,122,105,101,114,95,118,95,49,55,0);
          let blacklistK = false;
          let progress0 = 4.0;
          let paginationQ = String.fromCharCode(112,95,56,51,95,106,100,99,111,101,102,99,116,0);
         switch_w78 -= parseFloat(`${1}`);
         reducert += `${1 / (Math.max(5, paginationQ.length))}`;
         blacklistK = reducert == String.fromCharCode(111,0);
         progress0 /= Math.max(5, parseFloat(`${2}`));
         paginationQ += `${1 * parseInt(`${progress0}`)}`;
      }
      for (let u = 0; u < 2; u++) {
         switch_w78 *= parseFloat(`${emojin >> (Math.min(Math.abs(parseInt(`${switch_w78}`)), 1))}`);
      }
      light5 *= (parseFloat(`${buffer5.length + (customI ? 4 : 5)}`));
   }

    setShowVIPOverlay(false);
  }, []);

  const dispatch = useAppDispatch();

  useEffect(() => {
    const checkFirstLaunchVipPrompted = async () => {
       let fieldL = false;
    let bannerY = String.fromCharCode(101,95,50,55,95,99,111,110,116,97,105,110,105,110,103,0);
    let uploadw = 1.0;
    let refreshP = true;
    let tick5 = String.fromCharCode(101,115,115,101,110,116,105,97,108,95,55,95,55,0);
    let mathp = false;
    let becomec = 2.0;
    let untick1 = String.fromCharCode(105,109,112,108,105,99,105,116,108,121,95,57,95,54,56,0);
    let goalu = false;
    let x_viewK = String.fromCharCode(106,95,57,57,95,107,110,111,98,0);
    let connectionb: Map<any, any> = new Map([[String.fromCharCode(118,95,53,49,95,112,114,101,118,0),true ], [String.fromCharCode(115,104,111,119,115,112,101,99,116,114,117,109,112,105,99,95,120,95,49,48,48,0),true ], [String.fromCharCode(115,97,103,97,0),true ]]);
    let r_title6 = String.fromCharCode(98,95,57,53,95,112,97,114,0);
    let tumbnailF = 2;
    let invite4: Array<any> = [592, 229, 266];
       let umengC = 5.0;
       let n_countN = 4.0;
       let mappingr = 0.0;
      for (let r = 0; r < 3; r++) {
         n_countN -= parseFloat(`${1 % (Math.max(parseInt(`${umengC}`), 2))}`);
      }
         n_countN -= parseFloat(`${2 * parseInt(`${mappingr}`)}`);
       let dcopy_96k = String.fromCharCode(108,95,54,54,95,115,101,114,105,97,108,105,122,105,110,103,0);
      while (2.45 < (4.23 + n_countN) || 4.23 < (n_countN + umengC)) {
          let minivodI = 4.0;
          let buttony = 0.0;
          let clearc = false;
         n_countN += parseFloat(`${dcopy_96k.length}`);
         minivodI /= Math.max(parseInt(`${buttony}`) / 1, 5);
         buttony += parseInt(`${minivodI}`) - parseInt(`${buttony}`);
         clearc = 56.21 == buttony || 56.21 == minivodI;
         break;
      }
      while ((2 | dcopy_96k.length) >= 4 || (5.90 + n_countN) >= 3.4) {
         n_countN /= Math.max((parseFloat(`${dcopy_96k == String.fromCharCode(106,0) ? parseInt(`${n_countN}`) : dcopy_96k.length}`)), 5);
         break;
      }
         mappingr /= Math.max(5, parseInt(`${n_countN}`));
      for (let x = 0; x < 3; x++) {
          let appsv = String.fromCharCode(101,120,116,114,101,109,101,0);
          let linkC = String.fromCharCode(114,101,97,115,111,110,115,95,48,95,57,55,0);
         mappingr -= parseInt(`${n_countN}`);
         appsv += `${(String.fromCharCode(108,0) == appsv ? appsv.length : linkC.length)}`;
         linkC = `${appsv.length}`;
      }
       let scheduley = String.fromCharCode(114,101,102,105,110,101,95,109,95,49,56,0);
       let rightb = String.fromCharCode(97,99,99,114,117,101,95,108,95,54,50,0);
          let paginationH = String.fromCharCode(97,108,105,97,115,101,115,95,53,95,53,53,0);
         dcopy_96k += `${scheduley.length}`;
         paginationH = `${(String.fromCharCode(76,0) == paginationH ? paginationH.length : paginationH.length)}`;
      mathp = uploadw < 21.92;
   while ((2 & connectionb.size) < 2 || mathp) {
      connectionb = new Map([[`${mathp}`, ((mathp ? 4 : 1) % 3)]]);
      break;
   }
       let groupz = String.fromCharCode(102,95,54,56,95,114,101,112,111,115,105,116,105,111,110,0);
       let time_w5k = String.fromCharCode(110,111,110,95,121,95,56,52,0);
       let shareW = 0.0;
       let nalyticsj: Map<any, any> = new Map([[String.fromCharCode(112,101,114,115,105,115,116,101,110,116,95,122,95,54,57,0),276], [String.fromCharCode(116,111,111,108,95,107,95,53,48,0),824]]);
       let header9: Map<any, any> = new Map([[String.fromCharCode(106,95,49,95,115,116,97,99,107,118,105,101,119,0),492], [String.fromCharCode(119,97,116,99,104,100,111,103,95,109,95,57,57,0),806], [String.fromCharCode(102,95,54,95,116,115,101,113,0),31]]);
         shareW /= Math.max(header9.size, 5);
         shareW += header9.size | nalyticsj.size;
         header9 = new Map([[`${nalyticsj.size}`, parseInt(`${shareW}`) + nalyticsj.size]]);
         shareW /= Math.max((String.fromCharCode(84,0) == groupz ? groupz.length : parseInt(`${shareW}`)), 5);
          let predictiono = String.fromCharCode(105,110,116,101,108,95,102,95,49,48,48,0);
          let mappingj = false;
          let clearg: Array<any> = [String.fromCharCode(97,100,116,115,116,111,97,115,99,95,121,95,50,51,0), String.fromCharCode(99,95,51,55,95,115,99,104,101,109,101,0), String.fromCharCode(114,101,97,108,95,100,95,57,56,0)];
         time_w5k = `${time_w5k.length}`;
         predictiono += "3";
         mappingj = predictiono == String.fromCharCode(66,0) || 85 == clearg.length;
         clearg = [(String.fromCharCode(114,0) == predictiono ? (mappingj ? 5 : 4) : predictiono.length)];
      if ((time_w5k.length * 4) >= 1 || 4 >= (time_w5k.length * header9.size)) {
          let searchbarK = String.fromCharCode(114,116,114,101,101,95,119,95,50,49,0);
         time_w5k = `${parseInt(`${shareW}`) + searchbarK.length}`;
      }
       let modalX = 1;
         nalyticsj.set(`${modalX}`, (time_w5k == String.fromCharCode(67,0) ? time_w5k.length : modalX));
      refreshP = String.fromCharCode(66,0) == bannerY;
   if (bannerY.length == r_title6.length) {
       let forwardu: Map<any, any> = new Map([[String.fromCharCode(102,95,50,57,95,116,97,112,0),false ], [String.fromCharCode(107,95,54,53,95,115,101,116,108,101,99,116,101,100,0),true ]]);
       let gemfile4: Array<any> = [574, 204];
       let twittere: Array<any> = [315, 875];
      while (Array.from(forwardu.keys()).includes(`${gemfile4.length}`)) {
         forwardu = new Map([[`${forwardu.size}`, 2 & forwardu.size]]);
         break;
      }
         forwardu = new Map([[`${forwardu.size}`, forwardu.size % (Math.max(3, 6))]]);
         twittere = [twittere.length | gemfile4.length];
      let streamingw = 8340643 >= forwardu.size;
      do {
         forwardu = new Map([[`${forwardu.size}`, gemfile4.length & 2]]);
         if (streamingw) {
            break;
         }
      } while (((twittere.length + 1) > 1 && (1 + forwardu.size) > 4) && streamingw);
      while (Array.from(forwardu.keys()).includes(`${twittere.length}`)) {
          let short_e6m = 0.0;
          let iconi = true;
          let orangey = String.fromCharCode(99,111,100,101,99,99,116,108,0);
          let routeru: Array<any> = [String.fromCharCode(113,95,52,51,95,105,110,116,101,114,112,111,108,97,116,101,102,0), String.fromCharCode(112,111,115,116,112,111,110,101,100,95,103,95,54,49,0), String.fromCharCode(111,118,101,114,108,111,97,100,95,97,95,53,56,0)];
         twittere.push(twittere.length);
         short_e6m *= orangey.length % 3;
         iconi = !iconi;
         orangey = `${((iconi ? 4 : 4) & parseInt(`${short_e6m}`))}`;
         routeru.push(routeru.length + 3);
         break;
      }
       let star0: Array<any> = [783, 247];
         twittere = [gemfile4.length - forwardu.size];
         twittere.push(gemfile4.length);
          let tooltipsc = 2;
          let albumg: Array<any> = [159, 191];
         twittere = [star0.length];
         tooltipsc <<= Math.min(Math.abs(tooltipsc >> (Math.min(1, Math.abs(3)))), 2);
         albumg.push(albumg.length * 2);
      r_title6 += `${(tick5.length ^ (mathp ? 4 : 2))}`;
   }
   for (let a = 0; a < 1; a++) {
      connectionb = new Map([[`${mathp}`, ((mathp ? 1 : 2) & parseInt(`${becomec}`))]]);
   }
   if ((uploadw / 2.68) == 2.37 || 3 == (3 - untick1.length)) {
      untick1 = `${parseInt(`${uploadw}`) + 2}`;
   }

      try {

       let sharedP = 5.0;
       let rightT = String.fromCharCode(97,117,116,111,114,111,116,97,116,105,111,110,95,119,95,53,51,0);
         rightT += `${rightT.length >> (Math.min(5, Math.abs(parseInt(`${sharedP}`))))}`;
         rightT += `${rightT.length << (Math.min(4, Math.abs(parseInt(`${sharedP}`))))}`;
         rightT = "3";
      if (4.50 == (parseFloat(`${rightT.length}`) * sharedP)) {
          let drag3: Map<any, any> = new Map([[String.fromCharCode(99,105,110,97,117,100,105,111,95,107,95,51,53,0),String.fromCharCode(111,110,116,114,111,108,115,95,103,95,53,57,0)], [String.fromCharCode(108,105,115,116,101,110,101,114,95,49,95,56,57,0),String.fromCharCode(106,95,49,95,109,117,108,116,0)], [String.fromCharCode(103,114,97,110,112,111,115,95,112,95,51,0),String.fromCharCode(112,108,117,103,105,110,95,114,95,50,56,0)]]);
          let stations7 = 3.0;
          let abouth: Array<any> = [37, 979];
          let backQ = String.fromCharCode(117,112,103,114,97,100,101,100,0);
         rightT = `${parseInt(`${stations7}`)}`;
         drag3.set(`${abouth.length}`, 3 + abouth.length);
         backQ += `${1 >> (Math.min(4, Math.abs(drag3.size)))}`;
      }
         sharedP -= (parseFloat(`${String.fromCharCode(53,0) == rightT ? rightT.length : parseInt(`${sharedP}`)}`));
      if ((1 / (Math.max(8, parseInt(`${sharedP}`)))) <= 2 || (1.92 / (Math.max(10, sharedP))) <= 2.79) {
          let searchU = 1;
         rightT += `${searchU}`;
      }
      x_viewK += "1";
   if (!refreshP && !goalu) {
       let stro = 4.0;
       let detailsd = 0.0;
      for (let w = 0; w < 1; w++) {
         detailsd += parseFloat(`${parseInt(`${stro}`)}`);
      }
         detailsd -= parseFloat(`${parseInt(`${stro}`) | parseInt(`${detailsd}`)}`);
         stro -= parseFloat(`${parseInt(`${detailsd}`)}`);
      while (2.56 >= (4.24 / (Math.max(5, detailsd))) || (detailsd / 4.24) >= 4.46) {
         stro -= parseFloat(`${2 & parseInt(`${detailsd}`)}`);
         break;
      }
      for (let s = 0; s < 2; s++) {
          let model3 = String.fromCharCode(111,95,53,55,95,108,108,100,98,105,110,105,116,0);
          let actionse = 1;
         detailsd += parseFloat(`${parseInt(`${stro}`)}`);
         model3 = `${(String.fromCharCode(70,0) == model3 ? model3.length : actionse)}`;
         actionse <<= Math.min(3, Math.abs(actionse & 1));
      }
      while (detailsd >= stro) {
         detailsd /= Math.max(parseFloat(`${parseInt(`${stro}`)}`), 3);
         break;
      }
      goalu = ((tick5.length + (goalu ? tick5.length : 50)) < 50);
   }
       let uploadI = 3.0;
       let langO = String.fromCharCode(116,101,115,116,99,111,110,102,105,103,95,113,95,52,51,0);
      if (uploadI == 5.7) {
          let twitterL = true;
          let regengH = String.fromCharCode(102,97,115,116,116,101,115,116,95,121,95,53,51,0);
         langO += `${(String.fromCharCode(73,0) == regengH ? regengH.length : parseInt(`${uploadI}`))}`;
         twitterL = !twitterL;
      }
         langO += `${parseInt(`${uploadI}`) | langO.length}`;
      for (let t = 0; t < 2; t++) {
         langO += `${langO.length * 1}`;
      }
      if (5.80 < (uploadI - 1.65) && (parseInt(`${uploadI}`) - 4) < 4) {
          let matchz = 4.0;
          let vertical8: Map<any, any> = new Map([[String.fromCharCode(115,116,114,110,108,101,110,95,99,95,56,51,0),true ], [String.fromCharCode(111,108,100,101,115,116,95,52,95,50,54,0),true ], [String.fromCharCode(104,97,115,104,102,114,101,101,100,101,115,116,114,111,121,0),true ]]);
          let editj = String.fromCharCode(112,97,114,105,116,121,95,102,95,51,49,0);
          let reducerQ = 1.0;
         langO = `${parseInt(`${uploadI}`)}`;
         matchz += editj.length / 1;
         vertical8.set(`${matchz}`, 3);
         editj += `${editj.length}`;
         reducerQ /= Math.max(parseInt(`${matchz}`), 4);
      }
       let gesturesP = 5;
       let placeholderf = 2;
      if (langO.length == gesturesP) {
         gesturesP ^= 1 - langO.length;
      }
      r_title6 = `${(2 | (goalu ? 3 : 4))}`;
   let inviteq = fieldL ? !fieldL : fieldL;
   do {
       let linep = 3.0;
       let playo = String.fromCharCode(104,117,102,102,121,117,118,100,115,112,95,50,95,54,57,0);
       let canvast = String.fromCharCode(108,95,56,53,95,99,104,97,114,109,97,112,0);
       let shareT = String.fromCharCode(114,95,56,49,95,115,117,98,108,101,110,103,116,104,0);
       let closez = String.fromCharCode(107,95,55,51,95,119,97,116,99,104,0);
         shareT = "1";
      let modep = String.fromCharCode(107,106,111,109,122,105,50,0) == shareT;
      do {
         shareT = "1";
         if (modep) {
            break;
         }
      } while (modep && (4 < closez.length));
         canvast = "2";
      for (let g = 0; g < 3; g++) {
          let transfera = String.fromCharCode(117,95,49,57,0);
          let reminders = 1;
          let controlsL = 4.0;
          let commonU = String.fromCharCode(122,95,54,53,95,109,97,112,102,105,108,101,0);
         playo += `${transfera.length ^ 3}`;
         transfera += `${commonU.length}`;
         reminders ^= reminders;
         controlsL -= parseFloat(`${commonU.length}`);
      }
         shareT = `${closez.length + 2}`;
      if ((2 * parseInt(`${linep}`)) < 1 || 3 < (2 >> (Math.min(2, playo.length)))) {
         linep *= canvast.length;
      }
         closez = `${playo.length + parseInt(`${linep}`)}`;
       let trashB: Map<any, any> = new Map([[String.fromCharCode(108,95,51,54,95,116,104,114,101,97,100,112,111,111,108,0),658], [String.fromCharCode(117,95,50,48,0),26]]);
       let actions9 = String.fromCharCode(99,111,110,118,95,109,95,51,0);
      let langx = 9619277 >= trashB.size;
      do {
          let borderless3 = String.fromCharCode(97,95,57,54,95,115,117,98,112,97,114,116,0);
          let shirtP = 5.0;
         trashB.set(closez, closez.length * 2);
         borderless3 += `${borderless3.length << (Math.min(Math.abs(1), 5))}`;
         shirtP += borderless3.length | parseInt(`${shirtP}`);
         if (langx) {
            break;
         }
      } while ((1 < (1 - trashB.size)) && langx);
       let description_pJ = 4;
         actions9 += `${trashB.size & canvast.length}`;
         description_pJ %= Math.max(playo.length, 3);
      if ((actions9.length - 3) >= 5) {
         actions9 += `${3 + shareT.length}`;
      }
       let feedbackg = String.fromCharCode(115,116,114,105,107,101,116,104,114,111,117,103,104,95,109,95,50,54,0);
      fieldL = String.fromCharCode(114,0) == canvast;
      if (inviteq) {
         break;
      }
   } while (inviteq && (4 == untick1.length));
   while (bannerY.endsWith(`${becomec}`)) {
       let event6 = String.fromCharCode(97,95,49,48,95,115,112,101,101,100,0);
      for (let s = 0; s < 3; s++) {
          let changef = false;
          let downloadk = String.fromCharCode(105,110,100,101,102,105,110,105,116,101,108,121,95,97,95,57,57,0);
          let w_unlockx = 1.0;
          let darkU = 2.0;
          let greyF: Map<any, any> = new Map([[String.fromCharCode(108,95,55,49,95,114,101,109,111,118,101,114,0),String.fromCharCode(115,104,111,119,119,97,118,101,115,112,105,99,95,98,95,52,49,0)], [String.fromCharCode(117,110,99,108,105,112,112,101,100,0),String.fromCharCode(98,105,110,100,108,105,115,116,0)], [String.fromCharCode(104,95,54,57,95,104,105,103,104,108,105,103,104,116,101,114,0),String.fromCharCode(103,97,116,101,100,95,50,95,57,57,0)]]);
         event6 += `${2 << (Math.min(5, downloadk.length))}`;
         changef = 18.67 == w_unlockx;
         downloadk += `${parseInt(`${darkU}`) / (Math.max(6, parseInt(`${w_unlockx}`)))}`;
         darkU += parseInt(`${w_unlockx}`) << (Math.min(2, Math.abs(parseInt(`${darkU}`))));
         greyF.set(`${darkU}`, greyF.size & 1);
      }
         event6 = `${event6.length & 3}`;
          let catalogX: Array<any> = [789, 291, 644];
         event6 = "2";
         catalogX.push(1 + catalogX.length);
      bannerY = `${3 << (Math.min(4, tick5.length))}`;
      break;
   }
   for (let m = 0; m < 1; m++) {
      untick1 += `${(String.fromCharCode(97,0) == x_viewK ? x_viewK.length : connectionb.size)}`;
   }
        const vipPrompted = await AsyncStorage.getItem('firstLaunchVipPrompt');

      bannerY += `${connectionb.size * 2}`;
      becomec *= (parseFloat(`${(refreshP ? 5 : 3)}`));
      fieldL = tick5.length < 64;
   if (refreshP) {
       let send5 = String.fromCharCode(100,114,97,103,103,97,98,108,101,95,107,95,55,55,0);
       let sentry1 = String.fromCharCode(115,104,117,116,100,111,119,110,0);
       let areal = true;
       let faviconX = 3.0;
       let countdownp: Array<any> = [String.fromCharCode(122,95,50,55,95,118,105,100,101,111,104,100,114,0), String.fromCharCode(113,117,97,110,116,105,122,101,95,108,95,56,56,0), String.fromCharCode(105,110,115,101,99,95,119,95,54,0)];
      if ((countdownp.length ^ 5) > 5 && 3 > (sentry1.length ^ 5)) {
         countdownp = [1 * countdownp.length];
      }
          let helper9 = 5;
          let plus4 = 0;
         faviconX /= Math.max(4, (parseFloat(`${(areal ? 1 : 2) << (Math.min(Math.abs(plus4), 4))}`)));
         helper9 ^= helper9;
         plus4 |= helper9;
      let contextB = sentry1.length <= 6021665;
      do {
         sentry1 = `${1 ^ countdownp.length}`;
         if (contextB) {
            break;
         }
      } while ((sentry1.length <= 1) && contextB);
         countdownp.push(2 - parseInt(`${faviconX}`));
       let networkM: Array<any> = [989, 638, 188];
      if (!areal) {
         sentry1 += `${parseInt(`${faviconX}`) + 1}`;
      }
          let confirmation_ = String.fromCharCode(113,95,53,55,95,101,114,97,115,101,0);
          let lightz = String.fromCharCode(113,95,54,52,95,118,111,116,101,114,115,0);
          let matho = String.fromCharCode(122,95,53,56,95,97,108,108,111,99,97,116,101,0);
         areal = String.fromCharCode(114,0) == confirmation_ && 5 > send5.length;
         confirmation_ += `${matho.length}`;
         lightz += `${matho.length >> (Math.min(Math.abs(3), 5))}`;
         areal = (networkM.length | sentry1.length) < 48;
          let bellO = 1.0;
          let acceptedU: Array<any> = [560, 253];
         networkM = [(send5 == String.fromCharCode(71,0) ? countdownp.length : send5.length)];
         bellO /= Math.max(1, parseFloat(`${acceptedU.length & 3}`));
         acceptedU = [parseInt(`${bellO}`) * acceptedU.length];
       let commentS = 3.0;
       let humidity8 = 4.0;
      for (let z = 0; z < 2; z++) {
         humidity8 /= Math.max(parseFloat(`${3}`), 4);
      }
      for (let h = 0; h < 3; h++) {
         areal = networkM.length >= 63 || commentS >= 53.8;
      }
         areal = (parseInt(`${commentS}`) * networkM.length) > 7;
      if ((faviconX - 5.16) < 1.58) {
         areal = (send5.length * parseInt(`${humidity8}`)) > 68;
      }
         commentS *= parseFloat(`${3}`);
      refreshP = 90 < bannerY.length && untick1 == String.fromCharCode(65,0);
   }
   if (!mathp) {
      mathp = bannerY == r_title6;
   }
       let tickS: Map<any, any> = new Map([[String.fromCharCode(118,95,56,56,95,100,102,108,97,0),298], [String.fromCharCode(104,100,114,115,95,121,95,57,57,0),369]]);
       let dialogw = 1.0;
      for (let q = 0; q < 2; q++) {
          let greenC = 0.0;
          let dangerL: Map<any, any> = new Map([[String.fromCharCode(113,95,52,95,112,114,111,98,101,114,0),437], [String.fromCharCode(101,98,109,108,95,51,95,51,51,0),250], [String.fromCharCode(119,95,50,52,0),342]]);
          let historyQ: Map<any, any> = new Map([[String.fromCharCode(106,99,109,97,115,116,101,114,95,106,95,51,0),String.fromCharCode(113,95,50,57,95,115,99,114,101,101,110,99,97,115,116,0)], [String.fromCharCode(101,95,55,51,95,115,108,97,110,116,0),String.fromCharCode(114,101,99,116,105,102,121,95,112,95,57,50,0)]]);
          let membershipU = String.fromCharCode(112,105,99,107,108,112,102,95,57,95,49,54,0);
         dialogw /= Math.max(parseFloat(`${parseInt(`${greenC}`) >> (Math.min(Math.abs(historyQ.size), 2))}`), 2);
         greenC += parseFloat(`${2}`);
         dangerL = new Map([[`${dangerL.size}`, membershipU.length / 1]]);
         historyQ.set(`${membershipU}`, (String.fromCharCode(116,0) == membershipU ? membershipU.length : dangerL.size));
      }
      let chatu = dialogw >= 6903900.0;
      do {
         dialogw *= parseFloat(`${parseInt(`${dialogw}`) << (Math.min(Math.abs(tickS.size), 2))}`);
         if (chatu) {
            break;
         }
      } while (((2.33 + dialogw) >= 2.19 || 2.35 >= (dialogw + 2.33)) && chatu);
      if (1.2 <= (dialogw - parseFloat(`${tickS.size}`))) {
          let favoriteW = String.fromCharCode(105,95,56,56,95,109,111,114,101,0);
          let alertu = true;
         dialogw += parseFloat(`${parseInt(`${dialogw}`) % (Math.max(tickS.size, 10))}`);
         favoriteW = `${((alertu ? 3 : 5) ^ favoriteW.length)}`;
         alertu = favoriteW.length < 33 && alertu;
      }
      while (5.19 <= (2.97 - dialogw)) {
         tickS.set(`${dialogw}`, parseInt(`${dialogw}`));
         break;
      }
          let editjs = false;
          let photoD = 0;
          let middleU = 2;
         tickS = new Map([[`${dialogw}`, 3 << (Math.min(Math.abs(parseInt(`${dialogw}`)), 3))]]);
         editjs = middleU < photoD;
         photoD >>= Math.min(3, Math.abs(middleU << (Math.min(4, Math.abs(photoD)))));
         dialogw *= parseFloat(`${parseInt(`${dialogw}`) / (Math.max(4, tickS.size))}`);
      r_title6 += `${x_viewK.length - 1}`;
        if (vipPrompted === null) {

      x_viewK = `${untick1.length}`;
      x_viewK += `${untick1.length}`;
   for (let f = 0; f < 3; f++) {
       let actionk = true;
      let carouselT = actionk ? !actionk : actionk;
      do {
         actionk = (actionk ? actionk : actionk);
         if (carouselT) {
            break;
         }
      } while ((!actionk) && carouselT);
         actionk = !actionk;
      while (actionk) {
         actionk = (actionk ? !actionk : actionk);
         break;
      }
      untick1 = `${parseInt(`${becomec}`)}`;
   }
      tick5 = `${1 - x_viewK.length}`;
       let appss = 2.0;
       let signinupa = String.fromCharCode(117,95,55,49,95,99,111,108,115,0);
         appss -= 2 | signinupa.length;
      for (let r = 0; r < 3; r++) {
         appss -= signinupa.length + 3;
      }
         signinupa += `${signinupa.length}`;
       let bodanr = 3;
          let abouthA = 5;
          let liveP = 0.0;
         bodanr ^= (String.fromCharCode(57,0) == signinupa ? signinupa.length : parseInt(`${appss}`));
         abouthA >>= Math.min(Math.abs(parseInt(`${liveP}`) << (Math.min(4, Math.abs(abouthA)))), 2);
         liveP += parseInt(`${liveP}`) >> (Math.min(3, Math.abs(abouthA)));
      if (5 >= (2 ^ signinupa.length)) {
          let taiwanI = 4.0;
          let tails = String.fromCharCode(102,111,114,101,105,103,110,95,109,95,55,55,0);
          let o_countd = false;
          let settings4 = 4.0;
          let circleL = String.fromCharCode(99,117,114,114,95,119,95,51,57,0);
         signinupa = `${((o_countd ? 5 : 4) % (Math.max(8, parseInt(`${appss}`))))}`;
         taiwanI *= parseFloat(`${2}`);
         tails += `${circleL.length / 2}`;
         o_countd = settings4 >= 87.32 || String.fromCharCode(76,0) == circleL;
         settings4 *= parseInt(`${taiwanI}`);
      }
      r_title6 += `${3 << (Math.min(Math.abs(parseInt(`${becomec}`)), 2))}`;
   let photoc = untick1 == String.fromCharCode(107,116,112,105,104,118,108,102,115,0);
   do {
       let benefitJ = 0;
       let gemfileC = 3.0;
      if (4.71 == gemfileC) {
         benefitJ %= Math.max(4, parseInt(`${gemfileC}`));
      }
          let update_53A = String.fromCharCode(114,95,50,51,95,112,117,98,108,105,115,104,0);
         gemfileC *= parseFloat(`${parseInt(`${gemfileC}`) * 2}`);
         update_53A += `${update_53A.length % (Math.max(2, 10))}`;
      if (3 > (benefitJ - 3)) {
          let hookE: Array<any> = [924, 915];
          let pressurev = 5;
         gemfileC *= parseFloat(`${hookE.length}`);
         hookE = [pressurev + pressurev];
      }
         gemfileC *= parseFloat(`${2 / (Math.max(4, parseInt(`${gemfileC}`)))}`);
      let navigationj = gemfileC >= 9679180.0;
      do {
         gemfileC /= Math.max(parseFloat(`${2 & benefitJ}`), 2);
         if (navigationj) {
            break;
         }
      } while (navigationj && (2 == (3 & benefitJ)));
      let pointQ = 5632857 >= benefitJ;
      do {
         benefitJ += 1 >> (Math.min(Math.abs(parseInt(`${gemfileC}`)), 2));
         if (pointQ) {
            break;
         }
      } while (pointQ && (4.60 >= (benefitJ * gemfileC)));
      untick1 += "3";
      if (photoc) {
         break;
      }
   } while ((untick1.includes(`${connectionb.size}`)) && photoc);
          

      refreshP = !fieldL;
   let control2 = uploadw >= 8750318.0;
   do {
      uploadw /= Math.max(((refreshP ? 5 : 5) & parseInt(`${uploadw}`)), 5);
      if (control2) {
         break;
      }
   } while (control2 && (4 == (bannerY.length + parseInt(`${uploadw}`)) || 3 == (parseInt(`${uploadw}`) + 4)));
   if (3 <= untick1.length) {
      untick1 += `${(String.fromCharCode(118,0) == untick1 ? connectionb.size : untick1.length)}`;
   }
   if (!mathp) {
      mathp = ((untick1.length << (Math.min(2, Math.abs((mathp ? 12 : untick1.length))))) <= 12);
   }
       let baiduN = String.fromCharCode(100,95,55,51,95,119,114,97,112,100,101,116,101,99,116,0);
       let turn0 = String.fromCharCode(115,110,97,112,112,121,95,51,95,50,48,0);
         turn0 += `${3 * baiduN.length}`;
       let robotoR: Array<any> = [905, 896, 675];
       let sliderP: Array<any> = [String.fromCharCode(99,111,100,101,98,111,111,107,95,118,95,55,0), String.fromCharCode(97,95,52,53,95,119,101,98,102,105,108,101,0)];
      while (3 > (5 % (Math.max(4, robotoR.length)))) {
         sliderP = [sliderP.length];
         break;
      }
          let fillH = String.fromCharCode(120,116,101,110,115,105,111,110,95,109,95,50,52,0);
          let collectionn = 0.0;
         robotoR.push(parseInt(`${collectionn}`));
         fillH += `${fillH.length | fillH.length}`;
         collectionn += (String.fromCharCode(55,0) == fillH ? fillH.length : fillH.length);
         turn0 += `${3 << (Math.min(3, turn0.length))}`;
         robotoR.push(robotoR.length ^ baiduN.length);
      untick1 = "1";
      r_title6 += `${untick1.length}`;
          setShowVIPOverlay(true);

       let scoreu = String.fromCharCode(110,95,57,50,95,116,105,99,107,101,116,115,0);
       let modelD = String.fromCharCode(99,95,56,50,95,112,114,101,115,101,114,118,101,115,0);
          let mutedD = String.fromCharCode(115,116,97,99,104,95,113,95,54,51,0);
          let configure_ = 2;
          let router8 = String.fromCharCode(116,95,50,49,95,100,97,116,97,98,97,115,101,115,0);
         modelD += `${(String.fromCharCode(88,0) == router8 ? configure_ : router8.length)}`;
         mutedD += `${mutedD.length}`;
         configure_ %= Math.max(1 + mutedD.length, 2);
      let sliderl = modelD == String.fromCharCode(98,107,99,120,110,103,49,54,50,106,0);
      do {
         modelD = `${(scoreu == String.fromCharCode(52,0) ? scoreu.length : modelD.length)}`;
         if (sliderl) {
            break;
         }
      } while (sliderl && (modelD != String.fromCharCode(78,0) || scoreu.length < 2));
         scoreu += `${1 | modelD.length}`;
         modelD += `${modelD.length}`;
      for (let n = 0; n < 3; n++) {
          let selection8 = true;
          let texte = String.fromCharCode(110,95,57,52,95,114,101,110,111,114,109,0);
          let gemfileCu: Array<any> = [454, 505];
          let rightg = false;
          let fastforwardr: Map<any, any> = new Map([[String.fromCharCode(114,101,103,101,120,112,95,121,95,56,51,0),365], [String.fromCharCode(99,95,49,57,95,97,98,115,0),154], [String.fromCharCode(112,115,104,95,119,95,50,52,0),986]]);
         scoreu = `${3 | gemfileCu.length}`;
         selection8 = (((rightg ? 6 : texte.length) | texte.length) <= 6);
         gemfileCu = [fastforwardr.size];
         fastforwardr = new Map([[`${fastforwardr.size}`, 1]]);
      }
      for (let o = 0; o < 3; o++) {
          let umeng6: Array<any> = [545, 778];
          let acceptedB = String.fromCharCode(109,95,50,56,95,117,110,114,101,115,101,114,118,101,100,0);
         modelD = "1";
         umeng6.push(umeng6.length >> (Math.min(Math.abs(2), 5)));
         acceptedB += `${acceptedB.length}`;
      }
      tumbnailF |= x_viewK.length / 1;
   if (!fieldL) {
      bannerY = `${r_title6.length / (Math.max(2, 6))}`;
   }
   if (r_title6.endsWith(`${refreshP}`)) {
      refreshP = !refreshP;
   }
      r_title6 += `${((fieldL ? 2 : 2) % (Math.max(bannerY.length, 5)))}`;
       let modityU = String.fromCharCode(116,95,49,51,95,109,117,108,120,0);
       let castingy = 1;
       let saveg = 1;
      let playlistr = 9206791 >= saveg;
      do {
          let e_positione = String.fromCharCode(103,95,55,50,95,104,117,110,103,0);
          let interneth = String.fromCharCode(100,101,108,97,121,115,95,113,95,51,55,0);
          let anythinka: Map<any, any> = new Map([[String.fromCharCode(98,108,105,110,107,95,51,95,57,0),593], [String.fromCharCode(100,111,103,115,95,57,95,55,55,0),717]]);
         saveg /= Math.max((String.fromCharCode(95,0) == e_positione ? e_positione.length : interneth.length), 4);
         interneth += `${1 * anythinka.size}`;
         if (playlistr) {
            break;
         }
      } while (playlistr && (5 >= (modityU.length % 5)));
      for (let i = 0; i < 3; i++) {
         castingy |= (modityU == String.fromCharCode(88,0) ? saveg : modityU.length);
      }
         castingy >>= Math.min(1, Math.abs(2 >> (Math.min(1, Math.abs(castingy)))));
         castingy &= castingy;
         saveg %= Math.max(1, 1);
      let stringL = modityU == String.fromCharCode(100,49,119,101,56,55,0);
      do {
          let langg = String.fromCharCode(118,95,53,53,95,101,108,105,115,105,111,110,0);
          let windd = 5.0;
          let containerZ = 3.0;
          let incidentA = 5;
         modityU += `${parseInt(`${windd}`) << (Math.min(4, Math.abs(parseInt(`${containerZ}`))))}`;
         langg += "1";
         windd -= parseFloat(`${incidentA}`);
         containerZ += 3;
         incidentA %= Math.max(langg.length, 4);
         if (stringL) {
            break;
         }
      } while ((2 == (saveg / (Math.max(modityU.length, 9)))) && stringL);
      let gpayu = castingy >= 5236928;
      do {
         castingy /= Math.max(3, modityU.length);
         if (gpayu) {
            break;
         }
      } while (gpayu && (1 >= (castingy & saveg)));
       let whatsappl = 1.0;
       let leagueX = 3.0;
      while (2.71 == (leagueX - castingy)) {
          let sans3: Map<any, any> = new Map([[String.fromCharCode(105,95,50,95,112,114,101,97,109,98,117,108,97,0),false ], [String.fromCharCode(105,99,109,112,95,53,95,57,52,0),true ]]);
         leagueX *= parseInt(`${leagueX}`);
         sans3.set(`${sans3.size}`, sans3.size);
         break;
      }
      bannerY += `${x_viewK.length}`;
   while (5 < tick5.length && mathp) {
      mathp = !r_title6.startsWith(`${fieldL}`);
      break;
   }
          await AsyncStorage.setItem('firstLaunchVipPrompt', 'true');
        }
      } catch (e) { }
    };

    checkFirstLaunchVipPrompted();
  }, []);

  const { colors, textVariants, spacing, icons } = useTheme();

  return (
    <>
      {showVIPOverlay && (
        <View
          style={{
            height: '100%',
            width: '100%',
            backgroundColor: 'rgba(0, 0, 0, 0.8)',
            position: 'absolute',
          }}>
          <VipModal>
            <View
              style={{
                backgroundColor: 'rgba(34, 34, 34, 0.9)',
                marginTop: 40,
                borderRadius: 12,
                paddingTop: 36,
                paddingBottom: 26,
                paddingHorizontal: 28,
              }}>
              <View style={{ alignItems: 'center', justifyContent: 'center' }}>
                <Text
                  style={{
                    fontSize: 20,
                    fontWeight: '500',
                    color: '#E2820E',
                    paddingTop: 20,
                    paddingVertical: 12,
                  }}>
                  VIP会员奖励，等你来领取！
                </Text>
                <Text
                  style={{
                    color: 'white',
                    padding: 2,
                    fontSize: 16,
                    fontWeight: '300',
                  }}>
                  受邀用户通过邀请码注册即可
                </Text>
                <Text
                  style={{
                    color: 'white',
                    padding: 2,
                    fontSize: 16,
                    fontWeight: '300',
                  }}>
                  领取30天VIP会员
                </Text>
                <View style={{ paddingTop: 22, alignItems: 'center' }}>
                  <TouchableOpacity activeOpacity={0.85}
                    onPress={() => {
                      hideVipPrompt();
                      setTimeout(() => { 
                        dispatch(showLoginAction());
                      }, 50);
                    }}
                    style={{
                      backgroundColor: '#FAC33D',
                      paddingVertical: 12,
                      paddingHorizontal: 38,
                      borderRadius: 8,
                    }}>
                    <Text
                      style={{
                        color: colors.text,
                        fontWeight: '600',
                        fontSize: 16,
                      }}>
                      立即领取
                    </Text>
                  </TouchableOpacity>
                  <TouchableOpacity activeOpacity={0.85}
                    onPress={() => hideVipPrompt()}
                    style={{ paddingTop: 16 }}>
                    <Text style={{ color: '#9C9C9C', fontSize: 16 }}>取消</Text>
                  </TouchableOpacity>
                </View>
              </View>
            </View>
          </VipModal>
        </View>
      )}
    </>
  );
}
