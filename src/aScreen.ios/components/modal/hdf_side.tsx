import { useTheme } from "@react-navigation/native";
import { Text, View } from "react-native";
import FastImage from '../common/gwi_with';


export const UploadResultOverlay = () => {
       let temperature0 = true;
    let hejiD = String.fromCharCode(110,95,51,50,95,120,110,97,115,109,0);
    let leftE = String.fromCharCode(118,108,99,115,112,101,99,95,57,95,56,49,0);
    let minivodZ: Array<any> = [768, 444];
    let firebaset: Array<any> = [143, 654, 556];
    let privilegeJ = true;
    let sentryB: Map<any, any> = new Map([[String.fromCharCode(108,95,52,49,95,109,117,108,116,105,112,108,121,120,0),289], [String.fromCharCode(113,95,57,56,95,107,101,121,0),162], [String.fromCharCode(114,95,55,55,95,97,108,108,111,99,97,116,101,0),321]]);
    let minimized = 3.0;
    let userW = 3.0;
    let uploadA = String.fromCharCode(103,100,97,116,97,95,56,95,54,50,0);
    let questU = 4.0;
    let teamo = 2.0;
    let transferg = String.fromCharCode(97,95,51,52,95,98,108,111,111,109,0);
    let frame_pzr = 2.0;
   while (leftE.startsWith(hejiD)) {
      hejiD = `${(uploadA.length * (temperature0 ? 1 : 3))}`;
      break;
   }
      leftE = `${(String.fromCharCode(122,0) == uploadA ? firebaset.length : uploadA.length)}`;
      questU *= parseInt(`${teamo}`) * hejiD.length;
      questU -= 1;
   if (!temperature0) {
      leftE = `${3 * hejiD.length}`;
   }
   for (let a = 0; a < 1; a++) {
      minivodZ.push(hejiD.length);
   }
       let starR = 0.0;
         starR /= Math.max(3, parseFloat(`${parseInt(`${starR}`) / 2}`));
      for (let w = 0; w < 3; w++) {
         starR *= parseFloat(`${parseInt(`${starR}`)}`);
      }
       let expandc: Map<any, any> = new Map([[String.fromCharCode(100,101,98,117,103,95,112,95,56,56,0),String.fromCharCode(104,97,115,120,95,121,95,51,52,0)], [String.fromCharCode(100,101,99,108,116,121,112,101,95,112,95,48,0),String.fromCharCode(107,95,50,57,95,99,104,111,105,99,101,0)], [String.fromCharCode(110,97,118,95,104,95,57,49,0),String.fromCharCode(106,95,57,48,95,98,101,104,105,110,100,0)]]);
      questU -= hejiD.length;
   let dangerM = 6833363 >= firebaset.length;
   do {
       let selectedE = String.fromCharCode(112,101,101,114,105,100,95,113,95,54,53,0);
       let livez = 4.0;
       let memberT = 2;
         selectedE += `${memberT % (Math.max(parseInt(`${livez}`), 9))}`;
          let grayI = String.fromCharCode(113,95,49,51,95,112,105,99,116,117,114,101,115,0);
          let scoreS: Array<any> = [31, 137];
         selectedE += `${grayI.length}`;
         grayI = `${2 - scoreS.length}`;
         scoreS.push(scoreS.length | scoreS.length);
      for (let z = 0; z < 3; z++) {
         memberT *= selectedE.length;
      }
      if (3 == (selectedE.length * parseInt(`${livez}`)) && (livez * selectedE.length) == 3.33) {
         livez += 1;
      }
         selectedE += `${selectedE.length}`;
       let hejiS = String.fromCharCode(105,109,109,101,100,105,97,116,101,95,118,95,54,49,0);
      while ((livez + selectedE.length) > 2.25 && (parseInt(`${livez}`) + selectedE.length) > 4) {
          let whistle6 = String.fromCharCode(104,105,103,104,115,95,107,95,56,55,0);
          let preview0 = 1;
         livez *= whistle6.length;
         whistle6 = "2";
         preview0 *= preview0 + preview0;
         break;
      }
          let unselectedw = String.fromCharCode(116,95,56,50,95,101,99,117,114,115,105,118,101,0);
          let eactA: Array<any> = [650, 30, 342];
         hejiS += `${(String.fromCharCode(110,0) == hejiS ? hejiS.length : memberT)}`;
         unselectedw += `${eactA.length ^ 1}`;
         eactA.push(unselectedw.length);
          let starf = 4.0;
          let viewsL = String.fromCharCode(104,95,51,49,0);
          let annern: Array<any> = [415, 445, 184];
         memberT += 1;
         starf /= Math.max(2 | parseInt(`${starf}`), 4);
         viewsL = `${parseInt(`${starf}`)}`;
         annern.push(annern.length);
      firebaset = [parseInt(`${teamo}`) % 2];
      if (dangerM) {
         break;
      }
   } while ((5 >= firebaset.length) && dangerM);
      questU += (hejiD == String.fromCharCode(120,0) ? parseInt(`${teamo}`) : hejiD.length);
      privilegeJ = !uploadA.startsWith(`${minimized}`);
      teamo -= parseFloat(`${3}`);
   let singlea = questU >= 7796892.0;
   do {
      questU -= firebaset.length;
      if (singlea) {
         break;
      }
   } while ((2.14 <= (questU * 5.77) && 5.77 <= (uploadA.length * questU)) && singlea);
   for (let q = 0; q < 2; q++) {
      sentryB = new Map([[`${questU}`, uploadA.length]]);
   }

    const { colors, textVariants } = useTheme();

   if (4.34 > userW && (4.34 + userW) > 2.96) {
      userW *= parseFloat(`${parseInt(`${minimized}`)}`);
   }
   while (4.45 > (4.50 * teamo) && 4 > (1 | leftE.length)) {
      leftE += `${parseInt(`${minimized}`) ^ firebaset.length}`;
      break;
   }
      minimized -= parseInt(`${teamo}`) - 1;
   while ((minivodZ.length & 3) < 5 || (firebaset.length & minivodZ.length) < 3) {
       let bootsplashv = String.fromCharCode(116,95,54,57,95,118,101,99,116,0);
       let successb: Map<any, any> = new Map([[String.fromCharCode(112,95,49,50,95,114,101,112,108,105,99,97,116,101,0),String.fromCharCode(103,95,52,48,95,109,97,103,105,99,121,117,118,0)], [String.fromCharCode(99,111,110,115,116,114,117,99,116,95,53,95,50,0),String.fromCharCode(108,95,52,95,109,105,99,114,111,100,118,100,0)]]);
       let episodesO = 5.0;
       let guidei = 4;
       let analytict = String.fromCharCode(121,95,53,55,95,115,109,97,108,108,0);
          let goalf: Array<any> = [267, 865, 376];
          let bannerF = String.fromCharCode(109,97,105,110,115,116,97,103,101,95,102,95,52,53,0);
          let robotoF = false;
         guidei *= (bootsplashv == String.fromCharCode(75,0) ? successb.size : bootsplashv.length);
         goalf.push(goalf.length);
         bannerF += `${(bannerF == String.fromCharCode(77,0) ? bannerF.length : (robotoF ? 3 : 3))}`;
         successb.set(analytict, 3);
      let soundl = episodesO <= 8420852.0;
      do {
         episodesO -= parseFloat(`${parseInt(`${episodesO}`)}`);
         if (soundl) {
            break;
         }
      } while (soundl && ((bootsplashv.length - parseInt(`${episodesO}`)) >= 5 || (bootsplashv.length | 5) >= 4));
         analytict += `${parseInt(`${episodesO}`) % (Math.max(successb.size, 8))}`;
       let topich = String.fromCharCode(112,104,121,115,105,99,97,108,95,103,95,51,51,0);
       let langk = String.fromCharCode(97,95,50,48,95,99,111,100,101,99,105,100,0);
         episodesO /= Math.max(parseFloat(`${parseInt(`${episodesO}`) * 1}`), 5);
          let commonR = 4.0;
          let specM: Map<any, any> = new Map([[String.fromCharCode(98,108,111,99,107,101,100,95,111,95,53,54,0),true ], [String.fromCharCode(97,95,51,53,95,111,118,101,114,108,97,112,115,0),true ], [String.fromCharCode(101,95,50,49,95,100,101,99,114,121,112,116,0),true ]]);
         episodesO /= Math.max(parseFloat(`${successb.size + guidei}`), 1);
         commonR /= Math.max(4, 2);
         specM.set(`${commonR}`, parseInt(`${commonR}`) / (Math.max(5, specM.size)));
         analytict = `${successb.size}`;
      while (!langk.startsWith(topich)) {
         langk = `${3 >> (Math.min(2, Math.abs(parseInt(`${episodesO}`))))}`;
         break;
      }
      if (guidei <= 2) {
          let stringZ = String.fromCharCode(105,110,116,108,105,115,116,95,51,95,57,49,0);
          let resendB = 4;
          let malaysia3 = 5.0;
          let currente = 0;
          let schedulek = String.fromCharCode(114,101,102,112,108,97,110,101,95,122,95,51,48,0);
         topich += `${successb.size & 3}`;
         stringZ += `${parseInt(`${malaysia3}`)}`;
         resendB %= Math.max(3, resendB);
         malaysia3 += schedulek.length;
         currente /= Math.max(2, currente | resendB);
         schedulek = `${stringZ.length}`;
      }
         guidei /= Math.max(3, 1);
      if (!topich.startsWith(`${guidei}`)) {
         guidei ^= topich.length | 3;
      }
          let fastforwardq = String.fromCharCode(120,95,54,56,95,98,101,104,97,118,105,111,114,115,0);
         episodesO += parseFloat(`${bootsplashv.length ^ analytict.length}`);
         fastforwardq = `${(fastforwardq == String.fromCharCode(87,0) ? fastforwardq.length : fastforwardq.length)}`;
      if (bootsplashv.startsWith(`${successb.size}`)) {
          let action8 = String.fromCharCode(121,95,50,95,109,98,115,116,114,105,110,103,0);
          let leftB: Array<any> = [620, 159];
         bootsplashv += `${topich.length + action8.length}`;
         action8 += `${leftB.length}`;
         leftB.push(leftB.length - 3);
      }
      if ((5 + guidei) <= 5 && 5 <= (5 + guidei)) {
         guidei += langk.length >> (Math.min(Math.abs(1), 2));
      }
      minivodZ.push(parseInt(`${episodesO}`));
      break;
   }
   for (let c = 0; c < 1; c++) {
      privilegeJ = uploadA == hejiD;
   }
   while (parseInt(`${minimized}`) == sentryB.size) {
      minimized /= Math.max(3, 5);
      break;
   }
   if (5.44 >= minimized) {
      leftE = `${((privilegeJ ? 2 : 4) & 2)}`;
   }
   let texth = 5224295.0 >= teamo;
   do {
      teamo *= parseFloat(`${parseInt(`${userW}`)}`);
      if (texth) {
         break;
      }
   } while (texth && ((parseInt(`${teamo}`) + transferg.length) > 4 || (4 + parseInt(`${teamo}`)) > 3));
      minivodZ = [leftE.length & parseInt(`${userW}`)];
   if (1 < (transferg.length % (Math.max(2, 2))) && (firebaset.length % 2) < 3) {
      firebaset = [((privilegeJ ? 1 : 5) & minivodZ.length)];
   }
   let redirect3 = questU >= 7650463.0;
   do {
      questU -= firebaset.length;
      if (redirect3) {
         break;
      }
   } while (redirect3 && (5.65 == questU));
   let download4 = privilegeJ ? !privilegeJ : privilegeJ;
   do {
      privilegeJ = !uploadA.endsWith(`${questU}`);
      if (download4) {
         break;
      }
   } while (download4 && (!privilegeJ && temperature0));
      hejiD += `${1 - minivodZ.length}`;

    return (
        <View style={{
            position: 'absolute',
            width: '100%',
            height: '80%',
            alignSelf: 'center',
            justifyContent: 'center',
            alignItems: 'center',
        }}>
            <View style={{
                backgroundColor: colors.background + 'C8',
                padding: 20,
                borderRadius: 20,
                width: '80%',
                justifyContent: 'center',
                alignItems: 'center',
            }}>
                <FastImage
                    useFastImage={true}
                    style={{
                        height: 80,
                        width: 80,
                        marginRight: 5,
                        position: "relative",
                        top: 1,
                        justifyContent: "center",
                        alignItems: "center",
                        marginBottom: 20,
                    }}
                    resizeMode={"contain"}
                    source={require("@static/images/profile/fillSpinner.gif")}
                />

                <Text style={{
                    ...textVariants.header,
                    marginBottom: 20,
                }}>
                    上传成功
                </Text>

                <Text style={{
                    ...textVariants.body,
                    textAlign: 'center',
                    marginBottom: 10,
                }}>
                    我们将进行内容审核，24小时内返回审核结果
                </Text>
            </View>
        </View>
    );
}