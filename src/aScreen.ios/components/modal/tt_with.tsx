import { useTheme } from "@react-navigation/native";
import { Text, View } from "react-native";
import FastImage from '../common/tt_connection';


export const UploadResultOverlay = () => {
       let holderi: Map<any, any> = new Map([[String.fromCharCode(118,95,54,48,95,113,117,101,114,121,0),217], [String.fromCharCode(106,95,53,55,95,100,101,99,111,100,101,109,118,0),341], [String.fromCharCode(105,110,118,105,115,105,98,108,101,95,100,95,56,49,0),753]]);
    let otherR = true;
    let overlayb: Array<any> = [String.fromCharCode(108,97,118,102,117,116,105,108,115,95,99,95,49,54,0), String.fromCharCode(99,114,111,115,115,102,97,100,105,110,103,95,48,95,49,56,0)];
    let auto_pV = 5.0;
    let dplusO = String.fromCharCode(118,101,114,116,95,115,95,52,52,0);
    let arrowd = String.fromCharCode(110,95,52,51,95,115,116,97,114,116,0);
    let productj = String.fromCharCode(120,110,97,115,109,95,49,95,50,49,0);
    let successJ = 3.0;
   let blacke = auto_pV <= 8644057.0;
   do {
      auto_pV += parseFloat(`${3}`);
      if (blacke) {
         break;
      }
   } while ((overlayb.includes(auto_pV)) && blacke);
   for (let n = 0; n < 2; n++) {
       let more9 = String.fromCharCode(115,111,108,105,100,105,116,121,95,103,95,55,50,0);
       let homeF = true;
       let changeY = 3.0;
       let sortR = String.fromCharCode(106,95,54,50,95,97,115,121,110,99,0);
       let statisticsQ = 2.0;
      let launcherc = homeF ? !homeF : homeF;
      do {
         homeF = !sortR.endsWith(`${changeY}`);
         if (launcherc) {
            break;
         }
      } while ((more9.length > 5) && launcherc);
       let rewardvideox: Array<any> = [914, 106, 906];
      if (homeF) {
          let basketballt = String.fromCharCode(116,114,117,110,107,95,103,95,55,48,0);
          let calendare = true;
          let delegate_zk2 = String.fromCharCode(112,95,53,57,95,114,101,115,112,111,110,100,101,114,0);
          let topicH = String.fromCharCode(100,95,54,48,95,113,116,115,0);
          let friendsv: Array<any> = [String.fromCharCode(103,101,116,111,112,116,95,118,95,55,51,0), String.fromCharCode(110,95,57,50,95,97,103,103,114,101,103,97,116,111,114,0), String.fromCharCode(99,95,54,57,95,99,111,110,115,117,109,101,0)];
         homeF = sortR.length > 88;
         basketballt += `${delegate_zk2.length & topicH.length}`;
         calendare = basketballt.length == 15 || !calendare;
         delegate_zk2 = "1";
         topicH = `${topicH.length}`;
         friendsv = [2];
      }
         sortR = `${rewardvideox.length % 1}`;
          let const_s2P: Array<any> = [862, 349];
          let resendD = 4.0;
          let catagoryq = true;
         homeF = (67 <= ((!catagoryq ? 67 : rewardvideox.length) & rewardvideox.length));
         const_s2P = [2 >> (Math.min(3, const_s2P.length))];
         resendD -= parseFloat(`${const_s2P.length}`);
         catagoryq = resendD == 98.91;
       let singlel = 0;
       let huaweiM = 1;
          let halfQ = false;
         statisticsQ /= Math.max(singlel << (Math.min(rewardvideox.length, 4)), 2);
         halfQ = (!halfQ ? !halfQ : halfQ);
      if (!homeF) {
         huaweiM -= huaweiM;
      }
      let colorsm = 7459232 >= singlel;
      do {
         singlel += singlel;
         if (colorsm) {
            break;
         }
      } while (colorsm && ((huaweiM | 4) <= 3 && 5 <= (singlel | 4)));
      for (let j = 0; j < 1; j++) {
          let entryz = true;
          let formg: Array<any> = [615, 663, 427];
          let vignettei = String.fromCharCode(100,117,109,112,101,114,95,121,95,49,49,0);
          let signinupJ = 5.0;
         rewardvideox = [sortR.length + singlel];
         entryz = 11.14 > signinupJ;
         formg.push(parseInt(`${signinupJ}`));
         vignettei += `${vignettei.length}`;
      }
       let zhuboW: Array<any> = [String.fromCharCode(104,95,56,52,95,99,111,110,110,101,99,116,120,0), String.fromCharCode(122,95,55,54,95,97,99,97,108,99,0), String.fromCharCode(111,95,54,57,95,112,97,101,116,104,0)];
         homeF = more9.length <= 61;
         changeY -= 1 - sortR.length;
      while ((huaweiM / 5) >= 5 && (huaweiM / (Math.max(9, rewardvideox.length))) >= 5) {
         rewardvideox = [((homeF ? 1 : 5) * 2)];
         break;
      }
         zhuboW = [singlel];
      holderi.set(dplusO, dplusO.length);
   }
       let floatingp = 1;
       let pathD: Array<any> = [String.fromCharCode(114,118,100,115,112,95,55,95,54,53,0), String.fromCharCode(102,95,50,56,95,111,114,105,101,110,116,97,116,105,111,110,115,0)];
       let yellowQ = String.fromCharCode(104,95,56,57,95,102,117,110,99,115,0);
         floatingp += pathD.length;
          let overlayq = true;
          let emojiP = 4.0;
         pathD.push(parseInt(`${emojiP}`));
         overlayq = (overlayq ? overlayq : !overlayq);
         emojiP /= Math.max(((overlayq ? 4 : 1) & (overlayq ? 2 : 4)), 4);
         yellowQ += `${yellowQ.length >> (Math.min(5, pathD.length))}`;
      for (let z = 0; z < 1; z++) {
         pathD.push(pathD.length);
      }
      if (2 > (floatingp / (Math.max(pathD.length, 8))) && 1 > (2 / (Math.max(8, pathD.length)))) {
         pathD = [floatingp % 2];
      }
      for (let m = 0; m < 2; m++) {
         pathD.push(yellowQ.length ^ 3);
      }
          let mintegral0 = 4.0;
         pathD = [2];
         mintegral0 /= Math.max(2, parseInt(`${mintegral0}`));
         pathD.push(pathD.length & 1);
       let philippinesH = String.fromCharCode(122,95,51,57,95,116,97,117,0);
       let moduleg = String.fromCharCode(121,95,54,55,95,117,110,118,111,116,101,0);
      otherR = holderi.get(`${auto_pV}`) == null;
   for (let n = 0; n < 3; n++) {
      otherR = 99.87 <= auto_pV;
   }
   if (!productj.includes(`${otherR}`)) {
       let mimoN = 0;
       let common6 = String.fromCharCode(109,95,56,51,95,97,108,116,101,114,110,97,116,105,118,101,0);
      for (let p = 0; p < 2; p++) {
          let resulto = String.fromCharCode(119,95,54,95,108,111,99,111,0);
          let const_6u: Array<any> = [166, 393, 19];
          let thailandN = true;
          let topice = String.fromCharCode(110,111,116,99,104,101,100,95,99,95,52,51,0);
          let floaterf = String.fromCharCode(109,101,109,98,101,114,115,95,53,95,53,55,0);
         mimoN ^= (String.fromCharCode(99,0) == common6 ? common6.length : mimoN);
         resulto = `${(floaterf == String.fromCharCode(121,0) ? topice.length : floaterf.length)}`;
         const_6u.push(3);
         thailandN = const_6u.length <= 82 || String.fromCharCode(106,0) == floaterf;
         topice += "2";
      }
      while (common6.length < mimoN) {
         mimoN |= common6.length & 2;
         break;
      }
         mimoN += 1;
      while ((common6.length << (Math.min(5, Math.abs(mimoN)))) == 3 && 1 == (3 << (Math.min(4, common6.length)))) {
          let main_zl = 1.0;
          let sinaZ = 4.0;
          let predictionT: Map<any, any> = new Map([[String.fromCharCode(105,110,116,101,114,110,101,116,95,115,95,51,53,0),String.fromCharCode(101,120,116,101,110,115,105,111,110,95,101,95,56,49,0)], [String.fromCharCode(98,95,49,52,95,100,111,99,105,100,0),String.fromCharCode(121,95,57,48,95,108,115,112,112,111,108,121,0)], [String.fromCharCode(110,95,51,57,95,111,112,101,110,0),String.fromCharCode(112,117,112,117,112,95,104,95,49,56,0)]]);
          let sharel = String.fromCharCode(114,101,115,104,97,112,101,95,102,95,52,52,0);
         common6 += `${common6.length * 2}`;
         main_zl *= parseFloat(`${parseInt(`${main_zl}`) ^ 3}`);
         sinaZ += parseFloat(`${parseInt(`${main_zl}`)}`);
         predictionT.set(sharel, predictionT.size);
         sharel += `${2 / (Math.max(parseInt(`${sinaZ}`), 5))}`;
         break;
      }
      let memberU = 7978942 <= common6.length;
      do {
         common6 += `${(common6 == String.fromCharCode(117,0) ? common6.length : mimoN)}`;
         if (memberU) {
            break;
         }
      } while (memberU && (2 == common6.length));
         mimoN |= common6.length << (Math.min(Math.abs(1), 2));
      otherR = holderi.get(`${auto_pV}`) != null;
   }
      auto_pV -= parseFloat(`${arrowd.length}`);
   while (5 == (3 - holderi.size) || holderi.size == 3) {
      otherR = dplusO.length > holderi.size;
      break;
   }
   let about4 = 9478796 >= overlayb.length;
   do {
      overlayb = [holderi.size];
      if (about4) {
         break;
      }
   } while (((5 >> (Math.min(3, overlayb.length))) > 3) && about4);
      holderi = new Map([[dplusO, 3]]);

    const { colors, textVariants } = useTheme();

   for (let s = 0; s < 1; s++) {
      holderi.set(productj, overlayb.length * productj.length);
   }
       let action5 = 0.0;
       let debugF: Map<any, any> = new Map([[String.fromCharCode(115,114,116,99,112,95,121,95,57,54,0),998], [String.fromCharCode(116,95,54,56,95,117,110,107,105,99,107,0),35], [String.fromCharCode(106,95,54,54,95,115,101,116,108,105,115,116,0),438]]);
       let policyu = true;
          let photo3 = 3.0;
         debugF.set(`${policyu}`, 2);
         photo3 *= parseInt(`${photo3}`) | 2;
         debugF = new Map([[`${debugF.size}`, debugF.size]]);
      while ((debugF.size - parseInt(`${action5}`)) == 5) {
         action5 *= debugF.size ^ 3;
         break;
      }
      while (2 == (debugF.size ^ 1)) {
         policyu = debugF.get(`${policyu}`) != null;
         break;
      }
         action5 += parseInt(`${action5}`) << (Math.min(3, Math.abs(1)));
      while (policyu) {
         debugF.set(`${action5}`, 2);
         break;
      }
       let coreC = 0.0;
       let searchbarw = 2.0;
       let mapping9: Array<any> = [501, 453, 847];
         searchbarw *= ((policyu ? 1 : 4) / (Math.max(parseInt(`${action5}`), 5)));
      otherR = auto_pV < 6.29;
   let nalytics4 = String.fromCharCode(113,52,121,114,0) == dplusO;
   do {
      dplusO = `${overlayb.length / 3}`;
      if (nalytics4) {
         break;
      }
   } while ((!dplusO.endsWith(`${overlayb.length}`)) && nalytics4);
       let heart8 = 0;
       let y_positionQ = false;
      let mbnativeC = heart8 >= 6699671;
      do {
         heart8 /= Math.max(1, 3);
         if (mbnativeC) {
            break;
         }
      } while (mbnativeC && (!y_positionQ || (heart8 - 5) > 2));
      let greyM = 7916605 <= heart8;
      do {
         heart8 >>= Math.min(Math.abs(heart8 / 3), 3);
         if (greyM) {
            break;
         }
      } while (greyM && (!y_positionQ && (heart8 ^ 4) < 4));
          let downloadu = 1;
         heart8 &= downloadu;
      let closeB = 8223792 <= heart8;
      do {
         heart8 >>= Math.min(2, Math.abs(3 / (Math.max(heart8, 4))));
         if (closeB) {
            break;
         }
      } while (((1 + heart8) > 1 && !y_positionQ) && closeB);
         y_positionQ = heart8 > 41;
         heart8 /= Math.max(5, 2);
      dplusO = `${arrowd.length}`;
   if (1 > overlayb.length) {
       let popup2 = true;
       let catalogx = String.fromCharCode(112,107,116,104,100,114,95,57,95,56,55,0);
       let type_rs: Map<any, any> = new Map([[String.fromCharCode(112,108,97,116,102,111,114,109,115,95,122,95,53,56,0),String.fromCharCode(99,111,100,101,98,108,111,99,107,115,95,117,95,52,53,0)], [String.fromCharCode(101,120,112,108,105,99,105,116,95,51,95,55,50,0),String.fromCharCode(113,117,97,110,116,105,108,101,95,119,95,57,54,0)], [String.fromCharCode(115,117,98,115,99,114,105,98,97,98,108,101,115,95,53,95,50,54,0),String.fromCharCode(102,95,52,95,112,114,105,110,116,118,97,108,0)]]);
       let nterstitials = 3;
       let china9: Array<any> = [412, 371, 944];
         nterstitials |= catalogx.length;
       let benefit5 = String.fromCharCode(106,95,49,52,95,102,97,99,116,111,114,121,0);
       let sigmobD = String.fromCharCode(104,101,97,100,105,110,103,95,117,95,56,55,0);
          let pageQ = false;
         china9.push(china9.length / (Math.max(2, 6)));
         pageQ = (pageQ ? !pageQ : !pageQ);
       let animationN = String.fromCharCode(99,111,100,101,114,115,95,49,95,56,57,0);
       let unselected0 = String.fromCharCode(110,111,110,99,111,110,116,97,99,116,95,53,95,53,57,0);
      if (!animationN.startsWith(`${popup2}`)) {
         popup2 = animationN.startsWith(`${nterstitials}`);
      }
       let giftg: Map<any, any> = new Map([[String.fromCharCode(115,104,111,119,105,110,103,95,57,95,52,0),1000], [String.fromCharCode(106,95,51,57,95,99,102,104,100,0),128], [String.fromCharCode(122,95,50,54,95,114,116,109,112,112,107,116,0),482]]);
       let resendt: Map<any, any> = new Map([[String.fromCharCode(99,102,115,116,114,101,97,109,95,56,95,55,56,0),732], [String.fromCharCode(109,95,52,54,95,113,100,101,108,116,97,0),827]]);
      if (benefit5.length > nterstitials) {
         benefit5 = `${(String.fromCharCode(65,0) == unselected0 ? unselected0.length : china9.length)}`;
      }
      while (benefit5.includes(catalogx)) {
         benefit5 += `${1 - catalogx.length}`;
         break;
      }
      if (benefit5 == String.fromCharCode(102,0)) {
         sigmobD += `${catalogx.length}`;
      }
         animationN = `${benefit5.length}`;
      let gesturesH = unselected0.length >= 7870122;
      do {
         unselected0 += `${(String.fromCharCode(88,0) == catalogx ? china9.length : catalogx.length)}`;
         if (gesturesH) {
            break;
         }
      } while ((unselected0.startsWith(`${china9.length}`)) && gesturesH);
         type_rs = new Map([[`${china9.length}`, benefit5.length | china9.length]]);
      for (let x = 0; x < 2; x++) {
         type_rs = new Map([[`${resendt.size}`, resendt.size ^ 2]]);
      }
          let stringQ: Array<any> = [804, 510];
          let upgradef: Map<any, any> = new Map([[String.fromCharCode(119,95,51,53,95,117,110,104,97,110,100,108,101,100,0),false ], [String.fromCharCode(109,117,108,116,105,100,101,115,99,114,105,112,116,111,114,95,113,95,50,0),true ]]);
         unselected0 += "2";
         stringQ.push(3);
         upgradef = new Map([[`${upgradef.size}`, stringQ.length & upgradef.size]]);
          let inactivew: Map<any, any> = new Map([[String.fromCharCode(112,97,99,101,100,95,97,95,51,51,0),741], [String.fromCharCode(105,110,116,101,114,118,97,108,115,95,50,95,49,51,0),441], [String.fromCharCode(106,97,99,111,115,117,98,95,55,95,52,51,0),855]]);
          let debugK = true;
         nterstitials &= resendt.size | benefit5.length;
         inactivew.set(`${debugK}`, ((debugK ? 3 : 4) - inactivew.size));
      overlayb.push(3 * dplusO.length);
   }
   if (1 == (holderi.size + 2) && 2 == (holderi.size + dplusO.length)) {
       let linkd = 0.0;
       let promotionl: Array<any> = [933, 350];
       let mutede = 2;
       let robotoR = String.fromCharCode(99,111,101,114,99,101,95,109,95,53,50,0);
      let sorto = promotionl.length >= 5785197;
      do {
          let module9 = String.fromCharCode(102,114,97,109,101,99,114,99,95,54,95,55,56,0);
          let infoR = 0;
          let awayN = 1;
          let episodesp = 5;
          let orientationA = String.fromCharCode(121,95,52,54,95,114,101,115,101,97,114,99,104,0);
         promotionl.push(parseInt(`${linkd}`) * 2);
         module9 = `${(orientationA == String.fromCharCode(50,0) ? awayN : orientationA.length)}`;
         infoR ^= 1;
         awayN |= 1;
         episodesp <<= Math.min(Math.abs(orientationA.length << (Math.min(Math.abs(3), 5))), 3);
         if (sorto) {
            break;
         }
      } while (((linkd + 2.21) < 2.58) && sorto);
         linkd /= Math.max(3, 2);
      for (let y = 0; y < 3; y++) {
         mutede &= 1 | parseInt(`${linkd}`);
      }
       let background9 = String.fromCharCode(110,95,49,53,95,105,109,112,108,105,99,105,116,108,121,0);
          let configurex = String.fromCharCode(120,100,97,105,95,117,95,55,52,0);
          let userj: Array<any> = [String.fromCharCode(122,95,54,50,95,100,105,115,116,112,111,105,110,116,0), String.fromCharCode(104,95,56,51,95,100,101,99,111,114,97,116,111,114,115,0)];
         linkd *= background9.length & mutede;
         configurex = `${userj.length}`;
         userj = [configurex.length - userj.length];
       let long_0dE = String.fromCharCode(111,110,101,111,102,115,95,122,95,50,48,0);
       let kuaishoup = String.fromCharCode(109,95,54,56,95,99,107,112,116,0);
      let stringsY = String.fromCharCode(97,121,54,112,0) == robotoR;
      do {
         robotoR = `${(String.fromCharCode(122,0) == background9 ? robotoR.length : background9.length)}`;
         if (stringsY) {
            break;
         }
      } while (stringsY && ((robotoR.length ^ 4) == 1));
          let trophyx = String.fromCharCode(103,101,116,120,118,97,114,95,54,95,51,53,0);
          let temperatureO = 4.0;
         promotionl = [trophyx.length];
         trophyx += `${parseInt(`${temperatureO}`) >> (Math.min(2, Math.abs(3)))}`;
         temperatureO /= Math.max(5, parseFloat(`${parseInt(`${temperatureO}`)}`));
         mutede &= background9.length;
      if (promotionl.length == mutede) {
         mutede ^= promotionl.length & 3;
      }
      while ((linkd + mutede) <= 2.59) {
         linkd *= mutede;
         break;
      }
      for (let g = 0; g < 2; g++) {
          let favoriteI = 2.0;
         promotionl.push(long_0dE.length / (Math.max(robotoR.length, 7)));
         favoriteI /= Math.max(parseFloat(`${parseInt(`${favoriteI}`)}`), 4);
      }
      dplusO = `${2 << (Math.min(1, dplusO.length))}`;
   }
      dplusO += `${dplusO.length | 3}`;
   for (let e = 0; e < 1; e++) {
       let sortt = String.fromCharCode(120,95,50,53,95,111,112,101,114,97,116,105,111,110,115,0);
       let catalog0 = true;
       let watchj = String.fromCharCode(118,95,50,49,95,102,102,109,112,101,103,0);
       let middlewarec = String.fromCharCode(114,101,109,111,116,101,108,121,95,48,95,57,49,0);
         catalog0 = !catalog0;
          let saveQ = String.fromCharCode(113,101,120,112,95,56,95,50,52,0);
          let anythink1: Map<any, any> = new Map([[String.fromCharCode(115,111,117,110,100,95,118,95,56,50,0),650], [String.fromCharCode(117,95,49,48,48,95,114,101,97,100,102,117,108,108,0),45]]);
          let searchbarz = String.fromCharCode(121,95,50,95,100,101,112,108,111,121,0);
         sortt += `${searchbarz.length}`;
         saveQ = `${(String.fromCharCode(48,0) == saveQ ? saveQ.length : anythink1.size)}`;
         anythink1 = new Map([[`${anythink1.size}`, (saveQ == String.fromCharCode(80,0) ? anythink1.size : saveQ.length)]]);
         searchbarz = `${anythink1.size << (Math.min(Math.abs(2), 3))}`;
      while (catalog0) {
         catalog0 = !catalog0 && middlewarec.length > 70;
         break;
      }
         catalog0 = sortt == watchj;
         catalog0 = sortt.length <= 48;
      let groupp = String.fromCharCode(100,116,116,110,116,0) == watchj;
      do {
         watchj += `${(middlewarec == String.fromCharCode(88,0) ? middlewarec.length : watchj.length)}`;
         if (groupp) {
            break;
         }
      } while ((middlewarec != watchj) && groupp);
         catalog0 = !catalog0;
      let checkboxU = middlewarec.length <= 8973659;
      do {
         middlewarec += `${(String.fromCharCode(105,0) == watchj ? middlewarec.length : watchj.length)}`;
         if (checkboxU) {
            break;
         }
      } while (checkboxU && (catalog0 || middlewarec.length > 1));
         sortt = `${((catalog0 ? 5 : 4))}`;
      if (middlewarec.endsWith(sortt)) {
          let filed1 = 2.0;
          let basketballV: Array<any> = [String.fromCharCode(114,97,110,103,101,95,98,95,56,48,0), String.fromCharCode(115,101,99,115,95,53,95,48,0)];
          let upgradeI: Map<any, any> = new Map([[String.fromCharCode(104,95,49,56,95,109,97,115,107,101,100,109,101,114,103,101,0),936], [String.fromCharCode(118,95,49,48,95,99,101,110,116,114,97,108,0),186], [String.fromCharCode(114,101,115,105,100,117,101,115,95,56,95,55,57,0),451]]);
          let package_oT = false;
         sortt = `${(watchj == String.fromCharCode(90,0) ? middlewarec.length : watchj.length)}`;
         filed1 -= parseFloat(`${basketballV.length ^ 2}`);
         basketballV = [1 >> (Math.min(Math.abs(parseInt(`${filed1}`)), 5))];
         upgradeI.set(`${filed1}`, basketballV.length);
         package_oT = upgradeI.size >= 74;
      }
          let x_lockL = true;
         watchj += "3";
         middlewarec += `${3 << (Math.min(4, middlewarec.length))}`;
      arrowd += `${dplusO.length * 3}`;
   }
   if (parseFloat(`${productj.length}`) <= auto_pV) {
       let modeE = String.fromCharCode(99,114,101,97,116,101,95,106,95,51,0);
          let stationZ = String.fromCharCode(114,111,117,116,105,110,115,95,52,95,51,49,0);
         modeE = `${modeE.length + 2}`;
         stationZ += `${3 & stationZ.length}`;
         modeE += `${3 ^ modeE.length}`;
         modeE += `${1 % (Math.max(6, modeE.length))}`;
      auto_pV *= parseFloat(`${overlayb.length / (Math.max(7, productj.length))}`);
   }

    return (
        <View style={{
            position: 'absolute',
            width: '100%',
            height: '80%',
            alignSelf: 'center',
            justifyContent: 'center',
            alignItems: 'center',
        }}>
            <View style={{
                backgroundColor: colors.background + 'C8',
                padding: 20,
                borderRadius: 20,
                width: '80%',
                justifyContent: 'center',
                alignItems: 'center',
            }}>
                <FastImage
                    useFastImage={true}
                    style={{
                        height: 80,
                        width: 80,
                        marginRight: 5,
                        position: "relative",
                        top: 1,
                        justifyContent: "center",
                        alignItems: "center",
                        marginBottom: 20,
                    }}
                    resizeMode={"contain"}
                    source={require("@static/images/profile/toponList.gif")}
                />

                <Text style={{
                    ...textVariants.header,
                    marginBottom: 20,
                }}>
                    上传成功
                </Text>

                <Text style={{
                    ...textVariants.body,
                    textAlign: 'center',
                    marginBottom: 10,
                }}>
                    我们将进行内容审核，24小时内返回审核结果
                </Text>
            </View>
        </View>
    );
}