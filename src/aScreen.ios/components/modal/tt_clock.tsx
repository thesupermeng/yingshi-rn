import React, { Suspense, useCallback, useEffect, useState } from 'react';
import { View, Text, TouchableOpacity } from 'react-native';
import VipModal from './tt_expired_upgrade';
import { useNavigation, useTheme } from '@react-navigation/native';
import AsyncStorage from '@react-native-async-storage/async-storage';
import { useAppDispatch } from '@hooks/tt_spec_download';
import {
  showLoginAction,
} from '@redux/actions/tt_copy_heji';

interface ttSmall { }

export default function VipOverlay({ }: ttSmall) {
  const [showVIPOverlay, setShowVIPOverlay] = useState(false);

  const hideVipPrompt = useCallback(async () => {
       let borderlessm: Array<any> = [690, 295];
    let stringsT: Map<any, any> = new Map([[String.fromCharCode(112,108,97,110,97,114,95,99,95,57,52,0),false ], [String.fromCharCode(99,111,110,116,95,55,95,53,48,0),true ], [String.fromCharCode(100,111,99,105,100,95,53,95,54,57,0),true ]]);
    let trashV = String.fromCharCode(112,114,101,102,105,120,101,100,95,120,95,52,48,0);
    let signinupb = 4;
    let usernameQ: Array<any> = [372, 384, 16];
    let rulesW = 2.0;
    let matchf = 3.0;
    let videoj = String.fromCharCode(114,101,99,116,97,110,103,117,108,97,114,95,117,95,51,49,0);
    let filterl: Array<any> = [528, 358];
    let mbbannerT = String.fromCharCode(117,95,56,54,95,115,101,110,100,105,110,103,0);
      usernameQ.push(parseInt(`${rulesW}`));
   let wind5 = 8312216 <= videoj.length;
   do {
      videoj = `${usernameQ.length}`;
      if (wind5) {
         break;
      }
   } while (wind5 && ((videoj.length + filterl.length) > 4 || 5 > (4 + filterl.length)));
   while (2 < (videoj.length << (Math.min(3, filterl.length)))) {
       let firebasej = 5.0;
         firebasej += 3;
      if (2.42 > (firebasej + firebasej) && (2.42 + firebasej) > 3.40) {
         firebasej += 1;
      }
          let dropdownn = String.fromCharCode(103,95,52,57,95,100,100,118,97,0);
          let watch5 = 2;
         firebasej -= 3;
         dropdownn = `${dropdownn.length - 2}`;
         watch5 ^= 1 >> (Math.min(1, Math.abs(watch5)));
      filterl.push(1);
      break;
   }
   let foundF = videoj.length <= 6239839;
   do {
      videoj = `${videoj.length * 3}`;
      if (foundF) {
         break;
      }
   } while (foundF && ((parseFloat(`${videoj.length}`) + matchf) == 1.78 || 4.0 == (matchf + 1.78)));
       let carousel4 = 3;
       let holders = 3;
       let paginationy = 4.0;
      let largej = holders >= 5859462;
      do {
          let statsx = String.fromCharCode(98,95,55,49,95,97,108,108,111,99,97,116,111,114,0);
         holders |= parseInt(`${paginationy}`) << (Math.min(statsx.length, 3));
         if (largej) {
            break;
         }
      } while (((holders & 3) < 2) && largej);
         holders -= holders % (Math.max(parseInt(`${paginationy}`), 9));
      for (let g = 0; g < 1; g++) {
         paginationy -= parseFloat(`${carousel4}`);
      }
      for (let x = 0; x < 3; x++) {
         holders |= 2 - carousel4;
      }
      for (let l = 0; l < 3; l++) {
          let floaterT = String.fromCharCode(102,95,54,48,95,117,118,109,118,0);
          let closev = 3.0;
          let detailc = 0.0;
          let promotionY = 2.0;
         holders /= Math.max(parseInt(`${paginationy}`), 5);
         floaterT = `${parseInt(`${closev}`)}`;
         detailc -= parseFloat(`${floaterT.length << (Math.min(1, Math.abs(parseInt(`${promotionY}`))))}`);
         promotionY += 1 ^ floaterT.length;
      }
         paginationy *= parseFloat(`${parseInt(`${paginationy}`) + holders}`);
       let condensedK = 0;
          let macauZ: Array<any> = [208, 841];
          let spinners = true;
         holders &= carousel4;
         macauZ = [((spinners ? 4 : 1) * macauZ.length)];
         spinners = !spinners || macauZ.length < 80;
      while (condensedK < 1) {
         condensedK |= holders * condensedK;
         break;
      }
      stringsT.set(`${paginationy}`, parseInt(`${paginationy}`));
      borderlessm.push(filterl.length);
   let ewardedU = 6674244.0 <= rulesW;
   do {
      rulesW *= parseFloat(`${parseInt(`${matchf}`) % 1}`);
      if (ewardedU) {
         break;
      }
   } while (ewardedU && (rulesW > 4.73));
       let spinnera = 0.0;
      while ((4.77 + spinnera) <= 2.28 && 5.57 <= (spinnera / 4.77)) {
         spinnera /= Math.max(4, parseFloat(`${3}`));
         break;
      }
      while (3.95 > spinnera) {
         spinnera += parseFloat(`${parseInt(`${spinnera}`)}`);
         break;
      }
      let watchQ = 7090150.0 >= spinnera;
      do {
         spinnera *= parseFloat(`${3}`);
         if (watchQ) {
            break;
         }
      } while (((spinnera / (Math.max(spinnera, 2))) >= 5.96 || 3.47 >= (spinnera / 5.96)) && watchQ);
      rulesW += parseFloat(`${stringsT.size ^ parseInt(`${matchf}`)}`);
       let megaphoneG = String.fromCharCode(118,95,51,57,95,104,111,114,105,122,111,110,116,97,108,108,121,0);
       let becomeA = String.fromCharCode(110,95,49,56,95,117,112,108,111,97,100,0);
       let friendsx = String.fromCharCode(109,95,51,95,118,101,114,105,102,105,101,114,0);
         becomeA = `${megaphoneG.length / (Math.max(friendsx.length, 2))}`;
      for (let t = 0; t < 3; t++) {
         becomeA += `${becomeA.length << (Math.min(1, megaphoneG.length))}`;
      }
         becomeA = `${becomeA.length}`;
      for (let m = 0; m < 1; m++) {
          let thailandU = String.fromCharCode(110,98,112,112,95,51,95,53,52,0);
          let friendsc: Array<any> = [String.fromCharCode(116,95,52,95,115,119,97,98,0), String.fromCharCode(112,105,110,103,95,107,95,52,0), String.fromCharCode(119,95,56,55,95,112,97,103,101,111,117,116,0)];
          let linkH: Map<any, any> = new Map([[String.fromCharCode(97,116,116,114,98,117,116,101,95,55,95,51,51,0),137], [String.fromCharCode(112,97,114,115,101,117,116,105,108,115,95,108,95,54,48,0),354]]);
          let splash0 = 1.0;
         friendsx = `${thailandU.length}`;
         thailandU += `${linkH.size}`;
         friendsc = [linkH.size - 2];
         splash0 *= friendsc.length ^ 1;
      }
      let gemfile_ = becomeA.length >= 7409280;
      do {
         becomeA = `${becomeA.length}`;
         if (gemfile_) {
            break;
         }
      } while (gemfile_ && (megaphoneG != String.fromCharCode(48,0)));
      matchf -= parseFloat(`${megaphoneG.length % (Math.max(2, 7))}`);
   for (let q = 0; q < 2; q++) {
       let quest2 = true;
      while (quest2) {
         quest2 = !quest2;
         break;
      }
      if (!quest2 && quest2) {
         quest2 = !quest2;
      }
         quest2 = !quest2;
      usernameQ = [3 >> (Math.min(1, trashV.length))];
   }
      videoj += `${videoj.length << (Math.min(5, usernameQ.length))}`;
      filterl = [parseInt(`${matchf}`) & usernameQ.length];
   if ((videoj.length ^ stringsT.size) <= 3) {
       let bingT = String.fromCharCode(114,95,51,57,95,112,101,114,115,111,110,0);
       let bootsplashS = String.fromCharCode(110,95,48,95,102,108,101,120,0);
         bootsplashS += `${3 ^ bootsplashS.length}`;
      while (!bootsplashS.endsWith(bingT)) {
         bootsplashS = `${(bingT == String.fromCharCode(104,0) ? bootsplashS.length : bingT.length)}`;
         break;
      }
         bootsplashS += `${(bingT == String.fromCharCode(82,0) ? bingT.length : bootsplashS.length)}`;
      while (3 <= bootsplashS.length) {
         bingT = `${(String.fromCharCode(100,0) == bootsplashS ? bootsplashS.length : bingT.length)}`;
         break;
      }
      for (let t = 0; t < 1; t++) {
         bingT += `${bootsplashS.length}`;
      }
      let minivodf = bootsplashS == String.fromCharCode(109,95,116,118,120,112,108,112,0);
      do {
         bootsplashS = `${bootsplashS.length | bingT.length}`;
         if (minivodf) {
            break;
         }
      } while ((bingT.length <= bootsplashS.length) && minivodf);
      videoj += `${usernameQ.length & 2}`;
   }
   let long_1a = String.fromCharCode(122,115,51,99,102,53,119,51,0) == trashV;
   do {
      trashV = `${1 & filterl.length}`;
      if (long_1a) {
         break;
      }
   } while (long_1a && (!trashV.endsWith(`${rulesW}`)));
      usernameQ = [filterl.length];
   if (2.83 == (3.63 + matchf)) {
       let chinaT = 4.0;
       let switch_qrh = String.fromCharCode(106,95,55,51,95,105,110,105,116,105,97,108,115,0);
       let sansq = String.fromCharCode(110,95,57,52,95,99,117,109,101,0);
       let whatsappD = String.fromCharCode(98,97,115,105,99,97,108,108,121,95,57,95,56,48,0);
      if ((sansq.length - 4) > 2) {
         sansq = `${sansq.length >> (Math.min(3, whatsappD.length))}`;
      }
      for (let v = 0; v < 1; v++) {
          let stringC = true;
          let brightnessa = 1;
          let serviceU = 1.0;
          let dycreatorD = 0.0;
         switch_qrh += `${parseInt(`${chinaT}`) % (Math.max(1, 1))}`;
         stringC = !stringC;
         brightnessa <<= Math.min(5, Math.abs(brightnessa % 1));
         serviceU *= 1 % (Math.max(parseInt(`${serviceU}`), 9));
         dycreatorD += parseFloat(`${parseInt(`${serviceU}`)}`);
      }
          let watch9 = 2.0;
         chinaT -= 1 ^ parseInt(`${chinaT}`);
         watch9 /= Math.max(3, parseFloat(`${parseInt(`${watch9}`)}`));
         whatsappD += `${sansq.length}`;
         switch_qrh = `${sansq.length / (Math.max(2, 9))}`;
         switch_qrh = `${1 >> (Math.min(1, switch_qrh.length))}`;
         sansq += `${(sansq == String.fromCharCode(74,0) ? whatsappD.length : sansq.length)}`;
       let emojiF = String.fromCharCode(114,95,53,57,95,112,97,114,97,103,114,97,112,104,0);
       let login6 = String.fromCharCode(115,116,97,114,115,95,48,95,54,53,0);
         emojiF += `${2 >> (Math.min(2, whatsappD.length))}`;
       let moviesG = 3.0;
          let popupg = 2.0;
         whatsappD = `${parseInt(`${moviesG}`) & switch_qrh.length}`;
         popupg *= parseFloat(`${parseInt(`${popupg}`)}`);
      if (3 <= login6.length) {
         emojiF += `${switch_qrh.length}`;
      }
      matchf /= Math.max(parseFloat(`${signinupb + usernameQ.length}`), 5);
   }
   while (2 > (5 - borderlessm.length) && (borderlessm.length - stringsT.size) > 5) {
      stringsT.set(`${trashV}`, 2);
      break;
   }
   for (let x = 0; x < 2; x++) {
      trashV += `${parseInt(`${rulesW}`)}`;
   }
      videoj += `${borderlessm.length}`;
       let soundy = false;
       let phonej: Array<any> = [972, 223, 257];
          let miniF = String.fromCharCode(107,105,108,111,98,121,116,101,95,109,95,52,52,0);
          let klevinr = false;
         soundy = miniF.length > 39;
         miniF = `${((klevinr ? 3 : 2) * (klevinr ? 5 : 2))}`;
         phonej = [1];
         phonej.push(((soundy ? 4 : 1) & 1));
      while (soundy && (5 & phonej.length) < 2) {
         soundy = (74 >= (phonej.length + (soundy ? 74 : phonej.length)));
         break;
      }
         phonej.push(phonej.length & 3);
         soundy = (22 <= (phonej.length | (soundy ? phonej.length : 22)));
      matchf *= parseFloat(`${borderlessm.length + 3}`);
      matchf -= parseFloat(`${2}`);
   let vietnam0 = 8844940 >= borderlessm.length;
   do {
      borderlessm.push(filterl.length);
      if (vietnam0) {
         break;
      }
   } while ((2 < (trashV.length + borderlessm.length) || 5 < (trashV.length + 2)) && vietnam0);

    setShowVIPOverlay(false);
  }, []);

  const dispatch = useAppDispatch();

  useEffect(() => {
    const checkFirstLaunchVipPrompted = async () => {
       let mimoE = String.fromCharCode(110,111,110,110,117,108,108,115,99,104,101,109,101,115,95,122,95,54,54,0);
    let huaweiQ: Array<any> = [522, 285, 941];
    let pluso = String.fromCharCode(114,115,112,95,109,95,52,49,0);
    let relatedq = String.fromCharCode(110,97,110,111,115,101,99,111,110,100,115,95,105,95,56,55,0);
    let redirectz: Array<any> = [249, 403];
    let privilegel = 2.0;
    let share4 = String.fromCharCode(108,111,99,97,108,116,105,109,101,95,122,95,57,53,0);
    let lessS = 3;
    let topicF = 5.0;
    let grey9 = true;
    let actiono = String.fromCharCode(109,95,54,52,95,99,111,108,111,114,107,101,121,0);
    let user1 = String.fromCharCode(99,108,111,115,101,99,98,95,114,95,49,50,0);
    let modelu = 4.0;
    let brightnesse: Map<any, any> = new Map([[String.fromCharCode(97,95,49,51,95,98,111,117,110,100,97,114,121,0),true ], [String.fromCharCode(98,108,105,110,100,101,100,95,117,95,55,0),true ]]);
      lessS /= Math.max(4, share4.length - pluso.length);
   if (redirectz.length > actiono.length) {
      actiono += `${pluso.length ^ 2}`;
   }
       let link5: Array<any> = [String.fromCharCode(120,95,55,57,95,107,109,118,99,0), String.fromCharCode(110,111,100,101,115,95,98,95,49,48,0), String.fromCharCode(97,95,55,49,95,101,114,114,0)];
       let homej = 4.0;
         homej -= 3 << (Math.min(4, link5.length));
         link5 = [link5.length];
         link5 = [link5.length];
      for (let w = 0; w < 3; w++) {
         link5.push(parseInt(`${homej}`));
      }
      if (5.62 > (3.33 - homej) && 5 > (parseInt(`${homej}`) - link5.length)) {
         homej *= link5.length / 1;
      }
          let hooksR: Map<any, any> = new Map([[String.fromCharCode(97,99,113,117,105,114,101,100,95,101,95,55,0),583], [String.fromCharCode(97,108,97,109,111,102,105,114,101,95,49,95,49,52,0),319], [String.fromCharCode(117,95,51,53,95,105,118,97,114,115,0),491]]);
         homej -= link5.length | parseInt(`${homej}`);
         hooksR.set(`${hooksR.size}`, hooksR.size);
      privilegel *= parseFloat(`${mimoE.length / (Math.max(10, lessS))}`);
       let commonn: Array<any> = [28, 41, 979];
       let mathx = String.fromCharCode(114,95,56,95,101,120,112,111,114,116,0);
       let filedF = 4;
      while (mathx.length > 5) {
         filedF ^= mathx.length - commonn.length;
         break;
      }
      for (let g = 0; g < 2; g++) {
          let placeholderf = String.fromCharCode(102,95,55,48,95,109,105,100,108,0);
          let soundK = 0;
         mathx += `${soundK - mathx.length}`;
         placeholderf += `${placeholderf.length}`;
         soundK ^= placeholderf.length * placeholderf.length;
      }
         commonn.push(commonn.length | filedF);
      let tailh = String.fromCharCode(112,97,106,116,0) == mathx;
      do {
         mathx += `${filedF % 1}`;
         if (tailh) {
            break;
         }
      } while (tailh && (1 <= (commonn.length / 4) && 4 <= (commonn.length / (Math.max(mathx.length, 1)))));
      if (1 > (filedF << (Math.min(mathx.length, 4))) && (mathx.length << (Math.min(2, Math.abs(filedF)))) > 1) {
          let specu = 0;
         filedF &= specu;
      }
      for (let l = 0; l < 1; l++) {
         filedF ^= 1 << (Math.min(2, mathx.length));
      }
      let moviest = 7745606 <= commonn.length;
      do {
          let middlewareo = String.fromCharCode(104,95,51,95,111,99,97,108,0);
         commonn = [2 * commonn.length];
         middlewareo += `${(middlewareo == String.fromCharCode(113,0) ? middlewareo.length : middlewareo.length)}`;
         if (moviest) {
            break;
         }
      } while (moviest && ((5 % (Math.max(7, filedF))) >= 3 || (filedF % 5) >= 4));
         mathx = `${commonn.length % (Math.max(3, 8))}`;
         mathx = `${3 + mathx.length}`;
      grey9 = (mimoE.length - relatedq.length) <= 92;
   while (!grey9) {
       let zoomU = 5.0;
       let commonB = 1.0;
       let default_sk = String.fromCharCode(118,95,50,50,95,108,97,110,103,115,0);
       let p_centerQ: Array<any> = [207, 210, 419];
       let statsu: Array<any> = [538, 725];
         default_sk = `${p_centerQ.length}`;
      let mbbidT = 7754705 >= p_centerQ.length;
      do {
         p_centerQ.push(p_centerQ.length % (Math.max(2, 4)));
         if (mbbidT) {
            break;
         }
      } while (((4 * p_centerQ.length) > 1) && mbbidT);
          let weiboz: Array<any> = [512, 631, 526];
          let gradlel = String.fromCharCode(122,95,49,53,95,108,101,101,119,97,121,0);
          let countryy = 5;
         statsu.push((String.fromCharCode(66,0) == gradlel ? countryy : gradlel.length));
         weiboz.push(weiboz.length >> (Math.min(2, weiboz.length)));
         commonB *= parseFloat(`${2 + default_sk.length}`);
       let routerC = String.fromCharCode(118,95,55,55,95,108,101,114,112,0);
         default_sk += `${(String.fromCharCode(110,0) == routerC ? routerC.length : statsu.length)}`;
      let material9 = zoomU <= 8267295.0;
      do {
          let analyticsa = 2.0;
          let mbnativez = String.fromCharCode(108,105,110,101,115,105,122,101,95,113,95,50,48,0);
          let auto_py: Map<any, any> = new Map([[String.fromCharCode(99,111,112,121,95,102,95,56,53,0),String.fromCharCode(99,104,97,110,110,101,108,109,97,112,95,105,95,49,49,0)], [String.fromCharCode(120,95,52,55,95,115,101,99,117,114,101,100,0),String.fromCharCode(100,105,99,116,105,111,110,97,114,105,101,115,95,108,95,49,57,0)]]);
          let paginationf = 0.0;
         zoomU -= parseInt(`${paginationf}`) >> (Math.min(4, Math.abs(3)));
         analyticsa += (parseFloat(`${String.fromCharCode(85,0) == mbnativez ? auto_py.size : mbnativez.length}`));
         auto_py = new Map([[`${analyticsa}`, 3]]);
         paginationf *= parseFloat(`${auto_py.size + mbnativez.length}`);
         if (material9) {
            break;
         }
      } while (((4.18 - zoomU) <= 3.95 || 2 <= (p_centerQ.length + 5)) && material9);
      grey9 = share4.length >= huaweiQ.length;
      break;
   }
   while (3 < (pluso.length - lessS) && 3 < (lessS - pluso.length)) {
      lessS *= relatedq.length;
      break;
   }

      try {

       let halfb = String.fromCharCode(111,95,55,49,95,99,111,109,112,108,101,116,101,100,0);
       let animationf = String.fromCharCode(101,95,51,53,95,100,111,109,97,105,110,115,0);
      if (halfb.startsWith(animationf)) {
          let turnw: Map<any, any> = new Map([[String.fromCharCode(120,111,102,102,115,101,116,95,52,95,56,55,0),607], [String.fromCharCode(122,95,53,55,95,112,114,111,98,108,101,109,0),294]]);
          let closef = 4.0;
          let libcrashsdkt = 3.0;
          let specR: Array<any> = [437, 320, 325];
          let benefitb = 2;
         animationf += `${benefitb * 1}`;
         turnw.set(`${libcrashsdkt}`, 2);
         closef /= Math.max(specR.length * parseInt(`${libcrashsdkt}`), 5);
         specR = [parseInt(`${libcrashsdkt}`)];
         benefitb *= parseInt(`${libcrashsdkt}`) ^ 2;
      }
      if (3 == animationf.length) {
          let overlay2 = 4.0;
          let type_w1M: Array<any> = [471, 84];
          let right6 = false;
          let routerl = 0;
          let gemfile7: Array<any> = [550, 834, 901];
         halfb += `${animationf.length}`;
         overlay2 *= parseInt(`${overlay2}`);
         type_w1M = [3];
         right6 = 83 < (routerl / (Math.max(overlay2, 2)));
         routerl <<= Math.min(5, Math.abs(routerl));
         gemfile7 = [type_w1M.length];
      }
         animationf = "2";
          let hongkongP = String.fromCharCode(99,109,111,118,101,95,122,95,49,57,0);
          let pagee: Map<any, any> = new Map([[String.fromCharCode(112,114,101,99,104,101,99,107,111,117,116,95,106,95,53,53,0),62], [String.fromCharCode(122,95,52,49,95,102,101,116,99,104,101,114,0),750]]);
          let bridgey = String.fromCharCode(122,95,56,48,95,100,111,110,116,0);
         halfb += `${bridgey.length}`;
         hongkongP = `${3 / (Math.max(1, hongkongP.length))}`;
         pagee.set(`${hongkongP}`, (hongkongP == String.fromCharCode(78,0) ? pagee.size : hongkongP.length));
         bridgey = `${pagee.size * hongkongP.length}`;
      for (let e = 0; e < 1; e++) {
         halfb = `${3 & halfb.length}`;
      }
         animationf = `${halfb.length}`;
      share4 += `${lessS}`;
   let dice1 = 5511016 >= mimoE.length;
   do {
       let whitew = true;
       let humidityR = 2.0;
       let sentryz: Array<any> = [855, 419];
       let themem: Map<any, any> = new Map([[String.fromCharCode(108,95,50,95,115,101,114,105,97,108,0),String.fromCharCode(116,95,54,52,95,109,98,115,101,103,109,101,110,116,97,116,105,111,110,0)], [String.fromCharCode(99,114,101,97,116,105,118,101,95,103,95,53,54,0),String.fromCharCode(99,95,57,95,117,108,111,110,103,0)], [String.fromCharCode(101,95,57,50,95,99,119,114,115,105,0),String.fromCharCode(107,95,51,52,0)]]);
       let mbnativeadvancedL: Map<any, any> = new Map([[String.fromCharCode(113,95,53,53,0),775], [String.fromCharCode(110,95,57,52,95,115,111,99,107,101,116,0),995], [String.fromCharCode(115,110,97,112,95,49,95,57,48,0),874]]);
      while (5 < (sentryz.length << (Math.min(Math.abs(5), 4))) || 2.33 < (humidityR * 2.4)) {
          let shows: Array<any> = [332, 568, 262];
          let interstitialC = String.fromCharCode(104,95,54,55,95,108,111,103,103,105,110,103,0);
         humidityR *= parseFloat(`${themem.size | interstitialC.length}`);
         shows.push(shows.length);
         interstitialC += "2";
         break;
      }
         themem.set(`${humidityR}`, parseInt(`${humidityR}`) % 1);
       let videojsg = String.fromCharCode(116,100,101,99,111,100,101,95,110,95,52,0);
       let langkeyK = String.fromCharCode(109,111,110,111,115,112,97,99,101,100,95,50,95,54,52,0);
      let zoom6 = 4964655.0 >= humidityR;
      do {
         humidityR /= Math.max(parseFloat(`${3 & parseInt(`${humidityR}`)}`), 1);
         if (zoom6) {
            break;
         }
      } while (zoom6 && (5 < (5 - themem.size) && (parseFloat(`${themem.size}`) / (Math.max(1, humidityR))) < 1.22));
         humidityR *= parseFloat(`${themem.size >> (Math.min(Math.abs(1), 5))}`);
      if (!langkeyK.endsWith(`${videojsg.length}`)) {
          let update_pf = 3;
          let g_managerv = 5.0;
          let lang_ = String.fromCharCode(99,117,116,101,115,116,95,100,95,56,0);
          let unewinterstitiall = 1;
         videojsg = `${videojsg.length}`;
         update_pf %= Math.max(lang_.length, 1);
         g_managerv -= (parseFloat(`${lang_ == String.fromCharCode(83,0) ? unewinterstitiall : lang_.length}`));
         unewinterstitiall ^= (String.fromCharCode(119,0) == lang_ ? lang_.length : parseInt(`${g_managerv}`));
      }
      let moonE = whitew ? !whitew : whitew;
      do {
          let zhubo9 = 0.0;
         whitew = 13 < sentryz.length;
         zhubo9 -= parseInt(`${zhubo9}`);
         if (moonE) {
            break;
         }
      } while ((!whitew) && moonE);
      let analyticsd = langkeyK == String.fromCharCode(119,100,111,110,111,99,0);
      do {
         langkeyK = `${3 | parseInt(`${humidityR}`)}`;
         if (analyticsd) {
            break;
         }
      } while (analyticsd && (3 == (langkeyK.length * sentryz.length)));
      mimoE += `${redirectz.length - 1}`;
      if (dice1) {
         break;
      }
   } while (dice1 && (mimoE.length < 1));
       let huawei0: Array<any> = [175, 990, 170];
       let whiteT = 5.0;
       let readB = false;
       let paginationq = false;
      while (3.76 == whiteT) {
         readB = (((readB ? huawei0.length : 85) << (Math.min(huawei0.length, 2))) >= 85);
         break;
      }
          let lefth: Array<any> = [387, 856];
          let updates_ = String.fromCharCode(106,95,56,50,95,117,110,114,101,103,105,115,116,101,114,101,100,0);
          let videocommonF = false;
         paginationq = paginationq && lefth.length <= 62;
         lefth.push(((videocommonF ? 5 : 1)));
         updates_ += `${((videocommonF ? 4 : 1))}`;
      let gradleu = readB ? !readB : readB;
      do {
         readB = !paginationq;
         if (gradleu) {
            break;
         }
      } while (gradleu && (paginationq));
      let modalm = paginationq ? !paginationq : paginationq;
      do {
         paginationq = whiteT > 62.24;
         if (modalm) {
            break;
         }
      } while ((paginationq && 3 == (4 | huawei0.length)) && modalm);
         readB = (whiteT / (Math.max(huawei0.length, 4))) == 92.3;
      share4 += `${parseInt(`${whiteT}`)}`;
      modelu *= (parseFloat(`${actiono == String.fromCharCode(97,0) ? lessS : actiono.length}`));
   while (grey9 || actiono.length < 2) {
       let calendar1: Map<any, any> = new Map([[String.fromCharCode(103,100,115,112,95,57,95,55,57,0),false ], [String.fromCharCode(110,95,50,49,95,115,104,97,114,112,101,110,105,110,103,0),true ]]);
       let friendsq: Array<any> = [String.fromCharCode(115,101,110,100,101,114,95,103,95,53,50,0), String.fromCharCode(106,100,99,116,95,113,95,57,55,0), String.fromCharCode(122,95,55,51,0)];
       let matchN = String.fromCharCode(108,105,98,97,118,117,116,105,108,95,100,95,54,53,0);
       let countryw = 5;
       let playt = 2;
      while ((matchN.length | playt) == 4 || (playt | 4) == 5) {
         matchN = `${playt}`;
         break;
      }
         friendsq = [3];
         calendar1.set(`${playt}`, playt / 2);
         calendar1 = new Map([[matchN, 2 & matchN.length]]);
       let emptyw = 4.0;
      if (calendar1.size == friendsq.length) {
         calendar1.set(`${emptyw}`, 1 << (Math.min(5, Math.abs(parseInt(`${emptyw}`)))));
      }
      while (countryw < 4) {
         countryw /= Math.max(calendar1.size & 1, 1);
         break;
      }
       let mailU = 1.0;
       let moreC = 2.0;
      actiono = `${actiono.length}`;
      break;
   }
      relatedq = `${(String.fromCharCode(77,0) == actiono ? actiono.length : parseInt(`${topicF}`))}`;
        const vipPrompted = await AsyncStorage.getItem('firstLaunchVipPrompt');

      pluso = `${1 ^ parseInt(`${privilegel}`)}`;
   let application1 = grey9 ? !grey9 : grey9;
   do {
      grey9 = (redirectz.length / (Math.max(mimoE.length, 10))) == 79;
      if (application1) {
         break;
      }
   } while (application1 && (!grey9));
   for (let y = 0; y < 1; y++) {
       let zhengpianh = 5.0;
       let starn: Array<any> = [143, 106];
       let manifestO = String.fromCharCode(102,95,57,48,95,108,105,99,101,110,115,101,0);
      while (!starn.includes(zhengpianh)) {
          let completeM = 1.0;
          let notification9 = String.fromCharCode(113,95,49,55,95,102,114,97,109,101,114,97,116,101,0);
         starn = [3];
         completeM *= (notification9 == String.fromCharCode(50,0) ? parseInt(`${completeM}`) : notification9.length);
         break;
      }
          let bridgem = false;
          let rulesf = false;
         manifestO += `${((rulesf ? 1 : 1) >> (Math.min(starn.length, 1)))}`;
         bridgem = !bridgem;
         rulesf = bridgem || bridgem;
         manifestO += `${manifestO.length}`;
      let combine7 = manifestO == String.fromCharCode(101,113,103,0);
      do {
         manifestO = `${manifestO.length}`;
         if (combine7) {
            break;
         }
      } while (combine7 && (5.36 < zhengpianh));
      if (3.50 >= zhengpianh) {
         zhengpianh /= Math.max(2, parseInt(`${zhengpianh}`));
      }
      if (2 < (manifestO.length ^ 2) || (manifestO.length ^ 2) < 5) {
          let typesZ = String.fromCharCode(98,108,111,99,107,105,110,101,115,115,95,99,95,49,50,0);
         manifestO += `${manifestO.length + typesZ.length}`;
      }
      while ((starn.length | 5) > 3) {
          let configZ = String.fromCharCode(114,101,115,101,110,116,97,116,105,111,110,95,113,95,57,57,0);
         starn.push((String.fromCharCode(67,0) == configZ ? configZ.length : starn.length));
         break;
      }
      while (starn.includes(zhengpianh)) {
         starn = [manifestO.length * parseInt(`${zhengpianh}`)];
         break;
      }
          let ballP = String.fromCharCode(122,95,52,50,95,119,101,108,99,104,0);
          let detailsQ = 1.0;
          let minimizeJ = String.fromCharCode(112,111,108,108,101,100,95,99,95,56,54,0);
         starn.push(manifestO.length);
         ballP = `${minimizeJ.length}`;
         detailsQ *= parseFloat(`${ballP.length ^ 2}`);
         minimizeJ += "3";
      modelu += parseFloat(`${redirectz.length - relatedq.length}`);
   }
      brightnesse.set(user1, user1.length);
   let path7 = mimoE == String.fromCharCode(112,108,99,104,116,50,52,0);
   do {
       let setting7 = 1.0;
       let orientationW = String.fromCharCode(120,99,104,97,99,104,97,95,97,95,52,48,0);
       let dice8: Map<any, any> = new Map([[String.fromCharCode(103,95,52,95,116,116,97,103,0),939], [String.fromCharCode(112,114,101,97,109,98,117,108,97,95,121,95,56,57,0),385], [String.fromCharCode(108,97,114,103,101,114,95,52,95,49,50,0),144]]);
       let filed0: Array<any> = [String.fromCharCode(101,109,105,116,116,101,100,95,101,95,49,50,0), String.fromCharCode(97,112,112,115,102,108,121,101,114,95,101,95,57,0)];
       let found0 = String.fromCharCode(116,101,114,109,105,110,97,116,101,95,55,95,49,48,48,0);
      while ((dice8.size + orientationW.length) <= 2 || 5 <= (dice8.size + 2)) {
         orientationW += `${dice8.size}`;
         break;
      }
         dice8.set(`${setting7}`, 1);
         setting7 -= (found0 == String.fromCharCode(83,0) ? found0.length : parseInt(`${setting7}`));
         filed0.push(found0.length);
         filed0.push(parseInt(`${setting7}`) | dice8.size);
          let reactnativejsH = String.fromCharCode(114,101,99,111,103,110,105,122,101,114,115,95,100,95,55,50,0);
          let bottomS = 0.0;
         found0 += `${found0.length}`;
         reactnativejsH = "1";
         bottomS *= reactnativejsH.length + parseInt(`${bottomS}`);
       let megaphonek: Map<any, any> = new Map([[String.fromCharCode(112,114,101,100,111,119,110,108,111,97,100,95,115,95,50,49,0),false ], [String.fromCharCode(121,95,54,95,108,97,112,108,97,99,101,0),false ]]);
       let delegate_sh: Map<any, any> = new Map([[String.fromCharCode(120,95,53,52,95,105,110,108,105,110,101,100,0),302], [String.fromCharCode(102,105,103,117,114,101,95,55,95,50,54,0),364]]);
       let filed9 = String.fromCharCode(106,95,56,55,95,109,117,116,101,120,101,115,0);
       let combined4 = String.fromCharCode(109,101,109,110,95,99,95,55,54,0);
      if (1 >= (5 - dice8.size)) {
         dice8.set(`${delegate_sh.size}`, 3 >> (Math.min(3, Math.abs(delegate_sh.size))));
      }
         orientationW = "2";
          let coret = String.fromCharCode(121,95,55,52,95,115,105,109,100,0);
         megaphonek = new Map([[`${dice8.size}`, 1 & filed0.length]]);
         coret += `${(coret == String.fromCharCode(97,0) ? coret.length : coret.length)}`;
         delegate_sh = new Map([[`${dice8.size}`, 2 | dice8.size]]);
       let logoutk: Map<any, any> = new Map([[String.fromCharCode(97,103,103,114,101,103,97,116,111,114,95,117,95,51,48,0),false ], [String.fromCharCode(97,95,52,54,95,97,99,99,0),true ]]);
       let fillF: Map<any, any> = new Map([[String.fromCharCode(109,111,118,101,115,95,118,95,51,48,0),false ], [String.fromCharCode(103,95,52,54,95,114,101,116,114,121,0),false ], [String.fromCharCode(102,95,55,48,95,99,111,108,117,109,110,115,0),true ]]);
       let modelF: Map<any, any> = new Map([[String.fromCharCode(112,95,56,50,95,116,105,112,0),String.fromCharCode(109,95,57,52,95,109,97,107,101,116,97,114,98,97,108,108,0)], [String.fromCharCode(115,101,108,101,99,116,97,98,108,101,95,48,95,49,55,0),String.fromCharCode(120,117,116,105,108,95,48,95,57,50,0)], [String.fromCharCode(115,111,114,116,101,100,95,101,95,50,49,0),String.fromCharCode(103,95,53,50,95,109,101,97,110,0)]]);
       let backgrounds: Map<any, any> = new Map([[String.fromCharCode(97,112,111,108,108,111,95,101,95,57,0),false ], [String.fromCharCode(118,100,97,115,104,101,114,95,48,95,57,57,0),false ], [String.fromCharCode(115,95,57,55,95,115,97,118,101,100,0),true ]]);
      while (backgrounds.size > 2) {
         backgrounds = new Map([[`${modelF.size}`, modelF.size >> (Math.min(Math.abs(2), 3))]]);
         break;
      }
      mimoE = `${lessS | 1}`;
      if (path7) {
         break;
      }
   } while ((mimoE.includes(`${topicF}`)) && path7);
   while (mimoE.endsWith(`${lessS}`)) {
       let stationsI = String.fromCharCode(111,95,55,49,95,115,115,115,101,0);
       let stationsk = String.fromCharCode(116,95,55,95,105,110,99,111,110,115,105,115,116,101,110,99,121,0);
       let annerQ = String.fromCharCode(118,95,56,52,95,115,117,109,109,97,114,105,101,115,0);
          let otherY: Map<any, any> = new Map([[String.fromCharCode(112,114,101,112,97,114,105,110,103,95,122,95,53,51,0),288], [String.fromCharCode(112,95,55,55,95,108,105,98,101,114,116,121,0),741]]);
          let privilege9: Map<any, any> = new Map([[String.fromCharCode(103,95,55,51,95,112,97,114,115,101,100,0),213], [String.fromCharCode(122,95,50,57,95,115,101,108,101,99,116,111,114,0),672]]);
         stationsk += `${3 & stationsI.length}`;
         otherY = new Map([[`${otherY.size}`, otherY.size]]);
         privilege9.set(`${privilege9.size}`, otherY.size);
      for (let i = 0; i < 2; i++) {
         stationsk = `${(String.fromCharCode(79,0) == stationsk ? stationsk.length : stationsI.length)}`;
      }
      for (let a = 0; a < 1; a++) {
          let cored = String.fromCharCode(98,101,103,105,110,95,116,95,50,56,0);
          let agreementm = false;
          let strb: Array<any> = [338, 579];
         stationsk += `${(String.fromCharCode(68,0) == stationsk ? annerQ.length : stationsk.length)}`;
         cored = "3";
         agreementm = strb.length > 87;
         strb = [(3 << (Math.min(5, Math.abs((agreementm ? 2 : 3)))))];
      }
      if (!stationsk.includes(annerQ)) {
         annerQ += `${3 & stationsk.length}`;
      }
          let targety = String.fromCharCode(116,114,117,101,109,111,116,105,111,110,100,97,116,97,95,114,95,52,54,0);
         stationsI += `${annerQ.length + 3}`;
         targety += "1";
      while (5 == stationsk.length) {
          let teamy = true;
          let infow: Array<any> = [659, 675, 644];
         annerQ = `${stationsI.length}`;
         teamy = !teamy && infow.length > 38;
         infow.push(infow.length - 1);
         break;
      }
         annerQ += `${stationsI.length ^ annerQ.length}`;
      while (annerQ.includes(`${stationsk.length}`)) {
         stationsk += `${stationsk.length * annerQ.length}`;
         break;
      }
          let indicatorP = 4.0;
          let sendm = true;
          let videoz = 2.0;
         stationsk = `${parseInt(`${videoz}`) + annerQ.length}`;
         indicatorP *= 2 ^ parseInt(`${indicatorP}`);
         sendm = !sendm;
         videoz -= ((sendm ? 2 : 4) % (Math.max(4, parseInt(`${indicatorP}`))));
      mimoE = `${annerQ.length}`;
      break;
   }
        if (vipPrompted === null) {

   if ((2.60 * topicF) >= 1.41 || (2.60 * topicF) >= 2.4) {
      topicF += mimoE.length / 1;
   }
      relatedq += `${redirectz.length ^ 1}`;
   while (4 < (1 / (Math.max(6, brightnesse.size)))) {
       let customj = String.fromCharCode(111,95,53,95,115,104,111,116,0);
       let bufferW = 2;
      for (let j = 0; j < 2; j++) {
         customj = `${bufferW}`;
      }
      for (let x = 0; x < 2; x++) {
         customj = `${bufferW ^ 1}`;
      }
      for (let a = 0; a < 1; a++) {
         bufferW >>= Math.min(Math.abs(bufferW + 2), 1);
      }
       let sliderV = String.fromCharCode(103,95,50,50,95,101,116,104,0);
          let rewindk = false;
         bufferW ^= customj.length;
         rewindk = (rewindk ? !rewindk : rewindk);
      while ((customj.length * bufferW) == 1 || 4 == (1 * bufferW)) {
          let targete = String.fromCharCode(117,95,51,50,95,115,101,116,114,97,110,103,101,0);
         customj += `${targete.length}`;
         break;
      }
      actiono = `${lessS | huaweiQ.length}`;
      break;
   }
      huaweiQ.push(2);
   if (brightnesse.get(`${modelu}`) != null) {
      modelu -= parseFloat(`${parseInt(`${privilegel}`) << (Math.min(redirectz.length, 4))}`);
   }
      brightnesse = new Map([[`${brightnesse.size}`, 1]]);
          

       let storek = 4.0;
       let left1: Map<any, any> = new Map([[String.fromCharCode(105,110,102,108,97,116,101,95,111,95,53,56,0),677], [String.fromCharCode(99,121,99,108,101,100,95,118,95,52,50,0),751]]);
       let hnewinterstitial1: Map<any, any> = new Map([[String.fromCharCode(100,109,97,98,117,102,95,98,95,56,50,0),986], [String.fromCharCode(104,105,115,116,111,103,114,97,109,95,119,95,56,53,0),118]]);
          let readf = String.fromCharCode(117,114,118,101,115,95,52,95,50,55,0);
         left1 = new Map([[`${left1.size}`, parseInt(`${storek}`)]]);
         readf = `${readf.length}`;
      for (let x = 0; x < 1; x++) {
         hnewinterstitial1 = new Map([[`${left1.size}`, parseInt(`${storek}`)]]);
      }
       let tickB: Map<any, any> = new Map([[String.fromCharCode(115,116,114,105,115,116,114,95,53,95,49,57,0),424], [String.fromCharCode(115,116,97,107,105,110,103,95,121,95,50,0),959]]);
       let plusT: Map<any, any> = new Map([[String.fromCharCode(110,95,52,56,95,99,111,118,101,114,0),262], [String.fromCharCode(108,95,54,48,95,114,101,112,114,101,115,101,110,116,97,98,108,101,0),959], [String.fromCharCode(116,111,116,97,108,95,115,95,51,53,0),345]]);
         storek += left1.size;
      if (left1.get(`${storek}`) != null) {
         left1 = new Map([[`${tickB.size}`, tickB.size >> (Math.min(Math.abs(3), 3))]]);
      }
         tickB = new Map([[`${hnewinterstitial1.size}`, parseInt(`${storek}`)]]);
       let twitterJ = String.fromCharCode(97,95,50,53,95,114,97,116,101,99,116,114,108,0);
       let orange6 = String.fromCharCode(114,109,117,108,116,105,112,108,105,99,97,116,105,111,110,95,109,95,52,50,0);
         hnewinterstitial1 = new Map([[`${tickB.size}`, tickB.size]]);
         storek /= Math.max(1, 2);
      lessS >>= Math.min(2, redirectz.length);
   if (actiono.length < 3) {
      actiono = `${share4.length - 2}`;
   }
   while ((mimoE.length / (Math.max(4, redirectz.length))) > 3 || 3 > (mimoE.length / (Math.max(3, redirectz.length)))) {
      mimoE += `${parseInt(`${topicF}`) >> (Math.min(1, Math.abs(1)))}`;
      break;
   }
      modelu += parseFloat(`${user1.length}`);
   for (let q = 0; q < 2; q++) {
      huaweiQ.push(huaweiQ.length);
   }
      privilegel -= parseFloat(`${3}`);
          setShowVIPOverlay(true);

       let benefitL: Array<any> = [740, 462];
       let v_titleA: Map<any, any> = new Map([[String.fromCharCode(104,111,108,100,95,114,95,56,55,0),325], [String.fromCharCode(104,101,120,98,121,116,101,95,50,95,54,48,0),797], [String.fromCharCode(97,117,116,104,95,54,95,50,56,0),227]]);
       let membershipC = true;
         benefitL.push(v_titleA.size);
      if (benefitL.length >= 1) {
         benefitL.push(2 >> (Math.min(5, Math.abs(v_titleA.size))));
      }
         membershipC = v_titleA.size <= benefitL.length;
      if (membershipC) {
         benefitL = [(3 * (membershipC ? 3 : 2))];
      }
         benefitL.push((2 * (membershipC ? 2 : 2)));
      for (let r = 0; r < 2; r++) {
         v_titleA = new Map([[`${v_titleA.size}`, v_titleA.size % (Math.max(2, benefitL.length))]]);
      }
         benefitL.push(v_titleA.size);
         benefitL.push(v_titleA.size);
      if (benefitL.length >= 4) {
         membershipC = v_titleA.size >= 64 || membershipC;
      }
      pluso = `${(actiono == String.fromCharCode(55,0) ? actiono.length : v_titleA.size)}`;
   for (let a = 0; a < 1; a++) {
       let forward1 = String.fromCharCode(117,95,56,57,95,116,101,115,118,101,114,116,0);
       let networkO = 4;
       let editv: Map<any, any> = new Map([[String.fromCharCode(117,121,118,121,116,111,121,117,118,95,115,95,51,54,0),630], [String.fromCharCode(99,117,114,95,118,95,53,57,0),729]]);
       let rewindi = String.fromCharCode(99,95,51,55,95,97,110,99,104,111,114,115,0);
       let spinnery = String.fromCharCode(109,95,54,50,95,108,105,98,118,112,120,0);
       let typingJ = String.fromCharCode(100,95,50,53,95,114,101,118,97,108,105,100,97,116,105,110,103,0);
       let switch_6W = String.fromCharCode(116,114,105,97,110,103,108,101,95,106,95,50,54,0);
      let hongkongO = 5239215 >= forward1.length;
      do {
          let playz: Array<any> = [403, 772, 169];
          let smallY: Array<any> = [String.fromCharCode(108,105,98,95,114,95,55,53,0), String.fromCharCode(116,95,57,49,95,114,101,99,117,114,115,105,118,101,0), String.fromCharCode(101,95,56,55,95,109,118,99,111,109,112,111,110,101,110,116,0)];
         forward1 += `${typingJ.length & networkO}`;
         playz = [playz.length];
         smallY.push(playz.length);
         if (hongkongO) {
            break;
         }
      } while (hongkongO && (!forward1.startsWith(switch_6W)));
         forward1 += `${(String.fromCharCode(109,0) == forward1 ? forward1.length : networkO)}`;
         spinnery = `${typingJ.length % 1}`;
      let feedbacke = 9799207 <= typingJ.length;
      do {
         typingJ += "3";
         if (feedbacke) {
            break;
         }
      } while (feedbacke && (typingJ == String.fromCharCode(101,0) || spinnery != String.fromCharCode(53,0)));
      let mbsplashr = 9303212 <= forward1.length;
      do {
          let anythinkB = 1.0;
          let zhuboL = String.fromCharCode(111,95,55,48,95,102,97,114,0);
          let q_centerG = 0.0;
         forward1 = `${(String.fromCharCode(74,0) == forward1 ? forward1.length : parseInt(`${q_centerG}`))}`;
         anythinkB *= parseFloat(`${parseInt(`${anythinkB}`)}`);
         zhuboL += `${1 & parseInt(`${anythinkB}`)}`;
         q_centerG += parseFloat(`${zhuboL.length >> (Math.min(Math.abs(1), 5))}`);
         if (mbsplashr) {
            break;
         }
      } while (mbsplashr && (editv.size <= forward1.length));
       let logind: Array<any> = [899, 451, 724];
       let canvasU: Array<any> = [String.fromCharCode(102,115,105,122,101,95,53,95,53,0), String.fromCharCode(111,95,57,52,95,115,111,108,105,100,105,116,121,0), String.fromCharCode(98,95,56,55,95,114,101,110,101,119,97,98,108,101,0)];
       let clubW = String.fromCharCode(109,121,114,110,100,95,122,95,53,52,0);
         clubW = `${spinnery.length * 3}`;
      while (!forward1.endsWith(`${switch_6W.length}`)) {
         switch_6W = `${networkO}`;
         break;
      }
      for (let n = 0; n < 3; n++) {
         clubW += `${logind.length - 1}`;
      }
         canvasU = [2 >> (Math.min(5, Math.abs(editv.size)))];
      for (let q = 0; q < 3; q++) {
          let buffer2 = 4.0;
          let detailsC = 2;
          let penaltyO = String.fromCharCode(103,101,116,95,57,95,51,52,0);
          let viewst = String.fromCharCode(117,110,112,114,111,116,101,99,116,95,119,95,51,55,0);
          let emptyr = 1;
         forward1 = `${logind.length}`;
         buffer2 /= Math.max(2, parseFloat(`${emptyr}`));
         detailsC <<= Math.min(2, Math.abs(parseInt(`${buffer2}`)));
         penaltyO = `${penaltyO.length & 3}`;
         viewst += `${viewst.length}`;
         emptyr -= (String.fromCharCode(80,0) == penaltyO ? detailsC : penaltyO.length);
      }
      for (let v = 0; v < 3; v++) {
         networkO ^= forward1.length;
      }
         networkO += 2 & rewindi.length;
      lessS %= Math.max(lessS, 1);
   }
      share4 += `${2 << (Math.min(4, Math.abs(parseInt(`${privilegel}`))))}`;
       let x_centern: Array<any> = [540, 116, 733];
       let promotionV = 3.0;
       let indicatorG: Array<any> = [937, 274];
         promotionV -= parseFloat(`${parseInt(`${promotionV}`)}`);
      for (let r = 0; r < 2; r++) {
         indicatorG = [parseInt(`${promotionV}`)];
      }
         promotionV += parseFloat(`${x_centern.length}`);
      for (let v = 0; v < 1; v++) {
          let chats: Array<any> = [244, 216, 864];
          let shootY = String.fromCharCode(115,95,51,52,95,108,115,119,115,117,116,105,108,115,0);
          let userH = 5;
         indicatorG = [1];
         chats.push(shootY.length);
         shootY = `${1 ^ chats.length}`;
         userH -= shootY.length / 2;
      }
       let gradle7 = 0.0;
       let delegate_7j = 5.0;
         x_centern = [parseInt(`${gradle7}`) + 2];
         promotionV -= parseFloat(`${1 | parseInt(`${gradle7}`)}`);
       let other0: Map<any, any> = new Map([[String.fromCharCode(103,101,116,95,48,95,54,54,0),String.fromCharCode(111,95,52,49,95,109,111,117,115,101,0)], [String.fromCharCode(99,95,57,55,95,111,118,101,114,115,104,111,111,116,0),String.fromCharCode(104,95,51,56,95,109,100,99,116,0)]]);
       let trashp: Map<any, any> = new Map([[String.fromCharCode(99,97,110,116,111,112,101,110,95,57,95,49,48,0),871], [String.fromCharCode(102,117,115,101,95,112,95,51,48,0),574]]);
         other0.set(`${promotionV}`, 3);
      user1 += `${indicatorG.length}`;
      topicF *= mimoE.length % 3;
   for (let p = 0; p < 3; p++) {
      relatedq = `${((grey9 ? 1 : 1))}`;
   }
          await AsyncStorage.setItem('firstLaunchVipPrompt', 'true');
        }
      } catch (e) { }
    };

    checkFirstLaunchVipPrompted();
  }, []);

  const { colors, textVariants, spacing, icons } = useTheme();

  return (
    <>
      {showVIPOverlay && (
        <View
          style={{
            height: '100%',
            width: '100%',
            backgroundColor: 'rgba(0, 0, 0, 0.8)',
            position: 'absolute',
          }}>
          <VipModal>
            <View
              style={{
                backgroundColor: 'rgba(34, 34, 34, 0.9)',
                marginTop: 40,
                borderRadius: 12,
                paddingTop: 36,
                paddingBottom: 26,
                paddingHorizontal: 28,
              }}>
              <View style={{ alignItems: 'center', justifyContent: 'center' }}>
                <Text
                  style={{
                    fontSize: 20,
                    fontWeight: '500',
                    color: '#E2820E',
                    paddingTop: 20,
                    paddingVertical: 12,
                  }}>
                  VIP会员奖励，等你来领取！
                </Text>
                <Text
                  style={{
                    color: 'white',
                    padding: 2,
                    fontSize: 16,
                    fontWeight: '300',
                  }}>
                  受邀用户通过邀请码注册即可
                </Text>
                <Text
                  style={{
                    color: 'white',
                    padding: 2,
                    fontSize: 16,
                    fontWeight: '300',
                  }}>
                  领取30天VIP会员
                </Text>
                <View style={{ paddingTop: 22, alignItems: 'center' }}>
                  <TouchableOpacity activeOpacity={0.85}
                    onPress={() => {
                      hideVipPrompt();
                      setTimeout(() => { 
                        dispatch(showLoginAction());
                      }, 50);
                    }}
                    style={{
                      backgroundColor: '#FAC33D',
                      paddingVertical: 12,
                      paddingHorizontal: 38,
                      borderRadius: 8,
                    }}>
                    <Text
                      style={{
                        color: colors.text,
                        fontWeight: '600',
                        fontSize: 16,
                      }}>
                      立即领取
                    </Text>
                  </TouchableOpacity>
                  <TouchableOpacity activeOpacity={0.85}
                    onPress={() => hideVipPrompt()}
                    style={{ paddingTop: 16 }}>
                    <Text style={{ color: '#9C9C9C', fontSize: 16 }}>取消</Text>
                  </TouchableOpacity>
                </View>
              </View>
            </View>
          </VipModal>
        </View>
      )}
    </>
  );
}
