import React, {Suspense, useCallback, useEffect, useState} from 'react';
import {View, Text, TouchableOpacity} from 'react-native';
import VipModal from './yys_topic_gdtadv';
import {useNavigation, useTheme} from '@react-navigation/native';
import AsyncStorage from '@react-native-async-storage/async-storage';
import {useAppDispatch} from '@hooks/yys_frame';
import {showLoginAction} from '@redux/actions/yys_runtimescheduler';

interface yys_ConfigureUimanager {
  remainingDay: number;
  showVIPOverlay: boolean;
  setShowVIPOverlay: any;
}

export default function ExpiredOverlay({
  remainingDay = 0,
  showVIPOverlay = false,
  setShowVIPOverlay = () => {
       let forwardy: Array<any> = [648, 887, 981];
    let handlerx = String.fromCharCode(121,95,55,57,95,104,116,109,108,109,97,114,107,117,112,0);
    let fileO = 2.0;
    let collectionq = 5;
    let specn = 5.0;
    let tooltipsK = true;
    let stationq: Array<any> = [603, 351, 174];
    let historyQ: Array<any> = [726, 638];
    let path3 = 4;
    let roboto_: Array<any> = [55, 488];
    let star4 = 1.0;
    let merger8 = String.fromCharCode(121,95,49,95,100,111,112,115,0);
    let historyJ = 5.0;
    let usernameO = 1.0;
    let catagoryD = 0;
      roboto_.push(handlerx.length >> (Math.min(Math.abs(1), 4)));
   let colorsp = 6297569 <= collectionq;
   do {
      collectionq <<= Math.min(5, forwardy.length);
      if (colorsp) {
         break;
      }
   } while ((4 == (collectionq >> (Math.min(Math.abs(3), 4)))) && colorsp);
      forwardy = [parseInt(`${fileO}`) / 3];
   while (fileO < collectionq) {
      fileO /= Math.max(2, parseInt(`${specn}`));
      break;
   }
      roboto_.push(roboto_.length);
      collectionq *= historyQ.length;
      historyJ -= (parseFloat(`${handlerx == String.fromCharCode(77,0) ? handlerx.length : parseInt(`${specn}`)}`));
   for (let t = 0; t < 2; t++) {
      handlerx = `${path3}`;
   }
      star4 += parseFloat(`${parseInt(`${specn}`) << (Math.min(handlerx.length, 5))}`);
   if (2 >= (handlerx.length / 4)) {
       let sentryW = String.fromCharCode(114,111,116,97,116,105,110,103,95,110,95,53,51,0);
         sentryW = `${(String.fromCharCode(86,0) == sentryW ? sentryW.length : sentryW.length)}`;
          let shows = 4.0;
          let leakcheckere = String.fromCharCode(105,110,100,101,118,115,95,57,95,50,48,0);
          let acceptedb: Array<any> = [902, 15, 446];
         sentryW += `${(sentryW == String.fromCharCode(101,0) ? leakcheckere.length : sentryW.length)}`;
         shows *= parseFloat(`${acceptedb.length}`);
         leakcheckere = `${3 | parseInt(`${shows}`)}`;
         acceptedb.push(acceptedb.length);
         sentryW += `${sentryW.length}`;
      handlerx += `${merger8.length - 2}`;
   }
      collectionq -= 1;
   for (let g = 0; g < 2; g++) {
      tooltipsK = stationq.length <= parseInt(`${fileO}`);
   }
      historyJ -= parseFloat(`${1 + path3}`);
   for (let y = 0; y < 3; y++) {
      roboto_.push(forwardy.length);
   }
   while (forwardy.length >= handlerx.length) {
      forwardy.push((String.fromCharCode(66,0) == handlerx ? path3 : handlerx.length));
      break;
   }
       let windw = 5.0;
       let selected3 = 4;
       let pangle5 = String.fromCharCode(97,115,115,111,99,105,97,116,101,95,52,95,56,50,0);
         pangle5 += `${pangle5.length}`;
         pangle5 += `${selected3 << (Math.min(5, Math.abs(3)))}`;
         pangle5 = `${parseInt(`${windw}`) / 1}`;
      let stylesp = String.fromCharCode(107,99,121,56,0) == pangle5;
      do {
          let short_1oW = String.fromCharCode(113,95,52,53,95,112,105,101,0);
          let crossW = true;
          let libjsijniprofilerg = String.fromCharCode(116,104,114,101,97,100,109,101,115,115,97,103,101,95,57,95,55,56,0);
         pangle5 += `${short_1oW.length | pangle5.length}`;
         short_1oW = `${libjsijniprofilerg.length}`;
         crossW = libjsijniprofilerg.length > 25 && !crossW;
         if (stylesp) {
            break;
         }
      } while (((1 * pangle5.length) == 5) && stylesp);
          let turno: Array<any> = [721, 913];
         selected3 %= Math.max(2 / (Math.max(parseInt(`${windw}`), 8)), 1);
         turno.push(turno.length);
      while ((parseInt(`${windw}`) - pangle5.length) <= 4) {
         pangle5 = "3";
         break;
      }
      for (let z = 0; z < 2; z++) {
          let moonz = 2.0;
          let main_gZ = String.fromCharCode(106,95,52,54,95,98,105,110,107,100,97,116,97,0);
          let mbnativeO: Map<any, any> = new Map([[String.fromCharCode(107,95,52,48,95,110,105,108,0),988], [String.fromCharCode(114,101,116,114,121,97,98,108,101,95,51,95,57,49,0),78], [String.fromCharCode(106,109,108,105,115,116,95,111,95,57,54,0),793]]);
         windw /= Math.max(1, parseFloat(`${pangle5.length}`));
         moonz += parseFloat(`${2 % (Math.max(5, mbnativeO.size))}`);
         main_gZ += `${parseInt(`${moonz}`) << (Math.min(main_gZ.length, 4))}`;
         mbnativeO = new Map([[`${mbnativeO.size}`, mbnativeO.size & 2]]);
      }
      let filedR = selected3 >= 8708498;
      do {
         selected3 ^= 3;
         if (filedR) {
            break;
         }
      } while ((selected3 >= pangle5.length) && filedR);
      let zhubol = 8273564.0 <= windw;
      do {
         windw *= parseFloat(`${parseInt(`${windw}`) - 1}`);
         if (zhubol) {
            break;
         }
      } while ((1.99 == (selected3 - windw)) && zhubol);
      fileO *= 3;
   if ((4.68 - specn) > 5.52) {
       let plashB = 0.0;
       let sound9 = String.fromCharCode(105,110,115,116,97,110,99,101,115,95,113,95,57,56,0);
       let combinedn = String.fromCharCode(97,110,115,105,95,51,95,56,57,0);
         combinedn = `${(String.fromCharCode(75,0) == sound9 ? parseInt(`${plashB}`) : sound9.length)}`;
         plashB /= Math.max(1, parseFloat(`${combinedn.length}`));
       let controla: Map<any, any> = new Map([[String.fromCharCode(117,112,100,97,116,101,115,95,99,95,50,54,0),true ], [String.fromCharCode(121,117,118,114,103,98,95,97,95,50,56,0),false ], [String.fromCharCode(98,95,54,54,95,101,108,101,109,101,110,116,115,0),true ]]);
       let actiona: Map<any, any> = new Map([[String.fromCharCode(97,95,51,55,0),861], [String.fromCharCode(110,116,101,114,110,97,108,95,119,95,49,0),593], [String.fromCharCode(111,95,52,56,95,110,116,101,114,110,97,108,0),253]]);
          let readB = 0;
         plashB -= parseFloat(`${controla.size}`);
         readB /= Math.max(2, 2);
         plashB += parseFloat(`${sound9.length >> (Math.min(Math.abs(3), 4))}`);
      let calendare = combinedn.length <= 8889653;
      do {
          let bootsplashM = 1.0;
          let z_viewx = 0.0;
          let ncopy_oX = String.fromCharCode(115,109,104,100,95,116,95,54,56,0);
         combinedn = `${ncopy_oX.length - parseInt(`${z_viewx}`)}`;
         bootsplashM /= Math.max(2, parseFloat(`${parseInt(`${bootsplashM}`) ^ 1}`));
         z_viewx -= parseFloat(`${2 >> (Math.min(Math.abs(parseInt(`${bootsplashM}`)), 4))}`);
         if (calendare) {
            break;
         }
      } while (((actiona.size & 4) >= 5) && calendare);
       let orange5 = String.fromCharCode(118,95,49,55,95,108,111,117,100,110,111,114,109,0);
      while (sound9.length > combinedn.length) {
         combinedn = `${3 - sound9.length}`;
         break;
      }
      let desco = sound9.length >= 8492581;
      do {
         sound9 = `${controla.size}`;
         if (desco) {
            break;
         }
      } while ((orange5 != String.fromCharCode(83,0) || sound9 == String.fromCharCode(87,0)) && desco);
      path3 &= sound9.length << (Math.min(3, combinedn.length));
   }
   while ((parseInt(`${star4}`) - collectionq) > 1 && (star4 - parseFloat(`${collectionq}`)) > 1.61) {
       let klevinR = String.fromCharCode(115,105,116,101,109,97,112,95,99,95,56,49,0);
       let predictionG = 4;
       let codegenK: Map<any, any> = new Map([[String.fromCharCode(97,95,52,49,95,98,117,102,102,101,114,115,0),204], [String.fromCharCode(99,111,108,108,101,99,116,97,98,108,101,95,116,95,53,55,0),22]]);
       let modalb = false;
      for (let i = 0; i < 2; i++) {
          let proxyl = String.fromCharCode(116,95,55,55,95,102,105,108,101,0);
         modalb = String.fromCharCode(122,0) == proxyl;
      }
         klevinR += `${((modalb ? 1 : 5) + codegenK.size)}`;
         predictionG |= 2;
      for (let i = 0; i < 2; i++) {
         klevinR += "2";
      }
      let f_centerK = 5437357 >= codegenK.size;
      do {
         codegenK.set(`${modalb}`, (1 + (modalb ? 2 : 4)));
         if (f_centerK) {
            break;
         }
      } while ((3 < codegenK.size) && f_centerK);
          let attributedstringn = 2.0;
         klevinR += `${parseInt(`${attributedstringn}`) | 2}`;
       let huaweio: Map<any, any> = new Map([[String.fromCharCode(110,111,116,101,115,95,102,95,49,50,0),204], [String.fromCharCode(114,101,113,117,101,115,116,101,114,115,95,107,95,52,54,0),804], [String.fromCharCode(114,101,103,105,115,116,114,97,110,116,95,53,95,49,48,48,0),604]]);
      for (let l = 0; l < 3; l++) {
          let dycreatorY: Array<any> = [463, 114, 922];
          let configure3 = 2.0;
          let indicatord = 0.0;
          let modal2 = true;
         predictionG *= 3 - parseInt(`${configure3}`);
         dycreatorY.push(((modal2 ? 5 : 4) | parseInt(`${indicatord}`)));
         configure3 *= (parseFloat(`${(modal2 ? 5 : 2) ^ parseInt(`${indicatord}`)}`));
      }
      collectionq -= 1;
      break;
   }
   if (4 < (collectionq << (Math.min(Math.abs(5), 2)))) {
      collectionq <<= Math.min(4, handlerx.length);
   }
      stationq.push(2);
      collectionq ^= 3;
   let mbridgeB = tooltipsK ? !tooltipsK : tooltipsK;
   do {
      tooltipsK = path3 >= forwardy.length;
      if (mbridgeB) {
         break;
      }
   } while (mbridgeB && (3.80 <= (5.71 / (Math.max(10, specn)))));
   while (fileO <= 2.10) {
       let uimanagerN: Array<any> = [167, 136, 297];
      while (uimanagerN.length < 5) {
          let libsgcoreP = 2.0;
         uimanagerN.push(parseInt(`${libsgcoreP}`));
         break;
      }
       let saveN: Map<any, any> = new Map([[String.fromCharCode(99,116,120,112,95,115,95,52,56,0),370], [String.fromCharCode(97,101,118,97,108,95,111,95,57,57,0),379], [String.fromCharCode(111,118,101,114,114,105,100,101,110,0),249]]);
         uimanagerN = [2 - uimanagerN.length];
      historyQ.push(3 << (Math.min(3, stationq.length)));
      break;
   }
   let update_aeL = 5094239.0 <= historyJ;
   do {
       let bannera = String.fromCharCode(116,105,108,101,109,107,97,95,114,95,56,50,0);
       let halfu = 5.0;
       let robotoU: Array<any> = [537, 7, 889];
       let storek = String.fromCharCode(98,95,52,57,95,111,117,116,103,111,105,110,103,0);
       let main_hO = String.fromCharCode(121,95,49,57,0);
         robotoU.push(1);
      let photox = String.fromCharCode(49,112,48,101,97,113,108,0) == storek;
      do {
          let weibo0 = 3;
          let sigmobI = 2.0;
          let buttonX = 0;
          let profile1 = String.fromCharCode(115,95,54,54,95,99,111,109,102,111,114,116,0);
         storek += `${parseInt(`${sigmobI}`) | 3}`;
         weibo0 >>= Math.min(Math.abs(1 >> (Math.min(4, profile1.length))), 1);
         sigmobI += buttonX >> (Math.min(Math.abs(2), 1));
         buttonX *= 3;
         profile1 += `${buttonX | profile1.length}`;
         if (photox) {
            break;
         }
      } while ((!storek.endsWith(main_hO)) && photox);
         halfu *= parseFloat(`${main_hO.length}`);
         main_hO = `${robotoU.length | 3}`;
      for (let o = 0; o < 1; o++) {
         main_hO = `${bannera.length % (Math.max(3, 3))}`;
      }
         halfu += parseFloat(`${main_hO.length}`);
         robotoU = [storek.length];
      if (main_hO.length < storek.length) {
          let sendB = 5;
          let optionsP: Map<any, any> = new Map([[String.fromCharCode(121,95,54,55,95,110,111,110,110,117,108,108,100,101,115,116,105,110,97,116,105,111,110,0),568], [String.fromCharCode(103,95,54,52,95,116,119,111,112,97,115,115,0),735], [String.fromCharCode(115,95,52,51,95,115,108,105,112,112,97,103,101,0),873]]);
          let invite4 = String.fromCharCode(106,95,57,48,95,98,117,108,108,101,116,0);
          let final_vp = String.fromCharCode(122,95,52,53,95,110,112,117,116,0);
          let attributedstringt: Array<any> = [131, 925, 229];
         main_hO += "2";
         sendB &= sendB >> (Math.min(final_vp.length, 4));
         optionsP = new Map([[`${sendB}`, sendB]]);
         invite4 += `${final_vp.length + 1}`;
         attributedstringt.push(attributedstringt.length);
      }
      while (4 <= storek.length) {
         storek += `${main_hO.length * 1}`;
         break;
      }
      for (let i = 0; i < 2; i++) {
         robotoU = [bannera.length + storek.length];
      }
      for (let y = 0; y < 1; y++) {
         robotoU = [(String.fromCharCode(78,0) == bannera ? bannera.length : main_hO.length)];
      }
         storek += `${1 * robotoU.length}`;
         robotoU.push((String.fromCharCode(105,0) == storek ? storek.length : parseInt(`${halfu}`)));
      for (let z = 0; z < 3; z++) {
         storek = `${robotoU.length & parseInt(`${halfu}`)}`;
      }
      while (!bannera.includes(`${robotoU.length}`)) {
         bannera += `${robotoU.length}`;
         break;
      }
      historyJ /= Math.max(5, parseFloat(`${storek.length % (Math.max(2, 4))}`));
      if (update_aeL) {
         break;
      }
   } while (update_aeL && (stationq.length > 5));
       let mimoP: Map<any, any> = new Map([[String.fromCharCode(100,99,115,99,116,112,95,113,95,57,0),264], [String.fromCharCode(98,114,111,97,100,99,97,115,116,115,95,101,95,53,48,0),446]]);
       let pressurer = String.fromCharCode(99,111,108,108,95,102,95,50,57,0);
       let ajaxb: Map<any, any> = new Map([[String.fromCharCode(101,95,56,53,95,117,115,97,98,108,101,0),String.fromCharCode(115,95,50,56,95,102,116,115,121,121,0)], [String.fromCharCode(108,105,110,109,97,116,104,95,104,95,56,57,0),String.fromCharCode(102,95,55,49,95,100,117,114,105,110,103,0)]]);
      while (3 > (mimoP.size - 4)) {
         pressurer += `${1 | mimoP.size}`;
         break;
      }
      let success9 = pressurer == String.fromCharCode(101,115,122,112,100,103,109,112,0);
      do {
         pressurer = `${ajaxb.size}`;
         if (success9) {
            break;
         }
      } while ((5 >= (ajaxb.size >> (Math.min(Math.abs(1), 5)))) && success9);
          let containeri = String.fromCharCode(110,95,49,50,95,101,112,122,115,0);
          let entryg: Array<any> = [839, 225, 290];
         pressurer += `${pressurer.length}`;
         containeri = "2";
         entryg.push(entryg.length << (Math.min(containeri.length, 2)));
      while (1 >= (4 / (Math.max(4, ajaxb.size))) && 3 >= (ajaxb.size / 4)) {
         ajaxb.set(pressurer, (String.fromCharCode(117,0) == pressurer ? pressurer.length : ajaxb.size));
         break;
      }
         pressurer = `${pressurer.length}`;
         mimoP = new Map([[`${ajaxb.size}`, 1]]);
      let penaltyJ = mimoP.size >= 6285542;
      do {
          let adultM = 3.0;
          let expiredE = true;
          let langa = String.fromCharCode(114,101,115,116,111,114,101,95,109,95,49,52,0);
          let ajaxy = String.fromCharCode(109,106,112,101,103,100,101,99,95,117,95,53,57,0);
          let friendsP = String.fromCharCode(115,104,97,95,108,95,55,50,0);
         mimoP.set(ajaxy, 2);
         adultM /= Math.max(3, 2);
         expiredE = friendsP == String.fromCharCode(69,0) && adultM <= 84.0;
         langa = `${friendsP.length}`;
         ajaxy = `${parseInt(`${adultM}`) << (Math.min(Math.abs(2), 5))}`;
         if (penaltyJ) {
            break;
         }
      } while (penaltyJ && (Array.from(mimoP.values()).includes(ajaxb.size)));
      let hejiy = String.fromCharCode(121,109,116,107,54,0) == pressurer;
      do {
          let iconc = true;
          let graphH: Map<any, any> = new Map([[String.fromCharCode(98,95,49,57,95,101,108,101,109,101,110,116,115,0),782], [String.fromCharCode(105,110,99,114,95,48,95,50,55,0),148]]);
          let libfollyM = String.fromCharCode(112,95,55,52,95,115,121,109,108,105,110,107,0);
         pressurer += "2";
         iconc = graphH.size == 29;
         graphH.set(`${libfollyM}`, libfollyM.length);
         if (hejiy) {
            break;
         }
      } while (hejiy && (5 >= (pressurer.length / 1)));
      if ((pressurer.length << (Math.min(5, Math.abs(ajaxb.size)))) >= 2 || (pressurer.length << (Math.min(5, Math.abs(ajaxb.size)))) >= 2) {
         ajaxb.set(`${mimoP.size}`, ajaxb.size % 1);
      }
      tooltipsK = collectionq == 97;
      handlerx = `${3 & parseInt(`${historyJ}`)}`;
   for (let a = 0; a < 3; a++) {
      historyQ = [collectionq / (Math.max(forwardy.length, 6))];
   }
       let settingP: Array<any> = [717, 448, 286];
       let libfbP = String.fromCharCode(110,97,110,95,49,95,49,50,0);
       let nextw: Array<any> = [String.fromCharCode(98,95,56,48,95,112,97,114,97,103,114,97,112,104,0), String.fromCharCode(114,116,102,95,112,95,49,52,0)];
      if (3 < nextw.length) {
         nextw.push(libfbP.length);
      }
         settingP.push(1 & libfbP.length);
          let adultL = 5;
          let fastW = 1.0;
         nextw.push(1);
         adultL <<= Math.min(Math.abs(parseInt(`${fastW}`)), 5);
         fastW *= 3 / (Math.max(3, parseInt(`${fastW}`)));
         nextw.push(libfbP.length * 3);
          let reducer8 = String.fromCharCode(108,97,121,101,114,99,111,110,116,101,120,116,95,57,95,57,50,0);
          let stringsB = String.fromCharCode(102,101,116,99,104,101,114,115,95,99,95,53,51,0);
          let logoP = false;
         libfbP += `${libfbP.length}`;
         reducer8 = `${stringsB.length / (Math.max(3, 6))}`;
         stringsB = "3";
          let l_count0 = true;
         libfbP += `${nextw.length}`;
         l_count0 = (!l_count0 ? l_count0 : l_count0);
         libfbP = `${nextw.length >> (Math.min(Math.abs(3), 5))}`;
      if (libfbP.includes(`${nextw.length}`)) {
         libfbP = `${1 * settingP.length}`;
      }
      let toponD = String.fromCharCode(105,116,54,97,49,97,54,101,0) == libfbP;
      do {
         libfbP = `${nextw.length}`;
         if (toponD) {
            break;
         }
      } while (toponD && (1 <= (3 * settingP.length)));
      collectionq /= Math.max(5, nextw.length);
   for (let e = 0; e < 1; e++) {
      merger8 += `${collectionq & 2}`;
   }
   while (5.49 == specn) {
      specn /= Math.max(forwardy.length / 3, 4);
      break;
   }
   for (let t = 0; t < 1; t++) {
      collectionq += parseInt(`${historyJ}`);
   }
      star4 /= Math.max(3, parseFloat(`${handlerx.length}`));
       let borderless3: Map<any, any> = new Map([[String.fromCharCode(100,95,57,49,0),909], [String.fromCharCode(114,101,103,101,116,95,116,95,49,49,0),80], [String.fromCharCode(115,97,109,112,108,101,114,97,116,101,95,100,95,52,49,0),354]]);
      for (let c = 0; c < 3; c++) {
         borderless3 = new Map([[`${borderless3.size}`, 2]]);
      }
      while (5 > (borderless3.size / 4) || (borderless3.size / (Math.max(4, 9))) > 1) {
         borderless3 = new Map([[`${borderless3.size}`, borderless3.size + borderless3.size]]);
         break;
      }
      if (borderless3.size > 2) {
          let auto_mwQ = false;
         borderless3.set(`${auto_mwQ}`, borderless3.size);
      }
      collectionq %= Math.max(4, catagoryD % (Math.max(stationq.length, 7)));
   while (5 == (1 >> (Math.min(2, Math.abs(catagoryD)))) || 1 == (1 >> (Math.min(5, Math.abs(catagoryD))))) {
       let descD = String.fromCharCode(101,120,116,114,97,99,116,105,111,110,95,112,95,53,50,0);
       let ewarded3 = String.fromCharCode(100,101,102,97,117,108,116,95,118,95,51,53,0);
       let sheetM = String.fromCharCode(118,115,101,114,118,105,99,101,95,116,95,56,0);
       let bingx = String.fromCharCode(115,116,111,114,109,98,105,114,100,95,122,95,57,0);
       let stylesL = 5;
      for (let y = 0; y < 3; y++) {
         bingx = `${sheetM.length}`;
      }
         sheetM = `${sheetM.length * bingx.length}`;
       let action9 = String.fromCharCode(103,95,56,52,95,106,115,111,110,0);
      for (let e = 0; e < 3; e++) {
         bingx += `${ewarded3.length}`;
      }
          let x_playerN = String.fromCharCode(112,115,110,114,104,118,115,95,57,95,57,52,0);
          let policyL = String.fromCharCode(115,117,109,109,101,100,95,105,95,51,52,0);
         stylesL %= Math.max(action9.length - 1, 3);
         x_playerN += `${(String.fromCharCode(81,0) == x_playerN ? x_playerN.length : policyL.length)}`;
         policyL = `${x_playerN.length & 2}`;
       let launcherT = 1.0;
      for (let x = 0; x < 1; x++) {
          let carouselp: Map<any, any> = new Map([[String.fromCharCode(110,95,54,48,95,100,105,115,99,117,115,115,0),String.fromCharCode(114,101,109,97,105,110,105,110,103,95,52,95,49,0)], [String.fromCharCode(119,95,55,49,95,116,114,97,110,115,102,111,114,109,115,0),String.fromCharCode(110,95,49,50,95,101,110,100,0)], [String.fromCharCode(101,110,99,111,100,101,114,95,122,95,48,0),String.fromCharCode(102,95,52,55,95,99,97,110,100,108,101,0)]]);
         bingx += `${bingx.length}`;
         carouselp.set(`${carouselp.size}`, carouselp.size % 1);
      }
          let gradlewh = String.fromCharCode(100,101,99,111,100,101,120,95,118,95,49,49,0);
          let armvaq = String.fromCharCode(121,95,50,54,95,114,109,115,116,114,101,97,109,0);
         bingx = "3";
         gradlewh += `${armvaq.length}`;
         armvaq = `${gradlewh.length - 2}`;
      for (let m = 0; m < 3; m++) {
          let relatedb = String.fromCharCode(109,117,110,108,111,99,107,95,115,95,51,51,0);
          let dplusu = 3;
         descD = "3";
         relatedb = `${relatedb.length}`;
         dplusu /= Math.max(relatedb.length, 5);
      }
      while ((launcherT * 4.66) <= 3.1) {
          let register_mN = false;
          let dangerM = String.fromCharCode(116,114,101,101,116,111,107,95,51,95,54,57,0);
         ewarded3 += `${bingx.length}`;
         register_mN = ((dangerM.length << (Math.min(1, Math.abs((register_mN ? dangerM.length : 3))))) >= 3);
         break;
      }
      let loginj = action9.length <= 7878330;
      do {
         action9 += `${(action9 == String.fromCharCode(81,0) ? action9.length : descD.length)}`;
         if (loginj) {
            break;
         }
      } while ((!action9.startsWith(`${stylesL}`)) && loginj);
      let downloading3 = descD == String.fromCharCode(114,111,121,55,0);
      do {
         descD += `${parseInt(`${launcherT}`) << (Math.min(action9.length, 5))}`;
         if (downloading3) {
            break;
         }
      } while (downloading3 && (!action9.endsWith(descD)));
      while (sheetM == bingx) {
         bingx += `${parseInt(`${launcherT}`) / (Math.max(action9.length, 9))}`;
         break;
      }
         stylesL -= ewarded3.length;
         stylesL *= (String.fromCharCode(56,0) == bingx ? sheetM.length : bingx.length);
      catagoryD &= handlerx.length;
      break;
   }
   for (let q = 0; q < 2; q++) {
      specn -= merger8.length * stationq.length;
   }
   if (4 == (5 * collectionq)) {
      collectionq -= parseInt(`${fileO}`);
   }
   let libfbQ = 8862552 >= catagoryD;
   do {
      catagoryD ^= 1;
      if (libfbQ) {
         break;
      }
   } while (libfbQ && (catagoryD >= 1));
   let crownA = handlerx == String.fromCharCode(105,118,101,117,95,97,98,49,98,0);
   do {
      handlerx = `${parseInt(`${usernameO}`)}`;
      if (crownA) {
         break;
      }
   } while (crownA && (5 == (2 - handlerx.length)));
   for (let p = 0; p < 2; p++) {
       let areaN = String.fromCharCode(108,95,51,49,95,112,111,110,103,0);
       let emptyL = String.fromCharCode(105,110,116,102,114,0);
       let twitteri = 5;
       let viewst = String.fromCharCode(117,110,101,110,99,114,121,112,116,101,100,95,117,95,49,50,0);
       let score4 = true;
       let hejil: Map<any, any> = new Map([[String.fromCharCode(109,105,120,112,97,110,101,108,0),705], [String.fromCharCode(113,95,50,53,95,114,101,108,101,97,115,101,0),802], [String.fromCharCode(115,119,105,102,116,0),707]]);
      if (!emptyL.includes(`${hejil.size}`)) {
          let large5 = 4.0;
          let productz = 2.0;
          let kleviny: Map<any, any> = new Map([[String.fromCharCode(114,101,116,97,105,110,115,95,99,95,53,51,0),65], [String.fromCharCode(100,95,57,51,95,109,111,100,101,0),475]]);
         hejil.set(`${large5}`, 1);
         large5 *= parseFloat(`${2}`);
         productz *= kleviny.size;
         kleviny = new Map([[`${kleviny.size}`, kleviny.size & parseInt(`${productz}`)]]);
      }
      let feedback6 = 7378155 >= emptyL.length;
      do {
         emptyL += `${twitteri << (Math.min(areaN.length, 3))}`;
         if (feedback6) {
            break;
         }
      } while (feedback6 && (4 >= (hejil.size ^ emptyL.length)));
      if (2 >= viewst.length) {
         viewst = `${hejil.size / 3}`;
      }
      while (hejil.size >= 5) {
         score4 = !score4;
         break;
      }
      let videoe = String.fromCharCode(52,116,109,109,100,97,122,0) == emptyL;
      do {
          let ksadm = 1.0;
          let collectionH = 5.0;
         emptyL += `${((score4 ? 2 : 5) * 3)}`;
         ksadm /= Math.max(2 ^ parseInt(`${collectionH}`), 4);
         collectionH -= 1 | parseInt(`${ksadm}`);
         if (videoe) {
            break;
         }
      } while ((viewst.includes(emptyL)) && videoe);
         emptyL += `${(3 + (score4 ? 4 : 4))}`;
      if (!Array.from(hejil.values()).includes(twitteri)) {
          let condensed7 = String.fromCharCode(101,95,51,54,95,100,121,108,105,98,115,0);
          let anythinkk = 2.0;
         twitteri -= areaN.length | parseInt(`${anythinkk}`);
         condensed7 = "1";
         anythinkk -= parseFloat(`${2 / (Math.max(5, condensed7.length))}`);
      }
         twitteri %= Math.max(3, (viewst == String.fromCharCode(68,0) ? viewst.length : twitteri));
      while (!emptyL.includes(`${score4}`)) {
          let bridgep = 0;
          let fastE = 2.0;
          let androidO = false;
          let dangerO = String.fromCharCode(109,95,57,55,95,99,116,105,111,110,0);
         score4 = twitteri > 40;
         bridgep *= 1 & bridgep;
         fastE *= parseFloat(`${bridgep >> (Math.min(Math.abs(parseInt(`${fastE}`)), 4))}`);
         androidO = dangerO.length == 12 || androidO;
         dangerO += `${parseInt(`${fastE}`)}`;
         break;
      }
      for (let v = 0; v < 1; v++) {
         areaN = `${hejil.size}`;
      }
         score4 = (((score4 ? hejil.size : 36) / (Math.max(hejil.size, 2))) < 93);
       let annerJ = 0.0;
       let trash5 = 4.0;
      if (5.69 >= (trash5 + 5.7)) {
          let linkf = String.fromCharCode(102,95,50,48,95,112,114,101,118,0);
          let nativemodulea = String.fromCharCode(99,111,109,112,111,115,101,114,95,122,95,55,0);
          let stylesi = String.fromCharCode(102,95,49,48,95,114,101,110,101,119,0);
          let renewP: Array<any> = [974, 356];
          let colorsN = String.fromCharCode(99,95,49,56,95,109,97,112,0);
         score4 = stylesi.length < twitteri;
         linkf = `${linkf.length}`;
         nativemodulea += `${linkf.length ^ renewP.length}`;
         stylesi = `${renewP.length}`;
         colorsN += `${renewP.length % (Math.max(8, nativemodulea.length))}`;
      }
         emptyL += `${emptyL.length + 2}`;
      collectionq /= Math.max(1, 3);
   }
   let suggestionS = historyQ.length >= 5585385;
   do {
      historyQ = [1];
      if (suggestionS) {
         break;
      }
   } while (suggestionS && ((5 - historyQ.length) > 5 || 4.85 > (4.98 / (Math.max(1, fileO)))));
   if ((parseInt(`${specn}`) / (Math.max(8, forwardy.length))) >= 3 || (4.44 / (Math.max(3, specn))) >= 4.39) {
      forwardy = [1];
   }
   for (let h = 0; h < 3; h++) {
      stationq = [(path3 % (Math.max(7, (tooltipsK ? 3 : 2))))];
   }
       let path_ = String.fromCharCode(105,95,54,95,99,101,110,116,114,97,108,108,121,0);
       let adultI = String.fromCharCode(99,95,53,55,95,99,97,112,105,116,97,108,115,0);
      while (path_.length == 3) {
         adultI = `${adultI.length}`;
         break;
      }
       let configy = String.fromCharCode(120,95,57,55,95,116,114,97,110,115,102,111,114,109,97,116,105,111,110,115,0);
         adultI += `${adultI.length - 3}`;
      let libsgcoreE = adultI == String.fromCharCode(116,114,108,95,0);
      do {
          let googleX = String.fromCharCode(112,114,101,115,101,110,116,97,98,108,101,95,107,95,52,51,0);
         adultI += `${adultI.length}`;
         googleX = `${(googleX == String.fromCharCode(110,0) ? googleX.length : googleX.length)}`;
         if (libsgcoreE) {
            break;
         }
      } while (libsgcoreE && (path_ != String.fromCharCode(82,0)));
         adultI += "2";
          let progress_ = 4.0;
          let sends = String.fromCharCode(103,101,110,101,114,97,116,101,95,101,95,57,50,0);
         configy = `${adultI.length}`;
         progress_ += parseInt(`${progress_}`);
         sends = `${sends.length << (Math.min(Math.abs(3), 5))}`;
      collectionq |= roboto_.length ^ forwardy.length;
      star4 *= parseFloat(`${stationq.length & 2}`);
   let stringZ = 8980552 >= handlerx.length;
   do {
      handlerx += `${path3}`;
      if (stringZ) {
         break;
      }
   } while ((merger8 != String.fromCharCode(86,0)) && stringZ);
      usernameO -= 1 - parseInt(`${usernameO}`);
   if (merger8.length < 4) {
      catagoryD %= Math.max(catagoryD, 1);
   }
   let mbridgeL = 5752391 <= path3;
   do {
       let rootC = String.fromCharCode(109,111,99,107,105,110,103,95,116,95,53,49,0);
       let zhengpianm = String.fromCharCode(104,115,99,97,108,101,95,98,95,50,48,0);
       let score8: Array<any> = [6, 467];
         score8 = [score8.length % (Math.max(2, zhengpianm.length))];
         zhengpianm = "1";
         zhengpianm = `${rootC.length}`;
      let textC = 8882180 <= score8.length;
      do {
          let crossS = true;
          let rewinde = 1;
          let brightness6 = 1.0;
         score8 = [1];
         crossS = rewinde > parseInt(`${brightness6}`);
         rewinde >>= Math.min(Math.abs((rewinde ^ (crossS ? 5 : 4))), 2);
         brightness6 *= parseFloat(`${rewinde >> (Math.min(Math.abs(parseInt(`${brightness6}`)), 3))}`);
         if (textC) {
            break;
         }
      } while (textC && ((score8.length / 2) <= 1 || 2 <= (score8.length / (Math.max(zhengpianm.length, 3)))));
         rootC = `${score8.length | rootC.length}`;
       let anythinkp = String.fromCharCode(100,101,115,116,105,110,97,116,105,111,110,115,95,119,95,54,53,0);
       let sansE = String.fromCharCode(116,101,109,112,108,97,116,101,95,122,95,57,49,0);
      if ((score8.length / (Math.max(zhengpianm.length, 10))) <= 1) {
          let historym = 4;
          let register_1vm: Array<any> = [String.fromCharCode(98,121,116,101,108,101,110,95,122,95,49,50,0), String.fromCharCode(110,95,50,53,95,109,101,115,115,97,103,105,110,103,0), String.fromCharCode(114,95,54,95,103,114,97,118,105,116,121,0)];
          let cross6 = 1;
          let navigationH: Array<any> = [692, 503, 69];
         score8 = [register_1vm.length];
         historym |= cross6;
         register_1vm = [2];
         cross6 &= 3;
         navigationH = [historym];
      }
      if (rootC.length >= anythinkp.length) {
         anythinkp = "2";
      }
      if ((score8.length | 2) < 2) {
         anythinkp += `${(rootC == String.fromCharCode(101,0) ? rootC.length : zhengpianm.length)}`;
      }
      path3 *= 2 & parseInt(`${usernameO}`);
      if (mbridgeL) {
         break;
      }
   } while (mbridgeL && (path3 >= 5));
   if ((1 & stationq.length) > 1) {
       let gray7 = String.fromCharCode(107,105,99,107,101,100,95,107,95,53,57,0);
       let latnq = 4.0;
      let phoneT = String.fromCharCode(118,116,101,107,51,120,121,0) == gray7;
      do {
         gray7 = `${parseInt(`${latnq}`)}`;
         if (phoneT) {
            break;
         }
      } while (phoneT && (gray7.endsWith(`${latnq}`)));
         gray7 = "3";
         gray7 += `${gray7.length}`;
      for (let v = 0; v < 1; v++) {
          let roundm = 5;
          let actiony = 2.0;
         gray7 = `${gray7.length | parseInt(`${actiony}`)}`;
         roundm ^= 1 ^ roundm;
         actiony /= Math.max(3, parseFloat(`${roundm << (Math.min(Math.abs(2), 1))}`));
      }
         gray7 = `${gray7.length}`;
         latnq += gray7.length ^ parseInt(`${latnq}`);
      stationq.push(parseInt(`${fileO}`) ^ 3);
   }
   while (handlerx.length == 4) {
       let penalty6 = 1;
       let overa = 4.0;
       let thumbnails = 2.0;
      while (overa > 5.44) {
         thumbnails /= Math.max(3 << (Math.min(Math.abs(penalty6), 5)), 4);
         break;
      }
      let castingG = overa >= 6472856.0;
      do {
         overa /= Math.max(parseInt(`${thumbnails}`), 5);
         if (castingG) {
            break;
         }
      } while (castingG && (4.100 < thumbnails));
      let colorsM = 7814686.0 <= overa;
      do {
         overa /= Math.max(3, 2 - penalty6);
         if (colorsM) {
            break;
         }
      } while ((2.24 < overa) && colorsM);
      for (let x = 0; x < 1; x++) {
         penalty6 /= Math.max(parseInt(`${thumbnails}`) >> (Math.min(2, Math.abs(penalty6))), 3);
      }
         penalty6 /= Math.max(1, 1);
          let kickA: Array<any> = [95, 535, 488];
         overa *= 2;
         kickA.push(kickA.length);
         overa -= penalty6;
          let package_e1C = false;
          let schedulerG = 0;
         overa /= Math.max(parseInt(`${thumbnails}`), 1);
         package_e1C = 85 <= schedulerG || !package_e1C;
         schedulerG <<= Math.min(1, parseInt(`${Math.abs((schedulerG << (Math.min(5, Math.abs((package_e1C ? 3 : 2))))))}`));
      if (overa <= thumbnails) {
         overa /= Math.max(1, 5);
      }
      handlerx += `${parseInt(`${specn}`) * 3}`;
      break;
   }
   let mini1 = 7498584 <= stationq.length;
   do {
      stationq = [1];
      if (mini1) {
         break;
      }
   } while (((stationq.length * 2) < 2 || (stationq.length % (Math.max(2, 10))) < 5) && mini1);
      path3 += 1 >> (Math.min(3, forwardy.length));
   while (merger8.endsWith(`${roboto_.length}`)) {
       let detailV: Array<any> = [556, 715, 14];
       let condensedu = String.fromCharCode(103,95,54,52,95,117,110,97,118,97,105,98,108,101,0);
       let transfery = 3;
          let gemfilet = String.fromCharCode(100,109,117,108,95,49,95,53,50,0);
         transfery *= condensedu.length;
         gemfilet += "1";
          let proxyY: Array<any> = [242, 522];
         detailV.push(1 % (Math.max(7, transfery)));
         proxyY.push(proxyY.length + proxyY.length);
      for (let x = 0; x < 2; x++) {
          let modeW = String.fromCharCode(98,101,110,101,97,116,104,95,121,95,51,56,0);
          let stationsi = String.fromCharCode(99,120,100,97,116,97,95,117,95,49,49,0);
          let libturbomodulejsijni9: Array<any> = [String.fromCharCode(111,119,110,101,114,115,104,105,112,95,48,95,56,49,0), String.fromCharCode(119,95,57,50,95,109,121,114,110,100,0), String.fromCharCode(117,110,108,111,97,100,95,114,95,54,51,0)];
         detailV = [condensedu.length * libturbomodulejsijni9.length];
         modeW = `${modeW.length << (Math.min(Math.abs(1), 5))}`;
         stationsi += `${2 >> (Math.min(4, modeW.length))}`;
         libturbomodulejsijni9.push(2 / (Math.max(6, stationsi.length)));
      }
          let flippert = 4.0;
          let shared2: Array<any> = [String.fromCharCode(106,115,101,112,95,116,95,51,56,0), String.fromCharCode(122,95,51,53,95,105,100,99,105,110,0)];
         condensedu += "3";
         flippert *= parseInt(`${flippert}`);
         shared2 = [shared2.length * parseInt(`${flippert}`)];
      for (let l = 0; l < 1; l++) {
          let internetN: Map<any, any> = new Map([[String.fromCharCode(101,110,102,111,114,99,101,95,105,95,51,0),String.fromCharCode(110,95,51,56,95,104,119,117,112,108,111,97,100,0)], [String.fromCharCode(121,95,52,95,112,114,111,100,117,99,116,102,0),String.fromCharCode(112,114,111,109,105,115,105,102,121,95,98,95,52,49,0)], [String.fromCharCode(105,95,55,55,95,98,97,107,101,0),String.fromCharCode(107,95,56,52,95,105,100,101,110,116,105,102,105,97,98,108,101,0)]]);
          let encrypt3 = 5.0;
          let directX = String.fromCharCode(107,95,55,54,95,110,105,103,104,116,0);
         condensedu += "2";
         internetN = new Map([[`${internetN.size}`, internetN.size * parseInt(`${encrypt3}`)]]);
         encrypt3 /= Math.max(5, parseFloat(`${1 - directX.length}`));
         directX += `${3 ^ directX.length}`;
      }
       let sliderM: Map<any, any> = new Map([[String.fromCharCode(108,114,111,110,100,95,115,95,51,52,0),229], [String.fromCharCode(112,97,121,108,111,97,100,115,95,99,95,54,50,0),517]]);
       let sortt: Map<any, any> = new Map([[String.fromCharCode(119,95,54,51,95,99,104,97,114,103,101,0),String.fromCharCode(102,111,114,101,118,101,114,0)], [String.fromCharCode(121,95,52,53,95,105,110,103,114,101,115,115,0),String.fromCharCode(103,95,50,49,95,111,99,116,112,111,105,110,116,0)], [String.fromCharCode(98,105,116,120,95,115,95,53,53,0),String.fromCharCode(98,103,112,104,99,104,101,99,107,95,122,95,54,57,0)]]);
          let libswscalex = String.fromCharCode(113,95,53,57,95,108,115,98,115,0);
          let placeholdero = String.fromCharCode(98,95,51,48,95,100,111,119,110,115,104,105,102,116,0);
         transfery /= Math.max(4, 1);
         libswscalex = `${libswscalex.length}`;
         placeholdero += `${placeholdero.length}`;
      if (!Array.from(sortt.keys()).includes(`${sliderM.size}`)) {
         sortt.set(`${transfery}`, sortt.size);
      }
         detailV.push(sliderM.size);
      roboto_.push(roboto_.length);
      break;
   }
},
}: yys_ConfigureUimanager) {
  const navigator = useNavigation();
  const hideVipPrompt = useCallback(async () => {
       let movies6: Array<any> = [187, 280, 691];
    let faviconk = 1.0;
    let mini4: Map<any, any> = new Map([[String.fromCharCode(103,95,52,54,95,102,105,110,0),String.fromCharCode(104,97,115,104,116,97,103,95,49,95,55,49,0)], [String.fromCharCode(115,112,101,101,100,95,54,95,50,55,0),String.fromCharCode(116,114,97,110,95,107,95,52,48,0)], [String.fromCharCode(98,105,110,107,98,95,54,95,56,56,0),String.fromCharCode(117,108,111,110,103,95,97,95,54,50,0)]]);
    let store_ = 1.0;
    let langf = true;
    let gestures0 = 5;
    let libreactb = String.fromCharCode(113,112,101,103,0);
    let internetn = String.fromCharCode(116,115,97,110,95,110,95,53,53,0);
    let ajaxu = String.fromCharCode(115,101,108,101,99,116,105,111,110,115,95,107,95,56,52,0);
   while (gestures0 <= 5) {
      langf = libreactb == String.fromCharCode(121,0);
      break;
   }
   let s_center4 = store_ <= 8305485.0;
   do {
       let streamingP = String.fromCharCode(103,95,51,53,95,115,97,109,112,108,101,99,112,121,0);
       let usernameY = String.fromCharCode(97,95,50,55,95,105,110,99,108,117,100,101,0);
       let selectedm = 5.0;
       let progress4 = String.fromCharCode(101,95,54,48,95,114,116,112,114,101,99,101,105,118,101,114,0);
          let securityc = String.fromCharCode(117,110,114,101,115,101,114,118,101,100,95,55,95,56,51,0);
          let bridgeC = String.fromCharCode(116,95,52,51,95,112,104,121,115,0);
          let animationx: Array<any> = [708, 843];
         progress4 += "3";
         securityc += "2";
         bridgeC = `${(bridgeC == String.fromCharCode(81,0) ? bridgeC.length : animationx.length)}`;
         animationx = [animationx.length];
          let stringsx: Map<any, any> = new Map([[String.fromCharCode(118,95,53,57,95,100,101,110,111,114,109,97,108,0),962], [String.fromCharCode(110,101,119,114,111,119,0),931], [String.fromCharCode(114,101,116,114,97,110,115,109,105,116,115,95,52,95,57,55,0),513]]);
          let specM = 1;
          let topon_ = String.fromCharCode(105,110,116,101,114,110,97,116,105,111,110,97,108,108,121,95,104,95,54,56,0);
         selectedm += parseFloat(`${3}`);
         stringsx.set(topon_, (topon_ == String.fromCharCode(107,0) ? stringsx.size : topon_.length));
         specM ^= stringsx.size;
         usernameY += "1";
          let pinga = String.fromCharCode(117,110,101,109,98,101,100,0);
         selectedm += parseFloat(`${streamingP.length}`);
         pinga = `${pinga.length}`;
      let shrink0 = 9824044 >= usernameY.length;
      do {
          let casts = 3;
          let tickedX: Map<any, any> = new Map([[String.fromCharCode(97,110,116,105,97,108,105,97,115,105,110,103,95,102,95,49,51,0),145], [String.fromCharCode(99,108,97,122,122,95,106,95,56,56,0),424], [String.fromCharCode(119,105,116,104,95,97,95,57,51,0),72]]);
          let hiadt: Array<any> = [906, 374, 811];
          let updatesB = 2.0;
          let libglogD = 5.0;
         usernameY = `${hiadt.length + parseInt(`${selectedm}`)}`;
         casts *= parseInt(`${updatesB}`) & 2;
         tickedX.set(`${casts}`, tickedX.size & casts);
         hiadt = [casts % (Math.max(5, parseInt(`${updatesB}`)))];
         libglogD += parseFloat(`${3 * tickedX.size}`);
         if (shrink0) {
            break;
         }
      } while (shrink0 && (streamingP == usernameY));
      while (usernameY == String.fromCharCode(72,0) || streamingP != String.fromCharCode(52,0)) {
          let pauseS = String.fromCharCode(101,120,116,114,97,112,111,108,97,116,111,114,95,48,95,53,54,0);
          let abidetectu = String.fromCharCode(119,95,50,95,115,116,114,110,99,97,115,101,99,109,112,0);
          let configure_ = String.fromCharCode(112,101,114,105,111,100,105,99,95,117,95,56,52,0);
         usernameY += `${usernameY.length}`;
         pauseS = `${2 * pauseS.length}`;
         abidetectu += "1";
         configure_ += `${pauseS.length}`;
         break;
      }
      let resendH = String.fromCharCode(57,104,57,101,57,116,105,0) == progress4;
      do {
         progress4 = "1";
         if (resendH) {
            break;
         }
      } while ((progress4.startsWith(`${selectedm}`)) && resendH);
         usernameY += `${progress4.length}`;
      while (2.70 > selectedm) {
         streamingP += `${usernameY.length}`;
         break;
      }
         selectedm *= parseFloat(`${progress4.length | 1}`);
          let form1 = String.fromCharCode(117,95,56,49,95,119,101,98,114,116,99,0);
          let componentf: Map<any, any> = new Map([[String.fromCharCode(110,95,53,54,95,99,98,108,107,0),461], [String.fromCharCode(122,95,54,48,95,108,104,115,0),724], [String.fromCharCode(114,101,103,105,115,116,114,97,116,105,111,110,95,122,95,49,57,0),325]]);
         selectedm += (parseFloat(`${streamingP == String.fromCharCode(99,0) ? progress4.length : streamingP.length}`));
         form1 = `${form1.length}`;
         componentf.set(form1, 2 - componentf.size);
          let canvasX = String.fromCharCode(117,98,108,105,99,95,118,95,50,52,0);
          let gradlewb = 2.0;
          let handlerB = String.fromCharCode(97,116,116,114,105,98,117,116,101,100,95,117,95,54,57,0);
         streamingP = `${handlerB.length + progress4.length}`;
         canvasX = `${(canvasX == String.fromCharCode(67,0) ? parseInt(`${gradlewb}`) : canvasX.length)}`;
         gradlewb *= parseInt(`${gradlewb}`) % (Math.max(canvasX.length, 2));
         handlerB += `${parseInt(`${gradlewb}`)}`;
      store_ *= (String.fromCharCode(95,0) == libreactb ? libreactb.length : movies6.length);
      if (s_center4) {
         break;
      }
   } while (s_center4 && (4 >= (libreactb.length & 4) && 3 >= (parseInt(`${store_}`) + 4)));
      libreactb = `${(internetn == String.fromCharCode(110,0) ? mini4.size : internetn.length)}`;
   while ((1.88 + faviconk) < 4.86) {
      faviconk += parseFloat(`${parseInt(`${store_}`) + 3}`);
      break;
   }
   let statsX = gestures0 <= 6068856;
   do {
      gestures0 >>= Math.min(5, Math.abs(mini4.size & parseInt(`${faviconk}`)));
      if (statsX) {
         break;
      }
   } while (((gestures0 - 1) >= 2 || langf) && statsX);
   while (Array.from(mini4.values()).includes(faviconk)) {
      mini4 = new Map([[`${mini4.size}`, internetn.length]]);
      break;
   }
   let sliderf = 5407298 >= mini4.size;
   do {
      mini4 = new Map([[`${gestures0}`, 3]]);
      if (sliderf) {
         break;
      }
   } while ((store_ <= 4.41) && sliderf);
   let configure3 = gestures0 <= 5481598;
   do {
       let checkboxF: Array<any> = [673, 559, 911];
         checkboxF.push(checkboxF.length);
      while (!checkboxF.includes(checkboxF.length)) {
         checkboxF.push(1 & checkboxF.length);
         break;
      }
      for (let x = 0; x < 1; x++) {
         checkboxF.push(checkboxF.length);
      }
      gestures0 >>= Math.min(internetn.length, 3);
      if (configure3) {
         break;
      }
   } while ((2 <= (1 - gestures0) && (parseInt(`${faviconk}`) * gestures0) <= 1) && configure3);
   while (3.3 > (store_ / (Math.max(libreactb.length, 1)))) {
       let launcherI = String.fromCharCode(108,95,51,56,95,101,97,116,105,110,103,0);
       let activityA = String.fromCharCode(107,95,53,55,95,102,108,97,116,110,101,115,115,0);
       let diced = String.fromCharCode(115,95,57,95,98,108,111,99,107,103,114,111,117,112,0);
       let selectedu = 0.0;
         launcherI += `${launcherI.length}`;
          let nativeu = String.fromCharCode(112,95,52,49,95,104,97,115,104,105,110,105,116,0);
          let long_olN: Array<any> = [380, 27, 133];
         diced = `${3 + diced.length}`;
         nativeu += `${(nativeu == String.fromCharCode(52,0) ? long_olN.length : nativeu.length)}`;
         long_olN = [(nativeu == String.fromCharCode(69,0) ? long_olN.length : nativeu.length)];
      while ((1 / (Math.max(7, parseInt(`${selectedu}`)))) < 1 || 1 < (parseInt(`${selectedu}`) / (Math.max(diced.length, 6)))) {
          let shrinkR = String.fromCharCode(99,105,112,104,101,114,95,52,95,55,55,0);
          let abidetectuq = true;
         diced += `${3 | parseInt(`${selectedu}`)}`;
         shrinkR += "3";
         abidetectuq = !abidetectuq;
         break;
      }
         selectedu -= 1 ^ diced.length;
      let hejiv = 5401808.0 <= selectedu;
      do {
         selectedu -= launcherI.length | activityA.length;
         if (hejiv) {
            break;
         }
      } while ((1.8 <= selectedu) && hejiv);
      for (let n = 0; n < 3; n++) {
         activityA = `${parseInt(`${selectedu}`)}`;
      }
          let thailandC = true;
          let bootsplasht = 3.0;
          let constantsh = 5.0;
         activityA = `${launcherI.length}`;
         thailandC = 86.0 >= constantsh;
         bootsplasht += parseInt(`${bootsplasht}`) * 1;
         constantsh -= parseInt(`${constantsh}`) ^ 3;
      let referrerr = activityA.length >= 7872454;
      do {
          let klevinD: Array<any> = [849, 60];
         activityA += `${activityA.length / 1}`;
         klevinD = [klevinD.length >> (Math.min(Math.abs(1), 1))];
         if (referrerr) {
            break;
         }
      } while (referrerr && (launcherI == String.fromCharCode(114,0) && 3 >= activityA.length));
      if (launcherI == String.fromCharCode(87,0)) {
         activityA = `${activityA.length * 1}`;
      }
         activityA += `${parseInt(`${selectedu}`) % (Math.max(3, 10))}`;
      while ((2 * diced.length) == 1) {
         selectedu += 1 << (Math.min(4, activityA.length));
         break;
      }
         activityA += `${parseInt(`${selectedu}`)}`;
      libreactb += `${activityA.length / 2}`;
      break;
   }
       let changef = String.fromCharCode(110,95,51,54,95,103,101,116,0);
       let catagory5: Array<any> = [413, 397];
      while ((catagory5.length >> (Math.min(Math.abs(2), 3))) >= 3 && 2 >= (changef.length >> (Math.min(3, catagory5.length)))) {
          let libreanimatedO = 0;
          let unewinterstitialJ = String.fromCharCode(100,105,102,102,105,99,117,108,116,121,0);
          let y_centerJ = 5;
          let bingW = String.fromCharCode(99,111,109,112,105,108,101,114,95,122,95,53,55,0);
          let libpangleflippedl = true;
         catagory5.push(changef.length);
         libreanimatedO &= bingW.length / 1;
         unewinterstitialJ += `${libreanimatedO >> (Math.min(unewinterstitialJ.length, 4))}`;
         y_centerJ %= Math.max(5, (String.fromCharCode(107,0) == unewinterstitialJ ? y_centerJ : unewinterstitialJ.length));
         bingW = `${libreanimatedO}`;
         libpangleflippedl = !libpangleflippedl && 28 <= libreanimatedO;
         break;
      }
         catagory5 = [(changef == String.fromCharCode(71,0) ? catagory5.length : changef.length)];
      for (let i = 0; i < 3; i++) {
         changef += `${(String.fromCharCode(115,0) == changef ? catagory5.length : changef.length)}`;
      }
      let libimagepipeline5 = catagory5.length <= 6050427;
      do {
          let filled1 = 2.0;
         catagory5 = [(changef == String.fromCharCode(99,0) ? catagory5.length : changef.length)];
         filled1 += parseFloat(`${parseInt(`${filled1}`)}`);
         if (libimagepipeline5) {
            break;
         }
      } while ((3 <= (changef.length ^ catagory5.length) || (3 ^ catagory5.length) <= 3) && libimagepipeline5);
         changef += "2";
      let gdtadvr = catagory5.length <= 5327480;
      do {
          let footballf = String.fromCharCode(116,116,97,101,110,99,100,115,112,0);
          let matches6: Array<any> = [878, 338];
         catagory5.push(changef.length);
         footballf += `${1 ^ matches6.length}`;
         matches6 = [(String.fromCharCode(79,0) == footballf ? footballf.length : matches6.length)];
         if (gdtadvr) {
            break;
         }
      } while (gdtadvr && (!changef.includes(`${catagory5.length}`)));
      libreactb += `${(String.fromCharCode(69,0) == libreactb ? libreactb.length : gestures0)}`;
       let statisticsz: Map<any, any> = new Map([[String.fromCharCode(116,104,114,101,97,100,112,111,111,108,95,101,95,56,0),String.fromCharCode(114,95,50,57,95,115,119,105,102,116,0)], [String.fromCharCode(116,111,114,103,98,95,97,95,52,57,0),String.fromCharCode(105,95,49,55,95,102,105,108,116,101,114,105,110,103,0)]]);
       let reactnativejsb = false;
       let moviesO = String.fromCharCode(115,116,117,102,102,105,110,103,95,108,95,57,50,0);
         reactnativejsb = (38 == (statisticsz.size >> (Math.min(3, Math.abs((reactnativejsb ? statisticsz.size : 46))))));
      let sharem = statisticsz.size <= 5935694;
      do {
          let emojiF = 1.0;
         statisticsz.set(`${reactnativejsb}`, (1 / (Math.max(1, (reactnativejsb ? 3 : 2)))));
         emojiF /= Math.max(3, 3 | parseInt(`${emojiF}`));
         if (sharem) {
            break;
         }
      } while (sharem && ((1 - statisticsz.size) >= 5));
      while (statisticsz.size == 2) {
          let submitZ = String.fromCharCode(99,95,50,57,95,105,102,97,115,116,0);
          let mbnativeS = String.fromCharCode(104,95,51,57,95,101,110,99,111,100,101,100,0);
          let complete0 = 1.0;
         statisticsz.set(moviesO, statisticsz.size - 3);
         submitZ = `${(String.fromCharCode(48,0) == submitZ ? parseInt(`${complete0}`) : submitZ.length)}`;
         mbnativeS = `${submitZ.length}`;
         complete0 += submitZ.length;
         break;
      }
      let clubs = reactnativejsb ? !reactnativejsb : reactnativejsb;
      do {
         reactnativejsb = 74 < moviesO.length;
         if (clubs) {
            break;
         }
      } while ((statisticsz.size >= 5 && 3 >= (5 % (Math.max(4, statisticsz.size)))) && clubs);
      let playercommonr = reactnativejsb ? !reactnativejsb : reactnativejsb;
      do {
          let libreactnativeblobe = 0.0;
          let untickN = 1.0;
         reactnativejsb = moviesO.length < 34 && 43.15 < untickN;
         libreactnativeblobe /= Math.max(parseFloat(`${parseInt(`${libreactnativeblobe}`)}`), 3);
         untickN /= Math.max(parseFloat(`${parseInt(`${libreactnativeblobe}`) - 3}`), 3);
         if (playercommonr) {
            break;
         }
      } while (playercommonr && ((4 / (Math.max(7, statisticsz.size))) < 4 && statisticsz.size < 4));
         statisticsz = new Map([[`${reactnativejsb}`, 2 - moviesO.length]]);
      while ((statisticsz.size + moviesO.length) >= 1 || (1 + moviesO.length) >= 1) {
          let mapbuffere = String.fromCharCode(99,111,99,111,97,95,111,95,54,49,0);
         moviesO += `${moviesO.length / (Math.max(2, 8))}`;
         mapbuffere = `${(mapbuffere == String.fromCharCode(70,0) ? mapbuffere.length : mapbuffere.length)}`;
         break;
      }
      if (!reactnativejsb) {
         reactnativejsb = moviesO.length < 84;
      }
         reactnativejsb = statisticsz.size <= 27;
      gestures0 <<= Math.min(1, Math.abs(2 << (Math.min(2, Math.abs(mini4.size)))));
      store_ *= (String.fromCharCode(76,0) == ajaxu ? ajaxu.length : internetn.length);
   if (internetn.length == ajaxu.length) {
      ajaxu = `${2 << (Math.min(2, libreactb.length))}`;
   }
      movies6 = [internetn.length - ajaxu.length];
       let downj: Map<any, any> = new Map([[String.fromCharCode(115,97,116,100,120,95,111,95,53,0),785], [String.fromCharCode(110,101,116,95,97,95,55,50,0),914]]);
       let spinneri = 4.0;
       let appsj: Array<any> = [985, 713];
         appsj.push(2);
      while (Array.from(downj.values()).includes(spinneri)) {
          let catagoryT = false;
          let back1 = 5.0;
          let soundC = String.fromCharCode(114,101,99,111,114,100,105,110,103,99,111,110,110,95,111,95,49,52,0);
          let pingv = 1.0;
         downj = new Map([[`${downj.size}`, downj.size]]);
         catagoryT = (pingv - back1) >= 82.31;
         back1 /= Math.max(5, parseFloat(`${parseInt(`${back1}`)}`));
         soundC = "1";
         pingv *= 1 % (Math.max(7, parseInt(`${pingv}`)));
         break;
      }
         spinneri *= appsj.length << (Math.min(3, Math.abs(parseInt(`${spinneri}`))));
         downj = new Map([[`${appsj.length}`, 1 % (Math.max(4, appsj.length))]]);
      if (Array.from(downj.values()).includes(appsj.length)) {
          let activityD = String.fromCharCode(108,95,52,53,95,114,101,116,117,114,110,105,110,103,0);
          let middle8: Map<any, any> = new Map([[String.fromCharCode(121,95,51,54,95,102,114,101,111,112,101,110,0),112], [String.fromCharCode(105,110,100,105,99,97,116,101,100,95,113,95,57,49,0),694]]);
         appsj = [parseInt(`${spinneri}`) ^ appsj.length];
         activityD = "1";
         middle8 = new Map([[`${middle8.size}`, middle8.size << (Math.min(Math.abs(3), 1))]]);
      }
          let countryI = 4.0;
          let filledE: Map<any, any> = new Map([[String.fromCharCode(114,101,118,101,114,98,95,100,95,57,54,0),885], [String.fromCharCode(97,114,101,95,53,95,57,57,0),179]]);
         spinneri += downj.size ^ 1;
         countryI -= parseFloat(`${1 << (Math.min(4, Math.abs(parseInt(`${countryI}`))))}`);
         filledE = new Map([[`${filledE.size}`, parseInt(`${countryI}`)]]);
         downj = new Map([[`${downj.size}`, downj.size]]);
          let stationsf = String.fromCharCode(113,95,54,48,95,105,108,98,99,0);
         downj.set(`${appsj.length}`, 3 | downj.size);
         stationsf += `${3 * stationsf.length}`;
      while (spinneri > appsj.length) {
         appsj.push(2 - downj.size);
         break;
      }
      movies6 = [parseInt(`${store_}`)];
      internetn += `${gestures0}`;

    setShowVIPOverlay(false);
  }, []);

  const dispatch = useAppDispatch();

  
  
  

  const {colors, textVariants, spacing, icons} = useTheme();

  return (
    <>
      {showVIPOverlay && (
        <View
          style={{
            height: '100%',
            width: '100%',
            backgroundColor: 'rgba(0, 0, 0, 0.8)',
            position: 'absolute',
            zIndex: 1000,
          }}>
          <VipModal>
            <View
              style={{
                backgroundColor: 'rgba(34, 34, 34, 0.9)',
                marginTop: 40,
                borderRadius: 12,
                paddingTop: 36,
                paddingBottom: 26,
                paddingHorizontal: 28,
              }}>
              <View style={{alignItems: 'center', justifyContent: 'center'}}>
                <Text
                  style={{
                    fontSize: 20,
                    fontWeight: '500',
                    color: '#E2820E',
                    paddingTop: 20,
                    paddingVertical: 12,
                  }}>
                  VIP还剩{remainingDay}天
                </Text>
                <Text
                  style={{
                    color: 'white',
                    padding: 2,
                    fontSize: 16,
                    fontWeight: '300',
                  }}>
                  只需邀请更多好友
                </Text>
                <Text
                  style={{
                    color: 'white',
                    padding: 2,
                    fontSize: 16,
                    fontWeight: '300',
                  }}>
                  即可获得额外天数的VIP服务时长
                </Text>
                <View style={{paddingTop: 22, alignItems: 'center'}}>
                  <TouchableOpacity
                    onPress={() => {
                      hideVipPrompt();
                      navigator.navigate('邀请');
                    }}
                    style={{
                      backgroundColor: '#FAC33D',
                      paddingVertical: 12,
                      paddingHorizontal: 38,
                      borderRadius: 8,
                    }}>
                    <Text
                      style={{
                        color: '#000',
                        fontWeight: '600',
                        fontSize: 16,
                      }}>
                      立即邀请
                    </Text>
                  </TouchableOpacity>
                  <TouchableOpacity
                    onPress={() => hideVipPrompt()}
                    style={{paddingTop: 16}}>
                    <Text style={{color: '#9C9C9C', fontSize: 16}}>取消</Text>
                  </TouchableOpacity>
                </View>
              </View>
            </View>
          </VipModal>
        </View>
      )}
    </>
  );
}
