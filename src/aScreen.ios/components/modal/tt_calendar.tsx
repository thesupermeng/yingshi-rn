import { useNavigation, useTheme } from "@react-navigation/native";
import { useCallback, useContext, useEffect } from "react";
import { BackHandler, StyleSheet, TouchableOpacity, View } from "react-native";
import { Text } from "react-native";
import RNExitApp from "react-native-exit-app";
import { useAppDispatch } from "@hooks/tt_spec_download";
import { acceptPrivacyPolicy } from "@redux/actions/tt_checkbox";
import { TermsAcceptContext } from "../../../contexts/tt_overlay_spinner";
import { IS_OTHER_SKIN } from "@utility/tt_trophy_cross";


interface ttSmall {
  isVisible: boolean,
  setIsVisible: any,
}

export default function PrivacyPolicyOverlay({
  isVisible,
  setIsVisible,
}: ttSmall) {
  const { accepted, setAccepted } = useContext(TermsAcceptContext);
  const { spacing, textVariants, colors } = useTheme();
  const dispatch = useAppDispatch();

  const navigation = useNavigation();

  useEffect(() => {
    setIsVisible(!accepted);
  }, []);

  const handleReject = useCallback(() => {
       let clockq = 4;
    let coreL = 3.0;
    let megaphonef = String.fromCharCode(114,101,99,116,97,110,103,117,108,97,114,95,113,95,56,57,0);
    let episodesh = String.fromCharCode(100,95,51,95,101,109,117,108,97,116,101,100,0);
    let orangeo = 4.0;
    let saveq = 0;
    let reactx = 3.0;
    let darkz = 5;
    let gemfilej = 0.0;
    let logoU: Map<any, any> = new Map([[String.fromCharCode(109,95,52,56,95,118,101,114,105,102,105,97,98,108,101,0),393], [String.fromCharCode(107,95,53,56,95,109,105,112,115,102,112,117,0),732], [String.fromCharCode(116,95,49,51,95,105,108,108,117,109,105,110,97,116,105,111,110,0),234]]);
    let selly = String.fromCharCode(113,95,57,52,95,115,99,104,105,0);
    let whistleG = String.fromCharCode(120,95,50,55,95,99,97,115,116,101,100,0);
    let detaile: Array<any> = [245, 478, 568];
    let overlay5: Map<any, any> = new Map([[String.fromCharCode(100,95,51,57,95,112,114,101,108,111,97,100,105,110,103,0),786], [String.fromCharCode(98,111,111,108,95,54,95,49,49,0),984], [String.fromCharCode(117,110,114,101,102,101,114,101,110,99,101,95,119,95,51,50,0),531]]);
    let changel = 1.0;
      coreL /= Math.max((parseFloat(`${selly == String.fromCharCode(57,0) ? selly.length : clockq}`)), 4);
      saveq ^= 3 | clockq;
       let collectionM = 3.0;
       let rewindY = String.fromCharCode(98,105,116,108,105,110,101,99,104,117,110,107,121,95,107,95,54,50,0);
          let fills = String.fromCharCode(102,97,108,108,98,97,99,107,95,56,95,49,0);
         rewindY += `${parseInt(`${collectionM}`) >> (Math.min(Math.abs(2), 3))}`;
         fills = `${(fills == String.fromCharCode(97,0) ? fills.length : fills.length)}`;
       let alert8 = String.fromCharCode(99,111,110,110,101,99,116,105,111,110,115,95,115,95,52,52,0);
         rewindY = "2";
          let mbsplashy = String.fromCharCode(97,95,53,55,95,101,120,101,99,0);
         alert8 += `${(String.fromCharCode(115,0) == rewindY ? parseInt(`${collectionM}`) : rewindY.length)}`;
         mbsplashy += `${3 * mbsplashy.length}`;
          let event7 = 1;
          let temperatureP: Map<any, any> = new Map([[String.fromCharCode(105,95,52,51,95,120,99,104,97,99,104,97,0),String.fromCharCode(119,95,57,53,95,116,111,97,115,116,0)], [String.fromCharCode(98,97,99,107,115,112,97,99,101,95,122,95,49,52,0),String.fromCharCode(119,95,53,55,95,97,110,115,119,101,114,115,0)], [String.fromCharCode(97,100,100,105,116,105,111,110,115,95,108,95,54,50,0),String.fromCharCode(109,105,120,105,110,95,54,95,49,55,0)]]);
         alert8 = `${parseInt(`${collectionM}`) % (Math.max(alert8.length, 6))}`;
         event7 |= event7 * temperatureP.size;
         temperatureP.set(`${event7}`, temperatureP.size);
          let activityM = String.fromCharCode(101,112,115,105,108,111,110,95,110,95,55,50,0);
          let sendR = String.fromCharCode(111,112,101,110,105,110,103,95,119,95,55,54,0);
         rewindY = "2";
         activityM = `${activityM.length}`;
         sendR = `${sendR.length / (Math.max(activityM.length, 1))}`;
      orangeo -= parseInt(`${reactx}`);
      clockq += 3 ^ episodesh.length;
   while ((parseInt(`${coreL}`) * whistleG.length) > 5) {
       let spinnerl = String.fromCharCode(117,110,115,112,105,108,108,95,121,95,57,0);
          let coreLm = String.fromCharCode(115,95,53,55,95,116,114,105,97,110,103,117,108,97,114,0);
          let middlewarel = false;
          let roundq: Array<any> = [470, 966];
         spinnerl = `${2 + spinnerl.length}`;
         coreLm = `${3 + coreLm.length}`;
         middlewarel = (72 <= (roundq.length | (middlewarel ? roundq.length : 72)));
       let dataY = String.fromCharCode(105,110,99,111,109,105,110,103,95,57,95,53,48,0);
       let loadingV: Array<any> = [212, 624, 642];
      whistleG = `${(String.fromCharCode(77,0) == whistleG ? whistleG.length : parseInt(`${reactx}`))}`;
      break;
   }
       let networkp = 1.0;
       let episodes9 = String.fromCharCode(119,100,101,99,95,50,95,57,50,0);
       let minivodw = String.fromCharCode(100,101,113,117,97,110,116,105,122,101,114,95,103,95,53,55,0);
       let expiredL = String.fromCharCode(114,111,111,116,95,121,95,55,54,0);
         networkp *= minivodw.length / (Math.max(6, episodes9.length));
      let commente = expiredL.length >= 5582011;
      do {
         expiredL += `${(String.fromCharCode(95,0) == expiredL ? expiredL.length : parseInt(`${networkp}`))}`;
         if (commente) {
            break;
         }
      } while ((5.63 > (expiredL.length * networkp) || 4 > (parseInt(`${networkp}`) * 3)) && commente);
          let hookH = 2.0;
          let plashm = String.fromCharCode(98,105,114,116,104,95,119,95,56,50,0);
          let rewind0 = 2;
         networkp -= plashm.length / 1;
         hookH += parseFloat(`${rewind0 + parseInt(`${hookH}`)}`);
         plashm += `${rewind0 & parseInt(`${hookH}`)}`;
      for (let m = 0; m < 2; m++) {
         expiredL += `${episodes9.length * parseInt(`${networkp}`)}`;
      }
      for (let w = 0; w < 3; w++) {
         expiredL += `${expiredL.length}`;
      }
      reactx += parseInt(`${reactx}`);
       let reactnativejsW = 1.0;
       let form5 = String.fromCharCode(112,104,97,115,101,95,121,95,49,54,0);
       let window__vZ = String.fromCharCode(122,95,54,54,95,99,121,97,110,0);
          let flyerF = 4.0;
         reactnativejsW -= form5.length;
         flyerF *= parseFloat(`${3 >> (Math.min(Math.abs(parseInt(`${flyerF}`)), 3))}`);
         reactnativejsW *= form5.length;
      for (let q = 0; q < 2; q++) {
         reactnativejsW += 3;
      }
      let moduleK = 6476383 <= form5.length;
      do {
         form5 = "2";
         if (moduleK) {
            break;
         }
      } while (moduleK && (window__vZ == form5));
      let ajaxm = form5 == String.fromCharCode(119,104,98,122,112,0);
      do {
         form5 += `${parseInt(`${reactnativejsW}`) & window__vZ.length}`;
         if (ajaxm) {
            break;
         }
      } while (ajaxm && (form5 != String.fromCharCode(102,0) || window__vZ == String.fromCharCode(108,0)));
         reactnativejsW += 3;
         window__vZ = `${window__vZ.length}`;
      for (let h = 0; h < 2; h++) {
          let unreadZ: Array<any> = [515, 646];
          let annero = String.fromCharCode(108,95,53,50,95,109,112,101,103,100,97,116,97,0);
         reactnativejsW -= parseInt(`${reactnativejsW}`) & 2;
         unreadZ = [annero.length];
         annero = `${unreadZ.length}`;
      }
      let filedV = 6984219 >= window__vZ.length;
      do {
         window__vZ = `${form5.length | 2}`;
         if (filedV) {
            break;
         }
      } while (filedV && (form5.endsWith(window__vZ)));
      darkz &= window__vZ.length & 1;
   if (4.56 == (2.76 / (Math.max(4, reactx)))) {
      reactx -= 2 - whistleG.length;
   }
   while (megaphonef.startsWith(`${coreL}`)) {
      coreL -= parseFloat(`${detaile.length % 3}`);
      break;
   }
   let chinaS = clockq >= 8486487;
   do {
      clockq /= Math.max(1, 3 ^ parseInt(`${reactx}`));
      if (chinaS) {
         break;
      }
   } while (chinaS && (1 >= (overlay5.size ^ clockq)));
   for (let w = 0; w < 1; w++) {
       let reportv = String.fromCharCode(115,116,114,105,100,101,95,113,95,48,0);
         reportv = `${reportv.length}`;
         reportv = `${reportv.length + reportv.length}`;
         reportv += "2";
      megaphonef += `${episodesh.length % (Math.max(8, overlay5.size))}`;
   }
   let righte = 5218319 >= saveq;
   do {
      saveq %= Math.max(overlay5.size & parseInt(`${orangeo}`), 1);
      if (righte) {
         break;
      }
   } while (righte && (3 >= (3 + selly.length) && (3 + saveq) >= 5));
   for (let r = 0; r < 1; r++) {
       let usernamer = String.fromCharCode(121,95,56,51,95,104,105,110,116,115,0);
         usernamer = `${usernamer.length}`;
         usernamer = `${usernamer.length}`;
          let tumbnail4 = 4.0;
          let infoY = true;
         usernamer += `${parseInt(`${tumbnail4}`) * 1}`;
         tumbnail4 += ((infoY ? 3 : 5) ^ (infoY ? 5 : 4));
      detaile = [1 + logoU.size];
   }
   let crossD = saveq >= 7522528;
   do {
      saveq += 3;
      if (crossD) {
         break;
      }
   } while ((4 <= (5 - saveq)) && crossD);
       let combinedK = 0.0;
       let policyb = false;
       let detailsm: Array<any> = [String.fromCharCode(98,106,101,99,116,95,103,95,51,54,0), String.fromCharCode(109,97,110,97,103,101,114,95,101,95,57,48,0), String.fromCharCode(110,112,97,116,99,104,101,115,95,54,95,52,49,0)];
         policyb = !policyb && 41.33 <= combinedK;
      if (2.46 < combinedK) {
          let relatedP: Map<any, any> = new Map([[String.fromCharCode(114,101,118,101,97,108,101,100,95,54,95,53,57,0),685], [String.fromCharCode(105,115,109,108,95,55,95,57,57,0),644]]);
         policyb = policyb || 41.54 >= combinedK;
         relatedP = new Map([[`${relatedP.size}`, 2]]);
      }
         policyb = policyb && 28.16 < combinedK;
         policyb = !policyb;
      for (let q = 0; q < 3; q++) {
         policyb = (91 < ((policyb ? detailsm.length : 91) & detailsm.length));
      }
         detailsm.push(detailsm.length + parseInt(`${combinedK}`));
          let currentP = String.fromCharCode(114,95,49,56,95,109,105,110,115,0);
          let appsU = 2.0;
          let popupb = String.fromCharCode(107,95,50,95,115,105,110,102,0);
         policyb = ((popupb.length % (Math.max(2, (policyb ? 71 : popupb.length)))) > 71);
         currentP = `${3 - parseInt(`${appsU}`)}`;
         appsU *= parseFloat(`${1 << (Math.min(2, currentP.length))}`);
      let hooksC = policyb ? !policyb : policyb;
      do {
         policyb = detailsm.length < 1;
         if (hooksC) {
            break;
         }
      } while (hooksC && (!policyb));
          let trophy8 = 1.0;
         policyb = combinedK == trophy8;
      episodesh += `${parseInt(`${orangeo}`) | selly.length}`;
   while ((3 << (Math.min(4, Math.abs(clockq)))) == 2) {
      clockq ^= saveq;
      break;
   }
      episodesh += `${(selly == String.fromCharCode(56,0) ? parseInt(`${gemfilej}`) : selly.length)}`;
   for (let q = 0; q < 1; q++) {
       let filledM = 4.0;
      for (let e = 0; e < 1; e++) {
          let middlewareD = String.fromCharCode(112,114,111,98,115,95,119,95,52,56,0);
          let toponC = 1;
         filledM -= parseFloat(`${middlewareD.length}`);
         middlewareD += `${2 >> (Math.min(4, Math.abs(toponC)))}`;
         toponC |= toponC;
      }
      while ((filledM - 2.55) < 2.21 || 4.89 < (2.55 - filledM)) {
         filledM -= parseFloat(`${2 % (Math.max(parseInt(`${filledM}`), 8))}`);
         break;
      }
       let appleM = String.fromCharCode(111,95,49,57,95,107,101,112,116,0);
      logoU = new Map([[`${logoU.size}`, logoU.size << (Math.min(Math.abs(3), 1))]]);
   }
   if (4 <= saveq) {
      saveq ^= parseInt(`${coreL}`);
   }
   let editX = logoU.size <= 5823304;
   do {
      logoU = new Map([[`${darkz}`, darkz & 3]]);
      if (editX) {
         break;
      }
   } while (editX && (2 > (parseInt(`${gemfilej}`) / (Math.max(3, logoU.size)))));
      megaphonef = `${parseInt(`${coreL}`) % 3}`;
   let stringsR = 9174626 >= saveq;
   do {
      saveq ^= 3 & clockq;
      if (stringsR) {
         break;
      }
   } while (stringsR && (!episodesh.includes(`${saveq}`)));
      megaphonef = `${megaphonef.length}`;
      reactx *= episodesh.length / 2;

    RNExitApp.exitApp();
  }, []);

  const handleAccept = useCallback(() => {
       let configureM: Array<any> = [710, 891];
    let casty = 0.0;
    let subs8: Map<any, any> = new Map([[String.fromCharCode(101,95,52,95,115,118,97,103,0),899], [String.fromCharCode(107,95,51,48,95,102,111,114,107,0),688]]);
    let catalogE = 4;
    let topicy: Array<any> = [509, 555];
    let langkey1 = String.fromCharCode(103,95,55,53,95,108,111,110,103,109,117,108,97,119,0);
    let statisticsp: Array<any> = [String.fromCharCode(122,95,56,53,95,100,101,105,110,116,101,114,108,101,97,118,101,0), String.fromCharCode(117,95,52,50,95,114,101,110,100,101,114,0)];
    let actiony = true;
    let fastY = String.fromCharCode(122,95,50,54,95,97,100,100,105,116,105,111,110,97,108,0);
    let containerv = 1.0;
    let homeA = String.fromCharCode(120,95,53,51,95,115,99,97,110,115,0);
    let checkboxf = String.fromCharCode(112,97,115,115,112,111,114,116,95,50,95,57,56,0);
    let statisticsh: Map<any, any> = new Map([[String.fromCharCode(114,101,108,97,121,101,100,95,108,95,49,52,0),String.fromCharCode(104,95,57,53,95,97,121,98,114,0)], [String.fromCharCode(120,99,104,103,95,103,95,57,0),String.fromCharCode(98,95,52,54,95,99,111,109,112,105,108,101,114,0)]]);
    let away9 = 1.0;
    let nextx = 4.0;
       let combineq: Map<any, any> = new Map([[String.fromCharCode(108,95,56,52,95,105,100,97,116,97,0),String.fromCharCode(114,101,102,95,116,95,56,51,0)], [String.fromCharCode(112,114,101,115,101,116,95,99,95,49,49,0),String.fromCharCode(109,95,52,48,95,114,101,115,112,0)], [String.fromCharCode(105,110,118,111,108,118,101,100,95,107,95,49,50,0),String.fromCharCode(102,105,110,103,101,114,115,95,112,95,54,56,0)]]);
       let leftg = 5.0;
       let filedP: Map<any, any> = new Map([[String.fromCharCode(105,110,116,101,114,110,101,100,95,52,95,54,55,0),900], [String.fromCharCode(102,105,108,101,115,95,53,95,53,48,0),54]]);
         leftg /= Math.max(3, parseFloat(`${1}`));
       let fastforwardZ = String.fromCharCode(115,105,110,101,115,95,55,95,55,48,0);
         fastforwardZ = `${parseInt(`${leftg}`) | 2}`;
       let nativeexx = true;
      if (4 == fastforwardZ.length || !nativeexx) {
          let emptyX = 0;
          let transfere = 0.0;
          let chartX = String.fromCharCode(102,114,111,109,98,105,110,100,95,50,95,49,49,0);
          let traminis = 0;
          let canvasA = String.fromCharCode(100,111,119,110,115,104,105,102,116,95,98,95,51,49,0);
         fastforwardZ += `${((nativeexx ? 2 : 2) >> (Math.min(chartX.length, 5)))}`;
         emptyX %= Math.max(5, traminis >> (Math.min(Math.abs(2), 1)));
         transfere /= Math.max(parseFloat(`${emptyX * 3}`), 3);
         chartX += `${1 << (Math.min(Math.abs(traminis), 1))}`;
         canvasA += "3";
      }
      if (2 >= filedP.size || 4 >= (filedP.size ^ 2)) {
          let zhengpianw: Map<any, any> = new Map([[String.fromCharCode(108,95,51,48,95,114,101,118,105,115,105,111,110,0),985], [String.fromCharCode(106,95,54,52,95,112,97,114,101,110,116,104,101,115,105,115,0),1]]);
          let china8 = 4.0;
         filedP.set(`${leftg}`, parseInt(`${china8}`));
         zhengpianw = new Map([[`${zhengpianw.size}`, zhengpianw.size % 2]]);
      }
      let tooltips1 = combineq.size >= 6124107;
      do {
          let injuryq = 3;
          let mbridgeQ: Map<any, any> = new Map([[String.fromCharCode(105,95,51,51,95,116,121,112,101,115,101,116,116,101,114,0),394], [String.fromCharCode(113,117,97,110,116,95,117,95,49,53,0),784]]);
          let rulesI = 5.0;
          let z_playerU = String.fromCharCode(115,101,110,100,116,111,95,115,95,54,56,0);
         combineq = new Map([[`${filedP.size}`, combineq.size]]);
         injuryq *= injuryq;
         mbridgeQ.set(`${rulesI}`, 3 & mbridgeQ.size);
         rulesI -= parseFloat(`${1}`);
         z_playerU += `${mbridgeQ.size * 1}`;
         if (tooltips1) {
            break;
         }
      } while (((combineq.size + parseInt(`${leftg}`)) > 1) && tooltips1);
      for (let d = 0; d < 3; d++) {
         combineq = new Map([[fastforwardZ, 3]]);
      }
         filedP.set(fastforwardZ, (String.fromCharCode(70,0) == fastforwardZ ? fastforwardZ.length : parseInt(`${leftg}`)));
      configureM.push(2);
   let sheetS = 5144168.0 >= containerv;
   do {
      containerv /= Math.max(1, parseInt(`${containerv}`) << (Math.min(2, Math.abs(2))));
      if (sheetS) {
         break;
      }
   } while (sheetS && ((containerv / (Math.max(langkey1.length, 9))) == 2.42 && 1 == (parseInt(`${containerv}`) / (Math.max(langkey1.length, 5)))));
       let nativeexq = String.fromCharCode(107,95,55,95,108,122,115,115,0);
       let handlerU: Map<any, any> = new Map([[String.fromCharCode(99,114,101,97,116,101,101,120,95,113,95,52,55,0),String.fromCharCode(116,95,52,48,95,99,111,114,101,115,0)], [String.fromCharCode(109,97,114,103,105,110,115,95,113,95,55,52,0),String.fromCharCode(97,95,52,50,95,114,101,116,114,97,110,115,109,105,116,115,0)], [String.fromCharCode(102,100,99,116,100,115,112,95,111,95,53,55,0),String.fromCharCode(109,95,52,53,95,115,101,114,105,97,108,110,111,0)]]);
         nativeexq += `${1 + handlerU.size}`;
      for (let l = 0; l < 3; l++) {
         handlerU.set(nativeexq, handlerU.size ^ 1);
      }
          let headero = String.fromCharCode(105,110,116,101,103,101,114,115,95,48,95,52,52,0);
          let windK = String.fromCharCode(102,117,110,99,116,105,111,110,115,95,107,95,52,55,0);
          let update_5T = true;
         handlerU.set(`${update_5T}`, 3);
         headero = `${headero.length}`;
         windK = `${1 / (Math.max(4, windK.length))}`;
         handlerU = new Map([[`${handlerU.size}`, nativeexq.length & handlerU.size]]);
          let reportm: Map<any, any> = new Map([[String.fromCharCode(108,95,54,53,95,99,97,118,115,105,100,99,116,0),604], [String.fromCharCode(112,104,105,95,56,95,54,53,0),643], [String.fromCharCode(110,97,109,101,115,112,97,99,101,95,101,95,51,56,0),217]]);
         handlerU.set(`${nativeexq}`, handlerU.size);
         reportm = new Map([[`${reportm.size}`, reportm.size]]);
      for (let t = 0; t < 2; t++) {
         handlerU = new Map([[`${handlerU.size}`, (String.fromCharCode(110,0) == nativeexq ? handlerU.size : nativeexq.length)]]);
      }
      configureM = [3 | catalogE];
      fastY += `${homeA.length}`;
      actiony = parseInt(`${containerv}`) > statisticsp.length;
      fastY = `${(String.fromCharCode(83,0) == fastY ? configureM.length : fastY.length)}`;
   while (parseInt(`${containerv}`) <= configureM.length) {
       let groupV = 4.0;
       let closeA = String.fromCharCode(115,111,99,105,97,108,95,115,95,57,53,0);
       let whatsappK = String.fromCharCode(101,118,114,99,95,51,95,56,51,0);
      let bridgeF = 4931389 <= closeA.length;
      do {
          let footballe = String.fromCharCode(102,95,49,48,48,0);
         closeA = `${(footballe == String.fromCharCode(48,0) ? footballe.length : parseInt(`${groupV}`))}`;
         if (bridgeF) {
            break;
         }
      } while ((2 <= whatsappK.length && 2 <= closeA.length) && bridgeF);
         closeA = `${2 % (Math.max(7, closeA.length))}`;
          let maila: Map<any, any> = new Map([[String.fromCharCode(115,95,50,56,95,98,108,111,111,109,0),false ], [String.fromCharCode(120,95,57,49,95,101,120,116,101,110,115,105,111,110,115,0),false ], [String.fromCharCode(97,121,111,117,116,95,51,95,51,53,0),true ]]);
         groupV -= 2;
         maila = new Map([[`${maila.size}`, maila.size >> (Math.min(Math.abs(2), 2))]]);
          let constantsb = true;
          let humidityi = String.fromCharCode(106,95,54,53,95,109,97,120,106,0);
          let backgroundN: Map<any, any> = new Map([[String.fromCharCode(100,101,105,110,116,95,56,95,49,54,0),195], [String.fromCharCode(98,111,117,110,100,97,114,121,95,115,95,53,55,0),532]]);
         groupV /= Math.max(3, 3 | closeA.length);
         constantsb = (backgroundN.size | humidityi.length) == 57;
         humidityi = `${humidityi.length * backgroundN.size}`;
          let fieldJ: Map<any, any> = new Map([[String.fromCharCode(98,117,99,107,101,116,97,108,108,111,99,95,107,95,56,50,0),String.fromCharCode(99,95,48,95,108,117,114,97,108,105,122,97,116,105,111,110,0)], [String.fromCharCode(118,112,111,105,110,116,95,110,95,52,54,0),String.fromCharCode(108,95,53,57,0)]]);
         groupV -= (whatsappK == String.fromCharCode(65,0) ? whatsappK.length : closeA.length);
         fieldJ = new Map([[`${fieldJ.size}`, fieldJ.size & fieldJ.size]]);
      let promotionN = closeA.length >= 8332876;
      do {
         closeA = `${whatsappK.length}`;
         if (promotionN) {
            break;
         }
      } while (promotionN && (whatsappK != String.fromCharCode(77,0)));
         whatsappK = `${parseInt(`${groupV}`)}`;
         closeA += `${closeA.length}`;
      if (1 >= closeA.length) {
         whatsappK = `${closeA.length}`;
      }
      containerv /= Math.max(1, langkey1.length / (Math.max(fastY.length, 1)));
      break;
   }
   if (langkey1 != fastY) {
       let eighteeng: Map<any, any> = new Map([[String.fromCharCode(107,95,52,95,101,118,112,111,114,116,0),310], [String.fromCharCode(116,95,51,56,95,99,105,112,104,101,114,98,121,110,105,100,0),142]]);
      if (4 <= (eighteeng.size & 2) && (eighteeng.size & eighteeng.size) <= 2) {
          let options7: Map<any, any> = new Map([[String.fromCharCode(99,95,57,95,108,101,97,118,101,0),String.fromCharCode(112,95,49,51,95,112,114,111,118,105,100,101,114,115,0)], [String.fromCharCode(100,95,51,48,95,102,102,97,116,0),String.fromCharCode(106,95,52,0)], [String.fromCharCode(114,97,112,105,100,95,121,95,48,0),String.fromCharCode(103,95,52,95,115,117,103,103,101,115,116,105,111,110,115,0)]]);
          let statsK = 3.0;
          let rightL = 1.0;
          let v_playerH = String.fromCharCode(112,114,101,102,105,120,101,100,95,51,95,51,53,0);
          let connection9 = String.fromCharCode(97,95,56,49,95,112,117,116,115,0);
         eighteeng = new Map([[`${eighteeng.size}`, 3]]);
         options7.set(`${rightL}`, connection9.length - parseInt(`${rightL}`));
         statsK += parseFloat(`${v_playerH.length | parseInt(`${statsK}`)}`);
         v_playerH = `${2 ^ options7.size}`;
         connection9 += `${parseInt(`${rightL}`)}`;
      }
      let sinad = 8668609 >= eighteeng.size;
      do {
         eighteeng = new Map([[`${eighteeng.size}`, eighteeng.size ^ 3]]);
         if (sinad) {
            break;
         }
      } while (((eighteeng.size % (Math.max(eighteeng.size, 9))) < 1 && (eighteeng.size % (Math.max(3, eighteeng.size))) < 1) && sinad);
       let stringsR: Array<any> = [518, 853];
      fastY = "2";
   }
      actiony = 78 <= homeA.length;
      containerv += 3 >> (Math.min(2, topicy.length));
   if (1 > (topicy.length << (Math.min(5, statisticsp.length))) || (statisticsp.length << (Math.min(topicy.length, 2))) > 1) {
       let datal = 2.0;
       let rewindI: Map<any, any> = new Map([[String.fromCharCode(98,95,50,53,95,98,117,110,100,108,101,0),424], [String.fromCharCode(114,105,112,101,109,100,95,51,95,49,51,0),495]]);
       let pausey: Array<any> = [93, 331, 236];
      for (let l = 0; l < 2; l++) {
         rewindI.set(`${pausey.length}`, rewindI.size % (Math.max(10, pausey.length)));
      }
         rewindI = new Map([[`${pausey.length}`, pausey.length & 3]]);
      let dropdown5 = datal <= 5591668.0;
      do {
         datal += 1 >> (Math.min(2, pausey.length));
         if (dropdown5) {
            break;
         }
      } while ((4.86 > datal) && dropdown5);
       let phonev = String.fromCharCode(100,114,98,103,95,54,95,48,0);
       let targett = String.fromCharCode(101,100,105,116,101,100,95,55,95,54,0);
      let yellow6 = pausey.length <= 8183211;
      do {
         pausey.push(2 ^ targett.length);
         if (yellow6) {
            break;
         }
      } while (yellow6 && ((datal + 3.36) >= 4.62));
         pausey = [rewindI.size];
      if (rewindI.get(`${datal}`) != null) {
          let nalytics0 = String.fromCharCode(97,108,105,103,110,109,101,110,116,95,108,95,54,54,0);
         rewindI = new Map([[nalytics0, (nalytics0 == String.fromCharCode(114,0) ? nalytics0.length : parseInt(`${datal}`))]]);
      }
      let materialj = targett.length >= 7273019;
      do {
          let signinup8 = false;
         targett = `${targett.length << (Math.min(Math.abs(2), 1))}`;
         signinup8 = !signinup8;
         if (materialj) {
            break;
         }
      } while (materialj && (!targett.endsWith(phonev)));
      for (let t = 0; t < 1; t++) {
         datal -= parseInt(`${datal}`) / (Math.max(3, 3));
      }
      statisticsp.push(3);
   }
   for (let g = 0; g < 3; g++) {
      configureM = [homeA.length ^ 2];
   }
   for (let m = 0; m < 3; m++) {
      fastY += `${((actiony ? 4 : 3) & catalogE)}`;
   }

    setIsVisible(false)

       let robotor = String.fromCharCode(115,117,98,116,114,101,101,95,53,95,56,0);
       let activea = false;
         robotor = `${((activea ? 2 : 3) >> (Math.min(robotor.length, 2)))}`;
      let reactnativejsO = robotor == String.fromCharCode(97,110,112,122,100,97,113,114,122,0);
      do {
          let short_9_Q: Array<any> = [123, 715, 538];
         robotor += `${(1 & (activea ? 3 : 3))}`;
         short_9_Q = [short_9_Q.length | short_9_Q.length];
         if (reactnativejsO) {
            break;
         }
      } while ((!robotor.includes(`${activea}`)) && reactnativejsO);
      subs8.set(robotor, 2);
      topicy.push(homeA.length);
   if (4.67 == (casty / (Math.max(4.40, 9))) && 5 == (langkey1.length + 3)) {
       let foregroundo = 0;
       let mbnativeadvanceds = 4.0;
       let playlistU: Array<any> = [38, 896, 398];
       let streamingE = String.fromCharCode(107,95,53,50,95,117,110,105,113,117,101,108,121,0);
       let invitew = String.fromCharCode(102,97,116,101,95,50,95,50,0);
       let navigationr = String.fromCharCode(105,95,52,95,115,112,101,99,0);
       let reportM = String.fromCharCode(99,95,52,53,95,97,98,105,0);
      if ((invitew.length + 5) > 5 && 2 > (invitew.length + 5)) {
         invitew += `${playlistU.length}`;
      }
      if ((foregroundo >> (Math.min(Math.abs(5), 2))) >= 3) {
         foregroundo *= parseInt(`${mbnativeadvanceds}`);
      }
      for (let f = 0; f < 1; f++) {
          let reminderP = 0;
          let mathT = 5.0;
          let historym = 4.0;
         reportM = `${2 << (Math.min(5, Math.abs(foregroundo)))}`;
         reminderP |= parseInt(`${mathT}`) ^ 3;
         mathT /= Math.max(parseFloat(`${parseInt(`${historym}`)}`), 2);
      }
      if (2 >= foregroundo) {
         navigationr += `${streamingE.length}`;
      }
      if ((parseInt(`${mbnativeadvanceds}`) / (Math.max(navigationr.length, 3))) < 4 && (navigationr.length & 4) < 4) {
         mbnativeadvanceds *= 1 - parseInt(`${mbnativeadvanceds}`);
      }
      while (invitew.endsWith(`${playlistU.length}`)) {
          let preview8: Map<any, any> = new Map([[String.fromCharCode(114,95,55,51,95,117,112,108,111,97,100,115,0),true ], [String.fromCharCode(115,95,53,55,95,101,102,102,101,99,105,116,118,101,108,121,0),false ], [String.fromCharCode(115,116,114,105,114,101,112,108,97,99,101,95,108,95,53,57,0),true ]]);
          let regengJ = 4;
         playlistU.push(streamingE.length);
         preview8.set(`${regengJ}`, regengJ);
         break;
      }
         navigationr = `${parseInt(`${mbnativeadvanceds}`) + foregroundo}`;
      langkey1 += `${statisticsp.length}`;
   }
   let membershipy = 9538778.0 >= casty;
   do {
      casty -= parseFloat(`${3 ^ subs8.size}`);
      if (membershipy) {
         break;
      }
   } while (((4 << (Math.min(2, homeA.length))) >= 4) && membershipy);
      statisticsp.push((parseInt(`${casty}`) ^ (actiony ? 3 : 2)));
      statisticsp = [catalogE];
   let friends_ = 6791021.0 <= containerv;
   do {
      containerv /= Math.max(topicy.length, 1);
      if (friends_) {
         break;
      }
   } while (friends_ && ((containerv * 2.18) > 5.4));
   let detailsI = statisticsp.length >= 5182666;
   do {
       let ballw = 3.0;
       let catagoryG = String.fromCharCode(119,95,56,50,95,116,106,117,116,105,108,0);
       let otherQ = 5.0;
         ballw -= parseInt(`${ballw}`) ^ catagoryG.length;
      if (3.16 < (3.62 * ballw) || (ballw * catagoryG.length) < 3.62) {
         catagoryG = `${3 & parseInt(`${ballw}`)}`;
      }
         otherQ += parseFloat(`${1}`);
         catagoryG = `${catagoryG.length - parseInt(`${ballw}`)}`;
      while (5.35 < ballw) {
         ballw += (String.fromCharCode(87,0) == catagoryG ? catagoryG.length : parseInt(`${otherQ}`));
         break;
      }
      while ((4 * ballw) >= 3.41) {
          let singlee: Array<any> = [685, 860];
          let baidu3: Array<any> = [4, 856, 947];
          let temp4 = 1.0;
          let backgroundD = String.fromCharCode(120,95,51,55,95,115,121,109,98,111,108,105,99,97,116,105,111,110,0);
          let referrerD = String.fromCharCode(101,118,112,111,114,116,95,48,95,54,51,0);
         otherQ -= parseFloat(`${2}`);
         singlee.push((String.fromCharCode(89,0) == backgroundD ? backgroundD.length : referrerD.length));
         baidu3.push(3 >> (Math.min(4, backgroundD.length)));
         temp4 -= parseFloat(`${2}`);
         referrerD += `${referrerD.length / (Math.max(2, 5))}`;
         break;
      }
      for (let k = 0; k < 1; k++) {
         catagoryG = `${(String.fromCharCode(77,0) == catagoryG ? parseInt(`${ballw}`) : catagoryG.length)}`;
      }
          let switch_sv = String.fromCharCode(116,116,114,105,98,117,116,101,100,95,119,95,57,57,0);
          let path4 = 1;
         ballw += catagoryG.length;
         switch_sv = `${2 | switch_sv.length}`;
         path4 /= Math.max(2, switch_sv.length);
         otherQ += parseFloat(`${parseInt(`${otherQ}`)}`);
      statisticsp.push(catagoryG.length);
      if (detailsI) {
         break;
      }
   } while (((1 & statisticsp.length) < 2 || 3 < (1 & subs8.size)) && detailsI);
      checkboxf = `${((actiony ? 2 : 4))}`;
      checkboxf = `${configureM.length / (Math.max(checkboxf.length, 8))}`;
   if ((langkey1.length & configureM.length) >= 3) {
      langkey1 += `${fastY.length}`;
   }
      fastY += `${fastY.length}`;
      checkboxf += `${catalogE}`;
    setAccepted(true)

   for (let z = 0; z < 3; z++) {
      statisticsp.push(catalogE & 2);
   }
   if (actiony) {
      actiony = configureM.length >= langkey1.length;
   }
   while (checkboxf.length > 1) {
      checkboxf += `${configureM.length | 2}`;
      break;
   }
   while (!fastY.endsWith(`${statisticsp.length}`)) {
       let singleX = String.fromCharCode(114,95,55,50,95,112,114,101,112,97,114,101,0);
       let backh = true;
      for (let y = 0; y < 2; y++) {
         backh = !singleX.startsWith(`${backh}`);
      }
      while (!backh) {
         backh = !backh || singleX.length < 78;
         break;
      }
         singleX += `${((backh ? 2 : 3) << (Math.min(singleX.length, 1)))}`;
          let l_unlockh = String.fromCharCode(99,95,52,55,95,97,116,116,105,98,117,116,101,115,0);
          let carouselH = false;
          let foregroundb = String.fromCharCode(99,95,53,53,95,114,101,108,105,97,98,108,101,0);
         backh = (17 > (singleX.length >> (Math.min(2, Math.abs((backh ? singleX.length : 17))))));
         l_unlockh += `${((carouselH ? 5 : 2))}`;
         carouselH = 83 <= foregroundb.length;
         foregroundb += `${(2 ^ (carouselH ? 3 : 4))}`;
       let rulesf: Array<any> = [488, 902, 382];
       let calendarm: Array<any> = [572, 310];
      for (let y = 0; y < 3; y++) {
         backh = 15 <= rulesf.length;
      }
      fastY = "2";
      break;
   }
      statisticsp.push(1 ^ parseInt(`${casty}`));
      containerv *= (homeA.length - (actiony ? 5 : 5));
   for (let m = 0; m < 1; m++) {
       let countdownz = String.fromCharCode(119,95,54,54,95,115,116,114,105,107,101,116,104,114,111,117,103,104,115,0);
       let stylesw: Map<any, any> = new Map([[String.fromCharCode(112,97,114,115,101,100,95,114,95,54,54,0),435], [String.fromCharCode(104,119,97,101,115,95,98,95,49,0),907]]);
       let t_playerS = true;
      for (let g = 0; g < 3; g++) {
         t_playerS = stylesw.get(`${t_playerS}`) != null;
      }
      for (let m = 0; m < 2; m++) {
         countdownz += `${((t_playerS ? 1 : 1) % (Math.max(2, 3)))}`;
      }
      let mappingp = 9329649 >= stylesw.size;
      do {
         stylesw = new Map([[`${stylesw.size}`, (3 % (Math.max(2, (t_playerS ? 1 : 4))))]]);
         if (mappingp) {
            break;
         }
      } while ((t_playerS) && mappingp);
          let securityY = String.fromCharCode(103,95,52,52,95,115,108,105,112,0);
          let foregroundZ: Array<any> = [628, 881, 172];
         stylesw.set(securityY, countdownz.length >> (Math.min(Math.abs(3), 5)));
         securityY = `${foregroundZ.length % (Math.max(foregroundZ.length, 5))}`;
         stylesw.set(`${t_playerS}`, stylesw.size % 1);
         stylesw = new Map([[`${stylesw.size}`, ((t_playerS ? 3 : 1) ^ stylesw.size)]]);
      for (let s = 0; s < 3; s++) {
          let gray5: Array<any> = [853, 708, 991];
          let l_lock5 = 1.0;
          let sinaC: Map<any, any> = new Map([[String.fromCharCode(97,114,114,97,121,95,109,95,49,52,0),380], [String.fromCharCode(115,95,51,95,100,101,98,97,110,100,0),490]]);
         stylesw = new Map([[`${sinaC.size}`, stylesw.size]]);
         gray5 = [2 << (Math.min(Math.abs(parseInt(`${l_lock5}`)), 1))];
         l_lock5 -= parseFloat(`${1 + parseInt(`${l_lock5}`)}`);
         sinaC = new Map([[`${gray5.length}`, 1 % (Math.max(10, parseInt(`${l_lock5}`)))]]);
      }
      while ((2 | stylesw.size) >= 5 || 3 >= (2 | stylesw.size)) {
         countdownz += `${stylesw.size | 2}`;
         break;
      }
       let sportB: Array<any> = [876, 78, 432];
       let langA: Array<any> = [457, 771];
      checkboxf += `${statisticsh.size}`;
   }
      configureM = [statisticsp.length >> (Math.min(Math.abs(3), 4))];
   let applicationq = topicy.length <= 6778362;
   do {
      topicy = [(statisticsh.size >> (Math.min(4, Math.abs((actiony ? 5 : 1)))))];
      if (applicationq) {
         break;
      }
   } while ((5 < (checkboxf.length % (Math.max(1, 5)))) && applicationq);
   for (let z = 0; z < 2; z++) {
      topicy.push(configureM.length);
   }
      configureM = [2];
   if ((2 << (Math.min(5, configureM.length))) == 4) {
      catalogE <<= Math.min(Math.abs(3 | topicy.length), 2);
   }
   let flipper5 = 6630259.0 >= containerv;
   do {
       let whatsappG = 2;
       let rulesQ = String.fromCharCode(99,104,97,115,101,95,115,95,53,0);
       let sheety = 2.0;
         rulesQ = `${parseInt(`${sheety}`) << (Math.min(4, Math.abs(2)))}`;
      while ((sheety / (Math.max(4.33, 3))) < 4.37) {
         whatsappG %= Math.max(5, parseInt(`${sheety}`) & 1);
         break;
      }
      if ((rulesQ.length & 5) > 5) {
         rulesQ += `${(String.fromCharCode(49,0) == rulesQ ? whatsappG : rulesQ.length)}`;
      }
          let hooksT = String.fromCharCode(122,95,50,55,95,110,117,109,101,114,105,102,121,0);
         rulesQ += `${whatsappG}`;
         hooksT = `${hooksT.length % 1}`;
      while (5.70 <= (sheety - 4.98)) {
         rulesQ = "2";
         break;
      }
      let androidk = rulesQ.length <= 8441937;
      do {
         rulesQ = `${parseInt(`${sheety}`)}`;
         if (androidk) {
            break;
         }
      } while ((rulesQ.includes(`${whatsappG}`)) && androidk);
       let switch_wj: Map<any, any> = new Map([[String.fromCharCode(102,105,110,100,110,101,116,95,119,95,56,49,0),440], [String.fromCharCode(100,101,102,105,110,101,115,95,101,95,54,0),736]]);
         sheety += switch_wj.size * 1;
      if (!Array.from(switch_wj.values()).includes(whatsappG)) {
         switch_wj = new Map([[`${switch_wj.size}`, parseInt(`${sheety}`)]]);
      }
      containerv += 2 * parseInt(`${casty}`);
      if (flipper5) {
         break;
      }
   } while ((statisticsp.includes(containerv)) && flipper5);
    dispatch(acceptPrivacyPolicy())
  }, [])

  const handleReadTermsPrivacy = useCallback((view: '用户协议' | '隐私政策') => {
    const checkGoBack = ({ data }) => {
      if (data.state.routes[data.state.routes.length - 1].name === 'Home') {
        setIsVisible(true);
        navigation.removeListener('state', checkGoBack);
      }
    }
    navigation.addListener('state', checkGoBack);

    navigation.navigate(view);
    setIsVisible(false);
  }, [])

  return (
    <>
      {isVisible &&
        <View
          style={{
            height: '100%',
            width: '100%',
            backgroundColor: colors.background + 'BB',
            position: 'absolute',
            justifyContent: 'center',
            alignItems: 'center'
          }}>
          <View style={{
            width: '75%',
            padding: 20,
            gap: spacing.l,
            backgroundColor: colors.background,
            ...styles.overlay,
          }}>
            <Text style={{ ...textVariants.header, textAlign: 'center' }}>服务协议和隐私政策</Text>
            <Text
              style={{
                ...textVariants.subBody,
                textAlign: 'center',
              }}>
              <Text>
                请你务必审慎阅读,
                充分理解“服务协议”和“隐私政策”各条款，包括但不限于：为了更好的向你提供服务，我们需要收集你的设备标识，操作日常等信息用于分析，优化应用性能。你可阅读
              </Text>
              <Text onPress={() => handleReadTermsPrivacy('用户协议')}>
                <Text style={{ color: colors.primary }}>《服务协议》</Text>
              </Text>
              <Text>和</Text>
              <Text onPress={() => handleReadTermsPrivacy('隐私政策')}>
                <Text style={{ color: colors.primary }}>《隐私政策》</Text>
              </Text>
              <Text>
                了解详细信息。如果你同意，请点击下面按钮开始接受我们的服务。
              </Text>
            </Text>
            <View style={styles.btns}>
              <TouchableOpacity activeOpacity={0.85} style={styles.btn} onPress={handleReject}>
                <Text
                  style={{
                    ...textVariants.body,
                    ...styles.head,
                  }}>
                  暂不同意
                </Text>
              </TouchableOpacity>
              <TouchableOpacity activeOpacity={0.85} style={styles.btn} onPress={handleAccept}>
                <Text
                  style={{
                    ...textVariants.body,
                    ...styles.head,
                    color: IS_OTHER_SKIN ? colors.confirm : colors.primary,
                  }}>
                  同意并接受
                </Text>
              </TouchableOpacity>
            </View>
          </View>
        </View>
      }
    </>
  )
}

const styles = StyleSheet.create({
  overlay: {
    borderRadius: 14,
  },
  head: {
    textAlign: 'center',
  },
  body: {
    color: 'white',
  },
  link: {
    display: 'flex',
  },
  btns: {
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
  },
  btn: {
    flex: 1,
  },
});
