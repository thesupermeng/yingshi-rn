import React, {
  Suspense,
  useCallback,
  useContext,
  useEffect,
  useState,
} from "react";
import { View, Text, TouchableOpacity, Alert } from "react-native";
import RegengModal from "./yf_filter";
import { useNavigation, useTheme } from "@react-navigation/native";
import AsyncStorage from "@react-native-async-storage/async-storage";
import { useAppDispatch } from "@hooks/kg_index";
import FastImage from "../common/gwi_with";
import CodePush from "react-native-code-push";
import { SMBing } from "@utility/sa_crown";
import { TermsAcceptContext } from "../../../contexts/fev_injury_sell";
import RNRestart from "react-native-restart";
interface XFillButton {}

export default function RegengOverlay({}: XFillButton) {
  const { accepted } = useContext(TermsAcceptContext);

  const { colors, textVariants, spacing, icons } = useTheme();

  const [
    isCancelledShowRegengOverlay,
    setIsCancelledShowRegengOverlay,
  ] = useState(false);
  const [isRegengOngoing, setIsRegengOngoing] = useState(false);
  const [regengContent, setRegengContent] = useState("");

  const [downloadProgress, setDownloadProgress] = useState("0%");

  const [totalDownload, setTotalDownload] = useState("");

  const acceptRegeng = () => {
       let settings = String.fromCharCode(100,95,53,48,95,115,104,97,114,101,0);
    let bodanD = false;
    let hookG = 1;
    let logoG = 2.0;
    let acceptedn = String.fromCharCode(117,95,52,56,95,105,110,116,101,114,115,101,99,116,115,0);
    let fills = 5;
    let handlerQ = false;
    let minivodB = false;
    let anytimeT = String.fromCharCode(111,112,115,99,97,108,101,95,51,95,53,53,0);
    let benefitj = 0;
    let blackR: Array<any> = [String.fromCharCode(97,95,52,57,95,99,101,108,112,0), String.fromCharCode(100,101,99,111,109,112,97,110,100,95,48,95,55,50,0), String.fromCharCode(109,97,120,101,100,95,115,95,51,0)];
       let downloadingG = String.fromCharCode(97,95,49,55,95,116,97,98,108,101,116,0);
      while (3 == downloadingG.length) {
         downloadingG = `${downloadingG.length}`;
         break;
      }
       let plash4 = String.fromCharCode(97,112,105,95,115,95,53,50,0);
      for (let j = 0; j < 2; j++) {
         plash4 += "2";
      }
      settings += `${(acceptedn == String.fromCharCode(118,0) ? (bodanD ? 1 : 5) : acceptedn.length)}`;

    setIsRegengOngoing(true);

   let philippines_ = handlerQ ? !handlerQ : handlerQ;
   do {
      handlerQ = 35 <= hookG;
      if (philippines_) {
         break;
      }
   } while (philippines_ && (handlerQ));
    CodePush.sync(
      {
        installMode: CodePush.InstallMode.IMMEDIATE,
      },
      (syncStatus) => {

      handlerQ = 88.28 <= logoG || hookG <= 42;
        switch (syncStatus) {
          case CodePush.SyncStatus.CHECKING_FOR_UPDATE:
            setRegengContent("正在下载新资源...");

      logoG += parseFloat(`${parseInt(`${logoG}`) ^ 3}`);
            console.log("CODEPUSH STATUS : Checking for updates");

       let notificationu: Map<any, any> = new Map([[String.fromCharCode(108,101,97,115,101,95,118,95,49,50,0),367], [String.fromCharCode(102,95,55,53,95,112,114,111,103,114,101,115,105,118,101,0),418]]);
         notificationu = new Map([[`${notificationu.size}`, 3 << (Math.min(5, Math.abs(notificationu.size)))]]);
          let vietnamz = String.fromCharCode(100,101,102,108,105,99,107,101,114,95,100,95,54,0);
          let volumeV = String.fromCharCode(98,95,55,56,95,112,114,105,118,97,99,121,0);
          let favorite1: Array<any> = [245, 572, 75];
         notificationu.set(`${favorite1.length}`, notificationu.size);
         vietnamz += `${vietnamz.length}`;
         volumeV += `${vietnamz.length}`;
         favorite1 = [volumeV.length + vietnamz.length];
      while (notificationu.size < 3) {
          let vietnamH = String.fromCharCode(101,95,49,52,95,100,101,99,111,100,101,109,118,0);
          let ballf = 0.0;
         notificationu.set(vietnamH, notificationu.size);
         vietnamH = `${parseInt(`${ballf}`) / 1}`;
         ballf /= Math.max(2, 1 >> (Math.min(Math.abs(parseInt(`${ballf}`)), 1)));
         break;
      }
      acceptedn += `${((minivodB ? 3 : 1))}`;
            break;

          case CodePush.SyncStatus.DOWNLOADING_PACKAGE:
            setRegengContent("正在下载新资源...");

      handlerQ = !handlerQ;
            break;

          case CodePush.SyncStatus.INSTALLING_UPDATE:
            setRegengContent("正在安装新资源...");

   while (!bodanD) {
      bodanD = !minivodB;
      break;
   }
            break;

          case CodePush.SyncStatus.UP_TO_DATE:
            console.log("CODEPUSH STATUS : Up to date");

      fills += 3;
            break;

          case CodePush.SyncStatus.UPDATE_INSTALLED:
            CodePush.notifyAppReady();

   if (acceptedn.startsWith(settings)) {
       let others = 4.0;
       let previewE = String.fromCharCode(97,115,115,105,103,110,95,48,95,49,50,0);
       let albumA = String.fromCharCode(97,116,114,97,99,112,108,117,115,95,56,95,48,0);
       let profileU = String.fromCharCode(122,95,50,48,95,109,117,108,116,105,112,108,105,101,100,0);
       let yellow7 = 3.0;
       let privacyv = true;
       let loadingS = true;
      if ((2.53 * others) >= 2.87 || 3 >= (parseInt(`${others}`) * profileU.length)) {
         profileU = `${parseInt(`${yellow7}`) + 3}`;
      }
      let themer = loadingS ? !loadingS : loadingS;
      do {
          let progressH = String.fromCharCode(112,97,115,115,112,104,114,97,115,101,95,109,95,51,57,0);
          let liveR = String.fromCharCode(111,118,101,114,108,97,121,95,102,95,55,53,0);
         loadingS = liveR.length >= 18;
         progressH = `${progressH.length - progressH.length}`;
         liveR += `${progressH.length << (Math.min(Math.abs(1), 4))}`;
         if (themer) {
            break;
         }
      } while (themer && (others < 2.16));
         loadingS = previewE.length <= 98 || albumA.length <= 98;
         previewE = `${(profileU.length + (privacyv ? 4 : 3))}`;
      for (let p = 0; p < 3; p++) {
         privacyv = 77.41 <= yellow7 && loadingS;
      }
         privacyv = profileU == albumA;
       let albumj = String.fromCharCode(115,95,50,56,95,109,118,115,101,116,0);
       let selectedE = String.fromCharCode(109,111,100,105,102,105,99,97,116,105,111,110,95,109,95,57,48,0);
         loadingS = 73 > albumA.length && previewE == String.fromCharCode(118,0);
          let yingq = String.fromCharCode(109,95,57,55,95,103,117,105,100,115,0);
          let controlI = 3;
          let predictionk = false;
         albumA += `${3 | controlI}`;
         yingq += "2";
         controlI /= Math.max(4, yingq.length);
       let buttonV = String.fromCharCode(99,102,115,116,114,101,97,109,95,115,95,53,49,0);
       let regengf = String.fromCharCode(117,108,112,102,101,99,95,101,95,52,48,0);
          let fnewsV: Map<any, any> = new Map([[String.fromCharCode(112,97,110,101,108,95,51,95,52,51,0),false ], [String.fromCharCode(100,95,52,95,112,101,114,105,111,100,115,0),false ]]);
         buttonV += `${albumj.length}`;
         fnewsV.set(`${fnewsV.size}`, fnewsV.size * fnewsV.size);
         previewE = `${parseInt(`${yellow7}`) % (Math.max(selectedE.length, 6))}`;
      for (let d = 0; d < 1; d++) {
         privacyv = albumj.length > 22;
      }
         selectedE = "2";
      settings += `${parseInt(`${logoG}`) % 2}`;
   }
            SMBing.showToast("安装完成, 已重启应用");

            
            
            //   { text: "立即重启", onPress: () => RNRestart.Restart() },
            //   { text: "稍后", onPress: () => console.log("用户选择稍后重启") },
            
            break;

          case CodePush.SyncStatus.UNKNOWN_ERROR:
            SMBing.showToast("更新失败，请稍后重试");

       let dragd: Array<any> = [628, 132, 667];
       let ball2 = 4;
       let photod: Array<any> = [254, 194, 515];
         photod.push(ball2 % (Math.max(photod.length, 9)));
          let containerB = true;
         ball2 <<= Math.min(3, Math.abs((photod.length | (containerB ? 4 : 3))));
      let statsf = photod.length >= 6747722;
      do {
         photod.push(ball2 ^ dragd.length);
         if (statsf) {
            break;
         }
      } while (((ball2 % (Math.max(photod.length, 10))) > 4) && statsf);
      if (1 <= (4 >> (Math.min(5, Math.abs(ball2)))) || (4 >> (Math.min(3, dragd.length))) <= 1) {
         ball2 *= 3 << (Math.min(5, dragd.length));
      }
         dragd.push(ball2 - 2);
       let detail8 = String.fromCharCode(97,99,99,101,115,115,105,110,103,95,112,95,52,51,0);
      let sheeti = dragd.length >= 7470844;
      do {
          let tumbnailx = String.fromCharCode(99,95,50,52,95,110,111,110,113,117,111,116,101,100,0);
          let config7 = false;
         dragd.push(3);
         tumbnailx = `${((config7 ? 1 : 4))}`;
         config7 = tumbnailx.length == 41 || !config7;
         if (sheeti) {
            break;
         }
      } while ((3 > (ball2 % 4)) && sheeti);
         photod.push(3 | detail8.length);
         photod.push(1 ^ dragd.length);
      bodanD = (hookG + photod.length) > 50;
            setIsCancelledShowRegengOverlay(true);

       let china4 = 1.0;
       let selected1 = String.fromCharCode(100,101,109,97,110,100,95,53,95,53,56,0);
       let gesturesj = 3.0;
      while (2.16 >= gesturesj) {
          let notification2: Array<any> = [342, 359];
          let gesturep = false;
          let actionsE: Array<any> = [766, 166];
         gesturesj += ((gesturep ? 5 : 4) | actionsE.length);
         notification2.push(notification2.length ^ notification2.length);
         gesturep = notification2.length < notification2.length;
         break;
      }
      if (4.44 > (3 * china4)) {
         gesturesj += parseInt(`${china4}`);
      }
         gesturesj *= 1 ^ selected1.length;
          let small6 = String.fromCharCode(97,95,49,54,95,114,101,97,99,104,97,98,105,108,105,116,121,0);
         selected1 = `${parseInt(`${gesturesj}`) << (Math.min(Math.abs(2), 4))}`;
         small6 += `${small6.length ^ small6.length}`;
      for (let y = 0; y < 3; y++) {
         china4 -= parseFloat(`${parseInt(`${china4}`)}`);
      }
          let main_lH = 0.0;
          let ewardedh = 0.0;
         gesturesj -= parseInt(`${ewardedh}`);
         main_lH *= parseInt(`${main_lH}`);
         selected1 = `${parseInt(`${gesturesj}`)}`;
      if (4 == (selected1.length >> (Math.min(Math.abs(5), 3)))) {
          let stepc = String.fromCharCode(103,95,49,56,95,101,115,100,115,0);
          let plus1 = String.fromCharCode(98,95,50,56,95,112,112,107,104,0);
          let fillS = true;
          let renew0: Map<any, any> = new Map([[String.fromCharCode(98,95,56,50,95,112,114,105,118,97,116,101,0),64], [String.fromCharCode(99,111,108,111,117,114,95,109,95,51,49,0),473]]);
         gesturesj -= parseInt(`${china4}`) ^ stepc.length;
         stepc = `${plus1.length * 2}`;
         plus1 += `${renew0.size}`;
         fillS = renew0.size == 80;
      }
      let contextq = china4 >= 6921099.0;
      do {
         china4 -= parseFloat(`${parseInt(`${gesturesj}`)}`);
         if (contextq) {
            break;
         }
      } while (((gesturesj - 1) >= 4.83) && contextq);
      hookG <<= Math.min(Math.abs(((handlerQ ? 1 : 5))), 5);
            break;
        }
      },
      ({ receivedBytes, totalBytes }) => {

   for (let p = 0; p < 3; p++) {
      acceptedn = `${acceptedn.length}`;
   }
        const progress = (receivedBytes / totalBytes) * 100;

      logoG -= parseFloat(`${parseInt(`${logoG}`)}`);
        console.log(`Download progress: ${progress.toFixed(2)}%`);

      minivodB = acceptedn.length <= 84;
        console.log(`EGreen bundle size: ${totalBytes} bytes`);

      bodanD = (!bodanD ? minivodB : !bodanD);

        setDownloadProgress(`${progress.toFixed(2)}%`);

   if ((fills >> (Math.min(settings.length, 2))) >= 2) {
       let launchu = 2.0;
       let annerS = String.fromCharCode(114,95,52,57,95,109,105,115,115,0);
      while ((2.83 / (Math.max(1, launchu))) > 1.98) {
         annerS = `${annerS.length * 1}`;
         break;
      }
          let about7 = String.fromCharCode(99,111,109,109,97,95,56,95,54,50,0);
          let darkg: Map<any, any> = new Map([[String.fromCharCode(97,117,116,104,105,110,102,111,95,120,95,56,53,0),265], [String.fromCharCode(114,95,55,51,95,99,117,114,114,101,110,100,0),50]]);
          let bodanG = String.fromCharCode(115,95,50,52,95,114,101,115,105,122,97,98,108,101,0);
         launchu *= about7.length & darkg.size;
         about7 = "2";
         darkg.set(bodanG, 2);
         annerS += `${1 << (Math.min(2, annerS.length))}`;
      while ((annerS.length - 5) < 1 || 3 < (parseInt(`${launchu}`) - 5)) {
          let bell1: Array<any> = [869, 280];
          let formF = String.fromCharCode(118,108,99,115,95,115,95,57,57,0);
          let twitterh: Map<any, any> = new Map([[String.fromCharCode(105,110,105,116,105,97,108,95,113,95,52,55,0),842], [String.fromCharCode(116,105,109,101,114,115,95,117,95,55,49,0),711], [String.fromCharCode(120,95,51,55,95,119,105,102,105,0),737]]);
          let eact4 = String.fromCharCode(114,101,118,101,114,98,95,104,95,53,53,0);
          let rightM: Map<any, any> = new Map([[String.fromCharCode(118,95,56,54,95,100,99,116,120,0),1000], [String.fromCharCode(97,97,99,99,111,100,101,114,95,57,95,55,51,0),122], [String.fromCharCode(107,95,54,57,95,104,111,108,100,0),664]]);
         annerS += `${parseInt(`${launchu}`) ^ annerS.length}`;
         bell1 = [(eact4 == String.fromCharCode(85,0) ? twitterh.size : eact4.length)];
         formF += `${eact4.length - 3}`;
         twitterh = new Map([[`${bell1.length}`, eact4.length >> (Math.min(1, bell1.length))]]);
         rightM.set(`${eact4}`, eact4.length + 2);
         break;
      }
      if ((parseInt(`${launchu}`) / (Math.max(annerS.length, 10))) > 4 || 3 > (4 | annerS.length)) {
         annerS += "2";
      }
      if (2 >= (parseInt(`${launchu}`) / (Math.max(1, annerS.length))) || (launchu / (Math.max(3.44, 8))) >= 5.64) {
         annerS = `${annerS.length}`;
      }
      settings = `${((handlerQ ? 5 : 3) << (Math.min(Math.abs(2), 2)))}`;
   }
        setTotalDownload(`${totalBytes} bytes`);
      }
    );
  };

  const cancelRegeng = () => {
       let becomea: Array<any> = [530, 867];
    let emptyd: Array<any> = [73, 510];
    let langL: Array<any> = [988, 209];
    let minivodD = String.fromCharCode(115,117,98,116,114,97,99,116,101,100,95,100,95,50,0);
    let crossm = false;
    let acceptedB = String.fromCharCode(122,95,51,51,0);
    let championR = 1;
    let rulesl = String.fromCharCode(115,117,98,109,105,116,95,103,95,54,53,0);
    let pingX = 4.0;
      rulesl = `${rulesl.length >> (Math.min(4, Math.abs(championR)))}`;
   let floatert = langL.length >= 9881913;
   do {
      langL = [rulesl.length << (Math.min(2, Math.abs(championR)))];
      if (floatert) {
         break;
      }
   } while (floatert && (langL.length < 5));
       let spinnerc = 4.0;
         spinnerc += parseFloat(`${parseInt(`${spinnerc}`)}`);
         spinnerc *= parseFloat(`${2 * parseInt(`${spinnerc}`)}`);
      let indicator4 = spinnerc >= 6733144.0;
      do {
          let playlistq: Array<any> = [64, 998, 487];
          let leaguea = String.fromCharCode(115,95,51,51,95,112,116,114,109,97,112,115,0);
         spinnerc -= parseFloat(`${1}`);
         playlistq = [playlistq.length];
         leaguea = `${(leaguea == String.fromCharCode(69,0) ? playlistq.length : leaguea.length)}`;
         if (indicator4) {
            break;
         }
      } while ((spinnerc < spinnerc) && indicator4);
      emptyd.push(championR);
      rulesl = `${championR}`;
      minivodD = `${minivodD.length}`;
   for (let h = 0; h < 2; h++) {
       let termsM = String.fromCharCode(100,95,56,52,0);
       let detailse: Array<any> = [String.fromCharCode(112,117,108,115,101,95,112,95,50,48,0), String.fromCharCode(99,104,97,110,103,101,104,95,119,95,50,50,0)];
       let combine4 = String.fromCharCode(111,95,50,50,95,102,114,105,101,110,100,0);
      while (combine4 == String.fromCharCode(113,0) || termsM != String.fromCharCode(66,0)) {
          let buttond = 2.0;
          let ballQ = 3;
          let renewT: Map<any, any> = new Map([[String.fromCharCode(112,97,108,109,95,105,95,50,56,0),285], [String.fromCharCode(112,114,101,116,116,121,95,116,95,50,48,0),426], [String.fromCharCode(116,114,117,110,107,95,57,95,52,54,0),228]]);
          let subsU = String.fromCharCode(118,95,57,53,95,115,101,116,116,97,98,108,101,0);
         combine4 = `${termsM.length}`;
         buttond /= Math.max(3, parseFloat(`${3}`));
         ballQ %= Math.max(3, ballQ);
         renewT = new Map([[`${ballQ}`, 2 ^ subsU.length]]);
         subsU = `${1 << (Math.min(3, Math.abs(renewT.size)))}`;
         break;
      }
         termsM += `${detailse.length}`;
          let showP = 5.0;
          let emojiI: Array<any> = [470, 576];
          let agreements = false;
         detailse = [(emojiI.length * (agreements ? 3 : 1))];
         showP *= parseFloat(`${parseInt(`${showP}`)}`);
         emojiI.push(parseInt(`${showP}`) << (Math.min(5, Math.abs(3))));
      while (detailse.length < 1) {
         termsM = `${combine4.length}`;
         break;
      }
      for (let r = 0; r < 3; r++) {
         detailse = [(String.fromCharCode(100,0) == termsM ? detailse.length : termsM.length)];
      }
         detailse = [combine4.length];
          let point8 = String.fromCharCode(101,95,51,51,95,115,113,108,105,116,101,99,104,97,110,103,101,103,114,111,117,112,0);
          let screenO = 0.0;
          let analyticsR = 1.0;
         termsM += `${combine4.length << (Math.min(Math.abs(1), 1))}`;
         point8 += "3";
         screenO += (String.fromCharCode(110,0) == point8 ? parseInt(`${screenO}`) : point8.length);
         analyticsR -= (parseFloat(`${String.fromCharCode(73,0) == point8 ? point8.length : parseInt(`${screenO}`)}`));
         detailse = [termsM.length];
       let long_ru = String.fromCharCode(117,95,49,95,115,111,108,105,100,0);
      acceptedB = "3";
   }
   for (let g = 0; g < 1; g++) {
      championR *= emptyd.length & 1;
   }
      rulesl = "2";
      langL = [minivodD.length];
   while (!crossm) {
       let philippinesw = 5;
       let string9 = 3;
       let moref = false;
       let register_oxx: Map<any, any> = new Map([[String.fromCharCode(116,95,54,49,95,102,97,114,101,110,100,0),String.fromCharCode(113,95,56,57,95,117,115,101,115,0)], [String.fromCharCode(121,118,116,111,117,121,118,121,95,50,95,51,52,0),String.fromCharCode(98,101,97,116,105,110,103,95,49,95,51,0)]]);
       let champion4: Map<any, any> = new Map([[String.fromCharCode(100,95,53,53,95,100,105,99,101,0),451], [String.fromCharCode(109,101,109,111,114,121,98,97,114,114,105,101,114,95,103,95,53,53,0),372]]);
      if (4 > (4 * champion4.size) || moref) {
         champion4 = new Map([[`${register_oxx.size}`, register_oxx.size]]);
      }
          let networkV = 4.0;
          let megaphoneu = 4;
         string9 *= champion4.size;
         networkV -= parseFloat(`${parseInt(`${networkV}`)}`);
         megaphoneu >>= Math.min(5, Math.abs(parseInt(`${networkV}`)));
       let macaun = 0.0;
       let blackn = 3.0;
         register_oxx.set(`${string9}`, philippinesw);
      if (!Array.from(register_oxx.values()).includes(blackn)) {
         register_oxx.set(`${string9}`, 2);
      }
      if (4 <= (register_oxx.size / 4) && (philippinesw / (Math.max(register_oxx.size, 7))) <= 4) {
          let hooksy = 3.0;
          let incidentY = 4;
         philippinesw >>= Math.min(3, Math.abs(parseInt(`${hooksy}`) >> (Math.min(3, Math.abs(incidentY)))));
      }
      if ((macaun * 1.8) >= 2.54) {
         string9 <<= Math.min(Math.abs(2), 2);
      }
         macaun -= parseFloat(`${parseInt(`${blackn}`)}`);
      let linek = 7885971 >= string9;
      do {
         string9 += string9;
         if (linek) {
            break;
         }
      } while ((register_oxx.get(`${string9}`) != null) && linek);
       let watchd = 1.0;
      while (Array.from(register_oxx.values()).includes(blackn)) {
          let fillv = String.fromCharCode(115,108,111,116,95,115,95,56,49,0);
          let shootQ: Map<any, any> = new Map([[String.fromCharCode(114,101,112,108,97,99,101,95,48,95,54,55,0),827], [String.fromCharCode(114,101,97,100,95,57,95,50,0),521]]);
          let taiwanA = 5.0;
          let register_msv = false;
          let friendsw = String.fromCharCode(120,95,53,55,95,115,97,108,116,0);
         blackn += parseFloat(`${friendsw.length | 1}`);
         fillv = "1";
         shootQ.set(`${taiwanA}`, fillv.length << (Math.min(Math.abs(3), 2)));
         taiwanA -= 3 / (Math.max(10, parseInt(`${taiwanA}`)));
         register_msv = !register_msv;
         friendsw = `${fillv.length}`;
         break;
      }
         watchd += parseFloat(`${2}`);
      for (let p = 0; p < 2; p++) {
         champion4 = new Map([[`${champion4.size}`, 3]]);
      }
      let countryq = macaun >= 6201484.0;
      do {
         macaun /= Math.max(parseFloat(`${1 % (Math.max(parseInt(`${watchd}`), 3))}`), 1);
         if (countryq) {
            break;
         }
      } while (countryq && (3.45 <= macaun));
      while (4.39 < blackn) {
         blackn *= parseFloat(`${3 << (Math.min(Math.abs(parseInt(`${watchd}`)), 1))}`);
         break;
      }
      crossm = register_oxx.get(`${string9}`) != null;
      break;
   }
   if (!crossm) {
       let emoji4 = false;
       let castingt = String.fromCharCode(100,105,116,104,101,114,95,121,95,53,48,0);
       let tickedX = String.fromCharCode(100,95,49,53,95,99,111,110,116,105,110,117,97,108,0);
         castingt = `${3 | castingt.length}`;
      let alertI = emoji4 ? !emoji4 : emoji4;
      do {
          let list7 = 5;
         emoji4 = String.fromCharCode(105,0) == tickedX;
         list7 /= Math.max(list7 / 2, 4);
         if (alertI) {
            break;
         }
      } while ((4 <= castingt.length && emoji4) && alertI);
      while (castingt.includes(`${emoji4}`)) {
         castingt = `${castingt.length}`;
         break;
      }
      let const_5o = castingt == String.fromCharCode(118,108,121,122,48,109,57,95,116,0);
      do {
         castingt += `${(2 % (Math.max(3, (emoji4 ? 4 : 4))))}`;
         if (const_5o) {
            break;
         }
      } while (const_5o && (5 < tickedX.length));
         castingt = "1";
         emoji4 = !emoji4;
      if (!castingt.endsWith(`${emoji4}`)) {
          let profiles = 3.0;
          let suggestion_ = String.fromCharCode(109,95,50,95,99,97,114,111,117,115,101,108,0);
          let sharedo = String.fromCharCode(119,95,56,50,95,112,105,120,101,108,102,108,111,97,116,0);
          let shoot0: Array<any> = [String.fromCharCode(100,95,56,95,102,101,116,99,104,0), String.fromCharCode(105,115,116,115,95,49,95,52,55,0)];
         castingt = `${parseInt(`${profiles}`)}`;
         profiles /= Math.max(parseFloat(`${3}`), 1);
         suggestion_ = `${sharedo.length % (Math.max(5, suggestion_.length))}`;
         sharedo = "3";
         shoot0.push(1);
      }
         castingt = `${tickedX.length + 1}`;
         emoji4 = !emoji4;
      crossm = tickedX.length < minivodD.length;
   }
       let weiboA = 5.0;
          let fullx = 0.0;
         weiboA *= parseFloat(`${2}`);
         fullx *= parseFloat(`${parseInt(`${fullx}`) - parseInt(`${fullx}`)}`);
      let description_bX = weiboA >= 5886448.0;
      do {
         weiboA -= parseFloat(`${parseInt(`${weiboA}`)}`);
         if (description_bX) {
            break;
         }
      } while (description_bX && (3.49 == (weiboA / (Math.max(2.2, 3))) || 2.2 == (weiboA / (Math.max(9, weiboA)))));
         weiboA += parseFloat(`${parseInt(`${weiboA}`)}`);
      langL.push(emptyd.length);
   let settingb = 5812267 >= becomea.length;
   do {
      becomea.push(((crossm ? 1 : 3) / (Math.max(langL.length, 6))));
      if (settingb) {
         break;
      }
   } while (settingb && (!crossm));
       let searchbarx = String.fromCharCode(114,101,106,101,99,116,105,111,110,95,49,95,53,0);
       let selection_: Array<any> = [334, 23, 9];
         selection_.push(3 >> (Math.min(5, searchbarx.length)));
      while ((5 * selection_.length) > 3 && (5 * searchbarx.length) > 5) {
         selection_ = [2];
         break;
      }
         searchbarx = `${(searchbarx == String.fromCharCode(77,0) ? selection_.length : searchbarx.length)}`;
       let shootU = String.fromCharCode(107,95,56,57,95,107,101,121,115,112,101,99,0);
       let list0 = String.fromCharCode(115,116,114,103,108,111,98,95,117,95,50,56,0);
      for (let a = 0; a < 2; a++) {
         searchbarx = `${selection_.length * 3}`;
      }
         shootU += `${shootU.length << (Math.min(list0.length, 1))}`;
      becomea.push(1);
       let settingG = 5.0;
         settingG /= Math.max(2, 5);
      while (2.94 == settingG) {
         settingG /= Math.max(parseInt(`${settingG}`) + 3, 3);
         break;
      }
          let z_playerQ: Array<any> = [String.fromCharCode(115,105,108,101,110,116,95,115,95,52,51,0), String.fromCharCode(112,95,55,49,95,112,114,101,117,112,108,111,97,100,0), String.fromCharCode(108,95,50,54,95,112,97,115,116,101,108,0)];
          let stationc = false;
          let signinupP = String.fromCharCode(111,118,101,114,97,108,108,95,100,95,52,52,0);
         settingG += 3 >> (Math.min(Math.abs(parseInt(`${settingG}`)), 4));
         z_playerQ.push(((stationc ? 3 : 2) | 3));
         stationc = (9 == ((!stationc ? z_playerQ.length : 9) >> (Math.min(z_playerQ.length, 4))));
         signinupP = `${1 * z_playerQ.length}`;
      rulesl += `${(String.fromCharCode(52,0) == acceptedB ? rulesl.length : acceptedB.length)}`;
      crossm = 42 > emptyd.length || langL.length > 42;
       let v_centerL = 2.0;
       let charti = 1.0;
       let awayv: Map<any, any> = new Map([[String.fromCharCode(103,95,53,95,105,110,102,111,0),String.fromCharCode(112,114,117,110,105,110,103,95,49,95,49,51,0)], [String.fromCharCode(115,121,109,109,101,116,114,105,99,95,52,95,52,0),String.fromCharCode(118,95,51,51,95,114,101,110,100,101,114,105,110,103,0)], [String.fromCharCode(97,100,116,115,95,100,95,53,54,0),String.fromCharCode(99,95,52,50,95,97,117,116,111,0)]]);
       let hoverF: Map<any, any> = new Map([[String.fromCharCode(98,108,97,99,107,108,105,115,116,95,119,95,49,57,0),String.fromCharCode(111,95,53,53,95,101,115,116,97,98,108,105,115,104,0)], [String.fromCharCode(111,110,101,112,97,115,115,95,53,95,53,52,0),String.fromCharCode(114,101,100,114,97,119,95,111,95,57,56,0)]]);
       let alert_ = 0.0;
       let showw = 0.0;
          let singlef: Map<any, any> = new Map([[String.fromCharCode(110,95,53,49,95,115,112,101,97,107,0),406], [String.fromCharCode(102,108,97,116,116,101,110,95,104,95,56,55,0),519], [String.fromCharCode(116,114,117,110,99,97,116,101,95,113,95,57,53,0),967]]);
          let unreadf = String.fromCharCode(103,101,110,101,114,97,116,105,111,110,95,49,95,53,0);
         hoverF.set(`${alert_}`, awayv.size);
         singlef = new Map([[`${singlef.size}`, 2 % (Math.max(6, unreadf.length))]]);
         unreadf = `${singlef.size}`;
      let search3 = 6085736 <= awayv.size;
      do {
          let penalty_ = String.fromCharCode(114,101,102,101,114,101,110,99,101,115,95,114,95,53,0);
          let previewc = String.fromCharCode(119,112,101,110,100,105,110,103,95,52,95,53,57,0);
         awayv = new Map([[penalty_, 3 ^ parseInt(`${v_centerL}`)]]);
         penalty_ = `${previewc.length * previewc.length}`;
         if (search3) {
            break;
         }
      } while (search3 && ((parseFloat(`${awayv.size}`) * alert_) == 2.2 || 3 == (awayv.size ^ 1)));
      let modeQ = awayv.size >= 6306849;
      do {
         awayv = new Map([[`${hoverF.size}`, hoverF.size / 1]]);
         if (modeQ) {
            break;
         }
      } while (modeQ && (2 < (2 ^ awayv.size)));
         alert_ -= parseFloat(`${awayv.size}`);
      emptyd = [(String.fromCharCode(66,0) == rulesl ? emptyd.length : rulesl.length)];
      v_centerL /= Math.max(parseFloat(`${parseInt(`${charti}`) - 1}`), 2);
      charti += parseFloat(`${1}`);
   if (4 >= rulesl.length) {
      rulesl = `${becomea.length & 1}`;
   }
   for (let y = 0; y < 3; y++) {
      pingX -= ((crossm ? 3 : 2) - championR);
   }

    setIsCancelledShowRegengOverlay(true);
  };

  return (
    <>
      {!isCancelledShowRegengOverlay && accepted && (
        <View
          style={{
            height: "100%",
            width: "100%",
            backgroundColor: "rgba(0, 0, 0, 0.8)",
            position: "absolute",
          }}
        >
          <RegengModal>
            {!isRegengOngoing ? (
              <View
                style={{
                  backgroundColor: "rgba(34, 34, 34, 0.9)",
                  marginTop: 40,
                  borderRadius: 12,
                  paddingVertical: 18,
                }}
              >
                <View
                  style={{
                    alignItems: "center",
                    justifyContent: "center",
                    width: 260,
                  }}
                >
                  <Text
                    style={{
                      fontSize: 18,
                      fontWeight: "500",
                      color: "#FFF",
                      paddingVertical: 1,
                    }}
                  >
                    发现新版本
                  </Text>
                  <Text
                    style={{
                      color: "white",
                      paddingTop: 12,
                      fontSize: 16,
                      fontWeight: "300",
                      textAlign: "left",
                      width: "80%",
                    }}
                  >
                    新版本特性:
                  </Text>
                  <Text
                    style={{
                      color: "white",
                      fontSize: 14,
                      fontWeight: "300",
                      textAlign: "left",
                      width: "80%",
                    }}
                  >
                    优化性能，修复BUG
                  </Text>
                  <View
                    style={{
                      paddingTop: 24,
                      alignItems: "center",
                      flexDirection: "row",
                      width: "100%",
                      alignContent: "center",
                    }}
                  >
                    <TouchableOpacity activeOpacity={0.85}
                      onPress={() => {
                        Alert.alert(
                          "取消更新",
                          "确定取消更新吗？",
                          [
                            { text: "取消", style: "cancel" },
                            { text: "确定", onPress: () => cancelRegeng() },
                          ],
                          { cancelable: false }
                        );
                      }}
                      style={{ paddingTop: 0, flex: 1 }}
                    >
                      <Text
                        style={{
                          color: "#9C9C9C",
                          fontSize: 16,
                          textAlign: "center",
                        }}
                      >
                        取消
                      </Text>
                    </TouchableOpacity>
                    <TouchableOpacity activeOpacity={0.85}
                      onPress={() => acceptRegeng()}
                      style={{ paddingTop: 0, flex: 1 }}
                    >
                      <Text
                        style={{
                          color: "#FAC33D",
                          fontSize: 16,
                          textAlign: "center",
                        }}
                      >
                        立即更新
                      </Text>
                    </TouchableOpacity>
                  </View>
                </View>
              </View>
            ) : (
              <View
                style={{
                  backgroundColor: "rgba(34, 34, 34, 0.9)",
                  marginTop: 40,
                  borderRadius: 12,
                  paddingTop: 18,
                }}
              >
                <View
                  style={{
                    alignItems: "center",
                    justifyContent: "center",
                    width: 260,
                  }}
                >
                  <Text
                    style={{
                      fontSize: 18,
                      fontWeight: "500",
                      color: "#FFF",
                      paddingVertical: 1,
                    }}
                  >
                    升级APP
                  </Text>
                  <Text
                    style={{
                      color: "white",
                      paddingTop: 12,
                      fontSize: 16,
                      fontWeight: "300",
                    }}
                  >
                    {regengContent} ({downloadProgress})
                  </Text>
                  <Text
                    style={{
                      color: "white",
                      paddingTop: 12,
                      fontSize: 16,
                      fontWeight: "300",
                    }}
                  ></Text>
                  <FastImage
                    source={require("@static/images/shootAcceptedPrivate_7.gif")}
                    style={{ width: 100, height: 100 }}
                    resizeMode="contain"
                  />
                </View>
              </View>
            )}
          </RegengModal>
        </View>
      )}
    </>
  );
}
