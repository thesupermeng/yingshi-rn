import React, {Suspense, useCallback, useEffect, useState} from 'react';
import {View, Text, TouchableOpacity} from 'react-native';
import VipModal from './mayi_fill';
import {useNavigation, useTheme} from '@react-navigation/native';
import AsyncStorage from '@react-native-async-storage/async-storage';
import {useAppDispatch} from '@hooks/mayi_redirect';
import {showLoginAction} from '@redux/actions/mayi_iconorientation_chatroombackground';

interface mayi_GoogleViews {
  remainingDay: number;
  showVIPOverlay: boolean;
  setShowVIPOverlay: any;
}

export default function ExpiredOverlay({
  remainingDay = 0,
  showVIPOverlay = false,
  setShowVIPOverlay = () => {
       let selectz = String.fromCharCode(99,119,114,115,105,95,107,95,52,50,0);
    let dices = String.fromCharCode(115,105,108,101,110,116,95,117,95,51,54,0);
    let thailand4 = String.fromCharCode(114,100,109,117,108,116,95,101,95,51,48,0);
    let halffieldimageZ = 4.0;
    let nterstitialV = 4;
    let modelb = 5.0;
    let rncore5 = String.fromCharCode(120,95,55,56,95,112,99,109,98,0);
    let helpers: Map<any, any> = new Map([[String.fromCharCode(98,117,116,116,101,114,102,108,121,95,97,95,51,49,0),605], [String.fromCharCode(115,104,111,119,105,110,103,95,107,95,57,48,0),277], [String.fromCharCode(114,95,51,52,95,97,118,105,111,0),532]]);
    let detaily: Map<any, any> = new Map([[String.fromCharCode(109,117,120,95,57,95,56,48,0),false ], [String.fromCharCode(106,95,52,49,95,110,108,109,101,97,110,115,0),true ], [String.fromCharCode(99,111,109,112,117,116,101,95,108,95,53,48,0),true ]]);
    let dycreatorN = String.fromCharCode(116,114,117,101,109,111,116,105,111,110,95,49,95,51,53,0);
    let kuaishour = true;
    let usernameK = 4.0;
    let scorek = 0.0;
    let o_unlockI = String.fromCharCode(113,95,52,50,95,105,110,108,105,103,104,116,0);
    let paginationP = 4;
    let footballfield6 = String.fromCharCode(112,97,115,115,95,114,95,57,49,0);
      detaily.set(`${halffieldimageZ}`, parseInt(`${halffieldimageZ}`) * dices.length);
   if (1 > nterstitialV) {
      dycreatorN = `${dycreatorN.length}`;
   }
      dycreatorN += `${selectz.length | 3}`;
   if (1.26 <= (halffieldimageZ - 4.41) && (4.41 - halffieldimageZ) <= 1.0) {
      dices += `${detaily.size}`;
   }
   if ((nterstitialV << (Math.min(Math.abs(3), 2))) > 3 && (nterstitialV << (Math.min(dices.length, 5))) > 3) {
       let activityc = String.fromCharCode(100,95,53,51,95,99,104,97,116,0);
       let megaphoneQ: Array<any> = [212, 95, 183];
       let liveshareE = 2.0;
       let statsnomoredatas: Array<any> = [String.fromCharCode(115,99,101,110,101,95,100,95,53,55,0), String.fromCharCode(109,117,108,116,95,48,95,52,57,0), String.fromCharCode(103,108,121,112,104,115,95,53,95,56,51,0)];
       let owngoalE: Array<any> = [25, 31];
         activityc += `${megaphoneQ.length}`;
         liveshareE += parseFloat(`${activityc.length}`);
      while (megaphoneQ.length == activityc.length) {
         activityc = `${megaphoneQ.length}`;
         break;
      }
      if ((statsnomoredatas.length ^ 3) < 1 || 4 < (3 ^ activityc.length)) {
          let iconwatchnowE = 5.0;
          let a_title4 = String.fromCharCode(108,95,57,55,95,102,105,110,100,101,114,0);
         statsnomoredatas.push(3);
         iconwatchnowE += parseFloat(`${a_title4.length >> (Math.min(3, Math.abs(parseInt(`${iconwatchnowE}`))))}`);
         a_title4 = `${a_title4.length % (Math.max(2, 7))}`;
      }
      if (statsnomoredatas.length >= 4) {
         statsnomoredatas = [megaphoneQ.length - 2];
      }
         owngoalE = [(String.fromCharCode(75,0) == activityc ? parseInt(`${liveshareE}`) : activityc.length)];
      while ((5 - statsnomoredatas.length) > 2 && 4 > (5 - statsnomoredatas.length)) {
          let eventX: Array<any> = [262, 231, 949];
         activityc = "3";
         eventX.push(eventX.length);
         break;
      }
       let homeE: Array<any> = [String.fromCharCode(112,114,111,116,101,99,116,105,111,110,95,102,95,49,57,0), String.fromCharCode(100,105,118,105,100,101,100,95,50,95,49,55,0), String.fromCharCode(105,100,99,116,120,100,99,95,103,95,50,57,0)];
      let baselinej = owngoalE.length <= 6444485;
      do {
         owngoalE.push(2 | activityc.length);
         if (baselinej) {
            break;
         }
      } while (baselinej && (owngoalE.includes(liveshareE)));
      if (!statsnomoredatas.includes(liveshareE)) {
          let rncoreG = 2;
         liveshareE /= Math.max(parseFloat(`${parseInt(`${liveshareE}`) + 3}`), 1);
         rncoreG -= rncoreG * rncoreG;
      }
         homeE.push(parseInt(`${liveshareE}`) - 2);
       let temperatureR = String.fromCharCode(116,95,53,55,95,117,116,105,108,105,116,121,0);
       let shielddoneD = String.fromCharCode(99,111,110,116,105,110,117,101,95,54,95,52,56,0);
       let nextU = false;
       let animationsu = false;
      let yellowcirclebgX = 5468619 >= megaphoneQ.length;
      do {
         megaphoneQ.push((String.fromCharCode(102,0) == shielddoneD ? shielddoneD.length : (nextU ? 1 : 3)));
         if (yellowcirclebgX) {
            break;
         }
      } while (yellowcirclebgX && (!megaphoneQ.includes(liveshareE)));
         activityc += `${((nextU ? 4 : 5) | parseInt(`${liveshareE}`))}`;
      dices += `${(String.fromCharCode(81,0) == rncore5 ? owngoalE.length : rncore5.length)}`;
   }
   for (let t = 0; t < 3; t++) {
      dices += `${helpers.size - detaily.size}`;
   }
   if (detaily.size < rncore5.length) {
      detaily.set(`${thailand4}`, 2);
   }
   while (selectz == dycreatorN) {
      dycreatorN = `${helpers.size}`;
      break;
   }
      nterstitialV >>= Math.min(Math.abs(helpers.size - 1), 3);
      detaily.set(`${kuaishour}`, 3);
   while ((detaily.size << (Math.min(rncore5.length, 1))) < 4 && 4 < (rncore5.length << (Math.min(2, Math.abs(detaily.size))))) {
      detaily.set(`${kuaishour}`, dices.length);
      break;
   }
      helpers.set(`${nterstitialV}`, dycreatorN.length);
   if ((nterstitialV << (Math.min(dycreatorN.length, 1))) == 2 || (nterstitialV << (Math.min(dycreatorN.length, 2))) == 2) {
       let controlsL = String.fromCharCode(99,100,101,114,114,111,114,95,106,95,56,48,0);
         controlsL += "3";
         controlsL = `${controlsL.length ^ 3}`;
      while (controlsL.includes(controlsL)) {
         controlsL += `${controlsL.length % 3}`;
         break;
      }
      nterstitialV /= Math.max(4, 3 | detaily.size);
   }
   if (1 == (helpers.size | rncore5.length) || (rncore5.length | 1) == 2) {
       let mappingz: Array<any> = [String.fromCharCode(103,95,57,56,95,112,105,120,99,116,120,0), String.fromCharCode(101,120,97,99,116,108,121,95,52,95,53,55,0), String.fromCharCode(114,114,111,114,95,99,95,51,51,0)];
          let groupQ: Map<any, any> = new Map([[String.fromCharCode(99,114,101,97,116,111,114,115,95,118,95,50,49,0),String.fromCharCode(101,110,99,108,111,115,105,110,103,95,118,95,52,51,0)], [String.fromCharCode(118,100,97,115,104,101,114,95,105,95,55,52,0),String.fromCharCode(116,95,53,95,99,104,111,115,101,110,0)]]);
          let runtimeschedulerR = 2.0;
          let hovern = String.fromCharCode(99,97,118,115,105,100,99,116,95,104,95,54,0);
         mappingz = [2 ^ mappingz.length];
         groupQ.set(`${runtimeschedulerR}`, parseInt(`${runtimeschedulerR}`) >> (Math.min(hovern.length, 2)));
         hovern = `${groupQ.size * 2}`;
       let colorsu = String.fromCharCode(102,111,110,116,115,105,122,101,95,104,95,49,0);
       let graphics0 = String.fromCharCode(115,95,53,52,95,99,111,109,112,111,117,110,100,0);
      for (let u = 0; u < 2; u++) {
         colorsu = `${graphics0.length ^ 2}`;
      }
      helpers = new Map([[`${helpers.size}`, selectz.length]]);
   }
      nterstitialV <<= Math.min(4, Math.abs(2 >> (Math.min(Math.abs(parseInt(`${halffieldimageZ}`)), 1))));
      detaily.set(`${nterstitialV}`, 1 * nterstitialV);
       let nativeex7 = String.fromCharCode(119,101,105,103,104,116,101,100,95,113,95,55,50,0);
       let iconarrowrightorangeC = String.fromCharCode(104,95,50,95,107,98,105,116,0);
       let textinputX = String.fromCharCode(116,104,114,101,115,104,111,108,100,115,95,111,95,56,48,0);
         nativeex7 = `${(nativeex7 == String.fromCharCode(86,0) ? textinputX.length : nativeex7.length)}`;
         iconarrowrightorangeC = `${nativeex7.length / (Math.max(1, iconarrowrightorangeC.length))}`;
      modelb -= dices.length | nterstitialV;
      detaily = new Map([[`${kuaishour}`, parseInt(`${usernameK}`) & 3]]);
      thailand4 += `${(selectz == String.fromCharCode(50,0) ? detaily.size : selectz.length)}`;
       let libflipperF = 3;
         libflipperF |= 2 & libflipperF;
      for (let f = 0; f < 2; f++) {
         libflipperF %= Math.max(3, libflipperF);
      }
       let s_positionB = String.fromCharCode(98,117,105,108,100,105,110,103,95,48,95,55,51,0);
       let baidu1 = String.fromCharCode(104,95,55,57,95,97,110,105,109,0);
      rncore5 = `${nterstitialV + 1}`;
   let homeiconA = nterstitialV >= 8604520;
   do {
      nterstitialV /= Math.max((parseInt(`${usernameK}`) ^ (kuaishour ? 1 : 2)), 4);
      if (homeiconA) {
         break;
      }
   } while (homeiconA && (4 >= (5 >> (Math.min(1, Math.abs(nterstitialV))))));
   if (!rncore5.includes(dycreatorN)) {
      rncore5 = "3";
   }
      usernameK += 1;
       let yellowscoreballo = String.fromCharCode(120,95,57,56,95,110,105,98,98,108,101,0);
         yellowscoreballo += `${yellowscoreballo.length}`;
      while (yellowscoreballo.endsWith(yellowscoreballo)) {
         yellowscoreballo += `${yellowscoreballo.length | 3}`;
         break;
      }
         yellowscoreballo = `${yellowscoreballo.length}`;
      nterstitialV %= Math.max(4, helpers.size);
   while (3 <= (detaily.size | 1) || (1 | dices.length) <= 2) {
       let whistlev: Array<any> = [54, 753, 24];
       let themeA: Array<any> = [931, 374];
       let mathw = 1.0;
      if (2 < themeA.length) {
         themeA = [whistlev.length & 3];
      }
          let ballH = true;
         whistlev = [((ballH ? 4 : 2) / (Math.max(parseInt(`${mathw}`), 1)))];
      if (!themeA.includes(mathw)) {
          let playy: Array<any> = [String.fromCharCode(98,97,110,100,101,100,95,49,95,51,50,0), String.fromCharCode(108,95,50,48,95,101,116,97,0)];
          let heartw = String.fromCharCode(97,110,99,104,111,114,105,110,103,95,111,95,51,0);
         mathw -= 1 << (Math.min(1, themeA.length));
         playy = [(String.fromCharCode(68,0) == heartw ? heartw.length : playy.length)];
      }
      for (let h = 0; h < 2; h++) {
         themeA.push(themeA.length);
      }
      let nalyticsl = mathw <= 8952925.0;
      do {
          let overlayd: Array<any> = [113, 52];
          let shootyesgoalA = String.fromCharCode(97,95,57,54,95,109,100,99,118,0);
         mathw *= overlayd.length ^ 1;
         overlayd = [(shootyesgoalA == String.fromCharCode(57,0) ? shootyesgoalA.length : shootyesgoalA.length)];
         if (nalyticsl) {
            break;
         }
      } while (nalyticsl && (2.71 > (3.17 + mathw)));
         mathw *= 1 ^ themeA.length;
          let arrowV = 4;
          let fastforwardc = 3.0;
         themeA = [1 | whistlev.length];
         arrowV += arrowV;
         fastforwardc *= parseInt(`${fastforwardc}`) ^ 2;
      if (4.90 == (mathw - 5.72) && 2.79 == (mathw - 5.72)) {
         themeA.push(3);
      }
         mathw += 1;
      dices += `${thailand4.length >> (Math.min(Math.abs(1), 1))}`;
      break;
   }
       let xnewinterstitialA = String.fromCharCode(100,97,116,97,116,121,112,101,115,95,104,95,53,49,0);
       let libglogS = 5;
         xnewinterstitialA = `${xnewinterstitialA.length}`;
      let predictionbuttons = 6243561 <= libglogS;
      do {
         libglogS ^= libglogS;
         if (predictionbuttons) {
            break;
         }
      } while ((!xnewinterstitialA.startsWith(`${libglogS}`)) && predictionbuttons);
          let anytimeS: Map<any, any> = new Map([[String.fromCharCode(110,101,97,114,108,121,95,55,95,52,49,0),472], [String.fromCharCode(122,95,56,51,95,105,109,112,111,114,116,101,114,0),572]]);
         libglogS += (xnewinterstitialA == String.fromCharCode(122,0) ? xnewinterstitialA.length : anytimeS.size);
         libglogS ^= 3;
       let linkl: Map<any, any> = new Map([[String.fromCharCode(105,95,54,55,95,112,101,110,99,105,108,0),true ], [String.fromCharCode(98,97,115,101,100,95,111,95,56,0),true ]]);
      while ((4 % (Math.max(2, xnewinterstitialA.length))) < 2 && 4 < (xnewinterstitialA.length % (Math.max(4, 10)))) {
          let rocket7: Array<any> = [679, 100, 735];
          let tumbnailq: Array<any> = [464, 592];
          let homeiconz = String.fromCharCode(99,95,52,51,95,115,97,100,100,0);
         xnewinterstitialA = `${xnewinterstitialA.length % 3}`;
         rocket7.push(1);
         tumbnailq.push(tumbnailq.length);
         homeiconz = `${3 / (Math.max(9, rocket7.length))}`;
         break;
      }
      selectz += `${rncore5.length}`;
       let guidel = true;
       let currentW: Map<any, any> = new Map([[String.fromCharCode(111,95,56,55,95,111,117,116,102,105,108,101,115,0),String.fromCharCode(107,95,53,48,95,100,117,109,112,105,110,103,0)], [String.fromCharCode(115,95,57,57,95,99,108,97,105,109,115,0),String.fromCharCode(97,112,97,99,107,101,116,95,102,95,56,56,0)], [String.fromCharCode(103,101,116,115,111,99,107,111,112,116,95,109,95,57,56,0),String.fromCharCode(118,95,50,51,95,115,101,99,0)]]);
         currentW = new Map([[`${currentW.size}`, (1 | (guidel ? 1 : 4))]]);
      for (let r = 0; r < 1; r++) {
         currentW.set(`${guidel}`, ((guidel ? 3 : 1) | currentW.size));
      }
      let yellowcirclebgO = 5208950 <= currentW.size;
      do {
          let short_bo = 3.0;
         currentW.set(`${short_bo}`, 2 << (Math.min(3, Math.abs(currentW.size))));
         if (yellowcirclebgO) {
            break;
         }
      } while (yellowcirclebgO && (!guidel || 3 < (currentW.size & 3)));
      if (guidel) {
         guidel = currentW.size >= 39 || !guidel;
      }
      for (let q = 0; q < 1; q++) {
          let imagemanagerF = 5.0;
          let mimeW = 5.0;
          let yellowtoredG = String.fromCharCode(112,101,99,101,110,116,95,102,95,49,57,0);
          let activea: Array<any> = [183, 736];
         guidel = (currentW.size * parseInt(`${imagemanagerF}`)) < 12;
         imagemanagerF *= 2 * parseInt(`${mimeW}`);
         mimeW -= parseInt(`${mimeW}`) << (Math.min(yellowtoredG.length, 4));
         yellowtoredG = `${activea.length | 2}`;
         activea = [activea.length | 3];
      }
      let profilen = 5742534 >= currentW.size;
      do {
          let catagoryC = String.fromCharCode(119,105,114,101,102,114,97,109,101,95,51,95,54,56,0);
          let interstitialj: Map<any, any> = new Map([[String.fromCharCode(109,95,53,52,95,98,116,110,0),10], [String.fromCharCode(118,95,57,49,95,110,99,111,109,105,110,103,0),298], [String.fromCharCode(97,95,54,54,95,97,115,115,111,99,105,97,116,101,0),975]]);
          let libfileQ = 2.0;
          let androidf = 0.0;
         currentW = new Map([[`${currentW.size}`, currentW.size << (Math.min(Math.abs(1), 4))]]);
         catagoryC += `${interstitialj.size}`;
         interstitialj = new Map([[`${interstitialj.size}`, interstitialj.size % 1]]);
         libfileQ *= 1 / (Math.max(10, parseInt(`${libfileQ}`)));
         androidf /= Math.max(3, 1);
         if (profilen) {
            break;
         }
      } while ((!guidel && 5 == (currentW.size - 1)) && profilen);
      halffieldimageZ *= currentW.size + 2;
       let annerB = 2;
       let goalI: Map<any, any> = new Map([[String.fromCharCode(115,104,97,100,101,114,95,100,95,49,55,0),652], [String.fromCharCode(108,95,56,49,95,99,105,114,99,108,101,0),504]]);
      while (5 == (goalI.size * annerB)) {
         goalI = new Map([[`${goalI.size}`, annerB]]);
         break;
      }
      let profileactiveo = annerB >= 6619236;
      do {
         annerB ^= goalI.size + annerB;
         if (profileactiveo) {
            break;
         }
      } while ((!Array.from(goalI.keys()).includes(`${annerB}`)) && profileactiveo);
      for (let c = 0; c < 2; c++) {
          let classes1 = false;
          let delegate_z4J = String.fromCharCode(108,95,54,49,95,113,115,118,100,101,99,0);
         annerB -= goalI.size;
         classes1 = delegate_z4J.length <= 93 && !classes1;
         delegate_z4J += "3";
      }
          let catalogb = 5.0;
          let foundp = 2.0;
         goalI = new Map([[`${goalI.size}`, annerB]]);
         catalogb -= parseInt(`${catalogb}`) / 3;
         foundp += parseInt(`${catalogb}`);
      while (Array.from(goalI.keys()).includes(`${annerB}`)) {
          let catalog0 = String.fromCharCode(99,95,55,54,95,97,117,100,105,111,99,111,110,118,101,114,116,0);
          let gesturesz = String.fromCharCode(98,95,50,53,95,119,104,101,110,0);
          let mail5 = 2;
          let refreshborderlessx = String.fromCharCode(108,95,49,56,95,112,114,111,99,101,101,100,0);
         goalI = new Map([[`${goalI.size}`, goalI.size >> (Math.min(Math.abs(2), 5))]]);
         catalog0 = `${(String.fromCharCode(57,0) == refreshborderlessx ? refreshborderlessx.length : catalog0.length)}`;
         gesturesz += `${refreshborderlessx.length | 3}`;
         mail5 ^= gesturesz.length | 1;
         break;
      }
       let yellowcirclebgn = false;
       let flyerm = true;
      helpers = new Map([[rncore5, 3 * rncore5.length]]);
      thailand4 += `${2 | helpers.size}`;
   let viewerk = usernameK >= 9391726.0;
   do {
      usernameK *= nterstitialV;
      if (viewerk) {
         break;
      }
   } while ((3 == (dices.length / (Math.max(5, 4))) && 5.74 == (usernameK + dices.length)) && viewerk);
   for (let q = 0; q < 2; q++) {
       let downG: Map<any, any> = new Map([[String.fromCharCode(117,118,114,100,95,109,95,57,49,0),true ], [String.fromCharCode(114,95,56,55,95,99,111,109,112,111,115,105,116,101,0),true ]]);
       let libreanimated1 = String.fromCharCode(112,114,101,100,105,99,97,116,101,95,97,95,54,57,0);
       let commentg = true;
         downG.set(`${libreanimated1}`, (libreanimated1 == String.fromCharCode(90,0) ? downG.size : libreanimated1.length));
      let sharedN = libreanimated1.length <= 6390622;
      do {
         libreanimated1 += `${downG.size / (Math.max(2, 3))}`;
         if (sharedN) {
            break;
         }
      } while (sharedN && (commentg));
      for (let z = 0; z < 1; z++) {
         commentg = libreanimated1.length <= 57;
      }
      if (!libreanimated1.includes(`${commentg}`)) {
         commentg = 52 > libreanimated1.length;
      }
         commentg = downG.size >= 53 || libreanimated1 == String.fromCharCode(69,0);
      while (4 >= (downG.size - 5) || 5 >= downG.size) {
         downG.set(`${commentg}`, ((commentg ? 5 : 3) >> (Math.min(Math.abs(1), 1))));
         break;
      }
          let greyarrowupA = String.fromCharCode(116,95,57,49,95,114,101,99,111,109,98,105,110,101,0);
          let tooltipsj = 1.0;
          let calendarL = String.fromCharCode(111,95,53,57,95,112,97,108,109,0);
         commentg = String.fromCharCode(100,0) == greyarrowupA && tooltipsj >= 84.26;
         greyarrowupA = `${calendarL.length % (Math.max(calendarL.length, 7))}`;
         tooltipsj *= calendarL.length + calendarL.length;
         libreanimated1 = `${(libreanimated1.length * (commentg ? 1 : 2))}`;
         libreanimated1 += `${((commentg ? 5 : 1) - 2)}`;
      detaily = new Map([[o_unlockI, parseInt(`${halffieldimageZ}`) << (Math.min(o_unlockI.length, 4))]]);
   }
   let cancel1 = thailand4.length >= 6616774;
   do {
      thailand4 = `${dycreatorN.length}`;
      if (cancel1) {
         break;
      }
   } while (cancel1 && (modelb == 1.33));
   while (detaily.size == 5) {
      dycreatorN = `${((kuaishour ? 4 : 1) & 1)}`;
      break;
   }
   for (let d = 0; d < 3; d++) {
       let basketballicony = false;
       let placeholderx = String.fromCharCode(109,100,110,115,95,102,95,52,52,0);
      let typesh = placeholderx == String.fromCharCode(107,109,97,57,48,53,117,109,109,113,0);
      do {
          let exampleimage4 = 1.0;
         placeholderx += `${(parseInt(`${exampleimage4}`) ^ (basketballicony ? 4 : 2))}`;
         if (typesh) {
            break;
         }
      } while (typesh && (3 > placeholderx.length));
       let layoutb = String.fromCharCode(104,95,53,50,95,99,111,108,100,0);
      while (!basketballicony) {
         placeholderx = `${layoutb.length}`;
         break;
      }
         layoutb += `${(placeholderx == String.fromCharCode(57,0) ? placeholderx.length : (basketballicony ? 4 : 4))}`;
      let toponp = basketballicony ? !basketballicony : basketballicony;
      do {
         basketballicony = 10 < layoutb.length && 10 < placeholderx.length;
         if (toponp) {
            break;
         }
      } while (toponp && (!basketballicony));
      for (let t = 0; t < 2; t++) {
          let paginationC = String.fromCharCode(112,114,111,109,112,101,103,95,119,95,50,54,0);
          let downarrowW: Map<any, any> = new Map([[String.fromCharCode(100,111,119,110,108,111,97,100,95,101,95,52,49,0),859], [String.fromCharCode(115,98,108,111,103,95,115,95,51,54,0),82], [String.fromCharCode(97,95,57,54,95,112,116,114,115,0),638]]);
         placeholderx = `${paginationC.length + 2}`;
         paginationC = `${downarrowW.size}`;
         downarrowW = new Map([[`${downarrowW.size}`, downarrowW.size * downarrowW.size]]);
      }
      detaily.set(o_unlockI, (o_unlockI == String.fromCharCode(68,0) ? o_unlockI.length : nterstitialV));
   }
       let iconstarF = String.fromCharCode(117,110,109,97,112,102,105,108,101,95,102,95,56,50,0);
       let ballX = String.fromCharCode(103,95,56,52,95,117,110,101,109,112,116,121,0);
         ballX = "1";
      if (!ballX.startsWith(iconstarF)) {
         iconstarF += `${iconstarF.length | 3}`;
      }
      if (ballX.length < iconstarF.length) {
         iconstarF = `${1 & iconstarF.length}`;
      }
      for (let f = 0; f < 3; f++) {
          let storeT = String.fromCharCode(100,101,97,99,116,105,118,97,116,101,95,52,95,52,52,0);
         iconstarF += `${iconstarF.length}`;
         storeT = "2";
      }
         iconstarF += `${ballX.length}`;
         iconstarF += "3";
      o_unlockI += `${iconstarF.length % (Math.max(3, 8))}`;
       let stringg = String.fromCharCode(97,117,114,97,95,112,95,56,57,0);
       let sheet9 = 5.0;
       let profilepicy: Map<any, any> = new Map([[String.fromCharCode(104,105,100,105,110,103,95,110,95,54,54,0),755], [String.fromCharCode(100,105,103,114,97,112,104,95,97,95,51,48,0),989]]);
      while ((stringg.length & profilepicy.size) > 2 && 3 > (2 & profilepicy.size)) {
         profilepicy.set(`${sheet9}`, parseInt(`${sheet9}`) - profilepicy.size);
         break;
      }
         stringg += `${parseInt(`${sheet9}`) ^ 1}`;
      while (1 < (profilepicy.size & stringg.length)) {
         profilepicy.set(stringg, stringg.length);
         break;
      }
      for (let t = 0; t < 2; t++) {
         stringg = `${(String.fromCharCode(87,0) == stringg ? profilepicy.size : stringg.length)}`;
      }
      let unimplementedview0 = sheet9 >= 7876175.0;
      do {
         sheet9 -= (parseFloat(`${String.fromCharCode(117,0) == stringg ? stringg.length : profilepicy.size}`));
         if (unimplementedview0) {
            break;
         }
      } while (unimplementedview0 && (sheet9 <= 5.64));
      let regengg = String.fromCharCode(103,97,120,107,48,0) == stringg;
      do {
         stringg = "1";
         if (regengg) {
            break;
         }
      } while (regengg && ((1 + stringg.length) >= 5 || 4.59 >= (4.43 * sheet9)));
      while (4 < (stringg.length & profilepicy.size) || (4 & stringg.length) < 3) {
          let moon6 = 5;
          let awayA: Array<any> = [277, 179, 459];
         stringg = "1";
         moon6 += moon6;
         awayA.push(awayA.length | 3);
         break;
      }
          let renderz = String.fromCharCode(115,95,53,55,95,108,97,117,110,99,104,101,100,0);
          let transferS = 0.0;
         stringg += `${(String.fromCharCode(100,0) == renderz ? renderz.length : stringg.length)}`;
         transferS += parseInt(`${transferS}`) % 1;
      if (3.88 <= (5.91 + sheet9) && 5.25 <= (5.91 + sheet9)) {
         sheet9 += parseFloat(`${profilepicy.size}`);
      }
      paginationP >>= Math.min(Math.abs(profilepicy.size), 3);
   while (nterstitialV >= paginationP) {
      nterstitialV >>= Math.min(1, Math.abs(parseInt(`${usernameK}`)));
      break;
   }
   let unselectedw = detaily.size <= 7604328;
   do {
      detaily.set(`${modelb}`, ((kuaishour ? 2 : 3) | parseInt(`${modelb}`)));
      if (unselectedw) {
         break;
      }
   } while ((5.100 == (scorek - 5.50)) && unselectedw);
   let catalogE = selectz.length >= 7473735;
   do {
       let iconclosewhitewithbgf = String.fromCharCode(101,108,108,105,112,116,105,99,97,108,95,50,95,51,50,0);
       let executorB = false;
       let fullscreenmaxU = String.fromCharCode(117,110,97,118,97,105,98,108,101,95,105,95,49,56,0);
      for (let z = 0; z < 1; z++) {
         iconclosewhitewithbgf = `${fullscreenmaxU.length % 2}`;
      }
      let wnewinterstitialb = String.fromCharCode(57,104,53,0) == iconclosewhitewithbgf;
      do {
         iconclosewhitewithbgf = `${fullscreenmaxU.length}`;
         if (wnewinterstitialb) {
            break;
         }
      } while (wnewinterstitialb && (fullscreenmaxU == iconclosewhitewithbgf));
       let statisticsa: Map<any, any> = new Map([[String.fromCharCode(101,105,116,104,101,114,95,53,95,54,53,0),false ], [String.fromCharCode(109,111,100,101,120,112,95,112,95,49,48,0),true ]]);
         statisticsa = new Map([[`${statisticsa.size}`, statisticsa.size]]);
         fullscreenmaxU = `${statisticsa.size}`;
          let libcxxcomponentsd: Map<any, any> = new Map([[String.fromCharCode(106,95,51,54,95,105,109,112,111,114,116,97,110,99,101,0),43], [String.fromCharCode(103,95,54,52,95,97,117,100,105,111,0),686]]);
         statisticsa = new Map([[`${statisticsa.size}`, ((executorB ? 1 : 4) - statisticsa.size)]]);
         libcxxcomponentsd.set(`${libcxxcomponentsd.size}`, libcxxcomponentsd.size & libcxxcomponentsd.size);
      while ((4 % (Math.max(2, statisticsa.size))) == 3 && (statisticsa.size % 4) == 2) {
         iconclosewhitewithbgf = "2";
         break;
      }
      for (let m = 0; m < 2; m++) {
          let subss = false;
          let exampleimageh = String.fromCharCode(114,101,99,101,110,116,108,121,95,99,95,56,50,0);
         fullscreenmaxU += `${((executorB ? 3 : 3))}`;
         subss = exampleimageh.length <= exampleimageh.length;
      }
         iconclosewhitewithbgf += `${((executorB ? 1 : 3))}`;
      selectz += `${1 >> (Math.min(1, fullscreenmaxU.length))}`;
      if (catalogE) {
         break;
      }
   } while ((2 >= (5 * selectz.length)) && catalogE);
   for (let v = 0; v < 3; v++) {
       let profileg = true;
       let informationK: Array<any> = [String.fromCharCode(111,108,100,108,105,115,116,95,100,95,51,57,0), String.fromCharCode(114,95,55,54,95,108,105,98,112,104,111,110,101,110,117,109,98,101,114,0), String.fromCharCode(113,117,97,114,116,95,56,95,55,50,0)];
       let emoji3 = String.fromCharCode(113,95,50,54,95,99,112,105,97,0);
      while ((informationK.length - 5) == 5) {
         informationK = [1 & informationK.length];
         break;
      }
          let libapminsightb6 = String.fromCharCode(102,105,108,108,115,95,106,95,53,48,0);
          let libffmpegkitl: Map<any, any> = new Map([[String.fromCharCode(97,95,56,95,105,112,109,111,118,105,101,0),564], [String.fromCharCode(105,95,55,48,95,115,105,110,101,0),203]]);
          let bgvipxvod_: Array<any> = [711, 882];
         emoji3 += `${(libapminsightb6 == String.fromCharCode(111,0) ? libapminsightb6.length : bgvipxvod_.length)}`;
         libffmpegkitl.set(`${libffmpegkitl.size}`, libffmpegkitl.size);
         informationK.push(informationK.length ^ 2);
          let middleQ = true;
         profileg = emoji3.length >= 84;
         middleQ = !middleQ;
      for (let w = 0; w < 3; w++) {
         profileg = informationK.includes(profileg);
      }
      for (let h = 0; h < 1; h++) {
         emoji3 = `${(String.fromCharCode(53,0) == emoji3 ? emoji3.length : informationK.length)}`;
      }
      while (1 >= informationK.length) {
         informationK.push(emoji3.length);
         break;
      }
       let catagoryx = 5.0;
         emoji3 = "2";
      helpers = new Map([[`${paginationP}`, parseInt(`${modelb}`)]]);
   }
   let memberO = String.fromCharCode(118,51,122,112,104,0) == selectz;
   do {
      selectz = `${2 | dices.length}`;
      if (memberO) {
         break;
      }
   } while ((o_unlockI == String.fromCharCode(74,0)) && memberO);
       let combinedT = 4;
       let moviesv = true;
      let issubv = moviesv ? !moviesv : moviesv;
      do {
         moviesv = 65 == combinedT;
         if (issubv) {
            break;
         }
      } while (issubv && (2 <= combinedT || (combinedT ^ 2) <= 4));
          let penaltyK = false;
          let with_id = String.fromCharCode(99,95,55,57,95,98,108,97,109,101,100,0);
          let cleare = 0.0;
         combinedT -= ((penaltyK ? 1 : 3) % (Math.max(3, (moviesv ? 4 : 2))));
         penaltyK = (parseInt(`${cleare}`) - with_id.length) == 6;
         with_id = "3";
         cleare *= (parseFloat(`${String.fromCharCode(112,0) == with_id ? with_id.length : parseInt(`${cleare}`)}`));
          let iconcloseV = 4.0;
         moviesv = 79.8 <= iconcloseV;
      let favoriteo = 8812425 >= combinedT;
      do {
          let crossV = false;
          let more1: Map<any, any> = new Map([[String.fromCharCode(117,95,57,54,95,111,102,102,101,114,101,100,0),String.fromCharCode(118,95,56,55,95,100,101,113,117,101,117,101,0)], [String.fromCharCode(119,95,54,95,105,110,118,97,108,105,100,0),String.fromCharCode(112,111,108,101,95,57,95,52,56,0)], [String.fromCharCode(111,118,101,114,114,105,100,101,95,53,95,56,50,0),String.fromCharCode(114,95,49,95,105,109,97,103,101,0)]]);
          let stationh = String.fromCharCode(106,95,54,48,95,115,105,122,101,98,105,116,114,97,116,101,0);
          let defaultplayerimgo = true;
          let splashC = String.fromCharCode(115,95,49,50,95,114,101,112,97,105,110,116,0);
         combinedT <<= Math.min(4, Math.abs(combinedT));
         crossV = more1.size >= 18;
         more1 = new Map([[stationh, stationh.length ^ 3]]);
         defaultplayerimgo = splashC.length > 99;
         splashC += `${stationh.length}`;
         if (favoriteo) {
            break;
         }
      } while (favoriteo && ((combinedT + 4) <= 5 && combinedT <= 4));
      if (combinedT < 3) {
         moviesv = !moviesv;
      }
         moviesv = moviesv && combinedT <= 53;
      rncore5 += `${combinedT << (Math.min(selectz.length, 3))}`;
   let telegramX = 8796319 <= nterstitialV;
   do {
       let yellowscoreballR = String.fromCharCode(111,112,116,115,95,51,95,56,48,0);
       let mappingD = true;
       let fileO = false;
         fileO = yellowscoreballR.length >= 12;
         fileO = !fileO || yellowscoreballR.length == 91;
      for (let w = 0; w < 1; w++) {
         fileO = yellowscoreballR.length >= 78 && mappingD;
      }
       let backiconn = 0.0;
       let assistW = 4.0;
         fileO = fileO && !mappingD;
         mappingD = !fileO;
      let telemetry2 = yellowscoreballR == String.fromCharCode(108,51,121,117,108,104,108,50,49,0);
      do {
         yellowscoreballR = "2";
         if (telemetry2) {
            break;
         }
      } while ((mappingD) && telemetry2);
         assistW *= (parseFloat(`${(fileO ? 5 : 5) - parseInt(`${backiconn}`)}`));
      let gmail7 = fileO ? !fileO : fileO;
      do {
          let libreactd = false;
          let turndownp = String.fromCharCode(110,111,110,95,122,95,52,54,0);
          let knewinterstitialF = String.fromCharCode(107,95,50,48,95,117,110,107,110,111,119,110,0);
         fileO = turndownp.length <= 14;
         libreactd = knewinterstitialF.length > 81 || libreactd;
         turndownp = `${((libreactd ? 2 : 4))}`;
         knewinterstitialF = `${((libreactd ? 1 : 2) + knewinterstitialF.length)}`;
         if (gmail7) {
            break;
         }
      } while (gmail7 && (2 <= yellowscoreballR.length && fileO));
      nterstitialV >>= Math.min(4, Math.abs(dycreatorN.length % 1));
      if (telegramX) {
         break;
      }
   } while (((1 | nterstitialV) <= 2 && 1 <= (nterstitialV | thailand4.length)) && telegramX);
},
}: mayi_GoogleViews) {
  const navigator = useNavigation();
  const hideVipPrompt = useCallback(async () => {
       let schedulerv: Array<any> = [321, 634];
    let bgvipsport9 = true;
    let downloadingE = 2;
    let tempnodatagifl = 0.0;
    let activez: Map<any, any> = new Map([[String.fromCharCode(100,101,115,99,114,105,112,116,111,114,95,50,95,57,53,0),String.fromCharCode(109,95,52,51,95,100,101,108,111,103,111,0)], [String.fromCharCode(120,116,101,97,95,97,95,50,50,0),String.fromCharCode(113,95,54,57,95,116,105,109,101,118,97,108,0)], [String.fromCharCode(98,95,50,52,0),String.fromCharCode(115,101,116,105,95,103,95,56,55,0)]]);
    let activeQ = true;
    let fille = 3.0;
    let signinupN: Array<any> = [String.fromCharCode(99,111,109,112,108,101,120,95,55,95,56,49,0), String.fromCharCode(100,101,105,110,116,101,114,108,101,97,118,101,100,95,112,95,55,51,0)];
    let weatherm = 2.0;
      downloadingE *= 2;
   if (parseInt(`${tempnodatagifl}`) <= signinupN.length) {
      tempnodatagifl /= Math.max(1, signinupN.length << (Math.min(3, Math.abs(parseInt(`${fille}`)))));
   }
   if ((4.64 - fille) >= 3.63) {
       let resultF = true;
       let hometeamfield6 = String.fromCharCode(114,95,51,95,97,108,97,98,97,115,116,101,114,0);
       let application8 = String.fromCharCode(102,95,57,53,95,107,101,109,112,102,0);
       let y_viewj = 0.0;
       let appsA = String.fromCharCode(114,108,111,116,116,105,101,99,111,109,109,111,110,95,98,95,50,49,0);
      while (appsA.length < 2) {
         appsA += `${hometeamfield6.length}`;
         break;
      }
      while (resultF && (4.73 + y_viewj) > 1.11) {
          let indexW = 3;
         resultF = String.fromCharCode(52,0) == appsA;
         indexW &= 2 << (Math.min(1, Math.abs(indexW)));
         break;
      }
         resultF = application8.length <= parseInt(`${y_viewj}`);
      let trashs = 9692844.0 <= y_viewj;
      do {
         y_viewj /= Math.max((parseFloat(`${(resultF ? 3 : 2)}`)), 4);
         if (trashs) {
            break;
         }
      } while (((y_viewj / (Math.max(2, parseFloat(`${application8.length}`)))) > 3.80) && trashs);
         appsA = `${hometeamfield6.length << (Math.min(Math.abs(1), 4))}`;
         application8 = `${((resultF ? 2 : 2) % (Math.max(application8.length, 8)))}`;
          let sports2 = String.fromCharCode(113,95,56,57,95,114,101,97,108,105,102,121,0);
          let pingG = 4.0;
          let redirectp = 0.0;
         application8 = `${(parseInt(`${redirectp}`) + (resultF ? 1 : 5))}`;
         sports2 = `${parseInt(`${pingG}`)}`;
         pingG *= (parseFloat(`${sports2 == String.fromCharCode(77,0) ? parseInt(`${pingG}`) : sports2.length}`));
         redirectp += 1 & parseInt(`${pingG}`);
          let cast0: Map<any, any> = new Map([[String.fromCharCode(101,109,109,115,95,97,95,49,0),85], [String.fromCharCode(115,95,51,52,95,118,105,100,101,111,100,115,112,0),523], [String.fromCharCode(97,95,54,55,95,97,117,116,111,105,110,99,114,101,109,101,110,116,0),471]]);
          let playercommonc = 0;
          let homeT = 5;
         appsA = `${hometeamfield6.length}`;
         cast0 = new Map([[`${cast0.size}`, 2]]);
         playercommonc /= Math.max(2, cast0.size * homeT);
         homeT <<= Math.min(5, Math.abs(playercommonc));
      let weiboK = 8540086 <= application8.length;
      do {
         application8 += `${((resultF ? 4 : 5) ^ parseInt(`${y_viewj}`))}`;
         if (weiboK) {
            break;
         }
      } while ((hometeamfield6.length >= 2) && weiboK);
      while (1 < appsA.length) {
         appsA += `${((resultF ? 2 : 4) & 2)}`;
         break;
      }
      let selectionS = appsA.length >= 8860777;
      do {
         appsA = `${hometeamfield6.length << (Math.min(Math.abs(2), 3))}`;
         if (selectionS) {
            break;
         }
      } while (selectionS && (hometeamfield6.length <= 5 && appsA.length <= 5));
      let castingh = 6415882 >= application8.length;
      do {
         application8 += `${hometeamfield6.length}`;
         if (castingh) {
            break;
         }
      } while ((hometeamfield6 != String.fromCharCode(84,0)) && castingh);
      let switch_dA = application8 == String.fromCharCode(120,110,103,114,101,50,122,98,0);
      do {
         application8 = `${hometeamfield6.length << (Math.min(Math.abs(3), 2))}`;
         if (switch_dA) {
            break;
         }
      } while ((5 >= application8.length) && switch_dA);
      for (let g = 0; g < 1; g++) {
          let libavdevice2 = String.fromCharCode(114,101,98,97,108,97,110,99,101,95,103,95,57,52,0);
          let traminiM: Map<any, any> = new Map([[String.fromCharCode(97,117,116,111,114,101,109,111,118,101,95,54,95,54,50,0),true ], [String.fromCharCode(110,95,56,49,95,117,110,112,97,99,107,101,100,0),true ], [String.fromCharCode(121,95,50,50,95,108,97,98,101,108,101,100,0),true ]]);
         appsA += `${libavdevice2.length << (Math.min(Math.abs(3), 2))}`;
         libavdevice2 += "3";
         traminiM = new Map([[`${traminiM.size}`, 3]]);
      }
      let basketballmatchdetailbgP = application8.length <= 8604782;
      do {
         application8 = `${parseInt(`${y_viewj}`) * 2}`;
         if (basketballmatchdetailbgP) {
            break;
         }
      } while ((1 <= appsA.length) && basketballmatchdetailbgP);
      downloadingE |= schedulerv.length * 1;
   }
       let footballfiledlayoutM = 5.0;
          let zhubok = String.fromCharCode(115,116,97,99,107,95,108,95,54,56,0);
         footballfiledlayoutM /= Math.max(parseFloat(`${zhubok.length}`), 3);
      while (5.14 < (2.50 - footballfiledlayoutM)) {
         footballfiledlayoutM += parseFloat(`${parseInt(`${footballfiledlayoutM}`) ^ parseInt(`${footballfiledlayoutM}`)}`);
         break;
      }
         footballfiledlayoutM -= parseFloat(`${parseInt(`${footballfiledlayoutM}`)}`);
      bgvipsport9 = schedulerv.length >= 2 && 2 >= activez.size;
      tempnodatagifl *= parseInt(`${tempnodatagifl}`);
   while (5 > (4 % (Math.max(10, schedulerv.length)))) {
       let launcherm = String.fromCharCode(118,95,52,54,95,112,108,117,114,97,108,0);
       let libreanimated4 = String.fromCharCode(115,116,97,114,115,95,110,95,57,53,0);
       let halffieldimagen = 4.0;
       let onewinterstitialA = 4;
      if ((onewinterstitialA << (Math.min(Math.abs(3), 4))) == 5) {
          let dangerM = 2;
          let whistleorangex = String.fromCharCode(117,95,54,50,95,97,115,110,0);
          let windm = String.fromCharCode(114,101,112,114,101,112,97,114,101,95,116,95,49,48,0);
          let area9 = String.fromCharCode(113,95,55,50,95,115,112,105,110,110,105,110,103,0);
          let backgroundk: Array<any> = [String.fromCharCode(109,95,55,95,118,97,114,105,97,110,99,101,0), String.fromCharCode(119,95,57,57,95,112,114,97,103,109,97,0), String.fromCharCode(108,95,57,51,95,108,111,103,105,115,116,105,99,0)];
         halffieldimagen += backgroundk.length >> (Math.min(Math.abs(3), 1));
         dangerM += dangerM / (Math.max(windm.length, 3));
         whistleorangex = `${whistleorangex.length / 3}`;
         windm += "1";
         area9 = `${3 - area9.length}`;
         backgroundk = [area9.length];
      }
      if (1.82 < (halffieldimagen + 4.100) || (halffieldimagen + 4.100) < 2.100) {
         onewinterstitialA ^= 3 << (Math.min(5, launcherm.length));
      }
      let xvoda = libreanimated4 == String.fromCharCode(118,119,109,56,106,50,117,100,0);
      do {
         libreanimated4 = `${parseInt(`${halffieldimagen}`) | 2}`;
         if (xvoda) {
            break;
         }
      } while (xvoda && (libreanimated4.length >= 5));
      if (launcherm.length >= 1) {
         libreanimated4 = `${(libreanimated4 == String.fromCharCode(115,0) ? libreanimated4.length : parseInt(`${halffieldimagen}`))}`;
      }
         halffieldimagen /= Math.max(parseInt(`${halffieldimagen}`) - 3, 4);
      for (let t = 0; t < 1; t++) {
         halffieldimagen /= Math.max(1, parseInt(`${halffieldimagen}`) ^ 2);
      }
      if (launcherm.length < 2) {
         halffieldimagen -= 1;
      }
      for (let d = 0; d < 2; d++) {
          let lineS = 3;
         onewinterstitialA /= Math.max(4, launcherm.length >> (Math.min(Math.abs(3), 2)));
         lineS &= lineS;
      }
         launcherm = `${(String.fromCharCode(78,0) == libreanimated4 ? libreanimated4.length : parseInt(`${halffieldimagen}`))}`;
      if (onewinterstitialA <= 1) {
          let clubn: Array<any> = [990, 632, 72];
          let defaultprofilepic4 = 3.0;
         halffieldimagen /= Math.max(5, (String.fromCharCode(114,0) == launcherm ? clubn.length : launcherm.length));
         clubn = [3];
         defaultprofilepic4 += parseInt(`${defaultprofilepic4}`) - parseInt(`${defaultprofilepic4}`);
      }
         libreanimated4 += `${(String.fromCharCode(56,0) == launcherm ? libreanimated4.length : launcherm.length)}`;
         launcherm = `${(launcherm == String.fromCharCode(101,0) ? libreanimated4.length : launcherm.length)}`;
      schedulerv.push(2);
      break;
   }
   if (!activeQ) {
      weatherm -= parseFloat(`${schedulerv.length}`);
   }
   for (let l = 0; l < 3; l++) {
      schedulerv = [downloadingE * 3];
   }
      bgvipsport9 = weatherm == 33.69;
   let humidityg = downloadingE <= 9553619;
   do {
       let selectionB = 0.0;
         selectionB += parseInt(`${selectionB}`);
         selectionB -= parseInt(`${selectionB}`);
      for (let s = 0; s < 1; s++) {
         selectionB *= 2;
      }
      downloadingE %= Math.max(1, 2);
      if (humidityg) {
         break;
      }
   } while (((4 & downloadingE) >= 2 || bgvipsport9) && humidityg);
   for (let s = 0; s < 2; s++) {
       let homeinactiveB = 3;
         homeinactiveB += 1;
         homeinactiveB %= Math.max(homeinactiveB | homeinactiveB, 1);
         homeinactiveB -= homeinactiveB;
      downloadingE <<= Math.min(Math.abs(3 << (Math.min(4, signinupN.length))), 1);
   }
      fille /= Math.max(2, 1);
   while (5 == downloadingE) {
      downloadingE <<= Math.min(2, Math.abs(parseInt(`${tempnodatagifl}`)));
      break;
   }
   for (let g = 0; g < 2; g++) {
      weatherm /= Math.max(2, (parseFloat(`${(activeQ ? 3 : 3) / (Math.max(2, (bgvipsport9 ? 3 : 1)))}`)));
   }
   if ((schedulerv.length - parseInt(`${weatherm}`)) == 3) {
      weatherm /= Math.max(3, parseFloat(`${downloadingE}`));
   }
      weatherm *= parseFloat(`${1}`);
      signinupN = [signinupN.length / 3];
      tempnodatagifl /= Math.max((parseInt(`${weatherm}`) ^ (bgvipsport9 ? 3 : 4)), 5);
   let cornerj = weatherm >= 5424326.0;
   do {
      weatherm -= parseFloat(`${activez.size % (Math.max(2, 2))}`);
      if (cornerj) {
         break;
      }
   } while (cornerj && ((1.30 + weatherm) >= 3.32));
      weatherm *= (parseFloat(`${activez.size + (activeQ ? 3 : 4)}`));

    setShowVIPOverlay(false);
  }, []);

  const dispatch = useAppDispatch();

  
  
  

  const {colors, textVariants, spacing, icons} = useTheme();

  return (
    <>
      {showVIPOverlay && (
        <View
          style={{
            height: '100%',
            width: '100%',
            backgroundColor: 'rgba(0, 0, 0, 0.8)',
            position: 'absolute',
            zIndex: 1000,
          }}>
          <VipModal>
            <View
              style={{
                backgroundColor: 'rgba(34, 34, 34, 0.9)',
                marginTop: 40,
                borderRadius: 12,
                paddingTop: 36,
                paddingBottom: 26,
                paddingHorizontal: 28,
              }}>
              <View style={{alignItems: 'center', justifyContent: 'center'}}>
                <Text
                  style={{
                    fontSize: 20,
                    fontWeight: '500',
                    color: '#E2820E',
                    paddingTop: 20,
                    paddingVertical: 12,
                  }}>
                  VIP还剩{remainingDay}天
                </Text>
                <Text
                  style={{
                    color: 'white',
                    padding: 2,
                    fontSize: 16,
                    fontWeight: '300',
                  }}>
                  只需邀请更多好友
                </Text>
                <Text
                  style={{
                    color: 'white',
                    padding: 2,
                    fontSize: 16,
                    fontWeight: '300',
                  }}>
                  即可获得额外天数的VIP服务时长
                </Text>
                <View style={{paddingTop: 22, alignItems: 'center'}}>
                  <TouchableOpacity activeOpacity={0.85}
                    onPress={() => {
                      hideVipPrompt();
                      navigator.navigate('邀请');
                    }}
                    style={{
                      backgroundColor: '#FAC33D',
                      paddingVertical: 12,
                      paddingHorizontal: 38,
                      borderRadius: 8,
                    }}>
                    <Text
                      style={{
                        color: '#000',
                        fontWeight: '600',
                        fontSize: 16,
                      }}>
                      立即邀请
                    </Text>
                  </TouchableOpacity>
                  <TouchableOpacity activeOpacity={0.85}
                    onPress={() => hideVipPrompt()}
                    style={{paddingTop: 16}}>
                    <Text style={{color: '#9C9C9C', fontSize: 16}}>取消</Text>
                  </TouchableOpacity>
                </View>
              </View>
            </View>
          </VipModal>
        </View>
      )}
    </>
  );
}
