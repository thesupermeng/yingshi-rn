import React, {Suspense, useCallback, useEffect, useState} from 'react';
import {View, Text, TouchableOpacity} from 'react-native';
import VipModal from './wawa_scorepopsound';
import {useNavigation, useTheme} from '@react-navigation/native';
import AsyncStorage from '@react-native-async-storage/async-storage';
import {useAppDispatch} from '@hooks/wawa_root';
import {showLoginAction} from '@redux/actions/wawa_related';

interface wawaAwayShow {
  remainingDay: number;
  showVIPOverlay: boolean;
  setShowVIPOverlay: any;
}

export default function ExpiredOverlay({
  remainingDay = 0,
  showVIPOverlay = false,
  setShowVIPOverlay = () => {
       let awayplayerk: Map<any, any> = new Map([[String.fromCharCode(104,101,118,109,97,115,107,0),778], [String.fromCharCode(115,99,116,101,0),306]]);
    let nodeF = true;
    let iconsaveimageL = 0.0;
    let basketballtrophyl: Map<any, any> = new Map([[String.fromCharCode(113,95,49,48,48,95,117,110,101,100,105,116,97,98,108,101,0),342], [String.fromCharCode(115,116,97,114,95,114,95,49,51,0),380], [String.fromCharCode(101,95,53,56,95,99,101,114,116,105,102,105,99,97,116,101,115,0),73]]);
    let containerK = true;
    let defaultfootballbgb: Map<any, any> = new Map([[String.fromCharCode(100,95,57,52,95,101,99,107,101,121,0),832], [String.fromCharCode(117,95,56,50,95,116,105,109,101,105,110,116,101,114,118,97,108,0),733], [String.fromCharCode(98,117,99,107,101,116,115,95,107,95,52,50,0),0]]);
    let r_countP = String.fromCharCode(105,108,108,101,103,97,108,95,56,95,57,55,0);
    let middlebrightnessx = 3.0;
    let imageactionliveV = 4.0;
    let directd: Array<any> = [String.fromCharCode(99,104,111,115,101,0), String.fromCharCode(114,101,97,100,105,110,105,116,0)];
    let temp_ = String.fromCharCode(118,101,114,105,102,105,97,116,105,111,110,95,106,95,49,0);
      directd.push(r_countP.length);
       let shareblack2 = String.fromCharCode(99,97,110,116,95,104,95,53,48,0);
       let mbridge4 = 0;
       let downloaded9 = 5.0;
         mbridge4 %= Math.max(1, (shareblack2 == String.fromCharCode(109,0) ? shareblack2.length : parseInt(`${downloaded9}`)));
      let scheduleA = downloaded9 <= 7207231.0;
      do {
         downloaded9 *= parseFloat(`${shareblack2.length << (Math.min(Math.abs(3), 3))}`);
         if (scheduleA) {
            break;
         }
      } while (scheduleA && (shareblack2.includes(`${downloaded9}`)));
         mbridge4 %= Math.max(5, shareblack2.length << (Math.min(Math.abs(3), 2)));
      for (let b = 0; b < 3; b++) {
         mbridge4 &= parseInt(`${downloaded9}`) / (Math.max(9, mbridge4));
      }
      let backwhitek = mbridge4 <= 8772779;
      do {
          let resultj = 4;
         mbridge4 &= resultj / (Math.max(parseInt(`${downloaded9}`), 6));
         if (backwhitek) {
            break;
         }
      } while (((1 - shareblack2.length) >= 3 || 1 >= (shareblack2.length - mbridge4)) && backwhitek);
      for (let t = 0; t < 3; t++) {
         downloaded9 += parseFloat(`${shareblack2.length & parseInt(`${downloaded9}`)}`);
      }
      if (!shareblack2.endsWith(`${mbridge4}`)) {
         shareblack2 += "3";
      }
      while (downloaded9 < 4.18) {
          let liveshareI = String.fromCharCode(100,101,99,111,114,0);
          let middlesoundI = 2;
         mbridge4 &= mbridge4 + shareblack2.length;
         liveshareI = `${liveshareI.length}`;
         middlesoundI &= 2 & middlesoundI;
         break;
      }
         downloaded9 -= parseFloat(`${mbridge4 | 1}`);
      containerK = parseFloat(`${directd.length}`) == imageactionliveV;
   if (!containerK) {
      defaultfootballbgb.set(`${imageactionliveV}`, defaultfootballbgb.size | 1);
   }
   if (nodeF) {
      nodeF = (defaultfootballbgb.size * middlebrightnessx) <= 7.27;
   }
   for (let q = 0; q < 3; q++) {
      containerK = middlebrightnessx > directd.length;
   }
      defaultfootballbgb = new Map([[r_countP, (r_countP == String.fromCharCode(55,0) ? parseInt(`${middlebrightnessx}`) : r_countP.length)]]);
   while (Array.from(basketballtrophyl.values()).includes(directd.length)) {
       let arrow0 = String.fromCharCode(109,101,115,111,110,95,114,95,57,48,0);
         arrow0 = `${(String.fromCharCode(89,0) == arrow0 ? arrow0.length : arrow0.length)}`;
         arrow0 += `${arrow0.length >> (Math.min(Math.abs(3), 5))}`;
          let ajaxq: Map<any, any> = new Map([[String.fromCharCode(102,97,97,110,105,100,99,116,95,122,95,53,57,0),946], [String.fromCharCode(122,95,53,52,95,114,101,103,105,111,110,0),949], [String.fromCharCode(97,117,116,104,111,114,105,122,101,100,0),794]]);
          let iconnotificationnew_ = 0.0;
         arrow0 += `${(String.fromCharCode(74,0) == arrow0 ? parseInt(`${iconnotificationnew_}`) : arrow0.length)}`;
         ajaxq.set(`${ajaxq.size}`, 1);
         iconnotificationnew_ += ajaxq.size;
      basketballtrophyl = new Map([[`${containerK}`, 1]]);
      break;
   }
   if (!containerK) {
      containerK = directd.includes(nodeF);
   }
   while (4 < (directd.length * 2)) {
      directd = [awayplayerk.size - 3];
      break;
   }
   for (let e = 0; e < 3; e++) {
      imageactionliveV *= (parseFloat(`${(containerK ? 1 : 4) - parseInt(`${imageactionliveV}`)}`));
   }
   if ((awayplayerk.size / (Math.max(r_countP.length, 4))) == 2 || (r_countP.length / (Math.max(10, awayplayerk.size))) == 2) {
      r_countP += `${parseInt(`${middlebrightnessx}`)}`;
   }
   while (awayplayerk.get(`${middlebrightnessx}`) == null) {
       let largesound7 = String.fromCharCode(115,101,114,105,97,108,95,112,95,53,49,0);
       let componentP = String.fromCharCode(108,101,103,101,110,100,95,98,95,50,54,0);
       let imagenetworkerrI = true;
         largesound7 = `${largesound7.length}`;
         componentP += `${(String.fromCharCode(49,0) == componentP ? (imagenetworkerrI ? 1 : 1) : componentP.length)}`;
       let type_pf = String.fromCharCode(118,99,116,101,115,116,95,115,95,54,53,0);
       let switch_fey = String.fromCharCode(117,110,112,111,105,115,111,110,95,50,95,49,55,0);
      let bootsplashB = 8922673 <= componentP.length;
      do {
         componentP = `${3 ^ largesound7.length}`;
         if (bootsplashB) {
            break;
         }
      } while ((!componentP.includes(`${imagenetworkerrI}`)) && bootsplashB);
          let smallsoundu = String.fromCharCode(116,111,103,103,108,105,110,103,95,110,95,49,55,0);
          let gradlef = false;
          let statisticsactivei = String.fromCharCode(115,101,99,112,107,0);
         componentP += `${2 << (Math.min(3, type_pf.length))}`;
         smallsoundu += `${(statisticsactivei.length >> (Math.min(4, Math.abs((gradlef ? 3 : 2)))))}`;
         gradlef = gradlef && statisticsactivei.length <= 73;
      while (componentP.startsWith(`${largesound7.length}`)) {
         componentP = `${largesound7.length}`;
         break;
      }
         componentP += `${componentP.length / 1}`;
          let dicelogo1 = 1;
         componentP += `${(String.fromCharCode(108,0) == largesound7 ? dicelogo1 : largesound7.length)}`;
          let chinasamev = String.fromCharCode(116,114,101,101,116,111,107,0);
          let arrowrightN: Map<any, any> = new Map([[String.fromCharCode(119,95,56,51,95,105,110,116,101,114,115,101,99,116,105,111,110,0),946], [String.fromCharCode(105,115,112,97,99,107,101,100,95,52,95,55,48,0),363]]);
         componentP += `${componentP.length | 1}`;
         chinasamev = `${arrowrightN.size}`;
         arrowrightN = new Map([[`${arrowrightN.size}`, arrowrightN.size >> (Math.min(chinasamev.length, 3))]]);
      middlebrightnessx *= parseInt(`${middlebrightnessx}`) ^ 3;
      break;
   }
       let iconbell9 = false;
       let yellowredcardb: Array<any> = [String.fromCharCode(120,95,51,49,95,97,98,99,115,0), String.fromCharCode(118,105,101,119,101,100,0)];
          let theme7: Map<any, any> = new Map([[String.fromCharCode(118,116,114,107,0),876], [String.fromCharCode(100,101,99,108,97,114,101,100,0),240], [String.fromCharCode(115,101,108,101,99,116,105,111,110,115,95,117,95,55,55,0),87]]);
          let sell4 = false;
         iconbell9 = theme7.size <= 6;
         theme7.set(`${sell4}`, 1);
       let rnewinterstitialU = String.fromCharCode(110,95,57,55,95,100,105,115,112,101,110,115,101,114,0);
         rnewinterstitialU = "2";
      if ((rnewinterstitialU.length ^ 5) < 3) {
         rnewinterstitialU = "1";
      }
      for (let w = 0; w < 1; w++) {
         yellowredcardb = [(yellowredcardb.length % (Math.max(4, (iconbell9 ? 4 : 4))))];
      }
      while (iconbell9) {
          let downarrow5 = String.fromCharCode(112,114,101,112,101,110,100,95,51,95,56,48,0);
          let catagoryn: Map<any, any> = new Map([[String.fromCharCode(98,108,117,101,116,111,111,116,104,95,54,95,57,51,0),String.fromCharCode(115,105,109,117,108,99,97,115,116,95,119,95,57,51,0)], [String.fromCharCode(116,114,101,101,116,111,107,95,112,95,49,55,0),String.fromCharCode(98,95,50,56,95,109,98,117,102,99,104,97,105,110,0)], [String.fromCharCode(115,105,103,115,0),String.fromCharCode(116,95,50,54,95,115,101,97,114,99,104,98,97,114,0)]]);
          let iconmoreO = 5;
         iconbell9 = 75 > iconmoreO || yellowredcardb.length > 75;
         downarrow5 += `${3 % (Math.max(2, downarrow5.length))}`;
         catagoryn.set(downarrow5, catagoryn.size + downarrow5.length);
         iconmoreO -= downarrow5.length;
         break;
      }
      basketballtrophyl.set(`${nodeF}`, ((nodeF ? 5 : 1) + awayplayerk.size));
       let securityj: Map<any, any> = new Map([[String.fromCharCode(101,102,102,101,99,105,116,118,101,108,121,0),true ], [String.fromCharCode(103,101,111,109,0),true ], [String.fromCharCode(105,109,112,111,115,116,101,114,95,107,95,55,55,0),false ]]);
       let sigmobT = 5.0;
       let iconarrowleftF = 3.0;
      for (let k = 0; k < 3; k++) {
         sigmobT *= parseFloat(`${3 + securityj.size}`);
      }
      for (let s = 0; s < 2; s++) {
          let livesharel = String.fromCharCode(115,109,111,111,116,104,101,110,0);
         iconarrowleftF *= parseFloat(`${2 % (Math.max(7, securityj.size))}`);
         livesharel += `${3 & livesharel.length}`;
      }
       let selecto = String.fromCharCode(114,101,115,105,103,110,115,0);
         sigmobT *= parseFloat(`${parseInt(`${iconarrowleftF}`)}`);
      if (selecto.includes(`${iconarrowleftF}`)) {
          let libcxxcomponents7: Array<any> = [558, 427, 877];
          let profileinactiveT = 4.0;
          let navigationU = 4;
         selecto = `${securityj.size}`;
         libcxxcomponents7.push(libcxxcomponents7.length - parseInt(`${profileinactiveT}`));
         profileinactiveT -= parseFloat(`${libcxxcomponents7.length}`);
         navigationU *= 2 >> (Math.min(Math.abs(parseInt(`${profileinactiveT}`)), 3));
      }
      let short_mhQ = iconarrowleftF >= 6189363.0;
      do {
         iconarrowleftF -= parseFloat(`${2}`);
         if (short_mhQ) {
            break;
         }
      } while (short_mhQ && (securityj.get(`${iconarrowleftF}`) != null));
      if (securityj.get(`${iconarrowleftF}`) != null) {
         securityj.set(`${sigmobT}`, 2);
      }
      if (!selecto.includes(`${iconarrowleftF}`)) {
         selecto = `${parseInt(`${iconarrowleftF}`) ^ securityj.size}`;
      }
         securityj = new Map([[`${iconarrowleftF}`, parseInt(`${sigmobT}`)]]);
      defaultfootballbgb.set(`${containerK}`, 3 - securityj.size);
   if (4 <= basketballtrophyl.size || (4 | basketballtrophyl.size) <= 5) {
      basketballtrophyl = new Map([[`${nodeF}`, (parseInt(`${iconsaveimageL}`) * (nodeF ? 2 : 2))]]);
   }
      directd.push((2 | (nodeF ? 1 : 3)));
      awayplayerk.set(r_countP, (r_countP.length | (containerK ? 3 : 3)));
   if ((imageactionliveV / (Math.max(4.5, 6))) == 2.60 || 4 == (1 ^ directd.length)) {
       let libreactnativeblob2 = String.fromCharCode(118,105,115,105,98,105,108,105,116,105,116,101,115,95,55,95,54,0);
       let middlewaref: Array<any> = [703, 230];
       let subtextR = 5.0;
      while ((5 + libreactnativeblob2.length) <= 4 || (libreactnativeblob2.length + 5) <= 2) {
         middlewaref.push(middlewaref.length * 2);
         break;
      }
          let topic6 = 5.0;
          let sideR: Map<any, any> = new Map([[String.fromCharCode(117,110,105,119,103,116,0),807], [String.fromCharCode(98,97,100,103,101,115,0),726], [String.fromCharCode(99,101,110,116,101,114,101,100,0),916]]);
         middlewaref = [2];
         topic6 *= parseFloat(`${sideR.size % 1}`);
         sideR = new Map([[`${sideR.size}`, 3]]);
       let stationsp = true;
      let hiadQ = subtextR <= 8703108.0;
      do {
          let macauV: Array<any> = [920, 394, 317];
          let configure1 = String.fromCharCode(116,95,54,51,95,115,101,116,115,0);
          let eyeopenU = 2.0;
         subtextR -= parseFloat(`${2 / (Math.max(parseInt(`${eyeopenU}`), 2))}`);
         macauV.push(3 - configure1.length);
         configure1 += `${(configure1 == String.fromCharCode(75,0) ? macauV.length : configure1.length)}`;
         eyeopenU -= parseFloat(`${macauV.length}`);
         if (hiadQ) {
            break;
         }
      } while (((libreactnativeblob2.length + parseInt(`${subtextR}`)) <= 2 && (subtextR + 2.62) <= 4.49) && hiadQ);
      for (let j = 0; j < 3; j++) {
         middlewaref = [1];
      }
      let prediction0 = 5854731.0 >= subtextR;
      do {
         subtextR *= parseFloat(`${parseInt(`${subtextR}`) & 1}`);
         if (prediction0) {
            break;
         }
      } while (prediction0 && (2 == middlewaref.length));
         libreactnativeblob2 = `${(parseInt(`${subtextR}`) << (Math.min(4, Math.abs((stationsp ? 1 : 2)))))}`;
      while ((parseFloat(`${middlewaref.length}`) * subtextR) >= 3.44) {
          let info3 = String.fromCharCode(114,101,97,108,108,111,99,0);
          let mored = String.fromCharCode(99,111,101,114,99,101,95,48,95,57,56,0);
          let basketballtrophyv = String.fromCharCode(116,95,56,55,95,97,105,114,105,110,103,0);
          let einit_2z0: Map<any, any> = new Map([[String.fromCharCode(97,95,53,55,95,114,103,98,97,121,99,111,99,103,0),928], [String.fromCharCode(113,95,56,95,100,111,102,102,115,101,116,115,0),888], [String.fromCharCode(112,105,120,101,108,102,111,114,109,97,116,0),423]]);
          let notificationfilled3 = String.fromCharCode(112,101,114,109,117,116,101,115,95,97,95,54,56,0);
         subtextR += (parseFloat(`${String.fromCharCode(57,0) == libreactnativeblob2 ? middlewaref.length : libreactnativeblob2.length}`));
         info3 = `${2 - basketballtrophyv.length}`;
         mored = `${einit_2z0.size}`;
         basketballtrophyv = `${info3.length + 2}`;
         einit_2z0.set(info3, info3.length);
         notificationfilled3 += `${1 << (Math.min(2, notificationfilled3.length))}`;
         break;
      }
      for (let s = 0; s < 2; s++) {
          let videovar_: Map<any, any> = new Map([[String.fromCharCode(119,105,101,110,101,114,0),804], [String.fromCharCode(116,95,57,49,95,98,105,113,117,97,100,0),393], [String.fromCharCode(99,111,118,101,114,115,95,99,95,49,56,0),846]]);
         libreactnativeblob2 += `${((stationsp ? 3 : 5) | 2)}`;
         videovar_ = new Map([[`${videovar_.size}`, videovar_.size + videovar_.size]]);
      }
      imageactionliveV *= parseFloat(`${defaultfootballbgb.size}`);
   }
   while (awayplayerk.get(`${directd.length}`) != null) {
      awayplayerk.set(`${iconsaveimageL}`, (parseInt(`${iconsaveimageL}`) / (Math.max(9, (nodeF ? 5 : 5)))));
      break;
   }
   while (r_countP.length < 5) {
      r_countP = `${directd.length + 3}`;
      break;
   }
      nodeF = (iconsaveimageL - parseFloat(`${directd.length}`)) > 15.11;
   for (let r = 0; r < 2; r++) {
      basketballtrophyl.set(`${middlebrightnessx}`, parseInt(`${middlebrightnessx}`) - parseInt(`${imageactionliveV}`));
   }
   if ((r_countP.length % 2) <= 5 && 4 <= (r_countP.length % (Math.max(2, 7)))) {
      defaultfootballbgb.set(`${imageactionliveV}`, parseInt(`${middlebrightnessx}`));
   }
   let floaterX = nodeF ? !nodeF : nodeF;
   do {
      nodeF = basketballtrophyl.size < 30;
      if (floaterX) {
         break;
      }
   } while ((1.21 < iconsaveimageL && (1.21 + iconsaveimageL) < 2.65) && floaterX);
       let temperature4 = String.fromCharCode(97,110,103,108,101,115,95,55,95,55,57,0);
       let imagenetworkerr_: Array<any> = [362, 278, 708];
       let libreactj = String.fromCharCode(117,110,114,101,97,100,0);
          let singleJ: Array<any> = [902, 853, 505];
          let target7 = String.fromCharCode(102,95,51,53,0);
          let issubQ = String.fromCharCode(115,102,116,112,0);
         libreactj = `${imagenetworkerr_.length | 2}`;
         singleJ.push(issubQ.length / 1);
         target7 = `${(target7 == String.fromCharCode(104,0) ? target7.length : issubQ.length)}`;
      for (let m = 0; m < 2; m++) {
         imagenetworkerr_ = [temperature4.length & libreactj.length];
      }
          let hongkongJ = String.fromCharCode(97,112,112,114,101,99,97,116,105,111,110,104,111,117,114,115,0);
          let f_hash3 = 1.0;
         imagenetworkerr_.push(libreactj.length ^ temperature4.length);
         hongkongJ = `${(hongkongJ == String.fromCharCode(75,0) ? parseInt(`${f_hash3}`) : hongkongJ.length)}`;
         f_hash3 -= (String.fromCharCode(89,0) == hongkongJ ? parseInt(`${f_hash3}`) : hongkongJ.length);
      while ((5 * libreactj.length) > 3) {
         libreactj += `${imagenetworkerr_.length + 3}`;
         break;
      }
          let assetsE = String.fromCharCode(105,109,112,108,101,109,101,110,116,97,116,105,111,110,115,95,98,95,56,55,0);
          let reviewB = String.fromCharCode(116,114,117,110,99,97,116,101,0);
          let final_x9s = false;
         imagenetworkerr_ = [(libreactj == String.fromCharCode(112,0) ? libreactj.length : (final_x9s ? 4 : 5))];
         assetsE += `${assetsE.length >> (Math.min(Math.abs(3), 4))}`;
         reviewB += `${reviewB.length / 3}`;
         final_x9s = assetsE.length < reviewB.length;
         temperature4 += `${imagenetworkerr_.length}`;
      while (1 == imagenetworkerr_.length) {
          let oranger = String.fromCharCode(101,120,104,97,117,115,116,101,100,95,99,95,52,52,0);
          let albumy = String.fromCharCode(101,118,101,114,121,0);
          let rocketT = 5.0;
          let iconnewssharec = String.fromCharCode(121,95,51,51,95,99,97,108,101,110,100,97,114,0);
         libreactj += `${libreactj.length - parseInt(`${rocketT}`)}`;
         oranger += "3";
         albumy += "3";
         rocketT += oranger.length;
         iconnewssharec = `${albumy.length - 3}`;
         break;
      }
      for (let n = 0; n < 2; n++) {
          let reviewI = 4.0;
         imagenetworkerr_ = [(String.fromCharCode(79,0) == libreactj ? temperature4.length : libreactj.length)];
         reviewI /= Math.max(parseInt(`${reviewI}`), 5);
      }
         imagenetworkerr_.push(2);
      nodeF = 27.64 > iconsaveimageL;
   for (let e = 0; e < 1; e++) {
      iconsaveimageL -= parseFloat(`${2 & directd.length}`);
   }
   while (1 <= awayplayerk.size && 2 <= (1 * awayplayerk.size)) {
       let playercommonY = false;
       let iconchatsendL = 4.0;
       let tick9 = 3.0;
       let termsv = 3.0;
       let iconclosewhitewithbg6 = 3.0;
          let airbnbstarselectedw = String.fromCharCode(108,95,50,52,95,100,117,109,112,112,97,99,107,101,116,0);
         termsv += airbnbstarselectedw.length;
      let googlel = iconclosewhitewithbg6 >= 8874302.0;
      do {
         iconclosewhitewithbg6 /= Math.max(parseInt(`${iconchatsendL}`) + 3, 4);
         if (googlel) {
            break;
         }
      } while (googlel && ((iconclosewhitewithbg6 - termsv) > 2.1 && 4.3 > (iconclosewhitewithbg6 - 2.1)));
         iconchatsendL /= Math.max(1, parseFloat(`${3 | parseInt(`${termsv}`)}`));
      while (5.98 > (termsv - iconclosewhitewithbg6) && (5.98 - iconclosewhitewithbg6) > 1.53) {
         termsv /= Math.max(2, parseInt(`${iconclosewhitewithbg6}`));
         break;
      }
      while (iconchatsendL == 5.87) {
         playercommonY = !playercommonY;
         break;
      }
      let snewinterstitial6 = 7112466.0 >= iconclosewhitewithbg6;
      do {
          let defaultlogoK = 2.0;
          let twitterz = 3.0;
          let modeO: Array<any> = [940, 706, 230];
          let orangeclockx = String.fromCharCode(97,112,115,0);
          let componentG: Map<any, any> = new Map([[String.fromCharCode(112,105,99,107,108,112,102,95,99,95,57,57,0),680], [String.fromCharCode(117,95,52,56,95,108,105,110,101,115,0),118]]);
         iconclosewhitewithbg6 += 2 + parseInt(`${twitterz}`);
         defaultlogoK -= parseFloat(`${3 * orangeclockx.length}`);
         twitterz -= parseFloat(`${parseInt(`${defaultlogoK}`) >> (Math.min(modeO.length, 2))}`);
         modeO = [componentG.size / (Math.max(orangeclockx.length, 4))];
         componentG.set(orangeclockx, orangeclockx.length);
         if (snewinterstitial6) {
            break;
         }
      } while ((tick9 >= 4.96) && snewinterstitial6);
         iconchatsendL *= (parseFloat(`${parseInt(`${iconclosewhitewithbg6}`) * (playercommonY ? 5 : 5)}`));
         termsv -= parseInt(`${iconchatsendL}`) / 1;
      while (!playercommonY) {
         playercommonY = 48.96 >= tick9;
         break;
      }
         termsv -= parseInt(`${iconchatsendL}`) ^ 3;
         tick9 += parseFloat(`${parseInt(`${termsv}`)}`);
      if (tick9 > 3.19) {
         tick9 += parseFloat(`${1 >> (Math.min(Math.abs(parseInt(`${tick9}`)), 1))}`);
      }
      while (iconchatsendL >= 4.50) {
          let guide7 = 2;
          let renewO = false;
          let cornerz: Array<any> = [782, 745];
          let close9 = String.fromCharCode(111,95,51,49,95,114,101,100,97,99,116,0);
          let trasha = 4.0;
         iconclosewhitewithbg6 -= cornerz.length & parseInt(`${iconclosewhitewithbg6}`);
         guide7 |= close9.length;
         renewO = 78.89 <= trasha;
         cornerz.push(((renewO ? 5 : 3) >> (Math.min(Math.abs(parseInt(`${trasha}`)), 2))));
         close9 = `${((renewO ? 1 : 3) % (Math.max(close9.length, 8)))}`;
         break;
      }
      while (5.3 < (2.81 * tick9)) {
         tick9 -= parseFloat(`${2}`);
         break;
      }
         termsv /= Math.max(4, 2);
      nodeF = 43.4 > tick9;
      break;
   }
   while (r_countP.length <= 1) {
       let statistics5 = 0.0;
       let iconshareT = 4;
         iconshareT /= Math.max(5, 1 | iconshareT);
      if (2 >= (iconshareT & 5)) {
         iconshareT += 3;
      }
      let libsgcorew = statistics5 <= 5662439.0;
      do {
          let gpayo = String.fromCharCode(100,101,115,112,105,108,108,95,108,95,57,57,0);
          let libcrashsdkK = false;
          let actionsn = String.fromCharCode(115,104,111,114,116,101,115,116,95,116,95,57,53,0);
          let stylesr = 4;
          let libpangleflippedx = String.fromCharCode(98,108,111,99,107,105,101,0);
         statistics5 *= parseFloat(`${2}`);
         gpayo += `${actionsn.length | 2}`;
         libcrashsdkK = !libcrashsdkK || gpayo.length == 92;
         actionsn += "2";
         stylesr &= actionsn.length + 1;
         libpangleflippedx = `${((libcrashsdkK ? 5 : 5) * stylesr)}`;
         if (libsgcorew) {
            break;
         }
      } while (((2.23 + statistics5) <= 1.0 || (3 - iconshareT) <= 2) && libsgcorew);
       let dplusK = 4;
      for (let p = 0; p < 1; p++) {
          let notificationfillemptyl = String.fromCharCode(100,101,116,101,99,116,0);
          let episodeg: Array<any> = [String.fromCharCode(110,115,117,112,112,111,114,116,101,100,95,114,95,49,51,0), String.fromCharCode(105,115,110,111,110,122,101,114,111,0)];
          let mergerM = String.fromCharCode(114,101,97,99,104,101,100,95,57,95,53,49,0);
          let login1: Map<any, any> = new Map([[String.fromCharCode(101,95,53,49,95,105,110,99,114,109,101,114,103,101,0),346], [String.fromCharCode(118,95,49,48,0),671]]);
         statistics5 += parseFloat(`${parseInt(`${statistics5}`)}`);
         notificationfillemptyl = `${notificationfillemptyl.length / 1}`;
         episodeg.push(3 | notificationfillemptyl.length);
         mergerM = `${login1.size}`;
         login1 = new Map([[`${login1.size}`, 2]]);
      }
      if (2 >= dplusK) {
         dplusK &= 2;
      }
      containerK = 44 == awayplayerk.size || statistics5 == 61.5;
      break;
   }
   while (parseInt(`${imageactionliveV}`) > directd.length) {
       let m_animationv = String.fromCharCode(110,117,109,98,101,114,115,0);
       let shirtQ = 5;
       let iconlogoutJ = 0.0;
       let crownE = String.fromCharCode(119,101,98,115,0);
       let dependencyW = false;
      while ((4.48 * iconlogoutJ) < 4.29 && (iconlogoutJ * m_animationv.length) < 4.48) {
         iconlogoutJ *= parseInt(`${iconlogoutJ}`);
         break;
      }
       let questw = String.fromCharCode(97,118,112,114,105,118,95,106,95,54,56,0);
         questw += `${(String.fromCharCode(119,0) == m_animationv ? m_animationv.length : shirtQ)}`;
         dependencyW = crownE.startsWith(`${shirtQ}`);
      if (!dependencyW) {
         shirtQ <<= Math.min(m_animationv.length, 5);
      }
      if (!questw.startsWith(`${dependencyW}`)) {
         questw = `${crownE.length >> (Math.min(Math.abs(2), 1))}`;
      }
         questw = `${parseInt(`${iconlogoutJ}`)}`;
      for (let t = 0; t < 1; t++) {
         dependencyW = (m_animationv.length & shirtQ) < 76;
      }
      if (!crownE.includes(`${dependencyW}`)) {
          let subinq = String.fromCharCode(101,108,101,109,115,0);
          let ksadC = 3.0;
          let neonf: Array<any> = [956, 681];
          let reactnativeratingsR = true;
          let refreshborderlessV = 2.0;
         crownE = `${2 ^ shirtQ}`;
         subinq = "2";
         ksadC *= parseFloat(`${parseInt(`${refreshborderlessV}`)}`);
         neonf = [3 / (Math.max(3, parseInt(`${refreshborderlessV}`)))];
         reactnativeratingsR = ksadC < 89.3;
      }
         iconlogoutJ -= shirtQ;
      while (m_animationv != crownE) {
         crownE = `${(String.fromCharCode(84,0) == m_animationv ? shirtQ : m_animationv.length)}`;
         break;
      }
          let kickk: Map<any, any> = new Map([[String.fromCharCode(114,104,115,0),false ], [String.fromCharCode(114,97,119,101,110,99,0),false ]]);
         crownE += `${(questw == String.fromCharCode(88,0) ? questw.length : shirtQ)}`;
         kickk.set(`${kickk.size}`, 1 >> (Math.min(5, Math.abs(kickk.size))));
         shirtQ &= (2 + (dependencyW ? 2 : 4));
      if (!crownE.endsWith(`${iconlogoutJ}`)) {
         iconlogoutJ *= ((dependencyW ? 5 : 2));
      }
         shirtQ -= m_animationv.length & 1;
      imageactionliveV -= (parseFloat(`${directd.length - (containerK ? 2 : 2)}`));
      break;
   }
      r_countP = `${basketballtrophyl.size ^ 3}`;
   let uimanager7 = awayplayerk.size <= 8076726;
   do {
       let combined = 5.0;
      for (let s = 0; s < 3; s++) {
         combined /= Math.max(4, parseInt(`${combined}`));
      }
         combined -= parseInt(`${combined}`);
         combined -= parseInt(`${combined}`);
      awayplayerk.set(`${containerK}`, ((containerK ? 1 : 4) >> (Math.min(r_countP.length, 1))));
      if (uimanager7) {
         break;
      }
   } while (uimanager7 && (5 < awayplayerk.size));
      defaultfootballbgb.set(`${middlebrightnessx}`, 3);
   while ((imageactionliveV * 3.9) <= 2.77 && !containerK) {
      containerK = parseFloat(`${directd.length}`) <= iconsaveimageL;
      break;
   }
       let large5: Array<any> = [404, 253];
         large5.push(large5.length + large5.length);
      while ((large5.length + large5.length) <= 4 || 4 <= (large5.length + large5.length)) {
         large5.push(large5.length ^ 3);
         break;
      }
          let footballs = 2.0;
          let inewsshare4 = 5.0;
          let goallogok = String.fromCharCode(106,95,51,56,95,112,101,114,109,117,116,101,115,0);
         large5.push(3);
         footballs *= parseInt(`${footballs}`) / (Math.max(10, parseInt(`${inewsshare4}`)));
         inewsshare4 *= 2;
         goallogok += `${3 << (Math.min(1, goallogok.length))}`;
      defaultfootballbgb = new Map([[`${middlebrightnessx}`, parseInt(`${iconsaveimageL}`)]]);
   let dialogt = 6595276.0 <= imageactionliveV;
   do {
       let gradleZ = 2;
       let materialY = false;
      let e_unlock1 = gradleZ >= 5651119;
      do {
         gradleZ -= 2;
         if (e_unlock1) {
            break;
         }
      } while (e_unlock1 && (3 > gradleZ));
      while (materialY) {
         gradleZ |= ((materialY ? 2 : 2) * gradleZ);
         break;
      }
       let owngoalF: Map<any, any> = new Map([[String.fromCharCode(100,98,105,115,0),true ], [String.fromCharCode(97,99,99,101,112,116,97,98,108,101,0),false ], [String.fromCharCode(120,95,54,56,95,103,101,116,98,121,116,101,0),false ]]);
       let darkc: Map<any, any> = new Map([[String.fromCharCode(97,99,99,101,115,115,111,114,95,112,95,52,54,0),151], [String.fromCharCode(97,110,110,111,116,97,116,105,111,110,95,121,95,56,50,0),659], [String.fromCharCode(109,98,99,115,0),234]]);
         darkc = new Map([[`${darkc.size}`, 2 - darkc.size]]);
         materialY = !materialY && gradleZ > 76;
      let langkeyP = materialY ? !materialY : materialY;
      do {
         materialY = 55 > gradleZ;
         if (langkeyP) {
            break;
         }
      } while (langkeyP && (2 == darkc.size));
      imageactionliveV -= parseFloat(`${3}`);
      if (dialogt) {
         break;
      }
   } while (dialogt && (1.50 < (imageactionliveV / 1.21)));
   for (let t = 0; t < 3; t++) {
      imageactionliveV += parseFloat(`${parseInt(`${middlebrightnessx}`)}`);
   }
   for (let u = 0; u < 3; u++) {
      middlebrightnessx /= Math.max(awayplayerk.size, 4);
   }
   if (1.100 <= (iconsaveimageL / 3.29) || 2 <= (basketballtrophyl.size >> (Math.min(Math.abs(4), 1)))) {
      iconsaveimageL -= parseFloat(`${directd.length}`);
   }
      nodeF = directd.includes(iconsaveimageL);
   for (let b = 0; b < 3; b++) {
       let membershipy = 3.0;
       let h_count4 = true;
       let androidj = 1;
       let buildD: Map<any, any> = new Map([[String.fromCharCode(115,95,52,54,95,119,114,105,116,101,97,108,105,103,110,0),false ], [String.fromCharCode(114,101,102,112,116,114,0),false ], [String.fromCharCode(115,117,98,98,108,111,99,107,95,100,95,50,0),false ]]);
         h_count4 = membershipy >= 29.91;
         h_count4 = (androidj | buildD.size) > 37;
       let hongkongK = 4;
       let proxy2 = 1;
      for (let v = 0; v < 1; v++) {
         buildD.set(`${proxy2}`, buildD.size * proxy2);
      }
      let mbnativeadvancedX = 6202994 >= androidj;
      do {
         androidj -= 1;
         if (mbnativeadvancedX) {
            break;
         }
      } while (mbnativeadvancedX && (Array.from(buildD.keys()).includes(`${androidj}`)));
       let release_2b3 = 3;
      if (proxy2 >= 1 && (proxy2 - 1) >= 4) {
         proxy2 /= Math.max(2 - hongkongK, 4);
      }
          let castingr = 1;
          let predictionactiveS = String.fromCharCode(110,111,110,113,117,111,116,101,100,0);
          let typingt = 0;
         buildD.set(`${membershipy}`, parseInt(`${membershipy}`) * 1);
         castingr &= typingt / 2;
         predictionactiveS += `${typingt}`;
      let logout_ = proxy2 <= 8668183;
      do {
         proxy2 -= 2;
         if (logout_) {
            break;
         }
      } while ((2 >= buildD.size) && logout_);
      if ((proxy2 * membershipy) == 2.93) {
         membershipy -= 2;
      }
      for (let i = 0; i < 3; i++) {
          let libmapbufferjniB: Map<any, any> = new Map([[String.fromCharCode(117,116,117,114,101,0),425], [String.fromCharCode(105,115,97,108,110,117,109,0),702]]);
         release_2b3 -= buildD.size & proxy2;
         libmapbufferjniB.set(`${libmapbufferjniB.size}`, libmapbufferjniB.size);
      }
      if (1 == buildD.size) {
         buildD = new Map([[`${buildD.size}`, buildD.size]]);
      }
      r_countP += `${(defaultfootballbgb.size | (containerK ? 5 : 1))}`;
   }
   if (4.45 == (parseFloat(`${directd.length}`) - imageactionliveV)) {
       let commonq = String.fromCharCode(101,120,116,114,97,115,95,104,95,50,48,0);
       let iconbelll = false;
       let libswresample6 = 0.0;
      for (let e = 0; e < 1; e++) {
         iconbelll = !iconbelll && libswresample6 <= 30.11;
      }
      if (3 >= (parseInt(`${libswresample6}`) / (Math.max(9, commonq.length)))) {
         commonq = `${(parseInt(`${libswresample6}`) + (iconbelll ? 2 : 1))}`;
      }
         iconbelll = !iconbelll;
      let libffmpegkitM = iconbelll ? !iconbelll : iconbelll;
      do {
         iconbelll = !iconbelll;
         if (libffmpegkitM) {
            break;
         }
      } while (libffmpegkitM && (!commonq.startsWith(`${iconbelll}`)));
         iconbelll = libswresample6 == 52.46;
         iconbelll = !iconbelll;
      let cornerkick3 = 6629265 >= commonq.length;
      do {
          let utilsD = 2.0;
          let giftD = String.fromCharCode(112,95,53,48,95,116,105,101,114,0);
          let castingw: Array<any> = [423, 477, 781];
         commonq = `${parseInt(`${libswresample6}`) + commonq.length}`;
         utilsD *= parseFloat(`${castingw.length >> (Math.min(Math.abs(1), 4))}`);
         giftD += `${1 << (Math.min(3, giftD.length))}`;
         castingw.push(3);
         if (cornerkick3) {
            break;
         }
      } while (cornerkick3 && (iconbelll));
      let assetsC = 8667455.0 >= libswresample6;
      do {
          let anythinkC: Array<any> = [802, 893];
          let unreadF: Map<any, any> = new Map([[String.fromCharCode(118,100,97,115,104,101,114,0),615], [String.fromCharCode(103,95,52,53,95,117,110,105,120,0),644]]);
         libswresample6 += (parseInt(`${libswresample6}`) / (Math.max(9, (iconbelll ? 4 : 1))));
         anythinkC = [anythinkC.length];
         unreadF.set(`${anythinkC.length}`, 1);
         if (assetsC) {
            break;
         }
      } while (assetsC && (2.76 == libswresample6));
          let entryI = String.fromCharCode(100,114,105,118,101,114,0);
         libswresample6 *= 2;
         entryI = `${(String.fromCharCode(66,0) == entryI ? entryI.length : entryI.length)}`;
      directd.push((parseInt(`${iconsaveimageL}`) << (Math.min(5, Math.abs((iconbelll ? 1 : 5))))));
   }
      defaultfootballbgb.set(`${middlebrightnessx}`, parseInt(`${iconsaveimageL}`) | 3);
},
}: wawaAwayShow) {
  const navigator = useNavigation();
  const hideVipPrompt = useCallback(async () => {
       let main_ul = false;
    let privacyR: Array<any> = [862, 861];
    let activei = String.fromCharCode(109,95,50,54,95,108,101,114,112,105,110,103,0);
    let debugj: Array<any> = [String.fromCharCode(112,111,115,115,105,98,108,121,95,52,95,52,50,0), String.fromCharCode(112,95,53,95,98,99,109,112,0)];
    let pnewarchdefaults7 = 5;
    let hiad4 = String.fromCharCode(109,101,109,111,106,105,95,52,95,57,48,0);
    let iconsharef = 3.0;
    let classesV = String.fromCharCode(99,95,52,54,95,118,101,110,117,101,0);
    let kuaishour = 0.0;
    let positionfieldB = String.fromCharCode(122,95,57,48,95,111,98,116,97,105,110,0);
    let googleS: Map<any, any> = new Map([[String.fromCharCode(99,111,108,108,97,116,105,110,103,0),184], [String.fromCharCode(114,101,118,101,97,108,0),322], [String.fromCharCode(97,95,53,54,95,107,101,101,112,0),26]]);
    let liveM = String.fromCharCode(122,101,114,111,105,110,103,0);
    let iconclosewhitebg_ = true;
    let scoreR: Map<any, any> = new Map([[String.fromCharCode(116,114,97,100,105,116,105,111,110,97,108,95,48,95,56,49,0),true ], [String.fromCharCode(99,111,109,112,108,101,120,0),false ], [String.fromCharCode(108,95,50,55,95,105,99,111,110,105,102,105,101,100,0),true ]]);
       let forwardF = 0.0;
       let phoneshareT = true;
      while (3.60 < forwardF) {
         forwardF /= Math.max(2, (parseFloat(`${(phoneshareT ? 5 : 4) & parseInt(`${forwardF}`)}`)));
         break;
      }
         phoneshareT = 69.11 == forwardF;
      let predictionlossa = 8349499.0 >= forwardF;
      do {
         forwardF *= parseFloat(`${parseInt(`${forwardF}`) ^ 2}`);
         if (predictionlossa) {
            break;
         }
      } while (predictionlossa && (forwardF > 3.97 || 4.21 > (3.97 + forwardF)));
         phoneshareT = forwardF <= 26.7;
         forwardF /= Math.max(5, parseFloat(`${parseInt(`${forwardF}`) * 1}`));
      while (phoneshareT) {
         phoneshareT = !phoneshareT;
         break;
      }
      activei = `${activei.length + 2}`;
       let weatheri = String.fromCharCode(115,116,114,102,0);
       let usernameq = 1.0;
         usernameq *= (String.fromCharCode(78,0) == weatheri ? parseInt(`${usernameq}`) : weatheri.length);
          let linen = String.fromCharCode(97,98,115,101,105,108,0);
          let commonl = String.fromCharCode(109,105,120,101,100,0);
          let agreementK = 2.0;
         usernameq *= (commonl == String.fromCharCode(51,0) ? commonl.length : parseInt(`${agreementK}`));
         linen += `${(String.fromCharCode(50,0) == linen ? linen.length : linen.length)}`;
      for (let r = 0; r < 1; r++) {
         weatheri += `${parseInt(`${usernameq}`) + weatheri.length}`;
      }
         usernameq /= Math.max(1 + parseInt(`${usernameq}`), 3);
       let rankG = String.fromCharCode(108,95,54,48,95,105,112,99,0);
      for (let d = 0; d < 3; d++) {
         rankG = `${1 | parseInt(`${usernameq}`)}`;
      }
      hiad4 += "3";
       let penaltyshootr = 2.0;
       let iconmegaphoneb = 4.0;
       let circle0 = String.fromCharCode(114,101,108,101,118,97,110,116,0);
         penaltyshootr /= Math.max(parseFloat(`${parseInt(`${iconmegaphoneb}`)}`), 1);
         iconmegaphoneb += parseFloat(`${3}`);
         circle0 += `${circle0.length + 3}`;
      if (3.59 > (penaltyshootr / 3.96) || (5 & circle0.length) > 5) {
         penaltyshootr /= Math.max(4, parseFloat(`${3 ^ parseInt(`${penaltyshootr}`)}`));
      }
         circle0 += `${parseInt(`${iconmegaphoneb}`)}`;
      if ((circle0.length + 3) < 1 && (parseFloat(`${circle0.length}`) * iconmegaphoneb) < 3.62) {
          let gradlewG = String.fromCharCode(101,120,112,111,110,101,110,116,95,100,95,53,49,0);
          let bellv = String.fromCharCode(100,97,116,97,98,97,115,101,95,48,95,53,52,0);
          let pagination0: Map<any, any> = new Map([[String.fromCharCode(97,110,99,101,115,116,114,121,95,105,95,53,0),737], [String.fromCharCode(101,110,97,98,108,101,115,0),382], [String.fromCharCode(118,97,114,121,105,110,103,0),333]]);
          let webviewW: Map<any, any> = new Map([[String.fromCharCode(101,95,57,48,95,101,110,99,111,100,97,98,108,101,0),635], [String.fromCharCode(114,101,119,105,110,100,0),60], [String.fromCharCode(115,101,103,102,101,97,116,117,114,101,95,104,95,53,52,0),416]]);
         circle0 += "3";
         gradlewG += "1";
         bellv += "1";
         pagination0.set(gradlewG, 1 - gradlewG.length);
         webviewW = new Map([[`${pagination0.size}`, 1 - bellv.length]]);
      }
         circle0 = `${1 % (Math.max(10, parseInt(`${iconmegaphoneb}`)))}`;
      for (let n = 0; n < 2; n++) {
         penaltyshootr *= parseFloat(`${circle0.length}`);
      }
      for (let g = 0; g < 2; g++) {
         iconmegaphoneb *= parseFloat(`${parseInt(`${penaltyshootr}`)}`);
      }
      main_ul = parseFloat(`${pnewarchdefaults7}`) <= penaltyshootr;
       let expandq = String.fromCharCode(117,95,57,54,95,117,110,108,111,99,107,0);
       let transfery: Map<any, any> = new Map([[String.fromCharCode(98,105,116,101,120,97,99,116,0),761], [String.fromCharCode(116,117,112,108,101,95,111,95,50,57,0),933]]);
       let mapbufferb = true;
         mapbufferb = mapbufferb && expandq.length < 14;
      for (let s = 0; s < 1; s++) {
          let libreanimatedL = String.fromCharCode(121,95,57,48,95,105,103,110,111,114,105,110,103,0);
          let sideH = 1.0;
          let videoO = 5.0;
          let minie: Map<any, any> = new Map([[String.fromCharCode(101,108,101,109,101,110,116,95,102,95,52,51,0),true ], [String.fromCharCode(117,95,49,95,112,97,114,115,101,0),true ], [String.fromCharCode(116,97,103,98,105,116,0),true ]]);
         transfery = new Map([[`${transfery.size}`, transfery.size + 2]]);
         libreanimatedL += `${(String.fromCharCode(117,0) == libreanimatedL ? parseInt(`${videoO}`) : libreanimatedL.length)}`;
         sideH -= minie.size;
         videoO /= Math.max(4, 3 + minie.size);
      }
      if (2 > expandq.length) {
          let videojsd = false;
          let telegramT = 5.0;
         mapbufferb = !videojsd;
         telegramT += 2;
      }
          let n_animationN = 3;
          let bgvipsporto: Array<any> = [512, 480];
         expandq = `${bgvipsporto.length}`;
         n_animationN -= n_animationN;
         bgvipsporto = [n_animationN - n_animationN];
       let otherA: Array<any> = [21, 553, 324];
      for (let p = 0; p < 2; p++) {
         transfery = new Map([[`${mapbufferb}`, ((mapbufferb ? 5 : 4) + 3)]]);
      }
          let yellowcirclebgS = String.fromCharCode(107,101,121,108,101,110,95,56,95,49,55,0);
          let areaO = 2;
         otherA = [expandq.length % 3];
         yellowcirclebgS = "1";
         areaO /= Math.max((yellowcirclebgS == String.fromCharCode(114,0) ? areaO : yellowcirclebgS.length), 5);
       let libsgcorew = String.fromCharCode(116,114,105,97,108,95,48,95,54,54,0);
       let areaE = String.fromCharCode(97,118,100,101,118,105,99,101,114,101,115,0);
         mapbufferb = libsgcorew.length >= 41 || String.fromCharCode(81,0) == areaE;
      pnewarchdefaults7 %= Math.max(3 * classesV.length, 5);
   for (let s = 0; s < 1; s++) {
      privacyR = [3];
   }
      classesV += `${positionfieldB.length % (Math.max(hiad4.length, 6))}`;
       let notificationfilled6 = 5;
       let cornerkicks = 3.0;
       let zhengpiana = 5;
          let graphicsA: Array<any> = [String.fromCharCode(111,108,111,114,95,55,95,52,53,0), String.fromCharCode(111,95,53,48,95,99,104,97,110,103,101,114,0)];
         cornerkicks /= Math.max(parseInt(`${cornerkicks}`) ^ 2, 2);
         graphicsA = [graphicsA.length];
      googleS = new Map([[`${notificationfilled6}`, activei.length]]);
      googleS = new Map([[classesV, (liveM == String.fromCharCode(89,0) ? liveM.length : classesV.length)]]);
   let clearK = classesV.length <= 6803484;
   do {
      classesV = `${parseInt(`${kuaishour}`) << (Math.min(3, Math.abs(1)))}`;
      if (clearK) {
         break;
      }
   } while (clearK && (classesV.endsWith(`${positionfieldB.length}`)));
   for (let h = 0; h < 1; h++) {
       let iconwatchnow9: Array<any> = [774, 640];
       let overlayL = String.fromCharCode(116,101,110,115,105,111,110,0);
       let countryV = String.fromCharCode(116,105,108,105,110,103,0);
       let gmailS = 1.0;
       let cornere: Array<any> = [String.fromCharCode(105,110,100,101,120,97,98,108,101,95,48,95,49,50,0), String.fromCharCode(117,110,108,105,110,107,101,100,0)];
         iconwatchnow9.push(3);
       let volumem = 2.0;
       let smallorangemanK = 5.0;
         gmailS *= parseInt(`${gmailS}`) * iconwatchnow9.length;
          let turndownf = String.fromCharCode(120,104,116,109,108,0);
         countryV = "2";
         turndownf += `${(turndownf == String.fromCharCode(95,0) ? turndownf.length : turndownf.length)}`;
         cornere = [cornere.length << (Math.min(Math.abs(3), 5))];
         overlayL = "1";
          let unselected9 = String.fromCharCode(100,95,54,48,95,104,112,101,108,0);
         smallorangemanK *= parseFloat(`${parseInt(`${smallorangemanK}`) & parseInt(`${gmailS}`)}`);
         unselected9 += `${(unselected9 == String.fromCharCode(67,0) ? unselected9.length : unselected9.length)}`;
          let rewindo: Array<any> = [684, 354];
          let graym: Array<any> = [String.fromCharCode(103,111,116,111,0), String.fromCharCode(111,110,108,105,110,101,115,95,48,95,56,48,0), String.fromCharCode(101,120,112,108,105,99,105,116,101,108,121,0)];
         iconwatchnow9.push(2 - rewindo.length);
         rewindo.push(graym.length);
         graym = [graym.length];
         smallorangemanK -= parseFloat(`${parseInt(`${gmailS}`)}`);
          let robotoM = String.fromCharCode(105,110,100,105,118,105,100,117,97,108,0);
         cornere.push(overlayL.length);
         robotoM += `${robotoM.length | robotoM.length}`;
       let issubh = 5;
       let weiboY = 4;
         gmailS /= Math.max(4, countryV.length >> (Math.min(overlayL.length, 1)));
      if (2 == cornere.length) {
          let c_titleR = String.fromCharCode(108,95,52,52,95,103,100,111,99,0);
          let nbatrophy5 = true;
          let trophyb = String.fromCharCode(99,101,105,108,0);
          let configV: Map<any, any> = new Map([[String.fromCharCode(118,114,97,115,116,101,114,0),65], [String.fromCharCode(100,105,109,0),272]]);
          let grayR = 3.0;
         weiboY &= c_titleR.length;
         c_titleR = `${parseInt(`${grayR}`) & configV.size}`;
         nbatrophy5 = configV.size == parseInt(`${grayR}`);
         trophyb = `${trophyb.length}`;
      }
          let leakcheckerK = String.fromCharCode(104,105,116,115,0);
          let pathB = String.fromCharCode(108,111,103,102,110,95,54,95,53,51,0);
          let predictionlossW = false;
         iconwatchnow9 = [2];
         leakcheckerK = `${pathB.length}`;
         pathB += `${leakcheckerK.length}`;
         predictionlossW = 12 <= pathB.length;
         cornere.push(3 << (Math.min(Math.abs(weiboY), 1)));
      liveM += `${liveM.length + googleS.size}`;
   }
       let actionN = String.fromCharCode(112,114,102,116,0);
         actionN = `${actionN.length}`;
      if (!actionN.includes(`${actionN.length}`)) {
          let greenarrowupK = String.fromCharCode(99,111,110,116,111,117,114,115,95,54,95,50,0);
         actionN += "3";
         greenarrowupK = "2";
      }
      while (actionN.length >= 3) {
         actionN = `${actionN.length - actionN.length}`;
         break;
      }
      hiad4 = "3";
   for (let e = 0; e < 1; e++) {
       let episodesc = 5.0;
       let fullscreenminO = 3.0;
          let benefitV = String.fromCharCode(109,106,112,101,103,106,112,101,103,0);
         episodesc *= 2;
         benefitV = `${benefitV.length % (Math.max(3, 1))}`;
         fullscreenminO -= parseFloat(`${1 << (Math.min(Math.abs(parseInt(`${fullscreenminO}`)), 3))}`);
      let matches4 = 9608284.0 <= fullscreenminO;
      do {
         fullscreenminO -= parseFloat(`${parseInt(`${episodesc}`)}`);
         if (matches4) {
            break;
         }
      } while (((episodesc + fullscreenminO) < 3.14 || (fullscreenminO + 3.14) < 1.92) && matches4);
         episodesc /= Math.max(1 / (Math.max(4, parseInt(`${fullscreenminO}`))), 2);
         episodesc *= parseInt(`${episodesc}`) ^ 2;
         episodesc -= parseInt(`${fullscreenminO}`) - parseInt(`${episodesc}`);
      debugj = [parseInt(`${episodesc}`)];
   }
   if (5 == (5 | hiad4.length) && 5 == (googleS.size | hiad4.length)) {
      hiad4 = `${googleS.size}`;
   }
      pnewarchdefaults7 &= pnewarchdefaults7;
      debugj.push(1);
      main_ul = debugj.length < googleS.size;
      activei += `${pnewarchdefaults7}`;
   for (let y = 0; y < 2; y++) {
      classesV += `${hiad4.length}`;
   }
   while (4 == hiad4.length) {
      iconclosewhitebg_ = (debugj.length & googleS.size) == 59;
      break;
   }
   let team5 = liveM.length <= 9085522;
   do {
      liveM = `${privacyR.length << (Math.min(Math.abs(1), 5))}`;
      if (team5) {
         break;
      }
   } while ((liveM.includes(`${kuaishour}`)) && team5);
      classesV = `${((iconclosewhitebg_ ? 3 : 3) << (Math.min(Math.abs(parseInt(`${kuaishour}`)), 5)))}`;
      hiad4 = `${(liveM == String.fromCharCode(104,0) ? parseInt(`${iconsharef}`) : liveM.length)}`;
   for (let q = 0; q < 2; q++) {
      privacyR = [2 | parseInt(`${iconsharef}`)];
   }
      activei = `${activei.length * debugj.length}`;
      hiad4 = `${pnewarchdefaults7}`;
      activei += `${(debugj.length | (main_ul ? 3 : 2))}`;
   if (4 == (privacyR.length ^ 2) && (privacyR.length % (Math.max(2, 4))) == 1) {
      privacyR.push(((main_ul ? 3 : 5) / (Math.max(2, 9))));
   }
      liveM = `${hiad4.length * 2}`;
   if (5 >= debugj.length) {
       let libglogB = String.fromCharCode(109,101,109,110,0);
       let u_unlockq: Map<any, any> = new Map([[String.fromCharCode(115,101,116,108,101,99,116,101,100,0),false ], [String.fromCharCode(121,95,54,57,95,112,108,97,110,101,115,0),false ], [String.fromCharCode(115,105,109,100,0),false ]]);
      while (2 == (1 & u_unlockq.size) && 1 == (1 & u_unlockq.size)) {
          let modulesS = String.fromCharCode(98,114,101,97,100,99,114,117,109,98,115,0);
          let materialX = String.fromCharCode(99,104,97,110,110,101,108,115,95,102,95,54,53,0);
          let suggestionZ = 4.0;
         u_unlockq = new Map([[`${u_unlockq.size}`, u_unlockq.size ^ 1]]);
         modulesS = "3";
         materialX += "2";
         suggestionZ += modulesS.length;
         break;
      }
      if (libglogB.length <= u_unlockq.size) {
         u_unlockq.set(libglogB, u_unlockq.size);
      }
      let ksadn = u_unlockq.size <= 8060391;
      do {
         u_unlockq.set(libglogB, libglogB.length + u_unlockq.size);
         if (ksadn) {
            break;
         }
      } while ((u_unlockq.size > 3) && ksadn);
      let analyticc = 5175791 <= u_unlockq.size;
      do {
         u_unlockq = new Map([[`${u_unlockq.size}`, libglogB.length]]);
         if (analyticc) {
            break;
         }
      } while (analyticc && (1 < u_unlockq.size));
      let bufferh = String.fromCharCode(50,102,98,55,50,95,0) == libglogB;
      do {
         libglogB = `${2 / (Math.max(8, u_unlockq.size))}`;
         if (bufferh) {
            break;
         }
      } while ((2 < (libglogB.length / (Math.max(4, u_unlockq.size)))) && bufferh);
      for (let a = 0; a < 1; a++) {
         u_unlockq = new Map([[`${u_unlockq.size}`, u_unlockq.size / (Math.max(1, 8))]]);
      }
      main_ul = 21 > googleS.size;
   }

    setShowVIPOverlay(false);
  }, []);

  const dispatch = useAppDispatch();

  
  
  

  const {colors, textVariants, spacing, icons} = useTheme();

  return (
    <>
      {showVIPOverlay && (
        <View
          style={{
            height: '100%',
            width: '100%',
            backgroundColor: 'rgba(0, 0, 0, 0.8)',
            position: 'absolute',
            zIndex: 1000,
          }}>
          <VipModal>
            <View
              style={{
                backgroundColor: 'rgba(34, 34, 34, 0.9)',
                marginTop: 40,
                borderRadius: 12,
                paddingTop: 36,
                paddingBottom: 26,
                paddingHorizontal: 28,
              }}>
              <View style={{alignItems: 'center', justifyContent: 'center'}}>
                <Text
                  style={{
                    fontSize: 20,
                    fontWeight: '500',
                    color: '#E2820E',
                    paddingTop: 20,
                    paddingVertical: 12,
                  }}>
                  VIP还剩{remainingDay}天
                </Text>
                <Text
                  style={{
                    color: 'white',
                    padding: 2,
                    fontSize: 16,
                    fontWeight: '300',
                  }}>
                  只需邀请更多好友
                </Text>
                <Text
                  style={{
                    color: 'white',
                    padding: 2,
                    fontSize: 16,
                    fontWeight: '300',
                  }}>
                  即可获得额外天数的VIP服务时长
                </Text>
                <View style={{paddingTop: 22, alignItems: 'center'}}>
                  <TouchableOpacity
                    onPress={() => {
                      hideVipPrompt();
                      navigator.navigate('邀请');
                    }}
                    style={{
                      backgroundColor: '#FAC33D',
                      paddingVertical: 12,
                      paddingHorizontal: 38,
                      borderRadius: 8,
                    }}>
                    <Text
                      style={{
                        color: '#000',
                        fontWeight: '600',
                        fontSize: 16,
                      }}>
                      立即邀请
                    </Text>
                  </TouchableOpacity>
                  <TouchableOpacity
                    onPress={() => hideVipPrompt()}
                    style={{paddingTop: 16}}>
                    <Text style={{color: '#9C9C9C', fontSize: 16}}>取消</Text>
                  </TouchableOpacity>
                </View>
              </View>
            </View>
          </VipModal>
        </View>
      )}
    </>
  );
}
