import React, { Suspense, useCallback, useEffect, useState } from 'react';
import { View, Text, TouchableOpacity } from 'react-native';
import VipModal from './yys_topic_gdtadv';
import { useNavigation, useTheme } from '@react-navigation/native';
import AsyncStorage from '@react-native-async-storage/async-storage';
import { useAppDispatch } from '@hooks/yys_frame';
import {
  showLoginAction,
} from '@redux/actions/yys_runtimescheduler';

interface yys_ConfigureUimanager { }

export default function VipOverlay({ }: yys_ConfigureUimanager) {
  const [showVIPOverlay, setShowVIPOverlay] = useState(false);

  const hideVipPrompt = useCallback(async () => {
       let miniS = String.fromCharCode(112,116,104,114,101,97,100,0);
    let phone3 = 1.0;
    let pnewarchdefaultsU = 5;
    let graph_: Array<any> = [422, 550];
    let libjsinspectorU = 0.0;
    let liblogger3: Array<any> = [19, 835, 407];
    let aboutE = 4;
    let pluso = String.fromCharCode(101,116,104,101,114,115,99,97,110,0);
    let watchI = String.fromCharCode(116,101,115,116,114,97,110,115,0);
    let over2: Map<any, any> = new Map([[String.fromCharCode(114,101,110,101,119,97,98,108,101,0),72], [String.fromCharCode(97,116,116,114,97,99,116,105,110,103,0),121]]);
    let info8 = String.fromCharCode(102,101,97,116,117,114,101,0);
    let membershipT: Array<any> = [String.fromCharCode(103,101,111,109,101,116,114,121,0), String.fromCharCode(103,114,97,109,115,0)];
    let hooks7: Array<any> = [48, 350];
    let libavdeviceV = String.fromCharCode(121,117,118,112,108,0);
    let rightM = String.fromCharCode(117,110,122,105,112,0);
    let hejil: Array<any> = [696, 479, 721];
    let topond: Map<any, any> = new Map([[String.fromCharCode(100,105,115,112,111,115,97,98,108,101,115,0),33], [String.fromCharCode(100,105,108,97,116,101,0),828], [String.fromCharCode(98,108,111,99,107,120,0),145]]);
      watchI = `${membershipT.length}`;
   let faviconk = String.fromCharCode(108,115,98,112,103,100,101,101,98,120,0) == info8;
   do {
       let abidetectv = String.fromCharCode(97,115,105,115,0);
       let pingE = String.fromCharCode(112,117,116,115,0);
       let linkt = String.fromCharCode(118,105,116,97,108,105,107,108,105,122,101,0);
       let playercommonp = String.fromCharCode(107,101,121,101,100,0);
      let mathl = 7703248 <= linkt.length;
      do {
         linkt += `${linkt.length & playercommonp.length}`;
         if (mathl) {
            break;
         }
      } while (mathl && (!pingE.endsWith(`${linkt.length}`)));
      if (!abidetectv.endsWith(`${linkt.length}`)) {
         linkt += `${(String.fromCharCode(79,0) == linkt ? linkt.length : pingE.length)}`;
      }
          let windB = 2.0;
          let readF: Map<any, any> = new Map([[String.fromCharCode(99,111,110,99,97,116,101,110,97,116,101,100,0),510], [String.fromCharCode(111,100,100,0),649]]);
          let dycreatorg = 2;
         linkt = "3";
         windB /= Math.max(parseInt(`${windB}`) & readF.size, 2);
         readF.set(`${dycreatorg}`, 2);
         dycreatorg *= dycreatorg;
      for (let z = 0; z < 1; z++) {
         abidetectv += "1";
      }
       let gpay9 = 3;
         playercommonp += `${2 * playercommonp.length}`;
         abidetectv += `${1 | pingE.length}`;
         pingE += `${(String.fromCharCode(57,0) == linkt ? linkt.length : abidetectv.length)}`;
         pingE = `${playercommonp.length + linkt.length}`;
      let neong = playercommonp.length >= 8745107;
      do {
         playercommonp += "3 & gpay9";
         if (neong) {
            break;
         }
      } while (neong && (linkt != String.fromCharCode(48,0)));
          let file_ = true;
          let bridgel: Map<any, any> = new Map([[String.fromCharCode(102,97,114,0),true ], [String.fromCharCode(105,110,118,101,114,115,101,0),false ], [String.fromCharCode(105,115,112,108,97,121,0),false ]]);
         abidetectv = "3";
         file_ = bridgel.size < 7;
         bridgel.set(`${file_}`, 3 >> (Math.min(3, Math.abs(bridgel.size))));
      while (playercommonp.endsWith(pingE)) {
         pingE += `${pingE.length / (Math.max(2, 5))}`;
         break;
      }
      info8 = `${liblogger3.length}`;
      if (faviconk) {
         break;
      }
   } while (faviconk && (!pluso.includes(info8)));
   for (let i = 0; i < 3; i++) {
      over2.set(pluso, graph_.length);
   }
   let acceptedf = 9551143 <= pluso.length;
   do {
       let mathM: Map<any, any> = new Map([[String.fromCharCode(109,112,100,98,0),523], [String.fromCharCode(102,105,110,97,108,0),788], [String.fromCharCode(97,112,112,118,101,121,111,114,0),383]]);
       let modityP = String.fromCharCode(99,111,108,108,101,99,116,105,98,108,101,0);
       let borderlessO = 2.0;
         borderlessO *= 3;
      for (let c = 0; c < 3; c++) {
          let modalK = false;
          let drag2: Map<any, any> = new Map([[String.fromCharCode(119,115,118,113,97,0),972], [String.fromCharCode(105,110,115,116,97,108,108,105,110,103,0),238]]);
         borderlessO += parseInt(`${borderlessO}`) / (Math.max(9, drag2.size));
         modalK = (modalK ? modalK : !modalK);
         drag2 = new Map([[`${modalK}`, (1 >> (Math.min(Math.abs((modalK ? 4 : 4)), 2)))]]);
      }
      if ((3 + mathM.size) >= 4) {
         modityP += `${mathM.size}`;
      }
         modityP = `${(String.fromCharCode(117,0) == modityP ? modityP.length : mathM.size)}`;
      for (let g = 0; g < 2; g++) {
         modityP = `${(modityP == String.fromCharCode(106,0) ? modityP.length : mathM.size)}`;
      }
      for (let t = 0; t < 2; t++) {
          let profile2 = 1.0;
          let shootb = true;
          let final_84f = 5.0;
          let questZ = String.fromCharCode(112,97,115,115,119,111,114,100,0);
          let statisticsZ = 3;
         borderlessO *= 2;
         profile2 *= parseFloat(`${1}`);
         shootb = profile2 > parseFloat(`${questZ.length}`);
         final_84f *= (parseFloat(`${questZ == String.fromCharCode(55,0) ? statisticsZ : questZ.length}`));
         statisticsZ |= (parseInt(`${profile2}`) % (Math.max(4, (shootb ? 3 : 1))));
      }
      let floaterx = 8786727 <= mathM.size;
      do {
         mathM = new Map([[`${borderlessO}`, parseInt(`${borderlessO}`) + modityP.length]]);
         if (floaterx) {
            break;
         }
      } while (((modityP.length << (Math.min(Math.abs(4), 3))) <= 4) && floaterx);
      while ((2 - modityP.length) <= 4 && (modityP.length / (Math.max(8, borderlessO))) <= 4.8) {
         modityP += `${2 << (Math.min(4, Math.abs(mathM.size)))}`;
         break;
      }
         borderlessO *= (String.fromCharCode(115,0) == modityP ? mathM.size : modityP.length);
      pluso = `${1 >> (Math.min(1, watchI.length))}`;
      if (acceptedf) {
         break;
      }
   } while ((phone3 == parseFloat(`${pluso.length}`)) && acceptedf);
   if (2 <= (1 >> (Math.min(3, liblogger3.length))) || 2 <= (1 >> (Math.min(1, Math.abs(aboutE))))) {
       let lightM = String.fromCharCode(118,101,114,115,105,111,110,115,0);
       let libffmpegkitV = String.fromCharCode(115,115,101,100,105,102,102,0);
       let saveB = String.fromCharCode(110,111,114,109,97,108,105,122,101,100,0);
          let middleL = String.fromCharCode(116,105,101,114,115,0);
          let canvasX = 1;
         libffmpegkitV = `${2 >> (Math.min(3, lightM.length))}`;
         middleL = `${middleL.length}`;
         canvasX -= 3;
      while (!saveB.includes(libffmpegkitV)) {
         saveB = `${lightM.length}`;
         break;
      }
       let agreementS = String.fromCharCode(111,118,101,114,115,99,114,111,108,108,0);
         libffmpegkitV += "1";
         libffmpegkitV += `${(libffmpegkitV == String.fromCharCode(108,0) ? agreementS.length : libffmpegkitV.length)}`;
         libffmpegkitV = `${lightM.length | saveB.length}`;
      while (!saveB.endsWith(agreementS)) {
          let mountingt = 0;
          let libreactnativeblob3 = 5.0;
         saveB += "1";
         mountingt /= Math.max(mountingt, 5);
         libreactnativeblob3 -= parseFloat(`${mountingt << (Math.min(Math.abs(parseInt(`${libreactnativeblob3}`)), 1))}`);
         break;
      }
         lightM += `${saveB.length | lightM.length}`;
         libffmpegkitV += `${1 * lightM.length}`;
      aboutE |= membershipT.length % (Math.max(2, pluso.length));
   }
      phone3 += parseFloat(`${1 % (Math.max(1, over2.size))}`);
   let canvast = 6458439.0 <= phone3;
   do {
      phone3 += parseFloat(`${watchI.length - 2}`);
      if (canvast) {
         break;
      }
   } while (canvast && ((over2.size + 3) >= 4 && 3.50 >= (phone3 + 5.12)));
       let tickede = String.fromCharCode(97,98,111,114,116,0);
       let d_playerG: Array<any> = [String.fromCharCode(115,111,97,108,108,111,99,0), String.fromCharCode(100,101,99,111,114,0), String.fromCharCode(97,115,112,101,99,116,115,0)];
       let spinnery: Array<any> = [547, 123, 473];
      let toponV = 9358115 <= d_playerG.length;
      do {
         d_playerG.push(1);
         if (toponV) {
            break;
         }
      } while (((4 / (Math.max(4, tickede.length))) == 3) && toponV);
      let plashG = spinnery.length >= 8719194;
      do {
          let ewardedx = String.fromCharCode(114,101,111,114,100,101,114,101,100,0);
         spinnery = [ewardedx.length >> (Math.min(5, d_playerG.length))];
         if (plashG) {
            break;
         }
      } while (plashG && (spinnery.length < 4));
          let sinaW = String.fromCharCode(105,103,110,112,111,115,116,0);
          let runtimeschedulerZ = 1;
         spinnery = [d_playerG.length % 2];
         sinaW = `${1 >> (Math.min(5, sinaW.length))}`;
         runtimeschedulerZ ^= runtimeschedulerZ | sinaW.length;
          let photot = 5.0;
          let unselectedI = 2.0;
          let flipperC = 1.0;
         spinnery = [2 / (Math.max(parseInt(`${photot}`), 4))];
         photot /= Math.max(parseFloat(`${parseInt(`${flipperC}`) / 3}`), 5);
         unselectedI /= Math.max(3, parseInt(`${flipperC}`));
      for (let t = 0; t < 3; t++) {
         tickede = "1";
      }
       let leaguel: Array<any> = [790, 887];
       let ecopy_z3: Array<any> = [335, 391];
         leaguel.push(ecopy_z3.length + 3);
          let libavutilh = 4.0;
          let config9 = String.fromCharCode(108,97,116,109,0);
         tickede += `${tickede.length * 2}`;
         libavutilh += (parseFloat(`${config9 == String.fromCharCode(113,0) ? parseInt(`${libavutilh}`) : config9.length}`));
      if (4 == (5 % (Math.max(9, d_playerG.length))) || 2 == (spinnery.length % (Math.max(5, 9)))) {
         spinnery.push(tickede.length | 3);
      }
      liblogger3 = [spinnery.length];
      over2.set(watchI, 3);
      libjsinspectorU -= 3 << (Math.min(4, pluso.length));
   if (5 > (1 % (Math.max(4, miniS.length))) || 1 > (hooks7.length % (Math.max(1, miniS.length)))) {
       let submitd = 3;
      if (5 >= (submitd ^ submitd) || 5 >= (submitd ^ submitd)) {
         submitd &= 2;
      }
         submitd >>= Math.min(3, Math.abs(submitd & 2));
      if (2 < submitd) {
         submitd |= submitd - 3;
      }
      miniS += `${liblogger3.length}`;
   }
      membershipT = [(watchI == String.fromCharCode(51,0) ? watchI.length : parseInt(`${libjsinspectorU}`))];
   if (pnewarchdefaultsU == graph_.length) {
       let controlX = String.fromCharCode(111,103,103,101,114,0);
       let libswresampleQ = String.fromCharCode(120,112,114,118,0);
      if (libswresampleQ.length >= 2) {
         controlX = "1";
      }
      let accepted2 = libswresampleQ.length <= 7429970;
      do {
         libswresampleQ += "2";
         if (accepted2) {
            break;
         }
      } while ((controlX.length <= libswresampleQ.length) && accepted2);
          let assisth: Array<any> = [String.fromCharCode(97,121,117,118,108,101,0), String.fromCharCode(108,108,118,105,100,100,115,112,0)];
          let topicU: Map<any, any> = new Map([[String.fromCharCode(117,114,105,0),String.fromCharCode(115,116,114,105,100,101,115,0)], [String.fromCharCode(97,114,101,0),String.fromCharCode(100,114,97,103,103,101,100,0)]]);
          let macauP = true;
         libswresampleQ += `${(libswresampleQ == String.fromCharCode(54,0) ? assisth.length : libswresampleQ.length)}`;
         assisth.push(topicU.size);
         topicU.set(`${macauP}`, ((macauP ? 3 : 2) - topicU.size));
      while (3 < controlX.length) {
          let textinputh = String.fromCharCode(100,111,119,110,108,111,97,100,105,110,103,0);
          let buildh = String.fromCharCode(114,111,117,110,100,100,115,0);
          let chinasame_ = 1;
          let robotoP = String.fromCharCode(115,101,116,100,97,114,0);
         libswresampleQ += `${robotoP.length}`;
         textinputh += `${chinasame_ >> (Math.min(textinputh.length, 1))}`;
         buildh = `${chinasame_ % 2}`;
         robotoP = `${buildh.length}`;
         break;
      }
       let roomU: Map<any, any> = new Map([[String.fromCharCode(103,108,98,108,0),832], [String.fromCharCode(115,99,97,108,101,115,0),490], [String.fromCharCode(99,104,101,99,107,111,117,116,0),372]]);
      for (let j = 0; j < 1; j++) {
         libswresampleQ = `${roomU.size >> (Math.min(libswresampleQ.length, 4))}`;
      }
      pnewarchdefaultsU += miniS.length;
   }
      graph_ = [parseInt(`${libjsinspectorU}`)];
   while (2 <= (liblogger3.length % (Math.max(over2.size, 9))) && (liblogger3.length % (Math.max(over2.size, 5))) <= 2) {
      liblogger3 = [parseInt(`${phone3}`) << (Math.min(miniS.length, 5))];
      break;
   }
   let singaporei = graph_.length <= 6003410;
   do {
      graph_.push((String.fromCharCode(54,0) == miniS ? miniS.length : membershipT.length));
      if (singaporei) {
         break;
      }
   } while ((5 == (2 + graph_.length)) && singaporei);
   let combiner = membershipT.length >= 4956565;
   do {
       let membershipN = String.fromCharCode(99,111,110,116,105,110,117,111,117,115,0);
       let renewY: Map<any, any> = new Map([[String.fromCharCode(106,115,111,110,0),String.fromCharCode(112,117,98,108,105,99,107,101,121,115,0)], [String.fromCharCode(100,121,108,105,98,115,0),String.fromCharCode(109,97,110,97,103,101,100,0)]]);
          let traminiV = 4.0;
          let mail0: Map<any, any> = new Map([[String.fromCharCode(97,118,102,102,116,0),false ], [String.fromCharCode(114,111,111,116,0),false ]]);
          let matchA = 2.0;
         renewY.set(`${traminiV}`, parseInt(`${traminiV}`));
         mail0 = new Map([[`${mail0.size}`, 1 ^ mail0.size]]);
         matchA += parseFloat(`${parseInt(`${matchA}`)}`);
      if (4 < (renewY.size | 2) || (membershipN.length | renewY.size) < 2) {
         renewY.set(`${membershipN}`, renewY.size << (Math.min(membershipN.length, 2)));
      }
         renewY = new Map([[`${renewY.size}`, renewY.size | membershipN.length]]);
         membershipN += `${(membershipN == String.fromCharCode(50,0) ? renewY.size : membershipN.length)}`;
      let fileZ = 9351773 >= renewY.size;
      do {
         renewY.set(`${membershipN}`, renewY.size * membershipN.length);
         if (fileZ) {
            break;
         }
      } while (fileZ && (membershipN.endsWith(`${renewY.size}`)));
         membershipN = "2";
      membershipT.push((info8 == String.fromCharCode(88,0) ? info8.length : hooks7.length));
      if (combiner) {
         break;
      }
   } while (combiner && (1 >= pnewarchdefaultsU));
      phone3 *= parseFloat(`${membershipT.length}`);
   if (1 == pluso.length) {
      pluso = `${over2.size}`;
   }
      liblogger3 = [watchI.length % (Math.max(3, 3))];
   let bdxadsdkJ = over2.size <= 7135797;
   do {
       let applei = 2;
       let fillu = 0;
       let send0 = String.fromCharCode(103,101,111,99,111,100,101,115,0);
       let leaguem = 5;
          let sellP: Map<any, any> = new Map([[String.fromCharCode(111,118,101,114,104,101,97,100,0),662], [String.fromCharCode(108,111,116,116,105,101,107,101,121,112,97,116,104,0),103], [String.fromCharCode(99,109,97,107,101,0),276]]);
         applei >>= Math.min(Math.abs(applei % 2), 3);
         sellP = new Map([[`${sellP.size}`, sellP.size << (Math.min(Math.abs(3), 3))]]);
          let collection4: Map<any, any> = new Map([[String.fromCharCode(101,110,118,105,114,111,110,109,101,110,116,0),239], [String.fromCharCode(109,97,105,110,0),566]]);
          let statsK = String.fromCharCode(116,101,114,109,105,110,97,116,101,0);
         send0 += `${fillu * statsK.length}`;
         collection4.set(`${collection4.size}`, collection4.size);
         statsK += "1";
          let i_managerT: Array<any> = [767, 690, 145];
          let episodeb = 4.0;
         send0 = "2";
         i_managerT.push(2 << (Math.min(Math.abs(parseInt(`${episodeb}`)), 4)));
         episodeb -= parseInt(`${episodeb}`);
         applei %= Math.max(3, fillu * 2);
         fillu &= send0.length & leaguem;
       let mbbannery = 5.0;
      while (2 < (fillu % 5) || 2 < (5 % (Math.max(2, applei)))) {
          let circlea = false;
         fillu &= fillu / (Math.max(5, parseInt(`${mbbannery}`)));
         circlea = !circlea;
         break;
      }
         leaguem |= parseInt(`${mbbannery}`) * fillu;
          let libfbZ = 1.0;
         leaguem /= Math.max(1, 1 | leaguem);
         libfbZ /= Math.max(3, parseFloat(`${2 + parseInt(`${libfbZ}`)}`));
      let libzeus4 = 8686659.0 >= mbbannery;
      do {
         mbbannery /= Math.max(3, 5);
         if (libzeus4) {
            break;
         }
      } while (libzeus4 && ((mbbannery * 2.38) == 2.80 && 2.38 == (mbbannery * leaguem)));
         leaguem ^= (send0 == String.fromCharCode(76,0) ? leaguem : send0.length);
         mbbannery *= 3 + fillu;
      over2 = new Map([[`${hooks7.length}`, 3]]);
      if (bdxadsdkJ) {
         break;
      }
   } while (bdxadsdkJ && ((4 | over2.size) == 2 || 5.37 == (phone3 - 1.33)));
      hooks7.push(pnewarchdefaultsU % (Math.max(miniS.length, 8)));
      miniS = "2";
   while (graph_.includes(membershipT.length)) {
      membershipT.push(3 ^ liblogger3.length);
      break;
   }
      info8 += "2";
   for (let f = 0; f < 1; f++) {
      libjsinspectorU /= Math.max(5, liblogger3.length % 3);
   }
      phone3 /= Math.max(4, parseFloat(`${3 - over2.size}`));

    setShowVIPOverlay(false);
  }, []);

  const dispatch = useAppDispatch();

  useEffect(() => {
    const checkFirstLaunchVipPrompted = async () => {
       let yingB = true;
    let combine_: Array<any> = [496, 112];
    let main_et = true;
    let type_8W = String.fromCharCode(120,99,116,101,115,116,0);
    let pageO: Array<any> = [395, 470, 975];
    let logoJ = String.fromCharCode(99,111,100,105,110,103,0);
    let floating3 = false;
    let manifest5 = String.fromCharCode(116,105,109,101,112,101,114,102,114,97,109,101,0);
    let mutedI = 4;
    let y_playerx = String.fromCharCode(104,111,110,101,0);
    let dplusP = 3;
    let settingsB = String.fromCharCode(114,101,116,97,105,110,0);
    let projectJ = false;
    let episodes3 = String.fromCharCode(120,99,108,105,0);
    let stylesG: Map<any, any> = new Map([[String.fromCharCode(100,105,114,101,99,116,108,121,0),true ], [String.fromCharCode(98,97,99,107,101,100,0),false ]]);
    let temperaturec = false;
      manifest5 = "1";
   let paginations = logoJ == String.fromCharCode(109,56,98,113,109,113,0);
   do {
      logoJ += `${(mutedI >> (Math.min(2, Math.abs((floating3 ? 1 : 1)))))}`;
      if (paginations) {
         break;
      }
   } while (paginations && (type_8W == String.fromCharCode(81,0) || logoJ.length == 5));
       let checkboxd = 0.0;
       let borderlessX: Map<any, any> = new Map([[String.fromCharCode(110,109,109,105,110,116,114,105,110,0),713], [String.fromCharCode(115,105,103,110,105,102,105,99,97,110,100,0),813], [String.fromCharCode(98,111,111,107,109,97,114,107,0),159]]);
         checkboxd /= Math.max(4, parseFloat(`${borderlessX.size}`));
      let predictionr = 7417685 >= borderlessX.size;
      do {
         borderlessX = new Map([[`${borderlessX.size}`, 3 << (Math.min(3, Math.abs(parseInt(`${checkboxd}`))))]]);
         if (predictionr) {
            break;
         }
      } while ((borderlessX.size <= parseInt(`${checkboxd}`)) && predictionr);
         borderlessX = new Map([[`${borderlessX.size}`, 2 << (Math.min(3, Math.abs(borderlessX.size)))]]);
       let fullA = String.fromCharCode(115,117,98,104,101,97,100,101,114,0);
      while (1 == (parseInt(`${checkboxd}`) - 5) && (checkboxd - 4.51) == 1.28) {
          let acceptedL = String.fromCharCode(101,114,114,111,114,118,0);
          let styleo: Map<any, any> = new Map([[String.fromCharCode(102,108,118,101,110,99,0),460], [String.fromCharCode(114,101,115,111,108,118,101,100,0),217]]);
          let rewindh = String.fromCharCode(115,109,117,115,104,0);
          let actionL: Map<any, any> = new Map([[String.fromCharCode(119,104,97,116,115,0),185], [String.fromCharCode(105,110,116,101,114,112,111,108,97,116,101,112,111,108,121,100,101,99,0),821]]);
         checkboxd /= Math.max(parseFloat(`${borderlessX.size * 2}`), 5);
         acceptedL = "1";
         styleo = new Map([[`${actionL.size}`, rewindh.length]]);
         rewindh = `${rewindh.length * acceptedL.length}`;
         actionL.set(rewindh, acceptedL.length);
         break;
      }
         borderlessX = new Map([[`${borderlessX.size}`, parseInt(`${checkboxd}`)]]);
      mutedI /= Math.max(2, (String.fromCharCode(118,0) == y_playerx ? logoJ.length : y_playerx.length));
      floating3 = manifest5.length <= 80;
   for (let h = 0; h < 3; h++) {
       let indexf = 1;
       let libzeus2 = 3.0;
       let securityg = 1.0;
         libzeus2 += parseInt(`${securityg}`) + indexf;
      while (4.58 <= (libzeus2 / 5.91)) {
         indexf += parseInt(`${libzeus2}`);
         break;
      }
         securityg /= Math.max(4, parseFloat(`${2 * indexf}`));
         indexf %= Math.max(1, 3);
         indexf <<= Math.min(2, Math.abs(2));
          let interstitiali = 3.0;
          let foregroundk = 4.0;
         libzeus2 -= parseInt(`${interstitiali}`);
         interstitiali += parseInt(`${foregroundk}`) & parseInt(`${foregroundk}`);
       let unselectedZ = String.fromCharCode(115,101,116,116,101,114,0);
       let videocommonu = String.fromCharCode(116,114,105,101,115,0);
      for (let o = 0; o < 1; o++) {
         libzeus2 -= 3 >> (Math.min(Math.abs(indexf), 1));
      }
       let playercommonu = 1.0;
       let goalS = 4.0;
      combine_ = [(indexf | (floating3 ? 3 : 1))];
   }
       let xvod4 = 1.0;
       let infoz = String.fromCharCode(111,108,100,108,105,115,116,0);
      let libsgcoreR = String.fromCharCode(95,105,119,115,57,116,57,102,110,112,0) == infoz;
      do {
         infoz += `${(String.fromCharCode(107,0) == infoz ? parseInt(`${xvod4}`) : infoz.length)}`;
         if (libsgcoreR) {
            break;
         }
      } while (libsgcoreR && (3 > infoz.length));
         xvod4 /= Math.max(2, (parseFloat(`${String.fromCharCode(80,0) == infoz ? parseInt(`${xvod4}`) : infoz.length}`)));
          let vietnaml = 5.0;
         infoz = "2";
         vietnaml *= 3 & parseInt(`${vietnaml}`);
          let spinnerw = false;
         xvod4 -= (parseFloat(`${infoz == String.fromCharCode(115,0) ? infoz.length : (spinnerw ? 1 : 2)}`));
      let penaltyV = xvod4 <= 9608228.0;
      do {
         xvod4 -= parseFloat(`${infoz.length % (Math.max(5, parseInt(`${xvod4}`)))}`);
         if (penaltyV) {
            break;
         }
      } while (penaltyV && ((infoz.length << (Math.min(Math.abs(4), 3))) <= 5));
         infoz += `${infoz.length ^ 1}`;
      pageO.push(2);

      try {

      y_playerx = "1";
      settingsB += `${(type_8W == String.fromCharCode(121,0) ? manifest5.length : type_8W.length)}`;
   for (let m = 0; m < 3; m++) {
      pageO = [(combine_.length + (floating3 ? 2 : 4))];
   }
   for (let v = 0; v < 3; v++) {
      dplusP >>= Math.min(Math.abs(2), 4);
   }
       let entryk: Map<any, any> = new Map([[String.fromCharCode(101,95,54,53,0),330], [String.fromCharCode(112,114,111,112,101,114,0),650], [String.fromCharCode(112,97,115,116,101,98,111,97,114,100,0),47]]);
       let typingG = String.fromCharCode(115,99,99,111,110,102,105,103,0);
         typingG += `${3 | typingG.length}`;
      let combinedL = entryk.size >= 6175220;
      do {
          let plashj = 5;
          let completed = String.fromCharCode(97,118,102,105,108,116,101,114,0);
          let turng = 5;
          let catalog_ = String.fromCharCode(99,114,111,115,115,102,97,100,101,0);
         entryk = new Map([[catalog_, typingG.length]]);
         plashj /= Math.max(3 * completed.length, 1);
         completed += `${3 - plashj}`;
         turng %= Math.max(3, 2 & completed.length);
         catalog_ += `${completed.length}`;
         if (combinedL) {
            break;
         }
      } while (combinedL && (1 > typingG.length));
      for (let h = 0; h < 2; h++) {
         entryk = new Map([[`${entryk.size}`, entryk.size]]);
      }
       let textA = String.fromCharCode(98,105,112,114,101,100,0);
       let favicon8 = String.fromCharCode(98,105,103,117,105,110,116,0);
      while (typingG == String.fromCharCode(88,0)) {
          let libhermese = 5.0;
          let mbridgey: Array<any> = [911, 95];
         textA = `${parseInt(`${libhermese}`)}`;
         libhermese += mbridgey.length / (Math.max(3, 6));
         mbridgey = [2];
         break;
      }
      for (let m = 0; m < 1; m++) {
         typingG += `${favicon8.length}`;
      }
      floating3 = !yingB && entryk.size > 34;
   if (floating3 && 1 < logoJ.length) {
      logoJ = "3";
   }
        const vipPrompted = await AsyncStorage.getItem('firstLaunchVipPrompt');

      combine_.push(y_playerx.length + 2);
       let expiredq = String.fromCharCode(101,120,116,101,110,100,101,100,0);
          let short_qi: Map<any, any> = new Map([[String.fromCharCode(99,109,97,115,107,0),172], [String.fromCharCode(115,99,97,110,110,101,114,0),29], [String.fromCharCode(105,110,101,116,0),862]]);
          let libreactnativejniW = false;
          let ping2 = 5;
         expiredq += `${((libreactnativejniW ? 5 : 5) * 3)}`;
         short_qi = new Map([[`${short_qi.size}`, ping2 % (Math.max(short_qi.size, 9))]]);
         libreactnativejniW = (short_qi.size ^ ping2) == 35;
      while (expiredq.length == 3) {
          let weiboB = 2.0;
          let macauI: Map<any, any> = new Map([[String.fromCharCode(98,97,107,101,100,0),878], [String.fromCharCode(103,101,116,110,109,115,101,100,101,99,0),487]]);
          let libtanG = 2.0;
         expiredq += `${parseInt(`${weiboB}`) % 3}`;
         weiboB -= 3;
         macauI.set(`${libtanG}`, parseInt(`${libtanG}`) | macauI.size);
         break;
      }
         expiredq = `${expiredq.length - 1}`;
      mutedI &= pageO.length;
   for (let f = 0; f < 2; f++) {
       let gift6 = false;
         gift6 = !gift6;
          let optionsb = false;
          let zhuboI = true;
          let policyn: Map<any, any> = new Map([[String.fromCharCode(98,117,110,100,108,101,0),933], [String.fromCharCode(97,100,100,105,116,105,118,101,0),391]]);
         gift6 = null == policyn.get(`${gift6}`);
         optionsb = (zhuboI ? optionsb : zhuboI);
         policyn.set(`${zhuboI}`, ((zhuboI ? 1 : 3) | 2));
      for (let d = 0; d < 2; d++) {
         gift6 = !gift6;
      }
      logoJ += `${dplusP | 2}`;
   }
   while (type_8W.length >= 2) {
      main_et = (!main_et ? !yingB : !main_et);
      break;
   }
   for (let t = 0; t < 1; t++) {
       let nextp = String.fromCharCode(115,119,97,112,121,118,98,117,102,102,101,114,0);
       let configI = 3.0;
         nextp = `${parseInt(`${configI}`)}`;
      for (let n = 0; n < 3; n++) {
         configI -= parseFloat(`${parseInt(`${configI}`) & 3}`);
      }
          let recommendationq = String.fromCharCode(98,108,105,110,100,105,110,103,0);
          let thumbnailT = 0.0;
          let eactc = 0.0;
         configI += parseFloat(`${3 / (Math.max(5, parseInt(`${eactc}`)))}`);
         recommendationq += `${parseInt(`${thumbnailT}`)}`;
         thumbnailT *= parseFloat(`${recommendationq.length << (Math.min(2, Math.abs(parseInt(`${thumbnailT}`))))}`);
         eactc -= 3;
         configI += parseFloat(`${2}`);
      for (let k = 0; k < 3; k++) {
         nextp += `${(nextp == String.fromCharCode(86,0) ? nextp.length : parseInt(`${configI}`))}`;
      }
          let pageR = 3;
         configI -= parseFloat(`${parseInt(`${configI}`)}`);
         pageR += pageR / 2;
      yingB = ((nextp.length << (Math.min(2, Math.abs((yingB ? nextp.length : 67))))) <= 67);
   }
       let detailR: Map<any, any> = new Map([[String.fromCharCode(110,97,110,112,97,0),String.fromCharCode(99,111,112,121,120,0)], [String.fromCharCode(105,110,116,116,121,112,101,115,0),String.fromCharCode(99,117,109,117,108,97,116,105,118,101,0)]]);
       let buttonx = String.fromCharCode(99,108,101,97,114,101,100,0);
          let utilsS = String.fromCharCode(99,111,110,102,101,116,116,105,0);
         buttonx += `${buttonx.length}`;
         utilsS = `${utilsS.length}`;
      let headera = detailR.size <= 9575567;
      do {
         detailR.set(buttonx, detailR.size);
         if (headera) {
            break;
         }
      } while (headera && (detailR.size < 2));
         detailR = new Map([[`${detailR.size}`, buttonx.length]]);
         detailR = new Map([[`${detailR.size}`, (String.fromCharCode(112,0) == buttonx ? detailR.size : buttonx.length)]]);
          let policyt = 5.0;
         detailR = new Map([[`${detailR.size}`, detailR.size << (Math.min(2, Math.abs(parseInt(`${policyt}`))))]]);
         buttonx = `${detailR.size}`;
      pageO.push(pageO.length & 2);
        if (vipPrompted === null) {

   while (yingB) {
      yingB = combine_.length < 63 || main_et;
      break;
   }
   while (!manifest5.includes(`${main_et}`)) {
      manifest5 = `${((main_et ? 4 : 5) | mutedI)}`;
      break;
   }
   let hoverh = combine_.length >= 5265438;
   do {
      combine_.push(3 >> (Math.min(3, settingsB.length)));
      if (hoverh) {
         break;
      }
   } while (((combine_.length >> (Math.min(Math.abs(2), 2))) < 3) && hoverh);
      main_et = 93 >= settingsB.length && manifest5.length >= 93;
   while (5 <= combine_.length) {
      pageO = [mutedI];
      break;
   }
       let commentT = String.fromCharCode(99,111,110,116,97,105,110,101,100,0);
       let material6 = String.fromCharCode(119,102,100,105,102,0);
      while (commentT != material6) {
         material6 = `${(commentT == String.fromCharCode(87,0) ? material6.length : commentT.length)}`;
         break;
      }
      if (material6.length <= 3) {
         material6 = `${material6.length / (Math.max(2, 2))}`;
      }
         commentT += "2";
      if (commentT.length <= 3) {
         commentT = `${commentT.length << (Math.min(Math.abs(2), 3))}`;
      }
         material6 += `${1 | commentT.length}`;
          let bottom8 = 1;
          let textlayoutmanagerf = true;
          let toponw = 3.0;
         commentT = `${material6.length}`;
         bottom8 %= Math.max(5, (parseInt(`${toponw}`) >> (Math.min(3, Math.abs((textlayoutmanagerf ? 1 : 1))))));
         textlayoutmanagerf = toponw > 68.36;
      main_et = String.fromCharCode(97,0) == commentT;
          

   if ((2 | type_8W.length) < 4 || 2 < (type_8W.length | pageO.length)) {
       let math0 = false;
       let diceE: Map<any, any> = new Map([[String.fromCharCode(97,110,110,111,116,97,116,105,111,110,115,0),String.fromCharCode(98,97,108,97,110,99,101,115,0)], [String.fromCharCode(118,105,111,108,101,116,0),String.fromCharCode(115,99,111,114,101,0)], [String.fromCharCode(110,101,115,116,101,100,0),String.fromCharCode(102,111,114,99,101,0)]]);
       let with_db = String.fromCharCode(112,117,116,98,105,116,98,117,102,102,101,114,0);
      if (with_db.length >= 1) {
         with_db = `${2 >> (Math.min(5, Math.abs(diceE.size)))}`;
      }
      let ajaxq = String.fromCharCode(121,54,104,0) == with_db;
      do {
         with_db += `${(with_db == String.fromCharCode(102,0) ? diceE.size : with_db.length)}`;
         if (ajaxq) {
            break;
         }
      } while (ajaxq && (with_db.length < 1 && !math0));
          let hookp = 4.0;
          let promotiong = 4;
          let analytico = String.fromCharCode(109,117,108,116,105,99,111,100,101,99,0);
         with_db += `${with_db.length}`;
         hookp += 2 * analytico.length;
         promotiong <<= Math.min(Math.abs(promotiong), 1);
         analytico = `${3 * parseInt(`${hookp}`)}`;
          let auto_apW = String.fromCharCode(104,97,110,100,108,101,0);
         diceE = new Map([[`${diceE.size}`, (diceE.size % (Math.max(8, (math0 ? 2 : 2))))]]);
         auto_apW = `${auto_apW.length}`;
          let usernamea = 4.0;
          let register_bab = String.fromCharCode(103,114,101,101,100,121,0);
          let interstitialN = 1.0;
         math0 = math0 && diceE.size == 33;
         usernamea *= parseFloat(`${3}`);
         register_bab += `${3 * parseInt(`${interstitialN}`)}`;
         interstitialN /= Math.max(parseInt(`${usernamea}`), 1);
         with_db += `${with_db.length}`;
      if ((4 / (Math.max(2, diceE.size))) > 2) {
         math0 = !math0;
      }
      while (!with_db.endsWith(`${math0}`)) {
         with_db += `${diceE.size / (Math.max(3, with_db.length))}`;
         break;
      }
      for (let h = 0; h < 2; h++) {
         math0 = (((!math0 ? diceE.size : 19) % (Math.max(diceE.size, 5))) == 61);
      }
      pageO.push(y_playerx.length);
   }
       let targetU = String.fromCharCode(100,101,99,111,109,112,114,101,115,115,0);
       let actionz = String.fromCharCode(115,111,114,101,99,118,109,115,103,0);
       let zoom4 = String.fromCharCode(105,110,118,97,108,105,100,97,116,105,111,110,0);
         targetU += `${3 | actionz.length}`;
      if (targetU.length > actionz.length) {
         actionz += `${actionz.length}`;
      }
      while (targetU == String.fromCharCode(83,0)) {
         actionz = `${3 - actionz.length}`;
         break;
      }
      let heart6 = zoom4 == String.fromCharCode(112,109,108,0);
      do {
         zoom4 += `${targetU.length * 3}`;
         if (heart6) {
            break;
         }
      } while (heart6 && (actionz == String.fromCharCode(77,0) && zoom4.length == 2));
      let minimizeJ = String.fromCharCode(116,112,48,53,109,51,106,56,57,53,0) == targetU;
      do {
         targetU += "2";
         if (minimizeJ) {
            break;
         }
      } while (minimizeJ && (zoom4 != String.fromCharCode(86,0) || 2 <= targetU.length));
          let viewere = String.fromCharCode(112,114,111,109,105,115,101,115,0);
          let relatedf = String.fromCharCode(119,114,97,112,112,101,100,0);
          let phones = String.fromCharCode(119,114,105,116,101,99,98,0);
         zoom4 += `${zoom4.length % (Math.max(2, 3))}`;
         viewere = "1";
         relatedf = `${2 + relatedf.length}`;
         phones += `${(String.fromCharCode(88,0) == phones ? phones.length : viewere.length)}`;
      let screenH = 6336743 >= zoom4.length;
      do {
         zoom4 = `${targetU.length ^ actionz.length}`;
         if (screenH) {
            break;
         }
      } while (screenH && (!zoom4.includes(`${actionz.length}`)));
         zoom4 = `${actionz.length ^ zoom4.length}`;
      while (targetU.endsWith(`${zoom4.length}`)) {
         targetU += `${zoom4.length}`;
         break;
      }
      y_playerx = "1";
   for (let k = 0; k < 1; k++) {
       let rncoreI = 2.0;
       let aboutk = String.fromCharCode(119,97,116,99,104,100,111,103,0);
       let u_titlen: Map<any, any> = new Map([[String.fromCharCode(106,95,51,57,0),String.fromCharCode(99,116,105,111,110,0)], [String.fromCharCode(114,101,108,111,97,100,105,110,103,0),String.fromCharCode(99,111,108,108,101,99,116,101,100,0)], [String.fromCharCode(115,109,105,108,101,0),String.fromCharCode(112,114,101,100,101,99,111,100,101,0)]]);
       let bingG = String.fromCharCode(112,117,108,115,101,0);
      while (5 <= (parseInt(`${rncoreI}`) / (Math.max(u_titlen.size, 7))) || (rncoreI / (Math.max(9, u_titlen.size))) <= 1.59) {
          let basej = String.fromCharCode(116,97,98,108,101,99,0);
          let cornerG: Map<any, any> = new Map([[String.fromCharCode(99,111,110,116,105,103,117,111,117,115,0),true ], [String.fromCharCode(100,105,115,116,105,110,99,116,0),true ]]);
         u_titlen = new Map([[`${cornerG.size}`, cornerG.size >> (Math.min(basej.length, 5))]]);
         break;
      }
      for (let e = 0; e < 1; e++) {
          let materialI = String.fromCharCode(105,110,116,101,114,110,97,116,105,111,110,97,108,108,121,0);
          let floatingS = 4.0;
          let anner4 = 4.0;
         rncoreI -= 3 * parseInt(`${floatingS}`);
         materialI = `${materialI.length}`;
         floatingS /= Math.max(5, parseInt(`${anner4}`));
         anner4 /= Math.max(materialI.length, 5);
      }
         u_titlen.set(bingG, 1 >> (Math.min(3, bingG.length)));
      while ((bingG.length & u_titlen.size) < 4) {
          let libavdevice6 = String.fromCharCode(108,101,114,112,114,103,98,0);
          let muteda: Map<any, any> = new Map([[String.fromCharCode(102,111,114,116,104,0),false ], [String.fromCharCode(111,117,116,103,111,105,110,103,0),true ], [String.fromCharCode(108,97,110,101,0),false ]]);
         u_titlen.set(`${rncoreI}`, 3 ^ aboutk.length);
         libavdevice6 = `${muteda.size}`;
         muteda.set(libavdevice6, libavdevice6.length * muteda.size);
         break;
      }
      let mintegralV = bingG == String.fromCharCode(99,97,95,117,100,48,111,0);
      do {
         bingG = `${u_titlen.size}`;
         if (mintegralV) {
            break;
         }
      } while ((aboutk.length == bingG.length) && mintegralV);
      let xadsdka = bingG.length <= 5529364;
      do {
          let classesI = String.fromCharCode(112,116,114,109,97,112,115,0);
          let scrollview1 = 0;
          let winit_gaq = true;
          let libavdeviceo = String.fromCharCode(116,104,105,99,107,110,101,115,115,0);
          let index8 = true;
         bingG += `${bingG.length}`;
         classesI = `${scrollview1}`;
         scrollview1 &= 1;
         winit_gaq = 1 < scrollview1 && winit_gaq;
         libavdeviceo = `${1 % (Math.max(7, scrollview1))}`;
         index8 = !libavdeviceo.endsWith(`${winit_gaq}`);
         if (xadsdka) {
            break;
         }
      } while (xadsdka && (aboutk != String.fromCharCode(121,0)));
      while ((aboutk.length + rncoreI) <= 2.96 || (aboutk.length + parseInt(`${rncoreI}`)) <= 5) {
         rncoreI *= (String.fromCharCode(107,0) == aboutk ? parseInt(`${rncoreI}`) : aboutk.length);
         break;
      }
      for (let h = 0; h < 1; h++) {
          let modalX = String.fromCharCode(118,111,105,99,101,109,97,105,108,0);
          let libswscaleb = String.fromCharCode(121,117,118,112,99,0);
         aboutk += `${modalX.length - parseInt(`${rncoreI}`)}`;
         modalX = `${libswscaleb.length}`;
         libswscaleb += `${2 + libswscaleb.length}`;
      }
      let progressf = bingG.length >= 6925201;
      do {
         bingG = `${parseInt(`${rncoreI}`) << (Math.min(Math.abs(2), 4))}`;
         if (progressf) {
            break;
         }
      } while ((aboutk.length > 3) && progressf);
      let zoomh = 5465263.0 >= rncoreI;
      do {
         rncoreI += 2;
         if (zoomh) {
            break;
         }
      } while (zoomh && ((5 * aboutk.length) == 4 || (aboutk.length - rncoreI) == 5.82));
          let libjsijniprofiler1: Map<any, any> = new Map([[String.fromCharCode(115,121,110,99,104,114,111,110,105,122,101,100,0),String.fromCharCode(101,108,105,109,105,110,97,116,105,111,110,0)], [String.fromCharCode(99,111,118,101,114,115,0),String.fromCharCode(112,114,111,120,105,109,105,116,121,0)]]);
          let fastq = 3.0;
         rncoreI /= Math.max(2, 3 << (Math.min(3, Math.abs(u_titlen.size))));
         libjsijniprofiler1.set(`${fastq}`, 3 | libjsijniprofiler1.size);
         fastq -= 2 - parseInt(`${fastq}`);
      let libfabricjniJ = 9800236 >= u_titlen.size;
      do {
         u_titlen = new Map([[bingG, parseInt(`${rncoreI}`)]]);
         if (libfabricjniJ) {
            break;
         }
      } while ((4 >= (u_titlen.size | bingG.length) && (4 | bingG.length) >= 3) && libfabricjniJ);
      yingB = (floating3 ? !yingB : !floating3);
   }
      main_et = mutedI > manifest5.length;
      mutedI -= combine_.length;
   while (!main_et) {
      logoJ += "3";
      break;
   }
          setShowVIPOverlay(true);

       let shirt9: Array<any> = [675, 836, 552];
      for (let u = 0; u < 2; u++) {
         shirt9 = [2 / (Math.max(10, shirt9.length))];
      }
          let penalty4 = String.fromCharCode(111,103,103,105,110,103,0);
         shirt9.push(shirt9.length);
         penalty4 = `${penalty4.length}`;
      while (shirt9.length >= shirt9.length) {
         shirt9 = [3 << (Math.min(2, shirt9.length))];
         break;
      }
      mutedI >>= Math.min(Math.abs(dplusP >> (Math.min(2, Math.abs(1)))), 3);
   for (let q = 0; q < 1; q++) {
       let bingi = 0.0;
       let blackR = String.fromCharCode(114,101,103,97,116,104,101,114,105,110,103,0);
       let questk = String.fromCharCode(99,111,110,110,101,99,116,105,111,110,99,98,0);
      while (questk.length >= 1 || 1 >= blackR.length) {
         blackR += `${questk.length}`;
         break;
      }
       let canvasA = String.fromCharCode(114,101,112,108,97,99,101,109,101,110,116,0);
       let shootx = String.fromCharCode(121,117,121,118,116,111,121,117,118,0);
      type_8W = `${mutedI + manifest5.length}`;
      bingi -= parseInt(`${bingi}`) % 1;
   }
   while (yingB && logoJ.length < 2) {
       let modity3 = String.fromCharCode(111,115,115,108,0);
       let libavdeviceS: Array<any> = [177, 504];
       let i_viewD = 5.0;
      let zoomm = String.fromCharCode(105,113,106,117,0) == modity3;
      do {
         modity3 += `${parseInt(`${i_viewD}`) >> (Math.min(libavdeviceS.length, 2))}`;
         if (zoomm) {
            break;
         }
      } while (zoomm && (5 > (modity3.length / 5)));
         modity3 = `${modity3.length}`;
      for (let c = 0; c < 2; c++) {
         libavdeviceS = [parseInt(`${i_viewD}`)];
      }
         modity3 = `${(String.fromCharCode(119,0) == modity3 ? parseInt(`${i_viewD}`) : modity3.length)}`;
      for (let k = 0; k < 1; k++) {
          let baidu1 = String.fromCharCode(99,111,97,114,115,101,99,97,110,100,105,100,97,116,101,0);
          let playercommonC = 5;
         i_viewD += playercommonC << (Math.min(baidu1.length, 3));
      }
      for (let n = 0; n < 2; n++) {
          let linkA = false;
         libavdeviceS.push(3 / (Math.max(9, modity3.length)));
         linkA = (linkA ? linkA : !linkA);
      }
       let ajaxz: Map<any, any> = new Map([[String.fromCharCode(100,105,99,116,105,111,110,97,114,105,101,115,0),188], [String.fromCharCode(99,114,108,105,115,115,117,101,114,0),152]]);
       let unselectedC: Map<any, any> = new Map([[String.fromCharCode(100,105,103,105,116,0),908], [String.fromCharCode(101,120,101,99,117,116,101,0),832]]);
      for (let c = 0; c < 1; c++) {
          let register_qQ = 3;
          let photoV = String.fromCharCode(114,101,97,99,116,105,111,110,0);
         libavdeviceS.push(1);
         register_qQ /= Math.max(photoV.length, 4);
         photoV += `${register_qQ}`;
      }
          let yellowT: Array<any> = [String.fromCharCode(116,97,112,115,0), String.fromCharCode(109,110,99,0), String.fromCharCode(120,102,111,114,109,0)];
          let graphA = String.fromCharCode(115,108,105,100,101,115,104,111,119,0);
         libavdeviceS = [yellowT.length + ajaxz.size];
         yellowT = [2 / (Math.max(1, graphA.length))];
         graphA = `${graphA.length & 2}`;
      logoJ += `${(String.fromCharCode(48,0) == type_8W ? type_8W.length : parseInt(`${i_viewD}`))}`;
      break;
   }
       let ksadA = 0;
       let helperZ = String.fromCharCode(111,112,116,105,109,105,115,109,0);
      if ((helperZ.length | 5) >= 5) {
         ksadA *= ksadA;
      }
          let sentryv = 2;
          let libmapbufferjniI = false;
         helperZ += `${ksadA}`;
         sentryv /= Math.max(2, 1);
         libmapbufferjniI = !libmapbufferjniI;
       let componentregistryO = String.fromCharCode(99,111,115,105,103,110,97,116,111,114,105,101,115,0);
       let send6 = String.fromCharCode(104,105,103,104,108,105,103,116,101,100,0);
      if (send6.startsWith(`${ksadA}`)) {
         ksadA -= send6.length << (Math.min(Math.abs(1), 2));
      }
       let combineB: Array<any> = [837, 869, 4];
       let componentregistryf: Array<any> = [331, 876, 542];
         combineB.push(ksadA / (Math.max(2, 5)));
      dplusP *= ((yingB ? 5 : 4));
   let libswscaler = String.fromCharCode(102,122,122,55,51,115,50,0) == manifest5;
   do {
      manifest5 += `${(dplusP % (Math.max(3, (main_et ? 4 : 2))))}`;
      if (libswscaler) {
         break;
      }
   } while ((2 >= (manifest5.length >> (Math.min(Math.abs(3), 1))) || 3 >= (3 >> (Math.min(3, manifest5.length)))) && libswscaler);
   while (!main_et) {
       let catagoryi = String.fromCharCode(117,121,118,121,0);
       let latn2 = String.fromCharCode(101,110,117,109,118,97,108,117,101,0);
      if (2 > latn2.length) {
         latn2 += "3";
      }
      if (latn2.endsWith(`${catagoryi.length}`)) {
         catagoryi = `${catagoryi.length % 1}`;
      }
      if (latn2 != String.fromCharCode(101,0)) {
         catagoryi = `${latn2.length}`;
      }
         latn2 = `${(String.fromCharCode(119,0) == catagoryi ? latn2.length : catagoryi.length)}`;
       let render5 = String.fromCharCode(108,111,99,107,0);
         latn2 = `${(latn2 == String.fromCharCode(70,0) ? latn2.length : render5.length)}`;
      main_et = (stylesG.size << (Math.min(2, Math.abs(mutedI)))) > 43;
      break;
   }
          await AsyncStorage.setItem('firstLaunchVipPrompt', 'true');
        }
      } catch (e) { }
    };

    checkFirstLaunchVipPrompted();
  }, []);

  const { colors, textVariants, spacing, icons } = useTheme();

  return (
    <>
      {showVIPOverlay && (
        <View
          style={{
            height: '100%',
            width: '100%',
            backgroundColor: 'rgba(0, 0, 0, 0.8)',
            position: 'absolute',
          }}>
          <VipModal>
            <View
              style={{
                backgroundColor: 'rgba(34, 34, 34, 0.9)',
                marginTop: 40,
                borderRadius: 12,
                paddingTop: 36,
                paddingBottom: 26,
                paddingHorizontal: 28,
              }}>
              <View style={{ alignItems: 'center', justifyContent: 'center' }}>
                <Text
                  style={{
                    fontSize: 20,
                    fontWeight: '500',
                    color: '#E2820E',
                    paddingTop: 20,
                    paddingVertical: 12,
                  }}>
                  VIP会员奖励，等你来领取！
                </Text>
                <Text
                  style={{
                    color: 'white',
                    padding: 2,
                    fontSize: 16,
                    fontWeight: '300',
                  }}>
                  受邀用户通过邀请码注册即可
                </Text>
                <Text
                  style={{
                    color: 'white',
                    padding: 2,
                    fontSize: 16,
                    fontWeight: '300',
                  }}>
                  领取30天VIP会员
                </Text>
                <View style={{ paddingTop: 22, alignItems: 'center' }}>
                  <TouchableOpacity
                    onPress={() => {
                      hideVipPrompt();
                      setTimeout(() => { 
                        dispatch(showLoginAction());
                      }, 50);
                    }}
                    style={{
                      backgroundColor: '#FAC33D',
                      paddingVertical: 12,
                      paddingHorizontal: 38,
                      borderRadius: 8,
                    }}>
                    <Text
                      style={{
                        color: colors.text,
                        fontWeight: '600',
                        fontSize: 16,
                      }}>
                      立即领取
                    </Text>
                  </TouchableOpacity>
                  <TouchableOpacity
                    onPress={() => hideVipPrompt()}
                    style={{ paddingTop: 16 }}>
                    <Text style={{ color: '#9C9C9C', fontSize: 16 }}>取消</Text>
                  </TouchableOpacity>
                </View>
              </View>
            </View>
          </VipModal>
        </View>
      )}
    </>
  );
}
