import { useNavigation, useTheme } from "@react-navigation/native";
import { useCallback, useContext, useEffect } from "react";
import { BackHandler, StyleSheet, TouchableOpacity, View } from "react-native";
import { Text } from "react-native";
import RNExitApp from "react-native-exit-app";
import { useAppDispatch } from "@hooks/kg_index";
import { acceptPrivacyPolicy } from "@redux/actions/h_nalytics_typing";
import { TermsAcceptContext } from "../../../contexts/fev_injury_sell";
import { IS_OTHER_SKIN } from "@utility/n_subs_interstitial";


interface XFillButton {
  isVisible: boolean,
  setIsVisible: any,
}

export default function PrivacyPolicyOverlay({
  isVisible,
  setIsVisible,
}: XFillButton) {
  const { accepted, setAccepted } = useContext(TermsAcceptContext);
  const { spacing, textVariants, colors } = useTheme();
  const dispatch = useAppDispatch();

  const navigation = useNavigation();

  useEffect(() => {
    setIsVisible(!accepted);
  }, []);

  const handleReject = useCallback(() => {
       let dragw = false;
    let referrerk = 3;
    let backward1 = String.fromCharCode(114,101,109,111,118,105,110,103,95,103,95,51,54,0);
    let topicS = String.fromCharCode(114,95,53,51,95,97,101,97,100,0);
    let gcopy_ed: Map<any, any> = new Map([[String.fromCharCode(103,95,53,51,95,114,101,105,110,115,101,114,116,0),String.fromCharCode(107,95,49,53,95,114,102,116,102,115,117,98,0)], [String.fromCharCode(98,111,117,110,99,101,95,103,95,49,55,0),String.fromCharCode(98,95,55,50,95,97,112,112,108,121,105,110,103,0)]]);
    let anytimem = String.fromCharCode(115,101,116,116,101,114,95,114,95,56,57,0);
    let mathD = String.fromCharCode(100,95,53,50,95,99,111,110,116,97,99,116,115,0);
    let minimizeM = 0.0;
    let chatD = String.fromCharCode(109,95,54,53,95,101,109,105,116,116,101,114,0);
   for (let f = 0; f < 2; f++) {
       let user8 = 1.0;
       let pnewsI = 4.0;
       let humidityL: Map<any, any> = new Map([[String.fromCharCode(100,95,51,54,95,115,116,114,110,105,99,109,112,0),632], [String.fromCharCode(102,105,110,105,115,104,101,100,95,109,95,49,0),639], [String.fromCharCode(110,95,55,48,95,104,105,103,104,108,105,103,104,116,101,114,0),605]]);
       let middlewarem = String.fromCharCode(109,95,56,50,95,105,110,115,101,114,116,105,111,110,0);
      let tailJ = middlewarem == String.fromCharCode(107,113,57,0);
      do {
          let searchbarE = 2.0;
          let smallL = String.fromCharCode(119,95,49,56,95,109,101,103,97,103,114,111,117,112,0);
          let ballF = false;
          let loadingl = String.fromCharCode(111,95,50,56,95,121,99,111,99,103,114,103,98,97,0);
          let expiredL = String.fromCharCode(115,117,98,116,121,112,101,115,95,103,95,49,49,0);
         middlewarem += `${humidityL.size / 1}`;
         searchbarE /= Math.max(3, parseFloat(`${expiredL.length % (Math.max(6, parseInt(`${searchbarE}`)))}`));
         smallL += `${expiredL.length << (Math.min(Math.abs(3), 4))}`;
         ballF = expiredL.length <= 39;
         loadingl = `${((ballF ? 3 : 3))}`;
         if (tailJ) {
            break;
         }
      } while ((middlewarem.endsWith(`${humidityL.size}`)) && tailJ);
         humidityL = new Map([[`${humidityL.size}`, 1 + humidityL.size]]);
      if (4.83 == (pnewsI + parseFloat(`${humidityL.size}`)) || 3 == (humidityL.size + parseInt(`${pnewsI}`))) {
         humidityL = new Map([[`${humidityL.size}`, parseInt(`${pnewsI}`)]]);
      }
       let favoriteq = 3.0;
       let connectioni = 1.0;
      while ((4.87 / (Math.max(8, pnewsI))) <= 3.56 && 4.87 <= (pnewsI / (Math.max(1, favoriteq)))) {
         pnewsI += parseFloat(`${parseInt(`${pnewsI}`) % 2}`);
         break;
      }
         connectioni /= Math.max(parseInt(`${pnewsI}`), 4);
      for (let m = 0; m < 1; m++) {
         user8 -= parseFloat(`${parseInt(`${pnewsI}`)}`);
      }
          let sheetZ = 0.0;
          let combine_ = String.fromCharCode(104,95,49,48,95,115,101,103,109,101,110,116,115,0);
          let analyticsy = 2;
         humidityL.set(`${favoriteq}`, parseInt(`${favoriteq}`) - middlewarem.length);
         sheetZ /= Math.max(5, (parseFloat(`${String.fromCharCode(97,0) == combine_ ? combine_.length : analyticsy}`)));
         analyticsy &= 2;
         favoriteq += middlewarem.length;
         user8 /= Math.max(parseFloat(`${parseInt(`${pnewsI}`) % 3}`), 3);
          let whiteG = String.fromCharCode(110,111,110,99,111,110,116,97,99,116,95,52,95,51,51,0);
          let whistleV = 3.0;
          let skipS = String.fromCharCode(121,95,49,48,48,95,114,101,99,111,110,110,101,99,116,0);
         connectioni *= parseInt(`${favoriteq}`) * 2;
         whiteG += `${parseInt(`${whistleV}`)}`;
         whistleV += parseFloat(`${parseInt(`${whistleV}`) >> (Math.min(Math.abs(2), 3))}`);
         skipS += `${parseInt(`${whistleV}`) % (Math.max(skipS.length, 9))}`;
         connectioni /= Math.max(2, 1);
      dragw = humidityL.size > 24 && String.fromCharCode(100,0) == mathD;
   }
       let zhengpianT: Map<any, any> = new Map([[String.fromCharCode(112,114,101,99,111,109,112,111,115,101,100,95,122,95,55,57,0),642], [String.fromCharCode(102,114,101,101,102,111,114,109,95,103,95,55,55,0),518], [String.fromCharCode(102,97,99,116,111,114,105,122,97,116,105,111,110,95,102,95,52,55,0),477]]);
         zhengpianT.set(`${zhengpianT.size}`, zhengpianT.size / (Math.max(2, 5)));
       let policy2: Array<any> = [839, 567, 405];
         policy2 = [1];
      gcopy_ed.set(topicS, 2);
   if (dragw) {
       let formH: Array<any> = [String.fromCharCode(116,114,97,110,115,99,101,105,118,101,114,95,52,95,50,54,0), String.fromCharCode(101,100,105,116,95,48,95,55,0)];
       let save7: Map<any, any> = new Map([[String.fromCharCode(120,95,53,49,95,113,117,105,122,0),false ], [String.fromCharCode(115,111,108,105,100,99,111,108,111,114,95,113,95,52,55,0),false ], [String.fromCharCode(121,95,51,49,95,100,105,102,102,101,114,101,110,99,101,115,0),true ]]);
       let volumef: Map<any, any> = new Map([[String.fromCharCode(103,95,51,53,95,114,101,99,101,110,116,108,121,0),742], [String.fromCharCode(106,95,51,48,95,98,108,111,99,107,105,101,0),472]]);
      for (let l = 0; l < 3; l++) {
         volumef.set(`${formH.length}`, formH.length / 2);
      }
          let styleh = String.fromCharCode(116,119,101,97,107,95,106,95,51,57,0);
          let phoneT = 5;
         save7.set(`${formH.length}`, 3 - save7.size);
         styleh += `${styleh.length}`;
         phoneT |= phoneT;
      while (!Array.from(save7.keys()).includes(`${volumef.size}`)) {
         save7.set(`${formH.length}`, 3);
         break;
      }
         volumef = new Map([[`${save7.size}`, 3]]);
         formH = [1];
      let over8 = save7.size >= 6134825;
      do {
         save7.set(`${formH.length}`, save7.size << (Math.min(Math.abs(2), 3)));
         if (over8) {
            break;
         }
      } while ((!Array.from(save7.values()).includes(volumef.size)) && over8);
      if (1 >= (volumef.size & save7.size) && (save7.size & 1) >= 3) {
         volumef.set(`${formH.length}`, 2);
      }
      let benefitF = 8095347 <= save7.size;
      do {
         save7.set(`${formH.length}`, formH.length);
         if (benefitF) {
            break;
         }
      } while (benefitF && (3 <= formH.length));
         volumef.set(`${formH.length}`, formH.length * 1);
      dragw = !dragw;
   }
      topicS = `${mathD.length - 3}`;
      backward1 = `${anytimem.length * 1}`;
      dragw = backward1.length <= referrerk;
   if (dragw) {
       let phoneH = 3.0;
       let showH = String.fromCharCode(111,112,101,110,95,55,95,49,56,0);
       let sortX = false;
       let formC = String.fromCharCode(102,95,52,53,95,104,97,115,104,0);
       let ballW = String.fromCharCode(101,110,100,112,111,105,110,116,95,100,95,49,56,0);
      let shootw = String.fromCharCode(53,118,113,0) == formC;
      do {
          let carouselq = String.fromCharCode(101,95,56,52,95,102,114,97,109,101,115,121,110,99,0);
          let humidityZ = true;
          let halfl: Map<any, any> = new Map([[String.fromCharCode(116,104,117,110,107,95,98,95,52,54,0),710], [String.fromCharCode(120,95,53,55,95,115,110,97,107,101,0),10], [String.fromCharCode(112,95,57,49,95,105,110,118,105,116,101,114,115,0),253]]);
         formC = `${((sortX ? 3 : 2) / (Math.max(3, 1)))}`;
         carouselq = `${halfl.size}`;
         humidityZ = null == halfl.get(`${humidityZ}`);
         if (shootw) {
            break;
         }
      } while (shootw && (sortX));
          let membershipg = String.fromCharCode(101,110,108,97,114,103,101,95,112,95,53,0);
          let f_viewj: Array<any> = [String.fromCharCode(99,111,108,108,101,99,116,105,98,108,101,95,112,95,56,51,0), String.fromCharCode(120,95,56,52,95,113,117,101,117,101,0)];
         phoneH /= Math.max(1, (membershipg == String.fromCharCode(90,0) ? membershipg.length : (sortX ? 3 : 2)));
         f_viewj = [f_viewj.length];
      for (let t = 0; t < 3; t++) {
         phoneH -= ((sortX ? 5 : 3) >> (Math.min(Math.abs(2), 3)));
      }
         formC = `${formC.length}`;
         formC = `${((sortX ? 4 : 3) << (Math.min(Math.abs(parseInt(`${phoneH}`)), 3)))}`;
      for (let m = 0; m < 3; m++) {
         ballW = `${showH.length / (Math.max(formC.length, 7))}`;
      }
         phoneH -= parseInt(`${phoneH}`);
         showH = `${(2 << (Math.min(2, Math.abs((sortX ? 4 : 1)))))}`;
      if (showH.endsWith(formC)) {
          let rewindx = 5.0;
          let country7 = false;
          let collectiona = String.fromCharCode(112,111,115,105,116,105,111,110,105,110,103,95,51,95,51,53,0);
          let launcha = String.fromCharCode(115,101,116,95,101,95,52,48,0);
          let gpayJ = false;
         formC += `${parseInt(`${rewindx}`) >> (Math.min(1, Math.abs(2)))}`;
         rewindx /= Math.max(3, ((gpayJ ? 3 : 3) - launcha.length));
         country7 = !country7;
         collectiona += `${1 << (Math.min(3, collectiona.length))}`;
         launcha = `${((gpayJ ? 1 : 4) * 3)}`;
      }
      while (formC.endsWith(showH)) {
         formC = `${ballW.length / (Math.max(2, parseInt(`${phoneH}`)))}`;
         break;
      }
         ballW = `${ballW.length}`;
      while (showH.includes(`${sortX}`)) {
          let megaphone1 = String.fromCharCode(98,95,57,55,95,99,104,117,110,107,121,99,104,117,110,107,121,0);
          let pingu = 1.0;
          let history1: Map<any, any> = new Map([[String.fromCharCode(100,95,52,49,95,112,117,108,108,117,112,0),false ], [String.fromCharCode(114,101,108,97,121,111,117,116,95,120,95,52,52,0),true ], [String.fromCharCode(112,114,101,95,112,95,50,0),false ]]);
          let plashu = 4.0;
         sortX = ballW.length == 16;
         megaphone1 += `${parseInt(`${pingu}`) - history1.size}`;
         pingu /= Math.max(3, parseFloat(`${3}`));
         history1 = new Map([[megaphone1, megaphone1.length >> (Math.min(Math.abs(3), 5))]]);
         plashu /= Math.max(megaphone1.length, 4);
         break;
      }
      for (let s = 0; s < 3; s++) {
          let usere = true;
          let report5 = String.fromCharCode(111,112,116,103,114,111,117,112,95,99,95,53,49,0);
          let progressH = String.fromCharCode(108,97,110,103,105,100,95,115,95,50,0);
          let heartQ = 4.0;
          let trash3 = String.fromCharCode(109,98,115,101,103,109,101,110,116,95,113,95,57,49,0);
         showH = `${parseInt(`${heartQ}`) + 3}`;
         usere = report5.length == 54;
         report5 = "2";
         progressH = "2";
         heartQ -= (parseFloat(`${String.fromCharCode(89,0) == trash3 ? report5.length : trash3.length}`));
      }
      if (1 == ballW.length && !sortX) {
         sortX = 57 == ballW.length && showH.length == 57;
      }
          let long_y2 = 1.0;
         sortX = !sortX;
         long_y2 += parseFloat(`${parseInt(`${long_y2}`) ^ 2}`);
      referrerk &= anytimem.length;
   }
   for (let r = 0; r < 1; r++) {
      backward1 += `${referrerk | mathD.length}`;
   }
      dragw = topicS == String.fromCharCode(51,0);
   for (let d = 0; d < 1; d++) {
       let selectedE: Map<any, any> = new Map([[String.fromCharCode(101,95,54,95,112,114,111,109,111,0),true ], [String.fromCharCode(100,101,116,101,114,109,105,110,101,100,95,105,95,53,53,0),false ], [String.fromCharCode(118,101,110,100,111,114,95,107,95,56,50,0),true ]]);
       let carousely = true;
       let statistics7 = String.fromCharCode(121,95,53,57,0);
       let paginationb = String.fromCharCode(108,95,57,50,95,99,97,116,99,104,97,98,108,101,0);
      if (!carousely) {
         carousely = statistics7.length >= 17 || carousely;
      }
       let g_titleU = String.fromCharCode(107,95,57,50,95,115,101,113,118,105,100,101,111,0);
       let grayL = 1;
       let appso = 1;
         carousely = grayL == 90 && 90 == paginationb.length;
       let action5 = String.fromCharCode(99,103,117,95,117,95,56,51,0);
       let notification9 = String.fromCharCode(115,121,110,99,104,114,111,110,105,122,97,116,105,111,110,95,49,95,51,53,0);
         paginationb = `${3 ^ paginationb.length}`;
      for (let g = 0; g < 1; g++) {
         selectedE = new Map([[`${selectedE.size}`, ((carousely ? 5 : 1) ^ selectedE.size)]]);
      }
      let away5 = 7992130 >= action5.length;
      do {
          let tooltipsl: Map<any, any> = new Map([[String.fromCharCode(122,95,55,51,95,105,118,102,101,110,99,0),413], [String.fromCharCode(102,105,110,103,101,114,95,52,95,50,53,0),24]]);
          let typingC = 3.0;
          let trashb = String.fromCharCode(118,111,114,98,105,115,100,115,112,95,51,95,49,54,0);
          let darki = String.fromCharCode(116,95,55,55,95,100,101,112,114,101,99,97,116,101,100,0);
          let rightX = String.fromCharCode(102,114,101,113,117,101,110,116,95,108,95,52,48,0);
         action5 = `${tooltipsl.size}`;
         tooltipsl = new Map([[trashb, trashb.length + rightX.length]]);
         typingC /= Math.max(parseFloat(`${2 - darki.length}`), 2);
         darki = `${rightX.length << (Math.min(Math.abs(3), 3))}`;
         if (away5) {
            break;
         }
      } while (((5 & appso) < 2 && (action5.length & appso) < 5) && away5);
          let empty0 = String.fromCharCode(115,97,109,101,113,95,100,95,52,49,0);
          let readD: Map<any, any> = new Map([[String.fromCharCode(106,95,50,49,95,108,111,111,107,0),String.fromCharCode(101,100,105,97,95,56,95,49,51,0)], [String.fromCharCode(116,120,116,110,105,100,95,103,95,49,57,0),String.fromCharCode(103,117,101,115,115,95,115,95,49,48,0)]]);
         grayL += paginationb.length << (Math.min(Math.abs(3), 3));
         empty0 += `${empty0.length | 2}`;
         readD.set(`${empty0}`, empty0.length);
      if (1 >= grayL) {
         grayL >>= Math.min(4, Math.abs((String.fromCharCode(48,0) == action5 ? action5.length : grayL)));
      }
      let navigationv = 5279698 >= grayL;
      do {
          let backwardv = 2;
          let searchbarg = String.fromCharCode(100,105,105,110,95,50,95,56,53,0);
          let forwardH = 5;
          let assistk = false;
         grayL *= 2;
         backwardv *= forwardH;
         searchbarg = `${backwardv}`;
         forwardH *= backwardv / (Math.max(3, searchbarg.length));
         assistk = backwardv == 8;
         if (navigationv) {
            break;
         }
      } while ((2 < (appso | grayL) && 2 < (appso | grayL)) && navigationv);
          let success_: Map<any, any> = new Map([[String.fromCharCode(115,116,114,110,105,99,109,112,95,108,95,54,57,0),641], [String.fromCharCode(100,95,56,55,95,100,101,116,101,114,109,105,110,105,115,116,105,99,0),609]]);
          let typesS = String.fromCharCode(99,117,114,114,95,53,95,57,51,0);
          let starY = String.fromCharCode(112,95,56,50,95,119,114,105,116,101,99,111,112,121,0);
         appso += ((carousely ? 2 : 1) | statistics7.length);
         success_.set(`${typesS}`, success_.size);
         typesS += `${starY.length << (Math.min(Math.abs(3), 2))}`;
         starY += `${typesS.length}`;
      topicS = `${(String.fromCharCode(108,0) == paginationb ? selectedE.size : paginationb.length)}`;
   }
   for (let x = 0; x < 1; x++) {
      anytimem = "1";
   }
       let floatingI = 5.0;
          let streamingR = String.fromCharCode(118,95,54,51,95,101,103,119,105,116,0);
         floatingI -= parseFloat(`${3 | parseInt(`${floatingI}`)}`);
         streamingR += `${streamingR.length}`;
      if ((floatingI - 4.76) <= 4.51 || (4.76 - floatingI) <= 2.9) {
          let castingQ = 3.0;
          let sourceV = String.fromCharCode(106,95,52,55,95,117,110,97,108,105,103,110,0);
          let dialogT = String.fromCharCode(110,112,114,111,98,101,95,114,95,57,48,0);
          let private_kq = String.fromCharCode(101,115,116,105,109,97,116,101,115,95,108,95,52,52,0);
          let middle_: Array<any> = [833, 755, 819];
         floatingI += parseFloat(`${parseInt(`${castingQ}`)}`);
         sourceV += `${(private_kq == String.fromCharCode(82,0) ? sourceV.length : private_kq.length)}`;
         dialogT += `${2 & sourceV.length}`;
         middle_.push(3);
      }
      let membershipV = floatingI >= 5096188.0;
      do {
          let sideb = String.fromCharCode(111,115,116,114,101,97,109,95,100,95,52,49,0);
          let changec = false;
          let untickm = 4.0;
          let u_count9: Array<any> = [184, 376, 528];
         floatingI /= Math.max(parseFloat(`${1 % (Math.max(4, sideb.length))}`), 1);
         sideb += `${u_count9.length}`;
         changec = u_count9.length > 19;
         untickm /= Math.max(2 >> (Math.min(Math.abs(parseInt(`${untickm}`)), 4)), 3);
         if (membershipV) {
            break;
         }
      } while (membershipV && (4.77 == (floatingI * 5.43) && (floatingI * 5.43) == 4.32));
      mathD = `${3 / (Math.max(referrerk, 4))}`;
      backward1 = `${backward1.length}`;
      gcopy_ed = new Map([[`${gcopy_ed.size}`, anytimem.length / 1]]);
      mathD += `${mathD.length}`;

    RNExitApp.exitApp();
  }, []);

  const handleAccept = useCallback(() => {
       let overS = 3.0;
    let modaly = String.fromCharCode(119,104,101,101,108,95,116,95,52,51,0);
    let hooksv = 5.0;
    let field0 = String.fromCharCode(99,111,110,116,114,97,99,116,95,109,95,56,56,0);
    let descq = String.fromCharCode(105,99,101,99,97,115,116,95,115,95,52,53,0);
    let stationsx: Map<any, any> = new Map([[String.fromCharCode(117,95,54,50,95,97,117,116,104,111,114,115,0),939], [String.fromCharCode(117,110,115,101,108,101,99,116,95,100,95,51,54,0),419], [String.fromCharCode(107,95,51,52,95,119,101,114,101,0),463]]);
    let gray_ = String.fromCharCode(118,95,56,57,95,106,112,101,103,105,110,116,0);
    let mail4: Array<any> = [9, 410];
    let resulte = String.fromCharCode(114,101,102,105,110,101,109,101,110,116,95,102,95,54,56,0);
    let networkP: Array<any> = [707, 847];
    let clockH = String.fromCharCode(104,95,54,95,114,101,102,99,111,117,110,116,101,114,0);
    let blackP: Array<any> = [520, 217];
      field0 += `${modaly.length}`;
       let modelsJ = String.fromCharCode(103,95,53,53,95,105,102,102,116,0);
       let listP = 1.0;
       let w_counta = 1.0;
      while (4.11 <= (listP * 3.39) || (3.39 + w_counta) <= 2.39) {
         listP /= Math.max(1 ^ parseInt(`${listP}`), 1);
         break;
      }
      while (4.33 > (listP * w_counta) && 1.23 > (listP + 4.33)) {
         listP += parseInt(`${w_counta}`);
         break;
      }
      if (4 < (modelsJ.length + 4)) {
         modelsJ = "2";
      }
       let miniR = String.fromCharCode(118,105,100,101,111,99,111,100,101,99,95,113,95,49,49,0);
      let groupj = 6642564 >= miniR.length;
      do {
         miniR = `${2 >> (Math.min(3, modelsJ.length))}`;
         if (groupj) {
            break;
         }
      } while ((!modelsJ.startsWith(`${miniR.length}`)) && groupj);
         w_counta -= (String.fromCharCode(54,0) == miniR ? parseInt(`${w_counta}`) : miniR.length);
         miniR += `${modelsJ.length}`;
         listP *= 2 & modelsJ.length;
       let countdownS = 5.0;
       let middlewareO = 3.0;
      overS /= Math.max(5, parseInt(`${hooksv}`) | parseInt(`${w_counta}`));
   for (let t = 0; t < 2; t++) {
      mail4.push(parseInt(`${hooksv}`) >> (Math.min(resulte.length, 2)));
   }
   for (let k = 0; k < 2; k++) {
      stationsx.set(descq, (descq == String.fromCharCode(48,0) ? descq.length : modaly.length));
   }
      mail4 = [2 % (Math.max(2, mail4.length))];
   for (let w = 0; w < 2; w++) {
       let smallu = String.fromCharCode(110,112,114,111,98,101,95,120,95,54,53,0);
       let controlx: Array<any> = [167, 197];
       let episodeQ: Array<any> = [148, 710];
         episodeQ = [episodeQ.length >> (Math.min(2, controlx.length))];
      for (let c = 0; c < 3; c++) {
          let plusF: Map<any, any> = new Map([[String.fromCharCode(97,99,116,105,118,101,95,112,95,53,51,0),531], [String.fromCharCode(111,117,114,95,118,95,54,51,0),24]]);
          let shootL = true;
          let routerY = String.fromCharCode(119,97,108,108,112,97,112,101,114,95,101,95,50,48,0);
         episodeQ.push(3 & episodeQ.length);
         plusF.set(routerY, plusF.size | 1);
         shootL = (32 == ((!shootL ? plusF.size : 59) >> (Math.min(Math.abs(plusF.size), 4))));
         routerY = `${plusF.size}`;
      }
      while (controlx.length >= smallu.length) {
         controlx.push(3);
         break;
      }
         episodeQ = [controlx.length];
       let stepH = true;
         smallu += `${controlx.length}`;
      let dangerf = smallu.length <= 6516855;
      do {
         smallu = "2";
         if (dangerf) {
            break;
         }
      } while ((smallu.length > 4 || stepH) && dangerf);
         controlx = [smallu.length];
         stepH = controlx.length > 43;
      descq += "3";
   }
       let carousel_ = String.fromCharCode(115,95,52,54,95,103,101,110,101,114,97,116,111,114,0);
       let stationO = 4.0;
      let groupx = 7928809.0 >= stationO;
      do {
         stationO += carousel_.length;
         if (groupx) {
            break;
         }
      } while ((3 <= (parseInt(`${stationO}`) / (Math.max(carousel_.length, 2)))) && groupx);
      for (let x = 0; x < 2; x++) {
          let recommendations = false;
          let shirte = String.fromCharCode(108,95,52,50,95,111,110,101,99,104,0);
          let homeb = true;
          let whatsappd = 0;
          let charte = true;
         carousel_ += `${(String.fromCharCode(71,0) == shirte ? parseInt(`${stationO}`) : shirte.length)}`;
         recommendations = (homeb ? recommendations : !homeb);
         whatsappd &= ((charte ? 4 : 3) & whatsappd);
         charte = 87 <= whatsappd;
      }
      while (carousel_.startsWith(`${stationO}`)) {
          let verticalY = false;
          let episodesw = 5.0;
          let borderlessa: Array<any> = [939, 944, 40];
         carousel_ += `${((verticalY ? 1 : 5) | carousel_.length)}`;
         verticalY = (episodesw * parseFloat(`${borderlessa.length}`)) <= 56.93;
         episodesw += parseFloat(`${2 * parseInt(`${episodesw}`)}`);
         borderlessa.push(2);
         break;
      }
      while (2 == (carousel_.length - 4)) {
         stationO *= parseInt(`${stationO}`);
         break;
      }
      for (let j = 0; j < 2; j++) {
         carousel_ += `${parseInt(`${stationO}`)}`;
      }
         carousel_ += `${carousel_.length << (Math.min(2, Math.abs(parseInt(`${stationO}`))))}`;
      resulte = `${field0.length}`;
      overS += field0.length;
      gray_ = `${parseInt(`${overS}`) - 2}`;
      modaly += `${3 | resulte.length}`;

    setIsVisible(false)

      modaly = `${gray_.length}`;
       let star_ = false;
       let anneri = String.fromCharCode(97,95,56,52,95,100,112,114,105,110,116,0);
       let baiduU = String.fromCharCode(103,117,105,100,101,115,95,108,95,55,48,0);
         star_ = anneri == baiduU;
       let fullP = 5;
       let macaue = 2;
         anneri += `${((star_ ? 5 : 4))}`;
      while (fullP <= 5) {
         star_ = anneri.length > macaue;
         break;
      }
          let megaphoneV = 4.0;
          let greyw = false;
          let minimizen: Map<any, any> = new Map([[String.fromCharCode(100,105,114,97,99,100,115,112,95,53,95,55,49,0),929], [String.fromCharCode(100,95,49,48,95,119,105,114,101,108,101,115,115,0),158]]);
         star_ = baiduU == String.fromCharCode(82,0);
         megaphoneV -= parseFloat(`${minimizen.size}`);
         greyw = minimizen.size == 49;
      for (let l = 0; l < 2; l++) {
         fullP >>= Math.min(3, Math.abs(2 >> (Math.min(5, baiduU.length))));
      }
      for (let e = 0; e < 2; e++) {
          let overs = 0.0;
         fullP += (String.fromCharCode(90,0) == baiduU ? macaue : baiduU.length);
         overs += parseFloat(`${parseInt(`${overs}`) << (Math.min(2, Math.abs(2)))}`);
      }
         fullP |= fullP;
      if (fullP <= 3) {
          let customx = String.fromCharCode(115,116,105,99,107,101,114,115,95,98,95,55,54,0);
          let crownL: Array<any> = [218, 296, 981];
          let sheetx = 0;
          let expandk = 2;
         macaue -= ((star_ ? 2 : 2) % (Math.max(3, fullP)));
         customx += `${expandk / 2}`;
         crownL.push(2 & crownL.length);
         sheetx ^= 1;
         expandk *= sheetx << (Math.min(Math.abs(expandk), 5));
      }
      modaly = `${field0.length}`;
   let countdownR = String.fromCharCode(106,54,48,116,52,109,114,114,0) == descq;
   do {
       let zhengpianj: Map<any, any> = new Map([[String.fromCharCode(110,111,110,95,51,95,52,51,0),false ], [String.fromCharCode(100,105,103,101,115,116,95,49,95,53,49,0),true ], [String.fromCharCode(99,105,110,118,105,100,101,111,95,115,95,50,53,0),true ]]);
       let fastforwardi = 0;
         zhengpianj = new Map([[`${zhengpianj.size}`, zhengpianj.size]]);
      for (let i = 0; i < 1; i++) {
          let headerL = String.fromCharCode(120,106,112,101,103,95,100,95,52,50,0);
          let a_positionR = 5.0;
          let eventd = 0;
          let whistle0 = String.fromCharCode(97,118,105,111,95,50,95,56,53,0);
         zhengpianj.set(headerL, fastforwardi << (Math.min(Math.abs(1), 4)));
         headerL = `${eventd}`;
         a_positionR += (whistle0 == String.fromCharCode(76,0) ? eventd : whistle0.length);
      }
         fastforwardi *= fastforwardi | 1;
         zhengpianj = new Map([[`${zhengpianj.size}`, 3]]);
       let routeri = String.fromCharCode(116,95,55,0);
       let results = String.fromCharCode(109,101,109,100,101,98,117,103,95,105,95,55,48,0);
          let componenty = 2.0;
         routeri = `${results.length}`;
         componenty += parseFloat(`${parseInt(`${componenty}`) - 1}`);
      descq += `${fastforwardi ^ 1}`;
      if (countdownR) {
         break;
      }
   } while ((descq.length < 3) && countdownR);
      field0 = `${1 % (Math.max(10, parseInt(`${overS}`)))}`;
       let delegate_4V = String.fromCharCode(112,95,54,56,95,112,104,112,0);
       let projecty: Map<any, any> = new Map([[String.fromCharCode(111,112,116,105,109,105,122,101,100,95,113,95,52,50,0),696], [String.fromCharCode(105,95,50,57,95,99,105,110,101,109,97,0),222], [String.fromCharCode(114,95,54,54,95,111,112,101,110,115,115,108,0),35]]);
      let crownM = 6944148 >= delegate_4V.length;
      do {
         delegate_4V += `${(String.fromCharCode(102,0) == delegate_4V ? delegate_4V.length : projecty.size)}`;
         if (crownM) {
            break;
         }
      } while (crownM && ((1 | projecty.size) >= 3 && 1 >= (1 | projecty.size)));
         delegate_4V += `${(String.fromCharCode(77,0) == delegate_4V ? delegate_4V.length : projecty.size)}`;
         delegate_4V += `${(String.fromCharCode(50,0) == delegate_4V ? projecty.size : delegate_4V.length)}`;
       let friendso = false;
       let fillQ = true;
       let changeb = 5.0;
       let reducerF = 5.0;
         friendso = changeb < parseFloat(`${projecty.size}`);
      networkP.push(field0.length);
   let groupA = stationsx.size <= 8233854;
   do {
       let sheetg = 4;
       let privacyZ: Array<any> = [722, 316, 407];
          let telegramm: Map<any, any> = new Map([[String.fromCharCode(109,95,51,49,95,97,108,97,110,103,117,97,103,101,0),692], [String.fromCharCode(117,110,115,112,101,99,105,102,105,101,100,95,55,95,54,55,0),97], [String.fromCharCode(99,104,114,111,109,97,95,100,95,49,52,0),348]]);
          let dragm = String.fromCharCode(117,95,57,52,95,105,110,116,108,0);
          let sellY: Array<any> = [894, 264, 422];
         sheetg += telegramm.size;
         telegramm = new Map([[`${sellY.length}`, sellY.length]]);
         dragm = `${(dragm == String.fromCharCode(71,0) ? dragm.length : sellY.length)}`;
      if (3 >= (4 + privacyZ.length) || (sheetg + 4) >= 5) {
         privacyZ.push(2);
      }
          let sharedD = 5.0;
         privacyZ.push(privacyZ.length);
         sharedD /= Math.max(parseInt(`${sharedD}`), 4);
      for (let u = 0; u < 1; u++) {
         privacyZ.push(sheetg % (Math.max(6, privacyZ.length)));
      }
         sheetg >>= Math.min(4, privacyZ.length);
      let slidero = sheetg >= 5519977;
      do {
         sheetg |= 2 >> (Math.min(Math.abs(sheetg), 2));
         if (slidero) {
            break;
         }
      } while ((privacyZ.includes(sheetg)) && slidero);
      stationsx = new Map([[descq, 3]]);
      if (groupA) {
         break;
      }
   } while ((2 <= (gray_.length << (Math.min(1, Math.abs(stationsx.size)))) || 2 <= (gray_.length << (Math.min(Math.abs(2), 5)))) && groupA);
      gray_ = "1";
      hooksv += (parseFloat(`${String.fromCharCode(70,0) == clockH ? parseInt(`${hooksv}`) : clockH.length}`));
       let expiredu = String.fromCharCode(107,98,112,115,95,107,95,49,53,0);
          let splashf = 5.0;
          let halfP: Map<any, any> = new Map([[String.fromCharCode(113,95,57,54,95,108,98,108,0),855], [String.fromCharCode(110,111,119,95,109,95,55,56,0),726], [String.fromCharCode(99,104,105,108,100,95,100,95,56,0),771]]);
          let sourcem = 4;
         expiredu += `${parseInt(`${splashf}`)}`;
         splashf -= parseFloat(`${3 & sourcem}`);
         halfP.set(`${sourcem}`, 3 & sourcem);
         expiredu += `${1 >> (Math.min(2, expiredu.length))}`;
          let private_7sZ: Array<any> = [683, 243];
          let siden = String.fromCharCode(102,114,101,101,122,101,95,52,95,51,50,0);
         expiredu = `${2 >> (Math.min(4, private_7sZ.length))}`;
         private_7sZ = [siden.length % (Math.max(1, 5))];
         siden = `${siden.length + 1}`;
      clockH = `${gray_.length << (Math.min(2, Math.abs(stationsx.size)))}`;
   if (3 >= (clockH.length + parseInt(`${hooksv}`)) || (hooksv + parseFloat(`${clockH.length}`)) >= 1.21) {
       let questG = false;
       let flyerB: Array<any> = [470, 621];
         questG = flyerB.length <= 20 && !questG;
       let previewl = true;
       let popupz = false;
      while (!previewl) {
          let emojiL: Map<any, any> = new Map([[String.fromCharCode(110,111,115,105,109,100,95,56,95,53,53,0),563], [String.fromCharCode(104,95,55,53,95,99,100,97,116,101,0),767]]);
          let submitS = false;
         previewl = (!previewl ? !popupz : previewl);
         emojiL.set(`${submitS}`, emojiL.size ^ 3);
         break;
      }
         questG = (((questG ? flyerB.length : 75) % (Math.max(4, flyerB.length))) <= 75);
         flyerB = [((questG ? 5 : 4))];
      while ((flyerB.length - 3) >= 2 && previewl) {
         flyerB = [((previewl ? 2 : 1) << (Math.min(flyerB.length, 5)))];
         break;
      }
      clockH = `${((questG ? 5 : 2) ^ flyerB.length)}`;
   }
    setAccepted(true)

       let loadingt: Map<any, any> = new Map([[String.fromCharCode(115,101,110,115,111,114,95,113,95,57,50,0),String.fromCharCode(110,95,54,57,95,111,112,116,105,109,105,122,101,100,0)], [String.fromCharCode(105,95,51,56,95,115,102,116,112,0),String.fromCharCode(102,95,53,51,95,112,104,114,97,115,101,0)], [String.fromCharCode(114,101,99,111,110,115,116,114,117,99,116,101,100,95,121,95,57,53,0),String.fromCharCode(101,95,50,52,95,97,100,108,101,114,0)]]);
       let descN = String.fromCharCode(115,105,116,101,95,100,95,51,51,0);
      let borderless1 = 6458874 <= descN.length;
      do {
         descN += `${2 | descN.length}`;
         if (borderless1) {
            break;
         }
      } while (((2 << (Math.min(5, Math.abs(loadingt.size)))) > 5) && borderless1);
         loadingt.set(descN, loadingt.size);
      while (!descN.startsWith(`${loadingt.size}`)) {
         descN = `${descN.length}`;
         break;
      }
          let profilem: Array<any> = [304, 236];
          let privacy8 = 5;
         descN += `${(String.fromCharCode(115,0) == descN ? descN.length : loadingt.size)}`;
         profilem.push(privacy8 % (Math.max(profilem.length, 3)));
         privacy8 &= privacy8;
         loadingt.set(descN, descN.length >> (Math.min(Math.abs(1), 1)));
      if (descN.endsWith(`${loadingt.size}`)) {
         loadingt = new Map([[`${loadingt.size}`, loadingt.size / (Math.max(descN.length, 8))]]);
      }
      descq = `${networkP.length ^ 2}`;
   while ((hooksv - parseFloat(`${networkP.length}`)) == 2.23 || 1.20 == (hooksv - 2.23)) {
      networkP.push(field0.length >> (Math.min(Math.abs(3), 4)));
      break;
   }
   for (let b = 0; b < 1; b++) {
      stationsx.set(descq, (String.fromCharCode(104,0) == descq ? parseInt(`${hooksv}`) : descq.length));
   }
       let episodeI = String.fromCharCode(115,116,114,105,110,103,105,102,121,95,53,95,55,48,0);
       let native5 = String.fromCharCode(121,95,50,55,95,104,97,112,116,105,99,0);
       let memberD = String.fromCharCode(97,117,120,100,97,116,97,95,110,95,54,56,0);
         native5 = `${native5.length}`;
      while (episodeI.endsWith(memberD)) {
         episodeI += `${episodeI.length}`;
         break;
      }
         native5 = `${episodeI.length}`;
       let group6 = String.fromCharCode(107,95,56,55,95,111,102,102,115,0);
       let types1 = String.fromCharCode(102,95,57,52,95,99,111,108,111,99,97,116,101,100,0);
       let thailandf = 3.0;
      for (let n = 0; n < 1; n++) {
         native5 += `${parseInt(`${thailandf}`) >> (Math.min(episodeI.length, 1))}`;
      }
         group6 = `${(episodeI == String.fromCharCode(78,0) ? types1.length : episodeI.length)}`;
         group6 += `${native5.length}`;
      if (episodeI != String.fromCharCode(76,0)) {
         group6 = `${episodeI.length}`;
      }
      modaly += `${stationsx.size}`;
      hooksv /= Math.max(parseFloat(`${descq.length & 3}`), 1);
       let long_6mp = String.fromCharCode(98,105,116,115,116,114,101,97,109,95,114,95,49,56,0);
       let topic1 = 0.0;
          let infoC: Map<any, any> = new Map([[String.fromCharCode(117,95,57,56,95,112,108,97,110,97,114,116,111,121,117,121,0),false ], [String.fromCharCode(102,109,115,117,98,95,113,95,54,48,0),false ]]);
         long_6mp = `${parseInt(`${topic1}`)}`;
         infoC.set(`${infoC.size}`, infoC.size);
      let become_ = long_6mp == String.fromCharCode(101,102,56,52,57,111,0);
      do {
         long_6mp += `${1 | long_6mp.length}`;
         if (become_) {
            break;
         }
      } while (become_ && (long_6mp.length == 4));
         topic1 += (long_6mp == String.fromCharCode(68,0) ? parseInt(`${topic1}`) : long_6mp.length);
         long_6mp += `${(long_6mp == String.fromCharCode(103,0) ? parseInt(`${topic1}`) : long_6mp.length)}`;
      while ((parseInt(`${topic1}`) / 1) < 3 && 5 < (1 >> (Math.min(1, long_6mp.length)))) {
          let collectioni: Map<any, any> = new Map([[String.fromCharCode(114,101,97,115,115,101,109,98,108,101,114,95,104,95,52,52,0),String.fromCharCode(112,114,105,118,107,101,121,95,120,95,49,52,0)], [String.fromCharCode(108,105,103,104,116,95,117,95,49,54,0),String.fromCharCode(108,97,99,105,110,103,95,106,95,52,48,0)], [String.fromCharCode(116,95,49,53,95,101,118,105,99,101,0),String.fromCharCode(103,95,51,55,95,99,97,110,99,101,108,101,100,0)]]);
         long_6mp += `${collectioni.size}`;
         break;
      }
      if (topic1 <= 3.67) {
         long_6mp = `${parseInt(`${topic1}`)}`;
      }
      modaly = `${long_6mp.length << (Math.min(3, mail4.length))}`;
   if (3 <= (2 << (Math.min(5, networkP.length))) || 2 <= (gray_.length << (Math.min(3, networkP.length)))) {
      gray_ += `${blackP.length & 2}`;
   }
       let yingS = String.fromCharCode(101,113,117,105,118,97,108,101,110,116,95,108,95,49,57,0);
       let langkey8 = 1.0;
       let whatsappR = 0;
      while (whatsappR == 2) {
          let emptyY: Array<any> = [872, 58, 940];
          let alertC: Map<any, any> = new Map([[String.fromCharCode(112,117,116,98,105,116,98,117,102,102,101,114,95,120,95,50,48,0),18], [String.fromCharCode(118,101,110,117,101,95,99,95,54,52,0),16]]);
          let next_ = 2;
          let circleZ = false;
         whatsappR -= 1;
         emptyY = [((circleZ ? 1 : 4) * alertC.size)];
         alertC = new Map([[`${next_}`, next_]]);
         circleZ = alertC.size > emptyY.length;
         break;
      }
         langkey8 += (parseFloat(`${yingS == String.fromCharCode(74,0) ? yingS.length : parseInt(`${langkey8}`)}`));
       let progressK = 0.0;
         whatsappR &= parseInt(`${progressK}`);
         yingS = `${parseInt(`${progressK}`)}`;
       let optionss = 0.0;
      for (let u = 0; u < 2; u++) {
         optionss -= parseFloat(`${whatsappR + 2}`);
      }
      if (4.94 < (progressK / (Math.max(1, optionss))) && 4.94 < (optionss / (Math.max(progressK, 6)))) {
          let resendE: Map<any, any> = new Map([[String.fromCharCode(118,95,49,95,115,112,105,110,0),892], [String.fromCharCode(118,95,54,55,95,109,111,118,116,101,120,116,115,117,98,0),391]]);
         progressK /= Math.max(4, whatsappR % (Math.max(yingS.length, 6)));
         resendE.set(`${resendE.size}`, 1 * resendE.size);
      }
          let filledK: Map<any, any> = new Map([[String.fromCharCode(100,117,112,108,105,99,97,116,101,95,105,95,49,50,0),906], [String.fromCharCode(110,95,55,95,99,97,108,99,108,117,108,97,116,101,0),732]]);
          let downloadW = 5;
          let loginS = 0;
         progressK -= 2 << (Math.min(2, Math.abs(parseInt(`${langkey8}`))));
         filledK = new Map([[`${filledK.size}`, downloadW]]);
         downloadW += loginS & 2;
         loginS <<= Math.min(Math.abs(filledK.size | downloadW), 3);
      resulte = "3";
      mail4.push(mail4.length & 3);
   while (gray_ == resulte) {
      resulte += `${field0.length << (Math.min(2, descq.length))}`;
      break;
   }
    dispatch(acceptPrivacyPolicy())
  }, [])

  const handleReadTermsPrivacy = useCallback((view: '用户协议' | '隐私政策') => {
    const checkGoBack = ({ data }) => {
      if (data.state.routes[data.state.routes.length - 1].name === 'Home') {
        setIsVisible(true);
        navigation.removeListener('state', checkGoBack);
      }
    }
    navigation.addListener('state', checkGoBack);

    navigation.navigate(view);
    setIsVisible(false);
  }, [])

  return (
    <>
      {isVisible &&
        <View
          style={{
            height: '100%',
            width: '100%',
            backgroundColor: colors.background + 'BB',
            position: 'absolute',
            justifyContent: 'center',
            alignItems: 'center'
          }}>
          <View style={{
            width: '75%',
            padding: 20,
            gap: spacing.l,
            backgroundColor: colors.background,
            ...styles.overlay,
          }}>
            <Text style={{ ...textVariants.header, textAlign: 'center' }}>服务协议和隐私政策</Text>
            <Text
              style={{
                ...textVariants.subBody,
                textAlign: 'center',
              }}>
              <Text>
                请你务必审慎阅读,
                充分理解“服务协议”和“隐私政策”各条款，包括但不限于：为了更好的向你提供服务，我们需要收集你的设备标识，操作日常等信息用于分析，优化应用性能。你可阅读
              </Text>
              <Text onPress={() => handleReadTermsPrivacy('用户协议')}>
                <Text style={{ color: colors.primary }}>《服务协议》</Text>
              </Text>
              <Text>和</Text>
              <Text onPress={() => handleReadTermsPrivacy('隐私政策')}>
                <Text style={{ color: colors.primary }}>《隐私政策》</Text>
              </Text>
              <Text>
                了解详细信息。如果你同意，请点击下面按钮开始接受我们的服务。
              </Text>
            </Text>
            <View style={styles.btns}>
              <TouchableOpacity activeOpacity={0.85} style={styles.btn} onPress={handleReject}>
                <Text
                  style={{
                    ...textVariants.body,
                    ...styles.head,
                  }}>
                  暂不同意
                </Text>
              </TouchableOpacity>
              <TouchableOpacity activeOpacity={0.85} style={styles.btn} onPress={handleAccept}>
                <Text
                  style={{
                    ...textVariants.body,
                    ...styles.head,
                    color: IS_OTHER_SKIN ? colors.confirm : colors.primary,
                  }}>
                  同意并接受
                </Text>
              </TouchableOpacity>
            </View>
          </View>
        </View>
      }
    </>
  )
}

const styles = StyleSheet.create({
  overlay: {
    borderRadius: 14,
  },
  head: {
    textAlign: 'center',
  },
  body: {
    color: 'white',
  },
  link: {
    display: 'flex',
  },
  btns: {
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
  },
  btn: {
    flex: 1,
  },
});
