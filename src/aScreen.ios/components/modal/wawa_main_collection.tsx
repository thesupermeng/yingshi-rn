import { useTheme } from "@react-navigation/native";
import { Text, View } from "react-native";
import FastImage from '../common/wawa_iconarrowrightblack';


export const UploadResultOverlay = () => {
       let executorS: Array<any> = [String.fromCharCode(98,105,103,95,99,95,50,49,0), String.fromCharCode(116,101,108,101,103,114,97,95,100,95,49,48,0), String.fromCharCode(99,117,98,105,99,95,117,95,54,48,0)];
    let yellowcirclebgY = 5.0;
    let statisticsactiveB: Map<any, any> = new Map([[String.fromCharCode(118,112,108,97,121,101,114,95,55,95,53,50,0),String.fromCharCode(117,110,114,101,102,95,111,95,54,56,0)], [String.fromCharCode(98,95,54,48,95,116,97,98,108,101,99,0),String.fromCharCode(109,97,110,97,103,101,114,0)]]);
    let libreactperfloggerjnir: Map<any, any> = new Map([[String.fromCharCode(118,95,56,52,95,111,116,104,101,114,119,105,115,101,0),12], [String.fromCharCode(117,110,105,120,95,121,95,54,51,0),77], [String.fromCharCode(97,95,55,52,95,114,105,103,104,116,115,0),175]]);
    let libswscalem = String.fromCharCode(115,98,114,100,115,112,0);
    let penaltygoalJ = 3.0;
    let wifirouteru: Array<any> = [String.fromCharCode(100,95,57,95,99,97,108,99,117,108,97,116,105,110,103,0), String.fromCharCode(110,101,97,114,101,115,116,95,118,95,49,53,0)];
    let untick8 = String.fromCharCode(98,95,55,50,95,121,98,108,111,99,107,0);
    let final_to = false;
    let rewardL = true;
    let iconbellactiveb = 4;
    let field8: Map<any, any> = new Map([[String.fromCharCode(114,95,56,57,95,109,101,116,97,100,97,116,97,115,0),String.fromCharCode(99,111,108,108,105,100,101,114,95,51,95,51,52,0)], [String.fromCharCode(101,110,116,101,114,105,110,103,95,122,95,55,54,0),String.fromCharCode(100,101,115,116,114,111,121,105,110,103,95,49,95,54,49,0)]]);
    let liveshareo: Array<any> = [714, 201, 45];
    let gemfile_ = String.fromCharCode(115,116,114,117,99,116,95,122,95,54,55,0);
    let arrowselectdown1 = false;
    let actionj = 4.0;
    let detail1 = String.fromCharCode(97,118,101,114,97,103,101,115,0);
    let loginX = String.fromCharCode(119,95,55,49,95,109,102,113,101,0);
      penaltygoalJ *= parseFloat(`${3}`);
   let bodanq = statisticsactiveB.size >= 7003955;
   do {
       let nativeI: Array<any> = [225, 277, 645];
       let downloadedU = String.fromCharCode(102,95,54,49,95,109,105,120,105,110,115,0);
          let buffert: Array<any> = [217, 759];
          let submitG = String.fromCharCode(100,97,116,97,100,105,114,95,51,95,49,50,0);
          let shirtp: Array<any> = [12, 256, 384];
         downloadedU += `${(String.fromCharCode(65,0) == submitG ? buffert.length : submitG.length)}`;
         buffert = [1];
      for (let l = 0; l < 3; l++) {
         downloadedU += "1";
      }
         downloadedU += `${downloadedU.length}`;
       let imagenetworkerru = String.fromCharCode(111,95,54,48,95,115,109,97,99,107,101,114,0);
       let reactnativeultimatelistviewk = String.fromCharCode(122,95,54,48,95,108,105,98,120,109,108,0);
      for (let k = 0; k < 1; k++) {
         imagenetworkerru += `${downloadedU.length}`;
      }
       let episode9 = 1.0;
      statisticsactiveB = new Map([[`${statisticsactiveB.size}`, libswscalem.length]]);
      nativeI = [nativeI.length % (Math.max(6, nativeI.length))];
      if (bodanq) {
         break;
      }
   } while (bodanq && ((statisticsactiveB.size + 3) <= 5 && 3 <= statisticsactiveB.size));
   while (untick8.startsWith(`${libswscalem.length}`)) {
       let backwhiteT: Array<any> = [String.fromCharCode(122,95,54,51,95,100,101,99,111,109,112,114,101,115,115,105,111,110,0), String.fromCharCode(105,110,118,101,114,118,97,108,0), String.fromCharCode(118,95,56,51,95,105,110,116,114,110,108,0)];
       let path9 = String.fromCharCode(114,101,118,111,107,97,98,108,101,95,56,95,52,50,0);
         path9 += `${backwhiteT.length}`;
      for (let o = 0; o < 3; o++) {
         path9 += `${path9.length - 2}`;
      }
         path9 = `${backwhiteT.length}`;
      while (1 > (backwhiteT.length << (Math.min(Math.abs(1), 1))) && 1 > (backwhiteT.length << (Math.min(path9.length, 5)))) {
         path9 += "2";
         break;
      }
      let incidentc = 7414023 >= path9.length;
      do {
         path9 = `${backwhiteT.length & path9.length}`;
         if (incidentc) {
            break;
         }
      } while ((path9.endsWith(`${backwhiteT.length}`)) && incidentc);
          let librrcW = 5.0;
         backwhiteT = [path9.length >> (Math.min(2, backwhiteT.length))];
         librrcW /= Math.max(5, parseFloat(`${1}`));
      untick8 = "3";
      break;
   }
   while ((penaltygoalJ * 2.90) > 3.34 && (5 - wifirouteru.length) > 5) {
      penaltygoalJ *= parseFloat(`${2 | parseInt(`${penaltygoalJ}`)}`);
      break;
   }
   let update_xp = penaltygoalJ <= 9113126.0;
   do {
      penaltygoalJ += parseFloat(`${parseInt(`${yellowcirclebgY}`) << (Math.min(5, Math.abs(2)))}`);
      if (update_xp) {
         break;
      }
   } while ((2.36 < (penaltygoalJ / (Math.max(1.32, 9)))) && update_xp);
   if ((statisticsactiveB.size * libreactperfloggerjnir.size) >= 1 || 1 >= (libreactperfloggerjnir.size * statisticsactiveB.size)) {
      statisticsactiveB.set(`${rewardL}`, ((rewardL ? 4 : 3) / (Math.max(6, parseInt(`${yellowcirclebgY}`)))));
   }
   while (2 > (4 >> (Math.min(3, libswscalem.length)))) {
       let whitebellf = false;
       let liveendmodallogoW = 5.0;
         whitebellf = liveendmodallogoW >= 99.42;
          let bingw = String.fromCharCode(114,105,110,103,98,97,99,107,0);
         liveendmodallogoW -= (parseInt(`${liveendmodallogoW}`) | (whitebellf ? 1 : 1));
         bingw += `${1 - bingw.length}`;
         whitebellf = 90.16 <= liveendmodallogoW;
      if (!whitebellf) {
          let kuaishou8 = 3;
          let phonesharey: Map<any, any> = new Map([[String.fromCharCode(109,100,104,100,95,99,95,56,0),false ], [String.fromCharCode(119,101,101,107,95,121,95,51,51,0),true ]]);
          let fulls = String.fromCharCode(99,102,102,116,98,95,117,95,57,54,0);
          let photoc = String.fromCharCode(114,95,53,95,99,97,110,111,112,117,115,0);
          let membershipk: Map<any, any> = new Map([[String.fromCharCode(107,101,121,102,114,97,109,101,95,50,95,54,55,0),694], [String.fromCharCode(99,117,114,114,101,110,100,95,97,95,53,56,0),257]]);
         whitebellf = photoc.includes(`${liveendmodallogoW}`);
         kuaishou8 &= fulls.length;
         phonesharey.set(fulls, 1);
         photoc += `${2 - phonesharey.size}`;
         membershipk.set(`${kuaishou8}`, 1);
      }
      if (4.85 <= (liveendmodallogoW + 2.88)) {
         whitebellf = !whitebellf;
      }
         liveendmodallogoW *= ((whitebellf ? 4 : 5) & parseInt(`${liveendmodallogoW}`));
      field8 = new Map([[`${libreactperfloggerjnir.size}`, wifirouteru.length - 1]]);
      break;
   }
      rewardL = 70.61 >= yellowcirclebgY;
   while ((5.92 * penaltygoalJ) == 3.86 || 3 == (executorS.length + 3)) {
      penaltygoalJ += (parseFloat(`${(rewardL ? 4 : 5) / (Math.max(4, parseInt(`${penaltygoalJ}`)))}`));
      break;
   }
       let historyv = String.fromCharCode(106,95,50,51,95,118,97,108,117,101,100,0);
       let clockp = String.fromCharCode(115,95,50,57,95,115,116,100,111,117,116,0);
       let firebaseJ = String.fromCharCode(114,101,115,105,100,117,101,115,0);
       let rewardV = 3.0;
       let spinneri = 0.0;
         clockp += `${parseInt(`${spinneri}`) + historyv.length}`;
         rewardV -= parseInt(`${spinneri}`);
      for (let l = 0; l < 2; l++) {
         spinneri /= Math.max(historyv.length * clockp.length, 2);
      }
         spinneri *= 3 - parseInt(`${spinneri}`);
      let debugo = 8232407 >= firebaseJ.length;
      do {
         firebaseJ = `${1 | parseInt(`${rewardV}`)}`;
         if (debugo) {
            break;
         }
      } while (debugo && ((firebaseJ.length ^ 3) == 4 && 5.48 == (spinneri - firebaseJ.length)));
          let iconnewchatW = String.fromCharCode(102,95,53,55,95,101,112,105,99,0);
          let imagewatchlived = 5.0;
         historyv += `${parseInt(`${imagewatchlived}`) * 3}`;
         iconnewchatW = `${(String.fromCharCode(50,0) == iconnewchatW ? iconnewchatW.length : iconnewchatW.length)}`;
         imagewatchlived -= (iconnewchatW == String.fromCharCode(68,0) ? iconnewchatW.length : iconnewchatW.length);
       let mintegralK = true;
       let refreshborderless5 = false;
          let libfbf = false;
         clockp += `${((refreshborderless5 ? 1 : 5) << (Math.min(Math.abs(parseInt(`${rewardV}`)), 3)))}`;
         libfbf = !libfbf;
      final_to = liveshareo.length >= 76;
   let recommendationa = 7498510 >= field8.size;
   do {
       let feedbackv = String.fromCharCode(117,110,104,105,100,101,95,51,95,49,53,0);
       let basketballawayteamW = 1.0;
       let shielddonej = false;
       let whistlez: Array<any> = [634, 411];
       let iconorientationC = String.fromCharCode(112,103,109,120,95,115,95,49,53,0);
      if (!feedbackv.startsWith(`${shielddonej}`)) {
         feedbackv += "2";
      }
         basketballawayteamW *= parseFloat(`${2}`);
       let shootnogoalQ = 4;
         whistlez.push(2 * parseInt(`${basketballawayteamW}`));
      if (iconorientationC.length < 3) {
         iconorientationC += `${(String.fromCharCode(108,0) == feedbackv ? feedbackv.length : whistlez.length)}`;
      }
         iconorientationC = `${parseInt(`${basketballawayteamW}`) + 1}`;
         shootnogoalQ <<= Math.min(2, Math.abs(whistlez.length >> (Math.min(4, Math.abs(parseInt(`${basketballawayteamW}`))))));
      if (iconorientationC.length > 4) {
         shielddonej = whistlez.length >= 24;
      }
         feedbackv = `${2 << (Math.min(2, whistlez.length))}`;
      if (whistlez.length >= 2) {
         shielddonej = String.fromCharCode(89,0) == iconorientationC;
      }
      if (3 > (feedbackv.length | 5) || 1 > (5 | feedbackv.length)) {
         feedbackv = `${shootnogoalQ}`;
      }
      for (let y = 0; y < 2; y++) {
         feedbackv += `${((shielddonej ? 3 : 1) - parseInt(`${basketballawayteamW}`))}`;
      }
      while ((3 & whistlez.length) >= 2) {
          let typingG = 3.0;
          let ballA = 3.0;
          let textlayoutmanagerU = 2.0;
          let middlewareY = 2.0;
         iconorientationC = "3";
         typingG /= Math.max(5, parseFloat(`${parseInt(`${middlewareY}`) & parseInt(`${textlayoutmanagerU}`)}`));
         ballA *= 2;
         textlayoutmanagerU -= parseInt(`${ballA}`);
         middlewareY /= Math.max(3, parseInt(`${textlayoutmanagerU}`));
         break;
      }
      let liveendmodallogoC = iconorientationC == String.fromCharCode(118,112,112,97,108,55,0);
      do {
         iconorientationC = "3";
         if (liveendmodallogoC) {
            break;
         }
      } while (liveendmodallogoC && (2 >= (whistlez.length * 4) && 3 >= (whistlez.length * 4)));
       let a_managerO = 2.0;
      field8.set(libswscalem, libswscalem.length / (Math.max(1, 7)));
      if (recommendationa) {
         break;
      }
   } while (((field8.size * wifirouteru.length) > 2) && recommendationa);
   for (let f = 0; f < 1; f++) {
      field8.set(`${rewardL}`, ((rewardL ? 4 : 5) + executorS.length));
   }
      libreactperfloggerjnir = new Map([[`${iconbellactiveb}`, 3 | iconbellactiveb]]);

    const { colors, textVariants } = useTheme();

   for (let q = 0; q < 1; q++) {
       let libreanimatedi: Array<any> = [244, 511];
       let pause0: Map<any, any> = new Map([[String.fromCharCode(118,115,121,110,99,95,52,95,56,52,0),705], [String.fromCharCode(114,95,49,95,110,117,108,108,97,98,108,101,114,101,115,111,108,118,101,100,0),849], [String.fromCharCode(109,98,108,111,111,112,95,120,95,57,50,0),80]]);
       let changeS = String.fromCharCode(119,95,55,55,95,103,101,116,115,105,103,99,116,120,110,111,0);
       let canceld = String.fromCharCode(111,112,116,105,109,105,122,101,100,95,120,95,53,50,0);
       let pathi = 4;
         canceld += `${1 >> (Math.min(2, canceld.length))}`;
       let episodeE = false;
         episodeE = canceld.length >= 29;
       let androidu = String.fromCharCode(108,105,98,103,115,109,0);
      for (let i = 0; i < 3; i++) {
          let libyoga7: Array<any> = [358, 212, 895];
         androidu = "3";
         libyoga7.push(2);
      }
      while (canceld == String.fromCharCode(102,0)) {
          let schedulerp = 2.0;
          let privacyn: Array<any> = [736, 958, 717];
          let sportY: Map<any, any> = new Map([[String.fromCharCode(98,97,116,99,104,101,100,95,49,95,57,50,0),String.fromCharCode(116,101,120,116,98,101,95,54,95,51,48,0)], [String.fromCharCode(121,97,98,101,95,99,95,49,50,0),String.fromCharCode(102,97,108,115,101,95,49,95,50,0)]]);
          let castingW = String.fromCharCode(101,120,116,114,97,95,119,95,50,53,0);
          let forml: Map<any, any> = new Map([[String.fromCharCode(100,98,108,113,117,111,116,101,95,51,95,49,0),String.fromCharCode(112,97,114,105,116,105,111,110,105,110,103,95,121,95,54,55,0)], [String.fromCharCode(112,114,111,102,105,108,105,110,103,95,48,95,51,54,0),String.fromCharCode(115,101,110,100,118,95,54,95,56,0)], [String.fromCharCode(116,97,103,115,0),String.fromCharCode(100,101,108,105,109,105,116,101,114,95,117,95,49,57,0)]]);
         androidu = `${forml.size ^ 2}`;
         schedulerp += (parseFloat(`${castingW == String.fromCharCode(88,0) ? privacyn.length : castingW.length}`));
         privacyn.push(privacyn.length);
         sportY = new Map([[castingW, parseInt(`${schedulerp}`)]]);
         forml = new Map([[`${privacyn.length}`, parseInt(`${schedulerp}`) % 1]]);
         break;
      }
       let resendR = String.fromCharCode(110,111,115,99,97,108,101,0);
          let friendsR = false;
          let dicelogom = String.fromCharCode(108,105,98,111,112,101,110,106,112,101,103,95,117,95,55,52,0);
         libreanimatedi.push(((friendsR ? 3 : 1) - changeS.length));
         friendsR = 40 <= dicelogom.length;
         dicelogom += `${1 * dicelogom.length}`;
      if ((pause0.size / 3) == 4 || 3 == (pause0.size / (Math.max(9, changeS.length)))) {
         changeS += `${canceld.length + 3}`;
      }
         resendR = "2";
      if (androidu.includes(`${pathi}`)) {
         androidu += `${canceld.length | 3}`;
      }
      while (resendR.length == canceld.length) {
          let matchinactivey: Array<any> = [115, 298];
          let whistleH = String.fromCharCode(105,95,53,48,95,102,108,111,97,116,0);
          let settingV = String.fromCharCode(122,95,52,95,104,114,97,109,0);
          let regenga = 2;
         canceld += `${pathi}`;
         matchinactivey.push(matchinactivey.length);
         whistleH = `${matchinactivey.length}`;
         settingV = `${matchinactivey.length - 1}`;
         regenga %= Math.max(4, settingV.length);
         break;
      }
      if (4 <= androidu.length && !episodeE) {
         episodeE = 64 >= canceld.length || changeS.length >= 64;
      }
      let goale = androidu.length <= 8369578;
      do {
          let smallsound5 = 5.0;
          let teamdetailsbgb: Array<any> = [661, 591, 66];
          let nativemoduleW = 2.0;
          let eventI = true;
          let internetf = String.fromCharCode(103,108,98,108,95,103,95,56,53,0);
         androidu += `${((eventI ? 1 : 3) / (Math.max(pathi, 2)))}`;
         smallsound5 += 2 & parseInt(`${smallsound5}`);
         teamdetailsbgb = [2];
         nativemoduleW -= parseFloat(`${parseInt(`${smallsound5}`)}`);
         eventI = internetf.length < 45 && 45 < teamdetailsbgb.length;
         internetf = `${(String.fromCharCode(114,0) == internetf ? internetf.length : parseInt(`${smallsound5}`))}`;
         if (goale) {
            break;
         }
      } while ((2 < pathi) && goale);
         pause0.set(canceld, pathi);
      field8.set(`${yellowcirclebgY}`, parseInt(`${yellowcirclebgY}`));
   }
      penaltygoalJ += parseFloat(`${iconbellactiveb}`);
      untick8 = `${wifirouteru.length}`;
   while (4 >= (libswscalem.length + 3)) {
      statisticsactiveB.set(`${wifirouteru.length}`, wifirouteru.length);
      break;
   }
   if (!rewardL) {
      wifirouteru = [statisticsactiveB.size % (Math.max(6, liveshareo.length))];
   }
   while (1 >= (4 + gemfile_.length) || 1 >= (4 + liveshareo.length)) {
      gemfile_ = `${iconbellactiveb << (Math.min(2, Math.abs(3)))}`;
      break;
   }
   if (field8.get(`${iconbellactiveb}`) == null) {
       let libavfilterl = String.fromCharCode(103,95,53,54,95,97,114,116,0);
       let modulem: Map<any, any> = new Map([[String.fromCharCode(121,95,50,48,95,117,110,100,111,116,116,101,100,0),120], [String.fromCharCode(114,101,97,100,120,98,108,111,99,107,0),809], [String.fromCharCode(118,95,55,55,95,99,108,101,97,114,0),361]]);
       let downloadingM: Array<any> = [640, 707];
       let ping_: Map<any, any> = new Map([[String.fromCharCode(99,95,52,55,95,117,110,109,117,116,101,100,0),true ], [String.fromCharCode(102,105,102,111,115,0),true ]]);
         modulem.set(libavfilterl, (libavfilterl == String.fromCharCode(82,0) ? libavfilterl.length : downloadingM.length));
      if ((2 & downloadingM.length) <= 1 && (2 & downloadingM.length) <= 1) {
         ping_ = new Map([[`${modulem.size}`, (libavfilterl == String.fromCharCode(117,0) ? modulem.size : libavfilterl.length)]]);
      }
      let castingg = 6412948 >= ping_.size;
      do {
         ping_.set(`${downloadingM.length}`, downloadingM.length);
         if (castingg) {
            break;
         }
      } while (castingg && ((ping_.size / 3) == 5));
      if (4 > (2 - downloadingM.length) && 2 > (ping_.size - downloadingM.length)) {
         downloadingM.push((String.fromCharCode(50,0) == libavfilterl ? modulem.size : libavfilterl.length));
      }
         libavfilterl = `${ping_.size}`;
      let expandU = modulem.size <= 5798911;
      do {
          let libreact7 = String.fromCharCode(103,95,49,56,95,97,101,99,0);
         modulem.set(libavfilterl, 1);
         libreact7 += `${(libreact7 == String.fromCharCode(106,0) ? libreact7.length : libreact7.length)}`;
         if (expandU) {
            break;
         }
      } while ((2 >= libavfilterl.length) && expandU);
          let chinam = 3;
          let arrowrightwithtail5: Map<any, any> = new Map([[String.fromCharCode(118,95,53,52,95,100,105,115,109,105,115,115,101,100,0),true ], [String.fromCharCode(115,117,98,115,116,114,105,110,103,95,97,95,57,51,0),false ], [String.fromCharCode(104,95,55,49,95,97,98,111,114,116,0),true ]]);
          let history3: Array<any> = [197, 120, 343];
         libavfilterl = `${ping_.size}`;
         chinam -= chinam;
         arrowrightwithtail5.set(`${chinam}`, chinam >> (Math.min(Math.abs(arrowrightwithtail5.size), 3)));
         history3 = [1];
      while (libavfilterl.startsWith(`${modulem.size}`)) {
         modulem.set(libavfilterl, 3);
         break;
      }
          let footballe: Map<any, any> = new Map([[String.fromCharCode(105,109,109,117,116,97,98,108,101,0),String.fromCharCode(101,112,105,115,111,100,101,95,108,95,48,0)], [String.fromCharCode(100,111,117,98,108,101,105,110,116,115,116,114,95,111,95,51,48,0),String.fromCharCode(105,100,101,110,116,105,116,105,101,115,95,121,95,56,52,0)], [String.fromCharCode(115,95,50,51,95,108,115,112,108,112,99,0),String.fromCharCode(98,97,110,100,119,105,100,116,104,95,50,95,50,49,0)]]);
          let taiwan0: Map<any, any> = new Map([[String.fromCharCode(109,95,49,54,95,115,101,103,102,101,97,116,117,114,101,115,0),510], [String.fromCharCode(116,105,102,102,95,56,95,52,56,0),631], [String.fromCharCode(100,117,112,101,100,0),900]]);
          let iconwechata = 5.0;
         modulem = new Map([[`${taiwan0.size}`, 2 * parseInt(`${iconwechata}`)]]);
         footballe.set(`${footballe.size}`, footballe.size);
         taiwan0.set(`${footballe.size}`, footballe.size);
       let resultZ = 1.0;
       let checkboxb = 5.0;
         modulem.set(libavfilterl, modulem.size | libavfilterl.length);
      while (modulem.size <= 3) {
         modulem.set(`${checkboxb}`, 1);
         break;
      }
      iconbellactiveb ^= parseInt(`${actionj}`) >> (Math.min(1, Math.abs(2)));
   }
   let singleV = statisticsactiveB.size >= 8313324;
   do {
       let basketballplayerplaceholder_ = String.fromCharCode(115,119,97,112,112,97,98,108,101,95,51,95,56,56,0);
         basketballplayerplaceholder_ = `${basketballplayerplaceholder_.length ^ 3}`;
         basketballplayerplaceholder_ += `${basketballplayerplaceholder_.length}`;
         basketballplayerplaceholder_ += "1";
      statisticsactiveB = new Map([[`${wifirouteru.length}`, libswscalem.length + wifirouteru.length]]);
      if (singleV) {
         break;
      }
   } while ((field8.size <= statisticsactiveB.size) && singleV);
      wifirouteru.push(wifirouteru.length - 3);
   for (let f = 0; f < 3; f++) {
       let yellowscoreball4 = 5.0;
       let nodee = 1.0;
       let typingloadingm = 0.0;
       let questiconO = String.fromCharCode(102,95,54,49,95,115,97,116,100,0);
         typingloadingm *= parseFloat(`${1 % (Math.max(10, parseInt(`${yellowscoreball4}`)))}`);
      let volumeP = 8461257 <= questiconO.length;
      do {
          let text3 = String.fromCharCode(108,111,103,115,97,109,112,108,101,95,49,95,48,0);
          let canvasm = 4.0;
          let logoutp = 2.0;
          let reducerH: Array<any> = [625, 483];
          let giftG = String.fromCharCode(115,117,98,116,114,97,99,116,95,110,95,57,50,0);
         questiconO += `${(String.fromCharCode(88,0) == questiconO ? parseInt(`${nodee}`) : questiconO.length)}`;
         text3 = "1";
         canvasm -= parseFloat(`${reducerH.length}`);
         logoutp /= Math.max(1, parseInt(`${canvasm}`) * 1);
         reducerH = [3];
         giftG = `${2 | giftG.length}`;
         if (volumeP) {
            break;
         }
      } while (volumeP && ((4.49 + yellowscoreball4) <= 3.28));
          let tempD: Array<any> = [749, 423];
          let controls6: Array<any> = [866, 347, 912];
         typingloadingm += parseFloat(`${tempD.length % (Math.max(3, questiconO.length))}`);
         tempD.push(1 + controls6.length);
         controls6 = [controls6.length];
          let privilegeA = String.fromCharCode(116,105,112,95,99,95,57,49,0);
         yellowscoreball4 /= Math.max(2, parseFloat(`${2 << (Math.min(2, questiconO.length))}`));
         privilegeA += "2";
          let modityf: Map<any, any> = new Map([[String.fromCharCode(109,115,114,108,101,95,55,95,50,48,0),String.fromCharCode(105,110,118,105,116,97,116,105,111,110,115,95,119,95,52,50,0)], [String.fromCharCode(105,95,57,56,95,98,108,111,99,107,115,0),String.fromCharCode(107,95,50,0)], [String.fromCharCode(102,105,110,97,108,100,95,113,95,57,57,0),String.fromCharCode(114,95,52,48,95,102,100,99,116,120,0)]]);
          let customy = 0;
          let dependenciesA = String.fromCharCode(121,95,55,53,95,108,117,109,98,101,114,106,97,99,107,0);
         yellowscoreball4 *= parseFloat(`${modityf.size >> (Math.min(dependenciesA.length, 3))}`);
         modityf = new Map([[`${customy}`, customy]]);
      if ((1 ^ questiconO.length) < 3 || (questiconO.length / (Math.max(1, 8))) < 3) {
         typingloadingm *= parseFloat(`${3}`);
      }
         typingloadingm -= parseFloat(`${questiconO.length}`);
      if ((typingloadingm / (Math.max(7, yellowscoreball4))) > 1.59 || 3.29 > (1.59 / (Math.max(7, typingloadingm)))) {
         typingloadingm += parseFloat(`${questiconO.length}`);
      }
         nodee += parseFloat(`${questiconO.length / (Math.max(6, parseInt(`${nodee}`)))}`);
         nodee *= parseFloat(`${parseInt(`${yellowscoreball4}`) / 3}`);
          let airbnbstarU: Array<any> = [895, 397];
         questiconO = `${(questiconO == String.fromCharCode(117,0) ? questiconO.length : parseInt(`${yellowscoreball4}`))}`;
         airbnbstarU.push(3 & airbnbstarU.length);
      let holdery = 5423152.0 >= yellowscoreball4;
      do {
         yellowscoreball4 += parseFloat(`${3 >> (Math.min(Math.abs(parseInt(`${nodee}`)), 2))}`);
         if (holdery) {
            break;
         }
      } while (holdery && ((4 + nodee) == 5.26));
      arrowselectdown1 = iconbellactiveb >= 33 || 33 >= wifirouteru.length;
   }
      field8.set(`${rewardL}`, executorS.length);
   let othermatchdetailbgG = actionj >= 9260072.0;
   do {
      actionj /= Math.max(3, parseFloat(`${1}`));
      if (othermatchdetailbgG) {
         break;
      }
   } while (((statisticsactiveB.size >> (Math.min(Math.abs(2), 3))) >= 1) && othermatchdetailbgG);
   for (let d = 0; d < 2; d++) {
      untick8 += `${parseInt(`${yellowcirclebgY}`) | 2}`;
   }

    return (
        <View style={{
            position: 'absolute',
            width: '100%',
            height: '80%',
            alignSelf: 'center',
            justifyContent: 'center',
            alignItems: 'center',
        }}>
            <View style={{
                backgroundColor: colors.background + 'C8',
                padding: 20,
                borderRadius: 20,
                width: '80%',
                justifyContent: 'center',
                alignItems: 'center',
            }}>
                <FastImage
                    useFastImage={true}
                    style={{
                        height: 80,
                        width: 80,
                        marginRight: 5,
                        position: "relative",
                        top: 1,
                        justifyContent: "center",
                        alignItems: "center",
                        marginBottom: 20,
                    }}
                    resizeMode={"contain"}
                    source={require("@static/images/profile/checkboxRefresh.gif")}
                />

                <Text style={{
                    ...textVariants.header,
                    marginBottom: 20,
                }}>
                    上传成功
                </Text>

                <Text style={{
                    ...textVariants.body,
                    textAlign: 'center',
                    marginBottom: 10,
                }}>
                    我们将进行内容审核，24小时内返回审核结果
                </Text>
            </View>
        </View>
    );
}