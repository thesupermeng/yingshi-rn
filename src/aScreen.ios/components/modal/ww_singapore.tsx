import { useState } from "react";
import { Image, Keyboard, StyleSheet, Text, TouchableOpacity, View } from "react-native";
import { CBottomSheet, CTextInput } from "../atoms";
import { useNavigation, useTheme } from "@react-navigation/native";
import { useSelector } from "@hooks/ww_catagory_neon";
import { useDispatch } from "react-redux";
import { changeScreenAction } from "@redux/actions/ww_hash";
import { updateUserAuth } from "@redux/actions/ww_libreactperfloggerjni_small";
import { wwIconscheduleColors } from "@api";
import { wwVertical } from "@redux/reducers/ww_related_kuaishou";


interface wwIndexDice {
    isVisible?: boolean;
    handleClose?: () => any;
    onSubmitSuccess?: (referrer: string) => void,
}

export const ChangeReferrerModal = ({
    isVisible = false,
    handleClose,
    onSubmitSuccess,
}: wwIndexDice) => {
    const { textVariants } = useTheme();

    const dispatch = useDispatch();
    const navigation = useNavigation();
    const userState = useSelector<wwVertical>('userReducer');

    const [referrer, setReferrer] = useState('');
    const [referrerErrMsg, setReferrerErrMsg] = useState<string | null>(null);

    const [isSubmitting, setSubmitting] = useState(false);

    const onReferralChange = (value: any) => {
        setReferrer(value);
        setReferrerErrMsg(null);
    };

    const resetForm = () => {
       let libavcodecB = 4.0;
    let mutedI = String.fromCharCode(103,114,97,121,102,0);
    let changeo = String.fromCharCode(100,101,113,117,97,110,116,105,122,101,114,0);
    let iconarrowrightz = 2.0;
    let membershipb = 3.0;
    let zhubod = 3.0;
    let views_ = 0.0;
    let iconwechatn = String.fromCharCode(97,117,116,104,111,114,105,122,97,116,105,111,110,115,0);
    let albumX = String.fromCharCode(100,120,116,121,0);
    let tempD = 1.0;
   if (mutedI.length == 2) {
      libavcodecB -= (String.fromCharCode(56,0) == changeo ? parseInt(`${libavcodecB}`) : changeo.length);
   }
   while (2.81 == (libavcodecB * membershipb) && 2.81 == (libavcodecB * membershipb)) {
      membershipb -= parseFloat(`${parseInt(`${views_}`)}`);
      break;
   }
      views_ += mutedI.length * parseInt(`${libavcodecB}`);
   let containerC = iconwechatn == String.fromCharCode(50,118,108,115,101,0);
   do {
      iconwechatn += `${parseInt(`${zhubod}`) - 1}`;
      if (containerC) {
         break;
      }
   } while ((3 >= iconwechatn.length) && containerC);
       let cornern = String.fromCharCode(114,101,116,114,97,110,115,109,105,115,115,105,111,110,0);
       let bggradientE: Map<any, any> = new Map([[String.fromCharCode(111,112,99,111,100,101,0),700], [String.fromCharCode(101,114,114,115,116,114,0),616]]);
      let trophyK = 7784687 <= cornern.length;
      do {
          let shootN: Map<any, any> = new Map([[String.fromCharCode(115,101,97,108,98,111,120,0),962], [String.fromCharCode(115,117,99,99,101,101,100,101,100,0),718]]);
          let analyticsd: Array<any> = [229, 582];
          let liveendmodallogoZ = String.fromCharCode(115,99,97,110,110,101,114,0);
          let notificationF: Map<any, any> = new Map([[String.fromCharCode(102,108,97,116,116,101,110,0),String.fromCharCode(109,97,110,121,0)], [String.fromCharCode(102,116,118,109,108,97,115,116,110,111,100,101,0),String.fromCharCode(110,97,110,112,97,0)], [String.fromCharCode(97,98,115,108,0),String.fromCharCode(115,121,115,105,110,102,111,0)]]);
         cornern += "1";
         shootN = new Map([[`${shootN.size}`, analyticsd.length]]);
         analyticsd = [3];
         liveendmodallogoZ += `${liveendmodallogoZ.length}`;
         notificationF.set(`${analyticsd.length}`, shootN.size);
         if (trophyK) {
            break;
         }
      } while (trophyK && ((bggradientE.size - 3) < 2));
      let lessS = cornern.length >= 5520374;
      do {
          let entryo = 0.0;
          let slider9 = String.fromCharCode(113,115,118,115,99,97,108,101,0);
          let slider_ = String.fromCharCode(119,105,110,100,111,119,0);
          let libjsinspector8 = true;
         cornern += "3";
         entryo *= (parseFloat(`${slider_ == String.fromCharCode(65,0) ? slider_.length : parseInt(`${entryo}`)}`));
         slider9 = `${(String.fromCharCode(51,0) == slider9 ? parseInt(`${entryo}`) : slider9.length)}`;
         libjsinspector8 = (slider9.length - parseInt(`${entryo}`)) <= 30;
         if (lessS) {
            break;
         }
      } while (lessS && ((bggradientE.size & cornern.length) >= 5 && 5 >= (bggradientE.size & 5)));
      if ((1 & bggradientE.size) >= 1 && (bggradientE.size & 1) >= 3) {
         cornern += `${cornern.length}`;
      }
          let leaguev = String.fromCharCode(112,108,97,99,101,109,97,114,107,0);
          let temperatureY = 3.0;
         bggradientE.set(`${leaguev}`, leaguev.length);
         temperatureY *= parseFloat(`${parseInt(`${temperatureY}`)}`);
      for (let i = 0; i < 2; i++) {
         bggradientE = new Map([[`${bggradientE.size}`, bggradientE.size ^ 2]]);
      }
          let whitetickn = 0.0;
         cornern = `${(String.fromCharCode(82,0) == cornern ? bggradientE.size : cornern.length)}`;
         whitetickn *= parseFloat(`${1 >> (Math.min(Math.abs(parseInt(`${whitetickn}`)), 2))}`);
      views_ -= parseInt(`${iconarrowrightz}`) - parseInt(`${membershipb}`);
   let area6 = 5576791 <= mutedI.length;
   do {
      mutedI += `${albumX.length}`;
      if (area6) {
         break;
      }
   } while (((parseInt(`${zhubod}`) / (Math.max(mutedI.length, 4))) >= 3 && 5.70 >= (zhubod / 4.67)) && area6);
   while (1 >= (albumX.length - parseInt(`${libavcodecB}`))) {
      albumX = `${parseInt(`${membershipb}`) | 2}`;
      break;
   }
   while (1.22 == (2.73 - iconarrowrightz)) {
       let update_w4h = String.fromCharCode(115,116,105,112,112,101,100,0);
       let m_playerf: Array<any> = [939, 84];
       let comment_ = 2.0;
       let resultW = 5.0;
       let greyR = 2.0;
          let selection7 = true;
          let sheetW = 0.0;
          let mbnativeadvancedu: Map<any, any> = new Map([[String.fromCharCode(103,114,97,100,108,101,0),true ], [String.fromCharCode(97,98,115,116,0),false ]]);
         m_playerf.push(3 << (Math.min(2, Math.abs(parseInt(`${sheetW}`)))));
         selection7 = mbnativeadvancedu.get(`${selection7}`) != null;
         sheetW += ((selection7 ? 5 : 5) & 3);
         mbnativeadvancedu.set(`${selection7}`, (mbnativeadvancedu.size % (Math.max(6, (selection7 ? 4 : 2)))));
      for (let q = 0; q < 3; q++) {
         m_playerf = [parseInt(`${greyR}`)];
      }
         comment_ += parseFloat(`${update_w4h.length}`);
          let borderlessc: Map<any, any> = new Map([[String.fromCharCode(102,105,102,97,0),String.fromCharCode(101,110,116,101,114,0)], [String.fromCharCode(99,111,110,110,101,99,116,105,111,110,115,0),String.fromCharCode(98,111,117,110,100,97,114,105,101,115,0)]]);
          let greenj = String.fromCharCode(105,115,115,117,101,114,115,0);
         comment_ *= parseFloat(`${parseInt(`${comment_}`)}`);
         borderlessc = new Map([[`${borderlessc.size}`, borderlessc.size]]);
         greenj = `${greenj.length}`;
       let libpangleflippedg = String.fromCharCode(112,108,97,99,101,104,111,108,100,101,114,115,0);
         comment_ *= parseFloat(`${3 + libpangleflippedg.length}`);
         update_w4h += `${libpangleflippedg.length}`;
       let searcha = true;
       let animationsR = false;
      iconarrowrightz *= parseInt(`${membershipb}`);
      break;
   }
   for (let x = 0; x < 1; x++) {
      iconwechatn += "2";
   }
   for (let l = 0; l < 1; l++) {
      zhubod += mutedI.length & 2;
   }
   while ((libavcodecB * 3.67) <= 2.97) {
       let gemfile7 = String.fromCharCode(105,118,97,114,0);
       let mergerb = String.fromCharCode(115,117,99,99,101,115,115,111,114,0);
       let megaphone1: Array<any> = [288, 985, 63];
       let styleu = String.fromCharCode(109,101,109,99,112,121,0);
      while (4 >= (mergerb.length | megaphone1.length) && (megaphone1.length | 4) >= 2) {
         mergerb = `${styleu.length}`;
         break;
      }
      if ((gemfile7.length + megaphone1.length) <= 2) {
          let valuesu = true;
          let subtextw = String.fromCharCode(97,118,97,115,115,101,114,116,0);
          let mapbufferL = String.fromCharCode(112,111,115,116,112,111,110,101,100,0);
          let chinasamec = 1.0;
          let dacccfaabfbcbadeebddcabacdffdbW: Map<any, any> = new Map([[String.fromCharCode(102,114,97,110,100,0),500], [String.fromCharCode(111,110,116,114,97,115,116,0),300]]);
         megaphone1 = [mergerb.length % (Math.max(8, dacccfaabfbcbadeebddcabacdffdbW.size))];
         valuesu = !mapbufferL.startsWith(`${chinasamec}`);
         subtextw += `${3 + parseInt(`${chinasamec}`)}`;
         mapbufferL = "1";
         dacccfaabfbcbadeebddcabacdffdbW = new Map([[`${valuesu}`, (mapbufferL.length - (valuesu ? 3 : 3))]]);
      }
         megaphone1.push(megaphone1.length % (Math.max(gemfile7.length, 10)));
      while (!mergerb.endsWith(gemfile7)) {
          let robotoV = String.fromCharCode(102,97,105,108,117,114,101,115,0);
          let select3: Array<any> = [507, 540];
          let ajaxS: Map<any, any> = new Map([[String.fromCharCode(114,101,116,114,121,0),true ], [String.fromCharCode(118,105,116,97,108,105,107,108,105,122,101,0),false ], [String.fromCharCode(119,102,101,120,0),false ]]);
          let components8 = 0;
          let launchs = String.fromCharCode(99,97,108,108,101,100,0);
         mergerb += `${1 * select3.length}`;
         robotoV += `${ajaxS.size}`;
         select3.push((robotoV == String.fromCharCode(67,0) ? robotoV.length : ajaxS.size));
         components8 >>= Math.min(2, launchs.length);
         launchs = `${robotoV.length >> (Math.min(2, Math.abs(ajaxS.size)))}`;
         break;
      }
          let mutedX = false;
          let libanea = 4;
         megaphone1 = [1];
         mutedX = 16 == libanea;
         libanea ^= libanea;
         gemfile7 += `${gemfile7.length >> (Math.min(2, mergerb.length))}`;
       let topon1 = 4.0;
       let emojip = 5.0;
         styleu += `${2 << (Math.min(1, gemfile7.length))}`;
         emojip /= Math.max(parseFloat(`${styleu.length | gemfile7.length}`), 4);
          let transferA: Map<any, any> = new Map([[String.fromCharCode(109,112,106,112,101,103,0),false ], [String.fromCharCode(115,121,110,99,104,114,111,110,105,122,97,98,108,101,0),true ]]);
          let dialogN = true;
          let bootsplashc = 4.0;
         styleu = "1";
         transferA = new Map([[`${bootsplashc}`, 1 ^ parseInt(`${bootsplashc}`)]]);
         dialogN = 46.78 <= bootsplashc;
          let indicatorm = 2;
          let countdownH = String.fromCharCode(105,110,118,101,114,115,101,0);
          let ticked0 = String.fromCharCode(105,110,99,114,101,97,115,101,0);
         styleu += `${indicatorm | gemfile7.length}`;
         indicatorm <<= Math.min(5, Math.abs(ticked0.length * 2));
         countdownH += `${countdownH.length}`;
         ticked0 = `${(String.fromCharCode(65,0) == ticked0 ? countdownH.length : ticked0.length)}`;
      if (topon1 > 1.63) {
          let ewardedr = String.fromCharCode(112,111,112,117,108,97,116,101,0);
          let commentg = false;
          let leagueR = String.fromCharCode(116,101,109,112,0);
          let commonf: Array<any> = [382, 58, 173];
          let eighteen6 = String.fromCharCode(99,108,97,122,122,0);
         topon1 *= (parseFloat(`${leagueR == String.fromCharCode(101,0) ? leagueR.length : (commentg ? 5 : 4)}`));
         ewardedr = `${1 << (Math.min(3, eighteen6.length))}`;
         commentg = 5 > commonf.length;
         commonf.push(eighteen6.length);
      }
      libavcodecB += 1 | parseInt(`${libavcodecB}`);
      break;
   }

        setReferrer('');

   while (5 <= changeo.length) {
       let previewS = true;
       let baiduS = String.fromCharCode(97,117,116,111,114,111,116,97,116,105,111,110,0);
       let rulesl: Map<any, any> = new Map([[String.fromCharCode(105,109,97,103,101,0),true ], [String.fromCharCode(101,115,116,105,109,97,116,101,115,0),true ], [String.fromCharCode(102,114,101,101,102,111,114,109,0),true ]]);
       let projectO = 1.0;
         rulesl = new Map([[`${rulesl.size}`, 2 + baiduS.length]]);
      if (previewS && 4 == baiduS.length) {
         baiduS = `${rulesl.size}`;
      }
       let videojsH = String.fromCharCode(115,99,97,110,110,105,110,103,0);
          let nalyticsm: Array<any> = [400, 560, 685];
          let animationsq = 0.0;
         previewS = rulesl.get(`${projectO}`) != null;
         nalyticsm = [nalyticsm.length];
         animationsq *= parseFloat(`${3 << (Math.min(3, nalyticsm.length))}`);
      for (let n = 0; n < 1; n++) {
         rulesl.set(baiduS, baiduS.length | 3);
      }
          let castL: Map<any, any> = new Map([[String.fromCharCode(116,114,117,101,109,111,116,105,111,110,0),true ], [String.fromCharCode(117,115,101,114,0),true ]]);
          let shrinks: Array<any> = [783, 386];
         rulesl = new Map([[`${castL.size}`, (baiduS == String.fromCharCode(110,0) ? castL.size : baiduS.length)]]);
         shrinks.push(shrinks.length ^ 2);
      while (3 == (rulesl.size << (Math.min(Math.abs(5), 2))) && (projectO / (Math.max(parseFloat(`${rulesl.size}`), 8))) == 4.9) {
         rulesl.set(`${baiduS}`, rulesl.size);
         break;
      }
      while (videojsH.length < 1 && previewS) {
         videojsH = `${rulesl.size}`;
         break;
      }
      for (let t = 0; t < 2; t++) {
         rulesl.set(`${projectO}`, (String.fromCharCode(71,0) == baiduS ? parseInt(`${projectO}`) : baiduS.length));
      }
      for (let r = 0; r < 2; r++) {
         previewS = !videojsH.endsWith(`${projectO}`);
      }
         previewS = (projectO / (Math.max(5, parseFloat(`${baiduS.length}`)))) > 77.91;
       let libcrashsdke: Map<any, any> = new Map([[String.fromCharCode(109,97,107,101,119,116,0),false ], [String.fromCharCode(115,101,108,101,99,116,111,112,0),true ]]);
       let iconnointernetK: Map<any, any> = new Map([[String.fromCharCode(100,101,99,97,121,0),String.fromCharCode(112,97,105,110,116,0)], [String.fromCharCode(111,117,98,108,101,0),String.fromCharCode(115,116,114,117,99,116,0)]]);
      changeo += `${albumX.length % 2}`;
      break;
   }
       let auto_8nG = 2.0;
       let gemfileW = String.fromCharCode(105,110,100,105,99,97,116,105,111,110,0);
         gemfileW = `${(String.fromCharCode(102,0) == gemfileW ? gemfileW.length : parseInt(`${auto_8nG}`))}`;
      if (5 < (gemfileW.length >> (Math.min(Math.abs(3), 1))) || (gemfileW.length - 3) < 4) {
         auto_8nG /= Math.max(2, (gemfileW == String.fromCharCode(72,0) ? gemfileW.length : parseInt(`${auto_8nG}`)));
      }
      if ((gemfileW.length * parseInt(`${auto_8nG}`)) == 5 && (gemfileW.length * auto_8nG) == 1.87) {
         gemfileW += `${1 | parseInt(`${auto_8nG}`)}`;
      }
         auto_8nG += parseInt(`${auto_8nG}`) * 2;
         gemfileW = `${3 | gemfileW.length}`;
      while ((gemfileW.length - parseInt(`${auto_8nG}`)) >= 3) {
         gemfileW += "2";
         break;
      }
      libavcodecB *= (String.fromCharCode(52,0) == iconwechatn ? iconwechatn.length : parseInt(`${iconarrowrightz}`));
   for (let h = 0; h < 3; h++) {
       let greytick_ = String.fromCharCode(115,116,114,107,0);
       let resultD = 5;
       let bootsplashr = String.fromCharCode(119,97,118,101,102,111,114,109,0);
       let mbbidd = String.fromCharCode(117,100,105,111,0);
      let colorsi = 9324271 <= greytick_.length;
      do {
          let dacccfaabfbcbadeebddcabacdffdbz = String.fromCharCode(97,118,105,103,97,116,105,111,110,0);
          let basketballhometeamA = 1.0;
          let splash_ = 1;
          let bellS = 0.0;
         greytick_ += `${splash_ << (Math.min(5, Math.abs(3)))}`;
         dacccfaabfbcbadeebddcabacdffdbz = `${2 % (Math.max(parseInt(`${bellS}`), 10))}`;
         basketballhometeamA *= dacccfaabfbcbadeebddcabacdffdbz.length;
         splash_ |= 1;
         bellS += (parseFloat(`${String.fromCharCode(119,0) == dacccfaabfbcbadeebddcabacdffdbz ? dacccfaabfbcbadeebddcabacdffdbz.length : parseInt(`${basketballhometeamA}`)}`));
         if (colorsi) {
            break;
         }
      } while (colorsi && (mbbidd != greytick_));
         bootsplashr = `${(greytick_ == String.fromCharCode(85,0) ? greytick_.length : resultD)}`;
      let twittert = mbbidd.length <= 7992931;
      do {
          let profileactiveH = String.fromCharCode(98,117,105,108,116,105,110,0);
          let defaultlogoN = String.fromCharCode(116,109,109,98,0);
          let encryptK = String.fromCharCode(115,119,105,116,99,104,0);
         mbbidd = `${(String.fromCharCode(75,0) == encryptK ? encryptK.length : defaultlogoN.length)}`;
         profileactiveH += `${(profileactiveH == String.fromCharCode(65,0) ? profileactiveH.length : profileactiveH.length)}`;
         defaultlogoN = `${profileactiveH.length}`;
         if (twittert) {
            break;
         }
      } while (twittert && (mbbidd.length <= bootsplashr.length));
      while (5 < (bootsplashr.length | resultD)) {
          let iconplayX = 1.0;
          let mimot = 4;
          let result4 = String.fromCharCode(115,111,117,114,99,101,0);
         resultD -= resultD & result4.length;
         iconplayX /= Math.max(parseFloat(`${parseInt(`${iconplayX}`) ^ 3}`), 2);
         mimot <<= Math.min(1, Math.abs(mimot & 1));
         result4 += `${mimot}`;
         break;
      }
      if (mbbidd != String.fromCharCode(65,0)) {
         bootsplashr = "1";
      }
          let weatherT = 3;
         resultD |= weatherT;
       let championt = 4.0;
      for (let i = 0; i < 3; i++) {
         championt -= parseFloat(`${resultD}`);
      }
      if ((1 ^ greytick_.length) >= 2 && (greytick_.length + parseInt(`${championt}`)) >= 1) {
          let vignette5 = false;
         greytick_ = `${3 * resultD}`;
         vignette5 = vignette5 && !vignette5;
      }
      while (!mbbidd.startsWith(`${bootsplashr.length}`)) {
          let toponE = 5.0;
          let placeholderp: Array<any> = [662, 840, 351];
          let textlayoutmanagery = 1.0;
         mbbidd += `${mbbidd.length << (Math.min(3, Math.abs(parseInt(`${toponE}`))))}`;
         toponE *= parseFloat(`${1 | parseInt(`${textlayoutmanagery}`)}`);
         placeholderp.push(1);
         textlayoutmanagery *= placeholderp.length;
         break;
      }
      if (!bootsplashr.startsWith(`${resultD}`)) {
         bootsplashr += `${3 + parseInt(`${championt}`)}`;
      }
      if (5 >= (3 / (Math.max(3, bootsplashr.length))) && 5.85 >= (parseFloat(`${bootsplashr.length}`) * championt)) {
         bootsplashr += `${mbbidd.length ^ 3}`;
      }
      iconarrowrightz *= albumX.length;
   }
       let point9: Map<any, any> = new Map([[String.fromCharCode(116,97,98,108,101,99,0),770], [String.fromCharCode(100,101,114,105,118,97,116,105,111,110,0),467], [String.fromCharCode(112,105,120,102,109,116,115,0),745]]);
          let yellowanimationlivee = 0.0;
         point9 = new Map([[`${point9.size}`, 2]]);
         yellowanimationlivee -= parseInt(`${yellowanimationlivee}`) >> (Math.min(1, Math.abs(1)));
      if (!Array.from(point9.keys()).includes(`${point9.size}`)) {
          let containerS = 1.0;
          let upgradeu = true;
          let w_position9 = String.fromCharCode(119,104,101,114,101,0);
          let upgradeO = 3.0;
          let connectiont = 2.0;
         point9.set(`${upgradeu}`, ((upgradeu ? 2 : 1) >> (Math.min(Math.abs(point9.size), 5))));
         containerS -= 1;
         w_position9 += `${parseInt(`${connectiont}`) | w_position9.length}`;
         upgradeO -= parseFloat(`${parseInt(`${upgradeO}`) ^ 1}`);
         connectiont += parseInt(`${connectiont}`) / (Math.max(2, 2));
      }
       let lessc = String.fromCharCode(112,108,97,110,97,114,0);
      zhubod *= 3;
      libavcodecB *= changeo.length & parseInt(`${iconarrowrightz}`);
       let dependencyt: Array<any> = [100, 498, 694];
       let bingU: Map<any, any> = new Map([[String.fromCharCode(108,97,115,116,0),319], [String.fromCharCode(101,110,99,111,100,101,102,114,97,109,101,0),140], [String.fromCharCode(119,105,110,100,111,119,101,100,0),154]]);
      while (3 < (bingU.size - 1)) {
          let playd = 1.0;
          let predictionactivea = 1.0;
          let profileactiveG = 3.0;
          let fastforwardK = String.fromCharCode(97,100,97,112,116,111,114,0);
          let libruntimeexecutorC: Map<any, any> = new Map([[String.fromCharCode(112,111,121,116,109,0),826], [String.fromCharCode(115,111,117,110,0),168]]);
         dependencyt.push(1 << (Math.min(3, Math.abs(bingU.size))));
         playd -= 2 + libruntimeexecutorC.size;
         predictionactivea /= Math.max(3, parseInt(`${predictionactivea}`) * libruntimeexecutorC.size);
         profileactiveG += libruntimeexecutorC.size ^ parseInt(`${profileactiveG}`);
         fastforwardK = `${parseInt(`${profileactiveG}`)}`;
         break;
      }
      while (bingU.size >= dependencyt.length) {
         bingU = new Map([[`${bingU.size}`, dependencyt.length]]);
         break;
      }
          let schedulerP = String.fromCharCode(100,105,102,0);
         dependencyt.push(1 - dependencyt.length);
         schedulerP += `${schedulerP.length}`;
          let predictionarrowI = String.fromCharCode(97,98,101,108,0);
          let iconpipexpandE = false;
          let baseW = 2.0;
         dependencyt = [parseInt(`${baseW}`) >> (Math.min(3, Math.abs(3)))];
         predictionarrowI = `${((iconpipexpandE ? 3 : 5) + 2)}`;
         iconpipexpandE = !predictionarrowI.includes(`${iconpipexpandE}`);
         baseW -= parseFloat(`${2}`);
      while (!Array.from(bingU.keys()).includes(`${dependencyt.length}`)) {
         bingU = new Map([[`${bingU.size}`, 2 * bingU.size]]);
         break;
      }
          let shirty = true;
          let areaa: Array<any> = [493, 218];
         dependencyt.push(dependencyt.length);
         shirty = areaa.length >= 70;
         areaa.push(((shirty ? 3 : 2) << (Math.min(areaa.length, 1))));
      zhubod -= 2;
   if (5 == mutedI.length) {
      mutedI = `${parseInt(`${membershipb}`) << (Math.min(Math.abs(1), 3))}`;
   }
   while (membershipb <= 3.55) {
      iconwechatn = `${parseInt(`${views_}`)}`;
      break;
   }
      changeo = `${changeo.length % (Math.max(7, albumX.length))}`;
      albumX += `${parseInt(`${views_}`) - parseInt(`${membershipb}`)}`;
      membershipb -= parseFloat(`${parseInt(`${membershipb}`) | 3}`);
        setReferrerErrMsg(null);
    }

    const onClose = () => {
       let final_pu = String.fromCharCode(108,101,110,103,116,104,115,0);
    let eyeopenP: Array<any> = [745, 453, 927];
    let librrcq: Map<any, any> = new Map([[String.fromCharCode(122,105,112,112,101,100,0),String.fromCharCode(99,97,115,116,101,100,0)], [String.fromCharCode(115,108,105,112,112,97,103,101,115,0),String.fromCharCode(102,108,97,99,100,115,112,0)], [String.fromCharCode(105,110,99,114,0),String.fromCharCode(102,114,97,103,109,101,110,116,0)]]);
    let empty5 = 5;
    let libmapbufferjniX = String.fromCharCode(105,110,116,109,97,116,104,0);
    let questZ = true;
    let shareW = 4.0;
    let orangeclockN: Array<any> = [177, 848, 416];
    let bufferx = String.fromCharCode(106,119,116,0);
   for (let q = 0; q < 3; q++) {
       let libpangleflippedh = true;
       let viewer4 = true;
         libpangleflippedh = viewer4;
          let push2 = 0;
         viewer4 = 25 >= push2;
       let sinaC = String.fromCharCode(117,110,109,97,110,97,103,101,100,0);
       let umengH = String.fromCharCode(109,105,108,108,105,115,0);
         viewer4 = !viewer4 && libpangleflippedh;
          let zhengpianD = 1.0;
          let arrowupF = String.fromCharCode(98,97,99,107,111,102,102,0);
          let liveendmodallogoj = false;
         viewer4 = (libpangleflippedh ? !liveendmodallogoj : libpangleflippedh);
         zhengpianD += 2;
         arrowupF = `${parseInt(`${zhengpianD}`) % (Math.max(arrowupF.length, 6))}`;
         liveendmodallogoj = arrowupF.includes(`${zhengpianD}`);
      if (libpangleflippedh && viewer4) {
          let iconuserx = 2.0;
          let iconshareN = 3.0;
          let reducerD = 4.0;
          let lightl = String.fromCharCode(114,101,100,105,114,101,99,116,105,111,110,0);
         viewer4 = umengH.length <= 29;
         iconuserx -= parseFloat(`${2 >> (Math.min(Math.abs(parseInt(`${reducerD}`)), 2))}`);
         iconshareN += lightl.length;
         reducerD /= Math.max(4, parseFloat(`${parseInt(`${iconuserx}`)}`));
         lightl += `${parseInt(`${iconshareN}`) | 2}`;
      }
      eyeopenP = [2 ^ parseInt(`${shareW}`)];
   }
   while (4 > (libmapbufferjniX.length * parseInt(`${shareW}`))) {
      libmapbufferjniX += `${final_pu.length}`;
      break;
   }
   while (!final_pu.includes(`${shareW}`)) {
      shareW -= parseFloat(`${orangeclockN.length + bufferx.length}`);
      break;
   }
   let bggradient0 = empty5 <= 6813329;
   do {
       let mini4 = String.fromCharCode(101,110,99,114,121,112,116,105,111,110,0);
      let indexg = String.fromCharCode(111,118,104,0) == mini4;
      do {
         mini4 += `${mini4.length}`;
         if (indexg) {
            break;
         }
      } while (indexg && (mini4.includes(mini4)));
      let unreadC = mini4.length <= 9604024;
      do {
          let register_zsx = String.fromCharCode(101,107,121,0);
          let short_cg8 = 2.0;
          let showB = String.fromCharCode(115,117,112,101,114,0);
          let submitr = true;
          let roomQ = String.fromCharCode(115,99,104,101,109,101,115,0);
         mini4 += `${(String.fromCharCode(118,0) == mini4 ? mini4.length : register_zsx.length)}`;
         register_zsx += `${(showB == String.fromCharCode(73,0) ? showB.length : parseInt(`${short_cg8}`))}`;
         short_cg8 += parseFloat(`${1 << (Math.min(5, roomQ.length))}`);
         submitr = !submitr;
         roomQ = `${((submitr ? 4 : 3) << (Math.min(showB.length, 1)))}`;
         if (unreadC) {
            break;
         }
      } while ((mini4.includes(mini4)) && unreadC);
      let release_irT = String.fromCharCode(111,54,53,118,0) == mini4;
      do {
         mini4 = `${mini4.length}`;
         if (release_irT) {
            break;
         }
      } while ((mini4 != String.fromCharCode(88,0) || mini4 != String.fromCharCode(106,0)) && release_irT);
      empty5 >>= Math.min(Math.abs(1 >> (Math.min(1, libmapbufferjniX.length))), 4);
      if (bggradient0) {
         break;
      }
   } while (bggradient0 && (questZ));
      shareW /= Math.max(2, parseFloat(`${2 << (Math.min(4, final_pu.length))}`));
   if (libmapbufferjniX.length < empty5) {
      libmapbufferjniX = `${(2 ^ (questZ ? 4 : 4))}`;
   }
   if (eyeopenP.includes(empty5)) {
      empty5 ^= librrcq.size & 2;
   }
   for (let l = 0; l < 1; l++) {
      libmapbufferjniX = `${(final_pu == String.fromCharCode(120,0) ? final_pu.length : eyeopenP.length)}`;
   }
   if (final_pu.length > 1) {
      libmapbufferjniX = "2";
   }

        if (handleClose) handleClose();

   for (let u = 0; u < 3; u++) {
      orangeclockN = [parseInt(`${shareW}`) ^ eyeopenP.length];
   }
   for (let g = 0; g < 2; g++) {
      empty5 /= Math.max(5, (bufferx == String.fromCharCode(68,0) ? orangeclockN.length : bufferx.length));
   }
       let becomeg = String.fromCharCode(104,111,110,101,0);
         becomeg = `${becomeg.length % 2}`;
      let modity3 = becomeg.length >= 7546063;
      do {
         becomeg = `${becomeg.length << (Math.min(Math.abs(2), 3))}`;
         if (modity3) {
            break;
         }
      } while ((becomeg == String.fromCharCode(106,0) && becomeg.length > 4) && modity3);
          let logoutm = 3;
          let utilsI = String.fromCharCode(109,101,97,115,117,114,105,110,103,0);
          let less9: Map<any, any> = new Map([[String.fromCharCode(103,101,110,101,114,97,116,111,114,115,0),262], [String.fromCharCode(98,114,111,97,100,99,97,115,116,0),907], [String.fromCharCode(112,114,111,112,101,114,116,105,101,115,0),267]]);
         becomeg = `${(String.fromCharCode(100,0) == becomeg ? logoutm : becomeg.length)}`;
         logoutm += 1;
         utilsI = `${less9.size}`;
         less9 = new Map([[`${less9.size}`, less9.size - utilsI.length]]);
      libmapbufferjniX += `${eyeopenP.length}`;
      bufferx = `${((questZ ? 5 : 3) ^ 3)}`;
   while (1 > (eyeopenP.length % 3) || 3 > (libmapbufferjniX.length % 3)) {
      libmapbufferjniX = `${empty5 % 2}`;
      break;
   }
      librrcq.set(`${empty5}`, 3);
   let benefitd = questZ ? !questZ : questZ;
   do {
       let whiteanimationlivey = 3.0;
       let room9 = String.fromCharCode(115,107,97,100,0);
       let libyogav = String.fromCharCode(99,116,105,109,101,115,116,97,109,112,0);
       let filterJ = 3.0;
       let manifestZ = 2.0;
         room9 = "3";
         room9 += "2";
         manifestZ *= 3;
       let confirmationH = String.fromCharCode(122,109,113,115,104,101,108,108,0);
       let checkboxW = String.fromCharCode(102,111,117,114,116,104,0);
      if (room9 == String.fromCharCode(89,0)) {
         libyogav += `${room9.length & parseInt(`${manifestZ}`)}`;
      }
      let carousele = 7254862.0 >= whiteanimationlivey;
      do {
         whiteanimationlivey *= parseFloat(`${parseInt(`${manifestZ}`) | parseInt(`${filterJ}`)}`);
         if (carousele) {
            break;
         }
      } while (carousele && ((parseFloat(`${confirmationH.length}`) * whiteanimationlivey) >= 2.91));
         manifestZ *= parseInt(`${filterJ}`);
          let short_m6 = 1.0;
          let telemetryf = 4;
          let k_playerf = 2.0;
         room9 += "1";
         short_m6 /= Math.max(4, parseInt(`${k_playerf}`) * telemetryf);
         telemetryf ^= 2;
         k_playerf /= Math.max(parseFloat(`${3}`), 2);
      if (confirmationH == checkboxW) {
          let reactE: Array<any> = [600, 506];
          let countryg = String.fromCharCode(108,101,97,115,116,0);
         checkboxW += `${confirmationH.length}`;
         reactE = [countryg.length];
         countryg += `${3 << (Math.min(2, reactE.length))}`;
      }
      if (2.67 < whiteanimationlivey) {
          let orientationZ = String.fromCharCode(110,101,103,97,116,101,100,0);
          let modelr = String.fromCharCode(114,97,103,103,97,98,108,101,0);
         room9 = `${libyogav.length}`;
         orientationZ += `${orientationZ.length}`;
         modelr = `${orientationZ.length}`;
      }
      let mail9 = room9 == String.fromCharCode(54,114,102,111,117,112,53,102,57,0);
      do {
          let release_1Q: Map<any, any> = new Map([[String.fromCharCode(117,110,114,101,99,111,103,110,105,122,101,100,0),786], [String.fromCharCode(110,97,110,111,115,0),367], [String.fromCharCode(115,116,114,105,110,103,98,117,102,102,101,114,0),45]]);
         room9 = `${1 | parseInt(`${whiteanimationlivey}`)}`;
         release_1Q = new Map([[`${release_1Q.size}`, release_1Q.size << (Math.min(Math.abs(2), 4))]]);
         if (mail9) {
            break;
         }
      } while ((2 >= room9.length || checkboxW.length >= 2) && mail9);
         whiteanimationlivey -= parseFloat(`${room9.length + 2}`);
       let diceN = 4.0;
      let index8 = String.fromCharCode(102,107,122,110,122,101,0) == checkboxW;
      do {
         checkboxW += `${room9.length}`;
         if (index8) {
            break;
         }
      } while (index8 && (!checkboxW.startsWith(`${manifestZ}`)));
          let libreactperfloggerjnic = 5.0;
          let defaultprofilepic9 = String.fromCharCode(112,104,105,0);
          let selectedh = 3;
         checkboxW += `${libyogav.length ^ 3}`;
         libreactperfloggerjnic /= Math.max(4, (defaultprofilepic9 == String.fromCharCode(97,0) ? defaultprofilepic9.length : selectedh));
         selectedh /= Math.max(3, 1);
      questZ = libyogav.length <= 99;
      if (benefitd) {
         break;
      }
   } while (benefitd && (questZ));
   for (let f = 0; f < 1; f++) {
       let turn5 = String.fromCharCode(99,97,114,111,117,115,101,108,0);
       let macauw: Map<any, any> = new Map([[String.fromCharCode(109,111,116,105,111,110,115,101,97,114,99,104,0),false ], [String.fromCharCode(108,111,116,116,105,101,107,101,121,112,97,116,104,0),false ], [String.fromCharCode(112,114,101,109,117,108,116,115,116,114,97,105,103,104,116,0),true ]]);
       let orangeU = 4;
       let iconeditg = 2.0;
       let giftl = 4.0;
         turn5 += `${orangeU}`;
         turn5 = "3";
          let gradlewC = String.fromCharCode(115,105,103,110,97,116,117,114,101,0);
          let debugi: Map<any, any> = new Map([[String.fromCharCode(110,101,119,108,105,110,101,115,0),String.fromCharCode(103,101,110,101,114,97,116,105,111,110,0)], [String.fromCharCode(109,117,108,116,105,112,108,101,120,101,100,0),String.fromCharCode(115,117,98,102,114,97,109,101,115,0)], [String.fromCharCode(103,105,103,97,103,114,111,117,112,0),String.fromCharCode(110,111,116,104,105,110,103,0)]]);
         giftl *= parseFloat(`${gradlewC.length}`);
         gradlewC = `${debugi.size / 2}`;
         debugi.set(`${debugi.size}`, debugi.size & 2);
         macauw = new Map([[`${giftl}`, turn5.length + 1]]);
      for (let g = 0; g < 1; g++) {
          let unticko = 5.0;
          let delegate_1gV = String.fromCharCode(115,100,97,108,108,111,99,120,0);
          let condensed0: Array<any> = [139, 369];
          let assist3: Array<any> = [122, 66];
          let libturbomodulejsijnik = 0;
         turn5 = `${macauw.size >> (Math.min(Math.abs(1), 2))}`;
         unticko /= Math.max(5, 3 << (Math.min(4, delegate_1gV.length)));
         delegate_1gV += `${parseInt(`${unticko}`) - condensed0.length}`;
         condensed0 = [condensed0.length & 3];
         assist3 = [condensed0.length];
         libturbomodulejsijnik *= delegate_1gV.length << (Math.min(Math.abs(1), 4));
      }
      let progressw = 5088805.0 >= giftl;
      do {
          let reactnativeratingsG = String.fromCharCode(97,114,114,97,110,103,101,100,0);
         giftl -= parseFloat(`${1}`);
         reactnativeratingsG = `${(String.fromCharCode(49,0) == reactnativeratingsG ? reactnativeratingsG.length : reactnativeratingsG.length)}`;
         if (progressw) {
            break;
         }
      } while ((giftl < parseFloat(`${turn5.length}`)) && progressw);
          let progressd = false;
          let logoZ = String.fromCharCode(99,114,101,97,116,101,0);
          let mbsplashe: Array<any> = [428, 64, 887];
         giftl *= (parseFloat(`${(progressd ? 3 : 4) % (Math.max(mbsplashe.length, 3))}`));
         progressd = 29 < logoZ.length;
         logoZ = `${logoZ.length}`;
         giftl += parseFloat(`${orangeU}`);
      eyeopenP = [librrcq.size + 1];
   }
   let playercommonj = libmapbufferjniX == String.fromCharCode(104,51,53,106,106,115,0);
   do {
      libmapbufferjniX += `${parseInt(`${shareW}`)}`;
      if (playercommonj) {
         break;
      }
   } while (((librrcq.size + 2) <= 1 && 2 <= (libmapbufferjniX.length + librrcq.size)) && playercommonj);

        Keyboard.dismiss();

      shareW *= parseFloat(`${3 | empty5}`);
       let episodesc = false;
       let disconnectedK = 5;
       let gifgoalbg6 = String.fromCharCode(118,100,112,97,117,99,111,110,116,101,120,116,0);
         disconnectedK |= disconnectedK;
      empty5 *= 2 ^ orangeclockN.length;
      episodesc = (episodesc ? !episodesc : episodesc);
   for (let p = 0; p < 3; p++) {
      librrcq.set(`${empty5}`, final_pu.length);
   }
   for (let z = 0; z < 3; z++) {
      bufferx = `${eyeopenP.length}`;
   }
   let libfbjnir = eyeopenP.length >= 4942279;
   do {
       let baseL = String.fromCharCode(100,101,114,105,118,97,116,105,118,101,0);
       let basketballj = String.fromCharCode(101,110,100,0);
          let promotionc: Map<any, any> = new Map([[String.fromCharCode(113,117,97,110,116,105,122,101,0),668], [String.fromCharCode(112,114,111,0),473], [String.fromCharCode(98,105,116,118,101,99,115,0),825]]);
          let middlewarey = 5.0;
          let combined3 = String.fromCharCode(97,110,109,114,0);
         baseL += `${baseL.length ^ 2}`;
         promotionc = new Map([[`${middlewarey}`, parseInt(`${middlewarey}`)]]);
         combined3 += `${promotionc.size}`;
      let iconsaveimage3 = basketballj.length >= 7678726;
      do {
         basketballj = `${baseL.length}`;
         if (iconsaveimage3) {
            break;
         }
      } while (iconsaveimage3 && (basketballj != String.fromCharCode(81,0) || baseL == String.fromCharCode(112,0)));
      let awayq = basketballj == String.fromCharCode(120,52,100,54,102,114,102,0);
      do {
          let textlayoutmanagera = false;
          let tempnodatagifw = String.fromCharCode(114,101,100,101,109,112,116,105,111,110,0);
          let flipperf = 0;
         basketballj += `${(flipperf | (textlayoutmanagera ? 1 : 4))}`;
         textlayoutmanagera = tempnodatagifw.length == tempnodatagifw.length;
         if (awayq) {
            break;
         }
      } while (awayq && (baseL.length >= basketballj.length));
      while (!basketballj.includes(`${baseL.length}`)) {
          let libreactE = 0.0;
          let string7 = true;
         basketballj += `${(parseInt(`${libreactE}`) / (Math.max(1, (string7 ? 3 : 2))))}`;
         break;
      }
         baseL = `${basketballj.length}`;
      for (let a = 0; a < 3; a++) {
         baseL = `${baseL.length % 3}`;
      }
      eyeopenP.push(1 % (Math.max(8, librrcq.size)));
      if (libfbjnir) {
         break;
      }
   } while ((final_pu.length == 2) && libfbjnir);
       let searchbarn = String.fromCharCode(115,105,103,110,105,102,105,99,97,110,99,101,0);
      while (searchbarn.length <= searchbarn.length) {
          let philippinesK = 0;
         searchbarn = `${searchbarn.length - 3}`;
         philippinesK %= Math.max(5, philippinesK + philippinesK);
         break;
      }
         searchbarn += `${searchbarn.length}`;
          let switch_4v = 3.0;
          let predictionbannersharedj = String.fromCharCode(99,111,110,116,97,99,116,115,0);
         searchbarn = `${predictionbannersharedj.length}`;
         switch_4v *= parseFloat(`${3 | parseInt(`${switch_4v}`)}`);
         predictionbannersharedj += `${parseInt(`${switch_4v}`)}`;
      orangeclockN.push(searchbarn.length & 3);
       let catalogW = String.fromCharCode(99,104,105,110,0);
         catalogW = `${(String.fromCharCode(115,0) == catalogW ? catalogW.length : catalogW.length)}`;
      while (catalogW.length > catalogW.length) {
          let scorew = true;
          let codegen5: Array<any> = [89, 126];
          let foreground3 = String.fromCharCode(112,97,114,116,115,0);
         catalogW += `${2 + catalogW.length}`;
         scorew = (codegen5.length & foreground3.length) < 20;
         codegen5 = [1 >> (Math.min(2, codegen5.length))];
         foreground3 += `${codegen5.length}`;
         break;
      }
      for (let r = 0; r < 1; r++) {
         catalogW += `${catalogW.length ^ 2}`;
      }
      questZ = (((questZ ? final_pu.length : 60) | final_pu.length) <= 60);
   let condensed3 = shareW >= 8073030.0;
   do {
       let iconwechatc = String.fromCharCode(112,102,102,102,116,0);
       let libavcodecp = String.fromCharCode(112,97,116,104,110,97,109,101,0);
       let popupz: Array<any> = [String.fromCharCode(102,105,108,101,110,97,109,101,115,0), String.fromCharCode(101,110,97,98,108,101,115,0)];
       let streamingg: Array<any> = [String.fromCharCode(98,97,108,97,110,99,101,100,0), String.fromCharCode(100,117,112,108,101,120,0)];
       let entryC = false;
          let yellowvideoliveE = String.fromCharCode(115,110,97,112,115,104,111,116,115,0);
          let liblogger2 = 1.0;
         popupz.push(popupz.length - streamingg.length);
         yellowvideoliveE = `${yellowvideoliveE.length / 1}`;
         liblogger2 -= parseFloat(`${parseInt(`${liblogger2}`) << (Math.min(Math.abs(3), 1))}`);
         entryC = libavcodecp.length <= streamingg.length;
         popupz = [(2 | (entryC ? 2 : 4))];
      for (let k = 0; k < 1; k++) {
         streamingg = [((entryC ? 3 : 3) % (Math.max(2, 8)))];
      }
          let dacccfaabfbcbadeebddcabacdffdbT = String.fromCharCode(115,101,103,109,101,110,116,97,116,105,111,110,0);
          let customD = 0.0;
          let gestureK = false;
         libavcodecp += `${popupz.length / (Math.max(1, dacccfaabfbcbadeebddcabacdffdbT.length))}`;
         dacccfaabfbcbadeebddcabacdffdbT += `${parseInt(`${customD}`) % 2}`;
         customD *= (parseFloat(`${(gestureK ? 4 : 3) << (Math.min(Math.abs(parseInt(`${customD}`)), 5))}`));
         gestureK = !gestureK;
          let gmail4 = true;
          let auto_ooA = String.fromCharCode(117,95,54,56,0);
          let iconwechatg = 5;
         entryC = 59 >= iconwechatg || 59 >= popupz.length;
         gmail4 = auto_ooA.length <= 29;
         auto_ooA += `${auto_ooA.length}`;
         iconwechatg |= ((gmail4 ? 2 : 1));
      let notificationfillempty4 = libavcodecp.length <= 6865842;
      do {
          let securityT = 0.0;
          let sharedj = 3.0;
          let greyv = String.fromCharCode(109,101,97,115,117,114,101,114,0);
         libavcodecp = `${(libavcodecp == String.fromCharCode(56,0) ? parseInt(`${sharedj}`) : libavcodecp.length)}`;
         securityT -= parseFloat(`${parseInt(`${securityT}`) << (Math.min(greyv.length, 4))}`);
         sharedj += parseFloat(`${2 % (Math.max(8, greyv.length))}`);
         if (notificationfillempty4) {
            break;
         }
      } while (notificationfillempty4 && (!iconwechatc.endsWith(`${libavcodecp.length}`)));
      if (3 == (4 % (Math.max(2, popupz.length)))) {
          let bridgeM: Map<any, any> = new Map([[String.fromCharCode(114,101,109,111,118,101,114,0),812], [String.fromCharCode(98,111,114,100,101,114,101,100,0),408], [String.fromCharCode(101,120,116,101,114,105,111,114,0),762]]);
          let questicon8: Map<any, any> = new Map([[String.fromCharCode(118,109,97,112,115,105,110,0),593], [String.fromCharCode(102,116,115,97,117,120,0),353]]);
          let sharemodali = 0.0;
         popupz.push(2 * parseInt(`${sharemodali}`));
         bridgeM = new Map([[`${bridgeM.size}`, 2 << (Math.min(1, Math.abs(questicon8.size)))]]);
         questicon8.set(`${questicon8.size}`, questicon8.size);
         sharemodali *= 2;
      }
         entryC = streamingg.length > 94;
       let qaagc: Map<any, any> = new Map([[String.fromCharCode(100,101,114,105,118,101,0),219], [String.fromCharCode(115,101,109,97,110,116,105,99,0),678], [String.fromCharCode(104,105,103,104,105,103,104,116,0),877]]);
      for (let q = 0; q < 2; q++) {
         libavcodecp += "3";
      }
      while ((popupz.length + 5) < 1) {
         streamingg.push((qaagc.size ^ (entryC ? 3 : 4)));
         break;
      }
         iconwechatc = `${qaagc.size}`;
         popupz.push(popupz.length);
      while (5 <= (4 - popupz.length) || 4 <= (popupz.length - libavcodecp.length)) {
         popupz = [3 + libavcodecp.length];
         break;
      }
      shareW /= Math.max(5, parseFloat(`${2}`));
      if (condensed3) {
         break;
      }
   } while (condensed3 && (parseFloat(`${eyeopenP.length}`) < shareW));
      orangeclockN = [1];
        resetForm();
    }

    const onSubmitForm = async () => {
       let combineW = 5;
    let imagemanagerB = 3.0;
    let userh = true;
    let ping7 = String.fromCharCode(102,116,118,108,105,110,107,0);
    let teamdetailsbgb = 5.0;
    let libcrashsdks = 5;
    let membershipf: Array<any> = [891, 930, 667];
    let vignette8 = 5.0;
    let pagea = 5.0;
    let g_managerB: Map<any, any> = new Map([[String.fromCharCode(115,105,103,110,112,111,115,116,0),false ], [String.fromCharCode(118,105,115,105,98,105,108,105,116,105,116,101,115,0),false ]]);
    let fastforwardm = 2.0;
    let appsy: Array<any> = [96, 737];
      ping7 += `${combineW & parseInt(`${pagea}`)}`;
      pagea *= parseInt(`${imagemanagerB}`) / 2;

        if (referrerErrMsg !== null) return;

   while (4.33 < (pagea / 1.63)) {
      pagea -= (ping7 == String.fromCharCode(50,0) ? ping7.length : parseInt(`${pagea}`));
      break;
   }
      teamdetailsbgb *= 2 * ping7.length;

        if (isSubmitting) return;

      vignette8 *= 3 >> (Math.min(Math.abs(parseInt(`${teamdetailsbgb}`)), 2));
   if ((2.5 * pagea) < 2.34) {
      pagea *= 1 << (Math.min(4, membershipf.length));
   }
        setSubmitting(true);

   if (3.19 >= (3.49 * pagea) || (combineW & 5) >= 5) {
      pagea += (1 & (userh ? 3 : 4));
   }
   for (let f = 0; f < 3; f++) {
      pagea += libcrashsdks % (Math.max(combineW, 8));
   }

        try {

       let ballD: Map<any, any> = new Map([[String.fromCharCode(114,101,118,101,114,115,101,0),true ], [String.fromCharCode(105,109,112,111,115,115,105,98,108,101,0),true ]]);
       let iconmoreM = String.fromCharCode(116,114,117,101,109,111,116,105,111,110,100,97,116,97,0);
      let flyerF = String.fromCharCode(119,119,111,52,102,104,98,0) == iconmoreM;
      do {
         iconmoreM += `${iconmoreM.length}`;
         if (flyerF) {
            break;
         }
      } while (flyerF && (4 == (ballD.size >> (Math.min(Math.abs(4), 3))) || (ballD.size >> (Math.min(Math.abs(4), 5))) == 2));
         iconmoreM = `${ballD.size ^ 3}`;
          let matchactiveA = 1.0;
          let crownF = String.fromCharCode(115,116,101,112,115,0);
          let manifestw = String.fromCharCode(111,110,121,120,100,0);
         iconmoreM = `${crownF.length}`;
         matchactiveA *= parseFloat(`${3}`);
         crownF = `${parseInt(`${matchactiveA}`) + manifestw.length}`;
         manifestw += `${manifestw.length & 1}`;
      while (4 >= (ballD.size / (Math.max(8, iconmoreM.length)))) {
          let agreement2 = String.fromCharCode(105,116,101,114,97,116,105,118,101,0);
          let control1: Array<any> = [615, 689];
         ballD = new Map([[`${ballD.size}`, 3]]);
         agreement2 += "3";
         control1.push(2);
         break;
      }
          let baiduy = String.fromCharCode(97,118,102,111,114,109,97,116,0);
          let fcdaebecbcbafcdfceaaeccfeacdbi = true;
         iconmoreM = `${ballD.size}`;
         baiduy += `${(baiduy == String.fromCharCode(80,0) ? (fcdaebecbcbafcdfceaaeccfeacdbi ? 3 : 3) : baiduy.length)}`;
         fcdaebecbcbafcdfceaaeccfeacdbi = !baiduy.includes(`${fcdaebecbcbafcdfceaaeccfeacdbi}`);
       let filter6: Array<any> = [487, 514, 908];
       let string0: Array<any> = [128, 153, 890];
      vignette8 *= 2;
   let iconuserJ = 9248260 >= membershipf.length;
   do {
      membershipf.push(ping7.length & 1);
      if (iconuserJ) {
         break;
      }
   } while ((2.7 > teamdetailsbgb) && iconuserJ);
            await wwIconscheduleColors.updateUsername({
                username: userState.user?.userName ?? '',
                referralCode: referrer,
            });
        } catch (err: any) {
            if (
                err.errors &&
                err.errors.referral_code
            ) {

   while (ping7.startsWith(`${userh}`)) {
       let resendQ = String.fromCharCode(119,101,114,101,0);
       let moviest = String.fromCharCode(112,108,117,114,97,108,0);
       let connectionX = String.fromCharCode(120,109,108,101,115,99,97,112,101,0);
       let calendarB: Map<any, any> = new Map([[String.fromCharCode(103,101,116,104,100,114,0),true ], [String.fromCharCode(112,105,99,116,121,112,101,0),true ], [String.fromCharCode(100,105,102,102,101,114,101,110,99,101,115,0),true ]]);
       let entry_ = false;
         connectionX += `${connectionX.length}`;
       let switch_iE: Map<any, any> = new Map([[String.fromCharCode(105,115,112,111,114,116,0),794], [String.fromCharCode(116,101,109,112,110,97,109,101,0),90], [String.fromCharCode(97,109,111,117,110,116,0),720]]);
         connectionX += "2";
         moviest += `${resendQ.length / 1}`;
         moviest = "3";
         switch_iE = new Map([[`${calendarB.size}`, (moviest == String.fromCharCode(112,0) ? calendarB.size : moviest.length)]]);
      if (!entry_ && connectionX.length < 5) {
         entry_ = switch_iE.get(`${entry_}`) == null;
      }
          let schedulerI = 2;
         resendQ = `${(String.fromCharCode(87,0) == resendQ ? resendQ.length : moviest.length)}`;
         schedulerI |= 1 / (Math.max(8, schedulerI));
       let colorsf = String.fromCharCode(98,97,99,107,103,114,117,110,100,0);
       let weathern = String.fromCharCode(109,111,110,116,104,0);
         colorsf += `${switch_iE.size << (Math.min(moviest.length, 3))}`;
          let inouttargetredK = 1.0;
          let greyarrowupZ = String.fromCharCode(99,97,114,100,105,110,97,108,105,116,121,0);
          let photoX: Array<any> = [963, 784, 905];
         connectionX += "3";
         inouttargetredK += parseFloat(`${1 | parseInt(`${inouttargetredK}`)}`);
         greyarrowupZ = `${photoX.length}`;
         photoX.push(parseInt(`${inouttargetredK}`));
      for (let v = 0; v < 3; v++) {
         connectionX = `${2 * switch_iE.size}`;
      }
         weathern += `${connectionX.length % (Math.max(1, 2))}`;
      let componentsz = entry_ ? !entry_ : entry_;
      do {
         entry_ = 96 == calendarB.size;
         if (componentsz) {
            break;
         }
      } while ((resendQ.startsWith(`${entry_}`)) && componentsz);
       let bells: Map<any, any> = new Map([[String.fromCharCode(99,111,110,116,114,97,105,110,116,115,0),String.fromCharCode(99,116,105,111,110,0)], [String.fromCharCode(115,101,116,116,105,103,110,115,0),String.fromCharCode(100,111,110,116,0)]]);
      ping7 += `${3 % (Math.max(3, parseInt(`${pagea}`)))}`;
      break;
   }
   let largeQ = imagemanagerB <= 9507933.0;
   do {
       let klevino = String.fromCharCode(109,101,103,97,103,114,111,117,112,0);
       let header4 = 5;
       let gifgoal2 = false;
       let phoneshare9 = true;
         header4 %= Math.max(1 * klevino.length, 5);
          let backwardo: Map<any, any> = new Map([[String.fromCharCode(115,111,108,117,116,105,111,110,0),139], [String.fromCharCode(120,109,108,115,0),803], [String.fromCharCode(120,97,118,115,0),272]]);
          let qnewinterstitialr = String.fromCharCode(114,108,109,0);
          let langkeyd: Array<any> = [664, 603];
         klevino = "3 & header4";
         backwardo = new Map([[`${backwardo.size}`, backwardo.size]]);
         qnewinterstitialr += `${1 * backwardo.size}`;
         langkeyd.push(3 | qnewinterstitialr.length);
      let whistleorangeV = gifgoal2 ? !gifgoal2 : gifgoal2;
      do {
          let crosse = String.fromCharCode(101,120,112,105,114,105,110,103,0);
          let benefitf: Map<any, any> = new Map([[String.fromCharCode(98,95,57,55,0),true ], [String.fromCharCode(109,102,113,101,0),true ]]);
          let ajaxP = String.fromCharCode(112,105,110,99,104,105,110,103,0);
          let libturbomodulejsijnis = String.fromCharCode(114,101,99,111,110,110,101,99,116,101,100,0);
         gifgoal2 = !libturbomodulejsijnis.startsWith(`${gifgoal2}`);
         crosse = `${ajaxP.length}`;
         benefitf = new Map([[`${benefitf.size}`, benefitf.size & 1]]);
         ajaxP = "3";
         libturbomodulejsijnis += `${crosse.length}`;
         if (whistleorangeV) {
            break;
         }
      } while (whistleorangeV && (!gifgoal2));
          let fileF = true;
         header4 -= header4 & 2;
         fileF = !fileF;
      if (4 >= (1 & header4) && header4 >= 1) {
         gifgoal2 = (!phoneshare9 ? gifgoal2 : !phoneshare9);
      }
      imagemanagerB -= 3;
      if (largeQ) {
         break;
      }
   } while ((teamdetailsbgb <= 3.30) && largeQ);
                setReferrerErrMsg(err.errors.referral_code);
            }

            setSubmitting(false);

      ping7 = `${libcrashsdks}`;
   while ((combineW - 3) >= 4) {
      teamdetailsbgb /= Math.max(parseInt(`${imagemanagerB}`), 1);
      break;
   }
            return;

   while ((libcrashsdks & membershipf.length) < 1 || (membershipf.length & 1) < 2) {
      libcrashsdks ^= 3 & ping7.length;
      break;
   }
       let readI = 4;
       let gesturek = String.fromCharCode(100,105,115,116,111,114,116,105,111,110,0);
       let libsentry0 = true;
       let sportsj: Map<any, any> = new Map([[String.fromCharCode(100,101,99,111,109,112,114,101,115,115,105,111,110,0),725], [String.fromCharCode(115,116,114,0),294]]);
      for (let e = 0; e < 2; e++) {
         gesturek = `${3 ^ sportsj.size}`;
      }
      if (!gesturek.endsWith(`${libsentry0}`)) {
         libsentry0 = readI >= 28;
      }
          let telemetry_: Array<any> = [491, 652];
          let liveu: Array<any> = [888, 491];
         libsentry0 = sportsj.size >= 84 || readI >= 84;
         telemetry_.push(liveu.length | 1);
         liveu.push(liveu.length);
          let profileactiveP = false;
          let hooksz = true;
         gesturek = "3";
         profileactiveP = profileactiveP && profileactiveP;
         hooksz = (!profileactiveP ? !profileactiveP : profileactiveP);
          let grayj: Map<any, any> = new Map([[String.fromCharCode(105,109,109,101,100,105,97,116,101,115,105,103,110,97,108,0),false ], [String.fromCharCode(106,117,109,112,0),false ], [String.fromCharCode(100,115,116,0),true ]]);
          let policyb = 1.0;
          let livenodatabgimgi = String.fromCharCode(109,115,101,120,0);
         libsentry0 = 54 <= grayj.size || 47.42 <= policyb;
         grayj.set(livenodatabgimgi, livenodatabgimgi.length);
         policyb += (parseFloat(`${String.fromCharCode(107,0) == livenodatabgimgi ? livenodatabgimgi.length : livenodatabgimgi.length}`));
      while (gesturek.length == 5) {
          let libyogaO: Map<any, any> = new Map([[String.fromCharCode(102,97,115,116,101,115,116,0),863], [String.fromCharCode(101,115,99,97,112,101,115,0),191]]);
          let const_1aI = String.fromCharCode(97,117,116,111,117,112,100,97,116,101,0);
          let register_vm7 = String.fromCharCode(99,111,109,112,114,101,115,115,111,114,0);
         gesturek = `${1 | readI}`;
         libyogaO = new Map([[`${libyogaO.size}`, libyogaO.size % (Math.max(3, const_1aI.length))]]);
         const_1aI += "1";
         register_vm7 = `${(register_vm7 == String.fromCharCode(102,0) ? libyogaO.size : register_vm7.length)}`;
         break;
      }
      for (let j = 0; j < 2; j++) {
          let firebaseV: Map<any, any> = new Map([[String.fromCharCode(117,110,98,105,110,100,0),false ], [String.fromCharCode(99,102,115,116,114,101,97,109,0),true ], [String.fromCharCode(105,100,101,110,116,105,102,105,101,114,115,0),false ]]);
         sportsj = new Map([[`${readI}`, 2]]);
         firebaseV.set(`${firebaseV.size}`, firebaseV.size % 1);
      }
         gesturek += `${gesturek.length - 1}`;
      imagemanagerB += 3 % (Math.max(parseInt(`${vignette8}`), 10));
        }

        const result = await wwIconscheduleColors.getUserDetails();

   let moony = String.fromCharCode(99,117,109,113,112,114,109,104,0) == ping7;
   do {
      ping7 += "2";
      if (moony) {
         break;
      }
   } while (moony && ((ping7.length << (Math.min(Math.abs(5), 3))) > 2 || (ping7.length - parseInt(`${pagea}`)) > 5));
   for (let d = 0; d < 2; d++) {
      pagea += (combineW & (userh ? 1 : 4));
   }

        if (result == null) {

   for (let v = 0; v < 2; v++) {
       let iconsaveimage4 = false;
       let libreact0 = String.fromCharCode(115,101,110,100,97,108,108,0);
       let favorite0 = 3.0;
      let eyeopenf = iconsaveimage4 ? !iconsaveimage4 : iconsaveimage4;
      do {
         iconsaveimage4 = libreact0.length <= 51;
         if (eyeopenf) {
            break;
         }
      } while (eyeopenf && (favorite0 >= 5.94 || 4.46 >= (5.94 - favorite0)));
      while ((favorite0 * 4.1) <= 1.30) {
          let templateprocessorF: Map<any, any> = new Map([[String.fromCharCode(109,97,120,105,109,117,109,0),false ], [String.fromCharCode(118,105,115,105,98,105,116,121,0),false ]]);
          let defaultprofilepicK = false;
          let whiteanimationliveu = false;
         iconsaveimage4 = String.fromCharCode(81,0) == libreact0;
         templateprocessorF = new Map([[`${templateprocessorF.size}`, templateprocessorF.size + 1]]);
         defaultprofilepicK = templateprocessorF.size <= 24;
         whiteanimationliveu = null == templateprocessorF.get(`${defaultprofilepicK}`);
         break;
      }
      while (1 > libreact0.length) {
          let phoneshareo = String.fromCharCode(116,117,110,110,101,108,105,110,103,0);
          let controls7 = 5.0;
          let weiboS: Array<any> = [91, 804, 938];
         libreact0 = `${libreact0.length}`;
         phoneshareo += `${3 << (Math.min(5, Math.abs(parseInt(`${controls7}`))))}`;
         controls7 *= parseFloat(`${weiboS.length}`);
         weiboS = [weiboS.length - parseInt(`${controls7}`)];
         break;
      }
         iconsaveimage4 = libreact0 == String.fromCharCode(87,0);
          let away9: Array<any> = [466, 488, 130];
          let statisticsinactivem = String.fromCharCode(116,119,111,109,0);
         libreact0 += `${away9.length}`;
         away9.push(2 | statisticsinactivem.length);
         statisticsinactivem += `${statisticsinactivem.length >> (Math.min(Math.abs(3), 2))}`;
         libreact0 += `${libreact0.length * 2}`;
      for (let f = 0; f < 3; f++) {
         libreact0 = `${((iconsaveimage4 ? 1 : 4))}`;
      }
      for (let h = 0; h < 1; h++) {
         favorite0 -= libreact0.length;
      }
         favorite0 -= 3 | parseInt(`${favorite0}`);
      userh = (libreact0.length << (Math.min(4, Math.abs(libcrashsdks)))) < 52;
   }
      teamdetailsbgb /= Math.max(1, libcrashsdks);
            return;
        }

        await dispatch(updateUserAuth(result));

   for (let c = 0; c < 1; c++) {
      teamdetailsbgb *= 2 + membershipf.length;
   }
      pagea -= parseInt(`${imagemanagerB}`);
        dispatch(changeScreenAction("修改成功"));

   while (ping7.length > imagemanagerB) {
      imagemanagerB += ping7.length;
      break;
   }
      teamdetailsbgb -= 2 << (Math.min(4, membershipf.length));

        Keyboard.dismiss();

   if (g_managerB.size >= 2) {
      libcrashsdks <<= Math.min(3, Math.abs(3 + parseInt(`${teamdetailsbgb}`)));
   }
   while (userh) {
       let selectD = 2.0;
         selectD /= Math.max(3, parseInt(`${selectD}`) - 1);
      if ((4.42 * selectD) == 1.50) {
          let libflippern: Map<any, any> = new Map([[String.fromCharCode(97,100,106,117,115,116,101,114,0),true ], [String.fromCharCode(98,101,115,116,0),false ], [String.fromCharCode(116,97,112,116,105,99,0),true ]]);
          let circlez: Map<any, any> = new Map([[String.fromCharCode(112,111,115,105,116,105,118,101,0),String.fromCharCode(115,101,97,114,99,104,98,97,114,0)], [String.fromCharCode(104,97,110,103,117,112,0),String.fromCharCode(116,111,112,105,99,115,0)], [String.fromCharCode(99,111,110,116,111,117,114,115,0),String.fromCharCode(115,119,97,112,112,101,100,0)]]);
         selectD *= circlez.size;
         libflippern = new Map([[`${libflippern.size}`, libflippern.size % (Math.max(5, libflippern.size))]]);
         circlez = new Map([[`${libflippern.size}`, 3]]);
      }
         selectD += 3;
      userh = combineW > vignette8;
      break;
   }
        navigation.navigate("Home", {
            screen: "Profile",
        });

      imagemanagerB *= parseInt(`${fastforwardm}`) << (Math.min(ping7.length, 5));
   let upload6 = 9838368.0 <= imagemanagerB;
   do {
       let baselinef: Array<any> = [120, 143, 546];
       let unfill9 = String.fromCharCode(97,116,116,101,109,112,116,0);
       let o_countp: Map<any, any> = new Map([[String.fromCharCode(97,108,101,114,116,0),291], [String.fromCharCode(114,116,114,101,101,0),519], [String.fromCharCode(98,97,114,114,97,121,0),631]]);
       let armva0 = String.fromCharCode(109,101,109,109,103,114,0);
      while (5 == o_countp.size) {
         o_countp = new Map([[`${o_countp.size}`, 3]]);
         break;
      }
       let footballfiledlayoutt = String.fromCharCode(108,101,97,118,105,110,103,0);
       let catagory9 = String.fromCharCode(108,97,116,109,0);
         footballfiledlayoutt = `${footballfiledlayoutt.length >> (Math.min(3, armva0.length))}`;
      let middlewareM = o_countp.size >= 8200367;
      do {
          let fastforwardP = String.fromCharCode(97,117,116,111,114,101,118,101,114,115,101,100,0);
          let championR: Array<any> = [309, 846];
          let shirtX = String.fromCharCode(103,101,116,117,114,108,99,111,110,116,101,120,116,0);
          let armvaU = 1.0;
          let iconeditX = String.fromCharCode(97,99,116,117,97,108,105,122,101,100,0);
         o_countp.set(catagory9, 2 & catagory9.length);
         fastforwardP += `${3 >> (Math.min(1, shirtX.length))}`;
         championR.push(shirtX.length + iconeditX.length);
         armvaU -= 2;
         iconeditX = "3";
         if (middlewareM) {
            break;
         }
      } while ((5 == (o_countp.size | 2)) && middlewareM);
         armva0 += `${footballfiledlayoutt.length}`;
      while (unfill9 == String.fromCharCode(50,0) && catagory9.length <= 4) {
         unfill9 = `${catagory9.length}`;
         break;
      }
      let with_gmz = 5135848 <= catagory9.length;
      do {
          let clockb = String.fromCharCode(109,111,115,116,0);
          let overlayt = true;
          let iconstarj = 4.0;
          let yellow4 = String.fromCharCode(118,105,100,101,111,104,100,114,0);
         catagory9 += `${3 ^ catagory9.length}`;
         clockb += `${parseInt(`${iconstarj}`) & 2}`;
         overlayt = 49.43 < iconstarj;
         yellow4 += `${(yellow4 == String.fromCharCode(97,0) ? (overlayt ? 1 : 3) : yellow4.length)}`;
         if (with_gmz) {
            break;
         }
      } while ((unfill9 == String.fromCharCode(75,0)) && with_gmz);
          let activityN = String.fromCharCode(97,112,112,101,110,100,0);
         unfill9 += `${activityN.length >> (Math.min(Math.abs(3), 2))}`;
          let reactnativejsE = true;
          let hejip = String.fromCharCode(100,101,105,110,116,101,114,108,101,97,118,101,0);
          let basketball4 = 3;
         o_countp = new Map([[hejip, (String.fromCharCode(110,0) == catagory9 ? catagory9.length : hejip.length)]]);
         reactnativejsE = basketball4 < 49 || reactnativejsE;
         basketball4 += 1;
       let j_hash7 = String.fromCharCode(105,110,99,111,109,112,97,116,105,98,108,101,0);
       let movies5 = String.fromCharCode(109,105,112,115,100,115,112,0);
      for (let a = 0; a < 2; a++) {
         baselinef = [(String.fromCharCode(120,0) == armva0 ? armva0.length : o_countp.size)];
      }
      if (armva0.length <= 3) {
         catagory9 = `${(unfill9 == String.fromCharCode(86,0) ? movies5.length : unfill9.length)}`;
      }
      imagemanagerB += 1;
      if (upload6) {
         break;
      }
   } while ((!userh) && upload6);

        setSubmitting(false);
    }

    return (
        <CBottomSheet
            isVisible={isVisible}
            onBackdropPress={onClose}
            isLoading={isSubmitting}
        >
            <View style={styles.headerContainer}>
                <TouchableOpacity onPress={onClose}>
                    <Text style={textVariants.body}>
                        取消
                    </Text>
                </TouchableOpacity>

                <Text style={textVariants.bigHeader}>
                    设置昵称
                </Text>

                <TouchableOpacity onPress={onSubmitForm}>
                    <Text style={textVariants.body}>
                        保存
                    </Text>
                </TouchableOpacity>
            </View>
            <CTextInput
                value={referrer}
                onChangeText={onReferralChange}
                placeholder="补填邀请码 (只能填写一次)"
                showErrorStyle={referrerErrMsg !== null}
                maxLength={18}
            />

            <View
                style={{
                    ...styles.dangerWrap,
                }}
            >
                <View
                    style={{
                        justifyContent: "flex-start",
                        alignItems: "center",
                        flexDirection: "row",
                    }}
                >
                    {referrerErrMsg !== null && (
                        <>
                            <Image
                                style={{
                                    height: 22,
                                    width: 22,
                                    marginRight: 5,
                                    position: "relative",
                                    top: 1,
                                }}
                                source={require("@static/images/invite/countrySingaporeManager.png")}
                            />

                            <Text style={styles.danger}>{referrerErrMsg}</Text>
                        </>
                    )}
                </View>
            </View>
        </CBottomSheet>
    );
}

const styles = StyleSheet.create({
    headerContainer: {
        flexDirection: 'row',
        justifyContent: 'space-between',
        alignItems: 'flex-end',
        marginBottom: 20,
    },
    dangerWrap: {
        marginTop: 20,
        flexDirection: "row",
        justifyContent: "space-between",
        alignItems: "center",
    },
    danger: {
        fontWeight: "400",
        fontSize: 15,
        textAlign: "left",
        color: "#FF3434",
    },
});