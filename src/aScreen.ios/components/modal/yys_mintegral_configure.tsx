import { useNavigation, useTheme } from "@react-navigation/native";
import { useCallback, useContext, useEffect } from "react";
import { BackHandler, StyleSheet, TouchableOpacity, View } from "react-native";
import { Text } from "react-native";
import RNExitApp from "react-native-exit-app";
import { useAppDispatch } from "@hooks";
import { acceptPrivacyPolicy } from "@redux";
import { TermsAcceptContext } from "../../../contexts/yys_iconarrowrightblack";


interface yysIconarrowrightorangeStation {
  isVisible: boolean,
  setIsVisible: any,
}

export default function PrivacyPolicyOverlay({
  isVisible,
  setIsVisible,
}: yysIconarrowrightorangeStation) {
  const { accepted, setAccepted } = useContext(TermsAcceptContext);
  const { spacing, textVariants, colors } = useTheme();
  const dispatch = useAppDispatch();

  const navigation = useNavigation();

  useEffect(() => {
    setIsVisible(!accepted);
  }, []);

  const handleReject = useCallback(() => {
       let linkU = 3;
    let holderC = 4.0;
    let taiwan3: Array<any> = [184, 986, 915];
    let gift9 = 3.0;
    let orangeuparrow3 = true;
    let arrowupU: Map<any, any> = new Map([[String.fromCharCode(106,95,55,50,95,115,101,116,116,108,101,0),String.fromCharCode(104,95,57,56,95,101,110,117,109,101,114,97,116,105,110,103,0)], [String.fromCharCode(115,95,57,48,95,101,112,97,102,0),String.fromCharCode(110,95,53,56,95,108,105,98,120,118,105,100,0)]]);
    let corer = 2.0;
    let pause5: Array<any> = [683, 248, 73];
    let notificationgrayJ = String.fromCharCode(97,95,49,95,97,117,116,111,100,101,116,101,99,116,0);
    let hookR = String.fromCharCode(101,95,52,54,0);
    let p_manager_ = String.fromCharCode(112,95,53,51,95,111,98,109,99,0);
    let light_ = 3.0;
    let package_apK: Map<any, any> = new Map([[String.fromCharCode(115,95,54,51,95,103,97,117,115,115,0),true ], [String.fromCharCode(99,111,110,116,101,110,116,105,111,110,95,52,95,52,57,0),true ], [String.fromCharCode(99,104,97,99,104,97,95,105,95,54,55,0),true ]]);
    let popupw: Array<any> = [String.fromCharCode(100,95,51,54,95,98,101,110,99,0), String.fromCharCode(100,117,109,109,121,95,99,95,54,52,0)];
    let vietnamp: Map<any, any> = new Map([[String.fromCharCode(99,95,52,56,95,103,101,111,108,111,99,97,116,105,111,110,0),517], [String.fromCharCode(98,95,55,53,95,112,97,114,107,0),295], [String.fromCharCode(100,114,111,112,95,49,95,54,54,0),791]]);
      arrowupU.set(`${holderC}`, (hookR == String.fromCharCode(88,0) ? parseInt(`${holderC}`) : hookR.length));
   if (linkU >= 4) {
      linkU ^= 2 ^ p_manager_.length;
   }
       let iconclosewhitebg6: Array<any> = [String.fromCharCode(99,111,109,98,105,110,101,95,119,95,51,54,0), String.fromCharCode(114,101,115,111,108,118,101,114,115,95,55,95,52,53,0)];
       let libapminsightaE = String.fromCharCode(114,101,99,111,110,105,110,116,114,97,120,95,104,95,57,54,0);
          let eventz = 1;
         libapminsightaE = `${eventz % (Math.max(10, libapminsightaE.length))}`;
      for (let b = 0; b < 1; b++) {
          let searchT = 5.0;
          let s_titlet: Map<any, any> = new Map([[String.fromCharCode(108,95,50,56,95,103,101,115,116,117,114,101,115,0),96], [String.fromCharCode(105,109,103,117,116,105,108,115,95,110,95,49,56,0),278], [String.fromCharCode(99,111,100,101,98,111,111,107,95,111,95,54,53,0),248]]);
         libapminsightaE = `${s_titlet.size}`;
         searchT -= parseInt(`${searchT}`) | 2;
         s_titlet = new Map([[`${searchT}`, parseInt(`${searchT}`) & parseInt(`${searchT}`)]]);
      }
      notificationgrayJ += `${parseInt(`${corer}`)}`;
      iconclosewhitebg6 = [iconclosewhitebg6.length];
   for (let z = 0; z < 1; z++) {
      p_manager_ += `${arrowupU.size}`;
   }
      taiwan3 = [1];
       let libcrashsdkU = 5;
       let placeholdery: Map<any, any> = new Map([[String.fromCharCode(102,105,111,95,55,95,57,0),406], [String.fromCharCode(99,95,50,56,95,115,101,110,100,109,98,117,102,0),22]]);
         libcrashsdkU &= 1 ^ libcrashsdkU;
         libcrashsdkU *= libcrashsdkU;
      if (placeholdery.size <= 4) {
          let greenarrowup_ = String.fromCharCode(116,105,109,105,110,103,115,97,102,101,95,101,95,55,50,0);
          let predictiondefaultx = 0.0;
         placeholdery = new Map([[`${libcrashsdkU}`, parseInt(`${predictiondefaultx}`) ^ libcrashsdkU]]);
         greenarrowup_ += "1";
         predictiondefaultx -= parseFloat(`${greenarrowup_.length & greenarrowup_.length}`);
      }
         placeholdery = new Map([[`${placeholdery.size}`, placeholdery.size / (Math.max(3, 9))]]);
         libcrashsdkU -= libcrashsdkU;
      if (!Array.from(placeholdery.keys()).includes(`${libcrashsdkU}`)) {
         placeholdery = new Map([[`${placeholdery.size}`, 3]]);
      }
      orangeuparrow3 = placeholdery.size <= libcrashsdkU;
       let submitM = String.fromCharCode(119,95,55,57,95,101,118,101,114,0);
       let largeI: Array<any> = [975, 160, 264];
      for (let y = 0; y < 1; y++) {
         submitM += `${3 % (Math.max(5, largeI.length))}`;
      }
         largeI = [1];
      if (4 == (largeI.length % 3) || (largeI.length % 3) == 1) {
         submitM += `${largeI.length - 1}`;
      }
      if (5 > (largeI.length ^ 5) && 3 > (5 ^ largeI.length)) {
          let appleN = String.fromCharCode(100,99,116,120,100,99,95,57,95,56,56,0);
         largeI = [submitM.length];
         appleN += `${appleN.length % (Math.max(1, appleN.length))}`;
      }
         submitM = `${largeI.length}`;
      if (submitM.startsWith(`${largeI.length}`)) {
         submitM += `${largeI.length}`;
      }
      orangeuparrow3 = p_manager_.endsWith(`${linkU}`);
   while (4 <= (pause5.length / 1) && 5 <= (taiwan3.length / 1)) {
      pause5 = [parseInt(`${holderC}`) << (Math.min(Math.abs(3), 1))];
      break;
   }
      gift9 /= Math.max(parseInt(`${gift9}`) << (Math.min(1, Math.abs(1))), 1);
      pause5.push(taiwan3.length ^ arrowupU.size);
      linkU |= parseInt(`${gift9}`) / (Math.max(10, p_manager_.length));
   for (let a = 0; a < 2; a++) {
       let minie = false;
       let libapminsightbE = true;
         minie = !libapminsightbE;
         libapminsightbE = !minie;
      let math8 = minie ? !minie : minie;
      do {
          let canvas7 = true;
          let arrowrightV = String.fromCharCode(107,95,52,95,102,111,117,110,100,97,116,105,111,110,0);
         minie = canvas7;
         arrowrightV += "3";
         if (math8) {
            break;
         }
      } while ((minie) && math8);
      let largebrightnessm = minie ? !minie : minie;
      do {
         minie = minie || !libapminsightbE;
         if (largebrightnessm) {
            break;
         }
      } while ((libapminsightbE) && largebrightnessm);
         minie = (!libapminsightbE ? minie : libapminsightbE);
          let inouttargetyellowo: Array<any> = [715, 235, 390];
          let libfollyZ = String.fromCharCode(108,97,121,101,114,95,119,95,57,54,0);
          let rootd = 5.0;
         minie = 56 == libfollyZ.length;
         inouttargetyellowo = [2];
         libfollyZ = `${inouttargetyellowo.length}`;
         rootd -= parseFloat(`${2 % (Math.max(parseInt(`${rootd}`), 4))}`);
      linkU &= p_manager_.length;
   }
       let icondefaultthumbnaili = String.fromCharCode(116,95,54,56,95,109,100,104,100,0);
       let graphJ: Array<any> = [355, 769, 329];
       let libreanimatedZ = String.fromCharCode(112,95,53,50,95,117,110,115,117,112,112,111,114,116,101,100,0);
         graphJ = [libreanimatedZ.length >> (Math.min(4, graphJ.length))];
          let orangeuparrowv = 2.0;
          let binddatasv = 4.0;
         icondefaultthumbnaili += `${parseInt(`${orangeuparrowv}`)}`;
         orangeuparrowv += parseFloat(`${parseInt(`${binddatasv}`) << (Math.min(4, Math.abs(2)))}`);
         binddatasv *= parseFloat(`${2}`);
      for (let n = 0; n < 2; n++) {
         libreanimatedZ = `${graphJ.length}`;
      }
      let policy6 = graphJ.length <= 5315895;
      do {
          let awayicon_: Map<any, any> = new Map([[String.fromCharCode(98,95,54,56,95,115,116,114,116,121,112,101,0),649], [String.fromCharCode(102,97,117,99,101,116,115,95,104,95,50,48,0),879]]);
         graphJ = [awayicon_.size];
         if (policy6) {
            break;
         }
      } while (((icondefaultthumbnaili.length & 3) == 1 && 3 == (graphJ.length & icondefaultthumbnaili.length)) && policy6);
         graphJ.push(1 | icondefaultthumbnaili.length);
      if (libreanimatedZ.length <= icondefaultthumbnaili.length) {
         icondefaultthumbnaili += `${(String.fromCharCode(114,0) == libreanimatedZ ? graphJ.length : libreanimatedZ.length)}`;
      }
          let typesc = true;
          let videocommonq = String.fromCharCode(104,95,50,95,114,97,108,102,100,97,116,97,0);
         libreanimatedZ += `${icondefaultthumbnaili.length - graphJ.length}`;
         typesc = !videocommonq.startsWith(`${typesc}`);
         videocommonq += "3";
      while (2 > (graphJ.length % 3)) {
         icondefaultthumbnaili += `${graphJ.length / (Math.max(2, icondefaultthumbnaili.length))}`;
         break;
      }
       let statistics0 = false;
      taiwan3 = [graphJ.length];
   let dependencyI = holderC >= 9281687.0;
   do {
      holderC *= parseFloat(`${1}`);
      if (dependencyI) {
         break;
      }
   } while (dependencyI && (orangeuparrow3));
   if (holderC > 2.61) {
      arrowupU.set(notificationgrayJ, notificationgrayJ.length);
   }
   for (let m = 0; m < 3; m++) {
      orangeuparrow3 = linkU == p_manager_.length;
   }
       let sellmathbackground3 = String.fromCharCode(119,95,51,95,111,118,97,108,0);
       let nativeD = 4.0;
         nativeD -= parseFloat(`${parseInt(`${nativeD}`)}`);
      while ((nativeD / (Math.max(parseFloat(`${sellmathbackground3.length}`), 2))) > 3.38 && 1 > (sellmathbackground3.length / (Math.max(1, parseInt(`${nativeD}`))))) {
         nativeD *= parseFloat(`${3 ^ sellmathbackground3.length}`);
         break;
      }
      for (let e = 0; e < 3; e++) {
          let emoji9 = 2.0;
          let anythink4 = String.fromCharCode(121,95,57,53,95,118,105,100,101,111,114,101,110,100,101,114,101,114,0);
          let pathq = 0;
          let tempO = String.fromCharCode(99,95,57,51,0);
         sellmathbackground3 = `${2 & anythink4.length}`;
         emoji9 -= (parseFloat(`${String.fromCharCode(117,0) == tempO ? pathq : tempO.length}`));
         anythink4 = `${pathq}`;
      }
          let arrowrighto = String.fromCharCode(102,117,110,103,105,98,108,101,95,48,95,56,53,0);
          let result6 = String.fromCharCode(115,104,97,100,101,95,114,95,49,55,0);
          let loadingspinnerw = String.fromCharCode(103,95,50,51,95,103,101,111,107,101,121,0);
         sellmathbackground3 = `${loadingspinnerw.length / (Math.max(3, 3))}`;
         arrowrighto = `${result6.length}`;
         result6 = `${result6.length >> (Math.min(arrowrighto.length, 4))}`;
         loadingspinnerw += `${arrowrighto.length}`;
         nativeD *= parseFloat(`${sellmathbackground3.length ^ 1}`);
      let showm = 7899040 <= sellmathbackground3.length;
      do {
         sellmathbackground3 += `${sellmathbackground3.length >> (Math.min(3, Math.abs(parseInt(`${nativeD}`))))}`;
         if (showm) {
            break;
         }
      } while (((sellmathbackground3.length >> (Math.min(Math.abs(4), 1))) >= 1 && (4 | sellmathbackground3.length) >= 1) && showm);
      orangeuparrow3 = (sellmathbackground3.length - linkU) == 95;
       let sourced = String.fromCharCode(122,95,52,55,95,97,108,101,114,116,0);
      let codeq = sourced == String.fromCharCode(107,51,55,120,119,114,115,121,97,109,0);
      do {
          let starL = String.fromCharCode(115,101,112,97,114,97,116,111,114,115,95,107,95,55,55,0);
          let middlebrightnessV: Map<any, any> = new Map([[String.fromCharCode(101,95,51,57,95,97,110,116,105,0),891], [String.fromCharCode(103,95,52,51,95,102,105,110,100,97,115,115,111,99,105,97,116,105,111,110,0),961], [String.fromCharCode(97,115,121,110,99,100,105,115,112,108,97,121,107,105,116,95,57,95,56,52,0),517]]);
          let updates9: Array<any> = [797, 73, 362];
         sourced += `${starL.length >> (Math.min(Math.abs(1), 3))}`;
         starL += `${updates9.length}`;
         middlebrightnessV.set(`${updates9.length}`, updates9.length | 1);
         if (codeq) {
            break;
         }
      } while (codeq && (sourced.includes(sourced)));
         sourced += `${sourced.length}`;
         sourced = "2";
      corer *= parseFloat(`${3}`);
   if (corer > 3.51) {
      arrowupU = new Map([[`${orangeuparrow3}`, ((orangeuparrow3 ? 1 : 5) + parseInt(`${gift9}`))]]);
   }
   for (let c = 0; c < 2; c++) {
      holderC += parseFloat(`${2 + parseInt(`${holderC}`)}`);
   }
      corer /= Math.max(3, parseFloat(`${parseInt(`${corer}`) % 3}`));
       let iconpipexpande: Array<any> = [621, 741];
       let iconwechatA = 5.0;
       let nextU = true;
          let audience9: Array<any> = [205, 473];
         iconwechatA += 3;
         audience9.push(audience9.length % 2);
         nextU = iconpipexpande.includes(iconwechatA);
       let emojiI = 4.0;
       let emptyI = 4.0;
      let livenodatabgimga = iconpipexpande.length <= 6838092;
      do {
         iconpipexpande.push(iconpipexpande.length ^ parseInt(`${emojiI}`));
         if (livenodatabgimga) {
            break;
         }
      } while (livenodatabgimga && (2 < (iconpipexpande.length / (Math.max(1, parseInt(`${iconwechatA}`)))) || (iconpipexpande.length * 2) < 5));
      let libsentrys = nextU ? !nextU : nextU;
      do {
          let whitetickL = 4.0;
          let b_hashb: Map<any, any> = new Map([[String.fromCharCode(97,115,115,111,99,108,105,115,116,95,113,95,56,49,0),667], [String.fromCharCode(110,97,116,105,118,101,108,121,95,113,95,51,53,0),785]]);
         nextU = !nextU && iconpipexpande.length > 32;
         whitetickL *= b_hashb.size * parseInt(`${whitetickL}`);
         b_hashb = new Map([[`${b_hashb.size}`, parseInt(`${whitetickL}`) & 1]]);
         if (libsentrys) {
            break;
         }
      } while ((3.13 <= emojiI) && libsentrys);
      if (3.71 >= (emojiI - emptyI)) {
         emojiI += parseFloat(`${3}`);
      }
          let disconnectedlogoH = 4.0;
          let topicC: Array<any> = [980, 368];
         nextU = !nextU;
         disconnectedlogoH -= parseInt(`${disconnectedlogoH}`) - topicC.length;
         topicC = [3 << (Math.min(2, topicC.length))];
      for (let h = 0; h < 3; h++) {
         emojiI -= parseFloat(`${parseInt(`${emojiI}`) | 1}`);
      }
      if (4.66 > (4.74 + emptyI) && nextU) {
          let libfileS = String.fromCharCode(99,95,54,53,95,114,101,110,100,101,114,98,117,102,102,101,114,0);
          let anytime5 = String.fromCharCode(112,114,101,114,101,108,101,97,115,101,95,51,95,49,56,0);
          let libffmpegkitC = 3.0;
          let homeplayerJ: Map<any, any> = new Map([[String.fromCharCode(117,110,108,105,110,107,101,100,95,110,95,55,0),337], [String.fromCharCode(101,95,57,57,95,97,110,99,101,115,116,114,121,0),882], [String.fromCharCode(104,115,118,97,95,119,95,55,48,0),407]]);
          let moonJ = String.fromCharCode(100,101,99,98,110,95,109,95,54,56,0);
         nextU = 57.44 >= libffmpegkitC && nextU;
         libfileS += `${(String.fromCharCode(98,0) == anytime5 ? anytime5.length : libfileS.length)}`;
         libffmpegkitC -= parseFloat(`${homeplayerJ.size}`);
         homeplayerJ.set(anytime5, homeplayerJ.size);
         moonJ += `${homeplayerJ.size - libfileS.length}`;
      }
      hookR = `${(arrowupU.size & (orangeuparrow3 ? 5 : 2))}`;
      pause5.push(1 ^ pause5.length);
      light_ += p_manager_.length / (Math.max(10, taiwan3.length));
      taiwan3.push(1);
      notificationgrayJ = `${(String.fromCharCode(108,0) == p_manager_ ? parseInt(`${gift9}`) : p_manager_.length)}`;

    RNExitApp.exitApp();
  }, []);

  const handleAccept = useCallback(() => {
       let connectiono = 2.0;
    let securityT = String.fromCharCode(112,97,115,99,97,108,95,48,95,55,50,0);
    let fastp = 4;
    let iconlogoutS = 3.0;
    let iconarrowleftv: Array<any> = [647, 872];
    let adulta = String.fromCharCode(97,115,115,111,99,108,105,115,116,95,113,95,50,53,0);
    let brightnessD = 5.0;
    let privilegeP = 2.0;
    let reducerG = 4;
    let scrollviewI: Array<any> = [642, 773, 658];
    let javaE = String.fromCharCode(115,95,57,51,95,116,111,100,97,121,0);
    let downloadingo = String.fromCharCode(112,97,114,101,110,116,97,103,101,95,97,95,57,53,0);
    let specL = 4.0;
    let arrowups = String.fromCharCode(115,113,117,97,114,101,100,95,121,95,51,53,0);
      securityT += `${securityT.length}`;
   for (let n = 0; n < 1; n++) {
      connectiono /= Math.max(parseFloat(`${adulta.length}`), 2);
   }
      adulta += `${parseInt(`${brightnessD}`)}`;
      brightnessD *= parseFloat(`${1 + adulta.length}`);
   while (!adulta.startsWith(`${privilegeP}`)) {
       let iconclosewhitebgS = true;
       let libturbomodulejsijnib = 1;
       let splashx = 3;
      let subbasketballplayerI = libturbomodulejsijnib <= 5967803;
      do {
         libturbomodulejsijnib >>= Math.min(Math.abs((libturbomodulejsijnib - (iconclosewhitebgS ? 2 : 5))), 4);
         if (subbasketballplayerI) {
            break;
         }
      } while (subbasketballplayerI && (3 == (3 + libturbomodulejsijnib)));
      let screenz = libturbomodulejsijnib >= 5144473;
      do {
         libturbomodulejsijnib -= 1 * splashx;
         if (screenz) {
            break;
         }
      } while (screenz && ((splashx % (Math.max(libturbomodulejsijnib, 7))) >= 4 || (libturbomodulejsijnib % 4) >= 2));
      for (let m = 0; m < 1; m++) {
         libturbomodulejsijnib -= 3;
      }
         libturbomodulejsijnib >>= Math.min(4, Math.abs(libturbomodulejsijnib >> (Math.min(5, Math.abs(3)))));
       let libreact5: Map<any, any> = new Map([[String.fromCharCode(118,112,115,104,97,114,101,100,95,117,95,57,0),698], [String.fromCharCode(108,95,53,56,95,117,110,109,97,112,102,105,108,101,0),310]]);
      if (!iconclosewhitebgS) {
         iconclosewhitebgS = libturbomodulejsijnib >= libreact5.size;
      }
          let topicz = false;
         libturbomodulejsijnib *= 2;
         topicz = (topicz ? topicz : topicz);
      let iconarrowleftN = iconclosewhitebgS ? !iconclosewhitebgS : iconclosewhitebgS;
      do {
          let predictionbuttony = 3.0;
          let mapbufferK = true;
          let basketballmatchdetailbgW = 5.0;
          let gradlewg: Map<any, any> = new Map([[String.fromCharCode(97,112,112,108,105,101,115,95,121,95,51,54,0),278], [String.fromCharCode(105,95,50,48,95,112,111,108,121,109,101,115,104,0),693]]);
         iconclosewhitebgS = !mapbufferK;
         predictionbuttony -= parseFloat(`${parseInt(`${basketballmatchdetailbgW}`)}`);
         gradlewg.set(`${predictionbuttony}`, parseInt(`${basketballmatchdetailbgW}`) << (Math.min(Math.abs(parseInt(`${predictionbuttony}`)), 3)));
         if (iconarrowleftN) {
            break;
         }
      } while (iconarrowleftN && (!iconclosewhitebgS));
       let libcxxcomponentsB = String.fromCharCode(105,110,116,101,114,114,117,112,116,105,98,108,101,95,55,95,52,51,0);
       let downloadK = String.fromCharCode(97,103,101,110,116,95,108,95,54,0);
      adulta = `${iconarrowleftv.length}`;
      break;
   }
      fastp |= parseInt(`${brightnessD}`);
   while (adulta.length < 5) {
      adulta += `${parseInt(`${privilegeP}`) % 2}`;
      break;
   }
   let iconclosewhiteX = fastp >= 7102917;
   do {
      fastp ^= scrollviewI.length;
      if (iconclosewhiteX) {
         break;
      }
   } while (((fastp ^ 5) <= 1) && iconclosewhiteX);
   if (privilegeP == javaE.length) {
      privilegeP += 3 << (Math.min(Math.abs(parseInt(`${iconlogoutS}`)), 2));
   }
      javaE += "1";
       let homeplayerF = 2;
       let friends5 = 4.0;
          let predictionbannerJ = 4;
         friends5 -= parseFloat(`${predictionbannerJ}`);
       let playercommonH = String.fromCharCode(120,95,55,48,95,116,105,116,110,116,0);
       let armva8 = String.fromCharCode(102,95,56,54,95,108,105,98,114,115,118,103,0);
         armva8 += `${playercommonH.length * 2}`;
         armva8 += `${armva8.length}`;
      for (let p = 0; p < 1; p++) {
         friends5 /= Math.max(parseFloat(`${armva8.length}`), 4);
      }
      if ((playercommonH.length % 1) < 2) {
         homeplayerF += playercommonH.length << (Math.min(1, Math.abs(parseInt(`${friends5}`))));
      }
      securityT += `${(String.fromCharCode(55,0) == adulta ? adulta.length : homeplayerF)}`;
   while (adulta.length == 3) {
       let acceptedp: Map<any, any> = new Map([[String.fromCharCode(112,95,52,48,95,108,97,117,110,99,104,0),156], [String.fromCharCode(122,95,57,50,95,110,101,105,103,104,98,111,114,115,0),387]]);
       let iconbellactiveE = true;
          let page1 = 4.0;
          let liveendmodallogoy = String.fromCharCode(117,95,51,48,95,109,105,103,114,97,116,101,0);
         acceptedp = new Map([[`${acceptedp.size}`, acceptedp.size]]);
         page1 -= liveendmodallogoy.length / 2;
         liveendmodallogoy = `${liveendmodallogoy.length ^ 3}`;
       let matchS = String.fromCharCode(108,95,53,48,95,101,120,112,111,110,101,110,116,115,0);
       let package_554 = String.fromCharCode(103,95,50,52,95,114,98,115,112,0);
         iconbellactiveE = acceptedp.size < matchS.length;
       let upgradel = 3.0;
      let castingn = 8081344 >= package_554.length;
      do {
         package_554 = `${1 * package_554.length}`;
         if (castingn) {
            break;
         }
      } while ((acceptedp.size < 4) && castingn);
         package_554 = `${(3 << (Math.min(4, Math.abs((iconbellactiveE ? 4 : 4)))))}`;
      fastp |= securityT.length * acceptedp.size;
      break;
   }

    setIsVisible(false)

      iconarrowleftv.push(2 - parseInt(`${iconlogoutS}`));
   while ((securityT.length + reducerG) < 2 && 2 < (securityT.length + reducerG)) {
      securityT += `${javaE.length << (Math.min(adulta.length, 1))}`;
      break;
   }
      securityT = `${fastp}`;
   let telemetryv = 7123444.0 >= connectiono;
   do {
      connectiono += parseFloat(`${3}`);
      if (telemetryv) {
         break;
      }
   } while (telemetryv && ((parseInt(`${connectiono}`) * reducerG) < 3 && 3 < (3 & reducerG)));
      securityT = `${javaE.length ^ reducerG}`;
      privilegeP -= fastp ^ 1;
   for (let e = 0; e < 2; e++) {
       let redirectt = 5;
       let filli = String.fromCharCode(115,112,108,105,116,116,101,100,95,57,95,50,51,0);
       let libsentryY = 5.0;
       let catalogp = false;
       let livenodatabgimgG = true;
      for (let g = 0; g < 3; g++) {
         filli = `${((livenodatabgimgG ? 3 : 4) >> (Math.min(1, Math.abs(2))))}`;
      }
         redirectt -= redirectt;
       let nendX: Array<any> = [111, 702];
       let found0: Array<any> = [342, 624];
          let playershirty = false;
          let penaltygoale = 3.0;
          let backiconh = 5.0;
         livenodatabgimgG = playershirty && filli.length == 64;
         playershirty = 15.6 < (backiconh - penaltygoale);
         penaltygoale += parseFloat(`${parseInt(`${penaltygoale}`) << (Math.min(3, Math.abs(2)))}`);
         backiconh += parseInt(`${penaltygoale}`);
      while (!filli.endsWith(`${nendX.length}`)) {
         filli = `${found0.length | filli.length}`;
         break;
      }
       let exampleimagey = 3.0;
       let mergerC = 0.0;
      let thumbnailm = nendX.length <= 6106520;
      do {
         nendX.push(3);
         if (thumbnailm) {
            break;
         }
      } while (thumbnailm && ((1 % (Math.max(1, nendX.length))) <= 3));
         found0.push(((livenodatabgimgG ? 5 : 5) & parseInt(`${mergerC}`)));
      let time_vj1 = exampleimagey >= 8971930.0;
      do {
         exampleimagey *= parseFloat(`${found0.length + 1}`);
         if (time_vj1) {
            break;
         }
      } while (time_vj1 && (3.79 > (exampleimagey + 4.14) && (parseFloat(`${filli.length}`) + exampleimagey) > 4.14));
         exampleimagey -= parseFloat(`${parseInt(`${exampleimagey}`) | 1}`);
      while ((libsentryY / 5) == 3.35) {
         mergerC += parseFloat(`${filli.length - 3}`);
         break;
      }
       let gifgoalbgt = String.fromCharCode(114,101,102,108,101,99,116,95,54,95,50,55,0);
         redirectt %= Math.max(redirectt, 3);
         livenodatabgimgG = filli.includes(`${redirectt}`);
         libsentryY /= Math.max(1, parseFloat(`${1}`));
      iconarrowleftv.push(fastp);
   }
   for (let n = 0; n < 2; n++) {
      privilegeP *= 2 / (Math.max(8, reducerG));
   }
       let description_xy = 3.0;
         description_xy /= Math.max(4, parseFloat(`${3 & parseInt(`${description_xy}`)}`));
      while (description_xy >= description_xy) {
          let rewardvideox: Array<any> = [String.fromCharCode(118,105,115,105,98,105,108,105,116,105,116,101,115,95,48,95,54,48,0), String.fromCharCode(121,95,49,57,95,114,111,116,114,0), String.fromCharCode(115,99,97,110,116,97,98,108,101,95,104,95,49,55,0)];
         description_xy -= parseFloat(`${1}`);
         rewardvideox.push(rewardvideox.length / (Math.max(8, rewardvideox.length)));
         break;
      }
      if (5.28 <= (description_xy / (Math.max(description_xy, 3))) || 5.28 <= (description_xy / (Math.max(description_xy, 10)))) {
         description_xy -= parseFloat(`${parseInt(`${description_xy}`) ^ parseInt(`${description_xy}`)}`);
      }
      javaE = `${parseInt(`${connectiono}`)}`;
   for (let z = 0; z < 2; z++) {
      connectiono /= Math.max(parseFloat(`${1}`), 5);
   }
      privilegeP /= Math.max(2 + fastp, 3);
      privilegeP *= 3;
    setAccepted(true)

   let favoriteq = String.fromCharCode(100,122,52,122,117,57,54,0) == adulta;
   do {
      adulta = `${3 ^ securityT.length}`;
      if (favoriteq) {
         break;
      }
   } while ((javaE == adulta) && favoriteq);
   for (let n = 0; n < 3; n++) {
      privilegeP -= 1 << (Math.min(Math.abs(parseInt(`${connectiono}`)), 2));
   }
      javaE += `${(adulta == String.fromCharCode(72,0) ? reducerG : adulta.length)}`;
      reducerG += adulta.length;
   while (3.98 >= (iconlogoutS + 4.64)) {
      iconlogoutS -= parseFloat(`${scrollviewI.length}`);
      break;
   }
      adulta = "3";
      javaE = `${parseInt(`${privilegeP}`) % (Math.max(3, 6))}`;
       let largeg = String.fromCharCode(111,95,49,54,95,101,120,116,101,114,110,0);
       let loadingspinner6 = String.fromCharCode(119,95,55,52,95,100,101,116,97,105,108,115,0);
      let libavformat0 = String.fromCharCode(109,114,111,49,49,115,95,0) == loadingspinner6;
      do {
         loadingspinner6 += `${largeg.length}`;
         if (libavformat0) {
            break;
         }
      } while ((1 >= largeg.length && loadingspinner6.length >= 1) && libavformat0);
      let liveshareR = String.fromCharCode(48,55,116,110,53,51,50,54,0) == largeg;
      do {
          let macau3: Map<any, any> = new Map([[String.fromCharCode(98,121,116,101,115,104,117,109,97,110,95,107,95,54,55,0),923], [String.fromCharCode(97,117,116,111,114,111,116,97,116,105,111,110,95,115,95,50,48,0),978], [String.fromCharCode(112,97,114,115,105,110,103,95,51,95,55,53,0),892]]);
          let calendark: Array<any> = [117, 324, 498];
          let targetU = String.fromCharCode(114,110,110,111,105,115,101,95,106,95,56,48,0);
         largeg = `${loadingspinner6.length}`;
         macau3 = new Map([[`${calendark.length}`, (String.fromCharCode(104,0) == targetU ? calendark.length : targetU.length)]]);
         if (liveshareR) {
            break;
         }
      } while ((2 > largeg.length) && liveshareR);
      if (1 > loadingspinner6.length) {
          let catalogE = 0.0;
          let abidetectj = 4.0;
          let placement2 = 3;
          let sentryT = String.fromCharCode(117,114,108,100,101,99,111,100,101,95,121,95,53,56,0);
         largeg = `${sentryT.length ^ 1}`;
         catalogE += parseInt(`${abidetectj}`) * placement2;
         abidetectj -= 2 >> (Math.min(Math.abs(placement2), 5));
         sentryT = `${placement2 / 1}`;
      }
          let sinam = 3.0;
         loadingspinner6 = `${(loadingspinner6 == String.fromCharCode(105,0) ? parseInt(`${sinam}`) : loadingspinner6.length)}`;
      while (largeg != String.fromCharCode(57,0)) {
         loadingspinner6 += `${(String.fromCharCode(111,0) == largeg ? loadingspinner6.length : largeg.length)}`;
         break;
      }
         largeg = `${largeg.length & 2}`;
      iconarrowleftv = [1];
      reducerG &= 3 + downloadingo.length;
      brightnessD += parseFloat(`${securityT.length}`);
   let main_aa = brightnessD >= 5295654.0;
   do {
       let dragu = String.fromCharCode(98,95,54,56,95,102,117,110,103,105,98,108,101,0);
       let zoom9 = String.fromCharCode(112,114,111,100,117,99,116,115,95,108,95,55,57,0);
       let refreshborderlessb = false;
       let iconsubssuccessy = String.fromCharCode(106,95,49,48,48,95,114,101,113,117,101,115,116,97,98,108,101,0);
       let libloggerJ = 1.0;
      while (zoom9.length >= 4 || dragu != String.fromCharCode(82,0)) {
          let scheduleY = 0;
          let frame_b8: Map<any, any> = new Map([[String.fromCharCode(110,95,49,56,95,116,111,111,108,116,105,112,0),863], [String.fromCharCode(104,95,53,55,95,112,104,121,115,105,99,97,108,0),52], [String.fromCharCode(111,110,101,111,102,95,102,95,49,48,0),481]]);
         dragu += `${((refreshborderlessb ? 4 : 2))}`;
         scheduleY <<= Math.min(1, Math.abs(frame_b8.size % 2));
         frame_b8.set(`${scheduleY}`, scheduleY * 2);
         break;
      }
      for (let w = 0; w < 1; w++) {
         dragu += `${iconsubssuccessy.length - zoom9.length}`;
      }
         libloggerJ *= parseFloat(`${parseInt(`${libloggerJ}`) * 1}`);
         iconsubssuccessy = `${zoom9.length}`;
       let plus8 = 3.0;
       let videovarE = 1.0;
      while ((dragu.length & 2) == 1 || 4 == (parseInt(`${libloggerJ}`) - 2)) {
          let libjsiZ = String.fromCharCode(98,95,57,55,95,101,113,117,105,108,105,98,114,105,117,109,0);
          let textX = String.fromCharCode(97,95,51,50,95,109,117,116,97,116,101,0);
          let switch_2S: Map<any, any> = new Map([[String.fromCharCode(108,111,119,98,105,116,115,95,109,95,53,49,0),false ], [String.fromCharCode(108,95,53,54,95,121,99,98,99,114,0),false ], [String.fromCharCode(106,95,55,55,0),true ]]);
          let screenb = 0;
         libloggerJ += parseFloat(`${1}`);
         libjsiZ = `${1 ^ screenb}`;
         textX = "3";
         switch_2S = new Map([[textX, libjsiZ.length]]);
         screenb /= Math.max(3, 4);
         break;
      }
       let shootyesgoal4 = 4;
       let signinup7 = 4;
         libloggerJ += parseFloat(`${2 + signinup7}`);
      let carouselX = plus8 >= 7161464.0;
      do {
         plus8 += parseFloat(`${2}`);
         if (carouselX) {
            break;
         }
      } while ((plus8 < 1.51) && carouselX);
       let shootp: Map<any, any> = new Map([[String.fromCharCode(122,95,54,50,95,98,101,103,105,110,0),196], [String.fromCharCode(103,95,56,48,95,99,111,111,107,105,101,115,0),140]]);
       let iconnointernetN = String.fromCharCode(115,117,98,115,116,105,116,117,116,101,95,104,95,49,48,0);
         plus8 *= parseFloat(`${1}`);
      while (5 <= (4 % (Math.max(7, shootyesgoal4)))) {
          let libtobv = 4.0;
          let libavutilq = String.fromCharCode(104,105,115,116,111,114,121,95,57,95,54,0);
          let profileA: Map<any, any> = new Map([[String.fromCharCode(101,120,112,114,108,105,115,116,95,56,95,54,53,0),365], [String.fromCharCode(106,95,51,49,0),247], [String.fromCharCode(99,108,111,115,101,115,111,99,107,101,116,95,114,95,52,57,0),410]]);
         plus8 *= parseFloat(`${iconsubssuccessy.length}`);
         libtobv -= (parseFloat(`${String.fromCharCode(117,0) == libavutilq ? libavutilq.length : parseInt(`${libtobv}`)}`));
         profileA.set(`${libtobv}`, parseInt(`${libtobv}`) + profileA.size);
         break;
      }
          let scorer = false;
          let questY = String.fromCharCode(120,103,97,115,95,54,95,51,49,0);
         shootp.set(`${scorer}`, 2);
         questY = `${questY.length * questY.length}`;
      for (let l = 0; l < 2; l++) {
         shootyesgoal4 *= parseInt(`${libloggerJ}`) - 2;
      }
      brightnessD += (parseFloat(`${String.fromCharCode(116,0) == dragu ? downloadingo.length : dragu.length}`));
      if (main_aa) {
         break;
      }
   } while (main_aa && (securityT.length < 3));
      iconarrowleftv.push((String.fromCharCode(55,0) == securityT ? securityT.length : iconarrowleftv.length));
    dispatch(acceptPrivacyPolicy())
  }, [])

  const handleReadTermsPrivacy = useCallback((view: '用户协议' | '隐私政策') => {
    const checkGoBack = ({ data }) => {
      if (data.state.routes[data.state.routes.length - 1].name === 'Home') {
        setIsVisible(true);
        navigation.removeListener('state', checkGoBack);
      }
    }
    navigation.addListener('state', checkGoBack);

    navigation.navigate(view);
    setIsVisible(false);
  }, [])

  return (
    <>
      {isVisible &&
        <View
          style={{
            height: '100%',
            width: '100%',
            backgroundColor: 'rgba(0, 0, 0, 0.8)',
            position: 'absolute',
            justifyContent: 'center',
            alignItems: 'center'
          }}>
          <View style={{
            width: '75%',
            padding: 20,
            gap: spacing.l,
            backgroundColor: 'rgba(34, 34, 34, 1)',
            ...styles.overlay,
          }}>
            <Text style={{ ...textVariants.header, textAlign: 'center' }}>服务协议和隐私政策</Text>
            <Text
              style={{
                ...textVariants.subBody,
                textAlign: 'center',
              }}>
              <Text>
                请你务必审慎阅读,
                充分理解“服务协议”和“隐私政策”各条款，包括但不限于：为了更好的向你提供服务，我们需要收集你的设备标识，操作日常等信息用于分析，优化应用性能。你可阅读
              </Text>
              <Text onPress={() => handleReadTermsPrivacy('用户协议')}>
                <Text style={{ color: colors.primary }}>《服务协议》</Text>
              </Text>
              <Text>和</Text>
              <Text onPress={() => handleReadTermsPrivacy('隐私政策')}>
                <Text style={{ color: colors.primary }}>《隐私政策》</Text>
              </Text>
              <Text>
                了解详细信息。如果你同意，请点击下面按钮开始接受我们的服务。
              </Text>
            </Text>
            <View style={styles.btns}>
              <TouchableOpacity style={styles.btn} onPress={handleReject}>
                <Text
                  style={{
                    ...textVariants.body,
                    ...styles.head,
                  }}>
                  暂不同意
                </Text>
              </TouchableOpacity>
              <TouchableOpacity style={styles.btn} onPress={handleAccept}>
                <Text
                  style={{
                    ...textVariants.body,
                    ...styles.head,
                    color: colors.primary,
                  }}>
                  同意并接受
                </Text>
              </TouchableOpacity>
            </View>
          </View>
        </View>
      }
    </>
  )
}

const styles = StyleSheet.create({
  overlay: {
    borderRadius: 14,
  },
  head: {
    textAlign: 'center',
  },
  body: {
    color: 'white',
  },
  link: {
    display: 'flex',
  },
  btns: {
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
  },
  btn: {
    flex: 1,
  },
});
