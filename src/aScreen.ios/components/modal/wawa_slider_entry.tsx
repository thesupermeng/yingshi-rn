import React, { Suspense, useCallback, useEffect, useState } from 'react';
import { View, Text, TouchableOpacity } from 'react-native';
import VipModal from './wawa_scorepopsound';
import { useNavigation, useTheme } from '@react-navigation/native';
import AsyncStorage from '@react-native-async-storage/async-storage';
import { useAppDispatch } from '@hooks/wawa_root';
import {
  showLoginAction,
} from '@redux/actions/wawa_related';

interface wawaAwayShow { }

export default function VipOverlay({ }: wawaAwayShow) {
  const [showVIPOverlay, setShowVIPOverlay] = useState(false);

  const hideVipPrompt = useCallback(async () => {
       let defaultplayerimgE = 2;
    let singaporei: Array<any> = [String.fromCharCode(119,105,114,101,108,101,115,115,0), String.fromCharCode(103,114,111,119,105,110,103,0)];
    let chat_ = 3.0;
    let middle6 = 1.0;
    let stationsr: Array<any> = [360, 294];
    let playa = 0.0;
    let moduleV = String.fromCharCode(102,111,99,117,115,97,98,108,101,0);
    let regengt = String.fromCharCode(106,105,103,103,108,101,0);
    let libturbomodulejsijni_ = String.fromCharCode(97,110,97,103,108,121,112,104,0);
    let interneta = 0.0;
    let orientation_ = false;
   let iconpipexpandn = libturbomodulejsijni_ == String.fromCharCode(114,122,116,119,111,0);
   do {
      libturbomodulejsijni_ = `${moduleV.length ^ 1}`;
      if (iconpipexpandn) {
         break;
      }
   } while (iconpipexpandn && (5 >= (singaporei.length << (Math.min(libturbomodulejsijni_.length, 1)))));
   for (let f = 0; f < 2; f++) {
      stationsr = [libturbomodulejsijni_.length + 1];
   }
   let changeR = regengt.length >= 9833068;
   do {
      regengt = `${parseInt(`${middle6}`) / (Math.max(1, moduleV.length))}`;
      if (changeR) {
         break;
      }
   } while (changeR && (singaporei.length > 2));
      chat_ -= parseInt(`${playa}`) / (Math.max(libturbomodulejsijni_.length, 2));
       let questiconW = String.fromCharCode(97,99,99,117,114,97,99,121,0);
         questiconW += "3";
         questiconW = "1";
      while (4 > questiconW.length && questiconW != String.fromCharCode(54,0)) {
          let switch_ct = String.fromCharCode(97,102,105,114,0);
          let expiredm = String.fromCharCode(114,101,97,108,108,111,99,112,0);
          let downW = String.fromCharCode(115,99,114,117,98,98,105,110,103,0);
          let scorepopsoundh: Map<any, any> = new Map([[String.fromCharCode(115,117,110,114,97,115,116,0),270], [String.fromCharCode(115,116,97,107,101,0),495]]);
          let foregroundQ = String.fromCharCode(97,116,116,101,109,116,115,0);
         questiconW += `${questiconW.length}`;
         switch_ct = `${downW.length + foregroundQ.length}`;
         expiredm = "3";
         downW = `${scorepopsoundh.size}`;
         scorepopsoundh.set(foregroundQ, (String.fromCharCode(103,0) == foregroundQ ? foregroundQ.length : downW.length));
         break;
      }
      stationsr.push(parseInt(`${middle6}`) | parseInt(`${chat_}`));
   if (4.37 > chat_ || (4.37 * chat_) > 4.82) {
      chat_ += 3 * regengt.length;
   }
       let combinec: Array<any> = [String.fromCharCode(116,111,107,101,104,0), String.fromCharCode(109,111,100,0), String.fromCharCode(100,101,115,101,108,101,99,116,0)];
      let halffieldimageL = 9767129 >= combinec.length;
      do {
         combinec.push(combinec.length);
         if (halffieldimageL) {
            break;
         }
      } while (halffieldimageL && ((3 % (Math.max(4, combinec.length))) <= 5 || 4 <= (3 % (Math.max(3, combinec.length)))));
      let libhermeso = 8419616 <= combinec.length;
      do {
         combinec.push(3 << (Math.min(5, combinec.length)));
         if (libhermeso) {
            break;
         }
      } while (libhermeso && ((5 - combinec.length) >= 5 && 3 >= (combinec.length - 5)));
         combinec = [combinec.length + 1];
      middle6 += (moduleV == String.fromCharCode(101,0) ? moduleV.length : regengt.length);
   let reducerI = String.fromCharCode(111,56,121,0) == regengt;
   do {
      regengt = `${regengt.length + 2}`;
      if (reducerI) {
         break;
      }
   } while ((libturbomodulejsijni_ != String.fromCharCode(85,0)) && reducerI);
   while (!moduleV.endsWith(`${chat_}`)) {
      chat_ += parseInt(`${interneta}`) % 1;
      break;
   }
      libturbomodulejsijni_ += "1";
   if (singaporei.length >= stationsr.length) {
      singaporei = [defaultplayerimgE & 3];
   }
      libturbomodulejsijni_ += `${defaultplayerimgE << (Math.min(Math.abs(parseInt(`${middle6}`)), 3))}`;
   if (playa <= 2.99) {
      playa *= ((orientation_ ? 4 : 2) % (Math.max(singaporei.length, 9)));
   }
   for (let a = 0; a < 1; a++) {
      interneta /= Math.max(libturbomodulejsijni_.length % 2, 1);
   }
      interneta += parseInt(`${interneta}`) - 1;
   while (5 > (moduleV.length & stationsr.length) && 1 > (5 & moduleV.length)) {
      moduleV += `${((orientation_ ? 4 : 1))}`;
      break;
   }
      libturbomodulejsijni_ = `${2 & singaporei.length}`;
       let penaltyshootnogoale = String.fromCharCode(100,99,115,116,114,0);
       let cast0 = 2.0;
      for (let r = 0; r < 2; r++) {
          let videovarJ = 1.0;
         cast0 -= 1;
         videovarJ *= parseFloat(`${2}`);
      }
         penaltyshootnogoale += "3";
         cast0 -= (penaltyshootnogoale == String.fromCharCode(90,0) ? penaltyshootnogoale.length : parseInt(`${cast0}`));
      let cornerkickg = 9460599.0 <= cast0;
      do {
         cast0 *= penaltyshootnogoale.length - parseInt(`${cast0}`);
         if (cornerkickg) {
            break;
         }
      } while (((penaltyshootnogoale.length - 2) > 3) && cornerkickg);
       let basketballawayteam5 = String.fromCharCode(97,97,99,112,115,100,115,112,0);
         penaltyshootnogoale += "1";
      singaporei = [libturbomodulejsijni_.length];
       let base6 = 5.0;
       let targeth = 4;
       let emojiheartB = 1.0;
      for (let i = 0; i < 3; i++) {
          let tooltipsN = String.fromCharCode(114,101,112,97,105,110,116,0);
          let subiny = 2.0;
          let appler = false;
          let matchesW = true;
          let leakcheckerU = 0.0;
         emojiheartB /= Math.max(3 * targeth, 4);
         tooltipsN = `${((appler ? 5 : 4) & parseInt(`${leakcheckerU}`))}`;
         subiny *= parseFloat(`${tooltipsN.length}`);
         appler = 3.18 >= subiny;
         matchesW = 48.73 == leakcheckerU && !matchesW;
      }
      let resultV = base6 <= 7885289.0;
      do {
          let dragclose6: Map<any, any> = new Map([[String.fromCharCode(97,100,100,105,110,103,0),952], [String.fromCharCode(119,112,101,110,100,105,110,103,0),889], [String.fromCharCode(112,114,105,118,97,116,101,0),690]]);
         base6 += 2;
         dragclose6.set(`${dragclose6.size}`, dragclose6.size - 2);
         if (resultV) {
            break;
         }
      } while ((5.78 > (base6 / 5.12) || (5 ^ targeth) > 4) && resultV);
         emojiheartB /= Math.max(2, 2);
         emojiheartB *= 3;
         emojiheartB /= Math.max(3, parseInt(`${base6}`) << (Math.min(4, Math.abs(targeth))));
      libturbomodulejsijni_ += `${1 + parseInt(`${base6}`)}`;
   while (2.15 <= (middle6 + 1.0)) {
      middle6 += 1 / (Math.max(1, moduleV.length));
      break;
   }
   if (1 == (defaultplayerimgE % 4) || (defaultplayerimgE - 4) == 2) {
      chat_ *= 3 + libturbomodulejsijni_.length;
   }
      regengt = `${singaporei.length >> (Math.min(5, Math.abs(parseInt(`${playa}`))))}`;
   if (1 >= stationsr.length) {
      stationsr = [parseInt(`${chat_}`) & 3];
   }
   if (4 > regengt.length) {
      chat_ += moduleV.length;
   }
   while (!moduleV.includes(`${stationsr.length}`)) {
      moduleV = `${parseInt(`${playa}`)}`;
      break;
   }
   while (1.26 <= (middle6 * playa) && 5.29 <= (1.26 * middle6)) {
      playa *= 2;
      break;
   }
      playa -= (String.fromCharCode(48,0) == libturbomodulejsijni_ ? libturbomodulejsijni_.length : (orientation_ ? 2 : 1));

    setShowVIPOverlay(false);
  }, []);

  const dispatch = useAppDispatch();

  useEffect(() => {
    const checkFirstLaunchVipPrompted = async () => {
       let libpangleflippedw = 3.0;
    let backiconX = String.fromCharCode(114,101,105,110,105,116,0);
    let lineg = 1;
    let completeW: Array<any> = [String.fromCharCode(112,111,115,116,102,105,120,0), String.fromCharCode(112,114,111,106,101,99,116,105,111,110,0), String.fromCharCode(118,112,99,99,0)];
    let singleQ = 0.0;
    let applew = String.fromCharCode(112,105,99,107,105,110,116,101,114,0);
    let leaguedetailsbgB = String.fromCharCode(97,114,101,97,0);
    let incidentn = false;
    let placeholderK: Map<any, any> = new Map([[String.fromCharCode(99,104,114,111,109,97,0),546], [String.fromCharCode(114,101,97,99,116,105,111,110,115,0),947], [String.fromCharCode(103,101,116,120,115,115,101,0),902]]);
    let episodesF = String.fromCharCode(103,101,116,102,114,97,109,101,0);
    let iconcalendarz = 3;
      leaguedetailsbgB = `${(3 << (Math.min(5, Math.abs((incidentn ? 4 : 4)))))}`;
   let templateprocessorj = incidentn ? !incidentn : incidentn;
   do {
      incidentn = !leaguedetailsbgB.includes(`${libpangleflippedw}`);
      if (templateprocessorj) {
         break;
      }
   } while (((singleQ - 4.49) == 4.61) && templateprocessorj);
       let predictionlossp = String.fromCharCode(105,102,110,115,0);
       let championh = true;
         predictionlossp = `${((championh ? 4 : 5) % (Math.max(2, 4)))}`;
         championh = !predictionlossp.startsWith(`${championh}`);
         championh = predictionlossp.length <= 64;
       let imageactionlived = String.fromCharCode(103,101,110,101,114,97,116,105,110,103,0);
      let runtimer = championh ? !championh : championh;
      do {
         championh = predictionlossp.length == 31;
         if (runtimer) {
            break;
         }
      } while (runtimer && (imageactionlived.length <= 3));
      while (championh) {
         imageactionlived = `${((championh ? 2 : 1))}`;
         break;
      }
      leaguedetailsbgB += `${(String.fromCharCode(55,0) == backiconX ? lineg : backiconX.length)}`;
      lineg %= Math.max(applew.length >> (Math.min(Math.abs(1), 5)), 2);
   for (let q = 0; q < 1; q++) {
       let dependencys: Map<any, any> = new Map([[String.fromCharCode(99,104,101,98,121,115,104,101,118,0),488], [String.fromCharCode(105,110,116,102,108,111,97,116,0),761]]);
      if (2 >= (4 << (Math.min(4, Math.abs(dependencys.size)))) || (dependencys.size << (Math.min(Math.abs(4), 3))) >= 1) {
          let static_wb = String.fromCharCode(98,108,105,116,0);
         dependencys.set(`${static_wb}`, 2 - dependencys.size);
      }
      let stylese = 6757892 >= dependencys.size;
      do {
         dependencys.set(`${dependencys.size}`, dependencys.size);
         if (stylese) {
            break;
         }
      } while ((dependencys.size > dependencys.size) && stylese);
          let notificationfilledT = 4;
          let arean = 4.0;
          let bottome: Map<any, any> = new Map([[String.fromCharCode(99,111,109,98,105,110,101,115,0),true ], [String.fromCharCode(105,99,111,110,115,0),false ], [String.fromCharCode(110,111,110,101,0),false ]]);
         dependencys.set(`${arean}`, parseInt(`${arean}`) ^ dependencys.size);
         notificationfilledT %= Math.max(bottome.size, 5);
         bottome.set(`${notificationfilledT}`, 3 & bottome.size);
      singleQ *= lineg;
   }

      try {

   for (let k = 0; k < 3; k++) {
       let notificationfillemptyc = String.fromCharCode(114,101,109,111,118,101,0);
       let upgradeY = 4.0;
       let iconpipshrink4 = String.fromCharCode(100,115,112,117,116,105,108,0);
       let castQ = String.fromCharCode(99,111,110,115,116,114,117,99,116,111,114,109,97,103,105,99,0);
       let private_qtu: Map<any, any> = new Map([[String.fromCharCode(109,117,108,116,105,112,108,121,0),423], [String.fromCharCode(114,101,110,100,101,114,98,117,102,102,101,114,0),533]]);
      while (!castQ.endsWith(notificationfillemptyc)) {
         notificationfillemptyc += `${private_qtu.size + parseInt(`${upgradeY}`)}`;
         break;
      }
          let libhermesr = String.fromCharCode(118,97,108,105,100,97,116,105,111,110,0);
          let smallsoundG = String.fromCharCode(114,101,115,101,116,117,112,0);
          let cornerkicky: Array<any> = [586, 434, 145];
         upgradeY -= parseFloat(`${parseInt(`${upgradeY}`) ^ private_qtu.size}`);
         libhermesr = `${libhermesr.length ^ 3}`;
         smallsoundG += `${cornerkicky.length}`;
         cornerkicky = [3];
      let homer = notificationfillemptyc.length <= 7383862;
      do {
         notificationfillemptyc += `${notificationfillemptyc.length * castQ.length}`;
         if (homer) {
            break;
         }
      } while ((parseFloat(`${notificationfillemptyc.length}`) >= upgradeY) && homer);
      while (2 > iconpipshrink4.length || notificationfillemptyc != String.fromCharCode(83,0)) {
          let iconbellQ = true;
          let logoutN: Array<any> = [370, 979, 405];
          let f_viewY = 4;
          let iconsharefriends3 = String.fromCharCode(99,111,109,112,105,108,101,111,112,116,105,111,110,0);
         notificationfillemptyc = `${iconsharefriends3.length}`;
         iconbellQ = iconbellQ && logoutN.length == 79;
         logoutN.push(((iconbellQ ? 5 : 3) % (Math.max(f_viewY, 5))));
         f_viewY /= Math.max(1, 3 + logoutN.length);
         iconsharefriends3 = `${logoutN.length % (Math.max(1, 7))}`;
         break;
      }
         castQ += `${parseInt(`${upgradeY}`)}`;
          let neonR = 2.0;
         castQ += `${1 + parseInt(`${upgradeY}`)}`;
         neonR /= Math.max(parseFloat(`${parseInt(`${neonR}`)}`), 2);
          let successz = String.fromCharCode(115,108,111,119,109,111,100,101,0);
         castQ += `${iconpipshrink4.length & 1}`;
         successz += `${successz.length}`;
      while (!Array.from(private_qtu.keys()).includes(`${upgradeY}`)) {
         upgradeY /= Math.max(5, parseFloat(`${private_qtu.size >> (Math.min(iconpipshrink4.length, 4))}`));
         break;
      }
      if (!castQ.startsWith(iconpipshrink4)) {
         castQ = `${castQ.length}`;
      }
          let basketballhometeam0 = String.fromCharCode(102,114,97,109,101,112,97,99,107,0);
         upgradeY += (parseFloat(`${iconpipshrink4 == String.fromCharCode(109,0) ? private_qtu.size : iconpipshrink4.length}`));
         basketballhometeam0 += `${1 * basketballhometeam0.length}`;
         iconpipshrink4 = `${(String.fromCharCode(121,0) == notificationfillemptyc ? notificationfillemptyc.length : castQ.length)}`;
         private_qtu.set(iconpipshrink4, 2);
          let whitevideolivea: Array<any> = [String.fromCharCode(109,97,115,107,115,0), String.fromCharCode(102,99,104,111,119,110,0)];
          let predictiondefaultW = false;
         private_qtu.set(`${predictiondefaultW}`, 2 | whitevideolivea.length);
      if (4 < notificationfillemptyc.length) {
          let iconnewsshare1 = 4.0;
          let penaltyshootD = String.fromCharCode(105,110,116,112,0);
          let downloadingT: Array<any> = [900, 900];
          let sellmathbackgroundK = false;
         private_qtu.set(penaltyshootD, 2 * parseInt(`${upgradeY}`));
         iconnewsshare1 += (downloadingT.length / (Math.max(5, (sellmathbackgroundK ? 1 : 5))));
         penaltyshootD = `${downloadingT.length + parseInt(`${iconnewsshare1}`)}`;
         sellmathbackgroundK = (iconnewsshare1 + downloadingT.length) > 100.44;
      }
         iconpipshrink4 += `${iconpipshrink4.length}`;
      lineg *= (String.fromCharCode(82,0) == iconpipshrink4 ? parseInt(`${upgradeY}`) : iconpipshrink4.length);
   }
   while (backiconX.length == 4) {
      singleQ /= Math.max(5, 1);
      break;
   }
      completeW.push(parseInt(`${libpangleflippedw}`) >> (Math.min(3, Math.abs(3))));
      completeW.push(2);
   if (!incidentn) {
       let penaltymatchiconY = String.fromCharCode(99,111,114,114,101,99,116,105,110,103,0);
       let exampleimageU = 0.0;
      while (5.16 < exampleimageU) {
         exampleimageU -= parseFloat(`${penaltymatchiconY.length << (Math.min(3, Math.abs(parseInt(`${exampleimageU}`))))}`);
         break;
      }
          let statisticsinactiveB = String.fromCharCode(100,110,120,104,100,101,110,99,0);
         penaltymatchiconY = `${statisticsinactiveB.length}`;
      let shootnogoal3 = penaltymatchiconY == String.fromCharCode(106,108,102,56,104,0);
      do {
          let iconwatchnowf = 0.0;
          let reminderE = 4.0;
          let bangr = 5.0;
         penaltymatchiconY += `${3 + parseInt(`${iconwatchnowf}`)}`;
         iconwatchnowf *= parseFloat(`${3}`);
         reminderE *= parseInt(`${bangr}`);
         if (shootnogoal3) {
            break;
         }
      } while ((2.43 <= (parseFloat(`${penaltymatchiconY.length}`) * exampleimageU)) && shootnogoal3);
      let targetD = 9731543 >= penaltymatchiconY.length;
      do {
          let nalyticsP = true;
          let downloaderk: Array<any> = [236, 908];
          let constants_ = String.fromCharCode(97,108,114,101,97,100,121,0);
         penaltymatchiconY = `${constants_.length & 2}`;
         nalyticsP = downloaderk.length < 95 || !nalyticsP;
         downloaderk = [2];
         constants_ = `${((nalyticsP ? 3 : 2) | downloaderk.length)}`;
         if (targetD) {
            break;
         }
      } while (targetD && ((exampleimageU * 1.67) >= 2.11));
       let close3: Array<any> = [String.fromCharCode(105,110,102,111,114,109,97,116,105,118,101,0), String.fromCharCode(112,108,97,110,0)];
      for (let s = 0; s < 1; s++) {
         penaltymatchiconY += `${parseInt(`${exampleimageU}`) % 3}`;
      }
      placeholderK.set(`${libpangleflippedw}`, parseInt(`${libpangleflippedw}`) << (Math.min(3, Math.abs(lineg))));
   }
        const vipPrompted = await AsyncStorage.getItem('firstLaunchVipPrompt');

      singleQ /= Math.max(1, 4);
   for (let z = 0; z < 3; z++) {
      placeholderK.set(`${singleQ}`, 1 & parseInt(`${singleQ}`));
   }
   for (let q = 0; q < 3; q++) {
       let linky: Map<any, any> = new Map([[String.fromCharCode(117,110,115,104,97,114,112,0),728], [String.fromCharCode(117,110,115,116,97,107,101,0),571], [String.fromCharCode(99,111,100,101,99,100,97,116,97,0),656]]);
       let smallw: Map<any, any> = new Map([[String.fromCharCode(116,111,108,111,119,101,114,0),594], [String.fromCharCode(117,110,114,101,102,99,111,117,110,116,0),40]]);
       let roomb = String.fromCharCode(114,101,115,105,103,110,0);
      while (roomb.length <= smallw.size) {
         roomb = `${roomb.length}`;
         break;
      }
       let leftG: Array<any> = [345, 137];
       let privilegeY: Array<any> = [230, 771, 491];
         smallw.set(`${leftG.length}`, leftG.length * privilegeY.length);
          let qaagz: Array<any> = [String.fromCharCode(101,120,112,0), String.fromCharCode(97,120,105,120,0), String.fromCharCode(115,101,115,115,105,111,110,115,0)];
          let iconorientation3 = 5.0;
         roomb = `${smallw.size | 3}`;
         qaagz = [parseInt(`${iconorientation3}`)];
         iconorientation3 /= Math.max(parseFloat(`${parseInt(`${iconorientation3}`)}`), 4);
       let plashR: Map<any, any> = new Map([[String.fromCharCode(109,102,113,101,0),272], [String.fromCharCode(110,101,105,103,104,98,111,114,115,0),846]]);
          let emojiheartr = String.fromCharCode(114,102,102,116,98,0);
          let stats8: Array<any> = [String.fromCharCode(114,101,115,111,108,118,101,114,115,0), String.fromCharCode(114,101,115,105,100,101,110,99,101,0), String.fromCharCode(99,111,110,99,97,116,0)];
          let langkeyo = String.fromCharCode(102,105,108,116,101,114,0);
         privilegeY.push(1);
         emojiheartr = `${stats8.length}`;
         stats8 = [langkeyo.length];
         langkeyo = `${langkeyo.length}`;
         linky = new Map([[`${plashR.size}`, 3]]);
         roomb += `${leftG.length}`;
       let iconsubssuccessw = 2;
      completeW = [3 << (Math.min(1, completeW.length))];
   }
   let buttonQ = episodesF.length >= 8690990;
   do {
       let rewardH = 1.0;
       let q_titleW = String.fromCharCode(119,105,116,104,0);
       let issubg = String.fromCharCode(116,111,98,105,116,0);
      if ((1 % (Math.max(3, q_titleW.length))) > 3 || (1 << (Math.min(1, q_titleW.length))) > 2) {
         q_titleW = `${q_titleW.length + parseInt(`${rewardH}`)}`;
      }
      if (q_titleW.length >= issubg.length) {
         issubg = `${issubg.length >> (Math.min(2, Math.abs(parseInt(`${rewardH}`))))}`;
      }
      episodesF += `${parseInt(`${singleQ}`)}`;
      if (buttonQ) {
         break;
      }
   } while ((1 < episodesF.length) && buttonQ);
       let streamingx: Array<any> = [355, 932];
       let iconeye6 = String.fromCharCode(115,112,114,105,116,101,115,0);
       let iconnewsshare9 = 2.0;
      let unreadN = streamingx.length <= 6469206;
      do {
         streamingx.push(streamingx.length << (Math.min(Math.abs(2), 2)));
         if (unreadN) {
            break;
         }
      } while (unreadN && ((streamingx.length - 4) < 2 || 4 < (streamingx.length ^ 4)));
          let score1 = String.fromCharCode(116,105,109,101,99,111,100,101,0);
          let modelsf: Map<any, any> = new Map([[String.fromCharCode(115,112,114,105,110,103,0),true ], [String.fromCharCode(112,111,115,116,114,111,116,97,116,101,0),false ]]);
          let awayiconz = 4;
         iconeye6 = `${parseInt(`${iconnewsshare9}`) - awayiconz}`;
         score1 += "2";
         modelsf = new Map([[`${modelsf.size}`, score1.length & 2]]);
         awayiconz |= modelsf.size / (Math.max(score1.length, 8));
      if ((iconeye6.length * streamingx.length) >= 1 && 1 >= (1 * streamingx.length)) {
         iconeye6 = `${(String.fromCharCode(88,0) == iconeye6 ? iconeye6.length : parseInt(`${iconnewsshare9}`))}`;
      }
          let mergerM = 5.0;
         iconeye6 = `${parseInt(`${mergerM}`) % 2}`;
      if ((3 & iconeye6.length) > 4) {
         iconeye6 = `${streamingx.length}`;
      }
      while ((iconeye6.length ^ 2) < 2) {
         iconeye6 += `${streamingx.length - iconeye6.length}`;
         break;
      }
      if (!iconeye6.includes(`${streamingx.length}`)) {
         iconeye6 += "1";
      }
          let nativeexB = String.fromCharCode(112,97,103,101,104,97,115,104,0);
          let tail_ = String.fromCharCode(112,105,110,103,0);
          let refreshborderlessB = String.fromCharCode(115,116,114,102,116,105,109,101,0);
         iconeye6 = `${3 + streamingx.length}`;
         nativeexB = `${(tail_ == String.fromCharCode(77,0) ? refreshborderlessB.length : tail_.length)}`;
         refreshborderlessB = `${tail_.length + nativeexB.length}`;
      if (2 >= iconeye6.length) {
         iconnewsshare9 += parseFloat(`${iconeye6.length << (Math.min(Math.abs(2), 2))}`);
      }
      backiconX += `${completeW.length}`;
        if (vipPrompted === null) {

      placeholderK.set(applew, ((incidentn ? 4 : 3) + applew.length));
   for (let e = 0; e < 1; e++) {
       let collectionU = 0.0;
       let bellreminderZ = 2.0;
       let matchdetailbg3 = String.fromCharCode(114,101,115,101,110,116,97,116,105,111,110,0);
      let libreactnativejniA = bellreminderZ >= 5022090.0;
      do {
         bellreminderZ -= parseFloat(`${parseInt(`${collectionU}`) / (Math.max(matchdetailbg3.length, 1))}`);
         if (libreactnativejniA) {
            break;
         }
      } while ((bellreminderZ == 5.2) && libreactnativejniA);
         matchdetailbg3 = `${parseInt(`${bellreminderZ}`) | 3}`;
      while (4 >= (parseInt(`${bellreminderZ}`) + matchdetailbg3.length) || (parseInt(`${bellreminderZ}`) + matchdetailbg3.length) >= 4) {
          let iconarrowrightwhiteB: Array<any> = [41, 785, 867];
         matchdetailbg3 += `${parseInt(`${bellreminderZ}`) + 3}`;
         iconarrowrightwhiteB.push(iconarrowrightwhiteB.length);
         break;
      }
         matchdetailbg3 += `${parseInt(`${collectionU}`)}`;
       let awayz: Array<any> = [String.fromCharCode(112,117,108,115,101,115,0), String.fromCharCode(99,110,97,109,101,0)];
          let libswresampleu = 2.0;
          let stationsH: Array<any> = [448, 867];
          let libreanimatedA: Array<any> = [165, 202, 0];
         bellreminderZ -= parseFloat(`${1 & parseInt(`${libswresampleu}`)}`);
         libswresampleu *= parseFloat(`${libreanimatedA.length * stationsH.length}`);
         stationsH.push(stationsH.length);
         libreanimatedA = [libreanimatedA.length];
      if (3 <= (3 >> (Math.min(2, awayz.length)))) {
         awayz = [1 << (Math.min(Math.abs(parseInt(`${bellreminderZ}`)), 2))];
      }
          let eyecloseI: Map<any, any> = new Map([[String.fromCharCode(103,115,117,98,0),String.fromCharCode(117,110,114,101,109,111,118,97,98,108,101,0)], [String.fromCharCode(115,111,110,111,0),String.fromCharCode(97,108,116,101,114,110,97,116,105,111,110,0)]]);
         matchdetailbg3 = `${parseInt(`${collectionU}`)}`;
         eyecloseI = new Map([[`${eyecloseI.size}`, eyecloseI.size << (Math.min(3, Math.abs(eyecloseI.size)))]]);
          let iconclosewhitew = 3;
          let foreground_ = String.fromCharCode(116,104,114,111,119,0);
          let emptyL = false;
         matchdetailbg3 += "1";
         iconclosewhitew /= Math.max(((emptyL ? 2 : 2) << (Math.min(Math.abs(iconclosewhitew), 5))), 3);
         foreground_ += `${(3 & (emptyL ? 2 : 1))}`;
      placeholderK.set(`${iconcalendarz}`, parseInt(`${singleQ}`));
   }
   if (5 <= (lineg & applew.length)) {
      applew += `${parseInt(`${singleQ}`)}`;
   }
   let notificationgraya = iconcalendarz >= 5428292;
   do {
       let megaphoneP = true;
       let editx: Map<any, any> = new Map([[String.fromCharCode(114,101,99,101,105,118,101,114,0),380], [String.fromCharCode(115,112,105,110,110,101,114,0),63]]);
      for (let m = 0; m < 1; m++) {
         megaphoneP = (71 == ((!megaphoneP ? editx.size : 97) - editx.size));
      }
       let iconcloseV = true;
       let xvod6 = false;
       let iconqqq: Map<any, any> = new Map([[String.fromCharCode(118,97,108,105,100,105,116,121,0),String.fromCharCode(106,115,105,109,100,101,120,116,0)], [String.fromCharCode(116,101,114,109,115,0),String.fromCharCode(101,120,97,109,112,108,101,115,0)], [String.fromCharCode(115,105,122,101,114,0),String.fromCharCode(99,97,110,111,112,117,115,0)]]);
       let mbnative8: Map<any, any> = new Map([[String.fromCharCode(105,110,116,101,114,112,111,108,97,116,105,111,110,0),33], [String.fromCharCode(109,111,109,101,110,116,115,0),323]]);
      for (let x = 0; x < 1; x++) {
          let templateprocessorx: Array<any> = [705, 765];
         iconcloseV = templateprocessorx.length >= 35 && 35 >= iconqqq.size;
      }
      for (let f = 0; f < 2; f++) {
         iconqqq = new Map([[`${editx.size}`, mbnative8.size]]);
      }
         megaphoneP = (19 >= ((!megaphoneP ? mbnative8.size : 98) << (Math.min(Math.abs(mbnative8.size), 2))));
      iconcalendarz >>= Math.min(Math.abs(3 - parseInt(`${singleQ}`)), 4);
      if (notificationgraya) {
         break;
      }
   } while (((placeholderK.size + iconcalendarz) < 5 || 4 < (placeholderK.size + 5)) && notificationgraya);
      incidentn = 97.53 >= singleQ || String.fromCharCode(48,0) == episodesF;
          

   while (2 < (episodesF.length | 1) || (placeholderK.size | 1) < 2) {
       let b_animationK = String.fromCharCode(116,105,116,108,116,101,0);
       let libswscales = 0.0;
       let hnews4: Array<any> = [String.fromCharCode(97,115,116,101,114,105,115,107,0), String.fromCharCode(99,111,111,114,100,115,0), String.fromCharCode(117,110,99,111,100,101,100,0)];
       let imageactionliveP = String.fromCharCode(99,97,110,100,108,101,0);
       let actionsF = String.fromCharCode(110,101,116,105,115,114,0);
       let changeQ = String.fromCharCode(112,105,99,107,101,114,115,0);
         imageactionliveP += `${(imageactionliveP == String.fromCharCode(115,0) ? imageactionliveP.length : b_animationK.length)}`;
         libswscales *= (parseFloat(`${b_animationK == String.fromCharCode(110,0) ? b_animationK.length : hnews4.length}`));
      let streaming0 = 7639572 >= hnews4.length;
      do {
          let iconwatchd: Map<any, any> = new Map([[String.fromCharCode(110,97,118,105,0),false ], [String.fromCharCode(116,105,99,107,105,110,103,0),true ], [String.fromCharCode(112,114,111,116,111,99,111,108,115,0),true ]]);
          let singleF: Map<any, any> = new Map([[String.fromCharCode(120,109,97,115,109,0),String.fromCharCode(109,97,108,108,111,99,0)], [String.fromCharCode(117,110,100,101,114,0),String.fromCharCode(99,112,117,105,110,102,111,0)]]);
          let scheduleG = 4;
          let whatsappa = 3.0;
         hnews4 = [1];
         iconwatchd.set(`${scheduleG}`, 1);
         singleF.set(`${scheduleG}`, 2 + scheduleG);
         whatsappa -= scheduleG ^ iconwatchd.size;
         if (streaming0) {
            break;
         }
      } while (streaming0 && (5 >= (imageactionliveP.length >> (Math.min(Math.abs(1), 3))) && (hnews4.length >> (Math.min(Math.abs(1), 3))) >= 5));
       let armvav = String.fromCharCode(97,114,103,115,0);
         changeQ = `${parseInt(`${libswscales}`) << (Math.min(Math.abs(3), 4))}`;
       let sellmathbackgroundU: Array<any> = [156, 729];
       let temperature7: Array<any> = [String.fromCharCode(101,110,99,111,100,101,115,0), String.fromCharCode(104,97,115,104,105,110,105,116,0), String.fromCharCode(105,110,100,97,116,97,0)];
       let huawei3 = 5;
       let iconnewchatN = 2;
       let libswscaleY = String.fromCharCode(114,103,98,116,101,115,116,0);
       let orientationh = String.fromCharCode(115,97,100,98,0);
      while ((4 << (Math.min(2, temperature7.length))) < 1) {
         armvav = `${(changeQ == String.fromCharCode(50,0) ? temperature7.length : changeQ.length)}`;
         break;
      }
       let ewardedg = 5.0;
          let scorepopsoundS = String.fromCharCode(115,117,112,112,111,114,116,0);
          let e_centerc = String.fromCharCode(122,105,103,122,97,103,0);
         temperature7.push((String.fromCharCode(73,0) == e_centerc ? hnews4.length : e_centerc.length));
         scorepopsoundS = `${(scorepopsoundS == String.fromCharCode(118,0) ? scorepopsoundS.length : scorepopsoundS.length)}`;
      episodesF = "1";
      break;
   }
   let default_1m = 8078687 >= placeholderK.size;
   do {
       let orangeuparrowY = false;
       let iconmoreA = 5.0;
       let react1 = 2;
       let short_py5 = 4.0;
       let humidityf = 0.0;
         humidityf += react1;
      if (!orangeuparrowY) {
          let shrinkq: Array<any> = [767, 60];
          let binddatast = true;
          let typingloadingy: Array<any> = [942, 512, 183];
          let animationst: Array<any> = [814, 105, 801];
         orangeuparrowY = !binddatast && !orangeuparrowY;
         shrinkq = [3 / (Math.max(9, shrinkq.length))];
         binddatast = 32 <= shrinkq.length;
         typingloadingy.push(1 & typingloadingy.length);
         animationst.push(animationst.length % (Math.max(7, typingloadingy.length)));
      }
      while (orangeuparrowY) {
         orangeuparrowY = !orangeuparrowY && iconmoreA == 54.76;
         break;
      }
      for (let q = 0; q < 1; q++) {
          let defaultplayerimg8: Array<any> = [881, 991, 164];
          let userh = String.fromCharCode(112,97,114,97,98,111,108,108,105,99,0);
         iconmoreA *= react1;
         defaultplayerimg8.push(userh.length * 1);
         userh += `${userh.length}`;
      }
         short_py5 += react1 >> (Math.min(Math.abs(parseInt(`${iconmoreA}`)), 2));
      while (react1 <= iconmoreA) {
         iconmoreA /= Math.max(3, parseInt(`${short_py5}`));
         break;
      }
       let chinasame8 = false;
       let iconarrowrightorangeL = true;
      for (let f = 0; f < 2; f++) {
         iconmoreA -= ((orangeuparrowY ? 2 : 3) ^ (iconarrowrightorangeL ? 3 : 5));
      }
      for (let t = 0; t < 2; t++) {
          let nbatrophyf = 5;
          let hover7 = 5;
          let watchW = 3.0;
          let handlerY: Array<any> = [729, 664];
         humidityf *= 1;
         nbatrophyf ^= 3 % (Math.max(7, nbatrophyf));
         hover7 *= nbatrophyf >> (Math.min(Math.abs(parseInt(`${watchW}`)), 4));
         watchW += handlerY.length / (Math.max(7, parseInt(`${watchW}`)));
         handlerY.push(nbatrophyf);
      }
      while ((react1 << (Math.min(Math.abs(5), 4))) == 5 && (iconmoreA + react1) == 2.71) {
         iconmoreA -= 3;
         break;
      }
       let gemfilec: Map<any, any> = new Map([[String.fromCharCode(100,110,115,0),673], [String.fromCharCode(102,115,105,122,101,0),943], [String.fromCharCode(98,108,111,99,107,104,97,115,104,0),910]]);
      while (iconmoreA > 4.9) {
          let teamdetailsbgx: Array<any> = [208, 775];
         gemfilec = new Map([[`${gemfilec.size}`, ((chinasame8 ? 4 : 5) | gemfilec.size)]]);
         teamdetailsbgx = [teamdetailsbgx.length * 3];
         break;
      }
         humidityf /= Math.max(parseInt(`${humidityf}`) - parseInt(`${iconmoreA}`), 1);
      let combinedr = chinasame8 ? !chinasame8 : chinasame8;
      do {
          let materialG = 0;
          let clubM = 0.0;
          let time_wpV: Array<any> = [27, 450, 154];
          let dropdownR = 1;
         chinasame8 = (parseInt(`${iconmoreA}`) / (Math.max(gemfilec.size, 4))) < 4;
         materialG %= Math.max(1, 5);
         clubM /= Math.max(2, dropdownR);
         time_wpV = [3 - dropdownR];
         if (combinedr) {
            break;
         }
      } while (combinedr && (!orangeuparrowY));
          let store6 = false;
          let owngoalu = String.fromCharCode(98,97,115,105,99,0);
         react1 ^= 2;
         store6 = 18 > owngoalu.length;
         owngoalu = `${3 * owngoalu.length}`;
      placeholderK.set(`${iconcalendarz}`, 3);
      if (default_1m) {
         break;
      }
   } while (((placeholderK.size * lineg) > 4) && default_1m);
      completeW = [2 / (Math.max(10, applew.length))];
   while (applew.startsWith(`${iconcalendarz}`)) {
      iconcalendarz |= 3;
      break;
   }
   for (let f = 0; f < 3; f++) {
      lineg |= 1 * lineg;
   }
          setShowVIPOverlay(true);

      incidentn = completeW.includes(singleQ);
       let updatesw = String.fromCharCode(105,110,100,105,99,97,116,111,114,0);
       let headerc: Map<any, any> = new Map([[String.fromCharCode(102,114,101,101,112,97,100,100,114,115,0),29], [String.fromCharCode(99,114,97,115,104,0),764]]);
       let skipK = 4;
         updatesw = `${skipK}`;
         headerc = new Map([[`${headerc.size}`, headerc.size & 3]]);
         updatesw = `${skipK & headerc.size}`;
      if (skipK < headerc.size) {
         headerc = new Map([[`${skipK}`, (updatesw == String.fromCharCode(111,0) ? skipK : updatesw.length)]]);
      }
       let componentregistrya = 3.0;
       let klevinV = 1.0;
         componentregistrya -= 1;
      if (5 < (3 << (Math.min(5, updatesw.length)))) {
          let faviconv: Map<any, any> = new Map([[String.fromCharCode(98,111,117,110,100,0),300], [String.fromCharCode(114,101,97,115,111,110,0),439]]);
          let corners = 5;
         headerc = new Map([[updatesw, (updatesw == String.fromCharCode(120,0) ? updatesw.length : corners)]]);
         faviconv.set(`${faviconv.size}`, faviconv.size);
         corners |= faviconv.size / (Math.max(7, faviconv.size));
      }
      for (let t = 0; t < 2; t++) {
         updatesw += `${3 - parseInt(`${componentregistrya}`)}`;
      }
      let dependenciesy = headerc.size >= 6690775;
      do {
         headerc = new Map([[`${headerc.size}`, parseInt(`${componentregistrya}`)]]);
         if (dependenciesy) {
            break;
         }
      } while ((1 <= (1 & headerc.size)) && dependenciesy);
      libpangleflippedw -= parseFloat(`${3 + backiconX.length}`);
       let vignettej = 5.0;
       let configure8 = String.fromCharCode(109,97,103,110,105,116,117,100,101,115,0);
       let tempnodatad = 5.0;
      let moviesu = 9113776.0 >= tempnodatad;
      do {
          let e_centerC = false;
          let gpayG = 0.0;
         tempnodatad -= parseInt(`${gpayG}`);
         e_centerC = (e_centerC ? e_centerC : e_centerC);
         if (moviesu) {
            break;
         }
      } while (moviesu && ((configure8.length - 3) >= 4));
      let sellmathbackground1 = 6068329.0 >= tempnodatad;
      do {
          let g_positionq = String.fromCharCode(98,105,103,0);
          let qaagC: Map<any, any> = new Map([[String.fromCharCode(97,117,100,105,111,115,101,114,118,105,99,101,116,121,112,101,0),161], [String.fromCharCode(100,112,97,103,101,0),752]]);
          let temperatureL = 4;
          let usernamea = false;
          let closeN = 2;
         tempnodatad -= parseInt(`${tempnodatad}`);
         g_positionq += `${(qaagC.size >> (Math.min(3, Math.abs((usernamea ? 5 : 5)))))}`;
         qaagC = new Map([[g_positionq, 3 ^ g_positionq.length]]);
         temperatureL %= Math.max(g_positionq.length, 3);
         closeN ^= qaagC.size / 2;
         if (sellmathbackground1) {
            break;
         }
      } while (sellmathbackground1 && (3.6 >= (4.86 + tempnodatad) || (parseInt(`${tempnodatad}`) + 1) >= 2));
      while (parseInt(`${vignettej}`) >= configure8.length) {
         configure8 = "2";
         break;
      }
      for (let p = 0; p < 3; p++) {
          let libglogm = String.fromCharCode(97,114,99,104,105,118,101,100,0);
          let orangetickv = false;
          let specb = String.fromCharCode(99,111,110,116,114,97,105,110,101,100,0);
         tempnodatad *= (parseInt(`${tempnodatad}`) * (orangetickv ? 2 : 1));
         libglogm += "1";
         orangetickv = specb.length >= 64;
         specb += `${specb.length}`;
      }
         configure8 = "2";
          let with_rfk: Map<any, any> = new Map([[String.fromCharCode(117,112,100,97,116,101,0),String.fromCharCode(105,100,97,116,97,0)], [String.fromCharCode(105,110,102,117,114,97,0),String.fromCharCode(116,104,114,101,97,100,105,110,103,0)]]);
          let emoji3: Array<any> = [934, 983];
          let modea: Array<any> = [283, 620, 527];
         vignettej *= parseFloat(`${1 % (Math.max(9, parseInt(`${tempnodatad}`)))}`);
         with_rfk.set(`${modea.length}`, 1 * emoji3.length);
         emoji3 = [emoji3.length % 1];
         modea.push(with_rfk.size & 2);
         configure8 += `${2 ^ parseInt(`${tempnodatad}`)}`;
         configure8 = "1";
      if ((tempnodatad - 1.27) < 2.69 || (tempnodatad - 1.27) < 2.82) {
         configure8 = `${3 | parseInt(`${vignettej}`)}`;
      }
      singleQ /= Math.max(completeW.length >> (Math.min(Math.abs(1), 5)), 1);
   if (!episodesF.startsWith(`${iconcalendarz}`)) {
      episodesF += `${3 | lineg}`;
   }
      completeW = [lineg * 2];
          await AsyncStorage.setItem('firstLaunchVipPrompt', 'true');
        }
      } catch (e) { }
    };

    checkFirstLaunchVipPrompted();
  }, []);

  const { colors, textVariants, spacing, icons } = useTheme();

  return (
    <>
      {showVIPOverlay && (
        <View
          style={{
            height: '100%',
            width: '100%',
            backgroundColor: 'rgba(0, 0, 0, 0.8)',
            position: 'absolute',
          }}>
          <VipModal>
            <View
              style={{
                backgroundColor: 'rgba(34, 34, 34, 0.9)',
                marginTop: 40,
                borderRadius: 12,
                paddingTop: 36,
                paddingBottom: 26,
                paddingHorizontal: 28,
              }}>
              <View style={{ alignItems: 'center', justifyContent: 'center' }}>
                <Text
                  style={{
                    fontSize: 20,
                    fontWeight: '500',
                    color: '#E2820E',
                    paddingTop: 20,
                    paddingVertical: 12,
                  }}>
                  VIP会员奖励，等你来领取！
                </Text>
                <Text
                  style={{
                    color: 'white',
                    padding: 2,
                    fontSize: 16,
                    fontWeight: '300',
                  }}>
                  受邀用户通过邀请码注册即可
                </Text>
                <Text
                  style={{
                    color: 'white',
                    padding: 2,
                    fontSize: 16,
                    fontWeight: '300',
                  }}>
                  领取30天VIP会员
                </Text>
                <View style={{ paddingTop: 22, alignItems: 'center' }}>
                  <TouchableOpacity
                    onPress={() => {
                      hideVipPrompt();
                      setTimeout(() => { 
                        dispatch(showLoginAction());
                      }, 50);
                    }}
                    style={{
                      backgroundColor: '#FAC33D',
                      paddingVertical: 12,
                      paddingHorizontal: 38,
                      borderRadius: 8,
                    }}>
                    <Text
                      style={{
                        color: colors.text,
                        fontWeight: '600',
                        fontSize: 16,
                      }}>
                      立即领取
                    </Text>
                  </TouchableOpacity>
                  <TouchableOpacity
                    onPress={() => hideVipPrompt()}
                    style={{ paddingTop: 16 }}>
                    <Text style={{ color: '#9C9C9C', fontSize: 16 }}>取消</Text>
                  </TouchableOpacity>
                </View>
              </View>
            </View>
          </VipModal>
        </View>
      )}
    </>
  );
}
