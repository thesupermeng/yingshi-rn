import { RootStackScreenProps } from "@type/vrm_thailand";
import ScreenContainer from "../../components/container/ypy_fast";
import TitleWithBackButtonHeader from "../../components/header/cio_news_types_header";
import EmptyList from '../../components/common/jip_next_bell';
import { useAppDispatch, useSelector } from "@hooks/kg_index";
import { GDiceLogin } from "@redux/reducers/q_report_favorite";
import { FlatList, StyleSheet, Text, TouchableOpacity, View } from "react-native";
import { QRWModulePosition } from "@type/wpk_long";
import FastImage from '../../components/common/gwi_with';
import DefaultVideoThumbnail from '@static/images/smallPoint.svg';
import { useCallback, useMemo, useState } from "react";
import { useTheme } from "@react-navigation/native";
import CheckBoxSelected from "@static/images/thailandStationSell.svg";
import CheckBoxUnselected from "@static/images/middlewareGoalStation.svg";
import { Button } from "@rneui/themed";
import { removeUploadHistory } from "@redux/actions/ss_updates_detail";
import ConfirmationModal from "../../components/modal/nw_topic";

export const UploadHistory = ({
    route,
    navigation,
}: RootStackScreenProps<'uploadHistory'>) => {
    const { colors, textVariants, icons } = useTheme();
    const styles = useMemo(() => createStyles({ colors, textVariants }), [colors, textVariants]);

    const [showConfirmDialog, setShowConfirmDialog] = useState(false);
    const [isEditing, setEditing] = useState(false);
    const [videoSelected, setVideoSelected] = useState<number[]>([]);
    const dispatch = useAppDispatch();

    const uploadState = useSelector<GDiceLogin>('uploadReducer');

    const onEditPress = () => {
       let turnY = String.fromCharCode(101,95,53,48,95,108,117,116,100,0);
    let bannerU = String.fromCharCode(114,101,99,101,105,118,105,110,103,95,110,95,53,50,0);
    let shrinkk = String.fromCharCode(116,104,114,101,97,100,109,101,115,115,97,103,101,95,122,95,51,48,0);
    let vietnamk = 1.0;
    let teams = String.fromCharCode(119,95,57,95,119,104,116,120,0);
    let homeh: Array<any> = [603, 537, 725];
    let favorite2: Map<any, any> = new Map([[String.fromCharCode(119,111,114,107,95,116,95,50,55,0),244], [String.fromCharCode(99,111,109,98,105,110,101,114,95,50,95,55,50,0),712]]);
    let predictionS = 2.0;
    let anytimed = 1;
    let guide3 = 4.0;
    let nterstitial1: Array<any> = [774, 287, 601];
    let member_ = 3.0;
    let switch_65 = 5.0;
      homeh.push(homeh.length);
   while ((homeh.length / 2) <= 3) {
      shrinkk = "2";
      break;
   }
   while (3 >= homeh.length) {
       let chart9: Array<any> = [95, 287, 730];
       let miniX = String.fromCharCode(100,117,112,108,105,99,97,116,101,115,95,49,95,57,51,0);
       let bottomX = false;
         bottomX = (27 > ((bottomX ? 27 : chart9.length) >> (Math.min(chart9.length, 1))));
      if ((chart9.length / (Math.max(3, 1))) < 1) {
          let acceptedF = String.fromCharCode(121,95,51,56,95,97,99,107,110,111,119,108,101,100,103,101,100,0);
          let eactM = false;
         chart9.push(3);
         acceptedF += `${acceptedF.length}`;
         eactM = acceptedF.length < 22;
      }
         chart9.push(3);
      while (5 < (5 ^ chart9.length)) {
         chart9 = [1 & miniX.length];
         break;
      }
          let unselectedE: Map<any, any> = new Map([[String.fromCharCode(105,110,116,101,114,115,112,101,114,115,101,100,95,106,95,51,57,0),String.fromCharCode(107,95,56,57,95,115,108,111,112,101,0)], [String.fromCharCode(101,95,53,53,95,99,114,111,119,100,105,110,0),String.fromCharCode(114,97,110,100,111,109,105,122,101,95,100,95,57,55,0)], [String.fromCharCode(99,111,111,107,100,97,116,97,95,52,95,53,54,0),String.fromCharCode(106,95,57,95,119,101,98,114,116,99,0)]]);
          let tailj = 3.0;
         miniX = `${unselectedE.size}`;
         unselectedE = new Map([[`${tailj}`, parseInt(`${tailj}`) / 3]]);
      while (1 > (2 ^ miniX.length)) {
          let d_unlockY = 1.0;
          let langkeyC = String.fromCharCode(112,114,101,102,102,101,114,101,100,95,100,95,50,49,0);
          let moduleC = String.fromCharCode(104,95,50,53,95,100,105,115,107,0);
          let twittere = 4.0;
         miniX += `${(String.fromCharCode(57,0) == miniX ? chart9.length : miniX.length)}`;
         d_unlockY /= Math.max(1, parseInt(`${d_unlockY}`) / 3);
         langkeyC += `${(String.fromCharCode(51,0) == langkeyC ? moduleC.length : langkeyC.length)}`;
         moduleC = `${langkeyC.length % 1}`;
         twittere += parseFloat(`${2}`);
         break;
      }
      let default_ut6 = bottomX ? !bottomX : bottomX;
      do {
          let shootg = String.fromCharCode(109,95,57,52,95,99,104,97,114,97,99,116,101,114,105,115,116,105,99,115,0);
          let arrowu = String.fromCharCode(97,112,112,114,111,118,101,114,95,50,95,54,50,0);
         bottomX = shootg.length > arrowu.length;
         if (default_ut6) {
            break;
         }
      } while ((4 >= miniX.length && bottomX) && default_ut6);
         miniX += `${miniX.length}`;
      let contextw = miniX.length <= 9318014;
      do {
         miniX += `${chart9.length >> (Math.min(Math.abs(3), 3))}`;
         if (contextw) {
            break;
         }
      } while (contextw && (bottomX));
      homeh = [homeh.length];
      break;
   }
   if (3 >= (1 << (Math.min(3, turnY.length)))) {
       let redirects = String.fromCharCode(117,110,105,115,119,97,112,95,115,95,49,54,0);
         redirects = `${(String.fromCharCode(111,0) == redirects ? redirects.length : redirects.length)}`;
         redirects = `${redirects.length >> (Math.min(Math.abs(3), 3))}`;
       let mailU = String.fromCharCode(97,95,54,56,95,109,98,116,114,101,101,0);
      turnY = `${(shrinkk == String.fromCharCode(79,0) ? shrinkk.length : parseInt(`${predictionS}`))}`;
   }
   let auto_raK = 8161207 <= bannerU.length;
   do {
      bannerU += `${parseInt(`${vietnamk}`) + 2}`;
      if (auto_raK) {
         break;
      }
   } while (auto_raK && (4.43 < guide3));
   for (let u = 0; u < 3; u++) {
       let tailP = false;
       let cross4 = String.fromCharCode(121,95,53,53,95,102,117,108,108,98,97,110,100,0);
       let user9 = String.fromCharCode(120,95,50,56,95,115,111,117,110,100,0);
       let moonD = false;
      let shrinkg = tailP ? !tailP : tailP;
      do {
         tailP = (cross4.length ^ user9.length) >= 61;
         if (shrinkg) {
            break;
         }
      } while ((!moonD || tailP) && shrinkg);
      if (1 <= cross4.length) {
         tailP = user9.endsWith(`${tailP}`);
      }
      while (!moonD) {
         moonD = (49 >= ((!moonD ? user9.length : 49) % (Math.max(user9.length, 7))));
         break;
      }
      while (cross4.length > 1 || user9.length > 1) {
         cross4 += `${cross4.length & 2}`;
         break;
      }
         cross4 += `${cross4.length}`;
          let handler_ = 0;
          let editU: Map<any, any> = new Map([[String.fromCharCode(101,95,56,54,95,107,98,105,116,0),String.fromCharCode(107,95,49,48,48,95,115,116,117,98,0)], [String.fromCharCode(99,95,50,57,95,102,111,111,0),String.fromCharCode(104,112,101,108,95,57,95,54,53,0)]]);
          let statisticsa = 1.0;
         moonD = (handler_ & editU.size) <= 35;
         handler_ /= Math.max(4, 1 * parseInt(`${statisticsa}`));
         editU.set(`${statisticsa}`, parseInt(`${statisticsa}`));
       let product7 = 1.0;
         moonD = product7 < 24.6;
      while (!moonD) {
         moonD = !moonD;
         break;
      }
         moonD = tailP;
         cross4 = `${((tailP ? 4 : 3) | 2)}`;
         moonD = !moonD || product7 == 46.85;
      favorite2.set(bannerU, 2);
   }
      anytimed += nterstitial1.length ^ parseInt(`${predictionS}`);
       let orange4 = String.fromCharCode(112,114,105,118,97,116,101,95,51,95,52,55,0);
       let controlse = true;
      while (orange4.length >= 2) {
          let window_rgz: Array<any> = [771, 642, 198];
          let member9 = true;
          let schedulew = String.fromCharCode(105,109,112,111,114,116,97,110,99,101,95,114,95,56,0);
          let termsn = 1.0;
          let xnewsH: Map<any, any> = new Map([[String.fromCharCode(120,95,51,56,95,99,111,109,112,105,116,97,98,108,101,0),789], [String.fromCharCode(99,111,108,117,109,110,115,95,49,95,54,0),25], [String.fromCharCode(100,101,108,101,116,101,95,114,95,55,50,0),39]]);
         orange4 = `${(orange4 == String.fromCharCode(49,0) ? orange4.length : xnewsH.size)}`;
         window_rgz.push(((member9 ? 1 : 5) % 3));
         member9 = 25.0 == termsn;
         schedulew = `${2 >> (Math.min(5, Math.abs(parseInt(`${termsn}`))))}`;
         xnewsH.set(schedulew, parseInt(`${termsn}`) / (Math.max(schedulew.length, 9)));
         break;
      }
         orange4 += `${((controlse ? 2 : 2) & orange4.length)}`;
      while (!orange4.includes(`${controlse}`)) {
         orange4 += `${((controlse ? 4 : 5) & orange4.length)}`;
         break;
      }
         orange4 += `${2 / (Math.max(5, orange4.length))}`;
          let humidityA = true;
          let expiredh = true;
         controlse = !humidityA;
         expiredh = (!expiredh ? expiredh : !expiredh);
          let orientationH = 5;
          let smallS = String.fromCharCode(115,119,105,112,101,95,100,95,57,57,0);
          let countdownD = String.fromCharCode(99,111,108,95,52,95,51,54,0);
         controlse = orientationH > orange4.length;
         orientationH ^= countdownD.length - smallS.length;
         smallS += `${smallS.length}`;
         countdownD = "1";
      vietnamk += parseFloat(`${turnY.length - 3}`);
      homeh = [bannerU.length ^ parseInt(`${predictionS}`)];

        setEditing(!isEditing);

   let unselectedu = 6577435 >= nterstitial1.length;
   do {
       let redirect6 = false;
       let modalW = 5.0;
       let scopy_wwN = String.fromCharCode(105,112,108,105,109,97,103,101,95,114,95,57,0);
         redirect6 = String.fromCharCode(79,0) == scopy_wwN;
      while (scopy_wwN.startsWith(`${redirect6}`)) {
         redirect6 = 36.70 >= modalW || !redirect6;
         break;
      }
       let greenn: Map<any, any> = new Map([[String.fromCharCode(112,95,54,49,95,109,105,100,0),String.fromCharCode(103,95,56,57,95,121,101,116,0)], [String.fromCharCode(100,95,55,95,108,101,116,116,101,114,115,0),String.fromCharCode(114,111,111,109,95,56,95,56,49,0)], [String.fromCharCode(122,101,114,111,118,95,102,95,49,0),String.fromCharCode(102,112,115,95,51,95,54,53,0)]]);
       let network7: Array<any> = [518, 552];
      while (scopy_wwN.startsWith(`${greenn.size}`)) {
         greenn.set(scopy_wwN, (scopy_wwN == String.fromCharCode(118,0) ? scopy_wwN.length : (redirect6 ? 1 : 4)));
         break;
      }
         network7.push(2);
      if (3.72 >= (modalW * 4.10)) {
          let detaile: Array<any> = [21, 97, 998];
         network7.push(3);
         detaile.push(detaile.length + 2);
      }
         modalW -= scopy_wwN.length;
      while (1 == scopy_wwN.length) {
         redirect6 = 71 > greenn.size && 33.44 > modalW;
         break;
      }
      nterstitial1 = [2];
      if (unselectedu) {
         break;
      }
   } while (unselectedu && ((member_ / (Math.max(3.66, 3))) > 3.13 || 4 > (nterstitial1.length / 3)));
      nterstitial1.push(bannerU.length);
   for (let e = 0; e < 3; e++) {
       let sharek = true;
       let unselectedj = String.fromCharCode(111,95,52,54,95,112,97,97,100,0);
       let lessY = String.fromCharCode(113,95,51,52,95,122,101,116,97,0);
         sharek = String.fromCharCode(106,0) == unselectedj;
         unselectedj = `${(lessY == String.fromCharCode(115,0) ? lessY.length : (sharek ? 2 : 5))}`;
      favorite2 = new Map([[`${favorite2.size}`, favorite2.size * 1]]);
   }
   let otherj = 7578519 <= homeh.length;
   do {
      homeh.push(parseInt(`${member_}`) - 3);
      if (otherj) {
         break;
      }
   } while (otherj && (1 < (homeh.length + 2) && 1 < (2 + homeh.length)));
      favorite2 = new Map([[`${favorite2.size}`, favorite2.size]]);
   while ((1 * teams.length) < 4) {
      homeh = [homeh.length];
      break;
   }
   let gestureq = String.fromCharCode(98,98,55,100,97,113,107,0) == teams;
   do {
       let sharedF = String.fromCharCode(116,114,97,110,102,115,101,114,95,119,95,54,57,0);
         sharedF += `${sharedF.length}`;
      while (sharedF.length > 4) {
          let splash7: Map<any, any> = new Map([[String.fromCharCode(99,102,104,100,95,102,95,50,48,0),true ], [String.fromCharCode(112,114,101,104,101,97,116,95,103,95,55,52,0),false ]]);
          let fill2 = String.fromCharCode(109,95,56,57,95,110,111,116,0);
          let otherx = 4;
          let episode7: Map<any, any> = new Map([[String.fromCharCode(100,95,56,50,95,116,105,109,101,108,105,110,101,0),716], [String.fromCharCode(119,95,51,50,95,110,101,116,119,111,114,107,0),348], [String.fromCharCode(99,105,114,99,117,108,97,116,105,110,103,95,111,95,53,0),933]]);
          let appso: Array<any> = [String.fromCharCode(121,95,53,95,98,108,105,110,100,101,100,0), String.fromCharCode(117,108,108,115,99,114,101,101,110,95,119,95,52,56,0)];
         sharedF += `${1 & appso.length}`;
         splash7 = new Map([[`${episode7.size}`, 2]]);
         fill2 = `${fill2.length % (Math.max(1, splash7.size))}`;
         otherx <<= Math.min(5, Math.abs((fill2 == String.fromCharCode(101,0) ? splash7.size : fill2.length)));
         episode7.set(`${fill2}`, splash7.size - 2);
         appso = [fill2.length];
         break;
      }
      if (sharedF == String.fromCharCode(107,0) && 2 < sharedF.length) {
         sharedF = `${sharedF.length << (Math.min(Math.abs(3), 5))}`;
      }
      teams += `${shrinkk.length}`;
      if (gestureq) {
         break;
      }
   } while ((2.47 <= (vietnamk + 3.2)) && gestureq);
      guide3 /= Math.max(parseFloat(`${nterstitial1.length << (Math.min(Math.abs(2), 2))}`), 5);
      shrinkk += `${parseInt(`${guide3}`) - 2}`;

        if (!isEditing === false) {

   for (let w = 0; w < 1; w++) {
      favorite2.set(`${guide3}`, shrinkk.length & 3);
   }
      turnY += `${anytimed >> (Math.min(2, Math.abs(2)))}`;
      vietnamk += (parseFloat(`${String.fromCharCode(73,0) == teams ? teams.length : homeh.length}`));
   for (let g = 0; g < 3; g++) {
       let clearZ = String.fromCharCode(110,95,51,57,95,101,99,111,117,110,116,0);
       let ballv = 5;
      let fastforward4 = clearZ == String.fromCharCode(109,105,117,0);
      do {
          let networkg = String.fromCharCode(97,117,116,111,109,97,116,105,99,95,112,95,50,54,0);
          let viewer9: Map<any, any> = new Map([[String.fromCharCode(115,117,109,109,95,112,95,57,57,0),237], [String.fromCharCode(119,95,57,49,95,105,110,118,116,114,97,110,115,0),580]]);
          let footballK = String.fromCharCode(105,95,49,53,95,102,108,105,112,112,101,100,0);
          let score_ = false;
         clearZ += `${((score_ ? 4 : 3))}`;
         networkg += "1";
         viewer9 = new Map([[`${viewer9.size}`, footballK.length]]);
         footballK += `${1 * viewer9.size}`;
         score_ = footballK.length <= 18;
         if (fastforward4) {
            break;
         }
      } while (fastforward4 && ((clearZ.length ^ 1) >= 4));
          let network9 = String.fromCharCode(119,95,52,50,95,115,111,97,99,99,101,112,116,0);
          let member2 = 3.0;
          let zhubob = 5.0;
         ballv &= ballv / 2;
         network9 += `${parseInt(`${zhubob}`) * network9.length}`;
         member2 *= 1;
         zhubob += parseFloat(`${parseInt(`${zhubob}`)}`);
      let chinad = 7828535 <= ballv;
      do {
         ballv += ballv;
         if (chinad) {
            break;
         }
      } while (((4 << (Math.min(1, Math.abs(ballv)))) <= 2 || 5 <= (clearZ.length << (Math.min(Math.abs(4), 2)))) && chinad);
         clearZ = `${ballv ^ 1}`;
      let adultb = 7466446 <= ballv;
      do {
         ballv += clearZ.length & ballv;
         if (adultb) {
            break;
         }
      } while (adultb && ((clearZ.length * ballv) >= 5 || 3 >= (clearZ.length * 5)));
       let reado = 5;
      anytimed -= clearZ.length % 2;
   }
       let g_imagep = 3.0;
       let report6 = String.fromCharCode(105,95,53,49,95,106,112,101,103,0);
       let moduleg: Map<any, any> = new Map([[String.fromCharCode(105,110,116,101,114,114,117,112,116,95,98,95,49,55,0),257], [String.fromCharCode(106,95,54,56,95,105,102,110,115,0),804], [String.fromCharCode(115,121,110,99,104,114,111,110,105,122,97,116,105,111,110,95,113,95,53,52,0),272]]);
         g_imagep *= parseFloat(`${moduleg.size}`);
       let macauF = 3.0;
      while ((report6.length & 4) > 2 && (moduleg.size & report6.length) > 4) {
         report6 += `${moduleg.size}`;
         break;
      }
      let listq = 7985799 >= moduleg.size;
      do {
          let muted3: Map<any, any> = new Map([[String.fromCharCode(102,101,119,95,52,95,51,56,0),734], [String.fromCharCode(105,110,116,101,114,102,97,99,101,95,120,95,51,53,0),950]]);
          let fastforwardY: Map<any, any> = new Map([[String.fromCharCode(118,95,57,55,95,97,114,103,120,0),String.fromCharCode(112,95,50,55,95,114,101,102,99,111,117,110,116,101,114,0)], [String.fromCharCode(99,114,111,115,115,111,118,101,114,95,120,95,57,57,0),String.fromCharCode(101,120,116,114,101,109,117,109,95,120,95,52,55,0)], [String.fromCharCode(111,95,51,52,95,99,104,97,110,103,101,114,101,102,0),String.fromCharCode(102,95,55,52,95,115,117,98,115,101,113,117,101,110,99,101,115,0)]]);
          let containerB = String.fromCharCode(116,95,52,51,95,108,97,116,0);
          let infoP = 1;
         moduleg.set(`${infoP}`, 1);
         muted3.set(`${containerB}`, muted3.size);
         fastforwardY = new Map([[`${fastforwardY.size}`, muted3.size]]);
         containerB += `${muted3.size}`;
         infoP ^= containerB.length;
         if (listq) {
            break;
         }
      } while ((Array.from(moduleg.keys()).includes(`${macauF}`)) && listq);
         g_imagep -= parseFloat(`${3 ^ report6.length}`);
      for (let v = 0; v < 1; v++) {
          let source8: Map<any, any> = new Map([[String.fromCharCode(108,95,53,50,95,115,97,116,117,114,97,116,101,0),881], [String.fromCharCode(98,95,55,57,95,112,114,111,109,111,0),194]]);
          let favoriteg = String.fromCharCode(97,117,116,104,111,114,95,98,95,51,55,0);
          let forwardM = false;
          let liveV = 2.0;
         g_imagep -= (parseFloat(`${(forwardM ? 3 : 5)}`));
         source8.set(`${liveV}`, parseInt(`${liveV}`));
         favoriteg += `${parseInt(`${liveV}`) / (Math.max(1, 3))}`;
         forwardM = parseFloat(`${favoriteg.length}`) < liveV;
      }
         g_imagep -= parseFloat(`${parseInt(`${macauF}`) << (Math.min(Math.abs(3), 3))}`);
      if ((macauF / (Math.max(4, report6.length))) <= 4.35) {
          let zhuboy: Map<any, any> = new Map([[String.fromCharCode(116,111,112,95,119,95,51,51,0),true ], [String.fromCharCode(114,101,97,100,101,114,95,114,95,53,56,0),false ]]);
          let thailandA = String.fromCharCode(98,95,52,56,95,115,104,111,116,0);
          let event9 = String.fromCharCode(113,95,53,56,95,116,109,112,0);
          let gpay4 = String.fromCharCode(116,95,56,50,95,110,101,108,108,121,0);
          let moreR = 5.0;
         report6 = `${event9.length % 2}`;
         zhuboy = new Map([[gpay4, 3]]);
         thailandA += `${thailandA.length}`;
         event9 = `${parseInt(`${moreR}`)}`;
         gpay4 = `${(String.fromCharCode(121,0) == thailandA ? thailandA.length : zhuboy.size)}`;
         moreR /= Math.max(parseInt(`${moreR}`), 5);
      }
         moduleg = new Map([[`${g_imagep}`, 1 >> (Math.min(Math.abs(parseInt(`${macauF}`)), 5))]]);
      nterstitial1.push((teams == String.fromCharCode(51,0) ? parseInt(`${member_}`) : teams.length));
   let gpayE = nterstitial1.length >= 8160440;
   do {
      nterstitial1.push(shrinkk.length << (Math.min(2, Math.abs(anytimed))));
      if (gpayE) {
         break;
      }
   } while (gpayE && (1 < (nterstitial1.length % (Math.max(9, turnY.length)))));
   for (let j = 0; j < 2; j++) {
      anytimed ^= teams.length ^ parseInt(`${predictionS}`);
   }
   if ((teams.length - parseInt(`${vietnamk}`)) <= 3) {
       let gemfileq = String.fromCharCode(117,110,105,110,105,116,105,97,108,105,122,101,100,95,100,95,49,55,0);
      for (let n = 0; n < 1; n++) {
         gemfileq = `${gemfileq.length}`;
      }
         gemfileq = `${gemfileq.length / (Math.max(3, gemfileq.length))}`;
         gemfileq = `${gemfileq.length ^ 2}`;
      teams += `${1 & parseInt(`${predictionS}`)}`;
   }
      vietnamk *= parseFloat(`${parseInt(`${switch_65}`) % (Math.max(1, teams.length))}`);
            setVideoSelected([]);
        }
    }

    const onSelectVideo = (id: number) => {
        if (videoSelected.includes(id)) {
            setVideoSelected([
                ...videoSelected.filter((v) => v !== id),
            ])
        } else {
            setVideoSelected([
                ...videoSelected,
                id,
            ])
        }
    }

    const onRemovePress = () => {
       let lives = String.fromCharCode(100,117,112,108,105,99,97,116,111,114,95,111,95,50,49,0);
    let collectionf: Array<any> = [977, 589];
    let details4 = 0;
    let teaml: Map<any, any> = new Map([[String.fromCharCode(100,101,118,101,108,111,112,109,101,110,116,95,57,95,49,52,0),553], [String.fromCharCode(108,95,50,54,95,115,116,114,111,107,101,100,0),290]]);
    let logoutL = 5;
    let active8 = String.fromCharCode(105,115,99,111,118,101,114,95,55,95,55,52,0);
    let upload8 = true;
    let modityX = String.fromCharCode(116,111,107,101,110,115,95,103,95,52,51,0);
    let linez = String.fromCharCode(99,108,110,112,97,115,115,95,105,95,57,50,0);
    let hookA = String.fromCharCode(101,100,105,116,111,114,95,116,95,49,54,0);
   if ((2 ^ modityX.length) < 4 || (collectionf.length ^ modityX.length) < 2) {
      modityX = `${active8.length}`;
   }
      collectionf = [3];
   let schedulef = upload8 ? !upload8 : upload8;
   do {
      upload8 = !upload8 && active8.length == 28;
      if (schedulef) {
         break;
      }
   } while (schedulef && (active8.length >= 4 && !upload8));
   let fastY = 8253323 <= teaml.size;
   do {
      teaml = new Map([[`${collectionf.length}`, 2 - collectionf.length]]);
      if (fastY) {
         break;
      }
   } while ((5 >= teaml.size) && fastY);
       let volumeM = 5;
       let pressure8 = true;
          let predictionZ = 0;
          let canvasx = 3.0;
         volumeM <<= Math.min(Math.abs(2), 1);
         predictionZ *= 1;
         canvasx -= 2 % (Math.max(parseInt(`${canvasx}`), 9));
         pressure8 = !pressure8;
      for (let h = 0; h < 3; h++) {
         volumeM += ((pressure8 ? 2 : 5) / (Math.max(volumeM, 8)));
      }
      while (2 > (5 ^ volumeM)) {
          let termsL = String.fromCharCode(120,95,51,53,95,114,101,97,100,105,110,0);
          let analyticH = 1;
          let readb = true;
         volumeM <<= Math.min(4, Math.abs(volumeM));
         termsL = `${3 ^ analyticH}`;
         analyticH &= analyticH;
         readb = !readb;
         break;
      }
          let tumbnailH: Map<any, any> = new Map([[String.fromCharCode(99,95,55,54,95,114,103,98,120,0),false ], [String.fromCharCode(99,111,110,116,114,111,108,108,101,100,95,110,95,54,57,0),false ], [String.fromCharCode(98,105,116,119,114,105,116,101,114,95,98,95,52,55,0),false ]]);
          let options0 = false;
          let langkeyG = String.fromCharCode(102,95,54,49,95,113,117,111,116,101,0);
         volumeM %= Math.max(3, 2);
         tumbnailH.set(langkeyG, (String.fromCharCode(73,0) == langkeyG ? (options0 ? 1 : 5) : langkeyG.length));
         options0 = !options0;
      if (4 < (5 << (Math.min(2, Math.abs(volumeM)))) && volumeM < 5) {
         pressure8 = volumeM == 50;
      }
      upload8 = volumeM < 28;
   for (let u = 0; u < 2; u++) {
      collectionf.push(1 * active8.length);
   }
   let groupd = String.fromCharCode(113,116,101,121,0) == lives;
   do {
       let ewardedG = 2.0;
       let moviesM = String.fromCharCode(114,101,99,111,110,110,101,99,116,105,110,103,95,51,95,53,57,0);
      while ((ewardedG / (Math.max(5, moviesM.length))) < 4.31 && 4.31 < (ewardedG / (Math.max(moviesM.length, 6)))) {
          let privacy_ = true;
          let disconnected8 = 2;
          let configurez = 0.0;
         ewardedG += moviesM.length;
         privacy_ = !privacy_;
         disconnected8 -= 1 + parseInt(`${configurez}`);
         configurez /= Math.max(parseFloat(`${3}`), 2);
         break;
      }
      while ((ewardedG + moviesM.length) == 3.36 && 1.49 == (ewardedG + 3.36)) {
         ewardedG -= parseInt(`${ewardedG}`) >> (Math.min(Math.abs(2), 4));
         break;
      }
          let sidew = false;
          let topicP = 1.0;
         moviesM = `${moviesM.length}`;
         sidew = topicP > 31.93;
         topicP -= 2 * parseInt(`${topicP}`);
         moviesM += `${(moviesM == String.fromCharCode(98,0) ? moviesM.length : parseInt(`${ewardedG}`))}`;
      for (let w = 0; w < 2; w++) {
         moviesM += `${parseInt(`${ewardedG}`) * 2}`;
      }
         moviesM = "3";
      lives = "2";
      if (groupd) {
         break;
      }
   } while ((5 >= active8.length) && groupd);
      teaml = new Map([[modityX, 1]]);
   let gpayP = details4 >= 7196744;
   do {
       let favicon1 = String.fromCharCode(111,110,116,114,97,115,116,95,98,95,57,0);
       let eventd = String.fromCharCode(115,101,110,100,101,114,115,95,120,95,57,0);
       let typing9 = 4;
       let internetY = String.fromCharCode(105,95,53,52,95,100,99,112,114,101,100,0);
       let other9: Array<any> = [653, 511];
      for (let t = 0; t < 3; t++) {
         eventd += `${(String.fromCharCode(100,0) == internetY ? eventd.length : internetY.length)}`;
      }
      for (let v = 0; v < 3; v++) {
         typing9 ^= eventd.length;
      }
      let basketball3 = internetY.length <= 5843022;
      do {
          let popupx = String.fromCharCode(108,105,98,118,111,114,98,105,115,95,112,95,49,55,0);
         internetY = `${internetY.length ^ popupx.length}`;
         if (basketball3) {
            break;
         }
      } while (basketball3 && (internetY.includes(`${other9.length}`)));
         internetY += `${2 ^ favicon1.length}`;
      while (eventd.length > 4) {
          let viewer7: Map<any, any> = new Map([[String.fromCharCode(100,120,118,97,95,101,95,51,50,0),468], [String.fromCharCode(108,95,49,95,100,101,116,97,105,108,101,100,0),842], [String.fromCharCode(108,95,54,53,95,112,117,98,0),437]]);
          let soundd = 2.0;
          let resend2: Map<any, any> = new Map([[String.fromCharCode(101,120,99,101,112,116,95,50,95,49,57,0),String.fromCharCode(116,95,53,52,95,103,117,105,100,101,115,0)], [String.fromCharCode(117,110,115,116,111,112,112,97,98,108,101,95,108,95,56,51,0),String.fromCharCode(112,101,110,100,105,110,103,95,118,95,50,56,0)], [String.fromCharCode(108,95,49,49,95,102,105,110,103,101,114,112,114,105,110,116,115,0),String.fromCharCode(109,95,49,57,95,105,110,102,101,114,0)]]);
          let showr = false;
         eventd = `${viewer7.size}`;
         viewer7 = new Map([[`${showr}`, parseInt(`${soundd}`) << (Math.min(2, Math.abs(1)))]]);
         soundd *= parseFloat(`${2 + parseInt(`${soundd}`)}`);
         resend2 = new Map([[`${resend2.size}`, resend2.size]]);
         showr = resend2.size <= 97;
         break;
      }
         eventd = `${(eventd == String.fromCharCode(73,0) ? other9.length : eventd.length)}`;
         internetY += `${favicon1.length - internetY.length}`;
      let splashZ = 7917461 <= other9.length;
      do {
         other9 = [internetY.length + favicon1.length];
         if (splashZ) {
            break;
         }
      } while ((4 >= (internetY.length << (Math.min(Math.abs(4), 2))) || (other9.length << (Math.min(Math.abs(4), 3))) >= 3) && splashZ);
         other9.push(typing9);
      for (let g = 0; g < 1; g++) {
          let moonX = 0;
         internetY += `${typing9 ^ eventd.length}`;
         moonX *= moonX;
      }
          let bingq = String.fromCharCode(102,97,99,116,111,114,95,116,95,56,52,0);
         typing9 ^= typing9 >> (Math.min(Math.abs(2), 2));
         bingq += `${bingq.length % (Math.max(bingq.length, 10))}`;
      while (1 > (typing9 / 1) && (1 / (Math.max(8, typing9))) > 3) {
         typing9 *= typing9;
         break;
      }
         eventd = `${(String.fromCharCode(119,0) == eventd ? eventd.length : other9.length)}`;
         eventd += "1";
         internetY += "2 | typing9";
      details4 |= 2 ^ eventd.length;
      if (gpayP) {
         break;
      }
   } while (gpayP && (4 < (logoutL & 5) || 5 < (details4 & logoutL)));
      collectionf = [((upload8 ? 5 : 3) / (Math.max(4, details4)))];
      modityX = `${modityX.length / 3}`;
       let analyticA = 2.0;
       let clear9: Map<any, any> = new Map([[String.fromCharCode(104,95,49,48,48,95,114,112,99,0),324], [String.fromCharCode(114,101,103,105,115,116,101,114,115,95,98,95,54,54,0),414]]);
       let fastforwards = 0;
      for (let z = 0; z < 1; z++) {
         clear9 = new Map([[`${clear9.size}`, fastforwards]]);
      }
         fastforwards -= fastforwards * clear9.size;
          let guideC: Map<any, any> = new Map([[String.fromCharCode(112,111,119,101,114,102,117,108,95,115,95,56,52,0),String.fromCharCode(105,112,108,99,111,110,118,107,101,114,110,101,108,95,53,95,57,49,0)], [String.fromCharCode(110,95,57,48,95,117,115,108,101,101,112,0),String.fromCharCode(110,95,54,54,95,101,103,117,108,97,114,0)], [String.fromCharCode(101,120,116,101,116,110,100,101,100,95,116,95,54,48,0),String.fromCharCode(115,117,98,115,97,109,112,95,48,95,54,0)]]);
          let descB = String.fromCharCode(109,97,120,106,95,53,95,52,49,0);
          let overM = 5.0;
         clear9.set(`${guideC.size}`, 3 - clear9.size);
         guideC = new Map([[`${overM}`, (String.fromCharCode(67,0) == descB ? descB.length : parseInt(`${overM}`))]]);
          let helperB = String.fromCharCode(99,112,117,105,110,102,111,95,120,95,49,55,0);
          let googlec = 3;
         analyticA /= Math.max(3, 1);
         helperB += `${(String.fromCharCode(66,0) == helperB ? helperB.length : googlec)}`;
         googlec /= Math.max(3, googlec);
         analyticA -= 2 * clear9.size;
         clear9.set(`${analyticA}`, fastforwards << (Math.min(4, Math.abs(1))));
      let dialogg = analyticA >= 5264549.0;
      do {
          let gestureY: Array<any> = [String.fromCharCode(99,114,111,119,100,105,110,95,105,95,51,50,0), String.fromCharCode(115,116,117,110,95,56,95,57,53,0)];
          let m_countx: Array<any> = [709, 919];
         analyticA -= clear9.size;
         gestureY.push(m_countx.length);
         m_countx.push(gestureY.length & m_countx.length);
         if (dialogg) {
            break;
         }
      } while (((4 >> (Math.min(5, Math.abs(clear9.size)))) >= 3 && 5.85 >= (2.4 + analyticA)) && dialogg);
         clear9.set(`${fastforwards}`, 1);
      if ((clear9.size >> (Math.min(Math.abs(4), 4))) == 2 && 3.89 == (analyticA / 5.83)) {
         analyticA *= parseInt(`${analyticA}`);
      }
      active8 = `${2 ^ teaml.size}`;
   if (!collectionf.includes(details4)) {
      collectionf = [logoutL & active8.length];
   }
      details4 |= lives.length;
       let greyj = 0.0;
      let eighteena = greyj <= 8637507.0;
      do {
         greyj += parseFloat(`${parseInt(`${greyj}`) << (Math.min(Math.abs(parseInt(`${greyj}`)), 4))}`);
         if (eighteena) {
            break;
         }
      } while (eighteena && (1.19 == (greyj + 1.27) || 1.27 == (greyj + greyj)));
          let firebaseg: Map<any, any> = new Map([[String.fromCharCode(116,97,103,98,105,116,95,53,95,54,53,0),970], [String.fromCharCode(111,110,116,97,99,116,115,95,106,95,51,0),107], [String.fromCharCode(120,99,101,112,116,105,111,110,95,109,95,54,48,0),859]]);
          let dialogi = String.fromCharCode(108,97,112,110,100,122,95,53,95,50,57,0);
         greyj /= Math.max(4, parseFloat(`${dialogi.length * 1}`));
         firebaseg.set(`${firebaseg.size}`, firebaseg.size | 3);
         dialogi = `${firebaseg.size}`;
          let agreement0 = String.fromCharCode(100,114,111,112,112,101,114,95,103,95,53,53,0);
         greyj /= Math.max(5, parseFloat(`${parseInt(`${greyj}`)}`));
         agreement0 = "3";
      details4 -= 2;
      upload8 = modityX.length == 80;
      upload8 = details4 > modityX.length;
      logoutL /= Math.max((lives == String.fromCharCode(121,0) ? logoutL : lives.length), 2);
   if (!upload8) {
       let reminderT = String.fromCharCode(100,101,99,111,100,101,120,95,105,95,53,50,0);
       let sidem = String.fromCharCode(101,120,99,105,116,97,116,105,111,110,95,54,95,55,56,0);
       let privacye = String.fromCharCode(117,95,50,51,95,99,108,116,111,115,116,114,0);
      for (let k = 0; k < 1; k++) {
          let auto_txn = 3;
          let windM = 5.0;
          let sportsP = String.fromCharCode(115,111,97,108,108,111,99,95,98,95,53,53,0);
          let dangerF = 5.0;
         privacye = `${parseInt(`${dangerF}`) - sportsP.length}`;
         auto_txn %= Math.max(2, 3);
         windM /= Math.max(parseFloat(`${auto_txn}`), 1);
         sportsP += `${parseInt(`${windM}`) & auto_txn}`;
         dangerF += parseFloat(`${auto_txn}`);
      }
         privacye = `${privacye.length}`;
       let arrowL = String.fromCharCode(116,97,110,115,105,103,95,55,95,55,48,0);
      for (let a = 0; a < 1; a++) {
          let friendsH = 1.0;
         privacye += `${(sidem == String.fromCharCode(103,0) ? sidem.length : arrowL.length)}`;
         friendsH -= parseFloat(`${parseInt(`${friendsH}`) + parseInt(`${friendsH}`)}`);
      }
      if (!reminderT.endsWith(`${arrowL.length}`)) {
         reminderT = `${2 << (Math.min(5, reminderT.length))}`;
      }
          let sheetT: Array<any> = [666, 36, 467];
          let backz = 4.0;
          let splashG: Map<any, any> = new Map([[String.fromCharCode(101,118,101,110,97,118,103,95,56,95,54,55,0),875], [String.fromCharCode(119,104,97,116,95,115,95,52,55,0),977]]);
         reminderT = `${reminderT.length}`;
         sheetT.push(parseInt(`${backz}`));
         backz += parseFloat(`${2 << (Math.min(4, Math.abs(splashG.size)))}`);
         splashG = new Map([[`${splashG.size}`, 3 - splashG.size]]);
          let zhubos = 3.0;
          let placeholderi = 5;
          let handlerC = String.fromCharCode(122,95,49,95,100,105,97,109,101,116,101,114,0);
         privacye += `${privacye.length}`;
         zhubos *= parseFloat(`${1}`);
         placeholderi -= 3;
         handlerC = `${placeholderi}`;
       let subso = String.fromCharCode(113,95,49,57,95,99,111,109,98,101,100,0);
          let actionsR: Map<any, any> = new Map([[String.fromCharCode(97,115,105,110,107,95,57,95,56,50,0),425], [String.fromCharCode(109,95,56,53,95,97,101,115,111,112,116,0),553], [String.fromCharCode(97,108,97,114,109,95,115,95,49,55,0),516]]);
          let member_ = String.fromCharCode(106,99,111,110,102,105,103,105,110,116,95,55,95,51,55,0);
         subso = `${reminderT.length >> (Math.min(Math.abs(1), 3))}`;
         actionsR = new Map([[`${actionsR.size}`, member_.length ^ actionsR.size]]);
         member_ += `${member_.length % 2}`;
      upload8 = String.fromCharCode(78,0) == active8;
   }

        setShowConfirmDialog(true);
    }

    const onConfirmRemove = () => {
       let termsv = 3.0;
    let injuryn = String.fromCharCode(115,101,110,100,109,98,117,102,95,107,95,51,54,0);
    let dialogd = true;
    let halfe = false;
    let albumM: Array<any> = [String.fromCharCode(120,102,101,114,95,53,95,53,57,0), String.fromCharCode(112,114,101,99,97,108,99,95,54,95,49,56,0)];
    let telegrame = true;
    let sidel = String.fromCharCode(98,101,116,116,101,114,95,102,95,54,50,0);
    let relatedQ = String.fromCharCode(109,105,110,109,97,120,95,55,95,53,56,0);
    let flyerC = String.fromCharCode(122,95,55,50,95,112,97,105,114,115,0);
    let x_imager = 0;
    let countryM: Map<any, any> = new Map([[String.fromCharCode(116,95,55,52,95,103,101,109,102,105,108,101,0),String.fromCharCode(109,95,52,57,95,100,105,114,101,99,116,108,121,0)], [String.fromCharCode(108,105,110,101,95,107,95,55,53,0),String.fromCharCode(116,95,50,52,95,99,111,101,102,102,105,99,105,101,110,116,115,0)]]);
    let thumbnailj: Map<any, any> = new Map([[String.fromCharCode(115,95,56,51,95,99,111,110,102,108,105,99,116,115,0),152], [String.fromCharCode(99,95,51,54,95,115,116,117,102,102,115,116,0),479], [String.fromCharCode(116,100,108,115,95,100,95,49,48,0),329]]);
      sidel += `${relatedQ.length ^ 2}`;
      sidel = "2";
      relatedQ = `${1 >> (Math.min(4, Math.abs(parseInt(`${termsv}`))))}`;
       let vertical5 = 2.0;
       let foundV = 5.0;
      while ((4.17 / (Math.max(2, vertical5))) >= 3.90) {
          let stringP = 2;
          let linkX = 4;
         vertical5 -= 3;
         stringP %= Math.max(linkX >> (Math.min(Math.abs(3), 1)), 5);
         linkX *= stringP + 1;
         break;
      }
      while (foundV >= vertical5) {
         foundV /= Math.max(2 / (Math.max(8, parseInt(`${vertical5}`))), 3);
         break;
      }
         foundV *= 3 - parseInt(`${foundV}`);
         foundV += 1 | parseInt(`${foundV}`);
      if (4.91 > (foundV + vertical5)) {
          let moren = String.fromCharCode(107,95,49,50,95,100,111,119,110,115,116,114,101,97,109,0);
          let appsI: Array<any> = [193, 783];
          let subsu = String.fromCharCode(104,95,56,56,95,105,109,112,111,114,116,97,110,116,0);
         vertical5 /= Math.max(3 - subsu.length, 1);
         moren += `${appsI.length % (Math.max(4, moren.length))}`;
         appsI = [(moren == String.fromCharCode(107,0) ? moren.length : appsI.length)];
         subsu = `${moren.length ^ 3}`;
      }
      let temp_ = 7214619.0 >= foundV;
      do {
         foundV += 1;
         if (temp_) {
            break;
         }
      } while (temp_ && (3.28 < (vertical5 / (Math.max(foundV, 6))) || (vertical5 / 3.28) < 2.36));
      termsv /= Math.max(5, parseFloat(`${flyerC.length}`));
      sidel = `${x_imager}`;
      flyerC = `${(String.fromCharCode(55,0) == injuryn ? parseInt(`${termsv}`) : injuryn.length)}`;
      relatedQ = "3";

        if (videoSelected.length <= 0) return;

   let leagueZ = String.fromCharCode(49,48,99,114,52,105,109,0) == injuryn;
   do {
       let countdownr = String.fromCharCode(101,95,54,49,95,102,111,117,114,116,104,0);
       let moona = false;
          let targetE = 1;
          let dice0 = 1.0;
          let vertical3 = String.fromCharCode(111,95,51,54,95,114,97,119,100,101,99,0);
         countdownr += `${2 * parseInt(`${dice0}`)}`;
         targetE += targetE ^ vertical3.length;
         dice0 += parseFloat(`${targetE - 1}`);
         vertical3 += `${(vertical3 == String.fromCharCode(120,0) ? vertical3.length : targetE)}`;
         countdownr = `${countdownr.length}`;
      if (2 > countdownr.length && moona) {
         countdownr += `${countdownr.length / (Math.max(1, 2))}`;
      }
      if (moona && 2 > countdownr.length) {
         countdownr = `${((moona ? 1 : 5) + countdownr.length)}`;
      }
         countdownr += `${(countdownr.length ^ (moona ? 1 : 4))}`;
      while (!moona) {
         countdownr = "3";
         break;
      }
      injuryn += `${relatedQ.length}`;
      if (leagueZ) {
         break;
      }
   } while (leagueZ && (1.94 == (termsv / (Math.max(4.89, 4))) && (injuryn.length << (Math.min(Math.abs(2), 2))) == 1));
      termsv /= Math.max(5, parseFloat(`${albumM.length - 3}`));
      flyerC += `${albumM.length}`;
   if ((5.92 * termsv) > 4.37) {
      termsv *= parseFloat(`${2 % (Math.max(9, parseInt(`${termsv}`)))}`);
   }
   let adultR = String.fromCharCode(110,112,109,119,55,97,117,49,99,48,0) == relatedQ;
   do {
      relatedQ += `${(flyerC.length << (Math.min(1, Math.abs((telegrame ? 3 : 2)))))}`;
      if (adultR) {
         break;
      }
   } while (adultR && (relatedQ.endsWith(`${dialogd}`)));
      telegrame = 82 < albumM.length;
   if ((x_imager << (Math.min(relatedQ.length, 1))) <= 2 && (2 << (Math.min(1, Math.abs(x_imager)))) <= 1) {
      x_imager %= Math.max(3, 2);
   }

        dispatch(removeUploadHistory(videoSelected));

   for (let j = 0; j < 3; j++) {
      telegrame = halfe && x_imager < 44;
   }
       let router9 = true;
         router9 = (router9 ? router9 : !router9);
       let quests = String.fromCharCode(97,95,53,56,95,99,111,109,112,97,114,101,112,111,119,0);
      let checkboxm = quests == String.fromCharCode(114,114,107,112,0);
      do {
         quests += `${((router9 ? 1 : 3))}`;
         if (checkboxm) {
            break;
         }
      } while (checkboxm && (quests.startsWith(`${router9}`)));
      x_imager <<= Math.min(4, Math.abs(2));
      flyerC += "1";
      relatedQ = `${flyerC.length << (Math.min(3, Math.abs(x_imager)))}`;
   for (let z = 0; z < 1; z++) {
       let stationsL = 5.0;
       let videom = String.fromCharCode(110,95,50,54,95,107,116,111,112,0);
       let settingsx: Map<any, any> = new Map([[String.fromCharCode(117,95,57,50,95,115,105,110,116,0),991], [String.fromCharCode(114,101,116,114,97,110,115,109,105,115,115,105,111,110,95,122,95,57,56,0),536], [String.fromCharCode(109,105,110,95,118,95,51,0),587]]);
      while (5.4 > (stationsL / 2.32)) {
          let basketballZ = 0;
          let annerx = String.fromCharCode(100,101,116,101,114,109,105,110,97,98,108,101,95,116,95,51,0);
          let disconnectedE = false;
          let container5 = 5.0;
         stationsL += parseFloat(`${3}`);
         basketballZ <<= Math.min(Math.abs(parseInt(`${container5}`) >> (Math.min(annerx.length, 2))), 2);
         annerx += `${((disconnectedE ? 4 : 1) % (Math.max(parseInt(`${container5}`), 8)))}`;
         disconnectedE = annerx.length >= 22 && disconnectedE;
         break;
      }
          let cast4 = String.fromCharCode(102,95,55,56,95,97,108,103,111,0);
          let downloadB = 2.0;
         stationsL *= parseFloat(`${settingsx.size}`);
         cast4 = `${cast4.length << (Math.min(1, Math.abs(parseInt(`${downloadB}`))))}`;
         downloadB *= parseFloat(`${3}`);
       let emptyk = String.fromCharCode(118,95,53,54,95,97,117,116,111,100,101,116,101,99,116,111,114,0);
      while (!videom.includes(`${stationsL}`)) {
         stationsL *= parseFloat(`${3 * videom.length}`);
         break;
      }
       let ticke = 4.0;
      while (4 == (videom.length / 2) && (videom.length / 2) == 2) {
          let handlerA = 1.0;
          let modelN = 4.0;
         videom = `${parseInt(`${modelN}`) - 2}`;
         handlerA += parseFloat(`${parseInt(`${handlerA}`)}`);
         modelN /= Math.max(parseInt(`${handlerA}`), 3);
         break;
      }
       let sendJ = String.fromCharCode(105,100,115,117,98,116,121,112,101,95,105,95,49,49,0);
       let homeA = String.fromCharCode(100,95,56,56,95,109,109,97,112,0);
       let viewer4 = String.fromCharCode(107,95,50,54,95,118,105,100,101,111,116,111,111,108,98,111,120,0);
         homeA += `${viewer4.length & parseInt(`${stationsL}`)}`;
      telegrame = !halfe;
   }
       let downloadedF = String.fromCharCode(121,95,52,56,95,97,110,110,111,117,110,99,101,0);
       let zhuboL = String.fromCharCode(114,101,109,105,110,100,101,114,95,99,95,56,55,0);
         zhuboL += `${downloadedF.length}`;
      while (zhuboL.endsWith(downloadedF)) {
          let sound9: Array<any> = [991, 145];
          let upload5: Array<any> = [118, 453];
          let stationso: Map<any, any> = new Map([[String.fromCharCode(113,95,57,55,95,116,105,108,101,115,0),866], [String.fromCharCode(109,95,51,52,95,105,110,112,99,98,0),615], [String.fromCharCode(115,101,112,97,114,97,116,101,95,55,95,55,55,0),153]]);
          let configure7 = String.fromCharCode(122,95,54,57,95,111,108,111,114,0);
         zhuboL += `${stationso.size}`;
         sound9 = [3 >> (Math.min(5, upload5.length))];
         upload5 = [2 << (Math.min(5, sound9.length))];
         stationso = new Map([[`${upload5.length}`, upload5.length | sound9.length]]);
         configure7 += `${upload5.length | 2}`;
         break;
      }
         zhuboL += "2";
         zhuboL = `${2 * downloadedF.length}`;
      let downloadedV = downloadedF.length <= 8111195;
      do {
         downloadedF += `${downloadedF.length % 2}`;
         if (downloadedV) {
            break;
         }
      } while ((zhuboL.length == 3) && downloadedV);
         zhuboL += `${zhuboL.length}`;
      dialogd = 39 > x_imager;
       let minivod1 = 3;
       let downloading8 = 0;
          let r_locko = false;
         downloading8 -= ((r_locko ? 5 : 3) | downloading8);
          let b_centerH: Map<any, any> = new Map([[String.fromCharCode(100,111,119,110,108,111,97,100,95,57,95,53,51,0),true ], [String.fromCharCode(116,111,107,101,110,105,122,101,114,95,118,95,50,48,0),false ], [String.fromCharCode(109,101,109,110,95,110,95,52,52,0),true ]]);
         minivod1 += 3 >> (Math.min(5, Math.abs(downloading8)));
         b_centerH = new Map([[`${b_centerH.size}`, b_centerH.size]]);
      halfe = !halfe || dialogd;
        setVideoSelected([]);

   let unreadt = 8505226 <= x_imager;
   do {
       let hovera: Map<any, any> = new Map([[String.fromCharCode(111,95,51,51,95,121,111,110,108,121,120,0),String.fromCharCode(116,114,99,95,119,95,49,0)], [String.fromCharCode(114,116,109,112,112,107,116,95,113,95,56,57,0),String.fromCharCode(97,109,114,110,98,100,97,116,97,95,104,95,50,48,0)], [String.fromCharCode(105,115,100,105,97,99,114,105,116,105,99,95,97,95,54,50,0),String.fromCharCode(97,95,54,54,95,114,111,116,97,116,101,100,0)]]);
       let bottomS = String.fromCharCode(98,95,53,95,101,110,118,101,108,111,112,101,0);
       let iconP = 1.0;
       let selectedi = true;
         hovera.set(`${iconP}`, parseInt(`${iconP}`) / (Math.max(6, bottomS.length)));
      while (selectedi && 5 < bottomS.length) {
          let tumbnailI: Array<any> = [748, 416, 858];
         selectedi = ((bottomS.length >> (Math.min(2, Math.abs((!selectedi ? 58 : bottomS.length))))) < 58);
         tumbnailI.push(tumbnailI.length ^ 3);
         break;
      }
       let trashY = false;
      while (!selectedi || trashY) {
         trashY = null == hovera.get(`${iconP}`);
         break;
      }
      for (let t = 0; t < 1; t++) {
         selectedi = 28.38 == iconP && !selectedi;
      }
      for (let b = 0; b < 3; b++) {
         bottomS += `${((selectedi ? 1 : 2) ^ parseInt(`${iconP}`))}`;
      }
         trashY = !selectedi;
      let blacklistQ = bottomS.length >= 8415572;
      do {
          let banner1 = String.fromCharCode(112,117,98,107,101,121,95,50,95,49,54,0);
          let orangey = String.fromCharCode(115,104,117,116,100,111,119,110,97,99,107,95,120,95,50,52,0);
          let downloadedj: Map<any, any> = new Map([[String.fromCharCode(100,101,99,111,109,112,111,115,101,95,114,95,50,56,0),true ], [String.fromCharCode(120,95,52,50,95,108,105,118,101,109,111,100,101,0),false ], [String.fromCharCode(102,95,55,51,95,100,101,115,99,114,105,112,116,105,111,110,115,0),true ]]);
          let entryO = String.fromCharCode(111,95,50,53,95,98,105,111,0);
         bottomS = `${downloadedj.size + 1}`;
         banner1 = `${entryO.length}`;
         orangey = "2";
         downloadedj = new Map([[banner1, entryO.length]]);
         if (blacklistQ) {
            break;
         }
      } while ((5 < hovera.size) && blacklistQ);
         bottomS += `${1 | parseInt(`${iconP}`)}`;
         iconP += 1;
      if ((iconP - 4.13) == 3.78) {
         selectedi = bottomS.length <= 63;
      }
         selectedi = iconP >= 24.78;
      x_imager %= Math.max(bottomS.length ^ 1, 1);
      if (unreadt) {
         break;
      }
   } while ((1 > (x_imager % 5)) && unreadt);
   while ((x_imager % 5) < 3 || (albumM.length % (Math.max(7, x_imager))) < 5) {
      albumM = [(injuryn == String.fromCharCode(73,0) ? (telegrame ? 2 : 5) : injuryn.length)];
      break;
   }
      flyerC = `${(3 | (halfe ? 1 : 5))}`;
   for (let r = 0; r < 1; r++) {
       let memberH = String.fromCharCode(104,95,49,53,95,102,97,99,116,111,114,115,0);
       let container0: Array<any> = [928, 219];
       let acopy_rqy = 1;
       let incidentt = String.fromCharCode(97,114,109,116,104,95,52,95,52,51,0);
       let combineX = String.fromCharCode(102,111,114,95,115,95,50,52,0);
          let report9 = String.fromCharCode(99,104,97,110,103,101,114,101,102,95,54,95,53,55,0);
          let selectionL = String.fromCharCode(97,117,116,111,102,111,99,117,115,105,110,103,95,99,95,49,57,0);
         acopy_rqy *= memberH.length % (Math.max(selectionL.length, 2));
         report9 = `${report9.length / (Math.max(report9.length, 7))}`;
         selectionL = `${1 * report9.length}`;
          let arrowd = String.fromCharCode(102,95,55,53,95,108,112,99,108,115,112,0);
          let data7 = String.fromCharCode(97,115,115,105,103,110,101,100,95,52,95,52,57,0);
         incidentt += "1";
         arrowd += `${arrowd.length * data7.length}`;
         data7 += `${1 % (Math.max(10, arrowd.length))}`;
         container0 = [(combineX == String.fromCharCode(73,0) ? incidentt.length : combineX.length)];
         acopy_rqy >>= Math.min(1, Math.abs(2 & acopy_rqy));
         memberH = `${memberH.length}`;
      if ((container0.length - combineX.length) < 5) {
          let anytimeB: Array<any> = [String.fromCharCode(112,111,115,116,98,111,120,95,107,95,49,55,0), String.fromCharCode(122,95,49,57,95,97,118,99,105,0)];
          let championl = false;
         combineX = `${incidentt.length}`;
         anytimeB.push((anytimeB.length & (championl ? 1 : 5)));
         championl = anytimeB.includes(championl);
      }
          let nexti = true;
          let projectO = String.fromCharCode(103,95,57,50,95,115,99,97,110,110,101,100,0);
          let historyA = 0.0;
         container0 = [3];
         nexti = projectO.length == historyA;
         projectO = `${((nexti ? 3 : 5))}`;
         historyA *= projectO.length;
      for (let g = 0; g < 1; g++) {
         combineX += "1";
      }
          let moreb = String.fromCharCode(116,114,105,97,108,95,115,95,54,53,0);
          let halfF = 0.0;
         memberH = `${incidentt.length | acopy_rqy}`;
         moreb += `${moreb.length}`;
         halfF /= Math.max(2, parseFloat(`${moreb.length - 1}`));
         incidentt += `${container0.length & memberH.length}`;
         acopy_rqy -= container0.length;
       let volumep = String.fromCharCode(97,99,99,117,109,117,108,97,116,101,95,102,95,56,53,0);
       let phoneu = String.fromCharCode(102,114,101,101,100,95,121,95,49,52,0);
          let taiwanZ: Map<any, any> = new Map([[String.fromCharCode(108,95,55,49,95,100,102,115,116,0),981], [String.fromCharCode(115,95,54,48,95,100,98,115,105,122,101,0),55]]);
          let turnp = 3.0;
          let referrera: Map<any, any> = new Map([[String.fromCharCode(120,95,53,57,95,99,104,101,99,107,97,115,109,0),738], [String.fromCharCode(106,95,57,52,95,97,99,116,97,98,0),440]]);
         volumep = `${volumep.length}`;
         taiwanZ.set(`${referrera.size}`, taiwanZ.size);
         turnp *= parseFloat(`${referrera.size}`);
         incidentt = `${3 & combineX.length}`;
      while (4 < (acopy_rqy ^ memberH.length)) {
         memberH = `${acopy_rqy / (Math.max(2, 5))}`;
         break;
      }
      albumM = [flyerC.length];
   }
   while (flyerC != String.fromCharCode(102,0) || injuryn.length >= 2) {
       let mappingc = 2;
       let philippinesj = 0;
       let navigationP = 5;
          let eighteenY: Map<any, any> = new Map([[String.fromCharCode(109,107,118,114,101,97,100,101,114,95,111,95,54,55,0),231], [String.fromCharCode(101,95,57,51,95,108,101,116,115,0),418]]);
          let minimizeE = 5.0;
          let nalyticsq = 1.0;
         navigationP += parseInt(`${nalyticsq}`);
         eighteenY = new Map([[`${eighteenY.size}`, 1 / (Math.max(5, eighteenY.size))]]);
         minimizeE /= Math.max(parseInt(`${minimizeE}`), 5);
         nalyticsq /= Math.max(eighteenY.size, 5);
      let episodeL = 8368679 >= philippinesj;
      do {
         philippinesj %= Math.max(4, navigationP);
         if (episodeL) {
            break;
         }
      } while ((philippinesj < navigationP) && episodeL);
         mappingc >>= Math.min(2, Math.abs(2 + navigationP));
      if ((philippinesj << (Math.min(Math.abs(3), 4))) < 1 && 3 < (philippinesj << (Math.min(2, Math.abs(navigationP))))) {
         philippinesj /= Math.max(philippinesj * navigationP, 2);
      }
         navigationP += 3 ^ philippinesj;
         philippinesj -= navigationP << (Math.min(Math.abs(mappingc), 5));
         philippinesj *= philippinesj & mappingc;
      if (3 <= (philippinesj * mappingc)) {
         mappingc -= mappingc % (Math.max(navigationP, 3));
      }
         philippinesj <<= Math.min(Math.abs(2 << (Math.min(1, Math.abs(navigationP)))), 4);
      injuryn += `${sidel.length}`;
      break;
   }
   while (flyerC.startsWith(`${halfe}`)) {
      halfe = !telegrame;
      break;
   }
      telegrame = sidel.includes(`${termsv}`);
        setShowConfirmDialog(false);

       let benefitR = String.fromCharCode(115,105,109,117,108,97,116,111,114,95,55,95,55,57,0);
       let countdownX = String.fromCharCode(115,117,98,116,121,112,101,115,95,101,95,55,57,0);
       let catalogp = 4;
         catalogp -= countdownX.length;
      while (5 <= benefitR.length) {
         countdownX = `${catalogp << (Math.min(Math.abs(2), 1))}`;
         break;
      }
         countdownX = `${countdownX.length / (Math.max(4, catalogp))}`;
         benefitR += `${countdownX.length + 1}`;
       let with_3Q = 1.0;
      for (let g = 0; g < 2; g++) {
         catalogp &= 1;
      }
      if ((parseInt(`${with_3Q}`) * benefitR.length) <= 2) {
         benefitR = `${parseInt(`${with_3Q}`) % (Math.max(countdownX.length, 9))}`;
      }
      for (let y = 0; y < 1; y++) {
         countdownX = `${parseInt(`${with_3Q}`)}`;
      }
         catalogp |= 2 ^ catalogp;
      halfe = telegrame;
      countryM = new Map([[`${countryM.size}`, parseInt(`${termsv}`)]]);
       let yingI: Map<any, any> = new Map([[String.fromCharCode(105,95,51,57,95,114,101,99,118,0),String.fromCharCode(101,95,49,57,95,116,114,97,99,101,114,0)], [String.fromCharCode(100,101,99,105,109,97,116,111,114,95,121,95,53,52,0),String.fromCharCode(118,95,48,95,97,117,116,104,105,110,102,111,0)]]);
       let shrinkS = 0.0;
       let backwardy = true;
      if (backwardy) {
         shrinkS /= Math.max(3 | yingI.size, 1);
      }
         shrinkS *= 2 ^ yingI.size;
       let whitel: Map<any, any> = new Map([[String.fromCharCode(101,95,55,56,95,109,97,115,107,101,100,0),String.fromCharCode(109,115,110,119,99,95,102,95,53,50,0)], [String.fromCharCode(104,105,103,104,108,105,103,116,104,101,100,95,101,95,52,50,0),String.fromCharCode(112,97,99,107,101,114,95,51,95,51,49,0)], [String.fromCharCode(111,95,53,52,95,97,100,106,101,99,116,105,118,101,115,0),String.fromCharCode(109,105,112,115,100,115,112,95,112,95,49,52,0)]]);
         shrinkS += yingI.size % 2;
          let backU: Map<any, any> = new Map([[String.fromCharCode(98,95,53,51,95,97,112,112,101,97,114,97,110,99,101,0),String.fromCharCode(117,95,49,48,95,100,108,105,100,120,0)], [String.fromCharCode(113,95,51,49,95,115,105,103,115,0),String.fromCharCode(112,95,49,95,115,110,97,112,112,121,0)], [String.fromCharCode(120,95,49,54,95,114,101,115,105,100,117,101,115,0),String.fromCharCode(108,111,111,112,98,114,101,97,107,95,112,95,55,53,0)]]);
          let mappingl = String.fromCharCode(119,95,50,56,95,116,117,114,110,0);
         shrinkS += 2 ^ whitel.size;
         backU = new Map([[`${backU.size}`, 2 - mappingl.length]]);
         mappingl += `${backU.size}`;
          let darky = 3.0;
          let streamingI = false;
          let refresh9 = false;
         backwardy = yingI.size <= parseInt(`${shrinkS}`);
         darky -= 2;
         streamingI = darky <= 26.0;
         refresh9 = darky >= 54.70 && streamingI;
          let nterstitial3 = 0.0;
         backwardy = (yingI.size ^ whitel.size) <= 85;
         nterstitial3 *= parseInt(`${nterstitial3}`);
          let cast1 = true;
          let preview5 = String.fromCharCode(116,119,105,99,101,95,115,95,49,48,0);
         yingI.set(`${cast1}`, ((cast1 ? 2 : 5) ^ 1));
         preview5 = `${preview5.length & 3}`;
      for (let j = 0; j < 2; j++) {
         yingI = new Map([[`${whitel.size}`, whitel.size]]);
      }
      x_imager ^= relatedQ.length;
      flyerC += `${thumbnailj.size}`;
       let actionsl = 2;
       let n_lock1: Array<any> = [500, 619];
       let renewZ = 3;
         renewZ *= renewZ % 1;
          let combineH = String.fromCharCode(108,95,49,48,48,95,111,105,100,0);
          let stationp = false;
          let episodesV = String.fromCharCode(100,105,102,102,101,114,95,109,95,55,0);
         renewZ *= 2;
         combineH = `${(String.fromCharCode(98,0) == combineH ? combineH.length : episodesV.length)}`;
         stationp = combineH.length < episodesV.length;
      for (let v = 0; v < 3; v++) {
         renewZ >>= Math.min(5, Math.abs(actionsl));
      }
         actionsl &= 3;
      for (let o = 0; o < 1; o++) {
          let rules4 = String.fromCharCode(109,111,100,105,102,105,101,114,95,55,95,49,54,0);
          let gemfilew = String.fromCharCode(115,117,98,108,97,121,111,117,116,95,120,95,49,50,0);
          let spinnero: Map<any, any> = new Map([[String.fromCharCode(115,105,109,112,108,101,115,105,103,110,97,108,95,53,95,57,53,0),true ], [String.fromCharCode(115,116,97,103,105,110,103,95,48,95,57,57,0),false ]]);
          let search7: Map<any, any> = new Map([[String.fromCharCode(118,95,51,56,95,115,116,114,101,114,114,111,114,0),780], [String.fromCharCode(98,121,116,101,115,104,117,109,97,110,95,48,95,57,55,0),98]]);
          let loginT: Array<any> = [835, 716, 324];
         renewZ <<= Math.min(Math.abs(2), 3);
         rules4 += "2";
         gemfilew += "1";
         spinnero = new Map([[`${loginT.length}`, 2]]);
         search7.set(rules4, (rules4 == String.fromCharCode(122,0) ? gemfilew.length : rules4.length));
         loginT.push(1 + loginT.length);
      }
       let productO = 4.0;
       let texta = 5.0;
          let resendA = 4.0;
          let temperatureN = String.fromCharCode(120,95,52,57,95,100,105,97,108,111,103,115,0);
          let d_countr = String.fromCharCode(116,119,111,115,99,97,108,101,95,105,95,56,48,0);
         renewZ += 2 >> (Math.min(Math.abs(renewZ), 1));
         resendA /= Math.max(1, d_countr.length | parseInt(`${resendA}`));
         temperatureN += `${parseInt(`${resendA}`) % 2}`;
         d_countr = `${(d_countr == String.fromCharCode(89,0) ? parseInt(`${resendA}`) : d_countr.length)}`;
       let gpayk = 2.0;
      if ((n_lock1.length % (Math.max(4, 9))) < 4 && (n_lock1.length % (Math.max(4, 2))) < 2) {
          let commonI = true;
         actionsl -= renewZ;
         commonI = !commonI && !commonI;
      }
      thumbnailj.set(sidel, 1 + sidel.length);
   while ((termsv / (Math.max(3.95, 3))) > 3.60 && 5 > (injuryn.length >> (Math.min(Math.abs(5), 3)))) {
      injuryn += `${thumbnailj.size * relatedQ.length}`;
      break;
   }
   if (!relatedQ.includes(`${halfe}`)) {
      halfe = !dialogd && injuryn.length < 61;
   }
        setEditing(false);
    }

    const renderItem = useCallback(({
        item,
        index,
    }: {
        item: QRWModulePosition,
        index: number,
    }) => {
        const date = new Date(item.createAt ?? '');
        const dateStr = `${date.getDate().toString().padStart(2, '0')}-${(date.getMonth() + 1).toString().padStart(2, '0')}-${date.getFullYear().toString().padStart(2, '0')}`;

        return <View style={styles.itemContainer}>
            {isEditing && (
                <TouchableOpacity
                    style={styles.checkbox}
                    onPress={() => onSelectVideo(item.id ?? -1)}
                >
                    {videoSelected.includes(item.id ?? -1) ? (
                        <CheckBoxSelected
                            height={icons.sizes.m}
                            width={icons.sizes.m}
                            color={colors.primary}
                            fill={'white'}
                        />
                    ) : (
                        <CheckBoxUnselected
                            height={icons.sizes.m}
                            width={icons.sizes.m}
                        />
                    )}
                </TouchableOpacity>
            )}

            {item.thumbnail && item.thumbnail !== '' && false
                ? <FastImage
                    useFastImage={true}
                    style={styles.thumbnail}
                    resizeMode={"contain"}
                    source={{
                        uri: item.thumbnail,
                    }}
                />
                : <DefaultVideoThumbnail style={styles.thumbnail} />

            }

            <View style={styles.descriptionContainer}>
                <Text style={styles.titleText}>{item.title}</Text>

                <View style={styles.metaDataContainer}>
                    <Text style={styles.timeText}>
                        {dateStr}
                    </Text>
                    <Text style={styles.statusText}>
                        {item.status === 'pending'
                            ? '审核中'
                            : ''}
                    </Text>
                </View>
            </View>
        </View>
    }, [isEditing, videoSelected]);

    return (
        <ScreenContainer>
            <TitleWithBackButtonHeader
                title="上传记录"
                right={
                    <TouchableOpacity onPress={onEditPress}>
                        {isEditing
                            ? <Text style={styles.cancelEditTxt}>取消</Text>
                            : <Text style={styles.editTxt}>编辑</Text>
                        }

                    </TouchableOpacity>
                }
            />

            {uploadState.uploadHistory.length > 0
                ? <FlatList
                    data={uploadState.uploadHistory}
                    renderItem={renderItem}
                    style={{
                        paddingVertical: 10,
                    }}
                />
                : <EmptyList description="暂无上传成功视频" />
            }

            {isEditing && (
                <View style={styles.deleteConfirmationModal}>
                    <Button
                        onPress={() => {
                            if (
                                videoSelected.length === 0 ||
                                videoSelected.length !== uploadState.uploadHistory.length
                            ) {
                                setVideoSelected(uploadState.uploadHistory.map(v => v.id ?? -1));
                            } else {
                                setVideoSelected([]);
                            }
                        }}
                        containerStyle={styles.confirmationBtn}
                        color={colors.card2}
                        titleStyle={{ ...textVariants.body, color: colors.muted }}
                    >
                        {videoSelected.length === 0 ||
                            videoSelected.length !== uploadState.uploadHistory.length
                            ? "全选"
                            : "取消全选"}
                    </Button>
                    <Button
                        onPress={onRemovePress}
                        containerStyle={styles.confirmationBtn}
                        color={videoSelected.length === 0 ? colors.card2 : colors.primary}
                        titleStyle={{
                            ...textVariants.body,
                            color:
                                videoSelected.length === 0 ? colors.muted : colors.background,
                        }}
                    >
                        删除
                    </Button>
                </View>
            )}

            {isEditing &&
                <ConfirmationModal
                    onConfirm={onConfirmRemove}
                    onCancel={() => setShowConfirmDialog(false)}
                    isVisible={showConfirmDialog}
                    title="清除提示"
                    subtitle="您是否确定清除？"
                />
            }
        </ScreenContainer>
    );
}

const createStyles = ({ colors, textVariants }: any) => StyleSheet.create({
    editTxt: {
        ...textVariants.body,
        color: colors.primary,
    },
    cancelEditTxt: {
        ...textVariants.body,
    },
    itemContainer: {
        flexDirection: 'row',
        width: '100%',
        paddingVertical: 14,
    },
    thumbnail: {
        width: 140,
        aspectRatio: 16 / 9,
        marginRight: 14,
        borderRadius: 10,
    },
    descriptionContainer: {
        flex: 1,
        paddingBottom: 4,
    },
    titleText: {
        ...textVariants.body,
        flex: 1,
    },
    metaDataContainer: {
        flexDirection: 'row',
        justifyContent: 'space-between',
        alignItems: 'flex-end',
    },
    timeText: {
        ...textVariants.subbody,
        color: colors.muted,
    },
    statusText: {
        ...textVariants.body,
        
        color: colors.confirm,
    },
    checkbox: {
        padding: 5,
        marginRight: 10,
        alignSelf: 'center',
    },
    deleteConfirmationModal: {
        display: "flex",
        flexDirection: "row",
        alignItems: "center",
        justifyContent: "space-between",
        padding: 8,
    },
    confirmationBtn: {
        flex: 1,
        margin: 10,
        borderRadius: 10,
        marginTop: 8,
    },
});
