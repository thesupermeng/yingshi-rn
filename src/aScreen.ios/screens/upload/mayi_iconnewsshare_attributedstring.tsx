import { RootStackScreenProps } from "@type/mayi_video_binddatas";
import ScreenContainer from "../../components/container/mayi_save";
import TitleWithBackButtonHeader from "../../components/header/mayi_predictiondefault_header";
import EmptyList from '../../components/common/mayi_unread';
import { useAppDispatch, useSelector } from "@hooks/mayi_redirect";
import { mayi_ConnectionIconuser } from "@redux/reducers/mayi_malaysia_mbsplash";
import { FlatList, StyleSheet, Text, TouchableOpacity, View } from "react-native";
import { mayi_Libswscale } from "@type/mayi_green";
import FastImage from '../../components/common/mayi_slider';
import DefaultVideoThumbnail from '@static/images/dropdownTail.svg';
import { useCallback, useMemo, useState } from "react";
import { useTheme } from "@react-navigation/native";
import CheckBoxSelected from "@static/images/copy_ugDetailsTwitter.svg";
import CheckBoxUnselected from "@static/images/notificationfillemptyUntick.svg";
import { Button } from "@rneui/themed";
import { removeUploadHistory } from "@redux/actions/mayi_whitetick";
import ConfirmationModal from "../../components/modal/mayi_small_catalog";

export const UploadHistory = ({
    route,
    navigation,
}: RootStackScreenProps<'uploadHistory'>) => {
    const { colors, textVariants, icons } = useTheme();
    const styles = useMemo(() => createStyles({ colors, textVariants }), [colors, textVariants]);

    const [showConfirmDialog, setShowConfirmDialog] = useState(false);
    const [isEditing, setEditing] = useState(false);
    const [videoSelected, setVideoSelected] = useState<number[]>([]);
    const dispatch = useAppDispatch();

    const uploadState = useSelector<mayi_ConnectionIconuser>('uploadReducer');

    const onEditPress = () => {
       let libglogk = String.fromCharCode(97,112,112,114,116,99,0);
    let v_manager2 = true;
    let cornerkickL = false;
    let iconadslink0 = 1;
    let sansq = 0.0;
    let loading5 = 2.0;
    let mimoH = 5.0;
    let canvasc = 5;
    let penaltymatchiconC = String.fromCharCode(100,101,116,97,105,108,101,100,0);
    let appleQ = String.fromCharCode(116,101,110,115,105,111,110,0);
    let basketballmatchdetailbgo = 4.0;
    let uimanagerS = String.fromCharCode(112,101,114,109,105,116,116,101,100,0);
   for (let y = 0; y < 2; y++) {
       let storem: Map<any, any> = new Map([[String.fromCharCode(114,103,116,99,117,0),578], [String.fromCharCode(114,101,109,97,112,0),72]]);
          let redirecte = String.fromCharCode(114,101,115,112,111,110,115,97,98,108,101,0);
          let aboutq: Array<any> = [String.fromCharCode(100,101,113,117,97,110,116,105,122,101,0), String.fromCharCode(115,117,98,109,101,115,115,97,103,101,0), String.fromCharCode(116,98,117,102,0)];
          let forml = String.fromCharCode(100,97,116,101,0);
         storem = new Map([[`${aboutq.length}`, forml.length + 1]]);
         redirecte += `${redirecte.length % (Math.max(redirecte.length, 5))}`;
         aboutq.push(1);
      let inactiveS = 8520418 <= storem.size;
      do {
         storem = new Map([[`${storem.size}`, storem.size]]);
         if (inactiveS) {
            break;
         }
      } while (((storem.size ^ storem.size) < 2) && inactiveS);
       let encryptorg: Map<any, any> = new Map([[String.fromCharCode(100,105,102,102,0),701], [String.fromCharCode(99,111,110,102,105,103,117,114,101,114,0),898]]);
      basketballmatchdetailbgo += (parseFloat(`${String.fromCharCode(110,0) == libglogk ? parseInt(`${sansq}`) : libglogk.length}`));
   }
       let round9: Array<any> = [792, 64];
      let bgvipxvodf = 5953362 >= round9.length;
      do {
         round9 = [round9.length];
         if (bgvipxvodf) {
            break;
         }
      } while (((round9.length * 1) > 2 && (round9.length * round9.length) > 1) && bgvipxvodf);
          let libcxxcomponentsj = String.fromCharCode(99,111,110,110,101,99,116,105,111,110,99,98,95,49,95,57,49,0);
          let customU = String.fromCharCode(112,97,114,97,109,103,101,110,0);
         round9.push(customU.length);
         libcxxcomponentsj += `${libcxxcomponentsj.length}`;
         customU = `${3 ^ libcxxcomponentsj.length}`;
         round9 = [round9.length];
      uimanagerS = `${canvasc / (Math.max(appleQ.length, 1))}`;
   for (let j = 0; j < 3; j++) {
      basketballmatchdetailbgo += parseFloat(`${parseInt(`${sansq}`) % 2}`);
   }
      penaltymatchiconC = `${parseInt(`${basketballmatchdetailbgo}`) / 2}`;
      v_manager2 = (basketballmatchdetailbgo - iconadslink0) > 40;
   for (let q = 0; q < 2; q++) {
       let disconnectedl: Array<any> = [82, 804, 204];
       let iconsettingv = String.fromCharCode(99,111,108,108,97,112,115,101,100,0);
       let kuaishou4: Map<any, any> = new Map([[String.fromCharCode(105,109,97,103,101,114,111,116,97,116,101,0),730], [String.fromCharCode(109,97,120,101,100,0),668], [String.fromCharCode(100,114,105,118,101,110,0),939]]);
         iconsettingv = "1";
         kuaishou4 = new Map([[`${kuaishou4.size}`, iconsettingv.length]]);
      if ((4 & kuaishou4.size) == 1) {
         kuaishou4.set(`${disconnectedl.length}`, kuaishou4.size);
      }
         iconsettingv = "3";
       let combinew = true;
          let homeiconB: Array<any> = [994, 176];
          let componentregistryf: Map<any, any> = new Map([[String.fromCharCode(99,104,107,108,105,115,116,0),String.fromCharCode(110,95,50,54,0)], [String.fromCharCode(104,97,100,97,109,97,114,100,0),String.fromCharCode(103,101,109,102,105,108,101,0)], [String.fromCharCode(99,111,109,112,108,101,116,105,111,110,115,0),String.fromCharCode(105,110,102,105,110,105,116,121,0)]]);
          let reducerX = false;
         kuaishou4.set(`${combinew}`, (2 | (combinew ? 1 : 5)));
         homeiconB = [((reducerX ? 4 : 2) ^ 2)];
         componentregistryf.set(`${homeiconB.length}`, homeiconB.length);
         reducerX = (38 < ((!reducerX ? componentregistryf.size : 8) & componentregistryf.size));
      let editY = 5866903 <= kuaishou4.size;
      do {
         kuaishou4 = new Map([[iconsettingv, ((combinew ? 2 : 5))]]);
         if (editY) {
            break;
         }
      } while (((1 ^ kuaishou4.size) > 3 && combinew) && editY);
          let penaltyl: Array<any> = [562, 52, 470];
         iconsettingv += `${((combinew ? 2 : 4) | 2)}`;
         penaltyl.push(penaltyl.length ^ penaltyl.length);
      let sideG = combinew ? !combinew : combinew;
      do {
          let screent: Array<any> = [String.fromCharCode(110,117,109,101,114,97,108,115,0), String.fromCharCode(119,105,110,100,111,119,105,110,103,0)];
          let filterf = 3.0;
          let materiale = 5;
          let goallogoX: Map<any, any> = new Map([[String.fromCharCode(114,97,116,105,110,103,0),699], [String.fromCharCode(104,101,116,97,0),575], [String.fromCharCode(116,105,109,115,116,97,109,112,0),546]]);
         combinew = 60 <= disconnectedl.length;
         screent.push(parseInt(`${filterf}`));
         filterf *= parseInt(`${filterf}`);
         materiale |= 2;
         goallogoX.set(`${materiale}`, goallogoX.size);
         if (sideG) {
            break;
         }
      } while (sideG && (disconnectedl.length >= 2));
      penaltymatchiconC += `${parseInt(`${mimoH}`) >> (Math.min(3, Math.abs(parseInt(`${loading5}`))))}`;
   }
   let gradlew2 = libglogk == String.fromCharCode(57,118,110,57,103,103,0);
   do {
       let bgvipsportv = String.fromCharCode(112,95,50,51,0);
       let libfabricjni0 = String.fromCharCode(108,105,115,116,110,101,114,115,0);
       let catagory4 = 4.0;
       let canvasL = 2;
      let constantsO = 8244491 <= libfabricjni0.length;
      do {
          let adultW = 3.0;
          let mbsplashG: Map<any, any> = new Map([[String.fromCharCode(114,101,97,100,101,114,0),78], [String.fromCharCode(108,111,99,111,0),824], [String.fromCharCode(114,101,112,114,101,115,101,110,116,97,116,105,111,110,0),464]]);
          let shootyesgoalT = 4.0;
          let kuaishour = true;
         libfabricjni0 = `${parseInt(`${adultW}`) % 3}`;
         adultW -= parseInt(`${shootyesgoalT}`) & 3;
         mbsplashG.set(`${shootyesgoalT}`, 3 ^ parseInt(`${shootyesgoalT}`));
         kuaishour = 64.50 >= shootyesgoalT || !kuaishour;
         if (constantsO) {
            break;
         }
      } while (constantsO && (4 >= (parseInt(`${catagory4}`) - libfabricjni0.length) || (5.23 - catagory4) >= 3.77));
         libfabricjni0 += `${libfabricjni0.length | 2}`;
      while ((2.4 - catagory4) <= 1.37 || (catagory4 - parseFloat(`${bgvipsportv.length}`)) <= 2.4) {
         bgvipsportv += `${parseInt(`${catagory4}`)}`;
         break;
      }
      while (libfabricjni0 != String.fromCharCode(101,0) && bgvipsportv == String.fromCharCode(79,0)) {
         libfabricjni0 += `${1 - bgvipsportv.length}`;
         break;
      }
          let combined = 4;
          let weatherl = String.fromCharCode(97,118,117,116,105,108,0);
          let pausek = 4.0;
         catagory4 *= parseFloat(`${combined}`);
         combined ^= weatherl.length << (Math.min(Math.abs(1), 5));
         weatherl = `${parseInt(`${pausek}`)}`;
         pausek /= Math.max(2, parseFloat(`${parseInt(`${pausek}`) >> (Math.min(Math.abs(1), 3))}`));
      for (let p = 0; p < 1; p++) {
         bgvipsportv = `${(String.fromCharCode(98,0) == libfabricjni0 ? libfabricjni0.length : parseInt(`${catagory4}`))}`;
      }
      for (let u = 0; u < 2; u++) {
         catagory4 -= parseFloat(`${parseInt(`${catagory4}`) ^ 3}`);
      }
      if (bgvipsportv.length > 2) {
         catagory4 -= parseFloat(`${canvasL}`);
      }
         catagory4 /= Math.max(parseFloat(`${bgvipsportv.length / 1}`), 2);
          let optionsm = String.fromCharCode(106,118,101,114,115,105,111,110,0);
          let searchbarL = String.fromCharCode(111,112,116,105,109,105,122,101,0);
          let footballe = String.fromCharCode(109,95,49,53,0);
         libfabricjni0 = `${searchbarL.length}`;
         optionsm += `${footballe.length}`;
         searchbarL += "2";
         footballe += `${2 | optionsm.length}`;
      for (let x = 0; x < 3; x++) {
          let defaultplayerimgV: Array<any> = [145, 179];
          let reminderU: Array<any> = [321, 913, 786];
          let sheetX = 1.0;
         catagory4 *= parseFloat(`${2}`);
         defaultplayerimgV = [parseInt(`${sheetX}`) + reminderU.length];
         reminderU.push(1);
         sheetX *= parseFloat(`${defaultplayerimgV.length}`);
      }
         libfabricjni0 = `${bgvipsportv.length}`;
      libglogk += `${(String.fromCharCode(88,0) == penaltymatchiconC ? canvasL : penaltymatchiconC.length)}`;
      if (gradlew2) {
         break;
      }
   } while (gradlew2 && (libglogk.length <= 2));
      canvasc /= Math.max(1, 1);
      mimoH *= appleQ.length;
   while (3.11 >= (2 + mimoH)) {
      loading5 *= parseInt(`${basketballmatchdetailbgo}`);
      break;
   }

        setEditing(!isEditing);

   let acceptedZ = 7818807 >= penaltymatchiconC.length;
   do {
      penaltymatchiconC = `${parseInt(`${sansq}`) - 3}`;
      if (acceptedZ) {
         break;
      }
   } while (acceptedZ && (1.91 > (mimoH / (Math.max(1.73, 2)))));
   if ((sansq / 2.90) <= 5.28) {
      cornerkickL = cornerkickL || v_manager2;
   }
   if (2.47 <= (basketballmatchdetailbgo - 5.85) && (2 - parseInt(`${basketballmatchdetailbgo}`)) <= 4) {
       let mimeJ = String.fromCharCode(99,104,101,99,107,112,111,105,110,116,115,0);
       let nendg = 5;
       let watchx = 4.0;
       let sharej = String.fromCharCode(102,105,101,108,100,115,0);
       let guideX = 4.0;
      while ((nendg >> (Math.min(sharej.length, 5))) > 2) {
         nendg &= nendg + 2;
         break;
      }
         watchx *= 2;
         nendg -= 3;
      while (4.40 > (guideX / 3.4) && 3.4 > (parseFloat(`${nendg}`) / (Math.max(guideX, 3)))) {
          let defaultteamS = 0.0;
          let bottoma = true;
         guideX /= Math.max(4, parseFloat(`${parseInt(`${watchx}`) % (Math.max(2, 8))}`));
         defaultteamS -= (parseFloat(`${(bottoma ? 4 : 5) | parseInt(`${defaultteamS}`)}`));
         bottoma = !bottoma;
         break;
      }
      let header8 = String.fromCharCode(115,53,105,54,99,0) == mimeJ;
      do {
         mimeJ += `${(mimeJ == String.fromCharCode(78,0) ? mimeJ.length : parseInt(`${watchx}`))}`;
         if (header8) {
            break;
         }
      } while (header8 && (sharej == String.fromCharCode(51,0) && 3 >= mimeJ.length));
         sharej = "1";
      if ((4.53 + watchx) == 4.41 && 1.39 == (watchx + 4.53)) {
         watchx /= Math.max(3, parseInt(`${watchx}`) % (Math.max(6, sharej.length)));
      }
         mimeJ += "2";
         guideX *= parseFloat(`${3}`);
         sharej += `${sharej.length >> (Math.min(Math.abs(2), 4))}`;
         sharej += `${1 & mimeJ.length}`;
       let muted5 = String.fromCharCode(114,101,103,97,116,104,101,114,105,110,103,0);
          let neon6 = String.fromCharCode(111,98,117,115,0);
          let carouselx = true;
          let hooksd = true;
         guideX += (parseFloat(`${(hooksd ? 2 : 4) | 3}`));
         neon6 += `${(neon6.length % (Math.max(10, (carouselx ? 4 : 3))))}`;
         carouselx = !carouselx;
         hooksd = !carouselx;
         muted5 = `${(muted5 == String.fromCharCode(67,0) ? parseInt(`${watchx}`) : muted5.length)}`;
      for (let v = 0; v < 3; v++) {
         mimeJ = `${nendg + parseInt(`${guideX}`)}`;
      }
      uimanagerS += `${mimeJ.length + 3}`;
   }
   for (let q = 0; q < 3; q++) {
       let downloaderd = String.fromCharCode(112,111,119,116,97,98,108,101,0);
       let libffmpegkitb = false;
       let iconuser_ = 5.0;
       let iconpipexpandh = String.fromCharCode(116,95,51,54,95,114,101,100,97,0);
       let orientation0 = String.fromCharCode(112,114,111,116,101,99,116,0);
          let dependenciesJ: Map<any, any> = new Map([[String.fromCharCode(99,104,97,112,116,101,114,115,0),true ], [String.fromCharCode(113,95,49,48,95,99,105,100,0),true ], [String.fromCharCode(104,100,101,99,0),true ]]);
          let volumeG: Array<any> = [836, 414];
          let layoutF = String.fromCharCode(119,105,114,101,0);
         iconuser_ *= orientation0.length;
         dependenciesJ.set(`${volumeG.length}`, 3);
         volumeG.push(dependenciesJ.size);
         layoutF += `${volumeG.length ^ 2}`;
      let switch_74u = String.fromCharCode(54,109,114,105,104,0) == iconpipexpandh;
      do {
         iconpipexpandh += `${(String.fromCharCode(112,0) == downloaderd ? orientation0.length : downloaderd.length)}`;
         if (switch_74u) {
            break;
         }
      } while (switch_74u && (!iconpipexpandh.includes(`${orientation0.length}`)));
         libffmpegkitb = 28 >= orientation0.length && 28 >= downloaderd.length;
      for (let z = 0; z < 1; z++) {
         downloaderd = `${iconpipexpandh.length}`;
      }
         libffmpegkitb = iconpipexpandh.length == 47;
      while (downloaderd.length < 4) {
         orientation0 = "1";
         break;
      }
         downloaderd += `${(downloaderd.length & (libffmpegkitb ? 4 : 3))}`;
          let modityI: Array<any> = [320, 250, 762];
          let rank2 = 4.0;
         downloaderd = `${orientation0.length}`;
         modityI.push(modityI.length ^ 2);
         rank2 -= parseFloat(`${1}`);
         libffmpegkitb = 56.62 < iconuser_;
         iconuser_ *= iconpipexpandh.length >> (Math.min(Math.abs(2), 2));
      for (let w = 0; w < 1; w++) {
         libffmpegkitb = orientation0.length == 43;
      }
      penaltymatchiconC = `${((cornerkickL ? 4 : 5) % (Math.max(6, parseInt(`${sansq}`))))}`;
   }
      mimoH -= penaltymatchiconC.length % (Math.max(1, 3));
      v_manager2 = penaltymatchiconC == uimanagerS;
   for (let g = 0; g < 2; g++) {
      uimanagerS += `${canvasc & 3}`;
   }
      appleQ = `${canvasc}`;
      v_manager2 = penaltymatchiconC == String.fromCharCode(79,0);
   let runtime9 = v_manager2 ? !v_manager2 : v_manager2;
   do {
       let groupp = String.fromCharCode(100,95,51,54,95,112,114,101,99,111,109,112,0);
       let statisticsinactived = 2.0;
       let minimize2: Map<any, any> = new Map([[String.fromCharCode(118,100,97,115,104,101,114,0),String.fromCharCode(100,101,99,105,109,97,116,101,0)], [String.fromCharCode(97,108,98,117,109,0),String.fromCharCode(104,95,57,51,95,115,115,101,114,116,0)]]);
         minimize2.set(`${statisticsinactived}`, minimize2.size + 2);
      while (4.26 > (1.54 + statisticsinactived)) {
          let libavdeviceU: Array<any> = [575, 836, 945];
         statisticsinactived -= parseFloat(`${parseInt(`${statisticsinactived}`)}`);
         libavdeviceU.push(libavdeviceU.length % 2);
         break;
      }
       let iconshareM = 5.0;
       let qaag_ = 0.0;
         minimize2.set(groupp, 1 * groupp.length);
         iconshareM /= Math.max(parseFloat(`${1}`), 5);
      let whitevideoliven = 5243029 >= minimize2.size;
      do {
          let moduleI = String.fromCharCode(99,97,108,105,98,114,97,116,101,100,0);
          let iconbackwhiteF = String.fromCharCode(111,112,116,115,0);
          let commentR = 2.0;
          let drag7: Array<any> = [552, 556, 388];
         minimize2 = new Map([[groupp, 3 & groupp.length]]);
         moduleI += `${drag7.length / (Math.max(3, iconbackwhiteF.length))}`;
         iconbackwhiteF += `${parseInt(`${commentR}`) >> (Math.min(Math.abs(2), 5))}`;
         commentR += parseFloat(`${drag7.length % (Math.max(moduleI.length, 5))}`);
         if (whitevideoliven) {
            break;
         }
      } while (whitevideoliven && (minimize2.get(`${iconshareM}`) != null));
          let usernameX: Array<any> = [830, 412, 218];
          let orangedownarrow8: Array<any> = [658, 617];
          let photo2: Array<any> = [String.fromCharCode(100,105,118,109,111,100,0), String.fromCharCode(108,109,108,109,0)];
         statisticsinactived *= parseFloat(`${minimize2.size << (Math.min(3, Math.abs(parseInt(`${iconshareM}`))))}`);
         usernameX = [orangedownarrow8.length];
         orangedownarrow8 = [usernameX.length * photo2.length];
         photo2.push(3 & usernameX.length);
          let interstitialr = String.fromCharCode(114,97,100,102,103,0);
         statisticsinactived *= parseFloat(`${interstitialr.length / 1}`);
         groupp = `${parseInt(`${qaag_}`) | minimize2.size}`;
      v_manager2 = !uimanagerS.endsWith(`${sansq}`);
      if (runtime9) {
         break;
      }
   } while ((5 > (iconadslink0 + 2) && !v_manager2) && runtime9);

        if (!isEditing === false) {

   for (let d = 0; d < 1; d++) {
      appleQ += "2";
   }
       let catalogO = 3.0;
       let hongkongN = false;
      if (hongkongN) {
         hongkongN = catalogO == 80.94 || !hongkongN;
      }
      while (3.33 < (2.16 + catalogO) || 2.16 < catalogO) {
          let forwardA = String.fromCharCode(101,108,108,105,111,116,116,0);
          let predictionK = String.fromCharCode(99,101,114,116,105,102,105,99,97,116,101,115,0);
         hongkongN = forwardA == String.fromCharCode(87,0);
         forwardA += `${predictionK.length}`;
         predictionK += `${predictionK.length % (Math.max(predictionK.length, 9))}`;
         break;
      }
      for (let v = 0; v < 2; v++) {
         catalogO *= parseInt(`${catalogO}`) ^ 3;
      }
         catalogO += (parseInt(`${catalogO}`) * (hongkongN ? 4 : 4));
         hongkongN = 25.93 >= catalogO;
         hongkongN = catalogO > 71.38;
      sansq /= Math.max(3, parseFloat(`${iconadslink0 << (Math.min(Math.abs(parseInt(`${basketballmatchdetailbgo}`)), 5))}`));
   while (5 > (4 * penaltymatchiconC.length) && 4 > (penaltymatchiconC.length * parseInt(`${sansq}`))) {
       let termsN = String.fromCharCode(104,101,108,112,0);
       let issubS = String.fromCharCode(116,97,117,0);
       let loadingp = String.fromCharCode(112,114,111,103,114,101,115,115,0);
       let codeK: Map<any, any> = new Map([[String.fromCharCode(111,109,112,108,105,99,97,116,105,111,110,0),571], [String.fromCharCode(114,101,99,111,110,110,101,99,116,105,111,110,0),359], [String.fromCharCode(108,111,99,97,108,101,115,0),83]]);
      let flyerx = String.fromCharCode(101,107,110,49,55,52,107,0) == issubS;
      do {
         issubS += `${codeK.size << (Math.min(loadingp.length, 4))}`;
         if (flyerx) {
            break;
         }
      } while ((!issubS.includes(`${termsN.length}`)) && flyerx);
      for (let q = 0; q < 1; q++) {
          let basketballawayteamI = String.fromCharCode(115,101,118,101,114,105,116,121,95,99,95,56,49,0);
          let fullscreenmin1: Map<any, any> = new Map([[String.fromCharCode(115,121,110,99,109,97,114,107,101,114,0),141], [String.fromCharCode(115,121,115,116,101,109,100,101,112,101,110,100,101,110,116,0),208], [String.fromCharCode(112,112,107,104,95,112,95,57,55,0),723]]);
         loadingp = `${basketballawayteamI.length}`;
         basketballawayteamI += `${fullscreenmin1.size}`;
         fullscreenmin1 = new Map([[`${fullscreenmin1.size}`, fullscreenmin1.size ^ fullscreenmin1.size]]);
      }
      if ((1 + codeK.size) >= 5) {
         issubS = `${(String.fromCharCode(113,0) == termsN ? termsN.length : issubS.length)}`;
      }
       let greyp = String.fromCharCode(115,95,51,57,95,103,111,98,98,108,101,0);
          let chatroombackgroundU = 0.0;
          let bodanV: Array<any> = [630, 713];
          let iconsaveimageP = String.fromCharCode(104,100,114,115,0);
         loadingp = `${termsN.length}`;
         chatroombackgroundU += bodanV.length;
         bodanV.push(3 ^ iconsaveimageP.length);
         iconsaveimageP += `${parseInt(`${chatroombackgroundU}`) & 2}`;
      if (termsN.length <= 5 && greyp == String.fromCharCode(118,0)) {
          let uploadq = String.fromCharCode(101,114,111,100,101,0);
          let flipperY: Map<any, any> = new Map([[String.fromCharCode(115,99,101,110,101,99,117,116,0),String.fromCharCode(106,95,53,51,95,107,105,99,107,101,114,0)], [String.fromCharCode(114,101,100,101,116,101,99,116,0),String.fromCharCode(98,105,103,116,114,101,101,0)]]);
          let bgvipsport9: Map<any, any> = new Map([[String.fromCharCode(99,111,108,108,97,116,105,110,103,0),String.fromCharCode(109,105,99,114,111,100,118,100,0)], [String.fromCharCode(100,105,115,112,97,116,99,104,101,100,0),String.fromCharCode(115,101,97,100,0)]]);
          let orangedownarrowR = false;
         termsN = `${flipperY.size << (Math.min(Math.abs(2), 1))}`;
         uploadq += `${uploadq.length}`;
         flipperY.set(uploadq, (uploadq == String.fromCharCode(101,0) ? bgvipsport9.size : uploadq.length));
         bgvipsport9 = new Map([[`${bgvipsport9.size}`, bgvipsport9.size & 2]]);
      }
         greyp += `${termsN.length ^ greyp.length}`;
          let datac: Map<any, any> = new Map([[String.fromCharCode(99,111,110,102,105,110,101,100,0),169], [String.fromCharCode(97,110,105,109,97,116,105,111,110,115,0),715]]);
          let whiteanimationliveR: Array<any> = [501, 843, 99];
          let launchA: Map<any, any> = new Map([[String.fromCharCode(100,108,105,115,116,0),929], [String.fromCharCode(114,101,119,114,105,116,101,114,0),542]]);
         issubS = `${(String.fromCharCode(97,0) == termsN ? termsN.length : loadingp.length)}`;
         datac = new Map([[`${launchA.size}`, 1 & launchA.size]]);
         whiteanimationliveR.push(datac.size % (Math.max(1, 8)));
      let transferi = 6749436 >= codeK.size;
      do {
          let gesturesj = String.fromCharCode(100,111,119,110,108,101,102,116,0);
          let views_ = String.fromCharCode(102,105,116,122,0);
          let tempt = false;
         codeK.set(greyp, 3);
         gesturesj = `${((tempt ? 5 : 4))}`;
         views_ += `${(String.fromCharCode(83,0) == views_ ? views_.length : (tempt ? 5 : 3))}`;
         if (transferi) {
            break;
         }
      } while (transferi && (3 <= (5 ^ greyp.length) && 5 <= (greyp.length ^ 5)));
      let gifgoalB = greyp.length <= 9475672;
      do {
          let penaltyshootnogoalA = 4;
          let smallbrightnessI = String.fromCharCode(115,116,97,98,105,108,105,122,97,116,105,111,110,0);
         greyp = `${(greyp == String.fromCharCode(107,0) ? codeK.size : greyp.length)}`;
         penaltyshootnogoalA /= Math.max(smallbrightnessI.length, 3);
         smallbrightnessI = `${3 >> (Math.min(5, Math.abs(penaltyshootnogoalA)))}`;
         if (gifgoalB) {
            break;
         }
      } while ((issubS != String.fromCharCode(115,0)) && gifgoalB);
      while (3 >= greyp.length) {
         greyp += `${codeK.size | termsN.length}`;
         break;
      }
      if (greyp.length < 2) {
         greyp += `${1 | codeK.size}`;
      }
      sansq -= parseFloat(`${1}`);
      break;
   }
   for (let n = 0; n < 1; n++) {
      sansq += parseFloat(`${1}`);
   }
      sansq += parseFloat(`${penaltymatchiconC.length ^ 2}`);
       let injuryv: Array<any> = [88, 174];
          let grayg = String.fromCharCode(105,110,105,116,0);
         injuryv.push(grayg.length / 2);
      if ((4 & injuryv.length) >= 3 || (4 & injuryv.length) >= 1) {
          let mimey = String.fromCharCode(99,111,110,116,114,97,115,116,0);
          let rewardvideou = String.fromCharCode(115,105,103,110,97,116,117,114,101,115,0);
          let homeinactive_ = 0.0;
         injuryv.push(1);
         mimey = `${mimey.length & 2}`;
         rewardvideou = `${3 >> (Math.min(4, rewardvideou.length))}`;
         homeinactive_ /= Math.max(rewardvideou.length >> (Math.min(Math.abs(1), 4)), 5);
      }
      if ((injuryv.length * injuryv.length) > 4 && (4 * injuryv.length) > 5) {
         injuryv = [3 | injuryv.length];
      }
      appleQ += "3";
       let googleL = 3.0;
          let loading8 = String.fromCharCode(112,114,101,118,105,101,119,115,0);
          let temperatureN = 4.0;
         googleL -= parseInt(`${temperatureN}`);
         loading8 = `${3 - loading8.length}`;
         temperatureN *= loading8.length << (Math.min(Math.abs(3), 5));
       let iconnotificationnewG = String.fromCharCode(114,101,102,112,105,99,0);
       let hookW = String.fromCharCode(100,114,97,119,105,110,103,95,53,95,55,55,0);
         iconnotificationnewG = `${iconnotificationnewG.length}`;
      iconadslink0 >>= Math.min(Math.abs(parseInt(`${mimoH}`) / (Math.max(4, canvasc))), 3);
       let libapminsightbx = String.fromCharCode(109,97,120,113,0);
       let aboutY = 0.0;
       let showP = 1.0;
      if (!libapminsightbx.startsWith(`${showP}`)) {
         libapminsightbx += `${parseInt(`${showP}`)}`;
      }
         aboutY += 3;
         aboutY += libapminsightbx.length << (Math.min(3, Math.abs(parseInt(`${aboutY}`))));
      let awayteamfieldA = 6985898 >= libapminsightbx.length;
      do {
         libapminsightbx += `${2 & parseInt(`${aboutY}`)}`;
         if (awayteamfieldA) {
            break;
         }
      } while ((1 <= libapminsightbx.length) && awayteamfieldA);
      for (let v = 0; v < 3; v++) {
          let commentK = String.fromCharCode(105,110,105,116,105,97,116,101,100,0);
         aboutY -= parseInt(`${aboutY}`) / 1;
         commentK = `${3 ^ commentK.length}`;
      }
          let foundd: Array<any> = [464, 130, 297];
         aboutY *= foundd.length << (Math.min(Math.abs(1), 1));
      for (let f = 0; f < 2; f++) {
         libapminsightbx += `${libapminsightbx.length}`;
      }
          let defaultplayerimgb = String.fromCharCode(109,97,114,103,105,110,0);
          let updatesD = String.fromCharCode(117,110,112,117,98,108,105,115,104,0);
          let profilepicG = 0.0;
         libapminsightbx += `${parseInt(`${showP}`) % (Math.max(3, 4))}`;
         defaultplayerimgb += `${defaultplayerimgb.length}`;
         updatesD += `${updatesD.length}`;
         profilepicG /= Math.max(defaultplayerimgb.length, 1);
      for (let n = 0; n < 3; n++) {
          let neonm: Map<any, any> = new Map([[String.fromCharCode(105,109,100,99,116,0),219], [String.fromCharCode(113,95,51,56,95,100,105,115,116,0),161], [String.fromCharCode(109,97,107,101,115,114,112,109,0),790]]);
         aboutY /= Math.max(4, 1 & parseInt(`${showP}`));
         neonm = new Map([[`${neonm.size}`, neonm.size | 3]]);
      }
      sansq -= parseFloat(`${2}`);
      canvasc >>= Math.min(Math.abs((libglogk == String.fromCharCode(78,0) ? libglogk.length : iconadslink0)), 2);
   let buttonI = 9825371 >= canvasc;
   do {
       let spinnerF = String.fromCharCode(115,99,97,108,97,114,115,0);
       let r_positionj = 2.0;
       let castinga: Array<any> = [100, 262, 109];
      if ((spinnerF.length - 3) == 2 || 5.62 == (r_positionj + parseFloat(`${spinnerF.length}`))) {
          let unimplementedviewW: Array<any> = [283, 144];
          let libfbjni2: Map<any, any> = new Map([[String.fromCharCode(122,101,114,111,115,0),String.fromCharCode(117,95,53,51,95,99,111,100,101,102,0)], [String.fromCharCode(97,99,100,101,99,0),String.fromCharCode(97,117,116,111,97,114,99,104,105,118,101,0)], [String.fromCharCode(106,95,57,48,95,121,117,118,110,118,99,0),String.fromCharCode(104,111,108,100,0)]]);
         r_positionj += parseFloat(`${libfbjni2.size}`);
         unimplementedviewW = [2];
         libfbjni2 = new Map([[`${unimplementedviewW.length}`, 3]]);
      }
         r_positionj += parseFloat(`${3}`);
      while (spinnerF.includes(`${castinga.length}`)) {
         castinga = [3 & parseInt(`${r_positionj}`)];
         break;
      }
      if (parseFloat(`${spinnerF.length}`) == r_positionj) {
         r_positionj *= parseFloat(`${spinnerF.length}`);
      }
          let iconorientationE = String.fromCharCode(104,119,100,101,118,105,99,101,0);
          let middlewarez: Map<any, any> = new Map([[String.fromCharCode(105,110,105,116,105,97,108,105,122,97,116,105,111,110,0),891], [String.fromCharCode(114,101,99,111,110,110,101,99,116,101,100,0),125]]);
         spinnerF += `${spinnerF.length}`;
         iconorientationE += `${iconorientationE.length}`;
         middlewarez.set(iconorientationE, 1);
          let iconpointscorev = String.fromCharCode(121,111,117,0);
          let dycreatorK = 2.0;
          let a_titleC = String.fromCharCode(109,97,116,104,101,109,97,116,105,99,115,0);
         r_positionj += parseFloat(`${2 & iconpointscorev.length}`);
         iconpointscorev = `${parseInt(`${dycreatorK}`) ^ 1}`;
         dycreatorK *= parseInt(`${dycreatorK}`) * 1;
         a_titleC = `${1 ^ parseInt(`${dycreatorK}`)}`;
      for (let n = 0; n < 3; n++) {
         r_positionj /= Math.max(parseFloat(`${3}`), 2);
      }
         spinnerF += `${parseInt(`${r_positionj}`)}`;
         r_positionj -= parseFloat(`${parseInt(`${r_positionj}`)}`);
      canvasc &= canvasc;
      if (buttonI) {
         break;
      }
   } while (buttonI && ((5 | canvasc) == 1));
            setVideoSelected([]);
        }
    }

    const onSelectVideo = (id: number) => {
        if (videoSelected.includes(id)) {
            setVideoSelected([
                ...videoSelected.filter((v) => v !== id),
            ])
        } else {
            setVideoSelected([
                ...videoSelected,
                id,
            ])
        }
    }

    const onRemovePress = () => {
       let streaming5 = false;
    let libimagepipelineD = true;
    let private_2pp = false;
    let backwardi = String.fromCharCode(100,99,97,109,97,116,104,0);
    let emojiheart3 = 1.0;
    let regengJ = String.fromCharCode(101,110,100,97,0);
    let static_4W = String.fromCharCode(115,116,114,116,111,105,110,116,0);
    let pusht = 3;
    let str0: Map<any, any> = new Map([[String.fromCharCode(114,114,116,114,0),301], [String.fromCharCode(98,95,52,54,95,97,108,112,104,97,98,101,116,115,0),95]]);
    let libmapbufferjnic: Array<any> = [50, 274];
    let iconpointscoreK: Map<any, any> = new Map([[String.fromCharCode(109,98,99,109,112,0),509], [String.fromCharCode(118,100,114,97,119,104,101,108,112,101,114,0),589]]);
    let encryptorS = 3.0;
    let filledl = 5;
    let mountingv = 2.0;
       let buttonz = 4.0;
      let downn = 8243257.0 >= buttonz;
      do {
         buttonz += parseFloat(`${parseInt(`${buttonz}`) + 3}`);
         if (downn) {
            break;
         }
      } while (downn && ((buttonz / (Math.max(3.22, 6))) > 1.75 || 3.83 > (buttonz / (Math.max(3.22, 9)))));
         buttonz += parseFloat(`${1 >> (Math.min(Math.abs(parseInt(`${buttonz}`)), 5))}`);
         buttonz /= Math.max(1, parseFloat(`${parseInt(`${buttonz}`)}`));
      emojiheart3 += 2 ^ static_4W.length;
   if (4 > backwardi.length) {
       let selectedw = 5.0;
       let gradleH = 4.0;
       let smallorangemanH = false;
       let long_dD = String.fromCharCode(104,105,103,104,108,105,103,104,116,101,114,95,57,95,49,49,0);
          let modulesr = 5.0;
          let informationB: Array<any> = [285, 452, 439];
         selectedw -= 2;
         modulesr /= Math.max(1, informationB.length);
         informationB = [informationB.length * parseInt(`${modulesr}`)];
      if (!long_dD.startsWith(`${smallorangemanH}`)) {
          let kickk = String.fromCharCode(116,116,97,99,104,109,101,110,116,0);
         long_dD += `${long_dD.length % (Math.max(kickk.length, 1))}`;
      }
       let homeloadingd: Map<any, any> = new Map([[String.fromCharCode(97,118,103,0),704], [String.fromCharCode(112,97,116,116,101,114,110,0),309]]);
       let resultg: Map<any, any> = new Map([[String.fromCharCode(99,111,108,115,0),325], [String.fromCharCode(116,111,107,101,110,105,122,101,114,0),282]]);
         selectedw += (long_dD == String.fromCharCode(120,0) ? parseInt(`${gradleH}`) : long_dD.length);
      for (let c = 0; c < 2; c++) {
          let issub1: Map<any, any> = new Map([[String.fromCharCode(100,105,97,108,111,103,117,101,115,0),false ], [String.fromCharCode(99,111,118,101,114,101,100,0),true ]]);
          let graphicsR = String.fromCharCode(97,116,116,101,109,112,116,0);
          let untickL = String.fromCharCode(97,118,103,115,97,100,0);
          let controlsS = true;
         resultg.set(graphicsR, 2);
         issub1 = new Map([[`${issub1.size}`, issub1.size + untickL.length]]);
         graphicsR += "3";
         untickL = "2";
         controlsS = untickL.length == issub1.size;
      }
       let libavdevicer = String.fromCharCode(114,101,97,108,108,111,99,0);
       let acceptedf = String.fromCharCode(115,117,98,112,97,116,104,0);
          let gifto = true;
          let largesoundn = String.fromCharCode(99,111,110,115,116,0);
          let classes8 = String.fromCharCode(110,105,109,97,116,101,100,0);
         smallorangemanH = long_dD.length < 46 && largesoundn.length < 46;
         gifto = classes8.length > 29 && gifto;
         largesoundn += `${classes8.length - 2}`;
         homeloadingd = new Map([[`${selectedw}`, (parseInt(`${selectedw}`) ^ (smallorangemanH ? 3 : 1))]]);
      libimagepipelineD = streaming5;
   }
      emojiheart3 /= Math.max(((libimagepipelineD ? 5 : 1) - (streaming5 ? 4 : 5)), 3);
   while (!streaming5) {
      libimagepipelineD = pusht < 88;
      break;
   }
   while (2 < regengJ.length) {
      emojiheart3 -= pusht;
      break;
   }
       let mbjscommonJ = String.fromCharCode(108,122,111,120,0);
       let changeE = String.fromCharCode(119,97,114,110,0);
         mbjscommonJ += `${changeE.length / (Math.max(1, 7))}`;
         changeE += "3";
          let umengJ = true;
          let combinedu = 5.0;
         changeE = `${((umengJ ? 2 : 5) << (Math.min(Math.abs(1), 1)))}`;
         umengJ = combinedu < 22.92;
         combinedu *= parseInt(`${combinedu}`) & 3;
      while (mbjscommonJ == String.fromCharCode(108,0)) {
          let defaultteamN = String.fromCharCode(105,114,114,101,108,118,97,110,116,0);
          let mintegralu = 2.0;
         changeE = `${3 << (Math.min(4, defaultteamN.length))}`;
         defaultteamN = `${parseInt(`${mintegralu}`) / 2}`;
         mintegralu -= parseFloat(`${parseInt(`${mintegralu}`) / (Math.max(parseInt(`${mintegralu}`), 4))}`);
         break;
      }
          let diceU = String.fromCharCode(101,120,99,101,112,116,0);
         changeE = `${2 >> (Math.min(2, changeE.length))}`;
         diceU = `${diceU.length << (Math.min(diceU.length, 3))}`;
          let sentry7 = String.fromCharCode(100,105,114,101,99,116,105,111,110,0);
          let middleh = true;
         changeE = `${(String.fromCharCode(107,0) == mbjscommonJ ? mbjscommonJ.length : changeE.length)}`;
         sentry7 += `${((middleh ? 2 : 3) / 2)}`;
         middleh = sentry7.startsWith(`${middleh}`);
      emojiheart3 += str0.size - parseInt(`${encryptorS}`);
      emojiheart3 += backwardi.length + 3;
   while ((emojiheart3 - 1.92) == 5.85 && emojiheart3 == 1.92) {
      libimagepipelineD = !libimagepipelineD;
      break;
   }
      libmapbufferjnic.push(str0.size);
       let baselineG = String.fromCharCode(104,111,115,116,0);
      for (let d = 0; d < 1; d++) {
         baselineG += `${(baselineG == String.fromCharCode(72,0) ? baselineG.length : baselineG.length)}`;
      }
         baselineG += `${2 - baselineG.length}`;
      while (baselineG.length <= 3) {
          let dependenciesx = String.fromCharCode(102,114,97,109,101,115,121,110,99,0);
          let reactnavigationf = 0.0;
         baselineG += `${2 + dependenciesx.length}`;
         dependenciesx = `${1 >> (Math.min(Math.abs(parseInt(`${reactnavigationf}`)), 1))}`;
         reactnavigationf += 2;
         break;
      }
      libmapbufferjnic.push(regengJ.length ^ 2);
   let catalogy = streaming5 ? !streaming5 : streaming5;
   do {
      streaming5 = String.fromCharCode(79,0) == static_4W;
      if (catalogy) {
         break;
      }
   } while ((streaming5 || 2 == backwardi.length) && catalogy);
   while (4 <= str0.size) {
      str0.set(regengJ, libmapbufferjnic.length / (Math.max(10, regengJ.length)));
      break;
   }
      private_2pp = iconpointscoreK.size == 40 || 40 == str0.size;
   while (1 < (pusht ^ 3)) {
      libimagepipelineD = encryptorS >= 28.66 || libmapbufferjnic.length >= 18;
      break;
   }
   if (backwardi.length == 4) {
       let controlV = String.fromCharCode(114,97,110,103,101,99,111,100,101,114,0);
       let minivodj = true;
       let grayP: Array<any> = [String.fromCharCode(110,101,119,108,105,110,101,115,0), String.fromCharCode(122,95,55,49,95,115,110,97,107,101,0)];
       let update_nz6: Array<any> = [String.fromCharCode(110,105,98,0), String.fromCharCode(105,110,102,117,114,97,0)];
      while (grayP.length < 2) {
         minivodj = controlV.length >= 65 || !minivodj;
         break;
      }
      let crownb = String.fromCharCode(102,102,100,98,52,95,112,0) == controlV;
      do {
         controlV += `${update_nz6.length | 2}`;
         if (crownb) {
            break;
         }
      } while ((!minivodj) && crownb);
          let yellowcirclebg0: Map<any, any> = new Map([[String.fromCharCode(100,109,117,108,0),8], [String.fromCharCode(106,95,54,56,95,118,109,112,114,105,110,116,102,0),757], [String.fromCharCode(115,115,114,99,0),709]]);
          let zhubor = 0;
          let homeloadingt = String.fromCharCode(102,116,118,112,108,97,115,116,110,111,100,101,0);
         update_nz6.push(zhubor | 3);
         yellowcirclebg0 = new Map([[`${yellowcirclebg0.size}`, homeloadingt.length]]);
         zhubor >>= Math.min(2, Math.abs(1 | yellowcirclebg0.size));
         homeloadingt += `${yellowcirclebg0.size << (Math.min(Math.abs(3), 5))}`;
          let iconclosewhitewithbgb = 4.0;
          let china3 = String.fromCharCode(112,101,101,114,99,111,110,110,101,99,116,105,111,110,95,105,95,57,50,0);
          let teamdetailsbg0 = String.fromCharCode(112,111,115,116,105,111,110,0);
         controlV = `${(controlV == String.fromCharCode(90,0) ? controlV.length : update_nz6.length)}`;
         iconclosewhitewithbgb /= Math.max((String.fromCharCode(118,0) == teamdetailsbg0 ? teamdetailsbg0.length : parseInt(`${iconclosewhitewithbgb}`)), 1);
         china3 += `${teamdetailsbg0.length}`;
      for (let y = 0; y < 2; y++) {
         update_nz6.push(((minivodj ? 1 : 5) / 1));
      }
      emojiheart3 *= pusht;
   }
   for (let l = 0; l < 1; l++) {
      encryptorS += (String.fromCharCode(100,0) == regengJ ? libmapbufferjnic.length : regengJ.length);
   }
      backwardi = `${(1 ^ (private_2pp ? 1 : 4))}`;
   for (let e = 0; e < 1; e++) {
       let fullscreenmint = String.fromCharCode(102,111,114,99,101,0);
       let chaty = String.fromCharCode(117,112,108,111,97,100,105,110,103,0);
       let ewardedx = String.fromCharCode(116,101,97,114,0);
       let pangleU = String.fromCharCode(98,105,110,0);
       let sheetP = String.fromCharCode(103,95,49,53,95,110,97,118,105,103,97,116,105,111,110,0);
         pangleU = `${ewardedx.length * sheetP.length}`;
         chaty += `${(String.fromCharCode(84,0) == ewardedx ? ewardedx.length : pangleU.length)}`;
      for (let u = 0; u < 1; u++) {
         sheetP = `${(String.fromCharCode(53,0) == fullscreenmint ? fullscreenmint.length : chaty.length)}`;
      }
       let libreactnativeblobr = 4;
      let modelsQ = 6786790 >= ewardedx.length;
      do {
         ewardedx += `${fullscreenmint.length}`;
         if (modelsQ) {
            break;
         }
      } while (modelsQ && (fullscreenmint.length < ewardedx.length));
      let dice2 = String.fromCharCode(118,51,121,0) == fullscreenmint;
      do {
         fullscreenmint += `${fullscreenmint.length >> (Math.min(Math.abs(3), 1))}`;
         if (dice2) {
            break;
         }
      } while ((5 < pangleU.length) && dice2);
          let gradlewF: Map<any, any> = new Map([[String.fromCharCode(98,117,116,116,101,114,102,108,105,101,115,0),470], [String.fromCharCode(105,110,118,0),687], [String.fromCharCode(103,97,105,110,115,0),674]]);
          let predictionbuttonv = String.fromCharCode(102,111,114,109,97,116,116,105,110,103,0);
         chaty = `${chaty.length >> (Math.min(2, sheetP.length))}`;
         gradlewF.set(predictionbuttonv, predictionbuttonv.length);
      let teamj = 7135681 >= fullscreenmint.length;
      do {
         fullscreenmint = `${pangleU.length << (Math.min(Math.abs(2), 5))}`;
         if (teamj) {
            break;
         }
      } while (teamj && (chaty != String.fromCharCode(50,0)));
      while (4 >= fullscreenmint.length) {
         fullscreenmint = `${2 ^ pangleU.length}`;
         break;
      }
          let j_titleN = String.fromCharCode(121,95,51,55,95,97,115,115,111,99,0);
         libreactnativeblobr >>= Math.min(3, Math.abs(libreactnativeblobr % (Math.max(j_titleN.length, 9))));
      let sharewhiteW = sheetP.length >= 7090985;
      do {
          let basketballplayerplaceholderR: Map<any, any> = new Map([[String.fromCharCode(99,111,112,121,102,100,0),222], [String.fromCharCode(109,111,110,107,101,121,0),208], [String.fromCharCode(114,101,112,114,101,115,101,110,116,97,98,108,101,0),392]]);
          let typingr = 4.0;
          let rightW = 4.0;
          let controlf = 3.0;
         sheetP += `${(ewardedx == String.fromCharCode(104,0) ? parseInt(`${rightW}`) : ewardedx.length)}`;
         basketballplayerplaceholderR.set(`${typingr}`, 3);
         typingr /= Math.max(5, parseFloat(`${parseInt(`${controlf}`)}`));
         rightW -= parseInt(`${typingr}`);
         controlf *= parseFloat(`${basketballplayerplaceholderR.size}`);
         if (sharewhiteW) {
            break;
         }
      } while (sharewhiteW && (!sheetP.endsWith(pangleU)));
      while (chaty == String.fromCharCode(104,0)) {
         fullscreenmint += `${ewardedx.length & sheetP.length}`;
         break;
      }
         sheetP += `${libreactnativeblobr + chaty.length}`;
      while (pangleU.length == 3) {
         pangleU += `${(String.fromCharCode(77,0) == ewardedx ? libreactnativeblobr : ewardedx.length)}`;
         break;
      }
       let zoom1 = 3.0;
      encryptorS -= ((private_2pp ? 2 : 2) % 2);
   }
   while (!backwardi.includes(`${regengJ.length}`)) {
      regengJ += `${(String.fromCharCode(99,0) == backwardi ? backwardi.length : iconpointscoreK.size)}`;
      break;
   }
   if (5 == iconpointscoreK.size) {
      libimagepipelineD = !streaming5;
   }
   while (2.11 < (emojiheart3 - 4.92)) {
       let currentE: Map<any, any> = new Map([[String.fromCharCode(114,117,108,101,115,0),795], [String.fromCharCode(114,101,112,108,105,101,114,115,0),670], [String.fromCharCode(109,98,117,118,101,114,114,111,114,0),331]]);
       let h_hashq = true;
       let launcherX: Array<any> = [526, 421];
       let phoneK = String.fromCharCode(104,100,115,112,0);
       let sigmobI = String.fromCharCode(99,104,97,110,103,101,100,104,0);
         sigmobI = `${(currentE.size - (h_hashq ? 3 : 3))}`;
      while ((4 | sigmobI.length) == 2 && 1 == (4 | sigmobI.length)) {
         sigmobI += `${launcherX.length & sigmobI.length}`;
         break;
      }
         currentE.set(phoneK, (phoneK == String.fromCharCode(76,0) ? phoneK.length : sigmobI.length));
         sigmobI = `${((h_hashq ? 5 : 3))}`;
          let hongkongE = String.fromCharCode(119,120,109,109,116,101,115,116,0);
          let list9: Map<any, any> = new Map([[String.fromCharCode(102,116,114,117,110,99,97,116,101,0),false ], [String.fromCharCode(97,102,102,101,99,116,101,100,0),false ], [String.fromCharCode(99,111,100,101,99,99,116,108,95,56,95,56,56,0),true ]]);
          let predictionN = String.fromCharCode(99,108,101,97,114,101,100,0);
         h_hashq = predictionN.length < list9.size;
         hongkongE = "2";
         list9 = new Map([[hongkongE, hongkongE.length]]);
         launcherX = [sigmobI.length];
      let coreP = phoneK.length <= 6201990;
      do {
          let shielddones = String.fromCharCode(103,97,109,117,116,0);
          let photoF = String.fromCharCode(114,111,108,108,0);
          let reactnativeultimatelistview6 = 5.0;
         phoneK = `${photoF.length ^ parseInt(`${reactnativeultimatelistview6}`)}`;
         shielddones = `${(String.fromCharCode(73,0) == shielddones ? shielddones.length : shielddones.length)}`;
         photoF = "2";
         if (coreP) {
            break;
         }
      } while (coreP && (phoneK.length >= 4));
          let delegate_q6 = 5.0;
          let renewZ = 1.0;
          let untick8 = String.fromCharCode(99,111,115,113,105,0);
         phoneK = `${1 % (Math.max(3, currentE.size))}`;
         delegate_q6 += parseInt(`${renewZ}`);
         renewZ /= Math.max(1, parseFloat(`${3 + untick8.length}`));
         untick8 = `${untick8.length}`;
      if (2 == (launcherX.length >> (Math.min(Math.abs(4), 1)))) {
         h_hashq = sigmobI.length == 5;
      }
          let halffieldimage6: Array<any> = [505, 486];
         phoneK = `${halffieldimage6.length / 1}`;
          let iconqqo: Array<any> = [785, 601, 726];
          let modelsE: Array<any> = [597, 857];
          let profileactiveb: Array<any> = [516, 278];
         phoneK += `${((h_hashq ? 3 : 3) ^ 3)}`;
         iconqqo.push(modelsE.length / 1);
         modelsE.push(2);
         profileactiveb = [3 << (Math.min(1, modelsE.length))];
          let side8 = 2.0;
         currentE.set(sigmobI, sigmobI.length >> (Math.min(Math.abs(2), 1)));
         side8 -= parseInt(`${side8}`) << (Math.min(3, Math.abs(2)));
      if (h_hashq) {
         sigmobI += `${currentE.size / (Math.max(launcherX.length, 1))}`;
      }
      if (h_hashq) {
          let castingt = 4.0;
          let ewardedL = String.fromCharCode(112,114,101,118,105,101,119,0);
          let dicer = true;
         launcherX.push(currentE.size | phoneK.length);
         castingt -= (parseFloat(`${(dicer ? 5 : 4) << (Math.min(Math.abs(2), 1))}`));
         ewardedL += `${(parseInt(`${castingt}`) + (dicer ? 4 : 1))}`;
      }
      for (let x = 0; x < 1; x++) {
         h_hashq = sigmobI == String.fromCharCode(66,0);
      }
      emojiheart3 -= static_4W.length;
      break;
   }
   let iconadslinkr = iconpointscoreK.size >= 6040249;
   do {
      iconpointscoreK.set(`${encryptorS}`, str0.size);
      if (iconadslinkr) {
         break;
      }
   } while (iconadslinkr && (iconpointscoreK.size <= backwardi.length));

        setShowConfirmDialog(true);
    }

    const onConfirmRemove = () => {
       let completeW: Map<any, any> = new Map([[String.fromCharCode(97,114,101,118,101,114,115,101,0),true ], [String.fromCharCode(116,111,117,116,95,100,95,54,0),false ]]);
    let pressurer = String.fromCharCode(119,95,51,55,95,99,104,101,99,107,98,111,120,0);
    let bgvipxvod7 = String.fromCharCode(97,115,97,110,0);
    let dependencyt: Array<any> = [218, 34];
    let iconstarx = false;
    let defaultprofilepic_ = true;
    let fullscreenmin9 = String.fromCharCode(103,101,116,112,0);
    let faviconI = String.fromCharCode(99,100,102,116,95,104,95,56,57,0);
    let gifgoal4: Array<any> = [856, 615, 58];
    let areaw = 4;
    let owngoalr = true;
    let matchb = 3.0;
    let videocommon7 = String.fromCharCode(112,101,114,99,101,110,116,105,108,101,0);
       let crownk = 1.0;
       let iconarrowleftQ = String.fromCharCode(105,115,110,101,103,97,116,105,118,101,0);
       let tooltipsQ = String.fromCharCode(101,120,116,101,114,110,0);
      while (tooltipsQ.includes(`${crownk}`)) {
         tooltipsQ = `${parseInt(`${crownk}`)}`;
         break;
      }
         tooltipsQ += `${parseInt(`${crownk}`) + iconarrowleftQ.length}`;
          let matchdetailbgd = 3;
          let resultR = 1.0;
         crownk += parseFloat(`${iconarrowleftQ.length}`);
         matchdetailbgd ^= parseInt(`${resultR}`);
         resultR *= parseFloat(`${3}`);
         iconarrowleftQ += `${parseInt(`${crownk}`) / (Math.max(6, tooltipsQ.length))}`;
      for (let y = 0; y < 2; y++) {
         tooltipsQ = "3";
      }
      for (let e = 0; e < 1; e++) {
          let applec = String.fromCharCode(110,101,103,111,116,105,97,116,101,0);
          let videobufferloadingY = true;
          let libfileg = String.fromCharCode(116,114,117,110,99,0);
         crownk /= Math.max((parseFloat(`${String.fromCharCode(121,0) == applec ? applec.length : libfileg.length}`)), 4);
         videobufferloadingY = !videobufferloadingY;
      }
         iconarrowleftQ += `${iconarrowleftQ.length}`;
          let videocommonR = String.fromCharCode(97,102,116,101,114,108,105,102,101,0);
          let flipper9 = 1;
         tooltipsQ += `${(tooltipsQ == String.fromCharCode(112,0) ? tooltipsQ.length : flipper9)}`;
         videocommonR += `${videocommonR.length}`;
         flipper9 += videocommonR.length;
         iconarrowleftQ += `${(String.fromCharCode(105,0) == tooltipsQ ? parseInt(`${crownk}`) : tooltipsQ.length)}`;
      dependencyt.push(3);
      pressurer += `${parseInt(`${matchb}`)}`;
   for (let j = 0; j < 2; j++) {
       let uploadm: Map<any, any> = new Map([[String.fromCharCode(114,97,108,102,0),909], [String.fromCharCode(104,108,105,116,0),112]]);
       let philippinesS = 2;
       let latnu: Map<any, any> = new Map([[String.fromCharCode(109,97,99,114,111,115,0),true ], [String.fromCharCode(99,95,56,56,95,97,116,116,114,105,98,117,116,101,115,0),false ]]);
       let gpayS = 5.0;
       let libloggerN = 5.0;
          let moduleR = String.fromCharCode(115,104,105,109,115,0);
         uploadm = new Map([[`${latnu.size}`, 2 >> (Math.min(4, Math.abs(latnu.size)))]]);
         moduleR = `${moduleR.length ^ 1}`;
          let delegate_upe: Array<any> = [406, 533, 600];
         gpayS *= parseFloat(`${parseInt(`${gpayS}`) | 3}`);
         delegate_upe.push(delegate_upe.length << (Math.min(2, delegate_upe.length)));
      for (let i = 0; i < 1; i++) {
          let bgvipsporty: Array<any> = [566, 775];
          let helper_: Map<any, any> = new Map([[String.fromCharCode(100,105,115,112,97,116,99,104,95,116,95,49,57,0),336], [String.fromCharCode(116,114,117,110,99,97,116,105,111,110,0),815], [String.fromCharCode(116,111,103,103,108,101,95,57,95,54,54,0),109]]);
          let playlistW = false;
          let favoriteB = false;
          let containerk: Array<any> = [856, 806, 480];
         gpayS += parseFloat(`${uploadm.size}`);
         bgvipsporty = [bgvipsporty.length >> (Math.min(Math.abs(3), 5))];
         helper_ = new Map([[`${playlistW}`, ((favoriteB ? 3 : 2) & (playlistW ? 3 : 2))]]);
         favoriteB = helper_.size >= 12;
         containerk.push(3 * containerk.length);
      }
      while (Array.from(uploadm.keys()).includes(`${philippinesS}`)) {
         philippinesS ^= uploadm.size;
         break;
      }
         gpayS /= Math.max(parseFloat(`${parseInt(`${gpayS}`) << (Math.min(4, Math.abs(1)))}`), 3);
      if (!Array.from(latnu.values()).includes(philippinesS)) {
          let alertx = String.fromCharCode(97,117,116,111,99,111,114,114,101,108,97,116,101,0);
         philippinesS += parseInt(`${gpayS}`);
         alertx = `${alertx.length >> (Math.min(alertx.length, 1))}`;
      }
         latnu = new Map([[`${uploadm.size}`, 3 & philippinesS]]);
         gpayS *= parseFloat(`${3}`);
      defaultprofilepic_ = 79 == areaw;
   }
   if (1 < (3 + dependencyt.length) && 3 < (completeW.size + 3)) {
       let submitP = String.fromCharCode(100,117,109,112,0);
       let iconclosewhitebge = String.fromCharCode(118,116,97,98,0);
      if (submitP.length <= iconclosewhitebge.length) {
         iconclosewhitebge += `${(iconclosewhitebge == String.fromCharCode(100,0) ? submitP.length : iconclosewhitebge.length)}`;
      }
          let downloadedY: Array<any> = [457, 471];
          let langD = 5.0;
         iconclosewhitebge += `${iconclosewhitebge.length}`;
         downloadedY.push(1 % (Math.max(3, parseInt(`${langD}`))));
         langD *= downloadedY.length >> (Math.min(2, Math.abs(parseInt(`${langD}`))));
      for (let w = 0; w < 1; w++) {
         submitP += `${(String.fromCharCode(50,0) == submitP ? iconclosewhitebge.length : submitP.length)}`;
      }
      let delegate_rM = iconclosewhitebge.length >= 6615414;
      do {
         iconclosewhitebge += `${iconclosewhitebge.length}`;
         if (delegate_rM) {
            break;
         }
      } while ((submitP != iconclosewhitebge) && delegate_rM);
      let promotionb = iconclosewhitebge == String.fromCharCode(54,103,111,101,49,100,116,108,0);
      do {
         iconclosewhitebge = `${(submitP == String.fromCharCode(89,0) ? iconclosewhitebge.length : submitP.length)}`;
         if (promotionb) {
            break;
         }
      } while ((3 <= iconclosewhitebge.length) && promotionb);
       let projectY = 3.0;
      dependencyt.push(iconclosewhitebge.length);
   }
       let targetB: Map<any, any> = new Map([[String.fromCharCode(115,99,113,117,101,114,121,0),String.fromCharCode(115,117,98,115,101,116,0)], [String.fromCharCode(112,108,97,110,101,100,0),String.fromCharCode(98,114,105,100,103,105,110,103,0)]]);
       let libcxxcomponentsU: Map<any, any> = new Map([[String.fromCharCode(97,108,108,111,99,97,116,101,100,0),172], [String.fromCharCode(101,100,103,101,115,95,119,95,56,55,0),198]]);
       let defaultfootballbge = 0.0;
         libcxxcomponentsU = new Map([[`${targetB.size}`, targetB.size | libcxxcomponentsU.size]]);
      for (let m = 0; m < 2; m++) {
          let default_27z = String.fromCharCode(102,97,110,99,121,0);
          let teamdetailsbg0 = true;
          let baiduC = true;
         libcxxcomponentsU.set(default_27z, parseInt(`${defaultfootballbge}`) + 2);
         default_27z += "1";
         teamdetailsbg0 = !baiduC;
      }
      let dependenciesc = libcxxcomponentsU.size <= 6650036;
      do {
          let libavcodecY = 5.0;
          let stepq: Array<any> = [String.fromCharCode(114,95,53,53,95,111,102,102,115,99,114,101,101,110,0), String.fromCharCode(115,99,111,112,101,100,95,57,95,57,50,0)];
          let found5: Array<any> = [424, 170];
          let gemfileL: Array<any> = [String.fromCharCode(99,97,99,104,101,100,0), String.fromCharCode(102,100,99,116,0), String.fromCharCode(111,100,109,108,0)];
          let renewa = String.fromCharCode(114,101,102,105,110,105,110,103,0);
         libcxxcomponentsU.set(`${found5.length}`, found5.length / (Math.max(3, 7)));
         libavcodecY += renewa.length - gemfileL.length;
         stepq.push(1 - parseInt(`${libavcodecY}`));
         gemfileL.push(parseInt(`${libavcodecY}`) + renewa.length);
         if (dependenciesc) {
            break;
         }
      } while (((5.82 - defaultfootballbge) == 1.67 && 5.82 == (defaultfootballbge - parseFloat(`${libcxxcomponentsU.size}`))) && dependenciesc);
       let producty = String.fromCharCode(104,95,50,49,95,98,108,111,99,107,105,110,103,0);
         libcxxcomponentsU.set(`${libcxxcomponentsU.size}`, libcxxcomponentsU.size % (Math.max(targetB.size, 10)));
         targetB = new Map([[`${targetB.size}`, producty.length]]);
         defaultfootballbge += parseFloat(`${targetB.size}`);
         producty = `${targetB.size}`;
          let thailandl = String.fromCharCode(97,118,99,116,120,0);
         targetB = new Map([[`${libcxxcomponentsU.size}`, 1]]);
         thailandl = `${thailandl.length}`;
      iconstarx = 94.54 <= defaultfootballbge;
   for (let q = 0; q < 3; q++) {
      matchb -= (parseFloat(`${fullscreenmin9 == String.fromCharCode(100,0) ? pressurer.length : fullscreenmin9.length}`));
   }
      completeW.set(fullscreenmin9, fullscreenmin9.length << (Math.min(Math.abs(2), 5)));
   if (2 > (pressurer.length << (Math.min(3, Math.abs(completeW.size)))) && 2 > (2 << (Math.min(5, Math.abs(completeW.size))))) {
      pressurer += `${(completeW.size >> (Math.min(2, Math.abs((iconstarx ? 4 : 3)))))}`;
   }

        if (videoSelected.length <= 0) return;

   for (let h = 0; h < 3; h++) {
       let buttonw = false;
      let crossf = buttonw ? !buttonw : buttonw;
      do {
         buttonw = !buttonw;
         if (crossf) {
            break;
         }
      } while (crossf && (buttonw || buttonw));
         buttonw = (!buttonw ? buttonw : !buttonw);
      let loadingS = buttonw ? !buttonw : buttonw;
      do {
          let uimanagerI = 3;
         buttonw = !buttonw || 37 > uimanagerI;
         if (loadingS) {
            break;
         }
      } while ((!buttonw && !buttonw) && loadingS);
      iconstarx = !iconstarx;
   }
   let style7 = fullscreenmin9 == String.fromCharCode(106,104,118,100,109,50,55,56,122,0);
   do {
      fullscreenmin9 += `${bgvipxvod7.length - pressurer.length}`;
      if (style7) {
         break;
      }
   } while (style7 && (fullscreenmin9.length <= 2));
   let reactnativeultimatelistviewo = 5225441.0 >= matchb;
   do {
       let awayn = String.fromCharCode(109,101,100,105,97,115,0);
       let leakcheckerr: Array<any> = [String.fromCharCode(102,101,109,97,108,101,0), String.fromCharCode(99,104,97,110,110,101,108,115,95,106,95,54,52,0), String.fromCharCode(100,105,115,99,111,110,110,101,99,116,101,100,0)];
       let update_zy5 = String.fromCharCode(102,114,97,109,101,98,117,102,102,101,114,0);
       let foregroundi = 2;
      while (leakcheckerr.length == update_zy5.length) {
          let predictionbannerf = 0;
         leakcheckerr = [foregroundi ^ leakcheckerr.length];
         predictionbannerf -= 3;
         break;
      }
       let mbsplashu: Map<any, any> = new Map([[String.fromCharCode(100,111,119,110,115,97,109,112,108,101,0),580], [String.fromCharCode(112,101,114,115,112,0),715]]);
         mbsplashu = new Map([[`${leakcheckerr.length}`, awayn.length + 1]]);
      for (let s = 0; s < 2; s++) {
         update_zy5 += `${3 - update_zy5.length}`;
      }
          let basketballiconS = 5.0;
          let hejis = String.fromCharCode(98,114,101,97,100,99,114,117,109,98,115,0);
          let mime7 = String.fromCharCode(100,118,100,115,117,98,0);
         leakcheckerr.push(awayn.length);
         basketballiconS /= Math.max(1, 2);
         hejis = `${2 + mime7.length}`;
         mime7 = `${hejis.length * mime7.length}`;
       let mbbidF = 1.0;
       let arrowupY = 5.0;
      for (let j = 0; j < 1; j++) {
         update_zy5 += `${awayn.length}`;
      }
         mbbidF /= Math.max(3, awayn.length);
      while (3.1 < mbbidF) {
         mbbidF *= parseInt(`${mbbidF}`);
         break;
      }
      let sheet0 = 6500713.0 >= arrowupY;
      do {
          let homeiconE = String.fromCharCode(101,100,105,116,0);
          let container0: Array<any> = [String.fromCharCode(109,101,97,110,0), String.fromCharCode(99,97,100,101,110,99,101,0), String.fromCharCode(102,111,114,109,97,116,117,0)];
          let smallorangemanl = true;
          let componentL = String.fromCharCode(108,111,99,105,0);
         arrowupY -= (update_zy5 == String.fromCharCode(122,0) ? update_zy5.length : container0.length);
         homeiconE += `${(homeiconE == String.fromCharCode(71,0) ? (smallorangemanl ? 1 : 3) : homeiconE.length)}`;
         container0 = [componentL.length % (Math.max(3, 6))];
         smallorangemanl = !smallorangemanl;
         componentL = `${(2 ^ (smallorangemanl ? 1 : 4))}`;
         if (sheet0) {
            break;
         }
      } while (((3 >> (Math.min(5, awayn.length))) == 4 && (awayn.length / (Math.max(2, arrowupY))) == 2.83) && sheet0);
      while ((arrowupY - foregroundi) > 5.69) {
          let iconlogoutk: Map<any, any> = new Map([[String.fromCharCode(103,114,97,110,112,111,115,0),String.fromCharCode(115,109,112,116,101,104,100,98,97,114,115,0)], [String.fromCharCode(115,111,108,105,100,105,116,121,0),String.fromCharCode(114,101,112,108,121,95,111,95,51,50,0)], [String.fromCharCode(111,114,103,97,110,105,122,97,116,105,111,110,0),String.fromCharCode(114,116,97,100,100,114,115,0)]]);
         foregroundi %= Math.max(1 >> (Math.min(4, Math.abs(parseInt(`${arrowupY}`)))), 4);
         iconlogoutk.set(`${iconlogoutk.size}`, 1);
         break;
      }
      while ((mbsplashu.size % (Math.max(2, 7))) <= 2) {
         foregroundi -= (String.fromCharCode(101,0) == update_zy5 ? update_zy5.length : leakcheckerr.length);
         break;
      }
      matchb *= parseFloat(`${update_zy5.length >> (Math.min(Math.abs(3), 5))}`);
      if (reactnativeultimatelistviewo) {
         break;
      }
   } while ((!bgvipxvod7.endsWith(`${matchb}`)) && reactnativeultimatelistviewo);
      bgvipxvod7 += `${bgvipxvod7.length}`;
       let release_0E = String.fromCharCode(116,114,97,105,110,0);
      while (3 == release_0E.length) {
          let videojs3: Map<any, any> = new Map([[String.fromCharCode(112,101,114,115,111,110,97,108,0),true ], [String.fromCharCode(110,100,111,116,115,0),false ], [String.fromCharCode(99,97,109,101,108,108,105,97,0),false ]]);
          let telegramT = 5.0;
         release_0E += `${parseInt(`${telegramT}`)}`;
         videojs3.set(`${videojs3.size}`, videojs3.size);
         telegramT /= Math.max(4, videojs3.size);
         break;
      }
      let applicationt = 5831081 >= release_0E.length;
      do {
         release_0E += "1";
         if (applicationt) {
            break;
         }
      } while ((release_0E == release_0E) && applicationt);
         release_0E += `${(release_0E == String.fromCharCode(67,0) ? release_0E.length : release_0E.length)}`;
      dependencyt = [gifgoal4.length];
   let uimanagerV = 7660395 <= completeW.size;
   do {
      completeW.set(bgvipxvod7, bgvipxvod7.length);
      if (uimanagerV) {
         break;
      }
   } while (uimanagerV && ((areaw & 5) > 5 || 5 > (completeW.size & areaw)));
       let tickw = String.fromCharCode(97,108,112,104,97,101,120,116,114,97,99,116,0);
       let casti = String.fromCharCode(105,110,105,116,105,97,108,108,121,0);
       let pushW: Array<any> = [226, 95, 529];
      if (pushW.length < 3) {
         casti = `${tickw.length}`;
      }
          let iconshare2 = String.fromCharCode(109,97,110,97,103,101,97,98,108,101,0);
         pushW.push(1);
         iconshare2 = "1";
      if (tickw.length == casti.length) {
         tickw = `${tickw.length}`;
      }
      if (pushW.length == 1) {
         tickw += `${tickw.length / 3}`;
      }
      while (!casti.startsWith(`${tickw.length}`)) {
         casti += `${pushW.length ^ 1}`;
         break;
      }
       let flyerp = 3.0;
      for (let f = 0; f < 2; f++) {
         pushW = [parseInt(`${flyerp}`)];
      }
         tickw = `${(casti == String.fromCharCode(110,0) ? casti.length : pushW.length)}`;
      while (5 == (4 + pushW.length)) {
          let fastforwardC = String.fromCharCode(113,109,105,110,109,97,120,0);
         pushW.push(pushW.length + 3);
         fastforwardC += `${fastforwardC.length / 1}`;
         break;
      }
      dependencyt = [1 ^ parseInt(`${matchb}`)];
   for (let v = 0; v < 1; v++) {
      dependencyt.push((gifgoal4.length % (Math.max(5, (owngoalr ? 5 : 4)))));
   }

        dispatch(removeUploadHistory(videoSelected));

   let mimoL = 6595921 >= completeW.size;
   do {
      completeW.set(fullscreenmin9, 1);
      if (mimoL) {
         break;
      }
   } while (((fullscreenmin9.length & 1) < 3) && mimoL);
       let ball7 = false;
       let iconsettingg = String.fromCharCode(112,114,111,118,105,115,105,111,110,0);
       let phoneW = String.fromCharCode(102,105,103,104,116,101,114,115,0);
       let stationsZ = false;
         iconsettingg += `${(iconsettingg == String.fromCharCode(87,0) ? (stationsZ ? 4 : 1) : iconsettingg.length)}`;
         ball7 = iconsettingg == String.fromCharCode(65,0) || 37 == phoneW.length;
          let typingA = String.fromCharCode(97,116,111,109,105,99,111,112,115,0);
          let roundH = true;
         phoneW += `${iconsettingg.length}`;
         typingA += "1";
         roundH = 97 == typingA.length;
         phoneW = `${(iconsettingg == String.fromCharCode(120,0) ? iconsettingg.length : (stationsZ ? 1 : 1))}`;
          let trashW = String.fromCharCode(100,100,114,97,110,103,101,0);
          let starP = 1.0;
          let strings3 = 4.0;
         ball7 = iconsettingg.length >= 96 && ball7;
         trashW += `${2 + parseInt(`${strings3}`)}`;
         starP /= Math.max(3, parseFloat(`${parseInt(`${starP}`)}`));
         strings3 *= parseFloat(`${parseInt(`${strings3}`)}`);
       let information9: Array<any> = [419, 448, 597];
       let emptyQ: Array<any> = [String.fromCharCode(97,112,101,114,0), String.fromCharCode(102,100,101,99,0), String.fromCharCode(117,110,114,101,102,101,114,101,110,99,101,0)];
         stationsZ = information9.length < iconsettingg.length;
      if (phoneW.endsWith(`${ball7}`)) {
         ball7 = phoneW.length <= 96 || ball7;
      }
      videocommon7 = `${(fullscreenmin9.length - (owngoalr ? 5 : 5))}`;
      gifgoal4.push(gifgoal4.length * 3);
   while (!bgvipxvod7.endsWith(faviconI)) {
      bgvipxvod7 = `${parseInt(`${matchb}`) << (Math.min(1, Math.abs(3)))}`;
      break;
   }
   if (!iconstarx && faviconI.length <= 3) {
      iconstarx = videocommon7.length <= 16 && !defaultprofilepic_;
   }
       let yellowanimationlivei = 2.0;
       let mbbidA = String.fromCharCode(110,101,109,111,110,105,99,0);
       let videobufferloadingz: Array<any> = [147, 548, 400];
       let textlayoutmanagerM: Array<any> = [380, 944, 866];
      for (let x = 0; x < 2; x++) {
         mbbidA = `${parseInt(`${yellowanimationlivei}`) * mbbidA.length}`;
      }
       let buildx = 1.0;
       let videovarx = false;
       let megaphoneD = true;
      while (mbbidA.length <= parseInt(`${yellowanimationlivei}`)) {
         mbbidA = `${textlayoutmanagerM.length}`;
         break;
      }
      while ((5.59 + yellowanimationlivei) < 5.31 && 5.59 < (yellowanimationlivei + parseFloat(`${mbbidA.length}`))) {
         yellowanimationlivei += parseFloat(`${parseInt(`${buildx}`) * videobufferloadingz.length}`);
         break;
      }
      owngoalr = (35 <= ((owngoalr ? faviconI.length : 35) / (Math.max(faviconI.length, 8))));
   for (let o = 0; o < 2; o++) {
      gifgoal4 = [fullscreenmin9.length];
   }
   let twitterG = matchb <= 7463288.0;
   do {
      matchb /= Math.max(3, parseFloat(`${dependencyt.length}`));
      if (twitterG) {
         break;
      }
   } while (twitterG && (5.73 > (matchb - 1.37)));
        setVideoSelected([]);

      pressurer = `${pressurer.length << (Math.min(5, Math.abs(parseInt(`${matchb}`))))}`;
       let darkd = 2;
      for (let a = 0; a < 3; a++) {
          let profilepicf: Array<any> = [134, 691, 846];
         darkd <<= Math.min(Math.abs(2), 3);
         profilepicf.push(profilepicf.length * profilepicf.length);
      }
      if ((darkd % 3) <= 3) {
          let shareX: Array<any> = [600, 462];
          let libbufferX = String.fromCharCode(97,117,114,97,0);
          let libhermesF = String.fromCharCode(115,116,114,105,107,101,116,104,114,111,117,103,104,0);
          let lefte: Array<any> = [564, 636, 377];
          let mbnativeR = 4.0;
         darkd -= lefte.length;
         shareX.push(libbufferX.length / (Math.max(1, shareX.length)));
         libbufferX = `${parseInt(`${mbnativeR}`)}`;
         libhermesF += `${libhermesF.length}`;
         lefte = [parseInt(`${mbnativeR}`) + 2];
      }
          let screenB: Map<any, any> = new Map([[String.fromCharCode(108,95,56,54,95,116,101,120,116,97,116,116,114,0),true ], [String.fromCharCode(98,101,120,116,0),true ], [String.fromCharCode(100,101,112,97,114,116,109,101,110,116,0),false ]]);
         darkd *= 1 & darkd;
         screenB.set(`${screenB.size}`, screenB.size - 1);
      owngoalr = 8 <= completeW.size;
   if (dependencyt.length > gifgoal4.length) {
      gifgoal4 = [(faviconI.length * (defaultprofilepic_ ? 4 : 1))];
   }
   if (gifgoal4.length > pressurer.length) {
      gifgoal4.push(parseInt(`${matchb}`) / (Math.max(6, faviconI.length)));
   }
   if (owngoalr && bgvipxvod7.length >= 3) {
       let showlessb = String.fromCharCode(108,95,49,57,95,97,108,105,118,101,0);
       let private_4R = 4.0;
       let sellmathbackgroundc = String.fromCharCode(101,110,99,111,100,101,105,110,116,114,97,0);
       let user3 = 0.0;
         user3 *= parseFloat(`${parseInt(`${user3}`) % 2}`);
          let downarrow7 = String.fromCharCode(115,99,101,110,97,114,105,111,0);
         private_4R -= showlessb.length;
         downarrow7 += `${(downarrow7 == String.fromCharCode(107,0) ? downarrow7.length : downarrow7.length)}`;
         showlessb = `${(String.fromCharCode(51,0) == sellmathbackgroundc ? parseInt(`${private_4R}`) : sellmathbackgroundc.length)}`;
      let roundf = String.fromCharCode(48,111,118,51,105,97,52,55,116,103,0) == sellmathbackgroundc;
      do {
         sellmathbackgroundc += `${showlessb.length + 1}`;
         if (roundf) {
            break;
         }
      } while (roundf && (showlessb == sellmathbackgroundc));
         user3 /= Math.max(4, parseFloat(`${showlessb.length}`));
      let sliderW = 8744454.0 <= private_4R;
      do {
         private_4R *= parseInt(`${private_4R}`) >> (Math.min(Math.abs(2), 2));
         if (sliderW) {
            break;
         }
      } while (sliderW && (sellmathbackgroundc.length < parseInt(`${private_4R}`)));
      let floaterq = String.fromCharCode(52,108,119,57,104,120,0) == showlessb;
      do {
         showlessb = "1";
         if (floaterq) {
            break;
         }
      } while ((showlessb.length > 4) && floaterq);
      if (4.17 <= (1.60 + private_4R) || 2 <= (sellmathbackgroundc.length >> (Math.min(Math.abs(3), 3)))) {
          let libyogao = false;
         private_4R -= parseInt(`${user3}`) - 3;
         libyogao = (libyogao ? libyogao : libyogao);
      }
      if (4.52 < (private_4R - 1)) {
         user3 /= Math.max(parseFloat(`${sellmathbackgroundc.length << (Math.min(1, Math.abs(parseInt(`${private_4R}`))))}`), 2);
      }
      if (4 > (3 | sellmathbackgroundc.length)) {
         private_4R /= Math.max(3, parseInt(`${user3}`));
      }
      let iconarrowrightl = sellmathbackgroundc.length <= 7628391;
      do {
         sellmathbackgroundc += `${2 & parseInt(`${user3}`)}`;
         if (iconarrowrightl) {
            break;
         }
      } while (iconarrowrightl && ((parseFloat(`${sellmathbackgroundc.length}`) + user3) >= 2.13 || (parseFloat(`${sellmathbackgroundc.length}`) + user3) >= 2.13));
      while (sellmathbackgroundc.length <= 3) {
         private_4R -= parseInt(`${user3}`) * 2;
         break;
      }
      owngoalr = videocommon7.length < 95;
   }
      areaw += 3;
       let fillV: Map<any, any> = new Map([[String.fromCharCode(108,111,117,100,110,101,115,115,0),false ], [String.fromCharCode(118,97,114,105,97,110,116,0),true ]]);
      if (2 < (3 << (Math.min(3, Math.abs(fillV.size)))) || (3 << (Math.min(5, Math.abs(fillV.size)))) < 5) {
         fillV.set(`${fillV.size}`, fillV.size % 3);
      }
      let groupW = 7291397 >= fillV.size;
      do {
         fillV.set(`${fillV.size}`, 3);
         if (groupW) {
            break;
         }
      } while (groupW && ((5 >> (Math.min(2, Math.abs(fillV.size)))) < 2 && 5 < (fillV.size >> (Math.min(Math.abs(5), 4)))));
      if (1 > (fillV.size & fillV.size) && (fillV.size & fillV.size) > 1) {
          let episodeX = String.fromCharCode(112,97,110,100,105,110,103,0);
          let modityl: Array<any> = [864, 235, 357];
         fillV = new Map([[`${modityl.length}`, (String.fromCharCode(84,0) == episodeX ? episodeX.length : modityl.length)]]);
      }
      matchb += parseFloat(`${completeW.size}`);
      bgvipxvod7 = `${fullscreenmin9.length ^ parseInt(`${matchb}`)}`;
        setShowConfirmDialog(false);

   if (videocommon7.length > 2 && !iconstarx) {
      iconstarx = areaw < 96 || 96 < dependencyt.length;
   }
   for (let w = 0; w < 3; w++) {
       let mintegralP = String.fromCharCode(116,97,108,107,0);
         mintegralP += "3";
         mintegralP = `${(String.fromCharCode(117,0) == mintegralP ? mintegralP.length : mintegralP.length)}`;
      if (mintegralP != mintegralP) {
          let referrer1 = String.fromCharCode(106,95,50,52,95,102,95,49,56,0);
         mintegralP = `${referrer1.length}`;
      }
      pressurer += `${(2 & (owngoalr ? 4 : 2))}`;
   }
       let homeplayerT = false;
      let privatechatbgQ = homeplayerT ? !homeplayerT : homeplayerT;
      do {
         homeplayerT = !homeplayerT;
         if (privatechatbgQ) {
            break;
         }
      } while ((!homeplayerT || !homeplayerT) && privatechatbgQ);
      let mored = homeplayerT ? !homeplayerT : homeplayerT;
      do {
         homeplayerT = (homeplayerT ? homeplayerT : homeplayerT);
         if (mored) {
            break;
         }
      } while (mored && (homeplayerT));
      while (homeplayerT) {
         homeplayerT = (!homeplayerT ? !homeplayerT : !homeplayerT);
         break;
      }
      completeW.set(`${pressurer}`, pressurer.length | completeW.size);
       let largesoundd = String.fromCharCode(115,117,98,110,111,100,101,115,95,116,95,49,54,0);
       let reactQ = 1;
       let libffmpegkitM = String.fromCharCode(116,114,117,110,0);
      while (largesoundd.length > reactQ) {
         reactQ += 3;
         break;
      }
      let graphz = String.fromCharCode(116,106,57,113,0) == libffmpegkitM;
      do {
         libffmpegkitM = `${largesoundd.length + 3}`;
         if (graphz) {
            break;
         }
      } while ((!libffmpegkitM.includes(`${reactQ}`)) && graphz);
         libffmpegkitM = `${(String.fromCharCode(67,0) == largesoundd ? largesoundd.length : reactQ)}`;
         libffmpegkitM += "3";
      for (let r = 0; r < 1; r++) {
          let iconarrowrightT = 3.0;
          let sharewhiteV = String.fromCharCode(112,104,97,115,101,115,0);
          let iconuserb = String.fromCharCode(116,101,120,116,98,111,120,0);
          let libglogR = 2;
         libffmpegkitM += `${libffmpegkitM.length % (Math.max(3, 4))}`;
         iconarrowrightT *= parseFloat(`${3 % (Math.max(9, iconuserb.length))}`);
         sharewhiteV += `${sharewhiteV.length}`;
         iconuserb += `${3 + iconuserb.length}`;
         libglogR -= iconuserb.length;
      }
         largesoundd += `${(largesoundd == String.fromCharCode(115,0) ? largesoundd.length : reactQ)}`;
      while (!largesoundd.includes(libffmpegkitM)) {
         largesoundd = `${reactQ}`;
         break;
      }
          let ajaxp = String.fromCharCode(101,103,114,101,115,115,0);
          let activityx = 1.0;
         libffmpegkitM += `${ajaxp.length}`;
         ajaxp += `${parseInt(`${activityx}`) + 3}`;
         activityx *= parseFloat(`${parseInt(`${activityx}`) - 2}`);
          let runtimescheduler4 = false;
          let homeactive6 = true;
          let backiconc: Map<any, any> = new Map([[String.fromCharCode(99,102,102,116,98,0),440], [String.fromCharCode(120,118,105,100,0),125], [String.fromCharCode(108,111,99,97,108,105,116,121,95,120,95,57,53,0),946]]);
         reactQ -= ((homeactive6 ? 4 : 2) % (Math.max(libffmpegkitM.length, 8)));
         runtimescheduler4 = backiconc.get(`${runtimescheduler4}`) == null;
         homeactive6 = backiconc.size > 70;
      videocommon7 += `${((defaultprofilepic_ ? 3 : 2))}`;
   while (!videocommon7.startsWith(`${completeW.size}`)) {
      videocommon7 += `${dependencyt.length}`;
      break;
   }
   while (pressurer.length <= 1) {
      owngoalr = fullscreenmin9.length <= 11;
      break;
   }
      defaultprofilepic_ = areaw <= 47;
   for (let y = 0; y < 1; y++) {
       let executorY = 4.0;
       let main_cz = String.fromCharCode(115,117,112,112,114,101,115,115,101,100,0);
       let binddatasZ = String.fromCharCode(103,95,56,95,105,109,112,111,114,116,97,110,116,0);
       let libreactnativejniT = String.fromCharCode(103,101,110,101,114,97,108,0);
      let videocommon_ = String.fromCharCode(109,51,111,98,0) == libreactnativejniT;
      do {
          let downloadH = 0.0;
          let package_a2o = false;
          let eact0: Map<any, any> = new Map([[String.fromCharCode(114,101,118,101,97,108,101,100,0),751], [String.fromCharCode(109,111,100,105,102,105,101,100,0),510]]);
          let recommendation5 = String.fromCharCode(98,108,111,99,107,105,101,0);
         libreactnativejniT = `${(libreactnativejniT == String.fromCharCode(110,0) ? libreactnativejniT.length : eact0.size)}`;
         downloadH -= ((package_a2o ? 3 : 1) | parseInt(`${downloadH}`));
         package_a2o = !package_a2o;
         eact0.set(`${downloadH}`, parseInt(`${downloadH}`) / (Math.max(1, recommendation5.length)));
         recommendation5 += `${((package_a2o ? 5 : 2) - parseInt(`${downloadH}`))}`;
         if (videocommon_) {
            break;
         }
      } while (((parseInt(`${executorY}`) * libreactnativejniT.length) == 5 && 2 == (libreactnativejniT.length >> (Math.min(Math.abs(5), 3)))) && videocommon_);
       let score7 = String.fromCharCode(102,114,101,101,108,105,115,116,0);
       let diceB = String.fromCharCode(97,103,97,116,101,0);
         executorY -= parseFloat(`${parseInt(`${executorY}`) % (Math.max(libreactnativejniT.length, 9))}`);
      let downD = libreactnativejniT == String.fromCharCode(108,108,57,121,101,51,117,112,0);
      do {
         libreactnativejniT += `${1 * parseInt(`${executorY}`)}`;
         if (downD) {
            break;
         }
      } while (downD && (binddatasZ != String.fromCharCode(48,0)));
      if (score7 == String.fromCharCode(72,0) || diceB != String.fromCharCode(110,0)) {
         diceB = "2";
      }
          let owngoale = String.fromCharCode(115,99,97,108,101,99,117,100,97,0);
         libreactnativejniT = `${parseInt(`${executorY}`) ^ 1}`;
         owngoale += "2";
      let mbjscommonf = main_cz == String.fromCharCode(115,101,103,101,121,100,118,111,107,101,0);
      do {
         main_cz = `${diceB.length}`;
         if (mbjscommonf) {
            break;
         }
      } while ((!binddatasZ.includes(main_cz)) && mbjscommonf);
         executorY -= parseFloat(`${libreactnativejniT.length * diceB.length}`);
         executorY *= parseFloat(`${libreactnativejniT.length & 3}`);
       let iconclosewhitebgy = String.fromCharCode(105,110,116,120,120,0);
          let cedbadcebfbfbfbcfecbci = String.fromCharCode(114,110,110,111,105,115,101,95,55,95,51,56,0);
         score7 += `${(String.fromCharCode(66,0) == binddatasZ ? libreactnativejniT.length : binddatasZ.length)}`;
         cedbadcebfbfbfbcfecbci += `${cedbadcebfbfbfbcfecbci.length % 1}`;
      if (5.12 <= executorY) {
         libreactnativejniT = `${diceB.length}`;
      }
      areaw %= Math.max(parseInt(`${matchb}`) ^ 3, 5);
   }
        setEditing(false);
    }

    const renderItem = useCallback(({
        item,
        index,
    }: {
        item: mayi_Libswscale,
        index: number,
    }) => {
        const date = new Date(item.createAt ?? '');
        const dateStr = `${date.getDate().toString().padStart(2, '0')}-${(date.getMonth() + 1).toString().padStart(2, '0')}-${date.getFullYear().toString().padStart(2, '0')}`;

        return <View style={styles.itemContainer}>
            {isEditing && (
                <TouchableOpacity
                    style={styles.checkbox}
                    onPress={() => onSelectVideo(item.id ?? -1)}
                >
                    {videoSelected.includes(item.id ?? -1) ? (
                        <CheckBoxSelected
                            height={icons.sizes.m}
                            width={icons.sizes.m}
                            color={colors.primary}
                            fill={'white'}
                        />
                    ) : (
                        <CheckBoxUnselected
                            height={icons.sizes.m}
                            width={icons.sizes.m}
                        />
                    )}
                </TouchableOpacity>
            )}

            {item.thumbnail && item.thumbnail !== '' && false
                ? <FastImage
                    useFastImage={true}
                    style={styles.thumbnail}
                    resizeMode={"contain"}
                    source={{
                        uri: item.thumbnail,
                    }}
                />
                : <DefaultVideoThumbnail style={styles.thumbnail} />

            }

            <View style={styles.descriptionContainer}>
                <Text style={styles.titleText}>{item.title}</Text>

                <View style={styles.metaDataContainer}>
                    <Text style={styles.timeText}>
                        {dateStr}
                    </Text>
                    <Text style={styles.statusText}>
                        {item.status === 'pending'
                            ? '审核中'
                            : ''}
                    </Text>
                </View>
            </View>
        </View>
    }, [isEditing, videoSelected]);

    return (
        <ScreenContainer>
            <TitleWithBackButtonHeader
                title="上传记录"
                right={
                    <TouchableOpacity onPress={onEditPress}>
                        {isEditing
                            ? <Text style={styles.cancelEditTxt}>取消</Text>
                            : <Text style={styles.editTxt}>编辑</Text>
                        }

                    </TouchableOpacity>
                }
            />

            {uploadState.uploadHistory.length > 0
                ? <FlatList
                    data={uploadState.uploadHistory}
                    renderItem={renderItem}
                    style={{
                        paddingVertical: 10,
                    }}
                />
                : <EmptyList description="暂无上传成功视频" />
            }

            {isEditing && (
                <View style={styles.deleteConfirmationModal}>
                    <Button
                        onPress={() => {
                            if (
                                videoSelected.length === 0 ||
                                videoSelected.length !== uploadState.uploadHistory.length
                            ) {
                                setVideoSelected(uploadState.uploadHistory.map(v => v.id ?? -1));
                            } else {
                                setVideoSelected([]);
                            }
                        }}
                        containerStyle={styles.confirmationBtn}
                        color={colors.card2}
                        titleStyle={{ ...textVariants.body, color: colors.muted }}
                    >
                        {videoSelected.length === 0 ||
                            videoSelected.length !== uploadState.uploadHistory.length
                            ? "全选"
                            : "取消全选"}
                    </Button>
                    <Button
                        onPress={onRemovePress}
                        containerStyle={styles.confirmationBtn}
                        color={videoSelected.length === 0 ? colors.card2 : colors.primary}
                        titleStyle={{
                            ...textVariants.body,
                            color:
                                videoSelected.length === 0 ? colors.muted : colors.background,
                        }}
                    >
                        删除
                    </Button>
                </View>
            )}

            {isEditing &&
                <ConfirmationModal
                    onConfirm={onConfirmRemove}
                    onCancel={() => setShowConfirmDialog(false)}
                    isVisible={showConfirmDialog}
                    title="清除提示"
                    subtitle="您是否确定清除？"
                />
            }
        </ScreenContainer>
    );
}

const createStyles = ({ colors, textVariants }: any) => StyleSheet.create({
    editTxt: {
        ...textVariants.body,
        color: colors.primary,
    },
    cancelEditTxt: {
        ...textVariants.body,
    },
    itemContainer: {
        flexDirection: 'row',
        width: '100%',
        paddingVertical: 14,
    },
    thumbnail: {
        width: 140,
        aspectRatio: 16 / 9,
        marginRight: 14,
        borderRadius: 10,
    },
    descriptionContainer: {
        flex: 1,
        paddingBottom: 4,
    },
    titleText: {
        ...textVariants.body,
        flex: 1,
    },
    metaDataContainer: {
        flexDirection: 'row',
        justifyContent: 'space-between',
        alignItems: 'flex-end',
    },
    timeText: {
        ...textVariants.subbody,
        color: colors.muted,
    },
    statusText: {
        ...textVariants.body,
        
        color: colors.confirm,
    },
    checkbox: {
        padding: 5,
        marginRight: 10,
        alignSelf: 'center',
    },
    deleteConfirmationModal: {
        display: "flex",
        flexDirection: "row",
        alignItems: "center",
        justifyContent: "space-between",
        padding: 8,
    },
    confirmationBtn: {
        flex: 1,
        margin: 10,
        borderRadius: 10,
        marginTop: 8,
    },
});
