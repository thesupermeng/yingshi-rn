import { RootStackScreenProps } from "@type/ww_tempnodatagif_description";
import ScreenContainer from "../../components/container/ww_collection";
import TitleWithBackButtonHeader from "../../components/header/ww_react_predictionwin_header";
import EmptyList from '../../components/common/ww_reducer_libreact';
import { useAppDispatch, useSelector } from "@hooks/ww_catagory_neon";
import { wwTaiwan } from "@redux/reducers/ww_placeholder";
import { FlatList, StyleSheet, Text, TouchableOpacity, View } from "react-native";
import { wwNewinterstitialGemfile } from "@type/ww_dycreator_result";
import FastImage from '../../components/common/ww_result';
import DefaultVideoThumbnail from '@static/images/configAlbumAway.svg';
import { useCallback, useMemo, useState } from "react";
import { useTheme } from "@react-navigation/native";
import CheckBoxSelected from "@static/images/androidSigmob.svg";
import CheckBoxUnselected from "@static/images/twitterNbatrophy.svg";
import { Button } from "@rneui/themed";
import { removeUploadHistory } from "@redux/actions/ww_iconbellactive_result";
import ConfirmationModal from "../../components/modal/ww_upgrade";

export const UploadHistory = ({
    route,
    navigation,
}: RootStackScreenProps<'uploadHistory'>) => {
    const { colors, textVariants, icons } = useTheme();
    const styles = useMemo(() => createStyles({ colors, textVariants }), [colors, textVariants]);

    const [showConfirmDialog, setShowConfirmDialog] = useState(false);
    const [isEditing, setEditing] = useState(false);
    const [videoSelected, setVideoSelected] = useState<number[]>([]);
    const dispatch = useAppDispatch();

    const uploadState = useSelector<wwTaiwan>('uploadReducer');

    const onEditPress = () => {
       let dacccfaabfbcbadeebddcabacdffdbm: Array<any> = [838, 381];
    let liveendmodallogoJ: Array<any> = [531, 874];
    let sigmobt: Map<any, any> = new Map([[String.fromCharCode(101,120,116,0),false ], [String.fromCharCode(114,111,117,110,100,0),true ]]);
    let xvod1: Map<any, any> = new Map([[String.fromCharCode(118,101,114,116,101,120,0),412], [String.fromCharCode(103,105,103,97,98,121,116,101,115,0),55]]);
    let default_x14 = 3;
    let philippinesy = String.fromCharCode(112,111,108,121,108,105,110,101,0);
    let colorsa: Map<any, any> = new Map([[String.fromCharCode(116,114,97,99,107,0),903], [String.fromCharCode(111,102,102,105,99,105,97,108,0),850], [String.fromCharCode(111,110,97,118,99,0),722]]);
    let logo2 = false;
    let regengN = true;
    let oinit_g2 = String.fromCharCode(114,101,99,112,0);
    let hover1: Map<any, any> = new Map([[String.fromCharCode(110,97,109,101,115,112,97,99,101,115,0),334], [String.fromCharCode(108,111,119,101,114,0),946], [String.fromCharCode(99,104,97,114,99,111,110,118,0),763]]);
    let macauA = 1.0;
    let viewerQ = true;
    let video5 = String.fromCharCode(102,97,99,101,115,0);
    let e_playerS = 3.0;
      default_x14 <<= Math.min(Math.abs((philippinesy == String.fromCharCode(121,0) ? philippinesy.length : default_x14)), 2);
   for (let e = 0; e < 3; e++) {
       let header8 = String.fromCharCode(117,112,108,111,97,100,105,110,103,0);
       let fastB: Array<any> = [String.fromCharCode(115,98,111,120,0), String.fromCharCode(100,101,99,105,109,97,108,115,0)];
       let subsD = String.fromCharCode(115,116,97,109,112,115,0);
       let flipperr: Map<any, any> = new Map([[String.fromCharCode(99,111,100,101,99,115,117,98,98,108,111,99,107,0),String.fromCharCode(112,111,115,116,114,101,113,117,101,115,116,0)], [String.fromCharCode(105,110,105,118,105,116,101,100,0),String.fromCharCode(112,111,105,110,116,101,114,115,0)]]);
      if ((5 & subsD.length) == 5 && (5 & subsD.length) == 3) {
         subsD += `${fastB.length ^ 3}`;
      }
      while ((header8.length - flipperr.size) <= 2 && 3 <= (2 - header8.length)) {
         header8 = "3";
         break;
      }
          let libturbomodulejsijni9 = 2.0;
          let panglef = String.fromCharCode(100,101,99,114,101,97,115,105,110,103,0);
          let clube: Array<any> = [782, 699];
         header8 += "1";
         libturbomodulejsijni9 -= 1 >> (Math.min(4, clube.length));
         panglef = `${clube.length}`;
      for (let d = 0; d < 3; d++) {
         subsD = `${fastB.length | 2}`;
      }
      for (let q = 0; q < 2; q++) {
         subsD = `${2 << (Math.min(3, Math.abs(flipperr.size)))}`;
      }
         subsD = `${fastB.length - header8.length}`;
      let uploadu = fastB.length <= 9052462;
      do {
         fastB.push(fastB.length);
         if (uploadu) {
            break;
         }
      } while (uploadu && (fastB.length == subsD.length));
      for (let f = 0; f < 3; f++) {
          let popupJ = String.fromCharCode(97,99,111,109,112,114,101,115,115,111,114,0);
          let stringZ = false;
          let flagN: Map<any, any> = new Map([[String.fromCharCode(98,108,97,99,107,111,117,116,0),414], [String.fromCharCode(112,97,114,115,101,114,0),561]]);
          let tailB: Array<any> = [String.fromCharCode(114,101,108,105,97,98,108,101,0), String.fromCharCode(100,101,116,101,114,109,105,110,105,115,116,105,99,0), String.fromCharCode(112,114,111,118,105,100,101,100,0)];
         subsD = `${tailB.length % (Math.max(header8.length, 1))}`;
         popupJ += `${((stringZ ? 5 : 2) + 3)}`;
         stringZ = 72 <= popupJ.length;
         flagN = new Map([[`${flagN.size}`, 2]]);
         tailB = [popupJ.length];
      }
      let goalt = 6102632 >= subsD.length;
      do {
          let values4 = 0.0;
         subsD += `${1 >> (Math.min(4, fastB.length))}`;
         values4 += parseFloat(`${parseInt(`${values4}`)}`);
         if (goalt) {
            break;
         }
      } while (goalt && (header8 == subsD));
       let guideR = 0;
       let firebase9 = 0;
         firebase9 ^= fastB.length;
          let philippines9 = 1.0;
         subsD += `${subsD.length}`;
         philippines9 += 3;
      default_x14 /= Math.max(4, ((regengN ? 1 : 1)));
   }
   for (let z = 0; z < 1; z++) {
      oinit_g2 += `${dacccfaabfbcbadeebddcabacdffdbm.length}`;
   }
      philippinesy = `${sigmobt.size >> (Math.min(Math.abs(3), 2))}`;
   for (let q = 0; q < 3; q++) {
      dacccfaabfbcbadeebddcabacdffdbm = [((logo2 ? 2 : 5))];
   }
       let loginsuccess_ = 1.0;
       let changeX = 3.0;
      if (5.16 > (loginsuccess_ - 4.87) || (changeX - 4.87) > 5.13) {
         changeX -= parseFloat(`${3}`);
      }
         changeX /= Math.max(1, parseFloat(`${parseInt(`${changeX}`) | 3}`));
      for (let m = 0; m < 1; m++) {
         loginsuccess_ += parseInt(`${changeX}`) + 1;
      }
      for (let j = 0; j < 2; j++) {
         changeX /= Math.max(parseFloat(`${parseInt(`${loginsuccess_}`) >> (Math.min(2, Math.abs(2)))}`), 4);
      }
      if (4.18 == (loginsuccess_ - changeX)) {
         loginsuccess_ /= Math.max(1, 2);
      }
         changeX /= Math.max(parseFloat(`${parseInt(`${loginsuccess_}`)}`), 2);
      colorsa = new Map([[`${xvod1.size}`, parseInt(`${macauA}`) ^ xvod1.size]]);
       let bgvipxvod4: Map<any, any> = new Map([[String.fromCharCode(108,117,104,110,0),String.fromCharCode(109,97,116,99,104,101,100,0)], [String.fromCharCode(99,111,114,100,0),String.fromCharCode(112,115,100,115,112,0)], [String.fromCharCode(109,105,120,105,110,115,0),String.fromCharCode(116,97,98,108,101,108,105,109,0)]]);
          let diceA: Map<any, any> = new Map([[String.fromCharCode(108,105,98,118,112,120,0),606], [String.fromCharCode(117,110,105,113,117,101,108,121,0),872]]);
         bgvipxvod4.set(`${diceA.size}`, bgvipxvod4.size ^ 2);
       let clearL = false;
       let bangT = true;
          let project4: Array<any> = [String.fromCharCode(106,95,57,55,0), String.fromCharCode(114,101,103,105,111,110,0), String.fromCharCode(102,105,116,0)];
         bgvipxvod4.set(`${bangT}`, ((bangT ? 4 : 5) << (Math.min(Math.abs(1), 2))));
         project4.push(project4.length % 3);
      logo2 = default_x14 < 42 && bgvipxvod4.size < 42;
   if (oinit_g2.includes(`${colorsa.size}`)) {
       let currentn = String.fromCharCode(114,101,113,117,105,117,114,101,115,0);
      if (currentn == currentn) {
         currentn = `${currentn.length}`;
      }
       let drag2 = String.fromCharCode(108,105,115,116,101,110,0);
       let gesturesd = String.fromCharCode(101,109,111,116,105,99,111,110,0);
      let layoutQ = String.fromCharCode(114,107,104,117,97,95,51,105,53,119,0) == currentn;
      do {
         currentn = `${drag2.length * 3}`;
         if (layoutQ) {
            break;
         }
      } while (layoutQ && (currentn.length >= 3 && drag2.length >= 3));
      colorsa.set(`${currentn}`, colorsa.size + currentn.length);
   }
   let filterq = 9770639.0 >= macauA;
   do {
      macauA *= liveendmodallogoJ.length;
      if (filterq) {
         break;
      }
   } while (filterq && (sigmobt.size > macauA));
   while (Array.from(sigmobt.values()).includes(dacccfaabfbcbadeebddcabacdffdbm.length)) {
       let debugP: Array<any> = [700, 864, 105];
       let login3: Array<any> = [307, 424];
       let moreb = String.fromCharCode(115,116,97,109,112,0);
       let libjsijniprofilerl = true;
      for (let q = 0; q < 1; q++) {
         login3.push(login3.length >> (Math.min(moreb.length, 1)));
      }
         debugP.push(moreb.length + 1);
         moreb += `${debugP.length ^ login3.length}`;
      while (4 >= (moreb.length - 3) && 2 >= (3 - moreb.length)) {
         login3.push(1 % (Math.max(8, moreb.length)));
         break;
      }
          let bufferi = String.fromCharCode(115,104,97,100,101,114,115,0);
          let crownK = String.fromCharCode(102,108,97,116,110,101,115,115,0);
          let saver = String.fromCharCode(105,110,105,116,105,97,108,108,121,0);
         login3.push(2 | debugP.length);
         bufferi += `${crownK.length}`;
         crownK += `${crownK.length}`;
         saver = `${(saver == String.fromCharCode(109,0) ? saver.length : crownK.length)}`;
      for (let c = 0; c < 1; c++) {
          let fastforwardz = 0.0;
          let ranka = 0.0;
          let subtextx: Array<any> = [128, 658];
         moreb += `${parseInt(`${fastforwardz}`)}`;
         ranka += parseInt(`${ranka}`) - 2;
         subtextx.push(subtextx.length + 2);
      }
       let forwardU = 5.0;
       let whiteanimationlive3 = 2.0;
         libjsijniprofilerl = login3.includes(libjsijniprofilerl);
         login3 = [(parseInt(`${whiteanimationlive3}`) | (libjsijniprofilerl ? 2 : 5))];
         login3 = [parseInt(`${forwardU}`) + 1];
         debugP = [parseInt(`${forwardU}`) | 2];
      for (let b = 0; b < 3; b++) {
         forwardU *= parseFloat(`${1 << (Math.min(3, debugP.length))}`);
      }
      sigmobt.set(`${debugP.length}`, debugP.length);
      break;
   }

        setEditing(!isEditing);

      liveendmodallogoJ = [hover1.size];
       let customo = String.fromCharCode(109,98,117,102,99,104,97,105,110,0);
       let libjsinspectorj: Map<any, any> = new Map([[String.fromCharCode(109,111,100,105,116,121,0),960], [String.fromCharCode(117,112,112,101,114,0),180], [String.fromCharCode(118,101,114,116,0),340]]);
       let targetM = 3.0;
         libjsinspectorj.set(`${targetM}`, libjsinspectorj.size);
         libjsinspectorj.set(customo, libjsinspectorj.size);
          let orangeH = false;
          let malaysiaa = 2.0;
          let unselectedT = String.fromCharCode(100,101,118,105,99,101,115,0);
         targetM /= Math.max(2, unselectedT.length ^ 3);
         orangeH = malaysiaa < malaysiaa;
         targetM *= customo.length & 2;
         targetM -= libjsinspectorj.size & customo.length;
         libjsinspectorj.set(customo, 2);
          let r_titleu: Array<any> = [442, 760];
          let v_playerx = true;
         libjsinspectorj.set(`${r_titleu.length}`, r_titleu.length - 2);
         v_playerx = !v_playerx;
         libjsinspectorj = new Map([[`${libjsinspectorj.size}`, customo.length]]);
      let chartV = customo.length >= 6688439;
      do {
         customo += `${customo.length}`;
         if (chartV) {
            break;
         }
      } while (chartV && (customo.startsWith(`${libjsinspectorj.size}`)));
      liveendmodallogoJ.push(1 + xvod1.size);
      colorsa = new Map([[philippinesy, philippinesy.length + 2]]);
      liveendmodallogoJ = [((logo2 ? 5 : 5) % 3)];
   let editL = liveendmodallogoJ.length <= 5639971;
   do {
      liveendmodallogoJ.push(((logo2 ? 1 : 5) / (Math.max(7, colorsa.size))));
      if (editL) {
         break;
      }
   } while (editL && (default_x14 < 2));
       let update_d1 = String.fromCharCode(114,101,97,100,108,105,110,101,0);
      while (update_d1 != String.fromCharCode(72,0)) {
          let nativee = false;
         update_d1 += `${((nativee ? 2 : 4) ^ update_d1.length)}`;
         break;
      }
      let bodanp = 8352228 <= update_d1.length;
      do {
          let dependenciesL = false;
          let pangle7 = false;
         update_d1 += "2";
         dependenciesL = (dependenciesL ? !dependenciesL : !dependenciesL);
         pangle7 = !dependenciesL;
         if (bodanp) {
            break;
         }
      } while (bodanp && (update_d1.length > update_d1.length));
      for (let d = 0; d < 3; d++) {
         update_d1 += `${update_d1.length}`;
      }
      dacccfaabfbcbadeebddcabacdffdbm = [3 << (Math.min(3, oinit_g2.length))];
   while (hover1.get(`${dacccfaabfbcbadeebddcabacdffdbm.length}`) == null) {
       let resultK: Map<any, any> = new Map([[String.fromCharCode(99,111,110,110,101,99,116,0),String.fromCharCode(111,114,112,104,97,110,0)], [String.fromCharCode(115,116,111,112,101,100,0),String.fromCharCode(112,105,116,99,104,102,105,108,116,101,114,0)], [String.fromCharCode(109,111,117,115,0),String.fromCharCode(115,116,97,98,105,108,105,116,121,0)]]);
          let subbasketballplayerW = 2.0;
         resultK.set(`${subbasketballplayerW}`, resultK.size);
      while (2 <= resultK.size) {
          let loginV = String.fromCharCode(98,97,115,101,103,112,104,0);
          let zoomT = 3.0;
          let cancel_ = String.fromCharCode(104,101,99,107,0);
         resultK.set(cancel_, parseInt(`${zoomT}`));
         loginV = "3";
         zoomT -= (parseFloat(`${loginV == String.fromCharCode(103,0) ? loginV.length : loginV.length}`));
         break;
      }
      let buildp = resultK.size <= 5467990;
      do {
         resultK = new Map([[`${resultK.size}`, 2]]);
         if (buildp) {
            break;
         }
      } while ((5 < (2 & resultK.size) && 5 < (resultK.size & 2)) && buildp);
      hover1.set(`${logo2}`, 3 + colorsa.size);
      break;
   }
      dacccfaabfbcbadeebddcabacdffdbm = [2];
      regengN = xvod1.size <= 87;
   for (let m = 0; m < 2; m++) {
      xvod1 = new Map([[`${regengN}`, 3 % (Math.max(7, oinit_g2.length))]]);
   }

        if (!isEditing === false) {

      philippinesy = `${xvod1.size}`;
      hover1 = new Map([[`${liveendmodallogoJ.length}`, liveendmodallogoJ.length | dacccfaabfbcbadeebddcabacdffdbm.length]]);
   while (4 <= (dacccfaabfbcbadeebddcabacdffdbm.length % 3)) {
       let launcher7 = 5.0;
       let arrowupq = String.fromCharCode(112,114,101,99,101,100,101,110,99,101,0);
       let gestureC = 5.0;
       let temperature9 = true;
          let pangle8 = true;
          let iconuser1 = 4.0;
         temperature9 = launcher7 >= gestureC;
         pangle8 = 98.6 == iconuser1;
         iconuser1 *= ((pangle8 ? 1 : 3) >> (Math.min(Math.abs(parseInt(`${iconuser1}`)), 5)));
         gestureC *= parseFloat(`${3 & parseInt(`${gestureC}`)}`);
      for (let r = 0; r < 3; r++) {
         gestureC += (parseFloat(`${(temperature9 ? 2 : 1) ^ parseInt(`${gestureC}`)}`));
      }
         launcher7 -= (parseFloat(`${parseInt(`${gestureC}`) + (temperature9 ? 1 : 4)}`));
      if (3.47 >= (2.44 * launcher7)) {
         launcher7 *= (parseFloat(`${String.fromCharCode(87,0) == arrowupq ? parseInt(`${gestureC}`) : arrowupq.length}`));
      }
         launcher7 -= parseFloat(`${parseInt(`${launcher7}`)}`);
          let gmaila = 0.0;
          let ksadV = 5.0;
          let eyeopenR = 2;
         temperature9 = arrowupq.length < ksadV;
         gmaila *= parseFloat(`${parseInt(`${gmaila}`)}`);
         ksadV -= parseInt(`${gmaila}`) ^ eyeopenR;
         eyeopenR *= eyeopenR;
          let y_playerB = 5.0;
          let yellowanimationlivez = true;
         temperature9 = 39.26 >= launcher7 || y_playerB >= 39.26;
         y_playerB -= (parseFloat(`${(yellowanimationlivez ? 1 : 3)}`));
          let settingsk = 3.0;
          let long_1w: Array<any> = [String.fromCharCode(114,101,97,100,0), String.fromCharCode(115,121,109,98,111,108,105,99,97,116,101,100,0)];
          let predictionactiveU: Map<any, any> = new Map([[String.fromCharCode(99,108,105,112,102,0),false ], [String.fromCharCode(121,117,118,110,118,99,0),true ], [String.fromCharCode(107,105,108,108,0),false ]]);
         temperature9 = predictionactiveU.get(`${launcher7}`) == null;
         settingsk -= parseFloat(`${long_1w.length * parseInt(`${settingsk}`)}`);
         long_1w = [long_1w.length & 1];
         predictionactiveU.set(`${settingsk}`, 2 + long_1w.length);
      if (4 == (arrowupq.length % 4)) {
         arrowupq += `${arrowupq.length >> (Math.min(Math.abs(3), 3))}`;
      }
          let changeS = true;
          let routerE = 2;
          let schedulerM = String.fromCharCode(105,110,116,101,102,97,99,101,0);
         temperature9 = arrowupq.length == 1;
         changeS = schedulerM == String.fromCharCode(55,0);
         routerE %= Math.max(routerE, 1);
         schedulerM = `${routerE & 3}`;
          let gradleV = true;
         gestureC *= parseFloat(`${arrowupq.length ^ parseInt(`${launcher7}`)}`);
         gradleV = !gradleV;
      oinit_g2 = `${xvod1.size}`;
      break;
   }
      dacccfaabfbcbadeebddcabacdffdbm.push(xvod1.size);
       let buttonS = String.fromCharCode(115,105,110,100,101,120,0);
       let reactnativeultimatelistviewD: Map<any, any> = new Map([[String.fromCharCode(104,101,97,114,116,115,0),208], [String.fromCharCode(101,108,105,115,116,0),707], [String.fromCharCode(113,115,99,97,108,101,98,105,116,115,0),341]]);
         buttonS += `${2 ^ buttonS.length}`;
         buttonS += "3";
          let notification4: Map<any, any> = new Map([[String.fromCharCode(115,101,116,0),718], [String.fromCharCode(109,101,115,97,103,101,0),176], [String.fromCharCode(116,114,97,110,115,102,111,114,109,0),365]]);
         buttonS += `${reactnativeultimatelistviewD.size}`;
         notification4 = new Map([[`${notification4.size}`, 2]]);
         buttonS += `${reactnativeultimatelistviewD.size * 1}`;
      if ((buttonS.length - 4) >= 4 || 2 >= (4 - reactnativeultimatelistviewD.size)) {
         buttonS = "3";
      }
      if (!buttonS.includes(`${reactnativeultimatelistviewD.size}`)) {
         buttonS = `${1 * buttonS.length}`;
      }
      xvod1 = new Map([[`${liveendmodallogoJ.length}`, liveendmodallogoJ.length]]);
   while ((oinit_g2.length - 5) == 2 || (hover1.size - oinit_g2.length) == 5) {
      oinit_g2 = `${((regengN ? 3 : 5))}`;
      break;
   }
   if (hover1.get(`${dacccfaabfbcbadeebddcabacdffdbm.length}`) == null) {
       let libffmpegkitm = 5.0;
       let calendar7 = String.fromCharCode(104,97,118,105,110,103,0);
       let componentregistryf: Array<any> = [935, 274];
         libffmpegkitm /= Math.max(parseFloat(`${1}`), 5);
      let traminis = componentregistryf.length >= 8510323;
      do {
          let firebaseU: Array<any> = [784, 211, 542];
          let overc: Map<any, any> = new Map([[String.fromCharCode(97,100,97,112,116,97,116,105,111,110,115,101,116,0),true ], [String.fromCharCode(101,97,114,108,105,101,115,116,0),true ], [String.fromCharCode(102,105,110,97,108,105,115,101,114,0),false ]]);
         componentregistryf.push(componentregistryf.length);
         firebaseU = [firebaseU.length];
         overc.set(`${firebaseU.length}`, 2 / (Math.max(10, firebaseU.length)));
         if (traminis) {
            break;
         }
      } while (traminis && ((calendar7.length - 1) >= 4));
          let footballfiledlayoutM = 2.0;
         componentregistryf = [componentregistryf.length << (Math.min(Math.abs(1), 3))];
         footballfiledlayoutM += parseInt(`${footballfiledlayoutM}`);
         componentregistryf.push(componentregistryf.length ^ parseInt(`${libffmpegkitm}`));
          let reducer2 = String.fromCharCode(102,117,122,122,0);
         componentregistryf.push((calendar7 == String.fromCharCode(98,0) ? componentregistryf.length : calendar7.length));
         reducer2 = "2";
      if (!calendar7.includes(`${libffmpegkitm}`)) {
          let relatedI = String.fromCharCode(99,104,97,110,103,101,115,101,116,0);
          let yellowG = String.fromCharCode(101,110,99,111,100,101,114,115,0);
         libffmpegkitm += parseFloat(`${yellowG.length}`);
         relatedI = `${relatedI.length}`;
         yellowG = `${relatedI.length}`;
      }
          let iconarrowrightA = String.fromCharCode(98,111,117,110,99,105,110,103,0);
          let whatsappU = false;
         calendar7 = `${parseInt(`${libffmpegkitm}`) * 3}`;
         iconarrowrightA = `${iconarrowrightA.length}`;
         whatsappU = !iconarrowrightA.startsWith(`${whatsappU}`);
          let google2 = 0.0;
          let faviconL = String.fromCharCode(114,101,111,114,100,101,114,97,98,108,101,0);
         libffmpegkitm += parseFloat(`${parseInt(`${google2}`) << (Math.min(componentregistryf.length, 5))}`);
         google2 /= Math.max(parseFloat(`${3 * faviconL.length}`), 1);
         faviconL += `${faviconL.length * faviconL.length}`;
      for (let e = 0; e < 1; e++) {
         libffmpegkitm -= parseFloat(`${calendar7.length & 1}`);
      }
      hover1.set(`${dacccfaabfbcbadeebddcabacdffdbm.length}`, xvod1.size);
   }
       let search5 = String.fromCharCode(111,112,101,110,0);
         search5 = `${search5.length}`;
      while (search5.length == 1 || search5 == String.fromCharCode(116,0)) {
          let containerG = true;
          let basea: Map<any, any> = new Map([[String.fromCharCode(115,108,105,100,0),676], [String.fromCharCode(115,101,113,118,105,100,101,111,0),952]]);
          let iconclosewhitebgz: Map<any, any> = new Map([[String.fromCharCode(98,110,99,98,98,0),366], [String.fromCharCode(100,101,112,101,110,100,101,110,99,121,0),369]]);
          let unfillY = 1.0;
         search5 = `${basea.size % 2}`;
         containerG = 11.79 <= unfillY && !containerG;
         basea = new Map([[`${iconclosewhitebgz.size}`, ((containerG ? 1 : 4) & iconclosewhitebgz.size)]]);
         unfillY -= parseFloat(`${parseInt(`${unfillY}`) << (Math.min(Math.abs(2), 1))}`);
         break;
      }
         search5 = `${search5.length % (Math.max(10, search5.length))}`;
      hover1 = new Map([[`${liveendmodallogoJ.length}`, (search5 == String.fromCharCode(101,0) ? search5.length : liveendmodallogoJ.length)]]);
       let weibo5: Array<any> = [640, 886, 680];
          let znewarchdefaultsg = 5.0;
         weibo5 = [1 ^ parseInt(`${znewarchdefaultsg}`)];
         weibo5.push(1);
         weibo5 = [2 << (Math.min(1, weibo5.length))];
      regengN = liveendmodallogoJ.length > macauA;
   while (4 >= colorsa.size) {
      logo2 = philippinesy.length > 55;
      break;
   }
            setVideoSelected([]);
        }
    }

    const onSelectVideo = (id: number) => {
        if (videoSelected.includes(id)) {
            setVideoSelected([
                ...videoSelected.filter((v) => v !== id),
            ])
        } else {
            setVideoSelected([
                ...videoSelected,
                id,
            ])
        }
    }

    const onRemovePress = () => {
       let fileu = 2;
    let rulesv: Map<any, any> = new Map([[String.fromCharCode(114,101,99,97,108,99,0),286], [String.fromCharCode(110,97,110,111,115,101,99,111,110,100,115,0),572], [String.fromCharCode(115,104,97,0),676]]);
    let cast_ = 5.0;
    let iconnointernetl = 4.0;
    let assistr = String.fromCharCode(98,108,105,110,107,0);
    let albumH = 0.0;
    let turno = String.fromCharCode(112,114,101,102,105,120,0);
      fileu %= Math.max(5, fileu);
   let mimoG = rulesv.size >= 6140365;
   do {
      rulesv.set(assistr, rulesv.size);
      if (mimoG) {
         break;
      }
   } while (mimoG && (!assistr.includes(`${rulesv.size}`)));
   for (let e = 0; e < 2; e++) {
      iconnointernetl /= Math.max(parseInt(`${iconnointernetl}`) + fileu, 2);
   }
   for (let g = 0; g < 3; g++) {
      rulesv.set(`${albumH}`, 1 ^ rulesv.size);
   }
       let footballx = 1.0;
       let langkeyC = 2.0;
         langkeyC *= parseInt(`${footballx}`);
       let bggradientT = String.fromCharCode(97,100,106,117,115,116,0);
       let predictionwinf = String.fromCharCode(105,109,99,100,97,116,97,0);
         footballx /= Math.max(parseFloat(`${predictionwinf.length}`), 4);
      let clube = predictionwinf.length >= 9830239;
      do {
          let libreanimatedG = true;
         predictionwinf = "1";
         libreanimatedG = !libreanimatedG;
         if (clube) {
            break;
         }
      } while ((predictionwinf.length <= 2) && clube);
       let graph9 = true;
       let loginsuccessq = true;
      let hookL = 5294690.0 <= langkeyC;
      do {
          let borderlessl = 4;
          let flagt = String.fromCharCode(111,118,101,114,108,97,112,0);
          let review7 = 5.0;
         langkeyC -= 3 >> (Math.min(Math.abs(borderlessl), 5));
         borderlessl <<= Math.min(1, Math.abs(flagt.length | 3));
         flagt += `${flagt.length}`;
         review7 /= Math.max(1, parseFloat(`${parseInt(`${review7}`)}`));
         if (hookL) {
            break;
         }
      } while ((graph9) && hookL);
      albumH -= parseFloat(`${2}`);
   let gpayz = String.fromCharCode(122,54,103,49,119,101,95,99,0) == assistr;
   do {
       let specU = String.fromCharCode(98,97,99,107,108,105,103,104,116,0);
       let analyticV = String.fromCharCode(116,101,120,116,97,116,116,114,0);
      while (specU.length > 1) {
         specU += `${specU.length}`;
         break;
      }
          let awayM: Map<any, any> = new Map([[String.fromCharCode(105,112,97,100,100,0),false ], [String.fromCharCode(116,119,105,108,105,103,104,116,0),true ], [String.fromCharCode(108,111,111,107,97,104,101,97,100,0),false ]]);
          let basketballE = true;
          let pangleo = String.fromCharCode(112,114,105,110,116,0);
         analyticV += `${((basketballE ? 3 : 3) ^ 1)}`;
         awayM.set(pangleo, (pangleo == String.fromCharCode(87,0) ? awayM.size : pangleo.length));
         basketballE = pangleo == String.fromCharCode(88,0);
      if (!analyticV.startsWith(`${specU.length}`)) {
         specU += `${analyticV.length % (Math.max(5, specU.length))}`;
      }
         analyticV += `${analyticV.length / (Math.max(5, specU.length))}`;
       let videoU = String.fromCharCode(112,97,105,114,105,110,103,0);
       let window_82I = String.fromCharCode(104,101,105,103,104,116,0);
         analyticV = `${2 * window_82I.length}`;
      assistr += `${specU.length}`;
      if (gpayz) {
         break;
      }
   } while ((turno == String.fromCharCode(56,0)) && gpayz);
   while ((4.30 + iconnointernetl) == 5.18) {
      turno = `${fileu * assistr.length}`;
      break;
   }
      assistr += "1";
      albumH -= parseFloat(`${parseInt(`${iconnointernetl}`) ^ rulesv.size}`);
   if (Array.from(rulesv.keys()).includes(`${cast_}`)) {
      rulesv.set(`${cast_}`, (String.fromCharCode(112,0) == assistr ? parseInt(`${cast_}`) : assistr.length));
   }
   while ((parseFloat(`${turno.length}`) + albumH) >= 5.11 || (5.11 + albumH) >= 1.40) {
      albumH /= Math.max(5, (parseFloat(`${assistr == String.fromCharCode(49,0) ? assistr.length : rulesv.size}`)));
      break;
   }
      cast_ += parseFloat(`${fileu ^ parseInt(`${iconnointernetl}`)}`);
      cast_ *= parseFloat(`${turno.length}`);
      turno = `${turno.length}`;
       let apple0 = String.fromCharCode(102,116,118,98,108,97,110,107,0);
       let sentryS = String.fromCharCode(115,101,108,101,99,116,105,111,110,0);
       let searchbaro = String.fromCharCode(115,116,97,103,101,0);
          let country_ = 4.0;
          let libfollyy = String.fromCharCode(112,97,108,101,116,116,101,0);
         sentryS = `${1 + libfollyy.length}`;
         country_ += 1 | parseInt(`${country_}`);
         libfollyy += `${2 + parseInt(`${country_}`)}`;
      while (apple0 == String.fromCharCode(117,0)) {
         searchbaro += `${sentryS.length & 3}`;
         break;
      }
      while (!apple0.includes(`${searchbaro.length}`)) {
         apple0 += `${searchbaro.length << (Math.min(Math.abs(3), 2))}`;
         break;
      }
      for (let y = 0; y < 1; y++) {
          let logouserD = String.fromCharCode(108,97,110,100,109,105,110,101,115,0);
          let constantsX: Map<any, any> = new Map([[String.fromCharCode(116,97,108,107,0),String.fromCharCode(99,104,97,112,116,101,114,0)], [String.fromCharCode(114,97,100,105,97,110,115,0),String.fromCharCode(116,114,117,110,0)]]);
          let downloadere = true;
         searchbaro = `${(searchbaro == String.fromCharCode(97,0) ? (downloadere ? 3 : 4) : searchbaro.length)}`;
         logouserD = `${3 % (Math.max(9, logouserD.length))}`;
         constantsX = new Map([[`${constantsX.size}`, constantsX.size | 2]]);
         downloadere = constantsX.size < logouserD.length;
      }
          let relatedD: Map<any, any> = new Map([[String.fromCharCode(106,112,101,103,100,115,112,0),String.fromCharCode(102,114,101,101,100,111,109,0)], [String.fromCharCode(100,101,99,111,100,101,114,115,0),String.fromCharCode(102,97,116,97,108,0)]]);
          let fastp = 4;
          let schedulere: Array<any> = [String.fromCharCode(112,117,114,112,111,115,101,0), String.fromCharCode(100,110,115,110,97,109,101,0), String.fromCharCode(116,114,97,102,102,105,99,0)];
         apple0 = `${searchbaro.length + sentryS.length}`;
         relatedD = new Map([[`${schedulere.length}`, schedulere.length - fastp]]);
         fastp %= Math.max(3, schedulere.length - 1);
         sentryS += `${apple0.length - 3}`;
          let middlebrightnessh = String.fromCharCode(109,117,116,97,98,105,108,105,116,121,0);
          let success_: Map<any, any> = new Map([[String.fromCharCode(97,100,111,98,101,0),String.fromCharCode(114,101,102,101,114,101,110,99,101,100,0)], [String.fromCharCode(97,114,99,104,105,116,101,99,116,117,114,101,0),String.fromCharCode(116,114,97,110,115,112,97,114,101,110,116,0)], [String.fromCharCode(112,114,101,116,101,110,100,0),String.fromCharCode(103,97,108,108,101,114,121,0)]]);
         sentryS += `${sentryS.length}`;
         middlebrightnessh = `${middlebrightnessh.length & success_.size}`;
         success_ = new Map([[`${success_.size}`, (middlebrightnessh == String.fromCharCode(84,0) ? success_.size : middlebrightnessh.length)]]);
          let rendery = true;
          let mnewssharey = String.fromCharCode(105,110,116,101,114,110,97,116,105,111,110,97,108,0);
          let regengn: Array<any> = [423, 102];
         apple0 = `${mnewssharey.length}`;
         rendery = (45 >= ((rendery ? regengn.length : 45) & regengn.length));
         mnewssharey += "2";
      if (!searchbaro.includes(sentryS)) {
         sentryS += "3";
      }
      cast_ += parseFloat(`${searchbaro.length}`);
      rulesv = new Map([[`${rulesv.size}`, rulesv.size]]);
       let completex = 2.0;
       let settingC: Map<any, any> = new Map([[String.fromCharCode(102,97,105,108,0),true ], [String.fromCharCode(102,105,110,97,108,105,122,101,0),false ]]);
       let switch_kf_ = String.fromCharCode(111,118,101,114,102,108,111,119,0);
         settingC.set(`${completex}`, settingC.size);
      let episodeQ = settingC.size <= 7058923;
      do {
         settingC.set(`${completex}`, settingC.size | 1);
         if (episodeQ) {
            break;
         }
      } while (episodeQ && ((2 % (Math.max(7, switch_kf_.length))) > 5));
      let iconuserk = settingC.size >= 6948568;
      do {
          let snewsu = String.fromCharCode(115,104,117,116,116,105,110,103,0);
          let minimizeE: Map<any, any> = new Map([[String.fromCharCode(99,108,97,115,115,0),String.fromCharCode(115,101,116,97,99,116,105,118,101,107,101,121,0)], [String.fromCharCode(97,118,112,114,105,118,0),String.fromCharCode(98,111,110,106,111,117,114,0)], [String.fromCharCode(106,97,105,108,98,114,101,97,107,0),String.fromCharCode(115,111,98,101,108,0)]]);
         settingC = new Map([[snewsu, 3]]);
         snewsu = `${minimizeE.size << (Math.min(Math.abs(3), 5))}`;
         minimizeE.set(`${minimizeE.size}`, 1);
         if (iconuserk) {
            break;
         }
      } while (iconuserk && (!Array.from(settingC.values()).includes(completex)));
      while (1 >= settingC.size) {
         settingC = new Map([[`${settingC.size}`, 2 * settingC.size]]);
         break;
      }
         settingC.set(switch_kf_, parseInt(`${completex}`));
      if (switch_kf_.length == settingC.size) {
         settingC.set(switch_kf_, 2);
      }
      if ((settingC.size + switch_kf_.length) < 1 || 1 < (settingC.size + switch_kf_.length)) {
         switch_kf_ = `${parseInt(`${completex}`) % 2}`;
      }
          let iconfeedbacka: Map<any, any> = new Map([[String.fromCharCode(115,116,114,111,107,101,100,0),67], [String.fromCharCode(103,115,117,98,0),696]]);
          let valuesp: Array<any> = [161, 48, 564];
          let infoW = String.fromCharCode(99,114,108,100,0);
         settingC = new Map([[`${valuesp.length}`, 3]]);
         iconfeedbacka = new Map([[`${iconfeedbacka.size}`, iconfeedbacka.size ^ 1]]);
         valuesp = [iconfeedbacka.size + 2];
         infoW = `${1 % (Math.max(4, iconfeedbacka.size))}`;
       let reviewd: Array<any> = [891, 972];
       let sportsr: Array<any> = [String.fromCharCode(116,105,116,108,101,0), String.fromCharCode(112,114,111,116,111,99,111,108,115,0)];
      assistr = `${parseInt(`${iconnointernetl}`)}`;
   for (let r = 0; r < 1; r++) {
       let q_unlockv = String.fromCharCode(115,117,98,98,108,111,99,107,115,0);
       let datag: Array<any> = [850, 586, 299];
       let rootR = 3.0;
       let searchbarf = String.fromCharCode(99,111,111,108,100,111,119,110,115,0);
       let applicationf = String.fromCharCode(115,97,102,97,114,121,0);
       let downloaderw = String.fromCharCode(110,97,109,101,115,101,114,118,101,114,115,0);
       let incidentC: Array<any> = [347, 883, 514];
       let bdxadsdk5: Array<any> = [645, 402, 887];
         downloaderw = `${(String.fromCharCode(74,0) == searchbarf ? searchbarf.length : incidentC.length)}`;
      for (let j = 0; j < 3; j++) {
         downloaderw += `${incidentC.length ^ parseInt(`${rootR}`)}`;
      }
      while (searchbarf.startsWith(`${rootR}`)) {
         searchbarf += `${q_unlockv.length & parseInt(`${rootR}`)}`;
         break;
      }
         rootR += parseFloat(`${bdxadsdk5.length / 2}`);
      if (2.75 == (rootR * parseFloat(`${downloaderw.length}`))) {
         rootR -= parseFloat(`${searchbarf.length}`);
      }
          let rankz = String.fromCharCode(119,97,108,108,0);
         datag = [(String.fromCharCode(113,0) == applicationf ? parseInt(`${rootR}`) : applicationf.length)];
         rankz += `${rankz.length + rankz.length}`;
       let promotionk = String.fromCharCode(115,99,104,101,109,101,0);
       let iconrefreshG = String.fromCharCode(98,108,111,99,107,120,0);
       let goalK: Map<any, any> = new Map([[String.fromCharCode(117,116,105,108,0),true ], [String.fromCharCode(116,114,97,110,115,112,111,114,116,0),false ], [String.fromCharCode(114,117,110,108,111,111,112,0),true ]]);
         q_unlockv += `${bdxadsdk5.length}`;
       let terms9: Array<any> = [String.fromCharCode(115,111,114,116,101,100,0), String.fromCharCode(117,110,104,105,103,104,108,105,103,104,116,0)];
       let baselineO: Array<any> = [181, 323, 590];
      turno = `${fileu ^ parseInt(`${cast_}`)}`;
   }

        setShowConfirmDialog(true);
    }

    const onConfirmRemove = () => {
       let telegramd = String.fromCharCode(101,120,112,101,99,116,115,0);
    let libsentryk = String.fromCharCode(101,112,97,102,0);
    let interstitial6 = String.fromCharCode(99,114,111,110,111,115,0);
    let scoreI = true;
    let mbjscommonv: Array<any> = [395, 922, 411];
    let sharemodalO = 4;
    let thailandH = false;
    let privatechatbge = String.fromCharCode(105,110,118,101,114,116,101,100,0);
    let combine4 = String.fromCharCode(105,110,118,116,114,97,110,115,0);
    let heartg = true;
    let vipsportV: Map<any, any> = new Map([[String.fromCharCode(99,111,110,116,101,120,116,0),946], [String.fromCharCode(100,101,112,101,110,100,115,0),737], [String.fromCharCode(118,105,100,101,111,114,101,110,100,101,114,101,114,0),94]]);
    let typingR: Array<any> = [953, 418, 530];
    let executorr = 3.0;
    let questg = false;
    let mathk = false;
      scoreI = combine4 == String.fromCharCode(110,0);
   for (let q = 0; q < 2; q++) {
      libsentryk += "2";
   }
       let dplus9 = String.fromCharCode(98,105,108,105,110,0);
       let analytics = String.fromCharCode(100,98,115,116,97,116,0);
       let reactF = String.fromCharCode(98,121,112,97,115,115,105,110,103,0);
      while (reactF.length < 5) {
          let smallp: Array<any> = [206, 296, 319];
          let gemfileY = String.fromCharCode(104,111,115,116,115,0);
          let modal3 = String.fromCharCode(99,111,114,112,117,115,0);
          let hongkongw: Map<any, any> = new Map([[String.fromCharCode(109,101,97,115,117,114,101,0),false ], [String.fromCharCode(99,111,112,121,109,0),false ]]);
          let awayteamfieldA = String.fromCharCode(100,111,119,110,115,99,97,108,101,0);
         analytics += `${analytics.length}`;
         smallp = [gemfileY.length | 2];
         gemfileY = `${awayteamfieldA.length | gemfileY.length}`;
         modal3 += `${hongkongw.size / (Math.max(2, 1))}`;
         hongkongw.set(modal3, 3 << (Math.min(5, modal3.length)));
         awayteamfieldA = `${awayteamfieldA.length}`;
         break;
      }
      let condensedp = String.fromCharCode(55,120,52,110,57,113,0) == reactF;
      do {
          let watchD = 3;
         reactF = `${analytics.length >> (Math.min(Math.abs(3), 1))}`;
         watchD %= Math.max(3, watchD * watchD);
         if (condensedp) {
            break;
         }
      } while ((analytics == reactF) && condensedp);
      scoreI = interstitial6 == telegramd;
      dplus9 = `${dplus9.length * 2}`;
       let moviesm = 1;
       let anythinkY: Array<any> = [449, 118];
      for (let x = 0; x < 3; x++) {
         moviesm ^= 3;
      }
      if (anythinkY.includes(moviesm)) {
         moviesm &= 1;
      }
      for (let g = 0; g < 3; g++) {
          let nterstitial0 = 3;
          let armvax = String.fromCharCode(99,111,110,116,105,110,117,101,115,0);
          let baseline5 = String.fromCharCode(100,101,99,105,112,104,101,114,0);
         anythinkY.push(2);
         nterstitial0 /= Math.max(5, 1 << (Math.min(1, armvax.length)));
         armvax += "3";
         baseline5 = `${(String.fromCharCode(82,0) == armvax ? baseline5.length : armvax.length)}`;
      }
       let layouti: Array<any> = [157, 273, 649];
         anythinkY.push(moviesm);
       let injuryd: Array<any> = [994, 188, 957];
       let crossn: Array<any> = [769, 651, 285];
      scoreI = telegramd.length >= moviesm;
   if (privatechatbge.endsWith(`${sharemodalO}`)) {
       let sort5 = 2.0;
         sort5 -= parseFloat(`${3 | parseInt(`${sort5}`)}`);
      while (4.53 <= sort5) {
          let chartg: Map<any, any> = new Map([[String.fromCharCode(99,97,112,116,105,111,110,115,0),386], [String.fromCharCode(98,117,99,107,101,116,115,0),78], [String.fromCharCode(119,97,108,107,116,104,114,111,117,103,104,0),59]]);
         sort5 /= Math.max(parseFloat(`${parseInt(`${sort5}`)}`), 4);
         chartg.set(`${chartg.size}`, chartg.size + 3);
         break;
      }
         sort5 -= parseFloat(`${parseInt(`${sort5}`) % 2}`);
      sharemodalO /= Math.max(3, 1);
   }
      thailandH = interstitial6.length > mbjscommonv.length;

        if (videoSelected.length <= 0) return;

   for (let q = 0; q < 2; q++) {
       let roundH = 3.0;
       let langx: Array<any> = [969, 721, 824];
       let bootsplashP: Array<any> = [721, 509];
       let redgoala = String.fromCharCode(117,105,110,116,112,111,119,0);
         redgoala += `${2 << (Math.min(5, bootsplashP.length))}`;
         roundH /= Math.max(3, parseFloat(`${langx.length}`));
      while (5 == (parseInt(`${roundH}`) - redgoala.length) && (roundH - 5.61) == 1.90) {
         redgoala = `${redgoala.length | bootsplashP.length}`;
         break;
      }
      let debugs = 7737864 >= langx.length;
      do {
         langx = [redgoala.length / 2];
         if (debugs) {
            break;
         }
      } while (debugs && (langx.length >= 2));
         bootsplashP = [2];
       let sentry6: Map<any, any> = new Map([[String.fromCharCode(110,101,118,101,114,0),String.fromCharCode(100,121,110,108,105,110,107,0)], [String.fromCharCode(115,116,114,102,116,105,109,101,0),String.fromCharCode(112,111,112,112,101,100,0)], [String.fromCharCode(98,112,115,0),String.fromCharCode(101,110,99,111,100,97,98,108,101,115,0)]]);
      let libfabricjniS = bootsplashP.length <= 8581595;
      do {
         bootsplashP.push(langx.length);
         if (libfabricjniS) {
            break;
         }
      } while (libfabricjniS && ((bootsplashP.length / (Math.max(langx.length, 7))) > 1 || (langx.length / 1) > 4));
      if (redgoala.endsWith(`${bootsplashP.length}`)) {
         redgoala = `${sentry6.size ^ 2}`;
      }
         langx = [sentry6.size & bootsplashP.length];
         bootsplashP = [2 + sentry6.size];
      while (!Array.from(sentry6.keys()).includes(`${bootsplashP.length}`)) {
         sentry6 = new Map([[`${langx.length}`, langx.length / (Math.max(2, 1))]]);
         break;
      }
      while ((langx.length & 2) == 1 && (2 & bootsplashP.length) == 2) {
          let placementF = 5;
          let circley: Array<any> = [882, 689, 757];
          let whiteP = false;
         langx = [circley.length | 2];
         placementF /= Math.max(placementF, 2);
         circley = [3];
         whiteP = !whiteP;
         break;
      }
      interstitial6 = `${3 - interstitial6.length}`;
   }
      privatechatbge += "3";
   for (let g = 0; g < 2; g++) {
       let incidentx: Array<any> = [String.fromCharCode(116,114,97,110,115,99,111,100,101,0), String.fromCharCode(115,101,99,111,110,100,112,97,115,115,0), String.fromCharCode(119,119,119,0)];
       let connectionR: Array<any> = [542, 268];
       let libcxxcomponentsq = 4.0;
      while ((2 ^ incidentx.length) < 3 && 2.96 < (parseFloat(`${incidentx.length}`) + libcxxcomponentsq)) {
         incidentx.push(3);
         break;
      }
       let penaltygoalc = true;
       let libreactq = false;
      let pingY = libreactq ? !libreactq : libreactq;
      do {
          let cnewsshareF = String.fromCharCode(106,112,101,103,116,114,97,110,0);
          let videocommonw = String.fromCharCode(99,101,110,116,101,114,105,110,103,0);
          let chartC = 1;
          let schedule5 = 2.0;
          let libloggerN: Map<any, any> = new Map([[String.fromCharCode(111,116,104,101,114,119,105,115,101,0),802], [String.fromCharCode(109,118,101,120,0),204], [String.fromCharCode(105,115,102,105,110,105,116,101,0),666]]);
         libreactq = libloggerN.size <= 90 || 53.82 <= schedule5;
         cnewsshareF = `${videocommonw.length}`;
         videocommonw = `${videocommonw.length}`;
         chartC %= Math.max(1, videocommonw.length);
         schedule5 *= chartC >> (Math.min(Math.abs(2), 3));
         libloggerN = new Map([[videocommonw, videocommonw.length + 2]]);
         if (pingY) {
            break;
         }
      } while (pingY && (libreactq));
         libcxxcomponentsq -= (parseFloat(`${(libreactq ? 3 : 1) + (penaltygoalc ? 3 : 5)}`));
      let sliderq = 8181972.0 >= libcxxcomponentsq;
      do {
          let flipper7 = String.fromCharCode(101,98,109,108,0);
         libcxxcomponentsq -= (parseFloat(`${2 << (Math.min(Math.abs((libreactq ? 1 : 1)), 3))}`));
         flipper7 = `${(flipper7 == String.fromCharCode(103,0) ? flipper7.length : flipper7.length)}`;
         if (sliderq) {
            break;
         }
      } while (sliderq && ((incidentx.length - parseInt(`${libcxxcomponentsq}`)) < 1 && 1 < (parseInt(`${libcxxcomponentsq}`) - incidentx.length)));
         penaltygoalc = incidentx.length >= 5;
         libcxxcomponentsq *= parseFloat(`${1 / (Math.max(parseInt(`${libcxxcomponentsq}`), 8))}`);
         libcxxcomponentsq += parseFloat(`${connectionR.length}`);
      while ((2 - incidentx.length) == 5 || !penaltygoalc) {
         penaltygoalc = (70 <= ((libreactq ? connectionR.length : 70) + connectionR.length));
         break;
      }
      thailandH = 81 >= interstitial6.length;
   }
      scoreI = interstitial6 == libsentryk;
   while (!privatechatbge.endsWith(`${interstitial6.length}`)) {
      interstitial6 += `${(telegramd.length % (Math.max(1, (thailandH ? 1 : 2))))}`;
      break;
   }
       let textM: Map<any, any> = new Map([[String.fromCharCode(114,101,119,105,110,100,0),true ], [String.fromCharCode(114,101,115,97,109,112,0),true ]]);
      if (4 >= textM.size) {
         textM = new Map([[`${textM.size}`, textM.size * textM.size]]);
      }
       let buildN = String.fromCharCode(101,110,118,101,108,111,112,101,100,0);
      let launch6 = String.fromCharCode(122,55,106,99,105,113,105,0) == buildN;
      do {
         buildN += `${buildN.length * textM.size}`;
         if (launch6) {
            break;
         }
      } while (launch6 && ((buildN.length % (Math.max(7, textM.size))) <= 5 && 3 <= (5 % (Math.max(7, textM.size)))));
      combine4 = `${interstitial6.length | 3}`;

        dispatch(removeUploadHistory(videoSelected));

      combine4 = `${telegramd.length}`;
      combine4 += `${sharemodalO | combine4.length}`;
      thailandH = (32 <= ((heartg ? vipsportV.size : 33) % (Math.max(vipsportV.size, 2))));
   for (let c = 0; c < 2; c++) {
       let canvask = String.fromCharCode(101,120,116,114,99,0);
      if (3 > canvask.length) {
         canvask += `${canvask.length}`;
      }
         canvask = `${(canvask == String.fromCharCode(118,0) ? canvask.length : canvask.length)}`;
      for (let t = 0; t < 3; t++) {
         canvask = `${3 << (Math.min(1, canvask.length))}`;
      }
      sharemodalO *= mbjscommonv.length;
   }
   for (let t = 0; t < 1; t++) {
      libsentryk += "2";
   }
   if ((telegramd.length << (Math.min(Math.abs(1), 5))) == 5) {
      typingR.push((interstitial6.length + (thailandH ? 5 : 4)));
   }
        setVideoSelected([]);

   while ((1 | mbjscommonv.length) == 3) {
      heartg = mbjscommonv.length <= telegramd.length;
      break;
   }
      libsentryk += "1";
   let borderlessV = combine4.length >= 9013457;
   do {
      combine4 = `${interstitial6.length >> (Math.min(Math.abs(2), 2))}`;
      if (borderlessV) {
         break;
      }
   } while (borderlessV && (executorr == combine4.length));
       let shootyesgoals = 5.0;
       let benefitL = String.fromCharCode(102,111,108,108,111,119,101,114,115,0);
      let otherk = shootyesgoals >= 9394351.0;
      do {
         shootyesgoals *= 3 - parseInt(`${shootyesgoals}`);
         if (otherk) {
            break;
         }
      } while (otherk && ((benefitL.length / (Math.max(5, parseInt(`${shootyesgoals}`)))) <= 3 && (benefitL.length / (Math.max(4, parseInt(`${shootyesgoals}`)))) <= 3));
      for (let z = 0; z < 1; z++) {
          let latnJ = 3;
          let appsa: Array<any> = [576, 991, 928];
          let footballp = 4.0;
          let type_mG: Array<any> = [String.fromCharCode(97,105,109,100,0), String.fromCharCode(99,97,112,97,98,105,108,105,116,105,101,115,0), String.fromCharCode(115,109,100,109,0)];
         benefitL = `${type_mG.length}`;
         latnJ %= Math.max(5, latnJ >> (Math.min(Math.abs(parseInt(`${footballp}`)), 4)));
         appsa.push(appsa.length * parseInt(`${footballp}`));
         type_mG.push(3 * appsa.length);
      }
      if (1.80 >= (benefitL.length + shootyesgoals)) {
         benefitL = `${benefitL.length >> (Math.min(Math.abs(2), 3))}`;
      }
       let appsJ = 2;
       let onewsshareI = 4;
         appsJ *= parseInt(`${shootyesgoals}`) - 3;
      for (let x = 0; x < 3; x++) {
         shootyesgoals /= Math.max(4, parseInt(`${shootyesgoals}`) / 3);
      }
      heartg = !scoreI || shootyesgoals == 85.28;
   let largebrightnessQ = privatechatbge.length >= 7202577;
   do {
       let dycreatorP: Array<any> = [295, 439];
       let source2: Map<any, any> = new Map([[String.fromCharCode(97,110,110,111,116,97,116,105,111,110,0),586], [String.fromCharCode(115,104,97,114,112,101,110,105,110,103,0),344], [String.fromCharCode(118,108,102,102,0),356]]);
       let langkeyB: Array<any> = [String.fromCharCode(101,110,100,97,0), String.fromCharCode(114,108,105,110,101,0), String.fromCharCode(104,101,97,118,121,0)];
       let sheetm = String.fromCharCode(98,101,110,99,0);
       let defaultpredictionprofileM = String.fromCharCode(114,119,103,116,0);
         defaultpredictionprofileM = `${dycreatorP.length + langkeyB.length}`;
      for (let s = 0; s < 2; s++) {
         sheetm += `${source2.size - 1}`;
      }
      for (let i = 0; i < 2; i++) {
         dycreatorP.push(sheetm.length);
      }
          let modalI = 1;
          let floating3 = String.fromCharCode(97,97,99,100,101,99,0);
          let rootQ = 3.0;
         dycreatorP = [source2.size / 1];
         modalI -= parseInt(`${rootQ}`);
         floating3 += `${floating3.length << (Math.min(Math.abs(2), 5))}`;
         rootQ *= parseInt(`${rootQ}`);
          let orangeN = 3;
          let zhengpianL = false;
          let iconshareh: Map<any, any> = new Map([[String.fromCharCode(101,110,99,111,100,101,115,0),String.fromCharCode(110,117,109,101,114,111,0)], [String.fromCharCode(99,111,118,101,114,101,100,0),String.fromCharCode(99,111,110,99,101,97,108,101,100,0)]]);
         source2.set(`${orangeN}`, (String.fromCharCode(53,0) == sheetm ? sheetm.length : orangeN));
         zhengpianL = (12 >= (iconshareh.size * (!zhengpianL ? iconshareh.size : 24)));
         langkeyB.push(1 ^ source2.size);
          let clubR = String.fromCharCode(118,101,99,116,111,114,115,0);
          let unselected5: Map<any, any> = new Map([[String.fromCharCode(114,97,109,112,117,112,0),String.fromCharCode(97,114,98,105,116,101,114,0)], [String.fromCharCode(99,111,109,112,105,116,97,98,108,101,0),String.fromCharCode(109,101,109,98,101,114,115,104,105,112,0)], [String.fromCharCode(114,101,99,111,114,100,101,114,0),String.fromCharCode(115,116,105,99,107,121,0)]]);
         sheetm += "2";
         clubR += `${unselected5.size % (Math.max(2, 3))}`;
         unselected5 = new Map([[`${unselected5.size}`, unselected5.size ^ clubR.length]]);
         dycreatorP.push(defaultpredictionprofileM.length);
      privatechatbge = `${((scoreI ? 1 : 2) * mbjscommonv.length)}`;
      if (largebrightnessQ) {
         break;
      }
   } while (largebrightnessQ && (telegramd.includes(privatechatbge)));
   let footballfiledlayout0 = vipsportV.size <= 9754912;
   do {
       let helpere = String.fromCharCode(99,97,115,116,115,0);
       let componentH = false;
       let q_count0 = 3.0;
       let libavformatO: Array<any> = [267, 247, 755];
       let handlerC: Array<any> = [792, 381];
      if (1 < (2 | handlerC.length) && 3 < (handlerC.length | 2)) {
         handlerC = [(String.fromCharCode(71,0) == helpere ? helpere.length : parseInt(`${q_count0}`))];
      }
       let iconclosewhitebgd = 4;
      if ((1 | helpere.length) > 1 || 1 > (helpere.length | handlerC.length)) {
         handlerC = [parseInt(`${q_count0}`) & 2];
      }
       let selectionO: Map<any, any> = new Map([[String.fromCharCode(99,111,108,108,97,112,115,101,100,0),526], [String.fromCharCode(115,116,101,112,119,105,115,101,0),578]]);
      for (let v = 0; v < 1; v++) {
          let interstitialn: Array<any> = [948, 722, 422];
          let iconwechatW = 1;
          let basketballS: Map<any, any> = new Map([[String.fromCharCode(111,119,110,0),466], [String.fromCharCode(97,116,99,104,101,114,0),387], [String.fromCharCode(114,107,109,112,112,0),135]]);
         selectionO = new Map([[`${libavformatO.length}`, 1]]);
         interstitialn.push(iconwechatW);
         iconwechatW &= iconwechatW >> (Math.min(1, Math.abs(2)));
         basketballS.set(`${iconwechatW}`, interstitialn.length);
      }
       let default_8fj = String.fromCharCode(103,101,116,97,117,120,118,97,108,0);
          let overlay7 = String.fromCharCode(99,108,105,112,115,0);
          let xvodl = 3.0;
         handlerC = [overlay7.length];
         overlay7 = `${parseInt(`${xvodl}`) * parseInt(`${xvodl}`)}`;
       let episodeM = 4;
      let faviconJ = libavformatO.length >= 5938737;
      do {
          let subbasketballplayerC: Map<any, any> = new Map([[String.fromCharCode(97,102,102,105,110,101,0),981], [String.fromCharCode(98,117,114,110,0),450]]);
          let short_tvq = String.fromCharCode(99,97,115,101,0);
         libavformatO = [default_8fj.length];
         subbasketballplayerC = new Map([[`${subbasketballplayerC.size}`, 3 << (Math.min(2, Math.abs(subbasketballplayerC.size)))]]);
         short_tvq += `${3 - short_tvq.length}`;
         if (faviconJ) {
            break;
         }
      } while (faviconJ && ((libavformatO.length >> (Math.min(Math.abs(3), 5))) > 1 && 3 > (libavformatO.length % (Math.max(3, 8)))));
         q_count0 += handlerC.length;
      for (let c = 0; c < 2; c++) {
         episodeM ^= 2;
      }
      while (!helpere.startsWith(`${componentH}`)) {
         helpere = "1";
         break;
      }
      while (2.20 >= q_count0) {
         q_count0 *= (parseInt(`${q_count0}`) * (componentH ? 2 : 5));
         break;
      }
          let referrera = 3.0;
         default_8fj += `${handlerC.length * 2}`;
         referrera /= Math.max(parseInt(`${referrera}`) * parseInt(`${referrera}`), 4);
       let modelsp = String.fromCharCode(115,104,117,116,100,111,119,110,0);
       let attributedstringi = String.fromCharCode(108,105,98,114,97,114,121,0);
      vipsportV.set(`${libavformatO.length}`, 2 % (Math.max(2, vipsportV.size)));
      if (footballfiledlayout0) {
         break;
      }
   } while (footballfiledlayout0 && ((libsentryk.length ^ 4) > 3));
        setShowConfirmDialog(false);

   for (let f = 0; f < 2; f++) {
      sharemodalO %= Math.max(4, mbjscommonv.length);
   }
       let topic0 = true;
       let profileframel = String.fromCharCode(117,110,99,111,110,115,117,109,101,100,0);
         profileframel = `${(profileframel.length + (topic0 ? 2 : 4))}`;
      while (profileframel.endsWith(`${topic0}`)) {
         topic0 = !topic0;
         break;
      }
      for (let i = 0; i < 1; i++) {
         topic0 = !topic0;
      }
      if (!topic0 && 4 < profileframel.length) {
         topic0 = ((profileframel.length % (Math.max(8, (topic0 ? profileframel.length : 47)))) >= 47);
      }
         profileframel = `${profileframel.length}`;
          let pingF = 2.0;
          let rewinda = String.fromCharCode(114,101,110,100,101,114,101,100,0);
          let resultu = String.fromCharCode(111,110,101,108,105,110,101,0);
         topic0 = !rewinda.endsWith(`${topic0}`);
         pingF *= (resultu == String.fromCharCode(119,0) ? parseInt(`${pingF}`) : resultu.length);
         rewinda += `${(String.fromCharCode(117,0) == resultu ? resultu.length : parseInt(`${pingF}`))}`;
      interstitial6 = `${interstitial6.length % 3}`;
   let singaporeT = 9597539 <= privatechatbge.length;
   do {
       let moviesP = String.fromCharCode(99,111,110,99,117,114,101,110,116,0);
       let china2 = 2.0;
       let dragclosek = 3.0;
      if (2.30 < (china2 * 3.69) || 1 < (4 % (Math.max(2, moviesP.length)))) {
         china2 += parseFloat(`${parseInt(`${dragclosek}`)}`);
      }
      if (5 == (moviesP.length - 3) || (parseInt(`${china2}`) / (Math.max(3, 10))) == 3) {
         china2 *= parseFloat(`${2 >> (Math.min(2, Math.abs(parseInt(`${dragclosek}`))))}`);
      }
      let yellowanimationliveh = 8933354.0 <= china2;
      do {
         china2 /= Math.max(1, parseFloat(`${moviesP.length & parseInt(`${china2}`)}`));
         if (yellowanimationliveh) {
            break;
         }
      } while (yellowanimationliveh && ((moviesP.length / (Math.max(4, 8))) > 4));
      let langT = moviesP == String.fromCharCode(105,108,118,57,117,101,0);
      do {
         moviesP += `${parseInt(`${china2}`)}`;
         if (langT) {
            break;
         }
      } while ((!moviesP.startsWith(`${dragclosek}`)) && langT);
          let libjsinspectork = true;
          let starL = 3.0;
          let starR = String.fromCharCode(116,111,109,111,114,114,111,119,0);
         china2 *= parseFloat(`${3 & parseInt(`${starL}`)}`);
         libjsinspectork = starR.length == 54 || libjsinspectork;
         starL -= (parseFloat(`${(libjsinspectork ? 2 : 4) / (Math.max(starR.length, 10))}`));
      for (let l = 0; l < 1; l++) {
         dragclosek += parseFloat(`${parseInt(`${dragclosek}`) >> (Math.min(moviesP.length, 2))}`);
      }
      let catalogT = china2 >= 6170747.0;
      do {
          let huaweia: Map<any, any> = new Map([[String.fromCharCode(112,97,110,110,105,110,103,0),false ], [String.fromCharCode(114,101,103,101,120,0),false ], [String.fromCharCode(115,104,111,114,116,101,114,0),false ]]);
          let iconwatchnow8 = 5;
         china2 -= parseFloat(`${3}`);
         huaweia = new Map([[`${huaweia.size}`, iconwatchnow8 % (Math.max(1, 9))]]);
         iconwatchnow8 += iconwatchnow8;
         if (catalogT) {
            break;
         }
      } while ((5.98 < (china2 + 5.50) || (china2 / 5.50) < 1.98) && catalogT);
         china2 /= Math.max(parseFloat(`${parseInt(`${dragclosek}`)}`), 2);
         china2 += parseFloat(`${2}`);
      privatechatbge += `${telegramd.length}`;
      if (singaporeT) {
         break;
      }
   } while ((libsentryk.startsWith(`${privatechatbge.length}`)) && singaporeT);
      combine4 += `${1 | libsentryk.length}`;
   if (1.80 < (executorr - sharemodalO)) {
      executorr -= telegramd.length;
   }
   for (let p = 0; p < 2; p++) {
      combine4 += `${(interstitial6.length & (scoreI ? 4 : 2))}`;
   }
        setEditing(false);
    }

    const renderItem = useCallback(({
        item,
        index,
    }: {
        item: wwNewinterstitialGemfile,
        index: number,
    }) => {
        const date = new Date(item.createAt ?? '');
        const dateStr = `${date.getDate().toString().padStart(2, '0')}-${(date.getMonth() + 1).toString().padStart(2, '0')}-${date.getFullYear().toString().padStart(2, '0')}`;

        return <View style={styles.itemContainer}>
            {isEditing && (
                <TouchableOpacity
                    style={styles.checkbox}
                    onPress={() => onSelectVideo(item.id ?? -1)}
                >
                    {videoSelected.includes(item.id ?? -1) ? (
                        <CheckBoxSelected
                            height={icons.sizes.m}
                            width={icons.sizes.m}
                            color={colors.primary}
                            fill={'white'}
                        />
                    ) : (
                        <CheckBoxUnselected
                            height={icons.sizes.m}
                            width={icons.sizes.m}
                        />
                    )}
                </TouchableOpacity>
            )}

            {item.thumbnail && item.thumbnail !== '' && false
                ? <FastImage
                    useFastImage={true}
                    style={styles.thumbnail}
                    resizeMode={"contain"}
                    source={{
                        uri: item.thumbnail,
                    }}
                />
                : <DefaultVideoThumbnail style={styles.thumbnail} />

            }

            <View style={styles.descriptionContainer}>
                <Text style={styles.titleText}>{item.title}</Text>

                <View style={styles.metaDataContainer}>
                    <Text style={styles.timeText}>
                        {dateStr}
                    </Text>
                    <Text style={styles.statusText}>
                        {item.status === 'pending'
                            ? '审核中'
                            : ''}
                    </Text>
                </View>
            </View>
        </View>
    }, [isEditing, videoSelected]);

    return (
        <ScreenContainer>
            <TitleWithBackButtonHeader
                title="上传记录"
                right={
                    <TouchableOpacity onPress={onEditPress}>
                        {isEditing
                            ? <Text style={styles.cancelEditTxt}>取消</Text>
                            : <Text style={styles.editTxt}>编辑</Text>
                        }

                    </TouchableOpacity>
                }
            />

            {uploadState.uploadHistory.length > 0
                ? <FlatList
                    data={uploadState.uploadHistory}
                    renderItem={renderItem}
                    style={{
                        paddingVertical: 10,
                    }}
                />
                : <EmptyList description="暂无上传成功视频" />
            }

            {isEditing && (
                <View style={styles.deleteConfirmationModal}>
                    <Button
                        onPress={() => {
                            if (
                                videoSelected.length === 0 ||
                                videoSelected.length !== uploadState.uploadHistory.length
                            ) {
                                setVideoSelected(uploadState.uploadHistory.map(v => v.id ?? -1));
                            } else {
                                setVideoSelected([]);
                            }
                        }}
                        containerStyle={styles.confirmationBtn}
                        color={colors.card2}
                        titleStyle={{ ...textVariants.body, color: colors.muted }}
                    >
                        {videoSelected.length === 0 ||
                            videoSelected.length !== uploadState.uploadHistory.length
                            ? "全选"
                            : "取消全选"}
                    </Button>
                    <Button
                        onPress={onRemovePress}
                        containerStyle={styles.confirmationBtn}
                        color={videoSelected.length === 0 ? colors.card2 : colors.primary}
                        titleStyle={{
                            ...textVariants.body,
                            color:
                                videoSelected.length === 0 ? colors.muted : colors.background,
                        }}
                    >
                        删除
                    </Button>
                </View>
            )}

            {isEditing &&
                <ConfirmationModal
                    onConfirm={onConfirmRemove}
                    onCancel={() => setShowConfirmDialog(false)}
                    isVisible={showConfirmDialog}
                    title="清除提示"
                    subtitle="您是否确定清除？"
                />
            }
        </ScreenContainer>
    );
}

const createStyles = ({ colors, textVariants }: any) => StyleSheet.create({
    editTxt: {
        ...textVariants.body,
        color: colors.primary,
    },
    cancelEditTxt: {
        ...textVariants.body,
    },
    itemContainer: {
        flexDirection: 'row',
        width: '100%',
        paddingVertical: 14,
    },
    thumbnail: {
        width: 140,
        aspectRatio: 16 / 9,
        marginRight: 14,
        borderRadius: 10,
    },
    descriptionContainer: {
        flex: 1,
        paddingBottom: 4,
    },
    titleText: {
        ...textVariants.body,
        flex: 1,
    },
    metaDataContainer: {
        flexDirection: 'row',
        justifyContent: 'space-between',
        alignItems: 'flex-end',
    },
    timeText: {
        ...textVariants.subbody,
        color: colors.muted,
    },
    statusText: {
        ...textVariants.body,
        
        color: colors.confirm,
    },
    checkbox: {
        padding: 5,
        marginRight: 10,
        alignSelf: 'center',
    },
    deleteConfirmationModal: {
        display: "flex",
        flexDirection: "row",
        alignItems: "center",
        justifyContent: "space-between",
        padding: 8,
    },
    confirmationBtn: {
        flex: 1,
        margin: 10,
        borderRadius: 10,
        marginTop: 8,
    },
});
