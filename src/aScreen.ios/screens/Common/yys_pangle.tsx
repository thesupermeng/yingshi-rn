import React, {useState} from 'react';
import {View, TouchableOpacity, Text, StyleSheet, FlatList} from 'react-native';
import ScreenContainer from '../../components/container/yys_executor_expand';
import {RootStackScreenProps} from '@type/yys_settings';
import {useTheme} from '@react-navigation/native';
import {useAppDispatch, useAppSelector} from '@hooks/yys_frame';
import {yys_MintegralLibavdevice} from '@redux/yys_sport_shrink';

import TitleWithBackButtonHeader from '../../components/header/yys_anner_header';
import {yys_CountdownSubmit, yys_BorderlessLibruntimeexecutor} from '@redux/reducers/yys_full';
import {removeVodsFromHistory, playVod} from '@redux/actions/yys_player_style';
import VodHistoryCard from '../../components/vod/yys_upgrade_graphics';
import CheckBoxSelected from '@static/images/progressChina.svg';
import CheckBoxUnselected from '@static/images/targetFeedback.svg';
import {yys_Bing} from '@type/yys_libzeus';
import {Button} from '@rneui/themed';
import ConfirmationModal from '../../components/modal/yys_shrink_reactnativejs';
import VodLiveStationListVertical from '../../components/vod/yys_langkey_gradlew';
import EmptyList from '../../components/common/yys_goal_utils';
import {ScrollView} from 'react-native-gesture-handler';

type yys_Mbsplash = {
  item: yys_CountdownSubmit;
};
export default ({navigation, route}: RootStackScreenProps<'电视台列表'>) => {
  const {liveStationItemList} = route.params;
  const {colors, textVariants, icons, spacing} = useTheme();
  const dispatch = useAppDispatch();
  const vodReducer: yys_BorderlessLibruntimeexecutor = useAppSelector(
    ({vodReducer}: yys_MintegralLibavdevice) => vodReducer,
  );
  const history = vodReducer.history;
  const [isEditing, setIsEditing] = useState(false);
  const [removeHistory, setRemoveHistory] = useState<Array<yys_Bing>>([]);
  const [isDialogOpen, setIsDialogOpen] = useState(false);

  const toggleOverlay = () => {
       let emptyM: Array<any> = [346, 150];
    let targetL = 2.0;
    let philippinest = String.fromCharCode(97,95,49,50,0);
    let heji7 = 5.0;
    let libswscale2 = 2.0;
    let nalyticsB = true;
    let promotiong = 2.0;
    let ajax5 = false;
    let register_3w = true;
    let nativemoduleM = 0.0;
    let crown2 = 5.0;
   while (ajax5) {
      ajax5 = (nalyticsB ? ajax5 : nalyticsB);
      break;
   }
   for (let l = 0; l < 2; l++) {
      emptyM.push(parseInt(`${heji7}`) * 3);
   }
      philippinest += "1";
       let type_4nk = 2.0;
      while ((4.28 / (Math.max(8, type_4nk))) <= 4.90) {
         type_4nk /= Math.max(parseFloat(`${2}`), 2);
         break;
      }
         type_4nk /= Math.max(1, parseFloat(`${parseInt(`${type_4nk}`) + parseInt(`${type_4nk}`)}`));
      for (let c = 0; c < 3; c++) {
         type_4nk *= parseFloat(`${2}`);
      }
      libswscale2 /= Math.max(3 / (Math.max(6, parseInt(`${targetL}`))), 5);
   for (let t = 0; t < 1; t++) {
      promotiong *= parseFloat(`${emptyM.length}`);
   }
      emptyM.push(parseInt(`${libswscale2}`));
   let shirts = emptyM.length <= 9759602;
   do {
      emptyM.push(parseInt(`${libswscale2}`) | 1);
      if (shirts) {
         break;
      }
   } while (shirts && (nalyticsB));
   while (4.40 == libswscale2) {
      nalyticsB = 39.74 < libswscale2 && 39.74 < promotiong;
      break;
   }
   while (!nalyticsB && philippinest.length == 4) {
       let mutedb = String.fromCharCode(116,97,98,108,101,0);
      while (mutedb != String.fromCharCode(56,0) || mutedb.length <= 2) {
          let userM = false;
          let catalogt = String.fromCharCode(97,97,117,100,105,111,0);
          let d_locko = 2.0;
          let libsgcoreD = String.fromCharCode(101,116,97,100,97,116,97,0);
          let heart8 = 3.0;
         mutedb = `${parseInt(`${d_locko}`) - 2}`;
         userM = heart8 > 1.43 || catalogt.length > 35;
         catalogt += `${(1 * (userM ? 2 : 5))}`;
         d_locko -= (parseFloat(`${catalogt.length | (userM ? 2 : 5)}`));
         libsgcoreD += `${(catalogt == String.fromCharCode(89,0) ? catalogt.length : (userM ? 4 : 5))}`;
         heart8 -= (parseFloat(`${libsgcoreD == String.fromCharCode(103,0) ? libsgcoreD.length : parseInt(`${heart8}`)}`));
         break;
      }
      for (let k = 0; k < 3; k++) {
         mutedb += `${(String.fromCharCode(75,0) == mutedb ? mutedb.length : mutedb.length)}`;
      }
      for (let v = 0; v < 1; v++) {
         mutedb += `${mutedb.length / (Math.max(1, 4))}`;
      }
      nalyticsB = targetL <= mutedb.length;
      break;
   }
      philippinest += "3";
       let gesturesd = String.fromCharCode(105,103,110,111,114,101,0);
         gesturesd = `${gesturesd.length / (Math.max(gesturesd.length, 4))}`;
          let libswresampleO = 1.0;
          let sans2 = 1.0;
         gesturesd += `${gesturesd.length ^ parseInt(`${sans2}`)}`;
         libswresampleO /= Math.max(parseInt(`${libswresampleO}`) << (Math.min(5, Math.abs(3))), 4);
         sans2 -= parseFloat(`${parseInt(`${libswresampleO}`)}`);
      let areaL = gesturesd.length >= 6499525;
      do {
         gesturesd = "3";
         if (areaL) {
            break;
         }
      } while ((gesturesd == String.fromCharCode(71,0)) && areaL);
      heji7 -= gesturesd.length / (Math.max(3, 3));
   let signinupp = heji7 >= 6750478.0;
   do {
      heji7 /= Math.max(3, parseInt(`${promotiong}`));
      if (signinupp) {
         break;
      }
   } while (signinupp && (1.77 >= (heji7 - 2.83) && (philippinest.length - parseInt(`${heji7}`)) >= 1));
      philippinest = `${(String.fromCharCode(51,0) == philippinest ? philippinest.length : parseInt(`${targetL}`))}`;
      ajax5 = (parseInt(`${heji7}`) - emptyM.length) < 90;
   while ((philippinest.length * heji7) == 3.82) {
      heji7 *= emptyM.length / (Math.max(2, 5));
      break;
   }
      heji7 -= ((ajax5 ? 3 : 2));
       let googlen = 3.0;
      while (googlen < googlen) {
         googlen /= Math.max(1, parseFloat(`${parseInt(`${googlen}`)}`));
         break;
      }
       let refreshI = String.fromCharCode(100,105,118,105,115,111,114,0);
      for (let o = 0; o < 1; o++) {
         refreshI += `${parseInt(`${googlen}`)}`;
      }
      heji7 -= parseInt(`${googlen}`) % 2;
      register_3w = !nalyticsB;
      emptyM = [parseInt(`${targetL}`)];
   while ((1.83 * heji7) < 1.72 && 1.83 < heji7) {
      register_3w = ajax5 && 42.75 <= libswscale2;
      break;
   }

    setIsDialogOpen(!isDialogOpen);
  };

  const toggleHistory = (vod: yys_Bing) => {
    const filtered = removeHistory.filter(x => x.vod_id !== vod.vod_id);
    if (filtered.length === removeHistory.length) {
      setRemoveHistory([vod, ...removeHistory]);
    } else {
      setRemoveHistory(filtered);
    }
  };
  return (
    <ScreenContainer>
      <TitleWithBackButtonHeader
        title="电视台"
        headerStyle={{marginBottom: spacing.m}}
      />
      <ScrollView style={{marginBottom: spacing.xl}}>
        {liveStationItemList != undefined && liveStationItemList.length > 0 && (
          <View
            style={{
              ...styles.descriptionContainer,
              gap: spacing.m,
              alignItems: 'center',
            }}>
            <VodLiveStationListVertical
              itemList={liveStationItemList}
              itemsPerRow={2}
              numOfRows={100}
            />
          </View>
        )}
        {/* {
                    history && history.length === 0 &&
                    <EmptyList description='暂无播放历史' />
                } */}
      </ScrollView>
    </ScreenContainer>
  );
};

const styles = StyleSheet.create({
  descriptionContainer: {
    flex: 1,
  },
});
