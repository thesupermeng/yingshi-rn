import React, { useState } from 'react';
import { View, TouchableOpacity, Text, StyleSheet, FlatList } from 'react-native';
import ScreenContainer from '../../components/container/yys_matches';
import { RootStackScreenProps } from '@type';
import { useTheme } from '@react-navigation/native';
import { useAppDispatch, useAppSelector } from '@hooks';
import { yysIconclosewhiteBaiduads } from '@redux';

import TitleWithBackButtonHeader from '../../components/header/yys_away_predictionloss_header';
import { yysAnalytics, yysIconpointscoreCountdown } from '@redux';
import { removeVodsFromHistory, playVod } from '@redux';
import VodHistoryCard from '../../components/vod/yys_header_bgvipsport';
import { yysPenaltyshoot } from '@type';
import { Button } from '@rneui/themed';
import ConfirmationModal from '../../components/modal/yys_twitter';
import VodLiveStationListVertical from '../../components/vod/yys_basketballplayerplaceholder';
import EmptyList from '../../components/common/yys_benefit';
import { ScrollView } from 'react-native-gesture-handler';

type yysInternetAirbnbstarselected = {
  item: yysAnalytics;
};
export default ({ navigation, route }: RootStackScreenProps<'电视台列表'>) => {
  const { liveStationItemList } = route.params;
  const { colors, textVariants, icons, spacing } = useTheme();
  const dispatch = useAppDispatch();
  const vodReducer: yysIconpointscoreCountdown = useAppSelector(
    ({ vodReducer }: yysIconclosewhiteBaiduads) => vodReducer,
  );
  const history = vodReducer.history;
  const [isEditing, setIsEditing] = useState(false);
  const [removeHistory, setRemoveHistory] = useState<Array<yysPenaltyshoot>>([]);
  const [isDialogOpen, setIsDialogOpen] = useState(false);

  const toggleOverlay = () => {
       let y_viewy = String.fromCharCode(97,95,50,51,95,112,114,111,112,111,115,97,108,0);
    let libfollyB = String.fromCharCode(116,101,110,99,0);
    let loginw = 3;
    let libapminsightal: Map<any, any> = new Map([[String.fromCharCode(100,105,114,101,99,116,105,111,110,95,116,95,51,55,0),686], [String.fromCharCode(115,100,107,95,97,95,52,53,0),509]]);
    let liveshareO = String.fromCharCode(101,95,53,50,95,101,120,112,97,110,100,0);
    let skip1 = false;
    let notificationr = 1.0;
    let valuesu = 1.0;
    let basketballhometeam5: Map<any, any> = new Map([[String.fromCharCode(111,95,56,95,97,117,116,111,112,108,97,121,105,110,103,0),102], [String.fromCharCode(112,114,101,118,105,101,119,105,110,103,95,113,95,53,57,0),164]]);
    let subbasketballplayer3: Map<any, any> = new Map([[String.fromCharCode(100,101,115,116,114,117,99,116,105,118,101,95,120,95,53,57,0),802], [String.fromCharCode(116,95,54,48,95,104,101,118,99,0),228]]);
    let kuaishouJ = String.fromCharCode(122,95,53,52,95,100,101,116,101,99,116,111,114,0);
    let reactc = false;
    let libreactnativejniq = String.fromCharCode(102,108,111,97,116,105,110,103,95,51,95,53,48,0);
    let yellowscoreballw = 5.0;
    let infoJ: Map<any, any> = new Map([[String.fromCharCode(103,95,52,53,95,99,104,101,99,107,109,97,114,107,115,0),405], [String.fromCharCode(121,95,51,48,95,118,97,97,99,97,108,99,117,108,97,116,105,111,110,0),929], [String.fromCharCode(105,111,115,95,51,95,53,48,0),206]]);
    let sliderd: Array<any> = [430, 161, 568];
   while (!skip1 || 5.6 <= (5.9 - valuesu)) {
      skip1 = liveshareO.startsWith(`${loginw}`);
      break;
   }
       let usernamej = 3.0;
       let defaultroombgG = 5.0;
       let libnmsg = false;
         defaultroombgG -= ((libnmsg ? 3 : 3) * parseInt(`${usernamej}`));
         defaultroombgG /= Math.max(5, 3 | parseInt(`${defaultroombgG}`));
         usernamej += parseInt(`${defaultroombgG}`);
         libnmsg = 21.41 < usernamej || 21.41 < defaultroombgG;
      for (let h = 0; h < 2; h++) {
          let muted7 = 0;
          let targetn = String.fromCharCode(114,101,109,111,118,101,103,114,97,105,110,95,110,95,51,50,0);
          let customo = String.fromCharCode(97,99,116,105,118,101,109,97,112,95,49,95,53,52,0);
         defaultroombgG -= customo.length | targetn.length;
         muted7 /= Math.max(3, muted7);
         targetn = `${1 >> (Math.min(2, Math.abs(muted7)))}`;
      }
      if (!libnmsg) {
          let videovarr = 3;
          let nbatrophyo = String.fromCharCode(112,95,53,95,97,115,110,0);
         defaultroombgG *= 3;
         videovarr ^= 1;
         nbatrophyo += `${nbatrophyo.length}`;
      }
         usernamej /= Math.max(parseInt(`${usernamej}`) << (Math.min(4, Math.abs(2))), 1);
       let configS = String.fromCharCode(115,117,105,116,101,98,95,116,95,56,56,0);
       let philippinese = String.fromCharCode(115,101,114,105,97,108,108,121,95,48,95,53,49,0);
         philippinese += `${parseInt(`${defaultroombgG}`) >> (Math.min(configS.length, 2))}`;
      liveshareO = "3";
   for (let h = 0; h < 2; h++) {
       let main_fP = String.fromCharCode(104,95,50,48,95,106,97,99,99,97,114,100,100,105,115,116,0);
       let questiconS: Map<any, any> = new Map([[String.fromCharCode(115,112,105,110,110,101,114,95,51,95,53,55,0),String.fromCharCode(103,101,110,97,110,110,95,56,95,49,53,0)], [String.fromCharCode(112,111,115,105,116,105,111,110,115,0),String.fromCharCode(105,95,51,53,95,114,101,97,115,115,111,99,105,97,116,101,0)]]);
       let componentregistrys = true;
       let subtext_ = false;
       let thumbnailX = 5;
          let static_zT: Array<any> = [84, 139, 656];
          let penaltymatchicony = String.fromCharCode(99,104,101,99,107,105,110,103,95,99,95,52,52,0);
         thumbnailX /= Math.max(5, static_zT.length * penaltymatchicony.length);
      while (Array.from(questiconS.keys()).includes(`${thumbnailX}`)) {
         thumbnailX <<= Math.min(Math.abs(1 / (Math.max(10, questiconS.size))), 4);
         break;
      }
         subtext_ = null != questiconS.get(`${componentregistrys}`);
         subtext_ = componentregistrys;
      if (5 > questiconS.size) {
          let combinedj = String.fromCharCode(120,95,52,48,95,104,117,102,102,109,97,110,0);
          let mbnativeQ = 3.0;
          let apple8 = String.fromCharCode(115,111,98,105,110,100,95,53,95,51,51,0);
          let livenodatabgimgn: Map<any, any> = new Map([[String.fromCharCode(105,110,111,100,101,95,48,95,50,0),String.fromCharCode(111,95,52,49,95,119,101,105,103,104,116,112,0)], [String.fromCharCode(118,105,97,95,117,95,51,48,0),String.fromCharCode(105,115,99,111,118,101,114,0)], [String.fromCharCode(116,116,97,101,110,99,100,115,112,95,51,95,51,51,0),String.fromCharCode(120,109,97,115,109,95,114,95,52,50,0)]]);
          let libavfilter_: Array<any> = [698, 780];
         questiconS = new Map([[`${subtext_}`, (3 - (componentregistrys ? 1 : 5))]]);
         combinedj = `${libavfilter_.length}`;
         mbnativeQ /= Math.max(5, (String.fromCharCode(117,0) == combinedj ? combinedj.length : libavfilter_.length));
         apple8 += `${parseInt(`${mbnativeQ}`) - libavfilter_.length}`;
         livenodatabgimgn = new Map([[`${livenodatabgimgn.size}`, livenodatabgimgn.size + 3]]);
      }
          let userf = false;
         subtext_ = thumbnailX > 28;
         userf = !userf && !userf;
      for (let u = 0; u < 2; u++) {
         thumbnailX -= thumbnailX;
      }
       let actionsr = String.fromCharCode(109,95,54,57,95,109,105,115,99,0);
       let playercommony = String.fromCharCode(118,101,108,97,112,115,101,100,116,105,109,101,114,95,101,95,55,52,0);
         subtext_ = main_fP.length == 20;
      let actiong = componentregistrys ? !componentregistrys : componentregistrys;
      do {
          let buildd = String.fromCharCode(118,95,55,57,95,103,101,111,0);
          let statisticsactives: Array<any> = [17, 995, 72];
         componentregistrys = 9 > thumbnailX;
         buildd += `${3 ^ statisticsactives.length}`;
         statisticsactives.push(statisticsactives.length);
         if (actiong) {
            break;
         }
      } while ((!subtext_) && actiong);
      let dependencyG = String.fromCharCode(121,107,106,51,118,113,98,121,50,48,0) == main_fP;
      do {
         main_fP = `${2 & questiconS.size}`;
         if (dependencyG) {
            break;
         }
      } while (dependencyG && (!actionsr.endsWith(`${main_fP.length}`)));
      for (let h = 0; h < 1; h++) {
         subtext_ = main_fP.length <= 66 && componentregistrys;
      }
      while (!actionsr.endsWith(`${main_fP.length}`)) {
         actionsr = `${questiconS.size}`;
         break;
      }
         questiconS = new Map([[`${thumbnailX}`, actionsr.length]]);
       let iconstarK = String.fromCharCode(97,95,51,50,95,101,109,97,105,108,0);
       let iconnewchat7 = String.fromCharCode(114,102,102,116,102,0);
      subbasketballplayer3.set(`${thumbnailX}`, liveshareO.length & 3);
   }
       let profileinactive3 = String.fromCharCode(118,95,57,56,95,99,104,117,110,107,0);
          let largesoundA = 1;
          let detailsA = String.fromCharCode(98,114,111,97,100,99,97,115,116,105,110,103,95,114,95,55,51,0);
          let predictionbannersharedA: Array<any> = [533, 845];
         profileinactive3 += `${predictionbannersharedA.length + 1}`;
         largesoundA <<= Math.min(2, Math.abs(detailsA.length & largesoundA));
         detailsA = `${detailsA.length}`;
         predictionbannersharedA.push(detailsA.length);
         profileinactive3 += `${(String.fromCharCode(121,0) == profileinactive3 ? profileinactive3.length : profileinactive3.length)}`;
          let gpayN = 3.0;
         profileinactive3 += `${profileinactive3.length + parseInt(`${gpayN}`)}`;
      notificationr += parseFloat(`${libapminsightal.size}`);
   if (libfollyB.length < parseInt(`${valuesu}`)) {
      libfollyB = `${libreactnativejniq.length}`;
   }
   while (skip1) {
       let colorsy: Map<any, any> = new Map([[String.fromCharCode(98,95,53,57,95,97,117,116,111,99,111,114,114,101,108,97,116,105,111,110,0),String.fromCharCode(112,97,105,100,95,110,95,51,52,0)], [String.fromCharCode(101,95,49,55,95,114,97,100,105,117,115,0),String.fromCharCode(108,95,56,49,95,99,111,110,115,117,109,101,100,0)], [String.fromCharCode(107,95,52,54,95,99,114,111,112,0),String.fromCharCode(105,102,105,108,116,101,114,95,98,95,53,55,0)]]);
       let gifgoalbgV = String.fromCharCode(108,95,57,50,95,109,97,116,114,105,99,101,115,0);
       let y_hashE = true;
       let recommendationw: Map<any, any> = new Map([[String.fromCharCode(105,100,99,116,120,95,102,95,53,0),String.fromCharCode(114,102,102,116,102,95,103,95,56,50,0)], [String.fromCharCode(102,95,52,95,107,101,121,104,97,115,104,0),String.fromCharCode(105,110,118,105,115,105,98,108,101,0)]]);
       let bodanl: Map<any, any> = new Map([[String.fromCharCode(105,115,99,111,118,101,114,95,109,95,49,53,0),false ], [String.fromCharCode(115,102,116,112,95,53,95,53,48,0),false ]]);
         bodanl.set(`${y_hashE}`, 1);
         bodanl = new Map([[`${recommendationw.size}`, 2 / (Math.max(8, recommendationw.size))]]);
         recommendationw = new Map([[`${bodanl.size}`, 3]]);
         gifgoalbgV += `${recommendationw.size * bodanl.size}`;
      let matchactive2 = 9047988 >= colorsy.size;
      do {
          let greyarrowupT: Map<any, any> = new Map([[String.fromCharCode(99,104,101,99,107,112,97,99,107,101,116,95,121,95,57,0),226], [String.fromCharCode(106,95,57,52,95,114,101,99,111,110,110,101,99,116,101,100,0),167], [String.fromCharCode(105,95,50,54,95,117,115,97,98,108,101,0),40]]);
          let airbnbstar9 = false;
         colorsy.set(`${y_hashE}`, (greyarrowupT.size ^ (y_hashE ? 2 : 1)));
         greyarrowupT.set(`${airbnbstar9}`, ((airbnbstar9 ? 5 : 1)));
         if (matchactive2) {
            break;
         }
      } while (matchactive2 && ((5 | colorsy.size) >= 3 || 3 >= (recommendationw.size | 5)));
       let frame_13n = String.fromCharCode(116,101,108,101,103,114,97,95,104,95,57,49,0);
       let gestureW: Map<any, any> = new Map([[String.fromCharCode(111,95,53,52,95,99,108,101,97,110,0),123], [String.fromCharCode(105,95,57,51,95,115,116,112,115,0),336]]);
       let vipsportT: Map<any, any> = new Map([[String.fromCharCode(112,95,51,53,95,111,102,102,115,99,114,101,101,110,0),7], [String.fromCharCode(104,95,50,57,95,115,101,114,118,101,114,115,0),934]]);
         vipsportT = new Map([[`${bodanl.size}`, vipsportT.size - 1]]);
      basketballhometeam5 = new Map([[`${valuesu}`, ((y_hashE ? 3 : 1) + parseInt(`${valuesu}`))]]);
      break;
   }
       let googleC = 0.0;
       let jnews6 = 0;
       let otherd = 3;
          let pressureA = 4.0;
          let turnh = String.fromCharCode(108,95,53,95,97,99,116,105,118,97,116,101,100,0);
         otherd ^= parseInt(`${pressureA}`);
         pressureA *= (String.fromCharCode(53,0) == turnh ? turnh.length : turnh.length);
          let readb = String.fromCharCode(105,110,102,105,110,105,116,101,95,119,95,52,49,0);
          let orangedownarrowu: Array<any> = [String.fromCharCode(116,95,52,51,95,99,111,108,111,114,115,112,97,99,101,0), String.fromCharCode(101,95,50,53,95,101,120,97,99,116,108,121,0), String.fromCharCode(97,95,57,54,95,98,105,116,109,97,112,0)];
         googleC *= orangedownarrowu.length;
         readb = `${readb.length / 2}`;
         orangedownarrowu = [readb.length];
      for (let o = 0; o < 2; o++) {
         jnews6 |= parseInt(`${googleC}`);
      }
         otherd <<= Math.min(Math.abs(parseInt(`${googleC}`) + jnews6), 5);
      while (1 == otherd) {
         otherd <<= Math.min(Math.abs(2 << (Math.min(5, Math.abs(otherd)))), 3);
         break;
      }
       let anytime4 = false;
       let customA = true;
          let qaag_ = false;
          let rightR = 1.0;
          let descI = String.fromCharCode(99,104,97,114,115,95,122,95,54,48,0);
         googleC -= descI.length / 2;
         qaag_ = 77.100 >= rightR;
         rightR -= (parseFloat(`${(qaag_ ? 2 : 4) - parseInt(`${rightR}`)}`));
         descI += `${((qaag_ ? 4 : 1) << (Math.min(Math.abs(parseInt(`${rightR}`)), 1)))}`;
      let auto_nk = customA ? !customA : customA;
      do {
         customA = 37 <= jnews6;
         if (auto_nk) {
            break;
         }
      } while (auto_nk && (2 < jnews6));
          let footballfieldK = false;
          let icondefaultthumbnailC = String.fromCharCode(97,95,57,50,95,102,111,114,119,97,114,100,115,0);
         jnews6 /= Math.max(1, otherd);
         footballfieldK = !footballfieldK;
         icondefaultthumbnailC += "2";
      skip1 = libreactnativejniq.includes(`${otherd}`);
       let libavutil_ = String.fromCharCode(104,105,98,105,116,0);
         libavutil_ += "3";
      for (let m = 0; m < 2; m++) {
         libavutil_ += `${(libavutil_ == String.fromCharCode(80,0) ? libavutil_.length : libavutil_.length)}`;
      }
      for (let c = 0; c < 2; c++) {
         libavutil_ = "3";
      }
      y_viewy += `${libapminsightal.size}`;
   for (let x = 0; x < 3; x++) {
      basketballhometeam5.set(liveshareO, parseInt(`${yellowscoreballw}`) >> (Math.min(liveshareO.length, 4)));
   }
      subbasketballplayer3.set(y_viewy, (y_viewy == String.fromCharCode(67,0) ? y_viewy.length : libfollyB.length));
       let wifirouterN = false;
       let shareblackM = String.fromCharCode(115,95,52,54,95,116,120,116,0);
       let profileframeu = 5;
      if (wifirouterN) {
         shareblackM += `${(shareblackM == String.fromCharCode(71,0) ? (wifirouterN ? 4 : 5) : shareblackM.length)}`;
      }
      for (let z = 0; z < 1; z++) {
          let middlebrightnessC = true;
          let dependencyf = false;
         shareblackM = `${((dependencyf ? 1 : 2) | 3)}`;
         middlebrightnessC = (!middlebrightnessC ? !middlebrightnessC : middlebrightnessC);
         dependencyf = !middlebrightnessC && !middlebrightnessC;
      }
      if (shareblackM.startsWith(`${wifirouterN}`)) {
         wifirouterN = !wifirouterN || shareblackM.length <= 73;
      }
      let redgoall = 8932866 >= shareblackM.length;
      do {
         shareblackM += `${(2 & (wifirouterN ? 4 : 1))}`;
         if (redgoall) {
            break;
         }
      } while (((profileframeu & 2) < 2 && 2 < (2 & profileframeu)) && redgoall);
      for (let r = 0; r < 1; r++) {
          let shareblack1 = String.fromCharCode(110,95,53,48,95,98,105,110,107,0);
          let ajax7: Map<any, any> = new Map([[String.fromCharCode(101,95,55,95,101,110,99,114,121,112,116,101,100,0),false ], [String.fromCharCode(101,95,52,57,95,97,117,116,111,112,108,97,121,0),true ]]);
         profileframeu += (shareblack1 == String.fromCharCode(114,0) ? shareblack1.length : profileframeu);
         ajax7.set(`${ajax7.size}`, 3);
      }
         wifirouterN = shareblackM.endsWith(`${profileframeu}`);
         shareblackM = "1";
         shareblackM += `${profileframeu * 2}`;
      let basketballhometeamp = wifirouterN ? !wifirouterN : wifirouterN;
      do {
         wifirouterN = shareblackM.endsWith(`${wifirouterN}`);
         if (basketballhometeamp) {
            break;
         }
      } while (basketballhometeamp && (!wifirouterN && 2 >= (profileframeu << (Math.min(Math.abs(4), 4)))));
      libapminsightal.set(liveshareO, 3 - liveshareO.length);
      yellowscoreballw /= Math.max(libapminsightal.size >> (Math.min(liveshareO.length, 1)), 3);
   let liveshareY = String.fromCharCode(105,56,48,98,116,119,48,119,109,0) == y_viewy;
   do {
      y_viewy += `${((reactc ? 1 : 2))}`;
      if (liveshareY) {
         break;
      }
   } while (liveshareY && (!kuaishouJ.endsWith(y_viewy)));
      kuaishouJ += `${3 * parseInt(`${notificationr}`)}`;
      notificationr *= (parseFloat(`${String.fromCharCode(50,0) == y_viewy ? libapminsightal.size : y_viewy.length}`));
   let volumeL = 6433806 <= basketballhometeam5.size;
   do {
       let o_viewu = false;
       let with_kzz = 0.0;
       let network6 = 0;
      let expiredQ = o_viewu ? !o_viewu : o_viewu;
      do {
          let chatM = String.fromCharCode(110,95,57,52,95,115,101,103,0);
          let questiconv = 5.0;
          let floaterE = false;
         o_viewu = with_kzz < 12.55;
         chatM += `${((floaterE ? 1 : 1) + parseInt(`${questiconv}`))}`;
         questiconv *= (parseFloat(`${(floaterE ? 3 : 5)}`));
         if (expiredQ) {
            break;
         }
      } while (expiredQ && (1.28 >= (3.20 * with_kzz)));
      if (network6 >= with_kzz) {
         network6 >>= Math.min(Math.abs(3 ^ network6), 3);
      }
      while (with_kzz >= 2.82 || (with_kzz * 2.82) >= 1.75) {
         with_kzz *= parseFloat(`${2 << (Math.min(Math.abs(network6), 1))}`);
         break;
      }
      let libavformat0 = with_kzz <= 9464823.0;
      do {
         with_kzz += parseFloat(`${network6}`);
         if (libavformat0) {
            break;
         }
      } while (((with_kzz / 4.57) > 4.18) && libavformat0);
         with_kzz -= (parseFloat(`${parseInt(`${with_kzz}`) >> (Math.min(3, Math.abs((o_viewu ? 4 : 2))))}`));
      let helper3 = with_kzz >= 7381190.0;
      do {
         with_kzz += parseFloat(`${1 + parseInt(`${with_kzz}`)}`);
         if (helper3) {
            break;
         }
      } while (helper3 && ((with_kzz * 3.44) >= 5.32));
      let splashB = with_kzz <= 6542132.0;
      do {
          let leftP = 5.0;
          let iconclosewhiteO = 0.0;
          let unewarchdefaultsn = true;
         with_kzz += parseFloat(`${network6 - 2}`);
         leftP /= Math.max(3, parseFloat(`${3 * parseInt(`${iconclosewhiteO}`)}`));
         iconclosewhiteO /= Math.max(2, 2 | parseInt(`${leftP}`));
         unewarchdefaultsn = 53.90 > leftP;
         if (splashB) {
            break;
         }
      } while ((!o_viewu) && splashB);
      if (with_kzz > 4.89 && 4.100 > (4.89 * with_kzz)) {
         with_kzz /= Math.max(parseFloat(`${parseInt(`${with_kzz}`)}`), 4);
      }
      while (5.77 == (network6 * with_kzz)) {
         with_kzz += parseFloat(`${2 - parseInt(`${with_kzz}`)}`);
         break;
      }
      basketballhometeam5 = new Map([[libreactnativejniq, libreactnativejniq.length << (Math.min(Math.abs(2), 1))]]);
      if (volumeL) {
         break;
      }
   } while ((basketballhometeam5.get(`${infoJ.size}`) != null) && volumeL);
      reactc = (liveshareO.length + y_viewy.length) >= 74;
   if (5.42 < (valuesu + 3.26)) {
      y_viewy = `${(libfollyB == String.fromCharCode(101,0) ? libfollyB.length : infoJ.size)}`;
   }
       let awayplayerc = String.fromCharCode(120,95,53,56,95,99,111,109,109,105,116,0);
       let update_lfb: Map<any, any> = new Map([[String.fromCharCode(122,95,52,52,95,97,110,110,101,120,98,109,112,0),String.fromCharCode(97,98,103,114,95,102,95,54,51,0)], [String.fromCharCode(103,95,50,49,95,110,101,97,114,101,110,100,0),String.fromCharCode(105,110,101,116,95,56,95,49,48,0)], [String.fromCharCode(115,105,110,117,115,111,105,100,97,108,95,117,95,57,53,0),String.fromCharCode(100,101,109,117,120,101,114,115,0)]]);
      if ((3 ^ awayplayerc.length) <= 1 || (awayplayerc.length ^ 3) <= 5) {
         awayplayerc += `${(awayplayerc == String.fromCharCode(74,0) ? awayplayerc.length : update_lfb.size)}`;
      }
         update_lfb.set(awayplayerc, 3 / (Math.max(3, awayplayerc.length)));
       let awayS = String.fromCharCode(99,97,110,99,101,108,97,98,108,101,95,100,95,57,57,0);
       let searchbar4 = String.fromCharCode(115,95,54,54,95,112,97,114,115,101,114,0);
         update_lfb = new Map([[awayS, 2]]);
          let questf = String.fromCharCode(106,95,53,54,95,104,97,114,100,119,97,114,101,0);
          let forwardW = 2.0;
          let borderless4 = 4.0;
         searchbar4 += `${1 - awayplayerc.length}`;
         questf += `${parseInt(`${borderless4}`)}`;
         forwardW -= parseInt(`${forwardW}`) * parseInt(`${borderless4}`);
          let videocommonz: Map<any, any> = new Map([[String.fromCharCode(115,112,101,99,105,97,108,95,55,95,53,53,0),false ], [String.fromCharCode(105,95,56,48,95,112,111,115,116,105,110,103,0),false ], [String.fromCharCode(115,105,110,101,95,105,95,50,0),false ]]);
         awayS = "3";
         videocommonz.set(`${videocommonz.size}`, videocommonz.size);
      libapminsightal = new Map([[`${notificationr}`, ((skip1 ? 4 : 1) / (Math.max(4, parseInt(`${notificationr}`))))]]);
      notificationr -= parseFloat(`${3 - libapminsightal.size}`);
      valuesu -= parseFloat(`${parseInt(`${notificationr}`)}`);

    setIsDialogOpen(!isDialogOpen);
  };

  const toggleHistory = (vod: yysPenaltyshoot) => {
    const filtered = removeHistory.filter(x => x.vod_id !== vod.vod_id);
    if (filtered.length === removeHistory.length) {
      setRemoveHistory([vod, ...removeHistory]);
    } else {
      setRemoveHistory(filtered);
    }
  };
  return (
    <ScreenContainer>
      <TitleWithBackButtonHeader
        title="电视台"
        headerStyle={{ marginBottom: spacing.m }}
      />
      <ScrollView style={{ marginBottom: spacing.xl }}>
        {liveStationItemList != undefined && liveStationItemList.length > 0 && (
          <View
            style={{
              ...styles.descriptionContainer,
              gap: spacing.m,
              alignItems: 'center',
            }}>
            <VodLiveStationListVertical
              itemList={liveStationItemList}
              itemsPerRow={2}
              numOfRows={100}
            />
          </View>
        )}
        {/* {
                    history && history.length === 0 &&
                    <EmptyList description='暂无播放历史' />
                } */}
      </ScrollView>
    </ScreenContainer>
  );
};

const styles = StyleSheet.create({
  descriptionContainer: {
    flex: 1,
  },
});
