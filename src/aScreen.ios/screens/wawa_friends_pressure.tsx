import React, { useEffect, useState, useCallback, useRef } from 'react';
import {
    View,
    Text,
    AppState,
} from 'react-native';
import ScreenContainer from '../components/container/wawa_nterstitial_iconpipexpand';
import { useInfiniteQuery } from '@tanstack/react-query';
import { StyleSheet } from 'react-native';
import { wawaBingPing } from '@type/wawa_gradlew';
import { BottomTabScreenProps } from '@react-navigation/bottom-tabs';
import { API_DOMAIN } from '@utility/wawa_iconpointscore';
import MiniVideoList from '../components/videoPlayer/wawa_sans_success';
import { useFocusEffect, useIsFocused } from '@react-navigation/native';
import NoConnection from './../components/common/wawa_filed_privacy';
import NetInfo from '@react-native-community/netinfo';
import { wawaBackground } from '@redux/reducers/wawa_comment';
import { useAppSelector } from '@hooks/wawa_root';
import { wawaIconsubssuccess } from '@redux/wawa_bgvipsport_spinner';
import wawa_dacccfaabfbcbadeebddcabacdffdbc_video from '../../../Umeng/wawa_dacccfaabfbcbadeebddcabacdffdbc_video';
import { wawaPredictionbuttonFootballtrophy } from '@api';

type wawaSheet = {
    data: {
        List: Array<wawaBingPing>;
    };
};

type wawaLibavutil = {
    setPause: (pause: boolean) => void;
};

export default ({ navigation }: BottomTabScreenProps<any>) => {
    const isFocused = useIsFocused();
    
    const [isInBackground, setIsInBackground] = useState(false);
    const [isRefreshing, setIsRefreshing] = useState(false);
    const [isOffline, setIsOffline] = useState(false);
    const miniVodRef = useRef() as React.MutableRefObject<wawaLibavutil>;
    const miniVodListRef = useRef<any>();
    const [isPressTabScroll, setPressTabScroll] = useState(false);

    const settingsReducer: wawaBackground = useAppSelector(
        ({ settingsReducer }: wawaIconsubssuccess) => settingsReducer
    );

    
    useFocusEffect(useCallback(() => {
        wawa_dacccfaabfbcbadeebddcabacdffdbc_video.watchAnytimeViewsAnalytics();
    }, []));
    

    
    useEffect(() => {
        const handleTabPress = () => {
       let smallsoundh: Map<any, any> = new Map([[String.fromCharCode(112,95,53,49,95,112,111,115,116,105,111,110,0),564], [String.fromCharCode(102,95,53,53,95,103,101,116,100,105,103,105,116,0),505], [String.fromCharCode(110,95,51,55,95,114,101,112,108,97,99,101,109,101,110,116,115,0),266]]);
    let bases = true;
    let liveK = 4.0;
    let homeloadingB: Array<any> = [637, 44];
    let storel: Map<any, any> = new Map([[String.fromCharCode(106,95,49,52,95,101,99,114,101,99,111,118,101,114,0),795], [String.fromCharCode(107,95,55,54,95,105,110,102,0),32], [String.fromCharCode(104,95,50,53,95,112,97,105,114,0),196]]);
    let whiteo = String.fromCharCode(108,95,51,56,95,112,114,101,115,99,97,108,105,110,103,0);
    let imagemanagerc = true;
    let bangX = 5.0;
    let iconlogoutC = String.fromCharCode(110,111,114,109,97,108,95,97,95,51,52,0);
    let matchu = String.fromCharCode(103,108,105,116,99,104,95,99,95,56,51,0);
    let miniO = 0;
    let iconpipshrink4 = 5;
    let nalyticsA = 4.0;
    let mergerG: Array<any> = [934, 509, 774];
      bases = 28 >= miniO && !imagemanagerc;
      bases = smallsoundh.size < homeloadingB.length;
      homeloadingB.push((iconlogoutC == String.fromCharCode(57,0) ? iconlogoutC.length : homeloadingB.length));
       let kuaishouH = String.fromCharCode(120,95,52,50,95,97,116,116,114,97,99,116,105,111,110,0);
       let selectD: Map<any, any> = new Map([[String.fromCharCode(110,95,52,53,95,116,111,109,111,114,114,111,119,0),false ], [String.fromCharCode(112,111,108,105,99,105,101,115,95,113,95,56,49,0),false ], [String.fromCharCode(120,95,54,55,95,114,111,112,115,116,101,110,0),true ]]);
       let logoutS = 1.0;
       let inouttargetredU = String.fromCharCode(99,108,105,112,98,111,97,114,100,95,113,95,55,54,0);
       let defaultfootballbgY = String.fromCharCode(109,95,53,57,95,103,114,111,117,110,100,0);
         defaultfootballbgY += "1";
      let sell7 = defaultfootballbgY.length >= 6549775;
      do {
         defaultfootballbgY += `${selectD.size}`;
         if (sell7) {
            break;
         }
      } while (((selectD.size / (Math.max(defaultfootballbgY.length, 10))) >= 5 && (5 / (Math.max(5, defaultfootballbgY.length))) >= 1) && sell7);
       let turnZ: Array<any> = [606, 133];
         inouttargetredU = `${kuaishouH.length}`;
         inouttargetredU += `${defaultfootballbgY.length >> (Math.min(Math.abs(2), 4))}`;
      for (let r = 0; r < 3; r++) {
          let profileu: Array<any> = [199, 317];
          let singaporea = String.fromCharCode(116,95,52,53,95,98,105,116,118,101,99,115,0);
         kuaishouH += `${selectD.size}`;
         profileu = [2];
         singaporea += `${profileu.length}`;
      }
         logoutS *= inouttargetredU.length;
      if ((turnZ.length >> (Math.min(inouttargetredU.length, 4))) < 3) {
         inouttargetredU = `${3 / (Math.max(10, inouttargetredU.length))}`;
      }
      bangX -= (parseInt(`${bangX}`) * (bases ? 3 : 5));

            if (isFocused && !isRefreshing) {

      storel.set(`${bases}`, (2 * (bases ? 2 : 4)));
   while (4 == (4 >> (Math.min(5, Math.abs(smallsoundh.size)))) || 3 == (4 >> (Math.min(3, Math.abs(storel.size))))) {
      smallsoundh = new Map([[`${miniO}`, parseInt(`${liveK}`)]]);
      break;
   }
       let container3 = 2;
      if (1 < (2 ^ container3) || 2 < (container3 ^ container3)) {
          let arrowK: Map<any, any> = new Map([[String.fromCharCode(101,95,49,49,95,112,105,120,99,116,120,0),398], [String.fromCharCode(106,111,98,95,113,95,52,52,0),917]]);
          let imagenetworkerrX: Array<any> = [79, 247, 281];
          let disconnectedlogoC = String.fromCharCode(98,111,111,116,95,103,95,54,51,0);
          let statisticsactiveV = String.fromCharCode(119,95,49,49,95,115,117,98,98,108,111,99,107,115,0);
         container3 %= Math.max(3, 3);
         arrowK = new Map([[`${arrowK.size}`, disconnectedlogoC.length << (Math.min(Math.abs(1), 3))]]);
         imagenetworkerrX.push(disconnectedlogoC.length);
         statisticsactiveV += "1";
      }
      while (3 == container3) {
         container3 %= Math.max(4, container3 ^ container3);
         break;
      }
         container3 /= Math.max(5, container3);
      iconlogoutC += `${parseInt(`${liveK}`) >> (Math.min(Math.abs(1), 1))}`;
      bases = iconlogoutC.length < 23;
                handleRefresh();

       let hookn = true;
      while (hookn) {
          let package_8b: Map<any, any> = new Map([[String.fromCharCode(100,95,53,55,95,115,116,97,114,116,101,100,0),342], [String.fromCharCode(107,95,49,50,95,100,99,97,109,97,116,104,0),51], [String.fromCharCode(113,115,111,114,116,95,117,95,52,48,0),94]]);
         hookn = (36 > ((!hookn ? package_8b.size : 71) % (Math.max(package_8b.size, 6))));
         break;
      }
       let iconorientation1: Array<any> = [String.fromCharCode(98,95,54,56,95,99,111,117,108,100,0), String.fromCharCode(110,95,49,55,95,110,101,101,100,115,0), String.fromCharCode(102,95,50,53,95,106,115,105,109,100,101,120,116,0)];
       let readw: Array<any> = [670, 159];
      if (readw.length > 1) {
          let moonc = String.fromCharCode(98,95,54,51,95,110,111,99,104,101,99,107,0);
          let notificationfilledV = false;
         readw = [3];
         moonc = `${moonc.length}`;
         notificationfilledV = moonc == moonc;
      }
      bangX *= ((bases ? 2 : 3));
   for (let v = 0; v < 1; v++) {
      whiteo = `${parseInt(`${bangX}`)}`;
   }
   if ((miniO | 3) >= 5 && 2 >= (3 - miniO)) {
      miniO ^= 2;
   }
   if ((5 << (Math.min(3, Math.abs(storel.size)))) < 2 && 5 < storel.size) {
      imagemanagerc = (homeloadingB.length ^ storel.size) == 5;
   }

                setPressTabScroll(true);

      bangX += smallsoundh.size;
   let defaultplayerimgx = liveK <= 9825331.0;
   do {
       let smalla = 3;
       let thumbnail_: Map<any, any> = new Map([[String.fromCharCode(113,105,110,116,102,108,111,97,116,95,97,95,53,0),678], [String.fromCharCode(112,111,114,116,95,112,95,51,51,0),831], [String.fromCharCode(103,95,54,56,95,115,101,116,115,104,97,114,101,0),716]]);
      for (let i = 0; i < 1; i++) {
         smalla <<= Math.min(4, Math.abs(thumbnail_.size));
      }
         smalla |= smalla / (Math.max(thumbnail_.size, 1));
      if ((smalla | thumbnail_.size) < 5 || (thumbnail_.size | 5) < 2) {
         thumbnail_.set(`${smalla}`, thumbnail_.size);
      }
       let v_hashY = true;
         smalla ^= smalla;
      let libreanimatedW = 8746961 >= thumbnail_.size;
      do {
         thumbnail_.set(`${v_hashY}`, 3 + smalla);
         if (libreanimatedW) {
            break;
         }
      } while ((4 <= (smalla >> (Math.min(Math.abs(thumbnail_.size), 5))) || (smalla >> (Math.min(Math.abs(thumbnail_.size), 2))) <= 4) && libreanimatedW);
      liveK += parseInt(`${bangX}`);
      if (defaultplayerimgx) {
         break;
      }
   } while (defaultplayerimgx && (miniO <= 3));
   while (4 <= smallsoundh.size) {
       let notificationfilledG = String.fromCharCode(121,95,57,50,95,98,115,119,97,112,100,115,112,0);
       let z_unlock3 = true;
       let signinupw = String.fromCharCode(106,105,103,103,108,101,95,101,95,56,56,0);
      let sendP = signinupw.length <= 8918961;
      do {
         signinupw = "3";
         if (sendP) {
            break;
         }
      } while ((!z_unlock3) && sendP);
      while (z_unlock3) {
         z_unlock3 = notificationfilledG.length <= 49;
         break;
      }
       let smallu = String.fromCharCode(104,95,49,52,95,115,117,109,109,97,114,121,0);
          let signinupM: Map<any, any> = new Map([[String.fromCharCode(112,95,54,57,95,99,112,112,108,105,110,116,0),true ], [String.fromCharCode(115,99,104,101,100,117,108,105,110,103,95,98,95,50,55,0),true ]]);
         smallu = `${3 * smallu.length}`;
         signinupM = new Map([[`${signinupM.size}`, signinupM.size % 3]]);
          let iconnointernetv = 1.0;
         smallu += `${signinupw.length & parseInt(`${iconnointernetv}`)}`;
      for (let q = 0; q < 1; q++) {
          let area0 = false;
          let airbnbstar3: Array<any> = [917, 406];
          let otherx = String.fromCharCode(121,95,57,53,95,109,105,110,117,116,101,0);
         smallu += `${airbnbstar3.length}`;
         area0 = ((otherx.length * (area0 ? 52 : otherx.length)) >= 52);
         airbnbstar3.push(otherx.length);
      }
      let orangeuparrowH = signinupw.length <= 5518644;
      do {
         signinupw = `${signinupw.length}`;
         if (orangeuparrowH) {
            break;
         }
      } while ((!z_unlock3) && orangeuparrowH);
         z_unlock3 = !notificationfilledG.includes(`${z_unlock3}`);
          let annert = 0;
          let subinE = 4;
          let r_countz = 0.0;
         z_unlock3 = notificationfilledG.length >= 20;
         annert %= Math.max(3, annert << (Math.min(Math.abs(1), 1)));
         subinE &= parseInt(`${r_countz}`) / (Math.max(1, subinE));
         r_countz -= 3 - subinE;
      smallsoundh.set(`${liveK}`, signinupw.length ^ 2);
      break;
   }
      liveK -= 3 + parseInt(`${liveK}`);

                miniVodListRef.current?.scrollToIndex({
                    index: 0,
                    animated: true,
                });

   let iconnointernetF = 7234766 >= miniO;
   do {
      miniO <<= Math.min(4, homeloadingB.length);
      if (iconnointernetF) {
         break;
      }
   } while (iconnointernetF && ((miniO | 4) >= 2));
   let combine6 = smallsoundh.size <= 8778247;
   do {
       let runtimeschedulerI = 4.0;
       let libfabricjniL: Array<any> = [316, 553];
       let unewsz = false;
       let webviewX = 4;
         unewsz = 82 > webviewX;
      while ((webviewX / 1) > 5) {
         webviewX %= Math.max(5, webviewX % 3);
         break;
      }
      for (let t = 0; t < 2; t++) {
         webviewX %= Math.max(4, 1);
      }
         unewsz = (parseInt(`${runtimeschedulerI}`) / (Math.max(2, libfabricjniL.length))) < 97;
      if (2.86 >= (runtimeschedulerI + libfabricjniL.length) || 4 >= (libfabricjniL.length * 1)) {
         libfabricjniL = [parseInt(`${runtimeschedulerI}`)];
      }
      let icondownimg8 = webviewX >= 8371672;
      do {
         webviewX |= (webviewX - (unewsz ? 2 : 3));
         if (icondownimg8) {
            break;
         }
      } while (icondownimg8 && ((3 * libfabricjniL.length) < 2 && (webviewX * 3) < 5));
         webviewX >>= Math.min(Math.abs(3 - libfabricjniL.length), 4);
         webviewX -= parseInt(`${runtimeschedulerI}`) - 1;
         unewsz = webviewX < parseInt(`${runtimeschedulerI}`);
          let greenarrowupV = String.fromCharCode(112,95,50,51,95,119,105,110,115,111,99,107,0);
          let handlerJ = 5.0;
         libfabricjniL.push(1);
         greenarrowupV += `${parseInt(`${handlerJ}`)}`;
         handlerJ -= parseFloat(`${greenarrowupV.length}`);
      let half2 = 5992069.0 <= runtimeschedulerI;
      do {
         runtimeschedulerI += ((unewsz ? 1 : 4) / (Math.max(parseInt(`${runtimeschedulerI}`), 4)));
         if (half2) {
            break;
         }
      } while ((4.47 >= runtimeschedulerI) && half2);
      for (let w = 0; w < 2; w++) {
          let successV = 1.0;
          let predictionarrowT = 5;
          let whatsappW = true;
         libfabricjniL = [parseInt(`${runtimeschedulerI}`) + 2];
         successV += (parseFloat(`${parseInt(`${successV}`) * (whatsappW ? 3 : 1)}`));
         predictionarrowT >>= Math.min(3, Math.abs(predictionarrowT - 1));
         whatsappW = parseFloat(`${predictionarrowT}`) <= successV;
      }
      smallsoundh = new Map([[`${smallsoundh.size}`, smallsoundh.size & 1]]);
      if (combine6) {
         break;
      }
   } while (combine6 && (smallsoundh.get(`${liveK}`) == null));
   while (3.46 < (4.22 * liveK) || (storel.size * 5) < 3) {
      liveK += parseInt(`${liveK}`) >> (Math.min(Math.abs(storel.size), 3));
      break;
   }
      matchu = `${matchu.length >> (Math.min(Math.abs(2), 5))}`;

                

      matchu += `${parseInt(`${bangX}`) >> (Math.min(homeloadingB.length, 4))}`;
      imagemanagerc = homeloadingB.includes(liveK);
   while (2.78 == (liveK * 1.11) || (iconlogoutC.length * 4) == 3) {
       let penaltyT = String.fromCharCode(97,98,98,114,95,57,95,55,56,0);
       let collectiond: Array<any> = [682, 434, 685];
       let pushK: Map<any, any> = new Map([[String.fromCharCode(120,95,51,55,95,112,114,101,102,101,114,114,101,100,0),662], [String.fromCharCode(111,98,106,101,99,116,95,120,95,53,48,0),21]]);
       let cornershoot0: Map<any, any> = new Map([[String.fromCharCode(119,95,56,56,95,98,97,116,99,104,101,115,0),142], [String.fromCharCode(111,95,55,55,95,112,117,108,115,101,97,117,100,105,111,115,121,109,98,111,108,116,97,98,108,101,0),40], [String.fromCharCode(121,95,53,53,95,114,101,97,114,0),244]]);
      if ((1 % (Math.max(4, pushK.size))) >= 4 || (1 % (Math.max(10, penaltyT.length))) >= 5) {
          let typingX = String.fromCharCode(108,101,97,102,110,111,100,101,95,100,95,50,52,0);
          let uimanagerW = 4.0;
         pushK = new Map([[`${pushK.size}`, cornershoot0.size]]);
         typingX = `${typingX.length ^ parseInt(`${uimanagerW}`)}`;
         uimanagerW += parseFloat(`${typingX.length}`);
      }
       let minimizex = String.fromCharCode(112,114,101,115,101,110,116,101,114,95,50,95,57,48,0);
       let linec = String.fromCharCode(111,95,50,50,95,118,105,111,108,97,116,105,111,110,0);
      let loginsuccessh = 6974901 >= collectiond.length;
      do {
          let umeng7 = String.fromCharCode(118,95,50,54,95,112,97,115,115,101,100,0);
          let photo8 = 1.0;
          let agreementM = String.fromCharCode(112,108,97,99,101,104,111,108,100,101,114,115,95,106,95,53,49,0);
          let topicF = String.fromCharCode(114,95,51,51,95,111,109,105,116,116,105,110,103,0);
         collectiond.push(3);
         umeng7 = `${parseInt(`${photo8}`)}`;
         photo8 *= parseFloat(`${agreementM.length}`);
         agreementM = "3";
         topicF = `${umeng7.length}`;
         if (loginsuccessh) {
            break;
         }
      } while (loginsuccessh && ((5 / (Math.max(3, pushK.size))) <= 2 || (5 / (Math.max(1, collectiond.length))) <= 2));
      while (Array.from(pushK.values()).includes(collectiond.length)) {
          let predictionr = String.fromCharCode(107,95,54,54,95,110,111,104,119,0);
          let profileinactive0 = String.fromCharCode(115,95,52,55,95,100,105,115,109,105,115,115,97,108,0);
          let zhuboI = String.fromCharCode(118,95,52,54,95,111,114,97,110,103,101,0);
          let libreanimated6 = String.fromCharCode(121,95,51,55,95,98,105,116,112,97,99,107,101,100,0);
         collectiond.push(zhuboI.length);
         predictionr += `${predictionr.length}`;
         profileinactive0 = "3";
         zhuboI = `${predictionr.length & profileinactive0.length}`;
         libreanimated6 += `${1 - profileinactive0.length}`;
         break;
      }
         collectiond = [collectiond.length >> (Math.min(Math.abs(1), 3))];
      for (let n = 0; n < 1; n++) {
         penaltyT += `${cornershoot0.size}`;
      }
          let downg = String.fromCharCode(108,111,111,112,105,110,103,95,99,95,50,48,0);
          let assetsr: Array<any> = [965, 461, 2];
         cornershoot0.set(downg, 2 * downg.length);
         assetsr.push(assetsr.length & 1);
          let benefits = 5;
          let homeactiveL = String.fromCharCode(105,110,116,114,105,110,95,104,95,51,50,0);
         collectiond.push(3);
         benefits &= 3 - benefits;
         homeactiveL += `${3 ^ homeactiveL.length}`;
      for (let q = 0; q < 3; q++) {
         linec += `${minimizex.length | 1}`;
      }
      for (let f = 0; f < 3; f++) {
          let layoutq: Array<any> = [939, 212];
          let dplusn = 4.0;
          let upgradeD = String.fromCharCode(108,95,52,55,95,102,114,105,99,116,105,111,110,0);
          let fillZ = String.fromCharCode(114,101,115,111,108,118,101,95,116,95,53,50,0);
          let greytickR = String.fromCharCode(105,110,116,101,114,102,97,99,101,115,95,109,95,52,56,0);
         linec = `${minimizex.length}`;
         layoutq = [fillZ.length];
         dplusn -= parseInt(`${dplusn}`) - fillZ.length;
         upgradeD += `${upgradeD.length}`;
         greytickR += `${layoutq.length}`;
      }
         pushK.set(linec, minimizex.length / 1);
      while (4 < minimizex.length) {
          let rncoree = String.fromCharCode(97,95,55,56,95,100,101,116,97,105,108,101,100,0);
          let indicatorW = String.fromCharCode(116,95,55,57,95,106,115,111,110,114,112,99,0);
          let condensedr = true;
          let watchnowbgy: Map<any, any> = new Map([[String.fromCharCode(104,95,49,53,95,105,100,99,116,0),74], [String.fromCharCode(114,97,112,105,100,95,48,95,50,55,0),512]]);
          let shareb: Map<any, any> = new Map([[String.fromCharCode(118,99,100,97,116,97,95,104,95,49,51,0),46], [String.fromCharCode(105,95,55,54,95,100,111,119,110,0),147]]);
         minimizex = "3";
         rncoree += `${shareb.size}`;
         indicatorW += `${(String.fromCharCode(81,0) == rncoree ? rncoree.length : watchnowbgy.size)}`;
         condensedr = !condensedr || shareb.size > 26;
         watchnowbgy.set(indicatorW, 2 >> (Math.min(3, indicatorW.length)));
         break;
      }
      iconlogoutC += `${cornershoot0.size}`;
      break;
   }
      matchu += "2";
                setTimeout(() => {

   let referrer3 = imagemanagerc ? !imagemanagerc : imagemanagerc;
   do {
       let playercommonX = String.fromCharCode(111,95,52,49,95,115,101,116,102,105,101,108,100,0);
       let libhermesM = 1.0;
       let emojiheart3 = String.fromCharCode(122,95,51,56,95,115,117,98,116,105,116,108,101,0);
       let iconshareo: Array<any> = [String.fromCharCode(109,95,54,57,95,115,98,114,101,115,101,114,118,101,0), String.fromCharCode(117,95,51,57,95,105,110,115,116,97,110,116,105,97,116,101,0), String.fromCharCode(108,95,51,51,95,109,117,108,116,105,112,108,101,114,115,0)];
       let redcirclebgi: Map<any, any> = new Map([[String.fromCharCode(114,117,110,115,95,107,95,56,56,0),203], [String.fromCharCode(106,95,51,95,114,101,115,111,108,118,101,100,0),11], [String.fromCharCode(104,95,52,53,95,100,117,114,105,110,103,0),66]]);
      let activityW = redcirclebgi.size >= 5085954;
      do {
          let catagoryI = String.fromCharCode(114,95,55,55,0);
         redcirclebgi.set(emojiheart3, 3);
         catagoryI += `${catagoryI.length / 2}`;
         if (activityW) {
            break;
         }
      } while ((Array.from(redcirclebgi.keys()).includes(`${libhermesM}`)) && activityW);
          let sharewhiteO = String.fromCharCode(100,105,115,112,97,116,99,104,101,100,95,115,95,57,51,0);
          let basketballtrophy4 = 2.0;
          let colorsu = 1;
         iconshareo.push((playercommonX == String.fromCharCode(85,0) ? playercommonX.length : iconshareo.length));
         sharewhiteO = `${sharewhiteO.length / (Math.max(5, parseInt(`${basketballtrophy4}`)))}`;
         basketballtrophy4 += colorsu;
         colorsu &= 1;
       let mountingY: Map<any, any> = new Map([[String.fromCharCode(101,95,57,49,95,99,104,97,114,99,111,110,118,0),41], [String.fromCharCode(98,95,53,53,95,97,110,116,105,99,111,108,108,97,112,115,101,0),630], [String.fromCharCode(105,95,53,53,95,102,108,101,120,105,98,108,101,0),941]]);
       let static_y3: Map<any, any> = new Map([[String.fromCharCode(118,105,122,105,101,114,95,110,95,51,53,0),885], [String.fromCharCode(108,95,53,48,95,120,109,108,0),291]]);
      if ((2 & playercommonX.length) >= 1 && 2 >= (2 & static_y3.size)) {
          let eactg = 5.0;
         static_y3 = new Map([[`${libhermesM}`, 3 % (Math.max(1, parseInt(`${libhermesM}`)))]]);
         eactg -= parseFloat(`${parseInt(`${eactg}`) * parseInt(`${eactg}`)}`);
      }
         static_y3.set(`${static_y3.size}`, mountingY.size);
         libhermesM += redcirclebgi.size | 1;
         emojiheart3 += `${3 + redcirclebgi.size}`;
          let libreactperfloggerjni0 = String.fromCharCode(108,97,121,115,95,111,95,54,53,0);
          let register_jZ = String.fromCharCode(99,111,109,112,114,101,115,115,101,100,95,121,95,48,0);
         libhermesM *= 1 - static_y3.size;
         libreactperfloggerjni0 += `${(libreactperfloggerjni0 == String.fromCharCode(104,0) ? libreactperfloggerjni0.length : register_jZ.length)}`;
         register_jZ += `${register_jZ.length}`;
         mountingY.set(playercommonX, playercommonX.length);
         redcirclebgi = new Map([[`${mountingY.size}`, (emojiheart3 == String.fromCharCode(69,0) ? emojiheart3.length : mountingY.size)]]);
          let langm = String.fromCharCode(115,101,108,101,99,116,95,106,95,56,56,0);
          let spinnerp: Map<any, any> = new Map([[String.fromCharCode(109,95,52,52,95,115,117,98,116,114,101,101,115,0),183], [String.fromCharCode(120,95,56,52,95,109,101,100,105,117,109,0),27], [String.fromCharCode(112,114,105,110,116,102,95,114,95,51,55,0),125]]);
          let verticale = 4;
         emojiheart3 = `${parseInt(`${libhermesM}`)}`;
         langm += `${verticale}`;
         spinnerp.set(`${verticale}`, spinnerp.size);
          let leftf = String.fromCharCode(107,95,49,48,95,110,105,103,104,116,115,104,111,116,0);
          let exampleimageL = String.fromCharCode(116,109,105,120,95,100,95,56,49,0);
          let suggestiono = String.fromCharCode(97,95,50,56,95,115,108,105,100,101,114,0);
         redcirclebgi.set(`${libhermesM}`, redcirclebgi.size);
         leftf = `${exampleimageL.length}`;
         exampleimageL = "3";
         suggestiono = `${2 ^ leftf.length}`;
      for (let y = 0; y < 3; y++) {
         redcirclebgi.set(playercommonX, (playercommonX == String.fromCharCode(90,0) ? playercommonX.length : iconshareo.length));
      }
      let subout5 = 8279173.0 >= libhermesM;
      do {
          let storeN = 0;
         libhermesM /= Math.max(mountingY.size, 2);
         storeN |= storeN ^ storeN;
         if (subout5) {
            break;
         }
      } while (((libhermesM * 5.46) >= 4.81) && subout5);
         libhermesM /= Math.max(2, redcirclebgi.size ^ parseInt(`${libhermesM}`));
      imagemanagerc = redcirclebgi.size <= 9 || smallsoundh.size <= 9;
      if (referrer3) {
         break;
      }
   } while ((!imagemanagerc || (miniO / 4) <= 4) && referrer3);
   if (smallsoundh.size < miniO) {
       let matchactivey = 5;
       let owngoal3: Array<any> = [526, 939];
       let statsnomoredata4 = String.fromCharCode(111,95,53,55,95,98,101,104,105,110,100,0);
       let whatsappn = String.fromCharCode(114,95,53,56,0);
      while ((whatsappn.length | 4) <= 4) {
         whatsappn = `${matchactivey}`;
         break;
      }
         statsnomoredata4 += `${statsnomoredata4.length / (Math.max(whatsappn.length, 4))}`;
      while (owngoal3.length >= 5) {
         owngoal3.push(statsnomoredata4.length);
         break;
      }
      while (whatsappn.length >= statsnomoredata4.length) {
         statsnomoredata4 += "3";
         break;
      }
      if (statsnomoredata4.length <= owngoal3.length) {
         owngoal3.push(3 & statsnomoredata4.length);
      }
      for (let b = 0; b < 1; b++) {
         owngoal3.push(statsnomoredata4.length);
      }
      for (let c = 0; c < 3; c++) {
         owngoal3.push(3 % (Math.max(3, statsnomoredata4.length)));
      }
          let animationss = false;
         statsnomoredata4 = `${statsnomoredata4.length & 1}`;
         animationss = !animationss;
       let assets8 = String.fromCharCode(105,95,50,57,95,116,111,111,108,98,111,120,0);
      let humidityu = 4920094 >= whatsappn.length;
      do {
         whatsappn = `${owngoal3.length | statsnomoredata4.length}`;
         if (humidityu) {
            break;
         }
      } while ((whatsappn.length >= 2) && humidityu);
      let iconscheduleJ = 8055725 >= owngoal3.length;
      do {
         owngoal3.push(owngoal3.length / (Math.max(whatsappn.length, 10)));
         if (iconscheduleJ) {
            break;
         }
      } while (((owngoal3.length << (Math.min(Math.abs(5), 1))) < 3 || (matchactivey << (Math.min(owngoal3.length, 4))) < 5) && iconscheduleJ);
      for (let z = 0; z < 2; z++) {
         assets8 += "2";
      }
      smallsoundh.set(`${iconpipshrink4}`, iconpipshrink4);
   }
       let showF = false;
          let stringsk = 3.0;
         showF = !showF;
         stringsk *= parseFloat(`${parseInt(`${stringsk}`)}`);
      if (!showF || showF) {
         showF = (!showF ? !showF : showF);
      }
      let xadsdkF = showF ? !showF : showF;
      do {
         showF = !showF;
         if (xadsdkF) {
            break;
         }
      } while ((showF) && xadsdkF);
      bangX *= 3;
      iconlogoutC = "3";
                    setPressTabScroll(false);
                }, 500)
            }
        };
        const unsubscribe = navigation.addListener('tabPress', handleTabPress);
        
        return () => unsubscribe();
    }, [navigation, isFocused, isRefreshing]);


    const handleRefresh = useCallback(async () => {
       let areaH = true;
    let star2 = String.fromCharCode(114,101,103,105,115,116,114,97,116,105,111,110,95,120,95,52,55,0);
    let routerL = true;
    let predictiondefaultH = 5;
    let shootl = 3.0;
    let orangetickq: Map<any, any> = new Map([[String.fromCharCode(105,101,110,116,114,121,95,118,95,55,48,0),88], [String.fromCharCode(115,117,98,112,97,121,108,111,97,100,95,122,95,55,53,0),278]]);
    let stylesT: Map<any, any> = new Map([[String.fromCharCode(105,95,56,53,95,109,111,100,98,0),369], [String.fromCharCode(112,111,108,121,109,111,100,95,57,95,51,48,0),251], [String.fromCharCode(101,114,99,95,48,95,51,51,0),63]]);
    let skip4 = false;
    let redirectG: Map<any, any> = new Map([[String.fromCharCode(102,97,110,116,111,109,95,122,95,57,57,0),817], [String.fromCharCode(97,95,52,57,95,97,114,99,104,105,116,101,99,116,117,114,101,0),103]]);
    let side5 = 3.0;
    let defaultteam1 = 4.0;
    let libreactz = String.fromCharCode(116,101,97,109,115,95,54,95,55,50,0);
   for (let k = 0; k < 2; k++) {
      predictiondefaultH ^= orangetickq.size ^ 2;
   }
   for (let g = 0; g < 2; g++) {
      routerL = !routerL;
   }
      predictiondefaultH += star2.length * 3;
      predictiondefaultH ^= ((areaH ? 4 : 5) * 3);
      predictiondefaultH /= Math.max(5, 2 >> (Math.min(3, Math.abs(predictiondefaultH))));
   let statsO = areaH ? !areaH : areaH;
   do {
      areaH = (redirectG.size * orangetickq.size) == 2;
      if (statsO) {
         break;
      }
   } while ((areaH) && statsO);
       let minivodn: Map<any, any> = new Map([[String.fromCharCode(116,95,52,51,95,98,108,101,101,100,0),252], [String.fromCharCode(118,112,114,105,110,116,102,95,122,95,54,50,0),54]]);
         minivodn.set(`${minivodn.size}`, minivodn.size & minivodn.size);
         minivodn.set(`${minivodn.size}`, minivodn.size | minivodn.size);
         minivodn = new Map([[`${minivodn.size}`, minivodn.size]]);
      areaH = (orangetickq.size + parseInt(`${shootl}`)) <= 99;

        setIsRefreshing(true);

       let unimplementedviewJ: Map<any, any> = new Map([[String.fromCharCode(110,95,55,52,95,117,112,115,104,105,102,116,101,100,0),true ], [String.fromCharCode(101,95,53,50,95,97,114,99,104,105,118,105,110,103,0),false ]]);
       let nbatrophyV = String.fromCharCode(122,95,50,52,95,115,117,112,112,111,114,116,97,98,108,101,0);
      if ((unimplementedviewJ.size % (Math.max(1, nbatrophyV.length))) < 5 || (unimplementedviewJ.size % (Math.max(5, 1))) < 4) {
         nbatrophyV += `${nbatrophyV.length}`;
      }
         nbatrophyV = "1";
       let viewerS = 3.0;
       let helperK = 0.0;
      if (2 < (5 * nbatrophyV.length)) {
         helperK /= Math.max(parseFloat(`${parseInt(`${viewerS}`)}`), 5);
      }
      if (nbatrophyV.startsWith(`${viewerS}`)) {
         nbatrophyV = `${(String.fromCharCode(110,0) == nbatrophyV ? parseInt(`${helperK}`) : nbatrophyV.length)}`;
      }
      let register_kV = viewerS <= 8623523.0;
      do {
         viewerS -= parseFloat(`${parseInt(`${helperK}`)}`);
         if (register_kV) {
            break;
         }
      } while (register_kV && ((unimplementedviewJ.size ^ 1) <= 3 || (1 / (Math.max(7, unimplementedviewJ.size))) <= 2));
      skip4 = !areaH;
       let untick1: Array<any> = [String.fromCharCode(118,95,49,57,95,103,111,116,111,0), String.fromCharCode(108,95,54,54,95,111,102,102,115,101,116,0), String.fromCharCode(115,117,98,99,111,101,102,115,95,106,95,56,57,0)];
       let main_zr = 2.0;
      while (parseInt(`${main_zr}`) <= untick1.length) {
         untick1.push(1 + untick1.length);
         break;
      }
      let downloadd = 8917459.0 >= main_zr;
      do {
         main_zr += 3;
         if (downloadd) {
            break;
         }
      } while (downloadd && (main_zr == untick1.length));
         main_zr *= parseInt(`${main_zr}`);
         main_zr += untick1.length & parseInt(`${main_zr}`);
         main_zr -= 1;
         untick1.push(2);
      predictiondefaultH -= parseInt(`${shootl}`) * parseInt(`${main_zr}`);
       let libreactx = String.fromCharCode(112,114,111,120,105,101,115,95,117,95,49,49,0);
       let nbatrophyR = String.fromCharCode(116,95,56,56,95,108,111,117,112,101,0);
       let previewg: Map<any, any> = new Map([[String.fromCharCode(120,106,112,101,103,95,104,95,54,50,0),false ], [String.fromCharCode(111,112,101,110,109,112,116,95,104,95,49,56,0),false ]]);
      for (let x = 0; x < 1; x++) {
          let danger0 = 2;
          let hometeamfieldg = 0;
          let middlesoundA: Map<any, any> = new Map([[String.fromCharCode(113,95,56,55,95,114,101,118,105,101,119,0),true ], [String.fromCharCode(112,97,115,116,101,108,95,104,95,56,48,0),false ], [String.fromCharCode(102,95,52,48,95,102,105,110,97,108,105,122,101,0),false ]]);
          let showlessw = String.fromCharCode(116,95,54,48,95,103,101,116,112,101,101,114,97,100,100,114,0);
          let xvodD = String.fromCharCode(116,104,114,111,117,103,104,95,52,95,49,53,0);
         nbatrophyR += "1";
         danger0 += 3;
         hometeamfieldg &= 2;
         middlesoundA.set(`${danger0}`, danger0);
         showlessw = `${(showlessw == String.fromCharCode(75,0) ? showlessw.length : xvodD.length)}`;
         xvodD = `${middlesoundA.size}`;
      }
         libreactx = `${libreactx.length | 2}`;
      let sinit_e8M = 5624620 <= nbatrophyR.length;
      do {
         nbatrophyR = `${(nbatrophyR == String.fromCharCode(73,0) ? nbatrophyR.length : libreactx.length)}`;
         if (sinit_e8M) {
            break;
         }
      } while (sinit_e8M && (5 >= (1 & previewg.size)));
         libreactx = `${libreactx.length - 1}`;
      let plashQ = 9169171 >= nbatrophyR.length;
      do {
         nbatrophyR = `${nbatrophyR.length}`;
         if (plashQ) {
            break;
         }
      } while ((nbatrophyR.length > 5) && plashQ);
      let orangetick0 = String.fromCharCode(102,56,112,48,102,105,0) == nbatrophyR;
      do {
         nbatrophyR = `${nbatrophyR.length}`;
         if (orangetick0) {
            break;
         }
      } while ((nbatrophyR.length < libreactx.length) && orangetick0);
         libreactx = `${previewg.size}`;
       let dependenciesH = String.fromCharCode(107,100,102,95,107,95,50,54,0);
      if (nbatrophyR.startsWith(`${previewg.size}`)) {
         nbatrophyR += `${libreactx.length}`;
      }
      star2 += `${parseInt(`${side5}`)}`;
      shootl -= parseInt(`${shootl}`) % 1;
   let delegate_flG = skip4 ? !skip4 : skip4;
   do {
      skip4 = areaH;
      if (delegate_flG) {
         break;
      }
   } while ((!areaH) && delegate_flG);
   while (5 < (predictiondefaultH / (Math.max(4, 9))) && !areaH) {
      predictiondefaultH <<= Math.min(1, Math.abs((star2 == String.fromCharCode(102,0) ? star2.length : orangetickq.size)));
      break;
   }
   while (!areaH) {
      areaH = 2 >= predictiondefaultH;
      break;
   }
        

   while (5.66 >= (side5 * 1.20)) {
      side5 *= parseFloat(`${parseInt(`${side5}`)}`);
      break;
   }
   while (redirectG.get(`${orangetickq.size}`) == null) {
      redirectG = new Map([[`${orangetickq.size}`, orangetickq.size]]);
      break;
   }
   while (5 < (predictiondefaultH + 1)) {
       let halffieldimageE = 0;
       let nbatrophyJ = String.fromCharCode(99,111,110,102,108,105,99,116,105,110,103,95,51,95,51,54,0);
         nbatrophyJ += `${2 >> (Math.min(1, nbatrophyJ.length))}`;
      while (1 <= (nbatrophyJ.length * 5) || (nbatrophyJ.length * 5) <= 1) {
          let apple6 = String.fromCharCode(109,95,49,56,95,100,97,116,97,116,121,112,101,115,0);
          let privilegeh = String.fromCharCode(108,95,54,49,95,101,105,112,0);
          let inouttargetredF = String.fromCharCode(114,111,117,112,95,104,95,54,54,0);
          let turndown8 = false;
          let runtimeu = String.fromCharCode(114,95,56,54,95,115,109,97,108,108,101,115,116,0);
         nbatrophyJ = `${privilegeh.length}`;
         apple6 += `${runtimeu.length % 2}`;
         privilegeh = `${((turndown8 ? 4 : 5))}`;
         inouttargetredF = "2";
         runtimeu += `${(apple6 == String.fromCharCode(106,0) ? inouttargetredF.length : apple6.length)}`;
         break;
      }
      while ((nbatrophyJ.length % (Math.max(5, halffieldimageE))) < 4 || 3 < (halffieldimageE % (Math.max(4, 4)))) {
         nbatrophyJ += `${halffieldimageE * nbatrophyJ.length}`;
         break;
      }
      let basee = String.fromCharCode(110,112,97,95,100,117,48,0) == nbatrophyJ;
      do {
          let libreactperfloggerjnie = 2.0;
          let classes3: Map<any, any> = new Map([[String.fromCharCode(102,95,57,50,95,112,97,103,101,114,0),String.fromCharCode(113,95,55,48,95,112,100,122,112,0)], [String.fromCharCode(120,95,55,56,95,121,99,111,99,103,114,103,98,97,0),String.fromCharCode(109,95,53,48,95,120,119,109,97,0)], [String.fromCharCode(115,117,112,112,114,101,115,115,105,110,103,95,108,95,53,52,0),String.fromCharCode(115,101,99,117,114,101,100,95,50,95,53,0)]]);
          let mbridgeG = false;
          let bellreminderj: Array<any> = [280, 174];
         nbatrophyJ += `${(nbatrophyJ == String.fromCharCode(70,0) ? (mbridgeG ? 2 : 3) : nbatrophyJ.length)}`;
         libreactperfloggerjnie /= Math.max(parseFloat(`${bellreminderj.length % (Math.max(7, classes3.size))}`), 5);
         classes3 = new Map([[`${classes3.size}`, 1 << (Math.min(3, Math.abs(classes3.size)))]]);
         mbridgeG = 76 == bellreminderj.length && classes3.size == 76;
         if (basee) {
            break;
         }
      } while (basee && ((halffieldimageE / (Math.max(10, nbatrophyJ.length))) == 3 || 4 == (halffieldimageE / 3)));
       let orangetickk = String.fromCharCode(115,107,105,112,112,101,100,95,51,95,49,0);
       let sourcew = String.fromCharCode(122,95,52,95,115,101,110,100,0);
      let libavformatF = 6474136 <= orangetickk.length;
      do {
         orangetickk += `${orangetickk.length * 3}`;
         if (libavformatF) {
            break;
         }
      } while ((!orangetickk.startsWith(`${nbatrophyJ.length}`)) && libavformatF);
      routerL = 48.30 > shootl;
      break;
   }
   while (skip4) {
      orangetickq.set(`${shootl}`, parseInt(`${shootl}`) + 3);
      break;
   }
   for (let a = 0; a < 1; a++) {
      areaH = !routerL;
   }
      redirectG = new Map([[`${orangetickq.size}`, 1]]);
   let castingO = 8838519 >= redirectG.size;
   do {
       let malaysia1 = String.fromCharCode(115,112,101,110,100,101,114,95,116,95,55,55,0);
          let profileactives = String.fromCharCode(108,105,98,115,119,105,102,116,95,108,95,49,49,0);
         malaysia1 += `${malaysia1.length * 2}`;
         profileactives += `${profileactives.length * profileactives.length}`;
      for (let c = 0; c < 2; c++) {
          let livep = 2.0;
          let filledL = 0.0;
          let videobufferloading5: Array<any> = [765, 74];
         malaysia1 += "2";
         livep -= 1 - videobufferloading5.length;
         filledL += parseFloat(`${videobufferloading5.length ^ parseInt(`${livep}`)}`);
      }
          let showlessh = 5.0;
          let comment2 = 5.0;
         malaysia1 = `${parseInt(`${comment2}`)}`;
         showlessh /= Math.max(parseFloat(`${parseInt(`${showlessh}`)}`), 3);
         comment2 -= 1 - parseInt(`${showlessh}`);
      redirectG = new Map([[`${redirectG.size}`, redirectG.size % (Math.max(3, 4))]]);
      if (castingO) {
         break;
      }
   } while (castingO && (predictiondefaultH == redirectG.size));
        await refetch();

   let roundw = predictiondefaultH <= 6252942;
   do {
      predictiondefaultH ^= 1;
      if (roundw) {
         break;
      }
   } while (roundw && (!routerL));
   while (!routerL) {
      routerL = star2.length < 97;
      break;
   }
      star2 += `${1 >> (Math.min(Math.abs(parseInt(`${shootl}`)), 5))}`;
   let filedu = 7403603 <= predictiondefaultH;
   do {
      predictiondefaultH <<= Math.min(3, Math.abs(predictiondefaultH));
      if (filedu) {
         break;
      }
   } while (filedu && (1 <= (1 / (Math.max(9, stylesT.size))) || 1 <= (stylesT.size / (Math.max(10, predictiondefaultH)))));
       let darkl: Map<any, any> = new Map([[String.fromCharCode(115,101,110,116,101,110,99,101,95,116,95,56,50,0),796], [String.fromCharCode(115,112,108,97,116,95,105,95,50,48,0),578]]);
       let stylesc: Array<any> = [61, 660];
      let overlaya = darkl.size >= 7667034;
      do {
          let iconpipshrinkj = true;
         darkl = new Map([[`${darkl.size}`, (2 * (iconpipshrinkj ? 5 : 2))]]);
         if (overlaya) {
            break;
         }
      } while (overlaya && (1 <= (darkl.size | 4)));
         stylesc = [darkl.size - stylesc.length];
      if (darkl.size >= 3) {
         stylesc.push(darkl.size);
      }
         darkl.set(`${stylesc.length}`, stylesc.length);
         darkl = new Map([[`${darkl.size}`, 3]]);
      let shootyesgoalw = darkl.size <= 5875497;
      do {
         darkl = new Map([[`${darkl.size}`, darkl.size / (Math.max(1, stylesc.length))]]);
         if (shootyesgoalw) {
            break;
         }
      } while (((stylesc.length >> (Math.min(Math.abs(4), 3))) == 4) && shootyesgoalw);
      stylesT = new Map([[`${defaultteam1}`, predictiondefaultH << (Math.min(4, Math.abs(2)))]]);
      side5 -= parseFloat(`${3}`);
   for (let f = 0; f < 2; f++) {
      areaH = (83 == ((!areaH ? 83 : redirectG.size) | redirectG.size));
   }
        setIsRefreshing(false);

   if (routerL) {
      routerL = String.fromCharCode(105,0) == libreactz && 37.7 <= side5;
   }
   while (!routerL) {
       let math7 = String.fromCharCode(116,104,101,116,97,95,53,95,52,54,0);
       let libswresampleU = 3.0;
       let streamingV = String.fromCharCode(116,109,112,108,95,108,95,55,50,0);
      if (4 == (4 * streamingV.length) || 3.33 == (libswresampleU / (Math.max(parseFloat(`${streamingV.length}`), 2)))) {
         libswresampleU *= parseFloat(`${math7.length}`);
      }
      if ((streamingV.length >> (Math.min(Math.abs(3), 5))) > 5) {
          let homeloadingL = false;
          let basketballhometeamN = 2;
          let inviteX = 3.0;
          let arrow6: Map<any, any> = new Map([[String.fromCharCode(109,118,100,97,116,97,95,100,95,54,57,0),295], [String.fromCharCode(111,99,115,112,105,100,95,53,95,50,50,0),43], [String.fromCharCode(98,95,57,48,95,108,111,103,105,99,97,108,0),146]]);
         streamingV = `${basketballhometeamN}`;
         homeloadingL = !homeloadingL && arrow6.size == 54;
         basketballhometeamN *= 1 | parseInt(`${inviteX}`);
         inviteX += parseInt(`${inviteX}`);
         arrow6.set(`${inviteX}`, arrow6.size * parseInt(`${inviteX}`));
      }
      let thumbnailK = String.fromCharCode(113,104,101,117,109,117,111,114,115,49,0) == math7;
      do {
          let dragcloseu = String.fromCharCode(104,97,100,95,98,95,56,57,0);
          let phone4 = false;
          let icondefaultthumbnail6: Array<any> = [688, 113];
          let pointD = 1.0;
         math7 += `${((phone4 ? 2 : 1) - 2)}`;
         dragcloseu += `${1 & icondefaultthumbnail6.length}`;
         phone4 = 38 >= icondefaultthumbnail6.length;
         pointD *= dragcloseu.length >> (Math.min(Math.abs(3), 3));
         if (thumbnailK) {
            break;
         }
      } while (thumbnailK && (!math7.endsWith(streamingV)));
         libswresampleU += (parseFloat(`${streamingV == String.fromCharCode(116,0) ? parseInt(`${libswresampleU}`) : streamingV.length}`));
      while (1 >= (2 + parseInt(`${libswresampleU}`)) && (2 >> (Math.min(2, streamingV.length))) >= 4) {
          let canceld = 0;
          let statisticsinactive3 = 2.0;
          let trashT: Map<any, any> = new Map([[String.fromCharCode(109,95,49,48,48,95,110,105,107,111,110,0),133], [String.fromCharCode(119,95,53,57,95,101,108,115,0),748]]);
          let submitd = String.fromCharCode(115,105,109,112,108,101,95,53,95,52,51,0);
          let iconlogoutK: Array<any> = [281, 627, 117];
         streamingV = "1";
         canceld <<= Math.min(5, Math.abs(2));
         statisticsinactive3 /= Math.max(parseFloat(`${parseInt(`${statisticsinactive3}`) >> (Math.min(1, Math.abs(3)))}`), 4);
         trashT.set(`${statisticsinactive3}`, trashT.size * 3);
         submitd = `${submitd.length >> (Math.min(5, iconlogoutK.length))}`;
         iconlogoutK = [2 & canceld];
         break;
      }
         streamingV += `${parseInt(`${libswresampleU}`) >> (Math.min(Math.abs(2), 2))}`;
      let playercommonA = math7 == String.fromCharCode(49,117,102,114,103,48,53,113,120,0);
      do {
         math7 += `${streamingV.length | 2}`;
         if (playercommonA) {
            break;
         }
      } while ((1 <= math7.length) && playercommonA);
       let tail7 = 5.0;
      let privatechatbgA = 7771198.0 <= libswresampleU;
      do {
         libswresampleU /= Math.max(3, parseFloat(`${math7.length}`));
         if (privatechatbgA) {
            break;
         }
      } while (privatechatbgA && ((3 * parseInt(`${libswresampleU}`)) > 1 && (math7.length * parseInt(`${libswresampleU}`)) > 3));
      routerL = parseFloat(`${orangetickq.size}`) >= side5;
      break;
   }
      areaH = (predictiondefaultH - orangetickq.size) >= 16;
      shootl += ((areaH ? 4 : 4) & 2);
   while (libreactz.endsWith(`${areaH}`)) {
      libreactz += `${3 - redirectG.size}`;
      break;
   }
   while (!Array.from(orangetickq.values()).includes(stylesT.size)) {
       let watchnowbgY = 5;
       let libswscaleR = 1;
       let floaterI = String.fromCharCode(109,97,108,108,111,99,95,105,95,53,51,0);
       let cornershoot5 = String.fromCharCode(97,95,54,53,95,105,110,115,101,114,116,105,111,110,115,0);
       let long_tpZ = String.fromCharCode(112,97,99,107,101,116,111,117,116,95,113,95,55,54,0);
      if (cornershoot5 != floaterI) {
         floaterI += `${libswscaleR}`;
      }
         long_tpZ = "2";
         libswscaleR >>= Math.min(5, Math.abs(watchnowbgY - long_tpZ.length));
         floaterI += `${watchnowbgY}`;
      let icondefaultthumbnail6p = libswscaleR <= 7465085;
      do {
          let arrowselectdownC = 3.0;
          let klevin0 = String.fromCharCode(115,117,114,114,111,117,110,100,95,51,95,57,49,0);
          let modal9: Map<any, any> = new Map([[String.fromCharCode(98,97,114,99,111,100,101,95,119,95,50,54,0),true ], [String.fromCharCode(106,112,101,103,105,110,116,95,109,95,54,53,0),true ], [String.fromCharCode(108,101,116,115,95,120,95,55,52,0),true ]]);
          let mountingT: Array<any> = [829, 756];
          let stats7: Array<any> = [String.fromCharCode(115,111,97,99,99,101,112,116,95,98,95,55,0), String.fromCharCode(98,97,100,103,101,115,95,110,95,51,50,0)];
         libswscaleR &= watchnowbgY;
         arrowselectdownC += 3 >> (Math.min(Math.abs(parseInt(`${arrowselectdownC}`)), 1));
         klevin0 += `${klevin0.length ^ mountingT.length}`;
         modal9 = new Map([[`${stats7.length}`, stats7.length / (Math.max(3, 5))]]);
         mountingT.push(klevin0.length);
         if (icondefaultthumbnail6p) {
            break;
         }
      } while ((floaterI.includes(`${libswscaleR}`)) && icondefaultthumbnail6p);
         cornershoot5 = `${(floaterI == String.fromCharCode(109,0) ? floaterI.length : cornershoot5.length)}`;
          let register_wi = String.fromCharCode(101,120,116,115,107,95,110,95,51,48,0);
          let selection1 = String.fromCharCode(105,95,53,53,95,112,114,101,101,109,112,104,0);
          let iconeditk = true;
         libswscaleR <<= Math.min(5, Math.abs(long_tpZ.length % (Math.max(register_wi.length, 1))));
         register_wi = `${(selection1.length ^ (iconeditk ? 1 : 4))}`;
         selection1 += `${(String.fromCharCode(76,0) == selection1 ? (iconeditk ? 5 : 2) : selection1.length)}`;
         libswscaleR ^= 3 - long_tpZ.length;
       let routerW = 2.0;
      if ((watchnowbgY ^ floaterI.length) >= 5 && 5 >= (floaterI.length ^ watchnowbgY)) {
          let videocommonI = 5.0;
          let auto_yg_ = String.fromCharCode(116,97,99,107,95,106,95,56,53,0);
          let gifgoalbgC = String.fromCharCode(112,105,99,116,95,117,95,55,0);
         floaterI = `${libswscaleR | 3}`;
         videocommonI *= 1;
         auto_yg_ = `${(gifgoalbgC == String.fromCharCode(52,0) ? gifgoalbgC.length : parseInt(`${videocommonI}`))}`;
      }
      let activityn = String.fromCharCode(98,112,101,0) == long_tpZ;
      do {
          let green_ = String.fromCharCode(110,97,116,117,114,97,108,95,121,95,50,53,0);
         long_tpZ += `${libswscaleR}`;
         green_ += "2";
         if (activityn) {
            break;
         }
      } while (activityn && (2.80 == (long_tpZ.length - routerW) && 2 == (long_tpZ.length - parseInt(`${routerW}`))));
       let mbjscommonS = String.fromCharCode(101,110,99,111,100,101,114,115,95,120,95,49,55,0);
      let borderlesse = long_tpZ.length >= 5592124;
      do {
         long_tpZ += `${2 ^ watchnowbgY}`;
         if (borderlesse) {
            break;
         }
      } while ((routerW >= 5.0) && borderlesse);
      if (floaterI == String.fromCharCode(122,0)) {
          let verticalq = String.fromCharCode(109,97,116,114,105,99,101,115,95,51,95,50,51,0);
         long_tpZ = "1";
         verticalq = `${verticalq.length ^ verticalq.length}`;
      }
          let mintegralq = 1.0;
         libswscaleR >>= Math.min(Math.abs(watchnowbgY), 4);
         mintegralq += parseFloat(`${2 | parseInt(`${mintegralq}`)}`);
      orangetickq.set(`${skip4}`, (1 ^ (skip4 ? 2 : 5)));
      break;
   }
      areaH = skip4;
        return;
    }, []);

    const [flattenedVideos, setFlattenedVideos] = useState(Array<wawaBingPing>);
    const LIMIT = 300;
    const fetchVods = (page: number) => wawaPredictionbuttonFootballtrophy.getListByPage({
        page,
        limit: LIMIT,
    }).then((result) => result.List);

    const { data: videos, isSuccess, hasNextPage, fetchNextPage, isFetchingNextPage, isFetching, refetch } =
        useInfiniteQuery(['watchAnytime'], ({ pageParam = 1 }) => fetchVods(pageParam), {
            getNextPageParam: (lastPage, allPages) => {
                if (lastPage === null) {
                    return undefined;
                }
                const nextPage =
                    lastPage.length === LIMIT ? allPages.length + 1 : undefined;
                return nextPage;
            },
            onSuccess: (data) => {
            }
        });

    useEffect(() => {
        if (videos != undefined) {
            setFlattenedVideos(videos?.pages.flat().filter(x => x));
        }
    }, [videos])

    const checkConnection = useCallback(async () => {
       let build3 = true;
    let chatroombackgroundy = String.fromCharCode(101,95,55,56,95,103,112,111,115,116,102,105,108,116,101,114,0);
    let umeng1 = String.fromCharCode(116,105,109,101,102,105,108,116,101,114,95,108,95,54,55,0);
    let sliderj = String.fromCharCode(99,108,111,115,101,95,51,95,56,56,0);
    let bgvipsportx = 2.0;
    let airbnbstar9 = String.fromCharCode(100,105,97,109,111,110,100,95,56,95,52,49,0);
    let userN = String.fromCharCode(103,95,53,56,95,114,101,99,101,105,118,101,0);
    let dragY = true;
    let snewinterstitialK = 0.0;
    let bgvipxvod7 = 4.0;
    let iconqqg = 2;
    let mbsplashp = 5.0;
    let redgoal_ = 0;
    let zhuboF = 5.0;
      airbnbstar9 = `${3 - parseInt(`${bgvipxvod7}`)}`;
      build3 = iconqqg > 20 && airbnbstar9 == String.fromCharCode(118,0);
      iconqqg -= 1 >> (Math.min(3, Math.abs(iconqqg)));
   let nalyticsW = build3 ? !build3 : build3;
   do {
      build3 = chatroombackgroundy.length > 25;
      if (nalyticsW) {
         break;
      }
   } while (nalyticsW && (build3));
   if (umeng1.endsWith(`${build3}`)) {
      build3 = chatroombackgroundy.length == 71;
   }
       let defaultprofilepicJ = 4.0;
       let playercommonz = 2.0;
       let libfollyN = String.fromCharCode(99,95,56,56,95,101,110,116,105,116,101,115,0);
         defaultprofilepicJ /= Math.max((parseFloat(`${libfollyN == String.fromCharCode(57,0) ? libfollyN.length : parseInt(`${defaultprofilepicJ}`)}`)), 4);
      let showlessR = defaultprofilepicJ >= 7252334.0;
      do {
         defaultprofilepicJ -= parseFloat(`${2 + parseInt(`${playercommonz}`)}`);
         if (showlessR) {
            break;
         }
      } while (showlessR && ((defaultprofilepicJ / (Math.max(8, parseFloat(`${libfollyN.length}`)))) < 5.100));
         playercommonz -= libfollyN.length % (Math.max(6, parseInt(`${playercommonz}`)));
          let changet: Array<any> = [651, 634];
         playercommonz *= parseInt(`${playercommonz}`) * 3;
         changet = [changet.length];
         playercommonz += parseInt(`${playercommonz}`) - libfollyN.length;
         playercommonz += libfollyN.length ^ parseInt(`${playercommonz}`);
          let matchdetailbgr = false;
          let weiboz = 2.0;
          let showc = String.fromCharCode(111,95,49,52,95,105,110,115,116,97,110,116,105,97,116,101,0);
         playercommonz *= parseInt(`${weiboz}`) + parseInt(`${playercommonz}`);
         matchdetailbgr = !matchdetailbgr;
         weiboz += showc.length;
         showc += `${((matchdetailbgr ? 3 : 2))}`;
      if (playercommonz == 4.61) {
         playercommonz += libfollyN.length;
      }
         libfollyN += "2";
      snewinterstitialK *= parseFloat(`${parseInt(`${mbsplashp}`) & parseInt(`${defaultprofilepicJ}`)}`);
      dragY = (62 <= ((build3 ? sliderj.length : 62) * sliderj.length));
       let downarrowJ = String.fromCharCode(112,95,49,55,95,99,97,110,100,105,100,97,116,101,115,0);
       let spinner9 = String.fromCharCode(99,97,98,97,99,95,109,95,54,53,0);
       let libmapbufferjni_ = 1.0;
          let basketballtrophy7: Array<any> = [723, 523];
          let tempnodatagifs = 5.0;
          let arrowright5: Array<any> = [943, 256, 719];
         downarrowJ += `${(spinner9 == String.fromCharCode(107,0) ? spinner9.length : downarrowJ.length)}`;
         basketballtrophy7 = [arrowright5.length >> (Math.min(1, Math.abs(parseInt(`${tempnodatagifs}`))))];
         tempnodatagifs += 1 >> (Math.min(Math.abs(parseInt(`${tempnodatagifs}`)), 4));
         arrowright5.push(2);
          let sharewhite_ = 1.0;
          let inactiveT = String.fromCharCode(120,95,55,48,95,112,101,114,102,111,114,109,97,110,99,101,0);
          let fullr: Array<any> = [String.fromCharCode(103,95,57,57,95,114,101,99,111,114,100,101,100,0), String.fromCharCode(107,95,55,50,95,111,99,116,112,111,105,110,116,0), String.fromCharCode(108,95,55,95,99,111,109,112,111,110,101,110,116,115,0)];
         spinner9 += `${spinner9.length}`;
         sharewhite_ -= (inactiveT == String.fromCharCode(51,0) ? inactiveT.length : parseInt(`${sharewhite_}`));
         fullr.push((String.fromCharCode(54,0) == inactiveT ? inactiveT.length : parseInt(`${sharewhite_}`)));
       let runtimeschedulers = 1.0;
      while (2.81 < (libmapbufferjni_ / (Math.max(3.70, 5))) && (downarrowJ.length << (Math.min(Math.abs(3), 2))) < 2) {
          let rncoreX = 4.0;
         libmapbufferjni_ += parseInt(`${runtimeschedulers}`) & parseInt(`${rncoreX}`);
         break;
      }
      for (let d = 0; d < 3; d++) {
         spinner9 += "3";
      }
      let hookso = 5117969.0 <= runtimeschedulers;
      do {
          let u_playerB = true;
          let modelsr = String.fromCharCode(117,95,52,48,95,112,114,105,109,97,114,105,108,121,0);
          let arrowdowne = String.fromCharCode(103,112,109,100,95,120,95,55,55,0);
          let libavdevicet = 2.0;
          let downarrowk = 0.0;
         runtimeschedulers -= modelsr.length % (Math.max(5, downarrowJ.length));
         u_playerB = !u_playerB;
         modelsr = "2";
         arrowdowne += `${(arrowdowne.length % (Math.max(2, (u_playerB ? 4 : 3))))}`;
         libavdevicet *= parseFloat(`${2 * parseInt(`${libavdevicet}`)}`);
         downarrowk *= parseInt(`${downarrowk}`) & 2;
         if (hookso) {
            break;
         }
      } while ((runtimeschedulers == 4.38) && hookso);
         downarrowJ = `${(downarrowJ == String.fromCharCode(75,0) ? downarrowJ.length : spinner9.length)}`;
         runtimeschedulers *= downarrowJ.length;
      while (runtimeschedulers >= libmapbufferjni_) {
         runtimeschedulers /= Math.max(spinner9.length, 4);
         break;
      }
      bgvipxvod7 /= Math.max(((build3 ? 1 : 5) << (Math.min(chatroombackgroundy.length, 5))), 2);

        const state = await NetInfo.fetch();

   for (let y = 0; y < 3; y++) {
       let reducerE: Map<any, any> = new Map([[String.fromCharCode(119,95,49,56,95,101,116,104,101,114,110,101,116,0),true ], [String.fromCharCode(103,95,53,52,95,119,114,105,116,101,99,104,101,99,107,0),false ], [String.fromCharCode(100,101,116,101,114,109,105,110,105,115,116,105,99,95,99,95,57,51,0),true ]]);
      if ((reducerE.size * 3) == 4) {
          let greytickI: Array<any> = [String.fromCharCode(113,95,50,56,95,115,121,110,99,104,114,111,110,105,122,97,116,105,111,110,0), String.fromCharCode(97,95,52,55,95,115,105,114,105,0)];
          let weather5 = 5.0;
          let greytickv = String.fromCharCode(117,95,51,54,95,99,111,110,102,105,114,109,0);
          let formK = 1;
          let yellowscoreball0 = 1.0;
         reducerE = new Map([[`${formK}`, formK >> (Math.min(Math.abs(parseInt(`${weather5}`)), 4))]]);
         greytickI.push(3);
         weather5 /= Math.max((parseFloat(`${String.fromCharCode(81,0) == greytickv ? parseInt(`${yellowscoreball0}`) : greytickv.length}`)), 1);
         yellowscoreball0 -= 1;
      }
          let change9 = String.fromCharCode(97,98,105,95,51,95,55,48,0);
          let short_llR = true;
          let libjsijniprofilerQ: Map<any, any> = new Map([[String.fromCharCode(99,116,114,95,108,95,55,57,0),String.fromCharCode(108,95,52,53,95,114,101,112,117,98,108,105,115,104,0)], [String.fromCharCode(99,95,52,55,95,115,116,115,99,0),String.fromCharCode(110,95,56,54,95,116,97,100,100,0)], [String.fromCharCode(101,95,52,50,95,111,105,100,97,110,121,0),String.fromCharCode(97,99,107,110,111,119,108,101,100,103,101,100,95,121,95,52,52,0)]]);
         reducerE.set(`${short_llR}`, ((short_llR ? 2 : 4) << (Math.min(Math.abs(libjsijniprofilerQ.size), 1))));
         change9 += `${change9.length - change9.length}`;
         reducerE = new Map([[`${reducerE.size}`, 3 ^ reducerE.size]]);
      chatroombackgroundy = `${umeng1.length & sliderj.length}`;
   }
      userN = `${umeng1.length}`;
   while ((bgvipxvod7 / (Math.max(5.24, 3))) >= 3.84 && 5.24 >= (bgvipxvod7 / (Math.max(chatroombackgroundy.length, 5)))) {
       let videobufferloadingP: Map<any, any> = new Map([[String.fromCharCode(111,98,116,97,105,110,101,114,95,56,95,57,56,0),String.fromCharCode(113,115,118,100,101,105,110,116,95,48,95,53,49,0)], [String.fromCharCode(115,116,100,101,114,114,95,116,95,52,48,0),String.fromCharCode(100,105,115,116,114,97,99,116,105,111,110,95,104,95,53,49,0)], [String.fromCharCode(120,95,56,56,95,115,104,97,112,101,115,0),String.fromCharCode(114,101,109,111,118,101,103,114,97,105,110,95,118,95,52,57,0)]]);
       let eyeclosed = 5;
       let defaultlogoD = String.fromCharCode(119,105,100,101,102,101,108,101,109,95,114,95,49,50,0);
       let arrowrightwithtailw = String.fromCharCode(105,95,57,50,95,109,101,115,115,115,97,103,101,0);
       let with_uu = String.fromCharCode(116,97,105,108,95,102,95,55,0);
          let eighteenu = 3;
          let binga = String.fromCharCode(99,108,101,97,114,101,100,95,52,95,50,50,0);
         eyeclosed /= Math.max(defaultlogoD.length * with_uu.length, 4);
         eighteenu ^= eighteenu ^ binga.length;
         binga = `${binga.length + eighteenu}`;
      if (videobufferloadingP.size >= 2) {
          let tooltipsn = String.fromCharCode(99,97,108,108,111,117,116,95,52,95,53,49,0);
         arrowrightwithtailw += `${tooltipsn.length}`;
      }
          let videovarR = String.fromCharCode(112,95,51,56,95,100,105,115,112,0);
         defaultlogoD += `${defaultlogoD.length}`;
         videovarR += "2";
         arrowrightwithtailw += `${arrowrightwithtailw.length << (Math.min(Math.abs(2), 4))}`;
      bgvipxvod7 /= Math.max(parseInt(`${bgvipxvod7}`) | videobufferloadingP.size, 5);
      break;
   }
       let footballQ = true;
       let bannerq = String.fromCharCode(97,95,54,54,95,114,101,97,108,108,111,99,112,0);
      for (let s = 0; s < 2; s++) {
          let collectionG = String.fromCharCode(98,95,54,48,95,99,99,105,112,0);
         footballQ = bannerq.length > 41;
         collectionG = `${collectionG.length}`;
      }
      if (!footballQ || bannerq.length == 3) {
         bannerq = `${(2 + (footballQ ? 1 : 1))}`;
      }
      if (bannerq.endsWith(`${footballQ}`)) {
         bannerq += `${((footballQ ? 3 : 5) + bannerq.length)}`;
      }
      let defaultprofilepic6 = 8653921 >= bannerq.length;
      do {
         bannerq = `${(bannerq.length - (footballQ ? 5 : 2))}`;
         if (defaultprofilepic6) {
            break;
         }
      } while ((!bannerq.startsWith(`${footballQ}`)) && defaultprofilepic6);
       let pagea = String.fromCharCode(115,105,103,110,101,100,95,122,95,51,57,0);
          let smallbrightnessy = String.fromCharCode(121,95,51,57,95,99,111,110,118,101,110,105,101,110,99,101,0);
          let roomI: Map<any, any> = new Map([[String.fromCharCode(102,105,111,95,118,95,50,51,0),String.fromCharCode(117,95,56,52,95,115,119,97,112,121,118,98,117,102,102,101,114,0)], [String.fromCharCode(98,111,117,110,100,101,100,95,118,95,52,51,0),String.fromCharCode(121,95,53,95,115,101,99,0)]]);
          let libzeus5 = 5.0;
         pagea += `${pagea.length}`;
         smallbrightnessy += `${1 + smallbrightnessy.length}`;
         roomI.set(smallbrightnessy, smallbrightnessy.length);
         libzeus5 -= parseInt(`${libzeus5}`) + roomI.size;
      bgvipxvod7 += iconqqg % 2;
   for (let j = 0; j < 1; j++) {
      snewinterstitialK -= parseFloat(`${iconqqg}`);
   }
       let mimo5 = 4;
       let chartx = 3;
       let fieldY = 4.0;
      for (let i = 0; i < 2; i++) {
         mimo5 <<= Math.min(3, Math.abs(1 % (Math.max(9, parseInt(`${fieldY}`)))));
      }
         mimo5 += 3 % (Math.max(parseInt(`${fieldY}`), 4));
      for (let s = 0; s < 3; s++) {
          let questiconF: Map<any, any> = new Map([[String.fromCharCode(99,97,112,116,105,111,110,95,120,95,51,51,0),768], [String.fromCharCode(110,95,57,53,95,101,120,116,101,110,115,105,111,110,0),370], [String.fromCharCode(99,95,54,51,95,99,116,120,0),808]]);
         chartx &= mimo5;
         questiconF.set(`${questiconF.size}`, questiconF.size);
      }
         chartx /= Math.max(4, 2 * chartx);
      if ((mimo5 * 1) < 1 || (mimo5 * chartx) < 1) {
         chartx %= Math.max(parseInt(`${fieldY}`) & 1, 3);
      }
      while (2.42 > (3.72 + fieldY) && 2.37 > (fieldY + 3.72)) {
         fieldY -= parseFloat(`${chartx - 1}`);
         break;
      }
       let s_positionl = true;
       let orangeclockP = true;
         fieldY -= parseFloat(`${parseInt(`${fieldY}`)}`);
         orangeclockP = chartx >= 34 || 34 >= mimo5;
      bgvipsportx -= parseFloat(`${1 / (Math.max(parseInt(`${bgvipxvod7}`), 7))}`);
       let eyeopent = 3.0;
       let ksadH = String.fromCharCode(108,95,54,50,95,100,120,116,121,0);
         eyeopent += parseInt(`${eyeopent}`) & 1;
         eyeopent -= ksadH.length / 1;
         eyeopent -= ksadH.length;
      for (let g = 0; g < 1; g++) {
         ksadH = `${ksadH.length * 2}`;
      }
          let episodes_ = true;
         eyeopent -= ksadH.length / (Math.max(3, 4));
         episodes_ = (episodes_ ? episodes_ : episodes_);
         ksadH = `${1 + parseInt(`${eyeopent}`)}`;
      build3 = 40.27 == snewinterstitialK || build3;
   if ((mbsplashp - 5.43) < 3.47 || dragY) {
      mbsplashp -= airbnbstar9.length ^ parseInt(`${bgvipxvod7}`);
   }
        const offline = !(state.isConnected && state.isInternetReachable);

   let wifirouterL = userN.length >= 8578888;
   do {
      userN = `${iconqqg}`;
      if (wifirouterL) {
         break;
      }
   } while (wifirouterL && (userN.length >= 3 && !dragY));
      dragY = userN == String.fromCharCode(75,0);
      airbnbstar9 = `${parseInt(`${snewinterstitialK}`)}`;
   while ((4 >> (Math.min(4, Math.abs(redgoal_)))) < 2 || (3.49 * bgvipsportx) < 4.68) {
      bgvipsportx *= parseFloat(`${3}`);
      break;
   }
   while (!build3) {
      iconqqg += parseInt(`${mbsplashp}`);
      break;
   }
   let schedulerH = 8292502 <= umeng1.length;
   do {
      umeng1 += `${1 * umeng1.length}`;
      if (schedulerH) {
         break;
      }
   } while (schedulerH && ((2 + redgoal_) <= 2 && (redgoal_ + 2) <= 3));
   if (3 <= (5 / (Math.max(3, parseInt(`${mbsplashp}`)))) || 5 <= (airbnbstar9.length / (Math.max(7, parseInt(`${mbsplashp}`))))) {
      mbsplashp /= Math.max(iconqqg - sliderj.length, 5);
   }
   if (userN.length < 5) {
       let index4: Map<any, any> = new Map([[String.fromCharCode(115,109,105,109,101,95,57,95,56,57,0),true ], [String.fromCharCode(114,105,110,103,98,97,99,107,95,114,95,50,53,0),false ], [String.fromCharCode(97,95,55,95,108,111,111,112,101,120,105,116,0),false ]]);
          let floatinga: Array<any> = [653, 306];
          let iconnewsshareM = String.fromCharCode(98,95,56,56,95,99,97,110,110,111,116,0);
          let castb: Array<any> = [720, 564];
         index4 = new Map([[`${index4.size}`, iconnewsshareM.length]]);
         floatinga = [3];
         iconnewsshareM = `${floatinga.length}`;
         castb.push(floatinga.length);
      let ksadz = 5849289 <= index4.size;
      do {
          let awayq: Map<any, any> = new Map([[String.fromCharCode(100,97,116,97,108,105,115,116,95,54,95,49,54,0),897], [String.fromCharCode(113,95,52,57,95,109,97,99,114,111,0),920], [String.fromCharCode(100,101,98,117,103,95,108,95,52,55,0),727]]);
         index4.set(`${index4.size}`, index4.size);
         awayq.set(`${awayq.size}`, awayq.size / (Math.max(awayq.size, 6)));
         if (ksadz) {
            break;
         }
      } while (ksadz && ((3 & index4.size) < 1 && (3 & index4.size) < 2));
      let rankR = index4.size >= 9201848;
      do {
         index4.set(`${index4.size}`, index4.size);
         if (rankR) {
            break;
         }
      } while (((index4.size << (Math.min(4, Math.abs(index4.size)))) >= 2) && rankR);
      chatroombackgroundy += `${umeng1.length}`;
   }
        setIsOffline(offline);

   let bggradientg = redgoal_ <= 9452173;
   do {
       let station9 = 3.0;
         station9 += parseInt(`${station9}`);
       let chartv: Map<any, any> = new Map([[String.fromCharCode(100,118,97,117,100,105,111,95,115,95,50,53,0),false ], [String.fromCharCode(98,114,107,116,105,109,101,103,109,95,50,95,56,55,0),true ]]);
       let plashl: Map<any, any> = new Map([[String.fromCharCode(105,103,110,111,114,101,95,114,95,57,52,0),309], [String.fromCharCode(102,111,111,116,101,114,95,102,95,57,50,0),97]]);
      for (let n = 0; n < 2; n++) {
         plashl = new Map([[`${plashl.size}`, 2 * chartv.size]]);
      }
      redgoal_ -= 2 ^ parseInt(`${snewinterstitialK}`);
      if (bggradientg) {
         break;
      }
   } while ((redgoal_ >= 1) && bggradientg);
   let hnewsshareL = userN.length <= 8110532;
   do {
      userN = "2";
      if (hnewsshareL) {
         break;
      }
   } while (hnewsshareL && (redgoal_ <= userN.length));
       let inouttargetredC = 1.0;
       let moonb: Map<any, any> = new Map([[String.fromCharCode(122,95,57,56,95,109,112,101,103,112,105,99,116,117,114,101,0),true ], [String.fromCharCode(98,95,57,52,95,110,101,105,103,104,98,111,117,114,0),true ], [String.fromCharCode(105,95,57,50,95,114,97,109,112,117,112,0),true ]]);
          let gift7 = String.fromCharCode(119,95,56,52,95,112,114,111,116,101,99,116,105,111,110,0);
         inouttargetredC += gift7.length + moonb.size;
         moonb.set(`${inouttargetredC}`, parseInt(`${inouttargetredC}`) ^ 1);
          let editq: Array<any> = [245, 632, 438];
          let borderlessG: Map<any, any> = new Map([[String.fromCharCode(115,116,97,116,101,109,101,110,116,115,95,56,95,49,49,0),true ], [String.fromCharCode(101,118,97,108,117,97,116,111,114,95,121,95,53,0),false ], [String.fromCharCode(97,100,97,112,116,95,112,95,56,50,0),false ]]);
          let sharemodalq = 2.0;
         moonb = new Map([[`${borderlessG.size}`, borderlessG.size]]);
         editq = [parseInt(`${sharemodalq}`) ^ editq.length];
         sharemodalq /= Math.max(2, parseFloat(`${2 << (Math.min(Math.abs(parseInt(`${sharemodalq}`)), 2))}`));
         inouttargetredC /= Math.max(4, parseInt(`${inouttargetredC}`) & moonb.size);
          let inviteC = 1.0;
          let imageactionlive9 = false;
          let fillc = true;
         inouttargetredC -= parseInt(`${inouttargetredC}`) << (Math.min(4, Math.abs(2)));
         inviteC /= Math.max(3, (parseInt(`${inviteC}`) | (imageactionlive9 ? 5 : 2)));
         imageactionlive9 = !fillc;
         fillc = 48.24 <= inviteC;
      for (let i = 0; i < 1; i++) {
          let specL = false;
          let gestureb = 4.0;
          let yellowcirclebgc = String.fromCharCode(102,111,114,101,97,99,104,95,105,95,49,52,0);
         inouttargetredC /= Math.max(4, moonb.size - 3);
         specL = !yellowcirclebgc.startsWith(`${gestureb}`);
         gestureb -= parseFloat(`${3}`);
         yellowcirclebgc += `${yellowcirclebgc.length + 2}`;
      }
      umeng1 += `${iconqqg * parseInt(`${mbsplashp}`)}`;
      snewinterstitialK *= (parseFloat(`${(dragY ? 1 : 4)}`));
      mbsplashp *= iconqqg + 3;
      bgvipsportx /= Math.max(3, parseFloat(`${iconqqg}`));
   for (let w = 0; w < 3; w++) {
      chatroombackgroundy += `${((build3 ? 5 : 4) ^ iconqqg)}`;
   }
   let viewerH = userN == String.fromCharCode(54,97,98,0);
   do {
       let giflivestreamings: Map<any, any> = new Map([[String.fromCharCode(114,101,102,112,105,99,95,100,95,51,48,0),976], [String.fromCharCode(112,114,101,115,115,101,100,95,112,95,50,48,0),340]]);
       let valuesa = 0.0;
       let eyeclosep: Map<any, any> = new Map([[String.fromCharCode(114,95,52,51,95,115,97,109,112,108,101,0),true ], [String.fromCharCode(118,99,97,114,100,95,110,95,52,48,0),true ]]);
         giflivestreamings = new Map([[`${giflivestreamings.size}`, 1 & giflivestreamings.size]]);
          let libreactz = String.fromCharCode(105,102,111,114,109,97,116,95,52,95,51,0);
          let ynewinterstitialu = 4;
         valuesa += parseFloat(`${eyeclosep.size}`);
         libreactz += `${libreactz.length}`;
         ynewinterstitialu ^= (libreactz == String.fromCharCode(87,0) ? libreactz.length : ynewinterstitialu);
          let modelsd = 4.0;
         eyeclosep.set(`${modelsd}`, parseInt(`${modelsd}`));
       let formu = String.fromCharCode(98,111,111,116,104,95,103,95,51,51,0);
       let teamO = String.fromCharCode(105,95,49,51,95,115,105,110,101,119,105,110,0);
         teamO = "2";
      let imageactionlive3 = 9366724.0 >= valuesa;
      do {
         valuesa -= parseFloat(`${parseInt(`${valuesa}`) >> (Math.min(Math.abs(2), 1))}`);
         if (imageactionlive3) {
            break;
         }
      } while (imageactionlive3 && (parseInt(`${valuesa}`) > giflivestreamings.size));
      let fullscreenmax4 = formu == String.fromCharCode(105,56,56,48,104,55,109,53,122,0);
      do {
          let iconsubssuccesst: Array<any> = [625, 472, 465];
          let bdxadsdkL = String.fromCharCode(99,111,110,100,117,99,116,111,114,95,100,95,55,56,0);
          let defaultprofilepic8: Array<any> = [314, 901, 248];
         formu += `${iconsubssuccesst.length / 3}`;
         iconsubssuccesst = [2 & bdxadsdkL.length];
         bdxadsdkL = `${defaultprofilepic8.length << (Math.min(bdxadsdkL.length, 4))}`;
         defaultprofilepic8 = [defaultprofilepic8.length * 1];
         if (fullscreenmax4) {
            break;
         }
      } while (fullscreenmax4 && (teamO != formu));
          let settingsq = 0.0;
         eyeclosep = new Map([[`${eyeclosep.size}`, parseInt(`${settingsq}`)]]);
          let iconeditl = true;
          let proxyF: Array<any> = [245, 450];
          let filed2 = 3.0;
         valuesa += (parseFloat(`${(iconeditl ? 4 : 5) / 2}`));
         iconeditl = proxyF.includes(filed2);
         proxyF = [1 ^ proxyF.length];
         filed2 -= proxyF.length % 2;
      userN = `${((build3 ? 2 : 1) & iconqqg)}`;
      if (viewerH) {
         break;
      }
   } while (viewerH && (!userN.startsWith(`${bgvipxvod7}`)));
        if (!offline) {

      iconqqg -= 2;
       let linej = String.fromCharCode(114,103,101,110,95,106,95,57,55,0);
       let matchactiveg = 3.0;
       let xvode = true;
          let gpayf = 4.0;
          let iconplay4 = String.fromCharCode(97,98,111,114,116,97,98,108,101,95,57,95,50,52,0);
          let klevinu = String.fromCharCode(119,95,49,49,95,106,100,115,97,109,112,108,101,0);
         matchactiveg *= (parseFloat(`${String.fromCharCode(113,0) == linej ? linej.length : parseInt(`${matchactiveg}`)}`));
         gpayf += (String.fromCharCode(54,0) == iconplay4 ? parseInt(`${gpayf}`) : iconplay4.length);
         klevinu = `${(iconplay4 == String.fromCharCode(114,0) ? parseInt(`${gpayf}`) : iconplay4.length)}`;
         linej = `${2 + linej.length}`;
         linej = `${parseInt(`${matchactiveg}`)}`;
         xvode = 46.5 >= matchactiveg || !xvode;
         xvode = !xvode;
          let hometeamfieldY = false;
         matchactiveg -= parseFloat(`${linej.length}`);
         hometeamfieldY = (hometeamfieldY ? hometeamfieldY : hometeamfieldY);
      if (xvode) {
          let megaphoneX = String.fromCharCode(109,95,49,49,95,100,111,119,110,119,97,114,100,0);
          let libjsinspectord: Map<any, any> = new Map([[String.fromCharCode(118,95,51,52,95,99,105,112,104,101,114,0),String.fromCharCode(108,95,56,56,95,109,111,100,105,116,121,0)], [String.fromCharCode(102,95,54,52,95,108,101,114,112,0),String.fromCharCode(114,101,99,111,118,101,114,121,95,57,95,55,54,0)]]);
          let debugR = 4.0;
          let gpayG = 3.0;
         matchactiveg += parseFloat(`${parseInt(`${matchactiveg}`)}`);
         megaphoneX = `${parseInt(`${gpayG}`)}`;
         libjsinspectord = new Map([[`${debugR}`, parseInt(`${debugR}`)]]);
      }
      let mergerx = xvode ? !xvode : xvode;
      do {
          let clearj = String.fromCharCode(99,101,114,116,112,111,108,95,117,95,56,56,0);
          let iconu = String.fromCharCode(112,97,108,109,95,108,95,50,49,0);
          let libcrashsdko: Array<any> = [String.fromCharCode(115,95,56,53,95,118,111,105,112,0), String.fromCharCode(113,95,55,54,95,114,101,109,97,105,110,105,110,103,0), String.fromCharCode(101,95,52,50,95,99,111,108,117,109,110,108,105,115,116,0)];
          let yellowcirclebgr = 1.0;
          let yellowanimationlivej = String.fromCharCode(108,95,52,48,95,116,104,111,117,115,97,110,100,115,0);
         xvode = linej.length <= parseInt(`${matchactiveg}`);
         clearj += `${2 >> (Math.min(5, clearj.length))}`;
         iconu += `${(String.fromCharCode(77,0) == clearj ? libcrashsdko.length : clearj.length)}`;
         libcrashsdko.push(2);
         yellowcirclebgr -= parseFloat(`${1 & yellowanimationlivej.length}`);
         yellowanimationlivej += `${yellowanimationlivej.length}`;
         if (mergerx) {
            break;
         }
      } while (((3.78 / (Math.max(5, matchactiveg))) >= 3.14) && mergerx);
       let libsentryr = false;
      sliderj += `${((xvode ? 1 : 4) - redgoal_)}`;
      snewinterstitialK -= parseFloat(`${iconqqg}`);
      mbsplashp += parseInt(`${bgvipsportx}`);
   for (let i = 0; i < 1; i++) {
       let membershipp = String.fromCharCode(103,114,101,97,116,101,114,95,48,95,53,57,0);
       let bingK: Map<any, any> = new Map([[String.fromCharCode(112,110,105,101,108,115,95,51,95,56,55,0),false ], [String.fromCharCode(112,95,57,55,95,115,104,101,101,114,118,105,100,101,111,100,97,116,97,0),true ]]);
       let closeJ = String.fromCharCode(107,95,57,50,95,97,108,101,114,116,115,0);
       let libreanimatedc = String.fromCharCode(97,101,99,95,53,95,50,53,0);
      for (let k = 0; k < 2; k++) {
          let jingdongk = false;
          let uploads = 4.0;
         libreanimatedc += "2";
         jingdongk = 46.59 > uploads;
         uploads += parseFloat(`${3}`);
      }
       let teamP = String.fromCharCode(99,95,53,53,0);
       let inouttargetyellow5 = String.fromCharCode(116,95,50,95,99,104,97,105,110,115,0);
       let google9 = String.fromCharCode(100,95,49,57,95,115,104,117,102,102,108,101,112,108,97,110,101,115,0);
       let side7 = String.fromCharCode(112,114,101,118,111,117,115,95,54,95,56,54,0);
      let giftg = 5197145 >= closeJ.length;
      do {
          let predictionactiveA = String.fromCharCode(106,95,50,49,95,114,101,115,101,116,117,112,0);
         closeJ += `${membershipp.length >> (Math.min(5, Math.abs(bingK.size)))}`;
         predictionactiveA += `${predictionactiveA.length}`;
         if (giftg) {
            break;
         }
      } while ((closeJ.length < libreanimatedc.length) && giftg);
      if (!google9.includes(`${closeJ.length}`)) {
         google9 += `${google9.length}`;
      }
          let updatesl = true;
          let textinputV = true;
          let ksadD = String.fromCharCode(115,105,103,110,95,121,95,52,48,0);
         side7 = `${(String.fromCharCode(108,0) == membershipp ? teamP.length : membershipp.length)}`;
         updatesl = ksadD.length == 43 || !textinputV;
         textinputV = ((ksadD.length + (!updatesl ? 81 : ksadD.length)) == 81);
         teamP += `${2 + closeJ.length}`;
       let stationsk = String.fromCharCode(97,100,111,98,101,95,57,95,53,54,0);
      while (!side7.includes(`${bingK.size}`)) {
         bingK = new Map([[`${bingK.size}`, membershipp.length]]);
         break;
      }
          let predictionactive1 = 0.0;
         closeJ = `${google9.length}`;
         predictionactive1 -= parseFloat(`${3 << (Math.min(Math.abs(parseInt(`${predictionactive1}`)), 4))}`);
      if (4 == (bingK.size & libreanimatedc.length) && (libreanimatedc.length & 4) == 3) {
          let loadingD: Array<any> = [810, 406, 199];
          let predictionbannersharedt = String.fromCharCode(100,95,54,52,95,116,101,97,109,115,0);
         libreanimatedc += `${teamP.length * predictionbannersharedt.length}`;
         loadingD = [loadingD.length % 1];
         predictionbannersharedt += `${loadingD.length >> (Math.min(Math.abs(2), 2))}`;
      }
       let default_rsx = String.fromCharCode(116,95,56,50,95,99,114,101,100,101,110,116,105,97,108,115,0);
       let appsB = String.fromCharCode(112,111,105,110,116,101,114,115,95,102,95,55,48,0);
      snewinterstitialK += parseFloat(`${parseInt(`${zhuboF}`)}`);
   }
       let downloadb = String.fromCharCode(100,95,54,57,95,103,108,111,98,97,108,0);
       let overlay4 = String.fromCharCode(113,95,53,51,95,99,111,110,118,101,114,115,105,111,110,0);
      if (downloadb.endsWith(overlay4)) {
         overlay4 += `${overlay4.length}`;
      }
      while (downloadb.length <= 3 || overlay4 == String.fromCharCode(115,0)) {
         overlay4 = "2";
         break;
      }
      while (3 <= overlay4.length) {
         downloadb += `${downloadb.length / (Math.max(9, overlay4.length))}`;
         break;
      }
      let referrerj = downloadb == String.fromCharCode(107,102,95,120,110,102,0);
      do {
         downloadb += `${downloadb.length}`;
         if (referrerj) {
            break;
         }
      } while ((overlay4 != String.fromCharCode(90,0)) && referrerj);
       let areaM = true;
         areaM = overlay4 == downloadb;
      build3 = overlay4.endsWith(`${build3}`);
       let soundr: Map<any, any> = new Map([[String.fromCharCode(122,95,49,52,95,99,111,110,110,101,99,116,105,118,105,116,121,0),119], [String.fromCharCode(109,101,115,104,101,115,95,101,95,49,55,0),288], [String.fromCharCode(106,95,49,48,95,115,112,111,105,108,101,114,0),142]]);
       let flipperq = String.fromCharCode(116,95,49,56,95,100,116,109,102,0);
       let rewardw = String.fromCharCode(114,101,100,100,105,116,95,100,95,51,57,0);
         soundr = new Map([[flipperq, (rewardw == String.fromCharCode(110,0) ? rewardw.length : flipperq.length)]]);
      for (let g = 0; g < 3; g++) {
          let hooksD = String.fromCharCode(113,95,56,48,95,116,97,107,101,0);
         rewardw += `${1 | flipperq.length}`;
         hooksD += `${hooksD.length}`;
      }
      while (5 <= (5 | soundr.size) && (soundr.size | 5) <= 5) {
          let arrowupP = String.fromCharCode(116,101,108,101,116,101,120,116,95,111,95,55,49,0);
          let reviewI = 3.0;
         rewardw += "1";
         arrowupP += "2";
         reviewI -= parseInt(`${reviewI}`) / (Math.max(6, arrowupP.length));
         break;
      }
      while ((flipperq.length / (Math.max(4, 4))) <= 4 || (flipperq.length / 4) <= 2) {
         flipperq = `${(String.fromCharCode(116,0) == rewardw ? rewardw.length : soundr.size)}`;
         break;
      }
       let predictionarrowp = 3.0;
       let expiredj = 3.0;
      let templateprocessorD = expiredj >= 6735384.0;
      do {
          let applicationA = String.fromCharCode(108,95,52,48,95,100,117,114,97,116,105,111,110,0);
          let stringsI = String.fromCharCode(117,95,51,51,95,99,111,108,100,0);
          let iconnewsshareg = String.fromCharCode(120,95,53,54,95,114,119,103,116,0);
          let contextM = 3;
         expiredj /= Math.max(3 << (Math.min(5, Math.abs(soundr.size))), 1);
         applicationA += "2";
         stringsI = "2";
         iconnewsshareg = `${iconnewsshareg.length}`;
         contextM /= Math.max(applicationA.length, 3);
         if (templateprocessorD) {
            break;
         }
      } while ((Array.from(soundr.values()).includes(expiredj)) && templateprocessorD);
      let condensedb = 5675188 <= rewardw.length;
      do {
         rewardw += `${(String.fromCharCode(71,0) == flipperq ? soundr.size : flipperq.length)}`;
         if (condensedb) {
            break;
         }
      } while ((flipperq == String.fromCharCode(81,0) || 5 == rewardw.length) && condensedb);
      for (let f = 0; f < 1; f++) {
          let fcdaebecbcbafcdfceaaeccfeacdbZ = 0.0;
         expiredj += 3;
         fcdaebecbcbafcdfceaaeccfeacdbZ += parseFloat(`${1}`);
      }
       let defaultbasketballbg_: Map<any, any> = new Map([[String.fromCharCode(121,95,53,50,95,114,105,118,97,116,101,0),true ], [String.fromCharCode(99,111,108,115,101,116,95,111,95,54,56,0),false ]]);
       let popupJ: Map<any, any> = new Map([[String.fromCharCode(115,97,100,98,95,110,95,51,52,0),160], [String.fromCharCode(104,95,52,48,95,100,105,118,105,100,111,114,115,0),37]]);
      chatroombackgroundy += `${(userN == String.fromCharCode(101,0) ? userN.length : parseInt(`${bgvipsportx}`))}`;
   let configb = snewinterstitialK >= 7413921.0;
   do {
      snewinterstitialK *= parseFloat(`${2}`);
      if (configb) {
         break;
      }
   } while ((4.68 >= (3.37 - bgvipxvod7) || 1.43 >= (3.37 - snewinterstitialK)) && configb);
            handleRefresh();
        }
    }, []);

    useFocusEffect(useCallback(() => {
        if (!settingsReducer.isOffline && settingsReducer.isOffline !== isOffline) {
            setIsOffline(settingsReducer.isOffline);
            handleRefresh();
        } else if (settingsReducer.isOffline) {
            return () => {
                miniVodRef.current?.setPause(true);
                setIsOffline(settingsReducer.isOffline);
            }
        }
    }, [settingsReducer.isOffline]));

    useEffect(() => {
        const subscription = AppState.addEventListener(
            "change",
            handleAppStateChange
        );

        return () => {
            subscription.remove();
        };
    }, [])

    
    const handleAppStateChange = (nextAppState: any) => {
        setIsInBackground(nextAppState !== "active");
    };

    return (
        <ScreenContainer containerStyle={{ paddingLeft: 0, paddingRight: 0, paddingBottom: 10 }}>
            <View style={{ position: 'absolute', top: 0, left: 0, padding: 20, zIndex: 50, width: '100%', flexDirection: 'row', alignItems: 'center' }}>
                <Text style={{ color: '#FFF', fontSize: 20 }}>解说</Text>
            </View>
            {!isOffline &&
                <MiniVideoList
                    ref={miniVodRef}
                    miniVodListRef={miniVodListRef}
                    videos={flattenedVideos}
                    fetchNextPage={fetchNextPage}
                    hasNextPage={hasNextPage}
                    isFetching={isFetching}
                    isFetchingNextPage={isFetchingNextPage}
                    isActive={isFocused && !isInBackground}
                    setCollectionEpisode={(index: number) => { }}
                    handleRefreshMiniVod={handleRefresh}
                    isRefreshing={isRefreshing}
                    isPressTabScroll={isPressTabScroll}
                />
            }
            {isOffline && <NoConnection onClickRetry={checkConnection} />}
        </ScreenContainer>
    )
}

const styles = StyleSheet.create({

})
