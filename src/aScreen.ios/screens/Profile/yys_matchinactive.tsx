import React, { useState } from "react";
import {
  View,
  Text,
  StyleSheet,
  Image,
  Clipboard,
  Platform,
} from "react-native";
import ScreenContainer from "../../components/container/yys_matches";
import { RootStackScreenProps } from "@type";
import { useTheme } from "@react-navigation/native";
import TitleWithBackButtonHeader from "../../components/header/yys_away_predictionloss_header";
import { useSelector } from "@hooks";
import { TouchableOpacity } from "@gorhom/bottom-sheet";
import NotificationModal from "../../components/modal/yys_cornershoot_smallsound";
import { yysHomeinactiveUntick } from "@utility";
import FastImage from "../../components/common/yys_alert_backwhite";
import { ChangeUsernameModal } from "../../components/modal/yys_handler";
import { ChangeReferrerModal } from "../../components/modal/yys_smallorangeman_native";
import { yysIconplaySuggestion } from "@redux";

export default ({ navigation }: RootStackScreenProps<"个人中心">) => {
  const { colors } = useTheme();

  const userState = useSelector<yysIconplaySuggestion>('userReducer');

  const [username, setUsername] = useState(userState.userName);

  const [isShowUsernameModal, setShowUsernameModal] = useState(false);
  const [isShowReferrerModal, setShowReferrerModal] = useState(false);

  const [isDialogOpen, setIsDialogOpen] = useState(false);


  const toggleOverlay = () => {
       let macauV = false;
    let qaagf = String.fromCharCode(106,117,115,116,105,102,105,101,100,95,119,95,52,52,0);
    let sinaI: Array<any> = [110, 966];
    let smallorangemank = String.fromCharCode(102,114,97,109,101,105,110,102,111,95,49,95,50,55,0);
    let iconclosewhitewithbgH = false;
    let indexf = 0.0;
    let materialW: Map<any, any> = new Map([[String.fromCharCode(112,115,121,109,111,100,101,108,95,116,95,51,49,0),974], [String.fromCharCode(99,95,49,54,95,113,112,101,108,0),507]]);
    let playershirti = String.fromCharCode(112,95,49,53,95,118,112,100,97,116,97,0);
    let basketballawayteamU = 0;
    let arrowrightl = String.fromCharCode(100,105,103,105,116,95,122,95,56,49,0);
    let giftn = String.fromCharCode(121,95,50,57,95,99,97,108,99,117,108,97,116,101,100,0);
    let volume8 = String.fromCharCode(104,95,55,95,103,108,111,115,115,0);
    let componentregistryU = 1.0;
    let iconchatsends = 5;
    let basketballmatchdetailbgt = String.fromCharCode(97,116,111,98,111,111,108,95,120,95,49,51,0);
    let reactnavigationD: Array<any> = [690, 822];
   for (let k = 0; k < 3; k++) {
      playershirti = `${(qaagf == String.fromCharCode(49,0) ? (iconclosewhitewithbgH ? 4 : 2) : qaagf.length)}`;
   }
       let reducerD = String.fromCharCode(109,97,116,104,101,115,95,50,95,57,48,0);
      while (reducerD != reducerD) {
          let templateprocessor4 = String.fromCharCode(104,95,56,56,95,112,111,115,116,115,99,97,108,101,0);
          let b_title6 = 1.0;
         reducerD = `${reducerD.length}`;
         templateprocessor4 = `${templateprocessor4.length}`;
         b_title6 /= Math.max(parseFloat(`${templateprocessor4.length}`), 1);
         break;
      }
         reducerD += `${1 ^ reducerD.length}`;
         reducerD = `${reducerD.length}`;
      iconclosewhitewithbgH = qaagf == String.fromCharCode(50,0);
       let giftd = 3.0;
      while ((giftd / (Math.max(8, giftd))) == 5.87 || 3.54 == (giftd / 5.87)) {
          let awayplayerQ = String.fromCharCode(112,97,108,98,97,114,115,95,109,95,49,57,0);
          let libbufferH: Array<any> = [307, 743];
          let encryptorr = true;
         giftd /= Math.max(parseInt(`${giftd}`) ^ 2, 3);
         awayplayerQ = `${1 & libbufferH.length}`;
         libbufferH.push(libbufferH.length);
         encryptorr = !encryptorr && libbufferH.length >= 58;
         break;
      }
      while ((giftd / (Math.max(giftd, 3))) >= 3.3) {
         giftd *= parseInt(`${giftd}`) + 3;
         break;
      }
          let interstitialP: Array<any> = [720, 346];
         giftd -= 1;
         interstitialP.push(1 & interstitialP.length);
      iconclosewhitewithbgH = (!iconclosewhitewithbgH ? !macauV : !iconclosewhitewithbgH);
   for (let g = 0; g < 3; g++) {
      arrowrightl += `${((macauV ? 1 : 4))}`;
   }
   for (let y = 0; y < 3; y++) {
       let updatest = 1.0;
       let mailu = String.fromCharCode(117,95,56,57,95,111,114,103,97,110,105,122,97,116,105,111,110,0);
          let pressures = false;
          let ycopy_jdW: Map<any, any> = new Map([[String.fromCharCode(98,97,110,110,101,114,95,99,95,52,55,0),5], [String.fromCharCode(111,109,101,103,97,95,103,95,50,50,0),867], [String.fromCharCode(122,95,50,95,119,105,107,105,0),800]]);
          let minimizeN = String.fromCharCode(100,117,114,103,101,114,95,52,95,57,55,0);
         updatest -= parseFloat(`${3}`);
         pressures = !pressures && minimizeN.length == 40;
         ycopy_jdW = new Map([[`${ycopy_jdW.size}`, 3]]);
         minimizeN += `${ycopy_jdW.size}`;
          let targetJ: Array<any> = [188, 447];
          let flipperN = String.fromCharCode(119,95,52,56,95,118,112,120,115,116,97,116,115,0);
         updatest += parseFloat(`${targetJ.length & 3}`);
         targetJ = [(String.fromCharCode(51,0) == flipperN ? flipperN.length : flipperN.length)];
      for (let n = 0; n < 1; n++) {
         mailu = `${parseInt(`${updatest}`) % (Math.max(2, 9))}`;
      }
      if (!mailu.includes(`${updatest}`)) {
         mailu = `${3 >> (Math.min(4, mailu.length))}`;
      }
      while ((1.97 - updatest) == 5.12 && 3 == (5 * mailu.length)) {
         updatest *= parseFloat(`${parseInt(`${updatest}`) << (Math.min(Math.abs(1), 5))}`);
         break;
      }
         updatest += parseFloat(`${mailu.length}`);
      volume8 += `${(smallorangemank == String.fromCharCode(99,0) ? sinaI.length : smallorangemank.length)}`;
   }
      volume8 += "3";
       let detailsB = 0;
       let plusu = false;
         plusu = !plusu;
       let castingT: Map<any, any> = new Map([[String.fromCharCode(109,101,109,120,95,104,95,51,53,0),775], [String.fromCharCode(116,95,55,57,95,99,111,110,116,97,105,110,0),650]]);
         plusu = !plusu;
          let tempnodatah = 2.0;
          let arrowdownZ = String.fromCharCode(98,117,105,108,116,105,110,95,121,95,54,53,0);
          let abidetect6 = String.fromCharCode(109,95,50,54,95,105,110,116,101,114,112,111,108,0);
         plusu = tempnodatah == 12.72;
         tempnodatah /= Math.max(1, parseFloat(`${1 ^ abidetect6.length}`));
         arrowdownZ += `${(arrowdownZ == String.fromCharCode(90,0) ? arrowdownZ.length : abidetect6.length)}`;
      if (castingT.get(`${detailsB}`) != null) {
         detailsB *= castingT.size;
      }
         plusu = !plusu;
      volume8 += `${2 ^ detailsB}`;
   while (!giftn.endsWith(`${basketballawayteamU}`)) {
      basketballawayteamU &= (smallorangemank == String.fromCharCode(50,0) ? smallorangemank.length : sinaI.length);
      break;
   }
   let backiconmaske = sinaI.length >= 7685473;
   do {
       let mbbidv = 3;
       let rncoref: Map<any, any> = new Map([[String.fromCharCode(113,95,52,54,95,117,110,97,114,121,0),939], [String.fromCharCode(115,99,97,110,116,97,98,108,101,95,116,95,54,48,0),97]]);
       let pausez = 1.0;
          let zoom3 = String.fromCharCode(100,95,55,55,95,108,101,116,116,101,114,0);
         pausez -= parseFloat(`${zoom3.length}`);
         pausez *= parseFloat(`${2 - mbbidv}`);
          let fillede = false;
          let libimagepipeline0 = String.fromCharCode(97,110,116,105,99,111,108,108,97,112,115,101,95,99,95,56,49,0);
          let libturbomodulejsijniT = false;
         mbbidv *= rncoref.size + mbbidv;
         fillede = !libturbomodulejsijniT;
         libimagepipeline0 = "2";
       let recommendationF: Map<any, any> = new Map([[String.fromCharCode(104,111,109,101,95,104,95,57,53,0),String.fromCharCode(100,105,115,112,111,115,105,116,105,111,110,95,51,95,53,55,0)], [String.fromCharCode(100,95,57,49,95,100,118,100,115,117,98,0),String.fromCharCode(101,99,107,101,121,95,104,95,49,54,0)], [String.fromCharCode(114,115,97,122,95,102,95,55,48,0),String.fromCharCode(120,95,54,54,95,97,110,115,119,101,114,115,0)]]);
       let fullW = 4;
      for (let n = 0; n < 1; n++) {
          let championB: Array<any> = [479, 216, 711];
          let securityE = true;
          let minivodM: Array<any> = [73, 104, 872];
         rncoref.set(`${rncoref.size}`, rncoref.size << (Math.min(Math.abs(2), 4)));
         championB.push(((securityE ? 2 : 5) % (Math.max(championB.length, 2))));
         securityE = (68 > (minivodM.length * (!securityE ? minivodM.length : 68)));
      }
      for (let n = 0; n < 1; n++) {
         pausez *= parseFloat(`${fullW}`);
      }
         rncoref.set(`${recommendationF.size}`, 1);
      let materialS = recommendationF.size >= 5740375;
      do {
         recommendationF.set(`${rncoref.size}`, 3 - rncoref.size);
         if (materialS) {
            break;
         }
      } while (materialS && (5 == (recommendationF.size / 5) && 5 == (recommendationF.size / (Math.max(1, fullW)))));
      sinaI.push(arrowrightl.length * 2);
      if (backiconmaske) {
         break;
      }
   } while ((5 > (sinaI.length ^ giftn.length) && (sinaI.length ^ 5) > 3) && backiconmaske);
   let smallorangemankm = String.fromCharCode(108,100,120,105,50,0) == giftn;
   do {
      giftn += `${(String.fromCharCode(102,0) == giftn ? sinaI.length : giftn.length)}`;
      if (smallorangemankm) {
         break;
      }
   } while ((macauV) && smallorangemankm);
   if (5 <= (materialW.size >> (Math.min(Math.abs(5), 4)))) {
      arrowrightl = `${playershirti.length << (Math.min(Math.abs(2), 3))}`;
   }
   for (let r = 0; r < 1; r++) {
      playershirti += `${parseInt(`${indexf}`) % (Math.max(smallorangemank.length, 8))}`;
   }
   let issubW = qaagf.length <= 9610715;
   do {
      qaagf += `${materialW.size}`;
      if (issubW) {
         break;
      }
   } while (issubW && (sinaI.length > qaagf.length));
      arrowrightl = `${((macauV ? 2 : 2))}`;
   if (!macauV && 3 == volume8.length) {
      macauV = iconclosewhitewithbgH || sinaI.length > 38;
   }
       let libswscalei = String.fromCharCode(100,111,119,110,108,101,102,116,95,116,95,51,53,0);
       let layoutN = 3;
       let loginz: Array<any> = [String.fromCharCode(110,95,54,55,95,112,114,101,115,101,110,116,0), String.fromCharCode(114,95,50,95,116,114,97,110,115,102,101,114,114,101,100,0), String.fromCharCode(110,95,50,57,95,109,112,101,103,100,97,116,97,0)];
      while (2 == layoutN) {
         libswscalei += "3";
         break;
      }
         loginz = [3];
          let emojit = 1;
          let leftY = true;
         layoutN -= 2 << (Math.min(Math.abs(layoutN), 1));
         emojit <<= Math.min(1, Math.abs(emojit));
         leftY = !leftY && emojit >= 54;
      let mbjscommonz = 6323247 <= layoutN;
      do {
         layoutN &= loginz.length & 1;
         if (mbjscommonz) {
            break;
         }
      } while (((layoutN - 1) == 2) && mbjscommonz);
       let anythinkw: Array<any> = [String.fromCharCode(117,95,57,56,95,112,114,105,109,97,114,121,0), String.fromCharCode(116,120,104,97,115,104,95,111,95,50,56,0), String.fromCharCode(121,101,108,108,111,119,95,108,95,53,0)];
       let libavcodeck: Array<any> = [347, 701];
         libswscalei += `${layoutN}`;
       let greenarrowupi = String.fromCharCode(105,112,108,105,109,97,103,101,95,98,95,51,53,0);
       let weatherF = String.fromCharCode(101,121,99,104,97,105,110,95,119,95,50,51,0);
      while (5 <= loginz.length) {
         loginz = [(String.fromCharCode(89,0) == greenarrowupi ? greenarrowupi.length : weatherF.length)];
         break;
      }
         layoutN |= 2 >> (Math.min(3, weatherF.length));
      smallorangemank += `${giftn.length % (Math.max(volume8.length, 7))}`;
   while ((arrowrightl.length / (Math.max(6, materialW.size))) >= 1 && 1 >= (1 / (Math.max(8, materialW.size)))) {
       let topick = 4.0;
          let teamdetailsbg3 = 1.0;
          let animationQ = 5.0;
          let redscoreballA = 2;
         topick *= 2 & parseInt(`${topick}`);
         teamdetailsbg3 /= Math.max(2, redscoreballA << (Math.min(5, Math.abs(1))));
         animationQ += 3;
         redscoreballA -= 2 >> (Math.min(Math.abs(parseInt(`${teamdetailsbg3}`)), 1));
         topick -= parseInt(`${topick}`) / (Math.max(7, parseInt(`${topick}`)));
      while (3.79 <= (topick - 5.99) || 3.29 <= (topick - 5.99)) {
         topick *= parseInt(`${topick}`);
         break;
      }
      materialW = new Map([[`${sinaI.length}`, arrowrightl.length >> (Math.min(Math.abs(3), 2))]]);
      break;
   }
      arrowrightl += `${(String.fromCharCode(105,0) == smallorangemank ? smallorangemank.length : parseInt(`${indexf}`))}`;
      basketballawayteamU &= 3;
       let iconarrowrightblackV: Map<any, any> = new Map([[String.fromCharCode(115,95,53,54,95,109,97,107,101,115,114,112,109,0),878], [String.fromCharCode(116,105,107,101,114,95,120,95,49,48,48,0),165]]);
      let acceptedc = iconarrowrightblackV.size >= 7963457;
      do {
         iconarrowrightblackV.set(`${iconarrowrightblackV.size}`, iconarrowrightblackV.size >> (Math.min(Math.abs(3), 1)));
         if (acceptedc) {
            break;
         }
      } while ((2 > (iconarrowrightblackV.size % (Math.max(10, iconarrowrightblackV.size)))) && acceptedc);
         iconarrowrightblackV = new Map([[`${iconarrowrightblackV.size}`, 2]]);
         iconarrowrightblackV = new Map([[`${iconarrowrightblackV.size}`, 3]]);
      macauV = !macauV;
   let teamA = iconclosewhitewithbgH ? !iconclosewhitewithbgH : iconclosewhitewithbgH;
   do {
      iconclosewhitewithbgH = 22 > qaagf.length;
      if (teamA) {
         break;
      }
   } while ((giftn.startsWith(`${iconclosewhitewithbgH}`)) && teamA);
      giftn = `${sinaI.length >> (Math.min(qaagf.length, 3))}`;

    setIsDialogOpen(!isDialogOpen);
  };

  const onPressUsername = () => {
       let sharedE = String.fromCharCode(99,117,115,116,111,109,105,122,101,95,112,95,57,49,0);
    let singaporea: Array<any> = [23, 373];
    let modew = 4.0;
    let dycreatori = String.fromCharCode(117,95,50,57,95,112,114,111,112,111,114,116,105,111,110,97,108,0);
    let n_imageM = String.fromCharCode(104,95,56,53,95,101,120,116,101,110,115,105,98,108,101,0);
    let iconwatchnowx = 3.0;
    let bufferI: Array<any> = [472, 949];
    let airbnbstarq: Map<any, any> = new Map([[String.fromCharCode(109,117,114,109,117,114,95,111,95,54,49,0),794], [String.fromCharCode(108,95,55,48,95,115,119,114,101,115,97,109,112,108,101,114,101,115,0),173]]);
    let profilej = String.fromCharCode(105,95,51,55,95,116,104,114,101,97,100,101,100,0);
    let iconarrowleftM = false;
    let icondownimg9 = String.fromCharCode(117,95,49,57,95,114,97,116,101,99,116,114,108,0);
    let yellowY = 2.0;
   while (sharedE.includes(`${singaporea.length}`)) {
       let resendP = String.fromCharCode(114,95,52,95,98,101,99,97,117,115,101,0);
       let bingD = 0;
         resendP = "2";
      while (4 == resendP.length) {
         bingD += 3;
         break;
      }
      if (3 <= (bingD % (Math.max(resendP.length, 2)))) {
          let bannero: Array<any> = [String.fromCharCode(101,120,112,101,99,116,115,95,97,95,51,55,0), String.fromCharCode(112,95,56,52,95,112,97,115,115,102,98,0)];
          let typingP = String.fromCharCode(113,95,50,52,95,100,101,114,105,118,0);
          let downloaderu = String.fromCharCode(99,95,52,50,95,110,111,110,99,101,0);
         bingD += 3;
         bannero.push(2);
         typingP += `${typingP.length}`;
         downloaderu += `${typingP.length >> (Math.min(4, bannero.length))}`;
      }
      for (let p = 0; p < 3; p++) {
         bingD >>= Math.min(1, Math.abs(bingD % (Math.max(resendP.length, 2))));
      }
      while (2 < (bingD >> (Math.min(resendP.length, 2)))) {
          let libbufferx: Array<any> = [425, 357, 914];
          let usernamej = 4.0;
          let about0 = false;
          let modale = true;
          let philippinesr: Map<any, any> = new Map([[String.fromCharCode(114,116,114,105,109,95,99,95,49,53,0),832], [String.fromCharCode(99,95,49,95,114,101,99,111,110,105,110,116,114,97,0),912], [String.fromCharCode(115,95,51,54,95,97,108,116,101,114,110,97,116,105,111,110,0),488]]);
         resendP = `${parseInt(`${usernamej}`) & 3}`;
         libbufferx = [(libbufferx.length % (Math.max(9, (about0 ? 4 : 3))))];
         usernamej += ((about0 ? 5 : 5) % (Math.max(1, 9)));
         modale = libbufferx.length >= 33;
         philippinesr.set(`${about0}`, 3 >> (Math.min(2, Math.abs(philippinesr.size))));
         break;
      }
      if (4 > (bingD >> (Math.min(Math.abs(1), 5)))) {
         bingD += (String.fromCharCode(65,0) == resendP ? resendP.length : bingD);
      }
      singaporea.push(parseInt(`${iconwatchnowx}`) - bingD);
      break;
   }
   if (4 > (parseInt(`${iconwatchnowx}`) + dycreatori.length) && 4.23 > (4.43 + iconwatchnowx)) {
       let giftk = String.fromCharCode(97,108,115,97,115,121,109,98,111,108,116,97,98,108,101,95,104,95,52,52,0);
       let linkH = String.fromCharCode(111,95,54,50,0);
       let graphicsT: Array<any> = [647, 625];
       let actionb: Map<any, any> = new Map([[String.fromCharCode(104,95,52,50,95,97,100,100,105,116,105,118,101,0),230], [String.fromCharCode(121,95,56,54,0),678]]);
      while (3 <= (graphicsT.length ^ 2)) {
         linkH += `${actionb.size}`;
         break;
      }
       let sinaw = String.fromCharCode(99,95,57,54,95,112,108,97,121,0);
          let condensedH: Map<any, any> = new Map([[String.fromCharCode(105,95,51,49,95,121,117,118,0),71], [String.fromCharCode(99,95,49,51,95,102,109,105,120,0),161], [String.fromCharCode(97,100,100,114,101,115,115,101,115,95,108,95,52,56,0),578]]);
          let weiboO = 2.0;
         graphicsT.push(graphicsT.length ^ actionb.size);
         condensedH.set(`${weiboO}`, parseInt(`${weiboO}`) >> (Math.min(Math.abs(3), 3)));
      if (4 < (actionb.size & sinaw.length) && 4 < (sinaw.length & actionb.size)) {
         actionb.set(`${sinaw}`, (sinaw == String.fromCharCode(83,0) ? actionb.size : sinaw.length));
      }
      let libavdeviceu = linkH == String.fromCharCode(122,112,95,110,0);
      do {
         linkH += "3";
         if (libavdeviceu) {
            break;
         }
      } while (libavdeviceu && (!linkH.startsWith(`${graphicsT.length}`)));
         giftk += `${graphicsT.length >> (Math.min(Math.abs(3), 1))}`;
          let championh = String.fromCharCode(122,95,54,52,95,113,116,97,98,108,101,0);
          let gradlewM = String.fromCharCode(98,114,117,115,104,101,115,95,103,95,56,0);
          let chartC = String.fromCharCode(102,95,53,48,95,105,110,118,111,99,97,116,105,111,110,0);
         giftk += `${gradlewM.length * 3}`;
         championh = `${championh.length * chartC.length}`;
         gradlewM += `${chartC.length % (Math.max(9, championh.length))}`;
      for (let a = 0; a < 1; a++) {
         actionb = new Map([[`${graphicsT.length}`, graphicsT.length]]);
      }
       let middlesound8 = String.fromCharCode(100,105,114,101,99,116,111,114,121,95,49,95,55,55,0);
       let show8 = String.fromCharCode(115,121,109,98,111,108,95,53,95,57,50,0);
          let shootyesgoalL = String.fromCharCode(114,95,51,56,95,118,101,99,115,0);
          let dplusN: Map<any, any> = new Map([[String.fromCharCode(105,111,115,117,114,102,97,99,101,95,99,95,57,50,0),667], [String.fromCharCode(115,95,49,54,95,105,110,115,101,116,0),997], [String.fromCharCode(98,121,114,121,105,95,56,95,51,52,0),569]]);
          let debug0 = String.fromCharCode(120,95,56,53,95,105,110,118,111,99,97,116,105,111,110,0);
         middlesound8 += `${shootyesgoalL.length % (Math.max(4, middlesound8.length))}`;
         shootyesgoalL = `${1 << (Math.min(3, Math.abs(dplusN.size)))}`;
         dplusN.set(debug0, debug0.length | dplusN.size);
       let whitesmalltickt = String.fromCharCode(116,95,52,52,95,101,99,116,97,110,103,108,101,0);
         show8 += `${3 - middlesound8.length}`;
      dycreatori = `${((iconarrowleftM ? 5 : 4))}`;
   }
       let iconarrowrightblack8 = true;
      if (!iconarrowrightblack8) {
          let screeni = 0.0;
          let rncored = String.fromCharCode(97,100,97,112,116,111,114,95,116,95,57,55,0);
          let placementM: Array<any> = [233, 175, 916];
         iconarrowrightblack8 = screeni >= 54.20;
         screeni *= placementM.length >> (Math.min(Math.abs(3), 1));
         rncored += `${placementM.length}`;
      }
      while (iconarrowrightblack8 || iconarrowrightblack8) {
          let dropdownR = String.fromCharCode(97,99,111,108,111,114,115,95,54,95,50,53,0);
          let iconarrowrightorange_ = true;
          let eactq = 5.0;
          let tickE = String.fromCharCode(108,97,116,101,95,55,95,57,51,0);
          let iconnewchatH = String.fromCharCode(113,95,54,52,95,99,97,112,105,116,97,108,115,0);
         iconarrowrightblack8 = eactq < 97.10;
         dropdownR += `${(1 >> (Math.min(3, Math.abs((iconarrowrightorange_ ? 1 : 5)))))}`;
         iconarrowrightorange_ = !iconnewchatH.startsWith(`${iconarrowrightorange_}`);
         eactq -= dropdownR.length;
         tickE += `${iconnewchatH.length}`;
         break;
      }
      if (iconarrowrightblack8 || !iconarrowrightblack8) {
         iconarrowrightblack8 = !iconarrowrightblack8 || !iconarrowrightblack8;
      }
      singaporea = [(n_imageM == String.fromCharCode(56,0) ? singaporea.length : n_imageM.length)];
   let logouser2 = 6237488.0 >= iconwatchnowx;
   do {
      iconwatchnowx *= parseFloat(`${bufferI.length}`);
      if (logouser2) {
         break;
      }
   } while ((iconwatchnowx == 1.92) && logouser2);
      n_imageM += "2";
   while (2 < sharedE.length) {
       let refreshborderlesst = 4.0;
       let hnewsR = 3;
      let schedulep = hnewsR >= 7340576;
      do {
          let rewardvideoT: Map<any, any> = new Map([[String.fromCharCode(112,95,55,48,95,114,111,103,114,97,109,0),String.fromCharCode(97,116,116,114,105,98,115,95,118,95,54,56,0)], [String.fromCharCode(117,95,54,55,95,114,101,109,105,120,105,110,103,0),String.fromCharCode(108,105,98,95,120,95,50,49,0)]]);
          let libtobA = 1.0;
          let moonx = String.fromCharCode(114,95,53,56,95,105,110,115,101,116,115,0);
         hnewsR += 3;
         rewardvideoT = new Map([[`${rewardvideoT.size}`, parseInt(`${libtobA}`) << (Math.min(Math.abs(rewardvideoT.size), 4))]]);
         libtobA /= Math.max(1 >> (Math.min(4, Math.abs(rewardvideoT.size))), 1);
         moonx += `${moonx.length}`;
         if (schedulep) {
            break;
         }
      } while ((3 == (hnewsR * 5) && 2.43 == (parseFloat(`${hnewsR}`) * refreshborderlesst)) && schedulep);
      for (let n = 0; n < 2; n++) {
         refreshborderlesst *= parseFloat(`${parseInt(`${refreshborderlesst}`) ^ hnewsR}`);
      }
      for (let g = 0; g < 1; g++) {
         hnewsR |= hnewsR ^ 3;
      }
      let libreactnativejniH = 6269061 <= hnewsR;
      do {
         hnewsR += 1 * hnewsR;
         if (libreactnativejniH) {
            break;
         }
      } while (libreactnativejniH && (2 > hnewsR));
       let thumbnailT = String.fromCharCode(107,95,57,56,95,99,108,117,116,115,0);
          let umeng8 = 2;
         hnewsR <<= Math.min(4, Math.abs(parseInt(`${refreshborderlesst}`)));
         umeng8 |= umeng8;
      sharedE = "2";
      break;
   }
   for (let m = 0; m < 1; m++) {
       let libapminsighta3 = 0.0;
       let stringy = 0.0;
       let sound5 = String.fromCharCode(115,121,115,114,97,110,100,95,97,95,56,53,0);
       let register_yY = true;
      while (!sound5.includes(`${register_yY}`)) {
         sound5 += `${parseInt(`${stringy}`) ^ parseInt(`${libapminsighta3}`)}`;
         break;
      }
         libapminsighta3 += parseFloat(`${3}`);
         register_yY = sound5.length == 81;
      for (let t = 0; t < 1; t++) {
         stringy /= Math.max(2, parseInt(`${stringy}`));
      }
          let imagenomoredataP: Map<any, any> = new Map([[String.fromCharCode(116,111,100,112,95,115,95,50,48,0),901], [String.fromCharCode(109,95,51,50,95,109,105,103,114,97,116,101,0),95], [String.fromCharCode(102,112,117,116,115,95,108,95,51,57,0),255]]);
          let round8 = 2.0;
         stringy /= Math.max(4, imagenomoredataP.size * 2);
         imagenomoredataP.set(`${round8}`, parseInt(`${round8}`) >> (Math.min(1, Math.abs(parseInt(`${round8}`)))));
         sound5 += `${((register_yY ? 1 : 1) | parseInt(`${stringy}`))}`;
         sound5 = `${parseInt(`${libapminsighta3}`) | 3}`;
         stringy *= 1 & parseInt(`${stringy}`);
      airbnbstarq.set(`${iconwatchnowx}`, (String.fromCharCode(72,0) == sharedE ? parseInt(`${iconwatchnowx}`) : sharedE.length));
   }
       let sheetz = String.fromCharCode(114,101,107,101,121,95,97,95,54,49,0);
       let libcrashsdkn = 1.0;
       let tumbnail9: Map<any, any> = new Map([[String.fromCharCode(99,112,117,102,108,97,103,115,95,109,95,53,0),515], [String.fromCharCode(100,95,56,54,95,98,102,114,97,99,116,105,111,110,0),23]]);
      while (libcrashsdkn < parseFloat(`${sheetz.length}`)) {
         libcrashsdkn *= parseFloat(`${parseInt(`${libcrashsdkn}`)}`);
         break;
      }
      while (2 <= sheetz.length) {
         sheetz = "3";
         break;
      }
         libcrashsdkn -= parseFloat(`${2}`);
         sheetz += `${parseInt(`${libcrashsdkn}`)}`;
         libcrashsdkn -= parseFloat(`${1 ^ tumbnail9.size}`);
      let arrowrightwithtailj = libcrashsdkn >= 5849760.0;
      do {
          let gradlej = String.fromCharCode(116,114,101,102,95,49,95,53,49,0);
          let playi: Array<any> = [492, 812];
          let adultW = String.fromCharCode(118,95,51,49,95,112,97,114,116,115,0);
          let minir = String.fromCharCode(107,101,114,110,101,108,95,99,95,49,49,0);
         libcrashsdkn /= Math.max(parseFloat(`${adultW.length / 1}`), 2);
         gradlej = `${(gradlej == String.fromCharCode(78,0) ? gradlej.length : playi.length)}`;
         playi.push(playi.length);
         adultW = `${2 + minir.length}`;
         minir += `${gradlej.length}`;
         if (arrowrightwithtailj) {
            break;
         }
      } while ((tumbnail9.size > 2) && arrowrightwithtailj);
      let combineE = 5196268.0 >= libcrashsdkn;
      do {
         libcrashsdkn /= Math.max(2, parseFloat(`${tumbnail9.size}`));
         if (combineE) {
            break;
         }
      } while (combineE && (3 > sheetz.length));
         tumbnail9 = new Map([[`${libcrashsdkn}`, 3 ^ parseInt(`${libcrashsdkn}`)]]);
          let notificationfilledq = String.fromCharCode(102,95,54,56,95,116,104,114,101,97,100,109,101,115,115,97,103,101,0);
         tumbnail9.set(sheetz, (sheetz == String.fromCharCode(112,0) ? sheetz.length : tumbnail9.size));
         notificationfilledq = `${notificationfilledq.length}`;
      sharedE += `${(profilej == String.fromCharCode(121,0) ? profilej.length : parseInt(`${iconwatchnowx}`))}`;
      bufferI = [3 & parseInt(`${modew}`)];
   for (let f = 0; f < 3; f++) {
      profilej += `${parseInt(`${modew}`) & singaporea.length}`;
   }
      singaporea = [(parseInt(`${iconwatchnowx}`) << (Math.min(3, Math.abs((iconarrowleftM ? 5 : 2)))))];
   for (let o = 0; o < 2; o++) {
      n_imageM += `${singaporea.length << (Math.min(bufferI.length, 5))}`;
   }
       let langkeyg = String.fromCharCode(121,95,49,55,95,105,110,115,116,114,0);
       let backiconK = 5;
         langkeyg = `${langkeyg.length}`;
      let gifgoalbg0 = 6439669 <= backiconK;
      do {
          let iconfeedbackb = 0.0;
          let castingI = 4.0;
          let iconsaveimageA = false;
          let debugn: Array<any> = [911, 522];
         backiconK += 3 >> (Math.min(3, debugn.length));
         iconfeedbackb -= parseInt(`${castingI}`);
         iconsaveimageA = 53.47 < castingI;
         debugn.push(parseInt(`${castingI}`));
         if (gifgoalbg0) {
            break;
         }
      } while (gifgoalbg0 && (2 < (langkeyg.length << (Math.min(Math.abs(2), 2))) || 2 < (langkeyg.length << (Math.min(Math.abs(2), 4)))));
      dycreatori = `${n_imageM.length * airbnbstarq.size}`;
   while (4.25 < (4.60 / (Math.max(7, iconwatchnowx))) && 4.60 < (iconwatchnowx / (Math.max(parseFloat(`${singaporea.length}`), 10)))) {
      iconwatchnowx /= Math.max((parseFloat(`${sharedE == String.fromCharCode(80,0) ? sharedE.length : profilej.length}`)), 2);
      break;
   }
   if (singaporea.includes(modew)) {
       let over6 = false;
       let leaguedetailsbgc = String.fromCharCode(106,95,54,95,115,117,98,102,105,101,108,100,115,0);
       let executork = false;
         over6 = !over6 && leaguedetailsbgc.length < 41;
       let neon4 = String.fromCharCode(100,111,118,101,95,109,95,51,48,0);
       let activea = String.fromCharCode(109,112,101,103,97,117,100,105,111,100,101,99,104,101,97,100,101,114,95,119,95,53,55,0);
         neon4 += `${(String.fromCharCode(100,0) == neon4 ? neon4.length : activea.length)}`;
         over6 = leaguedetailsbgc.startsWith(`${over6}`);
         executork = !executork && !over6;
         activea = `${(leaguedetailsbgc.length * (executork ? 1 : 4))}`;
          let related6 = 1.0;
         leaguedetailsbgc = `${(activea == String.fromCharCode(86,0) ? (executork ? 1 : 3) : activea.length)}`;
         related6 += parseFloat(`${2 << (Math.min(Math.abs(parseInt(`${related6}`)), 2))}`);
       let bootsplashT = String.fromCharCode(122,95,51,51,95,105,100,99,116,100,115,112,0);
       let arrowselectdowna: Map<any, any> = new Map([[String.fromCharCode(104,95,55,51,95,114,101,116,105,110,97,0),241], [String.fromCharCode(117,112,108,111,97,100,101,114,95,116,95,56,57,0),366]]);
      singaporea = [1 >> (Math.min(1, dycreatori.length))];
   }
   for (let s = 0; s < 2; s++) {
      icondownimg9 += `${dycreatori.length >> (Math.min(Math.abs(1), 3))}`;
   }
      airbnbstarq.set(dycreatori, dycreatori.length << (Math.min(Math.abs(3), 5)));
   for (let e = 0; e < 2; e++) {
      bufferI = [icondownimg9.length];
   }
   for (let h = 0; h < 2; h++) {
      singaporea.push(airbnbstarq.size >> (Math.min(n_imageM.length, 3)));
   }
   if (4 <= n_imageM.length) {
      iconarrowleftM = icondownimg9 == dycreatori;
   }
   for (let m = 0; m < 3; m++) {
      singaporea = [(String.fromCharCode(73,0) == icondownimg9 ? (iconarrowleftM ? 5 : 3) : icondownimg9.length)];
   }
       let values9 = String.fromCharCode(108,105,110,107,115,95,121,95,49,48,0);
       let mbbanner1 = String.fromCharCode(99,111,110,116,114,97,115,116,95,99,95,50,48,0);
         values9 = `${values9.length}`;
      for (let b = 0; b < 2; b++) {
          let analyticZ = String.fromCharCode(118,111,114,98,105,115,102,108,111,97,116,102,108,111,97,116,95,103,95,50,50,0);
          let libswresampleI = 5;
          let greenL = 3;
          let q_unlockB = String.fromCharCode(110,95,52,95,100,114,97,119,117,116,105,108,115,0);
          let read5: Array<any> = [String.fromCharCode(105,95,55,56,95,99,111,112,121,98,97,99,107,0), String.fromCharCode(109,97,105,108,116,111,95,120,95,54,51,0), String.fromCharCode(112,114,105,111,114,95,56,95,57,55,0)];
         values9 = `${analyticZ.length}`;
         analyticZ += `${(q_unlockB == String.fromCharCode(121,0) ? q_unlockB.length : greenL)}`;
         libswresampleI >>= Math.min(3, Math.abs(3 - read5.length));
         greenL *= 1 / (Math.max(8, q_unlockB.length));
         read5.push(2 & libswresampleI);
      }
       let renewU = String.fromCharCode(98,95,53,95,109,107,118,109,117,120,101,114,117,116,105,108,0);
         renewU = `${2 - mbbanner1.length}`;
          let basketballplayerplaceholderm = 0.0;
         mbbanner1 += `${renewU.length / (Math.max(values9.length, 9))}`;
         basketballplayerplaceholderm *= parseInt(`${basketballplayerplaceholderm}`);
         mbbanner1 = `${3 & values9.length}`;
      sharedE = "3";

    setShowUsernameModal(true);
  }

  const onUsernameUpdateSuccess = (newUsername: string) => {
    setUsername(newUsername);
  }

  const onPressReferrer = () => {
       let subbasketballplayerh = String.fromCharCode(114,101,97,115,111,110,95,56,95,50,50,0);
    let iconeditU = 1.0;
    let navigationH = 3;
    let resendP = String.fromCharCode(105,102,105,108,116,101,114,95,55,95,53,0);
    let saveA: Array<any> = [143, 468];
    let iconplay_ = 1.0;
    let actionq = 5.0;
    let profileframeV = 3.0;
    let overlayt = String.fromCharCode(112,95,51,56,95,104,112,101,108,0);
    let yellowscoreballF = 2.0;
    let tooltipsX = String.fromCharCode(110,95,50,53,95,116,101,120,116,97,116,116,114,0);
    let suboutU: Array<any> = [663, 738];
    let settingv: Map<any, any> = new Map([[String.fromCharCode(115,112,97,114,115,101,110,101,115,115,95,106,95,54,53,0),String.fromCharCode(105,109,112,111,114,116,101,114,95,99,95,57,54,0)], [String.fromCharCode(107,95,50,53,95,98,97,110,100,0),String.fromCharCode(119,95,55,56,95,119,117,110,100,101,102,0)], [String.fromCharCode(103,100,97,116,97,95,53,95,52,56,0),String.fromCharCode(100,101,97,100,105,110,101,95,115,95,56,56,0)]]);
    let penaltygoalA: Map<any, any> = new Map([[String.fromCharCode(112,114,111,100,115,95,110,95,55,54,0),716], [String.fromCharCode(115,117,98,116,101,120,116,95,97,95,55,53,0),101]]);
    let play8 = String.fromCharCode(101,118,97,108,117,97,116,101,100,95,113,95,50,57,0);
    let pushP = String.fromCharCode(111,108,100,110,101,119,95,107,95,52,48,0);
   for (let v = 0; v < 3; v++) {
      tooltipsX += `${parseInt(`${iconeditU}`)}`;
   }
      overlayt += `${parseInt(`${yellowscoreballF}`)}`;
       let icontransferclubV = 5.0;
       let yellowj = 2;
       let statisticsinactived = 2.0;
         icontransferclubV *= 2 / (Math.max(yellowj, 1));
          let dragclose1 = 2.0;
          let favoritee = String.fromCharCode(111,114,103,95,98,95,54,54,0);
         statisticsinactived -= favoritee.length & parseInt(`${dragclose1}`);
         icontransferclubV /= Math.max(2, parseInt(`${statisticsinactived}`));
         yellowj <<= Math.min(Math.abs(1 % (Math.max(2, yellowj))), 1);
       let codegenf = false;
      if (statisticsinactived >= 1.86) {
         yellowj -= 2 | parseInt(`${icontransferclubV}`);
      }
          let fillD = 1.0;
          let playershirtk = 1;
          let libglogz: Map<any, any> = new Map([[String.fromCharCode(108,95,50,53,0),true ], [String.fromCharCode(102,108,118,101,110,99,95,115,95,51,53,0),true ], [String.fromCharCode(115,117,98,116,114,97,99,116,101,100,95,118,95,54,57,0),false ]]);
         statisticsinactived -= 3 >> (Math.min(Math.abs(parseInt(`${icontransferclubV}`)), 4));
         fillD *= libglogz.size;
         playershirtk += 3;
         libglogz = new Map([[`${libglogz.size}`, libglogz.size]]);
      for (let q = 0; q < 2; q++) {
         yellowj /= Math.max(3, yellowj);
      }
      if (1 > (2 >> (Math.min(4, Math.abs(yellowj)))) || 4.15 > (icontransferclubV + 1.85)) {
         yellowj %= Math.max(1, (parseInt(`${icontransferclubV}`) * (codegenf ? 2 : 1)));
      }
      saveA = [3];
      subbasketballplayerh = `${3 * parseInt(`${iconeditU}`)}`;
   if ((navigationH - overlayt.length) >= 1 || 3 >= (overlayt.length - 1)) {
       let predictionlossq = true;
       let subouto = String.fromCharCode(101,114,112,105,99,95,110,95,53,53,0);
       let icondownimg5 = String.fromCharCode(117,95,52,50,95,119,101,108,99,104,0);
      let friendsk = predictionlossq ? !predictionlossq : predictionlossq;
      do {
          let mnewsz: Array<any> = [621, 158];
          let profileframeq = String.fromCharCode(99,111,115,116,115,95,99,95,54,55,0);
         predictionlossq = mnewsz.length > profileframeq.length;
         if (friendsk) {
            break;
         }
      } while ((4 > subouto.length && predictionlossq) && friendsk);
          let dicelogom = String.fromCharCode(116,97,107,101,110,95,100,95,57,54,0);
          let yingh = 5.0;
         subouto = `${dicelogom.length & 2}`;
         dicelogom += `${parseInt(`${yingh}`)}`;
      navigationH |= parseInt(`${actionq}`) >> (Math.min(4, Math.abs(navigationH)));
   }
       let basketballmatchdetailbge = String.fromCharCode(110,95,55,54,95,100,105,115,112,97,116,99,104,101,100,0);
       let iconrightorange9 = String.fromCharCode(110,95,54,55,95,100,101,99,111,114,97,116,105,111,110,115,0);
       let libavdevicea: Array<any> = [String.fromCharCode(101,110,117,109,101,114,97,116,111,114,115,95,113,95,49,49,0), String.fromCharCode(109,102,113,101,95,100,95,49,48,0), String.fromCharCode(117,95,51,56,95,116,101,120,116,97,116,116,114,0)];
         iconrightorange9 += `${iconrightorange9.length}`;
          let cornershootr = String.fromCharCode(97,95,51,52,95,102,105,114,115,116,112,97,115,115,0);
          let telegramh = 4;
          let index0 = true;
         libavdevicea.push(basketballmatchdetailbge.length);
         cornershootr += "2";
         telegramh -= (cornershootr == String.fromCharCode(71,0) ? (index0 ? 4 : 1) : cornershootr.length);
         index0 = 44 <= telegramh;
         libavdevicea = [(iconrightorange9 == String.fromCharCode(81,0) ? basketballmatchdetailbge.length : iconrightorange9.length)];
      while (basketballmatchdetailbge == String.fromCharCode(68,0)) {
         iconrightorange9 += `${libavdevicea.length}`;
         break;
      }
      while (iconrightorange9.startsWith(`${basketballmatchdetailbge.length}`)) {
         basketballmatchdetailbge += `${(basketballmatchdetailbge == String.fromCharCode(72,0) ? basketballmatchdetailbge.length : libavdevicea.length)}`;
         break;
      }
       let statisticsinactivej = 1;
          let networkS = String.fromCharCode(114,101,108,97,121,111,117,116,95,112,95,53,51,0);
          let orangeclockV: Map<any, any> = new Map([[String.fromCharCode(118,95,49,57,95,97,100,100,114,115,0),511], [String.fromCharCode(105,116,97,108,105,99,95,97,95,53,50,0),938]]);
         libavdevicea.push(statisticsinactivej % (Math.max(networkS.length, 9)));
         networkS = `${1 + orangeclockV.size}`;
         orangeclockV.set(`${orangeclockV.size}`, orangeclockV.size % (Math.max(orangeclockV.size, 6)));
      let tempm = String.fromCharCode(49,116,48,109,99,108,110,118,106,0) == iconrightorange9;
      do {
          let scrollview3 = String.fromCharCode(116,97,100,100,95,97,95,53,49,0);
          let iconclosewhitewithbg6 = 3;
          let whitevideolive_ = true;
          let greyarrowup5 = 3.0;
         iconrightorange9 += "3";
         scrollview3 = `${(String.fromCharCode(104,0) == scrollview3 ? parseInt(`${greyarrowup5}`) : scrollview3.length)}`;
         iconclosewhitewithbg6 -= parseInt(`${greyarrowup5}`) - 2;
         whitevideolive_ = 50.82 < greyarrowup5;
         if (tempm) {
            break;
         }
      } while ((!iconrightorange9.includes(`${statisticsinactivej}`)) && tempm);
         statisticsinactivej %= Math.max(libavdevicea.length >> (Math.min(3, Math.abs(statisticsinactivej))), 1);
      saveA = [3 * parseInt(`${yellowscoreballF}`)];
   let calendarL = 7178593 <= resendP.length;
   do {
      resendP = `${3 ^ parseInt(`${iconeditU}`)}`;
      if (calendarL) {
         break;
      }
   } while (calendarL && (3 >= (resendP.length + parseInt(`${iconeditU}`)) && 3 >= (resendP.length & 3)));
       let championz = String.fromCharCode(102,95,57,55,95,108,108,118,105,100,100,115,112,101,110,99,0);
       let eighteenW: Map<any, any> = new Map([[String.fromCharCode(115,95,53,51,95,117,116,105,108,105,116,105,101,115,0),76], [String.fromCharCode(122,95,54,54,95,116,114,97,110,115,99,101,105,118,101,114,115,0),486], [String.fromCharCode(97,95,53,57,95,102,117,110,103,105,98,108,101,115,0),890]]);
       let tooltipsW: Map<any, any> = new Map([[String.fromCharCode(104,101,120,116,105,108,101,95,98,95,57,55,0),942], [String.fromCharCode(98,108,117,114,114,97,98,108,101,95,49,95,51,55,0),671], [String.fromCharCode(106,95,50,50,95,103,108,107,0),557]]);
      let moviesl = eighteenW.size <= 5755978;
      do {
         eighteenW = new Map([[`${tooltipsW.size}`, championz.length % 3]]);
         if (moviesl) {
            break;
         }
      } while ((5 <= (eighteenW.size / 2) && (eighteenW.size / (Math.max(2, 3))) <= 5) && moviesl);
         tooltipsW = new Map([[`${tooltipsW.size}`, tooltipsW.size]]);
      if (!championz.startsWith(`${eighteenW.size}`)) {
         eighteenW = new Map([[`${tooltipsW.size}`, eighteenW.size | 3]]);
      }
      if ((championz.length - eighteenW.size) > 5 && (eighteenW.size - championz.length) > 5) {
          let leagueI = String.fromCharCode(99,95,50,50,95,99,97,112,116,105,111,110,0);
          let mbnative4: Array<any> = [268, 571];
          let default_ut = 4.0;
          let tempnodataU = String.fromCharCode(110,95,55,54,95,103,101,110,99,98,0);
          let tempQ = 1.0;
         championz += `${leagueI.length}`;
         leagueI += `${parseInt(`${tempQ}`)}`;
         mbnative4 = [mbnative4.length - tempnodataU.length];
         default_ut -= parseFloat(`${mbnative4.length % (Math.max(1, 5))}`);
         tempnodataU += `${2 + parseInt(`${tempQ}`)}`;
      }
         tooltipsW = new Map([[`${eighteenW.size}`, eighteenW.size / 1]]);
         eighteenW = new Map([[`${tooltipsW.size}`, tooltipsW.size]]);
      let inouttargetredg = 9637284 <= eighteenW.size;
      do {
          let tempnodataK = String.fromCharCode(101,120,99,105,116,97,116,105,111,110,95,104,95,54,48,0);
          let buttonZ = false;
          let questM = 1.0;
          let abidetectU: Map<any, any> = new Map([[String.fromCharCode(115,98,114,100,115,112,95,113,95,54,57,0),String.fromCharCode(99,95,49,95,114,103,98,97,98,101,0)], [String.fromCharCode(115,121,110,99,104,114,111,110,111,117,115,108,121,95,119,95,49,49,0),String.fromCharCode(103,111,112,104,101,114,95,98,95,49,48,0)]]);
          let shrinkE: Array<any> = [String.fromCharCode(117,95,52,48,95,99,104,97,114,103,101,0), String.fromCharCode(105,110,105,116,105,97,116,101,95,100,95,53,50,0), String.fromCharCode(114,95,49,49,95,99,121,117,118,0)];
         eighteenW.set(`${buttonZ}`, ((buttonZ ? 1 : 3) / (Math.max(parseInt(`${questM}`), 5))));
         tempnodataK = `${shrinkE.length - 3}`;
         questM -= parseFloat(`${tempnodataK.length | 1}`);
         abidetectU.set(`${shrinkE.length}`, shrinkE.length);
         if (inouttargetredg) {
            break;
         }
      } while (inouttargetredg && (2 <= (championz.length + eighteenW.size) && 2 <= (championz.length + eighteenW.size)));
          let sharewhites = 4.0;
         tooltipsW.set(`${sharewhites}`, 2 % (Math.max(6, eighteenW.size)));
         championz += `${eighteenW.size | tooltipsW.size}`;
      actionq += parseInt(`${iconplay_}`);
   let iconpipexpandF = overlayt == String.fromCharCode(122,55,111,100,51,98,0);
   do {
       let unread9 = 2.0;
         unread9 += parseInt(`${unread9}`) - parseInt(`${unread9}`);
         unread9 *= 2;
         unread9 /= Math.max(parseInt(`${unread9}`) ^ 2, 5);
      overlayt += `${(String.fromCharCode(54,0) == overlayt ? parseInt(`${profileframeV}`) : overlayt.length)}`;
      if (iconpipexpandF) {
         break;
      }
   } while (iconpipexpandF && ((1 - overlayt.length) > 2));
      resendP += "1";
   while (3 < (saveA.length * 3)) {
      profileframeV += parseFloat(`${saveA.length / (Math.max(2, 6))}`);
      break;
   }
   while (resendP.length >= 1 || 1 >= tooltipsX.length) {
       let closes = String.fromCharCode(122,95,49,56,95,117,110,98,108,111,99,107,0);
       let utilsL = String.fromCharCode(102,97,99,116,111,114,105,122,97,116,105,111,110,95,104,95,56,51,0);
       let temperaturev = String.fromCharCode(111,116,111,105,95,57,95,49,53,0);
      let routerX = String.fromCharCode(112,121,111,104,0) == temperaturev;
      do {
         temperaturev = `${closes.length / (Math.max(3, 1))}`;
         if (routerX) {
            break;
         }
      } while ((3 < temperaturev.length) && routerX);
       let zhubo0 = 0;
      while (closes == String.fromCharCode(89,0)) {
         utilsL = `${1 - utilsL.length}`;
         break;
      }
         closes += `${utilsL.length}`;
         closes += `${temperaturev.length}`;
       let searchbar8 = 3.0;
         utilsL = "3";
      let uimanagerZ = searchbar8 >= 6457190.0;
      do {
         searchbar8 *= (temperaturev == String.fromCharCode(98,0) ? temperaturev.length : closes.length);
         if (uimanagerZ) {
            break;
         }
      } while (uimanagerZ && (3.39 > (searchbar8 * 3.38) && 3.38 > (searchbar8 * utilsL.length)));
      if (5 >= (utilsL.length ^ 3)) {
         utilsL += `${temperaturev.length}`;
      }
      resendP += `${temperaturev.length}`;
      break;
   }
       let video7 = 0.0;
         video7 -= parseInt(`${video7}`) & 2;
      let storev = video7 >= 4947164.0;
      do {
         video7 /= Math.max(parseInt(`${video7}`), 3);
         if (storev) {
            break;
         }
      } while ((1.70 < video7) && storev);
      let gestureJ = video7 >= 7527087.0;
      do {
          let temperaturer = String.fromCharCode(110,95,53,51,95,111,117,116,113,117,101,117,101,0);
          let bggradientw = String.fromCharCode(106,95,55,52,95,107,101,121,119,111,114,100,115,0);
          let whitep = 2;
         video7 -= temperaturer.length >> (Math.min(2, Math.abs(parseInt(`${video7}`))));
         temperaturer = `${(bggradientw == String.fromCharCode(117,0) ? whitep : bggradientw.length)}`;
         whitep |= 3;
         if (gestureJ) {
            break;
         }
      } while (gestureJ && ((video7 - 1) > 2.14));
      saveA.push(navigationH);
   if (resendP.length == 3) {
      tooltipsX += `${2 & saveA.length}`;
   }
   if (parseFloat(`${saveA.length}`) > iconplay_) {
      iconplay_ += parseFloat(`${2 | saveA.length}`);
   }
      navigationH >>= Math.min(5, Math.abs((String.fromCharCode(77,0) == resendP ? resendP.length : parseInt(`${actionq}`))));
       let dangerY: Map<any, any> = new Map([[String.fromCharCode(116,101,114,109,105,110,97,116,101,95,51,95,50,55,0),563], [String.fromCharCode(115,95,50,95,110,111,109,105,110,97,116,111,114,115,0),370]]);
      for (let x = 0; x < 2; x++) {
         dangerY.set(`${dangerY.size}`, dangerY.size / 1);
      }
      let libjsie = dangerY.size <= 4917043;
      do {
         dangerY.set(`${dangerY.size}`, dangerY.size / (Math.max(dangerY.size, 1)));
         if (libjsie) {
            break;
         }
      } while (libjsie && (dangerY.size <= dangerY.size));
      while (1 < (1 & dangerY.size)) {
         dangerY.set(`${dangerY.size}`, dangerY.size | dangerY.size);
         break;
      }
      resendP = `${parseInt(`${iconplay_}`)}`;
   for (let k = 0; k < 3; k++) {
      tooltipsX = `${2 ^ parseInt(`${actionq}`)}`;
   }
       let orientationO = true;
       let signinup0: Array<any> = [192, 272];
          let selecteds = 0.0;
          let white_ = true;
         signinup0 = [parseInt(`${selecteds}`) * 2];
         selecteds /= Math.max(4, (parseFloat(`${(white_ ? 4 : 5)}`)));
      if ((signinup0.length * 1) <= 2) {
          let ying7 = 0.0;
          let scoreE = 1;
          let selln = String.fromCharCode(112,114,101,115,101,108,101,99,116,95,116,95,50,55,0);
          let d_managerH: Array<any> = [532, 630, 514];
          let productg = 5;
         orientationO = 66 >= productg;
         ying7 += parseFloat(`${3}`);
         scoreE += selln.length & scoreE;
         selln = `${(selln == String.fromCharCode(69,0) ? parseInt(`${ying7}`) : selln.length)}`;
         d_managerH = [scoreE];
         productg -= parseInt(`${ying7}`) ^ 1;
      }
      while ((4 | signinup0.length) > 4) {
          let footballfieldD = String.fromCharCode(104,95,51,57,95,108,108,118,105,100,100,115,112,101,110,99,0);
          let ying6: Map<any, any> = new Map([[String.fromCharCode(119,95,53,52,95,109,97,107,101,102,105,108,101,0),825], [String.fromCharCode(101,95,49,56,95,97,116,114,97,99,116,97,98,0),135]]);
          let successY: Array<any> = [893, 921, 413];
          let mergerE = String.fromCharCode(101,110,117,109,101,114,97,116,101,100,95,56,95,57,50,0);
         orientationO = 74 >= mergerE.length || 74 >= successY.length;
         footballfieldD += `${ying6.size ^ footballfieldD.length}`;
         ying6 = new Map([[`${ying6.size}`, ying6.size]]);
         successY.push(ying6.size % 2);
         mergerE += `${footballfieldD.length}`;
         break;
      }
          let mail5: Map<any, any> = new Map([[String.fromCharCode(101,95,50,53,95,97,115,115,101,114,116,105,111,110,0),175], [String.fromCharCode(121,95,56,48,95,117,110,97,117,116,104,111,114,105,122,101,100,0),456], [String.fromCharCode(107,95,49,48,48,95,108,111,103,0),706]]);
         orientationO = 33 < signinup0.length;
         mail5.set(`${mail5.size}`, mail5.size);
         signinup0 = [((orientationO ? 1 : 3) - 3)];
      for (let n = 0; n < 1; n++) {
          let debugi: Array<any> = [742, 198];
          let iconcurrentmatchs = 5;
          let eyeopenx = String.fromCharCode(99,95,53,49,95,118,112,115,104,97,114,101,100,0);
         signinup0.push(((orientationO ? 2 : 5) >> (Math.min(Math.abs(iconcurrentmatchs), 2))));
         debugi = [debugi.length];
         iconcurrentmatchs >>= Math.min(Math.abs(eyeopenx.length * 2), 4);
         eyeopenx = `${eyeopenx.length}`;
      }
      saveA.push(overlayt.length >> (Math.min(Math.abs(2), 3)));
   let topic2 = String.fromCharCode(101,121,95,106,53,0) == tooltipsX;
   do {
       let application3 = String.fromCharCode(112,114,111,118,105,100,101,115,95,113,95,52,56,0);
       let libhermes_ = String.fromCharCode(109,98,105,110,116,114,97,95,54,95,49,56,0);
       let baiduz = String.fromCharCode(98,105,119,101,105,103,104,116,95,118,95,55,56,0);
       let bellreminderK = 5.0;
       let whistleorange1 = 2.0;
       let playlistP = 2.0;
      let greytickP = baiduz.length >= 5304125;
      do {
          let sharewhiteD: Map<any, any> = new Map([[String.fromCharCode(115,97,110,105,116,105,122,101,95,109,95,50,0),true ], [String.fromCharCode(104,109,97,99,95,107,95,53,50,0),true ], [String.fromCharCode(101,120,99,104,97,110,103,101,115,95,97,95,53,54,0),false ]]);
          let static_lgN = 2.0;
          let privatechatbgU: Array<any> = [String.fromCharCode(115,116,100,97,116,111,109,105,99,95,102,95,50,52,0), String.fromCharCode(102,95,55,95,119,104,105,116,101,115,112,97,99,101,0)];
          let attributedstringa = 1;
          let bgvipsportq = String.fromCharCode(116,114,97,110,115,117,112,112,95,120,95,55,52,0);
         baiduz = `${application3.length}`;
         sharewhiteD = new Map([[`${static_lgN}`, 3]]);
         static_lgN -= parseFloat(`${privatechatbgU.length ^ parseInt(`${static_lgN}`)}`);
         privatechatbgU.push(privatechatbgU.length);
         attributedstringa <<= Math.min(Math.abs(attributedstringa), 4);
         bgvipsportq = "3";
         if (greytickP) {
            break;
         }
      } while ((!libhermes_.endsWith(baiduz)) && greytickP);
       let encryptl = String.fromCharCode(117,118,109,118,95,106,95,54,57,0);
      let zoom_ = 8784520 >= application3.length;
      do {
         application3 = `${1 ^ parseInt(`${whistleorange1}`)}`;
         if (zoom_) {
            break;
         }
      } while (zoom_ && (5 == (application3.length % 4)));
       let arrowdownp = 3.0;
       let rocket3 = 3.0;
      if ((rocket3 - 2.45) <= 2.48 && (rocket3 - 2.45) <= 3.88) {
          let notificationfilled8 = String.fromCharCode(116,95,57,56,95,111,112,117,115,116,97,98,0);
          let backwhiteE: Array<any> = [String.fromCharCode(102,95,53,49,95,115,104,105,102,116,115,0), String.fromCharCode(112,95,51,54,95,102,97,114,0)];
          let ajaxw = 0.0;
          let homeplayerh = String.fromCharCode(99,95,57,52,95,114,101,99,101,112,116,105,111,110,0);
         libhermes_ = "2";
         notificationfilled8 += `${homeplayerh.length}`;
         backwhiteE.push(notificationfilled8.length << (Math.min(Math.abs(2), 2)));
         ajaxw -= 2 + backwhiteE.length;
         homeplayerh += `${3 ^ parseInt(`${ajaxw}`)}`;
      }
      for (let p = 0; p < 1; p++) {
         whistleorange1 *= parseInt(`${playlistP}`) % 1;
      }
      for (let r = 0; r < 1; r++) {
          let adultB = true;
          let distK = 5.0;
          let regengp: Array<any> = [725, 93, 936];
          let winit_g0a = 1.0;
          let template_6sG = 3.0;
         application3 = `${parseInt(`${arrowdownp}`) ^ 3}`;
         adultB = template_6sG < 46.32;
         distK *= ((adultB ? 3 : 2) & parseInt(`${distK}`));
         regengp = [parseInt(`${winit_g0a}`)];
         template_6sG /= Math.max(3, parseFloat(`${3 / (Math.max(parseInt(`${template_6sG}`), 4))}`));
      }
      tooltipsX += "1";
      if (topic2) {
         break;
      }
   } while ((3.49 < (yellowscoreballF - 3.74)) && topic2);
      yellowscoreballF += parseFloat(`${3 * resendP.length}`);
      saveA.push(2 + tooltipsX.length);
      yellowscoreballF += parseFloat(`${overlayt.length}`);
   while ((yellowscoreballF * 1.22) > 1.44 && 4 > (suboutU.length ^ 2)) {
       let sellN = 3.0;
       let turndownP = String.fromCharCode(110,95,56,49,95,115,117,98,109,105,116,0);
         turndownP = `${turndownP.length ^ 3}`;
         turndownP = `${parseInt(`${sellN}`) | 1}`;
      for (let w = 0; w < 1; w++) {
         turndownP += `${turndownP.length - parseInt(`${sellN}`)}`;
      }
       let strings9: Array<any> = [569, 875];
      let shareblackp = 8059418 <= turndownP.length;
      do {
         turndownP += `${parseInt(`${sellN}`) / 1}`;
         if (shareblackp) {
            break;
         }
      } while (shareblackp && (strings9.length <= 1));
      if (parseFloat(`${turndownP.length}`) > sellN) {
          let scrollviewD: Array<any> = [305, 723];
         sellN *= parseFloat(`${1}`);
         scrollviewD = [2 / (Math.max(7, scrollviewD.length))];
      }
      yellowscoreballF -= parseFloat(`${overlayt.length - 3}`);
      break;
   }

    setShowReferrerModal(true);
  }

  const onCopyReferralCode = () => {
       let xnewsshareP: Array<any> = [634, 565];
    let nalyticsC = 2;
    let shootyesgoalN: Array<any> = [479, 993];
    let logol = 4.0;
    let pagination5 = String.fromCharCode(101,95,55,53,95,117,110,105,102,111,114,109,115,0);
    let modulec = 3;
    let indicatorG: Array<any> = [60, 236, 218];
    let homeplayerE: Map<any, any> = new Map([[String.fromCharCode(109,97,106,111,114,95,105,95,54,56,0),740], [String.fromCharCode(120,95,57,52,95,97,117,120,100,97,116,97,0),113], [String.fromCharCode(116,95,55,56,95,98,105,116,0),636]]);
    let predictionbannershared6 = 0.0;
    let predictionwino: Array<any> = [27, 879, 766];
   for (let j = 0; j < 1; j++) {
      homeplayerE.set(`${modulec}`, 2 - nalyticsC);
   }
       let loginsuccessw = false;
      for (let i = 0; i < 2; i++) {
          let shoot5 = 5.0;
          let iconwatchJ = 0;
          let turnI = String.fromCharCode(122,95,56,50,95,115,104,105,101,108,100,0);
          let showmoreb = 0.0;
         loginsuccessw = loginsuccessw && shoot5 <= 56.89;
         shoot5 *= parseInt(`${showmoreb}`);
         iconwatchJ %= Math.max(4, 2);
         turnI = `${(turnI == String.fromCharCode(71,0) ? iconwatchJ : turnI.length)}`;
         showmoreb /= Math.max(5, parseInt(`${showmoreb}`));
      }
      if (loginsuccessw) {
         loginsuccessw = (!loginsuccessw ? loginsuccessw : loginsuccessw);
      }
         loginsuccessw = (loginsuccessw ? loginsuccessw : !loginsuccessw);
      predictionbannershared6 -= 3 >> (Math.min(2, indicatorG.length));
   for (let v = 0; v < 2; v++) {
      shootyesgoalN.push(2 << (Math.min(5, Math.abs(homeplayerE.size))));
   }
   while ((xnewsshareP.length + predictionbannershared6) <= 5.75) {
       let valuesa = String.fromCharCode(117,95,49,51,95,116,101,109,112,111,114,97,114,105,108,121,0);
       let bgvipxvod1 = 1;
       let away6 = String.fromCharCode(109,95,55,57,95,100,105,115,99,111,110,110,101,99,116,101,100,0);
       let calendarL = String.fromCharCode(109,117,108,116,120,95,50,95,53,0);
       let hooksv = String.fromCharCode(101,120,99,101,112,116,105,111,110,95,111,95,56,54,0);
       let goalg = String.fromCharCode(115,105,108,101,110,99,101,100,95,120,95,53,48,0);
          let iconrightorange4: Map<any, any> = new Map([[String.fromCharCode(109,117,120,95,109,95,50,52,0),954], [String.fromCharCode(102,105,108,101,119,114,105,116,101,115,116,114,101,97,109,95,108,95,56,55,0),764]]);
          let activityd = 5.0;
         hooksv = `${(calendarL == String.fromCharCode(89,0) ? parseInt(`${activityd}`) : calendarL.length)}`;
         iconrightorange4.set(`${iconrightorange4.size}`, iconrightorange4.size);
         activityd /= Math.max(2, iconrightorange4.size / 2);
         bgvipxvod1 /= Math.max(1, goalg.length * away6.length);
      for (let q = 0; q < 1; q++) {
         goalg += `${hooksv.length >> (Math.min(Math.abs(2), 2))}`;
      }
      while (valuesa == String.fromCharCode(73,0)) {
          let renewT = 0.0;
          let componentI = String.fromCharCode(110,95,50,48,95,114,101,97,100,98,105,116,115,0);
          let activem = 2.0;
          let showZ = 2.0;
         away6 = `${valuesa.length >> (Math.min(Math.abs(3), 1))}`;
         renewT /= Math.max(2, parseFloat(`${componentI.length}`));
         componentI = `${parseInt(`${showZ}`) - 3}`;
         activem -= 2;
         showZ *= parseInt(`${renewT}`);
         break;
      }
      while (calendarL.length > goalg.length) {
          let temperatureE = 1.0;
          let championy: Array<any> = [379, 376, 352];
          let default_7lG: Array<any> = [454, 873, 693];
         calendarL = `${default_7lG.length | goalg.length}`;
         temperatureE += parseInt(`${temperatureE}`);
         championy = [championy.length * parseInt(`${temperatureE}`)];
         default_7lG = [championy.length << (Math.min(Math.abs(2), 5))];
         break;
      }
          let bannerc: Map<any, any> = new Map([[String.fromCharCode(99,105,114,99,95,109,95,56,49,0),781], [String.fromCharCode(118,95,51,49,95,100,97,97,108,97,0),120]]);
          let placeholderK = 2.0;
         calendarL += `${away6.length * 2}`;
         bannerc = new Map([[`${bannerc.size}`, parseInt(`${placeholderK}`) << (Math.min(Math.abs(2), 2))]]);
         placeholderK += parseFloat(`${parseInt(`${placeholderK}`) * bannerc.size}`);
      let template_zwf = hooksv == String.fromCharCode(53,111,56,112,113,118,0);
      do {
         hooksv = `${bgvipxvod1 ^ 1}`;
         if (template_zwf) {
            break;
         }
      } while ((hooksv.includes(`${calendarL.length}`)) && template_zwf);
         valuesa = "2";
      if ((1 >> (Math.min(5, valuesa.length))) > 5 || 1 > (valuesa.length >> (Math.min(1, Math.abs(bgvipxvod1))))) {
         valuesa = `${goalg.length * calendarL.length}`;
      }
       let renderz = 1.0;
       let predictionbannersharedt = 3.0;
       let descE = 2.0;
      xnewsshareP = [(String.fromCharCode(113,0) == valuesa ? valuesa.length : calendarL.length)];
      break;
   }
   while (nalyticsC > pagination5.length) {
      nalyticsC %= Math.max(shootyesgoalN.length, 1);
      break;
   }
   if (pagination5.length == 1) {
       let privacy0: Array<any> = [62, 891];
       let roundW = 5;
       let service8 = false;
       let chatW = String.fromCharCode(99,111,110,100,105,116,105,111,110,97,108,95,111,95,52,50,0);
          let downp = 0.0;
          let template__6b = String.fromCharCode(119,95,49,52,95,114,101,116,97,105,110,101,100,0);
          let whiteanimationliveG = 5;
         chatW += `${chatW.length / 3}`;
         downp += parseFloat(`${parseInt(`${downp}`)}`);
         template__6b += `${2 + template__6b.length}`;
         whiteanimationliveG *= template__6b.length ^ parseInt(`${downp}`);
          let iconsharefriendsS: Array<any> = [165, 228];
          let executorg = String.fromCharCode(118,97,108,117,101,100,95,57,95,54,52,0);
         service8 = (61 <= (iconsharefriendsS.length & (service8 ? iconsharefriendsS.length : 61)));
         executorg = `${executorg.length - executorg.length}`;
          let pause4 = String.fromCharCode(97,99,101,110,99,95,112,95,50,54,0);
         service8 = chatW.length <= 15;
         pause4 += `${(pause4 == String.fromCharCode(121,0) ? pause4.length : pause4.length)}`;
         chatW = "1";
          let collectionn = 5;
          let iconwatchnowY: Map<any, any> = new Map([[String.fromCharCode(106,95,50,53,95,103,97,109,101,115,0),349], [String.fromCharCode(105,95,54,54,95,118,116,101,110,99,0),418], [String.fromCharCode(100,101,116,97,99,104,95,109,95,49,51,0),933]]);
          let untickc = String.fromCharCode(108,111,111,112,105,110,103,95,56,95,55,57,0);
         roundW += privacy0.length | 1;
         collectionn -= iconwatchnowY.size - collectionn;
         iconwatchnowY = new Map([[`${iconwatchnowY.size}`, untickc.length + 1]]);
         untickc = `${collectionn + iconwatchnowY.size}`;
      let dnewinterstitial5 = service8 ? !service8 : service8;
      do {
          let gifgoalbgo = 5.0;
         service8 = 43.45 < gifgoalbgo;
         if (dnewinterstitial5) {
            break;
         }
      } while (dnewinterstitial5 && (!service8 && chatW.length < 2));
      for (let b = 0; b < 3; b++) {
         service8 = 42 <= roundW;
      }
      let analytic4 = 8867852 <= chatW.length;
      do {
          let flag6 = String.fromCharCode(109,101,116,97,115,111,117,110,100,95,112,95,53,57,0);
         chatW += `${((service8 ? 4 : 4))}`;
         flag6 += "2";
         if (analytic4) {
            break;
         }
      } while ((1 == chatW.length && !service8) && analytic4);
      for (let d = 0; d < 1; d++) {
         chatW += `${privacy0.length}`;
      }
         roundW <<= Math.min(4, Math.abs(privacy0.length - 1));
      while ((chatW.length * 3) == 5) {
         chatW += `${chatW.length}`;
         break;
      }
       let cornerkickP = String.fromCharCode(98,105,97,115,95,108,95,54,50,0);
      logol += parseFloat(`${nalyticsC}`);
   }
   if ((nalyticsC ^ shootyesgoalN.length) == 1) {
      shootyesgoalN = [2];
   }
       let changeG: Map<any, any> = new Map([[String.fromCharCode(99,111,110,110,101,99,116,111,114,95,101,95,53,56,0),String.fromCharCode(108,95,50,53,95,122,101,114,111,122,101,114,111,0)], [String.fromCharCode(98,101,103,105,110,95,120,95,55,53,0),String.fromCharCode(113,95,49,50,95,100,105,115,99,117,115,115,0)]]);
       let arrowrightv = 0.0;
       let awayplayerp = 2.0;
         awayplayerp *= parseFloat(`${changeG.size}`);
      while (5.83 < (arrowrightv * 1.95) || 2.89 < (arrowrightv * 1.95)) {
          let loadingi = 2.0;
          let filez: Array<any> = [896, 87, 900];
          let filter7 = 5.0;
          let basketballmatchdetailbgQ = 1;
          let template_94 = 0.0;
         awayplayerp += parseFloat(`${parseInt(`${arrowrightv}`) | filez.length}`);
         loadingi -= basketballmatchdetailbgQ / 3;
         filez = [parseInt(`${filter7}`) & parseInt(`${loadingi}`)];
         filter7 -= 1 - parseInt(`${loadingi}`);
         basketballmatchdetailbgQ *= parseInt(`${loadingi}`);
         template_94 *= parseFloat(`${1}`);
         break;
      }
      let blackB = 8468269 <= changeG.size;
      do {
         changeG.set(`${arrowrightv}`, 3 * parseInt(`${arrowrightv}`));
         if (blackB) {
            break;
         }
      } while (blackB && (awayplayerp < 2.84));
      for (let j = 0; j < 3; j++) {
          let star_ = String.fromCharCode(115,95,55,48,95,117,112,103,114,97,100,101,0);
          let shielddonex = false;
         arrowrightv *= parseFloat(`${parseInt(`${arrowrightv}`)}`);
         star_ += `${star_.length}`;
         shielddonex = star_.length >= 95;
      }
         arrowrightv -= parseFloat(`${parseInt(`${awayplayerp}`)}`);
         awayplayerp /= Math.max(3, parseFloat(`${parseInt(`${awayplayerp}`)}`));
      while ((arrowrightv * 1.44) <= 2.33 || (arrowrightv * awayplayerp) <= 1.44) {
          let teamq = String.fromCharCode(107,95,49,53,95,114,101,99,111,110,105,110,116,101,114,0);
          let projectb = String.fromCharCode(115,105,109,112,108,101,116,97,103,95,98,95,57,50,0);
          let kickh = true;
         awayplayerp += parseFloat(`${1 % (Math.max(4, parseInt(`${arrowrightv}`)))}`);
         teamq += `${teamq.length}`;
         projectb += `${projectb.length}`;
         kickh = !projectb.endsWith(`${kickh}`);
         break;
      }
          let reducer8 = false;
          let resultJ = true;
          let defaultprofilepicx = String.fromCharCode(104,95,54,95,115,112,101,99,105,102,105,101,114,0);
         changeG = new Map([[defaultprofilepicx, (defaultprofilepicx.length / (Math.max(10, (resultJ ? 3 : 5))))]]);
         reducer8 = !reducer8;
         resultJ = !reducer8;
      for (let h = 0; h < 2; h++) {
         arrowrightv -= parseFloat(`${3 * changeG.size}`);
      }
      pagination5 += `${shootyesgoalN.length}`;
      logol *= parseFloat(`${shootyesgoalN.length}`);
      pagination5 = `${parseInt(`${predictionbannershared6}`) + 3}`;
      predictionbannershared6 /= Math.max(homeplayerE.size, 5);
   let defaultprofilepicb = 6195284 <= pagination5.length;
   do {
      pagination5 += `${modulec}`;
      if (defaultprofilepicb) {
         break;
      }
   } while (defaultprofilepicb && (!pagination5.includes(`${modulec}`)));
      nalyticsC -= 2;

    Clipboard.setString(userState.user?.userReferralCode ?? '');

   for (let x = 0; x < 1; x++) {
      nalyticsC <<= Math.min(Math.abs(modulec), 3);
   }
   let tempnodata7 = 8688499 <= predictionwino.length;
   do {
       let minio = 5;
       let halffieldimageb: Map<any, any> = new Map([[String.fromCharCode(109,97,100,100,95,104,95,52,52,0),true ], [String.fromCharCode(119,100,108,102,99,110,95,51,95,50,53,0),true ], [String.fromCharCode(100,95,56,53,95,99,108,111,115,101,100,0),true ]]);
       let orange7 = false;
         halffieldimageb = new Map([[`${halffieldimageb.size}`, minio]]);
         halffieldimageb.set(`${orange7}`, 2);
      while ((halffieldimageb.size / 4) == 5) {
         orange7 = (81 == ((orange7 ? halffieldimageb.size : 7) << (Math.min(Math.abs(halffieldimageb.size), 1))));
         break;
      }
          let megaphonec = 3;
          let iconclosewhiteS: Map<any, any> = new Map([[String.fromCharCode(101,95,57,49,95,117,112,109,105,120,0),166], [String.fromCharCode(98,95,52,54,95,99,111,110,118,101,114,115,105,111,110,115,0),186]]);
          let tempnodatat: Map<any, any> = new Map([[String.fromCharCode(102,95,51,55,95,116,111,109,105,99,0),600], [String.fromCharCode(110,95,56,55,95,100,111,116,108,111,99,107,0),320], [String.fromCharCode(110,111,110,110,117,108,108,115,115,114,99,115,95,114,95,54,57,0),887]]);
         orange7 = minio == 15;
         megaphonec -= iconclosewhiteS.size;
         iconclosewhiteS = new Map([[`${tempnodatat.size}`, megaphonec & 2]]);
         tempnodatat = new Map([[`${tempnodatat.size}`, tempnodatat.size]]);
      while (3 > (minio << (Math.min(Math.abs(2), 1))) && !orange7) {
         orange7 = halffieldimageb.get(`${orange7}`) == null;
         break;
      }
      while (orange7) {
         orange7 = !orange7 && minio <= 5;
         break;
      }
      let main_hs = minio >= 4987136;
      do {
         minio -= 1 - halffieldimageb.size;
         if (main_hs) {
            break;
         }
      } while ((2 > (3 % (Math.max(6, halffieldimageb.size))) || (minio % (Math.max(halffieldimageb.size, 8))) > 3) && main_hs);
          let iconuserB: Map<any, any> = new Map([[String.fromCharCode(99,111,110,118,101,114,116,105,110,103,95,107,95,50,0),260], [String.fromCharCode(104,95,50,52,95,109,97,112,112,101,114,0),366], [String.fromCharCode(117,95,50,95,102,97,97,110,105,100,99,116,0),743]]);
         halffieldimageb = new Map([[`${iconuserB.size}`, 3 + iconuserB.size]]);
         minio /= Math.max(2, 5);
      predictionwino.push(pagination5.length);
      if (tempnodata7) {
         break;
      }
   } while (tempnodata7 && ((indicatorG.length + predictionwino.length) >= 2));
   let whitetickZ = 5084995 <= indicatorG.length;
   do {
      indicatorG.push(modulec ^ predictionwino.length);
      if (whitetickZ) {
         break;
      }
   } while (whitetickZ && (4 < (modulec / 5)));
   if ((2 & predictionwino.length) > 1) {
      predictionwino = [2];
   }
       let libcxxcomponentsS = 5;
         libcxxcomponentsS >>= Math.min(4, Math.abs(1 << (Math.min(4, Math.abs(libcxxcomponentsS)))));
         libcxxcomponentsS ^= 1;
      for (let w = 0; w < 2; w++) {
          let predictionbannerb = String.fromCharCode(113,95,57,49,95,116,97,110,115,105,103,0);
          let aboutS = true;
          let libjsinspectorA: Map<any, any> = new Map([[String.fromCharCode(109,95,50,49,95,118,108,105,110,101,0),String.fromCharCode(100,111,99,117,109,101,110,116,97,116,105,111,110,95,116,95,48,0)], [String.fromCharCode(115,95,51,56,95,98,105,116,101,120,97,99,116,0),String.fromCharCode(115,101,113,118,105,100,101,111,95,97,95,54,55,0)]]);
          let classesZ = String.fromCharCode(119,95,56,56,95,112,101,99,101,110,116,0);
         libcxxcomponentsS >>= Math.min(5, Math.abs(libjsinspectorA.size * 2));
         predictionbannerb += "2";
         aboutS = predictionbannerb.length >= 75;
         libjsinspectorA.set(classesZ, (classesZ.length * (aboutS ? 5 : 5)));
      }
      homeplayerE.set(`${logol}`, parseInt(`${logol}`) | 2);
      predictionwino.push(homeplayerE.size ^ nalyticsC);
      indicatorG.push(indicatorG.length ^ 1);
      predictionbannershared6 /= Math.max(shootyesgoalN.length, 3);
      predictionwino = [1];
      homeplayerE = new Map([[`${xnewsshareP.length}`, 3 - xnewsshareP.length]]);
   for (let v = 0; v < 2; v++) {
      shootyesgoalN.push(predictionwino.length);
   }
   let attributedstring_ = modulec >= 7555193;
   do {
      modulec -= 1;
      if (attributedstring_) {
         break;
      }
   } while (attributedstring_ && (4 < (modulec % 2) && 4 < (modulec % (Math.max(2, 1)))));
   if ((4 ^ indicatorG.length) == 2 && (logol * 1.43) == 2.12) {
      logol -= parseFloat(`${3 - pagination5.length}`);
   }
    setIsDialogOpen(true);
  }

  return (
    <ScreenContainer>
      <TitleWithBackButtonHeader title='个人中心' />
      <View
        style={{
          justifyContent: 'space-between',
          flex: 1,
          paddingHorizontal: 10,
        }}
      >
        <View style={{ marginTop: 30 }}>
          <TouchableOpacity
            style={styles.pressableTextContainer}
            onPress={() => {
              yysHomeinactiveUntick.showToast('目前暂不支持更改头像，敬请期待');
            }}
          >
            <Text style={{ fontSize: 16, color: '#9C9C9C' }}>头像</Text>
            <FastImage
              style={{
                height: 34,
                width: 34,
              }}
              source={require('../../../../static/images/loadingspinnerPlaylist.png')}
            />
          </TouchableOpacity>

          { }
          <TouchableOpacity
            style={styles.pressableTextContainer}
            onPress={onPressUsername}
          >
            <Text style={{ fontSize: 16, color: 'white' }}>
              {username}
            </Text>
          </TouchableOpacity>

          { }
          <View style={styles.pressableTextContainer}>
            <Text style={{ fontSize: 16, color: '#9C9C9C' }}>
              {userState.user?.userEmail !== '' ? userState.user?.userEmail : userState.user?.userPhoneNumber}
            </Text>
          </View>

          { }
          {userState.user?.userReferrerName == '' &&
            userState.user.userAllowUpdateReferral == true &&
            <TouchableOpacity
              style={styles.pressableTextContainer}
              onPress={onPressReferrer}
            >
              <Text style={{ fontSize: 16, color: '#B6B6B6' }}>
                补填邀请码 (只能填写一次)
              </Text>
            </TouchableOpacity>
          }

          {userState.user?.userReferrerName != '' && (
            <View style={styles.pressableTextContainer}>
              <Text style={{ fontSize: 16, color: '#9C9C9C' }}>推介人</Text>

              <View
                style={{
                  flexDirection: 'row',
                  justifyContent: 'center',
                  alignItems: 'center',
                }}
              >
                <Text style={{ fontSize: 14, color: '#9C9C9C' }}>
                  {userState.user?.userReferrerName}
                </Text>
              </View>
            </View>
          )}

          { }
          <TouchableOpacity
            style={styles.pressableTextContainer}
            onPress={onCopyReferralCode}
          >
            <Text style={{ fontSize: 16, color: colors.primary }}>
              {userState.user?.userReferralCode}
            </Text>

            <View
              style={{
                flexDirection: 'row',
                justifyContent: 'center',
                alignItems: 'center',
              }}
            >
              <Text style={{ fontSize: 14, color: '#9C9C9C' }}>
                复制邀请码
              </Text>
              <Image
                style={{
                  height: 27,
                  width: 27,
                  position: 'relative',
                  top: 2,
                }}
                source={require('../../../../static/images/profile/predictionbannerIconlogoutCondensed.png')}
              />
            </View>
          </TouchableOpacity>

          <NotificationModal
            onConfirm={toggleOverlay}
            isVisible={isDialogOpen}
            title='复制成功'
            subtitle1=''
            subtitle2=''
            subtitle3=''
          />
        </View>

        <ChangeUsernameModal
          isVisible={isShowUsernameModal}
          handleClose={() => {
            setShowUsernameModal(false);
          }}
          initialUsername={username}
          onSubmitSuccess={onUsernameUpdateSuccess}
        />

        <ChangeReferrerModal
          isVisible={isShowReferrerModal}
          handleClose={() => {
            setShowReferrerModal(false);
          }}
        />
      </View>
    </ScreenContainer>
  );
};

const styles = StyleSheet.create({
  pressableTextContainer: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    backgroundColor: '#1d2023',
    marginTop: 20,
    paddingLeft: 18,
    paddingRight: 13,
    height: 48,
    borderRadius: 8,
    marginBottom: 20,
  },
});
