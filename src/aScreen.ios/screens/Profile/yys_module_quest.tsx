import React, { useState } from "react";
import {
  View,
  Text,
  StyleSheet,
  TextInput,
  TouchableOpacity,
  SafeAreaView,
} from "react-native";
import ScreenContainer from "../../components/container/yys_executor_expand";
import { RootStackScreenProps } from "@type/yys_settings";
import TitleWithBackButtonHeader from "../../components/header/yys_anner_header";

import { useTheme } from "@react-navigation/native";
import { ScrollView } from "react-native-gesture-handler";
import { APP_NAME_CONST } from "@utility/yys_ajax_switch";

export default ({ navigation }: RootStackScreenProps<"活动规则">) => {
  const { colors, textVariants, icons, spacing } = useTheme();
  const [isDialogOpen, setIsDialogOpen] = useState(false);

  const toggleOverlay = () => {
       let championL = String.fromCharCode(110,95,49,54,95,115,122,97,98,111,0);
    let benefit3 = String.fromCharCode(114,95,52,53,95,114,101,113,117,101,115,116,101,114,0);
    let hongkongG = 3.0;
    let sansc = String.fromCharCode(112,114,105,111,114,95,53,95,49,50,0);
    let gmailM = String.fromCharCode(97,100,108,101,114,95,118,95,49,56,0);
    let remindern = String.fromCharCode(106,95,57,49,95,114,101,115,105,103,110,101,100,0);
    let emojiw: Array<any> = [697, 28];
    let libyogaa = 0.0;
    let taiwanq = true;
    let armvad = String.fromCharCode(112,114,101,100,95,103,95,53,51,0);
    let zhubo5 = false;
       let favicony = 3;
       let vnewsW = String.fromCharCode(117,95,55,53,95,97,98,111,114,116,0);
       let greyP = String.fromCharCode(114,101,99,117,114,115,105,111,110,95,101,95,55,52,0);
      for (let f = 0; f < 3; f++) {
          let toponf = false;
          let mbridge4 = 5.0;
         vnewsW = `${1 + favicony}`;
         toponf = !toponf;
         mbridge4 *= parseFloat(`${parseInt(`${mbridge4}`) / 3}`);
      }
      let lnewsP = String.fromCharCode(102,102,116,103,110,55,56,109,118,109,0) == vnewsW;
      do {
         vnewsW = `${greyP.length}`;
         if (lnewsP) {
            break;
         }
      } while ((greyP.length > 3 && 3 > vnewsW.length) && lnewsP);
       let login9 = String.fromCharCode(106,95,50,48,95,109,105,100,110,105,103,104,116,0);
      for (let c = 0; c < 2; c++) {
         login9 += `${vnewsW.length * login9.length}`;
      }
          let redirecti = String.fromCharCode(108,95,55,53,95,112,99,109,119,98,0);
          let temperatureK = String.fromCharCode(115,116,97,114,116,105,110,103,95,109,95,52,0);
          let libavcodec9 = String.fromCharCode(99,111,110,116,101,110,116,108,101,115,115,95,100,95,53,52,0);
         favicony ^= vnewsW.length;
         redirecti = `${temperatureK.length}`;
         temperatureK = `${libavcodec9.length}`;
         libavcodec9 += `${libavcodec9.length * 1}`;
      while (vnewsW.startsWith(`${favicony}`)) {
          let debugk = String.fromCharCode(110,95,49,50,95,114,97,110,107,115,0);
         favicony ^= vnewsW.length;
         debugk += `${debugk.length}`;
         break;
      }
      if (greyP == String.fromCharCode(48,0)) {
         vnewsW = `${1 * greyP.length}`;
      }
       let gradleS = 2.0;
       let imagemanagerR = 3.0;
          let reducer7: Array<any> = [126, 14, 422];
          let temperatureD: Array<any> = [336, 214];
          let libswresamplem = 0.0;
         vnewsW = "2";
         reducer7.push(temperatureD.length & 1);
         temperatureD = [parseInt(`${libswresamplem}`) / (Math.max(reducer7.length, 10))];
         libswresamplem -= parseFloat(`${parseInt(`${libswresamplem}`) & reducer7.length}`);
      hongkongG -= vnewsW.length;
      gmailM = `${benefit3.length & 3}`;
       let libreactP = true;
       let native7 = false;
       let coreU: Map<any, any> = new Map([[String.fromCharCode(115,104,97,95,98,95,56,55,0),188], [String.fromCharCode(101,114,112,105,99,95,55,95,51,48,0),390], [String.fromCharCode(98,105,116,101,95,107,95,54,57,0),281]]);
          let megaphoneg: Array<any> = [235, 313, 522];
          let mbbannerw: Array<any> = [411, 263];
         coreU = new Map([[`${coreU.size}`, coreU.size % 2]]);
         megaphoneg = [1 - mbbannerw.length];
         mbbannerw.push(3);
          let xadsdkh = true;
         native7 = coreU.size <= 90;
         xadsdkh = xadsdkh || !xadsdkh;
      let gdtadvx = native7 ? !native7 : native7;
      do {
         native7 = libreactP;
         if (gdtadvx) {
            break;
         }
      } while (gdtadvx && ((coreU.size * 3) <= 2));
      while (!native7) {
         native7 = libreactP;
         break;
      }
          let animation1 = 5.0;
         native7 = !native7;
         animation1 *= parseFloat(`${parseInt(`${animation1}`) ^ parseInt(`${animation1}`)}`);
      while (!native7) {
         libreactP = !libreactP && native7;
         break;
      }
       let shrinkY = 5;
      while (!native7) {
         native7 = 97 >= shrinkY || coreU.size >= 97;
         break;
      }
       let shirt4: Map<any, any> = new Map([[String.fromCharCode(121,95,56,53,95,103,111,111,100,0),false ], [String.fromCharCode(102,109,97,99,95,105,95,49,48,48,0),true ]]);
      taiwanq = emojiw.length <= 44 || !taiwanq;
   let customt = 8175368 >= emojiw.length;
   do {
      emojiw = [sansc.length];
      if (customt) {
         break;
      }
   } while ((3 == emojiw.length) && customt);
      hongkongG *= emojiw.length ^ championL.length;
      emojiw = [gmailM.length];
   for (let n = 0; n < 2; n++) {
      emojiw = [2 * parseInt(`${libyogaa}`)];
   }
      sansc = `${2 * emojiw.length}`;
   while (!benefit3.includes(`${remindern.length}`)) {
      remindern = `${benefit3.length << (Math.min(2, Math.abs(parseInt(`${hongkongG}`))))}`;
      break;
   }
      taiwanq = emojiw.length == 99 || 99 == gmailM.length;
      taiwanq = libyogaa < 70.25;
      championL += `${championL.length}`;
   if (5.57 > libyogaa) {
      libyogaa /= Math.max(3, parseFloat(`${gmailM.length}`));
   }
   while (5.52 > (hongkongG / (Math.max(benefit3.length, 3)))) {
      hongkongG *= 2 * championL.length;
      break;
   }
      championL += `${((taiwanq ? 3 : 5))}`;
      remindern = "2";
   let orangeo = 7868505 <= remindern.length;
   do {
       let orientation6 = 2;
       let changed = 5;
       let round_ = false;
       let manifest8 = 4;
      if (!round_) {
          let gesturer: Array<any> = [String.fromCharCode(99,97,109,112,97,105,103,110,95,119,95,51,53,0), String.fromCharCode(115,101,97,108,97,110,116,95,51,95,57,51,0), String.fromCharCode(97,117,116,111,117,112,100,97,116,101,95,97,95,57,50,0)];
          let logoutk = 0;
          let rewardB: Map<any, any> = new Map([[String.fromCharCode(112,115,102,105,108,101,95,105,95,53,53,0),true ], [String.fromCharCode(99,111,111,114,100,105,110,97,116,101,95,99,95,54,48,0),false ]]);
         manifest8 -= orientation6 & 1;
         gesturer.push(rewardB.size ^ 3);
         logoutk &= 2 >> (Math.min(1, Math.abs(rewardB.size)));
      }
      let binddatasZ = round_ ? !round_ : round_;
      do {
          let applicationu: Map<any, any> = new Map([[String.fromCharCode(113,95,49,55,95,97,118,117,105,0),199], [String.fromCharCode(101,103,119,105,116,95,120,95,56,54,0),209], [String.fromCharCode(117,110,105,116,95,103,95,49,57,0),394]]);
          let minivodz: Map<any, any> = new Map([[String.fromCharCode(108,95,52,95,118,97,114,105,97,98,108,101,0),true ], [String.fromCharCode(101,95,57,50,95,102,105,97,116,0),false ]]);
          let libfollyO = 2.0;
          let dropdownH = true;
         round_ = (changed ^ applicationu.size) > 5;
         applicationu.set(`${dropdownH}`, 1);
         minivodz.set(`${dropdownH}`, (parseInt(`${libfollyO}`) / (Math.max(1, (dropdownH ? 3 : 4)))));
         libfollyO *= parseFloat(`${3 % (Math.max(10, parseInt(`${libfollyO}`)))}`);
         if (binddatasZ) {
            break;
         }
      } while ((round_) && binddatasZ);
         changed -= orientation6;
         orientation6 >>= Math.min(parseInt(`${Math.abs(((round_ ? 4 : 1) >> (Math.min(Math.abs(manifest8), 1))))}`), 5);
         changed /= Math.max(1, 2);
         round_ = 34 < changed;
      while (3 < orientation6) {
         round_ = 26 == orientation6;
         break;
      }
      let phone_ = 8031946 >= manifest8;
      do {
          let materialc = 5.0;
         manifest8 ^= manifest8;
         materialc /= Math.max(3, 3);
         if (phone_) {
            break;
         }
      } while (((2 + manifest8) <= 3 && (orientation6 + 2) <= 1) && phone_);
          let minivodV = 5.0;
          let benefito = 0;
          let kickR = 5;
         orientation6 &= ((round_ ? 1 : 1) >> (Math.min(Math.abs(changed), 2)));
         minivodV /= Math.max(benefito / 2, 5);
         benefito /= Math.max(benefito - 2, 1);
         kickR &= benefito >> (Math.min(Math.abs(parseInt(`${minivodV}`)), 5));
      for (let p = 0; p < 2; p++) {
         changed ^= changed / (Math.max(3, 7));
      }
         manifest8 %= Math.max(manifest8 + 3, 2);
      let libpangleflippedS = 7581274 <= orientation6;
      do {
         orientation6 %= Math.max(3 | orientation6, 2);
         if (libpangleflippedS) {
            break;
         }
      } while (libpangleflippedS && (2 > (orientation6 / (Math.max(3, 8)))));
      remindern = `${sansc.length}`;
      if (orangeo) {
         break;
      }
   } while (orangeo && (5 > remindern.length));
   if (5 > benefit3.length) {
       let dice3 = 4;
       let bellC = 1.0;
      if ((bellC - 3.36) <= 4.77) {
          let eighteenb: Map<any, any> = new Map([[String.fromCharCode(98,105,116,114,101,100,117,99,116,105,111,110,95,53,95,55,52,0),984], [String.fromCharCode(115,108,111,116,115,95,112,95,49,51,0),725], [String.fromCharCode(100,98,108,113,117,111,116,101,95,97,95,57,56,0),649]]);
          let themeV = 2.0;
         dice3 ^= 3;
         eighteenb.set(`${themeV}`, eighteenb.size);
         themeV -= parseInt(`${themeV}`) << (Math.min(Math.abs(eighteenb.size), 2));
      }
         bellC += parseInt(`${bellC}`) >> (Math.min(3, Math.abs(dice3)));
      while (3.12 == (2.15 - bellC) && (bellC - 2.15) == 1.29) {
         bellC /= Math.max(3, parseInt(`${bellC}`));
         break;
      }
          let huaweiW = String.fromCharCode(119,95,57,54,95,109,106,112,101,103,100,101,99,0);
          let regengA: Array<any> = [969, 965, 417];
         bellC += (huaweiW == String.fromCharCode(99,0) ? regengA.length : huaweiW.length);
      let switch_eS = bellC >= 5844251.0;
      do {
          let textinputz = 0.0;
          let ticked8 = String.fromCharCode(118,95,57,57,95,100,105,97,103,110,111,115,116,105,99,115,0);
          let networkZ: Map<any, any> = new Map([[String.fromCharCode(108,95,51,54,95,109,97,110,97,103,101,115,0),113], [String.fromCharCode(99,111,99,111,115,100,120,95,122,95,57,49,0),229]]);
          let inactive8 = String.fromCharCode(116,114,97,99,107,95,119,95,54,56,0);
         bellC /= Math.max(1, (ticked8 == String.fromCharCode(83,0) ? inactive8.length : ticked8.length));
         textinputz += networkZ.size;
         networkZ.set(`${textinputz}`, parseInt(`${textinputz}`));
         inactive8 += `${3 / (Math.max(7, parseInt(`${textinputz}`)))}`;
         if (switch_eS) {
            break;
         }
      } while (switch_eS && (1 == dice3));
      while ((dice3 * bellC) < 5.62) {
         dice3 /= Math.max(3, 3);
         break;
      }
      benefit3 = `${sansc.length << (Math.min(gmailM.length, 4))}`;
   }
      emojiw = [(String.fromCharCode(115,0) == benefit3 ? benefit3.length : gmailM.length)];
       let filterS = String.fromCharCode(119,95,52,53,95,114,101,97,115,115,101,109,98,108,121,0);
       let videocommono = String.fromCharCode(102,95,49,53,95,101,120,105,115,116,105,110,103,0);
       let carouseld = 4.0;
      let subsZ = filterS == String.fromCharCode(57,122,107,107,53,103,51,111,105,117,0);
      do {
         filterS += `${(String.fromCharCode(77,0) == videocommono ? videocommono.length : parseInt(`${carouseld}`))}`;
         if (subsZ) {
            break;
         }
      } while (subsZ && (filterS.length >= 3));
      let settingw = filterS.length >= 6653720;
      do {
         filterS += "3";
         if (settingw) {
            break;
         }
      } while (settingw && (filterS.length >= 5));
      let friendss = String.fromCharCode(100,101,114,95,117,110,106,112,0) == videocommono;
      do {
         videocommono = "3";
         if (friendss) {
            break;
         }
      } while ((2 >= (videocommono.length >> (Math.min(Math.abs(4), 4))) && 1.54 >= (carouseld + 2.17)) && friendss);
      let gradleI = String.fromCharCode(54,52,118,52,48,122,50,101,122,98,0) == videocommono;
      do {
         videocommono = `${parseInt(`${carouseld}`) >> (Math.min(videocommono.length, 4))}`;
         if (gradleI) {
            break;
         }
      } while ((3.34 > (carouseld - 3.71)) && gradleI);
      while (filterS.length == videocommono.length) {
          let hongkong8: Map<any, any> = new Map([[String.fromCharCode(112,95,56,57,95,120,114,101,115,0),true ], [String.fromCharCode(111,110,121,120,99,95,114,95,52,53,0),false ]]);
          let linkB = 1;
         filterS = `${parseInt(`${carouseld}`)}`;
         hongkong8 = new Map([[`${hongkong8.size}`, hongkong8.size + 1]]);
         linkB <<= Math.min(Math.abs(hongkong8.size), 2);
         break;
      }
      let infok = filterS.length >= 8568826;
      do {
          let selectionB = 3;
          let ajax2 = 3.0;
          let gesturesX = String.fromCharCode(101,95,49,54,95,112,97,117,115,101,100,0);
         filterS = `${videocommono.length & selectionB}`;
         selectionB += gesturesX.length;
         ajax2 *= parseInt(`${ajax2}`);
         gesturesX = `${parseInt(`${ajax2}`)}`;
         if (infok) {
            break;
         }
      } while (((filterS.length ^ 1) > 4) && infok);
         videocommono = "1";
          let listw = String.fromCharCode(106,95,51,51,95,102,114,101,101,100,0);
          let applea = String.fromCharCode(116,101,108,101,103,114,97,95,113,95,52,0);
         carouseld *= 2;
         listw = `${applea.length}`;
         applea += `${listw.length}`;
         videocommono += `${parseInt(`${carouseld}`)}`;
      benefit3 += `${2 % (Math.max(9, filterS.length))}`;
      emojiw = [benefit3.length + 3];

    setIsDialogOpen(!isDialogOpen);
  };

  return (
    // <SafeAreaView>
    <ScreenContainer>
      <View style={{ gap: spacing.m }}>
        <TitleWithBackButtonHeader
          title="活动规则"
          headerStyle={{ marginBottom: spacing.m }}
        />

        <View style={{ marginBottom: 100, paddingLeft: 20, paddingRight: 35 }}>
          <Text style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}>
            每推荐成功1位好友，即可增加VIP会员30天，好友获得30天VIP会员
          </Text>

          { }
          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",

              marginTop: 10,
            }}
          >
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              1. 推荐成功条件
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              推荐好友
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              好友下载{APP_NAME_CONST} APP
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              好友注册{APP_NAME_CONST} APP（需填写邀请码）
            </Text>
          </View>

          <Text style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}>
            好友通过您的分享链接，下载并打开app后，注册成功（需填写邀请码）成为新用户即视为推荐成功，双方均可获得奖励
          </Text>

          { }
          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",

              marginTop: 10,
            }}
          >
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              2. 受邀者奖励内容
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              受邀者通过好友分享链接下载并注册（需填写邀请码）成为新用户后，即可获得30日VIP会员，需注意如使用同一台设备或同一个账号无法重复获得奖励
            </Text>
          </View>

          { }
          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",

              marginTop: 10,
            }}
          >
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              3. 附加规则
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              用户达成奖励条件后，奖励会立即生效
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              同一设备、同一账号视为同一用户
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              如果有发现用户存在恶意使用虚假账号领取奖励多情况，平台有权利取消奖励或保留终止活动的权利
            </Text>
          </View>
        </View>
      </View>
    </ScreenContainer>
    // </SafeAreaView>
  );
};

const styles = StyleSheet.create({
  text: {},
  bullet: {
    width: 6,
    height: 6,
    borderRadius: 4,
    backgroundColor: "#fff", 
    marginLeft: 5, 
    marginRight: 8, 
    marginTop: 8,
  },
});
