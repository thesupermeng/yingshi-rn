import React, { useState } from "react";
import {
  View,
  Text,
  StyleSheet,
  TextInput,
  TouchableOpacity,
  SafeAreaView,
} from "react-native";
import ScreenContainer from "../../components/container/tt_backward";
import { RootStackScreenProps } from "@type/tt_temperature";
import TitleWithBackButtonHeader from "../../components/header/tt_typing_tail_header";

import { useTheme } from "@react-navigation/native";
import { ScrollView } from "react-native-gesture-handler";
import { APP_NAME_CONST } from "@utility/tt_trophy_cross";

export default ({ navigation }: RootStackScreenProps<"活动规则">) => {
  const { colors, textVariants, icons, spacing } = useTheme();
  const [isDialogOpen, setIsDialogOpen] = useState(false);

  const toggleOverlay = () => {
       let androidl: Array<any> = [673, 426, 970];
    let formd = 1.0;
    let eighteen0 = 3.0;
    let adult6 = String.fromCharCode(120,95,53,48,95,106,105,116,116,101,114,0);
    let sheetb = String.fromCharCode(118,95,50,49,95,115,116,114,105,110,103,115,0);
    let backgroundY: Array<any> = [531, 84];
    let controlj = String.fromCharCode(116,111,117,99,104,101,115,95,114,95,57,53,0);
    let injuryO = String.fromCharCode(112,95,55,53,95,97,115,121,110,99,100,105,115,112,108,97,121,107,105,116,0);
    let videocommon1 = 2;
    let tumbnail5: Array<any> = [String.fromCharCode(118,97,114,105,110,102,111,95,108,95,50,0), String.fromCharCode(97,99,116,105,118,105,116,121,95,107,95,55,53,0)];
    let eighteenJ = String.fromCharCode(110,95,49,48,48,95,120,109,112,101,103,0);
    let placeholder3 = 1.0;
    let username3 = String.fromCharCode(97,95,52,48,0);
    let playlist0 = 0;
   for (let z = 0; z < 3; z++) {
      controlj = `${2 - sheetb.length}`;
   }
      formd -= 3;
   for (let f = 0; f < 1; f++) {
      injuryO += `${parseInt(`${eighteen0}`)}`;
   }
   let ajaxh = 5788322 >= videocommon1;
   do {
      videocommon1 <<= Math.min(2, Math.abs(3 ^ tumbnail5.length));
      if (ajaxh) {
         break;
      }
   } while (ajaxh && ((videocommon1 | sheetb.length) > 2 && 1 > (2 | sheetb.length)));
      tumbnail5 = [parseInt(`${eighteen0}`)];
   if (3 < (5 | androidl.length) && (androidl.length | tumbnail5.length) < 5) {
      tumbnail5.push(eighteenJ.length);
   }
   while (eighteenJ.length > 1) {
      eighteenJ = `${(adult6 == String.fromCharCode(116,0) ? tumbnail5.length : adult6.length)}`;
      break;
   }
       let turng: Array<any> = [689, 486, 185];
       let stationU = false;
          let giftf = String.fromCharCode(119,99,104,97,114,95,112,95,53,55,0);
         turng.push(turng.length % (Math.max(1, 9)));
         giftf += `${giftf.length + 2}`;
      let refresha = 7515390 <= turng.length;
      do {
          let selectionF = String.fromCharCode(110,95,49,53,95,116,104,101,110,97,98,108,101,0);
          let constants1 = 0.0;
          let mbbannero = 2.0;
          let targett = String.fromCharCode(104,95,57,48,95,108,101,110,118,108,99,0);
         turng.push(parseInt(`${mbbannero}`));
         selectionF += `${parseInt(`${constants1}`) ^ 1}`;
         constants1 -= parseInt(`${constants1}`) | selectionF.length;
         mbbannero += parseInt(`${constants1}`) + targett.length;
         targett = "1";
         if (refresha) {
            break;
         }
      } while (refresha && (!stationU));
         turng = [turng.length];
      while (!stationU) {
         turng = [turng.length];
         break;
      }
         stationU = !stationU;
         stationU = !stationU;
      videocommon1 |= 1;
      formd *= parseInt(`${eighteen0}`) | eighteenJ.length;
      formd += 1;
   for (let n = 0; n < 3; n++) {
      formd *= eighteenJ.length;
   }
   for (let o = 0; o < 3; o++) {
      injuryO = `${parseInt(`${eighteen0}`)}`;
   }
      tumbnail5 = [controlj.length];
      androidl = [2];
   let searchbarz = injuryO.length <= 8423506;
   do {
      injuryO = `${tumbnail5.length}`;
      if (searchbarz) {
         break;
      }
   } while (searchbarz && (controlj.length >= 1));
   if (2 == (2 >> (Math.min(3, backgroundY.length))) && 5 == (backgroundY.length >> (Math.min(Math.abs(2), 4)))) {
      sheetb += `${2 | tumbnail5.length}`;
   }
       let googleK: Map<any, any> = new Map([[String.fromCharCode(99,109,97,99,95,48,95,51,50,0),269], [String.fromCharCode(112,114,111,118,105,115,105,111,110,95,113,95,57,51,0),889]]);
      if (!Array.from(googleK.values()).includes(googleK.size)) {
         googleK.set(`${googleK.size}`, googleK.size << (Math.min(Math.abs(2), 4)));
      }
       let type_vg1: Map<any, any> = new Map([[String.fromCharCode(115,105,110,99,101,95,122,95,48,0),399], [String.fromCharCode(99,97,99,104,101,115,105,122,101,95,51,95,57,52,0),230]]);
       let areay: Map<any, any> = new Map([[String.fromCharCode(109,95,53,55,95,115,117,105,116,101,0),String.fromCharCode(105,95,55,50,95,115,116,97,116,101,112,0)], [String.fromCharCode(113,95,55,49,95,105,110,105,116,105,97,108,105,122,97,116,105,111,110,115,0),String.fromCharCode(104,101,118,109,97,115,107,95,106,95,56,57,0)]]);
         type_vg1 = new Map([[`${googleK.size}`, type_vg1.size]]);
      injuryO += "1";
   let emptyO = formd <= 8058499.0;
   do {
       let x_imageI = false;
       let weiboi: Map<any, any> = new Map([[String.fromCharCode(111,98,116,97,105,110,95,50,95,51,53,0),String.fromCharCode(109,117,108,116,105,108,105,110,101,95,119,95,52,0)], [String.fromCharCode(121,95,49,48,95,97,97,99,101,110,99,100,115,112,0),String.fromCharCode(114,95,49,51,95,99,111,111,114,100,105,110,97,116,111,114,0)], [String.fromCharCode(113,95,51,52,95,101,118,97,108,117,97,116,101,0),String.fromCharCode(99,95,53,54,95,114,101,97,115,111,110,0)]]);
       let q_positiong: Array<any> = [981, 765, 206];
       let buildU = 5;
       let upgradei = false;
      while (5 <= (5 | weiboi.size) || (5 | weiboi.size) <= 3) {
         weiboi.set(`${buildU}`, 3 | q_positiong.length);
         break;
      }
      let lessB = upgradei ? !upgradei : upgradei;
      do {
         upgradei = !upgradei;
         if (lessB) {
            break;
         }
      } while (lessB && (!x_imageI));
      if (buildU == 1) {
          let hooki: Array<any> = [578, 810];
          let nativeexE: Array<any> = [161, 241, 369];
         buildU += 2;
         hooki.push(nativeexE.length - 2);
         nativeexE.push(hooki.length);
      }
      let forwardW = upgradei ? !upgradei : upgradei;
      do {
         upgradei = !upgradei;
         if (forwardW) {
            break;
         }
      } while ((5 >= q_positiong.length) && forwardW);
      while (x_imageI) {
          let inviteP = String.fromCharCode(104,95,57,55,95,97,102,116,101,114,108,105,102,101,0);
          let setting5 = String.fromCharCode(103,95,52,50,95,104,116,109,108,109,97,114,107,117,112,0);
         x_imageI = setting5.length >= 70;
         inviteP += `${(String.fromCharCode(112,0) == inviteP ? inviteP.length : inviteP.length)}`;
         setting5 += `${(inviteP == String.fromCharCode(108,0) ? inviteP.length : inviteP.length)}`;
         break;
      }
      let megaphoneK = 9528456 >= weiboi.size;
      do {
         weiboi.set(`${x_imageI}`, ((x_imageI ? 1 : 3) * 3));
         if (megaphoneK) {
            break;
         }
      } while (megaphoneK && (Array.from(weiboi.keys()).includes(`${q_positiong.length}`)));
      let calendaro = weiboi.size <= 5551219;
      do {
         weiboi = new Map([[`${q_positiong.length}`, 1]]);
         if (calendaro) {
            break;
         }
      } while (calendaro && ((weiboi.size << (Math.min(Math.abs(3), 4))) > 1 && 4 > (q_positiong.length << (Math.min(Math.abs(3), 5)))));
      while (weiboi.size < 5 || (weiboi.size + 5) < 5) {
         weiboi = new Map([[`${buildU}`, 1]]);
         break;
      }
         weiboi.set(`${x_imageI}`, (weiboi.size << (Math.min(1, Math.abs((x_imageI ? 4 : 3))))));
       let previewT: Map<any, any> = new Map([[String.fromCharCode(117,110,122,116,101,108,108,95,119,95,52,52,0),false ], [String.fromCharCode(105,95,52,54,95,101,108,105,109,105,110,97,116,105,111,110,0),false ], [String.fromCharCode(106,95,53,55,95,114,101,102,117,110,100,0),true ]]);
       let emojia: Map<any, any> = new Map([[String.fromCharCode(100,99,115,116,114,95,109,95,52,53,0),String.fromCharCode(106,95,49,53,95,109,111,110,116,104,115,0)], [String.fromCharCode(112,95,51,52,95,114,101,112,108,97,99,101,115,0),String.fromCharCode(115,99,104,101,109,97,115,95,109,95,51,54,0)]]);
          let customw = 1.0;
         emojia = new Map([[`${q_positiong.length}`, 3 << (Math.min(2, q_positiong.length))]]);
         customw += parseInt(`${customw}`);
      if (1 >= emojia.size) {
          let castingX = String.fromCharCode(118,95,52,55,95,116,114,97,110,115,108,97,116,105,111,110,115,0);
          let resendo = String.fromCharCode(97,95,51,57,95,116,97,110,0);
          let sideI = String.fromCharCode(110,95,55,57,95,100,98,108,105,110,116,0);
         emojia = new Map([[`${emojia.size}`, emojia.size]]);
         castingX += `${2 + sideI.length}`;
         resendo = `${resendo.length}`;
         sideI = "3";
      }
         q_positiong = [3];
      if (3 == (q_positiong.length / 1) || 4 == (emojia.size / 1)) {
         q_positiong = [(previewT.size | (upgradei ? 1 : 2))];
      }
         x_imageI = upgradei;
      formd -= controlj.length % 2;
      if (emptyO) {
         break;
      }
   } while (emptyO && (3.56 > (formd * sheetb.length) || 3 > (sheetb.length << (Math.min(Math.abs(3), 1)))));
   let pauseD = 5791130 >= backgroundY.length;
   do {
      backgroundY = [2 - parseInt(`${placeholder3}`)];
      if (pauseD) {
         break;
      }
   } while (pauseD && (3 <= (3 | eighteenJ.length) || (backgroundY.length | 3) <= 2));
   while (2 <= eighteenJ.length) {
       let emptyM = String.fromCharCode(97,98,115,108,95,53,95,53,0);
       let specw = 2.0;
       let linkN = 3.0;
       let fieldu = String.fromCharCode(99,111,115,116,115,95,108,95,54,49,0);
       let updates0: Array<any> = [744, 247];
       let auto_7cz = String.fromCharCode(100,101,116,101,114,109,105,110,105,115,116,105,99,95,121,95,52,52,0);
       let combinei = String.fromCharCode(116,101,115,116,114,97,110,115,95,106,95,51,50,0);
         emptyM = "3";
      for (let n = 0; n < 3; n++) {
         linkN += 3 & combinei.length;
      }
      for (let q = 0; q < 1; q++) {
          let backM = String.fromCharCode(115,117,98,115,99,114,105,112,116,95,110,95,54,57,0);
          let long_8dl = String.fromCharCode(120,95,50,51,95,112,114,105,109,101,115,0);
         emptyM += `${fieldu.length | parseInt(`${specw}`)}`;
         backM += `${backM.length}`;
         long_8dl = `${backM.length}`;
      }
         combinei += `${(emptyM == String.fromCharCode(65,0) ? parseInt(`${specw}`) : emptyM.length)}`;
          let list2: Map<any, any> = new Map([[String.fromCharCode(101,95,49,48,48,95,112,97,121,109,97,115,116,101,114,0),16], [String.fromCharCode(107,95,49,57,95,97,117,100,105,111,105,110,116,101,114,108,101,97,118,101,0),43]]);
          let tooltipsa = String.fromCharCode(112,95,49,95,99,108,111,115,101,115,111,99,107,101,116,0);
          let orangem: Map<any, any> = new Map([[String.fromCharCode(113,95,52,53,95,105,116,97,108,105,99,0),String.fromCharCode(115,121,109,98,111,108,105,99,97,116,97,98,108,101,95,97,95,57,54,0)], [String.fromCharCode(120,95,52,56,95,115,99,97,110,116,97,98,108,101,0),String.fromCharCode(118,101,114,116,101,120,95,49,95,57,49,0)], [String.fromCharCode(113,95,52,50,95,105,111,115,0),String.fromCharCode(119,95,50,57,95,98,108,111,98,115,105,122,101,0)]]);
         linkN += updates0.length | emptyM.length;
         list2.set(tooltipsa, 3);
         tooltipsa += `${tooltipsa.length % 2}`;
         orangem = new Map([[`${list2.size}`, 3]]);
          let controll = 0.0;
         auto_7cz = `${parseInt(`${specw}`)}`;
         controll += parseFloat(`${parseInt(`${controll}`) >> (Math.min(5, Math.abs(2)))}`);
         linkN -= 2 >> (Math.min(5, fieldu.length));
      let default_uX = auto_7cz.length <= 9732855;
      do {
         auto_7cz += "1";
         if (default_uX) {
            break;
         }
      } while (default_uX && (emptyM.length == auto_7cz.length));
          let modelst = 0.0;
          let screenS: Array<any> = [560, 704, 237];
         fieldu += `${parseInt(`${modelst}`)}`;
         modelst *= parseFloat(`${screenS.length - screenS.length}`);
      for (let o = 0; o < 1; o++) {
          let china0 = 5;
         combinei = `${updates0.length | 2}`;
         china0 &= china0 ^ 2;
      }
          let championM = String.fromCharCode(100,105,115,112,108,97,121,115,95,114,95,50,49,0);
         combinei = `${combinei.length}`;
         championM = "1";
      for (let l = 0; l < 2; l++) {
         emptyM = `${combinei.length}`;
      }
      for (let u = 0; u < 1; u++) {
          let o_count_ = 2;
         specw -= 1;
         o_count_ |= 2;
      }
       let updatesY = String.fromCharCode(109,95,49,57,95,114,105,100,0);
       let completev = String.fromCharCode(116,111,107,101,110,105,122,101,114,95,100,95,55,57,0);
      tumbnail5 = [emptyM.length & 2];
      break;
   }
   for (let o = 0; o < 3; o++) {
      eighteenJ += `${videocommon1 << (Math.min(Math.abs(parseInt(`${eighteen0}`)), 4))}`;
   }
       let grayW = String.fromCharCode(97,102,116,101,114,101,102,102,101,99,116,115,107,101,121,112,97,116,104,95,56,95,52,48,0);
      while (grayW.endsWith(`${grayW.length}`)) {
         grayW = `${1 >> (Math.min(1, grayW.length))}`;
         break;
      }
          let completeS: Array<any> = [322, 14];
          let controllh = 1.0;
          let vignetted = false;
         grayW = `${((vignetted ? 5 : 2) / (Math.max(parseInt(`${controllh}`), 8)))}`;
         completeS.push(completeS.length);
         controllh *= 3 / (Math.max(2, completeS.length));
      if (grayW.includes(grayW)) {
         grayW += `${grayW.length}`;
      }
      injuryO += "2";
   while (injuryO.length >= 3) {
       let sidek = String.fromCharCode(102,95,54,53,95,112,108,97,110,0);
       let currentr = String.fromCharCode(97,117,116,111,100,101,116,101,99,116,105,111,110,95,117,95,53,54,0);
       let single3 = String.fromCharCode(108,95,50,52,95,98,105,103,0);
       let floatings: Array<any> = [String.fromCharCode(118,95,51,95,105,110,97,99,116,105,118,101,0), String.fromCharCode(97,95,56,53,95,116,97,114,103,101,116,101,100,0), String.fromCharCode(120,95,53,57,95,100,101,108,111,103,111,0)];
          let internetd = 0;
         currentr = `${1 + internetd}`;
         floatings = [floatings.length];
      let feedbackw = floatings.length >= 8998864;
      do {
         floatings.push(1 | currentr.length);
         if (feedbackw) {
            break;
         }
      } while (feedbackw && (single3.includes(`${floatings.length}`)));
      for (let c = 0; c < 1; c++) {
         single3 = `${floatings.length - 2}`;
      }
       let helperw = String.fromCharCode(112,114,101,118,105,111,117,115,95,120,95,57,52,0);
      let ballX = currentr == String.fromCharCode(117,51,57,113,108,0);
      do {
          let rewardvideow = 3;
         currentr = "3";
         rewardvideow >>= Math.min(1, Math.abs(rewardvideow));
         if (ballX) {
            break;
         }
      } while (ballX && (!currentr.endsWith(`${floatings.length}`)));
      let liver = 6983353 >= floatings.length;
      do {
          let sportj = 5;
          let switch_tgL = String.fromCharCode(111,118,101,114,104,101,97,100,95,98,95,49,55,0);
          let sentryy = String.fromCharCode(97,118,99,116,120,95,119,95,51,54,0);
         floatings = [1 << (Math.min(2, sidek.length))];
         sportj %= Math.max(5, sentryy.length);
         switch_tgL += `${(String.fromCharCode(104,0) == switch_tgL ? sportj : switch_tgL.length)}`;
         sentryy = `${sportj / (Math.max(switch_tgL.length, 10))}`;
         if (liver) {
            break;
         }
      } while (liver && ((floatings.length - helperw.length) > 1));
      for (let j = 0; j < 2; j++) {
         floatings = [helperw.length * sidek.length];
      }
          let mimo_ = 2;
         currentr += `${1 >> (Math.min(Math.abs(mimo_), 3))}`;
      while ((floatings.length / (Math.max(3, 3))) == 2 || (3 / (Math.max(3, floatings.length))) == 3) {
         currentr += `${floatings.length}`;
         break;
      }
          let sheetA = true;
          let gemfilen = String.fromCharCode(102,95,49,51,95,121,111,117,114,0);
          let halfq: Map<any, any> = new Map([[String.fromCharCode(100,101,113,117,101,117,101,95,102,95,50,51,0),2], [String.fromCharCode(112,104,111,116,111,115,95,57,95,50,0),513]]);
         currentr += `${currentr.length - sidek.length}`;
         sheetA = 82 == halfq.size;
         gemfilen += `${halfq.size % (Math.max(gemfilen.length, 6))}`;
      while (5 > (2 << (Math.min(2, floatings.length)))) {
         floatings.push(1 | currentr.length);
         break;
      }
      eighteenJ += `${eighteenJ.length}`;
      break;
   }
       let detailsH = 3.0;
       let routerx = String.fromCharCode(97,114,103,115,95,121,95,56,53,0);
         detailsH *= routerx.length % 3;
          let controlq = String.fromCharCode(99,115,119,97,112,95,109,95,56,48,0);
          let sidee = 1.0;
          let searchF = String.fromCharCode(122,95,51,51,95,99,111,110,115,101,99,117,116,105,118,101,0);
         detailsH += 3 * parseInt(`${sidee}`);
         controlq = "2";
         sidee /= Math.max(2, searchF.length);
         searchF = `${controlq.length}`;
         routerx += `${(routerx == String.fromCharCode(67,0) ? routerx.length : parseInt(`${detailsH}`))}`;
          let result4 = 1.0;
          let mbridgek: Array<any> = [415, 524];
          let streaming9 = 1.0;
         routerx += `${2 >> (Math.min(Math.abs(parseInt(`${detailsH}`)), 4))}`;
         result4 *= parseFloat(`${mbridgek.length}`);
         mbridgek = [1 << (Math.min(5, mbridgek.length))];
         streaming9 -= 2;
         routerx += `${3 & parseInt(`${detailsH}`)}`;
      let project2 = 6221488 <= routerx.length;
      do {
         routerx += `${3 / (Math.max(7, parseInt(`${detailsH}`)))}`;
         if (project2) {
            break;
         }
      } while ((3 < (parseInt(`${detailsH}`) / (Math.max(routerx.length, 3))) && (routerx.length & 3) < 1) && project2);
      eighteenJ += `${controlj.length % (Math.max(9, parseInt(`${placeholder3}`)))}`;
   for (let p = 0; p < 1; p++) {
      controlj = `${eighteenJ.length}`;
   }

    setIsDialogOpen(!isDialogOpen);
  };

  return (
    // <SafeAreaView>
    <ScreenContainer>
      <View style={{ gap: spacing.m }}>
        <TitleWithBackButtonHeader
          title="活动规则"
          headerStyle={{ marginBottom: spacing.m }}
        />

        <View style={{ marginBottom: 100, paddingLeft: 20, paddingRight: 35 }}>
          <Text style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}>
            每推荐成功1位好友，即可增加VIP会员30天，好友获得30天VIP会员
          </Text>

          { }
          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",

              marginTop: 10,
            }}
          >
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              1. 推荐成功条件
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              推荐好友
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              好友下载{APP_NAME_CONST} APP
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              好友注册{APP_NAME_CONST} APP（需填写邀请码）
            </Text>
          </View>

          <Text style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}>
            好友通过您的分享链接，下载并打开app后，注册成功（需填写邀请码）成为新用户即视为推荐成功，双方均可获得奖励
          </Text>

          { }
          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",

              marginTop: 10,
            }}
          >
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              2. 受邀者奖励内容
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              受邀者通过好友分享链接下载并注册（需填写邀请码）成为新用户后，即可获得30日VIP会员，需注意如使用同一台设备或同一个账号无法重复获得奖励
            </Text>
          </View>

          { }
          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",

              marginTop: 10,
            }}
          >
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              3. 附加规则
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              用户达成奖励条件后，奖励会立即生效
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              同一设备、同一账号视为同一用户
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              如果有发现用户存在恶意使用虚假账号领取奖励多情况，平台有权利取消奖励或保留终止活动的权利
            </Text>
          </View>
        </View>
      </View>
    </ScreenContainer>
    // </SafeAreaView>
  );
};

const styles = StyleSheet.create({
  text: {},
  bullet: {
    width: 6,
    height: 6,
    borderRadius: 4,
    backgroundColor: "#fff", 
    marginLeft: 5, 
    marginRight: 8, 
    marginTop: 8,
  },
});
