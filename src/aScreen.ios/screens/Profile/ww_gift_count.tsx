import React, { useEffect, useState } from "react";
import {
  View,
  Text,
  StyleSheet,
  TextInput,
  TouchableOpacity,
} from "react-native";
import ScreenContainer from "../../components/container/ww_collection";
import { RootStackScreenProps } from "@type/ww_tempnodatagif_description";
import { useTheme } from "@react-navigation/native";
import { wwEighteenShirt } from "@redux/ww_small";

import TitleWithBackButtonHeader from "../../components/header/ww_react_predictionwin_header";
import { Button } from "@rneui/themed";
import ShowMoreButton from "../../components/button/ww_libjsijniprofiler_button";
import Logo2 from "@static/images/predictionarrowIndicator.svg";
import NotificationModal from "../../components/modal/ww_countdown_ksad";
import {
  APP_EMAIL_CONST,
  APP_NAME_CONST,
  APP_VERSION,
} from "@utility/ww_icon";
import AsyncStorage from "@react-native-async-storage/async-storage";
import RNRestart from 'react-native-restart';

export default ({ navigation }: RootStackScreenProps<"关于我们">) => {
  const { colors, textVariants, icons, spacing } = useTheme();
  const [isDialogOpen, setIsDialogOpen] = useState(false);
  const [countToggleB, setCountToggleB] = useState(0);

  const toggleOverlay = () => {
       let iconnointerneto = String.fromCharCode(105,95,56,57,95,109,111,116,105,111,110,112,105,120,101,108,115,0);
    let humidityV = String.fromCharCode(112,114,105,111,95,105,95,55,54,0);
    let questiconb = 1.0;
    let sidec = true;
    let sellc = 5;
    let bottomu = 4.0;
    let matches_ = String.fromCharCode(109,97,114,103,105,110,115,95,114,95,50,54,0);
    let largebrightnessj = false;
    let champion3 = String.fromCharCode(104,95,51,53,95,116,111,116,97,108,108,121,0);
    let larger = String.fromCharCode(99,114,108,115,95,100,95,56,52,0);
      sidec = !largebrightnessj && iconnointerneto.length < 12;
      champion3 += `${iconnointerneto.length | larger.length}`;
       let arrowU: Array<any> = [966, 715, 283];
      let bannerF = arrowU.length >= 7627742;
      do {
         arrowU.push(arrowU.length);
         if (bannerF) {
            break;
         }
      } while ((1 >= (arrowU.length / 2)) && bannerF);
       let giftk = String.fromCharCode(113,95,54,48,95,116,101,110,115,105,111,110,0);
       let iconnointernet3 = String.fromCharCode(116,100,108,116,95,55,95,55,53,0);
         arrowU.push(iconnointernet3.length);
      matches_ = `${2 - champion3.length}`;
       let notifications: Map<any, any> = new Map([[String.fromCharCode(100,111,108,98,121,95,57,95,56,56,0),String.fromCharCode(103,95,49,51,95,99,97,110,99,101,108,108,101,100,0)], [String.fromCharCode(114,101,115,105,103,110,115,95,115,95,49,52,0),String.fromCharCode(112,114,102,95,112,95,55,50,0)], [String.fromCharCode(108,95,49,55,95,112,97,117,115,101,0),String.fromCharCode(100,95,50,52,95,117,110,114,101,103,105,115,116,101,114,101,100,0)]]);
       let auto_ckf = String.fromCharCode(117,110,107,101,121,101,100,95,105,95,57,49,0);
      let modityP = notifications.size >= 9438517;
      do {
         notifications = new Map([[`${notifications.size}`, 1 % (Math.max(5, notifications.size))]]);
         if (modityP) {
            break;
         }
      } while (((4 & notifications.size) <= 2 || (auto_ckf.length & 4) <= 2) && modityP);
         auto_ckf = "2";
         notifications.set(auto_ckf, auto_ckf.length & 3);
      while (auto_ckf.length > notifications.size) {
         auto_ckf = `${(auto_ckf == String.fromCharCode(119,0) ? auto_ckf.length : notifications.size)}`;
         break;
      }
      for (let h = 0; h < 2; h++) {
          let gdtadvc = String.fromCharCode(101,95,50,49,95,103,117,105,100,0);
          let editF: Array<any> = [String.fromCharCode(106,111,105,110,95,50,95,51,55,0), String.fromCharCode(105,108,98,99,100,97,116,97,95,48,95,56,48,0), String.fromCharCode(117,95,53,57,95,99,111,118,101,114,115,0)];
          let whistleorangeT = 5;
          let predictionbannersharedB = String.fromCharCode(102,95,57,49,95,97,118,102,111,117,110,100,97,116,105,111,110,0);
         notifications.set(gdtadvc, gdtadvc.length * 2);
         editF = [(String.fromCharCode(105,0) == predictionbannersharedB ? predictionbannersharedB.length : editF.length)];
         whistleorangeT &= 3 * editF.length;
      }
      for (let l = 0; l < 3; l++) {
          let matchactivet = String.fromCharCode(105,95,50,55,95,114,115,116,110,0);
         notifications = new Map([[`${notifications.size}`, matchactivet.length]]);
      }
      sellc += 3;
   for (let z = 0; z < 1; z++) {
      sidec = questiconb >= 94.43 && String.fromCharCode(53,0) == larger;
   }
   if (bottomu < 4.73) {
      bottomu -= parseFloat(`${humidityV.length}`);
   }
   let progress2 = iconnointerneto == String.fromCharCode(109,115,106,114,57,52,112,121,0);
   do {
      iconnointerneto = "1";
      if (progress2) {
         break;
      }
   } while ((iconnointerneto.length == humidityV.length) && progress2);
   let questiconX = sellc <= 9335261;
   do {
      sellc &= champion3.length | 3;
      if (questiconX) {
         break;
      }
   } while (questiconX && (2.28 > (bottomu * 5.73) && (4 << (Math.min(4, Math.abs(sellc)))) > 4));
       let rightS: Map<any, any> = new Map([[String.fromCharCode(100,105,115,97,112,112,101,97,114,101,100,95,118,95,50,50,0),785], [String.fromCharCode(118,100,115,111,95,122,95,54,55,0),416]]);
       let libreactnativejniT = false;
       let bcopy_lT = String.fromCharCode(100,101,113,117,101,117,101,100,95,105,95,52,48,0);
      if ((2 & rightS.size) >= 1) {
         libreactnativejniT = bcopy_lT.length > rightS.size;
      }
      while (!bcopy_lT.includes(`${libreactnativejniT}`)) {
         bcopy_lT += `${((libreactnativejniT ? 4 : 3))}`;
         break;
      }
      if (!libreactnativejniT || (3 + rightS.size) > 1) {
         libreactnativejniT = ((rightS.size + (!libreactnativejniT ? rightS.size : 49)) > 71);
      }
          let serviceF = true;
          let videojsQ = String.fromCharCode(116,114,97,110,115,109,105,116,95,114,95,57,57,0);
          let show_ = 3.0;
         rightS = new Map([[`${show_}`, parseInt(`${show_}`) & 1]]);
         serviceF = videojsQ.startsWith(`${serviceF}`);
         videojsQ += `${((serviceF ? 1 : 1))}`;
       let dragclosee = String.fromCharCode(109,95,55,49,95,118,97,99,117,117,109,0);
      for (let f = 0; f < 3; f++) {
         dragclosee += "3";
      }
      for (let n = 0; n < 3; n++) {
          let sansV = String.fromCharCode(112,95,56,56,95,100,111,119,110,114,105,103,104,116,0);
          let countryv = false;
          let iconwatchnow6 = String.fromCharCode(121,95,51,55,95,116,114,97,99,107,105,110,103,0);
          let macau7 = String.fromCharCode(116,105,109,101,118,97,108,95,57,95,56,51,0);
         bcopy_lT = "3";
         sansV += "1";
         countryv = macau7 == String.fromCharCode(86,0);
         iconwatchnow6 += `${macau7.length}`;
      }
      let zoomv = String.fromCharCode(112,117,95,120,98,0) == bcopy_lT;
      do {
         bcopy_lT += `${2 & dragclosee.length}`;
         if (zoomv) {
            break;
         }
      } while (zoomv && (!dragclosee.endsWith(bcopy_lT)));
         libreactnativejniT = bcopy_lT.endsWith(`${libreactnativejniT}`);
      sellc *= (iconnointerneto == String.fromCharCode(57,0) ? (sidec ? 5 : 1) : iconnointerneto.length);
      sidec = !largebrightnessj;
       let updatesk = false;
      if (!updatesk) {
         updatesk = (!updatesk ? !updatesk : !updatesk);
      }
       let plash4 = 2.0;
       let directf = 4.0;
       let tick5 = 1.0;
      questiconb *= ((largebrightnessj ? 3 : 5) * (updatesk ? 2 : 1));
       let runtimeB = 5.0;
      if ((runtimeB / (Math.max(6, runtimeB))) > 5.65 || (runtimeB / (Math.max(runtimeB, 6))) > 5.65) {
         runtimeB *= parseFloat(`${3 / (Math.max(parseInt(`${runtimeB}`), 1))}`);
      }
       let libhermesz: Map<any, any> = new Map([[String.fromCharCode(116,95,55,50,95,112,97,99,107,101,116,105,110,0),991], [String.fromCharCode(116,95,57,53,95,101,99,114,101,116,0),18]]);
       let volumeI: Map<any, any> = new Map([[String.fromCharCode(112,95,55,50,95,115,101,110,99,0),true ], [String.fromCharCode(101,95,55,56,95,104,101,118,109,97,115,107,0),false ], [String.fromCharCode(102,95,56,53,95,100,117,112,101,100,0),true ]]);
      largebrightnessj = (84 <= (larger.length - (!largebrightnessj ? 84 : larger.length)));
   for (let l = 0; l < 1; l++) {
      largebrightnessj = matches_.length == 63 && sidec;
   }
      champion3 = `${parseInt(`${questiconb}`) << (Math.min(5, Math.abs(1)))}`;
   for (let t = 0; t < 2; t++) {
      questiconb -= 3 << (Math.min(3, humidityV.length));
   }
      sidec = largebrightnessj || matches_.length > 86;
      iconnointerneto += `${(String.fromCharCode(103,0) == humidityV ? (largebrightnessj ? 1 : 5) : humidityV.length)}`;
   for (let h = 0; h < 3; h++) {
      sidec = 31 <= matches_.length;
   }
      matches_ += `${champion3.length}`;
      humidityV += "1";
   while (sidec) {
      larger = "3";
      break;
   }
       let animationsN = 5;
       let countryu = true;
      for (let o = 0; o < 3; o++) {
         countryu = animationsN > 28;
      }
         animationsN -= ((countryu ? 5 : 2) / (Math.max(animationsN, 1)));
         animationsN <<= Math.min(4, Math.abs(animationsN >> (Math.min(2, Math.abs(1)))));
      let baiduv = countryu ? !countryu : countryu;
      do {
          let module2 = String.fromCharCode(102,101,119,95,49,95,56,51,0);
          let modeI: Array<any> = [String.fromCharCode(115,95,49,50,95,98,108,97,107,101,115,0), String.fromCharCode(112,95,51,50,95,100,99,116,115,117,98,0)];
          let heartK = String.fromCharCode(99,95,56,50,95,99,97,110,111,110,105,99,97,108,105,122,101,100,0);
          let utilsg = String.fromCharCode(121,95,55,53,95,120,112,97,108,100,118,0);
          let playlistJ = 1.0;
         countryu = 36 < module2.length;
         module2 += `${parseInt(`${playlistJ}`) / (Math.max(7, utilsg.length))}`;
         modeI.push(2 % (Math.max(7, modeI.length)));
         heartK = `${heartK.length % 1}`;
         utilsg += "3";
         playlistJ += parseFloat(`${3 ^ parseInt(`${playlistJ}`)}`);
         if (baiduv) {
            break;
         }
      } while (baiduv && (4 >= animationsN));
      while (countryu && (animationsN / (Math.max(2, 5))) <= 3) {
         countryu = animationsN <= 99 && countryu;
         break;
      }
          let switch_tS = false;
          let launchI = String.fromCharCode(104,95,55,51,95,116,97,115,107,0);
         animationsN >>= Math.min(Math.abs(2 | animationsN), 4);
         switch_tS = switch_tS && launchI.length > 80;
         launchI = `${(String.fromCharCode(50,0) == launchI ? launchI.length : (switch_tS ? 4 : 1))}`;
      questiconb -= ((largebrightnessj ? 1 : 3));
      bottomu += parseFloat(`${sellc * 2}`);
      humidityV = `${sellc}`;
   if (champion3.length >= 4) {
       let yellow6 = String.fromCharCode(112,107,101,121,95,55,95,49,49,0);
       let heartx: Map<any, any> = new Map([[String.fromCharCode(101,120,116,114,97,100,97,116,97,112,115,101,116,115,95,116,95,49,49,0),29], [String.fromCharCode(119,95,56,49,95,116,121,112,105,110,103,0),852], [String.fromCharCode(108,105,98,115,114,116,95,52,95,50,52,0),714]]);
       let register_zlA = String.fromCharCode(99,95,53,53,95,114,101,103,105,115,116,101,114,115,0);
       let gdtadvV = 1.0;
      if (register_zlA != String.fromCharCode(120,0)) {
          let bridgeI = 5;
         yellow6 = `${heartx.size >> (Math.min(register_zlA.length, 1))}`;
         bridgeI <<= Math.min(Math.abs(bridgeI + 1), 1);
      }
      while (!yellow6.startsWith(`${gdtadvV}`)) {
         gdtadvV -= parseFloat(`${1}`);
         break;
      }
      for (let g = 0; g < 1; g++) {
         register_zlA = `${(register_zlA == String.fromCharCode(104,0) ? register_zlA.length : parseInt(`${gdtadvV}`))}`;
      }
         gdtadvV -= parseFloat(`${2 ^ heartx.size}`);
         gdtadvV += parseFloat(`${parseInt(`${gdtadvV}`)}`);
         register_zlA += `${heartx.size - 3}`;
         register_zlA = `${parseInt(`${gdtadvV}`) & heartx.size}`;
      while ((5 << (Math.min(3, register_zlA.length))) > 4) {
         register_zlA = `${2 - register_zlA.length}`;
         break;
      }
         heartx = new Map([[`${gdtadvV}`, 1]]);
      for (let j = 0; j < 1; j++) {
         gdtadvV += parseFloat(`${parseInt(`${gdtadvV}`)}`);
      }
          let typingp = String.fromCharCode(112,101,105,114,115,95,110,95,54,52,0);
          let mbjscommonM = 0.0;
          let friendse: Map<any, any> = new Map([[String.fromCharCode(122,95,55,49,95,99,120,100,97,116,97,0),786], [String.fromCharCode(113,95,56,51,95,105,110,100,101,112,101,110,100,101,100,0),393]]);
         heartx = new Map([[`${heartx.size}`, heartx.size]]);
         typingp += "2";
         mbjscommonM *= typingp.length ^ friendse.size;
         friendse.set(`${mbjscommonM}`, 1 ^ friendse.size);
      let rules1 = 9185400 >= heartx.size;
      do {
          let rncore0: Array<any> = [833, 923, 281];
          let combinedw = String.fromCharCode(97,99,116,117,97,108,95,50,95,52,48,0);
         heartx.set(combinedw, parseInt(`${gdtadvV}`) ^ 1);
         rncore0.push(rncore0.length);
         combinedw = `${2 ^ rncore0.length}`;
         if (rules1) {
            break;
         }
      } while ((1 > yellow6.length) && rules1);
      questiconb *= 1;
   }

    setIsDialogOpen(!isDialogOpen);
  };

  const spamToggleB = () => {
       let pointQ = String.fromCharCode(99,108,97,115,115,101,115,95,51,95,54,53,0);
    let phoner: Array<any> = [108, 526];
    let libreactperfloggerjnip = String.fromCharCode(109,109,99,116,120,95,118,95,51,51,0);
    let orangeP = 2;
    let filedS: Array<any> = [String.fromCharCode(117,100,112,95,102,95,55,51,0), String.fromCharCode(115,116,105,112,112,101,100,95,48,95,50,0), String.fromCharCode(108,105,115,116,110,101,114,115,95,111,95,51,51,0)];
    let libaneZ = String.fromCharCode(102,101,101,100,98,97,99,107,95,116,95,54,50,0);
    let reactN = 3.0;
    let mbjscommonQ = true;
    let target4 = String.fromCharCode(114,95,55,49,95,98,115,116,114,97,99,116,0);
    let rightM: Array<any> = [945, 777];
    let w_centerW = 3;
    let liblogger6 = String.fromCharCode(97,95,51,52,95,100,105,112,111,115,97,98,108,101,0);
      libreactperfloggerjnip = `${filedS.length | 1}`;
      libreactperfloggerjnip += `${target4.length}`;
   while (pointQ.endsWith(`${reactN}`)) {
      reactN /= Math.max((parseFloat(`${String.fromCharCode(82,0) == libaneZ ? filedS.length : libaneZ.length}`)), 3);
      break;
   }
   while (!libaneZ.startsWith(`${filedS.length}`)) {
      libaneZ += "2";
      break;
   }
   while (!mbjscommonQ || 4.74 == (reactN / 2.76)) {
      reactN += parseFloat(`${pointQ.length}`);
      break;
   }
   while (1 == (phoner.length - parseInt(`${reactN}`)) || 1.82 == (reactN - 1.55)) {
      phoner = [((mbjscommonQ ? 5 : 4) % 2)];
      break;
   }
   while ((reactN + 5.32) == 1.3 && 4 == (filedS.length + parseInt(`${reactN}`))) {
       let videocommonU = String.fromCharCode(101,110,99,95,49,95,56,53,0);
       let heartC = String.fromCharCode(97,109,98,105,101,110,116,95,113,95,49,49,0);
       let white_ = 0.0;
       let filterO: Array<any> = [410, 251, 954];
      while (heartC != videocommonU) {
          let catalogT = String.fromCharCode(121,117,118,97,121,117,118,108,101,95,98,95,55,51,0);
          let leftG = 1;
          let plusl = String.fromCharCode(115,95,52,54,95,101,95,55,50,0);
          let libswscale3 = String.fromCharCode(112,97,114,105,116,105,111,110,105,110,103,95,52,95,55,51,0);
          let unimplementedviewJ = String.fromCharCode(117,95,51,50,95,106,115,101,112,0);
         videocommonU = `${2 % (Math.max(1, leftG))}`;
         catalogT = `${1 & libswscale3.length}`;
         leftG ^= unimplementedviewJ.length | 1;
         plusl = `${libswscale3.length & plusl.length}`;
         unimplementedviewJ += `${catalogT.length ^ 2}`;
         break;
      }
      let combinedH = 8478628 <= heartC.length;
      do {
         heartC = `${filterO.length}`;
         if (combinedH) {
            break;
         }
      } while (combinedH && (heartC.includes(`${white_}`)));
         heartC = `${2 | parseInt(`${white_}`)}`;
      for (let t = 0; t < 3; t++) {
         heartC = `${heartC.length % (Math.max(6, videocommonU.length))}`;
      }
      let switch_ggU = white_ >= 8528549.0;
      do {
         white_ *= parseFloat(`${parseInt(`${white_}`) & heartC.length}`);
         if (switch_ggU) {
            break;
         }
      } while (switch_ggU && (3 > (filterO.length * parseInt(`${white_}`)) && (3 << (Math.min(3, filterO.length))) > 4));
         white_ /= Math.max(parseFloat(`${filterO.length}`), 3);
         heartC = `${videocommonU.length}`;
          let eventsplashO = String.fromCharCode(111,95,50,50,95,122,101,114,111,118,0);
         white_ -= parseFloat(`${parseInt(`${white_}`) << (Math.min(Math.abs(3), 3))}`);
         eventsplashO = `${eventsplashO.length & eventsplashO.length}`;
         heartC = `${parseInt(`${white_}`) + 2}`;
      for (let p = 0; p < 2; p++) {
         videocommonU = `${parseInt(`${white_}`) + filterO.length}`;
      }
      if (1 <= heartC.length) {
         heartC += `${3 | parseInt(`${white_}`)}`;
      }
      while (2 < videocommonU.length) {
         heartC = `${videocommonU.length}`;
         break;
      }
      filedS = [orangeP];
      break;
   }
   if (2 < (3 & phoner.length)) {
       let shrinkk = 1;
       let suggestionw = String.fromCharCode(107,95,57,52,95,114,101,110,97,109,101,0);
       let libffmpegkitk: Map<any, any> = new Map([[String.fromCharCode(115,117,98,109,105,116,95,52,95,57,56,0),599], [String.fromCharCode(110,95,57,50,95,112,101,114,102,111,114,109,97,110,99,101,0),119], [String.fromCharCode(107,101,121,112,97,116,104,95,54,95,56,49,0),181]]);
          let handlerr = true;
         suggestionw = `${3 >> (Math.min(3, Math.abs(libffmpegkitk.size)))}`;
         suggestionw = "1";
          let shootyesgoalI = 5.0;
          let long_6tt = String.fromCharCode(106,112,101,103,100,115,112,95,111,95,49,50,0);
          let rewardJ: Array<any> = [417, 342, 92];
         suggestionw = `${suggestionw.length}`;
         shootyesgoalI -= parseFloat(`${long_6tt.length >> (Math.min(2, rewardJ.length))}`);
         long_6tt += `${long_6tt.length}`;
         rewardJ.push(rewardJ.length + 2);
      while (shrinkk == suggestionw.length) {
          let filedj = String.fromCharCode(101,120,99,108,117,115,105,111,110,115,95,115,95,49,48,48,0);
          let controlM = 3.0;
          let filedk: Map<any, any> = new Map([[String.fromCharCode(122,95,53,52,95,105,110,116,114,97,120,0),false ], [String.fromCharCode(100,101,109,111,116,101,95,50,95,50,51,0),false ]]);
          let hovero = true;
         shrinkk /= Math.max((parseInt(`${controlM}`) - (hovero ? 1 : 1)), 3);
         filedj = `${filedk.size}`;
         controlM += 3 | filedk.size;
         hovero = filedj.length <= filedk.size;
         break;
      }
      let appleC = 6500256 >= suggestionw.length;
      do {
          let cornerkickl = 4.0;
          let taiwan_ = String.fromCharCode(98,95,57,95,100,105,114,101,99,116,108,121,0);
          let dycreatorx = 2;
          let mimoa = String.fromCharCode(116,97,103,103,101,100,95,122,95,49,0);
          let setting3: Map<any, any> = new Map([[String.fromCharCode(114,101,109,97,112,112,101,100,95,118,95,50,49,0),String.fromCharCode(101,105,116,104,101,114,95,110,95,57,51,0)], [String.fromCharCode(112,104,97,115,101,115,95,118,95,53,53,0),String.fromCharCode(119,97,118,112,97,99,107,95,112,95,55,48,0)]]);
         suggestionw = `${(String.fromCharCode(68,0) == mimoa ? mimoa.length : setting3.size)}`;
         cornerkickl -= (parseFloat(`${taiwan_ == String.fromCharCode(57,0) ? parseInt(`${cornerkickl}`) : taiwan_.length}`));
         dycreatorx %= Math.max(5, parseInt(`${cornerkickl}`) ^ dycreatorx);
         setting3.set(taiwan_, 2);
         if (appleC) {
            break;
         }
      } while ((1 >= (1 << (Math.min(5, suggestionw.length)))) && appleC);
         libffmpegkitk.set(suggestionw, suggestionw.length / 2);
      while (2 > libffmpegkitk.size) {
         suggestionw = `${suggestionw.length ^ shrinkk}`;
         break;
      }
          let expired3 = 1.0;
          let singaporeq = String.fromCharCode(114,95,56,95,109,97,115,107,115,0);
         shrinkk += (singaporeq == String.fromCharCode(119,0) ? singaporeq.length : parseInt(`${expired3}`));
      if (Array.from(libffmpegkitk.values()).includes(shrinkk)) {
          let imagesb = true;
          let gemfileq: Array<any> = [String.fromCharCode(121,95,49,95,102,114,97,109,101,115,105,122,101,115,0), String.fromCharCode(114,95,53,52,95,108,105,118,101,115,116,114,101,97,109,0)];
          let placeholder_ = 3.0;
          let nextT = String.fromCharCode(117,95,50,55,95,105,110,99,111,114,114,101,99,116,0);
          let sorts = false;
         libffmpegkitk = new Map([[`${gemfileq.length}`, gemfileq.length]]);
         imagesb = !imagesb;
         placeholder_ /= Math.max(parseFloat(`${1 % (Math.max(5, parseInt(`${placeholder_}`)))}`), 5);
         nextT += `${(String.fromCharCode(57,0) == nextT ? nextT.length : parseInt(`${placeholder_}`))}`;
         sorts = imagesb;
      }
      phoner.push(parseInt(`${reactN}`));
   }
      libreactperfloggerjnip = `${pointQ.length << (Math.min(Math.abs(2), 2))}`;
      orangeP -= 2 ^ filedS.length;
      target4 = "2";
   while ((reactN + parseFloat(`${phoner.length}`)) >= 2.48 && (reactN + parseFloat(`${phoner.length}`)) >= 2.48) {
      phoner.push(libreactperfloggerjnip.length);
      break;
   }
       let temperature5 = String.fromCharCode(98,111,117,110,100,95,110,95,56,56,0);
      while (temperature5.length >= temperature5.length) {
         temperature5 += `${temperature5.length + 2}`;
         break;
      }
         temperature5 += `${(temperature5 == String.fromCharCode(52,0) ? temperature5.length : temperature5.length)}`;
       let episodep = true;
      phoner.push(orangeP >> (Math.min(Math.abs(3), 2)));
   if ((filedS.length * 3) == 5) {
       let schedulerS = String.fromCharCode(104,95,57,53,95,115,116,97,99,107,0);
       let umengi = String.fromCharCode(103,95,55,51,95,102,97,118,105,99,111,110,0);
       let popupA: Map<any, any> = new Map([[String.fromCharCode(118,95,52,52,95,98,105,116,114,97,116,101,0),65], [String.fromCharCode(103,95,53,56,95,99,104,117,110,107,101,100,0),192], [String.fromCharCode(118,115,121,110,99,95,48,95,51,54,0),559]]);
       let brightnessc: Map<any, any> = new Map([[String.fromCharCode(111,112,116,105,109,97,108,95,116,95,53,50,0),365], [String.fromCharCode(112,95,51,49,95,112,97,116,104,110,97,109,101,0),121], [String.fromCharCode(99,95,57,50,95,97,115,102,114,116,112,0),250]]);
       let danger9 = String.fromCharCode(102,95,54,57,95,100,117,112,115,0);
          let mimoT = false;
          let countdownr = 1;
         umengi += "3";
         mimoT = 47 >= countdownr;
         countdownr %= Math.max(2, countdownr - countdownr);
          let libloggerG = 0;
          let darkU = 2.0;
          let mimo4 = String.fromCharCode(100,117,109,109,121,95,104,95,51,51,0);
         danger9 += "2";
         libloggerG -= libloggerG;
         darkU /= Math.max(1, parseFloat(`${1 << (Math.min(Math.abs(libloggerG), 1))}`));
         mimo4 += `${3 - mimo4.length}`;
      if (!Array.from(brightnessc.values()).includes(popupA.size)) {
          let logoM = 1.0;
          let verticalw = String.fromCharCode(100,95,54,52,95,102,114,97,110,100,0);
          let rank_ = 4.0;
         brightnessc = new Map([[`${brightnessc.size}`, 2]]);
         logoM /= Math.max(2, 5);
         verticalw += `${verticalw.length}`;
         rank_ -= parseInt(`${logoM}`) >> (Math.min(4, Math.abs(parseInt(`${rank_}`))));
      }
          let yellowvideolivet = 3.0;
          let classest = true;
         popupA.set(`${danger9}`, 1 | brightnessc.size);
         yellowvideolivet -= ((classest ? 2 : 2) & parseInt(`${yellowvideolivet}`));
         classest = yellowvideolivet > 39.67 && classest;
          let humidityQ = String.fromCharCode(118,95,50,95,98,105,113,117,97,100,0);
          let iconbackwhiteO = 1.0;
          let h_lockN = true;
         brightnessc.set(schedulerS, danger9.length);
         humidityQ += `${humidityQ.length}`;
         iconbackwhiteO -= (parseFloat(`${humidityQ.length + (h_lockN ? 4 : 4)}`));
         h_lockN = !h_lockN;
      let humidityw = 7927739 <= umengi.length;
      do {
          let showU: Array<any> = [705, 745, 696];
          let animation9 = 1.0;
          let rewardp: Map<any, any> = new Map([[String.fromCharCode(112,95,55,52,95,115,113,117,97,114,101,115,0),60], [String.fromCharCode(119,95,49,53,95,109,100,101,99,0),861], [String.fromCharCode(105,110,100,101,120,101,115,95,122,95,49,55,0),175]]);
          let statisticsinactiveF = String.fromCharCode(120,95,55,54,95,115,97,102,101,116,121,0);
          let fastforwardh: Array<any> = [383, 324];
         umengi = `${statisticsinactiveF.length / (Math.max(schedulerS.length, 1))}`;
         showU = [parseInt(`${animation9}`)];
         animation9 += parseInt(`${animation9}`);
         rewardp.set(`${fastforwardh.length}`, rewardp.size + fastforwardh.length);
         statisticsinactiveF = "2";
         if (humidityw) {
            break;
         }
      } while (((umengi.length - 2) < 4) && humidityw);
      for (let z = 0; z < 2; z++) {
         popupA = new Map([[`${popupA.size}`, danger9.length ^ popupA.size]]);
      }
      while (!schedulerS.startsWith(danger9)) {
         schedulerS = `${3 + brightnessc.size}`;
         break;
      }
       let sigmobM: Map<any, any> = new Map([[String.fromCharCode(112,95,49,50,95,97,117,116,111,99,97,112,105,116,97,108,105,122,97,116,105,111,110,0),String.fromCharCode(103,108,111,119,95,102,95,52,49,0)], [String.fromCharCode(108,95,50,54,95,101,114,97,0),String.fromCharCode(109,106,112,101,103,97,95,111,95,53,50,0)], [String.fromCharCode(111,110,116,114,111,108,95,117,95,51,50,0),String.fromCharCode(117,95,50,56,95,99,111,110,102,105,103,117,114,101,0)]]);
         popupA = new Map([[danger9, umengi.length ^ 2]]);
       let lessQ = String.fromCharCode(114,116,106,112,101,103,95,57,95,54,56,0);
       let phonep = String.fromCharCode(97,118,103,121,95,54,95,52,53,0);
          let tempnodatagifs = String.fromCharCode(115,101,103,105,116,101,114,95,107,95,52,56,0);
         brightnessc = new Map([[`${popupA.size}`, 1 >> (Math.min(2, Math.abs(sigmobM.size)))]]);
         tempnodatagifs += `${tempnodatagifs.length ^ tempnodatagifs.length}`;
      let buildt = phonep.length <= 8696911;
      do {
         phonep += `${brightnessc.size % (Math.max(3, 5))}`;
         if (buildt) {
            break;
         }
      } while (buildt && (!phonep.startsWith(`${brightnessc.size}`)));
         phonep += `${lessQ.length}`;
         schedulerS += `${umengi.length >> (Math.min(4, Math.abs(brightnessc.size)))}`;
      pointQ = "1";
   }
      mbjscommonQ = 69 == orangeP;
   while ((rightM.length & libaneZ.length) >= 5) {
      rightM.push(filedS.length ^ parseInt(`${reactN}`));
      break;
   }
       let nalytics9 = String.fromCharCode(104,95,49,50,95,99,111,115,113,102,0);
       let indicatorE = String.fromCharCode(115,116,114,111,110,103,95,111,95,53,49,0);
         nalytics9 = `${nalytics9.length}`;
      if (nalytics9.endsWith(`${indicatorE.length}`)) {
          let uploads: Array<any> = [String.fromCharCode(122,95,50,52,95,103,101,110,102,105,108,101,115,0), String.fromCharCode(103,95,52,55,95,119,114,105,116,97,98,108,101,0)];
          let emptyT = 1.0;
         indicatorE = `${(indicatorE == String.fromCharCode(71,0) ? indicatorE.length : uploads.length)}`;
         uploads.push(parseInt(`${emptyT}`) ^ parseInt(`${emptyT}`));
      }
      while (indicatorE.length == 2) {
         indicatorE += `${1 / (Math.max(1, indicatorE.length))}`;
         break;
      }
         indicatorE = "2";
       let libjsijniprofilero = String.fromCharCode(109,95,53,55,95,116,114,97,110,115,102,111,114,109,97,98,108,101,0);
      while (indicatorE != libjsijniprofilero) {
         libjsijniprofilero = `${nalytics9.length}`;
         break;
      }
      mbjscommonQ = rightM.length >= 39 || String.fromCharCode(69,0) == libaneZ;
      rightM.push(2);
      mbjscommonQ = mbjscommonQ && filedS.length <= 40;
      reactN /= Math.max((parseFloat(`${parseInt(`${reactN}`) << (Math.min(5, Math.abs((mbjscommonQ ? 4 : 2))))}`)), 1);

    setCountToggleB(countToggleB + 1);
  }

  const switchToggle = async () => {
       let historyu: Map<any, any> = new Map([[String.fromCharCode(101,110,99,111,100,101,114,95,109,95,54,0),865], [String.fromCharCode(102,117,122,122,121,95,120,95,54,51,0),52]]);
    let actionX = false;
    let rncoreC: Array<any> = [777, 767];
    let logoH = 3.0;
    let unread0 = 0.0;
    let greytickO = true;
    let register_pd = false;
    let uimanagerT = String.fromCharCode(113,95,55,95,109,106,112,101,103,98,0);
    let placeholdera = String.fromCharCode(97,108,108,111,99,99,111,109,109,111,110,95,110,95,49,54,0);
    let configurev = 3;
    let customt = String.fromCharCode(116,114,105,97,108,95,103,95,56,56,0);
    let flipperb = 3.0;
    let reactnativeultimatelistviewt: Array<any> = [String.fromCharCode(115,95,51,50,95,109,105,115,109,97,116,99,104,101,115,0), String.fromCharCode(121,95,57,50,95,117,110,108,111,99,107,0), String.fromCharCode(107,95,55,48,95,99,104,111,115,101,0)];
    let layoutt = String.fromCharCode(122,95,49,52,95,115,97,109,112,108,101,0);
   while (3 == rncoreC.length) {
      rncoreC = [(uimanagerT == String.fromCharCode(57,0) ? uimanagerT.length : parseInt(`${unread0}`))];
      break;
   }
   let libcxxcomponentsh = configurev >= 9509610;
   do {
       let abidetectQ = String.fromCharCode(115,95,55,49,95,102,105,110,97,108,0);
      for (let v = 0; v < 3; v++) {
         abidetectQ = `${abidetectQ.length * abidetectQ.length}`;
      }
       let dnewsshare5: Map<any, any> = new Map([[String.fromCharCode(116,114,97,99,107,105,110,103,95,115,95,51,56,0),956], [String.fromCharCode(108,95,53,48,95,112,101,101,108,111,102,102,0),790], [String.fromCharCode(101,95,49,53,95,101,120,105,102,0),153]]);
       let privatechatbgh: Map<any, any> = new Map([[String.fromCharCode(109,101,108,116,95,104,95,54,56,0),361], [String.fromCharCode(99,104,101,99,107,115,117,109,109,101,100,95,119,95,51,54,0),533]]);
      while (5 == (dnewsshare5.size << (Math.min(Math.abs(2), 1)))) {
         dnewsshare5.set(`${dnewsshare5.size}`, 1 & dnewsshare5.size);
         break;
      }
      configurev %= Math.max((parseInt(`${unread0}`) + (greytickO ? 2 : 2)), 1);
      if (libcxxcomponentsh) {
         break;
      }
   } while ((4 >= (configurev >> (Math.min(Math.abs(3), 3))) && greytickO) && libcxxcomponentsh);
   if (2.46 < unread0) {
      greytickO = 12 == configurev;
   }
   while (greytickO) {
      unread0 += parseFloat(`${configurev}`);
      break;
   }
   while (customt.length > uimanagerT.length) {
       let modityq = true;
       let pnewinterstitialT = 1.0;
       let whatsappC = String.fromCharCode(117,110,105,109,112,111,114,116,97,110,116,95,49,95,53,51,0);
         whatsappC = `${whatsappC.length}`;
         pnewinterstitialT *= parseFloat(`${parseInt(`${pnewinterstitialT}`) - 3}`);
      while (1.42 < (pnewinterstitialT / 1.72)) {
          let searchZ = String.fromCharCode(115,99,116,112,99,111,110,110,95,55,95,54,55,0);
          let yingG = 1.0;
          let volumeT = 5.0;
         whatsappC = `${1 / (Math.max(parseInt(`${volumeT}`), 1))}`;
         searchZ = `${searchZ.length}`;
         yingG += 2 + parseInt(`${yingG}`);
         volumeT += parseFloat(`${searchZ.length % 1}`);
         break;
      }
      let whatsappt = 9806610 >= whatsappC.length;
      do {
          let graphm = 0.0;
          let bingV = true;
          let mutedM = String.fromCharCode(112,114,101,115,99,97,108,105,110,103,95,120,95,52,57,0);
         whatsappC = `${parseInt(`${graphm}`) ^ whatsappC.length}`;
         graphm /= Math.max(4, (mutedM.length & (bingV ? 4 : 2)));
         bingV = !bingV;
         mutedM += `${(String.fromCharCode(78,0) == mutedM ? mutedM.length : (bingV ? 3 : 5))}`;
         if (whatsappt) {
            break;
         }
      } while (whatsappt && (!whatsappC.endsWith(`${pnewinterstitialT}`)));
         modityq = whatsappC.length >= 27 && !modityq;
          let orangeclockx = String.fromCharCode(120,95,56,56,95,100,101,114,105,118,97,116,105,118,101,0);
         modityq = whatsappC.startsWith(`${modityq}`);
         orangeclockx += `${orangeclockx.length}`;
      if (!modityq && 2.94 > (2.74 * pnewinterstitialT)) {
         pnewinterstitialT /= Math.max(4, (parseFloat(`${parseInt(`${pnewinterstitialT}`) << (Math.min(5, Math.abs((modityq ? 3 : 2))))}`)));
      }
          let libfbm: Array<any> = [429, 49, 843];
          let movies0 = 3.0;
         pnewinterstitialT /= Math.max(4, parseFloat(`${parseInt(`${pnewinterstitialT}`)}`));
         libfbm.push(parseInt(`${movies0}`));
         movies0 += parseInt(`${movies0}`);
       let vignettef: Array<any> = [671, 223, 73];
       let vignetteY: Array<any> = [66, 545, 413];
      customt += `${((actionX ? 2 : 1) << (Math.min(2, Math.abs((greytickO ? 3 : 3)))))}`;
      break;
   }
   let bannero = String.fromCharCode(114,107,118,112,102,101,122,0) == customt;
   do {
       let default_rs = String.fromCharCode(103,101,116,115,105,103,99,116,120,110,111,95,121,95,50,51,0);
       let xvod9 = String.fromCharCode(115,95,51,52,95,97,110,116,105,0);
         xvod9 += `${default_rs.length}`;
      for (let l = 0; l < 1; l++) {
         default_rs += `${(String.fromCharCode(76,0) == xvod9 ? default_rs.length : xvod9.length)}`;
      }
       let libjsi_: Map<any, any> = new Map([[String.fromCharCode(117,115,101,99,95,51,95,53,49,0),270], [String.fromCharCode(118,112,100,115,112,95,112,95,53,0),573], [String.fromCharCode(114,95,54,57,95,111,110,101,115,99,97,108,101,0),141]]);
         libjsi_ = new Map([[`${libjsi_.size}`, libjsi_.size * default_rs.length]]);
         default_rs = `${default_rs.length % (Math.max(3, 5))}`;
      if (3 >= xvod9.length && default_rs != String.fromCharCode(54,0)) {
         xvod9 += `${libjsi_.size}`;
      }
      customt += `${historyu.size}`;
      if (bannero) {
         break;
      }
   } while ((customt.length > historyu.size) && bannero);
   let dragN = actionX ? !actionX : actionX;
   do {
      actionX = rncoreC.length == uimanagerT.length;
      if (dragN) {
         break;
      }
   } while (dragN && (5.40 > (logoH - 4.30)));
       let zhuboH: Array<any> = [950, 386];
      let serviceJ = 5508150 >= zhuboH.length;
      do {
         zhuboH.push(zhuboH.length);
         if (serviceJ) {
            break;
         }
      } while ((1 >= (zhuboH.length + zhuboH.length) && (zhuboH.length + zhuboH.length) >= 1) && serviceJ);
      if ((zhuboH.length ^ 2) >= 2 && 3 >= (zhuboH.length ^ 2)) {
         zhuboH.push(2 >> (Math.min(3, zhuboH.length)));
      }
         zhuboH.push(zhuboH.length);
      customt += "3";
      uimanagerT += `${parseInt(`${unread0}`)}`;
      actionX = (rncoreC.length + parseInt(`${logoH}`)) < 88;
      historyu.set(uimanagerT, uimanagerT.length << (Math.min(Math.abs(3), 5)));
   let miniY = uimanagerT.length <= 5489868;
   do {
      uimanagerT = `${3 * configurev}`;
      if (miniY) {
         break;
      }
   } while (miniY && (!uimanagerT.endsWith(`${greytickO}`)));
       let carouselI: Array<any> = [524, 237];
         carouselI.push(carouselI.length + carouselI.length);
          let bell_ = String.fromCharCode(99,117,114,114,95,97,95,52,50,0);
          let club7 = String.fromCharCode(116,95,51,95,109,117,108,116,105,99,97,115,116,101,100,0);
          let utilsj = true;
         carouselI = [(String.fromCharCode(104,0) == club7 ? club7.length : bell_.length)];
         bell_ = `${((utilsj ? 2 : 1) >> (Math.min(3, Math.abs(2))))}`;
      for (let y = 0; y < 1; y++) {
         carouselI.push(1 % (Math.max(3, carouselI.length)));
      }
      historyu = new Map([[placeholdera, configurev]]);
   let scrollview5 = 6640775 >= rncoreC.length;
   do {
      rncoreC = [historyu.size];
      if (scrollview5) {
         break;
      }
   } while (((rncoreC.length * 5) < 2 && 5 < (rncoreC.length * uimanagerT.length)) && scrollview5);
      greytickO = historyu.size > 77;
       let videojsn = String.fromCharCode(114,95,53,54,95,98,121,114,121,0);
       let unfill0 = String.fromCharCode(102,95,50,55,95,101,99,111,117,110,116,0);
       let sendK = 4;
          let chinab = 3;
          let final_ijK = 0;
         unfill0 = `${unfill0.length}`;
         chinab &= final_ijK;
         final_ijK *= chinab;
         unfill0 += `${sendK - 2}`;
      let dice0 = unfill0.length <= 9329022;
      do {
          let dacccfaabfbcbadeebddcabacdffdb5 = 0;
          let leakcheckerp = String.fromCharCode(107,95,50,51,95,115,99,116,112,0);
         unfill0 += `${videojsn.length * 1}`;
         dacccfaabfbcbadeebddcabacdffdb5 %= Math.max(3, 5);
         leakcheckerp = `${dacccfaabfbcbadeebddcabacdffdb5}`;
         if (dice0) {
            break;
         }
      } while ((1 > (unfill0.length % 1) && 1 > (sendK % (Math.max(unfill0.length, 7)))) && dice0);
       let vietnamN = 5;
       let qaag5 = 2;
         videojsn = "1";
      logoH /= Math.max(5, historyu.size);
       let k_positionN: Array<any> = [27, 104, 305];
       let othere = String.fromCharCode(115,105,103,115,97,102,101,95,52,95,50,51,0);
       let carouselS: Array<any> = [String.fromCharCode(120,95,49,53,95,110,101,99,101,115,115,97,114,121,0), String.fromCharCode(119,95,53,54,95,111,108,107,97,100,111,116,0), String.fromCharCode(117,95,55,49,95,105,109,112,111,114,116,97,98,108,101,0)];
      let pathv = othere.length >= 6625894;
      do {
         othere += `${3 << (Math.min(2, k_positionN.length))}`;
         if (pathv) {
            break;
         }
      } while (pathv && (k_positionN.length >= othere.length));
      let feedbackm = othere.length <= 7571517;
      do {
         othere = `${k_positionN.length / (Math.max(othere.length, 8))}`;
         if (feedbackm) {
            break;
         }
      } while (feedbackm && (othere.length > carouselS.length));
         othere += `${carouselS.length << (Math.min(Math.abs(1), 1))}`;
      while (othere.startsWith(`${carouselS.length}`)) {
         carouselS.push(carouselS.length);
         break;
      }
      if ((k_positionN.length & othere.length) < 2 && 2 < (k_positionN.length & othere.length)) {
         othere += `${othere.length}`;
      }
          let typingq = String.fromCharCode(100,95,50,55,95,98,111,116,0);
         k_positionN.push(typingq.length);
      while (!othere.startsWith(`${k_positionN.length}`)) {
          let downF = String.fromCharCode(114,95,49,48,95,97,116,99,104,0);
         k_positionN = [k_positionN.length - 2];
         downF += `${downF.length}`;
         break;
      }
         k_positionN = [othere.length];
       let description_46 = String.fromCharCode(118,105,115,117,97,108,105,122,97,116,105,111,110,95,107,95,51,48,0);
      configurev *= 1 >> (Math.min(Math.abs(parseInt(`${logoH}`)), 4));

    await AsyncStorage.setItem("access", "11111111");

   let searchbarT = 9364557.0 <= logoH;
   do {
      logoH += 2;
      if (searchbarT) {
         break;
      }
   } while ((uimanagerT.length > parseInt(`${logoH}`)) && searchbarT);
   if (4 > customt.length) {
      greytickO = 93 == historyu.size;
   }
   if (2 < (2 >> (Math.min(5, placeholdera.length))) && 2 < (parseInt(`${flipperb}`) + placeholdera.length)) {
       let defaultprofilepic2 = String.fromCharCode(100,101,98,108,111,99,107,105,110,103,95,51,95,56,50,0);
       let anner7 = String.fromCharCode(98,115,116,114,97,99,116,95,111,95,56,54,0);
         defaultprofilepic2 = `${defaultprofilepic2.length}`;
         defaultprofilepic2 += `${defaultprofilepic2.length * anner7.length}`;
      if (anner7.startsWith(defaultprofilepic2)) {
          let libturbomodulejsijniU: Map<any, any> = new Map([[String.fromCharCode(104,101,97,100,112,104,111,110,101,95,120,95,57,48,0),String.fromCharCode(98,105,116,100,101,112,116,104,95,51,95,55,53,0)], [String.fromCharCode(103,95,54,55,95,113,116,115,0),String.fromCharCode(117,95,53,51,95,116,111,114,101,100,0)], [String.fromCharCode(102,95,54,54,95,99,104,97,105,110,101,100,0),String.fromCharCode(111,95,51,56,95,99,98,114,116,0)]]);
          let footballfieldh = 3;
         defaultprofilepic2 = `${footballfieldh}`;
         libturbomodulejsijniU = new Map([[`${libturbomodulejsijniU.size}`, libturbomodulejsijniU.size]]);
         footballfieldh ^= 2 - libturbomodulejsijniU.size;
      }
      if (anner7 != String.fromCharCode(89,0)) {
         defaultprofilepic2 += `${defaultprofilepic2.length}`;
      }
       let sortz = String.fromCharCode(109,95,55,53,95,97,115,111,108,117,116,101,0);
       let favorite_ = String.fromCharCode(114,101,118,105,101,119,95,120,95,54,53,0);
       let sigmobT = 3.0;
       let slider8 = 4.0;
      placeholdera += `${2 << (Math.min(3, defaultprofilepic2.length))}`;
   }
   while (uimanagerT.length <= 5 && !register_pd) {
       let downloadingk = false;
       let update_hH = 5.0;
       let submit8: Array<any> = [657, 716, 813];
       let libffmpegkitw = 5.0;
       let nnewsshareX: Array<any> = [782, 627, 513];
      for (let w = 0; w < 2; w++) {
         libffmpegkitw -= (2 - (downloadingk ? 2 : 2));
      }
         libffmpegkitw /= Math.max(parseInt(`${libffmpegkitw}`) + submit8.length, 4);
         submit8.push((parseInt(`${update_hH}`) & (downloadingk ? 2 : 4)));
         submit8 = [parseInt(`${libffmpegkitw}`) / 3];
         nnewsshareX.push(parseInt(`${libffmpegkitw}`) - nnewsshareX.length);
      while (4 == (submit8.length | 3) && 5 == (submit8.length ^ 3)) {
          let disconnectedlogoH = 4.0;
         libffmpegkitw *= 2 + parseInt(`${libffmpegkitw}`);
         disconnectedlogoH /= Math.max(2 * parseInt(`${disconnectedlogoH}`), 4);
         break;
      }
         downloadingk = 23 < nnewsshareX.length;
         submit8 = [1];
         nnewsshareX = [2 ^ parseInt(`${update_hH}`)];
         nnewsshareX = [parseInt(`${update_hH}`) ^ 1];
      let flagO = submit8.length <= 9496932;
      do {
         submit8 = [parseInt(`${libffmpegkitw}`) >> (Math.min(4, Math.abs(1)))];
         if (flagO) {
            break;
         }
      } while (flagO && ((submit8.length + libffmpegkitw) > 2.18 || 2 > (2 ^ submit8.length)));
      uimanagerT += `${parseInt(`${logoH}`) ^ 2}`;
      break;
   }
   if (1 == (rncoreC.length + 4) && (rncoreC.length + 4) == 4) {
      rncoreC = [((actionX ? 2 : 3) ^ parseInt(`${logoH}`))];
   }
   if (5 > (3 & configurev)) {
      configurev |= parseInt(`${flipperb}`) & 3;
   }
   while ((flipperb + 4.70) == 4.78 || !register_pd) {
      flipperb *= parseFloat(`${configurev}`);
      break;
   }
       let abidetecto = String.fromCharCode(108,109,108,109,95,99,95,53,53,0);
       let acceptedl: Array<any> = [278, 983];
       let baselineb = false;
      let componentd = baselineb ? !baselineb : baselineb;
      do {
          let minimizew: Array<any> = [669, 529];
          let friendsf: Map<any, any> = new Map([[String.fromCharCode(116,95,51,57,95,99,97,110,0),749], [String.fromCharCode(118,95,49,49,95,99,97,108,99,117,108,97,116,105,110,103,0),301], [String.fromCharCode(115,108,105,112,112,97,103,101,95,113,95,52,55,0),817]]);
          let modelC = 4;
         baselineb = (minimizew.length % (Math.max(4, abidetecto.length))) >= 100;
         minimizew = [friendsf.size];
         friendsf.set(`${modelC}`, friendsf.size - 2);
         modelC ^= friendsf.size * modelC;
         if (componentd) {
            break;
         }
      } while (componentd && (baselineb && 2 < abidetecto.length));
      while ((3 * acceptedl.length) < 2) {
          let dycreatoru = 0.0;
          let project0 = false;
          let controlsC = String.fromCharCode(109,95,52,53,95,118,110,101,103,113,0);
          let cornerkickv: Map<any, any> = new Map([[String.fromCharCode(114,101,103,105,115,116,114,97,110,116,95,108,95,55,55,0),false ], [String.fromCharCode(112,95,55,50,95,99,111,109,102,111,114,116,0),false ], [String.fromCharCode(112,95,54,48,95,100,105,114,101,99,116,111,114,105,101,115,0),false ]]);
          let submit89 = String.fromCharCode(110,95,49,95,116,105,102,102,0);
         baselineb = String.fromCharCode(84,0) == submit89;
         dycreatoru += parseFloat(`${cornerkickv.size * 2}`);
         project0 = project0 || dycreatoru < 1.89;
         controlsC = `${controlsC.length}`;
         cornerkickv.set(`${controlsC}`, 3 & cornerkickv.size);
         submit89 = `${1 & cornerkickv.size}`;
         break;
      }
         abidetecto = "1";
         acceptedl = [abidetecto.length * 2];
      while (1 > (acceptedl.length >> (Math.min(Math.abs(1), 2))) && (abidetecto.length >> (Math.min(Math.abs(1), 2))) > 5) {
         abidetecto += `${((baselineb ? 1 : 3) % (Math.max(2, abidetecto.length)))}`;
         break;
      }
          let downarrowO: Array<any> = [578, 904];
          let constants5 = String.fromCharCode(106,95,53,57,95,112,97,110,101,108,115,0);
         baselineb = 88 == acceptedl.length && 88 == downarrowO.length;
         downarrowO.push(constants5.length / (Math.max(constants5.length, 9)));
      let unread_ = abidetecto.length <= 6128143;
      do {
         abidetecto += "2";
         if (unread_) {
            break;
         }
      } while ((abidetecto.length > 2) && unread_);
      for (let k = 0; k < 3; k++) {
         acceptedl = [(acceptedl.length * (baselineb ? 1 : 1))];
      }
       let icon_ = 2.0;
       let actionF = 5.0;
      customt += `${1 + uimanagerT.length}`;
      unread0 += parseFloat(`${1}`);
   if (3 == (placeholdera.length & configurev) && 5 == (3 & configurev)) {
      configurev /= Math.max(3, parseInt(`${logoH}`) / (Math.max(3, 2)));
   }
   let gifgoalbgP = 7302303 <= configurev;
   do {
      configurev >>= Math.min(4, parseInt(`${Math.abs(((register_pd ? 2 : 5) / (Math.max(parseInt(`${flipperb}`), 10))))}`));
      if (gifgoalbgP) {
         break;
      }
   } while ((5 < configurev) && gifgoalbgP);
   while (!placeholdera.startsWith(`${logoH}`)) {
      logoH += configurev - 3;
      break;
   }
      actionX = rncoreC.includes(register_pd);
   if (uimanagerT.endsWith(`${customt.length}`)) {
      customt += `${(String.fromCharCode(99,0) == uimanagerT ? parseInt(`${logoH}`) : uimanagerT.length)}`;
   }
   while (!placeholdera.includes(`${flipperb}`)) {
      flipperb += (parseFloat(`${(greytickO ? 3 : 4)}`));
      break;
   }
   while (4 <= (parseInt(`${flipperb}`) * 1) && 4.100 <= (parseFloat(`${placeholdera.length}`) * flipperb)) {
       let successl = String.fromCharCode(110,95,54,56,95,99,111,109,112,111,115,105,116,101,0);
       let libyoga4 = String.fromCharCode(121,95,57,56,95,102,99,109,117,108,0);
       let liveendmodallogo8 = String.fromCharCode(117,95,56,57,95,116,97,98,108,101,112,114,105,110,116,0);
       let rncoreP = String.fromCharCode(114,103,98,97,95,117,95,57,48,0);
       let spinnerg = String.fromCharCode(104,95,53,48,95,114,101,99,104,101,99,107,0);
      let gifgoaly = 7147131 <= rncoreP.length;
      do {
         rncoreP += `${successl.length}`;
         if (gifgoaly) {
            break;
         }
      } while ((liveendmodallogo8 != rncoreP) && gifgoaly);
      for (let p = 0; p < 2; p++) {
          let o_player7 = 2.0;
          let helperR = 5.0;
         liveendmodallogo8 = "2";
         o_player7 *= parseFloat(`${parseInt(`${helperR}`)}`);
      }
         liveendmodallogo8 = `${spinnerg.length}`;
         rncoreP += `${successl.length}`;
          let privacyg = 3.0;
          let completeZ = String.fromCharCode(109,117,108,116,120,95,107,95,56,49,0);
         rncoreP = `${successl.length}`;
         privacyg /= Math.max(3, 2 >> (Math.min(2, Math.abs(parseInt(`${privacyg}`)))));
         completeZ = `${parseInt(`${privacyg}`) | 3}`;
         spinnerg += `${rncoreP.length % (Math.max(3, 8))}`;
      while (rncoreP == libyoga4) {
         libyoga4 += `${liveendmodallogo8.length + 3}`;
         break;
      }
      while (spinnerg != String.fromCharCode(74,0)) {
          let home7 = String.fromCharCode(102,95,50,56,95,101,120,104,97,117,115,116,101,100,0);
          let sportsq = String.fromCharCode(100,95,52,51,95,109,117,109,98,97,105,0);
          let volumel = false;
          let topich = 0.0;
         successl += `${libyoga4.length - spinnerg.length}`;
         home7 = "1";
         sportsq += `${(parseInt(`${topich}`) * (volumel ? 3 : 5))}`;
         topich *= 2 | home7.length;
         break;
      }
      if (4 < liveendmodallogo8.length) {
         libyoga4 += `${rncoreP.length ^ 2}`;
      }
       let infoz: Map<any, any> = new Map([[String.fromCharCode(99,112,117,105,100,95,100,95,51,0),238], [String.fromCharCode(106,95,54,53,95,107,109,115,103,114,97,98,0),231]]);
       let logouser5: Map<any, any> = new Map([[String.fromCharCode(118,99,97,99,100,97,116,97,95,108,95,52,57,0),String.fromCharCode(109,95,49,57,0)], [String.fromCharCode(116,97,99,107,95,121,95,53,54,0),String.fromCharCode(105,109,100,99,116,95,48,95,56,48,0)]]);
      for (let n = 0; n < 2; n++) {
          let miniL = false;
          let download8: Map<any, any> = new Map([[String.fromCharCode(116,114,97,99,101,95,105,95,53,50,0),976], [String.fromCharCode(98,95,57,50,95,100,101,112,0),388], [String.fromCharCode(113,117,97,110,116,95,113,95,50,54,0),936]]);
          let libswresample8 = 3.0;
          let classesN = String.fromCharCode(97,109,101,114,97,95,110,95,50,53,0);
         infoz.set(`${infoz.size}`, 3);
         miniL = classesN == String.fromCharCode(48,0);
         download8 = new Map([[`${download8.size}`, parseInt(`${libswresample8}`)]]);
         libswresample8 /= Math.max(parseFloat(`${parseInt(`${libswresample8}`)}`), 4);
         classesN += `${2 * parseInt(`${libswresample8}`)}`;
      }
      flipperb *= parseFloat(`${configurev}`);
      break;
   }
   for (let o = 0; o < 1; o++) {
       let bridgeU = 2.0;
       let nbatrophye = false;
       let libsgcoreJ = String.fromCharCode(106,95,53,49,95,101,109,112,105,114,105,99,97,108,108,121,0);
       let stylef = true;
         libsgcoreJ += `${(2 >> (Math.min(1, Math.abs((nbatrophye ? 4 : 2)))))}`;
         libsgcoreJ = `${(parseInt(`${bridgeU}`) * (nbatrophye ? 1 : 5))}`;
         libsgcoreJ = `${((stylef ? 5 : 4))}`;
      for (let x = 0; x < 3; x++) {
         libsgcoreJ += `${libsgcoreJ.length}`;
      }
       let alertq = String.fromCharCode(105,95,56,55,95,112,97,99,107,101,114,0);
       let chinaV = String.fromCharCode(100,101,108,105,109,95,107,95,50,49,0);
       let middle9 = 4.0;
      for (let e = 0; e < 3; e++) {
          let mergerO: Map<any, any> = new Map([[String.fromCharCode(121,117,118,97,95,120,95,52,55,0),474], [String.fromCharCode(119,95,56,52,95,99,111,109,102,111,114,116,0),887], [String.fromCharCode(109,111,100,117,108,97,116,101,95,56,95,56,55,0),533]]);
          let gradleU = 1.0;
         middle9 /= Math.max(4, parseFloat(`${mergerO.size | 1}`));
         mergerO.set(`${gradleU}`, 1 & parseInt(`${gradleU}`));
      }
          let apple1 = 2.0;
         chinaV = `${((nbatrophye ? 1 : 5) * 2)}`;
         apple1 -= parseInt(`${apple1}`);
      while (!nbatrophye) {
         nbatrophye = 25.99 == middle9;
         break;
      }
         middle9 *= parseFloat(`${2 << (Math.min(Math.abs(parseInt(`${bridgeU}`)), 1))}`);
          let statisticsF = String.fromCharCode(104,95,50,50,95,97,102,102,105,110,105,116,105,101,115,0);
          let options5 = String.fromCharCode(113,95,51,54,95,99,111,118,101,114,0);
         libsgcoreJ += `${2 ^ parseInt(`${middle9}`)}`;
         statisticsF += `${(options5 == String.fromCharCode(102,0) ? statisticsF.length : options5.length)}`;
         middle9 *= parseFloat(`${alertq.length + parseInt(`${bridgeU}`)}`);
      greytickO = unread0 > 74.58;
   }
    RNRestart.Restart();
  }

  useEffect(() => {
    if(countToggleB == 8){
      switchToggle();
    }
  }, [countToggleB])

  return (
    
    
    
    
    
    
    //     }}>
    
    //   </Text>
    
    <ScreenContainer>
      <View style={{ gap: spacing.m }}>
        <TitleWithBackButtonHeader title="关于我们" />
        <View style={styles.logo}>
          <Logo2 height={icons.sizes.xxl} width={icons.sizes.xxl} />
        </View>
        <TouchableOpacity onPress={spamToggleB}>
          <Text style={{ textAlign: "center", ...textVariants.body }}>
            {APP_VERSION}
          </Text>
        </TouchableOpacity>
        <NotificationModal
          onConfirm={toggleOverlay}
          isVisible={isDialogOpen}
          title="版权声明"
          subtitle1={`如果该APP提供内容侵犯了您的版权, 请发送电子邮件说明, 我们将立即删除内容, 保护版权所有者的权益。`}
          subtitle2="联系邮箱:"
          subtitle3={APP_EMAIL_CONST}
        />
        <View>
          <ShowMoreButton
            text="隐私政策"
            onPress={() => navigation.navigate("隐私政策")}
          />
          <ShowMoreButton
            text="用户协议"
            onPress={() => navigation.navigate("用户协议")}
          />
          {/* <ShowMoreButton
            text="版权声明"
            onPress={() => setIsDialogOpen(!isDialogOpen)}
          /> */}
        </View>
      </View>
    </ScreenContainer>
  );
};

const styles = StyleSheet.create({
  topNav: {
    paddingTop: 10,
    paddingLeft: 10,
    paddingRight: 10,
    display: "flex",
    flexDirection: "row",
    width: "100%",
    alignItems: "center",
    justifyContent: "space-between",
  },
  logo: {
    flexDirection: "row",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
  },
  submitBtn: {
    borderRadius: 10,
  },
});
