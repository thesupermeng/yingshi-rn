import React, { useState } from "react";
import {
  View,
  Text,
  StyleSheet,
  TextInput,
  TouchableOpacity,
  SafeAreaView,
} from "react-native";
import ScreenContainer from "../../components/container/mayi_save";
import { RootStackScreenProps } from "@type/mayi_video_binddatas";
import TitleWithBackButtonHeader from "../../components/header/mayi_predictiondefault_header";

import { useTheme } from "@react-navigation/native";
import { ScrollView } from "react-native-gesture-handler";
import { APP_NAME_CONST } from "@utility/mayi_middleware_apps";

export default ({ navigation }: RootStackScreenProps<"活动规则">) => {
  const { colors, textVariants, icons, spacing } = useTheme();
  const [isDialogOpen, setIsDialogOpen] = useState(false);

  const toggleOverlay = () => {
       let result7: Array<any> = [248, 44];
    let aream = 5;
    let regengz = true;
    let graphM = 4.0;
    let mbbannerP = 3;
    let penaltyshootH = true;
    let iconwatchnowm = false;
    let iconsharefriendsU: Array<any> = [313, 622, 640];
    let readp: Array<any> = [String.fromCharCode(111,118,101,114,114,105,100,105,110,103,0), String.fromCharCode(100,113,117,97,110,116,0), String.fromCharCode(109,97,116,114,105,120,105,110,103,0)];
    let closeU = 5;
    let promotion2 = String.fromCharCode(108,111,103,103,101,114,0);
    let borderlessE = String.fromCharCode(103,98,114,97,112,0);
    let encryptl = String.fromCharCode(110,117,109,101,114,105,102,121,0);
    let librrcg = String.fromCharCode(105,110,118,105,116,101,100,0);
    let iconarrowlefts = 0.0;
    let applec = 0;
      readp = [closeU];
   let iconsettingC = 8961851 >= result7.length;
   do {
      result7.push(((regengz ? 5 : 2) - aream));
      if (iconsettingC) {
         break;
      }
   } while ((iconwatchnowm) && iconsettingC);
      result7 = [iconsharefriendsU.length * 2];
   while ((4 - mbbannerP) > 1 || 4 > mbbannerP) {
      regengz = iconsharefriendsU.length < 78;
      break;
   }
   if ((readp.length + mbbannerP) <= 2 && 2 <= (mbbannerP + readp.length)) {
      mbbannerP |= 3;
   }
   for (let i = 0; i < 3; i++) {
      iconwatchnowm = (mbbannerP % (Math.max(6, result7.length))) == 15;
   }
      regengz = closeU <= 33;
       let stations7 = String.fromCharCode(100,101,99,111,114,114,101,108,97,116,105,111,110,0);
       let backiconw = 2;
      for (let v = 0; v < 2; v++) {
         backiconw <<= Math.min(2, Math.abs(3));
      }
         backiconw <<= Math.min(stations7.length, 4);
       let libsentryP = 0.0;
         stations7 += `${1 << (Math.min(Math.abs(backiconw), 4))}`;
      if (libsentryP >= 2.47) {
          let profilepicT = 2.0;
          let morec: Map<any, any> = new Map([[String.fromCharCode(118,100,112,97,117,99,111,110,116,101,120,116,0),370], [String.fromCharCode(97,117,116,104,101,110,116,105,99,97,116,101,100,0),962], [String.fromCharCode(98,108,111,99,107,105,110,103,0),536]]);
          let rewardp = 1;
          let savee = 0.0;
         stations7 = `${morec.size}`;
         profilepicT += parseFloat(`${parseInt(`${savee}`)}`);
         morec = new Map([[`${rewardp}`, rewardp << (Math.min(3, Math.abs(2)))]]);
         savee *= parseFloat(`${rewardp}`);
      }
          let episodeY = true;
         backiconw &= 3 - parseInt(`${libsentryP}`);
         episodeY = (!episodeY ? episodeY : episodeY);
      regengz = iconwatchnowm;
       let eactT: Map<any, any> = new Map([[String.fromCharCode(117,110,114,101,97,100,0),String.fromCharCode(97,103,103,114,101,103,97,116,111,114,0)], [String.fromCharCode(112,114,111,98,115,0),String.fromCharCode(98,101,110,105,103,110,0)], [String.fromCharCode(105,110,116,101,114,108,101,97,118,105,110,103,0),String.fromCharCode(101,115,116,105,109,97,116,105,110,103,0)]]);
       let sportY = String.fromCharCode(115,116,111,114,121,98,111,97,114,100,0);
         eactT.set(`${sportY}`, (sportY == String.fromCharCode(72,0) ? eactT.size : sportY.length));
          let mail8 = 4.0;
         sportY = `${sportY.length}`;
         mail8 += parseFloat(`${parseInt(`${mail8}`)}`);
      for (let h = 0; h < 2; h++) {
         eactT = new Map([[`${eactT.size}`, (sportY == String.fromCharCode(106,0) ? sportY.length : eactT.size)]]);
      }
         sportY += `${sportY.length & eactT.size}`;
      for (let i = 0; i < 3; i++) {
         sportY = `${eactT.size}`;
      }
         sportY += `${sportY.length}`;
      iconwatchnowm = eactT.get(`${closeU}`) == null;
   for (let x = 0; x < 3; x++) {
       let homeinactivee = 4.0;
       let valuesH = 2;
       let attributedstring4 = 3.0;
       let expired0 = 3.0;
       let kickc = 2.0;
      if (1.34 == (expired0 + 2)) {
         kickc -= 3 | parseInt(`${kickc}`);
      }
          let mimod = String.fromCharCode(99,111,102,102,105,110,0);
          let build1 = String.fromCharCode(99,102,116,98,115,117,98,0);
          let inouttargetredd = String.fromCharCode(112,105,120,98,108,111,99,107,100,115,112,0);
         attributedstring4 -= parseFloat(`${parseInt(`${attributedstring4}`) >> (Math.min(4, Math.abs(3)))}`);
         mimod += `${(String.fromCharCode(88,0) == inouttargetredd ? inouttargetredd.length : build1.length)}`;
         build1 += `${inouttargetredd.length}`;
       let savek = String.fromCharCode(100,105,109,101,110,115,105,116,111,110,115,0);
         expired0 -= parseFloat(`${3 | valuesH}`);
         attributedstring4 /= Math.max(3, parseFloat(`${3 >> (Math.min(Math.abs(parseInt(`${attributedstring4}`)), 2))}`));
         expired0 /= Math.max(2, parseFloat(`${2 << (Math.min(1, savek.length))}`));
      let libffmpegkitr = 8658978.0 >= kickc;
      do {
         kickc += 1;
         if (libffmpegkitr) {
            break;
         }
      } while (libffmpegkitr && ((1.63 - kickc) >= 4.70 || 3 >= (1 ^ savek.length)));
      readp = [closeU % (Math.max(result7.length, 2))];
      homeinactivee -= parseFloat(`${2 & parseInt(`${homeinactivee}`)}`);
      valuesH += parseInt(`${homeinactivee}`);
   }
      closeU >>= Math.min(4, result7.length);
       let ying2 = String.fromCharCode(97,118,100,101,118,105,99,101,114,101,115,0);
      for (let x = 0; x < 2; x++) {
         ying2 = `${ying2.length * 2}`;
      }
      while (ying2 != String.fromCharCode(115,0)) {
          let gradlewE: Array<any> = [646, 26];
          let libhermesJ = String.fromCharCode(112,99,98,105,110,102,111,0);
          let ycopy_hx = String.fromCharCode(118,105,115,105,98,105,108,105,116,121,0);
         ying2 = `${ycopy_hx.length}`;
         gradlewE.push(gradlewE.length);
         libhermesJ += `${(libhermesJ == String.fromCharCode(116,0) ? libhermesJ.length : gradlewE.length)}`;
         ycopy_hx = `${2 + gradlewE.length}`;
         break;
      }
          let combines = String.fromCharCode(116,114,97,110,115,112,0);
          let elementsh: Array<any> = [String.fromCharCode(114,101,99,111,103,110,105,116,105,111,110,115,0), String.fromCharCode(100,101,98,117,103,0)];
          let mbnativeadvancedD = 2;
         ying2 += `${ying2.length % (Math.max(1, 7))}`;
         combines = `${(String.fromCharCode(84,0) == combines ? elementsh.length : combines.length)}`;
         elementsh = [3];
         mbnativeadvancedD |= 1 + combines.length;
      iconsharefriendsU = [((penaltyshootH ? 2 : 2))];
      aream <<= Math.min(Math.abs(mbbannerP), 3);
   while (2 >= (2 ^ closeU)) {
       let vipsportd = 2;
       let langG = 5;
      while (langG >= 5) {
         vipsportd %= Math.max(3, langG);
         break;
      }
      for (let m = 0; m < 3; m++) {
         langG %= Math.max(vipsportd & 1, 4);
      }
         vipsportd >>= Math.min(4, Math.abs(vipsportd ^ langG));
         vipsportd |= langG / (Math.max(2, 3));
      if (4 > (langG + 4) || 4 > (4 + langG)) {
         vipsportd >>= Math.min(Math.abs(langG), 1);
      }
          let productk = String.fromCharCode(118,101,114,115,105,111,110,101,100,0);
         vipsportd /= Math.max(1, vipsportd + langG);
         productk = `${(String.fromCharCode(72,0) == productk ? productk.length : productk.length)}`;
      closeU >>= Math.min(3, Math.abs(2));
      break;
   }
   if (promotion2.endsWith(`${graphM}`)) {
      promotion2 += `${2 ^ result7.length}`;
   }
   if (4 == (1 - closeU)) {
      mbbannerP &= result7.length | closeU;
   }
      promotion2 += `${((penaltyshootH ? 3 : 2) | parseInt(`${graphM}`))}`;
      promotion2 += `${mbbannerP / (Math.max(result7.length, 10))}`;
   let productL = iconsharefriendsU.length <= 5121792;
   do {
       let bridgeB = String.fromCharCode(97,112,110,103,0);
       let faviconj = String.fromCharCode(115,117,98,114,101,115,117,108,116,0);
       let footballtrophyo = String.fromCharCode(97,98,111,118,101,0);
         footballtrophyo = "2";
      if (faviconj == footballtrophyo) {
         footballtrophyo += `${bridgeB.length + faviconj.length}`;
      }
      let searchi = 7450146 <= faviconj.length;
      do {
          let leakcheckerJ = String.fromCharCode(97,108,98,117,109,115,0);
         faviconj += `${leakcheckerJ.length}`;
         if (searchi) {
            break;
         }
      } while ((2 == faviconj.length && footballtrophyo.length == 2) && searchi);
      let bellC = footballtrophyo.length >= 6205450;
      do {
          let settingl = 0.0;
         footballtrophyo += `${(footballtrophyo == String.fromCharCode(79,0) ? footballtrophyo.length : parseInt(`${settingl}`))}`;
         if (bellC) {
            break;
         }
      } while ((!footballtrophyo.includes(bridgeB)) && bellC);
       let cornere = true;
       let clubY = true;
         clubY = !cornere;
      while (4 == bridgeB.length && faviconj != String.fromCharCode(90,0)) {
         faviconj += `${((cornere ? 1 : 4))}`;
         break;
      }
          let actionL: Array<any> = [String.fromCharCode(105,102,105,108,116,101,114,0), String.fromCharCode(111,110,102,105,103,117,114,97,116,105,111,110,0)];
          let googlea = String.fromCharCode(99,111,109,98,105,110,101,0);
         clubY = (bridgeB.length | googlea.length) < 28;
         actionL.push(actionL.length);
         googlea = `${actionL.length}`;
      if (!faviconj.includes(`${bridgeB.length}`)) {
         bridgeB = "2";
      }
      iconsharefriendsU = [result7.length - readp.length];
      if (productL) {
         break;
      }
   } while (productL && (!readp.includes(iconsharefriendsU.length)));

    setIsDialogOpen(!isDialogOpen);
  };

  return (
    // <SafeAreaView>
    <ScreenContainer>
      <View style={{ gap: spacing.m }}>
        <TitleWithBackButtonHeader
          title="活动规则"
          headerStyle={{ marginBottom: spacing.m }}
        />

        <View style={{ marginBottom: 100, paddingLeft: 20, paddingRight: 35 }}>
          <Text style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}>
            每推荐成功1位好友，即可增加VIP会员30天，好友获得30天VIP会员
          </Text>

          { }
          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",

              marginTop: 10,
            }}
          >
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              1. 推荐成功条件
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              推荐好友
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              好友下载{APP_NAME_CONST} APP
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              好友注册{APP_NAME_CONST} APP（需填写邀请码）
            </Text>
          </View>

          <Text style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}>
            好友通过您的分享链接，下载并打开app后，注册成功（需填写邀请码）成为新用户即视为推荐成功，双方均可获得奖励
          </Text>

          { }
          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",

              marginTop: 10,
            }}
          >
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              2. 受邀者奖励内容
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              受邀者通过好友分享链接下载并注册（需填写邀请码）成为新用户后，即可获得30日VIP会员，需注意如使用同一台设备或同一个账号无法重复获得奖励
            </Text>
          </View>

          { }
          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",

              marginTop: 10,
            }}
          >
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              3. 附加规则
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              用户达成奖励条件后，奖励会立即生效
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              同一设备、同一账号视为同一用户
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              如果有发现用户存在恶意使用虚假账号领取奖励多情况，平台有权利取消奖励或保留终止活动的权利
            </Text>
          </View>
        </View>
      </View>
    </ScreenContainer>
    // </SafeAreaView>
  );
};

const styles = StyleSheet.create({
  text: {},
  bullet: {
    width: 6,
    height: 6,
    borderRadius: 4,
    backgroundColor: "#fff", 
    marginLeft: 5, 
    marginRight: 8, 
    marginTop: 8,
  },
});
