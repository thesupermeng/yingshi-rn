import React, { useEffect, useState } from 'react';
import {
  View,
  Text,
  StyleSheet,
  TextInput,
  Image,
  ImageBackground,
  ScrollView,
  RefreshControl,
} from 'react-native';
import ScreenContainer from '../../components/container/ypy_fast';
import { RootStackScreenProps } from '@type/vrm_thailand';
import { useTheme } from '@react-navigation/native';
import { PSmall } from '@redux/fj_prediction_thailand';

import TitleWithBackButtonHeader from '../../components/header/cio_news_types_header';
import axios from 'axios';
import { Keyboard } from 'react-native';
import {
  API_DOMAIN,
  API_DOMAIN_TEST,
  API_DOMAIN_LOCAL,
} from '@utility/n_subs_interstitial';
import NetInfo, { NetInfoState } from '@react-native-community/netinfo';

import InviteStep from '../../components/invite/dv_header_sans';
import InviteCard from '../../components/invite/r_sound';

import InviteHeader from '../../components/invite/bf_apple_filter_header';
import FastImage from '../../components/common/gwi_with';
import LinearGradient from 'react-native-linear-gradient';
import { useAppSelector, useSelector } from '@hooks/kg_index';
import {
  updateUserAuth,
  updateUserReferral,
} from '@redux/actions/rk_filed_watch';
import { useDispatch } from 'react-redux';
import { THFirebase } from '@api';
import { HDTGesturesList } from '@redux/reducers/f_gmail';
export default ({ navigation }: RootStackScreenProps<'邀请详情'>) => {
  const { colors, textVariants, icons, spacing } = useTheme();

  const userState = useSelector<HDTGesturesList>('userReducer');
  const dispatch = useDispatch();

  const [refreshing, setRefreshing] = useState(false);
  const [inviteList, setInviteList] = useState([]);

  const handleRefresh = async () => {
       let twitterI: Array<any> = [851, 254];
    let umengb = String.fromCharCode(115,105,103,110,97,108,105,110,103,95,54,95,50,50,0);
    let circlex = 3.0;
    let untickH = true;
    let progressa = 5.0;
    let small1 = 3.0;
    let annerC: Map<any, any> = new Map([[String.fromCharCode(110,95,51,52,95,97,115,121,110,99,0),384], [String.fromCharCode(101,110,116,101,114,101,100,95,54,95,54,51,0),4], [String.fromCharCode(101,95,49,52,0),467]]);
    let o_centerB = String.fromCharCode(97,97,102,99,95,97,95,57,54,0);
    let plusa = String.fromCharCode(114,95,55,54,95,112,114,111,112,111,115,97,108,115,0);
    let sharedJ: Array<any> = [194, 827];
    let feedbackv: Array<any> = [414, 484, 892];
    let showR = 2.0;
   if (3 > plusa.length) {
      plusa += `${feedbackv.length}`;
   }
   while (annerC.size < o_centerB.length) {
      annerC.set(`${feedbackv.length}`, feedbackv.length);
      break;
   }
       let point8 = true;
          let sheetS = String.fromCharCode(100,97,114,119,105,110,95,120,95,56,53,0);
         point8 = sheetS.startsWith(`${point8}`);
       let reducerD = 2.0;
          let privacyn = String.fromCharCode(99,95,55,52,95,112,105,120,101,108,0);
         point8 = reducerD < privacyn.length;
      plusa = `${((untickH ? 1 : 5) ^ 3)}`;
   for (let n = 0; n < 2; n++) {
      plusa += "2";
   }
      twitterI = [parseInt(`${circlex}`)];
       let t_playerT = 1.0;
       let bannerK = 5.0;
          let watchn = 1;
         t_playerT -= parseInt(`${bannerK}`);
         watchn %= Math.max(1, watchn * 2);
      if (bannerK == t_playerT) {
          let descJ = true;
          let championV = 3.0;
          let eventG = 2.0;
          let d_titleP = 0;
          let forwardY = String.fromCharCode(116,119,111,115,95,103,95,55,56,0);
         bannerK += 3 << (Math.min(Math.abs(parseInt(`${bannerK}`)), 5));
         descJ = !descJ || eventG == 10.11;
         championV /= Math.max(parseFloat(`${3}`), 1);
         eventG -= 3 * d_titleP;
         d_titleP ^= forwardY.length - 1;
         forwardY = "1";
      }
      while (3.26 > (bannerK + t_playerT) && (t_playerT + bannerK) > 3.26) {
         t_playerT += parseInt(`${bannerK}`);
         break;
      }
      for (let i = 0; i < 2; i++) {
         t_playerT -= 2 & parseInt(`${bannerK}`);
      }
         t_playerT /= Math.max(parseInt(`${bannerK}`), 1);
          let otherv: Map<any, any> = new Map([[String.fromCharCode(118,105,100,101,111,95,102,95,54,57,0),true ], [String.fromCharCode(105,116,120,102,109,95,49,95,49,56,0),false ]]);
          let flyerh: Array<any> = [731, 320];
         t_playerT /= Math.max(2, parseInt(`${bannerK}`));
         otherv = new Map([[`${otherv.size}`, otherv.size]]);
         flyerh.push(3 + flyerh.length);
      sharedJ = [parseInt(`${t_playerT}`)];
   for (let s = 0; s < 1; s++) {
      annerC = new Map([[plusa, (String.fromCharCode(68,0) == plusa ? plusa.length : parseInt(`${progressa}`))]]);
   }
   while (annerC.size <= 4) {
      progressa /= Math.max(2, (parseFloat(`${(untickH ? 1 : 5) + 1}`)));
      break;
   }
      o_centerB = `${parseInt(`${small1}`)}`;
   let combinedi = 8190585.0 <= small1;
   do {
      small1 -= (parseFloat(`${parseInt(`${progressa}`) >> (Math.min(5, Math.abs((untickH ? 5 : 3))))}`));
      if (combinedi) {
         break;
      }
   } while ((Array.from(annerC.values()).includes(small1)) && combinedi);
   for (let n = 0; n < 2; n++) {
       let long_r__ = String.fromCharCode(116,97,112,101,95,104,95,57,54,0);
       let mapping4: Array<any> = [817, 838, 296];
       let paginationd = String.fromCharCode(121,95,51,54,95,100,101,98,117,103,0);
         paginationd += "2";
         mapping4.push(mapping4.length);
      for (let o = 0; o < 3; o++) {
         long_r__ += `${long_r__.length}`;
      }
      while (!paginationd.includes(`${mapping4.length}`)) {
          let downt = String.fromCharCode(103,95,51,51,95,108,101,98,110,0);
          let leagueZ: Map<any, any> = new Map([[String.fromCharCode(119,95,55,54,95,109,111,100,101,109,0),String.fromCharCode(117,95,53,54,95,116,114,117,101,0)], [String.fromCharCode(103,95,56,51,95,105,115,115,112,97,99,101,0),String.fromCharCode(110,117,108,108,115,95,120,95,54,50,0)], [String.fromCharCode(106,95,57,95,102,111,114,109,97,110,116,0),String.fromCharCode(99,111,109,112,117,116,101,95,103,95,56,55,0)]]);
          let diceH: Map<any, any> = new Map([[String.fromCharCode(115,116,111,119,95,55,95,50,51,0),String.fromCharCode(102,115,101,101,107,95,101,95,48,0)], [String.fromCharCode(104,107,100,102,95,52,95,49,51,0),String.fromCharCode(101,95,53,52,95,110,111,109,101,109,0)], [String.fromCharCode(106,95,53,48,95,114,101,99,105,112,0),String.fromCharCode(102,95,50,51,95,101,120,116,114,97,100,97,116,97,112,115,101,116,115,0)]]);
          let turnR = String.fromCharCode(115,99,104,101,109,101,95,102,95,54,54,0);
          let launchE = 4.0;
         mapping4.push(paginationd.length);
         downt += `${leagueZ.size}`;
         leagueZ = new Map([[`${launchE}`, 3]]);
         diceH = new Map([[`${diceH.size}`, 1 ^ downt.length]]);
         turnR = `${leagueZ.size + parseInt(`${launchE}`)}`;
         break;
      }
      for (let o = 0; o < 1; o++) {
         paginationd += `${mapping4.length}`;
      }
         long_r__ = `${2 << (Math.min(1, paginationd.length))}`;
      if (3 < long_r__.length) {
         paginationd = `${(long_r__ == String.fromCharCode(76,0) ? mapping4.length : long_r__.length)}`;
      }
          let policyq = false;
         mapping4.push(3);
         policyq = !policyq;
      while (mapping4.length > long_r__.length) {
         mapping4 = [mapping4.length % (Math.max(10, long_r__.length))];
         break;
      }
      plusa += `${parseInt(`${circlex}`) >> (Math.min(Math.abs(3), 1))}`;
   }
   if ((small1 - 1.3) <= 3.51 && 2.47 <= (small1 - 1.3)) {
      small1 += parseFloat(`${2 & umengb.length}`);
   }
      umengb += `${plusa.length}`;

    setRefreshing(true);

   let gemfileb = small1 >= 8918315.0;
   do {
       let homeu = String.fromCharCode(102,108,105,99,95,105,95,52,0);
       let suggestionv = String.fromCharCode(113,95,51,48,95,105,109,112,108,101,109,101,110,116,115,0);
       let moonF = true;
       let sinak = 2.0;
       let singleW = String.fromCharCode(98,105,103,116,114,101,101,95,119,95,56,57,0);
      if (5.55 < sinak) {
         sinak *= (parseFloat(`${(moonF ? 5 : 4) % (Math.max(3, suggestionv.length))}`));
      }
         sinak *= parseFloat(`${homeu.length / 1}`);
      let moonK = String.fromCharCode(121,111,103,122,104,0) == singleW;
      do {
          let vignettes = String.fromCharCode(99,114,105,116,105,99,97,108,95,97,95,54,51,0);
          let sansn = true;
          let customi = String.fromCharCode(103,95,56,57,95,114,101,97,100,102,117,108,108,0);
          let pauseD: Map<any, any> = new Map([[String.fromCharCode(104,95,57,49,95,119,101,108,115,101,110,99,100,101,109,111,0),232], [String.fromCharCode(116,95,56,48,95,116,105,109,105,110,103,115,97,102,101,0),450]]);
         singleW = `${pauseD.size & 1}`;
         vignettes = `${(String.fromCharCode(71,0) == vignettes ? vignettes.length : customi.length)}`;
         sansn = !sansn && customi.length > 90;
         pauseD.set(customi, customi.length);
         if (moonK) {
            break;
         }
      } while (moonK && ((sinak / (Math.max(4.38, 1))) == 2.39 && 5.89 == (sinak / 4.38)));
      let downloadf = moonF ? !moonF : moonF;
      do {
         moonF = 32.8 <= sinak;
         if (downloadf) {
            break;
         }
      } while ((moonF || 2 == suggestionv.length) && downloadf);
         singleW += `${2 >> (Math.min(5, singleW.length))}`;
         sinak -= parseFloat(`${3}`);
         sinak += parseFloat(`${singleW.length - 2}`);
      if (4 <= (homeu.length - parseInt(`${sinak}`))) {
          let updatesi = true;
          let googlee: Map<any, any> = new Map([[String.fromCharCode(108,117,116,114,103,98,95,117,95,49,49,0),true ], [String.fromCharCode(112,114,101,115,101,108,101,99,116,95,102,95,53,0),true ]]);
          let description_7p8 = String.fromCharCode(116,114,97,102,95,97,95,56,48,0);
          let flyerP = String.fromCharCode(120,95,57,48,95,114,101,109,111,118,97,108,115,0);
          let frame_2_r = String.fromCharCode(98,95,55,57,0);
         homeu += `${singleW.length}`;
         updatesi = 43 >= googlee.size && String.fromCharCode(121,0) == flyerP;
         googlee = new Map([[`${googlee.size}`, 2 >> (Math.min(4, flyerP.length))]]);
         description_7p8 = `${googlee.size % 3}`;
         frame_2_r = `${googlee.size << (Math.min(Math.abs(1), 3))}`;
      }
         homeu += `${homeu.length}`;
         moonF = 83.44 < sinak;
          let assistT: Array<any> = [50, 152];
          let selected3 = String.fromCharCode(105,99,99,112,95,97,95,57,56,0);
          let heartN = String.fromCharCode(105,110,102,111,114,109,97,116,105,118,101,95,121,95,50,51,0);
         sinak *= parseFloat(`${heartN.length & 3}`);
         assistT.push(selected3.length);
         selected3 += `${selected3.length}`;
         heartN += `${(selected3 == String.fromCharCode(109,0) ? assistT.length : selected3.length)}`;
       let privacyd = 1.0;
          let pointi = String.fromCharCode(99,104,97,110,103,101,104,95,108,95,54,51,0);
          let splashp: Array<any> = [620, 666];
          let stationk = 5;
         singleW = `${stationk}`;
         pointi += `${pointi.length}`;
         splashp.push(2 + pointi.length);
         stationk |= 3 & splashp.length;
         privacyd *= parseFloat(`${2 ^ parseInt(`${sinak}`)}`);
       let contextb = 5.0;
      small1 *= parseFloat(`${singleW.length | o_centerB.length}`);
      if (gemfileb) {
         break;
      }
   } while (((small1 * parseFloat(`${feedbackv.length}`)) > 5.2) && gemfileb);
   let gray6 = small1 <= 7120914.0;
   do {
       let descR = 3.0;
       let projectp = 2.0;
         projectp /= Math.max(5, parseFloat(`${parseInt(`${descR}`)}`));
      while (4.33 >= (projectp * descR) && 1.46 >= (4.33 * descR)) {
         projectp -= parseFloat(`${3 + parseInt(`${descR}`)}`);
         break;
      }
      while (3.50 == (descR / (Math.max(projectp, 3))) || 4.47 == (3.50 / (Math.max(1, projectp)))) {
         projectp -= parseFloat(`${parseInt(`${projectp}`) ^ parseInt(`${descR}`)}`);
         break;
      }
       let sportV = 1.0;
       let layoutN = 2.0;
      let clearS = projectp >= 8368386.0;
      do {
         projectp *= parseFloat(`${parseInt(`${sportV}`)}`);
         if (clearS) {
            break;
         }
      } while (clearS && (5.26 <= (projectp * 3.40) || 1.73 <= (3.40 / (Math.max(9, projectp)))));
      for (let s = 0; s < 2; s++) {
         projectp -= parseFloat(`${1}`);
      }
      small1 *= parseFloat(`${parseInt(`${circlex}`)}`);
      if (gray6) {
         break;
      }
   } while (gray6 && (2.74 > (small1 * progressa) || 3.58 > (2.74 * progressa)));
   while (1 <= (5 / (Math.max(5, annerC.size)))) {
       let rulesA: Array<any> = [String.fromCharCode(120,95,50,53,95,98,97,115,105,99,115,0), String.fromCharCode(102,95,54,50,95,103,114,111,117,112,110,97,109,101,115,0), String.fromCharCode(104,97,100,97,109,97,114,100,120,95,51,95,55,55,0)];
       let phoneI: Array<any> = [113, 316];
      for (let a = 0; a < 1; a++) {
         rulesA.push(rulesA.length % (Math.max(4, phoneI.length)));
      }
       let backwardt = 4;
      while (!phoneI.includes(backwardt)) {
          let redirectc = 3.0;
          let tumbnailN: Map<any, any> = new Map([[String.fromCharCode(98,101,108,111,110,103,95,51,95,54,0),957], [String.fromCharCode(105,95,54,57,95,102,105,108,116,101,114,102,110,0),358], [String.fromCharCode(105,116,101,114,97,116,105,111,110,115,95,50,95,50,54,0),411]]);
          let loginz = String.fromCharCode(120,95,55,55,95,116,105,109,101,122,111,110,101,0);
         backwardt |= phoneI.length << (Math.min(2, Math.abs(backwardt)));
         redirectc *= parseFloat(`${tumbnailN.size}`);
         tumbnailN.set(`${redirectc}`, parseInt(`${redirectc}`));
         loginz = `${(loginz == String.fromCharCode(80,0) ? tumbnailN.size : loginz.length)}`;
         break;
      }
      for (let j = 0; j < 2; j++) {
          let playlistq: Map<any, any> = new Map([[String.fromCharCode(98,95,52,49,95,99,111,109,98,105,110,101,114,0),false ], [String.fromCharCode(97,95,49,52,95,114,102,102,116,98,0),true ], [String.fromCharCode(107,95,54,55,95,112,97,116,104,115,0),false ]]);
         rulesA.push(2);
         playlistq.set(`${playlistq.size}`, playlistq.size);
      }
       let xvode = 3.0;
       let benefitJ = 5.0;
         benefitJ /= Math.max(3, parseFloat(`${parseInt(`${xvode}`) >> (Math.min(2, Math.abs(backwardt)))}`));
      annerC.set(`${untickH}`, twitterI.length);
      break;
   }
   if (!umengb.endsWith(`${circlex}`)) {
       let dialog1 = true;
         dialog1 = !dialog1;
          let videoO: Array<any> = [539, 584, 347];
          let circlej = true;
          let referrerP = 4.0;
         dialog1 = videoO.length == 35;
         videoO.push(parseInt(`${referrerP}`) * 2);
         circlej = !circlej || referrerP < 45.64;
         dialog1 = !dialog1;
      umengb += "2";
   }
      plusa += `${2 << (Math.min(1, twitterI.length))}`;
      o_centerB += `${annerC.size & twitterI.length}`;
      plusa += `${sharedJ.length}`;
   for (let p = 0; p < 3; p++) {
       let bell_: Array<any> = [838, 847, 521];
       let baidu5 = 0.0;
       let projectS = 5.0;
       let sportsA = String.fromCharCode(99,111,111,108,100,111,119,110,115,95,120,95,50,52,0);
       let details2 = true;
          let incidentf = 2.0;
          let spec8 = String.fromCharCode(102,114,97,112,115,95,114,95,49,54,0);
         baidu5 /= Math.max((parseFloat(`${2 ^ (details2 ? 2 : 4)}`)), 3);
         incidentf += spec8.length;
         spec8 += `${spec8.length}`;
      if ((baidu5 - 5.69) > 5.72) {
         bell_ = [bell_.length >> (Math.min(1, Math.abs(parseInt(`${projectS}`))))];
      }
         details2 = (parseFloat(`${sportsA.length}`) * baidu5) < 94.47;
      let resultt = String.fromCharCode(108,116,113,112,50,120,99,100,0) == sportsA;
      do {
          let splashq = String.fromCharCode(102,111,114,105,95,113,95,54,56,0);
          let apple1: Map<any, any> = new Map([[String.fromCharCode(119,95,50,52,95,109,101,116,97,0),747], [String.fromCharCode(102,95,50,57,95,114,101,108,111,99,107,0),811]]);
          let l_centerV = 1;
          let hongkongw = 3.0;
          let activeF = false;
         sportsA = "2";
         splashq = `${splashq.length * parseInt(`${hongkongw}`)}`;
         apple1.set(`${activeF}`, ((activeF ? 4 : 1) / (Math.max(4, parseInt(`${hongkongw}`)))));
         l_centerV |= 2;
         if (resultt) {
            break;
         }
      } while ((details2 || 4 >= sportsA.length) && resultt);
         bell_.push((sportsA == String.fromCharCode(104,0) ? sportsA.length : parseInt(`${baidu5}`)));
         sportsA += `${bell_.length & parseInt(`${projectS}`)}`;
      let crossZ = details2 ? !details2 : details2;
      do {
          let moduleJ = false;
          let plashf: Map<any, any> = new Map([[String.fromCharCode(120,95,54,56,95,115,104,117,116,116,101,114,0),967], [String.fromCharCode(118,95,52,52,0),239]]);
          let left2: Array<any> = [873, 410, 332];
         details2 = plashf.size == 39 && moduleJ;
         moduleJ = (left2.length + left2.length) == 37;
         plashf = new Map([[`${left2.length}`, left2.length - left2.length]]);
         if (crossZ) {
            break;
         }
      } while (crossZ && (!details2));
          let helperZ = true;
         details2 = parseInt(`${projectS}`) >= bell_.length;
         helperZ = (helperZ ? !helperZ : helperZ);
      let chatY = String.fromCharCode(112,57,105,0) == sportsA;
      do {
         sportsA += `${3 << (Math.min(Math.abs(parseInt(`${baidu5}`)), 4))}`;
         if (chatY) {
            break;
         }
      } while ((!details2) && chatY);
         baidu5 -= parseFloat(`${bell_.length}`);
      if (sportsA.length >= 5) {
         details2 = String.fromCharCode(95,0) == sportsA && bell_.length >= 77;
      }
      if (4 <= sportsA.length) {
          let incident4: Array<any> = [309, 41, 201];
          let filledR = 4;
         details2 = filledR < 42;
         incident4 = [incident4.length];
         filledR -= incident4.length;
      }
         bell_ = [sportsA.length];
      let analytic5 = 7220398.0 >= projectS;
      do {
          let malaysiai: Map<any, any> = new Map([[String.fromCharCode(102,100,99,116,95,112,95,57,52,0),String.fromCharCode(102,114,97,99,116,105,111,110,97,108,95,120,95,50,57,0)], [String.fromCharCode(101,114,112,105,99,95,119,95,57,50,0),String.fromCharCode(106,95,56,54,95,97,112,112,101,97,114,0)]]);
         projectS /= Math.max(1, 3 & parseInt(`${baidu5}`));
         malaysiai = new Map([[`${malaysiai.size}`, malaysiai.size + 1]]);
         if (analytic5) {
            break;
         }
      } while ((bell_.includes(projectS)) && analytic5);
          let type_orA = true;
          let background4 = 2.0;
          let privacyV = String.fromCharCode(108,111,103,115,95,52,95,57,56,0);
         baidu5 += (parseFloat(`${(details2 ? 2 : 2) / 2}`));
         type_orA = String.fromCharCode(81,0) == privacyV;
         background4 /= Math.max(parseFloat(`${2 << (Math.min(Math.abs(parseInt(`${background4}`)), 4))}`), 4);
         privacyV += "1";
      untickH = !untickH;
   }
      twitterI.push(feedbackv.length ^ 1);
      plusa += `${umengb.length}`;
      feedbackv.push(umengb.length);
      sharedJ.push(twitterI.length % (Math.max(1, 9)));
      showR -= 2;
    await refreshUserState();

      untickH = 64.42 == small1;
   for (let b = 0; b < 1; b++) {
       let darkc: Array<any> = [194, 69];
       let connectionN = 5;
       let refreshk: Array<any> = [617, 325, 234];
       let linez = String.fromCharCode(115,95,51,57,95,112,97,99,107,115,0);
       let pointq = String.fromCharCode(97,100,100,114,105,110,102,111,95,106,95,53,48,0);
         refreshk = [3];
         refreshk.push(connectionN % (Math.max(8, linez.length)));
      if (!linez.includes(`${pointq.length}`)) {
         pointq = `${darkc.length}`;
      }
          let progressI = 5;
          let orientationK = 2.0;
          let b_position3 = true;
         pointq += `${1 >> (Math.min(Math.abs(progressI), 1))}`;
         progressI ^= 2 / (Math.max(5, parseInt(`${orientationK}`)));
         orientationK += parseInt(`${orientationK}`) & 3;
         b_position3 = b_position3 || 45.54 <= orientationK;
      if (darkc.length < pointq.length) {
          let tooltipsP = String.fromCharCode(100,105,116,104,101,114,95,103,95,57,49,0);
          let bannerH: Array<any> = [922, 845, 237];
         pointq += `${linez.length}`;
         tooltipsP = "2";
         bannerH.push(1);
      }
      while (2 < (connectionN | 2)) {
         connectionN -= 2;
         break;
      }
      while (connectionN == 4) {
         refreshk.push(linez.length);
         break;
      }
       let grayW = false;
      let splash_ = linez == String.fromCharCode(48,101,103,0);
      do {
          let statisticsf = true;
         linez = `${linez.length}`;
         statisticsf = !statisticsf;
         if (splash_) {
            break;
         }
      } while ((pointq.length > 3) && splash_);
         refreshk.push(linez.length);
      while (darkc.length < pointq.length) {
         pointq += `${1 + darkc.length}`;
         break;
      }
      while (!grayW) {
          let thumbnaill = true;
          let text6 = 4;
          let privacyU = 2.0;
          let control2 = String.fromCharCode(109,115,101,120,95,115,95,56,50,0);
         refreshk = [control2.length / (Math.max(2, 10))];
         thumbnaill = !thumbnaill;
         text6 <<= Math.min(Math.abs(2), 3);
         privacyU -= parseFloat(`${text6 % 2}`);
         control2 += "1";
         break;
      }
      for (let b = 0; b < 1; b++) {
         refreshk = [3 ^ linez.length];
      }
       let temp5 = String.fromCharCode(110,111,100,101,95,107,95,54,50,0);
       let unreadx = String.fromCharCode(119,95,52,95,102,108,105,99,0);
      if (!grayW) {
         linez += `${3 >> (Math.min(4, Math.abs(connectionN)))}`;
      }
      showR += o_centerB.length;
   }
       let rank3 = false;
       let fulln = 5.0;
       let ewardedi = true;
      if (fulln > 3.3) {
         fulln *= parseFloat(`${parseInt(`${fulln}`) | 1}`);
      }
      let twitterY = fulln >= 8414027.0;
      do {
         fulln += (parseFloat(`${(rank3 ? 2 : 5) << (Math.min(Math.abs(parseInt(`${fulln}`)), 5))}`));
         if (twitterY) {
            break;
         }
      } while ((3.41 > (4.70 + fulln)) && twitterY);
      while (rank3) {
         fulln -= parseFloat(`${parseInt(`${fulln}`) - 2}`);
         break;
      }
         ewardedi = !ewardedi;
      for (let z = 0; z < 2; z++) {
         ewardedi = (ewardedi ? !rank3 : !ewardedi);
      }
          let currentE: Array<any> = [556, 470, 214];
          let paginationG = String.fromCharCode(121,95,50,56,95,102,105,108,101,112,97,116,104,0);
         fulln -= (parseFloat(`${parseInt(`${fulln}`) | (rank3 ? 2 : 2)}`));
         currentE.push(currentE.length);
         paginationG += `${paginationG.length << (Math.min(3, currentE.length))}`;
      let brightness7 = fulln <= 9007256.0;
      do {
         fulln *= (parseFloat(`${(rank3 ? 5 : 2) & parseInt(`${fulln}`)}`));
         if (brightness7) {
            break;
         }
      } while (brightness7 && (fulln < 5.63 && (fulln / (Math.max(5.63, 5))) < 1.38));
      for (let j = 0; j < 2; j++) {
         fulln /= Math.max(2, (parseFloat(`${(rank3 ? 4 : 5)}`)));
      }
      let mappingT = fulln >= 9212484.0;
      do {
          let subsd: Map<any, any> = new Map([[String.fromCharCode(116,97,112,101,95,98,95,52,50,0),true ], [String.fromCharCode(106,95,49,49,95,115,116,111,114,97,103,101,0),true ], [String.fromCharCode(97,99,107,117,112,95,108,95,53,54,0),true ]]);
          let bufferb: Map<any, any> = new Map([[String.fromCharCode(115,112,97,99,101,95,117,95,50,52,0),true ], [String.fromCharCode(122,95,50,48,95,97,116,104,0),false ], [String.fromCharCode(113,95,57,54,95,110,112,97,116,99,104,101,115,0),false ]]);
          let description_rx = String.fromCharCode(101,116,104,101,114,115,99,97,110,95,105,95,51,49,0);
          let yingZ = String.fromCharCode(105,95,57,50,0);
         fulln += parseFloat(`${parseInt(`${fulln}`)}`);
         subsd = new Map([[`${bufferb.size}`, 3]]);
         bufferb = new Map([[`${subsd.size}`, (yingZ == String.fromCharCode(120,0) ? subsd.size : yingZ.length)]]);
         description_rx = `${description_rx.length}`;
         if (mappingT) {
            break;
         }
      } while ((5.48 >= fulln && (fulln * 5.48) >= 3.97) && mappingT);
      o_centerB = `${1 + umengb.length}`;
   let sportsO = feedbackv.length <= 9560803;
   do {
      feedbackv.push(1);
      if (sportsO) {
         break;
      }
   } while (sportsO && (1 <= (feedbackv.length & sharedJ.length) && (feedbackv.length & 1) <= 4));
       let profileT = String.fromCharCode(97,101,99,95,120,95,53,57,0);
       let plashv = String.fromCharCode(101,95,55,56,95,118,108,99,115,112,101,99,0);
       let star3 = String.fromCharCode(119,95,51,52,95,115,120,110,101,116,0);
      for (let e = 0; e < 1; e++) {
         profileT = `${profileT.length >> (Math.min(2, plashv.length))}`;
      }
       let sidem = 1.0;
      let telegramp = star3 == String.fromCharCode(51,56,119,106,57,98,119,122,54,54,0);
      do {
          let tooltipsD: Map<any, any> = new Map([[String.fromCharCode(102,95,56,53,95,116,105,109,101,122,111,110,101,0),629], [String.fromCharCode(106,95,49,49,95,99,111,108,108,101,99,116,105,111,110,115,0),544], [String.fromCharCode(114,111,120,121,95,120,95,49,48,0),160]]);
          let statse = String.fromCharCode(115,99,105,101,110,116,105,102,105,99,95,50,95,51,48,0);
          let awayL = String.fromCharCode(122,95,49,48,95,115,116,97,110,100,97,114,100,105,122,101,0);
          let complete1 = String.fromCharCode(102,105,108,108,95,102,95,54,53,0);
          let progressZ = false;
         star3 = `${plashv.length}`;
         tooltipsD = new Map([[`${tooltipsD.size}`, tooltipsD.size]]);
         statse = `${(String.fromCharCode(121,0) == complete1 ? complete1.length : statse.length)}`;
         awayL += `${statse.length}`;
         progressZ = statse.endsWith(`${progressZ}`);
         if (telegramp) {
            break;
         }
      } while (telegramp && ((5 >> (Math.min(3, star3.length))) <= 4));
      for (let u = 0; u < 2; u++) {
         sidem -= 2;
      }
      while (2 < star3.length) {
         plashv += `${(star3 == String.fromCharCode(87,0) ? star3.length : profileT.length)}`;
         break;
      }
      small1 *= parseFloat(`${profileT.length - twitterI.length}`);
   if ((twitterI.length - parseInt(`${showR}`)) <= 1 && (showR - 2.19) <= 1.76) {
       let becomeP: Map<any, any> = new Map([[String.fromCharCode(98,95,52,51,95,98,105,116,114,101,118,0),273], [String.fromCharCode(99,95,50,95,114,101,100,0),302], [String.fromCharCode(97,110,110,111,117,110,99,101,95,121,95,55,50,0),44]]);
       let switch_a8 = String.fromCharCode(98,95,55,53,95,117,110,114,101,102,99,111,117,110,116,0);
       let backgroundQ = String.fromCharCode(106,95,56,50,95,109,107,118,109,117,120,101,114,117,116,105,108,0);
         becomeP.set(backgroundQ, 2 << (Math.min(3, backgroundQ.length)));
       let sidef = false;
         switch_a8 = `${switch_a8.length}`;
       let containerP: Map<any, any> = new Map([[String.fromCharCode(109,95,56,51,95,100,114,111,112,112,101,100,0),388], [String.fromCharCode(97,103,114,101,101,109,101,110,116,95,100,95,49,0),304], [String.fromCharCode(121,99,98,99,114,95,118,95,57,57,0),0]]);
       let bottomI: Map<any, any> = new Map([[String.fromCharCode(114,101,102,101,114,114,101,114,95,97,95,49,49,0),467], [String.fromCharCode(111,98,106,101,99,116,115,95,53,95,51,55,0),51]]);
      if (1 > (becomeP.size / 2) || (2 / (Math.max(2, switch_a8.length))) > 5) {
          let backgroundV = 5;
          let membership6: Map<any, any> = new Map([[String.fromCharCode(113,95,50,48,95,103,105,103,97,98,121,116,101,115,0),String.fromCharCode(119,95,50,56,95,112,114,117,110,105,110,103,0)], [String.fromCharCode(102,95,57,55,95,110,102,116,0),String.fromCharCode(97,116,116,114,98,117,116,101,95,121,95,53,52,0)], [String.fromCharCode(113,95,55,49,95,102,105,108,116,101,114,105,110,103,0),String.fromCharCode(107,95,49,48,48,95,97,108,116,101,114,0)]]);
          let temperaturef: Array<any> = [541, 935];
         switch_a8 = "2";
         backgroundV += backgroundV - membership6.size;
         membership6.set(`${backgroundV}`, temperaturef.length);
         temperaturef.push(membership6.size / (Math.max(temperaturef.length, 3)));
      }
       let product3 = String.fromCharCode(112,97,114,116,105,99,108,101,95,57,95,50,56,0);
       let championx = String.fromCharCode(101,108,105,109,105,110,97,116,105,111,110,95,122,95,55,53,0);
       let crossI = String.fromCharCode(99,95,49,50,95,100,99,98,122,0);
          let anythinkD = 4.0;
          let termsv = String.fromCharCode(97,117,116,104,101,110,116,105,99,97,116,101,95,122,95,57,49,0);
         championx += `${becomeP.size ^ 3}`;
         anythinkD += (termsv == String.fromCharCode(56,0) ? termsv.length : parseInt(`${anythinkD}`));
          let suggestionl = 4;
          let loginzK = 1.0;
         sidef = switch_a8.length < 39;
         suggestionl |= parseInt(`${loginzK}`) + 2;
         loginzK += parseFloat(`${suggestionl}`);
      twitterI = [1 >> (Math.min(1, switch_a8.length))];
   }
      untickH = plusa == String.fromCharCode(67,0);
       let predictionI = true;
         predictionI = (!predictionI ? predictionI : predictionI);
         predictionI = !predictionI;
      if (!predictionI) {
          let kcopy_ma: Map<any, any> = new Map([[String.fromCharCode(108,95,54,49,95,98,121,116,101,114,117,110,0),128], [String.fromCharCode(100,95,54,52,95,99,111,109,112,97,114,101,112,111,119,0),799], [String.fromCharCode(115,105,108,101,110,116,95,57,95,51,50,0),284]]);
          let sansW = 2.0;
          let singaporeO: Array<any> = [String.fromCharCode(114,95,57,53,95,105,110,116,101,114,112,111,108,97,116,101,102,0), String.fromCharCode(111,95,54,53,95,116,109,112,111,0), String.fromCharCode(97,112,105,95,110,95,53,53,0)];
          let questJ = String.fromCharCode(113,95,56,53,0);
          let turnj = String.fromCharCode(116,105,109,105,110,103,95,99,95,52,50,0);
         predictionI = !predictionI;
         kcopy_ma = new Map([[`${singaporeO.length}`, 1 / (Math.max(2, singaporeO.length))]]);
         sansW += 2;
         questJ = `${questJ.length}`;
         turnj = `${(questJ == String.fromCharCode(90,0) ? questJ.length : singaporeO.length)}`;
      }
      small1 *= (parseFloat(`${parseInt(`${small1}`) >> (Math.min(5, Math.abs((untickH ? 1 : 4))))}`));
      annerC = new Map([[`${untickH}`, 3 << (Math.min(Math.abs(parseInt(`${progressa}`)), 4))]]);
       let playlistA = String.fromCharCode(101,95,57,56,95,115,116,111,112,0);
       let topicK = 2.0;
      while (5.98 < (5.72 + topicK)) {
          let adult9 = String.fromCharCode(103,95,50,95,99,101,114,116,115,0);
          let friendsr = String.fromCharCode(100,101,115,116,114,117,99,116,111,114,95,106,95,49,48,0);
          let showq = String.fromCharCode(112,114,101,100,105,99,116,105,111,110,95,52,95,54,53,0);
          let episodeB = 1;
          let gmailD: Map<any, any> = new Map([[String.fromCharCode(102,95,56,48,95,101,116,97,100,97,116,97,0),112], [String.fromCharCode(105,95,51,95,111,105,100,97,110,121,0),585], [String.fromCharCode(100,95,57,54,95,105,110,104,105,98,105,116,115,0),374]]);
         playlistA += `${gmailD.size}`;
         adult9 += "1";
         friendsr = `${adult9.length / 3}`;
         showq = `${adult9.length}`;
         episodeB |= episodeB >> (Math.min(Math.abs(1), 4));
         gmailD = new Map([[friendsr, (String.fromCharCode(115,0) == adult9 ? adult9.length : friendsr.length)]]);
         break;
      }
       let temperatureK = 5.0;
      if (4.38 >= topicK) {
         topicK /= Math.max(4, parseInt(`${temperatureK}`) % 3);
      }
      while ((playlistA.length % 5) < 3) {
          let modalo = String.fromCharCode(101,105,112,118,95,117,95,57,54,0);
         playlistA += `${parseInt(`${temperatureK}`)}`;
         modalo += `${modalo.length % (Math.max(6, modalo.length))}`;
         break;
      }
      if (3.32 >= topicK) {
          let weibo5 = String.fromCharCode(105,95,56,57,95,112,111,108,113,97,0);
          let favicon4 = 1;
          let networkR = 5;
         topicK -= weibo5.length >> (Math.min(3, Math.abs(favicon4)));
         weibo5 = `${networkR * 3}`;
         favicon4 >>= Math.min(Math.abs(networkR / (Math.max(1, 1))), 4);
      }
      let fastV = 9803069.0 <= topicK;
      do {
         topicK += playlistA.length ^ 3;
         if (fastV) {
            break;
         }
      } while (fastV && (4.46 == (topicK + 2.44)));
      small1 *= parseFloat(`${3 & umengb.length}`);
   let module5 = 5531300.0 >= circlex;
   do {
       let common4 = String.fromCharCode(101,108,108,105,112,116,105,99,97,108,95,100,95,53,55,0);
         common4 = `${common4.length ^ common4.length}`;
         common4 = `${common4.length}`;
          let incidente: Array<any> = [896, 948];
         common4 = `${1 % (Math.max(10, incidente.length))}`;
      circlex /= Math.max(parseFloat(`${3 ^ plusa.length}`), 1);
      if (module5) {
         break;
      }
   } while (module5 && (!Array.from(annerC.values()).includes(circlex)));
      showR *= 2;
   let resultK = annerC.size <= 6981812;
   do {
      annerC = new Map([[umengb, 1 | parseInt(`${progressa}`)]]);
      if (resultK) {
         break;
      }
   } while (resultK && (2 <= (annerC.size & twitterI.length) && (annerC.size & 2) <= 4));
    setRefreshing(false);
  };
  const refreshUserState = async () => {
       let membershipX = 4.0;
    let areae: Array<any> = [704, 190];
    let description_x3 = 4.0;
    let showR = 2.0;
    let layout5 = 5.0;
    let hongkong5: Array<any> = [708, 182];
    let animationN = String.fromCharCode(102,97,115,116,95,55,95,52,52,0);
    let analyticse = 4;
    let stationsb = false;
   for (let a = 0; a < 1; a++) {
      showR *= parseInt(`${description_x3}`);
   }
   if ((3 ^ analyticse) == 5 && (layout5 - analyticse) == 4.20) {
      layout5 += 2;
   }
      description_x3 *= animationN.length;
   if (!areae.includes(analyticse)) {
       let shrinkO = 5.0;
       let linkI = 0.0;
       let containerl = String.fromCharCode(108,115,112,102,95,104,95,53,48,0);
      while (2 == containerl.length) {
          let tailt = 1.0;
          let moduleG = String.fromCharCode(104,95,56,53,0);
          let completeE = 5;
          let combinedb = 2.0;
          let singaporeI = 0;
         containerl = `${parseInt(`${linkI}`)}`;
         tailt /= Math.max(2, singaporeI);
         moduleG += "1";
         completeE ^= 3 & moduleG.length;
         combinedb += parseFloat(`${completeE / (Math.max(parseInt(`${combinedb}`), 1))}`);
         singaporeI %= Math.max(parseInt(`${combinedb}`), 2);
         break;
      }
      let sliderL = containerl.length >= 9160483;
      do {
         containerl += `${parseInt(`${linkI}`)}`;
         if (sliderL) {
            break;
         }
      } while ((4 < (4 << (Math.min(2, containerl.length))) || 2.60 < (linkI - containerl.length)) && sliderL);
      for (let i = 0; i < 2; i++) {
         shrinkO /= Math.max(2 ^ parseInt(`${shrinkO}`), 2);
      }
          let notificationh: Map<any, any> = new Map([[String.fromCharCode(97,108,112,104,97,110,117,109,95,103,95,51,53,0),989], [String.fromCharCode(110,95,51,55,95,114,97,110,115,105,116,105,111,110,0),99], [String.fromCharCode(101,120,99,104,97,110,103,101,115,95,57,95,50,53,0),393]]);
          let searchbar1 = 0;
          let roboto9: Map<any, any> = new Map([[String.fromCharCode(112,97,115,116,101,98,111,97,114,100,95,57,95,52,54,0),String.fromCharCode(108,106,112,101,103,95,51,95,51,56,0)], [String.fromCharCode(119,95,50,52,95,97,108,112,104,97,98,101,116,115,0),String.fromCharCode(99,104,105,114,112,95,112,95,56,50,0)], [String.fromCharCode(99,104,97,110,103,101,104,95,110,95,50,48,0),String.fromCharCode(115,95,56,53,95,97,114,103,117,109,101,110,116,115,0)]]);
         containerl = "3";
         notificationh.set(`${searchbar1}`, searchbar1);
         roboto9.set(`${searchbar1}`, searchbar1);
      let servicet = 6707874.0 <= linkI;
      do {
         linkI *= containerl.length;
         if (servicet) {
            break;
         }
      } while ((3 < containerl.length) && servicet);
         linkI *= 2;
      let backgroundX = 7533827.0 <= linkI;
      do {
         linkI *= parseInt(`${linkI}`) * parseInt(`${shrinkO}`);
         if (backgroundX) {
            break;
         }
      } while ((2 < (parseInt(`${linkI}`) * containerl.length) && (containerl.length % 2) < 2) && backgroundX);
         shrinkO += 3;
       let episode4: Map<any, any> = new Map([[String.fromCharCode(101,95,49,50,95,103,101,111,107,101,121,0),716], [String.fromCharCode(102,95,53,57,95,108,105,110,107,101,100,0),540]]);
       let small2: Map<any, any> = new Map([[String.fromCharCode(116,121,112,95,57,95,52,57,0),String.fromCharCode(99,95,57,51,95,115,117,99,104,0)], [String.fromCharCode(99,95,53,57,95,100,115,116,114,101,97,109,0),String.fromCharCode(99,95,54,51,95,109,98,99,110,116,0)], [String.fromCharCode(108,115,112,108,112,99,95,119,95,50,56,0),String.fromCharCode(99,108,105,112,112,105,110,103,95,99,95,57,57,0)]]);
      areae.push(parseInt(`${linkI}`));
   }
      stationsb = (10 >= (hongkong5.length ^ (stationsb ? hongkong5.length : 10)));

    const result = await THFirebase.getUserDetails();

   for (let g = 0; g < 3; g++) {
      hongkong5 = [parseInt(`${layout5}`) - parseInt(`${showR}`)];
   }
   for (let y = 0; y < 1; y++) {
      description_x3 /= Math.max(2, parseInt(`${description_x3}`) / (Math.max(areae.length, 4)));
   }
   let text8 = 5852655.0 >= showR;
   do {
      showR *= parseInt(`${description_x3}`) & 1;
      if (text8) {
         break;
      }
   } while (((description_x3 / (Math.max(3.99, 4))) >= 5.87 || 2.25 >= (showR + 3.99)) && text8);
      hongkong5 = [1];
   if ((animationN.length >> (Math.min(Math.abs(3), 4))) <= 1 && (3 >> (Math.min(3, animationN.length))) <= 4) {
      layout5 += parseInt(`${showR}`);
   }
    if (result == null) {

   for (let k = 0; k < 1; k++) {
       let episodesT = String.fromCharCode(111,95,56,50,95,101,110,117,109,101,114,97,116,105,110,103,0);
       let skipW = String.fromCharCode(104,95,51,48,95,111,117,116,103,111,105,110,103,0);
       let crownu = String.fromCharCode(102,101,116,99,104,101,100,95,57,95,54,0);
       let private_biy = String.fromCharCode(107,95,54,50,95,106,112,101,103,108,115,0);
       let searchbarS = 0;
      if (1 <= (searchbarS << (Math.min(private_biy.length, 2))) || (searchbarS << (Math.min(Math.abs(1), 2))) <= 5) {
          let langkeyR = String.fromCharCode(101,116,97,95,52,95,54,56,0);
          let leftV = String.fromCharCode(117,110,105,109,112,111,114,116,97,110,116,95,105,95,57,48,0);
          let thailandF = 2.0;
         searchbarS += skipW.length;
         langkeyR = `${leftV.length}`;
         leftV += `${leftV.length}`;
         thailandF *= parseFloat(`${leftV.length}`);
      }
      for (let r = 0; r < 3; r++) {
          let castE: Array<any> = [134, 973];
          let firebaseF = String.fromCharCode(113,95,53,56,95,104,105,103,104,108,105,103,104,116,115,0);
          let filterK = 0.0;
          let albumc = 5.0;
          let whiteH = String.fromCharCode(117,110,112,114,101,109,117,108,116,105,112,108,121,95,57,95,49,52,0);
         crownu = `${parseInt(`${filterK}`)}`;
         castE.push(whiteH.length);
         firebaseF = "1";
         filterK -= parseFloat(`${castE.length - whiteH.length}`);
         albumc /= Math.max(3, parseFloat(`${2}`));
      }
       let downloader4 = 4.0;
       let aboutJ = 4.0;
         searchbarS <<= Math.min(4, Math.abs(1));
         private_biy = `${episodesT.length >> (Math.min(5, Math.abs(searchbarS)))}`;
          let langkey_: Array<any> = [493, 489, 499];
         downloader4 -= langkey_.length;
         downloader4 /= Math.max(3, 1 >> (Math.min(1, episodesT.length)));
      while ((private_biy.length >> (Math.min(4, Math.abs(searchbarS)))) == 2 || (searchbarS >> (Math.min(Math.abs(2), 5))) == 3) {
          let plashi: Map<any, any> = new Map([[String.fromCharCode(115,95,57,53,95,103,101,110,97,110,110,0),true ], [String.fromCharCode(112,108,97,110,97,114,116,111,121,117,121,95,49,95,54,0),false ], [String.fromCharCode(101,109,117,108,97,116,111,114,95,118,95,51,49,0),true ]]);
          let comments: Array<any> = [568, 353];
          let taiwanu = 3.0;
          let homey: Map<any, any> = new Map([[String.fromCharCode(99,95,52,49,95,115,117,112,112,111,114,116,101,100,0),String.fromCharCode(100,95,52,49,95,115,97,100,98,0)], [String.fromCharCode(99,95,52,50,95,115,117,103,103,101,115,116,105,111,110,0),String.fromCharCode(97,100,105,100,95,118,95,50,50,0)]]);
         searchbarS /= Math.max((String.fromCharCode(85,0) == episodesT ? episodesT.length : plashi.size), 3);
         plashi.set(`${taiwanu}`, 2);
         comments.push(comments.length);
         taiwanu += 3 ^ parseInt(`${taiwanu}`);
         homey.set(`${taiwanu}`, parseInt(`${taiwanu}`) & homey.size);
         break;
      }
         downloader4 += 2 | searchbarS;
      for (let l = 0; l < 3; l++) {
         skipW = "3";
      }
      let controlsK = episodesT == String.fromCharCode(102,108,112,55,57,122,112,116,54,122,0);
      do {
          let crownl = String.fromCharCode(99,111,110,116,114,111,108,108,101,114,115,95,56,95,53,52,0);
          let internetY: Map<any, any> = new Map([[String.fromCharCode(100,101,99,111,100,101,120,95,56,95,57,50,0),830], [String.fromCharCode(99,97,108,99,108,117,108,97,116,101,95,48,95,51,48,0),728], [String.fromCharCode(113,95,54,55,95,106,111,105,110,105,110,103,0),810]]);
          let viewers = String.fromCharCode(113,117,97,110,116,105,122,97,116,105,111,110,95,50,95,54,52,0);
          let half7 = 2.0;
         episodesT += `${private_biy.length}`;
         crownl = `${parseInt(`${half7}`)}`;
         internetY.set(`${half7}`, (viewers == String.fromCharCode(112,0) ? viewers.length : parseInt(`${half7}`)));
         if (controlsK) {
            break;
         }
      } while ((aboutJ <= 5.66) && controlsK);
      for (let o = 0; o < 2; o++) {
          let descA = 5.0;
          let analytick = String.fromCharCode(108,111,103,102,110,95,105,95,55,50,0);
          let forwardb: Map<any, any> = new Map([[String.fromCharCode(109,117,116,97,116,101,95,122,95,49,0),false ], [String.fromCharCode(99,95,55,54,95,105,110,105,116,105,97,108,105,122,97,116,105,111,110,115,0),false ], [String.fromCharCode(115,95,52,56,95,97,112,111,108,108,111,0),true ]]);
         aboutJ /= Math.max(parseInt(`${aboutJ}`) | skipW.length, 5);
         descA -= parseFloat(`${analytick.length + forwardb.size}`);
         analytick += `${analytick.length}`;
         forwardb.set(`${descA}`, 3 * parseInt(`${descA}`));
      }
      let viewerH = downloader4 <= 6808441.0;
      do {
         downloader4 *= 3 >> (Math.min(Math.abs(parseInt(`${downloader4}`)), 3));
         if (viewerH) {
            break;
         }
      } while (viewerH && (private_biy.length >= parseInt(`${downloader4}`)));
         aboutJ *= 1;
         skipW = `${(private_biy == String.fromCharCode(72,0) ? private_biy.length : episodesT.length)}`;
      membershipX -= parseFloat(`${areae.length << (Math.min(4, Math.abs(searchbarS)))}`);
   }
      stationsb = description_x3 > 24.11;
       let sansT = 4.0;
       let regengt = false;
         regengt = !regengt;
       let editX = 1.0;
       let stationsx = 0.0;
      areae.push(analyticse);
      sansT /= Math.max(4, parseInt(`${sansT}`) & 2);
   if (!animationN.endsWith(`${areae.length}`)) {
       let righte = String.fromCharCode(109,95,54,53,95,99,111,108,111,114,102,117,108,0);
       let brightnessb = 4.0;
       let trash4 = String.fromCharCode(116,97,98,108,101,105,110,105,116,95,114,95,55,53,0);
       let reminderl = 5.0;
      while (1.15 <= reminderl) {
          let placeholderz = String.fromCharCode(112,97,114,101,110,116,95,99,95,57,55,0);
         righte += `${3 & trash4.length}`;
         placeholderz = `${placeholderz.length}`;
         break;
      }
       let langkeyD = String.fromCharCode(105,110,102,95,122,95,50,53,0);
       let quest3: Map<any, any> = new Map([[String.fromCharCode(109,95,53,52,95,109,105,120,101,100,0),967], [String.fromCharCode(100,95,49,57,95,98,121,116,101,114,117,110,0),657]]);
         brightnessb *= (String.fromCharCode(97,0) == langkeyD ? langkeyD.length : parseInt(`${reminderl}`));
         quest3.set(`${trash4}`, 1 / (Math.max(7, trash4.length)));
      let formj = 7246022 >= langkeyD.length;
      do {
          let readR: Array<any> = [509, 404, 808];
         langkeyD = `${langkeyD.length << (Math.min(Math.abs(1), 2))}`;
         readR.push(2);
         if (formj) {
            break;
         }
      } while (formj && (brightnessb < langkeyD.length));
         righte += `${3 / (Math.max(8, langkeyD.length))}`;
         brightnessb -= righte.length % 1;
         righte += "2";
       let modeG = 4.0;
         brightnessb *= parseInt(`${brightnessb}`) / 3;
      for (let x = 0; x < 2; x++) {
         langkeyD = `${parseInt(`${reminderl}`)}`;
      }
      animationN = `${animationN.length}`;
   }
   let internet1 = 8914549.0 >= layout5;
   do {
      layout5 *= areae.length ^ 2;
      if (internet1) {
         break;
      }
   } while ((animationN.length > 4) && internet1);
      return;
    }

    await dispatch(updateUserAuth(result));

       let switch_k0 = String.fromCharCode(115,101,103,100,97,116,97,95,98,95,52,52,0);
       let sheetA = 4.0;
       let become3 = false;
         sheetA *= parseFloat(`${3 + switch_k0.length}`);
          let upgradeg: Array<any> = [413, 571];
         sheetA += parseFloat(`${parseInt(`${sheetA}`)}`);
         upgradeg = [upgradeg.length + 3];
       let plusx = true;
      let scheduley = switch_k0 == String.fromCharCode(48,106,48,120,0);
      do {
         switch_k0 += `${(parseInt(`${sheetA}`) ^ (become3 ? 5 : 5))}`;
         if (scheduley) {
            break;
         }
      } while (scheduley && (2 == switch_k0.length));
          let policyj = 2.0;
          let circlev: Map<any, any> = new Map([[String.fromCharCode(115,117,98,114,97,110,103,101,95,106,95,57,55,0),String.fromCharCode(122,95,53,57,95,110,111,115,99,97,108,101,0)], [String.fromCharCode(99,121,99,108,101,99,108,111,99,107,95,52,95,55,0),String.fromCharCode(110,95,48,95,113,117,105,99,107,0)], [String.fromCharCode(104,111,114,105,122,111,110,116,97,108,95,103,95,49,57,0),String.fromCharCode(99,111,112,121,102,100,95,107,95,53,56,0)]]);
          let anythinkm: Array<any> = [950, 431, 621];
         plusx = 20.74 <= sheetA && plusx;
         policyj *= parseFloat(`${circlev.size}`);
         circlev.set(`${anythinkm.length}`, circlev.size | anythinkm.length);
      for (let v = 0; v < 2; v++) {
         switch_k0 += `${((become3 ? 3 : 5) & parseInt(`${sheetA}`))}`;
      }
      let editN = sheetA >= 8821445.0;
      do {
          let showj = 4;
         sheetA -= parseFloat(`${switch_k0.length}`);
         showj -= 1;
         if (editN) {
            break;
         }
      } while ((2 < (switch_k0.length / (Math.max(5, 4))) && (1.63 + sheetA) < 5.92) && editN);
      while ((sheetA - 4.65) < 2.66 && !become3) {
          let orangeY = String.fromCharCode(101,120,116,101,110,115,105,111,110,115,95,57,95,52,57,0);
          let anner5 = false;
          let bing7: Map<any, any> = new Map([[String.fromCharCode(100,95,53,57,95,100,101,115,101,114,105,97,108,105,122,101,100,0),String.fromCharCode(114,101,108,101,97,115,101,95,104,95,51,0)], [String.fromCharCode(113,95,53,52,95,113,115,118,100,101,105,110,116,0),String.fromCharCode(112,111,112,117,112,115,95,100,95,51,49,0)]]);
         become3 = switch_k0.length < 74;
         orangeY += `${bing7.size * 2}`;
         anner5 = bing7.size >= 38;
         break;
      }
      if (switch_k0.endsWith(`${sheetA}`)) {
         sheetA /= Math.max(parseFloat(`${3 | parseInt(`${sheetA}`)}`), 1);
      }
      areae.push(((become3 ? 1 : 4) & parseInt(`${layout5}`)));
   while (layout5 < 5.19) {
       let linev = 2.0;
       let stylew: Array<any> = [478, 961, 156];
       let themeJ = 0.0;
         stylew.push(1);
      for (let b = 0; b < 2; b++) {
          let megaphoneg = String.fromCharCode(102,95,53,57,95,99,111,109,112,97,116,105,98,108,101,0);
          let editF = 5.0;
          let statisticsf = 0.0;
          let questy = String.fromCharCode(109,117,120,95,102,95,49,52,0);
          let splashl = 4;
         linev /= Math.max(parseFloat(`${2 & parseInt(`${statisticsf}`)}`), 4);
         megaphoneg += `${splashl ^ questy.length}`;
         editF += parseFloat(`${2 % (Math.max(10, questy.length))}`);
         statisticsf += (megaphoneg == String.fromCharCode(122,0) ? questy.length : megaphoneg.length);
         splashl %= Math.max(parseInt(`${editF}`), 5);
      }
          let friends4: Array<any> = [651, 344, 64];
         stylew = [1];
         friends4.push(friends4.length << (Math.min(3, friends4.length)));
       let humiditym: Array<any> = [769, 923, 123];
       let rewindR: Array<any> = [46, 742, 400];
         stylew = [rewindR.length];
       let soundb = false;
       let verticalb = true;
       let dice0 = 2.0;
       let bellG = 2.0;
         linev /= Math.max((parseFloat(`${(soundb ? 2 : 2) * 1}`)), 2);
      for (let b = 0; b < 1; b++) {
         themeJ /= Math.max(stylew.length >> (Math.min(Math.abs(3), 5)), 4);
      }
      layout5 += parseInt(`${description_x3}`) / 1;
      break;
   }
      areae.push(3);
   for (let n = 0; n < 3; n++) {
      stationsb = 95 > hongkong5.length;
   }
      stationsb = description_x3 >= 86.84 || stationsb;

    return;
  };

  useEffect(() => {
    

    let mergedArray;

    if (userState.user?.userUpline.created_at != '') {
      mergedArray = userState.user?.userInvitedUserList.concat(userState.user?.userUpline);
    } else {
      mergedArray = userState.user.userInvitedUserList;
    }

    mergedArray.sort((a: any, b: any) => {
      let dateA: any;
      dateA = new Date(a.created_at);
      let dateB: any;

      dateB = new Date(b.created_at);
      return dateB - dateA;
    });

    mergedArray = mergedArray.map((item: any) => {
      let displayText = '';
      if (item.text) {
        displayText = item.text;
        item.invited_vip_reward_day = item.vip_reward_day;
      } else {
        displayText = item.title;
      }

      return { ...item, displayText: displayText };
    });
    console.log('mergedArray');
    console.log(mergedArray);
    setInviteList(mergedArray);
  }, []);

  return (
    <ScreenContainer>
      <TitleWithBackButtonHeader title="累计奖励明细" />
      <ScrollView
        showsVerticalScrollIndicator={false}
        refreshControl={
          <RefreshControl refreshing={refreshing} onRefresh={handleRefresh} />
        }>
        { }
        <LinearGradient
          colors={['#323638', '#1a1d20']} 
          locations={[0.1, 1]}
          start={{ x: 1, y: 0 }} 
          end={{ x: 0, y: 1 }} 
          style={{
            marginTop: 40,
            paddingHorizontal: 20,
            paddingVertical: 10,
            borderRadius: 15,
            flexDirection: 'row', 
            justifyContent: 'space-between',
            alignItems: 'center',
            minHeight: 120,
          }}>
          <View style={styles.container}>
            { }
            <Text style={styles.title}>{userState.user?.userTotalInvite}</Text>
            <Text style={styles.subTitle}>已邀请人数</Text>
          </View>

          <View style={styles.container}>
            { }
            <Text style={styles.title}>
              {userState.user?.userAccumulateRewardDay}
            </Text>
            <Text style={styles.subTitle}>累计获得天数</Text>
          </View>
        </LinearGradient>

        { }
        <View style={{ marginTop: 40, alignItems: 'center' }}>
          <Text
            style={{ ...textVariants.header, fontSize: 16, fontWeight: '700' }}>
            邀请记录
          </Text>

          {inviteList.length > 0 && (
            <View
              style={{
                marginTop: 20,
                marginBottom: 40,
                backgroundColor: '#1D2023',
                borderRadius: 15,
                overflow: 'hidden',
                width: '100%',
              }}>
              {inviteList.map(item => (
                <View
                  key={item.displayText}
                  style={{
                    flexDirection: 'row',
                    justifyContent: 'space-between',
                    alignContent: 'center',
                    paddingHorizontal: 20,
                    paddingVertical: 15,
                  }}>
                  <View style={{ flex: 1, paddingRight: 10 }}>
                    <Text
                      style={{ fontSize: 16, color: '#fff' }}
                      numberOfLines={1}
                      ellipsizeMode="tail">
                      {item.displayText}
                    </Text>
                    <Text style={{ color: '#9c9c9c' }}>{item.created_at}</Text>
                  </View>

                  <View
                    style={{
                      justifyContent: 'center',
                      alignContent: 'center',
                    }}>
                    <Text
                      style={{
                        color: colors.primary,
                        fontSize: 16,
                        flex: 1,
                      }}>
                      +{item.invited_vip_reward_day}天
                    </Text>
                  </View>
                </View>
              ))}
            </View>
          )}

          {inviteList.length == 0 && (
            <View
              style={{
                marginTop: 20,
                marginBottom: 40,
                backgroundColor: '#1D2023',
                borderRadius: 15,
                overflow: 'hidden',
                width: '100%',
              }}>
              <View
                style={{
                  flexDirection: 'row',

                  alignContent: 'center',
                  paddingHorizontal: 20,
                  paddingVertical: 20,
                }}>
                <View>
                  <Text style={{ color: '#fff' }}>暂时无邀请记录</Text>
                </View>
              </View>
            </View>
          )}
        </View>
      </ScrollView>
    </ScreenContainer>
  );
};

const styles = StyleSheet.create({
  loading: {
    flexDirection: 'row',
    justifyContent: 'center',
    flex: 1,
  },
  backgroundImage: {
    flex: 1, 
    resizeMode: 'cover', 
  },
  container: {
    flex: 1,
    justifyContent: 'space-between',
    alignItems: 'flex-start',
    paddingLeft: 25,
  },
  title: {
    color: '#fff',
    fontSize: 26,
    fontWeight: '900',
  },
  subTitle: {
    fontSize: 16,
    fontWeight: '400',
    color: '#9C9C9C',
  },
});
