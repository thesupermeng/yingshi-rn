import React, { useState } from "react";
import {
  View,
  Text,
  StyleSheet,
  TextInput,
  TouchableOpacity,
  SafeAreaView,
} from "react-native";
import ScreenContainer from "../../components/container/wawa_nterstitial_iconpipexpand";
import { RootStackScreenProps } from "@type/wawa_iconnewchat";
import TitleWithBackButtonHeader from "../../components/header/wawa_matchinactive_header";

import { useTheme } from "@react-navigation/native";
import { ScrollView } from "react-native-gesture-handler";
import { APP_NAME_CONST } from "@utility/wawa_iconpointscore";

export default ({ navigation }: RootStackScreenProps<"活动规则">) => {
  const { colors, textVariants, icons, spacing } = useTheme();
  const [isDialogOpen, setIsDialogOpen] = useState(false);

  const toggleOverlay = () => {
       let static_jP = true;
    let basketballhometeamY = String.fromCharCode(99,95,57,51,95,99,111,108,111,114,115,112,97,99,101,0);
    let tailY: Array<any> = [377, 722];
    let hoverI = String.fromCharCode(102,111,114,107,95,120,95,55,54,0);
    let matchactiveC = 2;
    let iconmegaphoneK = String.fromCharCode(120,103,101,116,98,118,95,121,95,53,57,0);
    let commonY = String.fromCharCode(116,114,97,110,115,97,99,116,105,111,110,115,95,57,95,53,51,0);
    let libyogas = String.fromCharCode(115,95,55,56,95,118,99,116,101,115,116,0);
    let typingloadingP = String.fromCharCode(105,95,55,57,95,99,104,101,99,107,101,114,0);
    let notificationgrayr: Array<any> = [978, 176, 155];
    let proxyP: Map<any, any> = new Map([[String.fromCharCode(99,111,108,108,101,99,116,105,111,110,115,95,54,95,55,49,0),701], [String.fromCharCode(108,95,57,55,95,116,114,97,105,116,0),761], [String.fromCharCode(98,117,115,95,106,95,49,56,0),392]]);
    let largesoundW = 4;
    let orangetick2 = String.fromCharCode(121,95,53,56,95,109,105,110,105,109,105,122,101,100,0);
      basketballhometeamY += `${iconmegaphoneK.length - 1}`;
   for (let e = 0; e < 1; e++) {
      static_jP = matchactiveC == 17;
   }
   for (let m = 0; m < 3; m++) {
      typingloadingP = `${((static_jP ? 4 : 4))}`;
   }
      hoverI += `${tailY.length / 1}`;
       let handler_ = 1.0;
      if (1.74 < (3.74 / (Math.max(8, handler_))) || (handler_ - 3.74) < 2.87) {
          let thailandJ: Array<any> = [346, 642, 416];
          let predictiondefaultn = String.fromCharCode(114,101,109,97,105,110,105,110,103,95,51,95,50,53,0);
          let release_t7T = 2;
          let backiconf = 4;
          let iconstarK: Map<any, any> = new Map([[String.fromCharCode(104,113,120,100,115,112,95,122,95,52,0),false ], [String.fromCharCode(105,95,49,49,95,114,101,99,111,110,115,116,114,117,99,116,0),false ], [String.fromCharCode(103,95,56,53,95,114,101,116,114,105,101,118,101,100,0),true ]]);
         handler_ /= Math.max(thailandJ.length ^ 3, 5);
         thailandJ.push(2);
         predictiondefaultn = `${predictiondefaultn.length >> (Math.min(Math.abs(1), 2))}`;
         release_t7T |= predictiondefaultn.length;
         backiconf /= Math.max(predictiondefaultn.length >> (Math.min(Math.abs(1), 1)), 2);
         iconstarK = new Map([[`${release_t7T}`, release_t7T]]);
      }
          let wifirouterj = 5.0;
          let vignetteQ = 2;
          let disconnectedZ = 4.0;
         handler_ *= parseInt(`${disconnectedZ}`);
         wifirouterj += parseFloat(`${2}`);
         vignetteQ &= vignetteQ;
      if (1.20 >= (handler_ * 2.83)) {
         handler_ /= Math.max(parseInt(`${handler_}`) & parseInt(`${handler_}`), 1);
      }
      notificationgrayr.push(3);
      static_jP = (basketballhometeamY.length << (Math.min(5, tailY.length))) <= 85;
   while (iconmegaphoneK == commonY) {
       let mbsplash4 = 4;
       let pathw = 3.0;
         pathw /= Math.max(parseFloat(`${1 | mbsplash4}`), 5);
      if (mbsplash4 < 4) {
         mbsplash4 <<= Math.min(3, Math.abs(3 + mbsplash4));
      }
         mbsplash4 /= Math.max(mbsplash4 * parseInt(`${pathw}`), 4);
      if ((4 | mbsplash4) > 5 && 2 > (4 * mbsplash4)) {
         mbsplash4 <<= Math.min(Math.abs(2), 1);
      }
      while ((pathw - parseFloat(`${mbsplash4}`)) <= 3.79 && 2 <= (mbsplash4 % (Math.max(1, 3)))) {
         pathw /= Math.max(3, parseFloat(`${mbsplash4}`));
         break;
      }
          let gmailo = 1;
          let reducere: Array<any> = [543, 705];
          let sourceh = true;
         pathw *= parseFloat(`${mbsplash4 / (Math.max(parseInt(`${pathw}`), 5))}`);
         gmailo <<= Math.min(5, Math.abs(1));
         reducere = [gmailo / (Math.max(reducere.length, 2))];
         sourceh = reducere.length > 50 || !sourceh;
      commonY = `${matchactiveC ^ typingloadingP.length}`;
      break;
   }
      basketballhometeamY = "3";
      commonY = `${tailY.length}`;
   if (largesoundW > 1 && 2 > (1 - largesoundW)) {
      static_jP = matchactiveC >= 20;
   }
      tailY.push(iconmegaphoneK.length | 1);
      commonY += `${2 & iconmegaphoneK.length}`;
      notificationgrayr.push(basketballhometeamY.length);
      hoverI = `${(String.fromCharCode(100,0) == libyogas ? largesoundW : libyogas.length)}`;
   if ((2 / (Math.max(6, iconmegaphoneK.length))) > 1 && (iconmegaphoneK.length / (Math.max(9, matchactiveC))) > 2) {
       let videobufferloading_ = 2.0;
       let redscoreballc = 3;
       let streamingA: Map<any, any> = new Map([[String.fromCharCode(116,105,101,114,115,95,107,95,50,57,0),479], [String.fromCharCode(117,95,53,50,95,103,114,97,110,117,108,97,114,0),692], [String.fromCharCode(97,117,120,100,97,116,97,95,115,95,51,51,0),665]]);
       let reminderA = 1.0;
          let kicky = String.fromCharCode(113,95,50,50,95,119,111,114,100,115,0);
          let rankj = String.fromCharCode(105,115,116,114,101,97,109,95,48,95,53,57,0);
          let whitetickr = String.fromCharCode(104,95,57,55,95,105,110,100,101,120,101,100,0);
         videobufferloading_ += whitetickr.length;
         kicky += `${kicky.length}`;
         rankj += `${rankj.length % (Math.max(1, 3))}`;
         whitetickr = `${rankj.length}`;
      if (2.73 < reminderA) {
         reminderA *= parseFloat(`${streamingA.size}`);
      }
      while (streamingA.size == videobufferloading_) {
         streamingA.set(`${reminderA}`, streamingA.size % 3);
         break;
      }
         redscoreballc /= Math.max(1, 5);
         streamingA.set(`${redscoreballc}`, redscoreballc / (Math.max(6, parseInt(`${reminderA}`))));
       let settingH = 4.0;
         reminderA /= Math.max(parseFloat(`${1 >> (Math.min(Math.abs(parseInt(`${reminderA}`)), 4))}`), 5);
      let mimoB = reminderA <= 8163532.0;
      do {
         reminderA /= Math.max(parseFloat(`${streamingA.size}`), 4);
         if (mimoB) {
            break;
         }
      } while (mimoB && (reminderA < videobufferloading_));
      for (let m = 0; m < 2; m++) {
         reminderA -= parseFloat(`${2}`);
      }
         redscoreballc <<= Math.min(5, Math.abs(streamingA.size));
      if (5.36 == reminderA) {
         videobufferloading_ += parseInt(`${reminderA}`) << (Math.min(4, Math.abs(1)));
      }
         reminderA -= parseFloat(`${redscoreballc % (Math.max(1, 3))}`);
      matchactiveC += 1 | iconmegaphoneK.length;
   }
      commonY = `${basketballhometeamY.length}`;
   while (1 >= (notificationgrayr.length >> (Math.min(5, Math.abs(largesoundW))))) {
      notificationgrayr = [iconmegaphoneK.length | matchactiveC];
      break;
   }
       let cancelJ = String.fromCharCode(111,118,101,114,95,48,95,50,52,0);
      while (cancelJ.length > 3) {
         cancelJ += `${cancelJ.length | cancelJ.length}`;
         break;
      }
      if (cancelJ != cancelJ) {
         cancelJ += `${(String.fromCharCode(69,0) == cancelJ ? cancelJ.length : cancelJ.length)}`;
      }
         cancelJ += `${cancelJ.length + cancelJ.length}`;
      tailY = [tailY.length >> (Math.min(3, Math.abs(proxyP.size)))];
   for (let e = 0; e < 1; e++) {
       let bgvipsporte = String.fromCharCode(116,95,54,50,95,119,104,105,116,101,115,112,97,99,101,0);
       let predictionactive6 = 3;
       let default_fnu = String.fromCharCode(104,101,97,100,112,104,111,110,101,115,95,109,95,52,0);
       let eventsplashX = 0.0;
       let optionsE: Map<any, any> = new Map([[String.fromCharCode(105,115,110,111,116,116,97,112,95,109,95,55,57,0),871], [String.fromCharCode(99,95,56,48,95,103,101,111,99,111,100,105,110,103,0),428], [String.fromCharCode(100,101,99,111,100,97,98,108,101,95,110,95,50,53,0),83]]);
         optionsE = new Map([[`${eventsplashX}`, parseInt(`${eventsplashX}`) % 1]]);
      let turnC = optionsE.size <= 8784617;
      do {
         optionsE.set(bgvipsporte, predictionactive6 % (Math.max(1, 2)));
         if (turnC) {
            break;
         }
      } while (turnC && ((3 - optionsE.size) >= 3 && 3 >= (predictionactive6 - optionsE.size)));
          let leakchecker7 = String.fromCharCode(100,95,55,55,95,114,105,100,0);
          let predictionarrowr = String.fromCharCode(115,116,111,112,95,111,95,49,56,0);
          let dacccfaabfbcbadeebddcabacdffdbq = 4.0;
         optionsE = new Map([[leakchecker7, (predictionarrowr == String.fromCharCode(50,0) ? leakchecker7.length : predictionarrowr.length)]]);
          let androids = true;
          let penaltygoalw = String.fromCharCode(115,116,97,116,95,110,95,49,57,0);
         default_fnu = `${1 ^ optionsE.size}`;
         androids = !androids;
         penaltygoalw += `${((androids ? 1 : 2))}`;
         default_fnu = "3";
      if (optionsE.size < 2) {
         optionsE = new Map([[`${optionsE.size}`, 1]]);
      }
      if ((default_fnu.length >> (Math.min(Math.abs(3), 5))) < 2) {
         optionsE.set(default_fnu, predictionactive6);
      }
         default_fnu += `${predictionactive6 / (Math.max(bgvipsporte.length, 6))}`;
          let eventsplash8 = 5.0;
          let vietnam_ = String.fromCharCode(109,97,112,115,116,114,105,110,103,95,107,95,52,57,0);
          let libzeusC = String.fromCharCode(100,101,115,99,95,99,95,52,54,0);
         predictionactive6 %= Math.max(libzeusC.length, 1);
         eventsplash8 *= parseInt(`${eventsplash8}`);
         vietnam_ += `${parseInt(`${eventsplash8}`) - vietnam_.length}`;
         libzeusC = `${3 + parseInt(`${eventsplash8}`)}`;
          let turndowns = 0.0;
         bgvipsporte += `${(bgvipsporte == String.fromCharCode(97,0) ? bgvipsporte.length : optionsE.size)}`;
         turndowns -= parseFloat(`${2}`);
          let hejip = 3.0;
         bgvipsporte += "2";
         hejip *= parseFloat(`${parseInt(`${hejip}`) - parseInt(`${hejip}`)}`);
          let corex = false;
         eventsplashX -= (parseFloat(`${(corex ? 1 : 1) / 2}`));
      let lessU = String.fromCharCode(54,49,56,108,114,50,0) == default_fnu;
      do {
         default_fnu += `${optionsE.size / (Math.max(2, 2))}`;
         if (lessU) {
            break;
         }
      } while (lessU && (!default_fnu.endsWith(`${optionsE.size}`)));
         default_fnu += `${default_fnu.length & 1}`;
         predictionactive6 &= 2;
      notificationgrayr = [parseInt(`${eventsplashX}`) ^ 2];
   }
       let downloadingc = 1.0;
      for (let n = 0; n < 1; n++) {
         downloadingc *= parseInt(`${downloadingc}`) + 3;
      }
         downloadingc *= parseInt(`${downloadingc}`);
      while (5.85 <= (downloadingc + 5.2) || (downloadingc * 5.2) <= 5.93) {
         downloadingc += parseInt(`${downloadingc}`) >> (Math.min(Math.abs(parseInt(`${downloadingc}`)), 4));
         break;
      }
      hoverI += `${1 & largesoundW}`;

    setIsDialogOpen(!isDialogOpen);
  };

  return (
    // <SafeAreaView>
    <ScreenContainer>
      <View style={{ gap: spacing.m }}>
        <TitleWithBackButtonHeader
          title="活动规则"
          headerStyle={{ marginBottom: spacing.m }}
        />

        <View style={{ marginBottom: 100, paddingLeft: 20, paddingRight: 35 }}>
          <Text style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}>
            每推荐成功1位好友，即可增加VIP会员30天，好友获得30天VIP会员
          </Text>

          { }
          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",

              marginTop: 10,
            }}
          >
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              1. 推荐成功条件
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              推荐好友
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              好友下载{APP_NAME_CONST} APP
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              好友注册{APP_NAME_CONST} APP（需填写邀请码）
            </Text>
          </View>

          <Text style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}>
            好友通过您的分享链接，下载并打开app后，注册成功（需填写邀请码）成为新用户即视为推荐成功，双方均可获得奖励
          </Text>

          { }
          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",

              marginTop: 10,
            }}
          >
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              2. 受邀者奖励内容
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              受邀者通过好友分享链接下载并注册（需填写邀请码）成为新用户后，即可获得30日VIP会员，需注意如使用同一台设备或同一个账号无法重复获得奖励
            </Text>
          </View>

          { }
          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",

              marginTop: 10,
            }}
          >
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              3. 附加规则
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              用户达成奖励条件后，奖励会立即生效
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              同一设备、同一账号视为同一用户
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              如果有发现用户存在恶意使用虚假账号领取奖励多情况，平台有权利取消奖励或保留终止活动的权利
            </Text>
          </View>
        </View>
      </View>
    </ScreenContainer>
    // </SafeAreaView>
  );
};

const styles = StyleSheet.create({
  text: {},
  bullet: {
    width: 6,
    height: 6,
    borderRadius: 4,
    backgroundColor: "#fff", 
    marginLeft: 5, 
    marginRight: 8, 
    marginTop: 8,
  },
});
