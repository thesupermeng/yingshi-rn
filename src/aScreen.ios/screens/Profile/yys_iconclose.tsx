import React, { useEffect, useState } from "react";
import {
  View,
  Text,
  StyleSheet,
  TextInput,
  TouchableOpacity,
} from "react-native";
import ScreenContainer from "../../components/container/yys_matches";
import { RootStackScreenProps } from "@type";
import { useTheme } from "@react-navigation/native";
import { yysIconclosewhiteBaiduads } from "@redux";

import TitleWithBackButtonHeader from "../../components/header/yys_away_predictionloss_header";
import { Button } from "@rneui/themed";
import ShowMoreButton from "../../components/button/yys_gradle_dice_button";
import { Logo2Svg } from "@static";
import NotificationModal from "../../components/modal/yys_cornershoot_smallsound";
import {
  APP_EMAIL_CONST,
  APP_NAME_CONST,
  APP_VERSION,
} from "@utility";
import AsyncStorage from "@react-native-async-storage/async-storage";
import RNRestart from 'react-native-restart';

export default ({ navigation }: RootStackScreenProps<"关于我们">) => {
  const { colors, textVariants, icons, spacing } = useTheme();
  const [isDialogOpen, setIsDialogOpen] = useState(false);
  const [countToggleB, setCountToggleB] = useState(0);

  const toggleOverlay = () => {
       let libimagepipeline4 = 4.0;
    let kuaishouA: Array<any> = [31, 32, 898];
    let defaultfootballbge = String.fromCharCode(102,95,49,55,95,99,111,100,101,114,115,0);
    let countdownG = false;
    let baiduI: Map<any, any> = new Map([[String.fromCharCode(117,95,51,55,95,99,111,110,118,101,114,115,97,116,105,111,110,0),450], [String.fromCharCode(111,95,52,50,95,115,105,100,120,0),739], [String.fromCharCode(100,95,50,49,95,97,115,115,101,114,116,115,0),760]]);
    let gpayd = String.fromCharCode(116,95,52,53,95,115,112,108,105,116,0);
    let cornerB = String.fromCharCode(104,97,114,100,95,119,95,52,52,0);
    let inouttargetyellowe: Array<any> = [847, 462, 191];
    let liveshareO = 0.0;
      libimagepipeline4 /= Math.max((String.fromCharCode(71,0) == defaultfootballbge ? gpayd.length : defaultfootballbge.length), 5);
   while (cornerB == String.fromCharCode(73,0)) {
      gpayd += `${cornerB.length}`;
      break;
   }
   for (let n = 0; n < 2; n++) {
       let modal0 = 1;
      while ((modal0 / (Math.max(modal0, 2))) == 3 && 1 == (modal0 / 3)) {
          let notificationX = String.fromCharCode(103,95,55,49,95,117,110,114,101,102,0);
         modal0 %= Math.max(2, modal0);
         notificationX += `${notificationX.length % (Math.max(10, notificationX.length))}`;
         break;
      }
      if (modal0 > 5) {
          let o_count_: Map<any, any> = new Map([[String.fromCharCode(113,95,52,50,95,114,117,108,101,115,0),382], [String.fromCharCode(105,95,56,55,95,99,111,109,109,105,116,116,101,114,0),414]]);
          let showlessi = 1;
          let bell0 = 2.0;
          let libapminsightb9 = 4.0;
         modal0 /= Math.max(2, o_count_.size);
         o_count_.set(`${showlessi}`, 1);
         showlessi ^= showlessi;
         bell0 += showlessi;
         libapminsightb9 /= Math.max(1, parseFloat(`${parseInt(`${libapminsightb9}`) % 2}`));
      }
      let libcrashsdk0 = modal0 >= 8436164;
      do {
         modal0 %= Math.max(2, modal0);
         if (libcrashsdk0) {
            break;
         }
      } while (libcrashsdk0 && (4 <= (3 - modal0) && (modal0 - 3) <= 2));
      defaultfootballbge += `${defaultfootballbge.length << (Math.min(2, Math.abs(parseInt(`${libimagepipeline4}`))))}`;
   }
   let awayplayerr = kuaishouA.length <= 5288283;
   do {
       let brightnessV = String.fromCharCode(105,95,49,49,95,117,116,105,108,105,116,121,0);
      while (brightnessV.includes(brightnessV)) {
          let fulln = String.fromCharCode(100,105,115,112,111,115,97,98,108,101,115,95,120,95,55,50,0);
          let catagoryk = String.fromCharCode(112,95,54,55,95,114,97,110,100,101,110,0);
          let overlayk: Map<any, any> = new Map([[String.fromCharCode(107,95,54,52,95,111,102,111,114,109,97,116,0),String.fromCharCode(98,111,117,110,99,105,110,101,115,115,95,57,95,55,50,0)], [String.fromCharCode(113,95,54,56,95,100,105,97,99,114,105,116,105,99,0),String.fromCharCode(115,95,50,95,110,111,99,97,115,101,0)]]);
         brightnessV = `${(catagoryk == String.fromCharCode(116,0) ? overlayk.size : catagoryk.length)}`;
         fulln = `${fulln.length >> (Math.min(Math.abs(1), 3))}`;
         break;
      }
          let details1: Map<any, any> = new Map([[String.fromCharCode(106,95,56,49,95,105,118,97,114,0),849], [String.fromCharCode(97,95,48,95,101,110,100,112,111,105,110,116,0),948], [String.fromCharCode(101,95,54,57,95,102,101,116,99,104,105,110,103,0),528]]);
          let refresh2: Map<any, any> = new Map([[String.fromCharCode(114,101,103,95,54,95,51,55,0),877], [String.fromCharCode(101,95,52,55,95,105,115,109,101,109,115,101,116,0),500]]);
          let changeK: Map<any, any> = new Map([[String.fromCharCode(107,108,97,115,115,95,119,95,50,53,0),766], [String.fromCharCode(103,101,111,95,119,95,56,48,0),114]]);
         brightnessV = `${1 ^ details1.size}`;
         details1.set(`${changeK.size}`, refresh2.size);
         refresh2 = new Map([[`${refresh2.size}`, changeK.size]]);
      let referrerZ = brightnessV.length >= 7450681;
      do {
         brightnessV += `${2 >> (Math.min(3, brightnessV.length))}`;
         if (referrerZ) {
            break;
         }
      } while ((brightnessV.length <= brightnessV.length) && referrerZ);
      kuaishouA = [inouttargetyellowe.length];
      if (awayplayerr) {
         break;
      }
   } while ((gpayd.length > kuaishouA.length) && awayplayerr);
   let libreactnativejni4 = kuaishouA.length >= 6096683;
   do {
      kuaishouA = [gpayd.length];
      if (libreactnativejni4) {
         break;
      }
   } while ((parseInt(`${libimagepipeline4}`) > kuaishouA.length) && libreactnativejni4);
   let stationp = 5302450 >= baiduI.size;
   do {
      baiduI.set(`${libimagepipeline4}`, inouttargetyellowe.length);
      if (stationp) {
         break;
      }
   } while ((!Array.from(baiduI.keys()).includes(`${inouttargetyellowe.length}`)) && stationp);
      kuaishouA.push(parseInt(`${libimagepipeline4}`) - 3);
      cornerB += `${kuaishouA.length / (Math.max(1, 8))}`;
       let iconwechatc = 2.0;
      if (4.65 <= (4.64 - iconwechatc)) {
         iconwechatc -= parseInt(`${iconwechatc}`) + parseInt(`${iconwechatc}`);
      }
         iconwechatc /= Math.max(5, parseInt(`${iconwechatc}`) % 2);
         iconwechatc /= Math.max(parseInt(`${iconwechatc}`), 4);
      inouttargetyellowe = [kuaishouA.length];
   for (let a = 0; a < 1; a++) {
       let vietnamA: Array<any> = [862, 681, 62];
       let mintegraln = String.fromCharCode(112,95,48,95,114,101,110,100,105,116,105,111,110,0);
       let settingsu = String.fromCharCode(110,95,52,55,95,117,110,102,108,97,116,116,101,110,101,100,0);
       let launchQ = 4.0;
          let libavutilK = 1;
          let modea = String.fromCharCode(107,95,51,53,95,119,114,105,116,101,116,114,117,110,99,0);
          let defaultteamB = String.fromCharCode(102,114,101,113,115,95,57,95,49,48,0);
         launchQ *= parseFloat(`${3 ^ settingsu.length}`);
         libavutilK >>= Math.min(1, Math.abs(libavutilK));
         modea = `${defaultteamB.length ^ 3}`;
         defaultteamB = `${libavutilK}`;
       let matchinactiveN = String.fromCharCode(122,101,114,111,122,101,114,111,95,120,95,50,51,0);
       let androidg = String.fromCharCode(111,103,103,118,111,114,98,105,115,95,109,95,56,54,0);
      if (!mintegraln.includes(`${vietnamA.length}`)) {
         vietnamA = [3];
      }
      let iconbellZ = settingsu.length <= 8288456;
      do {
          let basketballdetailsbgM: Array<any> = [395, 54];
          let iconsetting4 = 2;
          let reactnativejsV = 2.0;
         settingsu = "1";
         basketballdetailsbgM = [basketballdetailsbgM.length];
         iconsetting4 *= iconsetting4 / (Math.max(basketballdetailsbgM.length, 9));
         reactnativejsV *= parseFloat(`${basketballdetailsbgM.length / (Math.max(5, parseInt(`${reactnativejsV}`)))}`);
         if (iconbellZ) {
            break;
         }
      } while (iconbellZ && (!settingsu.startsWith(`${launchQ}`)));
      for (let f = 0; f < 3; f++) {
         settingsu += "1";
      }
       let themeO = String.fromCharCode(116,97,107,101,111,118,101,114,95,120,95,55,52,0);
      let heartP = 5313372 <= mintegraln.length;
      do {
          let footballfiledlayout0: Map<any, any> = new Map([[String.fromCharCode(103,95,54,49,95,115,117,98,102,114,97,109,101,115,0),false ], [String.fromCharCode(109,97,116,99,104,101,95,122,95,53,54,0),false ], [String.fromCharCode(110,95,48,95,99,111,110,115,116,97,110,116,0),true ]]);
          let goal9 = 5;
         mintegraln += `${footballfiledlayout0.size % 1}`;
         footballfiledlayout0 = new Map([[`${goal9}`, goal9 * 2]]);
         if (heartP) {
            break;
         }
      } while (heartP && (!mintegraln.endsWith(`${themeO.length}`)));
      if ((3 & mintegraln.length) == 1 || 5.66 == (launchQ / 5.70)) {
          let libavfiltern: Array<any> = [353, 813, 555];
          let iconsaveimageT: Array<any> = [String.fromCharCode(99,95,55,53,95,110,99,111,110,102,0), String.fromCharCode(115,95,55,57,95,102,97,99,101,115,0)];
         launchQ -= parseFloat(`${vietnamA.length}`);
         libavfiltern.push(iconsaveimageT.length + libavfiltern.length);
         iconsaveimageT = [3 | libavfiltern.length];
      }
         mintegraln += `${themeO.length << (Math.min(3, Math.abs(parseInt(`${launchQ}`))))}`;
      let iconarrowrightwhiteM = launchQ >= 8893510.0;
      do {
         launchQ -= parseFloat(`${androidg.length >> (Math.min(1, vietnamA.length))}`);
         if (iconarrowrightwhiteM) {
            break;
         }
      } while (iconarrowrightwhiteM && ((matchinactiveN.length - parseInt(`${launchQ}`)) > 2 && (parseInt(`${launchQ}`) - matchinactiveN.length) > 2));
         launchQ += parseFloat(`${matchinactiveN.length ^ parseInt(`${launchQ}`)}`);
      if (3 >= vietnamA.length) {
         vietnamA.push(themeO.length);
      }
      cornerB = "1";
   }
   for (let c = 0; c < 1; c++) {
      libimagepipeline4 -= (cornerB.length ^ (countdownG ? 3 : 4));
   }
       let twitterD = String.fromCharCode(104,119,102,114,97,109,101,95,53,95,50,52,0);
         twitterD += `${3 * twitterD.length}`;
         twitterD += `${twitterD.length}`;
         twitterD += `${(twitterD == String.fromCharCode(118,0) ? twitterD.length : twitterD.length)}`;
      cornerB = `${cornerB.length & parseInt(`${libimagepipeline4}`)}`;
   let pauseX = kuaishouA.length <= 7436246;
   do {
       let wifirouter2 = 2;
       let reactnativeratingsR = 3.0;
       let fastM = 1;
         reactnativeratingsR /= Math.max(2, parseInt(`${reactnativeratingsR}`));
       let fastz: Map<any, any> = new Map([[String.fromCharCode(102,95,53,48,95,105,110,100,101,102,105,110,105,116,101,0),false ], [String.fromCharCode(97,99,99,114,117,101,95,120,95,54,51,0),true ], [String.fromCharCode(98,95,54,55,95,101,110,117,109,101,114,97,116,101,0),true ]]);
       let dragcloseQ: Map<any, any> = new Map([[String.fromCharCode(102,95,57,56,95,105,110,116,102,114,0),true ], [String.fromCharCode(117,109,98,114,101,108,108,97,95,104,95,50,56,0),true ]]);
         reactnativeratingsR /= Math.max(4, fastM * fastz.size);
      if ((reactnativeratingsR / (Math.max(2.42, 5))) <= 3.74) {
         reactnativeratingsR += wifirouter2 + 1;
      }
      for (let d = 0; d < 2; d++) {
         wifirouter2 *= parseInt(`${reactnativeratingsR}`);
      }
       let matchdetailbgb = 3.0;
         reactnativeratingsR -= wifirouter2;
      for (let d = 0; d < 1; d++) {
         dragcloseQ.set(`${matchdetailbgb}`, dragcloseQ.size);
      }
         fastM ^= wifirouter2 / (Math.max(5, parseInt(`${matchdetailbgb}`)));
      kuaishouA = [3];
      if (pauseX) {
         break;
      }
   } while ((inouttargetyellowe.includes(kuaishouA.length)) && pauseX);
   let iconpipshrinks = 9107660.0 <= libimagepipeline4;
   do {
      libimagepipeline4 += ((countdownG ? 3 : 5));
      if (iconpipshrinks) {
         break;
      }
   } while ((cornerB.length == 4) && iconpipshrinks);
   if (2 == (3 | inouttargetyellowe.length) || !countdownG) {
      countdownG = !countdownG;
   }
   let gemfileE = 5521642 >= cornerB.length;
   do {
      cornerB = `${(String.fromCharCode(49,0) == gpayd ? defaultfootballbge.length : gpayd.length)}`;
      if (gemfileE) {
         break;
      }
   } while ((3 <= (inouttargetyellowe.length % (Math.max(1, 9)))) && gemfileE);
      inouttargetyellowe.push(3 << (Math.min(5, Math.abs(baiduI.size))));
   while ((baiduI.size * 5) < 3 || (baiduI.size * 5) < 3) {
       let selectedM = 0.0;
       let pingU = 2.0;
       let graphicsr = 2.0;
       let commony = String.fromCharCode(104,109,97,99,105,100,95,99,95,55,53,0);
       let infoJ = 0;
         infoJ <<= Math.min(Math.abs(infoJ), 3);
         graphicsr /= Math.max(1, parseFloat(`${infoJ}`));
       let commentz = String.fromCharCode(100,95,50,51,95,109,111,100,105,102,105,99,97,116,105,111,110,115,0);
       let libhermesH = String.fromCharCode(119,95,54,53,95,115,117,98,98,108,111,99,107,115,0);
       let dataJ = String.fromCharCode(105,95,57,53,95,114,101,99,97,108,99,117,108,97,116,101,0);
      while (graphicsr < 2.36) {
          let rulesK = String.fromCharCode(106,95,56,57,95,99,97,99,104,101,0);
          let gestureT = String.fromCharCode(119,117,110,100,101,102,95,49,95,54,52,0);
          let baseV = String.fromCharCode(113,95,50,51,95,99,111,110,116,114,97,105,110,101,100,0);
          let sigmobr = String.fromCharCode(97,100,97,112,116,97,116,105,111,110,115,101,116,95,50,95,56,55,0);
          let telegramN: Map<any, any> = new Map([[String.fromCharCode(119,95,49,57,95,119,109,97,118,111,105,99,101,0),164], [String.fromCharCode(99,111,110,102,105,103,117,114,97,116,105,111,110,95,101,95,54,55,0),262]]);
         graphicsr /= Math.max((parseFloat(`${dataJ == String.fromCharCode(82,0) ? baseV.length : dataJ.length}`)), 3);
         rulesK += `${telegramN.size}`;
         gestureT += "2";
         baseV = `${gestureT.length}`;
         sigmobr = `${(sigmobr == String.fromCharCode(117,0) ? sigmobr.length : rulesK.length)}`;
         telegramN.set(`${sigmobr}`, sigmobr.length / (Math.max(1, telegramN.size)));
         break;
      }
      for (let y = 0; y < 2; y++) {
         commony += `${parseInt(`${pingU}`)}`;
      }
      if (!commony.startsWith(`${graphicsr}`)) {
          let sinay = String.fromCharCode(100,101,112,97,114,116,109,101,110,116,95,104,95,57,48,0);
          let long_uZ: Map<any, any> = new Map([[String.fromCharCode(118,95,49,51,95,109,99,111,114,101,0),773], [String.fromCharCode(122,95,52,49,95,109,112,101,103,100,97,116,97,0),513]]);
         commony += `${3 >> (Math.min(2, Math.abs(parseInt(`${selectedM}`))))}`;
         sinay = `${sinay.length}`;
         long_uZ.set(`${sinay}`, long_uZ.size);
      }
         infoJ <<= Math.min(Math.abs(1 << (Math.min(3, Math.abs(infoJ)))), 5);
      if (libhermesH.includes(`${commentz.length}`)) {
         libhermesH = `${2 << (Math.min(2, Math.abs(parseInt(`${graphicsr}`))))}`;
      }
      if ((pingU * parseFloat(`${commentz.length}`)) <= 3.3) {
         pingU /= Math.max(parseFloat(`${parseInt(`${selectedM}`)}`), 2);
      }
      for (let g = 0; g < 3; g++) {
          let upgradeO: Map<any, any> = new Map([[String.fromCharCode(115,117,98,98,108,111,99,107,115,95,111,95,55,57,0),429], [String.fromCharCode(97,95,55,52,95,119,97,107,101,0),543], [String.fromCharCode(112,97,105,114,119,105,115,101,95,119,95,54,49,0),970]]);
          let clubn = 5.0;
          let shrink7 = String.fromCharCode(109,101,116,104,111,100,95,55,95,50,0);
         graphicsr *= parseFloat(`${libhermesH.length}`);
         upgradeO.set(shrink7, parseInt(`${clubn}`) % (Math.max(10, shrink7.length)));
         clubn -= 2;
      }
      let configR = 6988433.0 >= pingU;
      do {
          let ccopy_9B: Array<any> = [865, 942, 967];
          let downloadingX: Map<any, any> = new Map([[String.fromCharCode(109,95,52,56,95,118,105,101,119,0),true ], [String.fromCharCode(106,95,57,55,95,117,110,108,111,99,107,0),true ], [String.fromCharCode(122,95,52,50,95,100,101,105,110,116,0),true ]]);
          let helperT = 1.0;
         pingU -= parseFloat(`${parseInt(`${selectedM}`) % (Math.max(10, parseInt(`${pingU}`)))}`);
         ccopy_9B = [parseInt(`${helperT}`) << (Math.min(ccopy_9B.length, 4))];
         downloadingX.set(`${helperT}`, downloadingX.size);
         if (configR) {
            break;
         }
      } while (((pingU * 5.26) < 1.36) && configR);
         selectedM *= commentz.length;
      if ((selectedM * libhermesH.length) == 2.53 && (4 * libhermesH.length) == 2) {
         selectedM -= dataJ.length;
      }
         selectedM *= infoJ;
      baiduI = new Map([[`${inouttargetyellowe.length}`, parseInt(`${selectedM}`) & 3]]);
      break;
   }

    setIsDialogOpen(!isDialogOpen);
  };

  const spamToggleB = () => {
       let bgvipsport5 = String.fromCharCode(110,95,50,51,95,115,97,118,101,0);
    let tempnodataY = String.fromCharCode(100,105,115,112,111,115,105,116,105,111,110,95,112,95,57,48,0);
    let rewardN = String.fromCharCode(97,95,52,54,95,97,100,111,98,101,0);
    let unimplementedviewp = String.fromCharCode(108,105,110,107,105,110,103,95,100,95,54,50,0);
    let showC: Array<any> = [String.fromCharCode(112,114,101,109,105,117,109,95,109,95,50,50,0), String.fromCharCode(121,95,49,95,114,101,102,99,111,117,110,116,0)];
    let iconwatchnowU: Array<any> = [String.fromCharCode(113,95,55,95,99,108,97,115,115,110,97,109,101,0), String.fromCharCode(115,101,113,110,111,95,105,95,49,51,0), String.fromCharCode(115,99,116,112,117,116,105,108,95,115,95,50,54,0)];
    let b_position8: Array<any> = [906, 269, 413];
    let modelsg = 1.0;
    let liveZ: Map<any, any> = new Map([[String.fromCharCode(115,117,98,116,121,112,101,115,95,102,95,49,54,0),525], [String.fromCharCode(105,110,116,101,114,112,111,108,97,116,111,114,95,51,95,49,48,0),676]]);
    let iconqqN: Array<any> = [11, 986];
    let unreadH = false;
    let controlse = String.fromCharCode(99,108,101,97,110,101,100,95,107,95,56,57,0);
    let iconarrowrightorangeB = 4;
    let othermatchdetailbgk = 1.0;
   if (2.31 >= (4.72 / (Math.max(9, modelsg))) || (modelsg / (Math.max(parseFloat(`${unimplementedviewp.length}`), 9))) >= 4.72) {
      unimplementedviewp = `${b_position8.length}`;
   }
      rewardN += `${(rewardN == String.fromCharCode(104,0) ? rewardN.length : iconqqN.length)}`;
   let statsB = 5392189 >= iconwatchnowU.length;
   do {
      iconwatchnowU = [((unreadH ? 4 : 1) ^ 3)];
      if (statsB) {
         break;
      }
   } while (statsB && (3 < (b_position8.length << (Math.min(Math.abs(5), 4))) || 5 < (b_position8.length << (Math.min(4, iconwatchnowU.length)))));
      unreadH = showC.length > bgvipsport5.length;
   for (let r = 0; r < 1; r++) {
      unimplementedviewp += `${2 ^ liveZ.size}`;
   }
   for (let v = 0; v < 3; v++) {
      unimplementedviewp += `${(iconqqN.length - (unreadH ? 5 : 2))}`;
   }
       let yellowscoreballH = String.fromCharCode(115,99,114,111,108,108,97,98,108,101,95,53,95,50,48,0);
       let rncore7 = 2.0;
       let placeholderw = true;
          let libswscalec = String.fromCharCode(101,95,49,55,95,102,105,116,122,0);
          let vignetteP = 5;
          let popup4 = false;
         rncore7 -= parseFloat(`${parseInt(`${rncore7}`)}`);
         libswscalec += `${(vignetteP - (popup4 ? 1 : 1))}`;
         vignetteP ^= vignetteP;
         popup4 = libswscalec.startsWith(`${vignetteP}`);
          let hookz = String.fromCharCode(116,114,97,110,115,108,97,116,101,100,95,48,95,54,57,0);
          let trophyO = 1.0;
          let valuesK = true;
         yellowscoreballH = `${((placeholderw ? 2 : 1) / 2)}`;
         hookz += `${(String.fromCharCode(51,0) == hookz ? parseInt(`${trophyO}`) : hookz.length)}`;
         trophyO -= (parseFloat(`${hookz == String.fromCharCode(119,0) ? hookz.length : parseInt(`${trophyO}`)}`));
         valuesK = trophyO > 17.39 || !valuesK;
      while (parseInt(`${rncore7}`) > yellowscoreballH.length) {
          let maths = 3.0;
         yellowscoreballH = `${parseInt(`${maths}`) % (Math.max(yellowscoreballH.length, 8))}`;
         break;
      }
      let redirectB = String.fromCharCode(115,49,54,118,108,103,110,55,104,107,0) == yellowscoreballH;
      do {
         yellowscoreballH = `${(String.fromCharCode(81,0) == yellowscoreballH ? (placeholderw ? 4 : 1) : yellowscoreballH.length)}`;
         if (redirectB) {
            break;
         }
      } while (redirectB && (2.28 > (rncore7 * parseFloat(`${yellowscoreballH.length}`)) && (2.28 * rncore7) > 4.78));
          let tickedh = 4.0;
          let applicationa = String.fromCharCode(108,95,56,95,98,108,111,119,102,105,115,104,0);
         yellowscoreballH += `${(String.fromCharCode(101,0) == yellowscoreballH ? parseInt(`${rncore7}`) : yellowscoreballH.length)}`;
         tickedh /= Math.max(parseInt(`${tickedh}`) / (Math.max(applicationa.length, 9)), 5);
         applicationa += `${(String.fromCharCode(88,0) == applicationa ? parseInt(`${tickedh}`) : applicationa.length)}`;
      for (let l = 0; l < 1; l++) {
         rncore7 += parseFloat(`${parseInt(`${rncore7}`)}`);
      }
         placeholderw = !yellowscoreballH.startsWith(`${rncore7}`);
      let libfabricjni8 = 8623386.0 >= rncore7;
      do {
          let basketballplayerplaceholderj = 2;
         rncore7 /= Math.max((parseFloat(`${(placeholderw ? 3 : 3) << (Math.min(Math.abs(parseInt(`${rncore7}`)), 2))}`)), 5);
         basketballplayerplaceholderj <<= Math.min(4, Math.abs(basketballplayerplaceholderj));
         if (libfabricjni8) {
            break;
         }
      } while ((yellowscoreballH.length <= parseInt(`${rncore7}`)) && libfabricjni8);
      let disconnecteds = yellowscoreballH == String.fromCharCode(113,53,49,57,107,117,56,55,105,0);
      do {
         yellowscoreballH = `${1 * parseInt(`${rncore7}`)}`;
         if (disconnecteds) {
            break;
         }
      } while ((2 > (2 - yellowscoreballH.length)) && disconnecteds);
      liveZ = new Map([[bgvipsport5, (bgvipsport5.length << (Math.min(5, Math.abs((unreadH ? 1 : 1)))))]]);
      b_position8 = [2 | iconwatchnowU.length];
   for (let q = 0; q < 2; q++) {
       let icondownimgC = 5;
       let iconpipshrinkZ = 5;
       let heartF = false;
      while (heartF || 1 < (1 >> (Math.min(1, Math.abs(icondownimgC))))) {
          let championr: Array<any> = [127, 770];
          let phoneL = String.fromCharCode(98,108,105,110,107,95,100,95,54,0);
         heartF = icondownimgC < 31;
         championr = [championr.length | phoneL.length];
         phoneL = `${(phoneL == String.fromCharCode(50,0) ? phoneL.length : championr.length)}`;
         break;
      }
          let iconbellactiveu = 3;
          let libruntimeexecutora = true;
         iconpipshrinkZ >>= Math.min(3, parseInt(`${Math.abs((iconpipshrinkZ / (Math.max(6, (heartF ? 4 : 5)))))}`));
         iconbellactiveu ^= 1;
         libruntimeexecutora = !libruntimeexecutora || 82 < iconbellactiveu;
       let arrowupp = String.fromCharCode(113,95,54,56,95,101,120,116,101,116,110,100,101,100,0);
         heartF = arrowupp == String.fromCharCode(84,0);
         heartF = 17 >= (iconpipshrinkZ | icondownimgC);
      while (3 >= arrowupp.length && !heartF) {
         heartF = icondownimgC > arrowupp.length;
         break;
      }
         iconpipshrinkZ /= Math.max(arrowupp.length ^ icondownimgC, 5);
         iconpipshrinkZ <<= Math.min(Math.abs(iconpipshrinkZ << (Math.min(1, Math.abs(icondownimgC)))), 2);
         iconpipshrinkZ &= 2 - icondownimgC;
      bgvipsport5 = `${iconwatchnowU.length}`;
   }
   if (unreadH || unimplementedviewp.length > 1) {
       let buttonO = 3;
       let gray3 = String.fromCharCode(99,95,55,57,95,98,115,119,97,112,100,115,112,0);
       let moden = String.fromCharCode(111,110,116,111,95,49,95,49,55,0);
      let homeL = moden == String.fromCharCode(107,113,112,109,54,99,49,116,0);
      do {
         moden = `${moden.length & 2}`;
         if (homeL) {
            break;
         }
      } while (((buttonO | moden.length) >= 3) && homeL);
         moden += `${gray3.length}`;
       let i_positionr: Array<any> = [String.fromCharCode(117,95,55,48,95,99,114,111,115,115,102,97,100,101,0), String.fromCharCode(107,95,57,53,95,100,101,110,111,114,109,97,108,105,122,101,0)];
         gray3 = "2";
      while (1 < gray3.length) {
         buttonO &= gray3.length;
         break;
      }
      if ((moden.length ^ 4) <= 3) {
         moden += `${i_positionr.length}`;
      }
      if (3 >= moden.length) {
         moden = "3";
      }
      while (5 < (moden.length | buttonO)) {
          let adultF = 3.0;
          let libfbjnik = String.fromCharCode(104,95,52,53,95,99,108,117,115,116,101,114,115,0);
          let league5: Map<any, any> = new Map([[String.fromCharCode(115,95,49,55,95,112,97,103,105,110,103,0),459], [String.fromCharCode(103,95,57,52,95,105,109,109,101,100,105,97,116,101,115,105,103,110,97,108,0),166], [String.fromCharCode(110,111,99,111,108,115,101,116,95,106,95,49,54,0),628]]);
         buttonO |= 1;
         adultF -= parseFloat(`${libfbjnik.length}`);
         libfbjnik = `${1 << (Math.min(3, Math.abs(parseInt(`${adultF}`))))}`;
         league5 = new Map([[`${league5.size}`, 2 >> (Math.min(3, libfbjnik.length))]]);
         break;
      }
      for (let q = 0; q < 1; q++) {
          let stepQ = String.fromCharCode(109,95,53,51,95,113,117,101,114,121,0);
          let zhuboD = 0;
          let profileactivem = String.fromCharCode(115,117,112,101,114,110,111,100,101,115,95,49,95,49,53,0);
          let readX = 4;
          let zhengpianQ = 3.0;
         i_positionr.push(buttonO << (Math.min(3, Math.abs(zhuboD))));
         stepQ += `${profileactivem.length | 3}`;
         zhuboD >>= Math.min(5, Math.abs(profileactivem.length - 3));
         readX += parseInt(`${zhengpianQ}`) & profileactivem.length;
         zhengpianQ *= parseFloat(`${1 / (Math.max(9, parseInt(`${zhengpianQ}`)))}`);
      }
      unimplementedviewp = `${tempnodataY.length | rewardN.length}`;
   }
       let membershipd = 4;
       let shrunk7 = String.fromCharCode(98,95,52,51,95,99,97,108,108,98,97,99,107,115,0);
      if (shrunk7.length > 2) {
          let downloadingy = String.fromCharCode(112,104,111,116,111,95,48,95,55,57,0);
          let graph5 = String.fromCharCode(103,97,117,103,101,95,117,95,50,48,0);
          let fnewarchdefaultsU = String.fromCharCode(101,95,52,51,95,111,119,110,101,114,0);
         membershipd <<= Math.min(5, Math.abs(membershipd));
         downloadingy += `${fnewarchdefaultsU.length & graph5.length}`;
         graph5 = `${(downloadingy == String.fromCharCode(86,0) ? downloadingy.length : fnewarchdefaultsU.length)}`;
      }
      let countryH = membershipd <= 8586771;
      do {
         membershipd <<= Math.min(4, Math.abs((shrunk7 == String.fromCharCode(99,0) ? shrunk7.length : membershipd)));
         if (countryH) {
            break;
         }
      } while (countryH && ((membershipd * shrunk7.length) >= 1 || (membershipd * 1) >= 3));
      let positionfieldz = membershipd <= 5903243;
      do {
          let embed_ = String.fromCharCode(116,95,49,48,95,114,101,109,117,120,0);
         membershipd <<= Math.min(Math.abs(3 | shrunk7.length), 2);
         embed_ = `${1 | embed_.length}`;
         if (positionfieldz) {
            break;
         }
      } while (((3 * membershipd) <= 1 || (shrunk7.length * 3) <= 1) && positionfieldz);
         membershipd >>= Math.min(3, Math.abs(membershipd | shrunk7.length));
         shrunk7 = `${(String.fromCharCode(75,0) == shrunk7 ? membershipd : shrunk7.length)}`;
      let minivodD = 7551198 <= membershipd;
      do {
          let classesP: Array<any> = [String.fromCharCode(115,97,108,115,97,95,53,95,50,51,0), String.fromCharCode(114,95,49,56,95,112,105,99,107,101,114,115,0), String.fromCharCode(103,95,53,54,95,98,121,116,101,99,111,100,101,0)];
          let targeto = 4.0;
         membershipd -= membershipd + 3;
         classesP = [parseInt(`${targeto}`)];
         targeto -= parseFloat(`${classesP.length}`);
         if (minivodD) {
            break;
         }
      } while (((membershipd / 3) < 4 || 2 < (shrunk7.length / (Math.max(3, 6)))) && minivodD);
      iconwatchnowU = [liveZ.size / 2];
      bgvipsport5 = `${(parseInt(`${modelsg}`) & (unreadH ? 4 : 3))}`;
   for (let i = 0; i < 2; i++) {
      iconqqN = [3];
   }
       let control3: Array<any> = [677, 8];
         control3.push(2);
         control3.push(1 * control3.length);
      if (5 < control3.length) {
         control3 = [2];
      }
      bgvipsport5 = `${controlse.length}`;
   let resendi = unreadH ? !unreadH : unreadH;
   do {
      unreadH = showC.includes(unreadH);
      if (resendi) {
         break;
      }
   } while (((5 | iconwatchnowU.length) > 1) && resendi);
   let stepo = liveZ.size <= 4916487;
   do {
      liveZ = new Map([[`${showC.length}`, rewardN.length]]);
      if (stepo) {
         break;
      }
   } while (stepo && (!controlse.startsWith(`${liveZ.size}`)));
       let attributedstrings = 4.0;
       let showlessG: Map<any, any> = new Map([[String.fromCharCode(105,122,101,114,111,95,51,95,51,54,0),849], [String.fromCharCode(102,95,57,95,111,112,117,115,0),834], [String.fromCharCode(100,95,57,95,112,111,110,103,0),884]]);
       let foreground0: Array<any> = [685, 566, 553];
       let libbufferJ = String.fromCharCode(116,95,57,49,95,111,118,112,97,103,101,0);
       let orangedownarrow4 = String.fromCharCode(113,95,50,52,95,103,108,105,116,99,104,0);
         orangedownarrow4 += `${foreground0.length}`;
         showlessG = new Map([[`${foreground0.length}`, foreground0.length >> (Math.min(Math.abs(3), 3))]]);
         orangedownarrow4 += `${foreground0.length << (Math.min(Math.abs(3), 4))}`;
      for (let o = 0; o < 3; o++) {
          let dependencys = String.fromCharCode(118,111,112,101,110,95,101,95,52,50,0);
         attributedstrings /= Math.max(1, parseFloat(`${3}`));
         dependencys = `${1 << (Math.min(2, dependencys.length))}`;
      }
          let playershirtY = 2;
         showlessG = new Map([[`${showlessG.size}`, 3 << (Math.min(4, Math.abs(parseInt(`${attributedstrings}`))))]]);
         playershirtY *= playershirtY / (Math.max(4, playershirtY));
      for (let m = 0; m < 2; m++) {
         showlessG.set(`${foreground0.length}`, foreground0.length);
      }
      while (foreground0.length >= 3) {
         foreground0.push(3 ^ orangedownarrow4.length);
         break;
      }
         libbufferJ = `${libbufferJ.length % 3}`;
      unreadH = (25 == (unimplementedviewp.length + (unreadH ? 25 : unimplementedviewp.length)));
       let libjsijniprofilere = String.fromCharCode(118,101,114,108,97,121,95,104,95,54,51,0);
      if (libjsijniprofilere.length <= 5 && 5 <= libjsijniprofilere.length) {
         libjsijniprofilere += `${libjsijniprofilere.length}`;
      }
       let confirmationV: Map<any, any> = new Map([[String.fromCharCode(116,105,109,101,108,105,110,101,115,95,98,95,52,55,0),980], [String.fromCharCode(113,115,99,97,108,101,113,108,111,103,95,52,95,57,56,0),349]]);
       let giflivestreamingK: Map<any, any> = new Map([[String.fromCharCode(105,95,54,48,95,117,110,108,111,99,107,0),true ], [String.fromCharCode(97,116,116,101,109,112,116,115,95,99,95,55,0),false ], [String.fromCharCode(97,108,112,104,97,101,120,116,114,97,99,116,95,99,95,50,54,0),true ]]);
         libjsijniprofilere += "3";
      modelsg += parseFloat(`${tempnodataY.length + parseInt(`${modelsg}`)}`);
       let animationsL = String.fromCharCode(105,95,50,50,95,118,109,100,118,105,100,101,111,0);
       let combineS = true;
       let minin: Map<any, any> = new Map([[String.fromCharCode(108,111,99,97,108,116,105,109,101,95,48,95,53,56,0),true ], [String.fromCharCode(99,97,112,116,117,114,101,95,49,95,48,0),true ], [String.fromCharCode(97,97,117,100,105,111,95,98,95,49,49,0),true ]]);
       let moduleF: Map<any, any> = new Map([[String.fromCharCode(103,95,53,50,95,100,101,112,111,115,105,116,0),String.fromCharCode(100,95,51,57,95,98,111,111,116,115,116,114,97,112,0)], [String.fromCharCode(103,95,49,55,95,99,111,110,116,101,110,116,105,111,110,0),String.fromCharCode(111,95,50,54,95,108,105,98,115,104,105,110,101,0)], [String.fromCharCode(103,114,97,121,95,49,95,54,52,0),String.fromCharCode(103,95,57,49,95,100,101,99,111,114,114,0)]]);
      if ((moduleF.size ^ minin.size) >= 5 && (moduleF.size ^ minin.size) >= 5) {
         moduleF = new Map([[`${moduleF.size}`, 2]]);
      }
      for (let q = 0; q < 3; q++) {
         moduleF.set(animationsL, animationsL.length);
      }
      if ((5 ^ minin.size) > 5 && !combineS) {
         combineS = minin.size <= 47;
      }
      while (3 > (minin.size + 4) || minin.size > 4) {
         combineS = animationsL.length >= 31;
         break;
      }
         animationsL += `${animationsL.length}`;
      controlse += `${parseInt(`${modelsg}`)}`;
      bgvipsport5 += `${tempnodataY.length}`;
      unreadH = (b_position8.length >> (Math.min(controlse.length, 3))) > 36;
       let mbridgei = String.fromCharCode(119,95,50,48,95,102,111,114,0);
         mbridgei = `${(mbridgei == String.fromCharCode(120,0) ? mbridgei.length : mbridgei.length)}`;
       let shrinkq: Map<any, any> = new Map([[String.fromCharCode(110,116,115,115,95,50,95,50,0),16], [String.fromCharCode(102,95,50,0),558]]);
       let basketballhometeamT: Map<any, any> = new Map([[String.fromCharCode(109,101,115,104,95,107,95,53,0),970], [String.fromCharCode(114,95,52,49,95,98,105,108,97,116,101,114,97,108,0),453], [String.fromCharCode(117,95,53,53,95,108,97,122,121,0),782]]);
      for (let a = 0; a < 1; a++) {
          let shootnogoalX = 0;
          let coded = 1.0;
         shrinkq.set(`${mbridgei}`, shrinkq.size * 2);
         shootnogoalX /= Math.max(3, shootnogoalX);
         coded /= Math.max(parseFloat(`${parseInt(`${coded}`)}`), 1);
      }
      tempnodataY += `${parseInt(`${modelsg}`) << (Math.min(Math.abs(2), 4))}`;

    setCountToggleB(countToggleB + 1);
  }

  const switchToggle = async () => {
       let reactnativeratingsm = 0;
    let livesharek = String.fromCharCode(114,105,110,103,105,110,103,95,114,95,49,52,0);
    let iconsetting6: Array<any> = [588, 657];
    let showmorey: Array<any> = [4, 280];
    let libtobO: Array<any> = [String.fromCharCode(109,111,118,101,116,111,95,109,95,49,54,0), String.fromCharCode(108,97,121,101,114,115,95,100,95,53,51,0)];
    let overG = 3.0;
    let cornerkick5: Map<any, any> = new Map([[String.fromCharCode(116,95,57,56,95,103,97,105,110,99,0),String.fromCharCode(102,111,117,110,100,95,56,95,53,48,0)], [String.fromCharCode(97,95,56,95,99,104,101,99,107,109,97,114,107,115,0),String.fromCharCode(99,95,54,48,95,99,111,110,116,105,110,101,110,116,0)]]);
    let iconpipexpandT = String.fromCharCode(114,101,108,99,116,120,95,106,95,55,0);
    let scrollviewt = String.fromCharCode(122,111,109,98,105,101,95,110,95,51,57,0);
    let iconorientationY = String.fromCharCode(100,101,97,99,116,95,102,95,50,51,0);
    let sportO: Map<any, any> = new Map([[String.fromCharCode(115,108,97,115,104,95,106,95,57,52,0),String.fromCharCode(111,111,117,114,97,95,121,95,54,53,0)], [String.fromCharCode(112,114,101,100,105,99,116,105,111,110,95,57,95,52,54,0),String.fromCharCode(97,112,112,114,101,99,97,116,105,111,110,95,54,95,49,55,0)]]);
    let launch_ = String.fromCharCode(114,104,115,95,113,95,48,0);
    let moduleB = String.fromCharCode(109,97,116,105,99,95,109,95,56,52,0);
    let pauseV = String.fromCharCode(98,95,48,95,98,97,99,107,103,114,111,117,110,100,105,110,103,0);
    let scrollviewy = String.fromCharCode(115,116,97,110,100,97,114,100,105,122,101,95,104,95,55,56,0);
    let codegenW: Map<any, any> = new Map([[String.fromCharCode(115,95,52,53,95,101,97,115,105,110,103,0),String.fromCharCode(103,101,111,116,97,103,115,95,54,95,49,49,0)], [String.fromCharCode(100,101,102,105,110,105,116,105,111,110,95,106,95,53,52,0),String.fromCharCode(121,95,54,95,116,114,105,103,103,101,114,0)]]);
    let regengP: Map<any, any> = new Map([[String.fromCharCode(116,95,53,53,95,102,111,112,101,110,0),710], [String.fromCharCode(122,95,49,48,95,111,110,97,118,99,100,97,116,97,0),637]]);
    let skipn: Array<any> = [427, 978];
    let kuaishoun: Map<any, any> = new Map([[String.fromCharCode(99,97,115,101,100,95,111,95,50,48,0),false ], [String.fromCharCode(115,95,55,53,95,122,111,111,109,105,110,103,0),false ]]);
    let contextc = String.fromCharCode(115,116,105,114,110,103,95,52,95,50,56,0);
      showmorey.push(3 ^ libtobO.length);
      sportO.set(iconorientationY, 1);
   let shrunkz = livesharek.length >= 8993789;
   do {
       let verticaly = String.fromCharCode(115,117,112,112,114,101,115,115,111,114,95,119,95,56,55,0);
       let airbnbstarselected9 = 4.0;
       let yellowredcardM = 1.0;
      for (let h = 0; h < 2; h++) {
         yellowredcardM -= parseFloat(`${parseInt(`${airbnbstarselected9}`)}`);
      }
      if (3.96 >= (yellowredcardM + airbnbstarselected9)) {
          let mergerk = 0;
         airbnbstarselected9 -= 3;
         mergerk &= 2;
      }
      if (yellowredcardM == airbnbstarselected9) {
         airbnbstarselected9 *= verticaly.length + parseInt(`${yellowredcardM}`);
      }
          let audiencen = String.fromCharCode(112,98,107,100,102,95,107,95,54,51,0);
          let themeb = 2.0;
          let cnewarchdefaultse: Array<any> = [761, 703, 101];
         airbnbstarselected9 -= (audiencen == String.fromCharCode(110,0) ? audiencen.length : parseInt(`${themeb}`));
         themeb += parseFloat(`${cnewarchdefaultse.length - 2}`);
          let iconpointscoreF = 1.0;
         yellowredcardM /= Math.max(parseFloat(`${verticaly.length * parseInt(`${iconpointscoreF}`)}`), 4);
      let greenarrowupj = yellowredcardM <= 8535062.0;
      do {
          let iconsubssuccess2 = String.fromCharCode(109,95,55,52,95,99,111,110,118,0);
          let sourceI = String.fromCharCode(121,95,51,53,95,97,117,116,111,99,111,109,109,105,116,0);
          let iconpointscoreW = 4;
          let rocketw: Array<any> = [String.fromCharCode(109,95,49,48,95,104,97,100,97,109,97,114,100,120,0), String.fromCharCode(102,95,56,95,117,110,122,116,101,108,108,0)];
          let recommendationi = String.fromCharCode(112,111,110,103,95,112,95,54,49,0);
         yellowredcardM *= parseFloat(`${3}`);
         iconsubssuccess2 += "3";
         sourceI = `${iconsubssuccess2.length}`;
         iconpointscoreW -= recommendationi.length;
         rocketw = [iconpointscoreW - 3];
         recommendationi = `${sourceI.length / (Math.max(1, 8))}`;
         if (greenarrowupj) {
            break;
         }
      } while (greenarrowupj && ((parseInt(`${yellowredcardM}`) / (Math.max(verticaly.length, 9))) >= 5 || 1 >= (5 * verticaly.length)));
          let upgradeg = String.fromCharCode(112,101,101,114,95,106,95,51,50,0);
          let fillL = 5.0;
          let kuaishouw = String.fromCharCode(101,109,117,101,100,103,101,95,100,95,54,52,0);
         verticaly = `${parseInt(`${fillL}`) / 3}`;
         upgradeg = `${(upgradeg == String.fromCharCode(56,0) ? upgradeg.length : kuaishouw.length)}`;
         fillL *= upgradeg.length;
         kuaishouw += `${kuaishouw.length / (Math.max(upgradeg.length, 5))}`;
      let shareds = 9206753.0 <= yellowredcardM;
      do {
         yellowredcardM += parseFloat(`${parseInt(`${airbnbstarselected9}`) / 1}`);
         if (shareds) {
            break;
         }
      } while ((1.79 >= airbnbstarselected9) && shareds);
       let chartj = 2.0;
      livesharek = "2";
      if (shrunkz) {
         break;
      }
   } while ((iconsetting6.length == livesharek.length) && shrunkz);
   while ((libtobO.length & livesharek.length) <= 5 || (libtobO.length & 5) <= 2) {
      libtobO.push(livesharek.length ^ 3);
      break;
   }
       let humidity7 = String.fromCharCode(119,95,57,95,109,97,106,0);
       let guide8 = true;
       let policyW = 1.0;
         policyW /= Math.max(1, parseFloat(`${parseInt(`${policyW}`) * 3}`));
         guide8 = (humidity7.length - parseInt(`${policyW}`)) > 79;
         guide8 = policyW >= 53.34 && guide8;
         guide8 = (96 == ((guide8 ? humidity7.length : 96) >> (Math.min(humidity7.length, 3))));
         humidity7 += `${((guide8 ? 5 : 1) % (Math.max(humidity7.length, 7)))}`;
          let renewS = 0.0;
          let thumbnailN = String.fromCharCode(115,95,50,51,95,105,112,104,111,110,101,0);
         humidity7 += `${parseInt(`${policyW}`) * parseInt(`${renewS}`)}`;
         renewS += thumbnailN.length % (Math.max(3, thumbnailN.length));
          let notificationgrayK = false;
          let middlesoundj = String.fromCharCode(117,108,116,105,95,120,95,49,54,0);
         humidity7 = `${parseInt(`${policyW}`) / 1}`;
         notificationgrayK = (19 >= (middlesoundj.length | (!notificationgrayK ? 19 : middlesoundj.length)));
      while (humidity7.startsWith(`${policyW}`)) {
         policyW *= parseFloat(`${humidity7.length}`);
         break;
      }
         guide8 = String.fromCharCode(86,0) == humidity7 || 65.50 >= policyW;
      showmorey.push(1 & libtobO.length);
   for (let q = 0; q < 3; q++) {
       let modulesx = true;
       let graym = 2.0;
       let liveshareQ = 2.0;
      if (5.74 < (2.65 * liveshareQ) && 2.65 < liveshareQ) {
          let suboutr = 2;
          let modulese = 2.0;
          let stepc = 2;
         liveshareQ -= parseFloat(`${parseInt(`${modulese}`) >> (Math.min(5, Math.abs(stepc)))}`);
         suboutr /= Math.max(3, suboutr / 2);
         modulese /= Math.max(4, 1);
      }
          let connectionv: Array<any> = [45, 147];
          let runtimec = String.fromCharCode(110,95,52,52,95,105,110,116,101,114,108,101,97,118,105,110,103,0);
          let e_lockA: Map<any, any> = new Map([[String.fromCharCode(120,95,51,51,95,101,100,105,116,97,98,108,101,0),String.fromCharCode(115,95,52,50,95,116,101,99,104,110,111,108,111,103,121,0)], [String.fromCharCode(115,101,110,100,116,111,95,105,95,53,54,0),String.fromCharCode(110,95,50,54,95,105,100,101,109,112,111,116,101,110,99,121,0)], [String.fromCharCode(116,95,55,51,95,114,97,110,100,111,109,0),String.fromCharCode(116,95,51,56,95,100,97,114,107,0)]]);
         liveshareQ += parseFloat(`${2 | e_lockA.size}`);
         connectionv.push(1);
         runtimec = `${1 >> (Math.min(4, connectionv.length))}`;
         e_lockA.set(runtimec, connectionv.length & runtimec.length);
      cornerkick5.set(iconorientationY, (iconorientationY == String.fromCharCode(100,0) ? iconorientationY.length : iconpipexpandT.length));
      modulesx = (modulesx ? modulesx : modulesx);
   }
      iconsetting6 = [reactnativeratingsm << (Math.min(iconsetting6.length, 2))];
       let homeplayern: Array<any> = [933, 207];
       let playK: Map<any, any> = new Map([[String.fromCharCode(122,95,52,54,95,112,101,114,102,111,114,109,97,110,99,101,0),336], [String.fromCharCode(110,101,101,100,101,100,95,55,95,49,0),512]]);
      for (let u = 0; u < 1; u++) {
         playK.set(`${homeplayern.length}`, playK.size & homeplayern.length);
      }
          let select0: Array<any> = [276, 878, 994];
          let defaultprofilepicP: Map<any, any> = new Map([[String.fromCharCode(100,105,116,104,101,114,95,102,95,50,50,0),929], [String.fromCharCode(118,97,114,105,110,102,111,95,97,95,54,52,0),708]]);
         playK = new Map([[`${select0.length}`, 2]]);
         select0.push(defaultprofilepicP.size);
         defaultprofilepicP.set(`${defaultprofilepicP.size}`, 2 & defaultprofilepicP.size);
          let backy: Array<any> = [192, 517, 319];
          let predictionarrow2 = String.fromCharCode(115,121,115,116,101,109,95,109,95,51,48,0);
         playK.set(`${homeplayern.length}`, 3);
         backy = [predictionarrow2.length & backy.length];
         predictionarrow2 += `${backy.length}`;
         homeplayern.push(1 + playK.size);
      if ((playK.size & 3) <= 4) {
         homeplayern.push(2);
      }
      if (!Array.from(playK.values()).includes(homeplayern.length)) {
         homeplayern = [playK.size];
      }
      iconsetting6.push(reactnativeratingsm);
   let bellreminderf = 5710347 <= sportO.size;
   do {
      sportO.set(iconorientationY, iconorientationY.length & showmorey.length);
      if (bellreminderf) {
         break;
      }
   } while ((4 >= (2 * sportO.size) || (sportO.size * scrollviewt.length) >= 2) && bellreminderf);
   let icondefaultthumbnail2 = 6459725 >= sportO.size;
   do {
       let renew3 = false;
       let loginh: Map<any, any> = new Map([[String.fromCharCode(113,95,50,55,95,98,105,108,97,116,101,114,97,108,0),685], [String.fromCharCode(108,111,119,101,114,95,108,95,54,54,0),455]]);
       let package_t7: Map<any, any> = new Map([[String.fromCharCode(101,110,116,114,105,101,115,95,102,95,55,55,0),741], [String.fromCharCode(102,97,114,109,101,95,99,95,50,51,0),356]]);
      while (3 > (1 - package_t7.size) && 5 > (loginh.size - 1)) {
          let philippinesn = String.fromCharCode(98,95,53,95,114,101,115,101,110,100,0);
          let sourcef = String.fromCharCode(102,95,49,52,95,101,110,99,111,100,101,100,115,116,114,101,97,109,0);
         package_t7 = new Map([[`${renew3}`, ((renew3 ? 1 : 4) & sourcef.length)]]);
         philippinesn += `${philippinesn.length / 2}`;
         sourcef += `${2 * philippinesn.length}`;
         break;
      }
       let playlistP = 4;
       let release_x_1 = 2;
      if (!renew3) {
         package_t7.set(`${release_x_1}`, release_x_1 ^ 1);
      }
      while (Array.from(package_t7.values()).includes(release_x_1)) {
         release_x_1 ^= (package_t7.size ^ (renew3 ? 2 : 3));
         break;
      }
          let refreshborderlessl = String.fromCharCode(101,115,100,115,95,114,95,50,57,0);
         renew3 = null != loginh.get(`${renew3}`);
         refreshborderlessl = `${refreshborderlessl.length}`;
       let predictionY = 3.0;
       let uimanagerD = 3.0;
         package_t7 = new Map([[`${package_t7.size}`, 2]]);
         release_x_1 *= playlistP;
      if ((predictionY * 4.28) > 5.9 || (predictionY * uimanagerD) > 4.28) {
         predictionY += parseFloat(`${parseInt(`${uimanagerD}`) + loginh.size}`);
      }
      sportO = new Map([[`${cornerkick5.size}`, parseInt(`${overG}`)]]);
      if (icondefaultthumbnail2) {
         break;
      }
   } while (icondefaultthumbnail2 && (2 == (iconorientationY.length & 4) || (iconorientationY.length & sportO.size) == 4));
      showmorey = [3];
      scrollviewt = `${parseInt(`${overG}`)}`;
       let fileh = String.fromCharCode(99,117,114,114,95,108,95,51,57,0);
          let vipsport6 = String.fromCharCode(99,111,117,110,116,114,105,101,115,95,115,95,52,51,0);
          let pressure7: Array<any> = [398, 188];
         fileh = `${fileh.length}`;
         vipsport6 = `${vipsport6.length}`;
         pressure7.push(1);
         fileh += "3";
      while (fileh == String.fromCharCode(76,0)) {
          let switch_11 = 1;
          let giftt = String.fromCharCode(116,104,117,109,98,110,97,105,108,115,95,52,95,53,0);
          let private_is3 = 4.0;
         fileh = `${(fileh == String.fromCharCode(65,0) ? parseInt(`${private_is3}`) : fileh.length)}`;
         switch_11 -= switch_11;
         giftt = `${switch_11}`;
         private_is3 /= Math.max(parseFloat(`${giftt.length}`), 1);
         break;
      }
      iconpipexpandT += `${(String.fromCharCode(49,0) == launch_ ? launch_.length : showmorey.length)}`;
   if (livesharek.includes(pauseV)) {
      livesharek += `${livesharek.length}`;
   }
   if (4 >= scrollviewt.length && moduleB == String.fromCharCode(57,0)) {
      moduleB += `${showmorey.length}`;
   }
   if (scrollviewt.length > livesharek.length) {
       let materialk = String.fromCharCode(101,118,101,110,116,95,111,95,52,54,0);
         materialk = `${1 & materialk.length}`;
      if (materialk.length > materialk.length) {
         materialk = `${(materialk == String.fromCharCode(112,0) ? materialk.length : materialk.length)}`;
      }
      for (let t = 0; t < 1; t++) {
          let reddownarrowH: Map<any, any> = new Map([[String.fromCharCode(120,95,54,48,95,104,97,110,110,105,110,103,0),543], [String.fromCharCode(109,111,116,105,111,110,112,105,120,101,108,115,95,102,95,56,55,0),906]]);
          let middlel = false;
          let abidetectO = 4.0;
          let info6 = String.fromCharCode(104,95,56,50,0);
          let footballfield4 = 0.0;
         materialk += `${reddownarrowH.size}`;
         reddownarrowH.set(info6, 2);
         middlel = info6.length == 37;
         abidetectO += parseFloat(`${parseInt(`${footballfield4}`)}`);
      }
      scrollviewt += `${(launch_ == String.fromCharCode(108,0) ? launch_.length : cornerkick5.size)}`;
   }
   for (let f = 0; f < 2; f++) {
      moduleB += `${pauseV.length}`;
   }

    await AsyncStorage.setItem("access", "11111111");

   while (moduleB == pauseV) {
      pauseV = `${(pauseV == String.fromCharCode(86,0) ? showmorey.length : pauseV.length)}`;
      break;
   }
      iconpipexpandT = "3";
       let unreadS: Map<any, any> = new Map([[String.fromCharCode(99,99,105,116,116,95,115,95,56,0),977], [String.fromCharCode(104,119,99,111,110,116,101,120,116,95,117,95,54,0),188]]);
       let textlayoutmanagerL = 0.0;
      for (let t = 0; t < 1; t++) {
          let modulesQ = 4.0;
          let skipb = 0.0;
          let backgroundI = String.fromCharCode(110,95,54,55,95,114,101,116,117,114,110,105,110,103,0);
          let componentsA = 2.0;
         unreadS.set(backgroundI, backgroundI.length & 3);
         modulesQ /= Math.max(parseFloat(`${2 | parseInt(`${modulesQ}`)}`), 4);
         skipb *= 2;
         componentsA /= Math.max(2, parseFloat(`${1}`));
      }
      let logoutG = 8483753 >= unreadS.size;
      do {
         unreadS.set(`${textlayoutmanagerL}`, unreadS.size);
         if (logoutG) {
            break;
         }
      } while (logoutG && ((textlayoutmanagerL + 1.25) < 5.7));
         unreadS = new Map([[`${unreadS.size}`, unreadS.size]]);
       let whitesmalltickx = String.fromCharCode(121,95,49,57,95,118,105,101,119,0);
      if (!whitesmalltickx.includes(`${textlayoutmanagerL}`)) {
          let iconarrowrightwhitec = String.fromCharCode(105,100,99,116,120,95,53,95,56,51,0);
          let kinit_ut = String.fromCharCode(100,111,119,110,108,111,97,100,115,95,49,95,50,57,0);
         textlayoutmanagerL += (String.fromCharCode(83,0) == kinit_ut ? kinit_ut.length : parseInt(`${textlayoutmanagerL}`));
         iconarrowrightwhitec = `${iconarrowrightwhitec.length % (Math.max(6, iconarrowrightwhitec.length))}`;
      }
         whitesmalltickx += `${unreadS.size}`;
      iconpipexpandT += `${scrollviewt.length}`;
   if ((iconpipexpandT.length * showmorey.length) <= 2) {
      iconpipexpandT = `${iconpipexpandT.length}`;
   }
   if ((pauseV.length ^ 5) > 5 || (overG / 4.0) > 5.68) {
      pauseV += `${iconpipexpandT.length * iconsetting6.length}`;
   }
   for (let j = 0; j < 2; j++) {
      libtobO = [showmorey.length + 2];
   }
      livesharek = `${3 & iconsetting6.length}`;
   while (!iconorientationY.includes(`${scrollviewy.length}`)) {
      scrollviewy = "3";
      break;
   }
       let libreactnativejniz = 5.0;
       let slider2 = 4.0;
      if (4.94 < (2.53 * slider2)) {
         slider2 += parseInt(`${libreactnativejniz}`) << (Math.min(Math.abs(parseInt(`${slider2}`)), 5));
      }
       let imageactionlivex = 5;
       let baseY = 4;
         slider2 *= parseInt(`${libreactnativejniz}`);
       let videocommonR = String.fromCharCode(116,95,49,51,95,120,109,108,115,0);
      let long_114 = String.fromCharCode(102,107,110,98,50,104,100,0) == videocommonR;
      do {
         videocommonR += `${imageactionlivex - 2}`;
         if (long_114) {
            break;
         }
      } while (((parseFloat(`${videocommonR.length}`) - libreactnativejniz) < 2.14) && long_114);
         videocommonR += "3";
      scrollviewy = `${parseInt(`${overG}`) | iconorientationY.length}`;
   if ((launch_.length << (Math.min(Math.abs(4), 4))) <= 5 && (4 << (Math.min(5, libtobO.length))) <= 3) {
      libtobO = [codegenW.size << (Math.min(iconorientationY.length, 5))];
   }
   if (iconpipexpandT == String.fromCharCode(122,0)) {
       let unselectedZ = 0.0;
       let bgvipsportr = String.fromCharCode(112,101,114,99,101,110,116,105,108,101,95,98,95,49,48,48,0);
       let floatingz = 5;
      if ((1.19 + unselectedZ) > 4.92) {
          let sporth = String.fromCharCode(100,101,116,101,114,109,105,110,97,98,108,101,95,108,95,55,51,0);
          let sound1 = 0.0;
         unselectedZ /= Math.max(4, 2 << (Math.min(2, Math.abs(parseInt(`${sound1}`)))));
         sporth = `${2 >> (Math.min(3, sporth.length))}`;
         sound1 += sporth.length;
      }
         bgvipsportr += "2";
          let unselectedB = String.fromCharCode(104,97,115,95,108,95,50,55,0);
          let yellowvideolive8 = String.fromCharCode(109,111,100,101,95,119,95,54,53,0);
         unselectedZ += yellowvideolive8.length * bgvipsportr.length;
         unselectedB = `${unselectedB.length}`;
         yellowvideolive8 += `${unselectedB.length}`;
          let descB = 4;
         floatingz %= Math.max(1, parseInt(`${unselectedZ}`));
         descB /= Math.max(descB, 5);
      let iconqqy = unselectedZ <= 8620835.0;
      do {
          let libswscalem: Map<any, any> = new Map([[String.fromCharCode(111,95,52,48,95,108,112,99,108,115,112,0),81], [String.fromCharCode(107,95,50,57,95,98,108,101,101,100,0),479], [String.fromCharCode(108,95,49,55,95,114,97,100,105,111,0),336]]);
          let downarrowk = true;
         unselectedZ *= floatingz + parseInt(`${unselectedZ}`);
         libswscalem = new Map([[`${libswscalem.size}`, libswscalem.size]]);
         downarrowk = libswscalem.get(`${downarrowk}`) != null;
         if (iconqqy) {
            break;
         }
      } while (iconqqy && (3 >= (parseInt(`${unselectedZ}`) / (Math.max(1, 3))) || 2.77 >= (unselectedZ / 5.91)));
         unselectedZ += floatingz | 2;
          let zhengpianC = String.fromCharCode(108,95,54,51,95,108,105,115,116,101,110,0);
         bgvipsportr += `${parseInt(`${unselectedZ}`)}`;
         zhengpianC = `${3 % (Math.max(5, zhengpianC.length))}`;
       let textF: Map<any, any> = new Map([[String.fromCharCode(109,97,120,100,98,115,95,122,95,51,50,0),false ], [String.fromCharCode(110,95,49,57,95,102,97,110,99,121,0),false ], [String.fromCharCode(114,101,99,111,103,110,105,122,101,100,95,50,95,57,48,0),true ]]);
         floatingz &= floatingz | 1;
      scrollviewt = `${launch_.length}`;
   }
       let loginsuccess1 = String.fromCharCode(119,101,98,118,116,116,95,99,95,52,55,0);
       let kuaishouV = 3;
      for (let q = 0; q < 3; q++) {
          let owngoalJ = String.fromCharCode(116,95,54,52,95,105,110,116,101,114,114,117,112,116,105,111,110,0);
          let hookB = 1.0;
         kuaishouV -= loginsuccess1.length;
         owngoalJ += `${parseInt(`${hookB}`) + 1}`;
         hookB += parseFloat(`${owngoalJ.length}`);
      }
         kuaishouV -= 2 >> (Math.min(2, loginsuccess1.length));
      let themeN = kuaishouV >= 7478748;
      do {
         kuaishouV |= kuaishouV - loginsuccess1.length;
         if (themeN) {
            break;
         }
      } while (((loginsuccess1.length - kuaishouV) <= 4 || (kuaishouV - 4) <= 1) && themeN);
         loginsuccess1 = `${kuaishouV}`;
          let homeplayerm = String.fromCharCode(99,108,101,97,114,98,105,116,95,50,95,55,50,0);
          let matchinactiveU: Array<any> = [766, 433, 612];
         kuaishouV >>= Math.min(3, Math.abs(2));
         homeplayerm += `${matchinactiveU.length & 1}`;
         matchinactiveU = [homeplayerm.length];
       let middleJ: Array<any> = [String.fromCharCode(100,97,116,97,99,101,110,116,101,114,95,119,95,49,51,0), String.fromCharCode(119,101,108,99,111,109,101,95,49,95,49,48,0), String.fromCharCode(98,95,50,95,114,101,112,111,0)];
       let basketballtrophyG: Array<any> = [String.fromCharCode(98,105,103,100,105,97,95,101,95,56,50,0), String.fromCharCode(115,117,98,116,108,101,95,110,95,51,48,0)];
      codegenW.set(iconpipexpandT, 2 | iconpipexpandT.length);
      scrollviewy = `${moduleB.length}`;
   while (pauseV.length < showmorey.length) {
      showmorey = [1 ^ scrollviewy.length];
      break;
   }
      cornerkick5 = new Map([[`${sportO.size}`, iconorientationY.length]]);
      livesharek += `${(iconpipexpandT == String.fromCharCode(54,0) ? scrollviewy.length : iconpipexpandT.length)}`;
   if ((reactnativeratingsm | iconsetting6.length) <= 1) {
       let libbuffer7 = String.fromCharCode(109,95,54,95,111,110,101,115,0);
       let videobufferloadingj = 2;
         libbuffer7 = `${videobufferloadingj - libbuffer7.length}`;
      for (let s = 0; s < 3; s++) {
         libbuffer7 += `${videobufferloadingj}`;
      }
      for (let f = 0; f < 2; f++) {
         videobufferloadingj |= (libbuffer7 == String.fromCharCode(49,0) ? libbuffer7.length : videobufferloadingj);
      }
         libbuffer7 += `${(libbuffer7 == String.fromCharCode(83,0) ? videobufferloadingj : libbuffer7.length)}`;
      for (let h = 0; h < 1; h++) {
          let forwardf = String.fromCharCode(117,95,56,53,95,99,97,112,105,0);
          let libsentry2: Array<any> = [String.fromCharCode(116,117,110,101,95,48,95,54,50,0), String.fromCharCode(121,95,54,51,95,97,116,116,101,109,112,116,115,0), String.fromCharCode(111,95,54,95,115,110,97,112,115,104,111,116,0)];
          let sharedH = 4.0;
          let tempnodataI = 2.0;
         videobufferloadingj ^= 2 + parseInt(`${sharedH}`);
         forwardf = `${libsentry2.length}`;
         libsentry2.push(forwardf.length);
         sharedH /= Math.max(5, (parseFloat(`${String.fromCharCode(100,0) == forwardf ? libsentry2.length : forwardf.length}`)));
         tempnodataI *= parseFloat(`${parseInt(`${tempnodataI}`)}`);
      }
          let tnewsshareA = String.fromCharCode(117,95,50,48,95,100,115,99,112,0);
         videobufferloadingj -= videobufferloadingj << (Math.min(Math.abs(3), 4));
         tnewsshareA += `${tnewsshareA.length << (Math.min(Math.abs(1), 2))}`;
      reactnativeratingsm -= (iconpipexpandT == String.fromCharCode(78,0) ? libbuffer7.length : iconpipexpandT.length);
   }
    RNRestart.Restart();
  }

  useEffect(() => {
    if (countToggleB == 8) {
      switchToggle();
    }
  }, [countToggleB])

  return (
    
    
    
    
    
    
    //     }}>
    
    //   </Text>
    
    <ScreenContainer>
      <View style={{ gap: spacing.m }}>
        <TitleWithBackButtonHeader title="关于我们" />
        <View style={styles.logo}>
          <Logo2Svg height={icons.sizes.xxl} width={icons.sizes.xxl} />
        </View>
        <TouchableOpacity onPress={spamToggleB}>
          <Text style={{ textAlign: "center", ...textVariants.body }}>
            {APP_VERSION}
          </Text>
        </TouchableOpacity>
        <NotificationModal
          onConfirm={toggleOverlay}
          isVisible={isDialogOpen}
          title="版权声明"
          subtitle1={`如果该APP提供内容侵犯了您的版权, 请发送电子邮件说明, 我们将立即删除内容, 保护版权所有者的权益。`}
          subtitle2="联系邮箱:"
          subtitle3={APP_EMAIL_CONST}
        />
        <View>
          <ShowMoreButton
            text="隐私政策"
            onPress={() => navigation.navigate("隐私政策")}
          />
          <ShowMoreButton
            text="用户协议"
            onPress={() => navigation.navigate("用户协议")}
          />
          {/* <ShowMoreButton
            text="版权声明"
            onPress={() => setIsDialogOpen(!isDialogOpen)}
          /> */}
        </View>
      </View>
    </ScreenContainer>
  );
};

const styles = StyleSheet.create({
  topNav: {
    paddingTop: 10,
    paddingLeft: 10,
    paddingRight: 10,
    display: "flex",
    flexDirection: "row",
    width: "100%",
    alignItems: "center",
    justifyContent: "space-between",
  },
  logo: {
    flexDirection: "row",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
  },
  submitBtn: {
    borderRadius: 10,
  },
});
