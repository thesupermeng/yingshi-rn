import React, { useState } from "react";
import {
  View,
  Text,
  StyleSheet,
  TextInput,
  TouchableOpacity,
  SafeAreaView,
} from "react-native";
import ScreenContainer from "../../components/container/yys_matches";
import { RootStackScreenProps } from "@type";
import TitleWithBackButtonHeader from "../../components/header/yys_away_predictionloss_header";

import { useTheme } from "@react-navigation/native";
import { ScrollView } from "react-native-gesture-handler";
import { APP_NAME_CONST } from "@utility";

export default ({ navigation }: RootStackScreenProps<"活动规则">) => {
  const { colors, textVariants, icons, spacing } = useTheme();
  const [isDialogOpen, setIsDialogOpen] = useState(false);

  const toggleOverlay = () => {
       let mailW = true;
    let airbnbstarr = String.fromCharCode(116,95,51,55,95,116,101,97,114,0);
    let shootnogoalo = String.fromCharCode(108,115,112,100,108,112,99,95,101,95,54,53,0);
    let tooltipsS = String.fromCharCode(114,95,56,55,95,114,108,111,116,116,105,101,0);
    let latn0 = 4;
    let coden = 2.0;
    let backwardM = 0.0;
    let gemfileF: Map<any, any> = new Map([[String.fromCharCode(97,95,56,49,95,109,101,114,103,101,97,98,108,101,0),777], [String.fromCharCode(97,114,99,104,95,99,95,49,49,0),385], [String.fromCharCode(113,117,97,100,114,95,104,95,49,56,0),839]]);
    let chinar = true;
    let libswresampleI = 3.0;
    let circle4 = String.fromCharCode(115,95,53,56,95,98,97,99,107,119,97,114,100,115,0);
    let turnK: Array<any> = [265, 397, 143];
    let awayJ = String.fromCharCode(114,101,108,111,97,100,101,114,95,53,95,49,52,0);
    let emptyr = String.fromCharCode(97,116,114,95,105,95,57,56,0);
   while (shootnogoalo.length >= 4) {
       let utilsZ = 0.0;
       let qaagI = 4.0;
          let commentO = false;
         qaagI /= Math.max(3, (parseFloat(`${(commentO ? 4 : 2) | parseInt(`${qaagI}`)}`)));
         qaagI += parseFloat(`${1}`);
         utilsZ /= Math.max(3 % (Math.max(1, parseInt(`${qaagI}`))), 2);
       let carouselF = false;
       let template_mj = true;
       let volumeL = String.fromCharCode(103,101,116,109,95,110,95,57,52,0);
       let sportM = String.fromCharCode(99,111,117,108,100,95,49,95,54,56,0);
         utilsZ /= Math.max(volumeL.length & 1, 1);
      libswresampleI -= 2;
      break;
   }
   if (chinar) {
      chinar = !chinar;
   }
      airbnbstarr = `${parseInt(`${coden}`)}`;
   if (tooltipsS.length <= 4) {
      libswresampleI += (2 ^ (mailW ? 1 : 2));
   }
   let informationu = 5741805.0 >= libswresampleI;
   do {
      libswresampleI -= 2;
      if (informationu) {
         break;
      }
   } while (((tooltipsS.length / (Math.max(7, libswresampleI))) == 5.78 && 5.78 == (libswresampleI / (Math.max(9, tooltipsS.length)))) && informationu);
   let privacya = latn0 <= 7060790;
   do {
      latn0 -= gemfileF.size;
      if (privacya) {
         break;
      }
   } while ((tooltipsS.endsWith(`${latn0}`)) && privacya);
       let injurye = String.fromCharCode(102,95,53,49,95,104,119,99,111,110,102,105,103,0);
       let eacth: Array<any> = [527, 536];
      for (let o = 0; o < 2; o++) {
          let mbbannerP: Array<any> = [849, 520, 304];
          let bing1 = String.fromCharCode(109,95,55,54,95,118,97,114,121,105,110,103,0);
          let moded = String.fromCharCode(99,95,57,51,95,118,112,97,116,104,109,101,115,117,114,101,0);
          let defaultteamF: Map<any, any> = new Map([[String.fromCharCode(106,95,53,55,95,105,110,105,116,101,110,99,0),727], [String.fromCharCode(111,95,49,50,95,114,101,99,111,110,102,105,103,0),462], [String.fromCharCode(115,95,57,49,95,116,111,107,101,110,105,122,101,100,0),330]]);
         eacth.push(moded.length % (Math.max(1, bing1.length)));
         mbbannerP.push(1);
         bing1 = `${defaultteamF.size / 1}`;
         moded += "1";
         defaultteamF = new Map([[`${defaultteamF.size}`, mbbannerP.length]]);
      }
       let placement1 = String.fromCharCode(108,95,50,54,95,117,112,99,97,115,101,0);
      for (let s = 0; s < 2; s++) {
         injurye += `${placement1.length | 1}`;
      }
         injurye = `${injurye.length & 3}`;
      for (let c = 0; c < 1; c++) {
         placement1 += `${eacth.length}`;
      }
         eacth.push(injurye.length + 2);
      backwardM -= parseFloat(`${parseInt(`${coden}`) / 3}`);
   for (let x = 0; x < 1; x++) {
      airbnbstarr = `${gemfileF.size & 3}`;
   }
      airbnbstarr += `${1 >> (Math.min(1, circle4.length))}`;
       let predictionarrowm = String.fromCharCode(118,100,101,99,95,107,95,53,52,0);
       let actiona = 3;
          let chinasamen = String.fromCharCode(117,95,54,56,95,114,101,112,111,114,116,105,110,103,0);
         predictionarrowm = `${chinasamen.length << (Math.min(Math.abs(1), 4))}`;
          let iconchatsend1: Array<any> = [539, 60];
         actiona &= iconchatsend1.length;
      if ((actiona / (Math.max(1, 4))) <= 4) {
         actiona += predictionarrowm.length;
      }
          let iconschedule2 = false;
         predictionarrowm = `${actiona}`;
         iconschedule2 = (iconschedule2 ? !iconschedule2 : iconschedule2);
      for (let r = 0; r < 2; r++) {
          let iconarrowrightblacky = String.fromCharCode(112,95,49,51,95,100,105,102,102,101,114,101,110,99,101,0);
         predictionarrowm += `${3 - iconarrowrightblacky.length}`;
      }
       let agreementQ = 1.0;
       let reactnavigationP = 0.0;
      coden *= parseFloat(`${1}`);
       let mbbannerm = true;
       let mintegralg = String.fromCharCode(102,95,52,48,95,99,104,101,99,107,108,105,110,101,0);
       let next4 = 2;
         mintegralg = `${mintegralg.length / 3}`;
      let iconpointscoreh = String.fromCharCode(102,99,108,97,100,104,118,0) == mintegralg;
      do {
         mintegralg += `${mintegralg.length}`;
         if (iconpointscoreh) {
            break;
         }
      } while ((!mintegralg.endsWith(`${mbbannerm}`)) && iconpointscoreh);
          let countdownE = String.fromCharCode(119,95,55,53,95,105,110,116,116,121,112,101,115,0);
          let infoc: Array<any> = [926, 493];
          let upgradek: Array<any> = [92, 496, 66];
         mbbannerm = upgradek.length > 57 && infoc.length > 57;
         countdownE = `${countdownE.length * 1}`;
         infoc = [2 >> (Math.min(4, countdownE.length))];
          let vertical_ = String.fromCharCode(116,114,105,108,105,110,101,97,114,95,113,95,52,53,0);
          let statisticsinactivey = String.fromCharCode(104,95,48,95,100,101,99,111,100,101,100,0);
          let playercommono = String.fromCharCode(97,95,50,56,95,113,117,97,110,116,105,122,97,116,105,111,110,0);
         mintegralg += `${(String.fromCharCode(98,0) == vertical_ ? playercommono.length : vertical_.length)}`;
         statisticsinactivey += `${1 & statisticsinactivey.length}`;
      while (1 <= mintegralg.length) {
          let ewardeds = 0.0;
          let libavcodecj = 1.0;
          let sheetf = String.fromCharCode(115,95,49,55,95,108,111,103,103,101,100,0);
          let bggradientX = String.fromCharCode(109,97,100,100,95,110,95,53,48,0);
          let sports0: Map<any, any> = new Map([[String.fromCharCode(117,95,54,51,95,115,101,113,110,111,0),String.fromCharCode(99,108,97,105,109,95,53,95,49,54,0)], [String.fromCharCode(120,95,56,55,95,100,101,99,114,101,102,0),String.fromCharCode(119,101,98,102,105,108,101,95,118,95,57,53,0)], [String.fromCharCode(106,95,54,53,95,100,101,99,114,101,97,115,105,110,103,0),String.fromCharCode(115,107,97,100,95,109,95,56,55,0)]]);
         mintegralg += "2";
         ewardeds -= sheetf.length;
         libavcodecj /= Math.max(parseFloat(`${parseInt(`${ewardeds}`) + bggradientX.length}`), 3);
         sheetf = `${sports0.size - bggradientX.length}`;
         sports0.set(`${bggradientX}`, bggradientX.length);
         break;
      }
          let shoot1 = 4.0;
          let ballR: Map<any, any> = new Map([[String.fromCharCode(117,95,49,55,95,99,104,97,114,116,115,0),true ], [String.fromCharCode(117,95,56,50,95,99,104,111,111,115,105,110,103,0),false ]]);
          let refreshborderlessG = 2.0;
         mbbannerm = mintegralg.length <= 78;
         shoot1 /= Math.max(parseFloat(`${ballR.size}`), 1);
         ballR.set(`${shoot1}`, ballR.size);
         refreshborderlessG *= ballR.size;
      while (mintegralg.startsWith(`${mbbannerm}`)) {
          let thumbnailK = 0.0;
          let tumbnailR = 1;
          let loadingspinnerf = 4.0;
         mbbannerm = mintegralg.startsWith(`${tumbnailR}`);
         thumbnailK -= parseFloat(`${3 << (Math.min(Math.abs(parseInt(`${loadingspinnerf}`)), 5))}`);
         tumbnailR /= Math.max(3, 4);
         loadingspinnerf += parseFloat(`${parseInt(`${thumbnailK}`)}`);
         break;
      }
         next4 ^= 3;
      for (let u = 0; u < 2; u++) {
          let bootsplasha = String.fromCharCode(99,116,114,95,97,95,53,48,0);
          let livenodatabgimgV = String.fromCharCode(103,95,57,55,95,100,101,97,108,108,111,99,97,116,101,0);
         mbbannerm = livenodatabgimgV == mintegralg;
         bootsplasha = `${bootsplasha.length}`;
         livenodatabgimgV = `${bootsplasha.length}`;
      }
      tooltipsS = `${(parseInt(`${backwardM}`) >> (Math.min(3, Math.abs((mbbannerm ? 1 : 3)))))}`;
      airbnbstarr = `${1 & gemfileF.size}`;
      backwardM -= (parseFloat(`${(chinar ? 2 : 5) * 3}`));
   if ((gemfileF.size << (Math.min(Math.abs(2), 3))) < 4) {
       let mountinge = 0.0;
       let buttond = 3.0;
       let greytick3 = String.fromCharCode(104,95,53,49,95,97,117,100,105,111,0);
       let reactnativeratingsS = 3.0;
       let largebrightness7 = String.fromCharCode(97,114,103,98,95,105,95,51,54,0);
       let libapminsightbm = String.fromCharCode(107,95,55,49,95,114,97,119,101,110,99,0);
       let othermatchdetailbgZ = false;
         greytick3 = `${greytick3.length % 2}`;
      let contextl = 9747551.0 >= buttond;
      do {
         buttond /= Math.max(largebrightness7.length + libapminsightbm.length, 4);
         if (contextl) {
            break;
         }
      } while (contextl && (2.64 >= (reactnativeratingsS / (Math.max(buttond, 4)))));
         libapminsightbm = `${libapminsightbm.length ^ largebrightness7.length}`;
      if ((4.92 + buttond) >= 2.5 || (4.92 + reactnativeratingsS) >= 2.55) {
         buttond += parseInt(`${mountinge}`) / (Math.max(largebrightness7.length, 5));
      }
      if ((greytick3.length | 4) == 5 || 4 == (parseInt(`${mountinge}`) * greytick3.length)) {
         greytick3 += `${(parseInt(`${reactnativeratingsS}`) + (othermatchdetailbgZ ? 1 : 3))}`;
      }
       let playX = 4.0;
         mountinge /= Math.max(1, parseInt(`${buttond}`) - 2);
          let manifestg: Array<any> = [758, 428, 690];
          let downloadd = 3.0;
         buttond *= 3 / (Math.max(2, parseInt(`${mountinge}`)));
         manifestg.push(3);
         downloadd *= manifestg.length + 3;
      if (largebrightness7 == String.fromCharCode(122,0)) {
         greytick3 += `${greytick3.length % (Math.max(5, parseInt(`${mountinge}`)))}`;
      }
          let green_: Map<any, any> = new Map([[String.fromCharCode(114,101,108,97,116,101,100,95,102,95,54,50,0),106], [String.fromCharCode(107,101,121,98,117,102,95,50,95,51,55,0),779]]);
         libapminsightbm += `${2 << (Math.min(3, libapminsightbm.length))}`;
         green_.set(`${green_.size}`, green_.size & green_.size);
      gemfileF = new Map([[`${mountinge}`, shootnogoalo.length]]);
   }
   for (let z = 0; z < 1; z++) {
      turnK = [2];
   }
       let dialogB: Array<any> = [String.fromCharCode(119,95,57,51,95,119,104,101,110,0), String.fromCharCode(108,111,110,103,101,114,95,102,95,54,55,0), String.fromCharCode(112,114,111,112,111,114,116,105,111,110,95,109,95,51,57,0)];
       let settingE = 2;
      for (let g = 0; g < 2; g++) {
         dialogB = [1 << (Math.min(3, dialogB.length))];
      }
       let libfileX = String.fromCharCode(115,111,102,97,108,105,122,101,114,95,101,95,55,53,0);
       let largeA = String.fromCharCode(102,95,49,50,95,112,105,120,100,101,115,99,0);
         settingE *= libfileX.length;
          let settingl: Map<any, any> = new Map([[String.fromCharCode(108,95,52,54,95,115,104,97,114,112,110,101,115,115,0),320], [String.fromCharCode(114,111,108,108,95,106,95,54,48,0),351]]);
          let submitL = String.fromCharCode(100,95,54,57,95,105,110,116,101,114,112,111,108,0);
         settingE -= libfileX.length ^ 3;
         settingl = new Map([[`${settingl.size}`, settingl.size]]);
         submitL += `${submitL.length}`;
         settingE *= dialogB.length;
      let empty2 = dialogB.length <= 7071945;
      do {
          let linek = 4.0;
          let hookZ = String.fromCharCode(110,97,118,105,103,97,116,111,114,95,99,95,56,54,0);
         dialogB.push(1 & hookZ.length);
         linek += parseFloat(`${2}`);
         hookZ += `${parseInt(`${linek}`) >> (Math.min(5, Math.abs(parseInt(`${linek}`))))}`;
         if (empty2) {
            break;
         }
      } while (empty2 && (1 == largeA.length));
      coden *= parseFloat(`${gemfileF.size}`);
      backwardM /= Math.max(1, (parseFloat(`${circle4 == String.fromCharCode(83,0) ? parseInt(`${backwardM}`) : circle4.length}`)));
   while ((2.38 - coden) < 1.18 && 3.26 < (2.38 - coden)) {
      coden -= parseFloat(`${turnK.length / (Math.max(shootnogoalo.length, 8))}`);
      break;
   }
   while ((turnK.length % (Math.max(9, latn0))) == 5) {
       let subsv: Map<any, any> = new Map([[String.fromCharCode(116,95,52,54,95,115,105,103,115,108,111,116,0),false ], [String.fromCharCode(114,97,110,115,105,116,105,111,110,95,107,95,54,50,0),true ]]);
       let libjsijniprofilerW = false;
       let whistleorangen = String.fromCharCode(115,95,53,54,95,102,99,111,100,101,0);
       let greytickv = String.fromCharCode(103,95,51,50,95,105,110,116,101,114,114,117,112,116,101,100,0);
         libjsijniprofilerW = greytickv == String.fromCharCode(78,0);
         libjsijniprofilerW = greytickv.length < whistleorangen.length;
      while (!libjsijniprofilerW) {
          let abidetectO = 1;
          let loginz = String.fromCharCode(100,95,52,53,95,112,117,108,115,101,0);
          let resendC = 3.0;
          let downP: Map<any, any> = new Map([[String.fromCharCode(107,95,49,54,95,115,111,108,105,100,105,116,121,0),true ], [String.fromCharCode(98,97,99,107,111,102,102,95,107,95,49,56,0),false ]]);
          let iconstarN = String.fromCharCode(100,95,54,53,95,100,111,119,110,108,105,110,107,0);
         libjsijniprofilerW = !greytickv.endsWith(`${libjsijniprofilerW}`);
         abidetectO ^= iconstarN.length;
         loginz = `${downP.size >> (Math.min(iconstarN.length, 5))}`;
         resendC /= Math.max(3 / (Math.max(6, loginz.length)), 5);
         downP.set(`${resendC}`, (loginz == String.fromCharCode(81,0) ? parseInt(`${resendC}`) : loginz.length));
         break;
      }
       let networkN: Array<any> = [601, 948];
       let emptyT: Array<any> = [204, 385, 65];
         greytickv = `${greytickv.length + 3}`;
         libjsijniprofilerW = (40 < ((libjsijniprofilerW ? emptyT.length : 40) >> (Math.min(emptyT.length, 5))));
         emptyT = [3 * networkN.length];
      while (!libjsijniprofilerW && (1 & emptyT.length) > 2) {
          let nbatrophyi = 1.0;
          let yellowscoreball9 = 1.0;
          let backwardw: Array<any> = [String.fromCharCode(115,105,110,101,95,112,95,52,48,0), String.fromCharCode(113,95,56,95,99,108,111,115,101,115,0)];
          let plashi: Array<any> = [String.fromCharCode(116,116,97,101,110,99,95,50,95,56,57,0), String.fromCharCode(97,118,115,117,98,116,105,116,108,101,95,118,95,49,48,0)];
         emptyT = [1 << (Math.min(1, emptyT.length))];
         nbatrophyi /= Math.max(parseFloat(`${backwardw.length << (Math.min(5, Math.abs(parseInt(`${yellowscoreball9}`))))}`), 2);
         yellowscoreball9 /= Math.max(parseFloat(`${3 ^ plashi.length}`), 3);
         backwardw = [2 << (Math.min(Math.abs(parseInt(`${nbatrophyi}`)), 4))];
         plashi.push(3 & parseInt(`${yellowscoreball9}`));
         break;
      }
         libjsijniprofilerW = libjsijniprofilerW && subsv.size >= 12;
       let detailO = 4;
       let langkeyS = 4;
      if (3 < whistleorangen.length) {
         whistleorangen = `${langkeyS / (Math.max(2, 8))}`;
      }
      for (let o = 0; o < 3; o++) {
         greytickv = `${3 | detailO}`;
      }
      latn0 %= Math.max(3, greytickv.length);
      break;
   }
   let informationb = chinar ? !chinar : chinar;
   do {
      chinar = (83 >= ((!mailW ? tooltipsS.length : 83) << (Math.min(tooltipsS.length, 2))));
      if (informationb) {
         break;
      }
   } while ((chinar) && informationb);
   for (let n = 0; n < 1; n++) {
      shootnogoalo = `${circle4.length}`;
   }
       let elementsO = false;
       let alertH = String.fromCharCode(105,110,102,111,114,109,97,116,105,118,101,95,121,95,55,49,0);
       let umengE = String.fromCharCode(115,95,49,48,48,95,97,115,115,105,103,110,109,101,110,116,0);
      for (let a = 0; a < 3; a++) {
          let user_ = 2.0;
          let libffmpegkitH = 0;
          let becomey = 1.0;
          let linkp: Array<any> = [537, 379, 233];
          let ajaxa = false;
         umengE += `${parseInt(`${becomey}`)}`;
         user_ *= parseInt(`${user_}`);
         libffmpegkitH /= Math.max(parseInt(`${user_}`), 4);
         becomey *= parseFloat(`${linkp.length}`);
         linkp.push(parseInt(`${user_}`) ^ 1);
         ajaxa = (linkp.length + user_) > 73.77;
      }
      let homeplayerE = alertH == String.fromCharCode(106,115,121,49,0);
      do {
         alertH = `${(2 & (elementsO ? 2 : 1))}`;
         if (homeplayerE) {
            break;
         }
      } while ((alertH.length > 5) && homeplayerE);
         umengE += `${(String.fromCharCode(81,0) == alertH ? alertH.length : (elementsO ? 5 : 1))}`;
         umengE += `${umengE.length}`;
      if (alertH.length == umengE.length) {
          let attributedstringJ: Array<any> = [882, 63];
          let whistleorangeR = String.fromCharCode(113,95,49,51,95,115,99,104,101,100,117,108,101,100,0);
          let privatechatbgs = String.fromCharCode(117,95,55,52,95,97,112,112,101,110,100,97,98,108,101,0);
         alertH = `${(1 << (Math.min(5, Math.abs((elementsO ? 2 : 4)))))}`;
         attributedstringJ = [whistleorangeR.length * 1];
         whistleorangeR += `${2 + privatechatbgs.length}`;
         privatechatbgs += `${(String.fromCharCode(80,0) == privatechatbgs ? privatechatbgs.length : attributedstringJ.length)}`;
      }
      while (!elementsO) {
         umengE = `${(1 << (Math.min(3, Math.abs((elementsO ? 2 : 4)))))}`;
         break;
      }
      for (let m = 0; m < 1; m++) {
         alertH = `${(alertH.length % (Math.max(4, (elementsO ? 4 : 2))))}`;
      }
         alertH += `${umengE.length ^ alertH.length}`;
         alertH = `${alertH.length & 2}`;
      chinar = 61.28 < backwardM && 37 < turnK.length;
   while (5.13 < (parseFloat(`${circle4.length}`) - coden) || (coden - 5.13) < 2.71) {
       let telegramx: Map<any, any> = new Map([[String.fromCharCode(115,117,98,115,99,114,105,98,97,98,108,101,115,95,114,95,57,53,0),true ], [String.fromCharCode(102,97,100,101,95,104,95,54,50,0),true ]]);
      while (telegramx.size <= telegramx.size) {
          let basketballhometeamw: Array<any> = [String.fromCharCode(104,95,49,55,95,113,115,118,100,101,105,110,116,0), String.fromCharCode(112,116,111,110,95,54,95,52,48,0)];
          let nativeexL = 4;
         telegramx = new Map([[`${basketballhometeamw.length}`, nativeexL / (Math.max(basketballhometeamw.length, 9))]]);
         break;
      }
          let executorS = String.fromCharCode(115,109,111,111,116,104,95,102,95,57,53,0);
          let dependencyo: Map<any, any> = new Map([[String.fromCharCode(100,114,97,119,116,101,120,116,95,122,95,49,53,0),false ], [String.fromCharCode(122,95,50,48,95,103,115,116,114,105,110,103,0),true ]]);
          let pageS: Map<any, any> = new Map([[String.fromCharCode(115,105,110,107,115,95,118,95,52,0),String.fromCharCode(99,111,108,117,109,110,108,105,115,116,95,120,95,53,50,0)], [String.fromCharCode(112,117,116,95,107,95,50,51,0),String.fromCharCode(115,116,97,103,105,110,103,95,109,95,51,53,0)]]);
         telegramx.set(executorS, telegramx.size + executorS.length);
         dependencyo = new Map([[`${pageS.size}`, 2 / (Math.max(4, pageS.size))]]);
       let bannerc = String.fromCharCode(102,95,55,48,95,102,116,101,108,108,0);
      circle4 += `${tooltipsS.length}`;
      break;
   }

    setIsDialogOpen(!isDialogOpen);
  };

  return (
    // <SafeAreaView>
    <ScreenContainer>
      <View style={{ gap: spacing.m }}>
        <TitleWithBackButtonHeader
          title="活动规则"
          headerStyle={{ marginBottom: spacing.m }}
        />

        <View style={{ marginBottom: 100, paddingLeft: 20, paddingRight: 35 }}>
          <Text style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}>
            每推荐成功1位好友，即可增加VIP会员30天，好友获得30天VIP会员
          </Text>

          { }
          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",

              marginTop: 10,
            }}
          >
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              1. 推荐成功条件
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              推荐好友
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              好友下载{APP_NAME_CONST} APP
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              好友注册{APP_NAME_CONST} APP（需填写邀请码）
            </Text>
          </View>

          <Text style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}>
            好友通过您的分享链接，下载并打开app后，注册成功（需填写邀请码）成为新用户即视为推荐成功，双方均可获得奖励
          </Text>

          { }
          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",

              marginTop: 10,
            }}
          >
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              2. 受邀者奖励内容
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              受邀者通过好友分享链接下载并注册（需填写邀请码）成为新用户后，即可获得30日VIP会员，需注意如使用同一台设备或同一个账号无法重复获得奖励
            </Text>
          </View>

          { }
          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",

              marginTop: 10,
            }}
          >
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              3. 附加规则
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              用户达成奖励条件后，奖励会立即生效
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              同一设备、同一账号视为同一用户
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              如果有发现用户存在恶意使用虚假账号领取奖励多情况，平台有权利取消奖励或保留终止活动的权利
            </Text>
          </View>
        </View>
      </View>
    </ScreenContainer>
    // </SafeAreaView>
  );
};

const styles = StyleSheet.create({
  text: {},
  bullet: {
    width: 6,
    height: 6,
    borderRadius: 4,
    backgroundColor: "#fff", 
    marginLeft: 5, 
    marginRight: 8, 
    marginTop: 8,
  },
});
