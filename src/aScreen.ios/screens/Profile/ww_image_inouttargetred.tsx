import React, { useState } from "react";
import {
  View,
  Text,
  StyleSheet,
  Image,
  Clipboard,
  Platform,
} from "react-native";
import ScreenContainer from "../../components/container/ww_collection";
import { RootStackScreenProps } from "@type/ww_tempnodatagif_description";
import { useTheme } from "@react-navigation/native";
import TitleWithBackButtonHeader from "../../components/header/ww_react_predictionwin_header";
import { useSelector } from "@hooks/ww_catagory_neon";
import { TouchableOpacity } from "@gorhom/bottom-sheet";
import NotificationModal from "../../components/modal/ww_countdown_ksad";
import { wwLaunchRound } from "@utility/ww_star";
import FastImage from "../../components/common/ww_result";
import { ChangeUsernameModal } from "../../components/modal/ww_iconuser";
import { ChangeReferrerModal } from "../../components/modal/ww_singapore";
import { wwVertical } from "@redux/reducers/ww_related_kuaishou";

export default ({ navigation }: RootStackScreenProps<"个人中心">) => {
  const { colors } = useTheme();

  const userState = useSelector<wwVertical>('userReducer');

  const [username, setUsername] = useState(userState.user?.userName ?? '');

  const [isShowUsernameModal, setShowUsernameModal] = useState(false);
  const [isShowReferrerModal, setShowReferrerModal] = useState(false);

  const [isDialogOpen, setIsDialogOpen] = useState(false);


  const toggleOverlay = () => {
       let routerl = 1;
    let libcxxcomponentsg: Array<any> = [String.fromCharCode(107,95,54,54,95,97,110,110,111,116,97,116,101,0), String.fromCharCode(120,95,50,55,95,99,104,111,109,112,0)];
    let smallX = String.fromCharCode(109,97,110,105,112,117,108,97,116,101,95,112,95,56,56,0);
    let temp8: Map<any, any> = new Map([[String.fromCharCode(113,95,49,48,95,109,115,105,122,101,0),573], [String.fromCharCode(115,105,103,110,108,101,95,104,95,50,49,0),712], [String.fromCharCode(105,95,50,48,95,117,110,112,114,101,109,117,108,116,105,112,108,121,121,117,118,0),788]]);
    let sharedA = false;
    let qaag6: Map<any, any> = new Map([[String.fromCharCode(100,113,117,97,110,116,95,110,95,51,49,0),913], [String.fromCharCode(117,116,109,111,115,116,95,119,95,50,48,0),172], [String.fromCharCode(105,95,53,51,95,114,101,115,101,116,98,117,102,0),820]]);
    let notificationfillemptyH = 3;
    let libzeusd: Map<any, any> = new Map([[String.fromCharCode(111,95,53,51,95,100,112,97,103,101,0),207], [String.fromCharCode(120,95,50,0),624], [String.fromCharCode(102,95,55,49,95,105,102,111,114,109,97,116,0),537]]);
    let eyeopen6 = String.fromCharCode(108,95,50,57,95,99,108,101,97,114,0);
    let sheete = String.fromCharCode(106,95,53,51,95,101,114,114,111,114,0);
    let whatsapp0 = 3.0;
    let zoome = true;
      libzeusd = new Map([[`${temp8.size}`, 2]]);
   let cornerkickQ = 7340159 <= qaag6.size;
   do {
      qaag6.set(smallX, 3 / (Math.max(3, qaag6.size)));
      if (cornerkickQ) {
         break;
      }
   } while (((qaag6.size * libcxxcomponentsg.length) < 1 || (1 * qaag6.size) < 1) && cornerkickQ);
   while (5 < (smallX.length * 2)) {
       let stationsX = String.fromCharCode(112,101,105,114,115,95,116,95,57,57,0);
      for (let h = 0; h < 3; h++) {
         stationsX += `${stationsX.length | stationsX.length}`;
      }
      let liste = String.fromCharCode(118,112,116,54,49,0) == stationsX;
      do {
         stationsX += `${stationsX.length * stationsX.length}`;
         if (liste) {
            break;
         }
      } while (liste && (stationsX.length < 2 || stationsX.length < 2));
       let langkey7 = String.fromCharCode(114,101,99,111,110,110,101,99,116,101,100,95,48,95,51,50,0);
      smallX = `${qaag6.size}`;
      break;
   }
      sharedA = routerl < qaag6.size;
      libcxxcomponentsg = [routerl];
      libzeusd = new Map([[`${libzeusd.size}`, libcxxcomponentsg.length]]);
   while (!Array.from(libzeusd.values()).includes(libcxxcomponentsg.length)) {
      libcxxcomponentsg = [smallX.length];
      break;
   }
   while (smallX.startsWith(`${temp8.size}`)) {
       let screenf = true;
       let editT = true;
      while (!screenf) {
          let trashP = String.fromCharCode(112,97,103,101,115,105,122,101,95,104,95,52,55,0);
         screenf = (!editT ? !screenf : editT);
         trashP += `${trashP.length + trashP.length}`;
         break;
      }
      let mathd = screenf ? !screenf : screenf;
      do {
         screenf = editT;
         if (mathd) {
            break;
         }
      } while (mathd && (editT));
      while (editT && screenf) {
         editT = !screenf || editT;
         break;
      }
         editT = !screenf;
      for (let b = 0; b < 3; b++) {
         screenf = (editT ? screenf : editT);
      }
      for (let g = 0; g < 1; g++) {
          let privatechatbgm = String.fromCharCode(120,95,55,56,95,100,101,108,111,103,111,0);
          let unimplementedviewc = String.fromCharCode(114,101,97,99,104,101,100,95,119,95,56,56,0);
          let loginsuccessH = String.fromCharCode(122,95,49,53,95,108,111,97,100,115,0);
          let placementV = false;
         editT = loginsuccessH.length > 37;
         privatechatbgm = `${unimplementedviewc.length}`;
         unimplementedviewc += `${privatechatbgm.length - 3}`;
         loginsuccessH = `${((placementV ? 1 : 4) + unimplementedviewc.length)}`;
      }
      temp8 = new Map([[`${temp8.size}`, (3 >> (Math.min(2, Math.abs((sharedA ? 1 : 3)))))]]);
      break;
   }
   if (Array.from(libzeusd.keys()).includes(`${libcxxcomponentsg.length}`)) {
      libcxxcomponentsg = [routerl];
   }
      routerl <<= Math.min(libcxxcomponentsg.length, 1);
   for (let u = 0; u < 3; u++) {
      routerl |= 1;
   }
      libzeusd = new Map([[smallX, ((sharedA ? 3 : 3))]]);
   let iconnewsshare5 = 7388823 >= qaag6.size;
   do {
      qaag6.set(`${smallX}`, qaag6.size ^ 1);
      if (iconnewsshare5) {
         break;
      }
   } while (iconnewsshare5 && ((1 >> (Math.min(1, Math.abs(qaag6.size)))) == 4 || (libzeusd.size >> (Math.min(Math.abs(qaag6.size), 3))) == 1));
      smallX += `${2 + qaag6.size}`;
      libcxxcomponentsg.push(routerl);
       let iconsettingt: Map<any, any> = new Map([[String.fromCharCode(106,95,57,51,95,113,117,97,100,116,114,101,101,0),78], [String.fromCharCode(102,116,118,99,108,95,99,95,54,52,0),783]]);
         iconsettingt = new Map([[`${iconsettingt.size}`, iconsettingt.size]]);
         iconsettingt = new Map([[`${iconsettingt.size}`, iconsettingt.size]]);
      for (let h = 0; h < 2; h++) {
         iconsettingt.set(`${iconsettingt.size}`, iconsettingt.size + 2);
      }
      notificationfillemptyH >>= Math.min(Math.abs(temp8.size), 2);
   if (1 < libzeusd.size) {
      libzeusd = new Map([[`${libzeusd.size}`, routerl]]);
   }
       let libglog4: Map<any, any> = new Map([[String.fromCharCode(101,98,117,114,95,117,95,49,56,0),false ], [String.fromCharCode(98,115,105,122,101,95,116,95,51,48,0),true ]]);
       let downloader5 = 4.0;
       let countdownl = 5.0;
          let orangeS = String.fromCharCode(108,95,49,54,95,122,101,114,111,118,0);
          let configU = 2.0;
          let analytic_: Array<any> = [843, 659];
         downloader5 /= Math.max(parseFloat(`${analytic_.length}`), 5);
         orangeS += "2";
         configU /= Math.max(1, (orangeS == String.fromCharCode(76,0) ? orangeS.length : parseInt(`${configU}`)));
         analytic_ = [(String.fromCharCode(75,0) == orangeS ? orangeS.length : parseInt(`${configU}`))];
         countdownl /= Math.max(1, parseFloat(`${libglog4.size}`));
      libzeusd.set(`${notificationfillemptyH}`, 1);

    setIsDialogOpen(!isDialogOpen);
  };

  const onPressUsername = () => {
       let weatherk = true;
    let constantsU = String.fromCharCode(101,109,111,106,105,95,52,95,53,56,0);
    let targetp = 4.0;
    let renderp = String.fromCharCode(118,95,54,52,95,99,111,110,99,117,114,101,110,116,0);
    let twitterU: Array<any> = [254, 561];
    let mimo7 = String.fromCharCode(106,95,49,57,95,114,103,116,99,117,0);
    let overv = 1;
    let moduleQ = 0.0;
   if (1 <= constantsU.length || weatherk) {
      constantsU = `${twitterU.length / (Math.max(renderp.length, 1))}`;
   }
       let libcrashsdka: Map<any, any> = new Map([[String.fromCharCode(101,110,99,111,100,101,95,109,95,50,57,0),true ], [String.fromCharCode(97,95,54,48,95,114,114,116,114,0),false ], [String.fromCharCode(109,98,105,110,116,114,97,95,104,95,55,52,0),false ]]);
       let tickJ = 2.0;
       let middlebrightnessw = String.fromCharCode(110,95,55,54,95,99,97,110,99,101,108,97,98,108,101,0);
         libcrashsdka.set(middlebrightnessw, 2);
         tickJ += parseFloat(`${parseInt(`${tickJ}`)}`);
       let bottomQ = true;
       let reminderc = false;
      if (1 > (middlebrightnessw.length + 2) || (tickJ / (Math.max(3.74, 4))) > 4.100) {
         middlebrightnessw += `${((bottomQ ? 3 : 4))}`;
      }
      for (let v = 0; v < 2; v++) {
          let frame_rR = 2.0;
          let expandw = 1.0;
          let agreementB: Map<any, any> = new Map([[String.fromCharCode(100,95,53,53,95,119,120,109,109,116,101,115,116,0),719], [String.fromCharCode(117,110,109,111,118,101,100,95,110,95,56,53,0),673], [String.fromCharCode(102,95,54,95,112,114,115,99,116,112,0),282]]);
         middlebrightnessw += `${parseInt(`${expandw}`)}`;
         frame_rR -= agreementB.size / (Math.max(1, 9));
         expandw /= Math.max(4, parseInt(`${frame_rR}`));
         agreementB = new Map([[`${agreementB.size}`, 2]]);
      }
         reminderc = tickJ <= parseFloat(`${libcrashsdka.size}`);
         middlebrightnessw = "2";
      let networkm = tickJ <= 8950422.0;
      do {
         tickJ /= Math.max((parseFloat(`${String.fromCharCode(86,0) == middlebrightnessw ? (bottomQ ? 3 : 2) : middlebrightnessw.length}`)), 3);
         if (networkm) {
            break;
         }
      } while ((Array.from(libcrashsdka.keys()).includes(`${tickJ}`)) && networkm);
          let fastforwardo = 2;
         middlebrightnessw = `${((bottomQ ? 5 : 1))}`;
         fastforwardo ^= fastforwardo;
      renderp = `${3 - middlebrightnessw.length}`;
   let core7 = renderp.length >= 5748677;
   do {
       let backwardb = String.fromCharCode(97,102,102,101,99,116,95,109,95,50,49,0);
       let logoe = String.fromCharCode(114,97,100,98,103,95,52,95,56,54,0);
         backwardb += `${logoe.length << (Math.min(Math.abs(3), 1))}`;
         backwardb += `${logoe.length}`;
      if (3 <= logoe.length || backwardb == String.fromCharCode(55,0)) {
         backwardb += "3";
      }
       let iconplaya = String.fromCharCode(107,95,57,55,95,100,101,115,99,112,114,105,112,116,105,111,110,0);
       let iconschedulef = String.fromCharCode(111,102,102,101,115,116,95,109,95,54,50,0);
          let gradlep = true;
          let private_pM = String.fromCharCode(118,105,101,119,115,95,113,95,57,53,0);
         backwardb = `${iconschedulef.length}`;
         gradlep = private_pM.length >= 65;
         private_pM += "1";
      for (let a = 0; a < 2; a++) {
         backwardb = `${(String.fromCharCode(118,0) == backwardb ? logoe.length : backwardb.length)}`;
      }
      renderp += `${1 << (Math.min(4, mimo7.length))}`;
      if (core7) {
         break;
      }
   } while ((renderp.startsWith(`${targetp}`)) && core7);
   if (4 == (twitterU.length | renderp.length) || (renderp.length | twitterU.length) == 4) {
      twitterU.push(renderp.length);
   }
   let bannerw = renderp == String.fromCharCode(119,103,112,51,53,101,0);
   do {
      renderp += `${2 + parseInt(`${targetp}`)}`;
      if (bannerw) {
         break;
      }
   } while (bannerw && (!renderp.startsWith(`${overv}`)));
      overv %= Math.max(twitterU.length | renderp.length, 3);
       let orientationR: Map<any, any> = new Map([[String.fromCharCode(119,95,50,95,97,115,116,114,111,110,111,109,105,99,97,108,0),155], [String.fromCharCode(103,95,50,51,95,111,115,116,114,101,97,109,0),454]]);
         orientationR = new Map([[`${orientationR.size}`, orientationR.size ^ orientationR.size]]);
      if (orientationR.size >= orientationR.size) {
         orientationR.set(`${orientationR.size}`, orientationR.size / 1);
      }
      while (4 > orientationR.size) {
          let splashH = 3.0;
          let targetd: Map<any, any> = new Map([[String.fromCharCode(100,105,118,109,111,100,95,121,95,52,0),610], [String.fromCharCode(105,110,116,101,114,114,117,112,116,105,98,108,101,95,122,95,52,48,0),248]]);
          let rankF = String.fromCharCode(107,95,53,52,95,108,111,99,107,0);
          let predictionbannersharedu = 5.0;
         orientationR.set(rankF, parseInt(`${splashH}`) * 1);
         splashH /= Math.max(parseFloat(`${targetd.size}`), 2);
         targetd.set(`${predictionbannersharedu}`, parseInt(`${predictionbannersharedu}`));
         rankF += `${targetd.size | 2}`;
         break;
      }
      overv %= Math.max(4, renderp.length);
   for (let x = 0; x < 1; x++) {
      overv %= Math.max(renderp.length / (Math.max(1, 5)), 3);
   }
      overv >>= Math.min(twitterU.length, 3);
   for (let v = 0; v < 1; v++) {
      twitterU = [twitterU.length - 2];
   }
   if (renderp.length == twitterU.length) {
      twitterU.push(renderp.length);
   }
   while (constantsU == String.fromCharCode(85,0)) {
      renderp = "3";
      break;
   }
   if (renderp.length > 2) {
      renderp += `${(renderp == String.fromCharCode(72,0) ? (weatherk ? 1 : 4) : renderp.length)}`;
   }
   let albuma = 6240226 >= overv;
   do {
       let mbbanner_ = 2.0;
       let greens = String.fromCharCode(97,95,53,95,104,97,114,109,111,110,105,99,0);
       let transfers: Array<any> = [String.fromCharCode(101,95,56,53,95,100,101,115,99,101,110,116,0), String.fromCharCode(117,105,110,116,95,106,95,55,48,0)];
      while ((greens.length / (Math.max(5, transfers.length))) < 3) {
          let casting6 = String.fromCharCode(114,95,54,56,95,103,114,111,119,116,104,0);
          let libimagepipeline4 = String.fromCharCode(109,99,111,114,101,95,51,95,57,49,0);
          let umengB = 5.0;
          let firebase0: Array<any> = [String.fromCharCode(99,95,54,56,95,119,105,110,100,111,119,115,0), String.fromCharCode(105,115,112,97,116,99,104,95,112,95,52,54,0), String.fromCharCode(102,95,57,57,95,97,114,101,113,117,101,115,116,0)];
          let middlex = String.fromCharCode(105,95,57,95,115,113,117,97,114,101,115,0);
         greens += `${libimagepipeline4.length}`;
         casting6 = "3";
         libimagepipeline4 = `${1 * parseInt(`${umengB}`)}`;
         umengB -= parseFloat(`${casting6.length + parseInt(`${umengB}`)}`);
         firebase0.push(parseInt(`${umengB}`));
         middlex += "3";
         break;
      }
      for (let z = 0; z < 1; z++) {
         transfers = [3 - greens.length];
      }
       let libimagepipelineW = 4;
      for (let i = 0; i < 2; i++) {
         greens += `${transfers.length}`;
      }
         greens = `${libimagepipelineW % (Math.max(greens.length, 10))}`;
       let megaphonen = true;
       let unimplementedviewv = true;
         megaphonen = (!unimplementedviewv ? !megaphonen : unimplementedviewv);
      for (let g = 0; g < 1; g++) {
         transfers.push((transfers.length >> (Math.min(5, Math.abs((unimplementedviewv ? 3 : 3))))));
      }
         transfers.push((libimagepipelineW >> (Math.min(2, Math.abs((megaphonen ? 1 : 4))))));
      overv -= 2;
      mbbanner_ += parseFloat(`${parseInt(`${mbbanner_}`)}`);
      if (albuma) {
         break;
      }
   } while (albuma && ((overv % (Math.max(mimo7.length, 1))) == 1 || (mimo7.length % (Math.max(1, 2))) == 4));
   let projectk = 6487307 >= renderp.length;
   do {
      renderp = "3";
      if (projectk) {
         break;
      }
   } while (projectk && (twitterU.length > 5));
      overv ^= mimo7.length;
   let catalogp = weatherk ? !weatherk : weatherk;
   do {
      weatherk = overv > mimo7.length;
      if (catalogp) {
         break;
      }
   } while ((2 >= constantsU.length) && catalogp);

    setShowUsernameModal(true);
  }

  const onUsernameUpdateSuccess = (newUsername: string) => {
    setUsername(newUsername);
  }

  const onPressReferrer = () => {
       let subsm = String.fromCharCode(119,105,110,100,111,119,95,107,95,53,0);
    let mappingh = 1.0;
    let indexH = String.fromCharCode(111,117,116,112,117,116,95,110,95,54,57,0);
    let topic_ = String.fromCharCode(108,95,57,95,112,108,97,99,101,104,111,108,100,101,114,115,0);
    let halffieldimagef = 4;
    let ballG: Map<any, any> = new Map([[String.fromCharCode(99,114,111,115,115,104,97,105,114,115,95,49,95,56,51,0),766], [String.fromCharCode(114,95,55,52,95,115,101,115,115,111,110,0),352]]);
    let projectT = true;
    let homek = 5;
    let projectl = String.fromCharCode(98,95,52,55,95,115,105,110,99,101,0);
   while (1 >= topic_.length && indexH != String.fromCharCode(73,0)) {
      indexH = `${(topic_ == String.fromCharCode(50,0) ? topic_.length : subsm.length)}`;
      break;
   }
      subsm += `${3 ^ ballG.size}`;
      mappingh -= 1;
      mappingh *= (indexH == String.fromCharCode(100,0) ? indexH.length : (projectT ? 5 : 1));
   while (!projectT || (3.72 * mappingh) <= 2.45) {
      mappingh -= (indexH == String.fromCharCode(108,0) ? halffieldimagef : indexH.length);
      break;
   }
       let cnewarchdefaultsh = String.fromCharCode(114,95,57,54,95,111,112,97,116,111,109,0);
          let notificationfillemptyT = String.fromCharCode(102,95,53,57,95,102,97,99,105,108,105,116,97,116,101,0);
          let runtimep = false;
          let episodeZ: Array<any> = [72, 744];
         cnewarchdefaultsh += `${episodeZ.length}`;
         notificationfillemptyT = `${((runtimep ? 4 : 5) << (Math.min(notificationfillemptyT.length, 3)))}`;
         runtimep = !runtimep;
         episodeZ = [((runtimep ? 3 : 4))];
         cnewarchdefaultsh += `${cnewarchdefaultsh.length % 2}`;
         cnewarchdefaultsh = `${cnewarchdefaultsh.length - cnewarchdefaultsh.length}`;
      projectT = indexH.length > 91;
   while ((indexH.length | ballG.size) <= 2) {
      ballG = new Map([[topic_, topic_.length % (Math.max(1, 9))]]);
      break;
   }
      subsm += `${((projectT ? 1 : 2))}`;
      mappingh -= 1;
   let selld = projectT ? !projectT : projectT;
   do {
      projectT = !indexH.endsWith(`${projectT}`);
      if (selld) {
         break;
      }
   } while (selld && (projectT));
   for (let j = 0; j < 3; j++) {
       let videovaro = 1;
       let historya: Array<any> = [615, 792, 743];
      if (5 == (historya.length & videovaro) || 5 == (videovaro & historya.length)) {
         videovaro |= 3;
      }
         historya.push(historya.length / (Math.max(2, 6)));
         videovaro %= Math.max(1, videovaro);
      let footballfiledlayoutq = historya.length >= 7772538;
      do {
         historya = [historya.length & videovaro];
         if (footballfiledlayoutq) {
            break;
         }
      } while ((3 == (historya.length >> (Math.min(3, Math.abs(videovaro))))) && footballfiledlayoutq);
      for (let w = 0; w < 2; w++) {
          let castingw = true;
          let logoutQ = 2.0;
          let exampleimageu: Map<any, any> = new Map([[String.fromCharCode(116,111,112,105,99,97,108,95,110,95,56,54,0),892], [String.fromCharCode(117,110,109,97,116,99,104,101,100,95,98,95,54,53,0),565], [String.fromCharCode(114,101,112,108,97,99,105,110,103,95,107,95,50,51,0),476]]);
         historya.push(videovaro);
         castingw = null != exampleimageu.get(`${logoutQ}`);
         logoutQ -= parseFloat(`${exampleimageu.size - 1}`);
      }
       let grayq: Array<any> = [295, 545, 634];
      indexH += "2";
   }
   if (subsm.endsWith(`${ballG.size}`)) {
      ballG.set(topic_, ballG.size + 2);
   }
      topic_ = `${3 * ballG.size}`;
   if ((4 + halffieldimagef) >= 3) {
      halffieldimagef *= parseInt(`${mappingh}`);
   }
   for (let r = 0; r < 1; r++) {
      indexH += "3";
   }
      ballG.set(indexH, 2 + topic_.length);

    setShowReferrerModal(true);
  }

  const onCopyReferralCode = () => {
       let textlayoutmanagerB = 2.0;
    let libsentryN = 2;
    let reactnativejsp = String.fromCharCode(99,97,114,114,105,97,103,101,95,100,95,57,53,0);
    let kinit_420 = String.fromCharCode(102,105,108,116,101,114,102,95,53,95,50,53,0);
    let selectede = true;
    let closes = String.fromCharCode(98,95,56,55,95,109,98,116,114,101,101,0);
    let controlsL = String.fromCharCode(110,95,51,53,95,115,104,111,119,119,97,118,101,115,112,105,99,0);
    let profileframe8 = 4.0;
    let middlewareF = String.fromCharCode(119,95,56,57,95,114,117,108,101,98,111,111,107,0);
    let holderD = 5;
    let cataloga: Map<any, any> = new Map([[String.fromCharCode(104,95,56,48,95,101,120,116,101,110,115,105,111,110,115,0),String.fromCharCode(106,97,99,111,98,105,95,100,95,53,57,0)], [String.fromCharCode(97,112,111,108,108,111,95,102,95,54,0),String.fromCharCode(99,95,50,52,95,102,111,117,114,99,99,115,0)], [String.fromCharCode(115,95,49,56,95,104,97,108,102,100,0),String.fromCharCode(104,95,52,95,112,97,103,101,110,111,0)]]);
    let profileframeo = 2;
    let umengb = String.fromCharCode(119,95,52,50,95,97,119,97,107,101,0);
    let const_tO: Array<any> = [891, 250, 505];
    let switch_c3Z: Array<any> = [447, 183, 930];
    let libavformatz = String.fromCharCode(107,95,54,50,95,100,105,115,112,108,97,121,115,0);
    let footballfiledlayoutd = String.fromCharCode(116,95,50,55,95,100,101,99,105,109,97,108,0);
   let fastX = cataloga.size >= 9080748;
   do {
      cataloga = new Map([[reactnativejsp, (String.fromCharCode(82,0) == kinit_420 ? reactnativejsp.length : kinit_420.length)]]);
      if (fastX) {
         break;
      }
   } while ((middlewareF.includes(`${cataloga.size}`)) && fastX);
   let indexv = profileframeo <= 9580795;
   do {
      profileframeo >>= Math.min(3, Math.abs(profileframeo / (Math.max(3, 6))));
      if (indexv) {
         break;
      }
   } while (indexv && (profileframeo == 2));
   for (let y = 0; y < 3; y++) {
      profileframeo &= cataloga.size;
   }
   if (kinit_420.startsWith(`${cataloga.size}`)) {
      cataloga.set(`${profileframeo}`, 1 << (Math.min(4, Math.abs(profileframeo))));
   }
   for (let p = 0; p < 1; p++) {
      profileframe8 -= 3 / (Math.max(4, parseInt(`${textlayoutmanagerB}`)));
   }
   let crossp = 6334488 <= libsentryN;
   do {
      libsentryN |= 2 >> (Math.min(4, Math.abs(holderD)));
      if (crossp) {
         break;
      }
   } while ((!kinit_420.includes(`${libsentryN}`)) && crossp);
   if (3 >= (libsentryN << (Math.min(reactnativejsp.length, 1))) || (3 << (Math.min(2, Math.abs(libsentryN)))) >= 3) {
       let dependency3 = String.fromCharCode(117,95,53,51,95,121,117,118,103,98,114,112,0);
       let classesI = 0.0;
       let materialq = 3.0;
       let showp = String.fromCharCode(109,97,116,105,99,95,99,95,57,54,0);
      while (dependency3.includes(`${materialq}`)) {
          let stepi: Map<any, any> = new Map([[String.fromCharCode(118,95,55,52,95,101,110,97,98,108,101,0),86], [String.fromCharCode(112,95,53,57,95,115,117,98,102,114,97,109,101,0),485]]);
          let downloadS = 2.0;
         materialq *= dependency3.length;
         stepi = new Map([[`${stepi.size}`, 3]]);
         downloadS *= stepi.size * parseInt(`${downloadS}`);
         break;
      }
       let readt = 5.0;
         dependency3 = `${(dependency3 == String.fromCharCode(78,0) ? dependency3.length : parseInt(`${readt}`))}`;
      if (materialq > dependency3.length) {
          let eactt: Array<any> = [821, 263];
         dependency3 += `${parseInt(`${classesI}`) % (Math.max(1, 7))}`;
         eactt = [eactt.length];
      }
          let notificationB: Map<any, any> = new Map([[String.fromCharCode(116,101,97,109,115,95,114,95,54,55,0),false ], [String.fromCharCode(118,95,55,55,95,100,111,102,102,115,101,116,115,0),true ]]);
          let downloading_ = String.fromCharCode(118,97,97,112,105,95,54,95,52,53,0);
          let debugk: Map<any, any> = new Map([[String.fromCharCode(116,111,108,101,114,97,110,99,101,95,117,95,49,0),true ], [String.fromCharCode(106,95,54,50,95,112,111,108,108,105,110,103,0),true ], [String.fromCharCode(103,101,116,99,117,116,95,120,95,49,48,48,0),true ]]);
         showp = `${parseInt(`${materialq}`)}`;
         notificationB.set(`${notificationB.size}`, debugk.size << (Math.min(4, Math.abs(notificationB.size))));
         downloading_ += `${downloading_.length >> (Math.min(Math.abs(2), 2))}`;
         debugk.set(`${downloading_}`, notificationB.size % (Math.max(downloading_.length, 3)));
      if (5.26 < (4 / (Math.max(7, readt)))) {
          let rewardvideoH = true;
          let matchactiveg: Map<any, any> = new Map([[String.fromCharCode(102,95,54,49,95,105,118,115,101,116,117,112,0),197], [String.fromCharCode(104,105,110,116,115,95,105,95,53,56,0),123]]);
          let yellowvideoliveD: Array<any> = [218, 888];
         classesI += (parseFloat(`${String.fromCharCode(73,0) == showp ? showp.length : dependency3.length}`));
         rewardvideoH = 58 < matchactiveg.size || yellowvideoliveD.length < 58;
         matchactiveg = new Map([[`${yellowvideoliveD.length}`, (1 | (rewardvideoH ? 1 : 5))]]);
      }
      while (2 >= dependency3.length) {
          let yellowanimationliveZ = 3.0;
          let forwardJ = false;
          let launcher7 = 1;
         dependency3 = `${1 | parseInt(`${yellowanimationliveZ}`)}`;
         yellowanimationliveZ /= Math.max((parseFloat(`${(forwardJ ? 1 : 5) >> (Math.min(Math.abs(launcher7), 5))}`)), 3);
         forwardJ = !forwardJ;
         launcher7 %= Math.max(5, launcher7);
         break;
      }
      while ((classesI - parseFloat(`${dependency3.length}`)) <= 2.31 && (classesI - parseFloat(`${dependency3.length}`)) <= 2.31) {
         dependency3 += `${(String.fromCharCode(76,0) == dependency3 ? dependency3.length : parseInt(`${materialq}`))}`;
         break;
      }
      while (materialq <= 4.55) {
         materialq += dependency3.length;
         break;
      }
         dependency3 = "2";
         readt *= 2 % (Math.max(3, dependency3.length));
          let moreV = String.fromCharCode(107,95,54,51,95,104,97,110,100,108,101,100,0);
         readt -= showp.length + 1;
         moreV += `${3 | moreV.length}`;
      libsentryN += (3 & (selectede ? 3 : 4));
   }
      selectede = 90 >= kinit_420.length;
   if (2.36 >= (textlayoutmanagerB * 1.44) && (textlayoutmanagerB * 1.44) >= 1.84) {
       let vignetteB = String.fromCharCode(119,95,56,54,95,115,117,110,114,105,115,101,0);
       let indexR = 4.0;
         indexR -= parseFloat(`${vignetteB.length << (Math.min(Math.abs(3), 3))}`);
         indexR *= parseFloat(`${parseInt(`${indexR}`)}`);
       let goalz = 1.0;
       let topict = 1.0;
       let sportT = 2.0;
       let kickt = 0.0;
      while (1.68 >= goalz) {
         indexR += parseFloat(`${parseInt(`${goalz}`) + 2}`);
         break;
      }
      for (let c = 0; c < 3; c++) {
          let clear_: Array<any> = [904, 290, 279];
          let otherq = String.fromCharCode(121,95,55,56,95,99,115,104,97,114,112,0);
          let sportm = 4.0;
         sportT *= parseFloat(`${2 << (Math.min(Math.abs(parseInt(`${topict}`)), 2))}`);
         clear_.push(3);
         otherq = `${clear_.length | 2}`;
         sportm /= Math.max(4, parseFloat(`${otherq.length}`));
      }
      controlsL += `${reactnativejsp.length - parseInt(`${profileframe8}`)}`;
   }
   while (reactnativejsp.length < 1 && !selectede) {
      reactnativejsp += `${1 / (Math.max(8, middlewareF.length))}`;
      break;
   }
   while (libsentryN >= profileframe8) {
      profileframe8 += profileframeo;
      break;
   }
   let referrerv = holderD <= 5678202;
   do {
      holderD &= kinit_420.length;
      if (referrerv) {
         break;
      }
   } while (referrerv && (5 == holderD));
   if ((profileframe8 / (Math.max(3.0, 7))) <= 5.94 || (1 / (Math.max(6, parseInt(`${profileframe8}`)))) <= 1) {
      profileframe8 *= profileframeo * 3;
   }
      cataloga.set(controlsL, kinit_420.length / (Math.max(9, controlsL.length)));
   for (let z = 0; z < 1; z++) {
      middlewareF += "2";
   }
   if (1 > (cataloga.size ^ 4)) {
      cataloga.set(closes, closes.length >> (Math.min(2, Math.abs(parseInt(`${textlayoutmanagerB}`)))));
   }
       let flippert = String.fromCharCode(115,101,97,116,95,55,95,50,0);
       let const_2g: Array<any> = [487, 737];
       let statsM: Map<any, any> = new Map([[String.fromCharCode(105,110,105,116,105,97,108,105,122,97,116,105,111,110,95,56,95,51,53,0),false ], [String.fromCharCode(117,118,108,99,95,55,95,50,55,0),false ], [String.fromCharCode(97,115,100,107,95,50,95,52,49,0),true ]]);
      for (let x = 0; x < 1; x++) {
         statsM = new Map([[`${const_2g.length}`, flippert.length & const_2g.length]]);
      }
         const_2g.push(const_2g.length % (Math.max(1, 6)));
         const_2g.push(statsM.size);
       let loading8: Map<any, any> = new Map([[String.fromCharCode(107,95,55,57,95,101,116,104,111,100,0),527], [String.fromCharCode(114,95,55,57,95,101,118,97,115,97,112,112,0),749], [String.fromCharCode(108,97,110,103,115,95,120,95,49,53,0),104]]);
      for (let c = 0; c < 3; c++) {
          let anytimeB = String.fromCharCode(116,114,97,110,115,97,99,116,105,111,110,115,95,55,95,56,54,0);
          let lang6 = String.fromCharCode(101,95,53,55,95,101,118,97,108,102,117,110,99,0);
          let nexte = 2.0;
         statsM.set(flippert, const_2g.length >> (Math.min(flippert.length, 4)));
         anytimeB = `${anytimeB.length & 2}`;
         lang6 += `${1 * anytimeB.length}`;
         nexte -= parseFloat(`${parseInt(`${nexte}`)}`);
      }
      for (let f = 0; f < 2; f++) {
         flippert = `${loading8.size + 2}`;
      }
      for (let p = 0; p < 1; p++) {
          let dark5 = 3.0;
          let episodea: Array<any> = [String.fromCharCode(121,95,57,55,95,98,105,116,119,114,105,116,101,114,0), String.fromCharCode(98,95,49,53,95,114,101,100,101,109,112,116,105,111,110,0)];
          let ewardedV: Map<any, any> = new Map([[String.fromCharCode(112,95,50,55,95,116,105,107,101,114,0),399], [String.fromCharCode(114,101,100,117,99,101,100,95,114,95,53,0),937], [String.fromCharCode(119,95,53,56,95,105,110,116,101,114,97,99,116,105,118,101,0),765]]);
          let teamdetailsbgh = 3.0;
          let acceptedd = 4;
         const_2g.push(parseInt(`${teamdetailsbgh}`));
         dark5 += parseFloat(`${acceptedd}`);
         episodea.push(acceptedd);
         ewardedV = new Map([[`${episodea.length}`, 1]]);
         teamdetailsbgh += ewardedV.size & parseInt(`${dark5}`);
      }
       let yellowu = 3.0;
       let libreactnativeblob9 = 4.0;
      let toponZ = 9103534 <= loading8.size;
      do {
         loading8.set(`${yellowu}`, 1);
         if (toponZ) {
            break;
         }
      } while ((4 <= (loading8.size % (Math.max(1, 3))) || 2.97 <= (loading8.size * libreactnativeblob9)) && toponZ);
      profileframeo -= kinit_420.length & 1;
   let reactnativeultimatelistviewl = libsentryN >= 7265260;
   do {
      libsentryN >>= Math.min(Math.abs(cataloga.size & middlewareF.length), 1);
      if (reactnativeultimatelistviewl) {
         break;
      }
   } while ((textlayoutmanagerB < 4.100) && reactnativeultimatelistviewl);

    Clipboard.setString(userState.user?.userReferralCode ?? '');

   let sell8 = 6428835 <= reactnativejsp.length;
   do {
      reactnativejsp += `${libsentryN ^ parseInt(`${textlayoutmanagerB}`)}`;
      if (sell8) {
         break;
      }
   } while (sell8 && ((4 << (Math.min(1, Math.abs(holderD)))) == 4));
   let libfbp = 4981234 >= closes.length;
   do {
      closes = `${(kinit_420 == String.fromCharCode(117,0) ? kinit_420.length : (selectede ? 5 : 3))}`;
      if (libfbp) {
         break;
      }
   } while ((profileframe8 >= closes.length) && libfbp);
       let h_player4 = 5.0;
       let splashM = String.fromCharCode(115,104,97,108,108,111,119,95,56,95,48,0);
         splashM += "3";
       let switch_ojJ = 1;
      for (let e = 0; e < 2; e++) {
         splashM = `${splashM.length | 3}`;
      }
         switch_ojJ -= splashM.length;
      if (!splashM.includes(`${switch_ojJ}`)) {
         splashM = "2";
      }
         switch_ojJ ^= 3 & switch_ojJ;
      libsentryN %= Math.max(4, parseInt(`${textlayoutmanagerB}`) * controlsL.length);
      h_player4 *= parseInt(`${h_player4}`);
   let macaus = kinit_420.length <= 8917328;
   do {
      kinit_420 += `${(String.fromCharCode(48,0) == reactnativejsp ? reactnativejsp.length : parseInt(`${textlayoutmanagerB}`))}`;
      if (macaus) {
         break;
      }
   } while (macaus && (kinit_420.length <= 3));
      profileframe8 /= Math.max(4, 2 << (Math.min(1, umengb.length)));
   if ((libsentryN & 4) >= 2 || 4 >= (parseInt(`${profileframe8}`) * libsentryN)) {
      profileframe8 += parseInt(`${profileframe8}`) | 1;
   }
   while ((libsentryN / (Math.max(umengb.length, 3))) < 3) {
       let injury2 = 3.0;
       let hoverG = 4.0;
      if (4.9 <= injury2) {
         hoverG /= Math.max(parseInt(`${injury2}`), 2);
      }
      if (injury2 > 1.19) {
          let hookf = String.fromCharCode(116,115,99,99,100,97,116,97,95,102,95,52,51,0);
          let watchnowbgS = true;
         injury2 /= Math.max(parseFloat(`${2 >> (Math.min(Math.abs(parseInt(`${hoverG}`)), 3))}`), 2);
         hookf += "3";
         watchnowbgS = !hookf.startsWith(`${watchnowbgS}`);
      }
      if ((2.28 + injury2) == 1.49 || (injury2 / 2.28) == 3.5) {
         injury2 /= Math.max(2, parseFloat(`${1}`));
      }
       let buildB = String.fromCharCode(113,95,54,53,95,105,102,111,114,119,97,114,100,0);
       let with_hx = String.fromCharCode(115,117,98,115,101,108,101,99,116,95,109,95,51,50,0);
      while (with_hx.includes(`${buildB.length}`)) {
         with_hx = `${buildB.length ^ 1}`;
         break;
      }
         hoverG += parseInt(`${injury2}`);
      libsentryN %= Math.max(3, 1);
      break;
   }
   let middlebrightnessP = 9031976 >= profileframeo;
   do {
       let chato = String.fromCharCode(101,95,56,57,95,114,101,97,100,113,0);
       let nalyticsZ: Map<any, any> = new Map([[String.fromCharCode(116,95,49,55,95,110,117,109,98,105,116,115,0),302], [String.fromCharCode(119,95,52,54,95,101,120,99,108,117,115,105,111,110,115,0),814], [String.fromCharCode(110,95,53,51,95,115,116,97,116,101,0),509]]);
      let verticalm = 8840441 >= nalyticsZ.size;
      do {
         nalyticsZ.set(chato, 3 | chato.length);
         if (verticalm) {
            break;
         }
      } while (verticalm && ((chato.length * 1) < 3 || (nalyticsZ.size * chato.length) < 1));
      for (let a = 0; a < 3; a++) {
          let stringsI = 4.0;
          let rewindT = String.fromCharCode(122,95,52,50,95,100,111,109,101,115,116,105,99,0);
          let iconnewsshare1 = 4.0;
          let libsgcore1 = String.fromCharCode(115,95,51,57,95,105,110,118,115,98,111,120,0);
          let fcdaebecbcbafcdfceaaeccfeacdbN = false;
         chato += `${rewindT.length}`;
         stringsI *= parseFloat(`${parseInt(`${iconnewsshare1}`) | 2}`);
         rewindT = "2";
         iconnewsshare1 += 3;
         libsgcore1 = "3";
         fcdaebecbcbafcdfceaaeccfeacdbN = 52.28 <= iconnewsshare1 || 52.28 <= stringsI;
      }
      profileframeo /= Math.max(3 * middlewareF.length, 1);
      if (middlebrightnessP) {
         break;
      }
   } while (middlebrightnessP && (profileframeo <= 2));
      cataloga.set(`${libsentryN}`, 1);
      profileframe8 -= profileframeo >> (Math.min(5, Math.abs(3)));
      reactnativejsp = `${middlewareF.length}`;
      profileframeo /= Math.max(1, libsentryN >> (Math.min(Math.abs(cataloga.size), 4)));
   if (2 <= (1 + parseInt(`${textlayoutmanagerB}`)) || (kinit_420.length + parseInt(`${textlayoutmanagerB}`)) <= 1) {
      textlayoutmanagerB /= Math.max(2, profileframeo + 2);
   }
      closes += `${((selectede ? 1 : 2) - profileframeo)}`;
   if (!selectede) {
      holderD += 1 / (Math.max(holderD, 1));
   }
   let short_bL = selectede ? !selectede : selectede;
   do {
       let resulty = String.fromCharCode(103,114,97,121,115,99,97,108,101,95,118,95,54,49,0);
       let statisticsinactiveo = String.fromCharCode(98,101,99,111,109,101,95,108,95,54,55,0);
       let vipsporti: Array<any> = [745, 313, 622];
       let debugX = 4.0;
      for (let l = 0; l < 2; l++) {
          let download9 = 3.0;
          let filedD = String.fromCharCode(117,110,119,114,97,112,112,101,100,95,52,95,56,48,0);
         vipsporti = [3];
         download9 /= Math.max((parseFloat(`${filedD == String.fromCharCode(57,0) ? filedD.length : parseInt(`${download9}`)}`)), 4);
      }
          let vipsport5 = 3.0;
         statisticsinactiveo = `${parseInt(`${vipsport5}`) & 1}`;
      if (!statisticsinactiveo.startsWith(`${vipsporti.length}`)) {
         vipsporti.push(vipsporti.length);
      }
      if (debugX <= 5.21) {
         debugX += (statisticsinactiveo == String.fromCharCode(88,0) ? resulty.length : statisticsinactiveo.length);
      }
      let penalty2 = debugX >= 8161858.0;
      do {
         debugX += (String.fromCharCode(69,0) == statisticsinactiveo ? statisticsinactiveo.length : parseInt(`${debugX}`));
         if (penalty2) {
            break;
         }
      } while (penalty2 && (4 < vipsporti.length));
      for (let l = 0; l < 3; l++) {
         vipsporti = [3];
      }
         debugX -= vipsporti.length;
         resulty = `${statisticsinactiveo.length}`;
      if (statisticsinactiveo != String.fromCharCode(49,0)) {
          let tempnodatagifL = 5.0;
          let xvoda = 4;
         resulty += `${parseInt(`${tempnodatagifL}`)}`;
         xvoda ^= xvoda + 2;
      }
         statisticsinactiveo += `${vipsporti.length % (Math.max(3, resulty.length))}`;
         resulty += `${(String.fromCharCode(80,0) == statisticsinactiveo ? vipsporti.length : statisticsinactiveo.length)}`;
          let defaultroombgI = 2.0;
          let configK = String.fromCharCode(102,95,49,56,95,114,101,97,100,121,0);
         vipsporti.push((String.fromCharCode(117,0) == statisticsinactiveo ? statisticsinactiveo.length : resulty.length));
         defaultroombgI /= Math.max(configK.length, 5);
         configK += `${parseInt(`${defaultroombgI}`)}`;
      selectede = 59 > controlsL.length;
      if (short_bL) {
         break;
      }
   } while ((!reactnativejsp.startsWith(`${selectede}`)) && short_bL);
       let disconnected5 = String.fromCharCode(118,95,57,51,95,112,97,115,116,101,108,0);
         disconnected5 += `${(disconnected5 == String.fromCharCode(69,0) ? disconnected5.length : disconnected5.length)}`;
         disconnected5 = `${disconnected5.length | disconnected5.length}`;
         disconnected5 += `${(disconnected5 == String.fromCharCode(85,0) ? disconnected5.length : disconnected5.length)}`;
      profileframe8 *= profileframeo >> (Math.min(Math.abs(2), 5));
      holderD += ((selectede ? 4 : 2) ^ 3);
    setIsDialogOpen(true);
  }

  return (
    <ScreenContainer>
      <TitleWithBackButtonHeader title='个人中心' />
      <View
        style={{
          justifyContent: 'space-between',
          flex: 1,
          paddingHorizontal: 10,
        }}
      >
        <View style={{ marginTop: 30 }}>
          <TouchableOpacity
            style={styles.pressableTextContainer}
            onPress={() => {
              wwLaunchRound.showToast('目前暂不支持更改头像，敬请期待');
            }}
          >
            <Text style={{ fontSize: 16, color: '#9C9C9C' }}>头像</Text>
            <FastImage
              style={{
                height: 34,
                width: 34,
              }}
              source={require('@static/images/bottomIconuserBanner.png')}
            />
          </TouchableOpacity>

          { }
          <TouchableOpacity
            style={styles.pressableTextContainer}
            onPress={onPressUsername}
          >
            <Text style={{ fontSize: 16, color: 'white' }}>
              {username}
            </Text>
          </TouchableOpacity>

          { }
          <View style={styles.pressableTextContainer}>
            <Text style={{ fontSize: 16, color: '#9C9C9C' }}>
              {userState.user?.userEmail !== '' ? userState.user?.userEmail : userState.user?.userPhoneNumber}
            </Text>
          </View>

          { }
          {userState.user?.userReferrerName == '' &&
            userState.user.userAllowUpdateReferral == true &&
            <TouchableOpacity
              style={styles.pressableTextContainer}
              onPress={onPressReferrer}
            >
              <Text style={{ fontSize: 16, color: '#B6B6B6' }}>
                补填邀请码 (只能填写一次)
              </Text>
            </TouchableOpacity>
          }

          {userState.user?.userReferrerName != '' && (
            <View style={styles.pressableTextContainer}>
              <Text style={{ fontSize: 16, color: '#9C9C9C' }}>推介人</Text>

              <View
                style={{
                  flexDirection: 'row',
                  justifyContent: 'center',
                  alignItems: 'center',
                }}
              >
                <Text style={{ fontSize: 14, color: '#9C9C9C' }}>
                  {userState.user?.userReferrerName}
                </Text>
              </View>
            </View>
          )}

          { }
          <TouchableOpacity
            style={styles.pressableTextContainer}
            onPress={onCopyReferralCode}
          >
            <Text style={{ fontSize: 16, color: colors.primary }}>
              {userState.user?.userReferralCode}
            </Text>

            <View
              style={{
                flexDirection: 'row',
                justifyContent: 'center',
                alignItems: 'center',
              }}
            >
              <Text style={{ fontSize: 14, color: '#9C9C9C' }}>
                复制邀请码
              </Text>
              <Image
                style={{
                  height: 27,
                  width: 27,
                  position: 'relative',
                  top: 2,
                }}
                source={require('@static/images/profile/pointDacccfaabfbcbadeebddcabacdffdbc.png')}
              />
            </View>
          </TouchableOpacity>

          <NotificationModal
            onConfirm={toggleOverlay}
            isVisible={isDialogOpen}
            title='复制成功'
            subtitle1=''
            subtitle2=''
            subtitle3=''
          />
        </View>

        <ChangeUsernameModal
          isVisible={isShowUsernameModal}
          handleClose={() => {
            setShowUsernameModal(false);
          }}
          initialUsername={username}
          onSubmitSuccess={onUsernameUpdateSuccess}
        />

        <ChangeReferrerModal
          isVisible={isShowReferrerModal}
          handleClose={() => {
            setShowReferrerModal(false);
          }}
        />
      </View>
    </ScreenContainer>
  );
};

const styles = StyleSheet.create({
  pressableTextContainer: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    backgroundColor: '#1d2023',
    marginTop: 20,
    paddingLeft: 18,
    paddingRight: 13,
    height: 48,
    borderRadius: 8,
    marginBottom: 20,
  },
});
