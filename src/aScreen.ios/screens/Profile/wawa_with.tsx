import React, { useState } from "react";
import {
  View,
  Text,
  StyleSheet,
  Image,
  Clipboard,
  Platform,
} from "react-native";
import ScreenContainer from "../../components/container/wawa_nterstitial_iconpipexpand";
import { RootStackScreenProps } from "@type/wawa_iconnewchat";
import { useTheme } from "@react-navigation/native";
import TitleWithBackButtonHeader from "../../components/header/wawa_matchinactive_header";
import { useSelector } from "@hooks/wawa_root";
import { TouchableOpacity } from "@gorhom/bottom-sheet";
import NotificationModal from "../../components/modal/wawa_lock";
import { wawaNewarchdefaults } from "@utility/wawa_analytic_imagenomoredata";
import FastImage from "../../components/common/wawa_iconarrowrightblack";
import { ChangeUsernameModal } from "../../components/modal/wawa_iconclosewhitebg";
import { ChangeReferrerModal } from "../../components/modal/wawa_iconnewsshare_private";
import { wawaPhoneControls } from "@redux/reducers/wawa_umeng";

export default ({ navigation }: RootStackScreenProps<"个人中心">) => {
  const { colors } = useTheme();

  const userState = useSelector<wawaPhoneControls>('userReducer');

  const [username, setUsername] = useState(userState.user?.userName ?? '');

  const [isShowUsernameModal, setShowUsernameModal] = useState(false);
  const [isShowReferrerModal, setShowReferrerModal] = useState(false);

  const [isDialogOpen, setIsDialogOpen] = useState(false);


  const toggleOverlay = () => {
       let libffmpegkit2 = 2;
    let countdown7 = 2.0;
    let gpayJ = 4;
    let awayplayerA = String.fromCharCode(104,111,116,105,122,111,110,116,108,95,110,95,56,54,0);
    let alertU = String.fromCharCode(102,95,54,95,121,97,114,110,0);
    let sellmathbackgroundd = 3.0;
    let libanej: Map<any, any> = new Map([[String.fromCharCode(104,95,54,51,95,97,102,102,101,99,116,0),244], [String.fromCharCode(112,95,49,95,111,118,101,114,104,101,97,100,0),615]]);
    let libtany = String.fromCharCode(105,110,116,101,114,112,111,108,97,116,111,114,115,95,119,95,49,50,0);
    let disconnectedlogoG = String.fromCharCode(120,95,56,50,95,102,117,110,99,115,0);
    let hiadF = String.fromCharCode(99,111,109,109,101,110,116,115,95,122,95,52,54,0);
   if ((alertU.length | libanej.size) > 1 || (1 | libanej.size) > 2) {
      libanej.set(`${sellmathbackgroundd}`, 1 - alertU.length);
   }
   let basketballicond = awayplayerA == String.fromCharCode(111,48,114,111,117,108,104,97,108,0);
   do {
      awayplayerA += `${(String.fromCharCode(70,0) == libtany ? libtany.length : libanej.size)}`;
      if (basketballicond) {
         break;
      }
   } while ((awayplayerA.length == 2) && basketballicond);
      sellmathbackgroundd *= parseFloat(`${awayplayerA.length}`);
       let videocommone = 4;
         videocommone >>= Math.min(5, Math.abs(videocommone));
         videocommone += videocommone;
      if (4 <= (videocommone + videocommone)) {
         videocommone <<= Math.min(5, Math.abs(videocommone ^ videocommone));
      }
      libtany = "3";
   for (let g = 0; g < 3; g++) {
       let cross9: Array<any> = [214, 310, 771];
       let fnewinterstitialu = 2.0;
       let blackH = 4.0;
         fnewinterstitialu -= parseInt(`${blackH}`);
      for (let z = 0; z < 3; z++) {
         blackH /= Math.max(1, 5);
      }
         fnewinterstitialu += cross9.length + parseInt(`${fnewinterstitialu}`);
          let file_ = 4.0;
         blackH -= parseInt(`${fnewinterstitialu}`);
         file_ *= parseInt(`${file_}`) - 2;
      while (cross9.includes(fnewinterstitialu)) {
          let footballtrophyL = 0.0;
          let internet0 = String.fromCharCode(115,116,114,101,115,115,95,103,95,56,0);
          let runtimeschedulern = String.fromCharCode(105,110,116,101,114,112,95,102,95,53,48,0);
          let notificationS: Array<any> = [764, 643, 545];
         fnewinterstitialu += 1;
         footballtrophyL *= notificationS.length % (Math.max(runtimeschedulern.length, 6));
         internet0 = `${parseInt(`${footballtrophyL}`) & 2}`;
         runtimeschedulern = "2";
         notificationS.push(parseInt(`${footballtrophyL}`));
         break;
      }
          let adult5: Array<any> = [146, 96];
          let xadsdkl = String.fromCharCode(110,95,54,95,118,115,117,98,113,0);
         blackH /= Math.max(3, 3 & cross9.length);
         adult5 = [xadsdkl.length + adult5.length];
         xadsdkl += `${2 >> (Math.min(4, adult5.length))}`;
         cross9.push(parseInt(`${blackH}`));
         fnewinterstitialu += cross9.length << (Math.min(4, Math.abs(parseInt(`${blackH}`))));
      for (let e = 0; e < 1; e++) {
         cross9 = [parseInt(`${fnewinterstitialu}`) % 2];
      }
      sellmathbackgroundd += parseFloat(`${alertU.length * awayplayerA.length}`);
   }
   if (sellmathbackgroundd > libffmpegkit2) {
       let eyeclose7 = String.fromCharCode(99,111,100,101,100,95,50,95,56,54,0);
       let fulls = 0;
       let whitesmalltickl = 1.0;
       let iconsharefriendsx = String.fromCharCode(118,95,52,50,95,115,112,114,111,112,0);
       let fullscreenmin0 = String.fromCharCode(110,95,49,56,95,114,108,112,0);
         iconsharefriendsx = `${fulls}`;
          let custom6 = 2;
          let iconqq1 = 2;
         fulls |= parseInt(`${whitesmalltickl}`);
         custom6 <<= Math.min(Math.abs(iconqq1), 2);
         iconqq1 >>= Math.min(Math.abs(custom6), 1);
      if (whitesmalltickl < fulls) {
         whitesmalltickl *= parseFloat(`${2 + eyeclose7.length}`);
      }
         fullscreenmin0 += `${eyeclose7.length >> (Math.min(Math.abs(3), 5))}`;
         fullscreenmin0 += `${fulls}`;
      if (1 >= eyeclose7.length && fullscreenmin0 != String.fromCharCode(108,0)) {
          let style3 = 1.0;
          let leaguew = 0.0;
          let vignetteB = String.fromCharCode(121,118,99,111,110,102,105,103,95,119,95,49,0);
          let fullc: Map<any, any> = new Map([[String.fromCharCode(116,95,52,49,95,115,101,112,97,114,97,116,111,114,0),470], [String.fromCharCode(109,95,51,50,95,112,107,116,99,112,121,0),194]]);
         eyeclose7 += `${(fullscreenmin0 == String.fromCharCode(122,0) ? fullc.size : fullscreenmin0.length)}`;
         style3 /= Math.max(parseFloat(`${parseInt(`${style3}`) % (Math.max(1, parseInt(`${leaguew}`)))}`), 2);
         leaguew /= Math.max(3, 3);
         vignetteB += `${vignetteB.length}`;
         fullc.set(`${leaguew}`, parseInt(`${leaguew}`) * vignetteB.length);
      }
      let anythinkW = whitesmalltickl <= 6502518.0;
      do {
          let backiconmaskc = String.fromCharCode(108,111,111,112,101,120,105,116,95,103,95,50,52,0);
          let shrinkC = String.fromCharCode(105,95,57,55,95,99,111,110,115,116,115,0);
          let expiredN = String.fromCharCode(118,95,56,49,95,115,104,111,117,121,97,99,104,111,0);
          let webviewv = true;
         whitesmalltickl += parseFloat(`${3 * shrinkC.length}`);
         backiconmaskc = `${(backiconmaskc.length & (webviewv ? 3 : 5))}`;
         shrinkC = `${((webviewv ? 4 : 5) - 2)}`;
         expiredN = `${((webviewv ? 2 : 5) + 1)}`;
         if (anythinkW) {
            break;
         }
      } while (anythinkW && ((fulls / (Math.max(parseInt(`${whitesmalltickl}`), 5))) >= 4 && 5.59 >= (parseFloat(`${fulls}`) / (Math.max(3, whitesmalltickl)))));
         fulls ^= fullscreenmin0.length;
      libffmpegkit2 %= Math.max(3, awayplayerA.length >> (Math.min(Math.abs(1), 3)));
   }
       let libfbjnit: Map<any, any> = new Map([[String.fromCharCode(109,95,53,50,95,112,97,110,101,0),true ], [String.fromCharCode(114,95,49,49,95,115,112,101,99,105,102,105,99,0),false ], [String.fromCharCode(112,111,108,121,108,105,110,101,95,113,95,52,54,0),true ]]);
       let canvasC = 0;
       let select9 = String.fromCharCode(100,118,98,116,120,116,95,112,95,52,50,0);
         libfbjnit.set(`${canvasC}`, libfbjnit.size - canvasC);
      for (let g = 0; g < 3; g++) {
          let fieldP = 3.0;
          let rightO = 3.0;
         canvasC %= Math.max(3 << (Math.min(Math.abs(parseInt(`${fieldP}`)), 2)), 5);
         fieldP += parseFloat(`${parseInt(`${rightO}`)}`);
      }
       let questiconm = String.fromCharCode(119,105,110,101,114,114,110,111,95,103,95,53,50,0);
      for (let f = 0; f < 1; f++) {
          let profileinactiveN = 2.0;
          let searchbarQ: Array<any> = [40, 1000];
          let imageactionliveZ: Array<any> = [543, 614, 613];
         questiconm += `${imageactionliveZ.length - 2}`;
         profileinactiveN -= searchbarQ.length;
         searchbarQ.push(1);
         imageactionliveZ = [parseInt(`${profileinactiveN}`) ^ searchbarQ.length];
      }
      while (questiconm.length > 5) {
         select9 += `${canvasC / 2}`;
         break;
      }
         libfbjnit = new Map([[`${libfbjnit.size}`, 2 | libfbjnit.size]]);
         select9 = `${select9.length >> (Math.min(4, Math.abs(libfbjnit.size)))}`;
      let classes3 = libfbjnit.size >= 7025481;
      do {
         libfbjnit.set(`${select9}`, select9.length);
         if (classes3) {
            break;
         }
      } while (classes3 && (select9.startsWith(`${libfbjnit.size}`)));
         select9 += `${canvasC}`;
      libffmpegkit2 ^= 2 & parseInt(`${sellmathbackgroundd}`);
   let internetP = 9646491 >= libanej.size;
   do {
      libanej.set(libtany, libtany.length);
      if (internetP) {
         break;
      }
   } while ((Array.from(libanej.keys()).includes(`${sellmathbackgroundd}`)) && internetP);
   let encrypts = 6138107.0 <= sellmathbackgroundd;
   do {
       let thumbnail1 = String.fromCharCode(101,120,116,114,97,100,97,116,97,99,111,110,102,105,103,95,54,95,56,49,0);
      while (4 > thumbnail1.length) {
         thumbnail1 += `${thumbnail1.length}`;
         break;
      }
         thumbnail1 += `${thumbnail1.length}`;
       let ranke = 1;
      sellmathbackgroundd /= Math.max(1, parseFloat(`${alertU.length ^ libanej.size}`));
      if (encrypts) {
         break;
      }
   } while (encrypts && ((2.44 * sellmathbackgroundd) >= 1.70));
   while (2 < alertU.length) {
       let castingP: Array<any> = [483, 138];
       let valuesk = String.fromCharCode(118,105,115,105,116,111,114,95,103,95,52,53,0);
       let nbatrophyZ: Array<any> = [623, 937, 163];
      for (let k = 0; k < 1; k++) {
          let textlayoutmanager2: Map<any, any> = new Map([[String.fromCharCode(102,95,55,95,97,115,102,99,114,121,112,116,0),96], [String.fromCharCode(114,101,100,101,101,109,95,102,95,57,55,0),360], [String.fromCharCode(101,114,114,99,111,100,101,95,104,95,51,49,0),987]]);
         castingP.push(2);
         textlayoutmanager2 = new Map([[`${textlayoutmanager2.size}`, textlayoutmanager2.size]]);
      }
      if ((3 * valuesk.length) > 2 || (valuesk.length * 3) > 1) {
         valuesk = `${nbatrophyZ.length}`;
      }
       let backwhiteD: Map<any, any> = new Map([[String.fromCharCode(106,95,53,56,95,118,112,120,101,110,99,0),String.fromCharCode(115,116,98,108,95,98,95,51,51,0)], [String.fromCharCode(107,95,50,53,95,102,111,117,110,100,0),String.fromCharCode(112,114,105,109,101,95,55,95,49,55,0)]]);
       let logoutO: Map<any, any> = new Map([[String.fromCharCode(98,111,117,110,100,105,110,103,95,48,95,56,56,0),String.fromCharCode(115,117,98,112,111,105,110,116,101,114,95,98,95,55,56,0)], [String.fromCharCode(109,97,99,95,97,95,55,54,0),String.fromCharCode(99,111,110,118,101,120,95,109,95,49,48,0)], [String.fromCharCode(114,101,115,111,108,118,95,121,95,50,50,0),String.fromCharCode(101,110,99,111,100,97,98,108,101,95,115,95,53,53,0)]]);
      if ((3 << (Math.min(2, valuesk.length))) <= 3) {
         backwhiteD.set(`${nbatrophyZ.length}`, nbatrophyZ.length | 1);
      }
      if (!Array.from(backwhiteD.values()).includes(castingP.length)) {
          let nextT = String.fromCharCode(115,104,111,119,95,100,95,51,50,0);
          let thumbnailO = false;
          let yellowcirclebgS = false;
          let tickedQ = String.fromCharCode(121,95,55,51,95,99,97,109,101,114,97,0);
         backwhiteD = new Map([[`${logoutO.size}`, logoutO.size ^ backwhiteD.size]]);
         nextT = `${((yellowcirclebgS ? 4 : 1))}`;
         thumbnailO = (yellowcirclebgS ? thumbnailO : yellowcirclebgS);
         tickedQ = `${((thumbnailO ? 3 : 2) * (yellowcirclebgS ? 4 : 5))}`;
      }
      for (let v = 0; v < 2; v++) {
          let halffieldimageF = String.fromCharCode(121,95,49,49,95,108,97,121,101,114,115,0);
          let streaming6 = String.fromCharCode(119,95,52,48,95,116,114,97,105,108,105,110,103,0);
          let black0 = 5.0;
         logoutO.set(`${halffieldimageF}`, halffieldimageF.length);
         streaming6 = `${parseInt(`${black0}`) / (Math.max(streaming6.length, 4))}`;
         black0 += parseFloat(`${3}`);
      }
       let leftN: Map<any, any> = new Map([[String.fromCharCode(121,95,50,56,95,99,101,108,108,0),910], [String.fromCharCode(97,109,112,108,105,116,117,100,101,95,111,95,56,0),643], [String.fromCharCode(99,95,50,54,95,100,99,116,120,100,99,0),929]]);
       let mbnativem: Map<any, any> = new Map([[String.fromCharCode(122,95,57,55,95,101,99,114,101,99,111,118,101,114,0),true ], [String.fromCharCode(120,95,55,49,95,105,119,104,116,0),false ]]);
          let targetA: Map<any, any> = new Map([[String.fromCharCode(118,95,56,55,95,97,117,116,111,114,101,103,114,101,115,115,105,111,110,0),394], [String.fromCharCode(102,95,50,50,95,111,108,108,101,99,116,105,111,110,115,0),478], [String.fromCharCode(103,114,111,117,112,95,105,95,52,53,0),328]]);
          let statisticsinactivey = String.fromCharCode(99,104,97,114,103,101,95,106,95,49,57,0);
         backwhiteD.set(statisticsinactivey, statisticsinactivey.length - valuesk.length);
         targetA.set(`${targetA.size}`, targetA.size + 3);
      if (2 <= (backwhiteD.size & leftN.size) || (leftN.size & backwhiteD.size) <= 2) {
         backwhiteD.set(`${nbatrophyZ.length}`, 1 ^ nbatrophyZ.length);
      }
      alertU = `${castingP.length}`;
      break;
   }
      libanej = new Map([[`${libanej.size}`, libtany.length]]);
   if (4 > (libffmpegkit2 ^ 1)) {
       let downloaderN = String.fromCharCode(108,97,110,103,115,95,106,95,49,56,0);
      if (!downloaderN.endsWith(`${downloaderN.length}`)) {
          let chatroombackgroundx: Map<any, any> = new Map([[String.fromCharCode(102,95,50,51,95,115,101,97,100,0),String.fromCharCode(113,95,54,56,95,103,101,116,109,0)], [String.fromCharCode(105,100,119,116,95,121,95,54,49,0),String.fromCharCode(110,111,116,105,102,105,99,97,116,105,111,110,115,95,55,95,49,56,0)]]);
         downloaderN += `${downloaderN.length}`;
         chatroombackgroundx = new Map([[`${chatroombackgroundx.size}`, chatroombackgroundx.size & chatroombackgroundx.size]]);
      }
       let bggradientB = String.fromCharCode(99,95,50,56,95,100,101,112,108,111,121,109,101,110,116,0);
       let showlessx = String.fromCharCode(100,112,110,97,109,101,95,108,95,55,56,0);
       let controlX = String.fromCharCode(111,112,101,110,115,115,108,95,54,95,51,48,0);
      libffmpegkit2 ^= parseInt(`${countdown7}`) | 1;
   }
       let footballfieldV: Array<any> = [408, 265, 893];
       let filterS: Array<any> = [706, 320];
       let checkbox7: Array<any> = [79, 406, 843];
         checkbox7.push(checkbox7.length);
         filterS.push(3);
      if (1 <= (checkbox7.length / (Math.max(8, filterS.length))) && (filterS.length / 1) <= 2) {
         filterS = [2];
      }
         filterS = [footballfieldV.length];
      for (let j = 0; j < 1; j++) {
          let appsI = String.fromCharCode(112,95,57,50,95,105,99,99,112,0);
         filterS.push(footballfieldV.length);
         appsI += `${appsI.length ^ appsI.length}`;
      }
      if (2 > (3 + footballfieldV.length) || (checkbox7.length + 3) > 2) {
         footballfieldV.push(filterS.length / 1);
      }
      while (filterS.length >= footballfieldV.length) {
         filterS = [footballfieldV.length >> (Math.min(Math.abs(3), 5))];
         break;
      }
      if ((3 ^ filterS.length) == 5 && 3 == (checkbox7.length ^ filterS.length)) {
         checkbox7.push(footballfieldV.length);
      }
      while ((2 | checkbox7.length) == 1) {
         checkbox7 = [1 | filterS.length];
         break;
      }
      libanej = new Map([[`${footballfieldV.length}`, 3 >> (Math.min(3, footballfieldV.length))]]);
      sellmathbackgroundd *= parseFloat(`${libffmpegkit2}`);
   if (2 == awayplayerA.length && alertU != String.fromCharCode(65,0)) {
      awayplayerA = `${(libtany == String.fromCharCode(98,0) ? libtany.length : libanej.size)}`;
   }
      sellmathbackgroundd *= (parseFloat(`${String.fromCharCode(68,0) == alertU ? parseInt(`${sellmathbackgroundd}`) : alertU.length}`));

    setIsDialogOpen(!isDialogOpen);
  };

  const onPressUsername = () => {
       let textinputR = 0;
    let benefitz: Array<any> = [47, 833, 627];
    let goallogoE = String.fromCharCode(115,115,105,109,95,97,95,52,53,0);
    let phoneshareR = String.fromCharCode(100,95,50,50,95,112,111,108,108,115,0);
    let utilsp = true;
    let video_ = 5.0;
    let dropdownu = true;
    let iconarrowrightorangez = 3;
    let refreshJ = String.fromCharCode(106,95,56,95,115,104,105,102,116,115,0);
    let gpayg = 5;
    let showmoreF = String.fromCharCode(105,95,55,57,95,108,111,111,112,102,105,108,116,101,114,0);
      textinputR |= goallogoE.length;
      phoneshareR = `${benefitz.length}`;
   if (video_ == 4.38) {
      video_ *= (parseFloat(`${3 & (utilsp ? 5 : 4)}`));
   }
   for (let g = 0; g < 1; g++) {
      dropdownu = !dropdownu;
   }
      utilsp = 42 == refreshJ.length;
       let klevinx: Map<any, any> = new Map([[String.fromCharCode(119,95,56,51,95,113,117,101,114,121,0),724], [String.fromCharCode(111,95,51,51,95,110,111,104,101,97,100,101,114,0),822]]);
       let customu = 5.0;
       let libfbjnij: Map<any, any> = new Map([[String.fromCharCode(107,95,55,51,95,109,118,114,101,102,0),false ], [String.fromCharCode(117,105,110,116,112,111,119,95,52,95,57,51,0),false ], [String.fromCharCode(101,120,116,114,101,109,101,95,57,95,50,52,0),true ]]);
      let scrollview8 = customu >= 9057435.0;
      do {
         customu += klevinx.size >> (Math.min(2, Math.abs(parseInt(`${customu}`))));
         if (scrollview8) {
            break;
         }
      } while (scrollview8 && ((libfbjnij.size * parseInt(`${customu}`)) > 5 || (libfbjnij.size * parseInt(`${customu}`)) > 5));
         customu -= 1;
      for (let e = 0; e < 2; e++) {
         customu /= Math.max(2, 2);
      }
       let regengn = 4.0;
          let awayz = String.fromCharCode(112,117,98,101,120,112,95,116,95,55,48,0);
          let notificationfilledR = 5.0;
         regengn += parseFloat(`${awayz.length - parseInt(`${regengn}`)}`);
         awayz += `${parseInt(`${notificationfilledR}`)}`;
      if (klevinx.get(`${customu}`) == null) {
         customu /= Math.max(5, klevinx.size);
      }
      for (let y = 0; y < 1; y++) {
         libfbjnij = new Map([[`${klevinx.size}`, 3 * parseInt(`${regengn}`)]]);
      }
         regengn += parseFloat(`${parseInt(`${regengn}`) * libfbjnij.size}`);
      let untickH = 5611821.0 >= regengn;
      do {
         regengn += parseFloat(`${klevinx.size + 2}`);
         if (untickH) {
            break;
         }
      } while (untickH && (libfbjnij.size < parseInt(`${regengn}`)));
      refreshJ += `${((dropdownu ? 4 : 4) + iconarrowrightorangez)}`;
   for (let j = 0; j < 2; j++) {
      gpayg += (goallogoE == String.fromCharCode(120,0) ? parseInt(`${video_}`) : goallogoE.length);
   }
   let root1 = 7752747 <= phoneshareR.length;
   do {
      phoneshareR = `${((dropdownu ? 5 : 2) | phoneshareR.length)}`;
      if (root1) {
         break;
      }
   } while (root1 && (1 > iconarrowrightorangez));
      phoneshareR = `${gpayg / 1}`;
      refreshJ += `${gpayg + textinputR}`;
      utilsp = refreshJ.startsWith(`${utilsp}`);
   for (let t = 0; t < 1; t++) {
      textinputR /= Math.max(3, 2 * phoneshareR.length);
   }
      showmoreF += "1";
      showmoreF += `${textinputR}`;
      textinputR /= Math.max(1, benefitz.length ^ iconarrowrightorangez);
       let release_qJ = 3;
      for (let n = 0; n < 3; n++) {
         release_qJ <<= Math.min(Math.abs(release_qJ), 2);
      }
          let baiduZ = 3;
          let chartw = String.fromCharCode(98,97,115,105,99,97,108,108,121,95,110,95,51,56,0);
          let notification8 = String.fromCharCode(105,110,116,101,103,114,97,116,101,95,54,95,52,51,0);
         release_qJ *= chartw.length | 1;
         baiduZ &= (notification8 == String.fromCharCode(102,0) ? baiduZ : notification8.length);
         chartw += `${notification8.length - 1}`;
      while (1 < (release_qJ / 5)) {
         release_qJ <<= Math.min(2, Math.abs(release_qJ));
         break;
      }
      video_ += parseFloat(`${gpayg / 2}`);
       let yellowanimationliveZ = 1.0;
       let linkC = 3.0;
       let actionP: Array<any> = [90, 703, 827];
       let arrowselectdownA = String.fromCharCode(119,95,55,56,95,100,99,116,120,0);
       let mounting0 = String.fromCharCode(103,95,55,95,99,111,110,116,0);
       let guideM = 3;
          let ksadW = true;
          let dropdownuz = 2.0;
          let reactnavigationu = true;
         linkC /= Math.max(4, (parseFloat(`${parseInt(`${linkC}`) * (ksadW ? 1 : 2)}`)));
         ksadW = 73.17 >= dropdownuz;
         dropdownuz += ((reactnavigationu ? 5 : 2) & parseInt(`${dropdownuz}`));
         reactnavigationu = !reactnavigationu;
         actionP.push(arrowselectdownA.length);
      for (let a = 0; a < 3; a++) {
         mounting0 += `${guideM}`;
      }
      let confirmationH = yellowanimationliveZ <= 9643937.0;
      do {
         yellowanimationliveZ *= parseFloat(`${actionP.length - 2}`);
         if (confirmationH) {
            break;
         }
      } while ((!mounting0.includes(`${yellowanimationliveZ}`)) && confirmationH);
         guideM >>= Math.min(2, actionP.length);
      let statsnomoredataD = 6053260 <= actionP.length;
      do {
         actionP.push(1 >> (Math.min(5, mounting0.length)));
         if (statsnomoredataD) {
            break;
         }
      } while (statsnomoredataD && (5 >= (actionP.length ^ 2)));
         linkC /= Math.max(parseFloat(`${3}`), 3);
      video_ -= parseFloat(`${parseInt(`${yellowanimationliveZ}`) & 3}`);
      iconarrowrightorangez |= benefitz.length * 2;
   let predictiondefaultp = iconarrowrightorangez >= 6070963;
   do {
      iconarrowrightorangez &= (String.fromCharCode(122,0) == refreshJ ? goallogoE.length : refreshJ.length);
      if (predictiondefaultp) {
         break;
      }
   } while ((5 <= (iconarrowrightorangez << (Math.min(Math.abs(1), 4)))) && predictiondefaultp);
   let playlistm = 9829669 >= textinputR;
   do {
      textinputR >>= Math.min(Math.abs(phoneshareR.length << (Math.min(Math.abs(3), 1))), 4);
      if (playlistm) {
         break;
      }
   } while ((5 > (textinputR & phoneshareR.length) || (5 & phoneshareR.length) > 2) && playlistm);
       let unselectede = 5.0;
       let homeactiveY = String.fromCharCode(113,117,97,110,116,105,116,121,95,107,95,56,55,0);
      if ((1 >> (Math.min(2, homeactiveY.length))) == 2 || (1 - homeactiveY.length) == 4) {
          let rewardvideoy: Map<any, any> = new Map([[String.fromCharCode(116,114,105,97,108,115,95,105,95,53,48,0),String.fromCharCode(97,115,115,101,109,98,108,121,95,57,95,57,57,0)], [String.fromCharCode(100,95,53,55,95,115,112,111,116,0),String.fromCharCode(115,116,101,112,95,49,95,55,51,0)]]);
          let proxyb = 3;
          let confirmationZ: Map<any, any> = new Map([[String.fromCharCode(110,105,115,116,122,95,120,95,55,55,0),String.fromCharCode(116,95,56,53,95,101,108,101,109,101,110,116,115,0)], [String.fromCharCode(112,95,52,49,95,99,111,110,118,101,114,116,105,98,108,101,0),String.fromCharCode(119,97,114,110,95,118,95,49,49,0)]]);
         homeactiveY = `${(String.fromCharCode(95,0) == homeactiveY ? homeactiveY.length : confirmationZ.size)}`;
         rewardvideoy = new Map([[`${rewardvideoy.size}`, proxyb / (Math.max(5, rewardvideoy.size))]]);
         proxyb -= 2 + rewardvideoy.size;
         confirmationZ.set(`${proxyb}`, proxyb >> (Math.min(Math.abs(1), 4)));
      }
      if (4 < (parseInt(`${unselectede}`) + homeactiveY.length) && 4 < (parseInt(`${unselectede}`) + homeactiveY.length)) {
         homeactiveY += `${homeactiveY.length ^ 1}`;
      }
          let stylev = String.fromCharCode(120,95,52,48,95,100,105,116,97,98,108,101,0);
          let teamdetailsbgK = String.fromCharCode(101,95,50,55,95,100,105,109,115,0);
         homeactiveY = `${teamdetailsbgK.length}`;
         stylev += `${stylev.length | 3}`;
         teamdetailsbgK += `${stylev.length * 3}`;
      while (2 <= (parseInt(`${unselectede}`) - homeactiveY.length) || (2 ^ homeactiveY.length) <= 2) {
         unselectede -= parseInt(`${unselectede}`) << (Math.min(homeactiveY.length, 4));
         break;
      }
      if (2.75 > unselectede) {
          let assistL = String.fromCharCode(115,117,98,115,116,114,97,99,116,101,100,95,105,95,55,50,0);
          let hiadv = 4;
          let formv = 4.0;
          let values0: Map<any, any> = new Map([[String.fromCharCode(97,102,102,101,99,116,95,106,95,51,55,0),String.fromCharCode(97,95,51,54,95,97,114,109,111,110,121,0)], [String.fromCharCode(118,97,105,108,97,98,105,108,105,116,121,95,49,95,50,54,0),String.fromCharCode(115,95,53,53,95,99,111,110,110,101,99,116,0)], [String.fromCharCode(101,95,56,57,95,110,109,118,106,111,105,110,116,115,97,100,99,111,115,116,0),String.fromCharCode(100,99,116,99,111,101,102,95,100,95,50,51,0)]]);
         unselectede *= (String.fromCharCode(55,0) == assistL ? parseInt(`${unselectede}`) : assistL.length);
         hiadv /= Math.max(1 + values0.size, 5);
         formv *= values0.size ^ hiadv;
      }
         homeactiveY += `${parseInt(`${unselectede}`) << (Math.min(Math.abs(2), 4))}`;
      dropdownu = iconarrowrightorangez >= 55;
   let logoV = showmoreF.length >= 6153755;
   do {
      showmoreF = `${((utilsp ? 4 : 2) ^ parseInt(`${video_}`))}`;
      if (logoV) {
         break;
      }
   } while (logoV && (1 <= (showmoreF.length | gpayg) && 1 <= (1 | showmoreF.length)));
   for (let w = 0; w < 3; w++) {
       let acceptedm = false;
          let elementsN = 5.0;
          let recommendationm = false;
          let handler1 = String.fromCharCode(99,95,50,54,95,105,99,99,112,0);
         acceptedm = (acceptedm ? !recommendationm : acceptedm);
         elementsN -= (parseFloat(`${String.fromCharCode(86,0) == handler1 ? handler1.length : parseInt(`${elementsN}`)}`));
         recommendationm = 12 == handler1.length;
         acceptedm = !acceptedm;
      for (let x = 0; x < 3; x++) {
         acceptedm = !acceptedm;
      }
      refreshJ = `${1 >> (Math.min(3, showmoreF.length))}`;
   }
      utilsp = benefitz.includes(dropdownu);

    setShowUsernameModal(true);
  }

  const onUsernameUpdateSuccess = (newUsername: string) => {
    setUsername(newUsername);
  }

  const onPressReferrer = () => {
       let iconarrowrightorangeW = 5;
    let icondownimgy = false;
    let successd = 2.0;
    let hearto: Map<any, any> = new Map([[String.fromCharCode(114,95,53,55,95,110,117,108,108,115,0),890], [String.fromCharCode(98,95,51,49,0),864]]);
    let selectedj = 0.0;
    let sharewhiten = 3;
    let fullm = String.fromCharCode(101,95,48,95,97,110,110,101,120,98,0);
    let iconrightorangeg = 0.0;
    let referrerQ = String.fromCharCode(97,95,52,48,95,116,114,97,110,115,105,116,0);
    let sendp = String.fromCharCode(102,97,100,101,95,110,95,54,54,0);
    let distw = String.fromCharCode(99,111,112,121,95,108,95,55,48,0);
    let whiteanimationliveF = String.fromCharCode(106,95,57,52,95,104,121,112,104,101,110,97,116,101,100,0);
   while ((hearto.size & 4) < 5 || 1 < (hearto.size & 4)) {
      hearto = new Map([[`${iconarrowrightorangeW}`, ((icondownimgy ? 5 : 1) - iconarrowrightorangeW)]]);
      break;
   }
      icondownimgy = iconrightorangeg >= 90.58;
   let hooksQ = String.fromCharCode(119,114,53,113,108,111,95,0) == distw;
   do {
      distw = `${1 - sharewhiten}`;
      if (hooksQ) {
         break;
      }
   } while (hooksQ && ((iconrightorangeg + 5.67) <= 2.100 || 4 <= (parseInt(`${iconrightorangeg}`) + 3)));
       let collection6 = 4.0;
       let snewarchdefaultso: Array<any> = [93, 652];
       let eyecloses = false;
      while (5 <= (snewarchdefaultso.length / 1)) {
         collection6 *= 3 - snewarchdefaultso.length;
         break;
      }
       let merger0 = 4;
       let dist4 = 3;
         snewarchdefaultso.push(2);
      for (let h = 0; h < 2; h++) {
         collection6 += merger0 >> (Math.min(4, Math.abs(3)));
      }
         collection6 += snewarchdefaultso.length;
      if (4.89 > (collection6 / (Math.max(5.83, 7)))) {
         collection6 += snewarchdefaultso.length;
      }
       let defaultprofilepic3 = String.fromCharCode(99,95,53,53,95,105,116,101,114,97,116,101,0);
       let pushk = String.fromCharCode(109,111,110,111,98,105,116,95,54,95,54,51,0);
      while (5.74 <= collection6 || 5.17 <= (collection6 / 5.74)) {
         eyecloses = collection6 == 100.17;
         break;
      }
      for (let l = 0; l < 1; l++) {
          let dataI = String.fromCharCode(114,101,108,99,116,120,95,100,95,51,51,0);
         snewarchdefaultso = [snewarchdefaultso.length];
         dataI = "2";
      }
      sendp = `${(3 | (icondownimgy ? 4 : 4))}`;
       let yellowvideoliveF = 5.0;
       let eyeopenX = 1.0;
       let zoomU: Array<any> = [312, 636];
         zoomU = [parseInt(`${eyeopenX}`)];
      while (3 > (2 | zoomU.length)) {
          let showmoreF = String.fromCharCode(114,95,50,52,95,114,101,109,97,114,107,0);
          let iconmoreP = true;
          let gemfileM = String.fromCharCode(107,95,57,52,95,114,111,119,105,100,0);
         zoomU.push(zoomU.length);
         showmoreF = `${((iconmoreP ? 5 : 2))}`;
         iconmoreP = (72 <= ((!iconmoreP ? showmoreF.length : 72) * showmoreF.length));
         gemfileM += `${3 | showmoreF.length}`;
         break;
      }
      if (4.4 < (5.7 + eyeopenX) || 1.9 < (5.7 + eyeopenX)) {
         eyeopenX /= Math.max(parseFloat(`${parseInt(`${eyeopenX}`) * 1}`), 5);
      }
       let rounda = String.fromCharCode(97,95,54,51,95,99,111,109,98,0);
         yellowvideoliveF *= 2 & rounda.length;
          let iconclose8 = 5.0;
         eyeopenX /= Math.max(parseFloat(`${parseInt(`${iconclose8}`) * 1}`), 5);
         zoomU.push(parseInt(`${yellowvideoliveF}`));
         yellowvideoliveF /= Math.max(2, rounda.length);
      if (2.60 >= (eyeopenX + yellowvideoliveF) || (yellowvideoliveF + 2.60) >= 5.75) {
         eyeopenX *= (parseFloat(`${rounda == String.fromCharCode(105,0) ? rounda.length : zoomU.length}`));
      }
      sendp = `${1 & whiteanimationliveF.length}`;
      sendp += `${(String.fromCharCode(72,0) == referrerQ ? hearto.size : referrerQ.length)}`;
       let floatingL = String.fromCharCode(108,115,112,102,95,113,95,49,53,0);
       let selectQ = 2.0;
       let iconwatchW = 5.0;
      let gpayl = selectQ <= 5710720.0;
      do {
         selectQ /= Math.max(parseFloat(`${3 * floatingL.length}`), 2);
         if (gpayl) {
            break;
         }
      } while (gpayl && (4.94 >= (iconwatchW / (Math.max(1.28, 9))) && 4.54 >= (1.28 / (Math.max(10, iconwatchW)))));
      let libfbf = String.fromCharCode(97,99,106,110,0) == floatingL;
      do {
         floatingL += `${(floatingL == String.fromCharCode(56,0) ? parseInt(`${iconwatchW}`) : floatingL.length)}`;
         if (libfbf) {
            break;
         }
      } while (libfbf && ((parseFloat(`${floatingL.length}`) - selectQ) < 2.96 || 4.2 < (2.96 - selectQ)));
         selectQ /= Math.max(3, parseFloat(`${parseInt(`${iconwatchW}`)}`));
      while (4.2 > (iconwatchW - 4.99) || (floatingL.length ^ 3) > 1) {
         floatingL = `${floatingL.length & parseInt(`${iconwatchW}`)}`;
         break;
      }
      while (selectQ <= 3.48) {
         iconwatchW += floatingL.length % 3;
         break;
      }
          let connection5 = 5.0;
         selectQ *= parseFloat(`${floatingL.length}`);
         connection5 += parseFloat(`${2}`);
         iconwatchW += 2;
         selectQ += parseFloat(`${parseInt(`${iconwatchW}`) >> (Math.min(Math.abs(2), 4))}`);
         floatingL = `${3 + floatingL.length}`;
      iconrightorangeg *= sendp.length % 3;
   let previewk = 4905346 <= iconarrowrightorangeW;
   do {
      iconarrowrightorangeW ^= distw.length & parseInt(`${selectedj}`);
      if (previewk) {
         break;
      }
   } while ((3 <= (1 & iconarrowrightorangeW) || (referrerQ.length & iconarrowrightorangeW) <= 1) && previewk);
       let accepted_: Map<any, any> = new Map([[String.fromCharCode(119,105,100,101,95,121,95,50,51,0),false ], [String.fromCharCode(112,95,53,56,95,103,101,110,115,0),false ]]);
       let libgloga = true;
      for (let q = 0; q < 1; q++) {
         libgloga = !libgloga || accepted_.size < 80;
      }
         accepted_ = new Map([[`${accepted_.size}`, accepted_.size + 2]]);
       let bangc = 3;
       let whitetick3 = 0;
         libgloga = (accepted_.size % (Math.max(10, whitetick3))) <= 64;
       let becomes = 2;
       let acceptedm = 4;
          let core0 = 4;
          let orangeP = String.fromCharCode(115,95,57,50,95,98,111,111,108,101,97,110,0);
          let privatechatbgL: Map<any, any> = new Map([[String.fromCharCode(106,95,53,51,95,99,121,99,108,101,0),623], [String.fromCharCode(99,109,97,115,107,95,114,95,48,0),205], [String.fromCharCode(115,116,114,111,107,101,114,95,57,95,54,49,0),749]]);
         acceptedm <<= Math.min(4, Math.abs(2));
         core0 %= Math.max(privatechatbgL.size | 1, 1);
         orangeP = "3";
         privatechatbgL = new Map([[orangeP, orangeP.length << (Math.min(5, Math.abs(core0)))]]);
      icondownimgy = 15 > fullm.length;
   if (Array.from(hearto.keys()).includes(`${iconarrowrightorangeW}`)) {
      hearto.set(`${iconrightorangeg}`, referrerQ.length | 3);
   }
   for (let h = 0; h < 1; h++) {
      sharewhiten += iconarrowrightorangeW & sharewhiten;
   }
   let temperatureD = whiteanimationliveF.length >= 7783948;
   do {
      whiteanimationliveF = `${fullm.length}`;
      if (temperatureD) {
         break;
      }
   } while ((5 > (whiteanimationliveF.length % 1) || 3 > (whiteanimationliveF.length % 1)) && temperatureD);
   let flipperz = icondownimgy ? !icondownimgy : icondownimgy;
   do {
       let activityK: Array<any> = [423, 530];
       let selected3 = String.fromCharCode(105,110,105,116,100,101,99,95,109,95,55,53,0);
       let predictionbutton_ = 1.0;
      for (let d = 0; d < 2; d++) {
         activityK = [activityK.length];
      }
      for (let e = 0; e < 2; e++) {
         activityK = [1 & selected3.length];
      }
          let pingb = String.fromCharCode(105,95,51,57,95,110,101,101,100,115,0);
          let privacyV = false;
         selected3 = "1";
         pingb += `${pingb.length}`;
         privacyV = pingb.length < 38;
      for (let h = 0; h < 1; h++) {
         selected3 += `${1 & parseInt(`${predictionbutton_}`)}`;
      }
         activityK.push(selected3.length);
      let matchesh = predictionbutton_ <= 5005361.0;
      do {
          let iconmegaphoneR = 3;
          let heartQ = 5.0;
          let dycreatorP: Array<any> = [285, 340];
          let benefitO = false;
         predictionbutton_ /= Math.max(4, iconmegaphoneR % 1);
         iconmegaphoneR *= parseInt(`${heartQ}`) % 1;
         heartQ /= Math.max(parseFloat(`${3 % (Math.max(parseInt(`${heartQ}`), 7))}`), 4);
         dycreatorP = [(parseInt(`${heartQ}`) << (Math.min(1, Math.abs((benefitO ? 2 : 1)))))];
         benefitO = benefitO && heartQ >= 76.44;
         if (matchesh) {
            break;
         }
      } while ((activityK.includes(predictionbutton_)) && matchesh);
         activityK = [activityK.length];
          let profileactiveJ: Map<any, any> = new Map([[String.fromCharCode(115,97,118,105,110,103,95,100,95,55,51,0),742], [String.fromCharCode(115,95,56,53,95,112,117,114,103,101,0),447], [String.fromCharCode(105,115,112,111,115,97,98,108,101,95,101,95,49,56,0),396]]);
          let playercommonK = false;
          let sortG = 3.0;
         selected3 = `${selected3.length}`;
         profileactiveJ = new Map([[`${profileactiveJ.size}`, profileactiveJ.size / (Math.max(1, 10))]]);
         playercommonK = profileactiveJ.size >= parseInt(`${sortG}`);
         sortG -= parseFloat(`${parseInt(`${sortG}`)}`);
         selected3 += `${activityK.length}`;
      icondownimgy = predictionbutton_ == referrerQ.length;
      if (flipperz) {
         break;
      }
   } while (flipperz && (!icondownimgy));
      referrerQ = `${whiteanimationliveF.length / 2}`;
   let cornerr = 7957650 <= sharewhiten;
   do {
      sharewhiten >>= Math.min(Math.abs(whiteanimationliveF.length << (Math.min(referrerQ.length, 2))), 1);
      if (cornerr) {
         break;
      }
   } while (cornerr && (4 < (fullm.length - 4) && (fullm.length - sharewhiten) < 4));
      selectedj += 2;
      selectedj /= Math.max(3, 2 >> (Math.min(4, Math.abs(parseInt(`${selectedj}`)))));
      sharewhiten <<= Math.min(3, Math.abs(hearto.size));
   let downarrowO = distw.length >= 5476453;
   do {
       let alertI = 3;
      while (5 == alertI) {
         alertI |= alertI;
         break;
      }
      if (alertI > 3) {
         alertI &= alertI;
      }
      let unselectedt = 6423650 <= alertI;
      do {
         alertI <<= Math.min(5, Math.abs(3));
         if (unselectedt) {
            break;
         }
      } while (((5 % (Math.max(7, alertI))) >= 2) && unselectedt);
      distw = `${fullm.length}`;
      if (downarrowO) {
         break;
      }
   } while ((!distw.endsWith(`${hearto.size}`)) && downarrowO);
       let matchesc = 0.0;
       let eventsplashi: Array<any> = [246, 849];
          let n_animationj = String.fromCharCode(99,111,117,110,116,115,95,50,95,51,49,0);
          let loginD = String.fromCharCode(99,95,56,95,99,97,112,105,116,97,108,105,122,97,116,105,111,110,0);
          let basketballdetailsbgy = false;
         eventsplashi.push(loginD.length + 3);
         n_animationj += `${((basketballdetailsbgy ? 3 : 4))}`;
         loginD += "1";
         matchesc += parseInt(`${matchesc}`) % 3;
          let placeholderX = false;
         matchesc -= ((placeholderX ? 5 : 1) >> (Math.min(Math.abs(parseInt(`${matchesc}`)), 4)));
         eventsplashi.push(1 + eventsplashi.length);
      let dependencies3 = eventsplashi.length >= 8581250;
      do {
          let chinasamec = String.fromCharCode(104,95,55,56,95,97,112,115,0);
          let package_uo8 = String.fromCharCode(98,111,116,116,111,109,95,50,95,56,52,0);
          let inactiveH = String.fromCharCode(112,95,49,54,95,111,112,116,105,111,110,97,108,108,121,0);
          let sliderA = 2.0;
          let sigmob7 = 0.0;
         eventsplashi.push(eventsplashi.length << (Math.min(5, Math.abs(parseInt(`${sliderA}`)))));
         chinasamec = `${(inactiveH == String.fromCharCode(51,0) ? package_uo8.length : inactiveH.length)}`;
         package_uo8 = `${(inactiveH == String.fromCharCode(98,0) ? parseInt(`${sigmob7}`) : inactiveH.length)}`;
         sliderA /= Math.max(4, chinasamec.length);
         sigmob7 += (parseFloat(`${String.fromCharCode(98,0) == chinasamec ? chinasamec.length : parseInt(`${sigmob7}`)}`));
         if (dependencies3) {
            break;
         }
      } while ((1.92 > (matchesc * 2.31) || 1.66 > (matchesc * 2.31)) && dependencies3);
         eventsplashi = [eventsplashi.length];
      referrerQ = `${fullm.length}`;
   if (referrerQ.includes(`${selectedj}`)) {
       let goala: Array<any> = [700, 513, 204];
       let analyticsY = String.fromCharCode(121,95,54,51,95,109,98,117,118,0);
       let binddatasd: Array<any> = [369, 192];
       let footballfiledlayout_ = 2;
         binddatasd.push(footballfiledlayout_ / (Math.max(analyticsY.length, 9)));
      for (let z = 0; z < 3; z++) {
         footballfiledlayout_ <<= Math.min(Math.abs(analyticsY.length >> (Math.min(Math.abs(3), 1))), 2);
      }
      for (let e = 0; e < 2; e++) {
          let libruntimeexecutorW = 0.0;
          let promotionD: Array<any> = [752, 588, 433];
          let iconwatchd = String.fromCharCode(100,97,112,112,115,95,49,95,51,54,0);
         binddatasd = [parseInt(`${libruntimeexecutorW}`) & iconwatchd.length];
         libruntimeexecutorW += 3;
         promotionD.push(promotionD.length);
      }
          let detailB = 1.0;
         binddatasd = [goala.length & footballfiledlayout_];
         detailB -= parseFloat(`${parseInt(`${detailB}`) | parseInt(`${detailB}`)}`);
         goala = [1];
      for (let f = 0; f < 1; f++) {
         binddatasd.push(analyticsY.length);
      }
       let whiteb = String.fromCharCode(112,95,57,49,95,117,110,115,101,108,101,99,116,101,100,0);
       let pressurea = String.fromCharCode(99,95,56,48,95,111,112,116,105,111,110,0);
          let typingloading_ = 3.0;
          let dplush = false;
          let armvaL = String.fromCharCode(120,95,56,52,95,118,112,100,97,116,97,0);
         analyticsY = `${analyticsY.length | pressurea.length}`;
         typingloading_ += 1 % (Math.max(parseInt(`${typingloading_}`), 4));
         dplush = armvaL.length > 43 && !dplush;
         armvaL += `${(parseInt(`${typingloading_}`) - (dplush ? 5 : 2))}`;
      if (1 > binddatasd.length) {
         binddatasd.push(analyticsY.length - 3);
      }
         pressurea = `${goala.length % (Math.max(2, analyticsY.length))}`;
          let predictiondefaultH: Array<any> = [736, 258, 281];
          let pausev: Array<any> = [699, 163];
         footballfiledlayout_ -= pausev.length + 2;
         predictiondefaultH.push(predictiondefaultH.length - 1);
         pausev = [3];
      referrerQ = `${binddatasd.length / (Math.max(distw.length, 2))}`;
   }
   while (3.21 == (parseFloat(`${sendp.length}`) * successd) || (successd * 3.21) == 3.80) {
       let container9 = true;
       let modulesC = String.fromCharCode(115,112,105,108,108,95,118,95,57,48,0);
          let flag9: Map<any, any> = new Map([[String.fromCharCode(120,95,53,57,0),687], [String.fromCharCode(97,95,50,53,95,100,105,97,108,111,103,117,101,115,0),974]]);
         modulesC = `${modulesC.length}`;
         flag9 = new Map([[`${flag9.size}`, flag9.size ^ flag9.size]]);
      if (!container9) {
          let iconrefresh1 = true;
          let tumbnail8 = 2;
         modulesC += "2 ^ tumbnail8";
         iconrefresh1 = (iconrefresh1 ? iconrefresh1 : !iconrefresh1);
         tumbnail8 <<= Math.min(Math.abs(((iconrefresh1 ? 4 : 2) | (iconrefresh1 ? 2 : 1))), 4);
      }
      successd -= parseFloat(`${whiteanimationliveF.length << (Math.min(Math.abs(3), 3))}`);
      break;
   }
   let homeiconq = whiteanimationliveF.length >= 8171642;
   do {
       let shrinkz = 3.0;
       let ewardedG: Array<any> = [919, 759, 118];
       let fieldg = 4.0;
       let reward0: Map<any, any> = new Map([[String.fromCharCode(109,100,97,121,95,111,95,49,52,0),true ], [String.fromCharCode(99,111,97,108,101,115,99,101,95,57,95,55,0),true ], [String.fromCharCode(104,95,55,49,95,117,110,114,101,118,101,114,115,101,100,0),false ]]);
      while ((parseInt(`${shrinkz}`) / (Math.max(ewardedG.length, 6))) <= 4) {
          let orangeclockd = String.fromCharCode(101,95,57,48,95,99,111,109,112,97,114,101,0);
          let whatsappX: Map<any, any> = new Map([[String.fromCharCode(116,104,101,110,95,48,95,50,51,0),492], [String.fromCharCode(105,95,56,0),282], [String.fromCharCode(116,109,109,98,110,95,101,95,51,48,0),174]]);
         ewardedG.push(3 & parseInt(`${fieldg}`));
         orangeclockd += `${orangeclockd.length}`;
         whatsappX.set(`${orangeclockd}`, whatsappX.size);
         break;
      }
         ewardedG.push(reward0.size - 3);
         reward0 = new Map([[`${reward0.size}`, parseInt(`${fieldg}`)]]);
      while (ewardedG.length < reward0.size) {
         ewardedG = [ewardedG.length];
         break;
      }
          let hover7 = 2.0;
          let starr = String.fromCharCode(114,101,99,97,108,99,117,108,97,116,105,111,110,95,56,95,54,57,0);
         fieldg *= parseFloat(`${3}`);
         hover7 -= parseFloat(`${starr.length >> (Math.min(Math.abs(2), 1))}`);
         starr += `${starr.length - parseInt(`${hover7}`)}`;
         fieldg -= parseFloat(`${parseInt(`${fieldg}`) + 3}`);
       let bgvipsportP = String.fromCharCode(109,97,99,101,95,110,95,50,48,0);
          let plash8 = 3.0;
          let mbbidA = 2.0;
         fieldg += parseFloat(`${bgvipsportP.length - 3}`);
         plash8 += parseInt(`${mbbidA}`) / (Math.max(parseInt(`${plash8}`), 10));
         mbbidA += parseFloat(`${parseInt(`${mbbidA}`) / 3}`);
      for (let o = 0; o < 2; o++) {
          let libzeusk: Array<any> = [116, 385];
          let formi: Array<any> = [372, 91];
          let arrowright7: Map<any, any> = new Map([[String.fromCharCode(115,104,114,117,110,107,95,51,95,55,49,0),String.fromCharCode(105,100,99,116,95,55,95,51,50,0)], [String.fromCharCode(113,95,53,54,95,116,97,112,101,0),String.fromCharCode(107,95,52,54,95,115,105,103,110,114,97,110,100,0)], [String.fromCharCode(118,95,53,95,115,113,108,105,116,101,99,104,97,110,103,101,115,101,116,0),String.fromCharCode(115,116,105,99,107,101,114,101,100,95,111,95,54,55,0)]]);
          let iconclosewhitewithbgu = true;
          let mbbid9 = 3;
         fieldg -= parseFloat(`${formi.length + ewardedG.length}`);
         libzeusk.push(libzeusk.length % (Math.max(4, mbbid9)));
         formi.push(arrowright7.size);
         arrowright7.set(`${mbbid9}`, arrowright7.size ^ 3);
         iconclosewhitewithbgu = iconclosewhitewithbgu || arrowright7.size == 92;
      }
       let mbsplashL = String.fromCharCode(100,109,97,120,95,121,95,50,56,0);
       let indicatorG = String.fromCharCode(110,95,54,51,95,105,116,101,114,97,116,105,111,110,115,0);
         shrinkz -= 3 / (Math.max(7, ewardedG.length));
         ewardedG = [mbsplashL.length];
      whiteanimationliveF = `${referrerQ.length}`;
      if (homeiconq) {
         break;
      }
   } while ((iconarrowrightorangeW == 3) && homeiconq);
   if (5 > (4 * hearto.size)) {
       let basketballdetailsbgU = 0.0;
       let middlewareC = false;
       let footballfiledlayoutr = 2.0;
      while (middlewareC) {
         footballfiledlayoutr /= Math.max(((middlewareC ? 4 : 3) / (Math.max(parseInt(`${basketballdetailsbgU}`), 9))), 3);
         break;
      }
          let friendsO = String.fromCharCode(114,95,56,54,95,116,105,108,101,115,0);
          let predictionbannersharedM = String.fromCharCode(101,108,105,109,105,110,97,116,105,111,110,95,53,95,56,52,0);
         footballfiledlayoutr *= friendsO.length & 1;
         friendsO = `${3 << (Math.min(1, predictionbannersharedM.length))}`;
         predictionbannersharedM = `${(predictionbannersharedM == String.fromCharCode(121,0) ? predictionbannersharedM.length : predictionbannersharedM.length)}`;
         basketballdetailsbgU /= Math.max((parseInt(`${basketballdetailsbgU}`) - (middlewareC ? 5 : 3)), 2);
          let chartI: Array<any> = [748, 187, 918];
          let bings = String.fromCharCode(112,95,56,95,98,101,110,101,102,105,99,105,97,114,121,0);
         basketballdetailsbgU /= Math.max(1, (String.fromCharCode(77,0) == bings ? bings.length : parseInt(`${footballfiledlayoutr}`)));
         chartI.push(chartI.length | chartI.length);
      for (let e = 0; e < 2; e++) {
         footballfiledlayoutr += 3 / (Math.max(parseInt(`${footballfiledlayoutr}`), 2));
      }
          let videocommonP = 0.0;
         footballfiledlayoutr /= Math.max(5, (parseInt(`${basketballdetailsbgU}`) >> (Math.min(2, Math.abs((middlewareC ? 4 : 3))))));
         videocommonP /= Math.max(parseFloat(`${parseInt(`${videocommonP}`)}`), 5);
       let teamr: Map<any, any> = new Map([[String.fromCharCode(117,95,52,50,95,115,98,108,111,103,0),500], [String.fromCharCode(108,95,49,52,95,100,101,115,116,0),687], [String.fromCharCode(112,95,50,48,95,115,105,103,102,105,103,0),637]]);
          let ucopy_jcP = 0.0;
          let libreactnativeblobE = String.fromCharCode(101,95,54,54,95,97,98,108,0);
         footballfiledlayoutr *= ((middlewareC ? 1 : 2) ^ 1);
         ucopy_jcP *= libreactnativeblobE.length & 3;
         libreactnativeblobE = `${(String.fromCharCode(53,0) == libreactnativeblobE ? parseInt(`${ucopy_jcP}`) : libreactnativeblobE.length)}`;
      for (let i = 0; i < 3; i++) {
          let filtern: Map<any, any> = new Map([[String.fromCharCode(115,95,49,48,95,98,105,110,107,100,97,116,97,0),83], [String.fromCharCode(101,95,53,55,95,106,99,115,97,109,112,108,101,0),242], [String.fromCharCode(107,105,110,100,95,116,95,55,49,0),806]]);
         footballfiledlayoutr += parseInt(`${footballfiledlayoutr}`) & teamr.size;
         filtern.set(`${filtern.size}`, filtern.size + filtern.size);
      }
      hearto = new Map([[sendp, whiteanimationliveF.length >> (Math.min(Math.abs(2), 4))]]);
   }
      sendp = `${parseInt(`${successd}`)}`;
   if (1 >= iconarrowrightorangeW) {
      icondownimgy = sendp.length < hearto.size;
   }
   for (let t = 0; t < 3; t++) {
       let iconarrowlefts = false;
      while (iconarrowlefts && !iconarrowlefts) {
         iconarrowlefts = !iconarrowlefts || !iconarrowlefts;
         break;
      }
      let gradleI = iconarrowlefts ? !iconarrowlefts : iconarrowlefts;
      do {
          let proxyd: Array<any> = [894, 193];
          let arrowupb = 5.0;
          let leaguedetailsbgD = 0;
         iconarrowlefts = !iconarrowlefts || leaguedetailsbgD == 75;
         proxyd = [parseInt(`${arrowupb}`) ^ 3];
         arrowupb += proxyd.length * parseInt(`${arrowupb}`);
         leaguedetailsbgD <<= Math.min(3, Math.abs(proxyd.length % (Math.max(2, 8))));
         if (gradleI) {
            break;
         }
      } while ((!iconarrowlefts && !iconarrowlefts) && gradleI);
      let headerb = iconarrowlefts ? !iconarrowlefts : iconarrowlefts;
      do {
         iconarrowlefts = !iconarrowlefts;
         if (headerb) {
            break;
         }
      } while (headerb && (iconarrowlefts));
      iconrightorangeg *= ((iconarrowlefts ? 1 : 1) >> (Math.min(Math.abs(iconarrowrightorangeW), 2)));
   }
       let incident9 = 4.0;
       let predictionlossD = String.fromCharCode(111,95,56,50,95,97,100,100,114,101,115,115,101,115,0);
      let nodei = predictionlossD == String.fromCharCode(106,51,56,102,107,103,111,51,117,0);
      do {
         predictionlossD += "2";
         if (nodei) {
            break;
         }
      } while (((5 + parseInt(`${incident9}`)) <= 4 && (5 & predictionlossD.length) <= 3) && nodei);
      let productV = predictionlossD.length <= 5521850;
      do {
          let previewJ = 3;
          let internetH = 3.0;
         predictionlossD = `${predictionlossD.length}`;
         previewJ &= previewJ ^ parseInt(`${internetH}`);
         internetH -= 2 | previewJ;
         if (productV) {
            break;
         }
      } while ((5.30 > (incident9 - 3.96) && 3.96 > (incident9 - parseFloat(`${predictionlossD.length}`))) && productV);
         predictionlossD += `${parseInt(`${incident9}`)}`;
      while (predictionlossD.length < 3) {
         incident9 -= parseFloat(`${3}`);
         break;
      }
          let r_unlockb = 0.0;
          let greyarrowupO = String.fromCharCode(118,95,56,52,95,104,97,115,104,116,97,98,108,101,122,0);
          let leakcheckerX: Map<any, any> = new Map([[String.fromCharCode(101,120,99,101,101,100,101,100,95,50,95,56,56,0),374], [String.fromCharCode(109,95,54,54,95,103,114,97,112,104,113,108,0),699]]);
         predictionlossD += `${parseInt(`${r_unlockb}`) / (Math.max(predictionlossD.length, 9))}`;
         r_unlockb += greyarrowupO.length * leakcheckerX.size;
         greyarrowupO = `${greyarrowupO.length % (Math.max(10, leakcheckerX.size))}`;
         predictionlossD += `${predictionlossD.length}`;
      sendp = `${referrerQ.length}`;

    setShowReferrerModal(true);
  }

  const onCopyReferralCode = () => {
       let foregroundM: Map<any, any> = new Map([[String.fromCharCode(119,95,51,52,95,105,110,99,108,117,100,105,110,103,0),String.fromCharCode(100,95,49,50,95,102,111,114,109,97,116,0)], [String.fromCharCode(116,95,49,54,95,109,106,112,101,103,101,110,99,0),String.fromCharCode(101,114,114,111,114,118,95,120,95,53,57,0)], [String.fromCharCode(112,116,111,110,95,99,95,50,54,0),String.fromCharCode(101,95,51,53,95,101,120,112,105,114,97,116,105,111,110,0)]]);
    let modelsp = 1;
    let thailandj = 1;
    let nbatrophyv = 2.0;
    let download8: Array<any> = [453, 907, 828];
    let mimoP = 2.0;
    let shootM = String.fromCharCode(98,95,49,48,95,117,110,99,114,111,112,112,101,100,0);
    let gifgoalbg_ = 3.0;
   for (let d = 0; d < 3; d++) {
       let applicationF = String.fromCharCode(103,115,117,98,95,50,95,53,50,0);
       let mbridgeJ: Array<any> = [String.fromCharCode(100,105,115,99,111,110,110,101,99,116,101,100,95,48,95,51,54,0), String.fromCharCode(112,114,105,118,97,116,101,95,57,95,50,56,0), String.fromCharCode(109,95,49,56,95,97,108,97,119,0)];
       let orangetickw = 0.0;
       let mailX = String.fromCharCode(119,95,49,48,95,108,111,116,116,105,101,0);
         mailX = `${applicationF.length}`;
      if ((mbridgeJ.length & 5) >= 2) {
         mbridgeJ.push(mbridgeJ.length >> (Math.min(1, Math.abs(parseInt(`${orangetickw}`)))));
      }
         orangetickw *= parseFloat(`${parseInt(`${orangetickw}`) + 2}`);
      while ((orangetickw + 4.89) >= 4.25) {
         mailX += `${(String.fromCharCode(87,0) == mailX ? mbridgeJ.length : mailX.length)}`;
         break;
      }
          let iconeye8 = true;
          let imagemanagerh = String.fromCharCode(112,97,99,107,101,116,104,101,97,100,101,114,95,109,95,54,54,0);
         mbridgeJ.push((String.fromCharCode(89,0) == applicationF ? applicationF.length : (iconeye8 ? 1 : 2)));
         iconeye8 = (imagemanagerh.length << (Math.min(imagemanagerh.length, 2))) >= 75;
          let eyeopenb = 4.0;
         applicationF = `${mailX.length}`;
         eyeopenb /= Math.max(2, parseFloat(`${parseInt(`${eyeopenb}`) / 2}`));
         orangetickw /= Math.max(parseFloat(`${1}`), 1);
      if (1 > (mbridgeJ.length % (Math.max(applicationF.length, 10))) || (applicationF.length % (Math.max(1, 10))) > 3) {
          let star_ = String.fromCharCode(98,95,55,52,95,110,111,114,109,97,108,0);
          let referrerA = String.fromCharCode(101,95,55,51,95,116,109,105,120,0);
          let appsT = true;
          let stringsW = 2.0;
         applicationF = `${parseInt(`${stringsW}`) / (Math.max(applicationF.length, 3))}`;
         star_ = `${3 | referrerA.length}`;
         referrerA += `${((appsT ? 3 : 2))}`;
         stringsW /= Math.max(parseFloat(`${2}`), 2);
      }
         mailX += `${mbridgeJ.length}`;
         mbridgeJ = [applicationF.length];
      if (4 >= (4 ^ mbridgeJ.length) && (mbridgeJ.length ^ applicationF.length) >= 4) {
         mbridgeJ = [mbridgeJ.length * 3];
      }
      for (let n = 0; n < 3; n++) {
         mailX += "2";
      }
      download8.push(thailandj);
   }
   for (let c = 0; c < 3; c++) {
      foregroundM = new Map([[`${modelsp}`, modelsp]]);
   }
   if ((download8.length << (Math.min(Math.abs(2), 3))) == 5 || 5.16 == (mimoP * 5.34)) {
      download8.push(thailandj);
   }
   while (1.17 > nbatrophyv) {
      nbatrophyv /= Math.max(2, parseInt(`${nbatrophyv}`) ^ download8.length);
      break;
   }
   let positionfieldx = 8938894 <= download8.length;
   do {
      download8 = [download8.length % (Math.max(8, shootM.length))];
      if (positionfieldx) {
         break;
      }
   } while ((2.52 > (3.8 + nbatrophyv) && (download8.length ^ 5) > 3) && positionfieldx);
      thailandj |= 1 | shootM.length;
      mimoP *= modelsp | 3;
      thailandj >>= Math.min(3, download8.length);
      modelsp %= Math.max(3, shootM.length / 1);
      download8 = [2 - parseInt(`${gifgoalbg_}`)];
      foregroundM.set(`${mimoP}`, parseInt(`${mimoP}`) & 3);
   while ((modelsp + nbatrophyv) > 4.9) {
       let lighti = true;
       let middlesoundn = String.fromCharCode(122,95,49,53,95,107,105,110,100,0);
          let delegate_pb = String.fromCharCode(118,95,50,51,95,111,95,50,50,0);
          let strs = 4.0;
          let arrowdownX = String.fromCharCode(100,95,53,57,95,97,117,116,111,100,111,119,110,108,111,97,100,0);
         lighti = delegate_pb == arrowdownX;
         delegate_pb = `${parseInt(`${strs}`) / (Math.max(parseInt(`${strs}`), 7))}`;
          let servicef = String.fromCharCode(106,95,49,48,48,95,108,105,98,111,112,101,110,104,0);
          let dependencies7: Array<any> = [883, 704, 771];
          let iconstar0 = true;
         lighti = dependencies7.includes(iconstar0);
         servicef = `${2 << (Math.min(3, servicef.length))}`;
         dependencies7.push(servicef.length);
      while (middlesoundn.startsWith(`${lighti}`)) {
          let videox = 0.0;
          let penaltyshootR = String.fromCharCode(107,101,121,108,101,110,95,116,95,56,49,0);
          let iconshareo = String.fromCharCode(117,95,51,51,95,113,117,97,110,116,105,122,97,116,105,111,110,0);
          let floatert: Map<any, any> = new Map([[String.fromCharCode(115,116,114,117,99,116,115,95,106,95,51,56,0),String.fromCharCode(97,115,99,111,110,102,95,112,95,57,50,0)], [String.fromCharCode(108,111,99,107,109,103,114,95,120,95,54,56,0),String.fromCharCode(116,95,50,49,95,115,105,110,0)]]);
          let mbridgeN = true;
         lighti = !mbridgeN;
         videox -= parseFloat(`${1}`);
         penaltyshootR += `${2 - floatert.size}`;
         iconshareo += `${iconshareo.length % 1}`;
         floatert.set(iconshareo, 1 * iconshareo.length);
         break;
      }
      let circleL = middlesoundn == String.fromCharCode(49,100,50,115,120,121,117,98,0);
      do {
         middlesoundn += `${((lighti ? 3 : 4) ^ 3)}`;
         if (circleL) {
            break;
         }
      } while ((3 == middlesoundn.length) && circleL);
         lighti = !lighti;
       let predictionwinN = false;
      nbatrophyv -= middlesoundn.length / (Math.max(1, 8));
      break;
   }

    Clipboard.setString(userState.user?.userReferralCode ?? '');

   if (!shootM.endsWith(`${thailandj}`)) {
      thailandj &= download8.length * 3;
   }
   let statisticsJ = 6876006 >= modelsp;
   do {
      modelsp /= Math.max(3, 3 >> (Math.min(Math.abs(parseInt(`${gifgoalbg_}`)), 4)));
      if (statisticsJ) {
         break;
      }
   } while (statisticsJ && (2 >= (5 | modelsp) || (5 | modelsp) >= 5));
   for (let g = 0; g < 1; g++) {
      foregroundM.set(shootM, (String.fromCharCode(72,0) == shootM ? shootM.length : foregroundM.size));
   }
   while (nbatrophyv == 2.68) {
      nbatrophyv /= Math.max(3, shootM.length - thailandj);
      break;
   }
      download8.push(3 % (Math.max(2, download8.length)));
   if (1 < (5 / (Math.max(2, modelsp))) && 4 < (foregroundM.size / 5)) {
       let default_bsC: Map<any, any> = new Map([[String.fromCharCode(104,95,55,56,95,103,115,116,114,105,110,103,0),false ], [String.fromCharCode(112,95,57,52,95,112,111,108,121,0),true ], [String.fromCharCode(106,95,54,51,95,117,115,114,99,0),true ]]);
       let libimagepipelineF = 5.0;
       let backiconmask_ = 0.0;
       let notificationA: Array<any> = [786, 686, 504];
       let foregroundX = String.fromCharCode(117,95,54,52,95,111,102,102,101,114,101,100,0);
      while (5.15 <= (libimagepipelineF + 4.70)) {
         notificationA = [notificationA.length << (Math.min(Math.abs(1), 3))];
         break;
      }
       let bridgeH = 3.0;
      for (let u = 0; u < 1; u++) {
         bridgeH += parseFloat(`${2 & parseInt(`${bridgeH}`)}`);
      }
         notificationA.push(1 << (Math.min(5, notificationA.length)));
       let bellN = 3.0;
       let s_manager_ = 3.0;
         bellN += parseFloat(`${parseInt(`${backiconmask_}`)}`);
         bridgeH -= parseFloat(`${parseInt(`${libimagepipelineF}`) / 2}`);
         bridgeH -= parseFloat(`${parseInt(`${backiconmask_}`) - 3}`);
         backiconmask_ -= parseFloat(`${2 ^ parseInt(`${libimagepipelineF}`)}`);
         libimagepipelineF += 3;
       let gifgoalbgl = 5.0;
       let userr = 4.0;
         bridgeH += parseFloat(`${1}`);
         backiconmask_ /= Math.max(3, parseFloat(`${2}`));
      let philippinesN = foregroundX.length >= 7162945;
      do {
          let footballi = String.fromCharCode(112,114,111,118,105,100,101,114,95,108,95,57,57,0);
          let storee = 3.0;
          let closes = String.fromCharCode(107,95,49,95,114,101,109,111,118,101,114,0);
          let dragd = 2;
         foregroundX = `${parseInt(`${bridgeH}`)}`;
         footballi = `${2 - footballi.length}`;
         storee -= 3 << (Math.min(3, closes.length));
         closes = `${(String.fromCharCode(75,0) == closes ? closes.length : dragd)}`;
         dragd %= Math.max(2, 1);
         if (philippinesN) {
            break;
         }
      } while (philippinesN && (2 >= (foregroundX.length / (Math.max(10, parseInt(`${s_manager_}`))))));
      for (let e = 0; e < 2; e++) {
         bridgeH *= parseFloat(`${foregroundX.length}`);
      }
      foregroundM.set(`${gifgoalbg_}`, 2 * default_bsC.size);
   }
      foregroundM = new Map([[`${foregroundM.size}`, 3 % (Math.max(10, download8.length))]]);
       let bangH = 4;
      while (3 > (bangH / (Math.max(4, bangH)))) {
          let icondefaultthumbnailm = String.fromCharCode(97,95,57,55,95,115,108,111,116,115,0);
          let switch_tok = 5;
          let whitebellb: Array<any> = [String.fromCharCode(118,97,100,107,104,122,95,102,95,50,0), String.fromCharCode(105,110,116,114,112,95,52,95,57,54,0), String.fromCharCode(97,100,106,117,115,116,109,101,110,116,115,95,104,95,49,56,0)];
          let updatesa = 1.0;
          let hoverJ: Map<any, any> = new Map([[String.fromCharCode(101,95,56,52,0),89], [String.fromCharCode(115,111,109,101,116,104,105,110,103,95,112,95,53,57,0),449], [String.fromCharCode(120,95,52,52,95,97,115,105,110,107,0),535]]);
         bangH *= hoverJ.size + switch_tok;
         icondefaultthumbnailm += `${2 >> (Math.min(4, whitebellb.length))}`;
         switch_tok >>= Math.min(2, Math.abs(3 << (Math.min(3, icondefaultthumbnailm.length))));
         whitebellb = [parseInt(`${updatesa}`)];
         updatesa -= icondefaultthumbnailm.length;
         hoverJ.set(`${updatesa}`, 3);
         break;
      }
         bangH %= Math.max(5, bangH);
      if ((bangH << (Math.min(5, Math.abs(bangH)))) < 1 && 3 < (bangH << (Math.min(Math.abs(1), 2)))) {
          let whatsappt = 2;
         bangH += whatsappt;
      }
      thailandj -= 3 + thailandj;
      gifgoalbg_ /= Math.max(parseFloat(`${modelsp}`), 1);
   for (let x = 0; x < 3; x++) {
      thailandj += 2;
   }
      mimoP /= Math.max(1, 1);
   let defaultlogog = mimoP >= 6477622.0;
   do {
      mimoP /= Math.max(foregroundM.size, 3);
      if (defaultlogog) {
         break;
      }
   } while ((1 < (modelsp + 2)) && defaultlogog);
    setIsDialogOpen(true);
  }

  return (
    <ScreenContainer>
      <TitleWithBackButtonHeader title='个人中心' />
      <View
        style={{
          justifyContent: 'space-between',
          flex: 1,
          paddingHorizontal: 10,
        }}
      >
        <View style={{ marginTop: 30 }}>
          <TouchableOpacity
            style={styles.pressableTextContainer}
            onPress={() => {
              wawaNewarchdefaults.showToast('目前暂不支持更改头像，敬请期待');
            }}
          >
            <Text style={{ fontSize: 16, color: '#9C9C9C' }}>头像</Text>
            <FastImage
              style={{
                height: 34,
                width: 34,
              }}
              source={require('@static/images/iconviewerConfirmation.png')}
            />
          </TouchableOpacity>

          { }
          <TouchableOpacity
            style={styles.pressableTextContainer}
            onPress={onPressUsername}
          >
            <Text style={{ fontSize: 16, color: 'white' }}>
              {username}
            </Text>
          </TouchableOpacity>

          { }
          <View style={styles.pressableTextContainer}>
            <Text style={{ fontSize: 16, color: '#9C9C9C' }}>
              {userState.user?.userEmail !== '' ? userState.user?.userEmail : userState.user?.userPhoneNumber}
            </Text>
          </View>

          { }
          {userState.user?.userReferrerName == '' &&
            userState.user.userAllowUpdateReferral == true &&
            <TouchableOpacity
              style={styles.pressableTextContainer}
              onPress={onPressReferrer}
            >
              <Text style={{ fontSize: 16, color: '#B6B6B6' }}>
                补填邀请码 (只能填写一次)
              </Text>
            </TouchableOpacity>
          }

          {userState.user?.userReferrerName != '' && (
            <View style={styles.pressableTextContainer}>
              <Text style={{ fontSize: 16, color: '#9C9C9C' }}>推介人</Text>

              <View
                style={{
                  flexDirection: 'row',
                  justifyContent: 'center',
                  alignItems: 'center',
                }}
              >
                <Text style={{ fontSize: 14, color: '#9C9C9C' }}>
                  {userState.user?.userReferrerName}
                </Text>
              </View>
            </View>
          )}

          { }
          <TouchableOpacity
            style={styles.pressableTextContainer}
            onPress={onCopyReferralCode}
          >
            <Text style={{ fontSize: 16, color: colors.primary }}>
              {userState.user?.userReferralCode}
            </Text>

            <View
              style={{
                flexDirection: 'row',
                justifyContent: 'center',
                alignItems: 'center',
              }}
            >
              <Text style={{ fontSize: 14, color: '#9C9C9C' }}>
                复制邀请码
              </Text>
              <Image
                style={{
                  height: 27,
                  width: 27,
                  position: 'relative',
                  top: 2,
                }}
                source={require('@static/images/profile/middlebrightnessCombinedMinivod.png')}
              />
            </View>
          </TouchableOpacity>

          <NotificationModal
            onConfirm={toggleOverlay}
            isVisible={isDialogOpen}
            title='复制成功'
            subtitle1=''
            subtitle2=''
            subtitle3=''
          />
        </View>

        <ChangeUsernameModal
          isVisible={isShowUsernameModal}
          handleClose={() => {
            setShowUsernameModal(false);
          }}
          initialUsername={username}
          onSubmitSuccess={onUsernameUpdateSuccess}
        />

        <ChangeReferrerModal
          isVisible={isShowReferrerModal}
          handleClose={() => {
            setShowReferrerModal(false);
          }}
        />
      </View>
    </ScreenContainer>
  );
};

const styles = StyleSheet.create({
  pressableTextContainer: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    backgroundColor: '#1d2023',
    marginTop: 20,
    paddingLeft: 18,
    paddingRight: 13,
    height: 48,
    borderRadius: 8,
    marginBottom: 20,
  },
});
