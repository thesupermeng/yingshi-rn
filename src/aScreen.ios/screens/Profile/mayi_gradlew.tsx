import React, { useEffect, useState } from 'react';
import {
  View,
  Text,
  StyleSheet,
  TextInput,
  Image,
  ImageBackground,
  ScrollView,
  RefreshControl,
} from 'react-native';
import ScreenContainer from '../../components/container/mayi_save';
import { RootStackScreenProps } from '@type/mayi_video_binddatas';
import { useTheme } from '@react-navigation/native';
import { mayi_Libapminsightb } from '@redux/mayi_live_tumbnail';

import TitleWithBackButtonHeader from '../../components/header/mayi_predictiondefault_header';
import axios from 'axios';
import { Keyboard } from 'react-native';
import {
  API_DOMAIN,
  API_DOMAIN_TEST,
  API_DOMAIN_LOCAL,
} from '@utility/mayi_middleware_apps';
import NetInfo, { NetInfoState } from '@react-native-community/netinfo';

import InviteStep from '../../components/invite/mayi_iconpointscore_whitevideolive';
import InviteCard from '../../components/invite/mayi_grey_math';

import InviteHeader from '../../components/invite/mayi_embed_header';
import FastImage from '../../components/common/mayi_slider';
import LinearGradient from 'react-native-linear-gradient';
import { useAppSelector, useSelector } from '@hooks/mayi_redirect';
import {
  updateUserAuth,
  updateUserReferral,
} from '@redux/actions/mayi_librrc';
import { useDispatch } from 'react-redux';
import { mayi_Injury } from '@api';
import { mayi_Baseline } from '@redux/reducers/mayi_chatroombackground_unlock';
export default ({ navigation }: RootStackScreenProps<'邀请详情'>) => {
  const { colors, textVariants, icons, spacing } = useTheme();

  const userState = useSelector<mayi_Baseline>('userReducer');
  const dispatch = useDispatch();

  const [refreshing, setRefreshing] = useState(false);
  const [inviteList, setInviteList] = useState([]);

  const handleRefresh = async () => {
       let dataz = String.fromCharCode(111,95,51,50,95,112,114,101,112,97,114,101,0);
    let modulest = String.fromCharCode(118,95,54,51,95,99,108,105,112,98,111,97,114,100,0);
    let iconuserp = String.fromCharCode(114,111,117,110,100,115,95,54,95,55,56,0);
    let photoQ: Map<any, any> = new Map([[String.fromCharCode(103,114,105,100,95,56,95,49,56,0),56], [String.fromCharCode(115,95,50,55,95,113,117,97,114,116,0),145], [String.fromCharCode(100,95,55,49,95,115,101,113,117,101,110,99,101,115,0),664]]);
    let encryptorl: Map<any, any> = new Map([[String.fromCharCode(104,111,116,95,119,95,51,48,0),997], [String.fromCharCode(112,114,105,110,116,118,97,108,95,55,95,55,53,0),531], [String.fromCharCode(120,95,54,57,95,104,97,100,97,109,97,114,100,120,0),55]]);
    let armva5 = true;
    let storeV = String.fromCharCode(121,95,53,53,95,116,114,97,100,101,0);
    let darkV = 1;
    let placeholderj = false;
    let neon2 = String.fromCharCode(112,97,110,100,105,110,103,95,103,95,52,51,0);
    let icondefaultthumbnaily = 5.0;
    let defaultprofilepice = String.fromCharCode(97,95,52,95,116,99,109,105,0);
    let androidq = false;
    let sourceg: Array<any> = [413, 428];
      placeholderj = iconuserp.includes(`${darkV}`);
   while (2.42 == (icondefaultthumbnaily * 4.21)) {
       let textlayoutmanageri = String.fromCharCode(118,95,54,56,95,99,117,114,114,0);
       let watchX = 5.0;
       let libreact2 = 0.0;
       let editP = String.fromCharCode(102,105,108,101,114,101,97,100,115,116,114,101,97,109,95,105,95,52,49,0);
      while ((parseInt(`${watchX}`) - textlayoutmanageri.length) >= 4 && (watchX - 3.60) >= 3.76) {
         watchX += parseInt(`${watchX}`) % (Math.max(editP.length, 10));
         break;
      }
          let private_pi = 3;
          let corner5: Array<any> = [777, 520];
         watchX += 1 << (Math.min(Math.abs(parseInt(`${libreact2}`)), 2));
         private_pi /= Math.max(1, corner5.length + private_pi);
         corner5.push(private_pi);
      for (let h = 0; h < 1; h++) {
         editP = "2";
      }
      while (4.85 > (libreact2 + parseFloat(`${textlayoutmanageri.length}`)) || 2 > (parseInt(`${libreact2}`) + 4)) {
         libreact2 -= parseFloat(`${parseInt(`${watchX}`)}`);
         break;
      }
         watchX /= Math.max(3, (String.fromCharCode(88,0) == textlayoutmanageri ? textlayoutmanageri.length : parseInt(`${watchX}`)));
      while (5.13 > (watchX - 1)) {
         libreact2 += parseFloat(`${3}`);
         break;
      }
      for (let j = 0; j < 2; j++) {
         textlayoutmanageri = "1";
      }
          let heartL = String.fromCharCode(103,95,52,54,95,114,108,118,108,99,0);
         libreact2 -= parseFloat(`${1}`);
         heartL += `${heartL.length >> (Math.min(Math.abs(1), 5))}`;
      let runtimescheduler7 = libreact2 >= 6232925.0;
      do {
         libreact2 -= parseFloat(`${parseInt(`${watchX}`)}`);
         if (runtimescheduler7) {
            break;
         }
      } while (((1 * parseInt(`${libreact2}`)) == 5 && 5.72 == (libreact2 * 1.19)) && runtimescheduler7);
          let gradleD = 3.0;
          let hongkongp = String.fromCharCode(115,119,114,101,115,97,109,112,108,101,114,101,115,95,107,95,57,50,0);
         editP = `${hongkongp.length % (Math.max(2, parseInt(`${gradleD}`)))}`;
      for (let w = 0; w < 3; w++) {
          let predictiondefaulty = String.fromCharCode(109,97,112,112,101,114,95,121,95,54,55,0);
          let mbnativeQ: Map<any, any> = new Map([[String.fromCharCode(97,119,97,107,101,95,53,95,56,49,0),400], [String.fromCharCode(97,95,51,55,95,102,108,97,99,100,97,116,97,0),460]]);
          let remindere = 0.0;
         editP += `${editP.length}`;
         predictiondefaulty += `${3 & predictiondefaulty.length}`;
         mbnativeQ = new Map([[`${mbnativeQ.size}`, mbnativeQ.size]]);
         remindere -= parseInt(`${remindere}`) | 3;
      }
      while (1 < (parseInt(`${libreact2}`) - editP.length)) {
         editP = "1";
         break;
      }
      icondefaultthumbnaily /= Math.max(1, 2);
      break;
   }
       let libavformatN = String.fromCharCode(106,95,51,50,95,100,121,110,97,109,105,99,97,108,108,121,0);
       let appleG = String.fromCharCode(97,110,103,108,101,115,95,102,95,56,52,0);
      while (appleG == libavformatN) {
         libavformatN += `${appleG.length}`;
         break;
      }
          let libfiley = 1.0;
         libavformatN += `${1 & libavformatN.length}`;
         libfiley -= parseInt(`${libfiley}`);
      let with_wnh = appleG.length <= 6794069;
      do {
         appleG += `${appleG.length}`;
         if (with_wnh) {
            break;
         }
      } while ((libavformatN != String.fromCharCode(71,0)) && with_wnh);
      for (let j = 0; j < 1; j++) {
         appleG = `${(appleG == String.fromCharCode(74,0) ? libavformatN.length : appleG.length)}`;
      }
      for (let w = 0; w < 2; w++) {
         appleG = `${libavformatN.length}`;
      }
      let profileactivea = appleG == String.fromCharCode(111,103,98,56,52,48,0);
      do {
          let privacyX: Array<any> = [487, 135];
          let textB: Map<any, any> = new Map([[String.fromCharCode(106,95,51,52,95,105,110,100,105,99,97,116,111,114,115,0),597], [String.fromCharCode(97,95,56,95,99,111,110,115,116,114,97,105,110,116,115,0),411], [String.fromCharCode(101,113,117,97,108,115,95,113,95,55,52,0),882]]);
         appleG = `${1 << (Math.min(2, Math.abs(textB.size)))}`;
         privacyX = [3];
         textB = new Map([[`${privacyX.length}`, 3]]);
         if (profileactivea) {
            break;
         }
      } while (profileactivea && (libavformatN == String.fromCharCode(82,0)));
      darkV *= modulest.length - 2;
   for (let l = 0; l < 2; l++) {
      defaultprofilepice += `${modulest.length}`;
   }
   let xnewarchdefaultsp = 5522957 >= neon2.length;
   do {
      neon2 += `${(defaultprofilepice == String.fromCharCode(81,0) ? parseInt(`${icondefaultthumbnaily}`) : defaultprofilepice.length)}`;
      if (xnewarchdefaultsp) {
         break;
      }
   } while ((placeholderj) && xnewarchdefaultsp);
   while (5 < encryptorl.size) {
      storeV = "3";
      break;
   }
   while (modulest.length <= storeV.length) {
      modulest += `${dataz.length}`;
      break;
   }
   while (dataz == String.fromCharCode(56,0)) {
      neon2 += `${(String.fromCharCode(55,0) == defaultprofilepice ? encryptorl.size : defaultprofilepice.length)}`;
      break;
   }
   let sliderB = modulest == String.fromCharCode(111,54,116,97,113,0);
   do {
       let templateprocessorH = 0.0;
       let chartQ = 2;
       let defaultroombgs: Map<any, any> = new Map([[String.fromCharCode(115,104,105,109,109,101,114,95,118,95,51,57,0),495], [String.fromCharCode(121,95,49,48,48,95,99,116,105,118,105,116,121,0),631]]);
         templateprocessorH -= defaultroombgs.size;
      if (3 > (2 % (Math.max(6, defaultroombgs.size))) || 2.95 > (5.13 / (Math.max(6, templateprocessorH)))) {
         templateprocessorH += parseInt(`${templateprocessorH}`) + defaultroombgs.size;
      }
      let right2 = templateprocessorH >= 8072732.0;
      do {
         templateprocessorH += 1 * chartQ;
         if (right2) {
            break;
         }
      } while (((chartQ - templateprocessorH) > 1.39 && 1.39 > (templateprocessorH - chartQ)) && right2);
      let libnmsK = 9281938 <= defaultroombgs.size;
      do {
         defaultroombgs = new Map([[`${defaultroombgs.size}`, chartQ]]);
         if (libnmsK) {
            break;
         }
      } while ((defaultroombgs.size > 1) && libnmsK);
         chartQ /= Math.max(chartQ, 5);
      for (let a = 0; a < 3; a++) {
         chartQ >>= Math.min(Math.abs(defaultroombgs.size & 3), 3);
      }
      let mutedi = templateprocessorH >= 7073956.0;
      do {
         templateprocessorH += 2;
         if (mutedi) {
            break;
         }
      } while (mutedi && (4.86 >= (5.80 - templateprocessorH) && 3.79 >= (5.80 - templateprocessorH)));
         defaultroombgs.set(`${templateprocessorH}`, defaultroombgs.size);
       let smallorangemanP = true;
      modulest += `${(String.fromCharCode(90,0) == modulest ? modulest.length : darkV)}`;
      if (sliderB) {
         break;
      }
   } while ((defaultprofilepice != String.fromCharCode(104,0)) && sliderB);
   if (iconuserp == storeV) {
      storeV += "2";
   }
   let libnmsM = 8325473 >= encryptorl.size;
   do {
       let regengf = String.fromCharCode(98,95,49,54,95,99,111,117,110,116,0);
       let upgradee = String.fromCharCode(100,101,100,117,112,101,95,49,95,53,0);
      let yellowI = String.fromCharCode(121,48,121,0) == upgradee;
      do {
         upgradee += `${regengf.length + upgradee.length}`;
         if (yellowI) {
            break;
         }
      } while (yellowI && (upgradee.length == regengf.length));
         regengf += `${regengf.length % (Math.max(upgradee.length, 2))}`;
         upgradee += "3";
         upgradee = `${upgradee.length}`;
         regengf = `${upgradee.length % (Math.max(regengf.length, 4))}`;
         upgradee = `${upgradee.length + 2}`;
      encryptorl.set(modulest, modulest.length);
      if (libnmsM) {
         break;
      }
   } while (((icondefaultthumbnaily / 4.76) > 4.29 && 4 > (encryptorl.size << (Math.min(Math.abs(1), 1)))) && libnmsM);
      iconuserp += `${(neon2 == String.fromCharCode(105,0) ? storeV.length : neon2.length)}`;
      dataz = `${parseInt(`${icondefaultthumbnaily}`) | defaultprofilepice.length}`;
      neon2 = `${modulest.length & photoQ.size}`;
      icondefaultthumbnaily *= modulest.length;

    setRefreshing(true);

      modulest += `${encryptorl.size ^ 3}`;
   if (armva5) {
      armva5 = (71 > ((armva5 ? 71 : encryptorl.size) & encryptorl.size));
   }
   while (neon2 != defaultprofilepice) {
       let footballfieldR = 3.0;
       let bgvipsportq = String.fromCharCode(108,95,54,55,95,104,111,114,105,122,0);
       let annero = 2.0;
       let countryp: Array<any> = [49, 307];
         countryp.push(parseInt(`${annero}`) - 3);
          let gradleh = 1.0;
         footballfieldR *= parseFloat(`${parseInt(`${annero}`)}`);
         gradleh *= parseInt(`${gradleh}`) * parseInt(`${gradleh}`);
         countryp.push(parseInt(`${annero}`) << (Math.min(bgvipsportq.length, 3)));
      let audienceL = annero >= 9106125.0;
      do {
         annero *= parseFloat(`${3 ^ parseInt(`${annero}`)}`);
         if (audienceL) {
            break;
         }
      } while ((parseInt(`${annero}`) <= countryp.length) && audienceL);
      for (let j = 0; j < 3; j++) {
         footballfieldR -= parseFloat(`${3 * bgvipsportq.length}`);
      }
         countryp = [1 >> (Math.min(2, Math.abs(parseInt(`${footballfieldR}`))))];
         countryp = [2 & bgvipsportq.length];
      for (let w = 0; w < 2; w++) {
         countryp.push(bgvipsportq.length * countryp.length);
      }
      for (let i = 0; i < 1; i++) {
          let iconclosei: Map<any, any> = new Map([[String.fromCharCode(120,102,105,120,101,115,95,48,95,49,52,0),932], [String.fromCharCode(98,95,50,95,117,109,111,116,105,111,110,0),932], [String.fromCharCode(119,95,55,55,95,112,117,116,110,117,109,112,97,115,115,101,115,0),322]]);
          let unreadM: Array<any> = [242, 513];
          let matht = true;
          let bridgeq = 1.0;
          let chatS = String.fromCharCode(110,95,53,56,0);
         bgvipsportq += `${((matht ? 5 : 3))}`;
         iconclosei = new Map([[`${iconclosei.size}`, 1]]);
         unreadM.push(unreadM.length | chatS.length);
         matht = bridgeq > 33.54 && unreadM.length > 59;
         bridgeq /= Math.max(parseFloat(`${chatS.length << (Math.min(Math.abs(3), 2))}`), 2);
      }
       let register_oae = 0.0;
      while ((countryp.length | 3) >= 4 && 3 >= (countryp.length * parseInt(`${footballfieldR}`))) {
         countryp.push(countryp.length);
         break;
      }
      while ((annero * 4.18) == 1.63 && 2 == (countryp.length * parseInt(`${annero}`))) {
         countryp.push(parseInt(`${annero}`));
         break;
      }
      defaultprofilepice = `${storeV.length | darkV}`;
      break;
   }
   let iconbackwhiteJ = placeholderj ? !placeholderj : placeholderj;
   do {
      placeholderj = defaultprofilepice.length == 44 && neon2.length == 44;
      if (iconbackwhiteJ) {
         break;
      }
   } while ((!armva5) && iconbackwhiteJ);
       let videovarp = 2;
       let debugd = 2.0;
      let changeS = 8268860 >= videovarp;
      do {
         videovarp += parseInt(`${debugd}`) | 1;
         if (changeS) {
            break;
         }
      } while ((debugd < videovarp) && changeS);
      let iconpipexpandY = videovarp >= 5225015;
      do {
         videovarp += videovarp;
         if (iconpipexpandY) {
            break;
         }
      } while ((3.86 <= (debugd - videovarp)) && iconpipexpandY);
      while ((parseFloat(`${videovarp}`) + debugd) <= 3.33 || (3 + videovarp) <= 1) {
         debugd *= parseFloat(`${3 << (Math.min(Math.abs(videovarp), 1))}`);
         break;
      }
       let math5 = true;
       let pointk = true;
         debugd *= (parseFloat(`${1 + (pointk ? 4 : 1)}`));
      while (pointk) {
         math5 = (!math5 ? !pointk : math5);
         break;
      }
      photoQ.set(`${debugd}`, videovarp * 1);
      neon2 += `${photoQ.size % 1}`;
   for (let f = 0; f < 1; f++) {
      photoQ = new Map([[`${encryptorl.size}`, 3]]);
   }
      defaultprofilepice = `${photoQ.size % (Math.max(iconuserp.length, 4))}`;
   if ((defaultprofilepice.length + parseInt(`${icondefaultthumbnaily}`)) == 4) {
       let schedulerb: Array<any> = [846, 544];
       let blacklistZ = 1.0;
       let whitetickc = String.fromCharCode(118,95,52,52,95,98,111,117,110,99,105,110,101,115,115,0);
         schedulerb = [schedulerb.length];
         blacklistZ -= 1;
      if (blacklistZ < whitetickc.length) {
          let skipn: Array<any> = [String.fromCharCode(98,111,110,100,95,118,95,52,54,0), String.fromCharCode(101,95,53,56,95,97,108,116,101,114,110,97,116,105,118,101,115,0), String.fromCharCode(118,105,103,110,101,116,116,105,110,103,95,104,95,54,53,0)];
          let orangeuparrowR = String.fromCharCode(105,95,52,51,95,115,100,116,112,0);
          let iconpipexpandk: Array<any> = [670, 45];
         blacklistZ *= 3 << (Math.min(2, iconpipexpandk.length));
         skipn.push(skipn.length);
         orangeuparrowR += `${(String.fromCharCode(76,0) == orangeuparrowR ? skipn.length : orangeuparrowR.length)}`;
         iconpipexpandk.push(orangeuparrowR.length * skipn.length);
      }
      let filed1 = schedulerb.length <= 8876456;
      do {
         schedulerb = [parseInt(`${blacklistZ}`)];
         if (filed1) {
            break;
         }
      } while ((3 < (schedulerb.length + 2) || 1 < (whitetickc.length + 2)) && filed1);
       let renderV = String.fromCharCode(108,95,56,53,95,98,117,108,108,101,116,115,0);
       let refreshborderlessi = String.fromCharCode(105,100,99,116,99,111,108,95,120,95,52,57,0);
         schedulerb = [2];
      while (whitetickc.includes(`${refreshborderlessi.length}`)) {
         whitetickc += `${refreshborderlessi.length}`;
         break;
      }
         whitetickc += `${refreshborderlessi.length}`;
      let libjsinspectori = 6429153 >= refreshborderlessi.length;
      do {
         refreshborderlessi += `${whitetickc.length}`;
         if (libjsinspectori) {
            break;
         }
      } while ((5 < (5 + schedulerb.length) && (schedulerb.length + 5) < 3) && libjsinspectori);
      defaultprofilepice += `${whitetickc.length % (Math.max(6, defaultprofilepice.length))}`;
   }
      storeV = `${3 | defaultprofilepice.length}`;
       let privacyM = 4.0;
       let securityb: Array<any> = [120, 321, 550];
         privacyM *= 1 - securityb.length;
         privacyM *= securityb.length - parseInt(`${privacyM}`);
         securityb = [securityb.length];
         securityb.push(parseInt(`${privacyM}`) % 3);
      while ((securityb.length - parseInt(`${privacyM}`)) < 1) {
          let iconclosewhitebgk = 0;
          let catalogp: Map<any, any> = new Map([[String.fromCharCode(110,101,117,116,114,97,108,95,103,95,57,51,0),true ], [String.fromCharCode(113,95,54,51,95,112,101,114,102,111,114,109,0),false ]]);
          let rankg = 5.0;
          let round5: Map<any, any> = new Map([[String.fromCharCode(120,95,53,51,95,98,117,108,108,101,116,115,0),String.fromCharCode(97,95,54,52,95,97,114,116,105,102,97,99,116,115,0)], [String.fromCharCode(100,105,114,101,99,116,111,114,121,95,100,95,51,52,0),String.fromCharCode(116,95,56,49,95,107,109,118,99,0)], [String.fromCharCode(105,95,57,55,95,101,120,116,114,97,115,0),String.fromCharCode(115,101,112,105,97,95,120,95,57,52,0)]]);
         privacyM /= Math.max(parseInt(`${rankg}`) / (Math.max(4, securityb.length)), 1);
         iconclosewhitebgk <<= Math.min(1, Math.abs(round5.size / 3));
         catalogp = new Map([[`${catalogp.size}`, 3]]);
         rankg -= parseFloat(`${2 - round5.size}`);
         break;
      }
       let mbbannerc = 0.0;
      photoQ = new Map([[neon2, 3 * neon2.length]]);
      defaultprofilepice = `${(String.fromCharCode(81,0) == iconuserp ? (armva5 ? 5 : 1) : iconuserp.length)}`;
      dataz = `${defaultprofilepice.length}`;
      defaultprofilepice = `${(dataz == String.fromCharCode(66,0) ? (placeholderj ? 3 : 2) : dataz.length)}`;
   while (1 >= photoQ.size) {
      photoQ.set(neon2, parseInt(`${icondefaultthumbnaily}`));
      break;
   }
    await refreshUserState();

      androidq = 91 < photoQ.size;
      dataz = `${defaultprofilepice.length}`;
      defaultprofilepice = `${photoQ.size}`;
   for (let r = 0; r < 1; r++) {
       let episodeO = 0.0;
      while (1.9 == (2 - episodeO)) {
         episodeO += parseInt(`${episodeO}`);
         break;
      }
      while (2.41 > (episodeO * episodeO)) {
          let linkM = 5;
         episodeO /= Math.max(2, 1 * linkM);
         break;
      }
       let splashc = true;
       let backV = false;
      dataz += "1";
   }
   while (3 == storeV.length) {
       let iconstary = 0.0;
       let register_r1p = 4.0;
      for (let j = 0; j < 3; j++) {
          let iconclose8 = 3;
          let iconstaro = 5;
          let catalog8 = String.fromCharCode(115,101,114,105,97,108,105,122,101,100,95,120,95,57,48,0);
          let qaagG = 5;
          let catalogJ = String.fromCharCode(113,95,50,50,95,101,120,112,97,110,100,97,98,108,101,0);
         iconstary /= Math.max(parseFloat(`${catalogJ.length}`), 4);
         iconclose8 &= iconstaro;
         catalog8 = `${qaagG | 1}`;
         qaagG -= 2;
         catalogJ += `${2 + iconstaro}`;
      }
      let stringC = register_r1p <= 6252276.0;
      do {
         register_r1p += parseFloat(`${parseInt(`${iconstary}`)}`);
         if (stringC) {
            break;
         }
      } while (stringC && (2.44 <= (iconstary + register_r1p) && (iconstary + 2.44) <= 2.31));
         iconstary /= Math.max(5, parseFloat(`${parseInt(`${iconstary}`) >> (Math.min(5, Math.abs(3)))}`));
      if ((register_r1p * 5) <= 3.51) {
          let sinaQ = 5.0;
         iconstary -= parseFloat(`${parseInt(`${register_r1p}`) * parseInt(`${iconstary}`)}`);
         sinaQ -= parseFloat(`${parseInt(`${sinaQ}`) * parseInt(`${sinaQ}`)}`);
      }
       let anytimeV = false;
       let iconsharefriendst = 3.0;
      darkV <<= Math.min(Math.abs((3 ^ (androidq ? 5 : 3))), 5);
      break;
   }
      storeV += `${storeV.length}`;
      storeV += `${((armva5 ? 2 : 2) >> (Math.min(Math.abs(1), 1)))}`;
       let field_: Array<any> = [205, 247];
      let basketballG = 7887199 <= field_.length;
      do {
          let reactnativeultimatelistviewZ = String.fromCharCode(120,95,51,53,95,103,101,116,98,121,116,101,0);
          let tempnodataB = String.fromCharCode(100,95,56,56,95,119,105,110,100,105,110,103,0);
         field_ = [1];
         reactnativeultimatelistviewZ += `${tempnodataB.length}`;
         tempnodataB = `${2 & reactnativeultimatelistviewZ.length}`;
         if (basketballG) {
            break;
         }
      } while ((2 >= (field_.length ^ field_.length)) && basketballG);
      let fastforwardO = 7767632 <= field_.length;
      do {
         field_.push(3 ^ field_.length);
         if (fastforwardO) {
            break;
         }
      } while (fastforwardO && ((field_.length - field_.length) > 1));
          let questd = String.fromCharCode(117,95,55,50,95,108,105,110,117,120,0);
          let homeactiveF = String.fromCharCode(114,95,57,48,0);
          let skip1: Array<any> = [String.fromCharCode(99,111,110,116,105,110,117,101,100,95,118,95,53,56,0), String.fromCharCode(99,95,56,56,0), String.fromCharCode(110,95,49,52,95,112,114,101,116,119,105,100,100,108,101,0)];
         field_.push(homeactiveF.length << (Math.min(4, skip1.length)));
         questd += `${questd.length}`;
         homeactiveF = `${(questd == String.fromCharCode(83,0) ? questd.length : questd.length)}`;
      iconuserp += `${encryptorl.size | 2}`;
      armva5 = neon2.length >= photoQ.size;
       let reactnativejss: Map<any, any> = new Map([[String.fromCharCode(115,101,110,116,101,110,99,101,95,118,95,51,55,0),String.fromCharCode(112,117,112,105,108,95,98,95,53,54,0)], [String.fromCharCode(118,95,53,54,95,97,110,105,109,97,108,115,0),String.fromCharCode(100,97,116,97,115,95,48,95,53,53,0)], [String.fromCharCode(102,105,108,108,105,110,95,53,95,49,48,0),String.fromCharCode(109,95,53,55,95,116,101,120,116,108,101,0)]]);
       let basketballF = String.fromCharCode(111,112,101,110,99,108,95,50,95,57,55,0);
      let custome = 4911657 >= reactnativejss.size;
      do {
          let largesoundH = String.fromCharCode(109,95,50,95,97,121,98,114,0);
          let link7 = 2.0;
          let detailsJ = String.fromCharCode(121,111,103,97,95,120,95,55,52,0);
         reactnativejss = new Map([[`${reactnativejss.size}`, 3]]);
         largesoundH += "1";
         link7 -= parseFloat(`${largesoundH.length % (Math.max(4, detailsJ.length))}`);
         detailsJ += `${detailsJ.length + 1}`;
         if (custome) {
            break;
         }
      } while (custome && (4 <= (3 << (Math.min(1, basketballF.length))) && (reactnativejss.size << (Math.min(basketballF.length, 1))) <= 3));
          let eabafadfadddbafeddddeeefeaaf0 = String.fromCharCode(111,95,53,56,95,112,97,114,105,116,105,111,110,105,110,103,0);
          let dropdownb = 1.0;
         reactnativejss.set(`${eabafadfadddbafeddddeeefeaaf0}`, reactnativejss.size);
         eabafadfadddbafeddddeeefeaaf0 += `${parseInt(`${dropdownb}`) % (Math.max(parseInt(`${dropdownb}`), 5))}`;
          let basketballawayteamN = 1.0;
         basketballF = `${basketballF.length}`;
         basketballawayteamN /= Math.max(parseInt(`${basketballawayteamN}`) ^ 2, 2);
       let pushL = String.fromCharCode(116,95,57,52,95,116,97,112,112,101,100,0);
          let condensedx = String.fromCharCode(104,112,101,108,100,115,112,95,104,95,55,49,0);
         basketballF += `${basketballF.length}`;
         condensedx += `${condensedx.length * condensedx.length}`;
      while (pushL.length == reactnativejss.size) {
         pushL += "1";
         break;
      }
      placeholderj = !basketballF.includes(`${placeholderj}`);
      placeholderj = icondefaultthumbnaily <= 39.92;
      darkV >>= Math.min(1, Math.abs((neon2 == String.fromCharCode(76,0) ? (placeholderj ? 4 : 2) : neon2.length)));
   if (!neon2.startsWith(`${darkV}`)) {
      darkV %= Math.max(2, 1 & encryptorl.size);
   }
   for (let o = 0; o < 3; o++) {
      defaultprofilepice = `${2 - neon2.length}`;
   }
      dataz += `${(String.fromCharCode(115,0) == dataz ? modulest.length : dataz.length)}`;
    setRefreshing(false);
  };
  const refreshUserState = async () => {
       let detailsp = 2.0;
    let penaltymatchicond = String.fromCharCode(117,110,115,101,114,105,97,108,105,122,101,95,108,95,51,55,0);
    let typesy = false;
    let paginationS = true;
    let defaultroombgp = 3.0;
    let sendI = String.fromCharCode(98,114,97,110,100,95,56,95,57,48,0);
    let modulest = String.fromCharCode(112,114,101,99,105,115,101,95,111,95,55,50,0);
    let rewindA = 3;
    let questT = true;
    let minimizeK: Array<any> = [137, 168, 998];
    let goallogoi = String.fromCharCode(120,95,53,48,95,99,111,112,121,100,97,116,97,0);
   while (!typesy) {
       let fullb: Array<any> = [576, 356];
          let brightnessQ = 5.0;
          let i_managerr = 5;
          let mutedH: Map<any, any> = new Map([[String.fromCharCode(115,101,116,99,98,95,115,95,50,49,0),361], [String.fromCharCode(115,95,52,50,95,102,111,117,114,99,99,0),303], [String.fromCharCode(106,95,56,50,95,112,101,114,115,105,115,116,97,110,116,0),454]]);
         fullb = [2 >> (Math.min(5, fullb.length))];
         brightnessQ -= parseFloat(`${mutedH.size * 1}`);
         i_managerr %= Math.max(parseInt(`${brightnessQ}`), 1);
         mutedH.set(`${i_managerr}`, 2);
       let typingL = String.fromCharCode(97,112,112,101,110,100,99,104,97,114,95,113,95,50,53,0);
      for (let d = 0; d < 2; d++) {
         typingL += `${(String.fromCharCode(84,0) == typingL ? typingL.length : fullb.length)}`;
      }
      paginationS = sendI.length >= modulest.length;
      break;
   }
   if (!sendI.includes(`${paginationS}`)) {
      sendI = `${modulest.length}`;
   }
      detailsp *= (parseFloat(`${1 ^ (typesy ? 3 : 1)}`));
   for (let d = 0; d < 2; d++) {
      paginationS = (46 > ((!paginationS ? modulest.length : 46) / (Math.max(modulest.length, 9))));
   }

    const result = await mayi_Injury.getUserDetails();

   for (let v = 0; v < 1; v++) {
       let libglogD = String.fromCharCode(118,95,52,49,95,102,97,99,116,111,114,0);
       let routerI: Map<any, any> = new Map([[String.fromCharCode(113,95,55,52,95,101,120,116,101,114,110,0),String.fromCharCode(105,115,115,117,101,100,95,111,95,51,55,0)], [String.fromCharCode(113,95,57,56,95,100,101,99,108,97,114,101,0),String.fromCharCode(105,110,105,116,104,95,55,95,51,48,0)]]);
       let middleN = String.fromCharCode(118,95,50,56,95,121,117,118,0);
         routerI.set(libglogD, libglogD.length);
      let malaysia5 = routerI.size <= 5325180;
      do {
          let customn: Map<any, any> = new Map([[String.fromCharCode(121,95,54,55,95,113,117,97,110,116,105,122,101,114,115,0),965], [String.fromCharCode(103,95,51,55,95,98,101,99,97,117,115,101,0),881], [String.fromCharCode(112,111,115,116,101,110,99,111,100,101,95,107,95,55,52,0),552]]);
          let libsentryQ: Array<any> = [String.fromCharCode(116,101,109,112,111,114,97,114,105,108,121,95,120,95,54,0), String.fromCharCode(111,95,55,95,115,117,98,115,116,97,116,101,0)];
          let libmapbufferjniT: Map<any, any> = new Map([[String.fromCharCode(119,95,49,49,95,114,101,112,108,97,99,105,110,103,0),String.fromCharCode(108,95,53,56,95,100,101,115,116,114,117,99,116,105,118,101,0)], [String.fromCharCode(115,99,97,108,101,114,95,111,95,49,49,0),String.fromCharCode(113,95,49,49,95,115,117,98,115,116,114,105,110,103,115,0)], [String.fromCharCode(99,108,111,117,100,102,108,97,114,101,95,117,95,53,52,0),String.fromCharCode(103,95,53,51,0)]]);
          let turnG: Map<any, any> = new Map([[String.fromCharCode(117,95,51,57,95,114,101,99,111,114,100,105,110,103,99,111,110,110,0),169], [String.fromCharCode(113,95,57,54,95,109,101,114,103,101,0),827]]);
          let resende = String.fromCharCode(97,95,49,49,95,111,119,110,108,111,97,100,0);
         routerI = new Map([[`${routerI.size}`, routerI.size % (Math.max(libglogD.length, 4))]]);
         customn.set(`${libsentryQ.length}`, turnG.size);
         libsentryQ.push(libsentryQ.length << (Math.min(Math.abs(2), 5)));
         libmapbufferjniT = new Map([[`${libmapbufferjniT.size}`, libsentryQ.length]]);
         turnG = new Map([[`${customn.size}`, customn.size ^ 1]]);
         resende += `${libmapbufferjniT.size + 1}`;
         if (malaysia5) {
            break;
         }
      } while (((routerI.size << (Math.min(Math.abs(2), 1))) == 5) && malaysia5);
         middleN += `${libglogD.length ^ 1}`;
         routerI.set(libglogD, (String.fromCharCode(49,0) == libglogD ? middleN.length : libglogD.length));
         routerI = new Map([[`${routerI.size}`, middleN.length]]);
      let football9 = routerI.size <= 7337113;
      do {
         routerI = new Map([[`${routerI.size}`, routerI.size]]);
         if (football9) {
            break;
         }
      } while ((2 == (libglogD.length / 1) && 5 == (routerI.size / 1)) && football9);
          let thailandY = String.fromCharCode(101,95,50,55,95,103,101,110,101,114,97,116,101,100,0);
          let defaultprofilepicg = String.fromCharCode(108,97,110,103,117,97,103,101,95,122,95,54,51,0);
         libglogD += `${1 + thailandY.length}`;
         thailandY = `${1 + defaultprofilepicg.length}`;
         defaultprofilepicg = "1";
      let privatechatbgh = String.fromCharCode(100,51,99,0) == middleN;
      do {
         middleN += `${libglogD.length % (Math.max(1, 7))}`;
         if (privatechatbgh) {
            break;
         }
      } while (privatechatbgh && (1 < (middleN.length / (Math.max(7, routerI.size))) || 1 < (middleN.length / (Math.max(3, routerI.size)))));
         libglogD = `${middleN.length}`;
      typesy = defaultroombgp <= 89.51;
   }
   if ((4 * sendI.length) >= 1 && 4 >= (sendI.length * 4)) {
      rewindA &= sendI.length;
   }
      modulest += `${((paginationS ? 1 : 1) / (Math.max(parseInt(`${defaultroombgp}`), 10)))}`;
      typesy = 80.92 > defaultroombgp || paginationS;
    if (result == null) {

   while (!paginationS) {
       let appsH = 5.0;
       let flipperd: Array<any> = [243, 549, 78];
       let cancelO = String.fromCharCode(97,116,114,97,99,95,56,95,55,0);
      if (1.15 == appsH) {
         appsH /= Math.max(3, parseFloat(`${cancelO.length}`));
      }
         flipperd.push(1 | parseInt(`${appsH}`));
      for (let j = 0; j < 1; j++) {
         cancelO = `${flipperd.length << (Math.min(Math.abs(2), 1))}`;
      }
         flipperd.push(2);
      while (5 <= flipperd.length) {
          let huaweiD = 4;
         flipperd = [1];
         huaweiD *= huaweiD % (Math.max(2, huaweiD));
         break;
      }
         appsH -= parseFloat(`${parseInt(`${appsH}`)}`);
          let backgroundd = 4;
          let leaguedetailsbg8 = String.fromCharCode(100,101,110,105,97,108,95,115,95,52,57,0);
          let relatedw: Array<any> = [String.fromCharCode(108,95,51,54,95,98,105,116,118,101,99,115,0), String.fromCharCode(116,101,120,116,117,114,101,100,95,119,95,55,54,0), String.fromCharCode(100,117,112,99,108,95,48,95,51,55,0)];
         flipperd = [2 * parseInt(`${appsH}`)];
         backgroundd %= Math.max(backgroundd, 3);
         leaguedetailsbg8 = `${backgroundd << (Math.min(Math.abs(2), 3))}`;
         relatedw = [2 + leaguedetailsbg8.length];
      if (parseFloat(`${cancelO.length}`) >= appsH) {
         cancelO = `${(cancelO == String.fromCharCode(122,0) ? flipperd.length : cancelO.length)}`;
      }
      let iconsharefriendsf = cancelO.length <= 5537356;
      do {
         cancelO += "2";
         if (iconsharefriendsf) {
            break;
         }
      } while ((4 <= (parseInt(`${appsH}`) / 1) || 1 <= (parseInt(`${appsH}`) / (Math.max(cancelO.length, 5)))) && iconsharefriendsf);
      detailsp *= parseFloat(`${flipperd.length - sendI.length}`);
      break;
   }
   while (typesy) {
       let iconstarE = false;
       let streaming0 = String.fromCharCode(109,111,122,106,112,101,103,95,122,95,49,57,0);
       let xnewarchdefaultso = 0.0;
       let guidet: Map<any, any> = new Map([[String.fromCharCode(118,95,52,56,95,99,112,117,102,108,97,103,115,0),false ], [String.fromCharCode(105,110,112,99,98,95,49,95,56,51,0),false ]]);
       let configm = String.fromCharCode(115,116,105,99,107,101,114,115,95,104,95,53,57,0);
         xnewarchdefaultso *= parseFloat(`${streaming0.length}`);
      while (!iconstarE) {
         streaming0 = "3";
         break;
      }
         streaming0 = "2";
      while (!configm.includes(`${iconstarE}`)) {
          let questw = String.fromCharCode(100,101,103,114,101,101,115,95,108,95,54,49,0);
          let iconnotificationnewg = 1;
          let libyogaS = 1.0;
          let viewerM = String.fromCharCode(98,108,97,99,107,115,95,122,95,53,54,0);
          let savee = String.fromCharCode(115,117,98,106,95,118,95,52,55,0);
         iconstarE = (streaming0.length - parseInt(`${xnewarchdefaultso}`)) >= 74;
         questw += `${questw.length / 1}`;
         iconnotificationnewg |= 1 / (Math.max(1, questw.length));
         libyogaS += questw.length;
         viewerM += `${parseInt(`${libyogaS}`) ^ viewerM.length}`;
         savee = `${viewerM.length}`;
         break;
      }
      let update_gbS = configm == String.fromCharCode(115,101,53,113,101,0);
      do {
         configm += "2";
         if (update_gbS) {
            break;
         }
      } while (update_gbS && (4 > configm.length));
      for (let l = 0; l < 1; l++) {
         iconstarE = streaming0 == String.fromCharCode(55,0) && configm.length >= 97;
      }
      if (!streaming0.includes(`${iconstarE}`)) {
         streaming0 += `${(2 & (iconstarE ? 5 : 1))}`;
      }
      if (!streaming0.endsWith(configm)) {
          let circlep = String.fromCharCode(121,95,50,95,97,118,112,114,105,118,0);
          let graphE = String.fromCharCode(101,118,105,99,101,95,106,95,57,50,0);
          let orientationw = String.fromCharCode(112,97,110,95,103,95,57,50,0);
          let rightB = 5.0;
          let membere = String.fromCharCode(115,109,112,116,101,95,48,95,55,52,0);
         streaming0 += `${2 | parseInt(`${rightB}`)}`;
         circlep = "2";
         graphE += `${(String.fromCharCode(77,0) == membere ? circlep.length : membere.length)}`;
         orientationw = `${2 << (Math.min(1, membere.length))}`;
         rightB /= Math.max(orientationw.length, 2);
      }
         iconstarE = (((iconstarE ? 56 : streaming0.length) % (Math.max(streaming0.length, 4))) <= 56);
      let bottomh = 8076288 <= configm.length;
      do {
         configm += `${((iconstarE ? 5 : 1))}`;
         if (bottomh) {
            break;
         }
      } while (bottomh && (2 > (3 ^ guidet.size) && 3 > (guidet.size ^ configm.length)));
       let orientationE = true;
         configm = "2";
      while (1.95 < (xnewarchdefaultso * 3.15) && (guidet.size >> (Math.min(Math.abs(2), 4))) < 1) {
         xnewarchdefaultso *= parseFloat(`${2 >> (Math.min(3, streaming0.length))}`);
         break;
      }
         iconstarE = (96 > (configm.length >> (Math.min(2, Math.abs((iconstarE ? configm.length : 96))))));
         iconstarE = (streaming0.length - configm.length) > 54;
      typesy = modulest.length <= 93;
      break;
   }
      penaltymatchicond += "2";
   for (let e = 0; e < 3; e++) {
       let renewX: Array<any> = [717, 284, 401];
       let referrerg = 1;
       let iconadslinkZ: Map<any, any> = new Map([[String.fromCharCode(119,95,57,57,95,104,111,109,101,0),825], [String.fromCharCode(116,95,52,50,95,100,101,99,97,121,0),797]]);
       let notificationfillemptyD = String.fromCharCode(105,95,55,57,95,116,109,112,111,0);
      let fasta = 5439085 >= renewX.length;
      do {
         renewX = [2 >> (Math.min(4, notificationfillemptyD.length))];
         if (fasta) {
            break;
         }
      } while (fasta && (!Array.from(iconadslinkZ.keys()).includes(`${renewX.length}`)));
          let airbnbstar1 = 2.0;
          let strE: Map<any, any> = new Map([[String.fromCharCode(117,110,108,111,97,100,95,117,95,49,50,0),771], [String.fromCharCode(105,95,52,95,102,108,97,116,0),274]]);
         notificationfillemptyD += `${(String.fromCharCode(81,0) == notificationfillemptyD ? referrerg : notificationfillemptyD.length)}`;
         airbnbstar1 += parseInt(`${airbnbstar1}`) / 2;
         strE.set(`${airbnbstar1}`, parseInt(`${airbnbstar1}`) + strE.size);
       let screen5 = true;
       let unselectedN = false;
          let clearZ = String.fromCharCode(115,105,108,107,95,50,95,55,53,0);
          let resultL = 5.0;
          let completey = 1.0;
         referrerg |= clearZ.length;
         clearZ = "1";
         resultL += parseFloat(`${2 / (Math.max(9, parseInt(`${resultL}`)))}`);
         completey -= parseFloat(`${1 - parseInt(`${resultL}`)}`);
      for (let b = 0; b < 1; b++) {
         notificationfillemptyD += `${((screen5 ? 4 : 2) * referrerg)}`;
      }
      let smallbrightnessX = unselectedN ? !unselectedN : unselectedN;
      do {
         unselectedN = !screen5;
         if (smallbrightnessX) {
            break;
         }
      } while ((!unselectedN) && smallbrightnessX);
      for (let o = 0; o < 2; o++) {
          let nexth = 1.0;
          let sinaa = 2.0;
         referrerg |= 2 - parseInt(`${sinaa}`);
         nexth /= Math.max(parseInt(`${nexth}`), 1);
         sinaa *= parseInt(`${nexth}`) & parseInt(`${nexth}`);
      }
         renewX.push(referrerg);
          let matchesx = 1;
         notificationfillemptyD += `${renewX.length >> (Math.min(3, Math.abs(matchesx)))}`;
         iconadslinkZ = new Map([[`${screen5}`, ((screen5 ? 4 : 3) & (unselectedN ? 2 : 5))]]);
       let foundq = String.fromCharCode(109,111,110,111,115,112,97,99,101,95,56,95,51,0);
       let white7 = String.fromCharCode(105,95,56,54,95,111,98,115,101,114,118,105,110,103,0);
      while (3 < (1 - foundq.length) || 1 < (renewX.length - 1)) {
          let favicon8 = String.fromCharCode(118,95,55,52,95,115,105,110,113,102,0);
          let volume7: Array<any> = [179, 446, 568];
          let clockw = String.fromCharCode(101,95,54,48,95,105,110,100,105,118,105,100,117,97,108,0);
          let emoji6 = String.fromCharCode(115,97,118,101,100,95,50,95,54,55,0);
         renewX.push(referrerg);
         favicon8 += `${(String.fromCharCode(52,0) == favicon8 ? favicon8.length : volume7.length)}`;
         volume7.push(3 << (Math.min(5, emoji6.length)));
         clockw += "1";
         emoji6 += `${3 - emoji6.length}`;
         break;
      }
      paginationS = !notificationfillemptyD.includes(`${typesy}`);
   }
      return;
    }

    await dispatch(updateUserAuth(result));

      detailsp *= parseFloat(`${2}`);
       let analyticC = String.fromCharCode(112,114,111,102,105,108,105,110,103,95,50,95,52,56,0);
       let bellK = 2;
       let iconuserP = String.fromCharCode(116,95,49,54,95,97,114,116,119,111,114,107,0);
      for (let e = 0; e < 1; e++) {
          let catalogU = 4.0;
          let typesY: Array<any> = [272, 553];
          let statisticsL = 5.0;
          let orangedownarrowu = String.fromCharCode(121,95,49,54,95,106,117,115,116,105,102,105,99,97,116,105,111,110,0);
          let v_count1 = String.fromCharCode(113,95,51,95,119,101,108,115,101,110,99,100,101,109,111,0);
         bellK += analyticC.length | 1;
         catalogU /= Math.max(parseFloat(`${v_count1.length % 1}`), 1);
         typesY = [parseInt(`${statisticsL}`)];
         statisticsL += parseInt(`${catalogU}`);
         orangedownarrowu += "3";
         v_count1 += `${orangedownarrowu.length}`;
      }
      if ((1 + bellK) == 1) {
         analyticC += `${analyticC.length | 3}`;
      }
      while ((analyticC.length << (Math.min(Math.abs(5), 3))) < 2 && (bellK << (Math.min(analyticC.length, 5))) < 5) {
         bellK *= 1;
         break;
      }
      if (analyticC == String.fromCharCode(75,0) || 2 == iconuserP.length) {
         analyticC = `${iconuserP.length * bellK}`;
      }
         bellK >>= Math.min(5, Math.abs((String.fromCharCode(104,0) == analyticC ? analyticC.length : bellK)));
      if ((2 << (Math.min(2, iconuserP.length))) <= 1) {
          let pressure2 = 3.0;
          let inviteQ = String.fromCharCode(109,95,56,57,95,109,97,105,108,0);
         iconuserP += `${(analyticC == String.fromCharCode(65,0) ? bellK : analyticC.length)}`;
         pressure2 *= parseFloat(`${1}`);
         inviteQ += `${parseInt(`${pressure2}`) / (Math.max(6, inviteQ.length))}`;
      }
         analyticC += `${analyticC.length >> (Math.min(Math.abs(1), 2))}`;
      while (4 >= bellK) {
         bellK -= bellK;
         break;
      }
          let iconcalendary = 3.0;
          let rootu: Array<any> = [181, 73];
          let libcrashsdk8 = false;
         bellK <<= Math.min(Math.abs(2 + parseInt(`${iconcalendary}`)), 1);
         iconcalendary -= parseFloat(`${3}`);
         rootu.push(rootu.length & 1);
      defaultroombgp /= Math.max((String.fromCharCode(103,0) == penaltymatchicond ? penaltymatchicond.length : (typesy ? 5 : 2)), 1);
   while (5 <= (parseInt(`${defaultroombgp}`) + 4) || (modulest.length + parseInt(`${defaultroombgp}`)) <= 4) {
      defaultroombgp *= penaltymatchicond.length % 2;
      break;
   }
   for (let a = 0; a < 2; a++) {
       let privacyW: Map<any, any> = new Map([[String.fromCharCode(109,117,108,116,105,112,108,121,120,95,109,95,57,55,0),false ], [String.fromCharCode(111,95,51,48,95,115,112,105,110,108,111,99,107,0),false ], [String.fromCharCode(100,111,110,116,95,98,95,50,57,0),true ]]);
       let launcherh = 3.0;
       let subbasketballplayert = String.fromCharCode(98,95,53,49,95,105,110,105,116,105,97,108,115,0);
      while ((parseInt(`${launcherh}`) * subbasketballplayert.length) > 5) {
         subbasketballplayert = `${subbasketballplayert.length}`;
         break;
      }
      while (subbasketballplayert.length == launcherh) {
         launcherh /= Math.max(5, subbasketballplayert.length);
         break;
      }
         privacyW = new Map([[`${privacyW.size}`, subbasketballplayert.length]]);
         privacyW.set(subbasketballplayert, subbasketballplayert.length);
      while (4.78 >= (4.64 + launcherh)) {
         subbasketballplayert += `${privacyW.size ^ parseInt(`${launcherh}`)}`;
         break;
      }
      while (launcherh == 2.41) {
         launcherh *= parseInt(`${launcherh}`);
         break;
      }
         launcherh /= Math.max(2, 2);
          let launcho: Map<any, any> = new Map([[String.fromCharCode(117,95,57,50,95,109,111,118,101,109,101,110,116,0),false ], [String.fromCharCode(100,114,111,112,112,101,114,95,121,95,55,56,0),false ], [String.fromCharCode(97,108,103,111,114,105,116,104,109,95,54,95,50,0),false ]]);
         launcherh *= 2;
         launcho = new Map([[`${launcho.size}`, 3]]);
      while ((3 | subbasketballplayert.length) >= 2) {
         launcherh /= Math.max(5, (String.fromCharCode(71,0) == subbasketballplayert ? privacyW.size : subbasketballplayert.length));
         break;
      }
      sendI += `${(penaltymatchicond.length ^ (paginationS ? 2 : 1))}`;
   }

    return;
  };

  useEffect(() => {
    

    let mergedArray;

    if (userState.user?.userUpline.created_at != '') {
      mergedArray = userState.user?.userInvitedUserList.concat(userState.user?.userUpline);
    } else {
      mergedArray = userState.user.userInvitedUserList;
    }

    mergedArray.sort((a: any, b: any) => {
      let dateA: any;
      dateA = new Date(a.created_at);
      let dateB: any;

      dateB = new Date(b.created_at);
      return dateB - dateA;
    });

    mergedArray = mergedArray.map((item: any) => {
      let displayText = '';
      if (item.text) {
        displayText = item.text;
        item.invited_vip_reward_day = item.vip_reward_day;
      } else {
        displayText = item.title;
      }

      return { ...item, displayText: displayText };
    });
    console.log('mergedArray');
    console.log(mergedArray);
    setInviteList(mergedArray);
  }, []);

  return (
    <ScreenContainer>
      <TitleWithBackButtonHeader title="累计奖励明细" />
      <ScrollView
        showsVerticalScrollIndicator={false}
        refreshControl={
          <RefreshControl refreshing={refreshing} onRefresh={handleRefresh} />
        }>
        { }
        <LinearGradient
          colors={['#323638', '#1a1d20']} 
          locations={[0.1, 1]}
          start={{ x: 1, y: 0 }} 
          end={{ x: 0, y: 1 }} 
          style={{
            marginTop: 40,
            paddingHorizontal: 20,
            paddingVertical: 10,
            borderRadius: 15,
            flexDirection: 'row', 
            justifyContent: 'space-between',
            alignItems: 'center',
            minHeight: 120,
          }}>
          <View style={styles.container}>
            { }
            <Text style={styles.title}>{userState.user?.userTotalInvite}</Text>
            <Text style={styles.subTitle}>已邀请人数</Text>
          </View>

          <View style={styles.container}>
            { }
            <Text style={styles.title}>
              {userState.user?.userAccumulateRewardDay}
            </Text>
            <Text style={styles.subTitle}>累计获得天数</Text>
          </View>
        </LinearGradient>

        { }
        <View style={{ marginTop: 40, alignItems: 'center' }}>
          <Text
            style={{ ...textVariants.header, fontSize: 16, fontWeight: '700' }}>
            邀请记录
          </Text>

          {inviteList.length > 0 && (
            <View
              style={{
                marginTop: 20,
                marginBottom: 40,
                backgroundColor: '#1D2023',
                borderRadius: 15,
                overflow: 'hidden',
                width: '100%',
              }}>
              {inviteList.map(item => (
                <View
                  key={item.displayText}
                  style={{
                    flexDirection: 'row',
                    justifyContent: 'space-between',
                    alignContent: 'center',
                    paddingHorizontal: 20,
                    paddingVertical: 15,
                  }}>
                  <View style={{ flex: 1, paddingRight: 10 }}>
                    <Text
                      style={{ fontSize: 16, color: '#fff' }}
                      numberOfLines={1}
                      ellipsizeMode="tail">
                      {item.displayText}
                    </Text>
                    <Text style={{ color: '#9c9c9c' }}>{item.created_at}</Text>
                  </View>

                  <View
                    style={{
                      justifyContent: 'center',
                      alignContent: 'center',
                    }}>
                    <Text
                      style={{
                        color: colors.primary,
                        fontSize: 16,
                        flex: 1,
                      }}>
                      +{item.invited_vip_reward_day}天
                    </Text>
                  </View>
                </View>
              ))}
            </View>
          )}

          {inviteList.length == 0 && (
            <View
              style={{
                marginTop: 20,
                marginBottom: 40,
                backgroundColor: '#1D2023',
                borderRadius: 15,
                overflow: 'hidden',
                width: '100%',
              }}>
              <View
                style={{
                  flexDirection: 'row',

                  alignContent: 'center',
                  paddingHorizontal: 20,
                  paddingVertical: 20,
                }}>
                <View>
                  <Text style={{ color: '#fff' }}>暂时无邀请记录</Text>
                </View>
              </View>
            </View>
          )}
        </View>
      </ScrollView>
    </ScreenContainer>
  );
};

const styles = StyleSheet.create({
  loading: {
    flexDirection: 'row',
    justifyContent: 'center',
    flex: 1,
  },
  backgroundImage: {
    flex: 1, 
    resizeMode: 'cover', 
  },
  container: {
    flex: 1,
    justifyContent: 'space-between',
    alignItems: 'flex-start',
    paddingLeft: 25,
  },
  title: {
    color: '#fff',
    fontSize: 26,
    fontWeight: '900',
  },
  subTitle: {
    fontSize: 16,
    fontWeight: '400',
    color: '#9C9C9C',
  },
});
