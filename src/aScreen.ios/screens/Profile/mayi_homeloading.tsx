import React, { useMemo, useState } from "react";
import {
  View,
  Text,
  StyleSheet,
  Image,
  Clipboard,
  Platform,
} from "react-native";
import ScreenContainer from "../../components/container/mayi_save";
import { RootStackScreenProps } from "@type/mayi_video_binddatas";
import { useTheme } from "@react-navigation/native";
import TitleWithBackButtonHeader from "../../components/header/mayi_predictiondefault_header";
import { useSelector } from "@hooks/mayi_redirect";
import { TouchableOpacity } from "@gorhom/bottom-sheet";
import NotificationModal from "../../components/modal/mayi_muted_edit";
import { mayi_Attributedstring } from "@utility/mayi_source";
import FastImage from "../../components/common/mayi_slider";
import { ChangeUsernameModal } from "../../components/modal/mayi_newinterstitial_ajax";
import { ChangeReferrerModal } from "../../components/modal/mayi_whiteanimationlive_desc";
import { mayi_Baseline } from "@redux/reducers/mayi_chatroombackground_unlock";

export default ({ navigation }: RootStackScreenProps<"个人中心">) => {
  const { colors, dark } = useTheme();
  const styles = useMemo(() => createStyles({ colors, dark }), [colors, dark]);

  const userState = useSelector<mayi_Baseline>('userReducer');

  const [username, setUsername] = useState(userState.user?.userName ?? '');

  const [isShowUsernameModal, setShowUsernameModal] = useState(false);
  const [isShowReferrerModal, setShowReferrerModal] = useState(false);

  const [isDialogOpen, setIsDialogOpen] = useState(false);


  const toggleOverlay = () => {
       let footballfiledlayouta = 5.0;
    let placementR: Array<any> = [88, 958, 108];
    let backgroundK = String.fromCharCode(119,99,104,97,114,0);
    let imagesh = 3.0;
    let nendl = false;
    let roomB = String.fromCharCode(104,111,110,101,121,115,119,97,112,0);
    let d_imageM = 2;
    let target6 = String.fromCharCode(115,121,115,105,110,102,111,0);
    let castingI = 2.0;
    let sideX = String.fromCharCode(109,117,116,97,98,108,101,0);
    let modulesi: Array<any> = [168, 881];
    let foregroundp = 5.0;
    let selectO = 4.0;
    let eabafadfadddbafeddddeeefeaaf7 = String.fromCharCode(101,116,104,101,114,110,101,116,0);
   while ((footballfiledlayouta / 2.60) < 2.18 && 5 < (sideX.length / (Math.max(5, parseInt(`${footballfiledlayouta}`))))) {
       let defaultplayerimgp = 1;
       let bgvipxvodd = String.fromCharCode(98,111,111,108,101,97,110,0);
      while (bgvipxvodd.length <= 4) {
          let stats4 = 3;
          let push0 = String.fromCharCode(97,110,103,108,101,0);
          let launchq = String.fromCharCode(97,117,116,111,108,111,99,107,0);
         defaultplayerimgp -= push0.length;
         stats4 <<= Math.min(3, launchq.length);
         push0 = `${(String.fromCharCode(106,0) == launchq ? stats4 : launchq.length)}`;
         break;
      }
      if (3 > (bgvipxvodd.length / (Math.max(4, 2)))) {
         defaultplayerimgp |= bgvipxvodd.length - defaultplayerimgp;
      }
      while (defaultplayerimgp >= 1) {
          let eactK = String.fromCharCode(100,117,114,103,101,114,0);
          let current5 = 5;
          let hookV: Array<any> = [286, 891];
         defaultplayerimgp >>= Math.min(Math.abs(eactK.length & current5), 3);
         eactK = `${hookV.length}`;
         current5 >>= Math.min(hookV.length, 5);
         break;
      }
         bgvipxvodd = `${bgvipxvodd.length}`;
      for (let d = 0; d < 2; d++) {
         defaultplayerimgp >>= Math.min(bgvipxvodd.length, 1);
      }
      while ((defaultplayerimgp | bgvipxvodd.length) < 4) {
         bgvipxvodd += `${bgvipxvodd.length}`;
         break;
      }
      sideX += "1";
      break;
   }
   if (sideX.length < castingI) {
      castingI += ((nendl ? 2 : 3) >> (Math.min(target6.length, 5)));
   }
      castingI -= ((nendl ? 5 : 4) | parseInt(`${castingI}`));
   while (backgroundK.length <= 2) {
      castingI /= Math.max(1 - d_imageM, 5);
      break;
   }
       let halffieldimageg = String.fromCharCode(118,98,112,114,105,110,116,102,0);
       let collectiong: Array<any> = [533, 308];
       let showlesso: Array<any> = [String.fromCharCode(115,99,97,108,101,115,0), String.fromCharCode(100,101,108,101,116,105,111,110,115,0), String.fromCharCode(115,99,97,110,0)];
         halffieldimageg += `${collectiong.length % (Math.max(1, 4))}`;
         collectiong.push(2 | showlesso.length);
      if (collectiong.length == 3) {
         halffieldimageg += "3";
      }
      let roundz = 5454775 <= collectiong.length;
      do {
         collectiong.push(2 ^ collectiong.length);
         if (roundz) {
            break;
         }
      } while (((halffieldimageg.length * collectiong.length) >= 3) && roundz);
          let regengd: Array<any> = [String.fromCharCode(98,116,114,101,101,0), String.fromCharCode(110,111,114,109,97,108,105,122,97,116,105,111,110,0)];
         halffieldimageg = `${(String.fromCharCode(73,0) == halffieldimageg ? collectiong.length : halffieldimageg.length)}`;
         regengd = [1];
      sideX += `${3 & halffieldimageg.length}`;
   let agreementL = 7689969.0 <= imagesh;
   do {
      imagesh *= parseFloat(`${1 & target6.length}`);
      if (agreementL) {
         break;
      }
   } while (((3.79 + imagesh) == 5.17 && (3.79 + imagesh) == 2.79) && agreementL);
      backgroundK = `${parseInt(`${foregroundp}`) / (Math.max(modulesi.length, 7))}`;
   let orangeuparrowE = 6796898 >= placementR.length;
   do {
       let owngoalJ = String.fromCharCode(102,114,97,109,101,115,101,116,116,101,114,0);
       let pointS: Array<any> = [606, 371];
       let guide9 = 5;
       let humidity8 = String.fromCharCode(112,114,105,109,101,115,0);
         humidity8 = `${guide9}`;
      if ((guide9 - owngoalJ.length) > 3 && (owngoalJ.length - 3) > 2) {
         owngoalJ = `${owngoalJ.length ^ pointS.length}`;
      }
         pointS.push(2);
          let m_manageri = true;
         humidity8 = `${(humidity8 == String.fromCharCode(51,0) ? guide9 : humidity8.length)}`;
         m_manageri = !m_manageri;
          let codeR = 1.0;
          let roboton = String.fromCharCode(99,97,115,101,100,0);
         pointS = [1];
         codeR += parseFloat(`${parseInt(`${codeR}`) / 3}`);
         roboton += `${(String.fromCharCode(76,0) == roboton ? parseInt(`${codeR}`) : roboton.length)}`;
      let connectionG = pointS.length >= 5741136;
      do {
          let backwhiteO = 0.0;
          let rncoreE = String.fromCharCode(111,117,116,108,105,110,107,0);
          let materialX = 0;
          let uimanagerj: Map<any, any> = new Map([[String.fromCharCode(97,112,112,114,111,118,101,100,0),String.fromCharCode(111,100,100,0)], [String.fromCharCode(115,119,114,101,115,97,109,112,108,101,0),String.fromCharCode(99,105,100,0)]]);
         pointS = [owngoalJ.length | 3];
         backwhiteO /= Math.max(uimanagerj.size % 1, 5);
         rncoreE += `${2 << (Math.min(4, Math.abs(parseInt(`${backwhiteO}`))))}`;
         materialX %= Math.max(parseInt(`${backwhiteO}`) * uimanagerj.size, 4);
         if (connectionG) {
            break;
         }
      } while (connectionG && (2 < (2 | humidity8.length)));
       let projectF = String.fromCharCode(102,105,110,100,97,115,111,99,0);
       let armvab = true;
      while (3 >= (pointS.length % 2)) {
         armvab = 100 >= owngoalJ.length;
         break;
      }
      while (projectF.length < 2) {
          let emojiheartl = String.fromCharCode(115,116,97,116,117,115,111,114,0);
          let screenB = true;
          let countryI: Map<any, any> = new Map([[String.fromCharCode(106,112,101,103,0),963], [String.fromCharCode(105,110,103,101,110,105,101,110,116,0),727]]);
          let iconqqo = String.fromCharCode(119,109,118,100,97,116,97,0);
         projectF += `${emojiheartl.length}`;
         emojiheartl = `${((screenB ? 3 : 5))}`;
         screenB = null != countryI.get(`${screenB}`);
         countryI = new Map([[`${countryI.size}`, ((screenB ? 2 : 2) % (Math.max(1, 6)))]]);
         iconqqo = "3";
         break;
      }
      while ((3 * guide9) >= 1) {
         guide9 ^= 1 / (Math.max(5, projectF.length));
         break;
      }
      let iconschedulem = armvab ? !armvab : armvab;
      do {
         armvab = owngoalJ.length <= 48;
         if (iconschedulem) {
            break;
         }
      } while ((owngoalJ.length <= 3 || !armvab) && iconschedulem);
      placementR = [(backgroundK == String.fromCharCode(87,0) ? backgroundK.length : (nendl ? 4 : 5))];
      if (orangeuparrowE) {
         break;
      }
   } while (orangeuparrowE && (roomB.startsWith(`${placementR.length}`)));
      imagesh -= parseFloat(`${placementR.length / (Math.max(2, 1))}`);
      target6 = `${roomB.length}`;
      sideX = `${(String.fromCharCode(52,0) == roomB ? roomB.length : backgroundK.length)}`;
   for (let i = 0; i < 2; i++) {
      foregroundp *= 1 * parseInt(`${footballfiledlayouta}`);
   }
      imagesh += parseFloat(`${2}`);
   if (modulesi.length >= 5) {
      castingI /= Math.max(3, 1);
   }
      imagesh *= parseFloat(`${2 ^ target6.length}`);
   let shirtt = roomB == String.fromCharCode(120,117,114,111,111,118,104,118,0);
   do {
       let moonu = true;
      for (let r = 0; r < 3; r++) {
         moonu = (moonu ? moonu : moonu);
      }
      for (let r = 0; r < 3; r++) {
         moonu = (!moonu ? !moonu : moonu);
      }
      while (!moonu) {
         moonu = (!moonu ? !moonu : !moonu);
         break;
      }
      roomB = `${parseInt(`${imagesh}`) << (Math.min(1, Math.abs(2)))}`;
      if (shirtt) {
         break;
      }
   } while (shirtt && (parseFloat(`${roomB.length}`) > footballfiledlayouta));
      castingI /= Math.max(placementR.length, 1);
   let handlerK = String.fromCharCode(49,118,49,109,116,0) == sideX;
   do {
      sideX += `${(backgroundK == String.fromCharCode(48,0) ? backgroundK.length : (nendl ? 1 : 3))}`;
      if (handlerK) {
         break;
      }
   } while ((sideX.includes(`${placementR.length}`)) && handlerK);
      sideX = `${((nendl ? 5 : 2))}`;
   let inouttargetredM = foregroundp <= 6209580.0;
   do {
       let bangI = 5.0;
       let googleo: Map<any, any> = new Map([[String.fromCharCode(113,114,99,111,100,101,0),false ], [String.fromCharCode(120,102,97,99,101,0),false ]]);
       let iconclosewhitewithbgD = 0.0;
       let mintegralU = 0;
       let type_6wF = String.fromCharCode(116,104,101,109,101,100,0);
          let refreshborderlessP = 4.0;
          let topon2 = 0.0;
          let circlef: Array<any> = [293, 596, 823];
         bangI -= 3 >> (Math.min(1, Math.abs(mintegralU)));
         refreshborderlessP *= parseFloat(`${parseInt(`${topon2}`)}`);
         circlef = [2];
         type_6wF += `${googleo.size << (Math.min(4, Math.abs(parseInt(`${bangI}`))))}`;
          let boot7 = String.fromCharCode(97,117,100,105,116,105,110,103,0);
          let playercommonl = String.fromCharCode(115,111,109,101,116,104,105,110,103,0);
         bangI *= 3 / (Math.max(4, boot7.length));
         boot7 += `${playercommonl.length ^ 3}`;
         playercommonl = `${playercommonl.length ^ playercommonl.length}`;
       let nterstitiali = true;
      for (let o = 0; o < 1; o++) {
         iconclosewhitewithbgD *= mintegralU / (Math.max(1, type_6wF.length));
      }
         nterstitiali = 26.27 < iconclosewhitewithbgD;
         iconclosewhitewithbgD -= 1 / (Math.max(parseInt(`${iconclosewhitewithbgD}`), 2));
      let agreementj = googleo.size <= 7609130;
      do {
          let encrypt_ = String.fromCharCode(97,116,114,97,99,112,108,117,115,0);
         googleo.set(`${iconclosewhitewithbgD}`, parseInt(`${iconclosewhitewithbgD}`) / (Math.max(googleo.size, 7)));
         encrypt_ = `${encrypt_.length / (Math.max(4, encrypt_.length))}`;
         if (agreementj) {
            break;
         }
      } while (agreementj && (googleo.get(`${iconclosewhitewithbgD}`) == null));
      for (let z = 0; z < 1; z++) {
         iconclosewhitewithbgD += ((nterstitiali ? 5 : 2) - type_6wF.length);
      }
      if (4 > mintegralU) {
         mintegralU |= ((nterstitiali ? 4 : 1) * 1);
      }
          let b_centerJ = 4.0;
         iconclosewhitewithbgD -= type_6wF.length;
         b_centerJ *= parseFloat(`${parseInt(`${b_centerJ}`) << (Math.min(1, Math.abs(parseInt(`${b_centerJ}`))))}`);
      if (1.81 >= (4.31 + iconclosewhitewithbgD) && 5.38 >= (iconclosewhitewithbgD + 4.31)) {
          let combine9 = String.fromCharCode(97,112,112,97,114,101,110,116,0);
          let penaltyshoots = String.fromCharCode(97,117,100,105,98,105,108,105,116,121,0);
          let imagemanagerx = 2;
         iconclosewhitewithbgD += mintegralU % 2;
         combine9 = `${1 - combine9.length}`;
         penaltyshoots = `${(String.fromCharCode(98,0) == combine9 ? combine9.length : penaltyshoots.length)}`;
         imagemanagerx -= (String.fromCharCode(69,0) == combine9 ? penaltyshoots.length : combine9.length);
      }
         iconclosewhitewithbgD *= 1 % (Math.max(9, parseInt(`${iconclosewhitewithbgD}`)));
         nterstitiali = !nterstitiali;
         mintegralU |= 2;
      foregroundp += parseInt(`${footballfiledlayouta}`) << (Math.min(Math.abs(2), 4));
      if (inouttargetredM) {
         break;
      }
   } while (inouttargetredM && ((footballfiledlayouta * foregroundp) == 2.15));
       let commentt = String.fromCharCode(99,111,108,111,114,109,97,112,0);
       let y_image2 = 4;
       let issubx: Array<any> = [584, 281, 836];
         commentt = `${1 + commentt.length}`;
         issubx.push(commentt.length & 2);
      let liveb = y_image2 >= 6879930;
      do {
         y_image2 *= commentt.length;
         if (liveb) {
            break;
         }
      } while (liveb && (issubx.length <= 4));
         issubx = [3];
      if ((y_image2 + 4) <= 1 && (commentt.length + 4) <= 5) {
          let point0 = 3;
         y_image2 += 1;
         point0 >>= Math.min(Math.abs(point0 * 2), 3);
      }
         commentt += `${commentt.length << (Math.min(Math.abs(3), 3))}`;
      let otherM = y_image2 >= 6086671;
      do {
         y_image2 <<= Math.min(commentt.length, 3);
         if (otherM) {
            break;
         }
      } while ((issubx.includes(y_image2)) && otherM);
       let umengY = String.fromCharCode(112,114,111,100,117,99,116,0);
       let heartH = String.fromCharCode(108,101,109,111,110,0);
          let dplusH = 0;
          let iconmoref = String.fromCharCode(115,101,108,101,99,116,111,114,0);
          let homeloadingQ = String.fromCharCode(101,110,100,105,97,110,110,101,115,115,0);
         commentt += `${iconmoref.length}`;
         dplusH /= Math.max(homeloadingQ.length - 1, 5);
         iconmoref += `${dplusH / (Math.max(homeloadingQ.length, 3))}`;
      backgroundK = `${(parseInt(`${imagesh}`) - (nendl ? 5 : 1))}`;
      d_imageM += roomB.length | 1;
   if (5 <= roomB.length) {
      nendl = roomB.startsWith(`${d_imageM}`);
   }
   while (d_imageM >= 2 || 3 >= (2 & d_imageM)) {
      nendl = (placementR.length >> (Math.min(5, Math.abs(d_imageM)))) > 19;
      break;
   }

    setIsDialogOpen(!isDialogOpen);
  };

  const onPressUsername = () => {
       let nendj = String.fromCharCode(118,111,112,101,110,0);
    let template_3pT = String.fromCharCode(99,111,108,111,114,0);
    let trophy6 = String.fromCharCode(109,97,102,113,0);
    let statisticss = 5;
    let predictionwinH = String.fromCharCode(104,105,116,0);
    let statsnomoredataw = String.fromCharCode(99,100,97,116,101,0);
    let bufferY = 0.0;
    let indexg = String.fromCharCode(100,105,115,116,97,110,99,101,115,0);
    let downloaderb = String.fromCharCode(102,114,97,109,101,110,117,109,0);
    let disconnectedC = 0;
      template_3pT = `${parseInt(`${bufferY}`) / (Math.max(template_3pT.length, 6))}`;
       let greyw: Array<any> = [812, 430, 222];
       let tempnodatagif6 = 5;
         greyw = [greyw.length];
         tempnodatagif6 &= 3 >> (Math.min(2, greyw.length));
       let description_j_ = String.fromCharCode(114,105,103,104,116,115,0);
          let arrowrightwithtailZ = String.fromCharCode(97,105,116,101,114,0);
          let redirectF = true;
          let anytimeu = String.fromCharCode(119,97,107,101,117,112,0);
         tempnodatagif6 ^= anytimeu.length;
         arrowrightwithtailZ += `${((redirectF ? 1 : 1))}`;
         redirectF = (60 <= ((redirectF ? arrowrightwithtailZ.length : 60) * arrowrightwithtailZ.length));
         anytimeu += `${arrowrightwithtailZ.length}`;
         description_j_ = `${description_j_.length}`;
      while (4 > description_j_.length) {
         description_j_ += "2 & tempnodatagif6";
         break;
      }
      nendj += `${1 << (Math.min(4, template_3pT.length))}`;
      nendj += `${(template_3pT == String.fromCharCode(99,0) ? template_3pT.length : nendj.length)}`;
       let gestureM = String.fromCharCode(114,101,108,111,97,100,0);
      while (gestureM != gestureM) {
         gestureM = `${gestureM.length / (Math.max(gestureM.length, 4))}`;
         break;
      }
          let iconnotificationnewQ = String.fromCharCode(115,105,100,101,0);
          let mutede: Array<any> = [423, 844, 914];
          let groupv = String.fromCharCode(100,101,99,111,114,114,0);
         gestureM += `${mutede.length / (Math.max(2, 7))}`;
         iconnotificationnewQ += `${groupv.length}`;
         mutede.push(2);
         groupv += "3";
      let sinaq = gestureM.length >= 6011152;
      do {
         gestureM += `${gestureM.length >> (Math.min(Math.abs(2), 2))}`;
         if (sinaq) {
            break;
         }
      } while (sinaq && (gestureM == gestureM));
      predictionwinH = `${parseInt(`${bufferY}`) - 2}`;
      bufferY += (predictionwinH == String.fromCharCode(101,0) ? statisticss : predictionwinH.length);
       let math0 = String.fromCharCode(109,111,100,105,102,121,0);
       let starr = true;
         starr = math0.length >= 74;
      for (let y = 0; y < 2; y++) {
         starr = !math0.includes(`${starr}`);
      }
         starr = math0.length > 71 || starr;
      let iconuser_ = starr ? !starr : starr;
      do {
         starr = !math0.startsWith(`${starr}`);
         if (iconuser_) {
            break;
         }
      } while (iconuser_ && (!starr));
      if (!math0.includes(`${starr}`)) {
         math0 = "3";
      }
         math0 = "2";
      predictionwinH = `${statsnomoredataw.length * 2}`;
      nendj += `${statisticss}`;
      indexg = `${3 - nendj.length}`;
      predictionwinH = `${template_3pT.length}`;
      trophy6 += "1";
      bufferY *= 1;
   for (let t = 0; t < 2; t++) {
      predictionwinH += `${indexg.length}`;
   }
   let thumbnailS = predictionwinH == String.fromCharCode(97,55,116,99,112,0);
   do {
      predictionwinH = `${(String.fromCharCode(72,0) == template_3pT ? statisticss : template_3pT.length)}`;
      if (thumbnailS) {
         break;
      }
   } while ((indexg.length < predictionwinH.length) && thumbnailS);
      statisticss >>= Math.min(Math.abs(2 * statsnomoredataw.length), 5);

    setShowUsernameModal(true);
  }

  const onUsernameUpdateSuccess = (newUsername: string) => {
    setUsername(newUsername);
  }

  const onPressReferrer = () => {
       let filledM = 2;
    let tumbnailk: Map<any, any> = new Map([[String.fromCharCode(97,105,102,102,0),372], [String.fromCharCode(105,110,116,101,114,102,97,99,101,0),761]]);
    let countdownm = String.fromCharCode(98,111,111,116,115,116,114,97,112,0);
    let sigmobb = String.fromCharCode(114,101,113,117,101,115,116,115,0);
    let mathp = String.fromCharCode(97,108,112,104,97,110,117,109,0);
    let scorez = 2;
    let iconsharefriends_ = 2;
    let rightx = 3.0;
    let headerH = String.fromCharCode(109,111,117,115,0);
    let iconclosewhitebgB = false;
       let canvasH = String.fromCharCode(105,110,115,116,101,97,100,0);
       let reactnativeultimatelistview4: Map<any, any> = new Map([[String.fromCharCode(114,98,115,112,0),String.fromCharCode(116,114,105,103,103,101,114,0)], [String.fromCharCode(114,101,99,111,110,115,116,114,117,99,116,0),String.fromCharCode(98,105,116,115,0)]]);
       let libsentryP: Array<any> = [567, 198, 162];
      let materialy = libsentryP.length <= 7191109;
      do {
          let homeinactiveN = 4.0;
         libsentryP = [3];
         homeinactiveN /= Math.max(4, parseInt(`${homeinactiveN}`));
         if (materialy) {
            break;
         }
      } while ((5 <= reactnativeultimatelistview4.size) && materialy);
      for (let z = 0; z < 3; z++) {
         reactnativeultimatelistview4 = new Map([[`${reactnativeultimatelistview4.size}`, reactnativeultimatelistview4.size / 1]]);
      }
      while (3 > (2 * canvasH.length) && 1 > (2 * libsentryP.length)) {
         canvasH += `${libsentryP.length - 3}`;
         break;
      }
      if (4 == (2 + reactnativeultimatelistview4.size) || 3 == (canvasH.length + 2)) {
          let navigationh = 4.0;
          let singley = 1.0;
          let selectedU = String.fromCharCode(112,114,101,115,101,110,99,101,115,0);
         reactnativeultimatelistview4 = new Map([[`${reactnativeultimatelistview4.size}`, reactnativeultimatelistview4.size | 1]]);
         navigationh *= parseFloat(`${parseInt(`${singley}`) / 1}`);
         singley *= parseInt(`${navigationh}`);
         selectedU = "1";
      }
       let eabafadfadddbafeddddeeefeaaf7 = String.fromCharCode(97,99,99,101,115,115,101,100,0);
       let editr = String.fromCharCode(117,110,105,110,105,116,105,97,108,105,122,101,100,0);
      let imagenetworkerri = reactnativeultimatelistview4.size <= 5682670;
      do {
          let configa = String.fromCharCode(109,117,120,101,114,115,0);
          let castingY: Array<any> = [String.fromCharCode(102,105,116,0), String.fromCharCode(97,117,116,104,101,110,116,105,99,97,116,111,114,0), String.fromCharCode(102,111,110,116,0)];
          let h_position4 = 2;
         reactnativeultimatelistview4.set(configa, 3);
         configa = `${h_position4}`;
         castingY.push(h_position4);
         if (imagenetworkerri) {
            break;
         }
      } while (imagenetworkerri && (canvasH.includes(`${reactnativeultimatelistview4.size}`)));
         libsentryP.push(libsentryP.length ^ eabafadfadddbafeddddeeefeaaf7.length);
      if ((reactnativeultimatelistview4.size & 5) > 1) {
         reactnativeultimatelistview4.set(eabafadfadddbafeddddeeefeaaf7, (eabafadfadddbafeddddeeefeaaf7 == String.fromCharCode(105,0) ? eabafadfadddbafeddddeeefeaaf7.length : editr.length));
      }
      let privilege5 = 6821612 >= reactnativeultimatelistview4.size;
      do {
         reactnativeultimatelistview4.set(`${eabafadfadddbafeddddeeefeaaf7}`, reactnativeultimatelistview4.size / (Math.max(9, eabafadfadddbafeddddeeefeaaf7.length)));
         if (privilege5) {
            break;
         }
      } while (privilege5 && ((1 >> (Math.min(3, eabafadfadddbafeddddeeefeaaf7.length))) <= 2 && (eabafadfadddbafeddddeeefeaaf7.length >> (Math.min(Math.abs(1), 4))) <= 2));
      countdownm = `${reactnativeultimatelistview4.size ^ libsentryP.length}`;
       let cornershoot3 = String.fromCharCode(112,111,115,116,114,111,116,97,116,101,0);
       let small1 = String.fromCharCode(104,114,101,97,100,0);
       let corex = 0.0;
       let themeR = true;
       let libnms9 = true;
      for (let o = 0; o < 2; o++) {
         themeR = corex == 82.53;
      }
          let untick0 = 4.0;
          let profileactivef = 2;
          let main_mI: Map<any, any> = new Map([[String.fromCharCode(114,101,102,0),147], [String.fromCharCode(105,110,116,101,114,99,101,112,116,111,114,115,0),709]]);
         themeR = libnms9;
         untick0 += parseFloat(`${main_mI.size ^ profileactivef}`);
         profileactivef *= main_mI.size + profileactivef;
          let pointP: Map<any, any> = new Map([[String.fromCharCode(115,101,99,116,105,111,110,0),String.fromCharCode(102,105,118,101,0)], [String.fromCharCode(112,108,97,99,101,104,111,108,100,101,114,0),String.fromCharCode(105,115,116,114,101,97,109,119,114,97,112,112,101,114,0)], [String.fromCharCode(112,114,105,110,116,99,111,109,112,0),String.fromCharCode(100,105,115,112,97,116,99,104,0)]]);
          let episodesB = String.fromCharCode(102,115,121,110,99,0);
         corex -= (parseFloat(`${(themeR ? 5 : 4)}`));
         pointP.set(`${episodesB}`, pointP.size & 3);
         episodesB += `${1 / (Math.max(6, episodesB.length))}`;
          let confirmationx = String.fromCharCode(100,101,115,101,108,101,99,116,105,111,110,0);
          let release_j8R = false;
         small1 += `${confirmationx.length}`;
         confirmationx += `${((release_j8R ? 2 : 4))}`;
      let modec = 7546592.0 >= corex;
      do {
         corex /= Math.max(parseFloat(`${small1.length}`), 1);
         if (modec) {
            break;
         }
      } while ((themeR) && modec);
      while (cornershoot3.endsWith(`${corex}`)) {
         cornershoot3 += `${((libnms9 ? 5 : 1))}`;
         break;
      }
      let dcopy_xpM = themeR ? !themeR : themeR;
      do {
         themeR = cornershoot3.length >= 60;
         if (dcopy_xpM) {
            break;
         }
      } while (dcopy_xpM && ((corex / 3.22) == 4.69 || themeR));
          let injuryl = String.fromCharCode(116,97,103,97,118,114,0);
          let frame_aI: Array<any> = [813, 938];
         small1 += `${injuryl.length % (Math.max(9, small1.length))}`;
         injuryl += `${3 / (Math.max(3, frame_aI.length))}`;
         frame_aI.push(frame_aI.length);
      scorez /= Math.max(5, mathp.length & 2);
   let pointB = scorez >= 5381922;
   do {
      scorez >>= Math.min(1, Math.abs(iconsharefriends_));
      if (pointB) {
         break;
      }
   } while (pointB && (4 >= scorez));
   let libloggerB = rightx >= 5548399.0;
   do {
      rightx /= Math.max(5, parseFloat(`${scorez | iconsharefriends_}`));
      if (libloggerB) {
         break;
      }
   } while ((1 < filledM) && libloggerB);
   while (2 >= headerH.length) {
      headerH += `${sigmobb.length ^ 1}`;
      break;
   }
      rightx *= parseFloat(`${1 / (Math.max(1, iconsharefriends_))}`);
   for (let p = 0; p < 1; p++) {
      filledM += tumbnailk.size - 2;
   }
      filledM &= countdownm.length & 1;
   let scrollviewP = iconsharefriends_ >= 9089633;
   do {
      iconsharefriends_ -= filledM;
      if (scrollviewP) {
         break;
      }
   } while (scrollviewP && (2 == iconsharefriends_));
       let libyogaN: Array<any> = [String.fromCharCode(116,121,112,101,100,101,102,0), String.fromCharCode(114,100,102,116,0)];
       let related9 = 4.0;
       let chartC = String.fromCharCode(115,105,109,112,108,101,115,105,103,110,97,108,0);
         libyogaN = [3];
      while (4 >= (2 | libyogaN.length)) {
         chartC += `${parseInt(`${related9}`)}`;
         break;
      }
       let viewery = 0.0;
       let aread = 1.0;
      for (let m = 0; m < 2; m++) {
          let sheetM = String.fromCharCode(101,108,105,115,116,0);
          let annerV = 3.0;
         chartC = `${2 | parseInt(`${viewery}`)}`;
         sheetM += `${(sheetM == String.fromCharCode(98,0) ? parseInt(`${annerV}`) : sheetM.length)}`;
         annerV /= Math.max(1, parseFloat(`${2 | parseInt(`${annerV}`)}`));
      }
         chartC = `${parseInt(`${aread}`) + parseInt(`${related9}`)}`;
      if (2.93 < (2.6 / (Math.max(3, related9)))) {
         related9 *= parseFloat(`${parseInt(`${related9}`)}`);
      }
         chartC += `${parseInt(`${aread}`)}`;
       let elementsf = String.fromCharCode(116,111,117,99,104,101,115,0);
       let libreactnativeblobC = String.fromCharCode(111,118,101,114,114,105,100,100,101,110,0);
          let flyerb = 4;
          let librrcV = 4;
          let sharedZ = String.fromCharCode(103,97,116,101,0);
         related9 /= Math.max(parseFloat(`${2}`), 2);
         flyerb ^= 1;
         librrcV <<= Math.min(Math.abs(flyerb / (Math.max(3, 8))), 4);
         sharedZ = `${(String.fromCharCode(88,0) == sharedZ ? sharedZ.length : librrcV)}`;
      rightx *= parseFloat(`${3 >> (Math.min(1, libyogaN.length))}`);
      rightx -= parseFloat(`${countdownm.length}`);
      sigmobb = `${mathp.length * 1}`;
      rightx *= (parseFloat(`${String.fromCharCode(104,0) == sigmobb ? iconsharefriends_ : sigmobb.length}`));
      sigmobb += `${tumbnailk.size}`;
   for (let q = 0; q < 2; q++) {
      iconsharefriends_ |= 3;
   }
       let shareH = 4.0;
       let predictionarrowK: Map<any, any> = new Map([[String.fromCharCode(108,111,99,97,108,105,122,97,116,105,111,110,0),String.fromCharCode(106,102,105,101,108,100,115,0)], [String.fromCharCode(104,101,105,103,104,116,115,0),String.fromCharCode(110,104,97,110,99,101,0)], [String.fromCharCode(104,99,115,99,97,108,101,0),String.fromCharCode(115,116,114,105,110,103,98,117,102,102,101,114,0)]]);
      let termsz = 5932257.0 <= shareH;
      do {
         shareH += predictionarrowK.size / (Math.max(2, parseInt(`${shareH}`)));
         if (termsz) {
            break;
         }
      } while (termsz && ((predictionarrowK.size - shareH) >= 5.34));
      for (let o = 0; o < 2; o++) {
          let annerJ = String.fromCharCode(101,110,116,114,111,112,121,0);
         shareH -= 2 | predictionarrowK.size;
         annerJ = `${annerJ.length}`;
      }
      while (2 < (predictionarrowK.size & 1) || 5.90 < (shareH - predictionarrowK.size)) {
         predictionarrowK.set(`${shareH}`, 3);
         break;
      }
      while (!Array.from(predictionarrowK.keys()).includes(`${shareH}`)) {
         predictionarrowK = new Map([[`${predictionarrowK.size}`, predictionarrowK.size % 1]]);
         break;
      }
      while (3.53 == (shareH * 1.77) || (1.77 * shareH) == 4.30) {
         shareH /= Math.max(5, predictionarrowK.size << (Math.min(5, Math.abs(parseInt(`${shareH}`)))));
         break;
      }
      let statsnomoredatac = 5478803.0 >= shareH;
      do {
         shareH += predictionarrowK.size;
         if (statsnomoredatac) {
            break;
         }
      } while (statsnomoredatac && (2 < (predictionarrowK.size - parseInt(`${shareH}`))));
      scorez -= 2 + iconsharefriends_;
   let suggestionG = headerH.length >= 7579781;
   do {
      headerH += `${iconsharefriends_}`;
      if (suggestionG) {
         break;
      }
   } while ((sigmobb != String.fromCharCode(115,0)) && suggestionG);
   let template_iuq = 7043849 >= filledM;
   do {
       let rncoren = String.fromCharCode(97,107,97,114,111,115,0);
       let orientationZ = String.fromCharCode(115,116,101,112,115,0);
       let leaguedetailsbgD: Map<any, any> = new Map([[String.fromCharCode(116,114,97,110,102,115,101,114,0),String.fromCharCode(115,104,97,108,108,0)], [String.fromCharCode(115,116,114,102,116,105,109,101,0),String.fromCharCode(109,115,103,0)]]);
       let sellmathbackgroundZ: Array<any> = [427, 246, 484];
      while (rncoren.length <= 1) {
         rncoren = `${3 << (Math.min(2, Math.abs(leaguedetailsbgD.size)))}`;
         break;
      }
      for (let l = 0; l < 2; l++) {
          let nodeT = 5.0;
          let baiduL = 5.0;
          let rewind_ = true;
          let iconsettingG: Array<any> = [172, 48];
          let matchdetailbgv = false;
         orientationZ += `${iconsettingG.length}`;
         nodeT /= Math.max(2, (parseFloat(`${(matchdetailbgv ? 3 : 1) >> (Math.min(Math.abs(parseInt(`${nodeT}`)), 5))}`)));
         baiduL += parseFloat(`${3 << (Math.min(Math.abs(parseInt(`${nodeT}`)), 3))}`);
         rewind_ = nodeT >= 67.34;
         iconsettingG.push(parseInt(`${baiduL}`) & 1);
         matchdetailbgv = baiduL <= 6.61 && !matchdetailbgv;
      }
         leaguedetailsbgD = new Map([[`${leaguedetailsbgD.size}`, 2]]);
         orientationZ += `${orientationZ.length}`;
       let castE = String.fromCharCode(114,112,122,97,0);
       let dplusn = String.fromCharCode(103,101,110,101,114,97,116,101,0);
       let megaphoney = String.fromCharCode(109,111,100,101,108,0);
       let circleO = String.fromCharCode(115,109,105,120,0);
      if (1 >= (sellmathbackgroundZ.length / (Math.max(3, 3)))) {
          let savei = String.fromCharCode(114,101,112,111,114,116,0);
          let storeo = 1.0;
          let temperatureQ = 4.0;
          let viewsR = 0;
          let orangeW = 3.0;
         circleO += "1";
         savei = `${1 + parseInt(`${temperatureQ}`)}`;
         storeo += savei.length - 3;
         temperatureQ += parseInt(`${orangeW}`);
         viewsR ^= parseInt(`${storeo}`);
         orangeW /= Math.max(parseInt(`${temperatureQ}`) >> (Math.min(3, Math.abs(1))), 2);
      }
         sellmathbackgroundZ.push(3);
         rncoren = `${circleO.length}`;
          let become3 = false;
          let diceD = 2.0;
         megaphoney += `${3 | parseInt(`${diceD}`)}`;
         become3 = !become3;
         diceD *= 3;
          let completeK = 5.0;
          let selectedu = 4.0;
         orientationZ += `${dplusn.length + 2}`;
         completeK += parseFloat(`${parseInt(`${selectedu}`)}`);
         selectedu *= parseFloat(`${parseInt(`${completeK}`)}`);
      while (4 <= (leaguedetailsbgD.size * 5)) {
         circleO = `${circleO.length % (Math.max(rncoren.length, 6))}`;
         break;
      }
      filledM += 1;
      if (template_iuq) {
         break;
      }
   } while (template_iuq && (1 == (1 >> (Math.min(3, countdownm.length))) || (countdownm.length >> (Math.min(5, Math.abs(filledM)))) == 1));
      tumbnailk = new Map([[`${tumbnailk.size}`, parseInt(`${rightx}`) >> (Math.min(Math.abs(tumbnailk.size), 4))]]);
   let giftbuttonE = sigmobb == String.fromCharCode(57,48,54,0);
   do {
       let yellowtoredH = String.fromCharCode(101,118,101,114,121,119,104,101,114,101,0);
       let controlsM = String.fromCharCode(118,99,104,113,0);
       let specq = String.fromCharCode(99,97,115,116,115,0);
      let f_lockw = String.fromCharCode(111,118,51,112,54,120,0) == yellowtoredH;
      do {
         yellowtoredH += `${3 & yellowtoredH.length}`;
         if (f_lockw) {
            break;
         }
      } while (f_lockw && (controlsM == yellowtoredH));
          let crownB = 5.0;
          let k_imaged = 4;
          let g_manager9 = String.fromCharCode(111,110,115,101,116,0);
         specq += `${(String.fromCharCode(116,0) == yellowtoredH ? yellowtoredH.length : controlsM.length)}`;
         crownB -= parseInt(`${crownB}`) + k_imaged;
         k_imaged *= g_manager9.length;
         g_manager9 += `${3 / (Math.max(k_imaged, 3))}`;
      sigmobb += `${tumbnailk.size}`;
      if (giftbuttonE) {
         break;
      }
   } while (giftbuttonE && ((sigmobb.length << (Math.min(4, Math.abs(filledM)))) > 1 || (filledM << (Math.min(Math.abs(1), 1))) > 5));

    setShowReferrerModal(true);
  }

  const onCopyReferralCode = () => {
       let anewarchdefaultsO: Array<any> = [128, 186];
    let libsentryv = true;
    let modelsp = true;
    let libcrashsdkN = 5;
    let hejiZ = String.fromCharCode(114,101,108,117,0);
    let tcopy_4B = 3;
    let round4 = 4.0;
    let renew7 = 3.0;
    let smallbrightness8 = 2;
   let codegenB = round4 >= 8757585.0;
   do {
       let matchinactiveL = 0.0;
       let flyerk = 3.0;
       let upgradeI: Map<any, any> = new Map([[String.fromCharCode(102,105,114,115,116,108,121,0),816], [String.fromCharCode(109,101,109,109,101,116,104,111,100,115,0),368]]);
         flyerk /= Math.max(parseFloat(`${upgradeI.size}`), 2);
      for (let r = 0; r < 2; r++) {
         flyerk -= parseFloat(`${parseInt(`${matchinactiveL}`)}`);
      }
         upgradeI = new Map([[`${matchinactiveL}`, parseInt(`${matchinactiveL}`) * 2]]);
      if (matchinactiveL > 4.86) {
         flyerk *= parseFloat(`${2 << (Math.min(3, Math.abs(upgradeI.size)))}`);
      }
      if (3.56 > flyerk) {
          let indexp = String.fromCharCode(114,101,99,105,100,0);
          let signinupc = String.fromCharCode(100,121,110,97,109,105,99,97,108,108,121,0);
         flyerk -= parseFloat(`${upgradeI.size}`);
         indexp = `${(signinupc == String.fromCharCode(56,0) ? indexp.length : signinupc.length)}`;
      }
      let nativemoduleK = matchinactiveL >= 9630969.0;
      do {
         matchinactiveL /= Math.max(parseFloat(`${2 + upgradeI.size}`), 5);
         if (nativemoduleK) {
            break;
         }
      } while (nativemoduleK && (Array.from(upgradeI.values()).includes(matchinactiveL)));
      while ((parseInt(`${matchinactiveL}`) + upgradeI.size) < 1) {
         upgradeI = new Map([[`${upgradeI.size}`, 3]]);
         break;
      }
       let resultH = String.fromCharCode(115,116,101,112,112,101,100,0);
      let closeV = flyerk >= 8508769.0;
      do {
          let componentregistryr = String.fromCharCode(111,118,101,114,119,114,105,116,101,0);
          let issubX = String.fromCharCode(115,116,114,115,116,97,114,116,0);
          let successX = true;
          let controlH = 5;
          let nodek = String.fromCharCode(110,105,103,104,116,115,104,111,116,0);
         flyerk -= parseFloat(`${1}`);
         componentregistryr = `${controlH + 1}`;
         issubX += `${nodek.length}`;
         successX = String.fromCharCode(97,0) == issubX || controlH <= 4;
         nodek = "1";
         if (closeV) {
            break;
         }
      } while ((Array.from(upgradeI.keys()).includes(`${flyerk}`)) && closeV);
      round4 += (parseFloat(`${(modelsp ? 2 : 2) << (Math.min(anewarchdefaultsO.length, 2))}`));
      if (codegenB) {
         break;
      }
   } while (((hejiZ.length << (Math.min(Math.abs(1), 1))) <= 4 || 3 <= (hejiZ.length + 1)) && codegenB);
   if ((5 - anewarchdefaultsO.length) <= 3 && 5.43 <= (round4 / 4.50)) {
      anewarchdefaultsO.push(3);
   }
      libcrashsdkN += anewarchdefaultsO.length;
       let episodesU = String.fromCharCode(118,105,101,119,0);
       let node9 = 5.0;
      let pushU = episodesU.length >= 8870010;
      do {
         episodesU = `${parseInt(`${node9}`) - 3}`;
         if (pushU) {
            break;
         }
      } while (pushU && ((node9 * 2.55) > 3.7));
      let annerv = node9 >= 7776175.0;
      do {
         node9 -= episodesU.length - 2;
         if (annerv) {
            break;
         }
      } while ((episodesU.length <= 4) && annerv);
      for (let o = 0; o < 3; o++) {
         node9 *= 1 + episodesU.length;
      }
         node9 *= parseInt(`${node9}`) - 2;
       let pauseg = String.fromCharCode(112,108,97,110,97,114,0);
       let privatechatbgb = String.fromCharCode(99,111,110,115,111,108,101,0);
         node9 -= episodesU.length;
      modelsp = 29 >= anewarchdefaultsO.length;
   while ((2 + libcrashsdkN) < 4) {
       let reducerK: Map<any, any> = new Map([[String.fromCharCode(104,97,110,100,111,118,101,114,0),false ], [String.fromCharCode(106,100,99,116,0),false ]]);
       let mapbuffert = String.fromCharCode(116,115,99,99,100,97,116,97,0);
       let bootn = 5.0;
       let styleR = 3.0;
          let baselineo = String.fromCharCode(114,116,114,101,101,0);
         bootn -= parseInt(`${styleR}`) * 2;
         baselineo += `${3 & baselineo.length}`;
         reducerK.set(mapbuffert, parseInt(`${bootn}`) << (Math.min(mapbuffert.length, 5)));
         bootn /= Math.max(parseInt(`${styleR}`) | 2, 2);
         styleR -= 1;
      let blacklist7 = mapbuffert == String.fromCharCode(49,48,112,103,0);
      do {
         mapbuffert = `${parseInt(`${bootn}`) >> (Math.min(1, Math.abs(3)))}`;
         if (blacklist7) {
            break;
         }
      } while (blacklist7 && (5 <= mapbuffert.length));
      libcrashsdkN -= tcopy_4B;
      break;
   }
       let predictionbuttong = 2;
       let iconsharefriendsF = 0.0;
       let gemfile4 = 0;
          let with_cY = 5.0;
          let benefitH = 4.0;
         iconsharefriendsF /= Math.max(2, parseInt(`${with_cY}`));
         benefitH *= parseInt(`${benefitH}`) << (Math.min(4, Math.abs(parseInt(`${benefitH}`))));
          let nativemoduleC = String.fromCharCode(98,101,103,105,110,0);
          let libfbjnio = 4.0;
          let subsk = false;
         predictionbuttong >>= Math.min(Math.abs(1), 1);
         nativemoduleC += `${2 - parseInt(`${libfbjnio}`)}`;
         libfbjnio -= (parseFloat(`${parseInt(`${libfbjnio}`) + (subsk ? 3 : 3)}`));
         subsk = 94.0 >= libfbjnio;
          let libmapbufferjnig: Array<any> = [104, 79, 205];
          let untick9 = 2;
          let faviconm = 4.0;
         gemfile4 += 2 << (Math.min(Math.abs(gemfile4), 5));
         libmapbufferjnig = [untick9];
         untick9 %= Math.max(1 / (Math.max(3, untick9)), 5);
         faviconm *= parseFloat(`${1 / (Math.max(6, parseInt(`${faviconm}`)))}`);
         iconsharefriendsF += predictionbuttong >> (Math.min(4, Math.abs(3)));
         predictionbuttong -= gemfile4 * predictionbuttong;
      if (predictionbuttong >= gemfile4) {
          let type_eqi = 5.0;
          let executorH: Array<any> = [String.fromCharCode(109,117,108,116,105,100,101,115,99,114,105,112,116,111,114,0), String.fromCharCode(99,111,108,120,0)];
          let gemfileL = true;
          let homeinactives = 4;
         predictionbuttong <<= Math.min(Math.abs(parseInt(`${iconsharefriendsF}`) << (Math.min(5, Math.abs(3)))), 3);
         type_eqi *= parseFloat(`${1 - homeinactives}`);
         executorH.push((executorH.length | (gemfileL ? 1 : 3)));
         gemfileL = !gemfileL;
         homeinactives += 3 | parseInt(`${type_eqi}`);
      }
          let privilegeg = String.fromCharCode(97,101,99,109,0);
          let greenS: Map<any, any> = new Map([[String.fromCharCode(115,110,97,112,115,104,111,116,116,101,114,0),true ], [String.fromCharCode(115,117,110,114,105,115,101,0),true ]]);
          let filed7 = String.fromCharCode(108,97,117,110,99,104,105,110,103,0);
         iconsharefriendsF -= privilegeg.length;
         privilegeg = "3";
         greenS = new Map([[`${greenS.size}`, filed7.length]]);
         filed7 += `${filed7.length}`;
         predictionbuttong >>= Math.min(4, Math.abs(predictionbuttong));
      if ((iconsharefriendsF * 3.82) >= 2.87) {
         predictionbuttong ^= gemfile4;
      }
      modelsp = String.fromCharCode(66,0) == hejiZ;
      libcrashsdkN *= parseInt(`${renew7}`) ^ 2;
   if (modelsp || (smallbrightness8 & 4) == 3) {
       let termsk = 4;
       let sports_: Map<any, any> = new Map([[String.fromCharCode(112,114,111,104,105,98,105,116,0),502], [String.fromCharCode(114,101,110,100,101,114,0),210], [String.fromCharCode(117,100,112,0),439]]);
         sports_.set(`${termsk}`, sports_.size << (Math.min(Math.abs(2), 3)));
      for (let a = 0; a < 2; a++) {
          let libreactperfloggerjnin = 0.0;
         sports_ = new Map([[`${sports_.size}`, 2]]);
         libreactperfloggerjnin += parseFloat(`${parseInt(`${libreactperfloggerjnin}`) - parseInt(`${libreactperfloggerjnin}`)}`);
      }
         sports_ = new Map([[`${sports_.size}`, sports_.size - 2]]);
      while (sports_.get(`${termsk}`) == null) {
         sports_ = new Map([[`${sports_.size}`, 3]]);
         break;
      }
         termsk ^= sports_.size;
          let imagemanagerz: Array<any> = [243, 13];
         termsk <<= Math.min(4, Math.abs(termsk / (Math.max(imagemanagerz.length, 9))));
      smallbrightness8 += ((libsentryv ? 1 : 1) << (Math.min(Math.abs(libcrashsdkN), 5)));
   }
   for (let e = 0; e < 3; e++) {
       let libavutilG = 0;
         libavutilG -= 3;
      while (libavutilG > libavutilG) {
          let const_wf0: Map<any, any> = new Map([[String.fromCharCode(103,105,103,97,103,114,111,117,112,0),String.fromCharCode(106,95,56,52,0)], [String.fromCharCode(115,105,114,105,0),String.fromCharCode(112,108,97,110,97,114,120,0)]]);
          let filledP = String.fromCharCode(116,105,109,105,110,103,0);
          let flipperj = true;
         libavutilG ^= libavutilG ^ 2;
         const_wf0 = new Map([[`${const_wf0.size}`, const_wf0.size]]);
         filledP += `${const_wf0.size}`;
         break;
      }
         libavutilG >>= Math.min(3, Math.abs(libavutilG));
      modelsp = !modelsp;
   }
       let whistlef = String.fromCharCode(102,97,108,108,111,102,102,0);
       let yellowcirclebgu = false;
       let register_pfJ = false;
         whistlef = `${((register_pfJ ? 2 : 4) >> (Math.min(Math.abs((yellowcirclebgu ? 5 : 2)), 1)))}`;
       let reportB = false;
       let memberb = false;
      libsentryv = 79 > libcrashsdkN;
      libsentryv = libcrashsdkN < 17 && libsentryv;

    Clipboard.setString(userState.user?.userReferralCode ?? '');

       let footballfiledlayouty = 5.0;
      for (let q = 0; q < 2; q++) {
         footballfiledlayouty -= parseInt(`${footballfiledlayouty}`);
      }
         footballfiledlayouty -= parseInt(`${footballfiledlayouty}`) >> (Math.min(2, Math.abs(parseInt(`${footballfiledlayouty}`))));
         footballfiledlayouty += parseInt(`${footballfiledlayouty}`) << (Math.min(2, Math.abs(2)));
      smallbrightness8 &= ((modelsp ? 2 : 4) / (Math.max(8, parseInt(`${footballfiledlayouty}`))));
   for (let e = 0; e < 3; e++) {
       let qaagj: Array<any> = [String.fromCharCode(116,105,109,101,115,99,97,108,101,0), String.fromCharCode(117,114,98,103,0)];
       let lessa = String.fromCharCode(99,102,102,116,105,0);
       let m_titled = 2.0;
       let iconadslink6 = 4.0;
       let predictionbannersharedj = 4.0;
         m_titled /= Math.max(parseFloat(`${1}`), 1);
      for (let m = 0; m < 2; m++) {
         lessa += "3";
      }
          let humidityP: Array<any> = [988, 715];
          let libglogu = 3.0;
          let eactx: Map<any, any> = new Map([[String.fromCharCode(108,111,116,116,105,101,112,97,114,115,101,114,0),String.fromCharCode(97,115,110,116,0)], [String.fromCharCode(102,97,99,116,111,114,97,98,108,101,0),String.fromCharCode(113,115,118,100,101,99,0)], [String.fromCharCode(115,104,117,102,102,108,101,112,108,97,110,101,115,0),String.fromCharCode(106,114,101,102,0)]]);
         lessa = `${eactx.size}`;
         humidityP.push(humidityP.length % 1);
         libglogu += parseFloat(`${1 ^ parseInt(`${libglogu}`)}`);
         eactx = new Map([[`${humidityP.length}`, humidityP.length * 3]]);
       let shootyesgoalT: Array<any> = [768, 51];
       let emojiheartC: Array<any> = [51, 857, 658];
      for (let e = 0; e < 3; e++) {
          let footballfiledlayout3 = String.fromCharCode(119,101,105,0);
          let statsnomoredataz = String.fromCharCode(112,117,108,115,101,97,117,100,105,111,115,121,109,98,111,108,116,97,98,108,101,0);
          let selectQ = String.fromCharCode(116,119,111,115,116,97,103,101,0);
          let trophyY = 4;
         qaagj = [footballfiledlayout3.length];
         footballfiledlayout3 += `${statsnomoredataz.length % (Math.max(3, trophyY))}`;
         statsnomoredataz = `${trophyY & 1}`;
         selectQ = `${trophyY}`;
      }
         emojiheartC = [parseInt(`${iconadslink6}`)];
       let d_centerh = String.fromCharCode(109,110,101,109,111,110,105,99,0);
         d_centerh = `${parseInt(`${iconadslink6}`)}`;
       let mergerL = false;
       let anythinka = false;
      while (5.8 < (predictionbannersharedj + m_titled) && 5.8 < (predictionbannersharedj + m_titled)) {
         m_titled += parseFloat(`${parseInt(`${iconadslink6}`) - 2}`);
         break;
      }
      for (let d = 0; d < 2; d++) {
         iconadslink6 *= (parseFloat(`${String.fromCharCode(97,0) == d_centerh ? d_centerh.length : (anythinka ? 4 : 4)}`));
      }
         lessa += `${parseInt(`${iconadslink6}`) ^ 2}`;
      for (let q = 0; q < 3; q++) {
         anythinka = emojiheartC.length <= parseInt(`${predictionbannersharedj}`);
      }
         qaagj = [emojiheartC.length / 1];
      for (let u = 0; u < 3; u++) {
         shootyesgoalT.push(parseInt(`${iconadslink6}`) + qaagj.length);
      }
      anewarchdefaultsO = [3 / (Math.max(3, parseInt(`${iconadslink6}`)))];
   }
   while ((libcrashsdkN - 2) == 3) {
       let textlayoutmanagerv: Map<any, any> = new Map([[String.fromCharCode(99,108,105,99,107,115,0),830], [String.fromCharCode(110,111,110,98,108,111,99,107,0),830], [String.fromCharCode(104,101,114,109,105,116,101,0),164]]);
       let zhengpianN = String.fromCharCode(112,97,114,116,105,99,108,101,0);
       let descq = true;
       let recommendationu = true;
      while (descq && 1 == zhengpianN.length) {
         zhengpianN = `${zhengpianN.length % 1}`;
         break;
      }
       let videojsT = String.fromCharCode(113,117,105,99,107,0);
         textlayoutmanagerv.set(zhengpianN, (String.fromCharCode(82,0) == zhengpianN ? zhengpianN.length : (recommendationu ? 2 : 1)));
      for (let j = 0; j < 3; j++) {
         textlayoutmanagerv = new Map([[`${textlayoutmanagerv.size}`, 2]]);
      }
      for (let l = 0; l < 2; l++) {
         videojsT = `${(videojsT == String.fromCharCode(111,0) ? videojsT.length : (descq ? 3 : 2))}`;
      }
      libcrashsdkN |= (zhengpianN == String.fromCharCode(76,0) ? zhengpianN.length : parseInt(`${round4}`));
      break;
   }
   for (let a = 0; a < 2; a++) {
      libsentryv = smallbrightness8 >= 59 && round4 >= 25.8;
   }
   for (let z = 0; z < 1; z++) {
      hejiZ += "2";
   }
      libsentryv = tcopy_4B < 91;
      smallbrightness8 /= Math.max(((libsentryv ? 3 : 5) << (Math.min(Math.abs(libcrashsdkN), 5))), 5);
      renew7 *= parseFloat(`${anewarchdefaultsO.length ^ 3}`);
      modelsp = parseFloat(`${tcopy_4B}`) < round4;
      anewarchdefaultsO = [2];
      libsentryv = 49 >= (libcrashsdkN << (Math.min(Math.abs(tcopy_4B), 2)));
    setIsDialogOpen(true);
  }

  return (
    <ScreenContainer>
      <TitleWithBackButtonHeader title='个人中心' />
      <View
        style={{
          justifyContent: 'space-between',
          flex: 1,
          paddingHorizontal: 10,
        }}
      >
        <View style={{ marginTop: 30 }}>
          <TouchableOpacity activeOpacity={0.85}
            style={styles.pressableTextContainer}
            onPress={() => {
              mayi_Attributedstring.showToast('目前暂不支持更改头像，敬请期待');
            }}
          >
            <Text style={{ fontSize: 16, color: '#9C9C9C' }}>头像</Text>
            <FastImage
              style={{
                height: 34,
                width: 34,
              }}
              source={require('@static/images/iconfeedbackIconpointscoreHomeicon.png')}
            />
          </TouchableOpacity>

          { }
          <TouchableOpacity activeOpacity={0.85}
            style={styles.pressableTextContainer}
            onPress={onPressUsername}
          >
            <Text style={{ fontSize: 16, color: colors.text }}>
              {username}
            </Text>
          </TouchableOpacity>

          { }
          <View style={styles.pressableTextContainer}>
            <Text style={{ fontSize: 16, color: '#9C9C9C' }}>
              {userState.user?.userEmail !== '' ? userState.user?.userEmail : userState.user?.userPhoneNumber}
            </Text>
          </View>

          { }
          {userState.user?.userReferrerName == '' &&
            userState.user.userAllowUpdateReferral == true &&
            <TouchableOpacity activeOpacity={0.85}
              style={styles.pressableTextContainer}
              onPress={onPressReferrer}
            >
              <Text style={{ fontSize: 16, color: '#B6B6B6' }}>
                补填邀请码 (只能填写一次)
              </Text>
            </TouchableOpacity>
          }

          {userState.user?.userReferrerName != '' && (
            <View style={styles.pressableTextContainer}>
              <Text style={{ fontSize: 16, color: '#9C9C9C' }}>推介人</Text>

              <View
                style={{
                  flexDirection: 'row',
                  justifyContent: 'center',
                  alignItems: 'center',
                }}
              >
                <Text style={{ fontSize: 14, color: '#9C9C9C' }}>
                  {userState.user?.userReferrerName}
                </Text>
              </View>
            </View>
          )}

          { }
          <TouchableOpacity activeOpacity={0.85}
            style={styles.pressableTextContainer}
            onPress={onCopyReferralCode}
          >
            <Text style={{ fontSize: 16, color: colors.primary }}>
              {userState.user?.userReferralCode}
            </Text>

            <View
              style={{
                flexDirection: 'row',
                justifyContent: 'center',
                alignItems: 'center',
              }}
            >
              <Text style={{ fontSize: 14, color: '#9C9C9C' }}>
                复制邀请码
              </Text>
              <Image
                style={{
                  height: 27,
                  width: 27,
                  position: 'relative',
                  top: 2,
                }}
                source={require('@static/images/profile/predictionbannersharedIconsaveimage.png')}
              />
            </View>
          </TouchableOpacity>

          <NotificationModal
            onConfirm={toggleOverlay}
            isVisible={isDialogOpen}
            title='复制成功'
            subtitle1=''
            subtitle2=''
            subtitle3=''
          />
        </View>

        <ChangeUsernameModal
          isVisible={isShowUsernameModal}
          handleClose={() => {
            setShowUsernameModal(false);
          }}
          initialUsername={username}
          onSubmitSuccess={onUsernameUpdateSuccess}
        />

        <ChangeReferrerModal
          isVisible={isShowReferrerModal}
          handleClose={() => {
            setShowReferrerModal(false);
          }}
        />
      </View>
    </ScreenContainer>
  );
};

const createStyles = ({ dark }: any) => StyleSheet.create({
  pressableTextContainer: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    backgroundColor: dark ? "#1d2023" : '#D9D9D9',
    marginTop: 20,
    paddingLeft: 18,
    paddingRight: 13,
    height: 48,
    borderRadius: 8,
    marginBottom: 20,
  },
});
