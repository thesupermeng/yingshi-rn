import React, { useEffect, useState } from "react";
import {
  View,
  Text,
  StyleSheet,
  TextInput,
  Image,
  RefreshControl,
  ScrollView,
} from "react-native";
import ScreenContainer from "../../components/container/wawa_nterstitial_iconpipexpand";
import { RootStackScreenProps } from "@type/wawa_iconnewchat";
import { useTheme } from "@react-navigation/native";
import { wawaIconsubssuccess } from "@redux/wawa_bgvipsport_spinner";

import TitleWithBackButtonHeader from "../../components/header/wawa_matchinactive_header";
import axios from "axios";
import { Keyboard } from "react-native";
import {
  API_DOMAIN,
  API_DOMAIN_TEST,
  API_DOMAIN_LOCAL,
} from "@utility/wawa_iconpointscore";
import NetInfo, { NetInfoState } from "@react-native-community/netinfo";

import InviteStep from "./../../components/invite/wawa_final";
import InviteCard from "./../../components/invite/wawa_pangle_macau";

import InviteHeader from "./../../components/invite/wawa_vipsport_header";
import { useAppDispatch, useAppSelector, useSelector } from "@hooks/wawa_root";
import { TouchableOpacity } from "@gorhom/bottom-sheet";
import { updateUserAuth } from "@redux/actions/wawa_loginsuccess";
import wawa_dacccfaabfbcbadeebddcabacdffdbc_video from "../../../../Umeng/wawa_dacccfaabfbcbadeebddcabacdffdbc_video";
import { wawaEvent } from "@api";
import { wawaPhoneControls } from "@redux/reducers/wawa_umeng";


export default ({ navigation }: RootStackScreenProps<"邀请">) => {
  const { colors, textVariants, icons, spacing } = useTheme();

  const userState = useSelector<wawaPhoneControls>('userReducer');

  const [refreshing, setRefreshing] = useState(false);
  const dispatch = useAppDispatch();

  
  useEffect(() => {
    wawa_dacccfaabfbcbadeebddcabacdffdbc_video.userCenterVipInviteViewsAnalytics();
  }, []);
  

  const handleRefresh = async () => {
       let disconnectedlogop = String.fromCharCode(121,95,53,55,95,112,97,116,99,104,115,101,116,0);
    let orangeclockI = true;
    let downi = 2.0;
    let live2 = 0.0;
    let dragclosed: Array<any> = [String.fromCharCode(98,95,57,56,95,116,101,115,116,98,105,116,0), String.fromCharCode(109,95,52,50,95,112,111,119,101,114,0)];
    let rewardvideob = 4.0;
    let jingdongw = 5.0;
    let submitm = String.fromCharCode(105,110,116,101,114,115,101,99,116,115,95,104,95,50,50,0);
    let progresse: Map<any, any> = new Map([[String.fromCharCode(115,95,55,56,95,99,117,115,116,111,109,0),277], [String.fromCharCode(115,101,112,97,114,97,116,101,95,112,95,50,50,0),690]]);
    let libreactnativejniA = 5.0;
    let middlewarek = 1.0;
    let defaultpredictionprofilel = false;
    let actions5 = 2.0;
   let reward1 = 5771371.0 <= middlewarek;
   do {
      middlewarek -= parseInt(`${libreactnativejniA}`) << (Math.min(2, Math.abs(3)));
      if (reward1) {
         break;
      }
   } while ((rewardvideob < middlewarek) && reward1);
   while (disconnectedlogop.includes(`${libreactnativejniA}`)) {
      disconnectedlogop += `${progresse.size}`;
      break;
   }
       let switch_8u = false;
         switch_8u = !switch_8u && !switch_8u;
      for (let l = 0; l < 3; l++) {
         switch_8u = !switch_8u && switch_8u;
      }
          let armvaS = String.fromCharCode(117,95,48,95,109,101,109,111,114,121,98,117,102,102,101,114,0);
         switch_8u = armvaS.length == 23;
      downi -= parseInt(`${rewardvideob}`);
   let subbasketballplayerP = 6665092.0 >= rewardvideob;
   do {
       let philippinesv = String.fromCharCode(105,116,111,97,95,49,95,51,51,0);
       let predictionwinp = 0.0;
       let zoomI: Map<any, any> = new Map([[String.fromCharCode(100,115,116,114,101,97,109,95,53,95,57,55,0),String.fromCharCode(120,95,53,49,95,98,97,99,107,103,114,111,117,110,100,0)], [String.fromCharCode(115,117,98,102,114,97,109,101,95,120,95,55,55,0),String.fromCharCode(100,95,56,52,95,109,97,107,101,119,116,0)]]);
       let greenarrowupw = 4;
         greenarrowupw ^= parseInt(`${predictionwinp}`) * zoomI.size;
          let moon6 = String.fromCharCode(117,114,108,115,95,108,95,49,49,0);
          let modal5 = 5;
         zoomI.set(philippinesv, (String.fromCharCode(119,0) == philippinesv ? parseInt(`${predictionwinp}`) : philippinesv.length));
         moon6 += `${(moon6 == String.fromCharCode(72,0) ? modal5 : moon6.length)}`;
         modal5 <<= Math.min(Math.abs(modal5), 5);
      if ((greenarrowupw << (Math.min(Math.abs(zoomI.size), 3))) >= 2 || 5 >= (greenarrowupw << (Math.min(Math.abs(2), 4)))) {
         greenarrowupw >>= Math.min(Math.abs(zoomI.size % (Math.max(philippinesv.length, 3))), 1);
      }
      if ((greenarrowupw & philippinesv.length) >= 5) {
         greenarrowupw >>= Math.min(Math.abs(2 << (Math.min(1, Math.abs(zoomI.size)))), 4);
      }
      let sigmobI = predictionwinp <= 5817019.0;
      do {
          let yingG = 1.0;
          let pressureL: Map<any, any> = new Map([[String.fromCharCode(120,95,53,55,95,119,105,110,100,111,119,115,0),String.fromCharCode(107,95,56,49,95,115,117,98,98,97,110,100,0)], [String.fromCharCode(114,95,55,54,95,99,109,97,115,107,0),String.fromCharCode(115,116,97,114,116,99,111,100,101,95,119,95,54,50,0)]]);
         predictionwinp -= parseFloat(`${greenarrowupw * 1}`);
         yingG /= Math.max(parseFloat(`${parseInt(`${yingG}`) + pressureL.size}`), 4);
         pressureL = new Map([[`${pressureL.size}`, parseInt(`${yingG}`) << (Math.min(Math.abs(3), 5))]]);
         if (sigmobI) {
            break;
         }
      } while ((1 < zoomI.size) && sigmobI);
       let reviewF = String.fromCharCode(102,105,110,100,110,101,116,95,51,95,49,55,0);
       let penaltygoalD = String.fromCharCode(117,95,49,48,95,99,108,111,117,100,102,108,97,114,101,0);
      for (let q = 0; q < 3; q++) {
          let iconpipshrinkx = 4.0;
          let basketballmatchdetailbgP = 3.0;
          let logoe = false;
         zoomI = new Map([[`${basketballmatchdetailbgP}`, 3 / (Math.max(parseInt(`${basketballmatchdetailbgP}`), 8))]]);
         iconpipshrinkx /= Math.max(((logoe ? 4 : 1) / (Math.max(parseInt(`${iconpipshrinkx}`), 1))), 3);
         logoe = iconpipshrinkx <= 12.24;
      }
         reviewF = `${zoomI.size}`;
         philippinesv += `${greenarrowupw % (Math.max(1, 7))}`;
         zoomI.set(penaltygoalD, 2 * parseInt(`${predictionwinp}`));
      for (let v = 0; v < 1; v++) {
         penaltygoalD = `${parseInt(`${predictionwinp}`) % 3}`;
      }
      for (let b = 0; b < 3; b++) {
         zoomI.set(reviewF, reviewF.length);
      }
      rewardvideob *= parseInt(`${live2}`);
      if (subbasketballplayerP) {
         break;
      }
   } while (subbasketballplayerP && ((rewardvideob * 1.75) > 1.43));
       let whitesmalltickZ = 0;
       let lessc = 2;
      for (let p = 0; p < 2; p++) {
          let episode6 = 3;
          let graphu = String.fromCharCode(108,101,110,103,116,104,95,98,95,53,55,0);
          let rncorez: Map<any, any> = new Map([[String.fromCharCode(120,95,55,55,95,115,105,103,110,105,102,105,99,97,110,100,0),102], [String.fromCharCode(117,95,52,53,95,101,100,105,116,97,98,108,101,0),46], [String.fromCharCode(115,105,103,110,105,110,103,95,103,95,50,54,0),157]]);
          let register_f6q = 4.0;
         lessc /= Math.max(episode6 * 3, 3);
         episode6 <<= Math.min(Math.abs(1), 3);
         graphu = `${rncorez.size}`;
         rncorez.set(`${register_f6q}`, parseInt(`${register_f6q}`));
      }
         lessc %= Math.max(2, 2);
      while ((whitesmalltickZ >> (Math.min(Math.abs(lessc), 2))) < 3 && (3 >> (Math.min(2, Math.abs(whitesmalltickZ)))) < 4) {
          let downloadc: Array<any> = [605, 869];
          let refreshborderlessp = 1.0;
          let iconarrowrightblackO = true;
         lessc %= Math.max(2, whitesmalltickZ);
         downloadc.push(downloadc.length);
         refreshborderlessp /= Math.max((parseFloat(`${(iconarrowrightblackO ? 5 : 4) >> (Math.min(Math.abs(parseInt(`${refreshborderlessp}`)), 2))}`)), 1);
         iconarrowrightblackO = refreshborderlessp > 5.19;
         break;
      }
       let mimo2 = String.fromCharCode(97,95,52,54,95,111,112,116,103,114,111,117,112,0);
          let mbridgeF = false;
          let uploadD = 1.0;
         mimo2 = `${lessc & 1}`;
         mbridgeF = !mbridgeF;
         uploadD -= parseInt(`${uploadD}`) / 2;
       let libcrashsdk1 = String.fromCharCode(104,95,51,95,97,108,112,104,97,98,101,116,115,0);
      submitm = `${parseInt(`${jingdongw}`) | 3}`;
      jingdongw *= parseFloat(`${parseInt(`${live2}`) << (Math.min(Math.abs(2), 2))}`);
   let const_baM = dragclosed.length >= 7703354;
   do {
      dragclosed.push(parseInt(`${libreactnativejniA}`));
      if (const_baM) {
         break;
      }
   } while ((1.1 <= (middlewarek / (Math.max(8, dragclosed.length))) || (dragclosed.length + 4) <= 2) && const_baM);
   for (let q = 0; q < 1; q++) {
      downi /= Math.max(2, 2 >> (Math.min(Math.abs(parseInt(`${downi}`)), 2)));
   }
      dragclosed.push(parseInt(`${libreactnativejniA}`));
   while ((1.35 + jingdongw) > 4.74 || (3 / (Math.max(6, progresse.size))) > 3) {
      progresse.set(`${libreactnativejniA}`, progresse.size);
      break;
   }
   while (1 <= (1 >> (Math.min(1, Math.abs(progresse.size)))) && 1 <= (1 >> (Math.min(3, Math.abs(progresse.size))))) {
       let roomC = true;
       let subsq = 2.0;
      while ((3.33 + subsq) > 4.1 || 3.33 > subsq) {
          let homeI = 5.0;
          let modelsT: Map<any, any> = new Map([[String.fromCharCode(122,95,51,53,95,103,101,111,108,111,99,97,116,105,111,110,0),String.fromCharCode(122,95,55,52,95,116,114,97,110,115,109,105,116,116,101,100,0)], [String.fromCharCode(119,95,49,50,95,112,114,101,100,105,99,116,111,114,0),String.fromCharCode(118,101,114,115,105,111,110,101,100,95,104,95,57,55,0)]]);
          let checkboxG = 1.0;
          let mbnativeK: Array<any> = [35, 898, 892];
          let iconrefreshk = String.fromCharCode(103,95,56,50,95,108,111,99,107,110,101,115,115,0);
         subsq /= Math.max(2, parseFloat(`${iconrefreshk.length * 2}`));
         homeI /= Math.max(3, parseFloat(`${mbnativeK.length << (Math.min(Math.abs(1), 3))}`));
         modelsT = new Map([[`${modelsT.size}`, parseInt(`${checkboxG}`)]]);
         checkboxG -= 1;
         mbnativeK.push(parseInt(`${checkboxG}`));
         iconrefreshk += "3";
         break;
      }
      while (3.93 >= (2.78 - subsq) || 2.78 >= subsq) {
         subsq += parseFloat(`${parseInt(`${subsq}`) << (Math.min(1, Math.abs(2)))}`);
         break;
      }
      if (5.38 == subsq) {
          let footballfiledlayoutt = 5.0;
         subsq += parseFloat(`${parseInt(`${footballfiledlayoutt}`) << (Math.min(1, Math.abs(1)))}`);
      }
         roomC = !roomC;
      while ((subsq * 4.60) > 4.65) {
          let j_playerK = 3.0;
          let delegate_3kd = String.fromCharCode(98,95,51,57,95,109,111,100,105,102,105,99,97,116,105,111,110,0);
          let fastforwardw: Array<any> = [String.fromCharCode(98,101,99,97,117,115,101,95,107,95,55,51,0), String.fromCharCode(115,112,105,110,95,52,95,49,51,0), String.fromCharCode(98,95,51,50,95,119,114,105,116,105,110,103,0)];
          let foundD = 4.0;
         subsq *= parseFloat(`${parseInt(`${subsq}`) + 3}`);
         j_playerK /= Math.max(2, fastforwardw.length << (Math.min(5, Math.abs(parseInt(`${foundD}`)))));
         delegate_3kd = "2";
         fastforwardw.push(delegate_3kd.length);
         foundD /= Math.max(5, parseFloat(`${parseInt(`${foundD}`) / (Math.max(6, delegate_3kd.length))}`));
         break;
      }
          let yellowcirclebgT = String.fromCharCode(110,101,105,103,104,98,111,117,114,95,51,95,56,48,0);
          let orangeuparrow6: Array<any> = [362, 43];
          let networkX: Map<any, any> = new Map([[String.fromCharCode(105,110,116,101,114,112,111,108,97,116,111,114,95,57,95,56,0),89], [String.fromCharCode(111,95,51,57,95,105,110,100,105,99,97,116,111,114,0),878], [String.fromCharCode(110,97,109,101,116,111,105,110,100,101,120,95,109,95,53,55,0),862]]);
         subsq *= (parseFloat(`${3 ^ (roomC ? 2 : 3)}`));
         yellowcirclebgT = `${orangeuparrow6.length}`;
         orangeuparrow6.push(networkX.size);
         networkX.set(`${orangeuparrow6.length}`, networkX.size / (Math.max(2, 8)));
      progresse.set(`${orangeclockI}`, parseInt(`${downi}`) / 1);
      break;
   }

    setRefreshing(true);

   for (let p = 0; p < 2; p++) {
      live2 /= Math.max(4, parseInt(`${libreactnativejniA}`));
   }
       let middleware8: Array<any> = [468, 7, 611];
      for (let q = 0; q < 1; q++) {
         middleware8.push(2);
      }
      for (let p = 0; p < 2; p++) {
         middleware8.push(middleware8.length * middleware8.length);
      }
          let whatsappT = 3;
         middleware8 = [middleware8.length];
         whatsappT |= whatsappT - whatsappT;
      progresse = new Map([[`${live2}`, (String.fromCharCode(56,0) == disconnectedlogop ? disconnectedlogop.length : parseInt(`${live2}`))]]);
      downi += ((orangeclockI ? 4 : 5) - progresse.size);
   for (let v = 0; v < 3; v++) {
      disconnectedlogop = `${parseInt(`${live2}`)}`;
   }
   if (progresse.size == 3) {
      progresse = new Map([[`${rewardvideob}`, 2 >> (Math.min(Math.abs(parseInt(`${live2}`)), 4))]]);
   }
   for (let t = 0; t < 2; t++) {
       let libswresample0: Map<any, any> = new Map([[String.fromCharCode(118,99,100,97,116,97,95,55,95,56,54,0),String.fromCharCode(112,111,108,121,115,116,97,114,95,122,95,52,51,0)], [String.fromCharCode(97,112,112,108,105,101,115,95,115,95,51,51,0),String.fromCharCode(105,110,102,111,115,95,49,95,56,57,0)], [String.fromCharCode(101,118,117,116,105,108,95,104,95,56,49,0),String.fromCharCode(114,111,119,115,107,105,112,95,121,95,55,0)]]);
       let ewardedW: Array<any> = [930, 458];
      let shootM = 7310374 >= ewardedW.length;
      do {
          let iconmorez = 1.0;
          let largesoundi = 3.0;
          let defaultprofilepic1 = String.fromCharCode(102,95,55,95,115,121,110,116,104,102,105,108,116,0);
          let textinputN: Map<any, any> = new Map([[String.fromCharCode(115,116,97,116,101,109,101,110,116,115,95,49,95,57,54,0),469], [String.fromCharCode(100,114,105,102,116,95,110,95,57,52,0),587], [String.fromCharCode(100,105,115,116,114,97,99,116,105,111,110,95,56,95,49,48,48,0),146]]);
          let rewardvideob8 = String.fromCharCode(122,95,53,51,95,99,108,105,112,98,111,97,114,100,0);
         ewardedW.push(libswresample0.size);
         iconmorez *= parseFloat(`${defaultprofilepic1.length / (Math.max(8, parseInt(`${largesoundi}`)))}`);
         largesoundi += parseInt(`${iconmorez}`);
         defaultprofilepic1 = "3";
         textinputN.set(`${iconmorez}`, parseInt(`${iconmorez}`) | textinputN.size);
         rewardvideob8 += "2";
         if (shootM) {
            break;
         }
      } while (shootM && (!Array.from(libswresample0.values()).includes(ewardedW.length)));
      for (let h = 0; h < 3; h++) {
         libswresample0.set(`${ewardedW.length}`, ewardedW.length);
      }
      submitm = `${parseInt(`${jingdongw}`) - parseInt(`${rewardvideob}`)}`;
   }
      dragclosed.push(parseInt(`${jingdongw}`) / (Math.max(1, 5)));
      downi -= parseInt(`${rewardvideob}`) & dragclosed.length;
      libreactnativejniA -= parseFloat(`${3 + dragclosed.length}`);
      middlewarek *= progresse.size;
   let smallorangemanj = dragclosed.length >= 9801319;
   do {
      dragclosed = [2];
      if (smallorangemanj) {
         break;
      }
   } while (smallorangemanj && (3 == (progresse.size / 1)));
    await refreshUserState();

   if (1 <= (2 | dragclosed.length)) {
      jingdongw -= parseFloat(`${parseInt(`${rewardvideob}`) >> (Math.min(3, Math.abs(parseInt(`${downi}`))))}`);
   }
   if ((live2 / (Math.max(libreactnativejniA, 7))) > 2.98 || 5.18 > (live2 / (Math.max(2.98, 10)))) {
       let miniC = String.fromCharCode(116,104,114,109,97,116,95,100,95,51,51,0);
       let gradleE = String.fromCharCode(110,95,53,53,95,100,111,109,101,115,116,105,99,0);
      while (gradleE == String.fromCharCode(106,0)) {
          let indext = 4.0;
          let bgvipxvodW = String.fromCharCode(98,95,48,95,99,111,101,102,102,0);
         miniC = `${gradleE.length + 3}`;
         indext += parseFloat(`${parseInt(`${indext}`) * bgvipxvodW.length}`);
         bgvipxvodW += `${(String.fromCharCode(113,0) == bgvipxvodW ? parseInt(`${indext}`) : bgvipxvodW.length)}`;
         break;
      }
         miniC = `${miniC.length >> (Math.min(2, gradleE.length))}`;
      if (miniC != String.fromCharCode(54,0) && gradleE != String.fromCharCode(74,0)) {
         gradleE = `${miniC.length << (Math.min(Math.abs(3), 3))}`;
      }
      if (gradleE.length > 2 && 2 > miniC.length) {
         gradleE = `${(miniC == String.fromCharCode(66,0) ? gradleE.length : miniC.length)}`;
      }
         gradleE += `${gradleE.length & 3}`;
         gradleE += "2";
      libreactnativejniA -= (parseFloat(`${(defaultpredictionprofilel ? 3 : 2)}`));
   }
       let chart7 = String.fromCharCode(99,111,108,111,114,101,100,95,50,95,56,56,0);
      while (chart7.endsWith(chart7)) {
         chart7 = `${1 & chart7.length}`;
         break;
      }
          let gdtadvJ = String.fromCharCode(98,95,56,55,95,102,105,108,108,112,0);
          let plusU = 5.0;
          let taiwanj = 4.0;
         chart7 += `${2 & gdtadvJ.length}`;
         gdtadvJ += `${parseInt(`${plusU}`)}`;
         taiwanj *= parseFloat(`${3 >> (Math.min(Math.abs(parseInt(`${plusU}`)), 5))}`);
      for (let i = 0; i < 1; i++) {
          let phoneshareE: Map<any, any> = new Map([[String.fromCharCode(120,95,55,50,95,101,108,97,112,115,101,100,0),true ], [String.fromCharCode(108,111,110,103,98,105,103,95,55,95,56,52,0),true ], [String.fromCharCode(102,114,101,113,98,97,114,107,95,52,95,55,48,0),true ]]);
          let transfern: Array<any> = [396, 722];
         chart7 += `${transfern.length}`;
         phoneshareE = new Map([[`${phoneshareE.size}`, 1]]);
         transfern = [phoneshareE.size & phoneshareE.size];
      }
      defaultpredictionprofilel = jingdongw <= 98.11;
       let iconsharefriends3 = 5.0;
      let pagea = iconsharefriends3 <= 9005656.0;
      do {
          let profileinactives = 4.0;
          let defaultpredictionprofileo = String.fromCharCode(117,110,98,108,117,114,95,104,95,49,53,0);
          let mnewsshareX = String.fromCharCode(104,95,57,57,95,100,114,101,102,0);
          let goalt: Map<any, any> = new Map([[String.fromCharCode(97,95,56,52,95,116,105,99,107,115,0),695], [String.fromCharCode(100,95,52,57,95,100,97,116,97,98,97,115,101,0),652]]);
         iconsharefriends3 *= (mnewsshareX == String.fromCharCode(81,0) ? mnewsshareX.length : parseInt(`${iconsharefriends3}`));
         profileinactives /= Math.max(parseFloat(`${defaultpredictionprofileo.length & 1}`), 3);
         defaultpredictionprofileo += `${2 + goalt.size}`;
         goalt = new Map([[`${goalt.size}`, 2 ^ goalt.size]]);
         if (pagea) {
            break;
         }
      } while (((iconsharefriends3 * 3.46) == 3.31) && pagea);
      let vipsports = 7690622.0 >= iconsharefriends3;
      do {
          let delegate_gfS = 2;
          let dycreatorl = String.fromCharCode(105,110,116,101,110,115,105,116,121,95,116,95,56,48,0);
          let reactnativeratingsm = String.fromCharCode(107,95,55,49,95,104,101,108,112,0);
          let pathV: Map<any, any> = new Map([[String.fromCharCode(97,117,120,100,97,116,97,95,108,95,55,50,0),String.fromCharCode(104,95,53,52,95,112,114,105,110,116,101,114,0)], [String.fromCharCode(122,95,54,57,95,119,105,110,115,111,99,107,0),String.fromCharCode(99,97,99,104,101,100,95,103,95,55,48,0)]]);
         iconsharefriends3 -= delegate_gfS;
         delegate_gfS %= Math.max(pathV.size, 1);
         dycreatorl = "3";
         reactnativeratingsm += `${pathV.size % (Math.max(9, reactnativeratingsm.length))}`;
         if (vipsports) {
            break;
         }
      } while ((iconsharefriends3 <= iconsharefriends3) && vipsports);
         iconsharefriends3 *= parseInt(`${iconsharefriends3}`);
      orangeclockI = dragclosed.length >= iconsharefriends3;
   for (let w = 0; w < 3; w++) {
       let tempP = String.fromCharCode(98,101,116,116,101,114,95,109,95,51,0);
       let type_f0c: Array<any> = [382, 64, 884];
       let gmaili = 4.0;
       let imagenetworkerrF = 1.0;
       let iconqqb = 5.0;
      for (let d = 0; d < 1; d++) {
         imagenetworkerrF += 2 - parseInt(`${gmaili}`);
      }
          let detailsO = true;
          let single9 = String.fromCharCode(115,104,97,114,112,110,101,115,115,95,122,95,53,57,0);
          let inactive6 = 4;
         type_f0c.push(2);
         detailsO = 95 >= inactive6 && single9.length >= 95;
         single9 += `${(2 & (detailsO ? 3 : 3))}`;
         inactive6 *= 2;
      while (3 < (type_f0c.length + tempP.length) && (type_f0c.length + 3) < 2) {
         tempP = `${parseInt(`${imagenetworkerrF}`)}`;
         break;
      }
         type_f0c.push(2);
         imagenetworkerrF /= Math.max(tempP.length, 3);
       let attributedstrings = true;
          let abidetectx: Array<any> = [String.fromCharCode(109,101,97,115,117,114,105,110,103,95,117,95,52,48,0), String.fromCharCode(112,95,53,52,95,104,101,97,100,0), String.fromCharCode(110,95,53,55,95,121,97,100,105,102,0)];
          let screenr: Map<any, any> = new Map([[String.fromCharCode(107,95,53,55,95,98,121,116,101,99,111,100,101,118,116,97,98,0),90], [String.fromCharCode(119,95,55,48,95,101,120,105,115,116,115,0),48]]);
          let holderM = true;
         gmaili -= parseFloat(`${screenr.size}`);
         abidetectx.push(abidetectx.length);
         screenr.set(`${holderM}`, 2);
          let libreactnativeblobm = 0;
          let iconbellactive8 = String.fromCharCode(101,120,97,99,116,108,121,95,99,95,52,56,0);
          let homeloadingL = String.fromCharCode(102,95,54,55,95,98,97,115,105,115,0);
         gmaili += (parseFloat(`${iconbellactive8 == String.fromCharCode(98,0) ? iconbellactive8.length : libreactnativeblobm}`));
         libreactnativeblobm ^= homeloadingL.length | 3;
         iconqqb /= Math.max(3, ((attributedstrings ? 3 : 3) | 2));
      if (4 <= (tempP.length | type_f0c.length)) {
         type_f0c.push(parseInt(`${iconqqb}`) / 2);
      }
      for (let y = 0; y < 2; y++) {
         imagenetworkerrF += (parseInt(`${gmaili}`) + (attributedstrings ? 5 : 2));
      }
          let penaltygoalG = 0;
          let headerR = String.fromCharCode(118,101,110,99,95,117,95,50,51,0);
          let typingloadingd = 2.0;
         type_f0c = [parseInt(`${imagenetworkerrF}`) * 1];
         penaltygoalG /= Math.max(1, (String.fromCharCode(84,0) == headerR ? parseInt(`${typingloadingd}`) : headerR.length));
         typingloadingd *= parseFloat(`${3 << (Math.min(4, Math.abs(parseInt(`${typingloadingd}`))))}`);
      let mathC = iconqqb <= 8333855.0;
      do {
         iconqqb -= type_f0c.length;
         if (mathC) {
            break;
         }
      } while (((4.87 - iconqqb) == 1.31) && mathC);
         type_f0c.push(parseInt(`${imagenetworkerrF}`));
      if (type_f0c.length <= 2) {
         imagenetworkerrF /= Math.max(2, 2);
      }
      disconnectedlogop += `${parseInt(`${rewardvideob}`) / 2}`;
   }
   let yingj = orangeclockI ? !orangeclockI : orangeclockI;
   do {
      orangeclockI = null != progresse.get(`${live2}`);
      if (yingj) {
         break;
      }
   } while (((downi / (Math.max(4.75, 6))) <= 3.85 && orangeclockI) && yingj);
      libreactnativejniA /= Math.max(2, parseFloat(`${parseInt(`${middlewarek}`) % 3}`));
   while (4.79 <= actions5) {
      actions5 *= (submitm == String.fromCharCode(79,0) ? submitm.length : progresse.size);
      break;
   }
   if (libreactnativejniA < jingdongw) {
      libreactnativejniA /= Math.max(5, parseFloat(`${2}`));
   }
   while (3 == (submitm.length ^ 5) && (progresse.size ^ submitm.length) == 5) {
       let rewardO = String.fromCharCode(106,95,55,54,95,97,118,101,114,97,103,101,105,105,114,100,0);
       let mintegralZ = String.fromCharCode(114,95,56,51,95,112,114,111,99,101,115,115,101,100,0);
       let profilet = true;
       let iconwatchnowg = 3;
       let minivod2 = false;
         iconwatchnowg <<= Math.min(Math.abs(2), 5);
      while (mintegralZ != rewardO) {
         rewardO += "3";
         break;
      }
         profilet = String.fromCharCode(67,0) == rewardO;
         iconwatchnowg %= Math.max(2, ((profilet ? 5 : 3) & 2));
       let description_6P: Array<any> = [714, 707];
       let refreshborderlessb: Array<any> = [String.fromCharCode(114,117,98,121,95,101,95,51,48,0), String.fromCharCode(98,95,56,95,97,116,111,102,0), String.fromCharCode(115,109,106,112,101,103,95,111,95,49,48,48,0)];
         iconwatchnowg /= Math.max(((minivod2 ? 3 : 2) / (Math.max(rewardO.length, 9))), 2);
      if (!mintegralZ.endsWith(`${description_6P.length}`)) {
         mintegralZ += `${mintegralZ.length}`;
      }
      for (let v = 0; v < 2; v++) {
          let u_center3: Array<any> = [String.fromCharCode(120,95,51,53,95,111,109,112,111,115,101,0), String.fromCharCode(112,97,114,101,110,116,95,121,95,50,52,0)];
          let bingJ = 3.0;
          let defaultpredictionprofilet = String.fromCharCode(99,104,97,115,101,95,120,95,49,56,0);
          let teamX = 4.0;
         iconwatchnowg += 3 >> (Math.min(2, Math.abs(parseInt(`${teamX}`))));
         u_center3.push(parseInt(`${bingJ}`) - 2);
         bingJ -= defaultpredictionprofilet.length + 3;
         defaultpredictionprofilet += `${(String.fromCharCode(97,0) == defaultpredictionprofilet ? defaultpredictionprofilet.length : u_center3.length)}`;
         teamX *= parseFloat(`${2}`);
      }
      let renewD = minivod2 ? !minivod2 : minivod2;
      do {
         minivod2 = 18 > description_6P.length || 18 > iconwatchnowg;
         if (renewD) {
            break;
         }
      } while (renewD && (minivod2));
         description_6P.push((iconwatchnowg - (minivod2 ? 3 : 1)));
         mintegralZ = `${(description_6P.length & (minivod2 ? 1 : 2))}`;
      let static_sx5 = 7510016 >= description_6P.length;
      do {
          let previewo = 3;
          let ewardeds: Map<any, any> = new Map([[String.fromCharCode(108,95,50,55,95,99,105,110,116,0),475], [String.fromCharCode(103,104,97,115,104,95,51,95,55,48,0),706]]);
          let pingX = 2.0;
         description_6P = [parseInt(`${pingX}`) >> (Math.min(2, Math.abs(2)))];
         previewo &= previewo - ewardeds.size;
         ewardeds = new Map([[`${ewardeds.size}`, ewardeds.size ^ 2]]);
         pingX += parseFloat(`${previewo - ewardeds.size}`);
         if (static_sx5) {
            break;
         }
      } while ((!profilet) && static_sx5);
         profilet = minivod2 || mintegralZ.length == 80;
          let fulla = String.fromCharCode(115,116,97,114,115,95,114,95,51,48,0);
         mintegralZ = `${(fulla == String.fromCharCode(50,0) ? iconwatchnowg : fulla.length)}`;
          let mailW = String.fromCharCode(111,95,52,49,95,101,120,116,101,110,115,105,98,105,108,105,116,121,0);
          let configA = 1;
         description_6P = [iconwatchnowg];
         mailW = `${configA}`;
         configA ^= mailW.length;
      submitm += "1";
      break;
   }
   if ((jingdongw + downi) == 4.4 && (4.4 + jingdongw) == 4.29) {
       let megaphoneW = false;
       let iconeditP = String.fromCharCode(120,95,52,56,95,114,116,112,109,97,112,0);
       let icontransferclubg = String.fromCharCode(114,97,110,115,112,111,114,116,95,49,95,52,53,0);
       let d_hash2 = String.fromCharCode(118,95,57,95,108,111,99,97,116,105,111,110,0);
       let popupW: Array<any> = [971, 380];
      while (popupW.length >= 5) {
         icontransferclubg += `${iconeditP.length >> (Math.min(Math.abs(2), 5))}`;
         break;
      }
         megaphoneW = String.fromCharCode(107,0) == d_hash2;
          let actionsp = String.fromCharCode(107,95,57,52,95,117,109,102,97,118,114,0);
         icontransferclubg = `${iconeditP.length}`;
         actionsp += `${(String.fromCharCode(99,0) == actionsp ? actionsp.length : actionsp.length)}`;
      while (d_hash2.length >= icontransferclubg.length) {
         d_hash2 = `${1 ^ icontransferclubg.length}`;
         break;
      }
      while (popupW.length == icontransferclubg.length) {
         icontransferclubg = `${iconeditP.length}`;
         break;
      }
         iconeditP = `${popupW.length & 1}`;
         iconeditP += `${d_hash2.length - icontransferclubg.length}`;
      let whitebellS = 6211993 >= popupW.length;
      do {
          let pointI = String.fromCharCode(105,95,50,55,95,99,97,116,101,103,111,114,105,122,101,0);
          let gesturez: Map<any, any> = new Map([[String.fromCharCode(122,95,56,56,95,97,115,121,110,99,104,114,111,110,111,117,115,0),String.fromCharCode(119,95,55,49,95,109,117,116,97,116,101,0)], [String.fromCharCode(112,111,115,116,102,105,108,116,101,114,95,112,95,50,51,0),String.fromCharCode(115,99,97,110,116,97,98,108,101,95,112,95,55,51,0)], [String.fromCharCode(110,111,110,110,117,108,108,98,117,102,102,101,114,95,54,95,54,51,0),String.fromCharCode(115,116,97,99,104,95,110,95,57,53,0)]]);
         popupW.push(gesturez.size);
         pointI = `${pointI.length}`;
         gesturez = new Map([[pointI, pointI.length / 2]]);
         if (whitebellS) {
            break;
         }
      } while (whitebellS && (megaphoneW || (4 & popupW.length) < 2));
      let refreshborderlessl = megaphoneW ? !megaphoneW : megaphoneW;
      do {
         megaphoneW = 48 < d_hash2.length;
         if (refreshborderlessl) {
            break;
         }
      } while (refreshborderlessl && (!megaphoneW));
          let flyerU = String.fromCharCode(115,95,53,56,95,109,111,118,105,101,0);
          let logoh = 1;
          let refresh8 = true;
         iconeditP = `${iconeditP.length}`;
         flyerU += `${3 << (Math.min(Math.abs(logoh), 5))}`;
         logoh <<= Math.min(2, Math.abs(2 + logoh));
         refresh8 = flyerU.length < logoh;
      for (let o = 0; o < 1; o++) {
         megaphoneW = popupW.length == d_hash2.length;
      }
       let inviteQ: Array<any> = [String.fromCharCode(102,117,122,122,105,110,103,95,51,95,55,57,0), String.fromCharCode(99,95,53,55,95,99,108,105,99,107,115,0), String.fromCharCode(112,101,101,114,115,95,106,95,55,49,0)];
       let binddatasX: Array<any> = [234, 34, 201];
         iconeditP = `${popupW.length % (Math.max(6, d_hash2.length))}`;
      let backT = 6113947 >= binddatasX.length;
      do {
          let emojiheart8 = 2.0;
         binddatasX.push(binddatasX.length);
         emojiheart8 -= 2;
         if (backT) {
            break;
         }
      } while ((1 == binddatasX.length) && backT);
      for (let w = 0; w < 3; w++) {
         icontransferclubg += `${popupW.length - d_hash2.length}`;
      }
      jingdongw -= parseFloat(`${1 >> (Math.min(4, iconeditP.length))}`);
   }
    setRefreshing(false);
  };

  const refreshUserState = async () => {
       let megaphoneT: Array<any> = [828, 175];
    let awayicono: Map<any, any> = new Map([[String.fromCharCode(113,95,57,49,95,98,97,110,100,0),452], [String.fromCharCode(101,109,109,105,110,116,114,105,110,95,112,95,51,57,0),389]]);
    let anewinterstitialI = 2.0;
    let chinasameM = String.fromCharCode(108,95,56,50,95,105,115,115,0);
    let friendsd = 3;
    let libfabricjniR = false;
    let privilege9 = false;
    let iconeditt = String.fromCharCode(109,95,54,54,95,100,105,118,105,100,101,100,0);
    let dependencyz = true;
    let basketballawayteam9 = String.fromCharCode(98,95,54,56,95,112,99,97,99,104,101,0);
    let bdxadsdkS = String.fromCharCode(113,95,50,51,95,97,103,101,110,116,115,0);
    let appleO: Array<any> = [254, 914, 150];
    let layoutP = 0.0;
    let event3 = true;
    let carouselR = String.fromCharCode(97,114,107,101,114,95,103,95,53,51,0);
    let anytimek = 4;
    let kickQ: Map<any, any> = new Map([[String.fromCharCode(120,95,55,54,95,101,110,99,111,100,101,100,102,114,97,109,101,0),513], [String.fromCharCode(100,95,54,55,95,99,97,114,101,102,117,108,108,121,0),1], [String.fromCharCode(118,95,57,57,95,114,97,105,116,0),917]]);
   if (!libfabricjniR) {
      friendsd %= Math.max(3 ^ iconeditt.length, 5);
   }
   if (!dependencyz) {
      dependencyz = anewinterstitialI < 28.42;
   }
   if (1 <= (iconeditt.length + awayicono.size)) {
       let assetsA = true;
       let gift_ = String.fromCharCode(116,95,57,49,95,119,101,98,118,105,101,119,0);
       let serviceQ = 2.0;
       let successI: Map<any, any> = new Map([[String.fromCharCode(111,95,51,95,101,113,117,101,115,116,0),557], [String.fromCharCode(104,95,52,95,112,111,115,116,99,111,100,101,0),68]]);
       let fullscreenmaxG: Map<any, any> = new Map([[String.fromCharCode(116,95,49,54,95,116,114,105,97,110,103,117,108,97,114,0),String.fromCharCode(116,95,55,95,112,101,110,99,105,108,0)], [String.fromCharCode(100,101,105,110,118,101,114,116,95,115,95,49,50,0),String.fromCharCode(102,95,53,51,95,109,105,100,100,108,101,0)]]);
      let appsS = serviceQ >= 8664474.0;
      do {
         serviceQ *= parseFloat(`${2}`);
         if (appsS) {
            break;
         }
      } while (appsS && (4.57 >= (2.29 / (Math.max(5, serviceQ)))));
         fullscreenmaxG = new Map([[`${fullscreenmaxG.size}`, 2]]);
         gift_ += `${1 - parseInt(`${serviceQ}`)}`;
         fullscreenmaxG = new Map([[`${serviceQ}`, parseInt(`${serviceQ}`) % 2]]);
         successI = new Map([[`${successI.size}`, (3 / (Math.max(4, (assetsA ? 2 : 2))))]]);
      for (let l = 0; l < 2; l++) {
          let about6 = 0.0;
          let hongkongy = false;
          let f_positionc = 2.0;
         gift_ += `${successI.size & parseInt(`${about6}`)}`;
         about6 -= (parseFloat(`${parseInt(`${f_positionc}`) >> (Math.min(1, Math.abs((hongkongy ? 1 : 3))))}`));
         hongkongy = f_positionc == 99.70;
      }
      let layoutj = String.fromCharCode(95,51,106,116,118,53,104,99,50,49,0) == gift_;
      do {
         gift_ = `${1 - parseInt(`${serviceQ}`)}`;
         if (layoutj) {
            break;
         }
      } while (layoutj && ((3 | gift_.length) <= 3 || (serviceQ / (Math.max(parseFloat(`${gift_.length}`), 8))) <= 3.60));
      if (3 == (fullscreenmaxG.size & 4)) {
         fullscreenmaxG.set(gift_, (gift_ == String.fromCharCode(110,0) ? gift_.length : fullscreenmaxG.size));
      }
      iconeditt = `${((dependencyz ? 4 : 2) + parseInt(`${anewinterstitialI}`))}`;
   }
   while (!dependencyz) {
       let plusV = String.fromCharCode(112,114,101,102,101,114,114,101,100,95,53,95,54,51,0);
       let gradlew = false;
       let yinge: Array<any> = [3, 185];
          let eventsplashR = false;
          let binddatasM = String.fromCharCode(112,108,97,121,111,117,116,95,103,95,53,57,0);
         gradlew = (((!eventsplashR ? yinge.length : 37) >> (Math.min(yinge.length, 1))) < 37);
         eventsplashR = 28 <= binddatasM.length;
         binddatasM += `${binddatasM.length % (Math.max(6, binddatasM.length))}`;
       let halffieldimagem = true;
      if (!halffieldimagem && gradlew) {
         gradlew = halffieldimagem;
      }
         gradlew = yinge.includes(gradlew);
       let scoreZ = String.fromCharCode(112,108,97,99,101,104,111,108,100,101,114,115,95,98,95,55,49,0);
         plusV = `${((halffieldimagem ? 4 : 5) | (gradlew ? 1 : 4))}`;
         scoreZ += `${scoreZ.length}`;
       let predictionlossb = String.fromCharCode(121,95,56,95,101,118,117,116,105,108,0);
       let sellj = String.fromCharCode(108,95,55,57,95,98,108,111,99,107,104,97,115,104,0);
      if ((3 & yinge.length) > 4) {
         halffieldimagem = scoreZ.length < 10;
      }
      dependencyz = 25 == friendsd;
      break;
   }
   for (let t = 0; t < 3; t++) {
       let templateprocessorA: Array<any> = [564, 895, 544];
       let libtanS = String.fromCharCode(121,95,56,48,95,100,105,121,102,112,0);
       let final_qr = 0.0;
      while ((2 ^ libtanS.length) >= 3 && (templateprocessorA.length ^ 2) >= 2) {
         libtanS += `${(String.fromCharCode(102,0) == libtanS ? libtanS.length : parseInt(`${final_qr}`))}`;
         break;
      }
         libtanS += `${libtanS.length >> (Math.min(1, templateprocessorA.length))}`;
          let chatroombackgroundq = 3;
         templateprocessorA = [chatroombackgroundq % 3];
      while ((5 + libtanS.length) <= 4) {
         final_qr -= libtanS.length >> (Math.min(3, Math.abs(parseInt(`${final_qr}`))));
         break;
      }
          let hongkongK = false;
          let libavdeviceg = String.fromCharCode(113,95,57,57,95,104,101,97,100,112,104,111,110,101,0);
          let downloadere = String.fromCharCode(97,110,99,101,115,116,114,121,95,121,95,52,56,0);
         libtanS += "3";
         hongkongK = libavdeviceg.length >= 17;
         libavdeviceg = `${(libavdeviceg == String.fromCharCode(86,0) ? downloadere.length : libavdeviceg.length)}`;
         downloadere += `${libavdeviceg.length}`;
      if (4 >= (libtanS.length << (Math.min(1, templateprocessorA.length))) || 4 >= (templateprocessorA.length << (Math.min(libtanS.length, 5)))) {
         templateprocessorA = [2];
      }
       let langkeyo = String.fromCharCode(102,95,51,52,95,98,97,110,100,0);
      while (templateprocessorA.length >= 4) {
         libtanS = `${templateprocessorA.length - parseInt(`${final_qr}`)}`;
         break;
      }
      if (4.76 < (3.99 * final_qr) && (langkeyo.length * final_qr) < 3.99) {
          let goallogoJ = String.fromCharCode(106,95,56,56,95,102,105,108,116,101,114,0);
          let libreanimatedh = 5.0;
          let rounde = String.fromCharCode(117,95,53,95,109,111,110,105,116,111,114,105,110,103,0);
          let viewsq = 4.0;
         final_qr += parseInt(`${final_qr}`) % 2;
         goallogoJ += `${(rounde == String.fromCharCode(106,0) ? rounde.length : parseInt(`${libreanimatedh}`))}`;
         libreanimatedh -= (parseFloat(`${String.fromCharCode(106,0) == goallogoJ ? parseInt(`${viewsq}`) : goallogoJ.length}`));
         viewsq /= Math.max(parseFloat(`${parseInt(`${viewsq}`)}`), 2);
      }
      megaphoneT = [chinasameM.length << (Math.min(5, appleO.length))];
   }
   for (let r = 0; r < 3; r++) {
      bdxadsdkS += `${megaphoneT.length}`;
   }
   while (basketballawayteam9.length > 3) {
      bdxadsdkS += `${((libfabricjniR ? 5 : 3) | 1)}`;
      break;
   }

    const result = await wawaEvent.getUserDetails();

   if (!privilege9) {
      privilege9 = appleO.length >= 56;
   }
   while (bdxadsdkS.length >= chinasameM.length) {
       let iconcalendarr = 4.0;
       let bingX = String.fromCharCode(114,95,52,53,95,101,110,99,111,100,97,98,108,101,0);
       let iconsharef = 1.0;
       let tickedD = 0.0;
       let downloading6: Array<any> = [389, 530, 872];
      let anytimeJ = iconsharef <= 8031987.0;
      do {
         iconsharef -= parseInt(`${iconcalendarr}`);
         if (anytimeJ) {
            break;
         }
      } while (anytimeJ && (tickedD == 5.98));
      let graph5 = 6505250.0 >= iconsharef;
      do {
         iconsharef *= parseInt(`${iconcalendarr}`);
         if (graph5) {
            break;
         }
      } while (((1 & bingX.length) > 4) && graph5);
          let libswscaleb = String.fromCharCode(101,122,111,115,95,117,95,53,0);
          let securityD = String.fromCharCode(106,95,49,56,95,97,116,116,97,99,104,109,101,110,116,0);
          let renew7 = String.fromCharCode(104,95,49,48,95,115,101,101,107,116,97,98,108,101,0);
         iconcalendarr += 3 * parseInt(`${tickedD}`);
         libswscaleb += `${securityD.length ^ 2}`;
         securityD = "3";
         renew7 += `${(securityD == String.fromCharCode(52,0) ? libswscaleb.length : securityD.length)}`;
      while (1.94 <= (iconsharef + bingX.length) && 4 <= (bingX.length / (Math.max(3, 7)))) {
         bingX += `${parseInt(`${iconsharef}`) + bingX.length}`;
         break;
      }
      if (3 <= (downloading6.length + bingX.length)) {
         bingX += `${parseInt(`${tickedD}`)}`;
      }
      if (5 >= (5 % (Math.max(6, downloading6.length)))) {
         tickedD -= parseFloat(`${3 - parseInt(`${tickedD}`)}`);
      }
         tickedD -= parseFloat(`${1}`);
      for (let c = 0; c < 2; c++) {
         bingX = "2";
      }
         iconcalendarr *= 2 | parseInt(`${iconsharef}`);
      let traminiV = iconsharef <= 8079382.0;
      do {
         iconsharef += parseInt(`${iconcalendarr}`) * 1;
         if (traminiV) {
            break;
         }
      } while (((iconcalendarr / (Math.max(3, iconsharef))) > 3.95) && traminiV);
         iconsharef *= parseInt(`${iconsharef}`);
         iconsharef *= 3;
      while (!bingX.startsWith(`${tickedD}`)) {
          let paginationg = String.fromCharCode(103,95,50,51,95,101,118,116,97,103,0);
         tickedD += (parseFloat(`${bingX == String.fromCharCode(112,0) ? parseInt(`${iconcalendarr}`) : bingX.length}`));
         paginationg += `${paginationg.length}`;
         break;
      }
          let bootsplashd: Map<any, any> = new Map([[String.fromCharCode(97,117,116,104,111,114,105,116,121,95,114,95,54,57,0),478], [String.fromCharCode(119,95,50,54,95,117,110,99,111,109,112,114,101,115,115,0),981], [String.fromCharCode(109,97,100,101,98,121,95,100,95,50,48,0),808]]);
         bingX = `${downloading6.length}`;
         bootsplashd.set(`${bootsplashd.size}`, bootsplashd.size + 1);
      for (let c = 0; c < 2; c++) {
          let pushp = 1.0;
         bingX = `${bingX.length * parseInt(`${pushp}`)}`;
      }
      bdxadsdkS += `${bdxadsdkS.length >> (Math.min(Math.abs(1), 2))}`;
      break;
   }
   while (iconeditt.startsWith(bdxadsdkS)) {
      bdxadsdkS = `${(chinasameM == String.fromCharCode(74,0) ? megaphoneT.length : chinasameM.length)}`;
      break;
   }
      anewinterstitialI /= Math.max(3, parseFloat(`${bdxadsdkS.length - appleO.length}`));
   if (basketballawayteam9.includes(`${dependencyz}`)) {
      dependencyz = parseFloat(`${megaphoneT.length}`) == anewinterstitialI;
   }
   while ((1 | friendsd) <= 2 && !privilege9) {
       let dycreatorG = String.fromCharCode(114,101,97,100,108,110,95,106,95,54,55,0);
       let imagenomoredataE: Array<any> = [927, 951];
       let rockete = 4;
          let windQ = String.fromCharCode(122,95,51,49,95,100,101,110,111,109,0);
          let overb = 1.0;
         imagenomoredataE = [rockete & windQ.length];
         windQ += `${parseInt(`${overb}`) >> (Math.min(4, Math.abs(3)))}`;
         overb *= parseInt(`${overb}`);
       let with_0v_ = 0.0;
       let termsS = 0.0;
         termsS += parseFloat(`${parseInt(`${termsS}`) / 2}`);
      if (5 < (1 ^ dycreatorG.length)) {
         dycreatorG = "2";
      }
      while ((rockete + with_0v_) <= 2.58) {
          let stary = String.fromCharCode(102,99,117,110,112,117,98,108,105,115,104,95,120,95,51,49,0);
         with_0v_ -= parseFloat(`${imagenomoredataE.length + rockete}`);
         stary = `${1 / (Math.max(8, stary.length))}`;
         break;
      }
      if (with_0v_ <= rockete) {
          let reactnavigationg = 0.0;
          let twittert = String.fromCharCode(114,95,54,55,95,105,109,105,113,0);
          let renewZ: Array<any> = [390, 986, 199];
          let awayD: Array<any> = [395, 656];
         rockete ^= parseInt(`${termsS}`);
         reactnavigationg /= Math.max(parseFloat(`${1}`), 2);
         twittert += `${renewZ.length}`;
         renewZ = [(String.fromCharCode(82,0) == twittert ? renewZ.length : twittert.length)];
         awayD = [parseInt(`${reactnavigationg}`)];
      }
          let emptyC: Map<any, any> = new Map([[String.fromCharCode(118,95,52,50,95,115,101,97,114,99,104,0),123], [String.fromCharCode(102,95,49,49,95,98,99,109,112,0),407], [String.fromCharCode(113,95,51,95,108,111,103,115,97,109,112,108,101,0),976]]);
          let toponK: Map<any, any> = new Map([[String.fromCharCode(113,95,50,52,95,112,105,100,0),512], [String.fromCharCode(105,110,116,114,105,110,95,110,95,57,0),544], [String.fromCharCode(110,111,110,115,101,99,117,114,101,95,106,95,50,56,0),133]]);
          let volume2 = 0;
         termsS += parseFloat(`${2}`);
         emptyC.set(`${volume2}`, emptyC.size + volume2);
         toponK = new Map([[`${toponK.size}`, emptyC.size / (Math.max(9, toponK.size))]]);
       let actionsj = 4;
       let chat8 = String.fromCharCode(105,109,112,108,105,101,115,95,110,95,53,54,0);
       let toponC = String.fromCharCode(114,101,115,117,109,101,100,95,101,95,54,49,0);
      friendsd ^= megaphoneT.length * parseInt(`${layoutP}`);
      break;
   }
      iconeditt += `${iconeditt.length}`;
    if (result == null) {

   while (3 == chinasameM.length) {
      appleO = [(iconeditt == String.fromCharCode(66,0) ? iconeditt.length : parseInt(`${anewinterstitialI}`))];
      break;
   }
   while ((2 >> (Math.min(1, megaphoneT.length))) >= 5) {
      megaphoneT = [appleO.length];
      break;
   }
      dependencyz = String.fromCharCode(100,0) == chinasameM && layoutP < 46.52;
   while (dependencyz) {
      appleO = [megaphoneT.length + 2];
      break;
   }
      privilege9 = ((chinasameM.length | (!event3 ? chinasameM.length : 91)) <= 91);
      appleO.push(1 >> (Math.min(Math.abs(parseInt(`${layoutP}`)), 4)));
   if (5 <= chinasameM.length) {
      chinasameM += `${appleO.length}`;
   }
      return;
    }

    await dispatch(updateUserAuth(result));

   for (let m = 0; m < 3; m++) {
      iconeditt += `${(String.fromCharCode(70,0) == bdxadsdkS ? (libfabricjniR ? 4 : 4) : bdxadsdkS.length)}`;
   }
      appleO = [awayicono.size | 1];
   while (3 == basketballawayteam9.length) {
      anewinterstitialI /= Math.max((parseFloat(`${(libfabricjniR ? 1 : 2) - megaphoneT.length}`)), 4);
      break;
   }
   while (basketballawayteam9 != String.fromCharCode(110,0)) {
      carouselR = `${carouselR.length - 1}`;
      break;
   }
   if (4 >= megaphoneT.length || (4 << (Math.min(5, megaphoneT.length))) >= 2) {
      megaphoneT.push(iconeditt.length);
   }
      layoutP *= parseFloat(`${appleO.length}`);
      basketballawayteam9 += "2";
    return;
  };

  return (
    <ScreenContainer>
      <TitleWithBackButtonHeader title="邀请好友奖励" />
      <ScrollView
        refreshControl={
          <RefreshControl refreshing={refreshing} onRefresh={handleRefresh} tintColor="#FAC33D" />
        }
      >
        {/* <TitleWithBackButtonHeader title="邀请好友奖励" /> */}
        <View style={{ paddingHorizontal: 0 }}>
          { }
          <InviteHeader />
          { }
          <InviteCard userState={userState.user!} />
          { }

          <TouchableOpacity
            onPress={() => {
              navigation.navigate("活动规则");
            }}
          >
            <View
              style={{
                flex: 1,
                justifyContent: "center",
                alignItems: "center",
                paddingVertical: 10,
              }}
            >
              <Text
                style={{
                  fontSize: 12,
                  textDecorationLine: "underline",
                  color: "#ffffff",
                }}
              >
                活动规则
              </Text>
            </View>
          </TouchableOpacity>
          { }
        </View>
        <InviteStep />
      </ScrollView>
    </ScreenContainer>
  );
};

const styles = StyleSheet.create({});
