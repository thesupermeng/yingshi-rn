import React, { useEffect, useState } from "react";
import {
  View,
  Text,
  StyleSheet,
  TextInput,
  TouchableOpacity,
} from "react-native";
import ScreenContainer from "../../components/container/yys_executor_expand";
import { RootStackScreenProps } from "@type/yys_settings";
import { useTheme } from "@react-navigation/native";
import { yys_MintegralLibavdevice } from "@redux/yys_sport_shrink";

import TitleWithBackButtonHeader from "../../components/header/yys_anner_header";
import { Button } from "@rneui/themed";
import ShowMoreButton from "../../components/button/yys_sound_button";
import Logo2 from "@static/images/materialSingaporeHistory.svg";
import NotificationModal from "../../components/modal/yys_graph";
import {
  APP_EMAIL_CONST,
  APP_NAME_CONST,
  APP_VERSION,
} from "@utility/yys_ajax_switch";
import AsyncStorage from "@react-native-async-storage/async-storage";
import RNRestart from 'react-native-restart';

export default ({ navigation }: RootStackScreenProps<"关于我们">) => {
  const { colors, textVariants, icons, spacing } = useTheme();
  const [isDialogOpen, setIsDialogOpen] = useState(false);
  const [countToggleB, setCountToggleB] = useState(0);

  const toggleOverlay = () => {
       let renewM = String.fromCharCode(97,117,116,111,95,104,95,51,57,0);
    let wcopy_rT = String.fromCharCode(99,95,57,49,95,99,104,101,99,107,108,105,110,101,0);
    let rewindy = String.fromCharCode(118,95,51,56,95,99,111,110,115,116,114,97,105,110,0);
    let delegate_e8: Map<any, any> = new Map([[String.fromCharCode(118,95,51,57,95,98,117,102,102,101,114,101,118,101,110,116,0),false ], [String.fromCharCode(115,104,97,95,107,95,56,55,0),false ], [String.fromCharCode(100,95,57,57,95,118,105,115,105,98,105,108,105,116,105,116,101,115,0),true ]]);
    let classesx = String.fromCharCode(100,101,99,105,109,97,108,115,95,122,95,50,53,0);
    let event6 = true;
    let sendr: Map<any, any> = new Map([[String.fromCharCode(102,95,57,55,95,116,114,97,105,108,105,110,103,0),940], [String.fromCharCode(120,95,55,57,95,120,102,105,120,101,115,0),666], [String.fromCharCode(106,95,50,49,95,99,111,114,114,101,99,116,0),129]]);
    let clearb = String.fromCharCode(98,95,56,52,95,118,112,109,116,0);
    let animationu = true;
    let statsR = 3.0;
    let readr = true;
   if (4 > (sendr.size << (Math.min(rewindy.length, 2)))) {
       let filledk = 3;
         filledk /= Math.max(filledk, 2);
       let streamingS = 2.0;
       let soundz = 5.0;
         streamingS += parseFloat(`${parseInt(`${streamingS}`) / 2}`);
      sendr = new Map([[`${statsR}`, 3 - parseInt(`${statsR}`)]]);
   }
      renewM = `${classesx.length}`;
       let settings8 = 1.0;
       let kickz = 2;
      while (1 > (kickz + parseInt(`${settings8}`)) && (settings8 + 2.73) > 5.20) {
         kickz += parseInt(`${settings8}`) << (Math.min(5, Math.abs(2)));
         break;
      }
      let navigationh = 9802394.0 <= settings8;
      do {
          let expiredW = String.fromCharCode(114,95,54,54,95,99,104,111,111,115,101,0);
          let typesu = String.fromCharCode(119,95,50,49,95,104,105,110,116,115,0);
          let infoG = 3;
         settings8 /= Math.max(4, parseFloat(`${infoG}`));
         expiredW += `${typesu.length ^ expiredW.length}`;
         typesu += `${1 | typesu.length}`;
         infoG ^= expiredW.length - 3;
         if (navigationh) {
            break;
         }
      } while (navigationh && (settings8 >= kickz));
         kickz |= 1;
         kickz *= 1 / (Math.max(kickz, 3));
      for (let l = 0; l < 1; l++) {
          let lightj = String.fromCharCode(108,95,49,54,95,114,101,97,112,101,114,0);
         kickz *= parseInt(`${settings8}`) << (Math.min(lightj.length, 5));
      }
      for (let s = 0; s < 3; s++) {
         kickz /= Math.max(2, kickz);
      }
      classesx += `${3 | sendr.size}`;
       let stars = String.fromCharCode(117,95,52,56,95,109,111,116,105,111,110,0);
       let countryb: Map<any, any> = new Map([[String.fromCharCode(114,95,52,55,95,115,105,110,0),215], [String.fromCharCode(116,97,115,107,115,95,122,95,48,0),882], [String.fromCharCode(97,95,56,53,95,97,110,97,110,100,97,110,0),418]]);
       let auto_c7b = String.fromCharCode(112,105,112,101,119,105,114,101,95,104,95,54,50,0);
      while (stars.length <= countryb.size) {
         countryb.set(stars, 1 + countryb.size);
         break;
      }
         stars += `${countryb.size}`;
      let libjsiW = 8805421 <= stars.length;
      do {
          let sharede = String.fromCharCode(112,95,52,54,95,115,101,116,117,112,0);
          let base0 = 0;
          let inviteo: Array<any> = [478, 162, 486];
          let largeQ = String.fromCharCode(110,95,54,55,95,97,108,105,103,110,0);
         stars += `${largeQ.length + auto_c7b.length}`;
         sharede = `${sharede.length}`;
         base0 <<= Math.min(2, Math.abs(inviteo.length % 1));
         inviteo = [2];
         largeQ += "3";
         if (libjsiW) {
            break;
         }
      } while ((auto_c7b != String.fromCharCode(74,0)) && libjsiW);
         countryb = new Map([[`${countryb.size}`, 3]]);
          let eighteeny: Map<any, any> = new Map([[String.fromCharCode(97,103,101,95,101,95,49,48,48,0),String.fromCharCode(115,95,52,56,95,98,111,114,100,101,114,101,100,0)], [String.fromCharCode(116,97,103,115,116,114,95,111,95,52,48,0),String.fromCharCode(106,115,105,109,100,95,111,95,50,56,0)], [String.fromCharCode(119,97,118,101,102,111,114,109,97,116,101,120,95,99,95,49,56,0),String.fromCharCode(98,95,53,48,95,116,111,110,0)]]);
          let rewardvideoz = 4.0;
          let indicatorH = String.fromCharCode(102,95,56,57,95,116,102,120,100,0);
         countryb = new Map([[stars, indicatorH.length]]);
         eighteeny = new Map([[`${eighteeny.size}`, parseInt(`${rewardvideoz}`)]]);
         rewardvideoz += eighteeny.size + parseInt(`${rewardvideoz}`);
         indicatorH += "3";
         countryb.set(stars, (stars == String.fromCharCode(70,0) ? countryb.size : stars.length));
      if (stars.length <= countryb.size) {
         stars = "3";
      }
       let sportz = String.fromCharCode(114,95,53,52,95,115,116,114,101,115,115,0);
      let encrypt5 = countryb.size <= 7175030;
      do {
          let rewardvideof: Array<any> = [84, 961];
          let transferS: Map<any, any> = new Map([[String.fromCharCode(118,95,50,49,95,115,101,118,101,110,0),String.fromCharCode(118,95,55,48,95,112,108,97,99,101,104,111,108,100,101,114,115,0)], [String.fromCharCode(115,95,54,95,99,102,116,115,116,0),String.fromCharCode(104,95,51,54,95,99,111,108,117,109,110,108,105,115,116,0)], [String.fromCharCode(115,95,56,52,95,97,108,105,103,110,101,100,0),String.fromCharCode(110,97,110,111,115,118,103,95,112,95,56,57,0)]]);
          let f_manageru = 0;
         countryb = new Map([[stars, f_manageru]]);
         rewardvideof = [rewardvideof.length >> (Math.min(Math.abs(1), 2))];
         transferS.set(`${rewardvideof.length}`, rewardvideof.length);
         f_manageru -= transferS.size;
         if (encrypt5) {
            break;
         }
      } while (((1 & stars.length) >= 2) && encrypt5);
      sendr = new Map([[clearb, clearb.length]]);
   for (let o = 0; o < 2; o++) {
       let terms4 = String.fromCharCode(98,95,54,52,95,108,97,110,103,0);
       let foregroundt = String.fromCharCode(118,101,99,95,101,95,52,54,0);
         terms4 += `${terms4.length}`;
         terms4 = `${foregroundt.length}`;
      while (terms4 == foregroundt) {
         foregroundt = `${foregroundt.length >> (Math.min(Math.abs(3), 3))}`;
         break;
      }
         terms4 = `${terms4.length}`;
      while (foregroundt == terms4) {
          let turnN = String.fromCharCode(109,95,57,53,95,115,109,105,109,101,0);
          let ewardede = 4.0;
          let flipper0: Map<any, any> = new Map([[String.fromCharCode(112,114,101,109,117,108,116,105,112,108,121,95,100,95,49,57,0),String.fromCharCode(122,95,54,95,101,115,116,105,109,97,116,101,100,0)], [String.fromCharCode(113,95,55,53,95,102,105,110,103,101,114,112,114,105,110,116,0),String.fromCharCode(113,95,57,48,95,99,117,114,116,97,105,110,115,0)], [String.fromCharCode(122,95,55,54,95,103,115,117,98,0),String.fromCharCode(121,95,54,51,95,100,101,103,114,101,101,115,0)]]);
          let episodew = 3.0;
         terms4 += `${1 - terms4.length}`;
         turnN = "1";
         ewardede += parseFloat(`${parseInt(`${episodew}`) ^ flipper0.size}`);
         flipper0 = new Map([[`${flipper0.size}`, flipper0.size / (Math.max(8, turnN.length))]]);
         episodew += parseFloat(`${parseInt(`${episodew}`) % 2}`);
         break;
      }
          let shrinkK = String.fromCharCode(98,95,54,48,95,115,97,108,116,115,0);
          let nativeJ: Map<any, any> = new Map([[String.fromCharCode(107,95,52,52,95,97,117,116,104,111,114,105,122,101,0),true ], [String.fromCharCode(116,95,52,95,112,115,110,114,104,118,115,0),true ]]);
          let placeholderX = 5;
         terms4 += "3";
         shrinkK += `${nativeJ.size}`;
         nativeJ.set(`${shrinkK}`, 1);
         placeholderX += 2;
      animationu = sendr.size < renewM.length;
   }
      sendr = new Map([[wcopy_rT, 1 | wcopy_rT.length]]);
   if (4 == (clearb.length / (Math.max(9, parseInt(`${statsR}`)))) || (2.66 / (Math.max(3, statsR))) == 4.61) {
       let backgroundU: Map<any, any> = new Map([[String.fromCharCode(107,95,50,51,95,109,117,116,101,120,0),String.fromCharCode(101,97,99,101,110,99,95,111,95,51,55,0)], [String.fromCharCode(119,115,97,117,100,95,120,95,51,57,0),String.fromCharCode(99,95,56,50,95,118,100,112,97,117,99,111,110,116,101,120,116,0)], [String.fromCharCode(100,95,57,95,98,105,110,104,101,120,0),String.fromCharCode(101,110,99,111,100,101,95,57,95,51,52,0)]]);
       let optionsh = String.fromCharCode(98,97,99,107,98,114,111,117,110,100,95,112,95,56,55,0);
       let libfbm = true;
       let gemfilew = String.fromCharCode(106,95,49,52,95,97,99,111,108,111,114,0);
       let chat7 = String.fromCharCode(103,117,105,100,95,51,95,54,51,0);
      let unimplementedview2 = gemfilew.length >= 5033560;
      do {
         gemfilew = `${(String.fromCharCode(51,0) == chat7 ? chat7.length : gemfilew.length)}`;
         if (unimplementedview2) {
            break;
         }
      } while ((optionsh.length >= gemfilew.length) && unimplementedview2);
         gemfilew += `${(String.fromCharCode(50,0) == gemfilew ? gemfilew.length : backgroundU.size)}`;
      while ((backgroundU.size >> (Math.min(optionsh.length, 3))) < 2 && (optionsh.length >> (Math.min(Math.abs(2), 3))) < 5) {
         optionsh = `${gemfilew.length}`;
         break;
      }
      let classesb = backgroundU.size <= 7199983;
      do {
         backgroundU = new Map([[`${backgroundU.size}`, 3]]);
         if (classesb) {
            break;
         }
      } while (classesb && ((backgroundU.size - optionsh.length) == 5 && (backgroundU.size - optionsh.length) == 5));
      let lefte = 9027509 <= chat7.length;
      do {
         chat7 = `${gemfilew.length | 3}`;
         if (lefte) {
            break;
         }
      } while (lefte && ((backgroundU.size >> (Math.min(Math.abs(5), 4))) > 2 && 3 > (5 >> (Math.min(5, Math.abs(backgroundU.size))))));
          let gesturee = 4;
          let helperQ: Array<any> = [467, 779, 978];
         libfbm = (77 > (gemfilew.length - (!libfbm ? 77 : gemfilew.length)));
         gesturee >>= Math.min(Math.abs(2), 5);
         helperQ = [gesturee | 2];
      let selectc = backgroundU.size <= 6037484;
      do {
         backgroundU.set(`${libfbm}`, backgroundU.size);
         if (selectc) {
            break;
         }
      } while ((2 >= (backgroundU.size % 1) && 1 >= (backgroundU.size % (Math.max(10, chat7.length)))) && selectc);
      if (!chat7.startsWith(`${libfbm}`)) {
          let libavfilter7: Map<any, any> = new Map([[String.fromCharCode(118,95,57,54,95,109,111,118,101,0),942], [String.fromCharCode(98,101,104,97,118,105,111,114,95,116,95,51,48,0),421], [String.fromCharCode(97,95,55,57,95,100,121,110,108,105,110,107,0),959]]);
          let sourceP = true;
         chat7 += `${optionsh.length}`;
         libavfilter7.set(`${libavfilter7.size}`, libavfilter7.size);
         sourceP = 36 < libavfilter7.size || libavfilter7.size < 36;
      }
         backgroundU.set(`${chat7}`, chat7.length * backgroundU.size);
      while (3 <= (backgroundU.size << (Math.min(Math.abs(3), 5))) && 5 <= (3 << (Math.min(5, Math.abs(backgroundU.size))))) {
         gemfilew = `${gemfilew.length}`;
         break;
      }
      let applicationg = 7008728 >= chat7.length;
      do {
          let holder6 = true;
          let activei = 0.0;
          let constantsL = 4;
          let backward1 = 2;
          let backP = false;
         chat7 = `${((libfbm ? 4 : 4))}`;
         holder6 = activei > 4.39;
         activei *= (backward1 / (Math.max(4, (backP ? 1 : 2))));
         constantsL >>= Math.min(4, Math.abs(1));
         backward1 &= backward1;
         backP = constantsL < activei;
         if (applicationg) {
            break;
         }
      } while (applicationg && (gemfilew != String.fromCharCode(51,0) && chat7 != String.fromCharCode(120,0)));
      let remindero = String.fromCharCode(103,51,54,112,118,104,53,108,120,100,0) == gemfilew;
      do {
         gemfilew = `${gemfilew.length - 2}`;
         if (remindero) {
            break;
         }
      } while (remindero && (chat7.includes(gemfilew)));
      if (chat7.startsWith(`${libfbm}`)) {
         libfbm = 91 == chat7.length;
      }
         backgroundU.set(optionsh, (optionsh.length + (libfbm ? 2 : 2)));
      while (3 <= (backgroundU.size >> (Math.min(optionsh.length, 1))) || (backgroundU.size >> (Math.min(optionsh.length, 2))) <= 3) {
          let sliderO = true;
          let indicator6 = String.fromCharCode(121,95,57,56,95,114,101,115,111,108,118,105,110,103,0);
          let libreanimatedN = String.fromCharCode(99,117,114,115,111,114,115,95,52,95,53,54,0);
         backgroundU.set(libreanimatedN, libreanimatedN.length);
         sliderO = (81 <= ((!sliderO ? 81 : indicator6.length) >> (Math.min(indicator6.length, 3))));
         break;
      }
      clearb = `${(String.fromCharCode(102,0) == clearb ? (libfbm ? 4 : 1) : clearb.length)}`;
   }
      event6 = statsR == 38.14 && String.fromCharCode(69,0) == wcopy_rT;
      event6 = 68 < classesx.length;
   while (1 < rewindy.length) {
      rewindy = `${((event6 ? 1 : 1) / (Math.max(renewM.length, 3)))}`;
      break;
   }
   for (let n = 0; n < 1; n++) {
      clearb += "2";
   }
      clearb = `${clearb.length}`;
   let commond = String.fromCharCode(97,56,106,105,0) == wcopy_rT;
   do {
      wcopy_rT += `${(rewindy == String.fromCharCode(72,0) ? (event6 ? 5 : 2) : rewindy.length)}`;
      if (commond) {
         break;
      }
   } while (commond && (!event6));
   for (let d = 0; d < 2; d++) {
      animationu = event6;
   }
       let topic5 = String.fromCharCode(116,95,56,56,95,105,110,103,114,101,115,115,0);
      let circleI = topic5.length <= 6272456;
      do {
         topic5 = "3";
         if (circleI) {
            break;
         }
      } while (circleI && (5 <= topic5.length));
          let videojsg = String.fromCharCode(105,115,110,111,110,122,101,114,111,95,103,95,53,57,0);
         topic5 = `${(String.fromCharCode(102,0) == topic5 ? topic5.length : videojsg.length)}`;
      let clock8 = topic5 == String.fromCharCode(106,121,122,48,53,0);
      do {
         topic5 = `${(String.fromCharCode(66,0) == topic5 ? topic5.length : topic5.length)}`;
         if (clock8) {
            break;
         }
      } while ((topic5.length >= topic5.length) && clock8);
      wcopy_rT = `${((event6 ? 2 : 1))}`;
   for (let l = 0; l < 3; l++) {
       let bannerM = String.fromCharCode(115,95,57,51,95,108,117,116,121,117,118,0);
       let modeo = 3.0;
       let teamw = String.fromCharCode(103,95,49,50,95,119,104,105,108,101,0);
       let sportsd = 4;
       let successL: Map<any, any> = new Map([[String.fromCharCode(112,111,108,121,109,111,100,95,111,95,52,52,0),665], [String.fromCharCode(104,95,57,49,95,118,105,100,101,111,114,101,110,100,101,114,101,114,0),997]]);
         modeo *= parseFloat(`${3 & sportsd}`);
       let libloggerW = String.fromCharCode(117,110,114,101,115,101,114,118,101,100,95,99,95,52,54,0);
         teamw = `${parseInt(`${modeo}`) / (Math.max(3, successL.size))}`;
          let mapbuffero = 5.0;
         bannerM += `${(String.fromCharCode(81,0) == libloggerW ? libloggerW.length : parseInt(`${mapbuffero}`))}`;
         bannerM += `${sportsd % 1}`;
          let libreactnativejnih: Map<any, any> = new Map([[String.fromCharCode(116,114,97,100,105,116,105,111,110,97,108,95,110,95,57,54,0),560], [String.fromCharCode(99,111,110,99,97,116,100,101,99,95,122,95,55,48,0),67], [String.fromCharCode(104,95,53,48,95,97,115,99,98,110,0),359]]);
         teamw += "2";
         libreactnativejnih.set(`${libreactnativejnih.size}`, libreactnativejnih.size);
       let attributedstring5 = String.fromCharCode(98,95,52,53,95,114,97,116,105,111,0);
         libloggerW = `${2 >> (Math.min(5, teamw.length))}`;
      let heart8 = String.fromCharCode(95,114,122,52,0) == attributedstring5;
      do {
          let loginU = true;
         attributedstring5 += `${libloggerW.length << (Math.min(4, Math.abs(parseInt(`${modeo}`))))}`;
         loginU = (!loginU ? !loginU : loginU);
         if (heart8) {
            break;
         }
      } while ((libloggerW == attributedstring5) && heart8);
      let bdxadsdk9 = 5605292 >= libloggerW.length;
      do {
         libloggerW += `${(String.fromCharCode(55,0) == bannerM ? sportsd : bannerM.length)}`;
         if (bdxadsdk9) {
            break;
         }
      } while ((teamw.length <= 4 || libloggerW == String.fromCharCode(120,0)) && bdxadsdk9);
          let libtanz = 5;
          let become8: Array<any> = [418, 873, 743];
          let hookse = String.fromCharCode(104,95,54,50,95,105,112,97,100,100,0);
         attributedstring5 = `${2 | libtanz}`;
         libtanz |= become8.length;
         become8 = [become8.length];
         hookse = `${become8.length}`;
          let bodanS = 3.0;
         libloggerW += `${parseInt(`${bodanS}`)}`;
         sportsd ^= (attributedstring5 == String.fromCharCode(54,0) ? libloggerW.length : attributedstring5.length);
          let paginationu: Array<any> = [951, 188];
          let downloadS = 2;
         sportsd |= libloggerW.length;
         paginationu = [2];
         downloadS %= Math.max(downloadS, 1);
         successL = new Map([[`${modeo}`, 1]]);
      rewindy = `${1 >> (Math.min(4, Math.abs(delegate_e8.size)))}`;
   }
       let dropdown5 = true;
       let stationsR = 3.0;
       let leaguec = String.fromCharCode(119,109,97,112,114,111,95,109,95,55,53,0);
      if (leaguec.endsWith(`${dropdown5}`)) {
         leaguec = `${parseInt(`${stationsR}`) + 1}`;
      }
          let langv = String.fromCharCode(112,117,98,101,120,112,95,53,95,49,56,0);
         stationsR += langv.length | leaguec.length;
      if ((5.88 / (Math.max(10, stationsR))) <= 4.17 || 5.88 <= stationsR) {
          let foundy = 1.0;
          let bridgeQ = String.fromCharCode(111,95,51,55,95,109,97,103,110,105,116,117,100,101,0);
         dropdown5 = bridgeQ.length >= foundy;
      }
      if ((4.62 - stationsR) > 1.77 || dropdown5) {
         stationsR += 1;
      }
         leaguec += `${1 * parseInt(`${stationsR}`)}`;
         leaguec += `${leaguec.length * 1}`;
         stationsR += ((dropdown5 ? 1 : 1) >> (Math.min(Math.abs(parseInt(`${stationsR}`)), 5)));
      let dicer = stationsR <= 9830261.0;
      do {
         stationsR -= (leaguec == String.fromCharCode(117,0) ? parseInt(`${stationsR}`) : leaguec.length);
         if (dicer) {
            break;
         }
      } while (dicer && (dropdown5));
          let shrink0 = 5.0;
         stationsR += parseInt(`${shrink0}`) + leaguec.length;
      delegate_e8.set(classesx, 3);
      wcopy_rT += `${(parseInt(`${statsR}`) * (animationu ? 3 : 3))}`;
   let memberj = String.fromCharCode(100,55,114,50,0) == classesx;
   do {
      classesx = `${1 >> (Math.min(4, wcopy_rT.length))}`;
      if (memberj) {
         break;
      }
   } while (memberj && (3 <= (classesx.length % 5) && 5 <= (parseInt(`${statsR}`) * 5)));
      sendr.set(`${clearb}`, delegate_e8.size >> (Math.min(Math.abs(3), 1)));
       let libyoga8: Array<any> = [String.fromCharCode(106,95,50,49,95,115,115,101,100,105,102,102,0), String.fromCharCode(113,95,53,57,95,115,101,108,101,99,116,105,110,103,0), String.fromCharCode(98,117,115,95,48,95,54,0)];
         libyoga8.push(1 & libyoga8.length);
      if (4 == (libyoga8.length / 4) || (libyoga8.length / (Math.max(libyoga8.length, 1))) == 4) {
         libyoga8.push(libyoga8.length - libyoga8.length);
      }
       let reducerH = 0.0;
       let backwardR = 1.0;
      renewM += `${(renewM == String.fromCharCode(57,0) ? renewM.length : (animationu ? 4 : 1))}`;
      wcopy_rT += `${rewindy.length}`;
      sendr.set(`${event6}`, 3);
   if ((4 % (Math.max(1, clearb.length))) > 3) {
      clearb += "3";
   }

    setIsDialogOpen(!isDialogOpen);
  };

  const spamToggleB = () => {
       let nativeC = 5.0;
    let layout3 = true;
    let mapbufferT = 1.0;
    let launch9: Array<any> = [709, 441, 541];
    let infow = String.fromCharCode(105,95,55,49,95,112,114,105,109,101,114,0);
    let cricketB = String.fromCharCode(97,102,102,105,110,101,95,52,95,51,51,0);
    let alertr = String.fromCharCode(102,117,122,122,101,114,95,51,95,51,51,0);
    let c_titlej = String.fromCharCode(104,95,50,49,95,119,105,115,101,0);
    let libreactnativeblob0 = 4.0;
    let playlistK = String.fromCharCode(111,95,57,52,95,115,111,117,110,100,101,120,0);
    let vignette1: Array<any> = [944, 275, 465];
      vignette1.push(vignette1.length);
   let patht = infow == String.fromCharCode(122,121,53,108,111,54,95,113,0);
   do {
       let photoi = 0.0;
       let mbsplashe = String.fromCharCode(103,95,54,57,95,98,98,111,120,0);
       let security6 = 4;
       let hejiL = 0.0;
       let hejim: Array<any> = [600, 119];
      for (let k = 0; k < 1; k++) {
          let sinaK = 4.0;
          let sharedU: Array<any> = [970, 251];
          let auto_hiF: Map<any, any> = new Map([[String.fromCharCode(116,95,55,95,100,105,115,116,114,105,99,116,0),691], [String.fromCharCode(104,95,51,56,95,101,120,112,111,114,116,0),733]]);
          let runtimeschedulerK: Array<any> = [320, 999, 873];
          let layout4 = 1.0;
         hejim = [parseInt(`${sinaK}`) ^ hejim.length];
         sinaK += runtimeschedulerK.length + 3;
         sharedU.push(2 / (Math.max(1, runtimeschedulerK.length)));
         auto_hiF = new Map([[`${runtimeschedulerK.length}`, runtimeschedulerK.length * 1]]);
         layout4 /= Math.max(2, parseFloat(`${3}`));
      }
         security6 /= Math.max(5, 2);
      while (2 >= (security6 % (Math.max(mbsplashe.length, 5))) || 3 >= (mbsplashe.length % 2)) {
         security6 *= 3;
         break;
      }
      while (2 <= (security6 << (Math.min(Math.abs(3), 1))) || 1 <= (mbsplashe.length << (Math.min(Math.abs(3), 1)))) {
         security6 -= hejim.length;
         break;
      }
          let speco: Array<any> = [389, 76, 513];
          let referrerQ = 5;
          let matchesN = 0;
         photoi *= hejim.length;
         speco = [matchesN % 3];
         referrerQ *= referrerQ << (Math.min(Math.abs(matchesN), 1));
         security6 *= parseInt(`${hejiL}`);
          let stepU = false;
          let eactQ = String.fromCharCode(98,105,116,108,105,110,101,99,104,117,110,107,121,95,54,95,55,57,0);
         hejim = [(parseInt(`${photoi}`) | (stepU ? 5 : 3))];
         stepU = (eactQ.length ^ eactQ.length) >= 100;
       let modityO = 0.0;
          let rewindt = 1;
          let minivodR = String.fromCharCode(116,95,57,53,95,115,101,101,100,101,100,0);
         modityO /= Math.max(parseFloat(`${parseInt(`${hejiL}`)}`), 5);
         rewindt <<= Math.min(Math.abs(1 & minivodR.length), 1);
         minivodR = `${rewindt - 3}`;
      let holdery = mbsplashe.length >= 5577357;
      do {
         mbsplashe = "2";
         if (holdery) {
            break;
         }
      } while ((mbsplashe.length <= parseInt(`${photoi}`)) && holdery);
          let upgradee = String.fromCharCode(109,102,104,100,95,108,95,57,49,0);
         security6 %= Math.max(3, 3);
         upgradee = `${upgradee.length / 3}`;
      let unselectedR = hejim.length >= 8529587;
      do {
         hejim.push(2);
         if (unselectedR) {
            break;
         }
      } while (((hejim.length / 1) <= 5 || (hejim.length * hejiL) <= 5.70) && unselectedR);
         security6 ^= security6 % 3;
          let containerR: Array<any> = [349, 96, 760];
          let plashT = false;
         hejim = [(mbsplashe == String.fromCharCode(87,0) ? mbsplashe.length : parseInt(`${photoi}`))];
         containerR.push(containerR.length ^ 2);
         plashT = ((containerR.length * (!plashT ? containerR.length : 73)) >= 73);
         security6 >>= Math.min(4, Math.abs((mbsplashe == String.fromCharCode(77,0) ? parseInt(`${photoi}`) : mbsplashe.length)));
      infow += `${security6 * 1}`;
      if (patht) {
         break;
      }
   } while ((5 > infow.length) && patht);
      layout3 = c_titlej.length >= alertr.length;
   while (alertr == cricketB) {
      cricketB = `${(parseInt(`${libreactnativeblob0}`) & (layout3 ? 5 : 4))}`;
      break;
   }
       let yellowR = String.fromCharCode(97,95,55,53,95,111,108,111,114,0);
       let viewsO = String.fromCharCode(115,116,114,101,115,115,95,115,95,49,51,0);
       let sendF: Map<any, any> = new Map([[String.fromCharCode(97,100,97,112,116,97,116,105,111,110,115,101,116,95,97,95,54,53,0),String.fromCharCode(100,101,109,111,116,101,95,100,95,57,52,0)], [String.fromCharCode(99,95,50,54,95,114,101,110,116,97,108,0),String.fromCharCode(103,95,50,49,95,104,101,120,116,105,108,101,0)], [String.fromCharCode(109,95,55,55,95,116,97,107,101,111,117,116,0),String.fromCharCode(114,101,115,111,108,117,116,105,111,110,115,95,55,95,51,48,0)]]);
       let greeny: Array<any> = [String.fromCharCode(105,109,112,108,101,109,101,110,116,115,95,105,95,52,57,0), String.fromCharCode(105,112,118,95,115,95,51,56,0)];
         yellowR += `${greeny.length}`;
         yellowR = `${1 & viewsO.length}`;
         viewsO += `${2 & sendF.size}`;
      for (let m = 0; m < 1; m++) {
         viewsO += `${greeny.length << (Math.min(viewsO.length, 4))}`;
      }
      if (1 <= (greeny.length & yellowR.length) || 2 <= (yellowR.length & 1)) {
         yellowR += `${yellowR.length}`;
      }
         viewsO = `${greeny.length}`;
         viewsO = `${viewsO.length / (Math.max(1, yellowR.length))}`;
         viewsO += `${viewsO.length >> (Math.min(Math.abs(2), 4))}`;
      layout3 = vignette1.length < 56 || mapbufferT < 65.41;
   while (playlistK.endsWith(`${vignette1.length}`)) {
      playlistK += "3";
      break;
   }
   while (launch9.length >= infow.length) {
       let zoomC = 3;
      while (3 == (zoomC - zoomC) || (zoomC - 3) == 5) {
         zoomC /= Math.max(5, zoomC);
         break;
      }
      if (4 == (zoomC ^ 3)) {
          let iconQ: Array<any> = [String.fromCharCode(122,95,50,55,95,103,112,111,115,116,102,105,108,116,101,114,0), String.fromCharCode(113,95,57,95,115,97,109,112,108,101,114,0)];
          let u_playerw = String.fromCharCode(98,95,53,50,95,114,101,99,101,110,116,0);
         zoomC >>= Math.min(5, Math.abs(zoomC * 3));
         iconQ = [u_playerw.length / 3];
         u_playerw += `${iconQ.length}`;
      }
      while (5 == zoomC) {
         zoomC ^= 1;
         break;
      }
      infow = `${parseInt(`${mapbufferT}`)}`;
      break;
   }
      c_titlej = `${launch9.length}`;
      playlistK += `${parseInt(`${nativeC}`)}`;
       let v_players = String.fromCharCode(105,110,100,101,111,95,120,95,49,54,0);
       let expiredJ = String.fromCharCode(100,95,54,55,0);
       let termsC = 5.0;
         expiredJ = `${parseInt(`${termsC}`) % 1}`;
       let lessy = true;
       let sourceO = false;
       let membershiph: Array<any> = [232, 720, 482];
      let i_lockF = 5651218 <= expiredJ.length;
      do {
         expiredJ += `${(expiredJ == String.fromCharCode(102,0) ? expiredJ.length : membershiph.length)}`;
         if (i_lockF) {
            break;
         }
      } while (((parseInt(`${termsC}`) * expiredJ.length) < 4 && (parseInt(`${termsC}`) * expiredJ.length) < 4) && i_lockF);
      if ((membershiph.length / 5) < 4) {
          let gdtadvY = 1.0;
          let binddatasR = String.fromCharCode(104,95,52,51,95,97,118,99,105,0);
          let shrinkZ = String.fromCharCode(112,101,101,114,110,97,109,101,95,57,95,50,57,0);
          let graphF = String.fromCharCode(100,102,115,116,95,112,95,49,48,0);
         membershiph = [((lessy ? 5 : 2) * parseInt(`${gdtadvY}`))];
         gdtadvY *= (parseFloat(`${String.fromCharCode(89,0) == graphF ? binddatasR.length : graphF.length}`));
         binddatasR = `${binddatasR.length >> (Math.min(Math.abs(1), 1))}`;
         shrinkZ += `${shrinkZ.length}`;
      }
       let sansW = String.fromCharCode(101,95,54,56,95,114,101,103,105,115,116,101,114,101,114,0);
       let libavformat4 = String.fromCharCode(99,95,57,48,95,97,118,117,116,105,108,114,101,115,0);
      let backe = lessy ? !lessy : lessy;
      do {
         lessy = ((expiredJ.length & (sourceO ? expiredJ.length : 71)) >= 71);
         if (backe) {
            break;
         }
      } while (backe && (!sansW.endsWith(`${lessy}`)));
         expiredJ = `${v_players.length | parseInt(`${termsC}`)}`;
      for (let w = 0; w < 2; w++) {
         libavformat4 += `${(libavformat4 == String.fromCharCode(117,0) ? libavformat4.length : (sourceO ? 2 : 2))}`;
      }
      mapbufferT *= parseInt(`${termsC}`);
      launch9.push(vignette1.length);
   while (3 >= (3 * vignette1.length) && 3 >= (alertr.length * vignette1.length)) {
      alertr += `${infow.length}`;
      break;
   }
      c_titlej += `${(2 % (Math.max(7, (layout3 ? 3 : 5))))}`;
      layout3 = playlistK.length <= mapbufferT;
      libreactnativeblob0 *= (parseFloat(`${infow == String.fromCharCode(111,0) ? vignette1.length : infow.length}`));
   let phonew = String.fromCharCode(54,117,111,57,48,109,107,49,103,97,0) == alertr;
   do {
       let bootsplashy = String.fromCharCode(114,95,56,56,95,109,117,109,98,97,105,0);
       let selecteds: Array<any> = [94, 931];
       let showS = 4;
       let detailsN: Array<any> = [String.fromCharCode(104,95,57,51,95,97,110,116,105,97,108,105,97,115,105,110,103,0), String.fromCharCode(102,95,55,50,95,97,100,112,99,109,0)];
       let playlistf = false;
         playlistf = showS < bootsplashy.length;
      let nalyticse = 9858192 <= showS;
      do {
          let sharedJ = 2.0;
          let predictionU: Array<any> = [600, 569];
          let searchbard = 5.0;
          let clearH = 2.0;
          let home8 = 5.0;
         showS <<= Math.min(3, Math.abs(2));
         sharedJ *= parseFloat(`${1 * parseInt(`${searchbard}`)}`);
         predictionU.push(1 + parseInt(`${sharedJ}`));
         searchbard *= parseFloat(`${parseInt(`${sharedJ}`)}`);
         clearH /= Math.max(parseFloat(`${parseInt(`${sharedJ}`)}`), 3);
         home8 -= parseInt(`${sharedJ}`) % 3;
         if (nalyticse) {
            break;
         }
      } while (((showS % 1) < 3) && nalyticse);
      while (playlistf) {
          let chartK = String.fromCharCode(116,95,50,55,95,112,97,114,0);
          let thumbnailR = false;
         detailsN = [(String.fromCharCode(49,0) == chartK ? chartK.length : (playlistf ? 4 : 5))];
         thumbnailR = !thumbnailR;
         break;
      }
       let nextj = String.fromCharCode(97,95,54,50,95,115,112,101,101,100,111,109,101,116,101,114,0);
       let classes_ = String.fromCharCode(119,95,53,49,95,100,101,102,105,110,101,100,0);
       let expireda = 4;
      if (detailsN.includes(showS)) {
         detailsN.push(((playlistf ? 4 : 3) * 3));
      }
         bootsplashy = `${nextj.length}`;
         detailsN.push(expireda);
      if ((detailsN.length + selecteds.length) < 5) {
          let controlsK = 0.0;
          let emojik: Map<any, any> = new Map([[String.fromCharCode(106,95,51,49,95,105,110,99,108,117,115,105,111,110,0),158], [String.fromCharCode(116,117,114,98,111,106,112,101,103,95,54,95,53,51,0),662], [String.fromCharCode(114,116,115,112,95,109,95,52,0),414]]);
          let sharedA = 0.0;
          let stringB = true;
         detailsN.push(showS / (Math.max(1, nextj.length)));
         controlsK *= parseFloat(`${1 | parseInt(`${sharedA}`)}`);
         emojik.set(`${sharedA}`, parseInt(`${controlsK}`));
         stringB = 34.52 >= sharedA;
      }
          let rulesG: Array<any> = [894, 25, 467];
          let minivodP = String.fromCharCode(104,95,54,50,95,100,97,112,112,0);
         selecteds = [minivodP.length / (Math.max(9, nextj.length))];
         rulesG.push(rulesG.length);
         minivodP = `${rulesG.length}`;
      while (bootsplashy.length == 1) {
         showS >>= Math.min(2, Math.abs(3 ^ selecteds.length));
         break;
      }
         bootsplashy = "2";
         nextj = `${showS}`;
         expireda ^= expireda;
      while (classes_.startsWith(`${expireda}`)) {
         classes_ += `${((playlistf ? 1 : 5))}`;
         break;
      }
      alertr += "1";
      if (phonew) {
         break;
      }
   } while (phonew && (!layout3));
      vignette1 = [1];
   let themeI = mapbufferT >= 6569481.0;
   do {
      mapbufferT *= launch9.length & 2;
      if (themeI) {
         break;
      }
   } while (themeI && (4.74 == (4.56 * mapbufferT) && 4.56 == mapbufferT));
      infow = `${launch9.length ^ alertr.length}`;
   for (let p = 0; p < 3; p++) {
      mapbufferT /= Math.max(5, playlistK.length);
   }
   for (let c = 0; c < 2; c++) {
       let anner5 = String.fromCharCode(101,95,57,55,95,111,108,97,110,97,0);
         anner5 += `${anner5.length}`;
      if (anner5.length > 4) {
         anner5 = `${anner5.length}`;
      }
      let animationst = anner5.length >= 9604326;
      do {
         anner5 = `${anner5.length >> (Math.min(Math.abs(2), 2))}`;
         if (animationst) {
            break;
         }
      } while (animationst && (anner5.length == 2 || anner5.length == 2));
      nativeC /= Math.max(parseFloat(`${parseInt(`${libreactnativeblob0}`)}`), 2);
   }
   while (!playlistK.includes(infow)) {
      infow = `${parseInt(`${nativeC}`)}`;
      break;
   }
   for (let d = 0; d < 2; d++) {
      launch9 = [c_titlej.length];
   }
   for (let a = 0; a < 1; a++) {
      infow = `${playlistK.length >> (Math.min(2, Math.abs(parseInt(`${libreactnativeblob0}`))))}`;
   }
      playlistK += `${parseInt(`${nativeC}`) % (Math.max(launch9.length, 6))}`;

    setCountToggleB(countToggleB + 1);
  }

  const switchToggle = async () => {
       let button9 = 4;
    let pangleK: Array<any> = [227, 107, 46];
    let typingv = 3.0;
    let benefitX = String.fromCharCode(110,111,110,110,117,108,108,115,115,114,99,95,103,95,55,53,0);
    let launchS = String.fromCharCode(105,110,99,114,101,109,101,110,116,115,95,112,95,50,57,0);
    let debugm = 5.0;
    let recommendation_ = 4.0;
    let umengJ = String.fromCharCode(98,95,51,57,95,109,105,99,114,111,115,111,102,116,0);
    let libavdevice3 = String.fromCharCode(98,95,52,52,95,99,97,99,104,101,115,0);
    let v_lockW = String.fromCharCode(104,95,55,95,100,101,99,111,100,101,100,0);
    let gradleF = 4.0;
    let tickedO = 0.0;
   for (let x = 0; x < 2; x++) {
      button9 -= 3;
   }
   while (v_lockW.startsWith(`${typingv}`)) {
       let halfM = false;
       let dataj = 5.0;
         halfM = !halfM;
      for (let b = 0; b < 3; b++) {
         dataj += parseFloat(`${1 % (Math.max(1, parseInt(`${dataj}`)))}`);
      }
       let z_positionk = 5.0;
      if (3.81 > z_positionk) {
          let canvasJ: Array<any> = [98, 899];
          let libjsinspectorz = true;
          let streaming8 = true;
          let stationg: Array<any> = [54, 242];
         halfM = streaming8;
         canvasJ.push(stationg.length);
         libjsinspectorz = (96 < (canvasJ.length + (!libjsinspectorz ? 96 : canvasJ.length)));
         stationg.push((canvasJ.length & (libjsinspectorz ? 2 : 1)));
      }
         halfM = !halfM;
      for (let s = 0; s < 1; s++) {
          let holderH = String.fromCharCode(112,114,117,110,105,110,103,95,105,95,53,49,0);
          let promotiona = false;
          let reducer7 = String.fromCharCode(107,95,50,55,95,100,111,119,110,115,99,97,108,101,0);
          let acceptedH = 3.0;
          let streaming8i = String.fromCharCode(115,105,103,115,97,102,101,95,104,95,57,51,0);
         dataj += (parseFloat(`${String.fromCharCode(70,0) == streaming8i ? streaming8i.length : parseInt(`${acceptedH}`)}`));
         holderH = `${(reducer7 == String.fromCharCode(104,0) ? (promotiona ? 5 : 3) : reducer7.length)}`;
         promotiona = !promotiona;
         acceptedH /= Math.max((parseFloat(`${(promotiona ? 1 : 1)}`)), 5);
      }
      typingv /= Math.max(3, pangleK.length | parseInt(`${debugm}`));
      break;
   }
   while (benefitX != umengJ) {
       let termsM = String.fromCharCode(112,114,101,102,105,120,101,100,95,55,95,56,56,0);
         termsM += `${termsM.length}`;
      let bootsplashx = String.fromCharCode(49,118,108,122,98,0) == termsM;
      do {
         termsM += `${termsM.length >> (Math.min(Math.abs(1), 3))}`;
         if (bootsplashx) {
            break;
         }
      } while ((termsM.length > termsM.length) && bootsplashx);
      let bootsplash4 = termsM == String.fromCharCode(122,51,50,110,100,56,104,114,97,0);
      do {
          let ewardedc = String.fromCharCode(97,95,49,55,95,115,97,100,98,0);
          let libfbX = String.fromCharCode(112,97,114,97,109,103,101,110,95,118,95,57,0);
          let favorite1 = 5;
          let middleI = String.fromCharCode(110,111,110,98,95,50,95,56,57,0);
          let runtimescheduler6 = String.fromCharCode(111,118,101,114,97,108,108,95,102,95,55,49,0);
         termsM += `${libfbX.length}`;
         ewardedc += `${ewardedc.length % 1}`;
         libfbX = `${(ewardedc == String.fromCharCode(67,0) ? favorite1 : ewardedc.length)}`;
         favorite1 &= 1;
         middleI = "2";
         runtimescheduler6 += `${middleI.length}`;
         if (bootsplash4) {
            break;
         }
      } while (bootsplash4 && (5 > termsM.length || termsM.length > 5));
      umengJ = `${v_lockW.length & 3}`;
      break;
   }
   if ((4.38 * recommendation_) == 3.95) {
       let showJ: Array<any> = [878, 857, 836];
       let renderf = 0;
       let libglogB = String.fromCharCode(115,108,117,114,112,95,104,95,50,54,0);
          let photo7: Map<any, any> = new Map([[String.fromCharCode(109,101,97,115,117,114,101,109,101,110,116,95,100,95,49,48,48,0),String.fromCharCode(118,95,49,50,95,98,97,115,101,110,97,109,101,0)], [String.fromCharCode(116,95,56,95,112,99,97,112,0),String.fromCharCode(114,101,102,101,114,101,110,99,101,115,95,119,95,55,0)], [String.fromCharCode(114,100,109,117,108,116,95,98,95,57,50,0),String.fromCharCode(119,95,53,49,95,102,114,105,99,116,105,111,110,0)]]);
         libglogB += `${photo7.size << (Math.min(5, Math.abs(renderf)))}`;
       let gmailS = false;
      let core4 = 9256677 <= renderf;
      do {
         renderf += renderf;
         if (core4) {
            break;
         }
      } while ((3 > (renderf % 3)) && core4);
      while (2 < (showJ.length & 3) || 3 < showJ.length) {
         showJ = [renderf | 3];
         break;
      }
      if (renderf < showJ.length) {
          let ajax3: Array<any> = [363, 600];
          let username9 = 1;
          let regengt = 1.0;
         renderf >>= Math.min(Math.abs(ajax3.length / 3), 5);
         ajax3.push(2 - username9);
         username9 <<= Math.min(5, Math.abs(parseInt(`${regengt}`)));
         regengt -= parseFloat(`${parseInt(`${regengt}`) << (Math.min(1, Math.abs(username9)))}`);
      }
          let signinupt = 4.0;
          let event7 = 0;
          let gradlewJ = String.fromCharCode(110,95,52,51,95,102,102,112,108,97,121,0);
         gmailS = (showJ.length << (Math.min(3, Math.abs(renderf)))) >= 57;
         signinupt -= event7;
         event7 %= Math.max(5, gradlewJ.length ^ parseInt(`${signinupt}`));
         gradlewJ += `${1 >> (Math.min(Math.abs(parseInt(`${signinupt}`)), 4))}`;
         showJ = [((gmailS ? 3 : 3) >> (Math.min(showJ.length, 5)))];
          let libyogad = String.fromCharCode(99,111,109,98,105,110,97,116,105,111,110,115,95,103,95,52,48,0);
         renderf &= libglogB.length;
         libyogad += "3";
         showJ = [2];
      pangleK = [1];
   }
       let expired0 = String.fromCharCode(118,95,55,56,95,118,98,101,122,105,101,114,0);
       let with_0bn = String.fromCharCode(104,121,112,104,101,110,97,116,101,100,95,121,95,49,56,0);
          let libavdevice6 = String.fromCharCode(103,95,53,55,95,116,101,115,115,101,108,97,116,111,114,0);
          let linkf: Array<any> = [20, 222, 112];
          let fileq = 2;
         expired0 = "3";
         libavdevice6 = `${1 << (Math.min(3, linkf.length))}`;
         linkf = [linkf.length];
         fileq <<= Math.min(5, Math.abs(linkf.length | 3));
      let x_lockX = 6129676 >= with_0bn.length;
      do {
          let stats3: Map<any, any> = new Map([[String.fromCharCode(115,95,51,56,95,105,110,105,116,104,0),750], [String.fromCharCode(111,95,50,49,95,101,120,116,115,107,0),176]]);
          let vignetteG = 1;
          let foundz = String.fromCharCode(109,109,99,111,115,95,49,95,55,53,0);
         with_0bn += "1";
         stats3.set(`${vignetteG}`, vignetteG);
         foundz = `${foundz.length * 2}`;
         if (x_lockX) {
            break;
         }
      } while (x_lockX && (with_0bn.endsWith(expired0)));
      while (3 < expired0.length) {
         expired0 += `${(expired0 == String.fromCharCode(57,0) ? expired0.length : with_0bn.length)}`;
         break;
      }
      while (expired0 == with_0bn) {
         with_0bn += `${with_0bn.length << (Math.min(Math.abs(2), 5))}`;
         break;
      }
         expired0 = `${with_0bn.length - expired0.length}`;
         expired0 = `${expired0.length}`;
      button9 &= 2;
      recommendation_ -= parseFloat(`${3}`);
      libavdevice3 += `${parseInt(`${debugm}`) << (Math.min(pangleK.length, 1))}`;
       let dplusq: Array<any> = [823, 591];
       let activity9 = String.fromCharCode(115,95,57,51,95,98,101,114,110,111,117,108,108,105,0);
       let utilsg = 5;
          let starz = String.fromCharCode(101,110,99,104,95,116,95,52,54,0);
          let vignetteZ = String.fromCharCode(99,117,116,95,119,95,55,55,0);
          let hooksx = String.fromCharCode(109,95,55,49,95,102,97,105,108,117,114,101,115,0);
         dplusq = [dplusq.length];
         starz = `${vignetteZ.length << (Math.min(starz.length, 1))}`;
         vignetteZ += `${vignetteZ.length}`;
         hooksx = `${vignetteZ.length}`;
      let thailandC = 5832101 >= dplusq.length;
      do {
          let anytime2: Map<any, any> = new Map([[String.fromCharCode(99,111,109,109,117,116,101,95,113,95,54,53,0),String.fromCharCode(116,95,57,54,95,109,101,97,110,105,110,103,102,117,108,0)], [String.fromCharCode(101,95,51,52,95,101,109,98,101,100,100,101,100,0),String.fromCharCode(115,105,109,117,108,97,116,101,95,116,95,54,50,0)], [String.fromCharCode(112,117,108,108,100,111,119,110,95,122,95,57,55,0),String.fromCharCode(99,97,110,100,105,100,97,116,101,95,105,95,53,56,0)]]);
          let layoutt = 4.0;
          let qnewsC = 3;
         dplusq = [anytime2.size | 3];
         anytime2 = new Map([[`${layoutt}`, qnewsC | 3]]);
         layoutt *= qnewsC;
         if (thailandC) {
            break;
         }
      } while ((5 < dplusq.length) && thailandC);
          let liveu = String.fromCharCode(107,95,57,55,95,115,101,108,101,99,116,105,111,110,115,0);
          let libavfiltero = String.fromCharCode(102,108,101,120,105,98,108,101,95,107,95,52,51,0);
          let faviconn = 5;
         dplusq = [1 + activity9.length];
         liveu = `${faviconn - 2}`;
         libavfiltero += `${libavfiltero.length}`;
         faviconn -= (String.fromCharCode(79,0) == libavfiltero ? libavfiltero.length : faviconn);
      for (let a = 0; a < 2; a++) {
          let darkg = String.fromCharCode(121,95,50,54,95,111,118,97,108,0);
         dplusq.push(dplusq.length - 3);
         darkg += "2";
      }
      if (activity9.length == 1) {
          let rulesd: Array<any> = [462, 858];
          let componentregistry_ = String.fromCharCode(108,101,118,101,108,95,98,95,51,0);
          let reminderN = String.fromCharCode(120,95,51,54,95,100,101,99,111,114,97,116,105,111,110,115,0);
         activity9 += `${componentregistry_.length}`;
         rulesd = [3];
         componentregistry_ += `${rulesd.length * reminderN.length}`;
         reminderN = `${1 - reminderN.length}`;
      }
      if (5 < (4 + activity9.length)) {
         dplusq = [(String.fromCharCode(115,0) == activity9 ? activity9.length : dplusq.length)];
      }
         dplusq.push(utilsg);
      while ((5 + dplusq.length) > 3) {
          let condensedn = String.fromCharCode(102,101,97,116,117,114,101,100,95,50,95,49,48,0);
          let episode6 = String.fromCharCode(97,118,112,105,99,116,117,114,101,95,49,95,50,51,0);
          let libfbjniZ = String.fromCharCode(99,108,117,115,116,101,114,95,117,95,54,50,0);
         dplusq.push(1);
         condensedn = "2";
         episode6 = `${(String.fromCharCode(81,0) == condensedn ? condensedn.length : episode6.length)}`;
         libfbjniZ = `${libfbjniZ.length % 2}`;
         break;
      }
          let agreementC = String.fromCharCode(102,95,49,49,95,100,115,109,111,116,105,111,110,0);
          let modelsb: Array<any> = [997, 589];
          let modityb = 3.0;
         activity9 += `${activity9.length - parseInt(`${modityb}`)}`;
         agreementC = `${2 - modelsb.length}`;
         modelsb = [3];
         modityb -= parseFloat(`${agreementC.length}`);
      v_lockW += `${3 * libavdevice3.length}`;
   if ((parseFloat(`${umengJ.length}`) - recommendation_) >= 3.50) {
      recommendation_ *= parseFloat(`${libavdevice3.length}`);
   }
   for (let r = 0; r < 3; r++) {
      pangleK = [pangleK.length | benefitX.length];
   }
   let downloadingo = benefitX.length >= 5988343;
   do {
      benefitX += `${parseInt(`${debugm}`) + 2}`;
      if (downloadingo) {
         break;
      }
   } while ((2 > benefitX.length) && downloadingo);
   while ((umengJ.length + 3) >= 5 || 4 >= (umengJ.length % 3)) {
       let frame_du = String.fromCharCode(118,97,105,108,97,98,105,108,105,116,121,95,97,95,52,55,0);
       let xadsdkW = 5.0;
       let stepY = 2.0;
         stepY -= parseFloat(`${1}`);
      let singaporew = xadsdkW <= 5603212.0;
      do {
          let moviesg = String.fromCharCode(101,110,104,97,110,99,101,109,101,110,116,115,95,118,95,51,57,0);
          let sharedK = String.fromCharCode(117,95,53,52,95,112,108,97,121,108,105,115,116,0);
          let leakcheckerG = String.fromCharCode(97,118,105,103,97,116,105,111,110,95,56,95,48,0);
          let i_centerR = 2.0;
          let prediction5 = false;
         xadsdkW -= (parseFloat(`${(prediction5 ? 5 : 4) / (Math.max(parseInt(`${i_centerR}`), 7))}`));
         moviesg = `${(String.fromCharCode(114,0) == sharedK ? sharedK.length : moviesg.length)}`;
         leakcheckerG += `${sharedK.length >> (Math.min(Math.abs(3), 3))}`;
         i_centerR -= parseFloat(`${leakcheckerG.length ^ sharedK.length}`);
         prediction5 = 24 <= sharedK.length && leakcheckerG.length <= 24;
         if (singaporew) {
            break;
         }
      } while ((3.48 > (xadsdkW - 5.63) && (5.63 * stepY) > 3.15) && singaporew);
      umengJ += `${parseInt(`${debugm}`) - 2}`;
      frame_du = `${frame_du.length}`;
      break;
   }
       let reminderP: Map<any, any> = new Map([[String.fromCharCode(115,101,116,98,105,116,115,95,48,95,49,52,0),false ], [String.fromCharCode(104,95,55,53,95,116,105,112,0),false ], [String.fromCharCode(105,110,115,116,95,57,95,53,52,0),false ]]);
       let halfy = String.fromCharCode(97,110,97,108,121,115,101,95,101,95,52,52,0);
       let mbbanner5 = 1.0;
          let moreV = 2.0;
          let zhengpian7: Array<any> = [693, 489];
         halfy += `${(halfy == String.fromCharCode(83,0) ? reminderP.size : halfy.length)}`;
         moreV *= parseFloat(`${parseInt(`${moreV}`)}`);
         zhengpian7 = [1 + zhengpian7.length];
      while (4 >= (parseInt(`${mbbanner5}`) - reminderP.size)) {
         reminderP.set(`${halfy}`, (String.fromCharCode(109,0) == halfy ? halfy.length : reminderP.size));
         break;
      }
      if (4.12 <= (5.57 * mbbanner5)) {
         mbbanner5 -= parseFloat(`${2}`);
      }
         reminderP = new Map([[`${reminderP.size}`, 2]]);
      let rightT = reminderP.size <= 8546223;
      do {
         reminderP = new Map([[`${reminderP.size}`, halfy.length | 3]]);
         if (rightT) {
            break;
         }
      } while (rightT && (halfy.length < reminderP.size));
       let gradlew5 = String.fromCharCode(116,95,51,51,95,100,117,114,97,116,105,111,110,115,0);
      while (gradlew5.length <= 5) {
          let fadfdeebbbfdabbbabdadfaaddaar: Array<any> = [740, 850, 808];
          let debugG: Array<any> = [392, 231];
          let optionsq = String.fromCharCode(116,102,114,97,95,109,95,54,49,0);
          let videoK = String.fromCharCode(118,101,99,116,111,114,115,95,120,95,56,48,0);
          let hookso: Array<any> = [String.fromCharCode(102,97,115,116,101,115,116,95,121,95,49,48,48,0), String.fromCharCode(118,95,54,50,95,112,114,101,115,101,116,0), String.fromCharCode(105,110,115,116,97,110,116,95,112,95,54,48,0)];
         halfy += `${parseInt(`${mbbanner5}`) & fadfdeebbbfdabbbabdadfaaddaar.length}`;
         fadfdeebbbfdabbbabdadfaaddaar.push(hookso.length);
         debugG = [videoK.length | hookso.length];
         optionsq = `${hookso.length * optionsq.length}`;
         videoK = `${hookso.length / (Math.max(5, videoK.length))}`;
         break;
      }
         mbbanner5 /= Math.max(parseFloat(`${parseInt(`${mbbanner5}`)}`), 3);
          let moviesgz = 0.0;
          let fullx = 3.0;
          let entry_ = 4.0;
         reminderP.set(`${halfy}`, reminderP.size);
         moviesgz /= Math.max(5, parseInt(`${fullx}`) + 1);
         fullx -= 2;
         entry_ -= parseInt(`${entry_}`) - 3;
      gradleF += parseFloat(`${1 >> (Math.min(5, Math.abs(parseInt(`${debugm}`))))}`);
      v_lockW += `${parseInt(`${gradleF}`) / 2}`;
   let chartj = 8450194.0 <= debugm;
   do {
       let minivodN = String.fromCharCode(122,95,56,48,95,100,114,111,112,0);
       let gpayS = String.fromCharCode(115,95,53,50,95,100,101,99,114,101,109,101,110,116,0);
       let const_j7 = 5.0;
          let moonq = String.fromCharCode(107,95,52,57,95,111,116,104,101,114,115,0);
          let contextL = 3.0;
         const_j7 += 2 | moonq.length;
         moonq = `${parseInt(`${contextL}`)}`;
      while ((const_j7 / 2.64) <= 3.52 && 5 <= (gpayS.length / (Math.max(4, parseInt(`${const_j7}`))))) {
         gpayS += `${gpayS.length / (Math.max(3, 4))}`;
         break;
      }
         const_j7 += parseInt(`${const_j7}`) | 2;
          let unimplementedview9 = 4;
          let libavformatJ = 4.0;
          let stylesT = 2.0;
         minivodN = `${unimplementedview9}`;
         unimplementedview9 -= parseInt(`${stylesT}`);
         libavformatJ += parseInt(`${stylesT}`);
      for (let b = 0; b < 2; b++) {
          let optionsn: Map<any, any> = new Map([[String.fromCharCode(107,95,51,48,95,108,101,98,110,0),false ], [String.fromCharCode(109,95,49,57,95,99,105,110,101,112,97,107,0),true ]]);
          let mbjscommonW: Map<any, any> = new Map([[String.fromCharCode(105,110,116,101,103,101,114,95,102,95,56,50,0),String.fromCharCode(104,95,57,95,100,101,112,114,101,99,97,116,101,100,0)], [String.fromCharCode(105,110,116,101,114,109,101,100,105,97,116,101,95,56,95,55,51,0),String.fromCharCode(113,95,52,51,95,105,109,97,103,101,121,117,118,99,111,110,102,105,103,0)], [String.fromCharCode(116,95,51,54,95,99,108,111,115,101,115,0),String.fromCharCode(97,110,103,117,108,97,114,95,100,95,56,53,0)]]);
          let success_ = 4.0;
          let clock4 = 4.0;
          let scrollviewG: Map<any, any> = new Map([[String.fromCharCode(102,95,55,52,95,109,111,110,111,116,111,110,105,116,121,0),899], [String.fromCharCode(106,95,57,95,99,111,109,109,117,110,105,99,97,116,111,114,0),334], [String.fromCharCode(98,95,53,50,95,112,111,115,105,116,105,111,110,115,0),275]]);
         gpayS = `${parseInt(`${clock4}`) << (Math.min(Math.abs(3), 5))}`;
         optionsn = new Map([[`${scrollviewG.size}`, optionsn.size - scrollviewG.size]]);
         mbjscommonW.set(`${scrollviewG.size}`, scrollviewG.size >> (Math.min(Math.abs(3), 4)));
         success_ /= Math.max(4, scrollviewG.size << (Math.min(Math.abs(optionsn.size), 5)));
         clock4 /= Math.max(parseFloat(`${parseInt(`${success_}`) - 2}`), 4);
      }
      while (3 == (5 & gpayS.length) || 1.89 == (4.57 - const_j7)) {
         const_j7 -= gpayS.length * 3;
         break;
      }
      if (minivodN.length > gpayS.length) {
         gpayS = `${1 & minivodN.length}`;
      }
      if (minivodN == gpayS) {
         gpayS = `${1 ^ minivodN.length}`;
      }
          let termsQ = 0.0;
         gpayS += "1";
         termsQ += parseInt(`${termsQ}`) * 1;
      debugm -= parseFloat(`${parseInt(`${gradleF}`) ^ 1}`);
      if (chartj) {
         break;
      }
   } while ((4.59 <= (2.61 / (Math.max(10, debugm))) && (umengJ.length / (Math.max(4, parseInt(`${debugm}`)))) <= 5) && chartj);
      v_lockW += `${launchS.length - parseInt(`${typingv}`)}`;

    await AsyncStorage.setItem("access", "11111111");

      typingv += 1 + umengJ.length;
       let xvod2 = String.fromCharCode(109,97,107,101,102,105,108,101,95,112,95,52,49,0);
       let streaming6 = true;
       let armvao = true;
      while (streaming6) {
         xvod2 += `${((armvao ? 5 : 1) - xvod2.length)}`;
         break;
      }
      for (let r = 0; r < 1; r++) {
          let windn = true;
          let actived: Map<any, any> = new Map([[String.fromCharCode(104,95,56,48,95,120,118,97,103,0),337], [String.fromCharCode(104,95,54,49,95,114,111,111,116,0),719]]);
         xvod2 += `${((armvao ? 5 : 4) * (streaming6 ? 2 : 1))}`;
         windn = !windn || actived.size < 4;
         actived.set(`${windn}`, (actived.size + (windn ? 5 : 5)));
      }
         armvao = xvod2.length >= 84;
         xvod2 += "1";
         armvao = ((xvod2.length % (Math.max(9, (armvao ? 72 : xvod2.length)))) == 72);
      if (streaming6) {
          let userc = 5.0;
          let sliderX = 4;
          let lightV = 1.0;
          let debugj: Array<any> = [494, 210, 212];
          let scorev = String.fromCharCode(118,115,117,98,113,95,113,95,52,0);
         armvao = (10 > ((streaming6 ? 10 : scorev.length) % (Math.max(scorev.length, 1))));
         userc -= 3 | parseInt(`${lightV}`);
         sliderX >>= Math.min(2, Math.abs(parseInt(`${lightV}`)));
         debugj.push(parseInt(`${userc}`) - sliderX);
      }
      while (xvod2.startsWith(`${armvao}`)) {
         xvod2 += "3";
         break;
      }
       let agreement2 = 4.0;
      for (let v = 0; v < 3; v++) {
          let floating0 = String.fromCharCode(101,120,116,101,114,110,97,108,95,51,95,54,51,0);
          let bootsplash_ = 0;
         armvao = bootsplash_ == 29;
         floating0 = `${floating0.length}`;
         bootsplash_ += floating0.length;
      }
      libavdevice3 += `${parseInt(`${gradleF}`)}`;
   while (umengJ.length > parseInt(`${tickedO}`)) {
      umengJ += "1";
      break;
   }
   if (typingv >= tickedO) {
      tickedO *= button9;
   }
      libavdevice3 += `${(umengJ == String.fromCharCode(103,0) ? umengJ.length : pangleK.length)}`;
   let temperatureP = 7666429.0 >= gradleF;
   do {
      gradleF += parseFloat(`${parseInt(`${typingv}`) + 1}`);
      if (temperatureP) {
         break;
      }
   } while ((3.57 >= (gradleF - 3.26) || (gradleF - parseFloat(`${pangleK.length}`)) >= 3.26) && temperatureP);
   if (debugm == parseFloat(`${libavdevice3.length}`)) {
       let dialogr: Map<any, any> = new Map([[String.fromCharCode(116,95,56,55,95,111,112,101,110,109,112,116,0),575], [String.fromCharCode(106,95,50,95,105,110,116,101,114,97,99,116,105,110,103,0),374], [String.fromCharCode(99,105,110,101,95,109,95,49,52,0),785]]);
       let gpayA = String.fromCharCode(101,108,105,109,105,110,97,116,105,111,110,95,48,95,57,54,0);
       let chinasamea = true;
      while (!chinasamea || gpayA.length > 1) {
          let baidu2 = 5.0;
          let libavdevicet: Array<any> = [String.fromCharCode(100,111,116,116,101,100,95,52,95,54,51,0), String.fromCharCode(102,95,54,48,95,114,101,117,112,108,111,97,100,0)];
         gpayA += `${gpayA.length | parseInt(`${baidu2}`)}`;
         baidu2 *= libavdevicet.length * 3;
         libavdevicet = [libavdevicet.length];
         break;
      }
          let favoritey = String.fromCharCode(106,95,56,54,95,108,105,98,114,97,114,121,0);
          let injuryv = String.fromCharCode(104,95,53,51,95,115,116,111,112,112,105,110,103,0);
         dialogr = new Map([[`${chinasamea}`, (favoritey.length - (chinasamea ? 2 : 1))]]);
         favoritey = `${injuryv.length}`;
         injuryv = `${injuryv.length}`;
      while (!chinasamea) {
          let countdown7 = String.fromCharCode(110,111,110,109,117,108,116,95,119,95,50,0);
         chinasamea = countdown7.length > 5;
         break;
      }
      let bdxadsdki = 5497345 >= dialogr.size;
      do {
          let photox: Map<any, any> = new Map([[String.fromCharCode(117,95,56,53,95,116,111,107,101,110,110,102,116,116,120,0),800], [String.fromCharCode(97,112,112,101,110,100,97,98,108,101,95,55,95,54,54,0),63]]);
          let countdown_ = false;
          let xadsdkWc: Array<any> = [621, 677, 429];
          let libsgcoreC = 3.0;
         dialogr.set(gpayA, 1 & xadsdkWc.length);
         photox = new Map([[`${photox.size}`, parseInt(`${libsgcoreC}`)]]);
         countdown_ = photox.size == 3;
         xadsdkWc = [((countdown_ ? 3 : 1) - parseInt(`${libsgcoreC}`))];
         if (bdxadsdki) {
            break;
         }
      } while (bdxadsdki && (chinasamea && (dialogr.size | 4) > 3));
      let securityv = chinasamea ? !chinasamea : chinasamea;
      do {
         chinasamea = (dialogr.size | gpayA.length) == 59;
         if (securityv) {
            break;
         }
      } while (securityv && (gpayA.length <= 4 && !chinasamea));
          let modalv = 0.0;
          let pauseK = String.fromCharCode(111,98,106,101,99,116,115,95,104,95,53,56,0);
         chinasamea = pauseK == String.fromCharCode(108,0) && dialogr.size >= 47;
         modalv += parseFloat(`${parseInt(`${modalv}`) | parseInt(`${modalv}`)}`);
         pauseK += `${1 | parseInt(`${modalv}`)}`;
         dialogr = new Map([[`${dialogr.size}`, dialogr.size << (Math.min(gpayA.length, 2))]]);
      for (let y = 0; y < 2; y++) {
         chinasamea = (76 > ((chinasamea ? gpayA.length : 76) / (Math.max(gpayA.length, 5))));
      }
      if ((gpayA.length >> (Math.min(2, Math.abs(dialogr.size)))) <= 5 && (dialogr.size >> (Math.min(Math.abs(5), 2))) <= 2) {
         gpayA += `${(3 - (chinasamea ? 3 : 3))}`;
      }
      libavdevice3 += "3";
   }
   let save8 = debugm >= 5768809.0;
   do {
       let gemfile1 = String.fromCharCode(115,95,54,52,95,97,112,116,120,0);
       let libyogap = false;
       let window_hwf = 1;
          let currentB = String.fromCharCode(100,114,101,102,95,101,95,53,55,0);
          let formA = 0.0;
         window_hwf /= Math.max(4, currentB.length / (Math.max(3, 7)));
         currentB += `${parseInt(`${formA}`) << (Math.min(3, Math.abs(1)))}`;
         formA -= parseInt(`${formA}`);
      if (gemfile1.endsWith(`${libyogap}`)) {
         gemfile1 += `${gemfile1.length + 2}`;
      }
      if ((window_hwf << (Math.min(Math.abs(1), 2))) >= 4) {
          let crossx = 2.0;
          let carouselv: Map<any, any> = new Map([[String.fromCharCode(110,95,55,50,95,105,115,119,104,105,116,101,115,112,97,99,101,0),false ], [String.fromCharCode(115,116,112,115,95,106,95,56,57,0),true ]]);
          let rootn = String.fromCharCode(118,99,97,99,100,97,116,97,95,106,95,54,54,0);
          let leagueI = String.fromCharCode(122,95,57,52,95,109,101,108,116,0);
         libyogap = gemfile1.length < 18;
         crossx *= parseInt(`${crossx}`);
         carouselv = new Map([[`${carouselv.size}`, rootn.length]]);
         rootn = "2";
         leagueI += `${3 >> (Math.min(1, Math.abs(parseInt(`${crossx}`))))}`;
      }
         libyogap = 51 >= window_hwf || !libyogap;
      if (gemfile1.length <= 3 && !libyogap) {
         libyogap = libyogap && window_hwf < 87;
      }
      for (let s = 0; s < 3; s++) {
         libyogap = libyogap || window_hwf <= 46;
      }
         gemfile1 = `${(1 - (libyogap ? 2 : 5))}`;
      if (gemfile1.includes(`${window_hwf}`)) {
         window_hwf <<= Math.min(Math.abs(3), 3);
      }
          let navigationr = false;
         gemfile1 = `${((navigationr ? 3 : 3) >> (Math.min(Math.abs(window_hwf), 3)))}`;
      debugm /= Math.max(parseFloat(`${gemfile1.length}`), 5);
      if (save8) {
         break;
      }
   } while (((5.49 - typingv) >= 5.55 && (5.49 * debugm) >= 5.92) && save8);
      benefitX += `${pangleK.length + parseInt(`${recommendation_}`)}`;
   if ((1 | libavdevice3.length) == 4 && (parseInt(`${typingv}`) / 1) == 1) {
       let giftk = String.fromCharCode(112,95,54,49,95,99,104,97,110,103,101,103,114,111,117,112,0);
       let streamingR = String.fromCharCode(116,97,112,112,105,110,103,95,49,95,53,0);
         streamingR += `${giftk.length * streamingR.length}`;
         streamingR += `${(String.fromCharCode(113,0) == giftk ? giftk.length : streamingR.length)}`;
      for (let s = 0; s < 1; s++) {
         giftk += `${giftk.length}`;
      }
      if (2 <= streamingR.length || giftk == String.fromCharCode(102,0)) {
         giftk = `${1 ^ giftk.length}`;
      }
       let membershipE = String.fromCharCode(108,95,53,48,95,104,115,116,114,105,110,103,0);
       let singaporeJ = String.fromCharCode(116,105,109,101,111,117,116,101,100,95,98,95,54,50,0);
          let text0 = String.fromCharCode(97,105,109,105,110,103,95,97,95,56,50,0);
          let tail7 = String.fromCharCode(111,95,55,49,95,97,118,117,116,105,108,114,101,115,0);
         giftk = `${3 / (Math.max(2, text0.length))}`;
         text0 += `${tail7.length / (Math.max(1, tail7.length))}`;
      libavdevice3 = `${launchS.length}`;
   }
   let o_imagev = 9108358 <= umengJ.length;
   do {
       let mimor = String.fromCharCode(110,97,116,117,114,97,108,95,52,95,56,51,0);
         mimor += `${mimor.length | 3}`;
      while (mimor.startsWith(`${mimor.length}`)) {
          let incidentR = String.fromCharCode(118,101,108,111,99,105,116,121,95,51,95,56,48,0);
          let mountingC = String.fromCharCode(100,95,51,57,95,97,100,116,115,0);
          let privilegeJ = 1;
         mimor = "3";
         incidentR = `${privilegeJ}`;
         mountingC = `${incidentR.length}`;
         privilegeJ ^= 2 ^ privilegeJ;
         break;
      }
         mimor += "3";
      umengJ = `${1 ^ parseInt(`${typingv}`)}`;
      if (o_imagev) {
         break;
      }
   } while (((parseInt(`${tickedO}`) / (Math.max(umengJ.length, 3))) <= 2 || (parseInt(`${tickedO}`) / (Math.max(umengJ.length, 3))) <= 2) && o_imagev);
      umengJ = `${(String.fromCharCode(51,0) == libavdevice3 ? parseInt(`${recommendation_}`) : libavdevice3.length)}`;
   if ((3.43 - debugm) <= 1.10 || (debugm - 3.43) <= 5.91) {
      recommendation_ -= (parseFloat(`${String.fromCharCode(49,0) == benefitX ? benefitX.length : launchS.length}`));
   }
       let o_player_ = 3.0;
       let anythinkO = true;
       let episodesW = String.fromCharCode(112,95,53,55,95,97,112,112,114,111,120,0);
         episodesW = `${((anythinkO ? 1 : 4) % (Math.max(parseInt(`${o_player_}`), 4)))}`;
         anythinkO = !episodesW.startsWith(`${anythinkO}`);
          let shirto = 2;
          let slider4: Map<any, any> = new Map([[String.fromCharCode(109,95,57,50,95,116,101,120,116,117,114,101,100,0),418], [String.fromCharCode(101,95,51,51,95,122,108,105,98,0),798]]);
         o_player_ *= (shirto * (anythinkO ? 4 : 3));
         shirto %= Math.max(3, slider4.size);
         slider4 = new Map([[`${slider4.size}`, 1]]);
         anythinkO = !anythinkO;
         anythinkO = o_player_ > 51.2 || anythinkO;
      for (let l = 0; l < 1; l++) {
          let acopy_0ld = 5.0;
          let edit4 = String.fromCharCode(112,114,101,102,111,114,109,97,116,116,101,100,95,102,95,54,54,0);
          let googlel = true;
         anythinkO = episodesW.length >= 47;
         acopy_0ld -= (parseFloat(`${(googlel ? 5 : 4)}`));
         edit4 = `${edit4.length + 1}`;
         googlel = !googlel;
      }
         o_player_ += (episodesW == String.fromCharCode(50,0) ? episodesW.length : (anythinkO ? 3 : 5));
         anythinkO = episodesW.length == o_player_;
         o_player_ += (parseInt(`${o_player_}`) << (Math.min(2, Math.abs((anythinkO ? 5 : 2)))));
      debugm /= Math.max(parseFloat(`${v_lockW.length + parseInt(`${tickedO}`)}`), 5);
   while ((4 ^ launchS.length) <= 1) {
      launchS = `${parseInt(`${typingv}`)}`;
      break;
   }
      typingv /= Math.max(3, 2);
    RNRestart.Restart();
  }

  useEffect(() => {
    if(countToggleB == 8){
      switchToggle();
    }
  }, [countToggleB])

  return (
    
    
    
    
    
    
    //     }}>
    
    //   </Text>
    
    <ScreenContainer>
      <View style={{ gap: spacing.m }}>
        <TitleWithBackButtonHeader title="关于我们" />
        <View style={styles.logo}>
          <Logo2 height={icons.sizes.xxl} width={icons.sizes.xxl} />
        </View>
        <TouchableOpacity onPress={spamToggleB}>
          <Text style={{ textAlign: "center", ...textVariants.body }}>
            {APP_VERSION}
          </Text>
        </TouchableOpacity>
        <NotificationModal
          onConfirm={toggleOverlay}
          isVisible={isDialogOpen}
          title="版权声明"
          subtitle1={`如果该APP提供内容侵犯了您的版权, 请发送电子邮件说明, 我们将立即删除内容, 保护版权所有者的权益。`}
          subtitle2="联系邮箱:"
          subtitle3={APP_EMAIL_CONST}
        />
        <View>
          <ShowMoreButton
            text="隐私政策"
            onPress={() => navigation.navigate("隐私政策")}
          />
          <ShowMoreButton
            text="用户协议"
            onPress={() => navigation.navigate("用户协议")}
          />
          {/* <ShowMoreButton
            text="版权声明"
            onPress={() => setIsDialogOpen(!isDialogOpen)}
          /> */}
        </View>
      </View>
    </ScreenContainer>
  );
};

const styles = StyleSheet.create({
  topNav: {
    paddingTop: 10,
    paddingLeft: 10,
    paddingRight: 10,
    display: "flex",
    flexDirection: "row",
    width: "100%",
    alignItems: "center",
    justifyContent: "space-between",
  },
  logo: {
    flexDirection: "row",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
  },
  submitBtn: {
    borderRadius: 10,
  },
});
