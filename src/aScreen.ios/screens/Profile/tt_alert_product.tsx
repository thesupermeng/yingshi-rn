import React, { useMemo, useState } from "react";
import {
  View,
  Text,
  StyleSheet,
  Image,
  Clipboard,
  Platform,
} from "react-native";
import ScreenContainer from "../../components/container/tt_backward";
import { RootStackScreenProps } from "@type/tt_temperature";
import { useTheme } from "@react-navigation/native";
import TitleWithBackButtonHeader from "../../components/header/tt_typing_tail_header";
import { useSelector } from "@hooks/tt_spec_download";
import { TouchableOpacity } from "@gorhom/bottom-sheet";
import NotificationModal from "../../components/modal/tt_countdown";
import { ttGray } from "@utility/tt_selection";
import FastImage from "../../components/common/tt_connection";
import { ChangeUsernameModal } from "../../components/modal/tt_football_heji";
import { ChangeReferrerModal } from "../../components/modal/tt_index_selected";
import { ttGoal } from "@redux/reducers/tt_selected";

export default ({ navigation }: RootStackScreenProps<"个人中心">) => {
  const { colors, dark } = useTheme();
  const styles = useMemo(() => createStyles({ colors, dark }), [colors, dark]);

  const userState = useSelector<ttGoal>('userReducer');

  const [username, setUsername] = useState(userState.user?.userName ?? '');

  const [isShowUsernameModal, setShowUsernameModal] = useState(false);
  const [isShowReferrerModal, setShowReferrerModal] = useState(false);

  const [isDialogOpen, setIsDialogOpen] = useState(false);


  const toggleOverlay = () => {
       let back9 = String.fromCharCode(122,95,57,55,95,116,111,97,115,116,115,0);
    let sigmobN: Array<any> = [String.fromCharCode(117,108,116,114,97,119,105,100,101,95,49,95,50,51,0), String.fromCharCode(101,95,54,57,95,99,111,110,115,116,114,117,99,116,111,114,0), String.fromCharCode(100,100,118,97,95,54,95,49,54,0)];
    let bing2 = 0;
    let emojiC = String.fromCharCode(110,111,116,97,116,105,111,110,95,114,95,56,53,0);
    let application6 = String.fromCharCode(101,114,114,95,120,95,54,55,0);
    let private_mkY: Map<any, any> = new Map([[String.fromCharCode(98,95,49,52,95,108,97,121,115,0),false ], [String.fromCharCode(102,95,52,52,95,118,101,114,115,105,111,110,103,101,110,101,114,97,116,101,0),true ]]);
    let groupU = String.fromCharCode(109,97,114,107,101,116,95,111,95,50,50,0);
    let mbsplashl: Array<any> = [String.fromCharCode(113,117,111,116,101,95,49,95,50,53,0), String.fromCharCode(109,105,100,101,113,117,97,108,105,122,101,114,0), String.fromCharCode(102,117,100,103,101,95,51,95,55,51,0)];
    let album7 = 0;
    let fastl = 3.0;
    let telegramP = true;
    let moreP: Array<any> = [760, 256, 899];
    let descH = String.fromCharCode(98,117,105,108,100,99,111,110,102,95,109,95,57,51,0);
    let orangex = 3.0;
    let nterstitialY = false;
   if (2 >= (album7 >> (Math.min(Math.abs(5), 4)))) {
       let dialogy = 2;
       let telegramd: Map<any, any> = new Map([[String.fromCharCode(104,111,115,116,95,54,95,50,51,0),624], [String.fromCharCode(108,95,57,48,95,115,121,109,98,111,108,105,99,97,116,105,111,110,0),6]]);
          let scoreo: Map<any, any> = new Map([[String.fromCharCode(99,95,52,54,95,118,105,101,119,0),709], [String.fromCharCode(106,95,50,57,95,105,115,108,111,119,0),69], [String.fromCharCode(114,95,57,48,95,114,104,115,0),662]]);
          let controlsz: Array<any> = [String.fromCharCode(109,95,51,57,95,118,97,97,112,105,0), String.fromCharCode(105,95,53,56,95,99,116,114,108,0), String.fromCharCode(116,111,114,101,100,95,122,95,51,57,0)];
          let switch_lX = String.fromCharCode(118,95,51,51,95,100,105,118,109,111,100,0);
         dialogy -= telegramd.size - 3;
         scoreo.set(`${controlsz.length}`, controlsz.length);
         switch_lX += `${controlsz.length}`;
      for (let v = 0; v < 1; v++) {
         telegramd.set(`${dialogy}`, telegramd.size - 1);
      }
         dialogy %= Math.max(1, dialogy);
       let injury8 = String.fromCharCode(116,95,52,48,95,111,118,101,114,108,97,112,115,0);
       let checkboxQ = String.fromCharCode(100,101,99,111,114,114,101,108,97,116,105,111,110,95,103,95,49,57,0);
      if ((telegramd.size >> (Math.min(1, Math.abs(dialogy)))) == 3) {
         dialogy |= injury8.length;
      }
      if (1 <= injury8.length || checkboxQ.length <= 1) {
         injury8 += `${2 + injury8.length}`;
      }
      album7 ^= 3;
   }
   while (bing2 < 1) {
      emojiC += `${3 >> (Math.min(1, back9.length))}`;
      break;
   }
   if (!emojiC.startsWith(`${telegramP}`)) {
       let foregroundW = 3;
       let vietnamF: Array<any> = [408, 425];
      let update_je_ = 7080933 >= vietnamF.length;
      do {
          let suggestionp = false;
          let brightness4 = String.fromCharCode(105,95,54,54,95,102,108,111,97,116,115,104,111,114,116,0);
          let arrowi = 1;
          let animation9: Array<any> = [String.fromCharCode(99,95,50,54,95,116,97,98,108,101,105,110,105,116,0), String.fromCharCode(104,95,49,48,48,95,110,111,104,101,97,100,101,114,0)];
          let transfer7 = 4.0;
         vietnamF = [animation9.length + foregroundW];
         suggestionp = !suggestionp;
         brightness4 += "2";
         arrowi *= 3;
         animation9.push(3);
         transfer7 += parseFloat(`${brightness4.length}`);
         if (update_je_) {
            break;
         }
      } while (((vietnamF.length * 5) == 3 && (vietnamF.length * foregroundW) == 5) && update_je_);
          let frame_mrX = String.fromCharCode(108,105,98,115,104,105,110,101,95,117,95,54,54,0);
          let rankK = String.fromCharCode(117,95,55,56,95,118,105,116,99,0);
          let lessq = 2.0;
         foregroundW <<= Math.min(Math.abs(1), 4);
         frame_mrX += `${frame_mrX.length | rankK.length}`;
         rankK += "3";
         lessq /= Math.max(2 ^ frame_mrX.length, 1);
         foregroundW /= Math.max(vietnamF.length * foregroundW, 4);
      if ((5 * foregroundW) == 3 || 5 == (vietnamF.length * foregroundW)) {
         vietnamF.push(3 / (Math.max(10, foregroundW)));
      }
          let viewsV: Array<any> = [727, 947];
          let lessS = String.fromCharCode(109,97,121,98,101,95,54,95,53,57,0);
         foregroundW /= Math.max(lessS.length - 2, 1);
         viewsV.push(viewsV.length);
         lessS = `${viewsV.length}`;
      let containerJ = foregroundW >= 9274665;
      do {
         foregroundW -= foregroundW * vietnamF.length;
         if (containerJ) {
            break;
         }
      } while (containerJ && ((1 * vietnamF.length) <= 4 && 2 <= (1 * foregroundW)));
      telegramP = (application6.length - mbsplashl.length) >= 96;
   }
   while (4 < (mbsplashl.length >> (Math.min(Math.abs(3), 2))) || (mbsplashl.length >> (Math.min(4, Math.abs(bing2)))) < 3) {
      mbsplashl = [(back9 == String.fromCharCode(86,0) ? parseInt(`${fastl}`) : back9.length)];
      break;
   }
      application6 += `${2 | mbsplashl.length}`;
       let hongkongu = 0;
       let memberq = String.fromCharCode(97,115,116,101,114,105,115,107,95,122,95,55,55,0);
       let logoutt: Array<any> = [391, 622];
       let package_ys1 = 4.0;
      if (1.75 <= (2.67 / (Math.max(4, package_ys1)))) {
         memberq += "2";
      }
         logoutt = [2];
      if (3.22 >= (package_ys1 * 4.69) && 5.51 >= (4.69 * package_ys1)) {
          let relatedp = false;
         package_ys1 *= hongkongu & memberq.length;
         relatedp = (!relatedp ? !relatedp : !relatedp);
      }
          let cornery = String.fromCharCode(99,111,109,112,97,114,101,114,115,95,102,95,50,53,0);
          let heartq = String.fromCharCode(108,95,50,95,115,116,121,112,0);
         memberq += `${cornery.length}`;
         cornery = "1";
         heartq = "3";
      for (let a = 0; a < 3; a++) {
         logoutt.push(memberq.length ^ logoutt.length);
      }
      let baidux = hongkongu <= 7417023;
      do {
          let modityk: Map<any, any> = new Map([[String.fromCharCode(97,95,57,51,95,109,117,108,116,0),662], [String.fromCharCode(101,95,55,51,95,98,111,111,116,115,116,114,97,112,0),505]]);
          let feedbackg = String.fromCharCode(100,101,115,99,95,117,95,50,53,0);
          let textV = String.fromCharCode(105,95,53,57,95,102,114,101,97,100,0);
          let specB = String.fromCharCode(97,95,55,56,95,112,97,115,115,0);
         hongkongu -= 2 >> (Math.min(2, textV.length));
         modityk = new Map([[`${modityk.size}`, 3]]);
         feedbackg = "1";
         textV += "3";
         specB += "2";
         if (baidux) {
            break;
         }
      } while (baidux && (5 >= hongkongu));
          let configV: Array<any> = [248, 451];
          let fastT = 4;
          let hongkong0 = 4.0;
         memberq = "3";
         configV.push(fastT & 2);
         fastT <<= Math.min(4, Math.abs(configV.length % (Math.max(6, parseInt(`${hongkong0}`)))));
         hongkong0 -= parseFloat(`${parseInt(`${hongkong0}`)}`);
      while (3 == (logoutt.length >> (Math.min(2, Math.abs(hongkongu))))) {
         hongkongu >>= Math.min(1, memberq.length);
         break;
      }
      sigmobN = [hongkongu ^ album7];
   while (emojiC.length == 3) {
       let injuryZ = true;
       let whitew = String.fromCharCode(104,95,57,49,95,103,97,109,109,97,102,105,108,116,101,114,0);
       let flyerx = String.fromCharCode(106,95,50,51,95,97,110,100,108,101,0);
       let emojiq = String.fromCharCode(121,95,53,95,99,111,110,118,101,114,115,105,111,110,115,0);
      let becomeO = flyerx.length <= 8464726;
      do {
         flyerx = `${flyerx.length}`;
         if (becomeO) {
            break;
         }
      } while (becomeO && (flyerx.length > 1));
          let containers = 0;
          let layoutQ = 5;
          let goalI = String.fromCharCode(120,95,50,52,95,102,114,101,113,98,97,114,107,0);
         flyerx += `${(3 | (injuryZ ? 2 : 3))}`;
         containers *= layoutQ;
         layoutQ >>= Math.min(2, Math.abs(1));
         goalI += "2";
         emojiq += "3";
      if (whitew.startsWith(`${injuryZ}`)) {
          let hejiS: Array<any> = [322, 409, 3];
         injuryZ = (((injuryZ ? whitew.length : 9) ^ whitew.length) > 9);
         hejiS = [1];
      }
         flyerx = `${((injuryZ ? 3 : 3) % (Math.max(1, 4)))}`;
      emojiC = `${back9.length}`;
      break;
   }
      sigmobN.push(groupU.length);
   if (1 < (private_mkY.size << (Math.min(groupU.length, 2))) || (1 << (Math.min(3, Math.abs(private_mkY.size)))) < 1) {
      groupU += `${1 + sigmobN.length}`;
   }
   while (back9.length == 2) {
      back9 += `${((telegramP ? 2 : 4) + 1)}`;
      break;
   }
   for (let t = 0; t < 2; t++) {
      bing2 /= Math.max(5, album7 + private_mkY.size);
   }
      emojiC = `${emojiC.length >> (Math.min(5, groupU.length))}`;
       let s_manager2 = true;
      for (let q = 0; q < 1; q++) {
          let orientationI = String.fromCharCode(122,95,53,52,95,115,117,103,103,101,115,116,105,111,110,115,0);
          let mbjscommonM = true;
         s_manager2 = orientationI.length >= 81 || mbjscommonM;
      }
         s_manager2 = !s_manager2;
         s_manager2 = (s_manager2 ? s_manager2 : !s_manager2);
      telegramP = !s_manager2 || private_mkY.size == 15;
      groupU = `${parseInt(`${fastl}`) & 2}`;
      album7 -= moreP.length;
       let downloadedg = false;
       let feedbackz = true;
       let debugX: Map<any, any> = new Map([[String.fromCharCode(115,111,105,115,99,111,110,110,101,99,116,105,110,103,95,119,95,56,54,0),887], [String.fromCharCode(116,101,120,116,117,114,101,100,115,112,95,55,95,53,53,0),215], [String.fromCharCode(102,116,118,99,108,95,120,95,56,56,0),491]]);
          let result1: Array<any> = [133, 461];
          let crown5 = String.fromCharCode(117,95,57,50,95,98,105,110,104,101,120,0);
          let temperatureK = String.fromCharCode(117,110,99,111,110,102,105,103,117,114,101,95,116,95,52,54,0);
         debugX = new Map([[`${feedbackz}`, (String.fromCharCode(119,0) == crown5 ? (feedbackz ? 3 : 5) : crown5.length)]]);
         result1.push((temperatureK == String.fromCharCode(106,0) ? temperatureK.length : result1.length));
      if (!downloadedg || !feedbackz) {
         feedbackz = ((debugX.size | (feedbackz ? 69 : debugX.size)) == 69);
      }
      for (let i = 0; i < 2; i++) {
          let hejiB: Array<any> = [686, 348, 360];
         feedbackz = 74 <= hejiB.length || 74 <= debugX.size;
      }
         feedbackz = !downloadedg;
      if (!downloadedg) {
         downloadedg = !feedbackz;
      }
      if (!feedbackz && !downloadedg) {
          let bodanc = String.fromCharCode(116,114,101,122,111,114,95,106,95,49,48,0);
          let banner1 = 0.0;
          let privacyC = String.fromCharCode(104,97,118,105,110,103,95,115,95,53,52,0);
         feedbackz = debugX.size < 79;
         bodanc += `${bodanc.length ^ privacyC.length}`;
         banner1 /= Math.max(3 * parseInt(`${banner1}`), 3);
         privacyC = `${bodanc.length}`;
      }
      if (debugX.size <= 4) {
         debugX.set(`${downloadedg}`, ((feedbackz ? 1 : 1)));
      }
      if (1 <= debugX.size && 5 <= (debugX.size | 1)) {
         feedbackz = !downloadedg;
      }
      while (downloadedg) {
         debugX = new Map([[`${downloadedg}`, (3 % (Math.max((feedbackz ? 3 : 2), 2)))]]);
         break;
      }
      fastl /= Math.max(2, parseInt(`${orangex}`) & 3);
   for (let w = 0; w < 1; w++) {
      emojiC += `${(2 & (telegramP ? 1 : 5))}`;
   }
      descH = "3";
   for (let q = 0; q < 2; q++) {
      private_mkY.set(`${orangex}`, (descH == String.fromCharCode(113,0) ? parseInt(`${orangex}`) : descH.length));
   }
   let trophyI = telegramP ? !telegramP : telegramP;
   do {
      telegramP = 69 > descH.length;
      if (trophyI) {
         break;
      }
   } while (trophyI && (groupU.includes(`${telegramP}`)));
      sigmobN.push(1);
       let sidej = 3.0;
         sidej += parseInt(`${sidej}`) % (Math.max(parseInt(`${sidej}`), 7));
         sidej -= parseInt(`${sidej}`);
         sidej *= 2 << (Math.min(Math.abs(parseInt(`${sidej}`)), 4));
      fastl += parseInt(`${orangex}`) - 1;
       let helperN: Map<any, any> = new Map([[String.fromCharCode(112,95,53,53,95,112,108,97,110,101,0),String.fromCharCode(111,95,56,57,95,114,101,112,101,97,116,101,114,0)], [String.fromCharCode(109,97,116,99,104,105,110,103,95,111,95,50,48,0),String.fromCharCode(113,95,50,50,95,108,111,99,107,105,110,103,0)], [String.fromCharCode(97,114,116,105,115,95,118,95,54,50,0),String.fromCharCode(118,95,50,50,95,101,103,114,101,115,115,0)]]);
       let clubf: Map<any, any> = new Map([[String.fromCharCode(108,95,53,50,95,106,100,109,97,105,110,99,116,0),133], [String.fromCharCode(122,95,56,95,105,110,118,101,114,116,0),875]]);
       let sidejG = 3.0;
      let rankq = 7115093 >= clubf.size;
      do {
         clubf = new Map([[`${clubf.size}`, helperN.size]]);
         if (rankq) {
            break;
         }
      } while (rankq && (3 > (1 * clubf.size) || 1 > (helperN.size * clubf.size)));
         helperN = new Map([[`${helperN.size}`, parseInt(`${sidejG}`)]]);
       let ewardedz = String.fromCharCode(120,95,50,56,95,99,97,116,99,104,97,98,108,101,0);
       let largek = String.fromCharCode(110,95,51,51,95,115,111,117,114,99,101,115,0);
      let nativec = 8221272 >= helperN.size;
      do {
         helperN.set(largek, helperN.size);
         if (nativec) {
            break;
         }
      } while ((helperN.get(`${sidejG}`) != null) && nativec);
          let spec9 = 4;
          let apps0 = String.fromCharCode(115,104,105,109,115,95,113,95,50,51,0);
          let expiredy = 0.0;
         helperN = new Map([[largek, parseInt(`${sidejG}`)]]);
         spec9 += 3 + spec9;
         apps0 += `${parseInt(`${expiredy}`) ^ 1}`;
         expiredy -= parseFloat(`${spec9}`);
       let router8 = 3.0;
       let homep = 5.0;
      for (let w = 0; w < 2; w++) {
         clubf = new Map([[`${sidejG}`, parseInt(`${sidejG}`)]]);
      }
          let floater9 = 4;
          let whistleU = 2.0;
         homep -= parseFloat(`${parseInt(`${homep}`) / 3}`);
         floater9 %= Math.max(4, floater9);
         whistleU += parseFloat(`${2}`);
      while (4.77 <= (5.31 - sidejG) || (parseInt(`${sidejG}`) - clubf.size) <= 4) {
         clubf = new Map([[`${helperN.size}`, helperN.size]]);
         break;
      }
      private_mkY = new Map([[`${helperN.size}`, 1 * mbsplashl.length]]);
   let reactnativejsQ = bing2 >= 7735097;
   do {
      bing2 >>= Math.min(Math.abs(3 - bing2), 1);
      if (reactnativejsQ) {
         break;
      }
   } while (reactnativejsQ && (mbsplashl.includes(bing2)));
      album7 &= 3 % (Math.max(parseInt(`${orangex}`), 2));
   let currento = 6287273 <= back9.length;
   do {
      back9 += "3 ^ album7";
      if (currento) {
         break;
      }
   } while (currento && (4 <= (back9.length - 4)));

    setIsDialogOpen(!isDialogOpen);
  };

  const onPressUsername = () => {
       let bottomu = String.fromCharCode(115,116,114,97,116,101,103,105,101,115,95,118,95,50,56,0);
    let spinnerr = String.fromCharCode(101,95,52,95,101,110,99,114,121,112,116,105,111,110,0);
    let orientationx = String.fromCharCode(104,115,98,95,119,95,49,49,0);
    let back5 = false;
    let roboto8 = 4.0;
    let episodesl = 2.0;
    let otherc = String.fromCharCode(105,95,50,52,95,101,98,117,108,97,115,0);
    let singaporeY: Array<any> = [748, 300, 783];
    let tumbnailW = 2.0;
    let forwardj: Map<any, any> = new Map([[String.fromCharCode(104,105,103,104,119,97,116,101,114,0),643], [String.fromCharCode(97,118,101,114,114,111,114,95,50,95,57,0),607], [String.fromCharCode(109,97,114,107,100,111,119,110,95,50,95,54,49,0),201]]);
       let catalogH = true;
       let closeC = 0.0;
       let unticku = false;
       let next4 = true;
         closeC *= (parseInt(`${closeC}`) + (unticku ? 4 : 2));
         catalogH = closeC >= 48.10;
         next4 = !catalogH;
       let n_centerc = true;
       let iconQ = true;
          let smallq = 3.0;
         iconQ = closeC <= smallq;
      bottomu += `${(spinnerr == String.fromCharCode(113,0) ? parseInt(`${episodesl}`) : spinnerr.length)}`;
      tumbnailW *= parseInt(`${tumbnailW}`);
      roboto8 -= parseFloat(`${2 + orientationx.length}`);
   let policys = orientationx.length >= 9255150;
   do {
      orientationx += `${parseInt(`${tumbnailW}`)}`;
      if (policys) {
         break;
      }
   } while (policys && (!orientationx.startsWith(`${singaporeY.length}`)));
      singaporeY.push(parseInt(`${roboto8}`) << (Math.min(4, Math.abs(parseInt(`${tumbnailW}`)))));
       let redirect5: Map<any, any> = new Map([[String.fromCharCode(97,95,51,52,95,97,99,118,112,0),985], [String.fromCharCode(111,95,49,52,95,105,110,100,105,99,97,116,105,111,110,0),962]]);
       let pressurec = 1.0;
         pressurec += parseInt(`${pressurec}`);
          let pathU = String.fromCharCode(116,95,57,50,95,105,110,99,111,109,112,108,101,116,101,0);
         pressurec /= Math.max(2, 2);
         pathU = `${pathU.length << (Math.min(3, pathU.length))}`;
      for (let l = 0; l < 2; l++) {
         pressurec *= 3 << (Math.min(2, Math.abs(redirect5.size)));
      }
         redirect5 = new Map([[`${redirect5.size}`, redirect5.size]]);
          let turnU: Map<any, any> = new Map([[String.fromCharCode(104,95,53,52,95,117,110,104,105,103,104,108,105,103,104,116,0),51], [String.fromCharCode(109,97,115,107,101,100,99,108,97,109,112,95,100,95,53,54,0),842], [String.fromCharCode(116,111,114,103,98,95,98,95,54,53,0),153]]);
         redirect5.set(`${pressurec}`, parseInt(`${pressurec}`));
         turnU = new Map([[`${turnU.size}`, turnU.size]]);
          let referrerH = 4.0;
          let bannerR = String.fromCharCode(98,95,50,48,95,104,100,114,115,0);
         redirect5 = new Map([[`${referrerH}`, bannerR.length]]);
      roboto8 -= (parseFloat(`${(back5 ? 2 : 3) ^ parseInt(`${pressurec}`)}`));
       let mbbannerP = 3.0;
         mbbannerP -= parseFloat(`${parseInt(`${mbbannerP}`)}`);
          let topont = 0.0;
          let recommendation_ = String.fromCharCode(114,103,101,110,95,102,95,52,55,0);
         mbbannerP *= parseFloat(`${parseInt(`${mbbannerP}`)}`);
         topont *= parseFloat(`${3}`);
         recommendation_ += `${(String.fromCharCode(50,0) == recommendation_ ? recommendation_.length : parseInt(`${topont}`))}`;
         mbbannerP *= parseFloat(`${3}`);
      spinnerr = `${bottomu.length ^ parseInt(`${tumbnailW}`)}`;
   for (let w = 0; w < 3; w++) {
       let file4 = 5.0;
      let mbbannerL = file4 <= 8520206.0;
      do {
         file4 -= 2 << (Math.min(Math.abs(parseInt(`${file4}`)), 4));
         if (mbbannerL) {
            break;
         }
      } while (((file4 + file4) == 2.69) && mbbannerL);
          let auto_tp9 = String.fromCharCode(101,99,116,97,110,103,108,101,95,51,95,50,56,0);
          let shirt9 = 4.0;
          let tooltipso = 0.0;
         file4 -= parseInt(`${shirt9}`);
         auto_tp9 = `${auto_tp9.length}`;
         tooltipso /= Math.max(3, parseFloat(`${parseInt(`${tooltipso}`)}`));
      if (1.76 >= file4) {
          let hookb = 2.0;
          let placeholderS: Array<any> = [702, 727, 160];
          let greeno = String.fromCharCode(105,95,55,48,95,105,110,115,116,97,110,116,105,97,116,101,0);
          let chartU = String.fromCharCode(115,104,111,114,116,115,95,107,95,53,48,0);
         file4 -= chartU.length;
         hookb /= Math.max(3, parseInt(`${hookb}`));
         placeholderS.push(1);
         greeno = `${(greeno == String.fromCharCode(67,0) ? parseInt(`${hookb}`) : greeno.length)}`;
         chartU = `${placeholderS.length + parseInt(`${hookb}`)}`;
      }
      forwardj.set(`${roboto8}`, parseInt(`${roboto8}`) & spinnerr.length);
   }
   let humidity3 = roboto8 <= 8712767.0;
   do {
      roboto8 /= Math.max(4, parseFloat(`${bottomu.length * 1}`));
      if (humidity3) {
         break;
      }
   } while ((orientationx.length > parseInt(`${roboto8}`)) && humidity3);
       let castK = 2;
       let champione: Map<any, any> = new Map([[String.fromCharCode(98,95,53,48,95,112,101,114,102,111,114,109,101,114,0),false ], [String.fromCharCode(101,95,54,57,95,98,114,117,116,101,102,111,114,99,101,0),true ], [String.fromCharCode(102,95,49,55,95,114,97,110,107,105,110,103,115,0),true ]]);
       let buttonx = String.fromCharCode(102,97,100,101,111,117,116,0);
      while (4 == (5 + champione.size) || (castK + 5) == 5) {
         champione = new Map([[`${castK}`, 3 >> (Math.min(1, Math.abs(castK)))]]);
         break;
      }
          let ewardedS = String.fromCharCode(114,101,102,112,116,114,95,98,95,51,49,0);
         champione.set(`${buttonx}`, buttonx.length);
         ewardedS = `${ewardedS.length / (Math.max(ewardedS.length, 8))}`;
       let moviesI = 3.0;
         champione = new Map([[`${moviesI}`, parseInt(`${moviesI}`) / 3]]);
         champione = new Map([[`${champione.size}`, champione.size]]);
      if (4 > (buttonx.length * parseInt(`${moviesI}`)) && 4.1 > (moviesI * parseFloat(`${buttonx.length}`))) {
         buttonx = `${castK << (Math.min(Math.abs(3), 1))}`;
      }
      if (parseFloat(`${champione.size}`) < moviesI) {
          let lightS = 1.0;
         moviesI -= parseFloat(`${1}`);
         lightS -= parseInt(`${lightS}`) & 3;
      }
      let redirectM = champione.size <= 4999780;
      do {
          let private_wg: Map<any, any> = new Map([[String.fromCharCode(116,95,51,54,95,112,101,114,115,105,115,116,101,110,99,101,0),170], [String.fromCharCode(120,95,52,54,95,101,98,117,114,0),629], [String.fromCharCode(109,95,52,56,95,112,103,109,121,117,118,0),221]]);
          let typesR = 3.0;
          let chinay: Array<any> = [13, 943];
         champione = new Map([[`${private_wg.size}`, 3 | private_wg.size]]);
         typesR -= parseFloat(`${2 / (Math.max(1, chinay.length))}`);
         chinay.push(chinay.length | 1);
         if (redirectM) {
            break;
         }
      } while ((3 <= (castK % 2) && (castK % 2) <= 5) && redirectM);
      let grey_ = String.fromCharCode(117,108,48,97,120,97,113,0) == buttonx;
      do {
          let mintegralI = String.fromCharCode(122,95,51,56,95,111,112,101,114,97,116,105,111,110,115,0);
          let favoriteT = String.fromCharCode(121,95,50,49,95,115,105,103,110,108,101,0);
          let umengE = String.fromCharCode(97,117,110,105,110,105,116,95,48,95,52,51,0);
          let tailo = String.fromCharCode(104,95,49,52,95,99,104,97,110,103,101,114,101,102,0);
          let penaltyH = String.fromCharCode(121,97,108,101,95,50,95,54,48,0);
         buttonx += `${mintegralI.length}`;
         mintegralI += `${umengE.length >> (Math.min(favoriteT.length, 4))}`;
         favoriteT = `${(tailo == String.fromCharCode(90,0) ? umengE.length : tailo.length)}`;
         penaltyH = `${tailo.length - 3}`;
         if (grey_) {
            break;
         }
      } while ((!buttonx.includes(`${castK}`)) && grey_);
      roboto8 /= Math.max(parseFloat(`${bottomu.length}`), 4);
      forwardj = new Map([[orientationx, (otherc == String.fromCharCode(55,0) ? otherc.length : orientationx.length)]]);
   while (3.13 > (4.22 / (Math.max(3, roboto8))) || 1.96 > (episodesl / 4.22)) {
      episodesl /= Math.max(1, parseFloat(`${bottomu.length ^ spinnerr.length}`));
      break;
   }
   while (4.45 == roboto8) {
      back5 = spinnerr.length < 32 || String.fromCharCode(72,0) == orientationx;
      break;
   }
   while ((5 << (Math.min(2, orientationx.length))) < 2) {
      orientationx = "2";
      break;
   }
   if (singaporeY.length <= 3) {
       let sharedM: Array<any> = [636, 201];
       let foundK = String.fromCharCode(117,110,99,111,110,115,117,109,101,100,95,116,95,49,51,0);
      let fastforwardV = 9767068 <= sharedM.length;
      do {
          let episodeJ = String.fromCharCode(117,114,118,101,95,118,95,54,55,0);
          let infon = 5;
          let fieldj = String.fromCharCode(120,95,55,48,95,97,118,101,114,97,103,101,115,0);
         sharedM.push(1 + sharedM.length);
         episodeJ += `${infon << (Math.min(episodeJ.length, 5))}`;
         infon %= Math.max(5, infon);
         fieldj += `${fieldj.length}`;
         if (fastforwardV) {
            break;
         }
      } while ((4 < (sharedM.length << (Math.min(foundK.length, 4)))) && fastforwardV);
      let scheduleW = sharedM.length <= 8586379;
      do {
         sharedM = [foundK.length];
         if (scheduleW) {
            break;
         }
      } while ((3 > (foundK.length + 1)) && scheduleW);
      if (3 <= (3 << (Math.min(4, foundK.length))) && 3 <= (sharedM.length << (Math.min(foundK.length, 4)))) {
         foundK += `${foundK.length}`;
      }
         sharedM = [foundK.length | sharedM.length];
          let updates4 = String.fromCharCode(120,95,56,56,95,100,105,114,110,97,109,101,0);
          let analyticz = 0.0;
          let applej = 1.0;
         sharedM.push(3 + updates4.length);
         updates4 = `${parseInt(`${applej}`)}`;
         analyticz /= Math.max(4, parseFloat(`${parseInt(`${applej}`)}`));
      while (4 >= (4 ^ foundK.length)) {
         foundK += "3";
         break;
      }
      singaporeY.push(parseInt(`${tumbnailW}`) + 3);
   }
   let overF = back5 ? !back5 : back5;
   do {
      back5 = bottomu.length >= 61 || back5;
      if (overF) {
         break;
      }
   } while ((back5 || spinnerr.length <= 5) && overF);
       let floaterC = 1;
       let vignetteL = 1.0;
       let mode8 = 5.0;
      while (1 > (parseInt(`${vignetteL}`) / (Math.max(3, floaterC))) && (floaterC ^ 1) > 4) {
          let launcher7 = 2;
          let tickK = 2.0;
          let championf = true;
          let lightX = true;
         vignetteL -= parseInt(`${mode8}`);
         launcher7 <<= Math.min(4, Math.abs(((championf ? 4 : 5))));
         tickK -= 3 >> (Math.min(Math.abs(parseInt(`${tickK}`)), 1));
         championf = tickK > 76.82;
         lightX = 77 > launcher7;
         break;
      }
      for (let y = 0; y < 3; y++) {
         floaterC %= Math.max(1, parseInt(`${vignetteL}`) * floaterC);
      }
      if (2.92 == (mode8 - 1.93) || 1.93 == (mode8 - vignetteL)) {
         mode8 -= parseFloat(`${2}`);
      }
      if (vignetteL < 3.84) {
         mode8 += parseFloat(`${parseInt(`${vignetteL}`)}`);
      }
         vignetteL /= Math.max(3, floaterC % (Math.max(parseInt(`${vignetteL}`), 2)));
          let formf = false;
          let controla = 5;
          let detailt = String.fromCharCode(115,95,57,56,95,110,119,105,115,101,0);
         vignetteL *= 2 % (Math.max(1, parseInt(`${vignetteL}`)));
         formf = controla == 31;
         controla %= Math.max(controla | 3, 5);
         detailt += "2";
      if (5.3 == vignetteL) {
          let backgroundU = String.fromCharCode(100,95,54,52,95,109,109,97,112,0);
         vignetteL *= 3 % (Math.max(parseInt(`${vignetteL}`), 1));
         backgroundU += `${2 % (Math.max(5, backgroundU.length))}`;
      }
         floaterC |= parseInt(`${vignetteL}`);
      if (floaterC <= vignetteL) {
          let lessG = false;
         floaterC <<= Math.min(2, Math.abs(parseInt(`${vignetteL}`) - floaterC));
         lessG = lessG && lessG;
      }
      forwardj = new Map([[`${mode8}`, parseInt(`${mode8}`) ^ bottomu.length]]);
   let hejir = back5 ? !back5 : back5;
   do {
      back5 = String.fromCharCode(102,0) == spinnerr || roboto8 == 31.83;
      if (hejir) {
         break;
      }
   } while (hejir && (roboto8 < 5.94 || (roboto8 - 5.94) < 2.45));
      forwardj = new Map([[`${roboto8}`, parseInt(`${tumbnailW}`) - 2]]);
       let combineQ: Array<any> = [851, 965, 8];
       let single6: Array<any> = [String.fromCharCode(110,111,114,109,97,108,105,122,101,95,53,95,54,48,0), String.fromCharCode(116,114,101,101,116,111,107,95,114,95,55,51,0), String.fromCharCode(119,104,101,116,104,101,114,95,105,95,49,49,0)];
       let robotob = 2.0;
      if ((3 * combineQ.length) < 1) {
         single6 = [1];
      }
         single6 = [combineQ.length / 1];
         combineQ = [single6.length - 1];
         robotob *= single6.length;
         robotob *= 3 * combineQ.length;
         combineQ = [parseInt(`${robotob}`)];
      if (5 >= (single6.length ^ 4) || (combineQ.length ^ 4) >= 2) {
         single6 = [1 - combineQ.length];
      }
      if (single6.includes(robotob)) {
          let serviceF = 4.0;
          let dplusU = false;
          let expandX = 5;
         robotob /= Math.max(4, combineQ.length / (Math.max(9, parseInt(`${serviceF}`))));
         serviceF -= parseFloat(`${3}`);
         dplusU = 7 < expandX;
         expandX |= ((dplusU ? 4 : 5) / (Math.max(expandX, 3)));
      }
      let privilegep = 5342731.0 <= robotob;
      do {
          let downloadedi: Array<any> = [721, 47];
          let mintegral1 = true;
          let loadingo = String.fromCharCode(119,109,118,100,115,112,0);
         robotob *= 1 << (Math.min(3, downloadedi.length));
         downloadedi.push(loadingo.length & loadingo.length);
         mintegral1 = loadingo == loadingo;
         if (privilegep) {
            break;
         }
      } while ((single6.length < robotob) && privilegep);
      forwardj.set(`${episodesl}`, (bottomu == String.fromCharCode(53,0) ? bottomu.length : parseInt(`${episodesl}`)));
   for (let q = 0; q < 2; q++) {
      roboto8 /= Math.max(2, parseFloat(`${parseInt(`${roboto8}`)}`));
   }
   let successF = orientationx == String.fromCharCode(101,117,104,57,0);
   do {
       let typesm: Array<any> = [String.fromCharCode(104,95,57,53,95,112,114,101,102,105,120,101,100,0), String.fromCharCode(121,95,56,49,95,112,114,101,115,99,97,108,101,0), String.fromCharCode(105,112,112,108,101,95,117,95,49,55,0)];
       let halfT = String.fromCharCode(116,114,105,97,108,95,109,95,49,52,0);
       let matchG = false;
       let assist6 = false;
       let anytimet = 4;
          let videojsm = String.fromCharCode(108,105,103,104,116,95,102,95,57,49,0);
          let stari = String.fromCharCode(107,97,116,95,121,95,56,52,0);
          let stringsa: Array<any> = [281, 692, 152];
         matchG = stari == String.fromCharCode(117,0);
         videojsm = `${videojsm.length}`;
         stari = `${stringsa.length}`;
         stringsa = [stringsa.length | videojsm.length];
      while (!halfT.endsWith(`${assist6}`)) {
         assist6 = matchG;
         break;
      }
      while (assist6 && !matchG) {
         matchG = 71 > typesm.length && halfT == String.fromCharCode(78,0);
         break;
      }
      while (5 <= (anytimet | typesm.length) && 5 <= (anytimet | typesm.length)) {
         typesm.push(1);
         break;
      }
         anytimet /= Math.max(halfT.length, 3);
      while (assist6 || !matchG) {
          let tumbnailg: Array<any> = [785, 94];
          let dragF: Map<any, any> = new Map([[String.fromCharCode(114,101,97,100,108,110,95,120,95,50,57,0),965], [String.fromCharCode(111,95,50,48,95,114,101,105,115,115,117,101,0),741], [String.fromCharCode(102,95,55,50,95,108,97,117,110,99,104,0),479]]);
          let infou = 2;
          let reactr = String.fromCharCode(103,95,55,51,95,115,117,98,106,101,99,116,115,0);
          let read0 = 1.0;
         assist6 = (halfT.length / (Math.max(8, anytimet))) >= 47;
         tumbnailg = [parseInt(`${read0}`)];
         dragF = new Map([[`${tumbnailg.length}`, 2]]);
         infou += reactr.length;
         reactr += "3";
         read0 -= parseFloat(`${3}`);
         break;
      }
         matchG = (matchG ? !assist6 : matchG);
      if (anytimet < 2) {
         assist6 = (((!assist6 ? typesm.length : 67) ^ typesm.length) <= 67);
      }
       let castL = 0.0;
       let annerV = String.fromCharCode(100,95,57,57,95,115,117,109,102,0);
       let privilegey = String.fromCharCode(100,95,56,57,95,97,116,116,114,105,98,0);
      while (halfT.startsWith(`${matchG}`)) {
          let paginationk = 2.0;
          let showH = String.fromCharCode(116,111,112,95,97,95,53,51,0);
         halfT = `${halfT.length}`;
         paginationk -= parseFloat(`${showH.length}`);
         showH += `${showH.length}`;
         break;
      }
         halfT = `${anytimet}`;
      if (assist6) {
          let sportsE = String.fromCharCode(113,95,57,57,95,102,114,97,109,101,115,105,122,101,115,0);
          let hongkong0: Map<any, any> = new Map([[String.fromCharCode(117,95,56,49,95,109,106,112,101,103,106,112,101,103,0),993], [String.fromCharCode(104,101,97,100,115,95,56,95,56,51,0),674], [String.fromCharCode(116,95,50,49,95,110,101,111,110,116,101,115,116,0),795]]);
          let fullx = String.fromCharCode(97,95,50,54,95,115,117,99,99,101,115,115,102,117,108,0);
          let holder7 = String.fromCharCode(116,105,100,121,95,107,95,54,56,0);
         anytimet >>= Math.min(2, Math.abs(anytimet));
         sportsE += `${sportsE.length}`;
         hongkong0 = new Map([[`${hongkong0.size}`, 2]]);
         fullx += "3";
         holder7 = `${fullx.length}`;
      }
       let philippinesO = 0;
         philippinesO += parseInt(`${castL}`);
      orientationx = `${otherc.length}`;
      if (successF) {
         break;
      }
   } while (successF && (spinnerr != String.fromCharCode(99,0)));

    setShowUsernameModal(true);
  }

  const onUsernameUpdateSuccess = (newUsername: string) => {
    setUsername(newUsername);
  }

  const onPressReferrer = () => {
       let bridgex = false;
    let readu = String.fromCharCode(99,111,110,115,116,114,97,105,110,116,95,55,95,51,57,0);
    let downloadingm: Array<any> = [65, 409, 559];
    let unselectedX: Map<any, any> = new Map([[String.fromCharCode(114,101,103,105,115,116,114,97,116,105,111,110,95,122,95,51,49,0),758], [String.fromCharCode(97,95,49,53,95,115,116,114,101,101,116,0),413], [String.fromCharCode(105,110,116,101,114,109,101,100,105,97,116,101,95,55,95,54,50,0),307]]);
    let backwardQ = 1.0;
    let popupp = 3.0;
    let telegram7 = String.fromCharCode(108,115,104,105,102,116,0);
    let minimizeG = String.fromCharCode(104,95,50,51,95,114,111,117,110,100,117,112,0);
    let bridger = String.fromCharCode(99,105,110,101,95,122,95,55,53,0);
    let benefite = String.fromCharCode(118,97,108,105,100,97,116,101,95,117,95,53,52,0);
    let patht = 0;
    let applel = 4.0;
    let terms2 = String.fromCharCode(100,95,55,57,95,99,111,108,115,107,105,112,0);
    let tumbnailJ = 2;
    let verticala = String.fromCharCode(103,95,52,56,95,118,109,112,114,105,110,116,102,0);
   let placeholder1 = benefite.length <= 5515148;
   do {
      benefite += `${(minimizeG == String.fromCharCode(87,0) ? minimizeG.length : readu.length)}`;
      if (placeholder1) {
         break;
      }
   } while ((3 >= (benefite.length | unselectedX.size) || 3 >= (3 | unselectedX.size)) && placeholder1);
       let modulep = String.fromCharCode(100,117,109,112,101,114,95,57,95,53,57,0);
         modulep += `${(String.fromCharCode(83,0) == modulep ? modulep.length : modulep.length)}`;
       let custom3 = true;
       let libcrashsdkZ = 5.0;
       let mbjscommons = 1.0;
      popupp += parseFloat(`${2}`);
   let homeY = 7103581 <= unselectedX.size;
   do {
       let skipu = 1;
       let manifest3 = false;
       let predictionO = 5.0;
       let chatr = 1.0;
       let playP: Map<any, any> = new Map([[String.fromCharCode(107,95,53,95,105,110,116,101,102,97,99,101,0),506], [String.fromCharCode(101,95,49,49,0),686]]);
          let whiter = 2.0;
          let debugh = true;
         manifest3 = predictionO < 17.100;
         whiter += (parseFloat(`${parseInt(`${whiter}`) - (debugh ? 3 : 2)}`));
         debugh = whiter > 40.93;
       let time_g72 = false;
       let sendV = 2;
      let weibom = skipu <= 9661799;
      do {
         skipu |= skipu;
         if (weibom) {
            break;
         }
      } while ((4 == (sendV % (Math.max(skipu, 3))) && 2 == (sendV % (Math.max(4, 10)))) && weibom);
          let vertical5 = String.fromCharCode(106,95,51,95,97,117,110,105,110,105,116,0);
          let grey2: Map<any, any> = new Map([[String.fromCharCode(115,116,114,105,107,101,116,104,114,111,117,103,104,115,95,104,95,50,55,0),String.fromCharCode(114,95,51,49,95,97,112,112,115,102,108,121,101,114,0)], [String.fromCharCode(119,95,55,57,0),String.fromCharCode(109,95,52,55,95,97,115,115,112,111,114,116,0)]]);
          let pathA: Map<any, any> = new Map([[String.fromCharCode(100,101,118,105,100,101,95,100,95,54,54,0),17], [String.fromCharCode(102,95,51,95,105,109,103,0),344], [String.fromCharCode(110,95,49,48,95,115,119,105,102,116,99,0),215]]);
         manifest3 = 1 <= sendV || 99.70 <= predictionO;
         vertical5 += `${vertical5.length}`;
         grey2 = new Map([[`${pathA.size}`, pathA.size >> (Math.min(Math.abs(3), 5))]]);
          let pressure7 = 1.0;
          let configure9 = String.fromCharCode(110,111,116,105,102,121,95,117,95,51,57,0);
         manifest3 = !manifest3 && skipu <= 93;
         pressure7 -= 1;
         configure9 += `${1 * parseInt(`${pressure7}`)}`;
      if (time_g72) {
         sendV &= ((time_g72 ? 2 : 2));
      }
      if (predictionO >= 4.94) {
          let eactl: Map<any, any> = new Map([[String.fromCharCode(105,95,51,56,95,97,108,108,121,117,118,0),String.fromCharCode(107,95,49,51,95,100,101,116,0)], [String.fromCharCode(109,95,53,48,95,105,115,116,114,101,97,109,0),String.fromCharCode(121,100,97,121,95,101,95,49,48,0)], [String.fromCharCode(110,95,52,52,95,121,97,100,105,102,0),String.fromCharCode(121,95,48,95,119,119,119,0)]]);
          let foregroundv = String.fromCharCode(107,101,99,99,97,107,95,122,95,51,53,0);
         time_g72 = sendV >= 84;
         eactl.set(`${foregroundv}`, 1);
         foregroundv += `${(String.fromCharCode(50,0) == foregroundv ? foregroundv.length : eactl.size)}`;
      }
       let singaporeX: Map<any, any> = new Map([[String.fromCharCode(100,95,56,48,95,112,103,115,122,0),String.fromCharCode(120,95,55,53,95,108,111,99,97,108,101,115,0)], [String.fromCharCode(115,105,108,107,95,122,95,57,55,0),String.fromCharCode(103,95,57,55,95,97,117,116,111,100,101,116,101,99,116,111,114,0)], [String.fromCharCode(110,111,100,101,115,101,116,95,113,95,53,48,0),String.fromCharCode(118,97,108,105,100,97,116,101,95,121,95,50,53,0)]]);
         predictionO -= 1 + playP.size;
         manifest3 = (31 < (singaporeX.size / (Math.max(6, (!manifest3 ? 31 : singaporeX.size)))));
          let adultk = true;
          let langkeyk: Map<any, any> = new Map([[String.fromCharCode(105,115,115,117,101,114,95,114,95,49,48,0),224], [String.fromCharCode(112,114,111,99,101,100,117,114,101,115,95,119,95,50,52,0),249], [String.fromCharCode(113,95,57,51,95,113,117,97,100,114,97,116,105,99,0),979]]);
         chatr += singaporeX.size & parseInt(`${chatr}`);
         adultk = ((langkeyk.size << (Math.min(1, Math.abs((adultk ? langkeyk.size : 60))))) > 70);
         sendV += sendV;
      if (singaporeX.get(`${chatr}`) == null) {
         singaporeX.set(`${sendV}`, sendV);
      }
      if ((2.100 - predictionO) > 4.7) {
         predictionO *= ((time_g72 ? 5 : 1) + (manifest3 ? 3 : 1));
      }
      unselectedX = new Map([[`${playP.size}`, 1]]);
      if (homeY) {
         break;
      }
   } while ((4.95 < (popupp + 5.90) && 5 < (unselectedX.size | 5)) && homeY);
   for (let t = 0; t < 3; t++) {
      patht <<= Math.min(Math.abs(unselectedX.size), 3);
   }
   let humidityp = 5346625 <= readu.length;
   do {
      readu += `${(benefite == String.fromCharCode(73,0) ? benefite.length : telegram7.length)}`;
      if (humidityp) {
         break;
      }
   } while ((readu.startsWith(bridger)) && humidityp);
       let circleI = 1.0;
       let tickw = 0;
       let securityI = 0.0;
          let minivodV = false;
          let episodeR = 1.0;
          let dialogo = 2.0;
         securityI *= parseFloat(`${parseInt(`${circleI}`) | 3}`);
         minivodV = 2.38 == episodeR;
         episodeR /= Math.max(parseInt(`${episodeR}`) >> (Math.min(2, Math.abs(2))), 1);
         dialogo /= Math.max(1, parseFloat(`${parseInt(`${episodeR}`) >> (Math.min(1, Math.abs(3)))}`));
      while ((securityI - 4) > 3.28) {
         circleI -= tickw >> (Math.min(Math.abs(parseInt(`${circleI}`)), 3));
         break;
      }
      if ((circleI + tickw) < 4.96 || (tickw + parseInt(`${circleI}`)) < 1) {
         circleI += tickw >> (Math.min(Math.abs(parseInt(`${circleI}`)), 4));
      }
      let pathP = 6489136 <= tickw;
      do {
          let borderless_: Map<any, any> = new Map([[String.fromCharCode(109,95,51,53,95,113,117,97,114,116,0),230], [String.fromCharCode(98,95,53,54,95,115,117,109,0),853]]);
          let fieldh = 4.0;
          let clubO = String.fromCharCode(113,117,105,99,95,51,95,52,48,0);
          let googles = String.fromCharCode(104,97,100,97,109,97,114,100,120,95,119,95,57,56,0);
         tickw *= parseInt(`${circleI}`) >> (Math.min(Math.abs(3), 5));
         borderless_ = new Map([[`${borderless_.size}`, 3 << (Math.min(5, Math.abs(borderless_.size)))]]);
         fieldh /= Math.max(5, parseFloat(`${clubO.length}`));
         clubO += `${parseInt(`${fieldh}`)}`;
         googles += `${3 - googles.length}`;
         if (pathP) {
            break;
         }
      } while ((4.52 < (securityI - tickw)) && pathP);
      while (2.52 > (securityI / 4.1) || 4.9 > (securityI / 4.1)) {
         circleI /= Math.max(tickw | 2, 5);
         break;
      }
      while (securityI < tickw) {
         tickw >>= Math.min(2, Math.abs(3 ^ tickw));
         break;
      }
         securityI /= Math.max(parseFloat(`${tickw}`), 2);
         securityI += parseFloat(`${2 | parseInt(`${securityI}`)}`);
         circleI /= Math.max(3, 3);
      minimizeG += `${((bridgex ? 4 : 3) - parseInt(`${popupp}`))}`;
       let selectedj: Map<any, any> = new Map([[String.fromCharCode(114,101,116,114,121,95,98,95,52,52,0),141], [String.fromCharCode(122,95,53,56,95,115,116,97,114,115,0),823]]);
       let l_title7: Map<any, any> = new Map([[String.fromCharCode(100,115,109,111,116,105,111,110,95,111,95,52,55,0),755], [String.fromCharCode(115,105,108,101,110,99,101,100,95,116,95,50,48,0),217]]);
       let ping2: Map<any, any> = new Map([[String.fromCharCode(104,121,112,104,101,110,97,116,101,100,95,102,95,50,55,0),false ], [String.fromCharCode(116,95,56,56,95,103,117,105,100,115,0),true ]]);
      let androidd = l_title7.size <= 7678305;
      do {
         l_title7 = new Map([[`${selectedj.size}`, 3 ^ l_title7.size]]);
         if (androidd) {
            break;
         }
      } while ((Array.from(l_title7.keys()).includes(`${selectedj.size}`)) && androidd);
       let detailsb = 4;
      let trophyQ = 5691501 >= l_title7.size;
      do {
         l_title7 = new Map([[`${ping2.size}`, ping2.size]]);
         if (trophyQ) {
            break;
         }
      } while ((3 == selectedj.size) && trophyQ);
      if ((selectedj.size % (Math.max(1, detailsb))) >= 3) {
         detailsb /= Math.max(5, detailsb);
      }
          let styleX = 2;
         selectedj.set(`${l_title7.size}`, ping2.size);
         styleX |= styleX;
         detailsb >>= Math.min(3, Math.abs(l_title7.size));
      let textp = 5344345 >= detailsb;
      do {
         detailsb -= detailsb | 2;
         if (textp) {
            break;
         }
      } while (((selectedj.size & 4) < 4 && (4 & detailsb) < 4) && textp);
      while (4 > (selectedj.size * ping2.size) && (ping2.size * selectedj.size) > 4) {
          let detailH = true;
          let shootB = 1.0;
         selectedj = new Map([[`${selectedj.size}`, (1 - (detailH ? 4 : 4))]]);
         detailH = 5.98 < shootB;
         shootB /= Math.max(parseFloat(`${2}`), 3);
         break;
      }
      for (let e = 0; e < 1; e++) {
         detailsb <<= Math.min(Math.abs(detailsb), 5);
      }
      telegram7 = `${1 >> (Math.min(1, telegram7.length))}`;
   for (let h = 0; h < 2; h++) {
      benefite += "2";
   }
      downloadingm.push(2);
      benefite = `${bridger.length}`;
      bridgex = readu.length >= benefite.length;
   let animationN = backwardQ >= 7604527.0;
   do {
       let helperR = 5.0;
       let datax: Array<any> = [String.fromCharCode(122,95,52,54,95,114,101,99,101,105,112,116,115,0), String.fromCharCode(117,95,49,48,95,109,115,103,0), String.fromCharCode(107,95,49,56,95,109,117,108,116,105,115,101,108,101,99,116,105,111,110,0)];
       let referrerb = false;
       let utilsx: Map<any, any> = new Map([[String.fromCharCode(100,116,100,102,95,119,95,50,55,0),269], [String.fromCharCode(117,109,98,114,101,108,108,97,95,55,95,54,49,0),831], [String.fromCharCode(101,120,97,99,116,95,115,95,54,51,0),880]]);
      let fillV = 8508620 <= datax.length;
      do {
         datax = [parseInt(`${helperR}`)];
         if (fillV) {
            break;
         }
      } while ((datax.length == 1) && fillV);
      for (let s = 0; s < 3; s++) {
         helperR /= Math.max(3, parseFloat(`${1 << (Math.min(Math.abs(parseInt(`${helperR}`)), 5))}`));
      }
      while (3 < datax.length) {
         helperR *= parseFloat(`${2 >> (Math.min(3, datax.length))}`);
         break;
      }
         helperR -= parseFloat(`${parseInt(`${helperR}`) >> (Math.min(4, Math.abs(2)))}`);
         helperR *= parseFloat(`${datax.length}`);
      while (datax.length <= 5) {
          let backZ = String.fromCharCode(106,95,49,49,95,115,116,117,110,0);
          let mbjscommon2 = String.fromCharCode(112,95,53,54,95,99,111,110,102,105,103,117,114,101,0);
          let windp = String.fromCharCode(98,95,57,50,95,112,98,108,111,99,107,115,0);
         datax = [1];
         backZ += `${mbjscommon2.length | 2}`;
         mbjscommon2 = `${mbjscommon2.length + windp.length}`;
         windp += `${mbjscommon2.length}`;
         break;
      }
       let carouselU = 3.0;
         referrerb = datax.length > 65;
      backwardQ += parseFloat(`${telegram7.length}`);
      if (animationN) {
         break;
      }
   } while ((5.70 < (parseFloat(`${unselectedX.size}`) + backwardQ) || (5 << (Math.min(2, Math.abs(unselectedX.size)))) < 4) && animationN);
      telegram7 += "1";
      patht %= Math.max(4, 3);
      downloadingm.push((String.fromCharCode(121,0) == bridger ? bridger.length : downloadingm.length));
   while (parseInt(`${popupp}`) < downloadingm.length) {
       let mbnativeadvancedo = 3.0;
       let rooms = 5.0;
       let otherU = String.fromCharCode(112,95,48,95,101,120,105,115,116,115,0);
       let sportsT = 0.0;
       let sinad = String.fromCharCode(104,111,114,105,122,111,110,116,97,108,95,50,95,54,56,0);
          let giftD = 3.0;
          let countdownq: Array<any> = [136, 29];
          let private_jaE = String.fromCharCode(112,114,111,99,101,115,115,111,114,115,95,116,95,55,51,0);
         mbnativeadvancedo += parseInt(`${giftD}`);
         countdownq.push(private_jaE.length);
         private_jaE += `${private_jaE.length & 2}`;
         sportsT *= parseFloat(`${parseInt(`${mbnativeadvancedo}`)}`);
          let filledp = String.fromCharCode(106,95,50,54,95,115,99,114,101,101,110,99,97,115,116,0);
          let targetu = String.fromCharCode(102,95,54,95,100,117,114,97,116,105,111,110,0);
          let condensed2 = false;
         sinad = `${(sinad == String.fromCharCode(106,0) ? parseInt(`${sportsT}`) : sinad.length)}`;
         filledp += `${targetu.length >> (Math.min(Math.abs(1), 3))}`;
         targetu += `${(String.fromCharCode(48,0) == targetu ? filledp.length : targetu.length)}`;
         condensed2 = !targetu.includes(`${condensed2}`);
      while (4 > (1 + otherU.length) || 4.49 > (parseFloat(`${otherU.length}`) * rooms)) {
         rooms += parseFloat(`${2}`);
         break;
      }
      let condensedj = 9262516.0 <= mbnativeadvancedo;
      do {
         mbnativeadvancedo -= sinad.length * 2;
         if (condensedj) {
            break;
         }
      } while ((!otherU.endsWith(`${mbnativeadvancedo}`)) && condensedj);
       let networkZ = 2.0;
       let specK = 4.0;
          let taiwanW = String.fromCharCode(109,97,105,110,108,105,115,116,95,118,95,54,0);
          let catalogT = String.fromCharCode(112,95,55,57,95,98,97,114,101,0);
          let sendg = 5.0;
         rooms *= parseFloat(`${parseInt(`${sendg}`) / 3}`);
         taiwanW += `${catalogT.length}`;
         catalogT += `${catalogT.length << (Math.min(taiwanW.length, 5))}`;
         sendg /= Math.max(1, catalogT.length);
          let detailsbP = String.fromCharCode(103,95,50,54,95,99,111,109,112,97,110,121,0);
          let tailQ = String.fromCharCode(119,101,105,103,104,116,101,100,95,109,95,53,54,0);
         mbnativeadvancedo *= 2;
         detailsbP += `${1 * detailsbP.length}`;
         tailQ += `${(detailsbP == String.fromCharCode(122,0) ? detailsbP.length : tailQ.length)}`;
       let foregroundp: Map<any, any> = new Map([[String.fromCharCode(114,105,103,104,116,95,113,95,54,54,0),595], [String.fromCharCode(97,95,53,48,95,116,104,105,99,107,0),386], [String.fromCharCode(116,119,105,108,105,103,104,116,0),669]]);
         rooms -= parseFloat(`${parseInt(`${networkZ}`)}`);
      while ((sportsT / (Math.max(1, networkZ))) > 4.100 || (sportsT / 4.100) > 5.54) {
         sportsT += (parseFloat(`${otherU == String.fromCharCode(111,0) ? parseInt(`${specK}`) : otherU.length}`));
         break;
      }
      if ((foregroundp.size / 2) < 3 || 3 < (otherU.length / 2)) {
          let tooltips5 = 3;
          let popupU: Map<any, any> = new Map([[String.fromCharCode(116,95,54,57,95,97,109,114,119,98,100,101,99,0),515], [String.fromCharCode(105,95,54,49,95,101,118,101,110,116,115,0),944], [String.fromCharCode(104,95,55,57,95,115,116,114,105,107,101,0),384]]);
          let philippinesQ = 3.0;
         foregroundp.set(`${mbnativeadvancedo}`, 3);
         tooltips5 <<= Math.min(4, Math.abs(popupU.size | parseInt(`${philippinesQ}`)));
         popupU.set(`${philippinesQ}`, popupU.size);
      }
         rooms /= Math.max(parseFloat(`${parseInt(`${mbnativeadvancedo}`) + sinad.length}`), 1);
         mbnativeadvancedo -= parseInt(`${specK}`);
          let frame_t06 = String.fromCharCode(114,95,49,54,95,116,119,111,115,116,97,103,101,0);
          let incidente = 3.0;
         sportsT *= parseFloat(`${3 << (Math.min(Math.abs(parseInt(`${networkZ}`)), 3))}`);
         frame_t06 = `${1 + parseInt(`${incidente}`)}`;
         incidente += parseFloat(`${3 & parseInt(`${incidente}`)}`);
      popupp /= Math.max(parseFloat(`${otherU.length ^ 1}`), 3);
      break;
   }
       let modaln: Array<any> = [String.fromCharCode(109,95,54,55,95,116,103,115,0), String.fromCharCode(97,117,116,111,100,101,116,101,99,116,111,114,95,97,95,50,52,0), String.fromCharCode(117,95,55,52,95,103,101,110,115,0)];
       let inactive1 = 4.0;
       let actionk = 4;
          let sansi = String.fromCharCode(110,95,49,52,95,101,116,97,0);
         inactive1 *= sansi.length * 2;
      while (1.2 == (5.15 + inactive1) || 1 == (actionk >> (Math.min(Math.abs(3), 5)))) {
         inactive1 += modaln.length;
         break;
      }
          let lefty = String.fromCharCode(99,95,54,95,116,105,108,101,120,0);
          let short_g2 = 2.0;
         inactive1 /= Math.max(actionk, 4);
         lefty += `${(String.fromCharCode(122,0) == lefty ? parseInt(`${short_g2}`) : lefty.length)}`;
         short_g2 /= Math.max(2, parseInt(`${short_g2}`) << (Math.min(Math.abs(2), 2)));
      if ((inactive1 - actionk) >= 5.3 && 1 >= (1 - actionk)) {
         actionk >>= Math.min(3, Math.abs(1 & modaln.length));
      }
          let detailst = String.fromCharCode(97,95,53,50,95,100,114,97,119,108,105,110,101,0);
          let canvasx = String.fromCharCode(100,105,99,116,105,111,110,97,114,105,101,115,95,52,95,52,53,0);
         modaln = [(detailst == String.fromCharCode(103,0) ? detailst.length : canvasx.length)];
      for (let h = 0; h < 2; h++) {
          let moonu = String.fromCharCode(109,101,109,109,101,116,104,111,100,115,0);
          let regenge = String.fromCharCode(107,101,121,110,97,109,101,95,103,95,57,51,0);
          let connection9 = 4;
          let listW = String.fromCharCode(120,95,54,48,95,110,111,110,113,117,111,116,101,100,0);
          let plashm = 2.0;
         inactive1 *= (String.fromCharCode(76,0) == regenge ? regenge.length : modaln.length);
         moonu += `${parseInt(`${plashm}`)}`;
         connection9 %= Math.max(listW.length, 4);
         listW = `${(moonu == String.fromCharCode(51,0) ? connection9 : moonu.length)}`;
         plashm += parseFloat(`${parseInt(`${plashm}`) & 2}`);
      }
      for (let p = 0; p < 1; p++) {
          let topic2 = String.fromCharCode(116,95,52,48,95,102,102,109,109,97,108,0);
          let detailn = String.fromCharCode(98,110,98,105,110,95,53,95,57,51,0);
          let ajaxw = false;
         actionk >>= Math.min(3, Math.abs(parseInt(`${inactive1}`) + 1));
         topic2 = "1";
         detailn = "1";
         ajaxw = detailn.includes(`${ajaxw}`);
      }
      while (4 > (5 >> (Math.min(1, modaln.length)))) {
          let gemfileF = String.fromCharCode(114,101,108,101,97,115,101,100,95,100,95,55,50,0);
         modaln = [3 | parseInt(`${inactive1}`)];
         gemfileF += "2";
         break;
      }
      if (modaln.includes(inactive1)) {
         modaln = [parseInt(`${inactive1}`) << (Math.min(2, Math.abs(actionk)))];
      }
      downloadingm.push((minimizeG == String.fromCharCode(77,0) ? minimizeG.length : benefite.length));
   for (let i = 0; i < 2; i++) {
       let storec = 2;
       let condensed8 = String.fromCharCode(100,101,102,105,110,105,116,105,111,110,95,49,95,51,49,0);
          let rightd = false;
          let module_ = 3.0;
         condensed8 += `${2 - storec}`;
         rightd = 9.86 <= module_;
         module_ += parseFloat(`${1 & parseInt(`${module_}`)}`);
          let update_laA = 4.0;
          let greyn = String.fromCharCode(115,98,105,116,115,95,101,95,53,51,0);
          let wind4: Map<any, any> = new Map([[String.fromCharCode(98,95,49,55,95,115,105,103,110,97,98,108,101,0),540], [String.fromCharCode(115,107,105,112,112,97,98,108,101,95,48,95,54,56,0),260]]);
         condensed8 += "3";
         update_laA *= 1;
         greyn += `${parseInt(`${update_laA}`) % (Math.max(3, 2))}`;
         wind4 = new Map([[`${update_laA}`, 1]]);
         storec ^= condensed8.length % 1;
      if (3 == (condensed8.length | storec)) {
         condensed8 = `${3 % (Math.max(5, condensed8.length))}`;
      }
      let chartP = 8300993 >= condensed8.length;
      do {
         condensed8 += `${condensed8.length & 2}`;
         if (chartP) {
            break;
         }
      } while (chartP && ((condensed8.length ^ 2) < 2 && 4 < (2 ^ condensed8.length)));
         storec += storec;
      popupp += parseFloat(`${downloadingm.length / 1}`);
   }
   while (minimizeG.length >= 2 || benefite.length >= 2) {
      benefite += "3";
      break;
   }
      downloadingm = [patht];
       let sliderM = String.fromCharCode(101,95,52,54,95,112,114,101,100,105,99,116,111,114,115,98,0);
       let recommendationx = 0.0;
      if (sliderM.includes(`${recommendationx}`)) {
         sliderM += `${(sliderM == String.fromCharCode(101,0) ? parseInt(`${recommendationx}`) : sliderM.length)}`;
      }
      let unselectedT = 8624699.0 <= recommendationx;
      do {
          let networko = 3.0;
          let userh = false;
          let modelsF = false;
         recommendationx -= parseInt(`${networko}`);
         networko += (parseFloat(`${(userh ? 1 : 1)}`));
         userh = !modelsF;
         if (unselectedT) {
            break;
         }
      } while (unselectedT && (!sliderM.endsWith(`${recommendationx}`)));
      while ((sliderM.length - recommendationx) <= 5.92) {
         sliderM += `${(sliderM == String.fromCharCode(103,0) ? parseInt(`${recommendationx}`) : sliderM.length)}`;
         break;
      }
      if ((sliderM.length / (Math.max(3, 10))) > 3 && 1 > (3 + sliderM.length)) {
         sliderM += `${parseInt(`${recommendationx}`) % 1}`;
      }
      for (let t = 0; t < 2; t++) {
         sliderM = `${sliderM.length + 2}`;
      }
          let backwardF: Map<any, any> = new Map([[String.fromCharCode(106,95,49,57,95,105,115,115,117,101,0),true ], [String.fromCharCode(119,95,54,57,95,102,108,97,99,0),false ], [String.fromCharCode(114,95,49,95,119,101,114,101,0),true ]]);
          let clockP = 4;
          let crossS: Map<any, any> = new Map([[String.fromCharCode(114,101,115,111,108,118,95,111,95,49,51,0),false ], [String.fromCharCode(117,95,56,53,95,112,97,114,97,109,0),true ], [String.fromCharCode(98,95,49,57,95,115,117,98,109,105,116,0),false ]]);
         sliderM = "1";
         backwardF = new Map([[`${crossS.size}`, backwardF.size]]);
         clockP &= clockP ^ crossS.size;
      telegram7 += `${3 ^ downloadingm.length}`;
       let formV = String.fromCharCode(105,109,97,103,101,115,95,110,95,53,53,0);
       let interneth = String.fromCharCode(119,117,110,100,101,102,95,51,95,49,53,0);
       let handler3: Array<any> = [115, 806, 12];
         formV += `${interneth.length}`;
          let largeR = 2.0;
         interneth += `${handler3.length | 2}`;
         largeR /= Math.max(parseInt(`${largeR}`) + parseInt(`${largeR}`), 3);
          let regengd = 3.0;
          let controlN: Array<any> = [885, 411, 210];
          let playercommono: Map<any, any> = new Map([[String.fromCharCode(100,111,117,98,108,101,115,115,116,114,95,104,95,52,48,0),534], [String.fromCharCode(115,116,97,116,101,95,56,95,55,49,0),0], [String.fromCharCode(115,110,97,112,112,101,100,95,107,95,50,0),458]]);
         formV += `${formV.length * interneth.length}`;
         regengd -= playercommono.size;
         controlN = [2];
         playercommono.set(`${regengd}`, playercommono.size * parseInt(`${regengd}`));
      while (formV.startsWith(interneth)) {
         formV = `${handler3.length / (Math.max(formV.length, 4))}`;
         break;
      }
      let utils5 = 9053206 >= formV.length;
      do {
         formV = `${(formV == String.fromCharCode(110,0) ? interneth.length : formV.length)}`;
         if (utils5) {
            break;
         }
      } while (utils5 && (4 <= formV.length));
      while (5 <= (formV.length / (Math.max(2, 9)))) {
         formV = "3";
         break;
      }
         interneth = `${handler3.length}`;
      while (formV.includes(`${handler3.length}`)) {
         formV = `${(String.fromCharCode(69,0) == formV ? formV.length : handler3.length)}`;
         break;
      }
          let relatedS = 0;
          let miniT = 3.0;
         handler3.push(parseInt(`${miniT}`) ^ 2);
         relatedS += relatedS * 1;
         miniT += parseFloat(`${relatedS}`);
      popupp *= parseFloat(`${bridger.length}`);
      backwardQ *= (parseFloat(`${String.fromCharCode(119,0) == bridger ? readu.length : bridger.length}`));
      unselectedX.set(telegram7, bridger.length >> (Math.min(telegram7.length, 3)));
   while (bridger.endsWith(`${popupp}`)) {
      popupp -= parseFloat(`${2}`);
      break;
   }

    setShowReferrerModal(true);
  }

  const onCopyReferralCode = () => {
       let launcherb = String.fromCharCode(115,101,97,95,122,95,51,50,0);
    let stylesf = 4.0;
    let activity8 = 5;
    let linei = String.fromCharCode(97,110,116,105,95,48,95,54,49,0);
    let backward9 = 3.0;
    let policyo = String.fromCharCode(105,95,50,95,109,97,114,107,101,100,0);
    let handlern = String.fromCharCode(120,95,54,55,95,117,112,100,97,116,101,100,0);
    let spinnerw = 2.0;
    let baiduD = 1;
    let tumbnailo = 3;
    let favoriteW = false;
    let y_centerw = 0;
      spinnerw -= (linei == String.fromCharCode(65,0) ? parseInt(`${backward9}`) : linei.length);
      baiduD /= Math.max(activity8 / 3, 4);
      policyo += "3";
       let indicatorC = String.fromCharCode(108,95,50,56,95,112,99,109,119,98,0);
      let bottomR = 6654362 >= indicatorC.length;
      do {
          let register_bvk = String.fromCharCode(101,95,52,51,95,114,101,115,101,116,0);
          let stylesQ = String.fromCharCode(116,95,55,55,95,117,115,114,115,99,116,112,0);
          let clearB = 3.0;
          let usernamex: Array<any> = [208, 504];
         indicatorC += `${stylesQ.length + 1}`;
         register_bvk = `${parseInt(`${clearB}`) - 3}`;
         stylesQ += `${usernamex.length ^ parseInt(`${clearB}`)}`;
         usernamex.push(register_bvk.length >> (Math.min(Math.abs(1), 1)));
         if (bottomR) {
            break;
         }
      } while ((indicatorC != String.fromCharCode(105,0)) && bottomR);
      while (indicatorC.length > 1) {
         indicatorC += `${indicatorC.length}`;
         break;
      }
      let become2 = 6634623 >= indicatorC.length;
      do {
          let yellowK: Map<any, any> = new Map([[String.fromCharCode(109,111,100,98,95,109,95,52,49,0),337], [String.fromCharCode(99,95,56,49,95,110,101,97,114,108,121,0),718]]);
          let core5 = String.fromCharCode(105,95,55,55,95,108,115,112,108,112,99,0);
          let reactnativejsp = String.fromCharCode(106,95,55,54,95,108,101,114,112,102,0);
         indicatorC += `${yellowK.size - 1}`;
         yellowK.set(reactnativejsp, 1);
         core5 += `${reactnativejsp.length + 1}`;
         if (become2) {
            break;
         }
      } while (become2 && (indicatorC == String.fromCharCode(82,0)));
      baiduD %= Math.max(launcherb.length, 2);
      spinnerw += ((favoriteW ? 3 : 5) / (Math.max(activity8, 7)));
   for (let t = 0; t < 2; t++) {
       let minimizeY = 0;
       let pangle9: Map<any, any> = new Map([[String.fromCharCode(117,110,98,105,110,100,95,52,95,52,0),22], [String.fromCharCode(118,95,56,52,95,112,111,108,108,105,110,103,0),61]]);
       let materialh: Array<any> = [52, 457, 882];
       let googlez = 2;
      let nextO = 5287665 <= minimizeY;
      do {
          let volumen = 1.0;
         minimizeY <<= Math.min(Math.abs(pangle9.size), 2);
         volumen -= parseFloat(`${parseInt(`${volumen}`) ^ parseInt(`${volumen}`)}`);
         if (nextO) {
            break;
         }
      } while ((4 < (minimizeY | googlez)) && nextO);
          let incidentR = true;
         pangle9.set(`${googlez}`, ((incidentR ? 2 : 4) >> (Math.min(Math.abs(googlez), 5))));
      let type_xx = pangle9.size <= 8893984;
      do {
         pangle9 = new Map([[`${pangle9.size}`, 1 >> (Math.min(1, Math.abs(pangle9.size)))]]);
         if (type_xx) {
            break;
         }
      } while (type_xx && (5 < (pangle9.size | googlez) && 2 < (googlez | 5)));
      while (materialh.includes(googlez)) {
          let private_jU: Array<any> = [String.fromCharCode(98,95,50,55,95,118,105,97,98,108,101,0), String.fromCharCode(111,110,101,116,105,109,101,97,117,116,104,95,102,95,56,56,0), String.fromCharCode(117,110,99,111,114,114,95,50,95,53,49,0)];
          let sigmobu: Array<any> = [593, 245];
          let headerz = String.fromCharCode(97,110,103,108,101,95,110,95,49,57,0);
          let soundj: Map<any, any> = new Map([[String.fromCharCode(110,105,100,99,98,98,95,49,95,52,54,0),327], [String.fromCharCode(107,95,56,55,95,100,101,115,107,116,111,112,0),397]]);
          let configureK = 1.0;
         materialh.push(headerz.length);
         private_jU.push(private_jU.length % (Math.max(1, 10)));
         sigmobu = [private_jU.length & sigmobu.length];
         headerz += `${soundj.size ^ sigmobu.length}`;
         soundj = new Map([[`${soundj.size}`, soundj.size]]);
         configureK *= parseFloat(`${1}`);
         break;
      }
      let sigmobT = 7955629 <= pangle9.size;
      do {
         pangle9.set(`${minimizeY}`, materialh.length - 1);
         if (sigmobT) {
            break;
         }
      } while (sigmobT && ((pangle9.size % (Math.max(1, 5))) < 4 || (googlez % (Math.max(1, 9))) < 5));
      if (2 < (3 ^ minimizeY) && 2 < (3 ^ minimizeY)) {
         minimizeY %= Math.max(3, googlez);
      }
          let splasho = String.fromCharCode(109,112,116,111,97,110,110,101,120,98,95,109,95,57,48,0);
          let dialogG = 3.0;
          let private_5vl = String.fromCharCode(121,95,52,51,95,108,97,115,114,0);
         googlez %= Math.max(3, private_5vl.length);
         splasho = `${splasho.length}`;
         dialogG *= 2;
         private_5vl += `${splasho.length - parseInt(`${dialogG}`)}`;
          let gesturer = 5.0;
          let successJ = 0.0;
          let sinaO = 1.0;
         materialh.push(parseInt(`${sinaO}`));
         gesturer *= parseInt(`${successJ}`);
         successJ -= parseInt(`${successJ}`) | parseInt(`${gesturer}`);
         sinaO /= Math.max(3, 3);
       let linka = String.fromCharCode(107,95,56,55,95,114,116,115,112,99,111,100,101,115,0);
       let u_manager4 = String.fromCharCode(115,113,108,108,111,103,0);
       let combinem = String.fromCharCode(104,95,57,95,99,117,114,116,97,105,110,0);
          let dropdowny: Array<any> = [208, 606, 64];
          let filledw = 0;
          let firebaseq = true;
         u_manager4 += `${dropdowny.length | googlez}`;
         dropdowny.push(filledw);
         filledw ^= filledw;
         firebaseq = !firebaseq;
       let dycreatorz: Map<any, any> = new Map([[String.fromCharCode(105,115,101,120,112,108,97,105,110,95,122,95,56,53,0),String.fromCharCode(106,95,50,55,95,108,105,98,0)], [String.fromCharCode(110,105,103,104,116,115,104,111,116,95,111,95,54,50,0),String.fromCharCode(102,101,108,101,109,95,115,95,57,51,0)], [String.fromCharCode(101,108,101,118,97,116,101,100,95,115,95,50,53,0),String.fromCharCode(99,97,112,97,98,105,108,105,116,121,95,113,95,54,52,0)]]);
      spinnerw /= Math.max(pangle9.size, 1);
   }
       let whatsappV = String.fromCharCode(105,110,100,101,111,95,107,95,53,52,0);
       let string5 = String.fromCharCode(119,114,105,116,116,101,110,95,109,95,55,0);
       let relatedQ = 2.0;
         relatedQ *= string5.length >> (Math.min(Math.abs(1), 4));
         whatsappV += `${string5.length - whatsappV.length}`;
      if (string5.startsWith(whatsappV)) {
          let s_positionj = String.fromCharCode(102,95,53,55,95,102,111,114,109,115,104,101,101,116,0);
          let mailE = false;
         whatsappV += `${1 ^ whatsappV.length}`;
         s_positionj += `${s_positionj.length}`;
         mailE = s_positionj.length == 69;
      }
      for (let f = 0; f < 2; f++) {
         relatedQ += parseInt(`${relatedQ}`);
      }
         string5 += `${string5.length >> (Math.min(Math.abs(3), 5))}`;
       let nativeQ: Map<any, any> = new Map([[String.fromCharCode(104,95,54,57,95,112,114,105,111,114,0),642], [String.fromCharCode(112,97,121,108,111,97,100,95,113,95,56,55,0),507], [String.fromCharCode(112,95,57,51,95,117,110,122,105,112,0),293]]);
       let foundH = 4;
       let sharedu = 4;
         relatedQ /= Math.max(2, nativeQ.size << (Math.min(5, Math.abs(sharedu))));
      while (whatsappV == string5) {
         string5 = `${foundH}`;
         break;
      }
      baiduD %= Math.max(3, whatsappV.length / (Math.max(5, policyo.length)));
      policyo = `${activity8}`;
      tumbnailo >>= Math.min(Math.abs((policyo == String.fromCharCode(98,0) ? parseInt(`${backward9}`) : policyo.length)), 4);
      launcherb = "2";
       let active0 = false;
       let wnewsc = String.fromCharCode(117,95,50,55,95,109,115,98,115,0);
       let routerb: Array<any> = [648, 756];
      if (routerb.length <= wnewsc.length) {
         wnewsc = `${wnewsc.length - 3}`;
      }
          let floatingo = String.fromCharCode(104,95,53,50,95,112,114,101,115,101,116,0);
         wnewsc = `${(wnewsc.length * (active0 ? 4 : 2))}`;
         floatingo = `${floatingo.length / (Math.max(2, 2))}`;
          let interstitiala = 0;
         routerb = [(wnewsc == String.fromCharCode(90,0) ? wnewsc.length : routerb.length)];
         interstitiala >>= Math.min(3, Math.abs(interstitiala % (Math.max(interstitiala, 1))));
       let anytimeD = 0.0;
         routerb = [routerb.length + 3];
      let smallt = 9589296 >= routerb.length;
      do {
         routerb = [3];
         if (smallt) {
            break;
         }
      } while ((routerb.includes(anytimeD)) && smallt);
      if (5.75 == (anytimeD * 1.54) && 1 == (4 >> (Math.min(1, routerb.length)))) {
         routerb.push(1 >> (Math.min(4, wnewsc.length)));
      }
      let chartH = 5577759 >= routerb.length;
      do {
         routerb = [parseInt(`${anytimeD}`)];
         if (chartH) {
            break;
         }
      } while ((4 == (parseInt(`${anytimeD}`) + routerb.length) && 3.0 == (5.4 + anytimeD)) && chartH);
      if ((5 & wnewsc.length) == 4) {
          let grayL = String.fromCharCode(111,102,102,115,101,116,115,95,116,95,51,51,0);
          let rewindd: Array<any> = [844, 791];
          let backwardl = 4.0;
         wnewsc = `${routerb.length}`;
         grayL += `${parseInt(`${backwardl}`)}`;
         rewindd = [parseInt(`${backwardl}`)];
      }
      linei = `${2 + parseInt(`${backward9}`)}`;
   if (favoriteW || 1 > launcherb.length) {
      launcherb += `${baiduD}`;
   }
      policyo = `${(parseInt(`${stylesf}`) & (favoriteW ? 4 : 1))}`;
   for (let m = 0; m < 2; m++) {
      favoriteW = 4.65 == stylesf && baiduD == 3;
   }
      activity8 %= Math.max(1, 1);

    Clipboard.setString(userState.user?.userReferralCode ?? '');

       let castk = true;
      for (let o = 0; o < 2; o++) {
         castk = (!castk ? !castk : !castk);
      }
      for (let b = 0; b < 2; b++) {
          let referrerl = 3.0;
          let umengn: Array<any> = [564, 245, 532];
         castk = umengn.includes(castk);
         referrerl -= parseFloat(`${parseInt(`${referrerl}`)}`);
         umengn.push(2);
      }
         castk = !castk;
      favoriteW = 27.14 <= spinnerw;
   for (let s = 0; s < 1; s++) {
      handlern += `${(String.fromCharCode(80,0) == policyo ? policyo.length : parseInt(`${stylesf}`))}`;
   }
   let mails = 7931454.0 >= spinnerw;
   do {
      spinnerw *= linei.length / 2;
      if (mails) {
         break;
      }
   } while (((launcherb.length + parseInt(`${spinnerw}`)) <= 1 || 5.82 <= (spinnerw + launcherb.length)) && mails);
   if ((baiduD << (Math.min(Math.abs(1), 4))) == 4 && favoriteW) {
       let header0: Map<any, any> = new Map([[String.fromCharCode(109,97,120,101,100,95,112,95,52,57,0),606], [String.fromCharCode(107,95,50,56,95,117,118,118,101,114,116,105,99,97,108,0),889]]);
       let scoreo = 4;
       let short_h_ = String.fromCharCode(101,95,52,57,95,112,97,110,110,105,110,103,0);
       let twitter7 = String.fromCharCode(108,95,49,56,95,114,101,99,111,118,101,114,0);
      for (let j = 0; j < 2; j++) {
         header0 = new Map([[`${header0.size}`, header0.size / (Math.max(3, 8))]]);
      }
      let logoutu = twitter7.length >= 8191540;
      do {
         twitter7 = `${short_h_.length + 1}`;
         if (logoutu) {
            break;
         }
      } while (logoutu && (twitter7.startsWith(`${scoreo}`)));
          let tickk = String.fromCharCode(111,95,49,95,112,97,114,116,105,116,105,111,110,115,0);
         header0.set(`${tickk}`, (String.fromCharCode(65,0) == tickk ? tickk.length : header0.size));
       let largey: Map<any, any> = new Map([[String.fromCharCode(108,95,49,50,95,99,105,110,118,105,100,101,111,0),669], [String.fromCharCode(116,95,51,49,95,103,101,111,99,111,100,105,110,103,0),141], [String.fromCharCode(105,95,57,49,95,103,97,112,0),552]]);
       let arrowk: Map<any, any> = new Map([[String.fromCharCode(122,95,56,57,95,98,108,111,98,115,105,122,101,0),180], [String.fromCharCode(111,95,56,57,95,115,112,101,99,116,114,117,109,0),914], [String.fromCharCode(116,119,101,97,107,115,95,120,95,54,53,0),153]]);
      let live0 = largey.size >= 5137865;
      do {
          let loadingo = String.fromCharCode(115,116,114,111,107,101,114,95,53,95,56,53,0);
          let fileg: Array<any> = [70, 514];
          let sharedE = true;
          let foundd: Map<any, any> = new Map([[String.fromCharCode(109,111,110,111,116,111,110,121,95,114,95,51,0),false ], [String.fromCharCode(97,100,100,114,101,115,115,101,115,95,104,95,51,0),true ]]);
          let holderS = String.fromCharCode(111,95,52,48,0);
         largey.set(`${fileg.length}`, arrowk.size);
         loadingo = `${foundd.size * loadingo.length}`;
         fileg = [(2 - (sharedE ? 2 : 2))];
         sharedE = 85 >= loadingo.length;
         foundd.set(holderS, foundd.size);
         holderS = "3";
         if (live0) {
            break;
         }
      } while (live0 && (4 <= (4 % (Math.max(2, twitter7.length)))));
      if (short_h_.includes(`${header0.size}`)) {
          let fillL: Array<any> = [503, 519, 535];
          let header0p = 4;
         short_h_ = `${header0.size}`;
         fillL = [fillL.length / (Math.max(8, header0p))];
         header0p %= Math.max(header0p % (Math.max(2, fillL.length)), 4);
      }
         scoreo &= 3;
          let privacyl: Map<any, any> = new Map([[String.fromCharCode(98,117,102,102,101,114,105,110,103,95,108,95,50,50,0),String.fromCharCode(104,95,57,55,95,97,99,100,101,99,0)], [String.fromCharCode(98,95,52,51,95,102,108,105,112,0),String.fromCharCode(116,101,115,118,101,114,116,95,118,95,52,54,0)], [String.fromCharCode(103,95,56,56,95,102,101,116,99,104,105,110,103,0),String.fromCharCode(113,95,54,95,105,110,115,116,97,108,108,0)]]);
         arrowk.set(twitter7, 3);
         privacyl = new Map([[`${privacyl.size}`, privacyl.size / (Math.max(3, 10))]]);
      let megaphoneh = String.fromCharCode(98,54,95,54,0) == twitter7;
      do {
         twitter7 += `${(String.fromCharCode(101,0) == twitter7 ? twitter7.length : arrowk.size)}`;
         if (megaphoneh) {
            break;
         }
      } while (((largey.size / (Math.max(8, twitter7.length))) < 4 || (largey.size / (Math.max(twitter7.length, 2))) < 4) && megaphoneh);
      for (let q = 0; q < 3; q++) {
         arrowk = new Map([[`${largey.size}`, short_h_.length + largey.size]]);
      }
       let register_e5J = 2.0;
       let styleG = 5.0;
      if (2.70 >= (register_e5J * twitter7.length)) {
         register_e5J -= 3;
      }
      baiduD >>= Math.min(4, Math.abs(1 * activity8));
   }
       let robotoO = String.fromCharCode(121,98,114,105,95,98,95,57,53,0);
       let modaln: Array<any> = [330, 752, 456];
       let interstitialP = String.fromCharCode(112,114,101,100,95,105,95,56,55,0);
         modaln = [(String.fromCharCode(78,0) == robotoO ? modaln.length : robotoO.length)];
          let sort8 = String.fromCharCode(117,110,97,114,121,95,120,95,50,55,0);
          let change4: Map<any, any> = new Map([[String.fromCharCode(106,95,53,51,95,112,97,103,101,111,117,116,0),953], [String.fromCharCode(100,95,56,53,95,102,105,110,97,108,100,0),458]]);
         modaln = [1];
         sort8 += `${change4.size}`;
         change4 = new Map([[`${change4.size}`, (sort8 == String.fromCharCode(80,0) ? sort8.length : change4.size)]]);
       let accepted6 = String.fromCharCode(102,105,108,101,110,97,109,101,95,50,95,53,54,0);
       let searchbarU = String.fromCharCode(112,95,50,52,95,115,99,101,110,97,114,105,111,0);
         robotoO += `${robotoO.length}`;
      if (accepted6 != String.fromCharCode(82,0)) {
         interstitialP += `${accepted6.length % (Math.max(1, 2))}`;
      }
      while (accepted6.length == 4) {
         accepted6 += `${interstitialP.length}`;
         break;
      }
      while (accepted6 == String.fromCharCode(109,0) && robotoO != String.fromCharCode(105,0)) {
         accepted6 = `${robotoO.length + interstitialP.length}`;
         break;
      }
         searchbarU = "2";
      let memberX = interstitialP.length <= 6416093;
      do {
         interstitialP = "1";
         if (memberX) {
            break;
         }
      } while ((2 <= (2 >> (Math.min(2, interstitialP.length)))) && memberX);
      launcherb += `${modaln.length % 1}`;
      handlern = `${3 | launcherb.length}`;
   for (let m = 0; m < 2; m++) {
      tumbnailo >>= Math.min(3, Math.abs((String.fromCharCode(56,0) == launcherb ? launcherb.length : parseInt(`${stylesf}`))));
   }
      backward9 -= policyo.length & 3;
       let prediction_ = true;
         prediction_ = (!prediction_ ? prediction_ : !prediction_);
       let floatingv: Array<any> = [String.fromCharCode(104,111,108,100,101,114,95,53,95,52,53,0), String.fromCharCode(111,110,108,121,95,111,95,56,54,0)];
      let sellg = prediction_ ? !prediction_ : prediction_;
      do {
         prediction_ = !prediction_;
         if (sellg) {
            break;
         }
      } while ((!prediction_) && sellg);
      spinnerw /= Math.max(4, 1);
   for (let y = 0; y < 1; y++) {
      launcherb = `${linei.length}`;
   }
   if (1.94 > (1.87 - backward9) || 1.87 > (spinnerw - backward9)) {
      spinnerw -= launcherb.length | 2;
   }
   while ((handlern.length / (Math.max(4, tumbnailo))) == 4 && (tumbnailo / 4) == 3) {
      handlern += "2 + activity8";
      break;
   }
   if (5 <= activity8) {
      activity8 += baiduD;
   }
   if (linei.length == 2) {
       let type_tm = 0.0;
         type_tm -= parseInt(`${type_tm}`) >> (Math.min(1, Math.abs(1)));
      for (let t = 0; t < 1; t++) {
          let streaming1 = String.fromCharCode(117,105,110,116,95,54,95,57,55,0);
          let dplus_ = 4.0;
          let desc6 = 1;
          let klevinb = String.fromCharCode(109,95,57,57,95,116,104,114,101,115,104,111,108,100,101,100,0);
          let mintegralO = 2.0;
         type_tm *= streaming1.length & parseInt(`${mintegralO}`);
         streaming1 += "1";
         dplus_ *= (String.fromCharCode(106,0) == klevinb ? klevinb.length : desc6);
         desc6 &= 3 / (Math.max(7, klevinb.length));
         mintegralO /= Math.max(4, parseFloat(`${klevinb.length >> (Math.min(5, Math.abs(desc6)))}`));
      }
      let default_ia = type_tm <= 7891750.0;
      do {
         type_tm += parseInt(`${type_tm}`) * 1;
         if (default_ia) {
            break;
         }
      } while (default_ia && ((type_tm / (Math.max(type_tm, 1))) >= 4.63 || (type_tm / (Math.max(type_tm, 1))) >= 4.63));
      backward9 += parseInt(`${spinnerw}`) + policyo.length;
   }
   for (let g = 0; g < 1; g++) {
       let single2 = String.fromCharCode(119,104,105,116,101,115,112,97,99,101,115,95,122,95,55,56,0);
       let dcopy_8n = 0.0;
       let roundN: Map<any, any> = new Map([[String.fromCharCode(116,95,55,54,95,99,112,111,115,0),false ], [String.fromCharCode(122,95,52,52,95,97,103,103,114,101,103,97,116,101,0),true ]]);
         dcopy_8n *= parseFloat(`${single2.length ^ 1}`);
          let sentryT = 1;
         dcopy_8n *= parseFloat(`${roundN.size * single2.length}`);
         sentryT >>= Math.min(Math.abs(2 & sentryT), 5);
       let select5: Array<any> = [String.fromCharCode(103,95,55,55,95,101,110,99,105,114,99,108,101,100,0), String.fromCharCode(122,111,110,101,95,100,95,54,50,0), String.fromCharCode(112,114,111,109,112,101,103,95,109,95,50,57,0)];
      for (let u = 0; u < 3; u++) {
         single2 += `${3 ^ parseInt(`${dcopy_8n}`)}`;
      }
      if (2 < (2 / (Math.max(1, roundN.size))) || 2 < (select5.length / (Math.max(roundN.size, 9)))) {
         roundN.set(`${dcopy_8n}`, roundN.size / (Math.max(3, 9)));
      }
      while ((parseFloat(`${single2.length}`) * dcopy_8n) >= 5.49) {
          let huaweiI = String.fromCharCode(109,105,110,117,116,101,95,107,95,54,52,0);
          let reactnativejsv = 3.0;
          let largeo = String.fromCharCode(103,95,57,50,95,99,108,111,115,101,0);
         dcopy_8n -= parseFloat(`${select5.length}`);
         huaweiI += `${parseInt(`${reactnativejsv}`)}`;
         reactnativejsv -= parseFloat(`${parseInt(`${reactnativejsv}`) * huaweiI.length}`);
         largeo += `${(String.fromCharCode(101,0) == huaweiI ? parseInt(`${reactnativejsv}`) : huaweiI.length)}`;
         break;
      }
         select5.push(roundN.size & 1);
      let stats3 = String.fromCharCode(101,102,109,98,117,102,107,120,98,0) == single2;
      do {
         single2 = `${parseInt(`${dcopy_8n}`) << (Math.min(single2.length, 2))}`;
         if (stats3) {
            break;
         }
      } while ((!single2.includes(`${roundN.size}`)) && stats3);
         single2 += `${parseInt(`${dcopy_8n}`) - single2.length}`;
      favoriteW = baiduD >= 1 && 1 >= tumbnailo;
   }
    setIsDialogOpen(true);
  }

  return (
    <ScreenContainer>
      <TitleWithBackButtonHeader title='个人中心' />
      <View
        style={{
          justifyContent: 'space-between',
          flex: 1,
          paddingHorizontal: 10,
        }}
      >
        <View style={{ marginTop: 30 }}>
          <TouchableOpacity activeOpacity={0.85}
            style={styles.pressableTextContainer}
            onPress={() => {
              ttGray.showToast('目前暂不支持更改头像，敬请期待');
            }}
          >
            <Text style={{ fontSize: 16, color: '#9C9C9C' }}>头像</Text>
            <FastImage
              style={{
                height: 34,
                width: 34,
              }}
              source={require('@static/images/projectTumbnailMbsplash.png')}
            />
          </TouchableOpacity>

          { }
          <TouchableOpacity activeOpacity={0.85}
            style={styles.pressableTextContainer}
            onPress={onPressUsername}
          >
            <Text style={{ fontSize: 16, color: colors.text }}>
              {username}
            </Text>
          </TouchableOpacity>

          { }
          <View style={styles.pressableTextContainer}>
            <Text style={{ fontSize: 16, color: '#9C9C9C' }}>
              {userState.user?.userEmail !== '' ? userState.user?.userEmail : userState.user?.userPhoneNumber}
            </Text>
          </View>

          { }
          {userState.user?.userReferrerName == '' &&
            userState.user.userAllowUpdateReferral == true &&
            <TouchableOpacity activeOpacity={0.85}
              style={styles.pressableTextContainer}
              onPress={onPressReferrer}
            >
              <Text style={{ fontSize: 16, color: '#B6B6B6' }}>
                补填邀请码 (只能填写一次)
              </Text>
            </TouchableOpacity>
          }

          {userState.user?.userReferrerName != '' && (
            <View style={styles.pressableTextContainer}>
              <Text style={{ fontSize: 16, color: '#9C9C9C' }}>推介人</Text>

              <View
                style={{
                  flexDirection: 'row',
                  justifyContent: 'center',
                  alignItems: 'center',
                }}
              >
                <Text style={{ fontSize: 14, color: '#9C9C9C' }}>
                  {userState.user?.userReferrerName}
                </Text>
              </View>
            </View>
          )}

          { }
          <TouchableOpacity activeOpacity={0.85}
            style={styles.pressableTextContainer}
            onPress={onCopyReferralCode}
          >
            <Text style={{ fontSize: 16, color: colors.primary }}>
              {userState.user?.userReferralCode}
            </Text>

            <View
              style={{
                flexDirection: 'row',
                justifyContent: 'center',
                alignItems: 'center',
              }}
            >
              <Text style={{ fontSize: 14, color: '#9C9C9C' }}>
                复制邀请码
              </Text>
              <Image
                style={{
                  height: 27,
                  width: 27,
                  position: 'relative',
                  top: 2,
                }}
                source={require('@static/images/profile/volumeUntick.png')}
              />
            </View>
          </TouchableOpacity>

          <NotificationModal
            onConfirm={toggleOverlay}
            isVisible={isDialogOpen}
            title='复制成功'
            subtitle1=''
            subtitle2=''
            subtitle3=''
          />
        </View>

        <ChangeUsernameModal
          isVisible={isShowUsernameModal}
          handleClose={() => {
            setShowUsernameModal(false);
          }}
          initialUsername={username}
          onSubmitSuccess={onUsernameUpdateSuccess}
        />

        <ChangeReferrerModal
          isVisible={isShowReferrerModal}
          handleClose={() => {
            setShowReferrerModal(false);
          }}
        />
      </View>
    </ScreenContainer>
  );
};

const createStyles = ({ dark }: any) => StyleSheet.create({
  pressableTextContainer: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    backgroundColor: dark ? "#1d2023" : '#D9D9D9',
    marginTop: 20,
    paddingLeft: 18,
    paddingRight: 13,
    height: 48,
    borderRadius: 8,
    marginBottom: 20,
  },
});
