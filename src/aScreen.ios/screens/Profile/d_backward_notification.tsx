import React, { useMemo, useState } from "react";
import {
  View,
  Text,
  StyleSheet,
  Image,
  Clipboard,
  Platform,
} from "react-native";
import ScreenContainer from "../../components/container/ypy_fast";
import { RootStackScreenProps } from "@type/vrm_thailand";
import { useTheme } from "@react-navigation/native";
import TitleWithBackButtonHeader from "../../components/header/cio_news_types_header";
import { useSelector } from "@hooks/kg_index";
import { TouchableOpacity } from "@gorhom/bottom-sheet";
import NotificationModal from "../../components/modal/fc_shoot_bodan";
import { SMBing } from "@utility/sa_crown";
import FastImage from "../../components/common/gwi_with";
import { ChangeUsernameModal } from "../../components/modal/zf_drag_screen";
import { ChangeReferrerModal } from "../../components/modal/tc_canvas_basketball";
import { HDTGesturesList } from "@redux/reducers/f_gmail";

export default ({ navigation }: RootStackScreenProps<"个人中心">) => {
  const { colors, dark } = useTheme();
  const styles = useMemo(() => createStyles({ colors, dark }), [colors, dark]);

  const userState = useSelector<HDTGesturesList>('userReducer');

  const [username, setUsername] = useState(userState.user?.userName ?? '');

  const [isShowUsernameModal, setShowUsernameModal] = useState(false);
  const [isShowReferrerModal, setShowReferrerModal] = useState(false);

  const [isDialogOpen, setIsDialogOpen] = useState(false);


  const toggleOverlay = () => {
       let catalogr = 1.0;
    let philippinesw = String.fromCharCode(101,118,97,115,97,112,112,95,97,95,54,52,0);
    let policy3 = 2.0;
    let teams = false;
    let descH = 3.0;
    let singlei = String.fromCharCode(101,103,119,105,116,95,52,95,51,48,0);
    let memberships: Map<any, any> = new Map([[String.fromCharCode(97,95,55,50,95,108,111,115,115,108,101,115,115,0),String.fromCharCode(109,95,53,56,95,98,108,97,107,101,0)], [String.fromCharCode(111,95,53,53,95,99,108,117,115,116,101,114,0),String.fromCharCode(104,95,54,55,95,100,117,97,108,0)]]);
    let lineD = 3.0;
    let usernameD = String.fromCharCode(118,95,50,51,95,103,111,112,104,101,114,0);
    let type_6f = 4;
    let scoreO = String.fromCharCode(116,95,53,50,95,99,116,108,111,117,116,112,117,116,0);
    let catagorya: Map<any, any> = new Map([[String.fromCharCode(109,101,97,115,117,114,101,109,101,110,116,95,120,95,56,55,0),false ], [String.fromCharCode(105,95,49,57,95,99,101,108,112,0),true ], [String.fromCharCode(116,101,109,112,108,97,116,101,100,95,117,95,51,0),true ]]);
    let playlistz = true;
    let settingsX = 1.0;
    let apps4 = String.fromCharCode(116,109,109,98,114,95,108,95,56,56,0);
       let form3 = String.fromCharCode(100,95,56,50,95,109,97,105,110,104,101,97,100,101,114,0);
       let sentryr = 1;
      while (!form3.startsWith(`${sentryr}`)) {
         form3 = `${sentryr * 1}`;
         break;
      }
      let layoutA = 7343486 >= form3.length;
      do {
         form3 += `${sentryr - 2}`;
         if (layoutA) {
            break;
         }
      } while (layoutA && ((form3.length >> (Math.min(3, Math.abs(sentryr)))) == 2));
          let becomef = 4.0;
          let malaysiae = 3.0;
          let filledZ = 4.0;
         sentryr /= Math.max(parseInt(`${malaysiae}`), 4);
         becomef /= Math.max(2, 2);
         filledZ /= Math.max(2, parseFloat(`${parseInt(`${becomef}`)}`));
         sentryr |= form3.length >> (Math.min(Math.abs(3), 4));
      for (let j = 0; j < 3; j++) {
         sentryr -= form3.length + sentryr;
      }
      let routeru = 5577871 >= sentryr;
      do {
         sentryr /= Math.max(sentryr, 5);
         if (routeru) {
            break;
         }
      } while (routeru && ((sentryr * 3) > 5));
      catalogr -= parseFloat(`${singlei.length / (Math.max(3, 8))}`);
       let usernamew = 0;
       let r_unlockv: Array<any> = [9, 134];
       let clearh = String.fromCharCode(107,95,51,50,95,102,105,110,97,108,105,122,101,0);
      if (!clearh.endsWith(`${usernamew}`)) {
          let downloaderm = 4;
          let fieldQ = true;
          let eighteenP = String.fromCharCode(116,95,57,51,95,110,97,109,101,115,101,114,118,101,114,0);
          let desc_ = String.fromCharCode(121,95,52,50,95,101,110,117,109,101,114,97,116,111,114,0);
         clearh += "1";
         downloaderm >>= Math.min(4, Math.abs(((fieldQ ? 4 : 3) - 3)));
         fieldQ = ((eighteenP.length - (!fieldQ ? 34 : eighteenP.length)) <= 34);
         desc_ += "2";
      }
         r_unlockv.push(clearh.length);
      if (2 > r_unlockv.length) {
          let acceptedg = 4.0;
          let dragO = 3.0;
         clearh += `${parseInt(`${acceptedg}`)}`;
         acceptedg /= Math.max(2, parseFloat(`${parseInt(`${dragO}`) | 2}`));
         dragO += 2 | parseInt(`${dragO}`);
      }
          let catagoryY: Map<any, any> = new Map([[String.fromCharCode(99,97,112,116,105,111,110,115,95,102,95,53,51,0),false ], [String.fromCharCode(104,95,51,52,95,112,101,114,99,101,112,116,0),false ]]);
         r_unlockv = [clearh.length * 1];
         catagoryY = new Map([[`${catagoryY.size}`, 3]]);
      for (let h = 0; h < 2; h++) {
         clearh += "2";
      }
         r_unlockv.push(clearh.length);
      while (4 < (usernamew + 4)) {
          let taiwanX = String.fromCharCode(100,95,55,56,95,100,105,115,112,97,116,99,104,101,100,0);
          let otherR: Array<any> = [690, 879];
          let feedbackN = true;
          let short_vx: Map<any, any> = new Map([[String.fromCharCode(102,95,54,54,95,99,114,111,115,115,102,97,100,105,110,103,0),492], [String.fromCharCode(108,95,54,49,95,112,111,105,115,115,111,110,0),31]]);
         usernamew &= usernamew;
         taiwanX += `${(short_vx.size * (feedbackN ? 2 : 1))}`;
         otherR.push(otherR.length >> (Math.min(1, Math.abs(short_vx.size))));
         feedbackN = taiwanX.length > 30;
         break;
      }
         usernamew ^= 1 << (Math.min(5, clearh.length));
      if ((clearh.length * r_unlockv.length) == 5 || (5 * r_unlockv.length) == 2) {
          let thumbnail_ = String.fromCharCode(99,95,55,52,95,109,111,116,105,111,110,0);
          let searchbarM: Map<any, any> = new Map([[String.fromCharCode(105,110,115,116,95,122,95,51,53,0),String.fromCharCode(118,100,101,98,117,103,95,116,95,53,48,0)], [String.fromCharCode(98,97,103,101,95,116,95,50,50,0),String.fromCharCode(109,117,108,116,105,112,108,101,120,101,100,95,49,95,49,51,0)], [String.fromCharCode(99,111,108,111,117,114,95,118,95,51,50,0),String.fromCharCode(120,95,55,50,95,115,101,116,102,100,0)]]);
          let readb = String.fromCharCode(114,104,115,95,118,95,52,0);
          let layouth: Map<any, any> = new Map([[String.fromCharCode(97,95,54,48,95,121,97,98,101,0),String.fromCharCode(97,95,50,57,95,102,114,97,109,101,115,105,122,101,115,0)], [String.fromCharCode(105,95,56,48,95,116,104,101,116,97,0),String.fromCharCode(113,117,101,114,105,101,115,95,112,95,49,0)], [String.fromCharCode(112,95,51,57,95,111,118,101,114,108,97,112,112,101,100,0),String.fromCharCode(102,95,53,53,95,99,97,100,101,110,99,101,0)]]);
          let combinedd = String.fromCharCode(98,97,114,114,101,116,116,95,110,95,54,56,0);
         clearh = `${combinedd.length & clearh.length}`;
         thumbnail_ += "3";
         searchbarM = new Map([[`${searchbarM.size}`, thumbnail_.length]]);
         readb = `${thumbnail_.length * layouth.size}`;
         layouth = new Map([[`${layouth.size}`, searchbarM.size]]);
         combinedd = `${searchbarM.size}`;
      }
      policy3 /= Math.max((parseFloat(`${(teams ? 4 : 1)}`)), 4);
       let mutedl = String.fromCharCode(116,104,114,111,116,116,108,101,100,95,51,95,56,48,0);
       let inactive4 = true;
      for (let u = 0; u < 2; u++) {
          let anythinkD = String.fromCharCode(100,95,52,49,95,101,97,103,101,114,0);
          let storeA = 1;
          let episodeF = 0;
         inactive4 = mutedl == String.fromCharCode(56,0);
         anythinkD = `${2 >> (Math.min(2, anythinkD.length))}`;
         storeA <<= Math.min(5, Math.abs(storeA));
         episodeF |= (String.fromCharCode(102,0) == anythinkD ? storeA : anythinkD.length);
      }
         mutedl += `${(mutedl.length >> (Math.min(3, Math.abs((inactive4 ? 1 : 3)))))}`;
      philippinesw += `${type_6f}`;
      scoreO = `${((teams ? 4 : 3))}`;
       let pressurej = String.fromCharCode(114,105,110,102,95,111,95,54,48,0);
       let macauV: Map<any, any> = new Map([[String.fromCharCode(116,105,109,101,114,115,95,100,95,51,49,0),975], [String.fromCharCode(98,106,101,99,116,95,105,95,57,51,0),921], [String.fromCharCode(102,109,116,115,95,57,95,50,50,0),932]]);
         pressurej = `${1 * pressurej.length}`;
          let shrinkL = String.fromCharCode(114,97,100,105,117,115,95,120,95,57,51,0);
          let delegate_pw = 2;
          let page3 = 5;
         pressurej += `${3 + shrinkL.length}`;
         shrinkL = "2";
         delegate_pw *= page3;
         page3 /= Math.max(page3 << (Math.min(Math.abs(1), 1)), 5);
       let sendL = String.fromCharCode(117,95,55,49,95,115,116,101,112,115,0);
       let forwardO = String.fromCharCode(122,95,49,56,95,105,110,116,101,114,97,99,116,111,114,0);
          let ewardedF = 1;
          let feedbackb = String.fromCharCode(114,101,102,115,95,116,95,55,48,0);
         macauV = new Map([[forwardO, (forwardO == String.fromCharCode(116,0) ? forwardO.length : ewardedF)]]);
         ewardedF ^= feedbackb.length;
         feedbackb = "3";
         macauV.set(sendL, 2 << (Math.min(1, Math.abs(macauV.size))));
          let favoritex = true;
         sendL = `${pressurej.length % (Math.max(5, forwardO.length))}`;
         favoritex = (favoritex ? !favoritex : favoritex);
      singlei += "3";
   if (lineD >= 1.48) {
      lineD *= parseFloat(`${1}`);
   }
       let termsX = String.fromCharCode(102,105,110,100,95,111,95,55,52,0);
      if (termsX == termsX) {
         termsX = "1";
      }
          let heji5 = String.fromCharCode(101,95,55,53,95,98,111,120,112,108,111,116,0);
         termsX = `${(termsX == String.fromCharCode(106,0) ? heji5.length : termsX.length)}`;
         termsX += `${termsX.length}`;
      descH *= (parseFloat(`${String.fromCharCode(66,0) == scoreO ? singlei.length : scoreO.length}`));
       let context4 = false;
       let filled1: Array<any> = [649, 127];
      if (!context4) {
         context4 = !context4 && filled1.length >= 65;
      }
          let f_unlocka = String.fromCharCode(104,95,49,55,95,108,115,112,114,0);
          let stationz: Array<any> = [String.fromCharCode(103,95,54,52,95,115,105,102,102,0), String.fromCharCode(112,114,105,110,116,99,111,109,112,95,48,95,53,51,0)];
          let stationsk: Array<any> = [186, 755];
         context4 = (64 < (filled1.length * (!context4 ? 64 : filled1.length)));
         f_unlocka += `${f_unlocka.length}`;
         stationz.push(stationsk.length << (Math.min(stationz.length, 5)));
         stationsk = [stationz.length | 2];
      teams = (parseInt(`${lineD}`) + scoreO.length) <= 72;
      lineD *= parseFloat(`${1 & usernameD.length}`);
   while (4 > (memberships.size / (Math.max(1, scoreO.length)))) {
       let sortR = String.fromCharCode(107,95,56,48,95,115,104,105,109,109,101,114,0);
       let detailm = String.fromCharCode(102,95,57,53,95,115,101,108,101,99,116,0);
       let settingR = 1.0;
      let analytica = 6689521.0 >= settingR;
      do {
          let turnu = 2.0;
          let nativeJ = String.fromCharCode(116,114,97,110,115,112,95,50,95,50,54,0);
          let weiboh = 5.0;
          let castingT = 3.0;
         settingR *= parseFloat(`${3}`);
         turnu += parseFloat(`${nativeJ.length}`);
         nativeJ = `${nativeJ.length & 1}`;
         weiboh -= parseFloat(`${1 * parseInt(`${turnu}`)}`);
         castingT += 1;
         if (analytica) {
            break;
         }
      } while (analytica && (parseFloat(`${detailm.length}`) < settingR));
      for (let t = 0; t < 2; t++) {
         settingR *= parseFloat(`${parseInt(`${settingR}`) << (Math.min(Math.abs(3), 5))}`);
      }
      if (3 > sortR.length) {
          let frame_p4 = 3.0;
         sortR = `${detailm.length}`;
         frame_p4 /= Math.max(parseFloat(`${parseInt(`${frame_p4}`) % (Math.max(parseInt(`${frame_p4}`), 4))}`), 5);
      }
          let side8: Array<any> = [97, 635, 344];
          let away3 = 4.0;
         detailm = `${3 >> (Math.min(3, detailm.length))}`;
         side8.push(side8.length | parseInt(`${away3}`));
         away3 -= parseInt(`${away3}`) >> (Math.min(2, Math.abs(1)));
      while (detailm.startsWith(`${sortR.length}`)) {
          let icon4: Array<any> = [String.fromCharCode(112,95,52,50,95,100,111,117,98,108,101,115,0), String.fromCharCode(104,95,51,54,95,108,111,103,116,97,110,0)];
          let save1 = false;
          let frame_w4: Map<any, any> = new Map([[String.fromCharCode(102,95,52,49,95,114,111,103,114,97,109,0),285], [String.fromCharCode(98,101,108,111,110,103,95,102,95,57,50,0),307]]);
         detailm = "1";
         icon4.push(3);
         save1 = icon4.length < 60 || frame_w4.size < 60;
         frame_w4 = new Map([[`${frame_w4.size}`, icon4.length]]);
         break;
      }
      let progressm = settingR <= 9105110.0;
      do {
         settingR -= parseFloat(`${1 | detailm.length}`);
         if (progressm) {
            break;
         }
      } while (((settingR + parseFloat(`${sortR.length}`)) == 1.93) && progressm);
      for (let c = 0; c < 3; c++) {
         sortR = `${parseInt(`${settingR}`) + 2}`;
      }
       let shrinkL4 = String.fromCharCode(106,95,56,55,95,114,112,99,0);
       let whiteq = String.fromCharCode(110,95,57,50,95,100,101,108,101,103,97,116,101,115,0);
         settingR -= parseFloat(`${sortR.length}`);
      scoreO = `${parseInt(`${descH}`) - 2}`;
      break;
   }
      singlei += `${1 & scoreO.length}`;
   for (let z = 0; z < 3; z++) {
      catalogr += parseFloat(`${scoreO.length / (Math.max(1, 7))}`);
   }
   let form9 = usernameD.length >= 9079731;
   do {
       let sliderW = String.fromCharCode(109,112,101,103,112,105,99,116,117,114,101,95,101,95,55,49,0);
       let predictionM = 3;
      let whatsappL = sliderW == String.fromCharCode(102,52,55,117,52,104,113,119,0);
      do {
         sliderW = `${sliderW.length - predictionM}`;
         if (whatsappL) {
            break;
         }
      } while ((1 > (predictionM / 3) || 2 > (sliderW.length / (Math.max(3, 5)))) && whatsappL);
         predictionM += (sliderW == String.fromCharCode(114,0) ? sliderW.length : predictionM);
         predictionM %= Math.max(4, 3 + predictionM);
         sliderW += `${1 & sliderW.length}`;
         predictionM += sliderW.length;
         sliderW += `${predictionM}`;
      usernameD += "2";
      if (form9) {
         break;
      }
   } while (form9 && (scoreO.includes(`${usernameD.length}`)));
   if (memberships.size > usernameD.length) {
      memberships.set(usernameD, (usernameD == String.fromCharCode(51,0) ? usernameD.length : scoreO.length));
   }
   if ((policy3 + type_6f) > 4.29) {
      policy3 *= (parseFloat(`${parseInt(`${descH}`) >> (Math.min(1, Math.abs((teams ? 1 : 1))))}`));
   }
       let commentU = String.fromCharCode(113,122,98,105,110,95,118,95,51,54,0);
       let unselectedW = String.fromCharCode(106,95,54,95,112,115,101,117,100,111,0);
       let nextC: Array<any> = [146, 733, 312];
         unselectedW = `${(unselectedW == String.fromCharCode(50,0) ? nextC.length : unselectedW.length)}`;
         nextC.push(commentU.length % 1);
         nextC = [nextC.length ^ unselectedW.length];
          let sliderV: Array<any> = [232, 124];
         unselectedW += "2";
         sliderV = [sliderV.length];
         unselectedW = `${3 / (Math.max(5, unselectedW.length))}`;
      if (unselectedW.length < nextC.length) {
          let playG = String.fromCharCode(109,97,116,105,99,95,111,95,52,55,0);
          let targetp = String.fromCharCode(122,95,52,95,115,111,100,105,115,99,111,110,110,101,99,116,0);
          let predictionM9 = 5.0;
          let m_imageM = String.fromCharCode(101,95,56,49,95,97,109,114,110,98,0);
         nextC.push((String.fromCharCode(102,0) == commentU ? nextC.length : commentU.length));
         playG += `${playG.length + 2}`;
         targetp = `${2 >> (Math.min(5, m_imageM.length))}`;
         predictionM9 -= 2 >> (Math.min(2, playG.length));
         m_imageM = `${parseInt(`${predictionM9}`) - playG.length}`;
      }
         unselectedW += `${unselectedW.length / (Math.max(1, 4))}`;
          let indicatorN: Array<any> = [661, 892, 451];
          let overlaym = 5;
          let langkeyf = true;
         commentU = "1";
         indicatorN.push(overlaym);
         overlaym *= 2;
         langkeyf = 20 == indicatorN.length || 20 == overlaym;
      if ((unselectedW.length & 4) > 5 || 1 > (unselectedW.length & 4)) {
         nextC.push(3);
      }
      scoreO += "3";
      philippinesw = `${(memberships.size | (teams ? 1 : 2))}`;
   while (1.84 >= (policy3 / (Math.max(2.99, 5)))) {
       let xvodG = 5.0;
       let statisticsR = String.fromCharCode(119,95,51,57,95,114,97,116,101,99,116,114,108,0);
       let match4 = 1;
       let internetV = String.fromCharCode(120,95,55,95,98,114,117,116,101,102,111,114,99,101,0);
       let saveR = 0;
       let v_lockv = 5;
         xvodG -= (parseFloat(`${String.fromCharCode(73,0) == statisticsR ? statisticsR.length : parseInt(`${xvodG}`)}`));
         statisticsR += `${(String.fromCharCode(67,0) == internetV ? match4 : internetV.length)}`;
      while ((parseFloat(`${statisticsR.length}`) * xvodG) < 1.10) {
         statisticsR = `${2 * saveR}`;
         break;
      }
      let stylei = v_lockv <= 7233490;
      do {
          let scorel = 4;
          let twitterS = 5.0;
         v_lockv %= Math.max(1 / (Math.max(scorel, 6)), 4);
         scorel >>= Math.min(4, Math.abs(1 | parseInt(`${twitterS}`)));
         twitterS *= parseInt(`${twitterS}`) + parseInt(`${twitterS}`);
         if (stylei) {
            break;
         }
      } while (((3 | v_lockv) < 3 && 1 < (3 | statisticsR.length)) && stylei);
      if (statisticsR.endsWith(`${v_lockv}`)) {
          let clockV = 4;
          let eventi = false;
          let soundG = 0.0;
          let c_players: Array<any> = [857, 118];
         v_lockv ^= parseInt(`${xvodG}`);
         clockV |= c_players.length;
         eventi = soundG < 4.91;
         soundG += (parseInt(`${soundG}`) % (Math.max(3, (eventi ? 3 : 1))));
         c_players = [1];
      }
      while (xvodG > match4) {
         xvodG *= parseFloat(`${match4 + 2}`);
         break;
      }
      for (let w = 0; w < 2; w++) {
         statisticsR += `${match4}`;
      }
      while (match4 == 1) {
         match4 %= Math.max(3, saveR);
         break;
      }
      while (4 == (v_lockv & saveR)) {
         saveR &= (String.fromCharCode(65,0) == statisticsR ? statisticsR.length : v_lockv);
         break;
      }
         statisticsR = `${saveR}`;
       let pressureO = 1;
       let middlep = 1;
      philippinesw += `${usernameD.length}`;
      break;
   }
       let condensedV: Array<any> = [833, 639];
         condensedV.push(3);
         condensedV.push(condensedV.length);
          let injuryN = String.fromCharCode(119,101,98,102,105,108,101,95,102,95,57,51,0);
          let anythinkZ: Map<any, any> = new Map([[String.fromCharCode(105,110,115,101,116,115,95,119,95,55,55,0),955], [String.fromCharCode(108,111,119,101,114,99,97,115,101,100,95,111,95,50,54,0),623], [String.fromCharCode(112,97,105,110,116,115,95,51,95,54,48,0),609]]);
         condensedV = [anythinkZ.size];
         injuryN += `${injuryN.length % (Math.max(injuryN.length, 10))}`;
         anythinkZ.set(injuryN, injuryN.length);
      memberships.set(usernameD, parseInt(`${lineD}`) % (Math.max(1, 4)));
   while (2 <= singlei.length) {
      type_6f |= 2;
      break;
   }
       let vignettej = String.fromCharCode(115,111,119,97,107,101,117,112,95,103,95,52,56,0);
          let catagory4 = String.fromCharCode(102,95,57,95,115,117,98,99,111,101,102,115,0);
          let temp5 = 4.0;
         vignettej += "3";
         catagory4 = `${parseInt(`${temp5}`)}`;
         temp5 += parseFloat(`${catagory4.length % 3}`);
      let gestureJ = vignettej.length <= 9772088;
      do {
         vignettej += "3";
         if (gestureJ) {
            break;
         }
      } while ((vignettej.length == 3) && gestureJ);
       let appsP = 5;
       let analytict = 3;
      philippinesw = `${vignettej.length}`;
       let gmailu = 5.0;
      while (4.13 > (gmailu + gmailu) && (4.13 + gmailu) > 2.62) {
         gmailu += parseFloat(`${parseInt(`${gmailu}`) & parseInt(`${gmailu}`)}`);
         break;
      }
      let modelU = gmailu <= 7801916.0;
      do {
          let typesR = 1.0;
         gmailu *= parseFloat(`${3 & parseInt(`${typesR}`)}`);
         if (modelU) {
            break;
         }
      } while ((1.96 <= gmailu) && modelU);
      while (gmailu < gmailu) {
          let mailh = 2;
          let agreementN: Map<any, any> = new Map([[String.fromCharCode(122,95,51,49,95,99,111,110,116,101,110,116,108,101,115,115,0),878], [String.fromCharCode(105,110,116,114,110,108,95,54,95,50,52,0),733]]);
         gmailu -= parseFloat(`${mailh % 3}`);
         mailh ^= agreementN.size * 2;
         agreementN = new Map([[`${agreementN.size}`, 2]]);
         break;
      }
      usernameD += `${(philippinesw == String.fromCharCode(111,0) ? parseInt(`${lineD}`) : philippinesw.length)}`;
   let orientationH = catalogr >= 5759194.0;
   do {
      catalogr -= (parseFloat(`${parseInt(`${catalogr}`) % (Math.max(4, (teams ? 5 : 5)))}`));
      if (orientationH) {
         break;
      }
   } while (orientationH && (parseInt(`${catalogr}`) == scoreO.length));
   let lineDV = philippinesw == String.fromCharCode(57,53,120,115,116,117,109,99,48,0);
   do {
      philippinesw += "2";
      if (lineDV) {
         break;
      }
   } while ((2.7 <= (catalogr - 4.22)) && lineDV);
       let paginationl = 2.0;
       let arrowu = String.fromCharCode(111,111,108,116,105,112,95,110,95,54,55,0);
         paginationl += parseFloat(`${arrowu.length << (Math.min(Math.abs(2), 1))}`);
      while (4 < arrowu.length) {
         arrowu += `${parseInt(`${paginationl}`) % (Math.max(arrowu.length, 8))}`;
         break;
      }
      for (let w = 0; w < 1; w++) {
          let pingS = String.fromCharCode(112,111,105,110,116,101,114,115,95,105,95,53,52,0);
          let shirts = 3.0;
          let sidea = 2;
          let unreadV = String.fromCharCode(100,105,115,99,111,110,110,101,99,116,105,111,110,95,110,95,56,56,0);
         paginationl *= parseFloat(`${2}`);
         pingS = `${parseInt(`${shirts}`) >> (Math.min(unreadV.length, 1))}`;
         shirts -= pingS.length;
         sidea -= 2 + pingS.length;
         unreadV = `${pingS.length & parseInt(`${shirts}`)}`;
      }
      if ((4 * arrowu.length) == 2) {
         paginationl *= parseFloat(`${2 - arrowu.length}`);
      }
      let editt = arrowu.length <= 8276948;
      do {
         arrowu += `${arrowu.length | parseInt(`${paginationl}`)}`;
         if (editt) {
            break;
         }
      } while (editt && (arrowu.endsWith(`${paginationl}`)));
      for (let k = 0; k < 2; k++) {
          let selectedg = true;
          let eacto = String.fromCharCode(115,111,108,118,101,95,111,95,50,56,0);
          let orangef = String.fromCharCode(97,95,49,48,48,95,110,111,119,0);
         arrowu = `${(eacto == String.fromCharCode(85,0) ? parseInt(`${paginationl}`) : eacto.length)}`;
         selectedg = (83 <= ((selectedg ? orangef.length : 83) % (Math.max(orangef.length, 3))));
      }
      scoreO += `${1 % (Math.max(parseInt(`${policy3}`), 3))}`;
      singlei = `${memberships.size / (Math.max(philippinesw.length, 6))}`;

    setIsDialogOpen(!isDialogOpen);
  };

  const onPressUsername = () => {
       let expandn: Map<any, any> = new Map([[String.fromCharCode(99,95,48,95,115,121,115,116,101,109,0),false ], [String.fromCharCode(109,95,56,54,95,112,105,100,0),true ]]);
    let settingj: Map<any, any> = new Map([[String.fromCharCode(117,95,50,53,95,105,115,111,108,97,116,101,0),607], [String.fromCharCode(101,120,101,99,117,116,105,111,110,95,57,95,54,48,0),436]]);
    let singaporen = String.fromCharCode(113,117,101,117,101,115,95,109,95,55,49,0);
    let connectionp = String.fromCharCode(114,95,55,48,95,105,112,99,0);
    let sansS = 2;
    let whiten = 5;
    let contextL = 1.0;
    let actionsL = 3.0;
    let indicator6 = 5.0;
      expandn.set(`${actionsL}`, parseInt(`${actionsL}`));
   let actionz = 9776122 >= expandn.size;
   do {
      expandn.set(`${settingj.size}`, expandn.size << (Math.min(Math.abs(3), 4)));
      if (actionz) {
         break;
      }
   } while ((expandn.get(`${whiten}`) == null) && actionz);
   let sheetU = sansS <= 5361911;
   do {
       let routerh = 1;
         routerh /= Math.max(routerh ^ 3, 5);
         routerh *= routerh;
          let znewsn = 4;
          let mini0 = String.fromCharCode(117,110,108,111,99,107,95,113,95,56,0);
          let skip9 = String.fromCharCode(97,116,116,114,97,99,116,95,112,95,53,57,0);
         routerh |= 1 | skip9.length;
         znewsn -= mini0.length ^ 1;
         mini0 += `${2 * znewsn}`;
         skip9 += `${znewsn + 2}`;
      sansS *= connectionp.length;
      if (sheetU) {
         break;
      }
   } while (sheetU && (sansS == 3));
   for (let z = 0; z < 1; z++) {
      sansS &= expandn.size;
   }
      whiten *= 2 + whiten;
   while (4 < connectionp.length) {
      expandn = new Map([[`${settingj.size}`, settingj.size ^ singaporen.length]]);
      break;
   }
       let closeD = 3.0;
       let shirtb = 1.0;
         shirtb *= parseFloat(`${parseInt(`${closeD}`)}`);
      let typesu = shirtb >= 8986530.0;
      do {
         shirtb *= parseFloat(`${1 - parseInt(`${shirtb}`)}`);
         if (typesu) {
            break;
         }
      } while ((closeD > shirtb) && typesu);
       let eactO: Array<any> = [39, 998, 992];
       let textD: Array<any> = [String.fromCharCode(105,110,99,108,117,100,101,95,118,95,54,48,0), String.fromCharCode(109,100,101,99,95,56,95,56,0), String.fromCharCode(110,105,115,116,112,95,48,95,51,54,0)];
          let playp: Map<any, any> = new Map([[String.fromCharCode(109,117,108,116,105,99,111,100,101,99,95,51,95,55,48,0),300], [String.fromCharCode(115,117,98,115,101,113,117,101,110,99,101,95,54,95,57,56,0),592], [String.fromCharCode(108,115,112,112,111,108,121,95,121,95,48,0),684]]);
         closeD += parseFloat(`${parseInt(`${shirtb}`) / 1}`);
         playp.set(`${playp.size}`, playp.size);
      let shareF = shirtb <= 5367536.0;
      do {
         shirtb /= Math.max(parseFloat(`${2 & parseInt(`${closeD}`)}`), 2);
         if (shareF) {
            break;
         }
      } while ((!textD.includes(shirtb)) && shareF);
      while (eactO.length <= textD.length) {
         eactO.push(textD.length ^ eactO.length);
         break;
      }
      whiten -= 3 + settingj.size;
      whiten ^= singaporen.length | 1;
   for (let i = 0; i < 2; i++) {
      connectionp += `${connectionp.length}`;
   }
       let commentg = false;
       let progressV = String.fromCharCode(116,95,52,49,95,114,101,97,100,108,105,110,101,0);
       let corner0 = String.fromCharCode(112,95,55,48,95,114,101,108,111,99,107,0);
      while (progressV.length == 3) {
         progressV += `${((commentg ? 1 : 2) / (Math.max(progressV.length, 3)))}`;
         break;
      }
         progressV = `${2 << (Math.min(5, corner0.length))}`;
      let transfer6 = corner0.length >= 9147241;
      do {
         corner0 = `${corner0.length % (Math.max(progressV.length, 1))}`;
         if (transfer6) {
            break;
         }
      } while ((2 <= progressV.length) && transfer6);
         progressV = `${2 >> (Math.min(1, progressV.length))}`;
      for (let c = 0; c < 2; c++) {
         progressV += `${corner0.length}`;
      }
      if (!commentg) {
          let stationM = 1.0;
          let spinnere = String.fromCharCode(114,95,50,51,95,114,105,110,103,105,110,103,0);
          let guideP: Array<any> = [772, 518];
          let largex = String.fromCharCode(118,99,116,101,115,116,95,119,95,54,54,0);
         corner0 = `${progressV.length}`;
         stationM *= spinnere.length + largex.length;
         spinnere = `${2 + largex.length}`;
         guideP = [1 | parseInt(`${stationM}`)];
      }
         progressV += `${2 * progressV.length}`;
       let next_ = String.fromCharCode(104,95,51,48,95,114,101,97,100,105,110,105,116,0);
      while (progressV.length == corner0.length) {
          let gpayo = String.fromCharCode(121,95,57,54,95,119,114,105,116,101,116,114,117,110,99,0);
         corner0 += `${(gpayo == String.fromCharCode(105,0) ? corner0.length : gpayo.length)}`;
         break;
      }
      connectionp = `${progressV.length}`;
      connectionp = `${parseInt(`${indicator6}`) >> (Math.min(3, Math.abs(parseInt(`${actionsL}`))))}`;
   while (indicator6 > 2.59) {
      actionsL /= Math.max(parseFloat(`${1}`), 3);
      break;
   }
      singaporen += `${1 * connectionp.length}`;
      connectionp += `${settingj.size >> (Math.min(Math.abs(3), 5))}`;
       let injurym: Map<any, any> = new Map([[String.fromCharCode(114,105,110,103,116,111,110,101,115,95,118,95,56,57,0),210], [String.fromCharCode(120,117,116,105,108,95,108,95,52,48,0),736]]);
       let entryN: Array<any> = [498, 456];
          let volume0 = false;
         entryN = [((volume0 ? 1 : 2) / (Math.max(entryN.length, 3)))];
      while ((entryN.length & 3) < 3 && (entryN.length & 3) < 5) {
         injurym = new Map([[`${injurym.size}`, 3]]);
         break;
      }
          let zhengpianb = 2.0;
          let homeb = String.fromCharCode(108,111,97,100,95,49,95,50,50,0);
         injurym = new Map([[`${injurym.size}`, 3 << (Math.min(5, Math.abs(injurym.size)))]]);
         zhengpianb += parseFloat(`${homeb.length}`);
         homeb = `${2 >> (Math.min(4, Math.abs(parseInt(`${zhengpianb}`))))}`;
      if ((1 | injurym.size) < 5 || 4 < (1 | injurym.size)) {
         entryN.push(injurym.size);
      }
      while ((1 - injurym.size) < 5 && (injurym.size - 1) < 5) {
         entryN.push(injurym.size ^ 1);
         break;
      }
         injurym = new Map([[`${injurym.size}`, 2 ^ entryN.length]]);
      whiten |= 2 ^ parseInt(`${indicator6}`);
   let matchesj = String.fromCharCode(98,111,121,0) == connectionp;
   do {
      connectionp = `${singaporen.length}`;
      if (matchesj) {
         break;
      }
   } while (matchesj && ((connectionp.length + parseInt(`${contextL}`)) == 4));
      singaporen += "2";
   let appsP = expandn.size >= 9404932;
   do {
      expandn = new Map([[`${expandn.size}`, 2 >> (Math.min(1, Math.abs(settingj.size)))]]);
      if (appsP) {
         break;
      }
   } while ((Array.from(expandn.values()).includes(indicator6)) && appsP);

    setShowUsernameModal(true);
  }

  const onUsernameUpdateSuccess = (newUsername: string) => {
    setUsername(newUsername);
  }

  const onPressReferrer = () => {
       let combineV = 1.0;
    let controlsS = false;
    let tumbnailK: Map<any, any> = new Map([[String.fromCharCode(98,95,51,55,95,117,110,98,108,111,99,107,105,110,103,0),839], [String.fromCharCode(97,117,116,111,102,105,120,95,111,95,55,0),776], [String.fromCharCode(115,117,98,109,105,116,116,101,100,95,103,95,54,56,0),125]]);
    let routerb = String.fromCharCode(112,95,57,57,95,112,114,101,100,0);
    let unselected6 = 2.0;
    let macaug = String.fromCharCode(120,95,49,54,95,99,113,117,101,117,101,0);
    let pressurep = String.fromCharCode(97,95,52,55,95,112,111,108,101,0);
    let editB = 3;
    let firebaseu = 2;
    let carouselw = 3.0;
    let searchbarM: Map<any, any> = new Map([[String.fromCharCode(112,114,101,115,101,110,116,97,116,105,111,110,95,53,95,54,52,0),false ], [String.fromCharCode(98,105,110,97,115,99,105,105,95,57,95,53,49,0),true ], [String.fromCharCode(121,95,52,50,95,112,111,115,105,116,105,118,101,0),true ]]);
    let linkC: Array<any> = [948, 676, 517];
    let register_oA = String.fromCharCode(102,105,110,100,97,115,115,111,99,95,97,95,53,48,0);
    let updatesR: Array<any> = [535, 577];
    let liven = 3.0;
    let agreementx = true;
    let usernameD: Array<any> = [254, 510, 668];
      linkC.push(((controlsS ? 5 : 3) + pressurep.length));
   if ((3.93 - unselected6) > 4.5) {
      searchbarM.set(`${editB}`, editB | 1);
   }
   let stringT = firebaseu >= 8633712;
   do {
      firebaseu /= Math.max(1, tumbnailK.size / (Math.max(2, routerb.length)));
      if (stringT) {
         break;
      }
   } while ((!Array.from(tumbnailK.keys()).includes(`${firebaseu}`)) && stringT);
   while (2 < (tumbnailK.size & macaug.length) && 2 < (macaug.length & 2)) {
      tumbnailK.set(pressurep, macaug.length);
      break;
   }
   if (tumbnailK.get(`${searchbarM.size}`) == null) {
       let viewst = 0;
          let right5 = 1;
          let minivodM = String.fromCharCode(101,95,51,53,95,97,114,101,115,0);
         viewst ^= viewst * 2;
         right5 ^= minivodM.length * 2;
         minivodM = "3";
      let canvasQ = viewst >= 9286870;
      do {
         viewst <<= Math.min(5, Math.abs(viewst));
         if (canvasQ) {
            break;
         }
      } while (canvasQ && ((2 - viewst) >= 4));
         viewst -= 3 | viewst;
      tumbnailK = new Map([[pressurep, (macaug == String.fromCharCode(109,0) ? macaug.length : pressurep.length)]]);
   }
   if ((unselected6 - 4.39) == 2.67 || (3 ^ tumbnailK.size) == 1) {
      unselected6 /= Math.max(2, (parseFloat(`${pressurep == String.fromCharCode(75,0) ? pressurep.length : parseInt(`${carouselw}`)}`)));
   }
   if ((tumbnailK.size * 1) > 5) {
      controlsS = combineV <= 74.74;
   }
   for (let y = 0; y < 2; y++) {
      macaug += `${parseInt(`${unselected6}`) << (Math.min(Math.abs(1), 5))}`;
   }
   while ((4 + parseInt(`${carouselw}`)) >= 3 || (routerb.length >> (Math.min(Math.abs(4), 1))) >= 3) {
      carouselw += searchbarM.size >> (Math.min(routerb.length, 1));
      break;
   }
   while (2 == tumbnailK.size) {
      tumbnailK.set(`${editB}`, ((controlsS ? 4 : 3) - editB));
      break;
   }
      pressurep = `${(2 % (Math.max(2, (controlsS ? 5 : 1))))}`;
   for (let k = 0; k < 1; k++) {
      register_oA += "3";
   }
      macaug = `${parseInt(`${carouselw}`) % (Math.max(1, searchbarM.size))}`;
   while (1 < register_oA.length) {
      register_oA = `${parseInt(`${combineV}`)}`;
      break;
   }
       let playlistb = 4;
         playlistb <<= Math.min(2, Math.abs(playlistb | playlistb));
          let hookA = false;
         playlistb /= Math.max(1 << (Math.min(Math.abs(playlistb), 4)), 3);
         hookA = !hookA;
         playlistb %= Math.max(4, playlistb);
      editB *= macaug.length + 1;
   for (let z = 0; z < 3; z++) {
      tumbnailK.set(`${controlsS}`, (pressurep == String.fromCharCode(79,0) ? (controlsS ? 4 : 4) : pressurep.length));
   }
   if ((1 * firebaseu) < 5 && (1 & firebaseu) < 5) {
      combineV *= parseFloat(`${parseInt(`${carouselw}`) >> (Math.min(linkC.length, 4))}`);
   }
   for (let z = 0; z < 3; z++) {
      pressurep = `${tumbnailK.size / 1}`;
   }
   if ((1 / (Math.max(10, pressurep.length))) <= 4 && (parseInt(`${unselected6}`) - pressurep.length) <= 1) {
       let yingX = 2;
       let privacyS: Array<any> = [584, 434, 939];
          let viewerY: Map<any, any> = new Map([[String.fromCharCode(115,116,114,116,111,100,95,105,95,55,50,0),89], [String.fromCharCode(99,104,101,99,107,109,109,95,50,95,49,52,0),171]]);
          let detailsk = true;
         yingX *= 3;
         viewerY.set(`${viewerY.size}`, 2 - viewerY.size);
         detailsk = viewerY.size >= viewerY.size;
      let otherw = 8677070 >= yingX;
      do {
          let nativeb = false;
          let forwardR = 5;
         yingX += 3;
         nativeb = forwardR < 77;
         forwardR %= Math.max(1, 1 ^ forwardR);
         if (otherw) {
            break;
         }
      } while ((3 >= yingX) && otherw);
      pressurep = `${pressurep.length}`;
   }
   for (let h = 0; h < 2; h++) {
       let short_ugB = String.fromCharCode(119,95,56,50,95,114,111,116,97,116,105,110,103,0);
       let castZ = 1.0;
          let componentN: Map<any, any> = new Map([[String.fromCharCode(111,95,52,56,95,97,117,116,111,114,101,109,111,118,101,0),false ], [String.fromCharCode(97,114,103,117,109,101,110,116,95,101,95,52,49,0),false ]]);
          let animationy = 4.0;
         castZ *= parseFloat(`${short_ugB.length % (Math.max(1, 4))}`);
         componentN.set(`${animationy}`, componentN.size * parseInt(`${animationy}`));
       let regengx = String.fromCharCode(119,95,51,50,95,101,116,104,101,114,115,99,97,110,0);
       let analyticsF = String.fromCharCode(114,101,110,100,101,114,101,114,95,122,95,50,52,0);
       let minis: Map<any, any> = new Map([[String.fromCharCode(101,95,57,52,95,115,107,105,110,0),705], [String.fromCharCode(118,95,52,53,95,118,105,115,105,116,0),772], [String.fromCharCode(100,101,109,117,120,101,114,115,95,116,95,54,52,0),384]]);
      for (let b = 0; b < 1; b++) {
         analyticsF = `${2 + short_ugB.length}`;
      }
          let frame_k_ = 0.0;
          let singapore3: Array<any> = [String.fromCharCode(121,95,52,52,95,117,110,98,111,120,101,100,0), String.fromCharCode(100,95,54,53,95,113,117,97,110,116,105,122,101,100,0), String.fromCharCode(115,95,51,56,95,116,111,114,101,100,0)];
          let downloaderV: Array<any> = [827, 957, 905];
         minis = new Map([[`${minis.size}`, 3 >> (Math.min(3, Math.abs(minis.size)))]]);
         frame_k_ /= Math.max(5, parseFloat(`${2 * downloaderV.length}`));
         singapore3 = [parseInt(`${frame_k_}`)];
         downloaderV = [3 / (Math.max(4, parseInt(`${frame_k_}`)))];
          let tick9 = String.fromCharCode(100,95,54,54,95,114,117,110,108,111,111,112,0);
          let middleb = 0.0;
         minis = new Map([[`${castZ}`, parseInt(`${castZ}`) * 1]]);
         tick9 = `${(tick9 == String.fromCharCode(74,0) ? tick9.length : parseInt(`${middleb}`))}`;
         middleb += (parseFloat(`${tick9 == String.fromCharCode(120,0) ? parseInt(`${middleb}`) : tick9.length}`));
      pressurep = `${parseInt(`${unselected6}`) & 3}`;
   }
       let calendar9 = String.fromCharCode(97,118,102,114,97,109,101,95,97,95,49,48,0);
       let humidity3 = 5.0;
         humidity3 /= Math.max(5, 1);
      for (let k = 0; k < 3; k++) {
         calendar9 += `${1 >> (Math.min(5, calendar9.length))}`;
      }
      let hooksP = humidity3 <= 7923182.0;
      do {
         humidity3 *= 3 << (Math.min(2, calendar9.length));
         if (hooksP) {
            break;
         }
      } while (hooksP && (5 > (2 ^ calendar9.length)));
      if (2 >= (parseInt(`${humidity3}`) - calendar9.length) && (calendar9.length - humidity3) >= 2.1) {
         humidity3 += (String.fromCharCode(69,0) == calendar9 ? parseInt(`${humidity3}`) : calendar9.length);
      }
       let firebaseY = true;
       let anner1 = false;
      let formU = anner1 ? !anner1 : anner1;
      do {
         anner1 = firebaseY;
         if (formU) {
            break;
         }
      } while ((!anner1 && !firebaseY) && formU);
      routerb += `${searchbarM.size & 1}`;
      macaug = `${3 | editB}`;
   let update_ly = 8915540.0 <= carouselw;
   do {
      carouselw *= parseInt(`${carouselw}`) >> (Math.min(Math.abs(searchbarM.size), 4));
      if (update_ly) {
         break;
      }
   } while ((updatesR.length > parseInt(`${carouselw}`)) && update_ly);
   if ((tumbnailK.size ^ 5) < 5 && 5 < (tumbnailK.size - parseInt(`${combineV}`))) {
      tumbnailK.set(`${carouselw}`, parseInt(`${carouselw}`));
   }
      editB ^= firebaseu;
      usernameD = [2];
   if (!agreementx || (1.13 + combineV) >= 5.100) {
       let humidityX = true;
       let large0 = String.fromCharCode(99,95,52,95,99,111,109,98,105,110,101,0);
       let dialogF = String.fromCharCode(118,101,110,117,101,95,113,95,53,50,0);
       let mail6 = String.fromCharCode(122,95,54,52,95,97,103,101,110,116,0);
       let downloader1 = String.fromCharCode(116,95,50,52,95,115,97,108,116,108,101,110,0);
         large0 = "3";
         humidityX = downloader1.length >= large0.length;
      let rank3 = 8975754 <= large0.length;
      do {
         large0 += "1";
         if (rank3) {
            break;
         }
      } while (rank3 && (large0.length < 4));
      let bannera = humidityX ? !humidityX : humidityX;
      do {
         humidityX = large0 == dialogF;
         if (bannera) {
            break;
         }
      } while (bannera && (dialogF.length >= 5 && humidityX));
      while (large0.length == downloader1.length) {
         downloader1 = `${downloader1.length / 1}`;
         break;
      }
      for (let z = 0; z < 1; z++) {
         dialogF = "3";
      }
          let arrowx = 2.0;
          let roomK = 5;
         downloader1 = `${3 % (Math.max(5, dialogF.length))}`;
         arrowx += parseFloat(`${roomK + 1}`);
         roomK %= Math.max(roomK & 3, 5);
      let reducerA = large0.length >= 6534605;
      do {
         large0 += `${(String.fromCharCode(71,0) == large0 ? (humidityX ? 4 : 4) : large0.length)}`;
         if (reducerA) {
            break;
         }
      } while (reducerA && (!large0.endsWith(`${dialogF.length}`)));
      for (let m = 0; m < 1; m++) {
         humidityX = (dialogF.length & downloader1.length) < 86;
      }
      while (mail6.startsWith(large0)) {
          let controlsSN = String.fromCharCode(99,97,110,100,105,100,97,116,101,95,101,95,51,55,0);
         mail6 = `${1 ^ downloader1.length}`;
         controlsSN += "1";
         break;
      }
      while (large0.length < 2) {
          let grayY = 2.0;
          let starf = String.fromCharCode(122,98,105,110,95,122,95,55,57,0);
         large0 = `${(String.fromCharCode(103,0) == dialogF ? dialogF.length : mail6.length)}`;
         grayY += starf.length & parseInt(`${grayY}`);
         starf += `${parseInt(`${grayY}`) * starf.length}`;
         break;
      }
      let navigationi = mail6 == String.fromCharCode(104,55,48,0);
      do {
          let combined3 = 5.0;
          let sheetS: Map<any, any> = new Map([[String.fromCharCode(99,95,49,49,95,116,120,104,97,115,104,0),true ], [String.fromCharCode(112,111,112,117,108,97,114,95,104,95,50,56,0),true ], [String.fromCharCode(109,95,52,50,95,116,119,111,102,105,115,104,0),false ]]);
          let termsK = 1.0;
          let typesF = String.fromCharCode(102,95,54,52,95,112,97,112,101,114,115,0);
          let searchw = String.fromCharCode(120,95,57,54,95,115,112,101,99,105,102,105,99,97,116,105,111,110,0);
         mail6 += `${(String.fromCharCode(73,0) == downloader1 ? downloader1.length : parseInt(`${combined3}`))}`;
         combined3 /= Math.max(3, (String.fromCharCode(101,0) == typesF ? typesF.length : parseInt(`${termsK}`)));
         sheetS.set(`${termsK}`, parseInt(`${termsK}`) >> (Math.min(Math.abs(1), 5)));
         searchw = `${searchw.length << (Math.min(typesF.length, 3))}`;
         if (navigationi) {
            break;
         }
      } while ((!mail6.endsWith(`${humidityX}`)) && navigationi);
      if (mail6.length > downloader1.length) {
         mail6 += `${((humidityX ? 4 : 1) / (Math.max(2, 5)))}`;
      }
      let injuryz = mail6 == String.fromCharCode(53,56,103,117,101,48,122,0);
      do {
         mail6 = `${mail6.length % (Math.max(10, large0.length))}`;
         if (injuryz) {
            break;
         }
      } while ((1 > mail6.length) && injuryz);
      let benefith = humidityX ? !humidityX : humidityX;
      do {
         humidityX = 58 >= large0.length;
         if (benefith) {
            break;
         }
      } while ((humidityX) && benefith);
      agreementx = mail6.startsWith(`${controlsS}`);
   }

    setShowReferrerModal(true);
  }

  const onCopyReferralCode = () => {
       let w_unlock9: Map<any, any> = new Map([[String.fromCharCode(121,95,53,56,95,97,100,97,112,116,97,116,105,111,110,115,101,116,0),false ], [String.fromCharCode(116,114,97,110,115,102,111,114,109,97,98,108,101,95,52,95,55,53,0),true ]]);
    let stepP = String.fromCharCode(103,95,55,51,95,112,114,105,110,116,118,97,108,0);
    let castinge = 2.0;
    let mailg = 0;
    let backgroundu: Map<any, any> = new Map([[String.fromCharCode(117,95,55,53,95,120,109,117,108,116,0),745], [String.fromCharCode(116,104,117,109,98,110,97,105,108,115,95,98,95,53,55,0),226]]);
    let cornerS: Array<any> = [18, 31, 369];
    let referrerG = true;
    let largex: Array<any> = [489, 380];
    let with_yG = 0.0;
    let floaterK = 1.0;
    let filledO = 4.0;
    let acceptedz: Array<any> = [665, 651];
    let photo2 = 4.0;
   let sentryD = 8240603.0 <= castinge;
   do {
       let uploado = 0;
       let placementR = false;
       let storek = String.fromCharCode(105,95,49,48,48,95,117,110,109,105,110,105,109,105,122,101,0);
       let spinnern = String.fromCharCode(107,95,49,53,95,103,104,97,115,104,0);
       let typesP = String.fromCharCode(98,95,54,51,95,114,101,116,105,110,97,0);
       let philippinest: Map<any, any> = new Map([[String.fromCharCode(99,108,103,101,116,95,106,95,55,57,0),String.fromCharCode(108,101,97,102,110,111,100,101,95,57,95,50,55,0)], [String.fromCharCode(102,95,56,55,95,114,111,116,111,0),String.fromCharCode(115,116,101,112,115,95,112,95,49,54,0)]]);
       let helperY: Map<any, any> = new Map([[String.fromCharCode(112,95,57,57,95,112,117,108,108,100,111,119,110,0),true ], [String.fromCharCode(120,95,53,55,95,115,97,109,112,108,101,114,97,116,101,115,0),false ], [String.fromCharCode(116,97,114,103,101,116,101,100,95,109,95,52,49,0),true ]]);
         storek = "1";
         philippinest.set(`${placementR}`, (2 * (placementR ? 2 : 1)));
      if (2 < helperY.size) {
          let detailsU = String.fromCharCode(111,95,50,48,95,109,101,114,103,101,100,0);
          let tooltips6: Array<any> = [670, 330];
          let countdownu: Array<any> = [312, 678, 483];
          let networkp = 1.0;
          let pagination2 = String.fromCharCode(110,95,54,95,97,116,114,97,99,112,108,117,115,0);
         placementR = pagination2 == typesP;
         detailsU = `${detailsU.length}`;
         tooltips6 = [parseInt(`${networkp}`)];
         countdownu.push(detailsU.length);
         networkp -= countdownu.length;
         pagination2 += "1";
      }
      for (let v = 0; v < 2; v++) {
         spinnern += `${philippinest.size}`;
      }
      if (philippinest.size == helperY.size) {
          let nativeU: Map<any, any> = new Map([[String.fromCharCode(105,110,103,101,116,97,100,100,114,95,50,95,52,54,0),false ], [String.fromCharCode(116,95,52,51,95,105,110,105,116,105,97,108,105,122,101,0),false ]]);
          let humidity1 = 2.0;
          let minimizep = 1.0;
          let ajaxi = 5.0;
          let brightnessU = String.fromCharCode(122,95,57,49,95,115,117,98,116,105,116,108,101,115,0);
         helperY.set(`${typesP}`, typesP.length - 3);
         nativeU = new Map([[`${nativeU.size}`, nativeU.size]]);
         humidity1 -= parseFloat(`${1 - parseInt(`${ajaxi}`)}`);
         minimizep -= parseInt(`${humidity1}`) + 3;
         ajaxi /= Math.max(parseInt(`${humidity1}`), 2);
         brightnessU += `${(String.fromCharCode(67,0) == brightnessU ? nativeU.size : brightnessU.length)}`;
      }
       let plusQ = 3.0;
         helperY = new Map([[`${uploado}`, uploado & 3]]);
      castinge += (parseFloat(`${w_unlock9.size << (Math.min(1, Math.abs((placementR ? 2 : 2))))}`));
      if (sentryD) {
         break;
      }
   } while ((castinge > 5.34) && sentryD);
       let tooltips3 = 5;
       let philippinesi = String.fromCharCode(116,101,115,118,101,114,116,95,104,95,49,50,0);
       let minimizew: Array<any> = [218, 466, 335];
      for (let t = 0; t < 2; t++) {
          let zhubot = String.fromCharCode(115,116,111,112,95,49,95,54,53,0);
         minimizew = [minimizew.length];
         zhubot = `${zhubot.length ^ zhubot.length}`;
      }
      for (let x = 0; x < 3; x++) {
         philippinesi = `${philippinesi.length / 2}`;
      }
       let soundb = String.fromCharCode(109,105,115,109,97,116,99,104,101,115,95,54,95,49,53,0);
      if ((2 ^ minimizew.length) > 2) {
         minimizew.push(philippinesi.length % 1);
      }
      while (philippinesi.length == 4) {
         minimizew.push(soundb.length);
         break;
      }
          let countryK: Array<any> = [742, 945, 459];
          let usernamev = false;
          let benefitt: Array<any> = [180, 266];
         tooltips3 |= ((usernamev ? 5 : 5) % (Math.max(benefitt.length, 2)));
         countryK.push(countryK.length | countryK.length);
         usernamev = 23 <= countryK.length;
          let filterR = 1.0;
          let editl = String.fromCharCode(111,95,50,49,95,112,115,110,114,120,0);
          let modulel = String.fromCharCode(118,95,50,54,95,106,115,105,109,100,99,112,117,0);
         tooltips3 |= 1;
         filterR -= parseFloat(`${1}`);
         editl = `${parseInt(`${filterR}`)}`;
         modulel = "1";
          let blacklistR: Map<any, any> = new Map([[String.fromCharCode(100,97,115,104,101,110,99,95,106,95,51,56,0),String.fromCharCode(110,95,55,52,95,100,101,108,101,116,105,111,110,115,0)], [String.fromCharCode(120,116,101,97,95,56,95,54,48,0),String.fromCharCode(111,99,116,101,116,95,56,95,57,52,0)]]);
         minimizew = [tooltips3 ^ 3];
         blacklistR = new Map([[`${blacklistR.size}`, blacklistR.size % 3]]);
         soundb = `${(philippinesi == String.fromCharCode(68,0) ? philippinesi.length : minimizew.length)}`;
      stepP = `${w_unlock9.size / (Math.max(6, stepP.length))}`;
   if ((largex.length + parseInt(`${floaterK}`)) <= 5 || 4 <= (largex.length - 5)) {
      floaterK += largex.length;
   }
   for (let j = 0; j < 1; j++) {
      filledO += parseFloat(`${backgroundu.size}`);
   }
   if ((floaterK - with_yG) < 3.11) {
       let sansl = 1;
       let firebaseG = 3.0;
       let analyticA = String.fromCharCode(97,116,114,97,99,95,122,95,56,54,0);
       let eventj = String.fromCharCode(102,111,117,114,120,95,116,95,49,49,0);
          let annerO: Array<any> = [355, 637];
          let pressureh = false;
          let searcha = String.fromCharCode(100,105,102,102,95,57,95,51,0);
         eventj += "2";
         annerO = [annerO.length];
         pressureh = !pressureh;
         searcha = `${searcha.length - 1}`;
         eventj += `${(String.fromCharCode(71,0) == analyticA ? parseInt(`${firebaseG}`) : analyticA.length)}`;
         firebaseG -= 3;
      let controls9 = analyticA == String.fromCharCode(101,117,48,116,52,49,0);
      do {
          let twitter0 = String.fromCharCode(115,105,110,107,115,95,115,95,50,53,0);
         analyticA = "2";
         twitter0 += `${twitter0.length + 3}`;
         if (controls9) {
            break;
         }
      } while (controls9 && (4 == eventj.length));
      if ((5.54 + firebaseG) == 5.14) {
          let views2 = String.fromCharCode(115,95,54,51,95,101,105,103,104,116,115,118,120,0);
          let catagoryw = 1.0;
          let hejim: Array<any> = [298, 646];
          let filed7 = 0.0;
          let dataS = String.fromCharCode(105,95,49,95,109,112,101,103,97,117,100,105,111,116,97,98,0);
         firebaseG /= Math.max(parseInt(`${catagoryw}`) | eventj.length, 3);
         views2 += `${hejim.length}`;
         catagoryw /= Math.max(parseInt(`${filed7}`), 1);
         hejim.push(parseInt(`${filed7}`) | 3);
         dataS = `${hejim.length & 3}`;
      }
      if (1.28 <= firebaseG) {
         firebaseG /= Math.max(1 + eventj.length, 3);
      }
       let m_centerk = true;
       let module9 = false;
          let thailandk = 3;
          let usernameo = true;
         eventj = `${thailandk}`;
         thailandk += ((usernameo ? 1 : 4));
      floaterK -= 3 << (Math.min(Math.abs(parseInt(`${firebaseG}`)), 1));
      sansl ^= sansl;
   }
   let moreL = 9114103.0 <= castinge;
   do {
      castinge -= parseFloat(`${parseInt(`${with_yG}`) ^ 3}`);
      if (moreL) {
         break;
      }
   } while (moreL && ((2 + filledO) > 1.17));
   if ((filledO / (Math.max(castinge, 6))) > 3.87) {
      castinge *= parseFloat(`${mailg}`);
   }
      stepP = `${cornerS.length - parseInt(`${castinge}`)}`;
   if ((mailg % (Math.max(cornerS.length, 6))) == 2 && (mailg % (Math.max(8, cornerS.length))) == 2) {
      cornerS = [parseInt(`${filledO}`)];
   }
   if (5 >= mailg) {
      referrerG = 75.97 < floaterK;
   }
   for (let v = 0; v < 2; v++) {
      stepP += "3";
   }
      referrerG = 56 > cornerS.length;
      largex.push(parseInt(`${filledO}`));
       let submitB = String.fromCharCode(114,95,52,54,95,102,105,108,108,101,100,0);
       let watchI: Map<any, any> = new Map([[String.fromCharCode(118,101,114,116,101,120,95,115,95,55,53,0),false ], [String.fromCharCode(118,95,51,95,101,116,104,114,101,97,100,105,110,103,0),true ], [String.fromCharCode(115,121,109,111,100,100,95,56,95,55,57,0),false ]]);
       let lighth = String.fromCharCode(112,114,111,99,101,115,115,111,114,115,95,120,95,56,49,0);
      if (lighth.length <= submitB.length) {
         submitB = `${watchI.size % 1}`;
      }
          let bingF = false;
          let anythinkX = 4;
          let thumbnailO = 3;
         submitB += "3";
         bingF = !bingF;
         anythinkX /= Math.max(((bingF ? 4 : 5) % (Math.max(2, anythinkX))), 3);
         thumbnailO >>= Math.min(Math.abs(3), 2);
          let delegate_2a: Map<any, any> = new Map([[String.fromCharCode(118,95,56,95,120,116,101,110,115,105,111,110,0),String.fromCharCode(97,109,112,108,105,102,121,95,114,95,54,54,0)], [String.fromCharCode(99,117,101,115,95,51,95,50,57,0),String.fromCharCode(113,95,53,56,95,112,114,101,115,101,110,116,0)], [String.fromCharCode(102,115,121,110,99,95,114,95,55,52,0),String.fromCharCode(113,95,54,48,95,115,99,97,108,101,115,0)]]);
         submitB = `${1 - watchI.size}`;
         delegate_2a.set(`${delegate_2a.size}`, delegate_2a.size);
      let humidityA = 8988141 <= lighth.length;
      do {
         lighth = "3";
         if (humidityA) {
            break;
         }
      } while (humidityA && ((lighth.length & watchI.size) == 5));
       let delegate_uj_ = 2.0;
       let modeli = 3.0;
       let optionsd = 2.0;
       let zhengpian9 = 3.0;
       let analyticso = String.fromCharCode(103,95,53,53,95,101,115,115,101,110,99,101,0);
       let countdown2 = String.fromCharCode(107,95,50,53,95,116,105,100,121,0);
         modeli += countdown2.length & 1;
          let acceptedy = 2.0;
          let emojiC = 1.0;
          let reminderi = String.fromCharCode(98,101,103,97,110,95,50,95,53,53,0);
         lighth = `${reminderi.length}`;
         acceptedy += 3;
         emojiC /= Math.max(parseInt(`${emojiC}`) << (Math.min(5, Math.abs(1))), 4);
         reminderi += `${parseInt(`${emojiC}`)}`;
      backgroundu = new Map([[`${backgroundu.size}`, 3 & backgroundu.size]]);
   if (with_yG < 4.43) {
      filledO += parseFloat(`${3}`);
   }
   for (let k = 0; k < 2; k++) {
       let userL: Array<any> = [String.fromCharCode(111,95,51,55,95,101,120,112,111,115,117,114,101,0), String.fromCharCode(119,104,97,116,95,52,95,51,51,0), String.fromCharCode(100,111,99,107,101,114,95,113,95,50,52,0)];
      for (let c = 0; c < 3; c++) {
         userL.push(userL.length * 3);
      }
      for (let k = 0; k < 1; k++) {
         userL = [userL.length];
      }
         userL = [userL.length / 3];
      backgroundu = new Map([[`${userL.length}`, userL.length - cornerS.length]]);
   }

    Clipboard.setString(userState.user?.userReferralCode ?? '');

       let main_uK = String.fromCharCode(109,101,97,115,117,114,101,109,101,110,116,95,115,95,52,49,0);
         main_uK += `${main_uK.length / (Math.max(9, main_uK.length))}`;
          let controlsS: Array<any> = [733, 255, 455];
         main_uK = `${2 & controlsS.length}`;
      if (main_uK.length > 5) {
         main_uK += `${main_uK.length + main_uK.length}`;
      }
      referrerG = filledO >= 71.31;
      backgroundu.set(`${referrerG}`, 3);
   while ((floaterK / (Math.max(with_yG, 7))) > 3.53 || 1.38 > (3.53 - with_yG)) {
      with_yG += 1;
      break;
   }
      backgroundu.set(`${referrerG}`, w_unlock9.size);
       let friends7 = String.fromCharCode(121,95,50,48,95,103,101,111,99,111,100,101,100,0);
       let mail8 = 3;
       let comment7 = 5.0;
         friends7 += "3 - mail8";
      if ((mail8 & 5) > 3 && 5 > (5 >> (Math.min(5, Math.abs(mail8))))) {
         mail8 /= Math.max(3, 1);
      }
          let mappingZ = String.fromCharCode(102,95,56,49,95,105,110,116,101,114,102,114,97,109,101,0);
          let recommendationT = String.fromCharCode(114,101,102,108,95,116,95,56,49,0);
         mail8 &= mappingZ.length ^ 3;
         mappingZ = `${recommendationT.length << (Math.min(3, recommendationT.length))}`;
       let kickJ: Map<any, any> = new Map([[String.fromCharCode(108,95,56,49,95,97,117,100,0),248], [String.fromCharCode(114,95,52,57,95,112,114,101,100,105,99,116,120,0),650]]);
       let searchbarq = String.fromCharCode(105,110,100,101,120,101,100,95,97,95,56,50,0);
       let loadingd = String.fromCharCode(120,95,50,51,95,99,105,114,99,108,101,0);
      if ((mail8 | friends7.length) >= 5 || (mail8 | 5) >= 4) {
         friends7 += `${friends7.length * 2}`;
      }
      while ((parseInt(`${comment7}`) + mail8) < 3 && (mail8 % 3) < 3) {
          let playJ = 4;
          let gmaile = String.fromCharCode(111,95,55,56,95,112,114,111,98,0);
          let v_locky = String.fromCharCode(118,97,100,100,113,95,108,95,50,52,0);
          let animationB: Map<any, any> = new Map([[String.fromCharCode(97,95,57,54,95,116,104,97,110,0),289], [String.fromCharCode(117,105,110,116,108,101,95,50,95,53,52,0),848]]);
          let favoriteQ = String.fromCharCode(100,105,115,112,97,116,99,104,101,100,95,100,95,57,54,0);
         comment7 -= 1 << (Math.min(4, Math.abs(kickJ.size)));
         playJ &= gmaile.length | v_locky.length;
         gmaile += `${v_locky.length}`;
         animationB = new Map([[v_locky, v_locky.length]]);
         favoriteQ = `${animationB.size << (Math.min(Math.abs(3), 4))}`;
         break;
      }
      while (friends7.length < 3) {
          let statisticsK = String.fromCharCode(122,95,50,56,95,112,97,114,97,109,101,116,101,114,115,0);
          let shirtM = String.fromCharCode(109,112,101,103,112,115,95,50,95,50,51,0);
          let halfR = 1;
          let reducery: Map<any, any> = new Map([[String.fromCharCode(100,95,55,50,95,115,108,111,112,101,0),821], [String.fromCharCode(109,111,118,105,110,103,95,54,95,54,53,0),692], [String.fromCharCode(116,101,108,101,112,104,111,116,111,95,111,95,54,48,0),727]]);
         loadingd = `${(friends7 == String.fromCharCode(90,0) ? friends7.length : halfR)}`;
         statisticsK = `${shirtM.length & 3}`;
         shirtM += `${1 - statisticsK.length}`;
         halfR <<= Math.min(statisticsK.length, 1);
         reducery.set(`${shirtM}`, 2 | shirtM.length);
         break;
      }
      for (let e = 0; e < 1; e++) {
          let orangej = String.fromCharCode(100,117,112,108,105,99,97,116,101,115,95,55,95,55,48,0);
         mail8 -= mail8;
         orangej = `${(orangej == String.fromCharCode(83,0) ? orangej.length : orangej.length)}`;
      }
      mailg <<= Math.min(Math.abs(cornerS.length + 2), 5);
   while (1 > (backgroundu.size / (Math.max(7, parseInt(`${floaterK}`)))) || 1.8 > (5.85 / (Math.max(3, floaterK)))) {
      floaterK -= mailg;
      break;
   }
      floaterK += largex.length | 1;
   if ((floaterK - 3.94) < 3.60 || (3.94 - floaterK) < 2.73) {
      castinge += parseFloat(`${2 * backgroundu.size}`);
   }
   for (let b = 0; b < 3; b++) {
      mailg *= 1 * mailg;
   }
       let incidente = String.fromCharCode(99,111,100,101,114,115,95,120,95,50,52,0);
      if (incidente.includes(`${incidente.length}`)) {
         incidente += `${incidente.length ^ incidente.length}`;
      }
       let expiredY = true;
         expiredY = incidente.length > 81;
      castinge /= Math.max(4, parseFloat(`${largex.length}`));
   for (let x = 0; x < 1; x++) {
       let volumea = 2.0;
       let buttonF = 3;
      if ((buttonF * parseInt(`${volumea}`)) >= 4 || (4 % (Math.max(5, buttonF))) >= 5) {
         volumea *= parseInt(`${volumea}`);
      }
      if (volumea == buttonF) {
         buttonF %= Math.max(4, buttonF);
      }
      while (volumea < buttonF) {
         volumea += 1;
         break;
      }
          let feedbackK = String.fromCharCode(112,101,114,115,111,110,95,120,95,51,51,0);
          let bodanD = 2.0;
          let downloadP = String.fromCharCode(109,97,120,105,109,117,109,95,121,95,56,53,0);
         buttonF <<= Math.min(downloadP.length, 3);
         feedbackK += `${feedbackK.length - parseInt(`${bodanD}`)}`;
         bodanD -= parseInt(`${bodanD}`) ^ feedbackK.length;
         downloadP += `${parseInt(`${bodanD}`) & feedbackK.length}`;
      let middlewares = 9823955 <= buttonF;
      do {
         buttonF %= Math.max(4, 2);
         if (middlewares) {
            break;
         }
      } while ((4 == (buttonF + 5) || (buttonF * 5) == 4) && middlewares);
      let window_kQ = buttonF >= 9378799;
      do {
          let networkz = 3;
          let serviceC = 2.0;
          let expand5: Array<any> = [482, 858];
          let previewG: Array<any> = [272, 127];
         buttonF ^= expand5.length;
         networkz -= 2 | parseInt(`${serviceC}`);
         serviceC *= parseFloat(`${networkz / (Math.max(parseInt(`${serviceC}`), 10))}`);
         expand5.push(previewG.length + parseInt(`${serviceC}`));
         previewG = [networkz];
         if (window_kQ) {
            break;
         }
      } while (window_kQ && ((volumea / 5.25) > 1.24));
      mailg /= Math.max(2, backgroundu.size);
   }
       let filterq = 1.0;
       let scheduleT = String.fromCharCode(122,95,56,50,95,109,97,112,108,105,109,105,116,0);
       let placeholder3 = String.fromCharCode(112,108,97,99,101,115,95,117,95,54,54,0);
         placeholder3 += `${scheduleT.length / (Math.max(8, parseInt(`${filterq}`)))}`;
      for (let y = 0; y < 3; y++) {
         scheduleT += `${scheduleT.length}`;
      }
      while (!placeholder3.startsWith(scheduleT)) {
         placeholder3 = `${1 + parseInt(`${filterq}`)}`;
         break;
      }
         scheduleT = `${placeholder3.length}`;
         scheduleT += `${(String.fromCharCode(108,0) == scheduleT ? scheduleT.length : parseInt(`${filterq}`))}`;
         scheduleT += `${placeholder3.length ^ scheduleT.length}`;
       let memberships = 1.0;
          let gemfileq = 4.0;
          let applex = 3.0;
          let tickM = 4.0;
         scheduleT = `${parseInt(`${applex}`) % (Math.max(placeholder3.length, 9))}`;
         gemfileq *= parseInt(`${gemfileq}`) ^ parseInt(`${tickM}`);
         applex *= parseFloat(`${2 * parseInt(`${tickM}`)}`);
      if (4 == (scheduleT.length - 3) || (filterq - 3.29) == 4.57) {
         scheduleT += `${placeholder3.length % 3}`;
      }
      filledO /= Math.max(parseFloat(`${3}`), 2);
   while (1 <= acceptedz.length) {
      acceptedz.push(1 & parseInt(`${filledO}`));
      break;
   }
   for (let d = 0; d < 3; d++) {
       let referrerGd = String.fromCharCode(99,111,110,118,101,110,105,101,110,99,101,95,49,95,50,55,0);
       let sliderC = 3;
       let termsr = 5;
      while (2 > (termsr & sliderC) || (2 & termsr) > 5) {
         sliderC >>= Math.min(Math.abs(referrerGd.length - sliderC), 5);
         break;
      }
      for (let z = 0; z < 2; z++) {
          let edito = String.fromCharCode(101,108,101,109,95,122,95,50,49,0);
          let configure2 = 1;
          let h_titled = 3.0;
          let anythink3 = true;
         sliderC |= 1;
         edito = `${2 << (Math.min(3, edito.length))}`;
         configure2 += edito.length << (Math.min(Math.abs(1), 1));
         h_titled *= parseInt(`${h_titled}`);
         anythink3 = 57.63 == h_titled;
      }
      castinge *= parseFloat(`${backgroundu.size}`);
   }
   for (let w = 0; w < 2; w++) {
       let showd = 2.0;
       let areaq: Array<any> = [324, 461];
       let greyv = true;
       let otherL = String.fromCharCode(99,111,100,101,114,115,95,112,95,56,53,0);
          let giftc = String.fromCharCode(108,111,97,100,95,122,95,55,50,0);
          let uploadb = String.fromCharCode(97,97,117,100,105,111,95,107,95,54,49,0);
         areaq.push((1 << (Math.min(1, Math.abs((greyv ? 5 : 5))))));
         giftc = `${giftc.length}`;
         uploadb += `${uploadb.length >> (Math.min(Math.abs(3), 3))}`;
      if ((areaq.length % 1) == 5 || 4.58 == (showd + 1.37)) {
         areaq.push(otherL.length + 1);
      }
          let controlc: Array<any> = [674, 928, 729];
         greyv = !greyv && showd < 72.53;
         controlc.push(3 | controlc.length);
      while (areaq.includes(showd)) {
          let desc7 = String.fromCharCode(114,95,55,51,95,109,98,98,121,0);
          let mutedI = false;
          let emptyr: Array<any> = [56, 340, 875];
         showd -= areaq.length;
         desc7 += `${((mutedI ? 1 : 4) % (Math.max(emptyr.length, 4)))}`;
         mutedI = emptyr.includes(mutedI);
         break;
      }
         areaq = [parseInt(`${showd}`) - 3];
      let d_titleY = greyv ? !greyv : greyv;
      do {
          let skip2 = 5;
          let minimizep8 = 0.0;
          let time_jcK = String.fromCharCode(105,95,49,48,95,105,109,112,108,105,99,105,116,101,108,121,0);
          let matchesf = true;
          let selectD = String.fromCharCode(100,101,97,99,116,105,118,97,116,105,111,110,95,110,95,52,52,0);
         greyv = ((otherL.length + (!greyv ? otherL.length : 90)) <= 90);
         skip2 <<= Math.min(Math.abs((String.fromCharCode(99,0) == selectD ? (matchesf ? 2 : 5) : selectD.length)), 1);
         minimizep8 -= time_jcK.length;
         time_jcK = `${((matchesf ? 3 : 5) << (Math.min(Math.abs(1), 4)))}`;
         if (d_titleY) {
            break;
         }
      } while (((5.71 + showd) == 1.87 || showd == 5.71) && d_titleY);
      while ((otherL.length * parseInt(`${showd}`)) <= 2 && (showd * 5.13) <= 5.92) {
         showd *= (String.fromCharCode(89,0) == otherL ? otherL.length : parseInt(`${showd}`));
         break;
      }
         greyv = otherL.length <= areaq.length;
      let alert8 = otherL == String.fromCharCode(121,98,112,114,104,110,0);
      do {
         otherL = `${(otherL == String.fromCharCode(99,0) ? (greyv ? 5 : 4) : otherL.length)}`;
         if (alert8) {
            break;
         }
      } while (alert8 && (2 > otherL.length));
       let grayN = String.fromCharCode(113,95,53,55,95,112,111,115,116,105,110,103,0);
      let pingI = areaq.length >= 7636535;
      do {
         areaq.push(otherL.length);
         if (pingI) {
            break;
         }
      } while ((5 <= areaq.length && (areaq.length - 5) <= 5) && pingI);
         areaq.push(grayN.length + 3);
      w_unlock9 = new Map([[`${areaq.length}`, parseInt(`${showd}`)]]);
   }
      referrerG = w_unlock9.get(`${floaterK}`) != null;
    setIsDialogOpen(true);
  }

  return (
    <ScreenContainer>
      <TitleWithBackButtonHeader title='个人中心' />
      <View
        style={{
          justifyContent: 'space-between',
          flex: 1,
          paddingHorizontal: 10,
        }}
      >
        <View style={{ marginTop: 30 }}>
          <TouchableOpacity activeOpacity={0.85}
            style={styles.pressableTextContainer}
            onPress={() => {
              SMBing.showToast('目前暂不支持更改头像，敬请期待');
            }}
          >
            <Text style={{ fontSize: 16, color: '#9C9C9C' }}>头像</Text>
            <FastImage
              style={{
                height: 34,
                width: 34,
              }}
              source={require('@static/images/actionSaveBackward.png')}
            />
          </TouchableOpacity>

          { }
          <TouchableOpacity activeOpacity={0.85}
            style={styles.pressableTextContainer}
            onPress={onPressUsername}
          >
            <Text style={{ fontSize: 16, color: colors.text }}>
              {username}
            </Text>
          </TouchableOpacity>

          { }
          <View style={styles.pressableTextContainer}>
            <Text style={{ fontSize: 16, color: '#9C9C9C' }}>
              {userState.user?.userEmail !== '' ? userState.user?.userEmail : userState.user?.userPhoneNumber}
            </Text>
          </View>

          { }
          {userState.user?.userReferrerName == '' &&
            userState.user.userAllowUpdateReferral == true &&
            <TouchableOpacity activeOpacity={0.85}
              style={styles.pressableTextContainer}
              onPress={onPressReferrer}
            >
              <Text style={{ fontSize: 16, color: '#B6B6B6' }}>
                补填邀请码 (只能填写一次)
              </Text>
            </TouchableOpacity>
          }

          {userState.user?.userReferrerName != '' && (
            <View style={styles.pressableTextContainer}>
              <Text style={{ fontSize: 16, color: '#9C9C9C' }}>推介人</Text>

              <View
                style={{
                  flexDirection: 'row',
                  justifyContent: 'center',
                  alignItems: 'center',
                }}
              >
                <Text style={{ fontSize: 14, color: '#9C9C9C' }}>
                  {userState.user?.userReferrerName}
                </Text>
              </View>
            </View>
          )}

          { }
          <TouchableOpacity activeOpacity={0.85}
            style={styles.pressableTextContainer}
            onPress={onCopyReferralCode}
          >
            <Text style={{ fontSize: 16, color: colors.primary }}>
              {userState.user?.userReferralCode}
            </Text>

            <View
              style={{
                flexDirection: 'row',
                justifyContent: 'center',
                alignItems: 'center',
              }}
            >
              <Text style={{ fontSize: 14, color: '#9C9C9C' }}>
                复制邀请码
              </Text>
              <Image
                style={{
                  height: 27,
                  width: 27,
                  position: 'relative',
                  top: 2,
                }}
                source={require('@static/images/profile/stepAgreement.png')}
              />
            </View>
          </TouchableOpacity>

          <NotificationModal
            onConfirm={toggleOverlay}
            isVisible={isDialogOpen}
            title='复制成功'
            subtitle1=''
            subtitle2=''
            subtitle3=''
          />
        </View>

        <ChangeUsernameModal
          isVisible={isShowUsernameModal}
          handleClose={() => {
            setShowUsernameModal(false);
          }}
          initialUsername={username}
          onSubmitSuccess={onUsernameUpdateSuccess}
        />

        <ChangeReferrerModal
          isVisible={isShowReferrerModal}
          handleClose={() => {
            setShowReferrerModal(false);
          }}
        />
      </View>
    </ScreenContainer>
  );
};

const createStyles = ({ dark }: any) => StyleSheet.create({
  pressableTextContainer: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    backgroundColor: dark ? "#1d2023" : '#D9D9D9',
    marginTop: 20,
    paddingLeft: 18,
    paddingRight: 13,
    height: 48,
    borderRadius: 8,
    marginBottom: 20,
  },
});
