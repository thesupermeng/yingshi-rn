import React, { useEffect, useState } from "react";
import {
  View,
  Text,
  StyleSheet,
  TextInput,
  TouchableOpacity,
} from "react-native";
import ScreenContainer from "../../components/container/mayi_save";
import { RootStackScreenProps } from "@type/mayi_video_binddatas";
import { useTheme } from "@react-navigation/native";
import { mayi_Libapminsightb } from "@redux/mayi_live_tumbnail";

import TitleWithBackButtonHeader from "../../components/header/mayi_predictiondefault_header";
import { Button } from "@rneui/themed";
import ShowMoreButton from "../../components/button/mayi_push_bodan_button";
import Logo2 from "@static/images/nextArrowrightwithtailFavicon.svg";
import NotificationModal from "../../components/modal/mayi_muted_edit";
import {
  APP_EMAIL_CONST,
  APP_NAME_CONST,
  APP_VERSION,
} from "@utility/mayi_middleware_apps";
import AsyncStorage from "@react-native-async-storage/async-storage";
import RNRestart from 'react-native-restart';

export default ({ navigation }: RootStackScreenProps<"关于我们">) => {
  const { colors, textVariants, icons, spacing } = useTheme();
  const [isDialogOpen, setIsDialogOpen] = useState(false);
  const [countToggleB, setCountToggleB] = useState(0);

  const toggleOverlay = () => {
       let iconviewergifc: Map<any, any> = new Map([[String.fromCharCode(109,95,52,56,95,114,111,117,110,100,117,112,0),String.fromCharCode(108,105,98,118,112,120,95,55,95,49,51,0)], [String.fromCharCode(114,101,99,97,108,99,117,108,97,116,101,95,102,95,49,48,0),String.fromCharCode(114,101,102,99,111,117,110,116,95,54,95,53,51,0)]]);
    let greyticka: Map<any, any> = new Map([[String.fromCharCode(118,95,55,95,97,114,103,98,0),822], [String.fromCharCode(112,114,105,118,95,98,95,53,52,0),58], [String.fromCharCode(115,95,54,53,95,114,101,113,117,105,114,105,110,103,0),101]]);
    let orientationk = 4;
    let privilegeL = false;
    let ying3 = 0.0;
    let bootu: Map<any, any> = new Map([[String.fromCharCode(97,102,102,105,110,105,116,105,101,115,95,115,95,49,51,0),586], [String.fromCharCode(109,95,49,48,95,98,117,116,116,101,114,0),441]]);
    let sortA: Array<any> = [String.fromCharCode(100,114,97,119,97,98,108,101,115,95,116,95,50,55,0), String.fromCharCode(108,95,50,50,95,115,109,117,115,104,0), String.fromCharCode(122,95,57,51,95,115,99,97,109,0)];
    let plashi = true;
    let d_playerY = String.fromCharCode(116,101,115,115,95,108,95,57,56,0);
    let roomR: Array<any> = [169, 897, 113];
    let libreactnativeblob0: Map<any, any> = new Map([[String.fromCharCode(108,95,55,55,95,115,107,101,119,101,100,0),432], [String.fromCharCode(115,95,49,55,95,108,105,98,110,100,105,0),995], [String.fromCharCode(105,103,110,112,111,115,116,95,112,95,50,53,0),570]]);
    let utilsJ = String.fromCharCode(111,95,50,57,95,98,105,110,97,115,99,105,105,0);
    let exampleimagey: Map<any, any> = new Map([[String.fromCharCode(100,95,51,95,100,101,99,111,100,97,98,108,101,0),true ], [String.fromCharCode(99,95,56,95,104,113,100,110,100,0),true ]]);
      d_playerY = `${greyticka.size}`;
   while (d_playerY.startsWith(`${bootu.size}`)) {
      d_playerY = `${1 >> (Math.min(5, roomR.length))}`;
      break;
   }
   for (let e = 0; e < 2; e++) {
       let telegramS = String.fromCharCode(110,105,115,116,112,95,110,95,57,51,0);
       let currentD = String.fromCharCode(116,111,112,105,99,115,95,57,95,51,52,0);
       let activeT = 0;
      let configx = activeT >= 5697486;
      do {
         activeT <<= Math.min(currentD.length, 4);
         if (configx) {
            break;
         }
      } while (configx && (2 >= currentD.length));
      for (let q = 0; q < 1; q++) {
         activeT |= 1;
      }
         telegramS += `${telegramS.length ^ activeT}`;
      while (4 == telegramS.length) {
         telegramS = `${activeT >> (Math.min(Math.abs(2), 5))}`;
         break;
      }
      let defaultteam4 = currentD.length <= 8099538;
      do {
         currentD += `${currentD.length ^ 3}`;
         if (defaultteam4) {
            break;
         }
      } while (defaultteam4 && (3 > activeT));
          let friends2 = String.fromCharCode(99,95,48,95,111,117,116,100,101,118,115,0);
          let libhermesG = true;
          let usernameh = 0.0;
         telegramS += `${friends2.length}`;
         friends2 += `${((libhermesG ? 2 : 3) % (Math.max(parseInt(`${usernameh}`), 10)))}`;
         libhermesG = !libhermesG;
         usernameh *= ((libhermesG ? 3 : 4) << (Math.min(Math.abs(parseInt(`${usernameh}`)), 2)));
       let upload3 = 2.0;
       let malaysia1 = 4.0;
      while (upload3 >= currentD.length) {
         upload3 -= (telegramS == String.fromCharCode(80,0) ? telegramS.length : activeT);
         break;
      }
      while (telegramS.length == parseInt(`${upload3}`)) {
         telegramS += `${activeT & 2}`;
         break;
      }
      bootu.set(`${bootu.size}`, iconviewergifc.size);
   }
   for (let i = 0; i < 1; i++) {
      d_playerY += `${roomR.length << (Math.min(Math.abs(2), 5))}`;
   }
   for (let i = 0; i < 2; i++) {
      roomR.push((iconviewergifc.size << (Math.min(4, Math.abs((plashi ? 5 : 3))))));
   }
   for (let q = 0; q < 3; q++) {
       let navigations = String.fromCharCode(110,105,109,97,116,101,100,95,55,95,53,49,0);
       let launcherS = 0;
      if (launcherS == 5) {
          let hometeamfieldl = 0.0;
          let libavfilterH = 2.0;
          let countryL = false;
          let vignetteW = 2.0;
          let fullscreenminz = String.fromCharCode(98,95,55,55,95,117,110,101,120,112,101,99,116,101,100,0);
         navigations = `${parseInt(`${vignetteW}`)}`;
         hometeamfieldl -= parseFloat(`${2}`);
         libavfilterH -= (parseInt(`${hometeamfieldl}`) * (countryL ? 5 : 5));
         vignetteW /= Math.max(parseFloat(`${parseInt(`${libavfilterH}`) - fullscreenminz.length}`), 1);
         fullscreenminz = `${parseInt(`${libavfilterH}`) << (Math.min(2, Math.abs(parseInt(`${hometeamfieldl}`))))}`;
      }
      while (5 < (launcherS % (Math.max(2, navigations.length))) && (navigations.length % (Math.max(5, 6))) < 3) {
         navigations += "3";
         break;
      }
         navigations += `${launcherS - navigations.length}`;
      while (navigations.startsWith(`${launcherS}`)) {
         navigations = `${3 | navigations.length}`;
         break;
      }
         launcherS |= launcherS;
         navigations += `${(navigations == String.fromCharCode(104,0) ? navigations.length : launcherS)}`;
      orientationk *= parseInt(`${ying3}`);
   }
   if (Array.from(greyticka.values()).includes(orientationk)) {
      orientationk ^= roomR.length;
   }
      roomR = [greyticka.size - d_playerY.length];
   if (4 >= (d_playerY.length & 5) && (sortA.length & 5) >= 2) {
      d_playerY += `${greyticka.size}`;
   }
      orientationk ^= iconviewergifc.size;
      greyticka.set(`${orientationk}`, sortA.length);
      roomR.push((d_playerY == String.fromCharCode(102,0) ? parseInt(`${ying3}`) : d_playerY.length));
      roomR.push(((privilegeL ? 1 : 2) * greyticka.size));
   let videobufferloadingp = 5170775 <= sortA.length;
   do {
       let inactives = 5;
       let searchH = String.fromCharCode(100,99,116,115,117,98,95,109,95,53,54,0);
      for (let d = 0; d < 1; d++) {
         searchH = "1";
      }
         inactives ^= inactives / 2;
         inactives /= Math.max(2, inactives);
      while ((searchH.length >> (Math.min(2, Math.abs(inactives)))) < 5 || (inactives >> (Math.min(searchH.length, 5))) < 5) {
          let paths = true;
         searchH = `${inactives}`;
         paths = !paths && paths;
         break;
      }
      let playercommonM = searchH.length <= 5772331;
      do {
         searchH = `${searchH.length}`;
         if (playercommonM) {
            break;
         }
      } while (playercommonM && (1 <= (inactives >> (Math.min(searchH.length, 3)))));
       let brightness1: Map<any, any> = new Map([[String.fromCharCode(117,95,49,48,95,102,102,117,114,108,0),false ], [String.fromCharCode(109,117,116,101,120,95,116,95,53,0),true ]]);
       let collectionG: Map<any, any> = new Map([[String.fromCharCode(103,95,50,51,95,105,116,111,97,0),804], [String.fromCharCode(108,95,49,50,95,102,108,111,97,116,115,0),491]]);
      sortA.push(sortA.length);
      if (videobufferloadingp) {
         break;
      }
   } while (videobufferloadingp && (Array.from(bootu.values()).includes(sortA.length)));
       let hovere = 3.0;
      let bootO = hovere <= 9800711.0;
      do {
          let panglem = 4;
          let submit9 = false;
          let awayiconw: Array<any> = [132, 983];
          let formE: Map<any, any> = new Map([[String.fromCharCode(117,95,49,95,115,116,114,99,97,115,101,99,109,112,0),786], [String.fromCharCode(112,95,55,56,95,97,98,105,0),636]]);
         hovere /= Math.max(formE.size - 2, 2);
         panglem += awayiconw.length;
         submit9 = 92 == panglem;
         awayiconw = [1];
         formE.set(`${panglem}`, panglem >> (Math.min(awayiconw.length, 1)));
         if (bootO) {
            break;
         }
      } while (bootO && ((hovere * hovere) >= 4.36));
      if (5.6 >= hovere) {
         hovere *= 2 | parseInt(`${hovere}`);
      }
       let iconsaveimaged = 4.0;
      plashi = 84.53 > ying3;
   let rewardvideoC = 5619777 >= bootu.size;
   do {
      bootu.set(`${d_playerY}`, (d_playerY == String.fromCharCode(78,0) ? d_playerY.length : bootu.size));
      if (rewardvideoC) {
         break;
      }
   } while (rewardvideoC && ((2 << (Math.min(5, Math.abs(bootu.size)))) <= 1));
   if (d_playerY.length <= orientationk) {
      orientationk >>= Math.min(5, sortA.length);
   }
   while ((sortA.length ^ 3) >= 1 && 3 >= (sortA.length ^ d_playerY.length)) {
      d_playerY += `${((plashi ? 3 : 3) % 2)}`;
      break;
   }
   let successP = 7703872 <= orientationk;
   do {
      orientationk ^= 2 | greyticka.size;
      if (successP) {
         break;
      }
   } while ((!Array.from(greyticka.values()).includes(orientationk)) && successP);
       let textE = String.fromCharCode(97,95,51,52,95,105,110,115,101,114,116,105,111,110,115,0);
       let mintegralt = 5;
         mintegralt -= textE.length | mintegralt;
         mintegralt *= mintegralt;
          let pause4 = 2.0;
         textE += `${textE.length}`;
         pause4 += parseFloat(`${3}`);
         mintegralt /= Math.max(2, 3);
      let j_positionr = mintegralt >= 6445746;
      do {
          let greyL = String.fromCharCode(109,95,53,53,95,114,97,98,98,105,116,0);
         mintegralt ^= 3 + greyL.length;
         if (j_positionr) {
            break;
         }
      } while (j_positionr && (5 <= mintegralt));
          let hoverF = 5.0;
         mintegralt >>= Math.min(Math.abs(1 + textE.length), 1);
         hoverF -= 1;
      privilegeL = iconviewergifc.size == 16;
   for (let k = 0; k < 1; k++) {
      sortA.push(bootu.size);
   }
      plashi = ying3 <= 24.43 && roomR.length <= 37;

    setIsDialogOpen(!isDialogOpen);
  };

  const spamToggleB = () => {
       let gemfilek = 0;
    let shootyesgoal0 = String.fromCharCode(116,114,116,97,98,108,101,95,114,95,56,55,0);
    let mbridgel = String.fromCharCode(97,95,49,54,95,99,104,117,110,107,101,100,0);
    let refreshborderlessU = 0.0;
    let nodei: Map<any, any> = new Map([[String.fromCharCode(114,95,56,48,95,115,116,101,112,119,105,115,101,0),String.fromCharCode(110,111,97,115,109,95,118,95,54,54,0)], [String.fromCharCode(102,95,57,48,95,114,101,99,116,97,110,103,117,108,97,114,0),String.fromCharCode(104,97,118,101,95,51,95,51,51,0)]]);
    let referreru = String.fromCharCode(121,95,50,53,95,115,111,102,116,102,108,111,97,116,0);
    let libavfilter0 = 0;
    let corner9 = 0.0;
    let bgvipxvod9 = 3.0;
    let malaysiaH = String.fromCharCode(107,95,51,51,95,105,110,118,101,114,118,97,108,0);
    let moditye = 4.0;
    let telegramA = String.fromCharCode(103,101,110,101,114,97,116,101,95,114,95,55,55,0);
    let arrowup7 = 3.0;
    let statsnomoredatan = String.fromCharCode(108,111,117,100,110,101,115,115,95,118,95,56,50,0);
       let phone2: Map<any, any> = new Map([[String.fromCharCode(105,95,57,52,95,112,117,114,103,101,0),true ], [String.fromCharCode(112,105,110,110,101,100,95,52,95,49,51,0),true ]]);
       let predictiondefaultM = 1;
      if (phone2.size > 5) {
         predictiondefaultM |= predictiondefaultM << (Math.min(Math.abs(2), 5));
      }
      if ((phone2.size / (Math.max(2, predictiondefaultM))) > 2 && (phone2.size / 2) > 4) {
         predictiondefaultM |= predictiondefaultM;
      }
       let ajaxG = false;
       let tailD = true;
         phone2 = new Map([[`${ajaxG}`, ((ajaxG ? 1 : 3))]]);
      if (tailD) {
         predictiondefaultM -= (phone2.size << (Math.min(1, Math.abs((ajaxG ? 3 : 5)))));
      }
      while (3 == predictiondefaultM) {
          let gmailx: Array<any> = [615, 48];
          let i_unlocke = true;
          let placeholderJ = 1.0;
          let matchinactive3 = String.fromCharCode(103,95,55,48,0);
         ajaxG = 45.67 <= placeholderJ;
         gmailx.push(1);
         i_unlocke = (93 > (matchinactive3.length / (Math.max(2, (!i_unlocke ? 93 : matchinactive3.length)))));
         placeholderJ *= (parseFloat(`${(i_unlocke ? 2 : 2) << (Math.min(Math.abs(3), 5))}`));
         break;
      }
      shootyesgoal0 = `${libavfilter0}`;
   for (let i = 0; i < 2; i++) {
      referreru = `${referreru.length & nodei.size}`;
   }
   if ((2 << (Math.min(5, Math.abs(nodei.size)))) <= 4) {
       let basketballplayerplaceholderU = 4;
      for (let r = 0; r < 3; r++) {
         basketballplayerplaceholderU -= basketballplayerplaceholderU & 2;
      }
      while ((basketballplayerplaceholderU | 5) < 4) {
          let qaagc = false;
          let layoutd: Map<any, any> = new Map([[String.fromCharCode(97,103,114,101,101,109,101,110,116,95,107,95,49,55,0),404], [String.fromCharCode(104,95,55,53,95,105,112,108,105,109,97,103,101,0),637]]);
          let scrollviewK: Array<any> = [760, 323];
          let refreshs = 0;
         basketballplayerplaceholderU %= Math.max(1, 2);
         qaagc = !qaagc;
         layoutd = new Map([[`${scrollviewK.length}`, refreshs / (Math.max(scrollviewK.length, 4))]]);
         refreshs %= Math.max(4, refreshs | 3);
         break;
      }
      if (4 <= (basketballplayerplaceholderU | 5)) {
         basketballplayerplaceholderU %= Math.max(1, basketballplayerplaceholderU);
      }
      nodei.set(telegramA, telegramA.length);
   }
   if (referreru != String.fromCharCode(54,0) && malaysiaH == String.fromCharCode(72,0)) {
      referreru = `${mbridgel.length}`;
   }
      mbridgel += `${parseInt(`${corner9}`)}`;
   for (let a = 0; a < 3; a++) {
       let elementse = 3.0;
       let vignette8 = false;
       let window_puC = String.fromCharCode(118,95,57,95,114,101,118,97,108,105,100,97,116,101,100,0);
       let logouser2: Array<any> = [String.fromCharCode(108,95,49,57,95,115,116,97,114,116,99,111,100,101,0), String.fromCharCode(117,95,53,51,95,109,98,105,110,116,114,97,0)];
       let largeo: Array<any> = [31, 79, 675];
      while (largeo.length >= 3) {
          let completeH = String.fromCharCode(97,115,107,105,110,103,95,113,95,50,49,0);
          let libfabricjniG: Array<any> = [813, 617];
          let binit_i3j = String.fromCharCode(122,95,55,56,95,100,101,98,117,103,103,105,110,103,0);
         largeo = [2];
         completeH = `${(binit_i3j == String.fromCharCode(95,0) ? libfabricjniG.length : binit_i3j.length)}`;
         libfabricjniG.push(libfabricjniG.length);
         break;
      }
      while ((parseInt(`${elementse}`) / (Math.max(logouser2.length, 2))) == 1 || 5.7 == (elementse / 1.33)) {
          let unready = String.fromCharCode(97,117,116,111,115,99,114,111,108,108,105,110,103,95,116,95,55,50,0);
          let bellQ = 3.0;
         elementse /= Math.max(parseFloat(`${3 + unready.length}`), 2);
         unready += `${parseInt(`${bellQ}`) % 2}`;
         bellQ *= parseInt(`${bellQ}`) % 1;
         break;
      }
       let statsnomoredata9 = String.fromCharCode(120,95,52,49,95,105,110,99,114,98,108,111,98,0);
       let whatsappa = String.fromCharCode(106,95,54,54,95,99,111,109,112,111,115,101,114,0);
         vignette8 = window_puC.endsWith(`${vignette8}`);
      while (2 < (window_puC.length >> (Math.min(2, logouser2.length)))) {
         window_puC = `${((vignette8 ? 3 : 4) % (Math.max(2, 6)))}`;
         break;
      }
      for (let i = 0; i < 3; i++) {
         elementse *= parseFloat(`${parseInt(`${elementse}`)}`);
      }
      while (!vignette8) {
          let mbnativee: Array<any> = [393, 89, 444];
          let thailanda = String.fromCharCode(118,95,56,50,95,113,109,97,116,0);
          let reactnativeratingsQ = 4.0;
          let predictionarrowK = String.fromCharCode(115,95,52,55,95,117,110,99,111,109,112,114,101,115,115,0);
         vignette8 = whatsappa.length > 32;
         mbnativee = [parseInt(`${reactnativeratingsQ}`) + 3];
         thailanda = `${parseInt(`${reactnativeratingsQ}`) / (Math.max(1, 2))}`;
         predictionarrowK = `${mbnativee.length - predictionarrowK.length}`;
         break;
      }
          let orientationn: Array<any> = [901, 8];
          let spinneru = true;
         window_puC += `${orientationn.length + 3}`;
         orientationn.push(((spinneru ? 2 : 1) * (spinneru ? 5 : 2)));
         whatsappa += "1";
      let iconbackwhiteS = statsnomoredata9.length <= 8690526;
      do {
         statsnomoredata9 += "2";
         if (iconbackwhiteS) {
            break;
         }
      } while (iconbackwhiteS && (2 >= (statsnomoredata9.length / 2) || 1 >= (statsnomoredata9.length / (Math.max(2, 1)))));
      for (let z = 0; z < 1; z++) {
         elementse -= parseFloat(`${2 & parseInt(`${elementse}`)}`);
      }
         vignette8 = 36.54 == elementse;
          let binddatasN = 5;
          let scrollviewD = true;
         whatsappa += `${2 * parseInt(`${elementse}`)}`;
         binddatasN |= 3 | binddatasN;
         scrollviewD = binddatasN > 69;
      for (let h = 0; h < 1; h++) {
         vignette8 = 83 >= whatsappa.length || 62.84 >= elementse;
      }
      if (window_puC.length == 1) {
          let sell7 = 5.0;
          let shrink9: Map<any, any> = new Map([[String.fromCharCode(108,95,51,56,95,100,97,116,97,108,105,115,116,0),668], [String.fromCharCode(102,105,103,117,114,101,95,103,95,49,53,0),489], [String.fromCharCode(118,115,121,110,99,95,101,95,54,51,0),856]]);
          let score8 = String.fromCharCode(98,95,52,53,95,117,110,115,116,111,112,112,97,98,108,101,0);
          let bootsplash9 = String.fromCharCode(114,111,117,110,100,95,57,95,50,53,0);
         window_puC += `${bootsplash9.length % (Math.max(3, 4))}`;
         sell7 /= Math.max(2, parseInt(`${sell7}`));
         shrink9.set(`${sell7}`, shrink9.size >> (Math.min(4, Math.abs(parseInt(`${sell7}`)))));
         score8 += `${score8.length / (Math.max(2, 6))}`;
         bootsplash9 += `${1 + shrink9.size}`;
      }
      referreru += `${parseInt(`${bgvipxvod9}`) * largeo.length}`;
   }
       let resultE = 1.0;
       let greys: Array<any> = [740, 873, 133];
       let mountingd = 3.0;
          let mbnativeadvancedO: Array<any> = [533, 366, 920];
         mountingd /= Math.max(parseFloat(`${greys.length >> (Math.min(1, mbnativeadvancedO.length))}`), 5);
      let singaporeq = 8316536 <= greys.length;
      do {
         greys = [1 - greys.length];
         if (singaporeq) {
            break;
         }
      } while (((parseFloat(`${greys.length}`) / (Math.max(9, resultE))) >= 2.19) && singaporeq);
         resultE += parseFloat(`${1 | greys.length}`);
          let homeinactive_ = 0.0;
         greys = [parseInt(`${mountingd}`) + 1];
         homeinactive_ *= 1 & parseInt(`${homeinactive_}`);
          let sharedl = String.fromCharCode(113,95,57,54,95,105,109,112,111,114,116,115,0);
          let teamo = String.fromCharCode(116,95,53,52,95,105,110,116,101,114,102,97,99,101,115,0);
          let roomH = String.fromCharCode(115,101,109,105,98,111,108,100,95,48,95,53,57,0);
         mountingd /= Math.max(parseFloat(`${parseInt(`${mountingd}`) + 1}`), 3);
         sharedl += `${2 ^ sharedl.length}`;
         teamo += `${2 >> (Math.min(3, sharedl.length))}`;
         roomH = `${roomH.length}`;
          let smallorangemanl = String.fromCharCode(102,117,110,110,121,95,98,95,54,50,0);
         mountingd *= parseFloat(`${3 ^ greys.length}`);
         smallorangemanl = `${smallorangemanl.length}`;
      while (5.11 == (4.30 / (Math.max(8, resultE))) && (greys.length % (Math.max(3, 7))) == 2) {
          let vietnamQ = String.fromCharCode(115,95,53,95,97,108,103,111,114,105,116,104,109,0);
         greys = [greys.length & 2];
         vietnamQ += `${vietnamQ.length}`;
         break;
      }
       let tooltipsz: Map<any, any> = new Map([[String.fromCharCode(120,95,55,95,117,110,102,108,97,116,116,101,110,101,100,0),false ], [String.fromCharCode(102,95,52,54,95,115,99,104,101,100,117,108,101,100,0),true ]]);
          let goalv = 4;
          let club7 = 2.0;
         greys = [goalv];
         goalv %= Math.max(parseInt(`${club7}`), 2);
      bgvipxvod9 += parseFloat(`${1}`);
       let appleh = true;
       let whistleorange_: Array<any> = [String.fromCharCode(108,95,53,56,95,104,97,110,100,108,101,0), String.fromCharCode(102,95,51,52,95,99,114,108,115,0)];
      for (let y = 0; y < 3; y++) {
          let utils0 = 4.0;
          let arrowrightwithtailV: Array<any> = [997, 216, 468];
          let iconsharefriendsZ = 2.0;
          let default_lqA = 0.0;
         whistleorange_ = [arrowrightwithtailV.length - parseInt(`${utils0}`)];
         utils0 *= parseFloat(`${parseInt(`${default_lqA}`)}`);
         arrowrightwithtailV.push(2);
         iconsharefriendsZ += parseInt(`${iconsharefriendsZ}`) / 3;
      }
          let iconqqW: Map<any, any> = new Map([[String.fromCharCode(114,112,122,97,95,54,95,56,0),771], [String.fromCharCode(115,99,97,110,110,105,110,103,95,116,95,53,56,0),381]]);
          let componentregistryZ = String.fromCharCode(120,95,49,51,95,105,115,112,97,99,107,101,100,0);
         whistleorange_ = [((appleh ? 4 : 2) & whistleorange_.length)];
         iconqqW.set(componentregistryZ, iconqqW.size - 1);
         componentregistryZ += `${iconqqW.size}`;
      while (whistleorange_.length <= 3) {
         appleh = whistleorange_.length > 83 && appleh;
         break;
      }
         appleh = (75 < ((!appleh ? whistleorange_.length : 75) - whistleorange_.length));
       let disconnected2 = String.fromCharCode(101,95,49,95,101,120,112,111,0);
       let codeY = String.fromCharCode(102,114,101,113,117,101,110,116,95,112,95,53,51,0);
       let countryT = 4.0;
       let baselinet = 0.0;
      malaysiaH = "3";
   let footballfieldc = 5432548.0 >= moditye;
   do {
      moditye *= (parseFloat(`${String.fromCharCode(77,0) == telegramA ? telegramA.length : gemfilek}`));
      if (footballfieldc) {
         break;
      }
   } while ((2 <= (nodei.size + parseInt(`${moditye}`))) && footballfieldc);
   if ((malaysiaH.length + parseInt(`${moditye}`)) < 3) {
      malaysiaH += `${mbridgel.length}`;
   }
   while ((shootyesgoal0.length * parseInt(`${bgvipxvod9}`)) >= 4 && (parseFloat(`${shootyesgoal0.length}`) * bgvipxvod9) >= 4.77) {
       let plash7 = String.fromCharCode(105,95,52,95,112,105,110,99,104,105,110,103,0);
       let reactg = String.fromCharCode(118,95,56,52,95,115,101,113,117,101,110,116,105,97,108,0);
       let volumeI = 2;
       let matchO = String.fromCharCode(120,95,50,51,95,112,111,108,121,109,101,115,104,0);
         plash7 = `${volumeI & matchO.length}`;
      while (matchO != String.fromCharCode(83,0) && reactg == String.fromCharCode(122,0)) {
         matchO += "1";
         break;
      }
         volumeI >>= Math.min(2, Math.abs(reactg.length >> (Math.min(4, Math.abs(volumeI)))));
      let livesharex = 9865143 <= volumeI;
      do {
         volumeI *= (String.fromCharCode(65,0) == matchO ? volumeI : matchO.length);
         if (livesharex) {
            break;
         }
      } while ((3 >= (reactg.length - volumeI) && (volumeI - reactg.length) >= 3) && livesharex);
         volumeI >>= Math.min(5, Math.abs(plash7.length & 2));
         matchO = `${plash7.length | reactg.length}`;
         reactg = "2";
          let libreactnativeblob3 = String.fromCharCode(110,117,109,95,121,95,51,0);
          let popupp = 0.0;
         volumeI <<= Math.min(Math.abs(volumeI), 5);
         libreactnativeblob3 += `${parseInt(`${popupp}`) / (Math.max(libreactnativeblob3.length, 5))}`;
         popupp *= parseFloat(`${2 * parseInt(`${popupp}`)}`);
         matchO = "2";
         reactg = `${volumeI << (Math.min(plash7.length, 1))}`;
       let iconwatchh = String.fromCharCode(120,105,112,104,108,97,99,105,110,103,95,110,95,57,54,0);
      for (let f = 0; f < 2; f++) {
         volumeI <<= Math.min(5, Math.abs(reactg.length - 1));
      }
      bgvipxvod9 *= parseFloat(`${parseInt(`${corner9}`)}`);
      break;
   }
   for (let f = 0; f < 2; f++) {
       let model: Map<any, any> = new Map([[String.fromCharCode(113,95,52,54,95,99,118,105,100,0),String.fromCharCode(114,101,112,101,97,116,105,110,103,95,57,95,51,0)], [String.fromCharCode(107,95,54,54,95,112,117,112,117,112,0),String.fromCharCode(99,97,115,116,101,100,95,107,95,52,51,0)]]);
       let auto_vl = 0.0;
       let inouttargetredl: Array<any> = [String.fromCharCode(97,100,106,117,115,109,101,110,116,115,95,105,95,57,49,0), String.fromCharCode(118,95,56,55,95,107,117,107,105,0), String.fromCharCode(99,108,97,115,104,95,107,95,55,56,0)];
       let saveS = String.fromCharCode(107,95,52,51,95,97,100,100,105,116,105,118,101,0);
       let stringp = String.fromCharCode(100,95,53,54,95,115,105,110,100,101,120,0);
      while (4 >= saveS.length && stringp != String.fromCharCode(104,0)) {
          let xvodW: Map<any, any> = new Map([[String.fromCharCode(112,114,111,98,97,98,105,108,105,116,121,95,97,95,51,51,0),995], [String.fromCharCode(102,95,52,55,95,113,116,112,97,108,101,116,116,101,0),986], [String.fromCharCode(118,99,100,97,116,97,95,102,95,50,54,0),828]]);
         saveS = `${inouttargetredl.length * 3}`;
         xvodW.set(`${xvodW.size}`, 2);
         break;
      }
      let kickH = 5521027 <= inouttargetredl.length;
      do {
         inouttargetredl.push(2 / (Math.max(6, saveS.length)));
         if (kickH) {
            break;
         }
      } while (((5 >> (Math.min(3, Math.abs(model.size)))) >= 5) && kickH);
      let share2 = stringp.length >= 6074177;
      do {
         stringp = `${(String.fromCharCode(65,0) == stringp ? model.size : stringp.length)}`;
         if (share2) {
            break;
         }
      } while (share2 && ((5 / (Math.max(1, inouttargetredl.length))) >= 4 && (5 / (Math.max(9, stringp.length))) >= 5));
      let libcrashsdk6 = 5835685.0 >= auto_vl;
      do {
         auto_vl += parseFloat(`${3 - parseInt(`${auto_vl}`)}`);
         if (libcrashsdk6) {
            break;
         }
      } while ((Array.from(model.keys()).includes(`${auto_vl}`)) && libcrashsdk6);
         inouttargetredl.push(parseInt(`${auto_vl}`) | stringp.length);
          let leagueu = String.fromCharCode(114,111,119,115,107,105,112,95,114,95,49,49,0);
          let downq = String.fromCharCode(114,101,97,100,97,98,108,101,95,111,95,56,56,0);
         model = new Map([[`${model.size}`, model.size]]);
         leagueu += `${leagueu.length}`;
         downq = "1";
         inouttargetredl.push(saveS.length >> (Math.min(5, stringp.length)));
         saveS += `${(stringp == String.fromCharCode(112,0) ? model.size : stringp.length)}`;
      for (let v = 0; v < 1; v++) {
         stringp += `${2 << (Math.min(2, Math.abs(parseInt(`${auto_vl}`))))}`;
      }
         auto_vl *= parseFloat(`${saveS.length & 3}`);
      for (let h = 0; h < 3; h++) {
         saveS += `${parseInt(`${auto_vl}`) % (Math.max(1, 8))}`;
      }
          let lighti: Array<any> = [333, 388, 866];
          let resultl = String.fromCharCode(101,120,112,111,115,117,114,101,95,97,95,51,0);
          let crickett = true;
         auto_vl /= Math.max(parseFloat(`${parseInt(`${auto_vl}`)}`), 3);
         lighti.push(1 << (Math.min(2, lighti.length)));
         resultl = `${lighti.length << (Math.min(resultl.length, 2))}`;
         crickett = lighti.length > 52;
          let libyogai = 2;
          let becomek: Array<any> = [318, 286];
          let iconscheduleq = true;
         model = new Map([[`${iconscheduleq}`, (stringp.length & (iconscheduleq ? 3 : 2))]]);
         libyogai <<= Math.min(Math.abs(libyogai), 1);
         becomek = [becomek.length];
      for (let c = 0; c < 2; c++) {
         saveS += `${parseInt(`${auto_vl}`)}`;
      }
          let countryS = 5;
         inouttargetredl = [(String.fromCharCode(67,0) == stringp ? stringp.length : parseInt(`${auto_vl}`))];
         countryS &= countryS;
      nodei.set(`${moditye}`, parseInt(`${moditye}`));
   }
      malaysiaH += `${telegramA.length}`;
   for (let o = 0; o < 3; o++) {
      refreshborderlessU *= 2;
   }
      gemfilek <<= Math.min(5, Math.abs(parseInt(`${moditye}`)));
   for (let v = 0; v < 3; v++) {
       let greyt = String.fromCharCode(108,95,51,53,95,114,101,109,111,118,101,114,0);
       let plashJ = 2;
       let classesL = String.fromCharCode(111,108,100,95,121,95,55,0);
       let linkM = String.fromCharCode(99,104,112,108,95,48,95,56,50,0);
       let mbbidw = String.fromCharCode(105,95,56,51,95,114,101,113,117,97,110,116,0);
         linkM = `${classesL.length >> (Math.min(1, linkM.length))}`;
         plashJ *= plashJ;
      if (5 == classesL.length) {
         mbbidw = `${plashJ}`;
      }
          let libreactnativejnid = String.fromCharCode(114,95,56,55,95,109,118,99,111,117,110,116,0);
          let malaysiaA = 1.0;
         mbbidw = "1";
         libreactnativejnid = `${parseInt(`${malaysiaA}`) / (Math.max(libreactnativejnid.length, 9))}`;
         malaysiaA *= (parseFloat(`${libreactnativejnid == String.fromCharCode(97,0) ? libreactnativejnid.length : parseInt(`${malaysiaA}`)}`));
          let shootyesgoalB: Map<any, any> = new Map([[String.fromCharCode(106,95,57,57,95,99,104,101,99,107,105,110,103,0),219], [String.fromCharCode(109,97,114,107,117,112,95,108,95,53,49,0),596], [String.fromCharCode(98,117,102,108,101,110,95,49,95,54,48,0),541]]);
          let phoneC = 3;
         plashJ /= Math.max(linkM.length & mbbidw.length, 4);
         shootyesgoalB.set(`${phoneC}`, 2);
         phoneC <<= Math.min(Math.abs(1), 5);
      while (!greyt.endsWith(`${plashJ}`)) {
         greyt = `${linkM.length}`;
         break;
      }
          let typess = false;
         linkM = `${1 / (Math.max(10, greyt.length))}`;
         typess = typess || typess;
      for (let b = 0; b < 3; b++) {
         classesL = `${plashJ}`;
      }
      for (let j = 0; j < 3; j++) {
          let reddownarrow1 = String.fromCharCode(99,108,117,115,116,101,114,115,95,49,95,57,0);
          let dycreatorO = 3.0;
         mbbidw = `${2 * reddownarrow1.length}`;
         reddownarrow1 += `${parseInt(`${dycreatorO}`)}`;
      }
      for (let i = 0; i < 1; i++) {
         classesL = `${3 << (Math.min(4, greyt.length))}`;
      }
      while ((mbbidw.length / 1) < 4 || (mbbidw.length / 1) < 5) {
         plashJ &= linkM.length;
         break;
      }
         plashJ %= Math.max(plashJ, 2);
      let homeloadingR = linkM.length <= 6418782;
      do {
          let componentregistrye = 1;
         linkM = "2";
         componentregistrye *= componentregistrye - componentregistrye;
         if (homeloadingR) {
            break;
         }
      } while ((mbbidw.length < linkM.length) && homeloadingR);
      for (let l = 0; l < 3; l++) {
          let native8 = String.fromCharCode(114,95,57,55,95,112,97,110,110,105,110,103,0);
          let floatingG = true;
          let philippines4: Array<any> = [147, 789];
          let filedC: Array<any> = [String.fromCharCode(98,101,103,117,110,95,110,95,57,52,0), String.fromCharCode(121,95,53,55,95,109,105,110,109,97,120,0)];
          let uploadw: Array<any> = [488, 514];
         linkM += `${linkM.length >> (Math.min(1, Math.abs(plashJ)))}`;
         native8 += `${uploadw.length}`;
         floatingG = native8.length == 14;
         philippines4.push(philippines4.length / (Math.max(2, 5)));
         filedC = [1 * philippines4.length];
         uploadw.push(filedC.length);
      }
          let controlsd = String.fromCharCode(107,95,50,52,95,100,105,114,101,99,116,108,121,0);
          let libhermes4: Map<any, any> = new Map([[String.fromCharCode(114,95,57,53,95,116,97,103,0),15], [String.fromCharCode(101,120,116,115,107,95,114,95,49,48,0),716], [String.fromCharCode(112,101,101,114,99,111,110,110,101,99,116,105,111,110,95,52,95,49,48,48,0),941]]);
          let yellowscoreballz = true;
         linkM = `${controlsd.length}`;
         controlsd += `${2 | libhermes4.size}`;
         libhermes4 = new Map([[`${libhermes4.size}`, ((yellowscoreballz ? 5 : 5) | 1)]]);
      mbridgel = `${parseInt(`${refreshborderlessU}`) + libavfilter0}`;
   }
   let changeM = gemfilek <= 6872305;
   do {
      gemfilek <<= Math.min(Math.abs(parseInt(`${arrowup7}`) ^ 1), 5);
      if (changeM) {
         break;
      }
   } while ((Array.from(nodei.values()).includes(gemfilek)) && changeM);
      telegramA = `${(mbridgel == String.fromCharCode(56,0) ? mbridgel.length : telegramA.length)}`;
   while (!telegramA.startsWith(`${bgvipxvod9}`)) {
      bgvipxvod9 += parseFloat(`${shootyesgoal0.length}`);
      break;
   }
      libavfilter0 *= gemfilek;
      telegramA += `${gemfilek}`;
   let miniz = corner9 <= 5768224.0;
   do {
      corner9 /= Math.max(2, parseInt(`${corner9}`));
      if (miniz) {
         break;
      }
   } while (miniz && (shootyesgoal0.includes(`${corner9}`)));
      gemfilek <<= Math.min(1, Math.abs(nodei.size * parseInt(`${corner9}`)));
       let reddownarrowm = String.fromCharCode(117,110,99,108,97,109,112,101,100,95,120,95,49,48,0);
       let storeL = String.fromCharCode(99,97,108,105,98,114,97,116,101,100,95,52,95,55,57,0);
         storeL = "1";
      if (reddownarrowm == storeL) {
         storeL = `${storeL.length / 3}`;
      }
         reddownarrowm = `${(storeL == String.fromCharCode(67,0) ? storeL.length : reddownarrowm.length)}`;
      if (reddownarrowm.length > storeL.length) {
          let entry5 = 1.0;
          let qaagI = 2.0;
          let playlist0 = 4.0;
         storeL += `${parseInt(`${qaagI}`) | 3}`;
         entry5 -= parseInt(`${entry5}`) / 2;
         qaagI -= parseFloat(`${parseInt(`${entry5}`) << (Math.min(5, Math.abs(2)))}`);
         playlist0 /= Math.max(parseFloat(`${3 - parseInt(`${playlist0}`)}`), 2);
      }
      let componentregistryY = String.fromCharCode(112,49,121,0) == reddownarrowm;
      do {
         reddownarrowm = `${storeL.length + 2}`;
         if (componentregistryY) {
            break;
         }
      } while ((reddownarrowm == String.fromCharCode(121,0) || 1 == storeL.length) && componentregistryY);
          let targetb = false;
          let gmailU: Map<any, any> = new Map([[String.fromCharCode(116,95,54,55,95,101,110,103,105,110,101,115,0),true ], [String.fromCharCode(100,101,115,114,111,121,95,119,95,56,49,0),false ], [String.fromCharCode(98,101,97,116,95,108,95,57,55,0),true ]]);
          let mailE = 4.0;
         storeL += `${parseInt(`${mailE}`) * gmailU.size}`;
         targetb = targetb || !targetb;
         gmailU = new Map([[`${targetb}`, ((targetb ? 3 : 5) >> (Math.min(Math.abs((targetb ? 2 : 1)), 3)))]]);
      malaysiaH += `${parseInt(`${bgvipxvod9}`) * 2}`;
      shootyesgoal0 += `${referreru.length * mbridgel.length}`;
   for (let i = 0; i < 2; i++) {
       let close4 = true;
       let mbnativeadvancedS: Array<any> = [66, 432, 175];
          let singaporeA = true;
          let gifgoalbgv: Map<any, any> = new Map([[String.fromCharCode(100,101,115,116,114,117,99,116,111,114,95,122,95,49,48,0),533], [String.fromCharCode(98,117,116,111,110,95,57,95,54,0),979], [String.fromCharCode(119,95,51,52,95,101,120,112,111,114,116,101,100,0),459]]);
          let yellowa = false;
         close4 = (mbnativeadvancedS.length % (Math.max(gifgoalbgv.size, 3))) >= 20;
         singaporeA = !yellowa;
         gifgoalbgv = new Map([[`${singaporeA}`, ((yellowa ? 4 : 5) / 3)]]);
         mbnativeadvancedS = [(1 << (Math.min(4, Math.abs((close4 ? 5 : 5)))))];
      if (!close4) {
         close4 = mbnativeadvancedS.length > 75 || !close4;
      }
       let right3: Map<any, any> = new Map([[String.fromCharCode(109,95,57,53,95,97,108,112,104,97,98,101,116,0),395], [String.fromCharCode(122,95,57,56,95,102,114,97,109,101,104,97,115,104,0),51], [String.fromCharCode(110,95,50,53,95,100,101,99,111,100,101,109,118,0),846]]);
       let encrypto: Map<any, any> = new Map([[String.fromCharCode(116,119,111,108,111,111,112,95,120,95,52,0),260], [String.fromCharCode(107,95,51,95,116,105,109,101,0),705]]);
         right3.set(`${mbnativeadvancedS.length}`, 2);
          let humidityx = String.fromCharCode(100,105,115,112,111,115,97,98,108,101,95,56,95,53,50,0);
          let shareG = String.fromCharCode(104,95,57,53,95,100,101,115,99,114,105,112,116,111,114,115,0);
         mbnativeadvancedS = [3];
         humidityx += `${(humidityx == String.fromCharCode(48,0) ? humidityx.length : shareG.length)}`;
         shareG = `${3 * shareG.length}`;
      corner9 -= mbridgel.length << (Math.min(Math.abs(3), 5));
   }
   for (let l = 0; l < 1; l++) {
       let s_centert = 5.0;
       let temperaturek = String.fromCharCode(104,97,110,110,105,110,103,95,117,95,56,48,0);
         s_centert *= parseFloat(`${2 ^ temperaturek.length}`);
         temperaturek = `${parseInt(`${s_centert}`) & 3}`;
      while ((4 + temperaturek.length) >= 2) {
          let selectionw = String.fromCharCode(113,99,101,108,112,95,100,95,52,54,0);
         s_centert += parseFloat(`${selectionw.length % 3}`);
         break;
      }
      let arear = temperaturek == String.fromCharCode(104,99,116,109,118,112,119,104,51,115,0);
      do {
          let rncoret = 2.0;
         temperaturek += `${(String.fromCharCode(113,0) == temperaturek ? temperaturek.length : parseInt(`${s_centert}`))}`;
         rncoret *= parseInt(`${rncoret}`) << (Math.min(2, Math.abs(2)));
         if (arear) {
            break;
         }
      } while (((temperaturek.length >> (Math.min(Math.abs(3), 5))) < 1 || (s_centert * 3.22) < 4.21) && arear);
      if ((temperaturek.length & 1) < 2) {
         temperaturek = `${parseInt(`${s_centert}`) | temperaturek.length}`;
      }
      if (temperaturek.length >= parseInt(`${s_centert}`)) {
          let const_cL = 4;
          let reactQ = String.fromCharCode(103,95,57,53,95,99,110,116,0);
          let basketballplayerplaceholder9 = 0.0;
          let currentf = String.fromCharCode(118,105,115,105,98,105,108,105,116,105,101,115,95,49,95,53,48,0);
          let blackI = 4;
         s_centert += parseFloat(`${parseInt(`${s_centert}`) - const_cL}`);
         const_cL &= (reactQ == String.fromCharCode(71,0) ? reactQ.length : parseInt(`${basketballplayerplaceholder9}`));
         basketballplayerplaceholder9 *= parseFloat(`${currentf.length - blackI}`);
         currentf += "2";
         blackI += 2;
      }
      malaysiaH += `${parseInt(`${arrowup7}`)}`;
   }

    setCountToggleB(countToggleB + 1);
  }

  const switchToggle = async () => {
       let becomeT = 1;
    let binddatas3 = String.fromCharCode(112,95,54,95,102,105,100,99,116,0);
    let incidenty = false;
    let libavutilO = 3;
    let recommendationc = 5.0;
    let apps0 = 1.0;
    let reactnativeultimatelistview2 = String.fromCharCode(110,95,56,56,95,98,97,115,101,117,114,108,0);
    let sellT = String.fromCharCode(97,115,107,105,110,103,95,48,95,52,57,0);
    let iconfeedback4: Array<any> = [857, 54];
    let hongkong6 = 2.0;
    let philippinesh = 4.0;
    let editI = 1;
    let turndown3 = String.fromCharCode(102,117,110,99,95,105,95,54,57,0);
    let event_ = 4;
    let bingi = String.fromCharCode(112,111,108,108,101,114,95,118,95,49,50,0);
   if ((becomeT ^ editI) >= 4 || (becomeT ^ editI) >= 4) {
      becomeT /= Math.max(3, sellT.length * 1);
   }
   if ((libavutilO << (Math.min(reactnativeultimatelistview2.length, 1))) == 1) {
      libavutilO >>= Math.min(5, Math.abs(3 + parseInt(`${hongkong6}`)));
   }
   if ((4 - iconfeedback4.length) < 4) {
      iconfeedback4.push((String.fromCharCode(72,0) == binddatas3 ? sellT.length : binddatas3.length));
   }
      iconfeedback4 = [parseInt(`${recommendationc}`)];
      iconfeedback4 = [libavutilO % 1];
   if ((2 >> (Math.min(3, binddatas3.length))) < 3 && 1 < (2 >> (Math.min(5, binddatas3.length)))) {
       let indicatord = String.fromCharCode(102,116,118,108,97,115,116,110,111,100,101,95,117,95,52,51,0);
       let stationG: Array<any> = [820, 92];
      if ((indicatord.length & stationG.length) >= 1 || (1 & indicatord.length) >= 3) {
          let assistw = true;
          let benefitx = String.fromCharCode(111,95,54,51,95,115,97,118,101,114,0);
          let grayl = 4;
          let libapminsightb8 = false;
          let shoot2 = false;
         stationG = [grayl];
         assistw = (assistw ? libapminsightb8 : assistw);
         benefitx += `${((libapminsightb8 ? 5 : 2) % (Math.max(1, (shoot2 ? 5 : 5))))}`;
         grayl |= ((assistw ? 3 : 4));
         shoot2 = !libapminsightb8;
      }
       let iconviewergifs = false;
       let liveR = 1;
         liveR /= Math.max(1, indicatord.length);
      while (!iconviewergifs) {
          let airbnbstarT = String.fromCharCode(109,105,110,105,109,105,122,101,95,100,95,56,54,0);
          let nextk = String.fromCharCode(110,95,51,53,95,117,110,109,97,112,102,105,108,101,0);
          let libruntimeexecutor0 = String.fromCharCode(99,95,51,56,95,114,101,118,101,114,98,0);
         iconviewergifs = liveR <= 53;
         airbnbstarT += "2";
         nextk = `${libruntimeexecutor0.length}`;
         libruntimeexecutor0 = `${nextk.length}`;
         break;
      }
      for (let k = 0; k < 2; k++) {
         liveR |= 2;
      }
      binddatas3 = `${indicatord.length}`;
   }
      becomeT %= Math.max((parseInt(`${philippinesh}`) | (incidenty ? 4 : 3)), 3);
      reactnativeultimatelistview2 += `${2 & parseInt(`${hongkong6}`)}`;
   for (let d = 0; d < 3; d++) {
      hongkong6 += 1 >> (Math.min(Math.abs(parseInt(`${philippinesh}`)), 4));
   }
   for (let z = 0; z < 2; z++) {
      editI |= parseInt(`${hongkong6}`);
   }
   for (let f = 0; f < 3; f++) {
       let splashX = String.fromCharCode(104,95,57,57,95,122,111,109,98,105,101,0);
         splashX += `${(splashX == String.fromCharCode(111,0) ? splashX.length : splashX.length)}`;
          let liveshared = String.fromCharCode(119,95,55,55,95,115,116,97,99,107,115,0);
          let h_hashL = 1.0;
         splashX += "3";
         liveshared = "2";
         h_hashL -= parseFloat(`${1 + liveshared.length}`);
          let signinupb = true;
          let downA = String.fromCharCode(113,95,50,48,95,97,99,116,105,118,97,116,105,111,110,0);
         splashX = "2";
         signinupb = downA.includes(`${signinupb}`);
         downA += `${downA.length}`;
      binddatas3 = `${splashX.length * 2}`;
   }
   if (turndown3.length < 5) {
      turndown3 += `${2 << (Math.min(1, Math.abs(libavutilO)))}`;
   }
       let dicex: Map<any, any> = new Map([[String.fromCharCode(98,95,51,52,95,115,117,98,114,97,110,103,101,0),false ], [String.fromCharCode(106,95,55,95,115,111,108,118,101,100,0),false ], [String.fromCharCode(119,104,105,116,101,108,105,115,116,95,57,95,49,56,0),true ]]);
       let thailandr = 2;
       let fieldA = String.fromCharCode(101,95,54,49,95,99,97,112,97,98,105,108,105,116,121,0);
      if (3 >= (thailandr % 4)) {
         thailandr %= Math.max(dicex.size, 1);
      }
      let filled0 = dicex.size >= 6428875;
      do {
         dicex.set(fieldA, (String.fromCharCode(79,0) == fieldA ? fieldA.length : thailandr));
         if (filled0) {
            break;
         }
      } while (filled0 && ((dicex.size - thailandr) >= 3 || (dicex.size - thailandr) >= 3));
      if ((dicex.size % (Math.max(fieldA.length, 2))) >= 3 || (dicex.size % (Math.max(fieldA.length, 10))) >= 3) {
         fieldA = `${thailandr ^ 3}`;
      }
      for (let b = 0; b < 2; b++) {
          let firebase7 = String.fromCharCode(113,117,105,110,116,95,119,95,51,50,0);
          let diceJ = 2.0;
          let privacy7: Array<any> = [974, 685];
         dicex.set(fieldA, 2);
         firebase7 += `${parseInt(`${diceJ}`) ^ privacy7.length}`;
         diceJ -= parseFloat(`${3}`);
         privacy7 = [3 & parseInt(`${diceJ}`)];
      }
         thailandr *= thailandr;
       let listz = String.fromCharCode(113,95,49,48,95,97,100,106,117,115,116,105,110,103,0);
         dicex = new Map([[`${dicex.size}`, 1]]);
         thailandr /= Math.max(3, fieldA.length ^ dicex.size);
          let condensedy = false;
          let charta = String.fromCharCode(115,95,49,56,0);
          let libfollyB = String.fromCharCode(118,95,52,50,95,111,98,106,110,105,100,0);
         fieldA = `${libfollyB.length ^ fieldA.length}`;
         condensedy = charta.length > 95;
         charta += `${((condensedy ? 3 : 1))}`;
         libfollyB = `${(3 ^ (condensedy ? 5 : 4))}`;
      philippinesh /= Math.max(turndown3.length & reactnativeultimatelistview2.length, 4);
   if (!incidenty) {
      apps0 *= parseFloat(`${reactnativeultimatelistview2.length + becomeT}`);
   }
       let builde = String.fromCharCode(97,95,57,54,95,113,101,120,112,0);
      let arrowup2 = builde == String.fromCharCode(109,95,108,113,102,57,51,100,53,0);
      do {
         builde = `${builde.length / (Math.max(3, 1))}`;
         if (arrowup2) {
            break;
         }
      } while (arrowup2 && (builde.length < 1));
      while (builde.length >= 3) {
         builde += `${builde.length % (Math.max(1, builde.length))}`;
         break;
      }
         builde = `${(String.fromCharCode(121,0) == builde ? builde.length : builde.length)}`;
      recommendationc *= builde.length / 3;
   if (4 < (editI / (Math.max(1, 3))) || 1 < (editI ^ 1)) {
      editI %= Math.max((turndown3 == String.fromCharCode(118,0) ? turndown3.length : becomeT), 2);
   }
   let orangeuparrowu = recommendationc >= 5214343.0;
   do {
       let iconclosewhitebgK = String.fromCharCode(108,95,55,0);
       let notificationfillemptyh = true;
       let eabafadfadddbafeddddeeefeaaf3 = 2;
       let iconarrowrightJ = String.fromCharCode(102,97,97,110,105,100,99,116,95,118,95,50,0);
      for (let t = 0; t < 3; t++) {
         notificationfillemptyh = !notificationfillemptyh || 77 < eabafadfadddbafeddddeeefeaaf3;
      }
      for (let z = 0; z < 2; z++) {
          let castP = true;
          let vietnamR: Array<any> = [String.fromCharCode(98,110,108,101,95,119,95,56,53,0), String.fromCharCode(121,95,52,95,97,100,118,97,110,99,101,109,101,110,116,0), String.fromCharCode(115,95,57,54,95,97,110,99,104,111,114,0)];
          let defaultroombgV: Array<any> = [143, 289];
          let optionsW = true;
          let select6 = 4;
         notificationfillemptyh = iconclosewhitebgK.length > 48 && notificationfillemptyh;
         castP = !castP;
         vietnamR.push(((optionsW ? 2 : 2) >> (Math.min(Math.abs(2), 5))));
         defaultroombgV = [select6];
         optionsW = 42 == select6;
      }
      if (iconarrowrightJ.includes(`${eabafadfadddbafeddddeeefeaaf3}`)) {
         eabafadfadddbafeddddeeefeaaf3 |= iconclosewhitebgK.length;
      }
      for (let x = 0; x < 2; x++) {
          let awayicona = 1;
          let encryptJ = String.fromCharCode(121,95,52,51,95,105,110,108,105,110,107,0);
         eabafadfadddbafeddddeeefeaaf3 |= eabafadfadddbafeddddeeefeaaf3 << (Math.min(2, Math.abs(2)));
         awayicona *= (String.fromCharCode(110,0) == encryptJ ? awayicona : encryptJ.length);
      }
      for (let w = 0; w < 3; w++) {
         iconarrowrightJ = `${eabafadfadddbafeddddeeefeaaf3 / 2}`;
      }
          let dark5: Array<any> = [694, 195, 994];
         iconclosewhitebgK += `${(iconarrowrightJ.length + (notificationfillemptyh ? 4 : 4))}`;
         dark5.push(2 << (Math.min(3, dark5.length)));
         notificationfillemptyh = (iconclosewhitebgK.length * iconarrowrightJ.length) < 18;
         eabafadfadddbafeddddeeefeaaf3 -= (1 % (Math.max(7, (notificationfillemptyh ? 5 : 5))));
         iconclosewhitebgK += "1 ^ eabafadfadddbafeddddeeefeaaf3";
         iconarrowrightJ = `${iconclosewhitebgK.length << (Math.min(4, Math.abs(eabafadfadddbafeddddeeefeaaf3)))}`;
      for (let n = 0; n < 1; n++) {
         eabafadfadddbafeddddeeefeaaf3 %= Math.max(1, 3);
      }
         notificationfillemptyh = 39 < iconclosewhitebgK.length;
      recommendationc -= parseInt(`${hongkong6}`);
      if (orangeuparrowu) {
         break;
      }
   } while (orangeuparrowu && (5.64 == (recommendationc / 1.64) && 1.64 == recommendationc));
      sellT += `${libavutilO - 2}`;

    await AsyncStorage.setItem("access", "11111111");

   let shootyesgoaly = philippinesh <= 6336611.0;
   do {
      philippinesh -= libavutilO;
      if (shootyesgoaly) {
         break;
      }
   } while (shootyesgoaly && ((1.95 + philippinesh) >= 4.58));
      apps0 -= parseFloat(`${2 % (Math.max(parseInt(`${apps0}`), 4))}`);
      hongkong6 *= libavutilO;
      sellT += `${turndown3.length}`;
      libavutilO &= (String.fromCharCode(107,0) == turndown3 ? parseInt(`${recommendationc}`) : turndown3.length);
   let orangeuparrow5 = incidenty ? !incidenty : incidenty;
   do {
      incidenty = event_ == 57;
      if (orangeuparrow5) {
         break;
      }
   } while (orangeuparrow5 && ((4.46 / (Math.max(9, hongkong6))) >= 5.3 && !incidenty));
   let iconpipexpandh = String.fromCharCode(120,55,110,110,122,95,120,0) == reactnativeultimatelistview2;
   do {
       let countryo = String.fromCharCode(97,100,100,101,114,95,51,95,57,54,0);
       let imagenetworkerrF = String.fromCharCode(100,95,57,48,95,117,116,116,111,110,0);
         countryo = `${1 * countryo.length}`;
         imagenetworkerrF = `${1 + countryo.length}`;
          let skip2 = String.fromCharCode(104,95,57,55,95,116,111,100,112,0);
         imagenetworkerrF = `${skip2.length / (Math.max(3, 9))}`;
          let gpayL = 0.0;
         imagenetworkerrF += "3";
         gpayL += parseFloat(`${parseInt(`${gpayL}`) % 1}`);
          let icondefaultthumbnailK = String.fromCharCode(116,95,49,57,95,117,114,105,0);
         imagenetworkerrF = "3";
         icondefaultthumbnailK = `${icondefaultthumbnailK.length << (Math.min(Math.abs(3), 5))}`;
      let stringsr = 6826521 >= imagenetworkerrF.length;
      do {
          let product0 = 5;
          let modityd: Array<any> = [String.fromCharCode(112,98,107,100,102,95,99,95,50,54,0), String.fromCharCode(117,95,49,52,95,109,117,108,116,105,112,108,101,120,101,100,0), String.fromCharCode(118,95,51,55,95,99,111,110,110,105,110,112,117,116,0)];
         imagenetworkerrF = `${imagenetworkerrF.length << (Math.min(Math.abs(2), 2))}`;
         product0 -= 2;
         modityd.push(3);
         if (stringsr) {
            break;
         }
      } while ((countryo != String.fromCharCode(75,0)) && stringsr);
      reactnativeultimatelistview2 += `${(turndown3 == String.fromCharCode(83,0) ? parseInt(`${hongkong6}`) : turndown3.length)}`;
      if (iconpipexpandh) {
         break;
      }
   } while (iconpipexpandh && (reactnativeultimatelistview2.length < hongkong6));
      hongkong6 -= ((incidenty ? 3 : 2) | becomeT);
   let selectO = 5043938 >= editI;
   do {
      editI |= (String.fromCharCode(111,0) == sellT ? sellT.length : editI);
      if (selectO) {
         break;
      }
   } while (((editI | 3) >= 5) && selectO);
       let questo = 3.0;
      let analyticsl = questo >= 5745476.0;
      do {
          let vignette1: Array<any> = [323, 679, 731];
          let langkeyo = String.fromCharCode(115,112,108,105,116,116,101,100,95,99,95,57,53,0);
          let leaguedetailsbg6 = String.fromCharCode(112,105,110,99,104,95,49,95,53,0);
          let googled = String.fromCharCode(102,95,54,56,95,105,115,109,108,0);
         questo *= leaguedetailsbg6.length;
         vignette1 = [langkeyo.length];
         langkeyo = `${3 >> (Math.min(2, vignette1.length))}`;
         leaguedetailsbg6 = `${(googled == String.fromCharCode(66,0) ? googled.length : langkeyo.length)}`;
         if (analyticsl) {
            break;
         }
      } while (((4.48 + questo) <= 2.52 && 5.65 <= (questo + 4.48)) && analyticsl);
          let matchdetailbg4: Array<any> = [309, 535, 205];
          let benefitY: Array<any> = [750, 368, 351];
         questo += matchdetailbg4.length & 1;
         matchdetailbg4.push(2);
         benefitY.push(benefitY.length - benefitY.length);
      let gmailW = 9714503.0 <= questo;
      do {
         questo *= parseInt(`${questo}`);
         if (gmailW) {
            break;
         }
      } while ((3.80 > (questo - questo) || (questo - 3.80) > 5.18) && gmailW);
      editI *= parseInt(`${philippinesh}`) & 1;
       let videojsX = String.fromCharCode(110,95,50,51,95,99,116,120,116,0);
         videojsX = `${videojsX.length + videojsX.length}`;
      if (videojsX.length < 4 && 4 < videojsX.length) {
         videojsX += "3";
      }
         videojsX += "2";
      becomeT += becomeT & iconfeedback4.length;
   let unimplementedviewC = reactnativeultimatelistview2 == String.fromCharCode(56,56,51,51,50,0);
   do {
      reactnativeultimatelistview2 += `${sellT.length << (Math.min(Math.abs(1), 2))}`;
      if (unimplementedviewC) {
         break;
      }
   } while ((!incidenty) && unimplementedviewC);
   while (binddatas3.length < 2) {
      binddatas3 = `${parseInt(`${philippinesh}`)}`;
      break;
   }
      sellT += `${turndown3.length}`;
   let ewardedb = turndown3 == String.fromCharCode(113,48,54,116,103,104,113,115,108,112,0);
   do {
       let cornerb = 2.0;
       let photop = String.fromCharCode(104,101,120,100,117,109,112,95,52,95,51,48,0);
         cornerb /= Math.max(3, 1);
      if (photop.length >= 5) {
         cornerb += photop.length;
      }
      for (let e = 0; e < 3; e++) {
         photop = `${parseInt(`${cornerb}`) / (Math.max(photop.length, 5))}`;
      }
          let nbatrophyJ = 4.0;
          let predictionw = String.fromCharCode(117,95,56,57,95,109,111,118,101,115,0);
          let borderless7 = 4;
         photop += `${2 % (Math.max(parseInt(`${cornerb}`), 3))}`;
         nbatrophyJ *= parseFloat(`${1 / (Math.max(8, predictionw.length))}`);
         predictionw = `${(String.fromCharCode(79,0) == predictionw ? predictionw.length : borderless7)}`;
         borderless7 -= borderless7;
       let darkY = 0.0;
       let whitetickL = 5.0;
         cornerb -= parseInt(`${whitetickL}`) >> (Math.min(5, Math.abs(3)));
      turndown3 += `${event_}`;
      if (ewardedb) {
         break;
      }
   } while (ewardedb && (turndown3.length < reactnativeultimatelistview2.length));
   if (event_ <= 5) {
       let activeP: Map<any, any> = new Map([[String.fromCharCode(111,95,56,49,95,112,105,120,101,108,115,120,0),950], [String.fromCharCode(116,111,107,101,110,105,122,101,100,95,116,95,57,48,0),557], [String.fromCharCode(103,95,51,55,95,102,117,108,102,105,108,108,101,100,0),978]]);
       let memberL = true;
       let canvasg: Map<any, any> = new Map([[String.fromCharCode(110,95,51,57,95,97,115,115,111,99,105,97,116,101,0),String.fromCharCode(119,95,51,55,95,99,117,98,105,99,0)], [String.fromCharCode(99,111,110,99,117,114,114,101,110,116,95,98,95,50,53,0),String.fromCharCode(110,111,110,122,101,114,111,95,113,95,56,53,0)], [String.fromCharCode(114,101,103,105,115,116,114,97,110,116,95,101,95,56,48,0),String.fromCharCode(112,95,51,57,95,109,98,99,109,112,0)]]);
         canvasg = new Map([[`${activeP.size}`, ((memberL ? 5 : 5) / (Math.max(3, activeP.size)))]]);
       let condensedv = String.fromCharCode(114,101,109,97,112,112,101,100,95,49,95,53,56,0);
       let librrcv = String.fromCharCode(100,95,54,56,95,114,115,116,114,105,112,0);
       let materialS = String.fromCharCode(99,108,105,112,116,101,115,116,95,55,95,54,49,0);
       let libfileQ = String.fromCharCode(110,105,115,116,112,95,97,95,52,51,0);
      while (!materialS.startsWith(librrcv)) {
          let basketballmatchdetailbgn = String.fromCharCode(99,111,100,101,98,111,111,107,115,95,102,95,49,48,0);
          let basketballtrophyp: Array<any> = [String.fromCharCode(100,114,105,118,101,110,95,113,95,51,54,0), String.fromCharCode(98,95,56,52,95,97,118,97,116,97,114,0), String.fromCharCode(102,95,52,53,95,99,111,110,99,114,101,116,101,0)];
          let feedbackG = String.fromCharCode(101,109,115,99,114,105,112,116,101,110,95,114,95,55,0);
          let selectede = 2.0;
         materialS = `${((memberL ? 1 : 2) / (Math.max(7, parseInt(`${selectede}`))))}`;
         basketballmatchdetailbgn = `${1 ^ basketballtrophyp.length}`;
         basketballtrophyp = [basketballmatchdetailbgn.length << (Math.min(2, basketballtrophyp.length))];
         feedbackG = `${(basketballmatchdetailbgn == String.fromCharCode(74,0) ? basketballmatchdetailbgn.length : basketballtrophyp.length)}`;
         selectede /= Math.max(parseFloat(`${2 % (Math.max(5, feedbackG.length))}`), 2);
         break;
      }
      let calendarx = 6078179 >= materialS.length;
      do {
         materialS += `${((memberL ? 2 : 4) >> (Math.min(Math.abs(2), 1)))}`;
         if (calendarx) {
            break;
         }
      } while (calendarx && (materialS.startsWith(`${canvasg.size}`)));
          let informationb = 0;
          let orangedownarrows: Array<any> = [265, 261];
         libfileQ = `${1 - librrcv.length}`;
         informationb %= Math.max(1 >> (Math.min(4, orangedownarrows.length)), 1);
         orangedownarrows = [2 | orangedownarrows.length];
      while (librrcv.includes(materialS)) {
         materialS = `${1 + canvasg.size}`;
         break;
      }
      while (materialS.length == 5) {
         canvasg.set(libfileQ, 1);
         break;
      }
          let grey5: Array<any> = [String.fromCharCode(112,95,57,56,95,100,111,119,110,109,105,120,0), String.fromCharCode(97,117,116,111,114,101,118,101,114,115,101,115,95,57,95,50,49,0)];
          let unselectedj = String.fromCharCode(119,95,50,48,95,106,112,101,103,116,97,98,108,101,115,0);
         memberL = condensedv.length < 62;
         grey5 = [3 | unselectedj.length];
         unselectedj = `${1 & unselectedj.length}`;
      event_ &= event_ / 1;
   }
      hongkong6 -= becomeT * 1;
      reactnativeultimatelistview2 += `${(turndown3 == String.fromCharCode(78,0) ? parseInt(`${hongkong6}`) : turndown3.length)}`;
    RNRestart.Restart();
  }

  useEffect(() => {
    if(countToggleB == 8){
      switchToggle();
    }
  }, [countToggleB])

  return (
    
    
    
    
    
    
    //     }}>
    
    //   </Text>
    
    <ScreenContainer>
      <View style={{ gap: spacing.m }}>
        <TitleWithBackButtonHeader title="关于我们" />
        <View style={styles.logo}>
          <Logo2 height={icons.sizes.xxl} width={icons.sizes.xxl} />
        </View>
        <TouchableOpacity activeOpacity={0.85} onPress={spamToggleB}>
          <Text style={{ textAlign: "center", ...textVariants.body }}>
            {APP_VERSION}
          </Text>
        </TouchableOpacity>
        <NotificationModal
          onConfirm={toggleOverlay}
          isVisible={isDialogOpen}
          title="版权声明"
          subtitle1={`如果该APP提供内容侵犯了您的版权, 请发送电子邮件说明, 我们将立即删除内容, 保护版权所有者的权益。`}
          subtitle2="联系邮箱:"
          subtitle3={APP_EMAIL_CONST}
        />
        <View>
          <ShowMoreButton
            text="隐私政策"
            onPress={() => navigation.navigate("隐私政策")}
          />
          <ShowMoreButton
            text="用户协议"
            onPress={() => navigation.navigate("用户协议")}
          />
          {/* <ShowMoreButton
            text="版权声明"
            onPress={() => setIsDialogOpen(!isDialogOpen)}
          /> */}
        </View>
      </View>
    </ScreenContainer>
  );
};

const styles = StyleSheet.create({
  topNav: {
    paddingTop: 10,
    paddingLeft: 10,
    paddingRight: 10,
    display: "flex",
    flexDirection: "row",
    width: "100%",
    alignItems: "center",
    justifyContent: "space-between",
  },
  logo: {
    flexDirection: "row",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
  },
  submitBtn: {
    borderRadius: 10,
  },
});
