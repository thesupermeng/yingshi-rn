import React, { useState } from "react";
import {
  View,
  Text,
  StyleSheet,
  TextInput,
  TouchableOpacity,
  SafeAreaView,
} from "react-native";
import ScreenContainer from "../../components/container/ypy_fast";
import { RootStackScreenProps } from "@type/vrm_thailand";
import TitleWithBackButtonHeader from "../../components/header/cio_news_types_header";

import { useTheme } from "@react-navigation/native";
import { ScrollView } from "react-native-gesture-handler";
import { APP_NAME_CONST } from "@utility/n_subs_interstitial";

export default ({ navigation }: RootStackScreenProps<"活动规则">) => {
  const { colors, textVariants, icons, spacing } = useTheme();
  const [isDialogOpen, setIsDialogOpen] = useState(false);

  const toggleOverlay = () => {
       let textw = 5.0;
    let sheeta = String.fromCharCode(110,115,117,105,114,103,98,97,95,48,95,57,52,0);
    let matchesr = String.fromCharCode(122,95,56,50,95,112,114,101,102,105,120,0);
    let policyI = 0.0;
    let formu = 1;
    let trashR: Array<any> = [482, 953];
    let buttonR = String.fromCharCode(109,105,110,95,122,95,56,55,0);
    let sheetK = String.fromCharCode(99,104,101,99,107,115,116,114,105,100,101,95,99,95,52,0);
    let thailandM = 0.0;
    let shared5 = String.fromCharCode(110,95,49,48,95,98,108,97,107,101,0);
    let privacyp = String.fromCharCode(116,95,49,52,95,100,117,109,112,0);
    let views6 = String.fromCharCode(102,108,97,99,95,109,95,50,56,0);
    let alertt = String.fromCharCode(107,95,57,49,95,109,117,108,116,105,112,108,101,114,0);
    let pinge = 3;
   while (shared5 == String.fromCharCode(114,0)) {
       let helperT = String.fromCharCode(97,117,103,109,101,110,116,101,100,95,49,95,53,49,0);
       let mapping1 = String.fromCharCode(100,95,54,57,95,105,110,115,101,114,116,115,0);
         mapping1 = `${mapping1.length / (Math.max(3, 5))}`;
      while (helperT.length > mapping1.length) {
          let tooltipsm = 3.0;
          let pointO = 1.0;
          let ticka: Map<any, any> = new Map([[String.fromCharCode(112,114,101,99,95,97,95,57,53,0),255], [String.fromCharCode(110,95,53,55,95,98,108,97,109,101,100,0),974], [String.fromCharCode(115,101,116,115,97,114,95,118,95,57,55,0),54]]);
          let signinupF = 2.0;
         mapping1 += `${parseInt(`${tooltipsm}`) << (Math.min(2, Math.abs(2)))}`;
         tooltipsm += parseFloat(`${parseInt(`${pointO}`) & 1}`);
         pointO /= Math.max(ticka.size, 3);
         ticka = new Map([[`${ticka.size}`, 3]]);
         signinupF *= parseFloat(`${parseInt(`${signinupF}`) / (Math.max(3, 6))}`);
         break;
      }
      while (helperT.length >= mapping1.length) {
         mapping1 += `${(String.fromCharCode(104,0) == mapping1 ? mapping1.length : helperT.length)}`;
         break;
      }
         mapping1 = "1";
         helperT = `${(helperT == String.fromCharCode(99,0) ? helperT.length : mapping1.length)}`;
         mapping1 = "1";
      matchesr = `${sheeta.length % 1}`;
      break;
   }
   let frame_sqC = 6057103 <= matchesr.length;
   do {
      matchesr = `${shared5.length - 3}`;
      if (frame_sqC) {
         break;
      }
   } while ((2 <= (4 >> (Math.min(4, matchesr.length))) || (trashR.length >> (Math.min(Math.abs(4), 1))) <= 3) && frame_sqC);
      sheetK += `${privacyp.length ^ 2}`;
   let matchesb = trashR.length >= 6533532;
   do {
      trashR = [1];
      if (matchesb) {
         break;
      }
   } while (matchesb && (5 >= (trashR.length + matchesr.length) && 5 >= (trashR.length + 5)));
      thailandM *= parseFloat(`${trashR.length + 1}`);
      textw += (sheeta == String.fromCharCode(104,0) ? formu : sheeta.length);
   while (buttonR.endsWith(`${trashR.length}`)) {
       let membershipA = 5.0;
       let gesturej = String.fromCharCode(104,95,55,56,95,117,110,107,110,111,119,110,0);
       let checkbox_ = 5;
       let carousel8 = 4;
          let filln = String.fromCharCode(108,95,49,49,95,105,110,115,101,114,116,115,0);
         carousel8 |= checkbox_;
         filln = "3";
          let referrerD = String.fromCharCode(99,97,110,99,101,108,95,99,95,57,55,0);
          let bingU = false;
          let spinnerY = true;
         gesturej += "3 ^ carousel8";
         referrerD = "3";
         bingU = !spinnerY;
      for (let h = 0; h < 3; h++) {
          let uploadc = String.fromCharCode(114,95,51,50,95,100,101,110,105,97,108,0);
          let fullR = String.fromCharCode(119,97,118,101,115,121,110,116,104,95,52,95,51,49,0);
         carousel8 -= gesturej.length;
         uploadc = `${uploadc.length}`;
         fullR += `${uploadc.length + 3}`;
      }
         gesturej += `${carousel8}`;
      for (let a = 0; a < 2; a++) {
         carousel8 ^= carousel8;
      }
      let whatsappJ = 5481902 >= carousel8;
      do {
         carousel8 ^= parseInt(`${membershipA}`) & 3;
         if (whatsappJ) {
            break;
         }
      } while (whatsappJ && (1 <= (gesturej.length - 5) && (gesturej.length - 5) <= 3));
      for (let z = 0; z < 1; z++) {
          let i_playerC = String.fromCharCode(100,95,52,56,95,115,121,110,99,104,114,111,110,105,122,97,98,108,101,0);
          let selectionG = String.fromCharCode(99,95,49,49,95,102,105,120,117,112,0);
          let singlea = String.fromCharCode(101,121,98,111,97,114,100,95,52,95,55,52,0);
          let goald = String.fromCharCode(115,116,114,99,97,115,101,99,109,112,95,115,95,57,53,0);
          let moviesT: Array<any> = [String.fromCharCode(99,95,52,55,95,114,97,105,115,101,100,0), String.fromCharCode(113,95,54,55,95,101,113,117,97,108,105,122,101,114,0)];
         carousel8 /= Math.max(4, parseInt(`${membershipA}`) | 2);
         i_playerC += `${selectionG.length}`;
         selectionG += `${selectionG.length | goald.length}`;
         singlea = `${2 >> (Math.min(5, selectionG.length))}`;
         goald = `${goald.length << (Math.min(Math.abs(2), 2))}`;
         moviesT = [(String.fromCharCode(102,0) == goald ? i_playerC.length : goald.length)];
      }
      let thumbnail2 = checkbox_ >= 7879273;
      do {
          let statisticsr = String.fromCharCode(115,101,97,114,99,104,97,98,108,101,95,98,95,57,50,0);
          let bufferp = false;
         checkbox_ <<= Math.min(4, Math.abs(2 ^ gesturej.length));
         statisticsr = "1";
         bufferp = !bufferp;
         if (thumbnail2) {
            break;
         }
      } while (thumbnail2 && ((3 * carousel8) <= 4));
      while ((gesturej.length & 5) == 1 || (checkbox_ & 5) == 2) {
         gesturej += `${carousel8 | 1}`;
         break;
      }
      while (2.11 >= (gesturej.length - membershipA) && 1.93 >= (membershipA - 2.11)) {
         gesturej = "1";
         break;
      }
      let disconnected7 = checkbox_ >= 5271178;
      do {
         checkbox_ >>= Math.min(2, Math.abs(carousel8));
         if (disconnected7) {
            break;
         }
      } while (((5 & checkbox_) < 5 || (5 & checkbox_) < 2) && disconnected7);
      for (let k = 0; k < 1; k++) {
         carousel8 ^= gesturej.length << (Math.min(3, Math.abs(parseInt(`${membershipA}`))));
      }
      buttonR = `${parseInt(`${membershipA}`)}`;
      break;
   }
       let heartC = 2.0;
          let register_0gz = true;
         heartC /= Math.max(5, parseFloat(`${3 << (Math.min(Math.abs(parseInt(`${heartC}`)), 1))}`));
         register_0gz = (register_0gz ? !register_0gz : !register_0gz);
         heartC += parseFloat(`${3 + parseInt(`${heartC}`)}`);
          let pingP = String.fromCharCode(98,95,53,56,95,109,97,100,101,98,121,0);
         heartC /= Math.max(parseFloat(`${parseInt(`${heartC}`)}`), 3);
         pingP += `${(String.fromCharCode(104,0) == pingP ? pingP.length : pingP.length)}`;
      views6 += `${2 | matchesr.length}`;
       let orangeh = String.fromCharCode(103,95,57,49,95,117,100,105,111,0);
       let less3 = 1.0;
      for (let g = 0; g < 1; g++) {
         less3 -= parseFloat(`${orangeh.length << (Math.min(Math.abs(2), 1))}`);
      }
          let tooltipsmo = 1.0;
          let main_yS = 2.0;
          let brightness4 = String.fromCharCode(101,95,51,50,95,108,97,109,112,0);
         less3 /= Math.max(parseFloat(`${parseInt(`${tooltipsmo}`) * 1}`), 2);
         tooltipsmo /= Math.max(brightness4.length & 1, 5);
         main_yS /= Math.max(4, parseFloat(`${brightness4.length | 3}`));
         less3 /= Math.max(2, (parseFloat(`${String.fromCharCode(86,0) == orangeh ? parseInt(`${less3}`) : orangeh.length}`)));
         orangeh = `${(String.fromCharCode(74,0) == orangeh ? orangeh.length : parseInt(`${less3}`))}`;
      while ((parseFloat(`${orangeh.length}`) - less3) >= 2.2 && (2 % (Math.max(2, orangeh.length))) >= 1) {
         orangeh = `${2 + parseInt(`${less3}`)}`;
         break;
      }
      for (let f = 0; f < 1; f++) {
         less3 *= (parseFloat(`${orangeh == String.fromCharCode(85,0) ? parseInt(`${less3}`) : orangeh.length}`));
      }
      matchesr += `${parseInt(`${textw}`) / (Math.max(trashR.length, 8))}`;
      formu ^= parseInt(`${textw}`) - 3;
       let assistk = String.fromCharCode(97,100,97,112,116,115,95,115,95,53,54,0);
       let hejiK = 0.0;
         hejiK += 1;
      for (let j = 0; j < 1; j++) {
          let rightZ = String.fromCharCode(99,95,54,95,115,101,116,116,101,114,0);
          let penalty1: Map<any, any> = new Map([[String.fromCharCode(97,95,52,54,95,112,101,101,114,99,111,110,110,101,99,116,105,111,110,105,110,116,101,114,102,97,99,101,0),58], [String.fromCharCode(115,99,97,116,116,101,114,95,100,95,51,55,0),582], [String.fromCharCode(112,111,111,108,95,109,95,51,54,0),496]]);
          let clearz = String.fromCharCode(111,95,51,53,95,109,101,100,105,97,0);
         hejiK /= Math.max(3, 2);
         rightZ += `${rightZ.length * clearz.length}`;
         penalty1.set(rightZ, 2);
         clearz = `${(clearz == String.fromCharCode(101,0) ? rightZ.length : clearz.length)}`;
      }
      if ((assistk.length / (Math.max(3, hejiK))) > 3.95) {
         hejiK += parseInt(`${hejiK}`);
      }
       let pressurew = String.fromCharCode(105,95,55,56,95,114,111,108,101,0);
         hejiK -= pressurew.length;
      while (3.16 < (4.25 * hejiK) || 2.85 < (4.25 * hejiK)) {
         pressurew = `${pressurew.length}`;
         break;
      }
      sheetK = `${views6.length}`;
      policyI /= Math.max(5, parseFloat(`${1}`));
   while ((4 - sheetK.length) < 2 || (parseInt(`${thailandM}`) / (Math.max(6, sheetK.length))) < 4) {
      sheetK += `${shared5.length}`;
      break;
   }
      trashR = [2];
       let shrink0 = String.fromCharCode(97,116,104,95,114,95,56,54,0);
       let combineD: Map<any, any> = new Map([[String.fromCharCode(111,114,100,101,114,115,95,100,95,50,48,0),782], [String.fromCharCode(113,95,57,52,95,117,110,101,110,99,114,121,112,116,101,100,0),880], [String.fromCharCode(108,95,55,51,95,101,108,105,115,116,0),661]]);
       let spinner7 = true;
      let l_lockA = combineD.size >= 8776582;
      do {
         combineD = new Map([[`${combineD.size}`, 2]]);
         if (l_lockA) {
            break;
         }
      } while (((2 ^ combineD.size) < 1) && l_lockA);
         shrink0 = "2";
      let predictionJ = spinner7 ? !spinner7 : spinner7;
      do {
          let favoriteI = 2.0;
         spinner7 = 6 == shrink0.length;
         favoriteI -= parseInt(`${favoriteI}`) ^ parseInt(`${favoriteI}`);
         if (predictionJ) {
            break;
         }
      } while (predictionJ && (spinner7));
      let yellow0 = shrink0 == String.fromCharCode(115,116,49,114,102,111,121,0);
      do {
         shrink0 += "3";
         if (yellow0) {
            break;
         }
      } while (yellow0 && (combineD.size > 3));
      for (let n = 0; n < 1; n++) {
         shrink0 = `${(shrink0 == String.fromCharCode(57,0) ? combineD.size : shrink0.length)}`;
      }
      while (1 >= combineD.size) {
         combineD.set(`${spinner7}`, shrink0.length);
         break;
      }
      let default_ytz = 9733540 <= combineD.size;
      do {
         combineD = new Map([[shrink0, (String.fromCharCode(84,0) == shrink0 ? shrink0.length : (spinner7 ? 5 : 5))]]);
         if (default_ytz) {
            break;
         }
      } while (default_ytz && (4 >= (combineD.size & shrink0.length) && (shrink0.length & combineD.size) >= 4));
      for (let d = 0; d < 2; d++) {
         combineD = new Map([[shrink0, ((spinner7 ? 5 : 4) ^ 3)]]);
      }
      while (2 == (1 - combineD.size)) {
         combineD.set(`${shrink0}`, shrink0.length - combineD.size);
         break;
      }
      trashR = [3 + parseInt(`${policyI}`)];
      privacyp = `${privacyp.length << (Math.min(1, Math.abs(parseInt(`${textw}`))))}`;
      buttonR += `${trashR.length % 2}`;
       let loading_ = String.fromCharCode(97,98,111,118,101,95,98,95,53,48,0);
         loading_ += `${loading_.length}`;
         loading_ += `${(String.fromCharCode(71,0) == loading_ ? loading_.length : loading_.length)}`;
      let views8 = loading_ == String.fromCharCode(102,95,51,113,112,120,122,97,57,0);
      do {
         loading_ += `${(loading_ == String.fromCharCode(75,0) ? loading_.length : loading_.length)}`;
         if (views8) {
            break;
         }
      } while ((3 > loading_.length) && views8);
      views6 = `${shared5.length & loading_.length}`;
      alertt += `${2 - shared5.length}`;
      textw /= Math.max(2, formu);
   for (let a = 0; a < 1; a++) {
       let frame_fjH = String.fromCharCode(110,111,110,99,101,95,119,95,54,53,0);
       let zhubo9 = String.fromCharCode(119,111,114,107,97,114,111,117,110,100,95,114,95,55,53,0);
       let sentryo = 2;
       let reminderR: Array<any> = [374, 791];
       let productQ = 5.0;
          let update_7fX: Array<any> = [678, 458, 762];
          let cross4 = String.fromCharCode(108,95,50,53,95,109,97,106,111,114,0);
          let favoritey = String.fromCharCode(105,110,99,114,101,109,101,110,116,97,108,95,120,95,50,54,0);
         zhubo9 = `${update_7fX.length & parseInt(`${productQ}`)}`;
         update_7fX = [cross4.length / 3];
         cross4 += `${cross4.length}`;
         favoritey = "2";
       let type_5g8 = 2.0;
       let clearv = 4.0;
         frame_fjH += `${frame_fjH.length - 2}`;
       let rank1 = 4.0;
       let stylem = 2.0;
         productQ -= parseInt(`${productQ}`) & 3;
      while (1.15 == (stylem / (Math.max(parseFloat(`${frame_fjH.length}`), 6))) && (stylem / 1.15) == 2.41) {
         frame_fjH += `${(zhubo9 == String.fromCharCode(107,0) ? parseInt(`${stylem}`) : zhubo9.length)}`;
         break;
      }
      if (5 >= (sentryo << (Math.min(Math.abs(5), 1)))) {
         reminderR = [1 - frame_fjH.length];
      }
      let successJ = 7420011 >= frame_fjH.length;
      do {
         frame_fjH = `${zhubo9.length}`;
         if (successJ) {
            break;
         }
      } while ((frame_fjH.length == parseInt(`${rank1}`)) && successJ);
         type_5g8 -= parseInt(`${stylem}`) & 2;
          let taiwanG = String.fromCharCode(109,95,53,54,95,102,100,99,116,100,115,112,0);
         sentryo <<= Math.min(4, Math.abs(2));
         taiwanG += `${taiwanG.length}`;
       let casting3 = String.fromCharCode(102,108,111,111,114,95,117,95,54,55,0);
       let telegramW = String.fromCharCode(109,95,56,57,95,116,111,109,111,114,114,111,119,0);
         sentryo &= parseInt(`${rank1}`);
          let rulesw = 3.0;
          let target0 = String.fromCharCode(114,95,49,55,95,105,110,112,111,115,0);
         clearv /= Math.max(reminderR.length, 4);
         rulesw *= (parseFloat(`${String.fromCharCode(90,0) == target0 ? parseInt(`${rulesw}`) : target0.length}`));
         type_5g8 += casting3.length | parseInt(`${productQ}`);
      let gpayU = telegramW == String.fromCharCode(120,109,104,117,106,97,48,103,97,0);
      do {
         telegramW += `${parseInt(`${productQ}`)}`;
         if (gpayU) {
            break;
         }
      } while (gpayU && (telegramW.includes(`${type_5g8}`)));
      alertt = `${3 ^ parseInt(`${policyI}`)}`;
   }
   let humidityO = 9749831 <= sheetK.length;
   do {
      sheetK += "3";
      if (humidityO) {
         break;
      }
   } while (humidityO && (matchesr.length > sheetK.length));

    setIsDialogOpen(!isDialogOpen);
  };

  return (
    // <SafeAreaView>
    <ScreenContainer>
      <View style={{ gap: spacing.m }}>
        <TitleWithBackButtonHeader
          title="活动规则"
          headerStyle={{ marginBottom: spacing.m }}
        />

        <View style={{ marginBottom: 100, paddingLeft: 20, paddingRight: 35 }}>
          <Text style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}>
            每推荐成功1位好友，即可增加VIP会员30天，好友获得30天VIP会员
          </Text>

          { }
          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",

              marginTop: 10,
            }}
          >
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              1. 推荐成功条件
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              推荐好友
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              好友下载{APP_NAME_CONST} APP
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              好友注册{APP_NAME_CONST} APP（需填写邀请码）
            </Text>
          </View>

          <Text style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}>
            好友通过您的分享链接，下载并打开app后，注册成功（需填写邀请码）成为新用户即视为推荐成功，双方均可获得奖励
          </Text>

          { }
          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",

              marginTop: 10,
            }}
          >
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              2. 受邀者奖励内容
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              受邀者通过好友分享链接下载并注册（需填写邀请码）成为新用户后，即可获得30日VIP会员，需注意如使用同一台设备或同一个账号无法重复获得奖励
            </Text>
          </View>

          { }
          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",

              marginTop: 10,
            }}
          >
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              3. 附加规则
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              用户达成奖励条件后，奖励会立即生效
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              同一设备、同一账号视为同一用户
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              如果有发现用户存在恶意使用虚假账号领取奖励多情况，平台有权利取消奖励或保留终止活动的权利
            </Text>
          </View>
        </View>
      </View>
    </ScreenContainer>
    // </SafeAreaView>
  );
};

const styles = StyleSheet.create({
  text: {},
  bullet: {
    width: 6,
    height: 6,
    borderRadius: 4,
    backgroundColor: "#fff", 
    marginLeft: 5, 
    marginRight: 8, 
    marginTop: 8,
  },
});
