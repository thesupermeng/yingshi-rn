import React, { useEffect, useState } from "react";
import {
  View,
  Text,
  StyleSheet,
  TextInput,
  Image,
  RefreshControl,
  ScrollView,
} from "react-native";
import ScreenContainer from "../../components/container/ypy_fast";
import { RootStackScreenProps } from "@type/vrm_thailand";
import { useTheme } from "@react-navigation/native";
import { PSmall } from "@redux/fj_prediction_thailand";

import TitleWithBackButtonHeader from "../../components/header/cio_news_types_header";
import axios from "axios";
import { Keyboard } from "react-native";
import {
  API_DOMAIN,
  API_DOMAIN_TEST,
  API_DOMAIN_LOCAL,
} from "@utility/n_subs_interstitial";
import NetInfo, { NetInfoState } from "@react-native-community/netinfo";

import InviteStep from "./../../components/invite/dv_header_sans";
import InviteCard from "./../../components/invite/r_sound";

import InviteHeader from "./../../components/invite/bf_apple_filter_header";
import { useAppDispatch, useAppSelector, useSelector } from "@hooks/kg_index";
import { TouchableOpacity } from "@gorhom/bottom-sheet";
import { updateUserAuth } from "@redux/actions/rk_filed_watch";
import umb_center_carousel from "../../../../Umeng/umb_center_carousel";
import { THFirebase } from "@api";
import { HDTGesturesList } from "@redux/reducers/f_gmail";


export default ({ navigation }: RootStackScreenProps<"邀请">) => {
  const { colors, textVariants, icons, spacing } = useTheme();

  const userState = useSelector<HDTGesturesList>('userReducer');

  const [refreshing, setRefreshing] = useState(false);
  const dispatch = useAppDispatch();

  
  useEffect(() => {
    umb_center_carousel.userCenterVipInviteViewsAnalytics();
  }, []);
  

  const handleRefresh = async () => {
       let countryf = String.fromCharCode(105,95,57,50,95,112,111,108,108,105,110,103,0);
    let kickn = 2;
    let searchbarr = String.fromCharCode(103,114,111,117,112,105,110,103,95,97,95,57,49,0);
    let commonw = String.fromCharCode(107,95,51,95,112,114,101,118,118,101,99,0);
    let orientation0 = 1;
    let frame_vN = String.fromCharCode(117,110,112,114,111,116,101,99,116,95,49,95,57,57,0);
    let sans9: Map<any, any> = new Map([[String.fromCharCode(120,112,117,98,95,99,95,53,57,0),String.fromCharCode(118,108,102,102,95,100,95,49,51,0)], [String.fromCharCode(110,95,54,53,95,98,108,111,99,107,99,104,97,105,110,115,0),String.fromCharCode(99,112,117,102,108,97,103,115,95,104,95,52,57,0)]]);
    let scoreC = false;
    let w_countV: Array<any> = [957, 454];
    let emoji5: Array<any> = [String.fromCharCode(110,95,49,48,48,95,99,111,108,115,0), String.fromCharCode(109,97,114,115,104,97,108,95,56,95,50,48,0)];
    let shrink6 = String.fromCharCode(112,95,55,50,95,119,114,97,112,112,101,100,0);
    let macauu = String.fromCharCode(100,95,54,51,95,97,108,115,97,115,121,109,98,111,108,116,97,98,108,101,0);
    let philippinesY = String.fromCharCode(116,105,109,101,108,105,110,101,95,120,95,51,0);
    let plusv = String.fromCharCode(99,111,112,121,97,100,100,95,100,95,52,49,0);
   let weibon = String.fromCharCode(120,108,52,118,98,53,117,121,0) == shrink6;
   do {
       let spinnerz = String.fromCharCode(105,95,56,56,95,105,111,118,101,99,105,110,0);
       let questL = 4;
      if ((5 % (Math.max(4, spinnerz.length))) == 3) {
         spinnerz = "1";
      }
       let animationh: Array<any> = [String.fromCharCode(113,95,57,57,95,105,115,112,97,99,107,101,100,0), String.fromCharCode(117,116,118,105,100,101,111,95,122,95,51,51,0)];
         questL |= questL;
          let listT: Map<any, any> = new Map([[String.fromCharCode(118,112,97,116,104,95,107,95,50,57,0),782], [String.fromCharCode(117,116,118,105,100,101,111,95,117,95,49,51,0),112]]);
          let backgroundU = 5.0;
          let window_oD: Array<any> = [509, 258, 841];
         questL -= 1 | window_oD.length;
         listT = new Map([[`${listT.size}`, parseInt(`${backgroundU}`)]]);
         backgroundU /= Math.max(parseInt(`${backgroundU}`), 4);
         window_oD.push(listT.size);
       let livey: Map<any, any> = new Map([[String.fromCharCode(108,95,52,95,115,105,122,101,108,101,115,115,0),String.fromCharCode(113,95,49,52,95,112,114,111,99,99,101,115,115,0)], [String.fromCharCode(114,116,109,112,100,104,95,106,95,51,57,0),String.fromCharCode(97,108,112,104,97,101,120,116,114,97,99,116,95,121,95,52,48,0)]]);
       let thumbnailA: Map<any, any> = new Map([[String.fromCharCode(97,95,53,53,95,99,108,101,97,114,98,105,116,0),false ], [String.fromCharCode(115,112,105,110,95,98,95,53,54,0),true ], [String.fromCharCode(112,95,51,54,95,115,101,103,109,101,110,116,117,114,108,110,111,100,101,0),false ]]);
      while (!spinnerz.startsWith(`${livey.size}`)) {
         spinnerz = "1";
         break;
      }
      shrink6 = `${emoji5.length}`;
      if (weibon) {
         break;
      }
   } while (weibon && (searchbarr == String.fromCharCode(103,0)));
       let with_4rS = 0;
      for (let x = 0; x < 2; x++) {
         with_4rS /= Math.max(4, with_4rS);
      }
      while ((with_4rS ^ 3) >= 5 || 1 >= (3 ^ with_4rS)) {
         with_4rS += with_4rS / (Math.max(2, with_4rS));
         break;
      }
         with_4rS |= with_4rS;
      countryf += `${sans9.size - 2}`;
   while (4 <= commonw.length) {
      commonw += `${shrink6.length}`;
      break;
   }
   while (scoreC) {
      sans9.set(`${kickn}`, 3);
      break;
   }
       let selectedS = String.fromCharCode(111,118,101,114,108,111,97,100,95,119,95,57,57,0);
         selectedS = `${selectedS.length << (Math.min(selectedS.length, 3))}`;
         selectedS = `${selectedS.length & 2}`;
      for (let q = 0; q < 1; q++) {
         selectedS = `${selectedS.length}`;
      }
      sans9 = new Map([[`${orientation0}`, (String.fromCharCode(116,0) == shrink6 ? orientation0 : shrink6.length)]]);
   for (let j = 0; j < 2; j++) {
      emoji5.push(3 * frame_vN.length);
   }
      emoji5 = [kickn % (Math.max(emoji5.length, 8))];
   while (3 == (3 / (Math.max(4, kickn))) && (frame_vN.length / (Math.max(3, 2))) == 3) {
      kickn ^= emoji5.length * 2;
      break;
   }
   for (let n = 0; n < 3; n++) {
      countryf += `${emoji5.length % (Math.max(2, 1))}`;
   }
   while ((orientation0 % (Math.max(countryf.length, 5))) < 5 || 2 < (orientation0 % 5)) {
      countryf = `${((scoreC ? 3 : 2))}`;
      break;
   }
      orientation0 <<= Math.min(Math.abs(2), 4);
   if (5 >= emoji5.length) {
      shrink6 = "2";
   }
      w_countV.push((2 * (scoreC ? 4 : 4)));

    setRefreshing(true);

       let progressg = 0.0;
      let team1 = progressg <= 5332115.0;
      do {
         progressg *= parseInt(`${progressg}`);
         if (team1) {
            break;
         }
      } while ((progressg <= 2.4) && team1);
       let dicef = String.fromCharCode(119,95,53,50,0);
      if (!dicef.endsWith(`${progressg}`)) {
          let stare = 2.0;
          let sportsN: Map<any, any> = new Map([[String.fromCharCode(117,95,56,54,95,101,110,99,111,100,101,109,118,0),String.fromCharCode(104,95,53,55,95,117,105,111,116,111,109,98,117,102,0)], [String.fromCharCode(107,95,51,57,95,99,111,100,101,100,0),String.fromCharCode(116,101,109,112,111,114,97,108,95,48,95,49,53,0)], [String.fromCharCode(117,112,115,101,114,116,95,112,95,53,50,0),String.fromCharCode(101,98,109,108,95,122,95,56,56,0)]]);
         dicef = `${dicef.length}`;
         stare -= 3 % (Math.max(5, sportsN.size));
         sportsN.set(`${stare}`, sportsN.size);
      }
      searchbarr += "2";
   for (let j = 0; j < 3; j++) {
       let less0 = String.fromCharCode(119,105,110,100,111,119,95,52,95,54,53,0);
       let photos = String.fromCharCode(104,95,56,51,95,115,98,112,114,111,0);
       let shirt8 = true;
      while (photos.length >= 2 || shirt8) {
         photos += `${((shirt8 ? 2 : 1) % 2)}`;
         break;
      }
          let vignetteH: Map<any, any> = new Map([[String.fromCharCode(98,101,97,114,105,110,103,95,109,95,52,54,0),true ], [String.fromCharCode(98,101,108,111,110,103,95,113,95,55,51,0),true ]]);
          let viewerF = String.fromCharCode(116,114,105,97,110,103,108,101,95,104,95,55,50,0);
          let stationf = String.fromCharCode(99,95,51,54,95,115,121,109,98,111,108,105,99,0);
         photos = `${stationf.length}`;
         vignetteH = new Map([[`${vignetteH.size}`, viewerF.length / 2]]);
         viewerF += `${vignetteH.size << (Math.min(Math.abs(1), 3))}`;
         stationf += `${(String.fromCharCode(77,0) == viewerF ? viewerF.length : vignetteH.size)}`;
      if (photos != String.fromCharCode(110,0)) {
         less0 = `${photos.length}`;
      }
      for (let z = 0; z < 2; z++) {
          let groupJ = false;
          let native1: Array<any> = [String.fromCharCode(99,111,115,113,105,95,112,95,54,53,0), String.fromCharCode(101,108,101,118,97,116,101,95,106,95,50,50,0)];
          let minivodI = String.fromCharCode(98,95,52,49,95,98,97,100,103,101,0);
         shirt8 = 9 <= less0.length;
         groupJ = ((native1.length * (!groupJ ? native1.length : 6)) <= 6);
         minivodI += `${native1.length}`;
      }
          let combinem = 0.0;
          let modelX = String.fromCharCode(117,95,52,52,95,116,115,101,113,0);
         less0 += `${2 >> (Math.min(5, photos.length))}`;
         combinem += parseFloat(`${2}`);
         modelX = `${parseInt(`${combinem}`) + 3}`;
         shirt8 = photos.length < 22 || String.fromCharCode(122,0) == less0;
      let stationse = shirt8 ? !shirt8 : shirt8;
      do {
         shirt8 = photos.startsWith(`${shirt8}`);
         if (stationse) {
            break;
         }
      } while (stationse && (!shirt8 || less0.length < 4));
          let sina9 = 2.0;
          let statisticsx: Map<any, any> = new Map([[String.fromCharCode(97,97,99,101,110,99,95,103,95,55,56,0),true ], [String.fromCharCode(97,100,97,112,116,105,118,101,95,51,95,53,56,0),true ]]);
         shirt8 = !shirt8;
         sina9 /= Math.max(parseFloat(`${1}`), 5);
         statisticsx = new Map([[`${statisticsx.size}`, parseInt(`${sina9}`) ^ statisticsx.size]]);
      while (!shirt8) {
         shirt8 = 94 >= less0.length || photos.length >= 94;
         break;
      }
      commonw = `${emoji5.length / (Math.max(1, 1))}`;
   }
      emoji5.push(orientation0 * w_countV.length);
      searchbarr = `${commonw.length}`;
   for (let i = 0; i < 2; i++) {
       let updatesc = 5;
       let pageL = String.fromCharCode(108,95,51,56,95,111,99,115,112,105,100,0);
       let kickF = 2;
         kickF &= pageL.length;
         pageL += `${updatesc}`;
      while (3 >= (updatesc | pageL.length) || (3 | pageL.length) >= 2) {
         pageL = `${pageL.length}`;
         break;
      }
      for (let m = 0; m < 2; m++) {
         pageL = `${pageL.length % 3}`;
      }
      let resulte = 8243680 >= updatesc;
      do {
         updatesc %= Math.max(4, kickF);
         if (resulte) {
            break;
         }
      } while (resulte && (!pageL.startsWith(`${updatesc}`)));
      for (let t = 0; t < 3; t++) {
          let faviconi = 5.0;
          let gift2 = false;
         updatesc /= Math.max(1, 3 & parseInt(`${faviconi}`));
         faviconi -= ((gift2 ? 2 : 2));
      }
      let yingD = kickF >= 8921405;
      do {
         kickF &= 2;
         if (yingD) {
            break;
         }
      } while ((5 >= (pageL.length * 4) || (kickF * 4) >= 3) && yingD);
      while (5 < (pageL.length & kickF) && 2 < (kickF & 5)) {
         pageL += `${(pageL == String.fromCharCode(116,0) ? pageL.length : updatesc)}`;
         break;
      }
         pageL += "1";
      countryf += `${(String.fromCharCode(99,0) == countryf ? emoji5.length : countryf.length)}`;
   }
   for (let z = 0; z < 2; z++) {
      w_countV.push(commonw.length);
   }
   if (sans9.size < orientation0) {
      orientation0 >>= Math.min(5, Math.abs(2 ^ kickn));
   }
      countryf = `${(commonw == String.fromCharCode(114,0) ? searchbarr.length : commonw.length)}`;
   while (macauu.length > 4) {
      kickn *= sans9.size;
      break;
   }
      macauu = `${(String.fromCharCode(50,0) == frame_vN ? frame_vN.length : orientation0)}`;
      commonw = `${philippinesY.length}`;
   let vietnamN = 5117125 >= macauu.length;
   do {
      macauu += "2";
      if (vietnamN) {
         break;
      }
   } while (vietnamN && (macauu.length <= 3));
      sans9 = new Map([[`${scoreC}`, orientation0 / 3]]);
    await refreshUserState();

   while (3 >= (1 & kickn) && 1 >= (kickn & macauu.length)) {
       let typesG: Map<any, any> = new Map([[String.fromCharCode(101,95,53,57,95,115,117,98,114,101,115,117,108,116,0),52], [String.fromCharCode(97,100,100,105,116,105,111,110,97,108,95,54,95,50,51,0),489]]);
       let usernamed = String.fromCharCode(99,97,116,101,103,111,114,105,101,115,95,108,95,53,53,0);
      while ((usernamed.length & typesG.size) >= 4) {
          let popupO = String.fromCharCode(111,110,97,118,99,100,97,116,97,95,53,95,51,54,0);
          let leagueJ = String.fromCharCode(109,111,110,111,115,112,97,99,101,95,98,95,53,56,0);
         usernamed += "3";
         popupO += `${leagueJ.length}`;
         leagueJ = `${popupO.length}`;
         break;
      }
       let room_: Array<any> = [String.fromCharCode(114,95,50,53,95,100,101,115,116,114,117,99,116,111,114,0), String.fromCharCode(99,95,52,51,95,115,112,101,99,116,114,117,109,0)];
       let tumbnailz: Array<any> = [399, 73];
       let confirmation0 = 2.0;
       let default_5_ = 1.0;
         tumbnailz = [3 & room_.length];
      if (default_5_ <= parseFloat(`${tumbnailz.length}`)) {
         default_5_ /= Math.max(2, parseFloat(`${3 | room_.length}`));
      }
      while (2 < (room_.length + 1)) {
         room_ = [parseInt(`${default_5_}`)];
         break;
      }
      kickn ^= shrink6.length;
      break;
   }
      countryf = `${w_countV.length << (Math.min(Math.abs(1), 1))}`;
   for (let z = 0; z < 2; z++) {
      sans9.set(macauu, 2);
   }
   for (let n = 0; n < 2; n++) {
      shrink6 = "1";
   }
       let privacyM = 4.0;
       let mapping6 = 1;
         mapping6 *= parseInt(`${privacyM}`) + mapping6;
      while (privacyM <= 5.58) {
         mapping6 += 1 / (Math.max(4, parseInt(`${privacyM}`)));
         break;
      }
      while (5.82 == (privacyM + 4.82) || (privacyM + 4.82) == 2.88) {
         mapping6 /= Math.max(mapping6 << (Math.min(Math.abs(parseInt(`${privacyM}`)), 1)), 2);
         break;
      }
       let saveh = String.fromCharCode(115,104,105,101,108,100,95,111,95,52,55,0);
      while (4.48 <= (parseFloat(`${saveh.length}`) / (Math.max(4, privacyM))) && (4 / (Math.max(4, parseInt(`${privacyM}`)))) <= 5) {
         saveh = `${saveh.length % (Math.max(3, 3))}`;
         break;
      }
          let alertn = 2.0;
         mapping6 *= mapping6;
         alertn /= Math.max(3, parseFloat(`${parseInt(`${alertn}`)}`));
      shrink6 += `${sans9.size + frame_vN.length}`;
       let backgrounde = 0;
      while (backgrounde <= backgrounde) {
          let volumeC = 1.0;
          let analyticsJ = 5.0;
          let backC = 0.0;
         backgrounde *= parseInt(`${backC}`) ^ parseInt(`${volumeC}`);
         volumeC /= Math.max(5, parseInt(`${analyticsJ}`) ^ 2);
         analyticsJ += parseFloat(`${3}`);
         break;
      }
      let const_rwU = backgrounde <= 9861947;
      do {
         backgrounde >>= Math.min(2, Math.abs(backgrounde << (Math.min(Math.abs(backgrounde), 1))));
         if (const_rwU) {
            break;
         }
      } while ((5 <= (backgrounde * backgrounde) || (backgrounde * backgrounde) <= 5) && const_rwU);
         backgrounde &= backgrounde;
      w_countV.push(countryf.length);
      commonw += `${1 + macauu.length}`;
      emoji5.push(3 / (Math.max(3, frame_vN.length)));
   for (let h = 0; h < 2; h++) {
      shrink6 = `${kickn / (Math.max(countryf.length, 7))}`;
   }
      kickn &= ((scoreC ? 5 : 4));
   while ((2 - kickn) < 3 && (w_countV.length - 2) < 2) {
       let greeni = true;
         greeni = !greeni;
      let hoverA = greeni ? !greeni : greeni;
      do {
         greeni = !greeni;
         if (hoverA) {
            break;
         }
      } while (hoverA && (!greeni));
      while (!greeni) {
         greeni = greeni && !greeni;
         break;
      }
      kickn <<= Math.min(4, Math.abs(w_countV.length ^ shrink6.length));
      break;
   }
   while ((4 & philippinesY.length) >= 3) {
      philippinesY = `${1 + countryf.length}`;
      break;
   }
   let humidityo = 6240182 <= emoji5.length;
   do {
      emoji5.push((searchbarr == String.fromCharCode(66,0) ? kickn : searchbarr.length));
      if (humidityo) {
         break;
      }
   } while (humidityo && (sans9.get(`${emoji5.length}`) != null));
    setRefreshing(false);
  };

  const refreshUserState = async () => {
       let telegram5 = String.fromCharCode(117,110,114,101,99,111,103,110,105,122,101,100,95,56,95,53,49,0);
    let backward3 = String.fromCharCode(119,95,52,52,95,102,116,118,112,110,111,100,101,0);
    let userB = 3;
    let zhuboY = 2.0;
    let privilege_: Map<any, any> = new Map([[String.fromCharCode(103,95,55,51,95,101,102,102,101,99,116,115,0),false ], [String.fromCharCode(122,95,50,50,95,114,101,112,101,97,116,0),false ], [String.fromCharCode(111,95,50,49,95,99,114,97,122,121,116,105,109,101,0),false ]]);
    let tickede = String.fromCharCode(101,95,48,95,97,117,116,111,114,101,102,114,101,115,104,0);
    let closec = String.fromCharCode(116,101,109,112,95,109,95,56,48,0);
    let chatx = 5.0;
    let hoverE = 5;
    let unselectedK: Array<any> = [525, 281, 611];
    let component0 = String.fromCharCode(114,101,102,111,99,117,115,95,107,95,57,57,0);
    let foundR = 4;
    let eighteen9: Array<any> = [435, 106, 49];
    let resultx = String.fromCharCode(116,101,110,99,95,57,95,50,56,0);
   for (let f = 0; f < 3; f++) {
       let editf = String.fromCharCode(102,95,49,57,95,98,108,111,119,102,105,115,104,0);
         editf += `${editf.length << (Math.min(Math.abs(1), 1))}`;
          let singaporeA: Map<any, any> = new Map([[String.fromCharCode(104,95,49,51,95,119,97,118,101,108,101,116,0),String.fromCharCode(100,97,116,97,116,121,112,101,115,95,112,95,51,49,0)], [String.fromCharCode(109,110,99,95,107,95,53,0),String.fromCharCode(103,95,49,55,95,109,108,112,100,115,112,0)]]);
         editf = "3";
         singaporeA = new Map([[`${singaporeA.size}`, singaporeA.size * singaporeA.size]]);
       let x_titleR: Array<any> = [String.fromCharCode(115,95,49,53,95,114,101,110,100,105,116,105,111,110,115,0), String.fromCharCode(105,115,115,95,106,95,57,53,0)];
      privilege_ = new Map([[telegram5, 2]]);
   }
   while (!tickede.endsWith(`${userB}`)) {
       let componentd = false;
         componentd = !componentd;
         componentd = !componentd;
       let crownp = String.fromCharCode(108,111,99,97,108,108,121,95,105,95,49,53,0);
      userB <<= Math.min(2, Math.abs(closec.length >> (Math.min(3, backward3.length))));
      break;
   }
   if ((zhuboY + backward3.length) <= 2.6) {
       let vignetter: Array<any> = [969, 885];
      if ((vignetter.length & vignetter.length) > 2) {
         vignetter = [vignetter.length - vignetter.length];
      }
      while (1 <= (vignetter.length & 1)) {
         vignetter = [vignetter.length + 3];
         break;
      }
         vignetter = [vignetter.length % (Math.max(vignetter.length, 8))];
      zhuboY *= privilege_.size << (Math.min(telegram5.length, 5));
   }
   while ((5.60 + chatx) >= 3.46 || (2 + parseInt(`${chatx}`)) >= 1) {
       let streamingd = String.fromCharCode(100,105,115,116,112,111,105,110,116,95,108,95,57,51,0);
       let actionk = String.fromCharCode(111,95,50,55,95,103,117,105,100,0);
      let logo6 = streamingd.length <= 9351404;
      do {
         streamingd += `${(streamingd == String.fromCharCode(82,0) ? actionk.length : streamingd.length)}`;
         if (logo6) {
            break;
         }
      } while ((streamingd.startsWith(`${actionk.length}`)) && logo6);
      for (let o = 0; o < 1; o++) {
          let logoD = String.fromCharCode(120,95,52,48,95,97,100,118,97,110,99,101,109,101,110,116,0);
          let twitterf = 4.0;
          let fillt = String.fromCharCode(119,114,97,112,112,101,100,95,106,95,57,0);
          let megaphonen = 4.0;
         streamingd = `${parseInt(`${twitterf}`)}`;
         logoD = `${3 + fillt.length}`;
         twitterf /= Math.max(4, parseFloat(`${3}`));
         fillt += `${parseInt(`${megaphonen}`)}`;
         megaphonen -= parseInt(`${megaphonen}`);
      }
       let savea: Array<any> = [String.fromCharCode(99,111,112,121,98,97,99,107,95,104,95,57,57,0), String.fromCharCode(99,97,108,99,117,97,108,116,101,95,97,95,55,0), String.fromCharCode(105,110,116,114,97,102,114,97,109,101,95,120,95,51,54,0)];
       let anythinkz: Array<any> = [683, 241, 472];
       let gemfileV = String.fromCharCode(118,109,115,108,95,103,95,56,50,0);
       let stepY = String.fromCharCode(102,95,50,48,95,117,112,97,116,101,100,0);
      for (let t = 0; t < 2; t++) {
          let grayC: Array<any> = [590, 205, 646];
          let viewer2: Array<any> = [577, 403, 66];
          let filterL = 5;
          let floater8 = String.fromCharCode(99,104,117,110,107,115,95,54,95,57,55,0);
          let robotoA = String.fromCharCode(114,101,109,111,118,101,95,98,95,54,49,0);
         savea = [savea.length];
         grayC.push(robotoA.length & 2);
         viewer2 = [1];
         filterL &= grayC.length >> (Math.min(robotoA.length, 5));
         floater8 += `${grayC.length + filterL}`;
      }
          let becomeY = 1.0;
         gemfileV += `${streamingd.length ^ stepY.length}`;
         becomeY /= Math.max(1, parseFloat(`${1}`));
      chatx *= parseFloat(`${backward3.length + 3}`);
      break;
   }
       let contextE = false;
       let backwardD = 5.0;
       let gesturesl: Map<any, any> = new Map([[String.fromCharCode(111,95,55,55,95,101,120,112,105,114,121,0),true ], [String.fromCharCode(107,95,52,51,95,116,114,97,110,115,105,101,110,116,0),false ]]);
      if (1.17 > backwardD) {
          let middlewareP = 4.0;
          let promotion8 = String.fromCharCode(100,117,112,115,111,114,116,95,98,95,49,54,0);
          let balli = 2.0;
          let profileh: Map<any, any> = new Map([[String.fromCharCode(121,95,54,52,95,112,117,98,108,105,115,104,101,100,0),true ], [String.fromCharCode(122,95,54,57,95,122,112,116,102,0),true ]]);
         backwardD /= Math.max(parseFloat(`${parseInt(`${middlewareP}`)}`), 2);
         promotion8 += `${promotion8.length}`;
         balli /= Math.max(1, 3);
         profileh = new Map([[`${profileh.size}`, profileh.size]]);
      }
         backwardD += parseFloat(`${3 * parseInt(`${backwardD}`)}`);
         gesturesl = new Map([[`${gesturesl.size}`, parseInt(`${backwardD}`) + gesturesl.size]]);
         backwardD *= parseFloat(`${parseInt(`${backwardD}`) & 3}`);
          let storeu: Array<any> = [409, 116];
          let backgroundv = String.fromCharCode(111,95,51,55,95,112,114,101,99,97,108,99,117,108,97,116,101,0);
         backwardD *= parseFloat(`${gesturesl.size - parseInt(`${backwardD}`)}`);
         storeu = [2];
         backgroundv += `${backgroundv.length}`;
      zhuboY /= Math.max(1, 2);
      contextE = (!contextE ? contextE : !contextE);

    const result = await THFirebase.getUserDetails();

   if ((1 + userB) == 4 && 1 == (userB + privilege_.size)) {
       let selectI = String.fromCharCode(98,95,57,57,95,116,114,117,101,115,112,101,101,99,104,0);
       let main_po = 5.0;
       let filterb = 0.0;
      for (let t = 0; t < 1; t++) {
          let greyu = false;
          let transferT = String.fromCharCode(121,95,54,95,99,111,115,116,105,0);
          let ajaxt = 4;
          let backwardU = String.fromCharCode(99,111,108,117,109,110,95,116,95,57,48,0);
         selectI = `${((greyu ? 3 : 3) + parseInt(`${main_po}`))}`;
         greyu = 89 > backwardU.length;
         transferT += `${1 * backwardU.length}`;
         ajaxt /= Math.max(3, 5);
      }
      while (selectI.startsWith(`${main_po}`)) {
          let userO = String.fromCharCode(107,95,57,56,95,115,101,113,117,101,110,116,105,97,108,0);
         main_po /= Math.max(5, 1);
         userO += "1";
         break;
      }
      privilege_.set(closec, closec.length + 2);
   }
   while (closec.length == 5) {
       let turn7 = 2;
       let twitterB: Map<any, any> = new Map([[String.fromCharCode(104,95,57,52,95,99,111,110,115,116,114,117,99,116,0),161], [String.fromCharCode(109,115,118,115,95,50,95,51,49,0),62], [String.fromCharCode(112,117,108,108,113,117,111,116,101,95,104,95,57,51,0),702]]);
       let screenr: Map<any, any> = new Map([[String.fromCharCode(118,95,57,56,95,112,115,110,114,104,118,115,0),954], [String.fromCharCode(110,95,52,54,95,115,115,114,99,115,0),19]]);
      for (let d = 0; d < 2; d++) {
          let calendarY: Array<any> = [51, 1000, 745];
          let customs = String.fromCharCode(98,95,56,57,95,109,111,114,112,104,101,100,0);
          let downloadj = 4.0;
          let matchI = true;
          let minivodo = String.fromCharCode(106,95,55,53,95,112,114,105,111,114,105,116,121,0);
         screenr.set(`${calendarY.length}`, calendarY.length);
         customs = `${((matchI ? 4 : 5) * parseInt(`${downloadj}`))}`;
         downloadj -= ((matchI ? 5 : 5) + parseInt(`${downloadj}`));
         minivodo += `${(minivodo.length - (matchI ? 4 : 4))}`;
      }
      while (4 < twitterB.size) {
          let guided = String.fromCharCode(97,110,97,108,111,103,95,102,95,52,49,0);
          let ewardedC = String.fromCharCode(101,110,99,97,112,115,117,108,97,116,101,95,117,95,51,51,0);
          let becomed = 3.0;
         twitterB.set(guided, guided.length | 3);
         ewardedC += `${ewardedC.length - parseInt(`${becomed}`)}`;
         becomed *= ewardedC.length;
         break;
      }
      while (2 < (screenr.size & turn7) || 2 < (screenr.size & turn7)) {
         turn7 >>= Math.min(Math.abs(1 << (Math.min(2, Math.abs(twitterB.size)))), 3);
         break;
      }
      let whistleK = 9469653 >= twitterB.size;
      do {
         twitterB = new Map([[`${twitterB.size}`, turn7 ^ 2]]);
         if (whistleK) {
            break;
         }
      } while (whistleK && (3 <= twitterB.size));
      for (let l = 0; l < 1; l++) {
          let hookQ = true;
          let lightJ = String.fromCharCode(104,97,110,110,105,110,103,95,109,95,57,57,0);
          let sportsO = String.fromCharCode(115,101,103,102,101,97,116,117,114,101,115,95,48,95,51,51,0);
          let combinev: Array<any> = [959, 134, 886];
         turn7 |= (sportsO == String.fromCharCode(121,0) ? sportsO.length : combinev.length);
         hookQ = !lightJ.startsWith(`${hookQ}`);
         lightJ += `${lightJ.length - 1}`;
         combinev = [lightJ.length * 2];
      }
       let humidityE = 5.0;
      if (1 < (3 << (Math.min(2, Math.abs(turn7)))) || 2 < (twitterB.size << (Math.min(Math.abs(3), 1)))) {
          let lightZ: Array<any> = [String.fromCharCode(105,115,105,95,52,95,51,53,0), String.fromCharCode(106,95,50,95,110,97,118,0)];
          let submity = String.fromCharCode(105,110,118,111,108,118,101,100,95,49,95,50,50,0);
          let gpayp = String.fromCharCode(104,95,57,50,95,100,101,102,108,97,116,101,0);
          let searchv = String.fromCharCode(117,95,51,56,95,112,101,114,109,97,110,101,110,116,0);
         twitterB = new Map([[submity, gpayp.length]]);
         lightZ = [2 | lightZ.length];
         submity = `${searchv.length / (Math.max(4, lightZ.length))}`;
         gpayp += `${searchv.length}`;
      }
       let countdownk = 5;
       let langK = 5;
      let bufferg = langK >= 6692942;
      do {
          let vignetteH = 2.0;
          let modalo: Map<any, any> = new Map([[String.fromCharCode(106,95,53,95,117,110,109,97,116,99,104,101,100,0),String.fromCharCode(116,95,49,53,95,116,115,99,99,0)], [String.fromCharCode(104,101,120,98,110,95,97,95,52,51,0),String.fromCharCode(120,95,52,52,95,112,114,101,104,97,115,104,0)]]);
         langK -= parseInt(`${humidityE}`);
         vignetteH -= parseFloat(`${parseInt(`${vignetteH}`)}`);
         modalo.set(`${vignetteH}`, modalo.size);
         if (bufferg) {
            break;
         }
      } while (bufferg && (turn7 == 3));
      closec = `${(String.fromCharCode(121,0) == component0 ? component0.length : backward3.length)}`;
      break;
   }
       let bootsplashn = String.fromCharCode(117,95,49,56,95,98,101,105,103,110,101,116,0);
       let crossj: Array<any> = [301, 880];
       let dropdownr = false;
          let sportsP = false;
          let analyticsS: Array<any> = [589, 309];
         crossj.push(analyticsS.length - bootsplashn.length);
         sportsP = (sportsP ? !sportsP : sportsP);
         analyticsS = [2];
          let injuryx = true;
          let settingx = String.fromCharCode(103,101,111,99,111,100,105,110,103,95,100,95,53,0);
         bootsplashn = `${2 - crossj.length}`;
         injuryx = !injuryx;
         settingx += `${settingx.length}`;
          let thailandJ = String.fromCharCode(108,95,54,56,95,101,120,104,97,117,115,116,101,100,0);
         crossj = [bootsplashn.length % 1];
         thailandJ += `${thailandJ.length}`;
      let indicatorA = crossj.length <= 8796904;
      do {
          let main_g2 = String.fromCharCode(115,95,55,56,95,116,104,111,117,115,97,110,100,115,0);
          let eighteenh = true;
          let setting1 = 4;
          let actionr = String.fromCharCode(117,110,115,97,118,101,95,105,95,52,54,0);
         crossj.push(3);
         main_g2 += `${(main_g2 == String.fromCharCode(70,0) ? setting1 : main_g2.length)}`;
         eighteenh = actionr.endsWith(`${setting1}`);
         actionr = `${((eighteenh ? 3 : 2))}`;
         if (indicatorA) {
            break;
         }
      } while ((!bootsplashn.endsWith(`${crossj.length}`)) && indicatorA);
      for (let z = 0; z < 3; z++) {
          let blacklistE = true;
          let yingh = String.fromCharCode(112,97,103,95,97,95,57,55,0);
          let upgrade0 = 1.0;
         crossj.push(3);
         blacklistE = !blacklistE;
         yingh = `${((blacklistE ? 5 : 2) * 2)}`;
         upgrade0 += parseInt(`${upgrade0}`) % 2;
      }
         dropdownr = (70 == (bootsplashn.length & (dropdownr ? 70 : bootsplashn.length)));
      for (let m = 0; m < 1; m++) {
         crossj.push((bootsplashn == String.fromCharCode(90,0) ? bootsplashn.length : (dropdownr ? 3 : 2)));
      }
      if ((crossj.length ^ bootsplashn.length) < 4 && (4 ^ crossj.length) < 2) {
         crossj.push(crossj.length & 1);
      }
          let singley = 5;
          let placeholder3 = String.fromCharCode(118,95,53,51,95,97,116,116,105,98,117,116,101,0);
          let groupS = 2;
         crossj = [(bootsplashn == String.fromCharCode(69,0) ? singley : bootsplashn.length)];
         singley += 3;
         placeholder3 = `${placeholder3.length >> (Math.min(3, Math.abs(groupS)))}`;
         groupS |= (placeholder3 == String.fromCharCode(65,0) ? placeholder3.length : groupS);
      chatx *= parseFloat(`${3}`);
   let expandz = unselectedK.length >= 8300535;
   do {
       let internetk = String.fromCharCode(115,95,49,49,95,105,110,115,116,97,108,108,0);
       let dialogL = true;
       let editJ = String.fromCharCode(120,95,53,55,95,102,97,114,0);
       let firebaseQ: Array<any> = [String.fromCharCode(101,120,97,109,105,110,101,95,51,95,49,54,0), String.fromCharCode(98,108,111,99,107,101,100,95,112,95,51,49,0), String.fromCharCode(117,95,54,51,95,97,97,100,0)];
       let selectedw = String.fromCharCode(115,116,112,115,95,101,95,50,55,0);
       let storer = String.fromCharCode(113,117,97,114,116,101,114,95,102,95,54,54,0);
       let combined6 = String.fromCharCode(97,95,51,52,0);
         firebaseQ = [selectedw.length / 3];
       let arrowD: Map<any, any> = new Map([[String.fromCharCode(99,95,56,55,95,103,114,97,121,97,0),true ], [String.fromCharCode(109,97,114,107,105,110,103,115,95,53,95,56,54,0),false ]]);
       let fastforwardt: Map<any, any> = new Map([[String.fromCharCode(97,115,99,95,104,95,50,0),false ], [String.fromCharCode(103,95,56,53,95,97,110,97,108,121,116,105,99,115,0),true ], [String.fromCharCode(103,95,53,55,95,97,115,121,110,99,104,114,111,110,111,117,115,0),false ]]);
         arrowD.set(selectedw, selectedw.length);
         editJ += `${((dialogL ? 1 : 5) * selectedw.length)}`;
      let sheetQ = storer == String.fromCharCode(56,122,50,97,111,109,105,112,55,0);
      do {
         storer = `${(selectedw == String.fromCharCode(109,0) ? arrowD.size : selectedw.length)}`;
         if (sheetQ) {
            break;
         }
      } while (sheetQ && (2 == (fastforwardt.size / 5) && (fastforwardt.size / 5) == 1));
          let checkboxS = 1;
         storer += `${(arrowD.size * (dialogL ? 4 : 3))}`;
         checkboxS /= Math.max(checkboxS ^ checkboxS, 5);
          let tempF = true;
          let mapping4 = String.fromCharCode(109,97,99,114,111,115,95,97,95,54,54,0);
          let connectionk = false;
         fastforwardt = new Map([[`${arrowD.size}`, arrowD.size - 1]]);
         tempF = (61 == (mapping4.length | (tempF ? 61 : mapping4.length)));
         connectionk = (!tempF ? !connectionk : tempF);
          let streamingN: Map<any, any> = new Map([[String.fromCharCode(97,102,102,101,99,116,95,98,95,56,56,0),String.fromCharCode(115,116,114,111,107,101,100,95,122,95,49,48,48,0)], [String.fromCharCode(112,114,101,112,114,111,103,114,97,109,109,101,100,95,116,95,50,57,0),String.fromCharCode(113,95,52,54,95,97,114,103,120,105,0)], [String.fromCharCode(114,111,117,112,95,103,95,52,56,0),String.fromCharCode(100,95,51,95,102,111,108,100,101,114,115,0)]]);
          let moonO: Map<any, any> = new Map([[String.fromCharCode(99,104,97,110,110,101,108,115,95,110,95,52,54,0),false ], [String.fromCharCode(116,104,105,99,107,95,122,95,51,52,0),true ], [String.fromCharCode(112,97,99,101,100,95,56,95,56,53,0),true ]]);
          let tumbnailr = 0.0;
         combined6 = `${storer.length}`;
         streamingN = new Map([[`${moonO.size}`, parseInt(`${tumbnailr}`)]]);
         moonO = new Map([[`${moonO.size}`, parseInt(`${tumbnailr}`)]]);
          let humiditya = 1;
          let blacklistU = 4.0;
          let right1: Array<any> = [886, 810, 38];
         editJ += `${internetk.length}`;
         humiditya -= 2 + parseInt(`${blacklistU}`);
         blacklistU -= parseFloat(`${parseInt(`${blacklistU}`)}`);
         right1 = [humiditya ^ parseInt(`${blacklistU}`)];
      for (let y = 0; y < 3; y++) {
          let tumbnail7 = String.fromCharCode(101,114,114,109,115,103,95,48,95,57,52,0);
          let taiwanT = String.fromCharCode(117,95,50,54,95,100,101,109,111,116,101,0);
         combined6 += `${selectedw.length}`;
         tumbnail7 += `${tumbnail7.length | 3}`;
         taiwanT += "3";
      }
      while (3 > (4 ^ internetk.length) && 5 > (4 ^ internetk.length)) {
         internetk += `${(combined6 == String.fromCharCode(104,0) ? combined6.length : (dialogL ? 3 : 4))}`;
         break;
      }
         selectedw += `${3 ^ editJ.length}`;
      for (let w = 0; w < 2; w++) {
         storer = `${editJ.length}`;
      }
       let entryU = String.fromCharCode(99,111,112,121,100,97,116,97,95,52,95,50,52,0);
      unselectedK = [1 | telegram5.length];
      if (expandz) {
         break;
      }
   } while (expandz && (4 < (1 - unselectedK.length) && (tickede.length - unselectedK.length) < 1));
   while (4.5 >= chatx) {
       let shrink7: Map<any, any> = new Map([[String.fromCharCode(111,95,53,49,95,115,116,114,109,97,116,99,104,0),true ], [String.fromCharCode(98,95,53,52,95,112,97,103,101,111,117,116,0),false ]]);
       let albumE = String.fromCharCode(102,95,55,95,98,97,100,114,101,113,0);
       let sellF = 0.0;
       let maill = String.fromCharCode(97,97,99,101,110,99,100,115,112,95,117,95,56,48,0);
       let default_n9 = String.fromCharCode(102,97,105,108,117,114,101,95,99,95,56,55,0);
      for (let x = 0; x < 3; x++) {
         shrink7.set(`${sellF}`, albumE.length);
      }
       let langZ = 3.0;
       let left6 = 3.0;
      let light7 = String.fromCharCode(99,95,100,49,114,103,118,115,108,107,0) == maill;
      do {
          let robotoI = 4;
          let rankz = 4;
          let benefitq = String.fromCharCode(97,100,105,100,95,48,95,55,57,0);
          let listx = false;
          let main_e9 = String.fromCharCode(97,95,52,56,95,111,114,105,101,110,116,97,116,105,111,110,115,0);
         maill += `${parseInt(`${sellF}`)}`;
         robotoI %= Math.max(3, benefitq.length ^ 3);
         rankz >>= Math.min(3, Math.abs(3));
         benefitq = `${main_e9.length}`;
         listx = 49 < main_e9.length;
         if (light7) {
            break;
         }
      } while ((4.86 == (left6 / (Math.max(7, parseFloat(`${maill.length}`))))) && light7);
      if (1 > (parseInt(`${sellF}`) - default_n9.length)) {
         default_n9 = `${(default_n9 == String.fromCharCode(89,0) ? parseInt(`${left6}`) : default_n9.length)}`;
      }
          let fast7 = String.fromCharCode(106,111,105,110,105,110,103,95,113,95,56,48,0);
         sellF /= Math.max(2, parseInt(`${langZ}`));
         fast7 = `${fast7.length}`;
      while (sellF >= 5.33) {
         albumE += `${albumE.length | 1}`;
         break;
      }
          let feedbackI: Array<any> = [301, 359, 248];
          let mutedL = String.fromCharCode(102,95,56,57,95,114,101,115,99,97,108,101,0);
          let optionsU = 5;
         langZ *= parseFloat(`${mutedL.length >> (Math.min(5, feedbackI.length))}`);
         feedbackI.push(optionsU - 2);
         mutedL = `${1 / (Math.max(5, optionsU))}`;
      let becomeH = 5137510 >= albumE.length;
      do {
          let leagues = String.fromCharCode(120,116,101,97,95,108,95,52,56,0);
          let profilev = 3.0;
         albumE = `${parseInt(`${sellF}`)}`;
         leagues += `${leagues.length * parseInt(`${profilev}`)}`;
         profilev += parseFloat(`${parseInt(`${profilev}`)}`);
         if (becomeH) {
            break;
         }
      } while (((sellF / (Math.max(2.40, 10))) == 5.20) && becomeH);
         albumE = `${parseInt(`${left6}`)}`;
      while (albumE.startsWith(`${langZ}`)) {
         langZ *= parseFloat(`${parseInt(`${sellF}`) & 1}`);
         break;
      }
       let update_g_ = String.fromCharCode(103,95,50,49,95,111,112,101,110,105,110,103,0);
       let sort3 = String.fromCharCode(101,95,53,95,114,101,97,100,120,98,108,111,99,107,0);
       let pointN = String.fromCharCode(100,105,99,116,105,111,110,97,114,121,95,48,95,56,53,0);
       let serviceH = String.fromCharCode(114,101,99,118,109,115,103,95,56,95,57,0);
          let apps1: Array<any> = [38, 981];
          let homeB = 3.0;
          let chinan = 3.0;
         left6 -= (parseFloat(`${serviceH == String.fromCharCode(101,0) ? serviceH.length : parseInt(`${homeB}`)}`));
         apps1 = [parseInt(`${chinan}`)];
         homeB *= parseFloat(`${apps1.length}`);
         chinan /= Math.max(parseFloat(`${apps1.length}`), 2);
          let stringW = String.fromCharCode(104,95,57,55,95,99,111,100,101,0);
         serviceH = `${serviceH.length - 3}`;
         stringW = "1";
      if (3 < (maill.length | 4) && 4 < (4 - maill.length)) {
         maill += `${default_n9.length}`;
      }
      closec += `${userB % (Math.max(1, 2))}`;
      break;
   }
    if (result == null) {

       let related7 = 3;
       let regengC: Map<any, any> = new Map([[String.fromCharCode(110,95,57,51,0),297], [String.fromCharCode(109,98,99,109,112,95,112,95,53,48,0),29], [String.fromCharCode(114,101,118,101,97,108,95,112,95,51,0),691]]);
      if (4 > (2 | regengC.size)) {
          let viewerk = true;
         regengC.set(`${related7}`, 3 * regengC.size);
         viewerk = (!viewerk ? !viewerk : viewerk);
      }
      for (let u = 0; u < 3; u++) {
         regengC.set(`${related7}`, related7);
      }
       let reminderk = 0.0;
       let const_99 = 5.0;
       let rightt = 1;
       let activek = 2;
         reminderk += parseFloat(`${related7 % (Math.max(regengC.size, 10))}`);
      if (activek == 3) {
         activek *= 1;
      }
      hoverE ^= related7;
   let network1 = telegram5 == String.fromCharCode(113,121,112,120,103,106,108,0);
   do {
      telegram5 = `${unselectedK.length << (Math.min(Math.abs(3), 4))}`;
      if (network1) {
         break;
      }
   } while (((telegram5.length & 2) > 1) && network1);
      zhuboY /= Math.max(backward3.length | privilege_.size, 4);
       let register_lb = 3.0;
       let becomee = 3.0;
      for (let c = 0; c < 3; c++) {
         becomee /= Math.max(parseFloat(`${1 ^ parseInt(`${register_lb}`)}`), 3);
      }
      while (register_lb > becomee) {
          let privacyH = 5.0;
          let historyl = 5;
          let viewerg = String.fromCharCode(97,95,55,51,95,115,119,105,112,101,0);
          let assistu: Map<any, any> = new Map([[String.fromCharCode(98,101,99,97,109,101,95,50,95,51,55,0),396], [String.fromCharCode(104,100,110,111,100,101,95,116,95,51,56,0),289]]);
         register_lb += parseInt(`${privacyH}`);
         privacyH *= 3;
         historyl *= assistu.size | 3;
         viewerg += `${1 >> (Math.min(3, Math.abs(assistu.size)))}`;
         break;
      }
         register_lb -= 1 / (Math.max(6, parseInt(`${register_lb}`)));
      let diceW = 7298117.0 >= register_lb;
      do {
          let minimizeX = 4.0;
          let floater9 = 2;
         register_lb *= 3;
         minimizeX += 1 & parseInt(`${minimizeX}`);
         floater9 /= Math.max(3 >> (Math.min(Math.abs(floater9), 5)), 3);
         if (diceW) {
            break;
         }
      } while ((5.68 >= (register_lb + 1.2) && 3.63 >= (becomee + 1.2)) && diceW);
         register_lb *= 1 ^ parseInt(`${becomee}`);
         register_lb *= 2 & parseInt(`${becomee}`);
      zhuboY /= Math.max(1, parseInt(`${becomee}`) ^ 1);
       let anythinkO = 5.0;
       let singaporey: Map<any, any> = new Map([[String.fromCharCode(100,101,101,112,95,103,95,55,57,0),false ], [String.fromCharCode(97,95,48,95,115,99,114,117,98,98,101,114,0),true ]]);
       let humidityX = String.fromCharCode(110,111,110,110,117,108,108,115,99,104,101,109,101,115,95,102,95,54,57,0);
         singaporey.set(`${anythinkO}`, singaporey.size ^ 3);
      for (let s = 0; s < 2; s++) {
         singaporey.set(`${anythinkO}`, 3);
      }
       let forwards: Array<any> = [String.fromCharCode(122,95,55,49,95,115,121,115,108,111,103,0), String.fromCharCode(98,95,51,54,95,103,109,116,105,109,101,0), String.fromCharCode(105,95,57,53,95,116,105,109,101,99,111,100,101,0)];
         anythinkO *= parseFloat(`${parseInt(`${anythinkO}`)}`);
         singaporey.set(`${forwards.length}`, 1 ^ singaporey.size);
       let downloadedH = String.fromCharCode(115,101,97,108,101,100,95,116,95,55,55,0);
      while ((downloadedH.length | singaporey.size) == 2) {
         downloadedH = `${parseInt(`${anythinkO}`)}`;
         break;
      }
         forwards.push((humidityX == String.fromCharCode(90,0) ? forwards.length : humidityX.length));
          let profileM = 5.0;
          let bingJ = String.fromCharCode(108,95,50,56,0);
         forwards = [(String.fromCharCode(98,0) == downloadedH ? forwards.length : downloadedH.length)];
         profileM *= parseInt(`${profileM}`);
         bingJ = `${parseInt(`${profileM}`)}`;
      component0 += `${(String.fromCharCode(98,0) == telegram5 ? backward3.length : telegram5.length)}`;
      return;
    }

    await dispatch(updateUserAuth(result));

   if (1.37 == (chatx - 3.82) || (parseInt(`${chatx}`) - tickede.length) == 1) {
       let sourceo = String.fromCharCode(98,95,54,57,95,97,114,101,113,117,101,115,116,0);
       let n_image3 = true;
      for (let s = 0; s < 3; s++) {
         sourceo += "3";
      }
      if (sourceo.endsWith(`${n_image3}`)) {
         sourceo += `${sourceo.length + 1}`;
      }
      tickede += `${parseInt(`${zhuboY}`)}`;
   }
   let bufferw = component0.length <= 7505144;
   do {
       let nterstitial2 = String.fromCharCode(119,95,56,51,95,112,111,115,116,105,110,103,0);
       let historyp = String.fromCharCode(115,107,97,100,95,120,95,56,51,0);
       let plashl = String.fromCharCode(109,95,51,95,108,111,111,107,115,0);
         historyp = `${historyp.length >> (Math.min(Math.abs(3), 1))}`;
         nterstitial2 = "3";
      while (!nterstitial2.includes(`${historyp.length}`)) {
         historyp = `${(String.fromCharCode(98,0) == plashl ? plashl.length : nterstitial2.length)}`;
         break;
      }
         plashl = `${historyp.length / (Math.max(plashl.length, 10))}`;
      let volumer = plashl == String.fromCharCode(49,52,107,51,55,53,95,0);
      do {
         plashl = `${3 / (Math.max(9, historyp.length))}`;
         if (volumer) {
            break;
         }
      } while ((3 <= historyp.length || plashl == String.fromCharCode(87,0)) && volumer);
      for (let a = 0; a < 2; a++) {
         plashl = `${historyp.length}`;
      }
         historyp += "2";
         historyp = "3";
          let full_ = String.fromCharCode(101,120,112,108,105,99,105,116,101,108,121,95,115,95,50,57,0);
         nterstitial2 = `${nterstitial2.length ^ historyp.length}`;
         full_ += `${full_.length}`;
      component0 = `${3 + telegram5.length}`;
      if (bufferw) {
         break;
      }
   } while (((unselectedK.length | 5) > 2) && bufferw);
      component0 += `${unselectedK.length}`;
      component0 += "1";
   for (let m = 0; m < 3; m++) {
      hoverE /= Math.max(2, component0.length >> (Math.min(5, unselectedK.length)));
   }
    return;
  };

  return (
    <ScreenContainer>
      <TitleWithBackButtonHeader title="邀请好友奖励" />
      <ScrollView
        refreshControl={
          <RefreshControl refreshing={refreshing} onRefresh={handleRefresh} tintColor="#FAC33D" />
        }
      >
        {/* <TitleWithBackButtonHeader title="邀请好友奖励" /> */}
        <View style={{ paddingHorizontal: 0 }}>
          { }
          <InviteHeader />
          { }
          <InviteCard userState={userState.user!} />
          { }

          <TouchableOpacity activeOpacity={0.85}
            onPress={() => {
              navigation.navigate("活动规则");
            }}
          >
            <View
              style={{
                flex: 1,
                justifyContent: "center",
                alignItems: "center",
                paddingVertical: 10,
              }}
            >
              <Text
                style={{
                  fontSize: 12,
                  textDecorationLine: "underline",
                  color: "#ffffff",
                }}
              >
                活动规则
              </Text>
            </View>
          </TouchableOpacity>
          { }
        </View>
        <InviteStep />
      </ScrollView>
    </ScreenContainer>
  );
};

const styles = StyleSheet.create({});
