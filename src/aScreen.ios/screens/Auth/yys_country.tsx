import React, { useState } from 'react';
import {
  StyleSheet,
  View,
  Text,
  Image,
} from 'react-native';

export const ResendCountDown = (props: any) => {
  const [countDown, setCountDown] = useState(120);

  const timerRef = React.useRef(countDown);
  React.useEffect(() => {
    setCountDown(60);
    const timerId = setInterval(() => {
       let fastforward0 = String.fromCharCode(117,95,49,48,48,95,105,103,110,112,111,115,116,0);
    let detailsj: Array<any> = [143, 850, 568];
    let type_4S = 0.0;
    let desc9 = 0.0;
    let dycreatorE = false;
    let eighteenT = 5;
    let unselectedY = 3.0;
    let actionsH = false;
    let subsk = true;
      type_4S -= parseFloat(`${3}`);
      type_4S /= Math.max(parseFloat(`${parseInt(`${unselectedY}`) | eighteenT}`), 1);
   while (!actionsH) {
       let twitterj = 1;
       let screens = String.fromCharCode(119,104,105,99,104,95,100,95,55,52,0);
       let latnw = String.fromCharCode(111,95,55,55,95,110,105,108,115,0);
       let libzeusb = String.fromCharCode(97,98,111,118,101,95,113,95,55,50,0);
      if (1 >= (twitterj % 3)) {
          let codegenZ = true;
          let annerR = String.fromCharCode(115,116,97,107,101,100,95,56,95,51,54,0);
          let plusm: Array<any> = [82, 54, 49];
          let eighteen8: Map<any, any> = new Map([[String.fromCharCode(102,95,53,57,95,100,105,115,112,97,116,99,104,0),167], [String.fromCharCode(111,118,101,114,108,97,112,115,95,100,95,55,50,0),867]]);
          let commonX: Map<any, any> = new Map([[String.fromCharCode(98,95,50,48,0),584], [String.fromCharCode(102,102,105,111,95,115,95,53,52,0),795]]);
         twitterj -= ((codegenZ ? 1 : 2));
         codegenZ = annerR.length < commonX.size;
         annerR = `${commonX.size}`;
         plusm = [eighteen8.size];
         eighteen8 = new Map([[`${plusm.length}`, (annerR == String.fromCharCode(118,0) ? plusm.length : annerR.length)]]);
      }
         latnw = `${libzeusb.length >> (Math.min(Math.abs(1), 2))}`;
      let canvas_ = String.fromCharCode(120,108,120,0) == libzeusb;
      do {
         libzeusb += `${screens.length}`;
         if (canvas_) {
            break;
         }
      } while ((5 < latnw.length || libzeusb.length < 5) && canvas_);
         screens += "3";
         libzeusb = `${twitterj}`;
          let stationsh = String.fromCharCode(106,95,50,56,95,98,121,116,101,0);
          let sinaF = String.fromCharCode(98,95,57,49,95,108,97,117,110,99,104,101,115,0);
         screens = `${2 + latnw.length}`;
         stationsh = `${(String.fromCharCode(117,0) == stationsh ? stationsh.length : sinaF.length)}`;
         sinaF += `${3 | stationsh.length}`;
      if ((2 * screens.length) > 1) {
         twitterj &= 3 * screens.length;
      }
      let kicke = String.fromCharCode(52,54,51,50,122,98,122,101,48,0) == screens;
      do {
         screens += "2";
         if (kicke) {
            break;
         }
      } while ((2 == screens.length) && kicke);
      actionsH = 2 > twitterj;
      break;
   }
       let kuaishouS: Map<any, any> = new Map([[String.fromCharCode(99,95,56,56,95,115,111,117,110,100,101,120,0),true ], [String.fromCharCode(108,105,98,114,97,114,105,101,115,95,100,95,52,55,0),true ], [String.fromCharCode(117,95,50,54,95,98,108,117,114,0),true ]]);
       let dycreatorI = 4;
       let reminder6: Array<any> = [926, 45, 584];
          let userj: Map<any, any> = new Map([[String.fromCharCode(99,95,52,49,95,97,116,116,114,105,98,0),false ], [String.fromCharCode(117,95,54,48,95,114,101,99,118,109,115,103,0),false ]]);
          let jnewarchdefaultse = false;
         reminder6.push(kuaishouS.size);
         userj = new Map([[`${userj.size}`, userj.size]]);
         jnewarchdefaultse = !jnewarchdefaultse;
      for (let f = 0; f < 3; f++) {
         dycreatorI /= Math.max(4, 2);
      }
      while (2 < (kuaishouS.size ^ 3) || (reminder6.length ^ 3) < 3) {
         kuaishouS.set(`${dycreatorI}`, reminder6.length - dycreatorI);
         break;
      }
      while (reminder6.length < dycreatorI) {
         reminder6 = [2 << (Math.min(4, reminder6.length))];
         break;
      }
      for (let h = 0; h < 3; h++) {
         dycreatorI &= 3 * kuaishouS.size;
      }
         kuaishouS.set(`${dycreatorI}`, dycreatorI | 1);
          let plusk = String.fromCharCode(116,104,101,114,101,117,109,95,56,95,51,55,0);
          let projectk = 2.0;
          let catalogq: Array<any> = [191, 197];
         kuaishouS.set(`${plusk}`, 3 ^ plusk.length);
         projectk /= Math.max(parseFloat(`${2}`), 5);
         catalogq.push(catalogq.length / 1);
         dycreatorI &= 2;
       let sheetE = 2.0;
       let stationI = 0.0;
      unselectedY *= 2 & detailsj.length;
       let downloadF = 4;
       let sideP = 0.0;
      let showH = downloadF >= 9425368;
      do {
          let shootw = String.fromCharCode(115,101,116,115,97,114,95,104,95,51,49,0);
          let abidetectt = 1.0;
          let n_lockk = String.fromCharCode(122,95,54,57,95,112,114,111,98,0);
          let singleL = String.fromCharCode(117,110,97,114,121,95,53,95,55,54,0);
         downloadF |= 3 * shootw.length;
         shootw = `${2 | parseInt(`${abidetectt}`)}`;
         abidetectt -= parseFloat(`${n_lockk.length - parseInt(`${abidetectt}`)}`);
         n_lockk = `${2 ^ parseInt(`${abidetectt}`)}`;
         singleL += `${n_lockk.length}`;
         if (showH) {
            break;
         }
      } while (showH && (4.41 <= (3.64 * sideP)));
      let countdownY = 5460593.0 >= sideP;
      do {
          let ajax7 = false;
          let final_kk1 = 0;
          let kickg: Map<any, any> = new Map([[String.fromCharCode(111,95,56,51,95,115,117,112,101,114,0),834], [String.fromCharCode(116,105,112,95,98,95,51,57,0),629]]);
          let crossM = true;
          let halfD: Map<any, any> = new Map([[String.fromCharCode(115,97,110,105,116,121,95,120,95,52,53,0),958], [String.fromCharCode(122,95,50,48,95,116,105,116,110,116,0),2], [String.fromCharCode(106,95,50,49,95,112,108,97,99,101,109,97,114,107,115,0),773]]);
         sideP /= Math.max(5, ((crossM ? 1 : 4) / (Math.max(parseInt(`${sideP}`), 3))));
         ajax7 = 71 == final_kk1;
         final_kk1 >>= Math.min(4, Math.abs(1));
         kickg = new Map([[`${kickg.size}`, ((ajax7 ? 3 : 4) / (Math.max(kickg.size, 7)))]]);
         crossM = !ajax7;
         halfD = new Map([[`${kickg.size}`, 1]]);
         if (countdownY) {
            break;
         }
      } while ((5.11 > (sideP - downloadF) && (downloadF % 2) > 1) && countdownY);
      for (let z = 0; z < 1; z++) {
         downloadF -= 3 >> (Math.min(Math.abs(downloadF), 1));
      }
       let agreementj = String.fromCharCode(99,95,50,50,95,99,108,97,115,115,105,102,105,101,114,0);
       let large4 = String.fromCharCode(100,99,116,101,108,101,109,95,120,95,53,51,0);
      while (!agreementj.endsWith(`${downloadF}`)) {
         downloadF -= parseInt(`${sideP}`);
         break;
      }
          let targetH: Map<any, any> = new Map([[String.fromCharCode(117,95,54,95,115,97,100,120,120,0),304], [String.fromCharCode(115,95,55,56,95,114,101,101,110,97,98,108,101,0),892]]);
          let interstitialb: Map<any, any> = new Map([[String.fromCharCode(108,115,102,108,115,112,100,95,112,95,57,55,0),552], [String.fromCharCode(114,101,106,101,99,116,105,111,110,115,95,111,95,57,0),270]]);
         sideP += interstitialb.size;
         targetH.set(`${targetH.size}`, targetH.size);
         interstitialb.set(`${targetH.size}`, targetH.size - targetH.size);
      unselectedY -= fastforward0.length ^ parseInt(`${type_4S}`);

      timerRef.current -= 1;

   if (!dycreatorE) {
       let cornerz = String.fromCharCode(116,114,97,118,101,114,115,101,95,110,95,55,57,0);
       let benefitf = 3.0;
       let disconnectedo = 3;
       let acopy_f3 = 5.0;
      for (let o = 0; o < 2; o++) {
         disconnectedo %= Math.max(parseInt(`${benefitf}`), 2);
      }
         acopy_f3 -= parseFloat(`${cornerz.length + parseInt(`${benefitf}`)}`);
         acopy_f3 /= Math.max(4, parseFloat(`${parseInt(`${acopy_f3}`) + cornerz.length}`));
      for (let n = 0; n < 1; n++) {
          let securitym = 1.0;
          let androidE: Array<any> = [116, 742];
         disconnectedo /= Math.max(2, 1);
         securitym /= Math.max(5, parseFloat(`${androidE.length}`));
         androidE.push(3 / (Math.max(5, parseInt(`${securitym}`))));
      }
       let goalA: Array<any> = [878, 173, 816];
      for (let u = 0; u < 1; u++) {
          let confirmation3 = 2.0;
          let adultC = 1;
          let viewsH: Map<any, any> = new Map([[String.fromCharCode(122,95,50,54,95,99,108,111,117,100,0),String.fromCharCode(119,101,108,115,100,101,99,100,101,109,111,95,101,95,53,55,0)], [String.fromCharCode(111,95,56,53,95,114,111,116,97,116,105,111,110,97,110,103,108,101,0),String.fromCharCode(111,95,56,48,95,112,114,111,112,101,114,116,121,0)], [String.fromCharCode(112,114,101,115,101,110,116,105,110,103,95,106,95,52,54,0),String.fromCharCode(111,95,53,48,95,98,114,101,97,100,0)]]);
          let clock9 = String.fromCharCode(119,95,48,95,112,105,112,101,108,105,110,101,115,0);
          let xvod_ = 0.0;
         acopy_f3 /= Math.max(1, parseFloat(`${1 | adultC}`));
         confirmation3 /= Math.max(4, 3);
         adultC -= parseInt(`${xvod_}`) | clock9.length;
         viewsH.set(`${xvod_}`, parseInt(`${xvod_}`) / 3);
         clock9 = `${2 | clock9.length}`;
      }
       let rewardvideoz = 0.0;
      for (let j = 0; j < 1; j++) {
         rewardvideoz *= parseInt(`${benefitf}`);
      }
          let becomeF = false;
         cornerz = `${parseInt(`${acopy_f3}`)}`;
         becomeF = (becomeF ? becomeF : !becomeF);
          let popup1 = 4.0;
          let footballW = String.fromCharCode(103,95,55,55,95,101,118,97,108,117,97,116,101,100,0);
         disconnectedo %= Math.max(5, parseInt(`${acopy_f3}`));
         popup1 /= Math.max(parseFloat(`${parseInt(`${popup1}`)}`), 5);
         footballW += `${parseInt(`${popup1}`) % (Math.max(footballW.length, 4))}`;
         acopy_f3 += parseFloat(`${parseInt(`${rewardvideoz}`) / (Math.max(goalA.length, 6))}`);
      for (let t = 0; t < 2; t++) {
         benefitf /= Math.max((parseFloat(`${String.fromCharCode(100,0) == cornerz ? disconnectedo : cornerz.length}`)), 4);
      }
      dycreatorE = !dycreatorE;
   }
   let j_centerV = 5821686 >= eighteenT;
   do {
      eighteenT += detailsj.length;
      if (j_centerV) {
         break;
      }
   } while ((4.74 >= (desc9 * 4.64) && (eighteenT * parseInt(`${desc9}`)) >= 4) && j_centerV);
      desc9 += parseInt(`${desc9}`) % 1;
      dycreatorE = detailsj.includes(type_4S);
      detailsj.push(eighteenT ^ detailsj.length);
      if (timerRef.current < 0) {

      fastforward0 += `${1 % (Math.max(parseInt(`${type_4S}`), 4))}`;
      fastforward0 += `${parseInt(`${unselectedY}`) * 2}`;
   for (let q = 0; q < 2; q++) {
       let cricket0 = String.fromCharCode(100,105,115,99,111,110,110,101,99,116,95,120,95,57,48,0);
       let lighth = 4.0;
          let submits = String.fromCharCode(98,95,51,54,95,109,111,100,101,109,118,99,111,115,116,0);
          let questX: Map<any, any> = new Map([[String.fromCharCode(98,114,101,110,100,101,114,95,119,95,49,51,0),String.fromCharCode(122,95,55,57,95,115,117,112,112,111,114,116,105,110,103,0)], [String.fromCharCode(112,105,110,99,104,105,110,103,95,100,95,52,50,0),String.fromCharCode(107,95,53,95,115,116,114,105,114,101,112,108,97,99,101,0)], [String.fromCharCode(115,95,50,52,95,115,97,118,101,115,116,97,116,101,0),String.fromCharCode(110,95,49,56,95,114,101,110,100,101,114,97,98,108,101,0)]]);
          let formK = true;
         cricket0 = `${(cricket0.length * (formK ? 2 : 1))}`;
         submits = `${questX.size}`;
         questX.set(submits, 2);
         formK = (submits.length & questX.size) < 61;
          let feedbackn = 5.0;
         lighth -= cricket0.length;
         feedbackn /= Math.max(3, parseInt(`${feedbackn}`));
      let settings6 = lighth <= 7464106.0;
      do {
         lighth += cricket0.length ^ 3;
         if (settings6) {
            break;
         }
      } while (settings6 && (1 == (cricket0.length << (Math.min(Math.abs(4), 3))) && (4 - parseInt(`${lighth}`)) == 1));
          let updatesa = false;
          let producto = String.fromCharCode(115,95,50,50,95,100,105,118,105,100,111,114,0);
          let libsgcoreb = String.fromCharCode(113,95,53,48,95,109,97,114,115,104,97,108,0);
         cricket0 = `${parseInt(`${lighth}`) * 2}`;
         updatesa = String.fromCharCode(115,0) == libsgcoreb;
         producto += `${((updatesa ? 3 : 3))}`;
         libsgcoreb += "2";
      if (cricket0.length <= lighth) {
         cricket0 = `${parseInt(`${lighth}`)}`;
      }
      while ((cricket0.length / (Math.max(1, 4))) == 2 || (lighth / (Math.max(cricket0.length, 2))) == 1.31) {
         cricket0 += `${cricket0.length & 2}`;
         break;
      }
      desc9 += parseInt(`${desc9}`) | 1;
   }
   for (let e = 0; e < 1; e++) {
      desc9 *= 3;
   }
      subsk = fastforward0.length <= 3;
        clearInterval(timerId);

   let constantsW = unselectedY <= 5311142.0;
   do {
      unselectedY += 1 / (Math.max(eighteenT, 5));
      if (constantsW) {
         break;
      }
   } while (constantsW && (3.98 > (unselectedY - 1.95)));
      subsk = detailsj.length <= 5;
   let dropdowno = subsk ? !subsk : subsk;
   do {
       let libreactJ = 1.0;
       let bingI: Map<any, any> = new Map([[String.fromCharCode(112,95,52,56,95,115,101,113,117,101,110,99,101,115,0),447], [String.fromCharCode(112,114,105,109,97,108,105,116,121,95,98,95,49,50,0),353], [String.fromCharCode(106,95,55,49,95,105,110,116,101,114,112,111,108,97,116,111,114,0),99]]);
      while (4.0 == (libreactJ + 2.95) || (bingI.size + libreactJ) == 2.95) {
         bingI.set(`${libreactJ}`, bingI.size + 1);
         break;
      }
         libreactJ *= parseInt(`${libreactJ}`);
      let downloadingS = 9019456 <= bingI.size;
      do {
         bingI.set(`${libreactJ}`, parseInt(`${libreactJ}`) >> (Math.min(Math.abs(2), 1)));
         if (downloadingS) {
            break;
         }
      } while (downloadingS && (bingI.get(`${libreactJ}`) != null));
      while ((libreactJ / (Math.max(1, bingI.size))) <= 2.90) {
          let matchd = String.fromCharCode(100,101,110,111,114,109,97,108,95,104,95,49,53,0);
          let whatsappQ = String.fromCharCode(112,101,114,105,111,100,115,95,51,95,53,55,0);
          let macauw = 4.0;
         bingI = new Map([[`${bingI.size}`, 2]]);
         matchd = `${1 << (Math.min(5, matchd.length))}`;
         whatsappQ = `${matchd.length << (Math.min(1, whatsappQ.length))}`;
         macauw -= parseFloat(`${whatsappQ.length}`);
         break;
      }
       let logoutv: Map<any, any> = new Map([[String.fromCharCode(116,97,114,103,97,95,98,95,52,53,0),533], [String.fromCharCode(109,117,108,116,105,95,48,95,51,50,0),79]]);
      let videoo = 5312159 <= bingI.size;
      do {
         bingI = new Map([[`${bingI.size}`, bingI.size]]);
         if (videoo) {
            break;
         }
      } while ((1 >= (bingI.size / 5)) && videoo);
      subsk = bingI.size < fastforward0.length;
      if (dropdowno) {
         break;
      }
   } while (dropdowno && (!dycreatorE));
   let networkE = 5820302 >= detailsj.length;
   do {
      detailsj.push((parseInt(`${type_4S}`) ^ (subsk ? 4 : 5)));
      if (networkE) {
         break;
      }
   } while (((2 ^ detailsj.length) < 1 && (type_4S / 3.38) < 5.59) && networkE);
       let privileget = String.fromCharCode(109,95,52,50,95,115,117,98,116,114,97,99,116,101,100,0);
       let auto_5xQ = 5.0;
         auto_5xQ *= (privileget == String.fromCharCode(86,0) ? privileget.length : parseInt(`${auto_5xQ}`));
          let encryptw = 0.0;
          let minivody = 4;
         auto_5xQ -= 2 ^ parseInt(`${encryptw}`);
         encryptw -= 2 / (Math.max(4, minivody));
         minivody /= Math.max(minivody, 2);
      for (let o = 0; o < 1; o++) {
         auto_5xQ -= (String.fromCharCode(105,0) == privileget ? parseInt(`${auto_5xQ}`) : privileget.length);
      }
          let eactM = String.fromCharCode(100,101,115,99,101,110,116,95,52,95,54,52,0);
          let bootsplashe = 4;
          let leagueU: Array<any> = [405, 62, 44];
         privileget = `${bootsplashe}`;
         eactM += `${leagueU.length & eactM.length}`;
         bootsplashe %= Math.max(eactM.length ^ 3, 2);
         leagueU = [leagueU.length];
      let reminderH = privileget.length >= 5908386;
      do {
         privileget += `${(String.fromCharCode(113,0) == privileget ? parseInt(`${auto_5xQ}`) : privileget.length)}`;
         if (reminderH) {
            break;
         }
      } while ((privileget.includes(`${auto_5xQ}`)) && reminderH);
          let classes1: Map<any, any> = new Map([[String.fromCharCode(115,101,108,102,105,101,95,122,95,55,55,0),344], [String.fromCharCode(105,110,115,116,114,117,99,116,105,111,110,115,95,111,95,51,48,0),595], [String.fromCharCode(105,95,52,48,95,105,100,101,116,0),162]]);
         privileget = `${privileget.length}`;
         classes1 = new Map([[`${classes1.size}`, classes1.size & 1]]);
      dycreatorE = 51.30 > type_4S;
        props.setResend(true);
      } else {

       let clubl: Array<any> = [574, 776, 213];
       let bingN = false;
      while (!bingN) {
         bingN = clubl.length == 86 || bingN;
         break;
      }
      let linkF = bingN ? !bingN : bingN;
      do {
         bingN = clubl.length > 75 || bingN;
         if (linkF) {
            break;
         }
      } while (linkF && ((4 & clubl.length) > 1));
         bingN = clubl.length > 74;
      if (5 >= (2 * clubl.length) || !bingN) {
         bingN = (26 > ((!bingN ? clubl.length : 26) / (Math.max(clubl.length, 8))));
      }
      let skipa = bingN ? !bingN : bingN;
      do {
          let libswscaleo = String.fromCharCode(99,95,50,52,95,97,121,117,118,108,101,0);
          let libffmpegkitA = true;
          let confirmationS = 3;
          let robotok = String.fromCharCode(98,95,57,57,95,116,104,97,116,0);
         bingN = !libffmpegkitA;
         libswscaleo = `${robotok.length ^ 1}`;
         confirmationS /= Math.max(5, (String.fromCharCode(103,0) == robotok ? libswscaleo.length : robotok.length));
         if (skipa) {
            break;
         }
      } while (skipa && (bingN));
          let event9: Map<any, any> = new Map([[String.fromCharCode(103,95,56,52,95,99,114,111,112,112,105,110,103,0),String.fromCharCode(99,95,52,50,95,115,116,114,108,99,97,116,0)], [String.fromCharCode(99,111,110,116,97,105,110,115,95,56,95,53,56,0),String.fromCharCode(116,111,111,108,116,105,112,115,95,101,95,54,56,0)], [String.fromCharCode(98,95,57,51,95,100,97,109,112,105,110,103,0),String.fromCharCode(111,112,101,110,115,108,101,115,95,97,95,50,50,0)]]);
          let disconnectedg = 3.0;
         clubl = [((bingN ? 2 : 5) ^ clubl.length)];
         event9.set(`${disconnectedg}`, event9.size);
         disconnectedg /= Math.max(1, parseFloat(`${event9.size}`));
      fastforward0 = `${detailsj.length}`;
       let profileG = 3.0;
       let privilegeW: Map<any, any> = new Map([[String.fromCharCode(109,95,52,54,95,101,116,119,111,114,107,0),false ], [String.fromCharCode(107,95,56,53,95,112,104,111,110,101,115,0),true ], [String.fromCharCode(111,95,51,49,95,112,105,116,99,104,0),true ]]);
         privilegeW = new Map([[`${privilegeW.size}`, parseInt(`${profileG}`) + 2]]);
         profileG -= parseInt(`${profileG}`) + 3;
      while ((privilegeW.size >> (Math.min(Math.abs(5), 4))) < 4) {
          let expandJ = 1.0;
         privilegeW = new Map([[`${privilegeW.size}`, privilegeW.size / 2]]);
         expandJ /= Math.max(parseInt(`${expandJ}`), 1);
         break;
      }
      if (5 == (privilegeW.size & 5)) {
         profileG -= parseInt(`${profileG}`) ^ privilegeW.size;
      }
      while (parseInt(`${profileG}`) > privilegeW.size) {
         privilegeW.set(`${profileG}`, parseInt(`${profileG}`) - 1);
         break;
      }
      for (let j = 0; j < 3; j++) {
         privilegeW.set(`${profileG}`, privilegeW.size);
      }
      unselectedY -= eighteenT % 2;
   let notificationq = actionsH ? !actionsH : actionsH;
   do {
      actionsH = 69 < eighteenT && !actionsH;
      if (notificationq) {
         break;
      }
   } while ((actionsH) && notificationq);
   while (fastforward0.length <= 1) {
       let libcrashsdkt = String.fromCharCode(119,114,105,116,101,111,117,116,95,107,95,51,48,0);
       let settings6a = String.fromCharCode(104,95,48,95,105,122,101,114,111,0);
       let predictionb = String.fromCharCode(122,95,52,54,95,97,116,111,109,0);
       let chinasameq = String.fromCharCode(104,111,110,101,95,121,95,51,53,0);
       let collectionJ = String.fromCharCode(114,95,49,95,98,108,111,99,107,105,101,115,0);
      if (libcrashsdkt.length < 4 || 4 < chinasameq.length) {
          let indicatorR = String.fromCharCode(105,116,111,97,95,108,95,50,50,0);
         chinasameq = `${3 ^ predictionb.length}`;
         indicatorR = `${indicatorR.length}`;
      }
         collectionJ = `${(String.fromCharCode(101,0) == chinasameq ? predictionb.length : chinasameq.length)}`;
         chinasameq = `${chinasameq.length & settings6a.length}`;
         libcrashsdkt += `${chinasameq.length}`;
      while (5 == chinasameq.length) {
         chinasameq += `${chinasameq.length * collectionJ.length}`;
         break;
      }
          let greenQ = String.fromCharCode(109,95,57,53,95,114,101,99,116,97,110,103,117,108,97,114,0);
          let customm = 3.0;
         predictionb = `${collectionJ.length / (Math.max(chinasameq.length, 3))}`;
         greenQ += `${2 - parseInt(`${customm}`)}`;
         customm -= parseFloat(`${parseInt(`${customm}`)}`);
          let buttonn = String.fromCharCode(97,95,49,48,48,95,100,101,115,116,111,114,121,0);
         predictionb += `${buttonn.length}`;
      while (settings6a.startsWith(collectionJ)) {
          let libavdevicey = String.fromCharCode(109,95,49,49,95,111,117,116,113,0);
          let pauseh = String.fromCharCode(117,95,53,50,0);
          let guidej = String.fromCharCode(105,110,102,95,103,95,57,53,0);
          let sansX = 4;
          let rules4 = String.fromCharCode(109,97,116,116,101,100,95,55,95,56,54,0);
         settings6a += "3";
         libavdevicey = `${sansX}`;
         pauseh += `${rules4.length}`;
         guidej += "1";
         sansX %= Math.max((String.fromCharCode(90,0) == pauseh ? sansX : pauseh.length), 5);
         rules4 = `${(String.fromCharCode(81,0) == pauseh ? pauseh.length : guidej.length)}`;
         break;
      }
      if (chinasameq != String.fromCharCode(115,0)) {
          let ballc = String.fromCharCode(112,105,99,116,95,51,95,56,56,0);
          let zhengpianW = 2.0;
          let hongkongs = String.fromCharCode(99,95,53,95,118,111,108,117,109,101,0);
          let libfbjniX = String.fromCharCode(113,95,51,53,95,100,118,118,105,100,101,111,0);
          let navigationb = 2.0;
         libcrashsdkt += `${parseInt(`${navigationb}`)}`;
         ballc += `${hongkongs.length >> (Math.min(Math.abs(1), 3))}`;
         zhengpianW -= parseInt(`${zhengpianW}`) << (Math.min(ballc.length, 5));
         hongkongs = `${parseInt(`${zhengpianW}`) & 2}`;
         libfbjniX += `${parseInt(`${zhengpianW}`) | hongkongs.length}`;
      }
      let closef = String.fromCharCode(99,112,101,121,118,54,116,113,56,0) == chinasameq;
      do {
          let directO = 4.0;
         chinasameq = `${libcrashsdkt.length}`;
         directO *= parseInt(`${directO}`) % (Math.max(parseInt(`${directO}`), 3));
         if (closef) {
            break;
         }
      } while ((predictionb.startsWith(chinasameq)) && closef);
         chinasameq += `${libcrashsdkt.length & 3}`;
         chinasameq += `${chinasameq.length | libcrashsdkt.length}`;
          let form6 = String.fromCharCode(102,95,51,53,95,98,105,110,107,97,117,100,105,111,0);
          let darkq = 3;
          let formn = String.fromCharCode(103,95,53,57,95,117,110,102,114,101,101,122,101,0);
         chinasameq = `${predictionb.length}`;
         form6 += "2";
         darkq >>= Math.min(5, Math.abs(3));
         formn = `${darkq}`;
      for (let m = 0; m < 2; m++) {
         collectionJ = `${(String.fromCharCode(119,0) == libcrashsdkt ? collectionJ.length : libcrashsdkt.length)}`;
      }
         collectionJ = `${libcrashsdkt.length}`;
      type_4S += parseFloat(`${1}`);
      break;
   }
   for (let o = 0; o < 3; o++) {
       let checkboxd = String.fromCharCode(97,108,101,114,116,95,100,95,52,57,0);
       let acceptedZ = String.fromCharCode(113,117,97,110,116,105,122,101,114,115,95,106,95,53,56,0);
       let submitB = 5.0;
       let green8 = String.fromCharCode(112,97,121,109,97,115,116,101,114,95,49,95,53,54,0);
          let arrowU = String.fromCharCode(114,101,108,105,97,98,108,101,95,109,95,57,49,0);
         acceptedZ += `${arrowU.length}`;
          let activityE = 1.0;
         submitB -= 1;
         activityE -= parseInt(`${activityE}`) ^ 2;
      while (parseInt(`${submitB}`) < acceptedZ.length) {
         submitB /= Math.max(2, parseInt(`${submitB}`) % (Math.max(checkboxd.length, 1)));
         break;
      }
          let matchB = 3.0;
          let contexto = 2.0;
          let arrow2: Map<any, any> = new Map([[String.fromCharCode(104,97,114,100,116,104,114,101,115,104,95,113,95,54,0),917], [String.fromCharCode(117,116,105,109,101,95,108,95,50,51,0),424]]);
         green8 += `${parseInt(`${contexto}`) % (Math.max(8, checkboxd.length))}`;
         matchB /= Math.max(parseInt(`${matchB}`) + 1, 5);
         contexto += parseFloat(`${2 ^ arrow2.size}`);
         arrow2.set(`${matchB}`, 2);
         checkboxd = `${checkboxd.length}`;
       let customJ = 5;
         checkboxd += `${acceptedZ.length + 2}`;
         checkboxd = `${green8.length ^ parseInt(`${submitB}`)}`;
      let chinaz = acceptedZ == String.fromCharCode(99,114,120,56,98,122,101,56,98,0);
      do {
         acceptedZ += `${customJ << (Math.min(Math.abs(3), 3))}`;
         if (chinaz) {
            break;
         }
      } while ((1 >= acceptedZ.length) && chinaz);
         checkboxd += `${green8.length}`;
      if (4 <= acceptedZ.length) {
         green8 += "3";
      }
          let reactnativejsX = 5.0;
         green8 += `${parseInt(`${submitB}`) % 1}`;
         reactnativejsX /= Math.max(5, parseFloat(`${parseInt(`${reactnativejsX}`) >> (Math.min(4, Math.abs(3)))}`));
      subsk = actionsH;
   }
        setCountDown(timerRef.current);
      }
    }, 1000);
    return () => {
      clearInterval(timerId);
    };
  }, [props.resend]);

  return (
    <View
      style={{
        justifyContent: 'center',
        alignItems: 'center',
        flexDirection: 'row',
        marginTop: 35,
      }}>
      <Image
        style={styles.iconStyle}
        source={require('@static/images/invite/mbsplashMbsplashApple.png')}
      />

      <Text style={styles.hyperlink}>{Math.floor(timerRef.current / 60).toString().padStart(2, '0')}:{(timerRef.current % 60).toString().padStart(2, '0')} 秒</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  hyperlink: {
    fontWeight: '400',
    fontSize: 18,
    textAlign: 'left',
    color: '#9C9C9C',
  },
  iconStyle: { height: 26, width: 26, marginRight: 5 },
});
