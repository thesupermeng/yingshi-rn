import React, { useState } from 'react';
import {
  StyleSheet,
  View,
  Text,
  Image,
} from 'react-native';

export const ResendCountDown = (props: any) => {
  const [countDown, setCountDown] = useState(120);

  const timerRef = React.useRef(countDown);
  React.useEffect(() => {
    setCountDown(60);
    const timerId = setInterval(() => {
       let iconsubssuccessQ = 5.0;
    let iconarrowrightoranget = String.fromCharCode(119,95,57,54,95,112,112,107,104,0);
    let fullscreenmin0 = 4.0;
    let vietnamY: Array<any> = [String.fromCharCode(117,110,105,100,101,110,116,105,102,105,101,100,95,50,95,54,54,0), String.fromCharCode(116,114,97,102,102,105,99,95,104,95,55,57,0), String.fromCharCode(114,101,118,97,108,105,100,97,116,105,110,103,95,122,95,51,52,0)];
    let moreE = String.fromCharCode(117,95,56,53,95,112,111,108,108,101,100,0);
    let uimanagers = String.fromCharCode(111,95,51,50,95,111,110,116,101,120,116,0);
    let refreshborderlessb = String.fromCharCode(106,95,49,53,95,102,108,97,115,104,0);
    let iconviewergifE = String.fromCharCode(109,101,109,100,98,95,56,95,53,52,0);
    let rocketE = false;
    let user7 = 2.0;
    let ewardedm: Array<any> = [97, 104];
      moreE += `${uimanagers.length}`;
      uimanagers = `${(String.fromCharCode(68,0) == uimanagers ? iconarrowrightoranget.length : uimanagers.length)}`;
   for (let a = 0; a < 3; a++) {
      refreshborderlessb += `${(String.fromCharCode(121,0) == iconviewergifE ? iconviewergifE.length : uimanagers.length)}`;
   }
   while (iconsubssuccessQ == vietnamY.length) {
       let faviconV = true;
       let storeo = 5.0;
       let transferP: Array<any> = [String.fromCharCode(101,95,56,95,103,97,117,115,115,105,97,110,0), String.fromCharCode(112,95,49,55,95,117,114,98,103,0), String.fromCharCode(111,95,57,48,95,116,114,117,110,99,112,97,115,115,101,115,0)];
       let indexQ = true;
         faviconV = 32 < transferP.length;
      while (storeo <= 5.97 || (5.97 * storeo) <= 1.57) {
         faviconV = transferP.length >= 13;
         break;
      }
          let verticalC = String.fromCharCode(112,97,115,116,101,100,95,104,95,51,53,0);
          let canvasg = true;
          let matchdetailbgO = String.fromCharCode(113,95,52,48,95,109,111,118,101,109,101,110,116,0);
         indexQ = !indexQ;
         verticalC = `${(verticalC == String.fromCharCode(99,0) ? verticalC.length : (canvasg ? 3 : 2))}`;
         canvasg = !canvasg;
         matchdetailbgO = `${matchdetailbgO.length}`;
       let switch_4cF = String.fromCharCode(101,95,50,95,115,112,97,99,105,110,103,115,0);
       let moduleV = String.fromCharCode(108,105,102,101,95,108,95,54,50,0);
      while ((3 + transferP.length) > 4 && faviconV) {
         transferP = [((indexQ ? 1 : 2))];
         break;
      }
          let templateprocessor8 = String.fromCharCode(108,95,53,51,95,114,101,99,111,110,110,101,99,116,105,111,110,0);
          let regeng5 = String.fromCharCode(117,116,105,108,105,116,121,95,122,95,54,50,0);
         moduleV = "1";
         templateprocessor8 = `${regeng5.length * 3}`;
         regeng5 = `${2 | templateprocessor8.length}`;
       let downloadedP = 5;
       let v_centerm = 2;
      let hover6 = faviconV ? !faviconV : faviconV;
      do {
          let selectA = 0;
          let znewinterstitialx = 0;
          let libjsinspectorz = false;
         faviconV = !indexQ || storeo >= 82.43;
         selectA <<= Math.min(4, Math.abs(2));
         znewinterstitialx <<= Math.min(2, Math.abs(3));
         libjsinspectorz = !libjsinspectorz;
         if (hover6) {
            break;
         }
      } while (((transferP.length + 2) < 3 && faviconV) && hover6);
      for (let p = 0; p < 3; p++) {
         v_centerm %= Math.max(v_centerm ^ 2, 3);
      }
       let backwardx = 2.0;
       let playlistt = 2.0;
      while (!indexQ && 2 <= (transferP.length / (Math.max(1, 1)))) {
         transferP = [moduleV.length / 1];
         break;
      }
      let o_managerR = faviconV ? !faviconV : faviconV;
      do {
         faviconV = v_centerm > 70;
         if (o_managerR) {
            break;
         }
      } while (o_managerR && (!faviconV));
      iconsubssuccessQ -= uimanagers.length | 1;
      break;
   }
   while ((vietnamY.length - 1) >= 5 && 1 >= (1 & vietnamY.length)) {
      iconsubssuccessQ /= Math.max(5, 3 / (Math.max(2, iconviewergifE.length)));
      break;
   }

      timerRef.current -= 1;

   for (let l = 0; l < 1; l++) {
       let encryptY = String.fromCharCode(118,95,54,49,95,100,121,110,97,109,105,99,115,0);
      while (encryptY != String.fromCharCode(100,0)) {
          let save4 = true;
          let playF = String.fromCharCode(103,95,55,52,95,116,106,117,116,105,108,0);
          let rewardvideoI = String.fromCharCode(108,97,110,99,122,111,115,95,105,95,53,53,0);
          let playercommon_ = String.fromCharCode(117,95,54,50,95,115,104,97,114,105,110,103,0);
          let static_llK: Array<any> = [638, 839];
         encryptY += `${1 & encryptY.length}`;
         save4 = (playercommon_.length % (Math.max(1, static_llK.length))) >= 16;
         playF += `${(playercommon_ == String.fromCharCode(95,0) ? static_llK.length : playercommon_.length)}`;
         rewardvideoI = `${2 & rewardvideoI.length}`;
         break;
      }
         encryptY = `${encryptY.length | encryptY.length}`;
       let commentU = 1;
       let orangeuparrow7 = 0;
      iconarrowrightoranget = `${refreshborderlessb.length + 2}`;
   }
   while (moreE != refreshborderlessb) {
      refreshborderlessb += "1";
      break;
   }
   while (moreE.length > uimanagers.length) {
      uimanagers += `${iconarrowrightoranget.length - refreshborderlessb.length}`;
      break;
   }
   if (iconarrowrightoranget.length <= uimanagers.length) {
      uimanagers = `${(String.fromCharCode(65,0) == iconarrowrightoranget ? iconarrowrightoranget.length : vietnamY.length)}`;
   }
      uimanagers = `${1 << (Math.min(2, uimanagers.length))}`;
      if (timerRef.current < 0) {

      refreshborderlessb = `${refreshborderlessb.length}`;
      rocketE = !uimanagers.startsWith(`${rocketE}`);
   while (iconviewergifE == moreE) {
       let scrollviewh = String.fromCharCode(99,111,109,112,111,115,101,105,95,113,95,53,57,0);
       let twitter2 = 5.0;
       let yellowscoreball8 = String.fromCharCode(115,104,97,112,101,115,95,109,95,57,48,0);
       let tickX = false;
       let eventI = String.fromCharCode(116,95,55,49,95,102,111,99,117,115,101,100,0);
      let backwardd = String.fromCharCode(57,115,122,105,105,95,110,95,0) == eventI;
      do {
         eventI += `${eventI.length}`;
         if (backwardd) {
            break;
         }
      } while (backwardd && (!tickX));
         scrollviewh = "1";
         yellowscoreball8 = "2";
          let otherS = 2.0;
         tickX = 33 < eventI.length;
         otherS /= Math.max(parseFloat(`${parseInt(`${otherS}`)}`), 3);
         scrollviewh = `${(2 - (tickX ? 4 : 4))}`;
         twitter2 *= (parseFloat(`${(tickX ? 5 : 4) ^ parseInt(`${twitter2}`)}`));
      while (1.46 == (twitter2 * parseFloat(`${scrollviewh.length}`)) || (4 + scrollviewh.length) == 5) {
          let mapbufferM: Array<any> = [93, 283];
          let collectionN = String.fromCharCode(97,112,112,95,110,95,53,50,0);
          let libyogao: Map<any, any> = new Map([[String.fromCharCode(114,101,115,97,109,112,95,111,95,56,0),834], [String.fromCharCode(103,95,49,53,95,99,111,101,102,102,115,112,0),200]]);
         scrollviewh = `${parseInt(`${twitter2}`) - 1}`;
         mapbufferM = [collectionN.length];
         collectionN = "1";
         libyogao = new Map([[`${mapbufferM.length}`, mapbufferM.length]]);
         break;
      }
       let favicon6: Array<any> = [138, 299];
       let hejiF: Array<any> = [985, 769];
         twitter2 -= parseFloat(`${hejiF.length % 2}`);
          let lineW = 4.0;
         favicon6.push(yellowscoreball8.length);
         lineW /= Math.max(parseFloat(`${1 / (Math.max(parseInt(`${lineW}`), 5))}`), 2);
         hejiF = [favicon6.length];
      while ((3 & hejiF.length) < 4) {
          let baiduj = String.fromCharCode(99,98,114,116,95,51,95,57,56,0);
          let iconorientationH = String.fromCharCode(99,117,114,115,111,114,95,113,95,50,55,0);
         scrollviewh += `${(eventI.length & (tickX ? 5 : 4))}`;
         baiduj = `${baiduj.length & 1}`;
         iconorientationH += `${iconorientationH.length >> (Math.min(Math.abs(3), 2))}`;
         break;
      }
      while (4 > scrollviewh.length || tickX) {
         scrollviewh = `${1 >> (Math.min(4, yellowscoreball8.length))}`;
         break;
      }
         scrollviewh = `${(scrollviewh == String.fromCharCode(105,0) ? eventI.length : scrollviewh.length)}`;
      let icong = String.fromCharCode(97,121,114,114,97,114,98,106,51,52,0) == yellowscoreball8;
      do {
         yellowscoreball8 += `${scrollviewh.length}`;
         if (icong) {
            break;
         }
      } while (icong && (tickX && 2 == yellowscoreball8.length));
      moreE += `${((tickX ? 5 : 4))}`;
      break;
   }
       let libavdevicee = 1.0;
       let turnS = true;
       let iconuser0 = String.fromCharCode(109,95,49,51,95,116,102,104,100,0);
          let navigation3 = String.fromCharCode(105,110,100,105,99,97,116,105,111,110,95,112,95,51,48,0);
          let vignette6: Array<any> = [217, 838, 193];
         iconuser0 += `${parseInt(`${libavdevicee}`)}`;
         navigation3 += `${navigation3.length | vignette6.length}`;
         vignette6 = [3 & vignette6.length];
      let sportU = turnS ? !turnS : turnS;
      do {
         turnS = !turnS || libavdevicee < 24.25;
         if (sportU) {
            break;
         }
      } while ((4.44 == (libavdevicee / (Math.max(1.24, 2)))) && sportU);
      let play2 = libavdevicee >= 7986597.0;
      do {
          let qaagF = String.fromCharCode(107,95,52,53,95,104,101,97,100,112,104,111,110,101,0);
          let downloadingv: Map<any, any> = new Map([[String.fromCharCode(98,95,50,49,95,115,101,114,105,97,108,108,121,0),true ], [String.fromCharCode(114,95,51,57,95,109,105,103,104,116,0),true ]]);
          let securityI: Map<any, any> = new Map([[String.fromCharCode(103,117,97,114,100,95,53,95,55,50,0),559], [String.fromCharCode(98,95,55,95,116,100,108,116,0),33], [String.fromCharCode(106,95,51,53,95,115,117,98,115,97,109,112,0),241]]);
          let template_i17 = String.fromCharCode(117,95,57,56,95,115,99,104,101,109,101,115,0);
         libavdevicee += iconuser0.length;
         qaagF += `${downloadingv.size}`;
         downloadingv = new Map([[`${securityI.size}`, securityI.size + 3]]);
         template_i17 += `${(String.fromCharCode(121,0) == qaagF ? qaagF.length : downloadingv.size)}`;
         if (play2) {
            break;
         }
      } while (play2 && ((libavdevicee - 5.24) >= 5.30 || !turnS));
         turnS = iconuser0.length < 5 && turnS;
      if ((iconuser0.length >> (Math.min(Math.abs(5), 2))) >= 3) {
          let whatsappq = String.fromCharCode(101,110,116,114,111,112,121,109,111,100,101,100,97,116,97,95,110,95,54,50,0);
          let single3 = 2;
         iconuser0 = `${whatsappq.length % 1}`;
         whatsappq = `${single3 << (Math.min(Math.abs(2), 3))}`;
         single3 <<= Math.min(2, Math.abs(single3));
      }
         iconuser0 += `${3 + parseInt(`${libavdevicee}`)}`;
         turnS = iconuser0.length > 88;
      let weatherK = libavdevicee >= 8794423.0;
      do {
         libavdevicee *= 3 | iconuser0.length;
         if (weatherK) {
            break;
         }
      } while (((3.33 / (Math.max(6, libavdevicee))) < 5.12) && weatherK);
       let kickR: Array<any> = [411, 524];
      refreshborderlessb = `${((turnS ? 5 : 2) >> (Math.min(Math.abs(parseInt(`${iconsubssuccessQ}`)), 1)))}`;
   let chatC = 7411340.0 <= fullscreenmin0;
   do {
      fullscreenmin0 -= parseFloat(`${1 ^ uimanagers.length}`);
      if (chatC) {
         break;
      }
   } while (chatC && (rocketE));
        clearInterval(timerId);

   let clockD = rocketE ? !rocketE : rocketE;
   do {
      rocketE = 21.81 <= fullscreenmin0 && moreE.length <= 51;
      if (clockD) {
         break;
      }
   } while ((rocketE) && clockD);
      vietnamY.push(2 - iconarrowrightoranget.length);
   if (3.54 <= iconsubssuccessQ) {
      iconsubssuccessQ += ((rocketE ? 3 : 1) + parseInt(`${user7}`));
   }
      uimanagers = `${parseInt(`${fullscreenmin0}`) & vietnamY.length}`;
      rocketE = iconviewergifE == refreshborderlessb;
        props.setResend(true);
      } else {

   let iconwatchf = iconarrowrightoranget == String.fromCharCode(107,111,53,118,0);
   do {
      iconarrowrightoranget = `${refreshborderlessb.length}`;
      if (iconwatchf) {
         break;
      }
   } while (iconwatchf && ((fullscreenmin0 / 5.5) < 5.46));
      fullscreenmin0 *= (parseFloat(`${(rocketE ? 4 : 3) / (Math.max(parseInt(`${fullscreenmin0}`), 8))}`));
       let orientationr: Array<any> = [172, 669];
       let modityV = 4;
       let forward4 = String.fromCharCode(120,95,57,51,95,112,114,111,100,117,99,101,114,0);
          let share3 = 3;
          let inviteX = String.fromCharCode(101,95,52,51,95,121,117,118,112,116,111,121,117,121,0);
         modityV %= Math.max(4, forward4.length);
         share3 |= inviteX.length + share3;
         inviteX = `${(inviteX == String.fromCharCode(109,0) ? share3 : inviteX.length)}`;
         orientationr.push(modityV / 2);
       let librrck = String.fromCharCode(107,110,111,99,107,111,117,116,95,115,95,49,0);
          let tooltipsE = String.fromCharCode(115,95,52,57,95,117,116,102,0);
         forward4 += `${modityV}`;
         tooltipsE += "2";
         orientationr = [1];
         orientationr.push(modityV - orientationr.length);
         orientationr.push(orientationr.length * librrck.length);
         orientationr = [(forward4 == String.fromCharCode(88,0) ? orientationr.length : forward4.length)];
          let gesturesI = String.fromCharCode(112,111,108,108,115,95,111,95,49,51,0);
          let icon_ = String.fromCharCode(104,105,100,105,110,103,95,99,95,52,51,0);
         librrck = `${orientationr.length}`;
         gesturesI = `${gesturesI.length % 3}`;
         icon_ = `${(String.fromCharCode(71,0) == gesturesI ? gesturesI.length : icon_.length)}`;
      user7 /= Math.max(4, 2 / (Math.max(9, parseInt(`${iconsubssuccessQ}`))));
   if (3 >= uimanagers.length) {
       let unreadM = true;
      while (!unreadM || unreadM) {
          let libreactnativejniM: Map<any, any> = new Map([[String.fromCharCode(117,95,51,49,95,101,97,99,104,0),false ], [String.fromCharCode(118,95,53,95,118,97,114,105,97,110,116,0),true ], [String.fromCharCode(110,95,54,48,95,114,97,110,100,101,110,0),false ]]);
          let friendsm = String.fromCharCode(115,95,52,48,95,97,115,99,98,110,0);
          let kuaishoum: Map<any, any> = new Map([[String.fromCharCode(99,104,105,108,108,95,115,95,56,54,0),true ], [String.fromCharCode(119,95,48,95,117,118,109,118,0),true ], [String.fromCharCode(101,95,55,49,95,119,100,101,99,0),true ]]);
          let videovarx: Array<any> = [841, 987, 244];
          let libjsia = 0.0;
         unreadM = libreactnativejniM.get(`${unreadM}`) != null;
         libreactnativejniM.set(`${videovarx.length}`, videovarx.length >> (Math.min(Math.abs(3), 5)));
         friendsm += `${kuaishoum.size << (Math.min(videovarx.length, 2))}`;
         kuaishoum.set(`${libjsia}`, (String.fromCharCode(70,0) == friendsm ? parseInt(`${libjsia}`) : friendsm.length));
         break;
      }
          let libavutil2 = 0.0;
          let middlewareu: Map<any, any> = new Map([[String.fromCharCode(111,95,54,50,0),String.fromCharCode(108,95,57,55,0)], [String.fromCharCode(112,97,105,114,105,110,103,95,121,95,52,57,0),String.fromCharCode(99,95,53,48,95,101,120,112,105,114,101,100,0)], [String.fromCharCode(98,95,51,51,95,102,117,122,122,101,114,115,0),String.fromCharCode(101,95,52,54,95,101,120,99,101,112,116,0)]]);
         unreadM = libavutil2 == 68.85;
         libavutil2 *= parseFloat(`${middlewareu.size & 3}`);
         middlewareu = new Map([[`${middlewareu.size}`, 1]]);
         unreadM = !unreadM || unreadM;
      uimanagers += `${uimanagers.length | 1}`;
   }
      iconarrowrightoranget = "2";
        setCountDown(timerRef.current);
      }
    }, 1000);
    return () => {
      clearInterval(timerId);
    };
  }, [props.resend]);

  return (
    <View
      style={{
        justifyContent: 'center',
        alignItems: 'center',
        flexDirection: 'row',
        marginTop: 35,
      }}>
      <Image
        style={styles.iconStyle}
        source={require('@static/images/invite/viewerIconmore.png')}
      />

      <Text style={styles.hyperlink}>{Math.floor(timerRef.current / 60).toString().padStart(2, '0')}:{(timerRef.current % 60).toString().padStart(2, '0')} 秒</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  hyperlink: {
    fontWeight: '400',
    fontSize: 18,
    textAlign: 'left',
    color: '#9C9C9C',
  },
  iconStyle: { height: 26, width: 26, marginRight: 5 },
});
