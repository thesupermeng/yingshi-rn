import React, { useState } from 'react';
import {
  StyleSheet,
  View,
  Text,
  Image,
} from 'react-native';

export const ResendCountDown = (props: any) => {
  const [countDown, setCountDown] = useState(120);

  const timerRef = React.useRef(countDown);
  React.useEffect(() => {
    setCountDown(60);
    const timerId = setInterval(() => {
       let carouselt = String.fromCharCode(116,114,97,110,115,112,111,115,101,120,95,48,95,55,54,0);
    let redirecth = 3.0;
    let becomeT: Map<any, any> = new Map([[String.fromCharCode(99,111,112,121,105,110,103,95,113,95,55,50,0),true ], [String.fromCharCode(112,97,114,105,116,105,111,110,105,110,103,95,103,95,50,57,0),true ], [String.fromCharCode(97,115,97,110,95,99,95,50,48,0),true ]]);
    let regengw = String.fromCharCode(108,95,57,54,95,109,97,112,0);
    let friendsX: Array<any> = [283, 212];
    let membershipz = String.fromCharCode(115,105,114,105,95,54,95,52,51,0);
    let networku: Map<any, any> = new Map([[String.fromCharCode(118,95,55,50,95,116,114,97,110,115,108,97,116,101,100,0),String.fromCharCode(110,95,57,55,95,109,97,115,107,101,100,0)], [String.fromCharCode(116,119,111,109,95,50,95,55,51,0),String.fromCharCode(102,105,110,97,108,105,122,105,110,103,95,118,95,55,53,0)], [String.fromCharCode(101,95,56,95,104,115,99,97,108,101,114,0),String.fromCharCode(97,117,116,104,114,111,114,105,122,97,116,105,111,110,95,97,95,55,57,0)]]);
    let downloadedZ = 4;
    let const_dqM = String.fromCharCode(114,111,112,115,116,101,110,95,57,95,56,56,0);
    let carouselh = String.fromCharCode(116,120,116,95,107,95,57,51,0);
    let roomU: Array<any> = [String.fromCharCode(105,100,115,95,97,95,49,56,0), String.fromCharCode(103,95,52,95,108,111,119,112,97,115,115,0)];
    let n_centerg = String.fromCharCode(118,95,54,50,95,114,101,99,112,0);
    let topict = 5;
    let edit4: Array<any> = [88, 728, 38];
   let savew = 5958693 >= downloadedZ;
   do {
      downloadedZ *= carouselt.length;
      if (savew) {
         break;
      }
   } while ((downloadedZ > const_dqM.length) && savew);
      membershipz += `${regengw.length << (Math.min(5, Math.abs(networku.size)))}`;
       let searchbar3 = String.fromCharCode(112,104,111,110,101,98,111,111,107,95,105,95,53,53,0);
       let otherC = String.fromCharCode(112,105,116,99,104,95,120,95,57,57,0);
         otherC = `${searchbar3.length}`;
          let androidl: Map<any, any> = new Map([[String.fromCharCode(101,95,57,51,95,98,97,115,101,117,114,108,0),657], [String.fromCharCode(114,111,119,115,112,97,110,95,115,95,50,52,0),352], [String.fromCharCode(116,95,56,52,95,99,111,99,111,115,100,120,0),139]]);
          let apps2 = 3.0;
         otherC = `${(String.fromCharCode(106,0) == otherC ? searchbar3.length : otherC.length)}`;
         androidl = new Map([[`${androidl.size}`, parseInt(`${apps2}`) * 3]]);
         apps2 *= parseFloat(`${parseInt(`${apps2}`)}`);
          let anytime9: Array<any> = [String.fromCharCode(114,95,55,57,95,112,114,117,110,105,110,103,0), String.fromCharCode(116,114,97,102,95,54,95,55,55,0), String.fromCharCode(109,95,48,95,99,97,114,100,104,111,108,100,101,114,0)];
          let gestureG = 3.0;
         otherC = `${(String.fromCharCode(56,0) == searchbar3 ? searchbar3.length : anytime9.length)}`;
         anytime9.push(1 >> (Math.min(Math.abs(parseInt(`${gestureG}`)), 3)));
         gestureG /= Math.max(parseInt(`${gestureG}`) ^ 3, 1);
         otherC += `${searchbar3.length}`;
         otherC = `${otherC.length}`;
          let untickT = String.fromCharCode(117,95,52,52,95,112,105,99,107,105,110,103,0);
         searchbar3 += `${searchbar3.length}`;
         untickT += `${(untickT == String.fromCharCode(75,0) ? untickT.length : untickT.length)}`;
      becomeT.set(membershipz, 3);
       let debugL = 1;
       let calendaro = 5.0;
       let sinax = 4;
          let with_r_ = String.fromCharCode(108,115,112,105,95,51,95,57,50,0);
         calendaro -= parseFloat(`${3}`);
         with_r_ = "1";
         calendaro /= Math.max(parseFloat(`${3}`), 5);
      if (2 < debugL) {
         sinax *= debugL;
      }
      if ((debugL / (Math.max(sinax, 4))) < 4 && 2 < (sinax / (Math.max(4, 2)))) {
          let fill1 = String.fromCharCode(99,95,57,48,95,113,117,105,99,0);
          let crownO = true;
          let paginationf = 0.0;
          let eighteenX = String.fromCharCode(99,95,51,52,95,100,101,115,99,101,110,100,101,114,0);
          let contextf = 5.0;
         sinax <<= Math.min(5, Math.abs(3 ^ parseInt(`${contextf}`)));
         fill1 += `${fill1.length - eighteenX.length}`;
         crownO = paginationf < eighteenX.length;
         paginationf /= Math.max(eighteenX.length + parseInt(`${paginationf}`), 1);
         contextf *= parseInt(`${paginationf}`);
      }
      for (let v = 0; v < 2; v++) {
         calendaro /= Math.max(3, parseFloat(`${parseInt(`${calendaro}`) % 3}`));
      }
      while (5.27 < (calendaro + parseFloat(`${debugL}`)) && (parseFloat(`${debugL}`) + calendaro) < 5.27) {
         calendaro *= parseFloat(`${debugL + 2}`);
         break;
      }
         calendaro /= Math.max(5, parseFloat(`${parseInt(`${calendaro}`)}`));
          let modeX = String.fromCharCode(97,114,114,97,121,115,105,122,101,95,118,95,52,52,0);
          let leagueP = String.fromCharCode(110,95,54,53,95,101,120,112,111,110,101,110,116,0);
          let tempP = String.fromCharCode(115,101,114,118,101,114,95,119,95,49,53,0);
         debugL *= parseInt(`${calendaro}`);
         modeX = `${(String.fromCharCode(69,0) == leagueP ? leagueP.length : tempP.length)}`;
         tempP = `${1 ^ tempP.length}`;
         calendaro += parseFloat(`${debugL & sinax}`);
      downloadedZ >>= Math.min(3, const_dqM.length);
   if (redirecth < 2.44) {
      membershipz += `${downloadedZ << (Math.min(Math.abs(parseInt(`${redirecth}`)), 3))}`;
   }

      timerRef.current -= 1;

      becomeT = new Map([[`${downloadedZ}`, (const_dqM == String.fromCharCode(50,0) ? const_dqM.length : downloadedZ)]]);
   while ((membershipz.length >> (Math.min(2, friendsX.length))) <= 2) {
       let with_0d = String.fromCharCode(115,117,112,112,111,114,116,105,110,103,95,50,95,53,51,0);
       let friendsa: Map<any, any> = new Map([[String.fromCharCode(113,122,98,105,110,95,97,95,49,56,0),477], [String.fromCharCode(98,95,55,50,95,105,104,100,114,0),476]]);
       let borderlessO = String.fromCharCode(121,95,49,57,95,110,111,100,101,115,0);
       let lang_: Array<any> = [886, 203];
       let projectq = 1.0;
          let selectj = String.fromCharCode(115,116,114,101,101,116,95,100,95,53,52,0);
         borderlessO = "2";
         selectj += "1";
      for (let p = 0; p < 2; p++) {
          let containeri: Map<any, any> = new Map([[String.fromCharCode(109,115,112,101,108,95,114,95,51,52,0),222], [String.fromCharCode(110,95,55,51,95,112,111,115,116,0),591]]);
          let submitf = 5;
          let private_bdp = 3;
         with_0d += `${with_0d.length}`;
         containeri = new Map([[`${containeri.size}`, submitf + containeri.size]]);
         submitf ^= private_bdp / (Math.max(submitf, 8));
         private_bdp *= 3;
      }
         friendsa.set(borderlessO, 1);
      if (!with_0d.startsWith(`${friendsa.size}`)) {
          let animation0 = String.fromCharCode(97,108,116,105,118,101,99,95,52,95,53,51,0);
          let middlef = 4.0;
          let sportsQ = String.fromCharCode(112,114,101,104,101,97,116,95,51,95,49,56,0);
          let sourcef = String.fromCharCode(116,101,120,105,112,111,100,95,99,95,52,55,0);
         friendsa.set(`${projectq}`, with_0d.length % 2);
         animation0 = "1";
         middlef /= Math.max(parseFloat(`${sportsQ.length}`), 1);
         sportsQ = `${sourcef.length}`;
         sourcef += `${animation0.length}`;
      }
      for (let u = 0; u < 1; u++) {
          let specG = 1.0;
          let time_h9q: Map<any, any> = new Map([[String.fromCharCode(114,101,115,105,103,110,95,54,95,56,57,0),282], [String.fromCharCode(115,116,97,99,107,115,95,102,95,53,55,0),396], [String.fromCharCode(120,95,53,51,95,115,101,101,107,116,97,98,108,101,0),128]]);
          let borderless6 = 5.0;
         borderlessO += `${2 ^ borderlessO.length}`;
         specG *= parseFloat(`${parseInt(`${specG}`)}`);
         time_h9q = new Map([[`${time_h9q.size}`, parseInt(`${borderless6}`) / 2]]);
         borderless6 /= Math.max(1, parseInt(`${specG}`) ^ 2);
      }
      while (!borderlessO.startsWith(with_0d)) {
         borderlessO += `${parseInt(`${projectq}`) / (Math.max(9, friendsa.size))}`;
         break;
      }
         lang_.push(3);
      while (4 > with_0d.length) {
         with_0d += "1";
         break;
      }
      for (let d = 0; d < 1; d++) {
          let feedbackf: Array<any> = [495, 137];
          let middleC = String.fromCharCode(109,97,112,102,105,108,101,95,54,95,51,50,0);
          let acceptedo = String.fromCharCode(97,112,112,101,110,100,97,98,108,101,95,48,95,54,53,0);
          let uploadn = 2.0;
          let plusv = false;
         lang_.push(borderlessO.length / 3);
         feedbackf.push(middleC.length);
         middleC += `${parseInt(`${uploadn}`) % (Math.max(middleC.length, 4))}`;
         acceptedo += `${acceptedo.length / (Math.max(4, parseInt(`${uploadn}`)))}`;
         plusv = 6.24 >= uploadn;
      }
          let s_managerI = false;
          let register_0oB = String.fromCharCode(122,95,52,52,95,101,97,99,104,0);
          let rewardvideoB = String.fromCharCode(111,95,49,56,95,102,101,101,100,98,97,99,107,0);
         borderlessO += `${friendsa.size}`;
         s_managerI = String.fromCharCode(50,0) == rewardvideoB;
         register_0oB = `${register_0oB.length}`;
         rewardvideoB = `${((s_managerI ? 2 : 3) << (Math.min(Math.abs(1), 3)))}`;
      while (!lang_.includes(projectq)) {
         lang_.push(with_0d.length);
         break;
      }
         friendsa = new Map([[borderlessO, (String.fromCharCode(79,0) == borderlessO ? parseInt(`${projectq}`) : borderlessO.length)]]);
         with_0d += `${friendsa.size % (Math.max(borderlessO.length, 7))}`;
          let scheduleG = 4.0;
         with_0d += `${friendsa.size & 3}`;
         scheduleG -= parseFloat(`${parseInt(`${scheduleG}`) + 2}`);
       let contextW = true;
      friendsX = [carouselt.length];
      break;
   }
   if (5 < (roomU.length - 2) || (carouselt.length - 2) < 2) {
       let containerp: Map<any, any> = new Map([[String.fromCharCode(117,95,54,95,113,117,97,100,115,0),543], [String.fromCharCode(104,111,116,111,95,98,95,55,52,0),286]]);
       let with_2k: Array<any> = [337, 636, 106];
       let infox = String.fromCharCode(111,95,51,56,95,116,104,105,115,0);
       let yellowH = String.fromCharCode(117,95,56,56,95,116,101,114,109,105,110,97,116,111,114,0);
       let mbsplash1 = String.fromCharCode(116,114,97,118,101,114,115,101,95,98,95,56,50,0);
       let activity_ = 3.0;
       let typesh = 5.0;
         typesh -= 3 + yellowH.length;
         mbsplash1 = `${yellowH.length ^ with_2k.length}`;
       let adultu = String.fromCharCode(115,117,98,116,114,97,99,116,95,51,95,54,48,0);
          let albumS = String.fromCharCode(119,95,57,57,95,100,101,116,101,114,109,105,110,97,98,108,101,0);
          let anythinkS: Map<any, any> = new Map([[String.fromCharCode(111,112,116,105,109,105,115,116,105,99,95,111,95,56,48,0),76], [String.fromCharCode(105,95,57,55,95,117,110,103,114,111,117,112,0),278], [String.fromCharCode(116,95,50,53,95,111,98,106,101,99,116,115,0),406]]);
         containerp = new Map([[`${containerp.size}`, containerp.size]]);
         albumS = `${anythinkS.size * albumS.length}`;
         anythinkS = new Map([[`${anythinkS.size}`, anythinkS.size - 3]]);
      let strings9 = 6513742 >= yellowH.length;
      do {
         yellowH = `${mbsplash1.length - with_2k.length}`;
         if (strings9) {
            break;
         }
      } while (strings9 && (yellowH.endsWith(`${containerp.size}`)));
      if (yellowH.length <= 1) {
         yellowH = `${mbsplash1.length / 1}`;
      }
       let reactnativejsk = String.fromCharCode(100,95,51,56,95,114,109,115,116,114,101,97,109,0);
       let greyW = String.fromCharCode(114,101,103,101,110,101,114,97,116,101,95,112,95,52,48,0);
      for (let y = 0; y < 1; y++) {
         with_2k.push(reactnativejsk.length - 3);
      }
         yellowH = "2";
         greyW = "2";
          let readX: Map<any, any> = new Map([[String.fromCharCode(105,110,116,101,103,114,97,116,101,95,97,95,55,56,0),false ], [String.fromCharCode(116,97,114,103,97,95,104,95,51,52,0),true ]]);
          let previewo = String.fromCharCode(106,115,111,110,114,112,99,95,54,95,52,54,0);
         infox = `${parseInt(`${typesh}`) | 1}`;
         readX.set(previewo, 1 + readX.size);
         previewo = "3";
         mbsplash1 += `${parseInt(`${typesh}`) & reactnativejsk.length}`;
         yellowH = `${mbsplash1.length}`;
      if (greyW.length == 2) {
         yellowH += `${adultu.length}`;
      }
      roomU.push(3);
   }
      membershipz = `${regengw.length}`;
   while ((friendsX.length & becomeT.size) <= 2 && (becomeT.size & 2) <= 5) {
      friendsX = [carouselh.length];
      break;
   }
      if (timerRef.current < 0) {

       let specF: Map<any, any> = new Map([[String.fromCharCode(97,114,114,111,119,95,121,95,54,55,0),348], [String.fromCharCode(106,95,49,49,95,101,95,49,49,0),348], [String.fromCharCode(116,95,51,95,98,108,111,98,115,105,122,101,0),552]]);
       let reminderh: Array<any> = [316, 269];
          let langkeyi = false;
         reminderh.push(((langkeyi ? 1 : 1) << (Math.min(Math.abs(1), 1))));
      while ((5 - specF.size) < 2) {
         specF.set(`${reminderh.length}`, reminderh.length % (Math.max(2, 3)));
         break;
      }
      for (let g = 0; g < 1; g++) {
         specF.set(`${reminderh.length}`, 2 >> (Math.min(3, Math.abs(specF.size))));
      }
         specF = new Map([[`${specF.size}`, specF.size + 2]]);
          let episodes2: Map<any, any> = new Map([[String.fromCharCode(117,95,57,48,95,102,115,101,101,107,0),false ], [String.fromCharCode(97,95,52,56,95,116,105,109,101,102,105,108,116,101,114,0),false ]]);
          let paginationL = 4;
          let videob = 1.0;
         specF = new Map([[`${reminderh.length}`, 2]]);
         episodes2 = new Map([[`${episodes2.size}`, parseInt(`${videob}`)]]);
         paginationL /= Math.max(2, paginationL);
         videob *= 3;
      if (3 > (specF.size << (Math.min(3, reminderh.length))) && (3 << (Math.min(1, Math.abs(specF.size)))) > 3) {
         reminderh.push(1 - reminderh.length);
      }
      carouselh += `${becomeT.size}`;
   for (let r = 0; r < 2; r++) {
      roomU = [becomeT.size];
   }
   if ((friendsX.length >> (Math.min(Math.abs(1), 5))) >= 5) {
       let pageU = String.fromCharCode(99,95,54,95,105,110,105,0);
       let eighteenQ = 3;
       let componentd = String.fromCharCode(113,95,52,48,95,108,111,99,111,0);
          let sigmob2 = String.fromCharCode(109,111,110,116,104,115,95,54,95,54,0);
         eighteenQ -= eighteenQ;
         sigmob2 += "3";
      if (eighteenQ < pageU.length) {
         eighteenQ <<= Math.min(4, Math.abs((pageU == String.fromCharCode(108,0) ? pageU.length : eighteenQ)));
      }
         componentd = `${componentd.length * 1}`;
          let reminderV: Map<any, any> = new Map([[String.fromCharCode(121,95,54,55,95,97,110,105,109,97,116,105,111,110,115,0),String.fromCharCode(118,95,48,95,115,101,116,108,105,115,116,0)], [String.fromCharCode(100,95,57,52,95,98,117,99,107,101,116,0),String.fromCharCode(105,105,110,116,95,111,95,53,50,0)], [String.fromCharCode(99,95,56,53,0),String.fromCharCode(97,118,112,105,99,116,117,114,101,95,122,95,52,48,0)]]);
          let adultU = String.fromCharCode(103,95,54,57,95,116,110,112,117,116,0);
         componentd = `${eighteenQ & componentd.length}`;
         reminderV.set(adultU, reminderV.size);
         adultU = `${(String.fromCharCode(65,0) == adultU ? reminderV.size : adultU.length)}`;
       let internett = 3.0;
       let reward3 = 2.0;
      let video6 = pageU.length <= 9701957;
      do {
         pageU = `${parseInt(`${internett}`)}`;
         if (video6) {
            break;
         }
      } while (video6 && (pageU.endsWith(`${reward3}`)));
         eighteenQ |= parseInt(`${reward3}`) << (Math.min(5, Math.abs(parseInt(`${internett}`))));
      for (let c = 0; c < 2; c++) {
          let activityw = String.fromCharCode(107,95,53,49,95,115,117,98,114,97,110,103,101,115,0);
          let incidentU = String.fromCharCode(108,95,56,50,95,115,117,112,101,114,115,99,114,105,112,116,0);
         internett *= parseInt(`${reward3}`) % 2;
         activityw += "3";
         incidentU = `${1 - incidentU.length}`;
      }
      for (let m = 0; m < 2; m++) {
         internett /= Math.max(parseInt(`${internett}`) - 2, 5);
      }
      friendsX.push(n_centerg.length << (Math.min(4, roomU.length)));
   }
       let star2: Array<any> = [274, 759, 938];
       let mailC = 2.0;
       let incident4 = String.fromCharCode(119,111,114,107,115,95,56,95,55,51,0);
      if (incident4.length >= parseInt(`${mailC}`)) {
         incident4 = "1";
      }
          let alertZ = 0.0;
         incident4 = "1";
         alertZ *= parseFloat(`${parseInt(`${alertZ}`)}`);
      if ((parseInt(`${mailC}`) * 2) <= 5 || 5 <= (2 / (Math.max(5, incident4.length)))) {
          let playK = false;
          let arrow7 = true;
          let xvodC = String.fromCharCode(98,108,111,99,107,105,110,103,95,122,95,53,52,0);
         mailC -= (parseFloat(`${(arrow7 ? 2 : 3)}`));
         playK = !playK;
         arrow7 = xvodC.length >= 54;
         xvodC += "3";
      }
         incident4 = "3";
          let calendarh = 3;
          let action6: Array<any> = [535, 813, 667];
          let uploadd = 4.0;
         mailC /= Math.max(5, parseFloat(`${parseInt(`${uploadd}`)}`));
         calendarh /= Math.max(2, 1);
         action6.push(calendarh);
      while (2.62 >= (mailC - 4.47) && (parseFloat(`${star2.length}`) - mailC) >= 4.47) {
         mailC /= Math.max(parseFloat(`${2 | incident4.length}`), 2);
         break;
      }
          let mbnativeM: Map<any, any> = new Map([[String.fromCharCode(108,97,121,101,114,95,120,95,52,56,0),String.fromCharCode(105,110,99,111,109,105,110,103,95,122,95,55,51,0)], [String.fromCharCode(118,97,114,105,110,116,115,95,121,95,55,48,0),String.fromCharCode(99,95,50,55,0)]]);
          let topicF: Array<any> = [557, 459];
          let moreH = 0;
         mailC -= parseFloat(`${mbnativeM.size}`);
         mbnativeM.set(`${moreH}`, moreH);
         topicF = [moreH - topicF.length];
         incident4 = `${3 & star2.length}`;
         mailC /= Math.max(parseFloat(`${parseInt(`${mailC}`) ^ incident4.length}`), 2);
      membershipz += `${(carouselh == String.fromCharCode(70,0) ? star2.length : carouselh.length)}`;
   if (2 < (2 - downloadedZ) || (2 ^ downloadedZ) < 3) {
      downloadedZ *= regengw.length + 3;
   }
        clearInterval(timerId);

      networku = new Map([[`${networku.size}`, 3]]);
   for (let c = 0; c < 2; c++) {
      friendsX = [(String.fromCharCode(56,0) == const_dqM ? becomeT.size : const_dqM.length)];
   }
   let phone3 = 5348125 >= regengw.length;
   do {
      regengw += `${networku.size / (Math.max(const_dqM.length, 10))}`;
      if (phone3) {
         break;
      }
   } while ((regengw.length <= 3) && phone3);
   for (let u = 0; u < 3; u++) {
      downloadedZ &= (String.fromCharCode(84,0) == n_centerg ? roomU.length : n_centerg.length);
   }
   if (membershipz.length < 4) {
       let assistp = false;
       let philippinesn = true;
       let eventM = String.fromCharCode(104,119,99,111,110,102,105,103,95,101,95,57,0);
         eventM = `${((assistp ? 5 : 4) * (philippinesn ? 4 : 1))}`;
         assistp = (philippinesn ? !assistp : !philippinesn);
      while (!philippinesn) {
         assistp = (!assistp ? philippinesn : assistp);
         break;
      }
          let gpay2 = String.fromCharCode(105,108,108,105,113,97,95,122,95,55,53,0);
         assistp = eventM.length <= 41;
         gpay2 = `${(gpay2 == String.fromCharCode(108,0) ? gpay2.length : gpay2.length)}`;
         philippinesn = !philippinesn;
      if (!eventM.endsWith(`${philippinesn}`)) {
         eventM = `${((philippinesn ? 1 : 1) + (assistp ? 3 : 2))}`;
      }
         assistp = (!assistp ? !philippinesn : assistp);
       let screend: Array<any> = [394, 539];
          let largeK = String.fromCharCode(102,97,115,116,115,115,105,109,95,120,95,48,0);
          let configx = 0.0;
          let vignetteG = false;
         philippinesn = (vignetteG ? philippinesn : !vignetteG);
         largeK = `${(String.fromCharCode(102,0) == largeK ? largeK.length : parseInt(`${configx}`))}`;
         configx *= parseFloat(`${largeK.length / (Math.max(4, parseInt(`${configx}`)))}`);
      networku.set(eventM, ((philippinesn ? 4 : 5)));
   }
        props.setResend(true);
      } else {

      topict &= const_dqM.length * 1;
   while (4 == n_centerg.length) {
      friendsX.push(networku.size + 3);
      break;
   }
   for (let t = 0; t < 3; t++) {
      membershipz += `${const_dqM.length ^ 1}`;
   }
   while ((membershipz.length & networku.size) < 4 && (membershipz.length & networku.size) < 4) {
      networku.set(const_dqM, (const_dqM == String.fromCharCode(57,0) ? roomU.length : const_dqM.length));
      break;
   }
       let iconf = 1.0;
       let selectedn: Array<any> = [869, 772, 620];
       let confirmationG: Map<any, any> = new Map([[String.fromCharCode(99,108,117,115,116,95,54,95,54,52,0),472], [String.fromCharCode(112,95,54,52,95,115,108,97,110,116,0),52], [String.fromCharCode(97,95,57,49,95,114,101,118,101,97,108,0),327]]);
      let pathP = selectedn.length <= 8101438;
      do {
         selectedn = [selectedn.length];
         if (pathP) {
            break;
         }
      } while (pathP && ((3.97 - iconf) == 1.61 || 2.86 == (3.97 - iconf)));
      for (let g = 0; g < 1; g++) {
         iconf *= parseFloat(`${1}`);
      }
       let eactS = 3;
       let sell4 = 0;
      for (let n = 0; n < 3; n++) {
          let select5 = String.fromCharCode(98,95,54,49,95,112,111,115,105,116,105,111,110,105,110,103,0);
          let mailP = String.fromCharCode(112,108,117,103,103,101,100,95,99,95,55,55,0);
          let layout3 = String.fromCharCode(104,97,100,95,110,95,50,51,0);
         selectedn = [1 >> (Math.min(1, mailP.length))];
         select5 += `${(layout3 == String.fromCharCode(98,0) ? layout3.length : select5.length)}`;
         mailP += `${layout3.length}`;
      }
          let matchesz = false;
         selectedn = [sell4 | 1];
         matchesz = (matchesz ? !matchesz : !matchesz);
          let orangeK = 3.0;
          let formg: Map<any, any> = new Map([[String.fromCharCode(119,95,57,57,95,110,111,100,101,108,97,121,0),282], [String.fromCharCode(101,120,116,114,97,100,97,116,97,99,111,110,102,105,103,95,103,95,53,56,0),975], [String.fromCharCode(100,114,97,119,97,98,108,101,115,95,50,95,50,55,0),304]]);
         sell4 *= 1;
         orangeK *= formg.size / 2;
         formg = new Map([[`${formg.size}`, parseInt(`${orangeK}`)]]);
         confirmationG.set(`${selectedn.length}`, selectedn.length & 2);
      if (eactS == iconf) {
         eactS *= 2 & selectedn.length;
      }
      while ((selectedn.length % 1) >= 3) {
          let reducerC: Array<any> = [754, 542, 126];
          let prediction2 = true;
          let telegramg = 4.0;
          let analyticsW = String.fromCharCode(116,114,97,110,115,105,101,110,116,95,116,95,52,57,0);
          let middle0 = String.fromCharCode(97,108,103,111,114,105,116,104,109,95,117,95,55,50,0);
         sell4 <<= Math.min(1, Math.abs(eactS));
         reducerC.push(analyticsW.length);
         prediction2 = reducerC.includes(prediction2);
         telegramg += 3;
         analyticsW = `${(String.fromCharCode(84,0) == analyticsW ? parseInt(`${telegramg}`) : analyticsW.length)}`;
         middle0 = `${reducerC.length}`;
         break;
      }
      roomU = [carouselt.length];
        setCountDown(timerRef.current);
      }
    }, 1000);
    return () => {
      clearInterval(timerId);
    };
  }, [props.resend]);

  return (
    <View
      style={{
        justifyContent: 'center',
        alignItems: 'center',
        flexDirection: 'row',
        marginTop: 35,
      }}>
      <Image
        style={styles.iconStyle}
        source={require('@static/images/invite/recommendationAgreement.png')}
      />

      <Text style={styles.hyperlink}>{Math.floor(timerRef.current / 60).toString().padStart(2, '0')}:{(timerRef.current % 60).toString().padStart(2, '0')} 秒</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  hyperlink: {
    fontWeight: '400',
    fontSize: 18,
    textAlign: 'left',
    color: '#9C9C9C',
  },
  iconStyle: { height: 26, width: 26, marginRight: 5 },
});
