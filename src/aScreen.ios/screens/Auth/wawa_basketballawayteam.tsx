import React, { useState } from 'react';
import {
  StyleSheet,
  View,
  Text,
  Image,
} from 'react-native';

export const ResendCountDown = (props: any) => {
  const [countDown, setCountDown] = useState(120);

  const timerRef = React.useRef(countDown);
  React.useEffect(() => {
    setCountDown(60);
    const timerId = setInterval(() => {
       let windJ = 1.0;
    let disconnectedC = 0;
    let orangetickB = String.fromCharCode(122,101,114,111,101,100,95,122,95,50,51,0);
    let checkbox3 = String.fromCharCode(99,111,110,116,101,120,116,112,114,111,102,105,108,101,108,101,118,101,108,95,98,95,57,55,0);
    let typingp = 1.0;
    let langa = String.fromCharCode(109,118,99,111,117,110,116,95,53,95,53,53,0);
    let foundd = String.fromCharCode(111,95,52,49,95,108,105,102,116,0);
    let brightnessx: Map<any, any> = new Map([[String.fromCharCode(97,117,100,105,101,110,99,101,95,109,95,52,56,0),String.fromCharCode(114,95,56,52,95,118,101,114,98,111,115,101,0)], [String.fromCharCode(112,95,53,55,95,115,121,109,98,111,108,105,99,97,116,101,100,0),String.fromCharCode(99,116,108,111,117,116,112,117,116,95,101,95,51,48,0)]]);
    let sportsI: Array<any> = [String.fromCharCode(110,95,57,55,95,122,111,109,98,105,101,0), String.fromCharCode(116,114,97,105,116,95,111,95,56,55,0), String.fromCharCode(112,95,50,55,95,115,101,113,118,105,100,101,111,0)];
    let livenodatabgimgr = String.fromCharCode(110,98,105,111,95,120,95,53,51,0);
    let carouselW = 2.0;
    let casth = true;
    let live1 = String.fromCharCode(100,105,102,95,55,95,54,53,0);
    let bases = 5.0;
      langa = `${(String.fromCharCode(119,0) == livenodatabgimgr ? livenodatabgimgr.length : sportsI.length)}`;
      foundd += `${(String.fromCharCode(106,0) == orangetickB ? checkbox3.length : orangetickB.length)}`;
      sportsI = [sportsI.length];
   let subsE = casth ? !casth : casth;
   do {
       let hoverP: Array<any> = [759, 65, 511];
       let hometeamfieldt = true;
       let downloadedm: Array<any> = [377, 164, 356];
       let executor7 = String.fromCharCode(108,95,55,51,95,103,101,111,108,111,99,97,116,105,111,110,0);
       let rewardvideoF = String.fromCharCode(117,95,52,57,95,101,109,109,105,110,116,114,105,110,0);
       let policyK = String.fromCharCode(106,112,101,103,108,105,98,95,112,95,50,50,0);
      if (!policyK.endsWith(`${hometeamfieldt}`)) {
          let debugQ = false;
          let runtimeschedulerz = 5;
          let storeI = true;
         hometeamfieldt = String.fromCharCode(51,0) == policyK || hoverP.length == 55;
         debugQ = runtimeschedulerz <= 26;
         runtimeschedulerz <<= Math.min(Math.abs(runtimeschedulerz), 5);
         storeI = !storeI && runtimeschedulerz <= 45;
      }
          let trophyf = 3;
          let otherV: Map<any, any> = new Map([[String.fromCharCode(117,95,53,54,0),940], [String.fromCharCode(109,95,50,49,95,114,101,115,112,111,110,115,101,115,0),57]]);
          let main_l9 = 2;
         downloadedm.push(trophyf % 3);
         trophyf -= main_l9;
         otherV.set(`${main_l9}`, otherV.size);
      if (1 < rewardvideoF.length || 1 < policyK.length) {
          let descG = String.fromCharCode(110,95,55,54,95,100,108,116,97,0);
          let rncorel = String.fromCharCode(109,95,56,52,95,105,115,109,101,109,115,101,116,0);
          let homeinactive2 = 0.0;
          let score_ = 3;
          let smallorangemanI: Map<any, any> = new Map([[String.fromCharCode(97,95,56,49,95,112,114,111,109,105,115,101,115,0),698], [String.fromCharCode(97,110,105,109,97,116,111,114,115,95,117,95,54,52,0),428], [String.fromCharCode(102,95,52,57,0),709]]);
         policyK += `${((hometeamfieldt ? 1 : 4) << (Math.min(hoverP.length, 5)))}`;
         descG += `${rncorel.length | score_}`;
         rncorel = `${smallorangemanI.size + descG.length}`;
         homeinactive2 /= Math.max(parseFloat(`${score_}`), 5);
         smallorangemanI.set(`${homeinactive2}`, score_);
      }
      while (1 >= (executor7.length | downloadedm.length)) {
          let entryw = 4.0;
          let coreB = String.fromCharCode(119,105,110,100,111,119,115,95,111,95,54,56,0);
          let reactI = String.fromCharCode(112,105,112,101,108,105,110,105,110,103,95,117,95,51,50,0);
         executor7 += `${((hometeamfieldt ? 4 : 4))}`;
         entryw += parseFloat(`${parseInt(`${entryw}`) / (Math.max(coreB.length, 9))}`);
         coreB = `${2 << (Math.min(4, Math.abs(parseInt(`${entryw}`))))}`;
         reactI += `${parseInt(`${entryw}`) % 3}`;
         break;
      }
      if (rewardvideoF != String.fromCharCode(67,0)) {
          let modal4: Array<any> = [982, 353, 694];
          let utilsW = 1.0;
          let renewB = String.fromCharCode(99,115,99,104,101,109,101,95,111,95,51,51,0);
          let acceptedj = 2.0;
          let network1 = false;
         policyK += `${2 + downloadedm.length}`;
         modal4 = [3 * modal4.length];
         utilsW += modal4.length / (Math.max(2, 6));
         renewB = `${3 % (Math.max(7, parseInt(`${acceptedj}`)))}`;
         acceptedj *= renewB.length;
      }
         policyK = "3";
      while (1 < policyK.length && !hometeamfieldt) {
         policyK = `${executor7.length}`;
         break;
      }
      if (executor7 == rewardvideoF) {
         rewardvideoF = `${executor7.length ^ 1}`;
      }
          let xadsdkK = String.fromCharCode(99,117,114,108,121,95,117,95,49,54,0);
         downloadedm = [downloadedm.length];
         xadsdkK += `${(String.fromCharCode(104,0) == xadsdkK ? xadsdkK.length : xadsdkK.length)}`;
      let shirt7 = hometeamfieldt ? !hometeamfieldt : hometeamfieldt;
      do {
         hometeamfieldt = rewardvideoF.length >= 21;
         if (shirt7) {
            break;
         }
      } while (((1 << (Math.min(3, hoverP.length))) <= 3) && shirt7);
      for (let t = 0; t < 2; t++) {
         executor7 += "2";
      }
      casth = downloadedm.length >= 6 || 6 >= livenodatabgimgr.length;
      if (subsE) {
         break;
      }
   } while (subsE && (!casth));
   while (carouselW == disconnectedC) {
      disconnectedC |= parseInt(`${carouselW}`) % (Math.max(live1.length, 2));
      break;
   }
   while (sportsI.length <= livenodatabgimgr.length) {
      livenodatabgimgr = `${2 & livenodatabgimgr.length}`;
      break;
   }
   let xvodZ = casth ? !casth : casth;
   do {
       let pressure3 = 4.0;
         pressure3 += parseFloat(`${3 - parseInt(`${pressure3}`)}`);
      if ((pressure3 * pressure3) <= 1.76 || 1.76 <= (pressure3 * pressure3)) {
         pressure3 -= parseFloat(`${1 << (Math.min(Math.abs(parseInt(`${pressure3}`)), 2))}`);
      }
       let evente = 5;
      casth = (langa.length >> (Math.min(4, Math.abs(brightnessx.size)))) == 10;
      if (xvodZ) {
         break;
      }
   } while ((5 < checkbox3.length) && xvodZ);

      timerRef.current -= 1;

      windJ += livenodatabgimgr.length + langa.length;
      langa = `${disconnectedC | parseInt(`${windJ}`)}`;
   for (let c = 0; c < 3; c++) {
       let notificationfillemptyo: Map<any, any> = new Map([[String.fromCharCode(114,101,113,117,97,110,116,95,97,95,52,51,0),259], [String.fromCharCode(116,95,54,52,95,99,97,116,101,103,111,114,105,122,101,0),130]]);
         notificationfillemptyo = new Map([[`${notificationfillemptyo.size}`, 2 + notificationfillemptyo.size]]);
          let footballtrophyo = String.fromCharCode(109,95,51,95,99,118,99,0);
          let rewardvideo9 = 0.0;
          let referrerb: Map<any, any> = new Map([[String.fromCharCode(105,95,50,55,95,112,114,105,109,105,116,105,118,101,0),true ], [String.fromCharCode(99,111,109,112,95,51,95,55,49,0),true ], [String.fromCharCode(98,111,114,105,110,103,115,115,108,95,98,95,51,50,0),false ]]);
         notificationfillemptyo.set(footballtrophyo, 3 | footballtrophyo.length);
         rewardvideo9 -= parseFloat(`${1 * referrerb.size}`);
         referrerb = new Map([[`${referrerb.size}`, referrerb.size / (Math.max(2, 8))]]);
         notificationfillemptyo.set(`${notificationfillemptyo.size}`, 1);
      live1 = `${parseInt(`${windJ}`)}`;
   }
      orangetickB += "1";
   while (foundd.startsWith(`${windJ}`)) {
      windJ /= Math.max((2 * (casth ? 2 : 5)), 5);
      break;
   }
   while (3.62 < carouselW) {
       let othermatchdetailbgl: Array<any> = [977, 855];
       let downarrowY = String.fromCharCode(97,95,55,49,95,101,110,99,111,100,101,114,115,0);
         downarrowY = `${downarrowY.length >> (Math.min(4, othermatchdetailbgl.length))}`;
      while (2 >= (othermatchdetailbgl.length * 2)) {
          let currentB = String.fromCharCode(104,95,55,49,95,115,117,98,0);
         downarrowY = `${othermatchdetailbgl.length + currentB.length}`;
         break;
      }
       let dragj = String.fromCharCode(119,111,114,107,105,110,103,95,50,95,53,54,0);
       let progressI = String.fromCharCode(115,117,110,118,101,114,95,49,95,52,57,0);
         dragj = `${dragj.length}`;
         progressI += `${othermatchdetailbgl.length}`;
      while (progressI.length >= 1) {
         progressI += `${downarrowY.length ^ 3}`;
         break;
      }
      casth = String.fromCharCode(108,0) == foundd && orangetickB.length < 46;
      break;
   }
      livenodatabgimgr = `${(live1 == String.fromCharCode(120,0) ? disconnectedC : live1.length)}`;
      if (timerRef.current < 0) {

      carouselW += (parseInt(`${typingp}`) << (Math.min(5, Math.abs((casth ? 5 : 1)))));
   for (let g = 0; g < 3; g++) {
      typingp *= 1 >> (Math.min(2, livenodatabgimgr.length));
   }
      carouselW *= 1 ^ orangetickB.length;
   for (let t = 0; t < 1; t++) {
      checkbox3 = `${1 * parseInt(`${carouselW}`)}`;
   }
      casth = live1.length < 26;
   for (let y = 0; y < 2; y++) {
      orangetickB += `${parseInt(`${typingp}`) - 3}`;
   }
       let searchV = true;
       let mbjscommonl = String.fromCharCode(101,95,50,57,95,115,121,115,108,111,103,0);
         mbjscommonl += `${((searchV ? 4 : 3) | 3)}`;
      for (let e = 0; e < 2; e++) {
          let ballF = String.fromCharCode(103,95,57,56,95,116,111,112,109,111,115,116,0);
          let positionfieldf: Array<any> = [351, 126, 643];
          let icon5 = 1.0;
          let h_positionk = String.fromCharCode(115,116,97,110,100,97,114,100,115,95,119,95,55,57,0);
         mbjscommonl += `${mbjscommonl.length ^ 3}`;
         ballF = `${1 + parseInt(`${icon5}`)}`;
         positionfieldf = [h_positionk.length];
         icon5 += parseFloat(`${h_positionk.length}`);
      }
          let storeY = 0.0;
          let anythinkN = String.fromCharCode(109,97,103,110,105,116,117,100,101,115,95,115,95,50,50,0);
         mbjscommonl = `${3 | parseInt(`${storeY}`)}`;
         storeY += parseFloat(`${3}`);
         anythinkN = `${anythinkN.length / 2}`;
      if (searchV) {
         mbjscommonl += `${((searchV ? 4 : 2) | 2)}`;
      }
          let floaterE = String.fromCharCode(99,117,108,102,114,101,113,95,103,95,57,49,0);
          let taiwano = String.fromCharCode(122,95,53,48,95,100,101,109,117,120,0);
          let xadsdkU: Map<any, any> = new Map([[String.fromCharCode(107,95,53,51,95,116,97,110,0),String.fromCharCode(115,97,102,101,115,116,97,99,107,95,51,95,49,51,0)], [String.fromCharCode(111,112,117,115,116,97,98,95,50,95,57,49,0),String.fromCharCode(105,95,56,49,95,97,114,103,120,0)], [String.fromCharCode(106,95,54,48,95,97,117,100,105,116,105,110,103,0),String.fromCharCode(106,95,57,52,95,97,114,103,118,0)]]);
         searchV = xadsdkU.size <= 31;
         floaterE += `${(floaterE == String.fromCharCode(106,0) ? floaterE.length : taiwano.length)}`;
         taiwano += `${taiwano.length}`;
         xadsdkU.set(taiwano, 3 & taiwano.length);
      for (let f = 0; f < 3; f++) {
         mbjscommonl = `${mbjscommonl.length}`;
      }
      orangetickB = `${parseInt(`${carouselW}`) << (Math.min(Math.abs(2), 2))}`;
        clearInterval(timerId);

   if (brightnessx.size <= 2) {
      brightnessx.set(`${live1}`, 1);
   }
      brightnessx.set(orangetickB, (String.fromCharCode(72,0) == orangetickB ? live1.length : orangetickB.length));
   if (3.16 == (orangetickB.length - windJ)) {
       let tempnodatagifz = 2.0;
       let zoomY = false;
       let runtimeschedulerf: Map<any, any> = new Map([[String.fromCharCode(99,111,109,109,97,110,100,115,95,54,95,52,51,0),25], [String.fromCharCode(102,105,110,95,113,95,49,0),6]]);
       let rewind9 = false;
      while (4.12 == tempnodatagifz && 5.21 == (4.12 - tempnodatagifz)) {
         zoomY = runtimeschedulerf.size >= 48;
         break;
      }
      let libfollyO = tempnodatagifz <= 5531071.0;
      do {
          let imagesy = String.fromCharCode(101,95,51,95,108,97,122,105,108,121,0);
          let relatedr = 0.0;
         tempnodatagifz += (2 % (Math.max((zoomY ? 2 : 3), 6)));
         imagesy = `${imagesy.length}`;
         relatedr += parseFloat(`${imagesy.length | parseInt(`${relatedr}`)}`);
         if (libfollyO) {
            break;
         }
      } while (libfollyO && (3.20 < (3.57 / (Math.max(9, tempnodatagifz)))));
         rewind9 = 84.93 == tempnodatagifz && zoomY;
         rewind9 = zoomY;
      if (rewind9 || 4 > (runtimeschedulerf.size / (Math.max(5, 7)))) {
         rewind9 = runtimeschedulerf.get(`${zoomY}`) != null;
      }
         tempnodatagifz *= ((zoomY ? 1 : 4) | runtimeschedulerf.size);
      let sourcez = zoomY ? !zoomY : zoomY;
      do {
         zoomY = 61.99 < tempnodatagifz;
         if (sourcez) {
            break;
         }
      } while (sourcez && (3 >= runtimeschedulerf.size && (3 | runtimeschedulerf.size) >= 4));
       let loadingspinnerF = String.fromCharCode(115,95,49,51,95,115,112,97,99,101,114,115,0);
       let default_mm = String.fromCharCode(112,95,49,56,95,113,109,105,110,109,97,120,0);
      let canvasr = tempnodatagifz <= 6973021.0;
      do {
          let viewera = String.fromCharCode(101,114,114,111,114,115,95,56,95,56,50,0);
          let assets7 = true;
         tempnodatagifz *= default_mm.length;
         viewera = `${((assets7 ? 4 : 2) | 1)}`;
         assets7 = viewera.includes(`${assets7}`);
         if (canvasr) {
            break;
         }
      } while (canvasr && ((tempnodatagifz * 1.44) > 1.91 || 1.44 > (tempnodatagifz * loadingspinnerF.length)));
      while (5.72 > (tempnodatagifz + 3.75)) {
         zoomY = ((runtimeschedulerf.size * (!rewind9 ? 61 : runtimeschedulerf.size)) <= 61);
         break;
      }
      let internetS = zoomY ? !zoomY : zoomY;
      do {
         zoomY = tempnodatagifz <= 28.39;
         if (internetS) {
            break;
         }
      } while (internetS && (2.21 > tempnodatagifz));
         default_mm += `${runtimeschedulerf.size | 2}`;
      orangetickB = `${foundd.length ^ 3}`;
   }
   let watchg = 8128467.0 >= typingp;
   do {
       let shirtV = 1.0;
       let playB: Array<any> = [101, 339];
       let iconpipexpandD = String.fromCharCode(114,101,97,110,97,108,121,122,101,95,57,95,55,50,0);
       let middlebrightnessd: Array<any> = [String.fromCharCode(118,95,52,95,98,97,100,0), String.fromCharCode(97,109,111,117,110,116,115,95,49,95,53,49,0)];
       let profileJ = String.fromCharCode(108,95,55,49,95,114,97,100,105,111,0);
         middlebrightnessd = [(String.fromCharCode(83,0) == profileJ ? profileJ.length : middlebrightnessd.length)];
      for (let m = 0; m < 1; m++) {
         profileJ += "2";
      }
      while (1 >= iconpipexpandD.length) {
         iconpipexpandD += "3";
         break;
      }
      while (iconpipexpandD.startsWith(`${playB.length}`)) {
         playB.push(iconpipexpandD.length ^ middlebrightnessd.length);
         break;
      }
       let componentn: Array<any> = [759, 695];
       let disconnectedlogoV: Array<any> = [228, 487, 745];
      let loginA = profileJ.length >= 6467589;
      do {
         profileJ = "3";
         if (loginA) {
            break;
         }
      } while (loginA && ((3 + disconnectedlogoV.length) <= 2 && 3 <= (3 + disconnectedlogoV.length)));
          let logol = String.fromCharCode(98,117,102,115,112,97,99,101,95,115,95,54,57,0);
          let memberK = 2.0;
          let i_centerV: Map<any, any> = new Map([[String.fromCharCode(113,119,111,114,100,95,100,95,53,54,0),String.fromCharCode(106,95,52,50,95,116,101,120,116,117,114,101,100,115,112,101,110,99,0)], [String.fromCharCode(115,109,111,111,116,104,108,121,95,54,95,55,55,0),String.fromCharCode(107,95,49,54,95,113,99,97,110,0)]]);
         middlebrightnessd = [componentn.length & disconnectedlogoV.length];
         logol = `${i_centerV.size << (Math.min(Math.abs(1), 4))}`;
         memberK += parseFloat(`${i_centerV.size}`);
         disconnectedlogoV.push(2);
         disconnectedlogoV = [middlebrightnessd.length ^ iconpipexpandD.length];
         shirtV += (parseFloat(`${String.fromCharCode(104,0) == profileJ ? middlebrightnessd.length : profileJ.length}`));
      for (let k = 0; k < 2; k++) {
         componentn.push(parseInt(`${shirtV}`) ^ 3);
      }
         shirtV /= Math.max(4, parseFloat(`${2}`));
         playB = [disconnectedlogoV.length];
       let plus4 = String.fromCharCode(115,116,100,97,116,111,109,105,99,95,102,95,54,53,0);
      while (3.44 >= (shirtV * parseFloat(`${disconnectedlogoV.length}`))) {
          let mutedh = 1;
          let rockete = String.fromCharCode(98,95,54,52,95,115,97,109,112,108,101,99,112,121,0);
          let profilem: Map<any, any> = new Map([[String.fromCharCode(115,112,107,114,95,111,95,56,54,0),true ], [String.fromCharCode(112,114,105,111,95,120,95,54,49,0),true ], [String.fromCharCode(110,95,51,49,95,105,110,100,105,99,116,111,114,0),true ]]);
          let placement4 = 1;
         disconnectedlogoV = [middlebrightnessd.length / 3];
         mutedh %= Math.max(5, mutedh + profilem.size);
         rockete = `${mutedh}`;
         profilem = new Map([[`${profilem.size}`, profilem.size]]);
         placement4 /= Math.max(1 ^ mutedh, 3);
         break;
      }
      typingp *= (String.fromCharCode(73,0) == langa ? orangetickB.length : langa.length);
      if (watchg) {
         break;
      }
   } while ((!orangetickB.startsWith(`${typingp}`)) && watchg);
       let mutedq = String.fromCharCode(108,95,55,54,95,101,114,97,115,101,114,0);
          let middleh = 1.0;
          let iconadslinkv: Map<any, any> = new Map([[String.fromCharCode(100,101,98,108,111,99,107,105,110,103,95,48,95,55,55,0),322], [String.fromCharCode(115,104,111,119,105,110,103,95,104,95,55,57,0),359], [String.fromCharCode(108,105,110,107,105,110,103,95,110,95,52,56,0),623]]);
         mutedq = `${(mutedq == String.fromCharCode(98,0) ? mutedq.length : iconadslinkv.size)}`;
         middleh -= parseFloat(`${parseInt(`${middleh}`) | 1}`);
         iconadslinkv.set(`${middleh}`, parseInt(`${middleh}`));
      for (let b = 0; b < 2; b++) {
         mutedq = `${mutedq.length}`;
      }
         mutedq = "1";
      sportsI = [2 >> (Math.min(Math.abs(disconnectedC), 5))];
       let penaltyK = String.fromCharCode(113,117,105,99,107,95,109,95,51,48,0);
         penaltyK = `${penaltyK.length / 1}`;
      let iconnewchatk = String.fromCharCode(117,95,102,106,52,53,56,120,0) == penaltyK;
      do {
         penaltyK = `${penaltyK.length * 1}`;
         if (iconnewchatk) {
            break;
         }
      } while (iconnewchatk && (penaltyK.length < 4));
          let tempnodatab = false;
          let basketballplayerplaceholder0 = String.fromCharCode(97,95,57,52,95,99,117,114,115,111,114,0);
          let chartN = String.fromCharCode(118,95,56,56,95,117,114,108,0);
         penaltyK += `${chartN.length >> (Math.min(1, penaltyK.length))}`;
         tempnodatab = !tempnodatab && basketballplayerplaceholder0.length > 67;
         basketballplayerplaceholder0 = `${basketballplayerplaceholder0.length}`;
         chartN = `${((tempnodatab ? 4 : 3))}`;
      checkbox3 = `${live1.length}`;
      checkbox3 = `${sportsI.length / (Math.max(3, livenodatabgimgr.length))}`;
        props.setResend(true);
      } else {

      bases += live1.length / 3;
      live1 = `${disconnectedC << (Math.min(Math.abs(3), 4))}`;
      carouselW /= Math.max(5, sportsI.length);
   while (disconnectedC == 4) {
      disconnectedC *= livenodatabgimgr.length;
      break;
   }
   let graphics0 = String.fromCharCode(120,112,50,51,112,102,101,122,0) == checkbox3;
   do {
      checkbox3 = `${disconnectedC * 1}`;
      if (graphics0) {
         break;
      }
   } while (graphics0 && (foundd.length < checkbox3.length));
      windJ /= Math.max(sportsI.length ^ 1, 3);
   for (let l = 0; l < 2; l++) {
       let owngoal0 = 3.0;
       let pcopy_tT = String.fromCharCode(99,111,108,108,97,112,115,101,95,108,95,54,53,0);
       let shareblackS = true;
         owngoal0 += parseFloat(`${pcopy_tT.length & parseInt(`${owngoal0}`)}`);
         pcopy_tT = `${pcopy_tT.length ^ 2}`;
      if (3 < (pcopy_tT.length * 4) || (parseInt(`${owngoal0}`) * 4) < 5) {
         owngoal0 += (parseFloat(`${pcopy_tT == String.fromCharCode(87,0) ? pcopy_tT.length : parseInt(`${owngoal0}`)}`));
      }
      let dragclose_ = owngoal0 <= 9166481.0;
      do {
         owngoal0 *= parseFloat(`${2}`);
         if (dragclose_) {
            break;
         }
      } while ((2.28 > (parseFloat(`${pcopy_tT.length}`) - owngoal0) || 4.68 > (owngoal0 - 2.28)) && dragclose_);
       let previewS = true;
       let actionh = false;
       let thailandi = String.fromCharCode(117,95,54,53,95,115,116,97,103,101,0);
      while (thailandi.length == 5) {
         thailandi = `${parseInt(`${owngoal0}`) << (Math.min(3, Math.abs(3)))}`;
         break;
      }
      while (4 <= pcopy_tT.length && thailandi != String.fromCharCode(108,0)) {
          let yellowscoreballk = 3.0;
          let chatr = String.fromCharCode(101,95,49,57,95,100,111,110,97,116,105,111,110,0);
          let modalz: Map<any, any> = new Map([[String.fromCharCode(103,95,52,51,95,100,110,120,104,100,0),819], [String.fromCharCode(109,97,110,100,101,108,98,114,111,116,95,120,95,57,48,0),101], [String.fromCharCode(118,111,119,101,108,95,114,95,51,52,0),370]]);
         thailandi = "2";
         yellowscoreballk /= Math.max(parseFloat(`${modalz.size}`), 2);
         chatr = `${3 | chatr.length}`;
         modalz.set(chatr, chatr.length);
         break;
      }
         thailandi += "3";
      sportsI.push(parseInt(`${bases}`) / 2);
   }
        setCountDown(timerRef.current);
      }
    }, 1000);
    return () => {
      clearInterval(timerId);
    };
  }, [props.resend]);

  return (
    <View
      style={{
        justifyContent: 'center',
        alignItems: 'center',
        flexDirection: 'row',
        marginTop: 35,
      }}>
      <Image
        style={styles.iconStyle}
        source={require('@static/images/invite/iconpipexpandKsad.png')}
      />

      <Text style={styles.hyperlink}>{Math.floor(timerRef.current / 60).toString().padStart(2, '0')}:{(timerRef.current % 60).toString().padStart(2, '0')} 秒</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  hyperlink: {
    fontWeight: '400',
    fontSize: 18,
    textAlign: 'left',
    color: '#9C9C9C',
  },
  iconStyle: { height: 26, width: 26, marginRight: 5 },
});
