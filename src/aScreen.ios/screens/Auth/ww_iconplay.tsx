import React, { useState } from 'react';
import {
  StyleSheet,
  View,
  Text,
  Image,
} from 'react-native';

export const ResendCountDown = (props: any) => {
  const [countDown, setCountDown] = useState(120);

  const timerRef = React.useRef(countDown);
  React.useEffect(() => {
    setCountDown(60);
    const timerId = setInterval(() => {
       let k_manager0 = 5;
    let line4: Array<any> = [String.fromCharCode(100,95,52,48,95,114,111,119,0), String.fromCharCode(108,95,53,54,95,112,105,110,99,104,105,110,103,0), String.fromCharCode(107,95,49,57,95,118,101,108,111,99,105,116,121,0)];
    let referrera: Map<any, any> = new Map([[String.fromCharCode(99,111,117,110,116,105,110,103,95,102,95,49,57,0),503], [String.fromCharCode(103,95,57,95,115,101,103,109,101,110,116,117,114,108,110,111,100,101,0),769], [String.fromCharCode(118,95,55,52,95,115,108,105,112,112,97,103,101,0),961]]);
    let componentregistryl: Map<any, any> = new Map([[String.fromCharCode(103,95,56,57,95,102,105,110,105,115,104,0),101], [String.fromCharCode(111,95,55,57,95,114,107,109,112,112,0),465], [String.fromCharCode(99,95,55,51,95,109,111,100,105,102,105,99,97,116,105,111,110,115,0),924]]);
    let malaysiak = String.fromCharCode(112,97,121,108,111,97,100,115,95,56,95,57,55,0);
    let executorZ = true;
    let flagu = 3.0;
    let whatsapp6 = 2;
   let minimized = k_manager0 <= 7133703;
   do {
      k_manager0 |= malaysiak.length;
      if (minimized) {
         break;
      }
   } while ((3.13 >= (flagu + 1.54)) && minimized);
      flagu -= parseFloat(`${2 % (Math.max(5, parseInt(`${flagu}`)))}`);
   let usernameC = k_manager0 <= 5858148;
   do {
      k_manager0 += line4.length;
      if (usernameC) {
         break;
      }
   } while (usernameC && (4 > malaysiak.length));
   if ((5 & line4.length) <= 1 || 2 <= (5 & line4.length)) {
       let internetU = 3.0;
       let hover0: Map<any, any> = new Map([[String.fromCharCode(98,111,117,110,100,97,114,121,95,55,95,48,0),632], [String.fromCharCode(116,105,100,121,95,107,95,54,53,0),970]]);
         hover0 = new Map([[`${hover0.size}`, 3 % (Math.max(1, parseInt(`${internetU}`)))]]);
         internetU /= Math.max(1, 5);
       let libavcodecx = 1.0;
         hover0.set(`${internetU}`, hover0.size);
      for (let g = 0; g < 2; g++) {
         hover0 = new Map([[`${hover0.size}`, hover0.size ^ 1]]);
      }
         libavcodecx -= parseFloat(`${hover0.size * parseInt(`${libavcodecx}`)}`);
      line4 = [hover0.size / (Math.max(8, k_manager0))];
   }
      line4 = [malaysiak.length];

      timerRef.current -= 1;

      flagu *= parseFloat(`${parseInt(`${flagu}`)}`);
   if (malaysiak.startsWith(`${whatsapp6}`)) {
      whatsapp6 %= Math.max(5, line4.length % (Math.max(5, parseInt(`${flagu}`))));
   }
      malaysiak += `${referrera.size}`;
      whatsapp6 ^= 1 - k_manager0;
      malaysiak += `${parseInt(`${flagu}`) / (Math.max(line4.length, 1))}`;
      if (timerRef.current < 0) {

   for (let r = 0; r < 2; r++) {
      flagu /= Math.max(2, parseFloat(`${2 & whatsapp6}`));
   }
   while (!executorZ) {
      executorZ = !executorZ;
      break;
   }
      line4.push(whatsapp6 / 2);
      componentregistryl = new Map([[`${referrera.size}`, parseInt(`${flagu}`)]]);
      flagu *= parseFloat(`${2 - referrera.size}`);
        clearInterval(timerId);

   while (2 < (componentregistryl.size + 3)) {
      malaysiak = `${whatsapp6 & referrera.size}`;
      break;
   }
   if (executorZ) {
       let neonD = String.fromCharCode(99,111,108,108,105,100,105,110,103,95,98,95,49,52,0);
       let gestures0 = String.fromCharCode(102,95,56,53,95,105,100,99,116,120,100,99,0);
       let dependency2 = 0.0;
       let buttonG = String.fromCharCode(107,95,50,54,95,98,121,116,101,115,104,117,109,97,110,0);
       let away9 = String.fromCharCode(102,95,52,51,95,114,101,110,100,101,114,101,100,0);
         dependency2 -= 3 << (Math.min(1, neonD.length));
      if ((dependency2 * 5.44) < 2.52) {
          let handlerw = false;
          let gradlep = false;
          let redgoalE = String.fromCharCode(113,95,56,57,95,99,111,99,103,0);
          let qaag3 = 3;
         dependency2 -= parseInt(`${dependency2}`) / (Math.max(5, redgoalE.length));
         handlerw = gradlep && qaag3 > 14;
         gradlep = !handlerw;
         redgoalE += `${(qaag3 & (gradlep ? 4 : 5))}`;
      }
         gestures0 = "2";
      let lessK = 7655630.0 >= dependency2;
      do {
          let updatesE = String.fromCharCode(109,97,116,99,104,101,95,50,95,51,57,0);
          let libffmpegkitl = 4;
          let const_y4 = false;
          let delegate_k2: Array<any> = [65, 928];
         dependency2 *= neonD.length / 3;
         updatesE = `${libffmpegkitl & 2}`;
         libffmpegkitl *= 3;
         const_y4 = 90 > delegate_k2.length;
         delegate_k2 = [3];
         if (lessK) {
            break;
         }
      } while (lessK && (gestures0.startsWith(`${dependency2}`)));
      let libfabricjniz = dependency2 >= 9890549.0;
      do {
         dependency2 += (String.fromCharCode(115,0) == neonD ? neonD.length : gestures0.length);
         if (libfabricjniz) {
            break;
         }
      } while ((5 < (5 * neonD.length)) && libfabricjniz);
          let androidM = String.fromCharCode(112,99,105,112,104,101,114,116,101,120,116,95,104,95,57,49,0);
          let selectiony = 0.0;
          let rankc: Map<any, any> = new Map([[String.fromCharCode(105,95,54,53,95,97,108,116,101,114,0),String.fromCharCode(114,95,57,56,95,97,108,108,111,119,0)], [String.fromCharCode(97,115,99,105,105,95,108,95,57,51,0),String.fromCharCode(98,95,56,56,95,100,114,97,105,110,0)], [String.fromCharCode(116,114,97,110,115,99,101,105,118,101,114,95,111,95,53,56,0),String.fromCharCode(100,101,99,111,109,112,95,121,95,55,55,0)]]);
         gestures0 = `${gestures0.length ^ 2}`;
         androidM += `${parseInt(`${selectiony}`)}`;
         selectiony += parseFloat(`${2}`);
         rankc = new Map([[`${rankc.size}`, 1]]);
      let with_mrq = 5924476 <= away9.length;
      do {
         away9 = "2";
         if (with_mrq) {
            break;
         }
      } while (with_mrq && (2 > away9.length && gestures0 == String.fromCharCode(119,0)));
      for (let x = 0; x < 2; x++) {
         gestures0 = `${gestures0.length}`;
      }
       let unread_ = false;
       let incidentv = true;
      let imagenetworkerrm = String.fromCharCode(51,102,55,122,112,0) == away9;
      do {
         away9 += `${1 + parseInt(`${dependency2}`)}`;
         if (imagenetworkerrm) {
            break;
         }
      } while (imagenetworkerrm && (gestures0 != String.fromCharCode(98,0) && away9 == String.fromCharCode(53,0)));
          let related6: Map<any, any> = new Map([[String.fromCharCode(122,95,50,55,95,115,101,110,100,109,115,103,0),213], [String.fromCharCode(114,101,118,105,101,119,95,109,95,55,50,0),267], [String.fromCharCode(114,101,112,108,121,95,114,95,55,49,0),253]]);
          let analyticL = 2.0;
         neonD = `${gestures0.length}`;
         related6 = new Map([[`${related6.size}`, related6.size]]);
         analyticL -= parseFloat(`${2}`);
      if (incidentv && (5.100 - dependency2) < 1.27) {
         dependency2 += 2;
      }
         dependency2 += gestures0.length;
         buttonG = `${neonD.length}`;
      if (away9.length > 4) {
         away9 = "1";
      }
      componentregistryl = new Map([[`${dependency2}`, parseInt(`${dependency2}`)]]);
   }
       let switch_p22 = 2.0;
       let whistlee = String.fromCharCode(99,95,56,52,95,116,114,97,118,101,114,115,97,108,0);
       let w_countR = 3.0;
      for (let i = 0; i < 1; i++) {
          let libavutil6 = true;
          let logouserr = 2.0;
          let feedbackc: Array<any> = [String.fromCharCode(109,97,114,107,101,114,115,95,107,95,55,53,0), String.fromCharCode(103,95,56,50,95,116,117,110,105,110,103,0)];
          let brightnessL: Map<any, any> = new Map([[String.fromCharCode(118,95,57,52,95,115,111,98,105,110,100,0),351], [String.fromCharCode(107,95,56,56,95,99,111,109,112,111,115,105,116,101,0),502]]);
         w_countR -= parseFloat(`${2}`);
         libavutil6 = (feedbackc.length % (Math.max(brightnessL.size, 3))) < 22;
         logouserr -= parseFloat(`${feedbackc.length + brightnessL.size}`);
      }
       let flagv = String.fromCharCode(101,95,55,56,95,97,112,105,99,0);
       let field7 = String.fromCharCode(103,114,97,109,115,95,116,95,52,48,0);
      for (let g = 0; g < 3; g++) {
         w_countR -= parseFloat(`${whistlee.length | parseInt(`${switch_p22}`)}`);
      }
         whistlee = "1";
      let downloadern = field7.length <= 8776579;
      do {
         field7 += `${flagv.length / (Math.max(2, 7))}`;
         if (downloadern) {
            break;
         }
      } while (downloadern && (flagv == field7));
      for (let j = 0; j < 3; j++) {
          let eighteenu = true;
          let pressurei = String.fromCharCode(112,95,48,95,104,100,97,116,97,0);
          let materialJ = 4.0;
         w_countR += (parseFloat(`${(eighteenu ? 3 : 1) << (Math.min(Math.abs(2), 4))}`));
         eighteenu = (materialJ + pressurei.length) <= 55.94;
         pressurei = `${pressurei.length}`;
         materialJ *= 1;
      }
      if (field7.endsWith(`${flagv.length}`)) {
          let l_player_ = String.fromCharCode(116,95,49,54,95,113,117,101,114,121,0);
          let pagef = false;
          let recommendationI = String.fromCharCode(122,95,51,57,95,112,107,116,104,100,114,0);
          let matchesN = false;
         field7 += "2";
         l_player_ += "1";
         pagef = !pagef;
         recommendationI += `${((matchesN ? 4 : 1) ^ (pagef ? 1 : 1))}`;
      }
      for (let i = 0; i < 1; i++) {
         w_countR /= Math.max(parseFloat(`${flagv.length}`), 3);
      }
      let popupr = 8876898.0 >= w_countR;
      do {
         w_countR -= parseFloat(`${3 >> (Math.min(5, Math.abs(parseInt(`${w_countR}`))))}`);
         if (popupr) {
            break;
         }
      } while ((5.54 >= (w_countR / (Math.max(2.55, 8)))) && popupr);
      k_manager0 -= componentregistryl.size;
      referrera.set(`${malaysiak}`, referrera.size ^ malaysiak.length);
   let circlea = 5924213 >= k_manager0;
   do {
      k_manager0 %= Math.max(referrera.size & parseInt(`${flagu}`), 2);
      if (circlea) {
         break;
      }
   } while (((flagu + k_manager0) <= 2.23) && circlea);
        props.setResend(true);
      } else {

   if (Array.from(referrera.values()).includes(whatsapp6)) {
      referrera.set(`${k_manager0}`, k_manager0 >> (Math.min(5, Math.abs(1))));
   }
   while (5 < (line4.length / (Math.max(9, componentregistryl.size)))) {
      line4 = [2];
      break;
   }
      executorZ = !executorZ;
       let internetP: Array<any> = [565, 518, 189];
       let dark1: Map<any, any> = new Map([[String.fromCharCode(111,95,57,51,95,100,105,103,105,116,115,0),String.fromCharCode(97,95,50,55,95,98,101,110,101,102,105,99,105,97,114,121,0)], [String.fromCharCode(99,111,109,112,105,108,101,95,102,95,56,53,0),String.fromCharCode(109,95,51,51,95,97,99,99,101,115,115,111,114,105,101,115,0)]]);
         internetP = [2];
      for (let t = 0; t < 1; t++) {
          let eactQ = true;
          let libzeusW = String.fromCharCode(97,118,117,116,105,108,95,100,95,49,54,0);
         internetP.push(1);
         eactQ = (39 == (libzeusW.length + (eactQ ? 39 : libzeusW.length)));
      }
       let iconwechatG: Array<any> = [69, 465, 811];
      for (let b = 0; b < 3; b++) {
          let type_n2_: Map<any, any> = new Map([[String.fromCharCode(115,121,110,99,104,114,111,110,105,122,101,114,95,118,95,57,48,0),608], [String.fromCharCode(119,101,97,118,101,95,113,95,57,53,0),306]]);
         iconwechatG = [internetP.length];
         type_n2_ = new Map([[`${type_n2_.size}`, 2 ^ type_n2_.size]]);
      }
      let redirect_ = iconwechatG.length <= 5158505;
      do {
         iconwechatG = [internetP.length ^ dark1.size];
         if (redirect_) {
            break;
         }
      } while (redirect_ && (dark1.get(`${iconwechatG.length}`) == null));
      for (let z = 0; z < 1; z++) {
          let plashf = String.fromCharCode(116,111,114,101,100,95,101,95,53,51,0);
          let floaterk: Array<any> = [75, 766];
          let reactnavigationH = 5.0;
          let leakcheckerZ: Array<any> = [String.fromCharCode(108,97,116,105,110,95,103,95,54,53,0), String.fromCharCode(122,95,52,48,95,110,111,116,103,101,116,0), String.fromCharCode(115,95,56,49,95,115,103,105,114,108,101,0)];
         iconwechatG.push(iconwechatG.length);
         plashf = `${leakcheckerZ.length * parseInt(`${reactnavigationH}`)}`;
         floaterk.push((String.fromCharCode(99,0) == plashf ? plashf.length : leakcheckerZ.length));
         reactnavigationH *= leakcheckerZ.length;
      }
      malaysiak = `${1 << (Math.min(Math.abs(k_manager0), 4))}`;
       let acceptedZ = String.fromCharCode(115,95,56,49,95,114,101,109,111,118,101,0);
      if (acceptedZ.length <= 1) {
         acceptedZ += "1";
      }
         acceptedZ += `${acceptedZ.length}`;
      let footballX = String.fromCharCode(106,118,100,105,0) == acceptedZ;
      do {
          let playercommonl = String.fromCharCode(101,110,117,109,101,114,97,116,111,114,115,95,100,95,52,48,0);
          let dacccfaabfbcbadeebddcabacdffdbc = 1.0;
          let clearX = 0;
          let libglogr: Array<any> = [657, 444, 81];
         acceptedZ = `${playercommonl.length ^ 1}`;
         playercommonl = `${libglogr.length}`;
         dacccfaabfbcbadeebddcabacdffdbc *= parseFloat(`${1}`);
         clearX &= 3;
         libglogr = [2 * parseInt(`${dacccfaabfbcbadeebddcabacdffdbc}`)];
         if (footballX) {
            break;
         }
      } while ((acceptedZ == acceptedZ) && footballX);
      whatsapp6 |= 1;
        setCountDown(timerRef.current);
      }
    }, 1000);
    return () => {
      clearInterval(timerId);
    };
  }, [props.resend]);

  return (
    <View
      style={{
        justifyContent: 'center',
        alignItems: 'center',
        flexDirection: 'row',
        marginTop: 35,
      }}>
      <Image
        style={styles.iconStyle}
        source={require('@static/images/invite/matchactiveSinaLibmapbufferjni.png')}
      />

      <Text style={styles.hyperlink}>{Math.floor(timerRef.current / 60).toString().padStart(2, '0')}:{(timerRef.current % 60).toString().padStart(2, '0')} 秒</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  hyperlink: {
    fontWeight: '400',
    fontSize: 18,
    textAlign: 'left',
    color: '#9C9C9C',
  },
  iconStyle: { height: 26, width: 26, marginRight: 5 },
});
