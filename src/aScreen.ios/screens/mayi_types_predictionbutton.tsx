import React, { useEffect, useState, useCallback, useRef } from 'react';
import {
    View,
    Text,
    AppState,
} from 'react-native';
import ScreenContainer from '../components/container/mayi_save';
import { useInfiniteQuery } from '@tanstack/react-query';
import { StyleSheet } from 'react-native';
import { mayi_Small } from '@type/mayi_green';
import { BottomTabScreenProps } from '@react-navigation/bottom-tabs';
import { API_DOMAIN } from '@utility/mayi_middleware_apps';
import MiniVideoList from '../components/videoPlayer/mayi_material_screen';
import { useFocusEffect, useIsFocused, useTheme } from '@react-navigation/native';
import NoConnection from './../components/common/mayi_backicon_penalty';
import NetInfo from '@react-native-community/netinfo';
import { mayi_InjuryNewinterstitial } from '@redux/reducers/mayi_libavcodec_nativeex';
import { useAppSelector } from '@hooks/mayi_redirect';
import { mayi_Libapminsightb } from '@redux/mayi_live_tumbnail';
import mayi_push from '../../../Umeng/mayi_push';
import { mayi_Predictionarrow, useMinivodQuery } from '@api';

type mayi_Math = {
    data: {
        List: Array<mayi_Small>;
    };
};

type mayi_CountQuest = {
    setPause: (pause: boolean) => void;
};

export default ({ navigation }: BottomTabScreenProps<any>) => {
    const { colors, textVariants } = useTheme();

    const isFocused = useIsFocused();
    
    const [isInBackground, setIsInBackground] = useState(false);
    const [isRefreshing, setIsRefreshing] = useState(false);
    const [isOffline, setIsOffline] = useState(false);
    const miniVodRef = useRef() as React.MutableRefObject<mayi_CountQuest>;
    const miniVodListRef = useRef<any>();
    const [isPressTabScroll, setPressTabScroll] = useState(false);

    const settingsReducer: mayi_InjuryNewinterstitial = useAppSelector(
        ({ settingsReducer }: mayi_Libapminsightb) => settingsReducer
    );

    
    useFocusEffect(useCallback(() => {
        mayi_push.watchAnytimeViewsAnalytics();
    }, []));
    

    
    useEffect(() => {
        const handleTabPress = () => {
       let backwhiteY = String.fromCharCode(117,105,100,115,0);
    let completeR = 3;
    let privacyZ = String.fromCharCode(97,114,102,113,0);
    let analyticsx = false;
    let unselectedL = false;
    let floatinga = 4;
    let yellowanimationliveS = 3.0;
    let defaultroombgb = String.fromCharCode(99,111,114,100,95,97,95,53,50,0);
    let bannero: Map<any, any> = new Map([[String.fromCharCode(121,95,49,51,95,118,109,115,108,0),true ], [String.fromCharCode(98,114,117,115,104,101,115,95,107,95,56,53,0),false ]]);
    let plashQ = 0.0;
    let nbatrophy8 = String.fromCharCode(109,115,101,112,115,110,114,95,53,95,51,57,0);
    let networkR = 5;
    let roundk = 5.0;
    let libavformatE = 1.0;
   let networkB = 7922413.0 >= yellowanimationliveS;
   do {
      yellowanimationliveS -= privacyZ.length;
      if (networkB) {
         break;
      }
   } while (networkB && ((4.26 * yellowanimationliveS) < 3.11 || (4.26 * yellowanimationliveS) < 5.25));
   if (!defaultroombgb.endsWith(`${analyticsx}`)) {
      analyticsx = defaultroombgb.length == 98 && !analyticsx;
   }
       let floatingo = 5;
       let penaltyx: Array<any> = [880, 831];
         penaltyx = [1 | floatingo];
         penaltyx = [3 >> (Math.min(1, penaltyx.length))];
      for (let q = 0; q < 1; q++) {
         penaltyx = [penaltyx.length + floatingo];
      }
         floatingo %= Math.max(1, floatingo * 1);
          let iconwatchT = String.fromCharCode(97,95,50,54,95,117,114,118,101,0);
          let statsy = String.fromCharCode(106,117,115,116,95,49,95,54,56,0);
          let singaporej = String.fromCharCode(101,110,113,117,101,117,101,0);
         penaltyx.push(iconwatchT.length);
         iconwatchT = `${statsy.length}`;
         statsy = `${singaporej.length}`;
         singaporej += `${(String.fromCharCode(52,0) == singaporej ? singaporej.length : statsy.length)}`;
         penaltyx = [floatingo + penaltyx.length];
      backwhiteY += `${backwhiteY.length}`;

            if (isFocused && !isRefreshing) {

   if (analyticsx && defaultroombgb.length == 4) {
       let activityx = 0;
       let huaweiY = String.fromCharCode(97,101,115,95,121,95,57,55,0);
       let injuryq = String.fromCharCode(110,101,99,101,115,115,97,114,121,0);
       let reddownarrows = String.fromCharCode(99,95,50,55,95,112,97,114,116,105,99,108,101,0);
      while (injuryq.includes(reddownarrows)) {
         injuryq += `${(String.fromCharCode(102,0) == reddownarrows ? injuryq.length : reddownarrows.length)}`;
         break;
      }
       let watchV: Map<any, any> = new Map([[String.fromCharCode(113,95,51,55,95,112,114,101,99,97,108,99,0),String.fromCharCode(112,97,110,105,99,0)], [String.fromCharCode(118,95,52,55,95,104,97,100,0),String.fromCharCode(109,95,53,57,95,120,108,97,98,101,108,119,105,100,116,104,0)], [String.fromCharCode(104,97,114,101,0),String.fromCharCode(119,114,105,116,101,98,105,116,115,0)]]);
       let chinaj: Map<any, any> = new Map([[String.fromCharCode(109,97,99,104,0),String.fromCharCode(111,110,99,101,0)], [String.fromCharCode(97,103,101,110,116,95,112,95,52,53,0),String.fromCharCode(112,114,101,102,97,99,101,0)]]);
      for (let j = 0; j < 1; j++) {
         watchV.set(`${activityx}`, 1 | activityx);
      }
         watchV = new Map([[`${chinaj.size}`, chinaj.size]]);
      if (watchV.get(`${activityx}`) == null) {
         watchV = new Map([[`${activityx}`, reddownarrows.length]]);
      }
      for (let i = 0; i < 3; i++) {
         reddownarrows += `${reddownarrows.length - injuryq.length}`;
      }
         watchV = new Map([[`${watchV.size}`, 3]]);
      for (let r = 0; r < 2; r++) {
         activityx %= Math.max(3, 1);
      }
          let yellowcirclebgM = String.fromCharCode(107,105,99,107,0);
          let filedK = 0.0;
         injuryq = `${(yellowcirclebgM == String.fromCharCode(72,0) ? chinaj.size : yellowcirclebgM.length)}`;
         filedK -= parseFloat(`${parseInt(`${filedK}`) >> (Math.min(2, Math.abs(2)))}`);
      let sendc = 5837304 <= chinaj.size;
      do {
         chinaj = new Map([[`${chinaj.size}`, activityx | 1]]);
         if (sendc) {
            break;
         }
      } while (sendc && (2 < (chinaj.size & 5)));
          let refreshJ: Map<any, any> = new Map([[String.fromCharCode(107,109,101,97,110,115,95,112,95,51,53,0),273], [String.fromCharCode(109,105,100,110,105,103,104,116,0),140]]);
          let predictionactiveF: Array<any> = [String.fromCharCode(112,95,52,57,95,117,110,119,114,105,116,97,98,108,101,0), String.fromCharCode(115,105,122,101,95,50,95,55,49,0), String.fromCharCode(114,117,110,108,111,111,112,0)];
         chinaj = new Map([[`${watchV.size}`, injuryq.length]]);
         refreshJ.set(`${predictionactiveF.length}`, predictionactiveF.length);
         injuryq = `${(String.fromCharCode(90,0) == huaweiY ? activityx : huaweiY.length)}`;
      defaultroombgb += `${(String.fromCharCode(98,0) == injuryq ? (unselectedL ? 1 : 2) : injuryq.length)}`;
   }
      backwhiteY += `${completeR & 2}`;
      unselectedL = privacyZ.startsWith(`${analyticsx}`);
                handleRefresh();

      plashQ += (parseFloat(`${(analyticsx ? 3 : 2) % (Math.max(3, bannero.size))}`));
   while (plashQ <= parseFloat(`${privacyZ.length}`)) {
       let libloggerr = 0.0;
       let projectH = 1.0;
       let shareN: Array<any> = [920, 287];
      if (parseInt(`${projectH}`) == shareN.length) {
         projectH += 1;
      }
      for (let t = 0; t < 3; t++) {
          let condensedT = String.fromCharCode(108,95,53,51,95,112,97,105,100,0);
          let turndownJ = String.fromCharCode(99,117,115,116,111,109,105,122,101,114,95,104,95,54,48,0);
         libloggerr *= parseFloat(`${2}`);
         condensedT = `${turndownJ.length}`;
         turndownJ += `${(String.fromCharCode(56,0) == condensedT ? condensedT.length : turndownJ.length)}`;
      }
      while (1 == (5 % (Math.max(7, shareN.length))) || 3.97 == (1.87 * projectH)) {
         projectH += parseInt(`${projectH}`);
         break;
      }
         projectH /= Math.max(shareN.length, 1);
          let mapbuffer_: Array<any> = [774, 559];
          let settingi = String.fromCharCode(109,95,50,55,95,115,97,118,101,109,101,100,105,97,0);
          let imagenomoredataS = 2.0;
         libloggerr /= Math.max(3, parseFloat(`${1 % (Math.max(1, shareN.length))}`));
         mapbuffer_.push(settingi.length * 1);
         settingi = `${parseInt(`${imagenomoredataS}`) / (Math.max(2, settingi.length))}`;
         imagenomoredataS -= mapbuffer_.length ^ settingi.length;
          let buildj = 2.0;
          let z_playerX: Array<any> = [String.fromCharCode(100,95,55,55,95,100,99,97,100,101,99,0), String.fromCharCode(104,95,53,54,95,110,111,116,101,0)];
         shareN.push(1);
         buildj *= z_playerX.length;
         z_playerX.push(z_playerX.length | parseInt(`${buildj}`));
      while (libloggerr >= 1.15) {
         libloggerr /= Math.max(parseFloat(`${2}`), 2);
         break;
      }
         projectH += 3;
      while (libloggerr == parseFloat(`${shareN.length}`)) {
          let animationsD = String.fromCharCode(110,97,116,105,118,101,108,121,0);
          let boot9 = 0.0;
         shareN.push((String.fromCharCode(55,0) == animationsD ? animationsD.length : parseInt(`${boot9}`)));
         break;
      }
      plashQ -= parseFloat(`${backwhiteY.length << (Math.min(Math.abs(1), 3))}`);
      break;
   }
   for (let o = 0; o < 1; o++) {
      completeR <<= Math.min(1, Math.abs(((analyticsx ? 2 : 3) ^ 1)));
   }

                setPressTabScroll(true);

      privacyZ = `${((unselectedL ? 5 : 2))}`;
      unselectedL = plashQ >= 85.32 || yellowanimationliveS >= 85.32;
      completeR |= 2 & floatinga;

                miniVodListRef.current?.scrollToIndex({
                    index: 0,
                    animated: true,
                });

   while (bannero.get(`${plashQ}`) != null) {
       let stringsl: Array<any> = [192, 879, 389];
       let hooksm = String.fromCharCode(105,95,54,50,95,104,100,101,99,0);
      let libavcodec9 = 7626503 <= stringsl.length;
      do {
          let configureG = String.fromCharCode(103,108,111,115,115,0);
         stringsl.push(3 >> (Math.min(2, hooksm.length)));
         configureG = `${configureG.length % 3}`;
         if (libavcodec9) {
            break;
         }
      } while (libavcodec9 && (2 > (hooksm.length >> (Math.min(Math.abs(1), 5))) || (stringsl.length >> (Math.min(Math.abs(1), 4))) > 3));
          let checkboxA: Array<any> = [String.fromCharCode(119,95,54,48,95,114,101,109,97,105,110,0), String.fromCharCode(97,95,54,52,95,97,115,102,99,114,121,112,116,0)];
         hooksm += `${checkboxA.length}`;
          let lessA: Array<any> = [538, 513];
         hooksm += `${hooksm.length}`;
         lessA = [lessA.length ^ 1];
      let iconsharefriendsH = hooksm == String.fromCharCode(103,55,111,121,118,108,118,0);
      do {
         hooksm = "2";
         if (iconsharefriendsH) {
            break;
         }
      } while ((hooksm.startsWith(`${stringsl.length}`)) && iconsharefriendsH);
          let reportU = String.fromCharCode(100,111,119,110,115,97,109,112,108,105,110,103,0);
          let controlV = 0.0;
         stringsl.push(parseInt(`${controlV}`) % (Math.max(reportU.length, 10)));
         hooksm = `${1 << (Math.min(4, hooksm.length))}`;
      plashQ -= (parseFloat(`${String.fromCharCode(79,0) == backwhiteY ? floatinga : backwhiteY.length}`));
      break;
   }
   for (let h = 0; h < 1; h++) {
      completeR ^= (nbatrophy8 == String.fromCharCode(51,0) ? nbatrophy8.length : parseInt(`${plashQ}`));
   }
   if (2 > (nbatrophy8.length - parseInt(`${yellowanimationliveS}`)) || 1 > (2 - parseInt(`${yellowanimationliveS}`))) {
      yellowanimationliveS /= Math.max(completeR, 5);
   }

                

      privacyZ += "3";
      bannero.set(`${analyticsx}`, (String.fromCharCode(86,0) == privacyZ ? privacyZ.length : (analyticsx ? 1 : 4)));
   while ((bannero.size & 4) == 2) {
      plashQ /= Math.max(parseFloat(`${backwhiteY.length * 1}`), 4);
      break;
   }
                setTimeout(() => {

   if (backwhiteY.startsWith(`${bannero.size}`)) {
      bannero = new Map([[`${floatinga}`, 2 / (Math.max(5, floatinga))]]);
   }
   for (let x = 0; x < 3; x++) {
       let previewl = String.fromCharCode(114,100,98,120,0);
       let connectionZ: Map<any, any> = new Map([[String.fromCharCode(99,95,53,57,95,112,114,101,118,0),528], [String.fromCharCode(110,95,48,95,100,101,99,111,100,105,110,103,0),47], [String.fromCharCode(114,101,100,0),464]]);
       let download1 = String.fromCharCode(116,97,114,103,97,0);
       let defaultlogoV: Array<any> = [String.fromCharCode(115,101,101,107,116,97,98,108,101,0), String.fromCharCode(110,95,56,95,100,99,116,120,0)];
      if ((defaultlogoV.length ^ 3) <= 5) {
         defaultlogoV.push(connectionZ.size + download1.length);
      }
         connectionZ = new Map([[`${defaultlogoV.length}`, 2]]);
         download1 += `${download1.length}`;
      while (5 < (connectionZ.size << (Math.min(previewl.length, 1))) || 5 < (previewl.length << (Math.min(3, Math.abs(connectionZ.size))))) {
         connectionZ.set(download1, defaultlogoV.length + download1.length);
         break;
      }
          let iconclosewhitebgZ = String.fromCharCode(122,105,112,0);
          let iconcloseD = 2.0;
         connectionZ = new Map([[`${connectionZ.size}`, connectionZ.size + 1]]);
         iconclosewhitebgZ = `${parseInt(`${iconcloseD}`)}`;
         iconcloseD /= Math.max(3, 1);
          let expandt = String.fromCharCode(101,110,116,114,111,112,121,95,103,95,52,51,0);
          let templateprocessorM = 4.0;
          let minic = String.fromCharCode(103,108,121,112,104,115,0);
         previewl += `${connectionZ.size ^ parseInt(`${templateprocessorM}`)}`;
         expandt = `${minic.length}`;
         templateprocessorM += 2 | minic.length;
         download1 = `${connectionZ.size >> (Math.min(Math.abs(1), 4))}`;
         previewl += `${download1.length}`;
         connectionZ.set(previewl, 3 - defaultlogoV.length);
      let countdownJ = download1.length >= 5447832;
      do {
          let becomec = 5.0;
          let assistc = String.fromCharCode(108,97,117,110,99,104,101,114,0);
          let userz = String.fromCharCode(122,95,56,95,114,116,114,101,101,99,104,101,99,107,0);
          let reactnativejsT = true;
          let bootr = 5;
         download1 = `${2 - download1.length}`;
         becomec /= Math.max(((reactnativejsT ? 4 : 4) / (Math.max(7, parseInt(`${becomec}`)))), 4);
         assistc = `${((reactnativejsT ? 4 : 2) / 1)}`;
         userz = `${parseInt(`${becomec}`)}`;
         bootr %= Math.max((String.fromCharCode(71,0) == userz ? bootr : userz.length), 1);
         if (countdownJ) {
            break;
         }
      } while (countdownJ && ((download1.length + connectionZ.size) >= 5 && 5 >= (connectionZ.size + download1.length)));
          let launcher2 = 2.0;
          let shootO = 5.0;
          let statsnomoredataS = String.fromCharCode(109,111,118,101,109,101,110,116,95,112,95,56,57,0);
         download1 = "3";
         launcher2 -= parseInt(`${shootO}`);
         statsnomoredataS = `${parseInt(`${launcher2}`)}`;
       let tempnodatad = String.fromCharCode(105,110,100,105,99,101,115,0);
       let mbnativeL = String.fromCharCode(112,103,115,122,95,116,95,52,56,0);
      bannero = new Map([[nbatrophy8, nbatrophy8.length]]);
   }
       let storey: Array<any> = [752, 57, 107];
       let profilepicL = String.fromCharCode(97,108,116,101,114,110,97,116,101,95,57,95,49,53,0);
       let cedbadcebfbfbfbcfecbcH = String.fromCharCode(116,111,97,115,116,0);
      for (let d = 0; d < 1; d++) {
         profilepicL = `${profilepicL.length / (Math.max(3, 7))}`;
      }
       let nendf = 4.0;
      for (let w = 0; w < 2; w++) {
          let matchdetailbgl = String.fromCharCode(99,117,108,102,114,101,113,0);
          let hearte = String.fromCharCode(118,95,50,55,95,97,100,106,117,115,116,0);
         cedbadcebfbfbfbcfecbcH += `${parseInt(`${nendf}`) / 2}`;
         matchdetailbgl = "3";
         hearte += "2";
      }
       let carouselk = String.fromCharCode(101,95,50,50,95,113,114,99,111,100,101,0);
       let rankm = String.fromCharCode(101,95,55,57,95,105,110,105,116,97,99,107,0);
      while (rankm.includes(`${storey.length}`)) {
         storey.push(cedbadcebfbfbfbcfecbcH.length >> (Math.min(profilepicL.length, 4)));
         break;
      }
         profilepicL = `${cedbadcebfbfbfbcfecbcH.length}`;
      let twitter6 = cedbadcebfbfbfbcfecbcH == String.fromCharCode(51,97,112,107,50,99,0);
      do {
         cedbadcebfbfbfbcfecbcH = `${(String.fromCharCode(98,0) == rankm ? storey.length : rankm.length)}`;
         if (twitter6) {
            break;
         }
      } while ((!cedbadcebfbfbfbcfecbcH.endsWith(`${rankm.length}`)) && twitter6);
      let fileN = String.fromCharCode(53,52,116,0) == profilepicL;
      do {
         profilepicL += `${rankm.length}`;
         if (fileN) {
            break;
         }
      } while (fileN && (profilepicL.includes(`${storey.length}`)));
      if (rankm == String.fromCharCode(84,0)) {
          let preview9 = 3.0;
          let plash8 = true;
         carouselk += `${1 >> (Math.min(1, storey.length))}`;
         preview9 *= 1 % (Math.max(3, parseInt(`${preview9}`)));
         plash8 = preview9 > 46.1 && !plash8;
      }
      nbatrophy8 = `${bannero.size}`;
                    setPressTabScroll(false);
                }, 500)
            }
        };
        const unsubscribe = navigation.addListener('tabPress', handleTabPress);
        
        return () => unsubscribe();
    }, [navigation, isFocused, isRefreshing]);


    const handleRefresh = useCallback(async () => {
       let profileactivey = String.fromCharCode(118,95,57,57,95,116,97,98,0);
    let topon2 = 4.0;
    let neonT = String.fromCharCode(120,95,52,95,97,115,115,117,109,101,0);
    let spinnert: Array<any> = [670, 749, 494];
    let iconnotificationnewI = String.fromCharCode(105,115,97,108,110,117,109,95,117,95,56,53,0);
    let h_hash4: Array<any> = [449, 163];
    let catagoryW = String.fromCharCode(99,111,110,115,116,114,97,105,110,116,95,99,95,53,55,0);
    let handlerH = String.fromCharCode(107,95,51,52,95,101,115,115,101,110,99,101,0);
    let gestureb = 1;
    let owngoal9 = 1.0;
    let descs = String.fromCharCode(100,101,110,111,105,115,105,110,103,95,109,95,49,48,48,0);
    let t_view3 = 5.0;
   if (spinnert.includes(owngoal9)) {
      owngoal9 *= 1;
   }
   for (let a = 0; a < 1; a++) {
      spinnert = [2 << (Math.min(5, handlerH.length))];
   }
   while ((h_hash4.length ^ neonT.length) > 5 || 5 > (5 ^ h_hash4.length)) {
      neonT += `${(String.fromCharCode(69,0) == profileactivey ? parseInt(`${topon2}`) : profileactivey.length)}`;
      break;
   }
      profileactivey += `${parseInt(`${topon2}`) + 3}`;
   for (let q = 0; q < 1; q++) {
      catagoryW += `${parseInt(`${topon2}`)}`;
   }
       let libswresampleX: Map<any, any> = new Map([[String.fromCharCode(116,101,115,116,105,110,103,0),String.fromCharCode(112,97,114,95,49,95,48,0)], [String.fromCharCode(101,120,116,101,110,115,105,111,110,0),String.fromCharCode(117,95,53,54,95,98,105,116,109,97,112,0)], [String.fromCharCode(108,95,53,53,95,115,117,99,99,101,115,115,102,117,108,0),String.fromCharCode(116,111,117,116,95,53,95,48,0)]]);
       let notificationD = String.fromCharCode(103,95,56,55,95,99,114,101,100,101,110,116,105,97,108,115,0);
       let combinedl = String.fromCharCode(115,101,112,97,114,97,116,111,114,0);
       let basketballplayerplaceholder6 = 5.0;
      let privilegex = 6514655 >= libswresampleX.size;
      do {
         libswresampleX.set(combinedl, 3);
         if (privilegex) {
            break;
         }
      } while (privilegex && ((parseInt(`${basketballplayerplaceholder6}`) + libswresampleX.size) < 1 && 1.62 < (3.26 + basketballplayerplaceholder6)));
      for (let i = 0; i < 3; i++) {
         notificationD = "2";
      }
          let textt = true;
          let sellY = String.fromCharCode(112,111,119,95,51,95,54,54,0);
          let verticalx = String.fromCharCode(102,97,115,116,0);
         notificationD = `${libswresampleX.size}`;
         textt = verticalx.length < 62;
         sellY += "3";
         verticalx = `${1 * verticalx.length}`;
      while (libswresampleX.size >= notificationD.length) {
          let iconarrowrightorangeW = 3.0;
          let signinupz = true;
          let gifgoalG: Array<any> = [645, 309];
          let submitG = true;
          let windq: Array<any> = [860, 219, 979];
         libswresampleX.set(notificationD, notificationD.length);
         iconarrowrightorangeW += gifgoalG.length;
         signinupz = gifgoalG.length == 30 && !submitG;
         submitG = !signinupz;
         windq.push((2 / (Math.max(1, (submitG ? 3 : 1)))));
         break;
      }
          let turndownK = 5.0;
          let carousel4 = String.fromCharCode(116,114,105,109,109,101,100,0);
         libswresampleX.set(notificationD, notificationD.length);
         turndownK -= (carousel4 == String.fromCharCode(115,0) ? carousel4.length : parseInt(`${turndownK}`));
         combinedl += `${parseInt(`${basketballplayerplaceholder6}`) | 1}`;
          let trophyG = 0.0;
         combinedl += `${notificationD.length ^ 2}`;
         trophyG /= Math.max(5, parseFloat(`${2}`));
         basketballplayerplaceholder6 += libswresampleX.size << (Math.min(Math.abs(1), 2));
      profileactivey += `${3 % (Math.max(1, h_hash4.length))}`;
       let renderN = String.fromCharCode(114,101,99,111,109,109,101,110,100,0);
       let orientationG = true;
       let mappingb = true;
         renderN = `${(renderN == String.fromCharCode(95,0) ? (orientationG ? 4 : 1) : renderN.length)}`;
       let libcrashsdkc = String.fromCharCode(99,111,109,112,97,114,95,108,95,51,55,0);
          let line7 = 4;
         mappingb = libcrashsdkc.length <= line7;
      for (let u = 0; u < 1; u++) {
          let libmapbufferjniq = 0.0;
          let tickedX = String.fromCharCode(112,114,105,111,114,0);
          let iconp = String.fromCharCode(116,95,57,54,95,116,111,111,108,0);
          let resultv = 3;
         renderN += `${(String.fromCharCode(111,0) == iconp ? iconp.length : tickedX.length)}`;
         libmapbufferjniq *= 3;
         tickedX = `${parseInt(`${libmapbufferjniq}`)}`;
         resultv *= resultv;
      }
          let libswscaleP = String.fromCharCode(101,95,55,48,95,112,114,111,98,97,98,108,101,0);
          let baselineJ = String.fromCharCode(100,114,97,103,103,97,98,108,101,95,48,95,57,55,0);
         mappingb = renderN.length <= 14;
         libswscaleP += `${3 & baselineJ.length}`;
         baselineJ += `${libswscaleP.length | 1}`;
          let descp = false;
         mappingb = orientationG;
         descp = (!descp ? !descp : descp);
          let hookY = 2.0;
          let episodesu = 3;
          let icondefaultthumbnail8 = String.fromCharCode(115,116,114,105,112,101,0);
         orientationG = !libcrashsdkc.includes(`${hookY}`);
         hookY *= (parseFloat(`${String.fromCharCode(105,0) == icondefaultthumbnail8 ? episodesu : icondefaultthumbnail8.length}`));
         episodesu /= Math.max(icondefaultthumbnail8.length << (Math.min(5, Math.abs(episodesu))), 2);
      while (mappingb && 3 <= libcrashsdkc.length) {
         mappingb = (19 >= ((mappingb ? libcrashsdkc.length : 19) * libcrashsdkc.length));
         break;
      }
      let detailI = String.fromCharCode(104,122,116,110,104,50,112,111,105,57,0) == renderN;
      do {
         renderN += `${((mappingb ? 1 : 3))}`;
         if (detailI) {
            break;
         }
      } while ((mappingb) && detailI);
      iconnotificationnewI = `${((orientationG ? 3 : 3))}`;
   while ((handlerH.length - parseInt(`${owngoal9}`)) > 4 || (handlerH.length & 4) > 5) {
      owngoal9 /= Math.max(parseInt(`${topon2}`), 3);
      break;
   }

        setIsRefreshing(true);

      neonT = `${spinnert.length}`;
   let floaterV = neonT == String.fromCharCode(107,55,122,110,0);
   do {
       let suggestioni: Map<any, any> = new Map([[String.fromCharCode(103,95,55,50,95,97,99,114,111,115,115,0),844], [String.fromCharCode(102,102,105,111,0),375], [String.fromCharCode(105,110,116,101,114,97,99,116,105,118,101,108,108,121,0),910]]);
       let detailC = 4.0;
       let mbsplashJ = String.fromCharCode(102,102,117,114,108,0);
       let internetp = true;
       let nativeexS = 1.0;
      let canvasC = nativeexS <= 5160626.0;
      do {
          let exampleimageE = String.fromCharCode(108,111,97,100,120,0);
         nativeexS -= parseFloat(`${parseInt(`${nativeexS}`)}`);
         exampleimageE += `${exampleimageE.length + 1}`;
         if (canvasC) {
            break;
         }
      } while ((!Array.from(suggestioni.values()).includes(nativeexS)) && canvasC);
      for (let o = 0; o < 1; o++) {
          let subbasketballplayerm = true;
         nativeexS -= parseFloat(`${parseInt(`${detailC}`)}`);
         subbasketballplayerm = (subbasketballplayerm ? !subbasketballplayerm : !subbasketballplayerm);
      }
         nativeexS += parseFloat(`${suggestioni.size & 2}`);
         nativeexS *= parseFloat(`${parseInt(`${detailC}`) + 2}`);
      if (internetp) {
         detailC /= Math.max((parseFloat(`${(internetp ? 3 : 2) % (Math.max(mbsplashJ.length, 5))}`)), 1);
      }
       let minimizeZ: Map<any, any> = new Map([[String.fromCharCode(97,108,103,0),89], [String.fromCharCode(99,121,99,108,101,100,0),490], [String.fromCharCode(116,107,104,100,95,98,95,51,0),579]]);
       let leakcheckerJ: Map<any, any> = new Map([[String.fromCharCode(97,110,116,105,95,110,95,49,0),347], [String.fromCharCode(115,117,112,112,114,101,115,115,0),120], [String.fromCharCode(120,112,117,98,95,110,95,54,0),714]]);
         leakcheckerJ = new Map([[mbsplashJ, parseInt(`${detailC}`)]]);
         mbsplashJ += `${minimizeZ.size}`;
         minimizeZ.set(`${leakcheckerJ.size}`, leakcheckerJ.size % (Math.max(minimizeZ.size, 5)));
       let anneru = 4.0;
       let arrowupZ = 5.0;
          let bottomJ = 0.0;
          let overlayH: Array<any> = [String.fromCharCode(108,111,110,103,98,105,103,0), String.fromCharCode(112,114,111,120,105,109,105,116,121,95,56,95,55,56,0)];
         detailC /= Math.max(parseFloat(`${overlayH.length / (Math.max(2, 4))}`), 2);
         bottomJ -= parseInt(`${bottomJ}`);
         overlayH.push(parseInt(`${bottomJ}`) / (Math.max(4, parseInt(`${bottomJ}`))));
      if (4.52 < (5.95 + arrowupZ)) {
          let headera: Map<any, any> = new Map([[String.fromCharCode(108,105,98,97,111,109,95,103,95,52,49,0),817], [String.fromCharCode(100,97,121,95,54,95,52,50,0),546]]);
         arrowupZ += parseFloat(`${minimizeZ.size}`);
         headera = new Map([[`${headera.size}`, 2 >> (Math.min(5, Math.abs(headera.size)))]]);
      }
      if (2 > (leakcheckerJ.size / (Math.max(5, 1)))) {
          let scrollviewa = 4.0;
          let libhermes3: Array<any> = [916, 805, 935];
         internetp = nativeexS < arrowupZ;
         scrollviewa *= libhermes3.length - parseInt(`${scrollviewa}`);
         libhermes3.push(libhermes3.length);
      }
      let placementG = internetp ? !internetp : internetp;
      do {
         internetp = ((mbsplashJ.length | (!internetp ? mbsplashJ.length : 71)) < 71);
         if (placementG) {
            break;
         }
      } while (placementG && (!internetp));
      while ((detailC * 2.97) <= 4.14 || 1.3 <= (detailC * 2.97)) {
         detailC *= parseFloat(`${1 >> (Math.min(4, Math.abs(parseInt(`${detailC}`))))}`);
         break;
      }
      neonT = `${((internetp ? 4 : 5) + h_hash4.length)}`;
      if (floaterV) {
         break;
      }
   } while ((neonT.startsWith(`${catagoryW.length}`)) && floaterV);
      neonT += `${iconnotificationnewI.length}`;
   for (let b = 0; b < 3; b++) {
       let materiall = 5.0;
       let attributedstringY = 0;
       let yellowscoreballq = String.fromCharCode(100,105,105,110,0);
       let owngoals = 1;
       let acceptedC = String.fromCharCode(107,95,54,56,95,104,97,115,104,97,98,108,101,0);
         yellowscoreballq += "1";
         attributedstringY &= attributedstringY * owngoals;
          let downloadJ: Array<any> = [String.fromCharCode(111,97,117,116,104,0), String.fromCharCode(105,100,99,116,120,95,117,95,57,49,0), String.fromCharCode(112,114,117,110,105,110,103,0)];
         attributedstringY |= yellowscoreballq.length & 3;
         downloadJ.push(downloadJ.length - downloadJ.length);
      for (let u = 0; u < 2; u++) {
          let checkboxJ = true;
         materiall /= Math.max(((checkboxJ ? 4 : 4)), 2);
      }
         attributedstringY ^= (yellowscoreballq == String.fromCharCode(75,0) ? yellowscoreballq.length : parseInt(`${materiall}`));
      if ((attributedstringY + parseInt(`${materiall}`)) >= 1 || (parseInt(`${materiall}`) + attributedstringY) >= 1) {
          let minimizeH = 1.0;
         attributedstringY <<= Math.min(Math.abs(2 % (Math.max(parseInt(`${materiall}`), 5))), 5);
         minimizeH += parseInt(`${minimizeH}`);
      }
      while (1 < (acceptedC.length & owngoals)) {
         owngoals >>= Math.min(1, Math.abs(yellowscoreballq.length << (Math.min(1, Math.abs(attributedstringY)))));
         break;
      }
         attributedstringY /= Math.max(acceptedC.length, 4);
         yellowscoreballq += `${3 + acceptedC.length}`;
         owngoals /= Math.max(3, owngoals | 2);
         materiall += acceptedC.length;
      while (1 <= (2 + owngoals) || 3.39 <= (owngoals * materiall)) {
          let incidenti: Array<any> = [384, 531];
          let downloadD = String.fromCharCode(100,101,102,105,110,105,116,105,111,110,115,0);
          let sourceN = String.fromCharCode(109,111,100,0);
         materiall *= 1 & sourceN.length;
         incidenti = [incidenti.length];
         downloadD = `${(String.fromCharCode(107,0) == downloadD ? incidenti.length : downloadD.length)}`;
         sourceN += `${incidenti.length | downloadD.length}`;
         break;
      }
      while (5 <= (owngoals - attributedstringY)) {
         owngoals <<= Math.min(Math.abs(yellowscoreballq.length - 3), 2);
         break;
      }
      if (yellowscoreballq.endsWith(`${attributedstringY}`)) {
         yellowscoreballq += `${attributedstringY}`;
      }
         owngoals -= yellowscoreballq.length | 1;
      topon2 /= Math.max(neonT.length & 3, 1);
   }
   for (let d = 0; d < 1; d++) {
      neonT = `${2 << (Math.min(3, neonT.length))}`;
   }
   for (let u = 0; u < 3; u++) {
       let nterstitialh: Array<any> = [311, 69];
       let dragR = 0.0;
          let zhuboj = 0.0;
          let renew6 = String.fromCharCode(105,110,116,101,114,108,101,97,118,101,95,108,95,56,48,0);
          let update_wU = 0;
         dragR *= parseFloat(`${parseInt(`${zhuboj}`)}`);
         zhuboj *= 2;
         renew6 = `${(renew6 == String.fromCharCode(79,0) ? renew6.length : update_wU)}`;
         update_wU >>= Math.min(5, Math.abs(3));
      let transfery = nterstitialh.length >= 8090204;
      do {
          let code6 = String.fromCharCode(99,95,57,52,95,122,108,105,98,112,114,105,109,101,0);
          let encryptS = 1;
          let t_locke: Map<any, any> = new Map([[String.fromCharCode(113,95,53,55,95,105,108,111,103,0),false ], [String.fromCharCode(118,95,50,52,95,115,117,103,103,101,115,116,0),true ], [String.fromCharCode(117,100,116,97,0),false ]]);
         nterstitialh = [2];
         code6 = `${encryptS}`;
         encryptS <<= Math.min(3, Math.abs(code6.length << (Math.min(Math.abs(2), 5))));
         t_locke = new Map([[`${t_locke.size}`, 1]]);
         if (transfery) {
            break;
         }
      } while (transfery && (nterstitialh.includes(dragR)));
         dragR *= parseFloat(`${parseInt(`${dragR}`)}`);
       let fastforwardv = 2;
       let homeplayer0 = 5;
         homeplayer0 -= 1 * fastforwardv;
         dragR += parseFloat(`${parseInt(`${dragR}`)}`);
      owngoal9 /= Math.max(1, parseInt(`${topon2}`) >> (Math.min(Math.abs(3), 5)));
   }
      gestureb &= 2 + gestureb;
      h_hash4 = [neonT.length ^ 2];
        

   while (3 > iconnotificationnewI.length) {
      catagoryW += `${profileactivey.length & 1}`;
      break;
   }
   for (let k = 0; k < 3; k++) {
      iconnotificationnewI += `${handlerH.length}`;
   }
       let configs = false;
       let sends: Map<any, any> = new Map([[String.fromCharCode(111,112,117,115,101,110,99,95,56,95,56,0),48], [String.fromCharCode(108,97,112,112,101,100,95,122,95,52,57,0),9]]);
      if (configs) {
         sends.set(`${configs}`, sends.size - 2);
      }
      while (4 < (sends.size | 1) || sends.size < 1) {
         configs = (6 <= ((configs ? 6 : sends.size) / (Math.max(sends.size, 9))));
         break;
      }
          let disconnectedy = String.fromCharCode(117,95,57,51,95,108,111,103,108,101,118,101,108,0);
          let useri = 3.0;
          let mimoA = String.fromCharCode(114,101,99,111,114,100,105,110,103,99,111,110,110,0);
         sends.set(disconnectedy, 1);
         disconnectedy = `${parseInt(`${useri}`) / (Math.max(mimoA.length, 6))}`;
         useri /= Math.max(parseFloat(`${parseInt(`${useri}`)}`), 3);
         mimoA += `${parseInt(`${useri}`) | 1}`;
      if (!configs) {
         configs = (((configs ? 40 : sends.size) << (Math.min(Math.abs(sends.size), 2))) == 40);
      }
      let playC = configs ? !configs : configs;
      do {
          let nativeexf = String.fromCharCode(99,111,112,116,115,0);
          let detailk: Map<any, any> = new Map([[String.fromCharCode(100,95,50,54,95,99,108,111,115,101,0),317], [String.fromCharCode(107,95,53,54,95,112,114,101,109,117,108,116,105,112,108,121,111,102,102,115,101,116,0),425]]);
          let topich: Array<any> = [133, 979, 138];
         configs = 59 < detailk.size;
         nativeexf += `${topich.length}`;
         detailk = new Map([[`${topich.length}`, topich.length % 3]]);
         if (playC) {
            break;
         }
      } while (((5 & sends.size) <= 2) && playC);
      let libmapbufferjni9 = configs ? !configs : configs;
      do {
         configs = !configs;
         if (libmapbufferjni9) {
            break;
         }
      } while (libmapbufferjni9 && (sends.size >= 2 || 4 >= (2 ^ sends.size)));
      owngoal9 += ((configs ? 4 : 5) << (Math.min(Math.abs(parseInt(`${owngoal9}`)), 5)));
   while (4 >= (handlerH.length >> (Math.min(3, h_hash4.length))) || 4 >= (handlerH.length >> (Math.min(Math.abs(4), 5)))) {
      handlerH += "3";
      break;
   }
   while (2 >= (catagoryW.length & h_hash4.length)) {
       let bootsplashz = false;
       let playlistp = String.fromCharCode(120,114,117,110,0);
       let iconscheduleT = 2;
       let shirtR = 0;
       let sigmobH = String.fromCharCode(114,101,115,105,103,110,101,100,0);
      for (let h = 0; h < 3; h++) {
          let checkboxn = 5.0;
          let chatroombackgroundS = String.fromCharCode(115,95,50,50,95,105,110,112,117,116,0);
          let awayD = String.fromCharCode(117,112,112,101,114,99,97,115,101,0);
         playlistp += `${playlistp.length + 1}`;
         checkboxn *= awayD.length;
         chatroombackgroundS = `${1 >> (Math.min(5, awayD.length))}`;
      }
      let nextK = bootsplashz ? !bootsplashz : bootsplashz;
      do {
         bootsplashz = playlistp.length == iconscheduleT;
         if (nextK) {
            break;
         }
      } while ((sigmobH.length >= 2) && nextK);
      while (iconscheduleT <= 2 || 5 <= (iconscheduleT | 2)) {
         bootsplashz = 92 <= sigmobH.length || 92 <= shirtR;
         break;
      }
         sigmobH += `${playlistp.length}`;
         sigmobH = `${3 >> (Math.min(2, Math.abs(shirtR)))}`;
      if (!bootsplashz) {
          let librrcW = true;
          let yellowanimationliveR = false;
         bootsplashz = librrcW;
         yellowanimationliveR = !yellowanimationliveR;
      }
      if (playlistp.length < 1) {
         playlistp = `${iconscheduleT & 2}`;
      }
      while (3 < (shirtR ^ 3) || (shirtR ^ iconscheduleT) < 3) {
         iconscheduleT += shirtR;
         break;
      }
         sigmobH += `${((bootsplashz ? 5 : 4) % (Math.max(2, playlistp.length)))}`;
      while ((shirtR * 4) >= 4) {
          let rightp = 3;
          let searchbar_ = String.fromCharCode(98,117,102,115,0);
          let countdownh = false;
          let interstitialX = 5.0;
          let resultV: Map<any, any> = new Map([[String.fromCharCode(101,105,112,95,119,95,49,48,0),717], [String.fromCharCode(118,112,115,104,97,114,101,100,0),746]]);
         bootsplashz = !bootsplashz;
         rightp &= 2;
         searchbar_ += `${searchbar_.length - 2}`;
         countdownh = !countdownh;
         interstitialX /= Math.max(2, rightp);
         resultV = new Map([[searchbar_, 1 * searchbar_.length]]);
         break;
      }
      if ((sigmobH.length >> (Math.min(3, Math.abs(iconscheduleT)))) <= 5 && 4 <= (5 >> (Math.min(3, Math.abs(iconscheduleT))))) {
         sigmobH += "2";
      }
         shirtR <<= Math.min(5, Math.abs(3 | sigmobH.length));
      if (1 < playlistp.length) {
         bootsplashz = iconscheduleT >= 75;
      }
      for (let u = 0; u < 2; u++) {
          let invitet: Array<any> = [222, 289];
          let awayn: Array<any> = [981, 772];
          let package_92: Array<any> = [514, 744, 220];
          let bufferT = String.fromCharCode(112,114,101,115,99,97,108,105,110,103,0);
          let listw = String.fromCharCode(98,108,111,99,107,120,95,55,95,51,57,0);
         sigmobH += `${1 / (Math.max(3, playlistp.length))}`;
         invitet.push(listw.length);
         awayn = [bufferT.length | 3];
         package_92.push(invitet.length / 1);
         bufferT = `${package_92.length}`;
         listw = `${listw.length >> (Math.min(Math.abs(2), 3))}`;
      }
         sigmobH = "2";
      catagoryW += "3";
      break;
   }
   let injuryy = h_hash4.length <= 7822011;
   do {
       let iconnewsshareB = 4.0;
       let saved = String.fromCharCode(106,95,57,56,95,97,114,109,118,0);
       let main_b2: Array<any> = [184, 718, 731];
       let listS = String.fromCharCode(114,101,115,116,114,105,99,116,105,111,110,115,0);
       let nnewsH = true;
      let rewardn = 8009218.0 <= iconnewsshareB;
      do {
         iconnewsshareB /= Math.max(saved.length, 4);
         if (rewardn) {
            break;
         }
      } while (((iconnewsshareB / 5.76) == 1.68) && rewardn);
          let descE: Array<any> = [String.fromCharCode(115,97,100,120,120,95,100,95,53,53,0), String.fromCharCode(121,95,56,95,108,111,111,107,117,112,0), String.fromCharCode(113,95,49,52,95,117,110,100,101,114,115,99,111,114,101,0)];
          let iconrightoranget = String.fromCharCode(110,97,118,105,103,97,116,105,110,103,95,51,95,52,55,0);
          let light4 = String.fromCharCode(100,98,105,115,0);
         listS = `${iconrightoranget.length * saved.length}`;
         descE = [(light4 == String.fromCharCode(66,0) ? descE.length : light4.length)];
         iconrightoranget += `${2 + light4.length}`;
      while (listS.length > 4) {
         listS += `${main_b2.length - 2}`;
         break;
      }
          let networkF = false;
          let groupg = String.fromCharCode(119,95,53,56,0);
         main_b2 = [main_b2.length & 1];
         networkF = groupg.length >= 82;
         groupg = `${((networkF ? 2 : 2) * 3)}`;
          let filterX = String.fromCharCode(121,95,51,54,95,108,111,103,111,115,0);
         saved = `${main_b2.length}`;
         filterX += `${filterX.length}`;
      let flyerm = 6881807.0 <= iconnewsshareB;
      do {
         iconnewsshareB -= 1 % (Math.max(6, parseInt(`${iconnewsshareB}`)));
         if (flyerm) {
            break;
         }
      } while (flyerm && ((iconnewsshareB / (Math.max(saved.length, 2))) > 4.86));
         saved += `${2 & main_b2.length}`;
         main_b2 = [main_b2.length / 2];
         iconnewsshareB += saved.length;
      let bingt = iconnewsshareB >= 6481416.0;
      do {
         iconnewsshareB *= 1 >> (Math.min(3, main_b2.length));
         if (bingt) {
            break;
         }
      } while (bingt && (!main_b2.includes(iconnewsshareB)));
          let libfollyi = String.fromCharCode(98,117,98,98,108,101,0);
          let audienceI = 4.0;
          let libavfilterK = String.fromCharCode(115,116,114,105,110,103,115,0);
         nnewsH = libavfilterK.length > 6;
         libfollyi += `${libfollyi.length}`;
         audienceI /= Math.max(3, parseFloat(`${parseInt(`${audienceI}`)}`));
         libavfilterK += `${libfollyi.length}`;
         iconnewsshareB /= Math.max(3, 4);
         listS += `${2 << (Math.min(Math.abs(parseInt(`${iconnewsshareB}`)), 2))}`;
      for (let t = 0; t < 1; t++) {
         main_b2.push(main_b2.length);
      }
         main_b2 = [((nnewsH ? 1 : 5) | parseInt(`${iconnewsshareB}`))];
      h_hash4.push(saved.length % 1);
      if (injuryy) {
         break;
      }
   } while ((handlerH.startsWith(`${h_hash4.length}`)) && injuryy);
   while ((catagoryW.length & spinnert.length) < 2) {
       let sellmathbackground5 = String.fromCharCode(98,105,116,114,101,97,100,101,114,0);
       let orangedownarrowN = 1.0;
       let icontransferclubD = String.fromCharCode(111,99,99,117,112,105,101,100,95,107,95,55,55,0);
       let libjsijniprofilerV = String.fromCharCode(112,114,105,110,116,97,98,108,101,0);
      if (4 > icontransferclubD.length || libjsijniprofilerV != String.fromCharCode(52,0)) {
          let information6 = String.fromCharCode(99,117,114,115,111,114,115,116,114,101,97,109,119,114,97,112,112,101,114,95,116,95,52,52,0);
          let trashB = 0.0;
          let push9 = 1.0;
          let untick7: Map<any, any> = new Map([[String.fromCharCode(106,95,53,50,95,99,108,101,97,114,118,105,100,101,111,100,97,116,97,0),26], [String.fromCharCode(109,95,50,50,95,99,111,108,111,99,97,116,101,100,0),329]]);
         icontransferclubD += `${3 >> (Math.min(3, icontransferclubD.length))}`;
         information6 += `${untick7.size}`;
         trashB += parseInt(`${trashB}`);
         push9 += parseFloat(`${parseInt(`${trashB}`) << (Math.min(5, Math.abs(parseInt(`${push9}`))))}`);
         untick7 = new Map([[`${untick7.size}`, (information6 == String.fromCharCode(82,0) ? information6.length : untick7.size)]]);
      }
          let reportE: Map<any, any> = new Map([[String.fromCharCode(115,121,109,98,111,108,105,99,97,116,101,95,110,95,50,53,0),String.fromCharCode(97,95,50,51,95,114,109,115,105,112,114,0)], [String.fromCharCode(117,95,55,56,95,100,117,112,115,111,114,116,0),String.fromCharCode(105,95,51,54,95,116,114,97,110,115,0)], [String.fromCharCode(102,109,116,99,111,110,118,101,114,116,0),String.fromCharCode(107,95,53,53,95,115,111,99,107,115,0)]]);
          let iconsubssuccessU: Map<any, any> = new Map([[String.fromCharCode(100,101,115,116,0),false ], [String.fromCharCode(121,97,108,101,95,51,95,57,49,0),true ]]);
          let iconorientationU = String.fromCharCode(111,95,52,53,95,97,112,112,114,111,120,105,109,97,116,105,111,110,0);
         orangedownarrowN /= Math.max(3, iconsubssuccessU.size / (Math.max(1, 6)));
         reportE = new Map([[`${reportE.size}`, iconorientationU.length * reportE.size]]);
         iconsubssuccessU = new Map([[`${reportE.size}`, iconorientationU.length / 1]]);
      while (!icontransferclubD.startsWith(libjsijniprofilerV)) {
          let package_o0r = String.fromCharCode(115,104,111,119,119,97,118,101,115,112,105,99,0);
          let xvodO = 0;
          let mountingg = String.fromCharCode(98,97,110,100,115,0);
          let predictioni = 2.0;
         icontransferclubD += "1";
         package_o0r += `${package_o0r.length}`;
         xvodO /= Math.max(4, package_o0r.length - xvodO);
         mountingg = `${xvodO >> (Math.min(Math.abs(2), 2))}`;
         predictioni += parseInt(`${predictioni}`) % 3;
         break;
      }
      while (4 <= libjsijniprofilerV.length) {
          let muted6: Map<any, any> = new Map([[String.fromCharCode(116,121,112,101,111,102,0),3], [String.fromCharCode(101,110,99,114,121,112,116,101,100,0),23]]);
         icontransferclubD = `${icontransferclubD.length}`;
         muted6.set(`${muted6.size}`, 2);
         break;
      }
      catagoryW += `${(handlerH == String.fromCharCode(67,0) ? handlerH.length : gestureb)}`;
      sellmathbackground5 = `${sellmathbackground5.length ^ sellmathbackground5.length}`;
      break;
   }
       let blacklistk = true;
      while (!blacklistk && blacklistk) {
          let agreementN = String.fromCharCode(105,110,105,116,101,110,99,95,53,95,57,53,0);
          let resendq = String.fromCharCode(98,111,120,98,108,117,114,95,108,95,48,0);
          let minivodp: Map<any, any> = new Map([[String.fromCharCode(99,111,115,105,103,110,97,116,111,114,105,101,115,0),725], [String.fromCharCode(116,114,105,110,103,0),96], [String.fromCharCode(97,99,100,115,112,0),387]]);
          let china2 = String.fromCharCode(98,102,114,97,99,116,105,111,110,95,121,95,49,52,0);
          let modelsX = String.fromCharCode(114,101,97,99,116,95,110,95,51,48,0);
         blacklistk = china2 == resendq;
         agreementN = `${(agreementN == String.fromCharCode(95,0) ? minivodp.size : agreementN.length)}`;
         resendq = `${agreementN.length / (Math.max(3, minivodp.size))}`;
         china2 = `${agreementN.length}`;
         modelsX += `${3 << (Math.min(5, Math.abs(minivodp.size)))}`;
         break;
      }
      while (blacklistk && !blacklistk) {
          let detailG = 5.0;
         blacklistk = detailG >= 73.77;
         break;
      }
      while (!blacklistk) {
          let g_unlockH = false;
          let owngoalR = 3.0;
          let moviesV = 5.0;
          let control4 = String.fromCharCode(116,104,101,110,95,49,95,53,51,0);
         blacklistk = !g_unlockH;
         g_unlockH = (owngoalR / (Math.max(moviesV, 7))) >= 45.93;
         owngoalR *= parseInt(`${owngoalR}`) << (Math.min(5, Math.abs(2)));
         moviesV /= Math.max(parseFloat(`${1}`), 3);
         control4 = `${parseInt(`${moviesV}`) << (Math.min(control4.length, 3))}`;
         break;
      }
      profileactivey += `${(parseInt(`${topon2}`) << (Math.min(3, Math.abs((blacklistk ? 5 : 1)))))}`;
        await refetch();

       let guidew = true;
       let reducer_: Map<any, any> = new Map([[String.fromCharCode(121,117,118,112,108,97,110,101,0),String.fromCharCode(115,95,54,56,95,114,105,115,101,0)], [String.fromCharCode(115,95,55,50,95,115,121,110,99,104,114,111,110,111,117,115,0),String.fromCharCode(115,105,103,0)]]);
         guidew = !guidew;
         guidew = ((reducer_.size + (!guidew ? reducer_.size : 6)) >= 38);
         reducer_.set(`${guidew}`, reducer_.size ^ 1);
      while (reducer_.size < 4) {
          let sellmathbackgroundm = String.fromCharCode(105,115,112,111,114,116,95,117,95,53,49,0);
         reducer_.set(`${sellmathbackgroundm}`, 2 & sellmathbackgroundm.length);
         break;
      }
          let halffieldimages = 3;
          let iconscheduleo = String.fromCharCode(110,117,109,101,114,97,108,0);
          let handlerc = String.fromCharCode(99,95,57,51,95,110,111,99,111,108,115,101,116,0);
         guidew = handlerc.startsWith(`${halffieldimages}`);
         halffieldimages ^= iconscheduleo.length / (Math.max(iconscheduleo.length, 8));
         reducer_ = new Map([[`${reducer_.size}`, 1]]);
      catagoryW += "1";
   if (iconnotificationnewI.includes(neonT)) {
       let mergerL = 1;
       let areah = false;
      for (let c = 0; c < 2; c++) {
         areah = !areah;
      }
         mergerL >>= Math.min(4, Math.abs(((areah ? 4 : 5) & mergerL)));
       let commentt = 1.0;
          let dialogU: Map<any, any> = new Map([[String.fromCharCode(110,111,110,110,117,108,108,105,110,99,111,109,105,110,103,95,101,95,50,51,0),String.fromCharCode(100,101,97,99,116,105,118,97,116,101,100,0)], [String.fromCharCode(112,97,114,116,105,116,105,111,110,0),String.fromCharCode(106,95,51,53,95,117,110,112,97,99,107,104,105,0)], [String.fromCharCode(102,99,112,117,98,108,105,115,104,95,120,95,55,50,0),String.fromCharCode(102,108,111,97,116,115,104,111,114,116,0)]]);
         commentt *= ((areah ? 4 : 4) >> (Math.min(Math.abs(mergerL), 2)));
         dialogU.set(`${dialogU.size}`, dialogU.size);
         mergerL %= Math.max(3, 2);
      if (areah) {
         mergerL -= parseInt(`${commentt}`) ^ mergerL;
      }
      neonT = "1";
   }
       let dependencyr: Map<any, any> = new Map([[String.fromCharCode(116,108,115,0),String.fromCharCode(122,95,51,51,95,105,101,110,116,114,121,0)], [String.fromCharCode(115,95,49,48,48,95,110,112,114,111,98,101,0),String.fromCharCode(107,95,53,53,95,111,116,111,98,0)]]);
       let iconarrowrightwhitea = String.fromCharCode(111,116,111,115,0);
       let libnmsZ = true;
         iconarrowrightwhitea = `${(iconarrowrightwhitea == String.fromCharCode(53,0) ? iconarrowrightwhitea.length : (libnmsZ ? 4 : 2))}`;
         iconarrowrightwhitea = `${dependencyr.size & 3}`;
      let mimeZ = 7200312 <= iconarrowrightwhitea.length;
      do {
         iconarrowrightwhitea = `${(3 * (libnmsZ ? 1 : 1))}`;
         if (mimeZ) {
            break;
         }
      } while (((3 & dependencyr.size) > 4) && mimeZ);
      for (let o = 0; o < 2; o++) {
         dependencyr.set(iconarrowrightwhitea, iconarrowrightwhitea.length << (Math.min(Math.abs(2), 4)));
      }
          let textb: Array<any> = [974, 679, 620];
         libnmsZ = !libnmsZ || iconarrowrightwhitea.length > 98;
         textb = [2 + textb.length];
      if (5 < (5 + iconarrowrightwhitea.length) && (5 + dependencyr.size) < 1) {
         dependencyr = new Map([[`${dependencyr.size}`, 3]]);
      }
         iconarrowrightwhitea += `${iconarrowrightwhitea.length}`;
         dependencyr.set(`${libnmsZ}`, 1);
      while (iconarrowrightwhitea.length > 5) {
         iconarrowrightwhitea = `${dependencyr.size}`;
         break;
      }
      profileactivey += `${gestureb}`;
   if (4 == (3 | gestureb)) {
      gestureb /= Math.max(3, 3);
   }
      gestureb |= profileactivey.length << (Math.min(4, Math.abs(parseInt(`${topon2}`))));
   while (1 < iconnotificationnewI.length && 1 < handlerH.length) {
      iconnotificationnewI += `${1 >> (Math.min(Math.abs(gestureb), 3))}`;
      break;
   }
   if ((2.5 + topon2) > 3.25) {
      topon2 -= catagoryW.length * iconnotificationnewI.length;
   }
   let cornerD = topon2 >= 7672041.0;
   do {
      topon2 += neonT.length;
      if (cornerD) {
         break;
      }
   } while (((topon2 + 5.61) <= 3.44 || (3 - spinnert.length) <= 5) && cornerD);
        setIsRefreshing(false);

      spinnert.push((iconnotificationnewI == String.fromCharCode(72,0) ? descs.length : iconnotificationnewI.length));
   for (let v = 0; v < 3; v++) {
      gestureb ^= 2 << (Math.min(4, h_hash4.length));
   }
       let pagen = String.fromCharCode(115,104,111,114,116,115,0);
       let binddatasZ = String.fromCharCode(99,111,108,108,101,99,116,101,100,95,97,95,57,54,0);
      if (!pagen.endsWith(`${binddatasZ.length}`)) {
         binddatasZ = `${binddatasZ.length << (Math.min(Math.abs(3), 2))}`;
      }
          let homeicond = String.fromCharCode(105,95,52,52,95,108,105,98,107,118,97,122,97,97,114,0);
          let rewards = 2;
          let iconwatcha = 2.0;
         binddatasZ = `${homeicond.length - parseInt(`${iconwatcha}`)}`;
         homeicond += `${rewards + 3}`;
         rewards %= Math.max(2, rewards & 1);
         binddatasZ += `${(binddatasZ == String.fromCharCode(83,0) ? binddatasZ.length : pagen.length)}`;
         binddatasZ += `${pagen.length ^ 1}`;
      for (let r = 0; r < 3; r++) {
         pagen = `${binddatasZ.length}`;
      }
      while (5 <= binddatasZ.length) {
          let topicV = String.fromCharCode(101,110,116,114,111,112,121,109,118,95,103,95,49,49,0);
         binddatasZ = `${(String.fromCharCode(87,0) == binddatasZ ? topicV.length : binddatasZ.length)}`;
         break;
      }
      topon2 /= Math.max(5, 3);
   while (catagoryW.length >= 2) {
      catagoryW += "1";
      break;
   }
      gestureb >>= Math.min(1, Math.abs(3));
   for (let t = 0; t < 1; t++) {
      h_hash4 = [profileactivey.length];
   }
   for (let g = 0; g < 3; g++) {
       let benefitj: Array<any> = [String.fromCharCode(115,104,97,114,100,0), String.fromCharCode(104,111,114,110,95,102,95,53,56,0), String.fromCharCode(111,95,49,51,95,114,101,103,114,101,115,115,0)];
       let textlayoutmanagerg: Map<any, any> = new Map([[String.fromCharCode(108,95,50,48,95,115,116,114,109,97,116,99,104,0),331], [String.fromCharCode(99,104,114,111,109,97,107,101,121,0),914], [String.fromCharCode(108,101,102,116,0),931]]);
       let eventG = String.fromCharCode(103,95,49,50,95,116,105,109,101,99,111,100,101,115,0);
      if ((benefitj.length / (Math.max(textlayoutmanagerg.size, 4))) <= 1) {
         benefitj = [eventG.length * 2];
      }
      let greenK = 7079962 <= benefitj.length;
      do {
         benefitj.push(benefitj.length + 2);
         if (greenK) {
            break;
         }
      } while ((1 == (2 ^ benefitj.length)) && greenK);
      for (let z = 0; z < 2; z++) {
         eventG = `${textlayoutmanagerg.size}`;
      }
       let animations4: Array<any> = [776, 687, 861];
          let upgradeh = String.fromCharCode(118,112,97,116,104,109,101,115,117,114,101,0);
         eventG = `${animations4.length}`;
         upgradeh += "1";
      for (let v = 0; v < 1; v++) {
         benefitj = [1 | textlayoutmanagerg.size];
      }
          let yingF = String.fromCharCode(97,95,54,53,95,114,116,112,112,114,111,116,111,0);
         animations4.push(yingF.length);
          let baseZ = 2.0;
          let indicatorT = String.fromCharCode(115,95,55,57,95,97,110,110,111,116,97,116,101,0);
         eventG += `${2 - eventG.length}`;
         baseZ /= Math.max(parseFloat(`${parseInt(`${baseZ}`) / 3}`), 2);
         indicatorT = `${2 % (Math.max(7, indicatorT.length))}`;
         benefitj = [1 & textlayoutmanagerg.size];
      t_view3 *= parseInt(`${owngoal9}`) % 3;
   }
   let elementsV = iconnotificationnewI == String.fromCharCode(114,114,121,118,114,108,100,0);
   do {
      iconnotificationnewI += "2";
      if (elementsV) {
         break;
      }
   } while (elementsV && (4 > neonT.length));
        return;
    }, []);

    const [flattenedVideos, setFlattenedVideos] = useState(Array<mayi_Small>);
    const LIMIT = 300;
    const fetchVods = (page: number) => mayi_Predictionarrow.getListByPage({
        page,
        limit: LIMIT,
    });

    const {
        data: videos,
        isSuccess,
        hasNextPage,
        fetchNextPage,
        isFetchingNextPage,
        isFetching,
        refetch,
    } = useMinivodQuery('normal', true, { isAscreen: true });

    useEffect(() => {
        if (videos != undefined) {
            setFlattenedVideos(videos?.pages.flat().filter(x => x));
        }
    }, [videos])

    const checkConnection = useCallback(async () => {
       let member9 = String.fromCharCode(102,95,49,51,95,98,117,102,0);
    let viewern = 0;
    let crickety: Array<any> = [290, 493, 314];
    let rncoreX = String.fromCharCode(105,115,97,99,102,105,120,0);
    let ballg = 0.0;
    let homeplayerr = false;
    let sellM = 5.0;
    let defaultprofilepicA = 3.0;
    let subs4: Array<any> = [496, 490];
    let libreanimatedN: Map<any, any> = new Map([[String.fromCharCode(114,95,52,52,95,98,108,101,101,100,0),801], [String.fromCharCode(102,102,118,108,0),248], [String.fromCharCode(112,95,52,95,114,111,117,110,100,117,112,0),975]]);
    let libavcodecY = 0.0;
    let nativen = false;
    let iconpipexpand5 = 5;
   while ((1 % (Math.max(3, subs4.length))) < 1) {
      member9 = `${2 << (Math.min(1, Math.abs(viewern)))}`;
      break;
   }
   for (let z = 0; z < 3; z++) {
      member9 += "1";
   }
       let react_ = 5.0;
         react_ -= parseFloat(`${parseInt(`${react_}`)}`);
      while (1.82 == (react_ / (Math.max(4.25, 4))) && 4.25 == (react_ / (Math.max(7, react_)))) {
          let reddownarrowU = 4;
          let bannerI = true;
          let layoutP: Map<any, any> = new Map([[String.fromCharCode(102,114,111,122,101,110,0),true ], [String.fromCharCode(122,95,51,48,95,98,105,116,115,116,114,101,97,109,0),true ], [String.fromCharCode(109,101,115,115,97,103,101,95,115,95,49,50,0),true ]]);
          let unreadI = true;
          let with_dkC = String.fromCharCode(110,95,51,56,95,105,110,105,116,97,108,0);
         react_ *= parseFloat(`${2 | reddownarrowU}`);
         reddownarrowU ^= with_dkC.length;
         bannerI = with_dkC.startsWith(`${unreadI}`);
         layoutP.set(`${bannerI}`, (2 >> (Math.min(3, Math.abs((bannerI ? 5 : 5))))));
         unreadI = !unreadI || with_dkC.length <= 3;
         break;
      }
      let strO = 7188830.0 <= react_;
      do {
         react_ *= parseFloat(`${parseInt(`${react_}`)}`);
         if (strO) {
            break;
         }
      } while ((2.46 == (react_ + 2.53) || (react_ - 2.53) == 2.95) && strO);
      homeplayerr = 57 >= member9.length;
   while (5 < (viewern + parseInt(`${sellM}`)) || (viewern / 5) < 2) {
      sellM /= Math.max(1, parseInt(`${ballg}`));
      break;
   }
   if (rncoreX.length >= 2) {
      homeplayerr = parseInt(`${ballg}`) == rncoreX.length;
   }
   for (let a = 0; a < 2; a++) {
      sellM /= Math.max(parseInt(`${ballg}`) / 1, 4);
   }
   if (2 == (libreanimatedN.size - 2)) {
       let default_sO = 0.0;
       let nbatrophyp: Array<any> = [495, 693, 244];
      for (let s = 0; s < 1; s++) {
         default_sO *= parseFloat(`${2}`);
      }
      let bridgeb = default_sO >= 7537846.0;
      do {
         default_sO *= parseFloat(`${3}`);
         if (bridgeb) {
            break;
         }
      } while ((parseInt(`${default_sO}`) < nbatrophyp.length) && bridgeb);
      if (nbatrophyp.length >= 4) {
         nbatrophyp.push(2);
      }
          let logoU = 2.0;
          let libfbf = false;
          let bingv: Map<any, any> = new Map([[String.fromCharCode(117,110,105,109,112,111,114,116,97,110,116,95,116,95,54,53,0),229], [String.fromCharCode(106,95,50,52,95,117,110,115,99,97,108,101,100,0),730]]);
         nbatrophyp = [parseInt(`${default_sO}`) << (Math.min(2, Math.abs(1)))];
         logoU *= bingv.size;
         libfbf = libfbf && logoU > 20.82;
         bingv = new Map([[`${bingv.size}`, 1]]);
         nbatrophyp.push(nbatrophyp.length);
         default_sO += parseFloat(`${parseInt(`${default_sO}`)}`);
      viewern %= Math.max(member9.length, 2);
   }

        const state = await NetInfo.fetch();

   if (libreanimatedN.get(`${sellM}`) == null) {
      sellM *= crickety.length * libreanimatedN.size;
   }
   if (!homeplayerr) {
      homeplayerr = subs4.length == defaultprofilepicA;
   }
       let inviteV = String.fromCharCode(104,95,56,51,95,97,110,105,109,97,116,101,100,0);
          let halfh: Map<any, any> = new Map([[String.fromCharCode(109,111,100,97,108,108,121,95,99,95,49,48,0),false ], [String.fromCharCode(105,100,105,111,109,95,48,95,50,57,0),false ], [String.fromCharCode(99,95,50,95,112,108,97,121,97,98,108,101,0),false ]]);
         inviteV = `${halfh.size}`;
      while (inviteV.startsWith(`${inviteV.length}`)) {
          let mathU = 3;
          let iconclosewhitebgC: Map<any, any> = new Map([[String.fromCharCode(119,95,49,50,0),539], [String.fromCharCode(115,99,97,108,97,98,105,108,105,116,121,95,102,95,52,56,0),891]]);
         inviteV = `${mathU}`;
         mathU >>= Math.min(3, Math.abs(1 >> (Math.min(5, Math.abs(iconclosewhitebgC.size)))));
         iconclosewhitebgC = new Map([[`${iconclosewhitebgC.size}`, iconclosewhitebgC.size * 2]]);
         break;
      }
         inviteV = `${2 % (Math.max(1, inviteV.length))}`;
      libreanimatedN = new Map([[`${crickety.length}`, 2 * parseInt(`${defaultprofilepicA}`)]]);
      subs4 = [parseInt(`${defaultprofilepicA}`)];
   if ((1 ^ member9.length) > 1 || (member9.length ^ viewern) > 1) {
       let page5 = 2.0;
       let belld = false;
       let tick_ = String.fromCharCode(98,105,103,103,101,115,116,95,119,95,52,52,0);
       let audiencet = 1.0;
       let selectedo = String.fromCharCode(118,100,114,97,119,104,101,108,112,101,114,95,107,95,54,51,0);
       let videocommonY: Array<any> = [674, 951, 789];
       let emojiheartg: Array<any> = [70, 750, 502];
       let static_u8 = String.fromCharCode(99,111,110,115,116,114,97,105,110,101,100,0);
      if (!belld || 1 < (5 ^ emojiheartg.length)) {
         emojiheartg = [(selectedo == String.fromCharCode(56,0) ? selectedo.length : videocommonY.length)];
      }
          let reminderH = false;
         belld = !reminderH || audiencet <= 31.85;
         static_u8 = "3";
          let confirmationE = 5.0;
          let iconpipexpandg = String.fromCharCode(99,111,99,111,97,95,51,95,55,52,0);
         emojiheartg = [emojiheartg.length];
         confirmationE += parseFloat(`${parseInt(`${confirmationE}`)}`);
         iconpipexpandg += `${iconpipexpandg.length}`;
          let cricketB = 3.0;
         belld = cricketB <= 3.3 || tick_ == String.fromCharCode(80,0);
          let eventn: Map<any, any> = new Map([[String.fromCharCode(115,101,110,100,95,117,95,55,54,0),257], [String.fromCharCode(115,108,101,101,112,0),226]]);
         page5 *= parseFloat(`${eventn.size >> (Math.min(Math.abs(3), 1))}`);
      for (let x = 0; x < 2; x++) {
         videocommonY.push(static_u8.length);
      }
       let playG: Array<any> = [970, 216, 89];
      if (videocommonY.length < static_u8.length) {
          let imagenetworkerrq: Array<any> = [317, 207, 647];
          let anytimeJ = String.fromCharCode(108,115,112,114,95,53,95,50,51,0);
          let typesn = String.fromCharCode(99,117,114,118,101,115,0);
          let downloaderF = 3;
         static_u8 = `${typesn.length & 1}`;
         imagenetworkerrq = [1 | anytimeJ.length];
         anytimeJ = "2";
         typesn += `${imagenetworkerrq.length}`;
         downloaderF *= 2;
      }
          let chatroombackgroundm = String.fromCharCode(98,108,97,109,101,100,0);
          let spinnerA = String.fromCharCode(101,102,102,101,99,116,95,106,95,53,56,0);
         static_u8 += `${static_u8.length}`;
         chatroombackgroundm += "1";
         spinnerA += "2";
          let tooltipsA = 5.0;
          let hongkongm = 0.0;
         selectedo += `${parseInt(`${tooltipsA}`)}`;
         tooltipsA /= Math.max(3, 2);
         hongkongm += parseFloat(`${2 << (Math.min(Math.abs(parseInt(`${hongkongm}`)), 1))}`);
         selectedo = `${emojiheartg.length & 2}`;
       let defaultprofilepic3 = true;
       let tickedp = false;
      member9 += `${viewern * 1}`;
   }
   let playercommonK = 6805002.0 >= sellM;
   do {
      sellM /= Math.max(viewern >> (Math.min(Math.abs(parseInt(`${sellM}`)), 4)), 1);
      if (playercommonK) {
         break;
      }
   } while (playercommonK && (sellM < subs4.length));
      libavcodecY -= member9.length;
        const offline = !(state.isConnected && state.isInternetReachable);

      subs4 = [3 & libreanimatedN.size];
   for (let r = 0; r < 1; r++) {
      libreanimatedN = new Map([[`${sellM}`, 1]]);
   }
   for (let b = 0; b < 3; b++) {
       let black5 = String.fromCharCode(97,95,51,55,95,99,116,105,109,101,115,116,97,109,112,0);
      while (black5 != black5) {
         black5 = `${black5.length}`;
         break;
      }
       let penaltyshootnogoalA = String.fromCharCode(100,111,119,110,103,114,97,100,101,95,102,95,52,57,0);
      if (penaltyshootnogoalA.includes(`${black5.length}`)) {
          let thumbnailQ = String.fromCharCode(110,95,56,50,95,101,113,117,101,115,116,0);
          let disconnectedf = true;
          let tailW = String.fromCharCode(113,95,57,95,108,115,102,112,111,108,121,0);
         penaltyshootnogoalA += `${penaltyshootnogoalA.length}`;
         thumbnailQ += `${3 / (Math.max(1, thumbnailQ.length))}`;
         disconnectedf = ((thumbnailQ.length >> (Math.min(5, Math.abs((!disconnectedf ? 98 : thumbnailQ.length))))) <= 98);
         tailW = `${thumbnailQ.length - 3}`;
      }
      sellM += 1;
   }
      libreanimatedN.set(rncoreX, 2);
       let scoreo = 1.0;
       let downW = true;
       let username1 = String.fromCharCode(100,101,109,97,116,101,114,105,97,108,105,122,101,95,57,95,50,57,0);
      while (3 <= username1.length) {
         username1 = `${((downW ? 1 : 5) & parseInt(`${scoreo}`))}`;
         break;
      }
          let transferI = String.fromCharCode(97,116,116,114,97,99,116,111,114,0);
          let renderq = String.fromCharCode(112,97,110,101,115,95,98,95,55,0);
          let typesv: Array<any> = [663, 518, 271];
         username1 = "2";
         transferI = `${(renderq == String.fromCharCode(107,0) ? transferI.length : renderq.length)}`;
         typesv.push(1 / (Math.max(10, renderq.length)));
      if (username1.endsWith(`${downW}`)) {
         username1 = `${(username1 == String.fromCharCode(78,0) ? parseInt(`${scoreo}`) : username1.length)}`;
      }
          let reminderb = 4;
          let matchk = true;
          let typingn = 0;
         scoreo += (parseFloat(`${(downW ? 3 : 1) * parseInt(`${scoreo}`)}`));
         reminderb <<= Math.min(2, Math.abs(reminderb % (Math.max(9, typingn))));
         matchk = 57 == typingn;
      while (downW) {
         scoreo *= (parseFloat(`${(downW ? 4 : 5) % 3}`));
         break;
      }
      if (!downW) {
         downW = username1.length < 36;
      }
      let foregroundz = String.fromCharCode(100,101,54,120,109,111,114,122,95,0) == username1;
      do {
         username1 += `${parseInt(`${scoreo}`)}`;
         if (foregroundz) {
            break;
         }
      } while ((parseInt(`${scoreo}`) == username1.length) && foregroundz);
         downW = 46.53 >= scoreo;
      while (!username1.startsWith(`${scoreo}`)) {
         username1 += `${username1.length}`;
         break;
      }
      crickety = [parseInt(`${ballg}`)];
   if ((3 >> (Math.min(2, member9.length))) > 1 && (3 >> (Math.min(3, member9.length))) > 2) {
       let defaultlogoc = 2.0;
       let uimanagerA: Array<any> = [359, 861, 324];
         defaultlogoc -= parseFloat(`${1}`);
       let holderX = String.fromCharCode(117,110,115,116,97,107,101,0);
      let tooltipsR = 7240112 >= uimanagerA.length;
      do {
          let libavcodecA = 5.0;
         uimanagerA = [holderX.length << (Math.min(4, Math.abs(parseInt(`${libavcodecA}`))))];
         if (tooltipsR) {
            break;
         }
      } while (tooltipsR && ((3 ^ uimanagerA.length) > 1 || (3 ^ holderX.length) > 5));
      let hometeamfield4 = uimanagerA.length >= 7583471;
      do {
         uimanagerA = [uimanagerA.length | 2];
         if (hometeamfield4) {
            break;
         }
      } while ((!holderX.startsWith(`${uimanagerA.length}`)) && hometeamfield4);
         uimanagerA.push(parseInt(`${defaultlogoc}`));
         holderX = "3";
      member9 += `${parseInt(`${sellM}`) / (Math.max(uimanagerA.length, 2))}`;
   }
      ballg -= parseFloat(`${parseInt(`${sellM}`) - parseInt(`${defaultprofilepicA}`)}`);
        setIsOffline(offline);

       let libtobY: Map<any, any> = new Map([[String.fromCharCode(114,95,57,49,95,112,97,99,107,115,0),373], [String.fromCharCode(99,111,109,112,117,116,101,95,117,95,53,55,0),497], [String.fromCharCode(105,110,116,114,111,100,117,99,116,105,111,110,95,105,95,52,0),896]]);
         libtobY = new Map([[`${libtobY.size}`, libtobY.size]]);
         libtobY = new Map([[`${libtobY.size}`, 1]]);
      let iconmoreL = libtobY.size <= 7084170;
      do {
         libtobY = new Map([[`${libtobY.size}`, libtobY.size * 3]]);
         if (iconmoreL) {
            break;
         }
      } while (iconmoreL && (Array.from(libtobY.keys()).includes(`${libtobY.size}`)));
      ballg += parseFloat(`${viewern}`);
      rncoreX += "3";
      member9 += `${subs4.length}`;
      homeplayerr = !nativen;
   for (let p = 0; p < 1; p++) {
      sellM -= 2 + parseInt(`${ballg}`);
   }
   while (1 >= (3 * member9.length) && (member9.length * 3) >= 3) {
      member9 += `${parseInt(`${sellM}`) << (Math.min(1, Math.abs(viewern)))}`;
      break;
   }
   let v_hash7 = nativen ? !nativen : nativen;
   do {
      nativen = (member9.length * libreanimatedN.size) <= 37;
      if (v_hash7) {
         break;
      }
   } while (v_hash7 && (libreanimatedN.size < 1));
        if (!offline) {

   for (let e = 0; e < 1; e++) {
       let filterv = 5;
      if (1 == (filterv & 4)) {
         filterv &= filterv;
      }
      if (filterv < filterv) {
         filterv <<= Math.min(5, Math.abs(filterv - 1));
      }
      if (filterv > 1) {
         filterv &= filterv;
      }
      ballg -= parseFloat(`${1}`);
   }
      crickety.push(3);
       let libffmpegkit3 = String.fromCharCode(103,95,49,54,95,114,101,101,108,0);
         libffmpegkit3 += `${libffmpegkit3.length % (Math.max(10, libffmpegkit3.length))}`;
       let memberc: Array<any> = [473, 632];
       let n_viewk: Array<any> = [String.fromCharCode(115,117,98,116,114,97,99,116,109,111,100,0), String.fromCharCode(101,105,103,104,116,115,118,120,95,119,95,56,49,0)];
      while (!libffmpegkit3.includes(`${n_viewk.length}`)) {
         n_viewk.push(n_viewk.length ^ memberc.length);
         break;
      }
      rncoreX += `${(viewern * (nativen ? 5 : 5))}`;
   let previewY = nativen ? !nativen : nativen;
   do {
       let penaltymatchiconZ: Array<any> = [String.fromCharCode(100,101,99,111,114,97,116,111,114,115,95,109,95,51,49,0), String.fromCharCode(99,95,50,51,95,97,114,114,97,121,115,0)];
       let nbatrophyZ = String.fromCharCode(119,114,105,116,101,116,114,117,110,99,95,97,95,53,48,0);
       let scrollviewc: Array<any> = [String.fromCharCode(116,111,117,99,104,101,100,0), String.fromCharCode(109,117,108,116,105,115,116,101,112,95,118,95,56,56,0), String.fromCharCode(102,95,51,56,95,115,121,110,111,110,121,109,0)];
       let text7 = 2.0;
         scrollviewc.push(nbatrophyZ.length & penaltymatchiconZ.length);
      for (let a = 0; a < 3; a++) {
          let layoutI: Map<any, any> = new Map([[String.fromCharCode(100,95,53,56,95,98,97,99,107,112,116,114,0),624], [String.fromCharCode(106,117,115,116,95,51,95,50,48,0),190], [String.fromCharCode(97,114,101,97,115,95,97,95,57,48,0),2]]);
         nbatrophyZ = "1";
         layoutI = new Map([[`${layoutI.size}`, layoutI.size * layoutI.size]]);
      }
      let owngoal7 = 5316234 >= scrollviewc.length;
      do {
         scrollviewc = [3];
         if (owngoal7) {
            break;
         }
      } while (owngoal7 && (3 >= scrollviewc.length));
       let interstitialW = 5.0;
       let defaultroombg1 = 0.0;
         text7 *= parseInt(`${defaultroombg1}`) - penaltymatchiconZ.length;
         defaultroombg1 /= Math.max(parseInt(`${interstitialW}`) % 3, 3);
       let privatechatbgs = 2;
       let logoutQ = 5;
      while (2.11 >= (defaultroombg1 * text7)) {
          let scorepopsound7 = 1.0;
          let spinnerj = String.fromCharCode(122,95,54,54,95,109,97,114,107,100,111,119,110,0);
          let cornerkickb = 1;
          let moduler = String.fromCharCode(111,98,116,97,105,110,101,114,95,116,95,49,48,0);
         text7 += 1 << (Math.min(4, penaltymatchiconZ.length));
         scorepopsound7 -= cornerkickb - 3;
         spinnerj = `${parseInt(`${scorepopsound7}`)}`;
         cornerkickb >>= Math.min(moduler.length, 1);
         moduler += `${moduler.length >> (Math.min(Math.abs(3), 5))}`;
         break;
      }
         penaltymatchiconZ.push(privatechatbgs);
      let popupR = 5985323.0 <= interstitialW;
      do {
          let mappingu = 4;
         interstitialW += logoutQ / (Math.max(1, 9));
         mappingu /= Math.max(mappingu, 4);
         if (popupR) {
            break;
         }
      } while (popupR && (!scrollviewc.includes(interstitialW)));
         penaltymatchiconZ.push(1);
      while (4 > (nbatrophyZ.length & scrollviewc.length) && (scrollviewc.length & 4) > 4) {
         nbatrophyZ += "3";
         break;
      }
      nativen = viewern > 54;
      if (previewY) {
         break;
      }
   } while ((member9.length >= 2) && previewY);
   for (let i = 0; i < 2; i++) {
      sellM /= Math.max(4, rncoreX.length);
   }
   for (let z = 0; z < 1; z++) {
      subs4.push(1 / (Math.max(9, subs4.length)));
   }
       let rootz = 4.0;
       let iconbackwhite7 = 5;
      if (1.27 > (rootz + 1.46)) {
         rootz += 2;
      }
      if ((iconbackwhite7 + 3) < 2) {
         iconbackwhite7 -= parseInt(`${rootz}`) ^ iconbackwhite7;
      }
         iconbackwhite7 += iconbackwhite7 - 1;
      let rewardV = 9108908 >= iconbackwhite7;
      do {
         iconbackwhite7 /= Math.max(3 ^ iconbackwhite7, 3);
         if (rewardV) {
            break;
         }
      } while ((4.94 <= (iconbackwhite7 - rootz) && (4.94 - rootz) <= 3.45) && rewardV);
      while (1 > (iconbackwhite7 * parseInt(`${rootz}`)) && 1 > (iconbackwhite7 * parseInt(`${rootz}`))) {
         iconbackwhite7 >>= Math.min(3, Math.abs(iconbackwhite7));
         break;
      }
      while (4.1 == rootz) {
         rootz -= iconbackwhite7 / 3;
         break;
      }
      nativen = iconpipexpand5 == 27 && 27 == member9.length;
            handleRefresh();
        }
    }, []);

    useFocusEffect(useCallback(() => {
        if (!settingsReducer.isOffline && settingsReducer.isOffline !== isOffline) {
            setIsOffline(settingsReducer.isOffline);
            handleRefresh();
        } else if (settingsReducer.isOffline) {
            return () => {
                miniVodRef.current?.setPause(true);
                setIsOffline(settingsReducer.isOffline);
            }
        }
    }, [settingsReducer.isOffline]));

    useEffect(() => {
        const subscription = AppState.addEventListener(
            "change",
            handleAppStateChange
        );

        return () => {
            subscription.remove();
        };
    }, [])

    
    const handleAppStateChange = (nextAppState: any) => {
        setIsInBackground(nextAppState !== "active");
    };

    return (
        <ScreenContainer containerStyle={{ paddingLeft: 0, paddingRight: 0, paddingBottom: 10 }}>
            <View style={{ position: 'absolute', top: 0, left: 0, padding: 20, zIndex: 50, width: '100%', flexDirection: 'row', alignItems: 'center' }}>
                <Text style={{ ...textVariants.bigHeader, color: colors.text, fontSize: 20 }}>解说</Text>
            </View>
            {!isOffline &&
                <MiniVideoList
                    ref={miniVodRef}
                    miniVodListRef={miniVodListRef}
                    videos={flattenedVideos}
                    fetchNextPage={fetchNextPage}
                    hasNextPage={hasNextPage}
                    isFetching={isFetching}
                    isFetchingNextPage={isFetchingNextPage}
                    isActive={isFocused && !isInBackground}
                    setCollectionEpisode={(index: number) => { }}
                    handleRefreshMiniVod={handleRefresh}
                    isRefreshing={isRefreshing}
                    isPressTabScroll={isPressTabScroll}
                />
            }
            {isOffline && <NoConnection onClickRetry={checkConnection} />}
        </ScreenContainer>
    )
}

const styles = StyleSheet.create({

})
