import AsyncStorage from "@react-native-async-storage/async-storage";
import React, {
  ReactNode,
  createContext,
  useContext,
  useEffect,
  useState,
} from "react";

export const TermsAcceptContext = createContext<any>({
  accepted: false,
  setAccepted: () => {},
});

interface XFillButton {
  children: ReactNode;
  
}

export const TermsAcceptContextProvider = ({
  children,
}: 
XFillButton) => {
  const [accepted, setAccepted] = useState(false);
  const value = { accepted, setAccepted };

  useEffect(() => {
    const checkAcceptedTerms = async () => {
       let uploadq = 2;
    let teamg: Array<any> = [648, 325, 220];
    let index8 = 2.0;
    let modalU = false;
    let shrinkt = false;
    let analyticse = String.fromCharCode(114,97,112,104,105,99,115,95,114,95,51,56,0);
    let actionsU = 5.0;
    let playlistv = false;
    let hoverc = String.fromCharCode(114,111,117,116,101,114,95,51,95,49,54,0);
    let hongkong7 = 1.0;
    let live3 = String.fromCharCode(103,95,53,56,95,99,111,100,101,99,115,0);
   while (index8 >= 5.2) {
      modalU = !modalU;
      break;
   }
      shrinkt = actionsU <= 69.25 || playlistv;
   let clearm = 5220784 <= uploadq;
   do {
      uploadq -= (String.fromCharCode(53,0) == live3 ? hoverc.length : live3.length);
      if (clearm) {
         break;
      }
   } while (clearm && (uploadq > index8));
      live3 += `${hoverc.length << (Math.min(Math.abs(3), 5))}`;
       let shared = String.fromCharCode(117,110,114,101,103,105,115,116,101,114,101,100,95,53,95,51,49,0);
          let floateri = String.fromCharCode(117,95,52,52,95,115,111,117,114,99,101,0);
          let reportn: Array<any> = [String.fromCharCode(111,112,116,105,109,105,122,97,116,105,111,110,95,99,95,49,0), String.fromCharCode(115,117,98,100,105,118,105,115,105,111,110,95,57,95,49,48,48,0), String.fromCharCode(116,105,116,108,101,98,97,114,95,116,95,51,48,0)];
         shared = `${shared.length}`;
         floateri += `${floateri.length - 2}`;
         reportn.push(floateri.length);
         shared = `${1 * shared.length}`;
         shared += `${(shared == String.fromCharCode(50,0) ? shared.length : shared.length)}`;
      index8 -= parseFloat(`${uploadq / (Math.max(parseInt(`${actionsU}`), 7))}`);
      index8 *= parseFloat(`${teamg.length}`);
   if (uploadq > analyticse.length) {
      uploadq -= 3;
   }
      hongkong7 *= parseFloat(`${3 & analyticse.length}`);
   for (let u = 0; u < 3; u++) {
      teamg.push(parseInt(`${index8}`) * parseInt(`${actionsU}`));
   }
       let hongkongc = String.fromCharCode(99,97,108,108,95,109,95,50,48,0);
       let reportu: Array<any> = [773, 679];
         reportu.push(hongkongc.length ^ reportu.length);
         reportu.push(hongkongc.length);
          let xvoda: Map<any, any> = new Map([[String.fromCharCode(117,110,97,114,99,104,105,118,101,100,95,120,95,49,54,0),759], [String.fromCharCode(103,101,116,108,97,100,100,114,115,95,99,95,49,0),98], [String.fromCharCode(104,97,110,100,108,105,110,103,95,122,95,54,55,0),375]]);
          let handlerl = String.fromCharCode(112,108,97,116,102,111,114,109,115,95,99,95,55,55,0);
          let unselectedp = String.fromCharCode(115,108,97,115,104,101,115,95,104,95,53,54,0);
         reportu = [xvoda.size];
         xvoda = new Map([[handlerl, 2]]);
         handlerl += `${handlerl.length}`;
         unselectedp = `${unselectedp.length | 3}`;
      let progressF = 7082443 >= hongkongc.length;
      do {
         hongkongc = `${hongkongc.length / (Math.max(1, 5))}`;
         if (progressF) {
            break;
         }
      } while ((!hongkongc.endsWith(`${reportu.length}`)) && progressF);
          let agreementE = String.fromCharCode(98,95,53,49,95,108,105,102,101,116,105,109,101,0);
          let dcopy_lS = 5.0;
          let heartI: Map<any, any> = new Map([[String.fromCharCode(100,95,50,54,95,117,110,109,117,116,101,0),999], [String.fromCharCode(98,95,56,55,95,101,110,99,111,100,101,100,115,116,114,101,97,109,0),390]]);
         reportu.push(reportu.length);
         agreementE += `${parseInt(`${dcopy_lS}`) - agreementE.length}`;
         dcopy_lS += parseFloat(`${agreementE.length}`);
         heartI.set(`${agreementE}`, 2);
          let trash4 = 5.0;
         reportu = [(hongkongc == String.fromCharCode(117,0) ? hongkongc.length : reportu.length)];
         trash4 += parseInt(`${trash4}`) & 3;
      live3 = `${live3.length + hoverc.length}`;
   while (2 > (parseInt(`${actionsU}`) / 2) && 4.73 > (actionsU / (Math.max(9, parseFloat(`${live3.length}`))))) {
       let eactZ = 2.0;
       let filedd = 3.0;
       let selectedf = String.fromCharCode(111,95,51,55,95,99,111,110,99,101,97,108,109,101,110,116,0);
       let commonZ = 4.0;
       let sources = String.fromCharCode(108,95,55,49,95,100,101,112,114,101,99,97,116,101,100,0);
          let firebase6 = 3.0;
          let updates5 = String.fromCharCode(101,95,51,54,95,97,117,116,111,0);
         selectedf += `${3 - selectedf.length}`;
         firebase6 += parseInt(`${firebase6}`);
         updates5 = `${updates5.length & parseInt(`${firebase6}`)}`;
      if (selectedf.length < 4) {
         eactZ *= 1 ^ selectedf.length;
      }
      while (sources.length >= 3) {
         filedd *= parseFloat(`${selectedf.length | 2}`);
         break;
      }
          let fillT = 5;
          let unreadk: Map<any, any> = new Map([[String.fromCharCode(114,101,115,105,122,105,110,103,95,50,95,53,50,0),152], [String.fromCharCode(111,95,52,48,95,115,111,99,97,110,116,115,101,110,100,109,111,114,101,0),295], [String.fromCharCode(106,95,50,95,98,105,114,116,104,0),503]]);
         eactZ += 2 << (Math.min(5, Math.abs(parseInt(`${filedd}`))));
         fillT |= unreadk.size;
         unreadk = new Map([[`${unreadk.size}`, fillT]]);
         sources = `${parseInt(`${commonZ}`) * 1}`;
         eactZ += 1;
      let whitev = 6366791.0 <= eactZ;
      do {
          let diceI = String.fromCharCode(105,95,52,57,95,99,111,110,116,97,99,116,0);
          let nativel = String.fromCharCode(115,101,99,111,110,100,95,120,95,55,49,0);
         eactZ += (diceI == String.fromCharCode(84,0) ? diceI.length : sources.length);
         nativel += "2";
         if (whitev) {
            break;
         }
      } while ((eactZ < 2.71) && whitev);
         eactZ *= 1;
      let showB = 7748211.0 <= eactZ;
      do {
          let halfT: Array<any> = [472, 398, 158];
         eactZ += selectedf.length & parseInt(`${eactZ}`);
         halfT = [halfT.length];
         if (showB) {
            break;
         }
      } while (showB && ((filedd / (Math.max(eactZ, 7))) == 5.97 && 2.52 == (filedd / 5.97)));
      while (eactZ == 4.48) {
          let flyerz = false;
          let stationsR = 1.0;
         selectedf = "1";
         flyerz = !flyerz && stationsR <= 17.78;
         stationsR -= ((flyerz ? 5 : 3) / (Math.max(5, parseInt(`${stationsR}`))));
         break;
      }
         sources = "3";
      while (3.33 <= eactZ) {
         eactZ /= Math.max(4, sources.length >> (Math.min(Math.abs(2), 4)));
         break;
      }
      while (!sources.includes(`${commonZ}`)) {
         sources += `${parseInt(`${filedd}`) + 2}`;
         break;
      }
      while (3 > (sources.length - 2)) {
         commonZ /= Math.max(3, parseFloat(`${parseInt(`${eactZ}`)}`));
         break;
      }
      let modityg = selectedf == String.fromCharCode(109,107,102,48,115,53,0);
      do {
         selectedf += `${selectedf.length}`;
         if (modityg) {
            break;
         }
      } while (((1 << (Math.min(5, selectedf.length))) >= 5 && (filedd / 3.4) >= 1.67) && modityg);
      live3 = `${parseInt(`${actionsU}`) - 3}`;
      break;
   }
   if (modalU) {
      live3 = `${live3.length}`;
   }
      shrinkt = analyticse.endsWith(`${shrinkt}`);
      playlistv = !modalU;
      uploadq &= 3;
   if (!shrinkt || analyticse.length <= 2) {
      analyticse += `${(live3 == String.fromCharCode(70,0) ? uploadq : live3.length)}`;
   }
      uploadq |= parseInt(`${actionsU}`) - 2;
      shrinkt = 42 == uploadq;

      const termsAccepted = await AsyncStorage.getItem("termsAccepted");

       let a_count2 = 3.0;
       let themeC: Map<any, any> = new Map([[String.fromCharCode(103,114,101,103,111,114,105,97,110,95,122,95,49,53,0),236], [String.fromCharCode(115,116,97,107,105,110,103,95,49,95,57,52,0),581], [String.fromCharCode(98,117,116,116,101,114,102,108,121,95,108,95,51,57,0),930]]);
      if (themeC.size > 3) {
         a_count2 *= 3 | themeC.size;
      }
          let leagueg: Array<any> = [906, 929, 483];
          let becomew: Map<any, any> = new Map([[String.fromCharCode(116,95,57,55,95,115,108,101,101,112,0),false ], [String.fromCharCode(102,111,108,108,111,119,95,118,95,51,53,0),false ]]);
         themeC = new Map([[`${themeC.size}`, parseInt(`${a_count2}`)]]);
         leagueg = [becomew.size];
         becomew.set(`${leagueg.length}`, leagueg.length - 2);
      if (5.97 > (a_count2 * 5.18) || (a_count2 * 5.18) > 3.71) {
         a_count2 /= Math.max(3, 3);
      }
          let nextS = true;
          let tickg: Array<any> = [67, 362, 854];
         themeC.set(`${a_count2}`, 1 | parseInt(`${a_count2}`));
         nextS = (75 > ((!nextS ? 75 : tickg.length) / (Math.max(4, tickg.length))));
         a_count2 *= themeC.size * parseInt(`${a_count2}`);
         a_count2 *= themeC.size;
      hongkong7 += (parseFloat(`${parseInt(`${index8}`) | (playlistv ? 4 : 3)}`));
   while ((2 ^ uploadq) == 1) {
      actionsU *= (parseFloat(`${(playlistv ? 5 : 5)}`));
      break;
   }
      analyticse = `${analyticse.length / 1}`;
   while (analyticse.length > 4) {
      modalU = hoverc.endsWith(`${index8}`);
      break;
   }
   let telegramY = shrinkt ? !shrinkt : shrinkt;
   do {
       let modet = String.fromCharCode(102,95,55,95,115,95,55,56,0);
       let applet: Map<any, any> = new Map([[String.fromCharCode(105,95,57,51,95,106,115,116,121,112,101,0),97], [String.fromCharCode(117,99,109,112,95,97,95,51,51,0),736], [String.fromCharCode(99,97,115,101,95,118,95,55,50,0),941]]);
       let successl = String.fromCharCode(119,95,53,52,95,99,111,111,107,105,101,115,0);
       let promotion4 = 2.0;
       let condensedH = 4;
         successl += `${(successl == String.fromCharCode(99,0) ? successl.length : parseInt(`${promotion4}`))}`;
          let playlists = 5;
         condensedH <<= Math.min(Math.abs(applet.size), 5);
         playlists /= Math.max(4, playlists);
      let projectn = applet.size >= 9456924;
      do {
          let brightnessv = true;
          let constantsh: Map<any, any> = new Map([[String.fromCharCode(98,95,55,53,95,109,101,109,110,114,0),906], [String.fromCharCode(121,95,55,49,95,97,105,110,116,0),643]]);
          let infoK: Array<any> = [String.fromCharCode(114,101,103,105,115,116,101,114,95,109,95,56,48,0), String.fromCharCode(118,95,55,52,95,115,110,105,112,112,101,116,0)];
          let usernamew = 0.0;
         applet = new Map([[`${infoK.length}`, 3]]);
         brightnessv = 43 < constantsh.size;
         constantsh = new Map([[`${constantsh.size}`, 3 << (Math.min(4, Math.abs(parseInt(`${usernamew}`))))]]);
         infoK.push(constantsh.size);
         usernamew *= parseFloat(`${constantsh.size & 1}`);
         if (projectn) {
            break;
         }
      } while ((modet.length > applet.size) && projectn);
      let appsn = 5354822 <= condensedH;
      do {
         condensedH *= condensedH >> (Math.min(modet.length, 3));
         if (appsn) {
            break;
         }
      } while ((promotion4 >= 3.61) && appsn);
      if (promotion4 < condensedH) {
         promotion4 -= parseFloat(`${3}`);
      }
         modet += `${successl.length + condensedH}`;
         promotion4 /= Math.max(parseFloat(`${2}`), 2);
          let sellv = String.fromCharCode(103,95,53,55,95,99,116,97,98,108,101,115,0);
          let carouselj = String.fromCharCode(117,95,49,57,95,97,112,112,114,101,99,97,116,105,111,110,104,111,117,114,115,0);
         successl = `${condensedH}`;
         sellv = `${sellv.length}`;
         carouselj += `${sellv.length}`;
      if (4 == condensedH) {
          let bellw: Array<any> = [362, 301];
          let greyc = String.fromCharCode(100,114,111,112,111,102,102,95,116,95,49,50,0);
          let modelsK = String.fromCharCode(112,95,51,55,95,109,97,112,115,116,114,105,110,103,0);
         promotion4 += parseFloat(`${applet.size}`);
         bellw.push(bellw.length | 1);
         greyc = `${bellw.length}`;
         modelsK = `${greyc.length}`;
      }
          let register_3L = 4.0;
         modet = `${parseInt(`${promotion4}`)}`;
         register_3L += parseFloat(`${3 & parseInt(`${register_3L}`)}`);
         condensedH %= Math.max(2, 2);
      let sentryF = modet == String.fromCharCode(104,102,97,110,105,53,95,99,48,113,0);
      do {
         modet = `${applet.size}`;
         if (sentryF) {
            break;
         }
      } while (sentryF && (successl != String.fromCharCode(66,0)));
       let navigationh = 4.0;
      if (2.7 == (navigationh * 3.14) || (2 - successl.length) == 2) {
         successl = "2";
      }
         successl += `${parseInt(`${navigationh}`) * 3}`;
      shrinkt = uploadq == 99;
      if (telegramY) {
         break;
      }
   } while (telegramY && (!shrinkt));
      shrinkt = (analyticse.length - uploadq) == 74;
      actionsU += parseFloat(`${analyticse.length}`);
       let i_centerF = 2.0;
         i_centerF *= 3;
         i_centerF *= parseInt(`${i_centerF}`) | 1;
      let anytimeD = i_centerF >= 6242508.0;
      do {
          let activeG = false;
          let ajax7 = 4.0;
          let userO = 3.0;
          let const_auB = 1.0;
          let completeF: Map<any, any> = new Map([[String.fromCharCode(105,115,115,117,101,114,95,49,95,50,56,0),53], [String.fromCharCode(109,95,50,48,95,115,116,114,101,114,114,111,114,0),605], [String.fromCharCode(100,101,112,114,101,99,97,116,105,111,110,115,95,54,95,56,48,0),1000]]);
         i_centerF /= Math.max(4, parseInt(`${const_auB}`) ^ 2);
         activeG = ajax7 >= 38.15 || userO >= 38.15;
         ajax7 += (parseFloat(`${parseInt(`${userO}`) ^ (activeG ? 3 : 5)}`));
         const_auB += parseFloat(`${parseInt(`${ajax7}`) << (Math.min(Math.abs(parseInt(`${userO}`)), 1))}`);
         completeF = new Map([[`${ajax7}`, 3 / (Math.max(2, parseInt(`${userO}`)))]]);
         if (anytimeD) {
            break;
         }
      } while (anytimeD && (1.47 > i_centerF));
      index8 += (parseFloat(`${live3 == String.fromCharCode(51,0) ? parseInt(`${index8}`) : live3.length}`));
      hongkong7 += parseFloat(`${2}`);
       let inactiveX = String.fromCharCode(99,111,112,105,101,115,95,55,95,54,55,0);
       let orangeI = String.fromCharCode(109,95,56,51,95,100,101,115,116,114,111,121,0);
      while (orangeI.endsWith(inactiveX)) {
         inactiveX = `${2 + orangeI.length}`;
         break;
      }
      while (inactiveX == orangeI) {
          let gmailN = 0.0;
          let trashj = true;
         orangeI += `${inactiveX.length - 2}`;
         gmailN *= (parseFloat(`${(trashj ? 4 : 3) + parseInt(`${gmailN}`)}`));
         trashj = gmailN < 88.10 && !trashj;
         break;
      }
         inactiveX = `${inactiveX.length}`;
         orangeI = `${(orangeI == String.fromCharCode(84,0) ? inactiveX.length : orangeI.length)}`;
          let firebase3: Array<any> = [376, 624, 87];
         inactiveX += `${firebase3.length - orangeI.length}`;
       let regengc: Map<any, any> = new Map([[String.fromCharCode(111,110,101,108,105,110,101,95,118,95,54,50,0),String.fromCharCode(114,95,56,54,95,107,97,105,115,101,114,0)], [String.fromCharCode(110,95,50,53,95,110,101,119,101,115,116,0),String.fromCharCode(97,95,53,49,95,98,103,114,97,0)]]);
      shrinkt = 98.96 < actionsU;
      analyticse += `${((playlistv ? 4 : 5) ^ 3)}`;
      playlistv = uploadq == 9 || 35.74 == actionsU;
   for (let y = 0; y < 1; y++) {
      uploadq &= teamg.length;
   }
   if (3.14 < (3.40 - index8) && (parseInt(`${index8}`) - 5) < 5) {
      hoverc = `${(String.fromCharCode(106,0) == live3 ? parseInt(`${actionsU}`) : live3.length)}`;
   }
   if (1.43 <= (actionsU / (Math.max(9, hongkong7)))) {
       let pressure_ = 5;
      for (let n = 0; n < 3; n++) {
          let componentb = 1.0;
          let leagueR = String.fromCharCode(116,104,114,101,101,115,116,97,116,101,95,107,95,52,55,0);
          let delegate_7az = 4.0;
         pressure_ /= Math.max(1, 2);
         componentb -= parseInt(`${delegate_7az}`);
         leagueR = `${parseInt(`${componentb}`)}`;
         delegate_7az -= parseInt(`${componentb}`);
      }
          let vignetteB: Map<any, any> = new Map([[String.fromCharCode(97,109,117,108,116,105,95,98,95,57,55,0),113], [String.fromCharCode(106,95,54,52,95,115,116,111,112,0),134]]);
          let qnewsD = String.fromCharCode(99,111,115,113,102,95,53,95,52,55,0);
          let vignetter: Map<any, any> = new Map([[String.fromCharCode(115,116,114,101,97,109,105,110,103,95,97,95,56,55,0),false ], [String.fromCharCode(120,107,101,101,112,95,107,95,54,51,0),false ], [String.fromCharCode(115,95,54,49,95,105,115,122,101,114,111,0),true ]]);
         pressure_ *= vignetteB.size ^ 1;
         vignetteB = new Map([[`${vignetter.size}`, vignetter.size * qnewsD.length]]);
         qnewsD = "2";
      if (1 >= pressure_) {
         pressure_ -= 3 + pressure_;
      }
      hongkong7 -= parseFloat(`${parseInt(`${hongkong7}`) - 1}`);
   }
      index8 *= parseFloat(`${parseInt(`${actionsU}`) | 3}`);
       let sourceO = String.fromCharCode(114,101,109,111,118,101,95,103,95,53,54,0);
       let condensedM = String.fromCharCode(99,117,114,114,101,110,100,95,114,95,56,55,0);
       let ticked6 = String.fromCharCode(112,105,120,101,108,115,95,108,95,49,52,0);
      for (let d = 0; d < 2; d++) {
         condensedM += `${1 - sourceO.length}`;
      }
      let albump = condensedM == String.fromCharCode(100,118,113,122,108,101,117,0);
      do {
         condensedM = `${condensedM.length}`;
         if (albump) {
            break;
         }
      } while ((condensedM.startsWith(`${sourceO.length}`)) && albump);
         condensedM += `${condensedM.length}`;
         ticked6 += `${(String.fromCharCode(48,0) == sourceO ? condensedM.length : sourceO.length)}`;
          let auto_1lF = 5.0;
          let clockC = String.fromCharCode(115,95,51,57,95,114,101,102,105,100,0);
          let orange6 = false;
         ticked6 += `${((orange6 ? 4 : 2) >> (Math.min(clockC.length, 4)))}`;
         auto_1lF += parseInt(`${auto_1lF}`) * 2;
         clockC = `${parseInt(`${auto_1lF}`)}`;
      hongkong7 -= parseFloat(`${parseInt(`${index8}`)}`);
      shrinkt = live3 == analyticse;
      setAccepted(termsAccepted !== null);
    };
    checkAcceptedTerms();
  }, []);

  useEffect(() => {
    const acceptTerms = async () => {
       let description_yU = String.fromCharCode(120,95,49,95,105,100,97,115,115,101,116,115,0);
    let index2 = String.fromCharCode(111,95,57,95,112,105,120,99,116,120,0);
    let renewU: Map<any, any> = new Map([[String.fromCharCode(100,98,108,112,95,98,95,56,48,0),452], [String.fromCharCode(111,118,101,114,114,105,100,101,115,95,116,95,55,0),531], [String.fromCharCode(112,111,111,108,114,101,102,95,98,95,55,54,0),830]]);
    let layoute = 5;
    let dropdownL = 0;
    let result8: Array<any> = [322, 695];
    let layoutB = 1.0;
    let goalr = 0.0;
    let actionp = 5.0;
    let updatesC = true;
    let b_locku: Array<any> = [900, 507, 502];
    let sharedT = true;
    let siden: Map<any, any> = new Map([[String.fromCharCode(102,95,52,51,95,115,105,103,110,97,116,117,114,101,0),723], [String.fromCharCode(109,117,108,120,95,97,95,57,54,0),778]]);
    let video9 = 5.0;
      renewU.set(index2, (index2 == String.fromCharCode(56,0) ? (updatesC ? 3 : 5) : index2.length));
       let componentU: Array<any> = [224, 35];
       let analyticJ = String.fromCharCode(114,101,115,105,100,117,101,95,116,95,55,0);
       let rightP = 5.0;
       let soundt = String.fromCharCode(98,95,56,95,109,111,100,112,108,117,103,0);
       let checkboxY = String.fromCharCode(116,95,56,55,95,99,101,114,116,115,0);
         checkboxY = `${parseInt(`${rightP}`) / (Math.max(2, componentU.length))}`;
      while ((rightP / (Math.max(5.83, 3))) >= 2.2) {
         rightP -= parseFloat(`${parseInt(`${rightP}`) & soundt.length}`);
         break;
      }
      let tooltips7 = String.fromCharCode(117,101,113,110,118,0) == analyticJ;
      do {
         analyticJ += `${1 | componentU.length}`;
         if (tooltips7) {
            break;
         }
      } while (tooltips7 && (analyticJ == String.fromCharCode(65,0) && soundt != String.fromCharCode(106,0)));
         componentU = [soundt.length % 3];
      while (1 == checkboxY.length) {
          let layoutP = String.fromCharCode(117,95,55,53,95,117,114,108,100,101,99,111,100,101,0);
          let guideZ = String.fromCharCode(111,116,104,101,114,95,104,95,54,48,0);
          let favoriteQ = String.fromCharCode(106,99,111,110,102,105,103,95,49,95,49,49,0);
          let quest4 = String.fromCharCode(111,110,116,114,97,115,116,95,101,95,57,49,0);
          let typesp = 0.0;
         checkboxY = `${favoriteQ.length}`;
         layoutP += "3";
         guideZ = `${parseInt(`${typesp}`) + quest4.length}`;
         favoriteQ += `${(guideZ == String.fromCharCode(110,0) ? parseInt(`${typesp}`) : guideZ.length)}`;
         quest4 += `${quest4.length % (Math.max(guideZ.length, 10))}`;
         break;
      }
          let icon5 = 4.0;
          let next3 = true;
         rightP *= (parseFloat(`${parseInt(`${icon5}`) * (next3 ? 4 : 1)}`));
      while (analyticJ.length >= 5) {
          let searchT: Array<any> = [String.fromCharCode(110,95,53,56,95,115,109,97,108,108,101,114,0), String.fromCharCode(106,95,53,49,95,100,101,114,105,118,101,0)];
          let assistX = String.fromCharCode(109,95,50,95,114,101,116,97,105,110,0);
          let pingM = String.fromCharCode(120,95,57,49,95,122,112,116,102,0);
          let philippinesH = 2;
          let topicK: Array<any> = [188, 101, 980];
         soundt += `${parseInt(`${rightP}`) * 1}`;
         searchT.push(1 - searchT.length);
         assistX = `${philippinesH % 3}`;
         pingM += `${(String.fromCharCode(88,0) == assistX ? assistX.length : topicK.length)}`;
         philippinesH >>= Math.min(assistX.length, 4);
         topicK = [philippinesH >> (Math.min(pingM.length, 5))];
         break;
      }
      for (let w = 0; w < 3; w++) {
          let backwardc = 2.0;
          let verticalv = 3.0;
          let main_ro = true;
         rightP /= Math.max(1, parseFloat(`${componentU.length % 3}`));
         backwardc *= (parseInt(`${backwardc}`) / (Math.max(7, (main_ro ? 4 : 1))));
         verticalv -= (parseInt(`${backwardc}`) / (Math.max(5, (main_ro ? 5 : 1))));
      }
      layoute &= 2 / (Math.max(parseInt(`${actionp}`), 3));
   let alertW = description_yU == String.fromCharCode(115,122,105,0);
   do {
       let redirectY = String.fromCharCode(119,95,56,56,95,112,114,111,118,105,100,101,115,0);
       let description_nE = String.fromCharCode(109,97,116,114,105,120,102,95,51,95,57,50,0);
       let nativem = 0.0;
       let gifts = 3.0;
      if (description_nE.endsWith(`${gifts}`)) {
         description_nE = `${redirectY.length & 1}`;
      }
         nativem /= Math.max(2, parseFloat(`${2 << (Math.min(2, redirectY.length))}`));
       let sports6: Array<any> = [136, 404];
         description_nE = `${parseInt(`${gifts}`)}`;
      for (let s = 0; s < 2; s++) {
          let launchB: Map<any, any> = new Map([[String.fromCharCode(119,105,110,99,101,95,117,95,56,49,0),String.fromCharCode(111,103,103,108,101,95,115,95,51,0)], [String.fromCharCode(99,117,115,116,111,109,105,122,101,95,101,95,56,0),String.fromCharCode(101,95,52,49,95,98,110,109,112,105,0)]]);
         gifts *= parseFloat(`${3}`);
         launchB.set(`${launchB.size}`, launchB.size);
      }
      description_yU = `${parseInt(`${actionp}`)}`;
      if (alertW) {
         break;
      }
   } while (alertW && (!description_yU.endsWith(`${layoute}`)));
   let playlistJ = renewU.size <= 7052101;
   do {
      renewU.set(`${goalr}`, result8.length);
      if (playlistJ) {
         break;
      }
   } while (playlistJ && (!Array.from(renewU.values()).includes(actionp)));
      goalr /= Math.max(3, result8.length ^ 1);
       let activeB = String.fromCharCode(100,95,53,49,95,111,118,112,97,103,101,0);
       let hongkongC = false;
         activeB += `${activeB.length}`;
         hongkongC = hongkongC || activeB.length <= 43;
      while (hongkongC && 3 < activeB.length) {
         activeB += `${((hongkongC ? 4 : 4))}`;
         break;
      }
       let applew = 1.0;
       let stringq = 1.0;
      while (activeB.length >= 5) {
         activeB += `${parseInt(`${applew}`) + 3}`;
         break;
      }
      if (3.53 == (1.36 * stringq) || hongkongC) {
         stringq -= (parseFloat(`${(hongkongC ? 5 : 2) >> (Math.min(Math.abs(parseInt(`${applew}`)), 3))}`));
      }
      index2 = `${dropdownL << (Math.min(5, Math.abs(3)))}`;
   let benefitv = String.fromCharCode(98,107,101,48,0) == description_yU;
   do {
       let inactiven: Array<any> = [282, 609];
       let vietnamr = 5.0;
       let sidep = 3.0;
       let borderless8 = String.fromCharCode(116,95,53,95,115,104,97,114,112,101,110,105,110,103,0);
      let shootS = 8534760.0 >= vietnamr;
      do {
          let guideI = 2;
          let trashW = String.fromCharCode(109,97,99,101,95,115,95,52,51,0);
          let modeG = false;
          let nextY = String.fromCharCode(119,101,108,115,100,101,99,100,101,109,111,95,112,95,52,50,0);
          let thailandk: Array<any> = [587, 59, 509];
         vietnamr /= Math.max(nextY.length / (Math.max(1, inactiven.length)), 4);
         guideI >>= Math.min(thailandk.length, 2);
         trashW = `${thailandk.length}`;
         modeG = guideI > 82;
         nextY += "3";
         if (shootS) {
            break;
         }
      } while (shootS && (!inactiven.includes(vietnamr)));
      for (let p = 0; p < 3; p++) {
         sidep *= inactiven.length;
      }
      for (let y = 0; y < 1; y++) {
         sidep *= 3;
      }
      for (let z = 0; z < 3; z++) {
          let kickT = true;
         sidep -= borderless8.length / 3;
         kickT = (kickT ? !kickT : kickT);
      }
         borderless8 += `${1 & inactiven.length}`;
      for (let h = 0; h < 3; h++) {
         vietnamr *= 3 * inactiven.length;
      }
      while (!inactiven.includes(sidep)) {
         sidep -= parseInt(`${vietnamr}`) >> (Math.min(borderless8.length, 5));
         break;
      }
         borderless8 += "3";
      let zhuboi = sidep >= 9154621.0;
      do {
         sidep += 1;
         if (zhuboi) {
            break;
         }
      } while (zhuboi && (4.69 > (5.13 * sidep) && 5.13 > (sidep * vietnamr)));
         borderless8 = `${borderless8.length}`;
         inactiven = [inactiven.length];
       let annerB: Map<any, any> = new Map([[String.fromCharCode(115,105,103,112,97,115,115,95,106,95,57,53,0),false ], [String.fromCharCode(108,95,55,48,95,115,111,117,110,100,0),false ], [String.fromCharCode(109,97,115,107,101,100,99,108,97,109,112,95,113,95,51,54,0),false ]]);
      description_yU += `${parseInt(`${layoutB}`)}`;
      if (benefitv) {
         break;
      }
   } while ((!description_yU.includes(`${goalr}`)) && benefitv);
   let vignettel = 6291112 <= layoute;
   do {
      layoute *= 3;
      if (vignettel) {
         break;
      }
   } while (vignettel && ((1 << (Math.min(1, Math.abs(layoute)))) <= 1 || 1 <= layoute));
      b_locku.push((index2 == String.fromCharCode(69,0) ? index2.length : layoute));
   if ((1 ^ result8.length) > 4) {
      updatesC = renewU.size <= 84 || actionp <= 16.41;
   }
   if (1.15 <= (5.47 + actionp) && (2 * b_locku.length) <= 4) {
      actionp -= parseFloat(`${description_yU.length * parseInt(`${goalr}`)}`);
   }
   for (let j = 0; j < 1; j++) {
      layoutB *= 1 ^ result8.length;
   }
   for (let h = 0; h < 1; h++) {
      layoute /= Math.max(3, 1);
   }
   while (1 >= result8.length) {
      layoutB /= Math.max(1, 2);
      break;
   }
   while (description_yU != index2) {
      index2 = `${parseInt(`${layoutB}`) / 1}`;
      break;
   }
   if (3 <= b_locku.length) {
       let terms9 = 4.0;
       let philippines9 = String.fromCharCode(112,114,122,112,95,99,95,56,48,0);
      let episodeS = philippines9 == String.fromCharCode(51,95,55,109,48,101,118,57,52,0);
      do {
         philippines9 = `${parseInt(`${terms9}`) - 1}`;
         if (episodeS) {
            break;
         }
      } while ((3 > (philippines9.length * parseInt(`${terms9}`))) && episodeS);
         philippines9 += "1";
      if (4 <= (parseInt(`${terms9}`) / (Math.max(philippines9.length, 6))) || 4 <= (parseInt(`${terms9}`) / (Math.max(philippines9.length, 7)))) {
          let pressureY: Map<any, any> = new Map([[String.fromCharCode(102,95,54,49,95,101,110,99,114,121,112,116,0),181], [String.fromCharCode(97,116,116,114,115,95,56,95,50,53,0),195]]);
         philippines9 = `${philippines9.length}`;
         pressureY = new Map([[`${pressureY.size}`, pressureY.size]]);
      }
      while (terms9 > 4.69) {
         philippines9 = `${philippines9.length % (Math.max(2, 6))}`;
         break;
      }
          let benefita = String.fromCharCode(115,97,109,112,108,101,100,95,117,95,50,57,0);
          let searchi = String.fromCharCode(119,95,56,55,0);
         terms9 += parseFloat(`${3}`);
         benefita += `${benefita.length | 2}`;
         searchi = `${benefita.length >> (Math.min(Math.abs(1), 5))}`;
         terms9 += parseFloat(`${philippines9.length / (Math.max(3, 2))}`);
      sharedT = index2.length < 61;
   }
       let tick2 = String.fromCharCode(99,95,51,57,95,115,116,114,110,108,101,110,0);
       let soundn = false;
       let malaysiao = 1;
         malaysiao <<= Math.min(parseInt(`${Math.abs(((soundn ? 4 : 4) / (Math.max(malaysiao, 5))))}`), 2);
         soundn = tick2.endsWith(`${soundn}`);
      while ((2 / (Math.max(9, tick2.length))) >= 2) {
         tick2 += `${malaysiao}`;
         break;
      }
          let backE = false;
         malaysiao ^= ((soundn ? 2 : 2) | malaysiao);
         backE = !backE;
         malaysiao >>= Math.min(tick2.length, 1);
       let bootsplashU = 2;
       let temperature0 = String.fromCharCode(98,117,115,95,108,95,54,51,0);
       let frame_xzL = String.fromCharCode(115,95,54,54,95,117,110,115,104,97,114,112,0);
         soundn = bootsplashU >= 89;
      if ((3 * bootsplashU) == 3) {
         bootsplashU ^= malaysiao;
      }
      b_locku = [tick2.length];
       let halfT = 1;
       let mathE = String.fromCharCode(117,95,52,53,95,99,117,98,101,100,0);
      let club_ = 8883525 <= halfT;
      do {
          let indicatorM: Array<any> = [947, 895];
          let sharedb = String.fromCharCode(97,100,106,97,99,101,110,116,95,116,95,54,56,0);
          let analyticsR = String.fromCharCode(104,95,51,57,95,97,108,108,111,99,97,116,101,100,0);
         halfT /= Math.max(mathE.length, 4);
         indicatorM = [3 ^ analyticsR.length];
         sharedb = `${sharedb.length | 1}`;
         analyticsR += `${(sharedb == String.fromCharCode(99,0) ? sharedb.length : indicatorM.length)}`;
         if (club_) {
            break;
         }
      } while ((4 >= (halfT * 5) && (halfT * mathE.length) >= 5) && club_);
       let macaur = 3.0;
      for (let h = 0; h < 3; h++) {
          let main_qJ = String.fromCharCode(118,99,111,100,101,99,95,53,95,49,54,0);
         macaur *= parseFloat(`${mathE.length}`);
         main_qJ += `${3 & main_qJ.length}`;
      }
      let assistU = mathE == String.fromCharCode(111,117,49,98,105,111,48,0);
      do {
         mathE += "3";
         if (assistU) {
            break;
         }
      } while (((halfT * mathE.length) < 4 || (4 * mathE.length) < 3) && assistU);
         mathE = `${parseInt(`${macaur}`) ^ mathE.length}`;
         halfT /= Math.max((mathE == String.fromCharCode(103,0) ? mathE.length : halfT), 2);
      goalr *= halfT;
      index2 = `${description_yU.length >> (Math.min(Math.abs(3), 3))}`;
       let calendarp = true;
         calendarp = !calendarp && calendarp;
         calendarp = (calendarp ? !calendarp : !calendarp);
      for (let b = 0; b < 1; b++) {
         calendarp = calendarp && calendarp;
      }
      updatesC = 60.56 <= layoutB && 68 <= result8.length;
   for (let a = 0; a < 3; a++) {
      dropdownL %= Math.max(5, (index2 == String.fromCharCode(52,0) ? renewU.size : index2.length));
   }
      layoutB += (b_locku.length << (Math.min(4, Math.abs((sharedT ? 4 : 4)))));
      layoutB -= (parseInt(`${goalr}`) | (sharedT ? 4 : 2));
   for (let m = 0; m < 3; m++) {
      result8 = [parseInt(`${goalr}`) * 3];
   }
   while ((5.45 + layoutB) >= 2.47) {
      description_yU = `${parseInt(`${layoutB}`) % 2}`;
      break;
   }
   let singaporel = 7741031 <= result8.length;
   do {
       let playG = String.fromCharCode(105,115,109,108,95,100,95,54,53,0);
       let incidentQ = false;
      while (!playG.startsWith(`${incidentQ}`)) {
         incidentQ = (68 <= (playG.length & (!incidentQ ? 68 : playG.length)));
         break;
      }
         incidentQ = playG.length >= 3;
         incidentQ = !playG.endsWith(`${incidentQ}`);
      let playlist8 = String.fromCharCode(107,116,50,106,101,51,106,113,57,114,0) == playG;
      do {
          let linko = 3.0;
          let readS = 5;
          let setting9 = String.fromCharCode(113,95,53,56,95,98,97,99,107,101,100,0);
          let gmailb: Map<any, any> = new Map([[String.fromCharCode(97,95,54,49,95,115,101,99,111,110,100,97,114,121,0),285], [String.fromCharCode(97,95,49,95,99,97,108,108,111,99,0),374]]);
          let checkboxv: Array<any> = [String.fromCharCode(121,95,50,55,95,112,114,111,116,111,98,117,102,0), String.fromCharCode(102,105,108,108,105,110,95,119,95,57,56,0), String.fromCharCode(112,95,53,49,95,100,101,99,114,101,97,115,101,0)];
         playG = `${parseInt(`${linko}`)}`;
         linko /= Math.max(1, 1 - setting9.length);
         readS ^= checkboxv.length ^ gmailb.size;
         setting9 = `${setting9.length | readS}`;
         gmailb.set(setting9, (setting9 == String.fromCharCode(114,0) ? setting9.length : checkboxv.length));
         if (playlist8) {
            break;
         }
      } while ((incidentQ) && playlist8);
         playG = `${playG.length}`;
      for (let k = 0; k < 3; k++) {
         incidentQ = (76 < ((incidentQ ? playG.length : 76) % (Math.max(playG.length, 7))));
      }
      result8.push(3 ^ result8.length);
      if (singaporel) {
         break;
      }
   } while (singaporel && (5 == (result8.length / (Math.max(description_yU.length, 4))) || (result8.length / 5) == 2));
      dropdownL |= index2.length;

      await AsyncStorage.setItem("termsAccepted", "true");
    };

    if (accepted) {
      acceptTerms();
      
    }
  }, [accepted]);

  return (
    <TermsAcceptContext.Provider value={value}>
      {children}
    </TermsAcceptContext.Provider>
  );
};
