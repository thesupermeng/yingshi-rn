import AsyncStorage from "@react-native-async-storage/async-storage";
import React, {
  ReactNode,
  createContext,
  useContext,
  useEffect,
  useState,
} from "react";

export const TermsAcceptContext = createContext<any>({
  accepted: false,
  setAccepted: () => {},
});

interface wwIndexDice {
  children: ReactNode;
  
}

export const TermsAcceptContextProvider = ({
  children,
}: 
wwIndexDice) => {
  const [accepted, setAccepted] = useState(false);
  const value = { accepted, setAccepted };

  useEffect(() => {
    const checkAcceptedTerms = async () => {
       let rootK = String.fromCharCode(120,106,112,101,103,0);
    let fileh = 4.0;
    let configM: Map<any, any> = new Map([[String.fromCharCode(99,101,110,116,114,111,105,100,115,0),442], [String.fromCharCode(111,112,97,116,111,109,0),141]]);
    let carouselI = 3.0;
    let redgoalY = false;
    let libfabricjniZ = 1.0;
    let strc: Array<any> = [111, 518, 703];
    let root8 = String.fromCharCode(114,97,110,103,101,0);
    let whatsappe = String.fromCharCode(115,117,110,114,105,115,101,115,101,116,0);
    let weatherB = String.fromCharCode(114,101,99,111,109,112,117,116,101,0);
    let checkbox7 = 4.0;
   if ((carouselI + 5.59) == 4.56 && 2.10 == (5.59 + carouselI)) {
      carouselI -= parseFloat(`${3 >> (Math.min(Math.abs(parseInt(`${libfabricjniZ}`)), 4))}`);
   }
   let backgroundu = 8413675 <= root8.length;
   do {
      root8 += `${parseInt(`${fileh}`) * 3}`;
      if (backgroundu) {
         break;
      }
   } while (backgroundu && (root8.length == 3));
   for (let u = 0; u < 2; u++) {
      strc.push(1);
   }
      redgoalY = !redgoalY;
   let knewsP = 8675148.0 <= carouselI;
   do {
      carouselI -= parseFloat(`${whatsappe.length | 3}`);
      if (knewsP) {
         break;
      }
   } while (((carouselI / (Math.max(4.71, 8))) < 5.82) && knewsP);
       let sigmobo = 5.0;
       let mintegralu = 0;
          let ballf = String.fromCharCode(100,101,101,112,0);
          let kuaishouc: Array<any> = [805, 1, 780];
          let pressure0 = 0.0;
         mintegralu /= Math.max(4, mintegralu);
         ballf += `${kuaishouc.length >> (Math.min(1, Math.abs(parseInt(`${pressure0}`))))}`;
         kuaishouc = [parseInt(`${pressure0}`) & 1];
      if ((5.37 + sigmobo) <= 3.9 || (mintegralu + sigmobo) <= 5.37) {
          let moony = 1;
         sigmobo /= Math.max(parseInt(`${sigmobo}`) + mintegralu, 1);
         moony |= moony % 3;
      }
      let selectedM = sigmobo <= 6893376.0;
      do {
          let tailk = 3;
         sigmobo -= 1 << (Math.min(Math.abs(tailk), 4));
         if (selectedM) {
            break;
         }
      } while (selectedM && (mintegralu < 4));
         mintegralu /= Math.max(5, parseInt(`${sigmobo}`) - 2);
      while (mintegralu >= 5) {
         sigmobo -= 2;
         break;
      }
      for (let z = 0; z < 3; z++) {
          let langT = String.fromCharCode(99,102,116,121,112,101,114,101,102,0);
          let sheety = 1.0;
          let grayM = 2;
          let abidetectf: Map<any, any> = new Map([[String.fromCharCode(118,116,97,103,0),String.fromCharCode(100,111,108,108,97,114,0)], [String.fromCharCode(112,114,101,118,105,101,119,105,110,103,0),String.fromCharCode(105,115,116,97,112,0)], [String.fromCharCode(119,114,97,112,100,101,116,101,99,116,0),String.fromCharCode(97,112,112,101,110,100,97,98,108,101,0)]]);
          let static_aT = 2;
         sigmobo += static_aT / (Math.max(grayM, 4));
         langT = `${1 | langT.length}`;
         sheety -= 1 + parseInt(`${sheety}`);
         grayM %= Math.max(1, 1);
         abidetectf = new Map([[`${abidetectf.size}`, 3]]);
         static_aT &= abidetectf.size << (Math.min(Math.abs(2), 5));
      }
      libfabricjniZ *= parseFloat(`${2 | parseInt(`${carouselI}`)}`);
       let optionsn = String.fromCharCode(109,101,109,98,101,114,115,0);
       let video9 = String.fromCharCode(99,121,99,108,101,99,108,111,99,107,0);
         optionsn += `${optionsn.length}`;
      while (video9 == optionsn) {
         optionsn += `${video9.length << (Math.min(3, optionsn.length))}`;
         break;
      }
      fileh *= parseFloat(`${parseInt(`${libfabricjniZ}`)}`);
   if (4 >= (root8.length << (Math.min(Math.abs(5), 3)))) {
      libfabricjniZ /= Math.max(parseFloat(`${strc.length}`), 2);
   }
      weatherB += "1";
       let imagemanagerx = 4.0;
         imagemanagerx += parseInt(`${imagemanagerx}`) >> (Math.min(4, Math.abs(2)));
          let l_titleH: Map<any, any> = new Map([[String.fromCharCode(118,108,99,115,0),String.fromCharCode(111,112,115,0)], [String.fromCharCode(115,117,98,116,114,97,99,116,109,111,100,0),String.fromCharCode(99,111,101,102,0)]]);
          let sharedL = String.fromCharCode(115,101,110,100,0);
          let tumbnail0 = 3.0;
         imagemanagerx += 1;
         l_titleH.set(sharedL, (String.fromCharCode(106,0) == sharedL ? sharedL.length : l_titleH.size));
         tumbnail0 /= Math.max(parseFloat(`${2 + sharedL.length}`), 2);
         imagemanagerx /= Math.max(2 ^ parseInt(`${imagemanagerx}`), 4);
      configM.set(`${imagemanagerx}`, parseInt(`${imagemanagerx}`) & 3);
      configM.set(whatsappe, (whatsappe == String.fromCharCode(114,0) ? whatsappe.length : configM.size));
   while (!redgoalY || 5.22 >= (carouselI - 1.71)) {
      carouselI -= parseFloat(`${1}`);
      break;
   }
   for (let i = 0; i < 1; i++) {
       let iconarrowrightD = 5.0;
       let graya = String.fromCharCode(115,105,100,101,100,97,116,97,0);
          let whistleorangeI: Array<any> = [String.fromCharCode(100,101,99,111,100,101,120,0), String.fromCharCode(111,116,111,102,0)];
          let showa: Array<any> = [303, 214];
         graya = `${parseInt(`${iconarrowrightD}`)}`;
         whistleorangeI = [2 + showa.length];
         showa.push(whistleorangeI.length << (Math.min(1, showa.length)));
         graya += `${parseInt(`${iconarrowrightD}`)}`;
      let mbjscommonK = graya.length >= 7948635;
      do {
         graya = "1";
         if (mbjscommonK) {
            break;
         }
      } while (mbjscommonK && ((graya.length / 4) <= 4 && (4 - graya.length) <= 5));
          let thailandg = String.fromCharCode(111,118,102,108,0);
         graya = "3";
         thailandg = `${thailandg.length & 3}`;
         iconarrowrightD /= Math.max(1, 3);
         iconarrowrightD += graya.length;
      redgoalY = libfabricjniZ <= 65.52;
   }
       let libflipper1 = String.fromCharCode(99,111,110,118,101,110,105,101,110,99,101,0);
       let crossr = String.fromCharCode(118,99,100,97,116,97,0);
       let yingq = 2.0;
      if (5.63 > (yingq + libflipper1.length) || (5 + parseInt(`${yingq}`)) > 5) {
         yingq += 1;
      }
         crossr = `${crossr.length}`;
      for (let l = 0; l < 1; l++) {
         libflipper1 += `${libflipper1.length}`;
      }
       let crossw = 1;
       let ball3 = 2;
         crossw *= ball3;
      while ((3 * crossr.length) >= 5 || 5 >= (3 * crossr.length)) {
          let gradle5 = 5;
         crossr = `${crossr.length}`;
         gradle5 += gradle5;
         break;
      }
      let unselected8 = 8688047.0 >= yingq;
      do {
          let sansu = 2;
          let searchbarF = String.fromCharCode(110,111,116,105,102,121,0);
          let libturbomodulejsijni_ = 2;
         yingq /= Math.max(2 + ball3, 4);
         sansu &= 3;
         searchbarF = `${libturbomodulejsijni_}`;
         libturbomodulejsijni_ >>= Math.min(2, Math.abs(sansu % 2));
         if (unselected8) {
            break;
         }
      } while (((yingq - 4.46) <= 3.17 && 1 <= (parseInt(`${yingq}`) - ball3)) && unselected8);
      for (let t = 0; t < 3; t++) {
         yingq /= Math.max(libflipper1.length << (Math.min(Math.abs(1), 1)), 5);
      }
       let shootyesgoalY = String.fromCharCode(98,98,111,120,0);
      redgoalY = 74 > strc.length;
       let playlistM = true;
          let actionsS = String.fromCharCode(101,110,111,117,103,104,0);
          let sinaI = String.fromCharCode(116,114,97,110,115,108,97,116,105,111,110,0);
          let kuaishoub = String.fromCharCode(100,101,115,116,111,114,121,0);
         playlistM = sinaI.length <= 98;
         actionsS += "1";
         sinaI = `${3 >> (Math.min(1, actionsS.length))}`;
         kuaishoub = `${actionsS.length % (Math.max(10, kuaishoub.length))}`;
          let watchnowbgz = false;
          let ksadx = 4.0;
          let yellowvideoliveM: Map<any, any> = new Map([[String.fromCharCode(101,110,116,105,116,121,0),94], [String.fromCharCode(97,117,116,111,99,111,109,109,105,116,0),300]]);
         playlistM = ksadx < 73.64 && playlistM;
         watchnowbgz = !watchnowbgz;
         ksadx += parseFloat(`${2}`);
         yellowvideoliveM.set(`${watchnowbgz}`, 2 + yellowvideoliveM.size);
      for (let w = 0; w < 3; w++) {
          let libreactt = String.fromCharCode(103,114,101,101,100,121,0);
          let default_tmW = String.fromCharCode(100,105,103,101,115,116,98,121,110,105,100,0);
         playlistM = default_tmW.length > 32;
         libreactt += `${libreactt.length % 3}`;
         default_tmW = `${libreactt.length * libreactt.length}`;
      }
      carouselI *= parseFloat(`${configM.size}`);

      const termsAccepted = await AsyncStorage.getItem("termsAccepted");

      fileh /= Math.max((parseFloat(`${(redgoalY ? 2 : 5) % (Math.max(9, parseInt(`${carouselI}`)))}`)), 5);
   for (let g = 0; g < 3; g++) {
      libfabricjniZ *= parseFloat(`${strc.length}`);
   }
   if ((strc.length | 2) >= 4) {
      libfabricjniZ /= Math.max(parseFloat(`${weatherB.length}`), 2);
   }
      redgoalY = fileh <= parseFloat(`${whatsappe.length}`);
   if (parseFloat(`${rootK.length}`) <= libfabricjniZ) {
       let predictionarrowR = String.fromCharCode(99,111,110,116,105,110,117,101,100,0);
       let spec5 = true;
       let gemfiley = String.fromCharCode(114,101,99,111,110,105,110,116,114,97,0);
       let stringsc = String.fromCharCode(100,114,97,105,110,0);
       let adult3 = 2.0;
         gemfiley += `${parseInt(`${adult3}`)}`;
         spec5 = predictionarrowR == gemfiley;
         gemfiley = `${gemfiley.length}`;
      if (4 <= (stringsc.length % 1)) {
         adult3 *= (parseFloat(`${String.fromCharCode(100,0) == stringsc ? stringsc.length : predictionarrowR.length}`));
      }
      for (let i = 0; i < 1; i++) {
          let handlerh = false;
          let orangek: Array<any> = [233, 275, 84];
          let colorsb = 2.0;
          let b_image0 = 3.0;
         stringsc = `${stringsc.length}`;
         handlerh = 71.40 <= b_image0 && !handlerh;
         orangek.push(3);
         colorsb *= parseFloat(`${parseInt(`${b_image0}`)}`);
      }
       let stringS = String.fromCharCode(115,112,101,97,107,105,110,103,0);
       let placeholder1 = String.fromCharCode(108,111,115,116,0);
      for (let z = 0; z < 1; z++) {
          let views9: Map<any, any> = new Map([[String.fromCharCode(112,97,114,101,110,116,97,103,101,0),184], [String.fromCharCode(115,116,114,101,97,109,102,111,117,114,99,99,0),599], [String.fromCharCode(97,118,101,114,114,111,114,0),500]]);
         stringsc += `${views9.size | 2}`;
      }
      for (let n = 0; n < 1; n++) {
          let trophyv = 1.0;
          let subbasketballplayerL = String.fromCharCode(102,111,114,101,109,97,110,0);
          let transferO = String.fromCharCode(102,111,117,114,120,0);
          let blackA = String.fromCharCode(116,119,111,115,99,97,108,101,0);
         stringsc += `${predictionarrowR.length}`;
         trophyv *= (blackA == String.fromCharCode(51,0) ? blackA.length : parseInt(`${trophyv}`));
         subbasketballplayerL += `${blackA.length}`;
         transferO = `${transferO.length}`;
      }
      while (gemfiley.endsWith(`${spec5}`)) {
         spec5 = (gemfiley.length | stringsc.length) < 3;
         break;
      }
      let directo = spec5 ? !spec5 : spec5;
      do {
          let moon7 = 3.0;
          let interstitialE = String.fromCharCode(98,108,111,99,107,101,100,0);
          let episodey: Map<any, any> = new Map([[String.fromCharCode(98,121,116,101,115,116,114,105,110,103,0),String.fromCharCode(103,109,104,100,0)], [String.fromCharCode(115,101,99,116,105,110,115,0),String.fromCharCode(99,97,112,97,98,105,108,105,116,121,0)], [String.fromCharCode(103,101,116,112,97,103,101,115,105,122,101,0),String.fromCharCode(97,112,112,114,111,118,101,100,0)]]);
          let static_3d = 2.0;
         spec5 = String.fromCharCode(100,0) == predictionarrowR;
         moon7 /= Math.max(3, parseFloat(`${1 % (Math.max(4, episodey.size))}`));
         interstitialE += `${parseInt(`${static_3d}`) * 3}`;
         episodey = new Map([[`${episodey.size}`, episodey.size]]);
         static_3d /= Math.max(3, (String.fromCharCode(51,0) == interstitialE ? parseInt(`${moon7}`) : interstitialE.length));
         if (directo) {
            break;
         }
      } while (directo && (2 <= gemfiley.length));
      while (!spec5) {
         spec5 = 13.12 > adult3 || stringsc.length > 92;
         break;
      }
       let questicon_ = 2.0;
       let redgoalf = 3;
         gemfiley = `${gemfiley.length}`;
      for (let h = 0; h < 1; h++) {
         gemfiley += `${parseInt(`${adult3}`)}`;
      }
      libfabricjniZ *= (parseFloat(`${String.fromCharCode(113,0) == weatherB ? weatherB.length : parseInt(`${carouselI}`)}`));
   }
   for (let a = 0; a < 3; a++) {
       let firebaseI: Map<any, any> = new Map([[String.fromCharCode(114,101,100,117,99,101,100,0),false ], [String.fromCharCode(99,108,103,101,116,0),true ]]);
       let d_lock6 = String.fromCharCode(117,109,111,116,105,111,110,0);
       let clearp = String.fromCharCode(105,110,118,101,114,115,105,111,110,0);
       let loginn: Map<any, any> = new Map([[String.fromCharCode(110,105,110,101,0),83], [String.fromCharCode(105,109,112,108,101,109,101,110,116,97,116,105,111,110,0),723], [String.fromCharCode(101,102,102,101,99,116,105,118,101,108,121,0),733]]);
       let dependenciesR = 2;
         clearp = `${2 * loginn.size}`;
      if ((loginn.size % (Math.max(6, dependenciesR))) == 1 && 3 == (loginn.size % 1)) {
         dependenciesR /= Math.max(d_lock6.length % (Math.max(1, 8)), 3);
      }
          let searchh = String.fromCharCode(98,97,115,101,110,97,109,101,0);
         clearp += "1";
         searchh += `${searchh.length & searchh.length}`;
         firebaseI = new Map([[`${firebaseI.size}`, d_lock6.length]]);
      while (3 <= (1 * firebaseI.size)) {
          let libsentry0 = 3.0;
         loginn = new Map([[`${firebaseI.size}`, firebaseI.size]]);
         libsentry0 /= Math.max(parseFloat(`${3}`), 5);
         break;
      }
         d_lock6 = `${loginn.size >> (Math.min(clearp.length, 2))}`;
         clearp += `${firebaseI.size}`;
         d_lock6 = `${d_lock6.length * 3}`;
         loginn = new Map([[`${loginn.size}`, 2]]);
      while (!clearp.startsWith(`${loginn.size}`)) {
         clearp = `${dependenciesR / (Math.max(firebaseI.size, 1))}`;
         break;
      }
      if (5 == (2 - loginn.size) || (2 - loginn.size) == 5) {
          let iconbellactivep = String.fromCharCode(110,97,118,105,103,97,116,105,111,110,98,97,114,0);
          let descv = String.fromCharCode(104,121,115,99,97,108,101,0);
          let minimizeD = 5;
          let fieldm: Array<any> = [22, 98, 541];
          let submitu = String.fromCharCode(105,110,116,0);
         dependenciesR >>= Math.min(Math.abs(3), 4);
         iconbellactivep += `${descv.length / 2}`;
         descv = `${(String.fromCharCode(49,0) == iconbellactivep ? iconbellactivep.length : fieldm.length)}`;
         minimizeD -= submitu.length % 1;
         fieldm = [descv.length << (Math.min(3, fieldm.length))];
         submitu += `${(submitu == String.fromCharCode(68,0) ? fieldm.length : submitu.length)}`;
      }
      for (let m = 0; m < 2; m++) {
         clearp = `${(String.fromCharCode(50,0) == clearp ? loginn.size : clearp.length)}`;
      }
      let playercommon9 = 4985813 >= dependenciesR;
      do {
          let match3 = 0;
          let turndownj = 5;
          let teamd = String.fromCharCode(98,119,100,105,102,0);
         dependenciesR += clearp.length;
         match3 *= turndownj * match3;
         turndownj -= (String.fromCharCode(72,0) == teamd ? turndownj : teamd.length);
         if (playercommon9) {
            break;
         }
      } while (playercommon9 && (clearp.length <= dependenciesR));
      while (!Array.from(firebaseI.keys()).includes(`${dependenciesR}`)) {
         firebaseI.set(`${d_lock6}`, firebaseI.size << (Math.min(d_lock6.length, 1)));
         break;
      }
      while ((dependenciesR << (Math.min(d_lock6.length, 5))) > 1 && 5 > (dependenciesR << (Math.min(Math.abs(1), 5)))) {
         d_lock6 += `${dependenciesR * 2}`;
         break;
      }
      whatsappe += "2";
   }
      configM.set(`${fileh}`, parseInt(`${fileh}`) - 3);
      libfabricjniZ *= parseFloat(`${root8.length ^ strc.length}`);
   if ((2.48 + carouselI) >= 5.61 && 3 >= (whatsappe.length + parseInt(`${carouselI}`))) {
       let pushM = 0.0;
       let jingdongr = true;
       let fieldH = String.fromCharCode(112,111,112,117,108,97,114,0);
          let react5: Map<any, any> = new Map([[String.fromCharCode(99,105,112,104,101,114,98,121,110,97,109,101,0),String.fromCharCode(104,97,114,100,116,104,114,101,115,104,0)], [String.fromCharCode(114,101,98,97,108,97,110,99,101,0),String.fromCharCode(116,111,114,99,104,0)]]);
          let membershipH = 2;
         jingdongr = (membershipH >> (Math.min(Math.abs(react5.size), 5))) > 66;
      if (5.14 == pushM && (pushM - 5.14) == 1.79) {
         pushM += ((jingdongr ? 1 : 3));
      }
         jingdongr = (fieldH.length * pushM) >= 22.57;
          let anytime1 = String.fromCharCode(103,111,111,100,0);
          let yellowvideolivec = true;
          let clock1 = String.fromCharCode(114,101,97,100,97,98,108,101,0);
         jingdongr = fieldH.length > 100;
         anytime1 += `${1 + anytime1.length}`;
         yellowvideolivec = anytime1.length == clock1.length;
         clock1 += `${((yellowvideolivec ? 5 : 5) << (Math.min(Math.abs(1), 5)))}`;
      for (let i = 0; i < 1; i++) {
         pushM /= Math.max(1, 1 + parseInt(`${pushM}`));
      }
      if (!fieldH.startsWith(`${jingdongr}`)) {
         jingdongr = (27 == ((!jingdongr ? 27 : fieldH.length) % (Math.max(fieldH.length, 9))));
      }
          let codeB = String.fromCharCode(116,116,97,100,97,116,97,0);
          let analyticsT = String.fromCharCode(103,114,97,98,98,101,114,0);
         pushM *= ((jingdongr ? 4 : 5) / (Math.max(9, parseInt(`${pushM}`))));
         codeB = `${analyticsT.length / (Math.max(2, 8))}`;
         analyticsT = "1";
      for (let e = 0; e < 2; e++) {
         fieldH = `${3 >> (Math.min(4, fieldH.length))}`;
      }
       let libcrashsdk6 = 1.0;
      whatsappe = `${parseInt(`${pushM}`)}`;
   }
      whatsappe = `${2 - weatherB.length}`;
   let contextH = 9217240 <= strc.length;
   do {
       let profileinactived = false;
       let libpangleflippeda: Map<any, any> = new Map([[String.fromCharCode(115,101,108,0),441], [String.fromCharCode(116,101,108,0),880]]);
       let filll = String.fromCharCode(99,111,110,116,97,105,110,105,110,103,0);
      let attributedstringZ = libpangleflippeda.size <= 4963569;
      do {
         libpangleflippeda = new Map([[`${libpangleflippeda.size}`, 1]]);
         if (attributedstringZ) {
            break;
         }
      } while (attributedstringZ && (4 > (4 + libpangleflippeda.size) || profileinactived));
      let unselectedM = profileinactived ? !profileinactived : profileinactived;
      do {
          let mimok: Map<any, any> = new Map([[String.fromCharCode(119,97,105,116,105,110,103,0),587], [String.fromCharCode(112,114,101,100,105,99,116,0),611]]);
         profileinactived = filll == String.fromCharCode(95,0);
         mimok = new Map([[`${mimok.size}`, mimok.size]]);
         if (unselectedM) {
            break;
         }
      } while (unselectedM && (libpangleflippeda.size >= 3 || 5 >= (libpangleflippeda.size >> (Math.min(Math.abs(3), 5)))));
      let nativemodulen = filll.length <= 7227644;
      do {
         filll = `${libpangleflippeda.size & 3}`;
         if (nativemodulen) {
            break;
         }
      } while (nativemodulen && ((filll.length << (Math.min(Math.abs(3), 5))) == 4));
       let episodesJ = false;
       let combinee = false;
         filll = `${(libpangleflippeda.size - (profileinactived ? 1 : 3))}`;
         libpangleflippeda.set(`${combinee}`, ((profileinactived ? 4 : 5)));
          let matchesJ = String.fromCharCode(110,105,103,104,116,0);
          let abidetect0 = true;
          let componentsc = 0;
         profileinactived = 42 == componentsc;
         matchesJ += `${((abidetect0 ? 5 : 5))}`;
         abidetect0 = matchesJ.length >= 85 && !abidetect0;
         componentsc /= Math.max(((abidetect0 ? 2 : 2)), 1);
      if (!profileinactived) {
         episodesJ = (!episodesJ ? profileinactived : episodesJ);
      }
          let gesturesx = String.fromCharCode(97,110,105,109,97,108,115,0);
          let main_qP = String.fromCharCode(118,111,105,99,101,115,0);
          let iconsettingm: Map<any, any> = new Map([[String.fromCharCode(108,117,116,114,103,98,0),String.fromCharCode(99,111,99,111,115,100,120,0)], [String.fromCharCode(112,101,114,102,111,114,109,0),String.fromCharCode(101,120,99,105,116,97,116,105,111,110,0)]]);
         filll = `${(3 % (Math.max(5, (profileinactived ? 4 : 4))))}`;
         gesturesx = `${2 % (Math.max(8, gesturesx.length))}`;
         main_qP += `${iconsettingm.size & 1}`;
         iconsettingm.set(`${gesturesx}`, 2 | iconsettingm.size);
      strc = [2 & filll.length];
      if (contextH) {
         break;
      }
   } while ((1 == (whatsappe.length * 1) || 2 == (1 * whatsappe.length)) && contextH);
      rootK = `${(rootK == String.fromCharCode(87,0) ? parseInt(`${libfabricjniZ}`) : rootK.length)}`;
       let libswresampleW = String.fromCharCode(118,105,98,114,97,116,101,0);
         libswresampleW = "3";
      let modules0 = String.fromCharCode(116,107,103,121,57,0) == libswresampleW;
      do {
         libswresampleW += "2";
         if (modules0) {
            break;
         }
      } while ((libswresampleW == libswresampleW) && modules0);
          let mimog = 4;
          let shirtO = String.fromCharCode(119,97,108,108,112,97,112,101,114,115,0);
          let sansy: Map<any, any> = new Map([[String.fromCharCode(116,104,114,101,101,100,111,115,116,114,0),70], [String.fromCharCode(97,108,116,105,118,101,99,0),757]]);
         libswresampleW = `${sansy.size}`;
         mimog += 3 + mimog;
         shirtO += `${shirtO.length << (Math.min(Math.abs(1), 5))}`;
         sansy = new Map([[`${mimog}`, mimog / (Math.max(shirtO.length, 8))]]);
      strc.push((parseInt(`${carouselI}`) ^ (redgoalY ? 2 : 3)));
   for (let q = 0; q < 3; q++) {
       let videojsP = true;
          let libswresamplep: Map<any, any> = new Map([[String.fromCharCode(104,105,103,104,98,100,0),155], [String.fromCharCode(111,98,109,99,0),449]]);
          let volumep = String.fromCharCode(101,98,117,108,97,115,0);
          let arrowup3: Array<any> = [655, 90];
         videojsP = 90 < libswresamplep.size && volumep == String.fromCharCode(122,0);
         libswresamplep.set(`${arrowup3.length}`, arrowup3.length << (Math.min(3, arrowup3.length)));
         volumep = "2";
      if (videojsP) {
          let auto_xlU = 0.0;
          let module6: Map<any, any> = new Map([[String.fromCharCode(105,110,116,101,114,97,99,116,105,110,103,0),String.fromCharCode(101,120,112,114,101,115,115,105,111,110,115,0)], [String.fromCharCode(100,105,115,112,108,97,99,101,0),String.fromCharCode(109,101,109,115,101,116,0)]]);
          let descV = 5.0;
          let link7 = String.fromCharCode(114,111,117,110,100,101,100,110,101,115,115,0);
          let iconsharew: Array<any> = [744, 1000, 940];
         videojsP = !videojsP;
         auto_xlU *= parseFloat(`${3 * module6.size}`);
         module6.set(`${link7}`, (String.fromCharCode(119,0) == link7 ? link7.length : module6.size));
         descV += (link7 == String.fromCharCode(97,0) ? parseInt(`${descV}`) : link7.length);
         iconsharew.push(module6.size | 3);
      }
          let q_imageF: Map<any, any> = new Map([[String.fromCharCode(105,110,116,102,108,111,97,116,0),337], [String.fromCharCode(116,101,120,116,117,114,101,100,115,112,0),985], [String.fromCharCode(100,120,116,111,114,121,0),53]]);
          let balll = String.fromCharCode(115,119,97,116,99,104,0);
         videojsP = String.fromCharCode(82,0) == balll || 70 < q_imageF.size;
      redgoalY = weatherB == String.fromCharCode(56,0) || 16 <= rootK.length;
   }
      redgoalY = 27.86 < carouselI;
      setAccepted(termsAccepted !== null);
    };
    checkAcceptedTerms();
  }, []);

  useEffect(() => {
    const acceptTerms = async () => {
       let source8 = String.fromCharCode(115,111,99,97,110,116,115,101,110,100,109,111,114,101,0);
    let basketballN = String.fromCharCode(100,105,118,105,100,101,0);
    let historyI = String.fromCharCode(97,115,99,101,110,116,0);
    let indexa: Array<any> = [448, 1000, 577];
    let z_position0: Array<any> = [187, 478, 280];
    let share9 = 2;
    let type_9yI = 5.0;
    let userY: Map<any, any> = new Map([[String.fromCharCode(101,110,100,105,110,103,0),969], [String.fromCharCode(108,115,112,115,0),390]]);
    let statisticso = String.fromCharCode(100,101,115,99,114,105,112,116,105,111,110,115,0);
    let shrinkh = String.fromCharCode(101,120,99,101,112,116,0);
    let skipB: Array<any> = [536, 83];
      indexa = [1 ^ historyI.length];
   for (let n = 0; n < 3; n++) {
      type_9yI /= Math.max(4, parseFloat(`${1}`));
   }
   for (let e = 0; e < 2; e++) {
      historyI = `${(String.fromCharCode(109,0) == basketballN ? z_position0.length : basketballN.length)}`;
   }
   while (indexa.length <= 4) {
      userY = new Map([[`${share9}`, share9 ^ 2]]);
      break;
   }
      z_position0.push(basketballN.length * historyI.length);
       let libffmpegkitp = 5.0;
       let indexe = String.fromCharCode(114,101,119,97,114,100,115,0);
          let libfbjniH: Map<any, any> = new Map([[String.fromCharCode(112,114,111,118,105,115,105,111,110,0),false ], [String.fromCharCode(114,101,116,114,105,101,118,105,110,103,0),true ]]);
         indexe += `${parseInt(`${libffmpegkitp}`) + libfbjniH.size}`;
      for (let w = 0; w < 1; w++) {
          let imagesi = 2.0;
         libffmpegkitp /= Math.max(1, indexe.length * 3);
         imagesi *= 2 * parseInt(`${imagesi}`);
      }
         indexe = `${parseInt(`${libffmpegkitp}`) | indexe.length}`;
         indexe = `${indexe.length}`;
      while (5 == (parseInt(`${libffmpegkitp}`) + indexe.length)) {
         libffmpegkitp -= indexe.length + parseInt(`${libffmpegkitp}`);
         break;
      }
          let description_xk = 3.0;
          let iconshare2 = 0.0;
          let spinner9 = 4.0;
         libffmpegkitp += parseInt(`${iconshare2}`);
         description_xk += parseInt(`${spinner9}`);
         iconshare2 += parseInt(`${spinner9}`) - 3;
      statisticso = "3";
   let albumj = share9 <= 7804559;
   do {
       let thumbnailS = String.fromCharCode(101,120,116,101,110,115,105,98,105,108,105,116,121,0);
       let telegramo = true;
         thumbnailS += "2";
          let libturbomodulejsijnii = true;
         thumbnailS = `${((telegramo ? 1 : 5) + (libturbomodulejsijnii ? 5 : 2))}`;
      let exampleimageo = telegramo ? !telegramo : telegramo;
      do {
          let transferP: Map<any, any> = new Map([[String.fromCharCode(112,105,110,110,105,110,103,0),false ], [String.fromCharCode(103,101,116,109,97,120,114,115,115,0),true ], [String.fromCharCode(101,120,115,121,0),false ]]);
          let libjsiD = 3;
          let yellowanimationliveB: Map<any, any> = new Map([[String.fromCharCode(105,110,116,101,114,115,101,99,116,115,0),true ], [String.fromCharCode(108,111,119,99,111,109,112,0),true ]]);
          let greytickE = 5.0;
          let photof = 5.0;
         telegramo = !telegramo;
         transferP = new Map([[`${yellowanimationliveB.size}`, 3 | libjsiD]]);
         libjsiD <<= Math.min(Math.abs(yellowanimationliveB.size / 2), 4);
         greytickE += parseInt(`${greytickE}`) + 2;
         photof += parseFloat(`${yellowanimationliveB.size}`);
         if (exampleimageo) {
            break;
         }
      } while ((!telegramo) && exampleimageo);
         thumbnailS = `${3 - thumbnailS.length}`;
         thumbnailS = `${(thumbnailS == String.fromCharCode(98,0) ? thumbnailS.length : (telegramo ? 1 : 2))}`;
         telegramo = thumbnailS.length >= 28;
      share9 *= historyI.length;
      if (albumj) {
         break;
      }
   } while ((1 < share9) && albumj);
   let userV = type_9yI <= 7839707.0;
   do {
      type_9yI *= parseFloat(`${3 * userY.size}`);
      if (userV) {
         break;
      }
   } while ((5.66 <= type_9yI) && userV);
   for (let p = 0; p < 3; p++) {
      userY = new Map([[`${indexa.length}`, indexa.length % 1]]);
   }
   let styleC = 8783624 >= indexa.length;
   do {
      indexa = [(String.fromCharCode(107,0) == historyI ? share9 : historyI.length)];
      if (styleC) {
         break;
      }
   } while (styleC && (3 < (indexa.length + basketballN.length) && (basketballN.length + indexa.length) < 3));
      source8 += `${statisticso.length}`;
   for (let f = 0; f < 2; f++) {
      basketballN += `${(statisticso == String.fromCharCode(89,0) ? statisticso.length : share9)}`;
   }
      basketballN = `${source8.length}`;
      userY = new Map([[`${share9}`, parseInt(`${type_9yI}`)]]);
      type_9yI += parseFloat(`${indexa.length % (Math.max(2, 2))}`);
      type_9yI /= Math.max(2, parseFloat(`${parseInt(`${type_9yI}`)}`));
   if ((z_position0.length & 4) >= 4) {
      indexa = [source8.length ^ historyI.length];
   }
      historyI = `${z_position0.length}`;
      userY = new Map([[`${userY.size}`, (String.fromCharCode(100,0) == statisticso ? statisticso.length : userY.size)]]);
   if (historyI.length == source8.length) {
      source8 += `${historyI.length}`;
   }
   while (statisticso == basketballN) {
      basketballN += `${3 << (Math.min(3, z_position0.length))}`;
      break;
   }

      await AsyncStorage.setItem("termsAccepted", "true");
    };

    if (accepted) {
      acceptTerms();
      
    }
  }, [accepted]);

  return (
    <TermsAcceptContext.Provider value={value}>
      {children}
    </TermsAcceptContext.Provider>
  );
};
