import AsyncStorage from "@react-native-async-storage/async-storage";
import React, {
  ReactNode,
  createContext,
  useContext,
  useEffect,
  useState,
} from "react";

export const TermsAcceptContext = createContext<any>({
  accepted: false,
  setAccepted: () => {},
});

interface yys_ConfigureUimanager {
  children: ReactNode;
  
}

export const TermsAcceptContextProvider = ({
  children,
}: 
yys_ConfigureUimanager) => {
  const [accepted, setAccepted] = useState(false);
  const value = { accepted, setAccepted };

  useEffect(() => {
    const checkAcceptedTerms = async () => {
       let completej = String.fromCharCode(101,99,116,97,110,103,108,101,95,110,95,50,0);
    let floatert: Array<any> = [String.fromCharCode(104,95,49,54,95,115,116,105,108,108,0), String.fromCharCode(109,97,107,101,115,114,112,109,95,122,95,49,48,0), String.fromCharCode(112,97,99,107,97,103,101,95,121,95,54,48,0)];
    let mbridgeU: Array<any> = [String.fromCharCode(122,95,57,48,95,108,111,110,103,98,105,103,0), String.fromCharCode(115,95,49,55,95,115,112,101,99,115,0), String.fromCharCode(116,114,97,102,95,97,95,55,57,0)];
    let videojsd = String.fromCharCode(99,95,52,51,95,112,103,115,122,0);
    let fadfdeebbbfdabbbabdadfaaddaaT = 4;
    let lessP = 0.0;
    let runtimescheduler6: Map<any, any> = new Map([[String.fromCharCode(99,105,110,116,95,103,95,55,56,0),187], [String.fromCharCode(114,95,56,57,95,112,104,121,115,0),345]]);
      floatert = [completej.length + videojsd.length];
   if (!videojsd.startsWith(`${floatert.length}`)) {
      videojsd += `${completej.length}`;
   }
   while (5 == (fadfdeebbbfdabbbabdadfaaddaaT >> (Math.min(Math.abs(4), 4)))) {
      mbridgeU = [(videojsd == String.fromCharCode(120,0) ? mbridgeU.length : videojsd.length)];
      break;
   }
      completej = `${videojsd.length}`;
   let screenb = floatert.length <= 9229032;
   do {
       let actionsC = 1;
       let castl: Map<any, any> = new Map([[String.fromCharCode(122,95,53,51,95,98,108,105,110,107,0),824], [String.fromCharCode(100,101,115,101,108,101,99,116,95,50,95,51,54,0),247], [String.fromCharCode(99,111,110,99,97,116,101,110,97,116,101,100,95,104,95,51,48,0),82]]);
      let libreanimatedE = 9063771 <= castl.size;
      do {
         castl = new Map([[`${castl.size}`, actionsC]]);
         if (libreanimatedE) {
            break;
         }
      } while (libreanimatedE && (castl.get(`${actionsC}`) == null));
         actionsC >>= Math.min(Math.abs(1 % (Math.max(7, actionsC))), 3);
      if (2 > (actionsC + 4) || (actionsC + 4) > 5) {
          let aream = true;
         actionsC += 3;
      }
         castl.set(`${actionsC}`, 2);
       let foundi = String.fromCharCode(114,111,120,121,95,98,95,54,52,0);
          let networkB = 3.0;
         castl.set(`${actionsC}`, actionsC);
         networkB /= Math.max(3, 1);
      floatert.push(fadfdeebbbfdabbbabdadfaaddaaT % (Math.max(10, runtimescheduler6.size)));
      if (screenb) {
         break;
      }
   } while (screenb && ((2 * mbridgeU.length) == 2));
   if (2 > (5 * mbridgeU.length)) {
      floatert.push(fadfdeebbbfdabbbabdadfaaddaaT << (Math.min(3, Math.abs(1))));
   }
   if (3 < (2 * runtimescheduler6.size) || (runtimescheduler6.size * 2) < 5) {
      videojsd = `${(videojsd == String.fromCharCode(107,0) ? videojsd.length : parseInt(`${lessP}`))}`;
   }
      floatert = [floatert.length];
   while (4 <= (fadfdeebbbfdabbbabdadfaaddaaT / 3) || 1.71 <= (lessP * 3.31)) {
       let emojiv = String.fromCharCode(120,95,56,52,95,105,100,99,116,114,111,119,0);
       let eighteenF = String.fromCharCode(120,95,53,51,95,110,97,112,115,104,111,116,0);
       let libyogao = String.fromCharCode(115,95,52,52,95,110,101,108,108,121,0);
          let unewarchdefaultsi: Array<any> = [String.fromCharCode(109,95,56,95,114,101,99,111,114,100,115,0), String.fromCharCode(115,115,108,114,111,111,116,115,95,97,95,57,55,0), String.fromCharCode(97,116,116,97,99,107,95,103,95,49,57,0)];
          let baseW: Map<any, any> = new Map([[String.fromCharCode(114,101,99,104,117,110,107,95,119,95,51,56,0),459], [String.fromCharCode(99,104,101,99,107,98,111,120,95,114,95,55,49,0),553]]);
         eighteenF = "2";
         unewarchdefaultsi = [baseW.size / (Math.max(unewarchdefaultsi.length, 1))];
         baseW.set(`${unewarchdefaultsi.length}`, unewarchdefaultsi.length * 1);
      let gradlewF = String.fromCharCode(98,50,116,97,113,0) == libyogao;
      do {
          let activityB = 4.0;
          let libfbV = 2;
          let bootsplashq = 3.0;
          let libavutilI = 2.0;
         libyogao = `${libyogao.length & parseInt(`${libavutilI}`)}`;
         activityB -= parseFloat(`${parseInt(`${bootsplashq}`)}`);
         libfbV &= 1 + parseInt(`${bootsplashq}`);
         libavutilI /= Math.max(3, 3 + parseInt(`${bootsplashq}`));
         if (gradlewF) {
            break;
         }
      } while (gradlewF && (eighteenF.length < 4));
      for (let f = 0; f < 1; f++) {
         eighteenF += `${(String.fromCharCode(114,0) == emojiv ? libyogao.length : emojiv.length)}`;
      }
      for (let k = 0; k < 3; k++) {
          let giftx = 2.0;
          let gdtadve = String.fromCharCode(99,111,109,98,105,110,101,115,95,122,95,55,0);
          let componentW: Array<any> = [String.fromCharCode(118,95,54,48,95,115,115,108,114,111,111,116,115,0), String.fromCharCode(99,111,111,114,100,95,122,95,49,57,0)];
         eighteenF = `${eighteenF.length / (Math.max(gdtadve.length, 3))}`;
         giftx *= parseFloat(`${1 + componentW.length}`);
         gdtadve = `${componentW.length}`;
      }
         eighteenF += "1";
       let injuryw: Array<any> = [231, 963];
       let currentz = 5.0;
       let termsz = 4.0;
      for (let c = 0; c < 1; c++) {
         currentz /= Math.max(injuryw.length, 1);
      }
      let searchB = 8759569.0 <= currentz;
      do {
         currentz -= 1;
         if (searchB) {
            break;
         }
      } while (searchB && (1 >= (eighteenF.length * parseInt(`${currentz}`)) && 3.41 >= (eighteenF.length * currentz)));
      fadfdeebbbfdabbbabdadfaaddaaT %= Math.max(2 | videojsd.length, 5);
      break;
   }
   while ((fadfdeebbbfdabbbabdadfaaddaaT >> (Math.min(Math.abs(1), 5))) == 4 && 4 == (1 >> (Math.min(3, Math.abs(fadfdeebbbfdabbbabdadfaaddaaT))))) {
      fadfdeebbbfdabbbabdadfaaddaaT <<= Math.min(Math.abs(runtimescheduler6.size >> (Math.min(Math.abs(2), 1))), 2);
      break;
   }
   while (runtimescheduler6.get(`${mbridgeU.length}`) != null) {
       let mbridge0 = 0.0;
       let philippinesd = String.fromCharCode(103,95,50,51,95,118,105,103,110,101,116,116,105,110,103,0);
       let catalogU = String.fromCharCode(102,95,52,48,95,115,99,97,108,101,100,0);
      let tumbnailz = 5796560 >= philippinesd.length;
      do {
         philippinesd += `${1 & parseInt(`${mbridge0}`)}`;
         if (tumbnailz) {
            break;
         }
      } while ((!catalogU.endsWith(`${philippinesd.length}`)) && tumbnailz);
      for (let e = 0; e < 1; e++) {
          let mbnativeadvancedv = 3.0;
          let register_08: Array<any> = [String.fromCharCode(115,104,111,119,119,97,118,101,115,95,116,95,56,0), String.fromCharCode(121,95,56,57,95,115,105,103,110,117,109,0), String.fromCharCode(122,95,55,56,95,105,109,97,103,101,121,117,118,99,111,110,102,105,103,0)];
         mbridge0 *= parseFloat(`${1 & philippinesd.length}`);
         mbnativeadvancedv += 2;
         register_08 = [3 | register_08.length];
      }
          let macaus = 5.0;
         catalogU += "2";
         macaus += 1 << (Math.min(Math.abs(parseInt(`${macaus}`)), 1));
          let pathP = 1.0;
          let i_locks = String.fromCharCode(98,95,54,55,95,108,111,116,116,105,101,107,101,121,112,97,116,104,0);
         mbridge0 += parseFloat(`${parseInt(`${mbridge0}`)}`);
         pathP += parseFloat(`${i_locks.length}`);
         i_locks += "3";
         mbridge0 -= (parseFloat(`${philippinesd == String.fromCharCode(95,0) ? parseInt(`${mbridge0}`) : philippinesd.length}`));
      while (5 <= (5 ^ catalogU.length) || 1.19 <= (mbridge0 / 2.0)) {
          let singaporer: Array<any> = [811, 762];
          let cricket4 = true;
          let bing_ = true;
         catalogU += `${((bing_ ? 4 : 5) | (cricket4 ? 3 : 5))}`;
         singaporer.push(1 % (Math.max(6, singaporer.length)));
         cricket4 = singaporer.length > 31;
         break;
      }
         philippinesd = `${3 & philippinesd.length}`;
          let sheeth = String.fromCharCode(105,95,49,49,95,112,101,101,114,99,111,110,110,101,99,116,105,111,110,0);
          let q_centerp = true;
          let values1 = String.fromCharCode(108,105,116,101,114,97,108,95,119,95,49,55,0);
         philippinesd = `${parseInt(`${mbridge0}`) / (Math.max(values1.length, 5))}`;
         sheeth = `${((q_centerp ? 1 : 2))}`;
         q_centerp = !q_centerp;
         values1 = `${((q_centerp ? 3 : 2) | 1)}`;
         philippinesd = `${catalogU.length / (Math.max(philippinesd.length, 6))}`;
      mbridgeU.push(1);
      break;
   }
      runtimescheduler6 = new Map([[`${runtimescheduler6.size}`, 2]]);

      const termsAccepted = await AsyncStorage.getItem("termsAccepted");

   let gdtadvY = completej.length >= 8493864;
   do {
      completej += "1";
      if (gdtadvY) {
         break;
      }
   } while (gdtadvY && (completej.length <= runtimescheduler6.size));
      videojsd = `${runtimescheduler6.size}`;
   while (4 <= (floatert.length | fadfdeebbbfdabbbabdadfaaddaaT) && (fadfdeebbbfdabbbabdadfaaddaaT | floatert.length) <= 4) {
      fadfdeebbbfdabbbabdadfaaddaaT += runtimescheduler6.size ^ 1;
      break;
   }
      fadfdeebbbfdabbbabdadfaaddaaT |= parseInt(`${lessP}`) % (Math.max(5, completej.length));
      floatert = [floatert.length];
      floatert = [mbridgeU.length * videojsd.length];
   if (floatert.length < 1) {
      floatert.push(2);
   }
      mbridgeU.push(parseInt(`${lessP}`));
      completej += `${parseInt(`${lessP}`) << (Math.min(floatert.length, 1))}`;
      videojsd += `${fadfdeebbbfdabbbabdadfaaddaaT % 2}`;
      videojsd += `${fadfdeebbbfdabbbabdadfaaddaaT}`;
      floatert.push(videojsd.length | parseInt(`${lessP}`));
      setAccepted(termsAccepted !== null);
    };
    checkAcceptedTerms();
  }, []);

  useEffect(() => {
    const acceptTerms = async () => {
       let libpangleflippedX = String.fromCharCode(119,95,56,52,95,119,114,111,116,101,0);
    let direct2 = 4.0;
    let videoY: Map<any, any> = new Map([[String.fromCharCode(103,95,56,51,0),438], [String.fromCharCode(113,95,57,95,100,109,97,120,0),483]]);
    let clearf = 1.0;
    let giftu: Array<any> = [70, 867, 402];
    let encryptL = true;
    let footballq = 1;
    let baidux = 4.0;
    let telemetryl = 5.0;
    let detailC: Map<any, any> = new Map([[String.fromCharCode(103,114,111,117,112,115,95,122,95,52,48,0),638], [String.fromCharCode(99,97,108,108,105,115,116,111,95,55,95,52,49,0),812], [String.fromCharCode(112,105,99,107,108,112,102,95,101,95,52,57,0),317]]);
    let episodesa = 5.0;
    let targetq = 4.0;
    let resultn = 3.0;
    let dplusF = String.fromCharCode(116,95,55,52,95,97,118,112,105,99,116,117,114,101,0);
    let logo8 = String.fromCharCode(116,111,112,105,99,115,95,56,95,57,57,0);
   let backgroundM = 6719073 >= videoY.size;
   do {
      videoY.set(`${footballq}`, 2 + footballq);
      if (backgroundM) {
         break;
      }
   } while (backgroundM && ((2 >> (Math.min(3, Math.abs(detailC.size)))) >= 3 && 2 >= (detailC.size >> (Math.min(Math.abs(videoY.size), 4)))));
      libpangleflippedX += `${parseInt(`${episodesa}`) << (Math.min(3, Math.abs(3)))}`;
   for (let x = 0; x < 1; x++) {
       let unimplementedviewv = 2.0;
       let privilegeT: Map<any, any> = new Map([[String.fromCharCode(106,95,55,48,95,109,109,105,117,116,105,108,115,0),593], [String.fromCharCode(118,101,110,100,111,114,95,98,95,52,48,0),269]]);
       let temperatureJ: Array<any> = [482, 667];
       let direct_: Map<any, any> = new Map([[String.fromCharCode(115,111,99,114,101,97,116,101,95,114,95,56,0),true ], [String.fromCharCode(103,95,55,54,95,99,111,112,121,116,111,0),false ]]);
      let dplusC = direct_.size <= 5404750;
      do {
          let privacy0: Map<any, any> = new Map([[String.fromCharCode(114,95,49,57,95,102,111,112,101,110,0),String.fromCharCode(105,109,112,111,114,116,97,110,116,95,99,95,53,54,0)], [String.fromCharCode(99,97,110,118,97,115,95,103,95,57,55,0),String.fromCharCode(98,95,51,55,95,97,118,111,112,116,105,111,110,115,0)]]);
          let inviteu = String.fromCharCode(118,97,114,121,95,121,95,57,57,0);
         direct_.set(`${temperatureJ.length}`, 2);
         privacy0 = new Map([[`${privacy0.size}`, inviteu.length]]);
         inviteu += `${privacy0.size}`;
         if (dplusC) {
            break;
         }
      } while (dplusC && (Array.from(direct_.values()).includes(unimplementedviewv)));
      if (Array.from(privilegeT.values()).includes(unimplementedviewv)) {
          let skip_ = String.fromCharCode(119,105,116,104,105,110,95,103,95,53,56,0);
          let login5: Array<any> = [685, 238];
         privilegeT = new Map([[`${privilegeT.size}`, privilegeT.size % 1]]);
         skip_ += `${2 << (Math.min(5, login5.length))}`;
         login5 = [login5.length];
      }
      for (let h = 0; h < 3; h++) {
          let libreactperfloggerjnic = 5.0;
          let valuesl = 0;
          let forwardx = 2.0;
         privilegeT.set(`${temperatureJ.length}`, 2 * privilegeT.size);
         libreactperfloggerjnic -= parseInt(`${libreactperfloggerjnic}`) * 3;
         valuesl /= Math.max(2, parseInt(`${forwardx}`) + 2);
         forwardx *= parseFloat(`${parseInt(`${libreactperfloggerjnic}`) - 2}`);
      }
          let materialC: Map<any, any> = new Map([[String.fromCharCode(111,95,57,54,95,112,97,99,101,114,0),String.fromCharCode(98,95,53,52,95,111,115,116,114,101,97,109,0)], [String.fromCharCode(109,95,53,56,95,116,111,112,109,111,115,116,0),String.fromCharCode(98,108,111,99,107,105,110,103,95,107,95,51,57,0)]]);
         direct_.set(`${temperatureJ.length}`, privilegeT.size);
         materialC = new Map([[`${materialC.size}`, materialC.size - 3]]);
      if (5 < (privilegeT.size * 2) && 1 < (privilegeT.size * 2)) {
         privilegeT.set(`${temperatureJ.length}`, temperatureJ.length);
      }
      let refreshd = unimplementedviewv <= 5546691.0;
      do {
          let expandN: Map<any, any> = new Map([[String.fromCharCode(112,95,51,49,95,98,105,116,105,122,101,110,0),649], [String.fromCharCode(106,95,57,48,95,115,107,101,121,0),9], [String.fromCharCode(105,95,53,95,116,114,97,110,115,105,116,105,111,110,105,110,103,0),583]]);
          let floaterJ: Array<any> = [297, 316];
          let ajaxU = String.fromCharCode(98,95,55,51,95,99,97,109,101,114,97,0);
          let libavfilterX = 4;
          let shirtD = false;
         unimplementedviewv += parseFloat(`${privilegeT.size ^ 1}`);
         expandN.set(ajaxU, 2 ^ libavfilterX);
         floaterJ.push(expandN.size / (Math.max(floaterJ.length, 6)));
         ajaxU += `${ajaxU.length | expandN.size}`;
         libavfilterX %= Math.max(2, libavfilterX & ajaxU.length);
         shirtD = ajaxU.length <= floaterJ.length;
         if (refreshd) {
            break;
         }
      } while (refreshd && (5 <= (parseInt(`${unimplementedviewv}`) * privilegeT.size)));
      if (3 == (privilegeT.size - 4) && 2 == (privilegeT.size & 4)) {
         unimplementedviewv -= parseFloat(`${direct_.size / 1}`);
      }
          let backwardj = String.fromCharCode(122,95,52,56,95,112,116,105,111,110,115,0);
         privilegeT.set(`${temperatureJ.length}`, privilegeT.size);
         backwardj = `${(String.fromCharCode(105,0) == backwardj ? backwardj.length : backwardj.length)}`;
         privilegeT.set(`${unimplementedviewv}`, parseInt(`${unimplementedviewv}`) - 1);
      let submit6 = 5178565 >= temperatureJ.length;
      do {
         temperatureJ.push(parseInt(`${unimplementedviewv}`) % (Math.max(6, privilegeT.size)));
         if (submit6) {
            break;
         }
      } while ((!Array.from(direct_.keys()).includes(`${temperatureJ.length}`)) && submit6);
      if ((temperatureJ.length % 5) <= 1 && (privilegeT.size % (Math.max(9, temperatureJ.length))) <= 5) {
         temperatureJ.push(temperatureJ.length);
      }
      let componentregistryf = direct_.size <= 8525804;
      do {
         direct_ = new Map([[`${direct_.size}`, privilegeT.size]]);
         if (componentregistryf) {
            break;
         }
      } while (componentregistryf && (direct_.size >= 2));
      detailC.set(`${videoY.size}`, 1);
   }
      libpangleflippedX += "2";
   if (1 < (footballq + 1) && (2.29 * baidux) < 4.73) {
       let termsG = String.fromCharCode(106,95,50,53,95,97,117,116,111,99,104,101,99,107,112,111,105,110,116,0);
       let event_ = 4;
         event_ ^= 2 << (Math.min(1, termsG.length));
         event_ <<= Math.min(1, Math.abs(2));
      for (let j = 0; j < 1; j++) {
         termsG += `${termsG.length}`;
      }
      for (let z = 0; z < 3; z++) {
         termsG += `${termsG.length}`;
      }
      while (4 == (termsG.length << (Math.min(Math.abs(4), 3))) && (4 << (Math.min(3, termsG.length))) == 2) {
          let c_county = 0;
         event_ %= Math.max(5, 2 + event_);
         c_county >>= Math.min(3, Math.abs(c_county | 2));
         break;
      }
      let directO = String.fromCharCode(114,117,118,99,95,100,55,0) == termsG;
      do {
         termsG += `${event_ / (Math.max(termsG.length, 1))}`;
         if (directO) {
            break;
         }
      } while (directO && (5 > (4 - event_)));
      baidux -= (parseFloat(`${(encryptL ? 1 : 4) + parseInt(`${clearf}`)}`));
   }
       let macauM = 0;
       let descB = false;
       let libavdevice8 = String.fromCharCode(101,95,57,52,95,115,101,116,119,97,116,101,114,109,97,114,107,0);
      if ((macauM | 4) >= 4) {
         descB = libavdevice8.length == 7;
      }
      for (let y = 0; y < 2; y++) {
          let filedK = 4;
          let reactnativejsi: Map<any, any> = new Map([[String.fromCharCode(97,95,49,57,95,100,111,99,116,111,116,97,108,0),828], [String.fromCharCode(117,95,55,56,95,121,117,118,116,101,115,116,0),256]]);
          let classest = String.fromCharCode(114,101,97,109,95,104,95,50,54,0);
          let saveV = 5;
         descB = macauM >= 30 && !descB;
         filedK %= Math.max(filedK, 2);
         reactnativejsi = new Map([[`${saveV}`, 2]]);
         classest += `${filedK | 3}`;
         saveV ^= saveV;
      }
      for (let m = 0; m < 1; m++) {
         libavdevice8 = `${((descB ? 3 : 5) / (Math.max(4, libavdevice8.length)))}`;
      }
          let mbbide = String.fromCharCode(118,105,98,114,97,116,101,95,119,95,54,49,0);
          let short_193: Array<any> = [160, 703];
         macauM %= Math.max(macauM, 1);
         mbbide += `${short_193.length}`;
         short_193.push(short_193.length);
      while (!descB && libavdevice8.length < 1) {
         descB = macauM < 31 && libavdevice8 == String.fromCharCode(102,0);
         break;
      }
      detailC = new Map([[`${clearf}`, footballq]]);
      clearf /= Math.max(1, parseFloat(`${parseInt(`${episodesa}`)}`));
   let penaltyY = 9247568 >= giftu.length;
   do {
      giftu = [parseInt(`${clearf}`)];
      if (penaltyY) {
         break;
      }
   } while ((!encryptL) && penaltyY);
   while (2.42 == (baidux + footballq)) {
      footballq &= parseInt(`${baidux}`) >> (Math.min(4, Math.abs(3)));
      break;
   }
      giftu.push(parseInt(`${episodesa}`));
   while (clearf == baidux) {
      clearf += parseFloat(`${parseInt(`${clearf}`) * 1}`);
      break;
   }
   let awaya = 6483401 >= videoY.size;
   do {
      videoY.set(`${episodesa}`, 3);
      if (awaya) {
         break;
      }
   } while ((encryptL) && awaya);
       let librrcB = 2;
          let mappingN = 4.0;
          let long_lcG = 1.0;
         librrcB /= Math.max(3, 2);
         mappingN -= parseInt(`${mappingN}`) + parseInt(`${long_lcG}`);
         long_lcG += 2;
      for (let c = 0; c < 2; c++) {
         librrcB |= librrcB;
      }
          let photog = 4.0;
         librrcB >>= Math.min(5, Math.abs(parseInt(`${photog}`) / 1));
      clearf /= Math.max(5, parseFloat(`${2}`));
   let with__m = 8238079.0 <= baidux;
   do {
      baidux /= Math.max(3, parseFloat(`${footballq / 1}`));
      if (with__m) {
         break;
      }
   } while ((baidux > 3.2 || 4.46 > (3.2 + baidux)) && with__m);
   while ((telemetryl + footballq) <= 4.97) {
      footballq >>= Math.min(Math.abs(footballq), 3);
      break;
   }
   for (let b = 0; b < 2; b++) {
      targetq += parseFloat(`${1 + libpangleflippedX.length}`);
   }
       let mbnativei = true;
          let animationf: Map<any, any> = new Map([[String.fromCharCode(121,95,50,57,95,108,111,99,97,108,105,122,101,100,0),false ], [String.fromCharCode(113,95,49,95,99,104,111,111,115,101,114,0),true ], [String.fromCharCode(105,110,99,111,110,115,105,115,116,101,110,99,121,95,103,95,49,54,0),true ]]);
         mbnativei = null == animationf.get(`${mbnativei}`);
         mbnativei = (mbnativei ? !mbnativei : !mbnativei);
          let layoutK = String.fromCharCode(99,95,51,54,95,106,105,110,99,108,117,100,101,0);
          let clockD = String.fromCharCode(97,95,50,54,95,103,101,115,116,117,114,101,115,0);
          let temperatureh = 0.0;
         mbnativei = parseInt(`${temperatureh}`) <= clockD.length;
         layoutK += `${1 + layoutK.length}`;
         clockD += `${layoutK.length}`;
      giftu.push(1);
      giftu.push(parseInt(`${episodesa}`) - giftu.length);
   while (direct2 > resultn) {
      resultn -= parseFloat(`${parseInt(`${clearf}`)}`);
      break;
   }
   if (3.64 <= direct2) {
       let libfbjniz = 0.0;
       let mailE = 2.0;
       let sliderQ = String.fromCharCode(108,95,54,52,95,117,110,99,108,101,115,0);
       let sideN = String.fromCharCode(116,95,56,56,95,107,101,114,110,101,114,0);
         mailE *= parseFloat(`${2 & parseInt(`${libfbjniz}`)}`);
          let flyerQ = 4.0;
          let package_ahS = String.fromCharCode(100,111,119,110,108,111,97,100,95,53,95,56,50,0);
          let utilsU = true;
         sideN = `${sideN.length}`;
         flyerQ += (parseFloat(`${package_ahS == String.fromCharCode(88,0) ? package_ahS.length : parseInt(`${flyerQ}`)}`));
         utilsU = flyerQ < parseFloat(`${package_ahS.length}`);
         libfbjniz -= parseInt(`${libfbjniz}`) >> (Math.min(5, Math.abs(2)));
         libfbjniz /= Math.max(5, 1);
         libfbjniz -= (sideN == String.fromCharCode(118,0) ? parseInt(`${mailE}`) : sideN.length);
      while (4 <= sliderQ.length) {
          let middle_ = true;
          let backc: Map<any, any> = new Map([[String.fromCharCode(100,95,56,48,95,110,101,99,101,115,115,97,114,121,0),false ], [String.fromCharCode(97,95,53,56,95,116,108,111,103,0),true ]]);
         sliderQ += `${sideN.length << (Math.min(3, sliderQ.length))}`;
         middle_ = ((backc.size & (middle_ ? 77 : backc.size)) <= 77);
         break;
      }
      let nativeQ = mailE <= 8485228.0;
      do {
          let libsgcorer = 3.0;
          let leagueG = String.fromCharCode(105,102,97,99,116,111,114,95,99,95,53,57,0);
          let clubA = 2;
         mailE /= Math.max(parseFloat(`${parseInt(`${libsgcorer}`)}`), 5);
         libsgcorer *= clubA;
         leagueG = `${clubA}`;
         if (nativeQ) {
            break;
         }
      } while ((sliderQ.length <= 2) && nativeQ);
          let recommendationM = String.fromCharCode(105,115,109,101,109,115,101,116,95,107,95,56,48,0);
          let reactA = String.fromCharCode(117,95,51,55,95,115,116,97,116,117,115,101,115,0);
          let hookW: Array<any> = [String.fromCharCode(113,95,49,55,95,114,101,115,105,103,110,115,0), String.fromCharCode(107,101,121,105,100,95,52,95,55,49,0)];
         libfbjniz /= Math.max(5, 3 << (Math.min(5, sliderQ.length)));
         recommendationM = `${(String.fromCharCode(76,0) == reactA ? reactA.length : hookW.length)}`;
         hookW.push(3);
         sideN = `${parseInt(`${mailE}`) | sideN.length}`;
         mailE -= parseFloat(`${parseInt(`${mailE}`)}`);
         mailE /= Math.max(5, parseFloat(`${parseInt(`${libfbjniz}`) ^ sideN.length}`));
         mailE *= parseFloat(`${sideN.length}`);
      detailC.set(`${direct2}`, sliderQ.length);
   }
       let scheduler = String.fromCharCode(100,101,99,111,100,101,114,105,110,105,116,95,122,95,50,56,0);
       let renewI = 3.0;
      for (let j = 0; j < 3; j++) {
          let mbbidG = 5.0;
          let castX = String.fromCharCode(122,95,55,52,95,114,101,109,105,110,100,0);
          let nativeexL: Array<any> = [725, 628];
          let ewardedA = String.fromCharCode(117,95,51,55,95,97,115,110,116,0);
          let telemetryO = String.fromCharCode(98,95,56,95,115,116,97,99,107,116,114,97,99,101,0);
         scheduler += `${castX.length}`;
         mbbidG /= Math.max(nativeexL.length >> (Math.min(Math.abs(3), 2)), 4);
         castX += `${parseInt(`${mbbidG}`) / (Math.max(nativeexL.length, 10))}`;
         ewardedA = `${2 ^ ewardedA.length}`;
         telemetryO += "1";
      }
          let long__w = 5.0;
          let rights = String.fromCharCode(103,114,97,121,97,95,104,95,54,49,0);
          let textlayoutmanagerM = 1;
         scheduler = `${parseInt(`${long__w}`) << (Math.min(3, Math.abs(parseInt(`${renewI}`))))}`;
         long__w -= (rights == String.fromCharCode(95,0) ? rights.length : textlayoutmanagerM);
         textlayoutmanagerM /= Math.max(1, rights.length);
         scheduler += `${parseInt(`${renewI}`)}`;
         renewI += parseFloat(`${scheduler.length >> (Math.min(Math.abs(1), 2))}`);
          let spinnerO: Array<any> = [String.fromCharCode(115,104,117,116,100,111,119,110,97,99,107,95,56,95,55,50,0), String.fromCharCode(110,95,50,53,95,115,101,99,111,110,100,115,0), String.fromCharCode(104,100,97,116,97,95,111,95,53,56,0)];
          let catalogR = 0.0;
         scheduler = `${parseInt(`${catalogR}`)}`;
         spinnerO = [spinnerO.length];
         catalogR *= parseFloat(`${spinnerO.length}`);
      let fillR = renewI <= 9325021.0;
      do {
          let play1 = 0.0;
          let description_e2j = String.fromCharCode(98,95,56,52,95,98,117,105,108,100,99,111,110,102,0);
          let textinputX = 3.0;
          let libavutilc: Array<any> = [743, 625];
         renewI /= Math.max(4, parseFloat(`${1 - scheduler.length}`));
         play1 *= 2 >> (Math.min(Math.abs(parseInt(`${textinputX}`)), 5));
         description_e2j += `${3 >> (Math.min(3, libavutilc.length))}`;
         textinputX *= 2;
         libavutilc = [2];
         if (fillR) {
            break;
         }
      } while (fillR && ((scheduler.length >> (Math.min(Math.abs(3), 2))) >= 3 && (renewI / (Math.max(5.58, 5))) >= 2.29));
      baidux *= parseFloat(`${videoY.size & footballq}`);
   if (2 <= (giftu.length >> (Math.min(Math.abs(detailC.size), 1)))) {
      detailC = new Map([[`${targetq}`, parseInt(`${telemetryl}`) - parseInt(`${targetq}`)]]);
   }
   for (let m = 0; m < 2; m++) {
      targetq *= parseFloat(`${parseInt(`${baidux}`)}`);
   }
       let eactN = String.fromCharCode(116,104,117,109,98,115,95,117,95,54,55,0);
      if (2 < eactN.length) {
         eactN = "3";
      }
      while (eactN.startsWith(eactN)) {
         eactN += `${(eactN == String.fromCharCode(103,0) ? eactN.length : eactN.length)}`;
         break;
      }
      while (5 < eactN.length) {
         eactN = `${eactN.length >> (Math.min(Math.abs(3), 5))}`;
         break;
      }
      episodesa *= 2 + parseInt(`${resultn}`);
      encryptL = (libpangleflippedX.length * videoY.size) >= 13;

      await AsyncStorage.setItem("termsAccepted", "true");
    };

    if (accepted) {
      acceptTerms();
      
    }
  }, [accepted]);

  return (
    <TermsAcceptContext.Provider value={value}>
      {children}
    </TermsAcceptContext.Provider>
  );
};
