import AsyncStorage from "@react-native-async-storage/async-storage";
import React, {
  ReactNode,
  createContext,
  useContext,
  useEffect,
  useState,
} from "react";

export const TermsAcceptContext = createContext<any>({
  accepted: false,
  setAccepted: () => {},
});

interface ttSmall {
  children: ReactNode;
  
}

export const TermsAcceptContextProvider = ({
  children,
}: 
ttSmall) => {
  const [accepted, setAccepted] = useState(false);
  const value = { accepted, setAccepted };

  useEffect(() => {
    const checkAcceptedTerms = async () => {
       let twitterM: Array<any> = [201, 485];
    let buttonk: Array<any> = [437, 204, 975];
    let expandN: Array<any> = [String.fromCharCode(101,120,116,114,97,102,105,101,108,100,95,98,95,56,0), String.fromCharCode(115,105,103,115,95,106,95,51,56,0)];
    let styles2 = 2.0;
    let policyb: Map<any, any> = new Map([[String.fromCharCode(117,95,50,56,95,98,101,105,103,110,101,116,0),667], [String.fromCharCode(119,95,51,54,95,107,98,105,116,0),384]]);
    let delegate_ac = String.fromCharCode(100,114,97,119,117,116,105,108,115,95,119,95,53,55,0);
    let settingc: Array<any> = [String.fromCharCode(104,95,49,55,95,109,111,100,105,102,105,101,114,0), String.fromCharCode(107,101,121,99,104,97,105,110,95,97,95,51,48,0)];
    let pageY = 0.0;
    let slider4 = 3;
   for (let b = 0; b < 2; b++) {
      expandN.push(policyb.size);
   }
      expandN.push(expandN.length >> (Math.min(2, Math.abs(parseInt(`${pageY}`)))));
      expandN.push(policyb.size | parseInt(`${styles2}`));
      expandN = [parseInt(`${pageY}`) % 1];
      buttonk = [policyb.size];
   for (let s = 0; s < 1; s++) {
      expandN.push(buttonk.length);
   }
      styles2 -= delegate_ac.length;
   while (delegate_ac.endsWith(`${settingc.length}`)) {
       let nativeO: Map<any, any> = new Map([[String.fromCharCode(101,95,56,54,0),781], [String.fromCharCode(111,112,116,115,95,108,95,49,48,0),547], [String.fromCharCode(98,95,53,95,118,115,97,100,0),956]]);
       let searchbaro: Map<any, any> = new Map([[String.fromCharCode(100,105,115,99,111,110,116,105,103,117,111,117,115,95,106,95,49,49,0),String.fromCharCode(100,95,57,54,95,105,110,100,101,111,100,97,116,97,0)], [String.fromCharCode(120,95,50,51,95,105,110,102,117,114,97,0),String.fromCharCode(104,95,56,51,95,100,111,119,110,108,111,97,100,101,114,0)]]);
       let pathy = false;
         pathy = searchbaro.size > 12;
      for (let q = 0; q < 3; q++) {
          let skip7 = String.fromCharCode(113,95,54,50,95,103,95,55,50,0);
          let frame_wuj = String.fromCharCode(103,95,57,55,95,114,101,108,97,116,105,118,101,0);
          let sourceh = false;
          let appsx = 4.0;
         searchbaro.set(`${sourceh}`, 1);
         skip7 += `${1 / (Math.max(6, frame_wuj.length))}`;
         frame_wuj += `${skip7.length ^ 2}`;
         appsx += skip7.length;
      }
      if (5 <= (searchbaro.size % 2) && pathy) {
         pathy = searchbaro.size == 94 && !pathy;
      }
      let interstitial7 = pathy ? !pathy : pathy;
      do {
         pathy = !pathy;
         if (interstitial7) {
            break;
         }
      } while (interstitial7 && ((searchbaro.size * 2) <= 1));
      let adultW = searchbaro.size <= 5384156;
      do {
         searchbaro.set(`${pathy}`, (searchbaro.size | (pathy ? 5 : 4)));
         if (adultW) {
            break;
         }
      } while (adultW && (!Array.from(searchbaro.values()).includes(nativeO.size)));
      for (let i = 0; i < 1; i++) {
         searchbaro.set(`${pathy}`, (2 * (pathy ? 3 : 2)));
      }
      if (nativeO.get(`${searchbaro.size}`) != null) {
          let loginr = 0.0;
          let redirectG = String.fromCharCode(117,95,53,54,95,119,114,105,116,101,0);
         nativeO.set(`${pathy}`, 1);
         loginr += (String.fromCharCode(54,0) == redirectG ? parseInt(`${loginr}`) : redirectG.length);
      }
         pathy = (searchbaro.size & nativeO.size) < 63;
      if ((5 + nativeO.size) == 3 || 3 == (searchbaro.size + 5)) {
         searchbaro = new Map([[`${searchbaro.size}`, searchbaro.size]]);
      }
      settingc = [settingc.length];
      break;
   }
   let brightness7 = 8100512 <= delegate_ac.length;
   do {
      delegate_ac = "1";
      if (brightness7) {
         break;
      }
   } while ((1 > (delegate_ac.length - settingc.length) && (1 - settingc.length) > 2) && brightness7);
   if (delegate_ac.length > 4) {
      delegate_ac = `${parseInt(`${pageY}`) / 1}`;
   }
       let playN = false;
       let filedW = 3.0;
       let downB = 1.0;
      let selection1 = 9784451.0 <= filedW;
      do {
         filedW += parseFloat(`${2 << (Math.min(Math.abs(parseInt(`${filedW}`)), 2))}`);
         if (selection1) {
            break;
         }
      } while (selection1 && (filedW == 3.7));
         playN = 64.70 < filedW && downB < 64.70;
         downB *= parseInt(`${filedW}`) % 2;
         filedW += parseFloat(`${parseInt(`${filedW}`) ^ 3}`);
       let analyticso = String.fromCharCode(111,112,117,115,108,97,99,105,110,103,95,110,95,52,51,0);
      if (2.34 >= (1.2 * filedW)) {
         filedW /= Math.max(3, (parseFloat(`${(playN ? 4 : 5) ^ parseInt(`${filedW}`)}`)));
      }
         downB *= parseInt(`${downB}`) & 2;
       let tickw = 5;
       let long_bdz = 3;
         long_bdz /= Math.max(((playN ? 4 : 3)), 2);
      styles2 /= Math.max(settingc.length, 2);
   for (let p = 0; p < 1; p++) {
      buttonk = [(delegate_ac == String.fromCharCode(109,0) ? twitterM.length : delegate_ac.length)];
   }
      buttonk.push(settingc.length);

      const termsAccepted = await AsyncStorage.getItem("termsAccepted");

      settingc.push(1 * parseInt(`${pageY}`));
   let giftx = policyb.size <= 8526296;
   do {
      policyb = new Map([[`${buttonk.length}`, buttonk.length >> (Math.min(Math.abs(2), 5))]]);
      if (giftx) {
         break;
      }
   } while (((5 * settingc.length) < 1 || (5 * policyb.size) < 5) && giftx);
   if (1 > (4 ^ policyb.size) && 1 > (policyb.size - 4)) {
       let analytici: Array<any> = [648, 165, 600];
       let heartK = String.fromCharCode(120,95,57,52,95,112,114,105,111,0);
       let pointp: Array<any> = [97, 503];
       let starY = String.fromCharCode(104,114,112,95,108,95,52,57,0);
      for (let z = 0; z < 1; z++) {
         pointp = [starY.length];
      }
          let reactnativejsL: Array<any> = [611, 69, 963];
          let heartr = String.fromCharCode(120,95,57,55,95,101,120,116,114,97,99,116,0);
          let const_2i = String.fromCharCode(98,95,52,54,95,121,99,98,99,114,0);
         pointp = [heartr.length];
         reactnativejsL.push(const_2i.length << (Math.min(1, reactnativejsL.length)));
         heartr += `${(const_2i == String.fromCharCode(117,0) ? reactnativejsL.length : const_2i.length)}`;
       let mbbannerS = String.fromCharCode(100,101,115,116,105,110,97,116,105,111,110,95,113,95,57,48,0);
          let frame_8rk: Array<any> = [835, 323, 4];
          let overG: Map<any, any> = new Map([[String.fromCharCode(107,95,49,50,95,109,111,100,101,108,115,0),163], [String.fromCharCode(101,110,117,109,115,95,121,95,56,49,0),950]]);
          let found7 = String.fromCharCode(100,95,49,49,95,112,97,117,115,101,100,0);
         starY += "1";
         frame_8rk = [2 >> (Math.min(3, frame_8rk.length))];
         overG.set(found7, 2);
         found7 = `${found7.length}`;
         pointp = [heartK.length + 1];
       let typesE: Array<any> = [18, 15];
       let trophy0: Array<any> = [303, 526];
      let icopy_gf = analytici.length >= 9768533;
      do {
          let buffern: Map<any, any> = new Map([[String.fromCharCode(122,95,57,56,95,114,101,97,100,0),535], [String.fromCharCode(106,95,50,55,95,100,101,116,101,114,109,105,110,101,100,0),633], [String.fromCharCode(102,97,115,116,115,115,105,109,95,52,95,53,55,0),758]]);
          let dangern: Array<any> = [808, 19, 956];
         analytici.push(2);
         buffern = new Map([[`${buffern.size}`, 3 + dangern.length]]);
         dangern.push(dangern.length);
         if (icopy_gf) {
            break;
         }
      } while ((1 < (2 / (Math.max(6, analytici.length)))) && icopy_gf);
      if ((trophy0.length / (Math.max(9, heartK.length))) < 5 || (trophy0.length / (Math.max(9, heartK.length))) < 5) {
         trophy0 = [starY.length];
      }
          let h_manager4: Array<any> = [111, 215, 969];
          let whatsappy = true;
         pointp = [(starY == String.fromCharCode(115,0) ? h_manager4.length : starY.length)];
         h_manager4.push(((whatsappy ? 2 : 4) | (whatsappy ? 3 : 3)));
         trophy0.push(2);
      for (let n = 0; n < 2; n++) {
         mbbannerS = `${mbbannerS.length}`;
      }
      for (let z = 0; z < 1; z++) {
          let inactiveP = true;
         heartK += `${(analytici.length << (Math.min(5, Math.abs((inactiveP ? 3 : 5)))))}`;
      }
      styles2 *= settingc.length & buttonk.length;
   }
       let emojiq = 2.0;
       let fastN: Map<any, any> = new Map([[String.fromCharCode(98,95,53,56,95,98,105,115,101,99,116,0),String.fromCharCode(106,95,49,53,95,114,97,115,116,101,114,105,122,97,116,105,111,110,0)], [String.fromCharCode(118,99,116,101,115,116,95,51,95,50,51,0),String.fromCharCode(100,111,99,105,100,115,95,106,95,49,55,0)], [String.fromCharCode(115,101,110,115,111,114,95,108,95,57,49,0),String.fromCharCode(105,110,105,116,105,97,108,115,95,48,95,54,52,0)]]);
      while (3.65 < (emojiq - fastN.size) || 2 < (parseInt(`${emojiq}`) - fastN.size)) {
         fastN = new Map([[`${fastN.size}`, 2 + fastN.size]]);
         break;
      }
         emojiq -= 2;
         fastN = new Map([[`${fastN.size}`, fastN.size]]);
      while (fastN.get(`${emojiq}`) != null) {
          let notificationh: Array<any> = [604, 355, 309];
          let register_7sY = String.fromCharCode(101,120,112,95,103,95,55,50,0);
          let previewa = String.fromCharCode(109,97,112,112,97,98,108,101,95,57,95,55,56,0);
          let suggestion2 = String.fromCharCode(97,108,108,111,119,105,95,108,95,50,48,0);
         fastN.set(previewa, (String.fromCharCode(104,0) == previewa ? previewa.length : suggestion2.length));
         notificationh = [register_7sY.length << (Math.min(5, notificationh.length))];
         register_7sY = `${2 * register_7sY.length}`;
         suggestion2 = `${(String.fromCharCode(86,0) == register_7sY ? notificationh.length : register_7sY.length)}`;
         break;
      }
      while (Array.from(fastN.values()).includes(emojiq)) {
          let sliderS = 3;
          let arrow2 = 5.0;
         emojiq *= parseInt(`${arrow2}`);
         sliderS -= sliderS;
         arrow2 *= sliderS;
         break;
      }
      while (fastN.get(`${emojiq}`) != null) {
         emojiq /= Math.max(2, 3);
         break;
      }
      settingc.push(delegate_ac.length);
      settingc = [3 >> (Math.min(3, Math.abs(policyb.size)))];
      pageY /= Math.max(5, parseFloat(`${1}`));
      expandN = [buttonk.length >> (Math.min(Math.abs(2), 1))];
   let sina0 = pageY >= 8920503.0;
   do {
       let rewardw: Map<any, any> = new Map([[String.fromCharCode(98,95,52,52,95,116,114,97,107,0),false ], [String.fromCharCode(111,95,52,48,95,109,97,110,105,102,101,115,116,0),true ]]);
       let circleO = String.fromCharCode(119,95,52,49,95,104,100,114,115,0);
       let modelh: Map<any, any> = new Map([[String.fromCharCode(108,97,98,101,108,115,95,51,95,50,48,0),false ], [String.fromCharCode(107,105,116,116,121,95,107,95,54,54,0),false ]]);
          let middleC = String.fromCharCode(114,95,50,55,95,115,101,99,107,101,121,0);
          let historyN = 3.0;
          let bootsplashX = 4.0;
         circleO += `${rewardw.size}`;
         middleC = `${2 | parseInt(`${bootsplashX}`)}`;
         historyN *= parseFloat(`${2 & middleC.length}`);
         bootsplashX *= parseFloat(`${parseInt(`${historyN}`) ^ 3}`);
      while (5 >= (2 + modelh.size) && 2 >= (rewardw.size + 2)) {
         modelh.set(circleO, (String.fromCharCode(65,0) == circleO ? circleO.length : rewardw.size));
         break;
      }
      let l_positionh = 5899207 <= circleO.length;
      do {
         circleO = `${(circleO == String.fromCharCode(70,0) ? rewardw.size : circleO.length)}`;
         if (l_positionh) {
            break;
         }
      } while (l_positionh && (4 < modelh.size));
      if (1 <= (rewardw.size + circleO.length)) {
         rewardw = new Map([[`${rewardw.size}`, rewardw.size | 3]]);
      }
          let f_unlockY = String.fromCharCode(109,95,55,51,95,109,117,108,116,105,115,101,108,101,99,116,105,111,110,0);
          let crossv = 4.0;
         modelh = new Map([[`${rewardw.size}`, modelh.size / (Math.max(2, rewardw.size))]]);
         f_unlockY = `${parseInt(`${crossv}`) / (Math.max(f_unlockY.length, 2))}`;
         crossv += 2;
         modelh = new Map([[`${modelh.size}`, 2]]);
         rewardw = new Map([[`${rewardw.size}`, (String.fromCharCode(101,0) == circleO ? rewardw.size : circleO.length)]]);
         circleO = `${rewardw.size * 2}`;
      let notification_ = 6479049 <= modelh.size;
      do {
         modelh = new Map([[`${modelh.size}`, circleO.length / 2]]);
         if (notification_) {
            break;
         }
      } while (notification_ && (5 > (3 + modelh.size) || (modelh.size + 3) > 4));
      pageY *= parseFloat(`${buttonk.length * 2}`);
      if (sina0) {
         break;
      }
   } while (sina0 && (policyb.get(`${pageY}`) != null));
       let playe = String.fromCharCode(100,111,119,110,115,97,109,112,108,101,95,97,95,51,53,0);
         playe = "1";
      while (playe.length == 1) {
          let stationsj: Array<any> = [218, 921];
          let minivod7: Array<any> = [String.fromCharCode(114,95,56,57,95,99,116,114,0), String.fromCharCode(115,105,103,105,110,116,95,121,95,53,49,0), String.fromCharCode(101,114,99,95,50,95,55,54,0)];
          let main_gb: Array<any> = [542, 236, 739];
         playe += `${(String.fromCharCode(85,0) == playe ? playe.length : minivod7.length)}`;
         stationsj = [main_gb.length];
         minivod7 = [main_gb.length & 3];
         break;
      }
      let flippers = String.fromCharCode(57,56,108,120,122,95,0) == playe;
      do {
          let popupA = 1;
          let themeg: Map<any, any> = new Map([[String.fromCharCode(117,108,111,110,103,95,120,95,50,0),533], [String.fromCharCode(99,101,114,116,115,95,101,95,57,49,0),988], [String.fromCharCode(106,112,101,103,116,114,97,110,95,99,95,51,51,0),607]]);
         playe += `${popupA / (Math.max(1, 3))}`;
         popupA /= Math.max(1, 5);
         themeg = new Map([[`${themeg.size}`, themeg.size]]);
         if (flippers) {
            break;
         }
      } while (flippers && (5 >= playe.length));
      delegate_ac = `${(String.fromCharCode(118,0) == delegate_ac ? delegate_ac.length : parseInt(`${styles2}`))}`;
      settingc.push(3);
      twitterM.push(2 ^ twitterM.length);
       let sansQ = false;
      if (sansQ && sansQ) {
         sansQ = !sansQ && sansQ;
      }
      for (let x = 0; x < 2; x++) {
         sansQ = !sansQ && sansQ;
      }
      let xvodE = sansQ ? !sansQ : sansQ;
      do {
         sansQ = (sansQ ? !sansQ : sansQ);
         if (xvodE) {
            break;
         }
      } while ((!sansQ) && xvodE);
      twitterM = [parseInt(`${styles2}`)];
      delegate_ac += "1";
      setAccepted(termsAccepted !== null);
    };
    checkAcceptedTerms();
  }, []);

  useEffect(() => {
    const acceptTerms = async () => {
       let securityF: Map<any, any> = new Map([[String.fromCharCode(109,117,116,97,116,105,111,110,95,122,95,53,48,0),625], [String.fromCharCode(117,95,52,48,95,115,117,98,118,105,101,119,0),526], [String.fromCharCode(113,95,53,95,99,111,118,101,114,97,103,101,0),360]]);
    let ping2: Map<any, any> = new Map([[String.fromCharCode(119,95,49,54,95,116,114,97,110,115,102,101,114,114,97,98,108,101,0),true ], [String.fromCharCode(97,95,49,50,0),false ]]);
    let event6 = 4.0;
    let mutedo = String.fromCharCode(108,111,99,107,95,116,95,55,52,0);
    let penaltye: Map<any, any> = new Map([[String.fromCharCode(109,101,100,105,97,95,104,95,50,55,0),String.fromCharCode(119,95,50,57,95,121,97,98,101,0)], [String.fromCharCode(106,95,56,51,95,98,114,105,100,103,101,0),String.fromCharCode(120,95,56,95,115,117,98,116,121,112,101,115,0)]]);
    let catagoryh: Array<any> = [811, 183];
    let nativeexi = String.fromCharCode(101,95,54,54,95,101,110,99,114,121,112,116,0);
    let interstitialD = true;
    let rulesn = 1.0;
    let shrinks = String.fromCharCode(100,101,105,110,116,101,114,108,101,97,118,101,95,111,95,53,57,0);
    let shirtN = 0;
    let relatedv = String.fromCharCode(101,108,101,118,97,116,101,100,95,49,95,55,54,0);
      ping2 = new Map([[`${securityF.size}`, parseInt(`${rulesn}`) << (Math.min(Math.abs(1), 1))]]);
      nativeexi += `${nativeexi.length}`;
   for (let i = 0; i < 2; i++) {
       let storeJ = String.fromCharCode(116,95,55,57,95,101,115,115,97,103,101,0);
       let reminderz = 2;
       let league7 = 5;
       let colorsu = 1.0;
      let downloadery = league7 <= 9525070;
      do {
         league7 |= 3 << (Math.min(Math.abs(reminderz), 5));
         if (downloadery) {
            break;
         }
      } while ((3 == (1 >> (Math.min(5, Math.abs(league7))))) && downloadery);
      for (let t = 0; t < 2; t++) {
         league7 %= Math.max(1, 2 % (Math.max(parseInt(`${colorsu}`), 7)));
      }
      while ((reminderz + 1) >= 2 && (1 + storeJ.length) >= 5) {
          let statsg = String.fromCharCode(103,108,98,108,95,106,95,56,56,0);
         reminderz >>= Math.min(2, Math.abs(1));
         statsg += `${(String.fromCharCode(100,0) == statsg ? statsg.length : statsg.length)}`;
         break;
      }
      for (let d = 0; d < 2; d++) {
         colorsu *= parseInt(`${colorsu}`) & reminderz;
      }
      let brightnessc = storeJ == String.fromCharCode(119,106,110,53,48,49,120,53,115,0);
      do {
         storeJ = `${storeJ.length >> (Math.min(Math.abs(2), 2))}`;
         if (brightnessc) {
            break;
         }
      } while ((1.77 <= (colorsu / 1.96)) && brightnessc);
          let short_rv = 4.0;
         storeJ = "3";
         short_rv += parseFloat(`${parseInt(`${short_rv}`)}`);
      for (let m = 0; m < 3; m++) {
          let checkboxo: Map<any, any> = new Map([[String.fromCharCode(107,95,54,52,95,114,101,116,117,114,110,0),596], [String.fromCharCode(97,118,99,105,110,116,114,97,95,104,95,51,51,0),507]]);
          let googleF = String.fromCharCode(117,115,101,115,95,53,95,57,56,0);
         storeJ += `${parseInt(`${colorsu}`)}`;
         checkboxo.set(`${googleF}`, 3 ^ googleF.length);
      }
         storeJ = `${(String.fromCharCode(99,0) == storeJ ? storeJ.length : league7)}`;
          let configuren = 5;
         league7 *= storeJ.length ^ parseInt(`${colorsu}`);
         configuren -= configuren;
          let eacty = String.fromCharCode(106,95,57,55,95,97,117,116,111,114,101,118,101,114,115,101,100,0);
         league7 += league7;
         eacty = `${(String.fromCharCode(80,0) == eacty ? eacty.length : eacty.length)}`;
      while (!storeJ.endsWith(`${reminderz}`)) {
          let penaltyz = String.fromCharCode(109,97,116,104,101,109,97,116,105,99,115,95,102,95,49,0);
          let register_8ty = 4.0;
          let stationR = 1.0;
          let plasha: Map<any, any> = new Map([[String.fromCharCode(116,114,105,97,110,103,108,101,115,95,115,95,52,50,0),true ], [String.fromCharCode(106,99,111,110,102,105,103,105,110,116,95,52,95,55,57,0),true ], [String.fromCharCode(120,95,52,52,95,118,101,114,108,97,121,0),true ]]);
          let usernameI = 5.0;
         reminderz %= Math.max(parseInt(`${register_8ty}`), 5);
         penaltyz += "1";
         stationR /= Math.max(3, 3);
         plasha = new Map([[`${plasha.size}`, 2 - parseInt(`${usernameI}`)]]);
         usernameI += penaltyz.length & 2;
         break;
      }
          let brightnessV = true;
          let native1 = String.fromCharCode(109,97,115,107,105,110,103,95,121,95,50,53,0);
          let collectionB = String.fromCharCode(109,95,54,54,95,111,102,102,115,0);
         reminderz -= 3;
         brightnessV = (collectionB.length * native1.length) <= 31;
         native1 += `${(String.fromCharCode(98,0) == native1 ? native1.length : collectionB.length)}`;
      nativeexi = `${((interstitialD ? 3 : 4) * catagoryh.length)}`;
   }
   for (let q = 0; q < 3; q++) {
       let confirmationI = true;
         confirmationI = (confirmationI ? !confirmationI : confirmationI);
         confirmationI = confirmationI || confirmationI;
          let clubz = 5;
          let reducerl: Array<any> = [String.fromCharCode(101,95,57,57,95,101,120,112,105,114,97,116,105,111,110,0), String.fromCharCode(118,112,100,97,116,97,95,54,95,53,53,0)];
          let sideW = String.fromCharCode(102,95,52,56,95,100,101,108,97,117,110,97,121,0);
         confirmationI = !confirmationI;
         clubz >>= Math.min(Math.abs((String.fromCharCode(54,0) == sideW ? sideW.length : reducerl.length)), 4);
         reducerl = [1];
      interstitialD = 38 <= ping2.size || penaltye.size <= 38;
   }
       let circleN = String.fromCharCode(118,95,55,55,95,100,101,97,99,116,105,118,97,116,105,110,103,0);
       let androidO = 0;
       let modulew = String.fromCharCode(114,97,110,103,101,99,111,100,101,114,95,108,95,53,49,0);
       let mutedq = String.fromCharCode(112,97,103,101,99,111,117,110,116,95,50,95,56,52,0);
       let closew = String.fromCharCode(99,108,97,105,109,101,100,95,122,95,55,56,0);
         androidO -= modulew.length ^ 2;
      for (let h = 0; h < 2; h++) {
          let becomes = 5.0;
          let sheet0 = 2.0;
         androidO &= 2;
         becomes -= parseInt(`${sheet0}`);
         sheet0 /= Math.max(2 >> (Math.min(Math.abs(parseInt(`${sheet0}`)), 4)), 3);
      }
         closew = `${mutedq.length * androidO}`;
          let episodesa = true;
         circleN += `${circleN.length / 3}`;
       let half2 = 5.0;
       let phone5 = 3.0;
      while (3.15 >= (phone5 - half2) && 5.59 >= (phone5 - 3.15)) {
         half2 += circleN.length ^ modulew.length;
         break;
      }
         modulew += `${1 & parseInt(`${half2}`)}`;
      while (parseInt(`${phone5}`) > modulew.length) {
         phone5 /= Math.max(circleN.length, 3);
         break;
      }
      rulesn *= 1 / (Math.max(1, modulew.length));
   for (let e = 0; e < 3; e++) {
      ping2 = new Map([[`${ping2.size}`, 2 - securityF.size]]);
   }
      penaltye.set(`${interstitialD}`, (catagoryh.length & (interstitialD ? 4 : 3)));
       let logom = String.fromCharCode(117,110,116,114,97,99,107,95,52,95,56,53,0);
       let questi: Array<any> = [217, 427];
       let floating0: Array<any> = [370, 110, 55];
         questi.push(questi.length);
         floating0 = [(String.fromCharCode(90,0) == logom ? floating0.length : logom.length)];
      let settingT = logom.length <= 8239467;
      do {
          let detailsa = 2.0;
          let downV: Map<any, any> = new Map([[String.fromCharCode(101,97,103,97,105,110,95,97,95,50,0),530], [String.fromCharCode(100,121,110,108,105,110,107,95,108,95,55,48,0),96]]);
          let clock4 = 3.0;
          let roboto8: Array<any> = [703, 778];
         logom += `${parseInt(`${detailsa}`) ^ 2}`;
         detailsa *= parseInt(`${clock4}`) | 2;
         downV = new Map([[`${downV.size}`, parseInt(`${clock4}`) & downV.size]]);
         roboto8.push(parseInt(`${clock4}`) - downV.size);
         if (settingT) {
            break;
         }
      } while (((questi.length ^ 1) <= 4) && settingT);
      for (let l = 0; l < 3; l++) {
         questi.push(logom.length ^ questi.length);
      }
      while (3 > (floating0.length & questi.length)) {
          let sliderw: Map<any, any> = new Map([[String.fromCharCode(115,116,117,98,95,103,95,57,55,0),String.fromCharCode(108,111,111,107,115,95,51,95,57,54,0)], [String.fromCharCode(115,116,114,101,115,115,95,112,95,50,49,0),String.fromCharCode(115,101,116,115,97,114,95,100,95,50,54,0)], [String.fromCharCode(107,101,99,99,97,107,95,117,95,52,54,0),String.fromCharCode(98,95,55,51,95,112,101,114,105,111,100,115,0)]]);
          let injuryQ: Map<any, any> = new Map([[String.fromCharCode(114,101,115,95,120,95,57,55,0),233], [String.fromCharCode(112,95,50,49,95,99,97,108,99,117,108,97,116,101,0),266], [String.fromCharCode(98,97,99,107,112,116,114,95,119,95,53,57,0),545]]);
         floating0.push(injuryQ.size - questi.length);
         sliderw = new Map([[`${sliderw.size}`, 1]]);
         injuryQ = new Map([[`${sliderw.size}`, sliderw.size >> (Math.min(Math.abs(1), 2))]]);
         break;
      }
       let bridgej = true;
      for (let s = 0; s < 3; s++) {
         logom = "3";
      }
      if ((questi.length * 4) < 1) {
         questi.push(((bridgej ? 1 : 1) - floating0.length));
      }
         floating0.push(2 / (Math.max(5, logom.length)));
      ping2.set(`${floating0.length}`, 3 + floating0.length);
      mutedo = `${ping2.size}`;
   if (Array.from(penaltye.keys()).includes(`${ping2.size}`)) {
      penaltye.set(`${nativeexi}`, nativeexi.length);
   }
   for (let w = 0; w < 3; w++) {
      mutedo += `${3 ^ catagoryh.length}`;
   }
       let mbbidS = String.fromCharCode(101,95,53,52,95,115,101,114,118,105,99,101,115,0);
       let areaW = String.fromCharCode(106,95,55,50,95,114,101,119,114,105,116,101,0);
          let sinaT = String.fromCharCode(115,99,101,110,97,114,105,111,95,102,95,50,57,0);
         mbbidS += `${mbbidS.length % (Math.max(9, areaW.length))}`;
         sinaT += `${sinaT.length * 2}`;
         mbbidS = "3";
      for (let z = 0; z < 1; z++) {
         mbbidS += `${areaW.length & mbbidS.length}`;
      }
      for (let m = 0; m < 2; m++) {
          let temperaturej = 2.0;
          let changeX: Array<any> = [663, 37];
         areaW = "1";
         temperaturej /= Math.max(parseFloat(`${parseInt(`${temperaturej}`) ^ changeX.length}`), 1);
         changeX.push(1 - changeX.length);
      }
      let textg = 8685130 >= areaW.length;
      do {
          let smallv = 3;
          let zhubol = 3.0;
          let minivodE = String.fromCharCode(120,95,57,51,95,115,121,115,99,116,108,115,0);
         areaW += `${minivodE.length >> (Math.min(Math.abs(3), 5))}`;
         smallv %= Math.max(3, 3 - smallv);
         zhubol /= Math.max(smallv >> (Math.min(4, Math.abs(3))), 3);
         minivodE = `${parseInt(`${zhubol}`)}`;
         if (textg) {
            break;
         }
      } while (textg && (areaW.length > 2));
      if (mbbidS.includes(areaW)) {
         mbbidS = `${areaW.length}`;
      }
      event6 /= Math.max(1, parseFloat(`${securityF.size}`));
      ping2.set(`${mutedo}`, mutedo.length + 3);
   for (let c = 0; c < 2; c++) {
      mutedo = `${3 + catagoryh.length}`;
   }
   let switch_rqw = mutedo.length <= 5271003;
   do {
      mutedo = `${nativeexi.length & 2}`;
      if (switch_rqw) {
         break;
      }
   } while ((mutedo.length == 5) && switch_rqw);
   let headerN = penaltye.size >= 7072893;
   do {
       let lineK = 1;
       let smallJ = String.fromCharCode(122,95,55,53,95,97,115,99,111,110,102,0);
       let mathD = false;
       let starO = String.fromCharCode(109,95,49,57,95,102,105,120,116,117,114,101,0);
      while (starO.length < 5) {
          let downloadedf = String.fromCharCode(105,110,99,114,101,109,101,110,116,97,108,95,106,95,54,52,0);
          let formL: Array<any> = [String.fromCharCode(100,95,55,56,95,114,97,110,107,105,110,103,0), String.fromCharCode(120,95,52,51,95,116,119,111,112,97,115,115,0)];
         lineK ^= starO.length;
         downloadedf = `${1 % (Math.max(10, downloadedf.length))}`;
         formL = [downloadedf.length];
         break;
      }
      let auto_xyH = mathD ? !mathD : mathD;
      do {
         mathD = 50 >= lineK;
         if (auto_xyH) {
            break;
         }
      } while (auto_xyH && (!mathD && starO.length < 1));
         smallJ += `${(starO == String.fromCharCode(72,0) ? starO.length : smallJ.length)}`;
         smallJ += `${(starO == String.fromCharCode(122,0) ? starO.length : (mathD ? 2 : 3))}`;
          let favoriteF = true;
          let station9 = String.fromCharCode(121,95,51,54,95,98,108,101,110,100,109,111,100,101,0);
         mathD = smallJ.length > 46;
         favoriteF = !station9.endsWith(`${favoriteF}`);
         station9 += "1";
       let colors4 = 0;
       let file4 = 4;
         starO += `${colors4 | file4}`;
       let whitei = String.fromCharCode(120,95,54,48,95,116,110,112,117,116,0);
       let bottomB = String.fromCharCode(98,95,51,56,95,112,114,101,115,115,101,100,0);
          let searchbarN = String.fromCharCode(120,95,54,50,95,105,115,115,117,101,100,0);
          let preview0 = true;
         mathD = 23 < searchbarN.length;
         searchbarN = "2";
          let time_b6 = 0.0;
          let reducer2 = 3.0;
          let combineB: Map<any, any> = new Map([[String.fromCharCode(97,95,51,54,95,108,101,103,101,110,100,0),566], [String.fromCharCode(111,95,51,48,95,109,101,114,103,101,97,98,108,101,0),625], [String.fromCharCode(117,95,49,54,95,114,116,109,112,100,104,0),522]]);
         whitei = `${whitei.length}`;
         time_b6 -= parseFloat(`${parseInt(`${reducer2}`) ^ 3}`);
         reducer2 -= parseFloat(`${2}`);
         combineB.set(`${time_b6}`, 2 / (Math.max(parseInt(`${time_b6}`), 10)));
      for (let u = 0; u < 1; u++) {
          let placeholderC: Array<any> = [472, 414, 724];
          let clockg = String.fromCharCode(102,117,108,108,121,95,101,95,53,54,0);
          let customB: Map<any, any> = new Map([[String.fromCharCode(113,99,101,108,112,95,52,95,56,55,0),String.fromCharCode(115,95,51,50,95,109,101,109,98,101,114,115,0)], [String.fromCharCode(117,95,53,55,95,112,97,121,109,97,115,116,101,114,0),String.fromCharCode(103,95,55,51,95,109,97,112,112,105,110,103,115,0)]]);
          let orangeY = 5;
         whitei += `${bottomB.length}`;
         placeholderC = [clockg.length & customB.size];
         clockg += `${clockg.length}`;
         customB = new Map([[clockg, orangeY & clockg.length]]);
         orangeY += orangeY;
      }
      while ((4 * file4) <= 3 && (whitei.length * 4) <= 4) {
         file4 /= Math.max(2, (lineK + (mathD ? 2 : 5)));
         break;
      }
      penaltye.set(starO, catagoryh.length);
      if (headerN) {
         break;
      }
   } while ((3 >= catagoryh.length) && headerN);
   if (penaltye.size <= 2) {
       let mbnativeadvancedx = 3;
      let resendV = 8685866 <= mbnativeadvancedx;
      do {
         mbnativeadvancedx /= Math.max(4, 1 + mbnativeadvancedx);
         if (resendV) {
            break;
         }
      } while ((2 == (mbnativeadvancedx + mbnativeadvancedx)) && resendV);
      for (let f = 0; f < 2; f++) {
         mbnativeadvancedx ^= mbnativeadvancedx ^ 2;
      }
      while (mbnativeadvancedx == mbnativeadvancedx) {
         mbnativeadvancedx /= Math.max(3, mbnativeadvancedx);
         break;
      }
      penaltye = new Map([[`${penaltye.size}`, ((interstitialD ? 3 : 3) + penaltye.size)]]);
   }
       let google6 = String.fromCharCode(117,117,105,100,117,115,109,116,95,54,95,55,49,0);
       let lessA = String.fromCharCode(109,95,56,57,0);
       let steps = 4;
      for (let h = 0; h < 2; h++) {
          let reactnativejsg = String.fromCharCode(112,114,111,103,114,101,115,115,101,115,95,121,95,50,50,0);
          let reminderp = String.fromCharCode(100,105,115,115,111,108,118,101,95,104,95,50,56,0);
          let telegramj = false;
          let actions_ = 3.0;
          let clubO = 5;
         lessA = `${clubO >> (Math.min(2, Math.abs(3)))}`;
         reactnativejsg += `${1 >> (Math.min(Math.abs(parseInt(`${actions_}`)), 5))}`;
         reminderp += "3";
         telegramj = reminderp.length >= 62;
         actions_ -= ((telegramj ? 4 : 4));
         clubO ^= 1;
      }
          let huaweiW = 2;
         lessA = "1";
         huaweiW <<= Math.min(Math.abs(huaweiW >> (Math.min(Math.abs(3), 4))), 2);
      let typesw = google6 == String.fromCharCode(49,57,56,112,51,114,117,112,54,0);
      do {
          let splashe = String.fromCharCode(116,111,110,95,122,95,51,54,0);
          let shoota = 3.0;
          let casting1: Map<any, any> = new Map([[String.fromCharCode(119,95,52,55,95,99,97,109,112,97,105,103,110,0),String.fromCharCode(110,95,54,57,95,111,108,97,110,97,0)], [String.fromCharCode(115,101,99,117,114,101,100,95,97,95,52,54,0),String.fromCharCode(112,114,111,102,105,108,101,95,101,95,56,50,0)], [String.fromCharCode(101,109,98,101,100,100,105,110,103,95,102,95,49,48,0),String.fromCharCode(114,101,102,101,114,114,101,114,95,57,95,53,50,0)]]);
          let styleO = 0;
         google6 += `${1 << (Math.min(2, google6.length))}`;
         splashe = `${splashe.length + parseInt(`${shoota}`)}`;
         shoota *= (String.fromCharCode(70,0) == splashe ? styleO : splashe.length);
         casting1.set(`${styleO}`, casting1.size);
         if (typesw) {
            break;
         }
      } while ((2 < (google6.length / (Math.max(5, steps)))) && typesw);
      if (google6.length < 1) {
          let connection4 = 2;
          let viewere = String.fromCharCode(116,97,105,108,95,105,95,54,51,0);
          let goalI = 2.0;
          let pointZ = 4;
         steps <<= Math.min(5, Math.abs(connection4 >> (Math.min(Math.abs(parseInt(`${goalI}`)), 4))));
         connection4 -= pointZ;
         viewere = `${pointZ}`;
         goalI += 3 + pointZ;
      }
      if (lessA == google6) {
         google6 += `${(google6 == String.fromCharCode(78,0) ? steps : google6.length)}`;
      }
      let componentj = String.fromCharCode(118,109,52,52,55,114,0) == lessA;
      do {
         lessA += `${lessA.length ^ 2}`;
         if (componentj) {
            break;
         }
      } while ((google6 != String.fromCharCode(55,0)) && componentj);
       let smallG = String.fromCharCode(110,95,50,50,95,120,103,97,115,0);
       let questx = String.fromCharCode(102,116,118,110,111,100,101,95,51,95,55,55,0);
      for (let w = 0; w < 2; w++) {
         steps %= Math.max(2 << (Math.min(3, questx.length)), 2);
      }
      if (questx.includes(`${steps}`)) {
         questx += `${steps}`;
      }
      securityF.set(`${catagoryh.length}`, penaltye.size - 2);
   for (let k = 0; k < 1; k++) {
      mutedo += `${(String.fromCharCode(88,0) == nativeexi ? securityF.size : nativeexi.length)}`;
   }
      nativeexi += `${parseInt(`${rulesn}`)}`;

      await AsyncStorage.setItem("termsAccepted", "true");
    };

    if (accepted) {
      acceptTerms();
      
    }
  }, [accepted]);

  return (
    <TermsAcceptContext.Provider value={value}>
      {children}
    </TermsAcceptContext.Provider>
  );
};
