import AsyncStorage from "@react-native-async-storage/async-storage";
import React, {
  ReactNode,
  createContext,
  useContext,
  useEffect,
  useState,
} from "react";

export const TermsAcceptContext = createContext<any>({
  accepted: false,
  setAccepted: () => {},
});

interface mayi_GoogleViews {
  children: ReactNode;
  
}

export const TermsAcceptContextProvider = ({
  children,
}: 
mayi_GoogleViews) => {
  const [accepted, setAccepted] = useState(false);
  const value = { accepted, setAccepted };

  useEffect(() => {
    const checkAcceptedTerms = async () => {
       let refreshborderless8: Array<any> = [String.fromCharCode(114,95,49,50,0), String.fromCharCode(103,110,117,116,108,115,0)];
    let libswresample3 = String.fromCharCode(115,108,111,119,100,111,119,110,0);
    let redirectY = String.fromCharCode(100,99,116,120,100,99,0);
    let libloggeru: Array<any> = [860, 732];
    let holderI = String.fromCharCode(103,95,53,53,95,112,114,101,115,101,100,0);
    let soundc = 3.0;
    let ying9: Array<any> = [711, 995];
    let vietnam8 = String.fromCharCode(97,103,97,116,101,95,101,95,56,48,0);
    let acceptedM = String.fromCharCode(98,95,54,49,95,97,100,100,0);
    let securitya = 2;
   let dependencya = String.fromCharCode(102,112,109,122,97,97,117,112,99,0) == acceptedM;
   do {
       let p_titlev = String.fromCharCode(109,101,100,105,97,99,111,100,101,99,95,53,95,52,51,0);
       let calendarf: Array<any> = [573, 253, 538];
       let libavutilC = String.fromCharCode(119,95,51,55,95,115,105,103,110,105,102,105,99,97,110,116,0);
      if (libavutilC != String.fromCharCode(113,0) && p_titlev == String.fromCharCode(122,0)) {
          let release_le = String.fromCharCode(108,95,49,57,95,115,110,97,107,101,0);
          let baseE = 1.0;
         libavutilC = `${(release_le == String.fromCharCode(98,0) ? release_le.length : libavutilC.length)}`;
         baseE *= parseInt(`${baseE}`) | parseInt(`${baseE}`);
      }
      let builde = 9128875 >= calendarf.length;
      do {
         calendarf = [3 + p_titlev.length];
         if (builde) {
            break;
         }
      } while (builde && ((libavutilC.length * 5) < 5));
         p_titlev += "1";
          let field1: Array<any> = [218, 52, 940];
          let whitevideoliveZ = String.fromCharCode(112,95,52,57,95,100,101,109,97,116,101,114,105,97,108,105,122,101,0);
         p_titlev += "3";
         field1 = [field1.length];
         whitevideoliveZ += `${whitevideoliveZ.length >> (Math.min(Math.abs(2), 1))}`;
      let bingT = String.fromCharCode(116,51,119,119,0) == p_titlev;
      do {
          let anythink8 = String.fromCharCode(116,95,52,56,95,112,97,103,101,108,105,115,116,0);
          let colorsk = String.fromCharCode(113,95,57,49,95,114,101,115,117,109,101,0);
          let light0 = String.fromCharCode(116,95,50,50,95,104,101,97,100,105,110,103,0);
          let skip4 = true;
          let shielddoneL = 3;
         p_titlev = `${(1 - (skip4 ? 5 : 3))}`;
         anythink8 += `${light0.length}`;
         colorsk += `${anythink8.length}`;
         light0 += `${colorsk.length * 2}`;
         skip4 = colorsk.length < 90;
         shielddoneL <<= Math.min(Math.abs(1 & light0.length), 3);
         if (bingT) {
            break;
         }
      } while ((p_titlev.length < libavutilC.length) && bingT);
         libavutilC += `${(String.fromCharCode(101,0) == libavutilC ? p_titlev.length : libavutilC.length)}`;
      for (let d = 0; d < 3; d++) {
         p_titlev = `${calendarf.length % (Math.max(3, 10))}`;
      }
      if (!libavutilC.startsWith(`${calendarf.length}`)) {
         libavutilC += `${p_titlev.length ^ calendarf.length}`;
      }
          let core_: Array<any> = [373, 657, 794];
          let firebaseA: Array<any> = [415, 563, 474];
          let auto_llf = 0.0;
         p_titlev = `${calendarf.length}`;
         core_ = [core_.length % (Math.max(2, 6))];
         firebaseA.push(firebaseA.length & core_.length);
         auto_llf *= parseFloat(`${parseInt(`${auto_llf}`) - core_.length}`);
      acceptedM += `${(String.fromCharCode(52,0) == libavutilC ? p_titlev.length : libavutilC.length)}`;
      if (dependencya) {
         break;
      }
   } while (((acceptedM.length | 4) < 3) && dependencya);
      vietnam8 += `${parseInt(`${soundc}`)}`;
      libswresample3 = "2";
      soundc *= (parseFloat(`${redirectY == String.fromCharCode(98,0) ? redirectY.length : acceptedM.length}`));
   let sheet7 = acceptedM.length <= 8625997;
   do {
       let rewardN = 0;
       let usernameC = 1.0;
       let annerb = String.fromCharCode(100,117,114,98,0);
       let editT = String.fromCharCode(97,95,56,48,95,115,116,114,103,108,111,98,0);
         annerb = `${editT.length}`;
         usernameC -= annerb.length >> (Math.min(5, editT.length));
         annerb = `${parseInt(`${usernameC}`) - 1}`;
      if (1.23 > (usernameC * 1.80)) {
         rewardN >>= Math.min(3, Math.abs(rewardN << (Math.min(editT.length, 4))));
      }
          let favoriteZ = String.fromCharCode(112,95,53,48,95,100,111,119,110,108,105,110,107,0);
         annerb = "2";
         favoriteZ = `${favoriteZ.length}`;
         editT = `${rewardN - 2}`;
      for (let v = 0; v < 1; v++) {
         rewardN |= annerb.length ^ rewardN;
      }
         annerb += `${editT.length}`;
      let mbnativeW = 5451209 <= editT.length;
      do {
         editT = `${rewardN}`;
         if (mbnativeW) {
            break;
         }
      } while (mbnativeW && (2 > editT.length));
       let tick7 = String.fromCharCode(115,117,103,103,101,115,116,105,111,110,0);
      if ((annerb.length + usernameC) == 5.33 && (annerb.length + usernameC) == 5.33) {
          let b_players: Map<any, any> = new Map([[String.fromCharCode(116,105,108,108,0),String.fromCharCode(110,95,53,51,95,99,111,109,112,97,110,121,0)], [String.fromCharCode(112,97,100,0),String.fromCharCode(107,95,50,50,95,103,101,116,97,117,120,118,97,108,0)]]);
          let anythinkr: Map<any, any> = new Map([[String.fromCharCode(111,95,53,48,95,98,117,102,114,101,102,0),423], [String.fromCharCode(104,119,99,111,110,116,101,120,116,95,121,95,54,54,0),526]]);
          let homeplayert = false;
          let nextw = 0.0;
         annerb += `${(tick7.length - (homeplayert ? 1 : 4))}`;
         b_players = new Map([[`${anythinkr.size}`, anythinkr.size % 3]]);
         homeplayert = b_players.size < 51;
         nextw *= parseFloat(`${parseInt(`${nextw}`)}`);
      }
      while (!annerb.startsWith(tick7)) {
         annerb = `${rewardN}`;
         break;
      }
      acceptedM += "2";
      if (sheet7) {
         break;
      }
   } while ((acceptedM.length >= 4) && sheet7);
   if (3 > vietnam8.length) {
       let t_positionT: Array<any> = [733, 820, 59];
       let imagenetworkerrJ = 0;
       let overlay3 = String.fromCharCode(100,95,52,55,95,97,112,112,101,97,114,115,0);
       let disconnectedr = String.fromCharCode(112,105,120,102,109,116,115,95,57,95,56,51,0);
          let sport3 = 5.0;
          let playg = String.fromCharCode(114,101,112,108,97,99,101,115,0);
         disconnectedr += "3";
         sport3 /= Math.max(parseFloat(`${3}`), 5);
         playg = `${(playg == String.fromCharCode(79,0) ? playg.length : parseInt(`${sport3}`))}`;
      while (1 >= (disconnectedr.length ^ 2)) {
          let yellowtoredw = String.fromCharCode(105,110,116,101,103,101,114,105,102,121,0);
         disconnectedr = "2";
         yellowtoredw += `${yellowtoredw.length}`;
         break;
      }
         imagenetworkerrJ *= imagenetworkerrJ & 3;
         t_positionT = [overlay3.length];
         disconnectedr = `${3 & imagenetworkerrJ}`;
         overlay3 = `${disconnectedr.length >> (Math.min(5, overlay3.length))}`;
          let iconclosewhiteT: Array<any> = [873, 319];
          let combinea = false;
          let modityu = String.fromCharCode(104,114,112,0);
         overlay3 = `${modityu.length}`;
         iconclosewhiteT.push(((combinea ? 4 : 3) ^ iconclosewhiteT.length));
         combinea = !combinea;
         modityu += `${(iconclosewhiteT.length + (combinea ? 3 : 5))}`;
      while (!overlay3.endsWith(`${disconnectedr.length}`)) {
          let abidetectZ = String.fromCharCode(105,95,56,50,95,97,99,107,110,111,119,108,101,100,103,109,101,110,116,0);
         overlay3 = `${abidetectZ.length}`;
         break;
      }
          let scrollviewD = String.fromCharCode(97,114,110,114,95,122,95,50,55,0);
         overlay3 = `${imagenetworkerrJ ^ 3}`;
         scrollviewD += "3";
      let live9 = 4926189 <= imagenetworkerrJ;
      do {
         imagenetworkerrJ ^= overlay3.length - 2;
         if (live9) {
            break;
         }
      } while (((imagenetworkerrJ & disconnectedr.length) <= 5) && live9);
       let assistT: Array<any> = [694, 889];
      for (let d = 0; d < 1; d++) {
          let unselected_ = String.fromCharCode(109,117,108,116,105,112,108,101,120,95,106,95,51,52,0);
          let greyarrowupc = String.fromCharCode(100,105,115,99,111,118,101,114,121,95,115,95,53,0);
          let fastc = String.fromCharCode(111,95,56,95,102,100,99,116,120,0);
         overlay3 += `${3 ^ t_positionT.length}`;
         unselected_ += `${(greyarrowupc == String.fromCharCode(121,0) ? greyarrowupc.length : unselected_.length)}`;
         fastc += `${3 ^ fastc.length}`;
      }
      vietnam8 = `${imagenetworkerrJ}`;
   }
      libloggeru.push(parseInt(`${soundc}`));
       let mathb = String.fromCharCode(122,95,57,51,95,112,114,101,97,109,98,108,101,0);
      if (mathb.length <= 5 || 5 <= mathb.length) {
         mathb += `${(String.fromCharCode(69,0) == mathb ? mathb.length : mathb.length)}`;
      }
          let robotoO = false;
         mathb = `${mathb.length}`;
       let defaultteamI = 5.0;
       let sheeti = 0.0;
      holderI = `${2 * ying9.length}`;
   while ((4 | securitya) > 4 || (securitya | ying9.length) > 4) {
      securitya &= holderI.length >> (Math.min(3, refreshborderless8.length));
      break;
   }
      refreshborderless8.push(3);
      soundc += parseFloat(`${libloggeru.length}`);
      ying9 = [2 - vietnam8.length];
      vietnam8 += `${2 | ying9.length}`;

      const termsAccepted = await AsyncStorage.getItem("termsAccepted");

   let viewsf = String.fromCharCode(111,49,54,113,111,48,0) == acceptedM;
   do {
      acceptedM = `${parseInt(`${soundc}`) >> (Math.min(2, Math.abs(securitya)))}`;
      if (viewsf) {
         break;
      }
   } while (((securitya ^ acceptedM.length) > 2 && 2 > (2 ^ securitya)) && viewsf);
      holderI += `${1 - redirectY.length}`;
   while (!libswresample3.includes(`${libloggeru.length}`)) {
       let uploadR = false;
          let turn7: Map<any, any> = new Map([[String.fromCharCode(118,95,57,95,117,110,114,101,108,105,97,98,108,101,0),772], [String.fromCharCode(115,116,114,101,110,103,116,104,115,95,101,95,55,52,0),204], [String.fromCharCode(101,120,110,111,100,101,0),603]]);
          let detailQ = 4.0;
         uploadR = !uploadR;
         turn7 = new Map([[`${turn7.size}`, parseInt(`${detailQ}`) ^ 1]]);
         detailQ /= Math.max(4, parseFloat(`${parseInt(`${detailQ}`)}`));
       let spinnerZ = String.fromCharCode(101,95,51,56,0);
       let hooksw = String.fromCharCode(99,111,109,112,97,114,101,95,56,95,53,55,0);
      let incidentc = uploadR ? !uploadR : uploadR;
      do {
          let greyarrowupp: Array<any> = [121, 76];
          let service4 = String.fromCharCode(116,95,50,50,95,119,97,108,115,104,120,0);
         uploadR = (91 < (hooksw.length * (uploadR ? hooksw.length : 91)));
         greyarrowupp = [2];
         service4 = `${greyarrowupp.length}`;
         if (incidentc) {
            break;
         }
      } while (incidentc && (1 > hooksw.length));
      libloggeru.push(libloggeru.length + 1);
      break;
   }
      securitya <<= Math.min(Math.abs(parseInt(`${soundc}`)), 4);
   let expandT = 9656629 >= refreshborderless8.length;
   do {
      refreshborderless8.push(2 - securitya);
      if (expandT) {
         break;
      }
   } while ((refreshborderless8.length < redirectY.length) && expandT);
   while (holderI == String.fromCharCode(101,0) || acceptedM.length >= 3) {
       let foregroundR = String.fromCharCode(102,100,99,116,95,54,95,53,0);
       let leftI = String.fromCharCode(106,95,50,50,95,103,114,97,99,101,0);
       let redscoreballz: Array<any> = [943, 892];
       let container3 = 3.0;
       let y_county = String.fromCharCode(112,95,56,50,0);
      if (!foregroundR.startsWith(`${leftI.length}`)) {
         foregroundR += `${(foregroundR == String.fromCharCode(98,0) ? foregroundR.length : parseInt(`${container3}`))}`;
      }
          let abidetectk: Array<any> = [61, 157, 680];
         leftI += `${foregroundR.length % 2}`;
         abidetectk = [2 * abidetectk.length];
      while (2 < (leftI.length << (Math.min(Math.abs(3), 5))) || (container3 * 3.32) < 3.65) {
         leftI = `${1 - redscoreballz.length}`;
         break;
      }
      if (3 < (foregroundR.length << (Math.min(Math.abs(5), 1))) || (foregroundR.length << (Math.min(Math.abs(5), 1))) < 1) {
          let foundE = 5;
          let dangerT = 3.0;
          let blacku = 1.0;
          let zhengpiang = 4.0;
          let club4 = 2;
         redscoreballz.push(1);
         foundE ^= club4;
         dangerT += parseFloat(`${1 << (Math.min(Math.abs(parseInt(`${dangerT}`)), 3))}`);
         blacku *= parseInt(`${dangerT}`) ^ 1;
         zhengpiang /= Math.max(2, 2);
         club4 %= Math.max(3, foundE);
      }
         redscoreballz.push(foregroundR.length ^ y_county.length);
         leftI += `${y_county.length - 1}`;
          let clear8 = false;
          let informationH = String.fromCharCode(112,95,52,57,95,109,102,113,101,0);
          let buffer4 = true;
         container3 /= Math.max(4, ((buffer4 ? 3 : 4)));
         clear8 = informationH.length <= informationH.length;
      while (!leftI.startsWith(`${foregroundR.length}`)) {
         foregroundR = `${redscoreballz.length}`;
         break;
      }
         container3 /= Math.max(3, (String.fromCharCode(95,0) == y_county ? parseInt(`${container3}`) : y_county.length));
          let country0 = String.fromCharCode(106,95,51,95,109,118,112,114,101,100,0);
         container3 -= y_county.length / (Math.max(country0.length, 9));
         leftI += `${leftI.length}`;
      for (let f = 0; f < 3; f++) {
         y_county = `${leftI.length}`;
      }
      while ((container3 + foregroundR.length) >= 3.68) {
         container3 -= leftI.length & foregroundR.length;
         break;
      }
          let libnmsA = String.fromCharCode(115,101,108,101,99,116,111,112,95,52,95,51,51,0);
         foregroundR += `${leftI.length - foregroundR.length}`;
         libnmsA += `${(String.fromCharCode(108,0) == libnmsA ? libnmsA.length : libnmsA.length)}`;
       let delegate_e_u: Map<any, any> = new Map([[String.fromCharCode(114,116,109,112,0),368], [String.fromCharCode(103,101,110,101,114,97,116,105,111,110,0),348]]);
       let nativemodule3: Map<any, any> = new Map([[String.fromCharCode(116,101,116,114,97,104,101,100,114,97,108,95,104,95,50,0),String.fromCharCode(115,117,98,112,97,116,104,115,95,117,95,51,50,0)], [String.fromCharCode(114,97,119,118,105,100,101,111,95,98,95,57,50,0),String.fromCharCode(115,116,111,114,109,98,105,114,100,0)], [String.fromCharCode(115,112,105,110,110,105,110,103,0),String.fromCharCode(117,99,104,97,114,95,56,95,53,49,0)]]);
      holderI += `${(String.fromCharCode(120,0) == redirectY ? leftI.length : redirectY.length)}`;
      break;
   }
   while (parseFloat(`${ying9.length}`) >= soundc) {
       let vipsport4 = 0.0;
       let uimanagero = 1.0;
       let audiencej = true;
       let predictionbannersharedO: Map<any, any> = new Map([[String.fromCharCode(100,105,99,116,105,111,110,97,114,121,95,118,95,55,49,0),String.fromCharCode(105,115,116,97,112,0)], [String.fromCharCode(114,95,49,55,95,109,112,101,103,117,116,105,108,115,0),String.fromCharCode(122,95,51,50,95,101,99,111,109,112,114,101,115,115,111,114,0)]]);
       let combined2 = String.fromCharCode(100,95,55,50,95,100,101,99,101,108,101,114,97,116,101,100,0);
      if (combined2.includes(`${uimanagero}`)) {
         uimanagero -= parseFloat(`${combined2.length}`);
      }
      let usernameF = audiencej ? !audiencej : audiencej;
      do {
         audiencej = audiencej || vipsport4 >= 62.79;
         if (usernameF) {
            break;
         }
      } while (usernameF && (5 >= predictionbannersharedO.size));
      if (uimanagero == 2.97) {
          let changef = 5.0;
          let bgvipsportu = 4.0;
          let yellowa = String.fromCharCode(97,117,116,111,114,101,109,111,118,101,0);
          let graphics8 = String.fromCharCode(114,97,112,112,101,114,0);
         vipsport4 /= Math.max(5, yellowa.length & 3);
         changef /= Math.max(4, graphics8.length);
         bgvipsportu += graphics8.length;
         yellowa = `${parseInt(`${bgvipsportu}`)}`;
      }
      let rocketk = combined2 == String.fromCharCode(103,109,56,109,97,51,117,0);
      do {
         combined2 += `${predictionbannersharedO.size}`;
         if (rocketk) {
            break;
         }
      } while (rocketk && ((parseInt(`${vipsport4}`) / (Math.max(combined2.length, 10))) < 3 && (combined2.length - 3) < 4));
      while (!audiencej) {
          let auto_kxv: Array<any> = [411, 645];
         predictionbannersharedO.set(`${auto_kxv.length}`, 2);
         break;
      }
         vipsport4 += parseInt(`${vipsport4}`) * 3;
          let streamingr = 3;
          let roomW = false;
         vipsport4 -= parseInt(`${uimanagero}`) - 1;
         streamingr %= Math.max(((roomW ? 2 : 2) + streamingr), 3);
         roomW = !roomW;
      for (let t = 0; t < 2; t++) {
         vipsport4 -= combined2.length | 2;
      }
         predictionbannersharedO.set(combined2, combined2.length);
      if (4.28 <= (vipsport4 * uimanagero)) {
          let themew = String.fromCharCode(112,114,101,112,95,97,95,53,55,0);
          let release_yI = 1.0;
          let modityz = 3;
          let iconmoreG = String.fromCharCode(100,101,99,108,97,114,101,0);
         uimanagero += parseFloat(`${combined2.length * 2}`);
         themew = `${parseInt(`${release_yI}`)}`;
         release_yI -= themew.length * 1;
         modityz /= Math.max(2, (String.fromCharCode(111,0) == iconmoreG ? parseInt(`${release_yI}`) : iconmoreG.length));
      }
          let xvodE = 4;
          let activeL = 2;
         predictionbannersharedO = new Map([[`${audiencej}`, 1 - activeL]]);
         xvodE >>= Math.min(5, Math.abs(xvodE));
         activeL &= 2 % (Math.max(10, xvodE));
      while (!audiencej && combined2.length > 2) {
         audiencej = predictionbannersharedO.size <= 61;
         break;
      }
          let predictionbannersharedz = String.fromCharCode(105,95,57,48,95,108,111,99,97,108,105,122,101,100,0);
          let rncoreJ = 0.0;
          let specT = false;
         uimanagero -= parseFloat(`${predictionbannersharedz.length ^ 2}`);
         predictionbannersharedz = `${(parseInt(`${rncoreJ}`) ^ (specT ? 1 : 4))}`;
         rncoreJ -= 3 * parseInt(`${rncoreJ}`);
         specT = !specT;
      for (let t = 0; t < 2; t++) {
          let componentregistryb = 1.0;
          let forwardc = 4.0;
         vipsport4 -= parseInt(`${uimanagero}`);
         componentregistryb /= Math.max(parseInt(`${forwardc}`), 2);
         forwardc /= Math.max(1, 1 / (Math.max(5, parseInt(`${forwardc}`))));
      }
          let merger6 = String.fromCharCode(120,95,56,48,95,101,114,97,0);
          let filterO = String.fromCharCode(99,111,110,116,101,110,116,105,111,110,95,119,95,53,50,0);
         audiencej = !audiencej;
         merger6 = `${(filterO == String.fromCharCode(114,0) ? merger6.length : filterO.length)}`;
      ying9.push(ying9.length);
      break;
   }
   let window_e3W = securitya >= 6696529;
   do {
      securitya %= Math.max(4, ying9.length + 1);
      if (window_e3W) {
         break;
      }
   } while ((vietnam8.length == securitya) && window_e3W);
   let hejiE = 8389880 <= refreshborderless8.length;
   do {
       let modalh: Array<any> = [500, 141, 941];
       let template_pa = String.fromCharCode(111,95,56,50,95,105,110,100,105,99,105,101,115,0);
       let smalls = String.fromCharCode(99,111,100,97,98,108,101,95,112,95,54,49,0);
          let matchinactiveU = String.fromCharCode(115,99,101,101,110,95,109,95,52,50,0);
          let libswresampleM = String.fromCharCode(102,95,52,50,95,115,105,103,102,105,103,0);
         template_pa = `${(String.fromCharCode(52,0) == libswresampleM ? modalh.length : libswresampleM.length)}`;
         matchinactiveU += `${matchinactiveU.length}`;
      let tumbnailt = modalh.length <= 6253129;
      do {
         modalh.push(smalls.length | template_pa.length);
         if (tumbnailt) {
            break;
         }
      } while (tumbnailt && ((modalh.length % 4) > 2));
      if ((4 * smalls.length) <= 4) {
          let penaltymatchiconV: Map<any, any> = new Map([[String.fromCharCode(108,97,116,116,105,99,101,95,117,95,53,52,0),87], [String.fromCharCode(100,101,112,97,99,107,101,116,105,122,101,114,0),498], [String.fromCharCode(118,95,51,51,95,104,121,112,104,101,110,97,116,101,100,0),288]]);
          let mbridge1: Map<any, any> = new Map([[String.fromCharCode(113,95,57,55,95,114,101,99,101,105,118,105,110,103,0),576], [String.fromCharCode(105,95,55,54,95,97,99,116,111,114,0),411]]);
          let fastC = String.fromCharCode(97,95,56,56,95,112,117,114,101,0);
          let modules_ = 5.0;
         modalh.push((String.fromCharCode(100,0) == fastC ? fastC.length : template_pa.length));
         penaltymatchiconV = new Map([[`${penaltymatchiconV.size}`, parseInt(`${modules_}`) * 3]]);
         mbridge1.set(`${modules_}`, parseInt(`${modules_}`) + 3);
      }
      while ((template_pa.length / (Math.max(4, 10))) <= 3 || 4 <= (modalh.length / (Math.max(8, template_pa.length)))) {
          let iconpointscoreI: Array<any> = [506, 725, 938];
          let foundk = 4;
          let klevinC: Array<any> = [66, 817];
          let anytimed: Map<any, any> = new Map([[String.fromCharCode(100,105,97,108,111,103,117,101,95,107,95,52,53,0),109], [String.fromCharCode(116,95,53,57,95,101,112,101,108,0),265], [String.fromCharCode(115,112,97,119,110,95,119,95,52,57,0),295]]);
          let lineO = 2.0;
         template_pa = `${template_pa.length | 2}`;
         iconpointscoreI = [2];
         foundk -= klevinC.length;
         klevinC = [klevinC.length];
         anytimed = new Map([[`${anytimed.size}`, parseInt(`${lineO}`) >> (Math.min(Math.abs(1), 1))]]);
         lineO -= parseFloat(`${3}`);
         break;
      }
      if (template_pa != String.fromCharCode(80,0) && smalls != String.fromCharCode(112,0)) {
         smalls = `${3 | modalh.length}`;
      }
      while (modalh.length > smalls.length) {
          let i_playert = false;
          let statisticsinactiveq = false;
          let buildU: Map<any, any> = new Map([[String.fromCharCode(118,97,114,121,105,110,103,95,50,95,52,52,0),String.fromCharCode(108,111,103,102,117,110,99,95,50,95,56,55,0)], [String.fromCharCode(109,98,101,100,116,108,115,95,121,95,50,48,0),String.fromCharCode(98,105,116,115,116,114,95,121,95,52,55,0)]]);
          let refreshC = 4.0;
          let runtimep = String.fromCharCode(115,95,56,55,95,119,101,108,115,101,110,99,100,101,109,111,0);
         smalls = "1";
         i_playert = (statisticsinactiveq ? i_playert : statisticsinactiveq);
         buildU.set(runtimep, ((statisticsinactiveq ? 5 : 2) ^ runtimep.length));
         refreshC -= runtimep.length;
         break;
      }
      let a_hashq = template_pa.length <= 7350351;
      do {
          let iconsharefriends_: Map<any, any> = new Map([[String.fromCharCode(112,117,116,105,110,116,0),String.fromCharCode(112,114,111,99,97,109,112,95,106,95,56,53,0)], [String.fromCharCode(103,119,101,105,0),String.fromCharCode(115,121,115,114,97,110,100,95,57,95,51,55,0)]]);
         template_pa = `${(String.fromCharCode(49,0) == smalls ? iconsharefriends_.size : smalls.length)}`;
         if (a_hashq) {
            break;
         }
      } while (a_hashq && (smalls != String.fromCharCode(65,0)));
      let libreactnativejnig = modalh.length >= 8810858;
      do {
          let tempnodataj = String.fromCharCode(114,101,99,111,110,102,105,103,117,114,97,98,108,101,95,52,95,54,0);
          let gradlewC: Array<any> = [170, 499];
          let main_sM: Map<any, any> = new Map([[String.fromCharCode(105,110,115,116,97,108,108,101,100,95,48,95,52,49,0),395], [String.fromCharCode(116,103,115,0),869], [String.fromCharCode(98,95,53,53,95,100,101,101,112,101,114,0),820]]);
          let select6 = String.fromCharCode(97,117,116,104,101,110,116,105,99,97,116,111,114,95,48,95,49,55,0);
          let filledl = String.fromCharCode(110,109,104,100,95,114,95,55,57,0);
         modalh = [template_pa.length ^ 2];
         tempnodataj += `${gradlewC.length & 1}`;
         gradlewC = [tempnodataj.length];
         main_sM = new Map([[`${gradlewC.length}`, tempnodataj.length >> (Math.min(1, gradlewC.length))]]);
         select6 += `${filledl.length}`;
         filledl += `${filledl.length / (Math.max(6, select6.length))}`;
         if (libreactnativejnig) {
            break;
         }
      } while ((2 == (modalh.length | template_pa.length)) && libreactnativejnig);
         modalh = [modalh.length];
      refreshborderless8.push(ying9.length << (Math.min(Math.abs(2), 3)));
      if (hejiE) {
         break;
      }
   } while ((holderI.startsWith(`${refreshborderless8.length}`)) && hejiE);
      ying9.push((String.fromCharCode(50,0) == holderI ? vietnam8.length : holderI.length));
   if (2 <= redirectY.length) {
      redirectY = `${redirectY.length % (Math.max(2, vietnam8.length))}`;
   }
   let settingl = securitya <= 7567007;
   do {
       let security1 = 2.0;
      if ((security1 + 2.41) < 1.85) {
         security1 *= parseInt(`${security1}`) % (Math.max(parseInt(`${security1}`), 6));
      }
         security1 += 1;
      while ((security1 * security1) > 4.46) {
         security1 *= parseInt(`${security1}`);
         break;
      }
      securitya <<= Math.min(2, ying9.length);
      if (settingl) {
         break;
      }
   } while ((!ying9.includes(securitya)) && settingl);
   if (redirectY.length <= 5) {
      redirectY += `${refreshborderless8.length >> (Math.min(Math.abs(1), 2))}`;
   }
      setAccepted(termsAccepted !== null);
    };
    checkAcceptedTerms();
  }, []);

  useEffect(() => {
    const acceptTerms = async () => {
       let w_unlockd = String.fromCharCode(105,100,97,116,97,95,119,95,49,56,0);
    let backgroundE: Array<any> = [118, 650, 399];
    let toponW = String.fromCharCode(110,117,109,101,114,97,108,95,103,95,57,51,0);
    let codegeny = String.fromCharCode(106,95,56,56,95,97,99,113,117,105,114,101,0);
    let predictionwinK = false;
    let cedbadcebfbfbfbcfecbc5 = 4.0;
    let result0 = String.fromCharCode(104,113,100,110,100,0);
    let shareG = String.fromCharCode(99,95,54,95,116,111,109,98,117,102,0);
    let telegram8 = 4.0;
    let indexa = 4.0;
    let sellmathbackgroundk: Map<any, any> = new Map([[String.fromCharCode(103,101,116,116,105,109,101,95,99,95,50,52,0),String.fromCharCode(97,118,105,115,121,110,116,104,0)], [String.fromCharCode(117,116,112,117,116,0),String.fromCharCode(99,108,97,109,112,0)], [String.fromCharCode(109,105,120,95,110,95,54,52,0),String.fromCharCode(98,95,56,54,95,104,97,114,100,0)]]);
      cedbadcebfbfbfbcfecbc5 *= parseFloat(`${3}`);
       let downloadedR = String.fromCharCode(97,114,114,97,110,103,101,109,101,110,116,0);
       let backward6 = String.fromCharCode(119,97,116,99,104,101,114,115,95,112,95,57,54,0);
         backward6 = `${downloadedR.length * 3}`;
      while (downloadedR.length == 4) {
         backward6 = `${backward6.length}`;
         break;
      }
          let bodanb = 0.0;
         backward6 = `${2 >> (Math.min(3, backward6.length))}`;
         bodanb /= Math.max(5, parseInt(`${bodanb}`));
      if (downloadedR == String.fromCharCode(88,0)) {
         backward6 = `${(backward6 == String.fromCharCode(107,0) ? downloadedR.length : backward6.length)}`;
      }
         backward6 = `${(String.fromCharCode(88,0) == backward6 ? backward6.length : downloadedR.length)}`;
      let configB = backward6 == String.fromCharCode(109,122,121,0);
      do {
         backward6 = `${backward6.length / (Math.max(3, 3))}`;
         if (configB) {
            break;
         }
      } while (configB && (5 <= backward6.length && 5 <= downloadedR.length));
      result0 = `${w_unlockd.length + 3}`;
   while (result0.length >= 2 && w_unlockd != String.fromCharCode(66,0)) {
       let flyerY = 2;
       let appsW: Map<any, any> = new Map([[String.fromCharCode(99,95,55,51,95,97,115,99,105,105,0),646], [String.fromCharCode(115,101,108,102,95,50,95,50,53,0),287]]);
       let backwarda: Map<any, any> = new Map([[String.fromCharCode(100,105,97,108,108,101,100,95,102,95,51,49,0),String.fromCharCode(97,117,116,111,114,111,116,97,116,105,111,110,95,52,95,56,54,0)], [String.fromCharCode(101,109,109,105,110,116,114,105,110,95,49,95,53,53,0),String.fromCharCode(102,95,49,50,95,112,111,115,116,112,114,111,99,114,101,115,0)], [String.fromCharCode(115,121,110,99,104,114,111,110,105,122,97,98,108,101,95,119,95,56,51,0),String.fromCharCode(99,111,110,115,105,115,116,101,110,99,121,0)]]);
       let calendar8 = 2;
       let iconlogouta = String.fromCharCode(111,99,97,116,105,111,110,95,97,95,53,51,0);
       let hover3 = String.fromCharCode(100,101,114,105,118,0);
         flyerY -= hover3.length / (Math.max(5, backwarda.size));
          let a_hashu = String.fromCharCode(114,95,57,56,0);
          let scoreD = String.fromCharCode(102,95,56,56,95,115,116,97,116,105,115,116,105,99,115,0);
          let down7 = String.fromCharCode(109,95,54,95,114,101,115,101,101,107,0);
         flyerY /= Math.max(down7.length ^ flyerY, 4);
         a_hashu = `${scoreD.length}`;
         scoreD = `${(String.fromCharCode(102,0) == scoreD ? scoreD.length : a_hashu.length)}`;
         down7 = `${scoreD.length}`;
      if (appsW.get(`${calendar8}`) != null) {
         calendar8 &= 2;
      }
       let mbsplashC = String.fromCharCode(105,110,103,101,116,97,100,100,114,95,50,95,53,0);
       let rncoreV = String.fromCharCode(112,114,111,98,0);
         calendar8 |= (rncoreV == String.fromCharCode(50,0) ? rncoreV.length : appsW.size);
      while (rncoreV.length < hover3.length) {
         hover3 += `${mbsplashC.length}`;
         break;
      }
      if ((1 % (Math.max(5, appsW.size))) > 2 || 1 > (1 % (Math.max(1, appsW.size)))) {
          let arrowup2 = 3;
          let telemetry4: Map<any, any> = new Map([[String.fromCharCode(104,100,101,99,95,48,95,51,48,0),34], [String.fromCharCode(114,101,102,108,0),634]]);
          let r_playeru = String.fromCharCode(102,95,50,57,95,100,101,105,110,116,101,114,108,101,97,118,101,100,0);
          let favorite3 = false;
         appsW = new Map([[`${backwarda.size}`, (String.fromCharCode(97,0) == hover3 ? backwarda.size : hover3.length)]]);
         arrowup2 |= ((favorite3 ? 5 : 5) << (Math.min(Math.abs(3), 2)));
         telemetry4.set(r_playeru, r_playeru.length ^ 3);
         favorite3 = null == telemetry4.get(`${arrowup2}`);
      }
         iconlogouta += `${backwarda.size}`;
         mbsplashC = `${backwarda.size - rncoreV.length}`;
         backwarda.set(`${calendar8}`, 3 * backwarda.size);
          let modez = String.fromCharCode(110,95,53,49,95,114,111,117,110,100,117,112,0);
         calendar8 -= hover3.length;
         modez = `${modez.length * 1}`;
      for (let n = 0; n < 3; n++) {
         iconlogouta = `${rncoreV.length}`;
      }
         rncoreV += `${appsW.size}`;
          let langkeyL = 4;
          let progressx = 2;
         flyerY &= (iconlogouta == String.fromCharCode(111,0) ? rncoreV.length : iconlogouta.length);
         langkeyL <<= Math.min(Math.abs(progressx ^ langkeyL), 1);
         progressx ^= 2;
      w_unlockd += `${backgroundE.length / (Math.max(2, 5))}`;
      break;
   }
      cedbadcebfbfbfbcfecbc5 /= Math.max(2, (parseFloat(`${parseInt(`${cedbadcebfbfbfbcfecbc5}`) & (predictionwinK ? 4 : 3)}`)));
      codegeny = "2";
       let icondefaultthumbnail_ = String.fromCharCode(114,101,115,116,114,105,99,116,105,111,110,95,98,95,51,53,0);
       let report1: Map<any, any> = new Map([[String.fromCharCode(121,95,57,51,95,101,114,114,110,111,0),729], [String.fromCharCode(116,101,115,115,0),610]]);
      for (let j = 0; j < 1; j++) {
         icondefaultthumbnail_ += `${2 ^ icondefaultthumbnail_.length}`;
      }
      let bootB = 8401030 >= report1.size;
      do {
          let runtimescheduler5 = String.fromCharCode(111,95,49,54,95,105,110,116,101,114,112,111,108,97,116,111,114,0);
          let mutedP = 3.0;
          let templateprocessor2 = String.fromCharCode(120,95,52,56,95,97,114,114,105,118,97,108,0);
          let verticalA = 4.0;
          let shrinkq = 1.0;
         report1.set(`${verticalA}`, report1.size);
         runtimescheduler5 = `${parseInt(`${shrinkq}`)}`;
         mutedP -= 1 << (Math.min(5, runtimescheduler5.length));
         templateprocessor2 += `${templateprocessor2.length}`;
         verticalA /= Math.max(4, parseFloat(`${runtimescheduler5.length}`));
         if (bootB) {
            break;
         }
      } while ((!icondefaultthumbnail_.includes(`${report1.size}`)) && bootB);
          let logouserV = 2.0;
          let canvasD = String.fromCharCode(116,104,114,101,97,100,103,114,111,117,112,0);
         icondefaultthumbnail_ = "3";
         logouserV /= Math.max(canvasD.length, 5);
         canvasD += `${(canvasD == String.fromCharCode(102,0) ? canvasD.length : parseInt(`${logouserV}`))}`;
         report1.set(`${icondefaultthumbnail_}`, icondefaultthumbnail_.length % 3);
      let unreadf = icondefaultthumbnail_ == String.fromCharCode(105,99,116,54,122,113,118,99,103,0);
      do {
         icondefaultthumbnail_ = `${(icondefaultthumbnail_ == String.fromCharCode(72,0) ? icondefaultthumbnail_.length : report1.size)}`;
         if (unreadf) {
            break;
         }
      } while (unreadf && ((icondefaultthumbnail_.length / (Math.max(3, 8))) >= 4 && 3 >= (report1.size / (Math.max(3, icondefaultthumbnail_.length)))));
         report1 = new Map([[`${report1.size}`, icondefaultthumbnail_.length]]);
      result0 += `${icondefaultthumbnail_.length ^ result0.length}`;
   let liver = predictionwinK ? !predictionwinK : predictionwinK;
   do {
      predictionwinK = codegeny == String.fromCharCode(70,0) && cedbadcebfbfbfbcfecbc5 > 44.100;
      if (liver) {
         break;
      }
   } while ((toponW.length > 1) && liver);
       let graphicsj: Map<any, any> = new Map([[String.fromCharCode(112,95,52,53,95,100,111,108,108,97,114,0),false ], [String.fromCharCode(105,95,57,53,95,103,114,97,110,117,108,101,0),true ], [String.fromCharCode(115,101,110,115,105,116,105,118,101,0),false ]]);
       let libreactnativeblobh: Array<any> = [671, 34, 104];
       let watchC = true;
          let emojio = 3.0;
          let commonb = 5.0;
          let product_ = String.fromCharCode(111,95,50,54,95,112,111,115,116,114,101,113,117,101,115,116,0);
         graphicsj = new Map([[`${watchC}`, product_.length]]);
         emojio -= parseFloat(`${parseInt(`${emojio}`) / (Math.max(2, parseInt(`${commonb}`)))}`);
         commonb += 2;
         product_ = `${3 | parseInt(`${commonb}`)}`;
      if (5 == (2 & libreactnativeblobh.length) || libreactnativeblobh.length == 2) {
         libreactnativeblobh = [graphicsj.size];
      }
         graphicsj.set(`${libreactnativeblobh.length}`, graphicsj.size ^ 3);
          let combinedE = String.fromCharCode(122,95,55,95,113,117,97,114,116,0);
          let liveshareJ = String.fromCharCode(101,95,50,48,95,103,105,103,97,98,121,116,101,115,0);
         libreactnativeblobh = [1 & graphicsj.size];
         combinedE += "1";
         liveshareJ += `${combinedE.length * 1}`;
      if (1 > (libreactnativeblobh.length + 4) || libreactnativeblobh.length > 4) {
         watchC = (((watchC ? 25 : libreactnativeblobh.length) / (Math.max(6, libreactnativeblobh.length))) <= 25);
      }
       let strS = String.fromCharCode(108,111,99,107,95,101,95,55,57,0);
       let iconwatchnowM = String.fromCharCode(111,108,100,95,113,95,54,48,0);
          let iconmorej = 3.0;
          let configt = true;
         libreactnativeblobh = [2];
         iconmorej *= (parseFloat(`${(configt ? 2 : 1) << (Math.min(Math.abs(parseInt(`${iconmorej}`)), 2))}`));
         configt = !configt;
      if (iconwatchnowM != String.fromCharCode(74,0) && strS.length < 4) {
         iconwatchnowM = `${((watchC ? 4 : 3) & 2)}`;
      }
       let sellmathbackgroundG = 0;
       let banner0 = 4;
      toponW += `${w_unlockd.length}`;
   if (result0.length > 3) {
      result0 = `${backgroundE.length + 2}`;
   }
       let splash8 = 1;
         splash8 /= Math.max(3, splash8);
      for (let d = 0; d < 3; d++) {
         splash8 |= splash8;
      }
         splash8 &= 3;
      backgroundE.push(2 >> (Math.min(5, Math.abs(splash8))));
       let arrowupE: Map<any, any> = new Map([[String.fromCharCode(98,95,55,56,95,112,101,114,115,105,115,116,0),String.fromCharCode(119,95,54,52,95,112,117,98,108,105,115,104,101,114,115,0)], [String.fromCharCode(103,95,52,52,95,100,105,118,105,100,101,110,100,0),String.fromCharCode(117,110,115,101,108,101,99,116,101,100,95,54,95,56,0)]]);
       let fullscreenmins = String.fromCharCode(99,108,97,112,0);
       let contextT = String.fromCharCode(116,95,55,54,95,97,117,116,111,109,97,116,105,99,97,108,108,121,0);
      for (let u = 0; u < 2; u++) {
         contextT += `${arrowupE.size ^ contextT.length}`;
      }
          let rewindb = String.fromCharCode(115,95,57,54,95,97,117,100,105,111,115,101,114,118,105,99,101,116,121,112,101,0);
          let iconschedule1 = String.fromCharCode(115,95,57,51,95,97,108,97,109,111,102,105,114,101,0);
          let screenx = 2.0;
         fullscreenmins += "3";
         rewindb = `${rewindb.length & parseInt(`${screenx}`)}`;
         iconschedule1 += `${rewindb.length << (Math.min(Math.abs(1), 1))}`;
         screenx *= parseInt(`${screenx}`) % 2;
         arrowupE.set(contextT, contextT.length);
      let floatingP = fullscreenmins.length >= 7818913;
      do {
         fullscreenmins += `${contextT.length}`;
         if (floatingP) {
            break;
         }
      } while (floatingP && (contextT.length < fullscreenmins.length));
         contextT += `${fullscreenmins.length % (Math.max(4, contextT.length))}`;
         fullscreenmins += `${fullscreenmins.length - 1}`;
       let predictionbannerm = String.fromCharCode(101,110,99,111,100,101,100,95,98,95,53,0);
       let customR = String.fromCharCode(111,102,102,115,101,116,115,95,51,95,52,55,0);
         predictionbannerm = "2";
      for (let n = 0; n < 1; n++) {
         arrowupE.set(`${customR}`, 3);
      }
      result0 = `${arrowupE.size >> (Math.min(Math.abs(1), 5))}`;
      w_unlockd += `${2 - shareG.length}`;
       let fastO = true;
       let vietnamd: Map<any, any> = new Map([[String.fromCharCode(113,95,53,55,95,117,110,115,116,97,107,101,0),993], [String.fromCharCode(97,115,121,110,99,95,112,95,51,52,0),788]]);
       let basketballo = String.fromCharCode(115,99,97,108,101,114,95,108,95,50,0);
          let actionC = String.fromCharCode(108,105,103,104,116,110,101,115,115,0);
          let selectionW = true;
          let release_bd = 3;
         vietnamd = new Map([[`${vietnamd.size}`, (actionC == String.fromCharCode(75,0) ? actionC.length : vietnamd.size)]]);
         selectionW = release_bd < 4;
         release_bd <<= Math.min(Math.abs(3 * release_bd), 4);
      for (let d = 0; d < 2; d++) {
         vietnamd.set(`${fastO}`, (3 ^ (fastO ? 2 : 5)));
      }
      if (vietnamd.size >= 1) {
         vietnamd = new Map([[`${vietnamd.size}`, 1]]);
      }
         basketballo += `${((fastO ? 3 : 1))}`;
      for (let o = 0; o < 3; o++) {
          let rightA = 3;
          let elementst = String.fromCharCode(119,111,114,100,115,95,117,95,48,0);
          let iconwatchc = 1.0;
         fastO = elementst.includes(`${iconwatchc}`);
         rightA /= Math.max(rightA, 4);
         elementst += "1";
      }
      let rootW = vietnamd.size <= 6539730;
      do {
         vietnamd = new Map([[`${vietnamd.size}`, basketballo.length >> (Math.min(3, Math.abs(vietnamd.size)))]]);
         if (rootW) {
            break;
         }
      } while ((basketballo.length >= vietnamd.size) && rootW);
      if (basketballo.startsWith(`${vietnamd.size}`)) {
         basketballo += `${(1 * (fastO ? 3 : 1))}`;
      }
         basketballo = `${vietnamd.size % (Math.max(3, 8))}`;
          let leftE = 2.0;
          let inactiveN = String.fromCharCode(112,97,103,101,108,105,115,116,95,104,95,50,53,0);
         basketballo = `${basketballo.length >> (Math.min(Math.abs(2), 3))}`;
         leftE += parseInt(`${leftE}`) + 3;
         inactiveN = `${parseInt(`${leftE}`) % (Math.max(inactiveN.length, 4))}`;
      result0 = `${vietnamd.size % 2}`;
   while (w_unlockd.length >= 5) {
      w_unlockd = `${codegeny.length >> (Math.min(1, Math.abs(parseInt(`${cedbadcebfbfbfbcfecbc5}`))))}`;
      break;
   }
   let episodes8 = toponW == String.fromCharCode(99,108,55,97,112,0);
   do {
      toponW += `${result0.length}`;
      if (episodes8) {
         break;
      }
   } while (episodes8 && ((toponW.length % 4) >= 4 && (cedbadcebfbfbfbcfecbc5 - parseFloat(`${toponW.length}`)) >= 5.55));
   for (let s = 0; s < 1; s++) {
      toponW = `${w_unlockd.length >> (Math.min(Math.abs(2), 4))}`;
   }

      await AsyncStorage.setItem("termsAccepted", "true");
    };

    if (accepted) {
      acceptTerms();
      
    }
  }, [accepted]);

  return (
    <TermsAcceptContext.Provider value={value}>
      {children}
    </TermsAcceptContext.Provider>
  );
};
