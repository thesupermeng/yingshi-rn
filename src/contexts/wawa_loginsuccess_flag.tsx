import AsyncStorage from "@react-native-async-storage/async-storage";
import React, {
  ReactNode,
  createContext,
  useContext,
  useEffect,
  useState,
} from "react";

export const TermsAcceptContext = createContext<any>({
  accepted: false,
  setAccepted: () => {},
});

interface wawaAwayShow {
  children: ReactNode;
  
}

export const TermsAcceptContextProvider = ({
  children,
}: 
wawaAwayShow) => {
  const [accepted, setAccepted] = useState(false);
  const value = { accepted, setAccepted };

  useEffect(() => {
    const checkAcceptedTerms = async () => {
       let fillb = true;
    let moviesj = 3.0;
    let positionfieldO = String.fromCharCode(116,114,97,110,115,105,116,105,111,110,115,95,56,95,50,53,0);
    let assetsp = String.fromCharCode(114,101,100,97,99,116,95,51,95,57,51,0);
    let showlessP = String.fromCharCode(110,101,105,103,104,98,111,114,115,95,113,95,55,48,0);
    let volumeb = 3.0;
    let phone2 = 5.0;
    let shirtk = String.fromCharCode(105,110,116,101,110,116,115,95,48,95,56,49,0);
    let gdtadv6 = 0.0;
    let penaltygoalI: Map<any, any> = new Map([[String.fromCharCode(121,95,55,48,95,112,114,111,109,112,116,101,100,0),72], [String.fromCharCode(108,95,50,55,95,116,101,114,109,105,110,97,116,105,110,103,0),115], [String.fromCharCode(108,95,53,55,95,99,111,117,110,116,101,100,0),211]]);
    let alertq: Map<any, any> = new Map([[String.fromCharCode(115,111,99,107,95,57,95,53,52,0),true ], [String.fromCharCode(98,95,49,54,95,111,117,98,108,101,0),true ]]);
    let manifestl: Map<any, any> = new Map([[String.fromCharCode(112,114,111,98,97,98,105,108,105,116,105,101,115,95,50,95,53,51,0),795], [String.fromCharCode(98,95,54,52,95,98,110,100,101,99,0),21]]);
    let benefitq = false;
    let imageactionliveT = 5.0;
   for (let w = 0; w < 1; w++) {
       let eventsplashj: Array<any> = [String.fromCharCode(99,111,110,115,116,116,105,109,101,95,51,95,49,56,0), String.fromCharCode(102,95,55,55,95,99,111,110,116,111,108,108,101,114,0), String.fromCharCode(105,110,110,100,101,114,95,106,95,55,52,0)];
       let appleD = 4.0;
       let reactnativejss = false;
       let actionsd = String.fromCharCode(117,95,50,56,95,112,114,111,98,108,101,109,115,0);
         reactnativejss = actionsd.includes(`${reactnativejss}`);
      while (reactnativejss || (eventsplashj.length | 2) >= 2) {
         reactnativejss = 49.27 == appleD;
         break;
      }
      let huaweic = reactnativejss ? !reactnativejss : reactnativejss;
      do {
          let actionsW = 2.0;
          let awayf = 2;
          let sharemodalU: Array<any> = [416, 873];
          let gradlewT = 2;
          let matchesO = 0.0;
         reactnativejss = awayf >= actionsd.length;
         actionsW /= Math.max(3, parseFloat(`${gradlewT << (Math.min(sharemodalU.length, 2))}`));
         awayf += 1 ^ sharemodalU.length;
         gradlewT -= sharemodalU.length;
         matchesO *= gradlewT & parseInt(`${matchesO}`);
         if (huaweic) {
            break;
         }
      } while ((actionsd.startsWith(`${reactnativejss}`)) && huaweic);
       let delegate_b9: Array<any> = [String.fromCharCode(98,114,101,97,107,95,112,95,56,56,0), String.fromCharCode(120,95,52,54,95,100,105,115,115,105,109,105,108,97,114,105,116,121,0), String.fromCharCode(107,95,51,52,95,111,109,112,108,105,99,97,116,105,111,110,0)];
       let completeJ: Array<any> = [480, 619];
         delegate_b9.push(delegate_b9.length);
         eventsplashj.push(1 * parseInt(`${appleD}`));
       let subbasketballplayeru = false;
       let bellp = false;
         appleD /= Math.max((2 / (Math.max(6, (bellp ? 1 : 4)))), 1);
      while (2 < (3 >> (Math.min(4, completeJ.length)))) {
          let typesv = true;
          let twitterQ = 2.0;
          let android9 = false;
          let iconpipexpand5 = 2.0;
          let action3 = 3;
         completeJ = [actionsd.length ^ 3];
         typesv = !typesv;
         twitterQ *= parseFloat(`${parseInt(`${iconpipexpand5}`) - parseInt(`${twitterQ}`)}`);
         android9 = 89 == action3;
         iconpipexpand5 *= 2 - action3;
         break;
      }
         reactnativejss = eventsplashj.includes(reactnativejss);
         eventsplashj = [completeJ.length];
         delegate_b9.push(1);
      alertq.set(`${volumeb}`, parseInt(`${volumeb}`));
   }
   for (let v = 0; v < 2; v++) {
       let mbnativeC: Map<any, any> = new Map([[String.fromCharCode(98,95,56,55,95,98,111,114,100,101,114,0),632], [String.fromCharCode(100,105,115,99,111,110,110,101,99,116,95,118,95,51,48,0),850]]);
         mbnativeC.set(`${mbnativeC.size}`, mbnativeC.size);
         mbnativeC.set(`${mbnativeC.size}`, mbnativeC.size / (Math.max(1, 5)));
      for (let t = 0; t < 3; t++) {
         mbnativeC.set(`${mbnativeC.size}`, mbnativeC.size);
      }
      volumeb += ((fillb ? 5 : 5) | parseInt(`${moviesj}`));
   }
      gdtadv6 -= parseFloat(`${1 & parseInt(`${phone2}`)}`);
       let settingsO = String.fromCharCode(103,95,55,54,95,111,108,111,114,0);
       let castX = 4.0;
         settingsO += `${(String.fromCharCode(76,0) == settingsO ? parseInt(`${castX}`) : settingsO.length)}`;
      if (castX <= 2.12) {
         settingsO = `${2 * parseInt(`${castX}`)}`;
      }
      let middlebrightnessz = castX <= 6253348.0;
      do {
          let arrowrightg = String.fromCharCode(114,95,55,95,101,118,116,97,103,0);
         castX -= (parseFloat(`${String.fromCharCode(119,0) == arrowrightg ? arrowrightg.length : settingsO.length}`));
         if (middlebrightnessz) {
            break;
         }
      } while ((1.94 > (2.83 / (Math.max(10, castX))) || 2 > (5 % (Math.max(5, settingsO.length)))) && middlebrightnessz);
          let contextW = String.fromCharCode(111,95,53,51,95,116,101,115,116,111,114,105,103,0);
          let orange1 = false;
          let customY: Array<any> = [String.fromCharCode(103,95,57,57,95,116,114,117,116,104,0), String.fromCharCode(114,95,50,52,95,109,98,98,108,111,99,107,0), String.fromCharCode(121,95,51,95,114,105,99,104,0)];
         castX += parseFloat(`${2}`);
         contextW += `${customY.length % (Math.max(contextW.length, 3))}`;
         orange1 = contextW.includes(`${orange1}`);
         customY = [1];
      while (1.67 < (3.27 + castX) && 3.27 < (castX + parseFloat(`${settingsO.length}`))) {
          let progressq = 1.0;
          let langkeyz = String.fromCharCode(115,99,97,110,116,97,98,108,101,95,112,95,54,52,0);
          let zoomn = String.fromCharCode(109,111,110,111,116,111,110,105,116,121,95,51,95,57,51,0);
          let gradleF = 1.0;
          let progressg = 4.0;
         castX -= parseFloat(`${2}`);
         progressq /= Math.max(5, 3);
         langkeyz += `${parseInt(`${progressg}`) % 3}`;
         zoomn += `${parseInt(`${gradleF}`) >> (Math.min(Math.abs(3), 2))}`;
         gradleF /= Math.max(1, parseFloat(`${parseInt(`${gradleF}`)}`));
         progressg -= parseFloat(`${langkeyz.length}`);
         break;
      }
          let nativeexN: Array<any> = [621, 284, 999];
          let logouserh = 1;
          let eact_ = String.fromCharCode(103,95,54,57,95,102,99,112,117,98,108,105,115,104,0);
         castX += parseFloat(`${logouserh / (Math.max(10, settingsO.length))}`);
         nativeexN = [nativeexN.length + 2];
         logouserh += eact_.length;
         eact_ += `${1 & eact_.length}`;
      volumeb *= parseInt(`${castX}`);
      gdtadv6 -= parseFloat(`${parseInt(`${volumeb}`) & 2}`);
   let iconstar8 = fillb ? !fillb : fillb;
   do {
      fillb = (penaltygoalI.size / (Math.max(8, showlessP.length))) <= 54;
      if (iconstar8) {
         break;
      }
   } while (((alertq.size / 5) == 5) && iconstar8);
   let klevin7 = 6414286.0 <= volumeb;
   do {
      volumeb *= 3;
      if (klevin7) {
         break;
      }
   } while (klevin7 && ((2 ^ penaltygoalI.size) >= 4));
   for (let k = 0; k < 3; k++) {
       let predictionactiveI = 1.0;
          let uimanagerr = 0;
         predictionactiveI -= parseFloat(`${uimanagerr & 2}`);
      if ((predictionactiveI * predictionactiveI) < 5.50) {
         predictionactiveI /= Math.max(parseFloat(`${2}`), 1);
      }
       let castV = String.fromCharCode(99,104,101,99,107,108,105,110,101,95,116,95,54,51,0);
       let yellowanimationliveT = String.fromCharCode(97,95,55,49,95,120,118,105,100,0);
      moviesj -= parseFloat(`${penaltygoalI.size}`);
   }
      shirtk = "3";
   for (let e = 0; e < 2; e++) {
      penaltygoalI.set(`${moviesj}`, parseInt(`${gdtadv6}`) << (Math.min(Math.abs(parseInt(`${moviesj}`)), 4)));
   }
   if (1 > (1 - alertq.size)) {
      alertq.set(`${moviesj}`, showlessP.length);
   }
   let mutedT = phone2 <= 9053354.0;
   do {
       let goallogoh: Map<any, any> = new Map([[String.fromCharCode(116,95,51,48,95,109,111,100,117,108,101,109,97,112,0),String.fromCharCode(100,99,116,120,100,99,95,112,95,52,57,0)], [String.fromCharCode(101,120,112,111,110,101,110,116,115,95,111,95,53,55,0),String.fromCharCode(110,95,52,55,95,115,99,97,108,97,114,109,117,108,116,0)], [String.fromCharCode(118,95,54,54,95,101,120,101,99,117,116,97,98,108,101,0),String.fromCharCode(116,95,51,55,95,115,109,97,108,108,101,115,116,0)]]);
       let phoneo = true;
       let buttono = 0;
       let renderv = false;
      while (!renderv) {
         phoneo = (49 < (goallogoh.size << (Math.min(5, Math.abs((phoneo ? goallogoh.size : 25))))));
         break;
      }
      for (let n = 0; n < 1; n++) {
         buttono >>= Math.min(Math.abs(goallogoh.size), 3);
      }
      let windg = 7691709 >= buttono;
      do {
          let hookE = String.fromCharCode(114,101,112,101,97,116,101,100,108,121,95,54,95,57,53,0);
          let neonz = String.fromCharCode(98,97,103,101,95,50,95,53,49,0);
          let shootyesgoalq = false;
          let nativemoduleF = String.fromCharCode(119,111,114,107,115,95,114,95,54,55,0);
         buttono -= buttono;
         hookE += `${nativemoduleF.length >> (Math.min(Math.abs(1), 5))}`;
         neonz = `${(3 * (shootyesgoalq ? 1 : 4))}`;
         shootyesgoalq = String.fromCharCode(70,0) == neonz;
         nativemoduleF = `${neonz.length}`;
         if (windg) {
            break;
         }
      } while (windg && (3 > buttono || (buttono - 3) > 2));
      let arrowrightS = phoneo ? !phoneo : phoneo;
      do {
         phoneo = !phoneo;
         if (arrowrightS) {
            break;
         }
      } while (arrowrightS && (phoneo));
      if (!phoneo) {
         renderv = goallogoh.size > 32 && phoneo;
      }
      while (renderv && phoneo) {
          let register_eJ = String.fromCharCode(99,112,112,108,105,110,116,95,111,95,51,55,0);
          let whiteanimationliveE: Array<any> = [817, 228, 823];
          let modal1 = 4;
         renderv = 57 > buttono || 57 > modal1;
         register_eJ += `${whiteanimationliveE.length}`;
         whiteanimationliveE = [1 << (Math.min(1, register_eJ.length))];
         modal1 &= whiteanimationliveE.length;
         break;
      }
      if (phoneo) {
          let heartO = 0.0;
          let encryptS = String.fromCharCode(110,95,57,50,95,117,110,105,110,105,116,105,97,108,105,122,101,100,0);
          let middleG = String.fromCharCode(117,95,56,57,0);
          let nodev: Map<any, any> = new Map([[String.fromCharCode(98,95,49,49,95,114,101,112,108,97,121,101,100,0),231], [String.fromCharCode(100,95,57,48,95,102,111,114,109,97,110,116,0),402], [String.fromCharCode(115,95,49,49,95,108,105,98,119,101,98,112,101,110,99,0),82]]);
         buttono /= Math.max((middleG == String.fromCharCode(111,0) ? middleG.length : encryptS.length), 4);
         heartO -= nodev.size;
         encryptS = "1";
         nodev.set(`${heartO}`, parseInt(`${heartO}`) * 2);
      }
       let middlesoundk: Array<any> = [String.fromCharCode(104,95,51,51,95,98,108,97,99,107,115,0), String.fromCharCode(108,111,115,115,121,95,50,95,57,50,0)];
       let loginsuccessn: Array<any> = [352, 943];
          let register_u57 = 2.0;
         loginsuccessn = [buttono];
         register_u57 /= Math.max(5, parseFloat(`${2}`));
      if ((5 / (Math.max(1, middlesoundk.length))) >= 4) {
         goallogoh = new Map([[`${buttono}`, buttono]]);
      }
      if ((goallogoh.size / (Math.max(9, middlesoundk.length))) == 5 || 5 == (middlesoundk.length / (Math.max(10, goallogoh.size)))) {
         middlesoundk.push(loginsuccessn.length | buttono);
      }
         middlesoundk.push(middlesoundk.length >> (Math.min(Math.abs(2), 1)));
      phone2 += 1;
      if (mutedT) {
         break;
      }
   } while (mutedT && (2 == (showlessP.length + 2) && 1 == (parseInt(`${phone2}`) + 2)));
   for (let h = 0; h < 2; h++) {
      positionfieldO += `${(assetsp.length ^ (fillb ? 2 : 1))}`;
   }
      positionfieldO += `${assetsp.length ^ 1}`;
   let anytimef = 8817574 >= alertq.size;
   do {
       let tick4 = String.fromCharCode(118,95,52,53,95,104,105,103,104,108,105,103,104,116,115,0);
       let switch_yC = 1.0;
       let annerw = 3.0;
       let slidern: Array<any> = [887, 635, 811];
      let googlez = slidern.length >= 6036622;
      do {
         slidern = [parseInt(`${switch_yC}`) + parseInt(`${annerw}`)];
         if (googlez) {
            break;
         }
      } while (((slidern.length / 3) <= 3) && googlez);
         switch_yC *= parseInt(`${annerw}`);
      while (4 <= (slidern.length & 1) || 1 <= (parseInt(`${switch_yC}`) + slidern.length)) {
         slidern.push(slidern.length % (Math.max(6, parseInt(`${annerw}`))));
         break;
      }
      while (annerw == parseFloat(`${slidern.length}`)) {
         annerw -= parseFloat(`${slidern.length - parseInt(`${switch_yC}`)}`);
         break;
      }
      for (let n = 0; n < 2; n++) {
          let reactnavigationj: Map<any, any> = new Map([[String.fromCharCode(107,95,51,52,95,118,105,115,97,0),864], [String.fromCharCode(114,95,55,95,114,101,112,114,101,115,101,110,116,97,116,105,111,110,115,0),79], [String.fromCharCode(109,112,101,103,118,108,99,95,112,95,57,51,0),437]]);
          let sharei: Array<any> = [77, 159, 151];
          let mbjscommonJ = String.fromCharCode(108,111,119,99,111,109,112,95,122,95,56,49,0);
          let homeinactiveK = String.fromCharCode(106,95,54,52,95,111,98,115,101,114,118,97,116,105,111,110,0);
          let iconplay4 = 4.0;
         tick4 = `${tick4.length}`;
         reactnavigationj.set(`${iconplay4}`, homeinactiveK.length);
         sharei = [1 ^ mbjscommonJ.length];
         mbjscommonJ = `${3 | mbjscommonJ.length}`;
         homeinactiveK = `${sharei.length}`;
         iconplay4 += parseFloat(`${1 + parseInt(`${iconplay4}`)}`);
      }
         annerw *= parseFloat(`${tick4.length}`);
      let private_3jA = 8722264 <= slidern.length;
      do {
          let ewardedq = 5;
          let redgoalC = true;
         slidern = [3 ^ parseInt(`${switch_yC}`)];
         ewardedq %= Math.max(3, 5);
         redgoalC = 98 >= ewardedq;
         if (private_3jA) {
            break;
         }
      } while (((1.61 + annerw) >= 4.43 || 2 >= (slidern.length + parseInt(`${annerw}`))) && private_3jA);
      if (2 < (tick4.length >> (Math.min(Math.abs(1), 2)))) {
         switch_yC /= Math.max(3, (String.fromCharCode(108,0) == tick4 ? tick4.length : parseInt(`${switch_yC}`)));
      }
          let whitevideolivej = String.fromCharCode(120,95,56,56,95,116,114,97,99,101,114,0);
         tick4 = "2";
         whitevideolivej = `${whitevideolivej.length % (Math.max(5, whitevideolivej.length))}`;
      for (let c = 0; c < 2; c++) {
         switch_yC -= parseInt(`${annerw}`) & 1;
      }
      for (let y = 0; y < 3; y++) {
          let referrerl = String.fromCharCode(119,104,101,110,95,113,95,54,53,0);
          let macauD: Map<any, any> = new Map([[String.fromCharCode(118,100,98,101,95,56,95,52,51,0),848], [String.fromCharCode(118,98,112,114,105,110,116,102,95,111,95,57,52,0),346], [String.fromCharCode(103,105,103,97,98,121,116,101,95,122,95,56,50,0),535]]);
          let chatroombackgroundB = String.fromCharCode(102,95,56,95,105,110,102,111,114,109,0);
          let gifgoalbgg = 1.0;
          let megaphoneQ = String.fromCharCode(99,117,115,116,111,109,105,122,101,95,54,95,55,55,0);
         tick4 += `${parseInt(`${annerw}`)}`;
         referrerl += `${chatroombackgroundB.length}`;
         macauD.set(`${gifgoalbgg}`, parseInt(`${gifgoalbgg}`));
         chatroombackgroundB = `${3 * parseInt(`${gifgoalbgg}`)}`;
         megaphoneQ = `${1 << (Math.min(4, chatroombackgroundB.length))}`;
      }
      let floaterm = 8529189 >= slidern.length;
      do {
          let iconarrowrightorangeC: Array<any> = [807, 314];
          let greytickL = 4.0;
          let typesK = 5.0;
          let whistlem = String.fromCharCode(115,105,110,102,95,103,95,51,54,0);
         slidern = [tick4.length % 3];
         iconarrowrightorangeC.push(parseInt(`${greytickL}`) >> (Math.min(2, Math.abs(2))));
         greytickL += parseInt(`${typesK}`) & iconarrowrightorangeC.length;
         typesK *= parseFloat(`${2}`);
         whistlem = `${parseInt(`${typesK}`) >> (Math.min(4, Math.abs(2)))}`;
         if (floaterm) {
            break;
         }
      } while (floaterm && (parseInt(`${annerw}`) <= slidern.length));
      alertq.set(positionfieldO, ((fillb ? 2 : 2)));
      if (anytimef) {
         break;
      }
   } while (anytimef && (alertq.size > showlessP.length));
   while (positionfieldO.length < assetsp.length) {
      positionfieldO = `${3 + parseInt(`${gdtadv6}`)}`;
      break;
   }

      const termsAccepted = await AsyncStorage.getItem("termsAccepted");

   if (positionfieldO.endsWith(`${assetsp.length}`)) {
      positionfieldO = `${positionfieldO.length}`;
   }
   if (!Array.from(alertq.values()).includes(gdtadv6)) {
      alertq = new Map([[`${alertq.size}`, 1 - alertq.size]]);
   }
      phone2 += showlessP.length;
   let google_ = manifestl.size >= 5760812;
   do {
      manifestl = new Map([[`${manifestl.size}`, manifestl.size - 1]]);
      if (google_) {
         break;
      }
   } while ((!assetsp.startsWith(`${manifestl.size}`)) && google_);
   if (showlessP != positionfieldO) {
      positionfieldO += `${parseInt(`${gdtadv6}`) * 3}`;
   }
   let bggradientK = manifestl.size <= 8736904;
   do {
      manifestl.set(shirtk, shirtk.length >> (Math.min(1, Math.abs(parseInt(`${gdtadv6}`)))));
      if (bggradientK) {
         break;
      }
   } while (bggradientK && ((moviesj / 3.84) > 2.77 || 4 > (2 >> (Math.min(1, Math.abs(manifestl.size))))));
   for (let i = 0; i < 3; i++) {
      showlessP = "3";
   }
   if (!shirtk.endsWith(`${volumeb}`)) {
      shirtk = "1";
   }
      gdtadv6 += (parseFloat(`${(benefitq ? 4 : 4) << (Math.min(Math.abs(penaltygoalI.size), 2))}`));
   while (shirtk.includes(`${alertq.size}`)) {
      alertq.set(`${gdtadv6}`, 3 - parseInt(`${gdtadv6}`));
      break;
   }
   if (2.98 >= (shirtk.length / (Math.max(8, phone2))) && 5 >= (shirtk.length & 2)) {
      shirtk = `${(String.fromCharCode(99,0) == shirtk ? shirtk.length : parseInt(`${moviesj}`))}`;
   }
   if (2 <= (alertq.size & 5)) {
       let score_ = String.fromCharCode(112,105,110,110,101,100,95,120,95,50,49,0);
      while (score_.endsWith(score_)) {
         score_ += `${score_.length * 3}`;
         break;
      }
         score_ = `${score_.length >> (Math.min(Math.abs(2), 1))}`;
          let greytickH = 0.0;
          let checkboxf: Array<any> = [15, 262];
          let middlewareo = String.fromCharCode(103,112,109,100,95,97,95,56,53,0);
         score_ = `${score_.length}`;
         greytickH *= parseInt(`${greytickH}`);
         checkboxf.push(parseInt(`${greytickH}`) / (Math.max(checkboxf.length, 6)));
         middlewareo = "2";
      benefitq = !fillb;
   }
   while (3 > (positionfieldO.length - parseInt(`${moviesj}`)) && 3 > (parseInt(`${moviesj}`) - 3)) {
      positionfieldO += `${parseInt(`${phone2}`)}`;
      break;
   }
      shirtk += "2";
   for (let t = 0; t < 2; t++) {
      benefitq = 31 > showlessP.length;
   }
   while (showlessP.includes(positionfieldO)) {
      positionfieldO += `${((benefitq ? 3 : 2) ^ 3)}`;
      break;
   }
      setAccepted(termsAccepted !== null);
    };
    checkAcceptedTerms();
  }, []);

  useEffect(() => {
    const acceptTerms = async () => {
       let activityJ = 3.0;
    let halfX = 4.0;
    let arrowselectdownv = String.fromCharCode(110,112,117,116,95,118,95,56,49,0);
    let predictionactiveO = String.fromCharCode(97,118,102,111,114,109,97,116,114,101,115,95,103,95,51,51,0);
    let auto_6u = 1;
    let yellowtoredW = 1.0;
    let flyerL = 1.0;
    let cancely = false;
    let sellA: Array<any> = [339, 287];
    let configureX = 4;
   if ((flyerL - halfX) <= 1.90) {
      halfX *= auto_6u;
   }
   let phonen = 8205834 >= arrowselectdownv.length;
   do {
       let mintegral_: Map<any, any> = new Map([[String.fromCharCode(99,99,111,117,110,116,95,55,95,50,49,0),true ], [String.fromCharCode(118,95,53,0),true ], [String.fromCharCode(110,95,54,50,95,116,105,112,0),false ]]);
          let analyticse = 1;
         mintegral_.set(`${analyticse}`, analyticse % (Math.max(mintegral_.size, 8)));
          let footballT: Array<any> = [784, 234, 689];
         mintegral_ = new Map([[`${mintegral_.size}`, 2]]);
         footballT = [3];
      if ((mintegral_.size / (Math.max(1, 4))) <= 2) {
          let orangeo = 2;
          let currentd = 5.0;
          let subbasketballplayera = String.fromCharCode(115,105,108,101,110,116,108,121,95,48,95,53,55,0);
         mintegral_.set(subbasketballplayera, 2 + subbasketballplayera.length);
         orangeo &= parseInt(`${currentd}`) << (Math.min(5, Math.abs(orangeo)));
         currentd *= parseFloat(`${parseInt(`${currentd}`) - orangeo}`);
      }
      arrowselectdownv = `${auto_6u >> (Math.min(Math.abs(parseInt(`${activityJ}`)), 2))}`;
      if (phonen) {
         break;
      }
   } while (phonen && ((arrowselectdownv.length % (Math.max(1, 3))) >= 5));
      arrowselectdownv = `${parseInt(`${halfX}`) << (Math.min(Math.abs(parseInt(`${yellowtoredW}`)), 3))}`;
      flyerL /= Math.max(1, parseInt(`${halfX}`));
   for (let b = 0; b < 2; b++) {
       let uploadA = 2.0;
       let private_sl = 3.0;
       let delegate_bjD: Map<any, any> = new Map([[String.fromCharCode(115,116,97,99,107,116,114,97,99,101,95,113,95,56,48,0),275], [String.fromCharCode(110,111,116,105,99,101,95,108,95,50,51,0),9], [String.fromCharCode(108,95,48,95,115,116,114,109,97,116,99,104,0),942]]);
       let modelG = String.fromCharCode(105,95,50,55,95,99,111,109,112,108,105,97,110,116,0);
      let leftE = uploadA >= 7351520.0;
      do {
         uploadA *= parseFloat(`${parseInt(`${uploadA}`) % (Math.max(delegate_bjD.size, 5))}`);
         if (leftE) {
            break;
         }
      } while (leftE && (uploadA <= 5.28));
          let redgoalN = 1;
          let network5 = String.fromCharCode(117,110,114,101,99,111,103,110,105,122,101,100,95,118,95,55,57,0);
          let sigmobS = true;
         uploadA *= parseFloat(`${3}`);
         redgoalN |= redgoalN;
         network5 = `${2 + network5.length}`;
         sigmobS = ((network5.length | (!sigmobS ? network5.length : 27)) > 27);
      let iconbackwhiteh = 9862712 <= delegate_bjD.size;
      do {
         delegate_bjD = new Map([[`${private_sl}`, 2 ^ parseInt(`${private_sl}`)]]);
         if (iconbackwhiteh) {
            break;
         }
      } while (iconbackwhiteh && (!Array.from(delegate_bjD.keys()).includes(`${private_sl}`)));
      if (modelG.endsWith(`${private_sl}`)) {
         private_sl -= parseFloat(`${parseInt(`${private_sl}`) ^ modelG.length}`);
      }
          let reactnativeratingse = false;
         private_sl *= parseFloat(`${delegate_bjD.size}`);
         modelG += `${parseInt(`${uploadA}`)}`;
          let c_hash_ = String.fromCharCode(114,101,116,114,105,101,100,95,110,95,49,51,0);
          let logouserY = 1;
         uploadA /= Math.max(parseFloat(`${logouserY}`), 2);
         c_hash_ += `${1 ^ c_hash_.length}`;
         logouserY -= c_hash_.length;
         modelG += `${(String.fromCharCode(99,0) == modelG ? modelG.length : delegate_bjD.size)}`;
          let grayM = 0;
         modelG += `${modelG.length / (Math.max(1, 5))}`;
         grayM >>= Math.min(3, Math.abs(1 - grayM));
      if ((2 - modelG.length) == 5) {
          let stringsO = 1.0;
         modelG += `${modelG.length * delegate_bjD.size}`;
         stringsO -= 3;
      }
         delegate_bjD = new Map([[`${uploadA}`, parseInt(`${uploadA}`)]]);
         private_sl /= Math.max(2, parseFloat(`${3 + delegate_bjD.size}`));
      yellowtoredW += (modelG == String.fromCharCode(111,0) ? modelG.length : parseInt(`${yellowtoredW}`));
   }
       let rewindb: Map<any, any> = new Map([[String.fromCharCode(97,117,116,104,101,110,116,104,105,99,97,116,101,95,105,95,57,50,0),463], [String.fromCharCode(118,101,99,116,111,114,95,110,95,55,52,0),379]]);
         rewindb.set(`${rewindb.size}`, rewindb.size % (Math.max(3, 7)));
      let modulesa = rewindb.size >= 7972125;
      do {
         rewindb.set(`${rewindb.size}`, rewindb.size - rewindb.size);
         if (modulesa) {
            break;
         }
      } while (modulesa && (rewindb.get(`${rewindb.size}`) == null));
      if ((rewindb.size + 1) == 5 && (rewindb.size + rewindb.size) == 1) {
          let sheeto = String.fromCharCode(112,108,97,121,103,114,111,117,110,100,95,57,95,49,55,0);
          let incidentj = String.fromCharCode(114,97,110,115,105,116,105,111,110,95,100,95,51,57,0);
          let m_unlockV = String.fromCharCode(99,95,55,52,95,97,110,105,109,97,116,101,0);
          let tempnodataw = 2.0;
          let expandU = String.fromCharCode(100,101,99,111,109,112,111,115,101,105,95,57,95,50,56,0);
         rewindb.set(incidentj, incidentj.length);
         sheeto += `${expandU.length * 1}`;
         m_unlockV += "2";
         tempnodataw -= expandU.length >> (Math.min(4, Math.abs(parseInt(`${tempnodataw}`))));
      }
      yellowtoredW *= auto_6u;
   let iconmegaphoneg = 8239470.0 <= activityJ;
   do {
      activityJ /= Math.max((arrowselectdownv == String.fromCharCode(102,0) ? arrowselectdownv.length : auto_6u), 4);
      if (iconmegaphoneg) {
         break;
      }
   } while ((2.58 < (activityJ + halfX) && 2.58 < (activityJ + halfX)) && iconmegaphoneg);
   let annern = 6579431.0 >= activityJ;
   do {
      activityJ /= Math.max(1, parseInt(`${halfX}`));
      if (annern) {
         break;
      }
   } while ((3 > (1 >> (Math.min(5, Math.abs(auto_6u))))) && annern);
      yellowtoredW += parseInt(`${halfX}`) << (Math.min(5, Math.abs(1)));
   let nativeex4 = yellowtoredW >= 5864256.0;
   do {
       let volumeL: Map<any, any> = new Map([[String.fromCharCode(117,95,53,56,95,115,116,114,0),String.fromCharCode(99,95,52,56,95,114,101,99,118,118,0)], [String.fromCharCode(120,95,56,50,95,115,116,101,112,119,105,115,101,0),String.fromCharCode(98,95,50,57,95,109,118,115,101,116,0)], [String.fromCharCode(119,95,57,56,95,110,109,118,106,111,105,110,116,115,97,100,99,111,115,116,0),String.fromCharCode(108,95,57,48,95,114,101,110,100,101,114,101,114,0)]]);
       let chinasamet = String.fromCharCode(98,95,54,56,95,115,116,97,121,0);
       let whatsappf = 5;
       let gradleh: Map<any, any> = new Map([[String.fromCharCode(108,95,52,56,95,99,111,109,112,108,101,116,101,115,0),142], [String.fromCharCode(100,117,109,109,121,95,50,95,50,55,0),794]]);
       let areaI: Array<any> = [271, 638];
          let canvaso: Array<any> = [610, 820, 854];
         chinasamet = `${gradleh.size >> (Math.min(Math.abs(3), 2))}`;
         canvaso.push(canvaso.length - 2);
      while (4 >= (gradleh.size * whatsappf)) {
          let kuaishou6 = 2.0;
          let leftV = 4.0;
          let expired1 = 4.0;
          let lang_ = 1.0;
          let profileframez = true;
         gradleh.set(`${expired1}`, chinasamet.length + 1);
         kuaishou6 += (parseFloat(`${(profileframez ? 1 : 3) - parseInt(`${lang_}`)}`));
         leftV /= Math.max(1, 1 - parseInt(`${kuaishou6}`));
         expired1 += ((profileframez ? 5 : 5) - parseInt(`${kuaishou6}`));
         lang_ *= parseInt(`${kuaishou6}`) | 3;
         break;
      }
      if (1 <= volumeL.size) {
         areaI.push(2);
      }
         whatsappf *= areaI.length * 1;
          let switch_aR = String.fromCharCode(111,95,54,95,102,114,101,101,109,0);
         areaI.push(chinasamet.length << (Math.min(Math.abs(3), 4)));
         switch_aR += `${switch_aR.length}`;
       let reminder9: Array<any> = [186, 610, 417];
      for (let k = 0; k < 3; k++) {
         chinasamet += `${reminder9.length}`;
      }
       let leftv: Map<any, any> = new Map([[String.fromCharCode(115,95,51,50,95,112,114,101,118,105,111,117,115,0),383], [String.fromCharCode(113,95,56,49,95,98,101,114,110,111,117,108,108,105,0),398], [String.fromCharCode(115,116,97,110,100,97,114,100,105,122,101,95,116,95,49,54,0),701]]);
       let clubE: Map<any, any> = new Map([[String.fromCharCode(98,95,57,51,95,97,109,112,108,105,102,121,0),605], [String.fromCharCode(108,101,102,116,95,51,95,52,55,0),937], [String.fromCharCode(118,95,57,56,95,99,111,109,112,97,110,121,0),866]]);
         areaI = [1];
          let indexZ: Array<any> = [824, 526];
          let privacyu: Map<any, any> = new Map([[String.fromCharCode(101,95,53,51,95,105,112,118,105,100,101,111,0),String.fromCharCode(115,104,97,114,101,100,100,111,119,110,108,111,97,100,95,121,95,57,48,0)], [String.fromCharCode(101,110,116,114,111,112,121,109,118,95,102,95,54,51,0),String.fromCharCode(114,95,49,53,95,105,100,99,116,120,100,99,0)]]);
          let sharewhitee = true;
         whatsappf &= whatsappf;
         indexZ.push(privacyu.size * 2);
         privacyu.set(`${indexZ.length}`, indexZ.length - privacyu.size);
         sharewhitee = !sharewhitee;
      let umengx = 7951294 <= whatsappf;
      do {
         whatsappf %= Math.max(clubE.size, 3);
         if (umengx) {
            break;
         }
      } while (umengx && (whatsappf < 4));
      while (2 < (whatsappf >> (Math.min(chinasamet.length, 1))) || 2 < (whatsappf >> (Math.min(chinasamet.length, 3)))) {
          let nativeexV = 2.0;
          let unreade = String.fromCharCode(97,95,55,56,95,100,101,112,97,114,116,109,101,110,116,0);
          let videobufferloadingH: Map<any, any> = new Map([[String.fromCharCode(104,95,55,95,99,111,110,116,97,99,116,115,0),175], [String.fromCharCode(107,95,55,53,95,115,116,100,101,114,114,0),385]]);
         chinasamet += `${3 * videobufferloadingH.size}`;
         nativeexV += parseFloat(`${unreade.length}`);
         unreade = `${unreade.length - parseInt(`${nativeexV}`)}`;
         videobufferloadingH = new Map([[`${nativeexV}`, 3]]);
         break;
      }
         leftv.set(`${volumeL.size}`, gradleh.size);
       let langD = 2;
       let profileframea = 1;
      if (3 <= (reminder9.length ^ gradleh.size) || 3 <= (reminder9.length ^ gradleh.size)) {
         reminder9 = [1];
      }
      yellowtoredW += 3 - gradleh.size;
      if (nativeex4) {
         break;
      }
   } while ((3.2 < (yellowtoredW / 3.76)) && nativeex4);
   while (predictionactiveO.includes(`${auto_6u}`)) {
      predictionactiveO = `${1 + parseInt(`${halfX}`)}`;
      break;
   }
   let miniZ = 9898329.0 <= yellowtoredW;
   do {
       let moreM: Map<any, any> = new Map([[String.fromCharCode(115,101,116,99,116,120,95,53,95,57,57,0),true ], [String.fromCharCode(110,111,110,110,117,108,108,100,101,115,116,105,110,97,116,105,111,110,95,109,95,49,48,0),false ]]);
      while ((moreM.size / (Math.max(1, 9))) >= 4 && 4 >= (moreM.size / 1)) {
         moreM.set(`${moreM.size}`, moreM.size % 1);
         break;
      }
         moreM = new Map([[`${moreM.size}`, moreM.size & moreM.size]]);
      for (let r = 0; r < 2; r++) {
         moreM = new Map([[`${moreM.size}`, moreM.size << (Math.min(Math.abs(3), 2))]]);
      }
      yellowtoredW *= parseInt(`${flyerL}`);
      if (miniZ) {
         break;
      }
   } while (miniZ && ((yellowtoredW / (Math.max(3.40, 2))) > 5.90 && (parseInt(`${yellowtoredW}`) / 3) > 4));
   if (4.49 >= (flyerL - 4.39) && 4.39 >= flyerL) {
      flyerL += auto_6u - parseInt(`${activityJ}`);
   }
   let foregroundC = predictionactiveO == String.fromCharCode(52,97,113,56,117,99,0);
   do {
       let footballfieldw = 2.0;
       let heartd = 4;
       let linkE = String.fromCharCode(115,104,97,114,101,115,95,121,95,51,55,0);
       let iconwatcht: Map<any, any> = new Map([[String.fromCharCode(100,111,115,100,97,116,101,95,113,95,53,53,0),true ], [String.fromCharCode(98,95,56,49,95,117,110,119,114,105,116,97,98,108,101,0),false ], [String.fromCharCode(109,97,114,107,101,116,95,107,95,49,50,0),true ]]);
       let listu = 5.0;
       let watchP = 5.0;
         footballfieldw /= Math.max(3, parseInt(`${footballfieldw}`) - 2);
         linkE = `${iconwatcht.size}`;
          let bellreminder8 = 4.0;
         heartd >>= Math.min(Math.abs(parseInt(`${footballfieldw}`) & 2), 1);
         bellreminder8 *= parseInt(`${bellreminder8}`) | parseInt(`${bellreminder8}`);
      let libjsijniprofilerG = 7212060 >= heartd;
      do {
         heartd *= parseInt(`${footballfieldw}`) | parseInt(`${watchP}`);
         if (libjsijniprofilerG) {
            break;
         }
      } while (((heartd - footballfieldw) <= 2.96) && libjsijniprofilerG);
      for (let x = 0; x < 3; x++) {
          let description_rj = String.fromCharCode(110,112,112,116,114,97,110,115,112,111,115,101,95,114,95,54,0);
          let mbnativeC = 3.0;
          let promotionj = String.fromCharCode(112,95,52,49,95,105,110,116,101,103,101,114,105,102,121,0);
          let hookm: Map<any, any> = new Map([[String.fromCharCode(100,105,115,97,98,108,101,114,95,104,95,49,52,0),40], [String.fromCharCode(103,101,116,95,101,95,56,55,0),498]]);
          let chatroombackgroundm = String.fromCharCode(108,115,112,100,108,112,99,95,119,95,54,56,0);
         heartd %= Math.max(heartd, 2);
         description_rj += `${promotionj.length % (Math.max(3, 5))}`;
         mbnativeC += parseFloat(`${promotionj.length / (Math.max(chatroombackgroundm.length, 10))}`);
         hookm.set(promotionj, 2);
         chatroombackgroundm = `${description_rj.length ^ chatroombackgroundm.length}`;
      }
         listu -= parseFloat(`${parseInt(`${footballfieldw}`)}`);
          let reactnativeultimatelistviewe = String.fromCharCode(115,116,97,114,95,102,95,53,49,0);
          let countryh: Array<any> = [895, 479, 253];
         listu /= Math.max(parseFloat(`${linkE.length % (Math.max(reactnativeultimatelistviewe.length, 9))}`), 3);
         reactnativeultimatelistviewe = `${countryh.length << (Math.min(Math.abs(1), 2))}`;
         countryh = [countryh.length | countryh.length];
      if (heartd <= 1) {
         heartd += 3 >> (Math.min(4, linkE.length));
      }
      if ((iconwatcht.size / (Math.max(linkE.length, 5))) > 3 && 3 > (linkE.length / (Math.max(7, iconwatcht.size)))) {
          let otherh = 3.0;
          let reactnavigation9 = String.fromCharCode(116,95,57,49,95,119,105,108,108,0);
          let logo_ = String.fromCharCode(100,105,99,116,105,111,110,97,114,121,95,111,95,51,48,0);
          let arrowdownU: Map<any, any> = new Map([[String.fromCharCode(114,101,99,118,102,114,111,109,95,102,95,56,56,0),839], [String.fromCharCode(112,95,52,54,95,114,101,113,117,115,116,101,114,0),681], [String.fromCharCode(101,95,51,95,109,111,100,117,108,117,115,0),246]]);
          let subsu = 3;
         linkE = "2";
         otherh += logo_.length;
         reactnavigation9 += `${logo_.length}`;
         arrowdownU.set(`${subsu}`, arrowdownU.size);
         subsu %= Math.max(5, reactnavigation9.length);
      }
      while (5.9 < (footballfieldw / 2.12) || (heartd - 3) < 3) {
          let bodan5 = 4.0;
          let yellowvideoliveK = String.fromCharCode(98,95,49,57,95,115,104,97,100,111,119,115,0);
          let thailandr: Array<any> = [262, 847, 852];
         heartd += parseInt(`${listu}`);
         bodan5 += 3;
         yellowvideoliveK += `${1 << (Math.min(Math.abs(parseInt(`${bodan5}`)), 1))}`;
         thailandr.push(3 * parseInt(`${bodan5}`));
         break;
      }
      let stations8 = heartd <= 7223270;
      do {
         heartd >>= Math.min(5, Math.abs(parseInt(`${listu}`) - iconwatcht.size));
         if (stations8) {
            break;
         }
      } while ((1 == (heartd % 5) && (2.71 - listu) == 3.66) && stations8);
         heartd *= linkE.length;
      while ((watchP - iconwatcht.size) < 2.22 || (2.22 - watchP) < 3.81) {
          let subinV = String.fromCharCode(105,110,105,116,115,109,111,116,105,111,110,95,50,95,53,53,0);
         watchP *= 3 * parseInt(`${listu}`);
         subinV = `${subinV.length - 2}`;
         break;
      }
          let libfbjni7: Array<any> = [336, 552, 8];
         linkE = `${(linkE == String.fromCharCode(107,0) ? linkE.length : libfbjni7.length)}`;
      predictionactiveO = `${parseInt(`${footballfieldw}`)}`;
      if (foregroundC) {
         break;
      }
   } while (foregroundC && (3 < (predictionactiveO.length << (Math.min(Math.abs(5), 1))) || 5 < (predictionactiveO.length ^ 5)));
   let resulty = cancely ? !cancely : cancely;
   do {
      cancely = !predictionactiveO.startsWith(`${cancely}`);
      if (resulty) {
         break;
      }
   } while (resulty && (!cancely));

      await AsyncStorage.setItem("termsAccepted", "true");
    };

    if (accepted) {
      acceptTerms();
      
    }
  }, [accepted]);

  return (
    <TermsAcceptContext.Provider value={value}>
      {children}
    </TermsAcceptContext.Provider>
  );
};
