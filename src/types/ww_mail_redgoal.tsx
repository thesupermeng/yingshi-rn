import { wwSharemodal } from "./ww_dycreator_result"


export type wwSearchbar = {
    isJoin: boolean,
    joinDate: string | null,
    roomId: string,
    messages: wwSharemodal[],
}

export type wwNativeex = {
    userId: string | null,
    isJoin: boolean,
    joinDate: string | null,
    roomId: string,
    messages: wwSharemodal[],
}

export type wwLargebrightnessWhite = {
    isJoin: boolean,
    joinDate: string | null,
    roomId: string,
    messages: wwSharemodal[],
}

export type wwLibrrc = {
    userId: string | null,
    isJoin: boolean,
    joinDate: string | null,
    roomId: string,
    messages: wwSharemodal[],
}