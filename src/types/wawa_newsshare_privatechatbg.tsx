import { wawaAdult } from "./wawa_gradlew"


export type wawaForegroundInouttargetyellow = {
    isJoin: boolean,
    joinDate: string | null,
    roomId: string,
    messages: wawaAdult[],
}

export type wawaHeader = {
    userId: string | null,
    isJoin: boolean,
    joinDate: string | null,
    roomId: string,
    messages: wawaAdult[],
}

export type wawaBackwhite = {
    isJoin: boolean,
    joinDate: string | null,
    roomId: string,
    messages: wawaAdult[],
}

export type wawaContainerFloating = {
    userId: string | null,
    isJoin: boolean,
    joinDate: string | null,
    roomId: string,
    messages: wawaAdult[],
}