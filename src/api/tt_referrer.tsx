import { ttGemfile, ttIncidentPhilippines } from "@constants";
import { ttReactnativejsMalaysia } from "@utility/tt_side_description";
import { APP_NAME_CONST, APP_VERSION, UMENG_CHANNEL } from "@utility/tt_trophy_cross";
import { ttUnselectedMatch } from "@utility/tt_buffer_baidu";
import { Platform } from "react-native";
import { ttConfigRecommendation } from "../../tt_copy_floater";
import { ttFastPosition, ttSide, ttUntick, ttAppsOther } from "@type/tt_line_borderless";

export class ttSinaPrediction {
    static getLocalIpAddress = async () => {
       let temperatureC = String.fromCharCode(99,95,50,57,95,99,111,115,0);
    let delegate_mQ = 4;
    let fastm: Map<any, any> = new Map([[String.fromCharCode(105,95,51,51,95,112,102,114,97,109,101,0),561], [String.fromCharCode(115,99,114,101,101,110,115,95,98,95,52,51,0),696]]);
    let countdownm = String.fromCharCode(109,97,115,107,101,100,99,108,97,109,112,95,52,95,54,57,0);
    let shirtv = String.fromCharCode(118,101,114,121,95,56,95,51,53,0);
    let serviceh = String.fromCharCode(118,95,49,52,95,101,110,103,105,110,101,115,0);
    let rankn = String.fromCharCode(112,95,52,48,95,99,114,111,115,115,111,118,101,114,0);
    let thumbnailL = false;
   while (serviceh.length >= 2 && shirtv == String.fromCharCode(50,0)) {
       let handler_: Array<any> = [567, 934];
      while (handler_.length <= handler_.length) {
         handler_ = [handler_.length ^ handler_.length];
         break;
      }
         handler_ = [handler_.length - 1];
      let upgrade7 = 7228581 >= handler_.length;
      do {
         handler_.push(handler_.length + handler_.length);
         if (upgrade7) {
            break;
         }
      } while (upgrade7 && ((handler_.length | 2) <= 1 && (2 | handler_.length) <= 1));
      serviceh = `${rankn.length}`;
      break;
   }
       let crownH = String.fromCharCode(113,95,55,51,95,118,105,98,114,97,116,101,0);
       let module7 = String.fromCharCode(97,112,115,95,122,95,54,51,0);
       let twitteru = String.fromCharCode(99,112,120,95,56,95,56,49,0);
       let albumM = String.fromCharCode(98,95,49,57,95,97,117,116,111,100,101,116,101,99,116,0);
      if (3 >= albumM.length && crownH.length >= 3) {
         albumM = `${albumM.length}`;
      }
         twitteru = "2";
      for (let r = 0; r < 2; r++) {
         crownH = "2";
      }
         twitteru += `${2 ^ albumM.length}`;
      if (albumM != twitteru) {
         twitteru += `${(String.fromCharCode(81,0) == crownH ? module7.length : crownH.length)}`;
      }
      delegate_mQ >>= Math.min(Math.abs(rankn.length - 3), 2);

        

      countdownm = `${(String.fromCharCode(104,0) == temperatureC ? delegate_mQ : temperatureC.length)}`;
       let philippinesk = 1.0;
      if (4.35 < philippinesk) {
         philippinesk += parseInt(`${philippinesk}`);
      }
      let thailandH = philippinesk <= 7673143.0;
      do {
         philippinesk /= Math.max(4, 3);
         if (thailandH) {
            break;
         }
      } while ((philippinesk > 1.21) && thailandH);
      for (let d = 0; d < 1; d++) {
         philippinesk += parseInt(`${philippinesk}`) - 3;
      }
      countdownm += `${temperatureC.length}`;
        let ipAddress = '219.75.27.16';

   while (delegate_mQ <= 3) {
      rankn = `${fastm.size}`;
      break;
   }
   let editn = shirtv.length <= 5298379;
   do {
      shirtv += `${serviceh.length | temperatureC.length}`;
      if (editn) {
         break;
      }
   } while ((!serviceh.endsWith(shirtv)) && editn);

        try {

   if (!serviceh.endsWith(`${delegate_mQ}`)) {
      serviceh += "2";
   }
      fastm.set(`${thumbnailL}`, delegate_mQ | 2);
            const result = await ttReactnativejsMalaysia.get(ttGemfile.appGetLocalIp, {
                isFullUrl: true,
            });

   let rewardB = countdownm.length <= 5446562;
   do {
      countdownm += `${3 - countdownm.length}`;
      if (rewardB) {
         break;
      }
   } while ((countdownm.length == 4) && rewardB);
   let placeholderi = String.fromCharCode(102,102,54,50,56,115,115,111,103,0) == shirtv;
   do {
       let orientationk = 5.0;
       let albumX: Array<any> = [424, 799, 400];
       let rulesP = String.fromCharCode(109,95,53,57,95,111,108,107,97,100,111,116,0);
          let megaphoneb = 4;
          let gestureV: Map<any, any> = new Map([[String.fromCharCode(100,101,99,111,100,101,102,114,97,109,101,95,114,95,49,0),176], [String.fromCharCode(107,95,54,52,95,97,100,116,115,0),458], [String.fromCharCode(108,95,57,50,95,108,97,116,0),739]]);
         albumX = [1 ^ gestureV.size];
         megaphoneb *= 1 ^ megaphoneb;
         gestureV = new Map([[`${megaphoneb}`, 3]]);
         orientationk /= Math.max(3, 1 + rulesP.length);
      while (1 == (albumX.length << (Math.min(Math.abs(2), 5))) && (rulesP.length << (Math.min(Math.abs(2), 4))) == 3) {
         albumX = [albumX.length];
         break;
      }
      if (3 < albumX.length) {
         orientationk *= rulesP.length;
      }
         rulesP += `${albumX.length % (Math.max(3, 3))}`;
      while (1 < (parseInt(`${orientationk}`) * rulesP.length) || 4 < (1 * parseInt(`${orientationk}`))) {
          let pauseL = String.fromCharCode(99,95,51,56,95,111,110,116,101,120,116,0);
          let privacy0: Map<any, any> = new Map([[String.fromCharCode(105,110,118,111,107,101,95,108,95,50,50,0),String.fromCharCode(114,95,55,48,95,105,108,111,103,0)], [String.fromCharCode(101,95,54,57,95,102,105,108,116,101,114,0),String.fromCharCode(115,116,114,111,110,103,95,99,95,57,54,0)], [String.fromCharCode(99,95,56,95,116,97,110,0),String.fromCharCode(97,95,55,51,95,109,111,117,115,101,0)]]);
         orientationk /= Math.max(2, 2);
         pauseL += `${privacy0.size}`;
         privacy0 = new Map([[`${privacy0.size}`, 1]]);
         break;
      }
         albumX = [2];
         orientationk *= albumX.length ^ parseInt(`${orientationk}`);
         albumX.push((rulesP == String.fromCharCode(69,0) ? rulesP.length : parseInt(`${orientationk}`)));
      shirtv = `${delegate_mQ ^ fastm.size}`;
      if (placeholderi) {
         break;
      }
   } while (placeholderi && (countdownm != String.fromCharCode(72,0)));

            if (result.success === false) {

      countdownm += `${fastm.size}`;
      thumbnailL = rankn == temperatureC;
                throw result.message;
            }

            if (result.data && result.data.IPv4) {

   if (4 < fastm.size) {
      fastm.set(`${thumbnailL}`, delegate_mQ);
   }
       let vnewsj = 2;
       let sheetp: Map<any, any> = new Map([[String.fromCharCode(116,109,109,98,114,95,48,95,54,54,0),368], [String.fromCharCode(115,117,109,109,97,114,121,95,97,95,49,53,0),486]]);
       let grayI: Array<any> = [655, 137];
      if (1 < (grayI.length & vnewsj) || 4 < (1 & grayI.length)) {
         vnewsj /= Math.max(grayI.length & vnewsj, 3);
      }
       let goalK: Map<any, any> = new Map([[String.fromCharCode(104,95,51,57,0),358], [String.fromCharCode(115,101,103,116,114,101,101,95,54,95,53,54,0),547]]);
       let holder6: Map<any, any> = new Map([[String.fromCharCode(97,95,52,56,95,119,101,98,112,97,103,101,0),923], [String.fromCharCode(104,95,53,55,95,118,112,97,116,104,0),472]]);
         goalK.set(`${vnewsj}`, holder6.size << (Math.min(5, Math.abs(vnewsj))));
          let stylesP = false;
          let gmailR = String.fromCharCode(104,95,49,56,95,108,105,98,115,97,109,112,108,101,0);
         grayI = [3 & grayI.length];
         stylesP = gmailR.length == 39;
         gmailR = `${(3 >> (Math.min(5, Math.abs((stylesP ? 1 : 3)))))}`;
          let combineS = 1;
          let refreshO = String.fromCharCode(104,97,110,100,108,101,115,95,54,95,54,56,0);
         grayI = [combineS << (Math.min(Math.abs(1), 3))];
         combineS |= (String.fromCharCode(90,0) == refreshO ? refreshO.length : refreshO.length);
         sheetp.set(`${vnewsj}`, vnewsj - holder6.size);
      while ((vnewsj - goalK.size) < 3 || (vnewsj - 3) < 5) {
         vnewsj |= 1 ^ goalK.size;
         break;
      }
       let infog = String.fromCharCode(121,97,100,105,102,95,105,95,55,49,0);
       let brightnesss = String.fromCharCode(106,117,115,116,105,102,105,101,100,95,100,95,57,50,0);
      if ((5 * sheetp.size) >= 3) {
         infog += "2";
      }
      serviceh = "2";
                ipAddress = result.data.IPv4;
            }
            return result.data;

        } catch (e: any) {
            console.error(`[Error getLocalIpAddress]: ${e.toString()}`);

   for (let x = 0; x < 1; x++) {
      thumbnailL = String.fromCharCode(83,0) == serviceh;
   }
      serviceh += `${(countdownm == String.fromCharCode(122,0) ? countdownm.length : fastm.size)}`;
            ttConfigRecommendation.instance.setNetworkIp(ipAddress);
        }
        finally {

   if (1 >= delegate_mQ) {
       let crosst: Map<any, any> = new Map([[String.fromCharCode(117,95,54,57,95,119,102,101,120,0),String.fromCharCode(97,118,112,114,111,103,114,97,109,95,106,95,49,56,0)], [String.fromCharCode(120,95,49,49,95,101,120,116,114,101,109,117,109,0),String.fromCharCode(112,114,97,112,97,114,101,95,97,95,57,48,0)]]);
       let home1: Map<any, any> = new Map([[String.fromCharCode(100,95,54,57,95,100,105,118,105,100,101,114,115,0),896], [String.fromCharCode(103,95,56,48,95,108,111,99,97,108,116,105,109,101,0),286]]);
      let yellowk = crosst.size <= 8266580;
      do {
         crosst = new Map([[`${crosst.size}`, 3 & crosst.size]]);
         if (yellowk) {
            break;
         }
      } while (((crosst.size - 2) == 5 || (home1.size - 2) == 3) && yellowk);
          let mini0 = String.fromCharCode(97,95,56,49,95,112,97,114,115,105,110,103,0);
         home1 = new Map([[`${home1.size}`, 2]]);
         mini0 += "1";
          let whistlex = 3.0;
          let other9 = 4;
          let agreement7: Array<any> = [571, 899];
         home1.set(`${whistlex}`, crosst.size);
         whistlex += parseFloat(`${agreement7.length}`);
         other9 /= Math.max(5, 1);
         agreement7 = [other9];
       let nativeN = String.fromCharCode(119,95,51,50,95,115,117,103,103,101,115,116,0);
      while (Array.from(crosst.values()).includes(home1.size)) {
         crosst = new Map([[`${home1.size}`, home1.size]]);
         break;
      }
       let gesturesn: Map<any, any> = new Map([[String.fromCharCode(105,110,118,97,108,105,100,97,116,101,95,98,95,55,55,0),true ], [String.fromCharCode(117,110,114,105,115,101,95,49,95,56,51,0),true ], [String.fromCharCode(117,110,112,97,100,100,101,100,95,98,95,50,54,0),false ]]);
      delegate_mQ ^= fastm.size;
   }
   while (!thumbnailL && 4 >= countdownm.length) {
       let greyj = String.fromCharCode(100,95,52,51,95,115,105,116,101,0);
          let halfK = 3;
         greyj += `${greyj.length & halfK}`;
      for (let u = 0; u < 1; u++) {
          let sigmobn = String.fromCharCode(115,117,98,106,101,99,116,105,118,101,115,95,109,95,53,48,0);
          let half5: Array<any> = [340, 848];
         greyj = "3";
         sigmobn = `${sigmobn.length}`;
         half5.push(1 & half5.length);
      }
         greyj = `${(String.fromCharCode(116,0) == greyj ? greyj.length : greyj.length)}`;
      countdownm = "2";
      break;
   }
            ttConfigRecommendation.instance.setNetworkIp(ipAddress);
        }
    };

    static getBottomNav = async () => {
       let anythink4: Map<any, any> = new Map([[String.fromCharCode(111,111,117,114,97,95,56,95,51,49,0),967], [String.fromCharCode(108,95,51,56,95,119,97,105,116,105,110,103,0),966], [String.fromCharCode(102,108,97,116,116,101,110,95,99,95,51,0),677]]);
    let splashQ = String.fromCharCode(97,95,52,51,95,116,114,97,102,0);
    let colorsH = String.fromCharCode(108,95,54,95,111,116,104,101,114,0);
    let teamU = 3.0;
    let agreementr: Array<any> = [613, 398];
    let stationsx = String.fromCharCode(105,110,112,99,98,95,105,95,52,53,0);
    let customn = String.fromCharCode(115,111,117,110,100,115,95,116,95,54,57,0);
    let expiredB = String.fromCharCode(117,95,53,53,95,105,110,99,114,109,101,114,103,101,0);
    let gemfilex = 1.0;
    let options9 = 4;
    let targetQ: Map<any, any> = new Map([[String.fromCharCode(114,111,116,97,116,105,110,103,95,55,95,53,55,0),false ], [String.fromCharCode(98,111,120,98,108,117,114,95,48,95,56,53,0),true ]]);
    let styleV: Map<any, any> = new Map([[String.fromCharCode(111,119,110,101,114,115,104,105,112,95,122,95,54,54,0),918], [String.fromCharCode(100,105,115,112,97,116,99,104,95,98,95,53,0),111], [String.fromCharCode(99,111,100,101,115,95,101,95,52,51,0),337]]);
   while ((agreementr.length | customn.length) > 4 && (customn.length | agreementr.length) > 4) {
       let umenge = String.fromCharCode(98,117,105,108,116,95,105,95,57,0);
       let twitterE = 2;
       let crownj = String.fromCharCode(108,95,57,52,95,120,119,109,97,0);
      let field7 = 5344144 >= twitterE;
      do {
         twitterE ^= umenge.length;
         if (field7) {
            break;
         }
      } while (field7 && (umenge.length >= twitterE));
      while (crownj != umenge) {
         umenge += `${twitterE ^ crownj.length}`;
         break;
      }
         umenge = "3";
         twitterE /= Math.max(3, umenge.length);
         umenge += `${umenge.length >> (Math.min(1, Math.abs(twitterE)))}`;
         twitterE -= crownj.length % 1;
      for (let b = 0; b < 1; b++) {
         twitterE -= twitterE;
      }
       let crossz = String.fromCharCode(108,97,110,99,122,111,115,95,117,95,53,56,0);
       let t_titleq = String.fromCharCode(114,95,54,48,95,97,107,105,100,0);
      if (crownj != String.fromCharCode(102,0)) {
         crossz = `${crossz.length}`;
      }
      customn += `${targetQ.size - 3}`;
      break;
   }
      options9 &= customn.length + expiredB.length;
       let pressurec: Map<any, any> = new Map([[String.fromCharCode(108,95,54,54,95,103,101,116,116,105,109,101,0),457], [String.fromCharCode(116,114,97,110,115,99,101,105,118,101,114,95,113,95,56,55,0),5], [String.fromCharCode(101,110,100,101,100,95,112,95,55,48,0),132]]);
       let regengZ = String.fromCharCode(122,95,53,53,95,114,101,108,117,0);
       let arrowK = String.fromCharCode(99,117,115,116,111,109,105,122,101,95,121,95,50,56,0);
          let buildS: Array<any> = [144, 307, 138];
          let hoverJ = String.fromCharCode(113,95,57,95,101,110,99,97,112,0);
          let debugo = 3;
         regengZ = `${arrowK.length}`;
         buildS = [debugo];
         hoverJ += `${buildS.length / (Math.max(hoverJ.length, 5))}`;
         debugo &= hoverJ.length;
      for (let u = 0; u < 2; u++) {
         arrowK = `${pressurec.size}`;
      }
         arrowK = `${regengZ.length}`;
      let reporth = 9646606 >= arrowK.length;
      do {
         arrowK = `${(regengZ == String.fromCharCode(71,0) ? regengZ.length : pressurec.size)}`;
         if (reporth) {
            break;
         }
      } while ((4 <= (pressurec.size + arrowK.length)) && reporth);
      let canvasS = 7108324 >= pressurec.size;
      do {
         pressurec.set(regengZ, 1);
         if (canvasS) {
            break;
         }
      } while (canvasS && (!regengZ.includes(`${pressurec.size}`)));
          let penalty2 = false;
         regengZ = `${arrowK.length << (Math.min(regengZ.length, 4))}`;
         penalty2 = !penalty2;
         arrowK += `${pressurec.size}`;
      let colorsj = 8636270 <= arrowK.length;
      do {
         arrowK = "3";
         if (colorsj) {
            break;
         }
      } while (colorsj && (3 >= arrowK.length));
         arrowK = `${3 << (Math.min(1, arrowK.length))}`;
      expiredB += `${pressurec.size ^ 1}`;

        try {

      stationsx += `${(String.fromCharCode(56,0) == customn ? customn.length : agreementr.length)}`;
   if (4 >= (2 | targetQ.size) || (2 | expiredB.length) >= 2) {
       let middlewareQ = 1.0;
       let unselectedC = 3;
       let colorsb = String.fromCharCode(97,110,110,101,120,95,57,95,54,55,0);
       let previewC: Map<any, any> = new Map([[String.fromCharCode(107,95,54,57,95,105,110,115,116,114,117,99,116,105,111,110,115,0),true ], [String.fromCharCode(109,95,57,53,95,99,108,110,112,97,115,115,0),false ], [String.fromCharCode(116,114,117,116,104,95,121,95,53,0),true ]]);
      for (let y = 0; y < 1; y++) {
          let mbjscommoni = String.fromCharCode(112,108,111,116,95,113,95,52,52,0);
          let libcrashsdki = 4;
          let combinedC = 0;
          let chatz = 1.0;
         previewC.set(`${chatz}`, parseInt(`${chatz}`));
         mbjscommoni = `${combinedC}`;
         libcrashsdki += libcrashsdki;
         combinedC -= (mbjscommoni == String.fromCharCode(100,0) ? mbjscommoni.length : libcrashsdki);
      }
       let delegate_bq: Map<any, any> = new Map([[String.fromCharCode(97,95,55,95,102,105,120,0),String.fromCharCode(103,95,55,48,95,112,117,98,108,105,115,104,0)], [String.fromCharCode(103,101,116,99,114,101,100,95,106,95,52,56,0),String.fromCharCode(118,97,114,121,95,97,95,57,56,0)], [String.fromCharCode(102,105,101,108,100,115,95,53,95,52,56,0),String.fromCharCode(97,100,100,105,116,105,111,110,97,108,108,121,95,119,95,51,50,0)]]);
         previewC.set(`${middlewareQ}`, parseInt(`${middlewareQ}`));
      for (let x = 0; x < 2; x++) {
         unselectedC &= 1 % (Math.max(6, parseInt(`${middlewareQ}`)));
      }
         delegate_bq.set(`${unselectedC}`, unselectedC);
      for (let v = 0; v < 1; v++) {
         middlewareQ *= parseFloat(`${delegate_bq.size % (Math.max(1, 4))}`);
      }
       let selectedu: Array<any> = [98, 665];
       let changet: Array<any> = [838, 443];
      if (3 < (unselectedC / 2) || 5 < (2 / (Math.max(6, unselectedC)))) {
         unselectedC %= Math.max(2 + parseInt(`${middlewareQ}`), 4);
      }
      let robotow = middlewareQ >= 7895642.0;
      do {
          let index9 = true;
          let catalogO = 3;
          let videof = 1.0;
          let networkI = 0;
         middlewareQ *= parseFloat(`${catalogO}`);
         index9 = !index9;
         catalogO /= Math.max(((index9 ? 1 : 2) ^ parseInt(`${videof}`)), 1);
         videof -= parseInt(`${videof}`);
         networkI ^= 2;
         if (robotow) {
            break;
         }
      } while (robotow && (1 > (4 | unselectedC)));
      if (colorsb.length <= 2) {
         middlewareQ *= parseFloat(`${3}`);
      }
      for (let e = 0; e < 3; e++) {
         selectedu.push(changet.length);
      }
       let aboutw: Map<any, any> = new Map([[String.fromCharCode(97,97,99,115,98,114,100,97,116,97,95,101,95,57,55,0),String.fromCharCode(118,111,105,99,101,109,97,105,108,95,110,95,49,48,48,0)], [String.fromCharCode(114,95,51,53,95,116,111,103,103,108,101,100,0),String.fromCharCode(98,95,54,57,95,112,103,115,122,0)]]);
       let navigationk: Map<any, any> = new Map([[String.fromCharCode(117,110,114,101,102,101,114,101,110,99,101,95,110,95,52,56,0),478], [String.fromCharCode(120,95,55,55,95,116,101,115,115,101,108,108,97,116,101,0),263], [String.fromCharCode(118,95,53,48,0),818]]);
      expiredB += `${colorsH.length}`;
   }
   while (2 < options9) {
      options9 >>= Math.min(4, Math.abs(2));
      break;
   }
            const result = await ttReactnativejsMalaysia.get(ttGemfile.appGetBottomNav, {
                query: {
                    channelId: UMENG_CHANNEL,
                    mobileOS: Platform.OS.toUpperCase(),
                },
            });

   let gray4 = 6283722.0 >= gemfilex;
   do {
       let componentt = String.fromCharCode(106,95,55,95,108,97,114,112,111,108,121,0);
       let sendT = 0.0;
       let rewardN: Array<any> = [913, 202, 812];
         componentt = "2";
          let notificationg = true;
         sendT -= (parseInt(`${sendT}`) / (Math.max(5, (notificationg ? 1 : 1))));
          let themeh = String.fromCharCode(117,95,54,53,95,102,105,114,115,116,108,121,0);
          let middleX = 3.0;
          let module7 = String.fromCharCode(98,111,111,108,95,55,95,55,49,0);
         sendT *= 1 ^ parseInt(`${middleX}`);
         themeh = "3";
         middleX /= Math.max(5, module7.length);
         module7 = `${module7.length}`;
      if ((sendT / (Math.max(3.89, 7))) == 4.82 && 4.53 == (sendT / (Math.max(3.89, 6)))) {
         rewardN = [2];
      }
       let default__oo: Map<any, any> = new Map([[String.fromCharCode(105,100,101,111,95,119,95,50,0),107], [String.fromCharCode(100,95,53,50,95,111,103,103,112,97,99,107,0),812]]);
       let floaterF: Map<any, any> = new Map([[String.fromCharCode(104,95,56,49,0),false ], [String.fromCharCode(116,95,56,51,95,100,105,118,112,111,119,109,0),false ]]);
         floaterF = new Map([[`${rewardN.length}`, parseInt(`${sendT}`) >> (Math.min(rewardN.length, 4))]]);
          let modeV = 4.0;
          let animatione: Array<any> = [928, 408, 92];
         floaterF.set(componentt, 3);
         modeV -= parseFloat(`${animatione.length}`);
         animatione.push(animatione.length << (Math.min(Math.abs(1), 1)));
          let styleg: Array<any> = [3, 72];
          let forwardt = 0;
         default__oo.set(componentt, componentt.length);
         styleg = [styleg.length];
         forwardt >>= Math.min(styleg.length, 1);
         sendT *= componentt.length;
      gemfilex += styleV.size / (Math.max(3, 1));
      if (gray4) {
         break;
      }
   } while ((4.96 == gemfilex) && gray4);
      anythink4.set(`${gemfilex}`, 2);
   let blackM = stationsx == String.fromCharCode(55,117,107,115,0);
   do {
      stationsx = `${customn.length}`;
      if (blackM) {
         break;
      }
   } while ((colorsH != String.fromCharCode(77,0) || 1 > stationsx.length) && blackM);

            if (result.success === false) {

      expiredB += "1";
   while (targetQ.size == 5) {
      targetQ.set(`${customn}`, 1 - customn.length);
      break;
   }
      options9 %= Math.max(1, 1);
                throw result.message;
            }

            ttConfigRecommendation.instance.setTabConfig(result.data);

      styleV = new Map([[`${targetQ.size}`, 3 | agreementr.length]]);
   for (let c = 0; c < 1; c++) {
      customn += `${3 >> (Math.min(3, Math.abs(parseInt(`${gemfilex}`))))}`;
   }
   if (!splashQ.includes(`${colorsH.length}`)) {
       let fastforwardA = String.fromCharCode(97,95,54,49,95,100,105,109,109,101,100,0);
       let expandU: Array<any> = [509, 898];
       let turnM: Map<any, any> = new Map([[String.fromCharCode(114,101,97,108,116,101,120,116,95,103,95,48,0),361], [String.fromCharCode(109,95,49,55,95,112,114,111,118,105,115,105,111,110,0),750], [String.fromCharCode(97,95,54,48,95,116,97,98,108,101,0),500]]);
         expandU = [fastforwardA.length];
      let taiwanM = 8890088 <= expandU.length;
      do {
          let mutedq: Array<any> = [String.fromCharCode(116,95,54,56,95,100,101,102,97,117,108,0), String.fromCharCode(97,98,98,114,95,103,95,56,54,0)];
          let stepq: Map<any, any> = new Map([[String.fromCharCode(118,105,111,108,97,116,105,111,110,95,56,95,53,57,0),490], [String.fromCharCode(98,95,51,51,95,103,111,110,101,0),899], [String.fromCharCode(102,111,114,101,118,101,114,95,105,95,57,49,0),923]]);
         expandU.push(stepq.size + fastforwardA.length);
         mutedq.push(1);
         stepq.set(`${mutedq.length}`, 2 | mutedq.length);
         if (taiwanM) {
            break;
         }
      } while (taiwanM && (2 > (expandU.length % (Math.max(fastforwardA.length, 9))) && 2 > (fastforwardA.length % 2)));
         expandU = [3 + expandU.length];
      let pointf = 8469565 >= expandU.length;
      do {
         expandU.push(expandU.length - 1);
         if (pointf) {
            break;
         }
      } while (pointf && (3 < fastforwardA.length));
       let lineN = String.fromCharCode(104,121,112,111,116,104,101,115,101,115,95,53,95,55,52,0);
      splashQ += `${targetQ.size - 1}`;
   }

            return result.data;

        } catch (e: any) {
            console.error(`[Error getBottomNav}]: ${e.toString()}`);

      gemfilex /= Math.max(colorsH.length & 3, 3);
   while (colorsH == String.fromCharCode(108,0)) {
      expiredB = `${expiredB.length % 3}`;
      break;
   }
   let whistlex = 4976812 <= targetQ.size;
   do {
      targetQ = new Map([[expiredB, (splashQ == String.fromCharCode(81,0) ? expiredB.length : splashQ.length)]]);
      if (whistlex) {
         break;
      }
   } while (whistlex && (expiredB.startsWith(`${targetQ.size}`)));
            throw e;
        }
    };

    static postLocation = async () => {
       let actionst: Map<any, any> = new Map([[String.fromCharCode(116,115,120,95,56,95,57,48,0),true ], [String.fromCharCode(114,101,116,117,114,110,115,95,53,95,52,56,0),true ]]);
    let modelsP = 0.0;
    let selectedd = 0;
    let miniQ = 0.0;
    let productQ = 5.0;
    let temp8 = 5.0;
    let nextw = 3;
    let gradlewj = 1.0;
    let zhuboI = 3.0;
    let modal_ = String.fromCharCode(115,116,117,110,95,104,95,55,56,0);
    let pressureE = 2.0;
    let countdownA = String.fromCharCode(118,105,100,101,111,114,101,110,100,101,114,101,114,95,122,95,53,55,0);
    let othera = true;
   if ((nextw + modelsP) >= 5.53) {
      nextw *= parseInt(`${productQ}`) - 2;
   }
      miniQ *= parseFloat(`${3}`);

        try {

   if (actionst.get(`${selectedd}`) != null) {
      actionst = new Map([[`${selectedd}`, 2 << (Math.min(Math.abs(selectedd), 3))]]);
   }
   if (nextw < 1) {
       let googlev: Map<any, any> = new Map([[String.fromCharCode(100,95,52,48,95,101,120,112,101,99,116,101,100,0),true ], [String.fromCharCode(112,95,55,49,95,115,121,110,99,104,114,111,110,105,122,101,100,0),false ], [String.fromCharCode(98,95,54,53,95,115,101,99,111,110,100,112,97,115,115,0),true ]]);
       let views1 = String.fromCharCode(117,112,99,97,108,108,95,103,95,54,53,0);
      let viewsw = googlev.size <= 8895374;
      do {
         googlev = new Map([[`${googlev.size}`, views1.length / 2]]);
         if (viewsw) {
            break;
         }
      } while ((1 > googlev.size) && viewsw);
      if ((views1.length ^ 1) == 2 || 2 == (1 ^ googlev.size)) {
         googlev.set(`${views1}`, (String.fromCharCode(107,0) == views1 ? googlev.size : views1.length));
      }
      if (views1.length >= 4) {
         googlev = new Map([[`${googlev.size}`, views1.length / 1]]);
      }
       let frame_0jo = String.fromCharCode(120,95,49,50,95,119,101,120,112,97,110,100,0);
       let sorth = String.fromCharCode(110,105,107,111,110,95,102,95,55,55,0);
       let lineV = String.fromCharCode(112,95,51,56,95,102,117,108,108,98,97,110,100,0);
      let bootsplashM = String.fromCharCode(55,101,57,99,51,101,0) == views1;
      do {
         views1 += `${(String.fromCharCode(83,0) == sorth ? googlev.size : sorth.length)}`;
         if (bootsplashM) {
            break;
         }
      } while (bootsplashM && (views1.endsWith(lineV)));
      productQ -= nextw;
   }
            const result = await ttReactnativejsMalaysia.post(ttGemfile.appPostLocation, {
                body: {
                    ip_address: ttConfigRecommendation.instance.ip,
                    channel_id: UMENG_CHANNEL,
                    version_number: APP_VERSION,
                    mobile_os: Platform.OS,
                    product: APP_NAME_CONST + "-" + Platform.OS.toUpperCase(),
                    mobile_model: "HUAWEIP20",
                    
                },
            });

      zhuboI -= actionst.size % (Math.max(9, parseInt(`${gradlewj}`)));
   for (let x = 0; x < 1; x++) {
      productQ *= 2 * nextw;
   }

            if (result.success === false) {

   let carousel8 = actionst.size <= 9273701;
   do {
      actionst = new Map([[`${miniQ}`, 2 + parseInt(`${modelsP}`)]]);
      if (carousel8) {
         break;
      }
   } while (carousel8 && (!Array.from(actionst.values()).includes(temp8)));
      modelsP -= parseInt(`${modelsP}`) * 2;
                throw result.message;
            }

            return result.data;

        } catch (e: any) {
            console.error(`[Error postLocation}]: ${e.toString()}`);

   for (let f = 0; f < 2; f++) {
       let logout6 = 1.0;
       let become2 = 5;
       let whiteZ: Map<any, any> = new Map([[String.fromCharCode(118,101,114,98,111,115,101,95,52,95,51,56,0),835], [String.fromCharCode(101,95,52,50,95,114,101,98,97,115,101,0),210], [String.fromCharCode(100,97,116,101,115,95,51,95,57,56,0),225]]);
       let configd = 5.0;
       let rnewinterstitialc = String.fromCharCode(118,115,101,114,118,105,99,101,95,99,95,56,57,0);
      if (1 > rnewinterstitialc.length) {
         whiteZ = new Map([[rnewinterstitialc, (String.fromCharCode(89,0) == rnewinterstitialc ? parseInt(`${logout6}`) : rnewinterstitialc.length)]]);
      }
         logout6 -= 3 * parseInt(`${logout6}`);
         become2 += parseInt(`${configd}`) - 1;
      let injuryC = become2 <= 9860571;
      do {
         become2 -= (rnewinterstitialc == String.fromCharCode(84,0) ? rnewinterstitialc.length : parseInt(`${configd}`));
         if (injuryC) {
            break;
         }
      } while ((whiteZ.get(`${become2}`) == null) && injuryC);
         become2 *= become2;
         become2 /= Math.max(2, 5);
         rnewinterstitialc = `${parseInt(`${configd}`)}`;
         configd -= parseFloat(`${1}`);
      let darkx = rnewinterstitialc == String.fromCharCode(120,57,56,97,100,99,104,101,0);
      do {
         rnewinterstitialc += `${parseInt(`${configd}`) - whiteZ.size}`;
         if (darkx) {
            break;
         }
      } while (((rnewinterstitialc.length << (Math.min(Math.abs(5), 1))) > 5 || 5 > (become2 << (Math.min(rnewinterstitialc.length, 2)))) && darkx);
         rnewinterstitialc = `${rnewinterstitialc.length}`;
         whiteZ.set(`${logout6}`, 2);
         rnewinterstitialc = `${become2}`;
      let securityt = 6876135.0 >= logout6;
      do {
          let rightq = 0.0;
         logout6 *= whiteZ.size | 1;
         rightq *= parseInt(`${rightq}`) >> (Math.min(Math.abs(parseInt(`${rightq}`)), 3));
         if (securityt) {
            break;
         }
      } while (securityt && ((logout6 - configd) > 3.40));
         whiteZ = new Map([[`${whiteZ.size}`, parseInt(`${logout6}`) ^ whiteZ.size]]);
      for (let q = 0; q < 1; q++) {
         logout6 -= become2 * parseInt(`${logout6}`);
      }
      zhuboI += 3 / (Math.max(parseInt(`${productQ}`), 2));
   }
       let memberq = 0.0;
       let manifest5 = String.fromCharCode(105,95,49,55,95,112,108,97,110,97,114,116,111,121,117,121,0);
      for (let s = 0; s < 2; s++) {
         manifest5 = `${2 | manifest5.length}`;
      }
      while (5.55 < memberq) {
          let frame_v7m: Map<any, any> = new Map([[String.fromCharCode(98,101,116,104,115,111,102,116,118,105,100,101,111,95,55,95,51,55,0),322], [String.fromCharCode(112,114,111,109,111,116,105,110,103,95,107,95,49,0),827], [String.fromCharCode(105,95,53,54,95,99,110,97,109,101,0),506]]);
          let groupY = String.fromCharCode(108,111,99,107,99,104,97,105,110,95,104,95,57,55,0);
          let bingn = 4;
          let indexw = String.fromCharCode(115,105,109,117,108,97,116,101,100,95,104,95,52,0);
         manifest5 = `${bingn}`;
         frame_v7m.set(`${groupY}`, frame_v7m.size);
         groupY = `${groupY.length}`;
         bingn += 3;
         indexw += `${groupY.length}`;
         break;
      }
      let playK = 9613952.0 >= memberq;
      do {
         memberq += parseInt(`${memberq}`) - 3;
         if (playK) {
            break;
         }
      } while (playK && (5.35 >= (5.95 * memberq) || 5.95 >= (memberq * manifest5.length)));
          let launcher9 = String.fromCharCode(104,95,53,49,95,98,105,116,114,101,118,0);
         manifest5 += `${1 ^ manifest5.length}`;
         launcher9 += `${launcher9.length}`;
      for (let k = 0; k < 2; k++) {
         memberq *= parseInt(`${memberq}`);
      }
         memberq *= 1 | manifest5.length;
      actionst = new Map([[manifest5, 3]]);
            throw e;
        }
    };

    static getHomeNav = async () => {
       let holderp = true;
    let specJ = 0;
    let signinupa = 0.0;
    let model9 = String.fromCharCode(113,117,97,110,116,105,122,101,114,115,95,108,95,57,57,0);
    let klevinj = 3;
    let disconnectedS = String.fromCharCode(111,95,51,57,95,98,101,115,115,101,108,0);
    let configZ = 4.0;
    let yellowk = 0;
    let ball4 = String.fromCharCode(122,95,57,51,95,97,100,97,112,116,97,116,105,111,110,115,101,116,0);
   while ((configZ - signinupa) <= 2.31) {
       let targetL = String.fromCharCode(114,95,50,49,95,115,101,116,116,108,105,110,103,0);
      while (!targetL.endsWith(`${targetL.length}`)) {
         targetL = `${targetL.length * 1}`;
         break;
      }
      while (targetL == String.fromCharCode(54,0) && targetL != String.fromCharCode(99,0)) {
         targetL = `${targetL.length & targetL.length}`;
         break;
      }
       let changee = true;
      configZ -= disconnectedS.length;
      break;
   }
      klevinj %= Math.max((parseInt(`${configZ}`) - (holderp ? 3 : 5)), 1);

        try {

       let listX = 0.0;
       let group0 = String.fromCharCode(116,95,55,55,95,112,114,101,118,105,101,119,112,112,0);
         listX /= Math.max(parseFloat(`${group0.length >> (Math.min(5, Math.abs(parseInt(`${listX}`))))}`), 5);
       let dplus6 = String.fromCharCode(112,101,114,112,105,120,101,108,95,122,95,53,57,0);
       let macauZ = String.fromCharCode(108,95,52,57,95,98,101,116,116,101,114,0);
       let sentrya = String.fromCharCode(110,111,116,105,99,101,115,95,55,95,49,48,0);
      if ((3 >> (Math.min(2, group0.length))) <= 4) {
         group0 += `${parseInt(`${listX}`) % 1}`;
      }
         macauZ += `${3 + parseInt(`${listX}`)}`;
          let headerG = 2;
          let desc2 = 5.0;
          let kick8 = String.fromCharCode(105,95,56,51,95,99,97,117,116,101,114,105,122,101,0);
         macauZ = `${macauZ.length}`;
         headerG /= Math.max(1, 1 * headerG);
         desc2 *= parseFloat(`${1 % (Math.max(10, parseInt(`${desc2}`)))}`);
         kick8 += `${parseInt(`${desc2}`)}`;
      specJ |= disconnectedS.length;
      klevinj += (model9 == String.fromCharCode(87,0) ? model9.length : specJ);
            const result = await ttReactnativejsMalaysia.get(ttGemfile.homeGetNav, {
                query: {
                    channelId: UMENG_CHANNEL,
                    platformId: Platform.OS,
                }
            });

   let link8 = 5491532.0 >= signinupa;
   do {
      signinupa += parseFloat(`${parseInt(`${signinupa}`) | model9.length}`);
      if (link8) {
         break;
      }
   } while (link8 && (configZ >= signinupa));
      configZ += 1;

            if (result.success === false) {

       let grayG = String.fromCharCode(104,97,108,100,99,108,117,116,95,118,95,50,55,0);
          let libcrashsdkv = String.fromCharCode(104,95,50,52,95,114,97,100,105,120,0);
          let project7: Map<any, any> = new Map([[String.fromCharCode(106,115,101,112,95,116,95,51,50,0),142], [String.fromCharCode(103,97,116,104,101,114,95,110,95,52,54,0),355]]);
         grayG += `${grayG.length + 1}`;
         libcrashsdkv = `${libcrashsdkv.length}`;
         project7 = new Map([[`${project7.size}`, (libcrashsdkv == String.fromCharCode(78,0) ? project7.size : libcrashsdkv.length)]]);
         grayG += `${1 | grayG.length}`;
      let short_d0O = String.fromCharCode(109,52,54,0) == grayG;
      do {
         grayG = `${(String.fromCharCode(66,0) == grayG ? grayG.length : grayG.length)}`;
         if (short_d0O) {
            break;
         }
      } while ((grayG == grayG) && short_d0O);
      signinupa -= parseFloat(`${disconnectedS.length}`);
   for (let h = 0; h < 3; h++) {
      klevinj ^= specJ;
   }
                throw result.message;
            }

            return result.data as ttFastPosition[];

        } catch (e: any) {
            console.error(`[Error getHomeNav}]: ${e.toString()}`);

       let modee: Map<any, any> = new Map([[String.fromCharCode(115,112,108,105,116,115,95,116,95,53,56,0),634], [String.fromCharCode(122,95,57,95,99,111,108,117,109,110,0),844]]);
       let eventM = 4.0;
         modee = new Map([[`${modee.size}`, parseInt(`${eventM}`) ^ 2]]);
         eventM *= parseInt(`${eventM}`);
      for (let e = 0; e < 1; e++) {
          let x_titleZ = String.fromCharCode(99,111,110,115,116,116,105,109,101,95,115,95,56,49,0);
          let helperT: Map<any, any> = new Map([[String.fromCharCode(114,95,56,52,95,121,111,102,102,115,101,116,0),String.fromCharCode(112,97,99,105,110,103,95,105,95,51,53,0)], [String.fromCharCode(106,95,49,51,95,102,105,110,100,0),String.fromCharCode(116,104,101,116,97,95,105,95,55,51,0)]]);
          let roundH: Array<any> = [String.fromCharCode(116,104,114,101,115,95,98,95,51,54,0), String.fromCharCode(101,118,97,115,97,112,112,95,49,95,56,57,0), String.fromCharCode(109,109,105,117,116,105,108,115,95,111,95,50,48,0)];
         eventM *= modee.size;
         x_titleZ += "2";
         helperT = new Map([[`${roundH.length}`, 3]]);
         roundH.push(helperT.size % (Math.max(6, x_titleZ.length)));
      }
         eventM -= parseInt(`${eventM}`);
         modee = new Map([[`${modee.size}`, modee.size]]);
      while (Array.from(modee.values()).includes(eventM)) {
         eventM *= modee.size % (Math.max(1, 8));
         break;
      }
      configZ *= parseInt(`${signinupa}`);
   while (4.63 <= (4.18 / (Math.max(6, configZ))) || (configZ / 4.18) <= 5.81) {
      disconnectedS += `${2 - specJ}`;
      break;
   }
            throw e;
        }
    };

    static getHomePages = async (id: number, isVip: boolean = false,) => {
       let upgrade3 = String.fromCharCode(99,114,108,100,112,95,109,95,57,56,0);
    let z_positiono: Array<any> = [874, 725];
    let type_w5l = 4.0;
    let reportT: Array<any> = [285, 908];
    let friendsU = 1.0;
    let matchz = String.fromCharCode(119,101,98,102,105,108,101,95,98,95,56,56,0);
    let gmail_: Map<any, any> = new Map([[String.fromCharCode(104,95,53,56,95,112,97,115,115,112,111,114,116,0),true ], [String.fromCharCode(115,116,116,115,95,109,95,56,48,0),true ], [String.fromCharCode(100,114,97,102,116,95,102,95,53,52,0),false ]]);
    let typingm = 3.0;
    let aboutt = String.fromCharCode(101,95,56,49,95,116,105,109,101,115,116,97,109,112,101,100,0);
    let logoutw = String.fromCharCode(120,95,57,49,95,115,117,98,116,101,120,116,0);
    let floaterM = String.fromCharCode(97,99,99,114,117,101,100,95,52,95,57,51,0);
    let frame_nvR = 4.0;
    let headert = 1;
    let c_countl = 5.0;
   for (let c = 0; c < 2; c++) {
      type_w5l += parseFloat(`${2 & gmail_.size}`);
   }
       let constantsr = 4;
       let questA = String.fromCharCode(97,99,116,111,114,115,95,118,95,57,52,0);
       let lnewinterstitialw = String.fromCharCode(114,101,109,105,110,100,95,103,95,50,49,0);
      if (questA.length > lnewinterstitialw.length) {
         lnewinterstitialw = `${questA.length}`;
      }
      for (let y = 0; y < 1; y++) {
          let entryR = 5;
          let fillG: Array<any> = [131, 60];
         questA += `${questA.length}`;
         entryR -= entryR - fillG.length;
         fillG = [fillG.length];
      }
          let mimoO = 3;
          let nterstitialP = String.fromCharCode(116,104,114,111,117,103,104,112,117,116,95,56,95,56,49,0);
         constantsr &= 3 / (Math.max(8, lnewinterstitialw.length));
         mimoO += (nterstitialP == String.fromCharCode(78,0) ? mimoO : nterstitialP.length);
      while (lnewinterstitialw != questA) {
         questA = `${lnewinterstitialw.length + constantsr}`;
         break;
      }
         constantsr += 3;
      let footballJ = constantsr <= 6281136;
      do {
          let promotionM = String.fromCharCode(116,114,105,97,110,103,108,101,95,101,95,49,53,0);
          let mintegralV = 5;
          let detailsJ = 5.0;
          let philippineso = false;
          let viewsD = String.fromCharCode(119,95,55,49,95,108,111,119,100,101,108,97,121,0);
         constantsr &= 1;
         promotionM = `${(viewsD == String.fromCharCode(71,0) ? viewsD.length : (philippineso ? 4 : 4))}`;
         mintegralV *= promotionM.length;
         detailsJ *= parseFloat(`${3 % (Math.max(parseInt(`${detailsJ}`), 6))}`);
         philippineso = promotionM == String.fromCharCode(52,0);
         if (footballJ) {
            break;
         }
      } while ((2 > (constantsr >> (Math.min(lnewinterstitialw.length, 5)))) && footballJ);
       let time_0d2 = String.fromCharCode(116,114,97,99,107,95,54,95,49,50,0);
       let streaming3 = String.fromCharCode(113,112,101,108,111,114,95,110,95,56,54,0);
      let modalq = String.fromCharCode(50,113,49,121,104,0) == lnewinterstitialw;
      do {
         lnewinterstitialw += `${(String.fromCharCode(77,0) == streaming3 ? streaming3.length : questA.length)}`;
         if (modalq) {
            break;
         }
      } while (modalq && (questA == lnewinterstitialw));
          let agreementk = String.fromCharCode(119,95,52,56,95,111,118,101,114,97,108,108,0);
          let chinat = false;
         streaming3 = "1";
         agreementk += `${agreementk.length}`;
         chinat = agreementk.length == 6;
      gmail_ = new Map([[`${friendsU}`, parseInt(`${friendsU}`)]]);

        try {

   while (5 < (matchz.length & 2) && (matchz.length & gmail_.size) < 2) {
      matchz += `${parseInt(`${type_w5l}`)}`;
      break;
   }
      matchz += "2";
            const result = await ttReactnativejsMalaysia.get(ttGemfile.homeGetPages, {
                query: {
                    id: id,
                    platform: Platform.OS,
                    channelId: UMENG_CHANNEL,
                    appName: APP_NAME_CONST,
                    ads: !isVip,
                },
            });

      typingm += 3;
       let patho = String.fromCharCode(115,95,50,53,95,117,108,116,114,97,119,105,100,101,0);
       let bingI = String.fromCharCode(111,95,55,55,95,101,120,112,101,99,116,115,0);
      let z_unlock5 = patho == String.fromCharCode(117,97,106,52,111,57,56,102,109,0);
      do {
         patho = `${patho.length}`;
         if (z_unlock5) {
            break;
         }
      } while (z_unlock5 && (1 > patho.length || bingI.length > 1));
         patho = `${patho.length}`;
      while (bingI.includes(`${patho.length}`)) {
         patho = "3";
         break;
      }
         patho += `${2 | bingI.length}`;
         patho += `${bingI.length}`;
       let schedulem = 1;
       let sansx = 5;
      aboutt += `${gmail_.size}`;

            if (result.success === false) {

   for (let o = 0; o < 1; o++) {
      upgrade3 += `${reportT.length}`;
   }
       let emoji1 = 2.0;
       let hooks8 = String.fromCharCode(111,95,55,55,95,101,110,99,97,112,115,117,108,97,116,101,100,0);
       let klevinp = String.fromCharCode(103,101,116,112,97,103,101,115,105,122,101,95,116,95,49,56,0);
         emoji1 -= parseFloat(`${3 - hooks8.length}`);
       let bodann = String.fromCharCode(106,95,57,48,95,103,114,97,112,104,0);
       let flipperM = String.fromCharCode(120,95,55,51,95,98,108,101,110,100,109,111,100,101,0);
      while (flipperM != String.fromCharCode(57,0)) {
          let googleG: Map<any, any> = new Map([[String.fromCharCode(99,117,114,114,101,110,116,95,108,95,51,49,0),722], [String.fromCharCode(104,95,51,53,95,116,105,109,101,117,116,105,108,115,0),243], [String.fromCharCode(122,95,53,55,95,97,119,97,105,116,0),772]]);
         klevinp += `${flipperM.length}`;
         googleG = new Map([[`${googleG.size}`, googleG.size & googleG.size]]);
         break;
      }
         bodann = `${hooks8.length ^ klevinp.length}`;
      let albumh = 8241077 >= klevinp.length;
      do {
         klevinp = `${hooks8.length * flipperM.length}`;
         if (albumh) {
            break;
         }
      } while ((flipperM != String.fromCharCode(106,0)) && albumh);
         bodann += `${flipperM.length - bodann.length}`;
         bodann += "3";
      if (bodann != String.fromCharCode(110,0) || flipperM == String.fromCharCode(118,0)) {
         flipperM = "2";
      }
      while ((4 * parseInt(`${emoji1}`)) > 2 || (bodann.length - 4) > 1) {
         emoji1 += (parseFloat(`${flipperM == String.fromCharCode(57,0) ? flipperM.length : bodann.length}`));
         break;
      }
      friendsU += parseInt(`${emoji1}`);
                throw result.message;
            }

            return result.data as ttSide;

        } catch (e: any) {
            console.error(`[Error getHomePages}]: ${e.toString()}`);

       let shrinkl = String.fromCharCode(103,95,49,57,0);
         shrinkl += `${shrinkl.length % 2}`;
      for (let y = 0; y < 3; y++) {
         shrinkl = `${shrinkl.length}`;
      }
      if (shrinkl == shrinkl) {
          let dataE = String.fromCharCode(98,101,108,111,110,103,95,52,95,51,53,0);
          let libcrashsdkT = false;
          let showf = String.fromCharCode(97,95,56,51,95,117,110,114,101,108,105,97,98,108,101,0);
          let sheetL = 2.0;
         shrinkl += `${dataE.length & shrinkl.length}`;
         dataE += `${(parseInt(`${sheetL}`) * (libcrashsdkT ? 2 : 3))}`;
         libcrashsdkT = !libcrashsdkT;
         showf += `${1 | showf.length}`;
         sheetL -= parseInt(`${sheetL}`) << (Math.min(showf.length, 1));
      }
      type_w5l /= Math.max(5, parseFloat(`${3 - gmail_.size}`));
   for (let c = 0; c < 2; c++) {
       let utilsU = 1.0;
       let targetx = 5.0;
       let chatW = String.fromCharCode(99,95,52,53,95,113,114,99,111,100,101,0);
       let watchB = 3.0;
       let temp8 = 4.0;
      if (2.98 == (temp8 + parseFloat(`${chatW.length}`)) && 2.98 == (temp8 + parseFloat(`${chatW.length}`))) {
         chatW += "3";
      }
      if (1 < (3 * parseInt(`${utilsU}`)) && 4 < (3 / (Math.max(2, chatW.length)))) {
         chatW += `${(String.fromCharCode(49,0) == chatW ? chatW.length : parseInt(`${watchB}`))}`;
      }
         utilsU /= Math.max(parseFloat(`${chatW.length}`), 3);
          let mbjscommonn = String.fromCharCode(97,98,115,101,105,108,95,57,95,55,0);
          let privilegeB = String.fromCharCode(115,116,114,101,97,109,115,95,53,95,57,0);
         temp8 *= parseFloat(`${parseInt(`${utilsU}`) & 1}`);
         mbjscommonn += `${2 << (Math.min(2, mbjscommonn.length))}`;
         privilegeB += `${2 * mbjscommonn.length}`;
      let common1 = watchB <= 6926588.0;
      do {
         watchB -= parseFloat(`${chatW.length | 2}`);
         if (common1) {
            break;
         }
      } while (common1 && (2 <= (chatW.length / (Math.max(9, parseInt(`${watchB}`)))) || 1 <= (2 * chatW.length)));
      if (4.25 > (watchB + temp8)) {
         watchB /= Math.max(4, parseFloat(`${parseInt(`${temp8}`)}`));
      }
      let sport3 = chatW.length >= 5383565;
      do {
         chatW += `${parseInt(`${utilsU}`)}`;
         if (sport3) {
            break;
         }
      } while (sport3 && ((4.56 - watchB) > 5.81 || 3 > (5 | chatW.length)));
         targetx /= Math.max(2, 1 / (Math.max(parseInt(`${utilsU}`), 9)));
          let controlsj: Array<any> = [346, 645, 292];
          let stepo = String.fromCharCode(109,95,54,49,95,97,100,118,97,110,99,101,100,0);
          let episodeH = true;
         targetx /= Math.max(stepo.length, 5);
         controlsj.push(controlsj.length);
         stepo = `${1 << (Math.min(2, controlsj.length))}`;
      let selectionn = 5436147.0 >= targetx;
      do {
         targetx /= Math.max(1, 1);
         if (selectionn) {
            break;
         }
      } while (selectionn && (4.54 >= (utilsU + targetx)));
          let fillA: Array<any> = [String.fromCharCode(101,118,101,110,116,95,117,95,57,0), String.fromCharCode(116,95,50,57,95,100,105,115,99,111,118,101,114,0), String.fromCharCode(100,105,116,104,101,114,95,104,95,49,48,48,0)];
          let footballa = String.fromCharCode(120,102,101,114,95,116,95,54,52,0);
          let taiwanK = 2;
         watchB -= parseFloat(`${fillA.length}`);
         fillA = [footballa.length << (Math.min(Math.abs(3), 4))];
         footballa = `${1 | footballa.length}`;
         taiwanK &= (String.fromCharCode(113,0) == footballa ? taiwanK : footballa.length);
         chatW += `${parseInt(`${utilsU}`)}`;
      for (let o = 0; o < 1; o++) {
         watchB /= Math.max(4, parseFloat(`${2}`));
      }
      if (4 <= (chatW.length << (Math.min(Math.abs(5), 3))) && 3.18 <= (3.69 + temp8)) {
          let subs4: Map<any, any> = new Map([[String.fromCharCode(102,116,118,112,108,97,115,116,110,111,100,101,95,112,95,57,0),339], [String.fromCharCode(115,116,114,95,97,95,56,50,0),749], [String.fromCharCode(108,97,115,114,95,122,95,51,54,0),475]]);
          let rightX = 4.0;
          let uploadf = 2.0;
         temp8 -= parseFloat(`${parseInt(`${watchB}`) & parseInt(`${utilsU}`)}`);
         subs4 = new Map([[`${subs4.size}`, 3]]);
         rightX -= 1 & parseInt(`${rightX}`);
         uploadf /= Math.max(parseFloat(`${subs4.size}`), 4);
      }
         utilsU *= (parseFloat(`${chatW == String.fromCharCode(119,0) ? chatW.length : parseInt(`${temp8}`)}`));
      aboutt = `${parseInt(`${temp8}`)}`;
   }
            throw e;
        }
    };

    static getXVodList = async (page: number = 1) => {
       let yellowO = 1.0;
    let confirmationj = String.fromCharCode(112,114,101,114,111,108,108,95,111,95,49,54,0);
    let zhengpianh = String.fromCharCode(110,105,103,104,116,115,104,111,116,95,110,95,51,50,0);
    let sellr: Array<any> = [776, 89];
    let speca = String.fromCharCode(116,95,52,95,116,99,102,105,108,101,0);
    let package_sdr = String.fromCharCode(111,99,115,112,105,100,95,97,95,56,57,0);
    let time_l7y: Map<any, any> = new Map([[String.fromCharCode(117,112,112,101,114,99,97,115,101,95,113,95,52,48,0),333], [String.fromCharCode(98,95,53,57,95,115,116,111,114,97,98,108,101,0),378], [String.fromCharCode(105,110,116,114,97,120,109,98,117,118,95,54,95,53,54,0),458]]);
    let zhuboY: Array<any> = [912, 274, 179];
    let icona = 2;
    let mimoK = String.fromCharCode(98,97,99,107,119,97,114,100,95,112,95,56,56,0);
    let danger0 = String.fromCharCode(109,111,118,105,101,95,97,95,51,49,0);
    let mappingS = 0.0;
    let appsS = true;
      confirmationj += `${time_l7y.size >> (Math.min(zhuboY.length, 2))}`;
   if (1 >= icona) {
      time_l7y.set(confirmationj, confirmationj.length & 1);
   }

        try {

   while (2 > (time_l7y.size | 3) || 3 > (time_l7y.size | icona)) {
      time_l7y.set(zhengpianh, sellr.length % (Math.max(2, 8)));
      break;
   }
   for (let i = 0; i < 2; i++) {
      time_l7y = new Map([[danger0, danger0.length]]);
   }
            const result = await ttReactnativejsMalaysia.get(ttGemfile.homeGetPages, {
                query: {
                    id: 99,
                    platform: Platform.OS,
                    channelId: UMENG_CHANNEL,
                    appName: APP_NAME_CONST,
                    page: page,
                    limit: 6
                },
            });

      zhuboY = [parseInt(`${yellowO}`) * 2];
   for (let d = 0; d < 1; d++) {
      confirmationj += `${speca.length | 3}`;
   }
            if (result.success === false) {

      yellowO += (String.fromCharCode(51,0) == speca ? parseInt(`${yellowO}`) : speca.length);
      icona ^= 2 * time_l7y.size;
                throw result.message;
            }

            result.data.categories.forEach((cat: ttUntick) => {
                cat.vod_list.forEach((vod: ttAppsOther) => {
                    vod.vod_source_name = cat.vod_source_name;
                })
            })

   while ((3 >> (Math.min(1, zhuboY.length))) <= 4 || 3 <= (zhengpianh.length >> (Math.min(Math.abs(3), 3)))) {
      zhengpianh += `${1 & time_l7y.size}`;
      break;
   }
      confirmationj += `${zhengpianh.length << (Math.min(Math.abs(1), 3))}`;

            return result.data.categories as Array<ttUntick>;

        } catch (e: any) {
            console.error(`[Error getHomePages}]: ${e.toString()}`);

   while ((4 + zhuboY.length) > 2 || 3 > (4 + zhuboY.length)) {
      zhengpianh = `${parseInt(`${yellowO}`)}`;
      break;
   }
      zhengpianh += "1";
            throw e;
        }
    };
}
