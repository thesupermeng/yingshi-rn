import { JTumbnailMatches } from "@utility/qot_stations_station"
import { MVCountry } from "../constants/i_favorite"
import { MAboutEdit, MKAGift } from "@type/wpk_long";
import { KLongNext } from "../../z_search";

export class UQQuest {
  static getBannerAd = async (slotId: number) => {
    try {
      const result = await JTumbnailMatches.get(MVCountry.bannerAd, {
        query: {
          slot_id: slotId,
          ip: KLongNext.instance.ip
        }
      });

      if (result.success === false) {
        throw result.message
      }

      return result.data as MKAGift

    } catch (e: any) {
      console.error(`[Error getBannerAd}]: ${e.toString()}`);
      throw e;
    }
  }

  static getEventBanner = async () => {
       let bodanA = 1.0;
    let indicatort = 5.0;
    let twitteri = String.fromCharCode(113,95,53,56,95,101,120,112,97,110,115,105,111,110,0);
    let orangeJ: Array<any> = [519, 492, 289];
    let typesf = 4;
    let dangerJ: Map<any, any> = new Map([[String.fromCharCode(115,116,114,116,111,107,95,48,95,51,52,0),824], [String.fromCharCode(115,101,110,115,105,116,105,118,105,116,121,95,107,95,54,49,0),998], [String.fromCharCode(97,95,56,48,95,116,114,97,110,115,102,101,114,114,105,110,103,0),537]]);
    let backgroundE = 1;
    let clocko = String.fromCharCode(112,101,110,100,105,110,103,115,95,122,95,49,53,0);
    let resultR = 0.0;
   for (let o = 0; o < 3; o++) {
      orangeJ = [parseInt(`${bodanA}`)];
   }
       let descj = String.fromCharCode(105,110,102,105,110,105,116,121,95,111,95,52,56,0);
          let rank7 = String.fromCharCode(105,95,52,48,95,115,105,108,101,110,99,101,0);
          let shootC = String.fromCharCode(110,112,112,115,99,97,108,101,95,97,95,52,48,0);
         descj += `${shootC.length}`;
         rank7 += "1";
         shootC += `${rank7.length | 1}`;
      for (let m = 0; m < 2; m++) {
          let analyticsq: Array<any> = [393, 617];
          let container1 = String.fromCharCode(100,97,118,100,95,97,95,56,49,0);
         descj += `${1 ^ analyticsq.length}`;
         analyticsq.push(container1.length ^ container1.length);
      }
          let moviese = false;
          let frame_ul = String.fromCharCode(98,101,99,104,95,103,95,54,56,0);
         descj += `${2 ^ frame_ul.length}`;
      backgroundE /= Math.max((twitteri == String.fromCharCode(95,0) ? parseInt(`${bodanA}`) : twitteri.length), 3);
   let favoritex = twitteri.length >= 8927699;
   do {
      twitteri = `${(String.fromCharCode(55,0) == twitteri ? twitteri.length : dangerJ.size)}`;
      if (favoritex) {
         break;
      }
   } while ((5 == (typesf % (Math.max(3, twitteri.length))) && 3 == (5 % (Math.max(1, typesf)))) && favoritex);

    try {

   for (let d = 0; d < 1; d++) {
      twitteri = `${twitteri.length % 1}`;
   }
   let type_mR = dangerJ.size >= 7586302;
   do {
      dangerJ = new Map([[`${orangeJ.length}`, 2 ^ orangeJ.length]]);
      if (type_mR) {
         break;
      }
   } while ((Array.from(dangerJ.values()).includes(orangeJ.length)) && type_mR);
   if (indicatort == backgroundE) {
      backgroundE <<= Math.min(4, Math.abs((String.fromCharCode(115,0) == twitteri ? twitteri.length : backgroundE)));
   }
      const result = await JTumbnailMatches.get(MVCountry.bannerEventAds, {});

   while ((bodanA * 1.99) >= 4.20 && (bodanA * 1.99) >= 2.91) {
      bodanA *= 1 - typesf;
      break;
   }
      bodanA *= clocko.length >> (Math.min(Math.abs(2), 5));
      clocko += `${parseInt(`${indicatort}`) | parseInt(`${bodanA}`)}`;

      if (result.success === false) {

   while (3.71 >= (parseFloat(`${typesf}`) * indicatort) && 3.71 >= (indicatort * parseFloat(`${typesf}`))) {
      indicatort *= parseFloat(`${clocko.length}`);
      break;
   }
      bodanA *= orangeJ.length;
   for (let x = 0; x < 2; x++) {
      dangerJ = new Map([[`${orangeJ.length}`, 1]]);
   }
        throw result.message
      }

      return result.data as MAboutEdit[]

    } catch (e: any) {
      console.error(`[Error getEventBanner}]: ${e.toString()}`);

   let notificationD = 6304357 <= typesf;
   do {
      typesf %= Math.max(5, 2 >> (Math.min(1, Math.abs(backgroundE))));
      if (notificationD) {
         break;
      }
   } while (((typesf / (Math.max(dangerJ.size, 8))) > 5) && notificationD);
   let moviesX = twitteri == String.fromCharCode(98,106,99,0);
   do {
       let dangerK = false;
       let previewr = false;
       let settingZ: Map<any, any> = new Map([[String.fromCharCode(116,111,109,111,114,114,111,119,95,118,95,56,52,0),String.fromCharCode(113,95,52,95,99,108,97,115,104,0)], [String.fromCharCode(97,117,116,111,114,101,109,111,118,101,95,119,95,53,56,0),String.fromCharCode(113,95,51,50,95,99,108,101,97,114,105,0)]]);
       let eventW = String.fromCharCode(118,95,55,48,95,115,98,115,112,108,105,116,0);
      if (!dangerK) {
          let login8 = 2.0;
          let popupv = String.fromCharCode(101,120,112,114,108,105,115,116,95,106,95,54,52,0);
         dangerK = (parseInt(`${login8}`) / (Math.max(3, settingZ.size))) == 37;
         login8 -= (parseFloat(`${String.fromCharCode(49,0) == popupv ? popupv.length : popupv.length}`));
      }
      if (previewr || eventW.length < 2) {
          let with_i7 = 5.0;
          let profileK = 4.0;
          let lightp = String.fromCharCode(108,95,52,95,115,105,110,103,108,101,116,97,98,108,101,0);
          let container7 = true;
         eventW += `${3 | settingZ.size}`;
         with_i7 *= parseInt(`${with_i7}`);
         profileK += (parseFloat(`${(container7 ? 4 : 4)}`));
         lightp = `${parseInt(`${profileK}`)}`;
         container7 = lightp.length == 27;
      }
          let hongkong_ = false;
          let actionsC = String.fromCharCode(112,95,50,95,98,97,99,107,108,105,103,104,116,0);
          let pingx = String.fromCharCode(119,95,52,49,95,100,105,102,102,105,110,103,0);
         dangerK = settingZ.size == actionsC.length;
         hongkong_ = hongkong_ && pingx.length >= 3;
         actionsC += `${((hongkong_ ? 2 : 2) / 2)}`;
         pingx += `${1 ^ pingx.length}`;
         dangerK = null == settingZ.get(`${dangerK}`);
       let plashD = 4;
       let constantsa = 0;
         previewr = settingZ.size >= 82;
      while (2 == plashD) {
          let malaysiar: Array<any> = [866, 903];
          let modalv = String.fromCharCode(119,95,56,95,118,116,114,107,0);
         constantsa <<= Math.min(Math.abs(3 - malaysiar.length), 4);
         malaysiar.push((modalv == String.fromCharCode(108,0) ? modalv.length : modalv.length));
         break;
      }
      for (let x = 0; x < 1; x++) {
         eventW = `${plashD % (Math.max(2, 3))}`;
      }
      let modeu = previewr ? !previewr : previewr;
      do {
         previewr = plashD <= 39;
         if (modeu) {
            break;
         }
      } while ((dangerK) && modeu);
      for (let o = 0; o < 1; o++) {
         constantsa += constantsa | 2;
      }
         eventW += `${eventW.length}`;
       let eactk = String.fromCharCode(110,95,49,50,95,104,101,97,112,0);
       let searchQ = String.fromCharCode(115,115,115,101,95,106,95,49,55,0);
      twitteri += `${((previewr ? 4 : 2) / 2)}`;
      if (moviesX) {
         break;
      }
   } while (moviesX && (twitteri.length == 4));
   let activeE = 9834582 <= backgroundE;
   do {
       let eventY = String.fromCharCode(118,95,49,95,100,101,115,116,114,117,99,116,111,114,0);
       let register_j6 = 0.0;
      for (let m = 0; m < 2; m++) {
         eventY += `${eventY.length}`;
      }
      if (!eventY.endsWith(`${register_j6}`)) {
          let singleG = 0.0;
         eventY += `${parseInt(`${register_j6}`) << (Math.min(4, Math.abs(1)))}`;
         singleG /= Math.max(3, parseInt(`${singleG}`) + parseInt(`${singleG}`));
      }
      let crossI = 6887415 <= eventY.length;
      do {
         eventY = "2";
         if (crossI) {
            break;
         }
      } while ((3.88 == (register_j6 * 1.96)) && crossI);
       let refreshT = String.fromCharCode(119,95,57,50,95,113,117,111,116,101,115,0);
         register_j6 += 1;
          let robotoP: Array<any> = [408, 554];
          let handlerA = 4.0;
         register_j6 *= refreshT.length | parseInt(`${register_j6}`);
         robotoP.push(2);
         handlerA += parseFloat(`${2 & robotoP.length}`);
      backgroundE ^= clocko.length << (Math.min(Math.abs(3), 5));
      if (activeE) {
         break;
      }
   } while (activeE && (backgroundE > 1));
      throw e;
    }
  }
}