import { yys_FavoriteCustom, yys_MbbannerComment } from "@constants";
import { yys_Checkbox, yys_SuccessPolicy, yys_Bing, yys_VolumeReward, yys_ConfigFound, yys_OrientationPath } from "@type/yys_libzeus";
import { yys_StringsVignette } from "@utility/yys_ping";
import { AD_VIDEO_SECONDS, APP_NAME_FOR_API, UMENG_CHANNEL } from "@utility/yys_ajax_switch";
import { Platform } from "react-native";
import { yys_MinivodPangle } from "../../yys_mimo_vignette";
import { yys_Progress } from "@utility/yys_less_field";

export class yys_Downloader {
   static getTopicType = async () => {
      let dycreatorG: Array<any> = [813, 216, 370];
      let cornerg = String.fromCharCode(106, 95, 51, 51, 95, 112, 114, 111, 116, 111, 99, 111, 108, 0);
      let termsl = 3;
      let bannerG = true;
      let videojs5 = 3.0;
      let leakcheckerr = String.fromCharCode(105, 110, 115, 116, 97, 108, 108, 101, 100, 95, 98, 95, 49, 51, 0);
      let tailD: Map<any, any> = new Map([[String.fromCharCode(109, 97, 99, 101, 120, 97, 109, 112, 108, 101, 95, 122, 95, 56, 49, 0), false], [String.fromCharCode(116, 97, 112, 112, 105, 110, 103, 95, 98, 95, 57, 52, 0), true]]);
      let applicationb: Map<any, any> = new Map([[String.fromCharCode(109, 98, 115, 101, 103, 109, 101, 110, 116, 97, 116, 105, 111, 110, 95, 98, 95, 54, 50, 0), String.fromCharCode(99, 112, 111, 115, 95, 119, 95, 53, 48, 0)], [String.fromCharCode(115, 95, 56, 54, 95, 98, 97, 111, 98, 97, 98, 0), String.fromCharCode(108, 95, 57, 57, 95, 115, 97, 118, 101, 0)]]);
      let yellowG = 2.0;
      let reminderu = 4;
      let matchesb: Map<any, any> = new Map([[String.fromCharCode(119, 95, 50, 55, 95, 99, 111, 110, 100, 0), String.fromCharCode(118, 95, 49, 48, 95, 105, 110, 102, 108, 97, 116, 101, 0)], [String.fromCharCode(103, 95, 54, 52, 95, 116, 114, 97, 118, 101, 108, 0), String.fromCharCode(106, 95, 52, 54, 95, 100, 101, 99, 98, 110, 0)]]);
      let styleG = String.fromCharCode(99, 95, 54, 48, 95, 103, 101, 116, 120, 115, 115, 101, 0);
      let orientationx = 1.0;
      let scrollviewT: Array<any> = [765, 414];
      let strA = 3.0;
      let reminderM = true;
      if ((termsl / (Math.max(videojs5, 8))) < 3.99) {
         videojs5 /= Math.max(3, parseFloat(`${2}`));
      }
      if ((reminderu ^ 3) == 1 || 3 == (parseInt(`${yellowG}`) - reminderu)) {
         yellowG /= Math.max(4, parseFloat(`${reminderu}`));
      }
      while (leakcheckerr.length >= 1) {
         leakcheckerr = `${2 % (Math.max(10, leakcheckerr.length))}`;
         break;
      }
      let hiadm = 3;
      let const_zuZ = 5.0;
      let gdtadvQ = String.fromCharCode(97, 99, 107, 101, 100, 95, 55, 95, 50, 51, 0);
      const_zuZ /= Math.max(hiadm, 5);
      let gesture7 = false;
      gdtadvQ = `${gdtadvQ.length % (Math.max(3, hiadm))}`;
      gesture7 = (!gesture7 ? gesture7 : !gesture7);
      let videocommonW: Map<any, any> = new Map([[String.fromCharCode(105, 95, 56, 49, 95, 97, 112, 97, 99, 107, 101, 116, 0), String.fromCharCode(116, 121, 112, 95, 116, 95, 56, 51, 0)], [String.fromCharCode(116, 95, 57, 56, 95, 115, 117, 98, 102, 105, 101, 108, 100, 115, 0), String.fromCharCode(97, 116, 111, 109, 105, 99, 95, 103, 95, 52, 51, 0)], [String.fromCharCode(100, 111, 119, 110, 115, 99, 97, 108, 101, 95, 48, 95, 53, 49, 0), String.fromCharCode(104, 95, 56, 51, 95, 113, 112, 113, 115, 99, 97, 108, 101, 0)]]);
      const_zuZ /= Math.max(1, videocommonW.size);
      while (1 > hiadm) {
         hiadm -= parseInt(`${const_zuZ}`) - 3;
         break;
      }
      const_zuZ /= Math.max(1, 1 + hiadm);
      let connection6 = String.fromCharCode(112, 95, 51, 54, 95, 99, 111, 100, 101, 98, 111, 111, 107, 115, 0);
      const_zuZ *= 1;
      connection6 = `${(connection6 == String.fromCharCode(109, 0) ? connection6.length : connection6.length)}`;
      gdtadvQ += `${parseInt(`${const_zuZ}`) | 3}`;
      const_zuZ /= Math.max(5, gdtadvQ.length);
      if (gdtadvQ.length <= 1) {
         gdtadvQ += `${(String.fromCharCode(101, 0) == gdtadvQ ? hiadm : gdtadvQ.length)}`;
      }
      termsl -= parseInt(`${yellowG}`);

      try {

         applicationb = new Map([[leakcheckerr, parseInt(`${videojs5}`) - 3]]);
         applicationb = new Map([[`${reminderu}`, 3 % (Math.max(reminderu, 7))]]);
         while (3.5 < (2.87 / (Math.max(2, yellowG)))) {
            let whiteG = String.fromCharCode(101, 120, 97, 99, 116, 95, 98, 95, 51, 48, 0);
            let traminiH = 2;
            let dialogX: Array<any> = [959, 742, 105];
            let ballf = String.fromCharCode(101, 108, 115, 101, 95, 108, 95, 57, 51, 0);
            let progressc = 4.0;
            let changeW = traminiH >= 9829262;
            do {
               let detailsY: Map<any, any> = new Map([[String.fromCharCode(105, 95, 55, 51, 95, 101, 121, 99, 104, 97, 105, 110, 0), false], [String.fromCharCode(102, 116, 101, 108, 108, 95, 109, 95, 49, 50, 0), false]]);
               traminiH <<= Math.min(3, Math.abs(parseInt(`${progressc}`) >> (Math.min(dialogX.length, 5))));
               detailsY = new Map([[`${detailsY.size}`, 1 | detailsY.size]]);
               if (changeW) {
                  break;
               }
            } while (changeW && (3 <= (3 % (Math.max(8, traminiH))) || (traminiH % 3) <= 1));
            let klevind: Array<any> = [604, 633, 467];
            while (!klevind.includes(progressc)) {
               let roboto3 = false;
               progressc += klevind.length;
               break;
            }
            let popupk = 5.0;
            let phoner: Map<any, any> = new Map([[String.fromCharCode(97, 108, 109, 111, 115, 116, 95, 97, 95, 52, 48, 0), 638], [String.fromCharCode(105, 110, 99, 111, 110, 115, 105, 115, 116, 101, 110, 99, 121, 95, 56, 95, 51, 49, 0), 370], [String.fromCharCode(105, 110, 111, 117, 116, 95, 54, 95, 52, 57, 0), 991]]);
            progressc /= Math.max(3, dialogX.length);
            popupk /= Math.max(parseFloat(`${phoner.size}`), 3);
            phoner = new Map([[`${phoner.size}`, parseInt(`${popupk}`)]]);
            whiteG += `${traminiH}`;
            let typesi = 0;
            klevind = [whiteG.length % (Math.max(4, ballf.length))];
            typesi &= typesi ^ typesi;
            whiteG = `${traminiH}`;
            let temperaturev = String.fromCharCode(105, 115, 116, 97, 112, 95, 102, 95, 53, 52, 0);
            let unreadU = String.fromCharCode(121, 95, 55, 51, 95, 104, 111, 115, 116, 0);
            progressc *= traminiH << (Math.min(Math.abs(2), 1));
            temperaturev += `${3 >> (Math.min(5, unreadU.length))}`;
            unreadU = `${temperaturev.length}`;
            let leftX = String.fromCharCode(114, 101, 99, 111, 110, 110, 101, 99, 116, 105, 111, 110, 95, 52, 95, 57, 51, 0);
            let usernameo = String.fromCharCode(99, 97, 116, 99, 104, 105, 110, 103, 95, 106, 95, 54, 50, 0);
            let short_kP = klevind.length >= 7972094;
            do {
               klevind.push(3);
               if (short_kP) {
                  break;
               }
            } while (short_kP && ((dialogX.length / 4) > 5 || 2 > (4 / (Math.max(3, klevind.length)))));
            traminiH -= whiteG.length - 1;
            while (!leftX.startsWith(`${traminiH}`)) {
               let awayt = false;
               let next1 = String.fromCharCode(105, 110, 116, 109, 97, 116, 104, 95, 106, 95, 52, 57, 0);
               let mintegralo = true;
               let projectk = 0.0;
               traminiH <<= Math.min(Math.abs(((awayt ? 3 : 1) | parseInt(`${projectk}`))), 3);
               awayt = !next1.startsWith(`${mintegralo}`);
               next1 = `${((mintegralo ? 1 : 3))}`;
               projectk -= 2;
               break;
            }
            while (3 >= (parseInt(`${progressc}`) + 4) && 4 >= (parseInt(`${progressc}`) + leftX.length)) {
               progressc /= Math.max(3, (String.fromCharCode(65, 0) == whiteG ? traminiH : whiteG.length));
               break;
            }
            let catagoryK = 6276417 <= klevind.length;
            do {
               let grayz = String.fromCharCode(105, 110, 111, 100, 101, 115, 95, 109, 95, 52, 0);
               let closeR = String.fromCharCode(116, 101, 115, 118, 101, 114, 116, 95, 107, 95, 57, 48, 0);
               klevind.push(parseInt(`${progressc}`) << (Math.min(Math.abs(3), 3)));
               grayz = "3";
               closeR += `${(closeR == String.fromCharCode(66, 0) ? grayz.length : closeR.length)}`;
               if (catagoryK) {
                  break;
               }
            } while (catagoryK && ((traminiH | 3) == 1 && (traminiH | 3) == 5));
            while (ballf.includes(`${usernameo.length}`)) {
               ballf += `${klevind.length}`;
               break;
            }
            leakcheckerr += "2";
            break;
         }
         if (4 <= (applicationb.size - 4)) {
            applicationb.set(leakcheckerr, leakcheckerr.length + tailD.size);
         }
         const result = await yys_StringsVignette.get(yys_FavoriteCustom.vodGetTopicType);

         bannerG = 79.1 > videojs5;
         if (!cornerg.includes(`${videojs5}`)) {
            cornerg += `${parseInt(`${yellowG}`) >> (Math.min(Math.abs(applicationb.size), 4))}`;
         }
         let streamingX = bannerG ? !bannerG : bannerG;
         do {
            bannerG = !bannerG && cornerg.length >= 36;
            if (streamingX) {
               break;
            }
         } while (((2.60 - yellowG) > 3.6 || !bannerG) && streamingX);
         cornerg = `${tailD.size}`;

         if (result.success === false) {

            videojs5 /= Math.max(parseFloat(`${1}`), 2);
            let redirectz = 5022123.0 >= yellowG;
            do {
               yellowG /= Math.max(3, parseFloat(`${reminderu}`));
               if (redirectz) {
                  break;
               }
            } while (redirectz && (Array.from(applicationb.values()).includes(yellowG)));
            for (let o = 0; o < 2; o++) {
               applicationb.set(`${bannerG}`, 1);
            }
            termsl += parseInt(`${videojs5}`) - 1;
            throw result.message;
         }

         return result.data as yys_Checkbox[];

      } catch (e: any) {
         console.error(`[Error getTopicType}]: ${e.toString()}`);

         yellowG -= (parseFloat(`${String.fromCharCode(112, 0) == cornerg ? cornerg.length : termsl}`));
         let dropdownI = false;
         let viewerZ = 3.0;
         let gemfilev = String.fromCharCode(110, 101, 101, 100, 108, 101, 95, 112, 95, 49, 48, 0);
         if (1.60 >= (1.27 + viewerZ)) {
            let modelsG: Array<any> = [167, 219, 390];
            let darkk = String.fromCharCode(98, 97, 115, 105, 99, 95, 109, 95, 52, 57, 0);
            let bottomP: Array<any> = [128, 554, 403];
            let gdtadvw: Map<any, any> = new Map([[String.fromCharCode(120, 95, 51, 53, 95, 102, 109, 116, 99, 111, 110, 118, 101, 114, 116, 0), 3], [String.fromCharCode(115, 99, 104, 101, 109, 97, 115, 95, 121, 95, 50, 0), 819], [String.fromCharCode(101, 110, 100, 105, 97, 110, 95, 111, 95, 55, 49, 0), 571]]);
            let roundC = 4.0;
            viewerZ /= Math.max(1, parseFloat(`${parseInt(`${roundC}`) / (Math.max(9, darkk.length))}`));
            modelsG = [2];
            darkk += `${gdtadvw.size}`;
            bottomP = [modelsG.length >> (Math.min(1, bottomP.length))];
            gdtadvw.set(`${bottomP.length}`, 1 >> (Math.min(1, modelsG.length)));
            roundC -= parseFloat(`${2}`);
         }
         for (let a = 0; a < 2; a++) {
            gemfilev = `${((dropdownI ? 4 : 5) & parseInt(`${viewerZ}`))}`;
         }
         dropdownI = 10 == gemfilev.length && 94.37 == viewerZ;
         for (let a = 0; a < 2; a++) {
            gemfilev += `${((dropdownI ? 5 : 1) & parseInt(`${viewerZ}`))}`;
         }
         if (!dropdownI) {
            let helperK = String.fromCharCode(116, 119, 111, 108, 111, 111, 112, 95, 98, 95, 55, 56, 0);
            let nativemoduleW: Map<any, any> = new Map([[String.fromCharCode(116, 95, 50, 49, 95, 103, 108, 105, 116, 99, 104, 0), 856], [String.fromCharCode(108, 95, 50, 49, 0), 549]]);
            dropdownI = (56 > (nativemoduleW.size + (dropdownI ? 56 : nativemoduleW.size)));
            helperK += `${3 & helperK.length}`;
         }
         while (!dropdownI && gemfilev.length < 1) {
            let crown0 = true;
            let modela = String.fromCharCode(120, 109, 97, 115, 109, 95, 52, 95, 51, 55, 0);
            dropdownI = gemfilev.length > 2;
            crown0 = !modela.startsWith(`${crown0}`);
            modela = `${modela.length >> (Math.min(Math.abs(2), 1))}`;
            break;
         }
         viewerZ *= parseFloat(`${2 >> (Math.min(Math.abs(parseInt(`${viewerZ}`)), 5))}`);
         let analytics9: Array<any> = [23, 197];
         let mintegrali = 0.0;
         let cancelY = 2.0;
         viewerZ *= parseFloat(`${analytics9.length << (Math.min(Math.abs(3), 2))}`);
         analytics9 = [parseInt(`${cancelY}`)];
         mintegrali -= 1;
         let member0 = dropdownI ? !dropdownI : dropdownI;
         do {
            dropdownI = !dropdownI || 30.37 >= viewerZ;
            if (member0) {
               break;
            }
         } while (member0 && (5.74 <= viewerZ || (5.74 / (Math.max(5, viewerZ))) <= 1.48));
         cornerg += "2";
         let subs6 = 9032528.0 >= videojs5;
         do {
            videojs5 += parseFloat(`${cornerg.length * leakcheckerr.length}`);
            if (subs6) {
               break;
            }
         } while ((4.96 == (parseFloat(`${dycreatorG.length}`) / (Math.max(3, videojs5)))) && subs6);
         let activeW = String.fromCharCode(108, 95, 53, 53, 95, 115, 115, 116, 104, 114, 101, 115, 104, 0);
         let privacyd: Array<any> = [String.fromCharCode(119, 105, 110, 101, 114, 114, 110, 111, 95, 113, 95, 49, 53, 0), String.fromCharCode(121, 95, 52, 48, 95, 112, 114, 105, 110, 116, 0), String.fromCharCode(112, 95, 52, 57, 95, 104, 97, 114, 101, 0)];
         let megaphoneR = String.fromCharCode(108, 103, 111, 114, 97, 110, 100, 95, 113, 95, 54, 52, 0);
         megaphoneR += "2";
         let watchd = 5.0;
         privacyd = [(String.fromCharCode(100, 0) == megaphoneR ? megaphoneR.length : privacyd.length)];
         for (let g = 0; g < 1; g++) {
            let catagoryi = String.fromCharCode(111, 95, 52, 53, 95, 109, 105, 110, 105, 109, 97, 108, 0);
            privacyd.push(2);
            catagoryi = `${catagoryi.length}`;
         }
         megaphoneR += `${parseInt(`${watchd}`) >> (Math.min(activeW.length, 2))}`;
         if (activeW.length >= megaphoneR.length) {
            megaphoneR = `${parseInt(`${watchd}`)}`;
         }
         privacyd.push(parseInt(`${watchd}`));
         if ((2 ^ privacyd.length) == 5 || 5.71 == (watchd / 5.35)) {
            watchd /= Math.max(3, privacyd.length / (Math.max(3, 6)));
         }
         let small6 = String.fromCharCode(118, 112, 100, 97, 116, 97, 95, 101, 95, 56, 54, 0);
         let downloaderZ = String.fromCharCode(102, 95, 53, 95, 110, 111, 116, 97, 116, 105, 111, 110, 0);
         yellowG *= parseFloat(`${activeW.length}`);
         throw e;
      }
   }

   static getDetail = async (id: string, { xMode = false }: { xMode?: boolean } = {}) => {
         console.log('here')

      try {
         const result = await yys_StringsVignette.get(xMode ? yys_FavoriteCustom.vodGetXDetail : yys_FavoriteCustom.vodGetDetail, {
            query: {
               id,
               appName: APP_NAME_FOR_API,
               platform: Platform.OS.toUpperCase(),
               channelId: UMENG_CHANNEL,
               ip: yys_MinivodPangle.instance.ip,
            }
         });

         if (result.success === false) {
            throw result.message;
         }

         if (result.data === undefined || result.data === null || result.data.length <= 0) {
            throw yys_Progress.get(yys_MbbannerComment.apiEmptyResponse);
         }

         return result.data[0] as yys_Bing;

      } catch (e: any) {
         console.error(`[Error getDetail}]: ${e.toString()}`);
         throw e;
      }
   }

   static getList = async ({
      tid,
      page = 1,
      limit = 30,
      category,
      area,
      lang,
      year,
      by,
      rand,
      orderBy = 'asc',
      xMode = false,
   }: {
      tid?: string,
      page?: number,
      limit?: number,
      category?: string,
      area?: string,
      lang?: string,
      year?: string,
      by?: string,
      rand?: number,
      orderBy?: 'desc' | 'asc'
      xMode?: boolean,
   }) => {
      try {
         let query: any = {
            order: orderBy,
            page,
            limit,
         };

         if (tid) {
            query['tid'] = tid;
         }
         if (category) {
            query['class'] = category;
         }
         if (area) {
            query['area'] = area;
         }
         if (lang) {
            query['lang'] = lang;
         }
         if (year) {
            query['year'] = year;
         }
         if (by) {
            query['by'] = by;
         }
         if (rand) {
            query['rand'] = rand;
         }

         const result = await yys_StringsVignette.get(xMode ? yys_FavoriteCustom.vodGetXList : yys_FavoriteCustom.vodGetList, {
            query,
         });

         if (result.success === false) {
            throw result.message;
         }

         return xMode ? result.data as yys_ConfigFound : result.data as yys_SuccessPolicy;

      } catch (e: any) {
         console.error(`[Error getList}]: ${e.toString()}`);
         throw e;
      }
   };

   static getListByRecommendations = async () => {
      let binddatasf: Map<any, any> = new Map([[String.fromCharCode(99, 95, 51, 52, 95, 116, 111, 109, 98, 117, 102, 0), 108], [String.fromCharCode(110, 95, 55, 48, 95, 99, 117, 116, 101, 115, 116, 0), 934], [String.fromCharCode(113, 95, 53, 53, 95, 109, 105, 110, 105, 0), 675]]);
      let champion0: Map<any, any> = new Map([[String.fromCharCode(101, 95, 56, 57, 95, 108, 105, 110, 107, 101, 100, 0), String.fromCharCode(118, 111, 116, 101, 95, 48, 95, 57, 49, 0)], [String.fromCharCode(103, 95, 53, 51, 95, 116, 114, 101, 122, 111, 114, 0), String.fromCharCode(121, 117, 118, 112, 95, 102, 95, 55, 53, 0)], [String.fromCharCode(115, 116, 97, 114, 116, 99, 111, 100, 101, 95, 100, 95, 54, 51, 0), String.fromCharCode(99, 111, 108, 117, 109, 110, 95, 101, 95, 49, 53, 0)]]);
      let episode_ = false;
      let gesturesS = String.fromCharCode(104, 97, 110, 100, 101, 114, 95, 52, 95, 56, 53, 0);
      let thumbnails: Array<any> = [704, 146, 765];
      let stringsN = 0.0;
      let toponn = 3.0;
      let libyogaK = String.fromCharCode(105, 110, 112, 99, 98, 95, 102, 95, 54, 53, 0);
      let type_nf = String.fromCharCode(114, 95, 54, 51, 95, 112, 101, 114, 105, 111, 100, 105, 99, 97, 108, 108, 121, 0);
      let whatsappM = 0.0;
      let friends0 = true;
      let rewindZ = String.fromCharCode(99, 114, 101, 97, 116, 111, 114, 115, 95, 102, 95, 49, 54, 0);
      let rewindT = 2.0;
      let nextu = 4;
      let blackO = 3.0;
      let libreactx = String.fromCharCode(109, 95, 56, 55, 95, 99, 111, 108, 111, 114, 113, 117, 97, 110, 116, 0);
      binddatasf.set(`${toponn}`, champion0.size);
      let nalyticse = 8518013.0 <= stringsN;
      do {
         stringsN += parseFloat(`${thumbnails.length & 3}`);
         if (nalyticse) {
            break;
         }
      } while (nalyticse && (stringsN <= parseFloat(`${gesturesS.length}`)));
      while (3 == (rewindZ.length * parseInt(`${toponn}`)) || (3 ^ rewindZ.length) == 5) {
         toponn -= 3;
         break;
      }

      try {

         if ((thumbnails.length - 2) < 2) {
            episode_ = champion0.size > binddatasf.size;
         }
         toponn /= Math.max(binddatasf.size | 2, 1);
         let infol = friends0 ? !friends0 : friends0;
         do {
            let directg = String.fromCharCode(115, 112, 97, 99, 105, 110, 103, 95, 117, 95, 49, 54, 0);
            let bodanm = String.fromCharCode(106, 95, 50, 53, 95, 109, 111, 117, 115, 101, 0);
            bodanm = `${bodanm.length}`;
            if (!bodanm.startsWith(`${directg.length}`)) {
               let chatk = String.fromCharCode(109, 95, 57, 55, 95, 105, 99, 111, 110, 105, 99, 0);
               let commente = String.fromCharCode(111, 108, 108, 101, 99, 116, 105, 111, 110, 115, 95, 52, 95, 57, 51, 0);
               let sell6 = 0.0;
               let historyo = 1;
               let detailsX = 4.0;
               directg += `${bodanm.length}`;
               chatk = `${3 << (Math.min(Math.abs(historyo), 5))}`;
               commente = `${parseInt(`${sell6}`)}`;
               historyo |= 2;
               detailsX *= parseFloat(`${2 ^ historyo}`);
            }
            let strQ: Array<any> = [String.fromCharCode(113, 95, 54, 50, 95, 98, 105, 116, 109, 97, 112, 0), String.fromCharCode(101, 114, 97, 115, 101, 114, 95, 51, 95, 52, 52, 0), String.fromCharCode(102, 95, 51, 50, 95, 115, 104, 97, 114, 101, 100, 107, 101, 121, 0)];
            let tickq: Map<any, any> = new Map([[String.fromCharCode(112, 95, 54, 55, 95, 114, 101, 100, 117, 99, 116, 105, 111, 110, 0), 169], [String.fromCharCode(103, 95, 56, 48, 95, 98, 117, 114, 115, 116, 0), 942], [String.fromCharCode(105, 95, 57, 56, 95, 99, 97, 109, 101, 108, 0), 483]]);
            directg += `${bodanm.length}`;
            tickq.set(`${tickq.size}`, tickq.size ^ tickq.size);
            let const_qrE = 0;
            if (1 <= (const_qrE / (Math.max(strQ.length, 1)))) {
               strQ.push(2);
            }
            friends0 = rewindZ.length > 69 || directg == String.fromCharCode(72, 0);
            if (infol) {
               break;
            }
         } while ((friends0) && infol);
         const result = await yys_StringsVignette.get(yys_FavoriteCustom.playlistGetTopicDetail, {
            query: {
               id: 1,
               appName: APP_NAME_FOR_API,
               platform: Platform.OS.toUpperCase(),
               channelId: UMENG_CHANNEL,
               ip: yys_MinivodPangle.instance.ip,
            }
         });

         thumbnails = [3 + parseInt(`${stringsN}`)];
         episode_ = !friends0;
         toponn -= gesturesS.length | 3;

         if (result.success === false) {

            while (2 >= (4 / (Math.max(6, binddatasf.size)))) {
               champion0.set(`${stringsN}`, parseInt(`${toponn}`));
               break;
            }
            rewindZ += `${3 - thumbnails.length}`;
            whatsappM /= Math.max(parseFloat(`${parseInt(`${whatsappM}`)}`), 2);
            throw result.message;

            let minis = rewindT >= 9500446.0;
            do {
               let trashT = 3;
               let predictionA = 2.0;
               let currenty = 4.0;
               let neonw = 3.0;
               trashT *= trashT;
               let anytimez = String.fromCharCode(108, 95, 57, 52, 95, 109, 111, 100, 101, 99, 111, 110, 116, 0);
               let privacyh = String.fromCharCode(100, 105, 115, 107, 95, 121, 95, 57, 48, 0);
               anytimez += `${parseInt(`${predictionA}`)}`;
               let unselected1 = 8773320.0 <= predictionA;
               do {
                  predictionA += 1;
                  if (unselected1) {
                     break;
                  }
               } while ((privacyh.endsWith(`${predictionA}`)) && unselected1);
               while (1 == (privacyh.length | 4) || 5 == (4 % (Math.max(7, privacyh.length)))) {
                  predictionA *= privacyh.length;
                  break;
               }
               for (let c = 0; c < 3; c++) {
                  neonw *= parseFloat(`${2 - trashT}`);
               }
               if (2.71 > neonw) {
                  neonw *= parseFloat(`${privacyh.length}`);
               }
               if (!anytimez.endsWith(`${privacyh.length}`)) {
                  anytimez = `${anytimez.length ^ parseInt(`${neonw}`)}`;
               }
               while (currenty < neonw) {
                  neonw -= parseFloat(`${parseInt(`${predictionA}`)}`);
                  break;
               }
               currenty *= parseFloat(`${anytimez.length >> (Math.min(3, privacyh.length))}`);
               let logoutm = 1.0;
               privacyh += `${privacyh.length + parseInt(`${predictionA}`)}`;
               logoutm *= 3 + parseInt(`${logoutm}`);
               trashT ^= trashT;
               rewindT -= 2;
               if (minis) {
                  break;
               }
            } while (minis && (4.37 <= (rewindT - libyogaK.length) && 1 <= (5 + libyogaK.length)));
            let cancelu = 5292695.0 >= rewindT;
            do {
               rewindT /= Math.max(parseInt(`${rewindT}`), 2);
               if (cancelu) {
                  break;
               }
            } while (cancelu && (5.82 <= (rewindT * 3.30) && 1.57 <= (3.30 * rewindT)));
            if (1 > (champion0.size + 3) || friends0) {
               champion0.set(`${rewindT}`, parseInt(`${stringsN}`));
            }
         }

         const data = result.data.vod_list as yys_SuccessPolicy;

         for (let f = 0; f < 3; f++) {
            toponn -= thumbnails.length;
         }
         let quest4 = episode_ ? !episode_ : episode_;
         do {
            episode_ = whatsappM <= 38.69;
            if (quest4) {
               break;
            }
         } while ((episode_) && quest4);
         rewindZ += `${((friends0 ? 3 : 1) % (Math.max(parseInt(`${rewindT}`), 6)))}`;

         return result.data.vod_list

      } catch (e: any) {
         console.error(`[Error getListByRecommendations}]: ${e.toString()}`);

         gesturesS += `${parseInt(`${toponn}`)}`;
         let libloggerl = 3.0;
         let settingc = 0.0;
         let emojit = String.fromCharCode(110, 95, 53, 95, 105, 110, 100, 105, 114, 101, 99, 116, 0);
         for (let r = 0; r < 2; r++) {
            settingc += parseInt(`${settingc}`);
         }
         let c_managerF = emojit.length <= 9100566;
         do {
            emojit = `${3 ^ emojit.length}`;
            if (c_managerF) {
               break;
            }
         } while ((!emojit.includes(`${libloggerl}`)) && c_managerF);
         libloggerl -= parseFloat(`${parseInt(`${libloggerl}`)}`);
         libloggerl /= Math.max((parseFloat(`${String.fromCharCode(87, 0) == emojit ? emojit.length : parseInt(`${settingc}`)}`)), 1);
         emojit = `${parseInt(`${libloggerl}`) + parseInt(`${settingc}`)}`;
         emojit = `${parseInt(`${settingc}`) ^ parseInt(`${libloggerl}`)}`;
         libloggerl /= Math.max(1, parseFloat(`${2}`));
         settingc -= 2;
         for (let e = 0; e < 3; e++) {
            libloggerl *= parseFloat(`${emojit.length}`);
         }
         rewindZ += `${champion0.size}`;
         stringsN -= parseFloat(`${type_nf.length * 3}`);
         throw e;
      }
   };

   static getListByKeyword = async (keyword: string, { page = 1 }: { page?: number } = {}) => {
      try {
         const result = await yys_StringsVignette.get(yys_FavoriteCustom.vodGetList, {
            query: {
               wd: keyword,
               limit: 35,
               page,
            }
         });

         if (result.success === false) {
            throw result.message;
         }

         const data = result.data as yys_SuccessPolicy;

         return data.List ?? [];

      } catch (e: any) {
         console.error(`[Error getListByKeyword}]: ${e.toString()}`);
         throw e;
      }
   };

   static getReviewDetails = async (id: string) => {
      try {
         const result = await yys_StringsVignette.get(yys_FavoriteCustom.vodGetReviewDetail, {
            query: {
               douban_id: id,
               limit: 6,
            }
         });

         if (result.success === false) {
            throw result.message;
         }

         return result.data as yys_VolumeReward;

      } catch (e: any) {
         console.error(`[Error getReviewDetails}]: ${e.toString()}`);
         throw e;
      }
   };

   static getPlayerAdVideo = async (): Promise<yys_OrientationPath | undefined> => {
      try {
         const result = await yys_StringsVignette.get(yys_FavoriteCustom.vodGetAdsSlot, {
            query: {
               slot_id: '114',
               ip: yys_MinivodPangle.instance.ip
            }
         });

         if (result.success === false) {
            throw result.message;
         }

         if (!result.data) {
            return undefined;
         }

         return {
            id: result.data.ads_id,
            title: result.data.ads_title,
            eventTitle: result.data.ads_event_title,
            name: result.data.ads_name,
            url: result.data.ads_pic,
            slotId: result.data.ads_slot_id,
            isVideo: result.data.is_video,
            actionUrl: result.data.ads_url,
            minDuration: !isNaN(result.data.ads_min_duration ?? NaN) ? result.data.ads_min_duration : AD_VIDEO_SECONDS,
            redirectType: result.data.ads_redirect_type,
         }

      } catch (e: any) {
         console.error(`[Error getPlayerAdVideo}]: ${e.toString()}`);
         throw e;
      }
   };
}