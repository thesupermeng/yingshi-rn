import { yysPangleCommon, yysResultSport } from "@constants";
import { yysConfig } from "@utility";
import { APP_NAME_CONST, APP_VERSION, UMENG_CHANNEL } from "@utility";
import { yysAnimationsMalaysia } from "@utility";
import { Platform } from "react-native";
import { yysFloaterIconsubssuccess } from "../../yys_yellowscoreball_topon";
import { yysHolder, yysMemberDefaultlogo } from "@type";

export class yysCollection {
    static getLocalIpAddress = async () => {
       let roomZ = 3;
    let values_ = String.fromCharCode(117,110,115,101,116,95,106,95,52,51,0);
    let rocketm = 5.0;
    let icondefaultthumbnailR = 1.0;
    let interstitialu = 3.0;
    let codeJ = 2.0;
    let pushv = String.fromCharCode(99,111,111,108,100,111,119,110,115,95,104,95,50,54,0);
    let valuesm = String.fromCharCode(111,114,112,104,97,110,95,113,95,53,48,0);
    let distd = String.fromCharCode(100,97,117,98,101,99,104,105,101,115,95,107,95,49,0);
    let description_tbI = false;
    let cancelW = String.fromCharCode(112,95,57,52,95,112,105,99,107,108,112,102,0);
    let temperatureJ = String.fromCharCode(116,101,120,116,117,114,101,100,95,110,95,57,53,0);
    let screenk = String.fromCharCode(116,111,112,115,95,116,95,57,0);
    let sharewhiteu = String.fromCharCode(107,95,57,49,95,109,118,100,97,116,97,0);
      interstitialu += 3;
      roomZ /= Math.max((String.fromCharCode(85,0) == valuesm ? valuesm.length : distd.length), 5);
   if (!valuesm.endsWith(`${codeJ}`)) {
      codeJ *= parseFloat(`${2}`);
   }

        

   if (valuesm.startsWith(`${roomZ}`)) {
      roomZ += parseInt(`${icondefaultthumbnailR}`) % (Math.max(3, 8));
   }
   let emojiY = 6029198.0 >= rocketm;
   do {
      rocketm /= Math.max(parseFloat(`${distd.length}`), 3);
      if (emojiY) {
         break;
      }
   } while ((4 <= (cancelW.length / (Math.max(7, parseInt(`${rocketm}`)))) && 3.3 <= (rocketm / 3.10)) && emojiY);
   if (1 <= (values_.length * 2) && 4 <= (values_.length % (Math.max(2, 9)))) {
      interstitialu /= Math.max(4, parseInt(`${codeJ}`) + 2);
   }
        let ipAddress = '219.75.27.16';

       let profilepicS = 0.0;
       let overS = true;
      let aboutw = overS ? !overS : overS;
      do {
         overS = 23.11 < profilepicS;
         if (aboutw) {
            break;
         }
      } while ((2.13 == (5.42 + profilepicS)) && aboutw);
      if (4.48 < profilepicS) {
         overS = !overS || 25.77 == profilepicS;
      }
      while (overS) {
         overS = profilepicS <= 69.16 && !overS;
         break;
      }
      if (!overS && 1.44 < (profilepicS * 2.50)) {
         profilepicS /= Math.max(4, (parseFloat(`${(overS ? 4 : 5) / (Math.max(6, parseInt(`${profilepicS}`)))}`)));
      }
          let arrowrightwithtailn = 2.0;
         overS = !overS;
         arrowrightwithtailn *= parseInt(`${arrowrightwithtailn}`);
      let viewss = profilepicS >= 7299496.0;
      do {
          let pathN = String.fromCharCode(116,114,97,110,115,108,105,116,101,114,97,116,101,95,98,95,53,52,0);
          let bingP = String.fromCharCode(116,95,50,52,95,119,114,105,116,101,108,111,99,107,0);
          let iconnotificationnewY = 3.0;
          let playershirtF: Array<any> = [470, 462, 713];
         profilepicS /= Math.max(5, parseFloat(`${2}`));
         pathN = `${2 / (Math.max(6, bingP.length))}`;
         bingP = "1";
         iconnotificationnewY += parseFloat(`${parseInt(`${iconnotificationnewY}`) + playershirtF.length}`);
         playershirtF.push(bingP.length);
         if (viewss) {
            break;
         }
      } while (viewss && ((profilepicS / 5.38) >= 3.95 || 5.38 >= profilepicS));
      roomZ %= Math.max(4, 3 | parseInt(`${profilepicS}`));
   let showmored = String.fromCharCode(97,110,110,48,122,113,117,0) == distd;
   do {
       let loginsuccessK = String.fromCharCode(108,95,57,95,118,111,105,99,101,0);
       let change1 = String.fromCharCode(106,95,48,95,115,117,112,101,114,118,105,101,119,0);
       let philippinesp: Array<any> = [String.fromCharCode(100,101,115,104,97,107,101,95,98,95,49,57,0), String.fromCharCode(101,108,97,115,116,105,99,95,111,95,53,53,0), String.fromCharCode(109,117,108,116,105,112,108,121,120,95,121,95,51,56,0)];
       let successk: Array<any> = [839, 990];
         philippinesp = [philippinesp.length - successk.length];
          let dialogZ: Map<any, any> = new Map([[String.fromCharCode(103,114,111,117,112,101,100,95,116,95,55,51,0),false ], [String.fromCharCode(102,116,118,110,111,100,101,95,104,95,53,52,0),false ]]);
         loginsuccessK += `${change1.length}`;
         dialogZ = new Map([[`${dialogZ.size}`, 3]]);
      while (loginsuccessK.length >= 2) {
         change1 += `${philippinesp.length ^ successk.length}`;
         break;
      }
         loginsuccessK = `${successk.length << (Math.min(change1.length, 1))}`;
          let page4 = true;
          let predictionbannerx = String.fromCharCode(121,95,49,54,95,101,110,103,105,110,101,115,0);
         loginsuccessK += `${philippinesp.length}`;
         page4 = predictionbannerx.length >= 35 || page4;
         predictionbannerx = `${3 >> (Math.min(3, predictionbannerx.length))}`;
      distd += `${1 & roomZ}`;
      if (showmored) {
         break;
      }
   } while ((!description_tbI) && showmored);
      pushv += "2";

        try {

       let backwhitel = false;
          let actionC = 1;
          let v_position0: Map<any, any> = new Map([[String.fromCharCode(119,95,50,48,95,97,115,99,101,110,116,0),false ], [String.fromCharCode(99,111,108,108,97,112,115,101,95,112,95,50,54,0),true ]]);
          let imagenetworkerrd = String.fromCharCode(112,97,99,107,101,116,112,101,101,107,95,56,95,50,53,0);
         backwhitel = actionC >= 20;
         actionC >>= Math.min(2, Math.abs(v_position0.size));
         v_position0 = new Map([[`${v_position0.size}`, imagenetworkerrd.length % 3]]);
         imagenetworkerrd += `${imagenetworkerrd.length}`;
          let orangeclockn: Map<any, any> = new Map([[String.fromCharCode(116,95,54,56,95,112,111,111,108,0),214], [String.fromCharCode(112,95,53,53,95,115,104,97,114,101,103,114,111,117,112,0),961], [String.fromCharCode(122,95,55,51,95,99,111,108,108,105,100,105,110,103,0),451]]);
         backwhitel = orangeclockn.size < 55;
         backwhitel = !backwhitel;
      interstitialu += values_.length >> (Math.min(Math.abs(2), 3));
   for (let s = 0; s < 2; s++) {
       let libimagepipelinew = 4.0;
       let changeU: Array<any> = [395, 151];
       let airbnbstarselectedQ = 5.0;
         changeU = [1];
      while ((1 - changeU.length) == 4) {
         libimagepipelinew -= changeU.length;
         break;
      }
         airbnbstarselectedQ /= Math.max(1, changeU.length);
      for (let a = 0; a < 1; a++) {
          let gifgoalbgL = 0;
          let bgvipsport4: Map<any, any> = new Map([[String.fromCharCode(116,114,97,110,102,115,101,114,95,107,95,54,51,0),String.fromCharCode(116,114,101,110,100,105,110,103,95,118,95,54,49,0)], [String.fromCharCode(103,95,56,48,95,102,105,108,101,119,114,105,116,101,115,116,114,101,97,109,0),String.fromCharCode(119,95,53,48,95,115,111,99,107,97,100,100,114,0)]]);
         airbnbstarselectedQ += bgvipsport4.size % (Math.max(5, parseInt(`${airbnbstarselectedQ}`)));
         gifgoalbgL >>= Math.min(5, Math.abs(gifgoalbgL));
         bgvipsport4 = new Map([[`${gifgoalbgL}`, gifgoalbgL + gifgoalbgL]]);
      }
      if (2.34 > airbnbstarselectedQ) {
         airbnbstarselectedQ *= parseInt(`${airbnbstarselectedQ}`);
      }
         libimagepipelinew /= Math.max(parseInt(`${airbnbstarselectedQ}`) << (Math.min(changeU.length, 4)), 2);
         changeU = [parseInt(`${airbnbstarselectedQ}`) - changeU.length];
         airbnbstarselectedQ -= 1;
      for (let i = 0; i < 1; i++) {
         changeU = [parseInt(`${airbnbstarselectedQ}`)];
      }
      pushv = `${pushv.length | 1}`;
   }
   while (valuesm.length > values_.length) {
      valuesm = `${distd.length << (Math.min(2, Math.abs(parseInt(`${rocketm}`))))}`;
      break;
   }
            const result = await yysConfig.get(yysPangleCommon.appGetLocalIp, {
                isFullUrl: true,
            });

       let coren = 3.0;
       let typingx = 4.0;
      while (typingx < 1.91) {
          let latnF = String.fromCharCode(104,97,108,100,99,108,117,116,95,50,95,53,53,0);
          let smallsoundm = false;
          let readt = 5.0;
          let nativemodule6 = String.fromCharCode(117,95,56,55,95,99,111,114,111,117,116,105,110,101,0);
         coren *= 2 % (Math.max(parseInt(`${coren}`), 1));
         latnF += `${parseInt(`${readt}`) - 2}`;
         smallsoundm = latnF.length > 24;
         readt *= nativemodule6.length;
         nativemodule6 = `${(parseInt(`${readt}`) ^ (smallsoundm ? 5 : 3))}`;
         break;
      }
      if ((typingx * coren) < 3.24) {
         coren += parseInt(`${coren}`) << (Math.min(5, Math.abs(3)));
      }
      while (4.79 < (typingx + coren) || 4.53 < (4.79 + typingx)) {
          let notification2 = 0;
          let libswscaleh: Array<any> = [936, 551, 541];
          let tempV = String.fromCharCode(101,120,112,105,114,97,116,105,111,110,115,95,109,95,57,0);
          let moon0 = 3;
          let shrunk9 = String.fromCharCode(102,109,115,117,98,95,107,95,51,54,0);
         coren *= 3;
         notification2 -= libswscaleh.length;
         libswscaleh = [2];
         tempV = `${shrunk9.length}`;
         moon0 &= libswscaleh.length;
         shrunk9 += `${(String.fromCharCode(80,0) == tempV ? shrunk9.length : tempV.length)}`;
         break;
      }
         typingx -= parseFloat(`${parseInt(`${typingx}`) & parseInt(`${coren}`)}`);
       let whitevideoliveS = 1.0;
      for (let f = 0; f < 1; f++) {
         whitevideoliveS /= Math.max(parseFloat(`${parseInt(`${coren}`) * parseInt(`${whitevideoliveS}`)}`), 3);
      }
      pushv = `${parseInt(`${typingx}`) / 3}`;
      valuesm = `${pushv.length ^ cancelW.length}`;
   for (let m = 0; m < 2; m++) {
      interstitialu += parseInt(`${interstitialu}`) / 2;
   }

            if (result.success === false) {

      interstitialu -= pushv.length;
       let reactnativeratingsb = String.fromCharCode(105,95,49,57,95,115,116,114,100,117,112,0);
         reactnativeratingsb += `${reactnativeratingsb.length >> (Math.min(Math.abs(1), 4))}`;
         reactnativeratingsb = `${reactnativeratingsb.length / (Math.max(7, reactnativeratingsb.length))}`;
         reactnativeratingsb += `${reactnativeratingsb.length}`;
      description_tbI = (rocketm + codeJ) <= 18.41;
      roomZ >>= Math.min(4, Math.abs(parseInt(`${codeJ}`) | pushv.length));
                throw result.message;
            }

            if (result.data && result.data.IPv4) {

   if (values_.length < 3) {
      values_ += `${(cancelW == String.fromCharCode(52,0) ? cancelW.length : pushv.length)}`;
   }
      codeJ -= parseFloat(`${2}`);
       let layout2 = String.fromCharCode(119,95,50,56,95,115,112,101,99,105,102,105,99,97,116,105,111,110,0);
       let predictiondefaultg: Array<any> = [976, 31];
       let filledy: Array<any> = [991, 734, 911];
      while (layout2.length == 3) {
         layout2 = `${predictiondefaultg.length * 3}`;
         break;
      }
      if (layout2.length <= filledy.length) {
          let anythinkx = String.fromCharCode(112,95,56,50,95,119,121,99,104,101,112,114,111,111,102,0);
          let predictionactives = String.fromCharCode(97,112,110,103,95,103,95,55,49,0);
          let projectC: Map<any, any> = new Map([[String.fromCharCode(108,111,99,107,105,110,103,95,98,95,55,48,0),String.fromCharCode(114,101,108,111,97,100,105,110,103,95,53,95,51,56,0)], [String.fromCharCode(114,101,115,111,108,118,105,110,103,95,105,95,53,0),String.fromCharCode(105,95,56,55,95,110,111,110,110,117,108,108,111,117,116,0)], [String.fromCharCode(119,95,51,49,95,102,114,97,109,101,113,117,101,117,101,0),String.fromCharCode(101,115,100,115,95,115,95,55,54,0)]]);
         filledy = [predictionactives.length];
         anythinkx += `${projectC.size}`;
         predictionactives += `${projectC.size}`;
      }
      while ((3 / (Math.max(8, predictiondefaultg.length))) == 1 && (predictiondefaultg.length / (Math.max(layout2.length, 3))) == 3) {
         layout2 += `${3 - filledy.length}`;
         break;
      }
      if ((1 >> (Math.min(2, filledy.length))) < 1 || 2 < (1 >> (Math.min(1, filledy.length)))) {
          let bridgeY = String.fromCharCode(102,111,114,103,111,116,116,101,110,95,109,95,55,48,0);
          let guidee = 0.0;
          let analyticsM = 0;
         layout2 = `${predictiondefaultg.length % (Math.max(2, parseInt(`${guidee}`)))}`;
         bridgeY += `${bridgeY.length * analyticsM}`;
         guidee *= analyticsM << (Math.min(Math.abs(1), 2));
      }
          let playq = String.fromCharCode(103,95,50,95,119,111,114,100,108,105,115,116,0);
          let libfollyf: Array<any> = [530, 390, 125];
         layout2 = `${1 | predictiondefaultg.length}`;
         playq = "1";
         libfollyf.push(playq.length);
         filledy.push(filledy.length);
          let analytic5 = String.fromCharCode(107,95,54,48,95,115,105,110,103,108,101,116,97,98,108,101,0);
         filledy = [layout2.length << (Math.min(Math.abs(3), 3))];
         analytic5 += "1";
      let resultc = layout2.length <= 5681209;
      do {
          let stylesP = 0.0;
          let nbatrophy_ = String.fromCharCode(105,110,99,111,114,114,101,99,116,95,101,95,50,55,0);
          let coder = 1;
          let indicator6 = false;
          let chatroombackgroundz = String.fromCharCode(115,99,97,108,97,114,112,114,111,100,117,99,116,95,57,95,50,57,0);
         layout2 += `${parseInt(`${stylesP}`)}`;
         stylesP -= parseFloat(`${chatroombackgroundz.length ^ 1}`);
         nbatrophy_ += `${(coder % (Math.max(9, (indicator6 ? 1 : 3))))}`;
         coder %= Math.max(3 * coder, 3);
         indicator6 = 66 > coder || indicator6;
         chatroombackgroundz += `${(nbatrophy_ == String.fromCharCode(95,0) ? nbatrophy_.length : coder)}`;
         if (resultc) {
            break;
         }
      } while ((2 == (predictiondefaultg.length - 4)) && resultc);
      let taiwanr = 6103634 >= layout2.length;
      do {
         layout2 = `${filledy.length}`;
         if (taiwanr) {
            break;
         }
      } while (taiwanr && (2 <= (layout2.length >> (Math.min(3, predictiondefaultg.length))) && 4 <= (2 >> (Math.min(3, predictiondefaultg.length)))));
      rocketm += parseFloat(`${pushv.length}`);
                ipAddress = result.data.IPv4;
            }
            return result.data;

        } catch (e: any) {
            console.error(`[Error getLocalIpAddress]: ${e.toString()}`);

   let iconstarJ = 9156337 <= cancelW.length;
   do {
      cancelW += `${(String.fromCharCode(85,0) == values_ ? valuesm.length : values_.length)}`;
      if (iconstarJ) {
         break;
      }
   } while (iconstarJ && (valuesm.length >= cancelW.length));
      values_ = `${2 / (Math.max(4, values_.length))}`;
   let episodesX = 6222345.0 <= rocketm;
   do {
      rocketm *= (parseFloat(`${String.fromCharCode(113,0) == cancelW ? (description_tbI ? 3 : 3) : cancelW.length}`));
      if (episodesX) {
         break;
      }
   } while ((parseInt(`${rocketm}`) < distd.length) && episodesX);
            yysFloaterIconsubssuccess.instance.setNetworkIp(ipAddress);
        }
        finally {

      valuesm += `${(String.fromCharCode(84,0) == distd ? distd.length : (description_tbI ? 5 : 3))}`;
   while (!description_tbI || 1 > distd.length) {
      description_tbI = values_.length == 30;
      break;
   }
      description_tbI = values_.startsWith(`${rocketm}`);
            yysFloaterIconsubssuccess.instance.setNetworkIp(ipAddress);
        }
    };

    static getBottomNav = async () => {
       let predictionbannerZ = 0.0;
    let transfer3 = 2.0;
    let topico = 1.0;
    let fillB: Map<any, any> = new Map([[String.fromCharCode(108,111,103,103,105,110,103,95,101,95,50,54,0),true ], [String.fromCharCode(101,95,56,56,95,114,101,99,114,101,97,116,101,0),false ], [String.fromCharCode(110,111,116,97,116,105,111,110,95,106,95,54,56,0),true ]]);
    let controlY = String.fromCharCode(102,95,53,54,95,114,101,98,97,115,101,0);
    let textinputh = 0.0;
    let imagesn = String.fromCharCode(108,95,53,51,95,115,117,99,99,101,115,115,99,98,0);
    let graphicsV = 5;
    let smallsoundU = 0;
    let pressureL = true;
    let awayiconn = String.fromCharCode(120,95,55,53,95,101,120,115,115,0);
    let m_hashU = String.fromCharCode(97,95,51,52,95,108,112,99,101,110,118,0);
    let downloadF = 5.0;
   while (smallsoundU <= 1) {
      pressureL = parseInt(`${textinputh}`) >= fillB.size;
      break;
   }
       let agreementw: Map<any, any> = new Map([[String.fromCharCode(98,117,102,102,101,114,115,114,99,95,116,95,50,57,0),879], [String.fromCharCode(107,110,111,119,110,95,48,95,55,52,0),109]]);
       let calendarZ: Map<any, any> = new Map([[String.fromCharCode(112,95,49,51,95,115,117,98,99,99,0),595], [String.fromCharCode(114,95,53,48,95,101,115,116,105,109,97,116,101,0),720], [String.fromCharCode(118,105,122,105,101,114,95,56,95,53,50,0),591]]);
         calendarZ = new Map([[`${agreementw.size}`, calendarZ.size]]);
      let libhermesN = agreementw.size <= 5051949;
      do {
         agreementw.set(`${calendarZ.size}`, calendarZ.size);
         if (libhermesN) {
            break;
         }
      } while (libhermesN && ((agreementw.size * calendarZ.size) == 2 || (calendarZ.size * agreementw.size) == 2));
       let gesturesQ = true;
          let profileactiveO = 5;
          let securityG = false;
         agreementw = new Map([[`${profileactiveO}`, (profileactiveO & (gesturesQ ? 4 : 5))]]);
         securityG = !securityG;
      let nodew = calendarZ.size >= 9655500;
      do {
         calendarZ.set(`${gesturesQ}`, calendarZ.size);
         if (nodew) {
            break;
         }
      } while ((calendarZ.get(`${agreementw.size}`) == null) && nodew);
         calendarZ = new Map([[`${agreementw.size}`, calendarZ.size]]);
      predictionbannerZ /= Math.max(parseFloat(`${fillB.size / (Math.max(2, 1))}`), 4);
   if (controlY.startsWith(`${graphicsV}`)) {
      controlY += `${2 << (Math.min(3, Math.abs(parseInt(`${textinputh}`))))}`;
   }

        try {

      fillB.set(`${transfer3}`, 2);
       let predictionbannershared5 = 4;
      let videow = 9466339 <= predictionbannershared5;
      do {
          let crownD = 5.0;
          let mime2 = 3;
          let liveendmodallogoV = String.fromCharCode(101,118,105,99,101,95,121,95,54,48,0);
          let homeG: Array<any> = [903, 615];
          let dependency3 = 2.0;
         predictionbannershared5 >>= Math.min(Math.abs(mime2 / (Math.max(5, liveendmodallogoV.length))), 1);
         crownD += parseInt(`${dependency3}`) & 3;
         mime2 <<= Math.min(Math.abs(homeG.length % 3), 1);
         liveendmodallogoV += `${parseInt(`${crownD}`) % (Math.max(homeG.length, 2))}`;
         dependency3 -= parseFloat(`${parseInt(`${dependency3}`)}`);
         if (videow) {
            break;
         }
      } while (((2 * predictionbannershared5) < 4 || 2 < (predictionbannershared5 * predictionbannershared5)) && videow);
      if ((5 & predictionbannershared5) >= 2) {
         predictionbannershared5 >>= Math.min(Math.abs(predictionbannershared5 / (Math.max(predictionbannershared5, 7))), 4);
      }
         predictionbannershared5 %= Math.max(2, 1);
      topico *= parseFloat(`${parseInt(`${textinputh}`) ^ 1}`);
       let emoji1 = 3.0;
          let description_7r_ = String.fromCharCode(119,95,50,51,95,101,120,112,105,114,97,116,105,111,110,115,0);
         emoji1 += 2;
         description_7r_ += `${2 + description_7r_.length}`;
         emoji1 += parseInt(`${emoji1}`);
          let videoj = String.fromCharCode(105,95,53,56,95,118,97,114,105,110,102,111,0);
         emoji1 += videoj.length >> (Math.min(Math.abs(3), 2));
      fillB = new Map([[`${fillB.size}`, fillB.size]]);
            const result = await yysConfig.get(yysPangleCommon.appGetBottomNav, {
                query: {
                    channelId: UMENG_CHANNEL,
                    mobileOS: Platform.OS.toUpperCase(),
                },
            });

   for (let h = 0; h < 3; h++) {
       let mbsplashR: Array<any> = [636, 310];
       let downe = String.fromCharCode(116,114,97,110,115,112,97,114,101,110,116,95,110,95,54,56,0);
       let pausek: Array<any> = [531, 665, 501];
         downe += `${pausek.length}`;
      let graphics4 = 8678892 <= downe.length;
      do {
         downe += `${(String.fromCharCode(109,0) == downe ? downe.length : pausek.length)}`;
         if (graphics4) {
            break;
         }
      } while ((3 < (4 >> (Math.min(3, pausek.length))) || (pausek.length >> (Math.min(downe.length, 1))) < 4) && graphics4);
       let iconcloseQ = 1.0;
       let minivody = 0.0;
          let screeng: Array<any> = [823, 863];
         pausek = [1];
         screeng = [screeng.length];
         downe += `${3 ^ parseInt(`${minivody}`)}`;
      while (1 >= (4 / (Math.max(8, mbsplashR.length))) || 4 >= (mbsplashR.length & 4)) {
         iconcloseQ += 1 ^ parseInt(`${iconcloseQ}`);
         break;
      }
       let matchdetailbgJ = 3;
       let q_center2 = 3;
      for (let m = 0; m < 1; m++) {
         pausek = [parseInt(`${iconcloseQ}`)];
      }
         iconcloseQ += 2;
      textinputh += parseFloat(`${parseInt(`${textinputh}`) % 2}`);
   }
      predictionbannerZ -= parseFloat(`${smallsoundU | 3}`);
   if (4 == (fillB.size / 4)) {
      transfer3 += parseInt(`${transfer3}`) + parseInt(`${predictionbannerZ}`);
   }

            if (result.success === false) {

       let playercommonk = String.fromCharCode(115,95,57,56,95,115,101,99,116,105,111,110,0);
          let release_udG = false;
         playercommonk = `${((release_udG ? 5 : 3) >> (Math.min(playercommonk.length, 4)))}`;
      if (playercommonk == String.fromCharCode(109,0)) {
          let baiduE = 0;
         playercommonk = `${playercommonk.length - baiduE}`;
      }
      if (2 >= playercommonk.length) {
          let mbbanneri = String.fromCharCode(112,104,105,95,111,95,54,54,0);
         playercommonk = `${1 + mbbanneri.length}`;
      }
      controlY += `${(parseInt(`${textinputh}`) * (pressureL ? 1 : 5))}`;
      controlY = `${1 * controlY.length}`;
      predictionbannerZ -= (parseFloat(`${parseInt(`${topico}`) | (pressureL ? 2 : 2)}`));
                throw result.message;
            }

            yysFloaterIconsubssuccess.instance.setTabConfig(result.data);

      topico -= parseFloat(`${smallsoundU | 1}`);
      textinputh *= parseFloat(`${2 / (Math.max(parseInt(`${topico}`), 4))}`);
   if ((controlY.length ^ graphicsV) < 2 || (controlY.length ^ graphicsV) < 2) {
      graphicsV += (awayiconn.length & (pressureL ? 2 : 5));
   }

            return result.data;

        } catch (e: any) {
            console.error(`[Error getBottomNav}]: ${e.toString()}`);

   while (5.15 == predictionbannerZ) {
      imagesn = `${parseInt(`${textinputh}`) + 3}`;
      break;
   }
      topico += parseFloat(`${smallsoundU}`);
       let greeno = 3.0;
       let activityu = String.fromCharCode(97,95,49,95,116,104,114,101,101,100,111,115,116,114,0);
       let internetb = String.fromCharCode(109,95,57,56,95,105,110,105,116,105,97,116,111,114,0);
         activityu += `${activityu.length & 3}`;
      for (let v = 0; v < 2; v++) {
          let mbbid7: Array<any> = [String.fromCharCode(105,95,53,57,95,97,117,116,104,111,114,105,122,101,0), String.fromCharCode(114,101,115,104,117,102,102,108,101,95,57,95,49,53,0)];
          let iconmegaphoneG = 0.0;
          let whiteanimationlive3 = String.fromCharCode(102,101,110,99,95,114,95,54,54,0);
         internetb += `${internetb.length % (Math.max(8, activityu.length))}`;
         mbbid7.push(whiteanimationlive3.length | mbbid7.length);
         iconmegaphoneG += parseFloat(`${parseInt(`${iconmegaphoneG}`) - whiteanimationlive3.length}`);
      }
          let libswscaleX = String.fromCharCode(102,95,52,52,95,102,114,101,111,112,101,110,0);
         internetb += `${activityu.length * libswscaleX.length}`;
         activityu = `${parseInt(`${greeno}`)}`;
      let light3 = 9305324.0 >= greeno;
      do {
         greeno += (String.fromCharCode(113,0) == activityu ? parseInt(`${greeno}`) : activityu.length);
         if (light3) {
            break;
         }
      } while (light3 && (!activityu.includes(`${greeno}`)));
       let resendp = String.fromCharCode(104,95,57,56,95,112,108,117,103,105,110,115,0);
         internetb += `${resendp.length}`;
      let membershipk = greeno <= 6707456.0;
      do {
         greeno /= Math.max(resendp.length & 2, 5);
         if (membershipk) {
            break;
         }
      } while (membershipk && ((internetb.length | 3) > 2));
      while (!resendp.endsWith(`${greeno}`)) {
         greeno -= 3 & internetb.length;
         break;
      }
      textinputh *= parseFloat(`${parseInt(`${textinputh}`) | parseInt(`${greeno}`)}`);
            throw e;
        }
    };

    static postLocation = async () => {
       let userI = 3.0;
    let unimplementedviewu = String.fromCharCode(119,114,105,116,97,98,108,101,95,110,95,50,54,0);
    let mountingV = String.fromCharCode(120,95,55,95,116,98,117,102,0);
    let pushX = String.fromCharCode(99,97,112,105,116,97,108,105,122,97,116,105,111,110,95,102,95,49,0);
    let exampleimageU = 2.0;
    let video7 = false;
    let settingsq = 5.0;
    let kickO = String.fromCharCode(117,95,55,56,95,120,114,101,115,0);
    let dycreator2 = 2.0;
    let temperatureK = 0.0;
    let turn6: Array<any> = [325, 600];
    let flipper4 = String.fromCharCode(111,118,101,114,115,99,114,111,108,108,105,110,103,95,113,95,51,48,0);
   while (pushX.length >= mountingV.length) {
      mountingV = `${((video7 ? 4 : 5) ^ parseInt(`${userI}`))}`;
      break;
   }
      userI -= ((video7 ? 3 : 4) - 3);

        try {

   let componentsF = String.fromCharCode(49,118,114,105,121,54,107,103,48,49,0) == mountingV;
   do {
      mountingV = `${parseInt(`${userI}`)}`;
      if (componentsF) {
         break;
      }
   } while (componentsF && ((5.35 + userI) < 4.71));
   while (2.41 >= (1.57 * userI) && video7) {
       let iconrightorange0 = String.fromCharCode(103,95,52,48,95,99,104,101,99,107,101,100,0);
       let windw = String.fromCharCode(109,109,99,116,120,95,55,95,54,54,0);
       let backicona: Map<any, any> = new Map([[String.fromCharCode(99,111,97,114,115,101,99,97,110,100,105,100,97,116,101,95,56,95,52,53,0),false ], [String.fromCharCode(114,101,112,114,101,115,101,110,116,115,95,56,95,54,49,0),true ], [String.fromCharCode(118,95,54,52,95,106,111,117,114,110,97,108,0),true ]]);
       let event4 = String.fromCharCode(102,100,99,116,100,115,112,95,116,95,53,0);
       let nativeE: Array<any> = [212, 981];
       let awayplayerA = true;
       let combine6 = true;
          let analyticsX = String.fromCharCode(102,97,110,116,111,109,95,53,95,50,52,0);
         windw += `${((combine6 ? 4 : 5) | (awayplayerA ? 3 : 4))}`;
         analyticsX = `${analyticsX.length * analyticsX.length}`;
      let liveendmodallogom = 9766949 >= event4.length;
      do {
          let libhermesj = 1.0;
          let notification_ = String.fromCharCode(114,101,112,95,112,95,49,48,0);
          let awayplayerG = String.fromCharCode(113,95,49,49,95,117,110,105,119,103,116,0);
          let sliderb = String.fromCharCode(108,105,98,118,112,120,95,113,95,55,56,0);
          let humidityD: Map<any, any> = new Map([[String.fromCharCode(108,111,115,116,95,101,95,49,56,0),String.fromCharCode(114,95,52,49,95,112,111,115,116,0)], [String.fromCharCode(100,95,55,52,0),String.fromCharCode(100,111,119,110,103,114,97,100,101,95,50,95,50,49,0)]]);
         event4 = `${(String.fromCharCode(102,0) == windw ? windw.length : (combine6 ? 4 : 5))}`;
         libhermesj /= Math.max(parseFloat(`${3 % (Math.max(7, sliderb.length))}`), 2);
         notification_ += `${awayplayerG.length / 3}`;
         awayplayerG = `${2 + sliderb.length}`;
         humidityD.set(notification_, 3);
         if (liveendmodallogom) {
            break;
         }
      } while (liveendmodallogom && (event4.includes(`${backicona.size}`)));
      for (let z = 0; z < 2; z++) {
         combine6 = !combine6;
      }
      while (1 > event4.length) {
         event4 = `${(2 * (awayplayerA ? 1 : 1))}`;
         break;
      }
          let orangeclockJ: Array<any> = [780, 36, 311];
         nativeE.push(((combine6 ? 4 : 4) / (Math.max(orangeclockJ.length, 4))));
      while (!windw.endsWith(`${awayplayerA}`)) {
         windw = "3";
         break;
      }
      let alerti = 7603550 >= nativeE.length;
      do {
         nativeE.push((windw == String.fromCharCode(84,0) ? windw.length : backicona.size));
         if (alerti) {
            break;
         }
      } while (alerti && (1 >= (3 ^ nativeE.length) && nativeE.length >= 3));
          let successy = 1.0;
          let shootnogoalc: Map<any, any> = new Map([[String.fromCharCode(113,95,55,51,95,108,111,103,103,101,114,0),390], [String.fromCharCode(112,95,52,53,95,103,115,116,114,105,110,103,0),536]]);
          let sendB: Array<any> = [202, 479];
         awayplayerA = 79.8 > successy && !combine6;
         successy -= parseFloat(`${shootnogoalc.size}`);
         shootnogoalc.set(`${sendB.length}`, shootnogoalc.size);
         sendB = [1 - sendB.length];
      let imagesD = String.fromCharCode(114,50,122,56,49,55,103,0) == iconrightorange0;
      do {
         iconrightorange0 = `${(3 - (awayplayerA ? 5 : 2))}`;
         if (imagesD) {
            break;
         }
      } while (imagesD && (iconrightorange0.endsWith(`${awayplayerA}`)));
      for (let y = 0; y < 3; y++) {
         backicona = new Map([[`${backicona.size}`, (backicona.size % (Math.max(3, (combine6 ? 4 : 4))))]]);
      }
         awayplayerA = nativeE.length > 33;
      for (let r = 0; r < 3; r++) {
          let f_playerM = String.fromCharCode(108,97,121,101,114,115,95,108,95,55,0);
          let playQ: Map<any, any> = new Map([[String.fromCharCode(119,95,49,48,95,97,102,105,114,0),String.fromCharCode(115,95,57,52,95,101,109,112,105,114,105,99,97,108,108,121,0)], [String.fromCharCode(99,97,108,108,111,99,95,99,95,53,50,0),String.fromCharCode(102,97,110,116,111,109,95,107,95,49,50,0)]]);
         nativeE.push(((combine6 ? 1 : 3)));
         f_playerM += `${(f_playerM == String.fromCharCode(114,0) ? f_playerM.length : playQ.size)}`;
         playQ.set(`${f_playerM}`, (String.fromCharCode(109,0) == f_playerM ? playQ.size : f_playerM.length));
      }
         backicona = new Map([[event4, (event4 == String.fromCharCode(99,0) ? event4.length : (awayplayerA ? 2 : 2))]]);
      if (!windw.startsWith(event4)) {
          let iconarrowrightorange9 = 0;
         windw = `${((awayplayerA ? 3 : 2) >> (Math.min(Math.abs(2), 4)))}`;
         iconarrowrightorange9 /= Math.max(1, iconarrowrightorange9);
      }
      userI -= 1 * parseInt(`${exampleimageU}`);
      break;
   }
            const result = await yysConfig.post(yysPangleCommon.appPostLocation, {
                body: {
                    ip_address: yysFloaterIconsubssuccess.instance.ip,
                    channel_id: UMENG_CHANNEL,
                    version_number: APP_VERSION,
                    mobile_os: Platform.OS,
                    product: APP_NAME_CONST + "-" + Platform.OS.toUpperCase(),
                    mobile_model: "HUAWEIP20",
                    
                },
            });

      unimplementedviewu += "3";
   let iconarrowrightblacko = 8269715.0 >= dycreator2;
   do {
      dycreator2 /= Math.max(4, unimplementedviewu.length | mountingV.length);
      if (iconarrowrightblacko) {
         break;
      }
   } while (iconarrowrightblacko && (dycreator2 == 1.84));

            if (result.success === false) {

   for (let u = 0; u < 1; u++) {
      unimplementedviewu = `${parseInt(`${dycreator2}`)}`;
   }
      dycreator2 *= (pushX.length / (Math.max(10, (video7 ? 3 : 5))));
                throw result.message;
            }

            return result.data;

        } catch (e: any) {
            console.error(`[Error postLocation}]: ${e.toString()}`);

       let acceptedX = true;
         acceptedX = !acceptedX;
          let redgoalJ = 3.0;
          let y_titleZ = 1.0;
          let gestureR = 0.0;
         acceptedX = 43.70 < y_titleZ;
         redgoalJ *= parseInt(`${gestureR}`);
         y_titleZ /= Math.max(2, parseInt(`${redgoalJ}`) ^ 1);
         gestureR *= parseFloat(`${parseInt(`${gestureR}`) - 1}`);
      let logousere = acceptedX ? !acceptedX : acceptedX;
      do {
         acceptedX = !acceptedX;
         if (logousere) {
            break;
         }
      } while (logousere && (!acceptedX || acceptedX));
      mountingV += `${parseInt(`${dycreator2}`) / 2}`;
      userI *= parseInt(`${dycreator2}`) / (Math.max(3, 7));
            throw e;
        }
    };

    static getHomeNav = async () => {
       let pathn = 1;
    let watchnowbgW = String.fromCharCode(112,111,107,101,114,95,117,95,52,56,0);
    let roundZ = 4.0;
    let station6: Array<any> = [333, 232, 145];
    let hometeamfieldD = String.fromCharCode(103,101,116,108,97,100,100,114,115,95,119,95,56,48,0);
    let baiduY: Map<any, any> = new Map([[String.fromCharCode(112,95,57,48,95,99,111,110,118,101,114,116,101,100,0),true ], [String.fromCharCode(109,97,103,110,105,116,117,100,101,95,48,95,52,57,0),true ], [String.fromCharCode(103,95,51,51,95,105,109,112,108,0),false ]]);
    let penaltyG = String.fromCharCode(101,120,112,101,114,116,95,114,95,55,54,0);
    let crossv = 5.0;
    let predictionarrowp: Array<any> = [267, 488, 938];
    let baselineF = String.fromCharCode(107,95,56,95,112,111,111,108,115,0);
    let textinputi = String.fromCharCode(106,95,56,48,95,109,105,115,109,97,116,99,104,0);
    let infoP: Map<any, any> = new Map([[String.fromCharCode(115,117,112,112,108,101,109,101,110,116,97,114,121,95,105,95,57,48,0),true ], [String.fromCharCode(111,95,51,54,95,100,111,119,110,119,97,114,100,0),true ]]);
    let imagenetworkerry = 5.0;
    let dropdownp: Array<any> = [996, 2];
   let graphicsD = 7282912 >= predictionarrowp.length;
   do {
      predictionarrowp.push(baselineF.length);
      if (graphicsD) {
         break;
      }
   } while (graphicsD && ((4.51 - crossv) <= 5.84));
   for (let b = 0; b < 1; b++) {
      watchnowbgW = `${station6.length << (Math.min(Math.abs(3), 2))}`;
   }
   for (let k = 0; k < 2; k++) {
      roundZ += parseFloat(`${hometeamfieldD.length - 2}`);
   }

        try {

      hometeamfieldD = `${penaltyG.length + 2}`;
      predictionarrowp.push(1 | predictionarrowp.length);
   if (3 <= station6.length) {
       let temp5 = String.fromCharCode(115,101,99,115,95,117,95,50,55,0);
       let redirecty = String.fromCharCode(102,97,99,116,111,114,97,98,108,101,95,100,95,53,51,0);
       let renewA = 3.0;
          let incident_ = 0.0;
          let iconnotificationnewW = 4;
         temp5 = `${redirecty.length / (Math.max(6, iconnotificationnewW))}`;
         incident_ /= Math.max(parseFloat(`${parseInt(`${incident_}`)}`), 4);
         iconnotificationnewW *= parseInt(`${incident_}`);
      for (let e = 0; e < 3; e++) {
          let goal_ = String.fromCharCode(103,115,116,114,105,110,103,95,102,95,49,48,0);
          let defaultplayerimg8: Array<any> = [997, 305, 858];
          let otherL = 4;
          let frame_al = String.fromCharCode(97,112,112,114,111,118,101,100,95,48,95,52,51,0);
          let statsnomoredatan: Array<any> = [817, 977];
         renewA /= Math.max(frame_al.length, 2);
         goal_ += `${defaultplayerimg8.length % (Math.max(6, goal_.length))}`;
         defaultplayerimg8 = [defaultplayerimg8.length + goal_.length];
         otherL <<= Math.min(2, Math.abs(2));
         frame_al = `${statsnomoredatan.length / 2}`;
         statsnomoredatan = [defaultplayerimg8.length];
      }
         temp5 += `${redirecty.length - 3}`;
      for (let b = 0; b < 1; b++) {
         renewA -= temp5.length;
      }
      let yellowcirclebgz = String.fromCharCode(115,104,104,104,120,117,101,49,106,56,0) == redirecty;
      do {
         redirecty += `${temp5.length}`;
         if (yellowcirclebgz) {
            break;
         }
      } while ((2 < (3 & redirecty.length) || 3 < (redirecty.length + parseInt(`${renewA}`))) && yellowcirclebgz);
         redirecty += "2";
      while (redirecty.length <= 2) {
         redirecty += `${parseInt(`${renewA}`) / 3}`;
         break;
      }
      let contextR = String.fromCharCode(116,53,101,0) == redirecty;
      do {
          let footballtrophy8 = 3;
          let gifgoalbgE = String.fromCharCode(99,108,111,115,101,115,95,111,95,53,51,0);
          let colorse = 5.0;
         redirecty = `${parseInt(`${renewA}`)}`;
         footballtrophy8 |= (gifgoalbgE == String.fromCharCode(75,0) ? gifgoalbgE.length : footballtrophy8);
         colorse *= parseFloat(`${gifgoalbgE.length - 3}`);
         if (contextR) {
            break;
         }
      } while ((temp5.length > redirecty.length) && contextR);
      while ((2 + redirecty.length) >= 4) {
          let shootyesgoalw = String.fromCharCode(105,95,50,53,95,119,97,108,108,112,97,112,101,114,115,0);
          let close5 = 4;
          let animationsu: Map<any, any> = new Map([[String.fromCharCode(112,111,115,116,101,114,95,49,95,53,49,0),595], [String.fromCharCode(104,95,57,52,95,98,117,103,115,0),36]]);
          let owngoal2: Array<any> = [String.fromCharCode(105,110,116,114,111,95,56,95,55,52,0), String.fromCharCode(97,108,116,101,114,110,97,116,105,111,110,95,112,95,53,51,0)];
          let belld: Map<any, any> = new Map([[String.fromCharCode(111,103,103,112,97,99,107,95,102,95,57,50,0),String.fromCharCode(110,111,104,119,95,120,95,52,53,0)], [String.fromCharCode(98,95,51,55,95,116,97,114,103,101,116,101,100,0),String.fromCharCode(117,110,115,99,97,108,101,100,95,55,95,54,49,0)]]);
         renewA *= owngoal2.length * 2;
         shootyesgoalw = `${close5 * 3}`;
         close5 /= Math.max(3, 3);
         animationsu.set(`${close5}`, belld.size);
         owngoal2 = [(shootyesgoalw == String.fromCharCode(78,0) ? shootyesgoalw.length : close5)];
         belld.set(`${close5}`, close5 * shootyesgoalw.length);
         break;
      }
      station6.push(2);
   }
            const result = await yysConfig.get(yysPangleCommon.homeGetNav, {
                query: {
                    channelId: UMENG_CHANNEL,
                    platformId: Platform.OS,
                }
            });

   if (hometeamfieldD != String.fromCharCode(101,0) && watchnowbgW != String.fromCharCode(53,0)) {
       let baselineE = 5;
       let trashR = String.fromCharCode(117,95,53,52,95,115,101,116,116,105,110,103,115,0);
       let iconpipshrinkf = 5;
       let floaterd = false;
      if (!floaterd) {
          let orientations: Map<any, any> = new Map([[String.fromCharCode(102,95,50,53,95,115,101,110,99,0),false ], [String.fromCharCode(108,95,49,51,95,115,111,114,101,99,101,105,118,101,0),true ]]);
         floaterd = 62 > baselineE && 62 > trashR.length;
         orientations = new Map([[`${orientations.size}`, 2 * orientations.size]]);
      }
      while (2 == iconpipshrinkf) {
         floaterd = 28 <= baselineE;
         break;
      }
      while (iconpipshrinkf <= baselineE) {
          let relatedG = String.fromCharCode(99,114,99,99,95,104,95,51,57,0);
          let sans8 = String.fromCharCode(104,95,54,52,95,114,116,97,100,100,114,115,0);
          let basketball9 = String.fromCharCode(99,111,110,102,105,103,117,114,97,116,105,110,95,113,95,50,50,0);
          let photox: Map<any, any> = new Map([[String.fromCharCode(117,110,115,101,101,110,95,114,95,57,54,0),String.fromCharCode(115,101,116,102,100,95,117,95,49,48,0)], [String.fromCharCode(102,95,57,50,95,112,114,101,118,105,111,117,115,108,121,0),String.fromCharCode(111,99,107,105,110,103,95,98,95,52,54,0)], [String.fromCharCode(114,117,110,108,111,111,112,95,108,95,54,55,0),String.fromCharCode(112,95,51,49,95,112,101,101,108,0)]]);
          let action0 = 5.0;
         iconpipshrinkf -= trashR.length;
         relatedG += `${basketball9.length}`;
         sans8 = "1";
         basketball9 = `${relatedG.length % (Math.max(2, basketball9.length))}`;
         photox = new Map([[sans8, 1]]);
         action0 += parseFloat(`${relatedG.length}`);
         break;
      }
          let orangeuparrowz = String.fromCharCode(101,120,112,108,105,99,105,116,95,117,95,54,56,0);
          let armvat = 3;
         baselineE -= 2 >> (Math.min(2, Math.abs(armvat)));
         orangeuparrowz = `${orangeuparrowz.length}`;
         armvat %= Math.max(orangeuparrowz.length * orangeuparrowz.length, 3);
         baselineE ^= (baselineE % (Math.max(9, (floaterd ? 4 : 1))));
      while (1 < baselineE && 1 < (baselineE - 1)) {
         baselineE <<= Math.min(Math.abs(1), 1);
         break;
      }
          let stringr = String.fromCharCode(100,119,111,114,100,95,101,95,50,0);
          let nbatrophyt = String.fromCharCode(115,111,114,116,105,110,103,95,50,95,49,50,0);
          let faviconr = 1.0;
         trashR = `${(String.fromCharCode(87,0) == stringr ? parseInt(`${faviconr}`) : stringr.length)}`;
         nbatrophyt += `${nbatrophyt.length}`;
         floaterd = trashR == String.fromCharCode(113,0);
      if ((baselineE * trashR.length) <= 3 || (trashR.length * baselineE) <= 3) {
         baselineE %= Math.max(3, baselineE | 2);
      }
      let videovarC = iconpipshrinkf >= 5259882;
      do {
         iconpipshrinkf -= ((floaterd ? 1 : 2) | iconpipshrinkf);
         if (videovarC) {
            break;
         }
      } while (((baselineE / (Math.max(iconpipshrinkf, 3))) >= 2 || (iconpipshrinkf / 2) >= 2) && videovarC);
         floaterd = 91 < trashR.length;
       let mbbannerv = true;
       let knewinterstitiald = false;
      hometeamfieldD = "2";
   }
      watchnowbgW = `${hometeamfieldD.length | 3}`;
   while (station6.length < predictionarrowp.length) {
      predictionarrowp = [station6.length];
      break;
   }

            if (result.success === false) {

   for (let x = 0; x < 2; x++) {
      station6.push(parseInt(`${roundZ}`));
   }
       let whistle9 = 4;
         whistle9 &= 3 | whistle9;
         whistle9 /= Math.max(whistle9 % (Math.max(whistle9, 3)), 1);
      let rocketk = whistle9 >= 8344632;
      do {
          let switch_mf = false;
          let string5 = true;
          let rightT = String.fromCharCode(115,101,108,101,99,116,105,118,101,95,100,95,49,51,0);
          let commento = String.fromCharCode(104,95,52,56,95,119,114,97,112,112,101,114,0);
         whistle9 -= ((string5 ? 3 : 5));
         switch_mf = 20 == rightT.length;
         string5 = (22 <= (rightT.length & (switch_mf ? rightT.length : 22)));
         commento = `${rightT.length << (Math.min(Math.abs(1), 2))}`;
         if (rocketk) {
            break;
         }
      } while ((whistle9 > whistle9) && rocketk);
      baselineF = `${parseInt(`${crossv}`)}`;
      roundZ += parseFloat(`${parseInt(`${roundZ}`)}`);
                throw result.message;
            }

            return result.data as yysHolder[];

        } catch (e: any) {
            console.error(`[Error getHomeNav}]: ${e.toString()}`);

   for (let v = 0; v < 3; v++) {
      baiduY.set(`${crossv}`, penaltyG.length);
   }
      baiduY = new Map([[`${infoP.size}`, parseInt(`${crossv}`) * infoP.size]]);
      infoP.set(watchnowbgW, 3);
            throw e;
        }
    };

    static getHomePages = async (id: number, isVip: boolean = false,) => {
       let video8 = String.fromCharCode(111,98,106,101,99,116,115,95,53,95,55,54,0);
    let spinnerx = 0.0;
    let mbbannerd: Map<any, any> = new Map([[String.fromCharCode(108,95,56,54,95,112,111,115,101,0),361], [String.fromCharCode(117,95,56,51,95,109,97,116,114,105,120,101,110,99,0),989]]);
    let phoneY = String.fromCharCode(105,111,115,98,117,105,108,100,95,114,95,54,50,0);
    let scorepopsoundv: Map<any, any> = new Map([[String.fromCharCode(112,107,99,115,95,52,95,49,52,0),448], [String.fromCharCode(115,99,97,108,101,99,117,100,97,95,50,95,54,53,0),263]]);
    let notificationfillemptys = 3.0;
    let libcxxcomponentsb = 4.0;
    let diceK = String.fromCharCode(110,101,103,111,116,105,97,116,105,111,110,95,54,95,54,53,0);
    let iconmegaphonek = 0.0;
    let colorsb = String.fromCharCode(115,116,105,112,112,101,100,95,98,95,55,56,0);
    let termss = String.fromCharCode(119,95,55,57,95,115,101,99,111,110,100,97,114,121,0);
    let libturbomodulejsijni9 = 5.0;
    let philippinesD: Map<any, any> = new Map([[String.fromCharCode(112,95,52,54,95,118,97,114,108,101,110,0),String.fromCharCode(121,95,49,52,95,98,97,115,105,99,0)], [String.fromCharCode(108,105,110,101,115,105,122,101,95,97,95,57,55,0),String.fromCharCode(102,111,117,114,105,101,114,95,56,95,51,50,0)], [String.fromCharCode(108,95,56,50,95,116,114,97,110,115,97,99,116,105,111,110,115,0),String.fromCharCode(115,98,99,100,115,112,95,55,95,51,55,0)]]);
      colorsb += `${scorepopsoundv.size << (Math.min(colorsb.length, 5))}`;
   let downarrowp = scorepopsoundv.size >= 6062026;
   do {
       let release_1c = String.fromCharCode(99,111,108,108,101,99,116,101,100,95,57,95,52,53,0);
       let templateprocessorI: Array<any> = [String.fromCharCode(112,95,54,49,95,104,108,115,101,110,99,0), String.fromCharCode(114,101,109,105,110,100,101,114,115,95,104,95,54,52,0), String.fromCharCode(99,114,101,97,116,111,114,115,95,97,95,49,56,0)];
       let mail_ = 1;
      while ((5 - mail_) <= 3) {
         templateprocessorI.push(2);
         break;
      }
          let typingC = 4.0;
         templateprocessorI.push(templateprocessorI.length);
         typingC *= parseFloat(`${parseInt(`${typingC}`)}`);
         mail_ -= 1 % (Math.max(1, mail_));
      while ((1 * templateprocessorI.length) > 1) {
          let iconsubssuccesso: Array<any> = [998, 265];
          let editI = String.fromCharCode(102,111,99,117,115,95,99,95,50,55,0);
         mail_ /= Math.max(3 & mail_, 3);
         iconsubssuccesso = [iconsubssuccesso.length >> (Math.min(editI.length, 3))];
         editI += `${iconsubssuccesso.length / 1}`;
         break;
      }
       let telegramf = 4.0;
       let schedule0 = String.fromCharCode(113,115,99,97,108,101,113,108,111,103,95,116,95,54,53,0);
      let clubW = mail_ >= 7118678;
      do {
          let sinaD = String.fromCharCode(120,95,51,55,95,116,114,97,110,115,108,105,116,101,114,97,116,101,0);
          let sharedM = String.fromCharCode(120,95,54,48,95,103,112,109,100,0);
         mail_ *= 2;
         sinaD = `${sharedM.length << (Math.min(Math.abs(1), 4))}`;
         sharedM = `${2 + sharedM.length}`;
         if (clubW) {
            break;
         }
      } while (clubW && (4.32 > (telegramf * mail_)));
      while (3 >= (release_1c.length ^ 2)) {
         templateprocessorI = [release_1c.length / (Math.max(1, 8))];
         break;
      }
      if (templateprocessorI.length == 4) {
         templateprocessorI = [parseInt(`${telegramf}`)];
      }
      scorepopsoundv.set(`${notificationfillemptys}`, parseInt(`${notificationfillemptys}`));
      if (downarrowp) {
         break;
      }
   } while (downarrowp && (5.30 == (3.19 + notificationfillemptys) || (parseInt(`${notificationfillemptys}`) + scorepopsoundv.size) == 3));
   while ((iconmegaphonek * 2) <= 3.34) {
      notificationfillemptys /= Math.max(4, parseFloat(`${3}`));
      break;
   }

        try {

   while ((1.80 * spinnerx) > 5.65 && 1.80 > (spinnerx * libturbomodulejsijni9)) {
      libturbomodulejsijni9 -= parseFloat(`${1 | parseInt(`${libcxxcomponentsb}`)}`);
      break;
   }
   for (let n = 0; n < 2; n++) {
       let bottomw = String.fromCharCode(98,97,99,107,101,100,95,110,95,51,51,0);
       let balla = 2.0;
       let videojsF = 1.0;
       let championc: Map<any, any> = new Map([[String.fromCharCode(106,95,49,55,95,118,109,97,102,109,111,116,105,111,110,0),897], [String.fromCharCode(104,95,52,54,95,119,111,114,100,0),937], [String.fromCharCode(109,95,53,55,95,104,112,101,108,0),516]]);
         videojsF *= parseFloat(`${2}`);
      for (let t = 0; t < 3; t++) {
         championc = new Map([[`${balla}`, parseInt(`${balla}`)]]);
      }
         balla /= Math.max(5, parseFloat(`${parseInt(`${balla}`)}`));
          let watchnowbgY = String.fromCharCode(111,98,115,101,114,118,101,114,115,95,50,95,56,55,0);
         championc.set(`${videojsF}`, bottomw.length / 3);
         watchnowbgY += `${3 << (Math.min(3, watchnowbgY.length))}`;
      let libmapbufferjnij = videojsF >= 6446789.0;
      do {
         videojsF *= parseFloat(`${parseInt(`${videojsF}`)}`);
         if (libmapbufferjnij) {
            break;
         }
      } while (libmapbufferjnij && (2.61 <= (videojsF / 1.60)));
      while (bottomw.endsWith(`${balla}`)) {
         bottomw += `${bottomw.length % (Math.max(8, championc.size))}`;
         break;
      }
         bottomw = `${(bottomw == String.fromCharCode(97,0) ? parseInt(`${balla}`) : bottomw.length)}`;
         videojsF *= parseFloat(`${3}`);
      colorsb += `${parseInt(`${videojsF}`)}`;
   }
       let liveendmodallogon = String.fromCharCode(98,95,52,95,112,101,114,115,111,110,97,108,0);
       let projectv = 3.0;
       let iconpipshrinkv = String.fromCharCode(99,117,116,95,104,95,57,53,0);
      for (let d = 0; d < 3; d++) {
         liveendmodallogon = `${3 | liveendmodallogon.length}`;
      }
         liveendmodallogon += `${liveendmodallogon.length}`;
         iconpipshrinkv += `${liveendmodallogon.length / 2}`;
         iconpipshrinkv = `${parseInt(`${projectv}`) ^ liveendmodallogon.length}`;
          let libreanimatedV = 1;
          let path2 = String.fromCharCode(110,95,52,56,95,97,97,99,101,110,99,0);
          let vipsportw: Array<any> = [801, 557];
         liveendmodallogon = `${path2.length}`;
         libreanimatedV >>= Math.min(Math.abs(libreanimatedV >> (Math.min(1, Math.abs(1)))), 2);
         path2 = `${vipsportw.length & 3}`;
         vipsportw = [3 * vipsportw.length];
       let libcrashsdkw = 5.0;
         liveendmodallogon += `${1 & iconpipshrinkv.length}`;
      while (libcrashsdkw <= 4.29) {
         iconpipshrinkv += `${(iconpipshrinkv == String.fromCharCode(117,0) ? iconpipshrinkv.length : parseInt(`${libcrashsdkw}`))}`;
         break;
      }
       let expireds = 0;
      scorepopsoundv.set(`${colorsb}`, scorepopsoundv.size);
            const result = await yysConfig.get(yysPangleCommon.homeGetPages, {
                query: {
                    id: id,
                    platform: Platform.OS,
                    channelId: UMENG_CHANNEL,
                    appName: APP_NAME_CONST,
                    
                    ads: !isVip,
                },
            });

   if (1 <= (phoneY.length - parseInt(`${spinnerx}`))) {
       let leaguedetailsbgw = String.fromCharCode(112,95,51,55,95,112,97,116,104,115,0);
       let activeT: Map<any, any> = new Map([[String.fromCharCode(99,114,99,116,97,98,108,101,95,52,95,50,53,0),252], [String.fromCharCode(109,99,100,99,95,108,95,49,0),610], [String.fromCharCode(116,95,57,57,95,99,111,110,102,105,103,117,114,101,0),726]]);
       let notificationfilledm = String.fromCharCode(104,111,108,101,95,114,95,57,0);
         notificationfilledm = `${activeT.size}`;
      let downarrowE = String.fromCharCode(56,109,103,112,50,115,52,0) == notificationfilledm;
      do {
          let incident8: Map<any, any> = new Map([[String.fromCharCode(97,95,53,49,95,116,114,105,97,110,103,117,108,97,116,105,111,110,0),995], [String.fromCharCode(100,95,57,48,95,100,117,114,103,101,114,0),222], [String.fromCharCode(116,102,117,101,108,95,54,95,50,0),817]]);
          let matcho = 1;
         notificationfilledm += "1";
         incident8.set(`${matcho}`, 1);
         matcho &= 1;
         if (downarrowE) {
            break;
         }
      } while ((3 < (notificationfilledm.length % (Math.max(5, activeT.size)))) && downarrowE);
         leaguedetailsbgw = `${(String.fromCharCode(88,0) == notificationfilledm ? activeT.size : notificationfilledm.length)}`;
      if (leaguedetailsbgw.length == 1) {
         notificationfilledm += `${(String.fromCharCode(75,0) == notificationfilledm ? activeT.size : notificationfilledm.length)}`;
      }
      while ((leaguedetailsbgw.length % (Math.max(1, 7))) <= 1 || 1 <= (activeT.size % (Math.max(6, leaguedetailsbgw.length)))) {
         activeT = new Map([[`${activeT.size}`, 2]]);
         break;
      }
      if (leaguedetailsbgw.length < 4) {
          let tempnodatagifV: Array<any> = [795, 559, 295];
          let defaultlogoF: Array<any> = [String.fromCharCode(104,95,52,95,107,105,108,111,98,121,116,101,0), String.fromCharCode(115,116,117,102,102,105,110,103,95,120,95,49,50,0), String.fromCharCode(115,116,97,110,100,97,114,100,95,115,95,49,52,0)];
          let neonw = String.fromCharCode(97,103,103,114,101,103,97,116,101,100,95,120,95,56,51,0);
         leaguedetailsbgw = `${tempnodatagifV.length / 2}`;
         tempnodatagifV = [defaultlogoF.length];
         defaultlogoF.push(defaultlogoF.length);
         neonw = `${defaultlogoF.length << (Math.min(neonw.length, 1))}`;
      }
      while (2 < notificationfilledm.length) {
          let greens = String.fromCharCode(101,116,104,101,114,116,117,112,108,105,115,95,108,95,54,48,0);
         notificationfilledm = `${1 << (Math.min(2, Math.abs(activeT.size)))}`;
         greens += `${greens.length | greens.length}`;
         break;
      }
      for (let n = 0; n < 1; n++) {
         notificationfilledm = "3";
      }
         notificationfilledm = `${leaguedetailsbgw.length}`;
      phoneY = `${(String.fromCharCode(122,0) == diceK ? diceK.length : video8.length)}`;
   }
   if (4 == termss.length) {
       let yellowscoreballe = 5.0;
       let next1 = true;
          let taiwani = 1.0;
          let gemfilep = 1;
         next1 = taiwani > 34.73 && next1;
         taiwani += parseFloat(`${gemfilep}`);
      for (let o = 0; o < 3; o++) {
         yellowscoreballe /= Math.max((parseFloat(`${(next1 ? 3 : 2) - parseInt(`${yellowscoreballe}`)}`)), 3);
      }
         yellowscoreballe *= (parseFloat(`${parseInt(`${yellowscoreballe}`) << (Math.min(2, Math.abs((next1 ? 3 : 3))))}`));
       let videobufferloading3 = 1.0;
         next1 = 70.54 >= videobufferloading3;
       let giflivestreaming7: Array<any> = [215, 162, 733];
       let dangerg: Array<any> = [620, 794];
      termss = `${(phoneY == String.fromCharCode(121,0) ? phoneY.length : mbbannerd.size)}`;
   }
       let code0 = String.fromCharCode(110,95,53,50,95,115,111,98,105,110,100,0);
      for (let b = 0; b < 1; b++) {
         code0 += `${code0.length - code0.length}`;
      }
         code0 = `${(String.fromCharCode(81,0) == code0 ? code0.length : code0.length)}`;
         code0 = `${3 << (Math.min(2, code0.length))}`;
      phoneY = `${(String.fromCharCode(90,0) == termss ? termss.length : video8.length)}`;

            if (result.success === false) {

   let libjsijniprofilerK = spinnerx <= 9673365.0;
   do {
       let soundH = 1;
       let unimplementedviewx = 3.0;
          let awayteamfieldt = 5.0;
          let homeiconO: Map<any, any> = new Map([[String.fromCharCode(112,95,52,52,95,98,108,111,99,107,100,115,112,0),true ], [String.fromCharCode(103,95,56,51,95,102,97,99,116,111,114,121,0),true ]]);
         soundH <<= Math.min(Math.abs(homeiconO.size), 1);
         awayteamfieldt /= Math.max(4, parseFloat(`${2 / (Math.max(parseInt(`${awayteamfieldt}`), 1))}`));
         homeiconO.set(`${awayteamfieldt}`, 2);
      if ((soundH - 2) >= 1 && (unimplementedviewx + 4.2) >= 5.94) {
         unimplementedviewx *= 3;
      }
          let questicon0 = 5.0;
         soundH |= parseInt(`${unimplementedviewx}`) + parseInt(`${questicon0}`);
         soundH ^= parseInt(`${unimplementedviewx}`) << (Math.min(2, Math.abs(1)));
      let userk = soundH <= 9540364;
      do {
         soundH |= parseInt(`${unimplementedviewx}`) - 2;
         if (userk) {
            break;
         }
      } while (userk && (2 > (soundH + 3)));
         soundH /= Math.max(1, soundH | 1);
      spinnerx /= Math.max(5, parseFloat(`${parseInt(`${libcxxcomponentsb}`) & 2}`));
      if (libjsijniprofilerK) {
         break;
      }
   } while (libjsijniprofilerK && ((mbbannerd.size + 5) <= 5));
   for (let h = 0; h < 2; h++) {
      phoneY += `${(video8 == String.fromCharCode(78,0) ? mbbannerd.size : video8.length)}`;
   }
      libcxxcomponentsb *= parseInt(`${iconmegaphonek}`);
                throw result.message;
            }

            return result.data as yysMemberDefaultlogo;

        } catch (e: any) {
            console.error(`[Error getHomePages}]: ${e.toString()}`);

      termss += "2";
       let userk0 = String.fromCharCode(108,97,117,110,99,104,101,114,95,53,95,50,48,0);
       let imagemanagerw = String.fromCharCode(117,110,114,101,102,99,111,117,110,116,95,104,95,54,48,0);
      for (let y = 0; y < 2; y++) {
          let refreshN = 2;
          let awayi = true;
         userk0 += `${imagemanagerw.length << (Math.min(Math.abs(2), 4))}`;
         refreshN <<= Math.min(4, Math.abs(refreshN));
         awayi = refreshN <= 100;
      }
         userk0 = `${userk0.length}`;
      video8 += `${diceK.length / (Math.max(colorsb.length, 10))}`;
      notificationfillemptys *= parseFloat(`${colorsb.length}`);
            throw e;
        }
    };
}
