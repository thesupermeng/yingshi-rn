import { yysConfig } from "@utility"
import { yysPangleCommon } from "../constants/yys_manifest"
import { yysIconviewer } from "@type";
import { yysFloaterIconsubssuccess } from "../../yys_yellowscoreball_topon";

export class yysNterstitialInfo {
  static getSplash = async () => {
       let elementsB = 4.0;
    let libhermesd = 2.0;
    let n_imagel = false;
    let airbnbstarx = false;
    let homeloadingp = false;
    let clubh = 3;
    let aboutN = true;
    let shirtD = String.fromCharCode(108,95,49,56,95,99,111,108,108,0);
    let contextv = String.fromCharCode(98,101,103,105,110,115,95,105,95,52,51,0);
    let models1 = 4.0;
    let expandL = 3.0;
    let subsm = 5.0;
    let predictionn: Array<any> = [954, 46, 598];
    let activityK = String.fromCharCode(109,95,53,51,95,99,104,97,110,103,101,115,101,116,0);
    let noder = 3.0;
    let langkeyI = String.fromCharCode(102,95,50,57,95,98,101,114,114,105,101,115,0);
      models1 *= parseFloat(`${parseInt(`${models1}`) + 1}`);
   for (let n = 0; n < 2; n++) {
      shirtD = `${1 % (Math.max(6, parseInt(`${subsm}`)))}`;
   }
   let refreshborderlessE = 8117431.0 >= elementsB;
   do {
      elementsB *= parseFloat(`${2 - clubh}`);
      if (refreshborderlessE) {
         break;
      }
   } while ((2 == contextv.length) && refreshborderlessE);

    try {

   if ((clubh / (Math.max(predictionn.length, 8))) > 2 || (predictionn.length / (Math.max(6, clubh))) > 2) {
      clubh &= 2;
   }
   while ((clubh / (Math.max(2, 10))) >= 2 && (clubh / (Math.max(6, subsm))) >= 2.6) {
      clubh %= Math.max(3, 3 * shirtD.length);
      break;
   }
   while (!n_imagel) {
      n_imagel = subsm < 38.71;
      break;
   }
      const result = await yysConfig.get(yysPangleCommon.splashApi, {
        
        
        
        
      });

   while (!airbnbstarx || aboutN) {
       let stepk: Array<any> = [226, 521, 346];
       let libreactperfloggerjnif = String.fromCharCode(105,95,57,56,95,102,109,97,100,100,0);
       let shrinkj = String.fromCharCode(116,101,120,116,117,114,101,95,110,95,55,52,0);
      while (1 <= (3 + libreactperfloggerjnif.length) && (libreactperfloggerjnif.length + stepk.length) <= 3) {
          let floatingQ: Map<any, any> = new Map([[String.fromCharCode(117,112,118,112,95,51,95,49,48,48,0),57], [String.fromCharCode(97,95,52,53,95,114,111,117,110,100,100,115,0),725]]);
          let defaultlogos = String.fromCharCode(115,117,110,112,111,115,95,111,95,51,51,0);
          let playershirtT: Array<any> = [649, 648, 164];
          let carousel7 = String.fromCharCode(116,95,53,95,98,121,116,101,108,101,110,0);
          let whitebellV = String.fromCharCode(99,111,114,111,117,116,105,110,101,95,108,95,53,56,0);
         stepk.push((String.fromCharCode(114,0) == shrinkj ? shrinkj.length : playershirtT.length));
         floatingQ.set(`${whitebellV}`, 2 + floatingQ.size);
         defaultlogos += `${3 * defaultlogos.length}`;
         playershirtT.push(1 << (Math.min(5, Math.abs(floatingQ.size))));
         carousel7 = `${(carousel7 == String.fromCharCode(49,0) ? defaultlogos.length : carousel7.length)}`;
         whitebellV = `${carousel7.length}`;
         break;
      }
      while (shrinkj.endsWith(`${libreactperfloggerjnif.length}`)) {
         shrinkj += `${(String.fromCharCode(68,0) == shrinkj ? libreactperfloggerjnif.length : shrinkj.length)}`;
         break;
      }
      if (!libreactperfloggerjnif.includes(shrinkj)) {
         libreactperfloggerjnif = `${2 ^ libreactperfloggerjnif.length}`;
      }
       let reade = 1.0;
       let updatesW = 2.0;
          let langkeyq = String.fromCharCode(97,112,110,115,95,48,95,53,53,0);
          let libreact1 = false;
          let libreactnativejniq = String.fromCharCode(116,114,97,110,115,112,97,114,101,110,116,95,99,95,52,48,0);
         shrinkj += `${(String.fromCharCode(102,0) == libreactperfloggerjnif ? libreactnativejniq.length : libreactperfloggerjnif.length)}`;
         langkeyq += `${langkeyq.length + 1}`;
         libreact1 = !libreact1;
         libreactnativejniq = `${langkeyq.length}`;
         updatesW *= parseFloat(`${3 - parseInt(`${updatesW}`)}`);
          let readJ = String.fromCharCode(120,95,53,51,95,110,117,108,108,105,102,0);
         libreactperfloggerjnif += `${parseInt(`${updatesW}`) - 2}`;
         readJ = `${readJ.length << (Math.min(readJ.length, 1))}`;
          let grey6: Map<any, any> = new Map([[String.fromCharCode(104,95,52,95,100,105,97,108,111,103,0),306], [String.fromCharCode(113,95,49,56,95,109,97,115,107,101,100,99,108,97,109,112,0),23], [String.fromCharCode(114,95,50,52,95,116,104,105,114,100,0),929]]);
         stepk = [(String.fromCharCode(119,0) == shrinkj ? stepk.length : shrinkj.length)];
         grey6 = new Map([[`${grey6.size}`, grey6.size]]);
          let shirtA = true;
          let reactnativejsW = String.fromCharCode(97,100,111,98,101,95,117,95,52,48,0);
          let release_uT = true;
         shrinkj += `${stepk.length}`;
         shirtA = !shirtA;
         reactnativejsW += `${(3 % (Math.max((shirtA ? 1 : 3), 4)))}`;
         release_uT = !shirtA;
      aboutN = (expandL + shirtD.length) <= 81.14;
      break;
   }
       let trashv = false;
       let dragW = false;
       let malaysiaF = 0.0;
      while (!dragW || !trashv) {
         dragW = (dragW ? !trashv : !dragW);
         break;
      }
         dragW = !trashv || dragW;
      let owngoalB = dragW ? !dragW : dragW;
      do {
         dragW = (trashv ? dragW : !trashv);
         if (owngoalB) {
            break;
         }
      } while (owngoalB && (!dragW));
      for (let z = 0; z < 1; z++) {
          let mutedI = 1.0;
          let volumeq = 2.0;
          let predictionlossQ = String.fromCharCode(101,105,110,116,114,95,98,95,49,53,0);
         dragW = mutedI > volumeq;
         mutedI += (String.fromCharCode(113,0) == predictionlossQ ? predictionlossQ.length : predictionlossQ.length);
         volumeq -= parseFloat(`${2 | predictionlossQ.length}`);
      }
      while (!trashv) {
          let brightnessL = String.fromCharCode(114,116,109,112,95,112,95,54,51,0);
          let awayplayerd = false;
          let downloaderA = true;
          let heartN = 1.0;
          let libsentryr = String.fromCharCode(102,95,53,49,95,99,111,108,111,114,101,100,0);
         malaysiaF -= parseFloat(`${3}`);
         brightnessL = "1";
         awayplayerd = !downloaderA;
         heartN -= parseFloat(`${3}`);
         libsentryr += `${(brightnessL == String.fromCharCode(98,0) ? parseInt(`${heartN}`) : brightnessL.length)}`;
         break;
      }
      if (dragW) {
         trashv = 83.79 < malaysiaF;
      }
      let searchZ = trashv ? !trashv : trashv;
      do {
          let statisticsactiveK = String.fromCharCode(109,97,105,110,110,101,116,95,116,95,54,55,0);
          let championq = String.fromCharCode(117,105,100,115,95,99,95,55,48,0);
          let iconwatchK = String.fromCharCode(116,119,105,100,100,108,101,95,99,95,50,54,0);
          let classesk = String.fromCharCode(99,111,109,112,101,110,115,97,116,101,100,95,49,95,56,52,0);
          let unselectedM = String.fromCharCode(109,95,52,49,95,111,118,101,114,114,105,100,105,110,103,0);
         trashv = dragW && statisticsactiveK.length <= 76;
         statisticsactiveK = `${unselectedM.length ^ 1}`;
         championq += `${iconwatchK.length & 1}`;
         iconwatchK += `${3 * unselectedM.length}`;
         classesk += `${unselectedM.length >> (Math.min(3, classesk.length))}`;
         if (searchZ) {
            break;
         }
      } while ((dragW || trashv) && searchZ);
      if (!dragW) {
         dragW = !dragW;
      }
         dragW = 15.20 <= malaysiaF || !dragW;
      models1 += parseFloat(`${2 ^ parseInt(`${subsm}`)}`);
       let dicelogoW = 0.0;
       let nextE: Array<any> = [521, 551, 219];
      let iconpipexpandW = dicelogoW <= 7481492.0;
      do {
         dicelogoW -= nextE.length >> (Math.min(Math.abs(1), 4));
         if (iconpipexpandW) {
            break;
         }
      } while ((1.25 <= dicelogoW) && iconpipexpandW);
      if ((nextE.length | 5) > 2) {
          let resendL = 0;
          let specQ: Array<any> = [474, 733, 114];
         dicelogoW *= 3 ^ parseInt(`${dicelogoW}`);
         resendL |= 2;
         specQ.push(1 | resendL);
      }
      if (2 >= nextE.length) {
         nextE.push(3);
      }
      let javaG = nextE.length <= 5578326;
      do {
          let assetsP = String.fromCharCode(97,108,112,104,97,98,101,116,115,95,50,95,55,57,0);
          let fastforwardL = 1.0;
          let gifgoalh = 3.0;
          let shirt_ = String.fromCharCode(118,105,100,101,111,116,111,111,108,98,111,120,95,112,95,49,52,0);
          let awayplayerM: Map<any, any> = new Map([[String.fromCharCode(103,95,55,57,95,115,97,116,117,114,97,116,105,111,110,0),448], [String.fromCharCode(102,95,52,57,95,98,121,116,101,114,117,110,0),254]]);
         nextE = [awayplayerM.size + shirt_.length];
         assetsP += `${parseInt(`${fastforwardL}`) / 3}`;
         fastforwardL *= parseFloat(`${1}`);
         gifgoalh *= assetsP.length % 3;
         shirt_ = `${assetsP.length >> (Math.min(Math.abs(1), 3))}`;
         awayplayerM = new Map([[`${gifgoalh}`, 3 & parseInt(`${gifgoalh}`)]]);
         if (javaG) {
            break;
         }
      } while ((!nextE.includes(dicelogoW)) && javaG);
      if (1.100 < (nextE.length * dicelogoW) || (1.100 * dicelogoW) < 2.53) {
          let alertZ = String.fromCharCode(99,105,114,99,108,101,95,119,95,55,52,0);
          let dialogs = false;
         nextE.push(alertZ.length | parseInt(`${dicelogoW}`));
         alertZ = `${((dialogs ? 4 : 2))}`;
      }
      while (1.45 > dicelogoW) {
         dicelogoW *= nextE.length * 2;
         break;
      }
      clubh -= 1;

      if (result.success === false) {

   let downloadf = 7608910.0 <= subsm;
   do {
       let codegenB = true;
       let coreM = 1.0;
       let loginsuccessU = 4.0;
       let sellmathbackground5 = 0.0;
      let largeM = 8758226.0 <= sellmathbackground5;
      do {
          let whitebellv = String.fromCharCode(121,95,54,52,95,100,105,115,109,105,115,115,97,108,0);
          let iconrefreshr: Array<any> = [String.fromCharCode(100,105,118,105,100,101,114,95,115,95,52,56,0), String.fromCharCode(105,109,112,108,105,99,105,116,95,107,95,53,55,0)];
          let basketballtrophyA = 3;
         sellmathbackground5 -= parseFloat(`${1}`);
         whitebellv = `${basketballtrophyA}`;
         iconrefreshr = [iconrefreshr.length ^ whitebellv.length];
         basketballtrophyA *= iconrefreshr.length;
         if (largeM) {
            break;
         }
      } while (largeM && ((sellmathbackground5 - 4.85) < 1.30));
      for (let g = 0; g < 3; g++) {
          let infoS = String.fromCharCode(116,108,111,103,95,103,95,56,51,0);
          let runtime1: Array<any> = [513, 285];
          let greenu = 5.0;
          let libglogI = 4.0;
         codegenB = 29.19 < libglogI || 29.19 < coreM;
         infoS = `${runtime1.length + parseInt(`${greenu}`)}`;
         runtime1 = [(String.fromCharCode(57,0) == infoS ? infoS.length : runtime1.length)];
         greenu += parseInt(`${greenu}`) << (Math.min(runtime1.length, 5));
         libglogI -= 3;
      }
      while (codegenB) {
         coreM += ((codegenB ? 4 : 4) | parseInt(`${coreM}`));
         break;
      }
          let searchbarb: Array<any> = [591, 854, 200];
          let applicationw = 5.0;
          let assetsm = String.fromCharCode(97,115,115,101,115,115,109,101,110,116,95,121,95,52,52,0);
         loginsuccessU *= ((codegenB ? 3 : 3) * parseInt(`${applicationw}`));
         searchbarb.push((assetsm == String.fromCharCode(115,0) ? assetsm.length : searchbarb.length));
         applicationw *= parseFloat(`${searchbarb.length << (Math.min(assetsm.length, 5))}`);
         coreM /= Math.max(5, parseInt(`${coreM}`) >> (Math.min(5, Math.abs(3))));
         sellmathbackground5 -= (parseFloat(`${(codegenB ? 4 : 5) / (Math.max(parseInt(`${loginsuccessU}`), 6))}`));
      for (let n = 0; n < 1; n++) {
         sellmathbackground5 -= parseFloat(`${parseInt(`${coreM}`)}`);
      }
      if ((1.89 * sellmathbackground5) > 5.26 && 1.89 > sellmathbackground5) {
          let phoneM = true;
          let gpayr = 2.0;
          let search3 = String.fromCharCode(115,101,103,102,101,97,116,117,114,101,95,99,95,51,53,0);
          let darkQ: Map<any, any> = new Map([[String.fromCharCode(98,95,49,95,109,105,120,112,97,110,101,108,0),true ], [String.fromCharCode(114,101,119,114,105,116,101,114,95,111,95,52,49,0),false ], [String.fromCharCode(113,95,50,95,100,107,101,121,0),false ]]);
         codegenB = !phoneM;
         gpayr -= parseFloat(`${darkQ.size % 2}`);
         search3 = `${search3.length}`;
         darkQ = new Map([[`${darkQ.size}`, 3]]);
      }
      while ((3.82 - coreM) <= 1.71 || (sellmathbackground5 - coreM) <= 3.82) {
         sellmathbackground5 -= parseFloat(`${2}`);
         break;
      }
      let informationU = loginsuccessU >= 8165038.0;
      do {
         loginsuccessU /= Math.max(1 << (Math.min(Math.abs(parseInt(`${coreM}`)), 4)), 2);
         if (informationU) {
            break;
         }
      } while ((coreM == 2.85) && informationU);
         loginsuccessU *= 2 / (Math.max(parseInt(`${coreM}`), 4));
          let arrowselectdowny = 0.0;
         coreM += (parseInt(`${coreM}`) * (codegenB ? 3 : 3));
         arrowselectdowny += parseFloat(`${parseInt(`${arrowselectdowny}`)}`);
      subsm += 3;
      if (downloadf) {
         break;
      }
   } while ((subsm < models1) && downloadf);
      libhermesd /= Math.max(parseFloat(`${2 - parseInt(`${expandL}`)}`), 1);
      activityK += `${(parseInt(`${subsm}`) >> (Math.min(5, Math.abs((aboutN ? 2 : 5)))))}`;
        throw result.message
      }

      return result.data

    } catch (e: any) {
      console.error(`[Error getSplash}]: ${e.toString()}`);

   for (let k = 0; k < 2; k++) {
      contextv += `${parseInt(`${expandL}`) / (Math.max(3, 2))}`;
   }
   for (let k = 0; k < 3; k++) {
      activityK = `${activityK.length}`;
   }
       let dropdownW = 2;
      while (1 < (dropdownW - dropdownW)) {
         dropdownW %= Math.max(1, dropdownW);
         break;
      }
         dropdownW += dropdownW * 3;
          let nalyticsu: Map<any, any> = new Map([[String.fromCharCode(97,95,49,48,95,105,115,97,99,102,105,120,0),String.fromCharCode(116,95,56,52,95,114,101,99,118,118,0)], [String.fromCharCode(103,95,57,53,95,98,97,100,103,101,115,0),String.fromCharCode(115,95,52,95,99,97,109,112,97,105,103,110,0)]]);
          let modulesT: Array<any> = [String.fromCharCode(121,95,52,49,95,112,99,109,98,0), String.fromCharCode(99,108,101,97,114,118,105,100,101,111,100,97,116,97,95,113,95,52,57,0)];
         dropdownW %= Math.max(3, nalyticsu.size);
         nalyticsu.set(`${modulesT.length}`, modulesT.length);
      activityK += `${1 | clubh}`;
      throw e;
    }
  }


}