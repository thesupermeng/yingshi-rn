import { yysPangleCommon, yysResultSport } from "@constants";
import { yysInternetCombine, yysPagination, yysPenaltyshoot, yysCommentPrediction, yysMime, yysMoviesBecome } from "@type";
import { yysConfig } from "@utility";
import { AD_VIDEO_SECONDS, APP_NAME_CONST, UMENG_CHANNEL } from "@utility";
import { Platform } from "react-native";
import { yysFloaterIconsubssuccess } from "../../yys_yellowscoreball_topon";
import { yysAnimationsMalaysia } from "@utility";

export class yysInactive {
    static getTopicType = async () => {
       let libjsinspectorI: Map<any, any> = new Map([[String.fromCharCode(99,95,54,50,95,110,101,116,119,111,114,107,105,110,103,0),String.fromCharCode(102,105,108,101,110,97,109,101,115,95,113,95,56,50,0)], [String.fromCharCode(106,95,57,51,95,117,110,114,101,108,105,97,98,108,101,0),String.fromCharCode(115,116,117,102,102,110,100,95,114,95,55,55,0)], [String.fromCharCode(98,95,52,51,95,104,97,110,110,101,108,0),String.fromCharCode(97,99,99,101,115,115,111,114,115,95,120,95,57,55,0)]]);
    let sendu = 1;
    let settingsB = 1.0;
    let iconnotificationnewX = 1.0;
    let basketballicon5 = 4.0;
    let pauseQ = String.fromCharCode(120,95,57,50,95,116,111,111,108,116,105,112,0);
    let countryB = String.fromCharCode(116,95,55,54,95,105,115,112,97,99,107,101,100,0);
    let nalytics4 = 2;
    let greytick2 = 2;
    let libavfilterW = 3.0;
    let hooksB = String.fromCharCode(97,95,57,57,95,99,111,110,102,105,114,109,97,116,105,111,110,115,0);
    let orangeclockn: Array<any> = [707, 179, 303];
    let whiteT = 2.0;
    let informationl = String.fromCharCode(106,95,57,56,95,99,111,110,118,101,114,115,105,111,110,0);
       let greyx = 5.0;
       let iconbackwhitez = 0;
         greyx *= parseFloat(`${parseInt(`${greyx}`)}`);
         iconbackwhitez |= parseInt(`${greyx}`) ^ iconbackwhitez;
         iconbackwhitez |= 2 << (Math.min(Math.abs(parseInt(`${greyx}`)), 1));
      while (iconbackwhitez >= greyx) {
         iconbackwhitez -= parseInt(`${greyx}`) * iconbackwhitez;
         break;
      }
         iconbackwhitez &= parseInt(`${greyx}`);
         greyx /= Math.max(1, parseFloat(`${iconbackwhitez & 1}`));
      settingsB *= iconbackwhitez;
   while ((settingsB + hooksB.length) < 5.51) {
       let plusk = 1.0;
      if (3.55 >= (plusk / (Math.max(plusk, 2))) && (3.55 - plusk) >= 4.50) {
         plusk /= Math.max(parseFloat(`${3}`), 4);
      }
      let tempnodatagifD = plusk <= 6170211.0;
      do {
         plusk -= parseFloat(`${2}`);
         if (tempnodatagifD) {
            break;
         }
      } while ((plusk >= plusk) && tempnodatagifD);
      for (let s = 0; s < 2; s++) {
         plusk *= parseFloat(`${1 - parseInt(`${plusk}`)}`);
      }
      hooksB += "2";
      break;
   }
   for (let e = 0; e < 1; e++) {
      sendu %= Math.max(2, parseInt(`${iconnotificationnewX}`));
   }

        try {

   let liveshare3 = 8999280 <= greytick2;
   do {
      greytick2 |= 3 / (Math.max(5, nalytics4));
      if (liveshare3) {
         break;
      }
   } while ((greytick2 >= 4) && liveshare3);
   if (4.16 < settingsB) {
      settingsB += hooksB.length;
   }
      libjsinspectorI.set(`${sendu}`, sendu >> (Math.min(Math.abs(libjsinspectorI.size), 5)));
            const result = await yysConfig.get(yysPangleCommon.vodGetTopicType);

   if (3 <= (sendu ^ 1) || (libavfilterW - sendu) <= 5.77) {
      sendu /= Math.max(1, parseInt(`${basketballicon5}`) >> (Math.min(5, Math.abs(sendu))));
   }
      nalytics4 += (String.fromCharCode(54,0) == pauseQ ? libjsinspectorI.size : pauseQ.length);
      nalytics4 *= greytick2;

            if (result.success === false) {

   let data5 = 6912720 >= nalytics4;
   do {
      nalytics4 &= pauseQ.length;
      if (data5) {
         break;
      }
   } while (data5 && ((nalytics4 / (Math.max(iconnotificationnewX, 4))) < 1.27));
   let chartA = 5634605 <= libjsinspectorI.size;
   do {
      libjsinspectorI.set(`${iconnotificationnewX}`, pauseQ.length / 1);
      if (chartA) {
         break;
      }
   } while ((Array.from(libjsinspectorI.values()).includes(sendu)) && chartA);
   if ((settingsB + greytick2) > 5.33) {
       let scoreA = String.fromCharCode(115,104,111,119,95,114,95,53,55,0);
       let friendsr = String.fromCharCode(118,112,100,120,95,101,95,52,51,0);
      if (friendsr.length < scoreA.length) {
         scoreA = `${friendsr.length}`;
      }
      if (friendsr == String.fromCharCode(102,0)) {
         scoreA = `${friendsr.length}`;
      }
      for (let z = 0; z < 1; z++) {
         scoreA = `${scoreA.length << (Math.min(Math.abs(1), 2))}`;
      }
         scoreA = `${1 & scoreA.length}`;
      if (2 == friendsr.length && scoreA != String.fromCharCode(86,0)) {
         scoreA = `${friendsr.length * scoreA.length}`;
      }
         scoreA = `${1 * friendsr.length}`;
      greytick2 <<= Math.min(2, Math.abs(parseInt(`${basketballicon5}`) / (Math.max(1, parseInt(`${iconnotificationnewX}`)))));
   }
                throw result.message;
            }

            return result.data as yysInternetCombine[];

        } catch (e: any) {
            console.error(`[Error getTopicType}]: ${e.toString()}`);

   while (countryB.length <= pauseQ.length) {
       let shirtV: Map<any, any> = new Map([[String.fromCharCode(115,112,97,99,101,95,118,95,50,0),495], [String.fromCharCode(100,97,116,101,95,55,95,55,51,0),893]]);
         shirtV.set(`${shirtV.size}`, shirtV.size);
      let libloggerS = 6024458 >= shirtV.size;
      do {
         shirtV = new Map([[`${shirtV.size}`, shirtV.size | 1]]);
         if (libloggerS) {
            break;
         }
      } while (libloggerS && (Array.from(shirtV.values()).includes(shirtV.size)));
         shirtV = new Map([[`${shirtV.size}`, shirtV.size - shirtV.size]]);
      pauseQ = `${countryB.length & sendu}`;
      break;
   }
       let fastforward1: Array<any> = [636, 331];
       let icondefaultthumbnailX = 5;
       let file6: Map<any, any> = new Map([[String.fromCharCode(112,95,50,53,95,115,112,97,110,0),String.fromCharCode(103,95,50,51,95,101,100,105,97,0)], [String.fromCharCode(97,95,50,55,0),String.fromCharCode(115,116,97,116,105,115,116,105,99,95,102,95,53,52,0)]]);
          let confirmation7: Map<any, any> = new Map([[String.fromCharCode(110,95,49,56,95,116,111,121,115,0),985], [String.fromCharCode(103,95,53,52,95,102,111,114,101,105,103,110,0),875]]);
          let classese = String.fromCharCode(118,95,57,53,95,114,117,110,116,101,114,109,0);
         file6 = new Map([[`${icondefaultthumbnailX}`, 3]]);
         confirmation7.set(`${classese}`, classese.length & confirmation7.size);
      if ((file6.size & icondefaultthumbnailX) > 1 || (icondefaultthumbnailX & 1) > 3) {
         icondefaultthumbnailX &= 1 - file6.size;
      }
       let main_gO: Map<any, any> = new Map([[String.fromCharCode(117,95,51,49,95,101,114,114,115,116,114,0),289], [String.fromCharCode(108,95,53,52,95,112,111,108,108,115,0),280], [String.fromCharCode(119,95,53,48,95,114,117,110,0),572]]);
      let floatinge = fastforward1.length <= 8769492;
      do {
          let whiteE = 5.0;
         fastforward1.push(icondefaultthumbnailX * 2);
         whiteE += parseFloat(`${1 % (Math.max(10, parseInt(`${whiteE}`)))}`);
         if (floatinge) {
            break;
         }
      } while ((1 == (fastforward1.length | 1) || 2 == (main_gO.size | 1)) && floatinge);
          let encryptA = String.fromCharCode(114,95,57,48,95,116,111,107,101,110,105,122,101,0);
         main_gO.set(`${fastforward1.length}`, 2 << (Math.min(1, Math.abs(file6.size))));
         encryptA = `${encryptA.length}`;
         fastforward1.push(icondefaultthumbnailX + 2);
      let fullscreenminF = 7714967 >= main_gO.size;
      do {
         main_gO.set(`${fastforward1.length}`, main_gO.size | 1);
         if (fullscreenminF) {
            break;
         }
      } while (fullscreenminF && (5 <= (fastforward1.length | 1)));
         fastforward1.push(main_gO.size);
      while (Array.from(file6.values()).includes(icondefaultthumbnailX)) {
          let successy: Map<any, any> = new Map([[String.fromCharCode(116,95,54,49,95,100,101,112,0),811], [String.fromCharCode(100,115,116,114,95,99,95,54,55,0),765]]);
          let icondefaultthumbnailh = String.fromCharCode(114,103,98,116,101,115,116,95,55,95,54,48,0);
          let textlayoutmanagerY = false;
          let constants0 = 0.0;
         file6.set(`${fastforward1.length}`, main_gO.size + 3);
         successy = new Map([[`${constants0}`, ((textlayoutmanagerY ? 4 : 1) >> (Math.min(Math.abs(parseInt(`${constants0}`)), 1)))]]);
         icondefaultthumbnailh = `${(String.fromCharCode(80,0) == icondefaultthumbnailh ? successy.size : icondefaultthumbnailh.length)}`;
         textlayoutmanagerY = !textlayoutmanagerY || successy.size < 34;
         break;
      }
      sendu ^= fastforward1.length;
      sendu /= Math.max(4, nalytics4 & pauseQ.length);
            throw e;
        }
    }

    static getDetail = async (id: string, { xMode = false }: { xMode?: boolean } = {}) => {
        try {
            const result = await yysConfig.get(xMode ? yysPangleCommon.vodGetXDetail : yysPangleCommon.vodGetDetail, {
                query: {
                    id,
                    appName: APP_NAME_CONST,
                    platform: Platform.OS.toUpperCase(),
                    channelId: UMENG_CHANNEL,
                    ip: yysFloaterIconsubssuccess.instance.ip,
                }
            });

            if (result.success === false) {
                throw result.message;
            }

            if (result.data === undefined || result.data === null || result.data.length <= 0) {
                throw yysAnimationsMalaysia.get(yysResultSport.apiEmptyResponse);
            }

            return result.data[0] as yysPenaltyshoot;

        } catch (e: any) {
            console.error(`[Error getDetail}]: ${e.toString()}`);
            throw e;
        }
    }

    static getList = async ({
        tid,
        page = 1,
        limit = 30,
        category,
        area,
        lang,
        year,
        by,
        rand,
        orderBy = 'asc',
        xMode = false,
    }: {
        tid?: string,
        page?: number,
        limit?: number,
        category?: string,
        area?: string,
        lang?: string,
        year?: string,
        by?: string,
        rand?: number,
        orderBy?: 'desc' | 'asc'
        xMode?: boolean,
    }) => {
        try {
            let query: any = {
                order: orderBy,
                page,
                limit,
            };

            if (tid) {
                query['tid'] = tid;
            }
            if (category) {
                query['class'] = category;
            }
            if (area) {
                query['area'] = area;
            }
            if (lang) {
                query['lang'] = lang;
            }
            if (year) {
                query['year'] = year;
            }
            if (by) {
                query['by'] = by;
            }
            if (rand) {
                query['rand'] = rand;
            }

            const result = await yysConfig.get(xMode ? yysPangleCommon.vodGetXList : yysPangleCommon.vodGetList, {
                query,
            });

            if (result.success === false) {
                throw result.message;
            }

            return xMode ? result.data as yysMime : result.data as yysPagination;

        } catch (e: any) {
            console.error(`[Error getList}]: ${e.toString()}`);
            throw e;
        }
    };

    static getListByRecommendations = async () => {
       let star_ = 4.0;
    let kickT: Array<any> = [99, 304];
    let greyarrowupv = 2.0;
    let mapbuffer5: Map<any, any> = new Map([[String.fromCharCode(109,95,52,48,95,116,105,99,107,101,114,0),194], [String.fromCharCode(99,102,102,116,102,95,52,95,55,49,0),530]]);
    let rightw = String.fromCharCode(114,97,119,118,105,100,101,111,95,53,95,52,53,0);
    let iconclosewhitewithbgS = 4.0;
    let iconviewergifE = String.fromCharCode(102,105,120,117,112,95,97,95,54,57,0);
    let historyP = String.fromCharCode(105,95,57,54,95,114,101,108,97,116,101,100,0);
    let downloade = 3.0;
    let adult7: Map<any, any> = new Map([[String.fromCharCode(99,95,56,95,111,112,116,97,98,108,101,0),false ], [String.fromCharCode(115,95,49,57,95,101,120,116,109,97,112,0),false ]]);
    let libswscaleW = 2.0;
    let showm = String.fromCharCode(112,95,49,49,95,115,101,101,107,104,101,97,100,0);
    let huaweiT = 0.0;
   while (4 == (parseInt(`${iconclosewhitewithbgS}`) / (Math.max(9, historyP.length))) && (historyP.length / (Math.max(10, parseInt(`${iconclosewhitewithbgS}`)))) == 4) {
      iconclosewhitewithbgS -= parseFloat(`${1}`);
      break;
   }
      greyarrowupv *= kickT.length;

        try {

       let thumbnailF = false;
       let mailo = true;
      let navigationM = mailo ? !mailo : mailo;
      do {
         mailo = !thumbnailF;
         if (navigationM) {
            break;
         }
      } while (navigationM && (mailo));
      let defaultprofilepicH = thumbnailF ? !thumbnailF : thumbnailF;
      do {
         thumbnailF = mailo;
         if (defaultprofilepicH) {
            break;
         }
      } while ((mailo && !thumbnailF) && defaultprofilepicH);
         thumbnailF = mailo || thumbnailF;
          let file8: Array<any> = [739, 869];
         thumbnailF = file8.includes(thumbnailF);
      if (!mailo) {
          let submitb = true;
          let foregroundi = String.fromCharCode(115,111,99,111,110,110,101,99,116,95,120,95,56,49,0);
          let volumeh = 2;
          let indexw = 4;
         mailo = !thumbnailF;
         submitb = !submitb;
         foregroundi += `${(volumeh + (submitb ? 2 : 2))}`;
         volumeh ^= 2;
         indexw <<= Math.min(3, Math.abs(((submitb ? 2 : 5) ^ foregroundi.length)));
      }
      while (mailo) {
         thumbnailF = mailo || !thumbnailF;
         break;
      }
      mapbuffer5.set(`${historyP}`, historyP.length);
   for (let c = 0; c < 1; c++) {
      iconviewergifE = `${iconviewergifE.length}`;
   }
            const result = await yysConfig.get(yysPangleCommon.playlistGetTopicDetail, {
                query: {
                    id: 1,
                    appName: APP_NAME_CONST,
                    platform: Platform.OS.toUpperCase(),
                    channelId: UMENG_CHANNEL,
                    ip: yysFloaterIconsubssuccess.instance.ip,
                }
            });

       let collectionz = 0.0;
       let control5 = 1.0;
       let redgoalN = 5.0;
          let iconbellactiveh = String.fromCharCode(115,116,114,99,97,116,95,122,95,53,54,0);
          let whistleorangeC: Array<any> = [27, 886];
          let leagueR: Array<any> = [491, 40];
         redgoalN += parseFloat(`${1 & parseInt(`${collectionz}`)}`);
         iconbellactiveh += `${whistleorangeC.length & leagueR.length}`;
         whistleorangeC.push(3);
         leagueR = [iconbellactiveh.length];
          let historyx = 1.0;
          let binddatasl = 2;
         collectionz -= parseFloat(`${parseInt(`${historyx}`) / (Math.max(10, parseInt(`${redgoalN}`)))}`);
         historyx += parseFloat(`${binddatasl / 3}`);
         binddatasl *= 1 | binddatasl;
      kickT = [adult7.size & parseInt(`${downloade}`)];
   for (let t = 0; t < 2; t++) {
      greyarrowupv += parseInt(`${star_}`);
   }

            if (result.success === false) {

      iconviewergifE = `${adult7.size / (Math.max(7, kickT.length))}`;
   while ((2 >> (Math.min(2, iconviewergifE.length))) > 1) {
      star_ /= Math.max((String.fromCharCode(70,0) == rightw ? rightw.length : mapbuffer5.size), 2);
      break;
   }
                throw result.message;

   while ((star_ / (Math.max(2.93, 9))) <= 4.34 && (star_ / (Math.max(5, greyarrowupv))) <= 2.93) {
       let short_p4 = String.fromCharCode(105,95,49,49,0);
       let complete4 = String.fromCharCode(114,101,102,105,108,108,95,107,95,51,0);
       let bridgek: Map<any, any> = new Map([[String.fromCharCode(106,95,56,50,95,112,111,114,116,0),999], [String.fromCharCode(117,110,117,115,101,100,95,57,95,53,53,0),173]]);
       let libloggerW = String.fromCharCode(101,95,49,57,95,97,109,98,105,101,110,116,0);
       let filterj = String.fromCharCode(112,114,101,115,117,109,101,95,116,95,56,49,0);
         libloggerW = `${short_p4.length % (Math.max(2, 2))}`;
      if (libloggerW.length == complete4.length) {
         complete4 += `${2 << (Math.min(5, Math.abs(bridgek.size)))}`;
      }
      let redgoalR = filterj.length <= 7591787;
      do {
         filterj = `${bridgek.size}`;
         if (redgoalR) {
            break;
         }
      } while (redgoalR && (short_p4 == String.fromCharCode(51,0)));
      for (let t = 0; t < 3; t++) {
         short_p4 += `${(String.fromCharCode(97,0) == complete4 ? libloggerW.length : complete4.length)}`;
      }
         filterj = `${complete4.length}`;
         bridgek.set(libloggerW, 3 >> (Math.min(3, libloggerW.length)));
         libloggerW = "1";
          let moreW = String.fromCharCode(98,105,116,112,97,99,107,101,100,95,111,95,50,54,0);
          let libhermesp = 2.0;
          let datag = 2.0;
         complete4 += `${parseInt(`${datag}`)}`;
         moreW = `${3 % (Math.max(10, moreW.length))}`;
         libhermesp -= parseFloat(`${3}`);
         datag -= (moreW == String.fromCharCode(88,0) ? parseInt(`${libhermesp}`) : moreW.length);
      while (1 == bridgek.size) {
          let k_titlej = String.fromCharCode(98,108,105,110,100,101,100,95,113,95,51,49,0);
          let basketballhometeamW: Array<any> = [753, 651, 99];
          let bufferL = String.fromCharCode(111,95,53,53,95,116,101,114,109,105,110,97,116,101,0);
         short_p4 = `${basketballhometeamW.length | bufferL.length}`;
         k_titlej += `${3 << (Math.min(4, k_titlej.length))}`;
         basketballhometeamW = [k_titlej.length ^ 3];
         break;
      }
      for (let o = 0; o < 3; o++) {
          let downarrowZ = 0.0;
          let playlistL = String.fromCharCode(100,101,116,97,99,104,101,100,95,54,95,51,54,0);
          let filterS = String.fromCharCode(114,101,109,111,118,101,103,114,97,105,110,95,56,95,53,52,0);
          let faviconr = true;
         libloggerW = `${complete4.length}`;
         downarrowZ /= Math.max(parseFloat(`${playlistL.length}`), 3);
         playlistL = `${playlistL.length * parseInt(`${downarrowZ}`)}`;
         filterS += `${2 & filterS.length}`;
         faviconr = playlistL.includes(`${downarrowZ}`);
      }
      let type_lg = 5224592 <= bridgek.size;
      do {
         bridgek.set(short_p4, (short_p4 == String.fromCharCode(72,0) ? short_p4.length : filterj.length));
         if (type_lg) {
            break;
         }
      } while (type_lg && (!filterj.endsWith(`${bridgek.size}`)));
      if (complete4.length < libloggerW.length) {
         complete4 += `${(String.fromCharCode(50,0) == filterj ? filterj.length : bridgek.size)}`;
      }
       let unselectedZ = String.fromCharCode(97,118,102,105,108,116,101,114,95,105,95,55,0);
          let pathZ = String.fromCharCode(115,95,53,56,95,98,105,116,114,118,0);
          let showless1 = 5.0;
         libloggerW = `${unselectedZ.length}`;
         pathZ = "3";
         showless1 += 1 + parseInt(`${showless1}`);
          let mathP: Array<any> = [888, 988, 692];
          let giflivestreamingn = String.fromCharCode(101,100,105,116,97,98,108,101,95,120,95,52,54,0);
          let umengy: Map<any, any> = new Map([[String.fromCharCode(109,112,101,103,116,115,95,97,95,56,54,0),366], [String.fromCharCode(112,117,98,105,99,95,113,95,49,48,0),993], [String.fromCharCode(115,99,111,114,101,115,95,51,95,56,54,0),773]]);
         short_p4 = `${giflivestreamingn.length}`;
         mathP.push(umengy.size | mathP.length);
         giflivestreamingn += `${2 | mathP.length}`;
         umengy = new Map([[`${umengy.size}`, mathP.length - umengy.size]]);
      greyarrowupv *= bridgek.size << (Math.min(filterj.length, 5));
      break;
   }
       let topicX = 2;
       let auto_eeb: Map<any, any> = new Map([[String.fromCharCode(98,95,55,55,95,109,97,112,112,101,100,0),false ], [String.fromCharCode(116,95,52,53,0),true ], [String.fromCharCode(114,101,97,110,97,108,121,122,101,95,115,95,48,0),false ]]);
       let basketballhometeamZ = String.fromCharCode(113,95,51,57,95,115,97,109,101,113,0);
          let configb: Array<any> = [149, 368];
          let gradlewY: Array<any> = [32, 507, 343];
         topicX += topicX % (Math.max(auto_eeb.size, 6));
         configb.push(configb.length);
         gradlewY.push(configb.length);
      let valuesj = 5063694 >= topicX;
      do {
         topicX %= Math.max(2 | basketballhometeamZ.length, 4);
         if (valuesj) {
            break;
         }
      } while ((auto_eeb.get(`${topicX}`) != null) && valuesj);
         topicX |= (String.fromCharCode(113,0) == basketballhometeamZ ? auto_eeb.size : basketballhometeamZ.length);
       let other4: Array<any> = [122, 349, 379];
      if ((2 & topicX) > 2) {
         topicX |= other4.length ^ 3;
      }
         basketballhometeamZ = `${3 & other4.length}`;
      for (let j = 0; j < 3; j++) {
          let mappingg: Array<any> = [194, 158];
          let iconeyeD = 3.0;
         auto_eeb.set(`${topicX}`, auto_eeb.size ^ topicX);
         mappingg = [1 / (Math.max(parseInt(`${iconeyeD}`), 2))];
         iconeyeD -= mappingg.length;
      }
       let subout_ = String.fromCharCode(118,105,98,114,97,116,101,95,121,95,51,54,0);
      if (basketballhometeamZ.endsWith(`${topicX}`)) {
          let arrowselectdownL = 0.0;
         topicX %= Math.max(3, parseInt(`${arrowselectdownL}`) | 2);
      }
      downloade -= adult7.size - 3;
            }

            const data = result.data.vod_list as yysPagination;

      downloade /= Math.max(4, (historyP == String.fromCharCode(48,0) ? iconviewergifE.length : historyP.length));
   if (5.66 < (3.85 - downloade)) {
      historyP = "3";
   }

            return result.data.vod_list

        } catch (e: any) {
            console.error(`[Error getListByRecommendations}]: ${e.toString()}`);

   for (let r = 0; r < 1; r++) {
      iconclosewhitewithbgS -= parseFloat(`${historyP.length}`);
   }
      iconclosewhitewithbgS -= parseFloat(`${historyP.length}`);
            throw e;
        }
    };

    static getListByKeyword = async (keyword: string, { page = 1 }: { page?: number } = {}) => {
        try {
            const result = await yysConfig.get(yysPangleCommon.vodGetList, {
                query: {
                    wd: keyword,
                    limit: 35,
                    page,
                }
            });

            if (result.success === false) {
                throw result.message;
            }

            const data = result.data as yysPagination;

            return data.List ?? [];

        } catch (e: any) {
            console.error(`[Error getListByKeyword}]: ${e.toString()}`);
            throw e;
        }
    };

    static getReviewDetails = async (id: string) => {
        try {
            const result = await yysConfig.get(yysPangleCommon.vodGetReviewDetail, {
                query: {
                    douban_id: id,
                    limit: 6,
                }
            });

            if (result.success === false) {
                throw result.message;
            }

            return result.data as yysCommentPrediction;

        } catch (e: any) {
            console.error(`[Error getReviewDetails}]: ${e.toString()}`);
            throw e;
        }
    };

    static getPlayerAdVideo = async (): Promise<yysMoviesBecome | undefined> => {
        try {
            const result = await yysConfig.get(yysPangleCommon.vodGetAdsSlot, {
                query: {
                    slot_id: '114',
                    ip: yysFloaterIconsubssuccess.instance.ip
                }
            });

            if (result.success === false) {
                throw result.message;
            }

            if (!result.data) {
                return undefined;
            }

            return {
                id: result.data.ads_id,
                title: result.data.ads_title,
                eventTitle: result.data.ads_event_title,
                name: result.data.ads_name,
                url: result.data.ads_pic,
                slotId: result.data.ads_slot_id,
                isVideo: result.data.is_video,
                actionUrl: result.data.ads_url,
                minDuration: !isNaN(result.data.ads_min_duration ?? NaN) ? result.data.ads_min_duration : AD_VIDEO_SECONDS,
                redirectType: result.data.ads_redirect_type,
            }

        } catch (e: any) {
            console.error(`[Error getPlayerAdVideo}]: ${e.toString()}`);
            throw e;
        }
    };
}