import { mayi_Iconclosewhitewithbg, mayi_Turndown } from "@constants";
import { mayi_Signinup, mayi_YingSmall, mayi_EmptyGradle, mayi_EntryGradlew, mayi_Iconarrowright, mayi_Report } from "@type/mayi_green";
import { mayi_ReactBang } from "@utility/mayi_zhubo";
import { AD_VIDEO_SECONDS, APP_NAME_CONST, UMENG_CHANNEL } from "@utility/mayi_middleware_apps";
import { Platform } from "react-native";
import { mayi_Librrc } from "../../mayi_gift";
import { mayi_Flipper } from "@utility/mayi_mintegral_buffer";

export class mayi_CrossChat {
    static getTopicType = async () => {
       let orientation4 = String.fromCharCode(114,101,102,105,110,101,95,97,95,51,53,0);
    let favorite0 = 1.0;
    let libavformatV = true;
    let homeinactiveS = 3.0;
    let sharewhiteh = String.fromCharCode(118,95,56,49,95,112,97,103,101,104,97,115,104,0);
    let resendr = String.fromCharCode(115,95,49,49,95,115,117,98,112,105,120,101,108,0);
    let imagesk = String.fromCharCode(119,97,108,108,112,97,112,101,114,115,95,53,95,57,54,0);
    let playlistB: Array<any> = [635, 335, 164];
    let modalb = false;
    let renewi: Array<any> = [252, 803, 167];
    let arrowq = true;
    let iconadslinkh = String.fromCharCode(99,95,56,51,95,99,107,112,116,0);
    let verticalp: Array<any> = [182, 660, 297];
    let watchB = false;
   if ((playlistB.length ^ sharewhiteh.length) > 4 || 2 > (sharewhiteh.length ^ 4)) {
      playlistB.push(parseInt(`${favorite0}`) << (Math.min(2, Math.abs(2))));
   }
      renewi.push((1 + (arrowq ? 4 : 4)));
      orientation4 += `${sharewhiteh.length}`;
   while (!libavformatV) {
       let list7 = String.fromCharCode(122,95,54,56,95,99,111,108,108,101,99,116,105,98,108,101,0);
       let orangedownarrowl: Array<any> = [236, 27, 858];
       let expiredK = 1.0;
       let hoverd = String.fromCharCode(105,95,54,57,95,115,115,105,109,118,0);
       let teamdetailsbgE = 5.0;
      while (teamdetailsbgE > 1.56) {
         teamdetailsbgE /= Math.max(orangedownarrowl.length >> (Math.min(Math.abs(1), 3)), 3);
         break;
      }
          let penaltyshootP = String.fromCharCode(102,95,50,52,0);
          let fnewinterstitialt = 3.0;
          let editq = String.fromCharCode(116,114,105,101,115,95,51,95,49,48,48,0);
         teamdetailsbgE -= penaltyshootP.length + hoverd.length;
         penaltyshootP = `${editq.length}`;
         fnewinterstitialt *= parseInt(`${fnewinterstitialt}`) | editq.length;
      let update_zQ = 5295989 <= hoverd.length;
      do {
          let reactnativeultimatelistviewC = 3.0;
          let utilsW = 2;
          let viewsP = 3;
         hoverd = `${utilsW}`;
         reactnativeultimatelistviewC += parseFloat(`${2}`);
         utilsW %= Math.max(1, parseInt(`${reactnativeultimatelistviewC}`));
         viewsP ^= viewsP | 3;
         if (update_zQ) {
            break;
         }
      } while (update_zQ && (hoverd.length == parseInt(`${teamdetailsbgE}`)));
          let mbnativex = String.fromCharCode(109,100,101,99,95,54,95,57,52,0);
          let temperatured = 4;
          let renewX = 1;
         list7 = `${(String.fromCharCode(100,0) == mbnativex ? hoverd.length : mbnativex.length)}`;
         temperatured *= renewX;
         renewX ^= temperatured - 2;
         orangedownarrowl.push(orangedownarrowl.length);
          let videoX = String.fromCharCode(117,116,105,108,95,122,95,57,51,0);
         orangedownarrowl = [hoverd.length ^ 2];
         videoX = `${videoX.length}`;
      while (expiredK <= teamdetailsbgE) {
          let shrink1 = String.fromCharCode(100,105,115,116,97,110,99,101,95,105,95,56,54,0);
         expiredK /= Math.max(2, 3 - parseInt(`${expiredK}`));
         shrink1 = "3";
         break;
      }
         expiredK /= Math.max(2, 1);
         teamdetailsbgE += parseInt(`${teamdetailsbgE}`) | hoverd.length;
          let championj = 1;
         orangedownarrowl.push(list7.length | parseInt(`${teamdetailsbgE}`));
         championj %= Math.max(championj, 2);
         orangedownarrowl = [orangedownarrowl.length >> (Math.min(Math.abs(3), 3))];
      favorite0 /= Math.max(4, parseFloat(`${2}`));
      break;
   }

        try {

       let wnewsv = 2.0;
       let constants_ = String.fromCharCode(114,101,99,101,110,116,101,114,95,109,95,52,53,0);
         wnewsv *= parseFloat(`${parseInt(`${wnewsv}`)}`);
         constants_ = `${1 << (Math.min(4, constants_.length))}`;
      libavformatV = String.fromCharCode(113,0) == sharewhiteh && 5.18 >= favorite0;
   if (orientation4.endsWith(`${libavformatV}`)) {
       let shootyesgoals = 3.0;
       let penaltyshootF = String.fromCharCode(105,95,53,54,95,114,101,111,112,101,110,0);
      while ((penaltyshootF.length / (Math.max(1, 5))) >= 2) {
         shootyesgoals += 1 << (Math.min(1, Math.abs(parseInt(`${shootyesgoals}`))));
         break;
      }
       let iconh: Array<any> = [721, 627, 314];
      for (let q = 0; q < 2; q++) {
          let info8: Array<any> = [String.fromCharCode(116,95,53,56,95,105,112,118,0), String.fromCharCode(100,95,54,52,95,97,112,112,108,105,99,97,116,105,111,110,0), String.fromCharCode(116,114,101,101,116,111,107,95,107,95,50,52,0)];
          let eventy: Map<any, any> = new Map([[String.fromCharCode(116,104,101,109,101,115,95,101,95,54,49,0),366], [String.fromCharCode(99,104,97,110,103,101,114,101,102,95,54,95,55,50,0),948], [String.fromCharCode(107,95,57,49,95,100,95,55,52,0),992]]);
         penaltyshootF += "3";
         info8.push(3);
         eventy.set(`${info8.length}`, info8.length);
      }
      while (4.57 <= (shootyesgoals / 4.10) && (shootyesgoals / 4.10) <= 5.53) {
          let robotoM: Map<any, any> = new Map([[String.fromCharCode(116,114,105,97,108,95,102,95,56,56,0),448], [String.fromCharCode(109,95,53,54,0),656]]);
          let interneti = 0.0;
          let libnmsd = String.fromCharCode(114,95,57,53,0);
          let librrcZ = false;
          let predictionbanner4 = false;
         penaltyshootF += `${robotoM.size}`;
         robotoM = new Map([[`${interneti}`, ((librrcZ ? 1 : 5) | parseInt(`${interneti}`))]]);
         libnmsd += `${parseInt(`${interneti}`)}`;
         librrcZ = interneti == parseFloat(`${libnmsd.length}`);
         predictionbanner4 = !libnmsd.startsWith(`${interneti}`);
         break;
      }
         penaltyshootF = "2";
          let next9: Array<any> = [632, 890];
         shootyesgoals /= Math.max(1 >> (Math.min(4, iconh.length)), 1);
         next9 = [next9.length];
      orientation4 += `${2 ^ playlistB.length}`;
   }
   while (4 > (2 >> (Math.min(1, playlistB.length))) || (playlistB.length >> (Math.min(Math.abs(2), 5))) > 2) {
      playlistB = [playlistB.length / (Math.max(10, parseInt(`${homeinactiveS}`)))];
      break;
   }
   let cornerv = 6178451 >= sharewhiteh.length;
   do {
      sharewhiteh = `${((modalb ? 5 : 4) | (libavformatV ? 2 : 4))}`;
      if (cornerv) {
         break;
      }
   } while (cornerv && (orientation4.length == sharewhiteh.length));
            const result = await mayi_ReactBang.get(mayi_Iconclosewhitewithbg.vodGetTopicType);

      arrowq = !resendr.includes(`${arrowq}`);
   let downloadedT = 6129874 <= imagesk.length;
   do {
      imagesk += `${((libavformatV ? 2 : 4) * parseInt(`${homeinactiveS}`))}`;
      if (downloadedT) {
         break;
      }
   } while ((orientation4 != imagesk) && downloadedT);
   if ((parseInt(`${homeinactiveS}`) / (Math.max(sharewhiteh.length, 6))) > 3) {
      homeinactiveS /= Math.max((1 * (arrowq ? 4 : 2)), 2);
   }
   if (5 <= resendr.length) {
       let iconeditA = 0.0;
       let mutedO: Map<any, any> = new Map([[String.fromCharCode(121,95,51,49,95,97,116,116,114,98,117,116,101,0),220], [String.fromCharCode(99,97,100,101,110,99,101,95,107,95,49,0),436], [String.fromCharCode(112,105,99,107,101,114,95,105,95,50,52,0),931]]);
         iconeditA += parseFloat(`${parseInt(`${iconeditA}`)}`);
         iconeditA *= parseFloat(`${parseInt(`${iconeditA}`)}`);
       let mintegralD = 3;
       let traminiy = 4;
      while (1.15 >= (traminiy / (Math.max(7, iconeditA)))) {
         traminiy *= 1 >> (Math.min(4, Math.abs(mintegralD)));
         break;
      }
          let kickm = String.fromCharCode(105,95,49,55,95,105,100,120,0);
          let datap = 0;
          let thumbnails = String.fromCharCode(106,95,51,50,95,117,110,100,101,114,102,108,111,119,0);
         mintegralD >>= Math.min(Math.abs(kickm.length & mutedO.size), 1);
         kickm += `${thumbnails.length | 3}`;
         datap -= thumbnails.length % (Math.max(1, 7));
          let winda = 3.0;
          let sort7: Map<any, any> = new Map([[String.fromCharCode(97,117,116,111,112,108,97,121,105,110,103,95,119,95,56,51,0),298], [String.fromCharCode(109,95,49,49,95,103,101,116,110,97,109,101,105,110,102,111,0),119]]);
          let reducerh = String.fromCharCode(105,100,108,105,115,116,95,120,95,56,53,0);
         iconeditA -= parseFloat(`${mintegralD / 2}`);
         winda /= Math.max(4, 3);
         sort7 = new Map([[`${sort7.size}`, 3 ^ sort7.size]]);
         reducerh += `${sort7.size}`;
      arrowq = resendr.length <= 17 || !modalb;
   }

            if (result.success === false) {

       let emojiG = 4.0;
       let libflipperB = 2;
      while ((libflipperB - 1) < 2 || (libflipperB - parseInt(`${emojiG}`)) < 1) {
          let goale = String.fromCharCode(105,111,115,98,117,105,108,100,95,57,95,50,50,0);
         libflipperB |= 1;
         goale += `${goale.length}`;
         break;
      }
         emojiG += parseInt(`${emojiG}`);
      let z_lockT = 5533162 >= libflipperB;
      do {
          let audienceS = 0.0;
          let long_zil = false;
          let crownh = String.fromCharCode(116,95,55,95,100,101,115,99,114,0);
          let tail3 = String.fromCharCode(104,95,49,95,105,110,116,101,114,120,0);
         libflipperB *= crownh.length;
         audienceS *= parseFloat(`${parseInt(`${audienceS}`) & 3}`);
         long_zil = tail3.length >= 63;
         crownh += `${1 ^ parseInt(`${audienceS}`)}`;
         tail3 = `${(parseInt(`${audienceS}`) << (Math.min(4, Math.abs((long_zil ? 5 : 1)))))}`;
         if (z_lockT) {
            break;
         }
      } while ((2.70 >= (2.59 + emojiG)) && z_lockT);
         libflipperB ^= parseInt(`${emojiG}`) / (Math.max(9, libflipperB));
      if ((libflipperB + emojiG) < 1.34) {
          let mailI = 3.0;
          let register__r1: Array<any> = [562, 182];
          let vignette7 = 2.0;
          let elementso = 5.0;
         libflipperB += libflipperB;
         mailI -= parseFloat(`${1 * parseInt(`${vignette7}`)}`);
         register__r1 = [1 << (Math.min(5, register__r1.length))];
         vignette7 += parseInt(`${mailI}`);
         elementso -= parseFloat(`${parseInt(`${vignette7}`)}`);
      }
         emojiG *= 3 & parseInt(`${emojiG}`);
      libavformatV = String.fromCharCode(104,0) == sharewhiteh || playlistB.length >= 87;
       let goallogoZ = 0.0;
      for (let w = 0; w < 2; w++) {
         goallogoZ *= parseFloat(`${parseInt(`${goallogoZ}`)}`);
      }
      for (let a = 0; a < 2; a++) {
         goallogoZ /= Math.max(parseFloat(`${parseInt(`${goallogoZ}`) << (Math.min(2, Math.abs(3)))}`), 3);
      }
         goallogoZ -= parseFloat(`${parseInt(`${goallogoZ}`) ^ parseInt(`${goallogoZ}`)}`);
      modalb = (renewi.length - imagesk.length) <= 63;
       let dependencyk = String.fromCharCode(97,95,49,51,95,118,101,110,117,101,115,0);
       let areaS = true;
       let countdown5 = 2.0;
      if (dependencyk.length > 5 && !areaS) {
         dependencyk = `${dependencyk.length & parseInt(`${countdown5}`)}`;
      }
       let description_v_X = 0.0;
      for (let m = 0; m < 3; m++) {
         countdown5 += (String.fromCharCode(121,0) == dependencyk ? dependencyk.length : parseInt(`${description_v_X}`));
      }
       let colorsI = 2;
       let blacklistR = 2;
         dependencyk += "3";
      while (countdown5 > 5.56) {
         colorsI &= parseInt(`${countdown5}`) ^ 3;
         break;
      }
      while (!areaS) {
          let thumbnail7 = String.fromCharCode(101,95,49,50,95,102,97,115,116,116,101,115,116,0);
          let currentq = 4.0;
          let homeplayerB = String.fromCharCode(99,108,101,97,110,95,102,95,57,50,0);
         areaS = 27.22 > countdown5;
         thumbnail7 += `${2 - thumbnail7.length}`;
         currentq /= Math.max(2, parseFloat(`${homeplayerB.length & parseInt(`${currentq}`)}`));
         homeplayerB += `${thumbnail7.length ^ homeplayerB.length}`;
         break;
      }
      if (5.96 < (countdown5 * 1.78)) {
         countdown5 *= colorsI / 3;
      }
         dependencyk = `${(String.fromCharCode(88,0) == dependencyk ? dependencyk.length : colorsI)}`;
      modalb = countdown5 >= orientation4.length;
   if (1 >= (5 & playlistB.length) && (5 & playlistB.length) >= 5) {
       let commonu = String.fromCharCode(98,95,53,50,95,101,120,112,111,110,101,110,116,105,97,116,105,111,110,0);
       let hongkongw = 2.0;
      while (2 < (parseInt(`${hongkongw}`) + commonu.length) || (3.26 + hongkongw) < 1.50) {
          let logousery = String.fromCharCode(117,95,55,54,95,101,100,105,116,0);
          let giftbuttonK = 4.0;
          let reactnativejsx = 4.0;
         commonu = `${parseInt(`${giftbuttonK}`) / (Math.max(6, logousery.length))}`;
         logousery += "2";
         giftbuttonK -= 2 | parseInt(`${reactnativejsx}`);
         break;
      }
         commonu = `${3 ^ commonu.length}`;
      for (let m = 0; m < 2; m++) {
         hongkongw += parseFloat(`${3}`);
      }
          let bufferd = String.fromCharCode(101,109,112,116,121,95,109,95,49,56,0);
          let streamingU = String.fromCharCode(98,105,110,107,98,95,53,95,50,54,0);
         hongkongw += parseFloat(`${commonu.length}`);
         bufferd += `${streamingU.length << (Math.min(Math.abs(2), 3))}`;
         streamingU += `${(String.fromCharCode(104,0) == streamingU ? streamingU.length : bufferd.length)}`;
         commonu = `${3 & parseInt(`${hongkongw}`)}`;
      while (5.88 < (parseFloat(`${commonu.length}`) / (Math.max(7, hongkongw)))) {
         hongkongw -= parseFloat(`${commonu.length / 1}`);
         break;
      }
      playlistB = [(String.fromCharCode(69,0) == orientation4 ? (arrowq ? 1 : 2) : orientation4.length)];
   }
                throw result.message;
            }

            return result.data as mayi_Signinup[];

        } catch (e: any) {
            console.error(`[Error getTopicType}]: ${e.toString()}`);

   if ((3 << (Math.min(3, imagesk.length))) >= 1) {
      imagesk = `${(String.fromCharCode(111,0) == resendr ? resendr.length : playlistB.length)}`;
   }
      homeinactiveS -= ((modalb ? 4 : 1));
   let backw = arrowq ? !arrowq : arrowq;
   do {
      arrowq = 9.13 == favorite0 && orientation4 == String.fromCharCode(103,0);
      if (backw) {
         break;
      }
   } while ((arrowq) && backw);
   if (homeinactiveS >= 4.55 || (homeinactiveS - 4.55) >= 4.94) {
      libavformatV = !modalb;
   }
            throw e;
        }
    }

    static getDetail = async (id: string, typeId: string, vodSourceName = "", { xMode = false }: { xMode?: boolean } = {}) => {
        try {
            const result = await mayi_ReactBang.get(xMode ? mayi_Iconclosewhitewithbg.vodGetXDetail : mayi_Iconclosewhitewithbg.vodGetDetail, {
                query: {
                    id,
                    appName: APP_NAME_CONST,
                    platform: Platform.OS.toUpperCase(),
                    channelId: UMENG_CHANNEL,
                    ip: mayi_Librrc.instance.ip,
                    tid: typeId,
                    vod_source_name: vodSourceName
                }
            });
            
            if (result.success === false) {
                throw result.message;
            }

            if (result.data === undefined || result.data === null || result.data.length <= 0) {
                throw mayi_Flipper.get(mayi_Turndown.apiEmptyResponse);
            }
            return result.data[0] as mayi_EmptyGradle;

        } catch (e: any) {
            console.error(`[Error getDetail}]: ${e.toString()}`);
            throw e;
        }
    }

    static getList = async ({
        tid,
        page = 1,
        limit = 30,
        category,
        area,
        lang,
        year,
        by,
        rand,
        orderBy = 'asc',
        xMode = false,
        vod_source_name = "",
    }: {
        tid?: string,
        page?: number,
        limit?: number,
        category?: string,
        area?: string,
        lang?: string,
        year?: string,
        by?: string,
        rand?: number,
        orderBy?: 'desc' | 'asc'
        xMode?: boolean,
        vod_source_name?: string,
    }) => {
        try {
            let query: any = {
                order: orderBy,
                page,
                limit,
            };

            if (tid) {
                query['tid'] = tid;
            }
            if (category) {
                query['class'] = category;
            }
            if (area) {
                query['area'] = area;
            }
            if (lang) {
                query['lang'] = lang;
            }
            if (year) {
                query['year'] = year;
            }
            if (by) {
                query['by'] = by;
            }
            if (rand) {
                query['rand'] = rand;
            }
            if(vod_source_name != "" && xMode == true){
                query['vod_source_name'] = vod_source_name;
            }
            const result = await mayi_ReactBang.get(xMode ? mayi_Iconclosewhitewithbg.vodGetXList : mayi_Iconclosewhitewithbg.vodGetList, {
                query,
            });
            
            if (result.success === false) {
                throw result.message;
            }

            return xMode ? result.data as mayi_Iconarrowright : result.data as mayi_YingSmall;

        } catch (e: any) {
            console.error(`[Error getList}]: ${e.toString()}`);
            throw e;
        }
    };

    static getListByRecommendations = async () => {
       let hoverX: Array<any> = [String.fromCharCode(99,111,114,101,105,109,97,103,101,95,122,95,57,56,0), String.fromCharCode(120,95,55,50,95,103,105,102,115,0), String.fromCharCode(114,101,99,101,105,112,116,115,95,121,95,55,52,0)];
    let homeinactivey = String.fromCharCode(116,95,52,50,95,97,99,99,101,108,101,114,97,116,105,111,110,0);
    let nodeo = 1;
    let moditym: Map<any, any> = new Map([[String.fromCharCode(111,95,54,51,95,115,117,110,112,111,115,0),true ], [String.fromCharCode(114,101,99,101,110,116,95,50,95,51,54,0),false ]]);
    let window_rvG = String.fromCharCode(110,99,114,121,112,116,105,111,110,95,100,95,57,48,0);
    let chatroombackgroundV = String.fromCharCode(97,114,99,104,105,118,105,110,103,95,105,95,56,54,0);
    let volumed: Map<any, any> = new Map([[String.fromCharCode(97,117,116,104,111,114,95,105,95,49,57,0),414], [String.fromCharCode(115,97,108,116,95,115,95,55,55,0),745]]);
    let redirectv = true;
    let iconarrowrightwhitew = String.fromCharCode(109,95,56,52,95,102,105,108,108,101,100,0);
    let homeloading5 = String.fromCharCode(115,117,99,99,101,115,115,99,98,95,98,95,55,51,0);
    let comment5: Map<any, any> = new Map([[String.fromCharCode(112,114,111,103,114,101,115,115,101,115,95,100,95,55,51,0),675], [String.fromCharCode(121,95,52,55,95,115,112,101,99,105,102,105,101,100,0),31]]);
    let anytimef = 1;
    let helperM = 0;
    let helperL = 4;
    let relatedf = String.fromCharCode(112,105,110,115,95,50,95,49,54,0);
      comment5 = new Map([[`${comment5.size}`, nodeo]]);
      volumed = new Map([[homeloading5, 2 - window_rvG.length]]);
       let dangert = String.fromCharCode(97,108,116,101,114,110,97,116,105,111,110,95,55,95,54,50,0);
         dangert += `${2 / (Math.max(3, dangert.length))}`;
         dangert += `${(dangert == String.fromCharCode(86,0) ? dangert.length : dangert.length)}`;
       let componenta: Map<any, any> = new Map([[String.fromCharCode(112,95,52,48,95,115,121,110,99,104,114,111,110,105,122,101,0),42], [String.fromCharCode(114,101,109,97,112,95,101,95,53,57,0),223]]);
      homeloading5 = `${comment5.size + homeloading5.length}`;

        try {

       let androidv: Array<any> = [973, 821, 365];
       let pangleu = String.fromCharCode(108,111,111,112,98,114,101,97,107,95,103,95,52,52,0);
          let context0: Array<any> = [201, 202, 347];
         androidv = [1 | context0.length];
      while (pangleu.length > 1) {
          let sideT: Array<any> = [572, 555, 460];
          let whistleorange8 = 1.0;
         pangleu += `${androidv.length}`;
         sideT.push(2);
         whistleorange8 -= parseInt(`${whistleorange8}`);
         break;
      }
         androidv = [pangleu.length << (Math.min(Math.abs(3), 4))];
          let predictionbannerU = String.fromCharCode(115,99,116,112,99,111,110,110,95,111,95,51,49,0);
          let umenga = String.fromCharCode(105,100,102,118,95,54,95,52,56,0);
          let routerq: Array<any> = [863, 855, 898];
         androidv.push(pangleu.length << (Math.min(2, androidv.length)));
         predictionbannerU = `${predictionbannerU.length + umenga.length}`;
         umenga += `${(String.fromCharCode(120,0) == predictionbannerU ? predictionbannerU.length : umenga.length)}`;
         routerq.push(predictionbannerU.length / (Math.max(9, umenga.length)));
      while (3 == (androidv.length - 4)) {
          let firebasen = String.fromCharCode(114,101,98,97,115,101,95,119,95,55,56,0);
          let questd = String.fromCharCode(108,95,52,95,109,105,120,105,110,103,0);
         androidv = [1];
         firebasen = `${questd.length}`;
         questd += "1";
         break;
      }
      if (1 == (pangleu.length | androidv.length) || 1 == (pangleu.length | androidv.length)) {
         androidv = [pangleu.length];
      }
      nodeo <<= Math.min(Math.abs((String.fromCharCode(51,0) == pangleu ? pangleu.length : window_rvG.length)), 1);
   let whistleorangeD = 5008865 <= comment5.size;
   do {
       let hovert = 1;
       let predictionbuttont = 2.0;
       let photoK = String.fromCharCode(115,116,111,114,97,98,108,101,95,53,95,50,54,0);
         photoK = `${2 - photoK.length}`;
      while (1.90 > predictionbuttont) {
          let nbatrophyN = String.fromCharCode(111,95,53,49,95,97,110,97,108,111,103,0);
          let greenA = 1.0;
         photoK += `${hovert}`;
         nbatrophyN += "3";
         greenA *= parseInt(`${greenA}`);
         break;
      }
      while ((2 << (Math.min(4, photoK.length))) < 2) {
         predictionbuttont *= parseInt(`${predictionbuttont}`);
         break;
      }
         hovert >>= Math.min(Math.abs(parseInt(`${predictionbuttont}`) / (Math.max(2, 8))), 2);
         predictionbuttont /= Math.max(2, hovert / (Math.max(parseInt(`${predictionbuttont}`), 5)));
         predictionbuttont += hovert;
       let about2 = 3;
          let mbsplashR = 5;
          let moduleX = true;
          let condensed3 = String.fromCharCode(120,95,54,55,95,101,110,100,115,0);
         photoK += `${condensed3.length & 1}`;
         mbsplashR /= Math.max(mbsplashR, 3);
         moduleX = !moduleX;
         condensed3 = `${((moduleX ? 1 : 2) - mbsplashR)}`;
      while (5.14 >= predictionbuttont) {
         predictionbuttont += photoK.length;
         break;
      }
      comment5 = new Map([[chatroombackgroundV, 1]]);
      if (whistleorangeD) {
         break;
      }
   } while (whistleorangeD && (2 >= (comment5.size | window_rvG.length) && (2 | comment5.size) >= 1));
      redirectv = (57 <= (volumed.size ^ (!redirectv ? volumed.size : 52)));
            const result = await mayi_ReactBang.get(mayi_Iconclosewhitewithbg.playlistGetTopicDetail, {
                query: {
                    id: 1,
                    appName: APP_NAME_CONST,
                    platform: Platform.OS.toUpperCase(),
                    channelId: UMENG_CHANNEL,
                    ip: mayi_Librrc.instance.ip,
                }
            });

      volumed = new Map([[`${hoverX.length}`, hoverX.length % (Math.max(2, 9))]]);
      iconarrowrightwhitew = "2";
   while ((hoverX.length % (Math.max(comment5.size, 7))) == 2 && 2 == (hoverX.length % (Math.max(comment5.size, 9)))) {
       let roundh = 5;
      for (let j = 0; j < 3; j++) {
         roundh /= Math.max(2, 2 ^ roundh);
      }
          let auto_lS = String.fromCharCode(105,113,109,102,95,117,95,57,54,0);
          let moviesW = String.fromCharCode(115,95,54,56,95,114,97,100,98,103,0);
          let hejiS = 3;
         roundh ^= moviesW.length;
         auto_lS = `${auto_lS.length << (Math.min(Math.abs(1), 1))}`;
         moviesW = `${hejiS}`;
         hejiS |= hejiS;
      while (roundh <= roundh) {
         roundh += 2;
         break;
      }
      comment5.set(`${redirectv}`, 1 + comment5.size);
      break;
   }

            if (result.success === false) {

   while (2 <= (homeinactivey.length + 1) || 1 <= (volumed.size + homeinactivey.length)) {
      homeinactivey = `${iconarrowrightwhitew.length << (Math.min(Math.abs(1), 4))}`;
      break;
   }
       let currentW = String.fromCharCode(116,114,97,110,115,102,111,114,109,95,55,95,49,51,0);
       let telegramM = true;
      while (currentW.includes(`${telegramM}`)) {
         telegramM = currentW.length > 91 && telegramM;
         break;
      }
       let l_lockq = String.fromCharCode(104,95,55,57,95,115,116,114,111,110,103,0);
       let friendsF = String.fromCharCode(118,95,49,95,97,109,117,108,116,105,0);
       let phonex = String.fromCharCode(108,95,49,48,48,95,114,119,110,100,0);
       let single2 = String.fromCharCode(117,95,55,53,95,110,111,116,0);
      let closet = telegramM ? !telegramM : telegramM;
      do {
         telegramM = 56 >= single2.length;
         if (closet) {
            break;
         }
      } while (closet && (l_lockq.length >= 2));
         currentW = "1";
          let u_centerE = String.fromCharCode(111,118,101,114,104,101,97,100,95,99,95,49,48,0);
          let loadingq = 4.0;
          let toponQ: Map<any, any> = new Map([[String.fromCharCode(98,95,49,50,95,116,119,111,109,0),712], [String.fromCharCode(98,95,55,56,95,98,114,97,110,100,0),649]]);
         telegramM = 13 == l_lockq.length && u_centerE.length == 13;
         u_centerE = `${toponQ.size}`;
         loadingq /= Math.max(parseInt(`${loadingq}`), 2);
         toponQ = new Map([[`${toponQ.size}`, parseInt(`${loadingq}`) ^ toponQ.size]]);
      homeinactivey = `${nodeo}`;
   while ((3 + homeloading5.length) >= 3) {
      homeloading5 = `${window_rvG.length}`;
      break;
   }
                throw result.message;

   while ((moditym.size << (Math.min(4, hoverX.length))) > 3) {
       let mimoR = String.fromCharCode(105,110,116,101,114,97,99,116,105,111,110,115,95,111,95,54,56,0);
         mimoR = `${mimoR.length}`;
      while (mimoR.length < 5) {
         mimoR += `${(mimoR == String.fromCharCode(119,0) ? mimoR.length : mimoR.length)}`;
         break;
      }
      for (let p = 0; p < 2; p++) {
         mimoR = `${(mimoR == String.fromCharCode(114,0) ? mimoR.length : mimoR.length)}`;
      }
      hoverX = [homeloading5.length - iconarrowrightwhitew.length];
      break;
   }
   if (!homeloading5.startsWith(homeinactivey)) {
       let adult3 = String.fromCharCode(99,111,108,117,109,115,95,55,95,51,50,0);
       let watch5 = String.fromCharCode(100,101,97,108,108,111,99,95,122,95,54,56,0);
       let termsd: Array<any> = [200, 149];
          let imagemanagerd = 0.0;
         watch5 += "3";
         imagemanagerd -= parseFloat(`${1}`);
      while ((3 - watch5.length) <= 2 && 2 <= (termsd.length - 3)) {
         termsd = [watch5.length];
         break;
      }
         watch5 = `${termsd.length}`;
         watch5 = `${(watch5 == String.fromCharCode(74,0) ? termsd.length : watch5.length)}`;
      for (let o = 0; o < 3; o++) {
          let routerD = 4.0;
          let mbjscommonS = 3.0;
          let chatroombackgroundr: Map<any, any> = new Map([[String.fromCharCode(111,95,50,54,0),318], [String.fromCharCode(116,95,51,55,95,109,116,105,109,101,0),335]]);
          let stationsO = String.fromCharCode(115,121,109,101,118,101,110,95,119,95,48,0);
          let unselectedq = 4;
         watch5 += `${1 + stationsO.length}`;
         routerD /= Math.max(parseFloat(`${parseInt(`${mbjscommonS}`)}`), 5);
         mbjscommonS -= parseInt(`${routerD}`) * 1;
         chatroombackgroundr.set(`${routerD}`, chatroombackgroundr.size << (Math.min(Math.abs(2), 5)));
         stationsO += `${chatroombackgroundr.size}`;
         unselectedq ^= 3;
      }
         adult3 = `${3 >> (Math.min(5, termsd.length))}`;
         watch5 += `${(String.fromCharCode(78,0) == watch5 ? watch5.length : termsd.length)}`;
         termsd = [(String.fromCharCode(106,0) == watch5 ? adult3.length : watch5.length)];
         termsd.push(termsd.length & 2);
      homeloading5 += `${homeinactivey.length >> (Math.min(Math.abs(1), 3))}`;
   }
   if (window_rvG.startsWith(`${hoverX.length}`)) {
      hoverX = [volumed.size];
   }
            }

            const data = result.data.vod_list as mayi_YingSmall;

   let matchinactivey = comment5.size <= 7785519;
   do {
       let heartD = 3;
       let unselected7 = String.fromCharCode(99,97,112,97,98,108,101,95,114,95,57,51,0);
       let root8 = String.fromCharCode(105,95,57,54,95,102,108,97,103,0);
       let logouserY = String.fromCharCode(116,101,108,101,103,114,97,109,95,103,95,52,48,0);
      if ((heartD - 4) < 1) {
         heartD ^= 3;
      }
          let stringK = 5.0;
         root8 = `${logouserY.length}`;
         stringK += parseInt(`${stringK}`) - 3;
         heartD ^= (root8 == String.fromCharCode(55,0) ? heartD : root8.length);
         logouserY = "3";
         logouserY += "2";
         heartD /= Math.max(heartD, 2);
         heartD >>= Math.min(2, Math.abs(1));
      if (unselected7 != String.fromCharCode(111,0)) {
          let cornershootR: Map<any, any> = new Map([[String.fromCharCode(114,117,108,101,98,111,111,107,95,122,95,49,53,0),522], [String.fromCharCode(112,95,51,56,95,98,101,108,111,110,103,0),588], [String.fromCharCode(102,95,56,53,95,117,110,105,116,115,0),297]]);
          let ping0: Map<any, any> = new Map([[String.fromCharCode(115,105,103,110,97,108,115,95,98,95,57,51,0),String.fromCharCode(108,95,54,53,95,106,111,117,114,110,97,108,0)], [String.fromCharCode(115,119,105,116,99,104,101,100,95,53,95,55,51,0),String.fromCharCode(120,95,51,51,95,110,105,108,0)], [String.fromCharCode(101,112,97,102,95,119,95,52,53,0),String.fromCharCode(109,95,54,55,95,114,101,99,111,118,101,114,0)]]);
         logouserY = `${root8.length + 2}`;
         cornershootR = new Map([[`${cornershootR.size}`, cornershootR.size]]);
         ping0.set(`${cornershootR.size}`, 2 << (Math.min(4, Math.abs(ping0.size))));
      }
         logouserY = `${unselected7.length}`;
      while ((root8.length << (Math.min(Math.abs(4), 5))) > 3) {
         root8 += `${unselected7.length}`;
         break;
      }
          let lightW = 1.0;
          let product8: Map<any, any> = new Map([[String.fromCharCode(97,110,100,108,101,95,55,95,51,54,0),false ], [String.fromCharCode(116,112,101,108,100,115,112,95,112,95,57,53,0),false ]]);
          let resultB: Array<any> = [272, 517, 986];
         logouserY += `${3 ^ resultB.length}`;
         lightW /= Math.max(5, parseInt(`${lightW}`));
         product8.set(`${lightW}`, parseInt(`${lightW}`) - product8.size);
         resultB = [1 * product8.size];
      while (3 > logouserY.length || root8 == String.fromCharCode(108,0)) {
         root8 += `${heartD}`;
         break;
      }
      comment5 = new Map([[`${redirectv}`, (heartD + (redirectv ? 5 : 3))]]);
      if (matchinactivey) {
         break;
      }
   } while (matchinactivey && (1 >= (homeloading5.length - 3) && (3 - homeloading5.length) >= 4));
      hoverX = [chatroombackgroundV.length];
   if ((2 << (Math.min(2, Math.abs(moditym.size)))) == 2) {
       let tailJ = String.fromCharCode(97,114,103,98,105,95,52,95,57,49,0);
       let topon6 = 5.0;
         tailJ = `${tailJ.length}`;
      if (2.40 == topon6) {
          let cleara = String.fromCharCode(109,95,55,49,95,122,99,111,110,118,111,108,118,101,0);
         tailJ += `${parseInt(`${topon6}`)}`;
         cleara = "3";
      }
       let moduleF = true;
      for (let h = 0; h < 2; h++) {
         tailJ += `${tailJ.length}`;
      }
         moduleF = !tailJ.startsWith(`${moduleF}`);
          let turnp = true;
          let iconorientationB = 4;
         tailJ = "3";
         turnp = iconorientationB > 6;
         iconorientationB <<= Math.min(4, Math.abs(iconorientationB));
      moditym = new Map([[`${moditym.size}`, moditym.size]]);
   }

            return result.data.vod_list

        } catch (e: any) {
            console.error(`[Error getListByRecommendations}]: ${e.toString()}`);

   for (let l = 0; l < 2; l++) {
      comment5.set(`${hoverX.length}`, 1);
   }
   if (chatroombackgroundV.length < 4) {
      chatroombackgroundV += `${hoverX.length - homeinactivey.length}`;
   }
   if (4 < (iconarrowrightwhitew.length ^ nodeo)) {
       let rocket1 = 4.0;
       let iconviewergifz = 3;
       let iconsettingu = false;
      let libcrashsdkX = iconviewergifz <= 4905339;
      do {
          let defaultlogoP = true;
          let strV = String.fromCharCode(99,120,100,97,116,97,95,116,95,55,54,0);
          let fastJ = 4.0;
         iconviewergifz |= (parseInt(`${rocket1}`) >> (Math.min(1, Math.abs((iconsettingu ? 3 : 3)))));
         defaultlogoP = (40 < (strV.length * (defaultlogoP ? strV.length : 40)));
         fastJ *= (1 - (defaultlogoP ? 4 : 4));
         if (libcrashsdkX) {
            break;
         }
      } while ((4 <= (3 + iconviewergifz)) && libcrashsdkX);
      let complete9 = rocket1 <= 9206349.0;
      do {
          let bnewst = false;
          let favicond = 0.0;
          let weiboh = 5.0;
         rocket1 += (parseFloat(`${parseInt(`${rocket1}`) & (iconsettingu ? 3 : 4)}`));
         bnewst = favicond <= 6.3;
         favicond -= (parseFloat(`${parseInt(`${weiboh}`) >> (Math.min(1, Math.abs((bnewst ? 1 : 2))))}`));
         weiboh *= parseInt(`${weiboh}`) % 3;
         if (complete9) {
            break;
         }
      } while (((rocket1 + 2.60) <= 5.70 || 2.60 <= (parseFloat(`${iconviewergifz}`) + rocket1)) && complete9);
         rocket1 -= parseFloat(`${iconviewergifz}`);
      let whatsappC = iconsettingu ? !iconsettingu : iconsettingu;
      do {
         iconsettingu = 99.85 >= rocket1 && iconsettingu;
         if (whatsappC) {
            break;
         }
      } while ((1.14 < rocket1) && whatsappC);
          let signinupb = String.fromCharCode(101,120,116,114,97,102,105,101,108,100,95,97,95,54,0);
          let register_oz = String.fromCharCode(116,95,53,52,0);
          let bell7 = 5.0;
         rocket1 *= (parseFloat(`${(iconsettingu ? 2 : 3)}`));
         signinupb += `${parseInt(`${bell7}`) % (Math.max(register_oz.length, 6))}`;
         register_oz += `${1 | signinupb.length}`;
         bell7 /= Math.max(2, 1 % (Math.max(6, signinupb.length)));
       let libavformatl: Map<any, any> = new Map([[String.fromCharCode(112,95,49,53,95,105,102,111,114,109,97,116,0),414], [String.fromCharCode(101,109,111,106,105,95,52,95,48,0),325]]);
       let libimagepipeline6: Map<any, any> = new Map([[String.fromCharCode(99,97,115,116,95,119,95,52,53,0),String.fromCharCode(104,95,52,55,95,105,102,97,115,116,0)], [String.fromCharCode(101,95,55,57,95,99,116,120,116,0),String.fromCharCode(100,95,49,57,95,115,101,101,100,0)], [String.fromCharCode(99,104,97,110,103,101,115,95,53,95,56,56,0),String.fromCharCode(105,101,101,101,95,112,95,52,56,0)]]);
       let mbbid2 = true;
      while (2.13 == rocket1) {
         iconsettingu = null == libimagepipeline6.get(`${iconsettingu}`);
         break;
      }
          let iconwatchd = String.fromCharCode(118,95,56,53,95,116,114,97,110,115,105,116,105,111,110,115,0);
          let updatesO = 5.0;
         iconsettingu = libimagepipeline6.get(`${mbbid2}`) != null;
         iconwatchd += `${parseInt(`${updatesO}`) | 3}`;
         updatesO += parseFloat(`${iconwatchd.length}`);
      nodeo &= iconarrowrightwhitew.length;
   }
            throw e;
        }
    };

    static getListByKeyword = async (keyword: string, { page = 1 }: { page?: number } = {}) => {
        try {
            const result = await mayi_ReactBang.get(mayi_Iconclosewhitewithbg.vodGetList, {
                query: {
                    wd: keyword,
                    limit: 35,
                    page,
                }
            });

            if (result.success === false) {
                throw result.message;
            }

            const data = result.data as mayi_YingSmall;

            return data.List ?? [];

        } catch (e: any) {
            console.error(`[Error getListByKeyword}]: ${e.toString()}`);
            throw e;
        }
    };

    static getReviewDetails = async (id: string) => {
        try {
            const result = await mayi_ReactBang.get(mayi_Iconclosewhitewithbg.vodGetReviewDetail, {
                query: {
                    douban_id: id,
                    limit: 6,
                }
            });

            if (result.success === false) {
                throw result.message;
            }

            return result.data as mayi_EntryGradlew;

        } catch (e: any) {
            console.error(`[Error getReviewDetails}]: ${e.toString()}`);
            throw e;
        }
    };

    static getPlayerAdVideo = async (): Promise<mayi_Report | undefined> => {
        try {
            const result = await mayi_ReactBang.get(mayi_Iconclosewhitewithbg.vodGetAdsSlot, {
                query: {
                    slot_id: '114',
                    ip: mayi_Librrc.instance.ip
                }
            });

            if (result.success === false) {
                throw result.message;
            }

            if (!result.data) {
                return undefined;
            }

            return {
                id: result.data.ads_id,
                title: result.data.ads_title,
                eventTitle: result.data.ads_event_title,
                name: result.data.ads_name,
                url: result.data.ads_pic,
                slotId: result.data.ads_slot_id,
                isVideo: result.data.is_video,
                actionUrl: result.data.ads_url,
                minDuration: !isNaN(result.data.ads_min_duration ?? NaN) ? result.data.ads_min_duration : AD_VIDEO_SECONDS,
                redirectType: result.data.ads_redirect_type,
            }

        } catch (e: any) {
            console.error(`[Error getPlayerAdVideo}]: ${e.toString()}`);
            throw e;
        }
    };
}