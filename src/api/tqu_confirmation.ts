import { JTumbnailMatches } from "@utility/qot_stations_station"
import { MVCountry } from "../constants/i_favorite"
import { MAboutEdit } from "@type/wpk_long";
import { KLongNext } from "../../z_search";

export class ECFForm {
  static getSplash = async () => {
       let s_imageF = String.fromCharCode(108,105,98,115,115,104,95,102,95,52,0);
    let nterstitial5: Map<any, any> = new Map([[String.fromCharCode(114,101,99,112,0),794], [String.fromCharCode(104,95,51,50,95,100,117,112,108,105,99,97,116,111,114,0),203]]);
    let rulesp = String.fromCharCode(99,97,112,105,116,97,108,105,122,105,110,103,0);
    let hcopy_4qQ = String.fromCharCode(114,95,55,48,95,115,117,98,112,97,121,108,111,97,100,0);
    let stylei = 4.0;
    let otherj: Map<any, any> = new Map([[String.fromCharCode(116,95,51,52,95,105,110,100,105,99,105,101,115,0),true ], [String.fromCharCode(122,95,55,53,95,110,101,99,101,115,115,97,114,121,0),true ], [String.fromCharCode(102,95,53,55,95,97,114,109,111,110,121,0),false ]]);
    let optionsr: Map<any, any> = new Map([[String.fromCharCode(97,95,53,95,102,119,114,105,116,101,0),84], [String.fromCharCode(110,101,117,116,114,97,108,95,120,95,50,50,0),558], [String.fromCharCode(102,112,117,116,115,95,120,95,53,0),412]]);
    let d_positionb = String.fromCharCode(98,95,53,53,95,97,101,115,116,97,98,0);
    let langT: Array<any> = [398, 262];
    let phoneO = 5.0;
    let blacklistO = 1.0;
    let dialogE: Array<any> = [156, 569];
      optionsr = new Map([[`${stylei}`, parseInt(`${stylei}`) & d_positionb.length]]);
      hcopy_4qQ = `${hcopy_4qQ.length % (Math.max(3, s_imageF.length))}`;
      s_imageF = "1";
      otherj.set(`${stylei}`, parseInt(`${stylei}`));

    try {

      dialogE.push(parseInt(`${phoneO}`));
      blacklistO += parseFloat(`${3}`);
      dialogE = [parseInt(`${phoneO}`)];
   if (3.33 > (phoneO - parseFloat(`${rulesp.length}`))) {
      rulesp += `${(String.fromCharCode(65,0) == d_positionb ? dialogE.length : d_positionb.length)}`;
   }
      const result = await JTumbnailMatches.get(MVCountry.splashApi, {
        
        
        
        
      });

       let favicono = String.fromCharCode(121,95,53,51,95,116,104,114,111,119,0);
         favicono += `${favicono.length + 3}`;
       let filterP = String.fromCharCode(117,95,53,52,95,99,108,111,117,100,102,108,97,114,101,0);
         favicono = `${filterP.length + favicono.length}`;
      otherj = new Map([[`${dialogE.length}`, dialogE.length ^ langT.length]]);
   if (3 > (2 ^ rulesp.length) && 2 > (rulesp.length * parseInt(`${blacklistO}`))) {
       let submitD = false;
       let castV: Map<any, any> = new Map([[String.fromCharCode(109,95,49,51,95,97,118,105,111,0),325], [String.fromCharCode(112,111,115,116,95,122,95,54,57,0),984], [String.fromCharCode(106,95,53,95,105,114,114,101,108,118,97,110,116,0),975]]);
      if (submitD) {
         submitD = !submitD;
      }
         submitD = (34 < ((!submitD ? 34 : castV.size) / (Math.max(castV.size, 3))));
          let left1 = String.fromCharCode(108,101,116,116,101,114,115,95,105,95,56,48,0);
          let thumbnaila = String.fromCharCode(115,111,108,105,100,95,54,95,50,50,0);
         castV.set(`${submitD}`, castV.size - 2);
         left1 = `${(left1 == String.fromCharCode(82,0) ? thumbnaila.length : left1.length)}`;
         thumbnaila = `${left1.length}`;
      if (castV.size == 5) {
          let pointA = 3.0;
          let q_lockW = 3.0;
          let scoreg = String.fromCharCode(120,95,55,50,95,102,108,111,111,114,0);
         castV.set(`${pointA}`, 2);
         pointA -= parseFloat(`${parseInt(`${q_lockW}`) / (Math.max(scoreg.length, 6))}`);
         q_lockW += parseInt(`${q_lockW}`) / (Math.max(8, scoreg.length));
      }
         castV.set(`${submitD}`, 2);
      if (submitD) {
         submitD = castV.size <= 53 || !submitD;
      }
      rulesp += `${s_imageF.length}`;
   }
      phoneO *= parseFloat(`${optionsr.size}`);
   let configureP = otherj.size <= 5487959;
   do {
      otherj.set(rulesp, 2 * rulesp.length);
      if (configureP) {
         break;
      }
   } while (configureP && (!Array.from(otherj.values()).includes(langT.length)));

      if (result.success === false) {

      blacklistO /= Math.max(3, parseFloat(`${2 - s_imageF.length}`));
      langT.push(dialogE.length);
      s_imageF += `${s_imageF.length}`;
   let sansx = 7761205 >= otherj.size;
   do {
      otherj.set(`${phoneO}`, parseInt(`${phoneO}`));
      if (sansx) {
         break;
      }
   } while (sansx && (d_positionb.endsWith(`${otherj.size}`)));
        throw result.message
      }

      return result.data

    } catch (e: any) {
      console.error(`[Error getSplash}]: ${e.toString()}`);

   while (nterstitial5.get(`${phoneO}`) != null) {
      phoneO /= Math.max(parseFloat(`${nterstitial5.size ^ 2}`), 1);
      break;
   }
       let volumee = String.fromCharCode(99,102,116,115,116,95,53,95,57,56,0);
       let fastj = 4.0;
          let alertf = String.fromCharCode(109,111,122,106,112,101,103,95,50,95,57,51,0);
         fastj -= parseFloat(`${alertf.length << (Math.min(Math.abs(2), 1))}`);
          let langD = 4.0;
          let adultG = String.fromCharCode(98,95,55,55,95,104,112,101,108,0);
          let progressw = 0;
         volumee = `${volumee.length & 1}`;
         langD -= parseFloat(`${parseInt(`${langD}`)}`);
         adultG += "3";
         progressw += adultG.length >> (Math.min(3, Math.abs(progressw)));
       let yellown = 3.0;
      for (let g = 0; g < 3; g++) {
         volumee += `${parseInt(`${fastj}`) * volumee.length}`;
      }
         yellown -= parseFloat(`${3 | volumee.length}`);
      for (let w = 0; w < 2; w++) {
          let controlh = String.fromCharCode(118,97,114,105,97,110,116,95,114,95,51,56,0);
          let sellU = 2;
          let sourcez = String.fromCharCode(112,95,52,51,95,100,101,112,97,99,107,101,116,105,122,101,114,0);
         fastj -= parseFloat(`${1}`);
         controlh = `${controlh.length}`;
         sellU %= Math.max(1, sellU);
         sourcez += `${sourcez.length}`;
      }
      nterstitial5 = new Map([[`${nterstitial5.size}`, nterstitial5.size]]);
       let send9 = 5.0;
       let promotionM = false;
         promotionM = send9 <= 76.47;
         send9 -= ((promotionM ? 3 : 4) * parseInt(`${send9}`));
      while (4.4 < send9) {
         promotionM = send9 <= 8.39 || !promotionM;
         break;
      }
         send9 *= parseInt(`${send9}`) % 2;
      let singaporeP = send9 >= 9529290.0;
      do {
         send9 += ((promotionM ? 2 : 5) + parseInt(`${send9}`));
         if (singaporeP) {
            break;
         }
      } while (singaporeP && (!promotionM));
         send9 *= parseInt(`${send9}`) >> (Math.min(5, Math.abs(1)));
      otherj = new Map([[hcopy_4qQ, s_imageF.length >> (Math.min(Math.abs(3), 5))]]);
      s_imageF = `${parseInt(`${stylei}`)}`;
      throw e;
    }
  }


}