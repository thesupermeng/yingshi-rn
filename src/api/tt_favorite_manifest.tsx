import { ttGemfile, ttIncidentPhilippines } from "@constants";
import { ttMbridge, ttZoom, ttAppsOther, ttIncidentBaidu, ttCenter, ttBenefitManager } from "@type/tt_line_borderless";
import { ttReactnativejsMalaysia } from "@utility/tt_side_description";
import { AD_VIDEO_SECONDS, APP_NAME_CONST, UMENG_CHANNEL } from "@utility/tt_trophy_cross";
import { Platform } from "react-native";
import { ttConfigRecommendation } from "../../tt_copy_floater";
import { ttUnselectedMatch } from "@utility/tt_buffer_baidu";

export class ttConfirmationChinasame {
    static getTopicType = async () => {
       let commenty: Map<any, any> = new Map([[String.fromCharCode(114,95,55,49,95,100,98,115,105,122,101,0),String.fromCharCode(100,111,119,110,108,111,97,100,105,110,103,95,48,95,52,57,0)], [String.fromCharCode(116,95,55,50,95,102,108,97,99,0),String.fromCharCode(115,101,114,105,97,108,105,122,97,116,105,111,110,95,56,95,53,48,0)]]);
    let taiwanz = 3;
    let sansz = String.fromCharCode(97,114,109,111,110,121,95,114,95,49,48,0);
    let ball_: Map<any, any> = new Map([[String.fromCharCode(104,105,103,104,108,105,103,104,116,101,114,95,54,95,48,0),699], [String.fromCharCode(114,101,115,101,116,115,0),313]]);
    let clearT = 2;
    let themeY: Array<any> = [100, 95];
    let awayL = 0.0;
    let launchH = String.fromCharCode(116,95,51,49,95,98,111,111,116,104,0);
    let football9 = String.fromCharCode(106,112,101,103,108,115,100,101,99,95,102,95,49,54,0);
    let rules7: Map<any, any> = new Map([[String.fromCharCode(103,95,57,50,95,115,98,99,100,101,99,0),872], [String.fromCharCode(99,97,116,101,103,111,114,121,95,53,95,53,56,0),199]]);
    let bottomP = 4.0;
   for (let q = 0; q < 2; q++) {
      commenty = new Map([[launchH, parseInt(`${awayL}`) >> (Math.min(launchH.length, 2))]]);
   }
       let progressi: Map<any, any> = new Map([[String.fromCharCode(100,105,121,102,112,95,107,95,51,48,0),609], [String.fromCharCode(118,95,53,56,95,112,97,115,115,99,111,100,101,0),182]]);
      if (Array.from(progressi.values()).includes(progressi.size)) {
          let yellowe = String.fromCharCode(118,95,57,56,95,116,114,97,105,108,101,114,0);
          let playZ = String.fromCharCode(97,119,97,105,116,105,110,103,95,51,95,50,55,0);
          let configz = 3.0;
         progressi = new Map([[`${progressi.size}`, playZ.length]]);
         yellowe = `${parseInt(`${configz}`) % (Math.max(3, yellowe.length))}`;
         playZ = `${parseInt(`${configz}`)}`;
      }
         progressi = new Map([[`${progressi.size}`, progressi.size ^ progressi.size]]);
         progressi.set(`${progressi.size}`, progressi.size);
      clearT &= 3;
   let gift_ = 8727117 >= clearT;
   do {
      clearT -= commenty.size - 2;
      if (gift_) {
         break;
      }
   } while (gift_ && (!Array.from(commenty.values()).includes(clearT)));

        try {

      ball_.set(sansz, sansz.length << (Math.min(2, launchH.length)));
      taiwanz *= taiwanz;
       let hook2: Array<any> = [String.fromCharCode(114,101,97,115,115,101,109,98,108,121,0), String.fromCharCode(113,95,56,53,95,112,115,101,117,100,111,99,111,108,111,114,0)];
       let leaguel = String.fromCharCode(112,95,51,53,95,109,101,115,115,97,103,105,110,103,0);
       let loginU = String.fromCharCode(99,95,54,49,95,104,119,99,111,110,116,101,120,116,0);
          let rewardm = 3.0;
          let unselected4 = 1;
         hook2 = [parseInt(`${rewardm}`) + unselected4];
      let countryo = 6961730 <= hook2.length;
      do {
         hook2 = [2];
         if (countryo) {
            break;
         }
      } while (countryo && (1 == (1 ^ hook2.length) && (1 ^ leaguel.length) == 5));
      themeY.push(parseInt(`${awayL}`) >> (Math.min(5, Math.abs(3))));
            const result = await ttReactnativejsMalaysia.get(ttGemfile.vodGetTopicType);

   if (!sansz.endsWith(`${awayL}`)) {
       let unticks = String.fromCharCode(115,111,117,110,0);
         unticks += "2";
       let source2 = String.fromCharCode(108,95,53,50,95,102,115,112,112,0);
       let reactnativejsW = String.fromCharCode(98,101,103,105,110,110,105,110,103,95,55,95,53,56,0);
      for (let m = 0; m < 1; m++) {
         unticks += `${unticks.length * reactnativejsW.length}`;
      }
      awayL += 2 + parseInt(`${awayL}`);
   }
      sansz = `${commenty.size / 2}`;
       let greyr = String.fromCharCode(115,101,97,100,95,114,95,54,51,0);
       let customV = true;
      let analyticsM = String.fromCharCode(111,104,115,54,106,105,104,0) == greyr;
      do {
          let humidityV: Array<any> = [String.fromCharCode(116,114,97,110,115,102,111,114,109,97,116,105,111,110,95,115,95,54,0), String.fromCharCode(119,95,55,48,95,105,110,116,108,101,0)];
          let megaphones = String.fromCharCode(109,95,57,95,114,101,112,108,97,99,101,109,101,110,116,0);
         greyr += `${(megaphones.length & (customV ? 5 : 3))}`;
         humidityV.push(3 - humidityV.length);
         megaphones = `${humidityV.length}`;
         if (analyticsM) {
            break;
         }
      } while (analyticsM && (customV));
         greyr += `${(greyr.length << (Math.min(4, Math.abs((customV ? 1 : 2)))))}`;
      for (let u = 0; u < 1; u++) {
         greyr += `${greyr.length}`;
      }
       let rightu: Map<any, any> = new Map([[String.fromCharCode(121,95,57,49,95,116,104,105,114,100,0),String.fromCharCode(97,95,56,56,95,110,101,103,111,116,105,97,116,101,0)], [String.fromCharCode(102,101,97,116,117,114,101,100,0),String.fromCharCode(115,95,55,56,95,99,111,110,115,105,115,116,101,110,99,121,0)]]);
       let singlex = 1.0;
         customV = (((customV ? 96 : greyr.length) - greyr.length) == 96);
      awayL += 2;

            if (result.success === false) {

      awayL += taiwanz & commenty.size;
       let jnews3 = false;
          let clearQ = String.fromCharCode(99,95,55,55,95,115,99,111,114,101,0);
          let register_41k = String.fromCharCode(98,97,100,95,114,95,55,54,0);
          let indexp = true;
         jnews3 = (clearQ.length % (Math.max(3, register_41k.length))) > 37;
         clearQ += `${((indexp ? 2 : 2) ^ 1)}`;
         register_41k += `${(3 / (Math.max(4, (indexp ? 1 : 3))))}`;
         jnews3 = (!jnews3 ? jnews3 : jnews3);
         jnews3 = !jnews3;
      sansz += "3";
       let soundn = String.fromCharCode(98,97,110,100,95,116,95,53,49,0);
       let downM = 5.0;
       let awayk = true;
         downM += soundn.length | parseInt(`${downM}`);
      let viewert = String.fromCharCode(51,108,52,50,99,99,117,116,101,54,0) == soundn;
      do {
         soundn += `${(parseInt(`${downM}`) | (awayk ? 3 : 5))}`;
         if (viewert) {
            break;
         }
      } while (((soundn.length + parseInt(`${downM}`)) > 3) && viewert);
      while (downM >= 4.25) {
         soundn += `${parseInt(`${downM}`) ^ 1}`;
         break;
      }
         downM += soundn.length + 1;
          let reminderH = String.fromCharCode(103,95,50,57,95,115,116,114,99,115,112,110,0);
          let private_hlm = 2.0;
          let colorsZ: Map<any, any> = new Map([[String.fromCharCode(113,95,52,49,95,103,97,112,115,0),90], [String.fromCharCode(103,105,116,104,117,98,95,121,95,50,0),822]]);
         awayk = soundn.length <= parseInt(`${downM}`);
         reminderH = `${colorsZ.size}`;
         private_hlm += parseFloat(`${parseInt(`${private_hlm}`)}`);
         colorsZ = new Map([[reminderH, reminderH.length >> (Math.min(Math.abs(2), 5))]]);
          let backwardk: Map<any, any> = new Map([[String.fromCharCode(122,111,111,109,0),true ], [String.fromCharCode(120,95,53,52,95,101,120,105,115,116,97,110,99,101,0),true ]]);
         downM -= 1;
         backwardk = new Map([[`${backwardk.size}`, 2 | backwardk.size]]);
      if (!soundn.endsWith(`${downM}`)) {
          let models_ = String.fromCharCode(108,97,116,116,105,99,101,95,110,95,49,56,0);
          let utilsn = false;
          let playn = String.fromCharCode(114,111,116,114,95,113,95,55,54,0);
          let securityu = String.fromCharCode(99,114,101,97,116,101,101,120,95,106,95,56,54,0);
         downM += ((utilsn ? 4 : 4) * securityu.length);
         models_ += `${models_.length}`;
         utilsn = models_ == playn;
         playn += `${playn.length << (Math.min(Math.abs(1), 2))}`;
         securityu += `${playn.length}`;
      }
      for (let q = 0; q < 2; q++) {
         soundn = `${((awayk ? 2 : 3) + parseInt(`${downM}`))}`;
      }
      if (!soundn.endsWith(`${downM}`)) {
          let regengP = String.fromCharCode(116,101,108,101,116,101,120,116,95,101,95,53,57,0);
          let weiboG = 0.0;
          let sheet9 = String.fromCharCode(112,97,103,101,110,117,109,98,101,114,95,119,95,54,0);
          let appsM = 3;
          let modalq = 5;
         soundn += `${modalq}`;
         regengP = `${3 + appsM}`;
         weiboG *= parseFloat(`${parseInt(`${weiboG}`) + regengP.length}`);
         sheet9 = "1";
         appsM *= 1;
         modalq %= Math.max(regengP.length * sheet9.length, 3);
      }
      awayL += 3;
                throw result.message;
            }

            return result.data as ttMbridge[];

        } catch (e: any) {
            console.error(`[Error getTopicType}]: ${e.toString()}`);

       let c_countv: Array<any> = [198, 788, 518];
         c_countv = [c_countv.length + 2];
      let friendsq = c_countv.length <= 5471493;
      do {
         c_countv = [c_countv.length + c_countv.length];
         if (friendsq) {
            break;
         }
      } while (friendsq && (2 == (c_countv.length * 5) && 2 == (c_countv.length * 5)));
          let package_1qt = 5.0;
          let type_ujh = 0.0;
         c_countv = [parseInt(`${type_ujh}`)];
         package_1qt /= Math.max(parseFloat(`${parseInt(`${package_1qt}`) + parseInt(`${package_1qt}`)}`), 2);
      sansz += `${football9.length << (Math.min(Math.abs(1), 2))}`;
   let forwardJ = 8185770 <= ball_.size;
   do {
      ball_.set(`${sansz}`, sansz.length);
      if (forwardJ) {
         break;
      }
   } while (forwardJ && (ball_.get(`${clearT}`) == null));
      clearT *= clearT;
            throw e;
        }
    }

    static getDetail = async (id: string, typeId: string, vodSourceName = "", { xMode = false }: { xMode?: boolean } = {}) => {
        try {
            const result = await ttReactnativejsMalaysia.get(xMode ? ttGemfile.vodGetXDetail : ttGemfile.vodGetDetail, {
                query: {
                    id,
                    appName: APP_NAME_CONST,
                    platform: Platform.OS.toUpperCase(),
                    channelId: UMENG_CHANNEL,
                    ip: ttConfigRecommendation.instance.ip,
                    tid: typeId,
                    vod_source_name: vodSourceName
                }
            });
            
            if (result.success === false) {
                throw result.message;
            }

            if (result.data === undefined || result.data === null || result.data.length <= 0) {
                throw ttUnselectedMatch.get(ttIncidentPhilippines.apiEmptyResponse);
            }
            return result.data[0] as ttAppsOther;

        } catch (e: any) {
            console.error(`[Error getDetail}]: ${e.toString()}`);
            throw e;
        }
    }

    static getList = async ({
        tid,
        page = 1,
        limit = 30,
        category,
        area,
        lang,
        year,
        by,
        rand,
        orderBy = 'asc',
        xMode = false,
        vod_source_name = "",
    }: {
        tid?: string,
        page?: number,
        limit?: number,
        category?: string,
        area?: string,
        lang?: string,
        year?: string,
        by?: string,
        rand?: number,
        orderBy?: 'desc' | 'asc'
        xMode?: boolean,
        vod_source_name?: string,
    }) => {
        try {
            let query: any = {
                order: orderBy,
                page,
                limit,
            };

            if (tid) {
                query['tid'] = tid;
            }
            if (category) {
                query['class'] = category;
            }
            if (area) {
                query['area'] = area;
            }
            if (lang) {
                query['lang'] = lang;
            }
            if (year) {
                query['year'] = year;
            }
            if (by) {
                query['by'] = by;
            }
            if (rand) {
                query['rand'] = rand;
            }
            if(vod_source_name != "" && xMode == true){
                query['vod_source_name'] = vod_source_name;
            }
            const result = await ttReactnativejsMalaysia.get(xMode ? ttGemfile.vodGetXList : ttGemfile.vodGetList, {
                query,
            });
            
            if (result.success === false) {
                throw result.message;
            }

            return xMode ? result.data as ttCenter : result.data as ttZoom;

        } catch (e: any) {
            console.error(`[Error getList}]: ${e.toString()}`);
            throw e;
        }
    };

    static getListByRecommendations = async () => {
       let sportsM: Array<any> = [710, 855, 154];
    let shrinkH = 5.0;
    let reports = String.fromCharCode(122,95,52,57,95,116,105,103,103,108,101,0);
    let injurye = false;
    let alertx: Map<any, any> = new Map([[String.fromCharCode(118,95,49,55,95,97,112,102,115,0),true ], [String.fromCharCode(115,95,50,52,95,111,110,101,115,99,97,108,101,0),true ]]);
    let showv: Array<any> = [String.fromCharCode(115,101,99,111,110,100,95,50,95,51,53,0), String.fromCharCode(112,114,111,112,101,114,121,95,107,95,55,48,0), String.fromCharCode(121,95,52,55,95,109,97,110,97,103,101,114,0)];
    let settingsW = String.fromCharCode(105,95,57,51,95,103,114,97,110,117,108,101,0);
    let z_centerj: Map<any, any> = new Map([[String.fromCharCode(107,109,115,95,100,95,53,49,0),821], [String.fromCharCode(115,99,117,98,98,101,114,95,120,95,53,57,0),180], [String.fromCharCode(101,95,52,55,95,115,116,114,99,109,112,0),605]]);
    let membershipK = String.fromCharCode(100,105,116,97,98,108,101,95,113,95,57,55,0);
    let ewardedq = 3.0;
    let combineS: Array<any> = [641, 918, 26];
    let arrowJ = String.fromCharCode(111,105,100,95,108,95,57,56,0);
    let flippere = String.fromCharCode(98,111,120,95,55,95,54,50,0);
    let nextj = 2;
    let minimizeJ = String.fromCharCode(97,117,116,111,100,101,116,101,99,116,111,114,95,108,95,51,55,0);
    let corner7: Map<any, any> = new Map([[String.fromCharCode(111,95,56,57,95,114,101,115,111,108,118,0),String.fromCharCode(97,99,116,105,118,97,116,101,95,98,95,50,54,0)], [String.fromCharCode(99,97,118,108,99,95,121,95,56,54,0),String.fromCharCode(97,103,97,105,110,115,116,0)]]);
      showv = [membershipK.length / (Math.max(1, 3))];
   while ((3 ^ settingsW.length) == 5) {
      settingsW += "3";
      break;
   }

        try {

      settingsW += `${((injurye ? 2 : 3) % (Math.max(sportsM.length, 10)))}`;
   while ((3 >> (Math.min(1, Math.abs(z_centerj.size)))) > 4 && 3 > (sportsM.length >> (Math.min(Math.abs(3), 2)))) {
      z_centerj.set(`${injurye}`, 1 << (Math.min(4, Math.abs(alertx.size))));
      break;
   }
            const result = await ttReactnativejsMalaysia.get(ttGemfile.playlistGetTopicDetail, {
                query: {
                    id: 1,
                    appName: APP_NAME_CONST,
                    platform: Platform.OS.toUpperCase(),
                    channelId: UMENG_CHANNEL,
                    ip: ttConfigRecommendation.instance.ip,
                }
            });

      showv.push(((injurye ? 2 : 1) | 2));
      membershipK += `${settingsW.length | 1}`;

            if (result.success === false) {

      arrowJ += `${settingsW.length}`;
   if ((showv.length + alertx.size) >= 5) {
      alertx.set(`${injurye}`, membershipK.length);
   }
                throw result.message;

       let megaphoneV: Array<any> = [417, 439];
      while (!megaphoneV.includes(megaphoneV.length)) {
          let gemfilel: Map<any, any> = new Map([[String.fromCharCode(101,95,49,49,95,97,100,109,105,110,0),true ], [String.fromCharCode(106,112,101,103,116,114,97,110,95,49,95,50,0),true ], [String.fromCharCode(115,99,116,101,95,103,95,57,48,0),true ]]);
          let dycreatorD = 5;
          let expandq = 5.0;
          let shoot1 = 0.0;
         megaphoneV.push(parseInt(`${shoot1}`) / 2);
         gemfilel = new Map([[`${gemfilel.size}`, 2 + dycreatorD]]);
         dycreatorD ^= 2;
         expandq *= parseFloat(`${parseInt(`${expandq}`)}`);
         shoot1 /= Math.max(1, parseFloat(`${1}`));
         break;
      }
      let targetn = megaphoneV.length >= 5433512;
      do {
         megaphoneV = [1];
         if (targetn) {
            break;
         }
      } while (targetn && (3 > (3 | megaphoneV.length) || (3 | megaphoneV.length) > 1));
          let ajaxY = String.fromCharCode(99,95,56,54,0);
          let animatione = false;
         megaphoneV.push(megaphoneV.length << (Math.min(Math.abs(3), 3)));
         ajaxY = `${((animatione ? 2 : 2))}`;
         animatione = ajaxY.length <= 60;
      ewardedq /= Math.max(2, parseFloat(`${arrowJ.length}`));
   let modelsO = settingsW == String.fromCharCode(95,111,49,107,98,56,50,107,108,0);
   do {
      settingsW = `${(settingsW == String.fromCharCode(97,0) ? settingsW.length : arrowJ.length)}`;
      if (modelsO) {
         break;
      }
   } while (modelsO && (1 == showv.length));
            }

            const data = result.data.vod_list as ttZoom;

       let moviesU = 3;
         moviesU += moviesU >> (Math.min(Math.abs(moviesU), 2));
         moviesU -= moviesU;
      for (let u = 0; u < 3; u++) {
         moviesU *= 1;
      }
      combineS.push(2);
   if (3.21 > shrinkH) {
      shrinkH += combineS.length;
   }

            return result.data.vod_list

        } catch (e: any) {
            console.error(`[Error getListByRecommendations}]: ${e.toString()}`);

       let catagoryY = 4.0;
       let modelI = 5.0;
       let googled: Array<any> = [String.fromCharCode(99,108,111,115,101,95,122,95,51,48,0), String.fromCharCode(97,102,116,101,114,108,105,102,101,0), String.fromCharCode(109,98,117,102,95,52,95,51,54,0)];
      for (let t = 0; t < 1; t++) {
          let renew9 = String.fromCharCode(114,95,56,55,95,101,102,102,101,99,116,0);
          let indicatorb = 0.0;
          let configp = String.fromCharCode(112,114,101,118,105,101,119,95,97,95,54,49,0);
         modelI *= 3;
         renew9 += `${parseInt(`${indicatorb}`)}`;
         indicatorb /= Math.max(4, 3);
         configp = `${3 + parseInt(`${indicatorb}`)}`;
      }
      let successz = 9746785.0 >= modelI;
      do {
         modelI /= Math.max(googled.length, 2);
         if (successz) {
            break;
         }
      } while (((catagoryY / (Math.max(modelI, 5))) < 2.43 && (catagoryY / (Math.max(7, modelI))) < 2.43) && successz);
      while (3.10 <= (catagoryY + 4.89) || (googled.length >> (Math.min(Math.abs(4), 4))) <= 5) {
         catagoryY *= 3 - googled.length;
         break;
      }
      while (1.44 == (5.49 * modelI) && (modelI - catagoryY) == 5.49) {
          let suggestionv: Array<any> = [188, 413];
          let minivodl = 3;
          let cleard = String.fromCharCode(122,95,55,51,95,115,117,98,116,114,101,101,115,0);
          let playercommon4 = String.fromCharCode(115,111,102,116,102,108,111,97,116,95,51,95,55,54,0);
          let play2: Array<any> = [String.fromCharCode(119,111,114,107,105,110,103,95,53,95,54,54,0), String.fromCharCode(103,95,56,53,95,99,111,102,97,99,116,111,114,0), String.fromCharCode(106,95,50,53,0)];
         modelI *= 3 << (Math.min(3, play2.length));
         suggestionv.push(suggestionv.length);
         minivodl /= Math.max(1 % (Math.max(8, suggestionv.length)), 1);
         cleard = "1";
         playercommon4 = `${3 / (Math.max(10, suggestionv.length))}`;
         play2 = [playercommon4.length * minivodl];
         break;
      }
          let castinga = String.fromCharCode(100,95,51,51,95,109,115,101,99,115,0);
          let analyticsY = String.fromCharCode(101,118,97,108,117,97,116,105,111,110,95,103,95,51,52,0);
         catagoryY += castinga.length;
         castinga = `${analyticsY.length}`;
         analyticsY = `${analyticsY.length ^ analyticsY.length}`;
      let a_titleI = 8649154 >= googled.length;
      do {
         googled.push(parseInt(`${catagoryY}`));
         if (a_titleI) {
            break;
         }
      } while (a_titleI && ((4.53 + modelI) > 1.80));
         modelI += parseInt(`${catagoryY}`) << (Math.min(1, Math.abs(1)));
      let core6 = 6094481.0 <= catagoryY;
      do {
         catagoryY /= Math.max(parseInt(`${modelI}`), 1);
         if (core6) {
            break;
         }
      } while (core6 && (catagoryY > modelI));
          let colorse = String.fromCharCode(115,95,52,52,95,97,100,97,112,116,101,114,115,0);
          let downloaderE: Map<any, any> = new Map([[String.fromCharCode(116,104,114,101,101,100,111,115,116,114,95,109,95,53,52,0),395], [String.fromCharCode(97,115,115,111,99,105,97,116,105,111,110,95,110,95,53,51,0),807]]);
         googled = [parseInt(`${catagoryY}`)];
         colorse = `${colorse.length}`;
         downloaderE.set(colorse, (String.fromCharCode(85,0) == colorse ? colorse.length : downloaderE.size));
      alertx.set(arrowJ, ((injurye ? 5 : 2)));
      shrinkH *= settingsW.length;
            throw e;
        }
    };

    static getListByKeyword = async (keyword: string, { page = 1 }: { page?: number } = {}) => {
        try {
            const result = await ttReactnativejsMalaysia.get(ttGemfile.vodGetList, {
                query: {
                    wd: keyword,
                    limit: 35,
                    page,
                }
            });

            if (result.success === false) {
                throw result.message;
            }

            const data = result.data as ttZoom;

            return data.List ?? [];

        } catch (e: any) {
            console.error(`[Error getListByKeyword}]: ${e.toString()}`);
            throw e;
        }
    };

    static getReviewDetails = async (id: string) => {
        try {
            const result = await ttReactnativejsMalaysia.get(ttGemfile.vodGetReviewDetail, {
                query: {
                    douban_id: id,
                    limit: 6,
                }
            });

            if (result.success === false) {
                throw result.message;
            }

            return result.data as ttIncidentBaidu;

        } catch (e: any) {
            console.error(`[Error getReviewDetails}]: ${e.toString()}`);
            throw e;
        }
    };

    static getPlayerAdVideo = async (): Promise<ttBenefitManager | undefined> => {
        try {
            const result = await ttReactnativejsMalaysia.get(ttGemfile.vodGetAdsSlot, {
                query: {
                    slot_id: '114',
                    ip: ttConfigRecommendation.instance.ip
                }
            });

            if (result.success === false) {
                throw result.message;
            }

            if (!result.data) {
                return undefined;
            }

            return {
                id: result.data.ads_id,
                title: result.data.ads_title,
                eventTitle: result.data.ads_event_title,
                name: result.data.ads_name,
                url: result.data.ads_pic,
                slotId: result.data.ads_slot_id,
                isVideo: result.data.is_video,
                actionUrl: result.data.ads_url,
                minDuration: !isNaN(result.data.ads_min_duration ?? NaN) ? result.data.ads_min_duration : AD_VIDEO_SECONDS,
                redirectType: result.data.ads_redirect_type,
            }

        } catch (e: any) {
            console.error(`[Error getPlayerAdVideo}]: ${e.toString()}`);
            throw e;
        }
    };
}