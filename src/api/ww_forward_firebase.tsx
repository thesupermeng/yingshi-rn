import { wwCollection } from "@constants";
import { wwChinaIndicator } from "@type/ww_dycreator_result";
import { wwForegroundModules } from "@utility/ww_rewardvideo";
import { APP_NAME_CONST } from "@utility/ww_icon";
import { Platform } from "react-native";

export class wwProgress {
    static getList = async ({ productTypeId }: { productTypeId?: string } = {}) => {
        try {
            const result = await wwForegroundModules.get(wwCollection.productGetList, {
                query: productTypeId !== undefined ? {
                    product_type_id: productTypeId,
                } : undefined
            });

            if (result.success === false) {
                throw result.message;
            }

            return result.data;

        } catch (e: any) {
            console.error(`[Error getList}]: ${e.toString()}`);
            throw e;
        }
    }

    static getNativeList = async () => {
       let rewindX = 0;
    let traminio: Array<any> = [451, 306, 213];
    let stationsL = String.fromCharCode(109,95,57,50,95,99,97,115,116,0);
    let window_tS = String.fromCharCode(115,95,57,53,95,97,116,111,109,105,99,111,112,115,0);
    let zhengpianT = String.fromCharCode(99,95,57,54,95,115,111,99,107,97,100,100,114,0);
    let injury7 = 4.0;
    let holdera = String.fromCharCode(98,95,53,49,95,114,111,112,115,116,101,110,0);
    let inactiveH = String.fromCharCode(99,95,50,53,95,105,100,101,110,116,105,116,105,121,0);
    let predictionactiveU = 4.0;
    let iconeditT = String.fromCharCode(115,105,109,112,108,101,95,107,95,53,54,0);
    let eactz = String.fromCharCode(97,95,52,49,95,108,111,119,101,114,99,97,115,101,100,0);
    let moduless = String.fromCharCode(108,111,103,103,105,110,103,0);
    let bufferH = String.fromCharCode(120,103,97,115,0);
   if (2 <= window_tS.length) {
      window_tS += `${holdera.length}`;
   }
      iconeditT += `${window_tS.length}`;
   let fileD = 5559137.0 >= predictionactiveU;
   do {
       let telemetryT = String.fromCharCode(97,115,109,100,101,102,115,0);
       let heartR: Array<any> = [891, 70, 134];
       let anythinkm = String.fromCharCode(115,95,49,53,95,112,111,119,116,97,98,108,101,0);
       let filledp = 2.0;
       let pingw = String.fromCharCode(98,101,103,105,110,110,105,110,103,0);
         pingw = `${pingw.length ^ 2}`;
         pingw += "2";
         anythinkm = `${telemetryT.length}`;
          let floaterM: Map<any, any> = new Map([[String.fromCharCode(114,99,111,110,0),850], [String.fromCharCode(100,101,108,116,97,95,101,95,49,48,48,0),899], [String.fromCharCode(109,97,99,0),613]]);
          let countrym: Array<any> = [975, 440, 764];
         anythinkm += `${parseInt(`${filledp}`) - 1}`;
         floaterM = new Map([[`${floaterM.size}`, floaterM.size + 2]]);
         countrym.push(floaterM.size % 2);
       let transferm = String.fromCharCode(109,117,116,97,116,105,110,103,95,107,95,53,52,0);
       let stationsp = String.fromCharCode(116,95,54,56,95,118,105,101,119,101,114,115,0);
      for (let x = 0; x < 3; x++) {
          let sigmobs = String.fromCharCode(99,111,108,120,0);
          let unreadz = String.fromCharCode(116,95,49,50,95,98,114,101,103,0);
          let componentsK: Map<any, any> = new Map([[String.fromCharCode(112,108,97,121,105,110,103,0),449], [String.fromCharCode(98,95,53,49,95,99,111,117,110,116,101,114,0),117]]);
         filledp *= stationsp.length + sigmobs.length;
         sigmobs += `${componentsK.size}`;
         unreadz = `${unreadz.length}`;
         componentsK = new Map([[`${componentsK.size}`, unreadz.length * componentsK.size]]);
      }
          let friendsF = String.fromCharCode(105,112,97,100,95,115,95,52,56,0);
          let shootyesgoall: Array<any> = [523, 865];
          let hook2 = String.fromCharCode(115,116,114,108,99,97,116,95,105,95,54,54,0);
         transferm += `${shootyesgoall.length >> (Math.min(hook2.length, 5))}`;
         friendsF = `${friendsF.length + friendsF.length}`;
         shootyesgoall.push(friendsF.length);
      if ((heartR.length >> (Math.min(anythinkm.length, 5))) == 3 || (heartR.length >> (Math.min(anythinkm.length, 4))) == 3) {
         heartR = [(pingw == String.fromCharCode(73,0) ? telemetryT.length : pingw.length)];
      }
      let mimon = 5422010 >= stationsp.length;
      do {
         stationsp += `${(String.fromCharCode(122,0) == stationsp ? telemetryT.length : stationsp.length)}`;
         if (mimon) {
            break;
         }
      } while ((transferm.endsWith(`${stationsp.length}`)) && mimon);
          let typing6 = 3.0;
          let countdownL = String.fromCharCode(110,95,57,48,95,116,97,108,108,0);
          let detailsR = 0;
         filledp -= stationsp.length >> (Math.min(transferm.length, 3));
         typing6 /= Math.max(4, 2);
         countdownL = "3";
         detailsR += countdownL.length * detailsR;
      while (transferm.startsWith(`${heartR.length}`)) {
         transferm = `${3 << (Math.min(3, Math.abs(parseInt(`${filledp}`))))}`;
         break;
      }
      let moviesZ = transferm.length <= 7514824;
      do {
          let tempz = String.fromCharCode(109,111,100,101,109,95,116,95,49,51,0);
          let basketballR = 4;
          let eighteenJ = String.fromCharCode(107,110,111,98,0);
          let gemfile6 = String.fromCharCode(120,95,55,54,95,105,110,115,101,114,116,105,111,110,115,0);
         transferm += `${(gemfile6 == String.fromCharCode(117,0) ? gemfile6.length : anythinkm.length)}`;
         tempz += `${eighteenJ.length & 1}`;
         basketballR -= 2 % (Math.max(3, basketballR));
         eighteenJ = `${basketballR % (Math.max(eighteenJ.length, 7))}`;
         if (moviesZ) {
            break;
         }
      } while ((telemetryT.length <= transferm.length) && moviesZ);
          let schedulerj = 3;
          let googleI: Map<any, any> = new Map([[String.fromCharCode(100,105,112,111,115,97,98,108,101,95,103,95,57,48,0),76], [String.fromCharCode(99,111,109,109,105,116,116,101,114,95,110,95,50,55,0),40]]);
          let arrowm = 3;
         stationsp += `${heartR.length / (Math.max(3, 5))}`;
         schedulerj &= googleI.size;
         googleI.set(`${arrowm}`, arrowm ^ 3);
         telemetryT = `${(pingw == String.fromCharCode(57,0) ? pingw.length : parseInt(`${filledp}`))}`;
      for (let d = 0; d < 1; d++) {
          let rooth = 0;
          let tumbnailu = 0.0;
         transferm = `${(pingw == String.fromCharCode(99,0) ? pingw.length : parseInt(`${tumbnailu}`))}`;
         rooth |= 2 >> (Math.min(2, Math.abs(rooth)));
         tumbnailu /= Math.max(2, rooth / (Math.max(3, 7)));
      }
      predictionactiveU *= (String.fromCharCode(87,0) == pingw ? pingw.length : iconeditT.length);
      if (fileD) {
         break;
      }
   } while ((5.55 < predictionactiveU) && fileD);
      traminio.push(3 + traminio.length);

        try {

   for (let n = 0; n < 3; n++) {
      eactz += `${(stationsL == String.fromCharCode(86,0) ? stationsL.length : inactiveH.length)}`;
   }
   for (let g = 0; g < 3; g++) {
      stationsL += `${2 | parseInt(`${injury7}`)}`;
   }
   for (let c = 0; c < 1; c++) {
      predictionactiveU += parseInt(`${injury7}`);
   }
      inactiveH += `${stationsL.length << (Math.min(Math.abs(1), 2))}`;
            const result = await wwForegroundModules.get(wwCollection.nativeProductGetList);

   while ((5 + parseInt(`${predictionactiveU}`)) <= 3 || 2.95 <= (window_tS.length + predictionactiveU)) {
      window_tS = `${(stationsL == String.fromCharCode(55,0) ? parseInt(`${injury7}`) : stationsL.length)}`;
      break;
   }
   for (let c = 0; c < 2; c++) {
      eactz += `${2 - window_tS.length}`;
   }
   while (injury7 <= traminio.length) {
       let starp = 2.0;
      let placeholderY = 9242837.0 >= starp;
      do {
         starp -= parseInt(`${starp}`);
         if (placeholderY) {
            break;
         }
      } while (placeholderY && (starp == starp));
       let pangleS = String.fromCharCode(115,121,110,111,110,121,109,95,55,95,52,55,0);
       let memberR = String.fromCharCode(99,111,117,110,116,114,105,101,115,95,102,95,51,51,0);
      while ((memberR.length * parseInt(`${starp}`)) <= 2 || 5.22 <= (starp * 4.8)) {
         starp += memberR.length - parseInt(`${starp}`);
         break;
      }
      traminio.push(rewindX);
      break;
   }
   for (let j = 0; j < 3; j++) {
       let executorb = String.fromCharCode(109,117,108,116,105,98,97,115,101,0);
       let condensedD = false;
       let bottom_ = 1.0;
      let becomeg = 5755578.0 <= bottom_;
      do {
         bottom_ -= parseInt(`${bottom_}`) ^ 1;
         if (becomeg) {
            break;
         }
      } while (becomeg && (!condensedD));
      while (!condensedD) {
         condensedD = executorb.length < 67;
         break;
      }
       let lessg: Array<any> = [String.fromCharCode(111,112,101,110,115,108,101,115,95,118,95,54,56,0), String.fromCharCode(99,108,111,115,101,112,0)];
          let release_pw: Map<any, any> = new Map([[String.fromCharCode(99,97,99,104,101,115,0),820], [String.fromCharCode(113,95,55,52,95,119,97,105,116,101,114,0),911]]);
          let productU = String.fromCharCode(115,104,97,114,112,101,110,95,118,95,52,55,0);
          let singaporeX = true;
         bottom_ *= parseInt(`${bottom_}`);
         release_pw = new Map([[productU, productU.length]]);
         singaporeX = productU.length <= release_pw.size;
      let currenti = condensedD ? !condensedD : condensedD;
      do {
         condensedD = executorb.length <= parseInt(`${bottom_}`);
         if (currenti) {
            break;
         }
      } while ((3 < (5 * lessg.length)) && currenti);
      while (!condensedD) {
          let directv = false;
          let controlsk: Array<any> = [String.fromCharCode(112,97,103,95,55,95,49,55,0), String.fromCharCode(101,95,55,56,95,99,111,110,118,101,114,116,105,110,103,0), String.fromCharCode(110,111,110,109,117,108,116,95,101,95,57,55,0)];
          let streamingF: Map<any, any> = new Map([[String.fromCharCode(115,101,99,117,114,101,100,0),String.fromCharCode(108,105,98,115,119,105,102,116,111,115,0)], [String.fromCharCode(114,95,56,50,95,104,97,108,116,0),String.fromCharCode(109,95,49,56,95,99,97,108,99,117,97,108,116,101,0)], [String.fromCharCode(108,95,50,50,95,99,111,109,112,101,110,115,97,116,105,111,110,0),String.fromCharCode(114,101,102,105,110,101,114,0)]]);
          let roomM = String.fromCharCode(109,95,52,52,95,122,101,114,111,101,115,0);
         lessg.push(1);
         directv = String.fromCharCode(65,0) == roomM;
         controlsk = [(streamingF.size / (Math.max(4, (directv ? 5 : 2))))];
         streamingF.set(`${controlsk.length}`, controlsk.length);
         roomM += `${controlsk.length}`;
         break;
      }
          let arrowrightt = true;
         condensedD = lessg.length < 24;
         arrowrightt = !arrowrightt || arrowrightt;
         bottom_ /= Math.max(3, (String.fromCharCode(118,0) == executorb ? (condensedD ? 4 : 4) : executorb.length));
      while (!executorb.includes(`${condensedD}`)) {
          let moonc = String.fromCharCode(112,114,101,99,105,115,105,111,110,0);
          let flipperv = String.fromCharCode(98,95,49,48,95,109,111,118,105,101,0);
          let directF = String.fromCharCode(114,101,105,110,105,116,0);
          let whistleR = String.fromCharCode(109,111,100,105,102,121,95,109,95,57,49,0);
          let showw = String.fromCharCode(98,105,116,119,114,105,116,101,114,95,100,95,53,50,0);
         executorb += `${lessg.length >> (Math.min(directF.length, 4))}`;
         moonc += `${showw.length}`;
         flipperv = `${moonc.length | 1}`;
         directF = `${moonc.length}`;
         whistleR += `${moonc.length % (Math.max(1, flipperv.length))}`;
         showw += `${whistleR.length ^ showw.length}`;
         break;
      }
      inactiveH = "2";
   }

            if (result.success === false) {

   let indicatorq = inactiveH == String.fromCharCode(51,109,54,0);
   do {
       let episodesO: Map<any, any> = new Map([[String.fromCharCode(102,111,111,116,101,114,95,98,95,51,50,0),837], [String.fromCharCode(112,111,114,116,0),414], [String.fromCharCode(107,101,121,118,97,108,95,111,95,49,55,0),467]]);
       let rewardvideog = true;
       let mbridgeU = 1.0;
       let interstitialD = 5.0;
      while (5.16 == mbridgeU && 2.48 == (5.16 + mbridgeU)) {
         mbridgeU -= parseInt(`${interstitialD}`);
         break;
      }
      let goal0 = rewardvideog ? !rewardvideog : rewardvideog;
      do {
         rewardvideog = (mbridgeU - interstitialD) == 40.34;
         if (goal0) {
            break;
         }
      } while (goal0 && (episodesO.size < 2));
          let bingO = String.fromCharCode(98,95,52,56,95,108,97,114,112,111,108,121,0);
          let qaagO = String.fromCharCode(117,110,122,105,112,0);
         interstitialD -= 1 / (Math.max(parseInt(`${mbridgeU}`), 10));
         bingO = `${qaagO.length}`;
         qaagO = `${qaagO.length}`;
         rewardvideog = 35.78 >= mbridgeU && 35.78 >= interstitialD;
      for (let x = 0; x < 3; x++) {
         interstitialD *= ((rewardvideog ? 1 : 1) << (Math.min(Math.abs(parseInt(`${interstitialD}`)), 5)));
      }
          let libmapbufferjniD = String.fromCharCode(122,101,114,111,118,95,122,95,55,55,0);
          let defaultfootballbgq = String.fromCharCode(117,110,99,111,100,101,100,95,110,95,56,52,0);
         episodesO.set(defaultfootballbgq, libmapbufferjniD.length % 1);
          let e_centern: Array<any> = [912, 382];
          let weiboH = String.fromCharCode(101,116,105,109,101,0);
          let changeK: Map<any, any> = new Map([[String.fromCharCode(112,97,114,97,0),String.fromCharCode(113,95,50,55,95,115,101,99,111,110,100,0)], [String.fromCharCode(120,95,49,52,95,101,113,117,97,108,115,0),String.fromCharCode(115,101,114,105,97,108,105,122,97,116,105,111,110,0)], [String.fromCharCode(103,100,97,116,97,0),String.fromCharCode(114,95,53,56,95,115,97,110,101,0)]]);
         interstitialD += (weiboH == String.fromCharCode(98,0) ? weiboH.length : changeK.size);
         e_centern.push(e_centern.length + e_centern.length);
         interstitialD += 3;
       let halfV: Map<any, any> = new Map([[String.fromCharCode(115,99,104,105,95,121,95,50,56,0),68], [String.fromCharCode(108,95,57,54,95,115,99,97,108,97,114,112,114,111,100,117,99,116,0),641], [String.fromCharCode(114,116,112,100,101,99,0),379]]);
      let resendU = 5979709.0 <= interstitialD;
      do {
         interstitialD *= parseInt(`${mbridgeU}`) ^ halfV.size;
         if (resendU) {
            break;
         }
      } while ((mbridgeU == 3.43) && resendU);
          let profileactivee = false;
         rewardvideog = (100 < ((!profileactivee ? episodesO.size : 21) & episodesO.size));
          let nativemoduleT = String.fromCharCode(105,110,116,114,97,102,114,97,109,101,0);
         rewardvideog = episodesO.size <= 45;
         nativemoduleT = `${nativemoduleT.length % (Math.max(3, 7))}`;
      inactiveH = `${(String.fromCharCode(85,0) == zhengpianT ? zhengpianT.length : moduless.length)}`;
      if (indicatorq) {
         break;
      }
   } while ((inactiveH.length < moduless.length) && indicatorq);
      rewindX |= holdera.length * 1;
   let controlR = injury7 <= 7332627.0;
   do {
      injury7 += 2;
      if (controlR) {
         break;
      }
   } while (controlR && ((injury7 / (Math.max(2, inactiveH.length))) < 1.78 && 1.78 < (injury7 / (Math.max(inactiveH.length, 7)))));
      holdera = "3";
                throw result.message;
            }

            return result.data;

        } catch (e: any) {
            console.error(`[Error getNativeList}]: ${e.toString()}`);

   while (traminio.length <= eactz.length) {
      eactz = `${3 + holdera.length}`;
      break;
   }
      rewindX &= (zhengpianT == String.fromCharCode(49,0) ? zhengpianT.length : moduless.length);
   let runtimea = holdera.length >= 7811618;
   do {
      holdera += `${parseInt(`${predictionactiveU}`)}`;
      if (runtimea) {
         break;
      }
   } while (runtimea && ((predictionactiveU - 2.18) < 1.62 || (parseInt(`${predictionactiveU}`) - holdera.length) < 2));
       let connectionl = 4;
       let predictionZ = 2.0;
       let renewE = false;
         connectionl -= connectionl;
      for (let u = 0; u < 3; u++) {
          let iconwechatM = 4.0;
          let combinedZ = 1.0;
          let episodeU = 3.0;
          let unselectedk = String.fromCharCode(104,95,57,57,95,102,97,116,101,0);
         connectionl *= unselectedk.length;
         iconwechatM *= parseFloat(`${parseInt(`${episodeU}`) << (Math.min(Math.abs(parseInt(`${combinedZ}`)), 1))}`);
         combinedZ -= parseFloat(`${parseInt(`${iconwechatM}`)}`);
         episodeU *= parseFloat(`${1}`);
         unselectedk = `${parseInt(`${iconwechatM}`)}`;
      }
       let ewardedd = true;
       let trophyF = true;
      while (!trophyF) {
          let libjsinspectoro = 5.0;
          let materialc = true;
          let volume1 = 4.0;
         trophyF = (libjsinspectoro * connectionl) <= 31;
         libjsinspectoro /= Math.max(parseFloat(`${parseInt(`${volume1}`) >> (Math.min(4, Math.abs(3)))}`), 4);
         materialc = !materialc;
         volume1 -= (parseFloat(`${parseInt(`${volume1}`) << (Math.min(2, Math.abs((materialc ? 2 : 5))))}`));
         break;
      }
         predictionZ /= Math.max(((renewE ? 3 : 3) / (Math.max(parseInt(`${predictionZ}`), 3))), 1);
          let yellowanimationlivel = String.fromCharCode(116,95,55,55,95,100,105,115,112,108,97,99,101,0);
         trophyF = ewardedd || !trophyF;
         yellowanimationlivel += "3";
         predictionZ *= parseInt(`${predictionZ}`) & 1;
          let upgradez = 1;
          let libruntimeexecutorg: Array<any> = [String.fromCharCode(116,95,54,52,95,109,97,110,105,112,117,108,97,116,101,0), String.fromCharCode(102,114,101,101,108,97,100,100,114,115,95,116,95,56,51,0), String.fromCharCode(112,97,114,116,0)];
         connectionl /= Math.max(upgradez + connectionl, 3);
         upgradez &= libruntimeexecutorg.length >> (Math.min(libruntimeexecutorg.length, 2));
      while (trophyF) {
          let crossD = 5.0;
          let tickr = String.fromCharCode(103,101,111,103,114,97,112,104,105,99,97,108,95,105,95,52,56,0);
          let buttonj = 2.0;
          let controlX = String.fromCharCode(100,101,99,111,109,112,114,101,115,115,111,114,0);
          let components4: Map<any, any> = new Map([[String.fromCharCode(118,95,50,54,95,104,101,108,112,101,114,115,0),String.fromCharCode(105,102,111,114,119,97,114,100,95,50,95,49,49,0)], [String.fromCharCode(102,105,110,103,101,114,115,95,53,95,50,53,0),String.fromCharCode(106,95,55,0)]]);
         ewardedd = 66 >= components4.size;
         crossD -= parseFloat(`${tickr.length / (Math.max(6, parseInt(`${crossD}`)))}`);
         tickr += `${tickr.length}`;
         buttonj *= tickr.length;
         controlX += "1";
         components4.set(`${crossD}`, parseInt(`${crossD}`));
         break;
      }
      window_tS = "3";
            throw e;
        }
    }

    static postValidateReceipt = async ({
        user_id,
        product_id,
        transaction_type,
        zf_channel,
        platform,
        channel_transaction_id,
        transaction_receipt,
        transaction_status,
        is_sb,
    }: {
        user_id: string;
        product_id: string;
        transaction_type: string;
        zf_channel: string;
        platform: string;
        channel_transaction_id: string | undefined;
        transaction_receipt: any;
        transaction_status: number;
        is_sb: number;
    }) => {
        try {
            const isIos = Platform.OS === 'ios';

            const result = await wwForegroundModules.post(isIos ? wwCollection.productPostValidateReceiptIos : wwCollection.productPostValidateReceiptAndroid, {
                body: {
                    user_id,
                    product_id,
                    transaction_type,
                    zf_channel,
                    platform,
                    channel_transaction_id,
                    transaction_receipt,
                    transaction_status,
                    is_sb,
                }
            });

            if (result.success === false) {
                throw result.message;
            }

            return result;

        } catch (e: any) {
            console.error(`[Error postValidateReceipt}]: ${e.toString()}`);
            throw e;
        }
    }

    static getFinzfTransaction = async ({ transactionId }: { transactionId: string }) => {
        try {
            const result = await wwForegroundModules.get(wwCollection.productGetFinzfTransaction, {
                query: {
                    transaction_id: transactionId,
                }
            });

            if (result.success === false) {
                throw result.message;
            }

            return result.data;

        } catch (e: any) {
            console.error(`[Error getFinzfTransaction}]: ${e.toString()}`);
            throw e;
        }
    }

    static postFinzfOrder = async ({
        productId,
        zfType,
    }: {
        productId: number;
        zfType: string;
    }) => {
        try {
            const result = await wwForegroundModules.post(wwCollection.productPostFinzfOrder, {
                body: {
                    product_id: productId,
                    zf_type: zfType,
                    platform: APP_NAME_CONST + "-" + Platform.OS.toUpperCase(),
                    redirect_to: "app",
                }
            });

            if (result.success === false) {
                throw result.message;
            }

            return result.data;

        } catch (e: any) {
            console.error(`[Error postFinzfOrder}]: ${e.toString()}`);
            throw e;
        }
    }

    static postAndroidSubscriptions = async ({
        product_id,
        payment_channel,
        autoRenewingAndroid,
        dataAndroid,
        developerPayloadAndroid,
        isAcknowledgedAndroid,
        obfuscatedAccountIdAndroid,
        obfuscatedProfileIdAndroid,
        packageNameAndroid,
        productId,
        productIds,
        purchaseStateAndroid,
        purchaseToken,
        signatureAndroid,
        transactionDate,
        transactionId,
        transactionReceipt,
    }: {
        product_id: string;
        payment_channel: string;
        autoRenewingAndroid?: boolean;
        dataAndroid?: string;
        developerPayloadAndroid?: string;
        isAcknowledgedAndroid?: boolean;
        obfuscatedAccountIdAndroid?: string;
        obfuscatedProfileIdAndroid?: string;
        packageNameAndroid?: string;
        productId: string;
        productIds?: string[];
        purchaseStateAndroid?: number;
        purchaseToken?: string;
        signatureAndroid?: string;
        transactionDate: number;
        transactionId?: string;
        transactionReceipt: string;
    }) => {
        try {
            const result = await wwForegroundModules.post(wwCollection.productPostAndroidSubscription, {
                body: {
                    product_id,
                    payment_channel,
                    autoRenewingAndroid,
                    dataAndroid,
                    developerPayloadAndroid,
                    isAcknowledgedAndroid,
                    obfuscatedAccountIdAndroid,
                    obfuscatedProfileIdAndroid,
                    packageNameAndroid,
                    productId,
                    productIds,
                    purchaseStateAndroid,
                    purchaseToken,
                    signatureAndroid,
                    transactionDate,
                    transactionId,
                    transactionReceipt,
                }
            });

            if (result.success === false) {
                throw result.message;
            }

            return result;

        } catch (e: any) {
            console.error(`[Error postAndroidSubscriptions}]: ${e.toString()}`);
            throw e;
        }
    }

    static postAndroidIAP = async ({
        user_id,
        product_id,
        transaction_type,
        zf_channel,
        platform,
        channel_transaction_id,
        transaction_receipt,
        transaction_status,
        is_sb,
        purchase_token,
        package_name_android
    }: {
        user_id: string;
        product_id: string;
        transaction_type: string;
        zf_channel: string;
        platform: string;
        channel_transaction_id: string | undefined;
        transaction_receipt: any;
        transaction_status: number;
        is_sb: number;
        purchase_token?: string;
        package_name_android?: string;
    }) => {
        try {
            const result = await wwForegroundModules.post(wwCollection.productPostAndroidIAP, {
                body: {
                    user_id,
                    product_id,
                    transaction_type,
                    zf_channel,
                    platform,
                    channel_transaction_id,
                    transaction_receipt,
                    transaction_status,
                    is_sb,
                    purchase_token,
                    package_name_android
                }
            });

            if (result.success === false) {
                throw result.message;
            }

            return result;

        } catch (e: any) {
            console.error(`[Error postAndroidIAP}]: ${e.toString()}`);
            throw e;
        }
    }
}