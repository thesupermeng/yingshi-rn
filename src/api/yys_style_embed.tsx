import { yysPangleCommon } from "@constants";
import { yysLibsentryOverlay } from "@type";
import { yysConfig } from "@utility";
import { APP_NAME_CONST } from "@utility";
import { Platform } from "react-native";

export class yysBaselineOrangedownarrow {
    static getList = async ({ productTypeId }: { productTypeId?: string } = {}) => {
        try {
            const result = await yysConfig.get(yysPangleCommon.productGetList, {
                query: productTypeId !== undefined ? {
                    product_type_id: productTypeId,
                } : undefined
            });

            if (result.success === false) {
                throw result.message;
            }

            return result.data;

        } catch (e: any) {
            console.error(`[Error getList}]: ${e.toString()}`);
            throw e;
        }
    }

    static getNativeList = async () => {
       let heji4 = 0.0;
    let typingloadingf = false;
    let redgoaly = 0.0;
    let greyarrowupg = String.fromCharCode(105,97,116,95,117,95,51,51,0);
    let starz = String.fromCharCode(102,97,100,101,111,117,116,95,117,95,52,0);
    let orangeclockM = 5;
    let stylesg = true;
    let showb = String.fromCharCode(115,108,111,119,109,111,100,101,95,103,95,49,55,0);
    let footballfiledlayoutf: Array<any> = [970, 165, 544];
    let textlayoutmanagerN = 3;
    let zoomP = String.fromCharCode(109,95,54,56,95,106,111,105,110,101,100,0);
   if (2.41 <= heji4 && (heji4 / (Math.max(2.41, 9))) <= 2.100) {
      heji4 -= (String.fromCharCode(56,0) == greyarrowupg ? greyarrowupg.length : (stylesg ? 2 : 1));
   }
   if (!stylesg) {
       let appsC: Map<any, any> = new Map([[String.fromCharCode(102,95,53,57,95,109,97,120,106,0),false ], [String.fromCharCode(108,95,56,55,95,99,107,112,116,0),true ], [String.fromCharCode(98,105,116,111,112,115,95,122,95,54,48,0),true ]]);
       let mappingF = String.fromCharCode(99,114,111,112,112,105,110,103,95,52,95,55,55,0);
       let mintegralk = String.fromCharCode(121,95,54,55,95,115,121,110,116,104,102,105,108,116,0);
      for (let c = 0; c < 3; c++) {
         mappingF = `${(String.fromCharCode(82,0) == mappingF ? mappingF.length : appsC.size)}`;
      }
      if (!mappingF.includes(`${appsC.size}`)) {
         mappingF += `${mintegralk.length}`;
      }
      stylesg = 62 > textlayoutmanagerN;
   }
   if (5 >= greyarrowupg.length) {
       let lnewsshare5 = false;
       let successY = String.fromCharCode(112,117,114,101,95,108,95,52,0);
       let fullscreenminG = 5.0;
          let types1 = 0;
          let cricketJ = String.fromCharCode(118,95,50,56,95,105,110,115,116,97,108,108,0);
          let shrinkc = 2.0;
         successY += `${2 * successY.length}`;
         types1 /= Math.max(5, types1);
         cricketJ += `${(cricketJ == String.fromCharCode(66,0) ? parseInt(`${shrinkc}`) : cricketJ.length)}`;
         shrinkc -= (cricketJ == String.fromCharCode(117,0) ? cricketJ.length : types1);
       let toponC = String.fromCharCode(113,95,49,48,48,95,97,118,102,111,114,109,97,116,0);
          let refreshd = true;
          let libavformate = String.fromCharCode(112,103,109,120,95,103,95,49,49,0);
          let pushD = 1.0;
         toponC += `${2 ^ parseInt(`${fullscreenminG}`)}`;
         refreshd = !refreshd;
         libavformate = `${3 * parseInt(`${pushD}`)}`;
         pushD *= parseFloat(`${parseInt(`${pushD}`) - libavformate.length}`);
          let iconstarB = String.fromCharCode(100,101,115,116,114,117,99,116,95,107,95,51,50,0);
          let baiduQ = true;
          let targetl = String.fromCharCode(100,101,99,114,121,112,116,95,118,95,56,51,0);
         successY += `${((lnewsshare5 ? 4 : 4))}`;
         iconstarB += `${((baiduQ ? 3 : 1) % 2)}`;
         baiduQ = iconstarB.endsWith(`${baiduQ}`);
         targetl += `${((baiduQ ? 2 : 3))}`;
      if (2 >= toponC.length) {
         toponC += `${toponC.length - parseInt(`${fullscreenminG}`)}`;
      }
          let watch2 = String.fromCharCode(121,95,52,48,95,110,97,114,114,111,119,0);
          let playj: Map<any, any> = new Map([[String.fromCharCode(111,95,53,95,114,101,97,108,0),String.fromCharCode(100,95,52,57,95,100,110,115,0)], [String.fromCharCode(112,95,55,57,95,112,114,101,97,109,98,117,108,97,0),String.fromCharCode(108,101,115,115,95,122,95,55,54,0)]]);
         successY = `${parseInt(`${fullscreenminG}`) >> (Math.min(Math.abs(1), 5))}`;
         watch2 = `${watch2.length & 2}`;
         playj.set(`${watch2}`, 2);
      let sharedb = lnewsshare5 ? !lnewsshare5 : lnewsshare5;
      do {
         lnewsshare5 = 96 <= toponC.length;
         if (sharedb) {
            break;
         }
      } while (sharedb && ((fullscreenminG + 2.88) > 3.16));
         lnewsshare5 = successY.length >= 53;
      if (parseInt(`${fullscreenminG}`) < toponC.length) {
         fullscreenminG -= (parseFloat(`${(lnewsshare5 ? 1 : 2) ^ successY.length}`));
      }
      greyarrowupg = `${(parseInt(`${fullscreenminG}`) ^ (typingloadingf ? 3 : 5))}`;
   }
       let nativeh: Array<any> = [String.fromCharCode(103,95,56,48,95,105,110,118,105,116,101,114,0), String.fromCharCode(105,95,54,53,95,116,101,115,116,105,110,103,0), String.fromCharCode(118,95,54,56,95,115,112,97,99,105,110,103,115,0)];
       let rewindS = String.fromCharCode(101,116,104,101,114,115,99,97,110,95,110,95,54,55,0);
       let login0: Array<any> = [608, 209, 274];
      for (let u = 0; u < 3; u++) {
         rewindS = `${login0.length * nativeh.length}`;
      }
         rewindS += `${nativeh.length}`;
       let attributedstringI = false;
       let logouserF = true;
      for (let w = 0; w < 3; w++) {
         login0.push(rewindS.length >> (Math.min(Math.abs(3), 4)));
      }
      while (3 <= nativeh.length || (3 * nativeh.length) <= 2) {
         nativeh.push(nativeh.length | 1);
         break;
      }
      let mbsplashL = attributedstringI ? !attributedstringI : attributedstringI;
      do {
         attributedstringI = rewindS.length < 2;
         if (mbsplashL) {
            break;
         }
      } while (mbsplashL && (!logouserF));
       let libfbjniC = String.fromCharCode(114,97,99,101,95,119,95,52,56,0);
       let libapminsightbd = String.fromCharCode(117,95,53,51,95,101,110,99,105,114,99,108,101,100,0);
         nativeh.push(libfbjniC.length);
      for (let j = 0; j < 3; j++) {
         nativeh = [login0.length | 1];
      }
      greyarrowupg = "3";

        try {

   for (let j = 0; j < 3; j++) {
      typingloadingf = orangeclockM <= greyarrowupg.length;
   }
   while (2 > textlayoutmanagerN) {
       let downloading1 = String.fromCharCode(116,114,97,99,101,114,95,109,95,53,57,0);
       let teamh = String.fromCharCode(120,97,109,112,108,101,95,108,95,56,53,0);
       let holderm = String.fromCharCode(103,117,97,114,100,95,117,95,52,55,0);
      if (1 < holderm.length) {
          let iconsharefriends8 = String.fromCharCode(108,95,48,95,97,100,114,109,0);
          let buildN = true;
          let macauM = String.fromCharCode(99,111,109,112,97,115,115,95,101,95,49,54,0);
         downloading1 += `${holderm.length / (Math.max(1, 3))}`;
         iconsharefriends8 = `${(iconsharefriends8.length - (buildN ? 3 : 2))}`;
         buildN = !buildN;
         macauM += `${3 / (Math.max(5, macauM.length))}`;
      }
          let componentsy = String.fromCharCode(100,108,105,115,116,95,115,95,49,52,0);
         teamh += `${componentsy.length >> (Math.min(5, holderm.length))}`;
       let taiwanY = 1.0;
       let predictiondefault5 = 1.0;
      if (holderm.length <= 1) {
         holderm += `${holderm.length}`;
      }
       let championT: Array<any> = [442, 130, 222];
      while (5.10 <= predictiondefault5) {
         predictiondefault5 /= Math.max(holderm.length, 5);
         break;
      }
      let recommendationE = String.fromCharCode(107,110,97,115,115,106,99,100,0) == downloading1;
      do {
          let incidentN = String.fromCharCode(115,95,49,52,95,117,110,112,114,111,99,101,115,115,101,100,0);
          let save2 = String.fromCharCode(121,95,56,55,95,99,115,99,104,101,109,101,0);
          let backl = 0;
          let profileinactivew = String.fromCharCode(119,95,51,49,95,116,114,97,110,115,112,97,114,101,110,99,121,0);
         downloading1 = `${teamh.length / (Math.max(2, 10))}`;
         incidentN += `${incidentN.length}`;
         save2 += `${profileinactivew.length ^ 3}`;
         backl %= Math.max(1, backl % (Math.max(incidentN.length, 4)));
         profileinactivew = `${1 + profileinactivew.length}`;
         if (recommendationE) {
            break;
         }
      } while ((3 >= (downloading1.length << (Math.min(1, championT.length)))) && recommendationE);
         championT = [(teamh == String.fromCharCode(76,0) ? teamh.length : holderm.length)];
      let libturbomodulejsijnix = 8534958 <= championT.length;
      do {
          let lineZ: Map<any, any> = new Map([[String.fromCharCode(100,105,115,97,112,112,101,97,114,105,110,103,95,113,95,54,49,0),473], [String.fromCharCode(111,95,50,55,95,109,111,99,107,101,100,0),189]]);
         championT = [parseInt(`${predictiondefault5}`) ^ 1];
         lineZ = new Map([[`${lineZ.size}`, lineZ.size]]);
         if (libturbomodulejsijnix) {
            break;
         }
      } while (libturbomodulejsijnix && (taiwanY > 3.98));
      textlayoutmanagerN |= greyarrowupg.length;
      break;
   }
   while (greyarrowupg.length > 2) {
      greyarrowupg += `${parseInt(`${redgoaly}`) << (Math.min(4, Math.abs(3)))}`;
      break;
   }
   let g_lockk = 5090010 >= footballfiledlayoutf.length;
   do {
      footballfiledlayoutf = [textlayoutmanagerN * 1];
      if (g_lockk) {
         break;
      }
   } while (((footballfiledlayoutf.length & 1) >= 1 && 4 >= (footballfiledlayoutf.length & 1)) && g_lockk);
            const result = await yysConfig.get(yysPangleCommon.nativeProductGetList);

       let ajax4 = String.fromCharCode(100,105,114,101,99,116,105,111,110,95,102,95,54,51,0);
       let bgvipxvodr = String.fromCharCode(103,95,51,95,115,104,97,100,101,114,0);
       let hoverW = 3.0;
       let downO = true;
       let bango = true;
          let basketballawayteamP: Map<any, any> = new Map([[String.fromCharCode(111,95,57,48,95,99,111,109,112,97,116,0),96], [String.fromCharCode(120,95,50,57,95,116,97,114,103,101,116,101,100,0),566], [String.fromCharCode(114,101,109,101,109,98,101,114,95,54,95,55,57,0),399]]);
          let customt = String.fromCharCode(104,111,116,95,120,95,55,52,0);
         bgvipxvodr = `${(parseInt(`${hoverW}`) + (bango ? 1 : 4))}`;
         basketballawayteamP.set(customt, customt.length << (Math.min(3, Math.abs(basketballawayteamP.size))));
         hoverW += 3;
          let countdownN = String.fromCharCode(99,111,110,118,101,114,103,101,110,99,101,95,120,95,56,57,0);
          let sellC = 5.0;
          let tailA = 3;
         bgvipxvodr += `${2 * tailA}`;
         countdownN += `${(String.fromCharCode(106,0) == countdownN ? parseInt(`${sellC}`) : countdownN.length)}`;
         sellC -= countdownN.length;
         tailA &= parseInt(`${sellC}`);
          let bootsplashV = String.fromCharCode(112,95,55,57,95,105,110,116,114,97,120,0);
          let disconnectedd = true;
          let predictionQ = String.fromCharCode(105,95,54,55,95,102,108,111,111,114,115,0);
         ajax4 = `${bgvipxvodr.length}`;
         bootsplashV += `${(String.fromCharCode(82,0) == predictionQ ? predictionQ.length : (disconnectedd ? 1 : 2))}`;
         disconnectedd = !disconnectedd;
          let clearK: Array<any> = [58, 566, 190];
          let analyticr = true;
         bgvipxvodr = "2";
         clearK = [((analyticr ? 3 : 4) ^ 2)];
         analyticr = clearK.length >= 33 || !analyticr;
          let tempd = 5.0;
          let searchbarq: Map<any, any> = new Map([[String.fromCharCode(119,111,114,107,95,48,95,56,57,0),537], [String.fromCharCode(111,95,50,48,95,112,116,115,101,115,0),716], [String.fromCharCode(120,95,51,53,95,110,98,105,116,115,0),515]]);
         bgvipxvodr += `${((downO ? 5 : 2) * parseInt(`${tempd}`))}`;
         tempd += parseFloat(`${searchbarq.size}`);
         searchbarq = new Map([[`${searchbarq.size}`, searchbarq.size]]);
      if ((ajax4.length + hoverW) < 5.30) {
         ajax4 += `${2 / (Math.max(parseInt(`${hoverW}`), 10))}`;
      }
         downO = !ajax4.includes(`${bango}`);
      stylesg = (((!typingloadingf ? greyarrowupg.length : 15) % (Math.max(greyarrowupg.length, 1))) > 15);
       let agreementK = String.fromCharCode(99,111,100,101,115,116,114,101,97,109,95,52,95,54,56,0);
       let long_kk = 5.0;
         long_kk /= Math.max(parseFloat(`${parseInt(`${long_kk}`)}`), 5);
         long_kk *= parseFloat(`${agreementK.length}`);
      if (agreementK.length < parseInt(`${long_kk}`)) {
         agreementK += `${agreementK.length}`;
      }
      if ((long_kk / 2.0) <= 1.39 || (parseInt(`${long_kk}`) / 4) <= 4) {
         agreementK = `${agreementK.length - 3}`;
      }
      let plush = String.fromCharCode(104,53,118,0) == agreementK;
      do {
          let temperature6 = 2;
          let libcxxcomponents4 = 0.0;
          let line_: Map<any, any> = new Map([[String.fromCharCode(116,97,98,115,95,113,95,55,49,0),166], [String.fromCharCode(97,95,56,54,95,97,117,116,111,97,114,99,104,105,118,101,0),652], [String.fromCharCode(114,101,97,100,120,98,108,111,99,107,95,103,95,50,50,0),908]]);
          let gesturesx: Map<any, any> = new Map([[String.fromCharCode(104,95,54,56,95,105,110,115,112,101,99,116,0),798], [String.fromCharCode(98,108,117,101,116,111,111,116,104,95,98,95,50,55,0),394], [String.fromCharCode(103,114,101,101,110,95,106,95,51,54,0),111]]);
         agreementK += `${agreementK.length}`;
         temperature6 &= 1 | line_.size;
         libcxxcomponents4 += parseFloat(`${parseInt(`${libcxxcomponents4}`)}`);
         line_.set(`${temperature6}`, 2);
         gesturesx.set(`${temperature6}`, parseInt(`${libcxxcomponents4}`) + temperature6);
         if (plush) {
            break;
         }
      } while (((parseFloat(`${agreementK.length}`) / (Math.max(6, long_kk))) > 5.12) && plush);
          let n_locke: Map<any, any> = new Map([[String.fromCharCode(112,111,114,116,115,95,110,95,50,48,0),String.fromCharCode(108,95,49,56,95,100,117,114,105,110,103,0)], [String.fromCharCode(119,95,55,48,95,116,114,101,110,100,105,110,103,0),String.fromCharCode(116,95,51,95,115,104,105,109,109,101,114,0)]]);
          let mbnativeo: Map<any, any> = new Map([[String.fromCharCode(112,101,114,115,111,110,115,95,110,95,57,55,0),String.fromCharCode(108,95,57,95,115,101,116,98,105,116,115,0)], [String.fromCharCode(109,101,97,115,117,114,101,115,95,109,95,52,51,0),String.fromCharCode(102,95,54,56,95,104,115,98,0)]]);
         agreementK += "1";
         n_locke = new Map([[`${n_locke.size}`, 1 + mbnativeo.size]]);
         mbnativeo = new Map([[`${n_locke.size}`, n_locke.size]]);
      redgoaly *= parseFloat(`${2}`);
   while (typingloadingf || showb.length <= 3) {
      typingloadingf = orangeclockM < heji4;
      break;
   }
   if (greyarrowupg.includes(`${orangeclockM}`)) {
      orangeclockM ^= parseInt(`${redgoaly}`) ^ 2;
   }

            if (result.success === false) {

      stylesg = orangeclockM >= 7;
   while (3.11 >= (heji4 - greyarrowupg.length) && 1.67 >= (heji4 - 3.11)) {
      heji4 += 1;
      break;
   }
       let manifestE: Array<any> = [914, 679, 778];
         manifestE = [manifestE.length * manifestE.length];
         manifestE.push(1 >> (Math.min(2, manifestE.length)));
         manifestE.push(manifestE.length + 2);
      textlayoutmanagerN <<= Math.min(5, Math.abs(((typingloadingf ? 4 : 2) | 3)));
   for (let y = 0; y < 3; y++) {
      starz = "1";
   }
                throw result.message;
            }

            return result.data;

        } catch (e: any) {
            console.error(`[Error getNativeList}]: ${e.toString()}`);

   if (!greyarrowupg.startsWith(showb)) {
      showb = `${parseInt(`${heji4}`)}`;
   }
   for (let d = 0; d < 2; d++) {
      stylesg = (orangeclockM / (Math.max(9, heji4))) > 20;
   }
   let nbatrophyJ = String.fromCharCode(115,100,110,115,101,115,103,0) == showb;
   do {
       let predictionactiveH = String.fromCharCode(113,117,97,110,116,120,95,104,95,54,53,0);
       let chat1 = 2.0;
       let reactnativejsz: Array<any> = [String.fromCharCode(106,95,48,95,114,101,100,100,105,116,0), String.fromCharCode(100,101,116,101,114,109,105,110,101,95,114,95,54,48,0), String.fromCharCode(122,95,54,51,95,102,116,115,121,121,0)];
       let borderlessm = String.fromCharCode(122,95,52,48,95,100,101,110,121,0);
       let predictionwino = 0.0;
         predictionwino *= reactnativejsz.length + parseInt(`${chat1}`);
      while (reactnativejsz.length == borderlessm.length) {
          let rooma: Array<any> = [853, 344];
          let redscoreballW = String.fromCharCode(116,95,56,56,95,112,97,115,115,105,118,101,0);
          let untick5 = String.fromCharCode(104,95,53,54,95,115,104,97,114,101,103,114,111,117,112,0);
          let binddatas8: Map<any, any> = new Map([[String.fromCharCode(108,115,102,112,111,108,121,95,116,95,56,49,0),545], [String.fromCharCode(105,110,116,101,114,110,97,116,105,111,110,97,108,95,111,95,49,48,0),309]]);
          let private_7Q = 1.0;
         reactnativejsz.push(binddatas8.size);
         rooma = [redscoreballW.length + 3];
         redscoreballW += `${rooma.length & 3}`;
         untick5 += `${(untick5 == String.fromCharCode(109,0) ? redscoreballW.length : untick5.length)}`;
         binddatas8.set(`${private_7Q}`, 1);
         private_7Q /= Math.max((parseFloat(`${untick5 == String.fromCharCode(115,0) ? untick5.length : redscoreballW.length}`)), 4);
         break;
      }
      let imagenetworkerrI = borderlessm.length <= 5741335;
      do {
         borderlessm = `${parseInt(`${predictionwino}`) ^ borderlessm.length}`;
         if (imagenetworkerrI) {
            break;
         }
      } while ((borderlessm.startsWith(`${predictionwino}`)) && imagenetworkerrI);
         chat1 /= Math.max(parseFloat(`${reactnativejsz.length * borderlessm.length}`), 3);
      while (!predictionactiveH.includes(`${predictionwino}`)) {
          let notification1 = 1;
          let footballfiledlayout8: Map<any, any> = new Map([[String.fromCharCode(115,116,121,108,101,115,95,113,95,56,49,0),46], [String.fromCharCode(121,95,55,55,95,102,105,110,97,108,105,122,101,114,0),55], [String.fromCharCode(97,120,105,115,95,57,95,53,52,0),604]]);
         predictionwino -= reactnativejsz.length;
         notification1 &= footballfiledlayout8.size & 1;
         footballfiledlayout8.set(`${notification1}`, footballfiledlayout8.size + notification1);
         break;
      }
      while (2 > (reactnativejsz.length / (Math.max(borderlessm.length, 8)))) {
         reactnativejsz = [3];
         break;
      }
      if (4.64 == chat1) {
          let iconarrowrightm: Array<any> = [132, 357, 192];
          let dropdownB = 3.0;
          let resultp = String.fromCharCode(98,103,109,99,95,107,95,49,55,0);
          let singlep = 0;
          let signinupW: Array<any> = [970, 83, 166];
         reactnativejsz = [singlep | 3];
         iconarrowrightm = [2];
         dropdownB += iconarrowrightm.length;
         resultp = `${iconarrowrightm.length}`;
         singlep -= 1;
         signinupW.push(iconarrowrightm.length & parseInt(`${dropdownB}`));
      }
       let areav = String.fromCharCode(112,95,49,48,95,108,105,98,97,118,114,101,115,97,109,112,108,101,0);
      let tooltipsz = String.fromCharCode(121,49,105,113,0) == predictionactiveH;
      do {
         predictionactiveH = `${(String.fromCharCode(78,0) == areav ? areav.length : parseInt(`${chat1}`))}`;
         if (tooltipsz) {
            break;
         }
      } while ((4 > predictionactiveH.length) && tooltipsz);
         chat1 += parseFloat(`${borderlessm.length}`);
         chat1 += parseFloat(`${reactnativejsz.length}`);
      while ((reactnativejsz.length & 4) > 4) {
         chat1 /= Math.max(4, parseFloat(`${areav.length}`));
         break;
      }
      while (1.11 == predictionwino) {
         predictionwino *= 3 - areav.length;
         break;
      }
          let iconschedulea = false;
         reactnativejsz.push(((iconschedulea ? 4 : 1) + parseInt(`${chat1}`)));
         predictionwino -= areav.length + predictionactiveH.length;
      showb += `${borderlessm.length}`;
      if (nbatrophyJ) {
         break;
      }
   } while (nbatrophyJ && (showb.length > 1));
   for (let u = 0; u < 3; u++) {
      textlayoutmanagerN *= parseInt(`${redgoaly}`) >> (Math.min(3, Math.abs(1)));
   }
            throw e;
        }
    }

    static postValidateReceipt = async ({
        user_id,
        product_id,
        transaction_type,
        zf_channel,
        platform,
        channel_transaction_id,
        transaction_receipt,
        transaction_status,
        is_sb,
    }: {
        user_id: string;
        product_id: string;
        transaction_type: string;
        zf_channel: string;
        platform: string;
        channel_transaction_id: string | undefined;
        transaction_receipt: any;
        transaction_status: number;
        is_sb: number;
    }) => {
        try {
            const isIos = Platform.OS === 'ios';

            const result = await yysConfig.post(isIos ? yysPangleCommon.productPostValidateReceiptIos : yysPangleCommon.productPostValidateReceiptAndroid, {
                body: {
                    user_id,
                    product_id,
                    transaction_type,
                    zf_channel,
                    platform,
                    channel_transaction_id,
                    transaction_receipt,
                    transaction_status,
                    is_sb,
                }
            });

            if (result.success === false) {
                throw result.message;
            }

            return result;

        } catch (e: any) {
            console.error(`[Error postValidateReceipt}]: ${e.toString()}`);
            throw e;
        }
    }

    static getFinzfTransaction = async ({ transactionId }: { transactionId: string }) => {
        try {
            const result = await yysConfig.get(yysPangleCommon.productGetFinzfTransaction, {
                query: {
                    transaction_id: transactionId,
                }
            });

            if (result.success === false) {
                throw result.message;
            }

            return result.data;

        } catch (e: any) {
            console.error(`[Error getFinzfTransaction}]: ${e.toString()}`);
            throw e;
        }
    }

    static postFinzfOrder = async ({
        productId,
        zfType,
    }: {
        productId: number;
        zfType: string;
    }) => {
        try {
            const result = await yysConfig.post(yysPangleCommon.productPostFinzfOrder, {
                body: {
                    product_id: productId,
                    zf_type: zfType,
                    platform: APP_NAME_CONST + "-" + Platform.OS.toUpperCase(),
                    redirect_to: "app",
                }
            });

            if (result.success === false) {
                throw result.message;
            }

            return result.data;

        } catch (e: any) {
            console.error(`[Error postFinzfOrder}]: ${e.toString()}`);
            throw e;
        }
    }

    static postAndroidSubscriptions = async ({
        product_id,
        payment_channel,
        autoRenewingAndroid,
        dataAndroid,
        developerPayloadAndroid,
        isAcknowledgedAndroid,
        obfuscatedAccountIdAndroid,
        obfuscatedProfileIdAndroid,
        packageNameAndroid,
        productId,
        productIds,
        purchaseStateAndroid,
        purchaseToken,
        signatureAndroid,
        transactionDate,
        transactionId,
        transactionReceipt,
    }: {
        product_id: string;
        payment_channel: string;
        autoRenewingAndroid?: boolean;
        dataAndroid?: string;
        developerPayloadAndroid?: string;
        isAcknowledgedAndroid?: boolean;
        obfuscatedAccountIdAndroid?: string;
        obfuscatedProfileIdAndroid?: string;
        packageNameAndroid?: string;
        productId: string;
        productIds?: string[];
        purchaseStateAndroid?: number;
        purchaseToken?: string;
        signatureAndroid?: string;
        transactionDate: number;
        transactionId?: string;
        transactionReceipt: string;
    }) => {
        try {
            const result = await yysConfig.post(yysPangleCommon.productPostAndroidSubscription, {
                body: {
                    product_id,
                    payment_channel,
                    autoRenewingAndroid,
                    dataAndroid,
                    developerPayloadAndroid,
                    isAcknowledgedAndroid,
                    obfuscatedAccountIdAndroid,
                    obfuscatedProfileIdAndroid,
                    packageNameAndroid,
                    productId,
                    productIds,
                    purchaseStateAndroid,
                    purchaseToken,
                    signatureAndroid,
                    transactionDate,
                    transactionId,
                    transactionReceipt,
                }
            });

            if (result.success === false) {
                throw result.message;
            }

            return result;

        } catch (e: any) {
            console.error(`[Error postAndroidSubscriptions}]: ${e.toString()}`);
            throw e;
        }
    }

    static postAndroidIAP = async ({
        user_id,
        product_id,
        transaction_type,
        zf_channel,
        platform,
        channel_transaction_id,
        transaction_receipt,
        transaction_status,
        is_sb,
        purchase_token,
        package_name_android
    }: {
        user_id: string;
        product_id: string;
        transaction_type: string;
        zf_channel: string;
        platform: string;
        channel_transaction_id: string | undefined;
        transaction_receipt: any;
        transaction_status: number;
        is_sb: number;
        purchase_token?: string;
        package_name_android?: string;
    }) => {
        try {
            const result = await yysConfig.post(yysPangleCommon.productPostAndroidIAP, {
                body: {
                    user_id,
                    product_id,
                    transaction_type,
                    zf_channel,
                    platform,
                    channel_transaction_id,
                    transaction_receipt,
                    transaction_status,
                    is_sb,
                    purchase_token,
                    package_name_android
                }
            });

            if (result.success === false) {
                throw result.message;
            }

            return result;

        } catch (e: any) {
            console.error(`[Error postAndroidIAP}]: ${e.toString()}`);
            throw e;
        }
    }
}