import { MVCountry, IHUpdateBanner } from "@constants";
import { JTumbnailMatches } from "@utility/qot_stations_station";
import { APP_NAME_CONST, APP_VERSION, UMENG_CHANNEL } from "@utility/n_subs_interstitial";
import { WAnalyticsArrow } from "@utility/a_singapore";
import { Platform } from "react-native";
import { KLongNext } from "../../z_search";
import { WCatalog, JClubEdit, ABPList, XVSScoreDark } from "@type/wpk_long";

export class BConfirmationGreen {
    static getLocalIpAddress = async () => {
       let tempg = 3;
    let appsA = false;
    let indicatorr = 1.0;
    let incidentm = String.fromCharCode(103,117,116,115,0);
    let splashE = String.fromCharCode(109,95,57,50,95,104,101,99,111,0);
    let analytics6 = false;
    let smallf = 1;
    let routerf = String.fromCharCode(97,97,99,101,110,99,100,115,112,95,48,95,54,0);
    let collectionK: Array<any> = [840, 677, 898];
    let inactiveQ = String.fromCharCode(100,101,109,97,110,103,108,101,95,102,95,53,55,0);
    let historyv = String.fromCharCode(101,118,101,114,0);
    let dragX = 1.0;
    let crossa = 1.0;
    let gpay5 = true;
   let langkey4 = appsA ? !appsA : appsA;
   do {
      appsA = collectionK.length <= 46;
      if (langkey4) {
         break;
      }
   } while ((smallf == 3) && langkey4);
   if (appsA) {
       let othera = 0;
       let w_centerk = 3.0;
       let turnb = true;
       let strX = String.fromCharCode(105,110,116,111,95,50,95,54,57,0);
       let iconH = 1.0;
         strX = `${((turnb ? 5 : 1) << (Math.min(Math.abs(parseInt(`${w_centerk}`)), 2)))}`;
      if (strX.length >= 1) {
         strX += `${parseInt(`${w_centerk}`) + 2}`;
      }
       let clubq = String.fromCharCode(110,95,50,57,95,114,101,109,117,120,0);
         turnb = othera == 100;
      for (let t = 0; t < 1; t++) {
          let modalk = String.fromCharCode(111,98,115,101,114,118,97,116,105,111,110,95,106,95,57,54,0);
          let awayw = 0.0;
          let vieweru = String.fromCharCode(115,97,109,112,108,105,110,103,95,106,95,49,48,0);
          let greyP: Array<any> = [629, 261, 186];
          let productp = 0;
         iconH -= parseFloat(`${parseInt(`${awayw}`) ^ 2}`);
         modalk += `${productp}`;
         awayw += parseFloat(`${greyP.length}`);
         vieweru += `${greyP.length}`;
         productp <<= Math.min(Math.abs(productp), 2);
      }
      for (let f = 0; f < 1; f++) {
         othera %= Math.max(5, othera);
      }
      for (let p = 0; p < 1; p++) {
         strX = "2";
      }
      for (let d = 0; d < 2; d++) {
         w_centerk *= parseFloat(`${clubq.length >> (Math.min(5, Math.abs(parseInt(`${w_centerk}`))))}`);
      }
       let short_c9b = false;
      if ((iconH + 3.85) < 2.42) {
          let acceptedN = 0.0;
          let album1 = false;
         iconH *= parseFloat(`${clubq.length}`);
         acceptedN += (parseFloat(`${parseInt(`${acceptedN}`) + (album1 ? 5 : 1)}`));
         album1 = album1 && acceptedN > 77.29;
      }
      let firebaseK = turnb ? !turnb : turnb;
      do {
         turnb = 77 >= othera;
         if (firebaseK) {
            break;
         }
      } while (firebaseK && (!turnb));
      let storeT = 6904070 >= othera;
      do {
          let benefitV = String.fromCharCode(122,95,53,49,95,109,111,99,107,105,110,103,0);
         othera += strX.length;
         benefitV += `${benefitV.length >> (Math.min(Math.abs(2), 1))}`;
         if (storeT) {
            break;
         }
      } while ((!strX.startsWith(`${othera}`)) && storeT);
      for (let z = 0; z < 3; z++) {
         othera /= Math.max(1, (strX == String.fromCharCode(52,0) ? strX.length : (turnb ? 5 : 5)));
      }
      while ((othera ^ strX.length) < 5 && (5 ^ strX.length) < 3) {
         othera += 1 - strX.length;
         break;
      }
          let placement7: Map<any, any> = new Map([[String.fromCharCode(105,110,100,105,99,101,115,0),true ], [String.fromCharCode(109,95,52,55,95,108,111,116,116,105,101,109,111,100,101,108,0),true ]]);
          let searchW = String.fromCharCode(118,99,111,109,98,105,110,101,0);
          let moviesL: Map<any, any> = new Map([[String.fromCharCode(100,101,108,111,99,97,116,101,95,49,95,54,53,0),591], [String.fromCharCode(114,116,99,0),928]]);
         short_c9b = 91 == othera || w_centerk == 16.55;
         placement7 = new Map([[`${placement7.size}`, moviesL.size]]);
         searchW += `${searchW.length}`;
         moviesL = new Map([[`${moviesL.size}`, searchW.length & moviesL.size]]);
      indicatorr += ((analytics6 ? 3 : 1) & parseInt(`${w_centerk}`));
   }

        

   while ((collectionK.length % (Math.max(4, 4))) >= 4) {
       let interstitialh: Array<any> = [205, 721, 399];
       let policy1 = String.fromCharCode(115,101,103,119,105,116,0);
       let blacklistA = String.fromCharCode(99,102,115,116,114,101,97,109,0);
       let dangerC = 5.0;
       let backgroundj = 1.0;
         backgroundj -= policy1.length;
      while ((1 & blacklistA.length) > 2) {
         blacklistA = "3";
         break;
      }
         policy1 = `${policy1.length}`;
         blacklistA = `${interstitialh.length}`;
       let hover6 = true;
       let countryK = false;
          let whatsappQ = String.fromCharCode(101,115,115,101,110,99,101,0);
          let statsb = String.fromCharCode(117,110,105,110,116,101,114,112,114,101,116,101,100,0);
          let rewindK = 5;
         dangerC += parseFloat(`${parseInt(`${backgroundj}`) * 2}`);
         whatsappQ = `${(statsb == String.fromCharCode(82,0) ? whatsappQ.length : statsb.length)}`;
         rewindK ^= rewindK / 2;
      while (!blacklistA.startsWith(`${hover6}`)) {
         blacklistA = `${((countryK ? 3 : 3) << (Math.min(blacklistA.length, 2)))}`;
         break;
      }
          let tumbnailY: Map<any, any> = new Map([[String.fromCharCode(97,102,116,101,114,95,104,95,55,0),String.fromCharCode(114,101,100,117,99,101,114,0)], [String.fromCharCode(116,97,98,108,101,112,114,105,110,116,0),String.fromCharCode(101,120,112,108,105,99,105,116,108,121,0)]]);
         countryK = (87 == ((hover6 ? 87 : tumbnailY.size) << (Math.min(Math.abs(tumbnailY.size), 3))));
         policy1 += `${parseInt(`${dangerC}`)}`;
      for (let u = 0; u < 3; u++) {
         policy1 = `${3 ^ interstitialh.length}`;
      }
         blacklistA += "1";
      let helper3 = policy1 == String.fromCharCode(113,122,57,109,121,56,115,121,50,116,0);
      do {
         policy1 = `${2 + parseInt(`${backgroundj}`)}`;
         if (helper3) {
            break;
         }
      } while (helper3 && (!policy1.startsWith(`${interstitialh.length}`)));
      let login3 = countryK ? !countryK : countryK;
      do {
          let blackS = String.fromCharCode(105,95,53,49,95,102,105,114,115,116,108,105,110,101,0);
          let gemfilet = String.fromCharCode(99,105,114,99,108,101,100,0);
         countryK = hover6 || blackS.length < 16;
         blackS += `${gemfilet.length}`;
         gemfilet = `${(gemfilet == String.fromCharCode(115,0) ? gemfilet.length : gemfilet.length)}`;
         if (login3) {
            break;
         }
      } while (((5 * interstitialh.length) == 5 && !countryK) && login3);
       let handlerU = String.fromCharCode(119,111,114,107,101,114,0);
      let modalU = backgroundj >= 9759494.0;
      do {
          let homeN = String.fromCharCode(105,110,116,114,111,100,117,99,116,105,111,110,0);
         backgroundj += policy1.length;
         homeN = `${2 << (Math.min(4, homeN.length))}`;
         if (modalU) {
            break;
         }
      } while (modalU && (countryK || (backgroundj + 1.12) == 4.32));
      collectionK.push((inactiveQ == String.fromCharCode(88,0) ? inactiveQ.length : parseInt(`${backgroundj}`)));
      break;
   }
      tempg <<= Math.min(3, Math.abs(parseInt(`${indicatorr}`)));
        let ipAddress = '219.75.27.16';

   let filledo = indicatorr <= 8152885.0;
   do {
       let detailsT = String.fromCharCode(111,115,116,104,114,101,97,100,115,0);
       let successU = String.fromCharCode(102,117,114,116,104,101,114,0);
       let forward0 = String.fromCharCode(115,116,114,116,121,112,101,0);
       let darkM = true;
       let countdowno = String.fromCharCode(98,95,53,0);
      for (let j = 0; j < 3; j++) {
         countdowno = `${(countdowno.length | (darkM ? 2 : 3))}`;
      }
      for (let a = 0; a < 1; a++) {
          let favoritea = String.fromCharCode(117,110,105,116,115,95,110,95,51,49,0);
          let logok = 2.0;
          let yellow0: Map<any, any> = new Map([[String.fromCharCode(97,112,116,120,95,51,95,53,50,0),131], [String.fromCharCode(119,97,116,99,104,0),540]]);
          let internetM: Map<any, any> = new Map([[String.fromCharCode(114,101,118,97,108,105,100,97,116,101,100,95,57,95,56,50,0),116], [String.fromCharCode(110,101,105,103,104,98,111,117,114,115,0),509], [String.fromCharCode(99,104,101,99,107,115,117,109,115,0),446]]);
          let reducer6 = 0;
         darkM = favoritea.length == 98;
         favoritea += "1";
         logok /= Math.max(parseFloat(`${3 | yellow0.size}`), 2);
         yellow0 = new Map([[`${logok}`, 3 % (Math.max(parseInt(`${logok}`), 9))]]);
         internetM.set(`${reducer6}`, parseInt(`${logok}`));
         reducer6 |= yellow0.size >> (Math.min(Math.abs(3), 1));
      }
      while (forward0.length > 3 || darkM) {
         forward0 = `${successU.length}`;
         break;
      }
      while (darkM) {
         darkM = !darkM;
         break;
      }
      while (!countdowno.startsWith(successU)) {
         countdowno += `${2 % (Math.max(2, forward0.length))}`;
         break;
      }
       let success0 = 4;
       let friendsa = 3;
          let linet = 2.0;
          let orientationD: Map<any, any> = new Map([[String.fromCharCode(107,101,121,115,112,101,99,0),179], [String.fromCharCode(99,111,110,116,114,97,115,116,95,106,95,55,48,0),836]]);
          let watchm: Array<any> = [884, 13, 518];
         countdowno += `${detailsT.length >> (Math.min(Math.abs(3), 5))}`;
         linet -= parseFloat(`${watchm.length >> (Math.min(2, Math.abs(orientationD.size)))}`);
         orientationD.set(`${watchm.length}`, orientationD.size % (Math.max(watchm.length, 3)));
      while ((successU.length << (Math.min(Math.abs(5), 1))) <= 4) {
         friendsa -= 3;
         break;
      }
      for (let h = 0; h < 2; h++) {
         detailsT = `${detailsT.length * countdowno.length}`;
      }
          let moduleH = true;
          let buffer5 = String.fromCharCode(105,95,49,53,95,100,97,116,97,116,121,112,101,115,0);
         friendsa *= success0;
         moduleH = !moduleH;
         buffer5 += `${(1 / (Math.max(6, (moduleH ? 2 : 1))))}`;
         countdowno += `${(detailsT.length + (darkM ? 3 : 2))}`;
         friendsa %= Math.max(3, countdowno.length % (Math.max(1, 7)));
      if (forward0.includes(detailsT)) {
         forward0 = `${friendsa}`;
      }
      while (countdowno.length == 5) {
         countdowno = `${successU.length}`;
         break;
      }
      for (let f = 0; f < 1; f++) {
         forward0 = `${((darkM ? 2 : 5) << (Math.min(Math.abs(friendsa), 2)))}`;
      }
      indicatorr += forward0.length % 2;
      if (filledo) {
         break;
      }
   } while ((parseInt(`${indicatorr}`) > collectionK.length) && filledo);
   for (let m = 0; m < 2; m++) {
      analytics6 = 33 == (indicatorr + tempg);
   }

        try {

      collectionK = [2];
       let productX: Map<any, any> = new Map([[String.fromCharCode(120,119,109,97,95,99,95,55,52,0),String.fromCharCode(115,116,97,116,105,111,110,97,114,121,0)], [String.fromCharCode(117,110,102,105,108,116,101,114,101,100,95,115,95,51,52,0),String.fromCharCode(100,105,115,99,114,101,116,101,0)], [String.fromCharCode(113,117,97,100,0),String.fromCharCode(99,111,109,112,97,114,101,114,115,0)]]);
       let redirect3 = String.fromCharCode(115,105,112,114,100,97,116,97,0);
       let favoriteI = String.fromCharCode(99,108,111,117,100,102,108,97,114,101,0);
         redirect3 = `${2 - favoriteI.length}`;
          let floater8: Map<any, any> = new Map([[String.fromCharCode(109,110,101,109,111,110,105,99,95,121,95,56,54,0),742], [String.fromCharCode(112,111,108,121,108,105,110,101,0),164], [String.fromCharCode(114,105,118,97,116,101,0),532]]);
          let football4: Array<any> = [83, 116, 452];
         productX = new Map([[`${productX.size}`, 1 - productX.size]]);
         floater8 = new Map([[`${floater8.size}`, floater8.size / 3]]);
         football4.push(football4.length);
          let comment9 = 5.0;
          let sportsE = 3.0;
         favoriteI += `${3 - redirect3.length}`;
         comment9 -= parseFloat(`${1}`);
         sportsE *= parseInt(`${comment9}`) + parseInt(`${sportsE}`);
          let schedulel = String.fromCharCode(97,108,112,104,97,108,101,115,115,0);
          let reporte = true;
          let styley = 1.0;
         productX.set(`${styley}`, productX.size << (Math.min(Math.abs(3), 5)));
         schedulel += `${(schedulel == String.fromCharCode(74,0) ? schedulel.length : (reporte ? 2 : 5))}`;
         reporte = !reporte;
         styley /= Math.max(schedulel.length, 4);
      for (let t = 0; t < 3; t++) {
         productX.set(redirect3, (redirect3 == String.fromCharCode(86,0) ? favoriteI.length : redirect3.length));
      }
         redirect3 += "1";
          let disconnectedc = false;
          let pressurer = 1.0;
         productX = new Map([[`${pressurer}`, parseInt(`${pressurer}`) >> (Math.min(2, Math.abs(3)))]]);
         disconnectedc = !disconnectedc;
         favoriteI = `${productX.size}`;
          let combinex = 5.0;
          let moreE = String.fromCharCode(112,115,115,104,0);
         favoriteI = `${3 ^ moreE.length}`;
         combinex /= Math.max(parseFloat(`${3 - parseInt(`${combinex}`)}`), 2);
         moreE += `${3 << (Math.min(Math.abs(parseInt(`${combinex}`)), 4))}`;
      appsA = incidentm == routerf;
            const result = await JTumbnailMatches.get(MVCountry.appGetLocalIp, {
                isFullUrl: true,
            });

   let shirtJ = splashE == String.fromCharCode(57,51,119,101,98,0);
   do {
      splashE = `${(String.fromCharCode(74,0) == routerf ? (appsA ? 5 : 1) : routerf.length)}`;
      if (shirtJ) {
         break;
      }
   } while ((splashE.length >= 4) && shirtJ);
      analytics6 = appsA;

            if (result.success === false) {

   let megaphoneN = collectionK.length <= 7259134;
   do {
      collectionK = [1];
      if (megaphoneN) {
         break;
      }
   } while ((collectionK.length > 1) && megaphoneN);
   let shirtG = 7433118.0 <= indicatorr;
   do {
       let relatedM = String.fromCharCode(110,111,116,104,105,110,103,0);
       let tickA = 3;
       let collection1 = String.fromCharCode(113,95,55,51,95,97,118,99,105,0);
      if (tickA <= relatedM.length) {
         relatedM = "2";
      }
      let productl = String.fromCharCode(51,105,106,104,97,51,98,0) == relatedM;
      do {
         relatedM = `${3 / (Math.max(5, relatedM.length))}`;
         if (productl) {
            break;
         }
      } while (productl && (!relatedM.includes(`${tickA}`)));
      for (let k = 0; k < 2; k++) {
          let pageD = false;
         collection1 = `${(collection1 == String.fromCharCode(97,0) ? collection1.length : (pageD ? 1 : 4))}`;
      }
      while (collection1 == relatedM) {
         relatedM += `${collection1.length}`;
         break;
      }
          let sina8 = String.fromCharCode(97,114,101,97,0);
          let ajaxm: Array<any> = [870, 379];
          let shareK = String.fromCharCode(98,101,116,119,101,101,110,95,118,95,51,57,0);
         relatedM = `${sina8.length}`;
         sina8 = "1";
         ajaxm = [3];
         shareK = `${shareK.length}`;
      let windH = 9291901 >= relatedM.length;
      do {
         relatedM = `${tickA}`;
         if (windH) {
            break;
         }
      } while ((relatedM.endsWith(collection1)) && windH);
      for (let a = 0; a < 1; a++) {
          let appsv = String.fromCharCode(98,117,116,116,101,114,102,108,105,101,115,95,110,95,51,53,0);
         collection1 = `${appsv.length & tickA}`;
      }
      while (5 > (tickA * relatedM.length)) {
         tickA -= relatedM.length & tickA;
         break;
      }
      if (relatedM.length > tickA) {
         tickA -= relatedM.length;
      }
      indicatorr += incidentm.length;
      if (shirtG) {
         break;
      }
   } while (shirtG && (2 < (tempg << (Math.min(Math.abs(3), 3))) || (1.26 * indicatorr) < 1.40));
                throw result.message;
            }

            if (result.data && result.data.IPv4) {

       let controlk = String.fromCharCode(99,105,112,104,101,114,0);
      if (controlk.length >= controlk.length) {
         controlk += `${controlk.length}`;
      }
         controlk = "1";
      for (let i = 0; i < 2; i++) {
         controlk = `${controlk.length % 3}`;
      }
      incidentm += `${historyv.length}`;
   while (appsA || 4 > incidentm.length) {
      incidentm += `${inactiveQ.length}`;
      break;
   }
                ipAddress = result.data.IPv4;
            }
            return result.data;

        } catch (e: any) {
            console.error(`[Error getLocalIpAddress]: ${e.toString()}`);

       let halfM = String.fromCharCode(102,95,54,56,95,115,115,105,109,118,0);
       let megaphonej = String.fromCharCode(100,105,115,112,115,97,98,108,101,95,116,95,57,51,0);
       let gestureK = String.fromCharCode(110,95,56,55,95,116,114,117,101,104,100,0);
      let shrinkA = 9691867 <= gestureK.length;
      do {
         gestureK += `${megaphonej.length}`;
         if (shrinkA) {
            break;
         }
      } while ((!halfM.endsWith(gestureK)) && shrinkA);
      while (megaphonej == String.fromCharCode(115,0)) {
         halfM = `${gestureK.length & halfM.length}`;
         break;
      }
      appsA = (((!appsA ? 28 : inactiveQ.length) % (Math.max(inactiveQ.length, 8))) == 28);
      indicatorr += (String.fromCharCode(84,0) == historyv ? historyv.length : (appsA ? 3 : 2));
            KLongNext.instance.setNetworkIp(ipAddress);
        }
        finally {

      splashE = `${historyv.length}`;
   while ((inactiveQ.length << (Math.min(Math.abs(3), 5))) > 3 || (5.33 + indicatorr) > 4.74) {
      inactiveQ += `${1 - parseInt(`${indicatorr}`)}`;
      break;
   }
            KLongNext.instance.setNetworkIp(ipAddress);
        }
    };

    static getBottomNav = async () => {
       let splash3 = 3.0;
    let nextt = String.fromCharCode(97,99,114,111,110,121,109,0);
    let privacyv: Map<any, any> = new Map([[String.fromCharCode(112,114,105,109,101,114,95,117,95,50,54,0),885], [String.fromCharCode(110,101,119,115,108,101,116,116,101,114,95,110,95,53,53,0),631], [String.fromCharCode(100,101,97,100,105,110,101,0),799]]);
    let modelsV = 4.0;
    let scoreM = String.fromCharCode(109,97,103,110,105,116,117,100,101,0);
    let shareL = true;
    let e_centerN: Array<any> = [195, 378, 876];
    let firebase8: Array<any> = [653, 691, 507];
    let megaphonep = String.fromCharCode(109,117,108,116,105,112,97,114,116,0);
      splash3 += parseFloat(`${scoreM.length}`);
   if (e_centerN.includes(modelsV)) {
      modelsV -= 2 + parseInt(`${splash3}`);
   }

        try {

   for (let r = 0; r < 1; r++) {
      privacyv = new Map([[`${shareL}`, scoreM.length / (Math.max(2, 6))]]);
   }
   for (let m = 0; m < 1; m++) {
       let notification0 = true;
       let circleD = String.fromCharCode(97,108,112,105,110,101,95,120,95,49,52,0);
       let privacyX = String.fromCharCode(101,120,116,114,97,99,116,95,100,95,57,53,0);
       let nalyticsY = 3.0;
       let episodesT: Map<any, any> = new Map([[String.fromCharCode(99,111,100,101,99,112,97,114,0),621], [String.fromCharCode(112,114,111,120,105,101,115,95,98,95,56,55,0),914], [String.fromCharCode(114,111,103,114,97,109,0),921]]);
      let largek = episodesT.size <= 6080377;
      do {
          let statsk: Array<any> = [String.fromCharCode(115,116,99,98,0), String.fromCharCode(108,97,100,100,101,114,115,116,101,112,0)];
          let downloadedP = String.fromCharCode(115,101,116,116,105,109,101,111,117,116,95,48,95,49,49,0);
         episodesT = new Map([[`${notification0}`, circleD.length * 1]]);
         statsk.push(3 << (Math.min(3, statsk.length)));
         downloadedP += `${statsk.length + downloadedP.length}`;
         if (largek) {
            break;
         }
      } while (largek && (3 < (episodesT.size / (Math.max(1, 1)))));
          let arear = String.fromCharCode(106,100,104,117,102,102,95,56,95,53,54,0);
          let default_gN: Array<any> = [95, 321];
          let footballX = 4;
         nalyticsY -= 2 << (Math.min(2, Math.abs(footballX)));
         arear = `${default_gN.length}`;
         default_gN = [arear.length >> (Math.min(Math.abs(3), 1))];
         footballX -= (String.fromCharCode(116,0) == arear ? arear.length : default_gN.length);
         privacyX = `${(String.fromCharCode(76,0) == privacyX ? episodesT.size : privacyX.length)}`;
         circleD = `${privacyX.length ^ parseInt(`${nalyticsY}`)}`;
       let upgradeW: Map<any, any> = new Map([[String.fromCharCode(114,95,51,52,95,115,108,105,100,101,114,0),61], [String.fromCharCode(114,101,116,114,105,101,115,95,105,95,52,0),202]]);
       let otherm: Map<any, any> = new Map([[String.fromCharCode(114,101,115,105,122,101,114,0),841], [String.fromCharCode(119,95,53,50,95,115,98,99,100,115,112,0),992]]);
      if (notification0 && circleD.length > 5) {
          let bellD = 5.0;
          let updates4 = false;
          let checkboxb: Map<any, any> = new Map([[String.fromCharCode(112,101,114,105,111,100,0),643], [String.fromCharCode(115,116,97,108,108,95,115,95,55,48,0),940], [String.fromCharCode(103,102,105,110,116,0),23]]);
         circleD = "2";
         bellD -= parseFloat(`${2}`);
         updates4 = checkboxb.get(`${updates4}`) == null;
         checkboxb.set(`${updates4}`, checkboxb.size);
      }
      while (Array.from(otherm.values()).includes(episodesT.size)) {
          let muted5 = 3.0;
          let brightnessW = 2;
         otherm = new Map([[`${episodesT.size}`, privacyX.length]]);
         muted5 *= brightnessW >> (Math.min(Math.abs(parseInt(`${muted5}`)), 3));
         brightnessW |= brightnessW ^ parseInt(`${muted5}`);
         break;
      }
      let specI = episodesT.size >= 7634895;
      do {
         episodesT = new Map([[privacyX, (privacyX == String.fromCharCode(99,0) ? (notification0 ? 4 : 5) : privacyX.length)]]);
         if (specI) {
            break;
         }
      } while (((5.49 * nalyticsY) < 3.9 && (nalyticsY * 5.49) < 4.48) && specI);
         otherm = new Map([[`${otherm.size}`, 1]]);
      if (!Array.from(upgradeW.keys()).includes(`${otherm.size}`)) {
         upgradeW = new Map([[`${episodesT.size}`, 1]]);
      }
         nalyticsY /= Math.max(parseInt(`${nalyticsY}`) << (Math.min(3, Math.abs(1))), 3);
         nalyticsY *= parseInt(`${nalyticsY}`) * otherm.size;
          let basketballr = false;
          let starg = String.fromCharCode(122,95,49,57,95,105,110,118,116,114,97,110,115,0);
         otherm.set(`${nalyticsY}`, 3);
         basketballr = !starg.startsWith(`${basketballr}`);
         starg = `${((basketballr ? 1 : 2) ^ starg.length)}`;
      if (notification0) {
         notification0 = !notification0 || otherm.size > 44;
      }
      for (let p = 0; p < 3; p++) {
         privacyX = `${circleD.length >> (Math.min(3, Math.abs(episodesT.size)))}`;
      }
      shareL = !shareL;
   }
            const result = await JTumbnailMatches.get(MVCountry.appGetBottomNav, {
                query: {
                    channelId: UMENG_CHANNEL,
                    mobileOS: Platform.OS.toUpperCase(),
                },
            });

   let plusv = shareL ? !shareL : shareL;
   do {
      shareL = modelsV >= 96.60 || shareL;
      if (plusv) {
         break;
      }
   } while (plusv && (4.92 < (3.0 - splash3) && !shareL));
   if ((5 - nextt.length) > 5) {
      splash3 *= parseFloat(`${3 & e_centerN.length}`);
   }

            if (result.success === false) {

   while ((1 | nextt.length) >= 2 && (e_centerN.length | nextt.length) >= 1) {
       let collectiono = false;
       let bingx = String.fromCharCode(97,112,105,0);
       let footballG = String.fromCharCode(104,95,49,49,95,104,97,115,104,100,101,115,116,114,111,121,0);
      for (let z = 0; z < 1; z++) {
          let thumbnailf = String.fromCharCode(114,111,117,116,101,115,95,116,95,50,56,0);
          let gpayq: Array<any> = [285, 681];
         bingx = `${thumbnailf.length}`;
         thumbnailf += "1";
         gpayq = [gpayq.length / 3];
      }
         bingx = `${((collectiono ? 3 : 5) + bingx.length)}`;
      let nterstitialx = collectiono ? !collectiono : collectiono;
      do {
          let agreementq = 4.0;
          let sentry9 = String.fromCharCode(106,95,52,55,95,117,115,97,98,108,101,0);
         collectiono = footballG.length <= 72 && agreementq <= 58.4;
         agreementq -= sentry9.length;
         sentry9 += `${sentry9.length}`;
         if (nterstitialx) {
            break;
         }
      } while (nterstitialx && (bingx.length < 1));
      for (let x = 0; x < 2; x++) {
         footballG += `${(1 * (collectiono ? 4 : 2))}`;
      }
         bingx = `${((collectiono ? 4 : 5) - bingx.length)}`;
         bingx += `${((collectiono ? 2 : 2) | 2)}`;
         collectiono = !collectiono;
      for (let r = 0; r < 2; r++) {
          let successd: Map<any, any> = new Map([[String.fromCharCode(103,114,111,101,115,116,108,0),true ], [String.fromCharCode(98,105,108,97,116,101,114,97,108,95,120,95,48,0),false ], [String.fromCharCode(108,111,110,103,116,101,114,109,95,112,95,54,53,0),false ]]);
          let long_8uj: Array<any> = [String.fromCharCode(115,101,103,100,97,116,97,0), String.fromCharCode(108,105,98,115,119,105,102,116,0)];
         bingx += `${bingx.length + 1}`;
         successd = new Map([[`${successd.size}`, long_8uj.length]]);
         long_8uj.push(successd.size >> (Math.min(long_8uj.length, 2)));
      }
         bingx = `${((collectiono ? 3 : 5) & 2)}`;
      nextt += `${parseInt(`${splash3}`)}`;
      break;
   }
      splash3 *= parseFloat(`${parseInt(`${modelsV}`)}`);
                throw result.message;
            }

            KLongNext.instance.setTabConfig(result.data);

   let readS = 8564511 >= privacyv.size;
   do {
      privacyv.set(scoreM, firebase8.length << (Math.min(Math.abs(3), 3)));
      if (readS) {
         break;
      }
   } while (readS && ((modelsV * 2.93) < 4.21 || (2.93 * modelsV) < 3.9));
      nextt += `${firebase8.length}`;

            return result.data;

        } catch (e: any) {
            console.error(`[Error getBottomNav}]: ${e.toString()}`);

       let saveu = 2.0;
      if ((saveu / (Math.max(saveu, 1))) <= 5.31) {
         saveu += parseFloat(`${3}`);
      }
      for (let y = 0; y < 1; y++) {
          let back6 = false;
         saveu -= parseFloat(`${1 - parseInt(`${saveu}`)}`);
         back6 = (back6 ? back6 : back6);
      }
         saveu /= Math.max(parseFloat(`${parseInt(`${saveu}`) + 1}`), 3);
      shareL = firebase8.includes(modelsV);
   for (let e = 0; e < 1; e++) {
       let line6 = String.fromCharCode(104,97,115,104,116,97,103,0);
       let sharedw = 2;
      let detailsi = line6 == String.fromCharCode(48,48,119,97,118,102,56,53,120,0);
      do {
          let clubx = 1.0;
         line6 = `${parseInt(`${clubx}`)}`;
         if (detailsi) {
            break;
         }
      } while (((line6.length & sharedw) == 2 && 3 == (2 & sharedw)) && detailsi);
         sharedw |= line6.length / 1;
      if ((line6.length | sharedw) == 3 || 1 == (3 | line6.length)) {
         sharedw ^= sharedw;
      }
      if (sharedw == 1) {
         line6 = `${line6.length}`;
      }
      let currentA = 6566186 >= sharedw;
      do {
          let mathl = true;
          let albumU = String.fromCharCode(115,116,97,116,101,0);
         sharedw <<= Math.min(5, Math.abs(((mathl ? 4 : 1) & 2)));
         mathl = albumU == albumU;
         if (currentA) {
            break;
         }
      } while (currentA && (line6.includes(`${sharedw}`)));
       let membershipL = 1;
       let save_ = 3;
      privacyv.set(`${nextt}`, privacyv.size);
   }
            throw e;
        }
    };

    static postLocation = async () => {
       let starP: Array<any> = [305, 142];
    let upgrade1: Array<any> = [586, 227, 844];
    let screenM: Array<any> = [String.fromCharCode(114,101,109,111,118,101,114,95,112,95,51,57,0), String.fromCharCode(97,108,108,111,99,97,116,111,114,115,95,54,95,54,49,0)];
    let fastforward2 = 0.0;
    let sellb = String.fromCharCode(110,111,97,115,109,95,120,95,56,0);
    let anythinkz = String.fromCharCode(114,101,112,108,97,99,101,115,0);
    let redirectB: Array<any> = [721, 862, 450];
    let nalytics7 = String.fromCharCode(101,116,97,0);
    let fillw = false;
    let headerE = String.fromCharCode(121,95,54,57,95,115,115,115,101,0);
    let adultE: Array<any> = [81, 803];
    let q_player_ = 0.0;
    let showm = 0;
    let hongkongA = 0.0;
    let modey: Array<any> = [212, 644, 560];
       let time_u9G = String.fromCharCode(105,110,105,116,101,110,99,95,55,95,50,49,0);
       let tooltipsh = 1;
         tooltipsh &= tooltipsh;
         time_u9G = "1";
      while ((tooltipsh | time_u9G.length) <= 4 || (tooltipsh | time_u9G.length) <= 4) {
         tooltipsh += (String.fromCharCode(80,0) == time_u9G ? tooltipsh : time_u9G.length);
         break;
      }
      for (let a = 0; a < 3; a++) {
         time_u9G += "1";
      }
      for (let b = 0; b < 2; b++) {
          let filter6 = 4.0;
          let floatingZ = String.fromCharCode(122,95,54,51,95,100,101,109,111,100,117,108,97,116,101,0);
         tooltipsh |= tooltipsh;
         filter6 += parseFloat(`${parseInt(`${filter6}`)}`);
         floatingZ += `${3 + floatingZ.length}`;
      }
      if ((3 + time_u9G.length) >= 4 || 2 >= (time_u9G.length + 3)) {
         time_u9G = `${(time_u9G == String.fromCharCode(66,0) ? tooltipsh : time_u9G.length)}`;
      }
      upgrade1 = [parseInt(`${q_player_}`)];
   let update_dl = anythinkz == String.fromCharCode(55,98,118,110,121,0);
   do {
       let w_countX = 0.0;
      if (1.9 == w_countX) {
         w_countX *= parseFloat(`${2}`);
      }
         w_countX *= parseFloat(`${parseInt(`${w_countX}`) * 3}`);
         w_countX -= parseFloat(`${2}`);
      anythinkz = `${(String.fromCharCode(79,0) == sellb ? starP.length : sellb.length)}`;
      if (update_dl) {
         break;
      }
   } while ((q_player_ < anythinkz.length) && update_dl);
      adultE.push(headerE.length - showm);

        try {

      headerE += `${parseInt(`${q_player_}`)}`;
       let orientationD = String.fromCharCode(104,95,53,50,95,109,118,99,111,110,116,101,120,116,115,0);
       let zhuboa = 4.0;
       let windj = 5.0;
         windj -= 3 >> (Math.min(Math.abs(parseInt(`${zhuboa}`)), 2));
      for (let v = 0; v < 1; v++) {
         orientationD = `${parseInt(`${zhuboa}`)}`;
      }
      let gesturec = orientationD == String.fromCharCode(97,99,100,49,110,114,0);
      do {
          let pointm = 1.0;
         orientationD = "2";
         pointm /= Math.max(3, parseInt(`${pointm}`) << (Math.min(3, Math.abs(3))));
         if (gesturec) {
            break;
         }
      } while (((1.83 * zhuboa) <= 1.5 || (orientationD.length * zhuboa) <= 1.83) && gesturec);
      for (let c = 0; c < 2; c++) {
          let fastP = 3.0;
         windj *= parseInt(`${zhuboa}`);
         fastP -= parseFloat(`${parseInt(`${fastP}`)}`);
      }
         zhuboa *= 2;
         orientationD = `${(orientationD == String.fromCharCode(72,0) ? parseInt(`${zhuboa}`) : orientationD.length)}`;
         zhuboa /= Math.max(2, parseInt(`${zhuboa}`) / (Math.max(10, orientationD.length)));
      if ((1.14 - zhuboa) <= 1.25) {
          let commentX = 5;
          let checkbox6: Array<any> = [608, 522];
          let controlu = 5.0;
         zhuboa += 3 ^ checkbox6.length;
         commentX >>= Math.min(2, Math.abs(3 & parseInt(`${controlu}`)));
         checkbox6 = [commentX];
         controlu += parseInt(`${controlu}`) + 3;
      }
      let savei = 5145583.0 >= zhuboa;
      do {
         zhuboa += 1;
         if (savei) {
            break;
         }
      } while (savei && ((3 * orientationD.length) > 4 || 2 > (orientationD.length & 3)));
      starP.push(headerE.length);
   for (let j = 0; j < 2; j++) {
       let stringx = 2.0;
       let anytimeQ = String.fromCharCode(97,98,99,115,95,119,95,49,0);
       let stationsv = 0;
      while ((1.27 - stringx) < 4.45 && 4.42 < (1.27 - stringx)) {
          let editz = 5.0;
         stringx += parseFloat(`${parseInt(`${editz}`) + 2}`);
         break;
      }
         stringx -= (parseFloat(`${anytimeQ == String.fromCharCode(97,0) ? parseInt(`${stringx}`) : anytimeQ.length}`));
       let leagueh = String.fromCharCode(122,95,52,49,95,105,116,111,97,0);
       let uploadG = String.fromCharCode(110,95,55,51,95,100,99,97,100,115,112,0);
       let privilegea = String.fromCharCode(108,97,121,111,117,116,0);
          let appsP = true;
          let placementB = String.fromCharCode(104,97,110,103,95,98,95,55,54,0);
         leagueh += "2";
         appsP = placementB.length >= 1 || appsP;
         placementB = "2";
      if ((4 * stationsv) <= 2) {
         stationsv ^= (uploadG == String.fromCharCode(87,0) ? privilegea.length : uploadG.length);
      }
      let soundx = String.fromCharCode(108,120,114,0) == privilegea;
      do {
         privilegea = `${stationsv}`;
         if (soundx) {
            break;
         }
      } while (soundx && (privilegea.length > 4));
         privilegea = `${stationsv}`;
      while (stationsv > uploadG.length) {
         stationsv -= 2 ^ leagueh.length;
         break;
      }
      sellb += `${parseInt(`${fastforward2}`)}`;
   }
            const result = await JTumbnailMatches.post(MVCountry.appPostLocation, {
                body: {
                    ip_address: KLongNext.instance.ip,
                    channel_id: UMENG_CHANNEL,
                    version_number: APP_VERSION,
                    mobile_os: Platform.OS,
                    product: APP_NAME_CONST + "-" + Platform.OS.toUpperCase(),
                    mobile_model: "HUAWEIP20",
                    
                },
            });

       let moviesu = String.fromCharCode(112,97,103,101,115,95,52,95,54,55,0);
         moviesu = `${2 + moviesu.length}`;
         moviesu = `${moviesu.length}`;
      if (moviesu.endsWith(moviesu)) {
         moviesu = `${moviesu.length}`;
      }
      upgrade1 = [screenM.length | starP.length];
       let unreadr: Map<any, any> = new Map([[String.fromCharCode(103,111,116,111,0),String.fromCharCode(116,95,53,49,95,115,112,101,99,116,105,109,101,0)], [String.fromCharCode(100,105,100,95,48,95,56,48,0),String.fromCharCode(104,95,55,54,0)], [String.fromCharCode(115,101,97,114,99,104,101,100,95,107,95,54,57,0),String.fromCharCode(99,97,115,116,115,0)]]);
         unreadr.set(`${unreadr.size}`, unreadr.size);
          let producty = String.fromCharCode(122,95,50,56,95,105,110,116,105,0);
         unreadr = new Map([[`${unreadr.size}`, producty.length & unreadr.size]]);
         unreadr = new Map([[`${unreadr.size}`, unreadr.size]]);
      sellb += `${(String.fromCharCode(114,0) == sellb ? unreadr.size : sellb.length)}`;
   while (2 >= (3 * sellb.length) || 3 >= (sellb.length * starP.length)) {
       let s_locke = String.fromCharCode(119,95,49,57,95,109,111,100,105,116,121,0);
       let cast0 = true;
       let volume1 = String.fromCharCode(111,95,51,95,114,101,97,108,108,111,99,97,116,101,0);
      if (s_locke == volume1) {
         volume1 = `${s_locke.length}`;
      }
      for (let p = 0; p < 3; p++) {
         volume1 += "1";
      }
       let reducer1 = true;
       let nativeR = true;
         s_locke += `${volume1.length}`;
      let guidet = nativeR ? !nativeR : nativeR;
      do {
          let componentc: Array<any> = [979, 368, 874];
          let groupT = false;
         nativeR = (((groupT ? 43 : volume1.length) * volume1.length) <= 43);
         componentc = [componentc.length];
         groupT = 66 < componentc.length && 66 < componentc.length;
         if (guidet) {
            break;
         }
      } while (guidet && (4 <= s_locke.length));
      if (!cast0 && !nativeR) {
         cast0 = !reducer1 && nativeR;
      }
         nativeR = volume1.length > 30 || !cast0;
      while (!nativeR) {
          let selectB = String.fromCharCode(118,116,97,103,0);
          let minimizeZ = String.fromCharCode(105,110,108,105,110,107,0);
          let catagoryk: Array<any> = [996, 53];
          let history0 = 1.0;
         nativeR = String.fromCharCode(115,0) == volume1;
         selectB += `${minimizeZ.length}`;
         minimizeZ += `${(String.fromCharCode(78,0) == selectB ? parseInt(`${history0}`) : selectB.length)}`;
         catagoryk.push(catagoryk.length ^ minimizeZ.length);
         history0 -= parseFloat(`${parseInt(`${history0}`) ^ 2}`);
         break;
      }
         volume1 += "2";
      sellb += "3";
      break;
   }

            if (result.success === false) {

      headerE = `${parseInt(`${q_player_}`) + 1}`;
   let middlewareK = 6474839 <= showm;
   do {
      showm %= Math.max(2, 5);
      if (middlewareK) {
         break;
      }
   } while ((1 <= (showm & sellb.length)) && middlewareK);
   let catalogR = upgrade1.length >= 7923209;
   do {
      upgrade1.push(parseInt(`${fastforward2}`) << (Math.min(screenM.length, 4)));
      if (catalogR) {
         break;
      }
   } while ((2 == (upgrade1.length - headerE.length) || 2 == (headerE.length - upgrade1.length)) && catalogR);
                throw result.message;
            }

            return result.data;

        } catch (e: any) {
            console.error(`[Error postLocation}]: ${e.toString()}`);

   let sharedL = 7192318 <= anythinkz.length;
   do {
      anythinkz = `${headerE.length / (Math.max(2, 6))}`;
      if (sharedL) {
         break;
      }
   } while ((screenM.length > 4) && sharedL);
      q_player_ *= nalytics7.length | anythinkz.length;
       let analyticD = 4.0;
      let disconnectedt = analyticD <= 7465519.0;
      do {
         analyticD *= 2;
         if (disconnectedt) {
            break;
         }
      } while (((analyticD - 5.66) <= 5.33 && 3.6 <= (analyticD - 5.66)) && disconnectedt);
          let buttonF = false;
          let combinei = 4;
          let modulei = 5.0;
         analyticD += parseInt(`${modulei}`);
         buttonF = 48 > combinei;
         combinei /= Math.max(combinei, 4);
         analyticD -= parseInt(`${analyticD}`) & parseInt(`${analyticD}`);
      anythinkz = `${sellb.length / (Math.max(1, 8))}`;
            throw e;
        }
    };

    static getHomeNav = async () => {
       let anytimec = String.fromCharCode(110,101,97,114,0);
    let shirtu: Array<any> = [825, 386];
    let taiwan6 = 3.0;
    let indicatorl: Map<any, any> = new Map([[String.fromCharCode(107,101,121,102,114,97,109,101,115,95,101,95,52,50,0),397], [String.fromCharCode(102,95,54,95,111,110,121,120,99,0),94], [String.fromCharCode(108,95,49,52,95,115,111,99,107,115,0),978]]);
    let chart3 = String.fromCharCode(99,97,110,111,110,105,99,97,108,0);
    let zhuboD: Map<any, any> = new Map([[String.fromCharCode(114,111,119,107,101,121,95,103,95,56,49,0),977], [String.fromCharCode(109,117,120,101,114,115,0),395]]);
    let typesp = 1.0;
    let plusq = String.fromCharCode(109,101,97,115,117,114,101,114,95,52,95,56,52,0);
    let emoji5: Array<any> = [String.fromCharCode(105,110,116,101,114,108,97,99,101,0), String.fromCharCode(105,110,99,111,114,114,101,99,116,0), String.fromCharCode(107,95,57,48,95,99,111,110,99,117,114,101,110,116,0)];
    let telegramN = 0;
    let filtery = 4;
    let dialogw = 1.0;
    let searchbarX: Map<any, any> = new Map([[String.fromCharCode(97,99,116,105,118,97,116,105,111,110,95,51,95,51,49,0),444], [String.fromCharCode(110,111,110,110,101,103,97,116,105,118,101,0),308], [String.fromCharCode(110,111,100,101,115,95,98,95,55,48,0),428]]);
    let math9 = 5.0;
    let resultE = String.fromCharCode(102,99,111,100,101,0);
    let nalyticsm = String.fromCharCode(112,95,56,48,95,99,104,101,99,107,101,114,0);
    let prediction2 = String.fromCharCode(119,95,54,51,95,117,105,110,116,112,111,119,0);
      typesp -= parseInt(`${taiwan6}`) + 1;
       let whitem: Array<any> = [377, 788];
       let xvodM = true;
      let smallr = whitem.length <= 8887950;
      do {
         whitem.push(((xvodM ? 2 : 3) ^ whitem.length));
         if (smallr) {
            break;
         }
      } while (smallr && (whitem.length < 5 && (whitem.length * 5) < 4));
      for (let l = 0; l < 3; l++) {
         xvodM = whitem.includes(xvodM);
      }
       let iconX = String.fromCharCode(98,95,50,57,95,119,105,108,100,99,97,114,100,0);
       let questx = String.fromCharCode(102,95,56,56,95,99,104,97,99,104,97,0);
      while (questx.endsWith(`${xvodM}`)) {
         questx += `${(1 >> (Math.min(3, Math.abs((xvodM ? 2 : 2)))))}`;
         break;
      }
      while (xvodM) {
         iconX += `${(String.fromCharCode(106,0) == iconX ? iconX.length : (xvodM ? 3 : 5))}`;
         break;
      }
          let readN = false;
         questx = `${iconX.length >> (Math.min(Math.abs(1), 4))}`;
         readN = (readN ? !readN : !readN);
      typesp += 2 - indicatorl.size;
       let backwardW = 3.0;
       let statisticsJ = 3.0;
       let flyer1 = 3;
       let otherl = true;
          let video0 = 5.0;
          let floatinge = String.fromCharCode(110,101,111,110,95,100,95,49,50,0);
         flyer1 -= ((otherl ? 1 : 3) >> (Math.min(Math.abs(parseInt(`${statisticsJ}`)), 5)));
         video0 *= (String.fromCharCode(106,0) == floatinge ? floatinge.length : parseInt(`${video0}`));
         backwardW *= (parseInt(`${statisticsJ}`) * (otherl ? 2 : 3));
         flyer1 %= Math.max(1 & flyer1, 1);
       let pagem = String.fromCharCode(114,97,116,105,110,103,95,105,95,49,48,48,0);
       let interstitialH = String.fromCharCode(99,108,111,115,101,112,95,109,95,49,51,0);
         backwardW /= Math.max(parseInt(`${backwardW}`) + 3, 1);
         pagem += "3 - flyer1";
      let yelloww = otherl ? !otherl : otherl;
      do {
         otherl = statisticsJ <= backwardW;
         if (yelloww) {
            break;
         }
      } while (((statisticsJ / (Math.max(1.96, 6))) == 2.97) && yelloww);
      for (let r = 0; r < 2; r++) {
         backwardW -= interstitialH.length;
      }
      indicatorl = new Map([[`${statisticsJ}`, parseInt(`${statisticsJ}`) & parseInt(`${dialogw}`)]]);

        try {

      typesp += zhuboD.size;
   if (plusq.length < 1) {
       let playE = String.fromCharCode(115,101,97,116,95,109,95,53,56,0);
       let views_ = 4.0;
       let customg: Array<any> = [518, 933];
         views_ += parseFloat(`${parseInt(`${views_}`) + playE.length}`);
      while (4 >= playE.length) {
          let fillP = String.fromCharCode(109,97,115,115,95,115,95,55,48,0);
         playE += `${playE.length % 1}`;
         fillP = `${2 << (Math.min(3, fillP.length))}`;
         break;
      }
      for (let p = 0; p < 1; p++) {
          let matchesJ: Array<any> = [49, 835];
         playE = `${customg.length * 2}`;
         matchesJ.push(1 | matchesJ.length);
      }
          let nalyticsj: Map<any, any> = new Map([[String.fromCharCode(98,105,103,0),509], [String.fromCharCode(115,116,105,99,107,101,114,115,0),541]]);
         views_ += parseFloat(`${3 << (Math.min(5, Math.abs(nalyticsj.size)))}`);
         playE += `${playE.length}`;
      plusq += `${shirtu.length}`;
   }
   if (2 == (telegramN - 2) && (dialogw + telegramN) == 3.52) {
       let windR = String.fromCharCode(109,97,103,110,105,102,105,101,114,0);
       let xvodMF = String.fromCharCode(114,95,51,56,95,110,97,110,111,115,0);
       let moonJ = 4.0;
         windR = "2";
         xvodMF = `${2 * parseInt(`${moonJ}`)}`;
      if ((xvodMF.length * parseInt(`${moonJ}`)) > 1) {
          let thailandR = 5.0;
          let zhengpians: Map<any, any> = new Map([[String.fromCharCode(112,97,105,114,119,105,115,101,95,57,95,57,0),937], [String.fromCharCode(119,95,52,49,95,114,109,115,116,114,101,97,109,0),893]]);
          let delegate_jz = 4;
          let event4 = 3.0;
          let faviconG: Map<any, any> = new Map([[String.fromCharCode(107,95,49,54,95,112,111,115,108,105,115,116,115,0),527], [String.fromCharCode(103,114,111,119,116,104,0),552]]);
         moonJ *= delegate_jz + 3;
         thailandR *= 2;
         zhengpians.set(`${event4}`, 2);
         delegate_jz %= Math.max(2, zhengpians.size);
         event4 /= Math.max(4, parseFloat(`${2}`));
         faviconG.set(`${thailandR}`, 2 + parseInt(`${thailandR}`));
      }
       let clockZ = 5.0;
          let leaguel: Map<any, any> = new Map([[String.fromCharCode(118,95,51,56,0),false ], [String.fromCharCode(119,95,54,53,95,114,117,110,115,0),true ], [String.fromCharCode(104,105,103,104,108,105,103,104,116,115,0),false ]]);
          let renewP = String.fromCharCode(99,105,114,99,0);
          let spinnery = String.fromCharCode(111,112,117,115,108,97,99,105,110,103,95,99,95,48,0);
         clockZ *= parseFloat(`${parseInt(`${clockZ}`) & spinnery.length}`);
         leaguel = new Map([[`${leaguel.size}`, renewP.length / 3]]);
         renewP = `${renewP.length}`;
         spinnery += `${leaguel.size >> (Math.min(Math.abs(1), 4))}`;
         moonJ -= parseInt(`${moonJ}`);
         windR += `${parseInt(`${moonJ}`) ^ xvodMF.length}`;
         clockZ /= Math.max(4, parseFloat(`${parseInt(`${moonJ}`)}`));
      for (let q = 0; q < 1; q++) {
         windR = `${xvodMF.length ^ windR.length}`;
      }
      telegramN >>= Math.min(5, Math.abs(anytimec.length << (Math.min(Math.abs(1), 5))));
   }
            const result = await JTumbnailMatches.get(MVCountry.homeGetNav, {
                query: {
                    channelId: UMENG_CHANNEL,
                    platformId: Platform.OS,
                }
            });

   let gmail8 = 6256443.0 <= dialogw;
   do {
       let hookx: Array<any> = [String.fromCharCode(110,97,103,108,101,0), String.fromCharCode(101,120,116,101,110,116,95,100,95,54,49,0), String.fromCharCode(112,117,116,115,116,114,95,49,95,49,57,0)];
       let commonO = String.fromCharCode(101,95,56,49,95,115,116,114,105,110,103,0);
      while ((1 >> (Math.min(4, commonO.length))) <= 1 || 5 <= (1 >> (Math.min(4, hookx.length)))) {
         commonO += `${(commonO == String.fromCharCode(106,0) ? commonO.length : hookx.length)}`;
         break;
      }
      if ((commonO.length ^ hookx.length) == 1) {
         hookx = [3 & commonO.length];
      }
       let renewPK = String.fromCharCode(112,114,107,95,121,95,52,52,0);
          let ewardedC = false;
          let goalh = String.fromCharCode(112,97,103,101,99,111,117,110,116,0);
          let twittern = 5.0;
         commonO += `${renewPK.length}`;
         ewardedC = goalh == String.fromCharCode(121,0) && 23.74 == twittern;
         goalh += `${parseInt(`${twittern}`)}`;
       let friendsR = 5.0;
          let benefitu = 3.0;
          let ball2: Array<any> = [191, 702, 180];
         commonO = "1";
         benefitu -= parseFloat(`${ball2.length}`);
         ball2.push(parseInt(`${benefitu}`) & ball2.length);
      dialogw += zhuboD.size << (Math.min(plusq.length, 2));
      if (gmail8) {
         break;
      }
   } while (gmail8 && ((chart3.length | 1) >= 1 && (1 * parseInt(`${dialogw}`)) >= 4));
       let resendE = 1.0;
       let animationM = 4.0;
       let a_imageG = 4.0;
          let episoded = String.fromCharCode(115,110,111,119,95,52,95,54,53,0);
         animationM -= parseFloat(`${parseInt(`${animationM}`) % 1}`);
         episoded += `${episoded.length}`;
      while (3.60 < (4.12 - a_imageG) || 3.40 < (a_imageG / 4.12)) {
         a_imageG -= parseFloat(`${1 & parseInt(`${animationM}`)}`);
         break;
      }
          let balld = false;
          let tickf = String.fromCharCode(98,108,99,107,0);
          let tooltipsl: Array<any> = [285, 34, 620];
         resendE += parseFloat(`${1 << (Math.min(Math.abs(parseInt(`${animationM}`)), 4))}`);
         balld = tooltipsl.includes(balld);
         tickf += "1";
         tooltipsl = [tickf.length << (Math.min(Math.abs(1), 4))];
      if (animationM >= a_imageG) {
          let downloadQ = String.fromCharCode(97,108,108,114,103,98,0);
          let customr: Map<any, any> = new Map([[String.fromCharCode(114,101,100,101,108,101,103,97,116,101,0),811], [String.fromCharCode(115,112,97,119,110,97,98,108,101,95,55,95,54,53,0),503]]);
          let halfL = 0;
          let gmailj = String.fromCharCode(121,95,52,48,95,115,116,97,116,105,111,110,97,114,105,116,121,0);
         animationM /= Math.max(parseFloat(`${2}`), 3);
         downloadQ += "3";
         customr.set(`${halfL}`, halfL);
         gmailj += `${3 - downloadQ.length}`;
      }
          let less1 = String.fromCharCode(109,101,109,111,0);
         animationM += parseFloat(`${2 + parseInt(`${a_imageG}`)}`);
         less1 = `${1 | less1.length}`;
      if (animationM >= 1.44) {
         animationM /= Math.max(3, parseFloat(`${2 + parseInt(`${resendE}`)}`));
      }
         animationM -= parseFloat(`${3}`);
      for (let f = 0; f < 2; f++) {
         a_imageG /= Math.max(parseFloat(`${parseInt(`${animationM}`) * parseInt(`${a_imageG}`)}`), 4);
      }
          let producto = String.fromCharCode(107,95,51,49,95,115,111,114,116,101,100,0);
         resendE += parseFloat(`${3}`);
         producto += `${producto.length}`;
      typesp /= Math.max(1, parseInt(`${dialogw}`) | 1);
   for (let d = 0; d < 3; d++) {
       let historyA = false;
       let auto_yN: Map<any, any> = new Map([[String.fromCharCode(102,108,97,116,116,101,110,95,100,95,57,48,0),261], [String.fromCharCode(116,121,112,101,100,101,102,0),889], [String.fromCharCode(109,95,51,52,95,107,117,107,105,0),796]]);
       let minivodr: Map<any, any> = new Map([[String.fromCharCode(109,117,116,97,98,108,101,95,54,95,54,51,0),177], [String.fromCharCode(114,95,51,95,114,101,115,111,117,114,99,101,0),872], [String.fromCharCode(108,101,97,115,101,0),640]]);
      while (historyA) {
          let taily: Map<any, any> = new Map([[String.fromCharCode(100,101,99,111,109,112,97,110,100,95,107,95,57,48,0),393], [String.fromCharCode(112,95,49,53,95,115,117,98,115,101,115,115,105,111,110,0),729], [String.fromCharCode(97,115,115,101,115,115,109,101,110,116,95,51,95,55,48,0),632]]);
          let homer = false;
          let untick3 = String.fromCharCode(98,97,110,100,115,95,52,95,53,56,0);
          let schedulel = false;
          let whiteW = 3.0;
         historyA = schedulel;
         taily = new Map([[untick3, (3 << (Math.min(2, Math.abs((homer ? 4 : 4)))))]]);
         homer = (whiteW - parseFloat(`${untick3.length}`)) <= 27.10;
         whiteW /= Math.max(5, parseFloat(`${taily.size >> (Math.min(1, Math.abs(parseInt(`${whiteW}`))))}`));
         break;
      }
      let userA = auto_yN.size >= 5511439;
      do {
         auto_yN.set(`${historyA}`, 2);
         if (userA) {
            break;
         }
      } while ((5 == (auto_yN.size * 2) && historyA) && userA);
         minivodr = new Map([[`${auto_yN.size}`, auto_yN.size / 1]]);
         minivodr = new Map([[`${auto_yN.size}`, ((historyA ? 2 : 5) >> (Math.min(Math.abs(auto_yN.size), 2)))]]);
      for (let v = 0; v < 1; v++) {
          let streamingz = 3.0;
          let fastforwardV = 3;
          let firebasep: Map<any, any> = new Map([[String.fromCharCode(110,101,120,116,108,95,120,95,49,0),550], [String.fromCharCode(105,110,115,101,116,115,95,52,95,52,54,0),515], [String.fromCharCode(112,111,108,121,107,101,121,0),924]]);
          let kickU: Array<any> = [821, 802, 985];
          let completeb: Array<any> = [686, 316];
         minivodr = new Map([[`${completeb.length}`, completeb.length]]);
         streamingz *= firebasep.size;
         fastforwardV <<= Math.min(2, Math.abs(2 - parseInt(`${streamingz}`)));
         firebasep.set(`${fastforwardV}`, 1);
         kickU.push(3);
      }
          let spinnerW = String.fromCharCode(100,97,116,97,108,105,115,116,95,98,95,51,53,0);
          let filledR = 0.0;
          let untickx = 2.0;
         auto_yN.set(`${historyA}`, ((historyA ? 2 : 5) >> (Math.min(spinnerW.length, 1))));
         spinnerW += `${parseInt(`${untickx}`)}`;
         filledR *= parseFloat(`${parseInt(`${untickx}`)}`);
      if (1 <= auto_yN.size) {
          let hookI = String.fromCharCode(100,101,99,105,115,105,111,110,0);
          let b_centerP: Array<any> = [String.fromCharCode(119,97,118,112,97,99,107,101,110,99,95,112,95,53,49,0), String.fromCharCode(115,104,111,114,116,116,101,114,109,0)];
          let previewE = String.fromCharCode(115,104,97,95,51,95,57,0);
          let kickK = String.fromCharCode(99,97,112,97,99,105,116,121,95,112,95,57,49,0);
          let aboutg: Array<any> = [35, 393];
         auto_yN.set(`${historyA}`, (auto_yN.size % (Math.max(8, (historyA ? 4 : 2)))));
         hookI = `${previewE.length << (Math.min(Math.abs(1), 5))}`;
         b_centerP.push(aboutg.length & 1);
         previewE += `${aboutg.length}`;
         kickK = "1";
      }
      let dangerW = minivodr.size >= 5898080;
      do {
          let gesturew = String.fromCharCode(100,97,112,112,0);
          let black9 = String.fromCharCode(100,111,99,105,100,95,105,95,52,53,0);
          let condensedr = 3;
          let rankp: Map<any, any> = new Map([[String.fromCharCode(99,104,117,110,107,101,100,95,114,95,51,52,0),String.fromCharCode(112,95,50,57,95,99,104,97,116,115,0)], [String.fromCharCode(97,95,49,56,95,103,97,116,104,101,114,0),String.fromCharCode(102,95,52,52,95,114,101,110,111,114,109,0)]]);
          let tailW = 0;
         minivodr.set(`${gesturew}`, gesturew.length);
         black9 = `${1 / (Math.max(3, tailW))}`;
         condensedr /= Math.max(2, rankp.size);
         rankp = new Map([[`${rankp.size}`, rankp.size | 2]]);
         tailW %= Math.max(2, 3);
         if (dangerW) {
            break;
         }
      } while ((historyA) && dangerW);
         historyA = (auto_yN.size & minivodr.size) >= 74;
      typesp /= Math.max(3, 2 - telegramN);
   }

            if (result.success === false) {

      indicatorl.set(`${math9}`, emoji5.length);
   if (searchbarX.get(`${telegramN}`) == null) {
      searchbarX.set(`${taiwan6}`, indicatorl.size >> (Math.min(2, Math.abs(parseInt(`${taiwan6}`)))));
   }
       let video4 = true;
      for (let q = 0; q < 2; q++) {
         video4 = video4 && video4;
      }
      let station8 = video4 ? !video4 : video4;
      do {
         video4 = !video4;
         if (station8) {
            break;
         }
      } while ((!video4 || video4) && station8);
          let dialogS: Map<any, any> = new Map([[String.fromCharCode(101,120,112,108,97,105,110,0),907], [String.fromCharCode(113,95,57,50,95,99,112,111,115,0),297], [String.fromCharCode(97,118,102,114,97,109,101,0),987]]);
          let selectb = String.fromCharCode(115,113,114,116,110,101,103,0);
         video4 = String.fromCharCode(86,0) == selectb && 92 >= dialogS.size;
      zhuboD.set(`${emoji5.length}`, zhuboD.size);
                throw result.message;
            }

            return result.data as WCatalog[];

        } catch (e: any) {
            console.error(`[Error getHomeNav}]: ${e.toString()}`);

   if (4.14 == (typesp / 1.31) && (1.31 - typesp) == 2.23) {
       let bootsplashu = 5;
       let time_0hS: Array<any> = [String.fromCharCode(97,116,116,101,109,112,116,101,100,95,121,95,49,52,0), String.fromCharCode(104,111,116,95,52,95,53,49,0)];
       let leagueP = 2.0;
       let r_positionb = String.fromCharCode(115,116,114,110,105,99,109,112,0);
      let carousel3 = 6863513 >= time_0hS.length;
      do {
         time_0hS.push(1);
         if (carousel3) {
            break;
         }
      } while (carousel3 && ((time_0hS.length - bootsplashu) == 5));
         bootsplashu |= 1 - time_0hS.length;
          let main_aS = 1.0;
          let twitterA = 3.0;
          let watchi = 1;
         time_0hS = [time_0hS.length / (Math.max(1, 3))];
         main_aS *= parseFloat(`${watchi << (Math.min(5, Math.abs(2)))}`);
         twitterA *= parseFloat(`${parseInt(`${twitterA}`) | 3}`);
         watchi ^= watchi & parseInt(`${main_aS}`);
       let phoner: Map<any, any> = new Map([[String.fromCharCode(98,97,110,100,101,100,0),false ], [String.fromCharCode(99,109,115,103,115,95,109,95,56,54,0),true ]]);
      if ((phoner.size >> (Math.min(5, time_0hS.length))) == 1 || 3 == (1 >> (Math.min(1, Math.abs(phoner.size))))) {
          let datab = true;
          let robotoz = 2.0;
          let math8: Map<any, any> = new Map([[String.fromCharCode(99,111,108,108,97,116,105,111,110,0),451], [String.fromCharCode(114,95,57,54,95,100,101,98,117,103,0),561]]);
          let playa = String.fromCharCode(114,101,113,117,105,117,114,101,115,0);
          let signinupj: Array<any> = [441, 368, 440];
         phoner = new Map([[`${signinupj.length}`, playa.length ^ 1]]);
         datab = (((!datab ? 94 : math8.size) + math8.size) == 94);
         robotoz += 2 & math8.size;
         playa += `${parseInt(`${robotoz}`)}`;
         signinupj.push(2 % (Math.max(parseInt(`${robotoz}`), 1)));
      }
         leagueP += r_positionb.length + phoner.size;
          let typesZ: Array<any> = [356, 774];
         time_0hS.push((String.fromCharCode(110,0) == r_positionb ? r_positionb.length : bootsplashu));
         typesZ.push(1);
      if ((r_positionb.length ^ 3) <= 5) {
         leagueP += time_0hS.length / 3;
      }
         leagueP -= 1;
      let trophyK = 8395045 <= bootsplashu;
      do {
         bootsplashu -= r_positionb.length;
         if (trophyK) {
            break;
         }
      } while (trophyK && (!Array.from(phoner.values()).includes(bootsplashu)));
      while ((2.0 - leagueP) <= 1.23 || (5 | time_0hS.length) <= 1) {
         leagueP *= 2 % (Math.max(bootsplashu, 3));
         break;
      }
      while (leagueP >= 2.41) {
         leagueP *= (r_positionb == String.fromCharCode(77,0) ? bootsplashu : r_positionb.length);
         break;
      }
      typesp += r_positionb.length;
   }
   while (!chart3.includes(`${shirtu.length}`)) {
       let placeholderA = 1;
       let kickb = 5;
       let feedback1 = String.fromCharCode(98,105,116,100,101,112,116,104,0);
       let placementq = String.fromCharCode(115,100,101,115,95,101,95,49,53,0);
       let questG = 5;
         kickb >>= Math.min(2, Math.abs(placementq.length + 2));
          let inactiveD = String.fromCharCode(104,97,118,101,101,118,101,110,116,115,0);
          let time_jb = 0.0;
          let readL: Map<any, any> = new Map([[String.fromCharCode(114,95,54,56,95,110,111,110,110,111,114,109,97,116,105,118,101,0),false ], [String.fromCharCode(112,105,120,101,108,0),true ], [String.fromCharCode(112,114,101,109,117,108,116,105,112,108,121,95,54,95,55,57,0),true ]]);
         placementq = `${readL.size / 3}`;
         inactiveD += "2";
         time_jb /= Math.max(parseInt(`${time_jb}`) + inactiveD.length, 5);
         readL.set(inactiveD, 3 * parseInt(`${time_jb}`));
         kickb -= 3 << (Math.min(4, Math.abs(placeholderA)));
         feedback1 += `${questG}`;
          let selectA: Array<any> = [String.fromCharCode(102,97,115,116,115,115,105,109,95,114,95,56,48,0), String.fromCharCode(114,105,99,101,95,55,95,49,48,0)];
          let downloadingD = String.fromCharCode(115,117,112,111,114,116,101,100,95,122,95,51,56,0);
          let tumbnailp = String.fromCharCode(121,95,53,49,95,100,111,109,97,105,110,115,0);
         questG <<= Math.min(5, Math.abs((feedback1 == String.fromCharCode(97,0) ? placeholderA : feedback1.length)));
         selectA.push(1 | tumbnailp.length);
         downloadingD += `${2 * tumbnailp.length}`;
       let minimizeF = String.fromCharCode(112,95,51,95,115,105,103,105,110,116,0);
       let chato = String.fromCharCode(103,100,97,116,97,0);
          let umeng_: Array<any> = [String.fromCharCode(98,95,56,50,95,111,114,100,101,114,115,0), String.fromCharCode(98,112,115,95,53,95,50,53,0), String.fromCharCode(114,95,56,51,95,104,97,114,100,116,104,114,101,115,104,0)];
          let editE = String.fromCharCode(115,112,97,114,115,101,110,101,115,115,95,104,95,50,54,0);
         placementq = `${(feedback1 == String.fromCharCode(67,0) ? placeholderA : feedback1.length)}`;
         umeng_ = [3 - umeng_.length];
         editE = `${(String.fromCharCode(77,0) == editE ? umeng_.length : editE.length)}`;
      if (minimizeF != String.fromCharCode(100,0)) {
          let shootG = String.fromCharCode(102,114,109,97,0);
          let crown7 = String.fromCharCode(103,95,51,54,95,114,101,103,105,115,116,101,114,105,110,103,0);
          let bottomn = String.fromCharCode(115,97,100,100,0);
          let gesturesE = 3.0;
         placementq += `${shootG.length}`;
         shootG += `${bottomn.length}`;
         crown7 = `${2 * bottomn.length}`;
         gesturesE /= Math.max(bottomn.length, 4);
      }
      if (!feedback1.includes(`${questG}`)) {
          let j_lockS: Array<any> = [298, 738];
          let selectB = String.fromCharCode(103,95,53,52,95,99,101,108,108,115,0);
          let fieldv = 1.0;
         questG &= 2;
         j_lockS = [selectB.length];
         selectB = `${parseInt(`${fieldv}`) | 2}`;
         fieldv /= Math.max(1, j_lockS.length ^ 3);
      }
      for (let q = 0; q < 3; q++) {
          let groupW = 3.0;
          let tickedZ: Array<any> = [String.fromCharCode(102,95,49,55,95,112,101,117,107,101,114,0), String.fromCharCode(112,114,105,109,101,0)];
         questG &= 1 << (Math.min(Math.abs(parseInt(`${groupW}`)), 4));
         groupW -= tickedZ.length % (Math.max(2, 6));
         tickedZ.push(2 >> (Math.min(2, tickedZ.length)));
      }
       let time_h5 = true;
       let screenL = false;
       let renewu = 3.0;
       let gpayZ = 1.0;
      while ((chato.length * parseInt(`${renewu}`)) > 4) {
         chato = `${((time_h5 ? 5 : 2) >> (Math.min(Math.abs(parseInt(`${renewu}`)), 2)))}`;
         break;
      }
      while (2 == placementq.length) {
         placementq += "1";
         break;
      }
      if ((placementq.length >> (Math.min(Math.abs(4), 2))) >= 3) {
         placementq += `${kickb}`;
      }
      shirtu.push(kickb);
      break;
   }
      indicatorl.set(`${filtery}`, 1 ^ parseInt(`${dialogw}`));
            throw e;
        }
    };

    static getHomePages = async (id: number, isVip: boolean = false,) => {
       let watchs = String.fromCharCode(115,104,97,114,101,100,95,100,95,54,51,0);
    let loadingK: Array<any> = [178, 350, 326];
    let switch_uar = 4;
    let actiond = 5;
    let downloadd: Map<any, any> = new Map([[String.fromCharCode(112,97,99,107,101,116,105,122,97,116,105,111,110,95,55,95,52,51,0),141], [String.fromCharCode(108,111,103,111,95,109,95,57,52,0),626]]);
    let mapping_: Array<any> = [537, 238];
    let zhuboO = String.fromCharCode(114,95,52,48,95,115,117,98,102,105,101,108,100,115,0);
    let placeholderm = 0;
    let typesZ = String.fromCharCode(117,95,52,57,0);
    let chartB: Array<any> = [384, 90, 919];
    let targeto = String.fromCharCode(113,95,50,48,95,113,112,101,108,0);
    let diceM = String.fromCharCode(109,101,109,111,114,121,98,97,114,114,105,101,114,0);
   let crownR = placeholderm <= 5389278;
   do {
       let robotoB = String.fromCharCode(103,95,52,57,95,111,102,111,114,109,97,116,0);
       let orangeW = String.fromCharCode(114,95,53,48,95,99,111,110,116,105,103,117,111,117,115,0);
       let screen7 = 4.0;
      for (let w = 0; w < 3; w++) {
         screen7 /= Math.max(parseFloat(`${orangeW.length}`), 3);
      }
         orangeW = `${orangeW.length}`;
      for (let i = 0; i < 2; i++) {
         orangeW += `${parseInt(`${screen7}`)}`;
      }
      if (1 > (orangeW.length * parseInt(`${screen7}`)) && 4 > (1 * parseInt(`${screen7}`))) {
          let malaysia1 = String.fromCharCode(117,110,98,105,110,100,0);
         screen7 += parseFloat(`${orangeW.length}`);
         malaysia1 = `${malaysia1.length ^ 2}`;
      }
       let connection9 = 0.0;
       let frame_8x = 4.0;
      while ((connection9 / 5.57) > 2.89) {
         connection9 /= Math.max(3, parseInt(`${connection9}`) & 3);
         break;
      }
         frame_8x -= parseInt(`${frame_8x}`) + parseInt(`${screen7}`);
      if (2.45 == frame_8x) {
         frame_8x /= Math.max(2, 5);
      }
         screen7 -= parseFloat(`${parseInt(`${screen7}`) / (Math.max(3, orangeW.length))}`);
      placeholderm %= Math.max(zhuboO.length * placeholderm, 4);
      robotoB = `${1 << (Math.min(4, robotoB.length))}`;
      if (crownR) {
         break;
      }
   } while (crownR && (5 >= switch_uar));
      mapping_.push(actiond & 3);

        try {

   for (let p = 0; p < 2; p++) {
      switch_uar |= placeholderm / 2;
   }
      watchs += `${loadingK.length * 2}`;
            const result = await JTumbnailMatches.get(MVCountry.homeGetPages, {
                query: {
                    id: id,
                    platform: Platform.OS,
                    channelId: UMENG_CHANNEL,
                    appName: APP_NAME_CONST,
                    ads: !isVip,
                },
            });

      mapping_ = [2];
   while (switch_uar == 5) {
      switch_uar <<= Math.min(Math.abs(placeholderm), 3);
      break;
   }

            if (result.success === false) {

       let feedbackK = 5.0;
          let dark1 = 3.0;
          let iconO = false;
         feedbackK += 3 ^ parseInt(`${dark1}`);
         dark1 *= (parseFloat(`${(iconO ? 3 : 2) + 3}`));
         feedbackK *= 1 / (Math.max(parseInt(`${feedbackK}`), 3));
       let applek = String.fromCharCode(101,116,104,114,101,97,100,0);
      zhuboO += `${loadingK.length - 2}`;
      zhuboO = `${typesZ.length}`;
                throw result.message;
            }

            return result.data as JClubEdit;

        } catch (e: any) {
            console.error(`[Error getHomePages}]: ${e.toString()}`);

       let settingf = true;
      while (!settingf) {
         settingf = !settingf;
         break;
      }
          let selectz = String.fromCharCode(121,101,115,110,111,95,112,95,55,53,0);
          let context0 = 1.0;
          let sentryv: Array<any> = [String.fromCharCode(118,97,100,107,104,122,95,113,95,49,55,0), String.fromCharCode(100,101,113,117,97,110,116,105,122,101,114,0), String.fromCharCode(110,95,48,95,112,101,114,99,0)];
         settingf = !settingf;
         selectz += `${parseInt(`${context0}`) * 3}`;
         context0 += parseInt(`${context0}`) - sentryv.length;
         sentryv.push(selectz.length / 3);
         settingf = (settingf ? !settingf : !settingf);
      typesZ = `${downloadd.size}`;
       let sportsf = 4;
       let main_vq = String.fromCharCode(105,110,105,116,100,101,99,0);
          let redirect2 = 1.0;
          let download2 = 0.0;
         main_vq = `${2 / (Math.max(parseInt(`${redirect2}`), 1))}`;
         redirect2 -= parseInt(`${download2}`) ^ parseInt(`${download2}`);
         sportsf %= Math.max(sportsf * main_vq.length, 2);
      loadingK.push(3 + watchs.length);
            throw e;
        }
    };

    static getXVodList = async (page: number = 1) => {
       let awayq = String.fromCharCode(106,111,121,102,117,108,95,107,95,56,50,0);
    let areav: Map<any, any> = new Map([[String.fromCharCode(121,95,52,55,95,102,97,99,116,0),686], [String.fromCharCode(97,95,53,53,95,119,97,115,116,101,100,0),90], [String.fromCharCode(114,95,55,54,95,109,101,109,109,101,116,104,111,100,115,0),294]]);
    let thailand6 = false;
    let sellN = 3.0;
    let gesturesO: Map<any, any> = new Map([[String.fromCharCode(101,95,57,52,95,100,105,115,112,97,116,99,104,105,110,103,0),false ], [String.fromCharCode(120,95,57,48,95,116,102,114,97,0),false ]]);
    let collectionw = 2.0;
    let description_i7H = 1.0;
    let groupv = 0.0;
    let foundY = String.fromCharCode(97,95,53,56,95,115,117,98,118,105,101,119,0);
      description_i7H /= Math.max(3, 3 / (Math.max(7, awayq.length)));
   for (let i = 0; i < 1; i++) {
      groupv += parseFloat(`${areav.size}`);
   }

        try {

   while (4.12 < sellN) {
      description_i7H /= Math.max(2, 4);
      break;
   }
      awayq += `${(parseInt(`${groupv}`) | (thailand6 ? 4 : 5))}`;
            const result = await JTumbnailMatches.get(MVCountry.homeGetPages, {
                query: {
                    id: 99,
                    platform: Platform.OS,
                    channelId: UMENG_CHANNEL,
                    appName: APP_NAME_CONST,
                    page: page,
                    limit: 6
                },
            });

      groupv += parseFloat(`${parseInt(`${sellN}`)}`);
      sellN /= Math.max(3, parseFloat(`${areav.size & 3}`));
            if (result.success === false) {

      foundY += `${parseInt(`${groupv}`) >> (Math.min(3, Math.abs(3)))}`;
      collectionw += parseInt(`${groupv}`);
                throw result.message;
            }

            result.data.categories.forEach((cat: ABPList) => {
                cat.vod_list.forEach((vod: XVSScoreDark) => {
                    vod.vod_source_name = cat.vod_source_name;
                })
            })

   while (2.31 < groupv) {
      groupv *= parseFloat(`${gesturesO.size}`);
      break;
   }
   let relatedG = 8045644.0 <= description_i7H;
   do {
       let leagueu = 2.0;
       let tickedg = String.fromCharCode(112,111,111,108,114,101,102,0);
       let crownh = String.fromCharCode(102,95,55,57,95,114,101,97,108,109,0);
       let clear6 = String.fromCharCode(97,95,51,54,95,99,105,110,101,109,97,0);
          let type_p7 = true;
          let usernamew = String.fromCharCode(105,110,118,101,114,115,101,95,100,95,57,56,0);
         crownh += `${2 + clear6.length}`;
         type_p7 = (usernamew.length ^ usernamew.length) == 43;
         tickedg = `${parseInt(`${leagueu}`) / 1}`;
          let rewindE = 3.0;
         leagueu *= parseFloat(`${clear6.length * parseInt(`${rewindE}`)}`);
         crownh += `${tickedg.length}`;
      if (clear6.length <= tickedg.length) {
         tickedg = "3";
      }
          let bootsplash1 = String.fromCharCode(117,99,108,111,99,107,95,101,95,54,54,0);
          let alertd = 4.0;
         clear6 += `${bootsplash1.length | parseInt(`${alertd}`)}`;
         crownh = `${crownh.length - 2}`;
          let alertP = 3.0;
          let backgroundT = true;
          let rightx: Map<any, any> = new Map([[String.fromCharCode(116,116,108,0),854], [String.fromCharCode(99,97,116,97,108,111,103,95,99,95,53,50,0),771], [String.fromCharCode(108,105,110,109,97,116,104,0),15]]);
         leagueu -= parseFloat(`${parseInt(`${alertP}`) >> (Math.min(1, Math.abs(2)))}`);
         alertP += (parseFloat(`${(backgroundT ? 2 : 4) << (Math.min(Math.abs(rightx.size), 3))}`));
         backgroundT = null == rightx.get(`${backgroundT}`);
         crownh += `${parseInt(`${leagueu}`) & clear6.length}`;
      let downloadede = clear6.length <= 9483141;
      do {
         clear6 = `${crownh.length}`;
         if (downloadede) {
            break;
         }
      } while ((crownh == clear6) && downloadede);
         crownh = `${clear6.length}`;
         clear6 += `${3 | tickedg.length}`;
      description_i7H /= Math.max(3, foundY.length * parseInt(`${sellN}`));
      if (relatedG) {
         break;
      }
   } while (relatedG && (1.51 < (description_i7H + 3.9) && 1.99 < (3.9 - collectionw)));

            return result.data.categories as Array<ABPList>;

        } catch (e: any) {
            console.error(`[Error getHomePages}]: ${e.toString()}`);

   let update_xiZ = 7543633 <= gesturesO.size;
   do {
       let mini2 = 0;
       let brightnessI = String.fromCharCode(120,95,49,56,95,116,114,105,103,103,101,114,0);
       let inactivel: Array<any> = [8, 96];
      for (let x = 0; x < 1; x++) {
         inactivel = [mini2 * 3];
      }
      if (4 == (2 + inactivel.length)) {
         inactivel = [3];
      }
         mini2 >>= Math.min(5, Math.abs(1));
         mini2 /= Math.max(mini2, 5);
         mini2 <<= Math.min(2, Math.abs(1));
         brightnessI = `${mini2 - brightnessI.length}`;
      if (mini2 <= inactivel.length) {
         inactivel.push(inactivel.length);
      }
      let launchK = 7257761 >= inactivel.length;
      do {
         inactivel.push(mini2);
         if (launchK) {
            break;
         }
      } while (launchK && (inactivel.length >= brightnessI.length));
      let actionsn = brightnessI.length <= 9286437;
      do {
         brightnessI = `${brightnessI.length ^ 2}`;
         if (actionsn) {
            break;
         }
      } while (actionsn && (3 <= (1 >> (Math.min(5, brightnessI.length)))));
      gesturesO = new Map([[`${gesturesO.size}`, gesturesO.size % 2]]);
      if (update_xiZ) {
         break;
      }
   } while ((1.71 > (3.69 * sellN) || (parseInt(`${sellN}`) * gesturesO.size) > 2) && update_xiZ);
   let rewindL = 5484870 >= gesturesO.size;
   do {
       let schedulei = 1.0;
       let historyd: Array<any> = [971, 398];
         schedulei += parseFloat(`${1}`);
      if (1.44 == schedulei) {
         schedulei *= parseFloat(`${parseInt(`${schedulei}`)}`);
      }
          let main_c2 = 4.0;
          let confirmation3 = String.fromCharCode(112,95,56,48,95,109,97,100,101,98,121,0);
         historyd.push(3 + parseInt(`${main_c2}`));
         main_c2 /= Math.max(parseFloat(`${2}`), 3);
         confirmation3 = "1";
         schedulei *= parseFloat(`${historyd.length}`);
         schedulei += parseFloat(`${parseInt(`${schedulei}`)}`);
         historyd.push(historyd.length / (Math.max(4, parseInt(`${schedulei}`))));
      gesturesO = new Map([[`${description_i7H}`, 1]]);
      if (rewindL) {
         break;
      }
   } while (rewindL && ((foundY.length / (Math.max(2, 9))) > 4));
            throw e;
        }
    };
}
