import { MVCountry, IHUpdateBanner } from "@constants";
import { LDGray } from "@type/wpk_long";
import { JTumbnailMatches } from "@utility/qot_stations_station";
import { APP_NAME_CONST, UMENG_CHANNEL } from "@utility/n_subs_interstitial";
import { WAnalyticsArrow } from "@utility/a_singapore";
import { Platform } from "react-native";
import { KLongNext } from "../../z_search";

export class RLarge {
    static getTopic = async (page: number = 1) => {
       let halfo = 3;
    let fill7: Array<any> = [701, 254, 895];
    let heji3 = 5;
    let x_positionq = String.fromCharCode(110,95,49,50,95,116,101,109,112,111,0);
    let linkh = 3.0;
    let settingsJ = String.fromCharCode(105,95,51,49,95,118,101,114,105,102,105,97,116,105,111,110,0);
    let floatern = 2.0;
    let sport5 = String.fromCharCode(97,108,116,101,114,95,100,95,56,55,0);
    let downloadedc = true;
      halfo |= (x_positionq == String.fromCharCode(55,0) ? parseInt(`${linkh}`) : x_positionq.length);
   if (3 > (1 >> (Math.min(2, settingsJ.length)))) {
      settingsJ += `${halfo << (Math.min(Math.abs(1), 3))}`;
   }

        try {

      fill7.push(2 ^ x_positionq.length);
   if (heji3 > fill7.length) {
      heji3 /= Math.max(settingsJ.length, 1);
   }
            const result = await JTumbnailMatches.get(MVCountry.playlistGetTopic, {
                query: {
                    page: page,
                    appName: APP_NAME_CONST,
                    platform: Platform.OS.toUpperCase(),
                    channelId: UMENG_CHANNEL,
                    ip: KLongNext.instance.ip,
                }
            });

   if ((parseFloat(`${settingsJ.length}`) + linkh) <= 4.26) {
      linkh /= Math.max(5, parseFloat(`${x_positionq.length % 3}`));
   }
      floatern += settingsJ.length + heji3;

            if (result.success === false) {

   while ((parseInt(`${linkh}`) - settingsJ.length) > 1 && 2 > (settingsJ.length & 1)) {
      settingsJ = `${x_positionq.length}`;
      break;
   }
   if (fill7.length >= 4) {
      linkh += parseFloat(`${heji3 << (Math.min(x_positionq.length, 1))}`);
   }
                throw result.message;
            }

            return result.data as LDGray;

        } catch (e: any) {
            console.error(`[Error getTopic}]: ${e.toString()}`);

       let auto_2Q = String.fromCharCode(101,115,100,115,95,100,95,53,56,0);
       let headerf: Array<any> = [227, 929, 944];
       let hoverX = false;
      for (let k = 0; k < 3; k++) {
          let chatO = 5.0;
          let whistleL = 3;
          let mailz = 5;
         headerf.push(headerf.length & auto_2Q.length);
         chatO /= Math.max(parseFloat(`${mailz % 3}`), 2);
         whistleL += mailz;
      }
         headerf = [2];
          let chatA = true;
         auto_2Q = `${((chatA ? 4 : 4) % (Math.max(6, headerf.length)))}`;
          let networkN = String.fromCharCode(101,95,55,52,0);
          let upgradeH: Map<any, any> = new Map([[String.fromCharCode(116,105,109,101,115,116,97,109,112,115,95,56,95,53,48,0),String.fromCharCode(122,95,49,48,48,95,118,97,108,105,100,105,116,121,0)], [String.fromCharCode(109,115,114,108,101,95,106,95,53,55,0),String.fromCharCode(97,118,102,109,116,95,50,95,53,56,0)]]);
         auto_2Q = "2";
         networkN = `${networkN.length | upgradeH.size}`;
         upgradeH.set(`${networkN}`, 1 * networkN.length);
          let v_lockb = 0.0;
         auto_2Q = `${auto_2Q.length / 1}`;
         v_lockb -= parseInt(`${v_lockb}`);
       let shared0 = 5;
         hoverX = auto_2Q.length <= headerf.length;
      for (let n = 0; n < 3; n++) {
          let logou = String.fromCharCode(107,95,57,53,95,114,101,109,111,118,101,0);
          let mode6: Map<any, any> = new Map([[String.fromCharCode(100,101,99,111,109,112,114,101,115,115,101,100,95,105,95,52,57,0),226], [String.fromCharCode(115,95,49,48,95,106,115,116,114,105,110,103,0),159]]);
          let telegramO = String.fromCharCode(108,95,53,49,95,109,97,107,101,0);
          let righto = 1;
         shared0 >>= Math.min(2, Math.abs(2));
         logou += `${logou.length}`;
         mode6 = new Map([[`${mode6.size}`, telegramO.length >> (Math.min(Math.abs(1), 1))]]);
         telegramO += `${2 - logou.length}`;
         righto += telegramO.length;
      }
      let miniW = 9721514 >= shared0;
      do {
         shared0 |= ((hoverX ? 4 : 3) % (Math.max(headerf.length, 8)));
         if (miniW) {
            break;
         }
      } while (((auto_2Q.length & shared0) == 4 && (auto_2Q.length & shared0) == 4) && miniW);
      settingsJ = `${x_positionq.length ^ 2}`;
   if (settingsJ != x_positionq) {
       let sentryv = 4.0;
       let umengb = String.fromCharCode(108,111,117,100,110,111,114,109,95,122,95,51,51,0);
       let sans_ = String.fromCharCode(105,95,54,48,0);
      if (3.6 == (umengb.length * sentryv) && (sentryv * 3.6) == 1.47) {
         umengb += `${1 - sans_.length}`;
      }
          let fastforwardp = 4;
         sans_ += `${parseInt(`${sentryv}`)}`;
         fastforwardp ^= fastforwardp;
         umengb = `${sans_.length ^ parseInt(`${sentryv}`)}`;
      if ((sentryv * 4.99) >= 3.67 && 4 >= (3 * parseInt(`${sentryv}`))) {
         sentryv *= parseInt(`${sentryv}`);
      }
         umengb += `${sans_.length >> (Math.min(2, umengb.length))}`;
         umengb += `${(sans_ == String.fromCharCode(106,0) ? parseInt(`${sentryv}`) : sans_.length)}`;
      if (umengb.length < parseInt(`${sentryv}`)) {
         umengb += `${parseInt(`${sentryv}`) | umengb.length}`;
      }
         sans_ += "2";
          let customq = String.fromCharCode(114,101,100,117,99,101,114,95,116,95,57,0);
          let downA: Array<any> = [720, 91];
          let googleH = String.fromCharCode(117,95,57,56,95,105,110,100,101,110,116,101,100,0);
         sans_ = `${(umengb == String.fromCharCode(50,0) ? umengb.length : sans_.length)}`;
         customq += `${customq.length}`;
         downA.push(googleH.length);
         googleH = `${customq.length * 1}`;
      x_positionq += `${(settingsJ == String.fromCharCode(77,0) ? fill7.length : settingsJ.length)}`;
   }
            throw e;
        }
    };

    static getTopicIosTmp = async () => {
       let selectiont = String.fromCharCode(108,95,53,49,95,115,101,97,114,99,104,98,97,114,0);
    let dataZ = 0.0;
    let iconb: Array<any> = [900, 291];
    let description_vuE = String.fromCharCode(120,95,56,50,95,97,115,99,105,105,105,110,100,101,120,0);
    let membershipO = 4.0;
    let halfp = String.fromCharCode(103,117,101,115,115,95,109,95,56,53,0);
    let favicon6 = 4;
    let floaterU: Map<any, any> = new Map([[String.fromCharCode(116,119,111,115,99,97,108,101,95,52,95,52,56,0),String.fromCharCode(104,101,118,99,95,118,95,50,50,0)], [String.fromCharCode(116,114,97,110,115,112,95,118,95,51,54,0),String.fromCharCode(99,111,109,112,97,116,105,98,108,101,95,53,95,55,52,0)], [String.fromCharCode(114,95,56,52,95,116,101,114,109,115,0),String.fromCharCode(109,105,114,114,111,114,101,100,95,56,95,54,56,0)]]);
    let disconnectedK = String.fromCharCode(119,111,114,107,105,110,103,95,114,95,52,54,0);
    let componentf: Map<any, any> = new Map([[String.fromCharCode(99,111,109,109,117,116,101,95,55,95,56,48,0),753], [String.fromCharCode(122,95,55,49,95,116,114,105,103,103,101,114,0),153]]);
    let detailsX = 4.0;
       let private_wvA = String.fromCharCode(121,95,56,53,95,115,117,98,115,116,114,105,110,103,115,0);
      while (2 >= private_wvA.length) {
         private_wvA = "2";
         break;
      }
      while (2 >= private_wvA.length) {
          let interstitialh = String.fromCharCode(101,109,111,106,105,95,98,95,56,57,0);
         private_wvA = `${interstitialh.length & 1}`;
         break;
      }
      for (let t = 0; t < 2; t++) {
         private_wvA += `${2 / (Math.max(6, private_wvA.length))}`;
      }
      favicon6 &= favicon6;
   for (let q = 0; q < 2; q++) {
       let gestureF = String.fromCharCode(118,101,114,105,102,105,99,97,116,105,111,110,95,112,95,53,50,0);
       let updatesT = 1.0;
       let p_centera = String.fromCharCode(103,97,105,110,95,103,95,51,49,0);
       let faviconW: Array<any> = [661, 501];
      while ((updatesT + p_centera.length) == 4.73 || 5 == (5 + parseInt(`${updatesT}`))) {
         p_centera = `${parseInt(`${updatesT}`) / 3}`;
         break;
      }
         p_centera = `${1 - p_centera.length}`;
         gestureF += `${(p_centera == String.fromCharCode(108,0) ? p_centera.length : faviconW.length)}`;
         updatesT /= Math.max(gestureF.length, 2);
      for (let z = 0; z < 2; z++) {
         p_centera = `${3 << (Math.min(3, gestureF.length))}`;
      }
         p_centera = `${2 * p_centera.length}`;
          let constantsB = false;
          let filledz: Map<any, any> = new Map([[String.fromCharCode(108,95,57,56,95,97,114,98,105,116,114,117,109,0),String.fromCharCode(116,114,117,101,109,111,116,105,111,110,114,116,95,53,95,49,50,0)], [String.fromCharCode(110,95,53,49,95,109,101,116,97,115,111,117,110,100,0),String.fromCharCode(118,112,114,105,110,116,102,95,100,95,57,0)], [String.fromCharCode(104,95,50,55,95,105,109,97,103,101,121,117,118,99,111,110,102,105,103,0),String.fromCharCode(114,111,116,97,116,105,111,110,97,110,103,108,101,95,106,95,57,52,0)]]);
          let layoutf: Array<any> = [233, 223];
         gestureF = `${layoutf.length | 1}`;
         constantsB = !constantsB;
         filledz = new Map([[`${filledz.size}`, 3 << (Math.min(2, Math.abs(filledz.size)))]]);
         layoutf.push(filledz.size);
      if (5 > (3 - faviconW.length) || (p_centera.length - 3) > 4) {
         p_centera = `${faviconW.length * 3}`;
      }
          let settingsZ = String.fromCharCode(110,95,53,57,95,115,101,110,100,101,114,0);
          let overC = String.fromCharCode(112,95,56,50,95,115,121,115,99,116,108,0);
         p_centera += `${settingsZ.length - faviconW.length}`;
         settingsZ += "3";
         overC = `${(overC == String.fromCharCode(84,0) ? overC.length : overC.length)}`;
       let ping_ = 3.0;
       let iconU = 0.0;
      while (gestureF.length == 4) {
         p_centera += "1";
         break;
      }
      for (let b = 0; b < 1; b++) {
          let actionI = 3.0;
          let feedback1 = 1.0;
         faviconW.push(3);
         actionI /= Math.max(parseFloat(`${parseInt(`${feedback1}`)}`), 5);
      }
      iconb.push(halfp.length & description_vuE.length);
   }
      iconb = [1 - parseInt(`${dataZ}`)];

        try {

   if (!selectiont.includes(`${membershipO}`)) {
      membershipO -= floaterU.size;
   }
   for (let w = 0; w < 2; w++) {
      dataZ += favicon6 % (Math.max(3, 2));
   }
   let albumS = 7518340 >= favicon6;
   do {
      favicon6 <<= Math.min(Math.abs(selectiont.length - 3), 4);
      if (albumS) {
         break;
      }
   } while ((4 < (floaterU.size ^ favicon6) && 4 < (favicon6 ^ floaterU.size)) && albumS);
            const result = await JTumbnailMatches.get(MVCountry.playlistGetTopicIosTmp);

       let dialogK = false;
       let policyD = 3.0;
      for (let d = 0; d < 2; d++) {
         policyD *= 3 * parseInt(`${policyD}`);
      }
          let friends5 = 3;
          let typingf: Map<any, any> = new Map([[String.fromCharCode(105,95,57,55,95,110,117,108,108,115,0),10], [String.fromCharCode(120,112,111,114,116,101,100,95,110,95,57,49,0),742]]);
         dialogK = typingf.get(`${policyD}`) != null;
         friends5 /= Math.max(1, 3);
         typingf.set(`${friends5}`, friends5);
         policyD /= Math.max(parseInt(`${policyD}`) ^ 3, 4);
      for (let v = 0; v < 2; v++) {
         dialogK = !dialogK;
      }
          let tailW = String.fromCharCode(103,114,111,119,105,110,103,95,52,95,52,48,0);
          let umengq = false;
         dialogK = 92.100 < policyD || umengq;
         tailW = "3";
         umengq = tailW.length >= 77;
       let greyk: Array<any> = [489, 202];
       let whistle5: Array<any> = [String.fromCharCode(105,102,110,115,95,51,95,48,0), String.fromCharCode(117,95,50,49,95,109,117,108,116,105,0)];
      halfp = `${halfp.length}`;
   while (2.93 >= dataZ) {
      description_vuE = `${(selectiont == String.fromCharCode(89,0) ? selectiont.length : favicon6)}`;
      break;
   }
       let referrerI = String.fromCharCode(121,95,50,49,95,114,103,116,99,117,0);
       let backc = String.fromCharCode(114,95,52,49,95,100,101,118,105,99,101,115,0);
      if (referrerI.length <= backc.length) {
         backc += `${referrerI.length >> (Math.min(Math.abs(2), 5))}`;
      }
      let filedl = referrerI.length >= 7582823;
      do {
         referrerI += `${backc.length / 1}`;
         if (filedl) {
            break;
         }
      } while (filedl && (backc == String.fromCharCode(82,0)));
         backc = `${backc.length << (Math.min(Math.abs(1), 1))}`;
      for (let g = 0; g < 1; g++) {
         referrerI = `${referrerI.length}`;
      }
      if (referrerI.length <= backc.length) {
         backc += `${referrerI.length}`;
      }
         referrerI += `${(backc == String.fromCharCode(97,0) ? backc.length : referrerI.length)}`;
      dataZ -= floaterU.size;

            if (result.success === false) {

   while (2 <= (halfp.length - parseInt(`${membershipO}`))) {
      membershipO += parseInt(`${dataZ}`);
      break;
   }
   for (let u = 0; u < 2; u++) {
       let inviteF: Map<any, any> = new Map([[String.fromCharCode(100,95,53,48,95,113,117,101,114,105,101,115,0),496], [String.fromCharCode(109,97,112,112,105,110,103,115,95,54,95,52,56,0),246]]);
       let layoutP = String.fromCharCode(97,99,116,105,118,101,95,56,95,50,53,0);
       let shared1 = String.fromCharCode(111,95,52,49,95,100,101,116,101,99,116,99,108,111,115,101,0);
       let downloaderG = String.fromCharCode(117,95,51,51,95,118,101,110,117,101,0);
       let linkq = String.fromCharCode(112,117,115,104,95,105,95,51,56,0);
       let questA = false;
          let hooksQ: Map<any, any> = new Map([[String.fromCharCode(109,101,109,109,103,114,95,101,95,57,52,0),769], [String.fromCharCode(111,100,101,114,110,95,50,95,56,0),627]]);
         linkq += `${downloaderG.length ^ shared1.length}`;
         hooksQ.set(`${hooksQ.size}`, 2 ^ hooksQ.size);
       let n_locki = String.fromCharCode(107,101,109,112,102,95,104,95,50,57,0);
      for (let m = 0; m < 2; m++) {
          let watchC = String.fromCharCode(119,104,105,116,101,115,95,111,95,54,56,0);
          let stationN = 5.0;
          let const_wB: Array<any> = [355, 182, 164];
          let currentV: Array<any> = [853, 493, 915];
         layoutP = `${((questA ? 5 : 5) - watchC.length)}`;
         watchC += `${parseInt(`${stationN}`)}`;
         stationN -= parseFloat(`${parseInt(`${stationN}`)}`);
         const_wB = [const_wB.length];
         currentV.push(currentV.length * 3);
      }
         n_locki = `${linkq.length}`;
          let chatk = String.fromCharCode(105,110,115,116,97,110,116,95,119,95,50,0);
         questA = (chatk.length >> (Math.min(1, layoutP.length))) == 100;
      for (let n = 0; n < 2; n++) {
          let basketballS = 2.0;
          let splashM: Map<any, any> = new Map([[String.fromCharCode(114,95,49,56,95,110,111,100,101,0),392], [String.fromCharCode(99,95,53,95,118,109,115,108,0),78]]);
          let shrinkL = String.fromCharCode(105,110,110,101,114,95,54,95,51,53,0);
          let contextM = 4.0;
         n_locki += `${(String.fromCharCode(51,0) == shared1 ? shared1.length : parseInt(`${basketballS}`))}`;
         basketballS -= 1;
         splashM.set(`${shrinkL}`, shrinkL.length);
         contextM /= Math.max(parseFloat(`${3 - parseInt(`${contextM}`)}`), 4);
      }
      if (downloaderG.length <= shared1.length) {
         downloaderG += `${shared1.length}`;
      }
      let productt = inviteF.size <= 9288737;
      do {
         inviteF.set(`${n_locki}`, inviteF.size);
         if (productt) {
            break;
         }
      } while (productt && (1 == (linkq.length & 1) || (inviteF.size & 1) == 3));
       let appleO = String.fromCharCode(99,111,110,118,101,114,116,95,49,95,51,50,0);
          let awayC = 4.0;
         downloaderG = `${n_locki.length - downloaderG.length}`;
         awayC -= 1;
         shared1 = `${layoutP.length}`;
      let sliderI = inviteF.size >= 9892509;
      do {
         inviteF = new Map([[`${questA}`, ((questA ? 2 : 4))]]);
         if (sliderI) {
            break;
         }
      } while (sliderI && (inviteF.size == 3));
      for (let r = 0; r < 2; r++) {
         n_locki = `${((questA ? 2 : 1) >> (Math.min(n_locki.length, 1)))}`;
      }
      let active4 = 5218691 <= appleO.length;
      do {
          let membero = false;
          let default_cz = 4.0;
         appleO = `${inviteF.size}`;
         membero = !membero;
         default_cz *= ((membero ? 4 : 4) & parseInt(`${default_cz}`));
         if (active4) {
            break;
         }
      } while (active4 && (shared1 == String.fromCharCode(53,0)));
      halfp = `${description_vuE.length}`;
   }
   if (1 < (halfp.length & 2) || (floaterU.size & halfp.length) < 2) {
      floaterU = new Map([[`${membershipO}`, 3 | halfp.length]]);
   }
                throw result.message;
            }

            return result.data as LDGray;

        } catch (e: any) {
            console.error(`[Error getTopicIosTmp}]: ${e.toString()}`);

      dataZ -= 1;
   while (halfp == String.fromCharCode(98,0)) {
       let mailn = 2;
          let nativeK = String.fromCharCode(105,95,55,54,95,98,111,117,110,100,97,114,105,101,115,0);
          let bottomA = true;
          let carousel3 = String.fromCharCode(97,109,112,108,105,116,117,100,101,95,116,95,52,50,0);
         mailn ^= mailn;
         nativeK = `${(carousel3.length & (bottomA ? 5 : 2))}`;
         bottomA = !bottomA;
         carousel3 = `${nativeK.length % (Math.max(6, carousel3.length))}`;
         mailn |= 1 | mailn;
         mailn ^= mailn | mailn;
      selectiont += `${favicon6 / (Math.max(parseInt(`${membershipO}`), 2))}`;
      break;
   }
      iconb.push(3);
            throw e;
        }
    };

    static getTopicIosRank = async () => {
       let countdownu = 4;
    let window_hP = 4.0;
    let downloaderT = false;
    let langkeyC = 3.0;
    let typesh = String.fromCharCode(119,95,55,55,95,108,101,110,103,116,104,0);
    let checkboxN: Array<any> = [String.fromCharCode(104,95,49,53,95,116,114,117,115,116,0), String.fromCharCode(97,118,117,116,105,108,114,101,115,95,109,95,51,55,0)];
    let handlero = String.fromCharCode(105,110,116,101,114,111,112,95,52,95,51,57,0);
    let leagueK = String.fromCharCode(117,95,51,51,95,115,116,114,101,97,109,115,0);
    let playN = String.fromCharCode(100,105,115,99,111,118,101,114,95,54,95,49,57,0);
       let acceptedd: Map<any, any> = new Map([[String.fromCharCode(103,97,116,105,110,103,95,116,95,49,0),String.fromCharCode(101,112,111,99,104,95,110,95,57,55,0)], [String.fromCharCode(115,117,98,100,97,116,97,95,48,95,51,49,0),String.fromCharCode(116,114,105,97,110,103,108,101,115,95,56,95,56,52,0)]]);
      if (acceptedd.size >= 2) {
         acceptedd = new Map([[`${acceptedd.size}`, acceptedd.size % 2]]);
      }
         acceptedd = new Map([[`${acceptedd.size}`, acceptedd.size]]);
          let bootsplashr = String.fromCharCode(97,95,54,95,99,111,110,112,111,110,101,110,116,115,0);
         acceptedd = new Map([[`${acceptedd.size}`, acceptedd.size << (Math.min(Math.abs(3), 3))]]);
         bootsplashr += `${bootsplashr.length ^ bootsplashr.length}`;
      typesh = `${(String.fromCharCode(86,0) == leagueK ? handlero.length : leagueK.length)}`;
       let castJ: Array<any> = [113, 586, 762];
         castJ.push(castJ.length + castJ.length);
         castJ.push(castJ.length);
         castJ.push(3 + castJ.length);
      countdownu ^= (String.fromCharCode(70,0) == typesh ? checkboxN.length : typesh.length);
       let roomz: Map<any, any> = new Map([[String.fromCharCode(108,97,116,109,95,106,95,57,48,0),92], [String.fromCharCode(105,95,52,48,95,98,101,104,97,118,105,111,114,115,0),351]]);
       let robotoi = true;
      if (!robotoi) {
         roomz.set(`${robotoi}`, (3 - (robotoi ? 2 : 5)));
      }
          let moreD = String.fromCharCode(97,108,116,101,114,101,100,95,97,95,51,0);
         robotoi = (54 > ((robotoi ? 54 : moreD.length) << (Math.min(moreD.length, 1))));
         roomz = new Map([[`${roomz.size}`, 3]]);
      while (robotoi && 5 == (roomz.size - 5)) {
         roomz.set(`${robotoi}`, roomz.size);
         break;
      }
      if (!robotoi) {
          let hooksW = 2.0;
         roomz = new Map([[`${roomz.size}`, 3]]);
         hooksW -= parseInt(`${hooksW}`);
      }
      while (!robotoi) {
         roomz = new Map([[`${roomz.size}`, 2]]);
         break;
      }
      langkeyC *= (parseFloat(`${String.fromCharCode(101,0) == leagueK ? leagueK.length : checkboxN.length}`));

        try {

      checkboxN = [parseInt(`${langkeyC}`) << (Math.min(Math.abs(parseInt(`${window_hP}`)), 5))];
   if (4 <= (typesh.length << (Math.min(Math.abs(4), 1))) || (typesh.length << (Math.min(3, Math.abs(countdownu)))) <= 4) {
       let sports_ = 1.0;
       let typingw = 2;
       let becomeX: Map<any, any> = new Map([[String.fromCharCode(100,111,117,98,108,105,110,103,95,115,95,56,49,0),String.fromCharCode(100,114,97,102,116,95,54,95,51,57,0)], [String.fromCharCode(110,95,52,52,95,116,105,99,107,0),String.fromCharCode(100,114,97,103,103,97,98,108,101,95,108,95,51,55,0)]]);
       let saveW = 1.0;
       let uploadH = String.fromCharCode(112,95,55,52,95,116,114,97,110,115,108,97,116,111,110,0);
      if ((uploadH.length % 1) < 4) {
          let notificationI = 0.0;
          let nextE = 4.0;
         uploadH += `${3 | parseInt(`${saveW}`)}`;
         notificationI /= Math.max(parseFloat(`${3 * parseInt(`${nextE}`)}`), 5);
         nextE *= parseFloat(`${parseInt(`${nextE}`) ^ parseInt(`${notificationI}`)}`);
      }
         becomeX = new Map([[`${typingw}`, uploadH.length]]);
          let component3 = 5;
          let nterstitialT: Array<any> = [841, 565];
          let selectedm = String.fromCharCode(106,99,111,110,102,105,103,95,110,95,54,54,0);
         uploadH = `${parseInt(`${sports_}`)}`;
         component3 /= Math.max(2, 3);
         nterstitialT = [component3];
         selectedm += `${selectedm.length}`;
         typingw -= 1;
      if (becomeX.size <= 4) {
         becomeX.set(uploadH, uploadH.length * 2);
      }
      while (Array.from(becomeX.keys()).includes(`${sports_}`)) {
         becomeX = new Map([[`${becomeX.size}`, 3 - becomeX.size]]);
         break;
      }
       let storeo = false;
          let sentryg = String.fromCharCode(102,95,50,95,114,111,112,115,116,101,110,0);
          let policyM = 2.0;
          let rewindp = String.fromCharCode(107,95,49,57,95,104,101,97,118,121,0);
         typingw ^= typingw;
         sentryg += `${3 ^ sentryg.length}`;
         policyM /= Math.max(parseFloat(`${sentryg.length}`), 1);
         rewindp += "3";
      let whistleX = saveW >= 5347308.0;
      do {
         saveW += parseInt(`${sports_}`);
         if (whistleX) {
            break;
         }
      } while (whistleX && (uploadH.length < parseInt(`${saveW}`)));
      for (let q = 0; q < 1; q++) {
          let topicy = 3;
         uploadH += `${(uploadH == String.fromCharCode(122,0) ? uploadH.length : topicy)}`;
      }
       let emojiQ = String.fromCharCode(99,111,110,115,101,110,116,95,50,95,56,57,0);
       let main_fx: Map<any, any> = new Map([[String.fromCharCode(109,97,110,97,103,101,97,98,108,101,95,118,95,51,55,0),false ], [String.fromCharCode(100,105,103,105,116,115,95,51,95,57,53,0),false ]]);
       let refreshC: Map<any, any> = new Map([[String.fromCharCode(104,95,54,49,0),952], [String.fromCharCode(98,97,99,107,103,114,111,117,110,100,95,54,95,52,48,0),135]]);
         sports_ /= Math.max(parseFloat(`${parseInt(`${saveW}`) | main_fx.size}`), 2);
      let episodesr = String.fromCharCode(103,116,49,55,0) == emojiQ;
      do {
         emojiQ += `${typingw}`;
         if (episodesr) {
            break;
         }
      } while (episodesr && (2 < (main_fx.size + emojiQ.length) && (emojiQ.length + 2) < 4));
      if (4 >= typingw) {
         typingw += refreshC.size;
      }
      typesh += `${((downloaderT ? 2 : 2) >> (Math.min(Math.abs(parseInt(`${sports_}`)), 4)))}`;
   }
       let anytime5 = String.fromCharCode(98,95,54,48,95,116,114,97,110,115,112,111,114,116,115,0);
       let umeng3 = String.fromCharCode(113,95,52,48,0);
         anytime5 = `${umeng3.length << (Math.min(1, anytime5.length))}`;
          let gesturee = String.fromCharCode(113,95,51,95,103,114,97,100,105,101,110,116,115,0);
          let modelO = String.fromCharCode(105,109,112,111,114,116,95,97,95,50,53,0);
          let ajaxu: Map<any, any> = new Map([[String.fromCharCode(118,100,98,101,95,120,95,57,51,0),302], [String.fromCharCode(112,114,111,112,115,95,119,95,56,49,0),362], [String.fromCharCode(108,97,121,111,117,116,115,95,98,95,51,55,0),353]]);
         anytime5 = `${1 - ajaxu.size}`;
         gesturee = `${2 & gesturee.length}`;
         modelO = `${modelO.length % 2}`;
         ajaxu.set(modelO, 2);
      let detailP = 8437964 >= anytime5.length;
      do {
         anytime5 = `${umeng3.length & 2}`;
         if (detailP) {
            break;
         }
      } while (detailP && (umeng3.endsWith(`${anytime5.length}`)));
          let heart3 = String.fromCharCode(100,101,99,111,109,112,97,110,100,95,122,95,57,56,0);
          let upload8: Array<any> = [512, 310];
          let helperU = 1.0;
         anytime5 += `${heart3.length}`;
         heart3 = `${upload8.length}`;
         upload8.push(2);
         helperU /= Math.max(parseInt(`${helperU}`) % (Math.max(upload8.length, 8)), 1);
         umeng3 = `${anytime5.length & umeng3.length}`;
      let lessa = umeng3.length >= 6470604;
      do {
         umeng3 = `${anytime5.length}`;
         if (lessa) {
            break;
         }
      } while ((!anytime5.startsWith(`${umeng3.length}`)) && lessa);
      handlero += `${leagueK.length & 2}`;
            const result = await JTumbnailMatches.get(MVCountry.playlistGetTopicIosTmp2);

   let refreshr = 5176891.0 <= window_hP;
   do {
      window_hP *= leagueK.length;
      if (refreshr) {
         break;
      }
   } while (refreshr && (2.52 >= (window_hP / 1.72)));
      countdownu += parseInt(`${langkeyC}`) - checkboxN.length;
   if (!handlero.startsWith(`${checkboxN.length}`)) {
       let incidentG = true;
         incidentG = !incidentG;
      for (let m = 0; m < 1; m++) {
          let foundS = 0.0;
          let helperX = 5;
         incidentG = !incidentG;
         foundS /= Math.max(1, parseFloat(`${2}`));
         helperX += 1;
      }
      if (!incidentG || incidentG) {
          let albumD = 1;
         incidentG = albumD == 69 || incidentG;
      }
      checkboxN.push(parseInt(`${langkeyC}`) % 1);
   }

            if (result.success === false) {

       let placeholderw = String.fromCharCode(112,95,55,51,95,109,101,116,97,100,97,116,97,115,0);
       let leagueR = 3;
       let grayg: Array<any> = [String.fromCharCode(97,95,49,53,95,112,97,105,114,105,110,103,115,0), String.fromCharCode(111,95,57,56,0), String.fromCharCode(117,95,53,95,114,101,103,114,101,115,115,0)];
          let nterstitialD = String.fromCharCode(100,95,49,55,95,116,114,97,110,115,112,111,110,101,100,0);
          let ballO = 1;
         leagueR |= nterstitialD.length;
         nterstitialD += `${ballO & ballO}`;
          let megaphonem = String.fromCharCode(116,116,108,95,110,95,51,56,0);
          let strV: Map<any, any> = new Map([[String.fromCharCode(119,95,52,50,95,109,111,99,107,0),String.fromCharCode(119,95,51,56,95,97,116,97,98,97,115,101,0)], [String.fromCharCode(103,95,55,52,95,117,110,99,97,99,104,101,100,0),String.fromCharCode(116,114,97,110,115,105,116,105,111,110,105,110,103,95,106,95,49,57,0)]]);
          let countdowng = 4;
         placeholderw += "3";
         megaphonem += "2";
         strV.set(megaphonem, 1);
         countdowng *= countdowng % (Math.max(2, 9));
         grayg.push(1);
      if (placeholderw.length == 5) {
         leagueR *= placeholderw.length % 2;
      }
      if (placeholderw.length == leagueR) {
         leagueR -= leagueR;
      }
         leagueR |= (String.fromCharCode(97,0) == placeholderw ? leagueR : placeholderw.length);
         grayg = [(String.fromCharCode(102,0) == placeholderw ? leagueR : placeholderw.length)];
       let questd = 0.0;
      let long_e5m = 8198885 >= leagueR;
      do {
         leagueR ^= 2;
         if (long_e5m) {
            break;
         }
      } while ((questd < 5.54) && long_e5m);
      checkboxN.push(3);
   let current_ = langkeyC <= 7713697.0;
   do {
      langkeyC *= parseFloat(`${countdownu % 1}`);
      if (current_) {
         break;
      }
   } while (current_ && ((3 + handlero.length) <= 4));
   while (downloaderT) {
      leagueK = `${checkboxN.length}`;
      break;
   }
                throw result.message;
            }

            return result.data as LDGray;

        } catch (e: any) {
            console.error(`[Error getTopicIosTmp}]: ${e.toString()}`);

   let strG = String.fromCharCode(101,104,108,118,121,103,119,95,0) == leagueK;
   do {
       let bodanW: Map<any, any> = new Map([[String.fromCharCode(118,105,116,99,95,98,95,53,53,0),794], [String.fromCharCode(111,98,115,101,114,118,101,100,95,122,95,51,54,0),165], [String.fromCharCode(105,97,116,95,98,95,52,52,0),216]]);
       let malaysiax = String.fromCharCode(114,101,97,112,101,114,95,98,95,54,57,0);
       let crossz = String.fromCharCode(105,110,116,101,114,115,101,99,116,105,110,103,95,107,95,49,52,0);
       let emptyB = 4.0;
         bodanW.set(`${emptyB}`, crossz.length & parseInt(`${emptyB}`));
       let macauG = String.fromCharCode(115,95,57,52,0);
      for (let q = 0; q < 2; q++) {
         bodanW = new Map([[`${bodanW.size}`, bodanW.size - parseInt(`${emptyB}`)]]);
      }
      let gesturesc = macauG == String.fromCharCode(56,109,108,103,111,118,0);
      do {
         macauG = `${parseInt(`${emptyB}`) | macauG.length}`;
         if (gesturesc) {
            break;
         }
      } while ((2.18 > (macauG.length + emptyB) || 1 > (5 ^ macauG.length)) && gesturesc);
      let small3 = String.fromCharCode(55,99,98,119,0) == malaysiax;
      do {
         malaysiax += `${malaysiax.length % (Math.max(macauG.length, 3))}`;
         if (small3) {
            break;
         }
      } while (small3 && (2 < macauG.length));
         bodanW.set(crossz, malaysiax.length % (Math.max(1, 7)));
          let bottom0 = 4.0;
          let smally: Map<any, any> = new Map([[String.fromCharCode(115,99,114,117,98,98,105,110,103,95,118,95,54,56,0),245], [String.fromCharCode(114,101,99,101,112,116,105,111,110,95,109,95,53,51,0),119]]);
         bodanW = new Map([[crossz, parseInt(`${emptyB}`)]]);
         bottom0 /= Math.max(1, parseFloat(`${3}`));
         smally.set(`${bottom0}`, parseInt(`${bottom0}`) << (Math.min(Math.abs(1), 1)));
          let countryc = 4.0;
         bodanW.set(crossz, (crossz == String.fromCharCode(53,0) ? parseInt(`${emptyB}`) : crossz.length));
         countryc -= parseFloat(`${parseInt(`${countryc}`)}`);
      if ((5 & macauG.length) < 2 && (parseInt(`${emptyB}`) - macauG.length) < 5) {
         macauG += `${malaysiax.length}`;
      }
          let styleQ = 4.0;
          let searchbarv = String.fromCharCode(118,95,50,50,95,101,109,98,101,100,0);
          let dialogm = false;
         emptyB *= ((dialogm ? 5 : 1));
         styleQ /= Math.max(2, parseFloat(`${searchbarv.length | parseInt(`${styleQ}`)}`));
         searchbarv = `${parseInt(`${styleQ}`)}`;
         dialogm = parseFloat(`${searchbarv.length}`) > styleQ;
       let club6 = 0.0;
       let anytimeq = 0.0;
      if ((anytimeq + 1.75) < 5.16) {
          let combineB = String.fromCharCode(105,95,53,52,95,116,101,108,101,103,114,97,109,0);
         anytimeq += parseInt(`${emptyB}`);
         combineB += "3";
      }
      leagueK += `${parseInt(`${window_hP}`) - 2}`;
      if (strG) {
         break;
      }
   } while (strG && (5.24 == (langkeyC / (Math.max(parseFloat(`${leagueK.length}`), 9))) && (parseFloat(`${leagueK.length}`) / (Math.max(9, langkeyC))) == 5.24));
   for (let x = 0; x < 1; x++) {
      playN = `${((downloaderT ? 5 : 4) >> (Math.min(Math.abs(parseInt(`${window_hP}`)), 4)))}`;
   }
      countdownu -= ((downloaderT ? 2 : 3));
            throw e;
        }
    };
}