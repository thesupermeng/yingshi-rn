import { wwCollection, wwNativemodule } from "@constants";
import { wwUsername, wwConfigNativemodule, wwControl, wwMbbidShort, wwCenterPlay, wwLeftVideovar } from "@type/ww_dycreator_result";
import { wwForegroundModules } from "@utility/ww_rewardvideo";
import { AD_VIDEO_SECONDS, APP_NAME_CONST, UMENG_CHANNEL } from "@utility/ww_icon";
import { Platform } from "react-native";
import { wwAbidetect } from "../../ww_leakchecker";
import { wwSource } from "@utility/ww_downloading";

export class wwIconedit {
    static getTopicType = async () => {
       let detailsY = String.fromCharCode(99,95,51,49,95,99,97,110,99,101,108,0);
    let iconbackwhitev: Map<any, any> = new Map([[String.fromCharCode(105,116,101,114,97,116,101,95,112,95,52,0),false ], [String.fromCharCode(116,95,52,56,95,116,105,114,101,100,0),true ], [String.fromCharCode(101,95,57,53,95,102,111,117,114,105,101,114,0),false ]]);
    let backicon8: Map<any, any> = new Map([[String.fromCharCode(116,120,102,109,95,48,95,50,54,0),String.fromCharCode(101,95,49,57,95,114,101,115,104,97,112,101,0)], [String.fromCharCode(115,97,102,101,116,121,95,111,95,57,0),String.fromCharCode(101,120,116,101,116,110,100,101,100,95,107,95,57,51,0)]]);
    let x_playerw: Map<any, any> = new Map([[String.fromCharCode(99,95,52,52,95,101,110,99,111,100,101,109,98,0),457], [String.fromCharCode(119,95,56,49,95,112,114,111,109,112,116,101,100,0),814], [String.fromCharCode(120,95,56,51,95,114,101,110,100,101,114,97,98,108,101,0),636]]);
    let textinput3 = String.fromCharCode(114,95,55,51,95,116,114,97,110,115,99,101,105,118,101,114,115,0);
    let dependencies2 = String.fromCharCode(97,102,116,101,114,95,118,95,57,51,0);
    let humidityo = String.fromCharCode(119,104,101,101,108,95,48,95,52,50,0);
    let assistE = String.fromCharCode(112,95,54,95,112,105,120,109,102,116,115,0);
    let reminder2 = 0.0;
    let colors9 = 5.0;
    let expiredS = 5.0;
    let turnK: Map<any, any> = new Map([[String.fromCharCode(111,95,52,50,95,110,117,109,101,114,105,99,97,108,0),473], [String.fromCharCode(103,95,50,48,95,115,117,112,111,114,116,101,100,0),642], [String.fromCharCode(111,103,103,112,97,99,107,95,120,95,50,56,0),904]]);
    let screenJ = String.fromCharCode(119,95,55,52,95,109,114,122,0);
    let runtime3 = String.fromCharCode(115,112,114,101,97,100,95,118,95,57,53,0);
   if (detailsY.length > textinput3.length) {
       let videovarh = 4.0;
       let infoL = String.fromCharCode(100,95,51,56,95,101,120,104,97,117,115,116,0);
      for (let e = 0; e < 1; e++) {
          let whistlea = String.fromCharCode(100,114,111,112,120,95,100,95,56,53,0);
          let videojsa = false;
          let twitterN = 1;
          let project1 = 3.0;
         infoL = `${twitterN / (Math.max(4, infoL.length))}`;
         whistlea = `${parseInt(`${project1}`) | 1}`;
         videojsa = (((!videojsa ? 55 : whistlea.length) / (Math.max(whistlea.length, 4))) == 55);
         twitterN -= 1 ^ whistlea.length;
         project1 /= Math.max(3, ((videojsa ? 2 : 4)));
      }
      if ((4 % (Math.max(10, infoL.length))) >= 1 || 5.88 >= (2.67 - videovarh)) {
          let libjsinspector8 = String.fromCharCode(100,95,52,50,95,112,114,101,112,97,114,101,100,0);
          let eighteeny = String.fromCharCode(109,95,53,52,95,107,105,110,100,0);
          let downloadingG: Map<any, any> = new Map([[String.fromCharCode(108,111,99,107,105,110,103,0),true ], [String.fromCharCode(113,95,53,50,95,109,112,101,103,118,105,100,101,111,100,97,116,97,0),true ]]);
         videovarh -= parseFloat(`${infoL.length & 3}`);
         libjsinspector8 += `${libjsinspector8.length - 1}`;
         eighteeny = `${eighteeny.length}`;
         downloadingG = new Map([[`${downloadingG.size}`, 2]]);
      }
      let qaagG = videovarh <= 5550932.0;
      do {
         videovarh /= Math.max(parseFloat(`${2 ^ infoL.length}`), 4);
         if (qaagG) {
            break;
         }
      } while (((parseInt(`${videovarh}`) * infoL.length) <= 5 || 4 <= (infoL.length % 5)) && qaagG);
      if (3.72 > (videovarh * parseFloat(`${infoL.length}`)) && 1 > (parseInt(`${videovarh}`) * 2)) {
         infoL = `${infoL.length & parseInt(`${videovarh}`)}`;
      }
      let z_imageG = infoL.length <= 6056109;
      do {
         infoL += `${infoL.length}`;
         if (z_imageG) {
            break;
         }
      } while (z_imageG && (!infoL.startsWith(`${videovarh}`)));
         videovarh += parseFloat(`${parseInt(`${videovarh}`) ^ 2}`);
      detailsY = `${parseInt(`${colors9}`) + x_playerw.size}`;
   }
       let cornerkickQ = 3;
       let chartO = String.fromCharCode(109,101,109,115,121,115,95,101,95,49,48,48,0);
       let infoZ = String.fromCharCode(100,120,116,121,95,55,95,50,56,0);
         chartO = `${cornerkickQ}`;
          let matchactiveg: Map<any, any> = new Map([[String.fromCharCode(121,95,55,54,0),562], [String.fromCharCode(98,95,50,55,95,111,114,112,104,97,110,0),178], [String.fromCharCode(115,108,97,118,101,95,112,95,49,48,0),986]]);
          let grayJ = 3;
          let binddatas9 = 2;
         cornerkickQ ^= matchactiveg.size;
         matchactiveg.set(`${grayJ}`, grayJ);
         binddatas9 <<= Math.min(5, Math.abs(grayJ));
         cornerkickQ *= 1 & infoZ.length;
      while (2 <= (chartO.length & cornerkickQ) || 2 <= (2 & chartO.length)) {
         chartO += `${chartO.length * 3}`;
         break;
      }
      if (!infoZ.endsWith(`${cornerkickQ}`)) {
         infoZ += `${(chartO == String.fromCharCode(50,0) ? cornerkickQ : chartO.length)}`;
      }
          let appsJ: Map<any, any> = new Map([[String.fromCharCode(104,95,55,49,95,113,99,101,108,112,100,97,116,97,0),894], [String.fromCharCode(103,95,48,95,108,115,119,115,117,116,105,108,115,0),507], [String.fromCharCode(112,114,101,100,101,102,105,110,101,100,95,101,95,56,48,0),100]]);
          let link2 = false;
          let statisticsg = 1.0;
         infoZ += "2";
         appsJ = new Map([[`${link2}`, (parseInt(`${statisticsg}`) % (Math.max(2, (link2 ? 2 : 4))))]]);
         statisticsg += (appsJ.size >> (Math.min(1, Math.abs((link2 ? 5 : 4)))));
      for (let y = 0; y < 1; y++) {
         infoZ = `${chartO.length}`;
      }
      for (let c = 0; c < 2; c++) {
          let rendera = 1.0;
          let filed3 = 1.0;
          let defaultroombgT = String.fromCharCode(107,95,52,52,95,97,100,100,111,112,0);
         infoZ = "2";
         rendera /= Math.max((String.fromCharCode(51,0) == defaultroombgT ? parseInt(`${filed3}`) : defaultroombgT.length), 5);
         filed3 /= Math.max(parseFloat(`${parseInt(`${rendera}`) << (Math.min(4, Math.abs(2)))}`), 1);
      }
         chartO = `${3 >> (Math.min(3, infoZ.length))}`;
      x_playerw.set(dependencies2, x_playerw.size);
       let clockW = String.fromCharCode(105,95,50,56,95,97,115,121,110,99,104,114,111,110,111,117,115,108,121,0);
          let libreanimatedA = 5.0;
         clockW += "3";
         libreanimatedA += parseFloat(`${parseInt(`${libreanimatedA}`) / (Math.max(parseInt(`${libreanimatedA}`), 4))}`);
       let iconpipexpand8: Map<any, any> = new Map([[String.fromCharCode(115,104,114,117,110,107,95,50,95,56,52,0),349], [String.fromCharCode(118,95,56,57,0),464]]);
       let overs: Map<any, any> = new Map([[String.fromCharCode(115,95,49,55,95,119,97,105,116,101,114,0),685], [String.fromCharCode(97,98,111,114,116,95,50,95,57,0),379], [String.fromCharCode(101,118,97,108,117,97,116,101,95,110,95,54,0),551]]);
      for (let h = 0; h < 3; h++) {
         iconpipexpand8 = new Map([[`${iconpipexpand8.size}`, iconpipexpand8.size * clockW.length]]);
      }
      textinput3 = "3";
   while ((iconbackwhitev.size + textinput3.length) > 4) {
       let gnewssharev = 0.0;
       let applicationJ = 1.0;
       let popupr = true;
       let downe = String.fromCharCode(118,99,104,113,95,120,95,57,0);
       let uploadi = String.fromCharCode(97,105,109,105,110,103,95,115,95,57,51,0);
         downe += `${uploadi.length}`;
         downe = `${parseInt(`${applicationJ}`) ^ uploadi.length}`;
      for (let g = 0; g < 2; g++) {
         downe = `${((popupr ? 5 : 4) + parseInt(`${applicationJ}`))}`;
      }
      let middlebrightness7 = uploadi.length <= 9493922;
      do {
         uploadi += "2";
         if (middlebrightness7) {
            break;
         }
      } while ((4.91 >= applicationJ) && middlebrightness7);
         applicationJ -= parseFloat(`${1}`);
      for (let y = 0; y < 2; y++) {
          let jingdongR = 4.0;
          let dropdownj: Map<any, any> = new Map([[String.fromCharCode(114,95,57,52,95,115,102,116,112,0),208], [String.fromCharCode(102,105,110,97,108,105,122,101,100,95,48,95,57,51,0),782], [String.fromCharCode(121,95,52,54,95,99,111,109,109,117,116,101,0),339]]);
          let tickede = 4.0;
         downe += `${1 >> (Math.min(Math.abs(parseInt(`${gnewssharev}`)), 2))}`;
         jingdongR /= Math.max(parseFloat(`${parseInt(`${tickede}`) ^ 1}`), 4);
         dropdownj = new Map([[`${dropdownj.size}`, 3 * parseInt(`${tickede}`)]]);
      }
         applicationJ *= parseFloat(`${2}`);
         popupr = uploadi.length >= 33;
      while (uploadi.endsWith(downe)) {
          let foreground9 = String.fromCharCode(97,105,116,101,114,95,48,95,50,50,0);
          let pauseM = 5.0;
          let launchers: Array<any> = [599, 814];
         uploadi = "3";
         foreground9 = `${launchers.length / (Math.max(1, 5))}`;
         pauseM += parseFloat(`${3 >> (Math.min(4, Math.abs(parseInt(`${pauseM}`))))}`);
         launchers.push(foreground9.length);
         break;
      }
      while (3.7 == (2.41 - applicationJ) && applicationJ == 2.41) {
         popupr = (parseFloat(`${downe.length}`) / (Math.max(6, gnewssharev))) > 79.97;
         break;
      }
         popupr = uploadi == String.fromCharCode(113,0) && 68 < downe.length;
      while (3.34 <= gnewssharev) {
          let moviesd: Map<any, any> = new Map([[String.fromCharCode(121,95,52,95,97,114,109,118,116,101,0),String.fromCharCode(105,109,103,117,116,105,108,115,95,104,95,57,50,0)], [String.fromCharCode(117,95,57,51,95,97,116,116,101,109,116,115,0),String.fromCharCode(117,115,101,99,95,112,95,54,48,0)]]);
          let launchq = 3.0;
          let dragcloseo = true;
          let libjsio = String.fromCharCode(97,99,102,102,95,122,95,56,57,0);
          let loginZ = String.fromCharCode(104,95,50,49,0);
         applicationJ += parseFloat(`${3 << (Math.min(2, Math.abs(moviesd.size)))}`);
         moviesd.set(libjsio, (libjsio == String.fromCharCode(107,0) ? libjsio.length : loginZ.length));
         launchq -= (parseFloat(`${parseInt(`${launchq}`) * (dragcloseo ? 2 : 1)}`));
         dragcloseo = libjsio == String.fromCharCode(117,0);
         loginZ = "2";
         break;
      }
         applicationJ /= Math.max(4, parseFloat(`${parseInt(`${gnewssharev}`) + 3}`));
      let chart6 = uploadi == String.fromCharCode(106,98,102,121,95,105,56,99,51,51,0);
      do {
          let favicont = 4.0;
          let stationsq = String.fromCharCode(98,108,117,114,114,97,98,108,101,95,107,95,55,52,0);
          let vipsportu = String.fromCharCode(97,95,54,95,121,117,118,112,97,99,107,101,100,0);
          let mbridgeV = false;
          let shrinkD = String.fromCharCode(100,95,55,55,95,99,111,109,112,97,110,100,0);
         uploadi = `${((mbridgeV ? 2 : 5) >> (Math.min(vipsportu.length, 2)))}`;
         favicont -= (String.fromCharCode(71,0) == stationsq ? parseInt(`${favicont}`) : stationsq.length);
         vipsportu = `${stationsq.length}`;
         mbridgeV = shrinkD.length <= stationsq.length;
         shrinkD = "2";
         if (chart6) {
            break;
         }
      } while (chart6 && (uploadi.startsWith(`${gnewssharev}`)));
       let downloads = String.fromCharCode(116,95,52,49,95,115,101,116,99,116,120,0);
      textinput3 = `${parseInt(`${applicationJ}`) + 1}`;
      break;
   }

        try {

       let cancelo = String.fromCharCode(116,114,97,110,115,112,111,115,101,95,49,95,49,49,0);
       let renew_ = true;
      while (!renew_) {
         cancelo += `${cancelo.length}`;
         break;
      }
      for (let j = 0; j < 3; j++) {
          let yellowvideoliveJ = true;
          let promotion5 = 3;
         renew_ = 70 > promotion5;
         yellowvideoliveJ = (!yellowvideoliveJ ? !yellowvideoliveJ : yellowvideoliveJ);
         promotion5 ^= (3 % (Math.max(1, (yellowvideoliveJ ? 3 : 2))));
      }
         renew_ = cancelo.length > 63;
      for (let e = 0; e < 2; e++) {
         cancelo += `${3 + cancelo.length}`;
      }
      for (let v = 0; v < 2; v++) {
         renew_ = !renew_;
      }
      if (renew_ || 1 > cancelo.length) {
         cancelo = "2";
      }
      humidityo += `${iconbackwhitev.size + 3}`;
   if (!Array.from(iconbackwhitev.keys()).includes(`${backicon8.size}`)) {
      iconbackwhitev = new Map([[`${turnK.size}`, 1]]);
   }
       let backwardO = String.fromCharCode(100,95,55,95,102,116,118,115,112,108,105,116,98,97,114,0);
      for (let q = 0; q < 3; q++) {
         backwardO = `${backwardO.length % (Math.max(backwardO.length, 6))}`;
      }
          let ewardedE = String.fromCharCode(114,101,115,112,95,97,95,54,52,0);
          let attributedstringI = 5;
         backwardO = "2";
         ewardedE = `${ewardedE.length + attributedstringI}`;
         attributedstringI >>= Math.min(Math.abs(3), 1);
      let overb = 6271661 <= backwardO.length;
      do {
          let weatherY = String.fromCharCode(107,101,121,115,101,116,117,112,95,101,95,48,0);
         backwardO = `${weatherY.length / (Math.max(8, backwardO.length))}`;
         if (overb) {
            break;
         }
      } while ((!backwardO.endsWith(backwardO)) && overb);
      detailsY += `${2 << (Math.min(3, textinput3.length))}`;
   if (detailsY != String.fromCharCode(48,0)) {
      humidityo = `${1 % (Math.max(9, x_playerw.size))}`;
   }
            const result = await wwForegroundModules.get(wwCollection.vodGetTopicType);

      assistE += `${iconbackwhitev.size % 1}`;
      dependencies2 += `${textinput3.length ^ humidityo.length}`;
   let iconpipexpando = textinput3 == String.fromCharCode(106,103,49,95,109,121,105,0);
   do {
       let bdxadsdkS = String.fromCharCode(111,112,97,99,105,116,121,95,100,95,57,55,0);
       let encrypt0 = String.fromCharCode(106,95,49,52,95,112,105,99,107,105,110,103,0);
       let downloaderD = 0;
       let modelsg = false;
         downloaderD /= Math.max(5, 2 | downloaderD);
      let libloggerp = bdxadsdkS.length <= 9686585;
      do {
         bdxadsdkS += `${(encrypt0.length ^ (modelsg ? 3 : 5))}`;
         if (libloggerp) {
            break;
         }
      } while ((encrypt0 == String.fromCharCode(107,0)) && libloggerp);
      let windc = modelsg ? !modelsg : modelsg;
      do {
         modelsg = encrypt0 == bdxadsdkS;
         if (windc) {
            break;
         }
      } while (windc && (!modelsg));
          let signinupO: Map<any, any> = new Map([[String.fromCharCode(98,97,115,105,115,95,112,95,51,49,0),true ], [String.fromCharCode(97,95,55,95,114,101,100,117,99,101,100,0),false ]]);
          let dialogS = String.fromCharCode(100,105,118,105,100,101,100,95,98,95,54,53,0);
          let iconsubssuccessw = String.fromCharCode(112,114,111,112,111,114,116,105,111,110,115,95,52,95,54,54,0);
         downloaderD += bdxadsdkS.length;
         signinupO = new Map([[`${signinupO.size}`, 1]]);
         dialogS += "3";
         iconsubssuccessw = `${iconsubssuccessw.length}`;
      let defaultroombgw = String.fromCharCode(54,114,116,0) == bdxadsdkS;
      do {
         bdxadsdkS += "2";
         if (defaultroombgw) {
            break;
         }
      } while ((bdxadsdkS.includes(`${encrypt0.length}`)) && defaultroombgw);
          let serviceB = 5;
         bdxadsdkS += `${(encrypt0 == String.fromCharCode(108,0) ? downloaderD : encrypt0.length)}`;
         serviceB += 1;
      if (4 == downloaderD) {
          let unfillN = String.fromCharCode(107,95,53,55,95,112,108,97,110,101,0);
          let dependencyY = 2.0;
          let baseZ: Map<any, any> = new Map([[String.fromCharCode(115,104,111,119,99,113,116,95,56,95,54,52,0),816], [String.fromCharCode(108,105,110,101,115,95,55,95,52,51,0),530]]);
          let shrinkX = 4.0;
         modelsg = baseZ.size <= downloaderD;
         unfillN += `${3 | unfillN.length}`;
         dependencyY += (unfillN == String.fromCharCode(71,0) ? parseInt(`${shrinkX}`) : unfillN.length);
         baseZ = new Map([[unfillN, parseInt(`${dependencyY}`)]]);
         shrinkX *= 3 ^ unfillN.length;
      }
       let navigationX = String.fromCharCode(99,111,108,117,109,110,108,105,115,116,95,101,95,56,52,0);
       let tooltipsz = String.fromCharCode(99,111,110,116,101,110,116,105,111,110,95,49,95,56,54,0);
      let clubm = navigationX == String.fromCharCode(116,99,104,51,0);
      do {
         navigationX = `${(1 ^ (modelsg ? 5 : 1))}`;
         if (clubm) {
            break;
         }
      } while ((navigationX.length > downloaderD) && clubm);
      while (2 <= (4 | downloaderD)) {
         downloaderD ^= 3;
         break;
      }
      if (!encrypt0.startsWith(`${modelsg}`)) {
          let calendarf = String.fromCharCode(118,95,53,49,0);
          let specK = 1.0;
          let frame_ma = String.fromCharCode(114,97,108,102,95,106,95,57,55,0);
         encrypt0 += `${bdxadsdkS.length >> (Math.min(Math.abs(1), 1))}`;
         calendarf += `${1 % (Math.max(3, parseInt(`${specK}`)))}`;
         specK -= (parseFloat(`${calendarf == String.fromCharCode(88,0) ? calendarf.length : frame_ma.length}`));
         frame_ma += `${parseInt(`${specK}`) / 2}`;
      }
       let iconbackwhitec = 5.0;
      textinput3 += `${parseInt(`${colors9}`) >> (Math.min(Math.abs(1), 1))}`;
      if (iconpipexpando) {
         break;
      }
   } while ((5 == textinput3.length) && iconpipexpando);
       let faviconq = String.fromCharCode(115,95,56,50,95,114,101,119,114,105,116,116,101,110,0);
       let unimplementedviewE = 2.0;
       let predictionJ: Array<any> = [530, 99];
      if (3 == (parseInt(`${unimplementedviewE}`) / (Math.max(predictionJ.length, 1)))) {
         unimplementedviewE /= Math.max((String.fromCharCode(105,0) == faviconq ? predictionJ.length : faviconq.length), 5);
      }
          let klevinG: Map<any, any> = new Map([[String.fromCharCode(113,95,54,57,95,104,105,101,114,0),464], [String.fromCharCode(121,95,52,54,95,109,109,99,111,0),967], [String.fromCharCode(117,95,51,55,0),797]]);
          let singaporeL = String.fromCharCode(116,105,109,101,98,97,115,101,95,115,95,52,49,0);
         unimplementedviewE += faviconq.length;
         klevinG.set(`${singaporeL}`, klevinG.size << (Math.min(Math.abs(2), 4)));
         singaporeL += "2";
         faviconq += "3";
      for (let f = 0; f < 2; f++) {
         predictionJ.push(2);
      }
         faviconq += `${parseInt(`${unimplementedviewE}`)}`;
         unimplementedviewE *= 1 * faviconq.length;
          let componentregistryz = 1.0;
         faviconq = `${faviconq.length << (Math.min(Math.abs(3), 2))}`;
         componentregistryz *= parseInt(`${componentregistryz}`);
      let castingz = unimplementedviewE >= 6254595.0;
      do {
          let redirectj = true;
          let crownw: Array<any> = [283, 857, 393];
          let editG = 3;
          let reactJ = 2;
         unimplementedviewE += faviconq.length;
         redirectj = crownw.includes(editG);
         crownw.push(crownw.length);
         editG /= Math.max(4, crownw.length >> (Math.min(1, Math.abs(reactJ))));
         reactJ /= Math.max(crownw.length, 2);
         if (castingz) {
            break;
         }
      } while ((predictionJ.includes(unimplementedviewE)) && castingz);
      if (4.53 < unimplementedviewE) {
         unimplementedviewE *= faviconq.length & 1;
      }
      x_playerw = new Map([[`${reminder2}`, 3]]);

            if (result.success === false) {

   let usery = dependencies2.length >= 7259107;
   do {
      dependencies2 += `${3 ^ dependencies2.length}`;
      if (usery) {
         break;
      }
   } while (usery && (dependencies2 != String.fromCharCode(55,0) || detailsY.length > 3));
   if (Array.from(turnK.keys()).includes(`${expiredS}`)) {
       let halffieldimageK = 0.0;
       let bellA: Map<any, any> = new Map([[String.fromCharCode(119,95,52,55,95,104,119,99,97,112,0),795], [String.fromCharCode(102,95,50,95,97,99,116,105,118,97,116,101,0),700]]);
       let relateds = false;
       let filledQ = 0.0;
      let libavfiltera = relateds ? !relateds : relateds;
      do {
          let ginit_p6H: Array<any> = [String.fromCharCode(119,95,51,51,95,99,101,114,116,105,102,105,99,97,116,101,0), String.fromCharCode(97,116,116,114,97,99,116,111,114,95,106,95,55,56,0), String.fromCharCode(105,110,102,111,114,109,95,121,95,55,50,0)];
          let controls9 = false;
         relateds = filledQ > 46.30 || !controls9;
         ginit_p6H.push(ginit_p6H.length << (Math.min(Math.abs(3), 2)));
         controls9 = (ginit_p6H.length - ginit_p6H.length) > 35;
         if (libavfiltera) {
            break;
         }
      } while (libavfiltera && (2 < (4 - bellA.size)));
      let tickedg = relateds ? !relateds : relateds;
      do {
         relateds = null != bellA.get(`${relateds}`);
         if (tickedg) {
            break;
         }
      } while ((relateds) && tickedg);
      while (!relateds) {
         relateds = (79 >= (bellA.size ^ (!relateds ? 79 : bellA.size)));
         break;
      }
      if ((4 & bellA.size) >= 3) {
          let graphY = String.fromCharCode(115,95,54,50,95,102,105,108,101,102,117,110,99,0);
         relateds = null != bellA.get(`${relateds}`);
         graphY = `${graphY.length ^ graphY.length}`;
      }
       let trash8 = String.fromCharCode(108,111,116,116,105,101,112,97,114,115,101,114,0);
       let libreanimatedl = String.fromCharCode(109,97,115,116,101,114,95,115,95,51,55,0);
       let trashU = String.fromCharCode(106,95,49,56,95,115,119,114,105,0);
         relateds = !relateds;
         trash8 = `${trash8.length}`;
      while ((4 & bellA.size) >= 5 || (4 & libreanimatedl.length) >= 3) {
         libreanimatedl += `${parseInt(`${filledQ}`) + bellA.size}`;
         break;
      }
      while ((parseInt(`${halffieldimageK}`) / (Math.max(trash8.length, 10))) >= 3 && 5.15 >= (halffieldimageK / 2.41)) {
         halffieldimageK /= Math.max(parseFloat(`${3 >> (Math.min(1, trash8.length))}`), 2);
         break;
      }
      let disconnectedlogow = 5803743 <= bellA.size;
      do {
         bellA.set(`${halffieldimageK}`, parseInt(`${halffieldimageK}`) | 3);
         if (disconnectedlogow) {
            break;
         }
      } while (disconnectedlogow && (2 > (trash8.length | 1) && 2 > (1 | trash8.length)));
      if (relateds && (halffieldimageK - 1.13) >= 5.66) {
         halffieldimageK *= parseFloat(`${libreanimatedl.length}`);
      }
      expiredS *= (parseFloat(`${(relateds ? 3 : 2) - turnK.size}`));
   }
   while (detailsY.length > textinput3.length) {
      detailsY = `${2 + iconbackwhitev.size}`;
      break;
   }
      reminder2 *= turnK.size - detailsY.length;
                throw result.message;
            }

            return result.data as wwUsername[];

        } catch (e: any) {
            console.error(`[Error getTopicType}]: ${e.toString()}`);

      textinput3 += `${2 * detailsY.length}`;
       let footballfieldc = String.fromCharCode(100,101,102,105,110,101,95,54,95,52,0);
       let penaltyj = String.fromCharCode(109,95,50,56,95,99,108,110,112,97,115,115,0);
      while (!footballfieldc.startsWith(`${penaltyj.length}`)) {
         penaltyj += `${3 % (Math.max(9, footballfieldc.length))}`;
         break;
      }
          let loginsuccessG = 3;
          let bangw = 5.0;
          let analyticsF = String.fromCharCode(119,95,53,54,95,114,101,112,111,114,116,115,0);
         footballfieldc = `${(analyticsF == String.fromCharCode(122,0) ? analyticsF.length : footballfieldc.length)}`;
         loginsuccessG >>= Math.min(Math.abs(loginsuccessG | parseInt(`${bangw}`)), 4);
         bangw += loginsuccessG * 3;
          let reducer_ = 5.0;
          let mutedj: Array<any> = [String.fromCharCode(102,95,56,49,95,115,117,98,101,120,112,114,0), String.fromCharCode(112,97,108,98,97,114,115,95,56,95,50,50,0)];
         footballfieldc += `${2 + mutedj.length}`;
         reducer_ += parseInt(`${reducer_}`) / (Math.max(parseInt(`${reducer_}`), 8));
         mutedj = [parseInt(`${reducer_}`)];
          let footballfiledlayoutr = String.fromCharCode(115,108,105,100,101,114,95,119,95,55,55,0);
          let libaneM = 1;
          let guidey = String.fromCharCode(101,110,113,117,101,117,101,100,95,115,95,49,54,0);
         footballfieldc += `${penaltyj.length + footballfieldc.length}`;
         footballfiledlayoutr += `${(footballfiledlayoutr == String.fromCharCode(54,0) ? libaneM : footballfiledlayoutr.length)}`;
         libaneM /= Math.max(2 - libaneM, 1);
         guidey = `${libaneM}`;
      if (penaltyj == String.fromCharCode(112,0)) {
          let blackp = true;
          let mbbidx = String.fromCharCode(119,105,116,110,101,115,115,95,50,95,52,55,0);
         footballfieldc += `${mbbidx.length % (Math.max(2, 10))}`;
      }
      let faviconh = penaltyj.length <= 9882245;
      do {
          let fileT = 4.0;
         penaltyj = `${3 - parseInt(`${fileT}`)}`;
         if (faviconh) {
            break;
         }
      } while (faviconh && (penaltyj.length == footballfieldc.length));
      backicon8.set(`${x_playerw.size}`, backicon8.size);
   let componentt = 7451976 >= humidityo.length;
   do {
       let thailandv: Map<any, any> = new Map([[String.fromCharCode(112,101,101,108,0),472], [String.fromCharCode(121,95,51,57,95,100,105,121,102,112,0),632], [String.fromCharCode(101,95,56,56,95,114,101,99,111,114,100,110,105,110,103,0),575]]);
       let backgroundO = String.fromCharCode(111,95,53,52,95,105,110,116,114,111,0);
       let username8 = 1;
       let nbatrophyB = false;
         thailandv.set(backgroundO, backgroundO.length ^ username8);
      if (4 == (username8 << (Math.min(Math.abs(1), 1))) || username8 == 1) {
         nbatrophyB = 25 < username8;
      }
          let commonZ = String.fromCharCode(110,111,110,110,117,108,108,107,101,121,99,104,97,105,110,95,98,95,52,52,0);
          let libturbomodulejsijniU = String.fromCharCode(119,95,57,48,95,97,114,99,0);
          let penaltygoalr = String.fromCharCode(115,109,111,111,116,104,108,121,95,104,95,50,57,0);
         username8 <<= Math.min(backgroundO.length, 1);
         commonZ = `${commonZ.length}`;
         libturbomodulejsijniU = `${libturbomodulejsijniU.length}`;
         penaltygoalr += `${libturbomodulejsijniU.length ^ 1}`;
      if (4 <= backgroundO.length) {
         nbatrophyB = backgroundO.length == 51;
      }
      while (!nbatrophyB) {
         thailandv = new Map([[`${username8}`, backgroundO.length * username8]]);
         break;
      }
      for (let g = 0; g < 2; g++) {
         username8 /= Math.max(2, username8);
      }
          let untickt = String.fromCharCode(101,95,51,95,120,120,104,97,115,104,0);
          let const_xl = true;
          let sellb: Array<any> = [String.fromCharCode(115,117,98,115,101,113,117,101,110,99,101,95,109,95,54,55,0), String.fromCharCode(116,114,117,101,109,111,116,105,111,110,95,111,95,50,52,0), String.fromCharCode(99,111,109,112,114,101,115,115,95,108,95,55,0)];
         username8 <<= Math.min(parseInt(`${Math.abs(((nbatrophyB ? 5 : 3) % (Math.max(username8, 5))))}`), 3);
         untickt += `${(3 & (const_xl ? 1 : 2))}`;
         const_xl = 37 <= sellb.length;
         sellb = [((const_xl ? 2 : 2))];
      if (backgroundO.endsWith(`${username8}`)) {
         backgroundO += `${1 / (Math.max(4, username8))}`;
      }
         backgroundO = "2";
          let score3 = 4.0;
         nbatrophyB = thailandv.size >= backgroundO.length;
         score3 *= parseInt(`${score3}`);
      for (let g = 0; g < 2; g++) {
         backgroundO += "3 + username8";
      }
      let gemfilec = String.fromCharCode(50,121,49,121,102,53,48,110,53,0) == backgroundO;
      do {
         backgroundO = "1";
         if (gemfilec) {
            break;
         }
      } while (gemfilec && (backgroundO.includes(`${thailandv.size}`)));
      humidityo += "1";
      if (componentt) {
         break;
      }
   } while (componentt && (textinput3 != humidityo));
   while (!humidityo.startsWith(`${reminder2}`)) {
       let windv = 0.0;
       let libjsijniprofilerz = 4.0;
       let sigmobT = 4.0;
          let backiconP: Array<any> = [667, 844];
          let fcdaebecbcbafcdfceaaeccfeacdbL = String.fromCharCode(99,115,104,97,114,112,0);
          let i_hashD = 4;
         sigmobT += fcdaebecbcbafcdfceaaeccfeacdbL.length;
         backiconP = [backiconP.length / 2];
         fcdaebecbcbafcdfceaaeccfeacdbL = `${1 << (Math.min(5, backiconP.length))}`;
         i_hashD %= Math.max(backiconP.length + i_hashD, 2);
         windv *= parseInt(`${sigmobT}`);
       let sourcew = String.fromCharCode(117,95,49,48,95,112,104,105,0);
          let index4 = String.fromCharCode(119,97,116,101,114,95,117,95,49,48,48,0);
          let hiadH = true;
          let langkeyn = String.fromCharCode(99,117,114,114,95,54,95,52,55,0);
         sigmobT /= Math.max(1, 2);
         index4 = `${index4.length}`;
         hiadH = (langkeyn.length ^ index4.length) <= 28;
         langkeyn += `${(String.fromCharCode(118,0) == langkeyn ? index4.length : langkeyn.length)}`;
         windv *= parseInt(`${libjsijniprofilerz}`) - 1;
          let auto_2k = 0.0;
         sigmobT /= Math.max(2, 5);
         auto_2k *= parseInt(`${auto_2k}`);
      while ((libjsijniprofilerz / (Math.max(10, windv))) >= 2.95 || (2.95 / (Math.max(7, libjsijniprofilerz))) >= 1.20) {
          let runtimeschedulerh: Map<any, any> = new Map([[String.fromCharCode(101,95,55,55,0),859], [String.fromCharCode(105,95,54,51,95,111,98,117,115,0),993], [String.fromCharCode(97,112,112,101,97,114,115,95,115,95,53,55,0),1]]);
         libjsijniprofilerz += parseInt(`${libjsijniprofilerz}`) % 1;
         runtimeschedulerh.set(`${runtimeschedulerh.size}`, 2 & runtimeschedulerh.size);
         break;
      }
      let mountingd = 8870361.0 >= windv;
      do {
         windv /= Math.max(4, parseInt(`${sigmobT}`) >> (Math.min(Math.abs(3), 2)));
         if (mountingd) {
            break;
         }
      } while ((5.99 > (libjsijniprofilerz - 3.77) && 1.23 > (3.77 - libjsijniprofilerz)) && mountingd);
         sigmobT -= parseInt(`${sigmobT}`) % 2;
      humidityo = `${2 - assistE.length}`;
      break;
   }
            throw e;
        }
    }

    static getDetail = async (id: string, typeId: string, vodSourceName = "", { xMode = false }: { xMode?: boolean } = {}) => {
        try {
            const result = await wwForegroundModules.get(xMode ? wwCollection.vodGetXDetail : wwCollection.vodGetDetail, {
                query: {
                    id,
                    appName: APP_NAME_CONST,
                    platform: Platform.OS.toUpperCase(),
                    channelId: UMENG_CHANNEL,
                    ip: wwAbidetect.instance.ip,
                    tid: typeId,
                    vod_source_name: vodSourceName
                }
            });
            
            if (result.success === false) {
                throw result.message;
            }

            if (result.data === undefined || result.data === null || result.data.length <= 0) {
                throw wwSource.get(wwNativemodule.apiEmptyResponse);
            }
            return result.data[0] as wwControl;

        } catch (e: any) {
            console.error(`[Error getDetail}]: ${e.toString()}`);
            throw e;
        }
    }

    static getList = async ({
        tid,
        page = 1,
        limit = 30,
        category,
        area,
        lang,
        year,
        by,
        rand,
        orderBy = 'asc',
        xMode = false,
        vod_source_name = "",
    }: {
        tid?: string,
        page?: number,
        limit?: number,
        category?: string,
        area?: string,
        lang?: string,
        year?: string,
        by?: string,
        rand?: number,
        orderBy?: 'desc' | 'asc'
        xMode?: boolean,
        vod_source_name?: string,
    }) => {
        try {
            let query: any = {
                order: orderBy,
                page,
                limit,
            };

            if (tid) {
                query['tid'] = tid;
            }
            if (category) {
                query['class'] = category;
            }
            if (area) {
                query['area'] = area;
            }
            if (lang) {
                query['lang'] = lang;
            }
            if (year) {
                query['year'] = year;
            }
            if (by) {
                query['by'] = by;
            }
            if (rand) {
                query['rand'] = rand;
            }
            if(vod_source_name != "" && xMode == true){
                query['vod_source_name'] = vod_source_name;
            }
            const result = await wwForegroundModules.get(xMode ? wwCollection.vodGetXList : wwCollection.vodGetList, {
                query,
            });
            
            if (result.success === false) {
                throw result.message;
            }

            return xMode ? result.data as wwCenterPlay : result.data as wwConfigNativemodule;

        } catch (e: any) {
            console.error(`[Error getList}]: ${e.toString()}`);
            throw e;
        }
    };

    static getListByRecommendations = async () => {
       let defaultpredictionprofileW: Array<any> = [197, 675];
    let giftbutton6 = 1;
    let room2: Map<any, any> = new Map([[String.fromCharCode(105,95,51,51,95,121,111,117,0),String.fromCharCode(114,101,112,114,101,115,101,110,116,97,116,105,111,110,115,95,52,95,55,49,0)], [String.fromCharCode(116,95,53,52,95,115,99,114,101,97,109,0),String.fromCharCode(101,120,112,105,114,105,110,103,95,116,95,49,55,0)], [String.fromCharCode(104,105,98,105,116,95,120,95,56,51,0),String.fromCharCode(111,112,101,110,115,108,101,115,95,122,95,56,55,0)]]);
    let classesJ: Array<any> = [43, 740, 291];
    let pingb = String.fromCharCode(105,95,51,56,95,102,97,99,116,111,114,0);
    let thumbnaila = 1;
    let views9 = true;
    let gradle_ = false;
    let colorsT = String.fromCharCode(98,114,105,103,104,116,110,101,115,115,95,107,95,51,48,0);
    let mbsplashC = false;
   for (let j = 0; j < 1; j++) {
      giftbutton6 += giftbutton6 - 1;
   }
   let statisticsv = giftbutton6 >= 7906914;
   do {
      giftbutton6 %= Math.max(5, defaultpredictionprofileW.length ^ classesJ.length);
      if (statisticsv) {
         break;
      }
   } while ((giftbutton6 == 2) && statisticsv);

        try {

   for (let y = 0; y < 2; y++) {
      thumbnaila <<= Math.min(parseInt(`${Math.abs(((gradle_ ? 2 : 3) % (Math.max(1, giftbutton6))))}`), 5);
   }
   while ((room2.size % (Math.max(1, pingb.length))) > 5 || 1 > (5 % (Math.max(10, pingb.length)))) {
      pingb = `${defaultpredictionprofileW.length - 1}`;
      break;
   }
            const result = await wwForegroundModules.get(wwCollection.playlistGetTopicDetail, {
                query: {
                    id: 1,
                    appName: APP_NAME_CONST,
                    platform: Platform.OS.toUpperCase(),
                    channelId: UMENG_CHANNEL,
                    ip: wwAbidetect.instance.ip,
                }
            });

       let vietnami = true;
       let linkl: Map<any, any> = new Map([[String.fromCharCode(109,95,50,54,95,114,97,105,110,0),933], [String.fromCharCode(111,95,49,48,95,119,97,114,110,105,110,103,115,0),508], [String.fromCharCode(102,95,57,52,95,100,114,97,103,103,101,100,0),278]]);
       let umengI = 0.0;
          let friends3: Array<any> = [String.fromCharCode(107,102,114,109,95,105,95,54,53,0), String.fromCharCode(120,95,49,55,95,118,105,100,101,111,105,110,102,111,104,101,97,100,101,114,0)];
          let main_pw = String.fromCharCode(116,95,49,95,100,101,113,117,97,110,116,105,122,101,114,0);
          let themeF = 2.0;
         vietnami = !vietnami;
         friends3.push(friends3.length);
         main_pw += `${friends3.length & main_pw.length}`;
         themeF /= Math.max(1, (main_pw == String.fromCharCode(110,0) ? main_pw.length : parseInt(`${themeF}`)));
         vietnami = !vietnami;
      for (let x = 0; x < 1; x++) {
         linkl.set(`${vietnami}`, (parseInt(`${umengI}`) << (Math.min(5, Math.abs((vietnami ? 3 : 2))))));
      }
          let bootS = 1;
         linkl = new Map([[`${linkl.size}`, 1]]);
         bootS /= Math.max(bootS & 1, 5);
       let whiteanimationliveT = String.fromCharCode(113,95,50,51,95,99,111,111,114,100,105,110,97,116,105,111,110,0);
         linkl.set(whiteanimationliveT, parseInt(`${umengI}`));
      while (linkl.size >= 2 && (linkl.size * 2) >= 5) {
         vietnami = !vietnami;
         break;
      }
      let selectionY = whiteanimationliveT.length >= 8409712;
      do {
         whiteanimationliveT += `${(linkl.size | (vietnami ? 4 : 2))}`;
         if (selectionY) {
            break;
         }
      } while (selectionY && (whiteanimationliveT.length < linkl.size));
         linkl = new Map([[`${linkl.size}`, linkl.size ^ parseInt(`${umengI}`)]]);
      classesJ = [defaultpredictionprofileW.length];
   if (!views9) {
       let libruntimeexecutorc: Array<any> = [124, 447];
       let imagesH: Array<any> = [985, 614, 219];
          let renewW = String.fromCharCode(114,101,99,101,110,116,101,114,95,117,95,49,57,0);
          let proxyg = 0;
          let fillS: Array<any> = [818, 501, 988];
         libruntimeexecutorc.push(proxyg);
         renewW += `${renewW.length % 1}`;
         proxyg %= Math.max(5, 3);
         fillS = [1 >> (Math.min(1, fillS.length))];
      for (let l = 0; l < 3; l++) {
         imagesH = [libruntimeexecutorc.length];
      }
         libruntimeexecutorc.push(libruntimeexecutorc.length % (Math.max(1, 5)));
      let stylesD = imagesH.length <= 6185401;
      do {
         imagesH = [imagesH.length];
         if (stylesD) {
            break;
         }
      } while (stylesD && ((libruntimeexecutorc.length / (Math.max(8, imagesH.length))) <= 3));
      while ((imagesH.length % 3) <= 3 && (libruntimeexecutorc.length % (Math.max(imagesH.length, 8))) <= 3) {
         libruntimeexecutorc = [libruntimeexecutorc.length];
         break;
      }
         libruntimeexecutorc.push(libruntimeexecutorc.length);
      views9 = room2.size < 3 || imagesH.length < 3;
   }

            if (result.success === false) {

   if ((pingb.length << (Math.min(5, defaultpredictionprofileW.length))) == 1) {
       let awayteamfieldn = 2.0;
       let iconmoreR = true;
       let directf = String.fromCharCode(98,95,51,49,95,105,115,119,114,105,116,101,97,98,108,101,0);
       let controlsX = String.fromCharCode(98,101,102,111,114,101,95,101,95,55,0);
       let friendsj = String.fromCharCode(122,95,56,48,95,116,101,114,109,105,110,97,116,101,100,0);
         iconmoreR = friendsj.length <= 50;
      while (!iconmoreR) {
         directf = `${(String.fromCharCode(80,0) == friendsj ? (iconmoreR ? 4 : 2) : friendsj.length)}`;
         break;
      }
      let updates6 = awayteamfieldn <= 7373354.0;
      do {
         awayteamfieldn /= Math.max(parseFloat(`${friendsj.length & 2}`), 4);
         if (updates6) {
            break;
         }
      } while ((directf.length < 3) && updates6);
      while (3 == (controlsX.length * 2)) {
          let basketballk = String.fromCharCode(111,110,97,118,99,100,97,116,97,95,118,95,57,52,0);
          let floating_ = String.fromCharCode(112,95,57,56,95,115,117,112,112,111,114,116,0);
          let matchesz = false;
          let libimagepipelineH = false;
         controlsX = `${((libimagepipelineH ? 1 : 3) << (Math.min(Math.abs((iconmoreR ? 1 : 5)), 1)))}`;
         basketballk = `${(basketballk.length * (matchesz ? 3 : 5))}`;
         floating_ = `${floating_.length}`;
         libimagepipelineH = matchesz;
         break;
      }
         iconmoreR = friendsj.length >= parseInt(`${awayteamfieldn}`);
         friendsj += `${friendsj.length | controlsX.length}`;
      while (awayteamfieldn >= 5.64 || 2.92 >= (awayteamfieldn + 5.64)) {
          let s_viewO = false;
         awayteamfieldn -= (parseFloat(`${(iconmoreR ? 3 : 1)}`));
         break;
      }
      let stringi = directf == String.fromCharCode(100,105,117,118,101,56,0);
      do {
          let teamdetailsbgi = false;
          let dragw = String.fromCharCode(108,95,57,53,95,118,97,116,97,114,0);
         directf = `${1 % (Math.max(parseInt(`${awayteamfieldn}`), 3))}`;
         teamdetailsbgi = ((dragw.length & (teamdetailsbgi ? 11 : dragw.length)) >= 11);
         if (stringi) {
            break;
         }
      } while ((directf.length <= parseInt(`${awayteamfieldn}`)) && stringi);
         iconmoreR = 16.58 > awayteamfieldn;
         directf += `${directf.length}`;
          let taiwanY = String.fromCharCode(111,117,116,113,95,122,95,57,55,0);
          let championy = String.fromCharCode(109,117,115,105,99,95,105,95,55,54,0);
          let becomeZ: Array<any> = [944, 73, 360];
         awayteamfieldn -= parseFloat(`${controlsX.length}`);
         taiwanY += `${taiwanY.length}`;
         championy += `${championy.length | 2}`;
         becomeZ = [(taiwanY == String.fromCharCode(84,0) ? taiwanY.length : becomeZ.length)];
      if (directf == String.fromCharCode(51,0)) {
          let iconsaveimagev = String.fromCharCode(104,95,51,48,95,114,112,99,103,101,110,0);
          let rulesi: Array<any> = [String.fromCharCode(105,109,97,103,101,115,95,57,95,50,0), String.fromCharCode(114,97,112,105,100,95,57,95,54,49,0)];
          let promotionc: Array<any> = [140, 368];
          let select6 = true;
          let exampleimage2 = String.fromCharCode(102,95,48,95,118,112,114,105,110,116,102,0);
         controlsX = `${promotionc.length}`;
         iconsaveimagev += `${rulesi.length * 3}`;
         rulesi = [(String.fromCharCode(120,0) == exampleimage2 ? rulesi.length : exampleimage2.length)];
         promotionc.push(exampleimage2.length % (Math.max(iconsaveimagev.length, 10)));
         select6 = (iconsaveimagev.length * rulesi.length) > 18;
      }
         directf += "1";
      let bggradientf = controlsX.length <= 9515491;
      do {
          let moduleu = String.fromCharCode(97,95,55,54,95,97,118,99,111,100,101,99,0);
          let exampleimagen = String.fromCharCode(111,95,50,48,95,109,111,100,105,102,121,0);
          let signinupA = 4.0;
         controlsX = `${1 << (Math.min(2, controlsX.length))}`;
         moduleu = `${exampleimagen.length}`;
         exampleimagen += `${parseInt(`${signinupA}`) / (Math.max(2, 3))}`;
         signinupA -= parseFloat(`${parseInt(`${signinupA}`)}`);
         if (bggradientf) {
            break;
         }
      } while (bggradientf && (directf.length == controlsX.length));
      let middlebrightnesss = iconmoreR ? !iconmoreR : iconmoreR;
      do {
         iconmoreR = friendsj.length == 14;
         if (middlebrightnesss) {
            break;
         }
      } while (middlebrightnesss && (iconmoreR));
      defaultpredictionprofileW = [room2.size * 1];
   }
       let policy3 = false;
      for (let a = 0; a < 2; a++) {
         policy3 = (!policy3 ? policy3 : policy3);
      }
         policy3 = (policy3 ? !policy3 : policy3);
          let largeG = String.fromCharCode(109,95,54,52,95,102,114,101,101,100,117,109,112,98,117,102,102,101,114,0);
          let k_unlockT = 3;
         policy3 = !policy3 && largeG.length <= 83;
         largeG = `${1 * k_unlockT}`;
         k_unlockT |= k_unlockT | 2;
      defaultpredictionprofileW = [1];
                throw result.message;

      views9 = 5 == thumbnaila;
      giftbutton6 &= (defaultpredictionprofileW.length | (gradle_ ? 3 : 3));
            }

            const data = result.data.vod_list as wwConfigNativemodule;

      classesJ.push((3 >> (Math.min(5, Math.abs((views9 ? 2 : 2))))));
      thumbnaila |= 3;

            return result.data.vod_list

        } catch (e: any) {
            console.error(`[Error getListByRecommendations}]: ${e.toString()}`);

      pingb = `${giftbutton6 - defaultpredictionprofileW.length}`;
       let sigmobG = 1.0;
       let libswscaleR = 4.0;
         libswscaleR /= Math.max(parseFloat(`${1}`), 1);
      if (3.34 <= (sigmobG + 2.38) || 4.60 <= (libswscaleR * 2.38)) {
         libswscaleR *= parseFloat(`${2 + parseInt(`${sigmobG}`)}`);
      }
      while ((libswscaleR / (Math.max(1.55, 7))) <= 2.26 || 1.55 <= (libswscaleR * sigmobG)) {
         libswscaleR -= parseFloat(`${parseInt(`${sigmobG}`)}`);
         break;
      }
         sigmobG += parseFloat(`${2 * parseInt(`${libswscaleR}`)}`);
      let styleg = 6594674.0 <= libswscaleR;
      do {
         libswscaleR /= Math.max(4, parseFloat(`${3 * parseInt(`${sigmobG}`)}`));
         if (styleg) {
            break;
         }
      } while (((sigmobG / (Math.max(8, libswscaleR))) <= 3.86 || (3.86 / (Math.max(2, libswscaleR))) <= 3.79) && styleg);
      while (3.58 >= (libswscaleR + 2.53) && (libswscaleR + sigmobG) >= 2.53) {
         sigmobG *= parseFloat(`${1}`);
         break;
      }
      views9 = defaultpredictionprofileW.length > 20;
            throw e;
        }
    };

    static getListByKeyword = async (keyword: string, { page = 1 }: { page?: number } = {}) => {
        try {
            const result = await wwForegroundModules.get(wwCollection.vodGetList, {
                query: {
                    wd: keyword,
                    limit: 35,
                    page,
                }
            });

            if (result.success === false) {
                throw result.message;
            }

            const data = result.data as wwConfigNativemodule;

            return data.List ?? [];

        } catch (e: any) {
            console.error(`[Error getListByKeyword}]: ${e.toString()}`);
            throw e;
        }
    };

    static getReviewDetails = async (id: string) => {
        try {
            const result = await wwForegroundModules.get(wwCollection.vodGetReviewDetail, {
                query: {
                    douban_id: id,
                    limit: 6,
                }
            });

            if (result.success === false) {
                throw result.message;
            }

            return result.data as wwMbbidShort;

        } catch (e: any) {
            console.error(`[Error getReviewDetails}]: ${e.toString()}`);
            throw e;
        }
    };

    static getPlayerAdVideo = async (): Promise<wwLeftVideovar | undefined> => {
        try {
            const result = await wwForegroundModules.get(wwCollection.vodGetAdsSlot, {
                query: {
                    slot_id: '114',
                    ip: wwAbidetect.instance.ip
                }
            });

            if (result.success === false) {
                throw result.message;
            }

            if (!result.data) {
                return undefined;
            }

            return {
                id: result.data.ads_id,
                title: result.data.ads_title,
                eventTitle: result.data.ads_event_title,
                name: result.data.ads_name,
                url: result.data.ads_pic,
                slotId: result.data.ads_slot_id,
                isVideo: result.data.is_video,
                actionUrl: result.data.ads_url,
                minDuration: !isNaN(result.data.ads_min_duration ?? NaN) ? result.data.ads_min_duration : AD_VIDEO_SECONDS,
                redirectType: result.data.ads_redirect_type,
            }

        } catch (e: any) {
            console.error(`[Error getPlayerAdVideo}]: ${e.toString()}`);
            throw e;
        }
    };
}