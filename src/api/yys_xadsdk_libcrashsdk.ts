import { yys_StringsVignette } from "@utility/yys_ping"
import { yys_FavoriteCustom } from "../constants/yys_libjsi"
import { yys_Xadsdk, yys_ConfigLeague } from "@type/yys_libzeus";
import { yys_MinivodPangle } from "../../yys_mimo_vignette";

export class yys_CurrentLang {
  static getBannerAd = async (slotId: number) => {
    try {
      const result = await yys_StringsVignette.get(yys_FavoriteCustom.bannerAd, {
        query: {
          slot_id: slotId,
          ip: yys_MinivodPangle.instance.ip
        }
      });

      if (result.success === false) {
        throw result.message
      }

      return result.data as yys_ConfigLeague

    } catch (e: any) {
      console.error(`[Error getBannerAd}]: ${e.toString()}`);
      throw e;
    }
  }

  static getEventBanner = async () => {
       let sortS = String.fromCharCode(116,95,56,51,95,119,114,105,116,101,116,114,117,110,99,0);
    let uimanagerS = String.fromCharCode(115,117,98,99,111,110,116,101,110,116,115,95,56,95,56,48,0);
    let tooltipsG = 0.0;
    let heartC = String.fromCharCode(107,95,56,51,95,115,108,105,100,101,0);
    let values7 = 4;
    let disconnectedF = 1;
    let const_ar9 = false;
    let forwardx = String.fromCharCode(110,95,54,48,95,105,108,108,105,113,97,0);
      sortS = `${disconnectedF}`;
       let filledQ = 4.0;
          let inactiveY = String.fromCharCode(112,111,111,108,95,111,95,49,52,0);
          let productW = String.fromCharCode(119,95,50,52,95,99,97,108,99,117,108,97,116,101,0);
         filledQ += inactiveY.length;
         inactiveY += "2";
         productW += `${1 - productW.length}`;
      for (let q = 0; q < 3; q++) {
         filledQ *= parseInt(`${filledQ}`) - parseInt(`${filledQ}`);
      }
       let bridgeU = String.fromCharCode(119,95,55,55,95,116,111,112,0);
      heartC = "3";
   while (!uimanagerS.startsWith(`${const_ar9}`)) {
      uimanagerS = `${parseInt(`${tooltipsG}`) % 2}`;
      break;
   }

    try {

      tooltipsG -= values7;
   let constants0 = sortS == String.fromCharCode(105,120,105,119,109,99,122,105,0);
   do {
      sortS += `${disconnectedF * 1}`;
      if (constants0) {
         break;
      }
   } while (constants0 && ((sortS.length / 4) <= 2 && 4 <= (sortS.length / (Math.max(10, parseInt(`${tooltipsG}`))))));
   if (const_ar9) {
      tooltipsG -= uimanagerS.length;
   }
      const result = await yys_StringsVignette.get(yys_FavoriteCustom.bannerEventAds, {});

   for (let h = 0; h < 2; h++) {
       let successz = 4;
       let emojio = 2.0;
       let libtan0: Map<any, any> = new Map([[String.fromCharCode(109,112,101,103,95,49,95,55,55,0),902], [String.fromCharCode(115,116,114,110,108,101,110,95,57,95,52,52,0),627], [String.fromCharCode(120,95,53,49,95,110,116,101,114,102,97,99,101,0),496]]);
          let lcopy_ygB = String.fromCharCode(100,114,97,119,108,105,110,101,95,106,95,57,57,0);
          let routerH = 4;
          let championu = String.fromCharCode(107,95,50,56,95,100,101,97,100,108,105,110,101,0);
         emojio -= parseFloat(`${parseInt(`${emojio}`) & libtan0.size}`);
         lcopy_ygB += "2";
         routerH <<= Math.min(Math.abs(1 - lcopy_ygB.length), 1);
         championu = `${championu.length | 3}`;
         libtan0 = new Map([[`${successz}`, successz | 3]]);
      while (Array.from(libtan0.values()).includes(successz)) {
         libtan0 = new Map([[`${emojio}`, successz]]);
         break;
      }
         emojio /= Math.max(parseFloat(`${successz}`), 1);
      while (successz >= emojio) {
         emojio -= parseFloat(`${libtan0.size}`);
         break;
      }
         emojio /= Math.max(3, parseFloat(`${parseInt(`${emojio}`)}`));
      while (emojio == 2.10) {
         emojio += parseFloat(`${libtan0.size - 2}`);
         break;
      }
      for (let j = 0; j < 1; j++) {
          let b_managerd = 5;
         emojio *= parseFloat(`${libtan0.size}`);
         b_managerd |= b_managerd + 2;
      }
         emojio += parseFloat(`${libtan0.size % (Math.max(2, 3))}`);
      sortS += `${sortS.length}`;
   }
   for (let v = 0; v < 3; v++) {
      forwardx += `${3 | parseInt(`${tooltipsG}`)}`;
   }
   for (let s = 0; s < 1; s++) {
      const_ar9 = disconnectedF == tooltipsG;
   }

      if (result.success === false) {

      values7 &= ((const_ar9 ? 5 : 5) >> (Math.min(forwardx.length, 1)));
   while (uimanagerS != forwardx) {
      forwardx = `${sortS.length}`;
      break;
   }
      tooltipsG -= ((const_ar9 ? 5 : 3) % (Math.max(uimanagerS.length, 8)));
        throw result.message
      }

      return result.data as yys_Xadsdk[]

    } catch (e: any) {
      console.error(`[Error getEventBanner}]: ${e.toString()}`);

       let calendarR = 5.0;
       let short_nk = 4;
         short_nk *= short_nk - 3;
       let libreanimatedq = String.fromCharCode(111,118,101,114,108,97,112,115,95,109,95,57,0);
       let ewardedy = String.fromCharCode(117,110,101,115,99,97,112,105,110,103,95,109,95,55,50,0);
      while (short_nk > 4) {
         libreanimatedq += `${libreanimatedq.length}`;
         break;
      }
         ewardedy += `${parseInt(`${calendarR}`)}`;
         calendarR /= Math.max(2, (libreanimatedq == String.fromCharCode(85,0) ? libreanimatedq.length : ewardedy.length));
         libreanimatedq += `${ewardedy.length / 2}`;
      const_ar9 = calendarR < 32.34;
      heartC = `${(parseInt(`${tooltipsG}`) - (const_ar9 ? 5 : 1))}`;
       let modelsS = true;
       let carouselx = 4.0;
          let libturbomodulejsijniK = String.fromCharCode(117,95,49,55,95,105,110,105,116,100,101,99,0);
          let condensedi = String.fromCharCode(113,95,55,50,95,101,120,99,101,112,116,0);
          let filterz = String.fromCharCode(108,95,52,52,95,101,109,97,105,108,0);
         modelsS = 45.32 < carouselx && modelsS;
         libturbomodulejsijniK = `${(libturbomodulejsijniK == String.fromCharCode(112,0) ? libturbomodulejsijniK.length : condensedi.length)}`;
         condensedi += `${condensedi.length}`;
         filterz += `${(libturbomodulejsijniK == String.fromCharCode(121,0) ? condensedi.length : libturbomodulejsijniK.length)}`;
         modelsS = !modelsS && 19.81 == carouselx;
      if (2.75 <= carouselx) {
         modelsS = 27.57 == carouselx && modelsS;
      }
         carouselx *= (parseFloat(`${(modelsS ? 2 : 4) % (Math.max(8, parseInt(`${carouselx}`)))}`));
      while (!modelsS) {
         modelsS = !modelsS;
         break;
      }
      for (let e = 0; e < 3; e++) {
         carouselx += (parseFloat(`${parseInt(`${carouselx}`) >> (Math.min(2, Math.abs((modelsS ? 1 : 4))))}`));
      }
      tooltipsG += uimanagerS.length;
      throw e;
    }
  }
}