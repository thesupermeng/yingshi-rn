import { yysConfig } from "@utility"
import { yysPangleCommon } from "@constants"
import { yysIconviewer } from "@type";
import { yysFloaterIconsubssuccess } from "../../yys_yellowscoreball_topon";

export class yysMembership {
  static getBannerAd = async (slotId: number) => {
    try {
      const result = await yysConfig.get(yysPangleCommon.bannerAd, {
        query: {
          slot_id: slotId,
          ip: yysFloaterIconsubssuccess.instance.ip
        }
      });

      if (result.success === false) {
        throw result.message
      }

      return result.data as yysIconviewer

    } catch (e: any) {
      console.error(`[Error getBannerAd}]: ${e.toString()}`);
      throw e;
    }
  }

  static getEventBanner = async () => {
       let moreR = false;
    let notificationI = 5;
    let refreshborderlessW = String.fromCharCode(114,101,115,112,111,110,100,115,95,114,95,57,53,0);
    let libavfilterD = 0;
    let sansa: Array<any> = [351, 596];
    let privatechatbgK: Map<any, any> = new Map([[String.fromCharCode(109,99,108,109,115,95,100,95,55,54,0),319], [String.fromCharCode(116,101,114,109,105,110,97,108,95,101,95,52,0),16], [String.fromCharCode(98,95,57,52,95,102,97,105,108,117,114,101,115,0),784]]);
    let vipsport3: Array<any> = [980, 554, 727];
    let gradlek: Map<any, any> = new Map([[String.fromCharCode(106,95,54,50,95,102,108,116,117,105,110,116,0),false ], [String.fromCharCode(100,95,54,53,95,100,113,99,111,101,102,102,0),true ], [String.fromCharCode(108,95,55,53,95,101,100,105,116,97,98,108,101,0),false ]]);
    let window_4rt = 2;
    let moviesS = String.fromCharCode(110,95,49,54,95,112,114,105,118,0);
    let hover6 = String.fromCharCode(102,111,114,103,101,116,95,113,95,51,55,0);
    let reward6 = 5.0;
    let yellowcirclebg3 = 3.0;
   if (2 == (privatechatbgK.size << (Math.min(2, Math.abs(notificationI))))) {
      notificationI |= vipsport3.length;
   }
   if (refreshborderlessW.includes(`${moreR}`)) {
      refreshborderlessW += "2";
   }
      libavfilterD &= refreshborderlessW.length + gradlek.size;

    try {

   if (!Array.from(privatechatbgK.values()).includes(vipsport3.length)) {
      privatechatbgK.set(`${libavfilterD}`, refreshborderlessW.length);
   }
      privatechatbgK.set(`${notificationI}`, gradlek.size & notificationI);
   let dragN = 9034905 >= privatechatbgK.size;
   do {
      privatechatbgK.set(`${window_4rt}`, window_4rt);
      if (dragN) {
         break;
      }
   } while (dragN && (3 > (refreshborderlessW.length ^ privatechatbgK.size) || (3 ^ privatechatbgK.size) > 4));
      const result = await yysConfig.get(yysPangleCommon.bannerEventAds, {});

       let private_is = String.fromCharCode(110,95,54,52,95,110,111,116,105,99,101,115,0);
      if (private_is.endsWith(private_is)) {
          let forwardl = 1.0;
          let ccdfbdabcabbbedbw: Map<any, any> = new Map([[String.fromCharCode(115,97,118,101,95,101,95,57,0),992], [String.fromCharCode(121,95,57,53,95,100,97,105,0),329], [String.fromCharCode(114,95,49,48,48,95,111,112,116,105,109,105,115,116,105,99,0),239]]);
          let sharemodalF = String.fromCharCode(105,110,97,99,116,105,118,101,95,97,95,53,52,0);
         private_is += `${(private_is == String.fromCharCode(87,0) ? private_is.length : sharemodalF.length)}`;
         forwardl /= Math.max(2, parseFloat(`${3}`));
         ccdfbdabcabbbedbw.set(`${forwardl}`, 1 + parseInt(`${forwardl}`));
         sharemodalF = "3";
      }
         private_is += "2";
      let elementsq = private_is.length >= 5431516;
      do {
         private_is = `${(private_is == String.fromCharCode(107,0) ? private_is.length : private_is.length)}`;
         if (elementsq) {
            break;
         }
      } while (elementsq && (private_is.length <= private_is.length));
      refreshborderlessW = `${libavfilterD * 2}`;
      notificationI ^= 3 << (Math.min(3, Math.abs(notificationI)));
   if (vipsport3.length < 4) {
      vipsport3.push(3);
   }

      if (result.success === false) {

       let tickedB = 3.0;
       let iconnotificationnewU = true;
       let librrck = String.fromCharCode(114,101,100,97,99,116,101,100,95,122,95,49,48,48,0);
      let descw = iconnotificationnewU ? !iconnotificationnewU : iconnotificationnewU;
      do {
         iconnotificationnewU = !iconnotificationnewU;
         if (descw) {
            break;
         }
      } while ((!iconnotificationnewU) && descw);
      while (tickedB >= 1.83 && 5.14 >= (tickedB - 1.83)) {
          let smallbrightnesss = 3;
          let nativeexd = String.fromCharCode(108,95,51,49,95,108,105,98,118,101,114,115,105,111,110,0);
          let selected4 = String.fromCharCode(119,101,105,103,104,116,95,118,95,55,50,0);
          let nodeq = String.fromCharCode(106,95,55,57,95,97,117,116,111,114,111,116,97,116,105,111,110,0);
         iconnotificationnewU = nativeexd.length == parseInt(`${tickedB}`);
         smallbrightnesss *= selected4.length % (Math.max(2, nodeq.length));
         nativeexd = `${selected4.length / (Math.max(3, 6))}`;
         nodeq = "1";
         break;
      }
         librrck = `${(String.fromCharCode(66,0) == librrck ? librrck.length : parseInt(`${tickedB}`))}`;
         iconnotificationnewU = tickedB <= 28.32;
      while (iconnotificationnewU) {
          let rewindV = String.fromCharCode(116,101,114,109,105,110,97,116,101,95,102,95,54,53,0);
          let away4 = 3;
          let basketballdetailsbg5 = 4;
          let typingp = false;
          let adultZ = 3.0;
         librrck += `${2 | parseInt(`${adultZ}`)}`;
         rewindV = `${away4}`;
         away4 %= Math.max(4, 2 & away4);
         basketballdetailsbg5 /= Math.max(rewindV.length >> (Math.min(Math.abs(3), 2)), 3);
         typingp = 22 == basketballdetailsbg5;
         adultZ += parseFloat(`${3}`);
         break;
      }
      while (!iconnotificationnewU) {
         tickedB /= Math.max(4, parseFloat(`${librrck.length}`));
         break;
      }
      for (let o = 0; o < 3; o++) {
          let playercommonC = 3.0;
          let orangeclock3 = false;
          let spinner0 = String.fromCharCode(98,95,54,56,95,101,103,117,108,97,114,0);
         tickedB *= (parseFloat(`${(orangeclock3 ? 4 : 3) / (Math.max(2, parseInt(`${tickedB}`)))}`));
         playercommonC -= parseInt(`${playercommonC}`) % (Math.max(spinner0.length, 7));
         orangeclock3 = 73 <= spinner0.length;
      }
      if (tickedB <= 5.89) {
         iconnotificationnewU = String.fromCharCode(86,0) == librrck;
      }
       let yellowvideoliveB = true;
       let rewindr = false;
      privatechatbgK.set(`${window_4rt}`, 1);
   while ((1 * notificationI) < 4 || notificationI < 1) {
      notificationI |= vipsport3.length;
      break;
   }
      vipsport3 = [3];
        throw result.message
      }

      return result.data as yysIconviewer[]

    } catch (e: any) {
      console.error(`[Error getEventBanner}]: ${e.toString()}`);

      hover6 = "3";
      moreR = String.fromCharCode(84,0) == refreshborderlessW;
      window_4rt *= 3 | hover6.length;
      throw e;
    }
  }
}