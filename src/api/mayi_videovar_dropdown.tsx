import { mayi_Iconclosewhitewithbg, mayi_Turndown } from "@constants";
import { mayi_ReactBang } from "@utility/mayi_zhubo";
import { APP_NAME_CONST, APP_VERSION, UMENG_CHANNEL } from "@utility/mayi_middleware_apps";
import { mayi_Flipper } from "@utility/mayi_mintegral_buffer";
import { Platform } from "react-native";
import { mayi_Librrc } from "../../mayi_gift";
import { mayi_VideoInfo, mayi_TraminiManifest, mayi_EmojiheartChinasame, mayi_EmptyGradle } from "@type/mayi_green";

export class mayi_Iconclosewhite {
    static getLocalIpAddress = async () => {
       let libnmsY = String.fromCharCode(107,95,56,53,95,116,119,111,115,99,97,108,101,0);
    let turndown1 = String.fromCharCode(115,97,116,117,114,97,116,101,95,53,95,53,56,0);
    let flipperT = 3;
    let homeb: Map<any, any> = new Map([[String.fromCharCode(97,108,101,114,116,95,119,95,55,55,0),201], [String.fromCharCode(97,117,116,111,114,111,116,97,116,105,111,110,95,114,95,52,48,0),64], [String.fromCharCode(98,95,57,52,95,110,117,108,108,105,102,0),753]]);
    let mutedL: Array<any> = [719, 932, 554];
    let away_ = 0.0;
    let holderr: Array<any> = [621, 137, 395];
    let mapbufferr: Map<any, any> = new Map([[String.fromCharCode(97,114,101,116,104,101,114,101,95,108,95,53,48,0),false ], [String.fromCharCode(106,95,55,57,95,97,100,100,0),true ]]);
    let window_j0n: Map<any, any> = new Map([[String.fromCharCode(100,109,117,108,95,118,95,50,56,0),true ], [String.fromCharCode(109,105,110,109,97,120,95,106,95,57,57,0),true ]]);
   while (turndown1 == libnmsY) {
      libnmsY += `${mutedL.length + 1}`;
      break;
   }
   let danger5 = flipperT <= 8467515;
   do {
      flipperT ^= (String.fromCharCode(70,0) == turndown1 ? turndown1.length : mutedL.length);
      if (danger5) {
         break;
      }
   } while (danger5 && (flipperT < mapbufferr.size));

        

      turndown1 = `${(turndown1 == String.fromCharCode(79,0) ? mutedL.length : turndown1.length)}`;
      mapbufferr.set(turndown1, flipperT % (Math.max(turndown1.length, 8)));
        let ipAddress = '219.75.27.16';

       let suggestion3: Array<any> = [110, 862, 433];
         suggestion3 = [suggestion3.length];
      for (let d = 0; d < 3; d++) {
          let chatroombackground3 = String.fromCharCode(112,114,101,115,101,116,115,95,54,95,50,53,0);
          let auto_sef = false;
         suggestion3.push(suggestion3.length);
         chatroombackground3 = `${chatroombackground3.length ^ chatroombackground3.length}`;
         auto_sef = chatroombackground3.length <= chatroombackground3.length;
      }
      while (suggestion3.length == 2) {
          let time_b3 = false;
          let iconviewergifM = 2;
          let graph3: Map<any, any> = new Map([[String.fromCharCode(99,95,51,48,95,112,111,112,117,112,0),70], [String.fromCharCode(117,95,56,51,95,100,101,103,114,97,100,97,116,105,111,110,0),184], [String.fromCharCode(106,95,54,54,95,98,103,109,99,0),680]]);
         suggestion3.push(2);
         time_b3 = iconviewergifM >= 39;
         iconviewergifM ^= iconviewergifM;
         graph3.set(`${iconviewergifM}`, graph3.size);
         break;
      }
      mutedL.push(suggestion3.length);
      turndown1 = `${libnmsY.length ^ mapbufferr.size}`;

        try {

   while (!Array.from(mapbufferr.keys()).includes(`${holderr.length}`)) {
      holderr.push(parseInt(`${away_}`));
      break;
   }
      away_ *= parseFloat(`${3}`);
            const result = await mayi_ReactBang.get(mayi_Iconclosewhitewithbg.appGetLocalIp, {
                isFullUrl: true,
            });

   for (let t = 0; t < 1; t++) {
      mapbufferr = new Map([[`${homeb.size}`, mutedL.length]]);
   }
   let popup2 = flipperT <= 6345741;
   do {
      flipperT %= Math.max(2, 2 - libnmsY.length);
      if (popup2) {
         break;
      }
   } while ((2 < turndown1.length) && popup2);

            if (result.success === false) {

      away_ /= Math.max(2, parseFloat(`${1}`));
      holderr = [parseInt(`${away_}`) * mutedL.length];
                throw result.message;
            }

            if (result.data && result.data.IPv4) {

   while (turndown1.length < homeb.size) {
      turndown1 = "1";
      break;
   }
   let r_managerI = libnmsY == String.fromCharCode(52,48,118,106,49,0);
   do {
      libnmsY += "1";
      if (r_managerI) {
         break;
      }
   } while (r_managerI && (4 == (mapbufferr.size >> (Math.min(libnmsY.length, 3))) && (4 >> (Math.min(2, Math.abs(mapbufferr.size)))) == 1));
                ipAddress = result.data.IPv4;
            }
            return result.data;

        } catch (e: any) {
            console.error(`[Error getLocalIpAddress]: ${e.toString()}`);

   let mbbidl = 7716442 >= holderr.length;
   do {
      holderr.push((String.fromCharCode(71,0) == libnmsY ? mapbufferr.size : libnmsY.length));
      if (mbbidl) {
         break;
      }
   } while ((5 <= (4 / (Math.max(4, window_j0n.size)))) && mbbidl);
   for (let u = 0; u < 3; u++) {
      holderr.push(2);
   }
            mayi_Librrc.instance.setNetworkIp(ipAddress);
        }
        finally {

      mutedL.push(flipperT);
   let windk = 5170234 <= turndown1.length;
   do {
      turndown1 += `${2 * homeb.size}`;
      if (windk) {
         break;
      }
   } while (windk && (libnmsY == turndown1));
            mayi_Librrc.instance.setNetworkIp(ipAddress);
        }
    };

    static getBottomNav = async () => {
       let filedY = true;
    let bgvipxvodH = 0.0;
    let screen3 = String.fromCharCode(104,95,56,53,95,118,111,116,101,114,0);
    let libruntimeexecutorh = 5;
    let libavdevice1 = 5.0;
    let cornerf: Array<any> = [754, 23, 288];
    let clockg: Array<any> = [834, 737, 970];
    let adultn = 3.0;
    let neony = 2;
    let arrowM = String.fromCharCode(106,95,57,55,95,112,101,114,102,111,114,109,97,110,99,101,0);
    let rankw = 1.0;
    let themeH = String.fromCharCode(99,95,54,48,95,117,114,112,111,115,101,0);
    let flyer9: Array<any> = [868, 675];
    let recommendationP = String.fromCharCode(111,95,57,53,95,101,120,99,108,0);
    let helperU = String.fromCharCode(115,104,114,105,110,107,95,56,95,51,49,0);
    let holderz = String.fromCharCode(105,110,116,101,114,99,101,112,116,95,118,95,49,50,0);
      bgvipxvodH *= parseFloat(`${screen3.length | parseInt(`${rankw}`)}`);
      adultn += 3;
      adultn /= Math.max(parseInt(`${bgvipxvodH}`) << (Math.min(5, Math.abs(2))), 4);

        try {

      libruntimeexecutorh &= ((filedY ? 1 : 1) << (Math.min(screen3.length, 3)));
      arrowM += `${2 ^ cornerf.length}`;
      cornerf.push(libruntimeexecutorh % (Math.max(parseInt(`${bgvipxvodH}`), 4)));
            const result = await mayi_ReactBang.get(mayi_Iconclosewhitewithbg.appGetBottomNav, {
                query: {
                    channelId: UMENG_CHANNEL,
                    mobileOS: Platform.OS.toUpperCase(),
                },
            });

       let orientationD = String.fromCharCode(99,95,50,48,95,99,111,100,101,98,108,111,99,107,115,0);
       let nbatrophy9 = String.fromCharCode(121,95,52,53,95,105,115,111,109,0);
       let clearc = 4.0;
      let connectionW = 6081870.0 <= clearc;
      do {
         clearc /= Math.max(parseInt(`${clearc}`) + 3, 5);
         if (connectionW) {
            break;
         }
      } while (connectionW && (clearc <= 1.80));
      let v_lockk = 9350682 >= nbatrophy9.length;
      do {
         nbatrophy9 = `${orientationD.length}`;
         if (v_lockk) {
            break;
         }
      } while ((orientationD != nbatrophy9) && v_lockk);
      let refreshborderlessU = nbatrophy9 == String.fromCharCode(48,97,104,104,0);
      do {
         nbatrophy9 = "2";
         if (refreshborderlessU) {
            break;
         }
      } while (refreshborderlessU && (2.91 == (clearc + 4.5) && (nbatrophy9.length >> (Math.min(Math.abs(4), 5))) == 1));
          let detailY: Map<any, any> = new Map([[String.fromCharCode(116,97,110,95,112,95,52,52,0),582], [String.fromCharCode(115,116,115,122,95,55,95,57,56,0),87]]);
         clearc -= parseInt(`${clearc}`);
         detailY = new Map([[`${detailY.size}`, 3 + detailY.size]]);
       let airbnbstar5: Map<any, any> = new Map([[String.fromCharCode(109,95,57,53,95,116,97,103,97,118,114,0),true ], [String.fromCharCode(101,108,108,105,112,116,105,99,97,108,95,106,95,50,49,0),true ]]);
          let qaagb = 3.0;
          let encryptoru: Array<any> = [128, 74, 646];
          let arrowP = 1;
         airbnbstar5 = new Map([[`${clearc}`, orientationD.length]]);
         qaagb += parseFloat(`${encryptoru.length >> (Math.min(Math.abs(3), 2))}`);
         encryptoru.push(encryptoru.length);
         arrowP %= Math.max(2, parseInt(`${qaagb}`));
      for (let a = 0; a < 2; a++) {
         nbatrophy9 += `${airbnbstar5.size}`;
      }
      let awayteamfieldw = String.fromCharCode(56,117,117,112,111,110,53,49,105,54,0) == orientationD;
      do {
         orientationD += `${parseInt(`${clearc}`)}`;
         if (awayteamfieldw) {
            break;
         }
      } while (((3 ^ orientationD.length) >= 3) && awayteamfieldw);
      let alertS = clearc >= 7369782.0;
      do {
          let floaters = String.fromCharCode(116,95,51,54,95,118,97,114,119,105,100,116,104,0);
          let utilsB: Map<any, any> = new Map([[String.fromCharCode(108,95,56,48,95,105,100,101,110,116,105,102,101,114,0),false ], [String.fromCharCode(116,95,49,57,95,99,114,111,112,0),false ], [String.fromCharCode(116,97,112,116,105,99,95,113,95,50,56,0),true ]]);
          let nativeexh = String.fromCharCode(115,116,114,108,95,120,95,57,48,0);
         clearc *= parseInt(`${clearc}`) >> (Math.min(floaters.length, 2));
         floaters += `${nativeexh.length}`;
         utilsB.set(nativeexh, utilsB.size);
         if (alertS) {
            break;
         }
      } while (alertS && ((5.91 + clearc) >= 4.68 && 5 >= (orientationD.length >> (Math.min(Math.abs(5), 4)))));
      libavdevice1 += cornerf.length + themeH.length;
      recommendationP += `${libruntimeexecutorh}`;
      cornerf = [((filedY ? 4 : 5) - parseInt(`${adultn}`))];

            if (result.success === false) {

      cornerf.push(parseInt(`${bgvipxvodH}`));
      flyer9 = [screen3.length - 2];
   if (!flyer9.includes(libruntimeexecutorh)) {
      flyer9 = [screen3.length ^ 2];
   }
                throw result.message;
            }

            mayi_Librrc.instance.setTabConfig(result.data);

   for (let i = 0; i < 2; i++) {
      recommendationP += `${parseInt(`${adultn}`) % 3}`;
   }
   for (let r = 0; r < 3; r++) {
      adultn *= flyer9.length;
   }
      screen3 += "1";

            return result.data;

        } catch (e: any) {
            console.error(`[Error getBottomNav}]: ${e.toString()}`);

      libavdevice1 *= parseInt(`${adultn}`);
       let overlayv = 5.0;
          let libavutilv = String.fromCharCode(103,101,111,103,114,97,112,104,105,99,97,108,95,104,95,49,56,0);
          let emojih = 3.0;
          let untickZ = 5.0;
         overlayv -= parseFloat(`${libavutilv.length}`);
         libavutilv = `${parseInt(`${untickZ}`)}`;
         emojih -= parseFloat(`${parseInt(`${untickZ}`) - 1}`);
      for (let h = 0; h < 3; h++) {
          let borderlessJ: Map<any, any> = new Map([[String.fromCharCode(116,114,97,118,101,114,115,97,108,95,106,95,55,56,0),95], [String.fromCharCode(116,95,56,54,95,99,111,110,118,115,97,109,112,0),612], [String.fromCharCode(103,95,55,53,95,104,97,115,104,101,115,0),220]]);
         overlayv *= parseFloat(`${borderlessJ.size * 3}`);
      }
         overlayv *= parseFloat(`${parseInt(`${overlayv}`)}`);
      arrowM += `${((filedY ? 4 : 5) << (Math.min(Math.abs(parseInt(`${bgvipxvodH}`)), 5)))}`;
      filedY = parseInt(`${bgvipxvodH}`) > flyer9.length;
            throw e;
        }
    };

    static postLocation = async () => {
       let mergerl = String.fromCharCode(112,114,111,116,101,99,116,111,114,95,54,95,50,54,0);
    let favoritej = true;
    let langA = 1.0;
    let libcxxcomponentsM = String.fromCharCode(117,95,50,52,95,109,117,108,116,105,112,108,121,0);
    let untick1: Array<any> = [867, 169, 372];
    let plusG = 1;
    let iconshare7: Map<any, any> = new Map([[String.fromCharCode(104,95,52,51,95,120,112,114,118,0),918], [String.fromCharCode(99,111,110,118,115,97,109,112,95,99,95,50,57,0),456], [String.fromCharCode(105,105,114,102,105,108,116,101,114,95,48,95,53,49,0),237]]);
    let bgvipxvodZ = String.fromCharCode(122,102,114,101,101,95,116,95,53,54,0);
    let iconarrowleftF = String.fromCharCode(105,95,52,56,95,105,110,100,101,120,101,100,0);
    let mbsplashR: Map<any, any> = new Map([[String.fromCharCode(102,95,50,55,95,115,104,114,105,110,107,0),928], [String.fromCharCode(102,101,119,95,102,95,51,52,0),805]]);
       let panglez: Map<any, any> = new Map([[String.fromCharCode(114,95,52,56,95,104,105,103,104,98,100,0),true ], [String.fromCharCode(102,95,50,55,95,102,95,49,49,0),true ], [String.fromCharCode(115,112,101,114,97,116,111,114,95,106,95,57,53,0),true ]]);
       let topicm = 4.0;
         panglez.set(`${topicm}`, parseInt(`${topicm}`));
      while ((5.70 - topicm) >= 1.71 && (1 + panglez.size) >= 5) {
         panglez.set(`${topicm}`, panglez.size);
         break;
      }
         panglez = new Map([[`${panglez.size}`, parseInt(`${topicm}`) / (Math.max(panglez.size, 2))]]);
         panglez.set(`${topicm}`, panglez.size);
      if (Array.from(panglez.keys()).includes(`${topicm}`)) {
          let cross2 = true;
         panglez.set(`${cross2}`, panglez.size);
      }
      for (let s = 0; s < 2; s++) {
         panglez.set(`${topicm}`, parseInt(`${topicm}`) * 2);
      }
      mergerl += `${parseInt(`${langA}`)}`;
   while (iconarrowleftF != mergerl) {
      mergerl = `${1 + bgvipxvodZ.length}`;
      break;
   }

        try {

   while (iconarrowleftF.length >= 4) {
      iconarrowleftF += "2";
      break;
   }
      iconshare7 = new Map([[`${untick1.length}`, 2 & untick1.length]]);
            const result = await mayi_ReactBang.post(mayi_Iconclosewhitewithbg.appPostLocation, {
                body: {
                    ip_address: mayi_Librrc.instance.ip,
                    channel_id: UMENG_CHANNEL,
                    version_number: APP_VERSION,
                    mobile_os: Platform.OS,
                    product: APP_NAME_CONST + "-" + Platform.OS.toUpperCase(),
                    mobile_model: "HUAWEIP20",
                    
                },
            });

   if (bgvipxvodZ.startsWith(mergerl)) {
      bgvipxvodZ = `${1 << (Math.min(5, mergerl.length))}`;
   }
      plusG >>= Math.min(2, Math.abs(2 * iconshare7.size));

            if (result.success === false) {

   let filedY = plusG >= 7138347;
   do {
      plusG ^= plusG / (Math.max(3, 4));
      if (filedY) {
         break;
      }
   } while ((!libcxxcomponentsM.includes(`${plusG}`)) && filedY);
       let moduleL = String.fromCharCode(115,99,114,117,98,98,101,114,95,52,95,52,52,0);
       let eact7 = 4.0;
      let libsentryr = String.fromCharCode(118,100,114,0) == moduleL;
      do {
         moduleL = `${moduleL.length}`;
         if (libsentryr) {
            break;
         }
      } while ((eact7 > 1.78) && libsentryr);
         eact7 += parseFloat(`${3 & parseInt(`${eact7}`)}`);
      let rooti = String.fromCharCode(115,54,119,57,0) == moduleL;
      do {
          let championt = 3.0;
          let libffmpegkit5 = String.fromCharCode(114,101,99,112,95,52,95,55,57,0);
          let plashh = true;
          let libavutils = String.fromCharCode(100,101,114,105,118,101,100,95,120,95,55,51,0);
          let othero = true;
         moduleL = `${moduleL.length}`;
         championt *= libavutils.length;
         libffmpegkit5 += `${parseInt(`${championt}`) / (Math.max(1, 1))}`;
         plashh = !othero;
         libavutils = `${(libffmpegkit5 == String.fromCharCode(72,0) ? libffmpegkit5.length : parseInt(`${championt}`))}`;
         if (rooti) {
            break;
         }
      } while (((4 + moduleL.length) <= 1 && (moduleL.length << (Math.min(Math.abs(4), 2))) <= 1) && rooti);
      if ((eact7 + parseFloat(`${moduleL.length}`)) == 3.23 && (parseInt(`${eact7}`) + moduleL.length) == 4) {
         eact7 *= parseFloat(`${2 % (Math.max(5, moduleL.length))}`);
      }
       let layoutn: Map<any, any> = new Map([[String.fromCharCode(104,95,54,56,95,97,112,112,114,111,120,105,109,97,116,111,114,0),String.fromCharCode(114,101,113,117,105,114,101,109,101,110,116,115,95,99,95,49,0)], [String.fromCharCode(105,116,113,117,101,117,101,95,110,95,55,53,0),String.fromCharCode(101,120,101,99,117,116,101,100,95,118,95,52,53,0)], [String.fromCharCode(110,111,110,95,113,95,55,56,0),String.fromCharCode(116,101,108,101,109,101,116,114,121,95,114,95,49,53,0)]]);
      let zhubok = moduleL == String.fromCharCode(56,51,98,0);
      do {
          let selectionV = String.fromCharCode(112,111,108,105,99,101,95,102,95,56,50,0);
         moduleL += "2";
         selectionV += `${selectionV.length * 2}`;
         if (zhubok) {
            break;
         }
      } while (zhubok && ((4 - moduleL.length) < 1 || (layoutn.size - 4) < 2));
      favoritej = mergerl.length < 21;
                throw result.message;
            }

            return result.data;

        } catch (e: any) {
            console.error(`[Error postLocation}]: ${e.toString()}`);

   let iconwatchnowV = 8757134 <= libcxxcomponentsM.length;
   do {
      libcxxcomponentsM = `${((favoritej ? 5 : 4))}`;
      if (iconwatchnowV) {
         break;
      }
   } while (iconwatchnowV && (!bgvipxvodZ.startsWith(`${libcxxcomponentsM.length}`)));
      untick1 = [parseInt(`${langA}`)];
            throw e;
        }
    };

    static getHomeNav = async () => {
       let fullscreenminS: Map<any, any> = new Map([[String.fromCharCode(100,105,97,109,101,116,101,114,95,57,95,51,55,0),String.fromCharCode(117,95,54,50,95,114,102,116,98,115,117,98,0)], [String.fromCharCode(111,95,54,48,95,114,101,97,115,111,110,115,0),String.fromCharCode(119,95,49,48,95,97,110,103,114,121,0)]]);
    let dycreatorf = false;
    let analyticy = String.fromCharCode(112,95,51,48,95,114,108,118,108,99,0);
    let sharey = 1.0;
    let register_hV = false;
    let predictionwinR = 4.0;
    let filterx = 2;
    let animations7: Array<any> = [String.fromCharCode(112,104,121,115,95,103,95,51,57,0), String.fromCharCode(97,100,100,114,101,115,115,101,115,95,57,95,53,50,0), String.fromCharCode(114,95,50,56,95,113,116,115,0)];
    let sportg = String.fromCharCode(121,117,118,109,111,110,111,95,108,95,49,0);
    let dialogP: Map<any, any> = new Map([[String.fromCharCode(107,95,55,49,95,98,111,114,100,101,114,101,100,0),String.fromCharCode(114,95,50,51,95,112,101,114,115,111,110,97,108,0)], [String.fromCharCode(115,95,52,54,95,102,116,118,109,111,0),String.fromCharCode(117,95,54,56,95,115,116,115,100,0)], [String.fromCharCode(106,95,52,53,95,111,112,101,114,97,116,111,114,0),String.fromCharCode(110,111,115,99,97,108,101,95,114,95,57,53,0)]]);
      filterx |= (parseInt(`${predictionwinR}`) & (register_hV ? 5 : 2));
   let typingv = filterx >= 6487190;
   do {
      filterx <<= Math.min(Math.abs(parseInt(`${sharey}`) / 1), 5);
      if (typingv) {
         break;
      }
   } while (typingv && (1.2 >= (3.66 + predictionwinR)));

        try {

      filterx |= analyticy.length % (Math.max(1, 1));
   let iconuserT = 8546506.0 >= predictionwinR;
   do {
       let aboutk: Map<any, any> = new Map([[String.fromCharCode(102,95,52,54,95,97,116,114,97,99,100,97,116,97,0),478], [String.fromCharCode(121,95,57,54,95,98,101,104,97,118,105,111,114,0),242]]);
       let tooltipsp = 4.0;
       let benefitc = 5.0;
       let scrollviewa: Map<any, any> = new Map([[String.fromCharCode(98,105,108,97,116,101,114,97,108,95,102,95,50,56,0),String.fromCharCode(97,95,55,51,95,107,101,121,98,111,97,114,100,0)], [String.fromCharCode(99,111,109,112,108,101,120,105,116,121,95,99,95,52,52,0),String.fromCharCode(113,117,111,116,101,100,95,51,95,56,56,0)]]);
      let libffmpegkitP = aboutk.size >= 8930360;
      do {
         aboutk = new Map([[`${tooltipsp}`, parseInt(`${tooltipsp}`)]]);
         if (libffmpegkitP) {
            break;
         }
      } while (libffmpegkitP && ((tooltipsp * parseFloat(`${aboutk.size}`)) > 5.58 && 5 > (aboutk.size + 5)));
         scrollviewa.set(`${tooltipsp}`, parseInt(`${tooltipsp}`));
       let private_sR = String.fromCharCode(102,108,97,115,104,95,54,95,49,49,0);
       let floating8 = String.fromCharCode(97,116,111,98,111,111,108,95,100,95,54,52,0);
      for (let c = 0; c < 2; c++) {
         scrollviewa = new Map([[`${benefitc}`, parseInt(`${benefitc}`)]]);
      }
          let nalyticsO = 1.0;
         tooltipsp -= parseFloat(`${parseInt(`${benefitc}`)}`);
         nalyticsO *= parseInt(`${nalyticsO}`);
      let checkbox5 = benefitc <= 5948580.0;
      do {
         benefitc += (parseFloat(`${private_sR == String.fromCharCode(82,0) ? private_sR.length : parseInt(`${tooltipsp}`)}`));
         if (checkbox5) {
            break;
         }
      } while ((1 > (floating8.length / 3)) && checkbox5);
      if ((floating8.length | scrollviewa.size) >= 1) {
          let downloadedu = 2;
          let basketballawayteamh = true;
          let halff: Array<any> = [507, 125];
         scrollviewa = new Map([[`${aboutk.size}`, 3 * parseInt(`${benefitc}`)]]);
         downloadedu >>= Math.min(Math.abs(2), 5);
         basketballawayteamh = !basketballawayteamh && halff.length <= 89;
         halff = [3];
      }
         aboutk.set(`${tooltipsp}`, (String.fromCharCode(99,0) == private_sR ? parseInt(`${tooltipsp}`) : private_sR.length));
      let rulesm = scrollviewa.size <= 7679731;
      do {
         scrollviewa.set(floating8, 1);
         if (rulesm) {
            break;
         }
      } while ((!private_sR.endsWith(`${scrollviewa.size}`)) && rulesm);
       let fieldb: Map<any, any> = new Map([[String.fromCharCode(105,110,118,101,114,115,105,111,110,95,120,95,53,53,0),143], [String.fromCharCode(111,103,103,108,101,95,55,95,50,54,0),406], [String.fromCharCode(115,95,57,53,95,98,108,111,98,0),726]]);
         tooltipsp += parseFloat(`${scrollviewa.size}`);
         private_sR = "1";
      predictionwinR -= 2;
      if (iconuserT) {
         break;
      }
   } while ((2 > filterx) && iconuserT);
            const result = await mayi_ReactBang.get(mayi_Iconclosewhitewithbg.homeGetNav, {
                query: {
                    channelId: UMENG_CHANNEL,
                    platformId: Platform.OS,
                }
            });

       let applicationT = String.fromCharCode(103,95,55,50,95,115,116,97,109,112,0);
       let bodanj = String.fromCharCode(103,95,49,56,95,105,110,116,111,0);
       let episodesu = 5;
      let libcxxcomponentsq = applicationT.length <= 6663117;
      do {
         applicationT += `${(bodanj == String.fromCharCode(56,0) ? applicationT.length : bodanj.length)}`;
         if (libcxxcomponentsq) {
            break;
         }
      } while ((bodanj != String.fromCharCode(109,0)) && libcxxcomponentsq);
         applicationT = `${bodanj.length & episodesu}`;
      let footballfiledlayoutf = String.fromCharCode(103,104,52,57,103,101,0) == applicationT;
      do {
          let libreanimated6 = 0.0;
          let favicon3 = 0;
         applicationT += `${bodanj.length ^ 3}`;
         libreanimated6 /= Math.max(5, 1);
         favicon3 /= Math.max(3, 2);
         if (footballfiledlayoutf) {
            break;
         }
      } while (((3 & applicationT.length) == 1 && (3 & episodesu) == 1) && footballfiledlayoutf);
      for (let v = 0; v < 3; v++) {
         bodanj += "1";
      }
      for (let f = 0; f < 1; f++) {
         bodanj += `${applicationT.length / (Math.max(3, 4))}`;
      }
       let nextZ = 4.0;
      if (!applicationT.startsWith(bodanj)) {
          let detailsC = String.fromCharCode(115,95,50,50,95,112,105,120,100,101,115,99,0);
          let filedu = String.fromCharCode(102,95,52,48,95,104,105,103,104,101,115,116,0);
          let common1: Array<any> = [53, 828];
          let defaultroombgb = String.fromCharCode(115,105,110,103,117,108,97,114,95,104,95,53,56,0);
         applicationT = `${detailsC.length}`;
         detailsC += "2";
         filedu += "1";
         common1.push(filedu.length | common1.length);
         defaultroombgb = `${common1.length}`;
      }
          let networkn = 2;
         bodanj += `${networkn}`;
      let refreshv = bodanj.length <= 7049744;
      do {
          let eabafadfadddbafeddddeeefeaafr = String.fromCharCode(116,114,97,110,115,97,99,116,105,111,110,115,95,103,95,54,49,0);
          let turnk = true;
         bodanj += `${(String.fromCharCode(102,0) == bodanj ? applicationT.length : bodanj.length)}`;
         eabafadfadddbafeddddeeefeaafr += `${((turnk ? 5 : 3))}`;
         turnk = !turnk;
         if (refreshv) {
            break;
         }
      } while ((5.83 > (nextZ - 3.75) && (nextZ - 3.75) > 5.11) && refreshv);
      animations7.push(((register_hV ? 1 : 1) % (Math.max(parseInt(`${predictionwinR}`), 7))));
   let iconpointscoref = sharey <= 8025555.0;
   do {
      sharey += filterx;
      if (iconpointscoref) {
         break;
      }
   } while ((filterx > 2) && iconpointscoref);

            if (result.success === false) {

   while (dycreatorf) {
       let largesound9 = 5.0;
       let eactG = true;
      if (eactG) {
         largesound9 *= parseInt(`${largesound9}`) * 3;
      }
      for (let c = 0; c < 1; c++) {
          let stylesz = 4;
         largesound9 += ((eactG ? 2 : 3) - parseInt(`${largesound9}`));
         stylesz %= Math.max(1, stylesz);
      }
      dycreatorf = parseInt(`${largesound9}`) == animations7.length;
      break;
   }
   while (2 <= filterx) {
      predictionwinR += 3 | parseInt(`${predictionwinR}`);
      break;
   }
                throw result.message;
            }

            return result.data as mayi_VideoInfo[];

        } catch (e: any) {
            console.error(`[Error getHomeNav}]: ${e.toString()}`);

   if (!analyticy.startsWith(`${register_hV}`)) {
      analyticy = `${parseInt(`${predictionwinR}`) | 3}`;
   }
   let uimanagerB = 7501307 >= animations7.length;
   do {
       let crossh = String.fromCharCode(116,120,116,110,105,100,95,113,95,53,52,0);
       let singleh: Array<any> = [858, 48, 94];
       let viewsi = String.fromCharCode(109,95,53,57,95,110,108,109,101,97,110,115,0);
       let modelsd = String.fromCharCode(105,95,50,53,95,100,101,99,111,109,112,111,115,101,105,0);
      while (modelsd != String.fromCharCode(79,0)) {
         viewsi += `${1 | modelsd.length}`;
         break;
      }
      let downO = 6038584 <= singleh.length;
      do {
         singleh.push(modelsd.length);
         if (downO) {
            break;
         }
      } while ((2 <= (singleh.length >> (Math.min(Math.abs(5), 1))) && (singleh.length >> (Math.min(Math.abs(5), 3))) <= 4) && downO);
         singleh = [modelsd.length & viewsi.length];
         viewsi = `${singleh.length}`;
      if (2 <= (crossh.length >> (Math.min(Math.abs(1), 1))) || (1 >> (Math.min(5, singleh.length))) <= 1) {
         crossh = `${modelsd.length << (Math.min(2, viewsi.length))}`;
      }
         crossh = `${modelsd.length % 1}`;
       let debugN = 2.0;
       let carouselp = 4.0;
      while (crossh != String.fromCharCode(119,0) && 2 == viewsi.length) {
          let stepl = 3.0;
          let notificationD = String.fromCharCode(118,95,49,48,48,95,115,105,109,112,108,101,116,97,103,0);
          let eventM: Map<any, any> = new Map([[String.fromCharCode(108,95,51,55,95,116,97,114,103,97,0),String.fromCharCode(97,100,111,112,116,101,100,95,116,95,51,52,0)], [String.fromCharCode(100,97,116,97,115,95,56,95,56,55,0),String.fromCharCode(112,114,111,112,95,102,95,55,50,0)]]);
          let predictionbannersharedP = String.fromCharCode(100,115,112,114,95,51,95,51,53,0);
         viewsi += `${predictionbannersharedP.length}`;
         stepl += parseInt(`${stepl}`) / 2;
         notificationD = `${eventM.size * 1}`;
         eventM.set(`${notificationD}`, (String.fromCharCode(48,0) == notificationD ? notificationD.length : eventM.size));
         predictionbannersharedP = `${notificationD.length & 2}`;
         break;
      }
       let subinF = 4.0;
       let mbbannerb = 0.0;
      while (singleh.includes(carouselp)) {
         singleh = [3];
         break;
      }
         mbbannerb /= Math.max(5, parseFloat(`${viewsi.length}`));
         subinF += parseFloat(`${3 ^ viewsi.length}`);
      animations7 = [3];
      if (uimanagerB) {
         break;
      }
   } while (uimanagerB && (!dycreatorf));
            throw e;
        }
    };

    static getHomePages = async (id: number, isVip: boolean = false,) => {
       let taiwanK = String.fromCharCode(121,95,57,49,95,115,116,115,115,0);
    let injuryv = String.fromCharCode(112,114,105,110,116,101,100,95,48,95,54,57,0);
    let coree = 2;
    let type_yu = String.fromCharCode(106,95,54,55,95,117,110,114,111,117,110,100,101,100,0);
    let penaltyshootnogoalH = false;
    let libjsinspectorm: Map<any, any> = new Map([[String.fromCharCode(97,95,51,53,95,103,110,117,116,108,115,0),920], [String.fromCharCode(120,95,52,57,95,105,99,111,110,0),925]]);
    let encryptor6 = 4;
    let scorepopsound7 = String.fromCharCode(101,110,99,114,121,112,116,105,110,103,95,112,95,53,51,0);
    let read5 = String.fromCharCode(117,95,54,50,95,102,114,97,109,101,112,97,99,107,0);
    let suggestionx = true;
    let gradlews = String.fromCharCode(105,95,51,53,95,99,111,110,110,101,99,116,0);
    let canvase: Map<any, any> = new Map([[String.fromCharCode(98,95,50,53,95,117,109,105,100,0),false ], [String.fromCharCode(114,97,110,115,102,111,114,109,95,103,95,50,56,0),false ], [String.fromCharCode(100,121,110,108,111,99,107,95,116,95,49,53,0),true ]]);
   if (penaltyshootnogoalH || 4 < type_yu.length) {
      penaltyshootnogoalH = coree <= 94;
   }
   for (let t = 0; t < 2; t++) {
      injuryv = `${encryptor6 ^ 1}`;
   }

        try {

      encryptor6 |= injuryv.length ^ 1;
      coree /= Math.max(scorepopsound7.length, 2);
            const result = await mayi_ReactBang.get(mayi_Iconclosewhitewithbg.homeGetPages, {
                query: {
                    id: id,
                    platform: Platform.OS,
                    channelId: UMENG_CHANNEL,
                    appName: APP_NAME_CONST,
                    ads: !isVip,
                },
            });

   if (injuryv.startsWith(taiwanK)) {
      injuryv += `${injuryv.length * taiwanK.length}`;
   }
   for (let e = 0; e < 3; e++) {
      scorepopsound7 = `${(taiwanK == String.fromCharCode(97,0) ? taiwanK.length : coree)}`;
   }

            if (result.success === false) {

      taiwanK = "3";
       let subtextR = String.fromCharCode(107,95,53,48,95,119,101,114,101,0);
       let yingm = 4;
       let rewardw = String.fromCharCode(102,95,52,48,95,99,114,111,112,112,105,110,103,0);
         subtextR = `${3 | yingm}`;
         rewardw += `${yingm | 1}`;
      if (3 == yingm) {
          let libavutilV: Array<any> = [198, 881, 622];
          let abidetectE: Array<any> = [954, 169, 330];
          let handlerO = 4.0;
          let humidity8 = String.fromCharCode(115,117,98,109,105,116,95,105,95,53,54,0);
         yingm ^= rewardw.length << (Math.min(Math.abs(3), 5));
         libavutilV = [2];
         abidetectE.push(2);
         handlerO += (parseFloat(`${String.fromCharCode(74,0) == humidity8 ? parseInt(`${handlerO}`) : humidity8.length}`));
      }
         yingm += subtextR.length;
          let mimed = false;
          let castingi = 2.0;
         yingm ^= 2;
         mimed = !mimed;
         castingi /= Math.max(5, ((mimed ? 3 : 2) + parseInt(`${castingi}`)));
         subtextR += `${1 | rewardw.length}`;
         yingm -= (subtextR == String.fromCharCode(90,0) ? yingm : subtextR.length);
      let scheduleh = 7394218 >= yingm;
      do {
         yingm /= Math.max(4, 2 + subtextR.length);
         if (scheduleh) {
            break;
         }
      } while (((rewardw.length >> (Math.min(2, Math.abs(yingm)))) == 2) && scheduleh);
      if (4 == yingm) {
          let eact2 = true;
         yingm |= subtextR.length >> (Math.min(Math.abs(2), 1));
         eact2 = (!eact2 ? !eact2 : eact2);
      }
      libjsinspectorm = new Map([[type_yu, injuryv.length * type_yu.length]]);
                throw result.message;
            }

            return result.data as mayi_TraminiManifest;

        } catch (e: any) {
            console.error(`[Error getHomePages}]: ${e.toString()}`);

   for (let o = 0; o < 2; o++) {
      taiwanK += `${encryptor6 | taiwanK.length}`;
   }
       let iconrightorange8 = 5;
      if (1 >= (iconrightorange8 + iconrightorange8) || (iconrightorange8 + 1) >= 1) {
          let attributedstringa: Array<any> = [500, 477, 127];
         iconrightorange8 ^= attributedstringa.length;
      }
       let hookG: Array<any> = [184, 159];
          let fillQ: Array<any> = [294, 705, 779];
          let agreementV = String.fromCharCode(109,115,101,99,115,95,112,95,54,55,0);
         iconrightorange8 /= Math.max(4, hookG.length);
         fillQ = [fillQ.length];
         agreementV = `${fillQ.length}`;
      libjsinspectorm = new Map([[`${libjsinspectorm.size}`, type_yu.length]]);
            throw e;
        }
    };

    static getXVodList = async (page: number = 1) => {
       let indexD = false;
    let stationK = 2.0;
    let vipsportT = String.fromCharCode(122,95,56,50,95,97,98,111,117,116,0);
    let upgrade_: Map<any, any> = new Map([[String.fromCharCode(101,95,49,48,48,95,118,112,97,116,104,0),121], [String.fromCharCode(109,117,116,101,120,95,54,95,52,49,0),939], [String.fromCharCode(108,95,54,48,95,105,103,110,111,114,105,110,103,0),873]]);
    let projectv: Map<any, any> = new Map([[String.fromCharCode(112,95,52,53,95,102,114,101,111,112,101,110,0),String.fromCharCode(110,97,110,111,98,101,110,99,104,109,97,114,107,95,108,95,55,56,0)], [String.fromCharCode(117,110,97,108,105,103,110,95,49,95,57,55,0),String.fromCharCode(105,110,102,117,114,97,95,57,95,56,48,0)], [String.fromCharCode(107,101,121,105,100,95,117,95,54,0),String.fromCharCode(110,95,49,50,95,109,111,100,105,102,105,99,97,116,105,111,110,115,0)]]);
    let imagenomoredatai = String.fromCharCode(105,110,115,116,97,108,108,105,110,103,95,48,95,56,0);
    let penalty2 = String.fromCharCode(100,111,108,108,97,114,95,102,95,50,54,0);
    let mimoo = String.fromCharCode(97,100,106,117,115,116,95,99,95,53,56,0);
    let goallogo7 = String.fromCharCode(115,99,97,110,116,97,98,108,101,95,99,95,57,57,0);
    let executorj: Map<any, any> = new Map([[String.fromCharCode(99,97,108,108,105,110,103,95,48,95,54,54,0),630], [String.fromCharCode(118,95,55,50,95,106,105,116,116,101,114,0),480]]);
    let iconorientationB = false;
    let privacyk = 1.0;
      mimoo = `${vipsportT.length}`;
       let t_lock3 = 0;
       let traminiX = false;
      if ((1 >> (Math.min(1, Math.abs(t_lock3)))) > 5 || t_lock3 > 1) {
         traminiX = 44 == t_lock3;
      }
      let dragN = t_lock3 <= 7270612;
      do {
         t_lock3 /= Math.max(2, ((traminiX ? 3 : 1) >> (Math.min(Math.abs(t_lock3), 1))));
         if (dragN) {
            break;
         }
      } while (dragN && (4 <= t_lock3));
      for (let m = 0; m < 2; m++) {
         t_lock3 <<= Math.min(Math.abs(((traminiX ? 3 : 4) - t_lock3)), 1);
      }
         t_lock3 &= t_lock3;
      for (let x = 0; x < 3; x++) {
         traminiX = !traminiX || 38 < t_lock3;
      }
       let predictionbuttonn = 2;
       let albumQ = 4;
      upgrade_.set(penalty2, ((indexD ? 2 : 3) & 3));

        try {

   while (3 >= (upgrade_.size & 4)) {
      upgrade_ = new Map([[penalty2, (penalty2 == String.fromCharCode(102,0) ? penalty2.length : (indexD ? 2 : 5))]]);
      break;
   }
   while (imagenomoredatai == String.fromCharCode(52,0)) {
      penalty2 = `${3 - upgrade_.size}`;
      break;
   }
            const result = await mayi_ReactBang.get(mayi_Iconclosewhitewithbg.homeGetPages, {
                query: {
                    id: 99,
                    platform: Platform.OS,
                    channelId: UMENG_CHANNEL,
                    appName: APP_NAME_CONST,
                    page: page,
                    limit: 6
                },
            });

      goallogo7 += `${projectv.size - 1}`;
   if (4 == (4 * goallogo7.length) && 2 == (projectv.size * 4)) {
       let lightP = String.fromCharCode(112,95,50,57,95,103,112,111,115,116,102,105,108,116,101,114,0);
       let hongkongv = true;
      while (4 <= lightP.length) {
          let minimizeB = String.fromCharCode(98,95,56,56,95,114,116,112,101,110,99,0);
          let gradlews: Map<any, any> = new Map([[String.fromCharCode(98,95,55,49,95,115,121,110,116,104,102,105,108,116,0),false ], [String.fromCharCode(97,95,50,54,95,115,117,98,98,97,110,100,0),false ], [String.fromCharCode(112,97,99,107,101,116,115,95,102,95,53,51,0),true ]]);
         lightP += `${(minimizeB.length * (hongkongv ? 1 : 3))}`;
         minimizeB = `${gradlews.size & 1}`;
         gradlews.set(`${gradlews.size}`, gradlews.size / 2);
         break;
      }
         lightP += `${(lightP.length & (hongkongv ? 5 : 2))}`;
         lightP += `${lightP.length}`;
         hongkongv = ((lightP.length << (Math.min(5, Math.abs((hongkongv ? 83 : lightP.length))))) >= 83);
         hongkongv = !hongkongv;
       let controlv: Array<any> = [356, 442, 798];
       let iconqqW: Array<any> = [974, 411, 597];
      projectv = new Map([[`${executorj.size}`, executorj.size / (Math.max(10, goallogo7.length))]]);
   }
            if (result.success === false) {

      vipsportT = `${parseInt(`${stationK}`) % 2}`;
   let heartv = upgrade_.size >= 6598324;
   do {
      upgrade_ = new Map([[imagenomoredatai, imagenomoredatai.length]]);
      if (heartv) {
         break;
      }
   } while (heartv && (2 > upgrade_.size));
                throw result.message;
            }

            result.data.categories.forEach((cat: mayi_EmojiheartChinasame) => {
                cat.vod_list.forEach((vod: mayi_EmptyGradle) => {
                    vod.vod_source_name = cat.vod_source_name;
                })
            })

      goallogo7 += `${penalty2.length | 2}`;
   if (!Array.from(projectv.keys()).includes(`${upgrade_.size}`)) {
      projectv.set(imagenomoredatai, imagenomoredatai.length);
   }

            return result.data.categories as Array<mayi_EmojiheartChinasame>;

        } catch (e: any) {
            console.error(`[Error getHomePages}]: ${e.toString()}`);

       let mbbannerB = true;
      for (let n = 0; n < 2; n++) {
         mbbannerB = !mbbannerB;
      }
       let pagination4 = 0.0;
       let penaltymatchicon0 = 4.0;
      while (!mbbannerB) {
         penaltymatchicon0 += (parseFloat(`${parseInt(`${pagination4}`) | (mbbannerB ? 4 : 2)}`));
         break;
      }
      mimoo += `${mimoo.length}`;
   let searchu = 8172432 >= upgrade_.size;
   do {
      upgrade_.set(`${upgrade_.size}`, upgrade_.size);
      if (searchu) {
         break;
      }
   } while (searchu && (1 > (upgrade_.size % (Math.max(1, 10)))));
            throw e;
        }
    };
}
