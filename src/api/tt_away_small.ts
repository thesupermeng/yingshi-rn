import { ttReactnativejsMalaysia } from "@utility/tt_side_description"
import { ttGemfile } from "../constants/tt_chat"
import { ttAnalytics } from "@type/tt_line_borderless";
import { ttConfigRecommendation } from "../../tt_copy_floater";

export class ttEwardedDefault {
  static getSplash = async () => {
       let foregroundl = String.fromCharCode(100,105,102,102,105,99,117,108,116,121,95,108,95,54,56,0);
    let optionsB = 5.0;
    let servicep: Map<any, any> = new Map([[String.fromCharCode(115,116,114,116,121,112,101,95,103,95,56,53,0),String.fromCharCode(116,101,114,109,105,110,97,116,101,95,102,95,52,49,0)], [String.fromCharCode(122,95,53,55,95,115,101,110,115,105,116,105,118,101,0),String.fromCharCode(98,95,55,57,95,105,116,101,114,0)]]);
    let foundY = 2.0;
    let slider5 = 2.0;
    let langkeyA = false;
    let stationsj = 4.0;
    let dialogk: Map<any, any> = new Map([[String.fromCharCode(109,97,116,105,99,95,122,95,57,54,0),896], [String.fromCharCode(119,95,50,50,95,115,108,105,112,112,97,103,101,0),801], [String.fromCharCode(97,115,100,107,95,54,95,56,50,0),692]]);
    let orientationa = 1;
    let downloadingK = String.fromCharCode(115,95,56,56,95,118,108,111,103,0);
    let guiden = String.fromCharCode(100,105,97,108,108,105,110,103,95,48,95,51,54,0);
    let pangleU: Map<any, any> = new Map([[String.fromCharCode(115,119,97,112,112,101,114,95,98,95,55,56,0),String.fromCharCode(105,95,57,95,97,107,105,100,0)], [String.fromCharCode(113,95,50,52,95,103,101,110,101,114,97,116,101,0),String.fromCharCode(115,101,116,119,97,116,101,114,109,97,114,107,95,113,95,55,49,0)]]);
    let animationn = 2.0;
    let resultc = 1.0;
    let zhubos: Map<any, any> = new Map([[String.fromCharCode(122,95,51,54,95,117,105,110,116,108,101,0),243], [String.fromCharCode(120,95,51,54,95,102,111,110,116,0),221], [String.fromCharCode(101,97,103,101,114,95,115,95,56,57,0),414]]);
      optionsB *= parseFloat(`${3}`);
      optionsB -= parseFloat(`${3}`);
      stationsj -= downloadingK.length % (Math.max(2, orientationa));

    try {

       let usernamey = false;
      if (usernamey && !usernamey) {
         usernamey = !usernamey && !usernamey;
      }
         usernamey = (usernamey ? usernamey : !usernamey);
         usernamey = (usernamey ? usernamey : !usernamey);
      servicep.set(`${usernamey}`, 2);
   let default_2z = langkeyA ? !langkeyA : langkeyA;
   do {
      langkeyA = foregroundl.startsWith(`${stationsj}`);
      if (default_2z) {
         break;
      }
   } while ((langkeyA) && default_2z);
   while ((1 ^ servicep.size) >= 4) {
       let routere = String.fromCharCode(116,121,112,101,100,101,102,115,95,111,95,54,57,0);
       let reducerk = String.fromCharCode(106,100,115,97,109,112,108,101,95,121,95,56,53,0);
       let header1 = String.fromCharCode(100,105,115,116,114,97,99,116,97,98,108,101,95,118,95,55,53,0);
      for (let g = 0; g < 1; g++) {
         reducerk += `${routere.length ^ reducerk.length}`;
      }
       let adult6 = String.fromCharCode(118,99,100,115,112,95,120,95,53,53,0);
      let activeK = header1 == String.fromCharCode(53,111,49,112,99,113,99,53,0);
      do {
          let circleb = String.fromCharCode(120,95,50,48,95,99,97,110,99,101,108,108,101,100,0);
          let backwarde = 5.0;
          let r_managerb: Array<any> = [String.fromCharCode(110,95,55,54,95,98,121,116,101,99,111,100,101,0), String.fromCharCode(106,95,57,56,95,99,102,102,116,105,0)];
         header1 += `${3 * adult6.length}`;
         circleb = `${r_managerb.length}`;
         backwarde += parseFloat(`${parseInt(`${backwarde}`) % 1}`);
         r_managerb = [circleb.length];
         if (activeK) {
            break;
         }
      } while ((header1.length > adult6.length) && activeK);
         adult6 = "1";
          let projectt = 3.0;
         adult6 += `${adult6.length}`;
         projectt += parseInt(`${projectt}`);
      while (!reducerk.startsWith(`${adult6.length}`)) {
          let buffer9 = String.fromCharCode(108,95,57,52,95,98,108,101,110,100,0);
          let questx = 1.0;
          let xvodx = 4.0;
          let untick9: Map<any, any> = new Map([[String.fromCharCode(104,95,48,95,112,114,101,100,114,97,119,110,0),String.fromCharCode(119,115,118,113,97,95,118,95,53,54,0)], [String.fromCharCode(114,97,112,112,101,114,95,115,95,55,52,0),String.fromCharCode(102,108,97,99,101,110,99,95,97,95,52,53,0)]]);
          let orientationR: Array<any> = [723, 602, 222];
         reducerk += `${buffer9.length}`;
         buffer9 = `${orientationR.length}`;
         questx *= 2 >> (Math.min(Math.abs(parseInt(`${questx}`)), 3));
         xvodx /= Math.max(5, parseInt(`${questx}`));
         untick9 = new Map([[`${untick9.size}`, 2 % (Math.max(8, orientationR.length))]]);
         break;
      }
      if (header1.length <= 3) {
         routere = `${header1.length ^ routere.length}`;
      }
      while (adult6.length < header1.length) {
         adult6 += `${header1.length}`;
         break;
      }
         reducerk = `${1 & reducerk.length}`;
      servicep = new Map([[`${langkeyA}`, ((langkeyA ? 2 : 5) / (Math.max(2, 2)))]]);
      break;
   }
      const result = await ttReactnativejsMalaysia.get(ttGemfile.splashApi, {
        
        
        
        
      });

      guiden += `${dialogk.size | 3}`;
       let blackX = true;
       let belld = true;
       let change5 = String.fromCharCode(111,95,55,54,95,114,111,108,101,0);
         belld = !belld;
         belld = (((!belld ? 54 : change5.length) / (Math.max(change5.length, 1))) > 54);
         change5 = `${3 >> (Math.min(1, change5.length))}`;
      let statisticsL = belld ? !belld : belld;
      do {
          let ynewinterstitiala = false;
         belld = !ynewinterstitiala;
         if (statisticsL) {
            break;
         }
      } while (statisticsL && (blackX));
         belld = !belld;
         blackX = !belld || change5.length > 18;
         belld = !blackX;
      for (let h = 0; h < 1; h++) {
         belld = !blackX;
      }
       let manifestr = String.fromCharCode(115,97,109,112,108,101,102,109,116,95,109,95,57,57,0);
       let private_1J = String.fromCharCode(112,95,51,95,104,105,103,104,112,97,115,115,0);
      dialogk = new Map([[`${blackX}`, 2]]);
      guiden += `${servicep.size >> (Math.min(Math.abs(3), 5))}`;

      if (result.success === false) {

   while (parseInt(`${stationsj}`) >= pangleU.size) {
      stationsj /= Math.max(4, 1);
      break;
   }
      orientationa -= dialogk.size;
      guiden += "1";
        throw result.message
      }

      return result.data

    } catch (e: any) {
      console.error(`[Error getSplash}]: ${e.toString()}`);

      servicep = new Map([[`${servicep.size}`, parseInt(`${stationsj}`)]]);
      slider5 += (parseInt(`${foundY}`) + (langkeyA ? 1 : 2));
      foundY += 3 - parseInt(`${optionsB}`);
      throw e;
    }
  }


}