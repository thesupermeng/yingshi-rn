import { wwCollection, wwNativemodule } from "@constants";
import { wwForegroundModules } from "@utility/ww_rewardvideo";
import { APP_NAME_CONST, APP_VERSION, UMENG_CHANNEL } from "@utility/ww_icon";
import { wwSource } from "@utility/ww_downloading";
import { Platform } from "react-native";
import { wwAbidetect } from "../../ww_leakchecker";
import { wwProduct, wwLibreactperfloggerjni, wwDownloadingScheduler, wwControl } from "@type/ww_dycreator_result";

export class wwRewardvideoMegaphone {
    static getLocalIpAddress = async () => {
       let combine2: Map<any, any> = new Map([[String.fromCharCode(111,99,116,101,116,0),974], [String.fromCharCode(110,117,108,108,115,0),483]]);
    let helperb: Map<any, any> = new Map([[String.fromCharCode(116,119,105,110,118,113,0),String.fromCharCode(98,108,117,114,97,121,0)], [String.fromCharCode(99,104,97,114,97,99,116,101,114,105,115,116,105,99,115,0),String.fromCharCode(114,117,110,116,101,115,116,115,0)]]);
    let movies0 = 1;
    let sharedO = true;
    let right9 = String.fromCharCode(109,101,101,116,117,112,0);
    let libsgcorec = String.fromCharCode(98,108,105,110,100,105,110,103,0);
    let selectT = 5;
    let placementC: Map<any, any> = new Map([[String.fromCharCode(112,95,53,52,95,114,101,97,100,0),false ], [String.fromCharCode(108,111,97,116,0),false ]]);
    let searchz: Map<any, any> = new Map([[String.fromCharCode(102,101,109,97,108,101,0),String.fromCharCode(115,101,110,99,0)], [String.fromCharCode(98,117,116,111,110,0),String.fromCharCode(98,111,117,110,100,115,112,101,99,105,102,105,99,0)], [String.fromCharCode(105,99,101,0),String.fromCharCode(110,99,111,110,102,0)]]);
    let penaltys = false;
    let debugx = 0.0;
    let starG = String.fromCharCode(103,101,116,112,97,100,100,114,115,0);
    let shoot_ = String.fromCharCode(97,108,114,101,97,100,121,0);
    let gifgoalw = String.fromCharCode(112,111,115,116,112,114,111,99,114,101,115,0);
    let championa = String.fromCharCode(115,119,105,112,101,100,0);
   for (let g = 0; g < 3; g++) {
      placementC = new Map([[`${searchz.size}`, 2 >> (Math.min(3, Math.abs(searchz.size)))]]);
   }
       let default_jt2 = 3;
      while (4 > (2 << (Math.min(5, Math.abs(default_jt2)))) || (2 << (Math.min(1, Math.abs(default_jt2)))) > 5) {
         default_jt2 &= 1;
         break;
      }
         default_jt2 <<= Math.min(Math.abs(1 * default_jt2), 3);
         default_jt2 += default_jt2 << (Math.min(Math.abs(1), 5));
      placementC = new Map([[`${placementC.size}`, searchz.size * placementC.size]]);

        

       let basketballF = 1.0;
       let zoomi: Map<any, any> = new Map([[String.fromCharCode(112,114,101,115,101,116,0),false ], [String.fromCharCode(108,97,114,103,101,115,116,0),true ], [String.fromCharCode(108,97,121,101,114,0),true ]]);
       let backiconJ: Array<any> = [876, 812];
      if (3 <= (backiconJ.length - 4) || (zoomi.size - backiconJ.length) <= 4) {
         zoomi.set(`${basketballF}`, backiconJ.length);
      }
      for (let k = 0; k < 1; k++) {
         backiconJ.push(2);
      }
       let holderQ = String.fromCharCode(100,105,114,101,99,116,120,0);
      if ((zoomi.size % 2) <= 5 || 2 <= (zoomi.size % (Math.max(holderQ.length, 9)))) {
         holderQ += `${backiconJ.length}`;
      }
         basketballF += parseFloat(`${zoomi.size >> (Math.min(backiconJ.length, 1))}`);
          let submit6 = 1;
         backiconJ = [backiconJ.length];
         submit6 *= submit6 / (Math.max(submit6, 2));
      if ((backiconJ.length * holderQ.length) < 4 && 3 < (4 * backiconJ.length)) {
         backiconJ.push(1 - parseInt(`${basketballF}`));
      }
      let alert6 = 5132368 <= zoomi.size;
      do {
          let predictionbannershared3 = 3;
         zoomi.set(`${basketballF}`, zoomi.size);
         predictionbannershared3 ^= 3 - predictionbannershared3;
         if (alert6) {
            break;
         }
      } while ((!Array.from(zoomi.keys()).includes(`${basketballF}`)) && alert6);
      let libavformatY = basketballF >= 8511769.0;
      do {
         basketballF += parseFloat(`${3}`);
         if (libavformatY) {
            break;
         }
      } while (libavformatY && (holderQ.includes(`${basketballF}`)));
      starG = `${movies0 ^ parseInt(`${debugx}`)}`;
   while (4.61 > (debugx + 3.53) && (4 - combine2.size) > 2) {
      combine2 = new Map([[`${searchz.size}`, 3]]);
      break;
   }
        let ipAddress = '219.75.27.16';

   if (helperb.size >= 3) {
       let transfero = 2.0;
       let starr = String.fromCharCode(101,110,117,109,115,0);
       let whistley = String.fromCharCode(109,100,97,116,101,0);
         transfero += (String.fromCharCode(105,0) == whistley ? whistley.length : parseInt(`${transfero}`));
      for (let v = 0; v < 1; v++) {
         starr += `${whistley.length - parseInt(`${transfero}`)}`;
      }
         whistley += `${whistley.length}`;
         transfero -= starr.length & 2;
          let shootS = String.fromCharCode(98,97,99,107,114,111,117,110,100,0);
          let ewarded5 = String.fromCharCode(100,117,114,97,116,105,111,110,115,0);
         transfero *= 1;
         shootS = `${ewarded5.length | shootS.length}`;
         ewarded5 += `${(ewarded5 == String.fromCharCode(99,0) ? shootS.length : ewarded5.length)}`;
      let ajax7 = 8756254.0 >= transfero;
      do {
         transfero -= (String.fromCharCode(103,0) == whistley ? whistley.length : starr.length);
         if (ajax7) {
            break;
         }
      } while ((3.23 == (transfero / 3.75) && (transfero / 3.75) == 3.46) && ajax7);
          let c_imageo = String.fromCharCode(104,101,97,100,101,114,0);
          let gesture7 = String.fromCharCode(97,95,49,53,95,102,116,118,108,105,110,107,0);
          let k_titleK = 5.0;
         whistley = `${starr.length}`;
         c_imageo += `${c_imageo.length}`;
         gesture7 += `${gesture7.length & c_imageo.length}`;
         k_titleK *= parseFloat(`${gesture7.length}`);
      for (let q = 0; q < 2; q++) {
         starr = `${parseInt(`${transfero}`)}`;
      }
         whistley += `${3 + starr.length}`;
      helperb.set(`${sharedO}`, (searchz.size | (sharedO ? 2 : 1)));
   }
       let libreactnativejniC = true;
       let sheet6: Map<any, any> = new Map([[String.fromCharCode(116,104,114,101,115,104,111,108,100,0),799], [String.fromCharCode(115,116,105,99,107,101,114,101,100,0),120], [String.fromCharCode(109,105,100,100,108,101,95,118,95,57,52,0),511]]);
      let disconnectedlogo_ = libreactnativejniC ? !libreactnativejniC : libreactnativejniC;
      do {
         libreactnativejniC = sheet6.get(`${libreactnativejniC}`) == null;
         if (disconnectedlogo_) {
            break;
         }
      } while (disconnectedlogo_ && (4 <= sheet6.size));
      while (5 < (sheet6.size * 3)) {
         sheet6 = new Map([[`${sheet6.size}`, 2]]);
         break;
      }
         sheet6 = new Map([[`${sheet6.size}`, ((libreactnativejniC ? 1 : 2) >> (Math.min(Math.abs(3), 2)))]]);
         sheet6.set(`${libreactnativejniC}`, 3);
         sheet6.set(`${libreactnativejniC}`, sheet6.size * 2);
      if (!libreactnativejniC || (sheet6.size | 3) <= 4) {
         sheet6.set(`${libreactnativejniC}`, (3 << (Math.min(3, Math.abs((libreactnativejniC ? 5 : 2))))));
      }
      starG += `${movies0 - 1}`;

        try {

      debugx += parseFloat(`${2 ^ combine2.size}`);
      placementC = new Map([[`${combine2.size}`, selectT % (Math.max(2, 2))]]);
            const result = await wwForegroundModules.get(wwCollection.appGetLocalIp, {
                isFullUrl: true,
            });

      sharedO = starG.length <= placementC.size;
       let tailB = String.fromCharCode(116,111,110,97,108,0);
       let nalytics9 = 5.0;
          let typingx: Array<any> = [59, 925];
          let context6 = String.fromCharCode(97,100,100,114,101,115,115,101,115,0);
          let tumbnailS: Map<any, any> = new Map([[String.fromCharCode(97,102,105,108,116,101,114,0),852], [String.fromCharCode(111,110,118,101,114,115,97,116,105,111,110,0),943], [String.fromCharCode(104,97,115,104,102,114,101,101,100,101,115,116,114,111,121,0),900]]);
         tailB = `${tailB.length}`;
         typingx.push(tumbnailS.size | 2);
         context6 += `${typingx.length % (Math.max(8, tumbnailS.size))}`;
      while ((tailB.length * 4) >= 5) {
         tailB = `${tailB.length % (Math.max(7, parseInt(`${nalytics9}`)))}`;
         break;
      }
         nalytics9 *= (tailB == String.fromCharCode(80,0) ? parseInt(`${nalytics9}`) : tailB.length);
          let matchJ = String.fromCharCode(112,97,103,101,99,111,117,110,116,0);
         nalytics9 /= Math.max(5, parseInt(`${nalytics9}`) % 3);
         matchJ += `${matchJ.length * 3}`;
          let phoneshareR = 0.0;
         tailB = `${1 & parseInt(`${phoneshareR}`)}`;
         tailB = "2";
      debugx /= Math.max(5, parseFloat(`${selectT}`));

            if (result.success === false) {

   for (let b = 0; b < 1; b++) {
      right9 = `${helperb.size}`;
   }
   while (Array.from(placementC.values()).includes(debugx)) {
       let tumbnailT = 1.0;
       let cornerkickb = String.fromCharCode(109,97,103,101,0);
       let jingdongH: Map<any, any> = new Map([[String.fromCharCode(116,101,120,116,117,114,101,0),974], [String.fromCharCode(108,97,116,101,115,116,0),468]]);
       let robotoE: Array<any> = [833, 931];
      let becomeI = 5809922 <= robotoE.length;
      do {
         robotoE.push(robotoE.length >> (Math.min(Math.abs(3), 1)));
         if (becomeI) {
            break;
         }
      } while (becomeI && (!cornerkickb.endsWith(`${robotoE.length}`)));
       let handler3 = 1;
      if ((tumbnailT / (Math.max(7, parseFloat(`${cornerkickb.length}`)))) <= 4.1 || (tumbnailT / 4.1) <= 1.89) {
         cornerkickb += "3 - handler3";
      }
       let philippines2: Map<any, any> = new Map([[String.fromCharCode(115,108,101,101,112,0),793], [String.fromCharCode(115,112,101,99,105,102,105,101,114,0),685], [String.fromCharCode(112,97,110,101,115,0),967]]);
       let chinaL = false;
         robotoE = [philippines2.size % (Math.max(2, 4))];
      while ((parseInt(`${tumbnailT}`) + robotoE.length) > 4) {
         robotoE.push(cornerkickb.length | parseInt(`${tumbnailT}`));
         break;
      }
      while (cornerkickb.length < 1) {
         philippines2.set(`${tumbnailT}`, 2);
         break;
      }
       let h_managert = false;
       let stringV = true;
      let firebasen = 6669725.0 <= tumbnailT;
      do {
         tumbnailT *= (parseFloat(`${String.fromCharCode(52,0) == cornerkickb ? cornerkickb.length : robotoE.length}`));
         if (firebasen) {
            break;
         }
      } while (((4 * parseInt(`${tumbnailT}`)) == 1 && 4 == (4 * cornerkickb.length)) && firebasen);
      while (1 <= philippines2.size) {
          let lighta = 1.0;
          let screenz: Array<any> = [879, 423];
         cornerkickb += `${(2 - (stringV ? 5 : 3))}`;
         lighta += 1 >> (Math.min(2, screenz.length));
         screenz = [screenz.length];
         break;
      }
       let mbridgeP = 4.0;
       let nativeexs = 1.0;
      placementC.set(cornerkickb, cornerkickb.length * combine2.size);
      break;
   }
                throw result.message;
            }

            if (result.data && result.data.IPv4) {

   while (Array.from(helperb.keys()).includes(`${debugx}`)) {
       let alertC = true;
      while (!alertC || alertC) {
         alertC = (alertC ? !alertC : alertC);
         break;
      }
         alertC = (alertC ? alertC : alertC);
          let langH = 1.0;
          let defaultpredictionprofileE = 2;
          let entrys = 2.0;
         alertC = 50.15 >= langH;
         langH /= Math.max(2, 1);
         defaultpredictionprofileE *= defaultpredictionprofileE;
         entrys /= Math.max(3, 2);
      debugx -= parseFloat(`${helperb.size}`);
      break;
   }
   if ((movies0 << (Math.min(Math.abs(5), 4))) < 1 && 1 < (searchz.size << (Math.min(Math.abs(5), 3)))) {
      searchz.set(`${sharedO}`, 2);
   }
                ipAddress = result.data.IPv4;
            }
            return result.data;

        } catch (e: any) {
            console.error(`[Error getLocalIpAddress]: ${e.toString()}`);

   let networko = helperb.size >= 9044045;
   do {
      helperb = new Map([[right9, 2]]);
      if (networko) {
         break;
      }
   } while (networko && (3 == (5 + combine2.size) && (helperb.size + combine2.size) == 5));
      combine2.set(`${sharedO}`, 1);
            wwAbidetect.instance.setNetworkIp(ipAddress);
        }
        finally {

      placementC = new Map([[`${helperb.size}`, searchz.size + helperb.size]]);
   let tail3 = 5102435 >= helperb.size;
   do {
      helperb = new Map([[`${searchz.size}`, selectT]]);
      if (tail3) {
         break;
      }
   } while ((1 > (helperb.size / 4)) && tail3);
            wwAbidetect.instance.setNetworkIp(ipAddress);
        }
    };

    static getBottomNav = async () => {
       let unselected7 = String.fromCharCode(105,114,114,101,108,118,97,110,116,0);
    let footballl = 3;
    let mappingL = String.fromCharCode(102,97,100,101,100,0);
    let long_htz = 0.0;
    let liveendmodallogoW: Array<any> = [79, 542];
    let statisticst = true;
    let utils4 = String.fromCharCode(118,105,101,119,101,114,0);
    let weibox = false;
    let privatechatbge = 5.0;
    let shirtM = true;
    let componentsB = String.fromCharCode(110,101,116,119,111,114,107,110,101,119,0);
      long_htz *= 2 % (Math.max(parseInt(`${long_htz}`), 8));
       let predictionactive6: Array<any> = [82, 824];
       let boot_ = 3;
       let dplusk = 5;
      while (boot_ <= 3) {
         predictionactive6.push(3 % (Math.max(boot_, 8)));
         break;
      }
      if (4 > predictionactive6.length) {
         predictionactive6.push(dplusk);
      }
      while (predictionactive6.includes(boot_)) {
          let inviteq = String.fromCharCode(112,101,114,99,101,110,116,0);
          let awayI = 5.0;
          let abouth = 3.0;
         boot_ += 3;
         inviteq = `${parseInt(`${abouth}`)}`;
         awayI += parseFloat(`${inviteq.length}`);
         abouth *= parseInt(`${abouth}`);
         break;
      }
         predictionactive6.push(dplusk / (Math.max(3, 1)));
      while (2 >= (predictionactive6.length / (Math.max(3, 3)))) {
         boot_ >>= Math.min(4, Math.abs(dplusk));
         break;
      }
      while (!predictionactive6.includes(boot_)) {
         boot_ >>= Math.min(2, Math.abs(1 / (Math.max(dplusk, 6))));
         break;
      }
      while (4 < (boot_ | 5) || (dplusk | 5) < 2) {
         boot_ >>= Math.min(2, predictionactive6.length);
         break;
      }
         boot_ >>= Math.min(3, Math.abs(3 | predictionactive6.length));
         boot_ &= predictionactive6.length;
      long_htz -= footballl;

        try {

   if (4 < (4 % (Math.max(1, unselected7.length)))) {
       let backi = 5.0;
       let mutedV = String.fromCharCode(114,101,100,118,0);
         backi += parseInt(`${backi}`);
      while (mutedV.startsWith(`${backi}`)) {
          let predictionactiveV = false;
          let baseu = String.fromCharCode(103,97,116,105,110,103,95,113,95,49,57,0);
          let yellowvideolivea = 1;
         mutedV += `${(mutedV == String.fromCharCode(90,0) ? yellowvideolivea : mutedV.length)}`;
         predictionactiveV = !baseu.startsWith(`${predictionactiveV}`);
         baseu = `${baseu.length}`;
         yellowvideolivea &= (String.fromCharCode(70,0) == baseu ? (predictionactiveV ? 1 : 3) : baseu.length);
         break;
      }
       let predictionactiveu = 4.0;
       let defaultprofilepicQ = 2.0;
      if (2 == (mutedV.length | 1) && 3.69 == (4.38 * defaultprofilepicQ)) {
          let apps3 = 2.0;
          let ballm = String.fromCharCode(109,111,98,105,117,115,0);
          let tempnodatagifx = false;
         mutedV = `${(String.fromCharCode(69,0) == ballm ? parseInt(`${backi}`) : ballm.length)}`;
         apps3 -= (parseFloat(`${(tempnodatagifx ? 1 : 4) / (Math.max(parseInt(`${apps3}`), 1))}`));
         tempnodatagifx = 27.15 >= apps3;
      }
         defaultprofilepicQ *= parseFloat(`${1}`);
         predictionactiveu *= parseFloat(`${parseInt(`${defaultprofilepicQ}`)}`);
      unselected7 += `${parseInt(`${privatechatbge}`)}`;
   }
       let zhuboz = String.fromCharCode(99,117,114,114,0);
       let libreactnativejnii = String.fromCharCode(106,115,111,110,115,0);
       let chartq: Map<any, any> = new Map([[String.fromCharCode(119,105,114,101,102,114,97,109,101,0),378], [String.fromCharCode(112,114,101,109,105,117,109,0),729]]);
      for (let l = 0; l < 3; l++) {
          let mbbidY = String.fromCharCode(112,104,112,0);
          let other8 = 4.0;
         libreactnativejnii = `${chartq.size - 2}`;
         mbbidY = `${mbbidY.length}`;
         other8 /= Math.max(3, mbbidY.length);
      }
      if (5 < (2 & zhuboz.length)) {
         zhuboz += `${(String.fromCharCode(122,0) == libreactnativejnii ? libreactnativejnii.length : chartq.size)}`;
      }
      while (3 <= libreactnativejnii.length) {
         libreactnativejnii = "2";
         break;
      }
          let eventsplashY: Map<any, any> = new Map([[String.fromCharCode(114,105,112,101,109,100,0),false ], [String.fromCharCode(116,101,115,101,100,103,101,0),false ], [String.fromCharCode(102,112,109,98,0),true ]]);
          let darkl = String.fromCharCode(112,97,114,115,101,0);
         zhuboz = `${zhuboz.length}`;
         eventsplashY = new Map([[`${eventsplashY.size}`, (String.fromCharCode(48,0) == darkl ? eventsplashY.size : darkl.length)]]);
      while (zhuboz.length < 4) {
         zhuboz += `${libreactnativejnii.length * chartq.size}`;
         break;
      }
         libreactnativejnii = `${chartq.size}`;
      for (let f = 0; f < 3; f++) {
          let sigmobS = String.fromCharCode(112,117,116,110,117,109,112,97,115,115,101,115,0);
          let shootyesgoal6 = 5.0;
          let reminderH = false;
          let photoV = 0.0;
          let videojsC: Array<any> = [242, 729];
         chartq.set(libreactnativejnii, parseInt(`${photoV}`));
         sigmobS += `${2 << (Math.min(2, Math.abs(parseInt(`${shootyesgoal6}`))))}`;
         shootyesgoal6 *= parseFloat(`${videojsC.length >> (Math.min(sigmobS.length, 3))}`);
         reminderH = videojsC.includes(reminderH);
         photoV += (videojsC.length - (reminderH ? 3 : 4));
      }
      for (let a = 0; a < 3; a++) {
          let iconwatchnowY: Map<any, any> = new Map([[String.fromCharCode(115,119,102,104,97,115,104,0),String.fromCharCode(97,108,98,117,109,115,0)], [String.fromCharCode(115,99,111,112,101,0),String.fromCharCode(116,114,97,110,115,0)], [String.fromCharCode(108,97,109,112,0),String.fromCharCode(98,101,99,97,117,115,101,0)]]);
          let ying7: Map<any, any> = new Map([[String.fromCharCode(99,111,109,109,97,110,100,108,105,110,101,102,108,97,103,0),19], [String.fromCharCode(116,95,56,57,95,101,112,111,99,104,0),268], [String.fromCharCode(97,112,115,0),364]]);
          let libfbjniv = String.fromCharCode(105,110,118,105,116,97,116,105,111,110,0);
         libreactnativejnii = `${libreactnativejnii.length}`;
         iconwatchnowY.set(libfbjniv, ying7.size);
         ying7 = new Map([[`${ying7.size}`, 1 >> (Math.min(2, Math.abs(iconwatchnowY.size)))]]);
         libfbjniv += `${iconwatchnowY.size}`;
      }
         zhuboz += `${chartq.size / 3}`;
      utils4 += `${zhuboz.length << (Math.min(5, mappingL.length))}`;
            const result = await wwForegroundModules.get(wwCollection.appGetBottomNav, {
                query: {
                    channelId: UMENG_CHANNEL,
                    mobileOS: Platform.OS.toUpperCase(),
                },
            });

   let usernameO = 5180844 >= liveendmodallogoW.length;
   do {
      liveendmodallogoW = [mappingL.length];
      if (usernameO) {
         break;
      }
   } while ((3 <= liveendmodallogoW.length) && usernameO);
   for (let a = 0; a < 2; a++) {
      utils4 += `${mappingL.length % (Math.max(utils4.length, 1))}`;
   }

            if (result.success === false) {

       let bgvipxvod9 = false;
       let long_15: Array<any> = [578, 293, 505];
       let googleP = false;
      let rootD = googleP ? !googleP : googleP;
      do {
         googleP = long_15.length >= 84 || !googleP;
         if (rootD) {
            break;
         }
      } while (rootD && (5 >= long_15.length));
         long_15 = [((googleP ? 3 : 2) << (Math.min(Math.abs(1), 4)))];
      while (!bgvipxvod9) {
          let googlep = true;
          let buildf = 5.0;
          let scheduleZ = 4;
         bgvipxvod9 = (scheduleZ / (Math.max(buildf, 6))) == 86;
         googlep = (googlep ? googlep : !googlep);
         buildf += ((googlep ? 4 : 3));
         break;
      }
      while ((long_15.length >> (Math.min(Math.abs(5), 4))) < 4) {
         long_15 = [3];
         break;
      }
      let unreadM = 8298064 <= long_15.length;
      do {
         long_15 = [2];
         if (unreadM) {
            break;
         }
      } while ((!bgvipxvod9) && unreadM);
         long_15 = [(3 >> (Math.min(5, Math.abs((bgvipxvod9 ? 2 : 1)))))];
      if (3 == (long_15.length & 1)) {
         long_15 = [((googleP ? 5 : 3) ^ 3)];
      }
      let whistleorangef = 5963502 >= long_15.length;
      do {
          let sliderz = 3;
          let phone5: Array<any> = [873, 223];
          let uimanager6: Array<any> = [String.fromCharCode(98,117,98,98,108,101,115,0), String.fromCharCode(109,115,110,119,99,0)];
         long_15.push(sliderz);
         sliderz *= phone5.length | 2;
         phone5 = [1 & uimanager6.length];
         uimanager6.push(3);
         if (whistleorangef) {
            break;
         }
      } while ((bgvipxvod9) && whistleorangef);
      if ((2 & long_15.length) == 4 || 2 == long_15.length) {
         bgvipxvod9 = long_15.length <= 47 && !googleP;
      }
      statisticst = !statisticst || unselected7.length <= 23;
   let trophyA = liveendmodallogoW.length <= 7577634;
   do {
       let backgroundZ = String.fromCharCode(99,104,115,99,97,108,101,0);
          let overa: Array<any> = [846, 140, 804];
         backgroundZ = `${overa.length ^ 3}`;
       let interstitialv: Map<any, any> = new Map([[String.fromCharCode(117,110,108,105,110,107,101,100,0),104], [String.fromCharCode(105,111,115,0),830], [String.fromCharCode(101,120,112,114,101,115,115,0),834]]);
       let changeY: Map<any, any> = new Map([[String.fromCharCode(115,105,103,110,97,108,115,0),949], [String.fromCharCode(99,111,100,101,114,0),700], [String.fromCharCode(105,110,116,101,114,105,116,101,109,0),520]]);
      if (backgroundZ.includes(`${interstitialv.size}`)) {
         interstitialv = new Map([[`${changeY.size}`, backgroundZ.length]]);
      }
      liveendmodallogoW = [((statisticst ? 2 : 2) >> (Math.min(Math.abs(parseInt(`${privatechatbge}`)), 1)))];
      if (trophyA) {
         break;
      }
   } while (trophyA && (utils4.length < liveendmodallogoW.length));
                throw result.message;
            }

            wwAbidetect.instance.setTabConfig(result.data);

      utils4 = `${parseInt(`${long_htz}`)}`;
       let runtimeq = 5.0;
      for (let w = 0; w < 2; w++) {
         runtimeq -= parseFloat(`${parseInt(`${runtimeq}`) % (Math.max(parseInt(`${runtimeq}`), 2))}`);
      }
          let dycreatorB = String.fromCharCode(99,97,108,99,117,108,97,116,105,111,110,0);
          let plashI = 0.0;
          let bootsplashF = String.fromCharCode(97,115,115,101,116,115,0);
         runtimeq -= parseFloat(`${3}`);
         dycreatorB = `${(bootsplashF == String.fromCharCode(102,0) ? bootsplashF.length : parseInt(`${plashI}`))}`;
         plashI += parseInt(`${plashI}`) << (Math.min(dycreatorB.length, 3));
         runtimeq *= parseFloat(`${parseInt(`${runtimeq}`)}`);
      utils4 += `${footballl & 3}`;

            return result.data;

        } catch (e: any) {
            console.error(`[Error getBottomNav}]: ${e.toString()}`);

      privatechatbge -= parseFloat(`${footballl}`);
   if (3 > utils4.length && !weibox) {
       let statisticss = String.fromCharCode(99,95,49,53,95,110,117,108,108,0);
       let pagination2 = 3.0;
       let playj = 1.0;
      let videoy = 7952424.0 <= playj;
      do {
          let videoP: Map<any, any> = new Map([[String.fromCharCode(118,99,111,109,98,105,110,101,0),667], [String.fromCharCode(110,95,55,52,95,102,97,108,108,98,97,99,107,0),786]]);
          let agreement1 = 4.0;
          let videojsL = String.fromCharCode(104,95,56,52,0);
          let grayS = 2.0;
         playj -= statisticss.length ^ 3;
         videoP.set(videojsL, videojsL.length);
         agreement1 += parseFloat(`${3 << (Math.min(5, Math.abs(parseInt(`${grayS}`))))}`);
         grayS -= (videojsL == String.fromCharCode(95,0) ? parseInt(`${agreement1}`) : videojsL.length);
         if (videoy) {
            break;
         }
      } while ((parseInt(`${playj}`) < statisticss.length) && videoy);
         statisticss = `${statisticss.length}`;
      while (1.35 == (pagination2 - 2.85) || 1.54 == (playj - 2.85)) {
         pagination2 += parseFloat(`${statisticss.length & 3}`);
         break;
      }
      for (let x = 0; x < 3; x++) {
         statisticss += "1";
      }
      let sinaw = 5807682.0 <= pagination2;
      do {
         pagination2 -= parseFloat(`${statisticss.length - parseInt(`${pagination2}`)}`);
         if (sinaw) {
            break;
         }
      } while (sinaw && (3.45 >= pagination2));
      while (1 == (5 & statisticss.length) && (4.36 + pagination2) == 5.68) {
          let footballt = 4.0;
          let completes = String.fromCharCode(98,114,101,110,100,101,114,0);
          let hoverF = 0;
          let libfabricjnii = 2;
          let videojsB: Array<any> = [165, 161];
         pagination2 *= parseFloat(`${parseInt(`${playj}`)}`);
         footballt += parseFloat(`${videojsB.length}`);
         completes += `${completes.length}`;
         hoverF -= libfabricjnii * parseInt(`${footballt}`);
         libfabricjnii ^= 3 << (Math.min(Math.abs(parseInt(`${footballt}`)), 3));
         videojsB.push(3);
         break;
      }
      for (let i = 0; i < 2; i++) {
          let mbridgew = String.fromCharCode(101,116,104,0);
          let userO = String.fromCharCode(104,95,54,51,0);
          let completee = 0.0;
         pagination2 += parseFloat(`${3}`);
         mbridgew = `${userO.length | mbridgew.length}`;
         userO += `${1 * userO.length}`;
         completee -= (userO == String.fromCharCode(72,0) ? parseInt(`${completee}`) : userO.length);
      }
         pagination2 += parseFloat(`${statisticss.length}`);
      if ((parseInt(`${pagination2}`) + statisticss.length) >= 1 && (statisticss.length - 1) >= 5) {
          let crown7 = 0.0;
          let dependenciesN = String.fromCharCode(105,110,115,116,0);
         pagination2 /= Math.max(parseFloat(`${1}`), 1);
         crown7 += parseInt(`${crown7}`) % 3;
         dependenciesN += `${dependenciesN.length}`;
      }
      weibox = 97.25 <= long_htz;
   }
            throw e;
        }
    };

    static postLocation = async () => {
       let sidej = 3.0;
    let l_positionx = String.fromCharCode(114,101,116,114,105,101,118,101,100,0);
    let stringsW = String.fromCharCode(109,105,100,0);
    let searchbaru = 3.0;
    let reactnativeultimatelistviewl = 1.0;
    let starE = 1.0;
    let awayteamfieldo = 1;
    let live_ = 0;
    let foregroundb = 0;
    let downloadr: Map<any, any> = new Map([[String.fromCharCode(101,98,109,108,110,117,109,0),false ], [String.fromCharCode(112,114,101,115,117,98,109,105,116,0),true ]]);
    let iconshare9 = String.fromCharCode(114,97,116,105,111,0);
    let mappingq = String.fromCharCode(98,121,116,101,108,101,110,0);
    let anner3 = String.fromCharCode(114,101,97,100,113,0);
    let schedulerG = 4;
      live_ >>= Math.min(Math.abs(parseInt(`${reactnativeultimatelistviewl}`) / (Math.max(2, 10))), 3);
       let playercommonr = 2;
          let reactnavigationp = false;
         playercommonr |= 3 + playercommonr;
         reactnavigationp = !reactnavigationp;
         playercommonr -= 1 & playercommonr;
          let clearL = String.fromCharCode(114,101,103,105,115,116,101,114,105,110,103,0);
         playercommonr >>= Math.min(Math.abs(playercommonr & clearL.length), 5);
      iconshare9 += `${parseInt(`${starE}`) << (Math.min(2, Math.abs(parseInt(`${reactnativeultimatelistviewl}`))))}`;
   while ((stringsW.length + parseInt(`${searchbaru}`)) > 3) {
       let dnewarchdefaultsW = 4.0;
       let member_: Map<any, any> = new Map([[String.fromCharCode(109,117,109,98,97,105,0),true ], [String.fromCharCode(110,111,116,105,102,105,99,97,116,111,110,115,0),false ], [String.fromCharCode(100,105,114,101,99,116,108,121,0),true ]]);
       let componentregistryN = 1;
       let vietnam8 = String.fromCharCode(112,116,120,99,0);
       let crownf = true;
          let reacti = String.fromCharCode(105,112,104,111,110,101,0);
          let unreadx: Map<any, any> = new Map([[String.fromCharCode(102,95,49,56,95,100,105,114,101,99,116,108,121,0),true ], [String.fromCharCode(104,119,102,114,97,109,101,0),false ]]);
          let footballfield7 = String.fromCharCode(114,101,115,105,115,116,97,110,99,101,0);
         componentregistryN &= componentregistryN ^ unreadx.size;
         reacti += `${3 % (Math.max(10, footballfield7.length))}`;
         unreadx.set(reacti, reacti.length);
         footballfield7 = "1";
          let stepn = String.fromCharCode(115,117,98,115,97,109,112,108,105,110,103,0);
          let emptye = 5.0;
         crownf = String.fromCharCode(118,0) == stepn;
         stepn = `${parseInt(`${emptye}`) - parseInt(`${emptye}`)}`;
         crownf = vietnam8 == String.fromCharCode(90,0);
          let e_managerZ: Array<any> = [81, 675];
         vietnam8 = `${parseInt(`${dnewarchdefaultsW}`)}`;
         e_managerZ.push(3 | e_managerZ.length);
       let floater2 = String.fromCharCode(114,101,115,111,108,117,116,105,111,110,0);
       let short_1_4 = String.fromCharCode(100,101,108,101,116,105,111,110,115,0);
       let gifgoal7 = String.fromCharCode(119,104,97,116,115,0);
      for (let m = 0; m < 3; m++) {
         componentregistryN ^= 1;
      }
      if (!short_1_4.endsWith(`${gifgoal7.length}`)) {
         short_1_4 = `${member_.size ^ componentregistryN}`;
      }
      while (1.67 < (dnewarchdefaultsW - 5.27) && (member_.size & 4) < 2) {
         member_.set(short_1_4, componentregistryN);
         break;
      }
      while (4 < (3 % (Math.max(6, componentregistryN))) && 3 < (member_.size % (Math.max(1, componentregistryN)))) {
         member_ = new Map([[`${dnewarchdefaultsW}`, componentregistryN & 2]]);
         break;
      }
         componentregistryN /= Math.max(componentregistryN * 2, 1);
       let diceE = 4.0;
      if ((short_1_4.length / 3) > 1 && 3 > (member_.size / (Math.max(9, short_1_4.length)))) {
         member_.set(`${crownf}`, (String.fromCharCode(88,0) == short_1_4 ? short_1_4.length : (crownf ? 4 : 5)));
      }
          let androida: Array<any> = [679, 771];
         gifgoal7 = `${floater2.length | short_1_4.length}`;
         androida = [androida.length / (Math.max(2, 4))];
      if (!gifgoal7.endsWith(`${crownf}`)) {
         gifgoal7 += `${(String.fromCharCode(81,0) == vietnam8 ? componentregistryN : vietnam8.length)}`;
      }
      searchbaru /= Math.max(3, parseFloat(`${3 & iconshare9.length}`));
      break;
   }

        try {

   let redgoalG = String.fromCharCode(111,49,95,95,109,111,120,54,53,0) == l_positionx;
   do {
      l_positionx = `${awayteamfieldo}`;
      if (redgoalG) {
         break;
      }
   } while ((!stringsW.includes(`${l_positionx.length}`)) && redgoalG);
   while ((parseInt(`${starE}`) / (Math.max(downloadr.size, 10))) < 5 && 5 < (downloadr.size / (Math.max(5, 7)))) {
      downloadr = new Map([[l_positionx, l_positionx.length]]);
      break;
   }
      stringsW = `${parseInt(`${reactnativeultimatelistviewl}`)}`;
            const result = await wwForegroundModules.post(wwCollection.appPostLocation, {
                body: {
                    ip_address: wwAbidetect.instance.ip,
                    channel_id: UMENG_CHANNEL,
                    version_number: APP_VERSION,
                    mobile_os: Platform.OS,
                    product: APP_NAME_CONST + "-" + Platform.OS.toUpperCase(),
                    mobile_model: "HUAWEIP20",
                    
                },
            });

   if (2 > (mappingq.length % 5)) {
       let darkC: Map<any, any> = new Map([[String.fromCharCode(122,95,51,49,0),false ], [String.fromCharCode(99,112,117,117,115,101,100,95,98,95,56,0),true ], [String.fromCharCode(100,101,108,97,121,115,0),true ]]);
       let combineA: Array<any> = [480, 417];
       let iconarrowrightU = 0;
         iconarrowrightU |= 2;
      if (2 <= (iconarrowrightU / 1) && (iconarrowrightU / 1) <= 2) {
          let o_managerz = String.fromCharCode(97,117,100,105,111,105,110,116,101,114,108,101,97,118,101,0);
          let active9 = true;
          let jingdongg = String.fromCharCode(115,116,97,116,115,0);
          let skipp = true;
          let manifest7: Map<any, any> = new Map([[String.fromCharCode(100,105,114,101,99,116,100,0),843], [String.fromCharCode(97,97,99,116,97,98,0),898]]);
         iconarrowrightU += o_managerz.length << (Math.min(4, jingdongg.length));
         o_managerz += "2";
         active9 = (!active9 ? !skipp : !active9);
         jingdongg = `${((skipp ? 1 : 5) * 2)}`;
         manifest7 = new Map([[`${skipp}`, ((skipp ? 2 : 4))]]);
      }
      if ((iconarrowrightU ^ darkC.size) == 2 && 2 == (iconarrowrightU ^ darkC.size)) {
          let kuaishoug = 2;
          let combinede = String.fromCharCode(102,105,108,101,114,101,97,100,115,116,114,101,97,109,0);
          let shootyesgoals = 5.0;
          let goalq: Array<any> = [854, 872];
         darkC.set(combinede, kuaishoug * combinede.length);
         kuaishoug *= parseInt(`${shootyesgoals}`) >> (Math.min(3, Math.abs(2)));
         shootyesgoals -= goalq.length << (Math.min(5, Math.abs(parseInt(`${shootyesgoals}`))));
         goalq = [parseInt(`${shootyesgoals}`)];
      }
         iconarrowrightU %= Math.max(combineA.length, 3);
       let notificationfillemptyq = String.fromCharCode(112,117,108,115,101,115,0);
       let leaguev = String.fromCharCode(101,95,54,55,95,108,111,99,107,110,101,115,115,0);
      if (2 <= iconarrowrightU) {
         notificationfillemptyq += "3";
      }
      if ((notificationfillemptyq.length & iconarrowrightU) > 2 || 2 > (iconarrowrightU & notificationfillemptyq.length)) {
         iconarrowrightU &= 2;
      }
      if ((1 - iconarrowrightU) >= 4 && 1 >= (1 - iconarrowrightU)) {
         iconarrowrightU |= combineA.length >> (Math.min(Math.abs(1), 4));
      }
          let photoO = String.fromCharCode(105,116,108,101,0);
          let mbbidy = String.fromCharCode(108,111,99,97,108,101,115,0);
         combineA = [darkC.size];
         photoO = `${mbbidy.length}`;
         mbbidy = `${1 + photoO.length}`;
      downloadr.set(l_positionx, l_positionx.length >> (Math.min(Math.abs(1), 2)));
   }
       let libfollyw = 1.0;
       let downarrowr = 3;
       let renewZ = true;
      for (let m = 0; m < 1; m++) {
         libfollyw += parseFloat(`${2}`);
      }
          let showY = 5;
         libfollyw /= Math.max(parseFloat(`${showY}`), 4);
         libfollyw += parseFloat(`${parseInt(`${libfollyw}`) | 1}`);
         downarrowr >>= Math.min(Math.abs(downarrowr & 2), 1);
      for (let f = 0; f < 1; f++) {
         libfollyw *= parseFloat(`${3}`);
      }
      if (downarrowr > 1 && 4 > (downarrowr + 1)) {
         downarrowr ^= 3;
      }
      let stats_ = 9745704 <= downarrowr;
      do {
         downarrowr %= Math.max(4, downarrowr | parseInt(`${libfollyw}`));
         if (stats_) {
            break;
         }
      } while (stats_ && ((downarrowr ^ 5) == 5 || downarrowr == 5));
         downarrowr |= downarrowr;
      while ((libfollyw / 3.19) <= 3.31 || !renewZ) {
         libfollyw -= parseFloat(`${3 >> (Math.min(Math.abs(parseInt(`${libfollyw}`)), 1))}`);
         break;
      }
      sidej += parseFloat(`${awayteamfieldo | 2}`);
   if (1.34 >= sidej) {
      downloadr = new Map([[mappingq, mappingq.length | 1]]);
   }

            if (result.success === false) {

   let whitetickw = foregroundb <= 5671857;
   do {
       let wind4: Array<any> = [311, 378];
          let indicatorT: Array<any> = [959, 431];
          let submitK = 1.0;
         wind4.push(parseInt(`${submitK}`));
         indicatorT = [2 * indicatorT.length];
         submitK += parseFloat(`${indicatorT.length * indicatorT.length}`);
      for (let e = 0; e < 1; e++) {
         wind4 = [3 + wind4.length];
      }
      let stationsH = 7152037 <= wind4.length;
      do {
          let types1 = String.fromCharCode(99,111,110,118,101,114,116,101,100,0);
          let navigationo = 4.0;
         wind4.push((types1 == String.fromCharCode(50,0) ? types1.length : parseInt(`${navigationo}`)));
         if (stationsH) {
            break;
         }
      } while ((wind4.includes(wind4.length)) && stationsH);
      foregroundb /= Math.max(4, 2);
      if (whitetickw) {
         break;
      }
   } while (whitetickw && (4 >= foregroundb));
      sidej *= (parseFloat(`${String.fromCharCode(90,0) == stringsW ? downloadr.size : stringsW.length}`));
   if (2.78 <= (searchbaru - 4.75)) {
      foregroundb &= live_ << (Math.min(Math.abs(3), 3));
   }
                throw result.message;
            }

            return result.data;

        } catch (e: any) {
            console.error(`[Error postLocation}]: ${e.toString()}`);

      foregroundb *= stringsW.length;
       let optionsk = String.fromCharCode(98,101,114,110,111,117,108,108,105,0);
       let binddatasb = 5.0;
       let baseQ: Array<any> = [19, 447, 41];
      let viewerh = baseQ.length <= 5036178;
      do {
         baseQ = [(String.fromCharCode(68,0) == optionsk ? parseInt(`${binddatasb}`) : optionsk.length)];
         if (viewerh) {
            break;
         }
      } while (viewerh && (baseQ.includes(binddatasb)));
          let floatinge = String.fromCharCode(99,114,111,115,115,102,97,100,101,0);
          let hejiA = String.fromCharCode(105,111,101,114,114,110,111,109,101,109,0);
         baseQ = [(String.fromCharCode(107,0) == optionsk ? optionsk.length : parseInt(`${binddatasb}`))];
         floatinge += `${(String.fromCharCode(99,0) == hejiA ? floatinge.length : hejiA.length)}`;
      let humidity5 = 7578706 <= baseQ.length;
      do {
         baseQ.push(2 >> (Math.min(Math.abs(parseInt(`${binddatasb}`)), 4)));
         if (humidity5) {
            break;
         }
      } while ((baseQ.length <= parseInt(`${binddatasb}`)) && humidity5);
      while ((baseQ.length * optionsk.length) > 1 || (optionsk.length * baseQ.length) > 1) {
         optionsk = `${baseQ.length}`;
         break;
      }
      let leaguek = optionsk.length >= 6407277;
      do {
          let logoutD = String.fromCharCode(119,111,114,107,105,110,103,0);
          let whitetickl = String.fromCharCode(116,101,97,114,100,111,119,110,0);
         optionsk = `${baseQ.length ^ 3}`;
         logoutD = `${logoutD.length}`;
         whitetickl = `${logoutD.length}`;
         if (leaguek) {
            break;
         }
      } while ((optionsk.length == parseInt(`${binddatasb}`)) && leaguek);
         binddatasb += parseInt(`${binddatasb}`) % (Math.max(5, baseQ.length));
      for (let y = 0; y < 3; y++) {
         binddatasb += 2;
      }
       let reminders = String.fromCharCode(97,115,112,101,99,116,0);
         baseQ.push(2);
      sidej += parseFloat(`${downloadr.size << (Math.min(Math.abs(1), 2))}`);
   while (!l_positionx.startsWith(`${foregroundb}`)) {
      l_positionx = `${live_}`;
      break;
   }
            throw e;
        }
    };

    static getHomeNav = async () => {
       let yellowg = String.fromCharCode(100,105,108,97,116,101,0);
    let submiti = 2.0;
    let skip_: Array<any> = [568, 554, 214];
    let dplusR: Array<any> = [827, 957, 928];
    let condensedR = 1;
    let string9 = 5.0;
    let graphD = false;
    let controlsE = 0.0;
    let iconsubssuccessJ = String.fromCharCode(120,95,55,55,95,116,119,111,115,116,97,103,101,0);
    let becomeX = String.fromCharCode(100,101,108,116,97,0);
    let blackB = 4.0;
   let grayh = 5066142.0 >= string9;
   do {
      string9 /= Math.max(dplusR.length, 5);
      if (grayh) {
         break;
      }
   } while ((yellowg.length <= string9) && grayh);
      string9 *= 3;

        try {

   if (2 < (skip_.length - 1)) {
      skip_ = [parseInt(`${controlsE}`) << (Math.min(3, Math.abs(3)))];
   }
       let chinasameF: Map<any, any> = new Map([[String.fromCharCode(108,97,116,0),516], [String.fromCharCode(108,105,98,121,117,118,0),686], [String.fromCharCode(116,111,107,101,110,115,0),880]]);
       let hooks5 = String.fromCharCode(112,102,114,97,109,101,0);
      let privacyw = chinasameF.size <= 9636410;
      do {
         chinasameF = new Map([[`${chinasameF.size}`, hooks5.length]]);
         if (privacyw) {
            break;
         }
      } while (((hooks5.length | chinasameF.size) <= 5 || (chinasameF.size | 5) <= 1) && privacyw);
      if (5 < (chinasameF.size / (Math.max(hooks5.length, 5))) || 2 < (5 / (Math.max(6, hooks5.length)))) {
          let lesss: Array<any> = [824, 635, 27];
          let teamy = 5.0;
         hooks5 += `${parseInt(`${teamy}`)}`;
         lesss.push(2);
         teamy += 1 & lesss.length;
      }
       let dycreatorw: Map<any, any> = new Map([[String.fromCharCode(117,110,102,108,97,116,116,101,110,101,100,0),String.fromCharCode(114,95,55,56,95,101,114,114,99,111,100,101,0)], [String.fromCharCode(99,97,110,100,105,100,97,116,101,115,0),String.fromCharCode(114,95,50,51,95,114,101,108,101,97,115,101,0)]]);
         hooks5 = `${3 | chinasameF.size}`;
      if (3 > dycreatorw.size) {
         hooks5 = `${1 | chinasameF.size}`;
      }
          let gdtadv7: Array<any> = [129, 732];
          let historyS = String.fromCharCode(114,101,117,115,101,0);
          let privatechatbgX = String.fromCharCode(108,95,50,53,95,97,108,108,111,119,105,0);
         hooks5 += `${1 + chinasameF.size}`;
         gdtadv7.push(historyS.length ^ 1);
         historyS += `${gdtadv7.length}`;
         privatechatbgX += `${gdtadv7.length >> (Math.min(historyS.length, 2))}`;
      string9 -= (skip_.length << (Math.min(1, Math.abs((graphD ? 3 : 2)))));
            const result = await wwForegroundModules.get(wwCollection.homeGetNav, {
                query: {
                    channelId: UMENG_CHANNEL,
                    platformId: Platform.OS,
                }
            });

   while ((string9 * 3.4) > 2.34) {
      string9 += 3 - parseInt(`${controlsE}`);
      break;
   }
      skip_.push(2);

            if (result.success === false) {

   for (let u = 0; u < 2; u++) {
      condensedR ^= dplusR.length - 2;
   }
      submiti /= Math.max(parseFloat(`${parseInt(`${string9}`)}`), 2);
                throw result.message;
            }

            return result.data as wwProduct[];

        } catch (e: any) {
            console.error(`[Error getHomeNav}]: ${e.toString()}`);

       let largebrightnessq: Array<any> = [String.fromCharCode(110,111,110,110,101,103,0), String.fromCharCode(119,101,108,99,104,0), String.fromCharCode(116,104,114,101,97,100,110,97,109,101,95,54,95,55,57,0)];
         largebrightnessq.push(largebrightnessq.length / 3);
      while (5 <= (largebrightnessq.length + largebrightnessq.length) || 1 <= (largebrightnessq.length + 5)) {
          let clubX = String.fromCharCode(111,112,116,105,109,97,108,0);
         largebrightnessq.push(3 % (Math.max(8, clubX.length)));
         break;
      }
         largebrightnessq.push(largebrightnessq.length);
      controlsE -= skip_.length;
       let reactnativejsc: Map<any, any> = new Map([[String.fromCharCode(118,99,111,119,112,116,114,0),282], [String.fromCharCode(99,100,101,114,114,111,114,0),610]]);
         reactnativejsc = new Map([[`${reactnativejsc.size}`, reactnativejsc.size]]);
         reactnativejsc.set(`${reactnativejsc.size}`, 1 | reactnativejsc.size);
      for (let j = 0; j < 1; j++) {
         reactnativejsc = new Map([[`${reactnativejsc.size}`, reactnativejsc.size]]);
      }
      dplusR = [condensedR + 3];
            throw e;
        }
    };

    static getHomePages = async (id: number, isVip: boolean = false,) => {
       let favoriteN: Array<any> = [567, 16];
    let merger6 = 5.0;
    let pointl = String.fromCharCode(100,105,115,112,111,115,105,116,105,111,110,0);
    let enewsshareM: Map<any, any> = new Map([[String.fromCharCode(112,97,99,107,101,116,105,122,97,116,105,111,110,0),true ], [String.fromCharCode(110,112,112,115,99,97,108,101,0),true ], [String.fromCharCode(114,101,97,99,116,105,111,110,115,0),false ]]);
    let codeB = 3;
    let static_on = false;
    let champion_: Map<any, any> = new Map([[String.fromCharCode(110,95,56,52,95,116,121,112,101,115,116,114,0),String.fromCharCode(98,101,108,111,110,103,115,0)], [String.fromCharCode(100,101,114,101,103,105,115,116,101,114,0),String.fromCharCode(116,101,115,115,101,108,97,116,101,0)], [String.fromCharCode(100,105,102,102,0),String.fromCharCode(108,101,118,97,114,105,110,116,0)]]);
    let bodan5 = false;
    let backiconf = 5.0;
    let texti = true;
    let settingsY: Array<any> = [278, 549];
       let invite5 = false;
       let iconsaveimage7 = String.fromCharCode(99,100,108,109,115,0);
       let redgoalm = false;
      while (iconsaveimage7.startsWith(`${invite5}`)) {
          let redgoald = 1;
         invite5 = !invite5;
         redgoald += redgoald;
         break;
      }
          let privacyP = String.fromCharCode(117,110,118,111,116,101,0);
          let long_6A = 3.0;
          let penaltygoalT = String.fromCharCode(111,103,103,101,114,0);
         invite5 = long_6A >= 93.5 || !invite5;
         privacyP = `${penaltygoalT.length / (Math.max(1, 7))}`;
         long_6A -= parseFloat(`${3}`);
         penaltygoalT = "1";
         iconsaveimage7 = `${((invite5 ? 2 : 5))}`;
          let result0 = String.fromCharCode(109,111,100,110,112,102,0);
         redgoalm = result0.length > 66;
      for (let v = 0; v < 1; v++) {
         iconsaveimage7 = `${((redgoalm ? 3 : 2) ^ 1)}`;
      }
      for (let z = 0; z < 2; z++) {
         redgoalm = !invite5;
      }
          let defaultprofilepicy = String.fromCharCode(115,104,111,114,116,101,110,0);
          let acceptedh = 1;
          let libpangleflippedo: Array<any> = [942, 744];
         iconsaveimage7 += `${acceptedh + 2}`;
         defaultprofilepicy += `${libpangleflippedo.length}`;
         acceptedh *= libpangleflippedo.length / (Math.max(defaultprofilepicy.length, 10));
      while (invite5) {
         invite5 = (87 >= (iconsaveimage7.length - (!redgoalm ? iconsaveimage7.length : 87)));
         break;
      }
         iconsaveimage7 = `${(iconsaveimage7 == String.fromCharCode(51,0) ? iconsaveimage7.length : (redgoalm ? 3 : 1))}`;
      enewsshareM = new Map([[`${favoriteN.length}`, favoriteN.length]]);
      merger6 += parseFloat(`${champion_.size}`);

        try {

      pointl += "3";
      codeB *= codeB - 3;
            const result = await wwForegroundModules.get(wwCollection.homeGetPages, {
                query: {
                    id: id,
                    platform: Platform.OS,
                    channelId: UMENG_CHANNEL,
                    appName: APP_NAME_CONST,
                    ads: !isVip,
                },
            });

   if (!pointl.includes(`${merger6}`)) {
      pointl += "2";
   }
      enewsshareM.set(`${merger6}`, 1);

            if (result.success === false) {

       let turnR = 2;
       let singlee: Array<any> = [276, 606, 110];
       let iconscheduleF: Array<any> = [705, 487, 988];
      for (let e = 0; e < 1; e++) {
         singlee.push(iconscheduleF.length * singlee.length);
      }
      if (5 == iconscheduleF.length) {
         iconscheduleF.push(3);
      }
       let profile2 = 3.0;
      if ((2 << (Math.min(3, singlee.length))) > 5) {
         profile2 /= Math.max(1, iconscheduleF.length << (Math.min(4, Math.abs(turnR))));
      }
         turnR &= 1;
          let reward5: Map<any, any> = new Map([[String.fromCharCode(107,95,57,52,95,122,105,103,122,97,103,0),427], [String.fromCharCode(116,95,54,51,95,110,101,103,97,116,101,100,0),457]]);
          let aboutc: Map<any, any> = new Map([[String.fromCharCode(104,108,105,110,101,0),String.fromCharCode(98,101,97,114,105,110,103,0)], [String.fromCharCode(117,101,117,101,0),String.fromCharCode(103,111,112,115,0)], [String.fromCharCode(100,101,99,0),String.fromCharCode(97,108,112,104,97,98,101,116,115,0)]]);
         profile2 /= Math.max(2 / (Math.max(4, turnR)), 5);
         reward5 = new Map([[`${reward5.size}`, 2 ^ reward5.size]]);
         aboutc.set(`${aboutc.size}`, aboutc.size << (Math.min(Math.abs(reward5.size), 1)));
         turnR |= parseInt(`${profile2}`) + turnR;
         singlee.push(iconscheduleF.length & singlee.length);
          let dialogz: Array<any> = [String.fromCharCode(115,101,101,107,104,101,97,100,0), String.fromCharCode(116,114,97,106,101,99,116,111,114,121,0)];
          let humidityg = false;
         iconscheduleF = [singlee.length];
         dialogz = [dialogz.length ^ 3];
         humidityg = !humidityg;
      merger6 += parseFloat(`${turnR * 3}`);
      favoriteN.push(2);
                throw result.message;
            }

            return result.data as wwLibreactperfloggerjni;

        } catch (e: any) {
            console.error(`[Error getHomePages}]: ${e.toString()}`);

       let controly = 1.0;
      while (1.54 >= controly) {
         controly /= Math.max(parseInt(`${controly}`) % 3, 2);
         break;
      }
         controly += parseInt(`${controly}`);
      for (let a = 0; a < 3; a++) {
         controly /= Math.max(1 - parseInt(`${controly}`), 3);
      }
      favoriteN = [champion_.size];
   for (let m = 0; m < 2; m++) {
      enewsshareM.set(`${bodan5}`, (2 << (Math.min(5, Math.abs((bodan5 ? 4 : 5))))));
   }
            throw e;
        }
    };

    static getXVodList = async (page: number = 1) => {
       let reactnativejsF = String.fromCharCode(100,97,116,97,114,0);
    let mutedM = String.fromCharCode(98,121,114,121,105,0);
    let redgoalR = String.fromCharCode(99,104,97,110,103,105,110,103,0);
    let dacccfaabfbcbadeebddcabacdffdbl = 1.0;
    let tumbnaill = true;
    let chartA: Map<any, any> = new Map([[String.fromCharCode(98,101,116,104,115,111,102,116,118,105,100,101,111,0),true ], [String.fromCharCode(114,101,117,115,97,98,108,101,0),true ], [String.fromCharCode(106,117,108,105,97,110,100,97,121,0),true ]]);
    let signinupl = String.fromCharCode(100,101,115,116,0);
    let proxyO = 1.0;
    let eventsplash_ = 5.0;
    let downarroww = 5;
    let mbbanner8 = false;
    let searchbarp = String.fromCharCode(109,101,109,100,98,0);
    let iconsubssuccessm = String.fromCharCode(109,111,109,101,110,116,115,0);
      reactnativejsF += `${(redgoalR == String.fromCharCode(80,0) ? redgoalR.length : reactnativejsF.length)}`;
      eventsplash_ -= parseFloat(`${parseInt(`${eventsplash_}`)}`);
   if (2 < (downarroww % 3)) {
      signinupl += `${mutedM.length & signinupl.length}`;
   }

        try {

   for (let q = 0; q < 1; q++) {
       let becomeE = 5;
       let iconnointernet3 = 2.0;
          let matchi: Map<any, any> = new Map([[String.fromCharCode(110,111,116,0),String.fromCharCode(108,97,116,101,98,105,110,100,105,110,103,115,121,109,98,111,108,116,97,98,108,101,95,105,95,57,0)], [String.fromCharCode(104,99,104,114,111,109,97,0),String.fromCharCode(115,112,111,110,115,111,114,101,100,0)], [String.fromCharCode(115,99,114,111,108,108,101,100,0),String.fromCharCode(105,109,105,116,97,116,101,0)]]);
          let uploadg = String.fromCharCode(97,115,110,0);
          let regengh = String.fromCharCode(115,116,97,114,116,115,0);
         iconnointernet3 *= (parseFloat(`${String.fromCharCode(122,0) == regengh ? becomeE : regengh.length}`));
         matchi = new Map([[`${matchi.size}`, matchi.size >> (Math.min(uploadg.length, 1))]]);
         uploadg += `${uploadg.length - matchi.size}`;
          let arrowupk = 4;
          let mathK = 2.0;
         becomeE >>= Math.min(Math.abs(arrowupk | 2), 3);
         arrowupk /= Math.max(2, parseInt(`${mathK}`));
         iconnointernet3 *= parseFloat(`${becomeE % 3}`);
          let upgrade1 = false;
         becomeE %= Math.max(2, 4);
         upgrade1 = !upgrade1;
      for (let i = 0; i < 1; i++) {
         becomeE ^= 3 + becomeE;
      }
         becomeE ^= becomeE;
      reactnativejsF = `${((mbbanner8 ? 5 : 1))}`;
   }
       let gemfilet = String.fromCharCode(115,116,97,99,104,0);
      if (5 <= gemfilet.length) {
         gemfilet = `${gemfilet.length}`;
      }
      let catalogY = gemfilet.length <= 8015615;
      do {
          let buttonH = String.fromCharCode(112,105,100,95,99,95,49,48,48,0);
         gemfilet += `${gemfilet.length ^ buttonH.length}`;
         if (catalogY) {
            break;
         }
      } while (catalogY && (gemfilet != gemfilet));
       let subsu = 1;
      signinupl += "1";
      proxyO -= parseFloat(`${mutedM.length << (Math.min(Math.abs(1), 3))}`);
            const result = await wwForegroundModules.get(wwCollection.homeGetPages, {
                query: {
                    id: 99,
                    platform: Platform.OS,
                    channelId: UMENG_CHANNEL,
                    appName: APP_NAME_CONST,
                    page: page,
                    limit: 6
                },
            });

   for (let x = 0; x < 2; x++) {
       let arrowc = String.fromCharCode(99,97,99,104,101,115,105,122,101,0);
       let indicator8 = String.fromCharCode(121,117,118,99,111,110,102,105,103,105,109,97,103,101,0);
      while (indicator8.length == 4) {
         arrowc += "2";
         break;
      }
         indicator8 = `${arrowc.length}`;
      if (!indicator8.startsWith(arrowc)) {
         indicator8 = `${3 - indicator8.length}`;
      }
      while (arrowc == String.fromCharCode(80,0)) {
         indicator8 = `${indicator8.length}`;
         break;
      }
      if (indicator8 == arrowc) {
          let privacyt = String.fromCharCode(102,111,114,109,0);
         arrowc += `${indicator8.length / 1}`;
         privacyt += "3";
      }
      let largebrightnessa = indicator8.length <= 9398964;
      do {
          let resenda = 5.0;
          let statistics8 = 5.0;
         indicator8 = `${(indicator8 == String.fromCharCode(77,0) ? arrowc.length : indicator8.length)}`;
         resenda /= Math.max(parseFloat(`${parseInt(`${resenda}`) << (Math.min(Math.abs(parseInt(`${statistics8}`)), 2))}`), 3);
         statistics8 *= parseInt(`${resenda}`);
         if (largebrightnessa) {
            break;
         }
      } while ((indicator8.length >= 2) && largebrightnessa);
      signinupl = `${2 & redgoalR.length}`;
   }
   while ((signinupl.length - downarroww) < 4 || 2 < (4 - signinupl.length)) {
      downarroww -= 3 << (Math.min(4, signinupl.length));
      break;
   }
   while ((proxyO + 3) <= 4.63) {
       let hoverb: Map<any, any> = new Map([[String.fromCharCode(97,112,99,109,0),530], [String.fromCharCode(114,101,108,101,97,115,101,100,0),759]]);
         hoverb.set(`${hoverb.size}`, hoverb.size);
       let iconpipexpandv: Map<any, any> = new Map([[String.fromCharCode(100,105,112,111,115,97,98,108,101,95,117,95,51,56,0),false ], [String.fromCharCode(105,110,116,101,114,114,117,112,116,0),true ]]);
      for (let e = 0; e < 2; e++) {
         iconpipexpandv = new Map([[`${iconpipexpandv.size}`, hoverb.size]]);
      }
      eventsplash_ *= parseFloat(`${hoverb.size}`);
      break;
   }
            if (result.success === false) {

      tumbnaill = String.fromCharCode(74,0) == mutedM && 24 >= chartA.size;
       let chata: Array<any> = [147, 790];
       let right1 = 2.0;
      while ((chata.length - parseInt(`${right1}`)) > 5 || 5.66 > (parseFloat(`${chata.length}`) - right1)) {
         right1 += parseFloat(`${chata.length - parseInt(`${right1}`)}`);
         break;
      }
          let phoneshareo = 1.0;
         right1 /= Math.max(3, parseFloat(`${parseInt(`${right1}`)}`));
         phoneshareo /= Math.max(parseFloat(`${2}`), 2);
       let bannerx = 5.0;
          let hongkong7 = 0.0;
         right1 -= parseFloat(`${3 + parseInt(`${right1}`)}`);
         hongkong7 /= Math.max(parseInt(`${hongkong7}`) + 1, 5);
         chata.push(chata.length);
      while ((right1 + bannerx) == 2.10) {
         right1 *= parseFloat(`${parseInt(`${bannerx}`)}`);
         break;
      }
      eventsplash_ -= parseFloat(`${reactnativejsF.length}`);
      tumbnaill = !mbbanner8;
                throw result.message;
            }

            result.data.categories.forEach((cat: wwDownloadingScheduler) => {
                cat.vod_list.forEach((vod: wwControl) => {
                    vod.vod_source_name = cat.vod_source_name;
                })
            })

      searchbarp += "1";
   let logouserJ = 4942413.0 <= proxyO;
   do {
       let basketballhometeamg = 3.0;
       let eventsplashP = 0.0;
          let videocommonh = true;
         basketballhometeamg /= Math.max(1, parseInt(`${eventsplashP}`) << (Math.min(5, Math.abs(2))));
         videocommonh = !videocommonh;
          let reviewW = 4.0;
          let buildc = 3;
         eventsplashP -= parseFloat(`${parseInt(`${reviewW}`) | 2}`);
         reviewW *= parseFloat(`${buildc}`);
      for (let p = 0; p < 1; p++) {
         eventsplashP += parseFloat(`${parseInt(`${basketballhometeamg}`) << (Math.min(3, Math.abs(3)))}`);
      }
         basketballhometeamg += 3 | parseInt(`${basketballhometeamg}`);
          let jingdongf = 0;
          let chartd = 5.0;
          let mbbanner6: Map<any, any> = new Map([[String.fromCharCode(116,98,108,101,110,100,0),972], [String.fromCharCode(99,111,110,102,105,103,117,114,101,114,95,111,95,57,57,0),350], [String.fromCharCode(97,115,99,105,105,105,110,100,101,120,0),440]]);
         basketballhometeamg -= parseInt(`${basketballhometeamg}`);
         jingdongf %= Math.max(5, parseInt(`${chartd}`));
         chartd /= Math.max(parseFloat(`${jingdongf & 3}`), 1);
         mbbanner6.set(`${jingdongf}`, mbbanner6.size << (Math.min(Math.abs(2), 3)));
      while ((basketballhometeamg * eventsplashP) < 5.28) {
         eventsplashP -= parseFloat(`${parseInt(`${basketballhometeamg}`)}`);
         break;
      }
      proxyO -= parseFloat(`${parseInt(`${basketballhometeamg}`)}`);
      if (logouserJ) {
         break;
      }
   } while (logouserJ && ((proxyO + 5.35) > 3.97));
      dacccfaabfbcbadeebddcabacdffdbl *= 1;

            return result.data.categories as Array<wwDownloadingScheduler>;

        } catch (e: any) {
            console.error(`[Error getHomePages}]: ${e.toString()}`);

      searchbarp = `${parseInt(`${eventsplash_}`) ^ parseInt(`${proxyO}`)}`;
       let libswscalez: Array<any> = [922, 221, 687];
      for (let p = 0; p < 3; p++) {
         libswscalez = [3];
      }
         libswscalez.push(2);
          let currentL: Map<any, any> = new Map([[String.fromCharCode(114,105,100,103,101,0),String.fromCharCode(102,109,117,108,0)], [String.fromCharCode(98,105,110,98,110,0),String.fromCharCode(105,95,52,55,0)], [String.fromCharCode(109,97,115,107,113,0),String.fromCharCode(112,95,53,49,0)]]);
          let reactnavigation7: Array<any> = [227, 249];
          let combinev: Array<any> = [864, 954, 0];
         libswscalez.push(currentL.size);
         currentL.set(`${combinev.length}`, reactnavigation7.length * combinev.length);
         reactnavigation7.push(3);
      redgoalR = `${(String.fromCharCode(102,0) == reactnativejsF ? reactnativejsF.length : (mbbanner8 ? 1 : 5))}`;
       let darkt: Array<any> = [String.fromCharCode(116,104,101,97,116,101,114,0), String.fromCharCode(101,120,116,114,97,100,97,116,97,0)];
       let type_khS = 2.0;
       let panglem = 1.0;
      let memberh = 5348042 <= darkt.length;
      do {
         darkt.push(parseInt(`${panglem}`));
         if (memberh) {
            break;
         }
      } while (memberh && (parseInt(`${panglem}`) == darkt.length));
         panglem /= Math.max(parseFloat(`${3}`), 4);
      searchbarp += `${downarroww + 2}`;
            throw e;
        }
    };
}
