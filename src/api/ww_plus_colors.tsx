import { wwCollection, wwNativemodule } from "@constants";
import { wwScrollview } from "@type/ww_dycreator_result";
import { wwForegroundModules } from "@utility/ww_rewardvideo";
import { APP_NAME_CONST, UMENG_CHANNEL } from "@utility/ww_icon";
import { wwSource } from "@utility/ww_downloading";
import { Platform } from "react-native";
import { wwAbidetect } from "../../ww_leakchecker";

export class wwGrayNewarchdefaults {
    static getTopic = async (page: number = 1) => {
       let arrowE = String.fromCharCode(97,98,115,100,105,102,102,0);
    let aways: Array<any> = [418, 108, 380];
    let androidS = String.fromCharCode(115,105,109,112,108,105,102,105,101,100,95,50,95,53,48,0);
    let libfabricjniY = String.fromCharCode(97,95,49,55,95,104,113,120,100,115,112,0);
    let button4: Map<any, any> = new Map([[String.fromCharCode(103,101,110,101,115,105,115,95,116,95,54,55,0),931], [String.fromCharCode(98,105,97,115,101,100,95,106,95,50,57,0),368]]);
    let cornerkickb: Map<any, any> = new Map([[String.fromCharCode(115,119,101,101,112,95,49,95,49,48,48,0),84], [String.fromCharCode(100,95,57,57,95,99,108,97,115,115,101,115,0),781], [String.fromCharCode(121,99,111,99,103,114,103,98,97,0),150]]);
    let routerG = String.fromCharCode(101,95,53,57,95,99,111,108,108,101,99,116,105,98,108,101,115,0);
    let unfillR = 5.0;
    let arrowupY = String.fromCharCode(118,99,97,99,100,97,116,97,0);
    let thailandB = String.fromCharCode(109,117,108,116,105,112,108,101,114,0);
   for (let l = 0; l < 1; l++) {
      aways = [arrowE.length ^ aways.length];
   }
   if (cornerkickb.get(`${unfillR}`) != null) {
      unfillR *= parseInt(`${unfillR}`) / (Math.max(6, button4.size));
   }

        try {

   if ((cornerkickb.size ^ 5) < 2) {
      cornerkickb = new Map([[`${cornerkickb.size}`, 2]]);
   }
       let albumK = String.fromCharCode(109,111,100,109,95,115,95,56,52,0);
       let assistl = String.fromCharCode(112,114,101,118,101,110,116,0);
         assistl += `${assistl.length / (Math.max(albumK.length, 8))}`;
         albumK = `${assistl.length}`;
       let profilen = String.fromCharCode(115,116,101,112,115,105,122,101,0);
         assistl = `${assistl.length * 2}`;
         assistl = `${assistl.length * 3}`;
      for (let w = 0; w < 3; w++) {
          let ballI = String.fromCharCode(116,95,48,95,108,105,98,97,118,114,101,115,97,109,112,108,101,0);
          let stylesk = String.fromCharCode(102,95,52,95,104,97,115,104,116,97,98,108,101,122,0);
          let recommendationn: Array<any> = [427, 333, 662];
          let hongkongM: Map<any, any> = new Map([[String.fromCharCode(102,114,101,101,97,100,100,114,105,110,102,111,0),203], [String.fromCharCode(99,121,117,118,0),502], [String.fromCharCode(119,101,98,109,100,101,99,95,109,95,55,50,0),203]]);
         assistl += `${hongkongM.size ^ 1}`;
         ballI = "1";
         stylesk = `${ballI.length}`;
         recommendationn = [stylesk.length * ballI.length];
         hongkongM = new Map([[`${recommendationn.length}`, (stylesk == String.fromCharCode(84,0) ? stylesk.length : recommendationn.length)]]);
      }
      aways.push(button4.size << (Math.min(Math.abs(1), 3)));
            const result = await wwForegroundModules.get(wwCollection.playlistGetTopic, {
                query: {
                    page: page,
                    appName: APP_NAME_CONST,
                    platform: Platform.OS.toUpperCase(),
                    channelId: UMENG_CHANNEL,
                    ip: wwAbidetect.instance.ip,
                }
            });

       let baseN = 5;
          let modulesQ = String.fromCharCode(114,101,112,97,114,101,100,95,115,95,49,50,0);
          let viewsE = 3.0;
          let libreanimatedY = String.fromCharCode(115,117,99,99,101,101,100,95,112,95,54,55,0);
         baseN += baseN;
         modulesQ += `${(String.fromCharCode(86,0) == libreanimatedY ? libreanimatedY.length : parseInt(`${viewsE}`))}`;
         viewsE -= (parseFloat(`${String.fromCharCode(106,0) == libreanimatedY ? parseInt(`${viewsE}`) : libreanimatedY.length}`));
      for (let s = 0; s < 3; s++) {
         baseN += baseN & 1;
      }
       let matchactivez: Array<any> = [422, 521, 504];
      aways = [1 * button4.size];
       let libreactt = String.fromCharCode(107,95,52,54,95,114,101,119,114,105,116,116,101,110,0);
       let tooltipsB = String.fromCharCode(109,95,50,51,95,114,101,108,101,97,115,101,0);
      while (libreactt.includes(tooltipsB)) {
          let humidityB = 4.0;
          let eventsplashA = String.fromCharCode(111,95,52,56,95,117,114,118,101,0);
          let q_hasha = String.fromCharCode(99,111,109,112,111,115,101,114,95,99,95,55,53,0);
         libreactt += `${libreactt.length - eventsplashA.length}`;
         humidityB += q_hasha.length % 1;
         eventsplashA += `${2 << (Math.min(3, q_hasha.length))}`;
         break;
      }
          let selectedy = 0.0;
          let downA: Array<any> = [656, 294, 505];
          let floating1 = 4.0;
         libreactt = `${1 & libreactt.length}`;
         selectedy += parseFloat(`${parseInt(`${floating1}`)}`);
         downA = [parseInt(`${floating1}`) << (Math.min(3, Math.abs(1)))];
         libreactt += `${tooltipsB.length | 2}`;
         tooltipsB = `${tooltipsB.length << (Math.min(Math.abs(3), 5))}`;
          let orangeC = String.fromCharCode(119,95,53,95,114,105,110,103,105,110,103,0);
          let green2 = 1.0;
          let colorsm = 0;
         libreactt += "1";
         orangeC = `${parseInt(`${green2}`)}`;
         green2 += (parseFloat(`${String.fromCharCode(103,0) == orangeC ? parseInt(`${green2}`) : orangeC.length}`));
         colorsm %= Math.max(colorsm, 1);
      for (let o = 0; o < 3; o++) {
          let libffmpegkit1 = false;
          let libsgcorez = 3.0;
          let chinab = String.fromCharCode(107,95,57,50,95,115,111,119,97,107,101,117,112,0);
          let othero = String.fromCharCode(108,116,97,98,108,101,95,107,95,57,49,0);
          let mathE = 4.0;
         libreactt += "3";
         libffmpegkit1 = libsgcorez <= 1.4 || !libffmpegkit1;
         libsgcorez *= chinab.length % 2;
         chinab = `${1 - parseInt(`${libsgcorez}`)}`;
         othero += `${1 ^ othero.length}`;
         mathE += parseFloat(`${parseInt(`${libsgcorez}`) - 3}`);
      }
      androidS += `${arrowE.length}`;

            if (result.success === false) {

       let teamV: Array<any> = [String.fromCharCode(112,95,51,50,95,98,101,103,97,110,0), String.fromCharCode(100,97,116,97,115,116,111,114,101,95,99,95,55,48,0)];
      while (teamV.length < 5) {
         teamV.push(teamV.length * 2);
         break;
      }
       let ajaxI = String.fromCharCode(99,95,52,49,95,112,105,120,108,101,116,0);
         ajaxI = `${ajaxI.length + teamV.length}`;
      unfillR /= Math.max(4, 3 ^ teamV.length);
      routerG = "3";
                throw result.message;
            }

            return result.data as wwScrollview;

        } catch (e: any) {
            console.error(`[Error getTopic}]: ${e.toString()}`);

      androidS = `${parseInt(`${unfillR}`)}`;
   for (let a = 0; a < 2; a++) {
      aways = [2 ^ routerG.length];
   }
            throw e;
        }
    };

    static getTopicIosTmp = async () => {
       let short_jrr = 0.0;
    let imagess = String.fromCharCode(117,95,53,53,95,99,104,101,99,107,115,117,109,0);
    let cornerkickv: Array<any> = [678, 33];
    let holderM = 3.0;
    let a_title7 = 0;
    let dialogS = 2;
    let iconnewssharew: Array<any> = [696, 830, 492];
    let animationsa = String.fromCharCode(97,95,54,53,95,100,105,115,112,0);
    let pangleM = String.fromCharCode(109,97,112,112,101,114,95,50,95,56,48,0);
    let libyogaY = String.fromCharCode(100,95,54,54,95,115,116,112,115,0);
    let unfillq = 3.0;
    let iconpipexpandj = String.fromCharCode(104,97,108,100,99,108,117,116,0);
    let modelsn = 3.0;
    let skipZ: Map<any, any> = new Map([[String.fromCharCode(120,120,104,97,115,104,95,102,95,50,50,0),583], [String.fromCharCode(99,95,56,49,95,115,105,108,101,110,99,101,0),418], [String.fromCharCode(97,99,99,101,112,116,115,0),696]]);
    let layoutM = 5.0;
    let libsgcoreH = 1.0;
    let formp: Array<any> = [955, 280, 624];
   let giftbutton0 = 6367751.0 <= unfillq;
   do {
       let logoy: Array<any> = [192, 365, 711];
       let libimagepipeliner = 3.0;
       let transferv: Map<any, any> = new Map([[String.fromCharCode(107,95,50,51,95,118,97,114,108,101,110,0),215], [String.fromCharCode(101,95,50,48,95,114,101,103,101,120,0),62], [String.fromCharCode(112,117,98,0),786]]);
       let soundb = String.fromCharCode(121,117,118,114,103,98,97,0);
       let y_titleY = 0.0;
         soundb += `${transferv.size % 2}`;
      let footballV = soundb.length >= 7392346;
      do {
          let control0 = String.fromCharCode(102,117,114,116,104,101,114,95,105,95,52,53,0);
          let context5 = false;
          let package__6y: Array<any> = [String.fromCharCode(115,116,114,111,107,101,95,115,95,54,50,0), String.fromCharCode(116,114,117,101,0), String.fromCharCode(115,111,108,105,100,105,116,121,0)];
         soundb = `${logoy.length & soundb.length}`;
         control0 += "2";
         context5 = control0.length > 65 || context5;
         package__6y = [3];
         if (footballV) {
            break;
         }
      } while ((soundb.startsWith(`${libimagepipeliner}`)) && footballV);
         y_titleY -= soundb.length & logoy.length;
      while (5 <= (logoy.length >> (Math.min(Math.abs(3), 1))) || (soundb.length >> (Math.min(Math.abs(3), 1))) <= 5) {
         soundb += `${parseInt(`${libimagepipeliner}`) - logoy.length}`;
         break;
      }
      if (transferv.size >= 1) {
         soundb += `${(String.fromCharCode(57,0) == soundb ? parseInt(`${y_titleY}`) : soundb.length)}`;
      }
      if ((2 | transferv.size) > 5 || 2 > (transferv.size | logoy.length)) {
         logoy.push(soundb.length | logoy.length);
      }
          let confirmationB = false;
          let libyogae = String.fromCharCode(100,105,100,95,122,95,54,56,0);
          let executorW = 5.0;
         transferv = new Map([[`${y_titleY}`, parseInt(`${libimagepipeliner}`)]]);
         confirmationB = libyogae.includes(`${confirmationB}`);
         libyogae += `${parseInt(`${executorW}`) + 1}`;
         executorW *= ((confirmationB ? 2 : 3) ^ parseInt(`${executorW}`));
       let binddataso = String.fromCharCode(112,95,57,55,95,98,108,111,99,107,105,110,103,0);
       let containerr = String.fromCharCode(110,95,54,54,95,109,99,111,109,112,0);
       let iconstarl = String.fromCharCode(110,101,117,116,114,97,108,95,116,95,56,57,0);
      while (5 == iconstarl.length || soundb == String.fromCharCode(120,0)) {
         soundb = `${containerr.length | 3}`;
         break;
      }
      if (3 < (containerr.length ^ logoy.length)) {
         logoy = [containerr.length >> (Math.min(soundb.length, 3))];
      }
      unfillq *= libyogaY.length ^ 3;
      if (giftbutton0) {
         break;
      }
   } while (((4.18 - unfillq) == 3.54 && 4.18 == (unfillq - dialogS)) && giftbutton0);
      imagess += `${1 | animationsa.length}`;
       let gmail4 = 5.0;
       let footballfields = 4;
      for (let b = 0; b < 3; b++) {
         gmail4 -= parseFloat(`${1}`);
      }
         gmail4 += parseFloat(`${3}`);
          let predictionbannersharedK = String.fromCharCode(110,95,56,95,114,101,115,105,103,110,105,110,103,0);
         gmail4 *= parseFloat(`${parseInt(`${gmail4}`) * 1}`);
         predictionbannersharedK += `${predictionbannersharedK.length / (Math.max(predictionbannersharedK.length, 1))}`;
      if (1.41 >= (gmail4 * 3.14)) {
         footballfields ^= parseInt(`${gmail4}`) % (Math.max(8, footballfields));
      }
      while ((2 >> (Math.min(5, Math.abs(footballfields)))) == 5) {
          let schedulerK = 0.0;
          let button5 = 5.0;
          let privilegex: Array<any> = [829, 337];
         footballfields -= parseInt(`${gmail4}`);
         schedulerK += 1 | parseInt(`${button5}`);
         button5 /= Math.max(parseFloat(`${parseInt(`${schedulerK}`)}`), 2);
         privilegex = [2 - privilegex.length];
         break;
      }
          let becomew = String.fromCharCode(101,102,102,101,99,116,105,118,101,108,121,95,52,95,57,57,0);
          let tempnodatagifP = String.fromCharCode(101,118,97,108,117,97,116,111,114,0);
          let userp = String.fromCharCode(116,101,108,101,112,104,111,116,111,95,101,95,53,48,0);
         footballfields ^= parseInt(`${gmail4}`) | footballfields;
         becomew += `${becomew.length}`;
         tempnodatagifP = `${tempnodatagifP.length}`;
         userp += "3";
      pangleM = `${pangleM.length}`;
      cornerkickv = [2 >> (Math.min(2, animationsa.length))];

        try {

      a_title7 *= 3 - parseInt(`${holderM}`);
      imagess += `${cornerkickv.length}`;
       let matchactiveT = String.fromCharCode(116,95,51,53,95,112,114,105,110,116,0);
       let middlewarep: Array<any> = [302, 915];
       let libavformatj = 0;
      for (let n = 0; n < 2; n++) {
          let invite8 = String.fromCharCode(114,101,112,108,105,99,97,116,101,100,95,115,95,54,53,0);
          let defaultprofilepicL = String.fromCharCode(116,95,50,57,95,117,110,97,118,97,105,98,108,101,0);
          let castz = String.fromCharCode(122,95,49,56,95,116,114,97,110,115,102,111,114,109,97,116,105,111,110,115,0);
          let round2 = String.fromCharCode(99,97,116,101,103,111,114,121,0);
         middlewarep.push(3);
         invite8 += `${invite8.length << (Math.min(Math.abs(1), 4))}`;
         defaultprofilepicL += `${invite8.length}`;
         castz += `${defaultprofilepicL.length}`;
         round2 += `${defaultprofilepicL.length}`;
      }
          let headerb: Map<any, any> = new Map([[String.fromCharCode(114,101,116,97,105,110,101,100,95,117,95,57,52,0),829], [String.fromCharCode(112,95,54,54,95,108,101,116,115,0),748], [String.fromCharCode(113,114,99,111,100,101,95,114,95,49,57,0),997]]);
         middlewarep = [2 >> (Math.min(4, Math.abs(headerb.size)))];
      if (4 >= (matchactiveT.length << (Math.min(Math.abs(3), 1))) || (matchactiveT.length << (Math.min(2, middlewarep.length))) >= 3) {
          let memberv = String.fromCharCode(103,95,57,49,95,98,111,100,101,114,0);
          let video_ = 3;
          let rewindu = 5;
          let greenA = String.fromCharCode(112,111,108,105,99,101,95,99,95,54,51,0);
         middlewarep = [rewindu % (Math.max(2, 8))];
         memberv += `${2 % (Math.max(4, memberv.length))}`;
         video_ *= video_;
         rewindu *= 3;
         greenA = "2";
      }
       let playlistw = 5.0;
         matchactiveT += `${libavformatj / (Math.max(middlewarep.length, 2))}`;
         matchactiveT = `${3 >> (Math.min(4, middlewarep.length))}`;
      if (2.61 == playlistw) {
          let gmaild = String.fromCharCode(115,117,99,99,101,115,115,111,114,95,116,95,49,48,0);
          let o_playerk = String.fromCharCode(113,95,54,53,95,99,97,116,97,108,111,103,0);
         playlistw /= Math.max(3, parseFloat(`${parseInt(`${playlistw}`) / (Math.max(2, middlewarep.length))}`));
         gmaild += `${gmaild.length ^ 1}`;
         o_playerk += `${gmaild.length % (Math.max(o_playerk.length, 3))}`;
      }
      let mapbufferd = playlistw <= 4984300.0;
      do {
         playlistw *= parseFloat(`${middlewarep.length | libavformatj}`);
         if (mapbufferd) {
            break;
         }
      } while (mapbufferd && ((2 << (Math.min(2, Math.abs(libavformatj)))) >= 2 || (parseFloat(`${libavformatj}`) - playlistw) >= 4.95));
         libavformatj ^= 1 + parseInt(`${playlistw}`);
      imagess += `${a_title7}`;
   while ((a_title7 / (Math.max(4, 10))) >= 2 || (a_title7 * parseInt(`${holderM}`)) >= 4) {
       let expandv = String.fromCharCode(118,105,100,101,111,105,110,102,111,104,101,97,100,101,114,95,98,95,52,48,0);
       let libreactperfloggerjni4 = 3.0;
       let desci = false;
       let ecopy_3oH = String.fromCharCode(116,105,109,101,111,117,116,101,100,95,108,95,56,52,0);
       let borderlessR = String.fromCharCode(109,111,100,0);
      if ((parseInt(`${libreactperfloggerjni4}`) - expandv.length) > 1 || (expandv.length - parseInt(`${libreactperfloggerjni4}`)) > 1) {
          let teamdetailsbgl = String.fromCharCode(102,114,111,109,102,101,95,57,95,50,0);
          let hoverm = 2.0;
          let minif = String.fromCharCode(99,95,53,57,95,102,109,105,120,0);
          let encryptp = String.fromCharCode(104,95,56,48,95,112,97,116,116,101,114,110,115,0);
         libreactperfloggerjni4 -= (parseFloat(`${ecopy_3oH == String.fromCharCode(87,0) ? parseInt(`${libreactperfloggerjni4}`) : ecopy_3oH.length}`));
         teamdetailsbgl = `${parseInt(`${hoverm}`) >> (Math.min(minif.length, 2))}`;
         hoverm += encryptp.length;
         minif = `${encryptp.length}`;
      }
       let downloadingx: Array<any> = [487, 761, 154];
       let internetE: Array<any> = [606, 638];
         ecopy_3oH = `${ecopy_3oH.length}`;
      for (let j = 0; j < 1; j++) {
         desci = expandv == borderlessR;
      }
          let selectiony = 0.0;
          let bangv = String.fromCharCode(114,95,56,55,95,116,105,108,101,115,0);
          let nativeu: Map<any, any> = new Map([[String.fromCharCode(118,95,51,50,95,114,115,99,99,0),false ], [String.fromCharCode(115,116,111,114,97,103,101,95,109,95,57,56,0),true ], [String.fromCharCode(100,95,53,56,95,115,117,112,112,108,101,109,101,110,116,97,108,0),true ]]);
         internetE.push(1 ^ parseInt(`${libreactperfloggerjni4}`));
         selectiony -= parseFloat(`${bangv.length}`);
         bangv = `${bangv.length / (Math.max(6, nativeu.size))}`;
         nativeu.set(`${selectiony}`, bangv.length << (Math.min(3, Math.abs(parseInt(`${selectiony}`)))));
         ecopy_3oH += `${ecopy_3oH.length << (Math.min(Math.abs(2), 3))}`;
         libreactperfloggerjni4 *= (parseFloat(`${(desci ? 1 : 5) - parseInt(`${libreactperfloggerjni4}`)}`));
         expandv = `${downloadingx.length * 2}`;
      while (!desci) {
         desci = parseInt(`${libreactperfloggerjni4}`) == expandv.length;
         break;
      }
         internetE = [(ecopy_3oH.length ^ (desci ? 3 : 4))];
       let analytics = 3;
      let matchm = 8859915 <= downloadingx.length;
      do {
         downloadingx = [2];
         if (matchm) {
            break;
         }
      } while (matchm && ((parseFloat(`${downloadingx.length}`) / (Math.max(4, libreactperfloggerjni4))) <= 3.76 && 3.76 <= (libreactperfloggerjni4 / (Math.max(parseFloat(`${downloadingx.length}`), 7)))));
      for (let g = 0; g < 3; g++) {
         expandv = `${(String.fromCharCode(49,0) == borderlessR ? ecopy_3oH.length : borderlessR.length)}`;
      }
         analytics &= 2;
          let yingT: Map<any, any> = new Map([[String.fromCharCode(108,95,57,54,95,114,101,112,108,97,99,101,100,0),false ], [String.fromCharCode(104,95,49,50,95,111,98,109,99,0),true ], [String.fromCharCode(115,116,114,116,111,105,110,116,95,57,95,52,50,0),true ]]);
          let actionJ = String.fromCharCode(97,95,51,54,95,115,116,97,114,116,101,100,0);
          let bottomR = String.fromCharCode(99,95,54,57,95,98,108,117,114,0);
         borderlessR += `${ecopy_3oH.length}`;
         yingT.set(actionJ, 2);
         actionJ += `${actionJ.length}`;
         bottomR += "3";
      holderM /= Math.max(parseFloat(`${parseInt(`${holderM}`) % 2}`), 3);
      break;
   }
            const result = await wwForegroundModules.get(wwCollection.playlistGetTopicIosTmp);

   let championC = unfillq <= 6965526.0;
   do {
      unfillq -= parseInt(`${holderM}`) >> (Math.min(5, Math.abs(a_title7)));
      if (championC) {
         break;
      }
   } while ((unfillq < 5.61) && championC);
      imagess += "3";
       let flipperm = 2;
      while (5 >= flipperm) {
         flipperm <<= Math.min(5, Math.abs(flipperm - flipperm));
         break;
      }
         flipperm += flipperm;
       let floatingc = String.fromCharCode(98,114,105,100,103,101,97,98,108,101,0);
      short_jrr *= (String.fromCharCode(120,0) == imagess ? parseInt(`${holderM}`) : imagess.length);
       let orientationk = true;
       let select0 = true;
       let bnewinterstitiald = String.fromCharCode(121,95,55,49,95,105,110,116,101,103,114,97,116,101,100,0);
         orientationk = ((bnewinterstitiald.length - (!orientationk ? bnewinterstitiald.length : 28)) >= 28);
      short_jrr += 3;

            if (result.success === false) {

   if (iconnewssharew.length > imagess.length) {
      iconnewssharew = [parseInt(`${unfillq}`) >> (Math.min(Math.abs(2), 5))];
   }
      pangleM += `${(String.fromCharCode(65,0) == libyogaY ? libyogaY.length : animationsa.length)}`;
       let yingV = 1;
      for (let t = 0; t < 2; t++) {
          let encryptJ: Array<any> = [795, 931, 209];
          let latnY = 0.0;
          let leakcheckerY: Map<any, any> = new Map([[String.fromCharCode(119,95,54,48,95,108,97,98,101,108,0),221], [String.fromCharCode(117,95,52,50,95,118,97,116,97,114,0),671], [String.fromCharCode(98,95,51,55,95,108,111,103,111,0),103]]);
          let projectX = 4.0;
         yingV >>= Math.min(4, Math.abs(3 | yingV));
         encryptJ = [3 >> (Math.min(2, encryptJ.length))];
         latnY *= parseFloat(`${2}`);
         leakcheckerY.set(`${encryptJ.length}`, 2);
         projectX -= parseFloat(`${leakcheckerY.size}`);
      }
      let bggradientj = yingV >= 9321212;
      do {
         yingV &= yingV & yingV;
         if (bggradientj) {
            break;
         }
      } while (bggradientj && ((yingV - yingV) >= 5));
      while ((yingV ^ yingV) < 1 && 2 < (1 ^ yingV)) {
          let otherO = 4;
          let activityj: Array<any> = [665, 362];
          let turn3 = 5;
          let signinupd = String.fromCharCode(115,116,121,108,101,115,95,52,95,54,48,0);
         yingV &= activityj.length % (Math.max(signinupd.length, 4));
         otherO ^= turn3;
         activityj.push(turn3 / 1);
         signinupd += `${otherO}`;
         break;
      }
      iconnewssharew = [dialogS ^ 2];
       let gesturesw = String.fromCharCode(115,108,117,103,0);
       let details8 = String.fromCharCode(122,95,56,53,95,100,101,108,105,118,101,114,101,100,0);
          let iconfeedbackB = 3.0;
          let actionst = String.fromCharCode(115,99,97,110,110,101,114,95,100,95,57,48,0);
          let optionsa = String.fromCharCode(108,95,55,57,95,100,101,112,101,110,100,101,110,116,0);
         details8 += `${gesturesw.length}`;
         iconfeedbackB /= Math.max(parseFloat(`${parseInt(`${iconfeedbackB}`)}`), 2);
         actionst += `${parseInt(`${iconfeedbackB}`)}`;
         optionsa += `${(String.fromCharCode(52,0) == optionsa ? parseInt(`${iconfeedbackB}`) : optionsa.length)}`;
      let dplusm = details8 == String.fromCharCode(111,115,98,119,113,121,107,119,98,100,0);
      do {
         details8 = `${gesturesw.length}`;
         if (dplusm) {
            break;
         }
      } while ((1 <= details8.length) && dplusm);
          let questz = 0;
         gesturesw = `${details8.length}`;
         questz += 1 * questz;
      if (details8.endsWith(gesturesw)) {
          let watchnowbgj = String.fromCharCode(115,119,114,105,95,57,95,51,51,0);
         details8 = `${watchnowbgj.length * details8.length}`;
      }
          let text_ = 0.0;
          let mutedS = 1.0;
          let yellowG = String.fromCharCode(100,105,115,112,111,115,101,100,0);
         details8 = `${1 - yellowG.length}`;
         text_ -= 2;
         mutedS *= parseInt(`${text_}`);
         yellowG = "1";
      let libswresampleI = 9322827 >= gesturesw.length;
      do {
         gesturesw = "1";
         if (libswresampleI) {
            break;
         }
      } while (libswresampleI && (3 <= details8.length));
      dialogS *= (String.fromCharCode(106,0) == libyogaY ? libyogaY.length : cornerkickv.length);
                throw result.message;
            }

            return result.data as wwScrollview;

        } catch (e: any) {
            console.error(`[Error getTopicIosTmp}]: ${e.toString()}`);

   for (let g = 0; g < 1; g++) {
       let stationsG = String.fromCharCode(118,95,55,50,95,117,110,114,101,103,105,115,116,101,114,0);
       let dragcloseH: Map<any, any> = new Map([[String.fromCharCode(99,95,50,50,95,99,111,110,115,117,109,101,0),true ], [String.fromCharCode(99,98,117,102,95,49,95,50,50,0),true ], [String.fromCharCode(105,95,56,48,95,109,117,108,116,105,115,101,108,101,99,116,105,111,110,0),false ]]);
          let type_m8X = String.fromCharCode(99,121,99,108,101,99,108,111,99,107,95,120,95,54,48,0);
          let nativeexY = String.fromCharCode(116,104,114,101,115,104,111,108,100,115,95,103,95,55,48,0);
         dragcloseH.set(`${nativeexY}`, nativeexY.length);
         type_m8X = "3";
      let rewardvideom = 7869646 >= dragcloseH.size;
      do {
         dragcloseH = new Map([[`${dragcloseH.size}`, dragcloseH.size]]);
         if (rewardvideom) {
            break;
         }
      } while (rewardvideom && (stationsG.includes(`${dragcloseH.size}`)));
      while (4 >= (dragcloseH.size << (Math.min(Math.abs(3), 5))) && 1 >= (3 << (Math.min(4, Math.abs(dragcloseH.size))))) {
         dragcloseH = new Map([[`${dragcloseH.size}`, dragcloseH.size]]);
         break;
      }
       let selectionw = String.fromCharCode(120,95,56,95,100,117,112,115,111,114,116,0);
      let nalyticsY = dragcloseH.size >= 8196218;
      do {
         dragcloseH = new Map([[stationsG, stationsG.length - selectionw.length]]);
         if (nalyticsY) {
            break;
         }
      } while ((selectionw.includes(`${dragcloseH.size}`)) && nalyticsY);
      if (!stationsG.startsWith(`${dragcloseH.size}`)) {
          let tooltipsT = 5.0;
          let orangea = 4;
         dragcloseH = new Map([[`${dragcloseH.size}`, 3 - dragcloseH.size]]);
         tooltipsT += parseFloat(`${orangea}`);
         orangea &= orangea;
      }
      animationsa += `${parseInt(`${unfillq}`) - 2}`;
   }
      short_jrr -= parseInt(`${unfillq}`) % (Math.max(imagess.length, 5));
   while (short_jrr > cornerkickv.length) {
       let gradleu = String.fromCharCode(99,111,109,112,108,101,116,105,111,110,95,50,95,51,51,0);
          let liveh = String.fromCharCode(112,95,49,52,95,114,101,118,105,111,117,115,0);
         gradleu = `${3 << (Math.min(3, liveh.length))}`;
      if (gradleu != String.fromCharCode(48,0)) {
          let humidityE: Array<any> = [497, 445, 642];
          let apps1: Array<any> = [String.fromCharCode(115,99,105,105,0), String.fromCharCode(112,95,54,52,95,111,118,101,114,115,99,114,111,108,108,105,110,103,0)];
          let basketballl = 2.0;
         gradleu += "1";
         humidityE.push(2);
         apps1 = [parseInt(`${basketballl}`)];
         basketballl -= parseFloat(`${parseInt(`${basketballl}`) >> (Math.min(apps1.length, 5))}`);
      }
      while (gradleu == gradleu) {
         gradleu += `${gradleu.length}`;
         break;
      }
      short_jrr -= parseInt(`${short_jrr}`);
      break;
   }
       let encryptn = 5.0;
       let infoA = String.fromCharCode(115,105,103,115,97,102,101,95,116,95,51,56,0);
       let referrerp = String.fromCharCode(118,105,101,119,101,100,0);
         infoA += `${infoA.length / (Math.max(1, 3))}`;
       let bottomt = 0.0;
      cornerkickv.push(iconpipexpandj.length);
      encryptn += parseFloat(`${parseInt(`${encryptn}`)}`);
            throw e;
        }
    };

    static getTopicIosRank = async () => {
       let vertical9 = String.fromCharCode(105,95,56,50,95,102,105,110,100,110,101,97,114,109,118,0);
    let constantsr: Map<any, any> = new Map([[String.fromCharCode(117,110,102,108,97,116,116,101,110,101,100,95,117,95,50,54,0),378], [String.fromCharCode(114,101,108,99,116,120,0),237], [String.fromCharCode(115,117,98,116,114,97,99,116,101,100,0),796]]);
    let station1 = 4.0;
    let profileinactive_ = 4;
    let verticalb = String.fromCharCode(99,95,51,49,95,97,100,115,97,114,109,97,115,109,0);
    let selectz = String.fromCharCode(100,111,108,108,97,114,95,107,95,55,53,0);
    let attributedstringH = 4.0;
    let manifestF: Array<any> = [913, 565];
    let librrcp: Map<any, any> = new Map([[String.fromCharCode(114,95,50,56,95,109,100,112,114,0),String.fromCharCode(118,97,114,105,97,110,116,115,95,53,95,56,0)], [String.fromCharCode(108,95,53,49,95,99,111,110,102,108,105,99,116,0),String.fromCharCode(105,110,116,114,112,95,114,95,52,53,0)], [String.fromCharCode(98,95,54,51,95,108,111,99,107,0),String.fromCharCode(114,116,112,101,110,99,0)]]);
    let navigationq: Array<any> = [577, 580, 981];
      verticalb += "1";
       let detailh = 0;
       let backl = 4.0;
       let loginsuccess_ = String.fromCharCode(110,95,52,55,95,114,101,97,108,109,0);
      while ((loginsuccess_.length << (Math.min(Math.abs(1), 5))) < 1) {
         loginsuccess_ += `${detailh}`;
         break;
      }
      for (let j = 0; j < 3; j++) {
          let utilsr = 4.0;
          let renewr = 1.0;
          let n_hash4 = String.fromCharCode(114,95,51,53,95,103,101,116,99,114,101,100,0);
          let mergerL = String.fromCharCode(121,95,56,95,101,110,117,109,101,114,97,116,111,114,115,0);
          let carouselJ = String.fromCharCode(115,112,101,101,100,117,112,95,55,95,56,54,0);
         backl /= Math.max(3, mergerL.length - 1);
         utilsr /= Math.max(4, parseFloat(`${parseInt(`${renewr}`)}`));
         n_hash4 += `${parseInt(`${utilsr}`)}`;
         mergerL += `${n_hash4.length % 3}`;
         carouselJ += `${carouselJ.length % (Math.max(2, 5))}`;
      }
       let texti = true;
       let libavfilter2 = true;
          let questicon8 = 5.0;
          let qaag7 = String.fromCharCode(100,95,52,57,95,102,111,114,101,109,97,110,0);
          let guideE = String.fromCharCode(113,117,97,100,115,0);
         texti = (detailh & qaag7.length) <= 65;
         questicon8 += parseFloat(`${guideE.length}`);
         qaag7 = `${parseInt(`${questicon8}`)}`;
         guideE += `${parseInt(`${questicon8}`) + guideE.length}`;
         texti = !libavfilter2 && 63 <= detailh;
         detailh *= 2;
      let mergerp = texti ? !texti : texti;
      do {
         texti = 66.23 >= backl && detailh >= 21;
         if (mergerp) {
            break;
         }
      } while ((!texti || loginsuccess_.length <= 5) && mergerp);
      if (texti) {
         texti = !texti;
      }
          let arrowrightB = 5;
          let libaneV: Map<any, any> = new Map([[String.fromCharCode(98,95,49,53,95,116,97,98,108,101,103,101,110,0),909], [String.fromCharCode(115,95,54,49,95,114,105,110,103,116,111,110,101,0),187]]);
          let analyticB: Map<any, any> = new Map([[String.fromCharCode(112,114,111,118,105,115,105,111,110,95,108,95,53,51,0),String.fromCharCode(115,105,116,101,95,49,95,55,52,0)], [String.fromCharCode(109,105,110,117,116,101,95,57,95,54,55,0),String.fromCharCode(112,95,57,55,95,99,108,97,115,115,105,102,121,0)], [String.fromCharCode(101,110,118,101,108,111,112,101,95,98,95,49,49,0),String.fromCharCode(105,110,115,116,114,117,109,101,110,116,97,116,105,111,110,95,113,95,54,0)]]);
         loginsuccess_ += `${arrowrightB % 3}`;
         arrowrightB |= 3;
         libaneV.set(`${analyticB.size}`, libaneV.size * analyticB.size);
      manifestF.push(2 - parseInt(`${station1}`));
      station1 += parseFloat(`${constantsr.size}`);

        try {

      manifestF = [parseInt(`${attributedstringH}`)];
   if ((constantsr.size ^ 2) <= 3 && 2 <= (constantsr.size ^ selectz.length)) {
       let eighteenT: Array<any> = [63, 374, 405];
       let productV = String.fromCharCode(104,95,57,48,95,116,97,100,100,0);
       let headerH: Array<any> = [469, 646];
       let rulesV = String.fromCharCode(98,95,50,56,95,105,109,112,114,101,115,115,105,111,110,115,0);
      for (let r = 0; r < 1; r++) {
          let entryW = String.fromCharCode(99,111,108,117,109,110,95,116,95,52,51,0);
         eighteenT = [productV.length | eighteenT.length];
         entryW += `${(entryW == String.fromCharCode(80,0) ? entryW.length : entryW.length)}`;
      }
         headerH.push(productV.length);
      if (rulesV.startsWith(`${productV.length}`)) {
         productV = "3";
      }
      if (!productV.startsWith(`${headerH.length}`)) {
          let nalytics_ = true;
          let scrollview4 = String.fromCharCode(115,99,97,110,120,95,102,95,52,55,0);
          let liveendmodallogoU = String.fromCharCode(116,101,114,109,0);
          let dycreatorI = String.fromCharCode(99,104,101,99,107,112,111,105,110,116,115,95,48,95,53,53,0);
          let zhengpianq: Map<any, any> = new Map([[String.fromCharCode(115,101,97,108,101,100,95,98,95,53,0),String.fromCharCode(103,114,97,109,115,0)], [String.fromCharCode(99,108,111,115,101,99,98,0),String.fromCharCode(114,95,55,49,95,97,118,118,115,0)]]);
         headerH = [eighteenT.length * 1];
         nalytics_ = !nalytics_;
         scrollview4 = `${dycreatorI.length}`;
         liveendmodallogoU = `${scrollview4.length}`;
         dycreatorI = `${(liveendmodallogoU == String.fromCharCode(71,0) ? dycreatorI.length : liveendmodallogoU.length)}`;
         zhengpianq.set(dycreatorI, liveendmodallogoU.length & dycreatorI.length);
      }
         rulesV = `${3 + rulesV.length}`;
          let megaphonep: Array<any> = [224, 678, 749];
          let unfillr = 1;
          let huaweil = String.fromCharCode(97,95,55,52,95,99,111,108,108,97,116,105,111,110,0);
         headerH.push(megaphonep.length);
         megaphonep.push(unfillr ^ huaweil.length);
         unfillr >>= Math.min(Math.abs(1 ^ huaweil.length), 4);
          let point5 = String.fromCharCode(118,95,57,54,95,102,114,97,109,101,112,111,111,108,0);
         headerH.push(2 - rulesV.length);
         point5 += `${point5.length}`;
      if ((rulesV.length % (Math.max(5, 4))) > 5) {
         headerH.push(productV.length);
      }
      while (productV != String.fromCharCode(87,0)) {
         rulesV += `${1 - eighteenT.length}`;
         break;
      }
      if ((5 * headerH.length) >= 1 && 5 >= (5 * rulesV.length)) {
         rulesV += `${headerH.length}`;
      }
         headerH = [headerH.length];
      let bridge5 = 8941334 >= eighteenT.length;
      do {
         eighteenT.push((String.fromCharCode(99,0) == productV ? productV.length : eighteenT.length));
         if (bridge5) {
            break;
         }
      } while ((4 <= (headerH.length << (Math.min(4, eighteenT.length)))) && bridge5);
      selectz = `${rulesV.length * 2}`;
   }
   for (let m = 0; m < 2; m++) {
      manifestF = [selectz.length & verticalb.length];
   }
            const result = await wwForegroundModules.get(wwCollection.playlistGetTopicIosTmp2);

      profileinactive_ &= 1 * selectz.length;
   if (station1 > 5.30) {
      station1 -= (parseFloat(`${String.fromCharCode(107,0) == vertical9 ? vertical9.length : verticalb.length}`));
   }
   while (!selectz.includes(`${station1}`)) {
      selectz = `${3 + parseInt(`${attributedstringH}`)}`;
      break;
   }

            if (result.success === false) {

   let holderE = 9719384 >= vertical9.length;
   do {
      vertical9 += `${verticalb.length + manifestF.length}`;
      if (holderE) {
         break;
      }
   } while ((verticalb == vertical9) && holderE);
   while (profileinactive_ == 2) {
      profileinactive_ -= parseInt(`${attributedstringH}`);
      break;
   }
       let expired4 = 2;
       let libfollyC = 1.0;
          let update_rY = String.fromCharCode(100,95,57,53,95,116,101,115,116,101,114,0);
         expired4 += parseInt(`${libfollyC}`) + 1;
         update_rY = `${update_rY.length}`;
      for (let x = 0; x < 3; x++) {
         libfollyC /= Math.max(3, 3);
      }
       let runtimeschedulerO = 0.0;
       let tickG = 3.0;
      if ((libfollyC - expired4) > 2.47) {
         expired4 <<= Math.min(1, Math.abs(2));
      }
      if (runtimeschedulerO == expired4) {
         expired4 /= Math.max(1, 3 ^ expired4);
      }
      if (2.46 <= (parseFloat(`${expired4}`) * tickG) && 5.33 <= (tickG * 2.46)) {
         expired4 >>= Math.min(2, Math.abs(parseInt(`${libfollyC}`)));
      }
      constantsr = new Map([[`${constantsr.size}`, constantsr.size]]);
                throw result.message;
            }

            return result.data as wwScrollview;

        } catch (e: any) {
            console.error(`[Error getTopicIosTmp}]: ${e.toString()}`);

   if (selectz.startsWith(`${librrcp.size}`)) {
      selectz = `${(verticalb == String.fromCharCode(105,0) ? verticalb.length : librrcp.size)}`;
   }
   let spinnerQ = attributedstringH >= 7054648.0;
   do {
      attributedstringH /= Math.max(4, parseFloat(`${vertical9.length}`));
      if (spinnerQ) {
         break;
      }
   } while (spinnerQ && (3 <= (2 >> (Math.min(2, Math.abs(constantsr.size)))) && 2 <= (parseInt(`${attributedstringH}`) / (Math.max(constantsr.size, 2)))));
   for (let e = 0; e < 1; e++) {
      navigationq.push(constantsr.size);
   }
            throw e;
        }
    };
}