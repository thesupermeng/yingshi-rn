import { MVCountry, IHUpdateBanner } from "@constants";
import { GUploadPhilippines, MDown, XVSScoreDark, KYDescriptionFast, POPrivateOverlay, WButtonMovies } from "@type/wpk_long";
import { JTumbnailMatches } from "@utility/qot_stations_station";
import { AD_VIDEO_SECONDS, APP_NAME_CONST, UMENG_CHANNEL } from "@utility/n_subs_interstitial";
import { Platform } from "react-native";
import { KLongNext } from "../../z_search";
import { WAnalyticsArrow } from "@utility/a_singapore";

export class RCanvas {
    static getTopicType = async () => {
       let submit4: Array<any> = [String.fromCharCode(112,114,105,109,105,116,105,118,101,95,120,95,54,53,0), String.fromCharCode(115,112,104,101,114,105,99,97,108,95,54,95,53,57,0)];
    let overj: Array<any> = [72, 673];
    let f_viewa = 2;
    let watchq = false;
    let bannerE = false;
    let filedI = 1.0;
    let sound2 = String.fromCharCode(119,95,49,48,48,95,116,111,107,101,110,110,102,116,116,120,0);
    let events: Map<any, any> = new Map([[String.fromCharCode(98,95,52,55,95,109,115,114,108,101,100,101,99,0),696], [String.fromCharCode(117,95,49,53,95,105,110,116,115,0),848], [String.fromCharCode(116,95,49,50,95,115,116,121,108,101,0),354]]);
    let screenN = false;
    let user3 = String.fromCharCode(116,111,110,101,95,103,95,55,50,0);
    let specS = String.fromCharCode(116,97,112,102,105,108,116,101,114,95,115,95,56,53,0);
   while (1 < f_viewa) {
      f_viewa >>= Math.min(Math.abs(1), 1);
      break;
   }
   let activeu = screenN ? !screenN : screenN;
   do {
       let bootsplashb = String.fromCharCode(102,97,115,116,101,115,116,95,107,95,50,54,0);
       let analyticsK = String.fromCharCode(120,95,49,0);
         bootsplashb = `${analyticsK.length / (Math.max(1, bootsplashb.length))}`;
         bootsplashb += `${(bootsplashb == String.fromCharCode(56,0) ? analyticsK.length : bootsplashb.length)}`;
         bootsplashb += `${analyticsK.length << (Math.min(bootsplashb.length, 4))}`;
       let zhuboJ: Map<any, any> = new Map([[String.fromCharCode(97,95,53,53,95,106,117,115,116,105,102,105,101,100,0),74], [String.fromCharCode(105,110,118,111,99,97,116,105,111,110,95,103,95,53,57,0),284], [String.fromCharCode(100,95,56,53,95,115,117,98,115,116,114,105,110,103,0),853]]);
       let themeW: Map<any, any> = new Map([[String.fromCharCode(107,95,51,53,95,97,114,116,119,111,114,107,0),String.fromCharCode(118,105,101,119,101,100,95,112,95,55,53,0)], [String.fromCharCode(106,95,53,53,95,115,119,97,98,0),String.fromCharCode(98,108,111,99,107,101,114,95,99,95,51,0)], [String.fromCharCode(121,95,53,50,95,108,108,97,117,100,100,115,112,0),String.fromCharCode(117,95,51,51,95,99,108,97,115,115,0)]]);
       let indicatorw: Map<any, any> = new Map([[String.fromCharCode(115,95,54,57,95,99,97,112,115,0),333], [String.fromCharCode(108,95,52,51,95,107,101,121,102,114,97,109,101,115,0),70], [String.fromCharCode(120,95,50,56,95,97,116,114,97,99,112,108,117,115,0),959]]);
         zhuboJ.set(analyticsK, 2 << (Math.min(4, analyticsK.length)));
      screenN = 39 > submit4.length;
      if (activeu) {
         break;
      }
   } while ((screenN) && activeu);
   for (let h = 0; h < 3; h++) {
      screenN = null == events.get(`${screenN}`);
   }
      user3 = `${f_viewa}`;

        try {

       let malaysiaN = 5.0;
      while (3.43 > malaysiaN) {
          let searchbar8 = 2.0;
         malaysiaN -= parseFloat(`${parseInt(`${searchbar8}`)}`);
         break;
      }
          let gmailT = 5;
         malaysiaN += parseFloat(`${gmailT & parseInt(`${malaysiaN}`)}`);
         malaysiaN -= parseFloat(`${parseInt(`${malaysiaN}`)}`);
      sound2 = `${sound2.length}`;
      events.set(`${overj.length}`, submit4.length | 2);
   if (!bannerE && 2 >= (events.size * 5)) {
      events = new Map([[sound2, 3 + sound2.length]]);
   }
   if (!Array.from(events.keys()).includes(`${filedI}`)) {
       let promotionE = 3.0;
      let video7 = promotionE <= 5984608.0;
      do {
          let chart4 = String.fromCharCode(108,97,112,110,100,122,95,48,95,50,49,0);
          let completeS = 3.0;
         promotionE *= (chart4 == String.fromCharCode(75,0) ? parseInt(`${promotionE}`) : chart4.length);
         completeS += parseFloat(`${parseInt(`${completeS}`) ^ 3}`);
         if (video7) {
            break;
         }
      } while (video7 && (promotionE < 4.65));
          let gesture8 = String.fromCharCode(113,95,55,48,95,111,108,107,97,100,111,116,0);
          let spech = true;
         promotionE /= Math.max(3, parseInt(`${promotionE}`));
         gesture8 = `${gesture8.length}`;
         spech = gesture8.startsWith(`${spech}`);
         promotionE *= parseInt(`${promotionE}`) >> (Math.min(4, Math.abs(1)));
      filedI -= parseFloat(`${submit4.length << (Math.min(1, Math.abs(parseInt(`${filedI}`))))}`);
   }
            const result = await JTumbnailMatches.get(MVCountry.vodGetTopicType);

   while ((2 >> (Math.min(3, Math.abs(events.size)))) == 2) {
      f_viewa %= Math.max(events.size, 5);
      break;
   }
      watchq = overj.length < 81;
       let football6: Array<any> = [31, 122];
          let private_5c: Array<any> = [489, 588, 229];
          let arrowD = String.fromCharCode(115,111,98,101,108,95,107,95,54,56,0);
         football6.push(private_5c.length);
         private_5c = [3 + arrowD.length];
         arrowD += `${arrowD.length}`;
          let anythinkd = String.fromCharCode(101,116,104,101,114,116,117,112,108,105,115,95,55,95,49,50,0);
          let feedbackC = String.fromCharCode(115,105,116,101,109,97,112,95,55,95,50,51,0);
          let condensed0 = String.fromCharCode(100,95,49,55,95,99,108,97,115,104,0);
         football6.push(football6.length);
         anythinkd = `${(condensed0 == String.fromCharCode(65,0) ? condensed0.length : feedbackC.length)}`;
         feedbackC = `${3 ^ anythinkd.length}`;
         football6.push(football6.length ^ football6.length);
      f_viewa |= sound2.length ^ parseInt(`${filedI}`);
       let morei = String.fromCharCode(110,97,103,108,101,95,99,95,55,56,0);
      while (morei != morei) {
         morei = "3";
         break;
      }
         morei = `${morei.length}`;
      if (morei == String.fromCharCode(84,0) || 2 == morei.length) {
         morei = `${1 - morei.length}`;
      }
      sound2 = `${2 + events.size}`;

            if (result.success === false) {

      f_viewa /= Math.max(5, ((bannerE ? 2 : 2) * (screenN ? 2 : 4)));
   let activeR = bannerE ? !bannerE : bannerE;
   do {
      bannerE = (submit4.length | events.size) <= 23;
      if (activeR) {
         break;
      }
   } while ((4.74 > (4.57 + filedI) || bannerE) && activeR);
       let carouselu = String.fromCharCode(116,95,57,50,95,105,116,115,101,108,102,0);
       let buttonV = String.fromCharCode(108,95,49,54,95,111,99,115,112,0);
       let sentry7 = String.fromCharCode(106,95,53,54,95,114,101,100,117,99,101,100,0);
          let membership2 = String.fromCharCode(119,95,53,52,95,100,98,112,97,103,101,0);
          let s_centerV = String.fromCharCode(106,95,52,49,95,108,111,119,112,97,115,115,0);
         buttonV = `${carouselu.length * 3}`;
         membership2 = `${s_centerV.length - membership2.length}`;
         s_centerV = `${membership2.length / (Math.max(s_centerV.length, 6))}`;
      let middlewareR = carouselu.length <= 8917040;
      do {
         carouselu = `${sentry7.length - carouselu.length}`;
         if (middlewareR) {
            break;
         }
      } while ((5 < buttonV.length) && middlewareR);
       let saveM = true;
      for (let b = 0; b < 1; b++) {
         sentry7 = `${buttonV.length}`;
      }
      let reducer7 = sentry7.length <= 8399676;
      do {
          let blacklistX: Map<any, any> = new Map([[String.fromCharCode(112,114,111,100,117,99,116,105,111,110,95,51,95,49,55,0),81], [String.fromCharCode(121,95,52,50,95,112,112,102,108,97,103,115,0),870]]);
          let settinga = String.fromCharCode(117,95,53,55,0);
          let cnewsl: Array<any> = [703, 349, 771];
         sentry7 = `${((saveM ? 3 : 1) | 2)}`;
         blacklistX = new Map([[`${blacklistX.size}`, (String.fromCharCode(69,0) == settinga ? settinga.length : blacklistX.size)]]);
         cnewsl.push((settinga == String.fromCharCode(120,0) ? cnewsl.length : settinga.length));
         if (reducer7) {
            break;
         }
      } while ((!saveM) && reducer7);
      let readE = saveM ? !saveM : saveM;
      do {
         saveM = sentry7 == carouselu;
         if (readE) {
            break;
         }
      } while ((sentry7.length > 1) && readE);
         sentry7 = `${((saveM ? 4 : 5) % (Math.max(10, carouselu.length)))}`;
      if (3 == sentry7.length) {
          let google7 = String.fromCharCode(100,101,103,114,101,101,115,95,105,95,49,51,0);
          let p_lockz = String.fromCharCode(114,97,110,107,105,110,103,95,121,95,53,49,0);
          let source2 = 2;
          let specH = 0.0;
         carouselu += `${parseInt(`${specH}`) & source2}`;
         google7 = `${2 | google7.length}`;
         p_lockz += `${p_lockz.length / (Math.max(1, 9))}`;
         source2 <<= Math.min(2, Math.abs((p_lockz == String.fromCharCode(115,0) ? p_lockz.length : google7.length)));
         specH /= Math.max(3, p_lockz.length);
      }
      let chinat = saveM ? !saveM : saveM;
      do {
         saveM = !carouselu.endsWith(`${saveM}`);
         if (chinat) {
            break;
         }
      } while ((!saveM && sentry7.length > 4) && chinat);
      overj.push(1);
      bannerE = 56 < overj.length;
                throw result.message;
            }

            return result.data as GUploadPhilippines[];

        } catch (e: any) {
            console.error(`[Error getTopicType}]: ${e.toString()}`);

   while ((4 + overj.length) >= 4) {
      overj.push(events.size);
      break;
   }
      events = new Map([[`${bannerE}`, ((bannerE ? 3 : 5) << (Math.min(Math.abs(2), 5)))]]);
       let thumbnail7 = 1.0;
          let const_z9k = 5;
          let alertz = String.fromCharCode(103,101,116,110,97,109,101,105,110,102,111,95,102,95,50,0);
          let away3: Map<any, any> = new Map([[String.fromCharCode(106,95,50,53,95,104,111,114,105,122,111,110,116,97,108,108,121,0),983], [String.fromCharCode(117,110,99,111,109,112,114,101,115,115,101,100,95,99,95,57,52,0),663]]);
         thumbnail7 += parseFloat(`${parseInt(`${thumbnail7}`)}`);
         const_z9k += (String.fromCharCode(52,0) == alertz ? alertz.length : away3.size);
         away3.set(alertz, alertz.length & 3);
         thumbnail7 += parseFloat(`${parseInt(`${thumbnail7}`)}`);
      let detailsi = 5113516.0 >= thumbnail7;
      do {
         thumbnail7 -= parseFloat(`${parseInt(`${thumbnail7}`) % 2}`);
         if (detailsi) {
            break;
         }
      } while ((2.59 >= thumbnail7) && detailsi);
      overj.push(3 * user3.length);
   while (overj.length <= 3 && (overj.length - 3) <= 4) {
      screenN = events.size > 63;
      break;
   }
            throw e;
        }
    }

    static getDetail = async (id: string, typeId: string, vodSourceName = "", { xMode = false }: { xMode?: boolean } = {}) => {
        try {
            const result = await JTumbnailMatches.get(xMode ? MVCountry.vodGetXDetail : MVCountry.vodGetDetail, {
                query: {
                    id,
                    appName: APP_NAME_CONST,
                    platform: Platform.OS.toUpperCase(),
                    channelId: UMENG_CHANNEL,
                    ip: KLongNext.instance.ip,
                    tid: typeId,
                    vod_source_name: vodSourceName
                }
            });
            
            if (result.success === false) {
                throw result.message;
            }

            if (result.data === undefined || result.data === null || result.data.length <= 0) {
                throw WAnalyticsArrow.get(IHUpdateBanner.apiEmptyResponse);
            }
            return result.data[0] as XVSScoreDark;

        } catch (e: any) {
            console.error(`[Error getDetail}]: ${e.toString()}`);
            throw e;
        }
    }

    static getList = async ({
        tid,
        page = 1,
        limit = 30,
        category,
        area,
        lang,
        year,
        by,
        rand,
        orderBy = 'asc',
        xMode = false,
        vod_source_name = "",
    }: {
        tid?: string,
        page?: number,
        limit?: number,
        category?: string,
        area?: string,
        lang?: string,
        year?: string,
        by?: string,
        rand?: number,
        orderBy?: 'desc' | 'asc'
        xMode?: boolean,
        vod_source_name?: string,
    }) => {
        try {
            let query: any = {
                order: orderBy,
                page,
                limit,
            };

            if (tid) {
                query['tid'] = tid;
            }
            if (category) {
                query['class'] = category;
            }
            if (area) {
                query['area'] = area;
            }
            if (lang) {
                query['lang'] = lang;
            }
            if (year) {
                query['year'] = year;
            }
            if (by) {
                query['by'] = by;
            }
            if (rand) {
                query['rand'] = rand;
            }
            if(vod_source_name != "" && xMode == true){
                query['vod_source_name'] = vod_source_name;
            }
            const result = await JTumbnailMatches.get(xMode ? MVCountry.vodGetXList : MVCountry.vodGetList, {
                query,
            });
            
            if (result.success === false) {
                throw result.message;
            }

            return xMode ? result.data as POPrivateOverlay : result.data as MDown;

        } catch (e: any) {
            console.error(`[Error getList}]: ${e.toString()}`);
            throw e;
        }
    };

    static getListByRecommendations = async () => {
       let notificationg = 4;
    let with_p_r = String.fromCharCode(108,95,50,56,95,114,101,109,111,116,101,0);
    let submitE = String.fromCharCode(105,110,115,116,114,95,102,95,49,0);
    let modulei = String.fromCharCode(108,97,121,101,114,95,110,95,54,57,0);
    let buttonl = String.fromCharCode(114,101,102,95,119,95,52,56,0);
    let starO = String.fromCharCode(97,95,55,48,95,109,97,114,107,115,0);
    let whatsappd: Array<any> = [377, 189, 774];
    let area0 = String.fromCharCode(104,95,57,50,95,115,117,98,115,97,109,112,0);
    let forwardv: Array<any> = [String.fromCharCode(119,95,53,56,95,99,104,114,111,109,97,116,105,99,0), String.fromCharCode(122,95,50,56,95,100,101,108,101,116,105,111,110,115,0), String.fromCharCode(121,95,50,54,0)];
    let eventk = String.fromCharCode(103,101,110,101,114,97,116,111,114,95,101,95,52,57,0);
    let appsG: Array<any> = [439, 397, 846];
    let predictionW: Map<any, any> = new Map([[String.fromCharCode(99,95,55,53,95,120,109,117,108,116,105,112,108,101,0),965], [String.fromCharCode(111,114,116,104,111,103,111,110,97,108,105,122,101,95,56,95,53,0),938]]);
    let specm = String.fromCharCode(98,95,57,48,95,97,114,116,0);
       let suggestionJ = true;
       let actions2: Map<any, any> = new Map([[String.fromCharCode(116,95,49,48,95,105,110,116,101,114,101,115,116,0),958], [String.fromCharCode(115,112,101,99,116,114,97,108,95,103,95,57,53,0),400]]);
       let frame_odX: Map<any, any> = new Map([[String.fromCharCode(105,115,114,101,97,100,111,110,108,121,95,113,95,50,52,0),false ], [String.fromCharCode(116,95,54,52,95,104,101,97,100,112,104,111,110,101,0),true ]]);
         actions2 = new Map([[`${actions2.size}`, 2]]);
      let launchR = actions2.size <= 5912591;
      do {
         actions2 = new Map([[`${actions2.size}`, frame_odX.size]]);
         if (launchR) {
            break;
         }
      } while ((actions2.size == 1 || 3 == (1 / (Math.max(5, actions2.size)))) && launchR);
      buttonl += `${1 ^ notificationg}`;
   for (let r = 0; r < 3; r++) {
       let header8 = 2.0;
       let privacyD = true;
          let roomZ = String.fromCharCode(105,115,110,111,116,116,97,112,95,110,95,53,0);
          let arrowQ = String.fromCharCode(119,114,105,116,97,98,108,101,95,49,95,57,48,0);
         header8 /= Math.max(4, 2 - parseInt(`${header8}`));
         roomZ += `${arrowQ.length | roomZ.length}`;
         arrowQ += "2";
      for (let f = 0; f < 3; f++) {
         header8 -= ((privacyD ? 5 : 1) % (Math.max(10, parseInt(`${header8}`))));
      }
          let editI = 2.0;
          let anythinkL = String.fromCharCode(102,95,50,54,95,115,99,113,117,101,114,121,0);
          let modelsz = 3;
         header8 /= Math.max(5, 3);
         editI *= parseFloat(`${anythinkL.length & parseInt(`${editI}`)}`);
         anythinkL = `${parseInt(`${editI}`) ^ 2}`;
         modelsz += modelsz;
          let producta = 5.0;
          let usernameR = String.fromCharCode(98,114,111,97,100,99,97,115,116,101,114,95,50,95,50,57,0);
          let canvasN = 5;
         privacyD = privacyD || header8 < 23.39;
         producta /= Math.max(1, parseFloat(`${3 - parseInt(`${producta}`)}`));
         usernameR = `${usernameR.length | parseInt(`${producta}`)}`;
         canvasN %= Math.max(4, parseInt(`${producta}`));
       let reada: Map<any, any> = new Map([[String.fromCharCode(117,95,56,53,95,116,114,97,110,115,112,0),784], [String.fromCharCode(111,95,49,49,95,103,101,111,103,114,97,112,104,105,99,97,108,0),872]]);
      for (let j = 0; j < 1; j++) {
         reada = new Map([[`${privacyD}`, ((privacyD ? 5 : 4) >> (Math.min(Math.abs(parseInt(`${header8}`)), 4)))]]);
      }
      whatsappd = [buttonl.length >> (Math.min(4, Math.abs(parseInt(`${header8}`))))];
   }

        try {

   for (let p = 0; p < 1; p++) {
       let matchB = String.fromCharCode(120,95,51,48,95,117,110,109,105,120,0);
       let corner7 = String.fromCharCode(113,95,49,57,95,115,101,97,100,0);
      while (4 > matchB.length) {
          let backgroundk: Map<any, any> = new Map([[String.fromCharCode(107,95,53,95,97,109,102,101,110,99,0),586], [String.fromCharCode(122,95,56,52,95,114,100,112,99,109,0),187]]);
          let catalogK = 5;
          let catalogx = String.fromCharCode(99,95,56,57,95,115,104,111,117,108,100,0);
          let modityh = String.fromCharCode(103,95,49,57,95,117,97,100,100,0);
         corner7 += "2";
         backgroundk = new Map([[modityh, catalogK ^ 3]]);
         catalogK <<= Math.min(2, Math.abs((String.fromCharCode(112,0) == catalogx ? catalogK : catalogx.length)));
         modityh += `${catalogx.length}`;
         break;
      }
          let delegate_071 = 0.0;
          let grayt = 1.0;
          let termsR = String.fromCharCode(97,95,50,48,95,101,110,102,111,114,99,101,0);
         corner7 = `${parseInt(`${grayt}`) ^ matchB.length}`;
         delegate_071 += (parseFloat(`${String.fromCharCode(73,0) == termsR ? parseInt(`${delegate_071}`) : termsR.length}`));
         grayt -= parseFloat(`${1}`);
      while (matchB.length < 1) {
         corner7 += `${matchB.length % 2}`;
         break;
      }
         corner7 += "2";
          let productb = 4;
          let groupv = String.fromCharCode(115,113,108,105,116,101,112,97,103,101,114,95,49,95,53,50,0);
          let sina7 = 5.0;
         corner7 = `${corner7.length}`;
         productb <<= Math.min(Math.abs(2), 3);
         groupv = `${parseInt(`${sina7}`)}`;
         sina7 /= Math.max(3 ^ parseInt(`${sina7}`), 2);
      for (let f = 0; f < 1; f++) {
          let expiredJ = 5.0;
         corner7 = `${matchB.length ^ 1}`;
         expiredJ *= parseFloat(`${2}`);
      }
      submitE += `${buttonl.length + forwardv.length}`;
   }
   if ((buttonl.length | 1) <= 3) {
      whatsappd.push(2);
   }
            const result = await JTumbnailMatches.get(MVCountry.playlistGetTopicDetail, {
                query: {
                    id: 1,
                    appName: APP_NAME_CONST,
                    platform: Platform.OS.toUpperCase(),
                    channelId: UMENG_CHANNEL,
                    ip: KLongNext.instance.ip,
                }
            });

   for (let m = 0; m < 2; m++) {
       let selectp = String.fromCharCode(110,95,51,95,108,119,115,115,112,110,0);
       let unreadu = 4;
       let splashq: Array<any> = [913, 965];
      for (let b = 0; b < 2; b++) {
         unreadu |= unreadu;
      }
          let bellS: Array<any> = [348, 971];
         unreadu ^= splashq.length;
         bellS.push(bellS.length + bellS.length);
      let emojiZ = splashq.length <= 8949870;
      do {
          let grayj = 2;
          let carouseln: Map<any, any> = new Map([[String.fromCharCode(98,116,114,101,101,95,116,95,48,0),String.fromCharCode(105,115,109,108,95,121,95,52,50,0)], [String.fromCharCode(112,114,111,98,108,101,109,115,95,51,95,51,53,0),String.fromCharCode(115,101,110,100,97,108,108,95,50,95,50,54,0)]]);
          let views6 = 2.0;
          let foundI: Map<any, any> = new Map([[String.fromCharCode(111,114,97,110,103,101,95,101,95,52,52,0),20], [String.fromCharCode(112,114,101,108,105,109,105,110,97,114,121,95,108,95,56,55,0),555]]);
         splashq.push(selectp.length);
         grayj -= 3;
         carouseln = new Map([[`${views6}`, 3 << (Math.min(Math.abs(grayj), 5))]]);
         views6 += parseFloat(`${foundI.size}`);
         foundI.set(`${views6}`, parseInt(`${views6}`));
         if (emojiZ) {
            break;
         }
      } while (emojiZ && ((splashq.length & 4) < 2 || (unreadu & splashq.length) < 4));
         unreadu /= Math.max(splashq.length + 1, 4);
         splashq.push(unreadu / 3);
         splashq.push(selectp.length);
      for (let o = 0; o < 1; o++) {
         selectp += `${selectp.length}`;
      }
      let mnewsm = 7297682 <= unreadu;
      do {
         unreadu /= Math.max(4, 1 & splashq.length);
         if (mnewsm) {
            break;
         }
      } while ((unreadu >= 5) && mnewsm);
         unreadu |= splashq.length - selectp.length;
      modulei = `${submitE.length - buttonl.length}`;
   }
   while (!buttonl.startsWith(`${eventk.length}`)) {
       let recommendationV: Map<any, any> = new Map([[String.fromCharCode(112,95,54,50,95,109,97,115,107,105,110,103,0),false ], [String.fromCharCode(111,95,52,51,95,99,111,111,107,105,101,0),false ]]);
       let plashr: Array<any> = [String.fromCharCode(103,95,56,53,95,115,105,122,101,100,0), String.fromCharCode(111,95,53,48,0), String.fromCharCode(104,95,53,95,102,97,113,0)];
       let indicatorg: Array<any> = [454, 903, 290];
       let modulem = String.fromCharCode(100,95,51,49,95,116,114,105,97,110,103,117,108,97,116,105,111,110,0);
       let moon7 = String.fromCharCode(114,95,54,50,95,105,110,116,115,0);
      if (moon7.length >= 4) {
          let themeL = String.fromCharCode(97,117,116,111,100,101,108,101,116,101,95,101,95,51,51,0);
          let search8 = true;
         moon7 = `${moon7.length % 1}`;
         themeL = `${themeL.length}`;
         search8 = (((!search8 ? 10 : themeL.length) << (Math.min(themeL.length, 2))) > 10);
      }
      for (let w = 0; w < 3; w++) {
         indicatorg.push(indicatorg.length + recommendationV.size);
      }
       let kickf = String.fromCharCode(112,114,111,109,111,116,101,95,117,95,52,48,0);
       let questG = String.fromCharCode(99,111,100,105,110,103,95,54,95,50,49,0);
         plashr = [recommendationV.size];
      while (2 > (indicatorg.length & moon7.length)) {
         indicatorg.push(modulem.length);
         break;
      }
          let grayV = 2;
          let expiredV = String.fromCharCode(97,108,108,111,119,105,95,53,95,50,57,0);
         moon7 += `${grayV}`;
         grayV &= expiredV.length;
         expiredV = `${expiredV.length}`;
         modulem = "3";
      for (let x = 0; x < 2; x++) {
          let modelsf = 5;
          let modity6 = 0;
          let greyi: Map<any, any> = new Map([[String.fromCharCode(115,99,97,110,95,55,95,56,56,0),true ], [String.fromCharCode(122,95,51,48,0),true ]]);
         indicatorg = [kickf.length ^ 3];
         modelsf *= greyi.size;
         modity6 %= Math.max(3, modelsf % (Math.max(greyi.size, 9)));
      }
       let half6 = String.fromCharCode(115,101,103,109,101,110,116,97,116,105,111,110,95,99,95,49,48,0);
       let matches8 = String.fromCharCode(111,115,100,101,112,95,48,95,51,51,0);
      while (modulem.length == half6.length) {
          let playM = String.fromCharCode(105,110,116,105,95,101,95,55,52,0);
          let sidea = String.fromCharCode(103,117,105,100,95,121,95,49,54,0);
          let cornerf = String.fromCharCode(116,97,98,108,101,105,110,105,116,95,119,95,56,54,0);
          let feedbackk = 2.0;
         modulem = `${parseInt(`${feedbackk}`) % (Math.max(10, sidea.length))}`;
         playM += `${cornerf.length >> (Math.min(playM.length, 5))}`;
         sidea += "1";
         cornerf += `${playM.length}`;
         feedbackk -= parseFloat(`${1 % (Math.max(1, cornerf.length))}`);
         break;
      }
         half6 += `${matches8.length / (Math.max(modulem.length, 4))}`;
      if (kickf != String.fromCharCode(104,0) || matches8.length <= 5) {
         kickf = `${(kickf == String.fromCharCode(121,0) ? kickf.length : plashr.length)}`;
      }
         plashr.push(1 & plashr.length);
      let switch_iuZ = 9814899 <= recommendationV.size;
      do {
          let privilegeu: Array<any> = [293, 764, 373];
          let tickK: Array<any> = [507, 900, 394];
          let assistg = String.fromCharCode(121,113,117,97,110,116,95,113,95,52,55,0);
         recommendationV = new Map([[`${indicatorg.length}`, moon7.length]]);
         privilegeu.push(1 + privilegeu.length);
         tickK = [tickK.length];
         assistg += "3";
         if (switch_iuZ) {
            break;
         }
      } while ((5 > (5 * matches8.length)) && switch_iuZ);
         matches8 += `${modulem.length - questG.length}`;
      eventk = "2";
      break;
   }

            if (result.success === false) {

      buttonl = "2";
   let downloaded5 = starO == String.fromCharCode(113,98,122,55,0);
   do {
      starO = `${(eventk == String.fromCharCode(98,0) ? eventk.length : notificationg)}`;
      if (downloaded5) {
         break;
      }
   } while ((5 < eventk.length || starO != String.fromCharCode(105,0)) && downloaded5);
                throw result.message;

   let sharedx = starO == String.fromCharCode(48,111,48,114,55,113,95,122,104,110,0);
   do {
      starO += "1";
      if (sharedx) {
         break;
      }
   } while ((!starO.includes(`${whatsappd.length}`)) && sharedx);
   for (let w = 0; w < 1; w++) {
      area0 += `${(modulei == String.fromCharCode(77,0) ? modulei.length : starO.length)}`;
   }
            }

            const data = result.data.vod_list as MDown;

   for (let y = 0; y < 2; y++) {
      with_p_r = `${submitE.length}`;
   }
       let backgroundD = String.fromCharCode(116,95,52,56,95,112,114,117,110,101,0);
       let helper0 = 3;
       let yellowB = 5;
          let hookx: Map<any, any> = new Map([[String.fromCharCode(119,114,105,116,101,108,111,99,107,95,114,95,50,52,0),true ], [String.fromCharCode(114,101,103,95,117,95,49,57,0),true ]]);
          let chatj = String.fromCharCode(117,97,110,95,109,95,50,55,0);
         helper0 ^= yellowB;
         hookx = new Map([[`${hookx.size}`, 3]]);
         chatj += `${(chatj == String.fromCharCode(108,0) ? hookx.size : chatj.length)}`;
      if ((yellowB % (Math.max(backgroundD.length, 1))) >= 4) {
         backgroundD += `${helper0 >> (Math.min(Math.abs(1), 3))}`;
      }
      if ((helper0 - 4) < 2 || 4 < (yellowB - helper0)) {
         helper0 &= 2;
      }
         backgroundD = `${(backgroundD == String.fromCharCode(115,0) ? helper0 : backgroundD.length)}`;
      if ((yellowB ^ helper0) < 4) {
         helper0 -= 1 >> (Math.min(5, Math.abs(helper0)));
      }
      if (backgroundD.startsWith(`${helper0}`)) {
         helper0 ^= 3;
      }
          let helper7 = true;
          let redirectM = String.fromCharCode(114,101,110,100,101,114,101,100,95,100,95,50,53,0);
         yellowB |= yellowB & 3;
         helper7 = !helper7;
         redirectM += `${(redirectM.length + (helper7 ? 5 : 1))}`;
       let emojii = 4.0;
       let overlay1 = 5.0;
      let saveV = backgroundD == String.fromCharCode(52,115,102,50,0);
      do {
          let livee = 0.0;
         backgroundD = `${backgroundD.length}`;
         livee *= parseInt(`${livee}`);
         if (saveV) {
            break;
         }
      } while ((!backgroundD.includes(`${yellowB}`)) && saveV);
      eventk = `${with_p_r.length}`;

            return result.data.vod_list

        } catch (e: any) {
            console.error(`[Error getListByRecommendations}]: ${e.toString()}`);

   if (area0.length <= submitE.length) {
      submitE = `${(String.fromCharCode(98,0) == area0 ? whatsappd.length : area0.length)}`;
   }
      buttonl += `${submitE.length}`;
            throw e;
        }
    };

    static getListByKeyword = async (keyword: string, { page = 1 }: { page?: number } = {}) => {
        try {
            const result = await JTumbnailMatches.get(MVCountry.vodGetList, {
                query: {
                    wd: keyword,
                    limit: 35,
                    page,
                }
            });

            if (result.success === false) {
                throw result.message;
            }

            const data = result.data as MDown;

            return data.List ?? [];

        } catch (e: any) {
            console.error(`[Error getListByKeyword}]: ${e.toString()}`);
            throw e;
        }
    };

    static getReviewDetails = async (id: string) => {
        try {
            const result = await JTumbnailMatches.get(MVCountry.vodGetReviewDetail, {
                query: {
                    douban_id: id,
                    limit: 6,
                }
            });

            if (result.success === false) {
                throw result.message;
            }

            return result.data as KYDescriptionFast;

        } catch (e: any) {
            console.error(`[Error getReviewDetails}]: ${e.toString()}`);
            throw e;
        }
    };

    static getPlayerAdVideo = async (): Promise<WButtonMovies | undefined> => {
        try {
            const result = await JTumbnailMatches.get(MVCountry.vodGetAdsSlot, {
                query: {
                    slot_id: '114',
                    ip: KLongNext.instance.ip
                }
            });

            if (result.success === false) {
                throw result.message;
            }

            if (!result.data) {
                return undefined;
            }

            return {
                id: result.data.ads_id,
                title: result.data.ads_title,
                eventTitle: result.data.ads_event_title,
                name: result.data.ads_name,
                url: result.data.ads_pic,
                slotId: result.data.ads_slot_id,
                isVideo: result.data.is_video,
                actionUrl: result.data.ads_url,
                minDuration: !isNaN(result.data.ads_min_duration ?? NaN) ? result.data.ads_min_duration : AD_VIDEO_SECONDS,
                redirectType: result.data.ads_redirect_type,
            }

        } catch (e: any) {
            console.error(`[Error getPlayerAdVideo}]: ${e.toString()}`);
            throw e;
        }
    };
}