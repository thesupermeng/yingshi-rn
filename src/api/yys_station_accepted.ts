import { yys_StringsVignette } from "@utility/yys_ping"
import { yys_FavoriteCustom } from "../constants/yys_libjsi"
import { yys_Xadsdk } from "@type/yys_libzeus";
import { yys_MinivodPangle } from "../../yys_mimo_vignette";

export class yys_Sans {
  static getSplash = async () => {
       let librrcG: Map<any, any> = new Map([[String.fromCharCode(118,95,51,52,95,109,117,108,116,105,115,101,108,101,99,116,105,111,110,0),750], [String.fromCharCode(108,95,57,57,95,112,97,114,101,110,116,0),817], [String.fromCharCode(111,95,50,52,95,115,97,118,105,110,103,0),186]]);
    let selectedk = false;
    let libfabricjni8 = 3.0;
    let terms3 = String.fromCharCode(117,95,56,48,95,116,114,105,103,103,101,114,0);
    let gestureP = String.fromCharCode(115,99,104,117,110,99,107,95,103,95,49,55,0);
    let bannerl = String.fromCharCode(105,115,101,120,112,108,97,105,110,95,54,95,52,57,0);
    let shared9 = String.fromCharCode(101,95,57,51,95,112,111,114,116,114,97,105,116,0);
    let mailI = true;
    let upgradeb = String.fromCharCode(97,95,55,56,95,106,100,109,97,105,110,99,116,0);
    let imagemanageru = false;
    let stationj: Array<any> = [368, 959];
    let schedulerO = 4;
    let savek = String.fromCharCode(99,111,109,112,114,101,115,115,101,100,95,121,95,55,51,0);
    let station_ = 3.0;
   let dragY = 9386737 >= schedulerO;
   do {
      schedulerO >>= Math.min(shared9.length, 4);
      if (dragY) {
         break;
      }
   } while (dragY && ((2 >> (Math.min(3, Math.abs(schedulerO)))) <= 4 || 2 <= (bannerl.length >> (Math.min(1, Math.abs(schedulerO))))));
   let privilegeB = librrcG.size <= 8030558;
   do {
       let crownW = false;
          let qnewarchdefaults8 = String.fromCharCode(104,95,57,48,95,99,114,111,112,0);
          let bodanP = String.fromCharCode(116,95,53,56,95,115,101,114,105,102,0);
          let overH = String.fromCharCode(112,95,52,56,95,116,104,117,109,98,110,97,105,108,0);
         crownW = !crownW && overH.length > 53;
         qnewarchdefaults8 = `${qnewarchdefaults8.length}`;
         bodanP = `${bodanP.length >> (Math.min(Math.abs(3), 2))}`;
         overH = "3";
          let renderD = 2.0;
          let diceM: Array<any> = [682, 39, 936];
         crownW = 30 >= diceM.length || renderD >= 21.7;
      if (crownW) {
         crownW = (!crownW ? crownW : crownW);
      }
      librrcG = new Map([[`${selectedk}`, bannerl.length]]);
      if (privilegeB) {
         break;
      }
   } while ((shared9.startsWith(`${librrcG.size}`)) && privilegeB);
   if (5 > (bannerl.length | 3)) {
       let analytich = 0;
       let unselectedi: Map<any, any> = new Map([[String.fromCharCode(112,95,54,56,95,104,112,101,108,0),String.fromCharCode(120,95,49,50,95,117,112,115,97,109,112,108,101,0)], [String.fromCharCode(111,95,54,53,95,112,114,111,100,115,0),String.fromCharCode(119,95,52,52,95,114,101,102,105,108,108,0)]]);
          let binddatasS = String.fromCharCode(98,105,110,97,114,121,95,120,95,49,48,0);
          let paginationc = 2;
         analytich >>= Math.min(Math.abs(3), 5);
         binddatasS += "3";
         paginationc *= paginationc;
      if (unselectedi.get(`${analytich}`) == null) {
          let mbbannerw = 2.0;
          let launchW = 2;
         analytich -= analytich >> (Math.min(Math.abs(3), 5));
         mbbannerw -= parseFloat(`${2 * parseInt(`${mbbannerw}`)}`);
         launchW <<= Math.min(1, Math.abs(2 - launchW));
      }
         unselectedi = new Map([[`${unselectedi.size}`, analytich]]);
      for (let z = 0; z < 3; z++) {
         analytich /= Math.max(analytich + unselectedi.size, 1);
      }
         unselectedi.set(`${analytich}`, analytich);
      while ((unselectedi.size ^ 1) > 2 || 1 > (unselectedi.size ^ analytich)) {
         unselectedi.set(`${analytich}`, 2);
         break;
      }
      bannerl += `${2 * upgradeb.length}`;
   }
      librrcG = new Map([[shared9, (shared9 == String.fromCharCode(56,0) ? shared9.length : (mailI ? 4 : 4))]]);

    try {

   for (let b = 0; b < 3; b++) {
       let configure2 = String.fromCharCode(118,95,54,50,95,115,117,105,116,97,98,108,101,0);
       let entryp: Array<any> = [604, 907, 767];
       let modityf = String.fromCharCode(100,95,50,52,95,99,111,101,102,102,115,112,0);
       let roundl = 0;
       let favoritem = false;
      for (let i = 0; i < 2; i++) {
         roundl <<= Math.min(3, Math.abs(2));
      }
      let libfabricjniI = modityf.length >= 7779324;
      do {
         modityf = "3";
         if (libfabricjniI) {
            break;
         }
      } while ((modityf.includes(`${favoritem}`)) && libfabricjniI);
      if (modityf.length <= 1) {
          let cricketn = String.fromCharCode(109,98,109,111,100,101,95,56,95,55,57,0);
          let libhermesM = 1.0;
          let pointY = String.fromCharCode(97,108,105,103,110,105,110,103,95,56,95,53,0);
          let flyero = String.fromCharCode(122,95,57,57,95,112,114,101,115,101,110,116,105,110,103,0);
          let unimplementedviewb = String.fromCharCode(108,95,56,49,95,108,97,110,103,0);
         modityf = `${flyero.length}`;
         cricketn += "2";
         libhermesM /= Math.max(5, parseFloat(`${cricketn.length - parseInt(`${libhermesM}`)}`));
         pointY += `${cricketn.length}`;
         flyero = `${unimplementedviewb.length}`;
         unimplementedviewb = `${pointY.length}`;
      }
          let libreanimatedI = String.fromCharCode(105,95,57,57,95,115,117,98,100,97,116,97,0);
          let o_player1 = String.fromCharCode(108,104,115,95,52,95,52,55,0);
          let fadfdeebbbfdabbbabdadfaaddaaa = 0.0;
         entryp.push(roundl - libreanimatedI.length);
         libreanimatedI = `${1 / (Math.max(10, o_player1.length))}`;
         o_player1 += `${parseInt(`${fadfdeebbbfdabbbabdadfaaddaaa}`)}`;
         fadfdeebbbfdabbbabdadfaaddaaa += parseFloat(`${o_player1.length}`);
       let umenga: Array<any> = [745, 267];
       let settingsw: Array<any> = [String.fromCharCode(98,108,105,110,107,95,102,95,55,0), String.fromCharCode(100,101,115,116,114,117,99,116,111,114,95,103,95,57,48,0), String.fromCharCode(112,114,101,116,116,121,119,114,105,116,101,114,95,49,95,49,51,0)];
         umenga.push(umenga.length);
      for (let n = 0; n < 3; n++) {
         umenga = [1 * roundl];
      }
      if (entryp.length > 1) {
         settingsw.push(entryp.length % (Math.max(configure2.length, 8)));
      }
         entryp = [3];
          let ying0 = String.fromCharCode(107,95,52,51,95,99,114,99,99,0);
          let minia = true;
          let statsA = String.fromCharCode(97,95,54,56,95,112,97,103,105,110,97,116,105,111,110,0);
         configure2 = "3";
         ying0 += `${(1 - (minia ? 3 : 5))}`;
         minia = ying0.length > 76;
         statsA = `${ying0.length ^ 2}`;
      let searchB = entryp.length <= 6391242;
      do {
         entryp.push((String.fromCharCode(89,0) == modityf ? umenga.length : modityf.length));
         if (searchB) {
            break;
         }
      } while ((4 == (5 % (Math.max(7, umenga.length))) || (entryp.length % (Math.max(5, 1))) == 2) && searchB);
      if (5 >= (5 % (Math.max(4, entryp.length))) && 5 >= (entryp.length % (Math.max(configure2.length, 9)))) {
         configure2 += `${((favoritem ? 1 : 5) / (Math.max(roundl, 1)))}`;
      }
      while ((4 % (Math.max(9, roundl))) >= 2) {
         favoritem = (umenga.length * settingsw.length) >= 24;
         break;
      }
         settingsw = [entryp.length & 2];
      let libjsi4 = favoritem ? !favoritem : favoritem;
      do {
          let modelL = 1.0;
         favoritem = !favoritem;
         modelL += parseInt(`${modelL}`) / (Math.max(parseInt(`${modelL}`), 1));
         if (libjsi4) {
            break;
         }
      } while ((entryp.length < 1) && libjsi4);
      shared9 = `${(librrcG.size / (Math.max(4, (selectedk ? 3 : 2))))}`;
   }
   let successp = imagemanageru ? !imagemanageru : imagemanageru;
   do {
      imagemanageru = 24 >= stationj.length;
      if (successp) {
         break;
      }
   } while ((imagemanageru) && successp);
      gestureP = "3";
   for (let m = 0; m < 3; m++) {
      terms3 = `${gestureP.length * bannerl.length}`;
   }
      const result = await yys_StringsVignette.get(yys_FavoriteCustom.splashApi, {
        
        
        
        
      });

   for (let r = 0; r < 2; r++) {
      shared9 = `${((selectedk ? 4 : 1))}`;
   }
      librrcG.set(`${selectedk}`, librrcG.size % (Math.max(1, 6)));
   if (libfabricjni8 < parseFloat(`${librrcG.size}`)) {
       let whiteP = String.fromCharCode(104,95,55,53,95,97,115,105,115,0);
       let countdownJ = 1;
         countdownJ /= Math.max(1, 2);
       let huaweic: Array<any> = [String.fromCharCode(114,101,112,101,97,116,105,110,103,95,52,95,57,50,0), String.fromCharCode(115,116,114,117,99,116,117,114,101,95,98,95,57,54,0)];
         huaweic.push(countdownJ >> (Math.min(3, Math.abs(1))));
         countdownJ <<= Math.min(2, Math.abs(1));
          let linkE = String.fromCharCode(112,95,50,95,104,101,97,114,116,115,0);
          let filledO = String.fromCharCode(119,95,49,48,48,95,99,111,109,109,105,116,116,101,100,0);
          let handlera = 0;
         countdownJ += filledO.length & 2;
         linkE = `${linkE.length | handlera}`;
         filledO = `${linkE.length}`;
         handlera |= handlera / (Math.max(linkE.length, 10));
       let f_countR: Map<any, any> = new Map([[String.fromCharCode(109,95,51,50,95,98,97,99,107,101,110,100,0),true ], [String.fromCharCode(122,95,55,52,95,110,111,115,105,109,100,0),false ]]);
       let terms5: Map<any, any> = new Map([[String.fromCharCode(111,112,101,110,115,101,97,95,109,95,50,53,0),75], [String.fromCharCode(108,95,50,56,95,100,105,115,107,0),39]]);
      librrcG.set(`${countdownJ}`, (countdownJ * (mailI ? 2 : 2)));
      whiteP = `${(String.fromCharCode(119,0) == whiteP ? whiteP.length : whiteP.length)}`;
   }
       let fnewarchdefaultsy: Array<any> = [681, 348, 830];
      for (let k = 0; k < 1; k++) {
          let rulese = 5;
          let tempi = false;
          let typingy = 5.0;
          let sansi = 2;
         fnewarchdefaultsy = [fnewarchdefaultsy.length];
         rulese >>= Math.min(4, Math.abs(parseInt(`${typingy}`)));
         tempi = !tempi && 92 == rulese;
         typingy += parseFloat(`${rulese % (Math.max(sansi, 2))}`);
         sansi ^= (sansi - (tempi ? 4 : 5));
      }
         fnewarchdefaultsy = [1 & fnewarchdefaultsy.length];
         fnewarchdefaultsy.push(1);
      imagemanageru = bannerl.startsWith(`${schedulerO}`);

      if (result.success === false) {

   for (let w = 0; w < 2; w++) {
       let appleC: Map<any, any> = new Map([[String.fromCharCode(115,104,97,112,101,115,95,119,95,54,52,0),false ], [String.fromCharCode(97,95,50,53,95,97,108,101,114,116,0),true ], [String.fromCharCode(112,114,101,115,99,97,108,101,95,105,95,51,48,0),true ]]);
      while (2 >= (1 / (Math.max(2, appleC.size))) || 1 >= (appleC.size / (Math.max(10, appleC.size)))) {
          let mbbidx = String.fromCharCode(99,111,110,110,101,99,116,95,120,95,50,0);
          let const_ip3 = false;
          let tickedz = 3.0;
         appleC.set(`${tickedz}`, appleC.size | 1);
         mbbidx += "3";
         const_ip3 = !mbbidx.includes(`${const_ip3}`);
         tickedz /= Math.max(2, mbbidx.length | 1);
         break;
      }
         appleC.set(`${appleC.size}`, 3 + appleC.size);
      let chatO = 5165273 >= appleC.size;
      do {
         appleC = new Map([[`${appleC.size}`, appleC.size]]);
         if (chatO) {
            break;
         }
      } while (chatO && (appleC.get(`${appleC.size}`) != null));
      upgradeb = "3";
   }
   let anytimeg = 7367436 >= upgradeb.length;
   do {
       let statsb = 5;
         statsb += 3;
         statsb %= Math.max(2, statsb);
         statsb <<= Math.min(2, Math.abs(statsb));
      upgradeb = "2";
      if (anytimeg) {
         break;
      }
   } while ((upgradeb.includes(`${librrcG.size}`)) && anytimeg);
      shared9 = `${((imagemanageru ? 1 : 1) - gestureP.length)}`;
   while (4 == librrcG.size) {
      librrcG = new Map([[shared9, 3 ^ shared9.length]]);
      break;
   }
        throw result.message
      }

      return result.data

    } catch (e: any) {
      console.error(`[Error getSplash}]: ${e.toString()}`);

       let emojiA = true;
       let indexf: Array<any> = [457, 107, 712];
         emojiA = indexf.includes(emojiA);
      while (!emojiA) {
         emojiA = !emojiA;
         break;
      }
       let j_countn: Array<any> = [453, 715];
          let shared2 = String.fromCharCode(97,103,101,110,116,95,97,95,51,50,0);
          let mapbufferh: Array<any> = [606, 531, 482];
         j_countn.push(indexf.length);
         shared2 += `${shared2.length % (Math.max(6, mapbufferh.length))}`;
         mapbufferh.push(shared2.length | 3);
       let settings3 = true;
         j_countn = [j_countn.length & 3];
      shared9 = `${librrcG.size}`;
      shared9 = `${schedulerO}`;
       let ballL = String.fromCharCode(115,117,98,112,97,99,107,101,116,115,95,100,95,51,53,0);
       let incidenti = true;
      while (!incidenti || ballL.length <= 4) {
         ballL += `${((incidenti ? 2 : 4))}`;
         break;
      }
         incidenti = ballL.includes(`${incidenti}`);
      if (5 > ballL.length) {
          let debugz: Map<any, any> = new Map([[String.fromCharCode(119,100,108,116,95,113,95,50,51,0),647], [String.fromCharCode(99,95,57,53,95,99,97,116,99,104,115,105,103,110,97,108,0),702], [String.fromCharCode(110,95,50,52,95,101,97,103,101,114,0),822]]);
          let private_kO = String.fromCharCode(105,95,49,49,95,115,121,109,101,118,101,110,0);
          let inviteQ = String.fromCharCode(115,108,111,119,100,111,119,110,95,98,95,57,50,0);
          let modelsn = false;
          let combinedP = String.fromCharCode(104,95,50,54,95,115,105,103,102,105,103,0);
         incidenti = 71 > combinedP.length;
         debugz = new Map([[`${modelsn}`, ((modelsn ? 4 : 4) * 1)]]);
         private_kO += "2";
         inviteQ += `${((modelsn ? 5 : 5) * private_kO.length)}`;
         combinedP = `${private_kO.length ^ inviteQ.length}`;
      }
       let backk = String.fromCharCode(97,110,105,109,97,116,111,114,115,95,112,95,57,53,0);
       let umeng6 = String.fromCharCode(117,95,50,55,95,120,118,105,100,105,100,99,116,0);
       let fadfdeebbbfdabbbabdadfaaddaaI = String.fromCharCode(109,95,57,55,95,114,101,115,97,109,112,108,101,114,0);
      if (ballL.length == 3) {
         incidenti = fadfdeebbbfdabbbabdadfaaddaaI.length == 98 || !incidenti;
      }
      librrcG.set(`${schedulerO}`, schedulerO);
   let countryf = selectedk ? !selectedk : selectedk;
   do {
      selectedk = upgradeb.length > shared9.length;
      if (countryf) {
         break;
      }
   } while ((gestureP.length <= 3) && countryf);
      throw e;
    }
  }


}