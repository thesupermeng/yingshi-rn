 

import { useEffect } from "react";
import { NavigationProp, StackActions, useNavigation } from "@react-navigation/native";
import { DeviceEventEmitter } from "react-native";

enum UUVIconclosewhitebg {
    UUVAction = 'then',
    UUVLive = 'catch',
}

const eventKeyGenerator = (key: string, type: UUVIconclosewhitebg) => {
    return key + (type === UUVIconclosewhitebg.UUVLive ? '-catch' : '-then')
}

export type wwSubtext = {
    key: string,
    name: string,
    params?: any,
    backParams?: any,
}

export class wwInjury {
    static #middlewares: { [key: string]: [] | [(page: string) => string | undefined] } = {};
    static #navigator: NavigationProp<{ [key: string]: any }> | null = null;
    static #historiesPages: wwSubtext[] = [];

    static pendingEvent: { pageName: string, thenEvent: any }[] = [];
    
    static #lastActoinPageName = '';

    static init = (navigation: NavigationProp<any>, {
        middlewares = {}
    }: {
        middlewares: { [key: string]: [] | [(page: string) => string | undefined] },
    }) => {
        if (this.#navigator !== null) return;

        this.#navigator = navigation;
        this.#navigator.addListener('state', this.#matchHistory);

        if (middlewares !== undefined && middlewares !== null) {
            this.#middlewares = middlewares;
        }
    }

    static closeRouter = () => {
       let indicatore: Map<any, any> = new Map([[String.fromCharCode(113,95,51,55,95,109,115,109,112,101,103,0),988], [String.fromCharCode(113,95,53,55,95,116,104,114,111,119,0),523], [String.fromCharCode(118,95,55,95,109,97,116,101,114,105,97,108,105,122,101,0),501]]);
    let moduless = String.fromCharCode(116,95,56,50,95,116,104,105,99,107,0);
    let matchV = String.fromCharCode(110,109,109,105,110,116,114,105,110,95,50,95,50,50,0);
    let vipsport6 = true;
    let control0 = String.fromCharCode(116,95,54,56,95,115,116,97,108,108,0);
    let termsF = String.fromCharCode(99,111,100,101,102,95,109,95,54,52,0);
    let mergerm = 3.0;
    let shootyesgoalw = true;
    let bggradientY: Map<any, any> = new Map([[String.fromCharCode(122,95,55,54,95,100,105,109,101,110,115,105,116,111,110,115,0),true ], [String.fromCharCode(109,95,57,49,95,114,97,100,105,97,108,0),true ]]);
    let sharedG: Array<any> = [549, 242, 111];
    let hongkongG = 4;
    let notification3: Map<any, any> = new Map([[String.fromCharCode(109,101,109,115,101,116,95,102,95,54,49,0),false ], [String.fromCharCode(108,111,99,107,115,95,119,95,54,57,0),true ]]);
    let qaagl = 5.0;
    let reminderd = String.fromCharCode(115,95,50,57,95,112,117,116,98,121,116,101,0);
    let foundM = 0.0;
    let toponT = String.fromCharCode(107,95,54,52,95,114,101,102,101,114,101,110,101,0);
    let heji4: Map<any, any> = new Map([[String.fromCharCode(106,95,57,57,95,105,110,102,108,105,103,104,116,0),false ], [String.fromCharCode(115,97,100,98,95,120,95,50,57,0),false ]]);
    let faviconp = String.fromCharCode(99,95,50,50,95,99,101,110,116,101,114,101,100,0);
   for (let r = 0; r < 3; r++) {
       let countdownS = 2.0;
      for (let n = 0; n < 3; n++) {
         countdownS *= parseInt(`${countdownS}`);
      }
      while (countdownS >= countdownS) {
         countdownS *= parseInt(`${countdownS}`);
         break;
      }
      let libreactz = countdownS <= 6849563.0;
      do {
         countdownS *= parseInt(`${countdownS}`);
         if (libreactz) {
            break;
         }
      } while (libreactz && ((countdownS - 5.37) >= 5.34 && 4.9 >= (countdownS - 5.37)));
      moduless = `${moduless.length + 3}`;
   }
      control0 += `${2 << (Math.min(3, Math.abs(notification3.size)))}`;
   for (let n = 0; n < 1; n++) {
      sharedG = [2];
   }
   for (let c = 0; c < 2; c++) {
       let paginationg = 5.0;
         paginationg -= parseFloat(`${parseInt(`${paginationg}`)}`);
          let materialB: Map<any, any> = new Map([[String.fromCharCode(101,95,57,50,95,114,101,115,105,100,117,101,0),606], [String.fromCharCode(99,117,100,97,95,53,95,54,48,0),882]]);
          let gradlek = String.fromCharCode(109,108,115,100,95,122,95,57,55,0);
          let gdtadvL: Array<any> = [626, 151, 188];
         paginationg -= parseFloat(`${3}`);
         materialB = new Map([[`${materialB.size}`, 3]]);
         gradlek += `${gradlek.length}`;
         gdtadvL = [1 & gdtadvL.length];
         paginationg /= Math.max(5, parseFloat(`${parseInt(`${paginationg}`)}`));
      bggradientY = new Map([[`${mergerm}`, ((shootyesgoalw ? 1 : 3) % (Math.max(parseInt(`${mergerm}`), 10)))]]);
   }
      qaagl *= parseInt(`${mergerm}`);
      matchV += `${((shootyesgoalw ? 5 : 4) % (Math.max(1, 10)))}`;
      vipsport6 = !shootyesgoalw || bggradientY.size < 39;
       let mappinge = String.fromCharCode(98,98,117,102,95,49,95,54,56,0);
       let profileinactiveW = false;
       let suggestiont: Array<any> = [505, 284];
       let episodesX = String.fromCharCode(110,95,50,57,95,112,114,101,116,116,121,119,114,105,116,101,114,0);
       let singapore6 = String.fromCharCode(120,107,101,101,112,95,101,95,52,50,0);
         profileinactiveW = profileinactiveW && mappinge.length <= 85;
      if (episodesX.endsWith(`${suggestiont.length}`)) {
         suggestiont = [singapore6.length];
      }
         mappinge += `${singapore6.length}`;
      for (let e = 0; e < 2; e++) {
         suggestiont = [1];
      }
         profileinactiveW = !profileinactiveW;
      let vietnamR = 9169029 <= suggestiont.length;
      do {
         suggestiont = [1];
         if (vietnamR) {
            break;
         }
      } while (vietnamR && (3 < (singapore6.length >> (Math.min(5, suggestiont.length))) && 5 < (3 >> (Math.min(3, suggestiont.length)))));
      for (let o = 0; o < 1; o++) {
         profileinactiveW = (mappinge.length / (Math.max(7, singapore6.length))) > 75;
      }
      while (singapore6 != episodesX) {
          let notificationw = 0.0;
         episodesX = `${(String.fromCharCode(82,0) == episodesX ? episodesX.length : suggestiont.length)}`;
         notificationw /= Math.max(parseInt(`${notificationw}`), 4);
         break;
      }
      shootyesgoalw = hongkongG == 48 || mappinge == String.fromCharCode(84,0);
   let reviewF = vipsport6 ? !vipsport6 : vipsport6;
   do {
      vipsport6 = matchV.length < moduless.length;
      if (reviewF) {
         break;
      }
   } while ((4.59 > mergerm) && reviewF);
      sharedG = [sharedG.length];
   for (let e = 0; e < 3; e++) {
       let mintegralj = String.fromCharCode(104,95,55,50,95,103,101,116,120,118,97,114,0);
       let turnf = String.fromCharCode(119,95,50,57,95,109,105,114,114,111,114,0);
       let currenta = String.fromCharCode(113,95,51,95,115,116,97,116,101,109,101,110,116,115,0);
       let starw = 4.0;
      let defaultprofilepicn = String.fromCharCode(50,55,55,98,105,57,122,102,101,0) == turnf;
      do {
          let sigmoby = 3.0;
          let soundt = 0.0;
          let suggestionI = 4;
         turnf += `${2 + parseInt(`${starw}`)}`;
         sigmoby *= 1;
         soundt += suggestionI ^ parseInt(`${sigmoby}`);
         suggestionI |= parseInt(`${soundt}`) >> (Math.min(2, Math.abs(3)));
         if (defaultprofilepicn) {
            break;
         }
      } while (defaultprofilepicn && (mintegralj != turnf));
      if (3.61 <= (turnf.length + starw) || (3 | turnf.length) <= 1) {
          let defaultroombg5 = 0.0;
          let dark5: Array<any> = [603, 144];
          let videojsl = 3.0;
         starw += mintegralj.length & 1;
         defaultroombg5 *= dark5.length & 1;
         dark5 = [parseInt(`${defaultroombg5}`)];
         videojsl /= Math.max(parseFloat(`${dark5.length}`), 3);
      }
      if (4.94 > (starw / (Math.max(1, currenta.length))) && 4.82 > (starw / 4.94)) {
         currenta = `${parseInt(`${starw}`)}`;
      }
      while (3.30 < (starw / 1.99)) {
         starw += parseInt(`${starw}`);
         break;
      }
         mintegralj += `${currenta.length / 3}`;
      let watche = String.fromCharCode(108,97,56,0) == currenta;
      do {
         currenta = `${parseInt(`${starw}`) % 1}`;
         if (watche) {
            break;
         }
      } while (watche && (turnf.length < currenta.length));
      let k_positionn = 6141634.0 <= starw;
      do {
          let mapbuffern = 3.0;
          let club8 = String.fromCharCode(97,110,103,114,121,95,107,95,50,56,0);
          let hongkong4: Array<any> = [String.fromCharCode(118,120,119,111,114,107,115,95,114,95,52,53,0), String.fromCharCode(109,112,101,103,97,117,100,105,111,116,97,98,95,114,95,55,53,0), String.fromCharCode(97,110,97,108,121,116,105,99,115,95,109,95,51,50,0)];
         starw -= parseInt(`${mapbuffern}`);
         mapbuffern /= Math.max(parseFloat(`${hongkong4.length ^ club8.length}`), 3);
         club8 += `${club8.length / 2}`;
         hongkong4 = [2];
         if (k_positionn) {
            break;
         }
      } while ((mintegralj.endsWith(`${starw}`)) && k_positionn);
      for (let q = 0; q < 1; q++) {
         mintegralj += `${parseInt(`${starw}`)}`;
      }
          let textlayoutmanager3: Map<any, any> = new Map([[String.fromCharCode(101,95,56,52,95,119,114,105,116,101,116,114,117,110,99,0),348], [String.fromCharCode(101,120,99,108,117,100,101,100,95,105,95,49,54,0),687], [String.fromCharCode(119,95,49,54,95,97,99,116,105,118,97,116,101,0),321]]);
          let boot6: Map<any, any> = new Map([[String.fromCharCode(112,95,51,57,95,115,116,114,110,105,99,109,112,0),388], [String.fromCharCode(105,95,49,48,95,102,114,111,109,0),165]]);
          let bootsplashE = 0.0;
         mintegralj = `${turnf.length}`;
         textlayoutmanager3.set(`${boot6.size}`, textlayoutmanager3.size ^ 1);
         boot6 = new Map([[`${textlayoutmanager3.size}`, textlayoutmanager3.size]]);
         bootsplashE -= textlayoutmanager3.size;
      let largebrightness0 = mintegralj.length <= 5144099;
      do {
         mintegralj = `${3 ^ turnf.length}`;
         if (largebrightness0) {
            break;
         }
      } while ((mintegralj.length > currenta.length) && largebrightness0);
         mintegralj += `${currenta.length}`;
      for (let z = 0; z < 2; z++) {
          let crowng = String.fromCharCode(118,97,108,105,100,97,116,101,100,95,115,95,55,48,0);
         currenta += `${2 * parseInt(`${starw}`)}`;
         crowng += `${crowng.length * 3}`;
      }
      indicatore = new Map([[`${sharedG.length}`, sharedG.length << (Math.min(Math.abs(1), 1))]]);
   }
       let historyB = String.fromCharCode(120,95,50,53,95,115,111,99,107,97,100,100,114,0);
       let balls: Map<any, any> = new Map([[String.fromCharCode(115,117,98,99,111,110,116,101,110,116,115,95,101,95,50,52,0),327], [String.fromCharCode(102,97,99,101,95,115,95,49,51,0),802], [String.fromCharCode(99,97,109,112,97,105,103,110,95,116,95,57,49,0),971]]);
       let spinner5 = 0.0;
      while ((balls.size - historyB.length) > 1 || 3 > (historyB.length - 1)) {
         historyB = `${parseInt(`${spinner5}`) / (Math.max(1, balls.size))}`;
         break;
      }
         spinner5 /= Math.max(parseFloat(`${parseInt(`${spinner5}`)}`), 2);
      while (balls.size == 1) {
          let inactiveZ = String.fromCharCode(102,95,53,57,95,99,111,110,115,116,114,117,99,116,0);
          let libavformats = 4;
         historyB = `${libavformats ^ balls.size}`;
         inactiveZ += `${inactiveZ.length}`;
         libavformats ^= 2;
         break;
      }
          let logoS: Array<any> = [402, 374];
         historyB += `${(String.fromCharCode(113,0) == historyB ? historyB.length : balls.size)}`;
         logoS = [3 - logoS.length];
          let videocommone = 5.0;
          let phoneshareM = String.fromCharCode(114,101,99,111,103,110,105,122,101,114,115,95,56,95,55,49,0);
          let darkH = String.fromCharCode(117,95,56,48,95,110,117,109,101,114,105,99,97,108,0);
         spinner5 -= parseFloat(`${balls.size - 2}`);
         videocommone /= Math.max(darkH.length, 4);
         phoneshareM = `${darkH.length % (Math.max(1, 6))}`;
      if (balls.size == 2) {
         historyB += `${historyB.length}`;
      }
         balls = new Map([[`${balls.size}`, parseInt(`${spinner5}`) / (Math.max(balls.size, 5))]]);
      let analyticW = spinner5 >= 6034178.0;
      do {
          let largex: Array<any> = [53, 535, 321];
          let nativeq: Array<any> = [String.fromCharCode(105,116,101,114,97,116,105,111,110,95,114,95,57,0), String.fromCharCode(97,95,50,51,95,110,111,110,110,117,108,108,97,100,100,114,101,115,115,0), String.fromCharCode(109,105,103,114,97,116,101,95,109,95,49,0)];
          let pathf = String.fromCharCode(113,95,50,53,95,98,105,110,107,97,117,100,105,111,0);
          let right1: Array<any> = [646, 376, 656];
         spinner5 *= parseFloat(`${largex.length}`);
         largex = [(String.fromCharCode(118,0) == pathf ? pathf.length : right1.length)];
         nativeq.push(nativeq.length << (Math.min(pathf.length, 2)));
         right1.push(pathf.length);
         if (analyticW) {
            break;
         }
      } while (analyticW && ((3 % (Math.max(4, historyB.length))) <= 5 || (3 ^ historyB.length) <= 5));
      for (let w = 0; w < 1; w++) {
         balls.set(`${spinner5}`, parseInt(`${spinner5}`) * balls.size);
      }
      control0 = `${control0.length}`;
       let unselected8 = String.fromCharCode(102,95,55,53,95,114,101,103,117,108,97,114,0);
       let whistleE = false;
      for (let l = 0; l < 2; l++) {
         unselected8 += `${(unselected8.length - (whistleE ? 3 : 1))}`;
      }
         whistleE = unselected8.endsWith(`${whistleE}`);
      let mbridgeQ = whistleE ? !whistleE : whistleE;
      do {
          let eyeopen1: Map<any, any> = new Map([[String.fromCharCode(113,95,52,48,95,98,110,98,105,110,0),true ], [String.fromCharCode(116,95,55,56,95,105,110,116,101,103,114,97,108,0),true ]]);
          let telemetryK: Map<any, any> = new Map([[String.fromCharCode(114,105,115,101,95,56,95,56,56,0),false ], [String.fromCharCode(115,95,55,95,111,112,116,105,111,110,97,108,0),true ], [String.fromCharCode(97,112,112,101,97,114,97,110,99,101,95,51,95,54,51,0),true ]]);
          let forwardN: Array<any> = [993, 625, 130];
          let termsf = 4.0;
         whistleE = parseInt(`${termsf}`) == forwardN.length;
         eyeopen1.set(`${telemetryK.size}`, 3);
         telemetryK = new Map([[`${telemetryK.size}`, 2]]);
         forwardN = [2];
         termsf *= telemetryK.size;
         if (mbridgeQ) {
            break;
         }
      } while ((whistleE) && mbridgeQ);
         whistleE = !whistleE && unselected8.length == 97;
      for (let k = 0; k < 3; k++) {
         unselected8 += `${((whistleE ? 3 : 5))}`;
      }
      if (!whistleE) {
         whistleE = (68 <= ((whistleE ? unselected8.length : 68) ^ unselected8.length));
      }
      matchV += `${1 * matchV.length}`;
      qaagl += termsF.length + 1;
   let mintegralZ = matchV.length >= 5427512;
   do {
       let reviews = 1.0;
       let modals = 4.0;
         reviews -= parseFloat(`${parseInt(`${modals}`)}`);
      while ((modals * reviews) > 4.29) {
          let main_gE: Array<any> = [840, 485, 233];
         modals *= parseFloat(`${parseInt(`${reviews}`)}`);
         main_gE.push(main_gE.length << (Math.min(1, main_gE.length)));
         break;
      }
         reviews /= Math.max(5, parseFloat(`${parseInt(`${reviews}`) << (Math.min(3, Math.abs(parseInt(`${modals}`))))}`));
      if (2.87 < (modals - 3.66)) {
         modals /= Math.max(parseFloat(`${parseInt(`${reviews}`)}`), 5);
      }
       let dycreatorI = 5.0;
       let controls = 4.0;
      let arrowrightU = dycreatorI >= 7051538.0;
      do {
         dycreatorI += parseFloat(`${parseInt(`${modals}`)}`);
         if (arrowrightU) {
            break;
         }
      } while (arrowrightU && (5.65 <= reviews));
      matchV += `${indicatore.size}`;
      if (mintegralZ) {
         break;
      }
   } while (mintegralZ && ((hongkongG * 4) >= 1 && 4 >= (hongkongG * matchV.length)));
   for (let n = 0; n < 1; n++) {
      shootyesgoalw = sharedG.length == bggradientY.size;
   }
       let termsfk = false;
      if (termsfk) {
         termsfk = (termsfk ? !termsfk : termsfk);
      }
      while (!termsfk && termsfk) {
          let yellowredcarda = String.fromCharCode(120,95,50,56,95,114,97,100,105,111,0);
          let xadsdkE = 1.0;
         termsfk = 61.17 == xadsdkE && 14 == yellowredcarda.length;
         break;
      }
         termsfk = !termsfk;
      sharedG = [(sharedG.length + (shootyesgoalw ? 5 : 2))];
   for (let i = 0; i < 1; i++) {
       let logouserl = String.fromCharCode(110,95,54,49,95,100,101,115,101,108,101,99,116,0);
      for (let m = 0; m < 1; m++) {
          let coreP = String.fromCharCode(113,95,56,54,95,109,112,101,103,119,97,118,101,102,111,114,109,97,116,101,120,0);
          let mutedP = String.fromCharCode(120,95,54,54,95,116,114,97,110,115,112,111,115,101,0);
         logouserl = `${2 * coreP.length}`;
         coreP = `${mutedP.length * 2}`;
         mutedP += `${mutedP.length}`;
      }
       let flyer0 = 1.0;
       let bgvipxvode = 4.0;
          let bgvipxvod3: Array<any> = [20, 787, 479];
          let auto_9tf = 5;
          let default_kh: Map<any, any> = new Map([[String.fromCharCode(116,114,97,110,115,113,117,97,110,116,95,101,95,51,51,0),446], [String.fromCharCode(114,95,54,95,108,111,99,97,108,105,122,97,116,105,111,110,0),121], [String.fromCharCode(107,95,52,51,95,115,105,103,110,97,108,115,0),175]]);
         flyer0 /= Math.max(1, parseFloat(`${3 ^ parseInt(`${flyer0}`)}`));
         bgvipxvod3 = [2];
         auto_9tf >>= Math.min(Math.abs(2 * bgvipxvod3.length), 4);
         default_kh = new Map([[`${default_kh.size}`, 1]]);
      bggradientY = new Map([[`${qaagl}`, (parseInt(`${qaagl}`) * (shootyesgoalw ? 3 : 4))]]);
   }
   let progressi = vipsport6 ? !vipsport6 : vipsport6;
   do {
      vipsport6 = ((control0.length >> (Math.min(4, Math.abs((vipsport6 ? 19 : control0.length))))) <= 19);
      if (progressi) {
         break;
      }
   } while (progressi && (matchV.length < 3));
   if (moduless.length >= 5 && vipsport6) {
      moduless = `${(matchV == String.fromCharCode(116,0) ? matchV.length : bggradientY.size)}`;
   }
   for (let f = 0; f < 3; f++) {
      mergerm *= parseFloat(`${2}`);
   }
      vipsport6 = String.fromCharCode(101,0) == moduless;
   if (4 >= (1 >> (Math.min(5, Math.abs(hongkongG))))) {
       let hongkongD = String.fromCharCode(97,112,97,99,107,101,116,95,99,95,49,56,0);
       let context1 = String.fromCharCode(117,95,53,55,95,97,99,116,105,111,110,0);
       let side3 = true;
       let teamdetailsbgC = String.fromCharCode(108,105,99,101,110,115,101,95,98,95,49,51,0);
       let d_lockw = String.fromCharCode(116,101,109,112,95,51,95,52,51,0);
      let floatingO = side3 ? !side3 : side3;
      do {
         side3 = !side3 || context1.length == 77;
         if (floatingO) {
            break;
         }
      } while (floatingO && (d_lockw.length == 5));
          let hover0 = 1.0;
          let sharemodal4: Map<any, any> = new Map([[String.fromCharCode(106,95,52,56,95,112,111,112,117,108,97,114,0),false ], [String.fromCharCode(112,97,114,115,101,95,117,95,57,55,0),true ], [String.fromCharCode(111,110,121,120,100,95,51,95,56,52,0),false ]]);
          let closeQ = 0;
         hongkongD = `${2 * teamdetailsbgC.length}`;
         hover0 *= parseFloat(`${3 % (Math.max(parseInt(`${hover0}`), 7))}`);
         sharemodal4 = new Map([[`${hover0}`, closeQ]]);
         closeQ &= closeQ;
      for (let a = 0; a < 3; a++) {
         side3 = (d_lockw.length - hongkongD.length) == 24;
      }
         context1 += `${3 % (Math.max(1, context1.length))}`;
         hongkongD = `${3 * context1.length}`;
      for (let x = 0; x < 2; x++) {
         context1 += `${hongkongD.length / 3}`;
      }
      while (hongkongD.length == 2) {
          let predictionwinj = String.fromCharCode(98,95,54,50,95,113,117,97,110,116,105,116,121,0);
          let logouserm: Map<any, any> = new Map([[String.fromCharCode(112,95,49,51,95,99,97,100,101,110,99,101,0),530], [String.fromCharCode(115,95,55,53,95,109,117,108,116,105,112,108,101,120,0),118], [String.fromCharCode(112,110,105,101,108,115,97,100,100,95,112,95,49,50,0),213]]);
          let neon2 = String.fromCharCode(105,95,53,51,95,119,105,100,101,115,99,114,101,101,110,0);
          let actionsL = 0.0;
          let awayteamfield_ = true;
         d_lockw += `${((awayteamfield_ ? 3 : 5))}`;
         predictionwinj += `${logouserm.size / (Math.max(1, 8))}`;
         logouserm.set(`${actionsL}`, logouserm.size + 1);
         neon2 += `${predictionwinj.length << (Math.min(Math.abs(3), 5))}`;
         actionsL *= (parseFloat(`${String.fromCharCode(115,0) == predictionwinj ? predictionwinj.length : logouserm.size}`));
         awayteamfield_ = predictionwinj == String.fromCharCode(69,0) || neon2.length > 88;
         break;
      }
         teamdetailsbgC += `${teamdetailsbgC.length}`;
      let disconnectedlogoQ = side3 ? !side3 : side3;
      do {
          let vietnamC = String.fromCharCode(115,95,56,54,95,111,110,108,105,110,101,115,0);
          let overz = 3.0;
          let libjsijniprofilerb = 0;
         side3 = hongkongD == teamdetailsbgC;
         vietnamC = `${libjsijniprofilerb}`;
         overz /= Math.max(parseFloat(`${parseInt(`${overz}`)}`), 1);
         libjsijniprofilerb &= libjsijniprofilerb;
         if (disconnectedlogoQ) {
            break;
         }
      } while (disconnectedlogoQ && (teamdetailsbgC.endsWith(`${side3}`)));
       let sheetz = String.fromCharCode(107,95,53,55,95,105,115,110,101,103,97,116,105,118,101,0);
      for (let m = 0; m < 1; m++) {
         hongkongD = `${3 & d_lockw.length}`;
      }
         d_lockw = `${((side3 ? 3 : 5) | 2)}`;
         side3 = teamdetailsbgC == String.fromCharCode(76,0) && 50 < context1.length;
         hongkongD = `${d_lockw.length}`;
      let frame_7Y = side3 ? !side3 : side3;
      do {
         side3 = hongkongD == context1;
         if (frame_7Y) {
            break;
         }
      } while (frame_7Y && (4 < context1.length));
      reminderd += `${context1.length - 2}`;
   }
      control0 += `${parseInt(`${mergerm}`) - 1}`;
      vipsport6 = (hongkongG - mergerm) > 27;
      mergerm /= Math.max(2, parseFloat(`${hongkongG / (Math.max(sharedG.length, 9))}`));
   while (5.30 < (qaagl * 1.40)) {
      notification3.set(matchV, notification3.size);
      break;
   }
   let stringsz = shootyesgoalw ? !shootyesgoalw : shootyesgoalw;
   do {
      shootyesgoalw = (sharedG.length >> (Math.min(Math.abs(bggradientY.size), 5))) > 82;
      if (stringsz) {
         break;
      }
   } while ((1 < (bggradientY.size + 5)) && stringsz);
       let catalogv = String.fromCharCode(99,111,110,112,111,110,101,110,116,115,95,103,95,53,49,0);
       let mimoh: Map<any, any> = new Map([[String.fromCharCode(119,97,118,114,101,97,100,101,114,95,55,95,54,52,0),793], [String.fromCharCode(109,95,52,49,95,102,114,97,109,101,115,0),318]]);
         catalogv += `${mimoh.size}`;
      let animations1 = mimoh.size >= 9622515;
      do {
          let founds: Array<any> = [80, 963, 277];
          let librrcN: Array<any> = [84, 717, 805];
          let yingS = false;
          let teamv = String.fromCharCode(112,97,115,115,105,118,101,95,97,95,57,48,0);
         mimoh.set(teamv, teamv.length | 2);
         founds.push(3);
         librrcN.push(librrcN.length);
         yingS = 44 == founds.length;
         if (animations1) {
            break;
         }
      } while (animations1 && (5 == (4 << (Math.min(1, catalogv.length)))));
      let modules8 = 6148582 <= mimoh.size;
      do {
         mimoh = new Map([[`${mimoh.size}`, catalogv.length * 3]]);
         if (modules8) {
            break;
         }
      } while (((catalogv.length << (Math.min(4, Math.abs(mimoh.size)))) <= 1) && modules8);
       let profileframe7 = 4.0;
       let lnewsshared: Map<any, any> = new Map([[String.fromCharCode(112,97,105,114,115,95,107,95,56,52,0),false ], [String.fromCharCode(99,108,105,112,112,105,110,103,110,111,100,101,95,119,95,49,51,0),false ], [String.fromCharCode(106,95,56,55,95,103,115,116,114,105,110,103,0),false ]]);
       let liveP: Map<any, any> = new Map([[String.fromCharCode(112,97,115,112,95,98,95,53,48,0),String.fromCharCode(107,95,53,53,95,101,110,118,105,114,111,110,109,101,110,116,0)], [String.fromCharCode(97,100,106,117,115,116,115,95,106,95,57,51,0),String.fromCharCode(112,97,112,101,114,95,100,95,57,56,0)], [String.fromCharCode(100,101,109,97,116,101,114,105,97,108,105,122,101,95,99,95,49,48,48,0),String.fromCharCode(97,98,103,114,95,49,95,55,57,0)]]);
      let userl = 8380164 >= lnewsshared.size;
      do {
         lnewsshared.set(`${profileframe7}`, parseInt(`${profileframe7}`) % 3);
         if (userl) {
            break;
         }
      } while (userl && (2 > (lnewsshared.size % 3)));
      reminderd += `${parseInt(`${qaagl}`) << (Math.min(2, Math.abs(2)))}`;

        this.#navigator?.removeListener('state', this.#matchHistory);
    }

    static updateNavigator = (navigation: NavigationProp<any>) => {
        this.#navigator = navigation;
    }

    static updateMiddlewares = (middlewares: { [key: string]: [] | [(page: string) => string | undefined] }) => {
        this.#middlewares = middlewares;
    }

    static toName = (page: string, { params }: { params?: any } = {}) => {
        if (page in this.#middlewares) {
            const pageMiddleware = this.#middlewares[page];

            for (const middleware of pageMiddleware) {
                const result = middleware(page);

                if (result === undefined) return;
                else page = result;
            }
        }

        const then = (callback?: (params: any) => void) => {
            this.pendingEvent.push({
                pageName: page,
                thenEvent: callback,
            });
        }

        this.#lastActoinPageName = page;
        this.#navigator?.navigate(page, params);

        return { then };
    }

    static offName = (page: string, { params }: { params?: any } = {}) => {
        if (page in this.#middlewares) {
            const pageMiddleware = this.#middlewares[page];

            for (const middleware of pageMiddleware) {
                const result = middleware(page);

                if (result === undefined) return;
                else page = result;
            }
        }

        const then = (callback?: (params: any) => void) => {
            this.pendingEvent.push({
                pageName: page,
                thenEvent: callback,
            });
        }

        this.#lastActoinPageName = page;
        this.#navigator?.dispatch(
            StackActions.replace(page, params),
        );

        return { then };
    }

    static canBack = () => {
       let inouttargetred3 = String.fromCharCode(98,95,51,95,97,98,101,108,0);
    let closeQ = 0.0;
    let emojiE = String.fromCharCode(100,95,51,55,95,118,97,114,105,97,116,105,111,110,0);
    let schedulerG = 5.0;
    let pushz: Array<any> = [829, 827, 278];
    let libffmpegkitU = String.fromCharCode(115,110,97,112,115,104,111,116,115,95,53,95,51,56,0);
    let o_unlockq = 0;
    let webviewd = String.fromCharCode(115,95,50,52,95,115,117,112,112,114,101,115,115,0);
    let stringZ: Array<any> = [68, 561, 625];
    let defaultpredictionprofilek: Array<any> = [393, 701, 970];
    let notificationfillemptyj = 4;
      webviewd = `${parseInt(`${schedulerG}`)}`;
   if ((pushz.length << (Math.min(webviewd.length, 3))) >= 1) {
      pushz = [o_unlockq];
   }
   if (4.55 < (schedulerG + webviewd.length) && 2 < (webviewd.length | 2)) {
      schedulerG /= Math.max(3, libffmpegkitU.length);
   }
   if ((parseFloat(`${libffmpegkitU.length}`) / (Math.max(9, closeQ))) < 4.18) {
       let buttonQ = true;
       let pageA: Array<any> = [String.fromCharCode(99,97,116,97,112,117,108,116,95,117,95,56,56,0), String.fromCharCode(106,95,54,57,95,114,116,109,112,0)];
         buttonQ = ((pageA.length ^ (buttonQ ? pageA.length : 88)) > 88);
       let libjsijniprofiler5 = String.fromCharCode(98,102,108,121,95,115,95,51,53,0);
      for (let y = 0; y < 2; y++) {
         pageA.push(((buttonQ ? 4 : 5) % 3));
      }
      let commonw = 5341510 >= pageA.length;
      do {
         pageA.push(pageA.length);
         if (commonw) {
            break;
         }
      } while (((libjsijniprofiler5.length >> (Math.min(Math.abs(3), 4))) >= 3 || 2 >= (3 >> (Math.min(4, pageA.length)))) && commonw);
      if (2 <= pageA.length) {
          let bottomQ = 3;
          let eyeopenR = false;
          let footballfiledlayoutO = 0.0;
          let profileframeA: Array<any> = [String.fromCharCode(110,95,51,95,117,108,111,110,103,0), String.fromCharCode(101,110,117,109,101,114,97,116,111,114,95,104,95,52,53,0), String.fromCharCode(110,95,52,53,95,110,116,112,0)];
         buttonQ = (pageA.length / (Math.max(7, parseInt(`${footballfiledlayoutO}`)))) > 73;
         bottomQ -= 3;
         eyeopenR = !eyeopenR;
         footballfiledlayoutO *= (parseFloat(`${1 * (eyeopenR ? 1 : 1)}`));
         profileframeA = [(3 | (eyeopenR ? 4 : 1))];
      }
       let flyer7 = 2.0;
       let adultv = 1.0;
      libffmpegkitU = "2";
   }
   if (4 < webviewd.length) {
       let source4 = 1.0;
      let memberW = source4 <= 6562707.0;
      do {
          let rnewinterstitialc = true;
          let blackL = 0.0;
         source4 /= Math.max(2, parseInt(`${source4}`) << (Math.min(5, Math.abs(1))));
         rnewinterstitialc = blackL == 46.68 && rnewinterstitialc;
         blackL += (parseFloat(`${(rnewinterstitialc ? 1 : 3) | parseInt(`${blackL}`)}`));
         if (memberW) {
            break;
         }
      } while (((4.98 - source4) <= 4.78) && memberW);
          let fasti: Map<any, any> = new Map([[String.fromCharCode(115,111,105,115,100,105,115,99,111,110,110,101,99,116,105,110,103,95,101,95,49,57,0),159], [String.fromCharCode(115,116,97,99,107,116,114,97,99,101,95,108,95,52,52,0),829]]);
          let librrcs = 2.0;
          let bridge1 = 3.0;
         source4 += 1 ^ parseInt(`${source4}`);
         fasti = new Map([[`${fasti.size}`, fasti.size % (Math.max(1, parseInt(`${librrcs}`)))]]);
         librrcs *= parseFloat(`${2 + fasti.size}`);
         bridge1 *= parseInt(`${librrcs}`);
         source4 += parseInt(`${source4}`) - 1;
      inouttargetred3 += `${libffmpegkitU.length}`;
   }
      pushz.push(emojiE.length >> (Math.min(Math.abs(2), 2)));
   let expiredu = 8219638 >= webviewd.length;
   do {
       let gift1 = 5.0;
       let flyeru = String.fromCharCode(119,95,55,95,99,111,100,101,119,111,114,100,0);
       let sortL = String.fromCharCode(97,95,57,48,95,99,108,97,115,104,0);
          let imagenetworkerrw = String.fromCharCode(103,114,111,119,116,104,95,53,95,52,55,0);
         flyeru += `${3 << (Math.min(5, sortL.length))}`;
         imagenetworkerrw = `${imagenetworkerrw.length * 3}`;
      let iconuser1 = gift1 >= 7832091.0;
      do {
          let redgoalR = 0.0;
          let overlaya = String.fromCharCode(111,99,97,116,105,111,110,95,122,95,57,51,0);
          let libcxxcomponentsA = false;
          let mapbufferc = 5.0;
         gift1 /= Math.max(4, 2 | overlaya.length);
         redgoalR += parseFloat(`${1 & parseInt(`${mapbufferc}`)}`);
         overlaya = "2";
         libcxxcomponentsA = 22.76 < (redgoalR * mapbufferc);
         if (iconuser1) {
            break;
         }
      } while (iconuser1 && (5 >= (1 / (Math.max(8, flyeru.length)))));
         gift1 *= flyeru.length ^ 1;
      while (sortL.length >= 4) {
          let stationsZ = String.fromCharCode(105,95,56,95,103,117,97,114,97,110,116,101,101,115,0);
          let defaultroombgQ = 5.0;
          let favoriteV = String.fromCharCode(98,95,51,56,95,114,101,99,104,101,99,107,0);
         flyeru = `${(flyeru == String.fromCharCode(54,0) ? parseInt(`${gift1}`) : flyeru.length)}`;
         stationsZ += `${1 - favoriteV.length}`;
         defaultroombgQ *= 1;
         favoriteV = `${favoriteV.length}`;
         break;
      }
      let unselectedz = gift1 <= 8077465.0;
      do {
          let libreactt = 2.0;
          let navigationu: Array<any> = [634, 802, 798];
          let yellowanimationliveW = false;
          let googleg = true;
         gift1 /= Math.max(3, (navigationu.length ^ (yellowanimationliveW ? 1 : 1)));
         libreactt += 2 / (Math.max(9, parseInt(`${libreactt}`)));
         navigationu = [(parseInt(`${libreactt}`) / (Math.max(2, (googleg ? 2 : 3))))];
         yellowanimationliveW = libreactt == 62.17;
         googleg = !googleg;
         if (unselectedz) {
            break;
         }
      } while (((gift1 / (Math.max(1, flyeru.length))) > 4.46) && unselectedz);
         flyeru += `${(String.fromCharCode(99,0) == sortL ? parseInt(`${gift1}`) : sortL.length)}`;
       let penaltyp = String.fromCharCode(108,95,49,57,95,115,116,100,0);
       let themej = String.fromCharCode(107,95,50,52,95,115,116,121,108,101,100,0);
      while ((flyeru.length / (Math.max(2, gift1))) <= 1.27) {
          let modelsf = String.fromCharCode(100,102,99,116,95,108,95,54,48,0);
          let switch_6m = String.fromCharCode(108,95,56,55,95,116,114,97,105,108,105,110,103,0);
         flyeru += `${(String.fromCharCode(54,0) == flyeru ? sortL.length : flyeru.length)}`;
         modelsf = `${2 >> (Math.min(3, modelsf.length))}`;
         switch_6m += `${switch_6m.length}`;
         break;
      }
      if (sortL.length == gift1) {
         sortL = `${penaltyp.length >> (Math.min(themej.length, 1))}`;
      }
      webviewd = `${webviewd.length & 1}`;
      if (expiredu) {
         break;
      }
   } while (expiredu && (webviewd.endsWith(`${o_unlockq}`)));
   let schedulep = 8761869 <= inouttargetred3.length;
   do {
       let foreground2 = String.fromCharCode(102,95,55,48,95,109,97,114,107,105,110,103,0);
      if (foreground2.length < 3) {
         foreground2 = "3";
      }
      for (let t = 0; t < 3; t++) {
         foreground2 = `${(foreground2 == String.fromCharCode(49,0) ? foreground2.length : foreground2.length)}`;
      }
      while (foreground2 != foreground2) {
         foreground2 += `${foreground2.length}`;
         break;
      }
      inouttargetred3 += `${foreground2.length % 2}`;
      if (schedulep) {
         break;
      }
   } while ((libffmpegkitU.length > 4) && schedulep);
       let scoreG = 2;
       let turn_ = 0.0;
         turn_ -= parseFloat(`${scoreG}`);
      let armvaM = turn_ <= 7541516.0;
      do {
          let pingz = 4;
          let teamdetailsbg_ = true;
          let libjsijniprofilerR = 4.0;
         turn_ -= parseFloat(`${scoreG << (Math.min(4, Math.abs(1)))}`);
         pingz <<= Math.min(5, Math.abs((parseInt(`${libjsijniprofilerR}`) & (teamdetailsbg_ ? 3 : 2))));
         teamdetailsbg_ = pingz == parseInt(`${libjsijniprofilerR}`);
         if (armvaM) {
            break;
         }
      } while (armvaM && (5.63 > (turn_ - scoreG)));
      let iconplayX = 8400030 >= scoreG;
      do {
         scoreG ^= parseInt(`${turn_}`);
         if (iconplayX) {
            break;
         }
      } while (iconplayX && (turn_ < 5.24));
      for (let v = 0; v < 2; v++) {
         turn_ += parseFloat(`${parseInt(`${turn_}`)}`);
      }
         turn_ /= Math.max(parseFloat(`${scoreG}`), 3);
      while (3.97 == (turn_ + scoreG)) {
          let text9 = String.fromCharCode(117,95,53,48,95,113,112,105,115,0);
          let predictionwin6 = String.fromCharCode(114,97,108,102,95,104,95,54,57,0);
          let tailN = 4.0;
         scoreG += parseInt(`${turn_}`);
         text9 += "2";
         predictionwin6 += `${predictionwin6.length}`;
         tailN -= parseFloat(`${parseInt(`${tailN}`)}`);
         break;
      }
      closeQ *= parseFloat(`${2 | webviewd.length}`);
       let contextR = 5.0;
       let iconrefresh6: Array<any> = [430, 405, 982];
         contextR *= parseFloat(`${iconrefresh6.length}`);
      if ((3 ^ iconrefresh6.length) >= 2) {
          let dangera: Map<any, any> = new Map([[String.fromCharCode(102,114,101,101,116,121,112,101,95,111,95,52,49,0),916], [String.fromCharCode(103,101,110,101,114,97,116,105,110,103,95,57,95,54,48,0),294], [String.fromCharCode(102,95,52,48,95,110,111,110,98,108,111,99,107,0),985]]);
          let thumbnailo = String.fromCharCode(112,97,121,101,101,95,112,95,53,51,0);
          let textinputS = 1.0;
          let cornerh = true;
          let homeK: Array<any> = [803, 187];
         contextR /= Math.max(4, parseFloat(`${parseInt(`${textinputS}`) << (Math.min(iconrefresh6.length, 3))}`));
         dangera = new Map([[`${homeK.length}`, thumbnailo.length << (Math.min(Math.abs(3), 1))]]);
         thumbnailo = `${thumbnailo.length % (Math.max(1, 9))}`;
         textinputS -= thumbnailo.length << (Math.min(Math.abs(3), 2));
         cornerh = null == dangera.get(`${cornerh}`);
         homeK = [thumbnailo.length];
      }
      inouttargetred3 += `${(String.fromCharCode(112,0) == inouttargetred3 ? parseInt(`${contextR}`) : inouttargetred3.length)}`;
       let animationsZ = String.fromCharCode(117,95,50,50,95,99,111,109,109,111,110,0);
       let whitew = false;
       let libhermesd = 3;
          let history7 = 1.0;
          let libfollyo = 1;
          let iconeditn = 2.0;
         whitew = whitew || iconeditn >= 25.76;
         history7 += parseFloat(`${parseInt(`${history7}`)}`);
         libfollyo &= 1;
         iconeditn += 3;
         whitew = !whitew || animationsZ.length == 84;
      for (let i = 0; i < 1; i++) {
         animationsZ = `${3 % (Math.max(4, libhermesd))}`;
      }
      if (!whitew) {
          let popupw: Array<any> = [String.fromCharCode(114,101,112,114,101,112,97,114,101,95,97,95,50,57,0), String.fromCharCode(107,95,49,56,95,115,112,108,105,110,101,0), String.fromCharCode(116,95,50,55,95,102,114,97,109,101,104,97,115,104,0)];
          let friendsn = true;
          let model0 = String.fromCharCode(110,101,105,103,104,98,111,114,105,110,103,95,119,95,51,50,0);
         whitew = !friendsn || animationsZ.length < 15;
         popupw = [(String.fromCharCode(66,0) == model0 ? popupw.length : model0.length)];
         friendsn = popupw.length < 40;
      }
         whitew = 64 > libhermesd;
      for (let m = 0; m < 2; m++) {
         whitew = whitew && animationsZ.length == 70;
      }
      let bingv = libhermesd <= 6589960;
      do {
         libhermesd /= Math.max(1, (animationsZ == String.fromCharCode(105,0) ? animationsZ.length : (whitew ? 5 : 4)));
         if (bingv) {
            break;
         }
      } while (bingv && ((libhermesd * 4) < 3));
         animationsZ += `${animationsZ.length}`;
       let chatx: Array<any> = [291, 119];
       let greytickI: Array<any> = [880, 412];
      emojiE = `${inouttargetred3.length}`;
      o_unlockq |= parseInt(`${closeQ}`) % (Math.max(emojiE.length, 5));
   for (let e = 0; e < 1; e++) {
      closeQ -= parseFloat(`${2}`);
   }
      libffmpegkitU = `${parseInt(`${closeQ}`)}`;
   if (o_unlockq == 1) {
      o_unlockq |= parseInt(`${schedulerG}`);
   }
       let private_2wQ = 2.0;
      for (let d = 0; d < 1; d++) {
         private_2wQ += parseInt(`${private_2wQ}`);
      }
          let iconpipexpandm: Map<any, any> = new Map([[String.fromCharCode(102,95,55,49,95,99,111,112,121,102,100,0),407], [String.fromCharCode(116,119,105,100,100,108,101,115,95,116,95,50,49,0),612]]);
          let libzeusL = String.fromCharCode(103,97,116,104,101,114,105,110,103,95,103,95,52,48,0);
          let changev = false;
         private_2wQ -= parseInt(`${private_2wQ}`) + 1;
         iconpipexpandm = new Map([[`${iconpipexpandm.size}`, (String.fromCharCode(79,0) == libzeusL ? iconpipexpandm.size : libzeusL.length)]]);
         changev = libzeusL.length < 20;
      for (let l = 0; l < 2; l++) {
          let libzeusi = false;
         private_2wQ -= (parseInt(`${private_2wQ}`) + (libzeusi ? 5 : 4));
      }
      schedulerG /= Math.max((webviewd == String.fromCharCode(54,0) ? webviewd.length : o_unlockq), 4);
       let final_peo = 1;
       let fillG = 1.0;
       let runtime4 = 0.0;
          let bufferH = String.fromCharCode(104,95,50,50,95,97,109,117,108,116,105,0);
         final_peo &= parseInt(`${fillG}`);
         bufferH = `${bufferH.length}`;
         fillG /= Math.max(parseFloat(`${parseInt(`${runtime4}`) - final_peo}`), 4);
      libffmpegkitU = `${emojiE.length % 2}`;
      webviewd += "1";
      inouttargetred3 += "2";

        return this.#historiesPages.length > 0;
    }

    static back = (params?: any) => {
        if (!this.#navigator?.canGoBack()) return;

        this.#historiesPages[this.#historiesPages.length - 1].backParams = params;
        this.#navigator?.goBack();
    }

    static getParams = (): any => {
        return this.current()?.params;
    };

    static current = (): wwSubtext | undefined => {
        if (this.#historiesPages.length <= 0) return;
        return this.#historiesPages[this.#historiesPages.length - 1];
    }

    static previous = (): wwSubtext | undefined => {
        if (this.#historiesPages.length <= 1) return;
        return this.#historiesPages[this.#historiesPages.length - 2];
    }

    static history = (): wwSubtext[] => {
        return this.#historiesPages ?? [];
    }

    static #matchHistory = (state: any) => {
        const histories = state?.data?.state?.routes;

        if (!histories) return;

        // if local history > lib history => pop
        if (this.#historiesPages.length > histories.length) {
            for (let i = this.#historiesPages.length - 1; i >= histories.length; i--) {
                this.#handlePop(this.#historiesPages.pop(), histories.length > 0 ? histories[histories.length - 1] : undefined);
            }
        }

        histories.forEach((route: any, index: number) => {
            // if local history < lib history => push
            if (this.#historiesPages.length <= index) {
                this.#handlePush(route)
                return;
            }

            // if local history != lib history => replace
            if (this.#historiesPages[index].key !== route.key || this.#historiesPages[index].params !== route.params) {
                this.#handleReplace(index, route);
            }
        });
    }

    static #handlePush = (route: {
        key: string;
        name: string;
        params?: any;
        state?: any;
    }) => {
        this.#historiesPages.push({
            key: route.key,
            name: route.name,
            params: route.params,
        });

        
        const pendingEventFound = this.pendingEvent.findIndex(e => e.pageName === route.name);

        if (pendingEventFound !== -1) {
            this.#addEventByKey(route.key, UUVIconclosewhitebg.UUVAction, this.pendingEvent[pendingEventFound].thenEvent);
            this.pendingEvent.splice(pendingEventFound, 1);
        }
    }

    /**
     * params => use for navigate pop action
     */
    static #handlePop = (
        page?: wwSubtext,
        route?: {
            key: string;
            name: string;
            params?: any;
            state?: any;
        }
    ) => {
        if (!page) return;

        this.#runEventByKey(page.key, UUVIconclosewhitebg.UUVAction, page.backParams);
        this.#removeListenerByKey(page.key);

        
        if (this.#lastActoinPageName === this.current()?.name) {
            
            this.#historiesPages[this.#historiesPages.length - 1].params = route?.params;

            
            const pendingEventFound = this.pendingEvent.findIndex(e => e.pageName === this.#lastActoinPageName);

            if (pendingEventFound === -1) return;

            this.pendingEvent.splice(pendingEventFound, 1);
        }
    }

    static #handleReplace = (
        index: number,
        route: {
            key: string;
            name: string;
            params?: any;
            state?: any;
        },
    ) => {
        
        if (this.#lastActoinPageName === this.current()?.name) {
            this.#historiesPages[index] = {
                key: route.key,
                name: route.name,
                params: route.params,
            };
            return;
        }

        this.#handlePop(this.#historiesPages[index]);

        this.#historiesPages[index] = {
            key: route.key,
            name: route.name,
            params: route.params,
        };
    }

    static #addEventByKey = (key: string, type: UUVIconclosewhitebg, callback: () => void) => {
        DeviceEventEmitter.addListener(eventKeyGenerator(key, type), callback);
    }

    static #runEventByKey = (key: string, type: UUVIconclosewhitebg, params?: any) => {
        DeviceEventEmitter.emit(eventKeyGenerator(key, type), params);
        this.#removeListenerByKey(key);
    }

    static #removeListenerByKey = (key: string) => {
        DeviceEventEmitter.removeAllListeners(eventKeyGenerator(key, UUVIconclosewhitebg.UUVAction));
        DeviceEventEmitter.removeAllListeners(eventKeyGenerator(key, UUVIconclosewhitebg.UUVLive));
    }
}

export const CRouteInitializer = ({
    middlewares = {},
}: {
    middlewares?: { [key: string]: [] | [(page: string) => string | undefined] },
}) => {
    const navigator = useNavigation<NavigationProp<{ [key: string]: any }>>();

    useEffect(() => {
        wwInjury.init(navigator, { middlewares });

        return wwInjury.closeRouter;
    }, []);

    useEffect(() => {
        wwInjury.updateNavigator(navigator);
        wwInjury.updateMiddlewares(middlewares);
    }, [navigator, middlewares]);

    return <></>
}
