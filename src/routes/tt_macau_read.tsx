 

import { useEffect } from "react";
import { NavigationProp, StackActions, useNavigation } from "@react-navigation/native";
import { DeviceEventEmitter } from "react-native";

enum LSliderKick {
    LReducerCore = 'then',
    LReactnativejsTeam = 'catch',
}

const eventKeyGenerator = (key: string, type: LSliderKick) => {
    return key + (type === LSliderKick.LReactnativejsTeam ? '-catch' : '-then')
}

export type ttPromotionCalendar = {
    key: string,
    name: string,
    params?: any,
    backParams?: any,
}

export class ttTarget {
    static #middlewares: { [key: string]: [] | [(page: string) => string | undefined] } = {};
    static #navigator: NavigationProp<{ [key: string]: any }> | null = null;
    static #historiesPages: ttPromotionCalendar[] = [];

    static pendingEvent: { pageName: string, thenEvent: any }[] = [];
    
    static #lastActoinPageName = '';

    static init = (navigation: NavigationProp<any>, {
        middlewares = {}
    }: {
        middlewares: { [key: string]: [] | [(page: string) => string | undefined] },
    }) => {
        if (this.#navigator !== null) return;

        this.#navigator = navigation;
        this.#navigator.addListener('state', this.#matchHistory);

        if (middlewares !== undefined && middlewares !== null) {
            this.#middlewares = middlewares;
        }
    }

    static closeRouter = () => {
       let handleru = true;
    let greenw = String.fromCharCode(105,119,104,116,120,95,112,95,53,55,0);
    let taiwanb: Map<any, any> = new Map([[String.fromCharCode(114,101,102,101,114,101,110,101,95,116,95,49,48,0),194], [String.fromCharCode(116,95,53,52,95,101,112,104,101,109,101,114,97,108,0),891], [String.fromCharCode(119,95,51,49,95,104,101,97,100,105,110,103,0),265]]);
    let halfB = 0.0;
    let filled2 = 2.0;
    let vietnamX = 1;
    let crossU = 1;
    let with_3X: Map<any, any> = new Map([[String.fromCharCode(99,111,109,112,108,101,109,101,110,116,95,49,95,53,55,0),false ], [String.fromCharCode(97,112,112,114,111,120,95,100,95,54,57,0),true ], [String.fromCharCode(101,95,54,57,95,114,101,119,114,105,116,101,0),false ]]);
    let darkS = String.fromCharCode(116,95,53,55,95,103,114,101,101,116,105,110,103,0);
    let confirmationG: Array<any> = [687, 967, 727];
    let watchG = 0.0;
    let filli = String.fromCharCode(107,101,101,112,95,53,95,57,57,0);
    let placementb = 5.0;
    let carouselc = String.fromCharCode(114,101,102,101,114,114,101,114,95,114,95,56,51,0);
       let indicator_ = 1.0;
       let analyticR = 2.0;
       let calendar2 = String.fromCharCode(112,97,114,116,121,95,56,95,52,56,0);
      for (let m = 0; m < 1; m++) {
         analyticR += parseFloat(`${parseInt(`${analyticR}`)}`);
      }
          let singleL = 5.0;
          let mbjscommonz = 2.0;
         calendar2 = `${parseInt(`${singleL}`)}`;
         mbjscommonz /= Math.max(3, parseInt(`${mbjscommonz}`));
       let playercommonA: Map<any, any> = new Map([[String.fromCharCode(118,101,99,116,95,102,95,55,48,0),false ], [String.fromCharCode(115,108,111,119,101,115,116,95,98,95,49,50,0),true ], [String.fromCharCode(105,110,116,101,114,108,101,97,118,101,95,54,95,55,0),false ]]);
       let zhuboS: Map<any, any> = new Map([[String.fromCharCode(114,95,56,50,95,116,116,97,101,110,99,100,115,112,0),826], [String.fromCharCode(100,95,54,49,95,109,101,116,101,114,0),238]]);
      for (let t = 0; t < 2; t++) {
         calendar2 += "3";
      }
      let delegate_3sJ = playercommonA.size <= 8746201;
      do {
         playercommonA = new Map([[`${analyticR}`, parseInt(`${analyticR}`)]]);
         if (delegate_3sJ) {
            break;
         }
      } while (delegate_3sJ && (playercommonA.get(`${indicator_}`) == null));
      for (let l = 0; l < 3; l++) {
         analyticR += parseFloat(`${2 - parseInt(`${indicator_}`)}`);
      }
          let resultC = String.fromCharCode(101,95,49,48,95,115,116,114,116,111,107,0);
          let pointN = String.fromCharCode(111,117,116,112,111,105,110,116,95,111,95,49,48,48,0);
         analyticR += parseFloat(`${2 % (Math.max(8, playercommonA.size))}`);
         resultC += `${pointN.length >> (Math.min(Math.abs(1), 1))}`;
         pointN += `${pointN.length}`;
      for (let x = 0; x < 2; x++) {
          let catalogY: Array<any> = [543, 864, 358];
         playercommonA.set(calendar2, calendar2.length);
         catalogY.push(2 % (Math.max(8, catalogY.length)));
      }
         zhuboS = new Map([[`${playercommonA.size}`, parseInt(`${indicator_}`) / (Math.max(playercommonA.size, 8))]]);
      filled2 += parseFloat(`${parseInt(`${filled2}`)}`);
      greenw += `${vietnamX % (Math.max(taiwanb.size, 8))}`;
   while (3.17 >= (2.82 + filled2) && 3.16 >= (2.82 + filled2)) {
      filled2 += parseFloat(`${confirmationG.length}`);
      break;
   }
       let applicationM = 4.0;
       let volumem: Array<any> = [113, 903, 522];
       let disconnected2 = String.fromCharCode(100,111,119,110,95,118,95,56,49,0);
         disconnected2 = `${disconnected2.length}`;
       let description_2x = 1;
       let clockO = 1;
          let untickm = String.fromCharCode(122,95,50,57,95,99,108,97,112,0);
          let promotionI = String.fromCharCode(112,101,114,99,101,112,116,117,97,108,95,115,95,55,51,0);
         clockO ^= 1;
         untickm = "3";
         promotionI += `${promotionI.length}`;
       let turnC: Array<any> = [320, 384];
         clockO &= turnC.length >> (Math.min(Math.abs(3), 3));
         description_2x &= description_2x * 1;
      while (!volumem.includes(clockO)) {
         volumem.push(1 * volumem.length);
         break;
      }
          let hejiN = 2;
          let activityw = String.fromCharCode(118,109,97,102,109,111,116,105,111,110,95,111,95,57,54,0);
         volumem.push(hejiN ^ 3);
         hejiN &= activityw.length;
         activityw = "2";
      let listd = String.fromCharCode(118,48,97,52,49,97,100,99,0) == disconnected2;
      do {
         disconnected2 += `${parseInt(`${applicationM}`)}`;
         if (listd) {
            break;
         }
      } while ((!disconnected2.endsWith(`${turnC.length}`)) && listd);
      greenw += "2";
   if (!handleru) {
      filled2 += parseFloat(`${crossU - taiwanb.size}`);
   }
      filli += `${2 >> (Math.min(1, darkS.length))}`;
   for (let y = 0; y < 3; y++) {
      handleru = filli.length >= 64;
   }
   for (let n = 0; n < 3; n++) {
      handleru = (watchG - filli.length) <= 3.33;
   }
   while ((with_3X.size - 1) >= 5) {
       let layoutS = 2.0;
       let whatsappi = false;
       let bingz = 2.0;
       let signinupO = 1.0;
      let album2 = 6557312.0 <= bingz;
      do {
         bingz *= (parseFloat(`${parseInt(`${layoutS}`) << (Math.min(4, Math.abs((whatsappi ? 5 : 1))))}`));
         if (album2) {
            break;
         }
      } while ((!whatsappi) && album2);
      for (let f = 0; f < 1; f++) {
          let stationsM: Array<any> = [248, 84, 35];
          let lessl = true;
          let modeh = false;
          let overv = 2.0;
         whatsappi = layoutS >= 79.89;
         stationsM = [parseInt(`${overv}`) << (Math.min(4, Math.abs(2)))];
         lessl = modeh || lessl;
         modeh = !modeh && 71.100 == overv;
      }
      while (whatsappi) {
          let backgroundj = 0;
          let ballJ = 4;
         bingz += parseFloat(`${1}`);
         backgroundj |= ballJ;
         ballJ *= 3;
         break;
      }
      while (whatsappi && 2.72 <= (layoutS - 2.59)) {
          let memberl = 3.0;
          let hooksL = false;
          let playW = String.fromCharCode(112,95,54,49,95,99,114,101,97,116,111,114,115,0);
          let about3: Map<any, any> = new Map([[String.fromCharCode(99,104,97,110,95,100,95,56,56,0),775], [String.fromCharCode(116,104,97,116,95,105,95,51,55,0),439]]);
          let footballi = String.fromCharCode(103,95,54,50,95,100,100,114,97,110,103,101,0);
         whatsappi = 57.45 == bingz;
         memberl *= parseFloat(`${1}`);
         hooksL = (parseFloat(`${playW.length}`) - memberl) < 99.40;
         playW += `${((hooksL ? 4 : 5) - parseInt(`${memberl}`))}`;
         about3 = new Map([[`${about3.size}`, about3.size << (Math.min(Math.abs(1), 3))]]);
         footballi = `${(footballi == String.fromCharCode(52,0) ? footballi.length : about3.size)}`;
         break;
      }
          let styleM = String.fromCharCode(118,101,110,117,101,95,104,95,56,49,0);
          let temp3: Array<any> = [389, 847, 788];
         bingz *= parseFloat(`${parseInt(`${bingz}`) ^ 2}`);
         styleM = `${styleM.length + temp3.length}`;
         temp3 = [styleM.length >> (Math.min(3, temp3.length))];
       let bufferS: Map<any, any> = new Map([[String.fromCharCode(112,97,114,115,101,95,122,95,53,0),987], [String.fromCharCode(107,95,56,54,95,97,115,115,101,115,115,109,101,110,116,0),967]]);
      while (4 >= (4 + bufferS.size) || 4 >= (parseInt(`${bingz}`) * bufferS.size)) {
         bingz += parseFloat(`${bufferS.size}`);
         break;
      }
      if (2 == (bufferS.size * 4) || 3 == (4 / (Math.max(5, bufferS.size)))) {
         layoutS += parseFloat(`${parseInt(`${layoutS}`) - bufferS.size}`);
      }
          let hejiw = String.fromCharCode(104,95,56,50,95,118,101,114,105,102,105,97,98,108,101,0);
         signinupO *= parseFloat(`${parseInt(`${bingz}`)}`);
         hejiw = `${1 / (Math.max(8, hejiw.length))}`;
       let dangerX = String.fromCharCode(114,95,52,48,95,114,101,112,108,105,101,115,0);
         whatsappi = 90.63 == layoutS;
         layoutS -= parseFloat(`${1}`);
      greenw = `${greenw.length}`;
      break;
   }
   let mbnativeadvancedH = 7566621 <= crossU;
   do {
       let tickedi = String.fromCharCode(98,101,108,108,95,120,95,49,53,0);
       let x_managerZ = false;
       let floating9 = false;
      if (x_managerZ) {
         x_managerZ = !x_managerZ;
      }
         x_managerZ = !x_managerZ;
         x_managerZ = (!floating9 ? !x_managerZ : floating9);
      let shrinka = x_managerZ ? !x_managerZ : x_managerZ;
      do {
          let signinupF = 1;
          let layout_ = 5.0;
          let const_5pp = 5.0;
         x_managerZ = signinupF == 22;
         signinupF %= Math.max(2, parseInt(`${const_5pp}`));
         layout_ *= parseFloat(`${parseInt(`${const_5pp}`)}`);
         if (shrinka) {
            break;
         }
      } while (shrinka && (tickedi.length > 3 || !x_managerZ));
         floating9 = x_managerZ || floating9;
         tickedi += "3";
      while (tickedi.length > 3) {
          let custom0: Array<any> = [486, 950, 318];
          let changeh = 0;
         tickedi += `${(3 | (x_managerZ ? 1 : 4))}`;
         custom0 = [3];
         changeh /= Math.max(2, 3);
         break;
      }
         x_managerZ = !tickedi.endsWith(`${x_managerZ}`);
         tickedi = `${tickedi.length}`;
      crossU /= Math.max(parseInt(`${filled2}`), 2);
      if (mbnativeadvancedH) {
         break;
      }
   } while (((vietnamX * crossU) >= 4) && mbnativeadvancedH);
   if (greenw.includes(`${halfB}`)) {
       let constants5: Array<any> = [388, 3, 461];
       let anytimeo = String.fromCharCode(115,97,108,116,115,95,107,95,49,49,0);
       let layoutf: Map<any, any> = new Map([[String.fromCharCode(109,117,108,116,120,109,117,108,116,95,109,95,51,57,0),125], [String.fromCharCode(115,97,108,115,97,95,98,95,50,50,0),399], [String.fromCharCode(114,101,112,114,101,115,101,110,116,97,116,105,111,110,115,95,50,95,56,53,0),584]]);
       let watcha = 1.0;
          let carouseli = String.fromCharCode(99,107,112,116,95,122,95,55,54,0);
         watcha += parseFloat(`${1 >> (Math.min(Math.abs(parseInt(`${watcha}`)), 5))}`);
         carouseli += "2";
         layoutf = new Map([[`${constants5.length}`, (String.fromCharCode(115,0) == anytimeo ? constants5.length : anytimeo.length)]]);
         constants5 = [constants5.length >> (Math.min(2, Math.abs(parseInt(`${watcha}`))))];
      let carouselg = 8891117 <= anytimeo.length;
      do {
          let philippinesN = String.fromCharCode(98,105,116,115,95,118,95,52,53,0);
          let inviteN = String.fromCharCode(99,95,53,48,95,107,101,121,115,0);
          let logouth = 4;
          let stationr = String.fromCharCode(102,95,54,51,95,97,108,97,119,0);
         anytimeo += `${logouth}`;
         philippinesN += `${philippinesN.length}`;
         inviteN = `${philippinesN.length * 3}`;
         logouth &= stationr.length >> (Math.min(5, philippinesN.length));
         stationr = `${(String.fromCharCode(99,0) == stationr ? inviteN.length : stationr.length)}`;
         if (carouselg) {
            break;
         }
      } while (carouselg && (!anytimeo.endsWith(`${watcha}`)));
      if ((anytimeo.length % 2) == 2) {
         anytimeo = "3";
      }
         constants5 = [2 * constants5.length];
         layoutf.set(anytimeo, parseInt(`${watcha}`) >> (Math.min(anytimeo.length, 5)));
      while (layoutf.get(`${constants5.length}`) == null) {
         layoutf.set(anytimeo, anytimeo.length);
         break;
      }
      while (2 > (layoutf.size & 5) || 4 > (5 & layoutf.size)) {
         anytimeo = `${constants5.length >> (Math.min(anytimeo.length, 4))}`;
         break;
      }
         watcha += (parseFloat(`${String.fromCharCode(77,0) == anytimeo ? anytimeo.length : parseInt(`${watcha}`)}`));
       let videojsU = String.fromCharCode(119,95,56,95,101,97,114,108,105,101,115,116,0);
       let fieldt = String.fromCharCode(122,95,57,53,95,112,97,115,115,119,111,114,100,0);
      while (!anytimeo.endsWith(fieldt)) {
         anytimeo += `${layoutf.size ^ 1}`;
         break;
      }
      halfB += parseFloat(`${parseInt(`${watchG}`) % 3}`);
   }
   while (filli == darkS) {
       let mimoT = String.fromCharCode(98,95,57,49,95,98,97,103,101,0);
       let active2 = 0;
       let targete = String.fromCharCode(119,104,105,99,104,95,49,95,57,51,0);
       let halfk = 2.0;
         targete += `${targete.length / (Math.max(3, 5))}`;
         targete = `${parseInt(`${halfk}`)}`;
         targete = `${(targete == String.fromCharCode(112,0) ? active2 : targete.length)}`;
          let combinedu: Array<any> = [String.fromCharCode(115,105,109,112,108,105,102,121,95,99,95,53,52,0), String.fromCharCode(116,111,98,105,116,95,52,95,49,54,0)];
         mimoT = "1";
         combinedu.push(combinedu.length << (Math.min(Math.abs(2), 3)));
       let selectionL = false;
       let b_positionM = true;
      while (selectionL || b_positionM) {
          let ranku = 4.0;
         b_positionM = 84.32 > ranku;
         break;
      }
         mimoT += "3";
      while (3.95 <= (5.36 - halfk) || halfk <= 5.36) {
         halfk += ((b_positionM ? 3 : 4));
         break;
      }
      for (let g = 0; g < 2; g++) {
         targete += `${parseInt(`${halfk}`)}`;
      }
       let button6 = true;
       let specD = true;
          let floatingD: Array<any> = [780, 213];
          let middleW = true;
          let clock0 = String.fromCharCode(108,111,117,100,110,111,114,109,95,110,95,51,52,0);
         specD = mimoT.length <= 76 || !selectionL;
         floatingD = [1];
         middleW = !middleW;
         clock0 = `${((middleW ? 3 : 3) * clock0.length)}`;
         b_positionM = specD;
      darkS += `${targete.length}`;
      break;
   }
   while (parseFloat(`${filli.length}`) >= halfB) {
       let calendarn: Map<any, any> = new Map([[String.fromCharCode(99,104,97,110,103,101,100,104,95,57,95,50,53,0),192], [String.fromCharCode(97,112,116,120,95,122,95,57,56,0),656], [String.fromCharCode(116,104,114,101,97,100,110,97,109,101,95,103,95,56,56,0),147]]);
       let robotoN = String.fromCharCode(111,112,116,105,109,105,115,116,105,99,95,105,95,51,50,0);
       let temperatureP = true;
         calendarn = new Map([[`${calendarn.size}`, (String.fromCharCode(84,0) == robotoN ? robotoN.length : calendarn.size)]]);
      for (let d = 0; d < 2; d++) {
          let darko = 5.0;
         robotoN = `${parseInt(`${darko}`) ^ 2}`;
      }
         temperatureP = (calendarn.size & robotoN.length) > 54;
      let textp = String.fromCharCode(98,109,118,121,110,48,111,102,56,51,0) == robotoN;
      do {
          let subsC: Map<any, any> = new Map([[String.fromCharCode(105,95,49,49,95,109,111,99,107,115,0),String.fromCharCode(105,95,50,56,95,115,112,97,99,101,0)], [String.fromCharCode(115,104,111,119,95,104,95,52,49,0),String.fromCharCode(111,95,55,55,95,115,121,110,99,104,114,111,110,105,122,101,0)], [String.fromCharCode(99,111,110,102,105,114,109,95,101,95,49,52,0),String.fromCharCode(114,116,112,114,101,99,101,105,118,101,114,95,52,95,50,57,0)]]);
          let componentW = String.fromCharCode(115,95,56,53,95,101,120,112,101,99,116,0);
          let darkQ = 5;
          let infos = 1.0;
         robotoN = `${componentW.length}`;
         subsC = new Map([[`${subsC.size}`, 3]]);
         componentW += `${darkQ | 2}`;
         darkQ /= Math.max(darkQ, 2);
         infos /= Math.max(4, parseFloat(`${subsC.size << (Math.min(Math.abs(1), 1))}`));
         if (textp) {
            break;
         }
      } while ((robotoN.startsWith(`${temperatureP}`)) && textp);
         robotoN = `${3 + calendarn.size}`;
      for (let x = 0; x < 1; x++) {
         temperatureP = robotoN.length > 59;
      }
         robotoN += `${calendarn.size << (Math.min(robotoN.length, 3))}`;
       let subsa = false;
         temperatureP = !subsa;
      halfB *= parseFloat(`${greenw.length}`);
      break;
   }
       let umengU = String.fromCharCode(100,101,99,111,100,101,109,118,95,121,95,55,56,0);
      let modityU = umengU == String.fromCharCode(48,114,117,48,112,56,100,98,115,0);
      do {
          let yingl = String.fromCharCode(109,95,49,52,95,97,114,111,117,110,100,0);
         umengU = `${yingl.length}`;
         if (modityU) {
            break;
         }
      } while (modityU && (umengU == umengU));
      while (umengU.length < umengU.length) {
          let reneww = 5.0;
          let phoneS = 1;
          let eactF: Array<any> = [225, 345];
         umengU = `${parseInt(`${reneww}`)}`;
         reneww -= phoneS;
         phoneS |= 1 * eactF.length;
         eactF = [eactF.length ^ phoneS];
         break;
      }
          let holder7 = String.fromCharCode(113,95,50,55,95,115,116,114,105,107,101,0);
          let heji2: Map<any, any> = new Map([[String.fromCharCode(102,95,51,95,97,99,99,101,110,116,0),String.fromCharCode(109,95,51,53,95,108,119,115,115,112,110,0)], [String.fromCharCode(105,95,55,49,95,112,97,110,105,99,0),String.fromCharCode(119,119,119,95,51,95,52,56,0)]]);
          let scheduleE: Map<any, any> = new Map([[String.fromCharCode(101,115,116,105,109,97,116,101,100,95,100,95,53,50,0),389], [String.fromCharCode(98,95,51,48,95,115,116,117,102,102,115,116,0),303], [String.fromCharCode(100,95,49,48,95,115,117,109,100,105,102,102,0),504]]);
         umengU = `${holder7.length}`;
         holder7 = `${scheduleE.size}`;
         heji2.set(`${scheduleE.size}`, 3 >> (Math.min(1, Math.abs(scheduleE.size))));
      handleru = 66 < vietnamX;
       let combine7 = String.fromCharCode(105,110,102,111,95,106,95,55,55,0);
       let downloadeds = 0;
       let nativeL = String.fromCharCode(109,95,54,49,95,109,117,108,116,105,101,110,100,0);
      for (let m = 0; m < 2; m++) {
          let inactive4 = String.fromCharCode(108,95,51,56,95,115,105,108,101,110,116,108,121,0);
         downloadeds *= (inactive4 == String.fromCharCode(88,0) ? downloadeds : inactive4.length);
      }
         downloadeds *= (nativeL == String.fromCharCode(70,0) ? nativeL.length : combine7.length);
          let k_positionD = 2.0;
          let stringsK = 1;
         nativeL = `${(combine7 == String.fromCharCode(99,0) ? downloadeds : combine7.length)}`;
         k_positionD /= Math.max(stringsK / 2, 1);
         stringsK -= parseInt(`${k_positionD}`) ^ 2;
         combine7 += `${downloadeds * 1}`;
         downloadeds -= nativeL.length >> (Math.min(Math.abs(1), 1));
      if ((3 / (Math.max(3, downloadeds))) < 4 && (downloadeds / (Math.max(combine7.length, 9))) < 3) {
         combine7 = `${downloadeds}`;
      }
         downloadeds &= combine7.length - downloadeds;
       let humidityq = String.fromCharCode(111,95,52,50,95,98,105,100,105,114,0);
       let searchbarJ = String.fromCharCode(115,95,50,54,95,105,115,102,105,110,105,116,101,0);
         combine7 += `${humidityq.length}`;
      greenw += `${greenw.length | nativeL.length}`;
   for (let x = 0; x < 2; x++) {
      halfB += parseFloat(`${2 << (Math.min(4, Math.abs(crossU)))}`);
   }
      halfB /= Math.max(parseFloat(`${greenw.length / 3}`), 3);
   for (let v = 0; v < 1; v++) {
       let philippinesM = String.fromCharCode(102,95,54,50,95,115,99,104,117,110,99,107,0);
       let androidb = 3.0;
         philippinesM = `${2 * parseInt(`${androidb}`)}`;
      let shrinkC = 6060472.0 >= androidb;
      do {
         androidb *= parseFloat(`${philippinesM.length << (Math.min(Math.abs(3), 5))}`);
         if (shrinkC) {
            break;
         }
      } while (shrinkC && ((5 & philippinesM.length) <= 5));
      while (3.8 <= (androidb - parseFloat(`${philippinesM.length}`)) && (2 << (Math.min(1, philippinesM.length))) <= 2) {
         philippinesM = `${philippinesM.length}`;
         break;
      }
       let tickX = String.fromCharCode(97,95,57,52,95,109,98,99,109,112,0);
         androidb *= parseFloat(`${philippinesM.length + 2}`);
         philippinesM = `${parseInt(`${androidb}`) + 3}`;
      confirmationG = [parseInt(`${halfB}`)];
   }
       let stationJ: Array<any> = [488, 677];
         stationJ = [stationJ.length];
         stationJ = [stationJ.length % 2];
          let confirmationH: Map<any, any> = new Map([[String.fromCharCode(106,95,51,48,95,97,98,115,101,105,108,0),441], [String.fromCharCode(102,97,115,116,95,55,95,51,55,0),289]]);
          let privacy3 = 2;
          let coreT = String.fromCharCode(97,95,51,54,95,116,121,112,0);
         stationJ.push(stationJ.length);
         confirmationH = new Map([[`${privacy3}`, 2 >> (Math.min(5, Math.abs(privacy3)))]]);
         coreT += `${confirmationH.size * 1}`;
      filli = `${parseInt(`${filled2}`) / 2}`;
       let tumbnailt = 1.0;
         tumbnailt /= Math.max(parseFloat(`${parseInt(`${tumbnailt}`)}`), 2);
      for (let v = 0; v < 3; v++) {
          let sheetF: Array<any> = [String.fromCharCode(101,95,53,54,95,116,114,97,110,115,108,97,116,105,111,110,115,0), String.fromCharCode(108,105,110,101,98,114,101,97,107,95,54,95,52,56,0), String.fromCharCode(106,95,53,57,95,119,101,108,108,98,101,104,97,118,101,100,0)];
          let internetE = String.fromCharCode(115,101,108,101,99,116,95,52,95,56,52,0);
          let incidentM: Array<any> = [682, 249];
         tumbnailt -= parseFloat(`${1}`);
         sheetF = [sheetF.length];
         internetE = `${(String.fromCharCode(112,0) == internetE ? internetE.length : sheetF.length)}`;
         incidentM.push(internetE.length);
      }
         tumbnailt += parseFloat(`${parseInt(`${tumbnailt}`)}`);
      filli = `${crossU << (Math.min(Math.abs(3), 4))}`;
       let macauF = String.fromCharCode(104,95,49,52,95,115,99,97,108,97,98,108,101,0);
          let handlerN = String.fromCharCode(114,101,97,100,102,117,108,108,95,103,95,54,55,0);
          let layoutj = String.fromCharCode(101,95,52,48,95,98,105,116,109,97,112,0);
          let alertu = String.fromCharCode(107,95,54,54,95,107,109,101,97,110,115,0);
         macauF = `${alertu.length & 3}`;
         handlerN += `${handlerN.length}`;
         layoutj += "1";
         alertu = `${layoutj.length}`;
         macauF += `${2 << (Math.min(5, macauF.length))}`;
      let loadingT = 7666106 >= macauF.length;
      do {
         macauF += "1";
         if (loadingT) {
            break;
         }
      } while (loadingT && (macauF.length == 4));
      filled2 -= parseFloat(`${with_3X.size | parseInt(`${watchG}`)}`);
   for (let e = 0; e < 2; e++) {
      filled2 /= Math.max(3, parseFloat(`${with_3X.size}`));
   }
       let productU = 5;
       let frame_jzz = String.fromCharCode(119,95,56,56,95,114,97,110,107,105,110,103,0);
      for (let t = 0; t < 1; t++) {
         frame_jzz += `${frame_jzz.length % 3}`;
      }
      if (!frame_jzz.startsWith(`${productU}`)) {
         productU += productU;
      }
      while (!frame_jzz.endsWith(`${productU}`)) {
          let materialP = 5.0;
         productU <<= Math.min(Math.abs(productU), 2);
         materialP -= parseFloat(`${parseInt(`${materialP}`) / (Math.max(parseInt(`${materialP}`), 6))}`);
         break;
      }
       let gemfileg = String.fromCharCode(119,95,57,48,95,115,117,98,118,105,101,119,101,114,0);
       let recommendationB = String.fromCharCode(102,111,110,116,95,122,95,53,54,0);
         recommendationB = `${(frame_jzz == String.fromCharCode(88,0) ? gemfileg.length : frame_jzz.length)}`;
      if (!frame_jzz.includes(gemfileg)) {
         frame_jzz += `${recommendationB.length + 3}`;
      }
      with_3X = new Map([[greenw, 1]]);
       let albumH = 5.0;
       let sideC: Array<any> = [421, 74, 122];
      for (let t = 0; t < 3; t++) {
          let volumen = String.fromCharCode(102,114,97,109,101,102,111,114,109,97,116,95,113,95,56,0);
          let refreshB = 4;
          let appsK = 5.0;
          let filterw = 2;
          let shoot4 = 3;
         albumH *= filterw % 2;
         volumen += `${refreshB}`;
         refreshB ^= 3 ^ parseInt(`${appsK}`);
         appsK /= Math.max(1, parseFloat(`${refreshB % (Math.max(shoot4, 6))}`));
         filterw ^= parseInt(`${appsK}`) % (Math.max(volumen.length, 4));
         shoot4 *= parseInt(`${appsK}`);
      }
          let umengZ = String.fromCharCode(101,120,115,121,95,117,95,55,51,0);
         albumH *= sideC.length;
         umengZ = "2";
         albumH += 1;
       let currentc = 1.0;
       let securityo = 2.0;
      let firebaseZ = currentc >= 9564760.0;
      do {
         currentc /= Math.max(1, 1 - parseInt(`${albumH}`));
         if (firebaseZ) {
            break;
         }
      } while (firebaseZ && ((securityo * 4) > 2.8));
      let filledA = 9805813.0 <= securityo;
      do {
         securityo += parseInt(`${currentc}`) / (Math.max(sideC.length, 8));
         if (filledA) {
            break;
         }
      } while (filledA && ((albumH - securityo) <= 2.68 && (securityo - 2.68) <= 4.68));
      crossU &= with_3X.size & parseInt(`${albumH}`);
   for (let z = 0; z < 1; z++) {
       let loadingd = 3.0;
       let expandT: Array<any> = [276, 146];
       let showj = 2.0;
       let uploadk = String.fromCharCode(105,100,101,109,112,111,116,101,110,99,121,95,99,95,52,50,0);
      if (2 <= (expandT.length % 2) && 1 <= (expandT.length << (Math.min(Math.abs(2), 5)))) {
         loadingd -= expandT.length;
      }
      while (1 <= (uploadk.length + 4) && 4 <= (parseInt(`${showj}`) + uploadk.length)) {
         showj /= Math.max(1, expandT.length);
         break;
      }
      if (2 == (uploadk.length * 2) && 5 == (uploadk.length * 2)) {
          let schedule7 = 4.0;
          let privacy0: Array<any> = [704, 440, 490];
          let volumeu = String.fromCharCode(97,112,112,114,101,99,97,116,105,111,110,104,111,117,114,95,49,95,55,0);
          let networkq = String.fromCharCode(100,101,99,111,114,97,116,101,95,121,95,54,55,0);
          let update_mo = String.fromCharCode(97,95,57,49,95,103,114,97,116,105,99,117,108,101,0);
         uploadk = `${parseInt(`${schedule7}`)}`;
         schedule7 *= parseFloat(`${networkq.length}`);
         privacy0.push((String.fromCharCode(110,0) == volumeu ? privacy0.length : volumeu.length));
         networkq = `${volumeu.length}`;
         update_mo = `${update_mo.length}`;
      }
       let rewardvideoP = String.fromCharCode(104,112,105,99,95,106,95,51,57,0);
         expandT = [2 + parseInt(`${loadingd}`)];
      let reducerg = 6545304 <= expandT.length;
      do {
         expandT = [expandT.length << (Math.min(5, Math.abs(parseInt(`${showj}`))))];
         if (reducerg) {
            break;
         }
      } while (reducerg && (rewardvideoP.startsWith(`${expandT.length}`)));
       let auto_xol = String.fromCharCode(114,95,52,52,95,119,114,97,112,112,101,114,0);
       let confirmationt = String.fromCharCode(107,95,57,48,95,103,101,116,99,117,116,0);
      for (let k = 0; k < 2; k++) {
         showj += 2;
      }
          let invitei = 4.0;
          let closed = 1;
          let gradlewu = true;
         confirmationt = `${1 ^ parseInt(`${invitei}`)}`;
         invitei *= (closed >> (Math.min(2, Math.abs((gradlewu ? 3 : 3)))));
         closed >>= Math.min(2, Math.abs(closed));
         gradlewu = 20 > closed && gradlewu;
          let soundv = true;
         auto_xol = `${confirmationt.length}`;
         soundv = (soundv ? soundv : !soundv);
       let nextu = false;
      let black6 = auto_xol.length >= 7729512;
      do {
         auto_xol += `${2 * parseInt(`${showj}`)}`;
         if (black6) {
            break;
         }
      } while ((5 == auto_xol.length) && black6);
      watchG += with_3X.size + 1;
   }
   for (let a = 0; a < 2; a++) {
      crossU += ((handleru ? 3 : 3) / 2);
   }
      greenw += `${1 ^ greenw.length}`;
   while ((with_3X.size ^ 2) <= 5) {
      handleru = crossU == parseInt(`${watchG}`);
      break;
   }
      vietnamX ^= vietnamX * darkS.length;

        this.#navigator?.removeListener('state', this.#matchHistory);
    }

    static updateNavigator = (navigation: NavigationProp<any>) => {
        this.#navigator = navigation;
    }

    static updateMiddlewares = (middlewares: { [key: string]: [] | [(page: string) => string | undefined] }) => {
        this.#middlewares = middlewares;
    }

    static toName = (page: string, { params }: { params?: any } = {}) => {
        if (page in this.#middlewares) {
            const pageMiddleware = this.#middlewares[page];

            for (const middleware of pageMiddleware) {
                const result = middleware(page);

                if (result === undefined) return;
                else page = result;
            }
        }

        const then = (callback?: (params: any) => void) => {
            this.pendingEvent.push({
                pageName: page,
                thenEvent: callback,
            });
        }

        this.#lastActoinPageName = page;
        this.#navigator?.navigate(page, params);

        return { then };
    }

    static offName = (page: string, { params }: { params?: any } = {}) => {
        if (page in this.#middlewares) {
            const pageMiddleware = this.#middlewares[page];

            for (const middleware of pageMiddleware) {
                const result = middleware(page);

                if (result === undefined) return;
                else page = result;
            }
        }

        const then = (callback?: (params: any) => void) => {
            this.pendingEvent.push({
                pageName: page,
                thenEvent: callback,
            });
        }

        this.#lastActoinPageName = page;
        this.#navigator?.dispatch(
            StackActions.replace(page, params),
        );

        return { then };
    }

    static canBack = () => {
       let mbbid7 = 1.0;
    let appsZ: Array<any> = [String.fromCharCode(107,95,52,54,95,109,111,110,111,0), String.fromCharCode(100,105,118,109,111,100,95,115,95,57,51,0)];
    let eighteena: Array<any> = [745, 681];
    let orange2 = 5.0;
    let less0 = false;
    let blacklisth = String.fromCharCode(119,101,98,114,116,99,95,109,95,54,53,0);
    let package_cq = String.fromCharCode(109,117,116,101,95,118,95,56,53,0);
    let tickX = 1.0;
    let options6 = true;
    let spinner3 = String.fromCharCode(99,111,112,121,104,95,55,95,52,50,0);
    let utils0 = 1.0;
    let play2: Map<any, any> = new Map([[String.fromCharCode(114,101,103,117,108,97,114,95,117,95,54,0),false ], [String.fromCharCode(101,111,109,101,116,114,121,95,57,95,51,50,0),false ]]);
    let indexy = 3.0;
    let fieldw: Array<any> = [642, 40];
    let configureO = 2;
    let combinej = 2.0;
    let skip6 = 0.0;
   if (2 == (package_cq.length << (Math.min(Math.abs(2), 4)))) {
      orange2 /= Math.max(2, 2 + parseInt(`${mbbid7}`));
   }
      orange2 += 2 / (Math.max(1, parseInt(`${mbbid7}`)));
   let nativeexl = package_cq.length >= 8639999;
   do {
       let vietnamM = String.fromCharCode(97,99,99,101,110,116,95,102,95,56,48,0);
       let arrow9: Map<any, any> = new Map([[String.fromCharCode(108,95,55,56,95,106,112,101,103,116,97,98,108,101,115,0),856], [String.fromCharCode(101,95,56,95,107,110,111,119,110,0),262]]);
       let networkg = String.fromCharCode(103,95,53,95,118,97,108,0);
       let mbnativeadvancedD = String.fromCharCode(110,95,53,52,95,105,109,112,111,114,116,97,98,108,101,100,0);
       let buttons = String.fromCharCode(120,95,54,56,95,97,116,116,0);
      while (!buttons.includes(`${mbnativeadvancedD.length}`)) {
         mbnativeadvancedD = "2";
         break;
      }
       let downloadn = true;
      let middlewarel = 6515684 <= mbnativeadvancedD.length;
      do {
         mbnativeadvancedD += `${networkg.length}`;
         if (middlewarel) {
            break;
         }
      } while ((buttons != String.fromCharCode(48,0) && 2 > mbnativeadvancedD.length) && middlewarel);
      if (buttons.endsWith(networkg)) {
         networkg = "1";
      }
          let mintegral8: Map<any, any> = new Map([[String.fromCharCode(111,100,100,95,110,95,56,54,0),993], [String.fromCharCode(114,101,109,105,110,100,95,48,95,50,50,0),312]]);
         mbnativeadvancedD += `${mbnativeadvancedD.length ^ 3}`;
         mintegral8.set(`${mintegral8.size}`, mintegral8.size % 3);
          let thailandd = String.fromCharCode(116,111,108,101,114,97,110,99,101,95,100,95,57,49,0);
          let reminderi = 2.0;
          let anythinkP: Map<any, any> = new Map([[String.fromCharCode(111,95,49,56,95,101,110,106,105,110,0),String.fromCharCode(111,95,56,95,113,100,109,100,97,116,97,0)], [String.fromCharCode(99,116,120,105,100,120,105,110,99,95,53,95,55,57,0),String.fromCharCode(111,116,104,101,114,110,97,109,101,95,115,95,53,56,0)], [String.fromCharCode(105,100,120,95,115,95,52,53,0),String.fromCharCode(100,101,112,108,111,121,109,101,110,116,95,106,95,49,55,0)]]);
         networkg = `${2 - mbnativeadvancedD.length}`;
         thailandd += `${thailandd.length << (Math.min(Math.abs(1), 4))}`;
         reminderi -= parseFloat(`${3}`);
         anythinkP = new Map([[thailandd, parseInt(`${reminderi}`)]]);
         arrow9.set(networkg, (networkg == String.fromCharCode(48,0) ? networkg.length : arrow9.size));
         downloadn = 67 <= networkg.length && 67 <= mbnativeadvancedD.length;
      let uploadR = String.fromCharCode(107,111,51,119,52,116,50,54,56,97,0) == networkg;
      do {
         networkg += `${arrow9.size}`;
         if (uploadR) {
            break;
         }
      } while (uploadR && (1 < buttons.length));
         mbnativeadvancedD = `${vietnamM.length % 2}`;
         buttons = `${networkg.length & mbnativeadvancedD.length}`;
          let borderlessj = false;
          let zoomE = 2.0;
          let placeholder2 = String.fromCharCode(114,97,110,100,95,120,95,52,51,0);
         arrow9 = new Map([[`${zoomE}`, 3 % (Math.max(8, parseInt(`${zoomE}`)))]]);
         borderlessj = !borderlessj && placeholder2.length == 77;
         placeholder2 = `${2 << (Math.min(3, placeholder2.length))}`;
          let anythinkM = false;
         downloadn = (buttons.length ^ vietnamM.length) <= 1;
         anythinkM = (anythinkM ? anythinkM : !anythinkM);
       let xvodd = String.fromCharCode(112,97,117,115,101,95,54,95,51,55,0);
         buttons = `${((downloadn ? 4 : 1) - 1)}`;
      package_cq = `${2 << (Math.min(3, Math.abs(play2.size)))}`;
      if (nativeexl) {
         break;
      }
   } while (nativeexl && ((package_cq.length + 1) > 5 || (play2.size + 1) > 5));
      less0 = appsZ.length == 75;
   if (spinner3.startsWith(`${utils0}`)) {
      spinner3 += `${3 >> (Math.min(3, blacklisth.length))}`;
   }
   if (options6) {
      options6 = orange2 == indexy;
   }
   let termsi = 4928605.0 <= utils0;
   do {
      utils0 /= Math.max(parseFloat(`${2}`), 3);
      if (termsi) {
         break;
      }
   } while ((1.49 == (mbbid7 / 1.32) && 1.32 == (mbbid7 / (Math.max(utils0, 2)))) && termsi);
   if (4.49 > tickX) {
       let toponz = false;
       let hooksM = String.fromCharCode(110,95,51,55,95,115,101,113,117,101,110,99,101,115,0);
          let lightf: Array<any> = [805, 929];
          let reminderM: Map<any, any> = new Map([[String.fromCharCode(117,95,57,55,95,111,112,101,110,0),String.fromCharCode(99,111,110,116,97,99,116,95,118,95,56,57,0)], [String.fromCharCode(97,102,105,108,116,101,114,95,98,95,52,57,0),String.fromCharCode(112,114,101,99,97,108,99,95,53,95,53,53,0)], [String.fromCharCode(109,95,55,49,95,109,105,110,111,114,0),String.fromCharCode(103,95,53,48,95,112,114,111,112,97,103,97,116,101,0)]]);
          let textz = true;
         hooksM = `${hooksM.length}`;
         lightf = [lightf.length + reminderM.size];
         reminderM = new Map([[`${reminderM.size}`, ((textz ? 5 : 2) << (Math.min(Math.abs(reminderM.size), 1)))]]);
         textz = !textz;
      let greenz = toponz ? !toponz : toponz;
      do {
          let auto_vO = String.fromCharCode(118,115,99,97,108,101,95,112,95,57,53,0);
          let appleH = 0.0;
         toponz = 63.99 > appleH || toponz;
         auto_vO += `${3 & auto_vO.length}`;
         appleH /= Math.max(auto_vO.length & 3, 1);
         if (greenz) {
            break;
         }
      } while ((!toponz || hooksM.length > 5) && greenz);
       let turn1 = 5.0;
          let foregroundx: Array<any> = [138, 542, 666];
          let short_o2: Array<any> = [979, 800, 503];
         toponz = foregroundx.length > 78 || turn1 > 70.65;
         foregroundx.push(short_o2.length >> (Math.min(Math.abs(3), 5)));
         short_o2.push(short_o2.length >> (Math.min(Math.abs(3), 5)));
         hooksM += `${3 / (Math.max(10, parseInt(`${turn1}`)))}`;
          let whatsappq: Map<any, any> = new Map([[String.fromCharCode(106,95,51,56,95,100,101,116,101,114,109,105,110,101,100,0),698], [String.fromCharCode(110,97,110,111,115,101,99,111,110,100,115,95,100,95,50,49,0),952], [String.fromCharCode(101,120,116,101,110,115,105,111,110,115,95,109,95,51,54,0),54]]);
          let stationsn = String.fromCharCode(103,95,57,49,95,108,109,100,98,0);
         turn1 *= parseFloat(`${stationsn.length % (Math.max(2, 9))}`);
         whatsappq.set(`${whatsappq.size}`, whatsappq.size ^ 3);
         stationsn += `${whatsappq.size ^ 3}`;
      tickX *= ((options6 ? 2 : 3) * parseInt(`${orange2}`));
   }
      utils0 /= Math.max(3, parseFloat(`${3 ^ blacklisth.length}`));
   if ((fieldw.length % 2) > 3) {
       let control1 = 2.0;
      for (let l = 0; l < 1; l++) {
         control1 /= Math.max(5, 1);
      }
         control1 /= Math.max(5, parseInt(`${control1}`) << (Math.min(5, Math.abs(2))));
      let refreshZ = 8899632.0 >= control1;
      do {
         control1 += 2 ^ parseInt(`${control1}`);
         if (refreshZ) {
            break;
         }
      } while (refreshZ && ((control1 + 2.41) == 1.2));
      fieldw = [spinner3.length << (Math.min(3, fieldw.length))];
   }
   let x_lockG = String.fromCharCode(112,49,117,118,56,49,119,0) == spinner3;
   do {
       let otheru = String.fromCharCode(103,95,53,51,95,116,114,97,100,101,0);
       let clockq: Map<any, any> = new Map([[String.fromCharCode(111,103,103,101,114,95,101,95,51,52,0),String.fromCharCode(101,95,49,52,95,103,101,116,115,111,99,107,97,100,100,114,0)], [String.fromCharCode(108,95,48,95,115,99,114,101,101,110,115,0),String.fromCharCode(114,101,109,97,116,114,105,120,105,110,103,95,116,95,51,49,0)], [String.fromCharCode(119,95,55,95,98,109,111,100,101,0),String.fromCharCode(111,98,106,99,95,122,95,57,51,0)]]);
      for (let q = 0; q < 2; q++) {
         otheru = `${clockq.size}`;
      }
       let carouselR = String.fromCharCode(110,95,50,95,114,111,111,116,115,0);
       let m_imageK = String.fromCharCode(116,97,115,107,95,55,95,50,53,0);
      while (1 >= (clockq.size ^ otheru.length)) {
          let clockN = 0.0;
         clockq.set(`${clockN}`, m_imageK.length / 2);
         break;
      }
          let livey = String.fromCharCode(118,95,51,50,95,99,114,101,100,105,98,105,108,105,116,121,0);
          let injuryH = 5.0;
          let photoi = String.fromCharCode(100,99,97,109,97,116,104,95,50,95,50,49,0);
         carouselR = `${parseInt(`${injuryH}`)}`;
         livey += `${photoi.length}`;
         injuryH /= Math.max(photoi.length >> (Math.min(Math.abs(2), 1)), 4);
          let stringsY = false;
          let yingT = String.fromCharCode(99,104,97,114,97,99,116,101,114,95,119,95,57,51,0);
         m_imageK += "2";
         stringsY = yingT.endsWith(`${stringsY}`);
         yingT += `${yingT.length}`;
      for (let k = 0; k < 1; k++) {
         clockq = new Map([[`${clockq.size}`, 2 | clockq.size]]);
      }
      spinner3 += "1";
      if (x_lockG) {
         break;
      }
   } while (x_lockG && (orange2 == 2.22));
   for (let v = 0; v < 1; v++) {
      indexy -= 3;
   }
      indexy -= eighteena.length;
       let splash_ = false;
       let fileS = String.fromCharCode(110,95,57,56,95,114,101,115,101,116,0);
       let appleG = String.fromCharCode(112,114,101,98,117,102,95,120,95,50,51,0);
      let heartd = fileS.length >= 4991215;
      do {
         fileS += `${((splash_ ? 5 : 1) % (Math.max(appleG.length, 9)))}`;
         if (heartd) {
            break;
         }
      } while (heartd && (fileS.startsWith(`${splash_}`)));
      for (let t = 0; t < 1; t++) {
         fileS += `${(appleG == String.fromCharCode(76,0) ? appleG.length : fileS.length)}`;
      }
          let b_managerA = 2.0;
         fileS += `${(String.fromCharCode(70,0) == appleG ? parseInt(`${b_managerA}`) : appleG.length)}`;
      for (let r = 0; r < 1; r++) {
         splash_ = appleG.length == 28;
      }
         fileS = `${(appleG.length ^ (splash_ ? 5 : 5))}`;
          let description_liJ = 1;
          let calendari: Map<any, any> = new Map([[String.fromCharCode(103,95,52,55,95,99,104,105,108,100,0),16], [String.fromCharCode(115,101,108,101,99,116,95,121,95,55,0),648], [String.fromCharCode(105,110,115,101,114,116,95,48,95,50,54,0),464]]);
         appleG += `${description_liJ}`;
         description_liJ <<= Math.min(1, Math.abs(calendari.size));
         calendari = new Map([[`${calendari.size}`, calendari.size << (Math.min(Math.abs(1), 2))]]);
         splash_ = appleG.startsWith(`${splash_}`);
      for (let w = 0; w < 3; w++) {
         fileS += `${(String.fromCharCode(112,0) == fileS ? fileS.length : appleG.length)}`;
      }
      let xnewsn = 6295613 >= fileS.length;
      do {
         fileS = `${3 << (Math.min(4, appleG.length))}`;
         if (xnewsn) {
            break;
         }
      } while (xnewsn && (appleG != String.fromCharCode(103,0)));
      eighteena.push(3 >> (Math.min(3, blacklisth.length)));
       let forme: Array<any> = [478, 884, 247];
       let entry6 = true;
       let dragK = 3;
         entry6 = forme.includes(dragK);
          let trophyZ = String.fromCharCode(112,95,56,51,95,115,108,117,114,112,0);
          let closeK = 2.0;
          let moviesb: Array<any> = [769, 342, 99];
         forme.push(((entry6 ? 3 : 2) >> (Math.min(Math.abs(parseInt(`${closeK}`)), 1))));
         trophyZ = `${moviesb.length << (Math.min(trophyZ.length, 4))}`;
         closeK -= parseFloat(`${trophyZ.length}`);
         moviesb.push(trophyZ.length % (Math.max(2, 10)));
         entry6 = (dragK * forme.length) <= 4;
      while (3 <= (5 * dragK) && !entry6) {
         dragK -= dragK ^ 2;
         break;
      }
      for (let x = 0; x < 3; x++) {
         dragK %= Math.max(2, 3 << (Math.min(1, forme.length)));
      }
      if (dragK <= 1 || (dragK + 1) <= 4) {
         entry6 = entry6 && 62 < dragK;
      }
       let switch_u8e = 3.0;
       let optionsc = 5.0;
         switch_u8e += parseFloat(`${forme.length}`);
      let championm = 9625988 <= forme.length;
      do {
         forme = [parseInt(`${optionsc}`) + 1];
         if (championm) {
            break;
         }
      } while (championm && (2 > (dragK + forme.length)));
      eighteena = [3 / (Math.max(parseInt(`${indexy}`), 4))];
       let gemfileJ = 5.0;
       let predictionB = String.fromCharCode(105,110,97,99,116,105,118,101,95,114,95,54,50,0);
       let indicatorO = 3.0;
      let gradleK = predictionB.length >= 6321828;
      do {
         predictionB = `${2 + parseInt(`${indicatorO}`)}`;
         if (gradleK) {
            break;
         }
      } while (gradleK && (5 == (parseInt(`${gemfileJ}`) / (Math.max(6, predictionB.length))) || 1.93 == (gemfileJ / (Math.max(parseFloat(`${predictionB.length}`), 10)))));
      let strings0 = gemfileJ <= 9213541.0;
      do {
          let goala = true;
          let containerS = 4;
          let styleC = String.fromCharCode(121,95,49,95,121,121,121,121,0);
         gemfileJ -= parseFloat(`${predictionB.length}`);
         goala = styleC.length <= containerS;
         containerS /= Math.max(containerS * 2, 1);
         styleC += `${containerS}`;
         if (strings0) {
            break;
         }
      } while (strings0 && (!predictionB.endsWith(`${gemfileJ}`)));
      if (3.12 < (gemfileJ - indicatorO) && 3.12 < (indicatorO - gemfileJ)) {
         indicatorO += predictionB.length;
      }
         indicatorO /= Math.max(2, 5);
      if (predictionB.includes(`${gemfileJ}`)) {
         predictionB = `${(String.fromCharCode(51,0) == predictionB ? parseInt(`${indicatorO}`) : predictionB.length)}`;
      }
      let bingW = indicatorO >= 5145879.0;
      do {
         indicatorO *= parseInt(`${gemfileJ}`);
         if (bingW) {
            break;
         }
      } while (bingW && (5.80 == (gemfileJ / (Math.max(3.77, 7))) && (3.77 - indicatorO) == 1.24));
      let downloaderl = indicatorO <= 8328495.0;
      do {
         indicatorO /= Math.max(parseInt(`${gemfileJ}`), 3);
         if (downloaderl) {
            break;
         }
      } while ((indicatorO < gemfileJ) && downloaderl);
      while (gemfileJ < indicatorO) {
         gemfileJ /= Math.max(parseFloat(`${predictionB.length}`), 4);
         break;
      }
      if (3 == predictionB.length) {
         predictionB = "1";
      }
      package_cq = `${((options6 ? 4 : 2))}`;
   if ((3 * package_cq.length) < 5) {
      fieldw.push(parseInt(`${orange2}`));
   }
   if ((2 - configureO) == 2 || (configureO - 2) == 4) {
       let leftc = false;
       let modalI = 1.0;
       let down_ = String.fromCharCode(108,105,98,120,95,49,95,52,53,0);
       let kuaishoue = String.fromCharCode(102,95,50,48,95,97,110,105,109,97,116,101,0);
      if ((down_.length % 1) <= 2 && (down_.length + parseInt(`${modalI}`)) <= 1) {
         modalI *= 1;
      }
      let frame_cw = 7978567.0 >= modalI;
      do {
         modalI /= Math.max(parseInt(`${modalI}`) / 3, 1);
         if (frame_cw) {
            break;
         }
      } while (((down_.length / 1) >= 5 || 5 >= (down_.length % 1)) && frame_cw);
      if (1 > (down_.length / 1)) {
         down_ += `${((leftc ? 3 : 3))}`;
      }
         down_ += `${((leftc ? 5 : 1) + kuaishoue.length)}`;
      if (!leftc && 1.66 <= (2.12 / (Math.max(2, modalI)))) {
         modalI -= ((leftc ? 3 : 2) >> (Math.min(Math.abs(1), 3)));
      }
         modalI *= ((leftc ? 2 : 1));
         modalI -= (3 << (Math.min(4, Math.abs((leftc ? 5 : 5)))));
          let modelq = 1.0;
          let navigationB = String.fromCharCode(109,95,51,57,95,112,97,105,110,116,0);
          let navigationr = String.fromCharCode(104,111,117,114,95,112,95,49,55,0);
         kuaishoue = `${kuaishoue.length}`;
         modelq -= parseFloat(`${navigationr.length}`);
         navigationB = "2";
         navigationr = `${navigationr.length}`;
         down_ += `${parseInt(`${modalI}`)}`;
      if (kuaishoue.length >= down_.length) {
         kuaishoue = `${2 - kuaishoue.length}`;
      }
       let style1 = 2;
       let default_jB = 4;
       let disconnectedV = 5;
       let pressureP = 4;
      blacklisth += `${3 ^ parseInt(`${tickX}`)}`;
   }
      spinner3 += `${parseInt(`${mbbid7}`) << (Math.min(4, Math.abs(parseInt(`${utils0}`))))}`;
      blacklisth = "2";
      indexy -= parseInt(`${mbbid7}`);
       let calendarU = String.fromCharCode(110,111,109,105,110,97,116,111,114,115,95,116,95,56,57,0);
       let bodany: Array<any> = [288, 781];
         calendarU = "1";
      for (let d = 0; d < 2; d++) {
         bodany.push(bodany.length | 3);
      }
         bodany = [(String.fromCharCode(48,0) == calendarU ? calendarU.length : bodany.length)];
      if ((bodany.length + calendarU.length) >= 2 || (calendarU.length + bodany.length) >= 2) {
         bodany.push(calendarU.length + bodany.length);
      }
       let filedL: Map<any, any> = new Map([[String.fromCharCode(104,95,55,49,95,97,112,112,118,101,121,111,114,0),229], [String.fromCharCode(99,95,52,49,95,98,101,103,105,110,115,0),887], [String.fromCharCode(106,95,51,48,95,97,117,103,109,101,110,116,97,116,105,111,110,0),948]]);
       let kleving: Map<any, any> = new Map([[String.fromCharCode(119,95,56,48,95,114,115,104,105,102,116,0),267], [String.fromCharCode(115,117,98,115,116,105,116,117,116,101,100,95,110,95,53,50,0),921], [String.fromCharCode(112,114,111,98,108,101,109,95,51,95,57,55,0),56]]);
      while (2 <= (kleving.size ^ filedL.size)) {
         kleving.set(`${bodany.length}`, bodany.length >> (Math.min(2, Math.abs(kleving.size))));
         break;
      }
      less0 = utils0 < 27.84;
   let gesturesi = 5785843.0 >= orange2;
   do {
      orange2 *= 3 - parseInt(`${indexy}`);
      if (gesturesi) {
         break;
      }
   } while (((fieldw.length % 3) > 5 || (fieldw.length % 3) > 1) && gesturesi);
   for (let h = 0; h < 1; h++) {
       let yellowz = String.fromCharCode(105,110,116,101,114,97,99,116,105,118,101,108,108,121,95,118,95,53,57,0);
       let matchesE = false;
          let matchj = String.fromCharCode(98,95,57,50,95,112,117,108,108,113,117,111,116,101,0);
          let huaweiH: Map<any, any> = new Map([[String.fromCharCode(112,114,111,112,101,114,116,121,95,111,95,53,55,0),true ], [String.fromCharCode(100,95,52,55,95,114,101,97,100,120,98,108,111,99,107,0),true ], [String.fromCharCode(109,95,53,51,95,112,97,114,101,110,116,97,103,101,0),false ]]);
         matchesE = yellowz.length == 76;
         matchj = `${huaweiH.size}`;
         huaweiH = new Map([[`${huaweiH.size}`, huaweiH.size]]);
         yellowz += `${((matchesE ? 2 : 3) << (Math.min(yellowz.length, 5)))}`;
      while (5 >= yellowz.length) {
         yellowz += `${(yellowz.length << (Math.min(3, Math.abs((matchesE ? 5 : 1)))))}`;
         break;
      }
      let package_1c = String.fromCharCode(102,116,112,95,118,120,49,0) == yellowz;
      do {
         yellowz += `${((matchesE ? 4 : 2) | 2)}`;
         if (package_1c) {
            break;
         }
      } while (package_1c && (2 > yellowz.length));
      if (yellowz.endsWith(`${matchesE}`)) {
         yellowz += `${yellowz.length}`;
      }
      for (let u = 0; u < 3; u++) {
          let fastforwardt = 0.0;
          let becomeP = String.fromCharCode(113,95,51,50,95,117,110,114,101,97,100,0);
          let tempU = String.fromCharCode(114,101,99,118,118,95,113,95,50,51,0);
         matchesE = becomeP.length > fastforwardt;
         fastforwardt /= Math.max(tempU.length, 3);
         becomeP += "3";
      }
      orange2 -= fieldw.length;
   }
       let sinaH = String.fromCharCode(114,95,52,50,95,110,117,108,108,97,98,108,101,114,101,115,111,108,118,101,100,0);
          let eighteen0 = String.fromCharCode(104,101,105,103,104,116,115,95,120,95,51,0);
         sinaH += `${eighteen0.length}`;
      if (!sinaH.startsWith(sinaH)) {
         sinaH += `${sinaH.length}`;
      }
      let userU = String.fromCharCode(103,113,98,116,113,98,48,55,98,100,0) == sinaH;
      do {
         sinaH += `${sinaH.length & 2}`;
         if (userU) {
            break;
         }
      } while (userU && (sinaH == sinaH));
      package_cq += `${2 << (Math.min(1, Math.abs(play2.size)))}`;
       let brightnesss: Array<any> = [880, 815];
       let episodesM = String.fromCharCode(119,95,57,49,95,114,101,97,100,108,110,0);
       let transferI = true;
       let benefit5 = true;
       let reade = true;
      let membero = transferI ? !transferI : transferI;
      do {
         transferI = episodesM.length >= 13 && !benefit5;
         if (membero) {
            break;
         }
      } while ((transferI) && membero);
      let plasho = reade ? !reade : reade;
      do {
         reade = transferI && !benefit5;
         if (plasho) {
            break;
         }
      } while ((2 >= episodesM.length) && plasho);
      while ((brightnesss.length & episodesM.length) < 3 && (episodesM.length & 3) < 1) {
         brightnesss = [((transferI ? 3 : 4) & (reade ? 3 : 1))];
         break;
      }
      if (!reade) {
         reade = transferI;
      }
          let blacklistu = String.fromCharCode(99,111,102,97,99,116,111,114,95,97,95,50,54,0);
          let moont = String.fromCharCode(109,97,103,101,110,116,97,95,108,95,55,56,0);
          let eactw = String.fromCharCode(120,95,55,56,95,109,118,99,111,109,112,111,110,101,110,116,0);
         brightnesss.push(moont.length >> (Math.min(Math.abs(1), 4)));
         blacklistu = `${3 ^ blacklistu.length}`;
         moont = "2";
         eactw += `${eactw.length + 1}`;
      while (benefit5) {
         benefit5 = episodesM.length <= 54;
         break;
      }
         reade = episodesM.endsWith(`${transferI}`);
      for (let v = 0; v < 2; v++) {
          let s_lockc = String.fromCharCode(103,95,52,48,95,109,97,112,115,105,122,101,0);
          let typesD = false;
         brightnesss = [((transferI ? 2 : 4) % 1)];
         s_lockc += `${(3 - (typesD ? 1 : 4))}`;
         typesD = s_lockc.includes(`${typesD}`);
      }
      indexy *= 1 % (Math.max(configureO, 5));

        return this.#historiesPages.length > 0;
    }

    static back = (params?: any) => {
        if (!this.#navigator?.canGoBack()) return;

        this.#historiesPages[this.#historiesPages.length - 1].backParams = params;
        this.#navigator?.goBack();
    }

    static getParams = (): any => {
        return this.current()?.params;
    };

    static current = (): ttPromotionCalendar | undefined => {
        if (this.#historiesPages.length <= 0) return;
        return this.#historiesPages[this.#historiesPages.length - 1];
    }

    static previous = (): ttPromotionCalendar | undefined => {
        if (this.#historiesPages.length <= 1) return;
        return this.#historiesPages[this.#historiesPages.length - 2];
    }

    static history = (): ttPromotionCalendar[] => {
        return this.#historiesPages ?? [];
    }

    static #matchHistory = (state: any) => {
        const histories = state?.data?.state?.routes;

        if (!histories) return;

        // if local history > lib history => pop
        if (this.#historiesPages.length > histories.length) {
            for (let i = this.#historiesPages.length - 1; i >= histories.length; i--) {
                this.#handlePop(this.#historiesPages.pop(), histories.length > 0 ? histories[histories.length - 1] : undefined);
            }
        }

        histories.forEach((route: any, index: number) => {
            // if local history < lib history => push
            if (this.#historiesPages.length <= index) {
                this.#handlePush(route)
                return;
            }

            // if local history != lib history => replace
            if (this.#historiesPages[index].key !== route.key || this.#historiesPages[index].params !== route.params) {
                this.#handleReplace(index, route);
            }
        });
    }

    static #handlePush = (route: {
        key: string;
        name: string;
        params?: any;
        state?: any;
    }) => {
        this.#historiesPages.push({
            key: route.key,
            name: route.name,
            params: route.params,
        });

        
        const pendingEventFound = this.pendingEvent.findIndex(e => e.pageName === route.name);

        if (pendingEventFound !== -1) {
            this.#addEventByKey(route.key, LSliderKick.LReducerCore, this.pendingEvent[pendingEventFound].thenEvent);
            this.pendingEvent.splice(pendingEventFound, 1);
        }
    }

    /**
     * params => use for navigate pop action
     */
    static #handlePop = (
        page?: ttPromotionCalendar,
        route?: {
            key: string;
            name: string;
            params?: any;
            state?: any;
        }
    ) => {
        if (!page) return;

        this.#runEventByKey(page.key, LSliderKick.LReducerCore, page.backParams);
        this.#removeListenerByKey(page.key);

        
        if (this.#lastActoinPageName === this.current()?.name) {
            
            this.#historiesPages[this.#historiesPages.length - 1].params = route?.params;

            
            const pendingEventFound = this.pendingEvent.findIndex(e => e.pageName === this.#lastActoinPageName);

            if (pendingEventFound === -1) return;

            this.pendingEvent.splice(pendingEventFound, 1);
        }
    }

    static #handleReplace = (
        index: number,
        route: {
            key: string;
            name: string;
            params?: any;
            state?: any;
        },
    ) => {
        
        if (this.#lastActoinPageName === this.current()?.name) {
            this.#historiesPages[index] = {
                key: route.key,
                name: route.name,
                params: route.params,
            };
            return;
        }

        this.#handlePop(this.#historiesPages[index]);

        this.#historiesPages[index] = {
            key: route.key,
            name: route.name,
            params: route.params,
        };
    }

    static #addEventByKey = (key: string, type: LSliderKick, callback: () => void) => {
        DeviceEventEmitter.addListener(eventKeyGenerator(key, type), callback);
    }

    static #runEventByKey = (key: string, type: LSliderKick, params?: any) => {
        DeviceEventEmitter.emit(eventKeyGenerator(key, type), params);
        this.#removeListenerByKey(key);
    }

    static #removeListenerByKey = (key: string) => {
        DeviceEventEmitter.removeAllListeners(eventKeyGenerator(key, LSliderKick.LReducerCore));
        DeviceEventEmitter.removeAllListeners(eventKeyGenerator(key, LSliderKick.LReactnativejsTeam));
    }
}

export const CRouteInitializer = ({
    middlewares = {},
}: {
    middlewares?: { [key: string]: [] | [(page: string) => string | undefined] },
}) => {
    const navigator = useNavigation<NavigationProp<{ [key: string]: any }>>();

    useEffect(() => {
        ttTarget.init(navigator, { middlewares });

        return ttTarget.closeRouter;
    }, []);

    useEffect(() => {
        ttTarget.updateNavigator(navigator);
        ttTarget.updateMiddlewares(middlewares);
    }, [navigator, middlewares]);

    return <></>
}
