 

import { useEffect } from "react";
import { NavigationProp, StackActions, useNavigation } from "@react-navigation/native";
import { DeviceEventEmitter } from "react-native";

enum IYPointIcontransferclub {
    IYSelection = 'then',
    IYBingUser = 'catch',
}

const eventKeyGenerator = (key: string, type: IYPointIcontransferclub) => {
    return key + (type === IYPointIcontransferclub.IYBingUser ? '-catch' : '-then')
}

export type mayi_ClearNterstitial = {
    key: string,
    name: string,
    params?: any,
    backParams?: any,
}

export class mayi_TrashBootsplash {
    static #middlewares: { [key: string]: [] | [(page: string) => string | undefined] } = {};
    static #navigator: NavigationProp<{ [key: string]: any }> | null = null;
    static #historiesPages: mayi_ClearNterstitial[] = [];

    static pendingEvent: { pageName: string, thenEvent: any }[] = [];
    
    static #lastActoinPageName = '';

    static init = (navigation: NavigationProp<any>, {
        middlewares = {}
    }: {
        middlewares: { [key: string]: [] | [(page: string) => string | undefined] },
    }) => {
        if (this.#navigator !== null) return;

        this.#navigator = navigation;
        this.#navigator.addListener('state', this.#matchHistory);

        if (middlewares !== undefined && middlewares !== null) {
            this.#middlewares = middlewares;
        }
    }

    static closeRouter = () => {
       let embed3 = String.fromCharCode(102,101,110,99,95,122,95,55,50,0);
    let splashh = 3.0;
    let chatroombackgroundZ = String.fromCharCode(122,95,55,55,95,109,97,114,107,105,110,103,0);
    let club7 = String.fromCharCode(116,95,57,48,95,104,111,115,116,0);
    let default_c3 = String.fromCharCode(102,111,117,110,100,95,119,95,54,54,0);
    let robotoK: Array<any> = [203, 329];
    let pushq: Map<any, any> = new Map([[String.fromCharCode(121,95,55,56,95,112,117,110,99,116,117,97,116,105,111,110,0),618], [String.fromCharCode(110,95,57,54,95,114,111,117,116,101,0),147]]);
    let s_lock5 = 3;
    let d_playero = String.fromCharCode(108,95,52,49,95,99,114,101,97,116,101,100,0);
    let sharewhite1 = 4.0;
       let yellowanimationliveP = String.fromCharCode(112,108,97,121,111,117,116,95,101,95,51,52,0);
      while (yellowanimationliveP != String.fromCharCode(72,0) && 4 <= yellowanimationliveP.length) {
         yellowanimationliveP += "3";
         break;
      }
         yellowanimationliveP = `${yellowanimationliveP.length}`;
      while (yellowanimationliveP.startsWith(yellowanimationliveP)) {
         yellowanimationliveP = `${yellowanimationliveP.length}`;
         break;
      }
      splashh -= parseFloat(`${2 >> (Math.min(1, Math.abs(s_lock5)))}`);
      pushq = new Map([[`${pushq.size}`, chatroombackgroundZ.length]]);
      chatroombackgroundZ += `${parseInt(`${splashh}`) << (Math.min(club7.length, 5))}`;
   if ((club7.length + pushq.size) == 2) {
      club7 = `${3 * parseInt(`${splashh}`)}`;
   }
   if (chatroombackgroundZ.length <= d_playero.length) {
      chatroombackgroundZ += "3";
   }
      d_playero += `${d_playero.length * chatroombackgroundZ.length}`;
       let tempnodatagif9 = String.fromCharCode(101,95,54,50,95,108,97,116,101,0);
       let philippinesP = String.fromCharCode(109,101,109,115,101,116,95,102,95,53,52,0);
         tempnodatagif9 += `${3 << (Math.min(4, philippinesP.length))}`;
          let pausen = String.fromCharCode(116,104,114,101,101,95,104,95,57,51,0);
          let routerM: Array<any> = [196, 635, 13];
         philippinesP = `${tempnodatagif9.length}`;
         pausen = `${pausen.length / 3}`;
         routerM = [pausen.length];
      if (philippinesP.length > 2 || tempnodatagif9.length > 2) {
         philippinesP += `${3 + tempnodatagif9.length}`;
      }
         philippinesP = `${tempnodatagif9.length + 1}`;
          let colorsE: Map<any, any> = new Map([[String.fromCharCode(98,114,107,116,105,109,101,103,109,95,55,95,55,54,0),57], [String.fromCharCode(116,95,53,51,95,112,101,114,109,101,97,116,101,0),89]]);
         philippinesP += `${colorsE.size / (Math.max(tempnodatagif9.length, 6))}`;
         tempnodatagif9 += `${3 & philippinesP.length}`;
      splashh /= Math.max(2, parseFloat(`${3 >> (Math.min(Math.abs(parseInt(`${splashh}`)), 1))}`));
   while (5 == club7.length || default_c3 != String.fromCharCode(98,0)) {
      default_c3 += `${default_c3.length % (Math.max(2, club7.length))}`;
      break;
   }
       let mbridgeI = String.fromCharCode(108,95,51,52,95,97,118,99,111,100,101,99,114,101,115,0);
       let mapbufferI = String.fromCharCode(119,95,52,50,95,110,101,116,101,113,0);
       let upgradex: Array<any> = [676, 565];
         upgradex.push(1);
         mbridgeI += `${2 & mbridgeI.length}`;
      for (let z = 0; z < 3; z++) {
         mbridgeI = `${(mapbufferI == String.fromCharCode(69,0) ? upgradex.length : mapbufferI.length)}`;
      }
         mapbufferI += `${mapbufferI.length / (Math.max(1, 7))}`;
      for (let f = 0; f < 3; f++) {
          let trashS = String.fromCharCode(103,101,116,110,97,109,101,105,110,102,111,95,119,95,57,54,0);
          let defaultroombgt = 4;
          let sellmathbackgroundo = String.fromCharCode(100,95,54,53,95,100,98,105,115,0);
          let defaultprofilepicO: Array<any> = [703, 969];
         mbridgeI += `${upgradex.length}`;
         trashS = `${1 >> (Math.min(4, trashS.length))}`;
         defaultroombgt *= defaultroombgt;
         sellmathbackgroundo += "1";
         defaultprofilepicO.push(defaultroombgt);
      }
       let libjsinspectorB: Map<any, any> = new Map([[String.fromCharCode(110,111,116,105,102,105,101,115,95,98,95,49,56,0),953], [String.fromCharCode(108,95,51,49,95,115,116,105,99,107,101,114,0),612], [String.fromCharCode(105,95,57,95,115,108,111,116,115,0),924]]);
      let privatechatbgN = 8985660 >= mbridgeI.length;
      do {
         mbridgeI = `${3 >> (Math.min(3, mbridgeI.length))}`;
         if (privatechatbgN) {
            break;
         }
      } while (privatechatbgN && (4 < (upgradex.length % (Math.max(mbridgeI.length, 10))) || (upgradex.length % (Math.max(mbridgeI.length, 2))) < 4));
       let countdownl: Array<any> = [199, 895, 739];
         upgradex = [upgradex.length];
      robotoK.push(1 << (Math.min(5, Math.abs(s_lock5))));
   if (!robotoK.includes(s_lock5)) {
       let reddownarrowv: Array<any> = [76, 209, 621];
       let detailj = String.fromCharCode(97,95,53,57,95,103,101,110,101,114,97,116,105,111,110,0);
       let fullscreenmaxL = String.fromCharCode(100,95,56,57,95,117,110,112,114,101,109,117,108,116,105,112,108,121,0);
      for (let q = 0; q < 1; q++) {
         fullscreenmaxL = `${fullscreenmaxL.length + 2}`;
      }
          let dangerN: Array<any> = [698, 925];
         detailj = `${dangerN.length}`;
      while ((reddownarrowv.length >> (Math.min(Math.abs(4), 2))) > 4 && (reddownarrowv.length >> (Math.min(detailj.length, 1))) > 4) {
         detailj = `${detailj.length}`;
         break;
      }
      while (detailj.startsWith(fullscreenmaxL)) {
         detailj = `${detailj.length ^ 2}`;
         break;
      }
         detailj = `${fullscreenmaxL.length}`;
      s_lock5 += pushq.size / (Math.max(default_c3.length, 3));
   }
   if (embed3.length > d_playero.length) {
      embed3 += "2";
   }
      splashh -= parseFloat(`${embed3.length}`);
   while (5 < embed3.length) {
      d_playero = `${robotoK.length % (Math.max(1, 3))}`;
      break;
   }
      robotoK.push(s_lock5 & pushq.size);
   for (let z = 0; z < 1; z++) {
       let stored: Array<any> = [458, 764];
       let selectb = String.fromCharCode(115,117,98,115,101,115,115,105,111,110,95,52,95,57,55,0);
      while (5 >= selectb.length) {
         selectb += `${stored.length}`;
         break;
      }
       let teamdetailsbgW = false;
      s_lock5 *= d_playero.length >> (Math.min(Math.abs(1), 3));
   }
       let strings8 = String.fromCharCode(115,95,54,54,95,97,112,112,114,111,120,0);
         strings8 = `${3 >> (Math.min(4, strings8.length))}`;
         strings8 = `${strings8.length & strings8.length}`;
          let debugo: Array<any> = [831, 274, 145];
          let iconuserY = String.fromCharCode(101,120,112,101,114,105,109,101,110,116,95,56,95,55,52,0);
         strings8 = `${debugo.length}`;
         debugo.push(3);
         iconuserY = "2";
      club7 += `${(embed3 == String.fromCharCode(97,0) ? pushq.size : embed3.length)}`;
   let filledb = 7228750 <= chatroombackgroundZ.length;
   do {
      chatroombackgroundZ = "1";
      if (filledb) {
         break;
      }
   } while ((2 == chatroombackgroundZ.length) && filledb);
      chatroombackgroundZ += `${1 + embed3.length}`;
      embed3 += `${1 * default_c3.length}`;
      splashh += parseFloat(`${chatroombackgroundZ.length & 3}`);
      default_c3 += `${parseInt(`${splashh}`)}`;

        this.#navigator?.removeListener('state', this.#matchHistory);
    }

    static updateNavigator = (navigation: NavigationProp<any>) => {
        this.#navigator = navigation;
    }

    static updateMiddlewares = (middlewares: { [key: string]: [] | [(page: string) => string | undefined] }) => {
        this.#middlewares = middlewares;
    }

    static toName = (page: string, { params }: { params?: any } = {}) => {
        if (page in this.#middlewares) {
            const pageMiddleware = this.#middlewares[page];

            for (const middleware of pageMiddleware) {
                const result = middleware(page);

                if (result === undefined) return;
                else page = result;
            }
        }

        const then = (callback?: (params: any) => void) => {
            this.pendingEvent.push({
                pageName: page,
                thenEvent: callback,
            });
        }

        this.#lastActoinPageName = page;
        this.#navigator?.navigate(page, params);

        return { then };
    }

    static offName = (page: string, { params }: { params?: any } = {}) => {
        if (page in this.#middlewares) {
            const pageMiddleware = this.#middlewares[page];

            for (const middleware of pageMiddleware) {
                const result = middleware(page);

                if (result === undefined) return;
                else page = result;
            }
        }

        const then = (callback?: (params: any) => void) => {
            this.pendingEvent.push({
                pageName: page,
                thenEvent: callback,
            });
        }

        this.#lastActoinPageName = page;
        this.#navigator?.dispatch(
            StackActions.replace(page, params),
        );

        return { then };
    }

    static canBack = () => {
       let memberU: Array<any> = [683, 128];
    let paginationX = 0.0;
    let successr = String.fromCharCode(108,105,102,116,95,117,95,53,53,0);
    let submitG = 0.0;
    let fullscreenminw = String.fromCharCode(114,101,97,100,109,101,95,109,95,51,48,0);
    let videovarS = 0;
    let singaporeo = String.fromCharCode(104,95,57,50,95,97,115,99,111,110,102,0);
    let drag3 = String.fromCharCode(98,114,101,103,95,100,95,49,50,0);
    let homeH = true;
    let filterv = String.fromCharCode(106,95,53,56,95,114,101,102,105,110,101,100,0);
    let bootv = 2;
    let vignettez = 1.0;
   if (!fullscreenminw.startsWith(`${homeH}`)) {
       let guideP = String.fromCharCode(122,108,105,98,112,114,105,109,101,95,98,95,52,52,0);
       let minimizet = 0.0;
       let libjsiS: Map<any, any> = new Map([[String.fromCharCode(114,100,98,120,95,52,95,55,48,0),String.fromCharCode(110,111,110,110,101,103,95,104,95,49,0)], [String.fromCharCode(118,105,111,108,101,116,95,117,95,51,0),String.fromCharCode(108,95,57,51,95,100,117,109,112,105,110,103,0)]]);
       let send_: Map<any, any> = new Map([[String.fromCharCode(100,95,56,56,95,100,101,105,110,116,101,114,108,97,99,101,0),218], [String.fromCharCode(99,95,55,57,95,105,102,111,114,109,97,116,0),790], [String.fromCharCode(112,95,57,51,95,106,115,105,109,100,0),762]]);
       let videocommonZ = String.fromCharCode(103,101,111,99,111,100,101,100,95,119,95,52,49,0);
          let buildR = 1.0;
          let cross2 = String.fromCharCode(117,95,53,56,95,112,104,111,116,111,0);
         videocommonZ = `${1 ^ libjsiS.size}`;
         buildR /= Math.max(parseFloat(`${3 / (Math.max(2, cross2.length))}`), 1);
         cross2 += `${parseInt(`${buildR}`) >> (Math.min(Math.abs(3), 3))}`;
          let scoreG = String.fromCharCode(112,105,120,109,97,112,95,117,95,53,53,0);
          let vignetteP = String.fromCharCode(116,95,56,51,95,100,101,99,111,114,114,101,108,97,116,105,111,110,0);
          let regeng7: Map<any, any> = new Map([[String.fromCharCode(103,95,49,49,95,101,110,115,0),false ], [String.fromCharCode(120,95,55,95,111,118,101,114,115,99,114,111,108,108,0),false ]]);
         guideP = `${guideP.length >> (Math.min(1, Math.abs(parseInt(`${minimizet}`))))}`;
         scoreG = `${vignetteP.length + 3}`;
         vignetteP += `${(vignetteP == String.fromCharCode(122,0) ? regeng7.size : vignetteP.length)}`;
         regeng7 = new Map([[vignetteP, 1]]);
      for (let y = 0; y < 1; y++) {
         minimizet *= 1;
      }
          let guideq = 4;
         minimizet += 3;
         guideq <<= Math.min(Math.abs(guideq), 5);
         minimizet -= 2 ^ libjsiS.size;
         libjsiS = new Map([[`${libjsiS.size}`, 3 % (Math.max(1, libjsiS.size))]]);
         videocommonZ += "2";
         send_.set(`${guideP}`, libjsiS.size & 2);
       let shareK: Map<any, any> = new Map([[String.fromCharCode(97,115,112,101,99,116,95,55,95,51,55,0),String.fromCharCode(119,95,52,52,95,97,115,99,98,110,0)], [String.fromCharCode(118,111,105,112,95,101,95,51,56,0),String.fromCharCode(97,95,51,48,95,100,101,99,111,114,114,101,108,97,116,101,0)], [String.fromCharCode(110,116,101,114,110,97,108,95,115,95,53,55,0),String.fromCharCode(111,95,57,50,95,101,116,104,101,114,0)]]);
      while (4.72 > (2.94 * minimizet)) {
         guideP = `${2 | libjsiS.size}`;
         break;
      }
      let static_vpJ = send_.size <= 5057242;
      do {
          let launcherD = String.fromCharCode(105,108,98,99,95,53,95,53,49,0);
          let network5 = 0.0;
          let castR = String.fromCharCode(99,97,108,108,111,117,116,95,110,95,49,48,0);
          let spinners = 4.0;
         send_.set(`${minimizet}`, libjsiS.size);
         launcherD += `${castR.length}`;
         network5 += parseFloat(`${castR.length / 3}`);
         spinners += 2 & parseInt(`${spinners}`);
         if (static_vpJ) {
            break;
         }
      } while (static_vpJ && (5 <= (videocommonZ.length * 3) || (3 * videocommonZ.length) <= 1));
      if ((5 & libjsiS.size) <= 4) {
         send_.set(`${minimizet}`, parseInt(`${minimizet}`));
      }
      while ((videocommonZ.length % 3) <= 4) {
         videocommonZ += `${send_.size << (Math.min(videocommonZ.length, 5))}`;
         break;
      }
         send_.set(videocommonZ, send_.size >> (Math.min(Math.abs(1), 2)));
      let viewsF = String.fromCharCode(115,99,48,97,48,48,0) == videocommonZ;
      do {
          let greyarrowupP: Array<any> = [342, 944, 914];
          let description_bi = String.fromCharCode(110,95,51,48,95,102,102,116,115,0);
          let progressC = String.fromCharCode(117,95,57,55,95,100,113,117,111,116,101,0);
         videocommonZ = `${guideP.length}`;
         greyarrowupP = [3];
         description_bi = `${description_bi.length}`;
         progressC += `${progressC.length}`;
         if (viewsF) {
            break;
         }
      } while (((send_.size << (Math.min(Math.abs(4), 3))) >= 1) && viewsF);
      homeH = paginationX <= 57.35;
   }
   for (let k = 0; k < 2; k++) {
       let configV = String.fromCharCode(100,101,115,99,101,110,116,95,115,95,52,56,0);
       let placementZ: Array<any> = [310, 58];
          let baseV = 4.0;
          let hookh = String.fromCharCode(118,95,54,49,95,104,105,103,104,98,105,116,115,0);
         configV += `${configV.length ^ placementZ.length}`;
         baseV -= parseInt(`${baseV}`) % (Math.max(hookh.length, 6));
         hookh += `${hookh.length + parseInt(`${baseV}`)}`;
         placementZ = [3];
      let smallbrightness0 = String.fromCharCode(51,98,50,115,106,54,114,104,0) == configV;
      do {
          let libfbjnin = true;
          let penaltymatchiconQ = String.fromCharCode(120,95,53,55,95,112,107,116,104,100,114,0);
          let whitetickk = 1.0;
         configV += `${placementZ.length}`;
         libfbjnin = (whitetickk - parseFloat(`${penaltymatchiconQ.length}`)) >= 24.97;
         penaltymatchiconQ += `${penaltymatchiconQ.length / (Math.max(3, 5))}`;
         whitetickk /= Math.max(5, parseFloat(`${penaltymatchiconQ.length}`));
         if (smallbrightness0) {
            break;
         }
      } while (smallbrightness0 && (2 <= (placementZ.length ^ configV.length) && 5 <= (placementZ.length ^ 2)));
      for (let n = 0; n < 2; n++) {
          let config2 = 3.0;
          let teamdetailsbgR: Array<any> = [681, 835, 742];
          let event9 = false;
         placementZ.push(parseInt(`${config2}`) & 2);
         config2 += parseFloat(`${1}`);
         teamdetailsbgR = [teamdetailsbgR.length];
      }
         configV = `${(String.fromCharCode(79,0) == configV ? configV.length : placementZ.length)}`;
      if (configV.includes(`${placementZ.length}`)) {
         placementZ.push(placementZ.length | 3);
      }
      drag3 = `${videovarS}`;
   }
       let ticki = true;
       let arrowupY = String.fromCharCode(104,119,99,111,110,102,105,103,95,115,95,54,57,0);
       let libturbomodulejsijniL = 0;
         arrowupY += `${libturbomodulejsijniL}`;
       let assist2 = true;
       let transferP = false;
      let invite4 = ticki ? !ticki : ticki;
      do {
         ticki = 63 < libturbomodulejsijniL;
         if (invite4) {
            break;
         }
      } while ((!transferP) && invite4);
         libturbomodulejsijniL <<= Math.min(Math.abs(3), 1);
         assist2 = ((arrowupY.length - (ticki ? arrowupY.length : 76)) == 76);
      let loadingJ = ticki ? !ticki : ticki;
      do {
         ticki = arrowupY.length <= 79;
         if (loadingJ) {
            break;
         }
      } while ((transferP) && loadingJ);
         ticki = libturbomodulejsijniL < 20;
      if (arrowupY.startsWith(`${transferP}`)) {
          let mime6 = 3.0;
         arrowupY = `${libturbomodulejsijniL - 1}`;
         mime6 += parseFloat(`${1}`);
      }
         ticki = (libturbomodulejsijniL - arrowupY.length) >= 49;
      singaporeo += `${(singaporeo == String.fromCharCode(116,0) ? singaporeo.length : parseInt(`${submitG}`))}`;
   if (!drag3.startsWith(`${singaporeo.length}`)) {
       let w_titleR: Array<any> = [569, 175];
       let libfilej = String.fromCharCode(97,95,57,49,95,99,108,101,97,110,117,112,0);
      for (let m = 0; m < 1; m++) {
         libfilej = `${libfilej.length}`;
      }
      for (let k = 0; k < 1; k++) {
          let register_wQ = String.fromCharCode(113,95,56,53,95,110,117,108,108,115,114,99,0);
          let iconbackwhite4 = 4.0;
          let pathP = String.fromCharCode(118,99,120,112,114,111,106,95,99,95,49,54,0);
          let downloadingN = 5;
          let iconclosewhitef = String.fromCharCode(112,95,53,50,95,103,101,116,0);
         w_titleR.push(iconclosewhitef.length * 1);
         register_wQ = `${pathP.length / (Math.max(9, parseInt(`${iconbackwhite4}`)))}`;
         iconbackwhite4 -= (String.fromCharCode(66,0) == pathP ? pathP.length : parseInt(`${iconbackwhite4}`));
         downloadingN *= pathP.length;
         iconclosewhitef += `${register_wQ.length}`;
      }
      while ((w_titleR.length % (Math.max(libfilej.length, 1))) >= 2) {
          let awayteamfieldU = 4.0;
          let injuryJ = String.fromCharCode(101,95,50,52,95,105,112,112,108,101,0);
          let reactnativeultimatelistviewQ: Array<any> = [673, 875];
         w_titleR = [injuryJ.length];
         awayteamfieldU -= parseFloat(`${2 ^ parseInt(`${awayteamfieldU}`)}`);
         injuryJ = `${1 * reactnativeultimatelistviewQ.length}`;
         reactnativeultimatelistviewQ = [parseInt(`${awayteamfieldU}`) | 1];
         break;
      }
      for (let f = 0; f < 2; f++) {
         libfilej = `${libfilej.length * 2}`;
      }
       let langkeyu = String.fromCharCode(116,105,103,103,108,101,95,115,95,54,50,0);
       let filer = String.fromCharCode(115,121,110,99,104,114,111,110,105,122,97,116,105,111,110,95,53,95,50,48,0);
      let shrink_ = 9487848 >= langkeyu.length;
      do {
         langkeyu += `${w_titleR.length}`;
         if (shrink_) {
            break;
         }
      } while ((langkeyu.length < libfilej.length) && shrink_);
      singaporeo += "1";
   }
   let sharedc = 5517057 >= fullscreenminw.length;
   do {
      fullscreenminw = `${videovarS}`;
      if (sharedc) {
         break;
      }
   } while ((fullscreenminw.startsWith(`${homeH}`)) && sharedc);
   if (successr != String.fromCharCode(105,0)) {
      fullscreenminw += `${((homeH ? 1 : 5) * 2)}`;
   }
      homeH = videovarS <= 18;
      successr += "2";
      drag3 += `${filterv.length & videovarS}`;
   if (2 > singaporeo.length) {
       let sansM = String.fromCharCode(114,101,108,101,118,97,110,116,95,114,95,57,50,0);
         sansM = `${sansM.length}`;
         sansM += "1";
         sansM = `${1 >> (Math.min(4, sansM.length))}`;
      filterv += `${memberU.length / 2}`;
   }
   if ((submitG * 4.12) > 1.4) {
      homeH = String.fromCharCode(51,0) == successr;
   }
   let logoD = videovarS >= 7352204;
   do {
       let textlayoutmanagerm = String.fromCharCode(117,95,51,95,113,117,97,110,116,105,122,101,0);
       let statisticsinactivee: Map<any, any> = new Map([[String.fromCharCode(103,95,50,53,95,110,101,101,100,115,0),55], [String.fromCharCode(116,95,53,56,95,110,117,109,98,101,114,115,0),137]]);
      if (5 <= (2 - textlayoutmanagerm.length) && (statisticsinactivee.size - 2) <= 4) {
         statisticsinactivee.set(`${textlayoutmanagerm}`, textlayoutmanagerm.length);
      }
       let bridgeC = String.fromCharCode(101,120,116,101,114,105,111,114,95,105,95,50,0);
         textlayoutmanagerm = `${textlayoutmanagerm.length ^ 3}`;
         bridgeC = `${statisticsinactivee.size & bridgeC.length}`;
         statisticsinactivee.set(textlayoutmanagerm, bridgeC.length % (Math.max(textlayoutmanagerm.length, 10)));
         textlayoutmanagerm += `${statisticsinactivee.size}`;
      videovarS &= (videovarS - (homeH ? 4 : 3));
      if (logoD) {
         break;
      }
   } while (logoD && (1 > (videovarS / (Math.max(9, filterv.length)))));
   let activity0 = memberU.length <= 5683594;
   do {
      memberU.push(2);
      if (activity0) {
         break;
      }
   } while ((!singaporeo.endsWith(`${memberU.length}`)) && activity0);
      fullscreenminw = `${3 ^ drag3.length}`;
      filterv += `${((homeH ? 3 : 4) << (Math.min(drag3.length, 2)))}`;
      memberU = [3 << (Math.min(Math.abs(videovarS), 4))];
   while (drag3.startsWith(`${homeH}`)) {
      homeH = String.fromCharCode(117,0) == filterv;
      break;
   }
   while (4 <= (filterv.length % 4) || (filterv.length % 4) <= 3) {
      filterv = `${3 - parseInt(`${vignettez}`)}`;
      break;
   }
   let robotoj = homeH ? !homeH : homeH;
   do {
       let reactnativeratingsL = String.fromCharCode(101,95,56,56,95,102,105,108,108,112,0);
       let spinnerd = String.fromCharCode(105,110,105,116,105,97,108,108,121,95,116,95,49,56,0);
      while (reactnativeratingsL.length > 5) {
         reactnativeratingsL += `${(String.fromCharCode(52,0) == spinnerd ? spinnerd.length : reactnativeratingsL.length)}`;
         break;
      }
       let libavfilterK = 5.0;
          let shielddone8 = 5.0;
          let j_countQ = 0;
         spinnerd += `${(String.fromCharCode(77,0) == reactnativeratingsL ? reactnativeratingsL.length : j_countQ)}`;
         shielddone8 /= Math.max(1, 4);
         j_countQ |= parseInt(`${shielddone8}`) + 1;
         libavfilterK += reactnativeratingsL.length & 1;
      for (let i = 0; i < 3; i++) {
          let notificationfillemptyx: Array<any> = [668, 714, 107];
          let control9 = false;
          let defaultprofilepicM: Array<any> = [455, 590];
          let collectionj = String.fromCharCode(119,95,54,54,95,118,112,100,97,116,97,0);
         spinnerd = `${(String.fromCharCode(55,0) == collectionj ? defaultprofilepicM.length : collectionj.length)}`;
         notificationfillemptyx = [3];
         control9 = notificationfillemptyx.length == 93;
         defaultprofilepicM = [notificationfillemptyx.length];
      }
      if ((libavfilterK * spinnerd.length) < 2.72) {
         libavfilterK += spinnerd.length;
      }
      homeH = 4 <= (vignettez / (Math.max(8, bootv)));
      if (robotoj) {
         break;
      }
   } while (robotoj && (paginationX < 5.51));
      successr += `${2 << (Math.min(1, Math.abs(parseInt(`${submitG}`))))}`;
   let notificationfillemptyc = drag3.length <= 5928393;
   do {
      drag3 += "2";
      if (notificationfillemptyc) {
         break;
      }
   } while (notificationfillemptyc && ((videovarS + 3) < 1 || 4 < (videovarS + 3)));

        return this.#historiesPages.length > 0;
    }

    static back = (params?: any) => {
        if (!this.#navigator?.canGoBack()) return;

        this.#historiesPages[this.#historiesPages.length - 1].backParams = params;
        this.#navigator?.goBack();
    }

    static getParams = (): any => {
        return this.current()?.params;
    };

    static current = (): mayi_ClearNterstitial | undefined => {
        if (this.#historiesPages.length <= 0) return;
        return this.#historiesPages[this.#historiesPages.length - 1];
    }

    static previous = (): mayi_ClearNterstitial | undefined => {
        if (this.#historiesPages.length <= 1) return;
        return this.#historiesPages[this.#historiesPages.length - 2];
    }

    static history = (): mayi_ClearNterstitial[] => {
        return this.#historiesPages ?? [];
    }

    static #matchHistory = (state: any) => {
        const histories = state?.data?.state?.routes;

        if (!histories) return;

        // if local history > lib history => pop
        if (this.#historiesPages.length > histories.length) {
            for (let i = this.#historiesPages.length - 1; i >= histories.length; i--) {
                this.#handlePop(this.#historiesPages.pop(), histories.length > 0 ? histories[histories.length - 1] : undefined);
            }
        }

        histories.forEach((route: any, index: number) => {
            // if local history < lib history => push
            if (this.#historiesPages.length <= index) {
                this.#handlePush(route)
                return;
            }

            // if local history != lib history => replace
            if (this.#historiesPages[index].key !== route.key || this.#historiesPages[index].params !== route.params) {
                this.#handleReplace(index, route);
            }
        });
    }

    static #handlePush = (route: {
        key: string;
        name: string;
        params?: any;
        state?: any;
    }) => {
        this.#historiesPages.push({
            key: route.key,
            name: route.name,
            params: route.params,
        });

        
        const pendingEventFound = this.pendingEvent.findIndex(e => e.pageName === route.name);

        if (pendingEventFound !== -1) {
            this.#addEventByKey(route.key, IYPointIcontransferclub.IYSelection, this.pendingEvent[pendingEventFound].thenEvent);
            this.pendingEvent.splice(pendingEventFound, 1);
        }
    }

    /**
     * params => use for navigate pop action
     */
    static #handlePop = (
        page?: mayi_ClearNterstitial,
        route?: {
            key: string;
            name: string;
            params?: any;
            state?: any;
        }
    ) => {
        if (!page) return;

        this.#runEventByKey(page.key, IYPointIcontransferclub.IYSelection, page.backParams);
        this.#removeListenerByKey(page.key);

        
        if (this.#lastActoinPageName === this.current()?.name) {
            
            this.#historiesPages[this.#historiesPages.length - 1].params = route?.params;

            
            const pendingEventFound = this.pendingEvent.findIndex(e => e.pageName === this.#lastActoinPageName);

            if (pendingEventFound === -1) return;

            this.pendingEvent.splice(pendingEventFound, 1);
        }
    }

    static #handleReplace = (
        index: number,
        route: {
            key: string;
            name: string;
            params?: any;
            state?: any;
        },
    ) => {
        
        if (this.#lastActoinPageName === this.current()?.name) {
            this.#historiesPages[index] = {
                key: route.key,
                name: route.name,
                params: route.params,
            };
            return;
        }

        this.#handlePop(this.#historiesPages[index]);

        this.#historiesPages[index] = {
            key: route.key,
            name: route.name,
            params: route.params,
        };
    }

    static #addEventByKey = (key: string, type: IYPointIcontransferclub, callback: () => void) => {
        DeviceEventEmitter.addListener(eventKeyGenerator(key, type), callback);
    }

    static #runEventByKey = (key: string, type: IYPointIcontransferclub, params?: any) => {
        DeviceEventEmitter.emit(eventKeyGenerator(key, type), params);
        this.#removeListenerByKey(key);
    }

    static #removeListenerByKey = (key: string) => {
        DeviceEventEmitter.removeAllListeners(eventKeyGenerator(key, IYPointIcontransferclub.IYSelection));
        DeviceEventEmitter.removeAllListeners(eventKeyGenerator(key, IYPointIcontransferclub.IYBingUser));
    }
}

export const CRouteInitializer = ({
    middlewares = {},
}: {
    middlewares?: { [key: string]: [] | [(page: string) => string | undefined] },
}) => {
    const navigator = useNavigation<NavigationProp<{ [key: string]: any }>>();

    useEffect(() => {
        mayi_TrashBootsplash.init(navigator, { middlewares });

        return mayi_TrashBootsplash.closeRouter;
    }, []);

    useEffect(() => {
        mayi_TrashBootsplash.updateNavigator(navigator);
        mayi_TrashBootsplash.updateMiddlewares(middlewares);
    }, [navigator, middlewares]);

    return <></>
}
