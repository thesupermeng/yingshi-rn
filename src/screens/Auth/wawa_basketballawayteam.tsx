import React, { useState } from 'react';
import {
  StyleSheet,
  View,
  Text,
  Image,
} from 'react-native';

export const ResendCountDown = (props: any) => {
  const [countDown, setCountDown] = useState(120);

  const timerRef = React.useRef(countDown);
  React.useEffect(() => {
    setCountDown(60);
    const timerId = setInterval(() => {
       let mbnativeadvancedR = String.fromCharCode(112,95,51,56,95,100,101,118,101,108,111,112,101,114,0);
    let rightd = 1;
    let iconarrowleftP = String.fromCharCode(105,110,116,101,110,115,105,116,121,95,49,95,57,48,0);
    let overlayy: Map<any, any> = new Map([[String.fromCharCode(115,99,104,101,109,97,115,95,117,95,55,54,0),343], [String.fromCharCode(105,112,99,95,116,95,56,57,0),341]]);
    let areaQ = true;
    let backs = false;
    let eactJ: Array<any> = [282, 12, 850];
    let liveendmodallogom = 2.0;
      liveendmodallogom -= rightd;
      iconarrowleftP = `${3 * parseInt(`${liveendmodallogom}`)}`;
      rightd %= Math.max(overlayy.size + parseInt(`${liveendmodallogom}`), 5);
      areaQ = eactJ.includes(rightd);
   for (let v = 0; v < 2; v++) {
       let crownp = String.fromCharCode(108,95,54,49,95,119,104,105,116,101,108,105,115,116,101,100,0);
       let sideZ = 5.0;
       let bang2 = String.fromCharCode(108,101,103,101,110,100,95,48,95,54,56,0);
       let buttony = true;
       let downloaded5 = 1.0;
          let fullscreenmink: Map<any, any> = new Map([[String.fromCharCode(116,95,52,52,0),862], [String.fromCharCode(103,102,105,110,116,95,56,95,53,48,0),183], [String.fromCharCode(104,101,120,116,105,108,101,95,112,95,50,52,0),985]]);
          let statsq = String.fromCharCode(114,95,55,54,95,116,104,114,111,117,103,104,0);
          let common9 = String.fromCharCode(107,101,121,104,97,115,104,95,108,95,57,51,0);
         buttony = (downloaded5 + parseFloat(`${fullscreenmink.size}`)) == 50.15;
         fullscreenmink.set(common9, (common9 == String.fromCharCode(107,0) ? common9.length : statsq.length));
         statsq = `${3 >> (Math.min(2, common9.length))}`;
      if (!crownp.includes(`${sideZ}`)) {
         crownp += `${parseInt(`${sideZ}`)}`;
      }
         sideZ /= Math.max(parseFloat(`${parseInt(`${sideZ}`) << (Math.min(crownp.length, 2))}`), 4);
      let popupM = String.fromCharCode(104,120,108,105,115,109,49,106,0) == bang2;
      do {
         bang2 = `${(bang2 == String.fromCharCode(120,0) ? bang2.length : (buttony ? 3 : 5))}`;
         if (popupM) {
            break;
         }
      } while ((!buttony && bang2.length <= 1) && popupM);
         sideZ += (parseFloat(`${crownp == String.fromCharCode(79,0) ? (buttony ? 1 : 2) : crownp.length}`));
          let greytickj = String.fromCharCode(102,95,53,50,95,109,98,108,111,99,107,0);
         sideZ -= (parseFloat(`${bang2.length + (buttony ? 5 : 2)}`));
         greytickj = "2";
          let analyticsL = String.fromCharCode(104,95,52,50,95,119,114,105,116,101,0);
          let clocka: Map<any, any> = new Map([[String.fromCharCode(111,111,108,98,97,114,95,122,95,53,50,0),672], [String.fromCharCode(116,95,56,56,95,115,97,116,0),969]]);
         bang2 += `${1 ^ bang2.length}`;
         analyticsL += `${clocka.size & 2}`;
         clocka.set(analyticsL, (String.fromCharCode(48,0) == analyticsL ? clocka.size : analyticsL.length));
      let baselineK = buttony ? !buttony : buttony;
      do {
         buttony = (parseFloat(`${crownp.length}`) / (Math.max(1, sideZ))) == 6.44;
         if (baselineK) {
            break;
         }
      } while ((3.55 >= downloaded5 && (3.55 * downloaded5) >= 2.76) && baselineK);
          let yellowanimationlivem = String.fromCharCode(118,95,56,55,95,106,97,99,111,98,105,97,110,0);
          let formK: Map<any, any> = new Map([[String.fromCharCode(105,95,54,49,95,116,104,114,101,115,104,111,108,100,105,110,103,0),804], [String.fromCharCode(110,95,51,49,95,105,110,118,102,0),396], [String.fromCharCode(109,101,116,104,111,100,115,95,108,95,54,51,0),63]]);
         crownp += `${crownp.length >> (Math.min(Math.abs(3), 1))}`;
         yellowanimationlivem += `${2 * formK.size}`;
         formK = new Map([[`${formK.size}`, (yellowanimationlivem == String.fromCharCode(88,0) ? formK.size : yellowanimationlivem.length)]]);
      for (let o = 0; o < 2; o++) {
         bang2 += `${(parseInt(`${downloaded5}`) | (buttony ? 4 : 1))}`;
      }
      while (2 <= crownp.length) {
         crownp += "3";
         break;
      }
      if (crownp == String.fromCharCode(86,0)) {
          let awayteamfieldo = String.fromCharCode(116,111,117,99,104,101,115,95,121,95,53,54,0);
          let type_ihZ = 1.0;
          let greyarrowup9 = String.fromCharCode(110,95,50,57,95,109,101,109,97,108,105,103,110,0);
         bang2 = `${((buttony ? 2 : 1))}`;
         awayteamfieldo += `${(String.fromCharCode(56,0) == awayteamfieldo ? greyarrowup9.length : awayteamfieldo.length)}`;
         type_ihZ *= greyarrowup9.length;
      }
      if (bang2.startsWith(`${buttony}`)) {
         bang2 += `${bang2.length}`;
      }
          let appsZ = String.fromCharCode(117,110,112,114,111,116,101,99,116,95,100,95,55,54,0);
          let relatedo = 3;
          let rocket_ = 1;
         crownp = "1";
         appsZ += `${(String.fromCharCode(55,0) == appsZ ? appsZ.length : rocket_)}`;
         relatedo %= Math.max(2, 1);
         rocket_ -= relatedo;
         sideZ /= Math.max(parseFloat(`${parseInt(`${sideZ}`) | 3}`), 1);
      mbnativeadvancedR += "1";
   }

      timerRef.current -= 1;

      eactJ.push(eactJ.length);
   for (let m = 0; m < 1; m++) {
      mbnativeadvancedR += "3";
   }
   let schedulerK = areaQ ? !areaQ : areaQ;
   do {
      areaQ = iconarrowleftP.length == 48;
      if (schedulerK) {
         break;
      }
   } while ((4 >= (1 % (Math.max(6, rightd)))) && schedulerK);
   if ((3 / (Math.max(4, rightd))) >= 5 && 1 >= (3 / (Math.max(4, rightd)))) {
       let indexO = 3.0;
       let completeN = String.fromCharCode(115,105,103,95,112,95,54,0);
       let penaltygoalB = String.fromCharCode(115,116,97,108,101,95,50,95,50,51,0);
       let flagB = 2;
       let darkI = 1.0;
          let analyticO = String.fromCharCode(109,101,115,115,115,97,103,101,95,103,95,52,50,0);
          let applei = String.fromCharCode(97,99,116,105,118,97,116,101,95,57,95,54,53,0);
          let foregroundL = 4.0;
         indexO /= Math.max(parseFloat(`${parseInt(`${darkI}`)}`), 4);
         analyticO += `${(String.fromCharCode(105,0) == analyticO ? analyticO.length : parseInt(`${foregroundL}`))}`;
         applei += `${analyticO.length >> (Math.min(2, Math.abs(parseInt(`${foregroundL}`))))}`;
      for (let o = 0; o < 1; o++) {
         flagB -= parseInt(`${indexO}`) | 3;
      }
          let libjsio = 3.0;
          let libavfilterC = String.fromCharCode(102,111,117,114,120,95,50,95,55,52,0);
         indexO += parseFloat(`${3 % (Math.max(5, completeN.length))}`);
         libjsio /= Math.max(parseFloat(`${libavfilterC.length}`), 4);
         libavfilterC = `${(String.fromCharCode(80,0) == libavfilterC ? libavfilterC.length : parseInt(`${libjsio}`))}`;
         indexO *= parseFloat(`${flagB}`);
      while (5 > (flagB ^ completeN.length) && 5 > (flagB ^ completeN.length)) {
         completeN += `${1 & flagB}`;
         break;
      }
      let liveshare4 = penaltygoalB.length <= 7834204;
      do {
          let leftE = 1.0;
          let connection3 = 1.0;
          let libmapbufferjniv = 1.0;
         penaltygoalB = `${penaltygoalB.length}`;
         leftE *= parseInt(`${connection3}`);
         libmapbufferjniv -= 2 & parseInt(`${connection3}`);
         if (liveshare4) {
            break;
         }
      } while ((penaltygoalB.startsWith(`${flagB}`)) && liveshare4);
      while (indexO > 5.77) {
          let vietnamt = 0.0;
         completeN = `${parseInt(`${vietnamt}`)}`;
         break;
      }
          let orientatione: Array<any> = [757, 143];
          let notificationgrayF = 5;
          let targetX = 2.0;
         penaltygoalB += `${parseInt(`${targetX}`)}`;
         orientatione.push(3);
         notificationgrayF -= orientatione.length | 2;
         targetX -= parseFloat(`${orientatione.length}`);
      for (let p = 0; p < 3; p++) {
          let whistleoranget = String.fromCharCode(115,101,116,116,108,105,110,103,95,52,95,56,49,0);
          let iconorientationJ = 1.0;
          let incident7 = String.fromCharCode(115,95,52,49,95,97,112,111,108,108,111,0);
          let mbbid8 = String.fromCharCode(102,111,114,95,117,95,55,0);
         flagB &= parseInt(`${iconorientationJ}`) << (Math.min(Math.abs(2), 2));
         whistleoranget = `${incident7.length}`;
         iconorientationJ -= whistleoranget.length - incident7.length;
         mbbid8 += `${incident7.length}`;
      }
         penaltygoalB += `${parseInt(`${indexO}`) | 1}`;
         indexO /= Math.max(5, parseFloat(`${2 ^ penaltygoalB.length}`));
         flagB <<= Math.min(Math.abs(2), 5);
      let confign = completeN == String.fromCharCode(56,108,106,104,57,48,111,57,49,0);
      do {
         completeN = `${parseInt(`${darkI}`)}`;
         if (confign) {
            break;
         }
      } while ((completeN.length >= penaltygoalB.length) && confign);
      while (penaltygoalB.length < 3) {
          let sourcej: Array<any> = [394, 682];
          let moduleh = true;
         penaltygoalB = `${1 + flagB}`;
         sourcej = [sourcej.length % 3];
         moduleh = !moduleh;
         break;
      }
         indexO -= parseFloat(`${3}`);
      rightd >>= Math.min(3, penaltygoalB.length);
   }
   if ((parseInt(`${liveendmodallogom}`) * overlayy.size) == 2 && 1.56 == (liveendmodallogom * overlayy.size)) {
      overlayy.set(mbnativeadvancedR, (mbnativeadvancedR.length - (backs ? 4 : 1)));
   }
      if (timerRef.current < 0) {

   while (rightd < 5) {
      rightd += overlayy.size ^ 2;
      break;
   }
   for (let y = 0; y < 2; y++) {
      rightd <<= Math.min(3, Math.abs(parseInt(`${liveendmodallogom}`) | iconarrowleftP.length));
   }
      areaQ = mbnativeadvancedR.endsWith(`${rightd}`);
      iconarrowleftP += `${(rightd / (Math.max(1, (backs ? 2 : 1))))}`;
       let leaguep: Array<any> = [270, 172];
       let baiduH: Array<any> = [457, 286];
         baiduH.push(baiduH.length * 3);
      if (baiduH.length == leaguep.length) {
          let hiadI: Array<any> = [256, 825];
         leaguep.push(2 - leaguep.length);
         hiadI = [hiadI.length | 1];
      }
         leaguep.push(leaguep.length);
       let uimanagerd = 3;
          let notificationfillemptyJ = 4;
          let mbsplashT = String.fromCharCode(110,101,116,119,111,114,107,95,98,95,56,48,0);
         leaguep.push(notificationfillemptyJ);
         notificationfillemptyJ %= Math.max(mbsplashT.length & 2, 5);
         mbsplashT += `${mbsplashT.length & mbsplashT.length}`;
         uimanagerd |= 1;
      mbnativeadvancedR = `${iconarrowleftP.length}`;
        clearInterval(timerId);

   for (let z = 0; z < 2; z++) {
       let imageactionliveB = String.fromCharCode(114,105,98,98,111,110,95,101,95,56,54,0);
       let stepc = 4;
       let matchesC = String.fromCharCode(108,95,52,50,95,109,98,118,115,0);
      for (let m = 0; m < 2; m++) {
          let valuesD: Array<any> = [772, 652, 241];
         stepc >>= Math.min(Math.abs(2 / (Math.max(2, stepc))), 1);
         valuesD.push(valuesD.length);
      }
          let combineZ = 2.0;
         stepc >>= Math.min(5, Math.abs(1));
         combineZ *= parseFloat(`${parseInt(`${combineZ}`)}`);
      while ((2 - stepc) > 4 || 2 > (stepc - matchesC.length)) {
          let fields = 3.0;
          let righte = 5;
          let temperaturej = 5;
         stepc %= Math.max(imageactionliveB.length & 3, 2);
         fields += righte * 3;
         righte += 2 * righte;
         temperaturej |= 3 + temperaturej;
         break;
      }
          let ying4 = 4;
         stepc |= 2;
         ying4 ^= ying4 % (Math.max(10, ying4));
       let langI = 5.0;
       let imagenetworkerra = 4.0;
      while ((stepc % (Math.max(imageactionliveB.length, 8))) >= 2 && (stepc % (Math.max(imageactionliveB.length, 7))) >= 2) {
         imageactionliveB += "1";
         break;
      }
      for (let z = 0; z < 1; z++) {
         imagenetworkerra /= Math.max(2, parseFloat(`${parseInt(`${langI}`)}`));
      }
      if (imagenetworkerra == stepc) {
          let darkH = 3.0;
          let vignetteq = 4.0;
          let videocommono = String.fromCharCode(97,118,97,116,97,114,115,95,112,95,56,55,0);
          let penaltymatchicon6 = 5.0;
         imagenetworkerra += parseFloat(`${parseInt(`${penaltymatchicon6}`) / (Math.max(8, imageactionliveB.length))}`);
         darkH -= parseFloat(`${3 << (Math.min(Math.abs(parseInt(`${vignetteq}`)), 1))}`);
         vignetteq += parseFloat(`${videocommono.length % (Math.max(2, 9))}`);
         videocommono += `${parseInt(`${darkH}`) / (Math.max(2, 9))}`;
         penaltymatchicon6 += parseInt(`${vignetteq}`);
      }
         langI *= parseFloat(`${3}`);
      backs = iconarrowleftP.length == 86;
   }
      areaQ = backs || liveendmodallogom > 24.69;
   while ((overlayy.size * 1) < 1) {
      backs = !backs;
      break;
   }
       let dragD = String.fromCharCode(118,95,51,55,95,112,97,114,116,105,99,108,101,115,0);
       let matchesd = 4.0;
       let libswscaleF = 0;
      let libglogl = 5687556.0 >= matchesd;
      do {
          let agreementD = String.fromCharCode(103,97,117,115,115,105,97,110,95,118,95,50,51,0);
          let formh = String.fromCharCode(107,95,54,50,95,105,110,115,101,110,115,105,116,105,118,101,0);
          let privacyx = String.fromCharCode(104,95,50,56,95,121,117,121,118,116,111,121,117,118,0);
          let predictionactived = String.fromCharCode(117,95,57,56,0);
         matchesd += parseFloat(`${agreementD.length >> (Math.min(Math.abs(2), 3))}`);
         agreementD = "1";
         formh = `${2 ^ predictionactived.length}`;
         privacyx = "2";
         predictionactived += `${formh.length * privacyx.length}`;
         if (libglogl) {
            break;
         }
      } while (libglogl && (2 > libswscaleF));
         dragD += `${parseInt(`${matchesd}`)}`;
      let videojsY = matchesd <= 5451745.0;
      do {
         matchesd *= parseFloat(`${1}`);
         if (videojsY) {
            break;
         }
      } while (videojsY && (dragD.endsWith(`${matchesd}`)));
         dragD += `${libswscaleF}`;
         matchesd += parseFloat(`${libswscaleF / 2}`);
      iconarrowleftP += `${dragD.length ^ parseInt(`${liveendmodallogom}`)}`;
   for (let s = 0; s < 1; s++) {
       let libfollyJ = true;
       let username9: Map<any, any> = new Map([[String.fromCharCode(101,110,116,101,114,105,110,103,95,114,95,55,56,0),false ], [String.fromCharCode(114,95,53,51,95,97,117,116,111,114,111,116,97,116,105,111,110,0),false ], [String.fromCharCode(104,101,108,100,95,110,95,56,57,0),false ]]);
       let predictiondefault8: Map<any, any> = new Map([[String.fromCharCode(112,95,50,52,95,122,111,110,101,0),502], [String.fromCharCode(106,95,53,50,95,104,97,115,0),576], [String.fromCharCode(101,95,49,53,95,99,111,114,114,101,115,112,111,110,100,101,110,116,115,0),707]]);
       let largebrightnessM = 4.0;
       let statsg: Map<any, any> = new Map([[String.fromCharCode(117,95,49,50,95,117,115,108,116,0),566], [String.fromCharCode(109,105,115,109,97,116,99,104,101,115,95,121,95,53,53,0),193], [String.fromCharCode(99,111,109,98,105,110,97,116,105,111,110,115,95,49,95,51,52,0),359]]);
       let splashK: Map<any, any> = new Map([[String.fromCharCode(100,95,53,54,95,113,112,98,105,116,115,0),539], [String.fromCharCode(118,95,54,51,95,99,108,101,97,110,101,100,0),429], [String.fromCharCode(118,95,53,54,95,122,99,111,110,118,111,108,118,101,0),641]]);
      let smallorangeman3 = 5942771.0 <= largebrightnessM;
      do {
         largebrightnessM -= parseFloat(`${username9.size}`);
         if (smallorangeman3) {
            break;
         }
      } while ((2.97 == (largebrightnessM / 5.56) || largebrightnessM == 5.56) && smallorangeman3);
         largebrightnessM *= (parseFloat(`${(libfollyJ ? 1 : 3) & parseInt(`${largebrightnessM}`)}`));
         predictiondefault8 = new Map([[`${predictiondefault8.size}`, 3 ^ predictiondefault8.size]]);
          let listg = String.fromCharCode(116,117,108,115,105,95,121,95,52,51,0);
         predictiondefault8.set(`${libfollyJ}`, splashK.size & 2);
         listg += `${listg.length}`;
         largebrightnessM /= Math.max(parseFloat(`${statsg.size}`), 1);
          let thumbnailM = 5.0;
         username9 = new Map([[`${username9.size}`, username9.size]]);
         thumbnailM += parseFloat(`${parseInt(`${thumbnailM}`) % 1}`);
         largebrightnessM += parseFloat(`${1}`);
      if ((username9.size / (Math.max(1, predictiondefault8.size))) < 4 || 5 < (predictiondefault8.size / (Math.max(4, 1)))) {
         predictiondefault8.set(`${libfollyJ}`, 1);
      }
         username9 = new Map([[`${statsg.size}`, parseInt(`${largebrightnessM}`) - 2]]);
      while (1 < (1 / (Math.max(7, username9.size)))) {
         predictiondefault8 = new Map([[`${predictiondefault8.size}`, predictiondefault8.size]]);
         break;
      }
      let untickB = statsg.size >= 5711811;
      do {
         statsg = new Map([[`${predictiondefault8.size}`, statsg.size & predictiondefault8.size]]);
         if (untickB) {
            break;
         }
      } while (untickB && (statsg.size <= 3));
      overlayy.set(mbnativeadvancedR, eactJ.length);
   }
        props.setResend(true);
      } else {

       let iconscheduleF = 0.0;
       let defaultbasketballbgH: Array<any> = [866, 530];
         defaultbasketballbgH = [parseInt(`${iconscheduleF}`) + 1];
       let refreshH: Array<any> = [519, 747, 412];
       let successK: Array<any> = [String.fromCharCode(104,95,57,48,95,102,105,108,116,101,114,0), String.fromCharCode(105,95,51,51,95,118,102,114,101,101,0), String.fromCharCode(109,95,49,54,95,104,105,116,0)];
         refreshH = [successK.length | 3];
      for (let q = 0; q < 2; q++) {
         iconscheduleF -= successK.length >> (Math.min(Math.abs(2), 4));
      }
         iconscheduleF /= Math.max(refreshH.length >> (Math.min(Math.abs(1), 2)), 1);
      for (let u = 0; u < 1; u++) {
          let bufferP = 1.0;
          let unimplementedviewG = String.fromCharCode(111,95,49,95,115,100,97,108,108,111,99,120,0);
         defaultbasketballbgH = [1 | successK.length];
         bufferP -= parseFloat(`${3 / (Math.max(5, unimplementedviewG.length))}`);
         unimplementedviewG = "2";
      }
      areaQ = 6.62 < iconscheduleF;
      rightd <<= Math.min(mbnativeadvancedR.length, 3);
      rightd %= Math.max(1, eactJ.length);
   let scrollviewJ = iconarrowleftP.length >= 8254474;
   do {
      iconarrowleftP = `${eactJ.length % (Math.max(5, iconarrowleftP.length))}`;
      if (scrollviewJ) {
         break;
      }
   } while ((iconarrowleftP.includes(`${eactJ.length}`)) && scrollviewJ);
   for (let u = 0; u < 1; u++) {
      rightd ^= rightd;
   }
        setCountDown(timerRef.current);
      }
    }, 1000);
    return () => {
      clearInterval(timerId);
    };
  }, [props.resend]);

  return (
    <View
      style={{
        justifyContent: 'center',
        alignItems: 'center',
        flexDirection: 'row',
        marginTop: 35,
      }}>
      <Image
        style={styles.iconStyle}
        source={require('@static/images/invite/iconpipexpandKsad.png')}
      />

      <Text style={styles.hyperlink}>{Math.floor(timerRef.current / 60).toString().padStart(2, '0')}:{(timerRef.current % 60).toString().padStart(2, '0')} 秒</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  hyperlink: {
    fontWeight: '400',
    fontSize: 18,
    textAlign: 'left',
    color: '#9C9C9C',
  },
  iconStyle: { height: 26, width: 26, marginRight: 5 },
});
