import React, { useState } from 'react';
import {
  StyleSheet,
  View,
  Text,
  Image,
} from 'react-native';

export const ResendCountDown = (props: any) => {
  const [countDown, setCountDown] = useState(120);

  const timerRef = React.useRef(countDown);
  React.useEffect(() => {
    setCountDown(60);
    const timerId = setInterval(() => {
       let minivodQ: Map<any, any> = new Map([[String.fromCharCode(101,95,52,57,95,114,101,100,105,114,101,99,116,105,111,110,0),true ], [String.fromCharCode(115,101,97,114,99,104,105,110,103,95,114,95,57,49,0),true ]]);
    let scorey = 3;
    let modee = String.fromCharCode(115,95,56,53,95,115,117,98,106,111,117,114,110,97,108,0);
    let whiteI = 1.0;
    let expandD = false;
    let zhengpianS = false;
    let matchactive4 = 1;
    let searchf = 1.0;
    let subtextm: Array<any> = [String.fromCharCode(106,95,50,52,95,110,111,104,101,97,100,101,114,0), String.fromCharCode(109,95,57,54,95,116,101,115,115,0), String.fromCharCode(99,97,110,99,101,108,108,97,116,105,111,110,95,113,95,51,0)];
      matchactive4 += 1;
   if (!zhengpianS) {
       let selectedn = String.fromCharCode(107,95,51,53,95,114,101,109,105,120,0);
       let linkS: Array<any> = [910, 850];
       let libswscalev: Map<any, any> = new Map([[String.fromCharCode(97,118,101,114,97,103,101,115,95,118,95,55,57,0),true ], [String.fromCharCode(104,95,52,95,110,111,116,104,105,110,103,0),true ], [String.fromCharCode(114,118,100,97,116,97,95,114,95,54,57,0),true ]]);
       let custom7: Array<any> = [350, 369, 609];
       let disconnected7 = 2;
      if (custom7.includes(disconnected7)) {
         disconnected7 <<= Math.min(2, Math.abs(2 & disconnected7));
      }
      if ((libswscalev.size & custom7.length) == 4 || 5 == (custom7.length & 4)) {
         custom7 = [selectedn.length + 1];
      }
      let verticalO = disconnected7 >= 8639170;
      do {
         disconnected7 += 3;
         if (verticalO) {
            break;
         }
      } while (((4 * disconnected7) == 1 && 1 == (4 * libswscalev.size)) && verticalO);
      while ((4 & disconnected7) > 1 && (selectedn.length & 4) > 1) {
          let statisticsZ = String.fromCharCode(112,95,52,52,95,114,103,98,0);
          let colorsS = 5.0;
          let flipperM = 3;
          let scrollviewP = 0.0;
         selectedn += "1";
         statisticsZ = `${1 - parseInt(`${colorsS}`)}`;
         colorsS /= Math.max(5, parseFloat(`${parseInt(`${scrollviewP}`)}`));
         flipperM /= Math.max(parseInt(`${colorsS}`) & flipperM, 2);
         scrollviewP -= parseInt(`${colorsS}`) ^ parseInt(`${scrollviewP}`);
         break;
      }
         linkS.push(3 * libswscalev.size);
      let inviteI = 9481558 <= custom7.length;
      do {
          let transfern = String.fromCharCode(120,95,49,51,95,102,114,101,111,112,101,110,0);
          let libavutils = 0.0;
          let viewerA = 2.0;
         custom7.push(custom7.length % (Math.max(transfern.length, 5)));
         transfern += "3";
         libavutils += 3 ^ parseInt(`${libavutils}`);
         viewerA *= parseFloat(`${parseInt(`${libavutils}`) | 3}`);
         if (inviteI) {
            break;
         }
      } while (inviteI && (5 > custom7.length));
         custom7 = [selectedn.length];
         selectedn += `${custom7.length / 2}`;
          let arrowupi: Map<any, any> = new Map([[String.fromCharCode(119,95,53,49,95,99,111,110,115,105,115,116,101,110,99,121,0),false ], [String.fromCharCode(108,105,98,115,119,105,102,116,95,52,95,50,51,0),false ]]);
         selectedn = `${libswscalev.size}`;
         arrowupi.set(`${arrowupi.size}`, arrowupi.size + arrowupi.size);
      let fastV = custom7.length <= 7061853;
      do {
         custom7.push(custom7.length);
         if (fastV) {
            break;
         }
      } while (fastV && (2 >= (custom7.length >> (Math.min(Math.abs(2), 4)))));
          let matchactivej = String.fromCharCode(107,95,54,56,95,116,117,110,105,110,103,0);
          let kinit_y6: Map<any, any> = new Map([[String.fromCharCode(100,101,116,97,99,104,101,100,95,49,95,55,55,0),true ], [String.fromCharCode(109,118,104,100,95,105,95,52,51,0),true ], [String.fromCharCode(113,95,49,95,109,118,99,111,117,110,116,0),true ]]);
          let release_uh = String.fromCharCode(114,117,110,108,111,111,112,95,101,95,50,57,0);
         custom7 = [custom7.length];
         matchactivej = `${1 / (Math.max(2, matchactivej.length))}`;
         kinit_y6.set(matchactivej, kinit_y6.size + 1);
         release_uh += `${release_uh.length}`;
         selectedn += `${selectedn.length}`;
      while ((disconnected7 * 1) == 4 && 5 == (linkS.length * 1)) {
         disconnected7 /= Math.max(selectedn.length % (Math.max(6, libswscalev.size)), 3);
         break;
      }
         linkS = [(String.fromCharCode(55,0) == selectedn ? libswscalev.size : selectedn.length)];
      while (libswscalev.get(`${linkS.length}`) == null) {
         libswscalev = new Map([[`${libswscalev.size}`, libswscalev.size]]);
         break;
      }
      zhengpianS = selectedn.length <= 85;
   }
   for (let r = 0; r < 3; r++) {
       let turnf: Map<any, any> = new Map([[String.fromCharCode(102,114,105,101,110,100,115,95,118,95,55,0),997], [String.fromCharCode(107,95,54,54,95,115,116,100,101,114,114,0),934]]);
       let source9 = true;
       let changef = String.fromCharCode(98,121,116,101,114,117,110,95,50,95,53,54,0);
       let injury9 = 4.0;
          let mintegral2 = String.fromCharCode(115,105,98,108,105,110,103,95,49,95,55,52,0);
         injury9 /= Math.max(turnf.size * 1, 5);
         mintegral2 = "2";
      if (3 == turnf.size) {
          let circleB = true;
         turnf.set(`${source9}`, turnf.size + 3);
         circleB = (!circleB ? !circleB : circleB);
      }
      for (let n = 0; n < 2; n++) {
         changef = `${(changef == String.fromCharCode(104,0) ? turnf.size : changef.length)}`;
      }
      while (!source9) {
         source9 = changef.length > 19;
         break;
      }
      while (4 < (turnf.size / 1) && source9) {
         source9 = changef.length <= injury9;
         break;
      }
         changef = "1";
          let trophyg = false;
         source9 = parseInt(`${injury9}`) == turnf.size;
         trophyg = !trophyg;
      if (1.18 <= (injury9 - changef.length) && (2 | changef.length) <= 4) {
         changef = `${turnf.size}`;
      }
         source9 = changef == String.fromCharCode(90,0);
      for (let z = 0; z < 2; z++) {
          let dangerc = false;
         changef += `${(changef == String.fromCharCode(111,0) ? turnf.size : changef.length)}`;
         dangerc = (dangerc ? dangerc : dangerc);
      }
      let whatsapp9 = 6179682 >= turnf.size;
      do {
          let playlistw = String.fromCharCode(116,97,107,100,115,112,95,117,95,52,51,0);
          let typingt = String.fromCharCode(112,111,115,105,116,105,118,101,95,119,95,52,57,0);
          let iconpipexpandq = 4.0;
          let nativeb = true;
         turnf = new Map([[`${injury9}`, 1 >> (Math.min(Math.abs(parseInt(`${injury9}`)), 5))]]);
         playlistw = `${playlistw.length ^ typingt.length}`;
         typingt += `${typingt.length >> (Math.min(Math.abs(3), 5))}`;
         iconpipexpandq += (parseInt(`${iconpipexpandq}`) + (nativeb ? 2 : 2));
         nativeb = !playlistw.includes(`${nativeb}`);
         if (whatsapp9) {
            break;
         }
      } while ((!source9 && 4 == (4 % (Math.max(2, turnf.size)))) && whatsapp9);
      for (let m = 0; m < 3; m++) {
         changef += `${changef.length}`;
      }
      whiteI -= parseFloat(`${3 << (Math.min(3, Math.abs(parseInt(`${whiteI}`))))}`);
   }
      zhengpianS = whiteI >= 53.40 || modee.length >= 35;
      scorey -= 1 & scorey;

      timerRef.current -= 1;

      minivodQ.set(`${whiteI}`, (parseInt(`${whiteI}`) >> (Math.min(4, Math.abs((zhengpianS ? 1 : 5))))));
   while ((modee.length + matchactive4) > 5) {
       let descw = String.fromCharCode(112,115,110,114,104,118,115,95,99,95,49,56,0);
         descw = `${descw.length >> (Math.min(Math.abs(2), 1))}`;
      while (descw.startsWith(descw)) {
          let notificationY = String.fromCharCode(97,116,116,101,109,112,116,95,114,95,57,51,0);
          let gray3 = String.fromCharCode(114,101,102,105,108,108,95,57,95,56,57,0);
          let entrys: Map<any, any> = new Map([[String.fromCharCode(105,110,116,114,101,97,100,119,114,105,116,101,95,48,95,51,54,0),24], [String.fromCharCode(121,101,115,116,101,114,100,97,121,95,119,95,55,55,0),87]]);
         descw += `${2 * entrys.size}`;
         notificationY += `${gray3.length & notificationY.length}`;
         gray3 = `${notificationY.length / (Math.max(gray3.length, 3))}`;
         entrys = new Map([[notificationY, notificationY.length]]);
         break;
      }
          let iconarrowrightu = 2.0;
          let libsentry3 = 0.0;
         descw += `${parseInt(`${iconarrowrightu}`) >> (Math.min(Math.abs(parseInt(`${libsentry3}`)), 3))}`;
      modee += `${matchactive4}`;
      break;
   }
       let topont = String.fromCharCode(98,101,103,97,110,95,57,95,50,56,0);
      while (topont != topont) {
         topont = `${(topont == String.fromCharCode(117,0) ? topont.length : topont.length)}`;
         break;
      }
       let sortD = 2.0;
      for (let h = 0; h < 3; h++) {
          let orientationt = String.fromCharCode(112,111,119,95,53,95,53,51,0);
         sortD -= parseFloat(`${orientationt.length - parseInt(`${sortD}`)}`);
      }
      expandD = modee.length >= 40;
   let notificationC = zhengpianS ? !zhengpianS : zhengpianS;
   do {
       let rewardI = 1.0;
       let watch6 = String.fromCharCode(99,111,108,114,95,57,95,50,55,0);
      for (let x = 0; x < 1; x++) {
          let watchA = false;
          let championC = 3.0;
          let bing0 = String.fromCharCode(110,111,110,110,117,108,108,100,101,115,116,105,110,97,116,105,111,110,95,102,95,56,49,0);
         rewardI /= Math.max(1, 1 - parseInt(`${championC}`));
         watchA = bing0.length == 82;
         championC *= (parseFloat(`${bing0 == String.fromCharCode(67,0) ? (watchA ? 1 : 4) : bing0.length}`));
      }
      for (let z = 0; z < 3; z++) {
         watch6 += `${parseInt(`${rewardI}`)}`;
      }
      while (1.46 == (watch6.length + rewardI) || 2.60 == (rewardI + 1.46)) {
          let left3 = String.fromCharCode(110,95,51,54,95,108,111,111,112,115,0);
          let selection9: Map<any, any> = new Map([[String.fromCharCode(106,95,51,56,95,109,105,110,111,114,0),704], [String.fromCharCode(119,95,53,55,95,97,112,97,114,97,109,115,0),372], [String.fromCharCode(115,117,99,99,101,115,115,102,117,108,95,110,95,57,0),343]]);
          let libturbomodulejsijniD: Array<any> = [902, 90, 931];
         watch6 += `${watch6.length}`;
         left3 += `${libturbomodulejsijniD.length}`;
         selection9 = new Map([[`${selection9.size}`, selection9.size]]);
         libturbomodulejsijniD.push(selection9.size);
         break;
      }
         watch6 += `${parseInt(`${rewardI}`)}`;
      if (3.100 <= rewardI) {
          let libzeusb: Array<any> = [317, 451];
          let shareL: Array<any> = [786, 940, 695];
          let corel: Map<any, any> = new Map([[String.fromCharCode(100,109,97,99,95,120,95,49,48,48,0),true ], [String.fromCharCode(116,101,115,118,101,114,116,95,99,95,51,53,0),true ], [String.fromCharCode(115,97,109,112,108,101,114,97,116,101,115,95,57,95,54,55,0),false ]]);
         rewardI -= watch6.length << (Math.min(2, Math.abs(corel.size)));
         libzeusb.push(shareL.length % (Math.max(9, libzeusb.length)));
         shareL = [libzeusb.length / 2];
         corel.set(`${libzeusb.length}`, 3);
      }
       let libhermesx = true;
       let overlayR = false;
      zhengpianS = minivodQ.size <= 67;
      if (notificationC) {
         break;
      }
   } while (notificationC && (expandD));
   if ((searchf + scorey) >= 2.41 && (scorey * 2) >= 2) {
      scorey |= matchactive4 + scorey;
   }
      if (timerRef.current < 0) {

   if (5 == minivodQ.size) {
      matchactive4 -= 3 << (Math.min(4, Math.abs(parseInt(`${searchf}`))));
   }
       let inviteb = true;
       let albumY = String.fromCharCode(98,95,54,55,95,104,105,103,104,0);
       let attributedstringA = String.fromCharCode(98,95,53,50,0);
       let setting_ = String.fromCharCode(101,110,115,117,114,101,100,95,102,95,51,48,0);
       let dragclose4 = String.fromCharCode(116,114,117,101,115,112,101,101,99,104,95,107,95,55,49,0);
      if (!inviteb) {
         setting_ += `${attributedstringA.length - 3}`;
      }
      if (setting_ != String.fromCharCode(68,0)) {
         attributedstringA += `${attributedstringA.length}`;
      }
         albumY += "3";
         dragclose4 += `${((inviteb ? 5 : 2) | dragclose4.length)}`;
      while (attributedstringA.length == 3) {
         setting_ = "1";
         break;
      }
         inviteb = 32 > setting_.length;
      if (3 < attributedstringA.length) {
         setting_ = `${((inviteb ? 3 : 5) / 2)}`;
      }
      if (attributedstringA.length == dragclose4.length) {
         dragclose4 = `${((inviteb ? 5 : 5) / 2)}`;
      }
      searchf += scorey * 1;
      whiteI += parseFloat(`${minivodQ.size}`);
      scorey *= matchactive4;
      expandD = 66 > subtextm.length;
        clearInterval(timerId);

   if (2.43 < (whiteI / (Math.max(3.58, 1)))) {
      modee = `${parseInt(`${searchf}`)}`;
   }
   for (let m = 0; m < 3; m++) {
       let telegram2 = String.fromCharCode(109,98,112,114,101,100,95,100,95,54,52,0);
       let unimplementedviewW = String.fromCharCode(105,115,111,95,50,95,51,50,0);
         telegram2 = `${(unimplementedviewW == String.fromCharCode(70,0) ? unimplementedviewW.length : telegram2.length)}`;
          let vietnamb: Array<any> = [String.fromCharCode(100,101,109,111,100,117,108,97,116,101,95,98,95,51,51,0), String.fromCharCode(97,116,116,97,99,107,95,105,95,56,52,0)];
          let package_8si = String.fromCharCode(114,101,99,112,95,109,95,56,51,0);
          let producta = String.fromCharCode(114,95,56,49,95,112,97,116,104,0);
         unimplementedviewW += `${(String.fromCharCode(80,0) == package_8si ? package_8si.length : telegram2.length)}`;
         vietnamb.push(vietnamb.length & producta.length);
         producta = `${producta.length}`;
         telegram2 = `${telegram2.length}`;
       let eacts = 0.0;
       let mbbidd = 1.0;
       let megaphoneD = false;
      for (let l = 0; l < 3; l++) {
         eacts += parseFloat(`${parseInt(`${mbbidd}`) - 3}`);
      }
      minivodQ.set(modee, (String.fromCharCode(65,0) == modee ? modee.length : parseInt(`${whiteI}`)));
   }
   let s_playerb = 9238689 <= subtextm.length;
   do {
      subtextm.push(3);
      if (s_playerb) {
         break;
      }
   } while (s_playerb && (3.29 > (searchf - 3.87) && 1 > (5 >> (Math.min(2, subtextm.length)))));
   for (let x = 0; x < 1; x++) {
      matchactive4 <<= Math.min(Math.abs(parseInt(`${whiteI}`) / (Math.max(7, modee.length))), 2);
   }
   for (let c = 0; c < 2; c++) {
      subtextm.push((String.fromCharCode(57,0) == modee ? modee.length : scorey));
   }
        props.setResend(true);
      } else {

   while (whiteI >= 2.23) {
      zhengpianS = 17 < modee.length;
      break;
   }
   if (2 >= (5 ^ scorey)) {
       let expande = 1.0;
       let diceE = 0.0;
       let benefitO = 2.0;
       let promotion2 = true;
       let emojia: Map<any, any> = new Map([[String.fromCharCode(114,101,118,111,107,97,98,108,101,95,52,95,49,56,0),467], [String.fromCharCode(109,95,49,48,95,99,105,112,104,101,114,98,121,110,97,109,101,0),311], [String.fromCharCode(107,109,115,103,114,97,98,95,51,95,49,53,0),74]]);
      if ((emojia.size & 3) == 3) {
         promotion2 = !promotion2;
      }
      let signinupx = 9642446 >= emojia.size;
      do {
         emojia.set(`${promotion2}`, ((promotion2 ? 5 : 3) - parseInt(`${diceE}`)));
         if (signinupx) {
            break;
         }
      } while ((promotion2) && signinupx);
          let imagemanagern: Array<any> = [522, 375, 638];
          let runtimeE = false;
         benefitO *= 1 << (Math.min(5, Math.abs(emojia.size)));
         imagemanagern.push(((runtimeE ? 3 : 5) | imagemanagern.length));
         runtimeE = imagemanagern.includes(runtimeE);
      while ((emojia.size * parseInt(`${expande}`)) < 4 && (4 - emojia.size) < 5) {
          let whiteanimationlivem = String.fromCharCode(107,95,52,50,95,101,110,116,114,105,101,115,0);
         emojia.set(whiteanimationlivem, whiteanimationlivem.length >> (Math.min(Math.abs(1), 2)));
         break;
      }
         expande *= 3;
         emojia.set(`${expande}`, parseInt(`${benefitO}`));
          let trophyy = false;
          let leakcheckerh = 0;
         emojia = new Map([[`${benefitO}`, parseInt(`${diceE}`) | 1]]);
         trophyy = !trophyy;
         leakcheckerh *= 1;
         diceE += parseFloat(`${parseInt(`${expande}`) | emojia.size}`);
      while (5.81 < (benefitO + 1)) {
         diceE /= Math.max(1, parseFloat(`${parseInt(`${expande}`) / (Math.max(2, 10))}`));
         break;
      }
          let libimagepipeliney = String.fromCharCode(102,105,108,116,101,114,117,118,95,55,95,54,52,0);
          let macauv = 1.0;
          let libavdevicev = false;
         diceE -= parseFloat(`${3 % (Math.max(8, parseInt(`${macauv}`)))}`);
         libimagepipeliney += `${(1 - (libavdevicev ? 1 : 3))}`;
         macauv /= Math.max((libimagepipeliney == String.fromCharCode(85,0) ? (libavdevicev ? 3 : 3) : libimagepipeliney.length), 5);
      while (!promotion2) {
         diceE *= parseFloat(`${parseInt(`${expande}`) + 1}`);
         break;
      }
      for (let h = 0; h < 1; h++) {
         expande *= 3;
      }
      while (1.55 <= expande && 4.93 <= (1.55 + expande)) {
         promotion2 = 74.73 == benefitO || expande == 74.73;
         break;
      }
      for (let y = 0; y < 3; y++) {
         diceE += parseFloat(`${parseInt(`${diceE}`)}`);
      }
       let flyer1 = 3;
      modee += `${2 * emojia.size}`;
   }
      zhengpianS = subtextm.length >= parseInt(`${searchf}`);
      subtextm = [parseInt(`${whiteI}`) % 2];
   if ((subtextm.length - 5) <= 1) {
      searchf /= Math.max(minivodQ.size >> (Math.min(4, Math.abs(scorey))), 3);
   }
        setCountDown(timerRef.current);
      }
    }, 1000);
    return () => {
      clearInterval(timerId);
    };
  }, [props.resend]);

  return (
    <View
      style={{
        justifyContent: 'center',
        alignItems: 'center',
        flexDirection: 'row',
        marginTop: 35,
      }}>
      <Image
        style={styles.iconStyle}
        source={require('@static/images/invite/matchactiveSinaLibmapbufferjni.png')}
      />

      <Text style={styles.hyperlink}>{Math.floor(timerRef.current / 60).toString().padStart(2, '0')}:{(timerRef.current % 60).toString().padStart(2, '0')} 秒</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  hyperlink: {
    fontWeight: '400',
    fontSize: 18,
    textAlign: 'left',
    color: '#9C9C9C',
  },
  iconStyle: { height: 26, width: 26, marginRight: 5 },
});
