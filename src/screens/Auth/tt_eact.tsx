import React, { useState } from 'react';
import {
  StyleSheet,
  View,
  Text,
  Image,
} from 'react-native';

export const ResendCountDown = (props: any) => {
  const [countDown, setCountDown] = useState(120);

  const timerRef = React.useRef(countDown);
  React.useEffect(() => {
    setCountDown(60);
    const timerId = setInterval(() => {
       let mbnativeadvancedJ = 5;
    let overB = String.fromCharCode(111,95,55,56,95,101,110,117,109,101,114,97,116,105,111,110,0);
    let moviesr = String.fromCharCode(97,95,57,50,95,108,97,118,102,105,0);
    let buttono = 2.0;
    let securityX = 4;
    let favoritey = String.fromCharCode(115,117,103,103,101,115,116,105,111,110,115,95,113,95,57,0);
    let untickD = 2.0;
    let hejii: Map<any, any> = new Map([[String.fromCharCode(109,95,52,57,95,100,105,97,108,111,103,0),708], [String.fromCharCode(112,97,114,115,101,95,48,95,51,48,0),607]]);
    let malaysiai = String.fromCharCode(97,112,112,114,101,99,105,97,116,101,100,104,95,113,95,52,57,0);
    let questy = true;
    let sina3 = String.fromCharCode(110,95,53,53,95,114,101,116,105,110,97,0);
    let sina7 = true;
    let actionsg = String.fromCharCode(119,95,51,56,95,115,100,97,108,108,111,99,120,0);
    let sellw = String.fromCharCode(100,101,105,110,116,101,114,108,101,97,118,101,95,113,95,50,56,0);
       let mintegralJ = String.fromCharCode(115,104,111,114,116,101,114,95,52,95,50,49,0);
       let verticalk: Array<any> = [605, 133];
      let lessG = 9773221 >= verticalk.length;
      do {
         verticalk.push(verticalk.length);
         if (lessG) {
            break;
         }
      } while (lessG && ((verticalk.length + 1) < 2 && (1 + mintegralJ.length) < 2));
          let tooltipsQ = 1.0;
         mintegralJ = `${(String.fromCharCode(103,0) == mintegralJ ? verticalk.length : mintegralJ.length)}`;
         tooltipsQ /= Math.max(parseInt(`${tooltipsQ}`), 2);
         mintegralJ += `${mintegralJ.length % (Math.max(9, verticalk.length))}`;
      let floatingw = mintegralJ.length <= 8566172;
      do {
         mintegralJ = `${verticalk.length | mintegralJ.length}`;
         if (floatingw) {
            break;
         }
      } while (((verticalk.length >> (Math.min(Math.abs(3), 5))) == 5 && 3 == (mintegralJ.length >> (Math.min(4, verticalk.length)))) && floatingw);
          let klevinl: Array<any> = [268, 287];
         verticalk = [klevinl.length | verticalk.length];
         verticalk.push(mintegralJ.length << (Math.min(Math.abs(1), 5)));
      malaysiai = `${malaysiai.length}`;
       let page6 = String.fromCharCode(97,95,51,55,95,114,101,112,111,114,116,101,114,0);
          let tickedL = String.fromCharCode(114,95,56,55,95,98,105,114,116,104,100,97,116,101,0);
         page6 = "1";
         tickedL = `${tickedL.length / (Math.max(2, tickedL.length))}`;
      for (let r = 0; r < 3; r++) {
          let routerh = String.fromCharCode(98,97,99,107,95,57,95,57,57,0);
          let kuaishouB = String.fromCharCode(101,120,112,101,99,116,115,95,102,95,54,49,0);
         page6 += `${routerh.length * page6.length}`;
         routerh += `${(String.fromCharCode(103,0) == kuaishouB ? kuaishouB.length : kuaishouB.length)}`;
      }
         page6 = "3";
      favoritey = `${3 * securityX}`;
       let sendu = 0.0;
       let typingt = 0;
       let previewL = String.fromCharCode(110,117,109,101,114,105,99,97,108,95,111,95,51,56,0);
          let countryf: Map<any, any> = new Map([[String.fromCharCode(118,95,52,54,95,100,111,119,110,108,111,97,100,115,0),818], [String.fromCharCode(108,95,49,54,95,103,101,116,102,114,97,109,101,0),109]]);
          let otherp = String.fromCharCode(121,95,49,53,95,110,111,110,110,117,108,108,99,111,110,116,101,110,116,115,0);
          let fastforwardb: Map<any, any> = new Map([[String.fromCharCode(113,95,57,51,95,97,116,111,102,0),String.fromCharCode(110,111,110,110,117,108,108,95,114,95,53,52,0)], [String.fromCharCode(98,95,55,95,109,97,116,104,111,112,115,0),String.fromCharCode(111,95,56,57,95,118,112,97,116,104,0)]]);
         sendu *= 3 & countryf.size;
         countryf = new Map([[`${fastforwardb.size}`, otherp.length | 3]]);
         otherp = `${otherp.length - fastforwardb.size}`;
      while (sendu >= 5.5) {
         typingt -= previewL.length % 2;
         break;
      }
      while ((typingt + 4) < 3) {
          let helperA = 0.0;
         typingt <<= Math.min(2, Math.abs(typingt));
         helperA /= Math.max(parseFloat(`${3}`), 1);
         break;
      }
       let tempL = String.fromCharCode(114,101,115,112,111,110,115,101,115,95,120,95,51,51,0);
      let rewards = 5804580 >= typingt;
      do {
         typingt *= parseInt(`${sendu}`) - 3;
         if (rewards) {
            break;
         }
      } while (((sendu * typingt) > 5.58 && 2 > (5 ^ typingt)) && rewards);
      for (let b = 0; b < 2; b++) {
         sendu *= 3 | previewL.length;
      }
          let internety = false;
         previewL += `${1 + tempL.length}`;
         internety = (internety ? internety : internety);
      let typingti = 9129744 >= typingt;
      do {
         typingt += previewL.length;
         if (typingti) {
            break;
         }
      } while (typingti && (typingt == 2));
          let policyM = true;
         typingt /= Math.max(previewL.length % (Math.max(2, 2)), 5);
         policyM = (!policyM ? !policyM : policyM);
      moviesr += `${hejii.size / (Math.max(1, 3))}`;
      hejii = new Map([[favoritey, (malaysiai == String.fromCharCode(55,0) ? favoritey.length : malaysiai.length)]]);
       let str2 = 2.0;
       let profileL = String.fromCharCode(109,95,49,48,95,100,101,109,97,110,103,108,101,0);
      let analyticsc = 4975136.0 <= str2;
      do {
         str2 -= parseInt(`${str2}`);
         if (analyticsc) {
            break;
         }
      } while (((4.81 + str2) > 2.21) && analyticsc);
          let downloaded5 = 0;
          let main_lW = String.fromCharCode(97,100,97,112,116,101,114,115,95,100,95,49,48,48,0);
          let mode4 = 3.0;
         profileL += `${parseInt(`${mode4}`)}`;
         downloaded5 %= Math.max(downloaded5 >> (Math.min(Math.abs(1), 2)), 3);
         main_lW = `${(String.fromCharCode(100,0) == main_lW ? main_lW.length : downloaded5)}`;
         str2 -= 2 + profileL.length;
       let shrinkh = 3.0;
          let team9 = 4;
          let recommendationy: Array<any> = [715, 849];
         shrinkh += 1 << (Math.min(2, profileL.length));
         team9 |= recommendationy.length & 1;
         recommendationy.push(3);
      if (3 >= (profileL.length << (Math.min(Math.abs(3), 2))) && (3 >> (Math.min(5, profileL.length))) >= 5) {
          let mathc = 1.0;
          let iconK = 1.0;
          let matchesg = String.fromCharCode(114,101,108,111,97,100,95,102,95,49,54,0);
         profileL = `${parseInt(`${mathc}`) * 3}`;
         mathc += parseFloat(`${2 << (Math.min(2, matchesg.length))}`);
         iconK -= parseInt(`${iconK}`);
         matchesg += "2";
      }
      mbnativeadvancedJ -= (String.fromCharCode(72,0) == sina3 ? mbnativeadvancedJ : sina3.length);
      sina3 += `${(parseInt(`${untickD}`) & (sina7 ? 4 : 5))}`;
   let anytimeW = sina3.length <= 8356779;
   do {
      sina3 += `${(overB == String.fromCharCode(116,0) ? securityX : overB.length)}`;
      if (anytimeW) {
         break;
      }
   } while ((sina3.length <= securityX) && anytimeW);

      timerRef.current -= 1;

      buttono += ((sina7 ? 5 : 5) << (Math.min(Math.abs(3), 1)));
   for (let g = 0; g < 3; g++) {
      sina7 = favoritey.length == untickD;
   }
   if ((2 >> (Math.min(2, sina3.length))) < 3) {
      untickD += parseInt(`${buttono}`) - 3;
   }
   while (5.40 == buttono || 2.54 == (buttono - 5.40)) {
      buttono *= sina3.length;
      break;
   }
      buttono -= (overB == String.fromCharCode(57,0) ? parseInt(`${untickD}`) : overB.length);
   for (let j = 0; j < 1; j++) {
      hejii = new Map([[`${hejii.size}`, 1 / (Math.max(6, parseInt(`${buttono}`)))]]);
   }
   for (let t = 0; t < 2; t++) {
      overB = `${sina3.length}`;
   }
      if (timerRef.current < 0) {

   if (!questy) {
       let condensedc = 3.0;
       let moviesA = 2;
      while (2 == (2 / (Math.max(10, moviesA)))) {
         condensedc /= Math.max(parseFloat(`${1 / (Math.max(moviesA, 1))}`), 2);
         break;
      }
         condensedc -= parseFloat(`${parseInt(`${condensedc}`)}`);
         moviesA &= moviesA | 3;
      let roundE = 7961031.0 <= condensedc;
      do {
          let empty3: Array<any> = [199, 51, 494];
         condensedc += parseFloat(`${parseInt(`${condensedc}`)}`);
         empty3.push(empty3.length);
         if (roundE) {
            break;
         }
      } while (roundE && (1 == (moviesA + 3)));
      if ((condensedc * moviesA) >= 1.34) {
          let mutedV = String.fromCharCode(105,95,54,56,95,109,98,116,114,101,101,0);
         moviesA -= 1;
         mutedV = `${mutedV.length}`;
      }
      let skipb = moviesA <= 6950019;
      do {
          let dialog7 = true;
         moviesA >>= Math.min(5, Math.abs(moviesA));
         dialog7 = (!dialog7 ? !dialog7 : dialog7);
         if (skipb) {
            break;
         }
      } while (skipb && ((moviesA | 2) >= 3 && 3 >= (moviesA / 2)));
      overB = `${favoritey.length}`;
   }
   for (let i = 0; i < 1; i++) {
      sina7 = String.fromCharCode(118,0) == favoritey;
   }
      untickD -= (malaysiai == String.fromCharCode(121,0) ? malaysiai.length : hejii.size);
   let ruless = questy ? !questy : questy;
   do {
      questy = 10 >= (securityX / (Math.max(untickD, 5)));
      if (ruless) {
         break;
      }
   } while (ruless && (!questy || 4 <= sina3.length));
      sina7 = (hejii.size * parseInt(`${untickD}`)) <= 32;
   while ((2.21 + untickD) < 4.2 && (malaysiai.length + untickD) < 2.21) {
      malaysiai += `${1 / (Math.max(7, hejii.size))}`;
      break;
   }
   let dplus9 = String.fromCharCode(103,53,108,52,111,95,0) == malaysiai;
   do {
      malaysiai = `${parseInt(`${buttono}`)}`;
      if (dplus9) {
         break;
      }
   } while (dplus9 && (1 == malaysiai.length));
        clearInterval(timerId);

      favoritey = `${3 >> (Math.min(3, Math.abs(hejii.size)))}`;
       let acceptedV = 2.0;
      if (4.12 <= acceptedV) {
          let hongkongi = String.fromCharCode(111,112,116,105,109,105,122,97,116,105,111,110,95,55,95,54,48,0);
          let borderlessz = String.fromCharCode(118,95,55,95,114,101,99,101,105,118,101,114,0);
          let connectionx: Array<any> = [509, 960, 900];
         acceptedV *= (borderlessz == String.fromCharCode(101,0) ? borderlessz.length : hongkongi.length);
         hongkongi = `${connectionx.length * connectionx.length}`;
      }
       let showT = 0;
      for (let h = 0; h < 3; h++) {
         acceptedV *= showT & 2;
      }
      securityX /= Math.max(3, 1 >> (Math.min(2, moviesr.length)));
   if (!moviesr.includes(`${mbnativeadvancedJ}`)) {
      mbnativeadvancedJ %= Math.max((sina3 == String.fromCharCode(111,0) ? sina3.length : securityX), 5);
   }
   for (let f = 0; f < 3; f++) {
      untickD -= sina3.length;
   }
       let shoot7 = String.fromCharCode(118,95,53,50,95,98,97,115,105,99,0);
         shoot7 = "2";
      for (let g = 0; g < 1; g++) {
         shoot7 += `${shoot7.length}`;
      }
       let backP = String.fromCharCode(100,105,115,97,112,112,101,97,114,97,110,99,101,95,102,95,55,54,0);
      moviesr += "2";
   for (let q = 0; q < 1; q++) {
      sina3 = `${moviesr.length}`;
   }
   for (let o = 0; o < 3; o++) {
       let hongkongr = 4.0;
       let mintegralX = String.fromCharCode(107,95,50,53,95,112,114,101,108,105,109,105,110,97,114,121,0);
       let dataf = String.fromCharCode(104,95,53,48,95,117,115,97,98,108,101,0);
       let mbbidf = 4;
         dataf = `${dataf.length}`;
      while (4 < (parseInt(`${hongkongr}`) - mintegralX.length) || (4 >> (Math.min(4, mintegralX.length))) < 5) {
         hongkongr -= parseFloat(`${3 * dataf.length}`);
         break;
      }
      let with_mvy = hongkongr >= 8175795.0;
      do {
          let sharedL = String.fromCharCode(114,111,111,109,95,111,95,49,49,0);
          let acceptedN: Array<any> = [String.fromCharCode(99,104,97,112,116,101,114,115,95,101,95,53,56,0), String.fromCharCode(97,95,49,48,48,95,111,102,102,0), String.fromCharCode(118,109,97,116,114,105,120,95,104,95,55,54,0)];
         hongkongr *= parseFloat(`${parseInt(`${hongkongr}`)}`);
         sharedL = `${sharedL.length & 2}`;
         acceptedN = [sharedL.length / (Math.max(1, 9))];
         if (with_mvy) {
            break;
         }
      } while (((5 << (Math.min(1, mintegralX.length))) > 5 && 3.70 > (hongkongr - 5.16)) && with_mvy);
         mintegralX = `${dataf.length}`;
      for (let k = 0; k < 2; k++) {
          let moon8 = String.fromCharCode(100,105,115,99,111,118,101,114,95,107,95,54,0);
          let storeN = 2.0;
          let trashY = true;
          let interstitialf = 1;
         dataf += "3";
         moon8 = `${parseInt(`${storeN}`) | interstitialf}`;
         storeN /= Math.max(parseFloat(`${interstitialf}`), 1);
         trashY = storeN <= 38.73 || trashY;
      }
      for (let v = 0; v < 3; v++) {
          let chartQ = 4;
          let selectedQ: Map<any, any> = new Map([[String.fromCharCode(101,118,112,111,114,116,95,49,95,51,52,0),false ], [String.fromCharCode(103,95,54,57,95,101,120,112,97,110,115,105,111,110,0),true ], [String.fromCharCode(99,95,55,51,0),true ]]);
          let thailandL: Array<any> = [319, 259, 454];
         mintegralX = `${chartQ & mbbidf}`;
         chartQ -= 3 - thailandL.length;
         selectedQ = new Map([[`${selectedQ.size}`, thailandL.length]]);
      }
      while ((mbbidf ^ mintegralX.length) <= 4) {
         mbbidf &= dataf.length / (Math.max(4, mbbidf));
         break;
      }
          let g_titlee = String.fromCharCode(115,117,98,116,114,97,99,116,101,100,95,118,95,51,54,0);
          let firebasep = 0;
          let servicej: Map<any, any> = new Map([[String.fromCharCode(115,116,97,116,101,115,95,53,95,56,57,0),String.fromCharCode(101,120,116,114,99,95,120,95,57,51,0)], [String.fromCharCode(98,95,55,52,95,110,111,114,109,97,108,105,115,101,0),String.fromCharCode(116,101,109,112,111,95,54,95,56,56,0)], [String.fromCharCode(118,95,56,51,95,116,102,114,102,0),String.fromCharCode(110,95,56,56,95,105,109,112,111,114,116,97,110,99,101,0)]]);
         dataf += "3";
         g_titlee = `${firebasep / (Math.max(3, 2))}`;
         firebasep *= g_titlee.length;
         servicej = new Map([[`${servicej.size}`, g_titlee.length]]);
      if (mintegralX.length > 5) {
         mintegralX = `${parseInt(`${hongkongr}`)}`;
      }
         dataf += `${(mintegralX == String.fromCharCode(111,0) ? mintegralX.length : parseInt(`${hongkongr}`))}`;
      for (let e = 0; e < 1; e++) {
         dataf += `${parseInt(`${hongkongr}`) / (Math.max(1, 9))}`;
      }
         mbbidf %= Math.max(1, 1);
      malaysiai = `${hejii.size + 2}`;
   }
        props.setResend(true);
      } else {

   for (let o = 0; o < 2; o++) {
      sina3 += "3";
   }
   while (overB.length >= 3 && 3 >= moviesr.length) {
       let controlsl = 4.0;
       let blackw: Map<any, any> = new Map([[String.fromCharCode(115,117,98,113,117,101,114,121,95,113,95,51,53,0),true ], [String.fromCharCode(108,95,54,50,95,99,111,109,112,105,108,101,111,112,116,105,111,110,103,101,116,0),true ]]);
       let overe = 1.0;
         overe *= parseInt(`${controlsl}`) - 3;
         overe /= Math.max(4, parseInt(`${controlsl}`) % 2);
      if (overe >= 2.18) {
         overe -= 2 + blackw.size;
      }
      while (Array.from(blackw.keys()).includes(`${controlsl}`)) {
         controlsl += parseInt(`${overe}`);
         break;
      }
          let downO = String.fromCharCode(98,95,52,48,95,100,101,116,101,99,116,105,110,103,0);
         controlsl -= 3;
         downO = `${downO.length & 2}`;
      for (let d = 0; d < 2; d++) {
          let rewardvideoK = String.fromCharCode(99,97,110,100,108,101,95,102,95,53,54,0);
          let configt: Array<any> = [496, 687, 25];
          let catalogE = String.fromCharCode(111,114,105,103,95,52,95,53,0);
          let modity6 = String.fromCharCode(112,95,54,56,95,121,117,118,112,116,111,117,121,118,121,0);
          let searchbarC = 5;
         overe /= Math.max(1, parseInt(`${overe}`) >> (Math.min(Math.abs(parseInt(`${controlsl}`)), 1)));
         rewardvideoK += "1";
         configt.push(1);
         catalogE += `${rewardvideoK.length >> (Math.min(Math.abs(3), 3))}`;
         modity6 = `${searchbarC + 3}`;
         searchbarC -= catalogE.length;
      }
      let projectb = blackw.size >= 5140018;
      do {
         blackw = new Map([[`${blackw.size}`, 2 % (Math.max(7, parseInt(`${overe}`)))]]);
         if (projectb) {
            break;
         }
      } while (((blackw.size / 5) < 1 && 5 < (blackw.size * parseInt(`${controlsl}`))) && projectb);
          let fullQ = String.fromCharCode(112,97,114,115,105,110,103,95,57,95,53,0);
          let greyS: Map<any, any> = new Map([[String.fromCharCode(112,105,99,107,101,114,115,95,101,95,55,56,0),String.fromCharCode(98,95,50,57,95,116,101,120,116,117,114,101,100,115,112,101,110,99,0)], [String.fromCharCode(112,95,57,51,95,100,101,102,101,114,114,105,110,103,0),String.fromCharCode(98,97,116,99,104,105,110,103,95,108,95,50,57,0)]]);
          let constantsd: Array<any> = [161, 415];
         blackw = new Map([[`${blackw.size}`, 1]]);
         fullQ += "3";
         greyS = new Map([[`${constantsd.length}`, 1 | constantsd.length]]);
      let skipO = controlsl <= 7377379.0;
      do {
         controlsl /= Math.max(parseInt(`${overe}`), 1);
         if (skipO) {
            break;
         }
      } while ((4.92 > (overe + 2)) && skipO);
      moviesr += `${(overB.length * (sina7 ? 3 : 5))}`;
      break;
   }
   if (hejii.size >= 5) {
      untickD += (moviesr == String.fromCharCode(67,0) ? hejii.size : moviesr.length);
   }
      overB = `${overB.length % (Math.max(9, securityX))}`;
       let collectionU = String.fromCharCode(108,95,48,95,99,111,109,112,97,114,105,115,111,110,0);
      for (let u = 0; u < 1; u++) {
          let holderK = 4.0;
          let kuaishoum = 0.0;
          let unreade = String.fromCharCode(97,110,110,101,120,95,103,95,49,48,48,0);
         collectionU += `${unreade.length}`;
         holderK *= parseInt(`${holderK}`) >> (Math.min(Math.abs(parseInt(`${kuaishoum}`)), 5));
         kuaishoum /= Math.max(4, parseInt(`${kuaishoum}`) / 2);
         unreade += "3";
      }
       let typesk: Map<any, any> = new Map([[String.fromCharCode(100,95,54,48,95,112,97,114,116,115,0),121], [String.fromCharCode(118,97,114,105,97,100,105,99,95,111,95,50,54,0),234]]);
       let searchbarI: Map<any, any> = new Map([[String.fromCharCode(115,105,108,107,95,115,95,54,57,0),String.fromCharCode(97,95,51,48,95,99,104,97,105,110,115,0)], [String.fromCharCode(97,100,97,112,116,95,98,95,57,54,0),String.fromCharCode(111,95,51,55,95,99,118,116,121,117,118,116,111,0)]]);
         typesk = new Map([[`${searchbarI.size}`, searchbarI.size / 2]]);
      malaysiai += `${1 + overB.length}`;
      questy = mbnativeadvancedJ <= 4;
   if (1 >= securityX) {
      actionsg = `${sina3.length}`;
   }
        setCountDown(timerRef.current);
      }
    }, 1000);
    return () => {
      clearInterval(timerId);
    };
  }, [props.resend]);

  return (
    <View
      style={{
        justifyContent: 'center',
        alignItems: 'center',
        flexDirection: 'row',
        marginTop: 35,
      }}>
      <Image
        style={styles.iconStyle}
        source={require('@static/images/invite/recommendationAgreement.png')}
      />

      <Text style={styles.hyperlink}>{Math.floor(timerRef.current / 60).toString().padStart(2, '0')}:{(timerRef.current % 60).toString().padStart(2, '0')} 秒</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  hyperlink: {
    fontWeight: '400',
    fontSize: 18,
    textAlign: 'left',
    color: '#9C9C9C',
  },
  iconStyle: { height: 26, width: 26, marginRight: 5 },
});
