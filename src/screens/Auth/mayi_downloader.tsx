import React, { useState } from 'react';
import {
  StyleSheet,
  View,
  Text,
  Image,
} from 'react-native';

export const ResendCountDown = (props: any) => {
  const [countDown, setCountDown] = useState(120);

  const timerRef = React.useRef(countDown);
  React.useEffect(() => {
    setCountDown(60);
    const timerId = setInterval(() => {
       let mbbannerM = String.fromCharCode(118,98,101,122,105,101,114,95,109,95,53,48,0);
    let activei = true;
    let mapbufferW: Map<any, any> = new Map([[String.fromCharCode(112,101,114,102,111,114,109,101,100,95,101,95,53,54,0),617], [String.fromCharCode(104,119,114,97,110,100,95,54,95,57,50,0),354]]);
    let traminiv = String.fromCharCode(105,100,101,110,116,105,102,105,99,97,116,105,111,110,95,105,95,52,0);
    let arrowupI = 0;
    let librrc_ = 2.0;
    let temperaturea = String.fromCharCode(117,95,51,54,95,112,105,110,99,104,0);
    let yellowtored_ = String.fromCharCode(120,104,116,109,108,95,98,95,53,48,0);
    let type_a6F: Map<any, any> = new Map([[String.fromCharCode(113,117,101,117,101,115,95,52,95,55,54,0),238], [String.fromCharCode(108,95,52,56,0),794]]);
      arrowupI ^= mapbufferW.size / (Math.max(8, yellowtored_.length));
   while (2 < (arrowupI - 2) || !activei) {
      activei = arrowupI > 6;
      break;
   }
       let upgrade2 = 3;
       let untickn: Map<any, any> = new Map([[String.fromCharCode(104,95,52,52,95,97,115,111,108,117,116,101,0),894], [String.fromCharCode(101,95,50,51,95,97,99,114,111,115,115,102,97,100,101,0),386]]);
         untickn.set(`${upgrade2}`, 2);
      let exampleimageD = 9493660 >= upgrade2;
      do {
         upgrade2 &= untickn.size;
         if (exampleimageD) {
            break;
         }
      } while ((3 > upgrade2) && exampleimageD);
          let sliderV: Map<any, any> = new Map([[String.fromCharCode(110,98,105,116,115,95,106,95,55,48,0),false ], [String.fromCharCode(122,95,54,50,95,110,111,110,98,108,111,99,107,105,110,103,0),false ], [String.fromCharCode(113,95,51,50,95,112,117,114,103,101,0),false ]]);
         untickn = new Map([[`${sliderV.size}`, 2 ^ untickn.size]]);
          let userh = String.fromCharCode(101,112,108,121,95,122,95,50,56,0);
         upgrade2 -= userh.length / 2;
          let libjsi5 = true;
          let graphn = String.fromCharCode(105,103,104,108,105,103,104,116,115,95,119,95,52,56,0);
          let phones: Array<any> = [853, 131];
         upgrade2 &= graphn.length & upgrade2;
         libjsi5 = libjsi5 && phones.length < 81;
         graphn += `${((libjsi5 ? 2 : 2) % (Math.max(phones.length, 6)))}`;
         untickn.set(`${upgrade2}`, upgrade2 | 1);
      librrc_ += (String.fromCharCode(81,0) == temperaturea ? temperaturea.length : upgrade2);
   for (let f = 0; f < 2; f++) {
      traminiv += `${3 >> (Math.min(5, mbbannerM.length))}`;
   }
   if (mbbannerM.startsWith(yellowtored_)) {
      yellowtored_ += `${2 >> (Math.min(5, mbbannerM.length))}`;
   }

      timerRef.current -= 1;

      activei = (mapbufferW.size & mbbannerM.length) >= 52;
   for (let b = 0; b < 1; b++) {
      arrowupI >>= Math.min(4, Math.abs(traminiv.length - mapbufferW.size));
   }
       let basketballtrophyN = String.fromCharCode(99,111,114,114,101,115,112,111,110,100,101,110,116,115,95,117,95,50,56,0);
      while (basketballtrophyN.length <= 4 && basketballtrophyN != String.fromCharCode(120,0)) {
         basketballtrophyN = `${(String.fromCharCode(81,0) == basketballtrophyN ? basketballtrophyN.length : basketballtrophyN.length)}`;
         break;
      }
         basketballtrophyN = `${(basketballtrophyN == String.fromCharCode(86,0) ? basketballtrophyN.length : basketballtrophyN.length)}`;
         basketballtrophyN = `${basketballtrophyN.length}`;
      mapbufferW.set(`${activei}`, basketballtrophyN.length);
      mapbufferW = new Map([[`${mapbufferW.size}`, parseInt(`${librrc_}`)]]);
   let description_8vZ = mbbannerM == String.fromCharCode(103,117,102,97,116,112,100,120,54,0);
   do {
       let sportsV = 5.0;
       let sharewhiteJ = String.fromCharCode(116,95,56,48,95,99,111,110,115,105,115,116,101,110,99,121,0);
       let predictionbannershared5 = 0.0;
       let reactnavigationm: Array<any> = [525, 52, 212];
       let iconorientationW = String.fromCharCode(115,116,114,105,115,116,97,114,116,95,100,95,57,55,0);
      for (let d = 0; d < 3; d++) {
         predictionbannershared5 += iconorientationW.length;
      }
      while (3 > (1 + reactnavigationm.length) || (1 + iconorientationW.length) > 5) {
         iconorientationW += `${parseInt(`${predictionbannershared5}`) / (Math.max(1, parseInt(`${sportsV}`)))}`;
         break;
      }
      while (reactnavigationm.length < parseInt(`${sportsV}`)) {
         reactnavigationm.push(parseInt(`${sportsV}`));
         break;
      }
      while (parseInt(`${predictionbannershared5}`) <= iconorientationW.length) {
         iconorientationW += `${iconorientationW.length}`;
         break;
      }
       let footballfield9 = 3.0;
      if (reactnavigationm.length < footballfield9) {
          let targett = 3.0;
          let popupl = String.fromCharCode(98,115,101,114,118,101,114,95,102,95,55,0);
          let nendd: Map<any, any> = new Map([[String.fromCharCode(98,108,111,99,107,115,105,122,101,95,106,95,57,54,0),true ], [String.fromCharCode(115,97,109,112,108,101,114,101,100,117,99,116,105,111,110,95,98,95,52,51,0),false ], [String.fromCharCode(117,95,51,57,95,105,112,111,100,0),true ]]);
          let constantsh = String.fromCharCode(108,95,51,53,95,110,111,116,0);
          let indicatorU = 3.0;
         footballfield9 /= Math.max(1 + nendd.size, 2);
         targett -= parseFloat(`${parseInt(`${indicatorU}`) ^ parseInt(`${targett}`)}`);
         popupl = `${constantsh.length}`;
         nendd.set(popupl, popupl.length << (Math.min(3, constantsh.length)));
         indicatorU -= 2;
      }
         predictionbannershared5 -= parseInt(`${predictionbannershared5}`);
          let readD = String.fromCharCode(117,95,52,48,95,104,111,114,105,103,0);
          let iconrightorangeN = String.fromCharCode(117,95,57,57,95,102,105,108,116,101,114,117,118,0);
         reactnavigationm.push(2 >> (Math.min(1, Math.abs(parseInt(`${footballfield9}`)))));
         readD += "2";
         iconrightorangeN = `${iconrightorangeN.length}`;
      for (let g = 0; g < 1; g++) {
         footballfield9 += reactnavigationm.length;
      }
          let routerx: Map<any, any> = new Map([[String.fromCharCode(115,98,114,100,115,112,95,105,95,56,51,0),340], [String.fromCharCode(107,95,51,51,0),904]]);
          let splashD = String.fromCharCode(101,95,49,54,95,100,113,99,111,101,102,102,0);
          let fastforwardW = 2.0;
         predictionbannershared5 *= 1 % (Math.max(8, parseInt(`${fastforwardW}`)));
         routerx = new Map([[`${routerx.size}`, routerx.size >> (Math.min(splashD.length, 2))]]);
         splashD = `${splashD.length}`;
         fastforwardW -= parseFloat(`${splashD.length % (Math.max(9, routerx.size))}`);
      let shrinkP = 7541229.0 >= predictionbannershared5;
      do {
         predictionbannershared5 += sharewhiteJ.length * parseInt(`${predictionbannershared5}`);
         if (shrinkP) {
            break;
         }
      } while ((sharewhiteJ.length > 3) && shrinkP);
      while (sportsV <= 4.29) {
          let ewardedb = 3.0;
          let auto_6gt = 5;
         footballfield9 += iconorientationW.length;
         ewardedb -= 2;
         auto_6gt *= auto_6gt ^ 2;
         break;
      }
          let trashy = String.fromCharCode(116,104,114,101,115,104,111,108,100,105,110,103,95,97,95,57,57,0);
          let rootk: Map<any, any> = new Map([[String.fromCharCode(99,101,114,116,105,102,105,99,97,116,101,115,95,115,95,50,50,0),561], [String.fromCharCode(110,95,51,55,95,100,101,118,112,111,108,108,0),808]]);
          let issubD = 3.0;
         sharewhiteJ += "2";
         trashy += "2";
         rootk.set(`${issubD}`, parseInt(`${issubD}`));
      while (predictionbannershared5 < 2.45) {
         predictionbannershared5 /= Math.max(4, sharewhiteJ.length);
         break;
      }
         sharewhiteJ = `${parseInt(`${sportsV}`)}`;
      mbbannerM = `${parseInt(`${librrc_}`)}`;
      if (description_8vZ) {
         break;
      }
   } while (description_8vZ && (!activei));
      if (timerRef.current < 0) {

   if (4.82 < (temperaturea.length - librrc_) && 3 < (parseInt(`${librrc_}`) - 2)) {
       let fields = String.fromCharCode(109,111,116,105,111,110,112,105,120,101,108,115,95,52,95,54,0);
       let values3 = 1.0;
       let rewardvideoX = 5.0;
      while (rewardvideoX <= 4.52) {
         rewardvideoX += parseInt(`${values3}`);
         break;
      }
      for (let c = 0; c < 1; c++) {
         values3 *= parseInt(`${rewardvideoX}`);
      }
      let uimanager0 = 5885733.0 <= values3;
      do {
         values3 /= Math.max(2, fields.length);
         if (uimanager0) {
            break;
         }
      } while (uimanager0 && (4.14 >= (values3 + rewardvideoX)));
         fields = `${(fields == String.fromCharCode(76,0) ? fields.length : parseInt(`${values3}`))}`;
          let guide5: Array<any> = [303, 182];
          let fullU = String.fromCharCode(112,95,56,53,95,117,116,118,105,100,101,111,100,115,112,0);
         rewardvideoX -= 3;
         guide5.push(1);
         fullU = `${fullU.length}`;
      let streamingH = values3 >= 6235508.0;
      do {
          let libimagepipelinew = 3.0;
          let chatm = String.fromCharCode(114,95,52,95,105,110,105,116,97,99,107,0);
         values3 /= Math.max(1 * parseInt(`${libimagepipelinew}`), 3);
         libimagepipelinew -= 1 << (Math.min(3, chatm.length));
         chatm += `${2 ^ chatm.length}`;
         if (streamingH) {
            break;
         }
      } while (((5.37 + values3) >= 4.76) && streamingH);
      while ((rewardvideoX - values3) <= 4.96) {
         values3 -= fields.length * 3;
         break;
      }
      if ((1.83 * rewardvideoX) > 5.12) {
         rewardvideoX -= 3;
      }
      for (let r = 0; r < 1; r++) {
         values3 += parseInt(`${rewardvideoX}`);
      }
      temperaturea += `${yellowtored_.length / 3}`;
   }
       let progress6 = 5.0;
       let armvaV = String.fromCharCode(115,99,114,101,97,109,95,113,95,56,52,0);
       let update_q8Y = String.fromCharCode(116,95,53,57,95,99,97,110,116,0);
         progress6 -= armvaV.length << (Math.min(Math.abs(3), 3));
          let dangerq: Map<any, any> = new Map([[String.fromCharCode(115,95,54,95,110,116,101,114,102,97,99,101,0),807], [String.fromCharCode(99,97,110,100,105,100,97,116,101,115,95,114,95,55,55,0),414]]);
         update_q8Y += `${armvaV.length ^ 2}`;
         dangerq.set(`${dangerq.size}`, dangerq.size);
      let iconbackwhite9 = String.fromCharCode(48,104,55,99,95,48,55,122,97,0) == armvaV;
      do {
          let modulesi = 2.0;
          let libjsiJ = String.fromCharCode(116,111,103,103,108,101,95,118,95,56,0);
          let cornerkickU = false;
          let termsU = 5.0;
          let yellowcirclebgn = 2.0;
         armvaV = `${libjsiJ.length - 1}`;
         modulesi += parseInt(`${yellowcirclebgn}`);
         libjsiJ += `${parseInt(`${termsU}`) % (Math.max(parseInt(`${yellowcirclebgn}`), 9))}`;
         cornerkickU = 77.29 <= modulesi;
         termsU -= parseInt(`${yellowcirclebgn}`) ^ 1;
         if (iconbackwhite9) {
            break;
         }
      } while (iconbackwhite9 && (3 > armvaV.length));
      while (update_q8Y.includes(`${armvaV.length}`)) {
         update_q8Y += `${armvaV.length}`;
         break;
      }
         update_q8Y += `${1 | update_q8Y.length}`;
      let selectedE = String.fromCharCode(112,101,121,55,0) == armvaV;
      do {
         armvaV += "3";
         if (selectedE) {
            break;
         }
      } while (selectedE && (progress6 > 4.38));
      let orangee = armvaV.length >= 8903302;
      do {
         armvaV = `${armvaV.length + parseInt(`${progress6}`)}`;
         if (orangee) {
            break;
         }
      } while (orangee && (4 > armvaV.length));
         armvaV += `${armvaV.length | 3}`;
          let blacklistK = 1.0;
          let fastY = 5.0;
          let orientationg = 5.0;
         update_q8Y = `${update_q8Y.length}`;
         blacklistK += parseFloat(`${3}`);
         fastY += parseInt(`${blacklistK}`);
         orientationg += parseFloat(`${parseInt(`${orientationg}`) % 3}`);
      librrc_ *= 1;
      mapbufferW = new Map([[traminiv, arrowupI]]);
   if ((arrowupI * traminiv.length) >= 1 || 1 >= (arrowupI * traminiv.length)) {
       let register_kO = 2;
       let redirectu: Array<any> = [215, 692, 912];
       let audienceA = 4.0;
       let nalytics8: Array<any> = [String.fromCharCode(116,104,114,111,119,105,110,103,95,118,95,56,49,0), String.fromCharCode(101,120,97,109,112,108,101,95,109,95,53,56,0), String.fromCharCode(112,95,55,95,116,101,108,108,97,114,0)];
       let fieldsW: Array<any> = [String.fromCharCode(109,111,100,97,108,108,121,95,111,95,53,51,0), String.fromCharCode(102,99,116,108,95,120,95,54,54,0)];
         register_kO &= nalytics8.length >> (Math.min(4, Math.abs(register_kO)));
      for (let v = 0; v < 3; v++) {
         redirectu.push(nalytics8.length | 2);
      }
         redirectu.push(redirectu.length);
      while (redirectu.includes(audienceA)) {
         audienceA += nalytics8.length;
         break;
      }
      if (4 <= (register_kO & 5)) {
         redirectu = [register_kO << (Math.min(1, Math.abs(3)))];
      }
      for (let l = 0; l < 2; l++) {
         register_kO <<= Math.min(nalytics8.length, 5);
      }
      if (nalytics8.includes(audienceA)) {
         nalytics8.push(2);
      }
      while (nalytics8.length == parseInt(`${audienceA}`)) {
         nalytics8 = [fieldsW.length];
         break;
      }
      traminiv += `${mbbannerM.length << (Math.min(5, Math.abs(parseInt(`${audienceA}`))))}`;
   }
   for (let o = 0; o < 2; o++) {
      arrowupI += traminiv.length;
   }
        clearInterval(timerId);

       let downloadZ = 0.0;
       let viewerL: Array<any> = [95, 487, 263];
       let untickq = String.fromCharCode(99,95,52,95,109,111,100,109,0);
          let modalm = String.fromCharCode(100,105,118,105,100,111,114,95,108,95,54,51,0);
          let whitevideoliveh = 3;
          let eactI = 4.0;
         viewerL.push(1);
         modalm = "2";
         whitevideoliveh += whitevideoliveh * parseInt(`${eactI}`);
         eactI += parseFloat(`${modalm.length >> (Math.min(Math.abs(1), 1))}`);
          let register_ok_ = 1.0;
         viewerL.push(untickq.length);
         register_ok_ /= Math.max(parseInt(`${register_ok_}`), 5);
      if ((untickq.length / 3) >= 3) {
         viewerL = [(untickq == String.fromCharCode(53,0) ? untickq.length : viewerL.length)];
      }
          let playg = false;
          let b_center5 = 2.0;
         viewerL = [untickq.length >> (Math.min(1, viewerL.length))];
         playg = 85.66 <= b_center5;
         b_center5 /= Math.max(1, (parseInt(`${b_center5}`) ^ (playg ? 5 : 2)));
          let refreshK: Array<any> = [166, 923, 541];
          let memberC = String.fromCharCode(105,110,116,101,110,116,105,111,110,95,55,95,48,0);
          let orangeuparrow2 = 5.0;
         viewerL = [viewerL.length % 3];
         refreshK.push(memberC.length - 1);
         memberC += `${memberC.length | 2}`;
         orangeuparrow2 *= parseFloat(`${memberC.length - 1}`);
         downloadZ -= parseFloat(`${2}`);
      for (let h = 0; h < 1; h++) {
          let tooltipsi = String.fromCharCode(105,95,56,95,103,97,116,104,101,114,105,110,103,0);
          let videovary = false;
         untickq = `${untickq.length}`;
         tooltipsi += `${((videovary ? 2 : 3))}`;
         videovary = !tooltipsi.endsWith(`${videovary}`);
      }
          let tempnodataO = String.fromCharCode(112,95,55,53,95,100,101,99,111,100,101,102,0);
         downloadZ += parseFloat(`${viewerL.length}`);
         tempnodataO += `${tempnodataO.length}`;
      for (let p = 0; p < 2; p++) {
         viewerL = [viewerL.length << (Math.min(Math.abs(1), 1))];
      }
      librrc_ -= parseInt(`${downloadZ}`) / (Math.max(viewerL.length, 6));
   let reportC = 7504973 <= mbbannerM.length;
   do {
      mbbannerM += `${((activei ? 4 : 5))}`;
      if (reportC) {
         break;
      }
   } while (reportC && (mbbannerM.startsWith(`${activei}`)));
   while (5 >= (mapbufferW.size % 3) || 2 >= (3 % (Math.max(5, temperaturea.length)))) {
       let librrch = 1.0;
       let iconuser4: Array<any> = [844, 600, 929];
      let vietnamn = iconuser4.length <= 8858535;
      do {
         iconuser4 = [parseInt(`${librrch}`) / 2];
         if (vietnamn) {
            break;
         }
      } while ((4.72 == (3.59 + librrch) || (iconuser4.length * 5) == 5) && vietnamn);
       let libruntimeexecutorm: Array<any> = [774, 938, 183];
       let result0: Array<any> = [703, 77];
          let shirt5: Array<any> = [445, 189];
         iconuser4 = [2 ^ libruntimeexecutorm.length];
         shirt5.push(shirt5.length | shirt5.length);
       let codegen5 = 2;
         iconuser4.push(parseInt(`${librrch}`));
      if (2 <= (codegen5 / 1) || (codegen5 / (Math.max(1, 1))) <= 2) {
         codegen5 <<= Math.min(Math.abs(libruntimeexecutorm.length - 1), 2);
      }
      temperaturea += `${traminiv.length ^ mapbufferW.size}`;
      break;
   }
       let progress8: Array<any> = [615, 377, 563];
       let iconqqk = 5;
       let mbbid4 = String.fromCharCode(118,95,50,55,95,97,108,98,117,109,115,0);
      if ((iconqqk / (Math.max(2, 1))) >= 1 && 4 >= (iconqqk / (Math.max(2, 8)))) {
          let pangleK = String.fromCharCode(120,95,56,54,95,97,100,97,112,116,105,118,101,0);
          let homef = 2.0;
          let sliderW = String.fromCharCode(115,116,105,108,108,95,120,95,57,51,0);
         mbbid4 += `${sliderW.length + 3}`;
         pangleK = `${3 >> (Math.min(3, pangleK.length))}`;
         homef /= Math.max(parseFloat(`${pangleK.length & 1}`), 4);
         sliderW += `${parseInt(`${homef}`) + pangleK.length}`;
      }
      while (!mbbid4.startsWith(`${iconqqk}`)) {
         iconqqk += mbbid4.length | 3;
         break;
      }
          let logoutl = String.fromCharCode(112,95,56,52,95,115,116,121,108,101,115,0);
          let dependenciesZ = 1.0;
          let bgvipsporty = String.fromCharCode(110,95,49,55,95,109,97,100,101,0);
         iconqqk &= (bgvipsporty == String.fromCharCode(99,0) ? bgvipsporty.length : iconqqk);
         logoutl += `${parseInt(`${dependenciesZ}`) ^ logoutl.length}`;
         dependenciesZ /= Math.max(2, logoutl.length);
      for (let g = 0; g < 3; g++) {
          let hongkonge = String.fromCharCode(114,95,49,57,95,97,100,118,97,110,99,101,109,101,110,116,0);
          let rankc = false;
          let forwardB: Array<any> = [885, 36, 929];
         iconqqk %= Math.max(1, (hongkonge == String.fromCharCode(66,0) ? hongkonge.length : (rankc ? 5 : 5)));
         rankc = 69 > forwardB.length;
      }
         mbbid4 += `${iconqqk - progress8.length}`;
          let reactB: Array<any> = [190, 864];
         mbbid4 = `${reactB.length % (Math.max(mbbid4.length, 9))}`;
      for (let s = 0; s < 1; s++) {
          let previewJ: Map<any, any> = new Map([[String.fromCharCode(118,105,122,105,101,114,95,116,95,53,49,0),824], [String.fromCharCode(112,111,115,116,101,114,95,114,95,53,0),94], [String.fromCharCode(108,95,57,56,95,101,110,116,105,114,101,0),137]]);
         mbbid4 += `${iconqqk << (Math.min(Math.abs(1), 2))}`;
         previewJ.set(`${previewJ.size}`, previewJ.size);
      }
      while (iconqqk < 2) {
         iconqqk *= iconqqk;
         break;
      }
      let sourceK = 8065034 >= mbbid4.length;
      do {
          let holderj = String.fromCharCode(102,97,108,115,101,95,107,95,49,49,0);
          let searchbari: Array<any> = [178, 243];
          let type_ijJ = 4.0;
          let sortp = 4.0;
         mbbid4 = `${parseInt(`${sortp}`)}`;
         holderj = `${1 + parseInt(`${type_ijJ}`)}`;
         searchbari = [3];
         type_ijJ += searchbari.length;
         sortp += parseFloat(`${searchbari.length * 2}`);
         if (sourceK) {
            break;
         }
      } while ((mbbid4.startsWith(`${progress8.length}`)) && sourceK);
      arrowupI <<= Math.min(2, Math.abs(temperaturea.length % (Math.max(9, iconqqk))));
   if (mbbannerM != String.fromCharCode(66,0)) {
      yellowtored_ = `${traminiv.length}`;
   }
        props.setResend(true);
      } else {

   while (mbbannerM.startsWith(`${activei}`)) {
      mbbannerM += "2";
      break;
   }
      librrc_ *= mbbannerM.length / 2;
       let inouttargetredX = String.fromCharCode(103,95,52,55,95,105,110,115,101,116,0);
       let icondefaultthumbnailr: Map<any, any> = new Map([[String.fromCharCode(119,95,52,95,115,101,116,117,112,0),false ], [String.fromCharCode(100,95,50,53,95,109,112,101,103,116,115,0),true ], [String.fromCharCode(105,110,118,105,116,101,114,95,119,95,56,48,0),false ]]);
      while (!inouttargetredX.startsWith(`${icondefaultthumbnailr.size}`)) {
          let tooltipsk = false;
         icondefaultthumbnailr.set(`${inouttargetredX}`, (inouttargetredX == String.fromCharCode(67,0) ? icondefaultthumbnailr.size : inouttargetredX.length));
         tooltipsk = !tooltipsk;
         break;
      }
         inouttargetredX = `${2 | inouttargetredX.length}`;
      let list7 = 7276712 >= icondefaultthumbnailr.size;
      do {
         icondefaultthumbnailr.set(inouttargetredX, icondefaultthumbnailr.size | inouttargetredX.length);
         if (list7) {
            break;
         }
      } while (list7 && (inouttargetredX.endsWith(`${icondefaultthumbnailr.size}`)));
      while (5 > (icondefaultthumbnailr.size - inouttargetredX.length) || (5 - icondefaultthumbnailr.size) > 2) {
         inouttargetredX = `${icondefaultthumbnailr.size | inouttargetredX.length}`;
         break;
      }
       let attributedstringb = String.fromCharCode(113,95,51,51,95,98,105,116,115,0);
       let middlewares = String.fromCharCode(114,101,115,101,116,95,103,95,55,51,0);
      while (attributedstringb == String.fromCharCode(117,0)) {
         inouttargetredX += `${2 << (Math.min(1, attributedstringb.length))}`;
         break;
      }
      temperaturea += `${traminiv.length << (Math.min(Math.abs(3), 3))}`;
   for (let g = 0; g < 1; g++) {
       let cinit_2y3: Array<any> = [11, 837, 823];
         cinit_2y3.push(cinit_2y3.length);
      if (4 >= (cinit_2y3.length % 3) && 2 >= (cinit_2y3.length % (Math.max(3, 9)))) {
          let encryptd = 2.0;
          let interstitialE = true;
          let recommendationt = 4;
          let privilegeR = 1.0;
         cinit_2y3.push(2 ^ recommendationt);
         encryptd *= parseFloat(`${3 % (Math.max(parseInt(`${privilegeR}`), 10))}`);
         interstitialE = privilegeR > 28.81 || interstitialE;
         recommendationt /= Math.max(2, 2 - parseInt(`${encryptd}`));
      }
       let middleD: Array<any> = [756, 576];
       let basketballmatchdetailbgP: Array<any> = [928, 428, 711];
      traminiv = `${yellowtored_.length % 2}`;
   }
      librrc_ *= ((activei ? 1 : 1));
        setCountDown(timerRef.current);
      }
    }, 1000);
    return () => {
      clearInterval(timerId);
    };
  }, [props.resend]);

  return (
    <View
      style={{
        justifyContent: 'center',
        alignItems: 'center',
        flexDirection: 'row',
        marginTop: 35,
      }}>
      <Image
        style={styles.iconStyle}
        source={require('@static/images/invite/viewerIconmore.png')}
      />

      <Text style={styles.hyperlink}>{Math.floor(timerRef.current / 60).toString().padStart(2, '0')}:{(timerRef.current % 60).toString().padStart(2, '0')} 秒</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  hyperlink: {
    fontWeight: '400',
    fontSize: 18,
    textAlign: 'left',
    color: '#9C9C9C',
  },
  iconStyle: { height: 26, width: 26, marginRight: 5 },
});
