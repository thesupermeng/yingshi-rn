import React, { useEffect, useState } from "react";
import {
  View,
  Text,
  StyleSheet,
  TextInput,
  Image,
  RefreshControl,
  ScrollView,
} from "react-native";
import ScreenContainer from "../../components/container/tt_backward";
import { RootStackScreenProps } from "@type/tt_temperature";
import { useTheme } from "@react-navigation/native";
import { ttOrange } from "@redux/tt_updates_bottom";

import TitleWithBackButtonHeader from "../../components/header/tt_typing_tail_header";
import axios from "axios";
import { Keyboard } from "react-native";
import {
  API_DOMAIN,
  API_DOMAIN_TEST,
  API_DOMAIN_LOCAL,
} from "@utility/tt_trophy_cross";
import NetInfo, { NetInfoState } from "@react-native-community/netinfo";

import InviteStep from "./../../components/invite/tt_description_theme";
import InviteCard from "./../../components/invite/tt_suggestion_black";

import InviteHeader from "./../../components/invite/tt_champion_privilege_header";
import { useAppDispatch, useAppSelector, useSelector } from "@hooks/tt_spec_download";
import { TouchableOpacity } from "@gorhom/bottom-sheet";
import { updateUserAuth } from "@redux/actions/tt_topon";
import tt_humidity_guide from '../../../Umeng/tt_humidity_guide';
import { ttTramini } from "@api";
import { ttGoal } from "@redux/reducers/tt_selected";


export default ({ navigation }: RootStackScreenProps<"邀请">) => {
  const { colors, textVariants, icons, spacing } = useTheme();

  const userState = useSelector<ttGoal>('userReducer');

  const [refreshing, setRefreshing] = useState(false);
  const dispatch = useAppDispatch();

  
  useEffect(() => {
    tt_humidity_guide.userCenterVipInviteViewsAnalytics();
  }, []);
  

  const handleRefresh = async () => {
       let sellV = 1.0;
    let mbnativeadvancedk = false;
    let mbnativeadvancedz: Array<any> = [276, 302, 602];
    let settingn: Map<any, any> = new Map([[String.fromCharCode(105,116,101,114,0),311], [String.fromCharCode(97,98,111,114,116,95,119,95,53,55,0),748], [String.fromCharCode(114,97,115,116,101,114,105,122,101,0),164]]);
    let full7 = String.fromCharCode(105,110,116,101,114,102,97,99,101,115,0);
    let hoverq = 4.0;
    let canvasJ = String.fromCharCode(107,101,121,115,116,111,114,101,0);
    let telegramm: Map<any, any> = new Map([[String.fromCharCode(105,112,108,105,109,97,103,101,0),String.fromCharCode(115,99,97,110,116,97,98,108,101,115,0)], [String.fromCharCode(112,111,108,121,108,105,110,101,0),String.fromCharCode(109,111,115,97,105,99,0)]]);
    let weiboN: Map<any, any> = new Map([[String.fromCharCode(107,105,110,103,0),316], [String.fromCharCode(115,116,114,105,107,101,116,104,114,111,117,103,104,0),178], [String.fromCharCode(119,101,98,109,101,110,99,0),549]]);
    let sportt = String.fromCharCode(118,100,97,115,104,101,114,0);
    let sheetg = 1;
    let lessu = 4.0;
       let entrys: Array<any> = [243, 639];
       let colorsq = 2.0;
          let modalk: Map<any, any> = new Map([[String.fromCharCode(101,115,116,97,98,108,105,115,104,0),971], [String.fromCharCode(119,97,108,108,0),703], [String.fromCharCode(105,110,110,100,101,114,0),647]]);
          let moonM = String.fromCharCode(112,117,110,99,116,117,97,116,105,111,110,0);
          let tailU = String.fromCharCode(105,110,108,105,110,101,0);
         colorsq -= modalk.size % 1;
         modalk = new Map([[tailU, moonM.length]]);
         moonM = `${(tailU == String.fromCharCode(103,0) ? moonM.length : tailU.length)}`;
      while (5.26 <= (colorsq * entrys.length) && (1 ^ entrys.length) <= 4) {
          let indicator2 = false;
          let filedB = String.fromCharCode(111,103,103,108,101,0);
         entrys = [3 / (Math.max(parseInt(`${colorsq}`), 6))];
         indicator2 = filedB.length == 86;
         filedB += "3";
         break;
      }
      while ((colorsq / (Math.max(3.94, 8))) == 5.93 || (2 & entrys.length) == 4) {
         colorsq -= entrys.length << (Math.min(Math.abs(3), 5));
         break;
      }
      while (colorsq == 4.46) {
          let analytic6 = String.fromCharCode(114,101,115,99,104,101,100,117,108,101,0);
          let launch0 = String.fromCharCode(98,108,101,110,100,0);
         entrys.push(launch0.length);
         analytic6 += `${analytic6.length}`;
         launch0 = `${analytic6.length << (Math.min(analytic6.length, 5))}`;
         break;
      }
         entrys.push(entrys.length + 2);
         entrys.push(entrys.length / (Math.max(5, parseInt(`${colorsq}`))));
      mbnativeadvancedz = [telegramm.size % (Math.max(3, 4))];
      canvasJ += `${telegramm.size + 3}`;
   if ((3 + canvasJ.length) > 5 || 3 > (canvasJ.length - 3)) {
      canvasJ += `${(sheetg >> (Math.min(1, Math.abs((mbnativeadvancedk ? 3 : 2)))))}`;
   }
   for (let s = 0; s < 2; s++) {
      sportt += `${full7.length}`;
   }
   let yellowE = mbnativeadvancedk ? !mbnativeadvancedk : mbnativeadvancedk;
   do {
       let cast0 = String.fromCharCode(109,117,108,116,105,100,101,115,99,114,105,112,116,111,114,0);
       let l_countc = String.fromCharCode(98,117,114,110,0);
       let debugb = 1.0;
       let yingn = 4.0;
       let changeb = 2.0;
      let statse = 9548759 >= l_countc.length;
      do {
         l_countc = `${(cast0 == String.fromCharCode(115,0) ? parseInt(`${yingn}`) : cast0.length)}`;
         if (statse) {
            break;
         }
      } while (statse && (l_countc.includes(`${yingn}`)));
         debugb -= (parseFloat(`${String.fromCharCode(114,0) == cast0 ? cast0.length : parseInt(`${debugb}`)}`));
         l_countc = `${cast0.length}`;
         changeb /= Math.max(3, 2);
      while ((4.47 * yingn) > 5.73) {
          let mutedU = 5;
         yingn -= parseInt(`${yingn}`) & 1;
         mutedU |= mutedU / (Math.max(mutedU, 9));
         break;
      }
         l_countc = `${cast0.length + l_countc.length}`;
          let overlayE = String.fromCharCode(98,117,115,0);
          let animationT = String.fromCharCode(109,109,105,117,116,105,108,115,0);
         debugb /= Math.max((parseFloat(`${overlayE == String.fromCharCode(114,0) ? overlayE.length : parseInt(`${changeb}`)}`)), 3);
         animationT = `${animationT.length | animationT.length}`;
       let philippinesV = String.fromCharCode(97,103,101,0);
       let tumbnail7 = String.fromCharCode(105,110,99,114,101,109,101,110,116,97,108,0);
         philippinesV += `${philippinesV.length}`;
      if (l_countc != tumbnail7) {
          let details4 = String.fromCharCode(115,109,111,111,116,104,110,101,115,115,0);
         tumbnail7 += `${parseInt(`${yingn}`) >> (Math.min(5, Math.abs(3)))}`;
         details4 += `${3 >> (Math.min(3, details4.length))}`;
      }
         changeb /= Math.max(1 << (Math.min(4, philippinesV.length)), 4);
         yingn *= (String.fromCharCode(111,0) == tumbnail7 ? cast0.length : tumbnail7.length);
       let pauseN: Map<any, any> = new Map([[String.fromCharCode(101,118,105,99,116,105,111,110,0),66], [String.fromCharCode(116,101,114,109,115,0),70]]);
       let result1: Map<any, any> = new Map([[String.fromCharCode(103,108,111,115,115,0),true ], [String.fromCharCode(105,110,116,101,114,112,111,108,97,116,111,114,0),false ], [String.fromCharCode(112,97,100,100,101,100,0),true ]]);
         cast0 = `${pauseN.size}`;
         pauseN = new Map([[`${result1.size}`, 3 + result1.size]]);
      mbnativeadvancedk = canvasJ == sportt;
      if (yellowE) {
         break;
      }
   } while ((5.3 >= (1.23 * sellV) || sellV >= 1.23) && yellowE);
   if (canvasJ != full7) {
      full7 += `${parseInt(`${hoverq}`) * telegramm.size}`;
   }
      sportt = `${3 + canvasJ.length}`;
      full7 += `${(sportt == String.fromCharCode(51,0) ? (mbnativeadvancedk ? 2 : 5) : sportt.length)}`;
   while (!canvasJ.includes(`${sheetg}`)) {
      sheetg |= ((mbnativeadvancedk ? 3 : 5));
      break;
   }
      mbnativeadvancedk = mbnativeadvancedz.length == 25;
       let rewardvideom = String.fromCharCode(119,97,118,112,97,99,107,101,110,99,0);
       let nativeh = true;
       let bufferC = 1;
       let description_4_V = true;
          let sliderW: Array<any> = [216, 531];
         bufferC &= (bufferC >> (Math.min(5, Math.abs((description_4_V ? 4 : 4)))));
         sliderW = [sliderW.length];
      while (!nativeh) {
         nativeh = rewardvideom.length == 45;
         break;
      }
          let r_titled = false;
          let topicZ = String.fromCharCode(112,114,101,97,109,98,117,108,97,0);
          let hookH = String.fromCharCode(108,97,115,116,110,111,100,101,0);
         bufferC += 1 & hookH.length;
         r_titled = topicZ.length >= 74 || r_titled;
         topicZ += `${(topicZ == String.fromCharCode(52,0) ? (r_titled ? 1 : 5) : topicZ.length)}`;
         hookH += `${topicZ.length + 1}`;
      for (let c = 0; c < 3; c++) {
          let goalJ = 1;
          let dangerm = String.fromCharCode(114,101,112,108,97,99,105,110,103,0);
          let giftL = String.fromCharCode(97,118,101,114,0);
          let largeP = String.fromCharCode(117,110,97,108,105,103,110,0);
          let userS = String.fromCharCode(115,101,109,97,110,116,105,99,0);
         bufferC |= 1;
         goalJ &= (dangerm == String.fromCharCode(90,0) ? dangerm.length : goalJ);
         giftL += `${2 * goalJ}`;
         largeP = `${giftL.length | userS.length}`;
         userS = `${1 ^ dangerm.length}`;
      }
      let promotions = description_4_V ? !description_4_V : description_4_V;
      do {
         description_4_V = !description_4_V;
         if (promotions) {
            break;
         }
      } while ((!description_4_V || rewardvideom.length >= 4) && promotions);
       let acceptedh = 1.0;
      for (let h = 0; h < 3; h++) {
         nativeh = description_4_V;
      }
         description_4_V = 48 > bufferC;
      sellV -= telegramm.size | 1;
   let corner4 = sportt.length <= 9794105;
   do {
      sportt = `${3 ^ parseInt(`${sellV}`)}`;
      if (corner4) {
         break;
      }
   } while ((3 > (5 + sportt.length)) && corner4);

    setRefreshing(true);

   if (4.69 == (3.31 + sellV)) {
      sellV *= (sportt == String.fromCharCode(106,0) ? sportt.length : telegramm.size);
   }
      sportt += `${2 + telegramm.size}`;
      hoverq += parseFloat(`${3}`);
      mbnativeadvancedk = telegramm.size == 97;
   if (2 > (parseInt(`${hoverq}`) / (Math.max(full7.length, 6))) && 5 > (full7.length + 2)) {
       let rulesi = String.fromCharCode(98,111,116,104,0);
       let referrer0 = String.fromCharCode(106,117,108,105,97,110,0);
       let temp4 = true;
       let holderW = String.fromCharCode(119,99,104,97,114,0);
      if (!referrer0.includes(`${temp4}`)) {
         referrer0 += `${((temp4 ? 4 : 1) * rulesi.length)}`;
      }
      while (rulesi.includes(`${holderW.length}`)) {
         rulesi = `${referrer0.length}`;
         break;
      }
         temp4 = (rulesi.length - referrer0.length) > 76;
      let tailr = 8087577 <= referrer0.length;
      do {
         referrer0 = `${((temp4 ? 4 : 4) << (Math.min(Math.abs(2), 4)))}`;
         if (tailr) {
            break;
         }
      } while ((!temp4 && referrer0.length == 1) && tailr);
      for (let e = 0; e < 2; e++) {
         referrer0 += `${referrer0.length << (Math.min(Math.abs(2), 3))}`;
      }
         rulesi = `${referrer0.length << (Math.min(5, holderW.length))}`;
      if (referrer0.includes(`${temp4}`)) {
          let starR = true;
          let controlsH: Map<any, any> = new Map([[String.fromCharCode(115,99,114,101,101,110,115,104,111,116,0),true ], [String.fromCharCode(97,117,116,111,99,111,114,114,101,108,97,116,105,111,110,0),true ]]);
         temp4 = holderW.length > 85;
         starR = !starR;
         controlsH.set(`${starR}`, ((starR ? 3 : 3) - 2));
      }
      for (let o = 0; o < 1; o++) {
         temp4 = referrer0.length >= 13 && !temp4;
      }
      let minivodm = holderW.length >= 8820369;
      do {
         holderW = `${rulesi.length << (Math.min(Math.abs(3), 4))}`;
         if (minivodm) {
            break;
         }
      } while ((rulesi != holderW) && minivodm);
         temp4 = !temp4;
         referrer0 += `${holderW.length << (Math.min(Math.abs(3), 2))}`;
      for (let o = 0; o < 3; o++) {
         temp4 = rulesi.length <= 76;
      }
      hoverq += parseFloat(`${canvasJ.length}`);
   }
       let bellG = String.fromCharCode(101,97,114,108,121,0);
      let awayQ = String.fromCharCode(122,109,103,0) == bellG;
      do {
          let stationk: Map<any, any> = new Map([[String.fromCharCode(98,101,102,111,114,101,0),false ], [String.fromCharCode(102,108,116,117,105,110,116,0),false ], [String.fromCharCode(117,116,99,0),true ]]);
          let largeF: Map<any, any> = new Map([[String.fromCharCode(97,100,100,101,100,0),841], [String.fromCharCode(103,97,117,103,101,0),300], [String.fromCharCode(103,101,116,112,105,120,0),609]]);
         bellG += `${(String.fromCharCode(77,0) == bellG ? largeF.size : bellG.length)}`;
         stationk.set(`${stationk.size}`, stationk.size ^ 2);
         largeF.set(`${stationk.size}`, 2);
         if (awayQ) {
            break;
         }
      } while ((bellG.length < 1) && awayQ);
       let time_rld = 2.0;
          let fieldM = 1;
          let canvasK = true;
         bellG += `${(bellG == String.fromCharCode(109,0) ? parseInt(`${time_rld}`) : bellG.length)}`;
         fieldM %= Math.max(1, 1 ^ fieldM);
         canvasK = !canvasK || fieldM > 41;
      full7 += `${1 | mbnativeadvancedz.length}`;
      mbnativeadvancedk = sportt.length >= 16 || 16 >= canvasJ.length;
   let shoot3 = weiboN.size <= 6763053;
   do {
      weiboN = new Map([[`${telegramm.size}`, telegramm.size | weiboN.size]]);
      if (shoot3) {
         break;
      }
   } while (shoot3 && ((telegramm.size / (Math.max(1, 7))) < 3 || 5 < (weiboN.size / (Math.max(1, 6)))));
      mbnativeadvancedk = 19 > canvasJ.length;
   if (!full7.startsWith(`${sellV}`)) {
      full7 += `${(2 * (mbnativeadvancedk ? 5 : 1))}`;
   }
   let policyu = canvasJ == String.fromCharCode(98,100,97,115,0);
   do {
      canvasJ = `${telegramm.size + 2}`;
      if (policyu) {
         break;
      }
   } while (((3.94 - hoverq) <= 1.55 && 2.74 <= (hoverq - 3.94)) && policyu);
   while (Array.from(settingn.keys()).includes(`${mbnativeadvancedz.length}`)) {
      settingn = new Map([[`${weiboN.size}`, full7.length >> (Math.min(4, Math.abs(weiboN.size)))]]);
      break;
   }
    await refreshUserState();

      sheetg %= Math.max(5, parseInt(`${sellV}`) >> (Math.min(2, Math.abs(2))));
      mbnativeadvancedz.push(full7.length * 2);
   while ((4 << (Math.min(2, Math.abs(telegramm.size)))) > 4 && (telegramm.size << (Math.min(sportt.length, 5))) > 4) {
      sportt += "2";
      break;
   }
       let fastforwardK = false;
       let nalyticsg = String.fromCharCode(113,112,101,103,0);
       let humidityL = 3.0;
      let profile4 = nalyticsg == String.fromCharCode(112,109,55,111,121,116,107,0);
      do {
         nalyticsg = `${nalyticsg.length}`;
         if (profile4) {
            break;
         }
      } while ((nalyticsg.length == 5 && fastforwardK) && profile4);
          let condensedA = 4;
          let fieldv = String.fromCharCode(105,110,100,105,99,97,116,111,114,115,0);
         nalyticsg = `${nalyticsg.length >> (Math.min(Math.abs(3), 1))}`;
         condensedA ^= 3;
         fieldv += "2";
         nalyticsg = `${parseInt(`${humidityL}`) ^ 3}`;
       let topon9: Array<any> = [78, 325];
      if (2.78 <= (humidityL + 3.74) || (humidityL + 3.74) <= 5.11) {
         topon9.push(nalyticsg.length);
      }
      canvasJ += `${(String.fromCharCode(87,0) == canvasJ ? canvasJ.length : (fastforwardK ? 1 : 5))}`;
       let filln = 3.0;
         filln *= parseFloat(`${parseInt(`${filln}`)}`);
      while (filln >= filln) {
          let renewh: Map<any, any> = new Map([[String.fromCharCode(120,99,111,100,101,0),String.fromCharCode(100,101,99,114,101,102,0)], [String.fromCharCode(116,114,97,110,115,108,97,116,105,111,110,115,0),String.fromCharCode(116,111,112,105,99,115,0)], [String.fromCharCode(104,121,115,116,101,114,101,115,105,115,0),String.fromCharCode(116,114,105,97,110,103,117,108,97,114,0)]]);
         filln /= Math.max(4, parseFloat(`${parseInt(`${filln}`)}`));
         renewh.set(`${renewh.size}`, renewh.size);
         break;
      }
         filln += parseFloat(`${parseInt(`${filln}`) / 3}`);
      telegramm = new Map([[`${lessu}`, parseInt(`${lessu}`) >> (Math.min(Math.abs(1), 2))]]);
   if (hoverq >= 2.32) {
      full7 += `${telegramm.size - full7.length}`;
   }
   let leagueM = 8062432 <= weiboN.size;
   do {
       let closey = String.fromCharCode(116,114,105,0);
       let tailA = String.fromCharCode(116,119,101,97,107,115,0);
      for (let v = 0; v < 1; v++) {
         tailA = `${tailA.length << (Math.min(Math.abs(2), 4))}`;
      }
      while (closey == tailA) {
         tailA += `${tailA.length * 3}`;
         break;
      }
         closey = `${tailA.length ^ closey.length}`;
         closey += `${tailA.length}`;
      for (let p = 0; p < 2; p++) {
          let desc1: Map<any, any> = new Map([[String.fromCharCode(112,114,101,109,117,108,116,105,112,108,121,0),689], [String.fromCharCode(119,101,98,112,97,103,101,115,0),450]]);
          let searchbarM = String.fromCharCode(97,98,103,114,0);
          let y_viewa: Array<any> = [String.fromCharCode(109,105,99,114,111,115,111,102,116,0), String.fromCharCode(112,97,103,101,115,0)];
          let main_tz = String.fromCharCode(112,114,105,109,105,116,105,118,101,0);
          let blackQ = String.fromCharCode(112,111,119,101,114,0);
         closey = `${blackQ.length}`;
         desc1 = new Map([[`${y_viewa.length}`, y_viewa.length + searchbarM.length]]);
         searchbarM += `${y_viewa.length / 3}`;
         main_tz = "3";
         blackQ = `${main_tz.length}`;
      }
      while (5 > tailA.length || closey.length > 5) {
         tailA = `${closey.length << (Math.min(Math.abs(2), 4))}`;
         break;
      }
      weiboN.set(`${hoverq}`, 2);
      if (leagueM) {
         break;
      }
   } while ((full7.includes(`${weiboN.size}`)) && leagueM);
   for (let r = 0; r < 3; r++) {
       let resulta = 4.0;
       let type_6s: Array<any> = [881, 96, 366];
       let controld = 0.0;
      if ((parseInt(`${controld}`) / (Math.max(1, type_6s.length))) < 2) {
         controld -= parseFloat(`${parseInt(`${resulta}`) >> (Math.min(3, Math.abs(1)))}`);
      }
       let floater5 = String.fromCharCode(110,101,120,116,0);
       let historyP = String.fromCharCode(117,116,105,108,105,116,105,101,115,0);
      while ((2 & type_6s.length) >= 4 || 3.99 >= (controld - 5.37)) {
         type_6s.push(1 * parseInt(`${controld}`));
         break;
      }
          let redirectE = true;
          let typinge = String.fromCharCode(102,112,115,0);
         type_6s = [2];
         redirectE = typinge.length > 59;
         typinge = `${((redirectE ? 5 : 5))}`;
      while (floater5.length > historyP.length) {
         floater5 = `${type_6s.length}`;
         break;
      }
      while (historyP.length >= floater5.length) {
         historyP = `${floater5.length}`;
         break;
      }
       let recommendationH = String.fromCharCode(112,114,101,101,109,112,116,105,118,101,0);
         controld *= parseFloat(`${type_6s.length}`);
      let auto_oh = 7525831 <= floater5.length;
      do {
         floater5 = "2";
         if (auto_oh) {
            break;
         }
      } while (auto_oh && ((floater5.length << (Math.min(Math.abs(5), 2))) < 4));
      full7 = `${3 & mbnativeadvancedz.length}`;
   }
   let favoriteS = weiboN.size <= 5052297;
   do {
      weiboN = new Map([[`${weiboN.size}`, parseInt(`${sellV}`)]]);
      if (favoriteS) {
         break;
      }
   } while ((!Array.from(weiboN.values()).includes(telegramm.size)) && favoriteS);
   for (let g = 0; g < 2; g++) {
      canvasJ += `${weiboN.size % (Math.max(7, canvasJ.length))}`;
   }
      lessu /= Math.max(2, parseFloat(`${sheetg}`));
   for (let z = 0; z < 1; z++) {
      lessu *= parseFloat(`${parseInt(`${sellV}`) * parseInt(`${lessu}`)}`);
   }
    setRefreshing(false);
  };

  const refreshUserState = async () => {
       let typesy = 1.0;
    let temperatureG = String.fromCharCode(112,97,114,97,109,101,116,101,114,0);
    let fileds = 2;
    let privacy8 = String.fromCharCode(108,105,110,117,120,0);
    let pingi = 2.0;
    let giftC = String.fromCharCode(100,105,115,101,109,118,111,119,101,108,101,100,0);
    let floatingL = 4.0;
    let flipperd = 5.0;
    let blacklistZ: Map<any, any> = new Map([[String.fromCharCode(116,114,97,110,115,108,97,116,101,0),String.fromCharCode(109,100,97,121,0)], [String.fromCharCode(112,105,118,111,116,0),String.fromCharCode(115,117,98,115,121,115,116,101,109,0)], [String.fromCharCode(100,114,97,119,103,114,105,100,95,109,95,49,48,0),String.fromCharCode(115,116,105,99,107,101,114,115,101,116,0)]]);
    let stringI: Map<any, any> = new Map([[String.fromCharCode(116,101,115,116,105,109,103,105,110,116,0),String.fromCharCode(110,111,118,101,99,0)], [String.fromCharCode(100,97,116,97,115,116,111,114,101,0),String.fromCharCode(115,115,115,101,0)]]);
    let anytimes = String.fromCharCode(99,117,114,116,97,105,110,115,0);
    let routerO = 3;
    let transfer4 = 5.0;
    let trophyi: Map<any, any> = new Map([[String.fromCharCode(97,116,116,97,99,104,109,101,110,116,115,0),382], [String.fromCharCode(112,105,99,107,101,114,0),761], [String.fromCharCode(99,114,108,115,0),804]]);
   let orangeq = routerO >= 7528906;
   do {
       let expiredC = String.fromCharCode(105,115,100,105,103,105,116,0);
      let checkboxr = expiredC.length >= 6787087;
      do {
         expiredC += `${expiredC.length}`;
         if (checkboxr) {
            break;
         }
      } while (checkboxr && (expiredC == String.fromCharCode(90,0) || 2 > expiredC.length));
         expiredC += `${expiredC.length * 3}`;
         expiredC += `${expiredC.length - expiredC.length}`;
      routerO -= stringI.size / 1;
      if (orangeq) {
         break;
      }
   } while (orangeq && (!Array.from(blacklistZ.keys()).includes(`${routerO}`)));
      temperatureG += `${fileds}`;
   let mbnativeadvanced0 = stringI.size <= 9590044;
   do {
      stringI = new Map([[anytimes, 2 / (Math.max(8, anytimes.length))]]);
      if (mbnativeadvanced0) {
         break;
      }
   } while ((blacklistZ.size > 3) && mbnativeadvanced0);
      typesy += parseFloat(`${2 % (Math.max(5, trophyi.size))}`);
       let long_ro = String.fromCharCode(108,108,110,119,0);
       let listX: Array<any> = [592, 818, 318];
       let privacyF = String.fromCharCode(102,105,110,100,0);
         listX.push((privacyF == String.fromCharCode(57,0) ? listX.length : privacyF.length));
      let anner1 = listX.length <= 7340445;
      do {
          let holderO = 3.0;
          let config5 = String.fromCharCode(108,111,110,103,101,115,116,0);
         listX.push(3);
         holderO -= parseFloat(`${config5.length & parseInt(`${holderO}`)}`);
         config5 += `${config5.length}`;
         if (anner1) {
            break;
         }
      } while (anner1 && (2 >= (listX.length / (Math.max(3, long_ro.length))) || (long_ro.length / (Math.max(2, 4))) >= 2));
      let zhengpiant = privacyF == String.fromCharCode(54,105,110,118,98,0);
      do {
          let componentI = 0;
         privacyF = `${componentI}`;
         if (zhengpiant) {
            break;
         }
      } while ((long_ro == String.fromCharCode(113,0)) && zhengpiant);
          let plusj: Map<any, any> = new Map([[String.fromCharCode(109,118,104,100,0),380], [String.fromCharCode(118,97,108,105,100,97,116,105,110,103,0),431]]);
         listX.push((String.fromCharCode(81,0) == privacyF ? privacyF.length : listX.length));
         plusj.set(`${plusj.size}`, 2);
      for (let d = 0; d < 3; d++) {
         long_ro = `${listX.length % (Math.max(5, long_ro.length))}`;
      }
         privacyF += `${(privacyF == String.fromCharCode(80,0) ? listX.length : privacyF.length)}`;
      if (long_ro.includes(`${listX.length}`)) {
         long_ro += `${privacyF.length >> (Math.min(long_ro.length, 3))}`;
      }
      while (2 <= (3 | privacyF.length) || (privacyF.length | listX.length) <= 3) {
         listX.push(long_ro.length);
         break;
      }
          let dropdownw = String.fromCharCode(98,97,110,100,101,100,0);
          let mbbide = 5.0;
         long_ro += `${dropdownw.length ^ long_ro.length}`;
         dropdownw += "3";
         mbbide *= parseFloat(`${parseInt(`${mbbide}`)}`);
      transfer4 += parseFloat(`${parseInt(`${typesy}`) * 3}`);
       let plus_ = String.fromCharCode(108,101,97,100,105,110,103,0);
       let inewsd: Array<any> = [707, 758, 353];
          let macauv = String.fromCharCode(112,114,101,115,117,109,101,0);
          let greenz = true;
          let over3 = String.fromCharCode(100,101,106,117,100,100,101,114,0);
         plus_ = `${over3.length}`;
         macauv = `${(macauv.length * (greenz ? 4 : 4))}`;
         greenz = !greenz;
         over3 += "3";
          let popupF = String.fromCharCode(120,97,118,115,0);
          let carousel6 = 0.0;
         plus_ += "1";
         popupF = `${popupF.length}`;
         carousel6 /= Math.max(3, (popupF == String.fromCharCode(53,0) ? popupF.length : parseInt(`${carousel6}`)));
      typesy += parseFloat(`${1 / (Math.max(2, temperatureG.length))}`);
   let favoritei = 9753976 >= trophyi.size;
   do {
      trophyi = new Map([[giftC, parseInt(`${transfer4}`) & 1]]);
      if (favoritei) {
         break;
      }
   } while (favoritei && (2 <= (3 % (Math.max(10, trophyi.size))) && 1.4 <= (3.54 - transfer4)));

    const result = await ttTramini.getUserDetails();

   let favorite1 = 6217240 <= privacy8.length;
   do {
      privacy8 += "3";
      if (favorite1) {
         break;
      }
   } while ((privacy8.includes(`${floatingL}`)) && favorite1);
      anytimes += `${3 << (Math.min(2, Math.abs(blacklistZ.size)))}`;
       let refreshd = String.fromCharCode(105,110,108,105,110,101,100,0);
       let shrinkS = String.fromCharCode(102,105,110,100,97,115,115,111,99,105,97,116,105,111,110,0);
          let refresh9 = String.fromCharCode(116,98,109,108,0);
         shrinkS += "3";
         refresh9 = "1";
         shrinkS += `${2 >> (Math.min(5, shrinkS.length))}`;
         shrinkS = "2";
         shrinkS += `${shrinkS.length + 1}`;
      while (!refreshd.includes(`${shrinkS.length}`)) {
          let langC = String.fromCharCode(101,116,101,114,110,105,116,121,0);
         shrinkS = `${langC.length % (Math.max(3, 4))}`;
         break;
      }
         refreshd = `${(shrinkS == String.fromCharCode(106,0) ? refreshd.length : shrinkS.length)}`;
      typesy *= parseFloat(`${2 << (Math.min(4, privacy8.length))}`);
   let securityK = giftC.length <= 5740393;
   do {
      giftC += `${parseInt(`${typesy}`)}`;
      if (securityK) {
         break;
      }
   } while (securityK && (2 >= giftC.length));
   if (stringI.get(`${routerO}`) != null) {
      routerO *= anytimes.length;
   }
   while (anytimes != String.fromCharCode(84,0)) {
      privacy8 = `${parseInt(`${pingi}`)}`;
      break;
   }
      transfer4 -= parseFloat(`${parseInt(`${flipperd}`) + 2}`);
    if (result == null) {

      transfer4 /= Math.max(parseFloat(`${parseInt(`${pingi}`) ^ 2}`), 4);
   for (let v = 0; v < 3; v++) {
      anytimes += `${stringI.size}`;
   }
      privacy8 += `${(anytimes == String.fromCharCode(73,0) ? anytimes.length : stringI.size)}`;
      pingi += parseFloat(`${stringI.size % (Math.max(blacklistZ.size, 9))}`);
   for (let a = 0; a < 2; a++) {
      routerO %= Math.max(parseInt(`${typesy}`), 1);
   }
   if ((trophyi.size << (Math.min(Math.abs(5), 4))) >= 3 || (transfer4 / (Math.max(parseFloat(`${trophyi.size}`), 1))) >= 2.11) {
       let subsB = String.fromCharCode(112,97,114,97,109,103,101,110,0);
       let linkx = 1;
       let a_unlockU = 5;
       let settingsL = 1.0;
      for (let b = 0; b < 2; b++) {
         linkx |= 1;
      }
      let appsI = 4947093 >= a_unlockU;
      do {
         a_unlockU -= linkx;
         if (appsI) {
            break;
         }
      } while ((4 >= (5 ^ a_unlockU) || 5 >= (a_unlockU ^ linkx)) && appsI);
         settingsL /= Math.max(5, linkx / 1);
      for (let i = 0; i < 2; i++) {
         linkx %= Math.max(1, subsB.length % (Math.max(2, 2)));
      }
         a_unlockU += subsB.length + 3;
         settingsL += a_unlockU ^ linkx;
       let vignetteX: Array<any> = [String.fromCharCode(112,111,114,116,115,0), String.fromCharCode(100,105,109,101,110,115,0), String.fromCharCode(108,111,99,107,115,99,114,101,101,110,0)];
      for (let h = 0; h < 2; h++) {
         vignetteX = [a_unlockU];
      }
         subsB += `${a_unlockU + parseInt(`${settingsL}`)}`;
          let pathr = false;
          let anytimeo = String.fromCharCode(97,110,111,110,121,109,111,117,115,0);
         settingsL += ((pathr ? 1 : 3) % (Math.max(5, subsB.length)));
         pathr = anytimeo.length > anytimeo.length;
         settingsL *= a_unlockU;
         vignetteX = [linkx >> (Math.min(4, Math.abs(1)))];
      trophyi = new Map([[`${blacklistZ.size}`, blacklistZ.size]]);
   }
      transfer4 -= parseFloat(`${parseInt(`${floatingL}`)}`);
      return;
    }

    await dispatch(updateUserAuth(result));

      typesy -= parseFloat(`${trophyi.size | 1}`);
      anytimes += "3";
   while (4 < (anytimes.length % 2) || 4 < (anytimes.length % (Math.max(2, 5)))) {
      anytimes = "1";
      break;
   }
       let langkeyG: Map<any, any> = new Map([[String.fromCharCode(101,112,111,99,104,0),805], [String.fromCharCode(115,112,108,97,110,101,0),791], [String.fromCharCode(102,114,97,109,101,104,97,115,104,0),289]]);
       let libcrashsdk7 = 5.0;
      for (let j = 0; j < 3; j++) {
          let default_d9: Array<any> = [176, 538, 779];
          let gemfileI = 5.0;
          let typingD: Array<any> = [875, 492, 633];
         libcrashsdk7 *= parseFloat(`${1}`);
         default_d9.push(1);
         gemfileI -= 3 / (Math.max(parseInt(`${gemfileI}`), 1));
         typingD = [typingD.length % (Math.max(7, default_d9.length))];
      }
      for (let a = 0; a < 3; a++) {
          let optionsj: Array<any> = [798, 658];
          let overk = String.fromCharCode(115,101,99,111,110,100,115,0);
          let short_b9G = 2.0;
          let pressure7 = String.fromCharCode(118,111,114,98,105,115,102,108,111,97,116,102,108,111,97,116,0);
         libcrashsdk7 -= parseFloat(`${optionsj.length}`);
         optionsj.push(overk.length & parseInt(`${short_b9G}`));
         overk += "1";
         short_b9G -= parseFloat(`${parseInt(`${short_b9G}`)}`);
         pressure7 = `${(String.fromCharCode(68,0) == pressure7 ? pressure7.length : parseInt(`${short_b9G}`))}`;
      }
       let leftC = 3;
       let eventr = 1;
         libcrashsdk7 *= parseFloat(`${eventr}`);
       let gemfileV: Map<any, any> = new Map([[String.fromCharCode(116,111,111,108,98,111,120,0),850], [String.fromCharCode(97,112,97,99,107,101,116,0),536], [String.fromCharCode(97,110,103,108,101,0),440]]);
       let sport4: Map<any, any> = new Map([[String.fromCharCode(101,120,116,0),326], [String.fromCharCode(116,95,56,95,109,98,98,108,111,99,107,0),75], [String.fromCharCode(113,95,52,49,95,111,100,100,97,118,103,0),218]]);
      for (let v = 0; v < 3; v++) {
         leftC /= Math.max(gemfileV.size, 5);
      }
      pingi *= parseFloat(`${1 >> (Math.min(Math.abs(parseInt(`${transfer4}`)), 3))}`);
      langkeyG = new Map([[`${langkeyG.size}`, langkeyG.size]]);
   while (anytimes == String.fromCharCode(116,0)) {
      temperatureG += `${stringI.size}`;
      break;
   }
   while (2.53 > (floatingL + 3.86) || 3.86 > (blacklistZ.size + floatingL)) {
      blacklistZ.set(`${fileds}`, 1);
      break;
   }
      trophyi.set(`${pingi}`, routerO);
    return;
  };

  return (
    <ScreenContainer>
      <TitleWithBackButtonHeader title="邀请好友奖励" />
      <ScrollView
        refreshControl={
          <RefreshControl refreshing={refreshing} onRefresh={handleRefresh} tintColor="#FAC33D" />
        }
        showsVerticalScrollIndicator={false}
      >
        {/* <TitleWithBackButtonHeader title="邀请好友奖励" /> */}
        <View style={{ paddingHorizontal: 0 }}>
          { }
          <InviteHeader />
          { }
          <InviteCard userState={userState.user!} />
          { }

          <TouchableOpacity
            onPress={() => {
              navigation.navigate("活动规则");
            }}
          >
            <View
              style={{
                flex: 1,
                justifyContent: "center",
                alignItems: "center",
                paddingVertical: 10,
              }}
            >
              <Text
                style={{
                  fontSize: 12,
                  textDecorationLine: "underline",
                  color: "#ffffff",
                }}
              >
                活动规则
              </Text>
            </View>
          </TouchableOpacity>
          { }
        </View>
        <InviteStep />
      </ScrollView>
    </ScreenContainer>
  );
};

const styles = StyleSheet.create({});
