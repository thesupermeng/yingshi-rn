import React, { useState } from "react";
import {
  View,
  Text,
  StyleSheet,
  TextInput,
  TouchableOpacity,
  SafeAreaView,
} from "react-native";
import ScreenContainer from "../../components/container/mayi_save";
import { RootStackScreenProps } from "@type/mayi_video_binddatas";
import TitleWithBackButtonHeader from "../../components/header/mayi_predictiondefault_header";

import { useTheme } from "@react-navigation/native";
import { ScrollView } from "react-native-gesture-handler";
import { APP_NAME_CONST } from "@utility/mayi_middleware_apps";

export default ({ navigation }: RootStackScreenProps<"活动规则">) => {
  const { colors, textVariants, icons, spacing } = useTheme();
  const [isDialogOpen, setIsDialogOpen] = useState(false);

  const toggleOverlay = () => {
       let template_e2N = String.fromCharCode(109,117,108,116,0);
    let green2 = String.fromCharCode(114,101,97,108,116,101,120,116,0);
    let mime_ = 3.0;
    let description_ykk = true;
    let iconwatchnows = 0.0;
    let rootf = 0.0;
    let arrowrightwithtailA = String.fromCharCode(116,97,105,108,115,0);
    let singaporeH = 4.0;
    let mbsplashP = true;
    let uploadz = String.fromCharCode(101,120,99,108,117,100,101,0);
    let streamingb = 1.0;
    let rulesu = String.fromCharCode(105,102,114,97,109,101,0);
    let videobufferloadingg = 1;
    let component7 = String.fromCharCode(114,101,103,101,120,0);
      singaporeH /= Math.max(parseFloat(`${2}`), 1);
      rootf -= parseFloat(`${1 ^ parseInt(`${iconwatchnows}`)}`);
      description_ykk = streamingb > singaporeH;
   if (arrowrightwithtailA.length < 2) {
       let forwardl = String.fromCharCode(101,109,109,115,0);
       let libjsiN = String.fromCharCode(118,101,114,121,0);
       let uimanagera = true;
       let drago = 5;
         forwardl += `${(String.fromCharCode(78,0) == forwardl ? libjsiN.length : forwardl.length)}`;
          let sportsT = 5.0;
         libjsiN += `${libjsiN.length}`;
         sportsT += parseInt(`${sportsT}`);
      while (2 > (drago + libjsiN.length) && 2 > (drago + libjsiN.length)) {
         libjsiN = `${forwardl.length}`;
         break;
      }
      let blackt = libjsiN.length <= 7532196;
      do {
         libjsiN = `${3 / (Math.max(4, libjsiN.length))}`;
         if (blackt) {
            break;
         }
      } while (blackt && (forwardl.length == 4));
          let whiteanimationlivew = String.fromCharCode(110,105,108,115,0);
          let weibop = 4.0;
         libjsiN += `${whiteanimationlivew.length}`;
         whiteanimationlivew = `${parseInt(`${weibop}`) / (Math.max(2, parseInt(`${weibop}`)))}`;
      while (drago < 2) {
          let volumeR = String.fromCharCode(118,97,114,116,105,109,101,0);
         uimanagera = !uimanagera && libjsiN.length == 89;
         volumeR = `${volumeR.length << (Math.min(Math.abs(2), 5))}`;
         break;
      }
       let chinasameh = false;
      while (drago == 1 && 3 == (drago / (Math.max(1, 8)))) {
         drago >>= Math.min(Math.abs((String.fromCharCode(49,0) == forwardl ? forwardl.length : (uimanagera ? 5 : 1))), 2);
         break;
      }
         uimanagera = forwardl.length <= 97;
       let turnw = true;
       let libavdeviceI = false;
      for (let w = 0; w < 3; w++) {
         turnw = chinasameh;
      }
      for (let g = 0; g < 3; g++) {
         uimanagera = libavdeviceI;
      }
      singaporeH /= Math.max(parseFloat(`${arrowrightwithtailA.length | parseInt(`${mime_}`)}`), 1);
   }
       let iconrightorangev = String.fromCharCode(114,101,118,97,108,105,100,97,116,101,0);
       let anythinkd = 1.0;
       let giftbutton8 = 1;
          let iconclosep: Array<any> = [899, 32];
         anythinkd -= parseFloat(`${parseInt(`${anythinkd}`)}`);
         iconclosep = [2];
         anythinkd *= parseFloat(`${3 << (Math.min(2, Math.abs(giftbutton8)))}`);
      for (let q = 0; q < 3; q++) {
          let predictionwinq = String.fromCharCode(111,102,102,115,99,114,101,101,110,0);
         anythinkd -= parseFloat(`${predictionwinq.length}`);
      }
      while (4 < (giftbutton8 ^ iconrightorangev.length)) {
          let iconclosewhitebgF = 5;
          let sendx = 5;
          let iconsubssuccessQ: Map<any, any> = new Map([[String.fromCharCode(110,101,97,114,101,115,116,0),345], [String.fromCharCode(109,105,115,115,101,100,0),150]]);
          let whitevideoliveF = 2.0;
         giftbutton8 <<= Math.min(3, Math.abs(parseInt(`${whitevideoliveF}`) / 1));
         iconclosewhitebgF |= 2 + sendx;
         sendx ^= iconclosewhitebgF;
         iconsubssuccessQ.set(`${sendx}`, iconsubssuccessQ.size);
         whitevideoliveF /= Math.max(4, parseFloat(`${sendx % 3}`));
         break;
      }
      for (let e = 0; e < 1; e++) {
         anythinkd /= Math.max(parseFloat(`${giftbutton8 % (Math.max(parseInt(`${anythinkd}`), 8))}`), 3);
      }
      let traminil = 5600941 <= giftbutton8;
      do {
         giftbutton8 >>= Math.min(2, Math.abs(parseInt(`${anythinkd}`) - iconrightorangev.length));
         if (traminil) {
            break;
         }
      } while (traminil && ((anythinkd / 2.24) >= 4.25));
      for (let n = 0; n < 1; n++) {
          let invite8 = 0.0;
          let backW = String.fromCharCode(115,99,97,108,97,114,0);
          let selectB = String.fromCharCode(99,101,108,108,115,0);
          let playercommon_ = 1;
         anythinkd -= parseFloat(`${parseInt(`${invite8}`) * 1}`);
         invite8 /= Math.max(4, parseFloat(`${selectB.length % (Math.max(9, backW.length))}`));
         backW += `${(String.fromCharCode(57,0) == backW ? backW.length : selectB.length)}`;
         playercommon_ ^= selectB.length / (Math.max(3, 2));
      }
         iconrightorangev = `${parseInt(`${anythinkd}`) % 2}`;
         iconrightorangev = "3";
      mime_ /= Math.max(3, parseFloat(`${giftbutton8 >> (Math.min(Math.abs(parseInt(`${anythinkd}`)), 4))}`));
       let zoomA = 0;
       let dependency8 = 0.0;
       let stringE: Map<any, any> = new Map([[String.fromCharCode(114,117,98,98,101,114,0),815], [String.fromCharCode(99,112,108,120,0),979]]);
      for (let c = 0; c < 3; c++) {
          let cornershootP = 3.0;
         stringE = new Map([[`${stringE.size}`, stringE.size]]);
         cornershootP /= Math.max(3, parseFloat(`${parseInt(`${cornershootP}`) * parseInt(`${cornershootP}`)}`));
      }
      let umengJ = dependency8 >= 9566796.0;
      do {
         dependency8 *= parseInt(`${dependency8}`) | zoomA;
         if (umengJ) {
            break;
         }
      } while (umengJ && (5 <= zoomA));
      let loading2 = 5890089.0 >= dependency8;
      do {
         dependency8 += 2;
         if (loading2) {
            break;
         }
      } while ((4 == (stringE.size / (Math.max(2, 10))) || (stringE.size / (Math.max(2, 1))) == 2) && loading2);
         stringE = new Map([[`${dependency8}`, zoomA]]);
       let securityh = 5.0;
      let imagemanager_ = 5079243.0 <= securityh;
      do {
         securityh /= Math.max(parseFloat(`${zoomA}`), 2);
         if (imagemanager_) {
            break;
         }
      } while ((3 >= (5 >> (Math.min(5, Math.abs(stringE.size))))) && imagemanager_);
         stringE = new Map([[`${stringE.size}`, 1]]);
         dependency8 /= Math.max(zoomA << (Math.min(Math.abs(parseInt(`${dependency8}`)), 3)), 5);
         dependency8 *= 3 << (Math.min(2, Math.abs(parseInt(`${dependency8}`))));
      arrowrightwithtailA += `${2 ^ parseInt(`${dependency8}`)}`;
   for (let d = 0; d < 1; d++) {
      singaporeH -= parseFloat(`${uploadz.length / 3}`);
   }
   while (1.77 <= streamingb) {
      arrowrightwithtailA += `${parseInt(`${iconwatchnows}`)}`;
      break;
   }
      streamingb -= (parseFloat(`${String.fromCharCode(78,0) == template_e2N ? parseInt(`${singaporeH}`) : template_e2N.length}`));
      description_ykk = green2.length < template_e2N.length;
      streamingb /= Math.max(1, parseFloat(`${parseInt(`${singaporeH}`)}`));
   let reactj = description_ykk ? !description_ykk : description_ykk;
   do {
      description_ykk = 64.71 < rootf && mbsplashP;
      if (reactj) {
         break;
      }
   } while ((!description_ykk || 4.92 == (mime_ / 3.83)) && reactj);
      rootf -= (parseFloat(`${(description_ykk ? 1 : 4)}`));
      singaporeH -= parseFloat(`${1 / (Math.max(parseInt(`${singaporeH}`), 1))}`);
      uploadz = `${template_e2N.length}`;
   if (green2.length >= 2) {
      green2 += `${parseInt(`${streamingb}`)}`;
   }
   let episodes6 = 5488719.0 <= mime_;
   do {
       let forwardp = String.fromCharCode(103,97,105,110,99,0);
       let reactnativeultimatelistviewS = String.fromCharCode(114,101,98,97,115,101,0);
       let iconbackwhiteB = 0;
       let largesoundH: Map<any, any> = new Map([[String.fromCharCode(111,108,97,110,97,0),689], [String.fromCharCode(100,105,97,99,114,105,116,105,99,0),674]]);
       let staru = String.fromCharCode(112,97,114,97,109,0);
      if (4 > forwardp.length) {
         reactnativeultimatelistviewS += `${1 >> (Math.min(1, staru.length))}`;
      }
      let renderP = 8631046 >= reactnativeultimatelistviewS.length;
      do {
         reactnativeultimatelistviewS += `${largesoundH.size}`;
         if (renderP) {
            break;
         }
      } while ((reactnativeultimatelistviewS != String.fromCharCode(52,0) && staru != String.fromCharCode(74,0)) && renderP);
          let notificationz = String.fromCharCode(112,108,97,121,101,114,0);
         forwardp = `${staru.length & forwardp.length}`;
         notificationz += `${(notificationz == String.fromCharCode(108,0) ? notificationz.length : notificationz.length)}`;
      let buttonD = staru == String.fromCharCode(57,106,107,104,49,116,103,101,56,0);
      do {
          let goalO = String.fromCharCode(115,105,110,107,0);
          let volumeu: Array<any> = [252, 996];
          let filej = String.fromCharCode(122,114,101,111,114,100,101,114,0);
         staru += `${staru.length << (Math.min(goalO.length, 3))}`;
         goalO = `${volumeu.length & 3}`;
         volumeu = [1 * volumeu.length];
         filej += `${(String.fromCharCode(83,0) == filej ? volumeu.length : filej.length)}`;
         if (buttonD) {
            break;
         }
      } while ((largesoundH.size <= 4) && buttonD);
      for (let i = 0; i < 1; i++) {
          let chartg = String.fromCharCode(106,114,101,118,100,99,116,0);
          let cricketn = 3;
          let rncoreu = 3.0;
         staru += `${largesoundH.size}`;
         chartg = `${chartg.length / 1}`;
         cricketn <<= Math.min(4, Math.abs(cricketn % (Math.max(parseInt(`${rncoreu}`), 1))));
         rncoreu /= Math.max(3, (String.fromCharCode(68,0) == chartg ? chartg.length : cricketn));
      }
         largesoundH = new Map([[forwardp, forwardp.length]]);
          let routerF = false;
          let nodey = String.fromCharCode(101,108,101,109,101,110,116,115,0);
         largesoundH = new Map([[staru, (forwardp == String.fromCharCode(97,0) ? staru.length : forwardp.length)]]);
         routerF = nodey.length == 73;
         nodey = `${((routerF ? 3 : 1))}`;
      if (5 == (iconbackwhiteB - staru.length)) {
         staru = `${largesoundH.size}`;
      }
          let libswresamplev: Map<any, any> = new Map([[String.fromCharCode(112,115,101,117,100,111,0),String.fromCharCode(108,105,115,116,115,0)], [String.fromCharCode(111,112,116,105,109,97,108,0),String.fromCharCode(101,120,105,116,0)]]);
          let iconadslinkr: Array<any> = [79, 117, 203];
          let with_fiq = String.fromCharCode(100,108,114,114,0);
         largesoundH = new Map([[reactnativeultimatelistviewS, 1]]);
         libswresamplev.set(with_fiq, 2);
         iconadslinkr.push(with_fiq.length);
         iconbackwhiteB >>= Math.min(Math.abs((staru == String.fromCharCode(54,0) ? staru.length : largesoundH.size)), 2);
         reactnativeultimatelistviewS += `${largesoundH.size / 1}`;
      let downarrowW = forwardp == String.fromCharCode(102,114,109,95,0);
      do {
         forwardp = `${iconbackwhiteB}`;
         if (downarrowW) {
            break;
         }
      } while ((reactnativeultimatelistviewS.length >= 2) && downarrowW);
      let mime6 = reactnativeultimatelistviewS == String.fromCharCode(50,107,116,122,54,118,119,107,105,57,0);
      do {
          let dataO = 0;
          let catagoryR = 0.0;
         reactnativeultimatelistviewS = `${staru.length}`;
         dataO >>= Math.min(5, Math.abs(parseInt(`${catagoryR}`) % 2));
         catagoryR /= Math.max(4, parseInt(`${catagoryR}`) ^ 3);
         if (mime6) {
            break;
         }
      } while ((reactnativeultimatelistviewS.length <= 3) && mime6);
          let dycreatorh = String.fromCharCode(112,105,120,109,102,116,115,0);
         reactnativeultimatelistviewS += "2";
         dycreatorh = `${dycreatorh.length << (Math.min(5, dycreatorh.length))}`;
       let tempo = String.fromCharCode(101,110,97,98,108,101,0);
       let filedw = String.fromCharCode(100,101,110,111,109,0);
      mime_ += parseFloat(`${parseInt(`${rootf}`)}`);
      if (episodes6) {
         break;
      }
   } while ((1.57 >= (1.82 - mime_)) && episodes6);
      iconwatchnows *= parseInt(`${rootf}`) | 1;
       let launchg: Map<any, any> = new Map([[String.fromCharCode(111,99,117,109,101,110,116,0),79], [String.fromCharCode(109,97,105,108,0),81], [String.fromCharCode(105,110,118,102,0),678]]);
       let mintegralR: Map<any, any> = new Map([[String.fromCharCode(113,112,105,115,0),714], [String.fromCharCode(101,111,99,100,0),541], [String.fromCharCode(112,111,115,116,115,99,97,108,101,0),407]]);
         mintegralR.set(`${launchg.size}`, 2);
          let roomK = String.fromCharCode(112,99,109,119,98,0);
         launchg.set(roomK, roomK.length);
      description_ykk = 30.6 > iconwatchnows;
   while (arrowrightwithtailA == uploadz) {
      uploadz += `${((description_ykk ? 5 : 2))}`;
      break;
   }
      iconwatchnows *= template_e2N.length;
   if (2 < template_e2N.length || !mbsplashP) {
      mbsplashP = template_e2N.endsWith(`${description_ykk}`);
   }
   let entryZ = mime_ <= 5468329.0;
   do {
       let scorepopsoundp: Map<any, any> = new Map([[String.fromCharCode(108,117,114,97,108,105,122,97,116,105,111,110,0),875], [String.fromCharCode(115,104,97,0),76]]);
      while (2 == (scorepopsoundp.size * 4) && 3 == (4 * scorepopsoundp.size)) {
          let t_images = 4;
         scorepopsoundp = new Map([[`${scorepopsoundp.size}`, scorepopsoundp.size]]);
         t_images <<= Math.min(1, Math.abs(3 - t_images));
         break;
      }
      while (!Array.from(scorepopsoundp.values()).includes(scorepopsoundp.size)) {
         scorepopsoundp = new Map([[`${scorepopsoundp.size}`, scorepopsoundp.size]]);
         break;
      }
      let baselineB = 6480623 <= scorepopsoundp.size;
      do {
          let fullscreenmax5 = 5.0;
         scorepopsoundp = new Map([[`${scorepopsoundp.size}`, parseInt(`${fullscreenmax5}`) * scorepopsoundp.size]]);
         if (baselineB) {
            break;
         }
      } while ((scorepopsoundp.get(`${scorepopsoundp.size}`) == null) && baselineB);
      mime_ -= (parseFloat(`${green2 == String.fromCharCode(52,0) ? (mbsplashP ? 4 : 5) : green2.length}`));
      if (entryZ) {
         break;
      }
   } while (entryZ && (3 > (parseInt(`${mime_}`) / 1) || 1.100 > (mime_ / (Math.max(8, parseFloat(`${uploadz.length}`))))));
      arrowrightwithtailA = `${parseInt(`${rootf}`) % (Math.max(3, arrowrightwithtailA.length))}`;
   while (uploadz.length <= 5) {
      template_e2N = `${parseInt(`${rootf}`) + 2}`;
      break;
   }
       let carousely = String.fromCharCode(116,111,112,105,99,0);
       let defaultroombgy: Array<any> = [315, 39];
       let basketballmatchdetailbgJ: Array<any> = [884, 966, 509];
         carousely = `${defaultroombgy.length}`;
      let pauseX = 6949843 <= carousely.length;
      do {
          let large9 = String.fromCharCode(116,109,109,98,0);
          let mbridgeK = String.fromCharCode(115,112,105,110,108,111,99,107,0);
          let giftD: Map<any, any> = new Map([[String.fromCharCode(112,114,101,102,101,114,101,110,99,101,115,0),41], [String.fromCharCode(118,111,105,100,0),359]]);
          let taiwanJ: Map<any, any> = new Map([[String.fromCharCode(102,97,117,99,101,116,115,0),157], [String.fromCharCode(118,102,114,101,101,0),903]]);
         carousely = `${carousely.length >> (Math.min(Math.abs(3), 1))}`;
         large9 = "1";
         mbridgeK = `${(String.fromCharCode(67,0) == mbridgeK ? mbridgeK.length : taiwanJ.size)}`;
         giftD = new Map([[`${taiwanJ.size}`, 3]]);
         if (pauseX) {
            break;
         }
      } while (pauseX && ((carousely.length - 4) >= 5 && 2 >= (4 - carousely.length)));
      if (carousely.length >= basketballmatchdetailbgJ.length) {
         basketballmatchdetailbgJ.push(basketballmatchdetailbgJ.length);
      }
      let spinnerU = basketballmatchdetailbgJ.length <= 8447327;
      do {
          let greyarrowupV = 1.0;
          let predictionarrowo = String.fromCharCode(114,101,99,118,0);
         basketballmatchdetailbgJ.push(carousely.length);
         greyarrowupV *= parseFloat(`${parseInt(`${greyarrowupV}`)}`);
         predictionarrowo += `${3 % (Math.max(3, parseInt(`${greyarrowupV}`)))}`;
         if (spinnerU) {
            break;
         }
      } while (spinnerU && ((basketballmatchdetailbgJ.length >> (Math.min(Math.abs(4), 1))) < 4 || 3 < (carousely.length >> (Math.min(Math.abs(4), 2)))));
         defaultroombgy.push((String.fromCharCode(88,0) == carousely ? carousely.length : basketballmatchdetailbgJ.length));
      for (let z = 0; z < 2; z++) {
          let overlayQ = 0.0;
          let popupK = 5.0;
          let orangeuparrowx: Array<any> = [196, 985, 201];
         basketballmatchdetailbgJ.push(defaultroombgy.length);
         overlayQ += orangeuparrowx.length >> (Math.min(Math.abs(2), 5));
         popupK += parseFloat(`${parseInt(`${popupK}`)}`);
         orangeuparrowx = [parseInt(`${popupK}`) ^ parseInt(`${overlayQ}`)];
      }
          let orangeuparrowI = 3;
         defaultroombgy = [basketballmatchdetailbgJ.length ^ orangeuparrowI];
      for (let w = 0; w < 2; w++) {
          let predictionbuttonJ = true;
          let friendsS = 2.0;
         defaultroombgy = [1 % (Math.max(parseInt(`${friendsS}`), 3))];
         predictionbuttonJ = !predictionbuttonJ;
         friendsS *= ((predictionbuttonJ ? 5 : 1) * (predictionbuttonJ ? 1 : 1));
      }
         defaultroombgy.push(basketballmatchdetailbgJ.length);
      uploadz = `${3 & parseInt(`${singaporeH}`)}`;

    setIsDialogOpen(!isDialogOpen);
  };

  return (
    // <SafeAreaView>
    <ScreenContainer>
      <View style={{ gap: spacing.m }}>
        <TitleWithBackButtonHeader
          title="活动规则"
          
        />

        <View style={{ marginBottom: 100, paddingLeft: 20, paddingRight: 35 }}>
          <Text style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}>
            每推荐成功1位好友，即可增加VIP会员30天，好友获得30天VIP会员
          </Text>

          { }
          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",

              marginTop: 10,
            }}
          >
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              1. 推荐成功条件
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              推荐好友
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              好友下载{APP_NAME_CONST} APP
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              好友注册{APP_NAME_CONST} APP（需填写邀请码）
            </Text>
          </View>

          <Text style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}>
            好友通过您的分享链接，下载并打开app后，注册成功（需填写邀请码）成为新用户即视为推荐成功，双方均可获得奖励
          </Text>

          { }
          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",

              marginTop: 10,
            }}
          >
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              2. 受邀者奖励内容
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              受邀者通过好友分享链接下载并注册（需填写邀请码）成为新用户后，即可获得30日VIP会员，需注意如使用同一台设备或同一个账号无法重复获得奖励
            </Text>
          </View>

          { }
          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",

              marginTop: 10,
            }}
          >
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              3. 附加规则
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              用户达成奖励条件后，奖励会立即生效
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              同一设备、同一账号视为同一用户
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              如果有发现用户存在恶意使用虚假账号领取奖励多情况，平台有权利取消奖励或保留终止活动的权利
            </Text>
          </View>
        </View>
      </View>
    </ScreenContainer>
    // </SafeAreaView>
  );
};

const styles = StyleSheet.create({
  text: {},
  bullet: {
    width: 6,
    height: 6,
    borderRadius: 4,
    backgroundColor: "#fff", 
    marginLeft: 5, 
    marginRight: 8, 
    marginTop: 8,
  },
});
