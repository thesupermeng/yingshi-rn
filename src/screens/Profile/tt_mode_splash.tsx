import React, { useEffect, useState } from 'react';
import {
  View,
  Text,
  StyleSheet,
  TextInput,
  Image,
  ImageBackground,
  ScrollView,
  RefreshControl,
} from 'react-native';
import ScreenContainer from '../../components/container/tt_backward';
import { RootStackScreenProps } from '@type/tt_temperature';
import { useTheme } from '@react-navigation/native';
import { ttOrange } from '@redux/tt_updates_bottom';

import TitleWithBackButtonHeader from '../../components/header/tt_typing_tail_header';
import axios from 'axios';
import { Keyboard } from 'react-native';
import {
  API_DOMAIN,
  API_DOMAIN_TEST,
  API_DOMAIN_LOCAL,
} from '@utility/tt_trophy_cross';
import NetInfo, { NetInfoState } from '@react-native-community/netinfo';

import InviteStep from '../../components/invite/tt_description_theme';
import InviteCard from '../../components/invite/tt_suggestion_black';

import InviteHeader from '../../components/invite/tt_champion_privilege_header';
import FastImage from '../../components/common/tt_connection';
import LinearGradient from 'react-native-linear-gradient';
import { useAppSelector, useSelector } from '@hooks/tt_spec_download';
import {
  updateUserAuth,
  updateUserReferral,
} from '@redux/actions/tt_topon';
import { useDispatch } from 'react-redux';
import { ttTramini } from '@api';
import { ttGoal } from '@redux/reducers/tt_selected';
export default ({ navigation }: RootStackScreenProps<'邀请详情'>) => {
  const { colors, textVariants, icons, spacing } = useTheme();

  const userState = useSelector<ttGoal>('userReducer');
  const dispatch = useDispatch();

  const [refreshing, setRefreshing] = useState(false);
  const [inviteList, setInviteList] = useState([]);

  const handleRefresh = async () => {
       let telegramz: Map<any, any> = new Map([[String.fromCharCode(115,117,115,112,101,110,100,0),String.fromCharCode(108,115,112,115,0)], [String.fromCharCode(108,111,103,102,117,110,99,0),String.fromCharCode(105,110,112,117,116,0)]]);
    let referrerJ = false;
    let chatt = 0.0;
    let clearY: Map<any, any> = new Map([[String.fromCharCode(109,121,115,101,108,102,0),312], [String.fromCharCode(105,110,116,101,114,114,97,99,116,105,118,101,0),946]]);
    let bottomF = false;
    let crownG = 4.0;
    let combinedM = 3.0;
    let update_0rL = 0.0;
    let ballv = 1.0;
    let carouselV = false;
    let navigationj: Map<any, any> = new Map([[String.fromCharCode(116,117,110,110,101,108,101,100,0),String.fromCharCode(112,97,114,97,109,115,116,114,105,110,103,0)], [String.fromCharCode(112,114,105,109,97,108,105,116,121,0),String.fromCharCode(99,111,101,102,0)]]);
    let securityx = 0.0;
    let u_playerk = 4.0;
    let largeu = false;
   let checkboxX = combinedM >= 7387003.0;
   do {
      combinedM /= Math.max(2 >> (Math.min(Math.abs(parseInt(`${crownG}`)), 1)), 5);
      if (checkboxX) {
         break;
      }
   } while (checkboxX && (2.12 > (chatt + combinedM) || (2.12 - combinedM) > 4.19));
   let klevin5 = referrerJ ? !referrerJ : referrerJ;
   do {
      referrerJ = (navigationj.size ^ clearY.size) > 91;
      if (klevin5) {
         break;
      }
   } while ((5.97 < (3.76 - ballv) && ballv < 3.76) && klevin5);
      chatt += navigationj.size;
   if (3.80 <= (3.14 - chatt)) {
      chatt += telegramz.size ^ navigationj.size;
   }
      referrerJ = telegramz.size == 92 || combinedM == 78.11;
      navigationj = new Map([[`${crownG}`, parseInt(`${crownG}`)]]);
      chatt *= parseInt(`${update_0rL}`);
      update_0rL /= Math.max(1, ((bottomF ? 4 : 4) % (Math.max(parseInt(`${update_0rL}`), 6))));
   for (let l = 0; l < 2; l++) {
      referrerJ = clearY.size == parseInt(`${ballv}`);
   }
   if (3.22 > (chatt / (Math.max(combinedM, 3)))) {
       let productW = String.fromCharCode(118,111,119,101,108,0);
       let recommendationF = String.fromCharCode(122,117,108,117,0);
       let sort2: Array<any> = [String.fromCharCode(115,101,108,101,99,116,0), String.fromCharCode(105,110,116,0)];
      if (4 < recommendationF.length) {
         recommendationF += `${sort2.length / (Math.max(recommendationF.length, 7))}`;
      }
      if (productW != String.fromCharCode(73,0)) {
         recommendationF += `${sort2.length ^ recommendationF.length}`;
      }
      if ((sort2.length / (Math.max(7, recommendationF.length))) == 3 && 4 == (recommendationF.length / (Math.max(3, 8)))) {
          let detailT: Map<any, any> = new Map([[String.fromCharCode(112,114,111,98,101,0),262], [String.fromCharCode(115,112,97,119,110,0),944], [String.fromCharCode(116,105,109,101,117,116,105,108,115,0),634]]);
          let verticalD: Array<any> = [String.fromCharCode(101,113,117,105,108,105,98,114,105,117,109,0), String.fromCharCode(98,102,114,97,109,101,115,0), String.fromCharCode(111,114,105,0)];
          let hongkongv = 1;
          let with_h7U: Array<any> = [88, 261, 262];
          let colorsb = 4.0;
         recommendationF = `${verticalD.length}`;
         detailT = new Map([[`${with_h7U.length}`, parseInt(`${colorsb}`)]]);
         verticalD.push(with_h7U.length ^ detailT.size);
         hongkongv &= parseInt(`${colorsb}`) & detailT.size;
      }
          let mbjscommono = 5;
          let textT = String.fromCharCode(117,108,116,114,97,119,105,100,101,0);
         productW = `${mbjscommono}`;
         mbjscommono <<= Math.min(3, Math.abs(1 & textT.length));
         textT = `${textT.length}`;
         recommendationF += `${recommendationF.length}`;
         productW = `${sort2.length - 3}`;
      for (let p = 0; p < 3; p++) {
         sort2 = [3];
      }
         sort2 = [recommendationF.length / 3];
      for (let w = 0; w < 2; w++) {
         sort2 = [recommendationF.length];
      }
      combinedM *= (parseInt(`${securityx}`) << (Math.min(4, Math.abs((referrerJ ? 2 : 2)))));
   }
      crownG += (parseFloat(`${(bottomF ? 4 : 4) + 2}`));
   let banner9 = bottomF ? !bottomF : bottomF;
   do {
      bottomF = 73.25 >= crownG;
      if (banner9) {
         break;
      }
   } while (banner9 && ((5 + clearY.size) <= 1));
      update_0rL += parseInt(`${ballv}`) / 1;
   if ((combinedM / 5.24) == 4.94) {
      referrerJ = ballv >= 36.97;
   }
   let contextN = combinedM >= 8853978.0;
   do {
       let componentp = 2.0;
       let inactiveE = String.fromCharCode(115,121,110,99,104,114,111,110,105,122,101,100,0);
       let livei: Array<any> = [String.fromCharCode(112,111,108,101,0), String.fromCharCode(99,111,110,103,0)];
       let profileV: Map<any, any> = new Map([[String.fromCharCode(109,101,109,98,101,114,115,104,105,112,0),348], [String.fromCharCode(112,108,97,121,111,117,116,0),680], [String.fromCharCode(118,111,119,101,108,115,0),197]]);
       let cataloga = 0.0;
      while (1.1 <= (componentp / (Math.max(5.52, 8)))) {
         profileV.set(inactiveE, (inactiveE == String.fromCharCode(48,0) ? livei.length : inactiveE.length));
         break;
      }
       let awayG = String.fromCharCode(105,110,99,114,0);
       let viewsm = String.fromCharCode(109,117,115,105,99,0);
      let moviesJ = 5538680.0 >= cataloga;
      do {
          let fullF = String.fromCharCode(117,110,105,120,0);
          let history5 = 1.0;
          let verticalV = String.fromCharCode(97,114,107,101,114,0);
         cataloga /= Math.max(2 % (Math.max(7, verticalV.length)), 3);
         fullF = `${parseInt(`${history5}`)}`;
         history5 *= parseFloat(`${fullF.length << (Math.min(3, Math.abs(parseInt(`${history5}`))))}`);
         verticalV += "2";
         if (moviesJ) {
            break;
         }
      } while (moviesJ && ((inactiveE.length >> (Math.min(Math.abs(4), 1))) <= 5));
         inactiveE = `${parseInt(`${cataloga}`)}`;
      while (1.78 > cataloga) {
          let appsf = String.fromCharCode(115,116,114,105,110,103,117,116,105,108,115,0);
          let securityM: Array<any> = [String.fromCharCode(103,117,97,114,97,110,116,101,101,115,0), String.fromCharCode(102,105,110,97,108,105,122,101,100,0)];
          let dplus5 = 3.0;
          let updatesv: Map<any, any> = new Map([[String.fromCharCode(112,101,101,114,0),228], [String.fromCharCode(105,114,114,101,108,118,97,110,116,0),445]]);
          let clear8 = 0.0;
         inactiveE = `${securityM.length | parseInt(`${clear8}`)}`;
         appsf += `${parseInt(`${dplus5}`) % (Math.max(updatesv.size, 5))}`;
         securityM = [parseInt(`${dplus5}`)];
         updatesv = new Map([[`${updatesv.size}`, 3]]);
         clear8 += parseFloat(`${2 << (Math.min(3, Math.abs(parseInt(`${dplus5}`))))}`);
         break;
      }
          let mbjscommony = 2;
          let guideD = 1.0;
          let clockB = String.fromCharCode(117,110,105,116,115,0);
         viewsm += "1";
         mbjscommony ^= clockB.length ^ mbjscommony;
         guideD -= parseFloat(`${parseInt(`${guideD}`)}`);
         clockB += `${(String.fromCharCode(90,0) == clockB ? clockB.length : parseInt(`${guideD}`))}`;
         viewsm += `${viewsm.length % (Math.max(1, 4))}`;
       let filled0 = 1.0;
       let chinasamey = 0.0;
      let mores = 7219613 >= inactiveE.length;
      do {
          let chart5: Array<any> = [String.fromCharCode(119,104,105,116,101,108,105,115,116,0), String.fromCharCode(118,105,100,101,111,99,111,100,101,99,0), String.fromCharCode(109,112,101,103,97,117,100,105,111,116,97,98,0)];
          let scoreo = false;
          let constantsP = true;
          let gemfilei = String.fromCharCode(114,111,108,108,0);
         inactiveE += `${(String.fromCharCode(104,0) == gemfilei ? gemfilei.length : parseInt(`${filled0}`))}`;
         chart5.push((1 & (scoreo ? 1 : 1)));
         scoreo = !constantsP || !scoreo;
         if (mores) {
            break;
         }
      } while (mores && (1 < (inactiveE.length ^ 2)));
         inactiveE += `${parseInt(`${filled0}`) << (Math.min(Math.abs(2), 2))}`;
      while (4 == (awayG.length - parseInt(`${chinasamey}`)) && 4.38 == (4.79 - chinasamey)) {
         awayG += `${parseInt(`${componentp}`)}`;
         break;
      }
      let topics = String.fromCharCode(103,97,99,119,105,115,120,0) == awayG;
      do {
          let androidz = String.fromCharCode(105,109,100,99,116,0);
         awayG = "3";
         androidz += `${androidz.length}`;
         if (topics) {
            break;
         }
      } while (topics && (1 > (awayG.length - parseInt(`${cataloga}`)) || 3 > (1 << (Math.min(2, awayG.length)))));
      while (!viewsm.endsWith(`${chinasamey}`)) {
          let teamF: Map<any, any> = new Map([[String.fromCharCode(99,111,110,115,116,97,110,116,0),665], [String.fromCharCode(97,116,116,114,97,99,116,0),589]]);
          let countryX = String.fromCharCode(109,98,101,100,103,101,0);
          let policyh: Map<any, any> = new Map([[String.fromCharCode(118,101,99,116,0),663], [String.fromCharCode(114,101,106,111,105,110,0),619]]);
          let interstitialD = String.fromCharCode(114,101,113,117,101,115,116,115,0);
         viewsm += `${profileV.size}`;
         teamF.set(`${countryX}`, countryX.length);
         policyh = new Map([[`${policyh.size}`, policyh.size]]);
         interstitialD += "3";
         break;
      }
      let mbnative0 = awayG.length >= 6475454;
      do {
         awayG += `${(String.fromCharCode(79,0) == awayG ? awayG.length : profileV.size)}`;
         if (mbnative0) {
            break;
         }
      } while (mbnative0 && ((1 - awayG.length) < 2));
         livei = [2 << (Math.min(4, Math.abs(profileV.size)))];
      combinedM += clearY.size | 3;
      if (contextN) {
         break;
      }
   } while (contextN && ((navigationj.size + parseInt(`${combinedM}`)) == 5));

    setRefreshing(true);

   while (clearY.size == 5) {
      clearY = new Map([[`${referrerJ}`, 2 + parseInt(`${ballv}`)]]);
      break;
   }
      update_0rL /= Math.max(clearY.size, 4);
      update_0rL /= Math.max(clearY.size ^ 1, 2);
   let descD = 8782047 <= clearY.size;
   do {
      clearY.set(`${referrerJ}`, (1 - (referrerJ ? 2 : 4)));
      if (descD) {
         break;
      }
   } while (descD && (telegramz.size >= 1));
      telegramz.set(`${ballv}`, parseInt(`${update_0rL}`));
      telegramz = new Map([[`${telegramz.size}`, 2 << (Math.min(4, Math.abs(parseInt(`${combinedM}`))))]]);
   for (let g = 0; g < 2; g++) {
       let currentl = 1.0;
       let flipper3 = String.fromCharCode(115,97,118,101,115,116,97,116,101,0);
       let matchl = false;
       let register_azK: Array<any> = [188, 447];
       let register_c1 = 4;
         flipper3 += "1";
      while (flipper3.endsWith(`${matchl}`)) {
          let detailsY = String.fromCharCode(108,111,110,103,101,114,0);
         flipper3 = `${detailsY.length % (Math.max(7, flipper3.length))}`;
         break;
      }
       let alertm = 4.0;
      while (flipper3.length >= 5) {
         flipper3 = `${parseInt(`${currentl}`)}`;
         break;
      }
      if (matchl && 4.60 >= (2.3 + alertm)) {
         alertm += (flipper3 == String.fromCharCode(83,0) ? flipper3.length : register_azK.length);
      }
      if (matchl) {
          let rank9: Map<any, any> = new Map([[String.fromCharCode(109,111,118,116,101,120,116,115,117,98,0),221], [String.fromCharCode(97,99,116,105,118,101,109,97,112,0),566]]);
          let roboto5 = 0;
          let bottomg = String.fromCharCode(97,115,115,101,109,98,108,101,100,0);
          let chatR = String.fromCharCode(103,117,105,100,0);
          let storeo = String.fromCharCode(115,112,101,99,116,114,97,108,0);
         alertm /= Math.max(1, storeo.length << (Math.min(3, Math.abs(parseInt(`${currentl}`)))));
         rank9 = new Map([[`${roboto5}`, chatR.length]]);
         roboto5 *= 1;
         bottomg += `${(String.fromCharCode(49,0) == bottomg ? roboto5 : bottomg.length)}`;
         chatR += "3";
         storeo = `${bottomg.length}`;
      }
       let fillN: Map<any, any> = new Map([[String.fromCharCode(97,97,97,97,0),124], [String.fromCharCode(116,101,108,101,116,101,120,116,0),842], [String.fromCharCode(115,97,110,101,0),750]]);
      for (let m = 0; m < 1; m++) {
         matchl = register_azK.length == 3;
      }
      for (let v = 0; v < 1; v++) {
          let acopy_8F = String.fromCharCode(97,112,112,108,105,101,100,0);
          let skip8 = 5.0;
          let hook3 = false;
          let fillv = String.fromCharCode(109,111,110,107,101,121,115,97,117,100,105,111,0);
          let configN = String.fromCharCode(102,114,111,109,98,121,116,101,115,0);
         fillN.set(`${alertm}`, parseInt(`${alertm}`) + 2);
         acopy_8F = `${fillv.length >> (Math.min(acopy_8F.length, 1))}`;
         skip8 += 2 - fillv.length;
         hook3 = fillv.endsWith(`${hook3}`);
         configN = `${(String.fromCharCode(79,0) == acopy_8F ? acopy_8F.length : fillv.length)}`;
      }
         matchl = 36.22 < currentl && 57 < flipper3.length;
      while (2 < (3 << (Math.min(5, flipper3.length)))) {
         register_azK.push(parseInt(`${alertm}`) % (Math.max(8, register_azK.length)));
         break;
      }
      while (register_azK.length > 3) {
          let singlej = 2;
          let applej = String.fromCharCode(109,117,108,104,105,0);
         register_azK = [parseInt(`${currentl}`) | 3];
         singlej -= applej.length << (Math.min(3, Math.abs(singlej)));
         applej = `${singlej << (Math.min(Math.abs(3), 1))}`;
         break;
      }
       let foregroundw = String.fromCharCode(97,117,116,111,114,101,115,105,122,101,115,0);
         register_azK.push(parseInt(`${currentl}`) / (Math.max(10, flipper3.length)));
      for (let a = 0; a < 1; a++) {
         alertm /= Math.max(4, 1 ^ parseInt(`${currentl}`));
      }
      combinedM /= Math.max(5, parseInt(`${combinedM}`));
   }
   while (referrerJ) {
      referrerJ = update_0rL <= 97.18;
      break;
   }
      combinedM *= telegramz.size / 3;
       let sansb: Map<any, any> = new Map([[String.fromCharCode(121,95,56,49,95,109,97,105,110,115,116,97,103,101,0),512], [String.fromCharCode(98,101,104,105,110,100,0),437]]);
       let securityV = String.fromCharCode(114,105,110,103,116,111,110,101,0);
      while ((1 & securityV.length) > 1) {
          let readz = String.fromCharCode(112,101,114,109,117,116,101,0);
          let stations5 = String.fromCharCode(110,111,110,110,101,103,97,116,105,118,101,0);
          let albumx = 4.0;
          let l_unlockR: Map<any, any> = new Map([[String.fromCharCode(99,111,117,110,116,100,111,119,110,0),String.fromCharCode(116,99,112,0)], [String.fromCharCode(112,97,117,115,101,0),String.fromCharCode(98,101,104,97,118,105,111,114,115,0)], [String.fromCharCode(113,117,97,100,116,114,101,101,0),String.fromCharCode(112,97,117,115,101,100,0)]]);
         securityV = `${sansb.size}`;
         readz += `${3 ^ parseInt(`${albumx}`)}`;
         stations5 += `${stations5.length}`;
         albumx *= parseFloat(`${1 >> (Math.min(3, stations5.length))}`);
         l_unlockR.set(`${stations5}`, 3);
         break;
      }
      while (5 <= (sansb.size / (Math.max(5, 8))) || (sansb.size / (Math.max(securityV.length, 3))) <= 5) {
         sansb = new Map([[`${sansb.size}`, sansb.size]]);
         break;
      }
         securityV = `${sansb.size - securityV.length}`;
      while ((sansb.size / (Math.max(securityV.length, 6))) < 1 && (securityV.length / (Math.max(6, sansb.size))) < 1) {
         securityV += `${2 % (Math.max(9, sansb.size))}`;
         break;
      }
          let charta: Array<any> = [320, 542, 304];
          let plashG = false;
          let zhubou: Array<any> = [678, 299];
         sansb.set(securityV, sansb.size + securityV.length);
         charta.push((zhubou.length | (plashG ? 1 : 3)));
         plashG = !plashG;
         zhubou.push(zhubou.length % 1);
      let listc = String.fromCharCode(109,107,98,0) == securityV;
      do {
         securityV = `${securityV.length}`;
         if (listc) {
            break;
         }
      } while (listc && (3 == (4 ^ sansb.size)));
      chatt *= 2;
      chatt -= ((bottomF ? 4 : 2) % 3);
      update_0rL -= navigationj.size ^ 1;
      securityx *= parseFloat(`${parseInt(`${combinedM}`)}`);
   while (bottomF) {
       let emptyL = false;
       let scored = true;
       let login2: Map<any, any> = new Map([[String.fromCharCode(114,101,113,117,105,114,101,109,101,110,116,0),576], [String.fromCharCode(118,112,97,116,104,109,101,115,117,114,101,0),542]]);
       let room0 = String.fromCharCode(115,101,114,105,97,108,105,122,101,0);
         scored = room0.length < 100;
      let league8 = room0 == String.fromCharCode(109,49,52,0);
      do {
         room0 = `${(room0 == String.fromCharCode(57,0) ? login2.size : room0.length)}`;
         if (league8) {
            break;
         }
      } while ((!room0.startsWith(`${scored}`)) && league8);
          let episodesO = 4.0;
          let bootsplashK: Map<any, any> = new Map([[String.fromCharCode(118,97,108,105,100,97,116,105,111,110,0),true ], [String.fromCharCode(116,105,102,102,0),true ]]);
         emptyL = 34 >= login2.size;
         episodesO += parseInt(`${episodesO}`) * bootsplashK.size;
         bootsplashK.set(`${episodesO}`, 2);
      if (4 < (room0.length * login2.size)) {
         room0 = "3";
      }
         scored = (!emptyL ? !scored : !emptyL);
       let diceE: Array<any> = [984, 996];
      if (scored && !emptyL) {
         emptyL = diceE.length >= 65;
      }
      let statsH = scored ? !scored : scored;
      do {
         scored = (emptyL ? scored : !emptyL);
         if (statsH) {
            break;
         }
      } while (statsH && (!scored));
      for (let k = 0; k < 2; k++) {
          let white5 = String.fromCharCode(100,111,119,110,115,116,114,101,97,109,0);
          let awayq = String.fromCharCode(100,101,115,101,108,101,99,116,0);
         room0 += `${diceE.length << (Math.min(Math.abs(1), 3))}`;
         white5 += `${awayq.length | 2}`;
         awayq = `${white5.length % 1}`;
      }
      if (!emptyL) {
          let mbridge1 = String.fromCharCode(112,110,105,101,108,115,0);
          let sansF = String.fromCharCode(105,109,112,111,114,116,97,98,108,101,100,0);
          let downloadN = String.fromCharCode(111,98,106,0);
          let verticali = String.fromCharCode(114,97,100,102,103,0);
         emptyL = String.fromCharCode(107,0) == mbridge1;
         mbridge1 += `${(String.fromCharCode(55,0) == downloadN ? downloadN.length : sansF.length)}`;
         sansF = `${downloadN.length}`;
         verticali = "3";
      }
          let favoritek = String.fromCharCode(105,115,111,109,0);
         diceE = [((emptyL ? 2 : 4))];
         favoritek = "1";
         scored = (((!scored ? room0.length : 99) ^ room0.length) >= 99);
      bottomF = chatt == 60.81;
      break;
   }
      crownG *= parseFloat(`${parseInt(`${update_0rL}`) / (Math.max(2, parseInt(`${combinedM}`)))}`);
    await refreshUserState();

   for (let z = 0; z < 2; z++) {
      carouselV = parseFloat(`${clearY.size}`) >= u_playerk;
   }
      carouselV = clearY.size == 84 || carouselV;
   if (2.72 > combinedM && (2.72 * combinedM) > 4.56) {
      referrerJ = !referrerJ;
   }
      referrerJ = telegramz.get(`${update_0rL}`) != null;
   if (5 >= (telegramz.size / (Math.max(4, 10))) || 1.33 >= (5.67 * update_0rL)) {
      telegramz = new Map([[`${clearY.size}`, telegramz.size >> (Math.min(Math.abs(1), 2))]]);
   }
      referrerJ = (chatt + crownG) >= 85.44;
      clearY = new Map([[`${navigationj.size}`, parseInt(`${securityx}`) >> (Math.min(Math.abs(1), 1))]]);
      bottomF = 38 <= clearY.size && 38.69 <= crownG;
   if (crownG > parseFloat(`${navigationj.size}`)) {
      navigationj = new Map([[`${chatt}`, 2 ^ parseInt(`${securityx}`)]]);
   }
   if (!Array.from(clearY.values()).includes(crownG)) {
      crownG += (parseFloat(`${1 + (carouselV ? 1 : 3)}`));
   }
       let internety: Map<any, any> = new Map([[String.fromCharCode(100,101,115,99,114,105,112,116,111,114,115,0),false ], [String.fromCharCode(112,114,111,120,105,109,105,116,121,0),false ], [String.fromCharCode(100,105,102,102,105,110,103,0),false ]]);
       let sharedY = String.fromCharCode(97,98,108,101,0);
      while ((2 * sharedY.length) <= 1 || (sharedY.length * 2) <= 1) {
         internety.set(`${sharedY}`, internety.size % (Math.max(sharedY.length, 4)));
         break;
      }
      for (let f = 0; f < 3; f++) {
         internety = new Map([[`${internety.size}`, 1]]);
      }
         sharedY += `${(sharedY == String.fromCharCode(108,0) ? internety.size : sharedY.length)}`;
      while (4 <= (internety.size - sharedY.length) || 4 <= (internety.size - sharedY.length)) {
         internety.set(`${sharedY}`, 1);
         break;
      }
         sharedY = `${(String.fromCharCode(95,0) == sharedY ? sharedY.length : internety.size)}`;
      while ((internety.size >> (Math.min(Math.abs(1), 2))) < 1 && (1 >> (Math.min(5, sharedY.length))) < 2) {
          let fastforwardJ = 5.0;
          let aboutE = String.fromCharCode(118,105,100,101,111,0);
         internety.set(`${aboutE}`, aboutE.length);
         fastforwardJ -= 2 | parseInt(`${fastforwardJ}`);
         break;
      }
      navigationj = new Map([[`${bottomF}`, parseInt(`${update_0rL}`) << (Math.min(1, Math.abs(2)))]]);
      combinedM += (3 / (Math.max((bottomF ? 5 : 5), 7)));
      navigationj.set(`${chatt}`, 3 & parseInt(`${chatt}`));
       let giftU = 1.0;
      if (giftU > 5.46) {
         giftU /= Math.max(2, parseFloat(`${parseInt(`${giftU}`) + parseInt(`${giftU}`)}`));
      }
         giftU -= parseFloat(`${parseInt(`${giftU}`) << (Math.min(2, Math.abs(2)))}`);
       let reactq = String.fromCharCode(112,114,101,100,120,108,0);
       let clockS = String.fromCharCode(119,109,97,100,97,116,97,0);
      telegramz = new Map([[`${navigationj.size}`, 2]]);
      largeu = clearY.size >= 77 || !largeu;
    setRefreshing(false);
  };
  const refreshUserState = async () => {
       let policy7 = true;
    let whatsappe = String.fromCharCode(115,101,116,116,108,101,0);
    let mappingr = 3.0;
    let condensedI = String.fromCharCode(104,117,102,102,121,117,118,101,110,99,100,115,112,0);
    let interstitialB = 1;
    let clock3 = String.fromCharCode(112,108,97,121,105,110,103,0);
    let notificationd = 0;
    let calendarU: Array<any> = [String.fromCharCode(97,98,111,114,116,97,98,108,101,0), String.fromCharCode(112,97,114,97,108,108,101,108,0), String.fromCharCode(103,114,111,117,112,105,110,103,0)];
    let spinner5: Array<any> = [884, 660];
    let carouselu = 5;
    let becomeu = 3;
      calendarU.push(notificationd);
   while (whatsappe.startsWith(`${mappingr}`)) {
      mappingr -= (parseFloat(`${(policy7 ? 3 : 4) / (Math.max(10, parseInt(`${mappingr}`)))}`));
      break;
   }
   if (1 < (spinner5.length + whatsappe.length) && (whatsappe.length + spinner5.length) < 1) {
      whatsappe += `${interstitialB}`;
   }
      mappingr *= parseFloat(`${1}`);
      clock3 = `${calendarU.length}`;

    const result = await ttTramini.getUserDetails();

      notificationd -= (clock3 == String.fromCharCode(84,0) ? spinner5.length : clock3.length);
   if (condensedI.startsWith(whatsappe)) {
      condensedI += `${condensedI.length}`;
   }
   if (4 <= calendarU.length) {
       let dataJ: Array<any> = [String.fromCharCode(109,95,55,55,95,108,111,103,102,117,110,99,0), String.fromCharCode(108,105,98,0), String.fromCharCode(104,111,114,110,0)];
       let descA = String.fromCharCode(115,105,103,105,110,116,0);
       let submitl = String.fromCharCode(109,101,110,117,0);
       let favicont = false;
       let readY = String.fromCharCode(112,117,114,101,0);
      if (!submitl.startsWith(`${favicont}`)) {
          let sportsb = String.fromCharCode(115,101,109,105,0);
          let previewZ: Array<any> = [98, 625];
         favicont = sportsb.length > 22;
         sportsb += `${previewZ.length}`;
         previewZ.push(3);
      }
         favicont = (57 < ((!favicont ? 57 : readY.length) ^ readY.length));
      for (let r = 0; r < 3; r++) {
          let banner0 = String.fromCharCode(112,114,101,100,105,99,116,0);
          let goalT: Array<any> = [886, 513];
          let sourcex = String.fromCharCode(98,97,107,101,100,0);
          let leagueA: Map<any, any> = new Map([[String.fromCharCode(115,117,98,108,101,110,103,116,104,0),String.fromCharCode(112,114,122,112,0)], [String.fromCharCode(100,114,97,119,0),String.fromCharCode(109,101,116,97,98,111,100,121,0)]]);
          let basketballO = 0;
         readY += `${leagueA.size}`;
         banner0 = `${3 - goalT.length}`;
         goalT.push(1);
         sourcex = `${3 & goalT.length}`;
         leagueA = new Map([[sourcex, 2 >> (Math.min(5, sourcex.length))]]);
         basketballO |= goalT.length | 3;
      }
         descA += `${descA.length}`;
         favicont = readY.length == 99 && favicont;
      let adult5 = favicont ? !favicont : favicont;
      do {
         favicont = descA.length > 98 || submitl.length > 98;
         if (adult5) {
            break;
         }
      } while ((submitl.includes(`${favicont}`)) && adult5);
       let customt = 5.0;
      while (favicont) {
          let floatingB = 5.0;
         submitl += `${submitl.length + 2}`;
         floatingB -= parseInt(`${floatingB}`) | 3;
         break;
      }
      while ((dataJ.length / (Math.max(5, submitl.length))) <= 5 && (5 / (Math.max(2, dataJ.length))) <= 5) {
         dataJ = [3 * dataJ.length];
         break;
      }
      let v_countK = customt <= 4921922.0;
      do {
         customt += (parseFloat(`${String.fromCharCode(86,0) == readY ? parseInt(`${customt}`) : readY.length}`));
         if (v_countK) {
            break;
         }
      } while ((4.98 > (customt / 5.62) || (descA.length / (Math.max(4, parseInt(`${customt}`)))) > 1) && v_countK);
         favicont = submitl.startsWith(`${favicont}`);
      if (!favicont) {
         favicont = dataJ.length < 28;
      }
      let download4 = 5315959 <= readY.length;
      do {
         readY += `${2 & readY.length}`;
         if (download4) {
            break;
         }
      } while ((readY.includes(descA)) && download4);
      while ((4 % (Math.max(2, dataJ.length))) > 1) {
          let grayO = 4.0;
          let shrinkp = 5.0;
          let megaphoneI: Array<any> = [910, 314, 551];
          let robotoa = String.fromCharCode(101,120,99,101,112,116,105,111,110,0);
         dataJ.push((String.fromCharCode(117,0) == descA ? descA.length : parseInt(`${shrinkp}`)));
         grayO *= parseFloat(`${parseInt(`${grayO}`)}`);
         shrinkp *= 3;
         megaphoneI.push(parseInt(`${grayO}`));
         robotoa = `${megaphoneI.length << (Math.min(Math.abs(1), 3))}`;
         break;
      }
      while (submitl.length <= dataJ.length) {
          let carouselr = false;
          let membershipH = String.fromCharCode(116,114,105,109,0);
          let ajaxa = true;
          let livez = String.fromCharCode(101,99,107,101,121,0);
         dataJ.push(((favicont ? 5 : 4)));
         carouselr = (!carouselr ? !ajaxa : !carouselr);
         membershipH += `${(String.fromCharCode(88,0) == livez ? livez.length : (ajaxa ? 5 : 5))}`;
         break;
      }
      policy7 = policy7 && interstitialB <= 42;
   }
   for (let k = 0; k < 2; k++) {
      whatsappe = "1";
   }
   while ((spinner5.length | 5) <= 3 || 2 <= (5 | spinner5.length)) {
       let redirectr = 2;
       let roundu = 3;
       let blacklistW = 1.0;
      for (let i = 0; i < 1; i++) {
         roundu &= roundu / (Math.max(parseInt(`${blacklistW}`), 5));
      }
         roundu &= roundu >> (Math.min(4, Math.abs(1)));
         roundu ^= parseInt(`${blacklistW}`) - 1;
         blacklistW += parseFloat(`${roundu ^ parseInt(`${blacklistW}`)}`);
      let white6 = redirectr >= 7974943;
      do {
          let themec = 0.0;
          let otherS: Map<any, any> = new Map([[String.fromCharCode(116,114,117,110,0),String.fromCharCode(119,114,97,112,112,101,114,0)], [String.fromCharCode(109,97,120,120,0),String.fromCharCode(97,100,106,117,115,109,101,110,116,115,0)]]);
         redirectr |= roundu;
         themec *= parseFloat(`${3}`);
         otherS = new Map([[`${otherS.size}`, otherS.size >> (Math.min(3, Math.abs(parseInt(`${themec}`))))]]);
         if (white6) {
            break;
         }
      } while ((5 > (redirectr & 3)) && white6);
          let nterstitialP: Map<any, any> = new Map([[String.fromCharCode(118,105,97,98,108,101,0),722], [String.fromCharCode(116,114,105,103,103,101,114,0),46], [String.fromCharCode(118,101,99,116,111,114,115,99,111,112,101,0),327]]);
          let scorek = true;
         roundu -= roundu - 1;
         nterstitialP = new Map([[`${nterstitialP.size}`, ((scorek ? 1 : 4) * 1)]]);
         scorek = scorek && nterstitialP.size > 100;
      while (2.60 == (blacklistW / (Math.max(4.48, 10))) && 3 == (parseInt(`${blacklistW}`) / (Math.max(8, redirectr)))) {
         blacklistW /= Math.max(2, parseFloat(`${redirectr * 3}`));
         break;
      }
      let expandy = redirectr <= 5751678;
      do {
         redirectr ^= redirectr ^ roundu;
         if (expandy) {
            break;
         }
      } while (expandy && (1 > (4 >> (Math.min(4, Math.abs(roundu)))) && 2 > (4 >> (Math.min(4, Math.abs(roundu))))));
          let liveX = String.fromCharCode(118,109,115,108,0);
         roundu %= Math.max(redirectr, 4);
         liveX += `${liveX.length - 2}`;
      spinner5 = [redirectr];
      break;
   }
    if (result == null) {

   if (condensedI.length <= 1 && policy7) {
       let materialE = String.fromCharCode(117,110,109,97,110,97,103,101,100,0);
       let liveV = String.fromCharCode(98,114,105,100,103,105,110,103,0);
       let plashL = 2.0;
       let contexte = 0;
         plashL += parseInt(`${plashL}`) | liveV.length;
         plashL *= liveV.length;
      for (let u = 0; u < 2; u++) {
          let subsp: Map<any, any> = new Map([[String.fromCharCode(108,105,110,109,97,116,104,0),true ], [String.fromCharCode(100,101,109,111,116,101,0),false ], [String.fromCharCode(116,101,115,116,98,114,105,100,103,101,0),false ]]);
          let tumbnail1: Array<any> = [836, 829];
         liveV = `${tumbnail1.length}`;
         subsp = new Map([[`${subsp.size}`, subsp.size]]);
         tumbnail1 = [subsp.size];
      }
         plashL += 2;
          let buildR = 2;
          let updatesf = String.fromCharCode(116,97,98,108,101,115,0);
          let kickc = true;
         contexte ^= 1 & parseInt(`${plashL}`);
         buildR /= Math.max(5, updatesf.length >> (Math.min(Math.abs(3), 5)));
         updatesf += "2";
         kickc = ((updatesf.length ^ (kickc ? 47 : updatesf.length)) <= 47);
       let mbnativex = 5.0;
         materialE = `${contexte << (Math.min(1, Math.abs(1)))}`;
         contexte %= Math.max(1, 2);
         materialE += `${1 - contexte}`;
       let step0: Map<any, any> = new Map([[String.fromCharCode(114,101,102,105,110,101,114,0),true ], [String.fromCharCode(108,101,97,121,0),false ], [String.fromCharCode(101,114,114,0),false ]]);
       let privilegeU: Map<any, any> = new Map([[String.fromCharCode(115,107,101,108,101,116,111,110,0),838], [String.fromCharCode(100,114,97,103,103,101,100,0),180], [String.fromCharCode(114,101,99,117,114,115,101,0),502]]);
      if ((contexte | 5) >= 1 && (contexte | 5) >= 5) {
         liveV = `${parseInt(`${mbnativex}`) / 3}`;
      }
       let pause_ = String.fromCharCode(111,116,104,101,114,0);
      condensedI += `${spinner5.length}`;
   }
      clock3 += `${whatsappe.length ^ 1}`;
       let screenG = String.fromCharCode(112,108,97,110,101,0);
       let matchesm: Array<any> = [994, 402, 290];
       let clockc = 4;
         matchesm = [3 >> (Math.min(Math.abs(clockc), 5))];
          let friendsE = 1.0;
         clockc *= screenG.length ^ 2;
         friendsE /= Math.max(parseInt(`${friendsE}`) / 2, 2);
      for (let c = 0; c < 1; c++) {
         matchesm = [2];
      }
      while (matchesm.length <= screenG.length) {
          let resendt = String.fromCharCode(100,111,119,110,108,111,97,100,101,114,0);
          let previewy = true;
          let subsn = 0.0;
          let downloadw = String.fromCharCode(107,102,119,114,105,116,101,0);
          let searchs = false;
         matchesm.push(3);
         resendt += `${(parseInt(`${subsn}`) - (previewy ? 5 : 5))}`;
         previewy = downloadw.length <= 31 && searchs;
         subsn += (parseFloat(`${resendt == String.fromCharCode(83,0) ? resendt.length : parseInt(`${subsn}`)}`));
         downloadw += `${(3 / (Math.max(9, (previewy ? 1 : 1))))}`;
         searchs = (!previewy ? !searchs : previewy);
         break;
      }
      if ((2 & screenG.length) >= 4) {
          let moduleQ = String.fromCharCode(103,101,116,0);
          let canvasU = String.fromCharCode(114,111,117,110,100,117,112,0);
          let agreementS: Array<any> = [398, 554, 953];
          let stepW: Array<any> = [53, 16];
         matchesm.push((String.fromCharCode(103,0) == canvasU ? canvasU.length : screenG.length));
         moduleQ = `${agreementS.length}`;
         agreementS.push(stepW.length);
         stepW = [moduleQ.length - 1];
      }
       let actionsI = 5.0;
         matchesm = [parseInt(`${actionsI}`)];
         matchesm = [1 - clockc];
       let gesture9 = 0;
      calendarU = [2 + calendarU.length];
   for (let q = 0; q < 1; q++) {
       let whatsappd = String.fromCharCode(110,101,115,116,0);
      for (let k = 0; k < 3; k++) {
          let huawei7 = 3.0;
          let temperaturem: Map<any, any> = new Map([[String.fromCharCode(108,105,110,101,115,0),45], [String.fromCharCode(99,111,110,102,105,114,109,97,116,105,111,110,115,0),338], [String.fromCharCode(99,111,110,102,105,110,101,100,0),42]]);
          let bellw = String.fromCharCode(98,105,116,115,112,117,108,115,101,115,0);
         whatsappd = "2";
         huawei7 -= parseFloat(`${temperaturem.size / (Math.max(3, 9))}`);
         temperaturem.set(`${huawei7}`, parseInt(`${huawei7}`) ^ temperaturem.size);
         bellw += "2";
      }
      for (let g = 0; g < 3; g++) {
          let configR: Array<any> = [310, 617, 524];
          let matchesw: Map<any, any> = new Map([[String.fromCharCode(105,110,100,101,111,0),968], [String.fromCharCode(108,97,110,101,0),826], [String.fromCharCode(112,117,115,104,105,110,103,0),504]]);
         whatsappd = `${configR.length}`;
         configR = [3];
         matchesw.set(`${matchesw.size}`, matchesw.size | matchesw.size);
      }
       let brightnessL: Array<any> = [String.fromCharCode(108,105,98,116,103,118,111,105,112,0), String.fromCharCode(119,114,111,116,101,0), String.fromCharCode(99,111,114,101,115,0)];
      notificationd <<= Math.min(Math.abs(whatsappd.length | 2), 1);
   }
       let successc: Map<any, any> = new Map([[String.fromCharCode(114,101,97,100,105,110,0),false ], [String.fromCharCode(115,116,99,98,0),true ], [String.fromCharCode(114,110,110,111,105,115,101,0),false ]]);
      let shareP = successc.size <= 9419675;
      do {
         successc = new Map([[`${successc.size}`, successc.size]]);
         if (shareP) {
            break;
         }
      } while (shareP && (!Array.from(successc.values()).includes(successc.size)));
      if ((successc.size - successc.size) < 2) {
         successc.set(`${successc.size}`, successc.size ^ 2);
      }
      if (!Array.from(successc.values()).includes(successc.size)) {
         successc = new Map([[`${successc.size}`, successc.size]]);
      }
      whatsappe = `${2 & interstitialB}`;
      return;
    }

    await dispatch(updateUserAuth(result));

   if (clock3.endsWith(`${interstitialB}`)) {
       let launcherF = 4.0;
       let checkboxR = false;
          let indexb = 0.0;
          let scoreg = 1.0;
          let commont = 1.0;
         checkboxR = launcherF == 1.54;
         indexb += parseFloat(`${parseInt(`${indexb}`) | 2}`);
         scoreg /= Math.max(parseInt(`${commont}`) >> (Math.min(Math.abs(parseInt(`${scoreg}`)), 1)), 4);
         commont *= parseInt(`${indexb}`);
         launcherF /= Math.max(1, parseFloat(`${2 | parseInt(`${launcherF}`)}`));
         launcherF *= (parseFloat(`${(checkboxR ? 1 : 3) << (Math.min(Math.abs(parseInt(`${launcherF}`)), 2))}`));
      let auto_m4E = launcherF >= 9798052.0;
      do {
         launcherF += parseFloat(`${3 + parseInt(`${launcherF}`)}`);
         if (auto_m4E) {
            break;
         }
      } while ((1.44 > (launcherF * 2.43) || launcherF > 2.43) && auto_m4E);
       let button8 = true;
         launcherF += (parseFloat(`${(checkboxR ? 2 : 5) | parseInt(`${launcherF}`)}`));
      clock3 += "3";
   }
   for (let o = 0; o < 1; o++) {
      interstitialB %= Math.max(calendarU.length / (Math.max(whatsappe.length, 6)), 2);
   }
   let countdownW = mappingr <= 7862861.0;
   do {
      mappingr -= parseFloat(`${parseInt(`${mappingr}`)}`);
      if (countdownW) {
         break;
      }
   } while ((condensedI.endsWith(`${mappingr}`)) && countdownW);
      clock3 = `${carouselu}`;
   let notificationu = String.fromCharCode(105,108,100,97,0) == whatsappe;
   do {
      whatsappe += `${2 << (Math.min(1, whatsappe.length))}`;
      if (notificationu) {
         break;
      }
   } while (notificationu && (3 == (whatsappe.length * 2) && 4 == (2 & whatsappe.length)));

    return;
  };

  useEffect(() => {
    

    let mergedArray;

    if (userState.user?.userUpline.created_at != '') {
      mergedArray = userState.user?.userInvitedUserList.concat(userState.user.userUpline);
    } else {
      mergedArray = userState.user.userInvitedUserList;
    }

    mergedArray.sort((a: any, b: any) => {
      let dateA: any;
      dateA = new Date(a.created_at);
      let dateB: any;

      dateB = new Date(b.created_at);
      return dateB - dateA;
    });

    mergedArray = mergedArray.map((item: any) => {
      let displayText = '';
      if (item.text) {
        displayText = item.text;
        item.invited_vip_reward_day = item.vip_reward_day;
      } else {
        displayText = item.title ;
      }

      return { ...item, displayText: displayText };
    });
    console.log('mergedArray');
    console.log(mergedArray);
    setInviteList(mergedArray);
  }, []);

  return (
    <ScreenContainer>
      <TitleWithBackButtonHeader title="累计奖励明细" />
      <ScrollView
        showsVerticalScrollIndicator={false}
        refreshControl={
          <RefreshControl refreshing={refreshing} onRefresh={handleRefresh} />
        }>
        { }
        <LinearGradient
          colors={['#323638', '#1a1d20']} 
          locations={[0.1, 1]}
          start={{ x: 1, y: 0 }} 
          end={{ x: 0, y: 1 }} 
          style={{
            marginTop: 40,
            paddingHorizontal: 20,
            paddingVertical: 10,
            borderRadius: 15,
            flexDirection: 'row', 
            justifyContent: 'space-between',
            alignItems: 'center',
            minHeight: 120,
          }}>
          <View style={styles.container}>
            { }
            <Text style={styles.title}>{userState.user?.userTotalInvite}</Text>
            <Text style={styles.subTitle}>已邀请人数</Text>
          </View>

          <View style={styles.container}>
            { }
            <Text style={styles.title}>
              {userState.user?.userAccumulateRewardDay}
            </Text>
            <Text style={styles.subTitle}>累计获得天数</Text>
          </View>
        </LinearGradient>

        { }
        <View style={{ marginTop: 40, alignItems: 'center' }}>
          <Text
            style={{ ...textVariants.header, fontSize: 16, fontWeight: '700' }}>
            邀请记录
          </Text>

          {inviteList.length > 0 && (
            <View
              style={{
                marginTop: 20,
                marginBottom: 40,
                backgroundColor: '#1D2023',
                borderRadius: 15,
                overflow: 'hidden',
                width: '100%',
              }}>
              {inviteList.map(item => (
                <View
                  key={item.displayText}
                  style={{
                    flexDirection: 'row',
                    justifyContent: 'space-between',
                    alignContent: 'center',
                    paddingHorizontal: 20,
                    paddingVertical: 15,
                  }}>
                  <View style={{ flex: 1, paddingRight: 10 }}>
                    <Text
                      style={{ fontSize: 16, color: '#fff' }}
                      numberOfLines={1}
                      ellipsizeMode="tail">
                      {item.displayText}
                    </Text>
                    <Text style={{ color: '#9c9c9c' }}>{item.created_at}</Text>
                  </View>

                  <View
                    style={{
                      justifyContent: 'center',
                      alignContent: 'center',
                    }}>
                    <Text
                      style={{
                        color: colors.primary,
                        fontSize: 16,
                        flex: 1,
                      }}>
                      +{item.invited_vip_reward_day}天
                    </Text>
                  </View>
                </View>
              ))}
            </View>
          )}

          {inviteList.length == 0 && (
            <View
              style={{
                marginTop: 20,
                marginBottom: 40,
                backgroundColor: '#1D2023',
                borderRadius: 15,
                overflow: 'hidden',
                width: '100%',
              }}>
              <View
                style={{
                  flexDirection: 'row',

                  alignContent: 'center',
                  paddingHorizontal: 20,
                  paddingVertical: 20,
                }}>
                <View>
                  <Text style={{ color: '#fff' }}>暂时无邀请记录</Text>
                </View>
              </View>
            </View>
          )}
        </View>
      </ScrollView>
    </ScreenContainer>
  );
};

const styles = StyleSheet.create({
  loading: {
    flexDirection: 'row',
    justifyContent: 'center',
    flex: 1,
  },
  backgroundImage: {
    flex: 1, 
    resizeMode: 'cover', 
  },
  container: {
    flex: 1,
    justifyContent: 'space-between',
    alignItems: 'flex-start',
    paddingLeft: 25,
  },
  title: {
    color: '#fff',
    fontSize: 26,
    fontWeight: '900',
  },
  subTitle: {
    fontSize: 16,
    fontWeight: '400',
    color: '#9C9C9C',
  },
});
