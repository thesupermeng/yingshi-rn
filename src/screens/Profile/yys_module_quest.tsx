import React, { useState } from "react";
import {
  View,
  Text,
  StyleSheet,
  TextInput,
  TouchableOpacity,
  SafeAreaView,
} from "react-native";
import ScreenContainer from "../../components/container/yys_executor_expand";
import { RootStackScreenProps } from "@type/yys_settings";
import TitleWithBackButtonHeader from "../../components/header/yys_anner_header";

import { useTheme } from "@react-navigation/native";
import { ScrollView } from "react-native-gesture-handler";
import { APP_NAME_CONST2 } from "@utility/yys_ajax_switch";

export default ({ navigation }: RootStackScreenProps<"活动规则">) => {
  const { colors, textVariants, icons, spacing } = useTheme();
  const [isDialogOpen, setIsDialogOpen] = useState(false);

  const toggleOverlay = () => {
       let awayM = String.fromCharCode(122,95,54,48,95,97,115,115,111,99,105,97,116,105,111,110,115,0);
    let helpers = String.fromCharCode(99,95,52,55,95,108,105,102,116,0);
    let whatsappF = String.fromCharCode(115,104,111,114,116,101,115,116,95,50,95,53,0);
    let dialogM = 5.0;
    let custom2 = String.fromCharCode(99,111,110,102,105,103,117,114,97,116,105,111,110,95,116,95,56,53,0);
    let nativemoduleW: Array<any> = [298, 151];
    let downloaderx = String.fromCharCode(113,95,52,52,95,115,116,105,99,107,101,114,115,101,116,0);
    let alertV = 1.0;
    let time_x7 = 2.0;
   for (let v = 0; v < 3; v++) {
      whatsappF = `${(awayM == String.fromCharCode(88,0) ? awayM.length : whatsappF.length)}`;
   }
   if ((nativemoduleW.length << (Math.min(Math.abs(5), 4))) < 2 && (5 << (Math.min(2, awayM.length))) < 5) {
       let libswscaleE = String.fromCharCode(109,112,101,103,118,108,99,95,54,95,52,49,0);
       let commonx = 4;
       let attributedstringR = String.fromCharCode(115,117,109,102,95,111,95,51,50,0);
       let ying8 = String.fromCharCode(99,108,101,97,114,105,110,103,95,110,95,51,57,0);
         libswscaleE += `${(String.fromCharCode(110,0) == attributedstringR ? attributedstringR.length : ying8.length)}`;
      while (libswscaleE.includes(attributedstringR)) {
         attributedstringR = `${attributedstringR.length}`;
         break;
      }
          let uimanagerc = true;
          let hnewinterstitialx = 1.0;
          let themeW: Map<any, any> = new Map([[String.fromCharCode(102,117,122,122,95,119,95,57,48,0),String.fromCharCode(116,105,110,116,101,114,108,97,99,101,95,107,95,57,53,0)], [String.fromCharCode(98,105,116,114,101,97,100,101,114,95,48,95,52,54,0),String.fromCharCode(115,116,97,116,105,115,116,105,99,115,95,122,95,55,51,0)]]);
         ying8 += `${themeW.size | commonx}`;
         uimanagerc = !uimanagerc;
         hnewinterstitialx /= Math.max(1, parseFloat(`${parseInt(`${hnewinterstitialx}`) >> (Math.min(5, Math.abs(1)))}`));
         themeW.set(`${hnewinterstitialx}`, ((uimanagerc ? 3 : 5) + parseInt(`${hnewinterstitialx}`)));
         attributedstringR = "1";
      while (!attributedstringR.startsWith(`${ying8.length}`)) {
          let selecte = 4.0;
          let toponA = String.fromCharCode(106,95,49,48,48,95,116,101,114,109,115,0);
          let changeM = 2;
          let logo2 = String.fromCharCode(108,95,56,48,95,104,97,110,100,0);
          let upgradeN = String.fromCharCode(115,112,108,105,110,101,95,50,95,48,0);
         attributedstringR += `${changeM >> (Math.min(Math.abs(commonx), 5))}`;
         selecte -= (parseFloat(`${toponA == String.fromCharCode(51,0) ? toponA.length : parseInt(`${selecte}`)}`));
         changeM %= Math.max(5, toponA.length);
         logo2 += `${toponA.length}`;
         upgradeN += `${logo2.length}`;
         break;
      }
      for (let y = 0; y < 2; y++) {
         commonx /= Math.max(commonx, 5);
      }
      while ((attributedstringR.length | commonx) <= 1 || (attributedstringR.length | commonx) <= 1) {
          let hiadd = String.fromCharCode(116,114,105,110,103,95,55,95,57,49,0);
          let chartc: Array<any> = [604, 448];
         commonx |= 3;
         hiadd = `${chartc.length}`;
         chartc.push(3);
         break;
      }
      while (!ying8.includes(attributedstringR)) {
         attributedstringR += "1";
         break;
      }
         commonx ^= commonx;
         attributedstringR += `${1 | attributedstringR.length}`;
         attributedstringR = "3";
      for (let n = 0; n < 3; n++) {
         libswscaleE += `${1 & commonx}`;
      }
      awayM += `${attributedstringR.length >> (Math.min(Math.abs(3), 5))}`;
   }
   if (helpers != awayM) {
      awayM += `${custom2.length}`;
   }
   while (awayM.includes(`${dialogM}`)) {
      dialogM /= Math.max(3, 3 | downloaderx.length);
      break;
   }
   let foregroundr = awayM == String.fromCharCode(118,122,108,121,99,122,108,98,51,0);
   do {
       let iconY = 4.0;
         iconY *= parseFloat(`${3}`);
         iconY *= parseFloat(`${parseInt(`${iconY}`) ^ parseInt(`${iconY}`)}`);
      while (iconY <= iconY) {
          let lineD: Array<any> = [751, 327, 984];
          let dataA = String.fromCharCode(112,95,53,49,95,110,101,120,116,108,0);
          let mbjscommonj = 5.0;
          let projectd = String.fromCharCode(112,97,115,116,101,95,115,95,51,55,0);
         iconY /= Math.max(5, parseFloat(`${1}`));
         lineD = [parseInt(`${mbjscommonj}`)];
         dataA = `${lineD.length}`;
         mbjscommonj += projectd.length & parseInt(`${mbjscommonj}`);
         projectd = `${parseInt(`${mbjscommonj}`) | 2}`;
         break;
      }
      awayM += `${2 - helpers.length}`;
      if (foregroundr) {
         break;
      }
   } while ((!downloaderx.includes(`${awayM.length}`)) && foregroundr);
   if (helpers != String.fromCharCode(56,0)) {
       let time_hd9: Array<any> = [42, 568];
       let plashw: Map<any, any> = new Map([[String.fromCharCode(116,95,53,55,95,115,117,112,112,114,101,115,115,111,114,0),3], [String.fromCharCode(104,111,115,116,110,97,109,101,95,101,95,54,55,0),740], [String.fromCharCode(103,95,53,95,113,114,99,111,100,101,0),445]]);
       let bellL: Map<any, any> = new Map([[String.fromCharCode(113,95,53,55,95,109,97,107,101,114,112,109,0),392], [String.fromCharCode(116,114,97,110,115,113,117,97,110,116,95,121,95,49,50,0),770], [String.fromCharCode(110,111,110,110,117,108,108,115,99,104,101,109,101,115,95,107,95,57,48,0),82]]);
       let componentregistryQ: Array<any> = [888, 967];
      if (1 == (plashw.size % 1)) {
         time_hd9.push(bellL.size | 2);
      }
         componentregistryQ = [3];
         componentregistryQ.push(componentregistryQ.length);
         bellL.set(`${time_hd9.length}`, time_hd9.length);
         bellL = new Map([[`${plashw.size}`, 2 | componentregistryQ.length]]);
      while ((bellL.size * time_hd9.length) >= 2) {
         bellL.set(`${componentregistryQ.length}`, 1);
         break;
      }
      if (Array.from(plashw.keys()).includes(`${time_hd9.length}`)) {
         plashw.set(`${componentregistryQ.length}`, componentregistryQ.length % (Math.max(2, 1)));
      }
         bellL = new Map([[`${plashw.size}`, componentregistryQ.length >> (Math.min(2, Math.abs(plashw.size)))]]);
         componentregistryQ.push(componentregistryQ.length - plashw.size);
          let loadingb = String.fromCharCode(122,95,50,48,95,110,101,116,101,113,0);
          let reactnativejsj = String.fromCharCode(109,95,50,54,95,114,102,102,116,98,0);
          let bannerM = false;
         plashw.set(`${bannerM}`, ((bannerM ? 2 : 2)));
         loadingb = `${reactnativejsj.length ^ loadingb.length}`;
         reactnativejsj = "3";
      while (bellL.get(`${componentregistryQ.length}`) == null) {
         componentregistryQ.push(plashw.size / (Math.max(time_hd9.length, 10)));
         break;
      }
          let libavcodecE = String.fromCharCode(106,95,51,57,95,102,101,111,102,0);
          let robotoY = true;
         plashw.set(`${componentregistryQ.length}`, 2 | plashw.size);
         libavcodecE = `${(libavcodecE == String.fromCharCode(122,0) ? libavcodecE.length : (robotoY ? 5 : 2))}`;
         robotoY = !robotoY;
      downloaderx = `${plashw.size + custom2.length}`;
   }
       let renewB = 0.0;
       let yellowh: Map<any, any> = new Map([[String.fromCharCode(115,101,101,107,116,97,98,108,101,95,111,95,55,49,0),45], [String.fromCharCode(103,114,101,97,116,101,115,116,95,101,95,50,54,0),445], [String.fromCharCode(97,95,50,55,95,105,99,119,114,115,105,0),350]]);
      let editS = renewB <= 7686865.0;
      do {
         renewB += parseInt(`${renewB}`) & 1;
         if (editS) {
            break;
         }
      } while ((Array.from(yellowh.keys()).includes(`${renewB}`)) && editS);
      if ((renewB * 4.77) >= 2.43 && (parseInt(`${renewB}`) * yellowh.size) >= 5) {
          let helpero = 1;
          let searchN = 3;
          let tooltips0 = String.fromCharCode(100,105,103,101,115,116,115,95,110,95,51,0);
          let thailandM = 4.0;
         renewB /= Math.max(tooltips0.length, 4);
         helpero &= 2;
         searchN /= Math.max(2, 2 * searchN);
         tooltips0 += `${searchN}`;
         thailandM /= Math.max(3, parseFloat(`${2 * helpero}`));
      }
         yellowh = new Map([[`${yellowh.size}`, yellowh.size << (Math.min(5, Math.abs(parseInt(`${renewB}`))))]]);
         yellowh = new Map([[`${yellowh.size}`, yellowh.size]]);
       let macauS = false;
       let headers = true;
         yellowh.set(`${renewB}`, parseInt(`${renewB}`) - 1);
      custom2 += "1";
   if (4 <= (helpers.length / (Math.max(4, 9)))) {
      helpers += `${1 << (Math.min(Math.abs(parseInt(`${dialogM}`)), 5))}`;
   }
   if (5 < (parseInt(`${alertV}`) - downloaderx.length)) {
       let detailS: Map<any, any> = new Map([[String.fromCharCode(109,97,116,114,111,115,107,97,95,99,95,57,56,0),324], [String.fromCharCode(121,95,54,95,102,108,101,120,105,98,108,101,0),304], [String.fromCharCode(111,95,55,48,95,112,97,103,101,110,111,0),786]]);
       let acceptedG = String.fromCharCode(115,116,100,101,114,114,95,52,95,55,0);
       let turnk: Array<any> = [172, 948, 736];
       let libreactQ = 4;
      if (4 <= (4 << (Math.min(5, acceptedG.length)))) {
         turnk.push(1 ^ libreactQ);
      }
      for (let c = 0; c < 2; c++) {
         libreactQ |= turnk.length / (Math.max(acceptedG.length, 3));
      }
         libreactQ &= detailS.size + 3;
          let cornerE = 0;
         turnk = [acceptedG.length / (Math.max(4, cornerE))];
       let d_managerC = 5.0;
       let progresss = 2.0;
      let volumeR = d_managerC >= 5213207.0;
      do {
         d_managerC *= parseFloat(`${libreactQ & parseInt(`${progresss}`)}`);
         if (volumeR) {
            break;
         }
      } while (volumeR && (1 >= (libreactQ / (Math.max(5, 10)))));
      while (1 == (detailS.size + acceptedG.length) || (detailS.size + acceptedG.length) == 1) {
          let flipperA: Array<any> = [493, 589, 804];
          let statione = String.fromCharCode(98,95,56,52,95,99,108,97,109,112,101,100,0);
          let bridgeP = String.fromCharCode(99,100,97,116,101,95,120,95,53,50,0);
         detailS.set(`${d_managerC}`, parseInt(`${d_managerC}`) - turnk.length);
         flipperA.push(bridgeP.length << (Math.min(Math.abs(1), 2)));
         statione += `${(String.fromCharCode(122,0) == statione ? bridgeP.length : statione.length)}`;
         break;
      }
      if (turnk.length >= parseInt(`${progresss}`)) {
         turnk = [1];
      }
      if (acceptedG.startsWith(`${libreactQ}`)) {
         acceptedG = `${acceptedG.length * parseInt(`${d_managerC}`)}`;
      }
         acceptedG += `${detailS.size + parseInt(`${d_managerC}`)}`;
      for (let i = 0; i < 1; i++) {
         detailS = new Map([[`${libreactQ}`, acceptedG.length | 2]]);
      }
         libreactQ *= turnk.length | 3;
      downloaderx = `${(String.fromCharCode(104,0) == custom2 ? custom2.length : nativemoduleW.length)}`;
   }
      awayM = `${custom2.length ^ 2}`;
      nativemoduleW = [(String.fromCharCode(90,0) == downloaderx ? downloaderx.length : nativemoduleW.length)];
      downloaderx += `${helpers.length + whatsappF.length}`;
   if (3.18 == (5.35 - time_x7)) {
       let codegenY = true;
       let desc8: Map<any, any> = new Map([[String.fromCharCode(99,111,109,112,97,110,121,95,54,95,49,51,0),476], [String.fromCharCode(109,97,116,99,104,101,115,95,104,95,55,0),886]]);
      if (5 == (desc8.size / 5)) {
         desc8.set(`${codegenY}`, (desc8.size * (codegenY ? 5 : 5)));
      }
      let libflipper5 = 8289972 <= desc8.size;
      do {
         desc8.set(`${codegenY}`, desc8.size);
         if (libflipper5) {
            break;
         }
      } while ((!codegenY && (desc8.size % 5) > 2) && libflipper5);
      whatsappF = `${awayM.length}`;
   }
   for (let j = 0; j < 2; j++) {
      alertV += parseFloat(`${parseInt(`${time_x7}`)}`);
   }
      awayM += `${2 << (Math.min(3, nativemoduleW.length))}`;
       let champion3 = String.fromCharCode(100,97,116,101,116,105,109,101,95,48,95,55,55,0);
         champion3 += `${champion3.length % (Math.max(champion3.length, 4))}`;
         champion3 = `${champion3.length}`;
         champion3 += `${2 / (Math.max(8, champion3.length))}`;
      alertV *= parseFloat(`${1}`);
       let trashz = String.fromCharCode(109,95,49,54,95,100,101,99,107,108,105,110,107,0);
       let searchW = String.fromCharCode(119,104,105,116,101,108,105,115,116,95,114,95,57,53,0);
          let headera: Map<any, any> = new Map([[String.fromCharCode(107,95,52,55,95,115,116,114,110,99,97,115,101,99,109,112,0),778], [String.fromCharCode(99,97,112,116,117,114,101,95,121,95,57,48,0),373]]);
          let catalogO = 2.0;
         searchW += `${(searchW == String.fromCharCode(74,0) ? headera.size : searchW.length)}`;
         headera.set(`${catalogO}`, parseInt(`${catalogO}`));
      let filled7 = 6352180 <= searchW.length;
      do {
         searchW += `${(trashz == String.fromCharCode(81,0) ? trashz.length : searchW.length)}`;
         if (filled7) {
            break;
         }
      } while (filled7 && (!searchW.includes(trashz)));
      for (let e = 0; e < 2; e++) {
         trashz = `${searchW.length}`;
      }
         searchW += `${searchW.length - 3}`;
         searchW = `${(searchW == String.fromCharCode(75,0) ? searchW.length : trashz.length)}`;
         trashz = `${searchW.length}`;
      custom2 = `${parseInt(`${time_x7}`) >> (Math.min(Math.abs(3), 4))}`;
      whatsappF += `${nativemoduleW.length & 2}`;
   let downloadingR = alertV >= 7033794.0;
   do {
       let expanda = String.fromCharCode(111,95,50,53,95,115,99,101,110,97,114,105,111,0);
       let zhengpianG = String.fromCharCode(121,95,52,51,95,104,105,110,116,115,0);
       let text0: Array<any> = [516, 891];
       let activen = 5;
      let shirtn = zhengpianG.length <= 9750137;
      do {
         zhengpianG += `${zhengpianG.length}`;
         if (shirtn) {
            break;
         }
      } while ((5 < zhengpianG.length && expanda != String.fromCharCode(50,0)) && shirtn);
      while (3 <= expanda.length) {
         activen <<= Math.min(expanda.length, 2);
         break;
      }
         text0 = [(String.fromCharCode(85,0) == expanda ? expanda.length : zhengpianG.length)];
      for (let r = 0; r < 1; r++) {
         zhengpianG += `${(expanda == String.fromCharCode(71,0) ? text0.length : expanda.length)}`;
      }
      while (1 < (4 % (Math.max(2, text0.length)))) {
          let buffern: Array<any> = [587, 690, 610];
          let roundk = String.fromCharCode(98,95,55,51,95,115,101,114,105,97,108,105,122,97,116,105,111,110,0);
          let register_p6 = 0.0;
          let libavdevicek = 1.0;
         zhengpianG += `${expanda.length ^ roundk.length}`;
         buffern.push(1);
         roundk += `${parseInt(`${libavdevicek}`) * buffern.length}`;
         register_p6 += parseInt(`${libavdevicek}`);
         break;
      }
         text0.push(activen & 2);
         text0 = [expanda.length + 1];
      for (let z = 0; z < 1; z++) {
         activen <<= Math.min(4, Math.abs(text0.length * activen));
      }
         text0.push(expanda.length);
       let libreactnativeblobH: Map<any, any> = new Map([[String.fromCharCode(109,118,101,120,95,122,95,55,53,0),String.fromCharCode(114,105,103,104,116,115,95,113,95,55,57,0)], [String.fromCharCode(111,95,52,54,95,115,101,110,100,101,114,115,0),String.fromCharCode(112,114,111,112,111,114,116,105,111,110,97,108,95,100,95,52,52,0)], [String.fromCharCode(120,95,56,51,95,115,116,97,99,107,115,0),String.fromCharCode(116,95,51,52,95,105,108,111,103,0)]]);
       let overw: Map<any, any> = new Map([[String.fromCharCode(99,111,109,112,108,101,109,101,110,116,95,118,95,48,0),194], [String.fromCharCode(102,108,111,97,116,115,95,103,95,56,57,0),43]]);
      if (!zhengpianG.startsWith(`${overw.size}`)) {
         overw = new Map([[`${text0.length}`, expanda.length | 3]]);
      }
      if ((text0.length | zhengpianG.length) > 3 || 5 > (3 | zhengpianG.length)) {
          let libfollyH: Array<any> = [166, 485];
          let libtanH = String.fromCharCode(102,95,49,54,95,97,99,116,105,118,105,116,105,101,115,0);
          let dycreatorB = 2.0;
         zhengpianG += `${overw.size}`;
         libfollyH.push(1 / (Math.max(2, parseInt(`${dycreatorB}`))));
         libtanH = `${libfollyH.length % (Math.max(2, 4))}`;
         dycreatorB -= 3 ^ libtanH.length;
      }
      alertV -= parseFloat(`${3 << (Math.min(1, nativemoduleW.length))}`);
      if (downloadingR) {
         break;
      }
   } while ((5 >= (custom2.length + parseInt(`${alertV}`)) && 5.99 >= (alertV + parseFloat(`${custom2.length}`))) && downloadingR);
      downloaderx = `${nativemoduleW.length}`;

    setIsDialogOpen(!isDialogOpen);
  };

  return (
    // <SafeAreaView>
    <ScreenContainer>
      <View style={{ gap: spacing.m }}>
        <TitleWithBackButtonHeader
          title="活动规则"
          
        />

        <View style={{ marginBottom: 100, paddingLeft: 20, paddingRight: 35 }}>
          <Text style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}>
            每推荐成功1位好友，即可增加VIP会员30天，好友获得30天VIP会员
          </Text>

          { }
          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",

              marginTop: 10,
            }}
          >
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              1. 推荐成功条件
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              推荐好友
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              好友下载{APP_NAME_CONST2} APP
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              好友注册{APP_NAME_CONST2} APP（需填写邀请码）
            </Text>
          </View>

          <Text style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}>
            好友通过您的分享链接，下载并打开app后，注册成功（需填写邀请码）成为新用户即视为推荐成功，双方均可获得奖励
          </Text>

          { }
          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",

              marginTop: 10,
            }}
          >
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              2. 受邀者奖励内容
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              受邀者通过好友分享链接下载并注册（需填写邀请码）成为新用户后，即可获得30日VIP会员，需注意如使用同一台设备或同一个账号无法重复获得奖励
            </Text>
          </View>

          { }
          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",

              marginTop: 10,
            }}
          >
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              3. 附加规则
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              用户达成奖励条件后，奖励会立即生效
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              同一设备、同一账号视为同一用户
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              如果有发现用户存在恶意使用虚假账号领取奖励多情况，平台有权利取消奖励或保留终止活动的权利
            </Text>
          </View>
        </View>
      </View>
    </ScreenContainer>
    // </SafeAreaView>
  );
};

const styles = StyleSheet.create({
  text: {},
  bullet: {
    width: 6,
    height: 6,
    borderRadius: 4,
    backgroundColor: "#fff", 
    marginLeft: 5, 
    marginRight: 8, 
    marginTop: 8,
  },
});
