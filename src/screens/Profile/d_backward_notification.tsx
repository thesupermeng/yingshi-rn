import React, { useMemo, useState } from "react";
import {
  View,
  Text,
  StyleSheet,
  Image,
  Clipboard,
  Platform,
} from "react-native";
import ScreenContainer from "../../components/container/ypy_fast";
import { RootStackScreenProps } from "@type/vrm_thailand";
import { useTheme } from "@react-navigation/native";
import TitleWithBackButtonHeader from "../../components/header/cio_news_types_header";
import { useSelector } from "@hooks/kg_index";
import { TouchableOpacity } from "@gorhom/bottom-sheet";
import NotificationModal from "../../components/modal/fc_shoot_bodan";
import { showToast } from "../../Sports/utility/lqu_gemfile";
import FastImage from "../../components/common/gwi_with";
import { ChangeUsernameModal } from "../../components/modal/zf_drag_screen";
import { ChangeReferrerModal } from "../../components/modal/tc_canvas_basketball";
import { HDTGesturesList } from "@redux/reducers/f_gmail";
import { SMBing } from "@utility/sa_crown";

export default ({ navigation }: RootStackScreenProps<"个人中心">) => {
  const { colors, dark } = useTheme();
  const styles = useMemo(() => createStyles({ colors, dark }), [colors, dark]);

  const userState = useSelector<HDTGesturesList>('userReducer');

  const [username, setUsername] = useState(userState.user?.userName ?? '');

  const [isShowUsernameModal, setShowUsernameModal] = useState(false);
  const [isShowReferrerModal, setShowReferrerModal] = useState(false);

  const [isDialogOpen, setIsDialogOpen] = useState(false);


  const toggleOverlay = () => {
       let delegate_cu = false;
    let sansV = 4;
    let data6: Array<any> = [527, 980, 849];
    let countdownj: Array<any> = [681, 107];
    let moviesM: Array<any> = [String.fromCharCode(109,95,55,50,95,103,117,116,115,0), String.fromCharCode(103,95,55,48,95,117,118,109,118,0)];
    let overE: Array<any> = [885, 898];
    let floating2 = String.fromCharCode(122,95,55,49,95,100,121,110,97,109,105,99,115,0);
    let goal0 = 3;
    let skipJ: Array<any> = [865, 799];
    let foundT = 4.0;
    let hooksx = String.fromCharCode(110,101,116,115,95,117,95,52,0);
    let baidu3: Map<any, any> = new Map([[String.fromCharCode(112,114,101,95,48,95,55,48,0),117], [String.fromCharCode(99,104,107,108,105,115,116,95,117,95,55,50,0),182], [String.fromCharCode(112,97,114,105,116,105,111,110,105,110,103,95,104,95,49,57,0),718]]);
    let middlewarev = true;
      foundT += (String.fromCharCode(75,0) == floating2 ? floating2.length : data6.length);
   if ((foundT + 4.52) == 5.48 && (floating2.length + foundT) == 4.52) {
      foundT /= Math.max(4, sansV);
   }
   let datae = moviesM.length >= 6179929;
   do {
       let statisticsL = 0.0;
      if (4.6 == statisticsL) {
         statisticsL *= parseInt(`${statisticsL}`);
      }
      while (2.11 == (statisticsL * statisticsL) && 1.12 == (statisticsL * 2.11)) {
         statisticsL /= Math.max(parseInt(`${statisticsL}`), 5);
         break;
      }
         statisticsL += parseInt(`${statisticsL}`);
      moviesM = [data6.length >> (Math.min(Math.abs(1), 2))];
      if (datae) {
         break;
      }
   } while (((skipJ.length % (Math.max(9, moviesM.length))) > 4 || 4 > (skipJ.length % (Math.max(3, moviesM.length)))) && datae);
      moviesM.push(moviesM.length);
      moviesM.push(skipJ.length * floating2.length);
   while (1 >= (countdownj.length * moviesM.length) && (moviesM.length * countdownj.length) >= 1) {
      moviesM = [hooksx.length];
      break;
   }
      data6.push(parseInt(`${foundT}`) << (Math.min(Math.abs(3), 3)));
   if (moviesM.length >= 4) {
       let country9: Array<any> = [135, 759, 288];
       let backwardH = 4;
       let goalw = true;
       let rankh: Map<any, any> = new Map([[String.fromCharCode(105,95,51,57,95,114,112,114,111,98,101,0),15], [String.fromCharCode(100,114,111,112,120,95,99,95,52,0),236], [String.fromCharCode(103,95,53,49,95,101,120,112,105,114,121,0),451]]);
         rankh = new Map([[`${country9.length}`, country9.length * 1]]);
      let downloaderj = backwardH <= 6266273;
      do {
          let reducer_: Map<any, any> = new Map([[String.fromCharCode(115,117,98,98,97,110,100,95,53,95,57,48,0),525], [String.fromCharCode(104,95,56,51,95,105,110,118,105,116,101,114,0),65], [String.fromCharCode(103,95,56,51,95,115,118,97,114,105,110,116,0),871]]);
          let time_zvL = 5.0;
          let tickF: Array<any> = [0, 252, 281];
          let starR = String.fromCharCode(114,101,108,111,103,105,110,95,116,95,48,0);
         backwardH &= 2 + rankh.size;
         reducer_.set(`${tickF.length}`, 1);
         time_zvL += 1 + reducer_.size;
         tickF = [starR.length];
         starR = "1";
         if (downloaderj) {
            break;
         }
      } while (downloaderj && (5 <= (rankh.size << (Math.min(Math.abs(1), 4))) && (1 << (Math.min(4, Math.abs(rankh.size)))) <= 5));
         country9 = [country9.length ^ 2];
      let heartx = 6059182 <= country9.length;
      do {
         country9 = [rankh.size & 2];
         if (heartx) {
            break;
         }
      } while (heartx && (country9.length > 1));
          let tailQ = String.fromCharCode(113,95,50,53,95,100,105,115,112,111,115,105,116,105,111,110,0);
          let teamc: Array<any> = [796, 955];
         goalw = (country9.length ^ backwardH) == 58;
         tailQ = `${tailQ.length}`;
         teamc = [teamc.length * tailQ.length];
       let shared9 = 5.0;
       let episodes1: Map<any, any> = new Map([[String.fromCharCode(98,95,56,57,95,111,99,97,108,105,122,97,116,105,111,110,0),false ], [String.fromCharCode(105,110,118,97,108,105,100,97,116,101,100,95,111,95,53,56,0),false ]]);
      for (let d = 0; d < 3; d++) {
         shared9 += 2;
      }
      let typingz = 9083111.0 <= shared9;
      do {
          let clockI = 3;
          let custom3: Array<any> = [260, 665, 528];
          let viewsa = true;
          let catagoryY = 3.0;
         shared9 += 2;
         clockI /= Math.max(((viewsa ? 3 : 1) ^ 1), 1);
         custom3 = [custom3.length];
         viewsa = viewsa && custom3.length == 99;
         catagoryY -= parseFloat(`${1 / (Math.max(9, parseInt(`${catagoryY}`)))}`);
         if (typingz) {
            break;
         }
      } while ((episodes1.size == shared9) && typingz);
      while (episodes1.get(`${shared9}`) == null) {
          let dark1 = String.fromCharCode(97,108,108,112,97,115,115,95,105,95,54,55,0);
         shared9 /= Math.max(5, rankh.size);
         dark1 = "1";
         break;
      }
       let settingsg = 2.0;
       let applen = 5.0;
         backwardH ^= country9.length - 3;
      floating2 += `${backwardH * 2}`;
   }
   if ((hooksx.length << (Math.min(3, Math.abs(sansV)))) == 4 || 4 == (hooksx.length << (Math.min(1, Math.abs(sansV))))) {
      hooksx = `${sansV ^ 3}`;
   }
   while ((overE.length | hooksx.length) == 3) {
      overE = [3];
      break;
   }
       let routerQ = String.fromCharCode(110,95,52,52,95,118,101,99,116,111,114,100,0);
       let stringN = 4.0;
       let pageN = String.fromCharCode(107,95,52,49,95,112,114,101,102,101,114,101,110,99,101,115,0);
      let guideS = routerQ.length <= 8151056;
      do {
         routerQ += `${2 / (Math.max(9, parseInt(`${stringN}`)))}`;
         if (guideS) {
            break;
         }
      } while (guideS && (pageN != String.fromCharCode(103,0)));
          let sinal = String.fromCharCode(114,95,55,49,95,97,100,106,117,115,109,101,110,116,115,0);
          let championR = 1.0;
          let frame_7y = String.fromCharCode(116,95,54,50,95,99,111,112,121,100,97,116,97,0);
         routerQ += `${1 + frame_7y.length}`;
         sinal = `${(sinal == String.fromCharCode(73,0) ? sinal.length : parseInt(`${championR}`))}`;
         championR += 1;
         frame_7y += "1";
          let megaphones = 5.0;
          let rewinds = 1;
          let nalyticsy = 3.0;
         stringN -= parseInt(`${stringN}`);
         megaphones += rewinds;
         rewinds |= rewinds;
         nalyticsy += parseFloat(`${rewinds}`);
      for (let u = 0; u < 1; u++) {
         stringN -= parseInt(`${stringN}`) << (Math.min(routerQ.length, 3));
      }
      while ((4 | pageN.length) > 5) {
         pageN += `${parseInt(`${stringN}`)}`;
         break;
      }
         pageN += "1";
          let inviteE = 3;
          let vignetteH = 3.0;
         stringN *= pageN.length;
         inviteE <<= Math.min(Math.abs(2), 3);
         vignetteH *= inviteE % 2;
          let orange4 = String.fromCharCode(110,95,52,51,95,100,111,99,108,105,115,116,0);
          let whiteI: Array<any> = [93, 987, 899];
          let play2: Array<any> = [String.fromCharCode(117,110,99,97,99,104,101,100,95,122,95,54,50,0), String.fromCharCode(116,111,109,99,114,121,112,116,95,54,95,49,53,0)];
         routerQ = `${(orange4 == String.fromCharCode(100,0) ? routerQ.length : orange4.length)}`;
         whiteI = [2];
         play2.push(play2.length);
          let paginationh = 3;
         stringN *= pageN.length;
         paginationh /= Math.max(5, 1);
      floating2 = "2";
   while ((skipJ.length >> (Math.min(hooksx.length, 3))) < 4 && (skipJ.length >> (Math.min(hooksx.length, 4))) < 4) {
      hooksx += `${sansV ^ overE.length}`;
      break;
   }
      overE = [(String.fromCharCode(82,0) == hooksx ? skipJ.length : hooksx.length)];
      countdownj = [3];
      data6 = [countdownj.length - skipJ.length];
      floating2 = `${goal0 ^ sansV}`;
       let time_r3 = String.fromCharCode(116,100,115,99,95,103,95,53,51,0);
       let gpay2: Map<any, any> = new Map([[String.fromCharCode(115,95,50,57,95,121,117,118,112,108,0),876], [String.fromCharCode(104,101,99,107,95,97,95,49,56,0),531]]);
          let emptyH = String.fromCharCode(99,97,112,105,95,113,95,53,51,0);
          let controlX = true;
         time_r3 = `${time_r3.length & gpay2.size}`;
         emptyH += `${(emptyH.length << (Math.min(3, Math.abs((controlX ? 5 : 3)))))}`;
         controlX = (96 == ((controlX ? emptyH.length : 96) / (Math.max(emptyH.length, 1))));
      while ((gpay2.size << (Math.min(time_r3.length, 5))) < 5) {
         gpay2 = new Map([[`${gpay2.size}`, time_r3.length]]);
         break;
      }
       let megaphonek = String.fromCharCode(119,119,119,95,55,95,48,0);
      while (time_r3.length >= gpay2.size) {
         gpay2 = new Map([[`${gpay2.size}`, gpay2.size << (Math.min(Math.abs(2), 4))]]);
         break;
      }
      let shrink9 = gpay2.size <= 7824517;
      do {
         gpay2.set(megaphonek, 1);
         if (shrink9) {
            break;
         }
      } while (shrink9 && (1 < (2 << (Math.min(5, Math.abs(gpay2.size)))) && 4 < (2 << (Math.min(1, time_r3.length)))));
      while ((5 | megaphonek.length) > 4 || (megaphonek.length | 5) > 4) {
         megaphonek += `${gpay2.size}`;
         break;
      }
      countdownj = [3 + moviesM.length];
      hooksx = "3";
   while (!delegate_cu || 3 >= (baidu3.size << (Math.min(Math.abs(2), 1)))) {
      baidu3.set(`${moviesM.length}`, countdownj.length);
      break;
   }
   while (hooksx.includes(`${overE.length}`)) {
      hooksx += `${hooksx.length}`;
      break;
   }
   while (hooksx.length < foundT) {
      hooksx += `${1 >> (Math.min(3, overE.length))}`;
      break;
   }
   for (let b = 0; b < 3; b++) {
      moviesM.push(overE.length);
   }
   for (let m = 0; m < 2; m++) {
      goal0 -= 2;
   }
   while (2.57 == (1.33 - foundT) && 5.35 == (1.33 - foundT)) {
      foundT /= Math.max(data6.length | skipJ.length, 1);
      break;
   }
   if ((hooksx.length >> (Math.min(Math.abs(4), 1))) <= 1 || (parseInt(`${foundT}`) / (Math.max(hooksx.length, 7))) <= 4) {
      foundT /= Math.max(parseInt(`${foundT}`), 1);
   }
      data6.push(1);

    setIsDialogOpen(!isDialogOpen);
  };

  const onPressUsername = () => {
       let anythinkC = 0.0;
    let dropdown8 = false;
    let ticked1 = 0.0;
    let subss: Array<any> = [String.fromCharCode(114,101,99,111,114,100,97,98,108,101,95,116,95,56,51,0), String.fromCharCode(111,95,53,48,95,109,111,118,116,101,120,116,115,117,98,0)];
    let streaming5 = 5.0;
    let rankW = 5.0;
    let time_srw = 2;
    let whatsappr = true;
    let homec: Map<any, any> = new Map([[String.fromCharCode(97,99,113,117,105,114,101,95,113,95,54,54,0),127], [String.fromCharCode(118,95,56,56,95,114,101,118,111,107,105,110,103,0),290], [String.fromCharCode(99,114,101,97,116,105,110,103,95,54,95,56,55,0),220]]);
    let controlsx = 5;
    let xvodG = String.fromCharCode(112,95,50,56,95,97,108,108,114,103,98,0);
    let basketballe: Array<any> = [312, 176];
    let whistlen: Array<any> = [String.fromCharCode(116,97,103,103,105,110,103,95,98,95,49,48,48,0), String.fromCharCode(109,95,49,49,95,115,99,105,105,0)];
    let fastforwardt: Map<any, any> = new Map([[String.fromCharCode(113,95,54,95,112,114,105,118,97,99,121,0),true ], [String.fromCharCode(115,95,53,48,95,104,99,104,97,99,104,97,0),false ]]);
    let shoot_ = 3;
    let roomG: Map<any, any> = new Map([[String.fromCharCode(116,114,97,110,115,102,111,114,109,101,114,95,99,95,51,48,0),765], [String.fromCharCode(114,95,54,50,95,97,116,116,114,115,0),691]]);
      anythinkC /= Math.max(2, parseInt(`${anythinkC}`) << (Math.min(2, Math.abs(3))));
       let upgradea: Array<any> = [878, 753, 354];
       let connection2 = String.fromCharCode(115,104,105,102,116,115,95,107,95,57,49,0);
      for (let f = 0; f < 3; f++) {
          let list9 = 2.0;
          let shareZ: Map<any, any> = new Map([[String.fromCharCode(100,95,57,50,95,97,99,116,117,97,108,105,122,97,116,105,111,110,0),false ], [String.fromCharCode(110,97,116,105,111,110,97,108,105,116,121,95,101,95,51,54,0),true ]]);
          let bootsplash4 = 0.0;
          let shrinkp = String.fromCharCode(122,95,49,50,95,116,114,97,110,115,108,97,116,105,111,110,0);
         connection2 = `${shrinkp.length % 2}`;
         list9 /= Math.max(4, parseFloat(`${parseInt(`${list9}`) / 2}`));
         shareZ = new Map([[`${list9}`, parseInt(`${bootsplash4}`) | 3]]);
         bootsplash4 -= parseFloat(`${parseInt(`${bootsplash4}`)}`);
         shrinkp += `${parseInt(`${list9}`) << (Math.min(Math.abs(parseInt(`${bootsplash4}`)), 3))}`;
      }
         upgradea = [connection2.length];
         connection2 += `${connection2.length ^ 1}`;
         upgradea.push(upgradea.length);
      for (let y = 0; y < 1; y++) {
         upgradea.push(upgradea.length);
      }
          let analyticsU: Map<any, any> = new Map([[String.fromCharCode(97,99,99,101,115,115,105,110,103,95,110,95,52,51,0),8], [String.fromCharCode(106,112,101,103,116,114,97,110,95,100,95,57,53,0),869]]);
          let watchB = String.fromCharCode(112,95,48,95,103,101,116,97,100,100,114,105,110,102,111,0);
         upgradea.push(connection2.length | upgradea.length);
         analyticsU = new Map([[`${analyticsU.size}`, watchB.length]]);
         watchB += `${2 & analyticsU.size}`;
      ticked1 -= 1;
   if (4.35 == (streaming5 + 5.76)) {
       let rewindc = String.fromCharCode(107,95,49,51,95,115,101,113,117,101,110,116,105,97,108,0);
       let circle3 = 3.0;
      for (let f = 0; f < 1; f++) {
          let areac = String.fromCharCode(101,120,112,97,110,100,95,108,95,49,51,0);
         circle3 /= Math.max(2, (String.fromCharCode(119,0) == areac ? rewindc.length : areac.length));
      }
      if ((parseInt(`${circle3}`) - rewindc.length) >= 1 && (1 << (Math.min(1, rewindc.length))) >= 3) {
         circle3 -= 1 + parseInt(`${circle3}`);
      }
         circle3 -= parseInt(`${circle3}`) >> (Math.min(rewindc.length, 2));
         rewindc = `${parseInt(`${circle3}`) * 3}`;
      if (4 <= (rewindc.length + parseInt(`${circle3}`))) {
         circle3 /= Math.max(4, parseInt(`${circle3}`) / (Math.max(1, 4)));
      }
      while (5.55 >= circle3) {
          let eventT: Map<any, any> = new Map([[String.fromCharCode(112,101,114,95,117,95,53,48,0),true ], [String.fromCharCode(103,95,52,50,95,112,108,97,121,111,117,116,0),false ], [String.fromCharCode(109,117,108,116,105,112,108,101,114,95,97,95,54,55,0),false ]]);
          let helper4 = 2.0;
          let humidityk = 4.0;
         circle3 *= 2;
         eventT = new Map([[`${humidityk}`, parseInt(`${humidityk}`) & 2]]);
         helper4 /= Math.max(parseFloat(`${parseInt(`${humidityk}`)}`), 4);
         break;
      }
      basketballe.push(parseInt(`${ticked1}`));
   }
      streaming5 /= Math.max(1, 5);
   for (let b = 0; b < 3; b++) {
      homec = new Map([[`${streaming5}`, parseInt(`${rankW}`)]]);
   }
      xvodG += `${homec.size}`;
   while (2.17 > (anythinkC / 5.29)) {
       let areau: Array<any> = [510, 325, 950];
       let private_ew_ = String.fromCharCode(120,95,50,53,95,119,97,108,108,112,97,112,101,114,115,0);
       let philippinesS = 2.0;
       let penaltyL = String.fromCharCode(102,97,105,108,101,100,95,56,95,51,50,0);
      for (let r = 0; r < 1; r++) {
         areau.push(parseInt(`${philippinesS}`) << (Math.min(Math.abs(3), 1)));
      }
       let backJ = 0;
       let turn5 = 2;
       let videow = String.fromCharCode(102,95,51,55,95,119,104,101,114,101,0);
       let google1 = String.fromCharCode(109,112,106,112,101,103,95,114,95,55,55,0);
      while ((2 + penaltyL.length) < 4 || (penaltyL.length + 2) < 3) {
         backJ |= turn5 * google1.length;
         break;
      }
       let watchY = 0;
       let sellf = 5;
         google1 = `${1 * penaltyL.length}`;
         areau = [parseInt(`${philippinesS}`)];
      while ((1 - private_ew_.length) < 2) {
          let controle: Array<any> = [380, 196];
         private_ew_ += `${turn5}`;
         controle = [controle.length / 2];
         break;
      }
         turn5 >>= Math.min(4, Math.abs((String.fromCharCode(57,0) == penaltyL ? penaltyL.length : backJ)));
         backJ |= sellf;
         philippinesS -= (parseFloat(`${String.fromCharCode(109,0) == private_ew_ ? sellf : private_ew_.length}`));
      while ((2 << (Math.min(3, Math.abs(sellf)))) == 4) {
         sellf |= 3 ^ private_ew_.length;
         break;
      }
      anythinkC *= 1 % (Math.max(10, controlsx));
      break;
   }
   for (let w = 0; w < 1; w++) {
      anythinkC -= 3 & controlsx;
   }
   let commonO = ticked1 >= 4986118.0;
   do {
      ticked1 *= (parseInt(`${anythinkC}`) << (Math.min(4, Math.abs((dropdown8 ? 1 : 1)))));
      if (commonO) {
         break;
      }
   } while (commonO && (3.61 <= (ticked1 - streaming5) || (3.61 - ticked1) <= 2.31));
       let saveR = 0;
       let style9 = String.fromCharCode(118,95,50,54,95,105,110,116,99,104,101,99,107,0);
       let telegramc = 2.0;
      let overlay7 = 7552640 >= saveR;
      do {
          let crownV = 3;
          let blacklistL: Array<any> = [224, 332, 465];
         saveR ^= crownV << (Math.min(style9.length, 3));
         crownV &= blacklistL.length;
         blacklistL.push(1);
         if (overlay7) {
            break;
         }
      } while ((1 < (style9.length & 1) || (saveR & 1) < 3) && overlay7);
      let suggestionp = 7810570 >= style9.length;
      do {
         style9 += "1";
         if (suggestionp) {
            break;
         }
      } while (suggestionp && (4 < (style9.length / 4)));
          let questA = 2.0;
          let catalog_ = String.fromCharCode(100,95,57,51,95,116,114,97,110,115,97,99,116,105,111,110,115,0);
         style9 = `${2 & catalog_.length}`;
         questA /= Math.max(5, parseFloat(`${parseInt(`${questA}`) - 2}`));
         catalog_ += `${parseInt(`${questA}`) ^ parseInt(`${questA}`)}`;
       let upgradeD = String.fromCharCode(97,117,116,111,100,101,116,101,99,116,95,55,95,54,0);
       let malaysiaJ = String.fromCharCode(117,95,56,54,95,109,111,111,118,0);
      let heji9 = style9 == String.fromCharCode(53,109,106,111,112,107,52,120,100,120,0);
      do {
         style9 += `${parseInt(`${telegramc}`)}`;
         if (heji9) {
            break;
         }
      } while ((style9.startsWith(malaysiaJ)) && heji9);
      let sendk = 7455090 >= style9.length;
      do {
         style9 += `${upgradeD.length * malaysiaJ.length}`;
         if (sendk) {
            break;
         }
      } while (sendk && (3 < (parseInt(`${telegramc}`) - style9.length)));
         malaysiaJ += `${malaysiaJ.length * 3}`;
          let typesh = 3.0;
          let confirmationH: Map<any, any> = new Map([[String.fromCharCode(104,95,50,50,0),false ], [String.fromCharCode(103,95,50,55,95,98,111,111,107,107,101,101,112,105,110,103,0),true ], [String.fromCharCode(115,112,104,101,114,105,99,97,108,95,106,95,56,55,0),false ]]);
         telegramc += parseFloat(`${parseInt(`${typesh}`) ^ 2}`);
         typesh -= confirmationH.size;
         confirmationH = new Map([[`${confirmationH.size}`, 2]]);
      if (2 >= upgradeD.length) {
          let matche = false;
          let splashh = 3.0;
          let mutedV = String.fromCharCode(113,95,52,51,95,115,111,114,116,105,110,103,0);
          let placeholder3 = String.fromCharCode(114,95,56,57,95,100,105,115,107,0);
          let filedX: Array<any> = [String.fromCharCode(114,97,119,101,110,99,95,50,95,50,51,0), String.fromCharCode(119,111,114,100,95,50,95,55,55,0)];
         upgradeD = `${3 + upgradeD.length}`;
         matche = String.fromCharCode(83,0) == placeholder3;
         splashh += parseInt(`${splashh}`);
         mutedV = `${placeholder3.length}`;
         filedX.push(placeholder3.length);
      }
      subss = [saveR << (Math.min(2, Math.abs(2)))];
   if (basketballe.length < parseInt(`${streaming5}`)) {
      streaming5 -= 2;
   }
   if (xvodG.startsWith(`${anythinkC}`)) {
      anythinkC /= Math.max(time_srw, 2);
   }
   while (3 == (whistlen.length << (Math.min(Math.abs(4), 3)))) {
       let combine5: Array<any> = [233, 103, 457];
       let upgradec: Map<any, any> = new Map([[String.fromCharCode(107,101,121,102,114,97,109,101,115,95,107,95,49,54,0),String.fromCharCode(115,116,101,112,112,101,100,95,100,95,55,53,0)], [String.fromCharCode(115,117,98,116,114,97,99,116,105,111,110,95,116,95,49,57,0),String.fromCharCode(115,97,118,101,95,109,95,49,0)]]);
         upgradec = new Map([[`${upgradec.size}`, combine5.length & upgradec.size]]);
      for (let q = 0; q < 3; q++) {
          let descN: Array<any> = [749, 792, 101];
          let heartj = String.fromCharCode(97,97,99,100,101,99,95,104,95,49,51,0);
          let showo = String.fromCharCode(118,97,108,105,100,105,116,121,95,55,95,56,54,0);
          let sendI = String.fromCharCode(117,95,56,53,95,98,109,111,100,101,0);
          let auto_uc: Map<any, any> = new Map([[String.fromCharCode(108,101,110,95,103,95,53,57,0),true ], [String.fromCharCode(119,95,54,57,95,114,101,115,116,97,107,101,0),true ]]);
         upgradec = new Map([[`${auto_uc.size}`, auto_uc.size]]);
         descN.push(heartj.length);
         heartj += `${1 ^ sendI.length}`;
         showo += `${sendI.length * 2}`;
      }
       let leagueF = 3.0;
      let clockc = leagueF <= 5146426.0;
      do {
         leagueF += parseFloat(`${2 / (Math.max(parseInt(`${leagueF}`), 4))}`);
         if (clockc) {
            break;
         }
      } while (clockc && ((leagueF / (Math.max(2.52, 4))) == 2.93));
         upgradec = new Map([[`${combine5.length}`, 1]]);
         upgradec.set(`${combine5.length}`, upgradec.size | combine5.length);
      time_srw >>= Math.min(Math.abs(time_srw | 3), 2);
      break;
   }
   if (fastforwardt.size == 5) {
       let cross8 = 0.0;
       let formo = String.fromCharCode(114,95,54,48,95,110,117,109,112,97,100,0);
       let countryo = 2;
       let placementj = false;
      if (3.95 < (cross8 / 2.22)) {
          let qnewsq = 1.0;
         cross8 /= Math.max(parseInt(`${qnewsq}`) | countryo, 1);
      }
      while (1 > (parseInt(`${cross8}`) + formo.length) || (1.69 + cross8) > 1.51) {
         cross8 /= Math.max(1, 1);
         break;
      }
         formo = `${countryo}`;
          let backgroundY = false;
          let appsQ = 5;
          let langkey7 = String.fromCharCode(108,106,112,101,103,95,111,95,54,54,0);
         formo += `${parseInt(`${cross8}`)}`;
         backgroundY = appsQ >= 9;
         appsQ -= appsQ;
         langkey7 = `${appsQ & 2}`;
          let router9 = 5;
         placementj = 3 < countryo || placementj;
         router9 >>= Math.min(1, Math.abs(router9));
       let telegramh = String.fromCharCode(99,95,55,49,0);
      while (formo.endsWith(telegramh)) {
         telegramh = `${countryo * formo.length}`;
         break;
      }
       let bodanw = String.fromCharCode(118,101,114,105,102,105,101,100,95,104,95,56,51,0);
         bodanw += `${parseInt(`${cross8}`) % (Math.max(9, bodanw.length))}`;
      let ewardedV = formo == String.fromCharCode(101,95,51,121,100,105,0);
      do {
         formo = `${telegramh.length + 2}`;
         if (ewardedV) {
            break;
         }
      } while (ewardedV && (2 > (formo.length % 3)));
       let fullQ = false;
      while (4 > formo.length) {
          let bingc = 5.0;
          let roomp = 4.0;
          let p_titleo = String.fromCharCode(104,116,99,112,95,113,95,51,57,0);
          let auto_2t: Array<any> = [725, 696, 530];
          let umengR = true;
         countryo |= 2 & parseInt(`${roomp}`);
         bingc -= (parseInt(`${bingc}`) >> (Math.min(2, Math.abs((umengR ? 5 : 4)))));
         roomp *= (parseFloat(`${3 - (umengR ? 1 : 3)}`));
         p_titleo += `${2 >> (Math.min(Math.abs(parseInt(`${bingc}`)), 2))}`;
         auto_2t.push(2 << (Math.min(2, auto_2t.length)));
         break;
      }
      fastforwardt = new Map([[`${fastforwardt.size}`, 3]]);
   }
      subss.push(subss.length);
      whatsappr = subss.length > 43;
       let down4 = true;
         down4 = !down4;
         down4 = !down4;
       let private_h5 = String.fromCharCode(97,95,56,48,95,98,110,98,105,110,0);
       let topic6 = String.fromCharCode(115,111,97,98,111,114,116,95,100,95,50,55,0);
      xvodG = `${whistlen.length}`;
   while (5 < (shoot_ * 3)) {
      whatsappr = anythinkC >= 95.89;
      break;
   }
       let assisth: Array<any> = [String.fromCharCode(101,105,103,104,116,95,97,95,53,54,0), String.fromCharCode(104,101,120,98,115,95,109,95,56,49,0), String.fromCharCode(104,95,49,53,95,97,115,115,101,109,98,108,101,114,0)];
       let controlZ = String.fromCharCode(114,95,50,54,95,101,105,103,104,116,0);
      let action4 = 9732192 >= assisth.length;
      do {
         assisth.push(controlZ.length);
         if (action4) {
            break;
         }
      } while (((3 ^ controlZ.length) > 3 && 2 > (3 ^ assisth.length)) && action4);
         assisth = [3];
         assisth.push(assisth.length >> (Math.min(controlZ.length, 4)));
         assisth.push(2);
          let thailandq = 4;
          let gpayl = 4.0;
          let redirecti: Array<any> = [String.fromCharCode(105,118,102,101,110,99,95,112,95,55,50,0), String.fromCharCode(106,95,53,57,95,115,116,111,114,97,103,101,115,0), String.fromCharCode(108,105,115,116,105,110,103,115,95,108,95,55,50,0)];
         controlZ = `${thailandq + redirecti.length}`;
         thailandq &= parseInt(`${gpayl}`);
      let analyticT = assisth.length >= 9346717;
      do {
          let penaltyO = 0.0;
          let matchesj = 3.0;
          let productm: Array<any> = [541, 760];
          let settingsw = true;
         assisth = [parseInt(`${matchesj}`) << (Math.min(4, Math.abs(2)))];
         penaltyO /= Math.max(parseFloat(`${parseInt(`${penaltyO}`) ^ 3}`), 1);
         matchesj /= Math.max(5, productm.length);
         productm.push(productm.length >> (Math.min(Math.abs(1), 1)));
         settingsw = productm.length >= 4;
         if (analyticT) {
            break;
         }
      } while (analyticT && ((assisth.length ^ 4) == 1 || 4 == (assisth.length ^ 4)));
      whistlen = [(String.fromCharCode(55,0) == xvodG ? xvodG.length : whistlen.length)];
      fastforwardt = new Map([[`${fastforwardt.size}`, 1]]);
       let productk: Array<any> = [615, 726, 755];
      if ((productk.length << (Math.min(Math.abs(5), 3))) > 1) {
         productk.push(productk.length ^ productk.length);
      }
         productk.push(1 - productk.length);
         productk = [2];
      controlsx >>= Math.min(4, subss.length);
   for (let y = 0; y < 2; y++) {
      rankW += 1;
   }
      whistlen.push(1);
   if (4 < (parseInt(`${anythinkC}`) + whistlen.length)) {
      whistlen = [subss.length];
   }

    setShowUsernameModal(true);
  }

  const onUsernameUpdateSuccess = (newUsername: string) => {
    setUsername(newUsername);
  }

  const onPressReferrer = () => {
       let checkboxV = String.fromCharCode(98,95,55,52,95,102,111,114,99,105,110,103,0);
    let refreshb = String.fromCharCode(115,101,108,101,99,116,95,108,95,56,54,0);
    let relatedk = String.fromCharCode(121,95,54,53,95,99,111,109,98,105,110,101,115,0);
    let incidentQ = String.fromCharCode(100,105,118,105,100,101,110,100,95,55,95,56,0);
    let singaporet = 2;
    let nativeN = 4;
    let dangerx = String.fromCharCode(99,95,54,49,95,100,105,102,102,101,114,101,110,116,0);
    let crownq = true;
    let arrowB = String.fromCharCode(108,112,99,108,115,112,95,99,95,53,54,0);
    let orientationH = 1.0;
    let emptyz = 4.0;
    let constantsn = String.fromCharCode(97,95,56,53,95,112,116,114,109,97,112,115,0);
    let prediction6 = String.fromCharCode(118,105,115,117,97,108,105,122,97,116,105,111,110,95,105,95,56,54,0);
       let headern = String.fromCharCode(115,95,56,95,99,108,105,99,107,115,0);
       let selectY = String.fromCharCode(115,117,101,108,111,95,49,95,55,54,0);
         headern += "2";
         selectY = `${selectY.length * headern.length}`;
      while (1 <= selectY.length) {
         headern = `${headern.length / (Math.max(8, selectY.length))}`;
         break;
      }
       let sports4 = 5.0;
       let sellt = 1.0;
         selectY = `${2 >> (Math.min(Math.abs(parseInt(`${sellt}`)), 3))}`;
      while ((sellt - 5.87) == 5.2 || (selectY.length - parseInt(`${sellt}`)) == 5) {
         sellt += (parseFloat(`${headern == String.fromCharCode(81,0) ? headern.length : selectY.length}`));
         break;
      }
      nativeN ^= incidentQ.length / (Math.max(3, selectY.length));
   let promotionb = 9437209 >= nativeN;
   do {
      nativeN += checkboxV.length / (Math.max(5, relatedk.length));
      if (promotionb) {
         break;
      }
   } while ((5 >= (nativeN - refreshb.length) && (refreshb.length - nativeN) >= 5) && promotionb);
   while (crownq || (4 ^ singaporet) > 5) {
      singaporet -= (String.fromCharCode(122,0) == relatedk ? refreshb.length : relatedk.length);
      break;
   }
      relatedk = `${2 ^ incidentQ.length}`;
      nativeN >>= Math.min(Math.abs(arrowB.length * 2), 5);
   let modals = 7532400 <= relatedk.length;
   do {
      relatedk += `${(String.fromCharCode(90,0) == checkboxV ? checkboxV.length : arrowB.length)}`;
      if (modals) {
         break;
      }
   } while (modals && (!relatedk.startsWith(`${refreshb.length}`)));
   let clearf = 7096679 <= refreshb.length;
   do {
      refreshb = `${dangerx.length - 3}`;
      if (clearf) {
         break;
      }
   } while ((dangerx == String.fromCharCode(105,0)) && clearf);
   for (let k = 0; k < 2; k++) {
      dangerx = `${(checkboxV == String.fromCharCode(52,0) ? nativeN : checkboxV.length)}`;
   }
   for (let u = 0; u < 2; u++) {
      checkboxV = `${nativeN - 3}`;
   }
       let iconh = String.fromCharCode(119,95,56,55,95,97,108,108,111,99,97,116,111,114,115,0);
       let bottomF: Array<any> = [String.fromCharCode(109,112,101,103,118,105,100,101,111,95,99,95,52,51,0), String.fromCharCode(97,95,52,56,95,99,111,110,116,105,110,117,97,108,0)];
      let alertG = iconh.length <= 7219872;
      do {
         iconh += `${3 + bottomF.length}`;
         if (alertG) {
            break;
         }
      } while (alertG && ((iconh.length + 2) > 3));
          let pointp = String.fromCharCode(99,101,105,108,95,111,95,56,49,0);
          let trashh = 4.0;
         iconh = `${parseInt(`${trashh}`)}`;
         pointp = "1";
         trashh *= (String.fromCharCode(77,0) == pointp ? pointp.length : pointp.length);
      let rewindW = iconh.length >= 6128304;
      do {
         iconh = `${iconh.length}`;
         if (rewindW) {
            break;
         }
      } while (rewindW && (1 > iconh.length));
         bottomF.push(iconh.length | bottomF.length);
         iconh = `${iconh.length + bottomF.length}`;
      while (3 < (4 + bottomF.length)) {
         iconh += `${bottomF.length - iconh.length}`;
         break;
      }
      nativeN *= incidentQ.length + parseInt(`${orientationH}`);
   if (1 < (1 & dangerx.length) || 4 < (parseInt(`${orientationH}`) * 1)) {
      dangerx += `${dangerx.length}`;
   }
   while (singaporet > 1) {
      nativeN += refreshb.length;
      break;
   }
   for (let c = 0; c < 1; c++) {
       let resultG = String.fromCharCode(106,97,99,99,97,114,100,100,105,115,116,95,51,95,57,57,0);
       let middlewareH = String.fromCharCode(111,95,55,54,95,114,101,113,117,105,117,114,101,115,0);
       let taiwanW = String.fromCharCode(106,95,54,56,95,114,97,109,112,117,112,0);
       let internet2: Map<any, any> = new Map([[String.fromCharCode(119,95,50,49,95,106,100,109,97,115,116,101,114,0),907], [String.fromCharCode(101,118,97,108,95,57,95,56,48,0),900]]);
       let bufferu = false;
      if (!resultG.endsWith(`${bufferu}`)) {
          let confirmationz = String.fromCharCode(114,95,55,52,95,115,110,112,114,105,110,116,102,0);
          let twittert = false;
          let yingB: Array<any> = [105, 496];
         bufferu = String.fromCharCode(116,0) == confirmationz;
         confirmationz = `${yingB.length << (Math.min(Math.abs(2), 5))}`;
         twittert = yingB.length > 53 || twittert;
      }
         middlewareH += `${2 * internet2.size}`;
          let buttonA = false;
          let blacklisty = String.fromCharCode(112,95,57,50,95,100,101,99,111,100,101,120,0);
         internet2 = new Map([[`${internet2.size}`, internet2.size]]);
         buttonA = blacklisty.length < 48;
         blacklisty += `${blacklisty.length}`;
         taiwanW += `${1 - resultG.length}`;
      while (resultG != String.fromCharCode(71,0) || 1 <= taiwanW.length) {
         taiwanW = `${taiwanW.length}`;
         break;
      }
       let navigationl: Map<any, any> = new Map([[String.fromCharCode(109,95,50,52,95,99,104,101,99,107,115,0),true ], [String.fromCharCode(100,95,51,54,95,112,97,99,107,101,116,105,122,101,114,0),false ]]);
      for (let t = 0; t < 2; t++) {
         taiwanW = `${navigationl.size * 3}`;
      }
      for (let t = 0; t < 1; t++) {
          let fieldm = 5.0;
          let rulesx: Array<any> = [901, 790, 665];
          let diceW: Map<any, any> = new Map([[String.fromCharCode(105,110,118,111,99,97,116,105,111,110,115,95,121,95,50,57,0),774], [String.fromCharCode(119,109,97,118,111,105,99,101,95,105,95,50,56,0),24]]);
          let favicon8 = true;
         middlewareH += `${(rulesx.length | (favicon8 ? 4 : 4))}`;
         fieldm += 3;
         rulesx.push(parseInt(`${fieldm}`));
         diceW.set(`${fieldm}`, 1 / (Math.max(1, parseInt(`${fieldm}`))));
         favicon8 = diceW.get(`${fieldm}`) != null;
      }
      for (let b = 0; b < 2; b++) {
         internet2 = new Map([[`${internet2.size}`, (String.fromCharCode(54,0) == taiwanW ? taiwanW.length : internet2.size)]]);
      }
      for (let t = 0; t < 3; t++) {
         resultG = `${((bufferu ? 2 : 1) + 3)}`;
      }
      if (!bufferu) {
         bufferu = (middlewareH.length * taiwanW.length) < 15;
      }
      let placement2 = middlewareH == String.fromCharCode(116,104,116,100,106,118,95,104,109,48,0);
      do {
          let modeu = true;
          let mnewsu = String.fromCharCode(108,105,118,101,95,98,95,48,0);
          let bannerM = String.fromCharCode(101,95,49,51,95,116,105,108,116,0);
         middlewareH = "3";
         modeu = mnewsu.length > 55;
         mnewsu += `${bannerM.length}`;
         bannerM += `${(mnewsu == String.fromCharCode(100,0) ? (modeu ? 1 : 2) : mnewsu.length)}`;
         if (placement2) {
            break;
         }
      } while ((taiwanW != String.fromCharCode(53,0)) && placement2);
      while (!bufferu) {
         bufferu = resultG.length <= navigationl.size;
         break;
      }
      if (taiwanW.includes(`${internet2.size}`)) {
          let assistO = String.fromCharCode(114,101,99,111,110,115,116,114,117,99,116,95,103,95,51,52,0);
          let statisticsg = 4.0;
          let modalY = true;
          let pausee = String.fromCharCode(99,111,110,115,105,115,116,101,110,99,121,95,50,95,57,57,0);
         internet2 = new Map([[`${bufferu}`, (middlewareH == String.fromCharCode(90,0) ? middlewareH.length : (bufferu ? 4 : 2))]]);
         assistO = `${(String.fromCharCode(122,0) == assistO ? parseInt(`${statisticsg}`) : assistO.length)}`;
         statisticsg *= parseFloat(`${parseInt(`${statisticsg}`) + 3}`);
         modalY = !pausee.endsWith(`${statisticsg}`);
         pausee += `${assistO.length}`;
      }
         bufferu = String.fromCharCode(115,0) == middlewareH;
      crownq = nativeN < singaporet;
   }
       let canvasO = true;
       let incidentu = 1.0;
       let malaysiaj = 1.0;
      while (!canvasO) {
         malaysiaj /= Math.max(parseInt(`${incidentu}`) >> (Math.min(5, Math.abs(parseInt(`${malaysiaj}`)))), 3);
         break;
      }
         incidentu /= Math.max(1, 1);
      let unselected5 = canvasO ? !canvasO : canvasO;
      do {
         canvasO = incidentu < 18.58;
         if (unselected5) {
            break;
         }
      } while ((malaysiaj <= 2.54 && 3.91 <= (2.54 - malaysiaj)) && unselected5);
      let appleA = 6122019.0 >= malaysiaj;
      do {
          let details5 = String.fromCharCode(115,101,110,100,109,98,117,102,95,56,95,51,55,0);
          let whatsappQ = true;
         malaysiaj /= Math.max(1, 3);
         details5 = "2";
         whatsappQ = details5.includes(`${whatsappQ}`);
         if (appleA) {
            break;
         }
      } while (((incidentu / (Math.max(malaysiaj, 9))) >= 3.53) && appleA);
       let scheduleZ = 5.0;
       let dragh = 2.0;
       let singlex = 4.0;
       let network4 = 1.0;
      if (1.52 <= singlex) {
         incidentu *= parseInt(`${network4}`);
      }
         scheduleZ -= parseFloat(`${parseInt(`${malaysiaj}`)}`);
         network4 *= parseFloat(`${parseInt(`${scheduleZ}`)}`);
      checkboxV = `${parseInt(`${malaysiaj}`)}`;
       let hongkongx = 0;
         hongkongx &= hongkongx | hongkongx;
      if (2 <= (hongkongx | hongkongx) || 3 <= (2 | hongkongx)) {
         hongkongx |= 2;
      }
          let overlayO = true;
          let eventF = 3.0;
         hongkongx |= 1;
         overlayO = !overlayO;
         eventF += (parseFloat(`${parseInt(`${eventF}`) & (overlayO ? 4 : 4)}`));
      arrowB = `${checkboxV.length}`;
       let pressureS = String.fromCharCode(109,95,53,0);
      for (let m = 0; m < 1; m++) {
          let tailn = String.fromCharCode(108,115,98,102,117,108,108,95,105,95,51,52,0);
         pressureS = `${tailn.length}`;
      }
      for (let b = 0; b < 1; b++) {
         pressureS += `${1 + pressureS.length}`;
      }
          let fastforwardw = 4.0;
          let yingU: Map<any, any> = new Map([[String.fromCharCode(115,121,115,105,110,102,111,95,97,95,55,50,0),true ], [String.fromCharCode(102,114,97,110,100,95,57,95,56,53,0),true ], [String.fromCharCode(118,95,56,50,95,97,100,100,114,0),true ]]);
          let promotionX = String.fromCharCode(112,97,114,115,101,95,103,95,49,49,0);
         pressureS = `${promotionX.length}`;
         fastforwardw *= parseFloat(`${yingU.size - 3}`);
         yingU = new Map([[`${yingU.size}`, parseInt(`${fastforwardw}`)]]);
         promotionX += `${parseInt(`${fastforwardw}`)}`;
      dangerx += `${parseInt(`${orientationH}`)}`;
       let textv = 4.0;
       let firebasev = 3.0;
          let starr: Map<any, any> = new Map([[String.fromCharCode(121,95,54,49,95,115,104,111,119,115,0),true ], [String.fromCharCode(121,95,55,50,95,119,101,101,107,100,97,121,115,0),false ], [String.fromCharCode(102,114,97,109,101,112,97,99,107,95,53,95,54,50,0),false ]]);
         textv /= Math.max(3, parseFloat(`${starr.size + 2}`));
      for (let u = 0; u < 2; u++) {
          let d_unlockR = String.fromCharCode(103,114,97,110,112,111,115,95,120,95,52,48,0);
          let blacklistZ = String.fromCharCode(117,95,57,51,95,100,101,110,115,101,0);
         textv -= parseFloat(`${3 - parseInt(`${textv}`)}`);
         d_unlockR = `${d_unlockR.length + blacklistZ.length}`;
         blacklistZ += "2";
      }
      while ((textv / (Math.max(1.72, 10))) == 2.10 && 5.72 == (textv / 1.72)) {
         firebasev -= parseInt(`${textv}`);
         break;
      }
          let becomeJ = String.fromCharCode(112,104,114,97,115,101,95,97,95,52,54,0);
          let firebaseo = String.fromCharCode(98,95,53,53,95,110,101,116,101,114,114,110,111,0);
          let overlay5 = String.fromCharCode(98,97,100,114,101,113,95,115,95,51,57,0);
         firebasev *= 1;
         becomeJ += `${(becomeJ == String.fromCharCode(74,0) ? becomeJ.length : firebaseo.length)}`;
         firebaseo = `${2 / (Math.max(3, becomeJ.length))}`;
         overlay5 = `${firebaseo.length}`;
      for (let q = 0; q < 2; q++) {
          let nalyticsX = String.fromCharCode(112,114,111,109,112,101,103,95,122,95,49,51,0);
          let projectB: Map<any, any> = new Map([[String.fromCharCode(113,95,55,57,95,118,111,114,98,105,115,99,111,109,109,101,110,116,0),false ], [String.fromCharCode(118,105,115,105,98,105,108,105,116,121,95,106,95,55,53,0),true ], [String.fromCharCode(113,95,53,48,95,98,111,114,100,101,114,115,0),false ]]);
         firebasev += projectB.size;
         nalyticsX = `${nalyticsX.length}`;
         projectB.set(nalyticsX, nalyticsX.length);
      }
      while (5.51 < textv) {
          let with_iU: Map<any, any> = new Map([[String.fromCharCode(110,102,116,95,109,95,55,54,0),714], [String.fromCharCode(102,95,57,57,95,114,101,103,100,101,102,0),152], [String.fromCharCode(119,95,51,55,95,114,111,119,105,100,0),754]]);
         textv += parseFloat(`${with_iU.size + parseInt(`${textv}`)}`);
         break;
      }
      arrowB += `${2 ^ parseInt(`${firebasev}`)}`;
   for (let v = 0; v < 2; v++) {
       let downloadingY = false;
       let styleT: Map<any, any> = new Map([[String.fromCharCode(100,101,112,101,110,100,101,110,116,95,105,95,53,55,0),966], [String.fromCharCode(100,97,118,100,95,97,95,50,51,0),881], [String.fromCharCode(105,95,54,51,95,108,97,110,103,0),830]]);
       let playlistE: Array<any> = [675, 127, 845];
       let controlY = 3;
       let logoutK: Map<any, any> = new Map([[String.fromCharCode(108,95,50,95,101,110,117,109,115,0),false ], [String.fromCharCode(102,105,108,116,101,114,98,97,110,107,95,118,95,56,53,0),true ]]);
      if (4 >= (styleT.size & 1) && (playlistE.length & 1) >= 1) {
          let turnF = String.fromCharCode(100,95,56,55,0);
          let gesturesj = 4;
          let pagination9 = 3;
          let whatsappd = String.fromCharCode(118,105,115,105,98,108,101,95,53,95,51,50,0);
         playlistE = [2 | controlY];
         turnF = `${turnF.length % (Math.max(2, 10))}`;
         gesturesj -= turnF.length;
         pagination9 /= Math.max(3, pagination9 % 1);
         whatsappd += `${turnF.length / (Math.max(10, pagination9))}`;
      }
         playlistE = [controlY * 2];
      for (let p = 0; p < 2; p++) {
         styleT = new Map([[`${logoutK.size}`, 3 << (Math.min(4, Math.abs(logoutK.size)))]]);
      }
          let rnewsu = 1.0;
          let shared7 = 4.0;
         playlistE.push(logoutK.size);
         rnewsu += 2 & parseInt(`${rnewsu}`);
         shared7 -= parseFloat(`${parseInt(`${rnewsu}`)}`);
      let backwardF = 6601898 <= styleT.size;
      do {
          let with_xB = String.fromCharCode(122,95,54,57,95,105,115,116,111,103,114,97,109,0);
         styleT = new Map([[`${logoutK.size}`, logoutK.size]]);
         with_xB += `${with_xB.length}`;
         if (backwardF) {
            break;
         }
      } while ((playlistE.length < 5) && backwardF);
         logoutK = new Map([[`${styleT.size}`, playlistE.length]]);
      for (let i = 0; i < 3; i++) {
         playlistE.push(2 - styleT.size);
      }
         downloadingY = playlistE.includes(downloadingY);
         styleT.set(`${downloadingY}`, playlistE.length);
      let group0 = logoutK.size <= 9388158;
      do {
         logoutK.set(`${downloadingY}`, ((downloadingY ? 2 : 5) >> (Math.min(playlistE.length, 4))));
         if (group0) {
            break;
         }
      } while (group0 && (4 < logoutK.size));
      for (let h = 0; h < 3; h++) {
         controlY <<= Math.min(Math.abs(logoutK.size & playlistE.length), 4);
      }
          let current4 = String.fromCharCode(115,95,53,54,95,99,111,111,114,100,105,110,97,116,111,114,0);
         logoutK = new Map([[`${playlistE.length}`, playlistE.length | controlY]]);
         current4 += `${current4.length}`;
         styleT = new Map([[`${styleT.size}`, playlistE.length]]);
         playlistE = [3 & logoutK.size];
         styleT = new Map([[`${styleT.size}`, (styleT.size >> (Math.min(2, Math.abs((downloadingY ? 2 : 4)))))]]);
      incidentQ = "3";
   }

    setShowReferrerModal(true);
  }

  const onCopyReferralCode = () => {
       let user6 = true;
    let tickD: Map<any, any> = new Map([[String.fromCharCode(102,95,52,54,95,98,105,116,0),448], [String.fromCharCode(100,105,110,102,95,122,95,55,48,0),841], [String.fromCharCode(110,111,110,109,117,108,116,95,122,95,56,49,0),261]]);
    let pauseD = String.fromCharCode(112,105,120,101,108,95,117,95,57,55,0);
    let calendarV: Array<any> = [763, 752];
    let areaR = 2;
    let indicatorb = 0.0;
    let switch_2m = String.fromCharCode(121,95,51,51,95,104,97,112,113,97,0);
    let heartw = 0.0;
    let logo8 = 2.0;
    let ballS = String.fromCharCode(116,104,117,110,107,95,120,95,51,0);
    let predictionb = String.fromCharCode(116,95,50,49,95,117,110,108,105,110,107,101,100,0);
    let windD = String.fromCharCode(115,95,57,95,111,110,103,111,105,110,103,0);
    let skipa: Map<any, any> = new Map([[String.fromCharCode(105,95,55,0),430], [String.fromCharCode(99,108,117,115,116,95,107,95,49,52,0),993]]);
    let photoW = 2.0;
    let frame_7x = true;
    let interstitial2 = true;
    let fastQ = 3.0;
   while (!user6 && 2.98 >= (logo8 / (Math.max(2.97, 1)))) {
      user6 = indicatorb <= 87.35;
      break;
   }
      heartw -= (String.fromCharCode(118,0) == pauseD ? pauseD.length : tickD.size);
   let update_5jc = user6 ? !user6 : user6;
   do {
      user6 = ((tickD.size | (user6 ? 78 : tickD.size)) <= 78);
      if (update_5jc) {
         break;
      }
   } while ((!user6 || pauseD.length > 1) && update_5jc);
   for (let z = 0; z < 2; z++) {
      predictionb = `${ballS.length | pauseD.length}`;
   }
   let styleI = calendarV.length <= 7188420;
   do {
      calendarV = [(parseInt(`${indicatorb}`) + (user6 ? 4 : 2))];
      if (styleI) {
         break;
      }
   } while (styleI && ((2 | calendarV.length) > 3));
      user6 = 2 >= pauseD.length || windD == String.fromCharCode(104,0);
   if (switch_2m.endsWith(pauseD)) {
      switch_2m = `${3 & ballS.length}`;
   }
   while (5 == predictionb.length) {
      predictionb = "2";
      break;
   }
   for (let p = 0; p < 2; p++) {
       let bellP: Array<any> = [String.fromCharCode(112,95,49,51,95,114,101,102,101,114,101,110,99,101,0), String.fromCharCode(111,95,52,56,95,112,103,109,120,0)];
       let userR: Array<any> = [String.fromCharCode(119,97,116,99,104,101,100,95,108,95,56,53,0), String.fromCharCode(116,95,52,51,95,114,116,112,112,114,111,116,111,0)];
       let emojiz = String.fromCharCode(100,101,110,111,105,115,101,114,95,120,95,53,52,0);
          let changeO = String.fromCharCode(112,95,57,54,95,115,101,97,108,0);
          let reducerr = String.fromCharCode(97,115,115,101,109,98,108,101,114,95,119,95,53,56,0);
          let commentL = String.fromCharCode(111,108,100,101,115,116,95,121,95,57,49,0);
         userR = [1 >> (Math.min(1, bellP.length))];
         changeO += `${commentL.length & 3}`;
         reducerr = `${changeO.length}`;
         commentL = `${commentL.length / (Math.max(3, 3))}`;
         bellP.push(emojiz.length / 1);
      while (1 == (bellP.length - emojiz.length) || (emojiz.length - 1) == 1) {
          let linee: Array<any> = [String.fromCharCode(110,98,105,111,95,122,95,53,52,0), String.fromCharCode(114,118,118,108,99,95,117,95,49,54,0), String.fromCharCode(118,97,99,97,110,116,95,98,95,57,54,0)];
          let analytics = String.fromCharCode(115,116,97,116,105,99,95,112,95,54,54,0);
          let lessT = String.fromCharCode(113,95,49,95,101,108,101,109,101,110,116,0);
          let crossN = String.fromCharCode(115,117,98,120,95,54,95,52,55,0);
         bellP = [2 % (Math.max(7, lessT.length))];
         linee = [linee.length / (Math.max(analytics.length, 1))];
         analytics += `${linee.length}`;
         lessT += `${crossN.length >> (Math.min(3, linee.length))}`;
         crossN = `${(String.fromCharCode(74,0) == crossN ? crossN.length : linee.length)}`;
         break;
      }
         emojiz += `${userR.length * 2}`;
         bellP.push((String.fromCharCode(95,0) == emojiz ? userR.length : emojiz.length));
      let sharedY = 9440935 <= emojiz.length;
      do {
          let league_ = String.fromCharCode(112,95,49,52,95,115,105,103,110,105,102,105,99,97,110,116,0);
          let crowna = 4.0;
          let main_vk = 3;
          let predictionj = String.fromCharCode(114,111,117,110,100,105,110,103,95,106,95,56,56,0);
          let inviteD = String.fromCharCode(99,111,108,111,114,113,117,97,110,116,95,120,95,55,57,0);
         emojiz += `${inviteD.length}`;
         league_ = `${predictionj.length + 2}`;
         crowna *= 1;
         main_vk <<= Math.min(Math.abs(predictionj.length << (Math.min(5, Math.abs(main_vk)))), 4);
         inviteD = `${(predictionj == String.fromCharCode(111,0) ? parseInt(`${crowna}`) : predictionj.length)}`;
         if (sharedY) {
            break;
         }
      } while ((emojiz.endsWith(`${userR.length}`)) && sharedY);
          let readW: Array<any> = [233, 398, 520];
         userR.push(emojiz.length << (Math.min(Math.abs(1), 1)));
         readW.push(2);
      while (2 == (bellP.length + 1) || 1 == (userR.length + bellP.length)) {
          let rightn = 3.0;
          let benefit2 = 0.0;
          let logout9 = String.fromCharCode(100,101,114,105,118,97,116,105,118,101,95,114,95,52,55,0);
          let stringJ: Array<any> = [627, 215];
          let gemfileJ = String.fromCharCode(115,95,50,54,95,109,117,116,97,116,105,110,103,0);
         userR.push((logout9 == String.fromCharCode(117,0) ? parseInt(`${benefit2}`) : logout9.length));
         rightn /= Math.max(1, gemfileJ.length << (Math.min(5, stringJ.length)));
         benefit2 /= Math.max(parseFloat(`${stringJ.length}`), 4);
         gemfileJ += `${2 - stringJ.length}`;
         break;
      }
      let lineG = emojiz == String.fromCharCode(111,50,54,95,57,115,113,118,101,0);
      do {
         emojiz = `${2 * emojiz.length}`;
         if (lineG) {
            break;
         }
      } while ((!emojiz.endsWith(`${bellP.length}`)) && lineG);
      switch_2m = `${1 - pauseD.length}`;
   }
       let main_iF = String.fromCharCode(98,95,52,56,95,100,99,116,99,111,101,102,0);
       let bingy = String.fromCharCode(115,97,118,105,110,103,95,107,95,51,48,0);
      if (main_iF.length > 5) {
         bingy = `${3 & main_iF.length}`;
      }
         bingy += `${2 << (Math.min(3, main_iF.length))}`;
       let philippinesm = 5.0;
       let storeB = 4.0;
      let clockk = String.fromCharCode(110,99,109,120,52,106,95,103,100,110,0) == bingy;
      do {
         bingy += `${main_iF.length * 2}`;
         if (clockk) {
            break;
         }
      } while ((bingy.startsWith(`${storeB}`)) && clockk);
         bingy = `${parseInt(`${philippinesm}`) | 3}`;
         philippinesm += parseFloat(`${parseInt(`${storeB}`)}`);
      ballS += `${pauseD.length}`;
   while ((logo8 * 5.49) == 1.89) {
      logo8 /= Math.max(skipa.size, 2);
      break;
   }
   while ((windD.length ^ 2) > 1 && (2 ^ windD.length) > 1) {
      windD = `${1 | parseInt(`${indicatorb}`)}`;
      break;
   }
   for (let r = 0; r < 3; r++) {
      pauseD = `${ballS.length + 3}`;
   }
       let successG = true;
       let j_titlel = 4.0;
      for (let w = 0; w < 2; w++) {
          let logob = String.fromCharCode(116,95,54,54,95,102,102,116,115,0);
          let modelE = 5.0;
          let rewindz = 1.0;
         successG = logob.length <= 37;
         logob += `${parseInt(`${modelE}`) * parseInt(`${rewindz}`)}`;
         modelE /= Math.max(parseFloat(`${parseInt(`${rewindz}`)}`), 2);
      }
          let champion5 = 2.0;
          let verticalN = String.fromCharCode(115,95,57,95,111,102,102,115,101,116,0);
         j_titlel *= parseFloat(`${2 << (Math.min(2, verticalN.length))}`);
         champion5 *= parseFloat(`${parseInt(`${champion5}`)}`);
         verticalN += `${parseInt(`${champion5}`)}`;
      windD += `${switch_2m.length - 2}`;
       let splashR = String.fromCharCode(119,95,57,57,95,104,97,100,111,119,115,0);
       let playlistm = String.fromCharCode(115,111,117,110,100,95,108,95,49,52,0);
       let analyticsv = String.fromCharCode(104,95,54,52,95,115,101,109,97,110,116,105,99,0);
         playlistm = "1";
      for (let e = 0; e < 3; e++) {
         playlistm += `${(String.fromCharCode(68,0) == analyticsv ? playlistm.length : analyticsv.length)}`;
      }
      if (!splashR.endsWith(`${playlistm.length}`)) {
          let alert8 = String.fromCharCode(102,95,52,52,95,101,118,97,108,117,97,116,111,114,0);
          let typingB = String.fromCharCode(98,95,52,54,95,99,97,108,101,110,100,97,114,0);
          let emoji8 = 2;
          let sans_ = 2;
         splashR += `${sans_}`;
         alert8 += `${typingB.length}`;
         typingB += `${alert8.length << (Math.min(Math.abs(3), 1))}`;
         emoji8 ^= (String.fromCharCode(86,0) == alert8 ? alert8.length : typingB.length);
         sans_ -= typingB.length;
      }
      for (let t = 0; t < 1; t++) {
         analyticsv = `${(playlistm == String.fromCharCode(104,0) ? analyticsv.length : playlistm.length)}`;
      }
      while (analyticsv.length >= 5) {
         analyticsv += `${analyticsv.length & 2}`;
         break;
      }
      for (let v = 0; v < 2; v++) {
         splashR = `${playlistm.length}`;
      }
      let termsd = playlistm == String.fromCharCode(111,100,101,109,103,109,54,54,0);
      do {
         playlistm += `${playlistm.length}`;
         if (termsd) {
            break;
         }
      } while (termsd && (analyticsv != String.fromCharCode(81,0)));
      while (playlistm == splashR) {
         splashR = `${(analyticsv == String.fromCharCode(74,0) ? analyticsv.length : splashR.length)}`;
         break;
      }
          let qcopy_pa = String.fromCharCode(117,95,49,50,95,108,111,103,111,115,0);
          let matchY: Array<any> = [356, 831];
          let robotoR: Map<any, any> = new Map([[String.fromCharCode(115,101,113,110,111,95,107,95,57,56,0),38], [String.fromCharCode(115,95,52,55,95,108,105,102,101,99,121,99,108,101,0),607]]);
         playlistm += `${(splashR == String.fromCharCode(105,0) ? splashR.length : qcopy_pa.length)}`;
         qcopy_pa += `${robotoR.size ^ matchY.length}`;
         matchY = [matchY.length * robotoR.size];
      pauseD = `${parseInt(`${logo8}`)}`;
   if (logo8 < 5.34) {
      tickD.set(`${areaR}`, areaR);
   }
      indicatorb -= parseFloat(`${calendarV.length}`);
   while (!predictionb.endsWith(`${user6}`)) {
       let starV = String.fromCharCode(115,105,103,110,97,116,117,114,101,95,57,95,56,51,0);
       let privacyu = 3.0;
       let nalytics6 = 0.0;
         privacyu -= parseFloat(`${starV.length}`);
       let benefitx = 5;
         nalytics6 *= 1 + parseInt(`${privacyu}`);
          let blacklistF = 2.0;
          let modeF = 2.0;
         nalytics6 += parseInt(`${privacyu}`);
         blacklistF -= parseFloat(`${parseInt(`${modeF}`)}`);
      if (parseFloat(`${starV.length}`) <= privacyu) {
          let tickO = String.fromCharCode(101,95,50,95,98,105,116,115,112,117,108,115,101,115,0);
         starV = `${starV.length}`;
         tickO = `${(String.fromCharCode(95,0) == tickO ? tickO.length : tickO.length)}`;
      }
       let relatedC = false;
       let x_titlef = true;
         starV += `${(starV == String.fromCharCode(98,0) ? parseInt(`${privacyu}`) : starV.length)}`;
      if ((benefitx / (Math.max(nalytics6, 6))) < 1.54 && (nalytics6 / 1.54) < 3.18) {
         benefitx |= 2;
      }
      if (!x_titlef && relatedC) {
         x_titlef = 99.22 <= nalytics6;
      }
      predictionb = `${3 - parseInt(`${nalytics6}`)}`;
      break;
   }
   for (let t = 0; t < 2; t++) {
      pauseD += `${pauseD.length / (Math.max(2, 5))}`;
   }

    Clipboard.setString(userState.user?.userReferralCode ?? '');

   for (let h = 0; h < 2; h++) {
      switch_2m += `${(ballS == String.fromCharCode(72,0) ? tickD.size : ballS.length)}`;
   }
   for (let c = 0; c < 3; c++) {
      ballS += `${windD.length}`;
   }
   while (4 < (parseInt(`${indicatorb}`) * calendarV.length)) {
      calendarV.push((1 + (user6 ? 5 : 2)));
      break;
   }
   while (!switch_2m.endsWith(`${calendarV.length}`)) {
      calendarV = [parseInt(`${heartw}`)];
      break;
   }
       let adults = 0;
       let update_7ih = 2.0;
       let signinupi = 4;
         update_7ih *= 3;
         adults |= parseInt(`${update_7ih}`);
       let checkboxq: Array<any> = [76, 802];
       let tumbnailk: Array<any> = [820, 254];
      if ((update_7ih - adults) == 5.90 || (3 % (Math.max(2, adults))) == 4) {
         update_7ih *= checkboxq.length;
      }
      while (4 > (5 * signinupi)) {
          let viewer_ = String.fromCharCode(98,95,55,48,95,105,110,105,116,105,97,108,0);
         signinupi >>= Math.min(4, viewer_.length);
         break;
      }
          let area6 = String.fromCharCode(117,112,100,97,116,105,110,103,95,101,95,52,56,0);
          let videoT = String.fromCharCode(98,97,103,101,95,113,95,52,48,0);
          let r_centerx = String.fromCharCode(113,95,52,95,99,112,117,117,115,101,100,0);
         checkboxq = [parseInt(`${update_7ih}`) >> (Math.min(checkboxq.length, 4))];
         area6 += `${(String.fromCharCode(81,0) == videoT ? videoT.length : r_centerx.length)}`;
         r_centerx = `${videoT.length}`;
       let select1 = String.fromCharCode(118,95,57,55,0);
       let policyc = String.fromCharCode(99,95,56,53,95,105,110,115,116,97,110,116,105,97,116,105,111,110,0);
          let drags: Map<any, any> = new Map([[String.fromCharCode(115,101,116,116,101,114,95,49,95,49,51,0),String.fromCharCode(120,100,99,97,109,95,119,95,55,50,0)], [String.fromCharCode(109,95,54,48,95,112,114,101,115,101,108,101,99,116,0),String.fromCharCode(113,117,111,116,105,101,110,116,95,100,95,51,53,0)]]);
          let controlB = 5.0;
         checkboxq.push(3);
         drags.set(`${controlB}`, parseInt(`${controlB}`));
      if (1 == signinupi) {
         checkboxq.push(tumbnailk.length + checkboxq.length);
      }
      predictionb += `${areaR + 1}`;
       let humidity0 = String.fromCharCode(109,95,49,48,48,95,118,108,98,117,102,0);
       let analyticsU = String.fromCharCode(104,95,53,55,95,112,114,101,116,116,121,0);
       let tempO = 0.0;
      if (1.0 >= (tempO + humidity0.length)) {
         tempO /= Math.max(5, humidity0.length);
      }
         humidity0 = `${(analyticsU == String.fromCharCode(90,0) ? analyticsU.length : parseInt(`${tempO}`))}`;
         humidity0 = `${(analyticsU == String.fromCharCode(116,0) ? parseInt(`${tempO}`) : analyticsU.length)}`;
       let anythinkK = 2.0;
          let delegate_g98 = 0;
          let icon0 = true;
         humidity0 = `${humidity0.length * parseInt(`${tempO}`)}`;
         delegate_g98 >>= Math.min(5, Math.abs(delegate_g98));
         icon0 = !icon0;
       let spinnerN = true;
      for (let i = 0; i < 2; i++) {
         humidity0 += "3";
      }
      while ((5.85 / (Math.max(5, tempO))) == 3.29 && 5.85 == tempO) {
          let moviesg = 4.0;
          let sans8 = String.fromCharCode(113,95,57,52,95,98,111,116,104,0);
         tempO /= Math.max(1 - parseInt(`${tempO}`), 4);
         moviesg *= parseInt(`${moviesg}`) >> (Math.min(Math.abs(2), 4));
         sans8 = `${sans8.length}`;
         break;
      }
      while (4 < analyticsU.length) {
         analyticsU += `${2 / (Math.max(2, analyticsU.length))}`;
         break;
      }
      ballS += `${ballS.length | 2}`;
      predictionb += `${predictionb.length}`;
   while (windD == switch_2m) {
       let weibog = true;
       let successP = String.fromCharCode(105,95,52,49,95,117,110,99,111,114,114,0);
       let entryc = String.fromCharCode(115,105,109,117,108,116,97,110,101,111,117,115,95,99,95,56,48,0);
      for (let l = 0; l < 1; l++) {
         entryc = `${((weibog ? 5 : 4))}`;
      }
          let assist2 = String.fromCharCode(104,95,52,57,95,116,97,112,101,115,0);
          let playM = false;
          let detail0 = String.fromCharCode(97,101,97,100,95,50,95,52,54,0);
         weibog = ((assist2.length + (playM ? 68 : assist2.length)) >= 68);
         playM = (detail0.length << (Math.min(1, detail0.length))) < 86;
         successP = `${entryc.length}`;
         weibog = successP.length >= 69;
       let const_4R = String.fromCharCode(103,95,51,50,95,97,100,100,0);
         const_4R += `${entryc.length / (Math.max(8, const_4R.length))}`;
          let sansQ = 3;
          let predictionT = true;
          let middley: Map<any, any> = new Map([[String.fromCharCode(105,110,116,101,103,114,97,116,105,111,110,95,110,95,50,55,0),String.fromCharCode(101,95,49,51,95,111,112,116,105,111,110,97,108,115,0)], [String.fromCharCode(109,95,55,48,95,97,112,112,101,97,114,105,110,103,0),String.fromCharCode(116,111,100,112,95,97,95,53,54,0)], [String.fromCharCode(102,95,49,48,48,95,108,97,110,103,105,100,0),String.fromCharCode(112,101,114,99,101,110,116,95,103,95,54,48,0)]]);
         const_4R += `${sansQ}`;
         sansQ ^= middley.size;
         predictionT = middley.size < 41 || predictionT;
         entryc = `${successP.length >> (Math.min(entryc.length, 2))}`;
         successP = `${entryc.length | 2}`;
      switch_2m += `${3 << (Math.min(4, Math.abs(parseInt(`${logo8}`))))}`;
      break;
   }
      user6 = frame_7x || photoW <= 27.0;
      skipa.set(ballS, (String.fromCharCode(82,0) == ballS ? pauseD.length : ballS.length));
      user6 = photoW <= 60.26;
   let downloadingw = user6 ? !user6 : user6;
   do {
       let found5 = String.fromCharCode(104,95,49,57,95,99,111,100,101,99,117,116,105,108,115,0);
         found5 += `${found5.length | 3}`;
      let tick4 = 5216687 <= found5.length;
      do {
         found5 = `${found5.length | 2}`;
         if (tick4) {
            break;
         }
      } while (tick4 && (!found5.endsWith(found5)));
      if (found5 != String.fromCharCode(108,0)) {
         found5 += `${found5.length}`;
      }
      user6 = (28 < ((!user6 ? switch_2m.length : 28) - switch_2m.length));
      if (downloadingw) {
         break;
      }
   } while ((3.17 < photoW) && downloadingw);
      user6 = frame_7x;
      frame_7x = predictionb.length <= 71;
       let philippinesG = String.fromCharCode(99,95,57,57,95,109,105,120,101,100,0);
       let analyticY = true;
         philippinesG += `${2 << (Math.min(3, philippinesG.length))}`;
          let countryq = String.fromCharCode(105,110,116,114,101,112,111,108,95,56,95,49,56,0);
          let transferW: Array<any> = [87, 553, 711];
         analyticY = countryq.length >= transferW.length;
      for (let w = 0; w < 1; w++) {
         analyticY = (1 == (philippinesG.length ^ (!analyticY ? philippinesG.length : 1)));
      }
          let benefit0 = 2.0;
          let reminder1 = String.fromCharCode(112,95,57,51,95,98,105,116,120,0);
          let subsq = 2.0;
         philippinesG = `${(reminder1 == String.fromCharCode(108,0) ? reminder1.length : (analyticY ? 3 : 3))}`;
         benefit0 -= parseInt(`${subsq}`) & 3;
         subsq *= parseInt(`${benefit0}`);
         analyticY = !philippinesG.startsWith(`${analyticY}`);
          let sans2 = true;
          let mappingt = String.fromCharCode(97,95,49,53,95,97,112,112,101,110,100,99,104,97,114,0);
          let filled0 = 3.0;
         philippinesG = `${mappingt.length}`;
         sans2 = filled0 > 38.52;
         mappingt += `${2 | parseInt(`${filled0}`)}`;
      heartw /= Math.max(2, ((frame_7x ? 4 : 1)));
      switch_2m += `${switch_2m.length % (Math.max(1, 2))}`;
   while (frame_7x || 1.29 >= (indicatorb / 1.31)) {
      indicatorb += parseFloat(`${predictionb.length}`);
      break;
   }
      indicatorb *= parseFloat(`${skipa.size}`);
   for (let y = 0; y < 2; y++) {
      switch_2m += `${calendarV.length << (Math.min(windD.length, 3))}`;
   }
    setIsDialogOpen(true);
  }

  return (
    <ScreenContainer>
      <TitleWithBackButtonHeader title='个人中心' />
      <View
        style={{
          justifyContent: 'space-between',
          flex: 1,
          paddingHorizontal: 10,
        }}
      >
        <View style={{ marginTop: 30 }}>

          <TouchableOpacity
            style={styles.pressableTextContainer}
            onPress={() => {
              SMBing.showToast('目前暂不支持更改头像，敬请期待');
            }}
          >
            <Text style={{ fontSize: 16, color: '#9C9C9C' }}>头像</Text>
            <FastImage
              style={{
                height: 34,
                width: 34,
              }}
              source={require('@static/images/actionSaveBackward.png')}
            />
          </TouchableOpacity>


          { }
          <TouchableOpacity
            style={styles.pressableTextContainer}
            onPress={onPressUsername}
          >
            <Text style={{ fontSize: 16, color: colors.text }}>
              {username}
            </Text>
          </TouchableOpacity>

          { }
          <View style={styles.pressableTextContainer}>
            <Text style={{ fontSize: 16, color: '#9C9C9C' }}>
              {userState.user?.userEmail !== '' ? userState.user?.userEmail : userState.user.userPhoneNumber}
            </Text>
          </View>

          { }
          {userState.user?.userReferrerName == '' &&
            userState.user.userAllowUpdateReferral == true &&
            <TouchableOpacity
              style={styles.pressableTextContainer}
              onPress={onPressReferrer}
            >
              <Text style={{ fontSize: 16, color: '#B6B6B6' }}>
                补填邀请码 (只能填写一次)
              </Text>
            </TouchableOpacity>
          }

          {userState.user?.userReferrerName != '' && (
            <View style={styles.pressableTextContainer}>
              <Text style={{ fontSize: 16, color: '#9C9C9C' }}>推介人</Text>

              <View
                style={{
                  flexDirection: 'row',
                  justifyContent: 'center',
                  alignItems: 'center',
                }}
              >
                <Text style={{ fontSize: 14, color: '#9C9C9C' }}>
                  {userState.user?.userReferrerName}
                </Text>
              </View>
            </View>
          )}

          { }
          <TouchableOpacity
            style={styles.pressableTextContainer}
            onPress={onCopyReferralCode}
          >
            <Text style={{ fontSize: 16, color: colors.primary }}>
              {userState.user?.userReferralCode}
            </Text>

            <View
              style={{
                flexDirection: 'row',
                justifyContent: 'center',
                alignItems: 'center',
              }}
            >
              <Text style={{ fontSize: 14, color: '#9C9C9C' }}>
                复制邀请码
              </Text>
              <Image
                style={{
                  height: 27,
                  width: 27,
                  position: 'relative',
                  top: 2,
                }}
                source={require('@static/images/profile/stepAgreement.png')}
              />
            </View>
          </TouchableOpacity>

          <NotificationModal
            onConfirm={toggleOverlay}
            isVisible={isDialogOpen}
            title='复制成功'
            subtitle1=''
            subtitle2=''
            subtitle3=''
          />
        </View>

        <ChangeUsernameModal
          isVisible={isShowUsernameModal}
          handleClose={() => {
            setShowUsernameModal(false);
          }}
          initialUsername={username}
          onSubmitSuccess={onUsernameUpdateSuccess}
        />

        <ChangeReferrerModal
          isVisible={isShowReferrerModal}
          handleClose={() => {
            setShowReferrerModal(false);
          }}
        />
      </View>
    </ScreenContainer>
  );
};

const createStyles = ({ dark }: any) => StyleSheet.create({
  pressableTextContainer: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    backgroundColor: dark ? "#1d2023" : '#D9D9D9',
    marginTop: 20,
    paddingLeft: 18,
    paddingRight: 13,
    height: 48,
    borderRadius: 8,
    marginBottom: 20,
  },
});
