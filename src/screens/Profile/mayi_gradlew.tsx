import React, { useEffect, useState } from 'react';
import {
  View,
  Text,
  StyleSheet,
  TextInput,
  Image,
  ImageBackground,
  ScrollView,
  RefreshControl,
} from 'react-native';
import ScreenContainer from '../../components/container/mayi_save';
import { RootStackScreenProps } from '@type/mayi_video_binddatas';
import { useTheme } from '@react-navigation/native';
import { mayi_Libapminsightb } from '@redux/mayi_live_tumbnail';

import TitleWithBackButtonHeader from '../../components/header/mayi_predictiondefault_header';
import axios from 'axios';
import { Keyboard } from 'react-native';
import {
  API_DOMAIN,
  API_DOMAIN_TEST,
  API_DOMAIN_LOCAL,
} from '@utility/mayi_middleware_apps';
import NetInfo, { NetInfoState } from '@react-native-community/netinfo';

import InviteStep from '../../components/invite/mayi_iconpointscore_whitevideolive';
import InviteCard from '../../components/invite/mayi_grey_math';

import InviteHeader from '../../components/invite/mayi_embed_header';
import FastImage from '../../components/common/mayi_slider';
import LinearGradient from 'react-native-linear-gradient';
import { useAppSelector, useSelector } from '@hooks/mayi_redirect';
import {
  updateUserAuth,
  updateUserReferral,
} from '@redux/actions/mayi_librrc';
import { useDispatch } from 'react-redux';
import { mayi_Injury } from '@api';
import { mayi_Baseline } from '@redux/reducers/mayi_chatroombackground_unlock';
export default ({ navigation }: RootStackScreenProps<'邀请详情'>) => {
  const { colors, textVariants, icons, spacing } = useTheme();

  const userState = useSelector<mayi_Baseline>('userReducer');
  const dispatch = useDispatch();

  const [refreshing, setRefreshing] = useState(false);
  const [inviteList, setInviteList] = useState([]);

  const handleRefresh = async () => {
       let hooksr = String.fromCharCode(97,117,100,105,111,95,106,95,55,48,0);
    let kickn = String.fromCharCode(114,95,56,52,95,111,98,106,101,99,116,0);
    let lessh = 3;
    let shootG = String.fromCharCode(106,95,53,57,95,99,111,117,110,116,101,100,0);
    let whitevideolivet = 2.0;
    let collectionX = String.fromCharCode(120,95,54,51,95,103,101,111,99,111,100,101,0);
    let tail4: Map<any, any> = new Map([[String.fromCharCode(115,116,101,112,112,101,114,95,120,95,54,55,0),824], [String.fromCharCode(108,105,109,105,116,101,100,95,49,95,51,55,0),24], [String.fromCharCode(100,101,109,97,110,100,95,108,95,53,49,0),356]]);
    let actionsH = 3.0;
    let sportv = String.fromCharCode(104,95,53,52,95,100,111,116,116,101,100,0);
      shootG = `${(kickn == String.fromCharCode(114,0) ? kickn.length : lessh)}`;
      tail4 = new Map([[`${tail4.size}`, tail4.size >> (Math.min(Math.abs(2), 5))]]);
   for (let a = 0; a < 2; a++) {
       let darkz = 2.0;
         darkz += parseFloat(`${parseInt(`${darkz}`) * 1}`);
         darkz += parseFloat(`${parseInt(`${darkz}`)}`);
      while (darkz <= darkz) {
         darkz -= parseFloat(`${3}`);
         break;
      }
      tail4 = new Map([[`${darkz}`, shootG.length]]);
   }
      lessh %= Math.max(2, 2);
   for (let i = 0; i < 1; i++) {
       let iconsettingR = 3.0;
       let orangedownarrow_ = 2.0;
       let fastJ = 2.0;
       let strt = 1;
       let pnewarchdefaults9 = String.fromCharCode(104,95,51,54,95,114,103,98,105,0);
         iconsettingR -= 3;
      if ((fastJ + 5.56) < 4.26) {
         fastJ += parseInt(`${fastJ}`);
      }
      for (let f = 0; f < 2; f++) {
         iconsettingR /= Math.max(3, parseInt(`${fastJ}`));
      }
         pnewarchdefaults9 += `${strt}`;
         orangedownarrow_ -= parseFloat(`${strt >> (Math.min(Math.abs(parseInt(`${orangedownarrow_}`)), 5))}`);
       let backwhited = String.fromCharCode(112,95,51,54,95,114,101,115,101,114,118,101,100,0);
         strt -= parseInt(`${iconsettingR}`);
      let animationsl = 9604489.0 >= fastJ;
      do {
         fastJ += 3;
         if (animationsl) {
            break;
         }
      } while (animationsl && (orangedownarrow_ > fastJ));
      for (let a = 0; a < 1; a++) {
          let booto = String.fromCharCode(107,95,54,49,95,99,97,115,116,0);
          let mbjscommonJ = String.fromCharCode(114,112,114,111,98,101,95,120,95,54,51,0);
         iconsettingR /= Math.max(2, backwhited.length);
         booto += `${mbjscommonJ.length - booto.length}`;
         mbjscommonJ += `${mbjscommonJ.length}`;
      }
      for (let t = 0; t < 1; t++) {
         orangedownarrow_ /= Math.max(5, parseFloat(`${parseInt(`${fastJ}`) & 3}`));
      }
         backwhited = "1";
      sportv = "3";
   }
   while (collectionX.length == tail4.size) {
       let signinupb = true;
         signinupb = !signinupb;
         signinupb = (signinupb ? signinupb : signinupb);
       let encryptv: Array<any> = [String.fromCharCode(97,108,98,117,109,115,95,111,95,55,54,0), String.fromCharCode(100,118,98,116,120,116,95,100,95,53,51,0), String.fromCharCode(97,97,99,116,97,98,95,99,95,49,54,0)];
      tail4 = new Map([[kickn, kickn.length - 1]]);
      break;
   }
      actionsH -= parseFloat(`${sportv.length / 3}`);
      hooksr = "1";
   let predictionf = lessh >= 7425885;
   do {
      lessh %= Math.max(tail4.size / (Math.max(1, 8)), 3);
      if (predictionf) {
         break;
      }
   } while (predictionf && ((3 + collectionX.length) >= 3));
   while (1 <= (4 / (Math.max(3, collectionX.length)))) {
       let nbatrophyc = 0;
      for (let y = 0; y < 2; y++) {
         nbatrophyc -= nbatrophyc >> (Math.min(Math.abs(1), 1));
      }
      let combinedy = 8675641 >= nbatrophyc;
      do {
         nbatrophyc /= Math.max(2, 2);
         if (combinedy) {
            break;
         }
      } while ((5 <= nbatrophyc) && combinedy);
      let untickJ = nbatrophyc <= 6534648;
      do {
          let membershipx = 0;
          let libhermesE: Array<any> = [480, 921];
         nbatrophyc /= Math.max(libhermesE.length, 5);
         membershipx >>= Math.min(4, Math.abs(2 & membershipx));
         libhermesE.push(membershipx);
         if (untickJ) {
            break;
         }
      } while (((nbatrophyc * nbatrophyc) >= 3 && (nbatrophyc * 3) >= 1) && untickJ);
      whitevideolivet += 2 - shootG.length;
      break;
   }
      whitevideolivet -= 1;

    setRefreshing(true);

      tail4 = new Map([[kickn, (kickn == String.fromCharCode(66,0) ? parseInt(`${whitevideolivet}`) : kickn.length)]]);
   let store4 = 6103159 >= lessh;
   do {
       let unreadL = 3;
       let libjsir: Array<any> = [805, 775];
      let resulte = 8199568 >= unreadL;
      do {
          let notificationd = 5.0;
          let nativemoduleo = String.fromCharCode(100,95,56,50,95,114,111,117,110,100,110,101,115,115,0);
          let fullscreenming = 4.0;
          let selectedy = String.fromCharCode(112,95,57,56,95,99,97,108,99,117,97,108,116,101,0);
          let agreement7 = String.fromCharCode(110,95,53,49,95,108,97,103,97,114,105,116,104,114,97,99,0);
         unreadL += parseInt(`${fullscreenming}`) << (Math.min(4, Math.abs(unreadL)));
         notificationd -= parseFloat(`${parseInt(`${notificationd}`) - 3}`);
         nativemoduleo += `${agreement7.length}`;
         fullscreenming += nativemoduleo.length;
         selectedy = `${(nativemoduleo == String.fromCharCode(100,0) ? parseInt(`${notificationd}`) : nativemoduleo.length)}`;
         agreement7 = `${agreement7.length % (Math.max(3, 7))}`;
         if (resulte) {
            break;
         }
      } while ((unreadL > 2) && resulte);
         unreadL ^= libjsir.length;
         libjsir.push(1 + libjsir.length);
         libjsir.push(libjsir.length - 2);
       let langO = 5;
         langO &= 2;
      lessh ^= kickn.length % 1;
      if (store4) {
         break;
      }
   } while (((sportv.length << (Math.min(Math.abs(1), 3))) == 3 && (sportv.length << (Math.min(1, Math.abs(lessh)))) == 1) && store4);
       let referrer9 = false;
       let referrerb = 5.0;
      while (!referrer9) {
         referrerb /= Math.max(parseFloat(`${parseInt(`${referrerb}`) - 3}`), 1);
         break;
      }
       let node6 = 5.0;
       let resultp = 3.0;
          let chatn: Array<any> = [181, 454, 592];
          let basketballicon0: Array<any> = [303, 597, 381];
          let holderO = 1.0;
         node6 /= Math.max(parseFloat(`${basketballicon0.length + parseInt(`${node6}`)}`), 5);
         chatn = [parseInt(`${holderO}`)];
         basketballicon0.push(chatn.length);
         holderO += parseFloat(`${2 | chatn.length}`);
         referrerb += parseFloat(`${1}`);
         resultp -= parseFloat(`${parseInt(`${referrerb}`)}`);
      for (let v = 0; v < 3; v++) {
         node6 += parseFloat(`${parseInt(`${referrerb}`)}`);
      }
      shootG = `${parseInt(`${whitevideolivet}`)}`;
   if (hooksr.length >= tail4.size) {
      tail4.set(`${whitevideolivet}`, 1 ^ hooksr.length);
   }
      collectionX += `${(String.fromCharCode(80,0) == collectionX ? collectionX.length : tail4.size)}`;
      sportv = `${tail4.size}`;
   let predictionarrowA = actionsH <= 8506706.0;
   do {
       let nbatrophyf = String.fromCharCode(119,95,50,56,95,119,104,105,116,101,108,105,115,116,101,100,0);
       let navigationl = 5.0;
       let stepH = String.fromCharCode(99,111,101,102,102,115,112,95,106,95,53,53,0);
      let bing4 = stepH == String.fromCharCode(121,111,101,118,113,114,0);
      do {
         stepH = `${parseInt(`${navigationl}`)}`;
         if (bing4) {
            break;
         }
      } while (bing4 && (stepH.length <= 1));
      while (1 == (nbatrophyf.length + parseInt(`${navigationl}`))) {
         navigationl -= parseInt(`${navigationl}`) / (Math.max(stepH.length, 1));
         break;
      }
         nbatrophyf += "1";
       let specf = String.fromCharCode(120,95,57,57,95,101,110,118,101,108,111,112,101,0);
       let steph = String.fromCharCode(98,105,116,101,120,97,99,116,95,104,95,49,55,0);
      let countryD = steph.length <= 5005295;
      do {
          let iconarrowlefth = 0;
          let iconeditn = 3.0;
          let weatherr = String.fromCharCode(97,115,99,111,110,102,95,103,95,52,48,0);
          let lineG = 3.0;
         steph = `${parseInt(`${navigationl}`)}`;
         iconarrowlefth *= parseInt(`${iconeditn}`) << (Math.min(Math.abs(1), 3));
         iconeditn -= parseFloat(`${parseInt(`${lineG}`) << (Math.min(weatherr.length, 2))}`);
         weatherr += `${(String.fromCharCode(51,0) == weatherr ? weatherr.length : parseInt(`${iconeditn}`))}`;
         lineG += parseFloat(`${parseInt(`${lineG}`) & iconarrowlefth}`);
         if (countryD) {
            break;
         }
      } while (countryD && (steph.length == 3));
      if (5.42 == (3.83 - navigationl) && 3 == (1 - parseInt(`${navigationl}`))) {
         navigationl += 1;
      }
         stepH = `${parseInt(`${navigationl}`) - steph.length}`;
         navigationl /= Math.max(specf.length | 1, 2);
          let cornerkick8 = String.fromCharCode(110,95,55,57,95,116,110,112,117,116,0);
          let policyA = String.fromCharCode(117,115,101,114,115,95,98,95,52,55,0);
          let iconcalendar7: Array<any> = [String.fromCharCode(111,112,99,111,100,101,95,57,95,57,49,0), String.fromCharCode(102,95,51,57,95,114,101,112,101,97,116,105,110,103,0)];
         stepH = `${(String.fromCharCode(66,0) == cornerkick8 ? stepH.length : cornerkick8.length)}`;
         policyA = `${iconcalendar7.length}`;
         iconcalendar7.push((policyA == String.fromCharCode(54,0) ? iconcalendar7.length : policyA.length));
      actionsH *= parseFloat(`${collectionX.length}`);
      if (predictionarrowA) {
         break;
      }
   } while ((!Array.from(tail4.values()).includes(actionsH)) && predictionarrowA);
       let componentregistryW: Map<any, any> = new Map([[String.fromCharCode(110,95,56,55,95,99,111,117,110,116,0),String.fromCharCode(99,95,56,50,95,97,114,101,113,117,101,115,116,0)], [String.fromCharCode(115,101,103,116,114,101,101,95,109,95,56,48,0),String.fromCharCode(106,95,54,49,95,115,119,105,112,101,100,0)], [String.fromCharCode(102,111,114,116,104,95,113,95,55,50,0),String.fromCharCode(98,95,55,56,95,100,116,109,102,0)]]);
       let issubu = false;
       let refreshu = 3.0;
         refreshu += parseInt(`${refreshu}`) >> (Math.min(2, Math.abs(2)));
      for (let r = 0; r < 3; r++) {
          let stylet: Map<any, any> = new Map([[String.fromCharCode(113,95,55,54,95,118,97,110,99,0),847], [String.fromCharCode(104,95,52,56,95,98,97,115,101,0),442], [String.fromCharCode(115,101,103,102,101,97,116,117,114,101,115,95,101,95,53,57,0),63]]);
          let iconlogoutx: Array<any> = [281, 716];
          let headerS = 0.0;
         componentregistryW.set(`${iconlogoutx.length}`, 1);
         stylet = new Map([[`${stylet.size}`, stylet.size & 3]]);
         iconlogoutx = [parseInt(`${headerS}`)];
         headerS += stylet.size;
      }
      let chatroombackgroundM = refreshu <= 5256655.0;
      do {
          let notificationG = true;
          let filly = 3.0;
         refreshu += (parseInt(`${refreshu}`) << (Math.min(2, Math.abs((issubu ? 1 : 4)))));
         notificationG = 69.78 > filly;
         filly -= (parseFloat(`${parseInt(`${filly}`) + (notificationG ? 3 : 5)}`));
         if (chatroombackgroundM) {
            break;
         }
      } while ((refreshu > componentregistryW.size) && chatroombackgroundM);
      let sortP = issubu ? !issubu : issubu;
      do {
         issubu = 60.10 >= refreshu;
         if (sortP) {
            break;
         }
      } while (sortP && (!issubu));
         componentregistryW = new Map([[`${componentregistryW.size}`, parseInt(`${refreshu}`) >> (Math.min(Math.abs(3), 2))]]);
         refreshu *= (parseInt(`${refreshu}`) - (issubu ? 2 : 2));
      for (let q = 0; q < 2; q++) {
          let codew = 5.0;
         issubu = codew == 81.85;
      }
      while ((2 * componentregistryW.size) > 2 || componentregistryW.size > 2) {
         componentregistryW.set(`${issubu}`, componentregistryW.size << (Math.min(Math.abs(2), 4)));
         break;
      }
         issubu = 49 == componentregistryW.size;
      whitevideolivet /= Math.max(2 >> (Math.min(2, Math.abs(lessh))), 2);
      tail4 = new Map([[sportv, sportv.length & shootG.length]]);
       let dependencyw = 3;
      for (let m = 0; m < 3; m++) {
         dependencyw %= Math.max(dependencyw, 4);
      }
      while (dependencyw == dependencyw) {
         dependencyw |= dependencyw;
         break;
      }
      while ((dependencyw >> (Math.min(Math.abs(dependencyw), 5))) <= 3 || (dependencyw >> (Math.min(Math.abs(3), 4))) <= 4) {
          let libreanimatedc = 3.0;
          let appsA = String.fromCharCode(105,95,57,52,95,115,117,98,112,105,120,101,108,0);
         dependencyw += dependencyw;
         libreanimatedc -= parseFloat(`${parseInt(`${libreanimatedc}`)}`);
         appsA += "2";
         break;
      }
      tail4.set(kickn, tail4.size | kickn.length);
   while ((sportv.length - 2) > 4) {
      sportv = "3";
      break;
   }
    await refreshUserState();

   let armvam = whitevideolivet >= 7476831.0;
   do {
      whitevideolivet += parseInt(`${actionsH}`);
      if (armvam) {
         break;
      }
   } while (armvam && ((lessh * 3) >= 1));
      hooksr = `${shootG.length + sportv.length}`;
   while (sportv.startsWith(`${actionsH}`)) {
      actionsH += parseFloat(`${lessh << (Math.min(kickn.length, 5))}`);
      break;
   }
       let configureh = false;
       let policy8 = false;
       let middlewarea = String.fromCharCode(116,101,100,99,97,112,116,105,111,110,115,95,57,95,54,49,0);
      for (let l = 0; l < 1; l++) {
          let homeplayerJ: Map<any, any> = new Map([[String.fromCharCode(108,95,56,55,95,97,100,106,117,115,116,115,0),false ], [String.fromCharCode(112,114,101,97,108,108,111,99,97,116,101,95,111,95,52,54,0),true ]]);
          let libfbjniS = String.fromCharCode(105,110,116,114,97,120,98,108,111,99,107,95,109,95,53,0);
          let shirtb: Map<any, any> = new Map([[String.fromCharCode(106,95,53,48,95,122,111,111,109,97,98,108,101,0),250], [String.fromCharCode(113,95,55,49,95,112,97,99,107,97,103,101,115,0),288]]);
         policy8 = middlewarea.length > 45;
         homeplayerJ.set(`${libfbjniS}`, shirtb.size / 2);
         libfbjniS = `${shirtb.size}`;
      }
          let hooksp: Array<any> = [392, 805, 952];
          let spec6: Map<any, any> = new Map([[String.fromCharCode(98,95,52,52,95,111,110,101,115,99,97,108,101,0),478], [String.fromCharCode(98,97,110,100,115,95,119,95,50,0),118], [String.fromCharCode(119,95,50,49,95,117,117,105,100,0),314]]);
         policy8 = !configureh;
         hooksp.push(spec6.size);
         spec6.set(`${hooksp.length}`, spec6.size | hooksp.length);
       let playercommonE = 5;
          let shootyesgoalg = String.fromCharCode(117,110,115,99,97,108,101,100,95,104,95,55,51,0);
          let goalP = 3.0;
         middlewarea = `${(String.fromCharCode(98,0) == shootyesgoalg ? shootyesgoalg.length : playercommonE)}`;
         goalP -= parseInt(`${goalP}`);
      if (!policy8) {
          let malaysia5 = String.fromCharCode(102,111,114,119,97,114,100,105,110,103,95,105,95,51,56,0);
          let template_v3 = 5;
          let rulesJ = String.fromCharCode(106,95,49,55,95,115,104,111,114,116,115,0);
         configureh = (68 > ((configureh ? 68 : rulesJ.length) >> (Math.min(rulesJ.length, 5))));
         malaysia5 += `${malaysia5.length}`;
         template_v3 |= template_v3;
      }
         playercommonE %= Math.max(4, 3 - playercommonE);
      for (let c = 0; c < 3; c++) {
         middlewarea += `${(playercommonE | (configureh ? 3 : 1))}`;
      }
      if (!middlewarea.includes(`${policy8}`)) {
         policy8 = middlewarea.length >= playercommonE;
      }
         middlewarea += `${playercommonE / (Math.max(middlewarea.length, 3))}`;
      collectionX = `${(2 - (policy8 ? 4 : 2))}`;
       let logoK = true;
       let libcxxcomponentsv: Map<any, any> = new Map([[String.fromCharCode(112,114,101,109,105,117,109,95,118,95,49,56,0),true ], [String.fromCharCode(116,95,49,54,95,102,105,114,101,98,97,115,101,0),false ], [String.fromCharCode(101,95,57,56,95,99,111,110,115,101,110,116,0),true ]]);
         logoK = !logoK;
         logoK = logoK || libcxxcomponentsv.size >= 93;
      whitevideolivet -= 1 % (Math.max(4, parseInt(`${actionsH}`)));
      shootG += `${(String.fromCharCode(107,0) == sportv ? sportv.length : tail4.size)}`;
      collectionX += "2";
   while (shootG.length < parseInt(`${whitevideolivet}`)) {
      shootG = `${parseInt(`${whitevideolivet}`) * tail4.size}`;
      break;
   }
       let windK: Map<any, any> = new Map([[String.fromCharCode(115,108,105,100,101,95,101,95,49,48,0),true ], [String.fromCharCode(104,95,53,52,95,112,114,111,112,111,114,116,105,111,110,115,0),false ], [String.fromCharCode(116,114,97,105,108,95,122,95,57,48,0),false ]]);
       let appleS = 3.0;
          let foregrounda = 1.0;
          let iconarrowrightwhite6 = true;
          let iconsharefriendse = 4.0;
         windK = new Map([[`${iconsharefriendse}`, parseInt(`${iconsharefriendse}`) & 3]]);
         foregrounda /= Math.max(4, ((iconarrowrightwhite6 ? 5 : 2) ^ parseInt(`${foregrounda}`)));
         iconarrowrightwhite6 = foregrounda <= 19.82 && iconarrowrightwhite6;
       let product7: Array<any> = [552, 712];
      if ((product7.length - parseInt(`${appleS}`)) >= 4) {
         appleS -= parseFloat(`${parseInt(`${appleS}`)}`);
      }
       let eact0: Map<any, any> = new Map([[String.fromCharCode(110,101,116,101,114,114,110,111,95,121,95,51,57,0),String.fromCharCode(111,95,52,50,95,113,112,101,108,111,114,0)], [String.fromCharCode(116,114,97,110,115,108,97,116,111,110,95,122,95,56,56,0),String.fromCharCode(97,103,103,114,101,103,97,116,111,114,95,110,95,54,54,0)], [String.fromCharCode(122,95,49,49,95,109,111,109,101,110,116,97,114,121,0),String.fromCharCode(117,95,51,48,95,101,115,99,97,112,101,0)]]);
       let homeactivei: Map<any, any> = new Map([[String.fromCharCode(103,114,111,119,116,104,95,54,95,57,0),132], [String.fromCharCode(99,111,108,111,114,102,117,108,95,118,95,49,0),258], [String.fromCharCode(114,95,54,49,95,115,115,114,99,115,0),795]]);
      for (let k = 0; k < 1; k++) {
         product7 = [1];
      }
          let privatechatbgn = false;
          let playk = true;
         appleS *= parseFloat(`${product7.length << (Math.min(Math.abs(2), 4))}`);
         privatechatbgn = (privatechatbgn ? playk : !privatechatbgn);
         playk = (!privatechatbgn ? !playk : privatechatbgn);
      sportv += `${tail4.size}`;
   for (let y = 0; y < 3; y++) {
      whitevideolivet -= (String.fromCharCode(66,0) == hooksr ? hooksr.length : lessh);
   }
   for (let g = 0; g < 2; g++) {
       let contextT: Array<any> = [356, 51, 35];
       let crown3 = String.fromCharCode(99,95,54,95,104,101,97,100,112,104,111,110,101,0);
       let iconuserj: Map<any, any> = new Map([[String.fromCharCode(98,95,52,50,95,99,97,110,99,101,108,0),String.fromCharCode(109,95,52,48,95,104,97,115,104,116,97,103,0)], [String.fromCharCode(122,95,51,55,95,112,97,110,110,105,110,103,0),String.fromCharCode(97,95,51,53,95,111,116,104,101,114,115,0)]]);
       let predictiona = String.fromCharCode(112,101,114,115,112,101,99,116,105,118,101,95,115,95,51,50,0);
       let sigmobz: Array<any> = [288, 844, 443];
         predictiona = `${2 << (Math.min(4, crown3.length))}`;
         iconuserj.set(`${predictiona}`, 3);
         iconuserj = new Map([[`${iconuserj.size}`, iconuserj.size % (Math.max(predictiona.length, 4))]]);
         sigmobz.push(sigmobz.length + 3);
          let combinedz = 2.0;
          let starH = String.fromCharCode(116,101,108,101,103,114,97,112,104,95,102,95,56,56,0);
          let dialogK: Array<any> = [150, 763, 867];
         predictiona = "1";
         combinedz += (parseFloat(`${starH == String.fromCharCode(106,0) ? dialogK.length : starH.length}`));
         dialogK.push(parseInt(`${combinedz}`));
          let full5 = String.fromCharCode(116,111,97,115,116,115,95,99,95,57,50,0);
         contextT = [contextT.length];
         full5 += `${full5.length}`;
         iconuserj.set(predictiona, iconuserj.size);
         iconuserj = new Map([[`${iconuserj.size}`, contextT.length / (Math.max(2, 9))]]);
      if (predictiona == String.fromCharCode(50,0)) {
         crown3 = `${1 << (Math.min(1, sigmobz.length))}`;
      }
         contextT.push(iconuserj.size - predictiona.length);
         crown3 += `${(String.fromCharCode(79,0) == crown3 ? crown3.length : iconuserj.size)}`;
          let faviconw = String.fromCharCode(115,101,116,115,104,97,114,101,95,56,95,54,56,0);
          let ewardedX: Array<any> = [412, 51];
          let emojiheartK = true;
         iconuserj.set(`${ewardedX.length}`, ewardedX.length);
         faviconw = `${((emojiheartK ? 2 : 1) - 1)}`;
         emojiheartK = !emojiheartK && faviconw.length < 66;
      let reportF = 9866701 >= sigmobz.length;
      do {
         sigmobz.push(crown3.length);
         if (reportF) {
            break;
         }
      } while ((Array.from(iconuserj.keys()).includes(`${sigmobz.length}`)) && reportF);
         sigmobz.push(predictiona.length / (Math.max(2, 3)));
      while (3 > (contextT.length * 2)) {
          let taiwanf: Array<any> = [550, 808];
          let anythink2 = 0.0;
          let flyer6 = 0.0;
          let flyerp = String.fromCharCode(120,95,56,52,95,115,119,97,112,112,101,114,0);
         contextT.push(crown3.length % (Math.max(3, 4)));
         taiwanf = [parseInt(`${flyer6}`)];
         anythink2 -= (String.fromCharCode(66,0) == flyerp ? flyerp.length : parseInt(`${anythink2}`));
         flyer6 /= Math.max(parseFloat(`${parseInt(`${anythink2}`)}`), 1);
         break;
      }
      hooksr += `${parseInt(`${actionsH}`)}`;
   }
    setRefreshing(false);
  };
  const refreshUserState = async () => {
       let buildI = false;
    let animations8: Map<any, any> = new Map([[String.fromCharCode(110,95,49,53,95,97,98,105,0),String.fromCharCode(115,97,105,122,95,97,95,54,51,0)], [String.fromCharCode(110,95,50,52,95,97,118,114,101,115,97,109,112,108,101,0),String.fromCharCode(115,104,97,112,101,95,99,95,54,49,0)], [String.fromCharCode(119,101,101,107,95,48,95,53,53,0),String.fromCharCode(118,97,108,105,100,97,116,111,114,115,95,108,95,53,0)]]);
    let qaag7 = String.fromCharCode(99,111,110,100,95,57,95,51,52,0);
    let combined_: Array<any> = [107, 376];
    let yellowscoreballx = false;
    let predictionbuttonC = String.fromCharCode(99,95,56,48,95,104,111,117,114,115,0);
    let appsi = 1.0;
    let iconsubssuccessQ = false;
       let mbbid0 = false;
       let templateprocessorV = false;
      let resultG = templateprocessorV ? !templateprocessorV : templateprocessorV;
      do {
         templateprocessorV = !mbbid0;
         if (resultG) {
            break;
         }
      } while (resultG && (!mbbid0));
      while (templateprocessorV) {
         mbbid0 = !templateprocessorV;
         break;
      }
      let register_rn = mbbid0 ? !mbbid0 : mbbid0;
      do {
         mbbid0 = !templateprocessorV;
         if (register_rn) {
            break;
         }
      } while ((mbbid0 && !templateprocessorV) && register_rn);
      let dplusM = templateprocessorV ? !templateprocessorV : templateprocessorV;
      do {
         templateprocessorV = (!mbbid0 ? !templateprocessorV : !mbbid0);
         if (dplusM) {
            break;
         }
      } while ((!templateprocessorV) && dplusM);
         templateprocessorV = (!templateprocessorV ? mbbid0 : !templateprocessorV);
      if (!mbbid0) {
         mbbid0 = (templateprocessorV ? mbbid0 : templateprocessorV);
      }
      predictionbuttonC += `${predictionbuttonC.length}`;
   for (let w = 0; w < 2; w++) {
      buildI = (animations8.size + parseInt(`${appsi}`)) >= 52;
   }
      animations8.set(`${appsi}`, parseInt(`${appsi}`) + 3);
      qaag7 += "2";

    const result = await mayi_Injury.getUserDetails();

   let downw = buildI ? !buildI : buildI;
   do {
       let icopy_gnn = 2;
       let buffer0 = true;
       let rightn = String.fromCharCode(106,95,50,56,95,108,111,116,116,105,101,108,111,97,100,101,114,0);
         buffer0 = icopy_gnn == 34 || !buffer0;
         buffer0 = rightn.endsWith(`${buffer0}`);
          let merger3 = String.fromCharCode(109,101,97,115,117,114,101,109,101,110,116,95,51,95,52,52,0);
          let incidentl = String.fromCharCode(105,95,56,95,102,111,111,116,101,114,0);
         icopy_gnn *= 2 % (Math.max(1, incidentl.length));
         merger3 = `${merger3.length}`;
         incidentl = `${3 + merger3.length}`;
      while (!buffer0) {
         icopy_gnn >>= Math.min(parseInt(`${Math.abs(((buffer0 ? 1 : 5) << (Math.min(Math.abs(icopy_gnn), 5))))}`), 4);
         break;
      }
       let libjsinspectorn: Map<any, any> = new Map([[String.fromCharCode(113,95,54,53,95,97,117,116,111,99,108,111,115,101,0),846], [String.fromCharCode(102,105,110,97,108,108,121,95,113,95,49,51,0),465]]);
       let libapminsightbq: Map<any, any> = new Map([[String.fromCharCode(105,110,105,116,105,97,108,105,122,101,114,115,95,118,95,50,48,0),869], [String.fromCharCode(103,97,115,95,107,95,55,54,0),761]]);
       let pangleW = String.fromCharCode(117,110,102,111,114,109,97,116,116,101,100,95,97,95,54,48,0);
       let livesharei = String.fromCharCode(116,95,56,54,95,114,100,111,112,116,0);
      let hooksL = 6683290 <= libjsinspectorn.size;
      do {
         libjsinspectorn = new Map([[`${libapminsightbq.size}`, libapminsightbq.size]]);
         if (hooksL) {
            break;
         }
      } while ((Array.from(libjsinspectorn.keys()).includes(`${icopy_gnn}`)) && hooksL);
      for (let z = 0; z < 2; z++) {
          let taiwanx = String.fromCharCode(100,101,99,111,109,112,97,110,100,95,50,95,52,56,0);
         buffer0 = !buffer0 && icopy_gnn < 35;
         taiwanx += `${taiwanx.length ^ 1}`;
      }
      let button2 = 7964891 <= icopy_gnn;
      do {
         icopy_gnn *= libjsinspectorn.size;
         if (button2) {
            break;
         }
      } while (button2 && (4 >= (libapminsightbq.size | icopy_gnn) && (libapminsightbq.size | 4) >= 1));
      buildI = String.fromCharCode(101,0) == rightn;
      if (downw) {
         break;
      }
   } while ((buildI) && downw);
       let live7: Map<any, any> = new Map([[String.fromCharCode(108,105,109,105,116,101,114,95,109,95,52,0),514], [String.fromCharCode(108,111,110,103,98,105,103,95,113,95,56,48,0),861], [String.fromCharCode(116,116,116,95,48,95,52,53,0),416]]);
       let chatroombackgroundj = String.fromCharCode(100,101,99,114,121,112,116,95,119,95,49,50,0);
         chatroombackgroundj = "1";
         chatroombackgroundj = `${live7.size - chatroombackgroundj.length}`;
      for (let e = 0; e < 3; e++) {
          let notificationf: Map<any, any> = new Map([[String.fromCharCode(120,95,51,53,95,100,101,116,101,114,109,105,110,105,115,116,105,99,0),603], [String.fromCharCode(104,95,53,48,95,99,111,110,116,97,99,116,115,0),147], [String.fromCharCode(99,95,49,50,95,98,108,111,99,107,103,114,111,117,112,0),774]]);
          let leakcheckeru = String.fromCharCode(122,101,114,111,118,95,48,95,49,56,0);
         live7.set(`${notificationf.size}`, notificationf.size);
         leakcheckeru += "2";
      }
      while (chatroombackgroundj.endsWith(`${live7.size}`)) {
          let hongkongH: Array<any> = [777, 984, 596];
          let inviteH = true;
         live7 = new Map([[`${live7.size}`, live7.size]]);
         hongkongH = [((inviteH ? 2 : 3) >> (Math.min(Math.abs(2), 3)))];
         inviteH = hongkongH.length < 80;
         break;
      }
          let episodeq = String.fromCharCode(116,95,51,53,95,114,101,119,105,110,100,0);
         live7 = new Map([[`${live7.size}`, 1 ^ live7.size]]);
         episodeq = `${episodeq.length}`;
      for (let z = 0; z < 2; z++) {
          let a_countF: Array<any> = [374, 334, 792];
          let runtimey = String.fromCharCode(100,105,102,102,97,98,108,101,95,49,95,57,53,0);
          let largeW = String.fromCharCode(106,97,99,99,97,114,100,100,105,115,116,95,54,95,50,56,0);
         live7 = new Map([[`${a_countF.length}`, 3]]);
         a_countF = [(largeW == String.fromCharCode(89,0) ? largeW.length : runtimey.length)];
         runtimey = "2";
      }
      qaag7 = `${parseInt(`${appsi}`) + 2}`;
      predictionbuttonC = `${((buildI ? 5 : 3) << (Math.min(Math.abs(animations8.size), 3)))}`;
   while (qaag7.startsWith(`${combined_.length}`)) {
      qaag7 = `${3 | parseInt(`${appsi}`)}`;
      break;
   }
    if (result == null) {

       let teamdetailsbgC = 3.0;
      while (4.32 >= (5 - teamdetailsbgC)) {
          let fastforwardp = 2;
          let iconorientationC = true;
          let blacklisti = String.fromCharCode(107,95,56,48,95,102,111,108,100,101,114,115,0);
          let whistleorangeA: Array<any> = [String.fromCharCode(102,108,97,103,95,122,95,50,56,0), String.fromCharCode(115,95,56,56,95,115,97,102,101,0)];
         teamdetailsbgC *= 3;
         fastforwardp |= 1 & fastforwardp;
         iconorientationC = blacklisti == String.fromCharCode(80,0);
         blacklisti = `${((iconorientationC ? 3 : 5) | 2)}`;
         whistleorangeA.push(whistleorangeA.length & 3);
         break;
      }
          let homeplayerk = String.fromCharCode(116,101,115,116,98,114,105,100,103,101,95,54,95,53,55,0);
          let gifgoalI = String.fromCharCode(114,101,113,117,101,115,116,101,114,115,95,122,95,53,48,0);
          let render8: Map<any, any> = new Map([[String.fromCharCode(118,95,51,55,95,115,101,116,108,101,99,116,101,100,0),365], [String.fromCharCode(110,95,54,50,95,98,105,116,111,112,115,0),205]]);
         teamdetailsbgC *= gifgoalI.length;
         homeplayerk += `${homeplayerk.length % (Math.max(2, 10))}`;
         gifgoalI += `${render8.size}`;
         render8 = new Map([[`${render8.size}`, render8.size]]);
      while (2.16 > (teamdetailsbgC - 5)) {
          let textlayoutmanagerK = 3;
         teamdetailsbgC -= parseInt(`${teamdetailsbgC}`) ^ 1;
         textlayoutmanagerK %= Math.max(3, textlayoutmanagerK);
         break;
      }
      qaag7 = `${(parseInt(`${appsi}`) + (yellowscoreballx ? 1 : 5))}`;
   if (yellowscoreballx) {
       let owngoalD = String.fromCharCode(109,97,105,110,104,101,97,100,101,114,95,100,95,56,50,0);
       let smallR: Array<any> = [791, 373, 847];
       let controli = String.fromCharCode(119,95,57,56,95,98,114,107,116,105,109,101,103,109,0);
       let constantsX = 1.0;
       let orientationb = 5.0;
      let homep = 8701764 <= smallR.length;
      do {
         smallR.push(2);
         if (homep) {
            break;
         }
      } while (homep && (4.84 <= (5.33 / (Math.max(2, orientationb))) || 5 <= (smallR.length << (Math.min(Math.abs(4), 4)))));
       let reactnativejsf = 1.0;
          let libnmso = 3;
          let resultP = 1.0;
          let defaultroombg5 = 4.0;
         owngoalD = `${(owngoalD == String.fromCharCode(102,0) ? owngoalD.length : parseInt(`${orientationb}`))}`;
         libnmso |= 1 >> (Math.min(Math.abs(parseInt(`${defaultroombg5}`)), 1));
         resultP += 1;
         defaultroombg5 -= libnmso & parseInt(`${resultP}`);
          let iconqqd = 4;
          let rankM: Map<any, any> = new Map([[String.fromCharCode(109,98,117,102,95,108,95,55,0),406], [String.fromCharCode(113,117,101,115,116,105,111,110,95,55,95,55,50,0),356], [String.fromCharCode(109,101,114,103,101,100,95,115,95,50,48,0),816]]);
          let giftw = 1;
         orientationb -= parseFloat(`${owngoalD.length ^ 2}`);
         iconqqd %= Math.max(giftw << (Math.min(Math.abs(2), 4)), 2);
         rankM = new Map([[`${giftw}`, giftw << (Math.min(5, Math.abs(iconqqd)))]]);
      if ((2 - controli.length) <= 5) {
         reactnativejsf *= 1 ^ owngoalD.length;
      }
          let becomeQ = String.fromCharCode(115,116,114,111,107,101,115,95,57,95,51,56,0);
         constantsX /= Math.max(parseFloat(`${parseInt(`${constantsX}`)}`), 2);
         becomeQ = "2";
      let libavformatx = constantsX <= 5301777.0;
      do {
          let libavfilterM = 0.0;
          let libavcodecl = 0.0;
          let gifgoalbg3 = false;
          let playercommonh = 2.0;
         constantsX += parseFloat(`${parseInt(`${playercommonh}`)}`);
         libavfilterM += parseFloat(`${2 | parseInt(`${libavcodecl}`)}`);
         libavcodecl += 3 / (Math.max(6, parseInt(`${libavfilterM}`)));
         gifgoalbg3 = 16.100 == libavcodecl && libavfilterM == 16.100;
         playercommonh += parseInt(`${libavcodecl}`) - 1;
         if (libavformatx) {
            break;
         }
      } while (((4 / (Math.max(10, owngoalD.length))) < 2 || 3 < (owngoalD.length % 4)) && libavformatx);
      if ((2 + reactnativejsf) < 4.84) {
         orientationb /= Math.max(parseFloat(`${controli.length}`), 5);
      }
      predictionbuttonC += "2";
   }
       let backiconQ: Map<any, any> = new Map([[String.fromCharCode(109,110,101,109,111,110,105,99,95,99,95,53,51,0),false ], [String.fromCharCode(115,95,53,55,95,103,105,116,104,117,98,0),true ], [String.fromCharCode(116,95,49,57,95,114,101,99,104,101,99,107,0),true ]]);
       let defaultplayerimgP = String.fromCharCode(106,95,48,95,112,114,111,98,108,101,109,0);
      for (let l = 0; l < 2; l++) {
         backiconQ = new Map([[`${backiconQ.size}`, backiconQ.size]]);
      }
       let mergerE = String.fromCharCode(116,95,48,95,99,97,114,101,102,117,108,108,121,0);
       let shooty = String.fromCharCode(115,111,99,107,101,116,115,95,99,95,51,53,0);
       let privacyR = String.fromCharCode(102,95,53,51,95,110,115,101,103,109,101,110,116,115,0);
          let movieso = 2;
          let mimeW: Map<any, any> = new Map([[String.fromCharCode(112,101,110,99,105,108,95,54,95,55,51,0),889], [String.fromCharCode(107,95,50,51,95,114,103,98,120,105,0),954]]);
          let libcxxcomponents5 = String.fromCharCode(101,95,50,49,95,109,97,112,112,97,98,108,101,0);
         privacyR = "3";
         movieso |= libcxxcomponents5.length;
         mimeW.set(libcxxcomponents5, libcxxcomponents5.length ^ 3);
         backiconQ = new Map([[privacyR, (defaultplayerimgP == String.fromCharCode(52,0) ? defaultplayerimgP.length : privacyR.length)]]);
      for (let g = 0; g < 1; g++) {
          let mintegralD = false;
          let cornerkickU: Map<any, any> = new Map([[String.fromCharCode(115,95,53,54,95,117,118,104,111,114,105,122,111,110,116,97,108,0),String.fromCharCode(104,95,53,48,95,114,117,115,115,105,97,110,0)], [String.fromCharCode(114,101,109,97,105,110,95,55,95,55,57,0),String.fromCharCode(110,95,55,57,95,111,112,97,99,105,116,121,0)]]);
         mergerE = `${cornerkickU.size}`;
         mintegralD = (!mintegralD ? mintegralD : !mintegralD);
         cornerkickU = new Map([[`${mintegralD}`, 3]]);
      }
      qaag7 += `${3 / (Math.max(10, predictionbuttonC.length))}`;
   while (buildI) {
       let alert4 = String.fromCharCode(116,97,103,95,97,95,57,57,0);
       let shareL = String.fromCharCode(100,117,97,108,95,57,95,49,0);
      if (!shareL.endsWith(alert4)) {
         alert4 += `${shareL.length / (Math.max(5, alert4.length))}`;
      }
         alert4 = `${3 - shareL.length}`;
         alert4 += `${shareL.length}`;
         alert4 += `${alert4.length >> (Math.min(shareL.length, 5))}`;
      if (alert4.length <= shareL.length) {
         shareL = `${shareL.length}`;
      }
         shareL = `${2 & shareL.length}`;
      animations8.set(alert4, alert4.length | 1);
      break;
   }
      return;
    }

    await dispatch(updateUserAuth(result));

      predictionbuttonC += `${parseInt(`${appsi}`) - predictionbuttonC.length}`;
   while (!buildI) {
      appsi -= parseFloat(`${combined_.length}`);
      break;
   }
   if (animations8.size <= 3 && 2 <= (animations8.size | 3)) {
       let utilsS = true;
       let nativec: Array<any> = [String.fromCharCode(104,97,115,120,95,117,95,51,50,0), String.fromCharCode(100,105,118,105,100,101,114,95,51,95,54,54,0), String.fromCharCode(100,95,57,55,95,101,97,99,104,0)];
       let nterstitialN = 5;
       let updates8 = String.fromCharCode(122,95,54,95,116,97,108,108,0);
         nativec = [nterstitialN | nativec.length];
          let iconarrowrightorange4: Array<any> = [689, 156];
          let cornerd = false;
         nterstitialN *= iconarrowrightorange4.length;
         nterstitialN /= Math.max((updates8.length / (Math.max(6, (utilsS ? 4 : 2)))), 2);
      let debugB = utilsS ? !utilsS : utilsS;
      do {
          let match2 = 2.0;
          let applicationn = 2.0;
          let libavutilU = true;
         utilsS = nativec.length <= 79;
         match2 -= parseInt(`${match2}`) / 1;
         applicationn += 2 | parseInt(`${applicationn}`);
         libavutilU = match2 <= 34.76;
         if (debugB) {
            break;
         }
      } while ((!updates8.includes(`${utilsS}`)) && debugB);
         nativec = [3 | nativec.length];
      let fillh = 5785516 <= nativec.length;
      do {
         nativec.push((updates8 == String.fromCharCode(117,0) ? updates8.length : (utilsS ? 4 : 4)));
         if (fillh) {
            break;
         }
      } while ((nativec.length >= 1) && fillh);
         nativec = [3 + nterstitialN];
      let step9 = 7219229 <= nativec.length;
      do {
          let pathx = false;
         nativec.push(1);
         pathx = !pathx;
         if (step9) {
            break;
         }
      } while (((nterstitialN * nativec.length) >= 1) && step9);
      let singaporeS = utilsS ? !utilsS : utilsS;
      do {
         utilsS = !updates8.endsWith(`${utilsS}`);
         if (singaporeS) {
            break;
         }
      } while (singaporeS && (4 == nterstitialN));
          let right5 = String.fromCharCode(112,95,52,50,95,114,101,108,97,121,115,0);
          let condensedf = String.fromCharCode(110,95,57,55,95,114,117,110,110,105,110,103,0);
          let runtimeschedulerk = 2.0;
         updates8 += `${3 * nativec.length}`;
         right5 = `${parseInt(`${runtimeschedulerk}`) << (Math.min(condensedf.length, 2))}`;
         condensedf += "1";
         runtimeschedulerk += condensedf.length;
         nterstitialN -= 2 ^ nativec.length;
      for (let g = 0; g < 2; g++) {
         updates8 += `${nterstitialN & 2}`;
      }
      animations8 = new Map([[predictionbuttonC, (predictionbuttonC == String.fromCharCode(120,0) ? predictionbuttonC.length : parseInt(`${appsi}`))]]);
   }
      combined_ = [predictionbuttonC.length];

    return;
  };

  useEffect(() => {
    

    let mergedArray;

    if (userState.user?.userUpline.created_at != '') {
      mergedArray = userState.user?.userInvitedUserList.concat(userState.user.userUpline);
    } else {
      mergedArray = userState.user.userInvitedUserList;
    }

    mergedArray.sort((a: any, b: any) => {
      let dateA: any;
      dateA = new Date(a.created_at);
      let dateB: any;

      dateB = new Date(b.created_at);
      return dateB - dateA;
    });

    mergedArray = mergedArray.map((item: any) => {
      let displayText = '';
      if (item.text) {
        displayText = item.text;
        item.invited_vip_reward_day = item.vip_reward_day;
      } else {
        displayText = item.title ;
      }

      return { ...item, displayText: displayText };
    });
    console.log('mergedArray');
    console.log(mergedArray);
    setInviteList(mergedArray);
  }, []);

  return (
    <ScreenContainer>
      <TitleWithBackButtonHeader title="累计奖励明细" />
      <ScrollView
        showsVerticalScrollIndicator={false}
        refreshControl={
          <RefreshControl refreshing={refreshing} onRefresh={handleRefresh} />
        }>
        { }
        <LinearGradient
          colors={['#323638', '#1a1d20']} 
          locations={[0.1, 1]}
          start={{ x: 1, y: 0 }} 
          end={{ x: 0, y: 1 }} 
          style={{
            marginTop: 40,
            paddingHorizontal: 20,
            paddingVertical: 10,
            borderRadius: 15,
            flexDirection: 'row', 
            justifyContent: 'space-between',
            alignItems: 'center',
            minHeight: 120,
          }}>
          <View style={styles.container}>
            { }
            <Text style={styles.title}>{userState.user?.userTotalInvite}</Text>
            <Text style={styles.subTitle}>已邀请人数</Text>
          </View>

          <View style={styles.container}>
            { }
            <Text style={styles.title}>
              {userState.user?.userAccumulateRewardDay}
            </Text>
            <Text style={styles.subTitle}>累计获得天数</Text>
          </View>
        </LinearGradient>

        { }
        <View style={{ marginTop: 40, alignItems: 'center' }}>
          <Text
            style={{ ...textVariants.header, fontSize: 16, fontWeight: '700' }}>
            邀请记录
          </Text>

          {inviteList.length > 0 && (
            <View
              style={{
                marginTop: 20,
                marginBottom: 40,
                backgroundColor: '#1D2023',
                borderRadius: 15,
                overflow: 'hidden',
                width: '100%',
              }}>
              {inviteList.map(item => (
                <View
                  key={item.displayText}
                  style={{
                    flexDirection: 'row',
                    justifyContent: 'space-between',
                    alignContent: 'center',
                    paddingHorizontal: 20,
                    paddingVertical: 15,
                  }}>
                  <View style={{ flex: 1, paddingRight: 10 }}>
                    <Text
                      style={{ fontSize: 16, color: '#fff' }}
                      numberOfLines={1}
                      ellipsizeMode="tail">
                      {item.displayText}
                    </Text>
                    <Text style={{ color: '#9c9c9c' }}>{item.created_at}</Text>
                  </View>

                  <View
                    style={{
                      justifyContent: 'center',
                      alignContent: 'center',
                    }}>
                    <Text
                      style={{
                        color: colors.primary,
                        fontSize: 16,
                        flex: 1,
                      }}>
                      +{item.invited_vip_reward_day}天
                    </Text>
                  </View>
                </View>
              ))}
            </View>
          )}

          {inviteList.length == 0 && (
            <View
              style={{
                marginTop: 20,
                marginBottom: 40,
                backgroundColor: '#1D2023',
                borderRadius: 15,
                overflow: 'hidden',
                width: '100%',
              }}>
              <View
                style={{
                  flexDirection: 'row',

                  alignContent: 'center',
                  paddingHorizontal: 20,
                  paddingVertical: 20,
                }}>
                <View>
                  <Text style={{ color: '#fff' }}>暂时无邀请记录</Text>
                </View>
              </View>
            </View>
          )}
        </View>
      </ScrollView>
    </ScreenContainer>
  );
};

const styles = StyleSheet.create({
  loading: {
    flexDirection: 'row',
    justifyContent: 'center',
    flex: 1,
  },
  backgroundImage: {
    flex: 1, 
    resizeMode: 'cover', 
  },
  container: {
    flex: 1,
    justifyContent: 'space-between',
    alignItems: 'flex-start',
    paddingLeft: 25,
  },
  title: {
    color: '#fff',
    fontSize: 26,
    fontWeight: '900',
  },
  subTitle: {
    fontSize: 16,
    fontWeight: '400',
    color: '#9C9C9C',
  },
});
