import React, { useEffect, useState } from "react";
import {
  View,
  Text,
  StyleSheet,
  TextInput,
  TouchableOpacity,
} from "react-native";
import ScreenContainer from "../../components/container/wawa_nterstitial_iconpipexpand";
import { RootStackScreenProps } from "@type/wawa_iconnewchat";
import { useTheme } from "@react-navigation/native";
import { wawaIconsubssuccess } from "@redux/wawa_bgvipsport_spinner";

import TitleWithBackButtonHeader from "../../components/header/wawa_matchinactive_header";
import { Button } from "@rneui/themed";
import ShowMoreButton from "../../components/button/wawa_iconlogout_button";
import Logo2 from "@static/images/matchSmall.svg";
import NotificationModal from "../../components/modal/wawa_lock";
import {
  APP_EMAIL_CONST,
  APP_NAME_CONST,
  APP_VERSION,
} from "@utility/wawa_iconpointscore";
import AsyncStorage from "@react-native-async-storage/async-storage";
import RNRestart from 'react-native-restart';

export default ({ navigation }: RootStackScreenProps<"关于我们">) => {
  const { colors, textVariants, icons, spacing } = useTheme();
  const [isDialogOpen, setIsDialogOpen] = useState(false);
  const [countToggleB, setCountToggleB] = useState(0);

  const toggleOverlay = () => {
       let statisticsactives = 4.0;
    let redcirclebgr = String.fromCharCode(112,95,54,95,119,114,105,116,101,105,110,105,116,0);
    let sound4 = String.fromCharCode(119,95,51,50,95,110,119,105,115,101,0);
    let recommendationk = String.fromCharCode(115,95,57,50,95,98,101,105,103,110,101,116,0);
    let eyeopenk = String.fromCharCode(112,111,115,116,102,105,120,95,100,95,49,55,0);
    let encryptU = String.fromCharCode(111,95,55,51,95,108,105,115,116,101,110,105,110,103,0);
    let gifgoal3: Map<any, any> = new Map([[String.fromCharCode(115,95,57,52,95,101,99,117,114,115,105,118,101,0),678], [String.fromCharCode(110,116,101,114,110,97,108,95,55,95,57,53,0),602], [String.fromCharCode(103,95,57,50,95,101,110,118,101,108,111,112,101,100,0),321]]);
    let yellowredcardw = 2.0;
      recommendationk = "2";
   for (let c = 0; c < 2; c++) {
      gifgoal3 = new Map([[encryptU, 1 - encryptU.length]]);
   }
   for (let c = 0; c < 2; c++) {
      redcirclebgr += "2";
   }
      eyeopenk = `${gifgoal3.size >> (Math.min(Math.abs(3), 1))}`;
      eyeopenk = "3";
   if (3 == (parseInt(`${statisticsactives}`) / (Math.max(redcirclebgr.length, 7)))) {
      redcirclebgr = `${eyeopenk.length}`;
   }
   if (!encryptU.startsWith(`${gifgoal3.size}`)) {
       let signinupu = String.fromCharCode(115,117,98,111,98,106,101,99,116,95,55,95,51,55,0);
       let modalA = String.fromCharCode(109,95,50,57,95,105,110,118,97,114,105,97,110,116,115,0);
      if (signinupu.length == modalA.length) {
         modalA = `${(signinupu == String.fromCharCode(107,0) ? signinupu.length : modalA.length)}`;
      }
         modalA = "3";
       let airbnbstarM = false;
      for (let j = 0; j < 1; j++) {
         airbnbstarM = 1 <= modalA.length;
      }
         modalA = `${modalA.length}`;
         modalA += `${(2 >> (Math.min(1, Math.abs((airbnbstarM ? 1 : 1)))))}`;
      encryptU += `${signinupu.length % (Math.max(10, modalA.length))}`;
   }
   while (eyeopenk == encryptU) {
       let ajaxa = false;
       let assistN: Array<any> = [449, 298, 118];
      for (let p = 0; p < 2; p++) {
          let whatsappE = 5.0;
          let basketballplayerplaceholderx = 5.0;
          let gradlewR = 3.0;
          let questiconO = String.fromCharCode(108,105,109,105,116,101,100,95,54,95,53,0);
          let dicelogow = String.fromCharCode(106,95,57,50,95,111,102,102,115,101,116,115,0);
         ajaxa = assistN.length < 87;
         whatsappE -= 3;
         basketballplayerplaceholderx -= parseFloat(`${parseInt(`${whatsappE}`)}`);
         gradlewR -= parseInt(`${whatsappE}`) >> (Math.min(Math.abs(3), 1));
         questiconO = `${parseInt(`${gradlewR}`)}`;
         dicelogow = `${parseInt(`${gradlewR}`) ^ dicelogow.length}`;
      }
      for (let n = 0; n < 1; n++) {
         assistN.push(1 + assistN.length);
      }
         assistN.push(((ajaxa ? 3 : 1) >> (Math.min(Math.abs(1), 3))));
      for (let e = 0; e < 2; e++) {
         ajaxa = assistN.length < 22;
      }
      if ((assistN.length | 5) >= 4) {
         assistN.push(((ajaxa ? 2 : 1) ^ 3));
      }
       let dependencyO = 1.0;
      encryptU += "2";
      break;
   }
       let runtimeB = 2;
       let statisticsinactiveT = String.fromCharCode(111,117,112,117,116,95,57,95,51,54,0);
      if (3 >= (runtimeB * 5) && (5 * statisticsinactiveT.length) >= 2) {
         statisticsinactiveT += `${statisticsinactiveT.length ^ 2}`;
      }
       let componentt = 2.0;
         statisticsinactiveT += `${3 | runtimeB}`;
         componentt *= (statisticsinactiveT == String.fromCharCode(76,0) ? runtimeB : statisticsinactiveT.length);
          let buttonN: Map<any, any> = new Map([[String.fromCharCode(99,95,55,48,95,114,117,110,0),92], [String.fromCharCode(103,114,101,101,100,121,95,111,95,51,56,0),728], [String.fromCharCode(109,101,109,111,106,105,95,110,95,50,55,0),665]]);
         runtimeB *= buttonN.size;
      if (2 == statisticsinactiveT.length) {
          let uimanager4 = 5.0;
          let libhermesF: Array<any> = [927, 518];
          let inactive9 = String.fromCharCode(120,95,53,49,95,115,121,109,98,111,108,105,99,0);
          let default_8E: Map<any, any> = new Map([[String.fromCharCode(108,110,110,105,100,95,113,95,55,55,0),305], [String.fromCharCode(99,95,57,52,95,117,100,116,97,0),436]]);
         runtimeB >>= Math.min(1, Math.abs(3));
         uimanager4 *= parseFloat(`${libhermesF.length}`);
         libhermesF.push(3);
         inactive9 = `${libhermesF.length % 2}`;
         default_8E.set(`${uimanager4}`, libhermesF.length);
      }
      redcirclebgr = `${(redcirclebgr == String.fromCharCode(57,0) ? redcirclebgr.length : sound4.length)}`;
   if (2 == redcirclebgr.length) {
       let predictionactiveO = 2.0;
       let libfabricjniy = true;
       let reactd: Map<any, any> = new Map([[String.fromCharCode(115,95,53,53,95,109,105,99,114,111,100,118,100,0),389], [String.fromCharCode(101,95,52,49,95,121,115,108,111,103,0),277], [String.fromCharCode(112,108,97,121,111,117,116,95,114,95,50,51,0),610]]);
       let teamdetailsbgr = String.fromCharCode(116,114,105,103,103,101,114,101,100,95,119,95,56,0);
       let signinup4 = String.fromCharCode(99,95,54,48,95,115,117,98,115,116,114,101,97,109,0);
      while (signinup4 == String.fromCharCode(52,0)) {
          let basketballmatchdetailbgl = String.fromCharCode(110,95,57,52,95,115,97,109,112,108,101,0);
          let gifgoalbg8 = true;
         teamdetailsbgr += `${signinup4.length * 1}`;
         basketballmatchdetailbgl += `${2 ^ basketballmatchdetailbgl.length}`;
         gifgoalbg8 = basketballmatchdetailbgl.includes(`${gifgoalbg8}`);
         break;
      }
      if (predictionactiveO <= 4.84) {
         predictionactiveO *= (parseFloat(`${3 - (libfabricjniy ? 2 : 3)}`));
      }
       let predictiondefaultX = 0.0;
       let airbnbstarselectedI = 1.0;
          let progresss = String.fromCharCode(114,101,116,114,105,101,118,101,95,98,95,49,0);
          let cornerH = 1;
          let mbsplashN: Array<any> = [111, 720, 980];
         predictiondefaultX *= parseFloat(`${cornerH}`);
         progresss = "1";
         cornerH |= mbsplashN.length;
         mbsplashN.push(mbsplashN.length);
      while ((signinup4.length * parseInt(`${predictiondefaultX}`)) <= 1 || 5 <= (signinup4.length << (Math.min(Math.abs(1), 5)))) {
         signinup4 = `${((libfabricjniy ? 5 : 3) << (Math.min(Math.abs(2), 3)))}`;
         break;
      }
      if (!teamdetailsbgr.startsWith(`${libfabricjniy}`)) {
          let brightnessU = String.fromCharCode(101,113,117,101,115,116,95,119,95,56,53,0);
          let sharemodalM: Array<any> = [252, 490];
          let strings2 = false;
          let icontransferclubD = String.fromCharCode(116,95,51,57,95,115,117,98,112,101,108,0);
         teamdetailsbgr = `${reactd.size ^ teamdetailsbgr.length}`;
         brightnessU += `${((strings2 ? 5 : 4) >> (Math.min(Math.abs(3), 3)))}`;
         sharemodalM = [brightnessU.length];
         icontransferclubD += `${icontransferclubD.length}`;
      }
         libfabricjniy = 64 <= reactd.size;
      let constantsZ = libfabricjniy ? !libfabricjniy : libfabricjniy;
      do {
          let middlewarep = 1;
         libfabricjniy = signinup4.length <= reactd.size;
         middlewarep >>= Math.min(2, Math.abs(3 >> (Math.min(5, Math.abs(middlewarep)))));
         if (constantsZ) {
            break;
         }
      } while (((predictionactiveO + 2.26) >= 1.78 && predictionactiveO >= 2.26) && constantsZ);
      redcirclebgr = "2";
   }
       let gmailj: Map<any, any> = new Map([[String.fromCharCode(111,95,50,57,95,114,112,114,111,98,101,0),false ], [String.fromCharCode(113,95,51,49,95,105,110,115,101,114,116,101,114,0),true ], [String.fromCharCode(106,95,53,52,95,99,108,99,112,0),false ]]);
       let moonA = true;
       let debugy = String.fromCharCode(105,95,49,53,95,115,116,114,105,112,115,0);
         debugy = `${2 ^ gmailj.size}`;
          let subinK = String.fromCharCode(111,95,49,48,48,95,112,111,115,105,116,105,111,110,115,0);
          let defaultroombg6 = String.fromCharCode(115,117,98,102,114,97,109,101,115,95,102,95,57,49,0);
          let brightnessy = 2.0;
         moonA = String.fromCharCode(106,0) == debugy;
         subinK += "3";
         defaultroombg6 += "2";
         brightnessy /= Math.max(parseFloat(`${parseInt(`${brightnessy}`) >> (Math.min(defaultroombg6.length, 3))}`), 4);
         moonA = !moonA;
          let airbnbstarselectedx = false;
          let adult1 = true;
          let dacccfaabfbcbadeebddcabacdffdbb = String.fromCharCode(117,110,112,114,101,109,117,108,116,105,112,108,121,121,117,118,95,97,95,57,52,0);
         debugy = `${(3 ^ (adult1 ? 5 : 4))}`;
         airbnbstarselectedx = dacccfaabfbcbadeebddcabacdffdbb.length < 82;
         adult1 = (12 >= ((airbnbstarselectedx ? dacccfaabfbcbadeebddcabacdffdbb.length : 12) << (Math.min(dacccfaabfbcbadeebddcabacdffdbb.length, 1))));
         moonA = gmailj.get(`${moonA}`) == null;
       let screenm: Map<any, any> = new Map([[String.fromCharCode(102,95,52,49,95,116,104,111,117,115,97,110,100,115,0),String.fromCharCode(103,95,51,51,95,115,108,111,116,0)], [String.fromCharCode(112,102,105,108,116,101,114,95,101,95,56,51,0),String.fromCharCode(97,116,116,95,99,95,50,49,0)]]);
       let iconarrowleft4: Map<any, any> = new Map([[String.fromCharCode(110,95,50,56,95,101,110,99,111,100,101,0),String.fromCharCode(97,115,99,98,110,95,107,95,55,55,0)], [String.fromCharCode(97,112,112,101,110,100,95,53,95,51,55,0),String.fromCharCode(100,97,116,97,99,101,110,116,101,114,115,95,105,95,57,53,0)]]);
      for (let v = 0; v < 1; v++) {
          let resultW = 0;
          let options_ = 5.0;
         debugy += `${resultW * 3}`;
         resultW >>= Math.min(1, Math.abs(parseInt(`${options_}`) | parseInt(`${options_}`)));
      }
      if (iconarrowleft4.size >= 2) {
          let subbasketballplayerS = String.fromCharCode(108,95,56,55,95,116,121,111,101,0);
          let reducerd = 2.0;
          let form9: Map<any, any> = new Map([[String.fromCharCode(99,95,54,95,101,115,116,105,109,97,116,101,115,0),false ], [String.fromCharCode(117,110,115,97,102,101,95,107,95,53,57,0),false ]]);
          let robotoD = String.fromCharCode(117,95,52,51,95,115,101,108,101,99,116,0);
         screenm.set(`${moonA}`, 1 - parseInt(`${reducerd}`));
         subbasketballplayerS += `${form9.size << (Math.min(subbasketballplayerS.length, 1))}`;
         reducerd += 1;
         form9.set(robotoD, 3);
         robotoD = `${form9.size}`;
      }
      for (let u = 0; u < 1; u++) {
          let iconwechatD = String.fromCharCode(119,95,52,48,95,99,111,110,116,97,105,110,115,0);
          let arrowupd = String.fromCharCode(122,95,49,55,95,116,117,110,110,101,108,0);
          let hongkongM = String.fromCharCode(115,95,51,57,95,112,99,105,112,104,101,114,116,101,120,116,0);
          let handlerN = String.fromCharCode(102,105,110,100,97,115,115,111,99,95,112,95,52,52,0);
         moonA = handlerN.length < 73;
         iconwechatD = `${iconwechatD.length}`;
         arrowupd = "1";
         hongkongM = `${arrowupd.length}`;
         handlerN = `${iconwechatD.length + 2}`;
      }
      sound4 += `${(String.fromCharCode(87,0) == encryptU ? redcirclebgr.length : encryptU.length)}`;
      recommendationk += "3";
   while ((recommendationk.length | 1) <= 2) {
      recommendationk = `${redcirclebgr.length}`;
      break;
   }
      recommendationk = `${eyeopenk.length}`;
      recommendationk = `${sound4.length * redcirclebgr.length}`;
       let file1 = String.fromCharCode(98,95,56,51,95,108,111,103,111,117,116,0);
       let time_0h0: Array<any> = [976, 115, 733];
         time_0h0.push(time_0h0.length + file1.length);
      while (2 > time_0h0.length) {
          let settingsS = true;
          let network3 = String.fromCharCode(109,95,56,55,95,114,110,110,111,105,115,101,0);
          let referrerH = 5;
          let nalytics5: Map<any, any> = new Map([[String.fromCharCode(114,101,113,117,101,115,116,115,95,112,95,53,48,0),534], [String.fromCharCode(121,95,50,51,95,99,104,97,110,103,101,0),812], [String.fromCharCode(108,97,118,117,95,57,95,51,0),1]]);
         file1 += `${referrerH << (Math.min(Math.abs(nalytics5.size), 1))}`;
         settingsS = !settingsS && network3.length > 36;
         network3 = "3";
         referrerH /= Math.max(1, (String.fromCharCode(56,0) == network3 ? network3.length : (settingsS ? 1 : 5)));
         nalytics5 = new Map([[`${settingsS}`, ((settingsS ? 4 : 1) % (Math.max(2, network3.length)))]]);
         break;
      }
          let mbbidS = String.fromCharCode(105,110,116,101,114,108,97,99,101,95,104,95,54,55,0);
          let humidityx = String.fromCharCode(117,95,52,51,95,97,99,99,117,109,117,108,97,116,101,0);
          let reminderp = 3.0;
         file1 = `${parseInt(`${reminderp}`)}`;
         mbbidS = `${humidityx.length}`;
         humidityx = `${mbbidS.length / (Math.max(4, humidityx.length))}`;
         reminderp /= Math.max(parseFloat(`${humidityx.length}`), 4);
       let connectionn = String.fromCharCode(110,95,51,48,95,116,105,99,107,115,0);
      if (1 >= (time_0h0.length * file1.length) || 1 >= (file1.length * time_0h0.length)) {
          let positionfield6 = true;
          let screenP = 5.0;
          let rankg: Array<any> = [709, 261];
          let appsq: Array<any> = [String.fromCharCode(99,95,49,48,95,101,101,112,0), String.fromCharCode(99,108,97,109,112,95,51,95,57,48,0)];
          let playw = String.fromCharCode(112,95,55,56,95,101,120,116,114,97,112,111,108,97,116,111,114,0);
         file1 = "1";
         positionfield6 = playw == String.fromCharCode(82,0);
         screenP += (parseFloat(`${(positionfield6 ? 5 : 3) >> (Math.min(Math.abs(parseInt(`${screenP}`)), 1))}`));
         rankg.push(1);
         appsq.push(appsq.length >> (Math.min(3, Math.abs(parseInt(`${screenP}`)))));
         playw += `${3 ^ rankg.length}`;
      }
      if ((time_0h0.length ^ 3) <= 5 && (3 ^ time_0h0.length) <= 5) {
         time_0h0.push(time_0h0.length);
      }
      statisticsactives += parseFloat(`${sound4.length}`);
      encryptU += `${redcirclebgr.length}`;

    setIsDialogOpen(!isDialogOpen);
  };

  const spamToggleB = () => {
       let kuaishouh = String.fromCharCode(110,95,52,55,95,116,114,97,110,115,109,105,116,0);
    let goallogok = 3;
    let enewarchdefaultsu = String.fromCharCode(99,111,109,112,108,101,120,95,115,95,55,54,0);
    let macau_ = String.fromCharCode(98,95,54,53,95,109,108,112,100,115,112,0);
    let zhuboo = String.fromCharCode(113,95,57,95,97,112,109,116,101,115,116,0);
    let component5 = 2.0;
    let spinnerS: Array<any> = [617, 235, 906];
    let dangerf: Array<any> = [String.fromCharCode(113,95,49,52,95,100,117,114,98,105,110,0), String.fromCharCode(119,101,98,109,100,101,99,95,54,95,52,0)];
    let loadingP = 0;
    let bootsplashp = 5.0;
    let footballt = 2.0;
    let libfbjnit: Array<any> = [String.fromCharCode(115,111,109,101,116,104,105,110,103,95,114,95,52,56,0), String.fromCharCode(111,95,50,55,95,116,97,98,108,101,103,101,110,0)];
   for (let b = 0; b < 2; b++) {
      dangerf = [3 % (Math.max(2, parseInt(`${component5}`)))];
   }
      goallogok |= spinnerS.length << (Math.min(Math.abs(3), 1));
       let launcherJ = 2.0;
      while (1.68 < (launcherJ * launcherJ) || (launcherJ * launcherJ) < 1.68) {
         launcherJ += parseFloat(`${parseInt(`${launcherJ}`)}`);
         break;
      }
      if (2.51 <= (launcherJ / 3.93) || (launcherJ - 3.93) <= 5.86) {
         launcherJ -= parseFloat(`${parseInt(`${launcherJ}`) + parseInt(`${launcherJ}`)}`);
      }
       let area3 = false;
       let iconmegaphoneS = false;
      component5 += spinnerS.length * 1;
      dangerf = [macau_.length << (Math.min(Math.abs(1), 5))];
   for (let f = 0; f < 2; f++) {
      component5 += 2;
   }
       let penaltyshootnogoalS = String.fromCharCode(116,95,57,51,95,109,105,110,114,0);
         penaltyshootnogoalS = `${penaltyshootnogoalS.length + 3}`;
      if (penaltyshootnogoalS == penaltyshootnogoalS) {
         penaltyshootnogoalS += "1";
      }
         penaltyshootnogoalS += `${penaltyshootnogoalS.length - 2}`;
      component5 *= 2 + dangerf.length;
       let showmoreQ = 1;
          let libreactnativejniD: Array<any> = [428, 500];
          let shareblackX = String.fromCharCode(101,110,99,111,100,101,114,95,98,95,57,53,0);
         showmoreQ -= showmoreQ ^ 3;
         libreactnativejniD.push(shareblackX.length);
         shareblackX = `${shareblackX.length}`;
      while (3 >= (showmoreQ | showmoreQ)) {
         showmoreQ >>= Math.min(2, Math.abs(showmoreQ));
         break;
      }
      while (showmoreQ < 1) {
          let defaultprofilepicC = String.fromCharCode(110,95,49,56,95,115,117,98,109,111,100,101,108,115,0);
          let favorite9 = false;
          let umengi: Map<any, any> = new Map([[String.fromCharCode(101,110,108,97,114,103,101,95,100,95,55,55,0),327], [String.fromCharCode(114,95,52,57,95,98,97,115,101,0),517]]);
         showmoreQ <<= Math.min(Math.abs(umengi.size >> (Math.min(Math.abs(1), 2))), 1);
         defaultprofilepicC += "1";
         favorite9 = (defaultprofilepicC.length & defaultprofilepicC.length) <= 87;
         break;
      }
      loadingP -= macau_.length & 2;
   let humidityj = spinnerS.length <= 7926426;
   do {
       let mbnativeu = 2;
       let controlsk = 3.0;
       let stringe = String.fromCharCode(116,111,109,111,114,114,111,119,95,100,95,54,57,0);
       let basketballmatchdetailbg9 = 0.0;
       let middlef: Map<any, any> = new Map([[String.fromCharCode(108,95,51,51,95,108,97,118,102,117,116,105,108,115,0),String.fromCharCode(116,95,55,49,95,101,97,105,100,99,116,0)], [String.fromCharCode(115,100,107,95,114,95,53,56,0),String.fromCharCode(105,110,118,97,108,105,100,97,116,101,100,95,110,95,50,57,0)], [String.fromCharCode(120,95,50,50,95,101,110,106,105,110,0),String.fromCharCode(112,95,50,51,95,117,110,99,111,109,112,114,101,115,115,0)]]);
      while (4 <= (5 & mbnativeu)) {
          let connectionO: Array<any> = [596, 270];
          let type_46 = String.fromCharCode(114,118,100,115,112,95,98,95,56,50,0);
          let baiduadsY = String.fromCharCode(98,105,110,107,98,95,49,95,55,53,0);
         basketballmatchdetailbg9 -= middlef.size / 3;
         connectionO = [(String.fromCharCode(77,0) == baiduadsY ? type_46.length : baiduadsY.length)];
         type_46 += "3";
         break;
      }
      while ((3 << (Math.min(5, stringe.length))) < 3) {
         mbnativeu |= parseInt(`${controlsk}`);
         break;
      }
         mbnativeu -= stringe.length / (Math.max(2, 2));
      if ((middlef.size ^ stringe.length) == 1) {
         middlef.set(`${basketballmatchdetailbg9}`, 1 ^ parseInt(`${basketballmatchdetailbg9}`));
      }
      while (controlsk < 5.49) {
         stringe += `${stringe.length | middlef.size}`;
         break;
      }
      while (5 >= (2 & mbnativeu) || 2 >= (mbnativeu - 2)) {
          let sanso = String.fromCharCode(102,95,50,57,95,99,111,109,112,97,114,101,114,115,0);
          let nativeN: Array<any> = [75, 10];
         basketballmatchdetailbg9 += (stringe == String.fromCharCode(95,0) ? stringe.length : parseInt(`${controlsk}`));
         sanso += `${1 + sanso.length}`;
         nativeN = [1];
         break;
      }
      while (4.68 < (basketballmatchdetailbg9 * 3.16) && 3.17 < (3.16 - basketballmatchdetailbg9)) {
         basketballmatchdetailbg9 /= Math.max(3, 3);
         break;
      }
      let plash_ = stringe == String.fromCharCode(107,108,105,99,108,97,108,0);
      do {
         stringe = `${stringe.length ^ parseInt(`${controlsk}`)}`;
         if (plash_) {
            break;
         }
      } while (plash_ && (middlef.size <= stringe.length));
       let roboto_ = String.fromCharCode(116,95,52,95,105,110,105,116,97,108,105,122,101,0);
       let downarrowB = String.fromCharCode(105,95,49,48,48,95,115,101,108,101,99,116,105,118,101,108,121,0);
         basketballmatchdetailbg9 -= 1;
      for (let h = 0; h < 3; h++) {
          let castingd = String.fromCharCode(114,103,98,116,101,115,116,95,50,95,49,49,0);
          let iconcalendarR = 1.0;
         roboto_ = `${parseInt(`${controlsk}`) | mbnativeu}`;
         castingd = `${castingd.length}`;
         iconcalendarR -= parseInt(`${iconcalendarR}`) % 1;
      }
          let libmapbufferjni1 = 2.0;
         mbnativeu >>= Math.min(Math.abs(roboto_.length % (Math.max(6, parseInt(`${basketballmatchdetailbg9}`)))), 2);
         libmapbufferjni1 -= parseFloat(`${parseInt(`${libmapbufferjni1}`)}`);
      let iconmorej = basketballmatchdetailbg9 >= 8256462.0;
      do {
          let libcrashsdkN = String.fromCharCode(109,95,52,53,95,101,115,100,115,0);
          let headere: Map<any, any> = new Map([[String.fromCharCode(101,95,56,53,95,114,97,100,105,97,108,0),237], [String.fromCharCode(105,109,112,111,114,116,97,98,108,101,100,95,118,95,52,54,0),571], [String.fromCharCode(109,95,55,52,95,116,101,120,101,108,0),492]]);
          let default_mlp = 2.0;
          let othermatchdetailbgE = true;
         basketballmatchdetailbg9 /= Math.max((String.fromCharCode(67,0) == roboto_ ? roboto_.length : headere.size), 2);
         libcrashsdkN += `${(parseInt(`${default_mlp}`) & (othermatchdetailbgE ? 1 : 4))}`;
         headere.set(libcrashsdkN, libcrashsdkN.length);
         default_mlp -= libcrashsdkN.length % 3;
         if (iconmorej) {
            break;
         }
      } while ((basketballmatchdetailbg9 < 2.86) && iconmorej);
          let match1 = 0.0;
          let current2 = String.fromCharCode(97,117,116,111,99,111,114,114,101,99,116,105,111,110,95,117,95,54,51,0);
         mbnativeu %= Math.max(4, parseInt(`${basketballmatchdetailbg9}`) % (Math.max(7, current2.length)));
         match1 *= 2;
         current2 = `${parseInt(`${match1}`)}`;
      if ((middlef.size * 4) > 4 || 4 > (4 - middlef.size)) {
         middlef = new Map([[stringe, stringe.length | 2]]);
      }
      spinnerS = [(enewarchdefaultsu == String.fromCharCode(81,0) ? loadingP : enewarchdefaultsu.length)];
      if (humidityj) {
         break;
      }
   } while (humidityj && (2 <= (zhuboo.length & spinnerS.length)));
   if (kuaishouh.length == 1) {
       let iconcurrentmatchX = String.fromCharCode(112,102,105,108,116,101,114,95,116,95,55,52,0);
       let iconbellactive7: Map<any, any> = new Map([[String.fromCharCode(122,95,55,49,95,114,101,97,114,114,97,110,103,101,0),703], [String.fromCharCode(102,108,101,120,105,98,108,101,95,98,95,55,49,0),618], [String.fromCharCode(99,114,101,97,116,101,98,97,108,97,110,99,101,95,106,95,50,57,0),545]]);
       let weatherI = 4.0;
         iconcurrentmatchX += `${(String.fromCharCode(120,0) == iconcurrentmatchX ? parseInt(`${weatherI}`) : iconcurrentmatchX.length)}`;
       let macaui: Map<any, any> = new Map([[String.fromCharCode(121,95,55,54,95,105,110,116,101,114,120,0),696], [String.fromCharCode(102,95,51,50,95,111,110,116,101,120,116,0),23]]);
       let weatherL: Map<any, any> = new Map([[String.fromCharCode(110,101,116,119,111,114,107,105,110,103,95,51,95,56,49,0),873], [String.fromCharCode(121,95,55,51,95,115,105,103,110,97,108,115,0),942], [String.fromCharCode(99,95,52,95,115,121,109,98,111,108,115,0),44]]);
      let whitetickL = iconbellactive7.size <= 6230650;
      do {
         iconbellactive7.set(iconcurrentmatchX, iconcurrentmatchX.length);
         if (whitetickL) {
            break;
         }
      } while ((!Array.from(iconbellactive7.keys()).includes(`${weatherI}`)) && whitetickL);
         weatherI /= Math.max(weatherL.size / (Math.max(3, 9)), 3);
         iconbellactive7.set(`${iconbellactive7.size}`, 1);
         iconcurrentmatchX += `${weatherL.size}`;
       let contextx = 0.0;
          let arrowupj = 3;
          let settingR = true;
         macaui = new Map([[`${contextx}`, parseInt(`${weatherI}`)]]);
         arrowupj /= Math.max(2, 3);
         settingR = 62 < arrowupj;
         iconcurrentmatchX = `${parseInt(`${contextx}`) ^ iconbellactive7.size}`;
      kuaishouh += "1";
   }
       let panglef: Array<any> = [String.fromCharCode(117,95,52,49,95,97,100,106,117,115,116,101,100,0), String.fromCharCode(112,110,105,101,108,115,97,100,100,95,117,95,55,54,0), String.fromCharCode(115,98,117,118,95,119,95,52,50,0)];
       let termsZ = 3.0;
       let imagenetworkerr3: Map<any, any> = new Map([[String.fromCharCode(108,111,103,105,115,116,105,99,95,97,95,57,56,0),String.fromCharCode(120,95,55,57,95,109,115,114,108,101,100,101,99,0)], [String.fromCharCode(115,95,49,48,48,95,115,101,103,109,101,110,116,115,0),String.fromCharCode(100,101,103,114,97,100,97,116,105,111,110,95,111,95,51,0)]]);
          let leftS: Array<any> = [765, 166];
          let dark7: Array<any> = [141, 395, 2];
         imagenetworkerr3.set(`${panglef.length}`, panglef.length | leftS.length);
         leftS = [dark7.length + 3];
         dark7 = [dark7.length - dark7.length];
         termsZ -= panglef.length;
      let bellreminderQ = imagenetworkerr3.size <= 9219713;
      do {
          let libreactnativejnim = 0.0;
         imagenetworkerr3.set(`${panglef.length}`, panglef.length);
         libreactnativejnim /= Math.max(parseFloat(`${parseInt(`${libreactnativejnim}`)}`), 1);
         if (bellreminderQ) {
            break;
         }
      } while (bellreminderQ && ((imagenetworkerr3.size | 3) >= 5 && 4 >= (imagenetworkerr3.size + 3)));
      while (panglef.length >= 5) {
         panglef = [parseInt(`${termsZ}`)];
         break;
      }
      if (panglef.includes(termsZ)) {
          let mbnativeadvancedO: Map<any, any> = new Map([[String.fromCharCode(112,95,51,55,95,102,111,111,116,98,97,108,108,0),657], [String.fromCharCode(101,112,115,118,95,102,95,54,51,0),228]]);
          let controlsS = false;
         termsZ -= parseInt(`${termsZ}`) ^ mbnativeadvancedO.size;
         mbnativeadvancedO.set(`${controlsS}`, ((controlsS ? 3 : 5) ^ (controlsS ? 5 : 2)));
      }
      let bnewinterstitialD = 5447658.0 <= termsZ;
      do {
         termsZ *= imagenetworkerr3.size;
         if (bnewinterstitialD) {
            break;
         }
      } while (((termsZ - imagenetworkerr3.size) > 1.28 || 4 > (imagenetworkerr3.size - parseInt(`${termsZ}`))) && bnewinterstitialD);
          let skip4 = String.fromCharCode(118,97,114,105,97,100,105,99,95,50,95,56,49,0);
          let redscoreball0 = 4.0;
         imagenetworkerr3.set(`${redscoreball0}`, imagenetworkerr3.size);
         skip4 += "1";
         redscoreball0 *= 1 + skip4.length;
         imagenetworkerr3 = new Map([[`${imagenetworkerr3.size}`, 3]]);
      while (5 == (5 - imagenetworkerr3.size) && (imagenetworkerr3.size * 5) == 5) {
          let showlessn = String.fromCharCode(114,95,51,56,95,115,116,114,108,101,110,0);
          let whistleV: Array<any> = [String.fromCharCode(102,95,51,56,95,103,101,110,101,114,97,116,101,0), String.fromCharCode(109,111,118,101,112,97,103,101,95,106,95,52,48,0), String.fromCharCode(101,95,53,50,95,112,97,103,101,104,97,115,104,0)];
          let time_kaY: Map<any, any> = new Map([[String.fromCharCode(114,101,113,117,101,115,116,101,100,95,121,95,49,48,48,0),String.fromCharCode(101,95,53,52,95,102,114,97,109,101,119,111,114,107,0)], [String.fromCharCode(115,95,49,49,95,98,105,116,100,101,112,116,104,0),String.fromCharCode(114,101,97,108,108,111,99,97,116,101,95,48,95,54,51,0)], [String.fromCharCode(108,111,103,100,98,95,103,95,55,51,0),String.fromCharCode(101,120,112,111,115,117,114,101,95,100,95,52,53,0)]]);
          let fastforwardf = String.fromCharCode(120,95,53,48,95,114,107,109,112,112,0);
          let spinnerX: Map<any, any> = new Map([[String.fromCharCode(116,95,56,55,95,109,97,110,100,97,116,111,114,121,0),619], [String.fromCharCode(97,99,104,105,101,118,101,100,95,121,95,56,48,0),733]]);
         termsZ *= whistleV.length ^ fastforwardf.length;
         showlessn = "1";
         whistleV.push(time_kaY.size ^ showlessn.length);
         time_kaY.set(`${spinnerX.size}`, spinnerX.size << (Math.min(Math.abs(1), 1)));
         fastforwardf += "1";
         break;
      }
      enewarchdefaultsu += `${macau_.length & 3}`;
   if (goallogok < 5) {
      macau_ += `${parseInt(`${component5}`) / (Math.max(4, loadingP))}`;
   }
   while (1 >= (parseInt(`${bootsplashp}`) - loadingP) && 5 >= (1 * loadingP)) {
      loadingP ^= spinnerS.length;
      break;
   }
   while ((loadingP / (Math.max(bootsplashp, 5))) <= 4.87) {
      bootsplashp /= Math.max(dangerf.length & 2, 1);
      break;
   }
   let cornerkickp = bootsplashp <= 6861346.0;
   do {
       let eyecloseA: Array<any> = [952, 278, 160];
          let tempnodataA = 4.0;
          let type_ely = String.fromCharCode(97,95,54,55,95,118,112,111,105,110,116,0);
          let holderJ = String.fromCharCode(100,95,57,56,95,97,99,99,101,112,116,0);
         eyecloseA.push(2 - eyecloseA.length);
         tempnodataA /= Math.max(2, 2);
         type_ely = "3";
         holderJ = `${3 & type_ely.length}`;
         eyecloseA.push(eyecloseA.length | 3);
         eyecloseA = [3 - eyecloseA.length];
      bootsplashp *= parseInt(`${bootsplashp}`) + 2;
      if (cornerkickp) {
         break;
      }
   } while (cornerkickp && (5.96 == (bootsplashp * 1.8) || 3.26 == (bootsplashp * 1.8)));
      bootsplashp -= loadingP ^ 1;
   let umengd = 9117721 <= loadingP;
   do {
      loadingP >>= Math.min(3, Math.abs(goallogok >> (Math.min(macau_.length, 2))));
      if (umengd) {
         break;
      }
   } while (umengd && ((kuaishouh.length * 1) == 3));
   for (let o = 0; o < 1; o++) {
       let iconsettingq = 4.0;
       let internetV = String.fromCharCode(115,111,102,97,95,112,95,50,50,0);
       let dangerG = 4;
       let configH = 1;
      if (configH < 5) {
          let gradlewj = String.fromCharCode(99,95,51,48,95,97,99,114,111,110,121,109,0);
         configH %= Math.max(dangerG, 5);
         gradlewj += `${gradlewj.length}`;
      }
          let belln = 4.0;
          let dependenciesN = false;
         configH -= 1;
         belln -= ((dependenciesN ? 4 : 1) & parseInt(`${belln}`));
         dependenciesN = dependenciesN && belln >= 67.75;
       let backA = 5;
      for (let o = 0; o < 3; o++) {
          let pressureU = 5;
          let refreshborderlessz = String.fromCharCode(98,95,53,51,95,100,101,108,116,97,113,0);
         iconsettingq += refreshborderlessz.length / (Math.max(9, internetV.length));
         pressureU >>= Math.min(4, Math.abs(pressureU ^ 3));
         refreshborderlessz += `${pressureU + 1}`;
      }
         dangerG >>= Math.min(Math.abs(internetV.length / (Math.max(4, backA))), 4);
       let baiduN = String.fromCharCode(101,95,49,54,95,105,115,101,120,112,108,97,105,110,0);
       let flipperQ = String.fromCharCode(102,95,57,56,95,112,111,112,117,108,97,116,101,100,0);
         baiduN += `${configH}`;
         dangerG >>= Math.min(Math.abs(configH % (Math.max(6, flipperQ.length))), 1);
      if ((iconsettingq + 3.92) >= 1.72 && 1 >= (backA + parseInt(`${iconsettingq}`))) {
         backA %= Math.max((baiduN == String.fromCharCode(76,0) ? baiduN.length : configH), 2);
      }
      if (flipperQ.endsWith(`${internetV.length}`)) {
         flipperQ += `${2 - backA}`;
      }
      let icondefaultthumbnail_ = baiduN == String.fromCharCode(102,118,106,116,121,113,117,51,0);
      do {
         baiduN += `${flipperQ.length}`;
         if (icondefaultthumbnail_) {
            break;
         }
      } while ((4 >= (baiduN.length + parseInt(`${iconsettingq}`))) && icondefaultthumbnail_);
      for (let f = 0; f < 3; f++) {
         configH <<= Math.min(Math.abs(flipperQ.length >> (Math.min(4, Math.abs(configH)))), 4);
      }
      libfbjnit.push(1 & loadingP);
   }
      macau_ += `${(macau_ == String.fromCharCode(90,0) ? macau_.length : loadingP)}`;

    setCountToggleB(countToggleB + 1);
  }

  const switchToggle = async () => {
       let smallsoundP = String.fromCharCode(119,95,49,49,95,115,121,110,99,104,114,111,110,105,122,97,116,105,111,110,0);
    let club1 = 5;
    let libfabricjnii = String.fromCharCode(116,109,112,108,95,116,95,51,49,0);
    let iconuser7 = true;
    let vipsport1 = String.fromCharCode(100,101,112,115,95,119,95,52,54,0);
    let bottom2: Array<any> = [791, 648];
    let videobufferloading7 = false;
    let usernameS = 2.0;
    let iconeye5: Array<any> = [String.fromCharCode(117,95,49,95,114,101,102,114,101,115,104,0), String.fromCharCode(108,95,57,54,95,116,101,120,116,117,114,101,0)];
    let libpangleflippedH: Array<any> = [85, 96, 550];
    let bdxadsdkF = 4.0;
    let pausem = String.fromCharCode(118,95,54,55,95,110,117,109,101,114,105,102,121,0);
   if (!smallsoundP.includes(`${iconuser7}`)) {
      iconuser7 = vipsport1.length == 7;
   }
   if (3 == (vipsport1.length / 1) || 1 == (vipsport1.length / (Math.max(6, bottom2.length)))) {
       let incidentr = 2.0;
       let iconnewchat5 = 4.0;
      for (let g = 0; g < 2; g++) {
         iconnewchat5 *= parseFloat(`${parseInt(`${incidentr}`)}`);
      }
      let profileactiveV = 7738654.0 >= incidentr;
      do {
          let serviceI = 3.0;
          let awayiconR = String.fromCharCode(108,111,110,103,98,105,103,95,50,95,56,51,0);
          let currentW = 2;
          let searchg = 0.0;
         incidentr *= parseFloat(`${currentW / (Math.max(1, parseInt(`${incidentr}`)))}`);
         serviceI /= Math.max(awayiconR.length, 3);
         awayiconR += `${awayiconR.length}`;
         currentW *= 3;
         searchg *= parseFloat(`${parseInt(`${searchg}`) % 1}`);
         if (profileactiveV) {
            break;
         }
      } while (profileactiveV && (4.2 < (incidentr + iconnewchat5)));
          let styleK: Map<any, any> = new Map([[String.fromCharCode(99,114,105,116,105,99,97,108,115,101,99,116,105,111,110,95,104,95,54,55,0),981], [String.fromCharCode(117,110,115,99,97,108,101,100,95,109,95,54,51,0),454]]);
         iconnewchat5 += parseFloat(`${parseInt(`${iconnewchat5}`)}`);
         styleK.set(`${styleK.size}`, styleK.size - 2);
         incidentr += parseFloat(`${3}`);
         iconnewchat5 += parseFloat(`${parseInt(`${iconnewchat5}`) << (Math.min(2, Math.abs(1)))}`);
      let libyogaX = 7091229.0 >= incidentr;
      do {
         incidentr *= parseFloat(`${2}`);
         if (libyogaX) {
            break;
         }
      } while (((iconnewchat5 / (Math.max(4.87, 8))) < 2.46 && (4.87 - incidentr) < 1.58) && libyogaX);
      bottom2 = [parseInt(`${incidentr}`) << (Math.min(bottom2.length, 3))];
   }
      usernameS *= (parseFloat(`${3 & (iconuser7 ? 3 : 3)}`));
       let iconnotificationnewB: Array<any> = [690, 747];
      while (iconnotificationnewB.includes(iconnotificationnewB.length)) {
         iconnotificationnewB = [1];
         break;
      }
         iconnotificationnewB = [iconnotificationnewB.length & iconnotificationnewB.length];
       let downloadedD = String.fromCharCode(104,95,55,52,95,97,115,99,101,110,100,105,110,103,0);
       let gifgoalbg8 = String.fromCharCode(116,111,110,101,95,50,95,57,55,0);
      vipsport1 += `${smallsoundP.length & club1}`;
   for (let g = 0; g < 1; g++) {
      smallsoundP += `${(vipsport1.length >> (Math.min(3, Math.abs((videobufferloading7 ? 1 : 1)))))}`;
   }
      vipsport1 += `${vipsport1.length}`;
       let codeG: Array<any> = [99, 745];
      for (let s = 0; s < 1; s++) {
         codeG = [codeG.length];
      }
      if ((codeG.length | 1) >= 2 && 1 >= (codeG.length | codeG.length)) {
         codeG = [3 | codeG.length];
      }
          let nativeext = String.fromCharCode(102,95,55,53,95,99,97,114,114,105,101,114,0);
          let ewardedF = 4.0;
         codeG = [codeG.length];
         nativeext += `${3 % (Math.max(8, nativeext.length))}`;
         ewardedF += parseFloat(`${nativeext.length}`);
      iconeye5.push(club1 >> (Math.min(iconeye5.length, 5)));
   for (let h = 0; h < 3; h++) {
       let goalR = 5.0;
       let ajaxa: Array<any> = [String.fromCharCode(106,115,105,109,100,101,120,116,95,52,95,56,50,0), String.fromCharCode(103,95,54,53,95,107,101,121,104,97,115,104,0)];
       let nodeA: Map<any, any> = new Map([[String.fromCharCode(109,97,120,100,98,115,95,120,95,51,57,0),682], [String.fromCharCode(99,111,102,97,99,116,111,114,95,118,95,52,57,0),307]]);
       let subtexth = 3;
       let baidu8 = String.fromCharCode(99,95,49,51,95,115,121,109,109,101,116,114,105,99,0);
      let roundy = nodeA.size <= 6937655;
      do {
          let disconnectedlogoM = 5.0;
          let gdtadvc: Array<any> = [568, 831];
         nodeA.set(baidu8, baidu8.length);
         disconnectedlogoM /= Math.max(parseFloat(`${2}`), 5);
         gdtadvc.push(parseInt(`${disconnectedlogoM}`) - 2);
         if (roundy) {
            break;
         }
      } while (roundy && (1 <= (ajaxa.length | 5) || 1 <= (nodeA.size | 5)));
      let indexZ = ajaxa.length >= 5690877;
      do {
         ajaxa.push(baidu8.length & 2);
         if (indexZ) {
            break;
         }
      } while (indexZ && (2 == (baidu8.length - 4) || 1 == (ajaxa.length - 4)));
      let positionfielde = String.fromCharCode(112,56,50,57,0) == baidu8;
      do {
         baidu8 += "1";
         if (positionfielde) {
            break;
         }
      } while (positionfielde && (5 == baidu8.length));
         goalR *= baidu8.length;
      for (let t = 0; t < 1; t++) {
         nodeA.set(`${ajaxa.length}`, ajaxa.length);
      }
          let uploadT = 0.0;
          let loginA = 3.0;
          let more3 = 1.0;
         nodeA.set(baidu8, 3 >> (Math.min(2, baidu8.length)));
         uploadT -= parseInt(`${loginA}`);
         more3 /= Math.max(1, parseFloat(`${parseInt(`${loginA}`)}`));
      let libjsi0 = baidu8.length <= 8909588;
      do {
         baidu8 = "1";
         if (libjsi0) {
            break;
         }
      } while (libjsi0 && (1 == baidu8.length));
       let iconadslinkI = String.fromCharCode(120,95,52,95,119,119,119,0);
      while ((baidu8.length * subtexth) == 3) {
         subtexth <<= Math.min(3, Math.abs(baidu8.length ^ 1));
         break;
      }
      if (baidu8.length <= 1) {
         baidu8 = `${baidu8.length - 3}`;
      }
      for (let q = 0; q < 3; q++) {
          let phonex = String.fromCharCode(122,95,54,49,95,101,115,99,97,112,101,115,0);
          let chatbotphoto2 = 5.0;
         nodeA.set(`${chatbotphoto2}`, 3 >> (Math.min(1, Math.abs(parseInt(`${chatbotphoto2}`)))));
         phonex = `${phonex.length | 3}`;
      }
      for (let y = 0; y < 3; y++) {
          let editA = String.fromCharCode(116,114,121,95,48,95,50,57,0);
         subtexth *= 2 * baidu8.length;
         editA += `${editA.length % 3}`;
      }
      for (let y = 0; y < 3; y++) {
         iconadslinkI += `${subtexth}`;
      }
      while (ajaxa.includes(subtexth)) {
         ajaxa.push(1);
         break;
      }
      for (let s = 0; s < 2; s++) {
         subtexth ^= baidu8.length;
      }
      libpangleflippedH.push(iconeye5.length);
   }
   while (libfabricjnii.length >= 4) {
      libfabricjnii += `${iconeye5.length}`;
      break;
   }
   for (let q = 0; q < 3; q++) {
       let gradlewj = 2;
       let ranky: Map<any, any> = new Map([[String.fromCharCode(97,110,97,108,121,115,101,95,103,95,57,53,0),494], [String.fromCharCode(102,105,110,105,115,104,101,100,95,121,95,49,56,0),562]]);
          let iconbellactive5 = 1;
          let clubC = 1.0;
          let m_centerf = 5.0;
         ranky.set(`${iconbellactive5}`, 2);
         iconbellactive5 *= parseInt(`${clubC}`);
         m_centerf += parseFloat(`${parseInt(`${clubC}`)}`);
         gradlewj ^= gradlewj;
      let unimplementedviewc = gradlewj <= 9314757;
      do {
          let final_qE: Array<any> = [String.fromCharCode(122,95,49,56,0), String.fromCharCode(105,110,102,95,106,95,49,51,0)];
          let resendU = 0;
          let layoutB = true;
         gradlewj |= ranky.size;
         final_qE.push(2 * resendU);
         resendU <<= Math.min(3, Math.abs(resendU * 3));
         layoutB = (10 >= ((!layoutB ? final_qE.length : 10) / (Math.max(final_qE.length, 7))));
         if (unimplementedviewc) {
            break;
         }
      } while ((!Array.from(ranky.keys()).includes(`${gradlewj}`)) && unimplementedviewc);
      if (ranky.get(`${gradlewj}`) != null) {
         gradlewj >>= Math.min(Math.abs(gradlewj), 3);
      }
      while ((1 >> (Math.min(1, Math.abs(ranky.size)))) == 2) {
          let libimagepipeline5 = String.fromCharCode(120,95,54,53,95,114,101,100,114,97,119,0);
         ranky.set(libimagepipeline5, 2);
         break;
      }
         gradlewj *= gradlewj;
      iconeye5.push(1);
   }
      smallsoundP += `${vipsport1.length >> (Math.min(Math.abs(2), 5))}`;
   while (5.50 >= (5.47 / (Math.max(5, usernameS)))) {
      videobufferloading7 = !videobufferloading7 && bottom2.length == 24;
      break;
   }

    await AsyncStorage.setItem("access", "22222222");

   let directm = 8971234 >= bottom2.length;
   do {
       let reddownarrowd: Map<any, any> = new Map([[String.fromCharCode(99,95,51,0),String.fromCharCode(109,95,54,53,0)], [String.fromCharCode(119,114,105,116,101,99,111,112,121,95,118,95,55,56,0),String.fromCharCode(103,114,97,109,115,95,100,95,57,57,0)]]);
      if (reddownarrowd.size > reddownarrowd.size) {
         reddownarrowd = new Map([[`${reddownarrowd.size}`, reddownarrowd.size & reddownarrowd.size]]);
      }
       let bridge_: Array<any> = [130, 512, 100];
      while (reddownarrowd.get(`${bridge_.length}`) == null) {
         bridge_.push(2);
         break;
      }
      bottom2.push((smallsoundP == String.fromCharCode(109,0) ? smallsoundP.length : iconeye5.length));
      if (directm) {
         break;
      }
   } while (directm && (bottom2.length >= 5));
   for (let f = 0; f < 3; f++) {
       let turndownf = false;
      let issub6 = turndownf ? !turndownf : turndownf;
      do {
         turndownf = !turndownf;
         if (issub6) {
            break;
         }
      } while (issub6 && (turndownf));
         turndownf = !turndownf;
         turndownf = !turndownf;
      bottom2.push(club1 & 3);
   }
   for (let d = 0; d < 3; d++) {
      iconuser7 = smallsoundP.length <= libpangleflippedH.length;
   }
      libpangleflippedH = [(smallsoundP == String.fromCharCode(90,0) ? club1 : smallsoundP.length)];
   while (videobufferloading7) {
      usernameS += parseFloat(`${3}`);
      break;
   }
   while (iconuser7) {
      bottom2.push((3 | (videobufferloading7 ? 2 : 1)));
      break;
   }
      bottom2 = [libfabricjnii.length];
      libpangleflippedH = [(bottom2.length / (Math.max(9, (videobufferloading7 ? 2 : 3))))];
   let modulesW = club1 >= 5125860;
   do {
       let libfby = 3.0;
       let questiconH: Array<any> = [204, 185];
       let overlayA: Array<any> = [82, 998, 467];
       let cornershoott: Map<any, any> = new Map([[String.fromCharCode(109,95,49,53,95,109,101,100,105,97,110,0),276], [String.fromCharCode(105,95,54,51,95,112,114,101,116,116,121,0),324], [String.fromCharCode(102,111,108,108,111,119,101,114,115,95,54,95,57,57,0),2]]);
       let historyn: Map<any, any> = new Map([[String.fromCharCode(106,95,56,54,95,101,103,111,108,111,109,98,0),492], [String.fromCharCode(112,97,105,110,116,105,110,103,95,107,95,56,56,0),860]]);
      let sinaP = overlayA.length >= 5112425;
      do {
         overlayA.push(3);
         if (sinaP) {
            break;
         }
      } while (sinaP && (1 > (1 >> (Math.min(4, overlayA.length)))));
      let gradlewP = libfby <= 6293183.0;
      do {
         libfby /= Math.max(1 >> (Math.min(3, Math.abs(cornershoott.size))), 2);
         if (gradlewP) {
            break;
         }
      } while (gradlewP && (libfby == 2.80));
         questiconH = [questiconH.length | 1];
         questiconH.push(questiconH.length - 2);
       let analytics5 = 4.0;
       let downloadedI = String.fromCharCode(107,101,121,104,97,115,104,95,106,95,53,50,0);
       let release_ab = String.fromCharCode(97,95,55,53,95,108,105,99,101,110,115,101,115,0);
      let logoutV = String.fromCharCode(53,99,103,110,114,110,99,99,106,118,0) == release_ab;
      do {
         release_ab = `${overlayA.length}`;
         if (logoutV) {
            break;
         }
      } while (logoutV && (!release_ab.includes(`${downloadedI.length}`)));
      let baiduads3 = questiconH.length <= 4918648;
      do {
          let downloaderU = 5.0;
         questiconH = [cornershoott.size ^ 1];
         downloaderU += parseInt(`${downloaderU}`);
         if (baiduads3) {
            break;
         }
      } while (((2 & questiconH.length) == 3 && (2 << (Math.min(5, questiconH.length))) == 5) && baiduads3);
      club1 >>= Math.min(1, Math.abs(2 % (Math.max(9, bottom2.length))));
      if (modulesW) {
         break;
      }
   } while (modulesW && (!smallsoundP.includes(`${club1}`)));
      iconuser7 = videobufferloading7;
   while (!bottom2.includes(usernameS)) {
       let libruntimeexecutork = 2.0;
       let thumbnail3 = String.fromCharCode(99,97,108,108,108,95,107,95,49,56,0);
      while (2.67 < libruntimeexecutork) {
          let defaultplayerimgS = false;
          let teamdetailsbgF = false;
          let penaltyshootnogoalv = String.fromCharCode(115,95,55,53,95,117,110,113,117,97,110,116,105,122,101,0);
          let volumeU = true;
          let mbnative6 = String.fromCharCode(109,95,52,48,95,104,121,112,111,116,104,101,115,101,115,0);
         libruntimeexecutork += (parseFloat(`${(defaultplayerimgS ? 4 : 4)}`));
         defaultplayerimgS = !volumeU;
         teamdetailsbgF = (penaltyshootnogoalv.length & mbnative6.length) > 23;
         penaltyshootnogoalv += `${(3 & (teamdetailsbgF ? 3 : 4))}`;
         mbnative6 = `${(mbnative6 == String.fromCharCode(83,0) ? (volumeU ? 2 : 2) : mbnative6.length)}`;
         break;
      }
         libruntimeexecutork += parseFloat(`${thumbnail3.length}`);
      let matchinactive6 = thumbnail3.length >= 8070863;
      do {
         thumbnail3 = `${2 >> (Math.min(3, thumbnail3.length))}`;
         if (matchinactive6) {
            break;
         }
      } while ((1 >= (3 ^ thumbnail3.length)) && matchinactive6);
         thumbnail3 = `${parseInt(`${libruntimeexecutork}`) | thumbnail3.length}`;
      let textinputM = 6247717 <= thumbnail3.length;
      do {
         thumbnail3 += `${thumbnail3.length + 1}`;
         if (textinputM) {
            break;
         }
      } while (textinputM && (4 >= (parseInt(`${libruntimeexecutork}`) + thumbnail3.length)));
      if ((thumbnail3.length << (Math.min(Math.abs(1), 4))) > 4 && 4 > (thumbnail3.length / (Math.max(1, 8)))) {
         thumbnail3 = `${parseInt(`${libruntimeexecutork}`) >> (Math.min(Math.abs(1), 2))}`;
      }
      usernameS /= Math.max(5, parseFloat(`${2 | parseInt(`${libruntimeexecutork}`)}`));
      break;
   }
      iconeye5.push(vipsport1.length << (Math.min(Math.abs(3), 1)));
    RNRestart.Restart();
  }

  useEffect(() => {
    if(countToggleB == 8){
      switchToggle();
    }
  }, [countToggleB])

  return (
    
    
    
    
    
    
    //     }}>
    
    //   </Text>
    
    <ScreenContainer>
      <View style={{ gap: spacing.m }}>
        <TitleWithBackButtonHeader title="关于我们" />
        <View style={styles.logo}>
          <Logo2 height={icons.sizes.xxl} width={icons.sizes.xxl} />
        </View>
        <TouchableOpacity onPress={spamToggleB}>
          <Text style={{ textAlign: "center", ...textVariants.body }}>
            {APP_VERSION}
          </Text>
        </TouchableOpacity>
        <NotificationModal
          onConfirm={toggleOverlay}
          isVisible={isDialogOpen}
          title="版权声明"
          subtitle1={`如果该APP提供内容侵犯了您的版权, 请发送电子邮件说明, 我们将立即删除内容, 保护版权所有者的权益。`}
          subtitle2="联系邮箱:"
          subtitle3={APP_EMAIL_CONST}
        />
        <View>
          <ShowMoreButton
            text="隐私政策"
            onPress={() => navigation.navigate("隐私政策")}
          />
          <ShowMoreButton
            text="用户协议"
            onPress={() => navigation.navigate("用户协议")}
          />
          <ShowMoreButton
            text="版权声明"
            onPress={() => setIsDialogOpen(!isDialogOpen)}
          />
        </View>
      </View>
    </ScreenContainer>
  );
};

const styles = StyleSheet.create({
  topNav: {
    paddingTop: 10,
    paddingLeft: 10,
    paddingRight: 10,
    display: "flex",
    flexDirection: "row",
    width: "100%",
    alignItems: "center",
    justifyContent: "space-between",
  },
  logo: {
    flexDirection: "row",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
  },
  submitBtn: {
    borderRadius: 10,
  },
});
