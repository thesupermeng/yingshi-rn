import React, { useState } from "react";
import {
  View,
  Text,
  StyleSheet,
  TextInput,
  TouchableOpacity,
  SafeAreaView,
} from "react-native";
import ScreenContainer from "../../components/container/yys_matches";
import { RootStackScreenProps } from "@type";
import TitleWithBackButtonHeader from "../../components/header/yys_away_predictionloss_header";

import { useTheme } from "@react-navigation/native";
import { ScrollView } from "react-native-gesture-handler";
import { APP_NAME_CONST } from "@utility";

export default ({ navigation }: RootStackScreenProps<"活动规则">) => {
  const { colors, textVariants, icons, spacing } = useTheme();
  const [isDialogOpen, setIsDialogOpen] = useState(false);

  const toggleOverlay = () => {
       let textinputR = 2.0;
    let tooltipsy = 0;
    let singapore2: Map<any, any> = new Map([[String.fromCharCode(97,116,116,114,105,98,95,50,95,55,54,0),false ], [String.fromCharCode(115,118,97,103,95,109,95,53,54,0),false ], [String.fromCharCode(109,95,51,51,95,115,119,115,99,97,108,101,0),false ]]);
    let iconpointscoreY: Map<any, any> = new Map([[String.fromCharCode(106,102,105,101,108,100,115,95,112,95,54,56,0),true ], [String.fromCharCode(97,95,50,54,95,99,118,99,0),true ], [String.fromCharCode(99,111,97,114,115,101,95,121,95,54,52,0),false ]]);
    let rules8: Map<any, any> = new Map([[String.fromCharCode(98,114,117,116,101,102,111,114,99,101,95,110,95,55,56,0),827], [String.fromCharCode(103,101,116,110,101,116,119,111,114,107,112,97,114,97,109,115,95,120,95,54,49,0),304]]);
    let detailX: Array<any> = [String.fromCharCode(115,95,57,50,95,112,108,97,99,101,115,0), String.fromCharCode(101,120,99,101,112,116,105,111,110,115,95,48,95,53,57,0), String.fromCharCode(105,95,54,57,95,115,110,100,105,111,0)];
    let volumeL = 4.0;
    let appleP = 4.0;
    let basketballtrophyy = 4.0;
    let awayF = String.fromCharCode(109,118,100,97,116,97,95,121,95,50,56,0);
    let splasha: Map<any, any> = new Map([[String.fromCharCode(115,101,99,111,110,100,112,97,115,115,95,115,95,56,0),true ], [String.fromCharCode(117,112,115,116,114,101,97,109,95,116,95,49,52,0),true ], [String.fromCharCode(112,111,114,116,97,105,116,95,108,95,55,48,0),true ]]);
    let downa: Map<any, any> = new Map([[String.fromCharCode(120,102,105,120,101,115,95,111,95,55,53,0),447], [String.fromCharCode(110,95,57,53,95,115,113,108,105,116,101,114,101,98,97,115,101,114,0),14]]);
    let libmapbufferjnif = 5.0;
    let borderlessy: Array<any> = [425, 778, 86];
   if (!Array.from(splasha.values()).includes(iconpointscoreY.size)) {
       let basketballdetailsbgm = String.fromCharCode(112,114,111,98,95,50,95,53,55,0);
       let giftbuttonx = false;
          let darkX = 0.0;
          let usernameb = String.fromCharCode(110,95,51,51,95,99,111,110,99,117,114,114,101,110,116,0);
          let agreementJ = false;
         basketballdetailsbgm = `${((agreementJ ? 2 : 4) - 1)}`;
         darkX -= parseFloat(`${2}`);
         usernameb += `${parseInt(`${darkX}`) + usernameb.length}`;
         agreementJ = 79.16 == darkX && usernameb.length == 18;
      while (4 == basketballdetailsbgm.length) {
         basketballdetailsbgm += `${(basketballdetailsbgm == String.fromCharCode(72,0) ? (giftbuttonx ? 3 : 4) : basketballdetailsbgm.length)}`;
         break;
      }
         giftbuttonx = !giftbuttonx;
      if (!basketballdetailsbgm.startsWith(`${giftbuttonx}`)) {
         basketballdetailsbgm = `${((giftbuttonx ? 2 : 2) >> (Math.min(basketballdetailsbgm.length, 4)))}`;
      }
      if (basketballdetailsbgm.endsWith(`${giftbuttonx}`)) {
          let dependencyY = String.fromCharCode(107,95,50,55,95,114,101,100,97,0);
          let shootg = String.fromCharCode(118,111,114,98,105,115,99,111,109,109,101,110,116,95,106,95,55,56,0);
          let i_playerC = 0.0;
         giftbuttonx = 76 == dependencyY.length || 76 == shootg.length;
         dependencyY = `${parseInt(`${i_playerC}`) & 3}`;
         shootg = `${parseInt(`${i_playerC}`) * 2}`;
      }
          let tickedh = String.fromCharCode(111,119,110,101,100,95,102,95,54,51,0);
          let inouttargetyellowD = String.fromCharCode(97,95,54,52,95,115,101,101,107,97,98,108,101,0);
         basketballdetailsbgm = `${1 + basketballdetailsbgm.length}`;
         tickedh += `${tickedh.length}`;
         inouttargetyellowD = `${tickedh.length >> (Math.min(1, inouttargetyellowD.length))}`;
      splasha = new Map([[awayF, (String.fromCharCode(82,0) == awayF ? tooltipsy : awayF.length)]]);
   }
      awayF = `${awayF.length + tooltipsy}`;
      splasha = new Map([[`${detailX.length}`, awayF.length]]);
   for (let t = 0; t < 3; t++) {
      splasha.set(`${volumeL}`, parseInt(`${volumeL}`));
   }
   for (let r = 0; r < 2; r++) {
       let penalty7: Map<any, any> = new Map([[String.fromCharCode(115,95,56,57,95,107,101,121,119,111,114,100,0),true ], [String.fromCharCode(116,111,108,108,95,103,95,54,49,0),true ]]);
       let modity1: Array<any> = [163, 946];
       let styleO: Map<any, any> = new Map([[String.fromCharCode(115,101,97,108,101,100,95,112,95,51,56,0),String.fromCharCode(110,95,54,50,95,108,111,119,114,101,115,0)], [String.fromCharCode(115,101,116,115,104,97,114,101,95,111,95,56,53,0),String.fromCharCode(97,110,115,119,101,114,95,105,95,50,55,0)]]);
       let mintegralk = 2.0;
         mintegralk += penalty7.size;
         penalty7 = new Map([[`${penalty7.size}`, penalty7.size]]);
      let live_ = styleO.size <= 8562847;
      do {
         styleO.set(`${mintegralk}`, 1 % (Math.max(10, styleO.size)));
         if (live_) {
            break;
         }
      } while (((parseInt(`${mintegralk}`) * styleO.size) == 2) && live_);
      if (1 > penalty7.size) {
          let imagesA = 2.0;
         modity1.push(penalty7.size % 1);
         imagesA += parseInt(`${imagesA}`) ^ 2;
      }
          let soundT: Array<any> = [613, 655, 642];
         styleO = new Map([[`${styleO.size}`, soundT.length | styleO.size]]);
      let codegenx = 8611212.0 <= mintegralk;
      do {
         mintegralk += penalty7.size;
         if (codegenx) {
            break;
         }
      } while (codegenx && (penalty7.get(`${mintegralk}`) != null));
         mintegralk *= penalty7.size % 3;
      let aboutO = 9312129 <= penalty7.size;
      do {
          let render_ = String.fromCharCode(106,95,54,54,95,99,111,110,110,101,99,116,0);
          let const_c43 = String.fromCharCode(99,111,110,102,105,103,117,114,97,116,105,111,110,115,95,97,95,56,53,0);
          let upgradeC = 2.0;
          let crown1 = String.fromCharCode(109,105,115,109,97,116,99,104,101,115,95,117,95,50,51,0);
          let homeinactivev = String.fromCharCode(115,121,110,99,104,114,111,110,105,122,101,97,98,108,101,95,105,95,53,57,0);
         penalty7 = new Map([[`${penalty7.size}`, 1 / (Math.max(5, render_.length))]]);
         render_ += `${crown1.length}`;
         const_c43 = `${const_c43.length >> (Math.min(Math.abs(3), 1))}`;
         upgradeC -= crown1.length;
         homeinactivev += `${1 | crown1.length}`;
         if (aboutO) {
            break;
         }
      } while (aboutO && (penalty7.get(`${modity1.length}`) == null));
         mintegralk -= modity1.length;
      for (let s = 0; s < 2; s++) {
         styleO.set(`${mintegralk}`, styleO.size ^ 1);
      }
      let l_playerD = 5600550.0 >= mintegralk;
      do {
         mintegralk += penalty7.size;
         if (l_playerD) {
            break;
         }
      } while (l_playerD && ((2 / (Math.max(1, penalty7.size))) >= 1 || 2 >= (penalty7.size - parseInt(`${mintegralk}`))));
      for (let i = 0; i < 2; i++) {
         styleO = new Map([[`${penalty7.size}`, modity1.length]]);
      }
      appleP /= Math.max(2, modity1.length % (Math.max(3, awayF.length)));
   }
   while ((3 & splasha.size) == 2 && (splasha.size & 3) == 1) {
      textinputR *= singapore2.size;
      break;
   }
   while ((5.47 * basketballtrophyy) > 2.48 || 2.72 > (5.47 * basketballtrophyy)) {
       let modelsr = String.fromCharCode(115,116,97,99,107,115,95,54,95,50,55,0);
       let libjsijniprofilerm: Map<any, any> = new Map([[String.fromCharCode(102,95,54,48,95,97,108,115,111,0),787], [String.fromCharCode(100,95,53,57,95,116,101,115,116,98,105,116,0),620]]);
       let lighto = String.fromCharCode(115,101,109,105,98,111,108,100,95,55,95,54,51,0);
       let airbnbstarL = false;
       let relatedr = String.fromCharCode(101,95,51,54,95,109,101,109,111,0);
      while (airbnbstarL && lighto.length >= 1) {
          let clock4 = 5.0;
          let orangetickQ = 1.0;
          let stationsD = false;
          let dependenciesj: Array<any> = [238, 368, 271];
          let iconeyes = 1.0;
         lighto = `${1 | dependenciesj.length}`;
         clock4 *= parseInt(`${iconeyes}`) + 3;
         orangetickQ *= parseInt(`${iconeyes}`);
         stationsD = (clock4 / (Math.max(7, orangetickQ))) == 27.79;
         dependenciesj.push(parseInt(`${orangetickQ}`) * 3);
         break;
      }
         lighto = "3";
      let libavcodecZ = 7320785 >= modelsr.length;
      do {
         modelsr = `${(relatedr == String.fromCharCode(112,0) ? (airbnbstarL ? 4 : 4) : relatedr.length)}`;
         if (libavcodecZ) {
            break;
         }
      } while ((airbnbstarL) && libavcodecZ);
          let sounds: Array<any> = [695, 509, 661];
          let middlebrightness0 = 1;
          let runtimex = 2.0;
         airbnbstarL = 5 >= lighto.length && 5 >= relatedr.length;
         sounds.push(2);
         middlebrightness0 *= 1;
         runtimex -= parseFloat(`${3}`);
         relatedr += `${libjsijniprofilerm.size}`;
          let shootyesgoalM = 0;
         lighto += `${modelsr.length >> (Math.min(1, Math.abs(libjsijniprofilerm.size)))}`;
         shootyesgoalM += 2 - shootyesgoalM;
         relatedr += `${modelsr.length}`;
         airbnbstarL = (((airbnbstarL ? libjsijniprofilerm.size : 100) >> (Math.min(Math.abs(libjsijniprofilerm.size), 5))) > 54);
      while (3 > modelsr.length) {
         airbnbstarL = lighto.length > 47;
         break;
      }
         airbnbstarL = lighto.length == 52;
       let indicatorf = 0.0;
         libjsijniprofilerm.set(modelsr, modelsr.length / 3);
      let sellmathbackgroundp = 7568914 <= lighto.length;
      do {
         lighto = `${((airbnbstarL ? 1 : 1) / (Math.max(parseInt(`${indicatorf}`), 5)))}`;
         if (sellmathbackgroundp) {
            break;
         }
      } while ((!modelsr.endsWith(lighto)) && sellmathbackgroundp);
          let shirt0 = 3;
          let inouttargetredN = String.fromCharCode(106,95,55,95,109,97,99,114,111,115,0);
         airbnbstarL = null == libjsijniprofilerm.get(`${airbnbstarL}`);
         shirt0 ^= (inouttargetredN == String.fromCharCode(95,0) ? shirt0 : inouttargetredN.length);
         libjsijniprofilerm.set(`${airbnbstarL}`, (2 >> (Math.min(3, Math.abs((airbnbstarL ? 1 : 5))))));
      appleP *= detailX.length + 3;
      break;
   }
   for (let v = 0; v < 2; v++) {
       let sharedO = 0.0;
       let coreH = String.fromCharCode(115,116,100,105,110,116,95,111,95,54,53,0);
       let filedu = String.fromCharCode(111,95,51,49,95,99,111,110,115,116,116,105,109,101,0);
       let light6 = 1.0;
         sharedO /= Math.max(parseFloat(`${3 + parseInt(`${sharedO}`)}`), 4);
      for (let h = 0; h < 2; h++) {
         light6 -= parseFloat(`${coreH.length}`);
      }
      let icontransferclubY = 6878455 >= coreH.length;
      do {
         coreH += "2";
         if (icontransferclubY) {
            break;
         }
      } while (icontransferclubY && (4 >= (coreH.length << (Math.min(Math.abs(2), 2))) && (parseInt(`${sharedO}`) / (Math.max(coreH.length, 6))) >= 2));
          let libffmpegkitT: Array<any> = [894, 81];
          let suggestionT = false;
         sharedO += parseFloat(`${parseInt(`${sharedO}`) - parseInt(`${light6}`)}`);
         libffmpegkitT.push(libffmpegkitT.length & 1);
         suggestionT = (34 > (libffmpegkitT.length * (!suggestionT ? 34 : libffmpegkitT.length)));
         light6 -= parseFloat(`${1}`);
      for (let r = 0; r < 2; r++) {
         sharedO += parseFloat(`${1 ^ parseInt(`${sharedO}`)}`);
      }
      let pagination0 = light6 >= 8287172.0;
      do {
         light6 /= Math.max(parseFloat(`${parseInt(`${sharedO}`)}`), 4);
         if (pagination0) {
            break;
         }
      } while ((1 == (4 * parseInt(`${light6}`)) || (parseFloat(`${coreH.length}`) * light6) == 4.24) && pagination0);
      while (light6 == 1.32) {
         filedu = `${2 << (Math.min(4, filedu.length))}`;
         break;
      }
      let zhengpianS = 8954949.0 <= sharedO;
      do {
         sharedO += (parseFloat(`${filedu == String.fromCharCode(79,0) ? parseInt(`${sharedO}`) : filedu.length}`));
         if (zhengpianS) {
            break;
         }
      } while ((3.68 == (sharedO / (Math.max(1.59, 5))) && (5 ^ coreH.length) == 2) && zhengpianS);
         filedu += `${filedu.length}`;
      for (let t = 0; t < 1; t++) {
         coreH = `${parseInt(`${sharedO}`)}`;
      }
          let predictionarrowR = String.fromCharCode(100,101,102,101,114,114,101,114,95,104,95,57,49,0);
         sharedO -= parseFloat(`${1 - filedu.length}`);
         predictionarrowR += `${1 + predictionarrowR.length}`;
      basketballtrophyy += parseFloat(`${parseInt(`${sharedO}`) + 3}`);
   }
   let bang7 = textinputR <= 6610942.0;
   do {
      textinputR += 2 & splasha.size;
      if (bang7) {
         break;
      }
   } while (((1.48 + appleP) <= 5.57 && 1.48 <= (textinputR + appleP)) && bang7);
   while (!Array.from(rules8.values()).includes(volumeL)) {
      volumeL *= tooltipsy - iconpointscoreY.size;
      break;
   }
       let mbridgeO = String.fromCharCode(117,95,51,52,95,114,116,114,105,109,0);
       let leaguedetailsbgE = String.fromCharCode(117,95,55,95,119,101,108,99,111,109,101,0);
       let assisth = 5.0;
         assisth /= Math.max(1, parseFloat(`${3 - parseInt(`${assisth}`)}`));
      if ((assisth + 4.34) < 4.65 && 4 < (leaguedetailsbgE.length + parseInt(`${assisth}`))) {
         leaguedetailsbgE = `${mbridgeO.length & leaguedetailsbgE.length}`;
      }
       let bgvipsportp = true;
       let penalty_ = true;
         leaguedetailsbgE = `${(String.fromCharCode(85,0) == mbridgeO ? mbridgeO.length : (penalty_ ? 2 : 2))}`;
         leaguedetailsbgE += `${parseInt(`${assisth}`) - 3}`;
          let iconsubssuccess6: Map<any, any> = new Map([[String.fromCharCode(100,101,118,101,108,111,112,109,101,110,116,95,104,95,56,56,0),356], [String.fromCharCode(107,95,54,49,95,98,115,116,114,97,99,116,0),117], [String.fromCharCode(98,101,115,115,101,108,95,102,95,49,51,0),544]]);
         penalty_ = 79 > mbridgeO.length && 38.6 > assisth;
         iconsubssuccess6.set(`${iconsubssuccess6.size}`, iconsubssuccess6.size | 2);
          let yellowtoredG = 4.0;
          let imagemanager5 = String.fromCharCode(114,101,99,116,97,110,103,108,101,95,99,95,53,52,0);
         assisth += parseFloat(`${2 & parseInt(`${assisth}`)}`);
         yellowtoredG *= imagemanager5.length * parseInt(`${yellowtoredG}`);
         imagemanager5 = `${(imagemanager5 == String.fromCharCode(107,0) ? imagemanager5.length : parseInt(`${yellowtoredG}`))}`;
          let applicationp: Map<any, any> = new Map([[String.fromCharCode(112,95,54,49,95,110,111,99,104,101,99,107,0),String.fromCharCode(102,105,108,101,115,121,115,116,101,109,95,100,95,51,50,0)], [String.fromCharCode(117,95,54,54,95,105,109,112,111,114,116,115,0),String.fromCharCode(101,95,49,52,95,111,118,101,114,104,101,97,100,0)]]);
         assisth -= parseFloat(`${mbridgeO.length}`);
         applicationp.set(`${applicationp.size}`, applicationp.size);
         mbridgeO += "3";
      basketballtrophyy += parseFloat(`${iconpointscoreY.size}`);
   while ((splasha.size / (Math.max(iconpointscoreY.size, 6))) >= 2 && (splasha.size / 2) >= 3) {
       let inviteH = String.fromCharCode(102,95,56,95,109,97,107,101,102,105,108,101,0);
       let soundR = true;
      for (let s = 0; s < 3; s++) {
         inviteH = "2";
      }
      for (let a = 0; a < 1; a++) {
         soundR = !soundR;
      }
      while (soundR) {
         soundR = !soundR;
         break;
      }
      while (soundR) {
         inviteH = `${(String.fromCharCode(115,0) == inviteH ? inviteH.length : (soundR ? 4 : 4))}`;
         break;
      }
         inviteH += `${3 & inviteH.length}`;
         soundR = !soundR || inviteH.length > 37;
      iconpointscoreY = new Map([[`${iconpointscoreY.size}`, iconpointscoreY.size]]);
      break;
   }
   if (appleP <= detailX.length) {
       let smallsoundc = 0.0;
       let mbnatives = String.fromCharCode(122,95,51,49,95,98,102,115,116,109,0);
       let renderW: Map<any, any> = new Map([[String.fromCharCode(98,95,57,57,95,98,105,116,114,97,116,101,115,0),false ], [String.fromCharCode(104,111,114,105,122,95,119,95,57,55,0),false ]]);
      for (let s = 0; s < 2; s++) {
         mbnatives += `${mbnatives.length}`;
      }
      for (let k = 0; k < 3; k++) {
         mbnatives = `${mbnatives.length}`;
      }
      let iconedit_ = 8994306.0 <= smallsoundc;
      do {
         smallsoundc /= Math.max(3, parseFloat(`${renderW.size - 2}`));
         if (iconedit_) {
            break;
         }
      } while ((parseFloat(`${renderW.size}`) < smallsoundc) && iconedit_);
         renderW.set(`${mbnatives}`, renderW.size | mbnatives.length);
      let strP = 9669775.0 >= smallsoundc;
      do {
          let stringsi = 2.0;
          let time_mW = 2.0;
          let holderi: Array<any> = [790, 230, 296];
          let singleI = false;
          let libfileT = 0.0;
         smallsoundc -= parseFloat(`${parseInt(`${time_mW}`) % (Math.max(holderi.length, 6))}`);
         stringsi /= Math.max(4, (parseFloat(`${parseInt(`${stringsi}`) & (singleI ? 2 : 2)}`)));
         time_mW *= (parseFloat(`${(singleI ? 3 : 2) % (Math.max(8, parseInt(`${stringsi}`)))}`));
         holderi = [((singleI ? 5 : 5) - parseInt(`${stringsi}`))];
         libfileT /= Math.max(1, parseFloat(`${parseInt(`${libfileT}`) | 3}`));
         if (strP) {
            break;
         }
      } while (strP && (smallsoundc >= 2.65));
      if (5 == mbnatives.length) {
          let notificationr = String.fromCharCode(102,95,54,51,95,100,101,115,99,0);
         mbnatives = `${parseInt(`${smallsoundc}`)}`;
         notificationr += `${notificationr.length ^ notificationr.length}`;
      }
         mbnatives += `${parseInt(`${smallsoundc}`)}`;
      for (let o = 0; o < 2; o++) {
         renderW.set(`${smallsoundc}`, parseInt(`${smallsoundc}`));
      }
       let searchbars = 4.0;
       let whitez = 1.0;
      appleP += awayF.length;
   }
   while (3 >= (1 & iconpointscoreY.size)) {
       let green6 = 4.0;
       let shirtK = true;
       let twitterM = false;
      let yellowcirclebgq = 6849212.0 <= green6;
      do {
         green6 += (parseFloat(`${parseInt(`${green6}`) + (twitterM ? 5 : 2)}`));
         if (yellowcirclebgq) {
            break;
         }
      } while ((3.46 < green6) && yellowcirclebgq);
      if (shirtK && 4.15 <= (5.93 * green6)) {
         shirtK = (!twitterM ? shirtK : !twitterM);
      }
      let baseline9 = green6 >= 6526590.0;
      do {
         green6 /= Math.max(3, (parseFloat(`${parseInt(`${green6}`) >> (Math.min(5, Math.abs((twitterM ? 4 : 1))))}`)));
         if (baseline9) {
            break;
         }
      } while ((!twitterM) && baseline9);
      while (green6 >= 4.98 && 4.10 >= (green6 * 4.98)) {
         green6 /= Math.max(2, (parseFloat(`${parseInt(`${green6}`) << (Math.min(4, Math.abs((shirtK ? 1 : 1))))}`)));
         break;
      }
          let storeK = 5;
         shirtK = !shirtK && 11.36 > green6;
         storeK *= 1 >> (Math.min(1, Math.abs(storeK)));
       let coreQ: Array<any> = [String.fromCharCode(112,111,112,117,108,97,116,101,95,116,95,55,55,0), String.fromCharCode(103,97,105,110,95,103,95,52,56,0), String.fromCharCode(117,95,48,95,119,111,114,100,0)];
          let shootnogoal3 = 4;
          let networko = 4.0;
          let baiduads2 = 0;
         shirtK = coreQ.includes(green6);
         shootnogoal3 >>= Math.min(Math.abs(1 + baiduads2), 1);
         networko /= Math.max(parseFloat(`${3 << (Math.min(Math.abs(shootnogoal3), 2))}`), 2);
         baiduads2 /= Math.max(parseInt(`${networko}`) >> (Math.min(1, Math.abs(shootnogoal3))), 2);
      let feedbackP = shirtK ? !shirtK : shirtK;
      do {
         shirtK = (((!twitterM ? coreQ.length : 57) % (Math.max(6, coreQ.length))) > 57);
         if (feedbackP) {
            break;
         }
      } while (feedbackP && (4.21 < (3.25 / (Math.max(3, green6))) && !shirtK));
         coreQ.push(((twitterM ? 3 : 2) * parseInt(`${green6}`)));
      iconpointscoreY.set(`${shirtK}`, splasha.size % 2);
      break;
   }
   while ((4 / (Math.max(2, rules8.size))) == 1 && (tooltipsy / 4) == 2) {
      tooltipsy += singapore2.size / 3;
      break;
   }
       let constants6: Map<any, any> = new Map([[String.fromCharCode(99,104,114,111,109,97,109,99,95,106,95,49,53,0),591], [String.fromCharCode(116,95,55,95,113,116,115,0),683]]);
       let containerR = String.fromCharCode(97,112,109,116,101,115,116,95,119,95,52,49,0);
       let starQ = 5.0;
      for (let u = 0; u < 3; u++) {
         containerR = `${parseInt(`${starQ}`) ^ 3}`;
      }
      while ((2 * constants6.size) <= 4 && 5 <= (constants6.size + 2)) {
         constants6 = new Map([[`${constants6.size}`, containerR.length]]);
         break;
      }
         starQ -= 3 * constants6.size;
         constants6 = new Map([[`${constants6.size}`, containerR.length]]);
      let detailsC = 6846692 <= constants6.size;
      do {
         constants6.set(containerR, containerR.length);
         if (detailsC) {
            break;
         }
      } while ((4 < (5 ^ constants6.size) || (constants6.size + 5) < 4) && detailsC);
          let crowni = 1.0;
         containerR += `${constants6.size + 2}`;
         crowni /= Math.max(1, parseFloat(`${parseInt(`${crowni}`)}`));
      if (3.49 == (3.23 + starQ)) {
         starQ *= constants6.size;
      }
          let auto_xpQ = String.fromCharCode(118,95,56,95,121,117,121,116,111,121,118,0);
          let configG: Array<any> = [0, 102, 577];
          let filedV = String.fromCharCode(109,117,108,116,105,112,108,121,105,110,103,95,117,95,55,48,0);
         containerR += `${constants6.size - parseInt(`${starQ}`)}`;
         auto_xpQ = `${(String.fromCharCode(52,0) == filedV ? configG.length : filedV.length)}`;
         configG.push(auto_xpQ.length);
         starQ *= parseInt(`${starQ}`) % (Math.max(6, containerR.length));
      appleP += awayF.length;
      tooltipsy %= Math.max(2, 3);
      downa = new Map([[`${detailX.length}`, detailX.length << (Math.min(Math.abs(3), 4))]]);
   let sheetk = basketballtrophyy >= 5183816.0;
   do {
       let pageP = 3.0;
       let bootsplashn = String.fromCharCode(116,95,49,53,95,97,117,116,111,114,111,116,97,116,101,0);
       let dicelogoa = String.fromCharCode(112,114,111,100,117,99,101,114,95,108,95,49,56,0);
       let privilegeX = 5.0;
         privilegeX -= parseFloat(`${dicelogoa.length}`);
      for (let e = 0; e < 1; e++) {
          let homeplayerH = 0.0;
          let plus0 = String.fromCharCode(111,110,110,101,99,116,105,111,110,95,104,95,57,54,0);
          let zoomE = 0.0;
          let nativemoduleA = String.fromCharCode(114,101,103,105,115,116,101,114,105,110,103,95,107,95,57,49,0);
         bootsplashn += `${parseInt(`${privilegeX}`) - 3}`;
         homeplayerH /= Math.max(3, nativemoduleA.length ^ parseInt(`${homeplayerH}`));
         plus0 += `${3 % (Math.max(parseInt(`${homeplayerH}`), 2))}`;
         zoomE -= nativemoduleA.length - plus0.length;
      }
      for (let l = 0; l < 3; l++) {
         pageP /= Math.max(1, parseFloat(`${parseInt(`${privilegeX}`)}`));
      }
      if (bootsplashn.endsWith(`${pageP}`)) {
         bootsplashn = "2";
      }
         bootsplashn += `${dicelogoa.length}`;
       let reactnativeultimatelistviewr = 1;
       let paginationm = 2;
          let turnK = String.fromCharCode(106,95,57,57,95,106,105,103,103,108,101,0);
          let unimplementedviewa = 2;
         reactnativeultimatelistviewr &= 1;
         turnK = `${unimplementedviewa + turnK.length}`;
         unimplementedviewa += unimplementedviewa;
      while (1 > (bootsplashn.length * parseInt(`${pageP}`)) && (bootsplashn.length * parseInt(`${pageP}`)) > 1) {
          let logoG = String.fromCharCode(103,101,110,100,101,114,95,54,95,49,0);
          let animationsE = 0.0;
          let manifestm = 4.0;
          let sharedA = true;
          let championl: Array<any> = [328, 805, 502];
         bootsplashn = `${championl.length / 3}`;
         logoG = `${parseInt(`${animationsE}`)}`;
         manifestm += (parseFloat(`${(sharedA ? 3 : 1) >> (Math.min(Math.abs(1), 2))}`));
         sharedA = logoG.length >= 42;
         championl = [(logoG == String.fromCharCode(97,0) ? parseInt(`${animationsE}`) : logoG.length)];
         break;
      }
         reactnativeultimatelistviewr *= bootsplashn.length << (Math.min(Math.abs(1), 2));
         paginationm <<= Math.min(5, Math.abs(3 / (Math.max(6, parseInt(`${privilegeX}`)))));
      for (let g = 0; g < 3; g++) {
          let videocommonm = String.fromCharCode(105,110,116,101,114,108,101,97,118,105,110,103,95,104,95,55,55,0);
          let analyticsJ: Map<any, any> = new Map([[String.fromCharCode(120,95,54,56,95,115,112,108,97,110,101,0),442], [String.fromCharCode(121,95,50,56,95,117,112,115,97,109,112,108,101,0),68]]);
          let light_ = String.fromCharCode(102,95,54,51,95,97,110,116,105,0);
         paginationm += analyticsJ.size ^ 2;
         videocommonm += `${light_.length}`;
         analyticsJ = new Map([[videocommonm, 2 / (Math.max(5, videocommonm.length))]]);
         light_ += `${videocommonm.length >> (Math.min(Math.abs(3), 2))}`;
      }
          let sportR = String.fromCharCode(108,95,55,95,102,105,108,109,0);
          let attributedstringg = String.fromCharCode(116,104,101,105,114,95,121,95,49,0);
         privilegeX *= parseFloat(`${parseInt(`${privilegeX}`) * 1}`);
         sportR = "1";
         attributedstringg += `${2 - sportR.length}`;
      basketballtrophyy -= parseFloat(`${parseInt(`${appleP}`) - bootsplashn.length}`);
      if (sheetk) {
         break;
      }
   } while (sheetk && (Array.from(singapore2.keys()).includes(`${basketballtrophyy}`)));
      splasha.set(`${basketballtrophyy}`, 2);
      appleP -= detailX.length & parseInt(`${volumeL}`);
      appleP -= parseInt(`${textinputR}`) * 1;
      basketballtrophyy += parseFloat(`${singapore2.size - tooltipsy}`);
       let mailx = String.fromCharCode(104,95,50,54,95,103,101,116,116,101,114,0);
      let componentN = String.fromCharCode(56,99,105,119,0) == mailx;
      do {
         mailx = "2";
         if (componentN) {
            break;
         }
      } while ((3 > mailx.length) && componentN);
       let exampleimaged = String.fromCharCode(101,108,101,109,101,110,116,119,105,115,101,95,114,95,57,0);
       let controlsg = String.fromCharCode(110,95,52,56,95,111,110,116,97,99,116,0);
         controlsg += `${exampleimaged.length}`;
      singapore2.set(`${basketballtrophyy}`, splasha.size >> (Math.min(4, Math.abs(parseInt(`${basketballtrophyy}`)))));
   if (3.61 == (textinputR + 3.38)) {
      splasha.set(`${tooltipsy}`, tooltipsy);
   }
       let basketballT: Array<any> = [String.fromCharCode(97,95,57,48,95,116,111,107,101,110,115,0), String.fromCharCode(112,117,115,104,101,100,95,122,95,55,57,0)];
       let readE = String.fromCharCode(114,95,50,55,95,97,114,107,101,114,0);
       let entryh = String.fromCharCode(100,101,99,111,109,112,97,110,100,95,48,95,55,57,0);
      let loadingspinnerF = 9803369 >= readE.length;
      do {
         readE += "3";
         if (loadingspinnerF) {
            break;
         }
      } while ((readE.length <= 5) && loadingspinnerF);
      let matchactives = 9508082 >= readE.length;
      do {
         readE = `${entryh.length}`;
         if (matchactives) {
            break;
         }
      } while (matchactives && ((basketballT.length + readE.length) < 2 && (2 + readE.length) < 1));
         readE = `${basketballT.length}`;
      for (let b = 0; b < 3; b++) {
         entryh = "2";
      }
       let formq = String.fromCharCode(110,95,54,52,95,99,111,100,101,98,111,111,107,115,0);
       let white3 = String.fromCharCode(114,97,110,100,111,109,110,101,115,115,95,53,95,53,53,0);
          let overs = String.fromCharCode(98,97,114,99,111,100,101,95,53,95,56,55,0);
         basketballT = [3 ^ entryh.length];
         overs = `${overs.length}`;
      if (formq.length <= 2) {
         entryh += `${entryh.length & formq.length}`;
      }
       let fieldx = String.fromCharCode(105,110,116,101,103,101,114,115,95,97,95,54,56,0);
          let yellowtoredP = true;
         fieldx += `${(String.fromCharCode(112,0) == entryh ? entryh.length : white3.length)}`;
         yellowtoredP = (yellowtoredP ? !yellowtoredP : !yellowtoredP);
      tooltipsy >>= Math.min(3, Math.abs(parseInt(`${appleP}`) % (Math.max(splasha.size, 1))));

    setIsDialogOpen(!isDialogOpen);
  };

  return (
    // <SafeAreaView>
    <ScreenContainer>
      <View style={{ gap: spacing.m }}>
        <TitleWithBackButtonHeader
          title="活动规则"
        
        />

        <View style={{ marginBottom: 100, paddingLeft: 20, paddingRight: 35 }}>
          <Text style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}>
            每推荐成功1位好友，即可增加VIP会员30天，好友获得30天VIP会员
          </Text>

          { }
          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",

              marginTop: 10,
            }}
          >
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              1. 推荐成功条件
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              推荐好友
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              好友下载{APP_NAME_CONST} APP
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              好友注册{APP_NAME_CONST} APP（需填写邀请码）
            </Text>
          </View>

          <Text style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}>
            好友通过您的分享链接，下载并打开app后，注册成功（需填写邀请码）成为新用户即视为推荐成功，双方均可获得奖励
          </Text>

          { }
          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",

              marginTop: 10,
            }}
          >
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              2. 受邀者奖励内容
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              受邀者通过好友分享链接下载并注册（需填写邀请码）成为新用户后，即可获得30日VIP会员，需注意如使用同一台设备或同一个账号无法重复获得奖励
            </Text>
          </View>

          { }
          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",

              marginTop: 10,
            }}
          >
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              3. 附加规则
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              用户达成奖励条件后，奖励会立即生效
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              同一设备、同一账号视为同一用户
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              如果有发现用户存在恶意使用虚假账号领取奖励多情况，平台有权利取消奖励或保留终止活动的权利
            </Text>
          </View>
        </View>
      </View>
    </ScreenContainer>
    // </SafeAreaView>
  );
};

const styles = StyleSheet.create({
  text: {},
  bullet: {
    width: 6,
    height: 6,
    borderRadius: 4,
    backgroundColor: "#fff", 
    marginLeft: 5, 
    marginRight: 8, 
    marginTop: 8,
  },
});
