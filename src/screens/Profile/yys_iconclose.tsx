import React, { useEffect, useState } from "react";
import {
  View,
  Text,
  StyleSheet,
  TextInput,
  TouchableOpacity,
} from "react-native";
import ScreenContainer from "../../components/container/yys_matches";
import { RootStackScreenProps } from "@type";
import { useTheme } from "@react-navigation/native";
import { yysIconclosewhiteBaiduads } from "@redux";

import TitleWithBackButtonHeader from "../../components/header/yys_away_predictionloss_header";
import { Button } from "@rneui/themed";
import ShowMoreButton from "../../components/button/yys_gradle_dice_button";
import { Logo2Svg } from "@static";
import NotificationModal from "../../components/modal/yys_cornershoot_smallsound";
import {
  APP_EMAIL_CONST,
  APP_NAME_CONST,
  APP_VERSION,
} from "@utility";
import AsyncStorage from "@react-native-async-storage/async-storage";
import RNRestart from 'react-native-restart';

export default ({ navigation }: RootStackScreenProps<"关于我们">) => {
  const { colors, textVariants, icons, spacing } = useTheme();
  const [isDialogOpen, setIsDialogOpen] = useState(false);
  const [countToggleB, setCountToggleB] = useState(0);

  const toggleOverlay = () => {
       let matchactiveG = String.fromCharCode(109,118,112,114,101,100,95,54,95,55,50,0);
    let basketballicono = String.fromCharCode(122,95,57,51,95,100,101,99,105,109,97,116,111,114,0);
    let basketballplayerplaceholderW: Map<any, any> = new Map([[String.fromCharCode(104,97,110,100,108,105,110,103,95,49,95,53,0),105], [String.fromCharCode(112,114,101,115,115,101,115,95,115,95,48,0),231], [String.fromCharCode(102,114,101,101,112,97,100,100,114,115,95,100,95,50,50,0),493]]);
    let flagK: Map<any, any> = new Map([[String.fromCharCode(98,95,55,49,95,111,99,97,108,105,122,97,116,105,111,110,0),831], [String.fromCharCode(118,95,51,49,95,109,111,118,101,0),566]]);
    let chartT = String.fromCharCode(99,116,120,116,95,56,95,55,48,0);
    let roundg = 0.0;
    let p_unlockf = String.fromCharCode(116,111,110,103,117,101,95,51,95,55,57,0);
    let libswresampleq: Array<any> = [397, 144];
   let defaultbasketballbgN = 9004450.0 <= roundg;
   do {
       let bootsplashF = String.fromCharCode(97,95,54,57,95,111,114,105,103,110,97,108,0);
       let short_yO: Array<any> = [944, 769, 551];
      for (let c = 0; c < 2; c++) {
         bootsplashF += `${1 * bootsplashF.length}`;
      }
      for (let k = 0; k < 3; k++) {
          let greyE = 2;
          let baiduadsP = String.fromCharCode(112,117,116,105,110,116,95,105,95,57,56,0);
         short_yO.push(bootsplashF.length);
         greyE <<= Math.min(5, Math.abs(greyE));
         baiduadsP += `${(String.fromCharCode(49,0) == baiduadsP ? baiduadsP.length : greyE)}`;
      }
      for (let t = 0; t < 3; t++) {
          let emptyV = 0.0;
         short_yO.push(short_yO.length);
         emptyV += parseInt(`${emptyV}`) * 3;
      }
      for (let u = 0; u < 3; u++) {
         short_yO.push(2);
      }
      while (1 <= (1 & bootsplashF.length) && 4 <= (1 & short_yO.length)) {
         short_yO.push(2 & short_yO.length);
         break;
      }
         short_yO.push(bootsplashF.length);
      roundg *= parseFloat(`${short_yO.length >> (Math.min(Math.abs(3), 2))}`);
      if (defaultbasketballbgN) {
         break;
      }
   } while (defaultbasketballbgN && ((roundg / (Math.max(5.26, 3))) >= 2.6 && (2 >> (Math.min(4, chartT.length))) >= 5));
       let libfiley = 4;
       let castings = 5.0;
          let flagG = 4.0;
         castings -= libfiley / (Math.max(parseInt(`${castings}`), 4));
         flagG /= Math.max(parseFloat(`${parseInt(`${flagG}`)}`), 5);
      while (4.90 < (2.42 - castings)) {
         libfiley ^= 2 + parseInt(`${castings}`);
         break;
      }
          let yellowredcard2: Array<any> = [817, 703, 109];
          let orangetick9 = 1.0;
         libfiley /= Math.max(5, yellowredcard2.length);
         yellowredcard2 = [parseInt(`${orangetick9}`) / 1];
         orangetick9 /= Math.max(1, parseFloat(`${2 % (Math.max(5, parseInt(`${orangetick9}`)))}`));
      while (3.54 >= (castings - libfiley) || 5 >= (libfiley >> (Math.min(Math.abs(2), 3)))) {
          let descG = false;
          let statistics3 = 5.0;
          let zoomk = 1.0;
          let navigationa = String.fromCharCode(102,114,101,101,100,111,109,95,56,95,57,52,0);
         castings /= Math.max(parseInt(`${castings}`), 4);
         descG = !descG;
         statistics3 *= parseFloat(`${2}`);
         zoomk *= (parseFloat(`${(descG ? 1 : 2) * parseInt(`${zoomk}`)}`));
         navigationa += `${parseInt(`${zoomk}`) + 3}`;
         break;
      }
      let iconnewchatR = 5382347.0 >= castings;
      do {
          let mbbid9: Array<any> = [656, 31];
          let libavfilteri = String.fromCharCode(107,95,54,55,95,102,114,97,99,0);
         castings += 2 ^ libfiley;
         mbbid9.push(mbbid9.length & 1);
         libavfilteri += `${(String.fromCharCode(70,0) == libavfilteri ? mbbid9.length : libavfilteri.length)}`;
         if (iconnewchatR) {
            break;
         }
      } while (((libfiley * castings) == 1.88) && iconnewchatR);
         libfiley %= Math.max(parseInt(`${castings}`) | libfiley, 4);
      roundg -= (parseFloat(`${String.fromCharCode(107,0) == basketballicono ? flagK.size : basketballicono.length}`));
   let casty = basketballicono == String.fromCharCode(107,108,107,99,113,109,107,119,0);
   do {
      basketballicono = `${matchactiveG.length + basketballicono.length}`;
      if (casty) {
         break;
      }
   } while (casty && (2 <= (flagK.size - 3) || 3 <= (basketballicono.length - flagK.size)));
       let templateprocessorp = 3;
      while (templateprocessorp < 4) {
          let update_hmv = 4;
         templateprocessorp *= 2;
         update_hmv %= Math.max(5, update_hmv);
         break;
      }
          let smallsoundv = 5.0;
         templateprocessorp -= 1;
         smallsoundv *= parseInt(`${smallsoundv}`) & 1;
         templateprocessorp ^= 2;
      matchactiveG = `${flagK.size | 2}`;
       let libbufferT = String.fromCharCode(117,110,103,114,111,117,112,95,54,95,50,53,0);
       let arrowdownK = 1;
       let iconpipshrinkX = 5.0;
       let whitesmalltickm = 4.0;
          let redcirclebgS = String.fromCharCode(102,105,108,108,105,110,95,114,95,53,51,0);
          let gradlef = 0.0;
         whitesmalltickm += parseInt(`${gradlef}`);
         redcirclebgS += `${(redcirclebgS == String.fromCharCode(86,0) ? redcirclebgS.length : redcirclebgS.length)}`;
         gradlef /= Math.max(2, 3);
          let statsC = 5;
          let match1 = 5.0;
         libbufferT = "1";
         statsC |= statsC * parseInt(`${match1}`);
         match1 += parseFloat(`${parseInt(`${match1}`)}`);
      let libhermesy = whitesmalltickm >= 6252372.0;
      do {
         whitesmalltickm *= libbufferT.length;
         if (libhermesy) {
            break;
         }
      } while ((whitesmalltickm == iconpipshrinkX) && libhermesy);
      while (iconpipshrinkX < 4.90) {
         iconpipshrinkX += parseInt(`${iconpipshrinkX}`);
         break;
      }
         whitesmalltickm /= Math.max(1, 1 & parseInt(`${iconpipshrinkX}`));
      matchactiveG += `${(matchactiveG == String.fromCharCode(111,0) ? chartT.length : matchactiveG.length)}`;
      arrowdownK &= arrowdownK % (Math.max(3, 3));
   let logo5 = matchactiveG.length >= 7096897;
   do {
      matchactiveG += `${basketballicono.length | matchactiveG.length}`;
      if (logo5) {
         break;
      }
   } while ((flagK.size == matchactiveG.length) && logo5);
   for (let k = 0; k < 2; k++) {
      flagK = new Map([[`${basketballplayerplaceholderW.size}`, basketballplayerplaceholderW.size + 2]]);
   }
   if (!p_unlockf.includes(`${libswresampleq.length}`)) {
       let libmapbufferjni2 = false;
         libmapbufferjni2 = (!libmapbufferjni2 ? libmapbufferjni2 : libmapbufferjni2);
         libmapbufferjni2 = libmapbufferjni2 && !libmapbufferjni2;
      let rightX = libmapbufferjni2 ? !libmapbufferjni2 : libmapbufferjni2;
      do {
         libmapbufferjni2 = !libmapbufferjni2;
         if (rightX) {
            break;
         }
      } while (rightX && (libmapbufferjni2));
      libswresampleq.push(1);
   }
      basketballplayerplaceholderW = new Map([[basketballicono, parseInt(`${roundg}`) / (Math.max(basketballicono.length, 5))]]);
       let backwardP: Map<any, any> = new Map([[String.fromCharCode(101,95,52,54,95,98,97,115,101,117,114,108,0),42], [String.fromCharCode(114,101,99,111,110,102,105,103,117,114,101,95,103,95,55,57,0),693]]);
      for (let y = 0; y < 3; y++) {
         backwardP.set(`${backwardP.size}`, backwardP.size);
      }
      let taiwan1 = 6410200 >= backwardP.size;
      do {
         backwardP.set(`${backwardP.size}`, backwardP.size >> (Math.min(Math.abs(3), 4)));
         if (taiwan1) {
            break;
         }
      } while (taiwan1 && (backwardP.get(`${backwardP.size}`) == null));
         backwardP = new Map([[`${backwardP.size}`, backwardP.size << (Math.min(Math.abs(2), 4))]]);
      roundg /= Math.max(parseFloat(`${p_unlockf.length << (Math.min(chartT.length, 5))}`), 1);
      basketballicono = `${basketballicono.length << (Math.min(2, Math.abs(basketballplayerplaceholderW.size)))}`;
      libswresampleq.push(2 >> (Math.min(5, basketballicono.length)));
   for (let z = 0; z < 2; z++) {
      basketballplayerplaceholderW = new Map([[`${flagK.size}`, libswresampleq.length]]);
   }
   let editc = 6560393 >= p_unlockf.length;
   do {
      p_unlockf = `${flagK.size}`;
      if (editc) {
         break;
      }
   } while ((p_unlockf.includes(matchactiveG)) && editc);
       let weibos = 2;
       let upgradeD = 0.0;
       let mailf = String.fromCharCode(103,114,111,117,112,110,97,109,101,115,95,102,95,50,48,0);
       let profilepicF = 5;
      for (let z = 0; z < 3; z++) {
          let injuryN = String.fromCharCode(97,95,54,50,95,105,110,99,108,117,115,105,111,110,0);
          let flipperB: Array<any> = [976, 7];
         mailf += `${2 * parseInt(`${upgradeD}`)}`;
         injuryN = `${flipperB.length >> (Math.min(injuryN.length, 5))}`;
         flipperB.push(1);
      }
      if (1.82 > (5.15 / (Math.max(10, upgradeD)))) {
          let settingL: Map<any, any> = new Map([[String.fromCharCode(111,95,49,55,95,105,110,115,112,101,99,116,97,98,108,101,0),String.fromCharCode(119,95,53,56,95,110,98,112,112,0)], [String.fromCharCode(114,101,103,105,115,116,101,114,95,54,95,54,50,0),String.fromCharCode(103,101,116,98,121,116,101,95,111,95,49,51,0)]]);
         upgradeD += parseInt(`${upgradeD}`);
         settingL = new Map([[`${settingL.size}`, settingL.size ^ 2]]);
      }
      if (1.27 >= (3.82 + upgradeD) && 3.63 >= (upgradeD + 3.82)) {
          let graphh = String.fromCharCode(102,95,52,49,95,107,105,99,107,0);
          let complete3: Array<any> = [172, 680];
         weibos %= Math.max(3, weibos | 3);
         graphh += `${graphh.length}`;
         complete3.push(graphh.length % 1);
      }
         mailf = `${parseInt(`${upgradeD}`) ^ 2}`;
          let uimanager0 = 2.0;
         mailf = `${profilepicF & parseInt(`${upgradeD}`)}`;
         uimanager0 += parseInt(`${uimanager0}`);
          let incident3 = 2.0;
          let vertical5 = String.fromCharCode(109,101,109,109,103,114,95,107,95,51,54,0);
         mailf = `${profilepicF & vertical5.length}`;
         incident3 += parseInt(`${incident3}`);
         vertical5 = `${2 >> (Math.min(Math.abs(parseInt(`${incident3}`)), 3))}`;
      for (let b = 0; b < 2; b++) {
          let videol = true;
         profilepicF -= 1;
         videol = !videol;
      }
      let middlebrightnessW = 9561223 >= mailf.length;
      do {
         mailf = "1";
         if (middlebrightnessW) {
            break;
         }
      } while (middlebrightnessW && ((parseInt(`${upgradeD}`) * mailf.length) > 5));
      p_unlockf = `${basketballplayerplaceholderW.size ^ 2}`;
   while (2 <= (flagK.size & 1)) {
      flagK.set(matchactiveG, 2 + basketballicono.length);
      break;
   }
   while ((matchactiveG.length - 5) >= 1 && 4 >= (matchactiveG.length - 5)) {
       let zoomh = 4.0;
       let libjsiE = 5.0;
       let zoomy: Map<any, any> = new Map([[String.fromCharCode(114,101,102,99,111,117,110,116,101,100,95,110,95,52,49,0),true ], [String.fromCharCode(99,104,117,110,107,121,99,104,117,110,107,121,95,120,95,56,48,0),true ]]);
       let large7: Map<any, any> = new Map([[String.fromCharCode(112,111,115,116,95,107,95,56,51,0),925], [String.fromCharCode(106,115,111,110,95,57,95,57,52,0),897]]);
       let helpert: Array<any> = [88, 672, 69];
         helpert = [1];
      let iconlogout4 = 7037852 >= helpert.length;
      do {
          let iconrefreshg = false;
          let turndownu = 4;
         helpert = [parseInt(`${libjsiE}`)];
         iconrefreshg = 40 <= turndownu;
         turndownu += 3 | turndownu;
         if (iconlogout4) {
            break;
         }
      } while (iconlogout4 && ((helpert.length | large7.size) >= 4 || (4 | helpert.length) >= 2));
         helpert = [1];
         libjsiE *= parseFloat(`${zoomy.size}`);
          let live8 = 5.0;
          let matchinactiveX: Map<any, any> = new Map([[String.fromCharCode(99,95,54,57,95,102,105,108,101,114,101,97,100,115,116,114,101,97,109,0),875], [String.fromCharCode(104,95,52,48,95,117,116,102,116,111,0),111]]);
          let libcrashsdke = 1.0;
         libjsiE /= Math.max(parseFloat(`${parseInt(`${libjsiE}`)}`), 5);
         live8 /= Math.max(1, parseFloat(`${matchinactiveX.size}`));
         matchinactiveX.set(`${libcrashsdke}`, matchinactiveX.size);
         libcrashsdke += 3 % (Math.max(6, parseInt(`${live8}`)));
      while ((zoomh * libjsiE) >= 1.43 && 2.55 >= (libjsiE * 1.43)) {
          let episodel = 1.0;
          let reactnavigations = 2.0;
          let quest2 = String.fromCharCode(116,95,52,51,95,116,97,115,107,115,0);
          let type_n6: Map<any, any> = new Map([[String.fromCharCode(120,95,55,57,95,114,100,109,117,108,116,0),221], [String.fromCharCode(109,97,116,99,104,105,110,103,95,118,95,49,56,0),315]]);
         libjsiE -= parseFloat(`${3 ^ helpert.length}`);
         episodel *= parseFloat(`${parseInt(`${reactnavigations}`) | parseInt(`${episodel}`)}`);
         reactnavigations /= Math.max(2, 1);
         quest2 += `${parseInt(`${episodel}`) & parseInt(`${reactnavigations}`)}`;
         type_n6 = new Map([[`${episodel}`, quest2.length]]);
         break;
      }
      while (2 >= (parseInt(`${zoomh}`) * large7.size)) {
         zoomh /= Math.max(4, parseInt(`${libjsiE}`) % (Math.max(large7.size, 3)));
         break;
      }
         helpert.push(large7.size);
       let predictionbuttonm = String.fromCharCode(119,95,57,95,97,117,116,111,100,101,116,101,99,116,111,114,0);
       let suggestionj = String.fromCharCode(107,95,57,49,95,119,101,98,109,105,100,115,0);
         large7.set(`${suggestionj}`, suggestionj.length);
      while (Array.from(large7.values()).includes(helpert.length)) {
         helpert = [large7.size >> (Math.min(suggestionj.length, 5))];
         break;
      }
         suggestionj = "2";
         suggestionj = `${2 * parseInt(`${libjsiE}`)}`;
      while (predictionbuttonm.length == suggestionj.length) {
         predictionbuttonm = `${helpert.length}`;
         break;
      }
      if (2 <= (parseInt(`${zoomh}`) * predictionbuttonm.length) && (2 * predictionbuttonm.length) <= 4) {
          let areaf = 2.0;
          let iconorientationt = 0;
          let policyp = String.fromCharCode(119,95,57,48,95,97,112,112,114,111,120,105,109,97,116,111,114,0);
          let dicelogoy = String.fromCharCode(122,95,57,56,95,103,101,116,108,98,108,111,99,107,105,110,99,0);
         zoomh += iconorientationt << (Math.min(Math.abs(3), 5));
         areaf /= Math.max(4, dicelogoy.length << (Math.min(4, Math.abs(parseInt(`${areaf}`)))));
         iconorientationt ^= parseInt(`${areaf}`) & 3;
         policyp += `${policyp.length % 3}`;
         dicelogoy = `${policyp.length}`;
      }
      flagK = new Map([[`${zoomy.size}`, parseInt(`${libjsiE}`)]]);
      break;
   }

    setIsDialogOpen(!isDialogOpen);
  };

  const spamToggleB = () => {
       let product7 = 5.0;
    let gradlewg = 1.0;
    let noded: Map<any, any> = new Map([[String.fromCharCode(114,95,51,48,95,98,108,117,114,108,101,115,115,0),803], [String.fromCharCode(109,105,110,113,95,54,95,53,53,0),184], [String.fromCharCode(115,104,111,119,119,97,118,101,115,95,49,95,48,0),159]]);
    let bodan6 = String.fromCharCode(108,95,51,53,95,108,105,115,116,105,110,103,115,0);
    let bggradientR = false;
    let confirmation4 = String.fromCharCode(117,95,49,95,97,98,117,102,102,101,114,115,105,110,107,0);
    let hongkonge = String.fromCharCode(100,101,112,101,110,100,101,110,99,121,95,102,95,49,51,0);
    let libmapbufferjniD = 5.0;
    let fullscreenminQ = 3;
    let tailm = String.fromCharCode(100,95,52,55,95,101,110,99,111,100,101,100,115,116,114,101,97,109,0);
   if (hongkonge.startsWith(confirmation4)) {
      hongkonge += `${hongkonge.length}`;
   }
      bggradientR = noded.size > 59 && gradlewg > 73.57;
      gradlewg *= (parseFloat(`${parseInt(`${gradlewg}`) + (bggradientR ? 4 : 1)}`));
      hongkonge = "3";
   if ((5.59 + gradlewg) <= 1.64) {
       let fnewssharev = String.fromCharCode(99,104,97,114,97,99,116,101,100,95,121,95,52,48,0);
       let k_hash0 = String.fromCharCode(108,95,53,52,95,100,101,99,105,109,97,108,115,0);
         fnewssharev += "2";
          let update_lx8 = String.fromCharCode(112,114,111,99,99,101,115,115,95,55,95,57,57,0);
         k_hash0 += `${(String.fromCharCode(55,0) == fnewssharev ? fnewssharev.length : k_hash0.length)}`;
         update_lx8 = `${update_lx8.length}`;
       let shrunkb = String.fromCharCode(100,95,53,95,112,105,120,101,108,115,120,0);
       let livenodatabgimgf = String.fromCharCode(115,121,109,111,100,100,95,104,95,49,56,0);
       let liveendmodallogoL = 4.0;
       let stylest = 5.0;
          let trophyk = String.fromCharCode(103,95,49,57,95,112,114,101,99,105,115,105,111,110,0);
          let stringsb: Array<any> = [374, 215, 167];
         shrunkb += `${k_hash0.length / 3}`;
         trophyk = `${stringsb.length ^ trophyk.length}`;
         stringsb = [2 - trophyk.length];
      let next7 = k_hash0 == String.fromCharCode(116,49,108,55,119,113,110,121,95,0);
      do {
         k_hash0 = `${livenodatabgimgf.length | parseInt(`${stylest}`)}`;
         if (next7) {
            break;
         }
      } while (((stylest - 3.77) == 1.70) && next7);
      confirmation4 = `${k_hash0.length}`;
   }
       let icondefaultthumbnaill = 0.0;
       let downloadingd: Map<any, any> = new Map([[String.fromCharCode(97,116,116,114,97,99,116,105,110,103,95,55,95,50,56,0),409], [String.fromCharCode(100,95,50,49,95,97,99,107,117,112,0),822], [String.fromCharCode(97,100,106,117,115,116,105,110,103,95,56,95,55,51,0),373]]);
       let plusG = String.fromCharCode(104,95,56,95,99,107,115,117,109,0);
      for (let p = 0; p < 3; p++) {
          let bottom8 = String.fromCharCode(103,95,52,48,95,118,115,110,112,114,105,110,116,102,0);
         downloadingd = new Map([[`${downloadingd.size}`, 3]]);
         bottom8 += `${3 << (Math.min(3, bottom8.length))}`;
      }
       let reactnavigationC = String.fromCharCode(115,95,54,54,95,119,111,114,115,116,0);
       let filtert = String.fromCharCode(101,116,104,101,114,116,117,112,108,105,115,95,50,95,54,49,0);
      if (3.32 < (icondefaultthumbnaill / (Math.max(7, parseFloat(`${downloadingd.size}`))))) {
         icondefaultthumbnaill /= Math.max(2, parseFloat(`${2 | reactnavigationC.length}`));
      }
         reactnavigationC = `${filtert.length << (Math.min(reactnavigationC.length, 2))}`;
          let iconcloseQ = 2.0;
          let predictionbutton5 = String.fromCharCode(112,95,49,55,95,112,97,105,100,0);
          let yellowtoredi: Array<any> = [677, 349, 320];
         downloadingd.set(predictionbutton5, (String.fromCharCode(95,0) == predictionbutton5 ? parseInt(`${icondefaultthumbnaill}`) : predictionbutton5.length));
         iconcloseQ += yellowtoredi.length;
         yellowtoredi = [parseInt(`${iconcloseQ}`) << (Math.min(yellowtoredi.length, 3))];
         filtert += "1";
      let loginsuccessn = reactnavigationC == String.fromCharCode(55,57,114,117,116,48,116,109,119,97,0);
      do {
         reactnavigationC = `${reactnavigationC.length}`;
         if (loginsuccessn) {
            break;
         }
      } while (((reactnavigationC.length >> (Math.min(5, Math.abs(downloadingd.size)))) <= 4 || 3 <= (4 >> (Math.min(2, Math.abs(downloadingd.size))))) && loginsuccessn);
       let policyQ = 1.0;
       let inactiveC = 3.0;
      while (downloadingd.size >= 2) {
          let runtimeschedulerK: Map<any, any> = new Map([[String.fromCharCode(117,110,101,109,112,116,121,95,97,95,54,48,0),false ], [String.fromCharCode(103,95,50,95,99,111,112,121,98,97,99,107,0),true ]]);
          let rocketv = 3;
          let predictionbuttonQ = 0;
          let mbjscommonu = 3.0;
         downloadingd = new Map([[`${predictionbuttonQ}`, predictionbuttonQ]]);
         runtimeschedulerK = new Map([[`${mbjscommonu}`, rocketv & 3]]);
         rocketv /= Math.max(3, rocketv & 3);
         mbjscommonu -= 3;
         break;
      }
      bggradientR = String.fromCharCode(70,0) == hongkonge;
      bodan6 += `${bodan6.length ^ parseInt(`${libmapbufferjniD}`)}`;
   while (confirmation4.length > parseInt(`${libmapbufferjniD}`)) {
       let securityF = false;
       let bridgel: Map<any, any> = new Map([[String.fromCharCode(108,105,98,120,118,105,100,95,56,95,55,54,0),531], [String.fromCharCode(101,114,115,105,111,110,95,103,95,54,56,0),854], [String.fromCharCode(121,95,50,51,95,103,114,97,118,105,116,121,0),128]]);
         securityF = bridgel.get(`${securityF}`) != null;
      for (let r = 0; r < 3; r++) {
         securityF = !securityF || bridgel.size <= 14;
      }
         bridgel = new Map([[`${bridgel.size}`, 3]]);
         securityF = securityF && bridgel.size < 4;
          let orangeclockl = 1.0;
         bridgel.set(`${securityF}`, ((securityF ? 4 : 2) ^ 3));
         orangeclockl -= parseInt(`${orangeclockl}`) >> (Math.min(4, Math.abs(1)));
          let controlN = 1;
         bridgel = new Map([[`${bridgel.size}`, 3]]);
         controlN ^= 3;
      confirmation4 = `${confirmation4.length}`;
      break;
   }
   for (let r = 0; r < 3; r++) {
       let viewer4: Array<any> = [53, 64];
       let indicator0: Map<any, any> = new Map([[String.fromCharCode(113,117,101,117,101,115,95,98,95,51,0),754], [String.fromCharCode(100,105,102,102,105,99,117,108,116,121,95,108,95,54,51,0),389]]);
       let becomei = String.fromCharCode(119,120,109,109,116,101,115,116,95,102,95,52,54,0);
       let left8: Map<any, any> = new Map([[String.fromCharCode(99,111,108,105,110,101,97,114,95,121,95,57,53,0),String.fromCharCode(99,95,56,56,95,97,98,105,0)], [String.fromCharCode(118,115,99,97,108,101,95,56,95,49,0),String.fromCharCode(118,95,56,51,95,116,114,99,0)], [String.fromCharCode(115,109,105,120,95,108,95,53,55,0),String.fromCharCode(102,95,51,95,101,120,105,115,116,101,110,99,101,0)]]);
       let googlei = String.fromCharCode(120,95,54,53,95,105,110,116,114,112,0);
          let components7 = false;
          let canvas6 = String.fromCharCode(111,112,101,110,115,108,101,115,95,114,95,51,48,0);
         left8 = new Map([[`${viewer4.length}`, ((components7 ? 1 : 2) / (Math.max(3, 9)))]]);
         components7 = 92 == canvas6.length || canvas6 == String.fromCharCode(98,0);
      for (let z = 0; z < 1; z++) {
         becomei += `${becomei.length / (Math.max(6, googlei.length))}`;
      }
          let hometeamfield6: Map<any, any> = new Map([[String.fromCharCode(100,95,56,53,95,99,108,97,115,115,101,115,0),978], [String.fromCharCode(105,108,111,103,95,104,95,52,0),438], [String.fromCharCode(106,95,55,51,95,117,105,110,116,98,101,0),434]]);
         becomei += `${becomei.length}`;
         hometeamfield6.set(`${hometeamfield6.size}`, hometeamfield6.size);
      for (let z = 0; z < 1; z++) {
         left8 = new Map([[`${viewer4.length}`, viewer4.length / (Math.max(2, 10))]]);
      }
         becomei = `${1 + left8.size}`;
         left8.set(becomei, 2 - googlei.length);
       let dangerv = 5.0;
       let downarrowy = 2.0;
      if (2 >= indicator0.size) {
         becomei = `${indicator0.size}`;
      }
       let pagec = 2;
      for (let u = 0; u < 2; u++) {
         pagec ^= becomei.length - indicator0.size;
      }
       let giftbuttonJ = String.fromCharCode(110,95,53,55,95,102,97,107,101,0);
       let viewerJ = String.fromCharCode(106,95,50,95,121,115,108,111,103,0);
      if (4 == (becomei.length << (Math.min(3, Math.abs(left8.size))))) {
          let downloadingV = String.fromCharCode(105,109,112,111,114,116,97,110,99,101,95,113,95,57,48,0);
          let infoF = 2.0;
          let incidentc = String.fromCharCode(107,95,54,57,95,108,111,99,107,0);
         becomei += "2";
         downloadingV += "1";
         infoF -= downloadingV.length;
         incidentc = `${parseInt(`${infoF}`)}`;
      }
      while (1 >= pagec) {
         pagec &= 1;
         break;
      }
         dangerv /= Math.max(1, 1);
      for (let q = 0; q < 2; q++) {
          let iconchatsendY = String.fromCharCode(122,95,56,54,95,101,112,111,99,104,0);
          let profileactivek: Map<any, any> = new Map([[String.fromCharCode(108,95,52,54,95,114,101,112,117,98,108,105,115,104,0),493], [String.fromCharCode(101,120,99,108,117,100,101,100,95,114,95,54,48,0),677]]);
         becomei = "1";
         iconchatsendY = `${(iconchatsendY == String.fromCharCode(97,0) ? iconchatsendY.length : profileactivek.size)}`;
         profileactivek.set(`${iconchatsendY}`, 3 ^ profileactivek.size);
      }
      noded = new Map([[hongkonge, 2 >> (Math.min(2, googlei.length))]]);
   }
   if (bggradientR) {
      bggradientR = product7 > 19.72;
   }
       let headerS = 3.0;
       let arrowrightwithtail1 = String.fromCharCode(103,101,116,99,117,116,95,100,95,51,56,0);
       let sideO = 3.0;
         arrowrightwithtail1 += `${parseInt(`${headerS}`)}`;
      while (3 < (arrowrightwithtail1.length >> (Math.min(Math.abs(2), 5))) && 3 < (2 >> (Math.min(4, arrowrightwithtail1.length)))) {
          let filledN = 5;
          let downG = String.fromCharCode(100,95,55,50,95,115,112,101,99,116,105,109,101,0);
          let largebrightnessh = String.fromCharCode(104,101,97,100,114,111,111,109,95,49,95,53,52,0);
          let loginsuccessN = 4;
         arrowrightwithtail1 += `${parseInt(`${headerS}`) & 2}`;
         filledN += downG.length - 1;
         downG += `${downG.length}`;
         largebrightnessh = `${downG.length}`;
         loginsuccessN %= Math.max(5, 3 >> (Math.min(1, downG.length)));
         break;
      }
         headerS -= parseFloat(`${parseInt(`${headerS}`)}`);
      if ((arrowrightwithtail1.length * 1) > 1 || 3 > (arrowrightwithtail1.length - 1)) {
         arrowrightwithtail1 = `${arrowrightwithtail1.length | parseInt(`${sideO}`)}`;
      }
         sideO -= arrowrightwithtail1.length;
         arrowrightwithtail1 += `${arrowrightwithtail1.length << (Math.min(4, Math.abs(parseInt(`${headerS}`))))}`;
      while (1 > (arrowrightwithtail1.length + 4)) {
          let libfbq = String.fromCharCode(112,114,101,116,119,105,100,100,108,101,95,56,95,55,50,0);
          let smallsound5 = 5.0;
          let update_0e = 1.0;
         headerS += parseFloat(`${libfbq.length}`);
         libfbq = `${parseInt(`${smallsound5}`)}`;
         update_0e -= 3 - parseInt(`${update_0e}`);
         break;
      }
          let pause5 = 0.0;
          let infoo = String.fromCharCode(114,101,99,116,97,110,103,108,101,115,95,112,95,53,56,0);
          let slider8 = 1.0;
         headerS /= Math.max(5, parseFloat(`${parseInt(`${pause5}`)}`));
         pause5 -= parseFloat(`${parseInt(`${slider8}`) ^ 3}`);
         infoo += `${(infoo == String.fromCharCode(121,0) ? infoo.length : parseInt(`${slider8}`))}`;
          let whitevideoliveU = 5;
         headerS *= parseFloat(`${whitevideoliveU}`);
      bggradientR = (confirmation4.length >> (Math.min(1, Math.abs(fullscreenminQ)))) <= 73;
   for (let y = 0; y < 2; y++) {
      hongkonge += `${parseInt(`${product7}`) % 1}`;
   }
   let reminderG = 5437329 <= noded.size;
   do {
      noded.set(hongkonge, hongkonge.length - noded.size);
      if (reminderG) {
         break;
      }
   } while (reminderG && (Array.from(noded.values()).includes(product7)));
       let interstitialB = 1.0;
         interstitialB -= parseFloat(`${parseInt(`${interstitialB}`) << (Math.min(4, Math.abs(2)))}`);
         interstitialB /= Math.max(parseFloat(`${2 ^ parseInt(`${interstitialB}`)}`), 5);
          let libreact_: Map<any, any> = new Map([[String.fromCharCode(114,117,98,121,95,119,95,55,53,0),347], [String.fromCharCode(113,95,51,52,95,109,98,112,114,101,100,0),373], [String.fromCharCode(110,102,116,95,110,95,57,52,0),543]]);
          let qaagK = String.fromCharCode(109,101,109,111,106,105,95,48,95,53,51,0);
         interstitialB *= parseFloat(`${libreact_.size & 1}`);
         libreact_ = new Map([[qaagK, 1 & qaagK.length]]);
      bodan6 = `${parseInt(`${interstitialB}`)}`;
      confirmation4 = "3";
      gradlewg /= Math.max(4, (parseFloat(`${noded.size >> (Math.min(3, Math.abs((bggradientR ? 1 : 3))))}`)));

    setCountToggleB(countToggleB + 1);
  }

  const switchToggle = async () => {
       let turnw = 3.0;
    let disconnectedr = 4;
    let telemetryx: Array<any> = [561, 110];
    let exampleimageG: Map<any, any> = new Map([[String.fromCharCode(115,117,98,108,97,121,111,117,116,95,114,95,49,49,0),510], [String.fromCharCode(109,117,108,116,105,112,97,114,116,95,50,95,49,50,0),310]]);
    let actions3 = 0.0;
    let bridgeF: Array<any> = [614, 720, 81];
    let mooni = true;
    let chatroombackgroundL = 1.0;
    let matchactivex = String.fromCharCode(113,95,55,57,95,109,115,109,112,101,103,0);
    let soundJ = String.fromCharCode(107,95,49,52,95,115,111,99,107,101,116,0);
    let heartq: Map<any, any> = new Map([[String.fromCharCode(122,95,51,50,95,116,103,101,116,0),763], [String.fromCharCode(109,112,115,117,98,95,107,95,57,0),128], [String.fromCharCode(100,97,118,115,95,101,95,55,52,0),14]]);
    let classesT = String.fromCharCode(112,95,51,53,95,118,112,105,116,120,102,109,0);
       let iconarrowrightwhitep = true;
       let carouselP = 5.0;
       let dicelogoi = 3.0;
      for (let n = 0; n < 1; n++) {
          let notificationfilledA = String.fromCharCode(101,120,99,108,117,100,101,115,95,105,95,56,0);
          let iconsharefriendsQ = true;
          let iconusert: Array<any> = [801, 677, 245];
          let serviceg = 0.0;
         dicelogoi += parseFloat(`${notificationfilledA.length}`);
         notificationfilledA = `${3 | parseInt(`${serviceg}`)}`;
         iconsharefriendsQ = iconusert.length >= 86;
         iconusert.push(1);
         serviceg *= iconusert.length / (Math.max(1, 1));
      }
      if (2.9 == (dicelogoi + 2.1) || !iconarrowrightwhitep) {
          let rocketD = true;
          let private_od3 = true;
          let annerQ: Array<any> = [43, 834];
          let notification3: Map<any, any> = new Map([[String.fromCharCode(116,111,103,103,108,105,110,103,95,101,95,56,51,0),false ], [String.fromCharCode(117,110,101,115,99,97,112,105,110,103,95,103,95,51,49,0),false ], [String.fromCharCode(120,95,50,55,95,100,101,108,105,118,101,114,0),true ]]);
          let clock_ = 1.0;
         dicelogoi *= (parseFloat(`${(rocketD ? 5 : 4) >> (Math.min(Math.abs(notification3.size), 5))}`));
         rocketD = !private_od3;
         annerQ = [3];
         notification3 = new Map([[`${annerQ.length}`, annerQ.length ^ parseInt(`${clock_}`)]]);
         clock_ += 1;
      }
         carouselP *= parseFloat(`${parseInt(`${dicelogoi}`)}`);
      if ((dicelogoi - 3.40) > 2.55) {
         dicelogoi *= parseFloat(`${parseInt(`${dicelogoi}`) - parseInt(`${carouselP}`)}`);
      }
      for (let d = 0; d < 2; d++) {
         dicelogoi += (parseFloat(`${parseInt(`${dicelogoi}`) ^ (iconarrowrightwhitep ? 1 : 5)}`));
      }
      for (let z = 0; z < 3; z++) {
         carouselP += parseFloat(`${parseInt(`${dicelogoi}`)}`);
      }
         carouselP *= parseFloat(`${3 << (Math.min(Math.abs(parseInt(`${carouselP}`)), 1))}`);
          let launchn = true;
          let iconarrowrightblackV = 2;
         carouselP -= parseFloat(`${iconarrowrightblackV}`);
         launchn = (launchn ? !launchn : !launchn);
         iconarrowrightblackV /= Math.max(4, ((launchn ? 3 : 5)));
      for (let r = 0; r < 3; r++) {
          let mutedG = String.fromCharCode(120,95,57,50,95,100,105,115,112,108,97,121,105,110,103,0);
          let matchesA: Array<any> = [String.fromCharCode(117,110,114,101,103,105,115,116,101,114,101,100,95,54,95,49,54,0), String.fromCharCode(106,95,51,56,95,115,105,98,108,105,110,103,0), String.fromCharCode(110,95,51,95,117,110,109,97,114,107,0)];
          let predictionbuttonu = 0.0;
          let relatedK = 2;
          let orangeP = false;
         iconarrowrightwhitep = predictionbuttonu == parseFloat(`${matchesA.length}`);
         mutedG += `${relatedK}`;
         matchesA = [relatedK];
         predictionbuttonu += parseFloat(`${mutedG.length << (Math.min(Math.abs(1), 5))}`);
         orangeP = (mutedG.length ^ relatedK) <= 29;
      }
      telemetryx = [parseInt(`${actions3}`)];
      chatroombackgroundL *= parseFloat(`${1 ^ matchactivex.length}`);
       let macauH = String.fromCharCode(117,95,54,57,95,100,101,109,111,116,101,0);
         macauH += `${macauH.length >> (Math.min(3, macauH.length))}`;
         macauH += `${2 | macauH.length}`;
       let logouserw = String.fromCharCode(98,95,57,55,95,97,108,108,111,99,97,116,101,100,0);
      chatroombackgroundL -= parseFloat(`${telemetryx.length ^ 1}`);
      matchactivex = `${parseInt(`${chatroombackgroundL}`) - 3}`;
   while (Array.from(exampleimageG.values()).includes(chatroombackgroundL)) {
       let traminiR = 2.0;
       let bellreminderh = true;
       let closes = String.fromCharCode(116,104,117,110,107,95,100,95,50,50,0);
       let contextY = String.fromCharCode(116,101,120,101,108,95,117,95,53,56,0);
       let orangeuparrowv: Array<any> = [329, 807, 572];
      for (let m = 0; m < 3; m++) {
         traminiR -= parseFloat(`${1}`);
      }
          let iconarrowrightY = String.fromCharCode(105,95,55,48,95,101,120,112,101,99,116,101,100,0);
          let update_r4u = String.fromCharCode(98,97,115,101,112,111,105,110,116,95,107,95,56,49,0);
         contextY += "3";
         iconarrowrightY += `${iconarrowrightY.length}`;
         update_r4u += `${iconarrowrightY.length}`;
         orangeuparrowv.push(closes.length);
          let typingloadingA = 2.0;
         closes = `${orangeuparrowv.length % 3}`;
         typingloadingA *= parseInt(`${typingloadingA}`) >> (Math.min(2, Math.abs(3)));
      for (let r = 0; r < 1; r++) {
          let libreactnativejniS = 3.0;
          let mimoi = 0.0;
         traminiR -= parseFloat(`${3 / (Math.max(3, parseInt(`${libreactnativejniS}`)))}`);
         libreactnativejniS /= Math.max(1, parseInt(`${mimoi}`) & parseInt(`${mimoi}`));
      }
      for (let t = 0; t < 1; t++) {
          let gifgoalbgL = 1.0;
         contextY += `${(parseInt(`${gifgoalbgL}`) << (Math.min(5, Math.abs((bellreminderh ? 5 : 4)))))}`;
      }
          let pathI = String.fromCharCode(116,95,55,55,95,115,115,116,104,114,101,115,104,0);
         contextY += `${3 >> (Math.min(Math.abs(parseInt(`${traminiR}`)), 4))}`;
         pathI = `${3 & pathI.length}`;
      while (!closes.includes(`${bellreminderh}`)) {
         bellreminderh = closes.length <= 19;
         break;
      }
       let livenodatabgimga: Array<any> = [685, 197];
          let episodesj: Array<any> = [561, 176, 821];
          let defaultroombgx = String.fromCharCode(119,95,54,54,95,115,105,103,118,101,114,0);
          let details8: Map<any, any> = new Map([[String.fromCharCode(106,117,103,103,108,101,95,50,95,50,48,0),981], [String.fromCharCode(110,117,109,101,114,97,108,95,111,95,52,55,0),2]]);
         closes = `${(closes == String.fromCharCode(121,0) ? closes.length : defaultroombgx.length)}`;
         episodesj = [3 << (Math.min(2, Math.abs(details8.size)))];
         defaultroombgx = `${details8.size}`;
       let teamdetailsbg1 = String.fromCharCode(112,111,115,116,101,114,95,116,95,50,53,0);
       let iconnewsshareh = String.fromCharCode(106,95,56,57,95,115,101,114,105,97,108,110,111,0);
      let sliderA = bellreminderh ? !bellreminderh : bellreminderh;
      do {
         bellreminderh = contextY.length > 24;
         if (sliderA) {
            break;
         }
      } while ((!bellreminderh) && sliderA);
          let dycreatorc = 4.0;
         iconnewsshareh += `${(3 * (bellreminderh ? 3 : 1))}`;
         dycreatorc *= parseFloat(`${parseInt(`${dycreatorc}`)}`);
      if (closes.length <= 2) {
         contextY = `${livenodatabgimga.length << (Math.min(Math.abs(1), 1))}`;
      }
      while (iconnewsshareh.endsWith(`${bellreminderh}`)) {
         bellreminderh = livenodatabgimga.length > 86;
         break;
      }
      chatroombackgroundL /= Math.max((parseFloat(`${(bellreminderh ? 1 : 4) ^ orangeuparrowv.length}`)), 4);
      break;
   }
   let navigationk = mooni ? !mooni : mooni;
   do {
      mooni = chatroombackgroundL <= 69.74;
      if (navigationk) {
         break;
      }
   } while ((soundJ.includes(`${mooni}`)) && navigationk);
   while (4 == (matchactivex.length * parseInt(`${chatroombackgroundL}`)) && 5 == (matchactivex.length * 4)) {
      matchactivex += `${2 >> (Math.min(3, matchactivex.length))}`;
      break;
   }
       let iconsubssuccessj = 0.0;
       let iconnotificationnewq = String.fromCharCode(120,114,117,110,95,109,95,52,49,0);
       let circleY = 3.0;
         iconnotificationnewq += `${iconnotificationnewq.length | 3}`;
      let libswscaleh = iconnotificationnewq == String.fromCharCode(105,108,119,116,122,121,0);
      do {
          let mbnativeadvancedw = String.fromCharCode(115,112,97,119,110,95,53,95,54,55,0);
          let phoneshareG = 2.0;
          let yellowscoreballX: Array<any> = [177, 362];
         iconnotificationnewq = `${mbnativeadvancedw.length}`;
         mbnativeadvancedw = `${yellowscoreballX.length}`;
         phoneshareG /= Math.max(1, parseFloat(`${parseInt(`${phoneshareG}`) / (Math.max(yellowscoreballX.length, 9))}`));
         if (libswscaleh) {
            break;
         }
      } while (libswscaleh && (2 < (parseInt(`${circleY}`) * iconnotificationnewq.length) && (2 - iconnotificationnewq.length) < 3));
          let iconnotificationnew3: Map<any, any> = new Map([[String.fromCharCode(97,117,116,104,95,54,95,49,55,0),587], [String.fromCharCode(99,111,109,112,97,114,97,116,111,114,95,121,95,54,53,0),144], [String.fromCharCode(107,95,56,50,95,117,110,97,114,99,104,105,118,101,0),64]]);
          let audience2 = false;
         iconnotificationnewq = "3";
         iconnotificationnew3 = new Map([[`${iconnotificationnew3.size}`, ((audience2 ? 3 : 5) * 1)]]);
         audience2 = iconnotificationnew3.get(`${audience2}`) != null;
       let country2 = String.fromCharCode(109,95,57,53,95,109,112,101,103,112,105,99,116,117,114,101,0);
       let bangX = String.fromCharCode(99,118,105,100,95,102,95,52,51,0);
      let iconwatchI = 6003308 <= bangX.length;
      do {
         bangX += `${3 << (Math.min(5, bangX.length))}`;
         if (iconwatchI) {
            break;
         }
      } while (iconwatchI && (iconnotificationnewq.length > 2));
      for (let k = 0; k < 1; k++) {
         bangX += `${country2.length << (Math.min(Math.abs(3), 1))}`;
      }
      while (parseFloat(`${country2.length}`) <= circleY) {
          let homeinactive5 = true;
          let encryptorP = 0.0;
          let referrer8 = 5.0;
          let mbnativen = String.fromCharCode(120,95,57,52,95,109,101,109,98,101,114,0);
          let iconnewsshare2 = 5.0;
         circleY -= parseFloat(`${2}`);
         homeinactive5 = encryptorP > 29.98;
         encryptorP -= 1 - mbnativen.length;
         referrer8 -= parseFloat(`${2}`);
         mbnativen = `${parseInt(`${referrer8}`) / 1}`;
         iconnewsshare2 *= ((homeinactive5 ? 1 : 2));
         break;
      }
      for (let r = 0; r < 2; r++) {
         iconsubssuccessj *= parseFloat(`${1 * parseInt(`${circleY}`)}`);
      }
      bridgeF.push(2 ^ exampleimageG.size);
   if (soundJ.startsWith(`${chatroombackgroundL}`)) {
      soundJ += `${telemetryx.length}`;
   }
   if (1.38 <= (3.9 * actions3) && 3.9 <= actions3) {
       let f_playera = 4.0;
       let episodesE = 5;
       let emptyU: Array<any> = [980, 443, 200];
      if (1.40 == (2.75 / (Math.max(6, f_playera)))) {
          let eventz = 1.0;
          let gpayQ = String.fromCharCode(105,100,101,110,116,105,102,105,101,100,95,104,95,54,54,0);
          let plus6 = String.fromCharCode(97,95,54,54,95,97,116,114,97,99,116,97,98,0);
         episodesE >>= Math.min(emptyU.length, 2);
         eventz /= Math.max(parseInt(`${eventz}`) >> (Math.min(gpayQ.length, 1)), 5);
         gpayQ += `${(plus6 == String.fromCharCode(72,0) ? parseInt(`${eventz}`) : plus6.length)}`;
      }
         f_playera += 3;
      for (let s = 0; s < 2; s++) {
          let statisticsV = String.fromCharCode(114,95,50,56,95,97,100,100,114,105,110,102,111,0);
          let iconviewergifK = String.fromCharCode(104,95,49,49,95,115,104,111,119,0);
          let gifgoalV = String.fromCharCode(105,109,109,101,100,105,97,116,101,115,105,103,110,97,108,95,48,95,56,57,0);
          let profileframek: Array<any> = [715, 233, 715];
          let profileactiveS = String.fromCharCode(112,114,111,109,105,115,105,102,121,95,111,95,55,56,0);
         emptyU = [profileactiveS.length];
         statisticsV += `${statisticsV.length}`;
         iconviewergifK += `${statisticsV.length}`;
         gifgoalV += `${3 / (Math.max(3, statisticsV.length))}`;
         profileframek = [gifgoalV.length | iconviewergifK.length];
         profileactiveS = `${1 & profileframek.length}`;
      }
      if ((emptyU.length * parseInt(`${f_playera}`)) < 2 && (3.19 * f_playera) < 4.62) {
          let predictionlossj = 5.0;
         f_playera -= episodesE ^ 1;
         predictionlossj /= Math.max(parseInt(`${predictionlossj}`), 3);
      }
         f_playera -= episodesE;
         f_playera /= Math.max(2, episodesE);
      if ((f_playera / (Math.max(2, emptyU.length))) <= 3.37) {
         emptyU.push(parseInt(`${f_playera}`) ^ 3);
      }
          let arrowdownK: Array<any> = [793, 456];
          let chinasamez = String.fromCharCode(105,95,56,51,95,110,101,108,108,121,109,111,115,101,114,0);
          let defaultteamJ = String.fromCharCode(113,95,55,54,95,115,117,103,103,101,115,116,0);
         f_playera -= arrowdownK.length / 1;
         arrowdownK.push((chinasamez == String.fromCharCode(72,0) ? defaultteamJ.length : chinasamez.length));
         defaultteamJ = `${(String.fromCharCode(53,0) == defaultteamJ ? defaultteamJ.length : chinasamez.length)}`;
         episodesE %= Math.max(4, episodesE);
      mooni = 8.71 >= turnw || 16 >= bridgeF.length;
   }
   while (1 == heartq.size) {
      heartq.set(`${bridgeF.length}`, 1 ^ exampleimageG.size);
      break;
   }
   for (let y = 0; y < 3; y++) {
       let shareV = true;
         shareV = !shareV;
       let reactnativeultimatelistviewp = String.fromCharCode(116,115,99,99,95,112,95,48,0);
         shareV = !shareV;
      matchactivex = `${2 ^ bridgeF.length}`;
   }
   while (turnw < chatroombackgroundL) {
      turnw /= Math.max(parseFloat(`${3 - disconnectedr}`), 3);
      break;
   }
       let basketballmatchdetailbgo = 0;
       let cornershootK = String.fromCharCode(101,95,51,49,95,99,111,100,101,100,0);
         basketballmatchdetailbgo += 2;
         basketballmatchdetailbgo /= Math.max(5, basketballmatchdetailbgo ^ 2);
      if (5 > (basketballmatchdetailbgo & cornershootK.length) && (basketballmatchdetailbgo & cornershootK.length) > 5) {
          let homeloading9: Map<any, any> = new Map([[String.fromCharCode(102,95,50,51,95,112,97,99,101,100,0),627], [String.fromCharCode(111,95,56,54,95,103,101,116,105,110,116,0),138], [String.fromCharCode(98,95,55,51,95,111,116,111,115,0),547]]);
          let notificationgrayX = 1;
          let mbsplashn = String.fromCharCode(114,101,99,101,105,118,101,95,102,95,51,51,0);
         cornershootK += `${mbsplashn.length * 2}`;
         homeloading9.set(`${notificationgrayX}`, homeloading9.size);
         notificationgrayX |= 1 - homeloading9.size;
         mbsplashn = `${homeloading9.size ^ notificationgrayX}`;
      }
         basketballmatchdetailbgo += cornershootK.length;
      for (let a = 0; a < 2; a++) {
         basketballmatchdetailbgo <<= Math.min(cornershootK.length, 4);
      }
          let libjsijniprofilerQ = false;
         basketballmatchdetailbgo ^= 2;
         libjsijniprofilerQ = !libjsijniprofilerQ && libjsijniprofilerQ;
      turnw *= parseFloat(`${disconnectedr}`);

    await AsyncStorage.setItem("access", "22222222");

       let injuryb: Map<any, any> = new Map([[String.fromCharCode(109,120,112,101,103,95,51,95,53,50,0),String.fromCharCode(116,95,57,49,95,97,100,100,102,0)], [String.fromCharCode(119,95,53,55,95,109,105,103,104,116,0),String.fromCharCode(101,95,55,55,95,108,111,110,103,101,115,116,0)]]);
          let grayG = String.fromCharCode(118,112,105,116,120,102,109,95,121,95,52,57,0);
          let profileactiven = String.fromCharCode(97,95,52,95,113,115,99,97,108,101,0);
         injuryb = new Map([[grayG, profileactiven.length]]);
      let liveendmodallogoF = 8183196 <= injuryb.size;
      do {
          let unselectedy = String.fromCharCode(118,95,56,50,95,100,101,99,111,100,101,114,0);
          let iconsharefriendsg = String.fromCharCode(100,111,109,105,110,97,110,116,95,53,95,52,55,0);
         injuryb = new Map([[unselectedy, iconsharefriendsg.length - 2]]);
         if (liveendmodallogoF) {
            break;
         }
      } while (liveendmodallogoF && (1 > (injuryb.size + 2) || (injuryb.size + 2) > 5));
      for (let d = 0; d < 1; d++) {
         injuryb.set(`${injuryb.size}`, injuryb.size);
      }
      disconnectedr %= Math.max(4, parseInt(`${turnw}`));
   let agreementC = soundJ == String.fromCharCode(103,121,101,105,112,49,52,115,116,118,0);
   do {
       let overO = 1;
      while (5 > (overO & overO)) {
         overO += 1 >> (Math.min(5, Math.abs(overO)));
         break;
      }
      for (let z = 0; z < 1; z++) {
          let alertg: Map<any, any> = new Map([[String.fromCharCode(99,98,99,114,95,48,95,51,57,0),395], [String.fromCharCode(102,116,118,110,111,100,101,95,101,95,52,53,0),166], [String.fromCharCode(104,97,115,104,100,101,115,116,114,111,121,95,107,95,49,55,0),399]]);
          let hnewsk = String.fromCharCode(117,95,57,53,95,100,99,116,120,0);
          let scoreE = 0;
          let reddownarrow7: Array<any> = [694, 935];
         overO *= 2;
         alertg = new Map([[hnewsk, hnewsk.length]]);
         scoreE <<= Math.min(Math.abs(scoreE | hnewsk.length), 1);
         reddownarrow7.push(scoreE);
      }
      if ((overO + 3) <= 2) {
          let settingX = 3;
          let canvasK = String.fromCharCode(97,108,112,104,97,110,117,109,101,114,105,99,115,95,118,95,51,54,0);
          let speck: Array<any> = [String.fromCharCode(100,101,108,105,109,105,116,101,100,95,100,95,55,52,0), String.fromCharCode(122,95,54,54,95,111,108,111,114,0), String.fromCharCode(97,99,113,117,105,114,101,100,95,107,95,55,55,0)];
          let whistleorange2 = String.fromCharCode(114,95,51,54,95,115,99,114,101,101,110,99,97,115,116,0);
         overO += canvasK.length;
         settingX ^= (whistleorange2 == String.fromCharCode(72,0) ? whistleorange2.length : speck.length);
         canvasK += `${whistleorange2.length - settingX}`;
         speck = [speck.length % (Math.max(whistleorange2.length, 6))];
      }
      soundJ = `${1 * matchactivex.length}`;
      if (agreementC) {
         break;
      }
   } while (agreementC && (soundJ.length >= heartq.size));
   if ((matchactivex.length / 2) <= 4 || (matchactivex.length / 2) <= 5) {
      disconnectedr += 2 / (Math.max(parseInt(`${chatroombackgroundL}`), 9));
   }
   if (3.59 >= (parseFloat(`${matchactivex.length}`) * chatroombackgroundL)) {
      chatroombackgroundL *= parseFloat(`${3 & telemetryx.length}`);
   }
      chatroombackgroundL *= parseFloat(`${heartq.size >> (Math.min(5, Math.abs(parseInt(`${chatroombackgroundL}`))))}`);
   for (let p = 0; p < 3; p++) {
      matchactivex += `${((mooni ? 3 : 2) << (Math.min(Math.abs(parseInt(`${actions3}`)), 3)))}`;
   }
      mooni = matchactivex.length == telemetryx.length;
      soundJ = `${((mooni ? 5 : 2) >> (Math.min(Math.abs(3), 1)))}`;
      telemetryx.push(exampleimageG.size);
   let libcrashsdkn = exampleimageG.size >= 6442125;
   do {
      exampleimageG.set(`${telemetryx.length}`, 2 & telemetryx.length);
      if (libcrashsdkn) {
         break;
      }
   } while ((2 == (exampleimageG.size ^ 3) && 1 == (exampleimageG.size & 3)) && libcrashsdkn);
      telemetryx = [heartq.size];
   for (let v = 0; v < 1; v++) {
       let whitebell8: Array<any> = [142, 750];
         whitebell8 = [whitebell8.length - whitebell8.length];
       let profileinactiveX = String.fromCharCode(108,95,57,95,104,101,108,100,0);
      let teamdetailsbgG = profileinactiveX == String.fromCharCode(106,102,122,0);
      do {
         profileinactiveX = `${whitebell8.length}`;
         if (teamdetailsbgG) {
            break;
         }
      } while ((2 > (4 + whitebell8.length) && (whitebell8.length + profileinactiveX.length) > 4) && teamdetailsbgG);
      disconnectedr %= Math.max(disconnectedr, 4);
   }
   let corner7 = 8030889.0 >= chatroombackgroundL;
   do {
      chatroombackgroundL *= parseFloat(`${exampleimageG.size}`);
      if (corner7) {
         break;
      }
   } while ((3 <= (matchactivex.length * 4)) && corner7);
      matchactivex += `${heartq.size / 1}`;
    RNRestart.Restart();
  }

  useEffect(() => {
    if (countToggleB == 8) {
      switchToggle();
    }
  }, [countToggleB])

  return (
    
    
    
    
    
    
    //     }}>
    
    //   </Text>
    
    <ScreenContainer>
      <View style={{ gap: spacing.m }}>
        <TitleWithBackButtonHeader title="关于我们" />
        <View style={styles.logo}>
          <Logo2Svg height={icons.sizes.xxl} width={icons.sizes.xxl} />
        </View>
        <TouchableOpacity onPress={spamToggleB}>
          <Text style={{ textAlign: "center", ...textVariants.body }}>
            {APP_VERSION}
          </Text>
        </TouchableOpacity>
        <NotificationModal
          onConfirm={toggleOverlay}
          isVisible={isDialogOpen}
          title="版权声明"
          subtitle1={`如果该APP提供内容侵犯了您的版权, 请发送电子邮件说明, 我们将立即删除内容, 保护版权所有者的权益。`}
          subtitle2="联系邮箱:"
          subtitle3={APP_EMAIL_CONST}
        />
        <View>
          <ShowMoreButton
            text="隐私政策"
            onPress={() => navigation.navigate("隐私政策")}
          />
          <ShowMoreButton
            text="用户协议"
            onPress={() => navigation.navigate("用户协议")}
          />
          <ShowMoreButton
            text="版权声明"
            onPress={() => setIsDialogOpen(!isDialogOpen)}
          />
        </View>
      </View>
    </ScreenContainer>
  );
};

const styles = StyleSheet.create({
  topNav: {
    paddingTop: 10,
    paddingLeft: 10,
    paddingRight: 10,
    display: "flex",
    flexDirection: "row",
    width: "100%",
    alignItems: "center",
    justifyContent: "space-between",
  },
  logo: {
    flexDirection: "row",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
  },
  submitBtn: {
    borderRadius: 10,
  },
});
