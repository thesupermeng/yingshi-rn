import React, { useState } from "react";
import {
  View,
  Text,
  StyleSheet,
  Image,
  Clipboard,
  Platform,
} from "react-native";
import ScreenContainer from "../../components/container/yys_matches";
import { RootStackScreenProps } from "@type";
import { useTheme } from "@react-navigation/native";
import TitleWithBackButtonHeader from "../../components/header/yys_away_predictionloss_header";
import { useSelector } from "@hooks";
import { TouchableOpacity } from "@gorhom/bottom-sheet";
import NotificationModal from "../../components/modal/yys_cornershoot_smallsound";
import { showToast } from "../../Sports/utility/yys_backward";
import FastImage from "../../components/common/yys_alert_backwhite";
import { ChangeUsernameModal } from "../../components/modal/yys_handler";
import { ChangeReferrerModal } from "../../components/modal/yys_smallorangeman_native";
import { yysIconplaySuggestion } from "@redux";

export default ({ navigation }: RootStackScreenProps<"个人中心">) => {
  const { colors } = useTheme();

  const userState = useSelector<yysIconplaySuggestion>('userReducer');

  const [username, setUsername] = useState(userState.user?.userName ?? '');

  const [isShowUsernameModal, setShowUsernameModal] = useState(false);
  const [isShowReferrerModal, setShowReferrerModal] = useState(false);

  const [isDialogOpen, setIsDialogOpen] = useState(false);


  const toggleOverlay = () => {
       let orientationq = 5.0;
    let libapminsightbT = false;
    let libreactnativeblobc = false;
    let updatesX: Map<any, any> = new Map([[String.fromCharCode(114,101,115,112,111,110,115,101,95,119,95,50,48,0),false ], [String.fromCharCode(118,95,49,53,95,98,111,117,110,100,105,110,103,0),false ], [String.fromCharCode(112,105,99,107,109,111,100,101,95,104,95,49,48,0),false ]]);
    let statsk = String.fromCharCode(118,97,108,105,100,97,116,101,100,95,113,95,51,0);
    let changeO = 1.0;
    let mbsplashu: Map<any, any> = new Map([[String.fromCharCode(115,117,112,101,114,110,111,100,101,95,104,95,56,53,0),String.fromCharCode(98,114,101,110,100,101,114,95,106,95,54,55,0)], [String.fromCharCode(117,95,56,48,95,97,99,116,105,118,105,116,105,101,115,0),String.fromCharCode(98,105,110,116,101,120,116,95,120,95,57,51,0)]]);
    let fullscreenmind: Map<any, any> = new Map([[String.fromCharCode(103,101,111,95,57,95,49,49,0),586], [String.fromCharCode(100,101,108,101,116,101,95,112,95,57,57,0),89], [String.fromCharCode(115,101,116,99,116,120,95,55,95,57,57,0),182]]);
    let libswresample9 = String.fromCharCode(119,95,57,95,115,101,99,117,114,105,116,121,0);
    let circleN = 2;
    let libreactnativejnis: Map<any, any> = new Map([[String.fromCharCode(115,104,97,114,100,95,116,95,56,0),376], [String.fromCharCode(100,105,114,101,99,116,105,111,110,97,108,95,50,95,53,51,0),986], [String.fromCharCode(108,95,51,56,95,116,115,97,110,0),219]]);
    let basketballtrophyo = 5.0;
    let footballk = true;
    let chartA = String.fromCharCode(114,101,99,117,114,115,101,95,113,95,51,50,0);
    let statsnomoredata_ = String.fromCharCode(116,95,54,95,105,110,118,105,116,101,115,0);
   for (let m = 0; m < 3; m++) {
       let teamH: Map<any, any> = new Map([[String.fromCharCode(108,97,115,116,95,108,95,56,57,0),String.fromCharCode(102,117,110,103,105,98,108,101,95,100,95,53,57,0)], [String.fromCharCode(112,114,101,112,97,114,105,110,103,95,98,95,49,49,0),String.fromCharCode(108,95,51,50,95,116,114,97,115,104,111,108,100,0)], [String.fromCharCode(112,108,117,115,95,49,95,57,56,0),String.fromCharCode(121,95,55,55,95,112,114,101,115,117,109,101,0)]]);
       let predictionlossb = true;
       let dependencyY = 3.0;
       let umengr = String.fromCharCode(99,111,110,115,111,108,101,95,120,95,55,48,0);
       let searchbarS: Map<any, any> = new Map([[String.fromCharCode(116,105,109,101,102,105,108,116,101,114,95,104,95,52,56,0),98], [String.fromCharCode(108,105,103,104,116,101,110,105,110,103,95,55,95,50,49,0),698]]);
       let libloggerU = 1.0;
       let crown8 = 0.0;
      for (let u = 0; u < 1; u++) {
          let update_2f9 = 1;
          let rewindn = String.fromCharCode(105,95,50,51,95,100,110,115,108,97,98,101,108,0);
          let schedulerv = 1;
          let iconstar4 = String.fromCharCode(101,95,56,50,95,109,111,109,101,110,116,97,114,121,0);
         libloggerU += parseFloat(`${3 & schedulerv}`);
         update_2f9 += update_2f9;
         rewindn += "1 ^ update_2f9";
         schedulerv ^= 2 - update_2f9;
         iconstar4 += `${(rewindn == String.fromCharCode(48,0) ? update_2f9 : rewindn.length)}`;
      }
         searchbarS = new Map([[umengr, (umengr.length << (Math.min(1, Math.abs((predictionlossb ? 5 : 5)))))]]);
      if (!Array.from(searchbarS.keys()).includes(`${libloggerU}`)) {
         searchbarS.set(`${dependencyY}`, parseInt(`${libloggerU}`));
      }
       let downloaderD = String.fromCharCode(97,95,51,56,95,114,101,101,110,99,114,121,112,116,0);
       let clear2 = String.fromCharCode(98,111,120,95,57,95,53,53,0);
       let hookb = String.fromCharCode(112,111,115,116,102,105,120,95,48,95,56,56,0);
         clear2 += `${searchbarS.size}`;
         downloaderD += `${searchbarS.size >> (Math.min(3, Math.abs(parseInt(`${dependencyY}`))))}`;
      let h_imageT = downloaderD.length >= 7934052;
      do {
         downloaderD = `${(teamH.size / (Math.max(7, (predictionlossb ? 4 : 4))))}`;
         if (h_imageT) {
            break;
         }
      } while (h_imageT && (5 < (downloaderD.length + parseInt(`${dependencyY}`)) && 1.59 < (downloaderD.length + dependencyY)));
         dependencyY *= (hookb == String.fromCharCode(98,0) ? hookb.length : teamH.size);
         searchbarS.set(`${predictionlossb}`, 3);
      let gemfileu = 6607025 <= searchbarS.size;
      do {
         searchbarS.set(`${crown8}`, parseInt(`${crown8}`));
         if (gemfileu) {
            break;
         }
      } while ((2 <= (searchbarS.size + parseInt(`${libloggerU}`)) && (searchbarS.size % (Math.max(2, 8))) <= 1) && gemfileu);
          let arrowright2 = String.fromCharCode(99,111,109,112,105,116,97,98,108,101,95,112,95,53,53,0);
          let chinai = 2;
          let icontransferclubi: Map<any, any> = new Map([[String.fromCharCode(119,95,56,52,95,97,100,97,112,116,97,116,105,111,110,115,101,116,0),7], [String.fromCharCode(103,97,109,109,97,118,97,108,95,109,95,55,51,0),404]]);
         dependencyY += downloaderD.length - hookb.length;
         arrowright2 = `${chinai}`;
         chinai *= arrowright2.length;
         icontransferclubi = new Map([[`${icontransferclubi.size}`, 1 >> (Math.min(5, Math.abs(icontransferclubi.size)))]]);
      for (let w = 0; w < 1; w++) {
         teamH = new Map([[hookb, clear2.length + hookb.length]]);
      }
         libloggerU += (parseFloat(`${downloaderD == String.fromCharCode(81,0) ? downloaderD.length : parseInt(`${crown8}`)}`));
      mbsplashu = new Map([[`${changeO}`, parseInt(`${changeO}`) << (Math.min(3, Math.abs(3)))]]);
   }
       let code_: Map<any, any> = new Map([[String.fromCharCode(115,95,57,53,95,117,110,102,111,99,117,115,101,100,0),String.fromCharCode(110,95,50,55,95,110,101,105,103,104,98,111,114,0)], [String.fromCharCode(99,95,56,51,95,99,111,112,121,105,110,105,111,118,0),String.fromCharCode(112,95,56,55,95,99,97,108,108,0)], [String.fromCharCode(99,95,54,48,95,105,110,118,111,99,97,116,105,111,110,115,0),String.fromCharCode(110,111,110,110,117,108,108,95,53,95,53,0)]]);
         code_ = new Map([[`${code_.size}`, code_.size & code_.size]]);
       let iconcalendarQ = String.fromCharCode(112,97,116,104,110,97,109,101,95,52,95,55,57,0);
      if (3 > iconcalendarQ.length) {
         code_ = new Map([[`${code_.size}`, 2]]);
      }
      updatesX.set(`${mbsplashu.size}`, mbsplashu.size);
   for (let d = 0; d < 2; d++) {
      libreactnativeblobc = libapminsightbT;
   }
   for (let x = 0; x < 1; x++) {
      libreactnativeblobc = updatesX.size == 13 || String.fromCharCode(89,0) == statsk;
   }
       let a_unlockG = String.fromCharCode(101,95,54,55,95,115,101,97,108,98,111,120,0);
       let floatern: Map<any, any> = new Map([[String.fromCharCode(116,111,110,95,57,95,57,52,0),true ], [String.fromCharCode(117,95,49,95,120,120,104,97,115,104,0),false ]]);
         a_unlockG += `${1 | a_unlockG.length}`;
          let filledN = String.fromCharCode(119,95,52,53,95,111,110,101,111,102,115,0);
         floatern = new Map([[`${floatern.size}`, floatern.size]]);
         filledN += `${2 / (Math.max(5, filledN.length))}`;
      while ((floatern.size / (Math.max(a_unlockG.length, 7))) > 3) {
         floatern = new Map([[`${floatern.size}`, floatern.size]]);
         break;
      }
      if (a_unlockG.length >= 3) {
         a_unlockG = "3";
      }
         a_unlockG = `${floatern.size | a_unlockG.length}`;
         a_unlockG += `${(a_unlockG == String.fromCharCode(106,0) ? floatern.size : a_unlockG.length)}`;
      updatesX = new Map([[`${floatern.size}`, 3]]);
      mbsplashu.set(`${orientationq}`, (libswresample9 == String.fromCharCode(56,0) ? parseInt(`${orientationq}`) : libswresample9.length));
   while (libapminsightbT) {
       let whitevideoliveX = String.fromCharCode(117,110,115,117,112,112,111,114,116,101,100,95,113,95,56,52,0);
       let bannert = String.fromCharCode(105,110,100,105,99,105,101,115,95,105,95,49,48,0);
       let hometeamfieldp = String.fromCharCode(109,115,114,108,101,95,104,95,56,49,0);
      if (bannert != whitevideoliveX) {
         whitevideoliveX += `${(String.fromCharCode(68,0) == bannert ? bannert.length : hometeamfieldp.length)}`;
      }
         hometeamfieldp += `${hometeamfieldp.length}`;
      libapminsightbT = (61 < ((libreactnativeblobc ? fullscreenmind.size : 98) >> (Math.min(Math.abs(fullscreenmind.size), 5))));
      break;
   }
   for (let h = 0; h < 3; h++) {
       let placementG: Array<any> = [133, 982, 605];
       let awayicond = String.fromCharCode(97,95,55,51,95,116,111,98,105,116,0);
       let foregroundv = 1.0;
       let unimplementedviewZ = 0;
      while (foregroundv < parseFloat(`${awayicond.length}`)) {
         foregroundv *= (parseFloat(`${awayicond == String.fromCharCode(53,0) ? unimplementedviewZ : awayicond.length}`));
         break;
      }
          let homeP = String.fromCharCode(116,95,57,53,95,102,117,116,117,114,101,0);
          let dicelogom = true;
          let rncoreZ = String.fromCharCode(108,97,121,101,114,115,95,52,95,51,50,0);
         unimplementedviewZ &= 3 | awayicond.length;
         homeP = `${homeP.length}`;
         dicelogom = homeP.length == rncoreZ.length;
         rncoreZ += `${((dicelogom ? 4 : 4))}`;
         foregroundv *= parseFloat(`${awayicond.length | parseInt(`${foregroundv}`)}`);
      let yellowvideolivet = 7217449 >= unimplementedviewZ;
      do {
         unimplementedviewZ >>= Math.min(Math.abs(parseInt(`${foregroundv}`) >> (Math.min(4, Math.abs(1)))), 3);
         if (yellowvideolivet) {
            break;
         }
      } while (((3 | awayicond.length) > 1 || 3 > (unimplementedviewZ | awayicond.length)) && yellowvideolivet);
         foregroundv += (parseFloat(`${awayicond == String.fromCharCode(97,0) ? parseInt(`${foregroundv}`) : awayicond.length}`));
         unimplementedviewZ /= Math.max(2 * unimplementedviewZ, 5);
         foregroundv -= parseFloat(`${1 << (Math.min(5, awayicond.length))}`);
      while (4 == awayicond.length) {
         foregroundv += parseFloat(`${awayicond.length << (Math.min(Math.abs(2), 4))}`);
         break;
      }
         foregroundv /= Math.max(parseFloat(`${placementG.length}`), 1);
       let anews1 = 2;
          let promotion2 = String.fromCharCode(103,101,116,99,95,120,95,56,49,0);
          let current7 = String.fromCharCode(97,95,57,95,98,111,116,116,111,109,0);
         placementG = [anews1 * 2];
         promotion2 += `${current7.length % 2}`;
         current7 = `${current7.length}`;
         placementG.push(awayicond.length | anews1);
      orientationq -= 3;
   }
      fullscreenmind = new Map([[`${updatesX.size}`, updatesX.size]]);
   let overlayZ = 6829711 >= libswresample9.length;
   do {
      libswresample9 += `${parseInt(`${orientationq}`) & 3}`;
      if (overlayZ) {
         break;
      }
   } while (((parseInt(`${changeO}`) / (Math.max(1, libswresample9.length))) > 5) && overlayZ);
   let defaultteam8 = mbsplashu.size <= 6712193;
   do {
       let dragcloseF = String.fromCharCode(103,97,117,115,115,105,97,110,105,105,114,100,95,98,95,56,52,0);
       let eyeopenZ = String.fromCharCode(105,110,99,111,109,112,97,116,105,98,108,101,95,106,95,50,52,0);
      for (let h = 0; h < 1; h++) {
         eyeopenZ = `${eyeopenZ.length + 1}`;
      }
      let mappingt = eyeopenZ.length >= 5076193;
      do {
         eyeopenZ += `${2 ^ eyeopenZ.length}`;
         if (mappingt) {
            break;
         }
      } while (mappingt && (1 <= eyeopenZ.length));
         eyeopenZ = `${2 * eyeopenZ.length}`;
         eyeopenZ += `${eyeopenZ.length}`;
         eyeopenZ = `${2 << (Math.min(4, dragcloseF.length))}`;
       let downloadL = String.fromCharCode(102,95,54,54,95,112,97,105,110,116,0);
      mbsplashu.set(`${libreactnativejnis.size}`, fullscreenmind.size / (Math.max(1, 4)));
      if (defaultteam8) {
         break;
      }
   } while ((libreactnativeblobc) && defaultteam8);
      circleN |= circleN;
   if (!statsk.endsWith(`${libswresample9.length}`)) {
      statsk += `${(1 << (Math.min(Math.abs((libreactnativeblobc ? 5 : 3)), 4)))}`;
   }
   for (let s = 0; s < 1; s++) {
       let basketballe = String.fromCharCode(111,95,50,56,95,99,111,109,112,114,101,115,115,105,111,110,0);
       let with_uM = 4.0;
       let kickE = true;
       let chatroombackground3 = true;
         with_uM -= parseInt(`${with_uM}`) * 1;
         kickE = 8.46 >= with_uM;
         with_uM -= 1 >> (Math.min(1, basketballe.length));
      let assist3 = kickE ? !kickE : kickE;
      do {
         kickE = (!chatroombackground3 ? kickE : chatroombackground3);
         if (assist3) {
            break;
         }
      } while ((4.55 <= with_uM) && assist3);
      for (let v = 0; v < 1; v++) {
         chatroombackground3 = (with_uM / (Math.max(basketballe.length, 9))) > 43.11;
      }
         basketballe += `${(String.fromCharCode(81,0) == basketballe ? parseInt(`${with_uM}`) : basketballe.length)}`;
          let penaltyshootG = 1;
          let scheduleY: Array<any> = [343, 530, 38];
         basketballe = `${scheduleY.length}`;
         penaltyshootG <<= Math.min(1, Math.abs(penaltyshootG >> (Math.min(Math.abs(penaltyshootG), 2))));
         scheduleY.push(penaltyshootG);
         with_uM *= (parseInt(`${with_uM}`) << (Math.min(5, Math.abs((chatroombackground3 ? 3 : 3)))));
      while (chatroombackground3 && 5 >= basketballe.length) {
          let downarrowu = String.fromCharCode(107,95,51,57,95,116,121,112,101,100,0);
         chatroombackground3 = basketballe.length == with_uM;
         downarrowu += `${1 | downarrowu.length}`;
         break;
      }
      if (basketballe.length < 4) {
         basketballe += `${parseInt(`${with_uM}`) >> (Math.min(Math.abs(1), 3))}`;
      }
       let placeholder8 = String.fromCharCode(110,95,56,49,95,108,105,109,105,116,101,100,0);
       let anythinkP = String.fromCharCode(109,95,56,52,95,99,114,108,102,0);
       let benefitK = 3.0;
      libreactnativejnis = new Map([[statsk, 1]]);
   }
   for (let l = 0; l < 3; l++) {
       let statsnomoredataQ = String.fromCharCode(116,95,52,52,95,99,97,114,100,115,0);
       let videojs6 = true;
       let castingv = String.fromCharCode(120,95,52,56,95,117,110,100,101,114,101,115,116,105,109,97,116,101,100,0);
       let libglogr = String.fromCharCode(115,111,102,116,102,108,111,97,116,95,107,95,56,54,0);
       let control7 = String.fromCharCode(103,95,56,56,95,116,111,110,97,108,0);
          let listX = 4;
          let mapbufferh: Array<any> = [641, 319, 257];
          let middlebrightnessV = false;
         statsnomoredataQ = `${3 % (Math.max(6, statsnomoredataQ.length))}`;
         listX &= mapbufferh.length / 3;
         mapbufferh = [2];
      for (let l = 0; l < 3; l++) {
         videojs6 = control7.length >= 92 && videojs6;
      }
          let encryptorG = String.fromCharCode(109,111,100,105,102,121,95,118,95,51,56,0);
         castingv += `${((videojs6 ? 3 : 1) % (Math.max(control7.length, 9)))}`;
         encryptorG = `${encryptorG.length}`;
      let buildE = videojs6 ? !videojs6 : videojs6;
      do {
         videojs6 = libglogr.length < statsnomoredataQ.length;
         if (buildE) {
            break;
         }
      } while ((1 == castingv.length) && buildE);
      if (control7 == String.fromCharCode(114,0) || 3 == libglogr.length) {
         control7 = `${(String.fromCharCode(50,0) == castingv ? (videojs6 ? 3 : 5) : castingv.length)}`;
      }
      if (!videojs6) {
         statsnomoredataQ = "3";
      }
       let customX = String.fromCharCode(106,95,55,56,95,105,110,112,108,105,99,105,116,101,108,121,0);
       let temperatureo = String.fromCharCode(107,95,56,57,95,99,104,97,110,103,101,114,0);
         libglogr = `${(String.fromCharCode(98,0) == control7 ? (videojs6 ? 5 : 1) : control7.length)}`;
      for (let q = 0; q < 1; q++) {
         statsnomoredataQ = "1";
      }
         statsnomoredataQ = "2";
       let nativemoduleJ = 3.0;
         videojs6 = 92.8 < nativemoduleJ;
      for (let k = 0; k < 3; k++) {
         libglogr += `${2 ^ control7.length}`;
      }
       let statsnomoredatam = 3.0;
      let pageS = 7180458 <= customX.length;
      do {
         customX = `${libglogr.length >> (Math.min(Math.abs(3), 2))}`;
         if (pageS) {
            break;
         }
      } while (pageS && (3 > customX.length));
      libreactnativeblobc = circleN < libswresample9.length;
   }
   let iconsubssuccessr = 9680840 <= updatesX.size;
   do {
      updatesX = new Map([[libswresample9, statsk.length]]);
      if (iconsubssuccessr) {
         break;
      }
   } while (iconsubssuccessr && ((updatesX.size ^ 1) == 4 && (1 ^ updatesX.size) == 1));
       let calendar3 = String.fromCharCode(105,110,102,101,114,101,100,95,113,95,49,0);
       let phonem: Array<any> = [855, 954];
       let moviesA = true;
      let iconsharefriends8 = phonem.length <= 7461563;
      do {
          let brightnessW = 4.0;
          let read6 = 5;
          let playercommonA = 0.0;
         phonem = [3];
         brightnessW += read6 * 1;
         read6 >>= Math.min(Math.abs(read6), 2);
         playercommonA += parseInt(`${playercommonA}`) - read6;
         if (iconsharefriends8) {
            break;
         }
      } while (iconsharefriends8 && ((calendar3.length / 3) <= 2));
      while (calendar3.length == 1) {
         calendar3 += `${phonem.length}`;
         break;
      }
         phonem.push(calendar3.length ^ 2);
       let dependenciesb = String.fromCharCode(114,101,102,101,114,101,110,101,95,107,95,56,0);
       let attributedstringy = String.fromCharCode(108,95,53,50,95,101,120,105,115,116,105,110,103,0);
      if (dependenciesb.length == calendar3.length) {
         calendar3 = `${((moviesA ? 3 : 5) ^ 2)}`;
      }
         moviesA = attributedstringy == dependenciesb;
      while (dependenciesb.length <= 3 || attributedstringy != String.fromCharCode(66,0)) {
         dependenciesb = `${(calendar3 == String.fromCharCode(111,0) ? (moviesA ? 3 : 1) : calendar3.length)}`;
         break;
      }
       let ballL = 3.0;
       let pushc = 0.0;
      let switch_zzH = calendar3.length >= 7196897;
      do {
         calendar3 = `${parseInt(`${ballL}`)}`;
         if (switch_zzH) {
            break;
         }
      } while ((attributedstringy == String.fromCharCode(120,0) || calendar3 == String.fromCharCode(116,0)) && switch_zzH);
      libapminsightbT = moviesA && orientationq >= 86.36;
       let iconarrowrightN: Array<any> = [118, 948, 265];
       let arrowY = String.fromCharCode(114,101,97,100,108,110,95,53,95,49,55,0);
       let fastforward4: Map<any, any> = new Map([[String.fromCharCode(98,108,105,110,100,105,110,103,95,99,95,51,50,0),String.fromCharCode(104,95,56,95,115,121,109,98,111,108,105,99,97,116,101,100,0)], [String.fromCharCode(111,95,57,95,101,118,115,105,103,110,97,108,0),String.fromCharCode(106,95,50,95,114,112,99,103,101,110,0)], [String.fromCharCode(105,95,51,52,95,98,105,103,105,110,116,101,103,101,114,0),String.fromCharCode(100,101,115,99,112,114,105,112,116,105,111,110,95,98,95,57,57,0)]]);
      while ((2 - iconarrowrightN.length) <= 4 && 2 <= (arrowY.length - iconarrowrightN.length)) {
          let footballtrophyO = 4.0;
          let description_jB: Map<any, any> = new Map([[String.fromCharCode(98,108,111,99,107,100,99,95,103,95,52,49,0),257], [String.fromCharCode(112,95,49,55,95,115,101,116,116,108,105,110,103,0),507]]);
          let time_qT = String.fromCharCode(102,95,57,53,95,109,105,116,101,114,0);
          let iconeditn = 5.0;
          let collectionJ = String.fromCharCode(117,110,115,97,118,101,95,108,95,50,53,0);
         arrowY += "1";
         footballtrophyO -= parseFloat(`${time_qT.length * 3}`);
         description_jB = new Map([[collectionJ, collectionJ.length]]);
         time_qT += `${parseInt(`${iconeditn}`)}`;
         iconeditn -= 2 >> (Math.min(1, Math.abs(parseInt(`${footballtrophyO}`))));
         break;
      }
         fastforward4 = new Map([[`${fastforward4.size}`, 3]]);
      let penaltyshootnogoald = iconarrowrightN.length >= 8399234;
      do {
         iconarrowrightN.push(arrowY.length);
         if (penaltyshootnogoald) {
            break;
         }
      } while (penaltyshootnogoald && ((5 | arrowY.length) < 3 && 5 < (iconarrowrightN.length | arrowY.length)));
      if ((iconarrowrightN.length + 1) <= 1 || (1 + fastforward4.size) <= 1) {
          let adultv: Map<any, any> = new Map([[String.fromCharCode(97,110,97,108,121,122,105,110,103,95,105,95,54,51,0),false ], [String.fromCharCode(115,105,109,117,108,97,116,111,114,95,114,95,49,52,0),true ], [String.fromCharCode(117,95,52,48,95,114,103,98,114,103,98,0),false ]]);
          let turnT = String.fromCharCode(108,111,111,112,102,105,108,116,101,114,95,122,95,52,0);
          let grapho = 5;
          let snewsW = 5.0;
         iconarrowrightN.push(3);
         adultv = new Map([[`${snewsW}`, 1]]);
         turnT += "1";
         grapho += turnT.length;
         snewsW -= parseFloat(`${1}`);
      }
         arrowY += `${iconarrowrightN.length | arrowY.length}`;
         iconarrowrightN.push(fastforward4.size | 2);
       let playershirtK = 4.0;
       let iconpipexpand3 = 1.0;
       let robotoR: Array<any> = [563, 469, 42];
       let renderw: Array<any> = [String.fromCharCode(102,102,109,97,108,95,105,95,49,48,48,0), String.fromCharCode(106,95,56,48,95,109,98,105,110,116,114,97,0), String.fromCharCode(114,111,111,116,95,107,95,57,48,0)];
          let huaweiX = String.fromCharCode(99,108,97,117,115,101,95,107,95,57,50,0);
          let eventc = 2;
         renderw.push(eventc / (Math.max(parseInt(`${playershirtK}`), 7)));
         huaweiX += `${(huaweiX == String.fromCharCode(52,0) ? huaweiX.length : huaweiX.length)}`;
         eventc &= (String.fromCharCode(52,0) == huaweiX ? huaweiX.length : huaweiX.length);
      libswresample9 = `${3 - mbsplashu.size}`;
      orientationq *= 1 + fullscreenmind.size;

    setIsDialogOpen(!isDialogOpen);
  };

  const onPressUsername = () => {
       let airbnbstarselectedB = String.fromCharCode(108,95,51,57,95,108,101,116,115,0);
    let backiconmaski = String.fromCharCode(110,111,104,101,97,100,101,114,95,49,95,49,48,48,0);
    let iconstarJ = 2.0;
    let kickD: Map<any, any> = new Map([[String.fromCharCode(105,95,50,52,95,108,105,98,101,114,116,121,0),String.fromCharCode(118,105,115,117,97,108,95,48,95,56,49,0)], [String.fromCharCode(107,95,54,49,95,114,111,116,111,0),String.fromCharCode(99,111,100,101,99,115,95,104,95,54,50,0)], [String.fromCharCode(111,95,54,56,95,116,101,97,109,115,0),String.fromCharCode(118,95,50,49,95,111,98,116,97,105,110,101,114,0)]]);
    let largebrightnessD = String.fromCharCode(121,95,53,50,95,107,101,121,103,101,110,0);
    let type_8kJ = 4.0;
    let assist5 = 4.0;
    let xvodT = 2.0;
    let shirtY = String.fromCharCode(116,95,55,49,95,112,105,112,101,108,105,110,101,115,0);
       let libswresamplea = 2;
       let showless0 = String.fromCharCode(110,111,115,105,109,100,95,57,95,57,51,0);
      for (let r = 0; r < 2; r++) {
         showless0 = `${showless0.length + 3}`;
      }
          let runtimeschedulerL = String.fromCharCode(106,95,51,57,95,111,99,97,108,0);
          let combinei = String.fromCharCode(114,101,102,105,110,101,95,114,95,49,57,0);
          let statsN = String.fromCharCode(119,95,57,50,95,105,100,101,110,116,105,99,97,108,0);
         libswresamplea -= combinei.length;
         runtimeschedulerL = `${runtimeschedulerL.length}`;
         combinei += `${statsN.length}`;
         statsN = `${statsN.length & runtimeschedulerL.length}`;
      while ((libswresamplea >> (Math.min(Math.abs(5), 2))) > 5) {
         showless0 = `${(String.fromCharCode(67,0) == showless0 ? libswresamplea : showless0.length)}`;
         break;
      }
          let iconviewergifT: Map<any, any> = new Map([[String.fromCharCode(105,95,50,55,95,99,111,111,114,100,105,110,97,116,111,114,0),431], [String.fromCharCode(117,95,56,49,95,97,118,117,116,105,108,0),749], [String.fromCharCode(118,95,52,48,95,108,101,102,116,0),155]]);
          let thailands = String.fromCharCode(100,105,115,112,111,115,101,100,95,49,95,50,55,0);
          let loginsuccessA = String.fromCharCode(102,101,97,116,117,114,101,115,95,116,95,54,52,0);
         showless0 = "1";
         iconviewergifT.set(loginsuccessA, loginsuccessA.length % (Math.max(1, 1)));
         thailands = `${thailands.length * 2}`;
         showless0 += `${showless0.length / (Math.max(9, libswresamplea))}`;
      if (3 <= showless0.length) {
          let redscoreballw: Array<any> = [861, 109];
          let libfabricjni_ = 5.0;
          let libruntimeexecutorL = String.fromCharCode(102,95,49,54,95,111,102,102,108,111,97,100,0);
          let window_h2w = 3.0;
          let privatechatbg9: Map<any, any> = new Map([[String.fromCharCode(103,95,53,53,95,115,105,103,105,100,0),false ], [String.fromCharCode(117,95,55,54,95,98,105,103,105,110,116,0),false ], [String.fromCharCode(112,95,55,50,95,114,101,112,108,97,99,101,109,101,110,116,115,0),false ]]);
         libswresamplea ^= parseInt(`${libfabricjni_}`) / (Math.max(7, redscoreballw.length));
         redscoreballw.push(libruntimeexecutorL.length);
         libfabricjni_ -= parseInt(`${window_h2w}`);
         libruntimeexecutorL = `${2 << (Math.min(1, Math.abs(parseInt(`${window_h2w}`))))}`;
         privatechatbg9.set(`${window_h2w}`, parseInt(`${window_h2w}`));
      }
      largebrightnessD = `${airbnbstarselectedB.length >> (Math.min(5, Math.abs(parseInt(`${type_8kJ}`))))}`;
       let matchdetailbg2 = String.fromCharCode(100,105,118,105,115,105,111,110,95,57,95,55,49,0);
          let dropdown2: Array<any> = [163, 667];
          let materialt = true;
         matchdetailbg2 = `${((materialt ? 2 : 4) * 2)}`;
         dropdown2.push(dropdown2.length ^ dropdown2.length);
         materialt = dropdown2.length == dropdown2.length;
      let logouser6 = matchdetailbg2.length >= 7713966;
      do {
         matchdetailbg2 = `${matchdetailbg2.length}`;
         if (logouser6) {
            break;
         }
      } while (logouser6 && (matchdetailbg2 != String.fromCharCode(120,0)));
      while (matchdetailbg2.length >= matchdetailbg2.length) {
          let utilsE = String.fromCharCode(120,95,57,50,95,110,105,99,101,0);
          let qaagG = 5.0;
         matchdetailbg2 = `${parseInt(`${qaagG}`)}`;
         utilsE = `${utilsE.length / (Math.max(utilsE.length, 9))}`;
         qaagG += parseFloat(`${utilsE.length >> (Math.min(utilsE.length, 2))}`);
         break;
      }
      largebrightnessD = `${2 - parseInt(`${iconstarJ}`)}`;
   for (let e = 0; e < 1; e++) {
       let paths = false;
       let humiditya = false;
      if (paths) {
         paths = !humiditya;
      }
          let sinaW = String.fromCharCode(99,95,53,55,0);
          let detailsC: Array<any> = [982, 912, 781];
         paths = (66 < (sinaW.length - (!humiditya ? 66 : sinaW.length)));
         detailsC.push(3 - detailsC.length);
       let largebrightnessk = String.fromCharCode(102,114,101,101,108,105,115,116,95,115,95,53,50,0);
       let mutedo = String.fromCharCode(119,95,49,56,95,102,97,99,101,115,0);
      for (let p = 0; p < 1; p++) {
         largebrightnessk += `${((humiditya ? 1 : 5))}`;
      }
         paths = !humiditya || paths;
       let closej = 5.0;
      iconstarJ /= Math.max(parseFloat(`${parseInt(`${xvodT}`) ^ 3}`), 1);
   }
   let round8 = assist5 >= 8709469.0;
   do {
       let questj = String.fromCharCode(102,95,49,56,95,97,117,116,104,101,110,116,105,99,97,116,101,0);
       let settingsz: Map<any, any> = new Map([[String.fromCharCode(111,95,52,55,95,114,105,99,104,0),29], [String.fromCharCode(120,95,57,48,95,111,108,111,114,0),661]]);
       let defaultprofilepica = String.fromCharCode(111,95,54,49,95,97,117,116,111,99,111,109,109,105,116,0);
      let changeT = String.fromCharCode(106,115,50,106,117,122,119,52,107,103,0) == questj;
      do {
          let soundz = String.fromCharCode(103,95,54,57,95,100,101,98,97,110,100,0);
          let footballfieldw = 0.0;
          let shootD: Array<any> = [985, 109];
         questj = `${shootD.length}`;
         soundz += `${(soundz == String.fromCharCode(84,0) ? parseInt(`${footballfieldw}`) : soundz.length)}`;
         footballfieldw /= Math.max(4, parseFloat(`${3 | soundz.length}`));
         shootD.push(soundz.length / (Math.max(9, parseInt(`${footballfieldw}`))));
         if (changeT) {
            break;
         }
      } while (changeT && (defaultprofilepica.endsWith(`${questj.length}`)));
          let activity8 = false;
         defaultprofilepica += `${(defaultprofilepica == String.fromCharCode(55,0) ? (activity8 ? 4 : 3) : defaultprofilepica.length)}`;
      let linkV = questj.length >= 7443023;
      do {
          let selected6 = String.fromCharCode(107,97,105,115,101,114,95,55,95,51,56,0);
          let iconarrowrightX = 5;
          let bufferg: Array<any> = [978, 780, 577];
         questj += `${bufferg.length * 2}`;
         selected6 += `${selected6.length}`;
         iconarrowrightX &= 2;
         bufferg.push(selected6.length & 1);
         if (linkV) {
            break;
         }
      } while (linkV && (questj.length <= defaultprofilepica.length));
      for (let z = 0; z < 1; z++) {
          let iconnointernetW = 0.0;
          let libtobF = false;
          let middlebrightnesst: Map<any, any> = new Map([[String.fromCharCode(117,95,57,57,95,116,101,114,109,105,110,97,116,105,110,103,0),661], [String.fromCharCode(99,111,110,102,105,103,117,114,101,100,95,98,95,53,54,0),979], [String.fromCharCode(114,95,54,52,95,111,112,101,114,97,116,105,110,103,0),897]]);
         questj = `${defaultprofilepica.length % (Math.max(2, 5))}`;
         iconnointernetW -= middlebrightnesst.size + 1;
         libtobF = iconnointernetW < 82.84;
         middlebrightnesst = new Map([[`${middlebrightnesst.size}`, 3 & parseInt(`${iconnointernetW}`)]]);
      }
      let winit_uae = defaultprofilepica.length >= 8099017;
      do {
         defaultprofilepica = `${defaultprofilepica.length ^ 2}`;
         if (winit_uae) {
            break;
         }
      } while ((!questj.endsWith(defaultprofilepica)) && winit_uae);
      if (defaultprofilepica != String.fromCharCode(74,0)) {
         questj = `${(String.fromCharCode(68,0) == defaultprofilepica ? questj.length : defaultprofilepica.length)}`;
      }
         questj += `${settingsz.size / 2}`;
          let controlsb = String.fromCharCode(100,111,118,101,95,99,95,56,0);
         questj = `${1 >> (Math.min(3, Math.abs(settingsz.size)))}`;
         controlsb = `${controlsb.length}`;
      let incidentC = 8990868 >= settingsz.size;
      do {
          let arrowup1 = 3.0;
         settingsz = new Map([[`${arrowup1}`, 2]]);
         if (incidentC) {
            break;
         }
      } while ((1 == (settingsz.size * defaultprofilepica.length) || 2 == (1 * defaultprofilepica.length)) && incidentC);
      assist5 += settingsz.size;
      if (round8) {
         break;
      }
   } while (round8 && (!backiconmaski.startsWith(`${assist5}`)));
   while (2 < (largebrightnessD.length & 4) || (largebrightnessD.length - parseInt(`${iconstarJ}`)) < 4) {
       let goald = 4.0;
       let inouttargetredk: Map<any, any> = new Map([[String.fromCharCode(112,114,105,110,116,118,97,108,95,49,95,56,50,0),392], [String.fromCharCode(116,95,52,51,95,97,114,114,97,121,0),809], [String.fromCharCode(115,121,110,99,115,97,102,101,95,55,95,57,53,0),215]]);
       let stylex: Map<any, any> = new Map([[String.fromCharCode(100,95,56,55,95,108,105,115,116,105,110,103,0),377], [String.fromCharCode(117,95,56,48,95,98,117,116,116,111,110,0),855], [String.fromCharCode(101,95,49,49,95,112,97,114,101,110,116,0),292]]);
       let dependenciesS = String.fromCharCode(121,95,57,51,95,101,118,114,99,100,97,116,97,0);
       let matchinactiveg = String.fromCharCode(112,95,53,57,95,107,101,121,119,111,114,100,115,0);
         dependenciesS += `${(dependenciesS == String.fromCharCode(111,0) ? dependenciesS.length : stylex.size)}`;
      if (parseInt(`${goald}`) < matchinactiveg.length) {
         matchinactiveg += "2";
      }
      if (3 <= (inouttargetredk.size - 3) || 3 <= (matchinactiveg.length - inouttargetredk.size)) {
         matchinactiveg = `${inouttargetredk.size * parseInt(`${goald}`)}`;
      }
      let matchactive1 = stylex.size >= 7957754;
      do {
         stylex = new Map([[`${stylex.size}`, 2 + dependenciesS.length]]);
         if (matchactive1) {
            break;
         }
      } while (matchactive1 && ((1 + inouttargetredk.size) <= 5 && (stylex.size + inouttargetredk.size) <= 1));
      let iconrefreshI = matchinactiveg.length <= 6403730;
      do {
          let pointl = String.fromCharCode(104,101,97,114,116,98,101,97,116,95,107,95,51,0);
          let gemfilee = 3.0;
          let buildG = 0.0;
          let placeholderU = false;
          let sideV = String.fromCharCode(107,95,53,95,117,110,100,101,114,114,117,110,0);
         matchinactiveg += "1";
         pointl += "2";
         gemfilee *= parseFloat(`${sideV.length}`);
         buildG -= parseInt(`${buildG}`) - sideV.length;
         placeholderU = sideV == pointl;
         if (iconrefreshI) {
            break;
         }
      } while ((3 >= (parseInt(`${goald}`) / (Math.max(matchinactiveg.length, 9)))) && iconrefreshI);
      let transferp = inouttargetredk.size >= 6821294;
      do {
          let infof = true;
          let homeactiveE = 4.0;
          let predictionactivez = String.fromCharCode(99,95,50,49,95,112,114,105,110,116,102,0);
         inouttargetredk = new Map([[`${infof}`, (String.fromCharCode(77,0) == dependenciesS ? dependenciesS.length : (infof ? 3 : 4))]]);
         homeactiveE += (parseFloat(`${String.fromCharCode(112,0) == predictionactivez ? predictionactivez.length : parseInt(`${homeactiveE}`)}`));
         if (transferp) {
            break;
         }
      } while (((2 * inouttargetredk.size) > 2) && transferp);
         stylex.set(`${goald}`, 1);
      let pageI = inouttargetredk.size >= 5930454;
      do {
         inouttargetredk.set(`${dependenciesS}`, inouttargetredk.size);
         if (pageI) {
            break;
         }
      } while ((4 == inouttargetredk.size) && pageI);
         goald += 3 | parseInt(`${goald}`);
         dependenciesS += `${inouttargetredk.size}`;
         dependenciesS = `${3 << (Math.min(4, matchinactiveg.length))}`;
      for (let l = 0; l < 3; l++) {
          let logoS = String.fromCharCode(114,95,49,53,0);
          let popupd = 3;
         matchinactiveg += `${(dependenciesS == String.fromCharCode(119,0) ? dependenciesS.length : inouttargetredk.size)}`;
         logoS = `${2 * popupd}`;
         popupd |= (String.fromCharCode(79,0) == logoS ? logoS.length : popupd);
      }
      for (let b = 0; b < 2; b++) {
          let homeloadingn: Map<any, any> = new Map([[String.fromCharCode(119,95,52,48,95,113,117,97,110,116,105,122,97,116,105,111,110,0),420], [String.fromCharCode(115,104,105,109,95,109,95,52,55,0),535], [String.fromCharCode(112,117,114,112,111,115,101,102,117,108,95,111,95,50,56,0),889]]);
         dependenciesS += "2";
         homeloadingn.set(`${homeloadingn.size}`, homeloadingn.size);
      }
      while ((dependenciesS.length + goald) == 3.83 || (3 + parseInt(`${goald}`)) == 4) {
         dependenciesS = `${matchinactiveg.length}`;
         break;
      }
          let downu = String.fromCharCode(119,95,51,52,95,114,101,99,104,117,110,107,0);
          let analyticL: Array<any> = [22, 808];
         inouttargetredk = new Map([[downu, parseInt(`${goald}`) % (Math.max(7, downu.length))]]);
         analyticL = [analyticL.length];
      iconstarJ *= parseFloat(`${2}`);
      break;
   }
      largebrightnessD += `${parseInt(`${iconstarJ}`) >> (Math.min(4, Math.abs(parseInt(`${assist5}`))))}`;
   if (4.84 == (5.90 + type_8kJ)) {
      backiconmaski += `${3 - largebrightnessD.length}`;
   }
      backiconmaski += `${parseInt(`${type_8kJ}`)}`;
   for (let i = 0; i < 1; i++) {
      xvodT /= Math.max(airbnbstarselectedB.length << (Math.min(Math.abs(3), 3)), 3);
   }
      largebrightnessD = `${parseInt(`${assist5}`) * airbnbstarselectedB.length}`;
      largebrightnessD += `${parseInt(`${iconstarJ}`)}`;
   let ajax1 = 6998998.0 >= iconstarJ;
   do {
      iconstarJ += parseFloat(`${parseInt(`${xvodT}`) / 3}`);
      if (ajax1) {
         break;
      }
   } while (ajax1 && ((parseInt(`${iconstarJ}`) - airbnbstarselectedB.length) < 3));
       let wind8 = 1.0;
       let username5 = String.fromCharCode(107,95,52,49,95,109,111,110,116,104,115,0);
         wind8 += 3 ^ parseInt(`${wind8}`);
         username5 += `${username5.length}`;
         username5 = `${1 * username5.length}`;
      let selectO = wind8 >= 6596110.0;
      do {
          let philippinesK = 2.0;
          let ajaxF: Map<any, any> = new Map([[String.fromCharCode(121,95,52,54,95,102,105,114,101,119,97,108,108,0),false ], [String.fromCharCode(111,95,53,56,95,110,111,110,110,117,108,108,97,100,100,114,101,115,115,0),false ]]);
          let logoutt = String.fromCharCode(113,95,55,56,95,101,120,112,111,110,101,110,116,115,0);
          let filedP = String.fromCharCode(99,95,55,52,95,109,117,108,116,105,112,108,101,114,115,0);
         wind8 *= username5.length ^ 1;
         philippinesK += 1;
         ajaxF.set(filedP, parseInt(`${philippinesK}`) << (Math.min(filedP.length, 4)));
         logoutt = `${ajaxF.size >> (Math.min(Math.abs(3), 2))}`;
         if (selectO) {
            break;
         }
      } while ((!username5.startsWith(`${wind8}`)) && selectO);
      if ((username5.length ^ 5) <= 4 && (username5.length * parseInt(`${wind8}`)) <= 5) {
         username5 += `${2 << (Math.min(1, Math.abs(parseInt(`${wind8}`))))}`;
      }
      while ((username5.length * wind8) < 1.16) {
         username5 = `${parseInt(`${wind8}`)}`;
         break;
      }
      xvodT -= 3 - airbnbstarselectedB.length;
       let spinnerC: Array<any> = [133, 932, 508];
       let iconcurrentmatchk: Map<any, any> = new Map([[String.fromCharCode(114,95,49,53,95,115,116,114,105,110,103,98,117,102,102,101,114,0),String.fromCharCode(120,95,51,53,95,101,120,99,108,117,100,101,115,0)], [String.fromCharCode(114,101,99,105,100,95,99,95,49,53,0),String.fromCharCode(119,101,108,115,95,116,95,50,0)]]);
      for (let z = 0; z < 1; z++) {
         spinnerC = [3 - spinnerC.length];
      }
         spinnerC.push(iconcurrentmatchk.size);
         spinnerC.push(iconcurrentmatchk.size / (Math.max(2, 9)));
      let sideT = 7964198 >= iconcurrentmatchk.size;
      do {
         iconcurrentmatchk.set(`${spinnerC.length}`, 3 ^ iconcurrentmatchk.size);
         if (sideT) {
            break;
         }
      } while (sideT && (!Array.from(iconcurrentmatchk.keys()).includes(`${spinnerC.length}`)));
      while (iconcurrentmatchk.get(`${spinnerC.length}`) != null) {
         spinnerC.push(iconcurrentmatchk.size / 2);
         break;
      }
      let basketballtrophyt = iconcurrentmatchk.size >= 4985809;
      do {
          let weiboT = String.fromCharCode(122,95,54,53,95,121,117,118,103,98,114,112,0);
          let dialogL: Map<any, any> = new Map([[String.fromCharCode(119,105,110,101,114,114,110,111,95,122,95,56,0),String.fromCharCode(101,95,53,54,95,111,112,101,110,115,101,97,0)], [String.fromCharCode(110,101,103,111,116,105,97,116,101,100,95,102,95,57,51,0),String.fromCharCode(98,95,54,55,95,99,111,109,112,97,114,0)]]);
          let gestureN = String.fromCharCode(108,111,119,112,97,115,115,95,53,95,53,54,0);
          let storeo = 2.0;
          let homeB = false;
         iconcurrentmatchk.set(`${storeo}`, 2 & parseInt(`${storeo}`));
         weiboT = `${(2 ^ (homeB ? 5 : 4))}`;
         dialogL.set(`${homeB}`, gestureN.length);
         gestureN = `${1 & dialogL.size}`;
         if (basketballtrophyt) {
            break;
         }
      } while ((!Array.from(iconcurrentmatchk.keys()).includes(`${spinnerC.length}`)) && basketballtrophyt);
      assist5 -= iconcurrentmatchk.size + spinnerC.length;
      assist5 += backiconmaski.length;
   if (4 <= (kickD.size * 1) || 1.76 <= (5.99 + iconstarJ)) {
      kickD = new Map([[`${iconstarJ}`, parseInt(`${type_8kJ}`) % 1]]);
   }
      airbnbstarselectedB += "1";

    setShowUsernameModal(true);
  }

  const onUsernameUpdateSuccess = (newUsername: string) => {
    setUsername(newUsername);
  }

  const onPressReferrer = () => {
       let auto_xM = String.fromCharCode(111,116,104,101,114,95,48,95,52,48,0);
    let usernameu = 4.0;
    let guidem: Map<any, any> = new Map([[String.fromCharCode(101,114,114,115,116,114,95,97,95,53,54,0),641], [String.fromCharCode(106,95,57,52,95,105,110,118,101,114,115,101,100,0),242]]);
    let iconpipexpandh = 0.0;
    let neonm = String.fromCharCode(111,118,101,114,115,99,114,111,108,108,95,119,95,57,50,0);
    let ynewinterstitialY: Map<any, any> = new Map([[String.fromCharCode(116,95,57,56,95,115,117,101,108,111,0),970], [String.fromCharCode(115,116,97,99,104,95,57,95,56,55,0),422]]);
    let textinputx = 4.0;
    let fulli = String.fromCharCode(106,100,109,97,115,116,101,114,95,120,95,57,50,0);
    let overl = String.fromCharCode(107,95,50,55,95,98,97,107,101,0);
    let matchdetailbgK = String.fromCharCode(100,95,56,48,95,105,110,100,101,120,0);
    let showD: Map<any, any> = new Map([[String.fromCharCode(98,101,101,110,95,53,95,51,51,0),193], [String.fromCharCode(114,95,48,95,115,112,97,99,105,110,103,115,0),397]]);
    let active7 = String.fromCharCode(100,97,116,101,115,95,98,95,55,0);
   let gifgoalbgO = String.fromCharCode(49,106,119,122,117,117,100,98,110,49,0) == neonm;
   do {
      neonm += `${ynewinterstitialY.size >> (Math.min(Math.abs(3), 5))}`;
      if (gifgoalbgO) {
         break;
      }
   } while (gifgoalbgO && (3 < (neonm.length & 3)));
   if ((neonm.length - parseInt(`${iconpipexpandh}`)) >= 1 && (iconpipexpandh - parseFloat(`${neonm.length}`)) >= 5.42) {
       let mbnativek = 1;
       let configureD = false;
         configureD = !configureD;
      let sliderU = mbnativek <= 5710404;
      do {
         mbnativek *= ((configureD ? 1 : 2) - mbnativek);
         if (sliderU) {
            break;
         }
      } while ((3 == (5 + mbnativek) && mbnativek == 5) && sliderU);
      for (let p = 0; p < 3; p++) {
          let abidetecta = 3.0;
          let anythinkc = String.fromCharCode(101,95,57,49,95,102,105,108,101,116,105,109,101,0);
         configureD = (abidetecta * parseFloat(`${anythinkc.length}`)) >= 20.28;
      }
         mbnativek <<= Math.min(Math.abs(2), 2);
         mbnativek /= Math.max(2, 3);
          let middlew = true;
         configureD = configureD || 100 <= mbnativek;
         middlew = !middlew;
      neonm += "1";
   }
   let episodes5 = guidem.size >= 8755376;
   do {
      guidem = new Map([[`${usernameu}`, parseInt(`${usernameu}`)]]);
      if (episodes5) {
         break;
      }
   } while ((parseInt(`${usernameu}`) >= guidem.size) && episodes5);
      usernameu /= Math.max(1 + parseInt(`${usernameu}`), 2);
   let predictionarrowT = neonm.length >= 6915380;
   do {
      neonm += `${fulli.length - 1}`;
      if (predictionarrowT) {
         break;
      }
   } while ((3 >= neonm.length) && predictionarrowT);
   for (let w = 0; w < 3; w++) {
      fulli = `${auto_xM.length}`;
   }
   let goallogoe = String.fromCharCode(111,122,107,103,119,98,111,0) == fulli;
   do {
      fulli += `${(overl == String.fromCharCode(56,0) ? overl.length : parseInt(`${usernameu}`))}`;
      if (goallogoe) {
         break;
      }
   } while (goallogoe && (3 == (guidem.size << (Math.min(fulli.length, 1))) || (3 << (Math.min(1, fulli.length))) == 4));
      fulli += `${fulli.length}`;
      fulli = `${parseInt(`${iconpipexpandh}`)}`;
      fulli = "2";
   while (iconpipexpandh <= 2.42) {
       let configurel = 0;
       let reward1 = false;
       let libffmpegkitS: Array<any> = [573, 75];
       let attributedstringY: Array<any> = [361, 225];
      let mintegralg = 5563698 <= attributedstringY.length;
      do {
          let androidr = String.fromCharCode(122,95,52,54,95,98,101,120,116,0);
         attributedstringY = [3 << (Math.min(Math.abs(configurel), 3))];
         androidr += `${androidr.length | 3}`;
         if (mintegralg) {
            break;
         }
      } while (mintegralg && ((attributedstringY.length | 3) == 2 || !reward1));
          let bottoma: Array<any> = [762, 893];
          let megaphoneV = false;
         configurel <<= Math.min(Math.abs((configurel | (reward1 ? 1 : 5))), 1);
         bottoma.push(bottoma.length * 2);
         megaphoneV = !megaphoneV;
      while ((libffmpegkitS.length ^ attributedstringY.length) == 1 || 1 == (1 ^ libffmpegkitS.length)) {
         libffmpegkitS.push(1 - attributedstringY.length);
         break;
      }
      let neon7 = 5507795 <= libffmpegkitS.length;
      do {
         libffmpegkitS.push(libffmpegkitS.length);
         if (neon7) {
            break;
         }
      } while (neon7 && (libffmpegkitS.length <= attributedstringY.length));
      for (let l = 0; l < 2; l++) {
         libffmpegkitS = [((reward1 ? 4 : 4) % 3)];
      }
         libffmpegkitS = [(1 >> (Math.min(1, Math.abs((reward1 ? 3 : 5)))))];
      let upgradeN = libffmpegkitS.length <= 4917228;
      do {
          let ajaxM = String.fromCharCode(101,95,49,55,95,103,105,102,115,0);
          let redirect4 = 4;
          let recommendation0 = String.fromCharCode(104,108,115,112,108,97,121,108,105,115,116,95,57,95,54,48,0);
          let iconclose7 = false;
         libffmpegkitS = [libffmpegkitS.length & 3];
         ajaxM += `${(recommendation0 == String.fromCharCode(84,0) ? recommendation0.length : redirect4)}`;
         redirect4 <<= Math.min(3, Math.abs(redirect4 & 1));
         iconclose7 = 41 >= redirect4;
         if (upgradeN) {
            break;
         }
      } while (upgradeN && (2 <= libffmpegkitS.length));
      if ((configurel + 2) <= 1) {
         reward1 = 85 < configurel;
      }
      if (3 >= configurel) {
         configurel /= Math.max(3, 2);
      }
         configurel += (attributedstringY.length ^ (reward1 ? 5 : 1));
          let modeI: Array<any> = [494, 896];
          let iconmoreH = 3.0;
         configurel -= configurel >> (Math.min(2, Math.abs(3)));
         modeI.push(modeI.length);
         iconmoreH -= parseFloat(`${3}`);
         attributedstringY.push(libffmpegkitS.length);
      auto_xM = `${(overl == String.fromCharCode(75,0) ? parseInt(`${iconpipexpandh}`) : overl.length)}`;
      break;
   }
      auto_xM = `${neonm.length}`;
       let resultW = 4.0;
         resultW /= Math.max(3, parseInt(`${resultW}`));
          let embedh = 3;
          let iconschedule6 = false;
          let sigmobL = 5.0;
         resultW *= parseInt(`${resultW}`) * 1;
         embedh >>= Math.min(1, Math.abs(parseInt(`${sigmobL}`)));
         iconschedule6 = !iconschedule6 || embedh <= 47;
         sigmobL /= Math.max(5, parseFloat(`${embedh}`));
         resultW += parseInt(`${resultW}`) >> (Math.min(Math.abs(parseInt(`${resultW}`)), 1));
      usernameu /= Math.max(1 << (Math.min(1, neonm.length)), 1);
   let crownb = 7770922 >= matchdetailbgK.length;
   do {
      matchdetailbgK += `${(String.fromCharCode(115,0) == auto_xM ? parseInt(`${usernameu}`) : auto_xM.length)}`;
      if (crownb) {
         break;
      }
   } while (crownb && (fulli.length > matchdetailbgK.length));
      textinputx *= guidem.size;
   while (2 > (matchdetailbgK.length * parseInt(`${usernameu}`)) || 5 > (2 & matchdetailbgK.length)) {
      usernameu *= (neonm == String.fromCharCode(85,0) ? parseInt(`${textinputx}`) : neonm.length);
      break;
   }
      overl = `${ynewinterstitialY.size}`;
   for (let u = 0; u < 2; u++) {
      usernameu -= ynewinterstitialY.size / (Math.max(1, 7));
   }

    setShowReferrerModal(true);
  }

  const onCopyReferralCode = () => {
       let animationsP = String.fromCharCode(105,95,50,95,98,108,105,110,107,0);
    let iconshareo = 0.0;
    let libtob4 = String.fromCharCode(116,95,49,55,95,104,113,97,100,115,112,0);
    let fastforward8 = 4.0;
    let componentP = 3;
    let iconwatchnowk = String.fromCharCode(114,101,118,101,97,108,95,108,95,49,48,0);
    let tempV = 2.0;
    let playercommonT = 3;
       let libavdeviceR = String.fromCharCode(100,101,99,108,105,110,101,100,95,102,95,49,56,0);
         libavdeviceR += `${libavdeviceR.length / (Math.max(2, 4))}`;
         libavdeviceR += "2";
          let aboutO = String.fromCharCode(117,110,115,116,111,112,112,97,98,108,101,95,99,95,54,56,0);
         libavdeviceR += `${aboutO.length}`;
      iconshareo *= parseFloat(`${parseInt(`${tempV}`) + 3}`);
   let circleD = 8985964.0 <= fastforward8;
   do {
       let kuaishouF = String.fromCharCode(114,115,116,110,95,97,95,54,49,0);
         kuaishouF = `${kuaishouF.length}`;
         kuaishouF += `${kuaishouF.length}`;
         kuaishouF += `${kuaishouF.length}`;
      fastforward8 /= Math.max(1, parseFloat(`${animationsP.length & libtob4.length}`));
      if (circleD) {
         break;
      }
   } while ((2.86 == (1.33 * fastforward8) && (5 + componentP) == 5) && circleD);
       let refreshborderlessb = 1;
       let iconpointscore1 = 0.0;
       let stored = String.fromCharCode(115,118,97,103,95,110,95,54,52,0);
          let userQ = true;
          let indicatorB = true;
         refreshborderlessb &= 1;
         userQ = !userQ;
         indicatorB = userQ;
       let iconsaveimagek: Map<any, any> = new Map([[String.fromCharCode(99,97,108,101,110,100,97,114,95,112,95,49,0),false ], [String.fromCharCode(114,105,99,101,95,57,95,56,53,0),false ]]);
       let leagueA: Map<any, any> = new Map([[String.fromCharCode(105,95,49,53,95,99,117,108,102,114,101,113,0),String.fromCharCode(122,95,49,0)], [String.fromCharCode(108,105,98,97,118,117,116,105,108,95,110,95,53,50,0),String.fromCharCode(106,95,56,57,95,109,101,100,105,97,99,111,100,101,99,100,101,99,0)], [String.fromCharCode(109,100,104,100,95,104,95,48,0),String.fromCharCode(121,95,55,50,95,107,110,111,119,110,0)]]);
      let icopy_4hl = 9471571.0 >= iconpointscore1;
      do {
         iconpointscore1 -= 3;
         if (icopy_4hl) {
            break;
         }
      } while (icopy_4hl && (1.28 > (1.37 * iconpointscore1) && (refreshborderlessb + 4) > 4));
       let arrowdownj: Map<any, any> = new Map([[String.fromCharCode(105,95,53,53,95,116,111,103,103,108,101,0),806], [String.fromCharCode(118,95,50,95,98,108,97,99,107,108,105,115,116,0),917], [String.fromCharCode(114,101,112,108,97,99,101,115,95,105,95,55,52,0),496]]);
         iconpointscore1 /= Math.max(2 - leagueA.size, 1);
          let yellowE = String.fromCharCode(115,112,107,114,95,101,95,51,49,0);
         stored += `${parseInt(`${iconpointscore1}`)}`;
         yellowE += `${yellowE.length << (Math.min(3, yellowE.length))}`;
          let whitevideolive9 = 3;
          let matchesJ = String.fromCharCode(115,95,50,51,95,112,105,120,109,97,112,0);
         stored += `${iconsaveimagek.size + matchesJ.length}`;
         whitevideolive9 >>= Math.min(3, Math.abs(3));
         matchesJ += `${whitevideolive9}`;
      while (5.74 >= iconpointscore1) {
         iconpointscore1 /= Math.max(arrowdownj.size << (Math.min(Math.abs(1), 2)), 5);
         break;
      }
      for (let l = 0; l < 2; l++) {
         iconpointscore1 -= (String.fromCharCode(104,0) == stored ? stored.length : leagueA.size);
      }
      componentP += parseInt(`${iconpointscore1}`) & parseInt(`${iconshareo}`);
   let iconchatsend9 = 5010737.0 >= fastforward8;
   do {
      fastforward8 *= parseFloat(`${libtob4.length}`);
      if (iconchatsend9) {
         break;
      }
   } while ((5 <= (4 | animationsP.length) || (animationsP.length | 4) <= 2) && iconchatsend9);
      componentP &= iconwatchnowk.length;
      iconwatchnowk = "2";
   if ((iconshareo - parseFloat(`${libtob4.length}`)) <= 1.38) {
      libtob4 += `${libtob4.length}`;
   }
      libtob4 = `${parseInt(`${tempV}`)}`;
   while ((iconwatchnowk.length >> (Math.min(Math.abs(2), 1))) == 5) {
      iconwatchnowk += `${parseInt(`${fastforward8}`) + parseInt(`${tempV}`)}`;
      break;
   }
   if (1 == libtob4.length) {
      libtob4 += `${parseInt(`${iconshareo}`)}`;
   }
      iconwatchnowk += `${parseInt(`${iconshareo}`) >> (Math.min(Math.abs(1), 1))}`;
      iconwatchnowk += `${parseInt(`${fastforward8}`)}`;

    Clipboard.setString(userState.user?.userReferralCode ?? '');

   let clearn = iconwatchnowk.length >= 8454923;
   do {
       let headerX = 3.0;
       let homeactive_ = 1.0;
      for (let u = 0; u < 3; u++) {
          let routers = String.fromCharCode(97,95,56,57,95,108,97,110,103,0);
         homeactive_ *= parseFloat(`${routers.length}`);
      }
      while (homeactive_ < 3.89) {
          let libruntimeexecutor4 = String.fromCharCode(113,95,49,49,95,109,101,115,115,97,103,105,110,103,0);
          let become1 = 3.0;
          let favoriteM = 4.0;
          let annern = String.fromCharCode(102,114,101,101,95,99,95,51,50,0);
         headerX /= Math.max(parseFloat(`${parseInt(`${favoriteM}`)}`), 3);
         libruntimeexecutor4 = `${libruntimeexecutor4.length}`;
         become1 -= annern.length;
         favoriteM -= parseFloat(`${libruntimeexecutor4.length % (Math.max(3, 3))}`);
         annern += `${annern.length / 3}`;
         break;
      }
         homeactive_ *= parseFloat(`${3 & parseInt(`${homeactive_}`)}`);
      for (let q = 0; q < 3; q++) {
          let sliderw = 3;
          let scoreq = 5.0;
          let combineM = 3.0;
          let dropdowna: Map<any, any> = new Map([[String.fromCharCode(115,121,109,98,111,108,105,99,97,116,101,95,115,95,52,52,0),true ], [String.fromCharCode(103,109,116,105,109,101,95,120,95,50,55,0),false ], [String.fromCharCode(119,95,50,49,95,101,118,105,99,101,0),false ]]);
         homeactive_ += parseFloat(`${parseInt(`${homeactive_}`) & sliderw}`);
         sliderw |= dropdowna.size;
         scoreq += dropdowna.size & 1;
         combineM -= parseFloat(`${1 / (Math.max(8, dropdowna.size))}`);
      }
      while (1.5 > (homeactive_ / (Math.max(9, headerX)))) {
         homeactive_ += parseFloat(`${parseInt(`${homeactive_}`) - 3}`);
         break;
      }
         homeactive_ *= parseFloat(`${parseInt(`${headerX}`)}`);
      iconwatchnowk += `${iconwatchnowk.length / 2}`;
      if (clearn) {
         break;
      }
   } while (clearn && (3.66 >= tempV));
   let penaltymatchiconB = 9388318 <= libtob4.length;
   do {
      libtob4 = `${(String.fromCharCode(104,0) == iconwatchnowk ? iconwatchnowk.length : libtob4.length)}`;
      if (penaltymatchiconB) {
         break;
      }
   } while ((5 > (3 ^ libtob4.length) || (libtob4.length ^ 3) > 4) && penaltymatchiconB);
      fastforward8 /= Math.max(parseFloat(`${animationsP.length / (Math.max(2, 6))}`), 3);
   let binddatasI = iconwatchnowk == String.fromCharCode(98,101,100,105,122,105,103,114,0);
   do {
      iconwatchnowk += "2";
      if (binddatasI) {
         break;
      }
   } while (binddatasI && (!iconwatchnowk.includes(animationsP)));
       let backwhite2 = String.fromCharCode(114,111,98,117,115,116,95,112,95,54,56,0);
       let condensedV = false;
      let close3 = String.fromCharCode(119,97,102,99,0) == backwhite2;
      do {
          let eactq = String.fromCharCode(98,95,57,49,95,100,105,115,99,111,118,101,114,0);
          let analyticr: Array<any> = [730, 804];
         backwhite2 += `${analyticr.length % (Math.max(9, eactq.length))}`;
         if (close3) {
            break;
         }
      } while (close3 && (!condensedV || backwhite2.length == 1));
      if (condensedV || 2 <= backwhite2.length) {
         condensedV = backwhite2.length > 60;
      }
      let whiteanimationlivei = backwhite2 == String.fromCharCode(109,49,53,109,51,53,122,117,116,0);
      do {
         backwhite2 += `${((condensedV ? 5 : 1))}`;
         if (whiteanimationlivei) {
            break;
         }
      } while (whiteanimationlivei && (backwhite2.endsWith(`${condensedV}`)));
         condensedV = backwhite2.length >= 94;
          let iconarrowrightp = 2.0;
         condensedV = backwhite2.length > 1;
         iconarrowrightp /= Math.max(2, 3);
      if (backwhite2.length >= 4) {
          let entryI = 2.0;
          let anytimeW = String.fromCharCode(117,110,99,111,100,101,100,95,107,95,57,49,0);
          let libjsinspectorr = 4.0;
         condensedV = backwhite2.length <= anytimeW.length;
         entryI -= 2 | parseInt(`${libjsinspectorr}`);
         anytimeW = `${parseInt(`${entryI}`) >> (Math.min(5, Math.abs(1)))}`;
         libjsinspectorr += parseFloat(`${parseInt(`${entryI}`) - 1}`);
      }
      tempV -= (backwhite2 == String.fromCharCode(76,0) ? backwhite2.length : iconwatchnowk.length);
      fastforward8 += parseFloat(`${iconwatchnowk.length}`);
      animationsP = `${playercommonT}`;
   if (iconwatchnowk.includes(`${iconshareo}`)) {
      iconwatchnowk = `${3 >> (Math.min(3, libtob4.length))}`;
   }
       let tick3: Map<any, any> = new Map([[String.fromCharCode(117,110,109,97,114,107,95,101,95,57,57,0),true ], [String.fromCharCode(104,95,49,95,114,101,99,105,112,105,101,110,116,115,0),false ]]);
       let znewinterstitial6 = String.fromCharCode(112,95,51,57,95,97,117,116,111,99,108,101,97,114,0);
       let arrowselectdown6 = false;
         arrowselectdown6 = 23 > tick3.size && znewinterstitial6 == String.fromCharCode(122,0);
         arrowselectdown6 = !arrowselectdown6;
      if (znewinterstitial6.length == 5) {
          let arrowupV = 1.0;
         znewinterstitial6 = `${(3 + (arrowselectdown6 ? 4 : 2))}`;
         arrowupV *= parseInt(`${arrowupV}`) + 3;
      }
          let colors5 = String.fromCharCode(116,114,105,99,107,108,101,95,57,95,54,56,0);
          let bootsplashe = 0.0;
          let layoutA = String.fromCharCode(98,95,54,57,95,116,111,107,101,110,105,122,101,100,0);
         znewinterstitial6 += `${colors5.length + znewinterstitial6.length}`;
         colors5 = `${3 >> (Math.min(4, Math.abs(parseInt(`${bootsplashe}`))))}`;
         bootsplashe -= parseInt(`${bootsplashe}`) / (Math.max(layoutA.length, 4));
         layoutA += `${2 % (Math.max(7, parseInt(`${bootsplashe}`)))}`;
         arrowselectdown6 = tick3.get(`${arrowselectdown6}`) == null;
      while (!arrowselectdown6 && 3 < (5 ^ tick3.size)) {
         arrowselectdown6 = !znewinterstitial6.includes(`${arrowselectdown6}`);
         break;
      }
         tick3 = new Map([[`${tick3.size}`, tick3.size]]);
      let fast6 = 6337006 <= tick3.size;
      do {
          let mutedP = String.fromCharCode(122,95,54,54,95,114,103,98,110,0);
          let bangH = false;
         tick3 = new Map([[mutedP, mutedP.length / 1]]);
         bangH = (bangH ? bangH : !bangH);
         if (fast6) {
            break;
         }
      } while ((tick3.size >= 5 && 1 >= (tick3.size >> (Math.min(Math.abs(5), 5)))) && fast6);
          let iconnointernet5: Map<any, any> = new Map([[String.fromCharCode(107,95,57,55,95,117,110,109,105,110,105,109,105,122,101,0),true ], [String.fromCharCode(99,100,101,114,114,111,114,95,97,95,57,52,0),false ]]);
         arrowselectdown6 = tick3.size <= znewinterstitial6.length;
         iconnointernet5 = new Map([[`${iconnointernet5.size}`, iconnointernet5.size ^ 2]]);
      tempV /= Math.max(5, parseInt(`${tempV}`) / 1);
       let libhermesu = String.fromCharCode(120,95,56,57,95,108,111,116,116,105,101,112,97,114,115,101,114,0);
       let dist3 = String.fromCharCode(99,95,49,48,48,95,114,112,99,103,101,110,0);
       let libfollyS = true;
      while (dist3 != String.fromCharCode(81,0) && 1 > libhermesu.length) {
          let headerK = String.fromCharCode(114,101,110,100,101,114,101,100,95,115,95,54,0);
         dist3 += `${dist3.length}`;
         headerK = `${headerK.length - 3}`;
         break;
      }
         dist3 += `${dist3.length * 1}`;
      while (libhermesu.includes(dist3)) {
         libhermesu = `${libhermesu.length * dist3.length}`;
         break;
      }
       let collection1 = 1;
         dist3 = `${collection1 / 1}`;
      if ((dist3.length * collection1) < 3) {
         dist3 += `${((libfollyS ? 2 : 3) >> (Math.min(Math.abs(2), 5)))}`;
      }
         dist3 += `${3 >> (Math.min(Math.abs(collection1), 2))}`;
      if (1 == (collection1 >> (Math.min(libhermesu.length, 4))) && (1 >> (Math.min(2, libhermesu.length))) == 4) {
         libhermesu += `${((libfollyS ? 2 : 4) * collection1)}`;
      }
      let areau = String.fromCharCode(99,116,49,51,49,122,105,0) == libhermesu;
      do {
         libhermesu += `${((libfollyS ? 2 : 2))}`;
         if (areau) {
            break;
         }
      } while (areau && (libhermesu.length > 4 || libfollyS));
      componentP >>= Math.min(Math.abs(parseInt(`${iconshareo}`) + libtob4.length), 5);
   for (let m = 0; m < 1; m++) {
       let armvaD = String.fromCharCode(103,95,49,52,95,99,104,97,114,97,99,116,101,114,105,115,116,105,99,115,0);
       let libcxxcomponentsV: Map<any, any> = new Map([[String.fromCharCode(110,95,51,54,95,97,107,97,114,111,115,0),true ], [String.fromCharCode(101,95,52,56,95,117,105,111,116,111,109,98,117,102,0),true ], [String.fromCharCode(110,97,109,101,115,112,97,99,101,115,95,50,95,53,49,0),false ]]);
      while (armvaD.length > 3) {
         armvaD = `${armvaD.length}`;
         break;
      }
      while ((libcxxcomponentsV.size % (Math.max(armvaD.length, 2))) >= 4 || (libcxxcomponentsV.size % (Math.max(armvaD.length, 7))) >= 4) {
         armvaD += `${armvaD.length & libcxxcomponentsV.size}`;
         break;
      }
      for (let g = 0; g < 1; g++) {
          let imagenetworkerr9 = 3.0;
         armvaD = `${armvaD.length * 3}`;
         imagenetworkerr9 += parseInt(`${imagenetworkerr9}`) & 3;
      }
         armvaD += `${libcxxcomponentsV.size << (Math.min(armvaD.length, 3))}`;
      let templateprocessor7 = armvaD.length <= 7199141;
      do {
          let whatsapp5: Array<any> = [519, 729];
          let switch_sQ = 5;
          let hometeamfieldb = String.fromCharCode(118,95,57,48,95,114,105,98,98,111,110,0);
          let mbbidB = String.fromCharCode(100,95,51,95,115,104,111,114,116,99,117,116,115,0);
          let anythinkz: Array<any> = [881, 706];
         armvaD = `${2 & libcxxcomponentsV.size}`;
         whatsapp5 = [2 - anythinkz.length];
         switch_sQ ^= 1 & switch_sQ;
         hometeamfieldb = `${mbbidB.length << (Math.min(Math.abs(1), 2))}`;
         mbbidB += `${switch_sQ}`;
         anythinkz = [(String.fromCharCode(106,0) == hometeamfieldb ? whatsapp5.length : hometeamfieldb.length)];
         if (templateprocessor7) {
            break;
         }
      } while ((4 == libcxxcomponentsV.size) && templateprocessor7);
         libcxxcomponentsV.set(armvaD, 1 | libcxxcomponentsV.size);
      componentP &= libtob4.length;
   }
   while (3 >= (componentP ^ libtob4.length)) {
      libtob4 += `${2 % (Math.max(1, parseInt(`${fastforward8}`)))}`;
      break;
   }
    setIsDialogOpen(true);
  }

  return (
    <ScreenContainer>
      <TitleWithBackButtonHeader title='个人中心' />
      <View
        style={{
          justifyContent: 'space-between',
          flex: 1,
          paddingHorizontal: 10,
        }}
      >
        <View style={{ marginTop: 30 }}>

          <View
            style={styles.pressableTextContainer}
          >
            <Text style={{ fontSize: 16, color: '#9C9C9C' }}>头像</Text>
            <FastImage
              style={{
                height: 34,
                width: 34,
              }}
              source={require('../../../static/images/loadingspinnerPlaylist.png')}
            />
          </View>


          { }
          <TouchableOpacity
            style={styles.pressableTextContainer}
            onPress={onPressUsername}
          >
            <Text style={{ fontSize: 16, color: 'white' }}>
              {username}
            </Text>
          </TouchableOpacity>

          { }
          <View style={styles.pressableTextContainer}>
            <Text style={{ fontSize: 16, color: '#9C9C9C' }}>
              {userState.user?.userEmail !== '' ? userState.user?.userEmail : userState.user.userPhoneNumber}
            </Text>
          </View>

          { }
          {userState.user?.userReferrerName == '' &&
            userState.user.userAllowUpdateReferral == true &&
            <TouchableOpacity
              style={styles.pressableTextContainer}
              onPress={onPressReferrer}
            >
              <Text style={{ fontSize: 16, color: '#B6B6B6' }}>
                补填邀请码 (只能填写一次)
              </Text>
            </TouchableOpacity>
          }

          {userState.user?.userReferrerName != '' && (
            <View style={styles.pressableTextContainer}>
              <Text style={{ fontSize: 16, color: '#9C9C9C' }}>推介人</Text>

              <View
                style={{
                  flexDirection: 'row',
                  justifyContent: 'center',
                  alignItems: 'center',
                }}
              >
                <Text style={{ fontSize: 14, color: '#9C9C9C' }}>
                  {userState.user?.userReferrerName}
                </Text>
              </View>
            </View>
          )}

          { }
          <TouchableOpacity
            style={styles.pressableTextContainer}
            onPress={onCopyReferralCode}
          >
            <Text style={{ fontSize: 16, color: colors.primary }}>
              {userState.user?.userReferralCode}
            </Text>

            <View
              style={{
                flexDirection: 'row',
                justifyContent: 'center',
                alignItems: 'center',
              }}
            >
              <Text style={{ fontSize: 14, color: '#9C9C9C' }}>
                复制邀请码
              </Text>
              <Image
                style={{
                  height: 27,
                  width: 27,
                  position: 'relative',
                  top: 2,
                }}
                source={require('../../../static/images/profile/predictionbannerIconlogoutCondensed.png')}
              />
            </View>
          </TouchableOpacity>

          <NotificationModal
            onConfirm={toggleOverlay}
            isVisible={isDialogOpen}
            title='复制成功'
            subtitle1=''
            subtitle2=''
            subtitle3=''
          />
        </View>

        <ChangeUsernameModal
          isVisible={isShowUsernameModal}
          handleClose={() => {
            setShowUsernameModal(false);
          }}
          initialUsername={username}
          onSubmitSuccess={onUsernameUpdateSuccess}
        />

        <ChangeReferrerModal
          isVisible={isShowReferrerModal}
          handleClose={() => {
            setShowReferrerModal(false);
          }}
        />
      </View>
    </ScreenContainer>
  );
};

const styles = StyleSheet.create({
  pressableTextContainer: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    backgroundColor: '#1d2023',
    marginTop: 20,
    paddingLeft: 18,
    paddingRight: 13,
    height: 48,
    borderRadius: 8,
    marginBottom: 20,
  },
});
