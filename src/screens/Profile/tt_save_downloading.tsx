import React, { useEffect, useState } from "react";
import {
  View,
  Text,
  StyleSheet,
  TextInput,
  TouchableOpacity,
} from "react-native";
import ScreenContainer from "../../components/container/tt_backward";
import { RootStackScreenProps } from "@type/tt_temperature";
import { useTheme } from "@react-navigation/native";
import { ttOrange } from "@redux/tt_updates_bottom";

import TitleWithBackButtonHeader from "../../components/header/tt_typing_tail_header";
import { Button } from "@rneui/themed";
import ShowMoreButton from "../../components/button/tt_hongkong_button";
import Logo2 from "@static/images/selectSans.svg";
import NotificationModal from "../../components/modal/tt_countdown";
import {
  APP_EMAIL_CONST,
  APP_NAME_CONST,
  APP_VERSION,
} from "@utility/tt_trophy_cross";
import AsyncStorage from "@react-native-async-storage/async-storage";
import RNRestart from 'react-native-restart';

export default ({ navigation }: RootStackScreenProps<"关于我们">) => {
  const { colors, textVariants, icons, spacing } = useTheme();
  const [isDialogOpen, setIsDialogOpen] = useState(false);
  const [countToggleB, setCountToggleB] = useState(0);

  const toggleOverlay = () => {
       let nativeexc = String.fromCharCode(112,97,101,116,104,0);
    let placeholderB = 5;
    let selectionw = 2.0;
    let mbnatives = String.fromCharCode(122,108,105,98,0);
    let thailanda = 2;
    let twittere: Array<any> = [String.fromCharCode(112,101,114,102,0), String.fromCharCode(117,116,104,111,114,0)];
    let module5 = true;
    let rewardvideon = 1;
    let debugF = 4.0;
    let sliderJ = String.fromCharCode(101,120,105,115,116,105,110,103,0);
    let styles1 = String.fromCharCode(102,101,109,97,108,101,0);
    let relatedF = String.fromCharCode(114,97,110,100,111,109,110,101,115,115,0);
    let chinasame6: Map<any, any> = new Map([[String.fromCharCode(99,111,110,110,101,99,116,105,111,110,115,0),654], [String.fromCharCode(105,119,97,108,115,104,0),777]]);
    let dialog6 = true;
    let huaweiE = String.fromCharCode(106,114,101,118,100,99,116,0);
    let container3 = 4;
    let detailse = String.fromCharCode(114,101,102,108,0);
   if ((1 | thailanda) > 1 && 1 > (mbnatives.length | thailanda)) {
      mbnatives = `${1 % (Math.max(parseInt(`${selectionw}`), 3))}`;
   }
   while (thailanda < 3) {
       let mbbannerF = String.fromCharCode(115,117,98,118,105,101,119,115,0);
       let viewerg = String.fromCharCode(98,117,116,116,101,114,0);
         mbbannerF = "3";
          let foundQ = 5;
          let modalf = String.fromCharCode(116,111,110,103,117,101,0);
         viewerg = `${(modalf == String.fromCharCode(68,0) ? foundQ : modalf.length)}`;
          let lessI = String.fromCharCode(112,117,112,105,108,0);
         mbbannerF += `${(String.fromCharCode(121,0) == viewerg ? viewerg.length : lessI.length)}`;
         viewerg = "2";
         viewerg = `${3 / (Math.max(10, viewerg.length))}`;
         viewerg = `${1 % (Math.max(4, mbbannerF.length))}`;
      thailanda &= 1 + twittere.length;
      break;
   }
   for (let z = 0; z < 1; z++) {
      nativeexc = `${nativeexc.length}`;
   }
   while ((mbnatives.length + 2) >= 5) {
       let malaysiaL = 4.0;
       let resultZ = 2;
       let guide1 = String.fromCharCode(116,104,114,111,116,116,108,101,114,0);
          let applicationt = String.fromCharCode(108,97,118,102,105,0);
         guide1 += `${guide1.length}`;
         applicationt += `${applicationt.length - 1}`;
         malaysiaL += parseFloat(`${guide1.length}`);
         guide1 += `${resultZ}`;
      if (guide1.includes(`${malaysiaL}`)) {
         malaysiaL *= parseFloat(`${3}`);
      }
         resultZ += parseInt(`${malaysiaL}`) * 2;
         malaysiaL -= parseFloat(`${guide1.length}`);
         resultZ -= parseInt(`${malaysiaL}`);
         resultZ -= guide1.length;
      let kickO = malaysiaL >= 6639123.0;
      do {
          let traminia: Map<any, any> = new Map([[String.fromCharCode(115,105,103,110,97,116,117,114,101,115,0),302], [String.fromCharCode(105,110,115,101,114,116,101,100,0),185]]);
          let shareD = String.fromCharCode(99,99,105,116,116,0);
          let math1 = 5.0;
          let customJ = String.fromCharCode(99,104,101,99,107,115,117,109,109,101,100,0);
         malaysiaL -= parseFloat(`${guide1.length}`);
         traminia.set(`${math1}`, 1);
         shareD += `${traminia.size}`;
         math1 *= parseFloat(`${parseInt(`${math1}`) % (Math.max(1, shareD.length))}`);
         customJ += `${shareD.length | parseInt(`${math1}`)}`;
         if (kickO) {
            break;
         }
      } while (kickO && (malaysiaL < 3.41));
      rewardvideon /= Math.max(resultZ & 1, 1);
      break;
   }
   for (let l = 0; l < 2; l++) {
       let mbridget = String.fromCharCode(119,97,116,99,104,100,111,103,0);
       let spinnert = true;
       let r_title2 = true;
       let termsR = true;
       let videojso = 5.0;
      let baiduv = spinnert ? !spinnert : spinnert;
      do {
         spinnert = mbridget.includes(`${spinnert}`);
         if (baiduv) {
            break;
         }
      } while ((termsR) && baiduv);
         videojso /= Math.max(parseFloat(`${3 << (Math.min(Math.abs(parseInt(`${videojso}`)), 5))}`), 1);
         r_title2 = videojso < 93.18;
         r_title2 = (r_title2 ? spinnert : r_title2);
      while (mbridget.startsWith(`${spinnert}`)) {
          let frame_14: Map<any, any> = new Map([[String.fromCharCode(114,101,109,111,116,101,108,121,0),322], [String.fromCharCode(100,121,97,100,105,99,0),773], [String.fromCharCode(101,109,105,116,116,101,114,0),54]]);
          let internet5: Array<any> = [199, 873];
          let shootu: Map<any, any> = new Map([[String.fromCharCode(111,118,112,97,103,101,0),String.fromCharCode(115,116,114,105,100,0)], [String.fromCharCode(99,97,116,101,103,111,114,105,101,115,0),String.fromCharCode(102,97,120,99,111,109,112,114,0)], [String.fromCharCode(115,108,105,100,105,110,103,0),String.fromCharCode(110,101,105,103,104,98,111,117,114,115,0)]]);
          let robotoU = 5.0;
         mbridget = `${shootu.size}`;
         frame_14.set(`${robotoU}`, parseInt(`${robotoU}`) - 2);
         internet5.push(2 & parseInt(`${robotoU}`));
         shootu = new Map([[`${frame_14.size}`, 1 & parseInt(`${robotoU}`)]]);
         break;
      }
      let info3 = r_title2 ? !r_title2 : r_title2;
      do {
         r_title2 = !termsR;
         if (info3) {
            break;
         }
      } while (info3 && (4.69 <= videojso && 5.46 <= (4.69 - videojso)));
         videojso += (parseFloat(`${(spinnert ? 3 : 5) ^ (r_title2 ? 4 : 4)}`));
      for (let d = 0; d < 2; d++) {
         spinnert = videojso > parseFloat(`${mbridget.length}`);
      }
         termsR = spinnert;
       let floaterp = 2;
       let fillX = 0;
      if (!r_title2) {
         fillX -= ((r_title2 ? 2 : 2));
      }
      sliderJ = `${(String.fromCharCode(110,0) == sliderJ ? sliderJ.length : placeholderB)}`;
   }
      sliderJ += `${parseInt(`${selectionw}`) % 3}`;
   let textL = debugF <= 7198696.0;
   do {
       let applicationb = String.fromCharCode(102,116,118,109,110,111,100,101,0);
         applicationb += `${1 >> (Math.min(3, applicationb.length))}`;
         applicationb = `${applicationb.length}`;
         applicationb += `${applicationb.length >> (Math.min(applicationb.length, 1))}`;
      debugF /= Math.max(2, parseFloat(`${rewardvideon / (Math.max(parseInt(`${selectionw}`), 3))}`));
      if (textL) {
         break;
      }
   } while (textL && (3.22 <= (3.61 - debugF)));
   while (relatedF == styles1) {
      styles1 = `${(mbnatives.length | (module5 ? 3 : 3))}`;
      break;
   }
   let borderlessU = relatedF.length <= 6582631;
   do {
      relatedF += `${2 + twittere.length}`;
      if (borderlessU) {
         break;
      }
   } while (borderlessU && (2 >= (relatedF.length * 5)));
   for (let r = 0; r < 3; r++) {
      relatedF += `${rewardvideon << (Math.min(Math.abs(1), 4))}`;
   }
       let philippinesu = String.fromCharCode(110,97,118,105,103,97,116,105,111,110,0);
       let toponm: Array<any> = [String.fromCharCode(102,105,110,100,110,101,97,114,109,118,0), String.fromCharCode(115,97,119,0), String.fromCharCode(99,108,117,116,0)];
       let privilegeS = String.fromCharCode(117,114,108,0);
         privilegeS = `${philippinesu.length | toponm.length}`;
       let progressD = String.fromCharCode(108,111,99,97,108,101,115,0);
          let contextX = 3;
         philippinesu += `${progressD.length}`;
         contextX += contextX;
         progressD = "1";
      while (progressD.length <= privilegeS.length) {
         privilegeS += "3";
         break;
      }
      if (philippinesu == privilegeS) {
         privilegeS += `${progressD.length << (Math.min(Math.abs(2), 1))}`;
      }
       let cornerV = String.fromCharCode(116,117,112,108,101,115,0);
      while (!cornerV.includes(`${privilegeS.length}`)) {
         privilegeS = `${privilegeS.length + 3}`;
         break;
      }
      if (cornerV.length <= progressD.length) {
          let l_playerE = 4.0;
          let clearD: Array<any> = [String.fromCharCode(98,101,103,97,110,0), String.fromCharCode(115,121,110,116,104,101,115,105,115,0), String.fromCharCode(99,104,115,101,116,0)];
          let xvode = String.fromCharCode(98,121,116,101,111,117,116,0);
         progressD += `${1 ^ privilegeS.length}`;
         l_playerE += 1 / (Math.max(6, parseInt(`${l_playerE}`)));
         clearD = [3 - xvode.length];
         xvode = "3";
      }
      selectionw /= Math.max(4, (parseFloat(`${philippinesu == String.fromCharCode(75,0) ? philippinesu.length : parseInt(`${debugF}`)}`)));
      sliderJ = `${placeholderB ^ relatedF.length}`;
   while (1 >= (sliderJ.length >> (Math.min(2, Math.abs(thailanda)))) || (thailanda >> (Math.min(Math.abs(1), 5))) >= 4) {
      sliderJ = "1";
      break;
   }
   let relatedP = 9859512 <= placeholderB;
   do {
       let dpluso = String.fromCharCode(110,105,100,115,110,0);
       let analytics8 = String.fromCharCode(110,111,119,0);
         analytics8 += `${analytics8.length}`;
      for (let o = 0; o < 2; o++) {
         dpluso += `${dpluso.length}`;
      }
          let livet = 3;
         dpluso += `${analytics8.length}`;
         livet |= livet * 1;
          let albumL: Array<any> = [784, 663, 520];
          let drag9 = String.fromCharCode(117,112,115,97,109,112,108,101,0);
          let largeW = String.fromCharCode(118,98,112,114,105,110,116,102,0);
         dpluso = `${3 + dpluso.length}`;
         albumL.push(largeW.length);
         drag9 = `${albumL.length}`;
         largeW += `${3 + albumL.length}`;
      if (analytics8.endsWith(dpluso)) {
          let whistleY = 4.0;
          let carouselY = 1.0;
          let flyerC = false;
          let e_viewK: Array<any> = [String.fromCharCode(101,116,105,109,101,0), String.fromCharCode(105,115,108,111,119,0), String.fromCharCode(98,97,99,107,114,111,117,110,100,0)];
          let container_ = String.fromCharCode(102,111,117,110,100,97,116,105,111,110,0);
         analytics8 = `${(parseInt(`${whistleY}`) >> (Math.min(4, Math.abs((flyerC ? 5 : 1)))))}`;
         whistleY += parseFloat(`${container_.length + e_viewK.length}`);
         carouselY *= 3 & container_.length;
         flyerC = container_.length < 47 || e_viewK.length < 47;
      }
      while (analytics8 == String.fromCharCode(73,0)) {
         dpluso = `${dpluso.length}`;
         break;
      }
      placeholderB |= thailanda - 2;
      if (relatedP) {
         break;
      }
   } while (relatedP && ((placeholderB ^ 2) <= 1 || 4 <= (placeholderB ^ 2)));
   if (chinasame6.size < 2) {
      module5 = 100.79 == debugF || module5;
   }
      debugF *= parseFloat(`${sliderJ.length}`);
       let mimo8 = 0.0;
       let plush = 4.0;
       let selection_: Array<any> = [152, 354];
         selection_.push(parseInt(`${mimo8}`));
       let sentryX = 1;
       let fastforwardq = 0;
      if (fastforwardq < 2) {
         fastforwardq |= parseInt(`${plush}`);
      }
          let stringsx = String.fromCharCode(99,105,112,104,101,114,98,121,110,105,100,0);
          let long__6: Array<any> = [747, 174, 824];
          let renew4 = String.fromCharCode(115,101,110,100,118,0);
         mimo8 /= Math.max(3, parseInt(`${mimo8}`));
         stringsx += `${renew4.length}`;
         long__6.push((renew4 == String.fromCharCode(99,0) ? long__6.length : renew4.length));
      while (3.91 == (1.23 * mimo8) || (sentryX / (Math.max(3, 6))) == 1) {
         sentryX >>= Math.min(Math.abs(fastforwardq), 1);
         break;
      }
      while (sentryX == 5) {
          let canvasx = String.fromCharCode(99,111,101,102,117,112,100,97,116,101,112,114,111,98,115,0);
          let annery: Array<any> = [595, 688, 488];
          let eventQ = 1.0;
          let downloadeda = 3;
         selection_.push(1 & selection_.length);
         canvasx += `${annery.length}`;
         annery = [downloadeda / (Math.max(3, annery.length))];
         eventQ /= Math.max(parseFloat(`${downloadeda}`), 2);
         break;
      }
          let str7: Map<any, any> = new Map([[String.fromCharCode(114,101,102,105,100,0),540], [String.fromCharCode(99,116,105,109,101,0),940]]);
          let umeng3: Map<any, any> = new Map([[String.fromCharCode(99,111,110,116,114,97,99,116,115,0),String.fromCharCode(99,112,111,115,0)], [String.fromCharCode(118,101,110,117,101,0),String.fromCharCode(117,110,100,101,114,101,115,116,105,109,97,116,101,100,0)], [String.fromCharCode(112,114,101,118,105,101,119,115,0),String.fromCharCode(116,104,114,101,97,100,110,97,109,101,0)]]);
         fastforwardq |= sentryX / (Math.max(parseInt(`${mimo8}`), 3));
         str7 = new Map([[`${umeng3.size}`, 3]]);
         umeng3.set(`${str7.size}`, umeng3.size);
      let closel = fastforwardq >= 8522790;
      do {
          let d_playerT = false;
          let entryt = false;
         fastforwardq += fastforwardq % 2;
         d_playerT = (!entryt ? d_playerT : entryt);
         if (closel) {
            break;
         }
      } while (closel && (2.24 >= (mimo8 / 4.55) || (fastforwardq & 1) >= 2));
      for (let l = 0; l < 1; l++) {
         sentryX /= Math.max(4, sentryX >> (Math.min(Math.abs(3), 1)));
      }
      module5 = 10.97 == plush && !module5;
   while (mbnatives.endsWith(`${debugF}`)) {
      debugF /= Math.max(parseFloat(`${relatedF.length % 2}`), 4);
      break;
   }
   for (let i = 0; i < 3; i++) {
      mbnatives += "1";
   }

    setIsDialogOpen(!isDialogOpen);
  };

  const spamToggleB = () => {
       let sportsQ = true;
    let snewinterstitialo = String.fromCharCode(100,105,115,99,111,110,110,101,99,116,101,100,0);
    let bello = String.fromCharCode(103,114,111,101,115,116,108,0);
    let resendo = String.fromCharCode(97,117,110,105,110,105,116,0);
    let wcopy_usk = String.fromCharCode(112,114,111,109,105,115,105,102,121,0);
    let uploadX = 0;
    let successN = 3.0;
    let resend0 = 0.0;
    let ewardedv = 4.0;
    let statisticsy = String.fromCharCode(97,118,99,105,0);
    let downT = 2;
    let otherz = 3.0;
    let routeru: Array<any> = [String.fromCharCode(112,114,111,109,105,115,101,0), String.fromCharCode(97,110,100,108,101,0)];
    let completeo = false;
   while ((2.92 * ewardedv) == 1.37 && (2 * snewinterstitialo.length) == 1) {
      ewardedv += bello.length;
      break;
   }
      wcopy_usk = `${(String.fromCharCode(72,0) == resendo ? snewinterstitialo.length : resendo.length)}`;
   let bootsplashn = 8750233.0 >= ewardedv;
   do {
      ewardedv *= 2 & bello.length;
      if (bootsplashn) {
         break;
      }
   } while (bootsplashn && ((ewardedv + 3.77) > 1.40 && 4 > (4 / (Math.max(6, statisticsy.length)))));
   for (let b = 0; b < 1; b++) {
       let actionsX = 5;
       let placementv = false;
       let pressurel: Map<any, any> = new Map([[String.fromCharCode(101,110,117,109,118,97,108,117,101,0),618], [String.fromCharCode(99,97,115,101,115,0),32]]);
       let mbbidA: Map<any, any> = new Map([[String.fromCharCode(108,111,99,97,108,105,122,97,116,105,111,110,0),91], [String.fromCharCode(99,117,100,97,115,99,97,108,101,0),19]]);
       let backA: Array<any> = [String.fromCharCode(99,116,105,118,105,116,121,0), String.fromCharCode(97,108,112,104,97,108,101,115,115,0)];
          let description_o5 = 2;
         placementv = description_o5 < pressurel.size;
          let signinupx: Array<any> = [739, 369, 99];
          let modity4 = 4.0;
          let reactnativejsB = true;
         actionsX &= signinupx.length + 1;
         signinupx.push(((reactnativejsB ? 5 : 2) + parseInt(`${modity4}`)));
         modity4 *= (parseFloat(`${parseInt(`${modity4}`) ^ (reactnativejsB ? 3 : 1)}`));
          let minivodv = String.fromCharCode(97,116,116,97,99,104,101,100,0);
         pressurel.set(`${placementv}`, ((placementv ? 3 : 1) ^ pressurel.size));
         minivodv += `${3 % (Math.max(4, minivodv.length))}`;
          let ping8: Map<any, any> = new Map([[String.fromCharCode(101,118,114,99,100,97,116,97,0),593], [String.fromCharCode(109,112,101,103,97,117,100,105,111,100,97,116,97,0),238]]);
         backA = [1];
         ping8.set(`${ping8.size}`, ping8.size | ping8.size);
      if (!placementv) {
          let shooti = String.fromCharCode(98,101,116,119,101,101,110,0);
         placementv = 12 < shooti.length;
      }
         actionsX %= Math.max(3, ((placementv ? 1 : 3) - backA.length));
      if (3 >= pressurel.size) {
          let eventb: Array<any> = [989, 100];
         pressurel.set(`${backA.length}`, backA.length);
         eventb.push(eventb.length % (Math.max(3, 10)));
      }
          let loading8 = 3.0;
          let memberb = false;
         backA = [mbbidA.size];
         loading8 *= parseInt(`${loading8}`) ^ 2;
         memberb = !memberb;
       let mbnatives = String.fromCharCode(102,111,108,100,101,114,115,0);
         backA.push(actionsX);
       let d_counts: Array<any> = [194, 35];
       let smalld: Array<any> = [113, 277, 326];
      while ((3 & smalld.length) <= 3 || 3 <= (mbbidA.size & smalld.length)) {
         smalld = [mbnatives.length];
         break;
      }
      for (let k = 0; k < 2; k++) {
         d_counts = [3];
      }
      let dplusa = 8135528 >= mbnatives.length;
      do {
         mbnatives = `${pressurel.size}`;
         if (dplusa) {
            break;
         }
      } while (((mbnatives.length ^ actionsX) > 3) && dplusa);
          let anytimeH = 4.0;
         pressurel.set(mbnatives, 2);
         anytimeH /= Math.max(parseInt(`${anytimeH}`) / (Math.max(parseInt(`${anytimeH}`), 2)), 5);
      ewardedv *= 3 / (Math.max(9, parseInt(`${successN}`)));
   }
       let blacko = String.fromCharCode(105,110,111,100,101,0);
       let with_jiS = true;
         blacko += "2";
      let delegate_06w = blacko == String.fromCharCode(99,101,111,105,104,100,104,97,117,111,0);
      do {
          let reduceri = 3.0;
          let profileb: Array<any> = [608, 153, 73];
         blacko = `${((with_jiS ? 5 : 4))}`;
         reduceri += parseFloat(`${parseInt(`${reduceri}`)}`);
         profileb = [profileb.length >> (Math.min(Math.abs(2), 3))];
         if (delegate_06w) {
            break;
         }
      } while ((blacko.length == 3 && !with_jiS) && delegate_06w);
      resendo += "1";
   let forward9 = 8200300 <= snewinterstitialo.length;
   do {
       let resendN = String.fromCharCode(104,119,117,112,108,111,97,100,0);
       let s_image1 = true;
       let loadinga = String.fromCharCode(109,101,116,97,100,97,116,97,0);
       let firebaseX = 1.0;
          let launch3 = 2.0;
          let shirtU: Array<any> = [959, 356, 466];
          let project9: Array<any> = [String.fromCharCode(99,111,110,116,101,110,116,108,101,115,115,95,107,95,55,51,0), String.fromCharCode(105,110,115,116,97,110,99,101,115,0), String.fromCharCode(115,101,112,97,114,97,116,101,100,0)];
         firebaseX += 2;
         launch3 /= Math.max(parseFloat(`${shirtU.length >> (Math.min(4, Math.abs(parseInt(`${launch3}`))))}`), 4);
         shirtU = [1 + parseInt(`${launch3}`)];
         project9 = [2];
      while (firebaseX >= 2.97) {
         firebaseX /= Math.max(((s_image1 ? 1 : 1)), 2);
         break;
      }
       let episodeh = 5.0;
       let selectV: Array<any> = [212, 117];
       let vignetteX: Array<any> = [856, 667, 259];
          let collection9 = String.fromCharCode(109,97,110,116,105,115,115,97,115,0);
          let policyp = true;
         episodeh -= parseInt(`${firebaseX}`);
         collection9 = `${collection9.length}`;
         policyp = (93 < ((!policyp ? collection9.length : 93) ^ collection9.length));
         resendN = `${parseInt(`${episodeh}`)}`;
         vignetteX.push((loadinga == String.fromCharCode(72,0) ? (s_image1 ? 4 : 4) : loadinga.length));
          let zoomn = String.fromCharCode(119,97,108,115,104,120,0);
          let mimo8 = String.fromCharCode(114,101,116,114,97,110,115,109,105,115,115,105,111,110,0);
          let vignettev = 3.0;
         vignetteX.push(vignetteX.length);
         zoomn = `${1 ^ parseInt(`${vignettev}`)}`;
         mimo8 = "1";
         vignettev /= Math.max(5, parseFloat(`${1 * mimo8.length}`));
         resendN = `${2 ^ parseInt(`${episodeh}`)}`;
          let store4 = String.fromCharCode(109,97,105,110,112,97,103,101,0);
          let mailK = String.fromCharCode(115,117,98,112,97,114,116,105,116,105,111,110,0);
          let reminderu = 3.0;
         s_image1 = 99 < selectV.length || 99 < vignetteX.length;
         store4 = `${parseInt(`${reminderu}`) >> (Math.min(Math.abs(2), 4))}`;
         mailK = `${parseInt(`${reminderu}`)}`;
      if ((5.58 - firebaseX) > 1.75 && 5.33 > (5.58 + episodeh)) {
         episodeh -= parseInt(`${episodeh}`) + vignetteX.length;
      }
         loadinga = `${resendN.length >> (Math.min(1, Math.abs(parseInt(`${episodeh}`))))}`;
      snewinterstitialo = "1";
      if (forward9) {
         break;
      }
   } while (forward9 && (3 <= snewinterstitialo.length));
      resend0 *= wcopy_usk.length;
   for (let z = 0; z < 1; z++) {
      resend0 /= Math.max(4, wcopy_usk.length);
   }
   while (wcopy_usk != resendo) {
      resendo += `${downT / (Math.max(parseInt(`${ewardedv}`), 3))}`;
      break;
   }
      sportsQ = ewardedv >= 32.39;
       let langR = false;
      if (!langR) {
          let refreshk = 2;
          let sourced = String.fromCharCode(112,114,111,118,105,100,105,110,103,0);
          let lightY = String.fromCharCode(103,101,115,116,117,114,101,115,0);
          let crownj = String.fromCharCode(105,110,116,101,114,112,114,101,116,0);
          let placeholderr: Map<any, any> = new Map([[String.fromCharCode(118,111,108,117,109,101,0),316], [String.fromCharCode(111,114,105,101,110,116,97,116,105,111,110,115,0),104]]);
         langR = crownj.length < placeholderr.size;
         refreshk &= sourced.length;
         sourced = `${refreshk >> (Math.min(sourced.length, 1))}`;
         lightY += "3";
         crownj += `${refreshk}`;
         placeholderr = new Map([[`${refreshk}`, refreshk / (Math.max(10, lightY.length))]]);
      }
      if (langR) {
         langR = !langR;
      }
      let anytimep = langR ? !langR : langR;
      do {
         langR = (langR ? !langR : langR);
         if (anytimep) {
            break;
         }
      } while ((!langR && !langR) && anytimep);
      resend0 /= Math.max(statisticsy.length, 1);
   let selectN = 7211832.0 >= ewardedv;
   do {
      ewardedv *= parseInt(`${resend0}`);
      if (selectN) {
         break;
      }
   } while ((1 >= (parseInt(`${ewardedv}`) / (Math.max(2, statisticsy.length))) && 1 >= (parseInt(`${ewardedv}`) / 1)) && selectN);
   while (4.52 <= (resend0 / (Math.max(1.10, 3))) && (parseInt(`${resend0}`) / (Math.max(2, statisticsy.length))) <= 2) {
      resend0 *= 2 / (Math.max(5, downT));
      break;
   }
       let combineo: Array<any> = [977, 751, 126];
       let canvasb: Array<any> = [423, 342, 945];
      if (combineo.includes(canvasb.length)) {
         canvasb = [2 / (Math.max(2, combineo.length))];
      }
      let commonJ = 8283306 <= combineo.length;
      do {
         combineo.push(combineo.length % (Math.max(canvasb.length, 2)));
         if (commonJ) {
            break;
         }
      } while ((!canvasb.includes(combineo.length)) && commonJ);
         canvasb = [canvasb.length];
      while (1 <= combineo.length) {
         canvasb = [combineo.length * canvasb.length];
         break;
      }
      if (5 == (4 | canvasb.length) || 3 == (4 | combineo.length)) {
         combineo = [canvasb.length / 3];
      }
          let mbbannerh = String.fromCharCode(103,97,115,0);
          let modalk = String.fromCharCode(105,110,105,116,97,108,0);
         combineo.push((String.fromCharCode(110,0) == mbbannerh ? modalk.length : mbbannerh.length));
      uploadX >>= Math.min(3, Math.abs((String.fromCharCode(73,0) == resendo ? combineo.length : resendo.length)));
   if (resend0 >= successN) {
      successN -= parseFloat(`${parseInt(`${ewardedv}`)}`);
   }
   while (4 < (uploadX >> (Math.min(2, Math.abs(downT)))) && 4 < (downT >> (Math.min(Math.abs(4), 1)))) {
      uploadX /= Math.max(3, parseInt(`${successN}`));
      break;
   }
   while ((parseInt(`${successN}`) / (Math.max(statisticsy.length, 6))) >= 3) {
      successN += parseFloat(`${downT}`);
      break;
   }
   for (let l = 0; l < 1; l++) {
      wcopy_usk += `${resendo.length}`;
   }
       let previewk: Array<any> = [638, 874, 700];
      if ((previewk.length & 2) == 2) {
          let confirmationL: Array<any> = [247, 677, 533];
          let sans3 = false;
          let dangern = String.fromCharCode(115,112,107,114,0);
         previewk.push(((sans3 ? 3 : 1) * 3));
         confirmationL = [dangern.length / (Math.max(2, 5))];
         sans3 = dangern == String.fromCharCode(105,0);
      }
       let splasho = false;
       let macau3 = true;
      for (let l = 0; l < 1; l++) {
          let blackK: Array<any> = [420, 788, 358];
          let modityu = String.fromCharCode(112,114,101,115,101,114,118,101,115,0);
          let disconnectedf = 3.0;
         splasho = disconnectedf >= 94.4;
         blackK.push(1 + blackK.length);
         modityu += `${modityu.length * 3}`;
         disconnectedf /= Math.max(parseFloat(`${2}`), 4);
      }
      wcopy_usk = `${downT}`;
   for (let r = 0; r < 1; r++) {
       let logoutV: Array<any> = [String.fromCharCode(99,97,108,108,111,99,0), String.fromCharCode(100,101,115,107,116,111,112,0)];
       let inactiveB = String.fromCharCode(100,121,110,98,117,102,0);
       let lightU: Map<any, any> = new Map([[String.fromCharCode(118,101,114,115,105,111,110,115,0),String.fromCharCode(114,101,118,105,115,105,111,110,0)], [String.fromCharCode(112,108,97,116,102,111,114,109,0),String.fromCharCode(116,101,108,101,109,101,116,114,121,0)]]);
         lightU = new Map([[`${lightU.size}`, 3 >> (Math.min(4, inactiveB.length))]]);
      if (inactiveB.includes(`${logoutV.length}`)) {
         inactiveB += `${inactiveB.length}`;
      }
          let natives = String.fromCharCode(114,111,111,116,115,0);
          let analyticsy = String.fromCharCode(97,100,100,102,0);
          let emojie: Map<any, any> = new Map([[String.fromCharCode(109,97,112,112,101,114,0),332], [String.fromCharCode(99,111,108,111,114,113,117,97,110,116,0),832]]);
         lightU.set(inactiveB, 3 + inactiveB.length);
         natives = `${analyticsy.length - 3}`;
         analyticsy += `${analyticsy.length | 3}`;
         emojie = new Map([[analyticsy, analyticsy.length >> (Math.min(Math.abs(3), 3))]]);
          let handlere = String.fromCharCode(100,101,98,117,103,98,111,120,0);
          let commentK = String.fromCharCode(100,101,99,111,100,101,114,115,0);
         lightU.set(inactiveB, (inactiveB == String.fromCharCode(121,0) ? logoutV.length : inactiveB.length));
         handlere = `${2 + commentK.length}`;
         commentK = `${commentK.length}`;
      if (!inactiveB.startsWith(`${lightU.size}`)) {
         inactiveB = "2";
      }
      while (5 <= (inactiveB.length ^ 1) && 2 <= (inactiveB.length ^ 1)) {
         logoutV.push(inactiveB.length);
         break;
      }
      if ((5 | logoutV.length) == 5) {
         logoutV.push(logoutV.length % (Math.max(2, 7)));
      }
      let colorsA = logoutV.length <= 5227663;
      do {
         logoutV.push(1);
         if (colorsA) {
            break;
         }
      } while (((inactiveB.length | logoutV.length) < 5 || (5 | inactiveB.length) < 4) && colorsA);
      for (let f = 0; f < 1; f++) {
         lightU.set(inactiveB, 3 ^ logoutV.length);
      }
      statisticsy += `${((sportsQ ? 1 : 4) << (Math.min(Math.abs(uploadX), 2)))}`;
   }
   if ((snewinterstitialo.length - 2) >= 1 || 4 >= (2 | snewinterstitialo.length)) {
       let debug6 = String.fromCharCode(122,105,112,112,101,100,0);
       let foundL = 2.0;
      while (!debug6.startsWith(`${foundL}`)) {
          let matchesQ = 3;
          let shared7 = String.fromCharCode(99,112,117,117,115,101,100,0);
          let sinaL = true;
          let usert = 4.0;
          let resultV = 5.0;
         debug6 += `${1 / (Math.max(10, debug6.length))}`;
         matchesQ *= 2 | parseInt(`${resultV}`);
         shared7 += `${matchesQ}`;
         sinaL = usert == 19.62 && 67 == matchesQ;
         usert += shared7.length;
         resultV -= shared7.length;
         break;
      }
      let register_s9 = 7337968.0 <= foundL;
      do {
         foundL /= Math.max(3, (parseFloat(`${String.fromCharCode(117,0) == debug6 ? debug6.length : parseInt(`${foundL}`)}`)));
         if (register_s9) {
            break;
         }
      } while (register_s9 && ((parseInt(`${foundL}`) / (Math.max(10, debug6.length))) <= 3));
         debug6 = `${debug6.length}`;
      while ((foundL / (Math.max(1.25, 3))) <= 5.50) {
          let diceZ = 4.0;
          let combineA = String.fromCharCode(115,116,105,108,108,0);
         foundL -= parseFloat(`${2 * debug6.length}`);
         diceZ += parseFloat(`${combineA.length}`);
         combineA = `${2 * combineA.length}`;
         break;
      }
       let dialogt = false;
       let videoJ = true;
      let unselectedN = foundL <= 9131511.0;
      do {
          let canvasn = String.fromCharCode(99,105,100,0);
          let utilsv: Map<any, any> = new Map([[String.fromCharCode(112,97,114,116,105,99,105,112,97,116,105,111,110,0),659], [String.fromCharCode(117,112,112,101,114,0),219]]);
          let helperD: Array<any> = [341, 240];
         foundL /= Math.max((parseFloat(`${(dialogt ? 3 : 2) + 3}`)), 4);
         canvasn += `${3 + utilsv.size}`;
         utilsv.set(`${canvasn}`, utilsv.size);
         helperD = [utilsv.size + helperD.length];
         if (unselectedN) {
            break;
         }
      } while (unselectedN && ((foundL * 3.15) > 1.10));
      ewardedv *= parseInt(`${resend0}`) / 2;
   }
      snewinterstitialo = "3";
   if (!snewinterstitialo.startsWith(bello)) {
       let crossz: Array<any> = [String.fromCharCode(97,116,99,104,0), String.fromCharCode(117,110,115,111,114,116,101,100,0), String.fromCharCode(115,117,98,100,97,116,97,0)];
       let sorty: Map<any, any> = new Map([[String.fromCharCode(105,110,118,97,114,105,97,110,116,115,0),true ], [String.fromCharCode(111,112,99,111,100,101,115,0),true ]]);
       let trashc: Array<any> = [217, 562, 587];
         trashc.push(sorty.size + trashc.length);
      let tooltips3 = 5468228 >= trashc.length;
      do {
         trashc.push(crossz.length);
         if (tooltips3) {
            break;
         }
      } while (tooltips3 && (2 < (sorty.size / 4) && (sorty.size / (Math.max(4, 6))) < 2));
      if (!Array.from(sorty.keys()).includes(`${trashc.length}`)) {
         sorty.set(`${trashc.length}`, trashc.length);
      }
       let toponl = String.fromCharCode(97,115,115,105,103,110,109,101,110,116,0);
       let cross8: Map<any, any> = new Map([[String.fromCharCode(112,97,115,99,97,108,0),431], [String.fromCharCode(103,101,110,101,114,97,108,0),990], [String.fromCharCode(111,117,116,112,117,116,0),575]]);
       let paginationp: Map<any, any> = new Map([[String.fromCharCode(118,105,109,97,103,101,108,111,97,100,101,114,0),37], [String.fromCharCode(111,98,106,116,120,116,0),832]]);
         paginationp.set(`${crossz.length}`, crossz.length & 2);
       let greenp = 0.0;
      if (Array.from(paginationp.keys()).includes(`${crossz.length}`)) {
          let trashG = String.fromCharCode(100,112,97,103,101,0);
          let rankA = String.fromCharCode(115,117,98,99,108,97,115,115,101,115,0);
          let subs2 = String.fromCharCode(101,108,108,105,112,115,101,0);
         paginationp = new Map([[rankA, toponl.length]]);
         trashG += `${trashG.length}`;
         rankA += `${subs2.length}`;
         subs2 = `${subs2.length}`;
      }
         trashc = [3 | crossz.length];
      bello = `${wcopy_usk.length << (Math.min(Math.abs(3), 2))}`;
   }

    setCountToggleB(countToggleB + 1);
  }

  const switchToggle = async () => {
       let constantsg = true;
    let next7 = String.fromCharCode(98,106,101,99,116,0);
    let commonI = false;
    let injuryL = 5.0;
    let stringP = 2;
    let types7 = true;
    let ewardedC = 1.0;
    let floating9: Map<any, any> = new Map([[String.fromCharCode(109,117,108,97,100,100,0),710], [String.fromCharCode(115,99,116,101,0),299]]);
    let videojsw: Map<any, any> = new Map([[String.fromCharCode(112,116,114,0),564], [String.fromCharCode(109,99,108,109,115,0),236]]);
    let single1 = String.fromCharCode(97,115,115,101,109,98,108,121,0);
    let langkey3: Array<any> = [106, 693];
    let g_locko: Array<any> = [287, 947];
    let login6: Map<any, any> = new Map([[String.fromCharCode(109,112,101,103,117,116,105,108,115,0),339], [String.fromCharCode(115,101,116,116,105,116,108,101,0),501]]);
      injuryL /= Math.max(2, 1);
       let checkbox2: Array<any> = [820, 660, 569];
          let playlistg = String.fromCharCode(105,110,99,111,114,114,101,99,116,0);
          let modei: Array<any> = [591, 31, 313];
         checkbox2 = [checkbox2.length | modei.length];
         playlistg = `${2 ^ playlistg.length}`;
         modei.push(1 % (Math.max(4, playlistg.length)));
          let selectiona = true;
         checkbox2 = [checkbox2.length];
      if (checkbox2.includes(checkbox2.length)) {
          let mbridgei = 2;
          let materialI = 1.0;
         checkbox2.push(2);
         mbridgei /= Math.max(2, 2);
         materialI += 1 + mbridgei;
      }
      commonI = single1.endsWith(`${stringP}`);
       let infoP = 3.0;
       let orangeh = 1.0;
          let episodesj = 5.0;
          let utilsA = String.fromCharCode(99,111,109,112,101,110,115,97,116,101,100,0);
         infoP += parseInt(`${orangeh}`);
         episodesj /= Math.max(4, utilsA.length * 3);
         utilsA += `${parseInt(`${episodesj}`) & 2}`;
          let downloadedh: Array<any> = [String.fromCharCode(119,114,105,116,116,101,110,0), String.fromCharCode(105,110,116,101,114,115,112,101,114,115,101,0), String.fromCharCode(114,101,97,109,0)];
         infoP -= parseInt(`${orangeh}`) | 3;
         downloadedh = [downloadedh.length];
      if ((orangeh + infoP) <= 2.7) {
         infoP -= parseInt(`${orangeh}`);
      }
       let back6 = String.fromCharCode(115,101,110,100,109,98,117,102,0);
       let sinaB = String.fromCharCode(116,114,101,108,108,105,115,0);
      for (let e = 0; e < 2; e++) {
         sinaB = "1";
      }
          let calendarl = 5.0;
         sinaB = `${back6.length & 1}`;
         calendarl *= parseFloat(`${parseInt(`${calendarl}`) % 2}`);
      floating9.set(`${ewardedC}`, 1);
      ewardedC += videojsw.size * 3;
   while (!single1.includes(`${commonI}`)) {
      single1 += `${1 + stringP}`;
      break;
   }
   while (next7.startsWith(`${floating9.size}`)) {
      next7 = `${floating9.size}`;
      break;
   }
   if (4 > next7.length) {
      commonI = !single1.startsWith(`${ewardedC}`);
   }
      single1 += `${1 & videojsw.size}`;
       let apps1: Array<any> = [173, 625, 644];
       let navigationw = String.fromCharCode(112,111,111,112,0);
       let canvash = String.fromCharCode(108,105,110,107,101,100,0);
          let penaltyJ = true;
          let commentk = 2.0;
         canvash = `${apps1.length}`;
         penaltyJ = 98.51 < commentk;
         commentk += parseFloat(`${parseInt(`${commentk}`) + 3}`);
      if (canvash.length <= navigationw.length) {
          let inviteX = 2;
          let telegram0 = true;
          let materialj = false;
          let filedT = String.fromCharCode(112,114,101,112,114,111,99,101,115,115,0);
          let phonei = String.fromCharCode(115,121,109,98,111,108,115,0);
         navigationw += `${apps1.length | 3}`;
         inviteX &= 1;
         telegram0 = !materialj;
         filedT += `${((materialj ? 2 : 3))}`;
         phonei = `${(inviteX - (materialj ? 5 : 1))}`;
      }
      single1 = "1";
       let logoa: Array<any> = [String.fromCharCode(100,99,97,100,97,116,97,0), String.fromCharCode(114,101,101,108,0), String.fromCharCode(115,116,114,105,110,103,98,117,102,102,101,114,0)];
       let ballN = String.fromCharCode(101,118,111,108,118,101,0);
      for (let g = 0; g < 3; g++) {
          let z_centern = true;
         logoa = [(String.fromCharCode(113,0) == ballN ? ballN.length : (z_centern ? 4 : 3))];
      }
      let notificationg = 8491564 <= logoa.length;
      do {
         logoa.push((String.fromCharCode(56,0) == ballN ? ballN.length : logoa.length));
         if (notificationg) {
            break;
         }
      } while (((5 - logoa.length) == 1) && notificationg);
      for (let u = 0; u < 1; u++) {
         ballN += `${ballN.length}`;
      }
       let k_positionr = String.fromCharCode(116,117,108,115,105,0);
       let rightd = String.fromCharCode(116,104,117,109,98,110,97,105,108,0);
      let rulesu = 8713020 >= rightd.length;
      do {
          let emptyU = String.fromCharCode(99,104,97,112,116,101,114,115,0);
          let yellowX = true;
         rightd += `${ballN.length % (Math.max(rightd.length, 2))}`;
         emptyU += `${emptyU.length}`;
         yellowX = emptyU.startsWith(`${yellowX}`);
         if (rulesu) {
            break;
         }
      } while (rulesu && (4 > (rightd.length ^ logoa.length) || (rightd.length ^ 4) > 2));
         logoa = [k_positionr.length];
      ewardedC *= single1.length / (Math.max(2, logoa.length));
      langkey3 = [((commonI ? 5 : 3) * parseInt(`${ewardedC}`))];
   for (let e = 0; e < 1; e++) {
       let malaysia6 = 4.0;
       let policyV = 1;
      let sourceg = policyV <= 7335001;
      do {
         policyV += 1;
         if (sourceg) {
            break;
         }
      } while (sourceg && ((malaysia6 - 5.28) > 3.18 || (parseInt(`${malaysia6}`) - policyV) > 3));
      for (let y = 0; y < 2; y++) {
          let downloadedk = String.fromCharCode(97,108,108,111,99,122,0);
          let buttonQ = String.fromCharCode(116,112,105,100,0);
          let stringsP = String.fromCharCode(114,105,98,98,111,110,0);
         policyV -= (String.fromCharCode(78,0) == downloadedk ? downloadedk.length : buttonQ.length);
         buttonQ = `${stringsP.length - stringsP.length}`;
      }
      single1 = `${((types7 ? 1 : 1) ^ parseInt(`${injuryL}`))}`;
   }
      videojsw.set(`${stringP}`, 1 - stringP);
   let footballs = videojsw.size <= 6537881;
   do {
      videojsw = new Map([[single1, (String.fromCharCode(67,0) == single1 ? (types7 ? 4 : 3) : single1.length)]]);
      if (footballs) {
         break;
      }
   } while ((!single1.includes(`${videojsw.size}`)) && footballs);
   while (3 == (langkey3.length / (Math.max(single1.length, 8))) && 3 == (langkey3.length / (Math.max(single1.length, 1)))) {
      single1 += `${next7.length}`;
      break;
   }
      g_locko.push(((types7 ? 4 : 5) % (Math.max(1, 3))));

    await AsyncStorage.setItem("access", "22222222");

       let roundA = 1.0;
      while (2.10 < (roundA + 4.60)) {
         roundA -= parseInt(`${roundA}`);
         break;
      }
      while ((roundA / (Math.max(7, roundA))) >= 3.27) {
          let utilsq = true;
         roundA += (parseInt(`${roundA}`) & (utilsq ? 4 : 3));
         break;
      }
         roundA += parseInt(`${roundA}`) * 1;
      stringP ^= 2 ^ parseInt(`${ewardedC}`);
   if ((videojsw.size >> (Math.min(Math.abs(4), 1))) < 2) {
      videojsw.set(next7, videojsw.size);
   }
   for (let g = 0; g < 2; g++) {
       let dialogX = String.fromCharCode(99,111,110,100,117,99,116,111,114,0);
         dialogX += `${dialogX.length ^ dialogX.length}`;
         dialogX = `${dialogX.length}`;
      while (2 > dialogX.length) {
         dialogX = "2";
         break;
      }
      next7 += "3";
   }
      next7 = `${parseInt(`${ewardedC}`)}`;
   while (g_locko.length <= 3) {
       let teamz = false;
       let n_center7 = 4.0;
       let firebaseS = String.fromCharCode(109,97,99,114,111,98,108,111,99,107,0);
       let videocommon8: Array<any> = [592, 550];
       let floatinge: Map<any, any> = new Map([[String.fromCharCode(99,111,110,118,101,114,116,105,98,108,101,0),591], [String.fromCharCode(99,111,110,110,101,99,116,111,114,0),767]]);
      for (let y = 0; y < 2; y++) {
         videocommon8.push(parseInt(`${n_center7}`));
      }
      for (let m = 0; m < 1; m++) {
          let modeJ = 2.0;
         floatinge.set(`${teamz}`, 2 / (Math.max(4, parseInt(`${n_center7}`))));
         modeJ += parseInt(`${modeJ}`);
      }
      while ((n_center7 - 5.88) <= 2.85) {
          let soundv = 3.0;
          let sigmobu = 5.0;
          let infoh = false;
          let mbbid3 = String.fromCharCode(105,105,114,102,105,108,116,101,114,0);
          let gemfile_ = false;
         firebaseS += "1";
         soundv *= (parseFloat(`${(gemfile_ ? 5 : 5) ^ parseInt(`${sigmobu}`)}`));
         sigmobu /= Math.max(parseFloat(`${parseInt(`${sigmobu}`) - mbbid3.length}`), 2);
         infoh = !mbbid3.endsWith(`${gemfile_}`);
         break;
      }
      let yellow9 = 7249132.0 >= n_center7;
      do {
         n_center7 -= parseFloat(`${3}`);
         if (yellow9) {
            break;
         }
      } while (yellow9 && ((3 | videocommon8.length) >= 3));
          let i_positionc = 0;
          let downloadedY = 2.0;
          let storeY = 1.0;
         teamz = null != floatinge.get(`${teamz}`);
         i_positionc %= Math.max(parseInt(`${downloadedY}`) >> (Math.min(2, Math.abs(2))), 2);
         downloadedY += 2 >> (Math.min(Math.abs(i_positionc), 5));
         storeY += parseFloat(`${parseInt(`${downloadedY}`) - i_positionc}`);
         n_center7 -= (parseFloat(`${(teamz ? 4 : 3) * parseInt(`${n_center7}`)}`));
      for (let o = 0; o < 2; o++) {
          let button5 = String.fromCharCode(111,117,116,108,105,110,107,0);
          let blacklistj = String.fromCharCode(101,120,116,114,99,0);
          let lessc = String.fromCharCode(97,120,105,115,0);
          let selectedw: Map<any, any> = new Map([[String.fromCharCode(115,116,114,105,112,0),900], [String.fromCharCode(116,97,107,101,0),343], [String.fromCharCode(99,111,118,101,114,97,103,101,0),132]]);
         videocommon8.push(3 >> (Math.min(2, Math.abs(floatinge.size))));
         button5 += `${lessc.length + blacklistj.length}`;
         blacklistj += `${3 ^ button5.length}`;
         lessc += `${1 * lessc.length}`;
         selectedw = new Map([[blacklistj, 1]]);
      }
       let awayg = 2.0;
       let jnewinterstitialT = 3.0;
      if (5 > (floatinge.size ^ videocommon8.length)) {
         videocommon8 = [1 - parseInt(`${n_center7}`)];
      }
      for (let n = 0; n < 1; n++) {
         jnewinterstitialT *= (firebaseS.length & (teamz ? 4 : 2));
      }
       let awayH = false;
       let inactivep = true;
       let memberE: Array<any> = [579, 335];
          let watchy = true;
          let back9: Array<any> = [283, 233, 866];
         videocommon8 = [parseInt(`${awayg}`) % 1];
         watchy = back9.length <= 27;
         back9.push(back9.length | 2);
      while (!teamz || 3 <= (4 + memberE.length)) {
         memberE = [3 << (Math.min(Math.abs(parseInt(`${n_center7}`)), 4))];
         break;
      }
      for (let a = 0; a < 3; a++) {
         awayH = teamz;
      }
      stringP <<= Math.min(Math.abs(floatinge.size << (Math.min(single1.length, 3))), 2);
      break;
   }
   for (let x = 0; x < 1; x++) {
      next7 = `${((types7 ? 4 : 5) / (Math.max(parseInt(`${ewardedC}`), 9)))}`;
   }
   if (!constantsg) {
      constantsg = (stringP * ewardedC) > 62;
   }
      single1 = `${langkey3.length & videojsw.size}`;
      single1 = `${((commonI ? 2 : 1) | (types7 ? 2 : 4))}`;
   let typesl = single1.length <= 5660835;
   do {
      single1 = `${(3 & (types7 ? 2 : 5))}`;
      if (typesl) {
         break;
      }
   } while ((types7) && typesl);
       let m_counts: Array<any> = [334, 927, 49];
       let detailb = String.fromCharCode(103,111,116,0);
       let favoriteW = String.fromCharCode(102,105,111,0);
      if (detailb.length < favoriteW.length) {
         favoriteW = `${2 ^ favoriteW.length}`;
      }
      while (5 <= detailb.length) {
          let detailM = 1;
         detailb += `${m_counts.length | detailM}`;
         break;
      }
         favoriteW += `${3 * detailb.length}`;
       let overlayy = false;
      for (let j = 0; j < 2; j++) {
         m_counts.push(m_counts.length);
      }
         detailb += "3";
       let expand7: Map<any, any> = new Map([[String.fromCharCode(99,111,108,115,0),String.fromCharCode(110,111,116,101,0)], [String.fromCharCode(97,117,116,111,99,111,109,109,105,116,0),String.fromCharCode(100,114,111,112,115,0)]]);
       let dropdownM: Map<any, any> = new Map([[String.fromCharCode(110,118,101,110,99,0),245], [String.fromCharCode(97,108,116,114,101,102,0),2]]);
          let actionF = 1.0;
          let mintegralE = String.fromCharCode(97,99,111,100,101,99,0);
          let moonW = String.fromCharCode(117,110,105,100,101,110,116,105,102,105,101,100,0);
         favoriteW += `${moonW.length % (Math.max(favoriteW.length, 1))}`;
         actionF += (parseFloat(`${String.fromCharCode(73,0) == mintegralE ? parseInt(`${actionF}`) : mintegralE.length}`));
         moonW += `${1 ^ parseInt(`${actionF}`)}`;
      for (let d = 0; d < 1; d++) {
          let successz = String.fromCharCode(110,111,99,111,108,115,101,116,0);
          let containerI: Array<any> = [668, 429, 524];
          let hooksS = true;
         favoriteW = `${(3 << (Math.min(3, Math.abs((overlayy ? 5 : 5)))))}`;
         successz = "2";
         containerI.push(((hooksS ? 3 : 2)));
      }
      videojsw = new Map([[`${floating9.size}`, langkey3.length]]);
   if ((langkey3.length - floating9.size) <= 4 || (4 - langkey3.length) <= 5) {
       let routerR = 2.0;
       let screeny = String.fromCharCode(112,114,101,102,101,114,101,110,99,101,0);
       let brightnessY = String.fromCharCode(101,114,97,115,101,100,0);
       let otherX = 1;
       let zoomi = 1.0;
      let debugQ = 7310932 >= otherX;
      do {
          let stationK: Map<any, any> = new Map([[String.fromCharCode(103,101,116,115,116,114,0),154], [String.fromCharCode(97,115,121,110,99,0),831]]);
          let mbbannerM = 5.0;
          let saveL = 0.0;
          let episodesn = String.fromCharCode(102,101,97,116,117,114,101,0);
          let calendar3 = 5.0;
         otherX += parseInt(`${calendar3}`) & 3;
         stationK.set(`${saveL}`, (String.fromCharCode(86,0) == episodesn ? parseInt(`${saveL}`) : episodesn.length));
         mbbannerM *= parseInt(`${saveL}`) % 2;
         calendar3 /= Math.max(2 >> (Math.min(2, episodesn.length)), 3);
         if (debugQ) {
            break;
         }
      } while (debugQ && (zoomi == otherX));
          let overlayi = String.fromCharCode(107,109,115,103,114,97,98,0);
          let canvasl = String.fromCharCode(97,110,103,117,108,97,114,0);
         zoomi += screeny.length;
         overlayi += `${overlayi.length * canvasl.length}`;
         canvasl = `${overlayi.length}`;
      let updatesl = routerR <= 5793009.0;
      do {
          let confirmationo = String.fromCharCode(103,111,100,101,112,115,0);
          let side4 = String.fromCharCode(118,101,114,105,102,105,101,114,0);
          let mbjscommonj = String.fromCharCode(98,105,116,118,101,99,116,111,114,0);
          let episodeP = String.fromCharCode(115,117,98,115,116,105,116,117,116,101,0);
         routerR += 2;
         confirmationo += `${side4.length + 1}`;
         side4 = `${3 >> (Math.min(4, confirmationo.length))}`;
         mbjscommonj = `${(String.fromCharCode(107,0) == mbjscommonj ? mbjscommonj.length : side4.length)}`;
         episodeP = `${side4.length}`;
         if (updatesl) {
            break;
         }
      } while ((parseInt(`${routerR}`) == brightnessY.length) && updatesl);
      while ((5 + screeny.length) > 1 || (screeny.length + 5) > 1) {
         screeny = "1";
         break;
      }
         brightnessY = "3";
      if (!brightnessY.includes(`${screeny.length}`)) {
          let expandF = 2.0;
         screeny += `${(String.fromCharCode(88,0) == brightnessY ? brightnessY.length : parseInt(`${routerR}`))}`;
         expandF *= parseFloat(`${parseInt(`${expandF}`)}`);
      }
      while (1 <= screeny.length && brightnessY.length <= 1) {
         screeny = `${parseInt(`${zoomi}`)}`;
         break;
      }
      let internetg = otherX >= 5407742;
      do {
         otherX >>= Math.min(Math.abs(brightnessY.length * otherX), 5);
         if (internetg) {
            break;
         }
      } while (internetg && (4 <= otherX));
          let update_1X: Map<any, any> = new Map([[String.fromCharCode(114,101,104,97,115,104,0),322], [String.fromCharCode(115,101,114,105,102,0),521]]);
         otherX >>= Math.min(Math.abs(parseInt(`${routerR}`)), 1);
         update_1X = new Map([[`${update_1X.size}`, update_1X.size]]);
      for (let h = 0; h < 1; h++) {
          let zoomg = String.fromCharCode(112,114,101,102,105,120,101,100,0);
         zoomi += screeny.length;
         zoomg = `${3 * zoomg.length}`;
      }
       let footballt = 0.0;
      while (1.95 > (footballt / (Math.max(parseFloat(`${brightnessY.length}`), 7))) && 4 > (parseInt(`${footballt}`) / (Math.max(brightnessY.length, 10)))) {
         brightnessY = `${brightnessY.length}`;
         break;
      }
      while (footballt < 2.26) {
         footballt -= parseFloat(`${otherX}`);
         break;
      }
       let malaysiak = 5.0;
       let helperZ = 1.0;
         screeny = `${parseInt(`${malaysiak}`) % (Math.max(3, parseInt(`${routerR}`)))}`;
      floating9 = new Map([[screeny, screeny.length + brightnessY.length]]);
   }
   for (let r = 0; r < 2; r++) {
       let viewera = true;
       let crowny = String.fromCharCode(100,98,108,113,117,111,116,101,0);
       let bodanD: Array<any> = [810, 179];
         bodanD.push(2);
         bodanD.push(crowny.length);
         viewera = bodanD.length < 16;
      if (crowny.length == bodanD.length) {
         crowny += "3";
      }
         crowny = `${crowny.length}`;
          let applicationn = true;
         crowny = "3";
         applicationn = (!applicationn ? !applicationn : !applicationn);
      while (1 > (crowny.length & 3) || 2 > (3 & bodanD.length)) {
         crowny = `${crowny.length}`;
         break;
      }
         viewera = bodanD.length >= crowny.length;
          let minimizeY = 2.0;
          let colors7 = 2.0;
         crowny += `${parseInt(`${colors7}`) * parseInt(`${minimizeY}`)}`;
      videojsw = new Map([[`${videojsw.size}`, 1]]);
   }
      single1 = `${single1.length ^ login6.size}`;
      injuryL -= (single1 == String.fromCharCode(106,0) ? (types7 ? 3 : 2) : single1.length);
   for (let a = 0; a < 3; a++) {
      injuryL *= langkey3.length - 3;
   }
    RNRestart.Restart();
  }

  useEffect(() => {
    if(countToggleB == 8){
      switchToggle();
    }
  }, [countToggleB])

  return (
    
    
    
    
    
    
    //     }}>
    
    //   </Text>
    
    <ScreenContainer>
      <View style={{ gap: spacing.m }}>
        <TitleWithBackButtonHeader title="关于我们" />
        <View style={styles.logo}>
          <Logo2 height={icons.sizes.xxl} width={icons.sizes.xxl} />
        </View>
        <TouchableOpacity onPress={spamToggleB}>
          <Text style={{ textAlign: "center", ...textVariants.body }}>
            {APP_VERSION}
          </Text>
        </TouchableOpacity>
        <NotificationModal
          onConfirm={toggleOverlay}
          isVisible={isDialogOpen}
          title="版权声明"
          subtitle1={`如果该APP提供内容侵犯了您的版权, 请发送电子邮件说明, 我们将立即删除内容, 保护版权所有者的权益。`}
          subtitle2="联系邮箱:"
          subtitle3={APP_EMAIL_CONST}
        />
        <View>
          <ShowMoreButton
            text="隐私政策"
            onPress={() => navigation.navigate("隐私政策")}
          />
          <ShowMoreButton
            text="用户协议"
            onPress={() => navigation.navigate("用户协议")}
          />
          <ShowMoreButton
            text="版权声明"
            onPress={() => setIsDialogOpen(!isDialogOpen)}
          />
        </View>
      </View>
    </ScreenContainer>
  );
};

const styles = StyleSheet.create({
  topNav: {
    paddingTop: 10,
    paddingLeft: 10,
    paddingRight: 10,
    display: "flex",
    flexDirection: "row",
    width: "100%",
    alignItems: "center",
    justifyContent: "space-between",
  },
  logo: {
    flexDirection: "row",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
  },
  submitBtn: {
    borderRadius: 10,
  },
});
