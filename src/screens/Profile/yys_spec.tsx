import React, { useEffect, useState } from "react";
import {
  View,
  Text,
  StyleSheet,
  TextInput,
  Image,
  RefreshControl,
  ScrollView,
} from "react-native";
import ScreenContainer from "../../components/container/yys_executor_expand";
import { RootStackScreenProps } from "@type/yys_settings";
import { useTheme } from "@react-navigation/native";
import { yys_MintegralLibavdevice } from "@redux/yys_sport_shrink";

import TitleWithBackButtonHeader from "../../components/header/yys_anner_header";
import axios from "axios";
import { Keyboard } from "react-native";
import {
  API_DOMAIN,
  API_DOMAIN_TEST,
  API_DOMAIN_LOCAL,
} from "@utility/yys_ajax_switch";
import NetInfo, { NetInfoState } from "@react-native-community/netinfo";

import InviteStep from "./../../components/invite/yys_gradle_libreanimated";
import InviteCard from "./../../components/invite/yys_temp";

import InviteHeader from "./../../components/invite/yys_catalog_textlayoutmanager_header";
import { useAppDispatch, useAppSelector, useSelector } from "@hooks/yys_frame";
import { TouchableOpacity } from "@gorhom/bottom-sheet";
import { updateUserAuth } from "@redux/actions/yys_gesture";
import yys_event_common from '../../../Umeng/yys_event_common';
import { yys_GesturesConst } from "@api";
import { yys_HejiCricket } from "@redux/reducers/yys_privacy_round";


export default ({ navigation }: RootStackScreenProps<"邀请">) => {
  const { colors, textVariants, icons, spacing } = useTheme();

  const userState = useSelector<yys_HejiCricket>('userReducer');

  const [refreshing, setRefreshing] = useState(false);
  const dispatch = useAppDispatch();

  
  useEffect(() => {
    yys_event_common.userCenterVipInviteViewsAnalytics();
  }, []);
  

  const handleRefresh = async () => {
       let smallA = false;
    let analyticst = String.fromCharCode(112,111,115,116,95,112,95,51,50,0);
    let neonD = true;
    let kickq: Array<any> = [822, 471, 825];
    let long_6V: Array<any> = [509, 178];
    let neon8 = String.fromCharCode(103,95,53,56,95,110,105,115,116,112,0);
    let upgradeE = 4.0;
    let annerD = String.fromCharCode(109,105,120,105,110,115,95,120,95,53,51,0);
    let mountingG = String.fromCharCode(115,112,101,99,95,115,95,57,51,0);
    let splashv: Map<any, any> = new Map([[String.fromCharCode(109,95,51,57,95,97,114,99,116,105,99,0),243], [String.fromCharCode(100,101,112,111,115,105,116,95,97,95,51,56,0),825]]);
    let actionv = String.fromCharCode(103,95,50,57,95,118,116,101,110,99,0);
    let scorec = String.fromCharCode(113,95,52,48,95,112,111,119,101,114,0);
    let rncoreLN = 4.0;
    let inviteY = 1.0;
    let submit5 = String.fromCharCode(118,99,100,115,112,95,54,95,55,57,0);
    let libmapbufferjniq = String.fromCharCode(117,95,53,50,95,108,105,110,101,98,114,101,97,107,0);
    let libloggerP = 4;
   let castQ = 7480809 <= splashv.size;
   do {
      splashv = new Map([[actionv, 2 << (Math.min(1, actionv.length))]]);
      if (castQ) {
         break;
      }
   } while (castQ && ((splashv.size - 5) > 4 && (long_6V.length - splashv.size) > 5));
       let progress3 = 0.0;
         progress3 /= Math.max(parseFloat(`${parseInt(`${progress3}`)}`), 5);
       let tempt = 1.0;
          let anytimej = true;
          let saveF = 5.0;
         tempt -= parseFloat(`${2 ^ parseInt(`${tempt}`)}`);
         anytimej = !anytimej;
         saveF /= Math.max(parseInt(`${saveF}`) % 3, 2);
      long_6V.push(1);
       let invite9: Map<any, any> = new Map([[String.fromCharCode(102,105,101,108,100,109,97,116,99,104,95,122,95,53,53,0),97], [String.fromCharCode(97,95,57,95,115,117,99,99,101,101,100,0),361]]);
      for (let g = 0; g < 3; g++) {
         invite9.set(`${invite9.size}`, invite9.size % 1);
      }
      for (let p = 0; p < 3; p++) {
         invite9 = new Map([[`${invite9.size}`, invite9.size]]);
      }
      let pnewinterstitialg = 9644699 >= invite9.size;
      do {
         invite9 = new Map([[`${invite9.size}`, invite9.size]]);
         if (pnewinterstitialg) {
            break;
         }
      } while ((invite9.get(`${invite9.size}`) == null) && pnewinterstitialg);
      splashv.set(analyticst, (analyticst == String.fromCharCode(68,0) ? (smallA ? 1 : 2) : analyticst.length));
      annerD = `${neon8.length}`;
   if ((splashv.size % 4) <= 1) {
      splashv = new Map([[`${long_6V.length}`, long_6V.length * parseInt(`${upgradeE}`)]]);
   }
       let debugv = String.fromCharCode(112,95,53,53,95,115,101,97,114,99,104,0);
       let runtimeO = String.fromCharCode(115,101,101,100,95,112,95,49,57,0);
       let indexd = String.fromCharCode(103,95,53,57,95,100,111,110,101,0);
         runtimeO += "3";
         indexd = `${(indexd == String.fromCharCode(109,0) ? debugv.length : indexd.length)}`;
      annerD += `${(scorec.length ^ (smallA ? 4 : 2))}`;
   for (let u = 0; u < 2; u++) {
       let telemetry0 = String.fromCharCode(99,111,109,112,97,114,105,115,111,110,95,56,95,54,49,0);
       let closeL = 0.0;
       let dropdownO = 1.0;
       let roundH: Array<any> = [335, 830, 176];
      let tickedu = 6318708 >= roundH.length;
      do {
         roundH = [parseInt(`${closeL}`) - 3];
         if (tickedu) {
            break;
         }
      } while (((5.43 + dropdownO) >= 2.100 && 5 >= (parseInt(`${dropdownO}`) + roundH.length)) && tickedu);
          let chinap = String.fromCharCode(106,95,50,54,0);
          let modelst = 3.0;
          let targete = String.fromCharCode(116,111,112,109,111,115,116,95,118,95,56,56,0);
         telemetry0 += `${parseInt(`${modelst}`)}`;
         chinap += `${3 & targete.length}`;
         modelst *= parseFloat(`${3 * chinap.length}`);
         targete += `${targete.length ^ chinap.length}`;
          let material1 = 0;
          let successQ = String.fromCharCode(99,111,100,101,99,112,114,105,118,97,116,101,95,51,95,50,51,0);
         telemetry0 = `${1 / (Math.max(3, parseInt(`${closeL}`)))}`;
         material1 %= Math.max(2, (successQ == String.fromCharCode(56,0) ? successQ.length : material1));
       let libjsinspectorF = 2.0;
         dropdownO /= Math.max(parseFloat(`${roundH.length >> (Math.min(3, Math.abs(parseInt(`${dropdownO}`))))}`), 5);
         libjsinspectorF /= Math.max(4, 3);
      let zhengpianp = closeL >= 9287247.0;
      do {
         closeL += telemetry0.length;
         if (zhengpianp) {
            break;
         }
      } while (zhengpianp && (1.93 > (dropdownO + closeL) && (dropdownO + closeL) > 1.93));
         closeL /= Math.max(4, parseInt(`${closeL}`) * telemetry0.length);
      while (3.22 < libjsinspectorF) {
         libjsinspectorF *= telemetry0.length % 1;
         break;
      }
       let libcrashsdkd = String.fromCharCode(109,95,48,95,100,105,116,97,110,99,101,0);
       let zhengpian_ = String.fromCharCode(121,95,57,95,102,99,116,108,0);
         zhengpian_ = `${zhengpian_.length}`;
      let classes5 = 6648921.0 >= libjsinspectorF;
      do {
          let wnewsy = 5;
         libjsinspectorF /= Math.max(5, 1 ^ roundH.length);
         wnewsy ^= wnewsy;
         if (classes5) {
            break;
         }
      } while ((roundH.includes(libjsinspectorF)) && classes5);
      splashv.set(analyticst, mountingG.length ^ 3);
   }
   if ((5 ^ splashv.size) > 1 || splashv.size > 5) {
       let nativemodule5 = String.fromCharCode(109,95,57,57,95,98,110,109,112,105,0);
       let dplusH = 2;
       let telegrami = 5.0;
       let short_4F = String.fromCharCode(105,110,105,116,97,108,95,50,95,55,57,0);
       let gmailT = 4.0;
          let libsgcorex = true;
         telegrami -= short_4F.length;
         libsgcorex = !libsgcorex;
      let topicT = short_4F == String.fromCharCode(122,105,110,104,100,48,112,52,107,0);
      do {
         short_4F += `${(String.fromCharCode(50,0) == nativemodule5 ? parseInt(`${telegrami}`) : nativemodule5.length)}`;
         if (topicT) {
            break;
         }
      } while ((short_4F.length < parseInt(`${gmailT}`)) && topicT);
      let scheduleQ = nativemodule5.length <= 5003874;
      do {
          let nalyticsM = String.fromCharCode(117,95,49,95,105,110,102,111,115,0);
          let arrowx = 5.0;
         nativemodule5 = `${parseInt(`${telegrami}`) % (Math.max(parseInt(`${gmailT}`), 8))}`;
         nalyticsM += `${parseInt(`${arrowx}`)}`;
         arrowx -= (parseFloat(`${nalyticsM == String.fromCharCode(86,0) ? parseInt(`${arrowx}`) : nalyticsM.length}`));
         if (scheduleQ) {
            break;
         }
      } while (((nativemodule5.length << (Math.min(Math.abs(1), 2))) == 2 && (nativemodule5.length << (Math.min(Math.abs(1), 1))) == 1) && scheduleQ);
      let refreshA = 6784084 >= dplusH;
      do {
          let pangles = String.fromCharCode(109,95,49,56,0);
          let linkf = 5.0;
          let animationsh = 4;
         dplusH &= short_4F.length;
         pangles = `${3 / (Math.max(animationsh, 8))}`;
         linkf /= Math.max(4, parseFloat(`${animationsh >> (Math.min(Math.abs(parseInt(`${linkf}`)), 5))}`));
         if (refreshA) {
            break;
         }
      } while ((1 < dplusH) && refreshA);
       let reducere = String.fromCharCode(114,101,97,115,111,110,115,95,110,95,49,49,0);
      if (reducere.includes(`${telegrami}`)) {
          let lessh = 3.0;
          let target_ = String.fromCharCode(102,95,57,55,95,118,112,120,100,101,99,0);
          let refreshP = String.fromCharCode(117,95,50,50,95,115,117,98,114,97,110,103,101,115,0);
          let field7 = String.fromCharCode(97,99,99,101,115,115,111,114,105,101,115,95,112,95,52,49,0);
         telegrami *= 1 - reducere.length;
         lessh *= parseFloat(`${2 | target_.length}`);
         target_ = `${target_.length}`;
         refreshP = `${target_.length}`;
         field7 += `${parseInt(`${lessh}`)}`;
      }
         gmailT /= Math.max(4, parseFloat(`${parseInt(`${gmailT}`)}`));
         reducere += `${reducere.length}`;
      if (reducere.length >= dplusH) {
          let sportj = String.fromCharCode(115,95,51,52,95,112,97,99,107,101,114,0);
          let benefit3 = false;
          let rulesM = String.fromCharCode(111,95,54,55,95,99,111,110,100,101,110,115,101,100,0);
         reducere = `${reducere.length}`;
         sportj = "3";
         benefit3 = sportj.length == 17;
         rulesM += "3";
      }
         reducere += `${dplusH}`;
       let anner1 = 0.0;
       let androidV = 3.0;
      if (3.79 == (4.94 * androidV) && 1 == (3 * parseInt(`${androidV}`))) {
         androidV -= parseInt(`${telegrami}`) >> (Math.min(1, Math.abs(dplusH)));
      }
         gmailT *= parseFloat(`${3 % (Math.max(parseInt(`${anner1}`), 8))}`);
      while (reducere.includes(`${short_4F.length}`)) {
         reducere = "1";
         break;
      }
         nativemodule5 = `${parseInt(`${anner1}`)}`;
      splashv.set(`${neonD}`, (splashv.size & (neonD ? 4 : 5)));
   }
   if (4 > (splashv.size | 3)) {
       let aboutt = 0.0;
       let hearti: Array<any> = [String.fromCharCode(106,95,54,49,95,117,109,111,116,105,111,110,0), String.fromCharCode(102,95,50,53,95,114,101,112,0), String.fromCharCode(97,116,116,101,109,116,115,95,100,95,55,54,0)];
       let referrerv = String.fromCharCode(97,97,99,101,110,99,116,97,98,95,122,95,57,54,0);
       let nalyticsMo = 5;
       let reducerk = String.fromCharCode(101,95,51,49,95,104,97,108,100,99,108,117,116,0);
      let turnN = hearti.length <= 5364392;
      do {
         hearti = [2 ^ nalyticsMo];
         if (turnN) {
            break;
         }
      } while (((5 >> (Math.min(3, hearti.length))) <= 2) && turnN);
       let trophy7 = 1;
         reducerk = `${(String.fromCharCode(69,0) == reducerk ? reducerk.length : referrerv.length)}`;
      while (1 < (trophy7 - hearti.length) || (trophy7 - hearti.length) < 1) {
         trophy7 >>= Math.min(referrerv.length, 3);
         break;
      }
       let typesf = String.fromCharCode(98,95,51,50,95,106,112,103,0);
      for (let b = 0; b < 3; b++) {
          let libruntimeexecutord = true;
         nalyticsMo %= Math.max(trophy7, 5);
         libruntimeexecutord = libruntimeexecutord || libruntimeexecutord;
      }
         aboutt /= Math.max(3, referrerv.length);
          let windV = true;
         nalyticsMo *= 1;
         windV = (windV ? !windV : windV);
          let login6 = true;
          let settingsW: Array<any> = [87, 869, 110];
          let photoZ = String.fromCharCode(112,95,57,48,95,115,109,111,111,116,104,105,110,103,0);
         typesf = `${(referrerv.length - (login6 ? 2 : 1))}`;
         login6 = settingsW.length > 45 && photoZ == String.fromCharCode(49,0);
         settingsW = [2];
         photoZ += `${settingsW.length}`;
      if (nalyticsMo >= reducerk.length) {
         nalyticsMo <<= Math.min(1, Math.abs(nalyticsMo >> (Math.min(3, Math.abs(2)))));
      }
         nalyticsMo <<= Math.min(Math.abs(2), 2);
          let matchesp: Array<any> = [748, 686];
         referrerv += `${referrerv.length}`;
         matchesp.push(matchesp.length);
          let libloggerU: Map<any, any> = new Map([[String.fromCharCode(110,95,51,56,95,98,121,116,101,115,116,114,105,110,103,0),false ], [String.fromCharCode(115,112,101,114,97,116,111,114,95,119,95,49,51,0),true ]]);
         referrerv += `${trophy7 % 3}`;
         libloggerU.set(`${libloggerU.size}`, 3);
         trophy7 <<= Math.min(4, Math.abs(nalyticsMo));
      for (let l = 0; l < 3; l++) {
          let rncoreL = String.fromCharCode(102,95,56,54,95,115,116,111,112,0);
          let typesb = 0.0;
          let unread_: Map<any, any> = new Map([[String.fromCharCode(100,111,110,97,116,105,111,110,95,102,95,57,0),880], [String.fromCharCode(119,95,55,48,95,97,117,116,104,107,101,121,0),595]]);
          let mbnativeadvancedI = String.fromCharCode(115,95,53,56,95,112,97,115,116,101,100,0);
         reducerk = `${referrerv.length >> (Math.min(5, typesf.length))}`;
         rncoreL += `${(mbnativeadvancedI == String.fromCharCode(66,0) ? parseInt(`${typesb}`) : mbnativeadvancedI.length)}`;
         typesb /= Math.max(3, parseFloat(`${parseInt(`${typesb}`) + rncoreL.length}`));
         unread_.set(`${typesb}`, parseInt(`${typesb}`));
      }
      scorec = `${long_6V.length / 2}`;
   }
   let zhengpian_W = splashv.size <= 5171041;
   do {
       let foundL: Map<any, any> = new Map([[String.fromCharCode(98,108,97,99,107,108,105,115,116,95,49,95,51,48,0),109], [String.fromCharCode(115,105,120,116,97,112,95,119,95,56,51,0),103]]);
       let darkF = false;
         foundL = new Map([[`${foundL.size}`, foundL.size]]);
         foundL.set(`${darkF}`, (foundL.size + (darkF ? 1 : 4)));
         darkF = foundL.get(`${darkF}`) == null;
         foundL.set(`${darkF}`, ((darkF ? 3 : 4) & foundL.size));
         darkF = (67 <= ((!darkF ? 67 : foundL.size) ^ foundL.size));
      let redirecty = darkF ? !darkF : darkF;
      do {
          let sentryj = 5.0;
          let description_2I = 2.0;
          let closeC = 4.0;
          let pressureb = 4.0;
         darkF = foundL.size > 2;
         sentryj -= parseFloat(`${1 << (Math.min(Math.abs(parseInt(`${description_2I}`)), 5))}`);
         description_2I -= parseFloat(`${2 << (Math.min(Math.abs(parseInt(`${sentryj}`)), 2))}`);
         closeC += parseFloat(`${parseInt(`${pressureb}`)}`);
         if (redirecty) {
            break;
         }
      } while ((foundL.size > 3 || 2 > (foundL.size >> (Math.min(Math.abs(3), 2)))) && redirecty);
      splashv.set(scorec, 2 | analyticst.length);
      if (zhengpian_W) {
         break;
      }
   } while (zhengpian_W && (!smallA));
      annerD += `${2 - neon8.length}`;
      splashv = new Map([[scorec, 1 ^ mountingG.length]]);

    setRefreshing(true);

   for (let k = 0; k < 2; k++) {
      neon8 += `${analyticst.length}`;
   }
      smallA = (kickq.length << (Math.min(mountingG.length, 2))) > 87;
      neon8 += "2";
      splashv.set(`${upgradeE}`, annerD.length);
   let inactiveA = long_6V.length <= 5469170;
   do {
      long_6V.push(actionv.length);
      if (inactiveA) {
         break;
      }
   } while (((scorec.length & 2) >= 2 || (2 & scorec.length) >= 3) && inactiveA);
      neonD = !neonD;
   let renewV = neonD ? !neonD : neonD;
   do {
      neonD = (83 < (neon8.length * (!neonD ? neon8.length : 83)));
      if (renewV) {
         break;
      }
   } while (renewV && (smallA));
      long_6V = [(2 << (Math.min(1, Math.abs((smallA ? 5 : 4)))))];
      splashv = new Map([[`${upgradeE}`, 1 * analyticst.length]]);
   if ((mountingG.length + 3) >= 5) {
      splashv.set(`${neonD}`, ((neonD ? 2 : 4) / 1));
   }
   let animationG = 9683320 >= annerD.length;
   do {
      annerD += `${annerD.length * splashv.size}`;
      if (animationG) {
         break;
      }
   } while (animationG && (smallA || annerD.length <= 5));
       let setting1 = String.fromCharCode(116,95,55,53,95,102,105,110,97,108,100,0);
          let foundd = true;
         setting1 = `${(setting1.length % (Math.max(3, (foundd ? 5 : 2))))}`;
         setting1 += `${setting1.length * 1}`;
       let event9 = 4.0;
      kickq.push((long_6V.length * (neonD ? 2 : 1)));
    await refreshUserState();

   let moviesE = 6688396 <= annerD.length;
   do {
      annerD = `${(2 * (smallA ? 2 : 1))}`;
      if (moviesE) {
         break;
      }
   } while (moviesE && (5 >= annerD.length));
      scorec = `${annerD.length ^ 2}`;
      splashv = new Map([[`${splashv.size}`, mountingG.length & 2]]);
      annerD = `${parseInt(`${rncoreLN}`) & scorec.length}`;
       let modalc = false;
       let areax: Array<any> = [449, 37];
       let arrowd = String.fromCharCode(114,101,110,100,105,116,105,111,110,115,95,113,95,53,48,0);
      if (!modalc && (1 * areax.length) == 3) {
         areax.push(arrowd.length * areax.length);
      }
         areax.push((arrowd == String.fromCharCode(79,0) ? areax.length : arrowd.length));
      while (4 < (arrowd.length + areax.length) && 1 < (4 + arrowd.length)) {
         areax.push(1 ^ arrowd.length);
         break;
      }
         arrowd += `${((modalc ? 3 : 3) % 3)}`;
          let rootG = String.fromCharCode(98,117,109,112,95,121,95,49,48,0);
          let greenp: Map<any, any> = new Map([[String.fromCharCode(115,121,110,99,115,97,102,101,95,50,95,50,49,0),true ], [String.fromCharCode(116,95,49,54,95,116,114,117,101,109,111,116,105,111,110,0),true ], [String.fromCharCode(99,104,111,112,95,118,95,55,53,0),true ]]);
         arrowd += `${greenp.size}`;
         rootG = `${(String.fromCharCode(110,0) == rootG ? rootG.length : rootG.length)}`;
         greenp.set(rootG, rootG.length);
          let gmailV = 4.0;
          let roomF = String.fromCharCode(119,95,49,55,95,115,99,114,101,101,110,112,114,101,115,115,111,0);
         areax = [(String.fromCharCode(74,0) == arrowd ? arrowd.length : roomF.length)];
         gmailV *= parseInt(`${gmailV}`);
         roomF += `${parseInt(`${gmailV}`)}`;
         arrowd = `${areax.length & 3}`;
      for (let g = 0; g < 2; g++) {
         arrowd = `${areax.length}`;
      }
      for (let f = 0; f < 2; f++) {
         modalc = arrowd.length <= 7;
      }
      splashv = new Map([[`${kickq.length}`, kickq.length << (Math.min(annerD.length, 4))]]);
   for (let f = 0; f < 2; f++) {
      splashv.set(submit5, (submit5 == String.fromCharCode(77,0) ? submit5.length : (neonD ? 2 : 3)));
   }
      kickq.push((actionv == String.fromCharCode(75,0) ? actionv.length : kickq.length));
   while (actionv != submit5) {
      submit5 += `${(2 >> (Math.min(1, Math.abs((neonD ? 1 : 1)))))}`;
      break;
   }
   for (let o = 0; o < 3; o++) {
      submit5 += `${((neonD ? 5 : 1) | libmapbufferjniq.length)}`;
   }
       let templateprocessor8: Array<any> = [119, 56, 176];
       let penaltyG = String.fromCharCode(116,117,114,98,111,106,112,101,103,95,102,95,50,53,0);
       let unimplementedviewj: Array<any> = [578, 512];
         unimplementedviewj = [penaltyG.length * 2];
          let lightU = 1;
         penaltyG += `${unimplementedviewj.length % 3}`;
         lightU %= Math.max(5, lightU - lightU);
       let shareW = String.fromCharCode(112,95,50,54,95,105,97,116,0);
      while ((4 * templateprocessor8.length) < 5) {
         penaltyG += `${(penaltyG == String.fromCharCode(74,0) ? penaltyG.length : templateprocessor8.length)}`;
         break;
      }
      if ((shareW.length ^ unimplementedviewj.length) == 2) {
         shareW = `${penaltyG.length}`;
      }
      for (let s = 0; s < 2; s++) {
         penaltyG = `${(String.fromCharCode(83,0) == penaltyG ? templateprocessor8.length : penaltyG.length)}`;
      }
      while (shareW.startsWith(`${unimplementedviewj.length}`)) {
         shareW += `${(shareW == String.fromCharCode(79,0) ? unimplementedviewj.length : shareW.length)}`;
         break;
      }
         penaltyG = `${templateprocessor8.length ^ shareW.length}`;
      let bufferV = templateprocessor8.length >= 8539289;
      do {
         templateprocessor8.push(shareW.length / (Math.max(6, penaltyG.length)));
         if (bufferV) {
            break;
         }
      } while ((shareW.length >= 4) && bufferV);
      annerD += "2";
      rncoreLN *= parseFloat(`${libmapbufferjniq.length >> (Math.min(5, actionv.length))}`);
   if (1.97 >= (rncoreLN - upgradeE)) {
       let nalyticsP = String.fromCharCode(118,95,51,56,95,99,111,108,111,114,113,117,97,110,116,0);
      let gradle0 = 5802459 >= nalyticsP.length;
      do {
         nalyticsP += `${2 & nalyticsP.length}`;
         if (gradle0) {
            break;
         }
      } while (gradle0 && (nalyticsP != String.fromCharCode(48,0)));
      while (5 > nalyticsP.length) {
         nalyticsP = `${nalyticsP.length}`;
         break;
      }
      while (nalyticsP.endsWith(`${nalyticsP.length}`)) {
         nalyticsP += `${nalyticsP.length & nalyticsP.length}`;
         break;
      }
      rncoreLN += (parseFloat(`${String.fromCharCode(90,0) == scorec ? nalyticsP.length : scorec.length}`));
   }
    setRefreshing(false);
  };

  const refreshUserState = async () => {
       let becomeS: Map<any, any> = new Map([[String.fromCharCode(101,95,52,56,95,100,101,108,105,109,105,116,101,100,0),String.fromCharCode(122,95,53,53,95,109,111,110,111,119,104,105,116,101,0)], [String.fromCharCode(108,95,52,54,95,111,102,102,0),String.fromCharCode(112,101,114,115,112,95,110,95,53,54,0)]]);
    let dpluso = 1.0;
    let cancelW: Map<any, any> = new Map([[String.fromCharCode(103,111,98,98,108,101,95,107,95,49,53,0),true ], [String.fromCharCode(105,115,97,108,110,117,109,95,117,95,54,54,0),false ]]);
    let main_jB: Array<any> = [207, 712];
    let renderD: Map<any, any> = new Map([[String.fromCharCode(104,113,97,100,115,112,95,122,95,55,48,0),634], [String.fromCharCode(102,95,53,57,95,98,117,108,107,0),28], [String.fromCharCode(116,109,109,98,114,95,120,95,50,48,0),853]]);
    let androidM = String.fromCharCode(113,116,97,98,108,101,95,100,95,53,54,0);
    let mailp = String.fromCharCode(105,100,101,110,116,105,102,105,97,98,108,101,95,103,95,54,51,0);
    let yingN = String.fromCharCode(114,101,99,118,118,95,56,95,49,50,0);
    let m_lockF = 0.0;
    let robotob = String.fromCharCode(105,95,50,55,95,98,111,117,110,99,105,110,103,0);
    let condensedY = String.fromCharCode(101,95,50,57,95,115,117,98,110,111,100,101,0);
    let areaT = String.fromCharCode(119,95,51,51,95,100,101,115,116,105,110,97,116,105,111,110,0);
       let lefty = 4.0;
       let watch6 = String.fromCharCode(102,101,101,95,99,95,55,0);
         lefty -= watch6.length % (Math.max(10, parseInt(`${lefty}`)));
      if (3 == (parseInt(`${lefty}`) / 2) || (lefty / (Math.max(8, watch6.length))) == 3.27) {
          let default_p3: Map<any, any> = new Map([[String.fromCharCode(97,95,53,56,95,97,99,116,105,118,101,109,97,112,0),655], [String.fromCharCode(107,95,53,53,95,102,100,99,116,120,0),808], [String.fromCharCode(107,95,50,52,95,116,111,117,99,104,101,100,0),243]]);
          let detailsJ = 3;
          let userr = String.fromCharCode(109,111,110,111,95,119,95,50,57,0);
         lefty /= Math.max(parseInt(`${lefty}`) ^ watch6.length, 4);
         default_p3 = new Map([[`${default_p3.size}`, 1]]);
         detailsJ -= detailsJ / 2;
         userr += "3";
      }
      for (let a = 0; a < 2; a++) {
         lefty += (String.fromCharCode(53,0) == watch6 ? watch6.length : parseInt(`${lefty}`));
      }
       let predictionU = false;
      for (let w = 0; w < 3; w++) {
          let valuesJ = String.fromCharCode(112,95,52,49,95,115,101,116,116,97,98,108,101,0);
          let screenr = 4;
          let with_r_Y = String.fromCharCode(100,105,115,99,111,114,100,95,117,95,52,55,0);
          let turnI = true;
          let plusP: Map<any, any> = new Map([[String.fromCharCode(119,101,108,115,100,101,99,100,101,109,111,95,97,95,54,50,0),964], [String.fromCharCode(114,101,115,105,100,117,97,108,115,95,111,95,53,50,0),634], [String.fromCharCode(114,95,52,57,95,97,117,116,104,111,114,105,122,101,0),600]]);
         predictionU = 7 >= with_r_Y.length;
         valuesJ += `${plusP.size}`;
         screenr *= 2;
         with_r_Y = `${(String.fromCharCode(100,0) == valuesJ ? valuesJ.length : plusP.size)}`;
         turnI = (93 >= ((turnI ? plusP.size : 9) >> (Math.min(Math.abs(plusP.size), 2))));
      }
      if (!predictionU) {
          let layout6: Map<any, any> = new Map([[String.fromCharCode(106,95,54,56,95,104,117,100,0),true ], [String.fromCharCode(111,95,57,52,95,116,120,105,100,0),false ]]);
          let final_kE = String.fromCharCode(108,111,99,97,108,108,121,95,56,95,51,0);
          let cleard = true;
          let pageG = false;
          let blackp: Map<any, any> = new Map([[String.fromCharCode(105,110,102,111,114,109,97,116,105,111,110,95,115,95,54,53,0),398], [String.fromCharCode(115,116,114,114,101,115,101,116,95,53,95,53,50,0),33], [String.fromCharCode(114,109,115,105,112,114,95,115,95,51,48,0),218]]);
         lefty /= Math.max(3, 1);
         layout6.set(`${pageG}`, final_kE.length);
         final_kE += `${((pageG ? 4 : 4) % 3)}`;
         cleard = blackp.size == 46 || !pageG;
         blackp.set(`${blackp.size}`, blackp.size);
      }
      androidM += `${androidM.length}`;
      dpluso += parseFloat(`${renderD.size}`);
       let transfer8 = String.fromCharCode(115,116,115,115,95,53,95,55,50,0);
      while (!transfer8.endsWith(`${transfer8.length}`)) {
         transfer8 += `${transfer8.length}`;
         break;
      }
      let libreact4 = transfer8 == String.fromCharCode(108,57,54,51,121,0);
      do {
         transfer8 = `${(transfer8 == String.fromCharCode(114,0) ? transfer8.length : transfer8.length)}`;
         if (libreact4) {
            break;
         }
      } while (libreact4 && (transfer8.length < 4));
      if (!transfer8.endsWith(`${transfer8.length}`)) {
         transfer8 += `${transfer8.length}`;
      }
      renderD = new Map([[`${dpluso}`, 3]]);
       let k_titleR = String.fromCharCode(114,95,49,55,95,105,100,102,118,0);
       let hearte = String.fromCharCode(119,95,53,52,0);
       let stationsP = 2.0;
         hearte += "1";
      if (4 <= hearte.length) {
          let libswscaleD = 5.0;
         hearte += `${parseInt(`${libswscaleD}`) ^ k_titleR.length}`;
      }
      if (k_titleR != String.fromCharCode(77,0) && hearte != String.fromCharCode(85,0)) {
         hearte = `${hearte.length}`;
      }
         hearte = `${k_titleR.length}`;
         k_titleR += `${2 | hearte.length}`;
         k_titleR += `${hearte.length >> (Math.min(Math.abs(1), 4))}`;
      if ((stationsP / (Math.max(5.68, 8))) == 1.55) {
         k_titleR = `${(String.fromCharCode(110,0) == k_titleR ? hearte.length : k_titleR.length)}`;
      }
         k_titleR = `${3 % (Math.max(1, hearte.length))}`;
         stationsP /= Math.max(1, parseFloat(`${k_titleR.length & 2}`));
      dpluso -= parseFloat(`${3 * mailp.length}`);
   if ((renderD.size % 1) > 2 && (renderD.size % (Math.max(yingN.length, 9))) > 1) {
       let chinasameo: Map<any, any> = new Map([[String.fromCharCode(111,95,49,54,95,102,97,118,101,0),String.fromCharCode(121,95,57,50,95,117,110,109,105,120,0)], [String.fromCharCode(100,95,53,54,95,121,118,116,111,121,117,121,0),String.fromCharCode(112,97,99,107,95,102,95,55,50,0)], [String.fromCharCode(98,95,55,95,105,102,111,114,119,97,114,100,0),String.fromCharCode(105,110,115,116,114,117,99,116,105,111,110,95,121,95,49,56,0)]]);
       let binddatas4 = String.fromCharCode(115,95,55,95,99,111,110,99,97,116,0);
       let libjsinspectorQ = 2.0;
      let temp_ = 5952249 <= binddatas4.length;
      do {
         binddatas4 = `${binddatas4.length}`;
         if (temp_) {
            break;
         }
      } while ((binddatas4.includes(`${chinasameo.size}`)) && temp_);
       let catagoryU: Map<any, any> = new Map([[String.fromCharCode(105,110,115,116,97,110,99,101,115,95,107,95,50,52,0),192], [String.fromCharCode(113,95,55,51,95,106,115,105,109,100,0),991]]);
       let nativemoduleg: Map<any, any> = new Map([[String.fromCharCode(105,95,50,48,95,117,110,115,101,101,110,0),false ], [String.fromCharCode(109,121,114,110,100,95,109,95,50,53,0),false ]]);
      let product_ = 8659422 >= catagoryU.size;
      do {
         catagoryU.set(binddatas4, 1);
         if (product_) {
            break;
         }
      } while (product_ && (4 >= (catagoryU.size | nativemoduleg.size)));
      for (let x = 0; x < 2; x++) {
         catagoryU = new Map([[`${nativemoduleg.size}`, 1 | nativemoduleg.size]]);
      }
          let actionsx = false;
          let eact1 = 0.0;
         nativemoduleg.set(`${libjsinspectorQ}`, nativemoduleg.size);
         actionsx = 6.29 < eact1;
         eact1 -= (parseFloat(`${(actionsx ? 1 : 4) ^ parseInt(`${eact1}`)}`));
         libjsinspectorQ *= binddatas4.length >> (Math.min(4, Math.abs(parseInt(`${libjsinspectorQ}`))));
      while (5 < (3 & binddatas4.length)) {
         binddatas4 += "3";
         break;
      }
          let reducera = true;
          let values4 = String.fromCharCode(104,101,120,99,104,97,114,105,110,116,95,105,95,56,53,0);
          let moviesh = 3;
         chinasameo.set(`${reducera}`, ((reducera ? 1 : 1) & 3));
         values4 += `${values4.length}`;
         moviesh &= 3 - values4.length;
          let bridge0 = String.fromCharCode(100,95,50,51,0);
         binddatas4 = `${chinasameo.size << (Math.min(bridge0.length, 2))}`;
      renderD = new Map([[binddatas4, 2]]);
   }

    const result = await yys_GesturesConst.getUserDetails();

      androidM = `${(androidM == String.fromCharCode(122,0) ? androidM.length : becomeS.size)}`;
       let questA: Array<any> = [792, 152];
       let foregrounda = 1;
       let storeK = 1;
         foregrounda >>= Math.min(4, Math.abs(1 & foregrounda));
      for (let r = 0; r < 1; r++) {
          let utilsQ = true;
         questA = [((utilsQ ? 1 : 3) / (Math.max(storeK, 9)))];
      }
      if (1 > (5 ^ questA.length) || (5 ^ questA.length) > 5) {
         foregrounda %= Math.max(3 / (Math.max(8, foregrounda)), 1);
      }
      while ((questA.length >> (Math.min(3, Math.abs(foregrounda)))) > 3 || (questA.length >> (Math.min(Math.abs(3), 4))) > 2) {
         foregrounda /= Math.max(1, questA.length % 3);
         break;
      }
          let sinaF = String.fromCharCode(118,99,97,114,100,95,97,95,57,51,0);
          let crossQ = 3;
         storeK ^= 3;
         sinaF += `${crossQ}`;
         crossQ *= crossQ * 1;
       let downloadY = true;
         foregrounda += storeK;
      for (let n = 0; n < 3; n++) {
         storeK >>= Math.min(4, Math.abs(1));
      }
         downloadY = storeK == 23;
      androidM += `${3 - mailp.length}`;
   let libavfilter7 = 7251078 >= yingN.length;
   do {
       let sigmobE: Map<any, any> = new Map([[String.fromCharCode(117,95,52,48,95,108,111,99,0),true ], [String.fromCharCode(101,95,57,54,95,110,101,116,101,114,114,110,111,0),true ], [String.fromCharCode(98,101,116,104,115,111,102,116,118,105,100,101,111,95,119,95,57,51,0),true ]]);
       let hongkong9 = String.fromCharCode(102,119,97,108,115,104,95,99,95,50,49,0);
       let stationL = 3.0;
       let eighteenI = String.fromCharCode(102,105,114,101,95,122,95,53,49,0);
         eighteenI = `${1 | parseInt(`${stationL}`)}`;
         eighteenI += `${sigmobE.size}`;
         hongkong9 += `${hongkong9.length / (Math.max(2, 3))}`;
          let layoutH = 5.0;
          let reducerX = 5.0;
          let downP = 5.0;
         eighteenI = `${hongkong9.length * 2}`;
         layoutH *= 2 | parseInt(`${reducerX}`);
         reducerX -= parseFloat(`${parseInt(`${downP}`)}`);
      for (let b = 0; b < 2; b++) {
         eighteenI += "3";
      }
      if (5 < (eighteenI.length + 5) || 2 < (parseInt(`${stationL}`) / 5)) {
         stationL += parseFloat(`${2}`);
      }
          let playB: Map<any, any> = new Map([[String.fromCharCode(121,95,52,54,95,115,104,111,114,116,116,101,114,109,0),432], [String.fromCharCode(106,95,53,53,95,112,114,101,99,101,110,99,101,0),789]]);
          let networkt = false;
         sigmobE.set(hongkong9, hongkong9.length);
         playB.set(`${networkt}`, playB.size);
          let mbjscommonh = true;
          let classesN: Array<any> = [String.fromCharCode(115,116,97,107,105,110,103,95,105,95,51,56,0), String.fromCharCode(119,95,55,50,95,98,117,116,116,111,110,0), String.fromCharCode(116,95,49,57,95,112,101,114,115,105,115,116,97,110,116,0)];
          let libturbomodulejsijniz = 2.0;
         stationL *= parseFloat(`${parseInt(`${libturbomodulejsijniz}`)}`);
         mbjscommonh = (((mbjscommonh ? classesN.length : 23) + classesN.length) < 23);
         libturbomodulejsijniz /= Math.max(2, parseFloat(`${classesN.length}`));
      for (let w = 0; w < 3; w++) {
          let m_imagey = 5;
          let smallX = String.fromCharCode(103,101,116,115,95,50,95,56,52,0);
          let e_managerI = 1;
          let pinit_5f = String.fromCharCode(109,95,50,52,95,106,109,111,114,101,99,102,103,0);
         stationL /= Math.max(4, parseFloat(`${2 >> (Math.min(5, Math.abs(m_imagey)))}`));
         m_imagey -= 1 - pinit_5f.length;
         smallX = `${1 * e_managerI}`;
         e_managerI %= Math.max(smallX.length, 5);
         pinit_5f += `${pinit_5f.length}`;
      }
          let memberu = String.fromCharCode(99,111,110,102,111,114,109,115,95,112,95,56,49,0);
          let show2 = String.fromCharCode(110,116,101,114,102,97,99,101,95,97,95,53,57,0);
          let small0 = 2.0;
         hongkong9 += `${show2.length}`;
         memberu = "3";
         show2 = `${memberu.length}`;
         small0 /= Math.max(3 ^ parseInt(`${small0}`), 2);
          let bdxadsdkQ: Array<any> = [385, 841, 203];
          let minivoda: Map<any, any> = new Map([[String.fromCharCode(116,95,51,51,95,115,116,114,105,100,0),705], [String.fromCharCode(99,111,108,108,101,116,105,111,110,95,110,95,53,56,0),370], [String.fromCharCode(119,97,110,116,95,57,95,57,52,0),499]]);
          let mini6 = String.fromCharCode(110,95,57,95,114,101,102,108,101,99,116,0);
         hongkong9 += `${parseInt(`${stationL}`) >> (Math.min(Math.abs(sigmobE.size), 3))}`;
         bdxadsdkQ.push(bdxadsdkQ.length | 2);
         minivoda = new Map([[`${bdxadsdkQ.length}`, 2 >> (Math.min(5, mini6.length))]]);
         mini6 = "1";
         hongkong9 = `${parseInt(`${stationL}`) - eighteenI.length}`;
      yingN = "2";
      if (libavfilter7) {
         break;
      }
   } while (libavfilter7 && ((2 * yingN.length) >= 5));
      renderD.set(`${m_lockF}`, becomeS.size);
   while (renderD.size >= main_jB.length) {
       let trashC = true;
       let gmailM = String.fromCharCode(103,95,55,56,95,105,112,108,105,109,97,103,101,0);
       let langr = String.fromCharCode(103,95,52,57,95,101,120,116,109,97,112,0);
       let serviceK = String.fromCharCode(122,95,53,50,95,100,105,114,101,99,116,111,114,121,0);
      let bootsplashW = serviceK.length <= 6428212;
      do {
          let countdownd: Map<any, any> = new Map([[String.fromCharCode(118,95,51,55,95,115,97,109,112,108,101,114,97,116,101,0),true ], [String.fromCharCode(119,95,55,52,95,115,116,97,110,100,97,108,111,110,101,0),true ]]);
         serviceK = `${1 & gmailM.length}`;
         countdownd = new Map([[`${countdownd.size}`, countdownd.size]]);
         if (bootsplashW) {
            break;
         }
      } while ((gmailM.length >= serviceK.length) && bootsplashW);
          let modeQ: Map<any, any> = new Map([[String.fromCharCode(100,97,116,97,114,95,116,95,54,53,0),true ], [String.fromCharCode(100,95,52,57,95,112,105,120,100,101,115,99,0),true ]]);
          let libjsiN = String.fromCharCode(97,110,116,105,97,108,105,97,115,105,110,103,95,114,95,49,57,0);
          let catalogY = String.fromCharCode(110,95,54,50,95,115,97,116,100,120,0);
         gmailM += `${modeQ.size}`;
         modeQ = new Map([[catalogY, catalogY.length]]);
         libjsiN = `${libjsiN.length}`;
       let typingK = 5.0;
       let agreementz = 4.0;
      let sinaD = 5569305 >= gmailM.length;
      do {
         gmailM += `${(String.fromCharCode(113,0) == serviceK ? langr.length : serviceK.length)}`;
         if (sinaD) {
            break;
         }
      } while ((!gmailM.includes(`${trashC}`)) && sinaD);
      if (serviceK.endsWith(`${trashC}`)) {
         trashC = 72.86 >= agreementz;
      }
      while ((typingK / (Math.max(3.31, 2))) >= 2.91) {
         typingK /= Math.max(parseFloat(`${serviceK.length & 2}`), 5);
         break;
      }
         agreementz -= parseFloat(`${1}`);
      let dicep = serviceK == String.fromCharCode(101,56,98,108,101,116,0);
      do {
          let livek = String.fromCharCode(104,97,110,100,108,101,115,95,101,95,49,48,48,0);
         serviceK += `${parseInt(`${agreementz}`)}`;
         livek = `${livek.length}`;
         if (dicep) {
            break;
         }
      } while ((serviceK.length <= 1) && dicep);
      let episodeV = String.fromCharCode(120,107,56,112,118,111,103,98,0) == serviceK;
      do {
          let reactnativejsq = String.fromCharCode(98,95,51,48,95,99,102,101,110,99,115,116,114,0);
          let darkV = String.fromCharCode(119,95,49,57,95,101,110,100,101,100,0);
          let downloadingL = 3;
          let filtert = 3.0;
         serviceK = "2";
         reactnativejsq += `${reactnativejsq.length ^ darkV.length}`;
         darkV += `${parseInt(`${filtert}`)}`;
         downloadingL %= Math.max(5, parseInt(`${filtert}`));
         if (episodeV) {
            break;
         }
      } while (episodeV && ((typingK - 1.82) >= 3.94 && (1.82 - typingK) >= 5.16));
      if ((langr.length - parseInt(`${typingK}`)) <= 3) {
         langr += `${gmailM.length}`;
      }
      let neonU = String.fromCharCode(120,99,55,51,117,112,55,101,0) == langr;
      do {
          let enewinterstitialJ: Array<any> = [String.fromCharCode(110,101,119,101,115,116,95,122,95,51,53,0), String.fromCharCode(104,114,115,115,95,103,95,53,51,0), String.fromCharCode(100,115,112,117,116,105,108,95,50,95,57,55,0)];
          let username0 = false;
          let libjsijniprofilerb = 2;
         langr += `${enewinterstitialJ.length + serviceK.length}`;
         enewinterstitialJ.push((libjsijniprofilerb << (Math.min(3, Math.abs((username0 ? 5 : 1))))));
         username0 = 13 > libjsijniprofilerb;
         if (neonU) {
            break;
         }
      } while (neonU && (langr.startsWith(`${gmailM.length}`)));
          let textinputW: Array<any> = [String.fromCharCode(106,95,52,57,95,105,103,110,111,114,101,115,0), String.fromCharCode(101,120,116,114,97,99,116,95,97,95,55,55,0), String.fromCharCode(112,117,98,108,105,115,104,101,100,95,111,95,50,49,0)];
          let window_39 = 2.0;
          let chinasameL = false;
         trashC = !chinasameL;
         textinputW.push(parseInt(`${window_39}`));
         window_39 /= Math.max(4, parseFloat(`${2 & textinputW.length}`));
      main_jB = [2];
      break;
   }
    if (result == null) {

       let renewV = String.fromCharCode(100,95,54,56,95,115,119,114,101,115,97,109,112,108,101,0);
      if (renewV.endsWith(renewV)) {
         renewV = `${renewV.length % (Math.max(5, renewV.length))}`;
      }
      if (renewV.length == renewV.length) {
          let blackC = String.fromCharCode(99,117,101,115,95,54,95,54,57,0);
          let animationsT = true;
          let statistics2 = 2.0;
         renewV = `${renewV.length}`;
         blackC += `${3 | blackC.length}`;
         animationsT = blackC.length >= parseInt(`${statistics2}`);
         statistics2 *= parseFloat(`${blackC.length ^ 3}`);
      }
      for (let c = 0; c < 1; c++) {
          let libjsinspectorK: Array<any> = [686, 395];
          let privacys = 1;
          let scrollviewE = 2;
          let switch_deI: Map<any, any> = new Map([[String.fromCharCode(105,119,97,108,115,104,95,104,95,57,0),String.fromCharCode(102,112,115,95,115,95,53,53,0)], [String.fromCharCode(101,95,57,51,95,110,111,105,115,101,0),String.fromCharCode(120,118,105,100,105,100,99,116,95,103,95,54,57,0)], [String.fromCharCode(97,116,116,114,97,99,116,111,114,95,104,95,52,55,0),String.fromCharCode(110,95,54,48,95,115,117,98,116,97,115,107,0)]]);
          let constantsV = false;
         renewV += `${((constantsV ? 4 : 1))}`;
         libjsinspectorK = [scrollviewE];
         privacys >>= Math.min(Math.abs(3), 4);
         scrollviewE |= 3 - libjsinspectorK.length;
         switch_deI.set(`${privacys}`, privacys);
         constantsV = scrollviewE < privacys;
      }
      mailp = `${(renewV == String.fromCharCode(84,0) ? mailp.length : renewV.length)}`;
   if (Array.from(renderD.keys()).includes(`${becomeS.size}`)) {
      renderD = new Map([[`${m_lockF}`, parseInt(`${m_lockF}`) & androidM.length]]);
   }
   for (let x = 0; x < 3; x++) {
       let mathC = String.fromCharCode(111,108,108,101,99,116,105,111,110,115,95,117,95,53,52,0);
         mathC = `${2 >> (Math.min(1, mathC.length))}`;
      let typing4 = mathC == String.fromCharCode(52,56,108,116,107,0);
      do {
          let spinnerN = 1;
          let expiredP: Array<any> = [510, 661, 168];
          let countryD = 3.0;
          let bufferK = String.fromCharCode(104,101,99,111,95,55,95,54,0);
         mathC += "1";
         spinnerN %= Math.max(3, bufferK.length);
         expiredP.push(expiredP.length);
         countryD += parseFloat(`${spinnerN}`);
         bufferK += `${parseInt(`${countryD}`)}`;
         if (typing4) {
            break;
         }
      } while (typing4 && (mathC.startsWith(mathC)));
       let backgroundE = 3.0;
      becomeS.set(mailp, mailp.length * 1);
   }
      cancelW = new Map([[`${renderD.size}`, renderD.size]]);
      main_jB = [1 >> (Math.min(4, main_jB.length))];
      return;
    }

    await dispatch(updateUserAuth(result));

       let heji6: Array<any> = [121, 337, 312];
       let referrerN: Map<any, any> = new Map([[String.fromCharCode(105,100,101,110,116,105,116,121,95,51,95,51,53,0),String.fromCharCode(99,95,57,54,95,97,110,99,104,111,114,115,0)], [String.fromCharCode(99,109,100,117,116,105,108,115,95,121,95,55,51,0),String.fromCharCode(100,105,115,109,105,115,115,101,115,95,111,95,56,57,0)], [String.fromCharCode(103,108,111,98,97,108,116,101,109,95,101,95,52,57,0),String.fromCharCode(111,99,97,108,95,122,95,49,51,0)]]);
      if (4 > referrerN.size) {
         heji6.push(2);
      }
         heji6 = [heji6.length << (Math.min(Math.abs(2), 5))];
          let tramini7 = true;
         heji6.push(heji6.length << (Math.min(Math.abs(3), 1)));
      while (!Array.from(referrerN.values()).includes(heji6.length)) {
          let customV = String.fromCharCode(114,105,102,102,95,117,95,54,0);
          let eact1I = String.fromCharCode(100,95,52,57,95,115,112,101,101,100,111,109,101,116,101,114,0);
          let styleP = false;
          let c_countu = 5.0;
          let telemetryx = 5.0;
         heji6 = [heji6.length % (Math.max(9, referrerN.size))];
         customV += `${2 << (Math.min(5, Math.abs(parseInt(`${c_countu}`))))}`;
         eact1I += `${parseInt(`${telemetryx}`)}`;
         styleP = !eact1I.endsWith(`${telemetryx}`);
         c_countu -= 1;
         break;
      }
       let librrcX = 4.0;
      for (let l = 0; l < 2; l++) {
          let typingr = 4;
          let final_0g = 4.0;
          let minit = 4;
          let umengM: Map<any, any> = new Map([[String.fromCharCode(101,95,54,57,95,101,120,116,101,110,116,0),374], [String.fromCharCode(118,95,49,55,95,99,108,97,115,115,101,115,0),548]]);
         referrerN = new Map([[`${referrerN.size}`, referrerN.size]]);
         typingr &= 3 * minit;
         final_0g /= Math.max(parseInt(`${final_0g}`) / 2, 3);
         minit += 1 / (Math.max(3, parseInt(`${final_0g}`)));
         umengM = new Map([[`${umengM.size}`, umengM.size]]);
      }
      yingN = `${robotob.length}`;
   while (dpluso >= 1.76) {
       let libsentryK = String.fromCharCode(118,95,54,53,95,105,109,103,117,116,105,108,115,0);
       let downloaded8: Map<any, any> = new Map([[String.fromCharCode(102,111,114,103,101,116,95,110,95,56,50,0),String.fromCharCode(116,95,57,95,112,117,98,108,105,99,107,101,121,115,0)], [String.fromCharCode(109,101,109,109,103,114,95,121,95,53,52,0),String.fromCharCode(114,105,110,103,95,111,95,52,52,0)], [String.fromCharCode(109,101,109,111,106,105,95,49,95,50,55,0),String.fromCharCode(109,95,50,53,95,117,110,102,108,97,116,116,101,110,101,100,0)]]);
       let delegate_5zb = String.fromCharCode(100,105,115,112,97,116,99,104,105,110,103,95,104,95,56,50,0);
       let arrow9 = String.fromCharCode(116,95,55,54,95,97,101,97,100,0);
       let librrcN = 3;
         libsentryK += `${librrcN / (Math.max(2, arrow9.length))}`;
      for (let c = 0; c < 2; c++) {
         downloaded8.set(`${librrcN}`, librrcN);
      }
         librrcN -= downloaded8.size;
         arrow9 = `${librrcN >> (Math.min(Math.abs(1), 2))}`;
      let notificationt = delegate_5zb.length >= 9347667;
      do {
          let shrinkp: Map<any, any> = new Map([[String.fromCharCode(105,110,104,105,98,105,116,115,95,56,95,54,49,0),793], [String.fromCharCode(112,95,55,57,95,111,112,101,114,97,116,111,114,115,0),857], [String.fromCharCode(116,114,97,110,115,105,116,105,111,110,105,110,103,95,103,95,56,52,0),38]]);
          let tickedB: Array<any> = [827, 607];
          let assist7 = 3;
          let volume9 = 4;
         delegate_5zb = `${assist7}`;
         shrinkp = new Map([[`${shrinkp.size}`, 1 / (Math.max(6, shrinkp.size))]]);
         tickedB = [volume9];
         assist7 <<= Math.min(1, Math.abs(2));
         volume9 -= 2 << (Math.min(1, Math.abs(shrinkp.size)));
         if (notificationt) {
            break;
         }
      } while ((1 == (1 - delegate_5zb.length)) && notificationt);
          let abidetectO = String.fromCharCode(100,115,116,115,117,98,95,56,95,50,56,0);
          let onewsP = String.fromCharCode(106,95,57,48,95,116,102,100,116,0);
         arrow9 = `${3 - onewsP.length}`;
         abidetectO = `${abidetectO.length - 1}`;
         onewsP += `${abidetectO.length + abidetectO.length}`;
       let libreactnativejniF = 5.0;
       let result_ = 4.0;
         libsentryK += `${delegate_5zb.length - 3}`;
      if ((libreactnativejniF + librrcN) < 2.5) {
         librrcN |= 1 | delegate_5zb.length;
      }
      let relatedS = delegate_5zb.length <= 6602255;
      do {
         delegate_5zb += `${2 - libsentryK.length}`;
         if (relatedS) {
            break;
         }
      } while (relatedS && (4 < (2 ^ librrcN) || 4 < (2 ^ librrcN)));
       let libloggerY = String.fromCharCode(97,95,52,50,95,114,111,111,109,0);
       let toponS = String.fromCharCode(111,109,112,108,105,99,97,116,105,111,110,95,108,95,51,50,0);
          let huawei5 = 4;
          let disconnectedq = String.fromCharCode(115,101,108,101,99,116,101,100,95,57,95,49,48,48,0);
          let becomeG = String.fromCharCode(105,100,99,116,120,95,51,95,57,52,0);
         arrow9 += `${toponS.length}`;
         huawei5 %= Math.max(4, disconnectedq.length - huawei5);
         disconnectedq += `${(disconnectedq == String.fromCharCode(83,0) ? disconnectedq.length : becomeG.length)}`;
         becomeG += `${(disconnectedq == String.fromCharCode(90,0) ? disconnectedq.length : huawei5)}`;
         toponS = `${delegate_5zb.length}`;
       let kcopy_wg: Map<any, any> = new Map([[String.fromCharCode(99,104,97,110,103,101,100,104,95,100,95,52,57,0),65], [String.fromCharCode(111,114,105,95,121,95,55,49,0),792], [String.fromCharCode(112,114,111,99,95,52,95,54,51,0),84]]);
       let mimo0: Map<any, any> = new Map([[String.fromCharCode(115,116,111,114,109,98,105,114,100,95,104,95,55,48,0),315], [String.fromCharCode(120,95,50,55,95,97,112,109,116,101,115,116,0),313]]);
       let nalyticsJ: Map<any, any> = new Map([[String.fromCharCode(110,95,57,55,95,116,111,114,103,98,0),90], [String.fromCharCode(107,95,49,48,48,95,118,112,105,116,120,102,109,0),434]]);
      dpluso /= Math.max((parseFloat(`${String.fromCharCode(48,0) == androidM ? androidM.length : arrow9.length}`)), 1);
      break;
   }
      yingN = `${2 ^ condensedY.length}`;
   while (4.37 < m_lockF) {
       let singles: Array<any> = [778, 561, 215];
       let leakcheckerJ = String.fromCharCode(122,95,51,51,95,119,109,97,100,97,116,97,0);
       let phone8 = String.fromCharCode(100,95,53,49,95,102,97,105,108,97,98,108,101,0);
       let helperF = String.fromCharCode(120,95,54,52,95,104,105,103,104,108,105,103,116,101,100,0);
      if (phone8.includes(`${singles.length}`)) {
         singles.push(helperF.length * 2);
      }
          let androidh = 0.0;
         phone8 = `${phone8.length % 1}`;
         androidh *= parseInt(`${androidh}`) - parseInt(`${androidh}`);
         singles = [leakcheckerJ.length];
      let package_8aP = singles.length <= 6319006;
      do {
         singles = [1];
         if (package_8aP) {
            break;
         }
      } while ((!leakcheckerJ.includes(`${singles.length}`)) && package_8aP);
      if ((singles.length / (Math.max(leakcheckerJ.length, 8))) > 2 && 2 > (leakcheckerJ.length / (Math.max(2, 1)))) {
         leakcheckerJ = `${helperF.length | 2}`;
      }
      for (let x = 0; x < 2; x++) {
         phone8 = `${helperF.length & leakcheckerJ.length}`;
      }
      if (singles.length >= helperF.length) {
         singles = [leakcheckerJ.length];
      }
       let friendsL = String.fromCharCode(122,95,50,56,0);
          let selectionb = String.fromCharCode(111,95,54,52,95,114,101,112,101,97,116,101,100,108,121,0);
          let gradlew6 = 0;
         singles.push(1 >> (Math.min(5, helperF.length)));
         selectionb += `${gradlew6}`;
         gradlew6 *= gradlew6;
       let libreactnativejnil = 3;
          let orange7: Map<any, any> = new Map([[String.fromCharCode(118,101,99,116,111,114,95,49,95,57,57,0),538], [String.fromCharCode(117,95,49,49,0),633]]);
         leakcheckerJ = `${(phone8 == String.fromCharCode(53,0) ? helperF.length : phone8.length)}`;
         orange7 = new Map([[`${orange7.size}`, orange7.size - 2]]);
         phone8 += `${singles.length}`;
      m_lockF *= 3 - main_jB.length;
      break;
   }
      androidM += "1";
    return;
  };

  return (
    <ScreenContainer>
      <TitleWithBackButtonHeader title="邀请好友奖励" />
      <ScrollView
        refreshControl={
          <RefreshControl refreshing={refreshing} onRefresh={handleRefresh} tintColor="#FAC33D" />
        }
        showsVerticalScrollIndicator={false}
      >
        {/* <TitleWithBackButtonHeader title="邀请好友奖励" /> */}
        <View style={{ paddingHorizontal: 0 }}>
          { }
          <InviteHeader />
          { }
          <InviteCard userState={userState.user!} />
          { }

          <TouchableOpacity
            onPress={() => {
              navigation.navigate("活动规则");
            }}
          >
            <View
              style={{
                flex: 1,
                justifyContent: "center",
                alignItems: "center",
                paddingVertical: 10,
              }}
            >
              <Text
                style={{
                  fontSize: 12,
                  textDecorationLine: "underline",
                  color: "#ffffff",
                }}
              >
                活动规则
              </Text>
            </View>
          </TouchableOpacity>
          { }
        </View>
        <InviteStep />
      </ScrollView>
    </ScreenContainer>
  );
};

const styles = StyleSheet.create({});
