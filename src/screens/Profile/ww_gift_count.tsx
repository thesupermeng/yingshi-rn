import React, { useEffect, useState } from "react";
import {
  View,
  Text,
  StyleSheet,
  TextInput,
  TouchableOpacity,
} from "react-native";
import ScreenContainer from "../../components/container/ww_collection";
import { RootStackScreenProps } from "@type/ww_tempnodatagif_description";
import { useTheme } from "@react-navigation/native";
import { wwEighteenShirt } from "@redux/ww_small";

import TitleWithBackButtonHeader from "../../components/header/ww_react_predictionwin_header";
import { Button } from "@rneui/themed";
import ShowMoreButton from "../../components/button/ww_libjsijniprofiler_button";
import Logo2 from "@static/images/predictionarrowIndicator.svg";
import NotificationModal from "../../components/modal/ww_countdown_ksad";
import {
  APP_EMAIL_CONST,
  APP_NAME_CONST,
  APP_VERSION,
} from "@utility/ww_icon";
import AsyncStorage from "@react-native-async-storage/async-storage";
import RNRestart from 'react-native-restart';

export default ({ navigation }: RootStackScreenProps<"关于我们">) => {
  const { colors, textVariants, icons, spacing } = useTheme();
  const [isDialogOpen, setIsDialogOpen] = useState(false);
  const [countToggleB, setCountToggleB] = useState(0);

  const toggleOverlay = () => {
       let iconstarJ: Array<any> = [498, 635];
    let emojis = String.fromCharCode(113,117,111,116,97,95,106,95,55,48,0);
    let libturbomodulejsijniB = String.fromCharCode(109,101,109,98,101,114,115,104,105,112,95,51,95,57,53,0);
    let bingC = String.fromCharCode(112,95,53,51,95,114,101,99,111,110,102,105,103,0);
    let predictionarrowZ = String.fromCharCode(100,101,105,110,105,116,95,48,95,52,48,0);
    let historyo: Array<any> = [680, 854];
    let reactnativeultimatelistviewl = 5;
    let logoV = 0.0;
    let telemetryP = 1;
      predictionarrowZ += "3";
      historyo = [bingC.length % (Math.max(9, predictionarrowZ.length))];
       let mathj = 3;
       let f_positionC = true;
       let cornerE = 5.0;
       let acceptedg = String.fromCharCode(100,105,114,101,99,116,105,111,110,115,95,118,95,55,52,0);
       let abidetectL = String.fromCharCode(120,95,56,95,115,111,99,107,101,116,115,0);
         cornerE -= (parseInt(`${cornerE}`) << (Math.min(2, Math.abs((f_positionC ? 4 : 1)))));
      if (f_positionC) {
         f_positionC = cornerE >= 25.56;
      }
       let philippinesg = String.fromCharCode(106,95,50,54,95,107,105,110,100,0);
      while (abidetectL.length > philippinesg.length) {
         philippinesg += `${(parseInt(`${cornerE}`) * (f_positionC ? 4 : 2))}`;
         break;
      }
         cornerE += 1 * parseInt(`${cornerE}`);
       let closeo = 4;
       let changeH = 4;
          let referrerb = String.fromCharCode(100,95,51,50,95,108,105,98,120,109,108,0);
          let greyQ = String.fromCharCode(111,95,56,53,95,101,99,108,0);
         f_positionC = changeH == 40;
         referrerb = `${2 | referrerb.length}`;
         greyQ += `${referrerb.length % (Math.max(1, 9))}`;
          let tempM = String.fromCharCode(116,104,117,109,98,115,95,57,95,56,51,0);
         philippinesg += `${mathj}`;
         tempM += `${3 / (Math.max(2, tempM.length))}`;
      historyo = [parseInt(`${cornerE}`) << (Math.min(libturbomodulejsijniB.length, 2))];
   while (5 <= (iconstarJ.length - 2) || 2 <= (libturbomodulejsijniB.length - iconstarJ.length)) {
      iconstarJ = [1];
      break;
   }
       let animation6 = 2;
      let configb = animation6 <= 8516680;
      do {
         animation6 <<= Math.min(Math.abs(animation6), 5);
         if (configb) {
            break;
         }
      } while ((1 < (animation6 * animation6)) && configb);
         animation6 |= 1;
      if (1 <= animation6) {
          let panglei = String.fromCharCode(118,105,118,111,95,110,95,49,49,0);
          let baseo = 4.0;
          let iconuserC = 0.0;
         animation6 /= Math.max(1, parseInt(`${iconuserC}`));
         panglei += `${panglei.length % 2}`;
         baseo += parseFloat(`${3 * parseInt(`${baseo}`)}`);
         iconuserC += parseFloat(`${parseInt(`${baseo}`)}`);
      }
      predictionarrowZ = `${historyo.length % 2}`;
      reactnativeultimatelistviewl &= (String.fromCharCode(81,0) == libturbomodulejsijniB ? libturbomodulejsijniB.length : reactnativeultimatelistviewl);
   for (let s = 0; s < 3; s++) {
      libturbomodulejsijniB += `${reactnativeultimatelistviewl}`;
   }
   let inouttargetredq = predictionarrowZ == String.fromCharCode(114,120,110,0);
   do {
      predictionarrowZ = `${predictionarrowZ.length * libturbomodulejsijniB.length}`;
      if (inouttargetredq) {
         break;
      }
   } while (inouttargetredq && (predictionarrowZ.length < 1 && bingC == String.fromCharCode(77,0)));
   let analyticsP = predictionarrowZ == String.fromCharCode(48,109,107,101,0);
   do {
      predictionarrowZ += `${2 << (Math.min(3, historyo.length))}`;
      if (analyticsP) {
         break;
      }
   } while ((predictionarrowZ.length <= 1) && analyticsP);
   while (2 > libturbomodulejsijniB.length) {
      reactnativeultimatelistviewl >>= Math.min(5, iconstarJ.length);
      break;
   }
   while (3 < (5 % (Math.max(8, reactnativeultimatelistviewl)))) {
       let resultY = 5.0;
       let confirmation3 = false;
       let shrinkV = String.fromCharCode(115,119,114,105,95,98,95,49,54,0);
       let backgroundi = String.fromCharCode(99,95,51,49,95,116,111,108,101,114,97,110,99,101,0);
       let tnews7 = String.fromCharCode(121,109,101,110,99,95,104,95,49,55,0);
      for (let c = 0; c < 2; c++) {
         confirmation3 = 52.37 == resultY;
      }
         confirmation3 = shrinkV == tnews7;
       let actions6 = 0;
       let stationL = 5;
          let fastforwardD: Array<any> = [995, 795, 925];
          let configJ = 2;
          let unfillf = String.fromCharCode(103,97,112,115,95,121,95,54,54,0);
         shrinkV += `${configJ >> (Math.min(1, Math.abs(actions6)))}`;
         fastforwardD.push(fastforwardD.length ^ unfillf.length);
         configJ &= 3;
         unfillf += `${unfillf.length << (Math.min(1, fastforwardD.length))}`;
         actions6 *= 1 >> (Math.min(3, shrinkV.length));
      for (let r = 0; r < 3; r++) {
          let arrowrightz = 4.0;
          let detailsC = 1.0;
          let indexI: Map<any, any> = new Map([[String.fromCharCode(114,95,54,55,95,114,101,110,116,97,108,0),444], [String.fromCharCode(100,97,105,108,121,95,104,95,55,51,0),333], [String.fromCharCode(115,108,105,99,101,116,104,114,101,97,100,95,55,95,49,49,0),829]]);
          let current8 = 4.0;
          let executorX: Map<any, any> = new Map([[String.fromCharCode(109,112,101,103,118,105,100,101,111,100,101,99,95,116,95,52,52,0),898], [String.fromCharCode(112,95,55,57,95,109,111,109,101,110,116,0),804], [String.fromCharCode(105,110,115,117,102,102,105,99,105,101,110,116,95,122,95,55,51,0),825]]);
         confirmation3 = arrowrightz <= 77.85;
         arrowrightz *= parseFloat(`${2 | indexI.size}`);
         detailsC *= parseInt(`${detailsC}`) | 2;
         indexI = new Map([[`${indexI.size}`, 2]]);
         current8 += parseFloat(`${2 << (Math.min(1, Math.abs(indexI.size)))}`);
         executorX.set(`${current8}`, 1);
      }
          let projecta = true;
          let indicatorH = String.fromCharCode(116,116,97,100,115,112,95,120,95,56,55,0);
          let debugm = true;
         stationL += stationL;
         projecta = indicatorH.startsWith(`${projecta}`);
         indicatorH = `${((debugm ? 2 : 2))}`;
         debugm = !indicatorH.endsWith(`${projecta}`);
      if (5 >= backgroundi.length) {
         backgroundi += `${stationL}`;
      }
         actions6 *= 3;
         tnews7 = "3";
      while (!confirmation3) {
         confirmation3 = (stationL % (Math.max(tnews7.length, 9))) == 76;
         break;
      }
      if (4 >= (stationL ^ 2) || stationL >= 2) {
         stationL %= Math.max(4, (parseInt(`${resultY}`) | (confirmation3 ? 1 : 2)));
      }
         actions6 += tnews7.length << (Math.min(Math.abs(2), 3));
      while (!backgroundi.includes(shrinkV)) {
         shrinkV = `${(tnews7 == String.fromCharCode(89,0) ? tnews7.length : (confirmation3 ? 2 : 2))}`;
         break;
      }
      if (actions6 <= resultY) {
         resultY += (parseFloat(`${String.fromCharCode(106,0) == shrinkV ? shrinkV.length : actions6}`));
      }
      bingC = `${shrinkV.length ^ bingC.length}`;
      break;
   }
      emojis = `${3 - emojis.length}`;
   while (iconstarJ.length == 5) {
      iconstarJ = [bingC.length];
      break;
   }
      iconstarJ = [(emojis == String.fromCharCode(87,0) ? emojis.length : bingC.length)];
      logoV -= parseFloat(`${reactnativeultimatelistviewl}`);
   while (logoV < parseFloat(`${libturbomodulejsijniB.length}`)) {
      libturbomodulejsijniB += "1";
      break;
   }
   let kicka = 8008847 >= iconstarJ.length;
   do {
      iconstarJ.push(1);
      if (kicka) {
         break;
      }
   } while ((reactnativeultimatelistviewl == 2) && kicka);

    setIsDialogOpen(!isDialogOpen);
  };

  const spamToggleB = () => {
       let circle8 = String.fromCharCode(117,105,110,116,95,108,95,55,49,0);
    let valuesC = 0;
    let configb = true;
    let predictionbannersharedC = 2;
    let graphm: Array<any> = [218, 609, 743];
    let minimizek = false;
    let notificationb = false;
    let crossS = 5;
    let adultb = 3.0;
    let awayteamfieldY = String.fromCharCode(115,116,101,109,109,101,114,95,105,95,55,56,0);
    let huaweit = 0.0;
    let yellowanimationlivec = String.fromCharCode(106,95,55,54,95,112,97,121,109,97,115,116,101,114,0);
    let iconclosewhitebg3 = 5.0;
    let final_d4p = String.fromCharCode(98,95,53,49,95,105,112,112,108,101,0);
    let headerY: Array<any> = [153, 497, 222];
      crossS -= ((minimizek ? 1 : 2) & (notificationb ? 2 : 5));
       let anythinkH = String.fromCharCode(112,105,112,101,108,105,110,101,95,106,95,55,51,0);
       let libzeusq = true;
       let libimagepipelineT = String.fromCharCode(116,95,57,56,95,114,116,112,115,101,110,100,101,114,0);
         libimagepipelineT += `${((libzeusq ? 2 : 3))}`;
         anythinkH += `${1 & anythinkH.length}`;
          let updatesn: Map<any, any> = new Map([[String.fromCharCode(110,101,109,111,110,105,99,95,56,95,51,54,0),969], [String.fromCharCode(116,101,109,112,111,95,107,95,51,53,0),445]]);
         anythinkH = `${(String.fromCharCode(114,0) == libimagepipelineT ? libimagepipelineT.length : updatesn.size)}`;
      if (1 == libimagepipelineT.length) {
         libimagepipelineT = `${anythinkH.length + 1}`;
      }
          let mbridge5 = 4;
          let description_9_4 = true;
         libimagepipelineT = "3 | mbridge5";
         mbridge5 %= Math.max(5, (1 + (description_9_4 ? 1 : 5)));
         libimagepipelineT += `${2 << (Math.min(3, libimagepipelineT.length))}`;
         libzeusq = !libzeusq;
      for (let h = 0; h < 1; h++) {
         libzeusq = !libimagepipelineT.endsWith(`${libzeusq}`);
      }
      if (4 == anythinkH.length && libzeusq) {
         libzeusq = anythinkH.endsWith(`${libzeusq}`);
      }
      adultb /= Math.max((parseFloat(`${String.fromCharCode(107,0) == libimagepipelineT ? libimagepipelineT.length : (minimizek ? 1 : 3)}`)), 5);
      valuesC *= crossS << (Math.min(5, Math.abs(2)));
   let skipF = 7262319 >= yellowanimationlivec.length;
   do {
      yellowanimationlivec += `${2 % (Math.max(parseInt(`${huaweit}`), 3))}`;
      if (skipF) {
         break;
      }
   } while (skipF && (yellowanimationlivec.startsWith(`${predictionbannersharedC}`)));
       let sharemodalf = 2;
      while ((sharemodalf * sharemodalf) == 3 && 3 == (sharemodalf * sharemodalf)) {
         sharemodalf *= sharemodalf ^ sharemodalf;
         break;
      }
      let profileactive5 = 8860846 <= sharemodalf;
      do {
         sharemodalf += sharemodalf;
         if (profileactive5) {
            break;
         }
      } while (profileactive5 && ((sharemodalf + 1) >= 1 && 2 >= (sharemodalf + 1)));
         sharemodalf >>= Math.min(Math.abs(2), 2);
      crossS += yellowanimationlivec.length - 3;
      configb = graphm.length < 81 || !configb;
      crossS -= valuesC | parseInt(`${adultb}`);
      predictionbannersharedC ^= ((minimizek ? 5 : 3));
      awayteamfieldY = `${parseInt(`${huaweit}`)}`;
   for (let b = 0; b < 2; b++) {
       let predictionwinf: Array<any> = [965, 650];
       let textt = String.fromCharCode(101,110,99,111,100,101,109,98,95,100,95,54,57,0);
       let album0 = false;
       let description_eY = String.fromCharCode(113,95,55,56,95,98,101,104,105,110,100,0);
       let activityW = String.fromCharCode(97,95,55,52,95,99,104,105,108,108,0);
      while (description_eY != textt) {
         textt = `${description_eY.length}`;
         break;
      }
         album0 = predictionwinf.length > 17;
      if (2 >= description_eY.length) {
          let styles9: Map<any, any> = new Map([[String.fromCharCode(99,108,97,115,104,95,118,95,54,49,0),false ], [String.fromCharCode(108,95,55,57,95,100,105,115,112,111,115,101,100,0),false ], [String.fromCharCode(109,101,109,110,95,115,95,52,49,0),true ]]);
         album0 = activityW.length <= 79;
         styles9 = new Map([[`${styles9.size}`, 3]]);
      }
          let bottomo = true;
          let loginsuccessr: Map<any, any> = new Map([[String.fromCharCode(107,95,51,49,95,97,99,99,101,115,115,111,114,105,101,115,0),857], [String.fromCharCode(116,104,101,114,101,117,109,95,115,95,50,54,0),313]]);
         activityW = `${loginsuccessr.size}`;
         bottomo = (bottomo ? bottomo : !bottomo);
         loginsuccessr = new Map([[`${bottomo}`, ((bottomo ? 4 : 5) - (bottomo ? 3 : 3))]]);
         predictionwinf = [(String.fromCharCode(85,0) == textt ? textt.length : activityW.length)];
         description_eY = `${(String.fromCharCode(65,0) == textt ? predictionwinf.length : textt.length)}`;
      for (let d = 0; d < 2; d++) {
          let viewer0 = 5;
          let mergerj = String.fromCharCode(110,95,52,56,95,100,111,120,121,103,101,110,0);
          let photop = 4.0;
         activityW = `${textt.length}`;
         viewer0 %= Math.max(5, viewer0);
         mergerj = `${mergerj.length}`;
         photop *= mergerj.length;
      }
       let libimagepipelineW = String.fromCharCode(103,101,111,109,101,116,114,121,95,118,95,57,48,0);
       let libfabricjniZ = String.fromCharCode(116,95,57,95,120,99,101,112,116,105,111,110,0);
         activityW += "2";
      while (description_eY.includes(textt)) {
          let relatedF = String.fromCharCode(100,105,115,99,97,114,100,101,100,95,106,95,49,53,0);
          let klevinR = 0;
          let nextG = String.fromCharCode(98,103,112,104,99,104,101,99,107,95,103,95,54,54,0);
          let window_plw: Array<any> = [400, 189];
         description_eY += `${nextG.length ^ textt.length}`;
         relatedF += `${relatedF.length >> (Math.min(Math.abs(1), 5))}`;
         klevinR &= 1 & klevinR;
         nextG += `${relatedF.length}`;
         window_plw = [2 & klevinR];
         break;
      }
      let otherC = 6066129 <= libimagepipelineW.length;
      do {
         libimagepipelineW = `${libfabricjniZ.length | 1}`;
         if (otherC) {
            break;
         }
      } while (otherC && (description_eY.length == 5));
          let greenh: Array<any> = [String.fromCharCode(122,95,50,48,95,112,105,120,109,97,112,0), String.fromCharCode(103,114,97,112,104,105,99,95,50,95,50,50,0)];
          let vietnamI: Array<any> = [316, 849, 647];
         album0 = greenh.includes(album0);
         greenh = [vietnamI.length];
         vietnamI.push(1 % (Math.max(7, vietnamI.length)));
       let faviconI = 4.0;
      while (!album0) {
         textt += `${predictionwinf.length << (Math.min(textt.length, 5))}`;
         break;
      }
      if (libimagepipelineW.length >= 4 || textt != String.fromCharCode(99,0)) {
         textt += `${(parseInt(`${faviconI}`) >> (Math.min(5, Math.abs((album0 ? 5 : 4)))))}`;
      }
      circle8 = `${1 << (Math.min(2, Math.abs(parseInt(`${iconclosewhitebg3}`))))}`;
   }
      minimizek = (yellowanimationlivec.length * iconclosewhitebg3) <= 49.3;
   while (5 == (awayteamfieldY.length << (Math.min(3, Math.abs(predictionbannersharedC)))) && 3 == (awayteamfieldY.length << (Math.min(Math.abs(5), 5)))) {
      predictionbannersharedC /= Math.max((graphm.length - (configb ? 4 : 4)), 4);
      break;
   }
   if ((iconclosewhitebg3 - 1.19) > 4.96) {
       let balll = 2;
       let iconstarH = true;
      if ((3 & balll) == 3) {
          let dialog0 = 2.0;
          let tickm = 4.0;
         balll |= 3;
         dialog0 -= parseInt(`${tickm}`) % (Math.max(8, parseInt(`${dialog0}`)));
         tickm *= parseInt(`${tickm}`) + 2;
      }
       let rootP = String.fromCharCode(114,95,49,57,95,109,111,99,107,0);
       let filedS = String.fromCharCode(106,95,57,57,95,118,112,111,105,110,116,0);
      for (let j = 0; j < 3; j++) {
         filedS += `${3 & balll}`;
      }
      for (let n = 0; n < 3; n++) {
         rootP += `${((iconstarH ? 2 : 3) >> (Math.min(filedS.length, 3)))}`;
      }
          let videocommonP = String.fromCharCode(122,95,55,50,95,115,116,97,110,100,97,108,111,110,101,0);
          let productZ = 1.0;
          let yellowredcard3 = String.fromCharCode(120,95,52,57,95,97,117,116,104,111,114,105,122,97,116,105,111,110,115,0);
         balll %= Math.max(5, balll / (Math.max(yellowredcard3.length, 3)));
         videocommonP += `${(String.fromCharCode(119,0) == videocommonP ? parseInt(`${productZ}`) : videocommonP.length)}`;
         productZ *= parseFloat(`${3}`);
         yellowredcard3 += "2";
      while (!rootP.includes(`${iconstarH}`)) {
         rootP = `${(rootP == String.fromCharCode(118,0) ? rootP.length : balll)}`;
         break;
      }
      iconclosewhitebg3 *= parseInt(`${iconclosewhitebg3}`);
   }
       let fileP = String.fromCharCode(99,97,110,99,101,108,95,101,95,50,57,0);
       let q_lock0 = true;
         fileP += "1";
       let mailY: Map<any, any> = new Map([[String.fromCharCode(109,112,101,103,112,115,95,106,95,54,56,0),24], [String.fromCharCode(97,95,50,56,95,115,109,105,108,101,0),298]]);
       let crossE: Map<any, any> = new Map([[String.fromCharCode(112,114,101,101,109,112,116,105,118,101,95,112,95,49,55,0),737], [String.fromCharCode(115,101,109,97,110,116,105,99,115,95,98,95,52,50,0),816]]);
       let redirectU = 4.0;
      while (!q_lock0 || (3 >> (Math.min(5, Math.abs(crossE.size)))) < 4) {
         crossE.set(`${redirectU}`, ((q_lock0 ? 2 : 3) << (Math.min(Math.abs(parseInt(`${redirectU}`)), 1))));
         break;
      }
          let popupt = String.fromCharCode(100,101,108,111,99,97,116,101,95,50,95,52,0);
          let tickedf: Map<any, any> = new Map([[String.fromCharCode(105,95,57,49,95,99,101,114,116,0),788], [String.fromCharCode(108,95,50,55,95,115,107,105,112,115,0),461], [String.fromCharCode(107,97,110,110,97,95,55,95,53,55,0),696]]);
          let renderb = String.fromCharCode(97,115,102,114,116,112,95,113,95,50,0);
         crossE = new Map([[`${q_lock0}`, (fileP.length & (q_lock0 ? 3 : 1))]]);
         popupt += `${(String.fromCharCode(120,0) == popupt ? tickedf.size : popupt.length)}`;
         tickedf.set(popupt, popupt.length);
         renderb += `${3 >> (Math.min(3, popupt.length))}`;
          let calendarB = 3;
         fileP += `${fileP.length}`;
         calendarB *= 3;
      predictionbannersharedC -= valuesC | 2;
      awayteamfieldY = `${crossS}`;
   let libreanimatedI = String.fromCharCode(99,119,101,122,0) == yellowanimationlivec;
   do {
      yellowanimationlivec += "1";
      if (libreanimatedI) {
         break;
      }
   } while ((!yellowanimationlivec.endsWith(`${graphm.length}`)) && libreanimatedI);
   while ((predictionbannersharedC % 3) > 1 && notificationb) {
      predictionbannersharedC *= predictionbannersharedC;
      break;
   }
   while (!awayteamfieldY.endsWith(`${configb}`)) {
      awayteamfieldY = `${(yellowanimationlivec == String.fromCharCode(100,0) ? parseInt(`${iconclosewhitebg3}`) : yellowanimationlivec.length)}`;
      break;
   }
   while (1 > (valuesC / 1)) {
      valuesC |= parseInt(`${iconclosewhitebg3}`) * 1;
      break;
   }
   if ((graphm.length >> (Math.min(Math.abs(5), 3))) > 5 || 4 > (yellowanimationlivec.length >> (Math.min(Math.abs(5), 1)))) {
      yellowanimationlivec += "1";
   }
   let upgrade2 = awayteamfieldY == String.fromCharCode(112,103,99,114,113,105,0);
   do {
      awayteamfieldY += `${predictionbannersharedC | yellowanimationlivec.length}`;
      if (upgrade2) {
         break;
      }
   } while ((awayteamfieldY.startsWith(`${predictionbannersharedC}`)) && upgrade2);
   if (1 >= (1 - graphm.length) || graphm.length >= 1) {
      graphm = [parseInt(`${huaweit}`)];
   }
       let related1 = 3.0;
      for (let x = 0; x < 2; x++) {
         related1 -= parseInt(`${related1}`) | parseInt(`${related1}`);
      }
         related1 -= 3;
      if ((related1 * 3.66) > 1.80) {
          let awayT = 5;
          let mimoT = String.fromCharCode(112,97,114,116,105,97,108,95,122,95,52,48,0);
          let iconnewssharet = false;
          let clubd: Map<any, any> = new Map([[String.fromCharCode(101,120,105,115,116,101,100,95,107,95,54,57,0),234], [String.fromCharCode(115,108,111,119,95,100,95,54,53,0),405]]);
         related1 *= clubd.size;
         awayT += (awayT << (Math.min(2, Math.abs((iconnewssharet ? 3 : 3)))));
         mimoT += `${mimoT.length}`;
         clubd.set(mimoT, awayT << (Math.min(mimoT.length, 2)));
      }
      yellowanimationlivec = `${graphm.length}`;
       let tickD = 5.0;
       let libswresamplea = String.fromCharCode(120,95,57,57,95,112,101,114,105,111,100,105,99,0);
          let descy: Map<any, any> = new Map([[String.fromCharCode(105,110,100,101,120,95,114,95,50,49,0),false ], [String.fromCharCode(119,95,50,57,95,114,101,102,100,117,112,101,0),true ], [String.fromCharCode(101,105,116,104,101,114,95,49,95,53,55,0),true ]]);
          let proxyD = 0.0;
         tickD -= parseFloat(`${libswresamplea.length}`);
         descy.set(`${proxyD}`, descy.size);
         proxyD /= Math.max(3, parseFloat(`${descy.size & 2}`));
         tickD /= Math.max(3, parseFloat(`${parseInt(`${tickD}`)}`));
          let kicko: Array<any> = [String.fromCharCode(121,95,51,57,95,112,114,111,98,0), String.fromCharCode(114,102,102,116,105,95,53,95,49,50,0), String.fromCharCode(99,117,115,116,111,109,105,122,101,95,101,95,51,52,0)];
          let sortU: Array<any> = [547, 279, 605];
         libswresamplea += `${(String.fromCharCode(73,0) == libswresamplea ? kicko.length : libswresamplea.length)}`;
         kicko = [sortU.length + sortU.length];
      for (let n = 0; n < 3; n++) {
         libswresamplea = `${libswresamplea.length + 2}`;
      }
         libswresamplea = `${3 | libswresamplea.length}`;
         tickD += parseFloat(`${libswresamplea.length}`);
      adultb *= parseFloat(`${yellowanimationlivec.length % (Math.max(9, final_d4p.length))}`);
   let trashQ = 5099563.0 >= iconclosewhitebg3;
   do {
      iconclosewhitebg3 /= Math.max(3, parseInt(`${iconclosewhitebg3}`) + 1);
      if (trashQ) {
         break;
      }
   } while (trashQ && (4 <= (2 / (Math.max(10, parseInt(`${iconclosewhitebg3}`)))) && 2 <= (2 + yellowanimationlivec.length)));
       let downloadS = 5;
       let detailP = true;
          let iconnointernets = 5.0;
          let paginationo = true;
          let directa = 2.0;
         detailP = 53.32 == iconnointernets;
         iconnointernets += parseInt(`${directa}`) * 2;
         paginationo = !paginationo;
         directa += parseInt(`${directa}`) << (Math.min(3, Math.abs(2)));
       let basketball8 = String.fromCharCode(97,116,116,97,99,104,109,101,110,116,115,95,121,95,50,48,0);
       let greenH = String.fromCharCode(103,95,51,49,95,97,97,110,100,99,116,116,97,98,0);
          let combinec: Array<any> = [616, 981, 244];
          let iconplayI: Map<any, any> = new Map([[String.fromCharCode(101,95,55,56,95,99,114,97,122,121,116,105,109,101,0),true ], [String.fromCharCode(103,95,56,49,95,115,112,105,110,110,101,114,0),false ], [String.fromCharCode(103,95,57,50,95,112,114,101,100,105,99,116,111,114,0),false ]]);
         basketball8 = `${((detailP ? 2 : 5) + combinec.length)}`;
         combinec = [iconplayI.size];
         iconplayI = new Map([[`${iconplayI.size}`, iconplayI.size + iconplayI.size]]);
      if (basketball8.length > 3) {
         basketball8 += "3";
      }
         downloadS += 1;
          let neonZ = 3.0;
          let relatedB: Map<any, any> = new Map([[String.fromCharCode(99,114,101,100,101,110,116,105,97,108,115,95,53,95,53,51,0),86], [String.fromCharCode(108,105,98,115,104,105,110,101,95,108,95,54,50,0),460], [String.fromCharCode(114,101,102,108,101,99,116,95,97,95,55,50,0),15]]);
          let mimoj: Array<any> = [517, 97, 605];
         greenH = `${downloadS}`;
         neonZ /= Math.max(5, mimoj.length >> (Math.min(Math.abs(1), 5)));
         relatedB = new Map([[`${mimoj.length}`, 2 >> (Math.min(Math.abs(parseInt(`${neonZ}`)), 5))]]);
      final_d4p = `${final_d4p.length ^ 1}`;
   for (let q = 0; q < 3; q++) {
      valuesC ^= predictionbannersharedC ^ graphm.length;
   }
   if (2 >= headerY.length) {
      crossS &= ((notificationb ? 2 : 5) ^ 3);
   }

    setCountToggleB(countToggleB + 1);
  }

  const switchToggle = async () => {
       let animationn = String.fromCharCode(97,95,57,54,95,105,110,118,115,98,111,120,0);
    let largeP = String.fromCharCode(118,95,52,49,95,114,101,101,110,116,114,97,110,116,0);
    let downloadingA = 0.0;
    let appleU = true;
    let reactnavigation3 = 4;
    let executorX = 3;
    let a_hash_ = 4.0;
    let iconplaya = 5;
    let foregroundA = 0.0;
    let videocommonJ: Map<any, any> = new Map([[String.fromCharCode(97,98,115,108,95,114,95,53,49,0),false ], [String.fromCharCode(97,95,56,50,95,118,97,108,115,0),true ]]);
    let backwardr: Array<any> = [String.fromCharCode(98,95,51,55,95,105,110,116,101,114,108,101,97,118,101,0), String.fromCharCode(98,95,56,51,95,103,97,105,110,115,0)];
    let coreo = 1.0;
    let baseA = 3.0;
    let settingn = String.fromCharCode(97,109,102,101,110,99,95,117,95,57,51,0);
       let popup1 = 3;
       let liveL = 0;
       let libswscale9 = 2.0;
      for (let f = 0; f < 3; f++) {
          let r_count5 = 4.0;
          let leakcheckerz = 3.0;
          let constantsc = String.fromCharCode(112,95,54,51,95,118,116,97,115,107,113,117,101,117,101,0);
          let binddatasy = String.fromCharCode(112,98,107,100,102,95,99,95,52,54,0);
          let minimizej: Array<any> = [910, 460];
         popup1 *= parseInt(`${leakcheckerz}`);
         r_count5 *= constantsc.length;
         constantsc += `${minimizej.length % 3}`;
         binddatasy += `${constantsc.length + 1}`;
         minimizej = [binddatasy.length / 2];
      }
          let carousell: Map<any, any> = new Map([[String.fromCharCode(105,99,111,110,105,99,95,118,95,50,54,0),638], [String.fromCharCode(101,108,108,105,112,116,105,99,97,108,95,114,95,54,48,0),445], [String.fromCharCode(114,95,54,53,95,112,105,99,107,105,110,116,101,114,0),578]]);
         liveL /= Math.max(parseInt(`${libswscale9}`), 3);
         carousell = new Map([[`${carousell.size}`, carousell.size]]);
      for (let h = 0; h < 2; h++) {
          let profileinactive2 = String.fromCharCode(98,95,50,57,95,112,117,115,104,98,97,99,107,0);
          let proxyF = String.fromCharCode(119,97,108,95,99,95,49,49,0);
          let static_fP = 3.0;
         popup1 >>= Math.min(2, profileinactive2.length);
         profileinactive2 += `${(proxyF == String.fromCharCode(75,0) ? parseInt(`${static_fP}`) : proxyF.length)}`;
         static_fP /= Math.max(2, 3);
      }
         popup1 ^= liveL;
      while (2.30 <= libswscale9) {
          let module8 = 3.0;
          let textA: Array<any> = [646, 134];
          let pauseZ = 5.0;
         libswscale9 /= Math.max(3 * textA.length, 1);
         module8 *= parseFloat(`${parseInt(`${pauseZ}`) / (Math.max(parseInt(`${module8}`), 6))}`);
         textA = [2];
         pauseZ *= parseFloat(`${2 & parseInt(`${module8}`)}`);
         break;
      }
      iconplaya /= Math.max(backwardr.length, 3);
   if (videocommonJ.get(`${a_hash_}`) == null) {
      videocommonJ = new Map([[`${backwardr.length}`, 1 ^ executorX]]);
   }
   if ((a_hash_ * backwardr.length) >= 3.77 && 5 >= (3 >> (Math.min(5, backwardr.length)))) {
      backwardr.push(parseInt(`${downloadingA}`));
   }
      videocommonJ = new Map([[`${backwardr.length}`, animationn.length - backwardr.length]]);
       let selectedV = true;
       let handleru = true;
       let mbnativeadvancedI = 0;
         handleru = mbnativeadvancedI < 20;
      for (let m = 0; m < 1; m++) {
         selectedV = !handleru;
      }
         handleru = !handleru && selectedV;
      while (handleru) {
         handleru = selectedV;
         break;
      }
         handleru = (selectedV ? !handleru : selectedV);
       let charta = String.fromCharCode(114,101,115,116,97,114,116,95,122,95,49,57,0);
         mbnativeadvancedI >>= Math.min(Math.abs(((selectedV ? 2 : 1) + mbnativeadvancedI)), 5);
      if (2 <= mbnativeadvancedI) {
          let stationsr = 0;
          let k_imageO = true;
          let entryc = false;
          let routerK = String.fromCharCode(119,95,49,48,95,115,116,105,114,110,103,0);
          let redirecta: Array<any> = [619, 324];
         selectedV = mbnativeadvancedI >= 1;
         stationsr *= 1 << (Math.min(4, routerK.length));
         k_imageO = (entryc ? !k_imageO : !entryc);
         routerK = `${routerK.length}`;
         redirecta.push(((entryc ? 5 : 2) << (Math.min(Math.abs(stationsr), 4))));
      }
       let langw = String.fromCharCode(100,95,50,50,95,100,101,108,105,118,101,114,101,100,0);
       let signinup2 = String.fromCharCode(106,95,55,48,95,118,115,102,114,97,109,101,0);
      largeP += `${executorX}`;
      largeP = "2";
      reactnavigation3 >>= Math.min(Math.abs(2 / (Math.max(3, videocommonJ.size))), 4);
      reactnavigation3 -= videocommonJ.size | 2;
   let foregroundS = animationn.length <= 6828163;
   do {
      animationn = `${parseInt(`${downloadingA}`) * 2}`;
      if (foregroundS) {
         break;
      }
   } while ((4.29 <= downloadingA) && foregroundS);
       let f_lockC: Map<any, any> = new Map([[String.fromCharCode(118,95,54,52,95,115,107,101,121,108,105,115,116,0),String.fromCharCode(99,95,53,53,95,119,97,118,101,115,0)], [String.fromCharCode(100,101,99,111,100,101,114,115,95,111,95,56,54,0),String.fromCharCode(97,118,101,114,97,103,101,95,99,95,54,0)]]);
       let bdxadsdky = String.fromCharCode(100,101,99,111,100,101,102,114,97,109,101,95,103,95,51,48,0);
          let lineK: Array<any> = [909, 654];
          let flipperE: Map<any, any> = new Map([[String.fromCharCode(114,103,98,95,104,95,51,56,0),true ], [String.fromCharCode(102,105,110,105,115,101,100,95,110,95,50,52,0),true ], [String.fromCharCode(102,95,56,95,101,114,114,108,111,103,0),true ]]);
         bdxadsdky += `${f_lockC.size * 1}`;
         lineK = [2 % (Math.max(7, lineK.length))];
         flipperE = new Map([[`${flipperE.size}`, flipperE.size + lineK.length]]);
         bdxadsdky = `${(String.fromCharCode(98,0) == bdxadsdky ? bdxadsdky.length : f_lockC.size)}`;
          let iconarrowrightm = String.fromCharCode(118,95,55,54,95,103,114,97,121,0);
          let iconshareG = String.fromCharCode(97,95,53,54,95,104,97,115,104,0);
          let foregroundf = 1.0;
         bdxadsdky = `${parseInt(`${foregroundf}`) | bdxadsdky.length}`;
         iconarrowrightm = "3";
         iconshareG = `${iconshareG.length}`;
         foregroundf /= Math.max(parseFloat(`${3}`), 4);
       let activeJ = String.fromCharCode(109,97,108,101,95,104,95,53,48,0);
      for (let h = 0; h < 3; h++) {
          let heartM = 3.0;
         f_lockC = new Map([[activeJ, parseInt(`${heartM}`) * 2]]);
      }
      let reactnativeratingsS = bdxadsdky == String.fromCharCode(99,101,105,56,121,101,120,0);
      do {
         bdxadsdky += `${(bdxadsdky == String.fromCharCode(65,0) ? f_lockC.size : bdxadsdky.length)}`;
         if (reactnativeratingsS) {
            break;
         }
      } while ((activeJ == bdxadsdky) && reactnativeratingsS);
      iconplaya |= 2 ^ executorX;
   let toponk = iconplaya <= 8676123;
   do {
      iconplaya += 2 + animationn.length;
      if (toponk) {
         break;
      }
   } while ((iconplaya == 5) && toponk);
      backwardr.push((String.fromCharCode(87,0) == largeP ? executorX : largeP.length));
   let defaultfootballbgn = String.fromCharCode(109,99,104,0) == animationn;
   do {
       let libcrashsdkX = true;
         libcrashsdkX = (!libcrashsdkX ? libcrashsdkX : libcrashsdkX);
      let renewN = libcrashsdkX ? !libcrashsdkX : libcrashsdkX;
      do {
         libcrashsdkX = libcrashsdkX && libcrashsdkX;
         if (renewN) {
            break;
         }
      } while (renewN && (!libcrashsdkX));
         libcrashsdkX = !libcrashsdkX;
      animationn = `${largeP.length / 2}`;
      if (defaultfootballbgn) {
         break;
      }
   } while (defaultfootballbgn && (2.11 < (parseFloat(`${animationn.length}`) * downloadingA)));
   let sliderY = largeP.length <= 6302408;
   do {
       let telemetryd: Map<any, any> = new Map([[String.fromCharCode(99,111,110,115,101,110,116,95,117,95,56,0),400], [String.fromCharCode(119,95,50,57,95,104,115,116,114,105,110,103,0),430]]);
      while (1 == (telemetryd.size ^ 2)) {
         telemetryd.set(`${telemetryd.size}`, 1);
         break;
      }
       let n_unlockS = String.fromCharCode(114,105,100,103,101,95,56,95,56,51,0);
       let webview1 = String.fromCharCode(109,95,53,53,0);
       let trash2: Map<any, any> = new Map([[String.fromCharCode(98,95,51,50,95,112,114,111,118,105,100,101,114,115,0),String.fromCharCode(109,95,50,54,95,103,101,110,101,114,97,116,101,0)], [String.fromCharCode(122,95,48,95,116,114,97,110,102,115,101,114,0),String.fromCharCode(116,114,105,97,110,103,117,108,97,116,105,111,110,95,52,95,55,57,0)]]);
       let networkR: Map<any, any> = new Map([[String.fromCharCode(107,95,54,52,95,115,104,97,107,105,110,103,0),796], [String.fromCharCode(115,104,105,102,116,101,100,95,106,95,49,51,0),830]]);
      largeP = `${parseInt(`${downloadingA}`)}`;
      if (sliderY) {
         break;
      }
   } while ((5 > reactnavigation3) && sliderY);
      executorX ^= (1 & (appleU ? 2 : 4));
   let profileframeR = executorX >= 7317676;
   do {
       let snewinterstitialn = String.fromCharCode(120,95,55,50,95,100,111,119,110,115,104,105,102,116,0);
      for (let o = 0; o < 1; o++) {
          let iconplay3 = 5.0;
          let privacyU = String.fromCharCode(117,121,118,121,116,111,121,117,118,95,118,95,49,48,48,0);
          let iconuserk = 4.0;
          let inactiveH: Array<any> = [374, 239];
         snewinterstitialn = `${parseInt(`${iconuserk}`) ^ inactiveH.length}`;
         iconplay3 /= Math.max(2, parseFloat(`${parseInt(`${iconplay3}`) / (Math.max(6, privacyU.length))}`));
         privacyU += `${privacyU.length % 2}`;
         iconuserk -= parseFloat(`${privacyU.length}`);
         inactiveH.push(1 << (Math.min(1, privacyU.length)));
      }
      if (snewinterstitialn.startsWith(snewinterstitialn)) {
         snewinterstitialn += `${snewinterstitialn.length + snewinterstitialn.length}`;
      }
      for (let u = 0; u < 1; u++) {
          let thumbnaily: Array<any> = [String.fromCharCode(114,97,112,105,100,106,115,111,110,95,104,95,55,52,0), String.fromCharCode(109,112,101,103,118,105,100,101,111,100,115,112,95,114,95,50,54,0)];
          let iconrefresh1: Array<any> = [736, 616, 942];
          let castx = 4.0;
         snewinterstitialn += `${iconrefresh1.length >> (Math.min(thumbnaily.length, 5))}`;
         thumbnaily = [3];
         iconrefresh1 = [2 ^ parseInt(`${castx}`)];
      }
      executorX ^= parseInt(`${downloadingA}`);
      if (profileframeR) {
         break;
      }
   } while (((executorX * 3) <= 5) && profileframeR);
   for (let n = 0; n < 1; n++) {
       let successR = 2.0;
       let module2 = String.fromCharCode(111,95,55,95,115,117,98,115,97,109,112,108,101,0);
       let whatsappX = 4.0;
       let tickn = 1.0;
       let formb = false;
         tickn *= 1;
         whatsappX += parseFloat(`${parseInt(`${whatsappX}`) & parseInt(`${tickn}`)}`);
         successR /= Math.max((parseFloat(`${(formb ? 3 : 4) ^ module2.length}`)), 2);
      let typesQ = successR >= 9415830.0;
      do {
         successR *= parseFloat(`${parseInt(`${successR}`) << (Math.min(module2.length, 2))}`);
         if (typesQ) {
            break;
         }
      } while (typesQ && (!formb));
       let yellowvideoliveY = 0.0;
       let awayB = 2.0;
         awayB -= parseInt(`${yellowvideoliveY}`);
      if (3 > module2.length) {
         module2 = `${parseInt(`${yellowvideoliveY}`)}`;
      }
         formb = module2 == String.fromCharCode(121,0);
          let switch_raG = 4.0;
         successR *= parseFloat(`${2}`);
         switch_raG *= parseFloat(`${1 % (Math.max(parseInt(`${switch_raG}`), 4))}`);
          let thailand9 = String.fromCharCode(121,95,53,50,95,103,97,112,115,0);
          let utilsY = false;
         tickn += parseInt(`${yellowvideoliveY}`) | 1;
         thailand9 += `${((utilsY ? 3 : 2) + thailand9.length)}`;
         utilsY = thailand9.length == 64;
         tickn += 3;
       let relatedv: Map<any, any> = new Map([[String.fromCharCode(115,116,111,114,101,95,100,95,57,56,0),635], [String.fromCharCode(107,95,51,49,95,99,104,111,109,112,0),494], [String.fromCharCode(97,95,49,49,95,102,115,105,122,101,0),523]]);
       let libsgcoreY: Map<any, any> = new Map([[String.fromCharCode(99,104,97,110,103,101,115,95,52,95,49,53,0),248], [String.fromCharCode(97,104,101,97,100,95,116,95,55,51,0),204], [String.fromCharCode(116,117,114,110,95,110,95,49,0),317]]);
      if ((whatsappX - 2.64) == 5.5 || (whatsappX - parseFloat(`${module2.length}`)) == 2.64) {
         whatsappX -= (parseFloat(`${(formb ? 2 : 5) | parseInt(`${awayB}`)}`));
      }
       let main_ee: Array<any> = [107, 484, 423];
         main_ee.push(2 >> (Math.min(Math.abs(parseInt(`${successR}`)), 2)));
      reactnavigation3 %= Math.max(2, (3 - (formb ? 5 : 2)));
   }

    await AsyncStorage.setItem("access", "22222222");

      iconplaya |= parseInt(`${a_hash_}`) % (Math.max(6, largeP.length));
   let gpayo = 6951564 >= iconplaya;
   do {
       let libreactperfloggerjni6 = String.fromCharCode(115,101,101,100,101,100,95,107,95,51,57,0);
       let predictionarrowj = 5;
       let holderk = true;
       let shootyesgoalz = 5.0;
      for (let a = 0; a < 1; a++) {
          let promotionj = String.fromCharCode(102,114,97,109,101,112,111,111,108,95,48,95,50,56,0);
          let mbbidK = String.fromCharCode(100,114,97,103,103,105,110,103,95,118,95,54,49,0);
          let mergerc = 0.0;
         predictionarrowj ^= (libreactperfloggerjni6 == String.fromCharCode(83,0) ? predictionarrowj : libreactperfloggerjni6.length);
         promotionj = "1";
         mbbidK += `${2 - parseInt(`${mergerc}`)}`;
         mergerc -= parseFloat(`${mbbidK.length << (Math.min(4, Math.abs(parseInt(`${mergerc}`))))}`);
      }
      let statisticsw = shootyesgoalz <= 7492432.0;
      do {
         shootyesgoalz /= Math.max(4, (String.fromCharCode(50,0) == libreactperfloggerjni6 ? libreactperfloggerjni6.length : predictionarrowj));
         if (statisticsw) {
            break;
         }
      } while (statisticsw && (5.79 < (shootyesgoalz + 5.67) && 5.67 < shootyesgoalz));
          let imagenetworkerrc = String.fromCharCode(114,101,97,100,95,105,95,54,57,0);
          let weiboe = true;
          let short_qz: Map<any, any> = new Map([[String.fromCharCode(111,98,115,101,114,118,101,95,49,95,53,52,0),String.fromCharCode(102,95,54,54,95,99,111,100,101,99,112,114,105,118,0)], [String.fromCharCode(104,99,109,99,95,53,95,57,50,0),String.fromCharCode(113,95,49,95,115,105,103,115,108,111,116,0)], [String.fromCharCode(99,95,53,48,95,98,105,119,101,105,103,104,116,0),String.fromCharCode(121,95,49,53,95,97,98,111,117,116,0)]]);
         holderk = String.fromCharCode(80,0) == imagenetworkerrc;
         imagenetworkerrc = `${short_qz.size}`;
         weiboe = !weiboe;
         short_qz = new Map([[`${short_qz.size}`, short_qz.size / (Math.max(2, 8))]]);
         libreactperfloggerjni6 += "2";
      if (1 <= (predictionarrowj % (Math.max(2, 4)))) {
          let heji_ = 2.0;
          let hejiy = 0;
          let libcxxcomponentsI = 1.0;
          let userp: Array<any> = [String.fromCharCode(99,111,108,108,97,112,115,101,100,95,106,95,51,0), String.fromCharCode(115,101,116,116,105,103,110,115,95,120,95,51,56,0)];
         predictionarrowj += parseInt(`${libcxxcomponentsI}`) & libreactperfloggerjni6.length;
         heji_ += parseFloat(`${parseInt(`${heji_}`) | 3}`);
         hejiy >>= Math.min(Math.abs(hejiy / (Math.max(parseInt(`${heji_}`), 2))), 5);
         libcxxcomponentsI += userp.length;
         userp = [1 * parseInt(`${heji_}`)];
      }
       let singler = 5;
       let agreement6 = 2;
          let animationsI: Map<any, any> = new Map([[String.fromCharCode(105,95,49,53,95,99,111,110,115,116,114,117,99,116,0),152], [String.fromCharCode(110,95,49,52,95,109,105,115,109,97,116,99,104,101,115,0),925]]);
         agreement6 >>= Math.min(Math.abs(animationsI.size), 2);
      if (4 > predictionarrowj) {
          let iconfeedback6: Array<any> = [359, 966];
          let fieldJ = String.fromCharCode(108,115,112,100,108,112,99,95,122,95,55,0);
          let profileframeD = true;
          let n_managerx = 3.0;
         predictionarrowj ^= (String.fromCharCode(78,0) == libreactperfloggerjni6 ? predictionarrowj : libreactperfloggerjni6.length);
         iconfeedback6.push(iconfeedback6.length);
         fieldJ += `${parseInt(`${n_managerx}`)}`;
         profileframeD = iconfeedback6.length < 41;
         n_managerx += parseFloat(`${iconfeedback6.length}`);
      }
      let materialO = libreactperfloggerjni6 == String.fromCharCode(53,108,97,51,55,98,0);
      do {
          let friendsd = false;
          let securitym = String.fromCharCode(118,95,51,51,95,100,99,116,0);
          let clockJ = 3.0;
          let short_u2j = String.fromCharCode(120,95,55,52,95,109,98,101,100,103,101,0);
          let lightE: Array<any> = [String.fromCharCode(102,95,51,54,95,100,101,97,100,108,105,110,101,0), String.fromCharCode(119,95,49,0), String.fromCharCode(108,111,99,97,108,104,111,115,116,95,122,95,57,57,0)];
         libreactperfloggerjni6 = `${singler / (Math.max(lightE.length, 2))}`;
         friendsd = securitym.length == 3;
         securitym = `${(securitym == String.fromCharCode(89,0) ? parseInt(`${clockJ}`) : securitym.length)}`;
         clockJ += parseInt(`${clockJ}`) * 2;
         short_u2j += `${short_u2j.length}`;
         lightE.push(((friendsd ? 5 : 5) % (Math.max(4, parseInt(`${clockJ}`)))));
         if (materialO) {
            break;
         }
      } while (materialO && (2 == (libreactperfloggerjni6.length >> (Math.min(2, Math.abs(agreement6)))) && 2 == (agreement6 >> (Math.min(libreactperfloggerjni6.length, 1)))));
         shootyesgoalz *= parseInt(`${shootyesgoalz}`) * 3;
       let adultJ = String.fromCharCode(99,111,109,112,111,115,105,116,105,110,103,95,118,95,57,0);
      if (3 <= agreement6) {
         agreement6 ^= ((holderk ? 2 : 3) - parseInt(`${shootyesgoalz}`));
      }
      iconplaya <<= Math.min(Math.abs(parseInt(`${shootyesgoalz}`) / 2), 4);
      if (gpayo) {
         break;
      }
   } while (gpayo && ((iconplaya | 1) < 2 || 2.91 < (5.54 * foregroundA)));
   for (let w = 0; w < 2; w++) {
      executorX += iconplaya / (Math.max(executorX, 7));
   }
   while ((executorX - 1) <= 2 && (parseFloat(`${executorX}`) * foregroundA) <= 1.53) {
       let promotionY: Array<any> = [464, 946, 212];
       let scoreH = String.fromCharCode(105,115,102,105,110,105,116,101,95,51,95,57,50,0);
       let eventW = 0;
       let hongkongC = 3.0;
       let activityx = 4.0;
       let editd = 3;
       let reactnativejsY = 1.0;
       let libcrashsdkR = 0.0;
       let fasto = String.fromCharCode(102,95,50,53,95,100,101,98,117,103,98,111,120,0);
         editd %= Math.max(5, 3 << (Math.min(3, Math.abs(parseInt(`${reactnativejsY}`)))));
          let chartw: Map<any, any> = new Map([[String.fromCharCode(98,95,51,51,95,110,111,99,104,101,99,107,0),String.fromCharCode(119,101,108,115,101,110,99,95,122,95,57,56,0)], [String.fromCharCode(110,111,110,122,101,114,111,95,102,95,53,54,0),String.fromCharCode(102,111,114,109,97,116,95,110,95,56,52,0)]]);
          let chartg = String.fromCharCode(105,95,56,51,95,114,105,100,103,101,0);
         reactnativejsY += parseFloat(`${chartw.size / 2}`);
         chartw = new Map([[chartg, (chartg == String.fromCharCode(105,0) ? chartg.length : chartg.length)]]);
         activityx /= Math.max(parseFloat(`${editd / 3}`), 4);
         activityx += parseFloat(`${fasto.length}`);
          let gpay6 = true;
         libcrashsdkR *= parseFloat(`${2 & parseInt(`${hongkongC}`)}`);
         gpay6 = (gpay6 ? gpay6 : gpay6);
       let dragcloseA = false;
          let winit_kvn = false;
         libcrashsdkR -= parseFloat(`${3}`);
         winit_kvn = (winit_kvn ? !winit_kvn : !winit_kvn);
      for (let q = 0; q < 1; q++) {
         activityx /= Math.max(3, parseFloat(`${1 * scoreH.length}`));
      }
      for (let s = 0; s < 3; s++) {
          let sport4: Array<any> = [800, 951];
          let profileinactiveI: Array<any> = [String.fromCharCode(109,111,110,111,99,104,114,111,109,101,95,121,95,49,54,0), String.fromCharCode(109,95,55,54,95,108,105,98,116,103,118,111,105,112,0), String.fromCharCode(114,95,55,53,95,99,111,111,107,105,101,0)];
          let dependency0 = true;
          let dangerc = false;
          let targetG = String.fromCharCode(105,95,54,48,95,102,114,97,103,0);
         dragcloseA = eventW > parseInt(`${hongkongC}`);
         sport4 = [((dangerc ? 4 : 2))];
         profileinactiveI.push(sport4.length);
         dependency0 = dangerc;
         targetG += "3";
      }
       let rootF = 2;
       let expandp = 2;
          let floaterA = String.fromCharCode(119,95,51,50,95,100,101,99,114,101,102,0);
          let gesturesS = 1.0;
          let previewz = String.fromCharCode(108,95,53,56,95,115,121,115,116,101,109,100,101,112,101,110,100,101,110,116,0);
         libcrashsdkR += parseFloat(`${fasto.length}`);
         floaterA = `${previewz.length / 1}`;
         gesturesS /= Math.max(5, parseFloat(`${parseInt(`${gesturesS}`) & previewz.length}`));
       let settingsv = 4.0;
      executorX += iconplaya;
      promotionY = [3];
      scoreH = `${promotionY.length / (Math.max(1, promotionY.length))}`;
      break;
   }
   if ((coreo / 3.17) == 5.30) {
      iconplaya >>= Math.min(5, Math.abs(executorX));
   }
   if (!appleU) {
      appleU = iconplaya > 75;
   }
       let skipW = false;
      if (!skipW) {
         skipW = !skipW;
      }
      for (let v = 0; v < 3; v++) {
         skipW = !skipW;
      }
      while (!skipW || !skipW) {
         skipW = (!skipW ? skipW : skipW);
         break;
      }
      backwardr = [parseInt(`${downloadingA}`) % (Math.max(4, largeP.length))];
      videocommonJ.set(`${executorX}`, executorX / 2);
      downloadingA /= Math.max(2, parseFloat(`${parseInt(`${coreo}`)}`));
      backwardr.push(iconplaya << (Math.min(Math.abs(1), 4)));
   if (1 >= (executorX / (Math.max(1, 10)))) {
      downloadingA /= Math.max(parseFloat(`${2}`), 2);
   }
   let penaltygoalU = 6373739 <= reactnavigation3;
   do {
      reactnavigation3 <<= Math.min(1, backwardr.length);
      if (penaltygoalU) {
         break;
      }
   } while (penaltygoalU && (3.81 >= (coreo * 2.59)));
   while (backwardr.length == 4) {
      backwardr.push(3);
      break;
   }
       let backward_: Array<any> = [String.fromCharCode(102,95,56,51,95,97,117,116,111,115,99,114,111,108,108,0), String.fromCharCode(122,95,56,50,95,100,114,97,110,100,0), String.fromCharCode(100,95,54,48,95,115,116,114,105,112,0)];
       let iconbellactiveS = false;
       let xvodJ = String.fromCharCode(122,95,52,48,95,114,101,99,118,118,0);
       let dialogy = String.fromCharCode(118,111,112,101,110,95,121,95,49,48,48,0);
       let apple4 = 3;
       let unimplementedviewu = 4;
      while (dialogy.includes(`${unimplementedviewu}`)) {
         dialogy = `${backward_.length | 3}`;
         break;
      }
      for (let o = 0; o < 3; o++) {
          let selectedx = String.fromCharCode(108,95,56,95,99,111,100,101,0);
          let watchnowbgH = String.fromCharCode(104,95,52,52,95,104,97,115,104,116,97,98,108,101,0);
          let release_tqT = 3.0;
         iconbellactiveS = apple4 >= dialogy.length;
         selectedx += "2";
         watchnowbgH = `${parseInt(`${release_tqT}`) - 2}`;
         release_tqT -= parseFloat(`${watchnowbgH.length}`);
      }
         backward_ = [(dialogy == String.fromCharCode(108,0) ? xvodJ.length : dialogy.length)];
      for (let r = 0; r < 3; r++) {
         dialogy += `${backward_.length}`;
      }
      coreo += parseFloat(`${reactnavigation3 % 1}`);
       let modulesX = String.fromCharCode(120,95,53,56,95,114,101,113,117,101,115,116,101,114,115,0);
       let nativeex9 = String.fromCharCode(118,95,51,48,0);
       let dependenciesj: Map<any, any> = new Map([[String.fromCharCode(99,104,101,99,107,101,100,95,48,95,55,56,0),537], [String.fromCharCode(99,104,97,110,103,101,114,95,110,95,57,48,0),282], [String.fromCharCode(115,95,54,48,95,105,111,101,114,114,110,111,109,101,109,0),104]]);
       let fileds = String.fromCharCode(103,97,105,110,99,95,101,95,55,0);
       let expiredI = String.fromCharCode(118,105,111,108,97,116,105,111,110,95,118,95,50,49,0);
      while (5 >= (dependenciesj.size * nativeex9.length)) {
         nativeex9 = `${modulesX.length >> (Math.min(4, nativeex9.length))}`;
         break;
      }
         nativeex9 = `${dependenciesj.size}`;
      let temperature9 = nativeex9 == String.fromCharCode(57,103,115,57,115,52,101,102,115,0);
      do {
         nativeex9 += `${dependenciesj.size ^ fileds.length}`;
         if (temperature9) {
            break;
         }
      } while (temperature9 && (fileds != String.fromCharCode(120,0)));
         dependenciesj.set(nativeex9, expiredI.length * 1);
         nativeex9 = "2";
         modulesX = "2";
          let profileframeh = 2.0;
         dependenciesj.set(modulesX, dependenciesj.size);
         profileframeh /= Math.max(parseInt(`${profileframeh}`), 4);
      let scoree = 7597810 <= fileds.length;
      do {
         fileds = `${(modulesX == String.fromCharCode(57,0) ? modulesX.length : fileds.length)}`;
         if (scoree) {
            break;
         }
      } while (scoree && (fileds.length > modulesX.length));
      reactnavigation3 >>= Math.min(Math.abs(parseInt(`${baseA}`) & 2), 4);
   let neong = appleU ? !appleU : appleU;
   do {
       let lightZ = 2;
         lightZ -= 2;
      if (3 < (lightZ << (Math.min(Math.abs(1), 2))) || 1 < (lightZ << (Math.min(Math.abs(1), 3)))) {
          let rightJ: Array<any> = [921, 631, 465];
         lightZ -= rightJ.length % 3;
      }
      for (let w = 0; w < 1; w++) {
          let submitC: Map<any, any> = new Map([[String.fromCharCode(101,97,114,108,121,95,112,95,55,54,0),808], [String.fromCharCode(121,95,53,0),436]]);
          let rewindy = String.fromCharCode(115,104,114,117,110,107,95,117,95,53,0);
          let robotoQ = String.fromCharCode(119,95,54,52,95,112,114,105,110,116,99,111,109,112,0);
         lightZ += 2;
         submitC.set(`${rewindy}`, submitC.size);
         rewindy += `${2 ^ rewindy.length}`;
         robotoQ += `${robotoQ.length - submitC.size}`;
      }
      appleU = executorX >= 85;
      if (neong) {
         break;
      }
   } while (neong && (!appleU || (baseA - 1.33) <= 1.48));
      foregroundA += parseFloat(`${1}`);
    RNRestart.Restart();
  }

  useEffect(() => {
    if(countToggleB == 8){
      switchToggle();
    }
  }, [countToggleB])

  return (
    
    
    
    
    
    
    //     }}>
    
    //   </Text>
    
    <ScreenContainer>
      <View style={{ gap: spacing.m }}>
        <TitleWithBackButtonHeader title="关于我们" />
        <View style={styles.logo}>
          <Logo2 height={icons.sizes.xxl} width={icons.sizes.xxl} />
        </View>
        <TouchableOpacity onPress={spamToggleB}>
          <Text style={{ textAlign: "center", ...textVariants.body }}>
            {APP_VERSION}
          </Text>
        </TouchableOpacity>
        <NotificationModal
          onConfirm={toggleOverlay}
          isVisible={isDialogOpen}
          title="版权声明"
          subtitle1={`如果该APP提供内容侵犯了您的版权, 请发送电子邮件说明, 我们将立即删除内容, 保护版权所有者的权益。`}
          subtitle2="联系邮箱:"
          subtitle3={APP_EMAIL_CONST}
        />
        <View>
          <ShowMoreButton
            text="隐私政策"
            onPress={() => navigation.navigate("隐私政策")}
          />
          <ShowMoreButton
            text="用户协议"
            onPress={() => navigation.navigate("用户协议")}
          />
          <ShowMoreButton
            text="版权声明"
            onPress={() => setIsDialogOpen(!isDialogOpen)}
          />
        </View>
      </View>
    </ScreenContainer>
  );
};

const styles = StyleSheet.create({
  topNav: {
    paddingTop: 10,
    paddingLeft: 10,
    paddingRight: 10,
    display: "flex",
    flexDirection: "row",
    width: "100%",
    alignItems: "center",
    justifyContent: "space-between",
  },
  logo: {
    flexDirection: "row",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
  },
  submitBtn: {
    borderRadius: 10,
  },
});
