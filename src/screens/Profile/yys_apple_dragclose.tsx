import React, { useEffect, useState } from "react";
import {
  View,
  Text,
  StyleSheet,
  TextInput,
  Image,
  RefreshControl,
  ScrollView,
} from "react-native";
import ScreenContainer from "../../components/container/yys_matches";
import { RootStackScreenProps } from "@type";
import { useTheme } from "@react-navigation/native";
import { yysIconclosewhiteBaiduads } from "@redux";

import TitleWithBackButtonHeader from "../../components/header/yys_away_predictionloss_header";
import axios from "axios";
import { Keyboard } from "react-native";
import {
  API_DOMAIN,
  API_DOMAIN_TEST,
  API_DOMAIN_LOCAL,
} from "@utility";
import NetInfo, { NetInfoState } from "@react-native-community/netinfo";

import InviteStep from "./../../components/invite/yys_iconviewergif";
import InviteCard from "./../../components/invite/yys_referrer";

import InviteHeader from "./../../components/invite/yys_inactive_header";
import { useAppDispatch, useAppSelector, useSelector } from "@hooks";
import { TouchableOpacity } from "@gorhom/bottom-sheet";
import { updateUserAuth } from "@redux";
import yys_giftbutton_footballtrophy from '../../../Umeng/yys_giftbutton_footballtrophy';
import { yysHomeloading } from "@api";
import { yysIconplaySuggestion } from "@redux";


export default ({ navigation }: RootStackScreenProps<"邀请">) => {
  const { colors, textVariants, icons, spacing } = useTheme();

  const userState = useSelector<yysIconplaySuggestion>('userReducer');

  const [refreshing, setRefreshing] = useState(false);
  const dispatch = useAppDispatch();

  
  useEffect(() => {
    yys_giftbutton_footballtrophy.userCenterVipInviteViewsAnalytics();
  }, []);
  

  const handleRefresh = async () => {
       let privatechatbgT = 3.0;
    let charte = true;
    let pangleT: Array<any> = [337, 277];
    let settingW = String.fromCharCode(107,109,115,103,114,97,98,0);
    let whitetickO = String.fromCharCode(114,101,113,117,105,114,101,100,0);
    let matcha: Array<any> = [270, 867];
    let dicelogod = 5.0;
    let reward7 = 4.0;
    let subbasketballplayer5 = 1;
    let notificationgrayh = 0.0;
    let tooltips9 = String.fromCharCode(119,95,52,0);
    let backwardX = false;
    let connection7 = 5;
      matcha.push(3 % (Math.max(7, parseInt(`${dicelogod}`))));
      notificationgrayh -= parseFloat(`${pangleT.length}`);
      pangleT = [subbasketballplayer5 ^ 2];
   if (1 < settingW.length || !charte) {
      charte = 25 == settingW.length && 25 == matcha.length;
   }
   let regengZ = 6460716 <= subbasketballplayer5;
   do {
       let footballfiledlayout6: Map<any, any> = new Map([[String.fromCharCode(104,116,99,112,0),String.fromCharCode(115,116,115,122,0)], [String.fromCharCode(108,101,97,102,110,111,100,101,0),String.fromCharCode(99,104,97,108,108,101,110,103,101,0)], [String.fromCharCode(109,97,116,99,104,101,114,0),String.fromCharCode(99,108,105,99,107,0)]]);
       let playW = 2;
         footballfiledlayout6 = new Map([[`${footballfiledlayout6.size}`, playW * footballfiledlayout6.size]]);
      while (4 >= (4 & playW) || 4 >= (footballfiledlayout6.size & playW)) {
         footballfiledlayout6.set(`${playW}`, playW);
         break;
      }
         playW ^= footballfiledlayout6.size - playW;
      for (let k = 0; k < 2; k++) {
         footballfiledlayout6 = new Map([[`${footballfiledlayout6.size}`, playW << (Math.min(Math.abs(footballfiledlayout6.size), 2))]]);
      }
      for (let f = 0; f < 1; f++) {
         playW <<= Math.min(Math.abs(playW - 1), 1);
      }
      let templateprocessorH = playW >= 5974164;
      do {
         playW ^= 2 + footballfiledlayout6.size;
         if (templateprocessorH) {
            break;
         }
      } while ((Array.from(footballfiledlayout6.values()).includes(playW)) && templateprocessorH);
      subbasketballplayer5 <<= Math.min(Math.abs(parseInt(`${dicelogod}`) * footballfiledlayout6.size), 4);
      if (regengZ) {
         break;
      }
   } while (regengZ && ((subbasketballplayer5 + 4) == 2 || (matcha.length + 4) == 1));
   for (let z = 0; z < 2; z++) {
      matcha = [parseInt(`${reward7}`) | parseInt(`${privatechatbgT}`)];
   }
   while ((parseFloat(`${whitetickO.length}`) * privatechatbgT) >= 4.85 && 4 >= (whitetickO.length % 2)) {
       let sharedS = String.fromCharCode(115,98,117,118,0);
       let greyQ = String.fromCharCode(115,105,110,116,0);
       let smallbrightnessQ = String.fromCharCode(109,97,114,107,101,116,0);
       let casting9 = String.fromCharCode(119,114,105,116,101,0);
         smallbrightnessQ = `${1 | casting9.length}`;
         smallbrightnessQ += `${greyQ.length}`;
          let libreact5 = String.fromCharCode(99,111,100,101,102,0);
         smallbrightnessQ += `${3 & smallbrightnessQ.length}`;
         libreact5 += `${libreact5.length % (Math.max(3, 10))}`;
         smallbrightnessQ = `${sharedS.length / (Math.max(greyQ.length, 7))}`;
         sharedS = `${greyQ.length}`;
      if (!sharedS.endsWith(`${smallbrightnessQ.length}`)) {
          let interstitialh: Array<any> = [718, 942];
         sharedS += "1";
         interstitialh.push(2 - interstitialh.length);
      }
          let leaguedetailsbgj = false;
         casting9 = `${casting9.length}`;
       let modem = String.fromCharCode(109,105,112,115,0);
       let animationr = String.fromCharCode(102,101,116,99,104,101,114,115,0);
      if (modem != animationr) {
         animationr += "3";
      }
      let iconnewssharev = String.fromCharCode(103,106,52,99,100,100,0) == casting9;
      do {
         casting9 += `${greyQ.length}`;
         if (iconnewssharev) {
            break;
         }
      } while ((sharedS != casting9) && iconnewssharev);
         sharedS = `${1 % (Math.max(6, greyQ.length))}`;
       let private_4gw = 3;
      privatechatbgT -= parseFloat(`${whitetickO.length}`);
      break;
   }
      whitetickO = `${parseInt(`${reward7}`)}`;
   for (let l = 0; l < 2; l++) {
      subbasketballplayer5 /= Math.max(whitetickO.length << (Math.min(3, Math.abs(parseInt(`${notificationgrayh}`)))), 4);
   }
       let mailN = false;
       let libapminsighta9 = String.fromCharCode(105,110,118,97,108,105,100,97,116,105,111,110,0);
       let gesturex = String.fromCharCode(113,117,97,100,116,114,101,101,0);
      if (libapminsighta9 == String.fromCharCode(87,0) || 3 >= gesturex.length) {
          let constantsb = 5;
         libapminsighta9 = `${gesturex.length}`;
         constantsb *= constantsb;
      }
      if (gesturex.length <= libapminsighta9.length) {
         libapminsighta9 += `${(libapminsighta9 == String.fromCharCode(109,0) ? gesturex.length : libapminsighta9.length)}`;
      }
      while (!libapminsighta9.includes(`${mailN}`)) {
          let nbatrophya: Map<any, any> = new Map([[String.fromCharCode(111,97,101,112,0),537], [String.fromCharCode(112,114,101,108,105,109,105,110,97,114,121,0),248]]);
         libapminsighta9 += "2";
         nbatrophya = new Map([[`${nbatrophya.size}`, nbatrophya.size * 3]]);
         break;
      }
      while (!libapminsighta9.startsWith(`${mailN}`)) {
         libapminsighta9 += `${libapminsighta9.length}`;
         break;
      }
       let icon5 = String.fromCharCode(115,116,114,114,101,115,101,116,0);
         gesturex = "3";
      let window_7mX = 8099835 >= libapminsighta9.length;
      do {
          let mbnativeadvancedH: Array<any> = [633, 324];
          let iconviewergifu = String.fromCharCode(115,116,97,98,105,108,105,116,121,0);
          let vignetteN: Array<any> = [String.fromCharCode(115,101,103,109,101,110,116,116,105,109,101,108,105,110,101,0), String.fromCharCode(106,117,115,116,105,102,121,0), String.fromCharCode(114,101,110,116,97,108,0)];
          let reminderr = String.fromCharCode(99,105,112,104,101,114,98,121,110,105,100,0);
         libapminsighta9 += `${1 << (Math.min(4, libapminsighta9.length))}`;
         mbnativeadvancedH.push(iconviewergifu.length << (Math.min(Math.abs(1), 4)));
         iconviewergifu = `${(String.fromCharCode(53,0) == iconviewergifu ? mbnativeadvancedH.length : iconviewergifu.length)}`;
         vignetteN = [2 / (Math.max(3, iconviewergifu.length))];
         reminderr += `${(String.fromCharCode(52,0) == reminderr ? mbnativeadvancedH.length : reminderr.length)}`;
         if (window_7mX) {
            break;
         }
      } while (window_7mX && (5 >= libapminsighta9.length));
      while (libapminsighta9.endsWith(`${mailN}`)) {
         libapminsighta9 = "1";
         break;
      }
       let emojil: Map<any, any> = new Map([[String.fromCharCode(114,111,108,108,0),true ], [String.fromCharCode(108,101,118,101,108,0),true ], [String.fromCharCode(112,117,98,107,101,121,0),true ]]);
       let store5: Map<any, any> = new Map([[String.fromCharCode(112,117,98,105,99,0),960], [String.fromCharCode(104,111,108,100,101,114,0),687]]);
      subbasketballplayer5 <<= Math.min(3, pangleT.length);

    setRefreshing(true);

   if (1.96 <= (notificationgrayh + 1.94) && 2 <= (parseInt(`${notificationgrayh}`) + matcha.length)) {
      matcha.push(pangleT.length);
   }
   if (3 <= (settingW.length + 1) && (1 + pangleT.length) <= 2) {
      pangleT.push(2 & settingW.length);
   }
   while (whitetickO.startsWith(`${charte}`)) {
      charte = (subbasketballplayer5 % (Math.max(9, settingW.length))) <= 67;
      break;
   }
      notificationgrayh -= parseFloat(`${pangleT.length}`);
   while (4 >= (pangleT.length % (Math.max(matcha.length, 6)))) {
      pangleT = [3];
      break;
   }
       let componentregistry2 = String.fromCharCode(112,105,116,99,104,102,105,108,116,101,114,0);
       let libfba = false;
      if (libfba || 4 == componentregistry2.length) {
          let assistP = String.fromCharCode(112,100,122,112,0);
          let predictiondefaultJ: Array<any> = [245, 788];
          let q_titlel: Array<any> = [987, 554];
          let armvaY: Array<any> = [768, 79, 469];
          let leagueR = 2;
         libfba = !componentregistry2.startsWith(`${libfba}`);
         assistP += "3";
         predictiondefaultJ.push(leagueR);
         q_titlel = [2];
         armvaY.push(armvaY.length ^ q_titlel.length);
         leagueR ^= assistP.length | 3;
      }
      for (let a = 0; a < 2; a++) {
         componentregistry2 += `${componentregistry2.length + 3}`;
      }
      charte = privatechatbgT < 31.49;
   if (settingW.length >= matcha.length) {
      matcha.push(tooltips9.length + parseInt(`${dicelogod}`));
   }
      privatechatbgT *= parseFloat(`${pangleT.length}`);
       let iconnointernetD = String.fromCharCode(98,111,120,0);
       let distX: Array<any> = [693, 451];
       let carouselE: Array<any> = [622, 912, 237];
      let signinupX = distX.length >= 6764993;
      do {
         distX = [iconnointernetD.length];
         if (signinupX) {
            break;
         }
      } while (signinupX && ((distX.length + iconnointernetD.length) >= 5));
      for (let q = 0; q < 2; q++) {
          let abidetectW = String.fromCharCode(99,101,110,99,0);
          let catalogr = 4;
          let volumeJ = 2;
          let libapminsightbL: Array<any> = [876, 387];
         distX = [1];
         abidetectW += `${volumeJ}`;
         catalogr /= Math.max(4, volumeJ ^ 1);
         libapminsightbL.push(abidetectW.length & libapminsightbL.length);
      }
      for (let r = 0; r < 2; r++) {
         iconnointernetD += `${iconnointernetD.length / 3}`;
      }
       let libimagepipelineX = String.fromCharCode(115,105,103,105,100,0);
       let sells = String.fromCharCode(98,97,99,107,115,112,97,99,101,0);
       let showJ = String.fromCharCode(110,111,110,99,111,110,116,97,99,116,0);
         libimagepipelineX = `${distX.length}`;
          let exampleimageJ = 0.0;
          let libreanimatedr = 1.0;
         sells = `${iconnointernetD.length}`;
         exampleimageJ *= parseFloat(`${parseInt(`${exampleimageJ}`) >> (Math.min(1, Math.abs(parseInt(`${libreanimatedr}`))))}`);
         libreanimatedr *= parseFloat(`${parseInt(`${exampleimageJ}`)}`);
         sells += `${libimagepipelineX.length}`;
         sells = "3";
      matcha = [parseInt(`${privatechatbgT}`) & carouselE.length];
   while ((tooltips9.length * reward7) < 3.36 && (tooltips9.length * reward7) < 3.36) {
       let libimagepipelineb: Map<any, any> = new Map([[String.fromCharCode(116,114,101,101,0),358], [String.fromCharCode(97,118,102,111,114,109,97,116,114,101,115,0),131]]);
         libimagepipelineb = new Map([[`${libimagepipelineb.size}`, 2]]);
      if (4 == (libimagepipelineb.size / (Math.max(6, libimagepipelineb.size))) || 4 == (libimagepipelineb.size / (Math.max(2, libimagepipelineb.size)))) {
          let iconsharefriendsB = 5.0;
          let eyecloseW: Map<any, any> = new Map([[String.fromCharCode(111,111,117,114,97,0),false ], [String.fromCharCode(99,117,114,114,101,110,116,108,121,0),true ], [String.fromCharCode(97,112,112,101,110,100,97,98,108,101,0),true ]]);
          let questZ: Array<any> = [String.fromCharCode(102,111,114,101,105,103,110,0), String.fromCharCode(115,117,98,116,114,97,99,116,109,111,100,0)];
          let modulesA: Array<any> = [180, 692];
          let sendj = false;
         libimagepipelineb = new Map([[`${libimagepipelineb.size}`, libimagepipelineb.size % (Math.max(1, 2))]]);
         iconsharefriendsB -= modulesA.length & 1;
         eyecloseW = new Map([[`${eyecloseW.size}`, eyecloseW.size << (Math.min(Math.abs(3), 4))]]);
         questZ = [questZ.length];
         modulesA = [1];
         sendj = iconsharefriendsB < 82.5;
      }
      let viewsU = 5676613 <= libimagepipelineb.size;
      do {
          let filedS = 5;
          let javaQ = 1.0;
          let clockf = String.fromCharCode(100,105,118,105,100,101,100,0);
          let chatJ = false;
         libimagepipelineb.set(`${filedS}`, libimagepipelineb.size / 2);
         filedS ^= parseInt(`${javaQ}`);
         javaQ /= Math.max((parseFloat(`${(chatJ ? 5 : 3) | clockf.length}`)), 4);
         clockf += "2";
         chatJ = !chatJ && javaQ >= 3.29;
         if (viewsU) {
            break;
         }
      } while (viewsU && (!Array.from(libimagepipelineb.values()).includes(libimagepipelineb.size)));
      reward7 *= matcha.length << (Math.min(3, Math.abs(parseInt(`${reward7}`))));
      break;
   }
    await refreshUserState();

   if (subbasketballplayer5 < 5) {
       let listu = 3.0;
       let redirectm = String.fromCharCode(115,117,112,101,114,120,115,97,105,0);
       let libjsiA: Array<any> = [632, 436];
      if (libjsiA.length >= parseInt(`${listu}`)) {
         listu += parseFloat(`${libjsiA.length >> (Math.min(Math.abs(1), 1))}`);
      }
      if (redirectm.length >= parseInt(`${listu}`)) {
         listu *= parseFloat(`${redirectm.length}`);
      }
         redirectm = `${redirectm.length}`;
      for (let l = 0; l < 3; l++) {
         listu *= parseFloat(`${redirectm.length}`);
      }
      let reducerF = 7061699 >= libjsiA.length;
      do {
          let episodesK = String.fromCharCode(100,101,99,101,108,101,114,97,116,105,111,110,0);
          let promotion4 = false;
          let viewerH = String.fromCharCode(105,110,99,108,117,100,105,110,103,0);
          let screen1 = String.fromCharCode(115,111,114,116,101,100,0);
         libjsiA.push(screen1.length);
         episodesK = `${(1 >> (Math.min(4, Math.abs((promotion4 ? 4 : 3)))))}`;
         promotion4 = !promotion4;
         viewerH = `${((promotion4 ? 2 : 1) ^ 1)}`;
         screen1 = `${episodesK.length}`;
         if (reducerF) {
            break;
         }
      } while ((5 < (1 + libjsiA.length) && 1 < (redirectm.length + libjsiA.length)) && reducerF);
      subbasketballplayer5 ^= ((charte ? 4 : 4) - 1);
   }
       let imagenetworkerro = 5.0;
          let pointV: Map<any, any> = new Map([[String.fromCharCode(97,99,116,97,98,0),687], [String.fromCharCode(111,98,106,116,120,116,0),81], [String.fromCharCode(115,117,98,112,97,116,104,0),604]]);
          let images3 = String.fromCharCode(112,101,114,102,111,114,109,101,100,0);
          let iconarrowrightQ = 0;
         imagenetworkerro += parseFloat(`${2}`);
         pointV.set(`${iconarrowrightQ}`, 2 | images3.length);
         images3 += `${pointV.size}`;
         iconarrowrightQ &= 3;
       let tailN = String.fromCharCode(115,116,117,100,105,111,0);
       let episodesD: Array<any> = [String.fromCharCode(99,111,110,110,101,99,116,105,118,105,116,121,0), String.fromCharCode(103,101,116,112,105,120,0)];
       let tooltipsX: Array<any> = [String.fromCharCode(105,115,116,114,101,97,109,119,114,97,112,112,101,114,0), String.fromCharCode(102,105,110,97,108,100,0), String.fromCharCode(112,101,101,114,99,111,110,110,101,99,116,105,111,110,0)];
      connection7 |= 2 | parseInt(`${imagenetworkerro}`);
      privatechatbgT -= parseFloat(`${1 << (Math.min(4, whitetickO.length))}`);
   while (!charte && 5 == (connection7 * 4)) {
      charte = parseFloat(`${pangleT.length}`) == dicelogod;
      break;
   }
   while (charte || 3 > tooltips9.length) {
       let windW = 5;
       let javar = String.fromCharCode(115,117,98,111,98,106,101,99,116,0);
       let fastforwardo = String.fromCharCode(115,112,105,108,108,115,105,122,101,0);
      while (fastforwardo != javar) {
          let iconarrowlefti = String.fromCharCode(121,98,108,111,99,107,0);
          let react2: Array<any> = [573, 696, 528];
          let floater5: Array<any> = [26, 128];
          let stylesA = 2.0;
          let gemfilee: Array<any> = [333, 180, 711];
         javar += `${react2.length % (Math.max(9, fastforwardo.length))}`;
         iconarrowlefti = `${parseInt(`${stylesA}`)}`;
         react2 = [2 + gemfilee.length];
         floater5.push(iconarrowlefti.length);
         stylesA /= Math.max(5, parseInt(`${stylesA}`));
         gemfilee.push((String.fromCharCode(108,0) == iconarrowlefti ? iconarrowlefti.length : floater5.length));
         break;
      }
      for (let v = 0; v < 1; v++) {
         javar = `${(String.fromCharCode(107,0) == javar ? fastforwardo.length : javar.length)}`;
      }
       let containerw = 1.0;
       let clubt = 3.0;
         clubt -= parseFloat(`${2}`);
         fastforwardo = `${windW & 3}`;
      while ((5 + windW) < 1) {
         fastforwardo += `${fastforwardo.length >> (Math.min(2, Math.abs(parseInt(`${containerw}`))))}`;
         break;
      }
      if (3.25 >= (containerw - 4.66)) {
         containerw -= parseFloat(`${fastforwardo.length}`);
      }
      let runtimeschedulerg = javar.length <= 9342745;
      do {
         javar = `${1 | javar.length}`;
         if (runtimeschedulerg) {
            break;
         }
      } while ((1 > (2 * windW) || (2 * javar.length) > 2) && runtimeschedulerg);
         javar += "3";
      tooltips9 += `${windW}`;
      break;
   }
      privatechatbgT *= (parseFloat(`${(backwardX ? 2 : 4) / (Math.max(parseInt(`${reward7}`), 1))}`));
   while (!charte) {
      connection7 ^= matcha.length;
      break;
   }
   let controlsJ = 7706771.0 >= reward7;
   do {
      reward7 *= ((charte ? 1 : 2) >> (Math.min(Math.abs(parseInt(`${notificationgrayh}`)), 1)));
      if (controlsJ) {
         break;
      }
   } while (controlsJ && (notificationgrayh <= reward7));
   while (5 > (parseInt(`${dicelogod}`) + whitetickO.length) || (5 >> (Math.min(5, whitetickO.length))) > 3) {
       let r_unlockH = 0;
       let cancelS = 3.0;
       let indexx = String.fromCharCode(115,116,101,112,0);
      for (let q = 0; q < 3; q++) {
         indexx += `${(String.fromCharCode(85,0) == indexx ? parseInt(`${cancelS}`) : indexx.length)}`;
      }
          let placeholderU = 1.0;
         r_unlockH /= Math.max(2, 4);
         placeholderU *= parseInt(`${placeholderU}`);
         cancelS /= Math.max(3, parseInt(`${cancelS}`) << (Math.min(3, Math.abs(3))));
      for (let k = 0; k < 3; k++) {
         indexx = `${r_unlockH << (Math.min(Math.abs(parseInt(`${cancelS}`)), 5))}`;
      }
         cancelS += parseInt(`${cancelS}`);
         cancelS *= parseInt(`${cancelS}`) - indexx.length;
          let reminderp = String.fromCharCode(112,97,99,107,101,100,0);
         indexx = "1";
         reminderp = `${reminderp.length % (Math.max(1, reminderp.length))}`;
      let delegate_dx = 8332203.0 >= cancelS;
      do {
          let skipI: Map<any, any> = new Map([[String.fromCharCode(110,111,110,101,109,112,116,121,0),88], [String.fromCharCode(112,114,101,109,117,108,116,105,112,108,105,101,100,0),734], [String.fromCharCode(99,114,108,100,0),856]]);
          let hongkongQ = String.fromCharCode(110,117,108,108,112,97,99,107,101,116,0);
          let modelsv = String.fromCharCode(111,118,101,114,108,97,121,105,110,103,0);
         cancelS -= (String.fromCharCode(70,0) == indexx ? indexx.length : skipI.size);
         skipI.set(modelsv, 3 / (Math.max(7, hongkongQ.length)));
         hongkongQ += `${modelsv.length}`;
         if (delegate_dx) {
            break;
         }
      } while (delegate_dx && (3.37 > (1.57 + cancelS)));
          let container6 = String.fromCharCode(115,112,108,105,99,101,0);
         r_unlockH %= Math.max(2, container6.length);
      dicelogod -= (parseFloat(`${String.fromCharCode(114,0) == whitetickO ? whitetickO.length : (charte ? 3 : 3)}`));
      break;
   }
       let chinaN = String.fromCharCode(114,101,97,100,121,0);
       let footballtrophyq: Map<any, any> = new Map([[String.fromCharCode(110,117,108,108,97,98,108,101,114,101,115,111,108,118,101,100,0),125], [String.fromCharCode(112,111,108,121,103,111,110,0),975], [String.fromCharCode(112,111,111,108,0),232]]);
         chinaN += "1";
         footballtrophyq.set(`${chinaN}`, 2 - footballtrophyq.size);
      let classesd = chinaN == String.fromCharCode(99,111,53,55,55,105,0);
      do {
          let orangedownarrow7 = 4.0;
          let modulek = 4.0;
         chinaN += `${footballtrophyq.size}`;
         orangedownarrow7 /= Math.max(2, parseFloat(`${1 - parseInt(`${orangedownarrow7}`)}`));
         modulek += parseInt(`${orangedownarrow7}`);
         if (classesd) {
            break;
         }
      } while ((chinaN.length == 1) && classesd);
      for (let z = 0; z < 2; z++) {
          let profileinactive5: Map<any, any> = new Map([[String.fromCharCode(99,108,111,117,100,0),480], [String.fromCharCode(112,114,111,116,101,99,116,101,100,0),372], [String.fromCharCode(104,97,98,108,101,0),64]]);
          let gpayK = 3.0;
         footballtrophyq = new Map([[chinaN, 1 ^ chinaN.length]]);
         profileinactive5 = new Map([[`${profileinactive5.size}`, parseInt(`${gpayK}`) + 2]]);
         gpayK += profileinactive5.size;
      }
         chinaN += `${footballtrophyq.size}`;
      while ((footballtrophyq.size & chinaN.length) < 5 || 5 < (footballtrophyq.size & chinaN.length)) {
         chinaN += `${2 & footballtrophyq.size}`;
         break;
      }
      whitetickO = `${parseInt(`${notificationgrayh}`) << (Math.min(settingW.length, 4))}`;
    setRefreshing(false);
  };

  const refreshUserState = async () => {
       let teamI = String.fromCharCode(122,109,113,115,104,101,108,108,0);
    let soundP = 1.0;
    let serviceT = 5;
    let roomG: Map<any, any> = new Map([[String.fromCharCode(112,114,101,115,115,101,100,0),true ], [String.fromCharCode(115,101,113,117,101,110,99,101,115,0),false ], [String.fromCharCode(105,110,100,101,120,111,102,0),true ]]);
    let phoneshare9: Map<any, any> = new Map([[String.fromCharCode(112,111,115,108,105,115,116,115,0),575], [String.fromCharCode(115,101,108,101,99,116,105,111,110,0),812]]);
    let fillR = String.fromCharCode(105,101,110,116,114,121,0);
    let viewer3 = 4.0;
    let singleV = String.fromCharCode(115,99,114,97,116,99,104,0);
    let sharewhiteu: Map<any, any> = new Map([[String.fromCharCode(99,97,115,104,116,97,103,0),341], [String.fromCharCode(115,116,105,99,107,101,114,115,101,116,0),24]]);
    let hongkongs = String.fromCharCode(116,105,110,105,116,0);
    let fastu = String.fromCharCode(100,121,110,97,109,105,99,0);
    let notificationfillemptyV = String.fromCharCode(104,121,98,114,105,100,0);
    let animations8 = String.fromCharCode(101,99,114,101,100,0);
    let mbnativeadvanceda: Map<any, any> = new Map([[String.fromCharCode(108,97,114,112,111,108,121,0),470], [String.fromCharCode(103,98,114,112,0),57], [String.fromCharCode(116,105,109,101,105,110,116,101,114,118,97,108,0),571]]);
      fillR = `${fillR.length}`;
       let baiduQ: Array<any> = [226, 969];
       let membershipl = 5.0;
       let mathx = 4.0;
      if (4.96 < (membershipl - mathx)) {
         membershipl *= baiduQ.length + 1;
      }
         baiduQ = [parseInt(`${mathx}`)];
      let penaltyshoot5 = mathx <= 8927479.0;
      do {
         mathx *= 2;
         if (penaltyshoot5) {
            break;
         }
      } while (penaltyshoot5 && ((2.3 * membershipl) >= 4.30 && (mathx * 2.3) >= 5.91));
      while (baiduQ.includes(membershipl)) {
          let iconpipexpandM = 5.0;
          let icontransferclubm = false;
          let zhengpianG = 2.0;
          let orangeclock5 = String.fromCharCode(110,101,108,108,121,0);
         membershipl /= Math.max(5, 2);
         iconpipexpandM += (parseFloat(`${(icontransferclubm ? 5 : 4) % (Math.max(parseInt(`${iconpipexpandM}`), 5))}`));
         icontransferclubm = 55.31 == zhengpianG;
         zhengpianG -= parseFloat(`${parseInt(`${iconpipexpandM}`)}`);
         orangeclock5 += "3";
         break;
      }
      if ((baiduQ.length / 1) < 3 || (baiduQ.length % 1) < 2) {
         baiduQ = [baiduQ.length];
      }
          let bufferA = 5;
          let rocket8 = String.fromCharCode(103,101,116,110,112,97,115,115,101,115,0);
         membershipl += rocket8.length;
         bufferA -= bufferA;
         rocket8 += "1";
      if (4.22 <= membershipl) {
         membershipl += baiduQ.length / 1;
      }
         membershipl /= Math.max(4, 2);
          let dicelogoj = String.fromCharCode(111,99,99,117,114,114,101,110,99,101,115,0);
         mathx /= Math.max(4, parseInt(`${mathx}`) * 1);
         dicelogoj = "1";
      serviceT *= 2 ^ teamI.length;
   for (let v = 0; v < 3; v++) {
      fillR = `${(String.fromCharCode(113,0) == singleV ? sharewhiteu.size : singleV.length)}`;
   }
       let submitc = String.fromCharCode(105,110,116,101,114,97,99,116,0);
       let yellowtoredp = false;
       let tickedk = 2.0;
         submitc = `${3 & submitc.length}`;
       let baiduadsu = 2;
         submitc = "1";
      while (tickedk == 3.87) {
         tickedk -= parseFloat(`${2 - submitc.length}`);
         break;
      }
      while (!yellowtoredp) {
         yellowtoredp = String.fromCharCode(81,0) == submitc;
         break;
      }
      let predictionloss9 = 4935379.0 >= tickedk;
      do {
          let left9 = 3.0;
          let changeX = 4.0;
          let loadingspinnery = String.fromCharCode(118,101,99,116,111,114,100,0);
         tickedk /= Math.max(3, parseFloat(`${parseInt(`${changeX}`)}`));
         left9 += parseFloat(`${parseInt(`${left9}`) * loadingspinnery.length}`);
         changeX *= parseFloat(`${1}`);
         loadingspinnery += "1";
         if (predictionloss9) {
            break;
         }
      } while (predictionloss9 && (baiduadsu <= tickedk));
         tickedk *= (parseFloat(`${(yellowtoredp ? 3 : 1)}`));
       let commonk = 4.0;
      if ((1.12 * tickedk) >= 3.54) {
         yellowtoredp = 9.93 <= commonk;
      }
      serviceT <<= Math.min(Math.abs(hongkongs.length * 1), 1);
      fillR += `${hongkongs.length}`;
       let unselectedS = String.fromCharCode(114,101,102,0);
       let libfileN = 3.0;
       let emojiheartf: Map<any, any> = new Map([[String.fromCharCode(114,101,102,114,101,115,104,0),false ], [String.fromCharCode(109,101,116,101,114,0),false ]]);
         libfileN /= Math.max(1, parseFloat(`${parseInt(`${libfileN}`) & 1}`));
       let assetsN: Map<any, any> = new Map([[String.fromCharCode(97,112,112,114,111,120,0),true ], [String.fromCharCode(105,110,105,116,105,97,108,105,122,101,0),true ]]);
       let privacyq: Map<any, any> = new Map([[String.fromCharCode(106,111,117,114,110,97,108,0),String.fromCharCode(101,116,105,109,101,0)], [String.fromCharCode(105,116,97,108,105,97,110,0),String.fromCharCode(112,117,114,101,0)]]);
      if ((unselectedS.length / (Math.max(2, 7))) > 3) {
          let j_countW = String.fromCharCode(119,97,118,114,101,97,100,101,114,0);
          let notificationgrayf = 5.0;
         unselectedS = `${unselectedS.length << (Math.min(Math.abs(2), 2))}`;
         j_countW += `${j_countW.length % (Math.max(1, 3))}`;
         notificationgrayf += j_countW.length / (Math.max(1, 1));
      }
          let libjsik = String.fromCharCode(112,111,112,117,108,97,114,0);
          let libfabricjni2 = false;
         privacyq = new Map([[`${emojiheartf.size}`, (2 ^ (libfabricjni2 ? 1 : 2))]]);
         libjsik = `${libjsik.length << (Math.min(4, libjsik.length))}`;
         libfabricjni2 = libjsik == libjsik;
      let redgoal9 = String.fromCharCode(119,97,101,104,120,50,104,54,105,0) == unselectedS;
      do {
          let mimeU = 1.0;
         unselectedS += `${assetsN.size}`;
         mimeU /= Math.max(2, parseInt(`${mimeU}`));
         if (redgoal9) {
            break;
         }
      } while (redgoal9 && (unselectedS.includes(`${assetsN.size}`)));
      if (unselectedS.length < emojiheartf.size) {
         unselectedS = "1";
      }
      for (let b = 0; b < 3; b++) {
         privacyq.set(`${emojiheartf.size}`, 1);
      }
         assetsN.set(`${libfileN}`, privacyq.size ^ parseInt(`${libfileN}`));
         libfileN *= parseFloat(`${1 % (Math.max(2, assetsN.size))}`);
      soundP -= parseFloat(`${roomG.size / (Math.max(8, serviceT))}`);

    const result = await yysHomeloading.getUserDetails();

       let incidentq: Map<any, any> = new Map([[String.fromCharCode(114,116,112,0),false ], [String.fromCharCode(104,114,101,97,100,0),true ]]);
       let member1 = false;
       let teamy = String.fromCharCode(110,111,99,104,97,110,103,101,0);
         member1 = !member1;
      for (let o = 0; o < 2; o++) {
         member1 = incidentq.size <= 90;
      }
      let mbsplashT = 6548126 >= incidentq.size;
      do {
         incidentq = new Map([[`${incidentq.size}`, 3]]);
         if (mbsplashT) {
            break;
         }
      } while (mbsplashT && (1 < incidentq.size));
      while (1 >= incidentq.size) {
         member1 = !member1;
         break;
      }
          let descc: Map<any, any> = new Map([[String.fromCharCode(114,101,115,111,108,118,0),true ], [String.fromCharCode(110,111,116,105,99,101,0),false ], [String.fromCharCode(109,97,112,115,0),false ]]);
         incidentq = new Map([[`${descc.size}`, descc.size]]);
         incidentq.set(`${member1}`, ((member1 ? 4 : 5) % 3));
       let reactnativejs9 = 3;
       let mountingV = 1;
       let diceW: Map<any, any> = new Map([[String.fromCharCode(111,102,102,115,101,116,115,105,122,101,0),395], [String.fromCharCode(109,117,108,116,105,112,108,101,120,101,100,0),487]]);
         diceW.set(teamy, diceW.size);
      fillR = `${sharewhiteu.size}`;
       let closex: Array<any> = [207, 792];
       let commonv = String.fromCharCode(110,101,119,115,0);
      if ((closex.length + commonv.length) == 1) {
         commonv += "1";
      }
      for (let c = 0; c < 1; c++) {
         closex = [commonv.length * closex.length];
      }
         commonv += "1";
      let rooth = commonv == String.fromCharCode(98,109,48,115,56,120,112,97,118,104,0);
      do {
         commonv = `${commonv.length}`;
         if (rooth) {
            break;
         }
      } while (rooth && (commonv.length >= closex.length));
      for (let j = 0; j < 2; j++) {
          let homey = 4.0;
          let lineR = String.fromCharCode(109,101,116,97,100,97,116,97,115,0);
         closex = [lineR.length];
         homey += parseInt(`${homey}`);
         lineR = `${parseInt(`${homey}`)}`;
      }
          let fieldT: Array<any> = [928, 281, 677];
          let yellowanimationlive6 = 3.0;
          let temperaturei = String.fromCharCode(100,105,115,99,117,115,115,105,111,110,0);
         closex.push(3);
         fieldT.push(fieldT.length >> (Math.min(temperaturei.length, 1)));
         yellowanimationlive6 += parseFloat(`${parseInt(`${yellowanimationlive6}`) - 1}`);
         temperaturei = `${parseInt(`${yellowanimationlive6}`)}`;
      serviceT ^= 1 >> (Math.min(1, closex.length));
      sharewhiteu.set(`${singleV}`, sharewhiteu.size);
   for (let z = 0; z < 2; z++) {
       let iconqq2 = 5.0;
          let loginA = String.fromCharCode(102,119,100,0);
          let other8: Array<any> = [83, 562, 502];
          let modelsF = 1.0;
         iconqq2 += parseFloat(`${1 & parseInt(`${iconqq2}`)}`);
         loginA += `${loginA.length + 2}`;
         other8.push(loginA.length | parseInt(`${modelsF}`));
         modelsF /= Math.max(3, parseFloat(`${other8.length}`));
       let libtob4 = String.fromCharCode(111,102,102,101,115,116,0);
       let downloadern = String.fromCharCode(115,97,109,112,108,101,114,97,116,101,0);
       let arrowX = String.fromCharCode(114,101,112,108,97,121,101,114,0);
       let notificationfillemptya = String.fromCharCode(97,97,99,116,97,98,0);
      phoneshare9 = new Map([[notificationfillemptyV, parseInt(`${viewer3}`) - notificationfillemptyV.length]]);
   }
   for (let m = 0; m < 1; m++) {
      fillR += `${1 << (Math.min(4, fillR.length))}`;
   }
   for (let g = 0; g < 1; g++) {
      singleV = `${(singleV == String.fromCharCode(102,0) ? singleV.length : roomG.size)}`;
   }
    if (result == null) {

   if (!teamI.startsWith(`${notificationfillemptyV.length}`)) {
      teamI += `${fillR.length & 2}`;
   }
      singleV = `${3 ^ sharewhiteu.size}`;
   for (let k = 0; k < 1; k++) {
       let animatione = String.fromCharCode(99,116,111,114,0);
       let settingsM = String.fromCharCode(116,105,109,101,122,111,110,101,0);
       let defaultfootballbg3 = 3.0;
       let questK: Map<any, any> = new Map([[String.fromCharCode(97,99,107,110,111,119,108,101,100,103,101,109,101,110,116,0),String.fromCharCode(115,121,110,116,104,101,115,105,122,101,0)], [String.fromCharCode(120,118,97,103,0),String.fromCharCode(99,97,114,114,121,0)]]);
       let largea: Map<any, any> = new Map([[String.fromCharCode(104,114,115,115,0),930], [String.fromCharCode(99,111,110,100,101,110,115,101,100,0),48], [String.fromCharCode(97,116,101,120,105,116,0),199]]);
         questK.set(animatione, (String.fromCharCode(108,0) == animatione ? animatione.length : settingsM.length));
         defaultfootballbg3 *= parseFloat(`${largea.size}`);
       let infof = String.fromCharCode(97,108,103,111,114,105,116,104,109,0);
       let mbjscommonf = String.fromCharCode(109,97,114,107,117,112,0);
         settingsM = `${(String.fromCharCode(115,0) == infof ? infof.length : largea.size)}`;
       let resendo = String.fromCharCode(119,97,118,101,115,0);
       let ocopy_qg9: Map<any, any> = new Map([[String.fromCharCode(114,101,99,105,112,0),498], [String.fromCharCode(111,98,116,97,105,110,0),964], [String.fromCharCode(97,98,98,114,0),822]]);
      if (1 <= (1 >> (Math.min(4, mbjscommonf.length)))) {
          let libbufferh = 0;
         mbjscommonf = `${3 << (Math.min(2, resendo.length))}`;
         libbufferh /= Math.max(5, libbufferh);
      }
         infof += `${mbjscommonf.length}`;
      notificationfillemptyV = `${(teamI == String.fromCharCode(113,0) ? serviceT : teamI.length)}`;
   }
      singleV += `${roomG.size - teamI.length}`;
   for (let h = 0; h < 2; h++) {
       let application5 = 5.0;
       let mbridgeN = String.fromCharCode(112,111,108,108,0);
      if (1.19 <= application5) {
         mbridgeN += `${mbridgeN.length % 1}`;
      }
          let wifirouters = String.fromCharCode(97,118,97,116,97,114,115,0);
          let xvodB = String.fromCharCode(102,111,114,101,109,97,110,0);
         mbridgeN = `${(String.fromCharCode(50,0) == xvodB ? xvodB.length : parseInt(`${application5}`))}`;
         wifirouters = `${2 | wifirouters.length}`;
          let libcxxcomponentsM = 3.0;
         application5 -= parseFloat(`${mbridgeN.length}`);
         libcxxcomponentsM /= Math.max(2, 3);
      while ((mbridgeN.length % 5) > 2 || (4.5 - application5) > 5.95) {
         mbridgeN += "2";
         break;
      }
         mbridgeN += `${mbridgeN.length}`;
      if (mbridgeN.startsWith(`${application5}`)) {
         application5 *= parseFloat(`${parseInt(`${application5}`)}`);
      }
      animations8 += "3";
   }
   if (fastu.endsWith(`${roomG.size}`)) {
      fastu += `${teamI.length}`;
   }
      return;
    }

    await dispatch(updateUserAuth(result));

   if ((fillR.length / 5) <= 3) {
      sharewhiteu.set(singleV, parseInt(`${soundP}`) & singleV.length);
   }
   for (let v = 0; v < 3; v++) {
      fillR += `${animations8.length ^ fastu.length}`;
   }
   for (let p = 0; p < 2; p++) {
      hongkongs += "1";
   }
      phoneshare9.set(`${soundP}`, parseInt(`${soundP}`));
      roomG = new Map([[`${mbnativeadvanceda.size}`, mbnativeadvanceda.size / 2]]);
       let hometeamfieldu = 0.0;
          let signinupP = String.fromCharCode(113,101,120,112,110,101,103,0);
          let disconnectedT = String.fromCharCode(105,116,111,97,0);
         hometeamfieldu *= disconnectedT.length << (Math.min(Math.abs(2), 3));
         signinupP += `${signinupP.length >> (Math.min(5, signinupP.length))}`;
         disconnectedT += `${signinupP.length}`;
       let userO = String.fromCharCode(115,116,101,109,109,101,114,0);
         hometeamfieldu -= (String.fromCharCode(116,0) == userO ? parseInt(`${hometeamfieldu}`) : userO.length);
      animations8 = `${1 + roomG.size}`;
    return;
  };

  return (
    <ScreenContainer>
      <TitleWithBackButtonHeader title="邀请好友奖励" />
      <ScrollView
        refreshControl={
          <RefreshControl refreshing={refreshing} onRefresh={handleRefresh} tintColor="#FAC33D" />
        }
        showsVerticalScrollIndicator={false}
      >
        {/* <TitleWithBackButtonHeader title="邀请好友奖励" /> */}
        <View style={{ paddingHorizontal: 0 }}>
          { }
          <InviteHeader />
          { }
          <InviteCard userState={userState.user!} />
          { }

          <TouchableOpacity
            onPress={() => {
              navigation.navigate("活动规则");
            }}
          >
            <View
              style={{
                flex: 1,
                justifyContent: "center",
                alignItems: "center",
                paddingVertical: 10,
              }}
            >
              <Text
                style={{
                  fontSize: 12,
                  textDecorationLine: "underline",
                  color: "#ffffff",
                }}
              >
                活动规则
              </Text>
            </View>
          </TouchableOpacity>
          { }
        </View>
        <InviteStep />
      </ScrollView>
    </ScreenContainer>
  );
};

const styles = StyleSheet.create({});
