import React, { useEffect, useState } from 'react';
import {
  View,
  Text,
  StyleSheet,
  TextInput,
  Image,
  ImageBackground,
  ScrollView,
  RefreshControl,
} from 'react-native';
import ScreenContainer from '../../components/container/ww_collection';
import { RootStackScreenProps } from '@type/ww_tempnodatagif_description';
import { useTheme } from '@react-navigation/native';
import { wwEighteenShirt } from '@redux/ww_small';

import TitleWithBackButtonHeader from '../../components/header/ww_react_predictionwin_header';
import axios from 'axios';
import { Keyboard } from 'react-native';
import {
  API_DOMAIN,
  API_DOMAIN_TEST,
  API_DOMAIN_LOCAL,
} from '@utility/ww_icon';
import NetInfo, { NetInfoState } from '@react-native-community/netinfo';

import InviteStep from '../../components/invite/ww_libjsi';
import InviteCard from '../../components/invite/ww_analytic';

import InviteHeader from '../../components/invite/ww_catalog_header';
import FastImage from '../../components/common/ww_result';
import LinearGradient from 'react-native-linear-gradient';
import { useAppSelector, useSelector } from '@hooks/ww_catagory_neon';
import {
  updateUserAuth,
  updateUserReferral,
} from '@redux/actions/ww_libreactperfloggerjni_small';
import { useDispatch } from 'react-redux';
import { wwIconscheduleColors } from '@api';
import { wwVertical } from '@redux/reducers/ww_related_kuaishou';
export default ({ navigation }: RootStackScreenProps<'邀请详情'>) => {
  const { colors, textVariants, icons, spacing } = useTheme();

  const userState = useSelector<wwVertical>('userReducer');
  const dispatch = useDispatch();

  const [refreshing, setRefreshing] = useState(false);
  const [inviteList, setInviteList] = useState([]);

  const handleRefresh = async () => {
       let loginsuccessC = String.fromCharCode(98,108,97,109,101,100,95,117,95,54,52,0);
    let spinners: Array<any> = [92, 285];
    let mail8: Map<any, any> = new Map([[String.fromCharCode(111,95,52,57,95,112,97,114,116,115,0),630], [String.fromCharCode(103,95,50,49,95,97,116,101,120,105,116,0),995]]);
    let tooltipsb = String.fromCharCode(108,95,52,56,95,114,103,98,120,105,0);
    let target5 = String.fromCharCode(115,109,97,114,116,95,115,95,57,54,0);
    let injuryX = String.fromCharCode(105,95,51,55,95,99,104,107,108,105,115,116,0);
    let subtextF = 0.0;
    let tickf = String.fromCharCode(98,95,49,48,48,95,100,101,115,99,0);
    let agreementX = 5.0;
    let langQ = String.fromCharCode(98,95,51,50,95,105,102,97,109,115,103,0);
   for (let v = 0; v < 2; v++) {
      tooltipsb += `${tickf.length % (Math.max(3, 4))}`;
   }
   while (4 <= (1 >> (Math.min(1, loginsuccessC.length))) && 4 <= (1 * loginsuccessC.length)) {
      agreementX *= parseFloat(`${injuryX.length}`);
      break;
   }
   while (langQ != String.fromCharCode(102,0)) {
      injuryX += `${(tickf == String.fromCharCode(49,0) ? injuryX.length : tickf.length)}`;
      break;
   }
       let round8: Map<any, any> = new Map([[String.fromCharCode(106,95,49,57,95,109,98,117,118,0),290], [String.fromCharCode(115,104,97,114,100,95,104,95,53,0),638]]);
      let rulesc = round8.size >= 4946934;
      do {
         round8.set(`${round8.size}`, round8.size);
         if (rulesc) {
            break;
         }
      } while (rulesc && (round8.get(`${round8.size}`) != null));
         round8 = new Map([[`${round8.size}`, 3]]);
          let pnewinterstitialM = 5.0;
          let privilege6: Map<any, any> = new Map([[String.fromCharCode(109,105,110,111,114,95,107,95,50,56,0),String.fromCharCode(98,95,54,54,95,109,111,100,105,102,105,99,97,116,105,111,110,0)], [String.fromCharCode(97,112,112,114,101,99,97,116,105,111,110,104,111,117,114,115,95,110,95,53,48,0),String.fromCharCode(115,95,49,48,95,117,110,119,105,110,100,0)]]);
         round8 = new Map([[`${round8.size}`, parseInt(`${pnewinterstitialM}`) / (Math.max(round8.size, 2))]]);
         pnewinterstitialM += parseFloat(`${2}`);
         privilege6.set(`${privilege6.size}`, privilege6.size);
      langQ = `${loginsuccessC.length * injuryX.length}`;
      spinners.push(target5.length - tooltipsb.length);
      tickf = "2";
      loginsuccessC += `${target5.length | parseInt(`${subtextF}`)}`;
       let reactnativejs2 = String.fromCharCode(105,110,105,116,105,97,108,95,104,95,55,51,0);
       let videovarg = 3.0;
      let baseV = 7024327.0 >= videovarg;
      do {
         videovarg += parseFloat(`${reactnativejs2.length - 1}`);
         if (baseV) {
            break;
         }
      } while (baseV && (!reactnativejs2.startsWith(`${videovarg}`)));
      if (1.61 > (parseFloat(`${reactnativejs2.length}`) + videovarg) || (reactnativejs2.length + parseInt(`${videovarg}`)) > 1) {
         reactnativejs2 = `${parseInt(`${videovarg}`) << (Math.min(Math.abs(3), 1))}`;
      }
      for (let q = 0; q < 2; q++) {
          let fcdaebecbcbafcdfceaaeccfeacdbM = 2.0;
          let profileframeK = String.fromCharCode(105,110,100,101,120,111,102,95,106,95,56,52,0);
         videovarg /= Math.max(parseFloat(`${3}`), 2);
         fcdaebecbcbafcdfceaaeccfeacdbM += parseFloat(`${1}`);
         profileframeK += `${parseInt(`${fcdaebecbcbafcdfceaaeccfeacdbM}`)}`;
      }
      for (let b = 0; b < 3; b++) {
         videovarg *= parseFloat(`${parseInt(`${videovarg}`) >> (Math.min(Math.abs(3), 2))}`);
      }
       let downloadingy = String.fromCharCode(108,95,50,52,95,116,119,114,112,0);
       let armvaa = String.fromCharCode(107,95,52,53,95,115,101,113,117,101,110,99,101,0);
      for (let o = 0; o < 2; o++) {
          let homex: Array<any> = [String.fromCharCode(115,105,103,110,97,108,105,110,103,95,118,95,52,53,0), String.fromCharCode(110,111,105,110,100,101,120,95,118,95,54,53,0), String.fromCharCode(115,116,114,116,111,100,95,115,95,56,52,0)];
          let ksad5 = String.fromCharCode(122,95,53,55,95,116,104,114,111,116,116,108,101,114,0);
          let notificationY = String.fromCharCode(120,100,97,105,95,57,95,55,55,0);
         armvaa += `${notificationY.length >> (Math.min(5, homex.length))}`;
         homex.push((String.fromCharCode(100,0) == ksad5 ? ksad5.length : ksad5.length));
      }
      target5 += `${(target5 == String.fromCharCode(75,0) ? target5.length : tooltipsb.length)}`;
   for (let o = 0; o < 1; o++) {
      tickf = `${1 ^ injuryX.length}`;
   }
      tooltipsb += `${parseInt(`${agreementX}`) - 1}`;
   if ((5 >> (Math.min(1, injuryX.length))) == 4 && 5 == (injuryX.length >> (Math.min(Math.abs(5), 5)))) {
       let eyeopenl = 4;
       let catalogm = String.fromCharCode(100,95,57,56,95,102,114,97,109,101,110,117,109,0);
         eyeopenl ^= catalogm.length + eyeopenl;
      while ((catalogm.length & eyeopenl) < 2 && 5 < (catalogm.length & 2)) {
         eyeopenl |= catalogm.length;
         break;
      }
          let profileframe9 = 0;
          let tumbnail8: Array<any> = [470, 628, 245];
         eyeopenl &= eyeopenl | profileframe9;
         profileframe9 *= tumbnail8.length;
         tumbnail8 = [3 % (Math.max(6, tumbnail8.length))];
       let stationC = true;
       let inewarchdefaults6 = true;
       let dragclose8 = false;
       let mergerd = true;
          let typingh = 5.0;
         dragclose8 = !dragclose8;
         typingh *= parseInt(`${typingh}`) - parseInt(`${typingh}`);
      spinners.push(3 + injuryX.length);
   }
   while ((spinners.length / 3) <= 1 || 3 <= (spinners.length / (Math.max(4, mail8.size)))) {
      mail8.set(tickf, tickf.length % 1);
      break;
   }

    setRefreshing(true);

   let networkE = tickf == String.fromCharCode(99,107,103,115,109,113,102,104,51,0);
   do {
      tickf = "3";
      if (networkE) {
         break;
      }
   } while ((4.89 <= agreementX) && networkE);
   let modityx = 9267138.0 >= subtextF;
   do {
      subtextF -= spinners.length ^ 2;
      if (modityx) {
         break;
      }
   } while (modityx && (mail8.size > parseInt(`${subtextF}`)));
   for (let t = 0; t < 1; t++) {
      tooltipsb = `${parseInt(`${subtextF}`)}`;
   }
      mail8 = new Map([[`${spinners.length}`, spinners.length & 1]]);
   while (5 == tickf.length) {
       let upgradeQ: Map<any, any> = new Map([[String.fromCharCode(110,95,54,51,95,115,104,111,117,108,100,0),566], [String.fromCharCode(118,95,56,0),305], [String.fromCharCode(112,111,115,116,102,105,120,95,111,95,56,56,0),273]]);
       let stringQ = 3.0;
       let libsgcorez = String.fromCharCode(115,116,121,108,101,100,95,108,95,51,54,0);
      if (4 <= (upgradeQ.size << (Math.min(Math.abs(1), 4)))) {
          let executorD = String.fromCharCode(112,117,98,105,99,95,50,95,54,50,0);
          let halfu = 5;
         upgradeQ = new Map([[libsgcorez, libsgcorez.length % 3]]);
         executorD = `${halfu}`;
         halfu += 3;
      }
         stringQ *= parseInt(`${stringQ}`) / (Math.max(libsgcorez.length, 1));
       let promotionU = String.fromCharCode(110,95,49,52,95,115,116,114,105,100,101,98,0);
       let mergerS = String.fromCharCode(119,95,51,51,95,105,110,115,116,114,117,99,116,105,111,110,0);
      if (1 == (upgradeQ.size >> (Math.min(Math.abs(2), 5))) && (stringQ - 2.100) == 2.29) {
          let penaltygoalP = 5.0;
          let giflivestreamingQ: Array<any> = [144, 387, 338];
         upgradeQ = new Map([[mergerS, parseInt(`${stringQ}`)]]);
         penaltygoalP += parseFloat(`${parseInt(`${penaltygoalP}`) & 2}`);
         giflivestreamingQ.push(parseInt(`${penaltygoalP}`) * 3);
      }
          let whistle5: Map<any, any> = new Map([[String.fromCharCode(97,99,99,95,115,95,53,48,0),String.fromCharCode(101,110,99,111,100,101,100,112,111,105,110,116,95,101,95,53,55,0)], [String.fromCharCode(107,95,51,50,95,104,105,103,104,101,115,116,0),String.fromCharCode(112,95,52,50,95,100,105,115,97,98,108,105,110,103,0)]]);
          let ballt = String.fromCharCode(110,105,100,110,105,115,116,95,120,95,52,49,0);
         upgradeQ.set(libsgcorez, (libsgcorez == String.fromCharCode(69,0) ? ballt.length : libsgcorez.length));
         whistle5.set(`${whistle5.size}`, whistle5.size);
         ballt = "1";
         libsgcorez = "2";
          let w_playera: Map<any, any> = new Map([[String.fromCharCode(117,110,114,101,103,105,115,116,101,114,95,121,95,50,48,0),114], [String.fromCharCode(119,95,54,50,95,100,97,116,101,0),902], [String.fromCharCode(99,117,114,115,111,114,115,95,97,95,56,52,0),707]]);
          let penaltygoalE = String.fromCharCode(114,111,117,110,100,115,95,113,95,51,56,0);
          let tooltipsE: Array<any> = [328, 774];
         stringQ *= upgradeQ.size - penaltygoalE.length;
         w_playera = new Map([[`${w_playera.size}`, 2 | w_playera.size]]);
         penaltygoalE = `${w_playera.size}`;
         tooltipsE.push(w_playera.size / 2);
       let reactnativejs5 = 0.0;
      tooltipsb = `${injuryX.length}`;
      break;
   }
   for (let j = 0; j < 1; j++) {
      spinners.push((tooltipsb == String.fromCharCode(121,0) ? tickf.length : tooltipsb.length));
   }
      langQ = "1";
   let playercommonI = 9245170.0 <= agreementX;
   do {
      agreementX -= parseFloat(`${loginsuccessC.length}`);
      if (playercommonI) {
         break;
      }
   } while (((3 ^ tickf.length) > 5 || (parseFloat(`${tickf.length}`) - agreementX) > 3.75) && playercommonI);
   while (tickf.length == 4 || 4 == tooltipsb.length) {
       let kuaishouw = 2;
       let launchers = 2.0;
          let playercommonj: Array<any> = [String.fromCharCode(105,110,105,116,105,97,108,95,115,95,52,54,0), String.fromCharCode(98,101,116,104,115,111,102,116,118,105,100,95,117,95,52,52,0), String.fromCharCode(116,95,53,55,95,117,110,105,110,105,116,105,97,108,105,122,101,100,0)];
          let defaultpredictionprofileF = String.fromCharCode(122,95,51,54,95,115,117,114,114,111,117,110,100,0);
          let iconshareX = 5.0;
         launchers += parseFloat(`${kuaishouw * 2}`);
         playercommonj = [(defaultpredictionprofileF == String.fromCharCode(66,0) ? playercommonj.length : defaultpredictionprofileF.length)];
         iconshareX -= (defaultpredictionprofileF == String.fromCharCode(88,0) ? playercommonj.length : defaultpredictionprofileF.length);
      for (let r = 0; r < 2; r++) {
          let panglet = 2;
          let ballh = 2.0;
          let arrowrights = 5.0;
          let clockW = String.fromCharCode(120,95,50,51,95,116,101,115,116,98,114,105,100,103,101,0);
         kuaishouw += 1;
         panglet += (String.fromCharCode(69,0) == clockW ? clockW.length : parseInt(`${ballh}`));
         ballh *= parseInt(`${ballh}`) % 1;
         arrowrights += parseFloat(`${clockW.length * 1}`);
      }
      tooltipsb += `${injuryX.length << (Math.min(4, target5.length))}`;
      break;
   }
       let type_6W = 4;
         type_6W <<= Math.min(Math.abs(2), 2);
      if (type_6W == 1) {
         type_6W ^= 1;
      }
       let commento = String.fromCharCode(115,116,114,99,97,116,95,102,95,49,53,0);
      langQ = "2";
   while (langQ.endsWith(`${spinners.length}`)) {
      langQ = `${injuryX.length}`;
      break;
   }
   for (let d = 0; d < 3; d++) {
       let routerN: Array<any> = [752, 137];
      if (3 <= (routerN.length & routerN.length)) {
         routerN = [routerN.length];
      }
          let predictionactivey = String.fromCharCode(106,95,49,57,95,108,105,116,116,108,101,0);
         routerN.push(routerN.length / (Math.max(3, 8)));
         predictionactivey += `${predictionactivey.length | 3}`;
      let spinnern = routerN.length >= 7576005;
      do {
         routerN.push(2 % (Math.max(6, routerN.length)));
         if (spinnern) {
            break;
         }
      } while (spinnern && (3 == (routerN.length ^ 2) && 5 == (routerN.length ^ 2)));
      loginsuccessC += `${routerN.length % 1}`;
   }
    await refreshUserState();

       let qaagJ = String.fromCharCode(99,111,109,98,105,110,97,116,111,114,95,118,95,50,55,0);
       let chartf = String.fromCharCode(104,95,55,53,95,99,111,108,108,97,112,115,105,110,103,0);
       let libglogl = String.fromCharCode(109,117,115,120,95,102,95,50,54,0);
      let videovark = qaagJ.length >= 7708573;
      do {
         qaagJ = `${qaagJ.length % 3}`;
         if (videovark) {
            break;
         }
      } while (videovark && (qaagJ.length < chartf.length));
      for (let i = 0; i < 3; i++) {
          let moduleM = String.fromCharCode(114,111,119,115,95,106,95,56,50,0);
          let default_2Z = false;
         qaagJ += `${chartf.length | 2}`;
         moduleM += `${((default_2Z ? 3 : 4) & 2)}`;
         default_2Z = moduleM.length < 63 && default_2Z;
      }
      for (let q = 0; q < 2; q++) {
         qaagJ += `${libglogl.length / (Math.max(9, chartf.length))}`;
      }
      while (1 <= libglogl.length) {
         chartf = `${chartf.length}`;
         break;
      }
          let mountingh: Array<any> = [String.fromCharCode(98,95,55,57,95,116,97,112,112,101,100,0), String.fromCharCode(108,95,54,48,95,115,121,110,99,0)];
         chartf = "1";
         mountingh = [mountingh.length];
      while (libglogl != qaagJ) {
         qaagJ += `${3 + libglogl.length}`;
         break;
      }
      let libjsinspectorK = chartf == String.fromCharCode(51,48,118,107,120,54,0);
      do {
         chartf += `${libglogl.length}`;
         if (libjsinspectorK) {
            break;
         }
      } while ((!qaagJ.endsWith(`${chartf.length}`)) && libjsinspectorK);
         chartf += `${libglogl.length / (Math.max(1, 3))}`;
      if (chartf != String.fromCharCode(114,0) || qaagJ != String.fromCharCode(86,0)) {
          let countdown9 = 1.0;
          let tempnodatagifi = String.fromCharCode(115,95,56,52,95,111,118,101,114,102,108,111,119,0);
          let profileframev: Map<any, any> = new Map([[String.fromCharCode(116,95,51,55,95,114,103,98,120,105,0),false ], [String.fromCharCode(115,95,54,55,95,97,108,108,0),false ], [String.fromCharCode(115,95,54,54,95,116,105,100,121,0),false ]]);
          let vnewarchdefaultsT: Map<any, any> = new Map([[String.fromCharCode(108,105,98,115,115,104,95,109,95,56,57,0),261], [String.fromCharCode(97,95,55,50,95,106,117,109,112,0),800], [String.fromCharCode(97,100,97,112,116,97,116,105,111,110,115,101,116,95,112,95,49,55,0),39]]);
          let notificationR = 1.0;
         qaagJ += `${libglogl.length - 1}`;
         countdown9 -= parseInt(`${notificationR}`) % 3;
         tempnodatagifi = `${parseInt(`${countdown9}`)}`;
         profileframev.set(`${countdown9}`, parseInt(`${countdown9}`));
         vnewarchdefaultsT.set(`${countdown9}`, parseInt(`${countdown9}`));
         notificationR *= parseFloat(`${parseInt(`${notificationR}`) >> (Math.min(Math.abs(vnewarchdefaultsT.size), 1))}`);
      }
      target5 = "3";
   let iconclosewhitebgs = mail8.size <= 5693217;
   do {
       let countryi = String.fromCharCode(117,118,97,114,105,110,116,95,100,95,51,49,0);
       let o_unlock0 = String.fromCharCode(120,95,54,54,95,117,110,102,111,114,109,97,116,116,101,100,0);
       let iconclosewhitebgW = String.fromCharCode(97,114,116,105,115,95,110,95,50,0);
       let targetU = true;
          let delegate_fG = String.fromCharCode(110,117,108,108,105,102,95,109,95,56,55,0);
          let libavcodecO = 0;
          let componenta = String.fromCharCode(115,116,115,115,95,102,95,55,50,0);
         iconclosewhitebgW += `${countryi.length << (Math.min(Math.abs(1), 1))}`;
         delegate_fG = `${3 | delegate_fG.length}`;
         libavcodecO -= componenta.length - 2;
         componenta += `${(componenta == String.fromCharCode(102,0) ? componenta.length : libavcodecO)}`;
      mail8.set(`${agreementX}`, parseInt(`${agreementX}`) & 3);
      if (iconclosewhitebgs) {
         break;
      }
   } while ((mail8.get(`${agreementX}`) == null) && iconclosewhitebgs);
   while (4 == (mail8.size - spinners.length) && (spinners.length - mail8.size) == 4) {
       let dragclosea = 2.0;
       let attributedstringc: Map<any, any> = new Map([[String.fromCharCode(107,95,54,48,95,115,97,100,120,120,0),false ], [String.fromCharCode(101,95,55,53,95,98,105,116,115,113,112,0),true ], [String.fromCharCode(106,95,54,50,95,114,110,110,111,105,115,101,0),true ]]);
      let libfollyg = dragclosea >= 9751920.0;
      do {
         dragclosea += 2;
         if (libfollyg) {
            break;
         }
      } while (libfollyg && (attributedstringc.get(`${dragclosea}`) == null));
         dragclosea /= Math.max(4, attributedstringc.size);
      if ((dragclosea * 1.29) < 2.77) {
         attributedstringc.set(`${dragclosea}`, 1 | attributedstringc.size);
      }
         attributedstringc = new Map([[`${attributedstringc.size}`, parseInt(`${dragclosea}`) + 3]]);
         attributedstringc.set(`${dragclosea}`, 1 & parseInt(`${dragclosea}`));
      for (let o = 0; o < 2; o++) {
         dragclosea += 1;
      }
      spinners = [injuryX.length / 1];
      break;
   }
   for (let v = 0; v < 2; v++) {
      injuryX = `${spinners.length | 2}`;
   }
   if (Array.from(mail8.values()).includes(spinners.length)) {
      mail8 = new Map([[tooltipsb, (String.fromCharCode(89,0) == target5 ? tooltipsb.length : target5.length)]]);
   }
   if (tooltipsb != String.fromCharCode(53,0) || injuryX != String.fromCharCode(72,0)) {
      tooltipsb += `${3 << (Math.min(2, langQ.length))}`;
   }
   for (let i = 0; i < 1; i++) {
       let applicationG = 2.0;
       let pingr = 2;
       let predictionwins = 5.0;
       let skipC = true;
       let chatK: Map<any, any> = new Map([[String.fromCharCode(112,104,111,110,101,95,52,95,51,51,0),67], [String.fromCharCode(106,95,54,95,100,101,114,105,118,101,0),580]]);
         pingr &= parseInt(`${predictionwins}`) * pingr;
       let bangj = 2;
         skipC = applicationG >= 14.2;
      for (let b = 0; b < 3; b++) {
          let kickl = String.fromCharCode(114,100,98,120,95,120,95,55,56,0);
          let goalG: Map<any, any> = new Map([[String.fromCharCode(99,95,54,53,95,115,116,121,112,0),String.fromCharCode(112,95,56,48,95,99,117,100,97,0)], [String.fromCharCode(116,104,114,101,101,115,116,97,116,101,95,106,95,57,52,0),String.fromCharCode(113,95,49,57,95,99,116,105,109,101,115,116,97,109,112,0)], [String.fromCharCode(99,95,55,95,116,105,109,101,103,109,0),String.fromCharCode(111,95,50,56,95,110,115,112,97,99,101,115,0)]]);
          let mbbannerL = String.fromCharCode(104,100,108,114,95,122,95,57,57,0);
         chatK = new Map([[`${goalG.size}`, pingr]]);
         kickl = `${kickl.length + 2}`;
         goalG.set(kickl, kickl.length % (Math.max(3, mbbannerL.length)));
         mbbannerL = `${kickl.length / 1}`;
      }
         applicationG += bangj >> (Math.min(Math.abs(3), 2));
         bangj &= 1 << (Math.min(Math.abs(bangj), 3));
       let injury2: Map<any, any> = new Map([[String.fromCharCode(100,95,52,50,95,99,121,112,114,101,115,115,0),714], [String.fromCharCode(112,95,50,55,95,99,111,114,100,122,0),272], [String.fromCharCode(101,95,57,53,95,114,108,105,110,101,0),93]]);
          let ynewinterstitialI = String.fromCharCode(105,110,100,105,99,97,116,101,100,95,114,95,52,55,0);
         applicationG -= parseInt(`${predictionwins}`);
         ynewinterstitialI += `${ynewinterstitialI.length}`;
         predictionwins *= pingr;
       let libfbjniQ = String.fromCharCode(97,100,100,120,95,107,95,55,0);
      for (let y = 0; y < 1; y++) {
          let commentV = String.fromCharCode(121,95,52,54,95,119,101,98,112,97,103,101,115,0);
          let previewD = false;
          let skiph = String.fromCharCode(97,95,52,57,95,111,112,101,114,97,116,105,111,110,115,0);
          let middleL: Map<any, any> = new Map([[String.fromCharCode(115,111,114,116,101,100,95,49,95,49,0),701], [String.fromCharCode(106,115,116,121,112,101,95,100,95,57,54,0),706]]);
          let firebasej: Map<any, any> = new Map([[String.fromCharCode(114,116,114,101,101,99,104,101,99,107,95,117,95,53,49,0),136], [String.fromCharCode(113,95,56,95,114,101,99,111,118,101,114,0),645]]);
         bangj <<= Math.min(5, Math.abs(parseInt(`${predictionwins}`) % 1));
         commentV += `${2 + middleL.size}`;
         previewD = 5 >= skiph.length;
         skiph += `${((previewD ? 4 : 3) % (Math.max(middleL.size, 5)))}`;
         firebasej.set(`${previewD}`, 2);
      }
          let floater7: Map<any, any> = new Map([[String.fromCharCode(110,95,52,95,98,97,99,107,112,116,114,0),false ], [String.fromCharCode(105,95,57,50,95,101,108,101,109,101,110,116,115,0),true ]]);
          let pushU: Map<any, any> = new Map([[String.fromCharCode(109,97,115,107,113,95,57,95,54,0),255], [String.fromCharCode(111,109,112,111,115,101,95,48,95,53,53,0),269]]);
         bangj >>= Math.min(Math.abs((1 | (skipC ? 1 : 5))), 4);
         floater7.set(`${floater7.size}`, 3);
         pushU = new Map([[`${pushU.size}`, floater7.size]]);
         injury2.set(`${chatK.size}`, chatK.size);
         chatK.set(`${applicationG}`, parseInt(`${predictionwins}`));
         injury2.set(`${bangj}`, chatK.size >> (Math.min(1, Math.abs(bangj))));
      loginsuccessC += `${chatK.size / (Math.max(injuryX.length, 4))}`;
   }
       let actionm = String.fromCharCode(99,106,112,101,103,95,105,95,50,50,0);
       let configurev = 4.0;
       let bellV = 0.0;
         actionm += `${actionm.length >> (Math.min(Math.abs(3), 3))}`;
         actionm = `${parseInt(`${configurev}`) & actionm.length}`;
          let searchbarh = String.fromCharCode(105,95,49,95,104,107,100,102,0);
          let mergerx = String.fromCharCode(120,112,101,114,105,109,101,110,116,97,108,95,102,95,51,52,0);
          let libglogA = 2.0;
         bellV -= (String.fromCharCode(103,0) == mergerx ? mergerx.length : parseInt(`${libglogA}`));
         searchbarh += `${searchbarh.length}`;
      for (let j = 0; j < 1; j++) {
         configurev /= Math.max(parseFloat(`${parseInt(`${bellV}`) << (Math.min(Math.abs(parseInt(`${configurev}`)), 3))}`), 1);
      }
      let flagX = 5456709.0 >= configurev;
      do {
          let e_imageo = 1;
          let nalyticsu = 3.0;
         configurev -= parseFloat(`${parseInt(`${configurev}`) * 1}`);
         e_imageo %= Math.max(4, parseInt(`${nalyticsu}`) >> (Math.min(1, Math.abs(e_imageo))));
         nalyticsu /= Math.max(parseFloat(`${parseInt(`${nalyticsu}`) | e_imageo}`), 3);
         if (flagX) {
            break;
         }
      } while (flagX && (5.72 < (1.21 * configurev) || 4.55 < (bellV - 1.21)));
         configurev *= parseFloat(`${1}`);
         configurev *= (parseFloat(`${String.fromCharCode(107,0) == actionm ? parseInt(`${bellV}`) : actionm.length}`));
      let whistleorangeZ = bellV >= 8312023.0;
      do {
         bellV -= parseInt(`${bellV}`) >> (Math.min(4, Math.abs(3)));
         if (whistleorangeZ) {
            break;
         }
      } while (((2.46 * bellV) == 3.52 && (configurev * 2.46) == 5.66) && whistleorangeZ);
      while (4.30 < (1.50 - configurev) || 3 < (5 >> (Math.min(4, actionm.length)))) {
         actionm = `${(actionm == String.fromCharCode(54,0) ? actionm.length : parseInt(`${configurev}`))}`;
         break;
      }
      subtextF /= Math.max((String.fromCharCode(83,0) == injuryX ? injuryX.length : loginsuccessC.length), 3);
   if ((subtextF / (Math.max(mail8.size, 7))) > 2.18 || 2 > (mail8.size % 1)) {
      subtextF *= 1 * injuryX.length;
   }
   for (let k = 0; k < 1; k++) {
       let nativemoduleL = String.fromCharCode(115,97,102,97,114,121,95,55,95,56,52,0);
       let d_countv = 5.0;
       let dependencyV = String.fromCharCode(103,95,50,55,95,99,111,109,112,97,99,116,0);
       let libimagepipelinef = 0.0;
       let macauB = String.fromCharCode(119,95,57,50,95,100,101,99,98,110,0);
      if ((macauB.length & 3) == 3 || 2.97 == (libimagepipelinef / 3.52)) {
         libimagepipelinef *= parseFloat(`${macauB.length}`);
      }
         d_countv += dependencyV.length;
      let descW = 8628958.0 <= libimagepipelinef;
      do {
          let dangerL: Map<any, any> = new Map([[String.fromCharCode(97,95,54,50,95,118,101,114,116,115,0),String.fromCharCode(116,95,57,50,95,115,117,98,106,0)], [String.fromCharCode(101,95,52,54,95,98,101,97,116,105,110,103,0),String.fromCharCode(115,95,57,55,95,111,117,112,117,116,0)], [String.fromCharCode(105,95,51,51,95,101,110,99,111,100,101,100,102,114,97,109,101,0),String.fromCharCode(100,95,57,53,95,109,105,110,102,0)]]);
          let previewL = String.fromCharCode(115,117,98,106,95,112,95,48,0);
          let resultd = 5;
          let alertP = 3.0;
         libimagepipelinef -= parseFloat(`${1 | parseInt(`${d_countv}`)}`);
         dangerL = new Map([[`${resultd}`, parseInt(`${alertP}`)]]);
         previewL += "2";
         resultd *= resultd;
         alertP += parseFloat(`${3}`);
         if (descW) {
            break;
         }
      } while ((libimagepipelinef >= 1.42) && descW);
      if (3.69 >= (d_countv / 5.16)) {
          let macauf: Array<any> = [142, 665, 110];
          let listL: Array<any> = [735, 292];
          let libflipperY: Array<any> = [723, 681, 971];
         d_countv /= Math.max(2, (String.fromCharCode(51,0) == dependencyV ? dependencyV.length : parseInt(`${libimagepipelinef}`)));
         macauf = [1];
         listL = [libflipperY.length];
         libflipperY.push(listL.length);
      }
         d_countv += (String.fromCharCode(81,0) == dependencyV ? dependencyV.length : parseInt(`${libimagepipelinef}`));
       let yellow9 = String.fromCharCode(100,95,52,51,95,111,112,116,105,111,110,97,108,0);
       let pause6 = String.fromCharCode(105,95,57,53,95,104,109,97,99,105,100,0);
          let componentregistryL = String.fromCharCode(97,95,50,51,95,105,110,115,117,102,102,105,99,105,101,110,116,0);
         libimagepipelinef -= parseFloat(`${nativemoduleL.length - componentregistryL.length}`);
          let submitc = 1;
          let crown9 = 4.0;
          let pangleQ = 1;
         dependencyV += `${2 >> (Math.min(Math.abs(parseInt(`${d_countv}`)), 3))}`;
         submitc %= Math.max(parseInt(`${crown9}`), 5);
         crown9 -= parseFloat(`${pangleQ}`);
         pangleQ >>= Math.min(3, Math.abs(3 - submitc));
      if (4 > (nativemoduleL.length | 1) || (nativemoduleL.length / (Math.max(4, parseInt(`${libimagepipelinef}`)))) > 1) {
         nativemoduleL = "3";
      }
         yellow9 = `${yellow9.length}`;
      while (3 <= pause6.length) {
          let dependencyi: Map<any, any> = new Map([[String.fromCharCode(99,95,49,53,95,115,116,97,114,116,105,110,103,0),356], [String.fromCharCode(110,95,52,57,95,112,115,110,114,104,118,115,0),623], [String.fromCharCode(102,95,49,50,95,116,121,112,101,99,111,100,101,0),534]]);
          let thumbnail_ = 4;
          let spinnerp = String.fromCharCode(121,95,52,49,95,119,111,114,100,108,101,110,0);
         pause6 += "3";
         dependencyi.set(spinnerp, spinnerp.length % (Math.max(5, thumbnail_)));
         thumbnail_ %= Math.max(thumbnail_ | 3, 2);
         break;
      }
      for (let u = 0; u < 3; u++) {
         d_countv *= 2;
      }
      let merger1 = nativemoduleL.length <= 9204946;
      do {
         nativemoduleL = `${1 + yellow9.length}`;
         if (merger1) {
            break;
         }
      } while (merger1 && (libimagepipelinef == parseFloat(`${nativemoduleL.length}`)));
      while ((dependencyV.length / (Math.max(5, 4))) == 2 && (d_countv - 1.35) == 2.18) {
         d_countv /= Math.max(4, parseInt(`${libimagepipelinef}`) * 1);
         break;
      }
         yellow9 += `${(String.fromCharCode(79,0) == nativemoduleL ? nativemoduleL.length : pause6.length)}`;
      spinners.push(1 & parseInt(`${libimagepipelinef}`));
   }
       let defaultplayerimg8 = String.fromCharCode(119,101,105,103,104,116,120,95,103,95,54,52,0);
       let transfer4 = 5.0;
       let annerd = 5.0;
      while ((3 << (Math.min(3, defaultplayerimg8.length))) >= 3 && 3 >= (parseInt(`${annerd}`) / (Math.max(defaultplayerimg8.length, 2)))) {
          let light3 = 5;
          let actionsE = String.fromCharCode(115,111,109,101,95,107,95,56,50,0);
          let const_ls7 = String.fromCharCode(115,116,117,110,95,111,95,55,52,0);
          let forma: Map<any, any> = new Map([[String.fromCharCode(108,95,52,55,95,99,108,105,101,110,116,0),false ], [String.fromCharCode(114,101,97,99,116,105,111,110,95,111,95,54,55,0),false ], [String.fromCharCode(114,95,55,52,95,101,114,114,111,114,99,98,0),false ]]);
          let proxy0 = String.fromCharCode(104,105,103,104,108,105,103,104,116,105,110,103,95,106,95,48,0);
         annerd += parseFloat(`${const_ls7.length / (Math.max(7, defaultplayerimg8.length))}`);
         light3 /= Math.max(4, 3 << (Math.min(3, Math.abs(forma.size))));
         actionsE += "3";
         const_ls7 = "2";
         forma = new Map([[proxy0, proxy0.length]]);
         break;
      }
      if ((2 & defaultplayerimg8.length) < 4 || (annerd / 5.32) < 4.59) {
         defaultplayerimg8 += `${parseInt(`${annerd}`)}`;
      }
          let package_qkx: Map<any, any> = new Map([[String.fromCharCode(118,95,52,57,95,100,105,103,101,115,116,0),434], [String.fromCharCode(109,109,97,112,95,115,95,49,57,0),759]]);
          let boot3 = 2.0;
          let countdownr = String.fromCharCode(100,95,57,49,95,100,119,111,114,100,0);
         transfer4 -= package_qkx.size ^ 2;
         package_qkx.set(`${boot3}`, countdownr.length & 3);
         boot3 /= Math.max(parseFloat(`${countdownr.length | parseInt(`${boot3}`)}`), 3);
      if (defaultplayerimg8.includes(`${annerd}`)) {
          let textinputO: Array<any> = [String.fromCharCode(105,99,111,110,115,95,57,95,56,56,0), String.fromCharCode(111,95,54,55,95,99,111,112,121,109,0), String.fromCharCode(108,95,55,57,95,115,112,105,108,108,115,105,122,101,0)];
         defaultplayerimg8 = `${parseInt(`${annerd}`) << (Math.min(Math.abs(parseInt(`${transfer4}`)), 1))}`;
         textinputO = [textinputO.length % 3];
      }
          let relatedw: Map<any, any> = new Map([[String.fromCharCode(103,97,112,115,95,97,95,51,53,0),String.fromCharCode(115,117,112,112,111,114,116,95,109,95,50,54,0)], [String.fromCharCode(110,95,54,55,95,115,105,103,110,108,101,0),String.fromCharCode(114,101,113,117,101,115,116,97,98,108,101,95,105,95,52,49,0)], [String.fromCharCode(101,95,54,53,95,117,110,99,108,97,109,112,101,100,0),String.fromCharCode(109,95,55,54,95,109,111,118,105,101,0)]]);
          let typingL: Array<any> = [64, 875, 640];
          let remindert = 3.0;
         defaultplayerimg8 += "3";
         relatedw.set(`${remindert}`, parseInt(`${remindert}`));
         typingL = [1];
      if (defaultplayerimg8.endsWith(`${annerd}`)) {
          let a_countf = String.fromCharCode(115,107,105,112,115,95,106,95,56,48,0);
         defaultplayerimg8 += `${parseInt(`${annerd}`)}`;
         a_countf = `${a_countf.length ^ 2}`;
      }
          let matche = String.fromCharCode(114,95,55,57,95,112,114,105,110,116,111,117,116,0);
          let selectedu = String.fromCharCode(109,115,122,104,95,51,95,51,51,0);
         transfer4 += parseInt(`${transfer4}`) >> (Math.min(selectedu.length, 3));
         matche = `${matche.length - matche.length}`;
         selectedu = `${1 % (Math.max(7, matche.length))}`;
      if (1.89 == (annerd + 3.67) && (annerd + 3.67) == 2.80) {
         transfer4 /= Math.max(3, 4);
      }
      for (let j = 0; j < 3; j++) {
         transfer4 += (defaultplayerimg8 == String.fromCharCode(52,0) ? parseInt(`${annerd}`) : defaultplayerimg8.length);
      }
      loginsuccessC += `${parseInt(`${annerd}`) / (Math.max(1, 9))}`;
   let bootsplashu = 5343945 >= spinners.length;
   do {
      spinners.push(tickf.length % (Math.max(injuryX.length, 10)));
      if (bootsplashu) {
         break;
      }
   } while (bootsplashu && (4 <= (tickf.length << (Math.min(Math.abs(1), 5)))));
    setRefreshing(false);
  };
  const refreshUserState = async () => {
       let plusV = true;
    let signinupP = 1.0;
    let webviewb = String.fromCharCode(109,97,120,120,95,98,95,53,0);
    let tooltipsO = 5;
    let switch_qM = String.fromCharCode(109,111,122,106,112,101,103,95,57,95,53,57,0);
    let giflivestreaminge: Map<any, any> = new Map([[String.fromCharCode(100,101,115,116,95,112,95,57,49,0),657], [String.fromCharCode(115,101,114,118,101,114,95,101,95,52,48,0),734]]);
    let emptyl = 0.0;
    let materialq = String.fromCharCode(101,118,117,116,105,108,95,97,95,54,53,0);
    let notificationfillemptye: Map<any, any> = new Map([[String.fromCharCode(121,95,56,52,95,121,117,118,99,111,110,102,105,103,105,109,97,103,101,0),false ], [String.fromCharCode(97,112,112,114,111,120,105,109,97,116,105,111,110,95,112,95,55,57,0),true ], [String.fromCharCode(103,95,53,51,95,115,108,105,100,105,110,103,0),true ]]);
    let malaysia_ = String.fromCharCode(118,95,56,56,95,115,101,101,100,101,100,0);
    let componentsj = 1.0;
   for (let c = 0; c < 1; c++) {
      malaysia_ += "1";
   }
   for (let c = 0; c < 1; c++) {
      giflivestreaminge = new Map([[materialq, materialq.length]]);
   }
   for (let i = 0; i < 1; i++) {
       let volumeP = 2.0;
       let submitv = false;
       let predictionarrowX = String.fromCharCode(103,101,110,101,114,105,99,115,95,52,95,49,51,0);
       let yellowvideoliveI = 4.0;
       let stringsp = String.fromCharCode(119,105,101,110,101,114,95,114,95,50,51,0);
      for (let k = 0; k < 2; k++) {
          let bodanl = String.fromCharCode(114,101,103,95,113,95,54,57,0);
         predictionarrowX += `${stringsp.length - 2}`;
         bodanl = `${1 - bodanl.length}`;
      }
       let jingdongI = String.fromCharCode(111,110,121,120,95,49,95,52,57,0);
      for (let v = 0; v < 2; v++) {
         jingdongI = `${((submitv ? 3 : 3) + parseInt(`${volumeP}`))}`;
      }
      if (4 > (parseInt(`${yellowvideoliveI}`) / 5) && (yellowvideoliveI / 3.3) > 5.16) {
          let components6 = true;
          let favicona = String.fromCharCode(103,95,57,49,95,114,101,119,114,105,116,101,114,0);
          let sigmobi: Map<any, any> = new Map([[String.fromCharCode(109,95,54,50,95,118,115,102,114,97,109,101,0),280], [String.fromCharCode(97,97,99,112,115,121,95,103,95,57,48,0),497], [String.fromCharCode(108,95,49,54,95,102,108,97,115,104,115,118,0),976]]);
          let constantsv = String.fromCharCode(111,95,57,51,95,103,101,111,116,97,103,115,0);
         yellowvideoliveI *= (parseFloat(`${(components6 ? 5 : 3) | parseInt(`${yellowvideoliveI}`)}`));
         components6 = sigmobi.size < 81;
         favicona += `${sigmobi.size ^ constantsv.length}`;
         constantsv = `${favicona.length}`;
      }
      let yellowredcardn = 9550436.0 <= volumeP;
      do {
          let actionl = false;
          let libimagepipelinec = 4;
         volumeP /= Math.max(predictionarrowX.length >> (Math.min(Math.abs(3), 2)), 5);
         actionl = libimagepipelinec <= 68;
         libimagepipelinec /= Math.max(2 % (Math.max(libimagepipelinec, 8)), 1);
         if (yellowredcardn) {
            break;
         }
      } while (yellowredcardn && (4 > (jingdongI.length * 2) || (volumeP - 4.61) > 4.65));
      let configurep = yellowvideoliveI >= 9899305.0;
      do {
         yellowvideoliveI += parseFloat(`${jingdongI.length}`);
         if (configurep) {
            break;
         }
      } while (configurep && (yellowvideoliveI >= 2.38 && 2.90 >= (yellowvideoliveI * 2.38)));
          let memberA = String.fromCharCode(103,108,97,115,115,95,112,95,49,57,0);
          let libhermesR = 1.0;
          let main_zx = String.fromCharCode(108,95,50,51,95,110,111,116,101,0);
         yellowvideoliveI -= parseFloat(`${parseInt(`${volumeP}`) / 2}`);
         memberA = `${parseInt(`${libhermesR}`)}`;
         libhermesR -= parseFloat(`${1}`);
         main_zx = "2";
       let librrcC: Array<any> = [827, 69, 519];
      let v_titleh = 7396610 <= librrcC.length;
      do {
         librrcC = [2 & parseInt(`${yellowvideoliveI}`)];
         if (v_titleh) {
            break;
         }
      } while ((librrcC.length < predictionarrowX.length) && v_titleh);
         stringsp = `${stringsp.length | 2}`;
      let yingV = submitv ? !submitv : submitv;
      do {
         submitv = (stringsp.length | predictionarrowX.length) == 28;
         if (yingV) {
            break;
         }
      } while ((1 >= stringsp.length) && yingV);
      for (let m = 0; m < 2; m++) {
         jingdongI += `${(String.fromCharCode(73,0) == jingdongI ? parseInt(`${volumeP}`) : jingdongI.length)}`;
      }
       let package_br: Array<any> = [515, 264, 145];
       let awayteamfieldf: Array<any> = [520, 706, 736];
         librrcC = [((submitv ? 1 : 4) % (Math.max(1, 2)))];
       let libyoga_ = String.fromCharCode(114,95,50,49,95,97,115,115,105,103,110,0);
      malaysia_ = `${giflivestreaminge.size + materialq.length}`;
   }
       let rulesT = String.fromCharCode(101,109,98,101,100,100,105,110,103,95,56,95,56,53,0);
       let liveS = String.fromCharCode(114,101,109,111,116,101,108,121,95,51,95,50,56,0);
         liveS = `${rulesT.length}`;
      for (let i = 0; i < 3; i++) {
         rulesT = `${(rulesT == String.fromCharCode(57,0) ? liveS.length : rulesT.length)}`;
      }
          let private_l6: Map<any, any> = new Map([[String.fromCharCode(100,95,51,57,95,119,101,97,107,0),false ], [String.fromCharCode(112,95,57,54,95,97,112,112,114,101,99,97,116,105,111,110,104,111,117,114,0),true ]]);
          let pressureg = String.fromCharCode(117,110,114,101,97,100,95,100,95,54,57,0);
          let libyogaM: Map<any, any> = new Map([[String.fromCharCode(119,105,112,101,95,118,95,49,57,0),String.fromCharCode(99,104,97,110,103,101,100,104,95,105,95,56,55,0)], [String.fromCharCode(116,105,109,101,99,111,100,101,95,120,95,49,52,0),String.fromCharCode(117,110,116,114,117,115,116,101,100,95,122,95,49,49,0)]]);
         liveS = `${liveS.length % 1}`;
         private_l6 = new Map([[`${private_l6.size}`, 1]]);
         pressureg = `${3 - libyogaM.size}`;
         libyogaM.set(`${pressureg}`, pressureg.length / (Math.max(9, private_l6.size)));
      let fastforwardU = 7851905 <= liveS.length;
      do {
         liveS += `${rulesT.length}`;
         if (fastforwardU) {
            break;
         }
      } while (fastforwardU && (liveS.length == 5));
      let sinat = liveS == String.fromCharCode(120,119,101,117,108,0);
      do {
         liveS = "3";
         if (sinat) {
            break;
         }
      } while ((liveS != String.fromCharCode(68,0) || rulesT == String.fromCharCode(71,0)) && sinat);
          let text2 = 2;
          let settingsM = 5;
         liveS += `${3 - liveS.length}`;
         text2 *= settingsM;
         settingsM |= settingsM & text2;
      giflivestreaminge = new Map([[`${giflivestreaminge.size}`, 2]]);
      materialq += `${tooltipsO << (Math.min(2, Math.abs(1)))}`;
      malaysia_ = "3";

    const result = await wwIconscheduleColors.getUserDetails();

      materialq += `${switch_qM.length}`;
      malaysia_ += `${malaysia_.length}`;
      webviewb = `${malaysia_.length / (Math.max(5, parseInt(`${componentsj}`)))}`;
   if (materialq.includes(`${giflivestreaminge.size}`)) {
      materialq = `${parseInt(`${componentsj}`) % (Math.max(3, malaysia_.length))}`;
   }
   while (3 == (3 % (Math.max(8, tooltipsO)))) {
      tooltipsO <<= Math.min(1, Math.abs(parseInt(`${componentsj}`)));
      break;
   }
      signinupP += parseFloat(`${3}`);
    if (result == null) {

   for (let g = 0; g < 1; g++) {
       let previewv = String.fromCharCode(120,95,55,57,95,122,111,110,101,105,110,102,111,0);
          let awayteamfieldw = String.fromCharCode(115,117,114,102,97,99,101,115,95,108,95,49,53,0);
          let floaterC = String.fromCharCode(98,105,119,101,105,103,104,116,95,51,95,57,51,0);
         previewv += `${previewv.length}`;
         awayteamfieldw = `${awayteamfieldw.length + 2}`;
         floaterC = `${floaterC.length + awayteamfieldw.length}`;
      for (let h = 0; h < 3; h++) {
         previewv += `${previewv.length}`;
      }
      for (let m = 0; m < 3; m++) {
          let roundj = true;
          let t_managerw = 1.0;
          let m_managerR = String.fromCharCode(115,95,53,95,112,116,114,109,97,112,115,0);
         previewv = `${2 & previewv.length}`;
         roundj = m_managerR.length >= 52;
         t_managerw /= Math.max(3, 2 << (Math.min(3, m_managerR.length)));
      }
      giflivestreaminge = new Map([[`${notificationfillemptye.size}`, notificationfillemptye.size ^ materialq.length]]);
   }
      giflivestreaminge.set(`${tooltipsO}`, webviewb.length - 2);
   while (2.36 > emptyl || 2.75 > (emptyl * 2.36)) {
      plusV = (signinupP - tooltipsO) >= 12;
      break;
   }
      signinupP *= parseFloat(`${tooltipsO ^ 3}`);
   if (materialq != String.fromCharCode(67,0) || switch_qM == String.fromCharCode(89,0)) {
       let loadinga = String.fromCharCode(115,108,105,99,101,99,111,110,116,101,120,116,95,110,95,49,0);
      for (let x = 0; x < 3; x++) {
         loadinga += `${loadinga.length}`;
      }
          let actionsk: Array<any> = [10, 928];
          let typesn = String.fromCharCode(115,116,114,116,97,103,95,102,95,51,50,0);
         loadinga = "2";
         actionsk = [typesn.length];
         typesn += `${typesn.length + actionsk.length}`;
      while (loadinga.length < 1) {
         loadinga = "3";
         break;
      }
      switch_qM += `${(String.fromCharCode(83,0) == webviewb ? switch_qM.length : webviewb.length)}`;
   }
   while ((4.78 / (Math.max(4, signinupP))) <= 1.73 || (signinupP / (Math.max(parseFloat(`${notificationfillemptye.size}`), 10))) <= 4.78) {
       let fcdaebecbcbafcdfceaaeccfeacdbf = String.fromCharCode(108,95,51,51,95,112,114,101,104,101,97,116,0);
      for (let j = 0; j < 2; j++) {
         fcdaebecbcbafcdfceaaeccfeacdbf = `${fcdaebecbcbafcdfceaaeccfeacdbf.length / 2}`;
      }
         fcdaebecbcbafcdfceaaeccfeacdbf += `${fcdaebecbcbafcdfceaaeccfeacdbf.length}`;
      while (!fcdaebecbcbafcdfceaaeccfeacdbf.endsWith(fcdaebecbcbafcdfceaaeccfeacdbf)) {
          let bridgea = true;
          let trash0 = true;
          let predictionactiveF: Map<any, any> = new Map([[String.fromCharCode(114,95,54,57,95,102,117,115,101,0),97], [String.fromCharCode(112,95,55,57,95,118,109,115,108,0),181], [String.fromCharCode(99,111,114,101,95,119,95,52,0),18]]);
         fcdaebecbcbafcdfceaaeccfeacdbf += `${(String.fromCharCode(51,0) == fcdaebecbcbafcdfceaaeccfeacdbf ? (trash0 ? 1 : 3) : fcdaebecbcbafcdfceaaeccfeacdbf.length)}`;
         bridgea = predictionactiveF.size <= 71 && !bridgea;
         trash0 = ((predictionactiveF.size << (Math.min(3, Math.abs((!bridgea ? predictionactiveF.size : 38))))) <= 43);
         break;
      }
      signinupP -= (parseFloat(`${parseInt(`${componentsj}`) & (plusV ? 5 : 2)}`));
      break;
   }
      return;
    }

    await dispatch(updateUserAuth(result));

   if (4 > (tooltipsO - 1)) {
       let feedbackL: Array<any> = [845, 947];
       let libzeusz = 2.0;
      while (feedbackL.length >= parseInt(`${libzeusz}`)) {
          let eventsplashb = 0.0;
          let iconmoreB = String.fromCharCode(114,101,102,101,114,101,110,99,101,95,97,95,50,49,0);
          let holderS: Array<any> = [String.fromCharCode(97,110,99,104,111,114,95,54,95,54,55,0), String.fromCharCode(112,97,114,116,95,108,95,49,49,0)];
          let x_titler = 5.0;
          let disconnectedlogoD: Array<any> = [802, 353, 27];
         libzeusz /= Math.max(4, iconmoreB.length);
         eventsplashb /= Math.max(3, 2);
         iconmoreB += `${holderS.length}`;
         holderS.push(1 << (Math.min(Math.abs(parseInt(`${x_titler}`)), 2)));
         x_titler /= Math.max(holderS.length, 2);
         disconnectedlogoD.push(holderS.length * 2);
         break;
      }
      for (let x = 0; x < 3; x++) {
         feedbackL = [2 >> (Math.min(2, feedbackL.length))];
      }
       let sources: Map<any, any> = new Map([[String.fromCharCode(97,97,99,116,97,98,95,104,95,51,48,0),23], [String.fromCharCode(97,100,108,101,114,95,113,95,55,56,0),528], [String.fromCharCode(109,111,98,105,117,115,95,120,95,50,52,0),964]]);
       let robotoN: Map<any, any> = new Map([[String.fromCharCode(117,98,108,105,99,95,50,95,57,54,0),553], [String.fromCharCode(100,95,55,54,95,115,116,117,102,102,0),916]]);
          let sheet8 = String.fromCharCode(109,98,97,102,102,95,54,95,54,51,0);
          let networkT = false;
         feedbackL.push(feedbackL.length);
         sheet8 = `${sheet8.length}`;
         networkT = sheet8.length < 35;
      let frame_oJ = 5080170 <= sources.size;
      do {
          let terms1 = 5;
          let register_yE = 3.0;
          let watch7: Map<any, any> = new Map([[String.fromCharCode(115,97,110,100,98,111,120,95,117,95,52,53,0),434], [String.fromCharCode(102,95,49,52,95,100,105,115,112,97,116,99,104,105,110,103,0),682]]);
          let dependencyh = false;
          let rootU = String.fromCharCode(120,95,50,57,95,97,118,112,105,99,116,117,114,101,0);
         sources = new Map([[`${dependencyh}`, 2]]);
         terms1 ^= 2;
         register_yE /= Math.max(watch7.size, 4);
         watch7 = new Map([[`${watch7.size}`, parseInt(`${register_yE}`)]]);
         dependencyh = terms1 > 68;
         rootU += `${rootU.length << (Math.min(1, Math.abs(watch7.size)))}`;
         if (frame_oJ) {
            break;
         }
      } while ((3 == (4 & sources.size) && (sources.size * libzeusz) == 2.20) && frame_oJ);
       let navigationX = String.fromCharCode(115,116,114,105,110,103,95,112,95,55,53,0);
       let liveSX = String.fromCharCode(115,116,105,112,112,101,100,95,109,95,54,0);
      tooltipsO |= 1 >> (Math.min(5, Math.abs(tooltipsO)));
   }
   while (switch_qM != String.fromCharCode(67,0) && malaysia_.length < 2) {
      switch_qM = `${webviewb.length}`;
      break;
   }
   while (2 >= (webviewb.length + tooltipsO)) {
      tooltipsO -= parseInt(`${emptyl}`) + webviewb.length;
      break;
   }
   for (let a = 0; a < 3; a++) {
       let fastforwardF = false;
       let loginf = 3;
          let nexth = 3.0;
          let loginsuccessL = 5.0;
         fastforwardF = loginsuccessL <= 96.44;
         nexth *= 3 / (Math.max(parseInt(`${nexth}`), 7));
         loginsuccessL -= parseInt(`${nexth}`) * 3;
      let controls1 = loginf >= 7903985;
      do {
         loginf += 2;
         if (controls1) {
            break;
         }
      } while ((loginf <= 3) && controls1);
          let righte = 0.0;
         loginf *= ((fastforwardF ? 5 : 4) % (Math.max(1, parseInt(`${righte}`))));
      for (let u = 0; u < 1; u++) {
          let iconpipexpandY = String.fromCharCode(104,95,51,51,95,112,111,115,105,116,105,118,101,0);
          let rootn = String.fromCharCode(108,111,103,100,98,95,57,95,54,56,0);
          let profileH = 5.0;
          let twitterZ: Map<any, any> = new Map([[String.fromCharCode(115,104,97,114,105,110,103,95,57,95,51,48,0),61], [String.fromCharCode(107,95,53,54,95,116,111,111,108,98,97,114,115,0),265], [String.fromCharCode(108,95,49,48,95,117,110,109,97,112,0),536]]);
          let holderI: Map<any, any> = new Map([[String.fromCharCode(99,95,54,51,0),870], [String.fromCharCode(121,95,52,55,95,98,97,99,107,101,100,0),711], [String.fromCharCode(120,95,56,56,95,103,97,105,110,99,0),88]]);
         fastforwardF = rootn.length == 71;
         iconpipexpandY += `${parseInt(`${profileH}`)}`;
         rootn += `${twitterZ.size}`;
         profileH += parseFloat(`${holderI.size * twitterZ.size}`);
         holderI.set(`${iconpipexpandY}`, holderI.size);
      }
      if ((5 << (Math.min(1, Math.abs(loginf)))) <= 5) {
          let rewardvideot = 0.0;
          let grayS = String.fromCharCode(108,115,112,100,108,112,99,95,107,95,56,55,0);
         loginf >>= Math.min(4, Math.abs((loginf | (fastforwardF ? 4 : 5))));
         rewardvideot += parseFloat(`${grayS.length}`);
         grayS = "2";
      }
         fastforwardF = 99 > loginf;
      signinupP *= parseFloat(`${materialq.length / (Math.max(1, 7))}`);
   }
   while (1 < (4 >> (Math.min(1, switch_qM.length)))) {
      signinupP += (parseFloat(`${(plusV ? 1 : 4) + parseInt(`${componentsj}`)}`));
      break;
   }
       let redgoal9 = 1;
       let navigationi: Array<any> = [793, 899];
       let fastforwardz = String.fromCharCode(111,98,106,101,99,116,115,95,48,95,54,54,0);
         fastforwardz = `${navigationi.length}`;
         navigationi = [1];
         fastforwardz = "1";
      let largew = 6250645 <= redgoal9;
      do {
          let statistics7 = String.fromCharCode(101,95,56,51,95,101,120,112,105,114,121,0);
          let goalH = String.fromCharCode(97,108,108,112,97,115,115,95,121,95,49,48,48,0);
          let themea = String.fromCharCode(115,95,56,56,95,122,114,101,111,114,100,101,114,0);
          let android6 = String.fromCharCode(106,115,105,109,100,99,102,103,95,119,95,55,52,0);
         redgoal9 *= (String.fromCharCode(69,0) == themea ? themea.length : statistics7.length);
         statistics7 += `${goalH.length}`;
         goalH += `${3 << (Math.min(2, android6.length))}`;
         android6 = `${(String.fromCharCode(105,0) == goalH ? goalH.length : android6.length)}`;
         if (largew) {
            break;
         }
      } while ((fastforwardz.endsWith(`${redgoal9}`)) && largew);
      for (let r = 0; r < 1; r++) {
         navigationi = [fastforwardz.length];
      }
      componentsj += parseFloat(`${giflivestreaminge.size * 1}`);

    return;
  };

  useEffect(() => {
    

    let mergedArray;

    if (userState.user?.userUpline.created_at != '') {
      mergedArray = userState.user?.userInvitedUserList.concat(userState.user.userUpline);
    } else {
      mergedArray = userState.user.userInvitedUserList;
    }

    mergedArray.sort((a: any, b: any) => {
      let dateA: any;
      dateA = new Date(a.created_at);
      let dateB: any;

      dateB = new Date(b.created_at);
      return dateB - dateA;
    });

    mergedArray = mergedArray.map((item: any) => {
      let displayText = '';
      if (item.text) {
        displayText = item.text;
        item.invited_vip_reward_day = item.vip_reward_day;
      } else {
        displayText = item.title ;
      }

      return { ...item, displayText: displayText };
    });
    console.log('mergedArray');
    console.log(mergedArray);
    setInviteList(mergedArray);
  }, []);

  return (
    <ScreenContainer>
      <TitleWithBackButtonHeader title="累计奖励明细" />
      <ScrollView
        showsVerticalScrollIndicator={false}
        refreshControl={
          <RefreshControl refreshing={refreshing} onRefresh={handleRefresh} />
        }>
        { }
        <LinearGradient
          colors={['#323638', '#1a1d20']} 
          locations={[0.1, 1]}
          start={{ x: 1, y: 0 }} 
          end={{ x: 0, y: 1 }} 
          style={{
            marginTop: 40,
            paddingHorizontal: 20,
            paddingVertical: 10,
            borderRadius: 15,
            flexDirection: 'row', 
            justifyContent: 'space-between',
            alignItems: 'center',
            minHeight: 120,
          }}>
          <View style={styles.container}>
            { }
            <Text style={styles.title}>{userState.user?.userTotalInvite}</Text>
            <Text style={styles.subTitle}>已邀请人数</Text>
          </View>

          <View style={styles.container}>
            { }
            <Text style={styles.title}>
              {userState.user?.userAccumulateRewardDay}
            </Text>
            <Text style={styles.subTitle}>累计获得天数</Text>
          </View>
        </LinearGradient>

        { }
        <View style={{ marginTop: 40, alignItems: 'center' }}>
          <Text
            style={{ ...textVariants.header, fontSize: 16, fontWeight: '700' }}>
            邀请记录
          </Text>

          {inviteList.length > 0 && (
            <View
              style={{
                marginTop: 20,
                marginBottom: 40,
                backgroundColor: '#1D2023',
                borderRadius: 15,
                overflow: 'hidden',
                width: '100%',
              }}>
              {inviteList.map(item => (
                <View
                  key={item.displayText}
                  style={{
                    flexDirection: 'row',
                    justifyContent: 'space-between',
                    alignContent: 'center',
                    paddingHorizontal: 20,
                    paddingVertical: 15,
                  }}>
                  <View style={{ flex: 1, paddingRight: 10 }}>
                    <Text
                      style={{ fontSize: 16, color: '#fff' }}
                      numberOfLines={1}
                      ellipsizeMode="tail">
                      {item.displayText}
                    </Text>
                    <Text style={{ color: '#9c9c9c' }}>{item.created_at}</Text>
                  </View>

                  <View
                    style={{
                      justifyContent: 'center',
                      alignContent: 'center',
                    }}>
                    <Text
                      style={{
                        color: colors.primary,
                        fontSize: 16,
                        flex: 1,
                      }}>
                      +{item.invited_vip_reward_day}天
                    </Text>
                  </View>
                </View>
              ))}
            </View>
          )}

          {inviteList.length == 0 && (
            <View
              style={{
                marginTop: 20,
                marginBottom: 40,
                backgroundColor: '#1D2023',
                borderRadius: 15,
                overflow: 'hidden',
                width: '100%',
              }}>
              <View
                style={{
                  flexDirection: 'row',

                  alignContent: 'center',
                  paddingHorizontal: 20,
                  paddingVertical: 20,
                }}>
                <View>
                  <Text style={{ color: '#fff' }}>暂时无邀请记录</Text>
                </View>
              </View>
            </View>
          )}
        </View>
      </ScrollView>
    </ScreenContainer>
  );
};

const styles = StyleSheet.create({
  loading: {
    flexDirection: 'row',
    justifyContent: 'center',
    flex: 1,
  },
  backgroundImage: {
    flex: 1, 
    resizeMode: 'cover', 
  },
  container: {
    flex: 1,
    justifyContent: 'space-between',
    alignItems: 'flex-start',
    paddingLeft: 25,
  },
  title: {
    color: '#fff',
    fontSize: 26,
    fontWeight: '900',
  },
  subTitle: {
    fontSize: 16,
    fontWeight: '400',
    color: '#9C9C9C',
  },
});
