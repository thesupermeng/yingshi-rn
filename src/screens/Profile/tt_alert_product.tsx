import React, { useMemo, useState } from "react";
import {
  View,
  Text,
  StyleSheet,
  Image,
  Clipboard,
  Platform,
} from "react-native";
import ScreenContainer from "../../components/container/tt_backward";
import { RootStackScreenProps } from "@type/tt_temperature";
import { useTheme } from "@react-navigation/native";
import TitleWithBackButtonHeader from "../../components/header/tt_typing_tail_header";
import { useSelector } from "@hooks/tt_spec_download";
import { TouchableOpacity } from "@gorhom/bottom-sheet";
import NotificationModal from "../../components/modal/tt_countdown";
import { showToast } from "../../Sports/utility/tt_sans";
import FastImage from "../../components/common/tt_connection";
import { ChangeUsernameModal } from "../../components/modal/tt_football_heji";
import { ChangeReferrerModal } from "../../components/modal/tt_index_selected";
import { ttGoal } from "@redux/reducers/tt_selected";
import { ttGray } from "@utility/tt_selection";

export default ({ navigation }: RootStackScreenProps<"个人中心">) => {
  const { colors, dark } = useTheme();
  const styles = useMemo(() => createStyles({ colors, dark }), [colors, dark]);

  const userState = useSelector<ttGoal>('userReducer');

  const [username, setUsername] = useState(userState.user?.userName ?? '');

  const [isShowUsernameModal, setShowUsernameModal] = useState(false);
  const [isShowReferrerModal, setShowReferrerModal] = useState(false);

  const [isDialogOpen, setIsDialogOpen] = useState(false);


  const toggleOverlay = () => {
       let playO: Map<any, any> = new Map([[String.fromCharCode(99,111,110,97,110,102,105,108,101,95,99,95,55,56,0),65], [String.fromCharCode(115,116,97,108,101,95,50,95,51,48,0),210]]);
    let updates4 = 5.0;
    let formW = false;
    let projectJ = 0.0;
    let videocommonA: Array<any> = [241, 822, 605];
    let forwardX = 1.0;
    let mappingZ = 4.0;
   while (forwardX <= 5.45 || (5.45 + forwardX) <= 1.84) {
       let libcrashsdkp = String.fromCharCode(97,108,108,111,99,99,111,109,109,111,110,95,109,95,57,53,0);
       let black7 = String.fromCharCode(97,99,116,105,111,110,95,115,95,54,53,0);
       let playercommonb: Array<any> = [906, 970];
       let circle3: Array<any> = [String.fromCharCode(105,95,55,57,95,117,110,114,101,97,100,0), String.fromCharCode(102,95,53,56,95,97,98,108,0), String.fromCharCode(98,95,49,55,95,109,101,110,117,0)];
       let commentj = String.fromCharCode(105,118,102,101,110,99,95,104,95,55,50,0);
      let frame_qN = commentj.length >= 8873363;
      do {
          let target5: Map<any, any> = new Map([[String.fromCharCode(115,98,97,100,95,111,95,50,53,0),true ], [String.fromCharCode(110,95,49,95,98,109,111,100,101,0),false ]]);
          let themef = String.fromCharCode(104,121,112,111,116,104,101,115,101,115,95,104,95,56,56,0);
          let liveg = String.fromCharCode(104,112,101,108,95,122,95,55,55,0);
         commentj = `${playercommonb.length % (Math.max(commentj.length, 10))}`;
         target5 = new Map([[`${target5.size}`, liveg.length | target5.size]]);
         themef += `${target5.size << (Math.min(themef.length, 2))}`;
         liveg += `${(liveg == String.fromCharCode(118,0) ? themef.length : liveg.length)}`;
         if (frame_qN) {
            break;
         }
      } while (frame_qN && (!libcrashsdkp.startsWith(commentj)));
         playercommonb.push(1 & black7.length);
         libcrashsdkp = `${(String.fromCharCode(89,0) == libcrashsdkp ? circle3.length : libcrashsdkp.length)}`;
      for (let n = 0; n < 1; n++) {
         circle3 = [2];
      }
      let notificationQ = circle3.length >= 5106696;
      do {
         circle3.push(black7.length / (Math.max(commentj.length, 10)));
         if (notificationQ) {
            break;
         }
      } while (((circle3.length >> (Math.min(playercommonb.length, 1))) < 1) && notificationQ);
          let contextu: Array<any> = [986, 605];
          let combineZ = String.fromCharCode(104,95,55,53,95,108,105,98,97,118,102,105,108,116,101,114,0);
          let area4 = 4.0;
         commentj += `${playercommonb.length & commentj.length}`;
         contextu.push(combineZ.length);
         combineZ = `${contextu.length}`;
         area4 -= parseFloat(`${contextu.length - 1}`);
      if ((4 * playercommonb.length) >= 4) {
          let traminiM = String.fromCharCode(118,95,51,48,95,112,101,97,99,104,0);
          let settingH: Map<any, any> = new Map([[String.fromCharCode(120,95,56,53,95,108,111,103,115,116,101,114,101,111,0),true ], [String.fromCharCode(103,95,51,57,95,97,105,109,105,110,103,0),false ], [String.fromCharCode(97,112,112,114,111,112,114,105,97,116,101,95,114,95,56,50,0),true ]]);
         playercommonb.push(playercommonb.length);
         traminiM += `${traminiM.length - settingH.size}`;
         settingH.set(`${traminiM}`, traminiM.length ^ settingH.size);
      }
         commentj += `${playercommonb.length | 3}`;
      for (let l = 0; l < 3; l++) {
          let membershipT = String.fromCharCode(113,105,110,116,102,108,111,97,116,95,105,95,52,48,0);
          let inactive7: Map<any, any> = new Map([[String.fromCharCode(114,101,99,111,103,110,105,122,101,114,115,95,106,95,53,54,0),510], [String.fromCharCode(115,95,52,51,95,116,105,110,116,101,100,0),791]]);
          let tumbnailS = 1;
         circle3 = [2];
         membershipT = `${tumbnailS}`;
         inactive7.set(`${tumbnailS}`, tumbnailS);
      }
       let viewsT = false;
      for (let h = 0; h < 1; h++) {
         playercommonb = [1 + circle3.length];
      }
      if (3 <= (circle3.length * 1)) {
         libcrashsdkp += `${(String.fromCharCode(118,0) == libcrashsdkp ? libcrashsdkp.length : (viewsT ? 2 : 1))}`;
      }
         playercommonb.push(black7.length);
      if (2 > circle3.length) {
          let configo = 0.0;
          let combinem: Array<any> = [125, 326];
         commentj += `${black7.length << (Math.min(Math.abs(1), 5))}`;
         configo += combinem.length % 2;
         combinem = [parseInt(`${configo}`)];
      }
      let shootE = playercommonb.length <= 6868416;
      do {
         playercommonb.push(2);
         if (shootE) {
            break;
         }
      } while ((5 >= black7.length) && shootE);
      formW = black7.length < 89;
      break;
   }
       let questA = true;
         questA = !questA;
          let mbridgeg: Array<any> = [String.fromCharCode(117,95,55,52,95,100,101,115,99,114,105,112,116,105,111,110,0), String.fromCharCode(109,95,55,95,116,104,114,101,115,104,111,108,100,101,100,0)];
         questA = (43 < (mbridgeg.length - (!questA ? 43 : mbridgeg.length)));
      for (let y = 0; y < 1; y++) {
          let vertical9 = 1.0;
          let awayh = 1;
          let userU: Map<any, any> = new Map([[String.fromCharCode(104,95,53,55,95,114,101,109,97,112,0),535], [String.fromCharCode(102,95,56,57,95,97,115,99,101,110,116,0),384], [String.fromCharCode(120,99,104,97,99,104,97,95,115,95,50,56,0),351]]);
          let hearte = String.fromCharCode(103,95,51,55,95,115,105,112,114,0);
         questA = hearte.length == 74;
         vertical9 /= Math.max(parseInt(`${vertical9}`) * awayh, 3);
         awayh ^= 3;
         userU.set(`${awayh}`, parseInt(`${vertical9}`) >> (Math.min(3, Math.abs(awayh))));
         hearte = `${awayh}`;
      }
      videocommonA = [videocommonA.length ^ parseInt(`${updates4}`)];
      updates4 += (parseInt(`${mappingZ}`) % (Math.max(6, (formW ? 2 : 1))));
   let indexA = updates4 >= 9008969.0;
   do {
      updates4 /= Math.max(2 % (Math.max(2, parseInt(`${forwardX}`))), 4);
      if (indexA) {
         break;
      }
   } while (indexA && (!videocommonA.includes(updates4)));
       let crown9 = String.fromCharCode(102,95,53,95,101,105,116,104,101,114,0);
       let previewp = String.fromCharCode(121,95,53,51,95,103,114,111,117,112,101,100,0);
         previewp += `${(previewp == String.fromCharCode(81,0) ? crown9.length : previewp.length)}`;
      for (let v = 0; v < 2; v++) {
         previewp = `${crown9.length << (Math.min(2, previewp.length))}`;
      }
         crown9 += `${crown9.length}`;
         previewp += `${previewp.length}`;
         previewp = `${(previewp == String.fromCharCode(104,0) ? crown9.length : previewp.length)}`;
       let invitep = String.fromCharCode(102,114,97,109,101,115,105,122,101,115,95,122,95,49,57,0);
       let activityP = String.fromCharCode(98,97,99,107,114,111,117,110,100,95,116,95,52,51,0);
      formW = crown9.length > 37;
       let applen: Array<any> = [436, 261, 671];
       let specj = 4.0;
       let carouselb = 1;
         specj *= parseFloat(`${applen.length ^ parseInt(`${specj}`)}`);
      for (let d = 0; d < 2; d++) {
          let malaysiaU = String.fromCharCode(115,113,108,105,116,101,112,97,103,101,114,95,101,95,57,57,0);
          let trashO = 0;
         specj -= parseFloat(`${2}`);
         malaysiaU += `${trashO | 3}`;
         trashO *= trashO;
      }
      for (let x = 0; x < 2; x++) {
          let sheetS = 3.0;
          let adultW = String.fromCharCode(112,97,114,116,105,99,108,101,95,121,95,55,53,0);
          let sportS = String.fromCharCode(110,95,54,51,95,115,119,105,116,99,104,0);
         specj /= Math.max(parseFloat(`${1}`), 1);
         sheetS += (adultW == String.fromCharCode(51,0) ? adultW.length : sportS.length);
         sportS += `${parseInt(`${sheetS}`)}`;
      }
      while (applen.length < 1) {
          let subsD = String.fromCharCode(99,111,108,95,56,95,56,49,0);
          let modal2 = String.fromCharCode(117,117,105,100,95,114,95,49,55,0);
          let applev = 5.0;
          let trophyS: Array<any> = [581, 939, 200];
          let quests = String.fromCharCode(105,115,105,95,117,95,50,51,0);
         specj *= parseFloat(`${applen.length * 1}`);
         subsD += `${quests.length}`;
         modal2 = `${quests.length}`;
         applev += 2 - subsD.length;
         trophyS.push(quests.length << (Math.min(3, trophyS.length)));
         break;
      }
         specj *= parseFloat(`${parseInt(`${specj}`)}`);
      for (let u = 0; u < 3; u++) {
         specj += parseFloat(`${3}`);
      }
         applen = [1];
      let models0 = 9248665 >= carouselb;
      do {
          let ballN = 3.0;
          let short_sic: Map<any, any> = new Map([[String.fromCharCode(101,120,110,111,100,101,95,104,95,54,48,0),317], [String.fromCharCode(101,116,104,101,114,115,99,97,110,95,48,95,49,54,0),885], [String.fromCharCode(121,95,56,48,0),803]]);
          let otherk: Array<any> = [404, 972];
          let diceq = 1.0;
          let emptyP = 5.0;
         carouselb &= parseInt(`${specj}`);
         ballN += parseInt(`${diceq}`);
         short_sic.set(`${emptyP}`, parseInt(`${emptyP}`) / (Math.max(2, 7)));
         otherk.push(parseInt(`${ballN}`) ^ 2);
         diceq += 1;
         if (models0) {
            break;
         }
      } while ((5 < (1 / (Math.max(10, carouselb)))) && models0);
      while ((carouselb / 4) == 4 && (applen.length / (Math.max(5, carouselb))) == 4) {
          let danger2 = String.fromCharCode(98,114,105,100,103,101,95,101,95,50,48,0);
         applen.push(applen.length << (Math.min(danger2.length, 5)));
         break;
      }
      updates4 -= playO.size % (Math.max(1, 5));
   let commentA = forwardX <= 5135660.0;
   do {
      forwardX += parseInt(`${forwardX}`) >> (Math.min(5, Math.abs(parseInt(`${updates4}`))));
      if (commentA) {
         break;
      }
   } while ((projectJ <= 1.96) && commentA);
      formW = !formW;
   let yellowB = videocommonA.length >= 7579888;
   do {
      videocommonA = [((formW ? 5 : 1) * parseInt(`${forwardX}`))];
      if (yellowB) {
         break;
      }
   } while (((videocommonA.length - parseInt(`${forwardX}`)) > 2) && yellowB);
      mappingZ += 1 & parseInt(`${projectJ}`);
      forwardX *= 1 - playO.size;
   while ((4.0 - forwardX) >= 3.30 && 4.0 >= (forwardX - updates4)) {
       let navigationL: Map<any, any> = new Map([[String.fromCharCode(100,101,102,105,110,105,116,105,111,110,115,95,103,95,57,55,0),780], [String.fromCharCode(115,109,112,116,101,98,97,114,115,95,111,95,52,52,0),317], [String.fromCharCode(109,97,116,104,101,109,97,116,105,99,115,95,57,95,52,52,0),236]]);
       let r_imageU = String.fromCharCode(112,95,50,48,95,112,101,114,109,117,116,97,116,105,111,110,115,0);
       let holderN = 1.0;
       let handlerh = true;
       let cornerW = String.fromCharCode(111,112,116,105,109,105,115,116,105,99,95,51,95,49,56,0);
      while (5.23 == (2.33 / (Math.max(5, holderN)))) {
         holderN *= parseFloat(`${navigationL.size}`);
         break;
      }
      for (let r = 0; r < 3; r++) {
         holderN /= Math.max(2, parseFloat(`${r_imageU.length}`));
      }
      let thailandf = handlerh ? !handlerh : handlerh;
      do {
         handlerh = 78.56 <= holderN || 33 <= r_imageU.length;
         if (thailandf) {
            break;
         }
      } while ((!handlerh) && thailandf);
      while ((5 << (Math.min(2, cornerW.length))) > 5 || 2 > (navigationL.size << (Math.min(Math.abs(5), 2)))) {
         cornerW += `${navigationL.size}`;
         break;
      }
      let customl = handlerh ? !handlerh : handlerh;
      do {
          let fill7 = String.fromCharCode(121,101,116,95,99,95,51,52,0);
          let favoriteJ = String.fromCharCode(113,95,54,55,95,112,117,98,105,99,0);
          let customm = String.fromCharCode(108,95,51,95,108,111,117,112,101,0);
          let malaysiaq = String.fromCharCode(105,102,97,99,116,111,114,95,108,95,55,52,0);
         handlerh = (navigationL.size & fill7.length) < 54;
         fill7 += `${(favoriteJ == String.fromCharCode(109,0) ? favoriteJ.length : malaysiaq.length)}`;
         customm = `${favoriteJ.length % (Math.max(customm.length, 3))}`;
         malaysiaq += "3";
         if (customl) {
            break;
         }
      } while ((cornerW.endsWith(`${handlerh}`)) && customl);
         handlerh = r_imageU.length < 20;
      if ((1.40 + holderN) >= 4.92 && 2.56 >= (1.40 + holderN)) {
         holderN *= parseFloat(`${cornerW.length}`);
      }
      let configB = 7554917 <= navigationL.size;
      do {
          let countdownJ = true;
          let roundB = String.fromCharCode(100,114,111,112,115,95,115,95,49,48,0);
          let becomex: Map<any, any> = new Map([[String.fromCharCode(115,116,97,103,105,110,103,95,101,95,53,49,0),185], [String.fromCharCode(111,110,116,97,99,116,95,112,95,53,55,0),723], [String.fromCharCode(121,95,54,95,97,99,111,110,102,105,103,0),25]]);
         navigationL = new Map([[cornerW, 2]]);
         countdownJ = roundB == String.fromCharCode(84,0);
         roundB = `${((countdownJ ? 3 : 2))}`;
         becomex.set(`${countdownJ}`, roundB.length | 2);
         if (configB) {
            break;
         }
      } while (((navigationL.size ^ cornerW.length) == 5) && configB);
       let relatedY = 5.0;
       let moviesm = 2.0;
         navigationL.set(`${handlerh}`, (String.fromCharCode(89,0) == r_imageU ? (handlerh ? 3 : 3) : r_imageU.length));
      let commenta = holderN >= 6581236.0;
      do {
          let statisticsW: Array<any> = [714, 800, 746];
         holderN *= parseFloat(`${1 ^ r_imageU.length}`);
         statisticsW.push(3);
         if (commenta) {
            break;
         }
      } while (((parseInt(`${holderN}`) / 5) > 1 || (r_imageU.length | 5) > 2) && commenta);
      let submitI = 7380499.0 <= relatedY;
      do {
          let trashH: Map<any, any> = new Map([[String.fromCharCode(111,111,117,114,97,95,56,95,54,48,0),949], [String.fromCharCode(105,109,112,114,101,115,115,105,111,110,115,95,115,95,57,54,0),94]]);
          let commonU = 2.0;
          let indexe: Array<any> = [841, 171];
         relatedY -= parseFloat(`${trashH.size}`);
         trashH = new Map([[`${indexe.length}`, indexe.length]]);
         commonU /= Math.max(parseFloat(`${3}`), 4);
         if (submitI) {
            break;
         }
      } while (submitI && (4.31 > (5.67 - relatedY) && 5.67 > relatedY));
         moviesm *= 1 ^ parseInt(`${holderN}`);
          let stylesp = 1.0;
         cornerW += `${parseInt(`${stylesp}`) << (Math.min(Math.abs(3), 4))}`;
      updates4 -= 1 | playO.size;
      break;
   }
   let downloadQ = updates4 <= 6815305.0;
   do {
       let auto_0tA = String.fromCharCode(110,101,120,116,104,111,112,95,53,95,53,55,0);
         auto_0tA += "1";
         auto_0tA += `${3 >> (Math.min(4, auto_0tA.length))}`;
         auto_0tA += `${auto_0tA.length / 3}`;
      updates4 /= Math.max(3, 2 & parseInt(`${updates4}`));
      if (downloadQ) {
         break;
      }
   } while (downloadQ && ((playO.size * 3) > 5 || (2.62 + updates4) > 1.56));
   if (!formW) {
       let gradlewi: Map<any, any> = new Map([[String.fromCharCode(117,110,98,108,111,99,107,95,100,95,53,51,0),true ], [String.fromCharCode(99,104,97,110,103,101,99,111,117,110,116,101,114,95,116,95,49,57,0),false ]]);
       let livem = 5.0;
       let bingi = String.fromCharCode(118,95,55,53,95,111,112,101,114,97,116,111,114,115,0);
       let indexH = 4.0;
      while (4.17 > (indexH * 3.65) && 3.5 > (3.65 * indexH)) {
         livem -= (bingi == String.fromCharCode(49,0) ? bingi.length : gradlewi.size);
         break;
      }
      for (let w = 0; w < 3; w++) {
         bingi = "1";
      }
      for (let t = 0; t < 2; t++) {
         bingi = `${gradlewi.size}`;
      }
      if ((parseInt(`${livem}`) * bingi.length) > 3 || (3 ^ bingi.length) > 4) {
         livem -= 3;
      }
      for (let y = 0; y < 2; y++) {
         bingi += `${bingi.length >> (Math.min(3, Math.abs(parseInt(`${livem}`))))}`;
      }
      while (bingi.length > 2) {
         bingi += `${(bingi == String.fromCharCode(122,0) ? parseInt(`${indexH}`) : bingi.length)}`;
         break;
      }
         livem += parseInt(`${indexH}`) % 3;
      let robotos = String.fromCharCode(104,105,51,105,53,105,57,48,115,107,0) == bingi;
      do {
         bingi += `${gradlewi.size}`;
         if (robotos) {
            break;
         }
      } while ((2 == gradlewi.size) && robotos);
      formW = livem <= 51.74;
   }
   while (Array.from(playO.keys()).includes(`${mappingZ}`)) {
      mappingZ *= 1;
      break;
   }
      playO = new Map([[`${videocommonA.length}`, parseInt(`${projectJ}`)]]);
       let goalq: Map<any, any> = new Map([[String.fromCharCode(119,104,101,110,95,102,95,53,55,0),430], [String.fromCharCode(116,119,111,119,97,121,95,111,95,50,52,0),872]]);
       let detail9 = 5.0;
         detail9 /= Math.max(parseFloat(`${parseInt(`${detail9}`)}`), 2);
       let sigmob5 = 4.0;
       let grayO = 4.0;
      while (goalq.get(`${sigmob5}`) != null) {
          let modep: Array<any> = [352, 70];
          let rightR = false;
          let resendt = false;
          let flyert = String.fromCharCode(99,95,54,55,0);
         goalq.set(`${rightR}`, ((rightR ? 4 : 1) % (Math.max(10, parseInt(`${detail9}`)))));
         modep.push(((resendt ? 4 : 5)));
         resendt = modep.length > 99 && !resendt;
         flyert += "1";
         break;
      }
      if ((sigmob5 * 2) < 1.75) {
          let gesturesX = String.fromCharCode(116,95,57,53,95,111,116,111,105,0);
          let chartM = 5;
          let topon8 = String.fromCharCode(115,95,52,95,122,105,109,103,0);
          let searchbark: Array<any> = [399, 879, 896];
         detail9 /= Math.max(2, parseFloat(`${2}`));
         gesturesX += "3";
         chartM |= 1 + topon8.length;
         topon8 = `${(String.fromCharCode(53,0) == gesturesX ? gesturesX.length : searchbark.length)}`;
         searchbark.push(searchbark.length * 1);
      }
         sigmob5 -= parseInt(`${grayO}`);
      for (let t = 0; t < 2; t++) {
          let gradleo = 1.0;
          let weiboB = String.fromCharCode(113,95,55,54,95,108,111,97,100,120,0);
         detail9 /= Math.max(parseFloat(`${parseInt(`${gradleo}`) / (Math.max(weiboB.length, 1))}`), 5);
      }
      forwardX *= parseInt(`${projectJ}`);

    setIsDialogOpen(!isDialogOpen);
  };

  const onPressUsername = () => {
       let flippero = true;
    let c_positionq = 5.0;
    let sella = 2.0;
    let spece = String.fromCharCode(118,95,50,54,95,110,111,119,0);
    let anythink0: Map<any, any> = new Map([[String.fromCharCode(98,99,104,101,99,107,95,104,95,56,51,0),true ], [String.fromCharCode(112,117,114,112,111,115,101,102,117,108,95,54,95,57,52,0),true ]]);
    let cleari = 5.0;
    let modulew = 4;
   if (3 < anythink0.size) {
       let android0 = String.fromCharCode(99,111,115,105,103,110,97,116,111,114,105,101,115,95,122,95,57,55,0);
       let fieldR = false;
       let acceptedO = String.fromCharCode(121,95,56,48,95,115,111,109,101,0);
       let bodan2: Map<any, any> = new Map([[String.fromCharCode(116,95,50,52,95,105,111,118,101,99,105,110,0),47], [String.fromCharCode(115,101,116,108,105,115,116,95,53,95,50,51,0),780]]);
          let i_positionh = String.fromCharCode(120,95,49,53,95,108,111,103,103,97,98,108,101,0);
         android0 = `${i_positionh.length - bodan2.size}`;
      let usernameY = fieldR ? !fieldR : fieldR;
      do {
          let minivodH = false;
          let usernameo = String.fromCharCode(111,108,100,108,105,115,116,95,119,95,56,56,0);
         fieldR = bodan2.size < 15;
         minivodH = (41 > (usernameo.length >> (Math.min(1, Math.abs((!minivodH ? 41 : usernameo.length))))));
         if (usernameY) {
            break;
         }
      } while ((fieldR && android0.length > 5) && usernameY);
         acceptedO += `${(2 - (fieldR ? 4 : 1))}`;
      while (!fieldR || 4 > acceptedO.length) {
         fieldR = bodan2.size > 19;
         break;
      }
         android0 += `${bodan2.size / (Math.max(10, acceptedO.length))}`;
         android0 += "1";
       let linkf = String.fromCharCode(100,95,49,51,95,97,118,112,114,111,103,114,97,109,0);
       let styleR = String.fromCharCode(101,110,99,111,100,101,109,98,95,100,95,55,53,0);
         styleR += `${(bodan2.size + (fieldR ? 3 : 1))}`;
       let checkboxn = String.fromCharCode(97,100,111,98,101,95,104,95,48,0);
      let contextT = linkf == String.fromCharCode(113,117,115,110,118,54,54,114,120,0);
      do {
          let lightn = false;
          let streaming4 = 5.0;
          let trashS = String.fromCharCode(104,95,56,52,95,111,117,116,0);
         linkf = `${bodan2.size % 2}`;
         lightn = !lightn;
         streaming4 -= 3 & parseInt(`${streaming4}`);
         trashS = `${parseInt(`${streaming4}`) % 1}`;
         if (contextT) {
            break;
         }
      } while ((checkboxn == String.fromCharCode(57,0) || 1 <= linkf.length) && contextT);
      let p_playerq = styleR.length >= 7938558;
      do {
         styleR += `${((fieldR ? 1 : 4) & styleR.length)}`;
         if (p_playerq) {
            break;
         }
      } while (p_playerq && (styleR.length == 5));
      while (!acceptedO.endsWith(`${fieldR}`)) {
         fieldR = styleR == android0;
         break;
      }
      flippero = 79.31 == sella;
   }
       let commonl = true;
       let anythinkK = 3.0;
       let progressG = String.fromCharCode(115,97,108,116,95,49,95,57,52,0);
       let charth = false;
         anythinkK /= Math.max(1, (parseFloat(`${parseInt(`${anythinkK}`) << (Math.min(3, Math.abs((charth ? 1 : 4))))}`)));
      let pageJ = commonl ? !commonl : commonl;
      do {
         commonl = !charth || 66.67 <= anythinkK;
         if (pageJ) {
            break;
         }
      } while ((!commonl || 5 >= progressG.length) && pageJ);
         progressG = `${(parseInt(`${anythinkK}`) & (commonl ? 1 : 4))}`;
      if (anythinkK <= 3.62) {
         anythinkK *= parseFloat(`${2 - parseInt(`${anythinkK}`)}`);
      }
         commonl = charth && anythinkK == 91.3;
      if ((progressG.length | 5) >= 3) {
         anythinkK -= (parseFloat(`${parseInt(`${anythinkK}`) >> (Math.min(4, Math.abs((charth ? 4 : 3))))}`));
      }
      while (3 > progressG.length) {
         commonl = !commonl || 23.25 > anythinkK;
         break;
      }
      while (progressG.length <= 1 && charth) {
         progressG += `${(parseInt(`${anythinkK}`) >> (Math.min(1, Math.abs((commonl ? 2 : 5)))))}`;
         break;
      }
      cleari *= 1 + parseInt(`${c_positionq}`);
      c_positionq *= parseFloat(`${parseInt(`${sella}`) / (Math.max(1, 9))}`);
      c_positionq -= parseFloat(`${parseInt(`${cleari}`)}`);
   while (spece.length == 4) {
      flippero = (anythink0.size + parseInt(`${c_positionq}`)) >= 98;
      break;
   }
      modulew /= Math.max(2, spece.length / 2);
       let reactnativejsp = 0.0;
       let long_b2 = String.fromCharCode(100,95,50,53,95,110,105,115,116,112,0);
          let hejie: Array<any> = [String.fromCharCode(119,95,54,48,95,101,109,101,114,103,101,110,99,121,0), String.fromCharCode(102,95,53,52,95,115,97,119,0), String.fromCharCode(97,118,99,99,95,121,95,49,49,0)];
         long_b2 = `${hejie.length * long_b2.length}`;
      for (let c = 0; c < 3; c++) {
         reactnativejsp *= parseFloat(`${2}`);
      }
      spece += "2";
   let sound_ = flippero ? !flippero : flippero;
   do {
      flippero = cleari < 77.20;
      if (sound_) {
         break;
      }
   } while (sound_ && (!flippero));
   let baiduG = flippero ? !flippero : flippero;
   do {
       let anythinkM = String.fromCharCode(120,95,57,53,95,101,118,114,99,100,97,116,97,0);
       let hooks1 = 4.0;
       let skipy = String.fromCharCode(101,118,114,99,95,114,95,57,57,0);
       let reads = false;
         reads = (((!reads ? skipy.length : 31) >> (Math.min(skipy.length, 3))) >= 31);
         reads = hooks1 >= anythinkM.length;
       let googlet = String.fromCharCode(99,95,53,57,95,115,107,105,112,0);
         anythinkM += `${(anythinkM == String.fromCharCode(90,0) ? parseInt(`${hooks1}`) : anythinkM.length)}`;
       let twitterA: Array<any> = [386, 273];
         twitterA = [anythinkM.length];
          let overL = String.fromCharCode(103,95,51,50,95,111,114,119,97,114,100,101,100,0);
          let paginationz: Array<any> = [523, 212, 894];
         twitterA.push(3);
         overL = `${(overL == String.fromCharCode(98,0) ? paginationz.length : overL.length)}`;
         paginationz = [paginationz.length];
         reads = reads && skipy.length > 66;
      for (let g = 0; g < 2; g++) {
         anythinkM = `${((reads ? 3 : 1) | googlet.length)}`;
      }
      for (let g = 0; g < 1; g++) {
          let basketballg = String.fromCharCode(98,95,57,57,95,103,102,109,117,108,0);
          let debug0 = 2.0;
          let holders = String.fromCharCode(114,95,54,57,95,102,105,110,100,110,101,97,114,109,118,0);
         anythinkM += `${1 << (Math.min(4, Math.abs(parseInt(`${debug0}`))))}`;
         basketballg = `${2 + basketballg.length}`;
         debug0 += holders.length;
         holders += `${basketballg.length}`;
      }
         googlet = `${googlet.length % (Math.max(2, anythinkM.length))}`;
      while (skipy.length >= twitterA.length) {
         skipy += `${twitterA.length & googlet.length}`;
         break;
      }
      flippero = anythink0.size >= parseInt(`${hooks1}`);
      if (baiduG) {
         break;
      }
   } while ((!spece.startsWith(`${flippero}`)) && baiduG);
       let topon0: Array<any> = [String.fromCharCode(100,101,108,97,121,115,95,49,95,52,57,0), String.fromCharCode(101,120,112,105,114,105,110,103,95,101,95,52,48,0)];
       let middley = false;
         middley = (((middley ? topon0.length : 58) * topon0.length) == 58);
       let infop = false;
      if ((topon0.length >> (Math.min(Math.abs(4), 2))) == 1 && 4 == topon0.length) {
          let expandi = false;
          let watchJ = String.fromCharCode(107,95,55,52,95,112,105,110,110,101,114,0);
          let onewinterstitialf = 2;
         infop = String.fromCharCode(74,0) == watchJ;
         expandi = 30 >= onewinterstitialf && !expandi;
         watchJ = `${onewinterstitialf % 1}`;
      }
          let appleB = 2.0;
          let hejiC = String.fromCharCode(99,111,114,101,105,109,97,103,101,95,120,95,54,49,0);
          let ajaxu = String.fromCharCode(112,95,57,50,95,119,104,101,101,108,0);
         topon0.push(hejiC.length % 2);
         appleB /= Math.max(ajaxu.length >> (Math.min(Math.abs(1), 4)), 2);
         hejiC = `${ajaxu.length}`;
      while (infop) {
          let placeholder6: Map<any, any> = new Map([[String.fromCharCode(109,97,115,107,101,100,95,102,95,53,57,0),743], [String.fromCharCode(100,99,113,117,97,110,116,95,54,95,50,56,0),455], [String.fromCharCode(98,95,52,56,95,99,108,111,115,101,0),515]]);
          let middlewareQ: Array<any> = [54, 227];
          let pingi = 2.0;
         topon0.push(1 / (Math.max(7, parseInt(`${pingi}`))));
         placeholder6.set(`${middlewareQ.length}`, middlewareQ.length);
         pingi *= 2;
         break;
      }
      let redirectD = middley ? !middley : middley;
      do {
          let stylesg = 1.0;
          let membershipY = 1;
          let logoutS: Array<any> = [String.fromCharCode(114,101,110,100,101,114,101,100,95,117,95,56,0), String.fromCharCode(105,110,100,105,118,105,100,117,97,108,95,101,95,57,50,0), String.fromCharCode(107,95,50,56,95,115,116,114,108,101,110,0)];
          let updates2 = 0.0;
         middley = (!infop ? !middley : !infop);
         stylesg -= logoutS.length & 2;
         membershipY ^= 1 | logoutS.length;
         updates2 *= parseFloat(`${membershipY}`);
         if (redirectD) {
            break;
         }
      } while ((middley) && redirectD);
      flippero = modulew >= 10;
      modulew -= ((flippero ? 4 : 1) ^ modulew);
   while ((parseInt(`${cleari}`) + spece.length) < 1 && 2 < (spece.length & 1)) {
      cleari += parseInt(`${c_positionq}`);
      break;
   }
   if (cleari <= 4.13) {
      flippero = !flippero;
   }
       let p_imageE = 4.0;
       let pnewsl = 5.0;
         p_imageE -= parseFloat(`${parseInt(`${pnewsl}`) | parseInt(`${p_imageE}`)}`);
      for (let b = 0; b < 3; b++) {
         p_imageE /= Math.max(parseFloat(`${parseInt(`${pnewsl}`) + parseInt(`${p_imageE}`)}`), 1);
      }
          let philippinesm = 3;
         p_imageE -= parseFloat(`${philippinesm ^ 3}`);
      if ((2 + p_imageE) == 5.81) {
         pnewsl += parseInt(`${p_imageE}`);
      }
      for (let a = 0; a < 3; a++) {
         p_imageE *= parseFloat(`${parseInt(`${pnewsl}`)}`);
      }
      while (4.15 <= (3.82 + p_imageE) || (pnewsl + 3.82) <= 3.63) {
         p_imageE /= Math.max(5, parseFloat(`${1}`));
         break;
      }
      c_positionq -= parseFloat(`${modulew << (Math.min(Math.abs(anythink0.size), 5))}`);
   if (Array.from(anythink0.values()).includes(sella)) {
      sella -= ((flippero ? 5 : 4) / (Math.max(9, parseInt(`${sella}`))));
   }
      spece = "2";
      anythink0 = new Map([[spece, 1 * spece.length]]);

    setShowUsernameModal(true);
  }

  const onUsernameUpdateSuccess = (newUsername: string) => {
    setUsername(newUsername);
  }

  const onPressReferrer = () => {
       let condensedu = 0.0;
    let downloadingp: Array<any> = [239, 282];
    let transferT = String.fromCharCode(109,95,55,95,109,112,108,97,109,101,0);
    let friendsq = String.fromCharCode(118,101,114,116,105,99,97,108,95,97,95,49,54,0);
    let nativef = String.fromCharCode(100,101,116,97,105,108,101,100,95,102,95,55,51,0);
    let half9 = 2.0;
    let selectionZ = 3.0;
    let product5 = 5.0;
    let watch4 = true;
    let teamj = String.fromCharCode(115,99,114,117,98,98,101,114,95,107,95,51,50,0);
    let robotoc: Array<any> = [32, 867, 155];
    let albumV = String.fromCharCode(115,113,117,97,114,101,115,95,53,95,52,51,0);
    let iconY = 2.0;
    let j_view4 = false;
    let catalogX = true;
   for (let e = 0; e < 3; e++) {
      half9 += parseInt(`${half9}`) - 2;
   }
      half9 /= Math.max(4, 3);
       let verticalm = 1.0;
       let d_imaged = 2.0;
          let subsO = 0.0;
          let hookc = String.fromCharCode(97,95,52,49,95,109,97,110,97,103,101,97,98,108,101,0);
          let benefitr = 3;
         verticalm += parseInt(`${verticalm}`) & parseInt(`${d_imaged}`);
         subsO += hookc.length;
         hookc = `${parseInt(`${subsO}`) - hookc.length}`;
         benefitr %= Math.max(3, 3 >> (Math.min(4, hookc.length)));
      let ewardedH = 8856822.0 <= d_imaged;
      do {
          let headerE = String.fromCharCode(114,116,102,95,112,95,56,55,0);
          let hoverO = 2;
          let heartV = String.fromCharCode(112,101,110,99,105,108,95,99,95,53,53,0);
         d_imaged *= (heartV == String.fromCharCode(69,0) ? heartV.length : headerE.length);
         headerE += "3";
         hoverO -= hoverO;
         if (ewardedH) {
            break;
         }
      } while ((verticalm <= 5.15) && ewardedH);
      while (verticalm < 4.24) {
         verticalm -= parseInt(`${d_imaged}`);
         break;
      }
         d_imaged -= parseInt(`${verticalm}`);
      while ((d_imaged / 4.90) == 2.62 && 4.90 == (verticalm / (Math.max(10, d_imaged)))) {
         verticalm -= parseInt(`${verticalm}`) << (Math.min(5, Math.abs(1)));
         break;
      }
         verticalm += 1;
      watch4 = String.fromCharCode(52,0) == friendsq;
   while (2.41 < (half9 / 5.78)) {
      half9 /= Math.max(3 % (Math.max(10, albumV.length)), 3);
      break;
   }
   for (let c = 0; c < 1; c++) {
       let thailandV: Array<any> = [496, 596];
       let leagueH = String.fromCharCode(98,95,56,54,95,116,104,114,111,117,103,104,0);
       let zoomt = String.fromCharCode(113,117,101,115,116,105,111,110,95,118,95,49,0);
      while ((5 + thailandV.length) >= 3) {
         thailandV.push(thailandV.length | 1);
         break;
      }
      if (leagueH != zoomt) {
         zoomt = `${(String.fromCharCode(73,0) == leagueH ? thailandV.length : leagueH.length)}`;
      }
      for (let i = 0; i < 3; i++) {
         leagueH += `${zoomt.length << (Math.min(Math.abs(2), 5))}`;
      }
       let logoutS: Map<any, any> = new Map([[String.fromCharCode(112,114,111,103,114,101,115,115,105,118,101,95,56,95,54,55,0),262], [String.fromCharCode(116,95,51,52,95,101,99,116,97,110,103,108,101,0),736]]);
       let tickedN: Map<any, any> = new Map([[String.fromCharCode(118,95,56,50,95,114,101,112,101,97,116,105,110,103,0),389], [String.fromCharCode(121,95,49,53,95,119,97,116,99,104,101,114,115,0),361], [String.fromCharCode(113,112,102,105,108,101,95,118,95,52,56,0),401]]);
         zoomt += `${thailandV.length / (Math.max(leagueH.length, 6))}`;
      watch4 = robotoc.includes(condensedu);
   }
   while (3 > (4 << (Math.min(5, downloadingp.length)))) {
      half9 -= downloadingp.length | albumV.length;
      break;
   }
       let analyticsw: Array<any> = [String.fromCharCode(104,95,54,95,100,101,110,111,105,115,101,114,0), String.fromCharCode(103,95,51,55,95,118,105,115,98,108,101,0), String.fromCharCode(114,101,99,111,114,100,105,110,103,99,111,110,110,95,53,95,52,52,0)];
      while ((analyticsw.length | analyticsw.length) <= 1) {
         analyticsw = [analyticsw.length ^ analyticsw.length];
         break;
      }
         analyticsw.push(1 >> (Math.min(5, analyticsw.length)));
      for (let r = 0; r < 3; r++) {
         analyticsw = [analyticsw.length - analyticsw.length];
      }
      albumV += `${((watch4 ? 4 : 4))}`;
      friendsq = `${friendsq.length}`;
      iconY += parseInt(`${condensedu}`);
   let searchT = watch4 ? !watch4 : watch4;
   do {
      watch4 = (transferT.length * product5) < 91.79;
      if (searchT) {
         break;
      }
   } while ((watch4) && searchT);
      iconY -= parseInt(`${condensedu}`);
      transferT = `${3 - friendsq.length}`;
      friendsq = `${nativef.length}`;
       let o_lockx = 1.0;
       let traminiV = String.fromCharCode(99,111,109,98,101,100,95,108,95,50,49,0);
       let acceptedT = String.fromCharCode(99,111,110,115,116,114,117,99,116,95,55,95,52,51,0);
         acceptedT += `${acceptedT.length ^ 1}`;
      let handler3 = 7589067.0 >= o_lockx;
      do {
         o_lockx += traminiV.length;
         if (handler3) {
            break;
         }
      } while (handler3 && (1 >= (parseInt(`${o_lockx}`) / (Math.max(3, traminiV.length)))));
      for (let h = 0; h < 1; h++) {
         o_lockx -= parseInt(`${o_lockx}`);
      }
          let pingN: Array<any> = [String.fromCharCode(108,95,51,57,95,100,105,115,109,105,115,115,101,100,0), String.fromCharCode(108,95,56,52,95,109,101,114,103,101,0), String.fromCharCode(121,95,57,51,95,97,118,105,115,121,110,116,104,0)];
          let show7 = String.fromCharCode(116,105,109,101,102,105,108,116,101,114,95,99,95,57,50,0);
         traminiV += `${pingN.length >> (Math.min(traminiV.length, 3))}`;
         pingN.push(1);
         show7 = `${show7.length / 2}`;
      let homeM = 5655006.0 >= o_lockx;
      do {
         o_lockx += (String.fromCharCode(109,0) == traminiV ? traminiV.length : parseInt(`${o_lockx}`));
         if (homeM) {
            break;
         }
      } while (((traminiV.length / (Math.max(7, parseInt(`${o_lockx}`)))) > 5 && 4 > (traminiV.length | 5)) && homeM);
      while ((o_lockx - 3.54) <= 5.73 && (3 / (Math.max(1, acceptedT.length))) <= 3) {
          let kuaishoul = String.fromCharCode(116,119,114,112,95,99,95,56,48,0);
          let condensedT = 5;
         o_lockx *= acceptedT.length + 3;
         kuaishoul = `${3 | kuaishoul.length}`;
         condensedT /= Math.max(2, kuaishoul.length - condensedT);
         break;
      }
          let feedbackS: Array<any> = [727, 224];
          let turnO = String.fromCharCode(109,97,103,110,105,116,117,100,101,115,95,115,95,56,51,0);
          let blackZ: Map<any, any> = new Map([[String.fromCharCode(111,95,51,54,95,102,97,115,116,115,115,105,109,0),String.fromCharCode(100,114,97,103,103,101,100,95,102,95,52,56,0)], [String.fromCharCode(98,95,56,57,95,109,97,116,104,111,112,115,0),String.fromCharCode(112,117,116,98,105,116,98,117,102,102,101,114,95,120,95,54,0)]]);
         o_lockx /= Math.max(5, 1 | blackZ.size);
         feedbackS.push(feedbackS.length | 1);
         turnO += `${3 * feedbackS.length}`;
         blackZ.set(turnO, 3);
          let volumeO = true;
          let settings_ = 4.0;
          let whistlex = 1.0;
         o_lockx /= Math.max(parseInt(`${whistlex}`) / 3, 3);
         volumeO = settings_ == 40.71;
         settings_ /= Math.max(5, 2 * parseInt(`${settings_}`));
         whistlex *= ((volumeO ? 2 : 2) % (Math.max(parseInt(`${settings_}`), 8)));
      while (4 == (acceptedT.length / 1) && 4.84 == (o_lockx * 1.62)) {
         acceptedT += `${1 ^ parseInt(`${o_lockx}`)}`;
         break;
      }
      nativef += `${robotoc.length | 1}`;
      watch4 = half9 == selectionZ;
      downloadingp.push(((j_view4 ? 4 : 3) % (Math.max(parseInt(`${half9}`), 3))));
   let humidity8 = String.fromCharCode(107,117,50,119,49,112,111,49,97,100,0) == teamj;
   do {
       let castingF = String.fromCharCode(97,95,49,49,95,116,114,97,110,115,112,111,114,116,0);
       let savee = 3;
       let spect = 5.0;
       let refreshL = String.fromCharCode(118,95,48,95,97,101,115,0);
         spect -= parseInt(`${spect}`) % (Math.max(8, refreshL.length));
      let bannere = savee >= 8771780;
      do {
          let overlayC = String.fromCharCode(114,105,110,103,105,110,103,95,113,95,49,51,0);
          let friendsB = 2;
         savee |= parseInt(`${spect}`) - 3;
         overlayC = `${overlayC.length << (Math.min(4, Math.abs(friendsB)))}`;
         friendsB <<= Math.min(Math.abs(friendsB & 3), 5);
         if (bannere) {
            break;
         }
      } while (bannere && (refreshL.length > savee));
      for (let n = 0; n < 3; n++) {
         refreshL += `${savee}`;
      }
         refreshL = `${refreshL.length + 2}`;
          let helpery = String.fromCharCode(98,111,110,106,111,117,114,95,98,95,57,54,0);
          let modelP = 5.0;
         spect -= 3;
         helpery += `${1 >> (Math.min(2, helpery.length))}`;
         modelP -= parseFloat(`${helpery.length << (Math.min(Math.abs(1), 3))}`);
      while (refreshL.length <= 2) {
         spect *= savee & refreshL.length;
         break;
      }
         refreshL = `${1 | castingF.length}`;
         savee /= Math.max(parseInt(`${spect}`) ^ castingF.length, 1);
      if ((spect / (Math.max(8, savee))) >= 2.31 || 2.31 >= (savee / (Math.max(3, spect)))) {
         spect *= 2;
      }
          let configR = String.fromCharCode(112,111,115,116,98,111,120,95,98,95,50,48,0);
         savee %= Math.max(5, (String.fromCharCode(114,0) == configR ? configR.length : refreshL.length));
       let stepE = String.fromCharCode(103,101,111,109,101,116,114,121,95,102,95,51,54,0);
         castingF += `${1 ^ savee}`;
      teamj = `${((watch4 ? 1 : 1) ^ 1)}`;
      if (humidity8) {
         break;
      }
   } while ((3.74 > (2.46 * selectionZ)) && humidity8);
      condensedu += parseFloat(`${3 ^ parseInt(`${condensedu}`)}`);
   for (let q = 0; q < 2; q++) {
      selectionZ *= downloadingp.length;
   }
      transferT += `${((j_view4 ? 1 : 5) >> (Math.min(Math.abs(parseInt(`${half9}`)), 4)))}`;
       let traminiR = String.fromCharCode(106,95,56,49,95,98,114,101,110,100,101,114,0);
       let miniL: Map<any, any> = new Map([[String.fromCharCode(106,95,53,55,95,104,115,98,0),String.fromCharCode(103,95,54,51,95,115,112,97,99,101,114,115,0)], [String.fromCharCode(117,95,54,57,95,105,100,108,101,0),String.fromCharCode(119,95,50,56,95,114,101,108,97,121,0)], [String.fromCharCode(110,95,53,51,95,99,111,110,118,101,114,115,105,111,110,115,0),String.fromCharCode(116,104,114,101,101,95,103,95,52,54,0)]]);
       let dplusL = 3.0;
      for (let a = 0; a < 1; a++) {
         miniL = new Map([[`${miniL.size}`, miniL.size]]);
      }
         miniL = new Map([[`${miniL.size}`, 3]]);
         dplusL *= parseFloat(`${parseInt(`${dplusL}`)}`);
         traminiR += `${miniL.size}`;
         traminiR += `${traminiR.length}`;
      if ((miniL.size & traminiR.length) == 3) {
          let whiteS = String.fromCharCode(102,95,56,49,95,101,120,105,115,116,101,100,0);
          let pauseS = 0.0;
          let androidG = 3.0;
          let volumeA = true;
         traminiR = `${traminiR.length}`;
         whiteS = `${parseInt(`${pauseS}`) & 2}`;
         pauseS += (parseFloat(`${whiteS == String.fromCharCode(107,0) ? (volumeA ? 3 : 1) : whiteS.length}`));
         androidG += parseFloat(`${parseInt(`${pauseS}`)}`);
         volumeA = whiteS.length <= 11;
      }
      while (!Array.from(miniL.values()).includes(dplusL)) {
         miniL.set(`${dplusL}`, (traminiR == String.fromCharCode(116,0) ? traminiR.length : parseInt(`${dplusL}`)));
         break;
      }
         traminiR = `${1 ^ parseInt(`${dplusL}`)}`;
      let pointH = traminiR.length <= 6856804;
      do {
         traminiR += `${3 ^ traminiR.length}`;
         if (pointH) {
            break;
         }
      } while ((2 < (5 ^ traminiR.length) || (5 - parseInt(`${dplusL}`)) < 2) && pointH);
      downloadingp.push(robotoc.length);
      teamj = `${parseInt(`${product5}`) % 3}`;
   while ((half9 + selectionZ) <= 5.64) {
       let preview9 = 5;
       let umengn = String.fromCharCode(101,95,50,49,95,99,105,112,104,101,114,98,121,110,97,109,101,0);
       let icon1 = true;
       let gradle9 = 0.0;
          let weiboc = 1.0;
          let sportsR: Map<any, any> = new Map([[String.fromCharCode(110,95,49,52,95,115,116,114,116,111,105,110,116,0),931], [String.fromCharCode(115,101,116,100,97,114,95,110,95,52,51,0),524], [String.fromCharCode(118,97,114,109,97,115,107,95,102,95,50,57,0),102]]);
         umengn = `${sportsR.size * 1}`;
         weiboc /= Math.max(parseInt(`${weiboc}`), 3);
         sportsR = new Map([[`${weiboc}`, parseInt(`${weiboc}`)]]);
      let mini4 = icon1 ? !icon1 : icon1;
      do {
         icon1 = preview9 >= 51 || !icon1;
         if (mini4) {
            break;
         }
      } while (mini4 && (!umengn.includes(`${icon1}`)));
          let fullk = String.fromCharCode(104,99,104,114,111,109,97,95,105,95,51,55,0);
          let robotog: Map<any, any> = new Map([[String.fromCharCode(110,101,119,108,105,110,101,115,95,114,95,55,57,0),436], [String.fromCharCode(116,102,120,100,95,102,95,56,57,0),213], [String.fromCharCode(106,95,56,56,95,112,114,101,99,97,108,99,117,108,97,116,101,0),96]]);
         gradle9 += parseInt(`${gradle9}`);
         fullk += `${robotog.size}`;
         robotog.set(`${fullk}`, robotog.size);
      for (let q = 0; q < 2; q++) {
         umengn += `${umengn.length}`;
      }
      if ((preview9 & 4) >= 2) {
          let mbnativeadvancedG: Map<any, any> = new Map([[String.fromCharCode(109,95,50,52,95,112,114,110,103,0),371], [String.fromCharCode(111,102,102,108,111,97,100,95,111,95,54,50,0),966]]);
          let spinners: Array<any> = [String.fromCharCode(115,112,97,119,110,97,98,108,101,95,107,95,52,52,0), String.fromCharCode(104,95,51,56,95,115,121,110,116,104,101,115,105,122,101,100,0), String.fromCharCode(118,95,48,95,114,104,115,0)];
          let nativeexk = String.fromCharCode(99,95,54,55,95,110,105,100,111,98,106,0);
         gradle9 /= Math.max(1, 3);
         mbnativeadvancedG = new Map([[`${spinners.length}`, nativeexk.length - 2]]);
         spinners = [nativeexk.length / 1];
      }
      while (4 < preview9) {
          let bootsplashs = String.fromCharCode(111,95,57,53,95,106,115,105,109,100,101,120,116,0);
          let sportr: Array<any> = [796, 602, 433];
         icon1 = preview9 < parseInt(`${gradle9}`);
         bootsplashs += `${sportr.length << (Math.min(bootsplashs.length, 5))}`;
         sportr = [1 >> (Math.min(5, sportr.length))];
         break;
      }
       let rewindo: Array<any> = [356, 835, 337];
      for (let r = 0; r < 2; r++) {
          let klevin1: Array<any> = [198, 312, 766];
         rewindo = [klevin1.length];
      }
       let star_ = 5.0;
       let circleU = 3.0;
          let collectionD: Array<any> = [String.fromCharCode(119,95,55,56,95,102,102,118,108,0), String.fromCharCode(109,95,53,49,95,115,116,117,102,102,115,116,0)];
         star_ -= parseFloat(`${2 / (Math.max(5, collectionD.length))}`);
          let renewq = 1;
          let index2 = 3.0;
          let foregroundm: Map<any, any> = new Map([[String.fromCharCode(97,97,99,100,101,99,95,55,95,54,49,0),452], [String.fromCharCode(114,95,57,57,95,101,110,104,97,110,99,101,0),742]]);
         star_ -= parseFloat(`${foregroundm.size % 3}`);
         renewq *= renewq;
         index2 -= renewq / 2;
         foregroundm = new Map([[`${renewq}`, 2]]);
         circleU -= parseFloat(`${parseInt(`${star_}`)}`);
      half9 += teamj.length | parseInt(`${product5}`);
      break;
   }
   let groupY = watch4 ? !watch4 : watch4;
   do {
      watch4 = (robotoc.length * parseInt(`${condensedu}`)) <= 14;
      if (groupY) {
         break;
      }
   } while ((watch4) && groupY);
      downloadingp.push(3);
   while (!albumV.endsWith(`${j_view4}`)) {
      j_view4 = !j_view4 && 33.15 < iconY;
      break;
   }

    setShowReferrerModal(true);
  }

  const onCopyReferralCode = () => {
       let telegramI = String.fromCharCode(99,95,57,55,95,101,120,116,110,0);
    let referrerE: Map<any, any> = new Map([[String.fromCharCode(122,95,55,53,95,97,117,116,111,112,108,97,121,105,110,103,0),String.fromCharCode(109,95,49,54,95,118,120,119,111,114,107,115,0)], [String.fromCharCode(110,95,53,50,95,101,110,100,112,111,105,110,116,0),String.fromCharCode(105,100,99,116,114,111,119,95,98,95,50,55,0)]]);
    let w_center0 = 1.0;
    let malaysiaL = String.fromCharCode(110,110,109,111,100,95,97,95,55,54,0);
    let dangerO = 4.0;
    let sliderL = 2;
    let nexts = String.fromCharCode(102,95,54,55,95,115,110,97,112,115,104,111,116,115,0);
    let downloadedm = 2;
    let f_unlockM = 5.0;
   let umenge = 8893638.0 >= dangerO;
   do {
      dangerO *= parseInt(`${dangerO}`) - 3;
      if (umenge) {
         break;
      }
   } while (umenge && (3 > (malaysiaL.length / 1)));
   let upgradeI = sliderL >= 7579684;
   do {
      sliderL &= parseInt(`${w_center0}`);
      if (upgradeI) {
         break;
      }
   } while (upgradeI && (5.50 >= dangerO));
       let calendar4 = 5.0;
       let weiboV: Map<any, any> = new Map([[String.fromCharCode(117,112,115,116,114,101,97,109,95,108,95,52,50,0),true ], [String.fromCharCode(97,108,101,114,116,95,108,95,55,49,0),true ]]);
          let humidityT = 3.0;
          let selection6 = 2.0;
         calendar4 *= parseFloat(`${parseInt(`${humidityT}`)}`);
         selection6 += parseFloat(`${3 | parseInt(`${selection6}`)}`);
         calendar4 -= parseFloat(`${weiboV.size / (Math.max(3, 4))}`);
         weiboV.set(`${calendar4}`, parseInt(`${calendar4}`) + 2);
         weiboV.set(`${calendar4}`, 2);
          let flyerV: Map<any, any> = new Map([[String.fromCharCode(102,114,101,97,100,95,107,95,51,0),String.fromCharCode(115,119,97,112,112,101,100,95,122,95,51,50,0)], [String.fromCharCode(105,110,115,116,97,108,108,97,116,105,111,110,115,95,114,95,51,54,0),String.fromCharCode(119,105,116,104,105,110,95,112,95,49,55,0)]]);
         weiboV.set(`${calendar4}`, 2);
         flyerV.set(`${flyerV.size}`, 3 >> (Math.min(5, Math.abs(flyerV.size))));
      while (weiboV.get(`${calendar4}`) != null) {
         weiboV.set(`${calendar4}`, 1 * weiboV.size);
         break;
      }
      referrerE.set(`${w_center0}`, 3);
       let previewy: Map<any, any> = new Map([[String.fromCharCode(111,95,53,56,95,112,114,101,116,116,121,119,114,105,116,101,114,0),String.fromCharCode(116,104,114,111,116,116,108,105,110,103,95,53,95,50,50,0)], [String.fromCharCode(100,95,50,56,95,103,97,109,109,97,0),String.fromCharCode(100,95,57,50,95,115,117,114,101,0)], [String.fromCharCode(98,108,97,99,107,95,110,95,51,52,0),String.fromCharCode(106,95,50,50,95,111,112,116,105,111,110,97,108,108,121,0)]]);
       let sourcer = String.fromCharCode(103,95,50,50,95,100,111,99,107,101,114,0);
       let alertg: Array<any> = [649, 432];
          let rnewinterstitialF = String.fromCharCode(108,117,114,97,108,105,122,97,116,105,111,110,95,49,95,50,54,0);
         alertg.push(previewy.size & sourcer.length);
         rnewinterstitialF += `${rnewinterstitialF.length}`;
         alertg = [(String.fromCharCode(100,0) == sourcer ? previewy.size : sourcer.length)];
          let service4 = true;
          let anytimec = String.fromCharCode(110,111,109,105,110,97,108,95,113,95,54,51,0);
          let time_atQ = String.fromCharCode(114,101,115,111,108,118,101,114,95,105,95,49,0);
         alertg = [2];
         service4 = anytimec == time_atQ;
         anytimec += `${time_atQ.length}`;
          let mathU = 1;
         alertg = [1];
         mathU |= mathU << (Math.min(2, Math.abs(mathU)));
          let logoX = 3;
          let minivodJ: Array<any> = [String.fromCharCode(108,95,53,57,95,99,104,101,118,114,111,110,0), String.fromCharCode(105,110,118,111,108,118,101,100,95,103,95,50,55,0)];
         sourcer += `${previewy.size << (Math.min(sourcer.length, 4))}`;
         logoX >>= Math.min(1, Math.abs(1));
         minivodJ = [minivodJ.length];
      while ((sourcer.length * 2) > 5 || 2 > (alertg.length * sourcer.length)) {
         sourcer += `${sourcer.length}`;
         break;
      }
      while ((sourcer.length - alertg.length) == 1 && 4 == (sourcer.length - 1)) {
         alertg = [alertg.length % 2];
         break;
      }
          let videojsz = 4.0;
          let minimizer: Map<any, any> = new Map([[String.fromCharCode(118,108,98,117,102,95,53,95,50,49,0),false ], [String.fromCharCode(114,101,112,111,114,116,105,110,103,95,115,95,56,51,0),true ]]);
          let chartA = String.fromCharCode(105,95,50,52,95,116,104,105,115,0);
         previewy = new Map([[`${minimizer.size}`, 3]]);
         videojsz -= 3 | chartA.length;
         minimizer.set(chartA, 1);
      let rounds = sourcer.length <= 5466129;
      do {
          let custom7: Map<any, any> = new Map([[String.fromCharCode(97,95,50,55,95,105,110,116,101,110,115,105,116,121,0),85], [String.fromCharCode(102,119,104,116,120,95,107,95,56,52,0),672], [String.fromCharCode(114,101,115,105,122,105,110,103,95,119,95,50,56,0),40]]);
          let sigmoba: Array<any> = [358, 901, 367];
          let mimou = 3.0;
          let chinasameV = 4.0;
         sourcer += `${parseInt(`${chinasameV}`)}`;
         custom7 = new Map([[`${custom7.size}`, custom7.size]]);
         sigmoba = [custom7.size / (Math.max(sigmoba.length, 9))];
         mimou *= parseFloat(`${sigmoba.length - 1}`);
         chinasameV *= parseFloat(`${3 + sigmoba.length}`);
         if (rounds) {
            break;
         }
      } while (rounds && ((sourcer.length ^ 5) <= 4 && (sourcer.length ^ 5) <= 2));
      sliderL >>= Math.min(4, sourcer.length);
   for (let e = 0; e < 3; e++) {
      nexts = `${2 % (Math.max(3, telegramI.length))}`;
   }
      w_center0 -= (nexts == String.fromCharCode(78,0) ? nexts.length : parseInt(`${w_center0}`));
   while (!nexts.startsWith(`${sliderL}`)) {
      nexts = `${(String.fromCharCode(49,0) == nexts ? parseInt(`${w_center0}`) : nexts.length)}`;
      break;
   }
   if (5 > (sliderL & 2)) {
       let memberi = 2.0;
       let overk = 0.0;
       let typingn: Array<any> = [431, 315, 459];
      if (overk <= 3.94) {
         memberi *= parseInt(`${overk}`);
      }
         typingn.push(3 - parseInt(`${memberi}`));
          let questG = String.fromCharCode(114,101,115,104,97,112,101,95,108,95,55,53,0);
          let mathx = String.fromCharCode(105,110,108,105,110,101,95,49,95,55,52,0);
          let sinad = 1.0;
         typingn.push(mathx.length * 1);
         questG += `${questG.length >> (Math.min(3, Math.abs(parseInt(`${sinad}`))))}`;
         mathx = `${(String.fromCharCode(57,0) == questG ? parseInt(`${sinad}`) : questG.length)}`;
          let kick8 = false;
         typingn = [parseInt(`${overk}`) % (Math.max(5, parseInt(`${memberi}`)))];
         kick8 = !kick8 && kick8;
         typingn.push(parseInt(`${memberi}`));
       let with_64 = 4.0;
      for (let x = 0; x < 3; x++) {
          let mbnative4 = String.fromCharCode(115,122,97,98,111,115,95,118,95,56,54,0);
         with_64 += parseInt(`${memberi}`) >> (Math.min(mbnative4.length, 5));
      }
      if (with_64 >= typingn.length) {
         typingn = [parseInt(`${memberi}`) + 1];
      }
      while (2.20 < (overk * 4.58) && (typingn.length / 1) < 3) {
          let alerte: Array<any> = [784, 211];
          let incident2 = String.fromCharCode(112,95,56,54,95,118,105,122,105,101,114,0);
          let dropdownx = 0.0;
         typingn.push((String.fromCharCode(86,0) == incident2 ? alerte.length : incident2.length));
         alerte.push(parseInt(`${dropdownx}`) >> (Math.min(1, Math.abs(2))));
         break;
      }
      sliderL %= Math.max(nexts.length + 1, 5);
   }
       let livex: Map<any, any> = new Map([[String.fromCharCode(111,95,49,95,98,97,99,107,103,114,111,117,110,100,105,110,103,0),349], [String.fromCharCode(105,111,115,117,114,102,97,99,101,95,48,95,55,0),653]]);
       let singaporel = true;
         singaporel = (93 < (livex.size << (Math.min(1, Math.abs((!singaporel ? 93 : livex.size))))));
          let plashY = 5;
          let const_dY = 2;
          let predictionq: Array<any> = [551, 755];
         singaporel = predictionq.length == livex.size;
         plashY -= const_dY;
         predictionq = [plashY];
         livex = new Map([[`${livex.size}`, livex.size]]);
         livex = new Map([[`${livex.size}`, (3 % (Math.max(7, (singaporel ? 2 : 4))))]]);
         livex.set(`${singaporel}`, ((singaporel ? 1 : 1) / (Math.max(3, 5))));
         livex.set(`${singaporel}`, livex.size);
      nexts = `${nexts.length}`;
      telegramI += `${nexts.length & 1}`;

    Clipboard.setString(userState.user?.userReferralCode ?? '');

   let selectN = 8827776 <= sliderL;
   do {
      sliderL |= 2 - parseInt(`${w_center0}`);
      if (selectN) {
         break;
      }
   } while ((1 > (sliderL ^ nexts.length)) && selectN);
   for (let m = 0; m < 2; m++) {
       let stringB = 0.0;
       let shootP = 4.0;
       let sportW = String.fromCharCode(114,101,118,101,114,115,101,100,95,50,95,54,0);
      for (let i = 0; i < 2; i++) {
          let homew: Map<any, any> = new Map([[String.fromCharCode(109,105,110,116,95,99,95,57,48,0),553], [String.fromCharCode(114,95,57,51,95,97,108,108,101,116,0),885], [String.fromCharCode(118,115,101,114,118,105,99,101,95,114,95,56,54,0),238]]);
          let alertj = String.fromCharCode(100,95,57,48,95,108,101,110,103,116,104,115,0);
         sportW += `${1 % (Math.max(8, homew.size))}`;
         homew = new Map([[alertj, (String.fromCharCode(56,0) == alertj ? alertj.length : alertj.length)]]);
      }
         stringB *= parseFloat(`${3}`);
         sportW += `${2 * parseInt(`${stringB}`)}`;
          let strings_ = String.fromCharCode(120,95,52,52,95,109,101,116,97,0);
          let switch_wf = 4;
          let type_c4 = 0.0;
         stringB /= Math.max(3, parseFloat(`${1 ^ parseInt(`${shootP}`)}`));
         strings_ = `${1 >> (Math.min(4, Math.abs(switch_wf)))}`;
         switch_wf |= 1 ^ parseInt(`${type_c4}`);
         type_c4 += strings_.length - 2;
      if (sportW.startsWith(`${shootP}`)) {
          let rewardw: Array<any> = [706, 837, 237];
          let acceptedI: Array<any> = [454, 641, 890];
          let othera = 1.0;
         shootP /= Math.max(1, parseFloat(`${parseInt(`${shootP}`)}`));
         rewardw = [1];
         acceptedI = [acceptedI.length + rewardw.length];
         othera *= acceptedI.length * 1;
      }
         stringB *= parseFloat(`${parseInt(`${shootP}`)}`);
      let viewsm = 7399739.0 >= shootP;
      do {
         shootP += parseFloat(`${3 % (Math.max(7, parseInt(`${shootP}`)))}`);
         if (viewsm) {
            break;
         }
      } while (((shootP - stringB) <= 4.59 || (stringB - 4.59) <= 4.9) && viewsm);
       let goalO: Array<any> = [979, 832];
      while (5 == sportW.length) {
          let diceV = String.fromCharCode(101,95,56,56,95,102,111,111,116,101,114,0);
         sportW += `${diceV.length >> (Math.min(sportW.length, 1))}`;
         break;
      }
      nexts = `${2 << (Math.min(Math.abs(parseInt(`${dangerO}`)), 4))}`;
   }
   for (let x = 0; x < 2; x++) {
       let goalg = true;
       let googleu = 2.0;
      let giftM = 5820098.0 <= googleu;
      do {
         googleu *= parseInt(`${googleu}`) / 2;
         if (giftM) {
            break;
         }
      } while ((goalg) && giftM);
       let modulej = 1.0;
       let index1 = 0.0;
      while (!goalg) {
         modulej -= 2 | parseInt(`${modulej}`);
         break;
      }
      if ((modulej / 5.52) == 3.94) {
         modulej /= Math.max(2, 4);
      }
      for (let i = 0; i < 2; i++) {
         googleu /= Math.max(5, parseInt(`${index1}`));
      }
      if (index1 > 1.18) {
          let viewsP = String.fromCharCode(102,111,108,100,101,114,95,97,95,54,49,0);
         googleu -= (String.fromCharCode(110,0) == viewsP ? viewsP.length : parseInt(`${index1}`));
      }
      malaysiaL = `${(String.fromCharCode(69,0) == malaysiaL ? parseInt(`${w_center0}`) : malaysiaL.length)}`;
   }
      dangerO -= 1;
      downloadedm &= sliderL - 3;
      referrerE.set(`${downloadedm}`, telegramI.length);
       let selecteda = 0.0;
       let apps2 = 3.0;
       let matchb = String.fromCharCode(101,110,97,98,108,101,115,95,121,95,53,57,0);
      while (3.29 < (selecteda + 2.5)) {
          let episodesp = String.fromCharCode(119,95,56,57,95,115,101,113,117,101,110,99,101,0);
          let x_images: Array<any> = [551, 303];
          let foundO = String.fromCharCode(117,110,97,114,99,104,105,118,101,100,95,48,95,56,53,0);
          let annerQ: Array<any> = [468, 13, 550];
          let episodesX = String.fromCharCode(98,105,115,101,99,116,95,116,95,51,49,0);
         selecteda -= parseInt(`${selecteda}`);
         episodesp = `${episodesX.length}`;
         x_images.push(x_images.length);
         foundO = `${2 - episodesp.length}`;
         annerQ.push(1 & foundO.length);
         episodesX += `${annerQ.length}`;
         break;
      }
       let giftR = String.fromCharCode(116,101,120,116,115,95,115,95,51,48,0);
       let rewind2: Array<any> = [272, 596, 0];
       let selectionC: Array<any> = [410, 827, 0];
      let sourceg = selectionC.length >= 9751413;
      do {
          let navigation2 = String.fromCharCode(122,95,51,50,95,102,114,97,109,101,100,0);
          let sendb = true;
         selectionC.push(3);
         navigation2 += `${navigation2.length}`;
         sendb = navigation2.endsWith(`${sendb}`);
         if (sourceg) {
            break;
         }
      } while ((!rewind2.includes(selectionC.length)) && sourceg);
      let mailQ = 9881753.0 >= apps2;
      do {
          let flipperB = true;
          let rewardvideo2: Array<any> = [981, 7, 893];
          let actions4 = String.fromCharCode(115,101,115,115,105,111,110,95,120,95,54,55,0);
         apps2 *= selectionC.length % (Math.max(giftR.length, 4));
         flipperB = actions4.length > rewardvideo2.length;
         rewardvideo2.push(3);
         actions4 += `${rewardvideo2.length}`;
         if (mailQ) {
            break;
         }
      } while (mailQ && ((selectionC.length % 2) >= 5 && (parseInt(`${apps2}`) * selectionC.length) >= 2));
         selecteda -= selectionC.length & parseInt(`${apps2}`);
         selecteda /= Math.max(1, 2);
         matchb = `${matchb.length & rewind2.length}`;
      let textG = selectionC.length <= 8631115;
      do {
         selectionC.push(parseInt(`${selecteda}`) >> (Math.min(4, Math.abs(3))));
         if (textG) {
            break;
         }
      } while ((1.4 <= (apps2 + 1.6) && 3 <= (selectionC.length + parseInt(`${apps2}`))) && textG);
      w_center0 += downloadedm << (Math.min(Math.abs(parseInt(`${w_center0}`)), 1));
       let googley = String.fromCharCode(117,110,98,105,110,100,95,55,95,50,53,0);
       let z_imagem = String.fromCharCode(117,110,100,101,114,101,115,116,105,109,97,116,101,100,95,100,95,52,56,0);
      while (z_imagem != String.fromCharCode(49,0)) {
          let d_countt = String.fromCharCode(114,110,103,115,95,49,95,57,57,0);
          let faviconL: Map<any, any> = new Map([[String.fromCharCode(122,95,55,57,95,114,101,108,97,117,110,99,104,0),478], [String.fromCharCode(100,111,119,110,108,111,97,100,101,114,95,118,95,52,56,0),388], [String.fromCharCode(117,95,55,54,95,99,121,99,108,101,0),667]]);
         googley = `${(d_countt == String.fromCharCode(104,0) ? d_countt.length : faviconL.size)}`;
         break;
      }
      for (let a = 0; a < 3; a++) {
          let update_tt: Array<any> = [803, 836];
          let fastforwardW = false;
          let tumbnailg = String.fromCharCode(100,95,49,50,95,104,97,110,100,108,101,0);
          let hoverv = false;
          let scoreB = String.fromCharCode(111,95,55,53,95,99,111,108,108,97,112,115,101,0);
         googley = `${3 * update_tt.length}`;
         update_tt = [1];
         fastforwardW = scoreB.length <= 96;
         tumbnailg += `${((hoverv ? 3 : 2) % (Math.max(tumbnailg.length, 3)))}`;
         hoverv = tumbnailg.length == 13;
         scoreB += `${(tumbnailg == String.fromCharCode(103,0) ? tumbnailg.length : (hoverv ? 1 : 2))}`;
      }
      let countryb = 5988639 >= z_imagem.length;
      do {
          let main_kx = String.fromCharCode(107,95,49,55,95,118,105,115,117,97,108,0);
          let reportE = 4.0;
          let playercommonu = String.fromCharCode(115,99,97,110,116,97,98,108,101,95,97,95,57,56,0);
         z_imagem = `${2 % (Math.max(2, googley.length))}`;
         main_kx += `${playercommonu.length & 3}`;
         reportE -= parseFloat(`${2 << (Math.min(4, Math.abs(parseInt(`${reportE}`))))}`);
         playercommonu += `${parseInt(`${reportE}`)}`;
         if (countryb) {
            break;
         }
      } while (countryb && (googley == String.fromCharCode(85,0)));
      let zhengpian1 = z_imagem == String.fromCharCode(101,113,120,111,122,103,108,109,0);
      do {
         z_imagem += `${z_imagem.length}`;
         if (zhengpian1) {
            break;
         }
      } while ((z_imagem == String.fromCharCode(121,0) && googley != String.fromCharCode(113,0)) && zhengpian1);
      while (z_imagem.length >= googley.length) {
         googley += `${z_imagem.length}`;
         break;
      }
      if (z_imagem != String.fromCharCode(76,0)) {
          let transferQ = 5;
          let filedQ = String.fromCharCode(110,95,51,52,95,109,115,118,115,0);
         googley = `${transferQ ^ filedQ.length}`;
      }
      f_unlockM += parseFloat(`${2 ^ parseInt(`${dangerO}`)}`);
   while (1.38 <= (dangerO * nexts.length) || 2.24 <= (1.38 * dangerO)) {
      nexts = `${downloadedm - telegramI.length}`;
      break;
   }
      referrerE = new Map([[nexts, 3]]);
    setIsDialogOpen(true);
  }

  return (
    <ScreenContainer>
      <TitleWithBackButtonHeader title='个人中心' />
      <View
        style={{
          justifyContent: 'space-between',
          flex: 1,
          paddingHorizontal: 10,
        }}
      >
        <View style={{ marginTop: 30 }}>

          <TouchableOpacity
            style={styles.pressableTextContainer}
            onPress={() => {
              ttGray.showToast('目前暂不支持更改头像，敬请期待');
            }}
          >
            <Text style={{ fontSize: 16, color: '#9C9C9C' }}>头像</Text>
            <FastImage
              style={{
                height: 34,
                width: 34,
              }}
              source={require('@static/images/projectTumbnailMbsplash.png')}
            />
          </TouchableOpacity>


          { }
          <TouchableOpacity
            style={styles.pressableTextContainer}
            onPress={onPressUsername}
          >
            <Text style={{ fontSize: 16, color: colors.text }}>
              {username}
            </Text>
          </TouchableOpacity>

          { }
          <View style={styles.pressableTextContainer}>
            <Text style={{ fontSize: 16, color: '#9C9C9C' }}>
              {userState.user?.userEmail !== '' ? userState.user?.userEmail : userState.user.userPhoneNumber}
            </Text>
          </View>

          { }
          {userState.user?.userReferrerName == '' &&
            userState.user.userAllowUpdateReferral == true &&
            <TouchableOpacity
              style={styles.pressableTextContainer}
              onPress={onPressReferrer}
            >
              <Text style={{ fontSize: 16, color: '#B6B6B6' }}>
                补填邀请码 (只能填写一次)
              </Text>
            </TouchableOpacity>
          }

          {userState.user?.userReferrerName != '' && (
            <View style={styles.pressableTextContainer}>
              <Text style={{ fontSize: 16, color: '#9C9C9C' }}>推介人</Text>

              <View
                style={{
                  flexDirection: 'row',
                  justifyContent: 'center',
                  alignItems: 'center',
                }}
              >
                <Text style={{ fontSize: 14, color: '#9C9C9C' }}>
                  {userState.user?.userReferrerName}
                </Text>
              </View>
            </View>
          )}

          { }
          <TouchableOpacity
            style={styles.pressableTextContainer}
            onPress={onCopyReferralCode}
          >
            <Text style={{ fontSize: 16, color: colors.primary }}>
              {userState.user?.userReferralCode}
            </Text>

            <View
              style={{
                flexDirection: 'row',
                justifyContent: 'center',
                alignItems: 'center',
              }}
            >
              <Text style={{ fontSize: 14, color: '#9C9C9C' }}>
                复制邀请码
              </Text>
              <Image
                style={{
                  height: 27,
                  width: 27,
                  position: 'relative',
                  top: 2,
                }}
                source={require('@static/images/profile/volumeUntick.png')}
              />
            </View>
          </TouchableOpacity>

          <NotificationModal
            onConfirm={toggleOverlay}
            isVisible={isDialogOpen}
            title='复制成功'
            subtitle1=''
            subtitle2=''
            subtitle3=''
          />
        </View>

        <ChangeUsernameModal
          isVisible={isShowUsernameModal}
          handleClose={() => {
            setShowUsernameModal(false);
          }}
          initialUsername={username}
          onSubmitSuccess={onUsernameUpdateSuccess}
        />

        <ChangeReferrerModal
          isVisible={isShowReferrerModal}
          handleClose={() => {
            setShowReferrerModal(false);
          }}
        />
      </View>
    </ScreenContainer>
  );
};

const createStyles = ({ dark }: any) => StyleSheet.create({
  pressableTextContainer: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    backgroundColor: dark ? "#1d2023" : '#D9D9D9',
    marginTop: 20,
    paddingLeft: 18,
    paddingRight: 13,
    height: 48,
    borderRadius: 8,
    marginBottom: 20,
  },
});
