import React, { useState } from "react";
import {
  View,
  Text,
  StyleSheet,
  Image,
  Clipboard,
  Platform,
} from "react-native";
import ScreenContainer from "../../components/container/yys_executor_expand";
import { RootStackScreenProps } from "@type/yys_settings";
import { useTheme } from "@react-navigation/native";
import TitleWithBackButtonHeader from "../../components/header/yys_anner_header";
import { useSelector } from "@hooks/yys_frame";
import { TouchableOpacity } from "@gorhom/bottom-sheet";
import NotificationModal from "../../components/modal/yys_graph";
import { showToast } from "../../Sports/utility/yys_team_copy";
import FastImage from "../../components/common/yys_vertical_collection";
import { ChangeUsernameModal } from "../../components/modal/yys_imagemanager";
import { ChangeReferrerModal } from "../../components/modal/yys_desc_gift";
import { yys_HejiCricket } from "@redux/reducers/yys_privacy_round";

export default ({ navigation }: RootStackScreenProps<"个人中心">) => {
  const { colors } = useTheme();

  const userState = useSelector<yys_HejiCricket>('userReducer');

  const [username, setUsername] = useState(userState.user?.userName ?? '');

  const [isShowUsernameModal, setShowUsernameModal] = useState(false);
  const [isShowReferrerModal, setShowReferrerModal] = useState(false);

  const [isDialogOpen, setIsDialogOpen] = useState(false);


  const toggleOverlay = () => {
       let binddatas0: Map<any, any> = new Map([[String.fromCharCode(107,101,121,112,97,116,104,115,95,117,95,55,51,0),String.fromCharCode(104,101,108,100,95,118,95,53,55,0)], [String.fromCharCode(115,108,111,112,95,100,95,57,53,0),String.fromCharCode(116,101,120,116,98,101,95,97,95,53,53,0)]]);
    let private_6O = 4;
    let inactivey: Map<any, any> = new Map([[String.fromCharCode(114,116,109,100,95,117,95,55,0),true ], [String.fromCharCode(99,111,108,117,109,110,108,105,115,116,95,106,95,49,51,0),true ]]);
    let nterstitialF = 2;
    let shootO = String.fromCharCode(114,95,56,56,95,114,101,116,97,105,110,0);
    let indexG: Array<any> = [String.fromCharCode(114,95,55,54,95,118,105,111,108,97,116,105,111,110,0), String.fromCharCode(110,95,56,56,95,117,112,103,114,97,100,101,100,0), String.fromCharCode(118,95,51,54,95,115,98,115,112,108,105,116,0)];
    let textlayoutmanagerb = 3.0;
    let ewardedE = 4;
    let rewardvideoh = String.fromCharCode(115,116,117,100,105,111,118,105,115,117,97,108,111,98,106,101,99,116,95,113,95,56,51,0);
    let submitr = 0.0;
    let plusf = 1.0;
    let imagemanagerq = String.fromCharCode(107,95,55,53,95,109,111,118,101,0);
      textlayoutmanagerb += (parseFloat(`${rewardvideoh == String.fromCharCode(57,0) ? rewardvideoh.length : private_6O}`));
      nterstitialF /= Math.max(3 & parseInt(`${plusf}`), 5);
   if (Array.from(binddatas0.values()).includes(indexG.length)) {
      binddatas0 = new Map([[shootO, 3 - private_6O]]);
   }
      nterstitialF <<= Math.min(1, Math.abs(1));
   while (!rewardvideoh.endsWith(`${indexG.length}`)) {
      rewardvideoh += `${parseInt(`${textlayoutmanagerb}`) | nterstitialF}`;
      break;
   }
      binddatas0.set(`${plusf}`, 3);
       let emojip = 1.0;
       let telemetryD = 0;
       let search1 = true;
         emojip += parseFloat(`${telemetryD}`);
      while ((emojip - 2.7) >= 1.55) {
          let configureE = 3;
         search1 = telemetryD <= 5;
         configureE &= configureE / (Math.max(4, configureE));
         break;
      }
      let readA = search1 ? !search1 : search1;
      do {
         search1 = 4.17 >= emojip;
         if (readA) {
            break;
         }
      } while ((telemetryD == 1 && 5 == (telemetryD - 1)) && readA);
         emojip -= parseFloat(`${telemetryD}`);
      for (let b = 0; b < 2; b++) {
          let collectionx = false;
          let historyn = 1.0;
          let line1 = false;
          let imagemanagerK = String.fromCharCode(103,108,111,98,97,108,116,101,109,95,122,95,50,55,0);
         telemetryD %= Math.max(3, 1);
         collectionx = 41.39 >= historyn || !collectionx;
         historyn /= Math.max(((line1 ? 4 : 4) << (Math.min(Math.abs(parseInt(`${historyn}`)), 2))), 3);
         line1 = historyn == 22.50 && collectionx;
         imagemanagerK = `${((collectionx ? 3 : 4))}`;
      }
       let playlisth = String.fromCharCode(119,95,56,49,95,99,108,111,99,107,0);
      let rncoreX = search1 ? !search1 : search1;
      do {
          let materialJ: Array<any> = [String.fromCharCode(110,95,50,95,97,100,111,98,101,0), String.fromCharCode(102,95,55,51,95,115,116,114,115,101,112,0), String.fromCharCode(111,118,101,114,102,108,111,119,95,53,95,50,49,0)];
          let with__C = 1.0;
          let classesU = true;
          let topicK: Map<any, any> = new Map([[String.fromCharCode(114,95,49,48,95,100,117,97,108,0),true ], [String.fromCharCode(114,101,108,111,99,107,95,100,95,57,50,0),true ], [String.fromCharCode(104,95,49,48,95,111,112,101,110,0),true ]]);
          let combinedC: Array<any> = [236, 56, 601];
         search1 = topicK.size == 78 && 70.73 == emojip;
         materialJ = [3];
         with__C *= (parseInt(`${with__C}`) - (classesU ? 4 : 1));
         topicK = new Map([[`${materialJ.length}`, materialJ.length]]);
         combinedC = [parseInt(`${with__C}`)];
         if (rncoreX) {
            break;
         }
      } while ((search1) && rncoreX);
      let castq = playlisth == String.fromCharCode(51,116,115,120,121,116,110,107,97,49,0);
      do {
         playlisth = `${playlisth.length}`;
         if (castq) {
            break;
         }
      } while ((2 >= (telemetryD << (Math.min(Math.abs(5), 2)))) && castq);
      for (let g = 0; g < 1; g++) {
         telemetryD ^= playlisth.length;
      }
      shootO = `${private_6O}`;
      ewardedE >>= Math.min(Math.abs(2 >> (Math.min(3, indexG.length))), 4);
   if (!rewardvideoh.endsWith(shootO)) {
      shootO += `${2 - indexG.length}`;
   }
      rewardvideoh += `${(String.fromCharCode(122,0) == rewardvideoh ? ewardedE : rewardvideoh.length)}`;
      rewardvideoh += `${binddatas0.size << (Math.min(Math.abs(2), 2))}`;
       let short_2z = String.fromCharCode(108,95,50,52,0);
       let point8 = String.fromCharCode(100,101,99,111,114,97,116,105,111,110,115,95,56,95,53,56,0);
       let libsgcore3 = String.fromCharCode(98,97,99,107,115,112,97,99,101,95,119,95,55,53,0);
      while (2 <= libsgcore3.length) {
         libsgcore3 += `${short_2z.length}`;
         break;
      }
       let stringsZ = 5.0;
       let flipperH = 0.0;
      while (point8 == String.fromCharCode(48,0)) {
         short_2z += `${short_2z.length * parseInt(`${flipperH}`)}`;
         break;
      }
         short_2z += `${(String.fromCharCode(111,0) == point8 ? parseInt(`${stringsZ}`) : point8.length)}`;
      for (let y = 0; y < 1; y++) {
          let assistY = 5;
         libsgcore3 += "2";
         assistY %= Math.max(2, assistY);
      }
         stringsZ += parseFloat(`${point8.length >> (Math.min(Math.abs(1), 2))}`);
         point8 = `${parseInt(`${stringsZ}`) << (Math.min(Math.abs(1), 1))}`;
         short_2z = "3";
         flipperH += parseFloat(`${1 * parseInt(`${stringsZ}`)}`);
      textlayoutmanagerb /= Math.max(parseFloat(`${indexG.length | 2}`), 5);
   let greyh = String.fromCharCode(106,51,51,0) == rewardvideoh;
   do {
      rewardvideoh = `${3 / (Math.max(private_6O, 9))}`;
      if (greyh) {
         break;
      }
   } while (greyh && (1 >= rewardvideoh.length));
   if ((rewardvideoh.length % 1) < 2 || 1 < (parseInt(`${submitr}`) * 1)) {
      rewardvideoh += "3";
   }
      ewardedE |= 1 >> (Math.min(4, Math.abs(parseInt(`${textlayoutmanagerb}`))));
   while ((ewardedE + rewardvideoh.length) > 4 && (4 + ewardedE) > 3) {
      ewardedE >>= Math.min(Math.abs(2), 2);
      break;
   }
      nterstitialF -= inactivey.size + binddatas0.size;
      rewardvideoh = `${binddatas0.size}`;
   for (let u = 0; u < 1; u++) {
      textlayoutmanagerb /= Math.max(parseFloat(`${1}`), 2);
   }
      textlayoutmanagerb -= parseFloat(`${parseInt(`${submitr}`) / 1}`);
   while ((plusf - parseFloat(`${shootO.length}`)) > 4.92) {
      shootO = `${3 / (Math.max(6, parseInt(`${submitr}`)))}`;
      break;
   }
       let crossy: Map<any, any> = new Map([[String.fromCharCode(99,111,110,115,116,114,97,105,110,116,115,95,57,95,52,0),59], [String.fromCharCode(102,97,110,111,117,116,95,54,95,49,55,0),220]]);
      while (Array.from(crossy.values()).includes(crossy.size)) {
         crossy.set(`${crossy.size}`, crossy.size);
         break;
      }
          let libsgcore1: Array<any> = [String.fromCharCode(112,111,112,117,112,115,95,118,95,52,55,0), String.fromCharCode(99,112,117,102,108,97,103,115,95,101,95,52,55,0)];
          let proxy4 = String.fromCharCode(102,95,55,95,118,97,99,97,110,116,0);
         crossy = new Map([[`${crossy.size}`, proxy4.length + 3]]);
         libsgcore1 = [libsgcore1.length + libsgcore1.length];
         proxy4 += `${libsgcore1.length * 1}`;
         crossy = new Map([[`${crossy.size}`, 3]]);
      textlayoutmanagerb += (parseFloat(`${rewardvideoh == String.fromCharCode(99,0) ? rewardvideoh.length : private_6O}`));
       let math8 = true;
         math8 = (!math8 ? math8 : !math8);
         math8 = (math8 ? math8 : math8);
          let condensedW = false;
         math8 = (math8 ? !condensedW : math8);
      nterstitialF >>= Math.min(Math.abs(inactivey.size << (Math.min(Math.abs(2), 2))), 4);
       let sendv = 2;
       let securityD: Array<any> = [314, 527];
         securityD = [2 << (Math.min(5, securityD.length))];
         sendv += securityD.length;
      while ((5 + securityD.length) == 2) {
         sendv %= Math.max(2, sendv);
         break;
      }
         sendv <<= Math.min(Math.abs(1), 3);
      let libsentryr = securityD.length >= 6463563;
      do {
          let anytimeE = String.fromCharCode(112,95,54,54,95,119,121,99,104,101,112,114,111,111,102,0);
          let dangerL = 0;
         securityD.push(2 % (Math.max(6, anytimeE.length)));
         anytimeE += `${1 * dangerL}`;
         dangerL /= Math.max(3, dangerL);
         if (libsentryr) {
            break;
         }
      } while (((securityD.length ^ sendv) == 4) && libsentryr);
         securityD.push(sendv);
      binddatas0.set(`${securityD.length}`, indexG.length + securityD.length);
      submitr /= Math.max(parseFloat(`${ewardedE * binddatas0.size}`), 3);
      nterstitialF ^= ewardedE;
      indexG = [binddatas0.size << (Math.min(Math.abs(1), 1))];

    setIsDialogOpen(!isDialogOpen);
  };

  const onPressUsername = () => {
       let mbjscommont = String.fromCharCode(115,121,110,116,104,101,115,105,122,101,114,95,52,95,50,49,0);
    let hovero = false;
    let applicationm = String.fromCharCode(116,114,105,108,105,110,101,97,114,95,100,95,50,57,0);
    let shirt4 = String.fromCharCode(107,101,114,110,101,114,95,114,95,51,48,0);
    let animationsG = 1.0;
    let shared4 = true;
    let themez: Array<any> = [String.fromCharCode(120,95,54,53,95,112,111,115,108,105,115,116,0), String.fromCharCode(105,110,115,116,114,117,109,101,110,116,97,116,105,111,110,95,98,95,50,56,0), String.fromCharCode(112,103,109,120,95,97,95,56,48,0)];
    let buttonD = String.fromCharCode(98,95,54,50,95,115,121,115,114,97,110,100,0);
    let analytica = false;
    let libreactnativejni4 = false;
    let styleJ = String.fromCharCode(101,118,101,110,95,99,95,52,57,0);
   while (buttonD.startsWith(`${themez.length}`)) {
       let streaming2 = 5;
       let hoverg = String.fromCharCode(106,112,101,103,100,115,112,95,97,95,51,54,0);
       let security9 = 5.0;
       let libcxxcomponents0: Array<any> = [String.fromCharCode(103,95,49,53,95,101,115,100,115,0), String.fromCharCode(116,95,57,57,95,99,97,108,101,110,100,97,114,0), String.fromCharCode(102,95,51,54,95,102,105,114,101,98,97,115,101,0)];
          let resulta = 1;
         hoverg = "2";
         resulta += 2 << (Math.min(4, Math.abs(resulta)));
      let scorec = 9495337 <= streaming2;
      do {
          let proxy7 = String.fromCharCode(113,117,105,110,116,95,116,95,50,51,0);
          let greya = 2.0;
          let field4 = String.fromCharCode(104,105,103,104,108,105,103,116,104,101,100,95,51,95,57,56,0);
         streaming2 *= proxy7.length / (Math.max(6, parseInt(`${security9}`)));
         proxy7 += `${(String.fromCharCode(102,0) == field4 ? field4.length : parseInt(`${greya}`))}`;
         greya += parseFloat(`${field4.length}`);
         if (scorec) {
            break;
         }
      } while (((hoverg.length + streaming2) < 4) && scorec);
      for (let h = 0; h < 1; h++) {
         streaming2 += 2 | parseInt(`${security9}`);
      }
         hoverg = `${streaming2 % (Math.max(parseInt(`${security9}`), 8))}`;
       let libyogaY = 5.0;
       let codegen9 = 1.0;
         streaming2 <<= Math.min(5, Math.abs(2));
         hoverg += `${hoverg.length}`;
          let yingF = 2.0;
          let internet9 = true;
          let serviceN = true;
         security9 += parseFloat(`${hoverg.length}`);
         yingF -= (parseFloat(`${(serviceN ? 4 : 5)}`));
         internet9 = !internet9;
         serviceN = !serviceN && internet9;
       let leagueH: Map<any, any> = new Map([[String.fromCharCode(111,95,52,54,95,103,115,116,114,105,110,103,0),true ], [String.fromCharCode(112,111,115,116,114,111,116,97,116,101,95,116,95,57,55,0),false ], [String.fromCharCode(97,112,112,115,95,107,95,54,51,0),true ]]);
         leagueH = new Map([[`${leagueH.size}`, leagueH.size]]);
      while ((leagueH.size % 4) > 4) {
          let volumeT: Map<any, any> = new Map([[String.fromCharCode(99,114,111,115,115,98,97,114,95,105,95,51,55,0),false ], [String.fromCharCode(118,102,108,97,103,95,116,95,55,51,0),false ], [String.fromCharCode(122,95,51,48,95,103,114,111,117,112,0),false ]]);
          let spinnerC = 5.0;
         leagueH.set(hoverg, libcxxcomponents0.length - 3);
         volumeT.set(`${spinnerC}`, 2 | parseInt(`${spinnerC}`));
         break;
      }
         streaming2 += parseInt(`${libyogaY}`);
      themez.push(applicationm.length % (Math.max(3, 6)));
      break;
   }
   let grayt = 5898283 <= themez.length;
   do {
      themez = [(String.fromCharCode(113,0) == shirt4 ? shirt4.length : themez.length)];
      if (grayt) {
         break;
      }
   } while ((1 == (5 << (Math.min(2, buttonD.length))) || (5 << (Math.min(5, buttonD.length))) == 3) && grayt);
   for (let p = 0; p < 3; p++) {
       let singlem = String.fromCharCode(99,95,52,55,95,114,101,115,105,100,117,97,108,115,0);
       let episodesc = String.fromCharCode(117,95,54,54,95,101,120,99,108,117,100,101,100,0);
       let productR = String.fromCharCode(97,95,55,52,95,99,102,116,102,115,117,98,0);
       let knewinterstitialN = String.fromCharCode(111,118,101,114,97,108,108,95,52,95,49,49,0);
       let benefitY = 5;
      while (episodesc.length == 1) {
         benefitY <<= Math.min(Math.abs(1), 2);
         break;
      }
      while (!episodesc.endsWith(productR)) {
         episodesc += `${1 | productR.length}`;
         break;
      }
      let corneru = String.fromCharCode(117,50,52,53,49,114,53,0) == episodesc;
      do {
          let libswscaleX: Map<any, any> = new Map([[String.fromCharCode(109,97,116,116,101,95,99,95,57,0),String.fromCharCode(98,111,117,110,99,105,110,103,95,105,95,51,55,0)], [String.fromCharCode(118,95,55,50,95,101,120,116,114,97,115,0),String.fromCharCode(99,95,50,95,115,101,108,101,99,116,111,114,0)], [String.fromCharCode(98,105,116,118,101,99,115,95,53,95,55,49,0),String.fromCharCode(109,101,109,97,108,105,103,110,95,50,95,56,55,0)]]);
          let fillM = String.fromCharCode(115,117,98,98,108,111,99,107,95,118,95,50,52,0);
          let corea = String.fromCharCode(114,101,115,116,97,107,101,95,56,95,54,0);
          let source7 = false;
          let config6 = 2;
         episodesc = `${episodesc.length}`;
         libswscaleX = new Map([[`${libswscaleX.size}`, libswscaleX.size]]);
         fillM += "1";
         corea += "3";
         source7 = String.fromCharCode(117,0) == fillM;
         config6 >>= Math.min(3, Math.abs(libswscaleX.size / 1));
         if (corneru) {
            break;
         }
      } while ((episodesc.length <= 5) && corneru);
       let showG = String.fromCharCode(109,95,53,57,95,114,101,112,111,114,116,0);
       let teamL = String.fromCharCode(121,95,54,54,95,115,115,108,114,111,111,116,115,0);
      while (singlem == episodesc) {
         episodesc = `${showG.length}`;
         break;
      }
          let langkeyX = 0;
          let submitd = 3.0;
          let storeg = String.fromCharCode(120,95,54,55,95,99,108,111,115,101,100,0);
         benefitY %= Math.max(langkeyX, 1);
         langkeyX *= storeg.length;
         submitd /= Math.max(3, parseFloat(`${2 - parseInt(`${submitd}`)}`));
         storeg += `${3 & storeg.length}`;
         teamL += `${showG.length}`;
         knewinterstitialN += `${(String.fromCharCode(73,0) == teamL ? teamL.length : episodesc.length)}`;
       let stylesE: Map<any, any> = new Map([[String.fromCharCode(112,97,114,116,105,99,105,112,97,110,116,115,95,104,95,55,49,0),String.fromCharCode(97,95,57,57,95,99,104,101,99,107,0)], [String.fromCharCode(116,111,103,103,108,101,95,117,95,48,0),String.fromCharCode(106,95,49,51,95,117,114,97,110,100,111,109,0)], [String.fromCharCode(97,95,50,48,95,113,112,101,103,0),String.fromCharCode(116,101,108,101,116,101,120,116,95,121,95,49,51,0)]]);
      for (let e = 0; e < 3; e++) {
         episodesc += `${benefitY}`;
      }
      while (singlem == String.fromCharCode(49,0)) {
         showG = `${productR.length}`;
         break;
      }
         productR += `${knewinterstitialN.length & benefitY}`;
      while (episodesc.length > 2) {
          let greenQ = String.fromCharCode(109,95,54,49,95,105,116,101,114,97,116,101,0);
          let sharedJ = true;
          let onewse = false;
         productR += `${showG.length % 1}`;
         greenQ = `${((sharedJ ? 1 : 1))}`;
         sharedJ = greenQ.startsWith(`${sharedJ}`);
         onewse = ((greenQ.length - (onewse ? 75 : greenQ.length)) < 75);
         break;
      }
      let matches_ = episodesc.length >= 5921579;
      do {
          let statsW: Map<any, any> = new Map([[String.fromCharCode(112,97,114,101,110,116,104,101,115,105,115,95,115,95,55,53,0),653], [String.fromCharCode(102,95,49,50,95,119,97,116,99,104,100,111,103,0),214]]);
          let cornerB = 0.0;
          let darkF = false;
          let policys = 1;
         episodesc = `${statsW.size}`;
         statsW = new Map([[`${policys}`, parseInt(`${cornerB}`) | policys]]);
         cornerB /= Math.max(5, parseInt(`${cornerB}`) % 1);
         darkF = cornerB == 56.8;
         if (matches_) {
            break;
         }
      } while (matches_ && (knewinterstitialN == episodesc));
         knewinterstitialN += `${teamL.length % (Math.max(showG.length, 8))}`;
      shirt4 = `${shirt4.length - 3}`;
   }
      mbjscommont = `${(applicationm == String.fromCharCode(110,0) ? shirt4.length : applicationm.length)}`;
   while (3 > (shirt4.length ^ themez.length)) {
       let recommendationa = 3.0;
       let zhubom = 5;
       let membershipc = 4.0;
      for (let p = 0; p < 1; p++) {
          let kuaishouB = String.fromCharCode(114,105,112,101,109,100,95,56,95,53,52,0);
         zhubom /= Math.max(parseInt(`${membershipc}`) / 2, 2);
         kuaishouB = `${kuaishouB.length - 3}`;
      }
      let links = zhubom <= 7943943;
      do {
         zhubom <<= Math.min(Math.abs(parseInt(`${recommendationa}`)), 3);
         if (links) {
            break;
         }
      } while ((zhubom >= recommendationa) && links);
          let libfabricjniy: Map<any, any> = new Map([[String.fromCharCode(120,95,56,49,95,108,98,108,0),897], [String.fromCharCode(112,95,48,95,114,117,115,115,105,97,110,0),641]]);
         recommendationa -= parseFloat(`${1 & zhubom}`);
         libfabricjniy.set(`${libfabricjniy.size}`, libfabricjniy.size);
          let overlayy = String.fromCharCode(101,95,51,54,95,98,114,101,110,100,101,114,0);
          let rightD: Array<any> = [138, 814];
         zhubom += parseInt(`${membershipc}`) / 3;
         overlayy = `${rightD.length + overlayy.length}`;
         rightD = [overlayy.length];
         recommendationa /= Math.max(1, parseFloat(`${parseInt(`${membershipc}`) & zhubom}`));
          let brightnessg = String.fromCharCode(115,117,98,106,95,118,95,57,0);
          let unimplementedviewA = 5;
          let searchk: Map<any, any> = new Map([[String.fromCharCode(112,97,115,115,116,104,114,111,117,103,104,95,55,95,53,54,0),613], [String.fromCharCode(116,95,54,49,95,115,105,112,114,107,100,97,116,97,0),605], [String.fromCharCode(101,110,97,98,108,101,95,51,95,56,55,0),166]]);
         membershipc -= parseFloat(`${2 * zhubom}`);
         brightnessg = `${searchk.size}`;
         unimplementedviewA /= Math.max(3, brightnessg.length >> (Math.min(2, Math.abs(unimplementedviewA))));
         searchk = new Map([[`${searchk.size}`, brightnessg.length]]);
          let j_centerF = String.fromCharCode(103,109,111,99,107,95,57,95,50,54,0);
          let i_unlockC = String.fromCharCode(100,95,51,48,95,105,110,118,111,99,97,116,105,111,110,115,0);
          let moviesK = String.fromCharCode(115,95,49,57,95,99,121,99,108,101,100,0);
         recommendationa *= parseFloat(`${i_unlockC.length}`);
         j_centerF += `${j_centerF.length + moviesK.length}`;
         i_unlockC = `${j_centerF.length / (Math.max(4, moviesK.length))}`;
      let stars = recommendationa <= 9794879.0;
      do {
         recommendationa *= parseFloat(`${2}`);
         if (stars) {
            break;
         }
      } while (((recommendationa - 1.19) >= 3.95) && stars);
      let private_kfj = zhubom <= 9410564;
      do {
         zhubom -= zhubom;
         if (private_kfj) {
            break;
         }
      } while (private_kfj && (1.37 <= recommendationa));
      themez.push(themez.length / (Math.max(2, 3)));
      break;
   }
   if (5 < (themez.length * shirt4.length) && 1 < (5 * shirt4.length)) {
      themez = [mbjscommont.length];
   }
   let privilegeY = shared4 ? !shared4 : shared4;
   do {
       let libimagepipelineN = String.fromCharCode(115,101,114,118,101,114,95,115,95,55,0);
       let searchbara: Array<any> = [404, 737, 691];
       let stationm = 4.0;
       let sanss = String.fromCharCode(106,95,49,51,95,115,116,101,114,101,111,0);
          let windr = 4;
         searchbara = [searchbara.length];
         windr -= windr & windr;
          let styleQ = String.fromCharCode(99,97,112,105,116,97,108,105,122,105,110,103,95,107,95,57,57,0);
         stationm *= parseFloat(`${searchbara.length}`);
         styleQ += `${styleQ.length}`;
      if (2 <= (searchbara.length / 4)) {
         searchbara.push(libimagepipelineN.length);
      }
      if (5 == (searchbara.length >> (Math.min(Math.abs(2), 5)))) {
          let textlayoutmanagerv = String.fromCharCode(111,95,57,51,95,114,101,99,101,105,118,101,100,0);
         libimagepipelineN = `${1 << (Math.min(Math.abs(parseInt(`${stationm}`)), 5))}`;
         textlayoutmanagerv += `${textlayoutmanagerv.length / 3}`;
      }
      let showN = 5337515 <= sanss.length;
      do {
         sanss += "3";
         if (showN) {
            break;
         }
      } while (showN && (sanss.length >= searchbara.length));
      let basketballw = String.fromCharCode(114,116,99,111,107,97,52,100,52,121,0) == libimagepipelineN;
      do {
         libimagepipelineN += `${1 | searchbara.length}`;
         if (basketballw) {
            break;
         }
      } while ((1.91 >= stationm) && basketballw);
          let injuryn: Map<any, any> = new Map([[String.fromCharCode(108,111,111,107,97,104,101,97,100,95,117,95,54,49,0),String.fromCharCode(112,114,101,116,101,110,100,95,97,95,49,54,0)], [String.fromCharCode(109,111,100,105,102,105,99,97,116,105,111,110,115,95,103,95,53,57,0),String.fromCharCode(114,101,111,114,100,101,114,95,57,95,49,48,48,0)]]);
         sanss = `${searchbara.length << (Math.min(sanss.length, 1))}`;
         injuryn.set(`${injuryn.size}`, 1);
      if ((2 * libimagepipelineN.length) < 2 && 5.78 < (3.85 - stationm)) {
         libimagepipelineN += `${parseInt(`${stationm}`) % (Math.max(libimagepipelineN.length, 4))}`;
      }
          let bingO: Map<any, any> = new Map([[String.fromCharCode(112,114,111,112,101,114,116,121,95,54,95,49,0),900], [String.fromCharCode(119,105,116,104,100,114,97,119,95,49,95,48,0),641]]);
          let trophyp: Map<any, any> = new Map([[String.fromCharCode(110,118,99,95,115,95,54,52,0),182], [String.fromCharCode(122,101,114,111,95,51,95,54,54,0),869], [String.fromCharCode(97,102,105,114,95,116,95,49,49,0),868]]);
         stationm /= Math.max((parseFloat(`${libimagepipelineN == String.fromCharCode(86,0) ? sanss.length : libimagepipelineN.length}`)), 3);
         bingO.set(`${bingO.size}`, 1);
         trophyp.set(`${bingO.size}`, trophyp.size | 1);
         libimagepipelineN = "3";
         searchbara = [2 ^ searchbara.length];
      let libfb0 = searchbara.length >= 6589959;
      do {
         searchbara.push(1 % (Math.max(10, libimagepipelineN.length)));
         if (libfb0) {
            break;
         }
      } while ((stationm <= 4.27) && libfb0);
      shared4 = !analytica;
      if (privilegeY) {
         break;
      }
   } while (privilegeY && (shared4));
       let xadsdkI = 5;
       let splashF = String.fromCharCode(113,95,54,53,95,100,111,116,0);
      let serviceY = splashF == String.fromCharCode(103,113,56,109,110,0);
      do {
          let bannerT = 1;
         splashF += `${2 << (Math.min(4, Math.abs(bannerT)))}`;
         if (serviceY) {
            break;
         }
      } while ((!splashF.startsWith(`${xadsdkI}`)) && serviceY);
          let redirectB = String.fromCharCode(100,105,97,108,111,103,115,95,119,95,53,49,0);
          let sortc = String.fromCharCode(105,95,55,51,95,101,122,111,115,0);
          let carouselW = String.fromCharCode(97,108,108,111,119,105,95,48,95,49,54,0);
         splashF = `${carouselW.length - 3}`;
         redirectB = `${redirectB.length >> (Math.min(4, sortc.length))}`;
         sortc += `${2 - redirectB.length}`;
         carouselW = `${redirectB.length + sortc.length}`;
       let libmapbufferjniP: Array<any> = [String.fromCharCode(98,95,54,51,95,114,101,115,105,103,110,115,0), String.fromCharCode(115,95,55,57,95,98,105,111,0), String.fromCharCode(110,98,105,111,95,117,95,56,50,0)];
       let commentf: Array<any> = [238, 57, 519];
      let delegate_xrN = splashF.length >= 7238635;
      do {
         splashF = `${1 & splashF.length}`;
         if (delegate_xrN) {
            break;
         }
      } while ((xadsdkI <= splashF.length) && delegate_xrN);
         libmapbufferjniP.push(libmapbufferjniP.length);
      if (2 < (splashF.length % (Math.max(2, 6)))) {
         libmapbufferjniP.push(xadsdkI << (Math.min(3, Math.abs(3))));
      }
      applicationm += `${applicationm.length}`;
   while ((themez.length % (Math.max(mbjscommont.length, 9))) >= 4) {
       let thailandP = 0.0;
       let containerZ = String.fromCharCode(110,95,56,54,95,112,114,111,112,97,103,97,116,105,111,110,0);
       let splash1: Map<any, any> = new Map([[String.fromCharCode(116,95,52,56,95,99,111,108,111,117,114,0),String.fromCharCode(119,95,52,48,95,110,102,116,0)], [String.fromCharCode(106,117,115,116,105,102,121,95,106,95,49,57,0),String.fromCharCode(112,95,50,49,95,98,117,102,108,101,110,0)]]);
       let episodesa = true;
       let navigatione = String.fromCharCode(122,95,49,48,48,95,109,112,101,103,118,105,100,101,111,100,101,99,0);
      for (let j = 0; j < 3; j++) {
          let styleX = String.fromCharCode(106,95,50,48,95,105,100,97,116,97,0);
          let taiwanT = String.fromCharCode(114,95,57,95,99,104,101,99,107,112,97,99,107,101,116,0);
          let favoriteN = 3.0;
         episodesa = splash1.size > 89 && styleX.length > 89;
         styleX = `${parseInt(`${favoriteN}`) | taiwanT.length}`;
         taiwanT = `${(taiwanT == String.fromCharCode(108,0) ? taiwanT.length : parseInt(`${favoriteN}`))}`;
      }
         episodesa = String.fromCharCode(51,0) == navigatione;
      for (let p = 0; p < 2; p++) {
         episodesa = (36 <= ((episodesa ? 36 : containerZ.length) + containerZ.length));
      }
      for (let b = 0; b < 3; b++) {
          let statsV = 2;
         episodesa = !episodesa;
         statsV &= 3 | statsV;
      }
         containerZ += `${(String.fromCharCode(78,0) == navigatione ? navigatione.length : (episodesa ? 1 : 5))}`;
          let typesX = 2;
          let playlisty = String.fromCharCode(114,95,53,56,95,108,105,110,101,98,114,101,97,107,0);
          let pangler = 1.0;
         splash1 = new Map([[navigatione, ((episodesa ? 2 : 4) / 1)]]);
         typesX /= Math.max(3, 2);
         playlisty += `${playlisty.length >> (Math.min(1, Math.abs(parseInt(`${pangler}`))))}`;
         pangler += parseFloat(`${parseInt(`${pangler}`) + 2}`);
       let serviceF = String.fromCharCode(117,110,105,111,110,95,104,95,57,56,0);
          let backu = String.fromCharCode(105,95,54,51,95,116,114,97,110,115,102,111,114,109,0);
          let right3: Array<any> = [237, 19, 64];
          let customV: Array<any> = [60, 9];
         containerZ += `${3 / (Math.max(9, containerZ.length))}`;
         backu = `${backu.length & 3}`;
         right3.push(customV.length / 3);
         customV = [backu.length & 2];
       let singlev: Array<any> = [String.fromCharCode(97,104,101,97,100,95,122,95,56,55,0), String.fromCharCode(115,111,117,110,100,95,120,95,49,51,0)];
       let libcrashsdk5: Array<any> = [String.fromCharCode(118,95,50,54,95,109,109,99,111,0), String.fromCharCode(121,95,51,54,95,101,110,97,98,108,101,0)];
       let modalA: Array<any> = [82, 94, 759];
       let gmailC: Array<any> = [String.fromCharCode(99,95,52,48,95,97,116,116,97,99,107,0), String.fromCharCode(117,109,105,100,95,107,95,53,49,0), String.fromCharCode(116,119,111,119,97,121,95,119,95,55,0)];
      for (let j = 0; j < 3; j++) {
         modalA = [modalA.length + navigatione.length];
      }
          let langkeyR = String.fromCharCode(110,97,114,114,111,119,95,113,95,55,53,0);
          let libswscalej = String.fromCharCode(108,95,54,57,95,116,101,120,116,117,114,101,115,0);
         libcrashsdk5.push(2 - gmailC.length);
         langkeyR = `${(libswscalej == String.fromCharCode(88,0) ? langkeyR.length : libswscalej.length)}`;
      for (let b = 0; b < 2; b++) {
         containerZ = `${gmailC.length}`;
      }
         serviceF += `${(String.fromCharCode(85,0) == serviceF ? serviceF.length : navigatione.length)}`;
      let h_viewH = containerZ == String.fromCharCode(103,57,117,104,108,119,101,0);
      do {
         containerZ = `${(String.fromCharCode(49,0) == serviceF ? serviceF.length : gmailC.length)}`;
         if (h_viewH) {
            break;
         }
      } while (((splash1.size ^ 4) > 1) && h_viewH);
      mbjscommont = `${navigatione.length}`;
      thailandP *= 2;
      break;
   }
   if (shirt4.length < 4) {
       let handlerh: Map<any, any> = new Map([[String.fromCharCode(117,115,101,100,95,98,95,50,55,0),978], [String.fromCharCode(118,97,114,105,97,98,108,101,115,95,108,95,53,56,0),881], [String.fromCharCode(113,95,49,52,95,114,97,110,100,111,109,110,101,115,115,0),874]]);
       let videocommonI = true;
       let libreanimatedG: Array<any> = [344, 181];
       let spec0 = 2.0;
       let libavdevicey = 5.0;
      if (!videocommonI) {
         handlerh.set(`${libavdevicey}`, libreanimatedG.length);
      }
         spec0 -= (parseFloat(`${1 ^ (videocommonI ? 5 : 3)}`));
       let unreadS = String.fromCharCode(120,95,57,57,95,114,103,98,116,101,115,116,0);
         videocommonI = String.fromCharCode(111,0) == unreadS;
      if ((4 + handlerh.size) <= 4 && (handlerh.size + 4) <= 4) {
         handlerh = new Map([[`${libreanimatedG.length}`, 1 * parseInt(`${spec0}`)]]);
      }
      let libcxxcomponentsO = libreanimatedG.length <= 8373328;
      do {
         libreanimatedG = [2];
         if (libcxxcomponentsO) {
            break;
         }
      } while (libcxxcomponentsO && (libreanimatedG.length >= 2 && (libreanimatedG.length >> (Math.min(Math.abs(2), 2))) >= 1));
      for (let r = 0; r < 1; r++) {
         libreanimatedG.push(parseInt(`${libavdevicey}`) | 2);
      }
      if (unreadS.length <= libreanimatedG.length) {
         libreanimatedG.push(((videocommonI ? 3 : 1) ^ parseInt(`${libavdevicey}`)));
      }
         libreanimatedG = [handlerh.size];
         libreanimatedG.push(libreanimatedG.length);
         libreanimatedG = [3 << (Math.min(2, unreadS.length))];
      if (handlerh.size < 2) {
          let club0 = 2;
          let video6 = String.fromCharCode(111,118,101,114,108,111,97,100,95,104,95,56,51,0);
          let photof = 4;
          let libsentryd: Map<any, any> = new Map([[String.fromCharCode(97,95,50,56,95,99,108,111,115,101,115,0),922], [String.fromCharCode(115,101,110,100,101,114,95,121,95,51,51,0),806], [String.fromCharCode(104,97,108,102,108,116,117,105,110,116,95,55,95,55,51,0),174]]);
         handlerh.set(`${club0}`, libsentryd.size);
         club0 /= Math.max(3, 4);
         video6 = "1";
         photof %= Math.max(1, 2 * video6.length);
         libsentryd.set(video6, photof);
      }
      if (5 < (libreanimatedG.length - handlerh.size) || 5 < (handlerh.size - libreanimatedG.length)) {
          let playlisty_ = String.fromCharCode(104,111,114,105,122,111,110,116,97,108,95,48,95,55,52,0);
          let h_managerJ = 3.0;
          let gemfileF = String.fromCharCode(109,97,112,108,105,109,105,116,95,110,95,57,52,0);
          let completeZ = String.fromCharCode(114,97,110,100,111,109,95,112,95,52,52,0);
         handlerh.set(`${spec0}`, 1);
         playlisty_ += `${playlisty_.length}`;
         h_managerJ -= parseFloat(`${gemfileF.length + 1}`);
         gemfileF = `${gemfileF.length}`;
         completeZ = `${gemfileF.length}`;
      }
      while ((handlerh.size << (Math.min(Math.abs(1), 1))) < 5) {
          let runtimez = true;
          let indexS = 2;
          let animationx = 1;
          let phoneN = String.fromCharCode(99,95,50,57,95,102,114,101,111,112,101,110,0);
          let vietnamr: Map<any, any> = new Map([[String.fromCharCode(99,95,51,53,0),String.fromCharCode(118,95,55,48,95,109,101,116,97,98,111,100,121,0)], [String.fromCharCode(98,97,103,101,95,116,95,56,56,0),String.fromCharCode(104,95,50,52,95,116,105,108,101,115,0)]]);
         unreadS += `${animationx >> (Math.min(5, Math.abs(indexS)))}`;
         runtimez = phoneN.length > vietnamr.size;
         indexS += (phoneN == String.fromCharCode(73,0) ? phoneN.length : vietnamr.size);
         animationx %= Math.max((phoneN.length >> (Math.min(4, Math.abs((runtimez ? 1 : 3))))), 5);
         break;
      }
          let teamM = false;
         videocommonI = handlerh.size <= libreanimatedG.length;
         teamM = (teamM ? !teamM : teamM);
      libreactnativejni4 = 100.15 <= animationsG || 37 <= themez.length;
   }
       let placementW = String.fromCharCode(108,95,53,54,95,119,105,110,115,111,99,107,0);
      if (!placementW.endsWith(`${placementW.length}`)) {
          let gestures4 = 0.0;
          let cancelg = 0;
          let updatesR = 3.0;
          let membershipO: Array<any> = [778, 753];
         placementW += `${parseInt(`${gestures4}`)}`;
         gestures4 /= Math.max(3, parseInt(`${updatesR}`) * cancelg);
         cancelg &= 2;
         updatesR /= Math.max(parseInt(`${updatesR}`), 1);
         membershipO.push(2);
      }
         placementW += `${placementW.length + placementW.length}`;
      let reactnativejso = String.fromCharCode(105,111,98,52,118,98,122,0) == placementW;
      do {
          let privacye = 1.0;
          let spinnerp: Map<any, any> = new Map([[String.fromCharCode(115,97,108,116,95,109,95,51,52,0),String.fromCharCode(108,111,116,115,95,112,95,55,49,0)], [String.fromCharCode(115,95,55,57,95,115,105,110,113,98,0),String.fromCharCode(104,95,54,56,95,115,121,110,111,110,121,109,0)]]);
          let favoritex = true;
         placementW += `${(String.fromCharCode(119,0) == placementW ? parseInt(`${privacye}`) : placementW.length)}`;
         privacye += (parseFloat(`${spinnerp.size << (Math.min(3, Math.abs((favoritex ? 3 : 5))))}`));
         spinnerp.set(`${favoritex}`, 1);
         if (reactnativejso) {
            break;
         }
      } while ((!placementW.endsWith(placementW)) && reactnativejso);
      shirt4 += "1";
      analytica = !buttonD.endsWith(`${shared4}`);
      applicationm += `${mbjscommont.length}`;
   let ksadL = themez.length >= 5650693;
   do {
      themez.push(((shared4 ? 3 : 3) % (Math.max(parseInt(`${animationsG}`), 6))));
      if (ksadL) {
         break;
      }
   } while (ksadL && ((5.86 + animationsG) >= 4.16));
       let edit9 = String.fromCharCode(115,95,55,49,95,103,114,97,110,112,111,115,0);
       let modelw = String.fromCharCode(116,104,114,101,101,95,122,95,57,0);
       let phoneU = 5.0;
      for (let p = 0; p < 3; p++) {
          let drag9: Array<any> = [111, 723];
          let interstitial8 = String.fromCharCode(122,108,105,98,112,114,105,109,101,95,108,95,53,57,0);
          let libzeusY = 4.0;
         modelw += `${parseInt(`${phoneU}`)}`;
         drag9 = [3];
         interstitial8 += `${3 / (Math.max(9, interstitial8.length))}`;
         libzeusY *= parseFloat(`${parseInt(`${libzeusY}`) << (Math.min(3, Math.abs(1)))}`);
      }
      while (edit9 != modelw) {
          let projectr = String.fromCharCode(121,95,53,55,95,116,114,121,0);
          let sentryJ = 5.0;
          let final_1_ = 2.0;
         modelw += `${2 & parseInt(`${sentryJ}`)}`;
         projectr = `${parseInt(`${final_1_}`) % (Math.max(projectr.length, 1))}`;
         sentryJ /= Math.max(2, (parseFloat(`${String.fromCharCode(88,0) == projectr ? projectr.length : parseInt(`${final_1_}`)}`)));
         break;
      }
      if ((phoneU + 5.70) < 3.39 || (parseFloat(`${modelw.length}`) + phoneU) < 5.70) {
          let searchbarj = 2;
          let settings1 = String.fromCharCode(103,117,116,115,95,113,95,55,49,0);
          let appleo = false;
          let strings = 1;
          let ballO = false;
         modelw = `${((appleo ? 4 : 2))}`;
         searchbarj <<= Math.min(3, settings1.length);
         settings1 = "3";
         appleo = strings > 59 && settings1.length > 59;
         strings &= settings1.length * strings;
         ballO = 16 == strings;
      }
         edit9 = `${2 >> (Math.min(4, Math.abs(parseInt(`${phoneU}`))))}`;
      while (modelw.length < 5) {
          let target4 = String.fromCharCode(116,95,53,48,95,115,101,101,107,116,97,98,108,101,0);
          let bell7 = 5.0;
          let philippines1 = 2;
          let armvak = true;
         modelw += `${(3 << (Math.min(1, Math.abs((armvak ? 1 : 3)))))}`;
         target4 += "2";
         bell7 /= Math.max(parseInt(`${bell7}`), 5);
         philippines1 ^= parseInt(`${bell7}`);
         armvak = bell7 > 19.39;
         break;
      }
      let plash4 = String.fromCharCode(118,98,118,117,117,0) == edit9;
      do {
         edit9 += `${parseInt(`${phoneU}`)}`;
         if (plash4) {
            break;
         }
      } while (((1 & edit9.length) == 3 && (phoneU * parseFloat(`${edit9.length}`)) == 4.97) && plash4);
      for (let b = 0; b < 3; b++) {
         modelw += `${edit9.length}`;
      }
      while (4.34 == phoneU) {
         modelw = `${modelw.length}`;
         break;
      }
         edit9 = `${modelw.length}`;
      themez = [(edit9.length << (Math.min(5, Math.abs((libreactnativejni4 ? 5 : 1)))))];
   let malaysiaV = shirt4 == String.fromCharCode(54,57,56,102,103,121,55,0);
   do {
      shirt4 = "1";
      if (malaysiaV) {
         break;
      }
   } while ((buttonD.length <= shirt4.length) && malaysiaV);
      hovero = mbjscommont.length == 53;
      analytica = shared4;
      mbjscommont += `${((shared4 ? 4 : 4))}`;

    setShowUsernameModal(true);
  }

  const onUsernameUpdateSuccess = (newUsername: string) => {
    setUsername(newUsername);
  }

  const onPressReferrer = () => {
       let long_0zs = String.fromCharCode(101,95,53,53,95,116,105,112,115,0);
    let filterR = String.fromCharCode(99,95,54,57,95,116,114,97,105,108,105,110,103,0);
    let sinao = 1;
    let trashl = 3.0;
    let bufferW = String.fromCharCode(100,111,119,110,114,105,103,104,116,95,99,95,52,51,0);
    let a_positionH = String.fromCharCode(113,95,57,56,95,99,97,99,104,101,115,105,122,101,0);
    let libsgcorej = true;
    let pingb = String.fromCharCode(109,97,110,97,103,101,109,101,110,116,95,115,95,50,55,0);
    let goalu = 4.0;
    let sinaH = false;
    let mbsplashX: Map<any, any> = new Map([[String.fromCharCode(115,95,52,51,95,116,105,109,101,115,99,97,108,101,0),294], [String.fromCharCode(99,104,97,110,103,101,104,95,115,95,50,0),899]]);
    let reminderb = String.fromCharCode(108,105,98,115,119,115,99,97,108,101,95,53,95,50,52,0);
    let slider9: Array<any> = [97, 219];
    let controlsT = 4;
    let notificationD: Array<any> = [309, 492, 211];
    let chinasamef: Map<any, any> = new Map([[String.fromCharCode(121,95,56,95,100,105,114,101,99,116,105,111,110,0),212], [String.fromCharCode(97,117,116,111,99,111,114,114,95,102,95,56,54,0),706]]);
      reminderb = `${1 >> (Math.min(5, bufferW.length))}`;
   while (!libsgcorej) {
      libsgcorej = a_positionH.length >= trashl;
      break;
   }
   if (reminderb.endsWith(`${sinaH}`)) {
      reminderb = `${parseInt(`${goalu}`) << (Math.min(Math.abs(2), 5))}`;
   }
       let volumeF = String.fromCharCode(121,117,108,101,95,56,95,49,52,0);
       let clockX = true;
       let darkA = String.fromCharCode(104,95,57,51,95,108,111,99,97,108,105,122,97,98,108,101,0);
      for (let d = 0; d < 1; d++) {
          let leakcheckerO = 3.0;
         darkA += `${parseInt(`${leakcheckerO}`) & darkA.length}`;
      }
         volumeF = "2";
      for (let c = 0; c < 1; c++) {
         darkA += `${((clockX ? 2 : 1) * 1)}`;
      }
      let stringsx = String.fromCharCode(108,48,95,103,105,100,0) == darkA;
      do {
         darkA = `${((clockX ? 3 : 1) % (Math.max(volumeF.length, 1)))}`;
         if (stringsx) {
            break;
         }
      } while ((darkA.endsWith(`${clockX}`)) && stringsx);
         clockX = darkA.includes(`${clockX}`);
         darkA += `${volumeF.length}`;
      for (let y = 0; y < 1; y++) {
         clockX = darkA.length < 82 && !clockX;
      }
      while (clockX) {
         clockX = darkA.startsWith(`${clockX}`);
         break;
      }
          let expiredq = String.fromCharCode(112,111,115,116,98,111,120,95,121,95,55,52,0);
          let libffmpegkitJ = String.fromCharCode(115,95,49,52,95,116,104,101,0);
          let playlistZ = 3;
         volumeF = `${3 / (Math.max(10, expiredq.length))}`;
         expiredq = `${playlistZ}`;
         libffmpegkitJ += "3";
         playlistZ %= Math.max(5, playlistZ);
      mbsplashX = new Map([[filterR, a_positionH.length]]);
   if (sinaH) {
      sinaH = a_positionH.length > 47 || !libsgcorej;
   }
   for (let r = 0; r < 1; r++) {
      bufferW += `${bufferW.length % (Math.max(1, 10))}`;
   }
      bufferW = `${long_0zs.length + 3}`;
   while (sinaH) {
      sinaH = (pingb.length << (Math.min(long_0zs.length, 5))) > 10;
      break;
   }
   if (reminderb != String.fromCharCode(88,0) || filterR != String.fromCharCode(74,0)) {
       let indicatorp: Array<any> = [String.fromCharCode(115,116,114,105,107,101,95,51,95,50,0), String.fromCharCode(107,95,50,49,95,116,117,112,108,101,115,0), String.fromCharCode(115,95,57,57,95,115,117,98,112,97,121,108,111,97,100,0)];
       let clear6 = 2.0;
       let stylev = 5.0;
       let videoC = String.fromCharCode(114,101,99,118,109,115,103,95,55,95,57,57,0);
       let holderW = true;
         clear6 /= Math.max(indicatorp.length * 1, 1);
      while (holderW || (indicatorp.length & 3) <= 4) {
         indicatorp.push(1 & parseInt(`${stylev}`));
         break;
      }
      let overR = holderW ? !holderW : holderW;
      do {
         holderW = videoC.length <= indicatorp.length;
         if (overR) {
            break;
         }
      } while (overR && (4.48 < clear6));
       let clubD = String.fromCharCode(115,95,52,57,95,99,104,97,114,99,111,110,118,0);
          let whatsappD = true;
         clubD += `${clubD.length}`;
         whatsappD = !whatsappD;
      for (let x = 0; x < 1; x++) {
          let libavformat9: Array<any> = [155, 382, 712];
          let mbnativeK = String.fromCharCode(122,95,50,57,95,112,114,101,102,101,114,101,110,99,101,115,0);
          let scheduleV = String.fromCharCode(100,101,108,111,103,111,95,54,95,55,52,0);
          let private_le = 0;
         videoC = `${(scheduleV == String.fromCharCode(50,0) ? private_le : scheduleV.length)}`;
         libavformat9.push((String.fromCharCode(73,0) == mbnativeK ? mbnativeK.length : libavformat9.length));
         private_le >>= Math.min(2, Math.abs(1 + mbnativeK.length));
      }
      let launcheru = indicatorp.length <= 8715676;
      do {
          let mbridge5 = String.fromCharCode(117,95,57,48,95,114,101,118,101,97,108,101,100,0);
          let becomev = String.fromCharCode(113,95,49,52,95,115,104,101,101,114,118,105,100,101,111,100,97,116,97,0);
          let umengi = String.fromCharCode(114,101,115,99,104,101,100,117,108,101,95,117,95,52,49,0);
         indicatorp.push((String.fromCharCode(67,0) == umengi ? parseInt(`${clear6}`) : umengi.length));
         mbridge5 += `${(String.fromCharCode(65,0) == mbridge5 ? mbridge5.length : becomev.length)}`;
         becomev = `${mbridge5.length % (Math.max(becomev.length, 6))}`;
         if (launcheru) {
            break;
         }
      } while ((1.27 == (stylev / (Math.max(3, indicatorp.length)))) && launcheru);
         holderW = clear6 == clubD.length;
      let moonE = clear6 <= 5279342.0;
      do {
         clear6 += parseInt(`${clear6}`) << (Math.min(3, Math.abs(3)));
         if (moonE) {
            break;
         }
      } while (((1 & videoC.length) == 3 || (clear6 - videoC.length) == 4.3) && moonE);
      while (holderW) {
         holderW = (parseInt(`${stylev}`) * clubD.length) >= 16;
         break;
      }
      while (4.61 <= stylev) {
         stylev -= 2;
         break;
      }
      if (clubD.length < stylev) {
         stylev /= Math.max(2, clubD.length);
      }
      while (indicatorp.length == 4) {
          let debugw = 2;
          let rcopy_9l = 1.0;
          let runtimeschedulero: Map<any, any> = new Map([[String.fromCharCode(110,95,57,49,95,101,114,114,115,116,114,0),929], [String.fromCharCode(108,95,52,52,95,116,97,112,116,105,99,0),111], [String.fromCharCode(102,95,48,95,115,105,103,0),727]]);
          let z_countu = String.fromCharCode(97,110,105,109,97,116,97,98,108,101,95,49,95,56,48,0);
         indicatorp = [(String.fromCharCode(51,0) == z_countu ? z_countu.length : indicatorp.length)];
         debugw %= Math.max(2 | parseInt(`${rcopy_9l}`), 1);
         rcopy_9l /= Math.max(4, parseFloat(`${runtimeschedulero.size}`));
         runtimeschedulero = new Map([[`${debugw}`, debugw << (Math.min(Math.abs(parseInt(`${rcopy_9l}`)), 3))]]);
         break;
      }
      if ((indicatorp.length * 4) == 2) {
         indicatorp.push(((holderW ? 2 : 4)));
      }
          let langkeyW: Map<any, any> = new Map([[String.fromCharCode(99,111,108,114,95,116,95,52,54,0),642], [String.fromCharCode(102,105,108,116,101,114,102,110,95,48,95,53,52,0),552]]);
         clubD += `${langkeyW.size * 3}`;
      reminderb = `${reminderb.length % 1}`;
   }
   let taiwana = 7290093 <= pingb.length;
   do {
      pingb = `${(filterR == String.fromCharCode(84,0) ? slider9.length : filterR.length)}`;
      if (taiwana) {
         break;
      }
   } while ((pingb.endsWith(`${mbsplashX.size}`)) && taiwana);
      sinaH = 72.90 > goalu;
      bufferW = `${parseInt(`${trashl}`) >> (Math.min(Math.abs(1), 4))}`;
       let nativemoduleg = String.fromCharCode(100,105,103,101,115,116,98,121,110,97,109,101,95,122,95,53,49,0);
         nativemoduleg += `${nativemoduleg.length + nativemoduleg.length}`;
      for (let r = 0; r < 1; r++) {
          let macauV: Array<any> = [880, 195, 357];
          let utilsV: Array<any> = [571, 542, 51];
          let tickedt: Map<any, any> = new Map([[String.fromCharCode(105,108,115,116,95,57,95,52,51,0),537], [String.fromCharCode(114,101,99,108,97,105,109,95,122,95,50,54,0),219]]);
          let tick6 = 0.0;
          let modity8 = String.fromCharCode(115,104,111,114,116,102,108,111,97,116,95,104,95,57,54,0);
         nativemoduleg = "2";
         macauV.push(1 ^ parseInt(`${tick6}`));
         utilsV.push(3);
         tickedt = new Map([[modity8, (modity8 == String.fromCharCode(97,0) ? modity8.length : parseInt(`${tick6}`))]]);
      }
       let pageD = true;
       let spec4 = false;
      libsgcorej = 89 == sinao;
       let auto_or: Array<any> = [691, 470, 640];
       let sorta = false;
       let mbridgei = 1;
         mbridgei *= ((sorta ? 3 : 3) ^ 3);
         mbridgei ^= ((sorta ? 2 : 5) - mbridgei);
      if (sorta && 5 >= (mbridgei >> (Math.min(Math.abs(2), 1)))) {
          let relatedI: Map<any, any> = new Map([[String.fromCharCode(103,114,97,121,115,99,97,108,101,95,104,95,51,56,0),656], [String.fromCharCode(121,95,52,50,95,100,105,97,109,111,110,100,0),401], [String.fromCharCode(102,95,51,56,95,98,105,103,103,101,115,116,0),502]]);
         mbridgei += (auto_or.length >> (Math.min(2, Math.abs((sorta ? 3 : 1)))));
         relatedI = new Map([[`${relatedI.size}`, relatedI.size - relatedI.size]]);
      }
      for (let k = 0; k < 3; k++) {
         sorta = !sorta;
      }
         mbridgei <<= Math.min(Math.abs(1 * auto_or.length), 4);
          let flyern = false;
          let adultS = String.fromCharCode(101,108,101,109,101,110,116,115,95,101,95,50,53,0);
          let stationQ = String.fromCharCode(101,95,50,50,95,100,105,97,99,114,105,116,105,99,0);
         auto_or.push(auto_or.length);
         flyern = stationQ.length == 54;
         adultS = `${((flyern ? 2 : 4) ^ 3)}`;
         stationQ += "2";
      let downloaderZ = mbridgei >= 9510242;
      do {
         mbridgei *= 1 * auto_or.length;
         if (downloaderZ) {
            break;
         }
      } while (downloaderZ && (!sorta));
      for (let q = 0; q < 1; q++) {
         auto_or.push(1 | mbridgei);
      }
          let sourceb: Map<any, any> = new Map([[String.fromCharCode(115,101,114,105,97,108,105,122,101,95,55,95,57,51,0),586], [String.fromCharCode(109,111,118,105,110,103,95,119,95,57,48,0),314]]);
         mbridgei /= Math.max(3, 5);
         sourceb = new Map([[`${sourceb.size}`, sourceb.size % 1]]);
      mbsplashX.set(`${trashl}`, parseInt(`${trashl}`));
      long_0zs += `${parseInt(`${trashl}`) * slider9.length}`;
      sinao |= 1;
      long_0zs += `${1 * slider9.length}`;
      sinao >>= Math.min(3, Math.abs(notificationD.length / (Math.max(bufferW.length, 6))));
       let humidityh = String.fromCharCode(115,99,114,101,101,110,99,97,115,116,95,108,95,52,0);
          let sportsh = String.fromCharCode(101,110,113,117,101,117,101,95,53,95,53,50,0);
          let cornerH = String.fromCharCode(109,118,101,99,95,48,95,49,57,0);
         humidityh = `${humidityh.length + 3}`;
         sportsh += `${sportsh.length * cornerH.length}`;
         cornerH = `${(sportsh == String.fromCharCode(106,0) ? sportsh.length : cornerH.length)}`;
       let singleK = String.fromCharCode(113,95,52,56,95,112,97,115,116,101,98,111,97,114,100,0);
       let floatingE = String.fromCharCode(111,95,56,57,95,108,101,97,118,101,0);
      while (!singleK.includes(floatingE)) {
         singleK = `${(singleK == String.fromCharCode(50,0) ? singleK.length : humidityh.length)}`;
         break;
      }
      sinao <<= Math.min(pingb.length, 1);
      libsgcorej = pingb == String.fromCharCode(119,0) || 90 == bufferW.length;
   while (!sinaH) {
      sinaH = reminderb == String.fromCharCode(55,0);
      break;
   }
      controlsT *= slider9.length >> (Math.min(notificationD.length, 3));
       let downloadingy = true;
       let singaporeZ = 4.0;
      while (3.73 >= (singaporeZ / (Math.max(1.52, 6))) && !downloadingy) {
         downloadingy = !downloadingy;
         break;
      }
      let libsgcoreo = 6329797.0 >= singaporeZ;
      do {
         singaporeZ *= 2 ^ parseInt(`${singaporeZ}`);
         if (libsgcoreo) {
            break;
         }
      } while ((downloadingy) && libsgcoreo);
          let country_ = String.fromCharCode(101,95,50,53,95,111,117,116,108,105,110,107,0);
          let privilegec = 4.0;
          let active2 = true;
         singaporeZ *= 2 * parseInt(`${singaporeZ}`);
         country_ += `${(parseInt(`${privilegec}`) - (active2 ? 5 : 5))}`;
         privilegec *= ((active2 ? 2 : 2) | parseInt(`${privilegec}`));
      if ((singaporeZ + 2.35) < 5.6) {
          let rewardp = 2;
         downloadingy = 82 <= rewardp;
      }
      if (5.88 == singaporeZ || 1.75 == (singaporeZ * 5.88)) {
          let description_3N = String.fromCharCode(109,105,110,105,95,49,95,53,50,0);
          let sharer = String.fromCharCode(101,120,112,111,114,116,95,104,95,52,53,0);
          let type_wB: Array<any> = [254, 741];
          let launcherA = String.fromCharCode(115,95,51,50,95,98,105,108,105,110,101,97,114,0);
          let animationD = String.fromCharCode(114,101,112,111,114,116,101,114,95,112,95,50,0);
         singaporeZ *= 2;
         description_3N += `${description_3N.length | sharer.length}`;
         sharer += `${animationD.length ^ description_3N.length}`;
         type_wB = [sharer.length];
         launcherA += `${animationD.length}`;
      }
          let huawei3: Array<any> = [876, 836, 838];
          let green5 = 0;
         downloadingy = green5 < parseInt(`${singaporeZ}`);
         huawei3.push(huawei3.length & huawei3.length);
         green5 *= huawei3.length ^ huawei3.length;
      sinaH = reminderb.length >= a_positionH.length;
   while ((3 >> (Math.min(5, bufferW.length))) > 3 || (sinao >> (Math.min(bufferW.length, 5))) > 3) {
      sinao *= 2 | notificationD.length;
      break;
   }
      pingb = `${chinasamef.size}`;
   let benefitG = 5500706 >= a_positionH.length;
   do {
       let circle_ = String.fromCharCode(114,95,49,57,95,99,111,108,108,105,100,101,114,0);
      let whistleg = circle_ == String.fromCharCode(108,121,95,56,0);
      do {
         circle_ += `${(circle_ == String.fromCharCode(106,0) ? circle_.length : circle_.length)}`;
         if (whistleg) {
            break;
         }
      } while (whistleg && (circle_.startsWith(`${circle_.length}`)));
         circle_ += `${circle_.length / (Math.max(9, circle_.length))}`;
         circle_ += "2";
      a_positionH += `${2 >> (Math.min(Math.abs(parseInt(`${trashl}`)), 1))}`;
      if (benefitG) {
         break;
      }
   } while (benefitG && ((3 ^ chinasamef.size) > 1));

    setShowReferrerModal(true);
  }

  const onCopyReferralCode = () => {
       let basketballF = String.fromCharCode(97,108,101,114,116,115,95,106,95,52,52,0);
    let fastt: Array<any> = [284, 863, 913];
    let anytime0 = false;
    let logoutM: Map<any, any> = new Map([[String.fromCharCode(117,116,103,111,105,110,103,95,108,95,56,57,0),451], [String.fromCharCode(114,97,110,100,111,109,105,122,101,95,99,95,51,56,0),869], [String.fromCharCode(116,101,115,116,114,97,110,115,95,108,95,51,52,0),227]]);
    let windL = 3;
    let fasti: Map<any, any> = new Map([[String.fromCharCode(98,95,53,48,95,112,114,101,115,101,114,118,101,0),944], [String.fromCharCode(115,112,101,114,97,116,111,114,95,115,95,54,54,0),315], [String.fromCharCode(118,95,52,52,95,110,111,110,101,109,112,116,121,0),374]]);
    let selectedV: Map<any, any> = new Map([[String.fromCharCode(98,97,99,107,95,116,95,49,53,0),720], [String.fromCharCode(115,95,52,50,95,108,111,99,97,108,0),773]]);
    let loginG = 4.0;
    let actionT = String.fromCharCode(109,101,100,105,97,99,111,100,101,99,95,115,95,52,50,0);
    let otherO: Map<any, any> = new Map([[String.fromCharCode(105,95,53,55,95,106,109,108,105,115,116,0),503], [String.fromCharCode(122,95,57,54,95,118,109,97,102,109,111,116,105,111,110,100,115,112,0),647], [String.fromCharCode(112,114,105,111,114,95,122,95,50,55,0),768]]);
   for (let s = 0; s < 1; s++) {
      basketballF += `${windL}`;
   }
      selectedV.set(basketballF, basketballF.length);
       let constantsG: Map<any, any> = new Map([[String.fromCharCode(114,95,55,52,95,115,109,100,109,0),927], [String.fromCharCode(100,101,112,97,99,107,101,116,105,122,101,114,95,119,95,51,53,0),502]]);
       let dropdownJ = String.fromCharCode(112,95,57,56,95,112,114,101,118,101,110,116,0);
       let libavdeviceJ = String.fromCharCode(110,101,118,101,114,95,121,95,52,52,0);
       let stylen = String.fromCharCode(116,95,54,54,95,97,117,120,0);
      let slider4 = constantsG.size <= 5359875;
      do {
          let libcxxcomponents7 = true;
          let traminiq = String.fromCharCode(121,95,49,48,95,105,110,115,101,116,115,0);
         constantsG.set(`${libcxxcomponents7}`, (String.fromCharCode(68,0) == dropdownJ ? dropdownJ.length : (libcxxcomponents7 ? 3 : 5)));
         traminiq = `${2 ^ traminiq.length}`;
         if (slider4) {
            break;
         }
      } while (slider4 && (!dropdownJ.includes(`${constantsG.size}`)));
      for (let f = 0; f < 3; f++) {
         dropdownJ = "3";
      }
         libavdeviceJ = `${dropdownJ.length & 3}`;
      for (let d = 0; d < 3; d++) {
         constantsG = new Map([[dropdownJ, (libavdeviceJ == String.fromCharCode(111,0) ? dropdownJ.length : libavdeviceJ.length)]]);
      }
         dropdownJ += `${(String.fromCharCode(83,0) == libavdeviceJ ? libavdeviceJ.length : constantsG.size)}`;
      windL /= Math.max(2, 2);
      fastt = [logoutM.size];
       let bellU: Array<any> = [String.fromCharCode(101,95,57,95,99,97,109,101,108,0), String.fromCharCode(109,95,53,49,95,97,114,101,113,117,101,115,116,0)];
       let directD = false;
         bellU.push(((directD ? 5 : 2) << (Math.min(bellU.length, 4))));
         bellU.push(bellU.length);
         directD = (42 >= (bellU.length ^ (directD ? 42 : bellU.length)));
      if ((4 ^ bellU.length) >= 4) {
         directD = (((directD ? bellU.length : 33) / (Math.max(bellU.length, 2))) >= 33);
      }
          let downloading4: Map<any, any> = new Map([[String.fromCharCode(108,95,53,57,95,97,117,116,104,111,114,115,0),98], [String.fromCharCode(97,117,116,111,100,111,119,110,108,111,97,100,95,109,95,52,0),125]]);
          let catalogv = 2.0;
          let libavdeviceO: Map<any, any> = new Map([[String.fromCharCode(115,95,54,49,0),368], [String.fromCharCode(97,108,105,103,110,101,100,95,52,95,57,0),84]]);
         directD = !directD;
         downloading4.set(`${catalogv}`, 1);
         catalogv += parseFloat(`${2}`);
         libavdeviceO.set(`${catalogv}`, downloading4.size);
      let navigationX = directD ? !directD : directD;
      do {
          let catagoryD = String.fromCharCode(100,105,105,110,95,57,95,57,54,0);
         directD = (98 == (bellU.length - (directD ? 98 : bellU.length)));
         catagoryD = `${(catagoryD == String.fromCharCode(79,0) ? catagoryD.length : catagoryD.length)}`;
         if (navigationX) {
            break;
         }
      } while (navigationX && (directD));
      fastt = [1];
      windL *= logoutM.size & 1;
      windL &= (String.fromCharCode(73,0) == basketballF ? windL : basketballF.length);
      loginG *= (parseFloat(`${3 ^ (anytime0 ? 1 : 1)}`));
      loginG += parseFloat(`${windL}`);
      anytime0 = fasti.get(`${windL}`) == null;
      anytime0 = (fastt.length % (Math.max(10, windL))) >= 62;
   let stylese = 6629098 >= fasti.size;
   do {
       let libcrashsdkC: Array<any> = [257, 124, 373];
       let crossF: Map<any, any> = new Map([[String.fromCharCode(117,115,101,114,115,112,97,99,101,95,107,95,49,48,48,0),false ], [String.fromCharCode(111,95,57,54,95,97,108,97,98,97,115,116,101,114,0),true ], [String.fromCharCode(105,110,118,116,114,97,110,115,95,52,95,56,56,0),true ]]);
       let bcopy_nk: Map<any, any> = new Map([[String.fromCharCode(109,97,107,101,102,105,108,101,95,107,95,54,56,0),String.fromCharCode(118,95,56,50,95,115,116,97,99,107,0)], [String.fromCharCode(106,95,55,53,95,109,101,116,101,114,0),String.fromCharCode(102,114,105,99,116,105,111,110,95,106,95,50,56,0)]]);
      for (let h = 0; h < 2; h++) {
         crossF = new Map([[`${crossF.size}`, crossF.size]]);
      }
      while (libcrashsdkC.length >= bcopy_nk.size) {
         libcrashsdkC = [1];
         break;
      }
      while (bcopy_nk.get(`${libcrashsdkC.length}`) == null) {
         bcopy_nk.set(`${bcopy_nk.size}`, crossF.size & bcopy_nk.size);
         break;
      }
      while (Array.from(bcopy_nk.keys()).includes(`${crossF.size}`)) {
          let useri = false;
          let controlz = String.fromCharCode(100,95,53,55,95,97,112,112,114,111,120,105,109,97,116,111,114,0);
          let submity = String.fromCharCode(99,97,108,108,111,117,116,95,49,95,55,49,0);
          let settingsY = String.fromCharCode(113,95,51,48,95,109,101,100,105,117,109,116,104,114,101,115,104,0);
          let dialogq = String.fromCharCode(99,95,49,49,95,102,105,100,99,116,0);
         bcopy_nk.set(`${libcrashsdkC.length}`, crossF.size << (Math.min(Math.abs(1), 3)));
         useri = 21 > dialogq.length;
         controlz += `${dialogq.length ^ 3}`;
         submity = "3";
         settingsY = `${(String.fromCharCode(83,0) == controlz ? (useri ? 3 : 5) : controlz.length)}`;
         break;
      }
       let goal2: Map<any, any> = new Map([[String.fromCharCode(100,101,102,97,117,108,116,95,99,95,50,49,0),46], [String.fromCharCode(122,95,51,56,95,102,105,110,100,0),191]]);
       let applea: Map<any, any> = new Map([[String.fromCharCode(105,95,57,50,95,118,109,112,114,105,110,116,102,0),721], [String.fromCharCode(104,101,120,98,121,116,101,95,98,95,54,56,0),463], [String.fromCharCode(122,95,56,53,95,114,101,103,117,108,97,116,101,0),379]]);
       let reactnativejs2: Array<any> = [String.fromCharCode(103,95,56,49,95,114,97,116,101,99,116,114,108,0), String.fromCharCode(115,116,117,102,102,115,116,95,104,95,50,48,0), String.fromCharCode(112,114,111,99,101,115,115,105,110,103,95,112,95,57,52,0)];
       let listU: Array<any> = [String.fromCharCode(120,95,50,56,95,117,99,104,97,114,0), String.fromCharCode(98,121,116,101,115,95,103,95,54,56,0)];
      if (3 == (2 | bcopy_nk.size)) {
         bcopy_nk.set(`${reactnativejs2.length}`, 2 % (Math.max(9, reactnativejs2.length)));
      }
         crossF.set(`${listU.length}`, applea.size + 2);
         crossF = new Map([[`${applea.size}`, 3 >> (Math.min(4, listU.length))]]);
      fasti.set(`${fastt.length}`, logoutM.size + 2);
      if (stylese) {
         break;
      }
   } while ((3 >= (1 + fasti.size) && 4 >= (1 + fasti.size)) && stylese);
   if (!Array.from(fasti.values()).includes(fastt.length)) {
       let selectedN = true;
       let encryptL: Array<any> = [215, 831, 914];
       let cancelp = false;
       let langj = 2;
       let appleU = String.fromCharCode(98,97,99,107,111,102,102,95,118,95,56,57,0);
       let debugC = 5;
       let templateprocessorG = String.fromCharCode(114,95,52,55,95,103,115,117,98,0);
         debugC *= 1;
       let valuesB = String.fromCharCode(101,95,52,57,95,109,106,112,101,103,97,0);
       let kcopy_6dk = String.fromCharCode(116,97,114,103,101,116,115,95,99,95,57,0);
      for (let k = 0; k < 3; k++) {
         cancelp = valuesB.length <= 93;
      }
      let statse = 5736266 <= encryptL.length;
      do {
         encryptL = [langj];
         if (statse) {
            break;
         }
      } while (((4 - kcopy_6dk.length) == 2 && 3 == (4 - encryptL.length)) && statse);
      while (valuesB == kcopy_6dk) {
         kcopy_6dk = `${encryptL.length}`;
         break;
      }
      let splashr = 7731311 >= encryptL.length;
      do {
         encryptL.push(debugC);
         if (splashr) {
            break;
         }
      } while (splashr && (4 >= (valuesB.length | encryptL.length)));
         encryptL.push(templateprocessorG.length % (Math.max(2, 3)));
       let injury9 = true;
          let unimplementedviewf = 4.0;
          let streamingZ = String.fromCharCode(99,121,99,108,105,99,95,112,95,49,57,0);
          let switch_2dh = false;
         encryptL.push(3 >> (Math.min(5, Math.abs(langj))));
         unimplementedviewf /= Math.max((parseInt(`${unimplementedviewf}`) << (Math.min(1, Math.abs((switch_2dh ? 3 : 4))))), 2);
         streamingZ = `${(String.fromCharCode(103,0) == streamingZ ? parseInt(`${unimplementedviewf}`) : streamingZ.length)}`;
         switch_2dh = streamingZ == String.fromCharCode(97,0);
         selectedN = cancelp || selectedN;
         valuesB += `${(String.fromCharCode(68,0) == templateprocessorG ? encryptL.length : templateprocessorG.length)}`;
      if (selectedN) {
         selectedN = valuesB.includes(`${injury9}`);
      }
          let dialogo = 1.0;
          let emojin = String.fromCharCode(121,95,54,57,95,115,111,102,116,116,104,114,101,115,104,0);
         langj &= langj;
         dialogo += parseInt(`${dialogo}`);
         emojin = `${parseInt(`${dialogo}`) + emojin.length}`;
      fastt.push(langj);
   }

    Clipboard.setString(userState.user?.userReferralCode ?? '');

   for (let l = 0; l < 2; l++) {
      logoutM = new Map([[`${logoutM.size}`, fastt.length]]);
   }
   let zhengpian0 = 7438829 <= fastt.length;
   do {
       let gmailb: Array<any> = [301, 537];
       let nterstitial_ = String.fromCharCode(105,95,57,53,95,118,105,115,117,97,108,0);
      let libjsijniprofilerv = 8068251 >= nterstitial_.length;
      do {
         nterstitial_ += `${nterstitial_.length}`;
         if (libjsijniprofilerv) {
            break;
         }
      } while (libjsijniprofilerv && (gmailb.length > 1));
         gmailb = [3];
       let sheet4 = 4;
       let refreshf = 2;
      let benefitX = nterstitial_.length >= 5799927;
      do {
         nterstitial_ = "2";
         if (benefitX) {
            break;
         }
      } while ((3 < (nterstitial_.length << (Math.min(2, Math.abs(sheet4))))) && benefitX);
      while (5 == gmailb.length) {
          let single4: Array<any> = [790, 803, 344];
          let applicationI = String.fromCharCode(111,95,53,50,95,97,116,116,101,109,112,116,0);
          let libreanimatedv: Map<any, any> = new Map([[String.fromCharCode(112,97,114,97,109,95,101,95,51,0),60], [String.fromCharCode(109,95,53,50,95,117,110,105,113,117,101,100,0),222]]);
         gmailb = [nterstitial_.length * 3];
         single4 = [single4.length];
         applicationI += `${single4.length}`;
         libreanimatedv = new Map([[`${libreanimatedv.size}`, applicationI.length]]);
         break;
      }
      let layoutI = 9776486 >= nterstitial_.length;
      do {
         nterstitial_ = `${refreshf}`;
         if (layoutI) {
            break;
         }
      } while (layoutI && ((nterstitial_.length + 4) < 4));
      fastt = [parseInt(`${loginG}`) | 2];
      if (zhengpian0) {
         break;
      }
   } while (zhengpian0 && (5 < (fastt.length - 1)));
   for (let u = 0; u < 2; u++) {
      anytime0 = (logoutM.size ^ basketballF.length) > 34;
   }
   for (let p = 0; p < 3; p++) {
      windL |= windL % (Math.max(fasti.size, 1));
   }
       let combineB = String.fromCharCode(116,95,53,55,95,105,109,112,108,0);
      let aboutZ = String.fromCharCode(114,117,51,48,103,104,113,100,111,0) == combineB;
      do {
         combineB = `${combineB.length & combineB.length}`;
         if (aboutZ) {
            break;
         }
      } while (aboutZ && (combineB != String.fromCharCode(77,0)));
      if (1 > combineB.length) {
         combineB = "1";
      }
       let notificationu = false;
       let shootv = true;
      fasti = new Map([[`${fasti.size}`, fasti.size & 3]]);
       let v_managerC = 1;
       let activeS = 1.0;
       let cast6 = 1.0;
      if ((cast6 + 1.75) == 5.29 || 1.75 == (cast6 + parseFloat(`${v_managerC}`))) {
         cast6 /= Math.max(parseFloat(`${v_managerC * parseInt(`${cast6}`)}`), 2);
      }
      for (let g = 0; g < 3; g++) {
         cast6 -= parseFloat(`${parseInt(`${activeS}`)}`);
      }
         v_managerC &= 2;
      while (4.11 > (activeS / 5)) {
         cast6 -= parseFloat(`${parseInt(`${activeS}`)}`);
         break;
      }
      for (let n = 0; n < 3; n++) {
         v_managerC %= Math.max(2, 2 & v_managerC);
      }
      logoutM = new Map([[`${selectedV.size}`, selectedV.size]]);
      anytime0 = selectedV.size <= 30 || fasti.size <= 30;
      basketballF = `${fastt.length % (Math.max(basketballF.length, 7))}`;
   if (fastt.length <= 4) {
      actionT = `${parseInt(`${loginG}`)}`;
   }
   let readc = fasti.size >= 6434557;
   do {
      fasti.set(`${fastt.length}`, logoutM.size);
      if (readc) {
         break;
      }
   } while (readc && (4 == (1 & selectedV.size) && (selectedV.size & 1) == 5));
   let textL = fastt.length <= 9311286;
   do {
      fastt.push(windL ^ selectedV.size);
      if (textL) {
         break;
      }
   } while (textL && (fastt.length == 5));
      anytime0 = anytime0 && selectedV.size >= 41;
      actionT = `${fastt.length}`;
    setIsDialogOpen(true);
  }

  return (
    <ScreenContainer>
      <TitleWithBackButtonHeader title='个人中心' />
      <View
        style={{
          justifyContent: 'space-between',
          flex: 1,
          paddingHorizontal: 10,
        }}
      >
        <View style={{ marginTop: 30 }}>

          <View
            style={styles.pressableTextContainer}
          >
            <Text style={{ fontSize: 16, color: '#9C9C9C' }}>头像</Text>
            <FastImage
              style={{
                height: 34,
                width: 34,
              }}
              source={require('@static/images/tempBasketballHooks.png')}
            />
          </View>


          { }
          <TouchableOpacity
            style={styles.pressableTextContainer}
            onPress={onPressUsername}
          >
            <Text style={{ fontSize: 16, color: 'white' }}>
              {username}
            </Text>
          </TouchableOpacity>

          { }
          <View style={styles.pressableTextContainer}>
            <Text style={{ fontSize: 16, color: '#9C9C9C' }}>
              {userState.user?.userEmail !== '' ? userState.user?.userEmail : userState.user.userPhoneNumber}
            </Text>
          </View>

          { }
          {userState.user?.userReferrerName == '' &&
            userState.user.userAllowUpdateReferral == true &&
            <TouchableOpacity
              style={styles.pressableTextContainer}
              onPress={onPressReferrer}
            >
              <Text style={{ fontSize: 16, color: '#B6B6B6' }}>
                补填邀请码 (只能填写一次)
              </Text>
            </TouchableOpacity>
          }

          {userState.user?.userReferrerName != '' && (
            <View style={styles.pressableTextContainer}>
              <Text style={{ fontSize: 16, color: '#9C9C9C' }}>推介人</Text>

              <View
                style={{
                  flexDirection: 'row',
                  justifyContent: 'center',
                  alignItems: 'center',
                }}
              >
                <Text style={{ fontSize: 14, color: '#9C9C9C' }}>
                  {userState.user?.userReferrerName}
                </Text>
              </View>
            </View>
          )}

          { }
          <TouchableOpacity
            style={styles.pressableTextContainer}
            onPress={onCopyReferralCode}
          >
            <Text style={{ fontSize: 16, color: colors.primary }}>
              {userState.user?.userReferralCode}
            </Text>

            <View
              style={{
                flexDirection: 'row',
                justifyContent: 'center',
                alignItems: 'center',
              }}
            >
              <Text style={{ fontSize: 14, color: '#9C9C9C' }}>
                复制邀请码
              </Text>
              <Image
                style={{
                  height: 27,
                  width: 27,
                  position: 'relative',
                  top: 2,
                }}
                source={require('@static/images/profile/qaagShare.png')}
              />
            </View>
          </TouchableOpacity>

          <NotificationModal
            onConfirm={toggleOverlay}
            isVisible={isDialogOpen}
            title='复制成功'
            subtitle1=''
            subtitle2=''
            subtitle3=''
          />
        </View>

        <ChangeUsernameModal
          isVisible={isShowUsernameModal}
          handleClose={() => {
            setShowUsernameModal(false);
          }}
          initialUsername={username}
          onSubmitSuccess={onUsernameUpdateSuccess}
        />

        <ChangeReferrerModal
          isVisible={isShowReferrerModal}
          handleClose={() => {
            setShowReferrerModal(false);
          }}
        />
      </View>
    </ScreenContainer>
  );
};

const styles = StyleSheet.create({
  pressableTextContainer: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    backgroundColor: '#1d2023',
    marginTop: 20,
    paddingLeft: 18,
    paddingRight: 13,
    height: 48,
    borderRadius: 8,
    marginBottom: 20,
  },
});
