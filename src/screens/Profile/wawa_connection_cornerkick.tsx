import React, { useEffect, useState } from "react";
import {
  View,
  Text,
  StyleSheet,
  TextInput,
  Image,
  RefreshControl,
  ScrollView,
} from "react-native";
import ScreenContainer from "../../components/container/wawa_nterstitial_iconpipexpand";
import { RootStackScreenProps } from "@type/wawa_iconnewchat";
import { useTheme } from "@react-navigation/native";
import { wawaIconsubssuccess } from "@redux/wawa_bgvipsport_spinner";

import TitleWithBackButtonHeader from "../../components/header/wawa_matchinactive_header";
import axios from "axios";
import { Keyboard } from "react-native";
import {
  API_DOMAIN,
  API_DOMAIN_TEST,
  API_DOMAIN_LOCAL,
} from "@utility/wawa_iconpointscore";
import NetInfo, { NetInfoState } from "@react-native-community/netinfo";

import InviteStep from "./../../components/invite/wawa_final";
import InviteCard from "./../../components/invite/wawa_pangle_macau";

import InviteHeader from "./../../components/invite/wawa_vipsport_header";
import { useAppDispatch, useAppSelector, useSelector } from "@hooks/wawa_root";
import { TouchableOpacity } from "@gorhom/bottom-sheet";
import { updateUserAuth } from "@redux/actions/wawa_loginsuccess";
import wawa_dacccfaabfbcbadeebddcabacdffdbc_video from '../../../Umeng/wawa_dacccfaabfbcbadeebddcabacdffdbc_video';
import { wawaEvent } from "@api";
import { wawaPhoneControls } from "@redux/reducers/wawa_umeng";


export default ({ navigation }: RootStackScreenProps<"邀请">) => {
  const { colors, textVariants, icons, spacing } = useTheme();

  const userState = useSelector<wawaPhoneControls>('userReducer');

  const [refreshing, setRefreshing] = useState(false);
  const dispatch = useAppDispatch();

  
  useEffect(() => {
    wawa_dacccfaabfbcbadeebddcabacdffdbc_video.userCenterVipInviteViewsAnalytics();
  }, []);
  

  const handleRefresh = async () => {
       let textlayoutmanagerc = String.fromCharCode(101,115,99,97,112,101,115,95,120,95,56,51,0);
    let o_countl = 0.0;
    let stringsF = String.fromCharCode(99,111,109,112,117,116,101,100,95,102,95,49,54,0);
    let muted_ = 1.0;
    let libmapbufferjni7: Map<any, any> = new Map([[String.fromCharCode(116,95,53,53,95,97,108,116,101,114,110,97,116,105,118,101,0),375], [String.fromCharCode(97,112,112,114,111,118,101,100,95,99,95,49,56,0),58], [String.fromCharCode(120,95,51,95,97,100,118,97,110,99,105,110,103,0),927]]);
    let helper_ = 4.0;
    let filter0: Array<any> = [791, 918, 590];
    let linek = 1.0;
    let privatechatbgQ: Array<any> = [450, 98, 648];
    let memberX: Array<any> = [406, 727, 820];
    let livey: Map<any, any> = new Map([[String.fromCharCode(103,95,54,48,95,105,110,112,117,116,120,0),169], [String.fromCharCode(97,110,103,108,101,100,95,114,95,54,57,0),124], [String.fromCharCode(102,95,50,56,95,108,111,99,97,108,105,122,97,116,105,111,110,115,0),86]]);
    let delegate_kpD: Array<any> = [945, 180, 460];
    let kuaishouY: Map<any, any> = new Map([[String.fromCharCode(114,103,98,97,95,98,95,50,49,0),String.fromCharCode(114,101,109,111,118,105,110,103,95,113,95,54,48,0)], [String.fromCharCode(122,95,55,53,95,114,101,108,97,121,101,100,0),String.fromCharCode(109,95,55,52,95,116,108,111,103,0)], [String.fromCharCode(109,105,99,114,111,115,111,102,116,95,103,95,56,0),String.fromCharCode(99,95,55,51,95,112,97,114,101,110,0)]]);
    let orange4 = true;
    let transferH: Array<any> = [108, 520, 951];
      o_countl *= filter0.length;
      stringsF += `${1 >> (Math.min(4, stringsF.length))}`;
      memberX.push(stringsF.length ^ 1);
   while (4 == (stringsF.length << (Math.min(Math.abs(2), 2))) && (parseInt(`${o_countl}`) * stringsF.length) == 2) {
      stringsF = "2";
      break;
   }
   let turndownL = stringsF == String.fromCharCode(121,116,111,105,113,0);
   do {
      stringsF = `${libmapbufferjni7.size}`;
      if (turndownL) {
         break;
      }
   } while (turndownL && (helper_ >= 3.24));
      libmapbufferjni7.set(`${libmapbufferjni7.size}`, 2);
      o_countl -= 3;
      muted_ *= memberX.length;
   if (2 > stringsF.length) {
       let unticks = String.fromCharCode(98,111,111,115,116,95,107,95,51,54,0);
      for (let e = 0; e < 3; e++) {
         unticks = `${unticks.length}`;
      }
      while (unticks.length == 4) {
         unticks += `${unticks.length / 3}`;
         break;
      }
       let scheduler = String.fromCharCode(100,105,115,116,105,110,99,116,95,109,95,56,57,0);
       let penaltyshootJ = String.fromCharCode(115,114,116,112,95,108,95,56,0);
      stringsF += `${livey.size}`;
   }
   while (4 == (memberX.length / (Math.max(8, libmapbufferjni7.size))) && (memberX.length / (Math.max(libmapbufferjni7.size, 2))) == 4) {
       let iconnewsshareS = String.fromCharCode(98,121,116,101,95,52,95,56,51,0);
          let rewardvideoP = String.fromCharCode(116,120,116,95,113,95,50,49,0);
         iconnewsshareS = `${rewardvideoP.length}`;
       let change6 = 2;
       let crossf = 0;
         iconnewsshareS += `${change6}`;
      libmapbufferjni7.set(`${privatechatbgQ.length}`, privatechatbgQ.length >> (Math.min(Math.abs(1), 3)));
      break;
   }
   let privilege0 = 5883121.0 <= linek;
   do {
      linek += parseFloat(`${memberX.length}`);
      if (privilege0) {
         break;
      }
   } while (((1.55 - linek) < 4.60 || (privatechatbgQ.length - parseInt(`${linek}`)) < 2) && privilege0);
      libmapbufferjni7.set(`${helper_}`, parseInt(`${muted_}`) - parseInt(`${helper_}`));
   for (let e = 0; e < 3; e++) {
      helper_ -= parseFloat(`${livey.size >> (Math.min(Math.abs(3), 3))}`);
   }

    setRefreshing(true);

      stringsF = `${3 & parseInt(`${o_countl}`)}`;
      privatechatbgQ.push(3 / (Math.max(2, parseInt(`${muted_}`))));
   let templateprocessor_ = o_countl >= 9515524.0;
   do {
      o_countl *= privatechatbgQ.length << (Math.min(Math.abs(2), 1));
      if (templateprocessor_) {
         break;
      }
   } while (templateprocessor_ && (3.15 > (3.4 + o_countl)));
       let downarrowo = String.fromCharCode(101,95,51,52,95,105,110,100,105,114,101,99,116,0);
       let buildj = true;
       let string7 = String.fromCharCode(99,117,98,101,100,95,103,95,49,0);
         downarrowo = `${1 & string7.length}`;
         string7 += "2";
      if (buildj) {
         buildj = !string7.startsWith(`${buildj}`);
      }
      while (1 < string7.length || 1 < downarrowo.length) {
         string7 += `${string7.length % 2}`;
         break;
      }
          let leakcheckerk = String.fromCharCode(114,95,56,54,95,114,116,112,0);
          let mbbannerH: Array<any> = [494, 637];
          let volumeR: Array<any> = [190, 947];
         string7 = `${volumeR.length + 2}`;
         leakcheckerk = `${3 % (Math.max(7, leakcheckerk.length))}`;
         mbbannerH.push(mbbannerH.length - 2);
         volumeR = [mbbannerH.length | leakcheckerk.length];
         downarrowo = `${(1 * (buildj ? 4 : 5))}`;
         buildj = buildj || string7.length > 77;
      let tickedi = downarrowo == String.fromCharCode(121,116,119,57,48,109,106,117,49,116,0);
      do {
          let fillc = String.fromCharCode(118,97,105,108,97,98,105,108,105,116,121,95,108,95,54,54,0);
         downarrowo = `${3 & fillc.length}`;
         if (tickedi) {
            break;
         }
      } while (tickedi && (!buildj || downarrowo.length > 5));
         buildj = !buildj;
      stringsF = `${filter0.length * stringsF.length}`;
      helper_ -= parseFloat(`${1}`);
   let loginsuccessR = muted_ <= 8847203.0;
   do {
      muted_ /= Math.max(memberX.length / 3, 5);
      if (loginsuccessR) {
         break;
      }
   } while ((!privatechatbgQ.includes(muted_)) && loginsuccessR);
      filter0.push(privatechatbgQ.length | libmapbufferjni7.size);
   if ((3.86 - o_countl) >= 2.49 || 3.31 >= (o_countl - 3.86)) {
       let chatroombackgroundU = true;
       let arrowrightg = 2.0;
       let cancelw: Array<any> = [562, 275, 818];
         chatroombackgroundU = arrowrightg > 32.84 || !chatroombackgroundU;
      for (let a = 0; a < 1; a++) {
         arrowrightg -= parseFloat(`${cancelw.length}`);
      }
         cancelw.push(((chatroombackgroundU ? 5 : 3) * 3));
         chatroombackgroundU = ((cancelw.length << (Math.min(5, Math.abs((chatroombackgroundU ? 26 : cancelw.length))))) > 26);
          let playershirtB = String.fromCharCode(115,117,98,102,114,97,109,101,95,108,95,55,51,0);
          let dicep = 3;
          let promotionf = String.fromCharCode(99,108,101,97,110,95,106,95,54,48,0);
         cancelw = [promotionf.length | playershirtB.length];
         playershirtB = `${1 * dicep}`;
         dicep -= dicep & dicep;
      if (chatroombackgroundU) {
          let shareblackh = String.fromCharCode(107,95,55,55,95,114,105,99,101,0);
          let iconcloseA = 5.0;
          let runtimeH = String.fromCharCode(115,95,51,54,95,99,97,114,101,102,117,108,108,121,0);
         chatroombackgroundU = arrowrightg <= 51.7 || !chatroombackgroundU;
         shareblackh = `${runtimeH.length ^ parseInt(`${iconcloseA}`)}`;
         iconcloseA += 1 * parseInt(`${iconcloseA}`);
         runtimeH += `${parseInt(`${iconcloseA}`) >> (Math.min(runtimeH.length, 4))}`;
      }
         chatroombackgroundU = arrowrightg == 40.60;
       let pangleb = 0.0;
       let modelsb = 4.0;
         pangleb *= (parseFloat(`${(chatroombackgroundU ? 1 : 1) % (Math.max(parseInt(`${arrowrightg}`), 9))}`));
      o_countl -= 1;
   }
       let ajaxQ = false;
       let a_countE = 0.0;
       let flipperY: Map<any, any> = new Map([[String.fromCharCode(119,95,56,50,95,112,101,114,99,101,110,116,105,108,101,0),292], [String.fromCharCode(114,102,99,116,95,105,95,49,50,0),943]]);
       let matchesC = 2.0;
      if (!Array.from(flipperY.keys()).includes(`${matchesC}`)) {
          let playlist5 = String.fromCharCode(98,95,52,50,95,116,104,117,109,98,110,97,105,108,0);
          let libane9 = 2.0;
          let crownG = false;
          let directB = String.fromCharCode(98,95,56,52,95,117,110,108,111,97,100,0);
         flipperY = new Map([[playlist5, (String.fromCharCode(57,0) == playlist5 ? playlist5.length : parseInt(`${libane9}`))]]);
         libane9 /= Math.max(2, (2 | (crownG ? 4 : 5)));
         crownG = crownG || directB.length >= 97;
         directB = `${((crownG ? 1 : 4) << (Math.min(directB.length, 4)))}`;
      }
         ajaxQ = (flipperY.size * parseInt(`${a_countE}`)) == 40;
         matchesC += parseFloat(`${flipperY.size / 2}`);
         a_countE -= 3;
       let libavformat0 = false;
       let zhengpianx = true;
      for (let j = 0; j < 1; j++) {
          let libflipperP = String.fromCharCode(99,117,114,114,101,110,100,95,99,95,49,54,0);
          let policyp = String.fromCharCode(119,95,55,57,95,99,103,105,109,97,103,101,0);
          let bellZ = String.fromCharCode(97,115,99,105,105,95,109,95,54,53,0);
         zhengpianx = policyp.length >= 1 || matchesC >= 77.88;
         libflipperP = `${(String.fromCharCode(112,0) == libflipperP ? bellZ.length : libflipperP.length)}`;
         policyp += `${bellZ.length}`;
      }
      for (let t = 0; t < 1; t++) {
         a_countE /= Math.max(2, ((zhengpianx ? 2 : 2) % (Math.max(5, (libavformat0 ? 2 : 1)))));
      }
       let reducer0 = String.fromCharCode(107,95,57,55,95,112,97,115,116,101,0);
       let configureh = String.fromCharCode(102,95,50,53,95,115,101,116,99,98,0);
      linek *= parseFloat(`${1}`);
      muted_ -= 1 * parseInt(`${linek}`);
   for (let e = 0; e < 1; e++) {
       let memberM = String.fromCharCode(109,95,54,56,95,115,101,116,116,101,114,0);
       let e_unlockj = 3;
       let dragU = 1;
       let disconnectedlogoi = String.fromCharCode(117,114,118,101,95,122,95,49,48,48,0);
       let iconarrowrightblack6 = String.fromCharCode(97,95,51,95,110,115,116,97,110,116,0);
       let iconeyex = String.fromCharCode(103,101,111,103,114,97,112,104,105,99,97,108,95,122,95,49,54,0);
          let chatroombackgroundI = String.fromCharCode(122,95,56,50,95,115,99,104,117,110,99,107,0);
          let libreactperfloggerjniy = String.fromCharCode(115,97,109,101,95,97,95,49,50,0);
         disconnectedlogoi += `${e_unlockj}`;
         chatroombackgroundI += `${libreactperfloggerjniy.length}`;
         libreactperfloggerjniy = `${libreactperfloggerjniy.length / (Math.max(chatroombackgroundI.length, 3))}`;
       let adultq = 1;
      for (let m = 0; m < 3; m++) {
         iconarrowrightblack6 = `${adultq}`;
      }
      if (iconeyex.length > 1) {
          let matchdetailbg3 = String.fromCharCode(100,101,112,116,104,95,120,95,57,51,0);
         iconeyex = `${(matchdetailbg3 == String.fromCharCode(80,0) ? disconnectedlogoi.length : matchdetailbg3.length)}`;
      }
      if (!disconnectedlogoi.includes(memberM)) {
          let stringa = String.fromCharCode(120,95,57,50,95,118,99,120,112,114,111,106,0);
          let mutedf: Map<any, any> = new Map([[String.fromCharCode(99,111,110,118,95,56,95,52,51,0),597], [String.fromCharCode(101,95,54,56,95,99,108,110,112,97,115,115,0),683]]);
          let middlesoundf: Array<any> = [String.fromCharCode(105,95,57,55,95,117,110,99,111,100,101,100,0), String.fromCharCode(108,101,110,95,108,95,56,52,0), String.fromCharCode(114,101,109,105,110,100,101,114,115,95,108,95,57,53,0)];
         memberM = `${3 ^ e_unlockj}`;
         stringa += `${mutedf.size % (Math.max(10, middlesoundf.length))}`;
         mutedf.set(`${middlesoundf.length}`, middlesoundf.length);
      }
      for (let s = 0; s < 3; s++) {
         disconnectedlogoi = `${(String.fromCharCode(55,0) == disconnectedlogoi ? disconnectedlogoi.length : iconarrowrightblack6.length)}`;
      }
       let iconmegaphoneM = false;
          let chinasamei = 5;
          let downloadL = String.fromCharCode(97,117,116,111,100,101,108,101,116,101,95,102,95,54,48,0);
         adultq ^= downloadL.length * 2;
         chinasamei %= Math.max(1, chinasamei);
         downloadL = "3";
      for (let f = 0; f < 2; f++) {
          let goallogoR = String.fromCharCode(111,116,104,101,114,119,105,115,101,95,105,95,53,56,0);
         memberM = `${e_unlockj + 2}`;
         goallogoR += `${goallogoR.length * goallogoR.length}`;
      }
       let c_viewm = 1.0;
          let gesturesq = String.fromCharCode(102,97,114,109,101,95,52,95,55,53,0);
          let reviewg = true;
          let dragcloseV: Array<any> = [String.fromCharCode(106,97,99,107,95,109,95,55,48,0), String.fromCharCode(119,95,57,54,95,117,115,108,101,101,112,0)];
         iconmegaphoneM = memberM == String.fromCharCode(69,0);
         gesturesq = `${((reviewg ? 3 : 5) % (Math.max(2, 4)))}`;
         reviewg = dragcloseV.length >= 10;
         dragcloseV.push((gesturesq == String.fromCharCode(76,0) ? gesturesq.length : (reviewg ? 1 : 2)));
      delegate_kpD.push(dragU ^ 2);
   }
   let forwardM = 8372985 <= privatechatbgQ.length;
   do {
      privatechatbgQ.push(1 * parseInt(`${muted_}`));
      if (forwardM) {
         break;
      }
   } while (((privatechatbgQ.length & 4) == 2) && forwardM);
   let airbnbstar7 = 5286776 >= delegate_kpD.length;
   do {
      delegate_kpD.push(parseInt(`${o_countl}`));
      if (airbnbstar7) {
         break;
      }
   } while (airbnbstar7 && ((delegate_kpD.length % (Math.max(10, stringsF.length))) >= 3));
    await refreshUserState();

      helper_ -= parseFloat(`${textlayoutmanagerc.length * 2}`);
      kuaishouY.set(`${linek}`, 2 + parseInt(`${linek}`));
      helper_ *= parseFloat(`${textlayoutmanagerc.length}`);
   for (let s = 0; s < 1; s++) {
      libmapbufferjni7 = new Map([[`${libmapbufferjni7.size}`, livey.size]]);
   }
      delegate_kpD = [parseInt(`${o_countl}`) / (Math.max(delegate_kpD.length, 1))];
   if (4 == (delegate_kpD.length + kuaishouY.size) || (4 + delegate_kpD.length) == 1) {
      delegate_kpD.push(1 - parseInt(`${helper_}`));
   }
   while ((memberX.length % (Math.max(2, 2))) < 5) {
      memberX = [memberX.length - livey.size];
      break;
   }
   while ((memberX.length | 5) <= 4 || (textlayoutmanagerc.length | memberX.length) <= 5) {
       let assetsW = true;
       let annera = 3.0;
       let annerF = String.fromCharCode(100,115,116,115,117,98,95,108,95,50,51,0);
       let libimagepipelineX: Array<any> = [124, 425];
       let watchC = String.fromCharCode(101,95,51,95,117,110,107,110,111,119,110,115,0);
          let internetl = false;
          let nativemoduleB: Array<any> = [942, 916, 670];
          let singaporeb = String.fromCharCode(99,108,101,97,110,95,101,95,50,49,0);
         annera /= Math.max(3, 2);
         internetl = singaporeb.length <= 40;
         nativemoduleB.push(((internetl ? 3 : 4) | 2));
         singaporeb += `${1 >> (Math.min(3, nativemoduleB.length))}`;
         annerF += `${libimagepipelineX.length}`;
      while (assetsW) {
          let albumM = 1;
          let promotion9 = 2.0;
         assetsW = !annerF.includes(`${assetsW}`);
         albumM += 3 ^ albumM;
         promotion9 /= Math.max(albumM / (Math.max(parseInt(`${promotion9}`), 2)), 5);
         break;
      }
         watchC += `${(String.fromCharCode(112,0) == watchC ? (assetsW ? 3 : 4) : watchC.length)}`;
          let assetsk: Map<any, any> = new Map([[String.fromCharCode(118,97,114,105,110,102,111,95,49,95,57,56,0),95], [String.fromCharCode(115,105,110,101,119,105,110,95,53,95,55,56,0),64]]);
         annerF = `${parseInt(`${annera}`) | assetsk.size}`;
      let libreanimated5 = String.fromCharCode(120,54,120,103,52,104,102,0) == annerF;
      do {
          let libjsik = 1;
         annerF += `${2 >> (Math.min(2, annerF.length))}`;
         libjsik += 2;
         if (libreanimated5) {
            break;
         }
      } while ((annerF.startsWith(`${annera}`)) && libreanimated5);
      for (let v = 0; v < 1; v++) {
         annera -= parseInt(`${annera}`);
      }
         watchC += `${annerF.length | 1}`;
      for (let n = 0; n < 2; n++) {
         assetsW = !watchC.includes(`${assetsW}`);
      }
       let main_yG = 0.0;
      let iconclosewhiteJ = annerF == String.fromCharCode(104,54,54,50,48,0);
      do {
         annerF = `${libimagepipelineX.length}`;
         if (iconclosewhiteJ) {
            break;
         }
      } while (iconclosewhiteJ && (annerF.endsWith(`${assetsW}`)));
      if (1 <= (4 - watchC.length)) {
         watchC += `${libimagepipelineX.length}`;
      }
       let found5 = String.fromCharCode(109,95,56,51,95,111,118,101,114,104,101,97,100,0);
      if (!found5.includes(watchC)) {
         found5 = "1";
      }
      if (!watchC.includes(`${assetsW}`)) {
          let videovarA: Array<any> = [String.fromCharCode(108,95,50,54,95,117,109,111,116,105,111,110,0), String.fromCharCode(109,95,51,48,95,98,97,110,0), String.fromCharCode(99,95,55,57,95,114,101,116,0)];
          let defaultlogoj = String.fromCharCode(111,95,48,95,115,104,111,119,115,0);
          let e_countJ = String.fromCharCode(102,95,54,95,99,111,112,105,101,115,0);
         assetsW = (parseInt(`${annera}`) * annerF.length) == 46;
         videovarA.push(defaultlogoj.length);
         defaultlogoj = `${3 << (Math.min(1, videovarA.length))}`;
         e_countJ = `${(String.fromCharCode(99,0) == e_countJ ? videovarA.length : e_countJ.length)}`;
      }
      memberX.push(filter0.length / (Math.max(annerF.length, 6)));
      break;
   }
      linek -= parseFloat(`${privatechatbgQ.length << (Math.min(Math.abs(2), 4))}`);
       let homeD = String.fromCharCode(114,95,48,95,108,111,99,97,116,101,0);
      for (let c = 0; c < 1; c++) {
          let graphF = String.fromCharCode(99,118,99,95,97,95,52,51,0);
          let applem = 2.0;
          let basketballmatchdetailbgw = true;
          let interstitialt = 5.0;
          let iconadslinkP = 3;
         homeD = `${parseInt(`${interstitialt}`) % 1}`;
         graphF = `${(parseInt(`${applem}`) >> (Math.min(2, Math.abs((basketballmatchdetailbgw ? 2 : 2)))))}`;
         applem += 2;
         interstitialt += parseFloat(`${iconadslinkP | 2}`);
         iconadslinkP += graphF.length * 1;
      }
         homeD += `${homeD.length / (Math.max(2, 7))}`;
         homeD += `${homeD.length}`;
      delegate_kpD.push(privatechatbgQ.length ^ filter0.length);
       let step3: Array<any> = [577, 194, 871];
       let elementsL = 2.0;
         elementsL -= 1;
      if (elementsL <= 4.31) {
          let disconnectedlogoq = 0.0;
          let unselectedX = true;
          let catalog5 = 1.0;
          let watchj = 0;
         step3 = [parseInt(`${disconnectedlogoq}`)];
         disconnectedlogoq /= Math.max(5, parseInt(`${catalog5}`));
         unselectedX = watchj < 4 && !unselectedX;
         catalog5 -= ((unselectedX ? 5 : 1) >> (Math.min(Math.abs(parseInt(`${catalog5}`)), 2)));
         watchj %= Math.max(parseInt(`${catalog5}`), 5);
      }
         step3 = [parseInt(`${elementsL}`)];
      for (let v = 0; v < 3; v++) {
          let teamdetailsbgA = 0.0;
          let iconlogout0: Array<any> = [String.fromCharCode(101,120,112,114,101,115,115,105,111,110,95,49,95,56,49,0), String.fromCharCode(98,101,110,101,102,105,99,105,97,114,121,95,101,95,55,48,0), String.fromCharCode(100,101,99,111,109,112,114,101,115,115,95,51,95,57,54,0)];
          let libfbjnic = 3.0;
          let regengJ = 5.0;
          let debugm = 5.0;
         step3 = [step3.length & 2];
         teamdetailsbgA /= Math.max(parseFloat(`${parseInt(`${teamdetailsbgA}`) | 1}`), 1);
         iconlogout0.push(3 & parseInt(`${libfbjnic}`));
         libfbjnic -= parseFloat(`${iconlogout0.length % (Math.max(10, parseInt(`${debugm}`)))}`);
         regengJ += 2;
         debugm -= parseFloat(`${3 / (Math.max(4, iconlogout0.length))}`);
      }
          let greenarrowupJ = 0.0;
         elementsL /= Math.max(4, 3);
         greenarrowupJ += 2;
      while (1 == (step3.length / (Math.max(4, 2))) && 1.28 == (elementsL / 2.15)) {
          let soundU = String.fromCharCode(112,114,102,116,95,102,95,50,55,0);
          let giftr = 1.0;
          let rewardvideom = 2.0;
         elementsL -= parseInt(`${giftr}`) - 1;
         soundU += `${(soundU == String.fromCharCode(102,0) ? parseInt(`${rewardvideom}`) : soundU.length)}`;
         giftr -= parseFloat(`${parseInt(`${rewardvideom}`)}`);
         break;
      }
      textlayoutmanagerc = `${parseInt(`${muted_}`)}`;
   if (livey.get(`${delegate_kpD.length}`) != null) {
      livey.set(`${kuaishouY.size}`, kuaishouY.size >> (Math.min(Math.abs(2), 1)));
   }
      helper_ += parseFloat(`${parseInt(`${o_countl}`)}`);
    setRefreshing(false);
  };

  const refreshUserState = async () => {
       let iconviewergifA: Array<any> = [672, 421, 553];
    let sinat = 0.0;
    let connectionX = String.fromCharCode(109,112,101,103,117,116,105,108,115,95,110,95,56,56,0);
    let materiale: Array<any> = [278, 461];
    let profileframee = String.fromCharCode(114,95,57,54,95,116,97,103,103,101,100,0);
    let largeD = 0.0;
    let castB = 0.0;
    let activityJ: Map<any, any> = new Map([[String.fromCharCode(110,101,103,95,50,95,55,56,0),String.fromCharCode(111,116,104,101,114,119,105,115,101,95,48,95,51,53,0)], [String.fromCharCode(104,95,53,52,95,110,101,119,108,105,110,101,0),String.fromCharCode(111,95,57,48,95,113,112,113,115,99,97,108,101,0)], [String.fromCharCode(121,95,51,51,95,112,97,103,0),String.fromCharCode(101,108,115,95,101,95,56,54,0)]]);
    let airbnbstarselectedm = String.fromCharCode(105,95,50,52,95,114,101,115,97,109,112,108,101,114,0);
    let unewsH = false;
    let forwardJ = true;
    let binddatase = 4;
    let proxyV = String.fromCharCode(105,95,53,53,95,109,99,100,101,99,0);
   for (let n = 0; n < 3; n++) {
      sinat *= parseFloat(`${1}`);
   }
   let penaltyshootnogoalk = unewsH ? !unewsH : unewsH;
   do {
      unewsH = !unewsH && iconviewergifA.length <= 10;
      if (penaltyshootnogoalk) {
         break;
      }
   } while (penaltyshootnogoalk && (unewsH && profileframee.length == 4));
       let imageactionlive_ = String.fromCharCode(114,116,109,112,101,95,107,95,57,0);
      while (imageactionlive_.startsWith(imageactionlive_)) {
         imageactionlive_ = `${imageactionlive_.length ^ imageactionlive_.length}`;
         break;
      }
      let commenth = String.fromCharCode(117,101,100,105,0) == imageactionlive_;
      do {
          let reddownarrowM = String.fromCharCode(99,108,97,115,115,105,99,95,122,95,53,52,0);
          let ballA: Array<any> = [456, 93, 697];
          let codeV = 3.0;
         imageactionlive_ += `${(imageactionlive_ == String.fromCharCode(116,0) ? imageactionlive_.length : parseInt(`${codeV}`))}`;
         reddownarrowM = `${(reddownarrowM == String.fromCharCode(71,0) ? ballA.length : reddownarrowM.length)}`;
         ballA = [3];
         codeV *= (reddownarrowM == String.fromCharCode(111,0) ? reddownarrowM.length : ballA.length);
         if (commenth) {
            break;
         }
      } while ((!imageactionlive_.startsWith(imageactionlive_)) && commenth);
      if (3 == imageactionlive_.length) {
         imageactionlive_ = `${imageactionlive_.length ^ imageactionlive_.length}`;
      }
      largeD += parseFloat(`${2 << (Math.min(1, profileframee.length))}`);
   while (unewsH) {
      largeD *= parseFloat(`${3}`);
      break;
   }
   for (let t = 0; t < 2; t++) {
       let telemetryk: Map<any, any> = new Map([[String.fromCharCode(117,95,51,57,95,110,117,109,115,0),String.fromCharCode(110,105,99,101,95,102,95,56,0)], [String.fromCharCode(116,105,109,101,98,97,115,101,95,114,95,57,57,0),String.fromCharCode(115,102,114,97,109,101,95,50,95,54,56,0)]]);
       let updatesD = String.fromCharCode(109,97,105,110,95,116,95,57,53,0);
      for (let t = 0; t < 2; t++) {
         telemetryk.set(updatesD, (updatesD == String.fromCharCode(87,0) ? telemetryk.size : updatesD.length));
      }
      for (let t = 0; t < 2; t++) {
          let clockH = String.fromCharCode(99,114,105,116,105,99,97,108,115,101,99,116,105,111,110,95,122,95,57,55,0);
          let arrowc: Array<any> = [453, 635, 41];
          let hoverR: Array<any> = [482, 705, 974];
          let condensede = 0.0;
          let headerT = String.fromCharCode(100,101,112,116,104,95,53,95,52,56,0);
         updatesD = `${2 ^ telemetryk.size}`;
         clockH += `${hoverR.length << (Math.min(Math.abs(2), 4))}`;
         arrowc = [(String.fromCharCode(117,0) == headerT ? parseInt(`${condensede}`) : headerT.length)];
         hoverR = [hoverR.length];
         condensede *= parseFloat(`${hoverR.length}`);
      }
      if ((updatesD.length >> (Math.min(Math.abs(4), 1))) < 2) {
         telemetryk = new Map([[`${telemetryk.size}`, 1]]);
      }
         updatesD = `${telemetryk.size << (Math.min(updatesD.length, 3))}`;
          let hoverA = String.fromCharCode(97,95,54,57,95,116,114,97,110,115,102,101,114,0);
          let edit1 = 2.0;
         telemetryk = new Map([[`${telemetryk.size}`, 2 - telemetryk.size]]);
         hoverA = `${hoverA.length}`;
         edit1 *= parseFloat(`${parseInt(`${edit1}`)}`);
       let darkV: Array<any> = [782, 676];
       let iconsaveimagei: Array<any> = [String.fromCharCode(114,101,112,108,121,95,56,95,55,54,0), String.fromCharCode(106,95,57,55,95,100,105,97,108,105,110,103,0)];
      largeD *= parseFloat(`${activityJ.size / 2}`);
   }
   while (airbnbstarselectedm.length < materiale.length) {
       let imagewatchlivea: Map<any, any> = new Map([[String.fromCharCode(109,95,54,55,95,105,100,97,116,97,0),String.fromCharCode(97,95,51,53,95,99,111,109,98,105,110,97,116,111,114,0)], [String.fromCharCode(99,101,114,116,105,102,105,99,97,116,101,95,111,95,57,54,0),String.fromCharCode(108,122,115,115,95,120,95,57,56,0)], [String.fromCharCode(97,115,121,109,95,113,95,54,54,0),String.fromCharCode(118,115,116,97,116,115,95,120,95,54,52,0)]]);
       let eighteeng = String.fromCharCode(116,97,98,108,101,115,95,99,95,55,55,0);
       let liveshareG = String.fromCharCode(99,111,109,109,97,110,100,115,95,116,95,55,49,0);
       let bgvipsportN: Map<any, any> = new Map([[String.fromCharCode(108,95,53,55,95,108,111,111,107,115,0),String.fromCharCode(101,118,101,114,121,111,110,101,95,114,95,49,55,0)], [String.fromCharCode(97,95,51,55,95,101,120,112,105,114,101,100,0),String.fromCharCode(110,95,52,57,95,99,111,111,114,100,0)]]);
       let plusQ: Map<any, any> = new Map([[String.fromCharCode(106,95,57,95,118,97,108,105,100,97,116,105,111,110,115,0),String.fromCharCode(113,95,50,57,95,114,97,105,116,0)], [String.fromCharCode(104,97,110,100,95,99,95,52,55,0),String.fromCharCode(106,111,105,110,116,95,98,95,57,53,0)]]);
      while (3 <= (liveshareG.length & 4)) {
         plusQ = new Map([[`${plusQ.size}`, plusQ.size * 3]]);
         break;
      }
          let middlebrightnessa = false;
          let defaultbasketballbgq = 0;
         bgvipsportN = new Map([[`${plusQ.size}`, plusQ.size ^ eighteeng.length]]);
         middlebrightnessa = defaultbasketballbgq == 27;
         defaultbasketballbgq ^= defaultbasketballbgq >> (Math.min(4, Math.abs(1)));
         plusQ = new Map([[`${imagewatchlivea.size}`, plusQ.size & imagewatchlivea.size]]);
          let close1 = 1.0;
         liveshareG = `${liveshareG.length}`;
         close1 += parseFloat(`${2}`);
      let readg = String.fromCharCode(99,119,121,103,50,95,0) == liveshareG;
      do {
         liveshareG = `${plusQ.size}`;
         if (readg) {
            break;
         }
      } while (((bgvipsportN.size % (Math.max(5, 1))) == 3 || 2 == (5 % (Math.max(1, bgvipsportN.size)))) && readg);
         bgvipsportN = new Map([[`${plusQ.size}`, plusQ.size * eighteeng.length]]);
       let eventsplash3 = false;
       let v_animationy = String.fromCharCode(100,101,115,99,95,55,95,51,49,0);
      airbnbstarselectedm = `${activityJ.size % 2}`;
      break;
   }
       let playercommonQ = 2;
       let whitevideoliveO = 5.0;
       let componentregistryj = String.fromCharCode(99,104,101,98,121,115,104,101,118,95,107,95,50,53,0);
         whitevideoliveO -= (parseFloat(`${String.fromCharCode(54,0) == componentregistryj ? componentregistryj.length : playercommonQ}`));
         playercommonQ %= Math.max(2, 4);
          let scorepopsoundv: Map<any, any> = new Map([[String.fromCharCode(121,95,51,53,95,98,111,120,98,108,117,114,0),649], [String.fromCharCode(112,114,101,115,101,116,115,95,103,95,53,50,0),648], [String.fromCharCode(115,112,101,99,116,114,97,108,95,53,95,54,55,0),188]]);
          let fcdaebecbcbafcdfceaaeccfeacdbV: Map<any, any> = new Map([[String.fromCharCode(102,95,54,51,95,98,111,111,108,0),678], [String.fromCharCode(97,108,103,111,114,95,117,95,51,50,0),312], [String.fromCharCode(114,101,112,108,97,121,101,100,95,50,95,49,56,0),847]]);
         whitevideoliveO *= parseFloat(`${scorepopsoundv.size & fcdaebecbcbafcdfceaaeccfeacdbV.size}`);
         whitevideoliveO += parseFloat(`${playercommonQ}`);
         componentregistryj = "1";
      connectionX = `${(playercommonQ * (unewsH ? 5 : 4))}`;

    const result = await wawaEvent.getUserDetails();

   if (!unewsH) {
       let progressY = 4;
       let chatbotphoto5 = String.fromCharCode(112,114,111,100,117,99,116,105,111,110,95,56,95,57,48,0);
         chatbotphoto5 += `${chatbotphoto5.length}`;
          let ball3 = String.fromCharCode(122,95,55,57,95,102,105,101,108,0);
          let with_dJ: Map<any, any> = new Map([[String.fromCharCode(114,101,112,97,114,101,100,95,98,95,52,57,0),624], [String.fromCharCode(102,114,97,99,116,105,111,110,95,55,95,53,53,0),137]]);
         progressY <<= Math.min(2, Math.abs((String.fromCharCode(102,0) == chatbotphoto5 ? progressY : chatbotphoto5.length)));
         ball3 = `${with_dJ.size & 2}`;
         with_dJ = new Map([[`${with_dJ.size}`, with_dJ.size << (Math.min(ball3.length, 4))]]);
      let smallsoundr = String.fromCharCode(111,95,118,52,121,57,0) == chatbotphoto5;
      do {
         chatbotphoto5 += `${(String.fromCharCode(110,0) == chatbotphoto5 ? chatbotphoto5.length : progressY)}`;
         if (smallsoundr) {
            break;
         }
      } while ((chatbotphoto5.length >= 2) && smallsoundr);
       let defaultfootballbg9 = true;
          let soundf = String.fromCharCode(114,95,55,52,95,99,114,101,97,116,105,110,103,0);
          let description_cA = true;
          let iconsettingH = String.fromCharCode(115,117,98,95,98,95,50,56,0);
         defaultfootballbg9 = (((description_cA ? 16 : soundf.length) / (Math.max(soundf.length, 8))) <= 16);
         description_cA = (iconsettingH.length & iconsettingH.length) > 79;
      let single2 = progressY >= 9451138;
      do {
         progressY -= (2 & (defaultfootballbg9 ? 4 : 1));
         if (single2) {
            break;
         }
      } while (single2 && (!defaultfootballbg9));
      forwardJ = !unewsH;
   }
      largeD *= parseFloat(`${materiale.length}`);
   for (let x = 0; x < 3; x++) {
      profileframee += `${iconviewergifA.length}`;
   }
      activityJ.set(`${largeD}`, parseInt(`${largeD}`) / (Math.max(activityJ.size, 6)));
   if (5 >= airbnbstarselectedm.length) {
      airbnbstarselectedm += `${parseInt(`${castB}`)}`;
   }
   for (let v = 0; v < 1; v++) {
       let profileinactiveu = false;
       let hometeamfieldY = 2;
      while (profileinactiveu) {
         profileinactiveu = 93 <= hometeamfieldY;
         break;
      }
      for (let r = 0; r < 2; r++) {
         hometeamfieldY -= 3 / (Math.max(9, hometeamfieldY));
      }
         profileinactiveu = profileinactiveu && 43 == hometeamfieldY;
          let submits = false;
         profileinactiveu = !profileinactiveu && submits;
          let combinedW = 3;
          let elementsn = true;
          let bingI = String.fromCharCode(99,95,52,53,95,112,111,108,101,0);
         profileinactiveu = !elementsn && bingI.length <= 61;
         combinedW <<= Math.min(3, Math.abs(1 << (Math.min(1, Math.abs(combinedW)))));
         elementsn = 24 <= combinedW;
         profileinactiveu = 69 > hometeamfieldY;
      sinat /= Math.max(3, parseFloat(`${iconviewergifA.length}`));
   }
   let securityb = 8463494.0 <= castB;
   do {
      castB -= 3 + binddatase;
      if (securityb) {
         break;
      }
   } while (((castB - 1.49) == 4.39) && securityb);
    if (result == null) {

   for (let f = 0; f < 2; f++) {
      forwardJ = 9.13 <= largeD;
   }
   while ((2.68 - castB) == 4.31) {
      connectionX = `${(binddatase | (unewsH ? 4 : 5))}`;
      break;
   }
       let matchinactiveV = String.fromCharCode(103,95,56,49,95,107,101,121,0);
         matchinactiveV = `${matchinactiveV.length - 1}`;
      if (matchinactiveV != String.fromCharCode(100,0)) {
         matchinactiveV += `${(String.fromCharCode(79,0) == matchinactiveV ? matchinactiveV.length : matchinactiveV.length)}`;
      }
         matchinactiveV = `${matchinactiveV.length ^ 1}`;
      airbnbstarselectedm += `${2 % (Math.max(2, matchinactiveV.length))}`;
      castB /= Math.max(3, 5);
   while (3.52 >= (3.60 + largeD)) {
       let stationC = 2;
       let stringsV = String.fromCharCode(108,97,117,110,99,104,95,114,95,53,51,0);
       let backiconmaskO = 0.0;
       let homeiconl = String.fromCharCode(104,95,52,95,112,97,114,101,110,116,97,103,101,0);
       let applev: Map<any, any> = new Map([[String.fromCharCode(102,101,110,99,95,106,95,50,49,0),594], [String.fromCharCode(113,95,54,51,95,104,97,108,102,0),764]]);
          let iconbellactivef: Map<any, any> = new Map([[String.fromCharCode(97,116,114,97,99,112,108,117,115,95,120,95,52,49,0),true ], [String.fromCharCode(114,97,103,103,97,98,108,101,95,109,95,49,0),false ], [String.fromCharCode(115,99,104,101,109,101,95,122,95,57,0),true ]]);
         stringsV = "1";
         iconbellactivef = new Map([[`${iconbellactivef.size}`, iconbellactivef.size]]);
      let chatbotphotod = 7173488 >= applev.size;
      do {
         applev.set(`${backiconmaskO}`, 3 | parseInt(`${backiconmaskO}`));
         if (chatbotphotod) {
            break;
         }
      } while (chatbotphotod && ((3 & applev.size) <= 5));
          let libreactnativejnih = String.fromCharCode(117,95,55,53,95,116,114,97,110,115,112,111,115,101,100,0);
          let greyl = 1.0;
         homeiconl = "2";
         libreactnativejnih += `${parseInt(`${greyl}`)}`;
         greyl -= parseFloat(`${1}`);
         stringsV = `${applev.size}`;
         backiconmaskO -= stringsV.length;
      let benefitk = homeiconl.length >= 6107469;
      do {
         homeiconl = `${homeiconl.length >> (Math.min(Math.abs(3), 2))}`;
         if (benefitk) {
            break;
         }
      } while (benefitk && ((5 / (Math.max(3, homeiconl.length))) > 4 || (homeiconl.length - backiconmaskO) > 3.88));
       let textinputa = String.fromCharCode(116,111,100,112,95,52,95,54,53,0);
      while (backiconmaskO < homeiconl.length) {
          let whistle1: Map<any, any> = new Map([[String.fromCharCode(115,95,49,54,95,104,97,115,104,116,97,98,108,101,0),true ], [String.fromCharCode(109,97,115,116,101,114,105,110,103,95,52,95,52,56,0),false ]]);
          let bggradientM = String.fromCharCode(114,101,103,114,101,115,115,95,108,95,55,56,0);
         backiconmaskO += stringsV.length | stationC;
         whistle1.set(bggradientM, whistle1.size);
         bggradientM += `${whistle1.size}`;
         break;
      }
         applev.set(stringsV, parseInt(`${backiconmaskO}`) << (Math.min(stringsV.length, 2)));
      for (let x = 0; x < 1; x++) {
          let predictionbannershared6 = String.fromCharCode(121,95,56,48,95,97,112,112,108,105,101,114,0);
          let splashn = String.fromCharCode(112,95,52,51,95,116,119,111,119,97,121,0);
          let downloadingI = String.fromCharCode(105,115,115,117,101,100,95,101,95,49,57,0);
          let iconpipshrinkn: Array<any> = [75, 22];
         textinputa += `${(String.fromCharCode(87,0) == downloadingI ? downloadingI.length : applev.size)}`;
         predictionbannershared6 = `${(String.fromCharCode(50,0) == splashn ? splashn.length : iconpipshrinkn.length)}`;
         iconpipshrinkn = [(splashn == String.fromCharCode(107,0) ? predictionbannershared6.length : splashn.length)];
      }
      if (textinputa.length == 4) {
         textinputa += `${(String.fromCharCode(111,0) == homeiconl ? homeiconl.length : textinputa.length)}`;
      }
         stationC *= parseInt(`${backiconmaskO}`);
      if (Array.from(applev.keys()).includes(`${stationC}`)) {
         applev.set(`${textinputa}`, textinputa.length);
      }
          let refreshU = false;
          let logouserp = 1.0;
          let reactnativejsa = String.fromCharCode(115,113,108,105,116,101,115,101,115,115,105,111,110,95,50,95,53,56,0);
         homeiconl += `${(stringsV == String.fromCharCode(79,0) ? stringsV.length : stationC)}`;
         refreshU = logouserp <= 85.38 || reactnativejsa == String.fromCharCode(90,0);
         logouserp += parseFloat(`${3}`);
         reactnativejsa = `${reactnativejsa.length}`;
          let default_8bL = 4;
          let less6 = String.fromCharCode(109,95,55,95,115,112,108,105,116,116,101,100,0);
         stringsV += `${applev.size}`;
         default_8bL <<= Math.min(Math.abs(1 / (Math.max(7, default_8bL))), 2);
         less6 = `${default_8bL}`;
      largeD /= Math.max(parseFloat(`${applev.size % (Math.max(3, 4))}`), 4);
      break;
   }
   if (3 <= connectionX.length) {
      airbnbstarselectedm = "3";
   }
       let calendarZ = String.fromCharCode(116,95,49,49,95,100,121,110,108,111,97,100,0);
       let downloadingM = String.fromCharCode(107,95,57,95,111,110,101,99,104,0);
       let weibow = 3.0;
      while (2 == downloadingM.length) {
          let chinasames = 1.0;
         weibow /= Math.max((calendarZ == String.fromCharCode(77,0) ? calendarZ.length : parseInt(`${chinasames}`)), 1);
         break;
      }
      for (let z = 0; z < 1; z++) {
         downloadingM += "3";
      }
      for (let z = 0; z < 2; z++) {
         weibow -= parseInt(`${weibow}`) | downloadingM.length;
      }
         downloadingM += `${calendarZ.length}`;
         weibow += downloadingM.length ^ 1;
          let tailD = String.fromCharCode(101,114,97,115,117,114,101,95,122,95,51,57,0);
          let iconuserj = 0.0;
          let themen = 4.0;
         calendarZ += `${tailD.length}`;
         tailD += `${parseInt(`${iconuserj}`) ^ parseInt(`${themen}`)}`;
         iconuserj *= 2;
         themen += parseFloat(`${parseInt(`${iconuserj}`) << (Math.min(5, Math.abs(parseInt(`${themen}`))))}`);
       let overlayM: Array<any> = [450, 313];
       let recommendation0: Array<any> = [708, 61];
      let fileM = weibow <= 7055419.0;
      do {
         weibow -= calendarZ.length % (Math.max(2, 5));
         if (fileM) {
            break;
         }
      } while (fileM && ((4.66 * weibow) >= 1.87 && (overlayM.length * weibow) >= 4.66));
         recommendation0 = [overlayM.length * 3];
      largeD /= Math.max(parseFloat(`${parseInt(`${weibow}`) + 2}`), 4);
      return;
    }

    await dispatch(updateUserAuth(result));

   if (2 <= (materiale.length + parseInt(`${largeD}`)) && (materiale.length & 2) <= 5) {
      materiale = [((forwardJ ? 2 : 5) - (unewsH ? 2 : 5))];
   }
   let sharedl = materiale.length >= 9390816;
   do {
      materiale.push(((unewsH ? 3 : 4) << (Math.min(Math.abs(parseInt(`${sinat}`)), 3))));
      if (sharedl) {
         break;
      }
   } while ((1 > activityJ.size) && sharedl);
   while (!profileframee.endsWith(`${castB}`)) {
       let ewardedC = true;
      let aboutH = ewardedC ? !ewardedC : ewardedC;
      do {
         ewardedC = (ewardedC ? !ewardedC : ewardedC);
         if (aboutH) {
            break;
         }
      } while (aboutH && (ewardedC));
      for (let g = 0; g < 2; g++) {
         ewardedC = !ewardedC;
      }
         ewardedC = !ewardedC;
      profileframee += `${3 << (Math.min(3, airbnbstarselectedm.length))}`;
      break;
   }
   for (let c = 0; c < 1; c++) {
       let logo7 = true;
       let downR = 3;
       let overlay9 = String.fromCharCode(114,101,100,117,99,101,114,95,118,95,51,52,0);
       let tumbnailL = String.fromCharCode(114,95,48,95,115,117,98,102,105,101,108,100,115,0);
       let w_animationj = false;
       let security_ = String.fromCharCode(117,95,54,55,95,112,111,115,108,105,115,116,0);
       let iconnotificationnewI = String.fromCharCode(97,117,114,97,95,54,95,56,0);
       let eyeopenh = String.fromCharCode(110,95,49,50,95,104,99,104,97,99,104,97,0);
      let uploadt = 8620469 <= downR;
      do {
          let nextf: Map<any, any> = new Map([[String.fromCharCode(119,95,53,55,95,97,101,115,111,112,116,0),146], [String.fromCharCode(113,95,49,56,95,105,110,99,114,101,97,115,105,110,103,0),461], [String.fromCharCode(99,95,54,49,95,99,97,112,116,117,114,105,110,103,0),539]]);
          let upload1 = 2.0;
         downR >>= Math.min(Math.abs(1), 1);
         nextf.set(`${upload1}`, parseInt(`${upload1}`));
         if (uploadt) {
            break;
         }
      } while (uploadt && (!iconnotificationnewI.endsWith(`${downR}`)));
         iconnotificationnewI += "3";
       let iconsettingo: Map<any, any> = new Map([[String.fromCharCode(111,95,54,57,95,116,114,117,110,99,112,97,115,115,101,115,0),false ], [String.fromCharCode(99,111,100,97,98,108,101,95,120,95,52,50,0),true ]]);
          let sellmathbackgroundu = String.fromCharCode(100,101,99,111,100,101,114,105,110,105,116,95,101,95,55,49,0);
          let dragq = String.fromCharCode(98,114,101,97,100,99,114,117,109,98,115,95,56,95,56,50,0);
         eyeopenh = "3";
         sellmathbackgroundu = `${dragq.length}`;
         dragq = `${sellmathbackgroundu.length / (Math.max(3, dragq.length))}`;
         iconnotificationnewI = `${((logo7 ? 2 : 5))}`;
         iconsettingo = new Map([[tumbnailL, 1]]);
          let wifiroutere = 3.0;
         security_ = `${overlay9.length}`;
         wifiroutere *= parseInt(`${wifiroutere}`) - parseInt(`${wifiroutere}`);
      if (iconsettingo.size <= 2 && 3 <= (iconsettingo.size >> (Math.min(Math.abs(2), 5)))) {
         iconsettingo.set(overlay9, ((logo7 ? 2 : 3) | 3));
      }
      while (!eyeopenh.includes(`${downR}`)) {
          let imagenomoredata4 = String.fromCharCode(120,95,51,50,95,100,101,97,100,105,110,101,0);
          let bgvipxvodn: Map<any, any> = new Map([[String.fromCharCode(104,121,112,111,116,104,101,115,101,115,95,118,95,49,53,0),false ], [String.fromCharCode(108,95,52,55,95,115,99,114,117,98,98,101,114,0),false ], [String.fromCharCode(108,95,49,57,95,110,97,116,105,111,110,97,108,105,116,121,0),true ]]);
          let indexN = true;
         downR %= Math.max(((logo7 ? 3 : 3) & 1), 1);
         imagenomoredata4 = `${(bgvipxvodn.size & (indexN ? 3 : 5))}`;
         bgvipxvodn.set(`${indexN}`, (bgvipxvodn.size - (indexN ? 2 : 3)));
         break;
      }
       let favicon8: Map<any, any> = new Map([[String.fromCharCode(112,97,114,97,115,101,116,95,122,95,50,54,0),String.fromCharCode(121,95,51,57,95,108,111,110,103,116,101,114,109,0)], [String.fromCharCode(97,95,51,52,95,99,111,110,116,97,105,110,101,114,0),String.fromCharCode(109,117,108,116,120,109,117,108,116,95,52,95,53,48,0)]]);
       let eyeopenn: Map<any, any> = new Map([[String.fromCharCode(121,95,49,49,95,105,102,97,109,115,103,0),String.fromCharCode(114,95,55,48,95,109,118,114,101,102,0)], [String.fromCharCode(98,111,117,110,100,97,114,105,101,115,95,56,95,56,57,0),String.fromCharCode(111,118,101,114,108,97,121,115,95,119,95,50,54,0)]]);
      while (3 < (2 * eyeopenn.size) || 5 < (2 * eyeopenn.size)) {
         favicon8 = new Map([[tumbnailL, (overlay9 == String.fromCharCode(101,0) ? overlay9.length : tumbnailL.length)]]);
         break;
      }
       let description_00s = String.fromCharCode(102,95,49,54,95,101,110,100,105,97,110,0);
       let iconsharefriendss = String.fromCharCode(112,111,115,116,114,101,113,117,101,115,116,95,112,95,56,0);
      for (let s = 0; s < 1; s++) {
         tumbnailL = "2";
      }
      iconviewergifA = [(String.fromCharCode(77,0) == airbnbstarselectedm ? parseInt(`${largeD}`) : airbnbstarselectedm.length)];
   }
   while (3.99 >= (sinat * 1.17)) {
      sinat /= Math.max(parseFloat(`${3}`), 5);
      break;
   }
      connectionX = `${parseInt(`${sinat}`) | connectionX.length}`;
       let settings4 = String.fromCharCode(110,95,56,50,95,97,117,116,111,97,114,99,104,105,118,101,0);
       let templateprocessorn: Map<any, any> = new Map([[String.fromCharCode(100,95,52,54,95,116,111,117,112,112,101,114,0),947], [String.fromCharCode(103,105,102,115,95,109,95,56,48,0),794]]);
       let rewardvideoy = 2.0;
         settings4 = `${templateprocessorn.size % (Math.max(7, settings4.length))}`;
      for (let e = 0; e < 1; e++) {
          let diceW = String.fromCharCode(112,95,56,55,95,101,118,97,115,97,112,112,0);
          let shootyesgoalA = 1.0;
         templateprocessorn.set(settings4, parseInt(`${rewardvideoy}`) ^ settings4.length);
         diceW = `${parseInt(`${shootyesgoalA}`)}`;
         shootyesgoalA += 1 * parseInt(`${shootyesgoalA}`);
      }
         templateprocessorn.set(settings4, templateprocessorn.size * settings4.length);
       let baselinea: Map<any, any> = new Map([[String.fromCharCode(102,95,50,48,95,115,112,101,108,108,0),452], [String.fromCharCode(106,101,114,114,111,114,95,117,95,52,52,0),140], [String.fromCharCode(105,115,112,97,116,99,104,95,118,95,56,52,0),183]]);
         settings4 += "3";
      binddatase %= Math.max((String.fromCharCode(98,0) == connectionX ? connectionX.length : materiale.length), 1);
    return;
  };

  return (
    <ScreenContainer>
      <TitleWithBackButtonHeader title="邀请好友奖励" />
      <ScrollView
        refreshControl={
          <RefreshControl refreshing={refreshing} onRefresh={handleRefresh} tintColor="#FAC33D" />
        }
        showsVerticalScrollIndicator={false}
      >
        {/* <TitleWithBackButtonHeader title="邀请好友奖励" /> */}
        <View style={{ paddingHorizontal: 0 }}>
          { }
          <InviteHeader />
          { }
          <InviteCard userState={userState.user!} />
          { }

          <TouchableOpacity
            onPress={() => {
              navigation.navigate("活动规则");
            }}
          >
            <View
              style={{
                flex: 1,
                justifyContent: "center",
                alignItems: "center",
                paddingVertical: 10,
              }}
            >
              <Text
                style={{
                  fontSize: 12,
                  textDecorationLine: "underline",
                  color: "#ffffff",
                }}
              >
                活动规则
              </Text>
            </View>
          </TouchableOpacity>
          { }
        </View>
        <InviteStep />
      </ScrollView>
    </ScreenContainer>
  );
};

const styles = StyleSheet.create({});
