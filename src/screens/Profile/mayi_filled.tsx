import React, { useEffect, useState } from "react";
import {
  View,
  Text,
  StyleSheet,
  TextInput,
  Image,
  RefreshControl,
  ScrollView,
} from "react-native";
import ScreenContainer from "../../components/container/mayi_save";
import { RootStackScreenProps } from "@type/mayi_video_binddatas";
import { useTheme } from "@react-navigation/native";
import { mayi_Libapminsightb } from "@redux/mayi_live_tumbnail";

import TitleWithBackButtonHeader from "../../components/header/mayi_predictiondefault_header";
import axios from "axios";
import { Keyboard } from "react-native";
import {
  API_DOMAIN,
  API_DOMAIN_TEST,
  API_DOMAIN_LOCAL,
} from "@utility/mayi_middleware_apps";
import NetInfo, { NetInfoState } from "@react-native-community/netinfo";

import InviteStep from "./../../components/invite/mayi_iconpointscore_whitevideolive";
import InviteCard from "./../../components/invite/mayi_grey_math";

import InviteHeader from "./../../components/invite/mayi_embed_header";
import { useAppDispatch, useAppSelector, useSelector } from "@hooks/mayi_redirect";
import { TouchableOpacity } from "@gorhom/bottom-sheet";
import { updateUserAuth } from "@redux/actions/mayi_librrc";
import mayi_push from '../../../Umeng/mayi_push';
import { mayi_Injury } from "@api";
import { mayi_Baseline } from "@redux/reducers/mayi_chatroombackground_unlock";


export default ({ navigation }: RootStackScreenProps<"邀请">) => {
  const { colors, textVariants, icons, spacing } = useTheme();

  const userState = useSelector<mayi_Baseline>('userReducer');

  const [refreshing, setRefreshing] = useState(false);
  const dispatch = useAppDispatch();

  
  useEffect(() => {
    mayi_push.userCenterVipInviteViewsAnalytics();
  }, []);
  

  const handleRefresh = async () => {
       let typingR = String.fromCharCode(114,95,57,53,95,117,110,105,110,115,116,97,108,108,0);
    let component0: Array<any> = [498, 163, 899];
    let nativemoduleT = 4.0;
    let penaltymatchiconk = 0;
    let gesturesd = 4.0;
    let description_iK = String.fromCharCode(116,95,54,54,95,99,111,101,114,99,101,0);
    let videovarn: Array<any> = [450, 612];
    let kick2 = String.fromCharCode(115,105,108,101,110,116,95,121,95,56,48,0);
    let blackb = String.fromCharCode(119,95,50,57,95,98,117,102,101,114,0);
    let cricket1 = 4.0;
    let delegate_mT: Array<any> = [59, 378];
    let predictionbutton4 = 5;
    let indicatorF = String.fromCharCode(115,101,116,116,105,103,110,115,95,54,95,55,56,0);
   while (5 > (5 - typingR.length)) {
       let privacyD = 0.0;
       let mapbufferP: Map<any, any> = new Map([[String.fromCharCode(103,101,116,115,103,110,99,116,120,110,111,95,57,95,57,55,0),207], [String.fromCharCode(105,109,112,108,101,109,101,110,116,115,95,98,95,52,0),349]]);
       let iconpipexpandO = 0.0;
       let buildd = String.fromCharCode(119,95,49,52,95,118,101,108,97,112,115,101,100,116,105,109,101,114,0);
          let dplush = 0;
         mapbufferP.set(`${dplush}`, dplush);
      if (5.24 < iconpipexpandO) {
          let expirede = true;
         iconpipexpandO /= Math.max(1, 3);
         expirede = !expirede;
      }
      if (1.88 == (1.0 - iconpipexpandO)) {
          let internetV: Map<any, any> = new Map([[String.fromCharCode(122,95,49,53,95,97,99,99,101,115,115,111,114,105,101,115,0),148], [String.fromCharCode(117,95,51,51,95,102,105,120,115,116,114,105,100,101,0),548], [String.fromCharCode(115,95,56,55,95,102,111,110,116,99,111,110,102,105,103,0),615]]);
          let photoQ = String.fromCharCode(102,95,51,55,95,101,110,117,109,101,114,97,116,105,111,110,0);
          let castA = true;
          let inactiveL = 0;
         buildd += `${buildd.length}`;
         internetV = new Map([[`${castA}`, 1 ^ photoQ.length]]);
         photoQ += `${inactiveL - 3}`;
         inactiveL -= internetV.size;
      }
         iconpipexpandO -= parseInt(`${privacyD}`) ^ 1;
      for (let w = 0; w < 2; w++) {
         iconpipexpandO *= parseInt(`${iconpipexpandO}`);
      }
       let optionsa = 0.0;
       let configv = 0.0;
          let arrowK = String.fromCharCode(120,95,50,53,95,98,115,101,114,118,101,114,0);
         buildd += `${mapbufferP.size}`;
         arrowK = `${(arrowK == String.fromCharCode(103,0) ? arrowK.length : arrowK.length)}`;
      let mapbufferZ = iconpipexpandO <= 5437101.0;
      do {
         iconpipexpandO *= 3 - parseInt(`${iconpipexpandO}`);
         if (mapbufferZ) {
            break;
         }
      } while (((1.19 * iconpipexpandO) < 3.63 || (iconpipexpandO / 1.19) < 5.22) && mapbufferZ);
      for (let k = 0; k < 1; k++) {
         privacyD += parseFloat(`${3 % (Math.max(10, parseInt(`${optionsa}`)))}`);
      }
          let mergerN = 3.0;
          let attributedstringw = true;
         buildd += `${(String.fromCharCode(107,0) == buildd ? buildd.length : parseInt(`${mergerN}`))}`;
         mergerN += (parseFloat(`${(attributedstringw ? 2 : 3)}`));
         optionsa -= parseFloat(`${2 >> (Math.min(4, Math.abs(mapbufferP.size)))}`);
       let singaporea = 5.0;
       let icondefaultthumbnailU = 1.0;
      penaltymatchiconk += videovarn.length | component0.length;
      break;
   }
   if ((5.89 * gesturesd) > 3.85) {
      gesturesd /= Math.max(5, blackb.length + parseInt(`${gesturesd}`));
   }
   let eventf = blackb.length >= 6118886;
   do {
       let dycreatorm = String.fromCharCode(115,112,97,110,95,106,95,50,57,0);
       let textY = String.fromCharCode(119,95,53,53,95,97,110,99,104,111,114,115,0);
       let reward3 = 4;
      for (let y = 0; y < 1; y++) {
          let whiteanimationlive5 = 4.0;
         dycreatorm = `${textY.length / 2}`;
         whiteanimationlive5 *= parseFloat(`${parseInt(`${whiteanimationlive5}`) << (Math.min(Math.abs(parseInt(`${whiteanimationlive5}`)), 1))}`);
      }
       let rankb = String.fromCharCode(120,95,53,54,95,111,114,105,101,110,116,97,116,105,111,110,0);
       let predictionbanner8 = String.fromCharCode(115,104,97,114,97,98,108,101,95,48,95,49,48,48,0);
      for (let w = 0; w < 2; w++) {
         textY = `${textY.length}`;
      }
          let libmapbufferjnib = 4.0;
         reward3 -= 1 | rankb.length;
         libmapbufferjnib -= parseInt(`${libmapbufferjnib}`);
         rankb = `${textY.length}`;
      let iconclose7 = 7954356 >= dycreatorm.length;
      do {
          let subsW = String.fromCharCode(101,95,51,52,95,99,117,116,0);
          let championQ = String.fromCharCode(119,95,54,50,95,99,103,105,109,97,103,101,0);
          let renderM = 1.0;
         dycreatorm = `${championQ.length * 2}`;
         subsW = `${2 | subsW.length}`;
         championQ += "2";
         renderM -= parseFloat(`${3 >> (Math.min(4, Math.abs(parseInt(`${renderM}`))))}`);
         if (iconclose7) {
            break;
         }
      } while ((textY != dycreatorm) && iconclose7);
          let const_g1W = String.fromCharCode(106,95,54,56,95,115,97,116,100,120,0);
          let readc: Map<any, any> = new Map([[String.fromCharCode(101,114,115,105,111,110,95,108,95,54,57,0),false ], [String.fromCharCode(99,108,99,112,95,103,95,51,52,0),false ], [String.fromCharCode(118,95,53,56,95,99,108,111,115,101,100,0),false ]]);
         dycreatorm = `${1 & rankb.length}`;
         const_g1W += `${readc.size}`;
         readc.set(const_g1W, (String.fromCharCode(122,0) == const_g1W ? readc.size : const_g1W.length));
       let pluse = String.fromCharCode(98,95,54,51,95,108,105,98,115,119,114,101,115,97,109,112,108,101,0);
      for (let r = 0; r < 1; r++) {
         predictionbanner8 = `${reward3}`;
      }
      blackb = `${description_iK.length}`;
      if (eventf) {
         break;
      }
   } while ((blackb.length <= nativemoduleT) && eventf);
      blackb = `${kick2.length - 2}`;
      penaltymatchiconk /= Math.max(1, blackb.length / (Math.max(2, 2)));
      nativemoduleT *= parseInt(`${cricket1}`) & 2;
       let screenb = true;
      while (screenb) {
         screenb = !screenb;
         break;
      }
          let pauseW: Array<any> = [826, 418, 123];
          let shielddone3 = String.fromCharCode(115,108,105,100,101,115,104,111,119,95,53,95,51,48,0);
          let gemfilea = String.fromCharCode(107,95,52,55,95,111,110,97,118,99,0);
         screenb = gemfilea == String.fromCharCode(82,0);
         pauseW.push(pauseW.length);
         shielddone3 += `${3 % (Math.max(10, pauseW.length))}`;
         gemfilea += "2";
      let libreactperfloggerjnin = screenb ? !screenb : screenb;
      do {
         screenb = (screenb ? !screenb : !screenb);
         if (libreactperfloggerjnin) {
            break;
         }
      } while (libreactperfloggerjnin && (!screenb));
      kick2 = `${component0.length % 2}`;
   let ewardedt = 5974681.0 >= cricket1;
   do {
      cricket1 *= parseInt(`${cricket1}`);
      if (ewardedt) {
         break;
      }
   } while ((delegate_mT.length >= cricket1) && ewardedt);
   let settingr = nativemoduleT >= 9351474.0;
   do {
      nativemoduleT /= Math.max(3, parseInt(`${gesturesd}`) + delegate_mT.length);
      if (settingr) {
         break;
      }
   } while ((2 > (1 >> (Math.min(4, component0.length))) || (1 & component0.length) > 1) && settingr);
   if (1 == (component0.length << (Math.min(Math.abs(3), 4))) && 3 == (kick2.length << (Math.min(5, component0.length)))) {
       let single_: Map<any, any> = new Map([[String.fromCharCode(106,95,52,57,95,99,111,109,109,117,110,105,99,97,116,111,114,0),333], [String.fromCharCode(103,101,109,102,105,108,101,95,49,95,53,56,0),68]]);
       let closeY = 0.0;
      for (let c = 0; c < 3; c++) {
         single_ = new Map([[`${single_.size}`, parseInt(`${closeY}`)]]);
      }
         closeY /= Math.max(parseFloat(`${1}`), 1);
          let whiteanimationlivev = 5;
          let orientationh = String.fromCharCode(103,95,56,50,95,117,110,119,114,97,112,0);
          let libcxxcomponents6 = 5.0;
         single_.set(`${libcxxcomponents6}`, (String.fromCharCode(76,0) == orientationh ? parseInt(`${libcxxcomponents6}`) : orientationh.length));
         whiteanimationlivev >>= Math.min(5, Math.abs(3));
      let expiredu = 6657744.0 <= closeY;
      do {
          let orangeuparrow5 = String.fromCharCode(121,95,56,48,95,114,101,99,116,0);
         closeY /= Math.max(parseFloat(`${single_.size}`), 4);
         orangeuparrow5 += `${(String.fromCharCode(111,0) == orangeuparrow5 ? orangeuparrow5.length : orangeuparrow5.length)}`;
         if (expiredu) {
            break;
         }
      } while (expiredu && (1.54 >= (closeY / (Math.max(parseFloat(`${single_.size}`), 5))) || (3 & single_.size) >= 1));
      if (2 >= single_.size) {
          let datao = String.fromCharCode(104,95,49,48,95,118,97,114,121,105,110,103,0);
          let smallorangemanG = 0;
          let bridger = String.fromCharCode(108,95,53,95,99,104,97,114,103,101,0);
         single_.set(datao, 1 + smallorangemanG);
         datao += "2";
         smallorangemanG += 3;
      }
          let subtextL: Map<any, any> = new Map([[String.fromCharCode(109,118,115,101,116,95,100,95,55,52,0),String.fromCharCode(100,120,103,105,95,113,95,56,52,0)], [String.fromCharCode(101,103,117,108,97,114,95,101,95,57,54,0),String.fromCharCode(118,111,119,101,108,115,95,98,95,57,48,0)]]);
          let showu: Array<any> = [175, 618, 536];
          let layoutn = 0.0;
         closeY -= parseFloat(`${parseInt(`${closeY}`) | single_.size}`);
         subtextL = new Map([[`${subtextL.size}`, parseInt(`${layoutn}`)]]);
         showu.push(1);
         layoutn += parseFloat(`${1}`);
      kick2 += `${blackb.length}`;
   }
      nativemoduleT += 2 + parseInt(`${nativemoduleT}`);

    setRefreshing(true);

       let subinY = 2;
         subinY |= 3 >> (Math.min(5, Math.abs(subinY)));
         subinY ^= subinY;
       let libswscaleD = String.fromCharCode(113,95,54,49,95,112,117,98,108,105,115,104,0);
      penaltymatchiconk &= parseInt(`${nativemoduleT}`);
   if (5 <= (videovarn.length % 1) && (1 % (Math.max(5, videovarn.length))) <= 1) {
      videovarn = [description_iK.length << (Math.min(5, Math.abs(parseInt(`${nativemoduleT}`))))];
   }
   let pauseg = 7427738 <= blackb.length;
   do {
       let grayQ = 5.0;
       let notificationi = 1.0;
       let reddownarrowP = 0;
       let playlist9 = 2.0;
       let launcherf = false;
      for (let q = 0; q < 1; q++) {
         playlist9 *= parseFloat(`${1 - parseInt(`${playlist9}`)}`);
      }
         playlist9 *= parseFloat(`${2}`);
      while ((2.10 + grayQ) < 3.33 || 2.10 < (playlist9 + grayQ)) {
         grayQ += parseInt(`${grayQ}`) >> (Math.min(1, Math.abs(1)));
         break;
      }
      if ((notificationi + parseFloat(`${reddownarrowP}`)) >= 1.20 && 1.20 >= (parseFloat(`${reddownarrowP}`) + notificationi)) {
         reddownarrowP += 1;
      }
         launcherf = notificationi < grayQ;
      let mappingc = launcherf ? !launcherf : launcherf;
      do {
         launcherf = 9.27 <= grayQ;
         if (mappingc) {
            break;
         }
      } while (mappingc && ((3.77 - playlist9) == 4.24));
          let membershipM = 4;
          let rncore0: Map<any, any> = new Map([[String.fromCharCode(109,108,115,100,95,51,95,53,52,0),867], [String.fromCharCode(101,95,56,52,95,100,111,117,98,108,101,105,110,116,115,116,114,0),796], [String.fromCharCode(98,95,50,50,95,117,112,108,111,97,100,105,110,103,0),928]]);
          let historyD: Map<any, any> = new Map([[String.fromCharCode(115,121,110,99,112,111,105,110,116,95,118,95,52,49,0),String.fromCharCode(116,111,103,103,108,101,95,99,95,49,0)], [String.fromCharCode(102,95,56,54,95,97,100,100,101,100,0),String.fromCharCode(112,95,52,49,95,99,97,108,108,115,0)], [String.fromCharCode(111,95,50,57,95,101,114,114,0),String.fromCharCode(101,118,101,114,121,95,111,95,49,48,48,0)]]);
         notificationi -= parseFloat(`${reddownarrowP ^ 1}`);
         membershipM &= rncore0.size - membershipM;
         rncore0 = new Map([[`${historyD.size}`, historyD.size | 1]]);
      if (4.33 < (grayQ / 4.16) || 4.16 < grayQ) {
          let baseV = 4;
          let armvaK = false;
         grayQ /= Math.max(1, (parseInt(`${grayQ}`) / (Math.max(9, (launcherf ? 4 : 5)))));
         baseV += baseV | 3;
         armvaK = 9 >= baseV || armvaK;
      }
       let pushU = 2.0;
       let rankd = 1.0;
          let expiredS: Array<any> = [String.fromCharCode(102,101,101,100,98,97,99,107,95,53,95,55,52,0), String.fromCharCode(101,110,116,114,111,112,121,109,118,95,114,95,51,0)];
         rankd *= parseFloat(`${1 | parseInt(`${grayQ}`)}`);
         expiredS = [expiredS.length];
       let resendb = 4.0;
       let sentryt = 0.0;
          let turnZ = String.fromCharCode(110,95,49,55,0);
          let reportn = 3;
          let nalyticsN = String.fromCharCode(99,97,117,115,101,95,120,95,49,56,0);
         launcherf = resendb > sentryt;
         turnZ = `${nalyticsN.length - 1}`;
         reportn |= nalyticsN.length | 2;
      let mbnativey = playlist9 >= 7531656.0;
      do {
         playlist9 += parseFloat(`${reddownarrowP}`);
         if (mbnativey) {
            break;
         }
      } while ((!launcherf) && mbnativey);
      for (let f = 0; f < 1; f++) {
         playlist9 += parseFloat(`${1}`);
      }
         reddownarrowP |= (parseInt(`${grayQ}`) % (Math.max(10, (launcherf ? 1 : 2))));
      blackb = `${reddownarrowP}`;
      if (pauseg) {
         break;
      }
   } while (pauseg && (4 == (5 >> (Math.min(1, delegate_mT.length)))));
   while ((videovarn.length << (Math.min(kick2.length, 5))) == 1 || (videovarn.length << (Math.min(Math.abs(1), 4))) == 1) {
      kick2 += `${typingR.length}`;
      break;
   }
       let typingO = 4;
       let philippines7 = String.fromCharCode(98,119,114,105,116,101,95,48,95,54,0);
       let xvodx = true;
      while (philippines7.length >= 5 && xvodx) {
         philippines7 = `${((xvodx ? 3 : 1))}`;
         break;
      }
      while (3 > (typingO / (Math.max(philippines7.length, 2)))) {
         philippines7 = `${typingO}`;
         break;
      }
         typingO >>= Math.min(Math.abs(typingO), 1);
          let crownF = String.fromCharCode(116,101,99,104,110,111,108,111,103,121,95,55,95,52,56,0);
          let kickp = true;
         philippines7 = `${typingO % (Math.max(7, philippines7.length))}`;
         crownF += `${crownF.length}`;
         kickp = !crownF.includes(`${kickp}`);
          let iconqqs = String.fromCharCode(104,95,52,56,95,105,110,99,114,101,97,115,105,110,103,0);
          let cornershoot7 = String.fromCharCode(120,95,49,57,95,108,105,98,114,97,114,105,101,115,0);
          let notificationfillemptyX: Array<any> = [465, 108, 824];
         philippines7 = "2";
         iconqqs += `${iconqqs.length % 2}`;
         cornershoot7 = `${iconqqs.length}`;
         notificationfillemptyX.push(iconqqs.length - notificationfillemptyX.length);
          let libfollyO = 1.0;
          let configN = 2;
          let w_countm: Array<any> = [659, 920, 394];
         philippines7 += `${philippines7.length * typingO}`;
         libfollyO *= parseFloat(`${parseInt(`${libfollyO}`) | 2}`);
         configN >>= Math.min(Math.abs(w_countm.length >> (Math.min(2, Math.abs(configN)))), 3);
         w_countm = [2];
      let infok = philippines7.length >= 5033994;
      do {
         philippines7 = `${typingO}`;
         if (infok) {
            break;
         }
      } while (((2 * philippines7.length) >= 3 || 1 >= (2 * typingO)) && infok);
       let bridgec: Array<any> = [999, 694, 957];
       let plays: Array<any> = [264, 68, 685];
       let baseT = false;
      nativemoduleT += delegate_mT.length + 1;
   while (5 >= (kick2.length - 5) || 4 >= (parseInt(`${gesturesd}`) / 5)) {
       let windH = String.fromCharCode(97,95,48,95,114,101,112,101,97,116,101,100,0);
       let questU = String.fromCharCode(114,95,54,51,95,97,115,121,110,99,100,105,115,112,108,97,121,107,105,116,0);
       let related4 = true;
       let notifications = 2.0;
       let videoh = String.fromCharCode(113,95,49,95,115,116,97,99,107,101,100,0);
      for (let l = 0; l < 1; l++) {
          let taiwanM = 3.0;
          let headerz = String.fromCharCode(105,95,49,56,95,97,110,110,111,116,97,116,101,0);
          let videoc = 3.0;
          let saveX = String.fromCharCode(114,95,51,56,95,97,116,111,109,105,99,0);
         questU += `${(headerz == String.fromCharCode(77,0) ? headerz.length : parseInt(`${notifications}`))}`;
         taiwanM += parseFloat(`${parseInt(`${videoc}`)}`);
         videoc *= parseFloat(`${parseInt(`${taiwanM}`)}`);
         saveX = `${parseInt(`${taiwanM}`)}`;
      }
          let predictionbuttonW = 4.0;
          let stats5 = String.fromCharCode(118,95,56,56,95,100,101,116,101,99,116,99,108,111,115,101,0);
         windH += `${((related4 ? 5 : 1))}`;
         predictionbuttonW /= Math.max(stats5.length, 1);
         stats5 += `${stats5.length << (Math.min(Math.abs(1), 3))}`;
         videoh += `${videoh.length % 3}`;
         windH = `${3 >> (Math.min(3, videoh.length))}`;
       let rank6 = String.fromCharCode(113,95,52,50,95,116,109,112,108,0);
       let mbridgeL = String.fromCharCode(107,95,57,57,95,110,99,111,109,105,110,103,0);
       let gestureH = String.fromCharCode(109,102,104,100,95,109,95,55,54,0);
      if (!related4) {
         notifications += windH.length + 1;
      }
       let defaultroombgC = String.fromCharCode(113,117,97,110,116,105,116,121,95,108,95,53,56,0);
       let episodes_ = String.fromCharCode(114,101,99,111,109,109,101,110,100,95,120,95,49,50,0);
       let strn = String.fromCharCode(113,105,110,100,101,120,95,98,95,53,52,0);
       let qaagC = String.fromCharCode(112,97,114,101,110,116,104,101,115,101,115,95,115,95,50,52,0);
      if (qaagC != strn) {
         strn = `${rank6.length & videoh.length}`;
      }
      if (3 < qaagC.length && 3 < mbridgeL.length) {
         mbridgeL = "2";
      }
      if (5 <= questU.length) {
         related4 = !questU.includes(`${related4}`);
      }
      while (mbridgeL.length < episodes_.length) {
         mbridgeL += `${questU.length * qaagC.length}`;
         break;
      }
      while (4 <= defaultroombgC.length) {
         related4 = qaagC.length == 19;
         break;
      }
      let commons = 6369365 >= questU.length;
      do {
          let desc4: Map<any, any> = new Map([[String.fromCharCode(99,111,109,112,108,101,116,105,111,110,95,100,95,53,55,0),44], [String.fromCharCode(112,111,111,108,95,104,95,54,55,0),51]]);
          let champion8 = String.fromCharCode(119,97,116,99,104,95,111,95,57,53,0);
          let membership9: Map<any, any> = new Map([[String.fromCharCode(107,95,56,54,95,108,104,97,115,104,0),false ], [String.fromCharCode(100,121,110,108,111,99,107,95,116,95,56,0),true ]]);
          let whitel = 2.0;
         questU = `${strn.length}`;
         desc4.set(`${whitel}`, parseInt(`${whitel}`) - 1);
         champion8 = `${membership9.size % 1}`;
         membership9.set(champion8, 2 * desc4.size);
         if (commons) {
            break;
         }
      } while ((episodes_ == String.fromCharCode(87,0) || questU == String.fromCharCode(82,0)) && commons);
      kick2 = `${3 >> (Math.min(Math.abs(parseInt(`${gesturesd}`)), 2))}`;
      break;
   }
      description_iK = `${parseInt(`${gesturesd}`)}`;
   let klevint = 5728654 <= videovarn.length;
   do {
      videovarn.push(3);
      if (klevint) {
         break;
      }
   } while (klevint && (!component0.includes(videovarn.length)));
   let searchbarR = 4975921 <= penaltymatchiconk;
   do {
       let ballP = String.fromCharCode(99,116,105,118,105,116,121,95,121,95,55,49,0);
       let iconarrowrightorangeJ: Array<any> = [692, 83];
       let predictionbannershared1: Map<any, any> = new Map([[String.fromCharCode(97,108,112,97,95,52,95,57,56,0),371], [String.fromCharCode(112,108,97,116,102,111,114,109,115,95,55,95,53,54,0),823], [String.fromCharCode(109,101,109,110,95,107,95,53,56,0),244]]);
      for (let c = 0; c < 3; c++) {
         ballP += "1";
      }
      if (1 >= iconarrowrightorangeJ.length) {
         iconarrowrightorangeJ.push(ballP.length & 3);
      }
         iconarrowrightorangeJ.push((String.fromCharCode(74,0) == ballP ? ballP.length : predictionbannershared1.size));
      let favorites = iconarrowrightorangeJ.length >= 8960017;
      do {
          let commonu = String.fromCharCode(100,99,98,108,111,99,107,95,48,95,52,51,0);
          let filledk = 0;
         iconarrowrightorangeJ = [filledk];
         commonu = "2";
         filledk &= commonu.length;
         if (favorites) {
            break;
         }
      } while (favorites && (ballP.endsWith(`${iconarrowrightorangeJ.length}`)));
      for (let n = 0; n < 3; n++) {
         predictionbannershared1 = new Map([[`${predictionbannershared1.size}`, iconarrowrightorangeJ.length | 1]]);
      }
          let register_abS = false;
          let switch_zhA = 2.0;
         iconarrowrightorangeJ.push(ballP.length);
         register_abS = switch_zhA == 37.5;
         switch_zhA /= Math.max((parseInt(`${switch_zhA}`) * (register_abS ? 1 : 5)), 2);
         predictionbannershared1.set(ballP, ballP.length);
      for (let a = 0; a < 3; a++) {
         predictionbannershared1 = new Map([[`${predictionbannershared1.size}`, predictionbannershared1.size]]);
      }
         ballP = `${ballP.length}`;
      penaltymatchiconk += ballP.length | 2;
      if (searchbarR) {
         break;
      }
   } while (searchbarR && (2 > (2 & penaltymatchiconk) && (penaltymatchiconk & 2) > 5));
      kick2 = `${typingR.length}`;
       let configf = 3;
       let profilepicT = String.fromCharCode(100,105,115,99,111,118,101,114,121,95,55,95,53,50,0);
       let sellt = String.fromCharCode(115,105,110,107,115,95,117,95,52,50,0);
      for (let v = 0; v < 3; v++) {
          let trash7 = String.fromCharCode(108,95,51,50,95,99,104,97,114,97,99,116,101,114,0);
         configf -= 3;
         trash7 = `${trash7.length}`;
      }
         profilepicT += `${(String.fromCharCode(72,0) == profilepicT ? configf : profilepicT.length)}`;
      for (let d = 0; d < 1; d++) {
         profilepicT += `${profilepicT.length - 2}`;
      }
      for (let h = 0; h < 3; h++) {
          let megaphoneX: Map<any, any> = new Map([[String.fromCharCode(110,95,57,55,95,103,101,116,99,117,116,0),false ], [String.fromCharCode(112,105,112,101,108,105,110,101,95,107,95,49,51,0),false ], [String.fromCharCode(111,112,116,97,114,103,95,48,95,57,57,0),true ]]);
          let grayc = String.fromCharCode(112,95,52,57,95,99,111,110,115,116,113,112,0);
          let auto_qt: Array<any> = [28, 67];
          let libfileY: Map<any, any> = new Map([[String.fromCharCode(98,95,49,51,95,115,97,109,112,108,101,115,0),String.fromCharCode(105,110,115,112,101,99,116,95,114,95,52,54,0)], [String.fromCharCode(101,95,50,53,95,108,97,110,103,115,0),String.fromCharCode(99,114,97,115,104,108,121,116,105,99,115,95,99,95,51,50,0)]]);
         configf %= Math.max(5, 2 << (Math.min(1, sellt.length)));
         megaphoneX = new Map([[`${libfileY.size}`, libfileY.size]]);
         grayc = `${auto_qt.length << (Math.min(Math.abs(1), 1))}`;
         auto_qt = [2];
      }
      let langkeyU = profilepicT.length >= 7147858;
      do {
         profilepicT += `${1 * profilepicT.length}`;
         if (langkeyU) {
            break;
         }
      } while ((sellt.startsWith(profilepicT)) && langkeyU);
         configf >>= Math.min(1, Math.abs((sellt == String.fromCharCode(70,0) ? sellt.length : configf)));
       let gpayJ = 3.0;
      let package_fdB = profilepicT == String.fromCharCode(117,110,115,111,113,117,102,0);
      do {
         profilepicT += `${2 ^ profilepicT.length}`;
         if (package_fdB) {
            break;
         }
      } while (package_fdB && (profilepicT.length <= 2 && 2 <= sellt.length));
      let bodanN = 9221653 >= configf;
      do {
          let flyer8 = 3;
          let constantsQ = String.fromCharCode(106,117,115,116,105,102,105,99,97,116,105,111,110,95,104,95,50,56,0);
         configf -= sellt.length | 1;
         flyer8 -= (String.fromCharCode(79,0) == constantsQ ? flyer8 : constantsQ.length);
         if (bodanN) {
            break;
         }
      } while ((!profilepicT.startsWith(`${configf}`)) && bodanN);
      delegate_mT.push(2);
    await refreshUserState();

      videovarn.push(parseInt(`${gesturesd}`) - 3);
   if ((5 ^ kick2.length) <= 4 || (kick2.length ^ 5) <= 4) {
       let zoom5 = 3;
       let searchbaro = 3.0;
       let values3 = 2.0;
      while (values3 > 3.95) {
         searchbaro -= 3 ^ zoom5;
         break;
      }
      for (let o = 0; o < 2; o++) {
         searchbaro += parseInt(`${values3}`) & parseInt(`${searchbaro}`);
      }
      while (searchbaro >= 3.76) {
         values3 *= parseFloat(`${2}`);
         break;
      }
      let projectt = searchbaro <= 8953168.0;
      do {
         searchbaro *= zoom5 ^ parseInt(`${searchbaro}`);
         if (projectt) {
            break;
         }
      } while ((4.78 <= (values3 * 3)) && projectt);
      for (let w = 0; w < 2; w++) {
          let buttonQ = String.fromCharCode(98,101,110,101,97,116,104,95,109,95,52,55,0);
          let savem = 2.0;
          let mbridgen = true;
         searchbaro /= Math.max(3, 2);
         buttonQ = `${buttonQ.length}`;
         savem /= Math.max(3, parseFloat(`${3 | parseInt(`${savem}`)}`));
         mbridgen = !buttonQ.startsWith(`${savem}`);
      }
         values3 -= parseFloat(`${parseInt(`${searchbaro}`)}`);
      if ((3 * zoom5) == 4) {
         zoom5 *= zoom5 | 3;
      }
         zoom5 %= Math.max(2, parseInt(`${values3}`));
      let themer = 7078683 <= zoom5;
      do {
         zoom5 /= Math.max(4, zoom5);
         if (themer) {
            break;
         }
      } while ((2.15 > values3) && themer);
      delegate_mT.push(parseInt(`${gesturesd}`));
   }
      nativemoduleT += videovarn.length;
   for (let j = 0; j < 1; j++) {
      penaltymatchiconk *= delegate_mT.length;
   }
       let bodanR: Map<any, any> = new Map([[String.fromCharCode(117,95,54,55,95,109,98,98,121,0),431], [String.fromCharCode(122,95,49,50,95,118,112,100,115,112,0),585], [String.fromCharCode(115,101,99,117,114,101,95,109,95,56,50,0),917]]);
          let linkR = 5;
          let iconarrowright3 = String.fromCharCode(105,100,101,110,116,105,102,105,97,98,108,101,95,100,95,54,0);
          let blacks: Map<any, any> = new Map([[String.fromCharCode(107,97,110,110,97,95,115,95,51,50,0),String.fromCharCode(113,95,53,50,95,111,98,115,101,114,118,105,110,103,0)], [String.fromCharCode(118,95,56,54,95,112,117,112,105,108,0),String.fromCharCode(102,97,100,115,116,95,118,95,56,56,0)], [String.fromCharCode(106,95,50,53,95,115,117,112,112,111,114,116,97,98,108,101,0),String.fromCharCode(117,95,56,52,95,117,116,103,111,105,110,103,0)]]);
         bodanR.set(`${linkR}`, bodanR.size);
         linkR |= iconarrowright3.length * blacks.size;
         iconarrowright3 = `${3 & blacks.size}`;
       let appsK = String.fromCharCode(112,114,111,100,117,99,116,102,95,104,95,53,54,0);
       let logouser_ = String.fromCharCode(114,101,115,116,114,105,99,116,101,100,95,112,95,56,57,0);
         bodanR = new Map([[`${bodanR.size}`, appsK.length | bodanR.size]]);
      cricket1 /= Math.max(bodanR.size, 2);
   while (typingR.length >= 3) {
      typingR += "1";
      break;
   }
      videovarn.push(typingR.length);
      nativemoduleT /= Math.max(delegate_mT.length / (Math.max(5, parseInt(`${cricket1}`))), 5);
   for (let h = 0; h < 1; h++) {
      videovarn = [2 >> (Math.min(2, typingR.length))];
   }
      penaltymatchiconk %= Math.max(2, parseInt(`${gesturesd}`));
   if (typingR.startsWith(`${videovarn.length}`)) {
      videovarn.push(description_iK.length);
   }
    setRefreshing(false);
  };

  const refreshUserState = async () => {
       let subsG = 0;
    let becomeG = 0;
    let eabafadfadddbafeddddeeefeaafY = String.fromCharCode(102,101,108,101,109,95,120,95,48,0);
    let appleY = true;
    let refreshg = false;
    let bangr = 1;
    let qaag6 = 0.0;
    let clearf = 3.0;
    let clubk: Map<any, any> = new Map([[String.fromCharCode(121,95,55,56,95,105,109,103,117,116,105,108,115,0),752], [String.fromCharCode(122,95,54,49,95,100,120,116,121,115,0),436]]);
    let matchesl = 0.0;
    let submito = String.fromCharCode(97,117,116,111,109,97,116,105,99,95,116,95,56,56,0);
    let zoomD = String.fromCharCode(99,104,101,99,107,115,95,122,95,50,52,0);
    let trashQ = 1.0;
    let layoutG = String.fromCharCode(122,95,49,48,95,101,113,117,97,116,97,98,108,101,0);
    let showlessj = 3.0;
   if (2 <= becomeG) {
       let helperJ = String.fromCharCode(121,95,53,54,95,114,101,103,105,115,116,101,114,0);
       let minimizey = String.fromCharCode(114,101,99,101,105,118,101,95,120,95,49,55,0);
      while (minimizey.length <= 2) {
          let graphics0 = 3.0;
          let libflipperv = String.fromCharCode(115,95,57,95,117,110,114,101,97,100,0);
          let reacti = true;
         minimizey = `${(String.fromCharCode(71,0) == libflipperv ? libflipperv.length : parseInt(`${graphics0}`))}`;
         graphics0 -= ((reacti ? 4 : 4));
         break;
      }
      while (minimizey.length <= 5) {
         helperJ += `${helperJ.length >> (Math.min(Math.abs(2), 2))}`;
         break;
      }
      becomeG ^= subsG;
   }
   for (let j = 0; j < 3; j++) {
      clearf += bangr;
   }
       let bottomo = String.fromCharCode(102,112,117,116,115,95,51,95,57,52,0);
       let borderlessJ = String.fromCharCode(115,117,98,108,97,121,101,114,115,95,112,95,55,48,0);
         bottomo = `${borderlessJ.length}`;
       let livez = false;
      while (3 == borderlessJ.length) {
          let traminia = String.fromCharCode(114,97,100,105,97,110,115,95,109,95,54,0);
         borderlessJ += `${borderlessJ.length}`;
         traminia += "2";
         break;
      }
      if (livez) {
          let libfbjniZ: Map<any, any> = new Map([[String.fromCharCode(111,95,53,55,95,115,100,101,115,0),844], [String.fromCharCode(107,95,57,51,95,115,111,108,105,100,99,111,108,111,114,0),647], [String.fromCharCode(101,95,55,48,95,114,102,102,116,98,0),952]]);
          let codegenh: Map<any, any> = new Map([[String.fromCharCode(103,111,111,103,95,115,95,54,51,0),String.fromCharCode(115,95,51,53,95,109,97,114,107,117,112,0)], [String.fromCharCode(116,114,97,110,115,99,116,105,111,110,95,51,95,54,49,0),String.fromCharCode(115,99,114,111,108,108,101,114,95,98,95,57,54,0)]]);
          let iconcloseB: Array<any> = [432, 598, 319];
          let materialc = 0.0;
          let indicatorl = 5;
         livez = libfbjniZ.size == codegenh.size;
         libfbjniZ = new Map([[`${iconcloseB.length}`, iconcloseB.length]]);
         codegenh.set(`${materialc}`, 3);
         materialc /= Math.max(1, parseFloat(`${1}`));
         indicatorl >>= Math.min(Math.abs(iconcloseB.length - parseInt(`${materialc}`)), 3);
      }
      for (let i = 0; i < 2; i++) {
         borderlessJ += "3";
      }
         livez = 95 >= bottomo.length;
      matchesl -= (String.fromCharCode(88,0) == bottomo ? bottomo.length : borderlessJ.length);
   for (let l = 0; l < 2; l++) {
      becomeG /= Math.max(parseInt(`${matchesl}`) + 1, 2);
   }
   if (bangr < 3) {
      bangr *= bangr << (Math.min(Math.abs(parseInt(`${qaag6}`)), 5));
   }
   if (5 < (bangr % (Math.max(3, 4))) || refreshg) {
      bangr %= Math.max(clubk.size, 3);
   }

    const result = await mayi_Injury.getUserDetails();

   while (qaag6 < 2.76 || (2.76 - qaag6) < 3.50) {
      appleY = !appleY;
      break;
   }
   for (let j = 0; j < 2; j++) {
      eabafadfadddbafeddddeeefeaafY = `${submito.length | 1}`;
   }
      eabafadfadddbafeddddeeefeaafY = `${eabafadfadddbafeddddeeefeaafY.length}`;
   let backgroundb = 5131095 >= clubk.size;
   do {
       let mime_ = String.fromCharCode(117,95,57,48,95,114,101,113,117,101,115,116,0);
       let basketballplayerplaceholderg = String.fromCharCode(114,95,54,56,95,97,118,103,121,0);
       let neonh = String.fromCharCode(106,95,49,56,95,105,110,116,101,114,108,101,97,118,101,0);
       let inactiveT = String.fromCharCode(114,101,97,100,102,117,108,108,95,52,95,56,51,0);
         neonh += `${inactiveT.length / (Math.max(1, 4))}`;
      for (let j = 0; j < 3; j++) {
          let libglogS = String.fromCharCode(98,95,54,57,95,104,105,103,104,112,97,115,115,0);
         neonh = `${inactiveT.length}`;
         libglogS = `${(libglogS == String.fromCharCode(100,0) ? libglogS.length : libglogS.length)}`;
      }
         neonh = `${inactiveT.length ^ neonh.length}`;
      for (let c = 0; c < 1; c++) {
          let frame_dK: Array<any> = [String.fromCharCode(99,98,99,114,95,54,95,50,56,0), String.fromCharCode(109,97,114,107,105,110,103,115,95,109,95,49,55,0), String.fromCharCode(117,110,109,97,112,102,105,108,101,95,56,95,49,48,48,0)];
          let greeni = String.fromCharCode(112,115,110,114,95,101,95,49,49,0);
          let libreactperfloggerjniA = false;
         basketballplayerplaceholderg = `${mime_.length ^ 1}`;
         frame_dK = [((libreactperfloggerjniA ? 3 : 5) / 2)];
         greeni = `${frame_dK.length}`;
      }
         inactiveT = `${neonh.length | 1}`;
      clubk = new Map([[`${becomeG}`, (eabafadfadddbafeddddeeefeaafY == String.fromCharCode(110,0) ? eabafadfadddbafeddddeeefeaafY.length : becomeG)]]);
      if (backgroundb) {
         break;
      }
   } while (backgroundb && (clubk.get(`${clearf}`) == null));
   while (2 > (submito.length % (Math.max(2, 3))) && 3 > (2 % (Math.max(6, submito.length)))) {
      submito += `${subsG}`;
      break;
   }
       let profileactiveS = String.fromCharCode(115,116,114,105,100,101,95,116,95,57,55,0);
       let contextq = 1.0;
       let basketballtrophyw: Map<any, any> = new Map([[String.fromCharCode(105,110,116,114,101,97,100,119,114,105,116,101,95,48,95,52,51,0),869], [String.fromCharCode(104,119,117,112,108,111,97,100,95,104,95,49,50,0),497]]);
      let iconclosewhiteK = basketballtrophyw.size >= 9826893;
      do {
         basketballtrophyw = new Map([[`${basketballtrophyw.size}`, basketballtrophyw.size]]);
         if (iconclosewhiteK) {
            break;
         }
      } while (iconclosewhiteK && (profileactiveS.length > 1));
         basketballtrophyw.set(`${contextq}`, parseInt(`${contextq}`));
         contextq /= Math.max((profileactiveS == String.fromCharCode(114,0) ? parseInt(`${contextq}`) : profileactiveS.length), 5);
      for (let o = 0; o < 3; o++) {
         profileactiveS += `${profileactiveS.length >> (Math.min(Math.abs(3), 4))}`;
      }
      if (basketballtrophyw.size <= 2) {
         profileactiveS = "3";
      }
      if ((1.37 * contextq) == 4.84 && 3.86 == (1.37 * contextq)) {
         basketballtrophyw.set(`${contextq}`, 3 << (Math.min(5, Math.abs(parseInt(`${contextq}`)))));
      }
         basketballtrophyw.set(`${contextq}`, 1 >> (Math.min(5, profileactiveS.length)));
      while (5.17 == (contextq / 1.95) || 1 == (basketballtrophyw.size / (Math.max(5, parseInt(`${contextq}`))))) {
         basketballtrophyw.set(`${contextq}`, 2);
         break;
      }
         basketballtrophyw.set(`${contextq}`, basketballtrophyw.size << (Math.min(Math.abs(1), 4)));
      clearf += (submito == String.fromCharCode(55,0) ? submito.length : parseInt(`${qaag6}`));
    if (result == null) {

   while ((matchesl - qaag6) < 1.69 || 3.29 < (1.69 * qaag6)) {
       let backicon4 = String.fromCharCode(116,95,56,55,95,115,111,102,114,101,101,0);
       let penaltyshooth = true;
       let schedules = 0.0;
       let mimey = String.fromCharCode(119,95,54,55,95,112,97,103,101,0);
       let whiteanimationliveO = 3.0;
       let fastforwardw: Map<any, any> = new Map([[String.fromCharCode(112,105,99,116,111,114,95,103,95,55,0),String.fromCharCode(109,101,100,105,117,109,116,104,114,101,115,104,95,110,95,52,50,0)], [String.fromCharCode(114,101,115,111,108,118,101,95,111,95,56,55,0),String.fromCharCode(100,105,109,101,110,115,105,116,111,110,115,95,97,95,50,57,0)]]);
       let bingz: Map<any, any> = new Map([[String.fromCharCode(100,105,114,101,99,116,95,101,95,49,57,0),676], [String.fromCharCode(109,101,109,95,99,95,54,52,0),35]]);
         fastforwardw = new Map([[mimey, mimey.length / 2]]);
         bingz = new Map([[`${whiteanimationliveO}`, mimey.length >> (Math.min(Math.abs(1), 5))]]);
         mimey = `${fastforwardw.size - 1}`;
         penaltyshooth = whiteanimationliveO > 69.69;
       let resendF = true;
         bingz = new Map([[`${bingz.size}`, parseInt(`${whiteanimationliveO}`) % 1]]);
      let bingb = 5295172.0 >= schedules;
      do {
         schedules += mimey.length;
         if (bingb) {
            break;
         }
      } while ((5.30 < (schedules / (Math.max(1.35, 7))) && 3 < (3 >> (Math.min(1, Math.abs(fastforwardw.size))))) && bingb);
         schedules *= ((resendF ? 5 : 2));
         schedules -= (3 + (penaltyshooth ? 1 : 1));
      while (schedules < 2.100) {
          let sortg = 0.0;
         schedules -= 1 >> (Math.min(Math.abs(parseInt(`${schedules}`)), 5));
         sortg -= 3;
         break;
      }
      qaag6 /= Math.max(1 | parseInt(`${qaag6}`), 3);
      backicon4 += `${backicon4.length | 1}`;
      break;
   }
   let basef = matchesl <= 6333724.0;
   do {
       let iconpointscorem = String.fromCharCode(105,115,109,108,95,49,95,55,55,0);
       let unewinterstitiali = String.fromCharCode(114,97,116,101,99,111,110,116,114,111,108,95,121,95,52,48,0);
       let videojsY = 3.0;
       let informationC = 4.0;
      for (let u = 0; u < 1; u++) {
         iconpointscorem = `${1 << (Math.min(2, iconpointscorem.length))}`;
      }
       let successz = 4.0;
       let constantsP = 1.0;
         constantsP *= parseFloat(`${2}`);
      while (informationC > 5.85) {
         informationC += parseFloat(`${unewinterstitiali.length}`);
         break;
      }
      let rocket2 = 6335323.0 <= constantsP;
      do {
         constantsP *= parseFloat(`${parseInt(`${constantsP}`)}`);
         if (rocket2) {
            break;
         }
      } while (rocket2 && ((3.92 * constantsP) >= 3.55));
      matchesl /= Math.max(parseInt(`${matchesl}`) / 2, 3);
      if (basef) {
         break;
      }
   } while (basef && (!zoomD.includes(`${matchesl}`)));
      refreshg = String.fromCharCode(99,0) == zoomD;
      zoomD += `${(parseInt(`${qaag6}`) >> (Math.min(1, Math.abs((refreshg ? 2 : 3)))))}`;
   while (refreshg) {
      zoomD += `${bangr}`;
      break;
   }
      bangr ^= 3;
      return;
    }

    await dispatch(updateUserAuth(result));

      bangr |= parseInt(`${clearf}`);
   while (zoomD.endsWith(`${appleY}`)) {
      zoomD += `${((refreshg ? 4 : 5) >> (Math.min(Math.abs((appleY ? 1 : 4)), 1)))}`;
      break;
   }
   if (!appleY) {
      appleY = zoomD == String.fromCharCode(53,0);
   }
   let calendark = appleY ? !appleY : appleY;
   do {
      appleY = submito.length >= eabafadfadddbafeddddeeefeaafY.length;
      if (calendark) {
         break;
      }
   } while (calendark && (appleY || 2 >= zoomD.length));
      appleY = (((appleY ? zoomD.length : 73) * zoomD.length) == 73);
   let libswresampleP = 9640454.0 <= clearf;
   do {
      clearf += eabafadfadddbafeddddeeefeaafY.length;
      if (libswresampleP) {
         break;
      }
   } while ((Array.from(clubk.values()).includes(clearf)) && libswresampleP);
    return;
  };

  return (
    <ScreenContainer>
      <TitleWithBackButtonHeader title="邀请好友奖励" />
      <ScrollView
        refreshControl={
          <RefreshControl refreshing={refreshing} onRefresh={handleRefresh} tintColor="#FAC33D" />
        }
        showsVerticalScrollIndicator={false}
      >
        {/* <TitleWithBackButtonHeader title="邀请好友奖励" /> */}
        <View style={{ paddingHorizontal: 0 }}>
          { }
          <InviteHeader />
          { }
          <InviteCard userState={userState.user!} />
          { }

          <TouchableOpacity
            onPress={() => {
              navigation.navigate("活动规则");
            }}
          >
            <View
              style={{
                flex: 1,
                justifyContent: "center",
                alignItems: "center",
                paddingVertical: 10,
              }}
            >
              <Text
                style={{
                  fontSize: 12,
                  textDecorationLine: "underline",
                  color: "#ffffff",
                }}
              >
                活动规则
              </Text>
            </View>
          </TouchableOpacity>
          { }
        </View>
        <InviteStep />
      </ScrollView>
    </ScreenContainer>
  );
};

const styles = StyleSheet.create({});
