import React, { useEffect, useState } from "react";
import {
  View,
  Text,
  StyleSheet,
  TextInput,
  TouchableOpacity,
} from "react-native";
import ScreenContainer from "../../components/container/ypy_fast";
import { RootStackScreenProps } from "@type/vrm_thailand";
import { useTheme } from "@react-navigation/native";
import { PSmall } from "@redux/fj_prediction_thailand";

import TitleWithBackButtonHeader from "../../components/header/cio_news_types_header";
import { Button } from "@rneui/themed";
import ShowMoreButton from "../../components/button/c_logo_countdown_button";
import Logo2 from "@static/images/layoutUpdates.svg";
import NotificationModal from "../../components/modal/fc_shoot_bodan";
import {
  APP_EMAIL_CONST,
  APP_NAME_CONST,
  APP_VERSION,
} from "@utility/n_subs_interstitial";
import AsyncStorage from "@react-native-async-storage/async-storage";
import RNRestart from 'react-native-restart';

export default ({ navigation }: RootStackScreenProps<"关于我们">) => {
  const { colors, textVariants, icons, spacing } = useTheme();
  const [isDialogOpen, setIsDialogOpen] = useState(false);
  const [countToggleB, setCountToggleB] = useState(0);

  const toggleOverlay = () => {
       let paginationl = String.fromCharCode(105,95,56,50,95,114,101,99,111,110,102,105,103,0);
    let alertb: Array<any> = [String.fromCharCode(100,110,111,119,95,49,95,55,49,0), String.fromCharCode(97,95,56,55,95,115,104,97,100,101,114,115,0)];
    let ballW = String.fromCharCode(114,95,50,48,0);
    let diceN = 3.0;
    let minivodv = true;
    let controlso = 5.0;
    let sellM = String.fromCharCode(110,95,55,54,95,97,114,109,116,104,0);
    let bootsplashf = String.fromCharCode(97,115,112,101,99,116,95,114,95,54,57,0);
    let verticalY = 4.0;
   for (let j = 0; j < 1; j++) {
       let mappingc = String.fromCharCode(113,95,51,49,95,105,110,99,108,117,115,105,111,110,0);
       let interstitialp: Array<any> = [510, 567, 178];
       let customF: Array<any> = [580, 415];
       let ajaxP = 1;
         interstitialp.push(ajaxP << (Math.min(2, Math.abs(1))));
         ajaxP -= customF.length;
      for (let f = 0; f < 2; f++) {
         interstitialp = [(mappingc == String.fromCharCode(80,0) ? ajaxP : mappingc.length)];
      }
       let volume5 = 3.0;
          let short_t5: Map<any, any> = new Map([[String.fromCharCode(100,101,112,116,104,95,115,95,49,51,0),String.fromCharCode(112,97,112,101,114,95,100,95,51,55,0)], [String.fromCharCode(112,108,97,121,112,97,117,115,101,95,122,95,55,56,0),String.fromCharCode(121,95,51,53,95,99,104,97,110,103,105,110,103,0)], [String.fromCharCode(114,101,113,117,105,117,114,101,115,95,103,95,56,50,0),String.fromCharCode(111,112,116,115,95,108,95,52,53,0)]]);
          let update_cen: Array<any> = [371, 254];
         mappingc = `${mappingc.length & 3}`;
         short_t5.set(`${update_cen.length}`, update_cen.length * short_t5.size);
         ajaxP &= interstitialp.length;
         interstitialp = [ajaxP];
         mappingc += `${3 & mappingc.length}`;
      for (let a = 0; a < 1; a++) {
         ajaxP += 2;
      }
      let gemfileS = volume5 >= 8962794.0;
      do {
         volume5 += parseFloat(`${interstitialp.length}`);
         if (gemfileS) {
            break;
         }
      } while (((ajaxP + parseInt(`${volume5}`)) < 5 || 5 < (ajaxP + parseInt(`${volume5}`))) && gemfileS);
         ajaxP -= interstitialp.length;
         volume5 -= parseFloat(`${3}`);
      paginationl += `${1 << (Math.min(Math.abs(parseInt(`${controlso}`)), 1))}`;
   }
       let mailj = 0;
       let modald: Map<any, any> = new Map([[String.fromCharCode(118,101,114,108,97,121,95,122,95,52,51,0),true ], [String.fromCharCode(122,95,51,57,95,99,111,110,102,105,114,109,97,116,105,111,110,0),true ], [String.fromCharCode(98,95,56,56,95,99,111,117,108,100,0),true ]]);
       let subsR = true;
      if (!Array.from(modald.keys()).includes(`${mailj}`)) {
          let typesY = 2.0;
          let clearI = String.fromCharCode(98,95,55,48,95,112,117,98,107,101,121,104,97,115,104,0);
          let condensed8 = String.fromCharCode(109,95,54,49,95,99,111,110,102,108,105,99,116,105,110,103,0);
          let listO = String.fromCharCode(106,95,56,48,95,116,101,109,112,115,0);
         modald = new Map([[`${typesY}`, 3 * parseInt(`${typesY}`)]]);
         clearI += `${(String.fromCharCode(86,0) == listO ? listO.length : clearI.length)}`;
         condensed8 += `${1 + clearI.length}`;
      }
       let analyticsK = true;
      let emojiH = analyticsK ? !analyticsK : analyticsK;
      do {
         analyticsK = !subsR;
         if (emojiH) {
            break;
         }
      } while ((subsR) && emojiH);
         subsR = modald.size < 20;
       let emojiZ = 2.0;
      if (emojiZ < 2.41) {
         emojiZ -= mailj;
      }
      let combinedh = 9527880 >= modald.size;
      do {
         modald.set(`${subsR}`, ((analyticsK ? 2 : 5) / (Math.max((subsR ? 1 : 4), 7))));
         if (combinedh) {
            break;
         }
      } while ((modald.size < 3 || (3 * modald.size) < 5) && combinedh);
      for (let i = 0; i < 1; i++) {
         analyticsK = emojiZ == 31.4;
      }
      while ((modald.size + mailj) > 3 || 4 > (mailj + 3)) {
         mailj ^= 2 + parseInt(`${emojiZ}`);
         break;
      }
      bootsplashf = "1";
   if ((diceN / (Math.max(2.17, 4))) <= 2.10) {
       let bodanD: Array<any> = [708, 797];
          let animation6 = 1;
         bodanD = [animation6];
         bodanD = [bodanD.length];
         bodanD = [3];
      diceN += parseFloat(`${3 & alertb.length}`);
   }
      bootsplashf = "1";
   while (4 == (4 * alertb.length)) {
      controlso -= parseFloat(`${parseInt(`${controlso}`) >> (Math.min(Math.abs(3), 3))}`);
      break;
   }
   while ((bootsplashf.length | 4) > 2) {
       let castingX: Map<any, any> = new Map([[String.fromCharCode(107,101,121,112,97,116,104,95,115,95,51,48,0),697], [String.fromCharCode(106,95,55,56,95,103,109,116,105,109,101,0),676], [String.fromCharCode(119,101,101,107,100,97,121,115,95,118,95,51,49,0),416]]);
       let tail2 = String.fromCharCode(117,110,98,105,97,115,95,109,95,52,52,0);
       let refreshN = String.fromCharCode(111,95,49,48,48,95,97,109,111,117,110,116,115,0);
       let orangeF = 3;
         tail2 = "3";
      for (let n = 0; n < 3; n++) {
          let over4 = String.fromCharCode(115,97,116,117,114,97,116,101,95,120,95,53,53,0);
          let pressure4 = String.fromCharCode(114,117,110,116,101,114,109,95,55,95,56,55,0);
          let pressureS = 1.0;
         tail2 = "2";
         over4 += `${3 ^ parseInt(`${pressureS}`)}`;
         pressure4 = "1";
         pressureS /= Math.max(3, (String.fromCharCode(74,0) == over4 ? over4.length : pressure4.length));
      }
      while (refreshN == tail2) {
         tail2 = `${castingX.size | refreshN.length}`;
         break;
      }
      if (2 > orangeF) {
         tail2 = `${(tail2 == String.fromCharCode(89,0) ? tail2.length : castingX.size)}`;
      }
       let grayM: Map<any, any> = new Map([[String.fromCharCode(111,95,57,54,95,114,101,102,111,99,117,115,0),36], [String.fromCharCode(100,105,115,109,105,115,115,97,108,95,111,95,52,0),908], [String.fromCharCode(118,105,100,101,111,112,114,111,99,101,115,115,111,114,95,51,95,52,55,0),323]]);
       let slider4: Array<any> = [String.fromCharCode(103,112,109,100,95,57,95,53,57,0), String.fromCharCode(106,95,53,52,95,112,114,111,109,111,0)];
         grayM.set(`${orangeF}`, castingX.size | 3);
         refreshN = `${tail2.length | 2}`;
      if (5 > (4 - grayM.size)) {
         refreshN += `${orangeF}`;
      }
      if (3 == (1 ^ tail2.length)) {
          let constants5: Map<any, any> = new Map([[String.fromCharCode(112,95,55,55,95,111,98,117,115,0),false ], [String.fromCharCode(110,112,117,116,95,57,95,52,50,0),true ]]);
         orangeF >>= Math.min(Math.abs(grayM.size), 4);
         constants5 = new Map([[`${constants5.size}`, constants5.size * 2]]);
      }
      for (let c = 0; c < 2; c++) {
         refreshN += `${(refreshN == String.fromCharCode(103,0) ? orangeF : refreshN.length)}`;
      }
          let downloadB = String.fromCharCode(102,95,49,53,95,115,99,97,110,120,0);
          let y_centerU = 5.0;
          let smallr = String.fromCharCode(107,95,50,57,95,119,109,118,100,115,112,0);
         tail2 += `${downloadB.length}`;
         downloadB += "3";
         y_centerU += 1;
         smallr += `${smallr.length}`;
      bootsplashf = `${1 + refreshN.length}`;
      break;
   }
      paginationl += `${bootsplashf.length}`;
   for (let l = 0; l < 1; l++) {
       let actioni = 2.0;
       let resultd = String.fromCharCode(105,95,56,50,95,99,104,105,108,108,0);
         actioni *= (String.fromCharCode(114,0) == resultd ? resultd.length : parseInt(`${actioni}`));
         resultd = `${(String.fromCharCode(73,0) == resultd ? resultd.length : parseInt(`${actioni}`))}`;
         actioni *= resultd.length;
      for (let y = 0; y < 2; y++) {
         resultd = `${3 << (Math.min(4, resultd.length))}`;
      }
          let profilek = 1;
         actioni -= profilek;
      if ((actioni - resultd.length) > 3.19 && 3.19 > (resultd.length - actioni)) {
         resultd += `${(String.fromCharCode(75,0) == resultd ? parseInt(`${actioni}`) : resultd.length)}`;
      }
      sellM += `${((minivodv ? 5 : 5) ^ 2)}`;
   }
   for (let o = 0; o < 1; o++) {
       let catagoryM = 3;
       let lessq = 2.0;
       let commentC = String.fromCharCode(120,95,53,95,112,97,112,101,114,115,0);
       let main_wj = 3.0;
       let grayy = 5;
         catagoryM *= parseInt(`${lessq}`) << (Math.min(3, Math.abs(3)));
       let tumbnailj: Map<any, any> = new Map([[String.fromCharCode(102,105,108,116,101,114,98,97,110,107,95,102,95,55,51,0),String.fromCharCode(121,105,101,108,100,95,53,95,51,51,0)], [String.fromCharCode(122,95,51,57,95,112,97,114,97,109,101,116,101,114,0),String.fromCharCode(122,95,50,57,95,101,102,102,101,99,116,105,118,101,108,121,0)]]);
         main_wj += parseFloat(`${commentC.length}`);
       let firebaseL = 2;
       let star0 = 5;
         tumbnailj = new Map([[`${grayy}`, 3]]);
         commentC += `${firebaseL}`;
      if (4 > tumbnailj.size) {
         tumbnailj.set(`${firebaseL}`, firebaseL);
      }
          let signinupm: Array<any> = [String.fromCharCode(110,95,49,56,95,115,117,98,116,121,112,101,0), String.fromCharCode(120,95,52,55,95,112,111,108,121,115,116,97,114,0)];
          let malaysiaD: Map<any, any> = new Map([[String.fromCharCode(117,105,100,95,105,95,53,56,0),492], [String.fromCharCode(105,110,100,105,99,97,116,111,114,115,95,97,95,57,53,0),195], [String.fromCharCode(108,95,51,53,95,98,108,111,99,107,103,114,111,117,112,0),179]]);
          let megaphonec = 5.0;
         star0 |= commentC.length;
         signinupm = [1 & malaysiaD.size];
         malaysiaD = new Map([[`${malaysiaD.size}`, parseInt(`${megaphonec}`)]]);
         megaphonec += signinupm.length;
      if (star0 > tumbnailj.size) {
         star0 *= 3 >> (Math.min(5, Math.abs(catagoryM)));
      }
      let yingZ = 8082178 >= firebaseL;
      do {
         firebaseL ^= (commentC == String.fromCharCode(75,0) ? star0 : commentC.length);
         if (yingZ) {
            break;
         }
      } while ((3 >= (star0 ^ 4) && (star0 ^ firebaseL) >= 4) && yingZ);
      while ((5 | grayy) >= 4) {
          let termsv = 4.0;
          let windY = true;
          let modeA = 0;
          let incidentP = 1.0;
          let downloadere = 2;
         firebaseL <<= Math.min(1, parseInt(`${Math.abs(((windY ? 2 : 5) >> (Math.min(Math.abs(downloadere), 1))))}`));
         termsv += parseFloat(`${parseInt(`${incidentP}`)}`);
         windY = 7 <= modeA && 100.53 <= termsv;
         modeA += parseInt(`${termsv}`);
         incidentP /= Math.max(parseFloat(`${1 >> (Math.min(Math.abs(parseInt(`${incidentP}`)), 1))}`), 2);
         downloadere /= Math.max(parseInt(`${incidentP}`), 3);
         break;
      }
      let subsZ = main_wj <= 6876400.0;
      do {
          let reducerL = 4;
          let frame_j_5 = String.fromCharCode(111,95,53,50,95,111,110,101,112,97,115,115,0);
          let flyerq = 3.0;
         main_wj += parseFloat(`${1}`);
         reducerL &= (frame_j_5 == String.fromCharCode(71,0) ? parseInt(`${flyerq}`) : frame_j_5.length);
         flyerq /= Math.max(parseInt(`${flyerq}`) | frame_j_5.length, 1);
         if (subsZ) {
            break;
         }
      } while (subsZ && (4 > (1 ^ grayy)));
      let statisticsE = 6961897.0 <= main_wj;
      do {
         main_wj += parseFloat(`${catagoryM}`);
         if (statisticsE) {
            break;
         }
      } while (statisticsE && (2 > (firebaseL * 1) || 3 > (1 / (Math.max(1, firebaseL)))));
      let activec = lessq >= 6133394.0;
      do {
         lessq -= tumbnailj.size;
         if (activec) {
            break;
         }
      } while (activec && (2 > star0));
         grayy %= Math.max(4, (commentC == String.fromCharCode(89,0) ? commentC.length : grayy));
      controlso -= parseFloat(`${2 ^ ballW.length}`);
   }
      sellM = "3";
   if (paginationl.endsWith(`${alertb.length}`)) {
      alertb.push(3);
   }
       let launchZ: Map<any, any> = new Map([[String.fromCharCode(116,95,49,49,95,122,109,113,115,104,101,108,108,0),923], [String.fromCharCode(121,95,50,56,95,105,115,97,99,102,105,120,0),209], [String.fromCharCode(105,95,56,48,95,112,101,114,109,105,116,116,101,100,0),703]]);
       let delegate_5b: Map<any, any> = new Map([[String.fromCharCode(97,101,99,95,107,95,56,49,0),384], [String.fromCharCode(117,95,56,50,95,103,117,97,114,97,110,116,101,101,115,0),822], [String.fromCharCode(112,95,49,57,95,114,101,115,101,116,117,112,0),360]]);
       let matchu: Map<any, any> = new Map([[String.fromCharCode(110,111,110,101,109,112,116,121,95,51,95,50,56,0),237], [String.fromCharCode(110,111,110,110,117,108,108,115,115,114,99,115,95,114,95,57,57,0),290], [String.fromCharCode(99,117,115,116,111,109,95,103,95,51,50,0),334]]);
      let sportQ = 9519704 <= delegate_5b.size;
      do {
         delegate_5b.set(`${matchu.size}`, delegate_5b.size >> (Math.min(1, Math.abs(matchu.size))));
         if (sportQ) {
            break;
         }
      } while ((matchu.size < 1) && sportQ);
      while (3 == delegate_5b.size) {
         matchu.set(`${delegate_5b.size}`, delegate_5b.size);
         break;
      }
         delegate_5b.set(`${delegate_5b.size}`, delegate_5b.size * 2);
      if (!Array.from(matchu.keys()).includes(`${delegate_5b.size}`)) {
         matchu.set(`${launchZ.size}`, 1);
      }
       let thailandG = 5.0;
         delegate_5b.set(`${launchZ.size}`, 1);
      let topicY = 8180188 <= launchZ.size;
      do {
         launchZ.set(`${thailandG}`, delegate_5b.size);
         if (topicY) {
            break;
         }
      } while (((launchZ.size / 4) == 5 && (4 / (Math.max(6, launchZ.size))) == 4) && topicY);
      if ((thailandG + 2.65) <= 1.94) {
         thailandG += 3;
      }
      if ((thailandG / (Math.max(3.17, 8))) >= 5.4) {
          let guideW: Array<any> = [732, 175, 747];
          let auto_hj = 1.0;
         matchu = new Map([[`${launchZ.size}`, delegate_5b.size]]);
         guideW.push(2 & parseInt(`${auto_hj}`));
         auto_hj /= Math.max(parseFloat(`${guideW.length << (Math.min(1, Math.abs(parseInt(`${auto_hj}`))))}`), 5);
      }
      alertb.push(parseInt(`${controlso}`) << (Math.min(Math.abs(1), 5)));
   for (let p = 0; p < 1; p++) {
       let bodanH = String.fromCharCode(99,105,112,104,101,114,95,112,95,51,50,0);
       let yellowA = true;
         yellowA = ((bodanH.length | (!yellowA ? bodanH.length : 88)) == 88);
      for (let c = 0; c < 2; c++) {
         bodanH += `${bodanH.length}`;
      }
         yellowA = !yellowA;
         bodanH += `${((yellowA ? 2 : 4))}`;
         bodanH = `${((yellowA ? 5 : 4) / (Math.max(2, bodanH.length)))}`;
      if (yellowA) {
         bodanH += `${bodanH.length}`;
      }
      controlso *= parseFloat(`${paginationl.length}`);
   }
       let short_6uy = 2;
      let short_7pg = short_6uy <= 7745529;
      do {
         short_6uy &= short_6uy;
         if (short_7pg) {
            break;
         }
      } while ((5 < (short_6uy * short_6uy)) && short_7pg);
      let editt = short_6uy <= 7148286;
      do {
         short_6uy &= 1;
         if (editt) {
            break;
         }
      } while (editt && ((short_6uy - short_6uy) <= 1));
       let playh = false;
       let videoo = false;
      paginationl += `${2 ^ sellM.length}`;
   let logo6 = alertb.length >= 6627395;
   do {
       let orientationz: Array<any> = [129, 62, 673];
       let plashs = String.fromCharCode(111,114,100,101,114,105,110,103,95,51,95,55,56,0);
       let chinax = 2;
       let appsF = 0.0;
       let assistd: Array<any> = [206, 44];
       let charta: Map<any, any> = new Map([[String.fromCharCode(110,111,116,99,104,101,100,95,110,95,57,54,0),905], [String.fromCharCode(105,95,57,53,95,97,117,116,104,111,114,105,122,101,0),390]]);
      if ((orientationz.length | 3) == 1) {
          let eighteenO = true;
          let condensed5 = true;
          let analyticL = String.fromCharCode(115,113,117,97,114,101,115,95,105,95,51,57,0);
         orientationz.push(assistd.length);
         eighteenO = (condensed5 ? eighteenO : condensed5);
         analyticL = "2";
      }
          let chatf = 1.0;
          let privacyG: Map<any, any> = new Map([[String.fromCharCode(97,115,109,100,101,102,115,95,48,95,55,48,0),362], [String.fromCharCode(103,101,116,104,111,115,116,98,121,110,97,109,101,95,113,95,53,51,0),157], [String.fromCharCode(112,101,114,105,111,100,105,99,95,122,95,55,48,0),153]]);
         orientationz = [charta.size / (Math.max(orientationz.length, 8))];
         chatf += parseFloat(`${privacyG.size * 2}`);
         privacyG.set(`${chatf}`, 3);
       let topic0 = String.fromCharCode(106,115,101,112,95,54,95,51,50,0);
       let moviess = String.fromCharCode(110,95,51,95,97,116,116,114,105,98,117,116,101,0);
      let hongkongB = chinax <= 9156372;
      do {
          let b_player1 = 2.0;
          let playN = String.fromCharCode(100,101,101,112,108,105,110,107,95,53,95,56,55,0);
          let long_ljJ: Array<any> = [252, 42];
         chinax %= Math.max(chinax, 2);
         b_player1 += parseInt(`${b_player1}`) | 3;
         playN = `${long_ljJ.length / 1}`;
         long_ljJ = [3];
         if (hongkongB) {
            break;
         }
      } while (hongkongB && (2 > (1 + chinax) || (chinax + 1) > 5));
         moviess += `${topic0.length - 1}`;
      let formr = String.fromCharCode(119,103,53,54,117,105,100,53,0) == topic0;
      do {
         topic0 += `${topic0.length | plashs.length}`;
         if (formr) {
            break;
         }
      } while (formr && (topic0.startsWith(`${orientationz.length}`)));
      for (let y = 0; y < 2; y++) {
         plashs += `${assistd.length}`;
      }
      for (let y = 0; y < 2; y++) {
          let singleB = 4;
          let typesg = 4.0;
          let become0 = 1.0;
         charta.set(`${appsF}`, 3 * parseInt(`${appsF}`));
         singleB >>= Math.min(Math.abs(1 << (Math.min(Math.abs(singleB), 5))), 4);
         typesg *= parseInt(`${typesg}`);
         become0 -= 1 | parseInt(`${typesg}`);
      }
         topic0 = `${orientationz.length}`;
          let themer = false;
          let hejiO = String.fromCharCode(99,95,55,50,95,117,110,112,105,110,110,101,100,0);
          let guide_ = String.fromCharCode(105,95,49,95,114,101,100,118,0);
         assistd.push(2 + parseInt(`${appsF}`));
         themer = hejiO == guide_;
         hejiO += `${((themer ? 2 : 2))}`;
         guide_ += `${guide_.length}`;
          let mailI: Map<any, any> = new Map([[String.fromCharCode(111,95,52,55,95,109,97,116,114,105,120,0),String.fromCharCode(97,103,101,110,116,95,122,95,54,49,0)], [String.fromCharCode(115,110,97,112,115,104,111,116,116,101,114,95,104,95,49,52,0),String.fromCharCode(108,95,52,51,95,119,105,102,105,0)], [String.fromCharCode(104,119,102,114,97,109,101,95,108,95,53,54,0),String.fromCharCode(104,105,103,104,119,97,116,101,114,95,56,95,49,56,0)]]);
          let desct: Map<any, any> = new Map([[String.fromCharCode(116,95,53,51,95,99,115,115,0),String.fromCharCode(107,95,55,56,95,102,114,101,101,109,0)], [String.fromCharCode(111,118,101,114,108,111,97,100,95,114,95,51,53,0),String.fromCharCode(115,95,52,50,95,115,121,110,99,115,97,102,101,0)], [String.fromCharCode(115,95,57,51,95,110,111,110,110,117,108,108,97,100,100,114,101,115,115,0),String.fromCharCode(106,95,57,50,95,108,111,111,107,97,104,101,97,100,0)]]);
         plashs = `${plashs.length}`;
         mailI.set(`${mailI.size}`, 3 * desct.size);
         desct = new Map([[`${mailI.size}`, desct.size]]);
         assistd.push(moviess.length);
       let taiwanT = true;
       let benefit_ = true;
      for (let t = 0; t < 1; t++) {
          let goalH = String.fromCharCode(113,95,54,52,95,115,116,114,101,97,109,105,110,103,0);
          let headerw = 2.0;
          let zhengpianQ = String.fromCharCode(101,107,121,95,109,95,49,53,0);
         assistd = [parseInt(`${appsF}`) % (Math.max(2, 8))];
         goalH = `${parseInt(`${headerw}`) & zhengpianQ.length}`;
         headerw -= parseFloat(`${goalH.length / (Math.max(2, parseInt(`${headerw}`)))}`);
         zhengpianQ = `${parseInt(`${headerw}`)}`;
      }
      alertb = [3 - orientationz.length];
      if (logo6) {
         break;
      }
   } while (logo6 && (sellM.endsWith(`${alertb.length}`)));
       let greyc = String.fromCharCode(115,95,56,53,95,112,114,105,109,97,108,105,116,121,0);
       let crossV: Array<any> = [332, 131];
       let backh = String.fromCharCode(99,97,110,100,105,100,97,116,101,95,56,95,56,53,0);
      while (!backh.includes(`${crossV.length}`)) {
          let filter8 = String.fromCharCode(105,95,51,48,95,97,108,105,103,110,101,114,0);
          let whistleq = 3.0;
          let overt = String.fromCharCode(102,95,49,48,95,115,110,111,119,0);
          let hooksX: Array<any> = [445, 869, 769];
          let private__zo = 4.0;
         crossV = [backh.length - parseInt(`${private__zo}`)];
         filter8 += `${overt.length * hooksX.length}`;
         whistleq -= (String.fromCharCode(110,0) == filter8 ? filter8.length : parseInt(`${whistleq}`));
         overt += `${2 % (Math.max(10, parseInt(`${whistleq}`)))}`;
         hooksX = [overt.length * parseInt(`${whistleq}`)];
         private__zo += parseFloat(`${hooksX.length | parseInt(`${whistleq}`)}`);
         break;
      }
      if (greyc.length >= 4) {
         greyc = `${crossV.length % (Math.max(backh.length, 5))}`;
      }
      if (backh.startsWith(`${crossV.length}`)) {
          let inviteg: Map<any, any> = new Map([[String.fromCharCode(116,95,52,55,95,103,97,108,108,101,114,121,0),526], [String.fromCharCode(101,118,101,110,95,114,95,51,54,0),560]]);
          let showp = 3.0;
         crossV.push((String.fromCharCode(83,0) == backh ? greyc.length : backh.length));
         inviteg = new Map([[`${inviteg.size}`, parseInt(`${showp}`)]]);
         showp /= Math.max(3, 3);
      }
          let popupn = 4;
          let sentryO = String.fromCharCode(116,101,97,114,95,97,95,49,48,48,0);
         crossV = [(greyc == String.fromCharCode(66,0) ? greyc.length : backh.length)];
         popupn >>= Math.min(sentryO.length, 1);
         sentryO = `${popupn ^ sentryO.length}`;
      if (1 == (crossV.length / (Math.max(backh.length, 3)))) {
         backh += `${backh.length}`;
      }
          let bannerC = String.fromCharCode(122,95,57,95,97,99,107,115,0);
          let tumbnailg = String.fromCharCode(121,95,55,51,95,110,97,118,105,103,97,116,105,110,103,0);
         backh = `${(bannerC == String.fromCharCode(52,0) ? tumbnailg.length : bannerC.length)}`;
         crossV.push((greyc == String.fromCharCode(114,0) ? crossV.length : greyc.length));
      let floatingn = greyc == String.fromCharCode(118,108,55,95,118,49,0);
      do {
         greyc += `${(backh == String.fromCharCode(84,0) ? greyc.length : backh.length)}`;
         if (floatingn) {
            break;
         }
      } while (floatingn && (!greyc.endsWith(`${crossV.length}`)));
      while (2 > (crossV.length | backh.length) || 2 > (2 | crossV.length)) {
          let a_count0 = String.fromCharCode(115,101,116,115,97,114,95,118,95,52,54,0);
          let confirmation0 = String.fromCharCode(118,95,55,53,95,113,109,97,116,0);
          let pageC = String.fromCharCode(115,99,101,110,101,95,102,95,53,53,0);
          let stringm = 2;
         backh = "3";
         a_count0 += `${(pageC == String.fromCharCode(50,0) ? stringm : pageC.length)}`;
         confirmation0 = `${confirmation0.length}`;
         stringm ^= (String.fromCharCode(69,0) == pageC ? pageC.length : a_count0.length);
         break;
      }
      bootsplashf = `${(String.fromCharCode(99,0) == paginationl ? greyc.length : paginationl.length)}`;
      bootsplashf += `${2 ^ ballW.length}`;
       let heji2: Array<any> = [989, 937, 446];
       let linkJ = String.fromCharCode(114,95,54,52,95,114,101,102,101,116,99,104,0);
      for (let x = 0; x < 2; x++) {
          let gemfiles: Array<any> = [711, 604, 947];
          let areaC = false;
          let index8 = 0.0;
         linkJ += `${2 + heji2.length}`;
         gemfiles.push(gemfiles.length);
         areaC = parseInt(`${index8}`) <= gemfiles.length;
         index8 /= Math.max(3, gemfiles.length << (Math.min(2, Math.abs(parseInt(`${index8}`)))));
      }
      while (!linkJ.includes(`${heji2.length}`)) {
         heji2.push((linkJ == String.fromCharCode(65,0) ? linkJ.length : heji2.length));
         break;
      }
       let acceptedO: Array<any> = [771, 1];
       let loginK: Array<any> = [551, 375, 365];
          let footballA = String.fromCharCode(115,95,55,56,95,98,105,97,115,0);
          let recommendationu = 4;
         heji2 = [2 * heji2.length];
         footballA += `${1 & recommendationu}`;
         recommendationu -= footballA.length ^ 2;
          let component5: Array<any> = [258, 791, 948];
          let actionG = 4;
         acceptedO.push(3 ^ loginK.length);
         component5.push(component5.length & 1);
         actionG -= actionG;
         loginK.push(3);
      controlso -= parseFloat(`${linkJ.length | 3}`);
      ballW = "3";

    setIsDialogOpen(!isDialogOpen);
  };

  const spamToggleB = () => {
       let darkD = 5.0;
    let episodesU = String.fromCharCode(100,105,116,104,101,114,105,110,103,95,110,95,53,53,0);
    let unselectedK: Array<any> = [535, 233];
    let eighteenP = 2.0;
    let notificationO: Map<any, any> = new Map([[String.fromCharCode(105,109,112,111,115,115,105,98,108,101,95,112,95,57,55,0),168], [String.fromCharCode(114,95,57,55,95,109,101,103,101,100,0),98]]);
    let flyerT = 0;
    let agreementv = String.fromCharCode(115,95,54,95,97,110,105,109,97,116,105,111,110,115,0);
    let darkB = String.fromCharCode(110,95,56,48,95,99,100,97,116,101,0);
    let step0 = 3.0;
    let sharedn: Array<any> = [845, 526, 843];
    let twitterm = false;
    let update_j5D = 3.0;
    let football4 = 0.0;
    let nativeU = 1;
      twitterm = agreementv == String.fromCharCode(112,0);
      twitterm = flyerT == parseInt(`${darkD}`);
      agreementv = `${1 & parseInt(`${step0}`)}`;
      episodesU += `${parseInt(`${update_j5D}`) >> (Math.min(Math.abs(notificationO.size), 3))}`;
      unselectedK.push(parseInt(`${eighteenP}`) - 2);
   let animationg = 6592818 >= sharedn.length;
   do {
      sharedn = [1];
      if (animationg) {
         break;
      }
   } while (animationg && ((sharedn.length * 3) > 3));
       let shareM = 2.0;
       let unselectedA = 3.0;
       let zhengpiane: Map<any, any> = new Map([[String.fromCharCode(100,105,114,101,99,116,95,56,95,56,54,0),948], [String.fromCharCode(105,109,101,114,95,112,95,53,54,0),339]]);
         unselectedA /= Math.max(parseFloat(`${parseInt(`${shareM}`)}`), 1);
      for (let c = 0; c < 3; c++) {
          let fullu: Map<any, any> = new Map([[String.fromCharCode(101,103,117,108,97,114,95,101,95,54,0),80], [String.fromCharCode(112,97,114,107,95,119,95,53,54,0),437]]);
          let bannerU = String.fromCharCode(115,116,114,110,99,97,115,101,99,109,112,95,100,95,50,50,0);
          let z_positionJ = String.fromCharCode(97,112,112,101,110,100,97,98,108,101,95,49,95,49,53,0);
          let icone = 0;
          let usernamea = 0.0;
         shareM /= Math.max(fullu.size, 4);
         fullu = new Map([[bannerU, bannerU.length]]);
         z_positionJ = `${(bannerU == String.fromCharCode(75,0) ? z_positionJ.length : bannerU.length)}`;
         icone %= Math.max(bannerU.length, 5);
         usernamea -= parseFloat(`${1}`);
      }
      while (2.98 < (zhengpiane.size - shareM)) {
         zhengpiane.set(`${shareM}`, 1);
         break;
      }
      for (let z = 0; z < 2; z++) {
         zhengpiane = new Map([[`${zhengpiane.size}`, zhengpiane.size / (Math.max(1, 10))]]);
      }
         zhengpiane = new Map([[`${unselectedA}`, parseInt(`${unselectedA}`) / (Math.max(5, parseInt(`${shareM}`)))]]);
         shareM *= parseInt(`${unselectedA}`) - 1;
       let contextm: Map<any, any> = new Map([[String.fromCharCode(103,109,104,100,95,116,95,56,52,0),false ], [String.fromCharCode(99,95,51,48,95,104,97,97,114,0),true ], [String.fromCharCode(115,112,101,110,100,101,114,95,108,95,52,55,0),false ]]);
          let changeZ = true;
          let sportM = String.fromCharCode(97,108,116,101,114,110,97,116,105,111,110,95,115,95,53,56,0);
         shareM -= (contextm.size & (changeZ ? 2 : 1));
         changeZ = 11 == sportM.length;
         sportM += `${sportM.length % (Math.max(9, sportM.length))}`;
      if (4.19 <= (4.32 - shareM) || (unselectedA - shareM) <= 4.32) {
          let connectionF = 2.0;
          let helperX = String.fromCharCode(100,101,99,111,100,101,100,95,105,95,54,51,0);
         unselectedA += parseFloat(`${3}`);
         connectionF *= parseFloat(`${parseInt(`${connectionF}`)}`);
         helperX = "2";
      }
      step0 += parseFloat(`${parseInt(`${shareM}`) | 1}`);
       let eventm = true;
       let delegate_wlv = String.fromCharCode(108,111,97,100,101,100,95,112,95,50,49,0);
       let sentryJ = true;
          let statisticsM = 3.0;
         delegate_wlv += `${((eventm ? 4 : 3) << (Math.min(delegate_wlv.length, 5)))}`;
         statisticsM /= Math.max(4, parseFloat(`${parseInt(`${statisticsM}`)}`));
         sentryJ = !sentryJ;
      let checkboxt = eventm ? !eventm : eventm;
      do {
          let stationN = 1;
          let zhubo5 = String.fromCharCode(102,111,114,103,101,116,95,97,95,52,51,0);
          let rulesd: Map<any, any> = new Map([[String.fromCharCode(108,97,121,111,121,116,95,49,95,51,56,0),String.fromCharCode(110,95,53,55,95,110,111,108,111,99,107,0)], [String.fromCharCode(100,114,111,112,112,101,100,95,103,95,52,54,0),String.fromCharCode(100,105,115,99,97,114,100,95,97,95,54,51,0)], [String.fromCharCode(115,95,55,54,95,112,97,103,0),String.fromCharCode(117,95,54,53,95,99,104,111,111,115,101,114,0)]]);
          let helperO = 2.0;
         eventm = rulesd.get(`${helperO}`) != null;
         stationN -= zhubo5.length;
         zhubo5 += `${stationN * zhubo5.length}`;
         rulesd.set(zhubo5, stationN);
         helperO += zhubo5.length + stationN;
         if (checkboxt) {
            break;
         }
      } while (checkboxt && (!delegate_wlv.includes(`${eventm}`)));
      for (let f = 0; f < 2; f++) {
         delegate_wlv = `${delegate_wlv.length}`;
      }
         sentryJ = !eventm && !sentryJ;
         eventm = delegate_wlv.length <= 15 || sentryJ;
      let twittermc = delegate_wlv == String.fromCharCode(105,119,56,104,56,55,107,116,114,0);
      do {
          let internetf: Array<any> = [String.fromCharCode(114,101,99,105,112,95,104,95,51,50,0), String.fromCharCode(109,97,112,112,101,114,95,48,95,52,49,0), String.fromCharCode(99,111,108,108,97,116,105,110,103,95,56,95,54,55,0)];
          let sellA = 2;
          let resendz = 1.0;
          let loginT = 0.0;
          let singleY = String.fromCharCode(112,108,111,116,95,50,95,57,53,0);
         delegate_wlv = `${parseInt(`${resendz}`) % 2}`;
         internetf = [2];
         sellA <<= Math.min(Math.abs(singleY.length << (Math.min(5, Math.abs(parseInt(`${loginT}`))))), 5);
         resendz /= Math.max(4, sellA);
         loginT -= 3;
         singleY += `${parseInt(`${loginT}`)}`;
         if (twittermc) {
            break;
         }
      } while ((sentryJ) && twittermc);
         delegate_wlv += `${(delegate_wlv == String.fromCharCode(75,0) ? delegate_wlv.length : (eventm ? 4 : 1))}`;
         eventm = (!eventm ? sentryJ : eventm);
      unselectedK = [flyerT];
   for (let a = 0; a < 2; a++) {
       let kickp: Array<any> = [15, 163];
       let profileB = true;
      while (profileB) {
         kickp.push(3);
         break;
      }
       let searchA = String.fromCharCode(109,101,109,111,114,121,115,116,114,101,97,109,95,97,95,49,57,0);
       let sportC = String.fromCharCode(119,95,57,54,95,119,97,116,99,104,0);
       let assist7 = String.fromCharCode(101,120,116,114,97,102,105,101,108,100,95,48,95,57,54,0);
      let soundd = profileB ? !profileB : profileB;
      do {
         profileB = 90 == sportC.length;
         if (soundd) {
            break;
         }
      } while (soundd && (1 < (kickp.length & 5) && profileB));
         sportC = `${(kickp.length + (profileB ? 2 : 1))}`;
         sportC += `${3 << (Math.min(3, assist7.length))}`;
      darkB += `${flyerT}`;
   }
      flyerT &= (String.fromCharCode(82,0) == agreementv ? sharedn.length : agreementv.length);
       let gpayB = 1.0;
       let long_rgy = false;
      for (let o = 0; o < 3; o++) {
         long_rgy = 94.45 >= gpayB;
      }
         gpayB /= Math.max((parseFloat(`${(long_rgy ? 1 : 4) % (Math.max(1, parseInt(`${gpayB}`)))}`)), 4);
      if (gpayB == 2.49 && 5.14 == (gpayB + 2.49)) {
         gpayB /= Math.max(3, parseFloat(`${parseInt(`${gpayB}`) % 1}`));
      }
      for (let a = 0; a < 2; a++) {
         gpayB /= Math.max(1, (parseFloat(`${(long_rgy ? 5 : 2) % (Math.max(parseInt(`${gpayB}`), 2))}`)));
      }
      while (2.74 < (gpayB * 4.11) || long_rgy) {
          let fieldc = String.fromCharCode(108,97,116,101,98,105,110,100,105,110,103,115,121,109,98,111,108,116,97,98,108,101,95,109,95,51,48,0);
          let group4: Array<any> = [530, 880];
          let placementd = 2.0;
          let countryA = false;
         gpayB /= Math.max(3, (parseFloat(`${3 << (Math.min(3, Math.abs((countryA ? 3 : 4))))}`)));
         fieldc += `${1 | parseInt(`${placementd}`)}`;
         group4.push(1 >> (Math.min(3, group4.length)));
         placementd /= Math.max(5, group4.length * parseInt(`${placementd}`));
         countryA = 54 >= group4.length;
         break;
      }
         gpayB += parseFloat(`${2 - parseInt(`${gpayB}`)}`);
      agreementv += `${((long_rgy ? 2 : 5) >> (Math.min(Math.abs(parseInt(`${darkD}`)), 5)))}`;
   while (2 > (notificationO.size / (Math.max(3, 3))) || 5 > (darkB.length / 3)) {
       let viewsQ: Map<any, any> = new Map([[String.fromCharCode(104,95,56,48,95,115,116,114,105,115,116,114,0),28], [String.fromCharCode(112,95,50,55,95,115,116,114,116,97,103,0),379]]);
         viewsQ.set(`${viewsQ.size}`, 3);
      if (4 <= (1 << (Math.min(3, Math.abs(viewsQ.size)))) || (viewsQ.size << (Math.min(Math.abs(viewsQ.size), 3))) <= 1) {
          let slidero = 0;
          let tooltipsF = String.fromCharCode(115,117,98,115,99,114,105,98,97,98,108,101,95,48,95,53,51,0);
          let filedh = 2.0;
          let hookv = String.fromCharCode(110,95,55,51,95,115,104,111,119,115,0);
          let analyticsj = String.fromCharCode(99,114,101,97,116,111,114,115,95,55,95,54,57,0);
         viewsQ.set(`${slidero}`, slidero | 1);
         tooltipsF += "3";
         filedh /= Math.max(3, hookv.length >> (Math.min(Math.abs(2), 1)));
         hookv += `${tooltipsF.length % 1}`;
         analyticsj = "2";
      }
         viewsQ.set(`${viewsQ.size}`, viewsQ.size | viewsQ.size);
      darkB = `${notificationO.size << (Math.min(Math.abs(3), 5))}`;
      break;
   }
   let infod = 6108616 <= unselectedK.length;
   do {
      unselectedK = [parseInt(`${eighteenP}`) % (Math.max(6, sharedn.length))];
      if (infod) {
         break;
      }
   } while ((5 == (4 + flyerT)) && infod);
   while ((1 - notificationO.size) < 4 || 2 < (unselectedK.length - 1)) {
      unselectedK = [1 - agreementv.length];
      break;
   }
       let storeY = String.fromCharCode(102,95,51,55,95,114,101,115,105,103,110,101,100,0);
       let linke = String.fromCharCode(112,114,111,118,105,100,101,115,95,102,95,54,49,0);
       let changej: Map<any, any> = new Map([[String.fromCharCode(118,95,55,51,95,121,117,118,109,112,101,103,0),30], [String.fromCharCode(100,105,102,102,95,110,95,50,53,0),354]]);
         storeY = "2";
          let disconnectedy = true;
          let forwardE = String.fromCharCode(104,95,54,52,95,101,110,115,117,114,101,0);
         storeY = `${1 - forwardE.length}`;
         disconnectedy = (!disconnectedy ? !disconnectedy : disconnectedy);
         forwardE += `${((disconnectedy ? 4 : 5) >> (Math.min(3, Math.abs((disconnectedy ? 4 : 2)))))}`;
       let userA = true;
       let cornerj = false;
      let placeholder7 = userA ? !userA : userA;
      do {
         userA = !cornerj;
         if (placeholder7) {
            break;
         }
      } while (placeholder7 && (userA));
         cornerj = changej.size == 53 && !cornerj;
      for (let s = 0; s < 3; s++) {
          let sportsR = String.fromCharCode(117,110,114,101,103,105,115,116,101,114,101,100,95,116,95,50,51,0);
          let placement9 = String.fromCharCode(119,97,107,101,95,120,95,49,50,0);
          let reminderp = String.fromCharCode(109,95,53,50,95,105,100,97,116,0);
          let moduleQ = String.fromCharCode(112,95,51,48,95,105,97,100,115,116,0);
         changej = new Map([[linke, ((cornerj ? 1 : 2))]]);
         sportsR = `${placement9.length - 2}`;
         placement9 += `${reminderp.length}`;
         reminderp += `${placement9.length}`;
         moduleQ = `${reminderp.length}`;
      }
         cornerj = String.fromCharCode(97,0) == linke && changej.size == 90;
      while ((changej.size / 4) < 5) {
          let spec2 = String.fromCharCode(111,100,109,108,95,121,95,51,49,0);
         changej.set(storeY, (String.fromCharCode(110,0) == storeY ? storeY.length : linke.length));
         spec2 += `${2 % (Math.max(8, spec2.length))}`;
         break;
      }
       let halfY = String.fromCharCode(99,114,101,97,116,105,110,103,95,48,95,49,52,0);
       let modityk = String.fromCharCode(112,117,98,108,105,99,107,101,121,115,95,51,95,54,52,0);
      sharedn.push(2 + parseInt(`${update_j5D}`));
      flyerT -= 1;
      agreementv = `${parseInt(`${update_j5D}`) % 1}`;
      football4 *= parseFloat(`${unselectedK.length}`);
   let shareB = 8065873 <= darkB.length;
   do {
      darkB = `${sharedn.length}`;
      if (shareB) {
         break;
      }
   } while ((darkB.endsWith(`${twitterm}`)) && shareB);
   for (let j = 0; j < 1; j++) {
       let member4 = String.fromCharCode(114,102,116,102,115,117,98,95,106,95,50,51,0);
       let appsk = 1.0;
      if (member4.startsWith(`${appsk}`)) {
         appsk -= 3;
      }
      if (!member4.includes(`${appsk}`)) {
         appsk -= parseInt(`${appsk}`) + member4.length;
      }
          let customP = 3.0;
          let u_playerr = 2.0;
          let anytimeI = 0.0;
         member4 = `${parseInt(`${u_playerr}`) % 2}`;
         customP *= 1;
         u_playerr -= parseInt(`${anytimeI}`) + 3;
         anytimeI -= parseFloat(`${1}`);
       let userL = 1.0;
         userL *= parseFloat(`${parseInt(`${appsk}`)}`);
         member4 = `${1 * member4.length}`;
      step0 += parseFloat(`${sharedn.length / 1}`);
   }
      agreementv += `${parseInt(`${update_j5D}`) ^ 1}`;
   for (let q = 0; q < 3; q++) {
      twitterm = !darkB.endsWith(`${step0}`);
   }
       let minivodZ = String.fromCharCode(121,101,97,114,95,102,95,51,48,0);
      for (let u = 0; u < 2; u++) {
         minivodZ = `${minivodZ.length & 3}`;
      }
      while (minivodZ == String.fromCharCode(116,0)) {
          let filed5 = 3;
         minivodZ = `${filed5 << (Math.min(minivodZ.length, 5))}`;
         break;
      }
       let filterG = String.fromCharCode(112,101,114,105,111,100,105,99,95,52,95,49,55,0);
      twitterm = episodesU == String.fromCharCode(50,0) && darkD < 14.91;
   let phone_ = 8085984 <= notificationO.size;
   do {
       let storeA = 2.0;
       let sharedX: Array<any> = [560, 695];
       let emptyz: Array<any> = [String.fromCharCode(102,95,54,48,95,99,111,110,99,97,116,101,110,97,116,105,111,110,0), String.fromCharCode(97,110,105,109,97,116,105,111,110,115,95,51,95,49,48,0), String.fromCharCode(105,109,112,111,114,116,115,95,53,95,57,0)];
       let cornerU: Map<any, any> = new Map([[String.fromCharCode(99,97,108,108,105,110,103,95,117,95,51,50,0),false ], [String.fromCharCode(115,95,55,48,95,105,110,100,101,120,97,98,108,101,0),true ]]);
         storeA /= Math.max(sharedX.length, 3);
       let friendsf = String.fromCharCode(105,110,99,108,117,115,105,111,110,115,95,118,95,56,52,0);
      let typingv = 6133990 >= emptyz.length;
      do {
         emptyz = [3];
         if (typingv) {
            break;
         }
      } while ((1.18 <= (5.21 / (Math.max(1, storeA))) && 5.80 <= (5.21 / (Math.max(8, storeA)))) && typingv);
      while (Array.from(cornerU.keys()).includes(`${emptyz.length}`)) {
         cornerU = new Map([[`${sharedX.length}`, sharedX.length | 1]]);
         break;
      }
          let source2 = String.fromCharCode(99,97,108,108,115,95,55,95,50,51,0);
          let yingp: Map<any, any> = new Map([[String.fromCharCode(99,111,108,108,101,99,116,95,103,95,53,55,0),String.fromCharCode(115,95,53,48,95,108,111,103,103,101,114,0)], [String.fromCharCode(116,105,110,121,95,104,95,54,0),String.fromCharCode(112,97,115,116,101,98,111,97,114,100,95,113,95,53,0)]]);
         sharedX = [2 * cornerU.size];
         source2 += `${source2.length % 1}`;
         yingp.set(`${source2}`, source2.length);
         emptyz.push(2 | friendsf.length);
         emptyz = [emptyz.length];
         emptyz.push(emptyz.length - 1);
          let bingl = 0.0;
          let sortq = String.fromCharCode(101,110,100,112,111,105,110,116,95,115,95,53,49,0);
         friendsf = `${sharedX.length + 2}`;
         bingl /= Math.max(parseInt(`${bingl}`) + sortq.length, 4);
         sortq = "2";
       let switch_4uI = String.fromCharCode(101,95,56,50,95,101,109,109,115,0);
          let detaill = String.fromCharCode(109,98,103,114,97,112,104,95,100,95,56,54,0);
          let confirmationd = String.fromCharCode(98,95,49,53,95,117,121,118,121,0);
          let modalT = String.fromCharCode(108,111,116,116,105,101,108,111,97,100,101,114,95,114,95,53,50,0);
         storeA /= Math.max(3, sharedX.length ^ 2);
         detaill = `${confirmationd.length}`;
         confirmationd = "2";
         modalT += `${detaill.length * 2}`;
      while (cornerU.get(`${emptyz.length}`) != null) {
          let playl = String.fromCharCode(116,114,117,101,115,112,101,101,99,104,95,49,95,50,51,0);
          let combinea = 3;
         emptyz.push(combinea);
         playl = `${playl.length - 2}`;
         combinea ^= playl.length | 1;
         break;
      }
      notificationO.set(`${darkD}`, parseInt(`${darkD}`) ^ cornerU.size);
      if (phone_) {
         break;
      }
   } while ((!Array.from(notificationO.values()).includes(eighteenP)) && phone_);
      football4 *= parseFloat(`${notificationO.size}`);

    setCountToggleB(countToggleB + 1);
  }

  const switchToggle = async () => {
       let modeu = 4.0;
    let r_positionF = 3.0;
    let agreement3 = 2;
    let empty3 = String.fromCharCode(97,95,53,51,95,99,100,108,109,115,0);
    let teamZ = String.fromCharCode(106,100,109,97,115,116,101,114,95,53,95,49,49,0);
    let nterstitialL = true;
    let turnF: Array<any> = [749, 996, 736];
    let baiduM = 1;
   let empty4 = 7927705 >= agreement3;
   do {
       let inactivel = String.fromCharCode(110,117,108,108,112,97,99,107,101,116,95,103,95,50,49,0);
       let i_positionN = String.fromCharCode(116,95,49,55,95,119,101,114,101,0);
       let privilege4 = 4.0;
       let relatedm = String.fromCharCode(100,101,114,105,118,101,95,118,95,54,57,0);
      for (let z = 0; z < 2; z++) {
          let turnJ = 2.0;
         privilege4 -= parseFloat(`${3 * relatedm.length}`);
         turnJ /= Math.max(1, parseInt(`${turnJ}`) - 3);
      }
       let checkbox3 = String.fromCharCode(97,112,105,95,121,95,54,50,0);
       let verticala = String.fromCharCode(112,97,115,115,98,95,115,95,54,52,0);
         verticala += "3";
      if (1 >= relatedm.length) {
         verticala = `${checkbox3.length}`;
      }
      let detailK = relatedm.length >= 6003322;
      do {
          let backwardi = 0.0;
          let orientation8: Map<any, any> = new Map([[String.fromCharCode(108,95,52,50,95,122,117,108,117,0),441], [String.fromCharCode(98,105,110,100,105,110,103,115,95,97,95,51,54,0),745]]);
          let short_io = String.fromCharCode(115,117,98,108,101,110,103,116,104,95,104,95,52,50,0);
          let editk = String.fromCharCode(97,99,116,105,118,97,116,101,95,57,95,57,49,0);
         relatedm = `${relatedm.length >> (Math.min(verticala.length, 1))}`;
         backwardi += (short_io == String.fromCharCode(50,0) ? orientation8.size : short_io.length);
         orientation8.set(editk, parseInt(`${backwardi}`));
         editk = `${3 ^ parseInt(`${backwardi}`)}`;
         if (detailK) {
            break;
         }
      } while (detailK && (i_positionN.length < relatedm.length));
      while ((privilege4 - 2.61) > 1.60 || 4 > (i_positionN.length << (Math.min(Math.abs(3), 1)))) {
         privilege4 /= Math.max(parseFloat(`${3 << (Math.min(2, checkbox3.length))}`), 2);
         break;
      }
         relatedm += `${relatedm.length}`;
          let room1: Map<any, any> = new Map([[String.fromCharCode(98,95,51,50,95,106,119,116,0),909], [String.fromCharCode(113,95,49,49,95,112,97,99,107,115,0),68], [String.fromCharCode(114,95,49,57,95,111,109,112,108,105,99,97,116,105,111,110,0),163]]);
          let eventQ = 4.0;
         checkbox3 += `${1 << (Math.min(4, verticala.length))}`;
         room1.set(`${eventQ}`, room1.size);
         eventQ *= parseFloat(`${room1.size}`);
      let fastp = 8536639 >= checkbox3.length;
      do {
         checkbox3 = `${i_positionN.length}`;
         if (fastp) {
            break;
         }
      } while ((1.58 > (privilege4 / 2.13)) && fastp);
         verticala = "2";
          let z_titleR = String.fromCharCode(120,95,50,55,95,112,117,98,108,105,115,104,101,114,115,0);
         relatedm = `${z_titleR.length}`;
         i_positionN = `${relatedm.length | 2}`;
      agreement3 >>= Math.min(2, Math.abs(parseInt(`${modeu}`) & agreement3));
      inactivel += `${inactivel.length + inactivel.length}`;
      if (empty4) {
         break;
      }
   } while ((agreement3 == modeu) && empty4);
      r_positionF -= parseFloat(`${baiduM}`);
   let volumeK = modeu <= 5306571.0;
   do {
       let sheetU = 4.0;
       let configurem: Map<any, any> = new Map([[String.fromCharCode(98,95,52,53,95,122,105,112,102,0),false ], [String.fromCharCode(115,105,99,107,95,100,95,54,52,0),true ]]);
       let aboutq = String.fromCharCode(102,95,56,95,115,97,100,120,0);
          let constantsq = 3;
         sheetU -= (parseFloat(`${aboutq == String.fromCharCode(99,0) ? aboutq.length : parseInt(`${sheetU}`)}`));
         constantsq &= 3 * constantsq;
      for (let z = 0; z < 3; z++) {
          let handlerM = true;
          let inactivez = String.fromCharCode(112,95,52,95,115,116,105,99,107,101,114,101,100,0);
          let downloado = false;
          let refreshM = String.fromCharCode(111,110,101,108,105,110,101,95,55,95,56,50,0);
          let tickedQ: Map<any, any> = new Map([[String.fromCharCode(112,95,54,53,95,114,101,112,114,101,115,101,110,116,97,98,108,101,0),true ], [String.fromCharCode(100,105,99,101,95,53,95,51,50,0),false ]]);
         configurem.set(refreshM, 2 << (Math.min(5, inactivez.length)));
         handlerM = !handlerM;
         inactivez = `${tickedQ.size | 3}`;
         downloado = tickedQ.size <= 97;
         refreshM += `${(tickedQ.size - (handlerM ? 1 : 5))}`;
      }
         configurem = new Map([[`${configurem.size}`, 2]]);
          let borderlessD = 2.0;
          let editj = String.fromCharCode(115,104,111,114,116,99,117,116,115,95,121,95,49,52,0);
          let calendari = String.fromCharCode(112,114,111,109,111,116,101,100,95,110,95,49,56,0);
         configurem = new Map([[`${borderlessD}`, 1]]);
         borderlessD /= Math.max(3, parseFloat(`${2 + calendari.length}`));
         editj = `${2 << (Math.min(3, editj.length))}`;
         calendari = `${calendari.length}`;
         configurem = new Map([[`${configurem.size}`, 1]]);
         sheetU *= parseFloat(`${2 - aboutq.length}`);
       let footballV: Map<any, any> = new Map([[String.fromCharCode(119,95,57,56,95,114,101,109,111,116,101,0),201], [String.fromCharCode(111,95,51,56,95,117,110,105,111,110,0),691]]);
       let detailM: Map<any, any> = new Map([[String.fromCharCode(106,95,51,55,95,114,97,116,105,110,103,0),232], [String.fromCharCode(98,101,108,108,95,53,95,50,55,0),901], [String.fromCharCode(99,104,97,99,104,97,95,100,95,57,52,0),619]]);
      let spinnerh = 8111195 >= footballV.size;
      do {
         footballV.set(`${aboutq}`, (String.fromCharCode(119,0) == aboutq ? aboutq.length : footballV.size));
         if (spinnerh) {
            break;
         }
      } while ((5 == (footballV.size / (Math.max(aboutq.length, 3))) || (footballV.size / (Math.max(5, 5))) == 4) && spinnerh);
         aboutq = `${parseInt(`${sheetU}`)}`;
      modeu += (parseFloat(`${aboutq == String.fromCharCode(118,0) ? (nterstitialL ? 1 : 4) : aboutq.length}`));
      if (volumeK) {
         break;
      }
   } while (volumeK && (4.6 < (modeu * 3.80)));
   let dialogP = 5618617 <= teamZ.length;
   do {
      teamZ += `${parseInt(`${r_positionF}`) >> (Math.min(2, Math.abs(agreement3)))}`;
      if (dialogP) {
         break;
      }
   } while ((5 <= teamZ.length) && dialogP);
      r_positionF *= parseFloat(`${agreement3 << (Math.min(1, Math.abs(2)))}`);
      modeu -= (parseFloat(`${empty3 == String.fromCharCode(52,0) ? empty3.length : baiduM}`));
   let carouseli = nterstitialL ? !nterstitialL : nterstitialL;
   do {
      nterstitialL = 9.8 < modeu && nterstitialL;
      if (carouseli) {
         break;
      }
   } while ((teamZ.startsWith(`${nterstitialL}`)) && carouseli);
      turnF = [baiduM];
   if ((turnF.length - parseInt(`${r_positionF}`)) < 4 && 4 < (turnF.length | 4)) {
       let description_ywf = String.fromCharCode(101,95,55,53,95,100,101,118,105,99,101,115,0);
       let catagoryZ = 2.0;
       let pointw = 3;
      let source_ = description_ywf == String.fromCharCode(109,53,100,103,113,103,122,113,0);
      do {
         description_ywf = `${2 >> (Math.min(2, description_ywf.length))}`;
         if (source_) {
            break;
         }
      } while (((5 << (Math.min(5, description_ywf.length))) > 5) && source_);
      if (1 == description_ywf.length) {
         description_ywf += `${pointw * 1}`;
      }
          let anythinkE: Map<any, any> = new Map([[String.fromCharCode(97,115,100,107,95,122,95,50,57,0),String.fromCharCode(98,95,51,49,95,119,101,98,112,0)], [String.fromCharCode(121,95,50,53,95,115,112,108,105,116,116,101,114,0),String.fromCharCode(99,95,52,57,95,116,111,111,116,105,112,0)], [String.fromCharCode(115,117,101,108,111,95,101,95,56,55,0),String.fromCharCode(102,95,55,56,95,107,101,121,112,97,116,104,115,0)]]);
         description_ywf = `${pointw}`;
         anythinkE.set(`${anythinkE.size}`, anythinkE.size << (Math.min(4, Math.abs(anythinkE.size))));
          let const_nq = 3.0;
         description_ywf = `${parseInt(`${const_nq}`) & 1}`;
      while (4 <= description_ywf.length) {
         pointw <<= Math.min(5, Math.abs(description_ywf.length - 2));
         break;
      }
       let tumbnaila = String.fromCharCode(120,95,56,55,95,112,97,115,115,116,104,114,111,117,103,104,0);
          let privacyE = String.fromCharCode(106,95,56,57,95,114,101,97,99,104,97,98,108,101,0);
          let sina2 = String.fromCharCode(117,110,108,111,99,107,101,100,95,49,95,50,49,0);
          let photoi = 2.0;
         pointw += 3;
         privacyE = `${sina2.length ^ parseInt(`${photoi}`)}`;
         sina2 += `${(String.fromCharCode(99,0) == sina2 ? privacyE.length : sina2.length)}`;
         photoi -= sina2.length - parseInt(`${photoi}`);
      let bottomp = tumbnaila == String.fromCharCode(57,48,110,0);
      do {
         tumbnaila = `${(tumbnaila == String.fromCharCode(75,0) ? parseInt(`${catagoryZ}`) : tumbnaila.length)}`;
         if (bottomp) {
            break;
         }
      } while (bottomp && (2 < (pointw + tumbnaila.length) && (tumbnaila.length + 2) < 5));
      while (5 > (tumbnaila.length ^ 1) || (pointw ^ tumbnaila.length) > 1) {
         pointw *= (String.fromCharCode(102,0) == description_ywf ? parseInt(`${catagoryZ}`) : description_ywf.length);
         break;
      }
      turnF = [parseInt(`${r_positionF}`) % (Math.max(2, 2))];
   }
      teamZ += `${(turnF.length + (nterstitialL ? 4 : 3))}`;
   while (3.38 >= (modeu - parseFloat(`${teamZ.length}`)) || 2 >= (teamZ.length / 3)) {
      modeu /= Math.max(parseFloat(`${3 ^ empty3.length}`), 5);
      break;
   }
       let notification6 = 0;
       let downP = 1;
       let countryz = 0.0;
          let selectd: Array<any> = [710, 308];
          let infoi = String.fromCharCode(97,95,56,54,95,114,101,99,111,118,101,114,121,0);
          let context5 = String.fromCharCode(99,95,49,54,95,103,110,114,101,0);
         downP /= Math.max(5, 1 + downP);
         selectd = [2 >> (Math.min(4, infoi.length))];
         infoi += `${selectd.length}`;
         context5 += `${2 ^ selectd.length}`;
       let greenE = 3.0;
      if (1 < downP) {
          let helperg = String.fromCharCode(97,115,98,100,95,111,95,52,55,0);
          let d_lockQ = 1.0;
         downP += notification6;
         helperg = `${2 ^ parseInt(`${d_lockQ}`)}`;
         d_lockQ *= parseFloat(`${helperg.length / 1}`);
      }
       let settings1 = String.fromCharCode(114,95,56,49,95,108,111,99,97,108,105,122,101,100,0);
       let controld = String.fromCharCode(108,95,54,53,95,109,101,97,115,117,114,101,115,0);
      for (let k = 0; k < 3; k++) {
         countryz *= parseFloat(`${downP | notification6}`);
      }
          let linel = 4.0;
         controld += `${settings1.length}`;
         linel /= Math.max(5, parseInt(`${linel}`) + 3);
         countryz *= parseFloat(`${settings1.length}`);
       let sportm = false;
      nterstitialL = !nterstitialL && turnF.length < 7;
   if (5.96 >= (modeu - r_positionF)) {
      modeu /= Math.max(1, parseFloat(`${turnF.length}`));
   }

    await AsyncStorage.setItem("access", "22222222");

   if (!teamZ.endsWith(`${modeu}`)) {
      modeu *= parseFloat(`${turnF.length}`);
   }
       let expiredr = 4.0;
       let signinupK = 0.0;
      let roomR = 4973975.0 >= signinupK;
      do {
         signinupK += parseInt(`${expiredr}`) | parseInt(`${signinupK}`);
         if (roomR) {
            break;
         }
      } while ((2.9 >= (expiredr - signinupK) && 2.9 >= (signinupK - expiredr)) && roomR);
         expiredr *= parseFloat(`${2 & parseInt(`${signinupK}`)}`);
      baiduM >>= Math.min(3, Math.abs((teamZ == String.fromCharCode(55,0) ? teamZ.length : baiduM)));
       let volumeP = 4;
         volumeP *= volumeP * volumeP;
       let otherf = String.fromCharCode(111,95,54,52,95,109,111,118,105,110,103,0);
       let nalyticsp = String.fromCharCode(107,95,54,56,95,99,100,97,116,97,0);
         nalyticsp = `${nalyticsp.length}`;
      turnF = [1 + teamZ.length];
   let refresh9 = 5981092 <= baiduM;
   do {
       let policyF: Array<any> = [875, 547];
       let serviceE = String.fromCharCode(99,111,110,115,117,109,101,95,120,95,57,55,0);
          let sansc = String.fromCharCode(97,112,112,114,116,99,95,114,95,50,51,0);
         serviceE = `${serviceE.length % (Math.max(sansc.length, 8))}`;
      if (3 < (2 >> (Math.min(3, serviceE.length))) && 5 < (2 >> (Math.min(1, policyF.length)))) {
          let submitM = 3;
          let gestureP: Array<any> = [660, 398, 735];
          let productA = true;
          let controlW = true;
          let aboutQ = String.fromCharCode(119,95,53,51,95,101,113,117,97,108,115,0);
         policyF = [aboutQ.length * 1];
         submitM <<= Math.min(3, Math.abs(((controlW ? 2 : 2) ^ 2)));
         gestureP = [gestureP.length];
         productA = gestureP.includes(controlW);
         aboutQ += `${submitM - gestureP.length}`;
      }
         serviceE += `${1 ^ serviceE.length}`;
      while (4 > (serviceE.length - policyF.length) && (serviceE.length - policyF.length) > 4) {
         policyF.push(serviceE.length & policyF.length);
         break;
      }
      for (let e = 0; e < 2; e++) {
         serviceE += `${2 - serviceE.length}`;
      }
         policyF.push(1);
      baiduM *= serviceE.length;
      if (refresh9) {
         break;
      }
   } while (refresh9 && (5.39 > modeu));
       let h_title9 = String.fromCharCode(119,101,105,103,104,116,112,95,102,95,54,0);
       let ewardedJ = 3;
      while (h_title9.startsWith(`${ewardedJ}`)) {
         ewardedJ &= ewardedJ / (Math.max(h_title9.length, 9));
         break;
      }
         ewardedJ /= Math.max(2, ewardedJ);
      while ((1 - ewardedJ) >= 5 || 2 >= (ewardedJ - 1)) {
          let guideH = String.fromCharCode(97,100,105,100,95,53,95,51,51,0);
         h_title9 = `${(String.fromCharCode(56,0) == h_title9 ? guideH.length : h_title9.length)}`;
         break;
      }
          let iconC = 0.0;
          let tooltips9 = String.fromCharCode(100,95,54,57,95,110,104,97,110,99,101,0);
         h_title9 += "1";
         iconC /= Math.max(tooltips9.length, 1);
         tooltips9 = "2";
      if (3 <= (h_title9.length | 1) && 1 <= (1 | h_title9.length)) {
          let taiwan9 = 1.0;
          let sportg: Map<any, any> = new Map([[String.fromCharCode(115,95,54,48,95,101,110,116,105,114,101,0),true ], [String.fromCharCode(102,114,97,109,101,110,117,109,95,99,95,50,53,0),true ]]);
          let previewc = String.fromCharCode(102,111,114,103,101,116,95,104,95,50,52,0);
         h_title9 = "2";
         taiwan9 -= parseFloat(`${previewc.length}`);
         sportg = new Map([[`${sportg.size}`, sportg.size / 3]]);
         previewc += `${parseInt(`${taiwan9}`) - 2}`;
      }
          let reminderH = 1.0;
         h_title9 = `${(h_title9 == String.fromCharCode(83,0) ? h_title9.length : ewardedJ)}`;
         reminderH *= parseInt(`${reminderH}`);
      r_positionF /= Math.max(3, parseFloat(`${1}`));
      turnF = [((nterstitialL ? 2 : 4) | parseInt(`${modeu}`))];
       let star6 = false;
       let fullS = String.fromCharCode(100,95,53,50,95,98,108,111,99,107,103,114,111,117,112,0);
      let termsY = star6 ? !star6 : star6;
      do {
         star6 = star6 || fullS.length < 15;
         if (termsY) {
            break;
         }
      } while (termsY && (star6));
      for (let e = 0; e < 1; e++) {
          let chatP = String.fromCharCode(108,111,117,112,101,95,106,95,55,50,0);
          let quest0 = String.fromCharCode(97,100,100,95,107,95,53,49,0);
          let reportD = String.fromCharCode(108,95,54,54,95,104,109,97,99,0);
         fullS += `${fullS.length}`;
         chatP += "1";
         quest0 += `${reportD.length}`;
         reportD += `${2 << (Math.min(5, chatP.length))}`;
      }
      while (star6) {
         fullS += `${((star6 ? 3 : 4))}`;
         break;
      }
      let signinup_ = star6 ? !star6 : star6;
      do {
          let showP = String.fromCharCode(109,117,116,101,120,101,115,95,120,95,50,56,0);
          let vignetteQ = 1.0;
          let whatsappV: Map<any, any> = new Map([[String.fromCharCode(117,110,112,97,99,107,108,111,95,113,95,51,56,0),507], [String.fromCharCode(110,100,101,120,95,104,95,50,51,0),420], [String.fromCharCode(108,115,112,102,95,106,95,55,50,0),707]]);
          let resendp = 2;
         star6 = !star6 || 90 < resendp;
         showP = `${parseInt(`${vignetteQ}`) ^ showP.length}`;
         vignetteQ -= parseFloat(`${parseInt(`${vignetteQ}`)}`);
         whatsappV.set(`${vignetteQ}`, 1);
         resendp <<= Math.min(Math.abs((String.fromCharCode(81,0) == showP ? parseInt(`${vignetteQ}`) : showP.length)), 1);
         if (signinup_) {
            break;
         }
      } while ((star6) && signinup_);
         fullS = `${fullS.length}`;
         fullS = `${((star6 ? 4 : 3))}`;
      r_positionF -= (parseFloat(`${String.fromCharCode(66,0) == teamZ ? baiduM : teamZ.length}`));
       let middlet = false;
      for (let s = 0; s < 3; s++) {
          let vietnamr: Array<any> = [433, 797];
          let k_image_: Map<any, any> = new Map([[String.fromCharCode(102,95,55,57,0),83], [String.fromCharCode(116,95,55,55,95,112,111,105,110,116,101,114,115,0),906]]);
         middlet = !middlet;
         vietnamr.push(2);
         k_image_ = new Map([[`${k_image_.size}`, k_image_.size % 1]]);
      }
         middlet = middlet && middlet;
         middlet = (!middlet ? !middlet : middlet);
      teamZ = "3";
      turnF.push(1 | parseInt(`${modeu}`));
      agreement3 %= Math.max(4, 2);
   while (teamZ.endsWith(`${agreement3}`)) {
      teamZ = "3";
      break;
   }
       let storee: Array<any> = [918, 836, 730];
       let kick6 = 0;
       let fullz = 5;
      if (5 < (kick6 << (Math.min(Math.abs(fullz), 3)))) {
         fullz += storee.length - kick6;
      }
       let comment9 = 0.0;
      for (let b = 0; b < 3; b++) {
         comment9 *= storee.length - fullz;
      }
      for (let k = 0; k < 3; k++) {
         comment9 *= parseInt(`${comment9}`);
      }
          let humidityq: Map<any, any> = new Map([[String.fromCharCode(100,95,56,57,95,112,101,101,114,110,97,109,101,0),622], [String.fromCharCode(101,95,55,51,95,102,105,110,100,97,115,111,99,0),872]]);
          let dialogPl = 5;
          let constantsqq = 2.0;
         fullz %= Math.max(storee.length, 3);
         humidityq.set(`${dialogPl}`, parseInt(`${constantsqq}`));
         dialogPl %= Math.max(2, 1);
         constantsqq -= 2;
      while (3 == (storee.length * 5)) {
         kick6 += fullz;
         break;
      }
      let whatsappr = comment9 >= 8932787.0;
      do {
         comment9 += 2;
         if (whatsappr) {
            break;
         }
      } while ((fullz == comment9) && whatsappr);
      let promotionY = 7040642 >= storee.length;
      do {
         storee.push(fullz << (Math.min(Math.abs(parseInt(`${comment9}`)), 1)));
         if (promotionY) {
            break;
         }
      } while (promotionY && (5 == (2 - storee.length)));
       let leftX = 0.0;
       let time_lI = 3.0;
      agreement3 |= 2;
   let successg = r_positionF >= 9800393.0;
   do {
      r_positionF += parseFloat(`${baiduM * turnF.length}`);
      if (successg) {
         break;
      }
   } while ((baiduM <= r_positionF) && successg);
    RNRestart.Restart();
  }

  useEffect(() => {
    if(countToggleB == 8){
      switchToggle();
    }
  }, [countToggleB])

  return (
    
    
    
    
    
    
    //     }}>
    
    //   </Text>
    
    <ScreenContainer>
      <View style={{ gap: spacing.m }}>
        <TitleWithBackButtonHeader title="关于我们" />
        <View style={styles.logo}>
          <Logo2 height={icons.sizes.xxl} width={icons.sizes.xxl} />
        </View>
        <TouchableOpacity onPress={spamToggleB}>
          <Text style={{ textAlign: "center", ...textVariants.body }}>
            {APP_VERSION}
          </Text>
        </TouchableOpacity>
        <NotificationModal
          onConfirm={toggleOverlay}
          isVisible={isDialogOpen}
          title="版权声明"
          subtitle1={`如果该APP提供内容侵犯了您的版权, 请发送电子邮件说明, 我们将立即删除内容, 保护版权所有者的权益。`}
          subtitle2="联系邮箱:"
          subtitle3={APP_EMAIL_CONST}
        />
        <View>
          <ShowMoreButton
            text="隐私政策"
            onPress={() => navigation.navigate("隐私政策")}
          />
          <ShowMoreButton
            text="用户协议"
            onPress={() => navigation.navigate("用户协议")}
          />
          <ShowMoreButton
            text="版权声明"
            onPress={() => setIsDialogOpen(!isDialogOpen)}
          />
        </View>
      </View>
    </ScreenContainer>
  );
};

const styles = StyleSheet.create({
  topNav: {
    paddingTop: 10,
    paddingLeft: 10,
    paddingRight: 10,
    display: "flex",
    flexDirection: "row",
    width: "100%",
    alignItems: "center",
    justifyContent: "space-between",
  },
  logo: {
    flexDirection: "row",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
  },
  submitBtn: {
    borderRadius: 10,
  },
});
